---
title: 评估-优化：什么时候该先出稿，再迭代打磨
type: D
section: 智能体模式
order: 13
description: 看懂一种“先写一版，再按标准返工”的工作流：什么时候单次出稿就够，什么时候值得加一轮评估-优化。
demo: true
math: false
draft: false
sourceUrl: "https://www.anthropic.com/engineering/building-effective-agents"
---

import { CodeTabs, Tab } from "@/components/mdx/code-tabs";
import { CompareSlider } from "@/components/mdx/compare-slider";


## 为什么只记结论不足以掌握评估-优化：什么时候该先出稿，再迭代打磨

学习“评估-优化：什么时候该先出稿，再迭代打磨”时，第一步不是记住结论，而是冻结输入、上下文、版本和成功标准。只有这些条件明确，评估-优化：什么时候该先出稿，再迭代打磨的含义才不会随着样例变化。正文已有的概念说明要与结构图、运行轨迹和失败样本互相印证，不能只凭最终输出看似正确就宣布完成。

结构分析从先打个比方开始：列出参与者、职责、连接方向、生命周期和所有权，再沿正常路径追踪数据或控制流。每一条边都要说明为什么存在、谁创建、谁消费、谁负责清理；如果边界被跨越，必须能在证据中找到第一处异常。

机制验证要把这种模式在做什么：先出一版，再按标准返工写成可以执行的条件。正常样本证明主路径，恰好边界样本验证等号和空值，单故障样本只破坏一个假设。三类样本使用同一份观察指标，避免因为测试口径变化而把偶然结果误认成规律。

成本分析同时记录时间、空间、延迟、耦合、可维护性和不可逆操作。它仍是工作流，不是自主 agent不是一句“可能失败”，而是可复现输入、预期停点、实际轨迹、错误分类与清理步骤。任何自动重试都要有次数、预算和幂等边界。

方案比较不能只列优点。需要给出直接实现、当前方案和至少一个替代方案，逐项比较复杂度、扩展点、故障隔离和团队认知成本。当问题规模很小或变化轴稳定时，更简单的实现往往更好；模式与框架必须由真实变化压力证明。

实现阶段把大结论拆成可检查的中间产物：配置快照、结构清单、状态转移、输入输出样本、日志摘要和测试结果。每个产物带来源与生成命令，下一阶段只消费已通过门禁的版本，避免旧缓存或隐式默认值污染结论。

解释结果时必须区分相关性与因果性、接口承诺与实现细节、设计意图与运行事实。对“两个适配信号：标准清楚 + 返工真能变好”的判断要由独立证据支持，并明确适用范围；一旦输入分布、版本、硬件或组织边界改变，就重新运行最小实验。

复盘从首个分叉开始，而不是从最后一个报错倒推。先比较冻结输入，再比较第一份结构化中间产物，随后检查状态、约束和副作用。这样可以把复杂系统的排错范围收缩到一个阶段，避免在多个层次同时修改造成新的不确定性。

迁移到真实项目时，先选择一个最小但有代表性的切片，保存改造前基线，再逐步引入“评估-优化：什么时候该先出稿，再迭代打磨”中的机制。每一步只改变一个变量并保留回滚点；性能、正确性、安全性和可理解性至少各有一项可量化指标。

最终验收要求读者能脱离页面重新画出结构、口述关键链路、实现最小版本、构造一个反例并解释失败位置。若只能复述名词而不能预测中间状态，说明知识仍停留在识记层，需要回到图示和实验重新验证。

本页用、

、、、建立统一坐标。先预测这些概念在结构图和运行轨迹中的位置，再操作实验控件；如果结果与预测不一致，停止在首个分叉，不要用后续补丁掩盖早期错误。

## 权威目录与核心概念逐项对照

- 评估-优化：什么时候该先出稿，再迭代打磨
- 先打个比方
- 这种模式在做什么：先出一版，再按标准返工
- 它仍是工作流，不是自主 agent
- 两个适配信号：标准清楚 + 返工真能变好
- 信号一：标准清楚，能说出“好稿子长什么样”
- 信号二：返工真的能让结果变好，而不是只会重写
- 动手看：这个任务该不该上评估-优化？


## 可复现的最小实现

先把决策记录写成机器可读结构：

```json
{
  "unit": "评估-优化：什么时候该先出稿，再迭代打磨",
  "inputFrozen": true,
  "scenario": "normal | boundary | single-fault",
  "firstDivergence": null,
  "cleanupRequired": true
}
```

再用同一条执行链处理三类样本：

```ts
type Evidence = { stage: string; expected: string; actual: string };

function verify(sample: unknown, expected: readonly Evidence[]) {
  const trace = runFromCleanState(sample);
  return expected.find((item, index) => trace[index]?.actual !== item.expected);
}
```

最后保存回归门禁，禁止失败样本静默通过：

```text
normal      -> complete, invariant preserved
boundary    -> complete or explicit rejection
singleFault -> stop at first divergence, no stale output
```

用统一评分解释实验结果：

$$
Q = C_{correct} + C_{trace} + C_{recover} - R_{residual}
$$

$$
C_{coverage} = \frac{N_{verified}}{N_{official}}
$$

$$
R_{residual} = P(failure) \times I(impact)
$$

$$
Accept = (C_{coverage} \ge 0.90) \land (R_{residual} \le R_{budget})
$$

## 本章回顾

- 评估-优化：什么时候该先出稿，再迭代打磨必须绑定冻结输入和明确成功标准。
- 先打个比方必须能画成结构并沿边追踪责任。
- 这种模式在做什么：先出一版，再按标准返工要由正常、边界和单故障样本共同验证。
- 它仍是工作流，不是自主 agent必须保存第一处偏离和清理重建步骤。
- 两个适配信号：标准清楚 + 返工真能变好决定方案是否可以进入下一阶段。


## 术语表


{/* 知识点清单：1. 评估-优化（图：）2. 评估标准 rubric（图：）3. 评估器 evaluator（图：）4. 优化器 optimizer（代码对照：英文产品更新说明 -> 中文发布稿）5. 常见误区：标准太虚 / 不可改善任务硬上循环 / 只改文风不校事实（图：）。Term↔Glossary：评估-优化 / 评估标准 / 评估器 / 优化器。 */}


## 先打个比方

写一封对外发布的通知，有时像聊天回个消息：意思讲明白就行，一遍写完直接发，不值得来回推敲。

但也有些稿子不是这样。比如要发给客户、媒体或全公司，第一版往往只是“东西都在”，还没到“拿得出手”。这时最稳的做法不是重头再想一遍，而是先按几个明确标准挑毛病，再针对这些毛病返工。

这一章讲的，就是这种“先交第一版，再按清单打磨”的做法。它解决的问题不是“怎么从零想出来”，而是“第一版已经有了，怎样更稳地把它修到达标”。如果少了这一步，你常会得到一个大体对、细节却不够发布的半成品。

## 这种模式在做什么：先出一版，再按标准返工

当任务不是“有没有答案”，而是“这份答案够不够好”时，就很适合上。

它的最小骨架只有三步：先产出初稿，再拿一把清楚的尺子去检查，最后按检查结果返工。这里最关键的不是“多跑几轮”，而是中间那把尺子，也就是。没有它，返工就会变成“感觉不太好，再来一版看看”，很容易越改越漂。


看图时抓住一句话：**先有草稿，再按标准返工。** 它不要求第一轮就完美，而是把“写得更好”拆成一个可重复的动作：先暴露问题，再定向修补。Anthropic 在《Building Effective Agents》里把它放在工作流光谱中偏右的位置，因为它比普通链式更会利用反馈，但仍然不是开放式自主 agent。

## 它仍是工作流，不是自主 agent

这一步很容易误判。评估-优化看起来像“会自己反思”，但它本质上还是工作流，因为循环骨架是人预先写死的：

- 先写初稿
- 再按固定标准评估
- 再按评估意见返工
- 通过就停，不通过就最多再来几轮

其中真正新出现的是 和 这两个槽位；但“先谁后谁、何时停止、最多几轮”都还是你在代码里提前规定好的。模型没有在开放环境里自己改目标、自己找工具、自己决定任务边界，所以它仍属于工作流，而不是自主 agent。


把它和自主 agent 区分开，最简单的问法是：**系统是在固定骨架里返工，还是在开放世界里自己决定下一步要做什么？** 前者是评估-优化，后者才更像 agent。别因为它带循环，就自动把它判成“已经自主”。

## 两个适配信号：标准清楚 + 返工真能变好

不是每个任务都值得加这一轮。评估-优化最适合的，是下面两个信号同时成立的时候。

### 信号一：标准清楚，能说出“好稿子长什么样”

第一条是：你能把“好”和“不好”的差别写成相对稳定的检查项。比如中文发布稿是否保留关键更新点、语气是否像正式公告、术语是否统一、是否避免夸大承诺，这些都能写进 rubric。标准越清楚，评估器越像在打样稿，而不是在猜老板心情。

如果你自己都说不清“到底怎样算更好”，评估器就只能给出含糊反馈，比如“再自然一点”“更高级一点”。这种反馈很难驱动稳定返工，循环通常只会增加调用次数，不会稳定提质。

### 信号二：返工真的能让结果变好，而不是只会重写

第二条是：任务本身允许“看完点评再改一版”带来实质提升。文案润色、摘要打磨、代码补注释、报告压结构，这类任务都常见“第一版够了七成，第二版更能打”的情况。

但如果任务主要瓶颈不在表达，而在“根本没拿到正确事实”“缺关键输入”“答案对错一翻两瞪眼”，那多跑几轮润色往往没用。评估器可以指出“这个数字没依据”，可如果系统没有新数据源，优化器也只能围着旧错误打转。**评估-优化擅长的是迭代表达和结构，不是凭空补真相。**

## 动手看：这个任务该不该上评估-优化？

上面两条记住后，真正难的是遇到具体任务时怎么判断。下面这张图把选择过程压成了两个判断：先看标准清不清，再看返工是否真能提高质量。两问都答“是”，才值得把任务送进评估-优化循环。

> 猜一猜：如果任务是“把英文产品更新说明改成可直接发给中国用户的正式发布稿”，你会怎样回答这两问？那如果任务只是“从数据库里取最新版本号并拼成一句通知”呢？


把这张图记成一句工程判断：**不是输出重要就上评估-优化，而是“有明确尺子，而且改一轮真的会更好”才上。** 如果标准本来就很模糊，或者任务本质上只是查对事实、拼对字段，那一轮生成加程序校验通常更省、更稳。

## 代码对照：一次出稿写法 vs 评估-优化循环

下面都用同一个题材：把一段英文产品更新说明改写成中文发布稿。我们关心的不是翻译 API 怎么调，而是结构有没有多出“评估”和“返工”这两步。

先看输入任务长什么样：

> 英文原稿：`We redesigned the dashboard, added CSV export, and reduced report generation time by 40%. The rollout starts next Monday for Pro users.`

目标输出不是逐字直译，而是一段能发给中国用户的正式发布稿：信息完整、语气稳、术语统一，不夸大承诺。

### 第一步：一次出稿写法，最短也最便宜

最直接的写法，就是把要求一次性写进 prompt，让模型直接交付终稿。如果这类稿子本来就短、标准也不严，一轮往往已经够用。

<CodeTabs>
  <Tab label="单次出稿：一轮直接交付">

```ts
const ENGLISH_UPDATE = `
We redesigned the dashboard, added CSV export,
and reduced report generation time by 40%.
The rollout starts next Monday for Pro users.
`;

async function writeReleaseOnce() {
  return callModel(`
把这段英文产品更新说明改写成正式中文发布稿。
要求：保留关键事实，语气稳重，适合发给用户。
原文：${ENGLISH_UPDATE}
`);
}
```

  </Tab>
  <Tab label="评估-优化：先允许有一版初稿">

```ts
const ENGLISH_UPDATE = `
We redesigned the dashboard, added CSV export,
and reduced report generation time by 40%.
The rollout starts next Monday for Pro users.
`;

async function draftRelease() {
  return callModel(`
先写中文发布稿初稿，优先把事实说全。
原文：${ENGLISH_UPDATE}
`);
}
```

  </Tab>
</CodeTabs>

左边的核心是假设“这一轮就该交终稿”；右边则刻意把第一轮降格成“先有一版能评的草稿”。这一步的心态差别很重要：评估-优化不是一开始就要求模型一次命中，而是接受初稿不完美，但要求后面有清楚的打磨路径。

### 第二步：加评估器，让“哪里不好”有统一说法

一旦进入循环，最值钱的就是统一标准。评估器不是简单说“再自然一点”，而是按 rubric 拆解：事实是否完整、受众语气是否合适、术语是否一致、是否把上线范围说清楚。

<CodeTabs>
  <Tab label="单次出稿：没有显式评估环节">

```ts
async function publishDirectly() {
  const release = await writeReleaseOnce();
  return release;
}
```

  </Tab>
  <Tab label="评估-优化：先评，再决定是否返工">

```ts
async function evaluateRelease(draft: string) {
  return callModel(`
按 rubric 评估这版中文发布稿：
1. 关键事实是否都保留
2. 语气是否像正式发布公告
3. 术语是否统一，不夸大承诺
4. 是否明确上线时间与适用用户
输出：pass/fail、问题列表、修改建议
待评估文本：${draft}
`);
}
```

  </Tab>
</CodeTabs>

这一步把“好不好”从主观感觉，变成了相对稳定的检查清单。没有显式评估时，你只能指望首轮 prompt 足够强；有了评估器，你就能知道这版稿子到底差在漏事实、文风不稳，还是发布范围没说清。


### 第三步：把评语喂给优化器，再加一道停机条件

最后才是返工。优化器不是重新从零写一篇，而是拿着“这版哪里没过”去定向修正；同时你要给循环一条明确的停止线，比如通过即停，或最多改两轮。

<CodeTabs>
  <Tab label="单次出稿：没有返工闭环">

```ts
async function shipOnce() {
  const release = await writeReleaseOnce();
  return { status: "done", release };
}
```

  </Tab>
  <Tab label="评估-优化：评语驱动的返工循环">

```ts
async function writeReleaseWithLoop(maxRounds = 2) {
  let draft = await draftRelease();
  for (let round = 0; round < maxRounds; round += 1) {
    const review = await evaluateRelease(draft);
    if (review.pass) return { status: "pass", release: draft };
    draft = await callModel(`
按这些反馈重写中文发布稿：
${review.feedback}
当前稿：${draft}
`);
  }
  return { status: "max-rounds", release: draft };
}
```

  </Tab>
</CodeTabs>

左边的结束条件很简单：模型一写完就交付。右边则多了一个明确闭环：先评、再改、通过就停、超轮次也得停。这样做的收益是，你不必把所有要求都压到首轮 prompt 里，而是允许系统边检查边收敛；代价是调用次数、时延和控制逻辑都会增加。

把三段代码看完后，再用一眼图压缩记忆，会更容易把差别记牢：

<CompareSlider
  leftLabel="单次出稿"
  rightLabel="评估-优化循环"
  initial={48}
  left={
    <div className="not-prose flex h-full flex-col justify-center gap-3 rounded-card border border-border bg-bg p-5">
      <p className="text-sm font-bold text-primary">⚡ 一次出稿的关键特征</p>
      <ul className="m-0 list-disc pl-5 text-xs leading-relaxed text-primary">
        <li>首轮 prompt 直接瞄准终稿</li>
        <li>没有显式评估和返工步骤</li>
        <li>更便宜、更快，适合标准不严的短任务</li>
      </ul>
      <p className="text-xs text-secondary">
        适合“写出来就能用”的任务，不适合高要求外发稿件的细修。
      </p>
    </div>
  }
  right={
    <div className="not-prose flex h-full flex-col justify-center gap-3 rounded-card border border-accent bg-bg p-5">
      <p className="text-sm font-bold text-accent">🛠️ 评估-优化的关键特征</p>
      <ul className="m-0 list-disc pl-5 text-xs leading-relaxed text-primary">
        <li>允许先产出一版能评的初稿</li>
        <li>用 rubric 找问题，再定向返工</li>
        <li>必须有停机条件，避免无限打磨</li>
      </ul>
      <p className="text-xs text-secondary">
        适合“第一版常常只够七成，但改一轮会明显更好”的任务。
      </p>
    </div>
  }
/>

## 容易踩的坑


## 小结

- 评估-优化 = 先出稿，再按 rubric 返工
- 它仍是工作流，骨架和停机条件都由人写死
- 值得上它，要同时满足“标准清楚 + 返工真能变好”
- 它擅长打磨表达与结构，不擅长凭空补事实
- 一定要写明通过条件和轮次上限


## 名词解释


