---
title: 并行与编排-工作者：什么时候同时做，什么时候先拆再派
type: D
section: 智能体模式
order: 12
description: 看懂两种“多人协作”工作流：什么时候把固定块同时跑，什么时候先让编排者读任务再临场拆活。
demo: true
math: false
draft: false
sourceUrl: "https://www.anthropic.com/engineering/building-effective-agents"
---

import { CodeTabs, Tab } from "@/components/mdx/code-tabs";
import { CompareSlider } from "@/components/mdx/compare-slider";


## 为什么只记结论不足以掌握并行与编排-工作者：什么时候同时做，什么时候先拆再派

学习“并行与编排-工作者：什么时候同时做，什么时候先拆再派”时，第一步不是记住结论，而是冻结输入、上下文、版本和成功标准。只有这些条件明确，并行与编排-工作者：什么时候同时做，什么时候先拆再派的含义才不会随着样例变化。正文已有的概念说明要与结构图、运行轨迹和失败样本互相印证，不能只凭最终输出看似正确就宣布完成。

结构分析从先打个比方开始：列出参与者、职责、连接方向、生命周期和所有权，再沿正常路径追踪数据或控制流。每一条边都要说明为什么存在、谁创建、谁消费、谁负责清理；如果边界被跨越，必须能在证据中找到第一处异常。

机制验证要把并行：同一批活，预先分块后一起做写成可以执行的条件。正常样本证明主路径，恰好边界样本验证等号和空值，单故障样本只破坏一个假设。三类样本使用同一份观察指标，避免因为测试口径变化而把偶然结果误认成规律。

成本分析同时记录时间、空间、延迟、耦合、可维护性和不可逆操作。并行不只一种：分片（sectioning）和投票（voting）不是一句“可能失败”，而是可复现输入、预期停点、实际轨迹、错误分类与清理步骤。任何自动重试都要有次数、预算和幂等边界。

方案比较不能只列优点。需要给出直接实现、当前方案和至少一个替代方案，逐项比较复杂度、扩展点、故障隔离和团队认知成本。当问题规模很小或变化轴稳定时，更简单的实现往往更好；模式与框架必须由真实变化压力证明。

实现阶段把大结论拆成可检查的中间产物：配置快照、结构清单、状态转移、输入输出样本、日志摘要和测试结果。每个产物带来源与生成命令，下一阶段只消费已通过门禁的版本，避免旧缓存或隐式默认值污染结论。

解释结果时必须区分相关性与因果性、接口承诺与实现细节、设计意图与运行事实。对“编排-工作者：先看任务，再临场拆活”的判断要由独立证据支持，并明确适用范围；一旦输入分布、版本、硬件或组织边界改变，就重新运行最小实验。

复盘从首个分叉开始，而不是从最后一个报错倒推。先比较冻结输入，再比较第一份结构化中间产物，随后检查状态、约束和副作用。这样可以把复杂系统的排错范围收缩到一个阶段，避免在多个层次同时修改造成新的不确定性。

迁移到真实项目时，先选择一个最小但有代表性的切片，保存改造前基线，再逐步引入“并行与编排-工作者：什么时候同时做，什么时候先拆再派”中的机制。每一步只改变一个变量并保留回滚点；性能、正确性、安全性和可理解性至少各有一项可量化指标。

最终验收要求读者能脱离页面重新画出结构、口述关键链路、实现最小版本、构造一个反例并解释失败位置。若只能复述名词而不能预测中间状态，说明知识仍停留在识记层，需要回到图示和实验重新验证。

本页用、

、、、建立统一坐标。先预测这些概念在结构图和运行轨迹中的位置，再操作实验控件；如果结果与预测不一致，停止在首个分叉，不要用后续补丁掩盖早期错误。

## 权威目录与核心概念逐项对照

- 并行与编排-工作者：什么时候同时做，什么时候先拆再派
- 先打个比方
- 并行：同一批活，预先分块后一起做
- 并行不只一种：分片（sectioning）和投票（voting）
- 编排-工作者：先看任务，再临场拆活
- 它们都像“多人一起干”，但差别在子任务是不是预先写死
- 动手看：该预先并行，还是让编排者临场拆活？
- 代码对照：同一任务，固定分工写法 vs 先规划再派工写法


## 可复现的最小实现

先把决策记录写成机器可读结构：

```json
{
  "unit": "并行与编排-工作者：什么时候同时做，什么时候先拆再派",
  "inputFrozen": true,
  "scenario": "normal | boundary | single-fault",
  "firstDivergence": null,
  "cleanupRequired": true
}
```

再用同一条执行链处理三类样本：

```ts
type Evidence = { stage: string; expected: string; actual: string };

function verify(sample: unknown, expected: readonly Evidence[]) {
  const trace = runFromCleanState(sample);
  return expected.find((item, index) => trace[index]?.actual !== item.expected);
}
```

最后保存回归门禁，禁止失败样本静默通过：

```text
normal      -> complete, invariant preserved
boundary    -> complete or explicit rejection
singleFault -> stop at first divergence, no stale output
```

用统一评分解释实验结果：

$$
Q = C_{correct} + C_{trace} + C_{recover} - R_{residual}
$$

$$
C_{coverage} = \frac{N_{verified}}{N_{official}}
$$

$$
R_{residual} = P(failure) \times I(impact)
$$

$$
Accept = (C_{coverage} \ge 0.90) \land (R_{residual} \le R_{budget})
$$

## 本章回顾

- 并行与编排-工作者：什么时候同时做，什么时候先拆再派必须绑定冻结输入和明确成功标准。
- 先打个比方必须能画成结构并沿边追踪责任。
- 并行：同一批活，预先分块后一起做要由正常、边界和单故障样本共同验证。
- 并行不只一种：分片（sectioning）和投票（voting）必须保存第一处偏离和清理重建步骤。
- 编排-工作者：先看任务，再临场拆活决定方案是否可以进入下一阶段。


## 术语表


{/* 知识点清单：1. 并行化 parallelization（图：）2. 分片 sectioning 与投票 voting（图：）3. 编排-工作者 orchestrator-workers（图：）4. 并行 vs 编排-工作者 vs 链式/路由/自主智能体边界（动画：）5. 常见误区：依赖步骤硬并行 / 固定三块活硬上编排者 / worker 重复查同样内容（图：）。Term↔Glossary：并行化 / 分片 / 投票 / 编排者 / 编排-工作者。 */}


## 先打个比方

做一份竞品调研，有时像分工明确的小组作业：开工前就知道要查价格、功能、用户反馈三栏，于是三个人分头去查，最后把三份材料拼成一页简报。这种做法解决的是“同样的几块活每次都要做，怎么更快做完”。

但也有些任务不是这样。老板可能只说一句：“帮我看清这个竞品到底值不值得跟。” 这时如果没人先看题、决定这次该查哪些部分，大家就容易有人重复查同样的东西，也有人漏掉最关键的点，最后不是慢，就是查偏。

这一章就讲这两种安排分工的方法：一种适合“每次都是固定几块活”，另一种适合“这次该拆哪些块，要先看题才知道”。选错了，要么白白多一道安排成本，要么把本该重点看的地方漏过去。

## 并行：同一批活，预先分块后一起做

最适合这样的活：你已经知道要拆哪几块，而且这些块彼此基本独立，不用前一个结果出来后，后一个才能动。


看图时盯住两个关键词：**预先写死**、**同时开工**。比如做一份竞品调研简报，你已经确定每次都要查“价格 / 功能 / 用户反馈”三块，那么这三件事完全可以三路同时跑，最后把结果合并成报告。Anthropic 在《Building effective agents》里把这种模式归为工作流：结构是人先设计好的，只是把几路 LLM 调用并排展开。

什么时候该想到它？当你主要想解决的是两类问题：

- **提速**：同样三块活，串着做要等三轮，并行能把等待压到最长那一轮。
- **分焦点**：复杂任务里有多个独立考量时，让每一轮只盯一个维度，往往比一轮里让模型同时兼顾所有维度更稳。

## 并行不只一种：分片（sectioning）和投票（voting）

Anthropic 把并行再拆成两种常见变体： 和。


它们都叫并行，但目的不一样：

- **分片**：大家做的是不同块。像竞品简报里的价格、功能、口碑三块，各查各的，最后拼起来。
- **投票**：大家做的是同一题。比如让三个 reviewer 同时审一段代码、分别给风险判断，再汇总成最终结论。

一句话记忆：**分片是在分工，投票是在求稳。** 如果你是“这活本来就有几块”，更像分片；如果你是“同一题想多看几眼、降低漏判”，更像投票。

## 编排-工作者：先看任务，再临场拆活

出现时，说明问题已经不是“固定三块一起跑”这么简单了。你需要先判断：这次到底该拆几块、拆成什么、哪些值得查。

就是为这种情况准备的。


和并行最像的地方，是它最后也会出现“一路派给多个工作者同时做”；但真正的分界线在前面那一步：**子任务不是你写代码时就列好的，而是编排者先看了这次输入，再临场决定。**

这很适合复杂但仍可控的任务。Anthropic 给的典型例子是复杂编码和多源搜索：你事先不知道这次会改 2 个文件还是 7 个文件，也不知道该查 3 个来源还是 8 个来源，所以不能把子任务写死；但你仍然希望整个结构是“先规划、再派工、再收敛”，而不是完全放飞成开放式 agent。

## 它们都像“多人一起干”，但差别在子任务是不是预先写死

把前面几章一起放进脑子里，最好记成一条从“更固定”到“更灵活”的线：

- **链式**：固定顺序，一步接一步。后一步依赖前一步输出，所以不适合同时跑。
- **路由**：先分到某一类，再走那一条预先写死的支线。重点是“选哪条固定路”。
- **并行**：不是选一条路，而是把几条**预先写死**的独立支线同时跑起来。
- **编排-工作者**：支线数量和内容不再预先写死，而是先由编排者看输入后再决定这次怎么拆。

它和自主智能体的边界也要分清：如果你的系统不只是“先拆活再派工”，而是开始进入一个开放循环，自己反复决定下一步目标、要不要继续搜、要不要改计划、什么时候停，而且这些路径大都没被框在固定脚手架里，那就已经更接近自主智能体了。**编排-工作者还是工作流；自主智能体才是开放式自驱循环。**

## 动手看：该预先并行，还是让编排者临场拆活？

> 猜一猜：如果每次竞品简报都固定要查“价格 / 功能 / 用户反馈”三块，你会把决策停在“预先并行”还是继续往“编排者拆活”走？那如果这次老板只说“帮我判断这个竞品的威胁在哪”，但没说该查哪些维度呢？


这张图可以浓缩成两个判断：

1. **子任务能不能提前列清楚？** 能，而且彼此独立，就优先并行。
2. **如果不能提前列清楚，是否仍能用“先规划、再分发、再汇总”的固定骨架承接？** 能，就用编排-工作者。

再往右一步才是自主智能体：当连“先规划、再派工、再收敛”这个骨架都不够，你需要模型自己反复探索、改目标、调工具、设停点时，才值得进入 agent 区域。不要把“需要一点动态拆活”误判成“必须全自主”。

## 代码对照：同一任务，固定分工写法 vs 先规划再派工写法

同样都是“做一份竞品调研简报”，真正要对照的不是“有没有多个人一起做”，而是这三件事：**任务清单是不是提前写死、worker 接到的说明是不是每次一样、汇总时要不要带着本次计划一起收束。** 下面按这三处逐段对照。

### 第一步：入口处，任务列表是不是开工前就已经写死

> 猜一猜：如果这次需求写的是“重点看它和我们相比最大的威胁点”，你会在函数开头直接写死三块任务，还是先让系统读需求再出这次的任务清单？

<CodeTabs>
  <Tab label="并行：固定三块预先写死">

```ts
type FixedTrack = "pricing" | "features" | "reviews";

export async function buildBrief(company: string) {
  const tasks: FixedTrack[] = ["pricing", "features", "reviews"];
  const jobs = tasks.map((kind) => runFixedWorker({ company, kind }));
  const findings = await Promise.all(jobs);
  return mergeFixedSections(findings);
}
```

  </Tab>
  <Tab label="编排-工作者：先读需求再出计划">

```ts
export async function buildBrief(request: string) {
  const plan = await orchestrateResearch(request);
  const jobs = plan.tasks.map((task) => runPlannedWorker(task));
  const findings = await Promise.all(jobs);
  return synthesizeFromPlan(plan, findings);
}
```

  </Tab>
</CodeTabs>

第一眼先看函数开头。左边的任务清单在代码里已经写死成 `pricing / features / reviews`，所以它是“固定分工后同时跑”；右边多出来的 `orchestrateResearch(request)` 才是分界线，它说明这次到底拆成哪几块，要先看输入再决定。

### 第二步：worker 接到的说明，是固定模板还是本次计划的一部分

固定并行里，worker 拿到的是“永远查这一栏”的固定说明；编排-工作者里，worker 拿到的是编排者刚刚写出来的“这次只查什么、不查什么”。

<CodeTabs>
  <Tab label="并行：worker 角色长期固定">

```ts
function runFixedWorker(input: { company: string; kind: FixedTrack }) {
  return callModel(`
你只负责 ${input.kind} 这一栏。
输出字段：summary、evidence、risk。
不要评价别的栏位。
公司：${input.company}
`);
}
```

  </Tab>
  <Tab label="编排-工作者：worker 说明跟着本次计划走">

```ts
function runPlannedWorker(task: {
  goal: string;
  output: string;
  outOfScope: string;
}) {
  return callModel(`
你只负责：${task.goal}
必须产出：${task.output}
不要重复：${task.outOfScope}
`);
}
```

  </Tab>
</CodeTabs>

这一步最容易看出“多 worker”不等于“编排-工作者”。左边也有多个 worker，但每个 worker 的职责是开发时就固定好的；右边的 worker 说明是运行时才生成的，所以它们处理的是“这次的计划”，不是“永远那三栏”。


### 第三步：汇总时，是按固定栏位拼接，还是按本次计划收束结论

最后看收尾。固定并行通常只是把几块固定栏位按原顺序拼回去；编排-工作者则往往要把“这次为什么这么拆、每块查到了什么”一起交给汇总器，才能写出最终结论。

<CodeTabs>
  <Tab label="并行：按固定栏位直接拼">

```ts
function mergeFixedSections(findings: WorkerResult[]) {
  return {
    pricing: findings[0],
    features: findings[1],
    reviews: findings[2],
  };
}
```

  </Tab>
  <Tab label="编排-工作者：带着计划一起收束">

```ts
function synthesizeFromPlan(plan: Plan, findings: WorkerResult[]) {
  return callModel({
    brief: "按本次计划写成一页判断",
    plan,
    findings,
  });
}
```

  </Tab>
</CodeTabs>

左边的汇总器默认“第 1 块就是价格、第 2 块就是功能”，天然依赖固定栏目；右边的汇总器要同时看 `plan` 和 `findings`，因为这次可能拆成“定价 / 渠道 / 留存”，下次又可能拆成“生态 / 风险 / 护城河”。**两边都能并发跑多个 worker，但只有右边把“这次拆什么”也纳入了运行时输入。**

把三段代码读完后，再用一眼图复习一次，会更容易把差别记牢：

<CompareSlider
  leftLabel="并行：固定分工"
  rightLabel="编排-工作者：先规划再派工"
  initial={50}
  left={
    <div className="not-prose flex h-full flex-col justify-center gap-3 rounded-card border border-border bg-bg p-5">
      <p className="text-sm font-bold text-primary">⚡ 并行的关键特征</p>
      <ul className="m-0 list-disc pl-5 text-xs leading-relaxed text-primary">
        <li>任务清单在写代码时就列好</li>
        <li>每个 worker 长期盯固定一栏</li>
        <li>汇总时按固定栏位直接拼回去</li>
      </ul>
      <p className="text-xs text-secondary">
        适合每次都固定查同样几块，而且这些块彼此独立。
      </p>
    </div>
  }
  right={
    <div className="not-prose flex h-full flex-col justify-center gap-3 rounded-card border border-accent bg-bg p-5">
      <p className="text-sm font-bold text-accent">🧠 编排-工作者的关键特征</p>
      <ul className="m-0 list-disc pl-5 text-xs leading-relaxed text-primary">
        <li>先读需求，临场产出这次计划</li>
        <li>worker 说明跟着这次计划变化</li>
        <li>汇总时要带着本次计划一起收束</li>
      </ul>
      <p className="text-xs text-secondary">
        适合拆法要看输入才知道，但整体仍能落在“先规划、再派工、再汇总”骨架里。
      </p>
    </div>
  }
/>

对照看差别很集中：左边的任务清单、worker 角色、汇总栏位，三处都在开发时写死了，所以它属于并行；右边先有一轮 `orchestrateResearch(request)`，接着 worker 说明和汇总方式都跟着本次计划走，所以它属于编排-工作者。**两边都可能最后并发跑多个 worker，但是否“先临场拆活”决定了它们不是同一种模式。**

## 容易踩的坑


## 小结

- 并行：固定独立子任务，预先写死后一起跑
- 分片求分工，投票求稳健
- 编排-工作者：先看输入，再决定这次拆哪些活
- 和并行的分界线：子任务是不是能提前列清
- 再往右才是自主智能体：进入开放式自驱循环


## 名词解释


