---
title: 工作流 vs 智能体：何时用哪个
type: D
section: 智能体模式
order: 10
description: 先用最简单的方案，只在需要时才加自主性——什么时候用写死的工作流、什么时候才值得上自主智能体。
demo: true
math: false
draft: false
sourceUrl: "https://www.anthropic.com/engineering/building-effective-agents"
---

import {
  OfficialAiAgentAppsWorkflowVsAgentMapLab,
  OfficialAiAgentAppsWorkflowVsAgentExperimentLab,
  OfficialAiAgentAppsWorkflowVsAgentEvidenceLab,
} from "@/components/mdx/ai-agent-apps/diagrams/workflow-vs-agent";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

{/* OFFICIAL_COURSE_ENHANCEMENT */}

<Objectives>

- 能解释工作流 vs 智能体：何时用哪个的输入、核心结构、状态变化和输出合同
- 能绘制并修改工作流 vs 智能体：何时用哪个的最小模型，比较至少一种替代方案
- 能复现正常、边界和单故障样本，定位第一条证据分叉
- 能根据成本与风险判断方案适用范围，并完成清理重建

</Objectives>

## 为什么只记结论不足以掌握工作流 vs 智能体：何时用哪个

学习“工作流 vs 智能体：何时用哪个”时，第一步不是记住结论，而是冻结输入、上下文、版本和成功标准。只有这些条件明确，工作流 vs 智能体：何时用哪个的含义才不会随着样例变化。正文已有的概念说明要与结构图、运行轨迹和失败样本互相印证，不能只凭最终输出看似正确就宣布完成。

结构分析从先打个比方开始：列出参与者、职责、连接方向、生命周期和所有权，再沿正常路径追踪数据或控制流。每一条边都要说明为什么存在、谁创建、谁消费、谁负责清理；如果边界被跨越，必须能在证据中找到第一处异常。

机制验证要把再辨析一次：差别不在步数，在「路是谁定的」写成可以执行的条件。正常样本证明主路径，恰好边界样本验证等号和空值，单故障样本只破坏一个假设。三类样本使用同一份观察指标，避免因为测试口径变化而把偶然结果误认成规律。

成本分析同时记录时间、空间、延迟、耦合、可维护性和不可逆操作。一个核心原则：先用最简单的，只在需要时加自主性不是一句“可能失败”，而是可复现输入、预期停点、实际轨迹、错误分类与清理步骤。任何自动重试都要有次数、预算和幂等边界。

方案比较不能只列优点。需要给出直接实现、当前方案和至少一个替代方案，逐项比较复杂度、扩展点、故障隔离和团队认知成本。当问题规模很小或变化轴稳定时，更简单的实现往往更好；模式与框架必须由真实变化压力证明。

实现阶段把大结论拆成可检查的中间产物：配置快照、结构清单、状态转移、输入输出样本、日志摘要和测试结果。每个产物带来源与生成命令，下一阶段只消费已通过门禁的版本，避免旧缓存或隐式默认值污染结论。

解释结果时必须区分相关性与因果性、接口承诺与实现细节、设计意图与运行事实。对“同一件事，多种自主性档位：智能体模式光谱”的判断要由独立证据支持，并明确适用范围；一旦输入分布、版本、硬件或组织边界改变，就重新运行最小实验。

复盘从首个分叉开始，而不是从最后一个报错倒推。先比较冻结输入，再比较第一份结构化中间产物，随后检查状态、约束和副作用。这样可以把复杂系统的排错范围收缩到一个阶段，避免在多个层次同时修改造成新的不确定性。

迁移到真实项目时，先选择一个最小但有代表性的切片，保存改造前基线，再逐步引入“工作流 vs 智能体：何时用哪个”中的机制。每一步只改变一个变量并保留回滚点；性能、正确性、安全性和可理解性至少各有一项可量化指标。

最终验收要求读者能脱离页面重新画出结构、口述关键链路、实现最小版本、构造一个反例并解释失败位置。若只能复述名词而不能预测中间状态，说明知识仍停留在识记层，需要回到图示和实验重新验证。

本页用<Term def="工作流 vs 智能体：何时用哪个连接工作流 vs 智能体：何时用哪个的机制、边界与可复查证据。">工作流 vs 智能体：何时用哪个</Term>、

<Term def="先打个比方连接工作流 vs 智能体：何时用哪个的机制、边界与可复查证据。">先打个比方</Term>、<Term def="再辨析一次：差别不在步数，在「路是谁定的」连接工作流 vs 智能体：何时用哪个的机制、边界与可复查证据。">再辨析一次：差别不在步数，在「路是谁定的」</Term>、<Term def="一个核心原则：先用最简单的，只在需要时加自主性连接工作流 vs 智能体：何时用哪个的机制、边界与可复查证据。">一个核心原则：先用最简单的，只在需要时加自主性</Term>、<Term def="同一件事，多种自主性档位：智能体模式光谱连接工作流 vs 智能体：何时用哪个的机制、边界与可复查证据。">同一件事，多种自主性档位：智能体模式光谱</Term>
建立统一坐标。先预测这些概念在结构图和运行轨迹中的位置，再操作实验控件；如果结果与预测不一致，停止在首个分叉，不要用后续补丁掩盖早期错误。

## 权威目录与核心概念逐项对照

- 工作流 vs 智能体：何时用哪个
- 先打个比方
- 再辨析一次：差别不在步数，在「路是谁定的」
- 一个核心原则：先用最简单的，只在需要时加自主性
- 同一件事，多种自主性档位：智能体模式光谱
- 动手看：上不上 agent，是怎么一步步问出来的
- 代码对照：同一个任务，工作流写法 vs agent 写法
- 容易踩的坑

## 三视图实验：结构、变量与证据

<Stepper>
  <Step title="第一步：冻结结构并先预测">
    先预测参与者、连接方向与关键状态，再点击结构节点核对。
    <OfficialAiAgentAppsWorkflowVsAgentMapLab />
  </Step>
  <Step title="第二步：只改变一个变量">
    保持其他条件不变，移动覆盖率或风险滑块，观察门禁变化。
    <OfficialAiAgentAppsWorkflowVsAgentExperimentLab />
  </Step>
  <Step title="第三步：注入单故障并保存证据">
    注入一个边界故障，记录第一处偏离、拒绝原因和恢复条件。
    <OfficialAiAgentAppsWorkflowVsAgentEvidenceLab />
  </Step>
</Stepper>

<Callout type="trap">
  <strong>误区：</strong>
  看到最终结果正确就认为机制正确。中间状态可能已经偏离，只是被后续默认值或重试偶然修补。应保存首个结构化产物并逐阶段比较。
</Callout>

<Callout type="trap">
  <strong>误区：</strong>
  为了使用模式或框架而制造抽象。若变化轴、失败边界和替代方案没有写清，新增层次只会扩大认知成本和故障面。
</Callout>

## 可复现的最小实现

先把决策记录写成机器可读结构：

```json
{
  "unit": "工作流 vs 智能体：何时用哪个",
  "inputFrozen": true,
  "scenario": "normal | boundary | single-fault",
  "firstDivergence": null,
  "cleanupRequired": true
}
```

再用同一条执行链处理三类样本：

```ts
type Evidence = { stage: string; expected: string; actual: string };

function verify(sample: unknown, expected: readonly Evidence[]) {
  const trace = runFromCleanState(sample);
  return expected.find((item, index) => trace[index]?.actual !== item.expected);
}
```

最后保存回归门禁，禁止失败样本静默通过：

```text
normal      -> complete, invariant preserved
boundary    -> complete or explicit rejection
singleFault -> stop at first divergence, no stale output
```

用统一评分解释实验结果：

$$
Q = C_{correct} + C_{trace} + C_{recover} - R_{residual}
$$

$$
C_{coverage} = \frac{N_{verified}}{N_{official}}
$$

$$
R_{residual} = P(failure) \times I(impact)
$$

$$
Accept = (C_{coverage} \ge 0.90) \land (R_{residual} \le R_{budget})
$$

## 本章回顾

- 工作流 vs 智能体：何时用哪个必须绑定冻结输入和明确成功标准。
- 先打个比方必须能画成结构并沿边追踪责任。
- 再辨析一次：差别不在步数，在「路是谁定的」要由正常、边界和单故障样本共同验证。
- 一个核心原则：先用最简单的，只在需要时加自主性必须保存第一处偏离和清理重建步骤。
- 同一件事，多种自主性档位：智能体模式光谱决定方案是否可以进入下一阶段。

<Exercises>

**问题 1**：为“工作流 vs 智能体：何时用哪个”写出最小输入合同，并指出一个不能省略的边界条件。

<Answer>
  输入合同至少包含样本身份、版本、配置、成功标准和清理方式；边界条件必须能改变结构或状态，而不是只换一段描述。
</Answer>

**问题 2**：构造一个只破坏“再辨析一次：差别不在步数，在「路是谁定的」”的单故障样本，第一条预期证据是什么？

<Answer>
  先冻结正常轨迹，只改变该条件；第一条证据应是最早依赖它的结构化中间产物，而不是最终错误消息。
</Answer>

**问题 3**：什么情况下不应采用本页方案？

<Answer>
  当变化轴稳定、直接实现更清晰、额外层次无法降低风险或提供独立测试边界时，应选择更简单方案并记录重新评估触发条件。
</Answer>

</Exercises>

## 术语表

<Glossary>
  <GlossaryItem term="工作流 vs 智能体：何时用哪个">
    工作流 vs 智能体：何时用哪个用于描述“工作流 vs
    智能体：何时用哪个”中的输入、结构、约束或证据边界，必须能在实验记录中定位。
  </GlossaryItem>
  <GlossaryItem term="先打个比方">
    先打个比方用于描述“工作流 vs
    智能体：何时用哪个”中的输入、结构、约束或证据边界，必须能在实验记录中定位。
  </GlossaryItem>
  <GlossaryItem term="再辨析一次：差别不在步数，在「路是谁定的」">
    再辨析一次：差别不在步数，在「路是谁定的」用于描述“工作流 vs
    智能体：何时用哪个”中的输入、结构、约束或证据边界，必须能在实验记录中定位。
  </GlossaryItem>
  <GlossaryItem term="一个核心原则：先用最简单的，只在需要时加自主性">
    一个核心原则：先用最简单的，只在需要时加自主性用于描述“工作流 vs
    智能体：何时用哪个”中的输入、结构、约束或证据边界，必须能在实验记录中定位。
  </GlossaryItem>
  <GlossaryItem term="同一件事，多种自主性档位：智能体模式光谱">
    同一件事，多种自主性档位：智能体模式光谱用于描述“工作流 vs
    智能体：何时用哪个”中的输入、结构、约束或证据边界，必须能在实验记录中定位。
  </GlossaryItem>
</Glossary>

<Attribution
  adaptedFrom="站内应用专题《AI 智能体应用开发》，依据Anthropic官方Agent模式与工具工程资料，2026-07-18核定 · 独立教学重构"
  adaptedUrl="https://www.anthropic.com/engineering/building-effective-agents"
/>

{/* D 对比型章（第四篇「智能体模式」开篇）：承接第 1 章「智能体是什么 + 自主性光谱」的初识，本章不重复那些定义，只讲「决策框架 + 第四篇路线图」——何时用工作流、何时才值得上自主 agent，并总览即将展开的模式。核心立意参考 Anthropic《Building Effective Agents》：先用最简单的方案，只在确实需要时才加自主性。主 viz = PatternDecisionDiagram（animotor 5 步决策树）。math:false 整节省略。知识点 → viz 清单（动笔先行）：1. 工作流 vs 智能体再辨析（路径写死 vs 路径动态）→ <WorkflowVsAgentDiagram />（静态并排）；2. 决策框架·只在需要时加自主性【主动画·核心】→ <PatternDecisionDiagram />（animotor 决策树）；3. 模式光谱 / 第四篇路线图 → <PatternSpectrumDiagram />（静态横向光谱）；4. 取舍（自主性↑→灵活↑ 但 成本/延迟/不可预测↑）→ <CompareSlider>（同一任务 工作流 vs agent 两面板）；5. 选错方案的翻车现场 → <PatternChoiceTrapDiagram />（静态双栏错误效果图）。Term 高亮术语：工作流 workflow / 自主性 / 智能体模式 / 编排 orchestration → 均进章末 Glossary，id 对齐（智能体 / 工具 / 智能体循环等第 1 章已收的不重复定义，确需用一句话引用）。 */}

{/* ===== 1. 学习目标｜spec §一：D 型含一条「能说出什么场景用工作流 / 什么场景上 agent」；末条检验问题 ===== */}

<Objectives>

- 能说出工作流和智能体最本质的区别（路是谁定的、何时定的），而不是用「步数多少」去分
- 能拿一个决策框架，对一个具体任务判断该用工作流（甚至纯代码）还是该上自主 agent，并说出理由
- 能回答：把一个固定可预测的任务硬做成自主 agent，通常会付出哪些代价、又得到什么？

</Objectives>

{/* ===== 2. 直觉引入｜spec §二：≤3 段 × ≤120 字；零术语；类比统一（办事 / SOP / 请人）===== */}

## 先打个比方

公司里办事，有两种路子。一种是**照流程走**：报销怎么填、走哪几个审批、谁盖章，写在制度里，每次都一样。这种事最省心——照着做就行，不会出岔子，也不用动脑子。

另一种是**请个人来办**：「帮我把这趟出差安排妥当。」具体怎么订、遇到没票了怎么改、临时改行程怎么应对，你说不全，只能交给一个能自己拿主意的人，让他见机行事。

关键在于：**流程固定的事照流程走最稳最省，情况多变、要随机应变的事才值得请个能自己拿主意的人。** 这一章就讲：做这类程序时，怎么判断一件事该「照流程走」还是该「请个人」——杀鸡别用牛刀，牛刀也别拿来杀鸡。选错了，要么白白浪费，要么根本办不成事。

{/* ===== 3. 概念讲解｜spec §三：一节一概念；术语首现 <Term def id> 写句中；每概念配图；新核心概念 ≤5 ===== */}

## 再辨析一次：差别不在步数，在「路是谁定的」

第 1 章我们已经认过这几个词：<Term def="工作流（Workflow）：把若干步骤按固定顺序串成的流水线，每一步、走哪条路都是人预先写死的，每次执行都一样、可预测。">工作流（Workflow）</Term> 是路径写死的流水线，智能体是在循环里自己拿主意的程序。这一节只把那条分界线擦得更清楚——很多人会以为「步数多就是智能体、步数少就是工作流」，**这是错的**。

真正的分界线是：**这条路，是人在写代码时就定死的，还是 LLM 在运行时自己边跑边选的？** 一张图把同一件「订高铁票」的事，两种走法并排放出来：

<WorkflowVsAgentDiagram />

看清左右两栏：左边工作流的三步——查车次、下单、发确认——是人**提前写死**的，箭头直来直去、不分叉，没票了也照样往下走；右边智能体没有写死的步骤，只有一个**循环**：想一步、做一步、看一眼，由 LLM **当场**决定这次调哪个工具、要不要换车次、何时收尾。一个工作流可以有几十步却仍是工作流（步步写死），一个 agent 可能只转三圈却是 agent（步步自己选）。**记住这条线，本章后面全靠它做判断。**

## 一个核心原则：先用最简单的，只在需要时加自主性

知道了区别，下一个问题是「那我到底该用哪个」。这里有一条贯穿整个智能体工程的原则，也是本章的灵魂：**先用最简单的方案，只在确实需要时才加自主性。**

这里的 <Term def="自主性（Autonomy）：程序在多大程度上「自己决定下一步」而非按人写死的逻辑走。自主性越高越灵活，能应付越开放的任务，但也越难预测、越难控制、通常越贵越慢。" id="autonomy">自主性</Term> 不是越多越好。给程序加自主性，等于把「决定下一步」的权力交给 LLM——这换来了灵活，但也同时换来了成本、延迟和不可预测性（下一节细看这笔账）。所以正确的姿势不是「先想怎么把它做成最酷的 agent」，而是**反过来**：先问「能不能用最笨最简单的办法（纯代码、写死的工作流）解决」，只有当简单办法确实顶不住时，才一点点往上加自主性。能省则省，这是工程上的稳妥，不是偷懒。

## 同一件事，多种自主性档位：智能体模式光谱

「加自主性」也不是从工作流一步跳到全自主 agent，中间还有一整排档位。把它们按自主性从低到高排开，就是一道 <Term def="智能体模式（Agentic Patterns）：介于「纯工作流」和「全自主 agent」之间的一系列构建方式，如链式、路由、并行、编排-工作者、评估-优化。它们大多仍是路径预先编排好的工作流，只是在不同槽位用上 LLM。" id="agentic-patterns">智能体模式</Term> 光谱，也正好是第四篇接下来要逐章展开的路线图：

<PatternSpectrumDiagram />

从左到右：单次调用 → 链式 → 路由 → 并行 → 编排-工作者 → 评估-优化 → 自主 agent，越往右 LLM 自己拿主意越多。要点有两个：一是**多数实用系统落在中间这些工作流模式上**，自主 agent 只是最右端、最灵活也最贵的那一档，并非默认选项；二是中段那些模式（如 <Term def="编排（Orchestration）：由一个「编排者」按预先设计好的结构，把任务拆给若干「工作者」分头去做、再把结果汇总。控制流是人编排好的，所以它仍属工作流模式，而非完全自主。" id="orchestration">编排</Term>-工作者）虽然用到 LLM，但整体控制流是人预先编排好的，路径不是 LLM 临场决定的——所以它们仍算工作流。链式 / 路由在 Ch12、并行 / 编排-工作者在 Ch13、评估-优化在 Ch14 展开，本章先把全景给你。

{/* ===== 4. 数学推导｜math:false：整节删除 ===== */}

{/* ===== 5. 交互 Demo｜spec §五：主 viz = PatternDecisionDiagram；猜一猜引导；可暂停/单步/拖进度 ===== */}

## 动手看：上不上 agent，是怎么一步步问出来的

光谱给了全景，但落到一个具体任务上，到底怎么选？下面这个动画把决策框架一步步演出来——它不让你一上来就纠结「做成什么样的 agent」，而是先问几个问题，把简单方案优先排在前面。

> 猜一猜：把任务「每天把固定格式的日报转成邮件发出去」丢进这个决策框架，第一问「任务可预测、步数固定吗」你会答「是」还是「否」？答完会落到哪个终点——工作流，还是 agent？先想一想，再单步播放对一对。

<PatternDecisionDiagram />

盯住一件事：决策框架**先问「能不能写死」，能写死就止步于工作流**，根本不往 agent 那边走；只有当任务确实开放、步数不定、没法预先把路写死时，才往右选自主 agent。最后那句结论是整章的题眼——**能简单别复杂，只在确实需要时才加自主性**。这不是保守，而是因为自主性是有代价的，下一节我们就把这笔账摊到代码上算清楚。

{/* ===== 6. 代码对照｜spec §六：D 型并排 diff；用 CompareSlider，左右传 JSX 面板；单块 ≤30 行，块间解说差异 ===== */}

## 代码对照：同一个任务，工作流写法 vs agent 写法

把上面的取舍落到代码里最直观。下面是同一个任务——「帮用户订一张高铁票」——的两种写法。拖动滑块，左边是写死的工作流，右边是带循环让 LLM 决定的 agent，注意那个 `while` 循环带来的差别：

> 猜一猜：左边那个三行顺序调用的工作流，遇到「查到的车次都没票了」会怎样？右边那个带 `while` 的 agent 又会怎样？拖过去看看，再想想这份「灵活」是用什么换来的。

<CompareSlider
  leftLabel="工作流写法（路径写死 · 便宜可预测）"
  rightLabel="agent 写法（带循环 · 灵活但贵）"
  initial={48}
  left={
    <div className="not-prose flex h-full flex-col justify-center gap-1 rounded-card border border-border bg-bg p-5">
      <p className="text-sm font-bold text-primary">
        🛤️ 工作流：三步顺序调，写死
      </p>
      <pre className="overflow-x-auto font-mono text-xs leading-relaxed text-primary">
        {`def book_ticket(req):
    trains = search_trains(req)   # 步骤 1：写死
    chosen = trains[0]            # 步骤 2：写死（取第一个）
    return pay_and_issue(chosen)  # 步骤 3：写死
# 没票就崩——路是写死的，不会变通`}
      </pre>
      <p className="mt-1 text-xs text-secondary">
        可预测、便宜、可靠；但遇到没预设的情况（都没票）就卡死，不会随机应变。
      </p>
    </div>
  }
  right={
    <div className="not-prose flex h-full flex-col justify-center gap-1 rounded-card border border-accent bg-bg p-5">
      <p className="text-sm font-bold text-accent">
        🔄 agent：循环里让 LLM 决定下一步
      </p>
      <pre className="overflow-x-auto font-mono text-xs leading-relaxed text-primary">
        {`def book_ticket(goal):
    ctx = [system_prompt, goal]
    while True:                   # 灵活就来自这个循环
        step = llm(ctx)           # LLM 当场决定下一步
        if step.done:
            return step.answer
        out = call_tool(step.tool, step.args)
        ctx += [step, out]        # 没票就自己改查别的车次`}
      </pre>
      <p className="mt-1 text-xs text-secondary">
        遇到没票能自己换车次、再查；但每步都要一轮 LLM
        调用——更慢、更贵、路径不固定。
      </p>
    </div>
  }
/>

逐处对照两边的差异：左边是 `search → 取第一个 → pay` 三个函数**顺序直调**，路径写死，没有循环，也没有「LLM 决定下一步」——所以查到的车次没票时它无路可走，直接崩。右边核心是一个 `while` 循环，每一圈 `llm(ctx)` 让模型看当前情况、**自己决定**调哪个工具或收尾——没票它能自己改查别的车次。这份灵活不是白来的：右边每转一圈都要一次 LLM 调用，**更慢、更贵，而且同样输入下一步走哪不再固定**。这就是自主性的账：左边便宜可控但死板，右边灵活但贵且不定。

{/* ===== 7. 常见误区｜spec §七：D 型 ≥2 个，必含 1 个「场景选错方案」坑；固定 现象→原因→修法 三行 ===== */}

## 容易踩的坑

先别急着读三段文字，先看一眼两种最典型的翻车现场。左边是该照章办事却硬请了个爱临场发挥的人，右边是明明状况百出却非要拿死流程去套：

<PatternChoiceTrapDiagram />

<Callout type="trap">
  **现象**：一个明明很固定的任务（按死规则审核报销单：金额超标或缺发票就驳回），非要做成自主
  agent，上线后又慢又贵，还时不时误判。 **原因**：这是「固定可预测任务硬上
  agent」——审核规则是确定、可预先写死的，却把「决定下一步」的权力交给了
  LLM，平白引入随机性、成本和延迟，自主性完全用不上。
  **修法**：按决策框架反向收——第一问「可预测、步数固定吗」答「是」，就止步于工作流（甚至纯代码）：规则命中即驳回
  / 通过，稳、快、便宜。
</Callout>

<Callout type="trap">
  **现象**：一个明明很开放的任务（客户用大白话提的任意运维诉求），硬塞进一个写死的
  if-else 工作流，结果一遇到没预设的情况就卡死、无路可走。
  **原因**：这是反方向的坑——「该用 agent
  的开放任务硬塞进写死工作流」。开放任务的分支你永远列不全，写死的路径碰到没枚举过的情况就走投无路。
  **修法**：这类开放、不可预测的任务本就该往右选自主 agent，让 LLM
  看懂诉求后在循环里自己决定调哪些工具、怎么处理，而不是指望人把所有分支提前枚举。
</Callout>

<Callout type="trap">
  **现象**：「为了通用、省得以后改」，把每个功能都一上来做成最自主的
  agent，结果连最简单的功能也背上了高成本、高延迟和不可预测。
  **原因**：误把自主性当成免费的通用性，把旋钮一拧到底。大多数功能根本不需要运行时决策，自主性纯属浪费。
  **修法**：默认从最简单的方案起步，逐功能按决策框架判断；只有确实开放的功能才加自主性，并优先选光谱中段的工作流模式，而非一步到位全自主。
</Callout>

{/* ===== 8. 小结与练习｜spec §八：小结 ≤5 条呼应目标；2-3 题含 1 道选型题；Answer 折叠 ===== */}

## 小结

- 工作流 vs 智能体的本质区别：路是人写死的，还是 LLM 边跑边自己选的——不看步数
- 核心原则：先用最简单的方案，只在确实需要时才加自主性，能简单别复杂
- 决策框架：先问「任务可预测、步数固定吗」——是 → 工作流（甚至纯代码）；否 → 才上 agent
- 自主性是把双刃剑：自主性↑ → 灵活↑，但成本 / 延迟 / 不可预测性也一起↑
- 第四篇是一道光谱：链式 / 路由 / 并行 / 编排-工作者 / 评估-优化，自主 agent 只是最右一档

<Exercises>

**练习 1（辨析题）** 下面两句关于「工作流 vs 智能体」的说法，对还是错？为什么？
（A）「一个有 20 个步骤的数据处理流水线，步骤这么多，肯定是智能体。」
（B）「这份灵活是免费的，所以功能能做成 agent 就尽量做成 agent。」

<Answer>

（A）**错**。区别不在步数多少，而在「路是谁定的」。20 步的流水线如果每一步、走哪条路都是人写死的，它仍是**工作流**；判断要看路由是写死的还是 LLM 运行时自己选的，而不是数有几步。
（B）**错**。灵活不是免费的——加自主性等于让 LLM 每步决策，换来灵活的同时也换来成本、延迟和不可预测。正确姿势是「先用最简单的，只在需要时才加自主性」，多数功能根本不需要运行时决策。

</Answer>

**练习 2（选型题 · 给场景选方案）** 下面三个任务，分别该用「工作流（甚至纯代码）」还是「自主 agent」？说明理由。
（甲）每月把数据库里的订单导出成固定格式的 Excel，再上传到指定网盘。
（乙）用户上传一段杂乱的会议录音，要你「整理出待办事项，缺信息时自己想办法补全、必要时上网查」。
（丙）把用户输入的身份证号校验格式、查出归属地、填进固定表单。

<Answer>

（甲）**工作流 / 纯代码**：步骤固定（查库 → 套格式 → 上传），每次一样、可预测，决策框架第一问答「是」，止步于工作流，最稳最便宜。
（乙）**自主 agent**：任务开放、步数不定、要随机应变——整理到哪、缺什么、要不要上网查、查哪些，都得看中间结果临场决定，没法预先写死路径，第一问答「否」，往右上 agent。
（丙）**纯代码 / 工作流**：格式校验和查表是确定规则，写死即可，连 LLM 都未必需要。判别诀窍：能把每一步和分支预先写死的 → 工作流；列不全分支、必须临场决定的 → agent。

</Answer>

**练习 3（改 Demo 代码题）** 看本章「代码对照」里右边那个 agent 写法。它用了 `while True`，万一 LLM 反复改车次却始终订不到票（比如全程没票），会发生什么？请改写这个循环，加一道保护，让它在转太多圈后强制停下并返回兜底回答。说出你改了哪里、为什么。

<Answer>

会**死循环**——agent 无限地「查 → 改车次 → 再查」却永远收不了尾，白白烧钱、卡住不返回。修法是加一个**最大步数上限**：

```python
def book_ticket(goal, max_steps=8):
    ctx = [system_prompt, goal]
    for _ in range(max_steps):     # 不再 while True，最多转 max_steps 圈
        step = llm(ctx)
        if step.done:
            return step.answer
        out = call_tool(step.tool, step.args)
        ctx += [step, out]
    return "抱歉，试了多次仍没订到票，建议换个日期再试。"  # 强制兜底
```

关键改动：把 `while True` 换成 `for _ in range(max_steps)`，转满还没收尾就跳出、返回兜底回答。这正是自主性的代价之一——把决定权交给 LLM 的同时，人必须留一道「最多转几圈」的闸，否则不可预测会变成不可收场。这也反过来说明：能用工作流写死的任务，根本不会有这种「转不停」的风险。

</Answer>

</Exercises>

{/* ===== 9. 出处声明｜spec §九：改写参考 Anthropic《Building Effective Agents》 ===== */}

<Attribution
  adaptedFrom="Anthropic《Building Effective Agents》"
  adaptedUrl="https://www.anthropic.com/research/building-effective-agents"
/>

{/* ===== 10. 名词解释｜spec §十：与正文 Term 高亮一一对应，id 对齐，零基础大白话；第 1 章已收的（智能体 / 工具 / 循环）不重复 ===== */}

## 名词解释

<Glossary>
  <GlossaryItem term="工作流（Workflow）">
    把若干步骤按固定顺序串成的流水线，每一步、走哪条路都是人提前写死的，每次执行都一样、可预测。和智能体的根本区别：工作流的路是人写代码时定死的，智能体的路是
    LLM 在循环里边跑边选的——不看步数多少。详见本章「再辨析一次」。
  </GlossaryItem>
  <GlossaryItem term="自主性（Autonomy）" id="autonomy">
    程序在多大程度上「自己决定下一步」而不是按人写死的逻辑走。它是一把双刃剑：自主性越高越灵活、能应付越开放的任务，但也越难预测、越难控制、通常越贵越慢——不是越大越好。本章的核心原则就是「只在确实需要时才加自主性」。详见本章「一个核心原则」。
  </GlossaryItem>
  <GlossaryItem term="智能体模式（Agentic Patterns）" id="agentic-patterns">
    介于「纯工作流」和「全自主
    agent」之间的一系列构建方式，如链式、路由、并行、编排-工作者、评估-优化。它们大多仍是路径预先编排好的工作流，只是在不同槽位用上了
    LLM；按自主性从低到高排开就成了一道光谱。第四篇会逐章展开。详见本章「智能体模式光谱」。
  </GlossaryItem>
  <GlossaryItem term="编排（Orchestration）" id="orchestration">
    由一个「编排者」按预先设计好的结构，把任务拆给若干「工作者」分头去做、再把结果汇总。虽然用到了多个
    LLM，但整体控制流是人编排好的、不是 LLM
    临场决定的，所以它仍属于工作流模式，落在光谱中段，而非完全自主的
    agent。详见本章「智能体模式光谱」。
  </GlossaryItem>
</Glossary>
