---
title: 提示工程精要
type: A
section: 上下文工程
order: 4
description: 提示是上下文里最可控的一环——好提示由哪些部件构成，具体化、少样本、思维链怎么让输出脱胎换骨。
demo: true
math: false
draft: true
sourceUrl: "https://www.anthropic.com/engineering/building-effective-agents"
---

import {
  OfficialAiAgentAppsPromptEngineeringMapLab,
  OfficialAiAgentAppsPromptEngineeringExperimentLab,
  OfficialAiAgentAppsPromptEngineeringEvidenceLab,
} from "@/components/mdx/ai-agent-apps/diagrams/prompt-engineering";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

{/* OFFICIAL_COURSE_ENHANCEMENT */}

<Objectives>

- 能解释提示工程精要的输入、核心结构、状态变化和输出合同
- 能绘制并修改提示工程精要的最小模型，比较至少一种替代方案
- 能复现正常、边界和单故障样本，定位第一条证据分叉
- 能根据成本与风险判断方案适用范围，并完成清理重建

</Objectives>

## 为什么只记结论不足以掌握提示工程精要

学习“提示工程精要”时，第一步不是记住结论，而是冻结输入、上下文、版本和成功标准。只有这些条件明确，提示工程精要的含义才不会随着样例变化。正文已有的概念说明要与结构图、运行轨迹和失败样本互相印证，不能只凭最终输出看似正确就宣布完成。

结构分析从先打个比方开始：列出参与者、职责、连接方向、生命周期和所有权，再沿正常路径追踪数据或控制流。每一条边都要说明为什么存在、谁创建、谁消费、谁负责清理；如果边界被跨越，必须能在证据中找到第一处异常。

机制验证要把第一个概念：提示工程，调的是「喂给模型的那段话」写成可以执行的条件。正常样本证明主路径，恰好边界样本验证等号和空值，单故障样本只破坏一个假设。三类样本使用同一份观察指标，避免因为测试口径变化而把偶然结果误认成规律。

成本分析同时记录时间、空间、延迟、耦合、可维护性和不可逆操作。第二个概念：清晰具体——同样一句话，模糊和具体差到天上不是一句“可能失败”，而是可复现输入、预期停点、实际轨迹、错误分类与清理步骤。任何自动重试都要有次数、预算和幂等边界。

方案比较不能只列优点。需要给出直接实现、当前方案和至少一个替代方案，逐项比较复杂度、扩展点、故障隔离和团队认知成本。当问题规模很小或变化轴稳定时，更简单的实现往往更好；模式与框架必须由真实变化压力证明。

实现阶段把大结论拆成可检查的中间产物：配置快照、结构清单、状态转移、输入输出样本、日志摘要和测试结果。每个产物带来源与生成命令，下一阶段只消费已通过门禁的版本，避免旧缓存或隐式默认值污染结论。

解释结果时必须区分相关性与因果性、接口承诺与实现细节、设计意图与运行事实。对“第三个概念：少样本——与其费劲描述，不如直接给个范例”的判断要由独立证据支持，并明确适用范围；一旦输入分布、版本、硬件或组织边界改变，就重新运行最小实验。

复盘从首个分叉开始，而不是从最后一个报错倒推。先比较冻结输入，再比较第一份结构化中间产物，随后检查状态、约束和副作用。这样可以把复杂系统的排错范围收缩到一个阶段，避免在多个层次同时修改造成新的不确定性。

迁移到真实项目时，先选择一个最小但有代表性的切片，保存改造前基线，再逐步引入“提示工程精要”中的机制。每一步只改变一个变量并保留回滚点；性能、正确性、安全性和可理解性至少各有一项可量化指标。

最终验收要求读者能脱离页面重新画出结构、口述关键链路、实现最小版本、构造一个反例并解释失败位置。若只能复述名词而不能预测中间状态，说明知识仍停留在识记层，需要回到图示和实验重新验证。

本页用<Term def="提示工程精要连接提示工程精要的机制、边界与可复查证据。">提示工程精要</Term>、

<Term def="先打个比方连接提示工程精要的机制、边界与可复查证据。">先打个比方</Term>、<Term def="第一个概念：提示工程，调的是「喂给模型的那段话」连接提示工程精要的机制、边界与可复查证据。">第一个概念：提示工程，调的是「喂给模型的那段话」</Term>、<Term def="第二个概念：清晰具体——同样一句话，模糊和具体差到天上连接提示工程精要的机制、边界与可复查证据。">第二个概念：清晰具体——同样一句话，模糊和具体差到天上</Term>、<Term def="第三个概念：少样本——与其费劲描述，不如直接给个范例连接提示工程精要的机制、边界与可复查证据。">第三个概念：少样本——与其费劲描述，不如直接给个范例</Term>
建立统一坐标。先预测这些概念在结构图和运行轨迹中的位置，再操作实验控件；如果结果与预测不一致，停止在首个分叉，不要用后续补丁掩盖早期错误。

## 权威目录与核心概念逐项对照

- 提示工程精要
- 先打个比方
- 第一个概念：提示工程，调的是「喂给模型的那段话」
- 第二个概念：清晰具体——同样一句话，模糊和具体差到天上
- 第三个概念：少样本——与其费劲描述，不如直接给个范例
- 第四个概念：思维链——让它先一步步想，再给答案
- 把四招连起来：系统提示 vs 单次任务提示
- 动手看：一个提示怎么一步步「精化」到能用

## 三视图实验：结构、变量与证据

<Stepper>
  <Step title="第一步：冻结结构并先预测">
    先预测参与者、连接方向与关键状态，再点击结构节点核对。
    <OfficialAiAgentAppsPromptEngineeringMapLab />
  </Step>
  <Step title="第二步：只改变一个变量">
    保持其他条件不变，移动覆盖率或风险滑块，观察门禁变化。
    <OfficialAiAgentAppsPromptEngineeringExperimentLab />
  </Step>
  <Step title="第三步：注入单故障并保存证据">
    注入一个边界故障，记录第一处偏离、拒绝原因和恢复条件。
    <OfficialAiAgentAppsPromptEngineeringEvidenceLab />
  </Step>
</Stepper>

<Callout type="trap">
  <strong>误区：</strong>
  看到最终结果正确就认为机制正确。中间状态可能已经偏离，只是被后续默认值或重试偶然修补。应保存首个结构化产物并逐阶段比较。
</Callout>

<Callout type="trap">
  <strong>误区：</strong>
  为了使用模式或框架而制造抽象。若变化轴、失败边界和替代方案没有写清，新增层次只会扩大认知成本和故障面。
</Callout>

## 可复现的最小实现

先把决策记录写成机器可读结构：

```json
{
  "unit": "提示工程精要",
  "inputFrozen": true,
  "scenario": "normal | boundary | single-fault",
  "firstDivergence": null,
  "cleanupRequired": true
}
```

再用同一条执行链处理三类样本：

```ts
type Evidence = { stage: string; expected: string; actual: string };

function verify(sample: unknown, expected: readonly Evidence[]) {
  const trace = runFromCleanState(sample);
  return expected.find((item, index) => trace[index]?.actual !== item.expected);
}
```

最后保存回归门禁，禁止失败样本静默通过：

```text
normal      -> complete, invariant preserved
boundary    -> complete or explicit rejection
singleFault -> stop at first divergence, no stale output
```

用统一评分解释实验结果：

$$
Q = C_{correct} + C_{trace} + C_{recover} - R_{residual}
$$

$$
C_{coverage} = \frac{N_{verified}}{N_{official}}
$$

$$
R_{residual} = P(failure) \times I(impact)
$$

$$
Accept = (C_{coverage} \ge 0.90) \land (R_{residual} \le R_{budget})
$$

## 本章回顾

- 提示工程精要必须绑定冻结输入和明确成功标准。
- 先打个比方必须能画成结构并沿边追踪责任。
- 第一个概念：提示工程，调的是「喂给模型的那段话」要由正常、边界和单故障样本共同验证。
- 第二个概念：清晰具体——同样一句话，模糊和具体差到天上必须保存第一处偏离和清理重建步骤。
- 第三个概念：少样本——与其费劲描述，不如直接给个范例决定方案是否可以进入下一阶段。

<Exercises>

**问题 1**：为“提示工程精要”写出最小输入合同，并指出一个不能省略的边界条件。

<Answer>
  输入合同至少包含样本身份、版本、配置、成功标准和清理方式；边界条件必须能改变结构或状态，而不是只换一段描述。
</Answer>

**问题 2**：构造一个只破坏“第一个概念：提示工程，调的是「喂给模型的那段话」”的单故障样本，第一条预期证据是什么？

<Answer>
  先冻结正常轨迹，只改变该条件；第一条证据应是最早依赖它的结构化中间产物，而不是最终错误消息。
</Answer>

**问题 3**：什么情况下不应采用本页方案？

<Answer>
  当变化轴稳定、直接实现更清晰、额外层次无法降低风险或提供独立测试边界时，应选择更简单方案并记录重新评估触发条件。
</Answer>

</Exercises>

## 术语表

<Glossary>
  <GlossaryItem term="提示工程精要">
    提示工程精要用于描述“提示工程精要”中的输入、结构、约束或证据边界，必须能在实验记录中定位。
  </GlossaryItem>
  <GlossaryItem term="先打个比方">
    先打个比方用于描述“提示工程精要”中的输入、结构、约束或证据边界，必须能在实验记录中定位。
  </GlossaryItem>
  <GlossaryItem term="第一个概念：提示工程，调的是「喂给模型的那段话」">
    第一个概念：提示工程，调的是「喂给模型的那段话」用于描述“提示工程精要”中的输入、结构、约束或证据边界，必须能在实验记录中定位。
  </GlossaryItem>
  <GlossaryItem term="第二个概念：清晰具体——同样一句话，模糊和具体差到天上">
    第二个概念：清晰具体——同样一句话，模糊和具体差到天上用于描述“提示工程精要”中的输入、结构、约束或证据边界，必须能在实验记录中定位。
  </GlossaryItem>
  <GlossaryItem term="第三个概念：少样本——与其费劲描述，不如直接给个范例">
    第三个概念：少样本——与其费劲描述，不如直接给个范例用于描述“提示工程精要”中的输入、结构、约束或证据边界，必须能在实验记录中定位。
  </GlossaryItem>
</Glossary>

<Attribution
  adaptedFrom="站内应用专题《AI 智能体应用开发》，依据Anthropic官方Agent模式与工具工程资料，2026-07-18核定 · 独立教学重构"
  adaptedUrl="https://www.anthropic.com/engineering/building-effective-agents"
/>

{/* A 概念型章·第二篇「上下文工程」开篇：提示工程 = 上下文工程里最可控的一环（精心设计「喂给模型的指令」）。WHAT + WHY 为主。主 viz = PromptRefinementDiagram（animotor 6 步「左右两栏 + 逐步」：左侧提示累加部件、右侧输出随之变好）。math:false 整节省略。原创、无 Attribution。知识点清单（动笔先行，每项至少配一种 viz）：1. 提示的解剖（角色/任务/上下文/约束/示例/输出格式 六部件）→ <PromptAnatomyDiagram />（静态拆块图）；2. 清晰具体（模糊 vs 具体）→ <CompareSlider>（左模糊右具体两段文本）；3. 少样本 few-shot（给几个例子照着学）→ <FewShotDiagram />（0-shot 瞎猜 vs few-shot 对版两栏）；4. 思维链 CoT（一步步推更准）→ <CotDiagram />（直接答错 vs 分步推对两栏）；5. 逐步精化一个提示【主 viz·核心】→ <PromptRefinementDiagram />（animotor 6 步）；6. 系统提示 vs 单次任务提示 → 并入 PromptAnatomyDiagram 标注 + Callout tip。Term 高亮术语：提示工程 / 系统提示 / 少样本 few-shot / 思维链 chain-of-thought / 零样本 zero-shot / 输出格式 → 均进章末 Glossary，id 对齐 */}

{/* ===== 1. 学习目标｜spec §一：A 型必含「能解释 角色/示例/思维链 各起什么作用」；末条检验问题 ===== */}

<Objectives>

- 能用自己的话说清「提示工程」是什么，并说明它为什么是上下文工程里最可控的一环
- 能解释 角色、示例（少样本）、思维链 在一个提示里各起什么作用、分别治什么病
- 能回答：同一段需求，写给 agent 当系统提示，和写成一次性的单次任务提示，到底差在哪？

</Objectives>

{/* ===== 2. 直觉引入｜spec §二：≤3 段 × 零术语；类比「交代任务」 ===== */}

## 先打个比方

想象你请人帮你办一件事。你只甩一句「你看着办」，对方多半办得一塌糊涂——不是他笨，是你没说清楚。换个说法：先告诉他**你希望他以什么身份来办**（「就当你是我们店的老客服」），再讲清**到底要办成什么样**，给他**一个办好的范例**照着学，最后让他**别急着拍板、先把思路理一遍再动手**——同一个人，办出来的结果天差地别。

模型也一样。它本身的「脑子」没变，但你**喂给它的那段话**写得好不好，直接决定它办成什么样。把任务交代清楚、给个范例、让它先想再答——这门「把话说到位」的手艺，就是这一章要讲的东西。

它还是这一整篇（上下文工程）里**最容易上手、最立竿见影**的一环：不用改模型、不用接数据库，只要把喂进去的那段话改好，输出当场就不一样。所以我们从它开篇。

{/* ===== 3. 概念讲解｜spec §三：一节一概念，配图；术语首现 <Term def id> 写句中；新核心概念 ≤5 ===== */}

## 第一个概念：提示工程，调的是「喂给模型的那段话」

把上面那件事翻译成技术语言，就是 <Term def="提示工程（Prompt Engineering）：通过精心设计「喂给模型的那段输入文字」来引导模型给出更好输出的手艺。它不改模型权重，只改输入——是上下文工程里最直接、最可控的一环。" id="prompt-engineering">提示工程</Term>。注意它的边界：它**不碰模型本身**，只打磨你递进去的那段话。模型是同一个模型，换一段写得更好的话，输出就脱胎换骨——这正是它「最可控」的原因：成本最低、反馈最快、人人能改。

那一段「写得好」的话，长什么样？它不是随口一句，而是由几块固定的部件拼成的：

<PromptAnatomyDiagram />

盯住这六块：**角色**（让它扮谁）、**任务**（到底要它做什么）、**上下文**（做这件事要的背景料）、**约束**（边界与禁区）、**示例**（给一两个范例照着学），以及 <Term def="输出格式（Output Format）：提示里对「答案该长什么样」的明确约定，比如「只输出一个词」「分两行」「用 JSON」。把格式说死能让输出整齐、好被下游程序解析。" id="output-format">输出格式</Term>（答案长什么样）。不是每个提示都要齐六块，但你越缺哪块、模型就越容易在哪块上跑偏。提示工程的日常，就是照着这张清单，看自己的提示**缺了哪块、该补哪块**。

## 第二个概念：清晰具体——同样一句话，模糊和具体差到天上

六块里最容易偷懒、也最致命的，是把**任务**写得含含糊糊。模型不会读心，你说得有多含糊，它就猜得有多离谱。下面拖动滑块，对比同一个需求的「模糊版」和「具体版」分别会得到什么：

> 猜一猜：左边那句模糊的「帮我写个文案」，模型会写出什么？它知道是给谁看、要多长、什么口吻吗？拖到右边看看，把同样的需求说具体之后，输出会规整多少。

<CompareSlider
  leftLabel="模糊（模型只能瞎猜）"
  rightLabel="具体（模型有谱了）"
  initial={42}
  left={
    <div className="not-prose flex h-full flex-col justify-center gap-2 rounded-card border border-danger bg-bg p-5">
      <p className="text-sm font-bold text-danger">😵 模糊提示</p>
      <p className="font-mono text-sm text-primary">「帮我写个文案。」</p>
      <p className="mt-2 text-xs text-secondary">
        给谁看？多长？什么口吻？卖什么？——全靠模型瞎猜，十次十个样，多半跑题。
      </p>
    </div>
  }
  right={
    <div className="not-prose flex h-full flex-col justify-center gap-2 rounded-card border border-accent bg-bg p-5">
      <p className="text-sm font-bold text-accent">🎯 具体提示</p>
      <p className="font-mono text-sm text-primary">
        「给一款保温杯写朋友圈文案，面向上班族，≤ 40 字，口吻轻松、带一个
        emoji。」
      </p>
      <p className="mt-2 text-xs text-secondary">
        受众、长度、口吻、对象全说清——模型有谱了，输出当场对版。
      </p>
    </div>
  }
/>

差别就一句话：**模糊提示把决策权全甩给了模型，具体提示替它把关键决策都定死了。** 你每多写一个具体约束（受众、长度、口吻、范围），就少给模型一次跑偏的机会。这是提示工程里回报最高的习惯——几乎不花成本，却最管用。

## 第三个概念：少样本——与其费劲描述，不如直接给个范例

有些东西，用话描述特别费劲，给个例子却一秒说清——比如「我要的输出格式」。这就引出 <Term def="少样本（few-shot）：在提示里塞进几个「输入 → 期望输出」的范例，让模型照着这些例子的格式和口径来做新任务。和它相对的是零样本——一个例子都不给，直接让模型做。" id="few-shot">少样本</Term>：在提示里先放几个「输入 → 输出」的范例，模型照着学。它的对照面叫 <Term def="零样本（zero-shot）：一个范例都不给，只描述任务就让模型直接做。简单任务够用，但格式 / 口径稍复杂时模型容易凭自己的理解瞎猜。" id="zero-shot">零样本</Term>——一个例子都不给。

<FewShotDiagram />

看出门道了吗：零样本时模型没见过你要的格式，只能凭自己的理解瞎猜，输出又长又不对版；少样本时它有范例可循，照着套就齐整了。**经验法则：凡是格式 / 口径稍微复杂一点的任务，给两三个范例，往往比堆一大段格式描述更管用、更省事。**

## 第四个概念：思维链——让它先一步步想，再给答案

最后一招专治「复杂推理一上来就答错」。<Term def="思维链（Chain-of-Thought，CoT）：在提示里要求模型把推理过程一步步写出来、最后再给答案，而不是张口就报结论。摊开中间步骤后，每步都可检验，复杂推理 / 多步计算的准确率明显提升。" id="chain-of-thought">思维链</Term> 的做法极简单：在提示里加一句「请一步步推理，再给出答案」，逼模型把中间过程摊开。

<CotDiagram />

道理很朴素：复杂题逼模型「一步到位」，它容易在心算里漏掉一步（左栏漏算了「又买 2 篮」）；让它**先把步骤一条条写出来再下结论**，每一步都看得见、可检验，自然更准（右栏）。代价是输出更长、更慢——所以它用在**真需要推理的复杂题**上，简单事实问答用不着。

## 把四招连起来：系统提示 vs 单次任务提示

上面四招（具体、少样本、思维链，加上六部件）都是在打磨「那段话」。但这段话挂在哪儿，有两种挂法，新手常分不清：

一种是写成 <Term def="系统提示（System Prompt）：在每一轮对话开头都会自动带上的那段固定指令，用来设定 agent 的身份、目标、规矩、能用的工具和说话风格。它是 agent 的「人设与守则」，一次写定、轮轮生效。" id="system-prompt">系统提示</Term>——给 agent 立一次「人设与守则」（你是谁、目标、规矩、口吻），之后**每一轮都自动带上**。另一种是临时为**这一次任务**写的提示，办完就丢，下一次重写。同一套部件，差别只在「写一次轮轮复用」还是「一次性临时拼」。

<Callout type="tip">
  经验法则：**稳定不变的写进系统提示，因任务而变的留给单次提示。**
  角色、口吻、红线、通用规矩——一次写进系统提示，省得每轮重复；而「这次具体要处理的内容、这次特有的少样本范例」则放进单次任务提示。给
  agent
  写系统提示尤其要克制：它每轮都占上下文窗口，写得越臃肿越贵、越容易稀释重点。
</Callout>

{/* ===== 4. 数学推导｜math:false：整节删除 ===== */}

{/* ===== 5. 交互 Demo｜spec §五：主 viz = PromptRefinementDiagram；猜一猜引导；可暂停/单步/拖进度 ===== */}

## 动手看：一个提示怎么一步步「精化」到能用

前面几招是分开讲的，真做事时它们是**一块块叠上去**的。下面这个动画把这个过程演给你看——任务是「写一句给老顾客的退货提醒」，从一句模糊提示开始，每一步加一块部件，看右边的输出怎么一截截变好。

> 猜一猜：只把第 ① 步那句模糊的「写点东西」，加上第 ② 步的一个「角色」（你是资深客服），输出会有多大变化？是从「完全跑题」直接变「能用」，还是只先把口吻掰对、内容仍欠火候？先想一想，再单步播放第 ①→② 步看答案。

<PromptRefinementDiagram />

盯住一件事：左边每加一块部件（角色 → 上下文 → 约束 → 示例 → 格式），右边的输出就**好一截**，底下的质量条就**长一段**。提示工程的增益不是某一句「魔法咒语」带来的，而是这样**一块块攒**出来的——这也正是为什么它「最可控」：每一块的效果你都能单独看见、单独调。

{/* ===== 6. 代码对照｜spec §六：A 型贴 1–2 段最能说明概念的示例；单块 ≤30 行，块间有解说 ===== */}

## 用代码看一个「带范例 + 定格式」的提示长什么样

上面动画里精化好的提示，落到代码里就是一个 messages 数组：开头是系统提示（人设 + 规矩），中间塞几条少样本范例，最后才是这次真要处理的输入。下面这段就是它的骨架：

```python
messages = [
    # ① 系统提示：每轮都带的人设 + 规矩（含输出格式约束）
    {"role": "system", "content":
        "你是资深电商客服。判断顾客这一单能否退并给出理由。"
        "只依据给定政策；政策没写的就说「需人工」。"
        "输出两行：① 能否退 ② 一句话理由。"},

    # ② 少样本范例：给一组「输入 → 期望输出」让模型照着学
    {"role": "user", "content": "未拆封，下单第 3 天，普通商品。"},
    {"role": "assistant", "content": "① 可退\n② 在 7 天可退期内，且未拆封。"},

    # ③ 这次真要处理的输入（单次任务部分）
    {"role": "user", "content": "已拆封，下单第 9 天，生鲜。"},
]
```

逐块对照看：`system` 那条就是**系统提示**——角色、约束、输出格式都写在这里，**每轮复用**；中间一对 `user` / `assistant` 是一条**少样本范例**，告诉模型「碰到这种输入，就照这个两行格式答」；最后那条 `user` 才是**这一次**真要判断的订单，属于单次任务部分。

如果这道题需要推理（比如政策有多条、要逐条核对），只需在系统提示里加一句，就把**思维链**也接上了：

```python
    # 在系统提示末尾追加一句，要求先推理再下结论
    "...先逐条核对政策再下结论，最后只输出上面规定的两行。"
```

注意这一句的措辞：**先**逐条核对（摊开推理过程），但**最后只输出**规定的两行格式。这样既拿到了思维链的准确率，又不会让一堆推理过程污染最终给用户看的输出——把「想」和「答」分开，是思维链落地时的常用技巧。

{/* ===== 7. 常见误区｜spec §七：A 型 ≥2 个；固定 现象→原因→修法 三行格式 ===== */}

## 容易踩的坑

<Callout type="trap">
  **现象**：「我把提示写得老长老长，恨不得把所有要求都塞进去，输出反而更糊、还更贵了。」
  **原因**：把「提示越长越好」当成了真理。其实啰嗦的提示会**稀释重点**——关键约束被淹没在一堆废话里，模型抓不住，还白白多烧
  token。 **修法**：长 ≠
  好，**清晰具体**才好。删掉空话套话，每一句都对准一个明确决策（受众 / 长度 /
  口吻 / 范围 / 格式）；稳定的挪进系统提示复用，别在单次提示里反复堆。
</Callout>

<Callout type="trap">
  **现象**：复杂的输出格式，光用文字描述「我要这样那样的格式」，模型还是时对时错、对不齐。
  **原因**：复杂格式靠「描述」远不如靠「范例」准。文字描述容易有歧义，模型只能猜你到底要哪种；而一个具体的
  **少样本范例**，把格式直接摆在它面前，照着套就行。
  **修法**：与其多写两段格式说明，不如给两三个「输入 →
  期望输出」的范例（few-shot）。复杂格式 / 口径，给例子几乎总比给描述强。
</Callout>

<Callout type="trap">
  **现象**：让模型做需要几步推理的题（多步计算、逻辑判断），它张口就给个结论，经常是错的。
  **原因**：逼它「一步到位」，中间步骤全在心算里走，漏一步就错——它没把过程摊开，你也无从发现哪步错了。
  **修法**：对真需要推理的复杂题，加一句「请一步步推理再给答案」（**思维链**），让它先摊开过程；简单事实问答则不必，免得徒增啰嗦与开销。
</Callout>

{/* ===== 8. 小结与练习｜spec §八：小结 ≤5 条与目标呼应；练习含 1 道「改提示」实践题；答案折叠 ===== */}

## 小结

- 提示工程 = 打磨「喂给模型的那段话」，不改模型、成本最低、反馈最快——是上下文工程里最可控的一环
- 一个好提示由六块拼成：角色 / 任务 / 上下文 / 约束 / 示例 / 输出格式，缺哪块就在哪块跑偏
- 清晰具体替模型定死关键决策；少样本（few-shot）用范例教它格式；思维链让它先想再答、专治复杂推理
- 稳定不变的写进系统提示（轮轮复用），因任务而变的留给单次任务提示
- 提示的增益是一块块「精化」攒出来的，不是某句魔法咒语——每块效果都能单独看见、单独调

<Exercises>

**练习 1（改提示 · 实践题）** 下面是一个烂提示，请你按「六部件」清单把它补全成一个像样的提示：

> 「帮我把用户评论分个类。」

提示：想想它缺了哪些部件——它扮谁？分成哪几类？有没有给范例？要什么输出格式？

<Answer>

它几乎只有「任务」一块，且任务本身还很含糊。按六部件补全后，大致长这样：

- **角色**：你是一名电商评论分析助手。
- **任务**：把每条用户评论分类。
- **上下文 / 约束**：类别只有三种——「正面 / 负面 / 中性」，无法判断时归「中性」；只依据评论文字本身。
- **示例（少样本）**：`真香，下次还买 → 正面`；`等了半个月还没到 → 负面`。
- **输出格式**：每条只输出一个词（正面 / 负面 / 中性），不要解释。

要点：原提示把「分成哪几类、什么格式、拿不准怎么办」全甩给了模型去猜。补上约束（限定类别）、示例（给范例定口径）、输出格式（只要一个词）之后，模型才有谱、输出才稳定可用。

</Answer>

**练习 2（辨析题）** 下面三件事，分别该靠「清晰具体」「少样本」还是「思维链」来解决？为什么？
（A）模型总把回答写得太长，你只想要一句话。
（B）模型分类时格式总对不齐，你要它严格输出固定的几个标签之一。
（C）一道要算好几步的应用题，模型张口就报错答案。

<Answer>

（A）靠**清晰具体**——在提示里把约束写死：「只用一句话、≤ 20 字回答」。这是把含糊的期望变成明确约束。
（B）靠**少样本**——给两三个「输入 → 标签」范例，让它照着对版；复杂 / 固定的格式，范例比描述更准。
（C）靠**思维链**——加一句「请一步步推理再给答案」，逼它摊开中间步骤，多步计算才不容易漏错。
判别诀窍：要它「按规矩来」→ 说清楚（具体）/ 给范例（少样本）；要它「别漏算」→ 让它先想（思维链）。

</Answer>

**练习 3（思考题）** 给一个客服 agent 写提示时，下面这些内容，哪些该放进「系统提示」、哪些该放进「单次任务提示」？为什么？
（甲）「你是某电商的客服，语气亲切、绝不承诺政策外的退款。」
（乙）「这位顾客的订单号是 20231001，问的是能不能退。」
（丙）「输出永远分两行：能否退 + 一句话理由。」

<Answer>

- **甲、丙 放系统提示**：角色 / 口吻 / 红线（甲）和固定输出格式（丙）都是**稳定不变、每轮都该生效**的规矩——写进系统提示一次定死、轮轮复用，省得每次重复，也保证一致。
- **乙 放单次任务提示**：具体订单号、这次的问题是**因任务而变**的内容，每次都不一样，属于这一次性的输入。

要点：判断标准就一条——**这条内容会不会每轮都一样**。会，放系统提示；不会，放单次提示。还要记得系统提示每轮都占上下文窗口，别把一次性的东西塞进去，徒增成本又稀释重点。

</Answer>

</Exercises>

{/* ===== 9. 出处声明｜原创内容，不写 Attribution ===== */}

{/* ===== 10. 名词解释｜spec §十：与正文 Term 高亮一一对应，零基础大白话；id 与 Term 对齐 ===== */}

## 名词解释

<Glossary>
  <GlossaryItem term="提示工程（Prompt Engineering）" id="prompt-engineering">
    通过精心设计「喂给模型的那段输入文字」来引导模型给出更好输出的手艺。它不动模型权重，只打磨输入——所以成本最低、反馈最快、人人能改，是上下文工程里最直接、最可控的一环。详见本章「第一个概念」。
  </GlossaryItem>
  <GlossaryItem term="系统提示（System Prompt）" id="system-prompt">
    每一轮对话开头都会自动带上的那段固定指令，用来设定 agent
    的身份、目标、规矩、可用工具和说话风格——好比给它立一份「人设与守则」，一次写定、轮轮生效。稳定不变的内容才该写进来。详见本章「把四招连起来」。
  </GlossaryItem>
  <GlossaryItem term="少样本（few-shot）" id="few-shot">
    在提示里塞进几个「输入 →
    期望输出」的范例，让模型照着这些例子的格式和口径做新任务。复杂格式靠范例比靠文字描述更准。详见本章「第三个概念」。
  </GlossaryItem>
  <GlossaryItem term="零样本（zero-shot）" id="zero-shot">
    一个范例都不给，只描述任务就让模型直接做。简单任务够用，但格式 /
    口径稍复杂时模型容易凭自己的理解瞎猜——它是少样本的对照面。详见本章「第三个概念」。
  </GlossaryItem>
  <GlossaryItem term="思维链（Chain-of-Thought，CoT）" id="chain-of-thought">
    在提示里要求模型把推理过程一步步写出来、最后再给答案，而不是张口就报结论。摊开中间步骤后每步可检验，多步计算
    /
    复杂推理的准确率明显提升；代价是输出更长更慢，简单问答用不着。详见本章「第四个概念」。
  </GlossaryItem>
  <GlossaryItem term="输出格式（Output Format）" id="output-format">
    提示里对「答案该长什么样」的明确约定，比如「只输出一个词」「分两行：能否退 +
    理由」「用
    JSON」。把格式说死能让输出整齐、好被下游程序解析，是六部件里最实用的一块。详见本章「第一个概念」。
  </GlossaryItem>
</Glossary>
