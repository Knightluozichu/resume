---
title: Function Calling 原理
type: A
section: 工具使用
order: 7
description: 模型怎么"看见"工具、又凭什么决定调哪个——拆开 function calling 的决策机制，而不只是消息格式。
demo: true
math: false
draft: false
sourceUrl: "https://www.anthropic.com/engineering/building-effective-agents"
---

import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释Function Calling 原理的输入、核心结构、状态变化和输出合同
- 能绘制并修改Function Calling 原理的最小模型，比较至少一种替代方案
- 能复现正常、边界和单故障样本，定位第一条证据分叉
- 能根据成本与风险判断方案适用范围，并完成清理重建

</Objectives>

## 为什么只记结论不足以掌握Function Calling 原理

学习“Function Calling 原理”时，第一步不是记住结论，而是冻结输入、上下文、版本和成功标准。只有这些条件明确，Function Calling 原理的含义才不会随着样例变化。正文已有的概念说明要与结构图、运行轨迹和失败样本互相印证，不能只凭最终输出看似正确就宣布完成。

结构分析从先打个比方开始：列出参与者、职责、连接方向、生命周期和所有权，再沿正常路径追踪数据或控制流。每一条边都要说明为什么存在、谁创建、谁消费、谁负责清理；如果边界被跨越，必须能在证据中找到第一处异常。

机制验证要把第一个概念：工具是怎么进入模型「视野」的写成可以执行的条件。正常样本证明主路径，恰好边界样本验证等号和空值，单故障样本只破坏一个假设。三类样本使用同一份观察指标，避免因为测试口径变化而把偶然结果误认成规律。

成本分析同时记录时间、空间、延迟、耦合、可维护性和不可逆操作。第二个概念：模型怎么从菜单里挑一个——工具选择不是一句“可能失败”，而是可复现输入、预期停点、实际轨迹、错误分类与清理步骤。任何自动重试都要有次数、预算和幂等边界。

方案比较不能只列优点。需要给出直接实现、当前方案和至少一个替代方案，逐项比较复杂度、扩展点、故障隔离和团队认知成本。当问题规模很小或变化轴稳定时，更简单的实现往往更好；模式与框架必须由真实变化压力证明。

实现阶段把大结论拆成可检查的中间产物：配置快照、结构清单、状态转移、输入输出样本、日志摘要和测试结果。每个产物带来源与生成命令，下一阶段只消费已通过门禁的版本，避免旧缓存或隐式默认值污染结论。

解释结果时必须区分相关性与因果性、接口承诺与实现细节、设计意图与运行事实。对“第三个概念：挑中之后，多个调用是并排发还是排队发”的判断要由独立证据支持，并明确适用范围；一旦输入分布、版本、硬件或组织边界改变，就重新运行最小实验。

复盘从首个分叉开始，而不是从最后一个报错倒推。先比较冻结输入，再比较第一份结构化中间产物，随后检查状态、约束和副作用。这样可以把复杂系统的排错范围收缩到一个阶段，避免在多个层次同时修改造成新的不确定性。

迁移到真实项目时，先选择一个最小但有代表性的切片，保存改造前基线，再逐步引入“Function Calling 原理”中的机制。每一步只改变一个变量并保留回滚点；性能、正确性、安全性和可理解性至少各有一项可量化指标。

最终验收要求读者能脱离页面重新画出结构、口述关键链路、实现最小版本、构造一个反例并解释失败位置。若只能复述名词而不能预测中间状态，说明知识仍停留在识记层，需要回到图示和实验重新验证。

本页用<Term def="Function Calling 原理连接Function Calling 原理的机制、边界与可复查证据。">Function Calling 原理</Term>、

<Term def="先打个比方连接Function Calling 原理的机制、边界与可复查证据。">先打个比方</Term>、<Term def="第一个概念：工具是怎么进入模型「视野」的连接Function Calling 原理的机制、边界与可复查证据。">第一个概念：工具是怎么进入模型「视野」的</Term>、<Term def="第二个概念：模型怎么从菜单里挑一个——工具选择连接Function Calling 原理的机制、边界与可复查证据。">第二个概念：模型怎么从菜单里挑一个——工具选择</Term>、<Term def="第三个概念：挑中之后，多个调用是并排发还是排队发连接Function Calling 原理的机制、边界与可复查证据。">第三个概念：挑中之后，多个调用是并排发还是排队发</Term>
建立统一坐标。先预测这些概念在结构图和运行轨迹中的位置，再操作实验控件；如果结果与预测不一致，停止在首个分叉，不要用后续补丁掩盖早期错误。

## 权威目录与核心概念逐项对照

- Function Calling 原理
- 先打个比方
- 第一个概念：工具是怎么进入模型「视野」的
- 第二个概念：模型怎么从菜单里挑一个——工具选择
- 第三个概念：挑中之后，多个调用是并排发还是排队发
- 动手看：模型在工具菜单里挑中一个的全过程
- 用代码看那份「菜单」长什么样
- 容易踩的坑

<Callout type="trap">
  <strong>误区：</strong>
  看到最终结果正确就认为机制正确。中间状态可能已经偏离，只是被后续默认值或重试偶然修补。应保存首个结构化产物并逐阶段比较。
</Callout>

<Callout type="trap">
  <strong>误区：</strong>
  为了使用模式或框架而制造抽象。若变化轴、失败边界和替代方案没有写清，新增层次只会扩大认知成本和故障面。
</Callout>

## 可复现的最小实现

先把决策记录写成机器可读结构：

```json
{
  "unit": "Function Calling 原理",
  "inputFrozen": true,
  "scenario": "normal | boundary | single-fault",
  "firstDivergence": null,
  "cleanupRequired": true
}
```

再用同一条执行链处理三类样本：

```ts
type Evidence = { stage: string; expected: string; actual: string };

function verify(sample: unknown, expected: readonly Evidence[]) {
  const trace = runFromCleanState(sample);
  return expected.find((item, index) => trace[index]?.actual !== item.expected);
}
```

最后保存回归门禁，禁止失败样本静默通过：

```text
normal      -> complete, invariant preserved
boundary    -> complete or explicit rejection
singleFault -> stop at first divergence, no stale output
```

用统一评分解释实验结果：

$$
Q = C_{correct} + C_{trace} + C_{recover} - R_{residual}
$$

$$
C_{coverage} = \frac{N_{verified}}{N_{official}}
$$

$$
R_{residual} = P(failure) \times I(impact)
$$

$$
Accept = (C_{coverage} \ge 0.90) \land (R_{residual} \le R_{budget})
$$

## 本章回顾

- Function Calling 原理必须绑定冻结输入和明确成功标准。
- 先打个比方必须能画成结构并沿边追踪责任。
- 第一个概念：工具是怎么进入模型「视野」的要由正常、边界和单故障样本共同验证。
- 第二个概念：模型怎么从菜单里挑一个——工具选择必须保存第一处偏离和清理重建步骤。
- 第三个概念：挑中之后，多个调用是并排发还是排队发决定方案是否可以进入下一阶段。

<Exercises>

**问题 1**：为“Function Calling 原理”写出最小输入合同，并指出一个不能省略的边界条件。

<Answer>
  输入合同至少包含样本身份、版本、配置、成功标准和清理方式；边界条件必须能改变结构或状态，而不是只换一段描述。
</Answer>

**问题 2**：构造一个只破坏“第一个概念：工具是怎么进入模型「视野」的”的单故障样本，第一条预期证据是什么？

<Answer>
  先冻结正常轨迹，只改变该条件；第一条证据应是最早依赖它的结构化中间产物，而不是最终错误消息。
</Answer>

**问题 3**：什么情况下不应采用本页方案？

<Answer>
  当变化轴稳定、直接实现更清晰、额外层次无法降低风险或提供独立测试边界时，应选择更简单方案并记录重新评估触发条件。
</Answer>

</Exercises>

## 术语表

<Glossary>
  <GlossaryItem term="Function Calling 原理">
    Function Calling 原理用于描述“Function Calling
    原理”中的输入、结构、约束或证据边界，必须能在实验记录中定位。
  </GlossaryItem>
  <GlossaryItem term="先打个比方">
    先打个比方用于描述“Function Calling
    原理”中的输入、结构、约束或证据边界，必须能在实验记录中定位。
  </GlossaryItem>
  <GlossaryItem term="第一个概念：工具是怎么进入模型「视野」的">
    第一个概念：工具是怎么进入模型「视野」的用于描述“Function Calling
    原理”中的输入、结构、约束或证据边界，必须能在实验记录中定位。
  </GlossaryItem>
  <GlossaryItem term="第二个概念：模型怎么从菜单里挑一个——工具选择">
    第二个概念：模型怎么从菜单里挑一个——工具选择用于描述“Function Calling
    原理”中的输入、结构、约束或证据边界，必须能在实验记录中定位。
  </GlossaryItem>
  <GlossaryItem term="第三个概念：挑中之后，多个调用是并排发还是排队发">
    第三个概念：挑中之后，多个调用是并排发还是排队发用于描述“Function Calling
    原理”中的输入、结构、约束或证据边界，必须能在实验记录中定位。
  </GlossaryItem>
</Glossary>

<Attribution
  adaptedFrom="站内应用专题《AI 智能体应用开发》，依据Anthropic官方Agent模式与工具工程资料，2026-07-18核定 · 独立教学重构"
  adaptedUrl="https://www.anthropic.com/engineering/building-effective-agents"
/>

{/* A 概念型章（第三篇「工具使用」开篇）：讲清「模型侧」function calling 的决策机制——工具怎么进入模型视野、模型凭什么从中挑一个、并行还是串行。区别于 Ch7「工具调用协议」讲的消息往返格式，本章绝不重复 tool_calls 解析/回填那套线路协议。主 viz = FunctionCallingDecisionDiagram（animotor 5 步：request→read-intent→match→pick→emit）。math:false 整节省略。原创、无 Attribution。知识点清单（动笔先行，每项至少配一种 viz）：1. 工具怎么进入模型视野（schema 序列化注入上下文）→ <ToolsAsMenuDiagram />（静态：定义卡→注入→工具菜单）；2. 工具选择（拿意图比对 description 挑一个）【含「description 起什么作用」】→ 并入主 viz；3. 并行 vs 串行调用 → <ParallelVsSequentialDiagram />（静态两栏）；4. 模型怎么挑工具【主 viz·核心】→ <FunctionCallingDecisionDiagram />（animotor 5 步决策动画）。Term 高亮术语：Function Calling / 工具 schema 注入 tool-schema-injection / 工具选择 tool-selection / 并行工具调用 parallel-tool-calls / 串行工具调用 sequential-tool-calls → 均进章末 Glossary，id 对齐 */}

{/* ===== 1. 学习目标｜spec §一：A 型必含「能解释 ___ 在智能体里的位置」；末条检验问题 ===== */}

<Objectives>

- 能说清工具是怎么进入模型「视野」的，以及模型在智能体里凭什么决定调哪个工具
- 能解释工具定义里的 description 在模型选工具时起什么作用，并判断两个调用该并行还是串行
- 能回答：给模型配了一堆工具，但用户问的事跟谁都不沾边，模型会怎么做？

</Objectives>

{/* ===== 2. 直觉引入｜spec §二：≤3 段 × 零术语；类比统一（工具箱清单/照活儿挑趁手的）===== */}

## 先打个比方

想象你递给一个新来的师傅一份**工具箱清单**：每一件工具旁边都写着一句话——「这把是拧十字螺丝的」「这把是剪铁丝的」「这把是量长度的」。师傅接到活儿，并不会把工具箱翻个底朝天，而是照着这份清单上的那句话，挑出最趁手的那件。

挑得准不准，几乎全看那句话写得清不清楚。要是两件工具旁边都写着「拧东西用的」，师傅就只能瞎抓一件；要是清单上压根没有能干这活儿的工具，他就会停下来说「这活儿我手头没工具，我直接告诉你怎么办」。

模型用工具——这套让模型自己决定调哪个工具的能力，叫 <Term def="让模型在回答里『提议调用某个外部工具/函数』的能力：你把工具定义喂给它，它据此判断该不该调、调哪个、传什么参数，并以结构化形式给出这个调用意图；真正执行函数的是你的程序。" id="function-calling">Function Calling</Term>——也是一模一样的道理。这一章不讲工具调用的消息格式（那是上一章的事），只盯一件事：模型到底**怎么「看见」那份清单、又凭什么从中挑出一件**。看不懂这一层，你就只会埋怨「模型怎么老挑错」，却不知道病根其实在你写的那句话上。

{/* ===== 3. 概念讲解｜spec §三：一节一概念；术语首现 <Term def id>；每概念配图；新核心概念 ≤5 ===== */}

## 第一个概念：工具是怎么进入模型「视野」的

模型并没有「内置」你的函数——它看到工具的唯一途径，是你把工具的定义当**文本**塞进这一轮的上下文里。这个动作叫 <Term def="工具 schema 注入（Tool Schema Injection）：把每个工具的定义——函数名、参数形状、一句 description——序列化成文本，放进这一轮发给模型的上下文里。模型据此「看见」有哪些工具可用，而不是「内置」了这些函数。" id="tool-schema-injection">工具 schema 注入</Term>：把每个工具的名字、参数、一句描述序列化成一段文字，注入上下文。下面这张图把这条路画了出来：

<ToolsAsMenuDiagram />

盯住从左到右这一程：左边是你写的工具定义，中间被序列化成文本注入上下文，右边模型就「看见」了一份**工具菜单**——每项 = 一个名字 + 一句描述。所以记住一句话：**模型对工具的全部认知，就来自菜单上那几行字。** 这几行写得清不清楚、准不准确，直接决定了模型能不能用对。这也是为什么后面要反复强调那句 description。

## 第二个概念：模型怎么从菜单里挑一个——工具选择

模型「看见」菜单之后，下一步就是 <Term def="工具选择（Tool Selection）：模型读懂用户意图后，从注入上下文的那份工具菜单里挑出最该调用的那个（或判定都不合适、不调）的过程。它靠的是拿用户意图去逐个比对各工具的 description。" id="tool-selection">工具选择</Term>：读懂用户到底想干嘛，再拿这份意图去对每个工具的描述，挑出最贴的那个。这里的关键抓手，就是工具定义里那句 **description**。

description 是专门写给模型看的——它是模型判断「这个工具到底干嘛、什么时候该用」的主要依据。名字（name）只像菜单上的菜名，给模型一个对号的标签；真正说清用途、让模型拿意图去比对的，是那句描述。所以描述写得越清楚、越贴近真实用途，模型挑得越准；含糊或几个工具写得雷同，它就容易点错菜。**这一步具体怎么转，下一节的动画会一帧一帧演给你看。**

## 第三个概念：挑中之后，多个调用是并排发还是排队发

模型挑工具，有时一次只挑一个，有时一口气挑好几个。这就引出一对概念：<Term def="并行工具调用（Parallel Tool Calls）：模型在一轮里一次性提议多个彼此独立、互不依赖的调用，它们可以同时发出、一起拿回结果，省时间。" id="parallel-tool-calls">并行工具调用</Term> 和 <Term def="串行工具调用（Sequential Tool Calls）：后一个调用要用到前一个调用的结果，只能排队——先调 A 拿到结果，再把它当输入去调 B。调用之间存在数据依赖时只能串行。" id="sequential-tool-calls">串行工具调用</Term>。一张图分清：

<ParallelVsSequentialDiagram />

决定走哪条路的，就一件事——**调用之间有没有「数据依赖」**。查订单和查天气是两件互不相干的事，谁也不等谁，模型一轮里同时提议、一起拿回结果，省时间，这就是并行；而「先查到退货政策、再按政策看这单还在不在可退期内」，后一步要吃前一步的结果，就只能先 A 后 B 排成一条链，这就是串行。**彼此独立 → 并行；后者吃前者输出 → 串行。** 这个判断，正是评估一个 agent 设计好不好的常见考点。

{/* ===== 4. 数学推导｜math:false：整节删除 ===== */}

{/* ===== 5. 交互 Demo｜spec §五：主 viz = FunctionCallingDecisionDiagram；猜一猜引导；可暂停/单步/拖进度 ===== */}

## 动手看：模型在工具菜单里挑中一个的全过程

前面三个概念里，**「模型怎么挑」**最难凭文字想象。下面这个动画把它一步步拆开——用一个具体场景：用户问「我的快递到哪了？」，菜单里有查订单、查天气、搜文档三个工具。

> 猜一猜：动画第 ③ 步，模型拿用户意图逐个比对每个工具的 description。如果有**两个工具的 description 写得差不多**，到第 ④ 步它会怎样？是稳稳挑中一个，还是来回犹豫、随机挑一个？先想一想，再单步播放到 ③、④ 步对比着看。

<FunctionCallingDecisionDiagram />

盯住这条决策链：① 用户意图 + 整份工具菜单一起进入模型视野 → ② 模型先读懂用户到底想要什么（「快递到哪了」= 想查订单状态）→ ③ 拿这份意图去逐个比对每个工具的 description，估匹配度 → ④ 挑中最贴的 `check_order`，其余压暗（**若都不贴 → 不调任何工具、直接用文字答**）→ ⑤ 产出调用意图「调 `check_order(order_id=…)`」。要害有两个：一是模型挑得准不准，全压在「比对 description」这一步，所以那句描述是你能攥在手里的方向盘；二是它自始至终只「**提议**」，从不自己执行——真去跑函数的是 app 的代码。

{/* ===== 6. 代码对照｜spec §六：A 型只贴最能说明概念的 1–2 段；单块 ≤30 行；不重复 Ch7 的 tool_calls 解析/回填 ===== */}

## 用代码看那份「菜单」长什么样

A 型章不展开实现（解析、回填那套上一章已经讲透）。这里只贴最能说明本章概念的一段——你交给模型的 `tools` 列表里，两三个工具各自的 `name` 和 `description`：

```python
tools = [
    {
        "name": "check_order",
        # description 才是模型选它的依据：写清「干嘛的 + 何时调」
        "description": "查某一笔具体订单的实时状态（到货进度 / 能否退货）；问到「我的单到哪了 / 能退吗」时调",
    },
    {
        "name": "search_docs",
        # 与 check_order 划清边界：查通用政策条文，不针对具体某一单
        "description": "搜通用帮助文档与政策条文（如退货规则、运费说明）；不针对某一笔具体订单",
    },
    {
        "name": "get_weather",
        "description": "查某城市的实时天气；问到天气、冷热、下不下雨时调",
    },
]
```

逐行对照前面那张菜单图看：模型看到的就是这几行字。`name` 只是标签，**真正让它分得清「这活儿该点哪道菜」的，是每条 `description`**——它写得清不清、几个工具之间边界划得开不开，直接决定模型挑得准不准。注意 `check_order` 和 `search_docs` 都跟「订单/退货」沾边，所以两句描述特意写成职责互斥（一个查「这一单的具体状态」、一个查「通用政策条文」），免得模型挑花眼。这一段没有任何调用解析或结果回填——那是上一章工具调用协议的事，本章只管「模型凭这几行字怎么挑」。

{/* ===== 7. 常见误区｜spec §七：A 型 ≥2 个；固定 现象→原因→修法 三行格式 ===== */}

## 容易踩的坑

<Callout type="trap">
  **现象**：给 agent
  配了几十个工具，越配越全，结果模型反而经常挑错工具、或该调的漏调。
  **原因**：工具太多 = 菜单太长，模型要在一大堆 description
  里逐个比对，干扰项一多就「挑花眼」——这正是工具选择那一步被噪声淹没了。
  **修法**：精简工具集，按场景只注入当下相关的子集；合并职责重叠的工具；把每个
  description 写得更可区分。本质是减少模型在「比对 description」时的干扰项。
</Callout>

<Callout type="trap">
  **现象**：两个工具的 description
  写得几乎一样（都像「查订单相关信息」），模型一会儿挑这个、一会儿挑那个，来回犯错。
  **原因**：description 是模型区分工具的唯一抓手，写雷同 =
  这个抓手失效了，两者匹配度难分高下，模型只能随机挑。根子是工具设计上职责重叠、边界不清。
  **修法**：要么把两个工具合并成一个；要么把两句描述改成明确互斥、各写清独有的触发场景，让模型有据可分（如本章代码里
  `check_order` 与 `search_docs` 的划法）。
</Callout>

<Callout type="trap">
  **现象**：默认「只要配了工具，模型每轮都会调一个」，于是代码里不写「模型没调工具」这条分支，线上撞到就崩。
  **原因**：模型逐个比对后，若没有工具的 description
  贴合用户意图，它会**不调任何工具、直接用文字答**——「有工具」不等于「必然调工具」。
  **修法**：把「模型没调工具、直接给了文字答案」当成一条正常分支兜住：每次回复先判断有没有调用意图，没有就直接把它的文字答案返回给用户。
</Callout>

{/* ===== 8. 小结与练习｜spec §八：小结 ≤5 条与目标呼应；含 1 道场景判断题；答案折叠 ===== */}

## 小结

- 模型不「内置」工具：靠工具 schema 注入，把名字 + 参数 + 描述当文本塞进上下文，它才「看见」一份工具菜单
- 工具选择 = 读懂用户意图 → 拿意图逐个比对每个工具的 description → 挑最贴的；都不贴就不调、直接答
- description 是模型选工具的主要依据：写得清不清、几个工具边界划得开不开，直接决定挑得准不准
- 调用之间有没有数据依赖决定并行还是串行：彼此独立 → 并行省时间；后者吃前者输出 → 只能串行
- 模型只「提议」调用、从不自己执行；工具太多或描述雷同会让它挑花眼、乱挑

<Exercises>

**练习 1（辨析题）** 下面三句对「模型怎么挑工具」的说法，哪些对、哪些错？说明理由。
（A）「模型是把我所有函数的源代码读了一遍，才知道有哪些工具。」
（B）「模型主要靠工具的 name 来挑，description 只是注释，写不写无所谓。」
（C）「就算配了工具，用户问的事跟谁都不沾边时，模型可以一个都不调、直接答。」

<Answer>

（A）**错**。模型看不到你的源代码，它看到的只是被序列化注入上下文的工具 schema——名字 + 参数 + 一句 description 这几行文本。这就是「工具 schema 注入」。
（B）**错**，而且是常见误解。name 只是个对号的标签，真正说清「这个工具干嘛、何时该用」、让模型拿意图去比对的是 description。描述写不好，名字再漂亮也照样挑错。
（C）**对**。模型逐个比对后若没有工具贴合意图，就不调任何工具、直接用文字答。「有工具」不等于「必然调工具」，这条分支代码必须兜住。

</Answer>

**练习 2（场景判断题 · 判断会不会调 / 调哪个）** 一个客服 agent 的菜单里有：`check_order`（查某一笔具体订单的实时状态：到货 / 退货资格）、`search_docs`（搜通用帮助文档 / 政策条文）、`get_weather`（查天气）。下面三个用户输入，模型分别会调哪个工具，还是不调？为什么？
（甲）「我下周想去三亚玩，那边这几天热不热？」
（乙）「你们家七天无理由退货具体是怎么规定的？」
（丙）「我的订单 A123 现在还能退吗？」

<Answer>

（甲）调 **`get_weather`**——意图是问某地天气，最贴 `get_weather` 的描述；另两个工具跟天气不沾边，匹配度低被压下去。
（乙）调 **`search_docs`**——问的是「通用政策怎么规定」，不针对某一笔具体订单，正落在 `search_docs`「搜政策条文」的描述里；`check_order` 是查「某一单的具体状态」，边界不同。
（丙）调 **`check_order`**——「我这单 A123 能不能退」要的是**这一笔具体订单**的退货资格，正是 `check_order` 的用途；这里能挑准，靠的就是 `check_order` 和 `search_docs` 的 description 把「具体某单」和「通用政策」划开了。
判别诀窍：把用户意图和每条 description 对一遍，挑最贴的；都不贴就不调。

</Answer>

**练习 3（设计题）** 你发现自己的旅行助手老是「查机票」和「查酒店」两个工具用混。结合本章原理，说说病根可能在哪、怎么改才能让模型挑得更准。

<Answer>

病根八成在两个工具的 **description 写得太像、职责边界不清**——因为 description 是模型区分工具的唯一抓手，描述雷同时它的匹配度难分高下，只能乱挑。
改法：把两句描述改成**明确互斥、各写清独有的触发场景**，例如 `search_flights` 写「查城市间航班的班次 / 价格 / 时刻，问到机票、航班、飞过去时调」，`search_hotels` 写「查目的地酒店的房型 / 价格 / 空房，问到住宿、订房、住哪时调」。再顺手做两件事：删掉或合并职责重叠的多余工具，减少干扰项；工具实在多时按场景只注入相关子集。把「比对 description」这一步的噪声降下来，模型挑选行为就稳定可预期，而不是碰运气。

</Answer>

</Exercises>

{/* ===== 9. 出处声明｜原创内容，不写 Attribution ===== */}

{/* ===== 10. 名词解释｜spec §十：与正文 Term 高亮一一对应，id 对齐，零基础大白话 ===== */}

## 名词解释

<Glossary>
  <GlossaryItem term="Function Calling" id="function-calling">
    让模型在回答里「提议调用某个外部工具 /
    函数」的能力：你把工具定义喂给它，它据此判断该不该调、调哪个、传什么参数，并以结构化形式给出这个调用意图。注意它产出的是**意图**，真正执行函数的是你的程序。本章讲的是它的「模型侧决策机制」，不是消息格式。详见全章。
  </GlossaryItem>
  <GlossaryItem
    term="工具 schema 注入（Tool Schema Injection）"
    id="tool-schema-injection"
  >
    把每个工具的定义——函数名、参数形状、一句
    description——序列化成文本，放进这一轮发给模型的上下文里。模型并没有「内置」你的函数，它是像看菜单一样在上下文里「看见」有哪些工具可用。模型对工具的全部认知，就来自这几行字。详见本章「第一个概念」。
  </GlossaryItem>
  <GlossaryItem term="工具选择（Tool Selection）" id="tool-selection">
    模型读懂用户意图后，从注入上下文的那份工具菜单里挑出最该调用的那个（或判定都不合适、不调）的过程。它靠的是拿用户意图去逐个比对各工具的
    description，选匹配度最高的——所以描述写得清不清，直接决定它挑得准不准。详见本章「第二个概念」与「动手看」。
  </GlossaryItem>
  <GlossaryItem
    term="并行工具调用（Parallel Tool Calls）"
    id="parallel-tool-calls"
  >
    模型在一轮里一次性提议多个**彼此独立、互不依赖**的调用（比如同时查订单又查天气），它们可以同时发出、一起拿回结果，省时间。能不能并行，看调用之间有没有数据依赖。详见本章「第三个概念」。
  </GlossaryItem>
  <GlossaryItem
    term="串行工具调用（Sequential Tool Calls）"
    id="sequential-tool-calls"
  >
    后一个调用要用到前一个调用的结果，只能排队：先调 A
    拿到结果，再把它当输入去调
    B。调用之间存在**数据依赖**时（如先查到政策、再按政策判断某单），就只能串成一条链。详见本章「第三个概念」。
  </GlossaryItem>
</Glossary>
