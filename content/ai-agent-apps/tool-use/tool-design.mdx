---
title: 设计好用的工具
type: D
section: 工具使用
order: 8
description: 同一个工具，设计好坏让 agent 的表现天差地别——命名、描述、粒度、错误信息、返回值，逐项看好坏对比。
demo: true
math: false
draft: false
sourceUrl: "https://www.anthropic.com/engineering/building-effective-agents"
---

import { CompareSlider } from "@/components/mdx/compare-slider";
import { ToolGranularityDiagram } from "@/components/mdx/diagrams/tool-granularity-diagram";
import { ToolErrorRecoveryDiagram } from "@/components/mdx/diagrams/tool-error-recovery-diagram";


## 为什么只记结论不足以掌握设计好用的工具

学习“设计好用的工具”时，第一步不是记住结论，而是冻结输入、上下文、版本和成功标准。只有这些条件明确，设计好用的工具的含义才不会随着样例变化。正文已有的概念说明要与结构图、运行轨迹和失败样本互相印证，不能只凭最终输出看似正确就宣布完成。

结构分析从先打个比方开始：列出参与者、职责、连接方向、生命周期和所有权，再沿正常路径追踪数据或控制流。每一条边都要说明为什么存在、谁创建、谁消费、谁负责清理；如果边界被跨越，必须能在证据中找到第一处异常。

机制验证要把先看全景：好工具的五个维度写成可以执行的条件。正常样本证明主路径，恰好边界样本验证等号和空值，单故障样本只破坏一个假设。三类样本使用同一份观察指标，避免因为测试口径变化而把偶然结果误认成规律。

成本分析同时记录时间、空间、延迟、耦合、可维护性和不可逆操作。第一个取舍：工具粒度——不大不小才好用不是一句“可能失败”，而是可复现输入、预期停点、实际轨迹、错误分类与清理步骤。任何自动重试都要有次数、预算和幂等边界。

方案比较不能只列优点。需要给出直接实现、当前方案和至少一个替代方案，逐项比较复杂度、扩展点、故障隔离和团队认知成本。当问题规模很小或变化轴稳定时，更简单的实现往往更好；模式与框架必须由真实变化压力证明。

实现阶段把大结论拆成可检查的中间产物：配置快照、结构清单、状态转移、输入输出样本、日志摘要和测试结果。每个产物带来源与生成命令，下一阶段只消费已通过门禁的版本，避免旧缓存或隐式默认值污染结论。

解释结果时必须区分相关性与因果性、接口承诺与实现细节、设计意图与运行事实。对“第二个关键：错误信息——决定 agent 能不能自己爬起来”的判断要由独立证据支持，并明确适用范围；一旦输入分布、版本、硬件或组织边界改变，就重新运行最小实验。

复盘从首个分叉开始，而不是从最后一个报错倒推。先比较冻结输入，再比较第一份结构化中间产物，随后检查状态、约束和副作用。这样可以把复杂系统的排错范围收缩到一个阶段，避免在多个层次同时修改造成新的不确定性。

迁移到真实项目时，先选择一个最小但有代表性的切片，保存改造前基线，再逐步引入“设计好用的工具”中的机制。每一步只改变一个变量并保留回滚点；性能、正确性、安全性和可理解性至少各有一项可量化指标。

最终验收要求读者能脱离页面重新画出结构、口述关键链路、实现最小版本、构造一个反例并解释失败位置。若只能复述名词而不能预测中间状态，说明知识仍停留在识记层，需要回到图示和实验重新验证。

本页用、

、、、建立统一坐标。先预测这些概念在结构图和运行轨迹中的位置，再操作实验控件；如果结果与预测不一致，停止在首个分叉，不要用后续补丁掩盖早期错误。

## 权威目录与核心概念逐项对照

- 设计好用的工具
- 先打个比方
- 先看全景：好工具的五个维度
- 第一个取舍：工具粒度——不大不小才好用
- 第二个关键：错误信息——决定 agent 能不能自己爬起来
- 第三个关键：返回值——给精料，别甩一地原料
- 动手看：错误信息的好坏，怎么让 agent 一个爬起来、一个躺平
- 代码对照：同一个工具，坏的写法 vs 好的写法


## 可复现的最小实现

先把决策记录写成机器可读结构：

```json
{
  "unit": "设计好用的工具",
  "inputFrozen": true,
  "scenario": "normal | boundary | single-fault",
  "firstDivergence": null,
  "cleanupRequired": true
}
```

再用同一条执行链处理三类样本：

```ts
type Evidence = { stage: string; expected: string; actual: string };

function verify(sample: unknown, expected: readonly Evidence[]) {
  const trace = runFromCleanState(sample);
  return expected.find((item, index) => trace[index]?.actual !== item.expected);
}
```

最后保存回归门禁，禁止失败样本静默通过：

```text
normal      -> complete, invariant preserved
boundary    -> complete or explicit rejection
singleFault -> stop at first divergence, no stale output
```

用统一评分解释实验结果：

$$
Q = C_{correct} + C_{trace} + C_{recover} - R_{residual}
$$

$$
C_{coverage} = \frac{N_{verified}}{N_{official}}
$$

$$
R_{residual} = P(failure) \times I(impact)
$$

$$
Accept = (C_{coverage} \ge 0.90) \land (R_{residual} \le R_{budget})
$$

## 本章回顾

- 设计好用的工具必须绑定冻结输入和明确成功标准。
- 先打个比方必须能画成结构并沿边追踪责任。
- 先看全景：好工具的五个维度要由正常、边界和单故障样本共同验证。
- 第一个取舍：工具粒度——不大不小才好用必须保存第一处偏离和清理重建步骤。
- 第二个关键：错误信息——决定 agent 能不能自己爬起来决定方案是否可以进入下一阶段。


## 术语表


{/* D 对比型章（第三篇「工具使用」第 2 章）：承接 Ch8「模型靠 description 挑工具」，本章往下讲「怎么把工具设计得好用」——核心是方案差异与取舍。五个维度（命名/描述/粒度/错误信息/返回值）逐项好坏对照。不重复 Ch8 的「模型怎么挑」机制。主 viz = ToolErrorRecoveryDiagram（animotor 4 步两路对照：错误信息质量 → agent 能不能自救）。math:false 整节省略。原创、无 Attribution。知识点 → viz 清单（动笔先行）：1. 五个维度总览 →（静态表，每维一句好/坏对照）；2. 好工具 vs 坏工具主对比【D 型】→ <CompareSlider>（左坏右好两个 JSX 面板）；3. 工具粒度取舍 → <ToolGranularityDiagram />（静态三态：太细/合适/太粗）；4. 错误信息设计【主动画·核心】→ <ToolErrorRecoveryDiagram />（animotor 两路对照）。Term 高亮术语：工具粒度 tool-granularity / 可恢复错误 recoverable-error / 结构化返回 structured-return / 职责单一原则 single-responsibility → 均进章末 Glossary，id 对齐 */}

{/* ===== 1. 学习目标｜spec §一：D 型含一条「能说出什么场景用细/粗粒度工具」；末条检验问题 ===== */}


{/* ===== 2. 直觉引入｜spec §二：≤3 段 × 零术语；类比统一（给师傅的工具趁不趁手）===== */}

## 先打个比方

还是上一章那个新来的师傅。上一章我们说，他照着工具旁边的那句话挑趁手的家伙——但挑中之后用得顺不顺，是另一回事。你递给他的工具本身，称不称手？

想想一把真正好用的工具：手柄握着称手（**命名**清楚），旁边的说明贴得明明白白（**描述**到位），它干的活儿不大不小刚刚好（**粒度**合适），万一卡住了会「咔」一声提示你「该换刀片了」而不是默默崩坏（**错误信息**友好），干完活儿给你的是一件成品而不是一地零件（**返回值**干净）。换成一把烂工具——名字看不懂、没说明、要么细到拧颗螺丝得换五次头、要么大到一把抵整个工具箱、坏了一声不吭、用完扔给你一堆没归拢的料——再聪明的师傅也得被它拖垮。

agent 用工具也一样：**模型再会挑，工具本身设计得糟，它照样办不成事。** 这一章不讲「模型怎么挑」（那是上一章的事），只讲一件事——**怎么把工具设计得让 agent 用着顺手**。我们会沿着五个维度，把「好工具」和「坏工具」一项一项摆在一起对照着看。

{/* ===== 3. 概念讲解｜spec §三：一节一概念；术语首现——一个工具一次到底该管多大一件事。这件事没有「越细越好」的标准答案，太细和太粗各有各的坑。一张图把三态摆在一起：

<ToolGranularityDiagram />

看这三栏：拆得**太细**（左），办成「订一张票」要让 agent 把 search、pick、pay、issue 一长串调用串起来，链路越长越慢、越容易在中途某一步翻车——这叫组合爆炸；揉得**太粗**（右），一个工具把订票、改签、退票、查天气全包了，参数一大堆，模型分不清这次到底想用它干哪件事，反而更易选错、传错。**合适**的那栏（中）刚刚好：一个 `book_train_ticket` 工具，一次调用办成一件完整的事。这正是 在工具上的体现——一个工具，一件事。

## 第二个关键：错误信息——决定 agent 能不能自己爬起来

工具会失败，这很正常。真正拉开差距的是：**失败时你返回给模型的，是一句它能照做的话，还是一个它看不懂的裸异常。** 这就引出。

为什么这件事对 agent 格外要命？因为 agent 在那一刻**手里只有工具返回的这段文字**——它看不到你的日志、查不了文档、问不了同事。信息够，它就能自己纠正参数、重试成功；信息不够（一个光秃秃的 `KeyError`），它就只能原样再试一次、卡死，或者瞎猜乱试。下一节的动画会把这条岔路一帧一帧演给你看。

## 第三个关键：返回值——给精料，别甩一地原料

最后一项是工具**返回**什么。新手最常犯的错，是把工具拿到的原始数据（整段 API 的 raw JSON、整张数据库表）原样丢回给模型。正确的做法是——把结果整理成字段清晰、只留要紧信息的形状。

原样丢一大坨有两宗罪：一是把宝贵的上下文窗口塞满，挤掉真正要紧的信息、还更费钱；二是要紧的几个字段被埋在几十个噪声字段里，模型更难抓重点、更容易答偏。好的返回值像一份**摘要**：替模型把噪声筛掉，只留它和用户真正用得上的那几项。

{/* ===== 4. 数学推导｜math:false：整节删除 ===== */}

{/* ===== 5. 交互 Demo｜spec §五：主 viz = ToolErrorRecoveryDiagram；猜一猜引导；可暂停/单步/拖进度 ===== */}

## 动手看：错误信息的好坏，怎么让 agent 一个爬起来、一个躺平

前面三个取舍里，**「错误信息怎么影响 agent」**最难凭文字想象。下面这个动画把它演出来——同一次工具调用失败（agent 传了个格式不对的 `order_id`），左右两路只有一个差别：工具返回的错误信息写得好不好。

> 猜一猜：左路的工具只抛了一个光秃秃的 `KeyError`，没说哪里错、该怎么改。agent 下一步会怎么做？是看懂了、把参数改对再试，还是看不懂、原样把同一个调用再撞一次？先想一想，再单步播放到第 ③、④ 步对比着看。

<ToolErrorRecoveryDiagram />

盯住这条岔路：① 两路从同一个失败的调用出发 → ② 工具返回——**分叉就在这一步**：左路抛裸异常 `KeyError`，右路返回「格式应为 A + 4 位数字，你传的是 '123'」→ ③ agent 看到后，左路看不懂、只能原样重试，右路读懂了、主动把参数改成 `'A0123'` → ④ 结局：左路还是同一个错、卡死，右路这次成功、自己把错纠了过来。一句话记住：**错误信息不是写给日志看的，是写给一个只能读这段字的执行者看的操作指引。** 写清「错在哪 + 怎么改」，agent 就能自救；甩个裸异常，它就只能躺平。

{/* ===== 6. 代码对照｜spec §六：D 型用并排 diff 突出差异；单块 ≤30 行；块间解说差异行 ===== */}

## 代码对照：同一个工具，坏的写法 vs 好的写法

把上面五个维度落到代码里。下面拖动滑块，对比同一个「查订单」工具的**坏写法**和**好写法**——注意名字、描述、错误、返回这几处的差异：

> 猜一猜：左边那个 `get_data`、抛裸异常、返回整行 raw 记录的写法，交给 agent 会处处别扭在哪？拖到右边看看，把五个维度都做对之后，工具会「干净」多少。

<CompareSlider
  leftLabel="坏工具（处处别扭）"
  rightLabel="好工具（处处顺手）"
  initial={44}
  left={
    <div className="not-prose flex h-full flex-col justify-center gap-1 rounded-card border border-danger bg-bg p-5">
      <p className="text-sm font-bold text-danger">✗ 坏工具</p>
      <pre className="overflow-x-auto font-mono text-xs leading-relaxed text-primary">
        {`def get_data(x):          # 名字含糊：干嘛的全靠猜
    # 没有 description
    row = db[x]            # x 不存在就抛裸异常 KeyError
    return str(row)        # 返回整行 raw 记录，一大坨`}
      </pre>
      <p className="mt-1 text-xs text-secondary">
        含糊名 + 无描述 + 裸异常 + raw 返回——agent
        既挑不准、出错也救不回、还撑爆上下文。
      </p>
    </div>
  }
  right={
    <div className="not-prose flex h-full flex-col justify-center gap-1 rounded-card border border-accent bg-bg p-5">
      <p className="text-sm font-bold text-accent">✓ 好工具</p>
      <pre className="overflow-x-auto font-mono text-xs leading-relaxed text-primary">
        {`def get_order_status(order_id):   # 名字一看就懂
    """查某一笔订单的实时状态；order_id 形如 A0123"""
    if not valid(order_id):       # 失败给可照做的提示
        return {"error": f"order_id 应为 A+4位，"
                f"你传的是 &#123;order_id!r&#125;，请改正后重试"}
    o = db[order_id]
    return {"status": o.status,   # 结构化、只留要紧字段
            "can_refund": o.can_refund}`}
      </pre>
      <p className="mt-1 text-xs text-secondary">
        清晰名 + 精确描述 + 可恢复错误 + 结构化精简返回——agent
        挑得准、出错能自救、上下文不浪费。
      </p>
    </div>
  }
/>

逐处对照这两段的差异行：**名字**从 `get_data` 改成 `get_order_status`（含糊 → 一眼懂）；**描述**从「没有」改成一句写清用途和参数格式的 docstring；**错误**从「`x` 不存在直接抛 `KeyError`」改成「返回一句说清『错在哪 + 怎么改』的可恢复错误」；**返回值**从「`str(row)` 整行 raw」改成「只留 `status` 和 `can_refund` 两个要紧字段的结构化对象」。五处差异，对应五个维度——左边那个看着能跑，真交给 agent 处处是坑。

{/* ===== 7. 常见误区｜spec §七：D 型 ≥2 个，必含 1 个「场景选错方案」坑；固定 现象→原因→修法 三行 ===== */}

## 容易踩的坑


{/* ===== 8. 小结与练习｜spec §八：小结 ≤5 条呼应目标；2-3 题含 1 道选型题；Answer 折叠 ===== */}

## 小结

- 工具好不好用，看五个维度：命名、描述、粒度、错误信息、返回值——每项都有好坏之分
- 粒度取舍：太细组合爆炸、太粗职责不清；「一个工具办成一件完整的事」（职责单一）通常最好
- 错误信息决定 agent 能不能自救：裸异常让它卡死，「错在哪 + 怎么改」的可恢复错误让它纠正重试
- 返回值要结构化、只留要紧字段：原样甩 raw 数据会撑爆上下文、淹没重点
- 模型再会挑工具，工具本身设计得糟，agent 照样办不成事——好工具是「挑得准」之后用得顺的前提


{/* ===== 9. 出处声明｜原创内容，不写 Attribution ===== */}

{/* ===== 10. 名词解释｜spec §十：与正文 Term 高亮一一对应，id 对齐，零基础大白话 ===== */}

## 名词解释

