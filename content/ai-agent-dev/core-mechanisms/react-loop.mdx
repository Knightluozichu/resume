---
title: ReAct：推理与行动循环
type: A
section: 核心机制
order: 3
description: 读完能说清 ReAct 的「思考→行动→观察」循环怎么把推理和动手交织起来，并解释它为什么比只闷头推理的纯思维链更靠谱。
demo: true
math: false
sourceUrl: "https://www.anthropic.com/engineering/building-effective-agents"
draft: false
---

import { ReActTraceDiagram } from "@/components/mdx/diagrams/react-trace-diagram";
import { ReActStepAnatomy } from "@/components/mdx/demos/react-step-anatomy";


## 为什么只记结论不足以掌握ReAct：推理与行动循环

学习“ReAct：推理与行动循环”时，第一步不是记住结论，而是冻结输入、上下文、版本和成功标准。只有这些条件明确，ReAct：推理与行动循环的含义才不会随着样例变化。正文已有的概念说明要与结构图、运行轨迹和失败样本互相印证，不能只凭最终输出看似正确就宣布完成。

结构分析从小特办事，是边想边查边调整的开始：列出参与者、职责、连接方向、生命周期和所有权，再沿正常路径追踪数据或控制流。每一条边都要说明为什么存在、谁创建、谁消费、谁负责清理；如果边界被跨越，必须能在证据中找到第一处异常。

机制验证要把ReAct：把「想」和「干」拧成一股绳写成可以执行的条件。正常样本证明主路径，恰好边界样本验证等号和空值，单故障样本只破坏一个假设。三类样本使用同一份观察指标，避免因为测试口径变化而把偶然结果误认成规律。

成本分析同时记录时间、空间、延迟、耦合、可维护性和不可逆操作。Thought：把「我现在在想什么」写出来不是一句“可能失败”，而是可复现输入、预期停点、实际轨迹、错误分类与清理步骤。任何自动重试都要有次数、预算和幂等边界。

方案比较不能只列优点。需要给出直接实现、当前方案和至少一个替代方案，逐项比较复杂度、扩展点、故障隔离和团队认知成本。当问题规模很小或变化轴稳定时，更简单的实现往往更好；模式与框架必须由真实变化压力证明。

实现阶段把大结论拆成可检查的中间产物：配置快照、结构清单、状态转移、输入输出样本、日志摘要和测试结果。每个产物带来源与生成命令，下一阶段只消费已通过门禁的版本，避免旧缓存或隐式默认值污染结论。

解释结果时必须区分相关性与因果性、接口承诺与实现细节、设计意图与运行事实。对“Action 与 Observation：一个去做，一个把结果带回来”的判断要由独立证据支持，并明确适用范围；一旦输入分布、版本、硬件或组织边界改变，就重新运行最小实验。

复盘从首个分叉开始，而不是从最后一个报错倒推。先比较冻结输入，再比较第一份结构化中间产物，随后检查状态、约束和副作用。这样可以把复杂系统的排错范围收缩到一个阶段，避免在多个层次同时修改造成新的不确定性。

迁移到真实项目时，先选择一个最小但有代表性的切片，保存改造前基线，再逐步引入“ReAct：推理与行动循环”中的机制。每一步只改变一个变量并保留回滚点；性能、正确性、安全性和可理解性至少各有一项可量化指标。

最终验收要求读者能脱离页面重新画出结构、口述关键链路、实现最小版本、构造一个反例并解释失败位置。若只能复述名词而不能预测中间状态，说明知识仍停留在识记层，需要回到图示和实验重新验证。

本页用、

、、、建立统一坐标。先预测这些概念在结构图和运行轨迹中的位置，再操作实验控件；如果结果与预测不一致，停止在首个分叉，不要用后续补丁掩盖早期错误。

## 权威目录与核心概念逐项对照

- ReAct：推理与行动循环
- 小特办事，是边想边查边调整的
- ReAct：把「想」和「干」拧成一股绳
- Thought：把「我现在在想什么」写出来
- Action 与 Observation：一个去做，一个把结果带回来
- ReAct vs 纯 CoT：一个去查，一个瞎猜
- 动手一：拆开一轮 ReAct，看清谁写的、谁填的
- 动手二：同一个问题，ReAct 怎么把纯 CoT 比下去


## 可复现的最小实现

先把决策记录写成机器可读结构：

```json
{
  "unit": "ReAct：推理与行动循环",
  "inputFrozen": true,
  "scenario": "normal | boundary | single-fault",
  "firstDivergence": null,
  "cleanupRequired": true
}
```

再用同一条执行链处理三类样本：

```ts
type Evidence = { stage: string; expected: string; actual: string };

function verify(sample: unknown, expected: readonly Evidence[]) {
  const trace = runFromCleanState(sample);
  return expected.find((item, index) => trace[index]?.actual !== item.expected);
}
```

最后保存回归门禁，禁止失败样本静默通过：

```text
normal      -> complete, invariant preserved
boundary    -> complete or explicit rejection
singleFault -> stop at first divergence, no stale output
```

用统一评分解释实验结果：

$$
Q = C_{correct} + C_{trace} + C_{recover} - R_{residual}
$$

$$
C_{coverage} = \frac{N_{verified}}{N_{official}}
$$

$$
R_{residual} = P(failure) \times I(impact)
$$

$$
Accept = (C_{coverage} \ge 0.90) \land (R_{residual} \le R_{budget})
$$

## 本章回顾

- ReAct：推理与行动循环必须绑定冻结输入和明确成功标准。
- 小特办事，是边想边查边调整的必须能画成结构并沿边追踪责任。
- ReAct：把「想」和「干」拧成一股绳要由正常、边界和单故障样本共同验证。
- Thought：把「我现在在想什么」写出来必须保存第一处偏离和清理重建步骤。
- Action 与 Observation：一个去做，一个把结果带回来决定方案是否可以进入下一阶段。


## 术语表


{/* A 概念型·核心机制篇第 1 章：承接基础篇「LLM 会幻觉、不知实时信息」，讲清 ReAct 这套「让它去查而非空想」的提示/执行模式。重心在概念讲解(§3)与三大 viz，数学整节删(math:false)，代码只放一段 ReAct while 循环骨架呼应 trace 图。知识点清单：① ReAct 范式（思考→行动→观察交织成循环）= <ReActTraceDiagram /> anime.js 旗舰逐卡点亮动画 + Glossary；② Thought 推理轨迹 = trace 图首两卡 + <ReActStepAnatomy /> 交互拆解 + Glossary；③ Action 与 Observation = <ReActStepAnatomy /> 三标签交互（点明 Observation 是系统填回的）+ 代码 §6 + Glossary；④ ReAct vs 纯 CoT = 对比交互（实时问题：闷头推理瞎猜 vs 推理+实证）+ Glossary。承接：第2章说 LLM 会幻觉、不知实时信息 → ReAct 正是「让它去查而非空想」的机制。 */}

{/* 1. 学习目标（spec §一）：A 型必含「能解释 Thought/Action/Observation 各是什么」；末条是可当场自测的检验问题。 */}


{/* 2. 直觉引入（spec §二）：≤3 段 × ≤120 字；零术语；回答「解决什么问题/没有它会怎样」；用小特边想边查边调整、不是闷头一口气想完。 */}

## 小特办事，是边想边查边调整的

你让私人助理**小特**：「看看今天上海要不要带伞。」一个靠谱的小特不会闷头一拍脑袋说「带吧」——他会先想「这得看天气」，然后真的去查一下上海今天的预报，看到「小雨」，才回你「带伞」。

注意他这一套：**想一下 → 动手查 → 看到结果 → 再接着想**，一小步一小步交替着来，而不是关在屋里凭空把整件事想完。每动一步，他都拿到了新信息，下一步才想得更准。

这一章就讲：怎么让一个 AI 也这样「边想边干边看」地办事。没有它会怎样？AI 只能闷头硬想——可它根本不知道上海今天下不下雨，于是只能瞎猜一个，还说得特别笃定。

{/* 3. 概念讲解（spec §三，A 型重心）：一小节一概念；术语首现。

它的名字就是 **Re**asoning（推理）+ **Act**ing（行动）拼起来的，干的事正是把这两件事**拧成一股绳**：

- 光推理（想）不够——它不知道实时信息，只能瞎猜；
- 光行动（干）不够——它得先想清楚该查什么、再想清楚查到的结果说明什么。

所以 ReAct 让 LLM **想一步、干一步、看一步，再接着想**，循环往复。这正好呼应第 1 章那条 Agent 循环（感知→决策→行动→观察）——只不过 ReAct 是它落到「一段提示 + 一轮轮执行」上的**具体打法**：每一轮，LLM 都显式写出自己的思考、决定下一个动作，系统执行后把结果喂回去，再转下一轮。

下面这张图把一条具体的 ReAct 轨迹逐轮点亮给你看，点播放，跟着小特走一遍「看今天上海要不要带伞」：

<ReActTraceDiagram />

看那个节奏：它从来不是「一口气把答案想出来」，而是**想→查→看→再想→答**，一环扣一环。中间「行动」那步真的去调了工具、「观察」那步拿回了真实数据——这就是 ReAct 比闷头空想靠谱的根。

## Thought：把「我现在在想什么」写出来

这一圈里，每一轮 LLM 先做的事叫——它把「我现在该想什么、下一步干什么」**显式地写出来**。

比如小特写：「要回答带不带伞，得先知道上海今天的天气，我自己不知道，得去查。」这句话不直接解决问题，但它把模型的**判断过程摊到了明面上**。好处有两个：一是我们能跟着看它怎么想的、想错了能发现；二是它自己写下来后，下一步该干嘛就顺理成章了。这就是「让推理看得见」——而不是黑箱里一团乱麻。

## Action 与 Observation：一个去做，一个把结果带回来

Thought 之后，LLM 决定动手，这一步叫：它写出一个**要调用的动作**，通常就是「调哪个工具、传什么参数」。比如小特写 `weather("上海")`——意思是「去调那个查天气的工具，参数是上海」。

系统照着这个 Action 真的把工具跑一遍，再把返回的结果填回来，这一步叫：比如「小雨，降水 60%」。**这条结果是系统真去查回来的，不是大脑编的**——这是 ReAct 全章最关键、也最容易被搞混的一点。

把这一轮三部分连起来看：`Thought`（想）和 `Action`（做什么）是 **LLM 自己写的**，`Observation`（结果）是 **系统填回的**。下个小节的 Demo 你可以点三个标签，亲眼看清这条分界线在哪。

## ReAct vs 纯 CoT：一个去查，一个瞎猜

你可能会问：让 LLM「一步步想」不是早就有了吗？是的，那叫。但 CoT 只会**在脑子里想**，从头到尾不碰外部世界。

差别就在这：问一个**需要实时信息**的问题——「现在从北京去上海最快的高铁几点发车」——纯 CoT 不知道今天的时刻表，只能凭印象编一个（可能是幻觉）；ReAct 则会先想「我不知道，得去查」，然后**真去调时刻表工具**，拿到真实数据再回答。下面这个 Demo 把两条路摆一起，切换看看它们答同一个问题的天差地别：


一句话记牢：**CoT 是闷头推理，ReAct 是推理 + 实证**。凡是需要实时、事实信息的任务（查天气、查时刻表、搜最新资料、算具体数），就不能只靠 CoT 空想，必须用 ReAct 让它去查。

{/* 4. 数学推导（spec §四）：math:false，整节省略。 */}

{/* 5. 交互 Demo（spec §五）：放 <ReActStepAnatomy /> 与（已在 §3 末引用，这里给「猜一猜」引导深入玩），各带「猜一猜」。 */}

## 动手一：拆开一轮 ReAct，看清谁写的、谁填的

光读不如自己点。下面这个 Demo 把小特「看今天上海要不要带伞」其中一轮的 ReAct 文本摊开，分成 `Thought`、`Action`、`Observation` 三行。点上方三个标签，对应那行高亮，旁注告诉你它是「LLM 自己写的」还是「系统填回的」。

> 猜一猜：这三行里，`Observation: 小雨，降水 60%` 这条天气结果，是 LLM 自己「想」出来的，还是系统真去查回来填上的？先猜一个，再点「Observation」标签验证。

<ReActStepAnatomy />

你会看到一条清楚的分界：`Thought` 和 `Action` 是 LLM 生成的（它自己想、自己决定调什么工具），而 `Observation` 是**系统执行工具后填回**的真实数据。搞混这条线，是初学者最常踩的坑——以为 Observation 也是模型编的，那 ReAct 就和瞎猜没区别了。

## 动手二：同一个问题，ReAct 怎么把纯 CoT 比下去

回到上一节那个对比 Demo，这次自己拨开关玩。问题是「现在从北京去上海最快的高铁几点发车」——一个 LLM 脑子里没有实时答案的问题。

> 猜一猜：纯 CoT 那条路，它不知道今天的时刻表，最后会怎么收尾？是老实说「我不知道」，还是自信地编一个发车时间出来？关掉开关切到「纯 CoT」看看。


切到纯 CoT，你会看到它一路推理，最后**自信地编了个 07:00**（标红——可能是幻觉）；切回 ReAct，它先承认「我不知道，得查」，调时刻表工具拿到真实的 06:20，答案才靠谱（标绿）。这就是为什么：需要事实的任务，光会「想」不够，得会「查」。

{/* 6. 代码对照（spec §六，A 型选，只贴 1 段最能说明概念的）：一段 ReAct while 循环骨架，呼应 ReActTraceDiagram；块前后有解说。 */}

## 代码长什么样：一段 ReAct 循环骨架

上面那张 trace 图，翻译成代码，核心也是一个 `while` 循环：让 LLM 产出 Thought + Action，系统执行 Action 拿到 Observation，把它拼回对话，再转下一轮——直到 LLM 决定收尾。下面这段 Python 骨架（把真实工具细节省了）就是 ReAct 的心脏：

```python
def run_react(question):
    messages = [{"role": "user", "content": question}]
    while True:
        step = llm.think(messages)        # LLM 产出这一轮的 Thought + Action
        if step.action == "finish":       # 它认为信息够了，收尾给答案
            return step.answer
        obs = run_tool(step.action)        # 系统真去执行工具，拿回 Observation
        messages.append(step.thought_text) # 把这一轮的思考+行动拼回对话
        messages.append(f"Observation: {obs}")  # 再把工具结果（系统填的）拼回去
        # 带着新到的 Observation，下一圈 llm.think 会想得更准
```

四个关键点正好对应图里的四样东西：`llm.think` 产出 **Thought + Action**（这两样是 LLM 写的），`run_tool` 执行后得到 **Observation**（这是系统填的），`messages.append` 把它们**拼回对话**当作下一轮的输入，而 `while True` 就是那条「再转一圈」的循环。

注意 `obs = run_tool(...)` 这一步——它会真的去调外部工具、把现实世界的数据带回来。这正是 ReAct 和纯 CoT 的分水岭：CoT 没有这一步，全程闷在脑子里；ReAct 有了它，才能「实证」而不是「空想」。把这个循环拆成只跑一次、且去掉 `run_tool`，ReAct 立刻退化回纯 CoT。

{/* 7. 常见误区（spec §七，A 型 ≥2）：固定格式 现象→原因→修法；每个 ≤150 字。 */}

## 容易踩的坑


{/* 8. 小结与练习（spec §八）：小结 ≤5 条且与学习目标一一呼应；练习 2-3 题含 1 道「给 trace 判断哪步是 Thought/Action/Observation」+ 1 道改 Demo 题，答案折叠。 */}

## 小结

- **ReAct = Reasoning + Acting**：把「思考 Thought → 行动 Action → 观察 Observation」交织成一个循环，边想边干边看，直到办成——它是第 1 章 Agent 循环落到提示 / 执行上的具体打法
- **Thought** 是 LLM 把「我现在该想什么、下一步干什么」显式写出来，让推理过程看得见
- **Action** 是 LLM 决定要调的动作（调哪个工具、传什么参数），**Observation** 是系统执行工具后填回的真实结果
- 关键分界：**Thought / Action 是 LLM 自己写的，Observation 是系统填回的**——不是模型编的
- **ReAct vs 纯 CoT**：CoT 只闷头推理、遇到实时 / 事实信息只能瞎猜；ReAct 推理 + 真去查，能实证——需要实时 / 事实信息的任务必须 ReAct


{/* 10. 名词解释（spec §十）：与正文 <Term> 高亮一一对应（5 个，按首现顺序）；释义面向零基础；位置章末、出处声明之前。 */}


{/* 9. 出处声明（spec §九）：原创改编，sourceUrl 为空字符串，Attribution 空标签自动处理。 */}

