---
title: 提示工程与角色设定
type: C
section: 基础原理
order: 3
description: 读完能写出一个带角色设定、few-shot 示例和结构化输出要求的提示，并解释这三味「配料」分别让模型的输出好在哪。
demo: true
math: false
sourceUrl: ""
draft: false
---

{/* C 实战型·全书第 3 章·本书首个 C 型：代码是重心。承前两章「大脑（LLM）+ 它的脾气」，本章讲「怎么给小特下指令 / 发岗位说明书」让它干得准。重心在代码对照(§6，真实 Python prompt 构造覆盖≥80%)与两个交互 Demo；数学整节删(math:false)。知识点清单：① system prompt / 角色设定 = <PromptAssemblyDiagram /> 第①层 + 代码 §6(a) + Glossary；② 提示的三层结构 = <PromptAssemblyDiagram /> anime.js 旗舰拼装动画；③ few-shot 示例 = <PromptRecipeDemo /> 配料②开关 + 代码 §6(b) + Glossary；④ 结构化输出 = <StructuredOutputDemo /> 散文 vs JSON 交互 + 代码 §6(c) + Glossary；提示模板 = 代码 §6(d) PROMPT.format。 */}

{/* 1. 学习目标（spec §一）：C 型必含「能写出一个带角色+few-shot+结构化输出的提示」；末条是可当场自测的检验问题。 */}

<Objectives>

- 能说清提示的三层结构（系统设定 + few-shot 示例 + 用户消息），并解释「角色设定 / few-shot / 结构化输出」这三味配料分别让模型输出好在哪
- 能写出一个带角色设定、few-shot 示例和「只输出 JSON」要求的完整提示，并用 `json.loads` 把模型回答解析成程序能用的字段
- 能回答：同样问「把这句口语订票需求整理出来」，只加「角色设定」一味配料，输出会变好在哪一点？还差哪一步才能被程序稳定解析？

</Objectives>

{/* 2. 直觉引入（spec §二）：≤3 段 × ≤120 字；零术语；回答「解决什么问题/没有它会怎样」；同样的活会下指令 vs 不会下，小特干出来天差地别。 */}

## 同样一句吩咐，会下指令和不会下，天差地别

你对私人助理**小特**说：「把这句话整理一下——后天下午去广州，预算别超 600。」他可能给你回一长段热情的废话：「去广州玩呀，早茶别错过哦～」也可能干脆利落地列出「城市：广州，时间：后天下午」。同样一颗大脑，差别全在你**怎么吩咐**。

这一章就讲：怎么把吩咐写好——给小特发一份「岗位说明书」、给他几个范例照着做、再要求他按固定格式交活。三招下去，他的输出就从「没法用」变成「精准、规范、程序能直接接」。

没学会会怎样？你只能拿到时好时坏、忽长忽短、夹着闲聊的回答，每次都得自己再收拾一遍——更别提让程序去自动处理它了。

{/* 3. 概念讲解（spec §三，C 型可压缩，各概念简讲 + Term + 引用 viz）：核心新概念 4 个：角色设定/system prompt、提示的三层结构、few-shot 示例、结构化输出。 */}

## 第一味：给小特发「岗位说明书」——角色设定

你交代差事前，最该先说清楚的是：**你希望它以什么身份、按什么规矩来答**。这就是 <Term def="在提示最前面给 LLM 设定的身份、职责与行为规则，相当于给它发一份「岗位说明书」。它框定模型整段对话的基调——是谁、能干嘛、按什么风格和约束答。">角色设定（system prompt）</Term>：一句「你是严谨的订票助理，只按事实回答、不确定就说不知道」，就把小特从「热情的话痨」框成了「靠谱的办事员」。

它装在提示最前面、独立的一层里（叫 `system`，系统设定）。它不回答任何具体问题，只**定调**——后面不管你问什么，它都照这份说明书的身份和规矩来答。你会在下一节的 Demo 里看到：光加这一味，输出就从跑题闲聊变得聚焦正事。

## 第二味与全貌：提示的三层结构

你以为「发给大脑的提示」就是你手打的那句话？其实不是。真正送进大脑的，是 <Term def="一段完整提示由三层拼成：① 系统设定（角色）+ ② few-shot 示例 + ③ 用户消息。模型看到的是这三层拼起来的整段，而不是你单独打的那句话。">提示的三层结构</Term>拼成的一整段：最底层是刚说的**系统设定（角色）**，中间是**few-shot 示例**，最上面才是你这一轮的**用户消息**。

下面这张图把三层一层层拼给你看，点播放，看一段提示是怎么组装出来的：

<PromptAssemblyDiagram />

记住这个全貌：你能调的不只是「问什么」，还有「给它配什么角色、给它看什么范例」。后两层正是把输出从「能用」逼到「好用」的关键。

## 第三味：给几个范例,让它照葫芦画瓢——few-shot 示例

中间那层装的是 <Term def="在提示里塞进几个「输入 → 输出」的范例（few = 几个，shot = 一次示范），让模型照着范例的格式、字段和风格来答，而不用你长篇大论地解释要求。">few-shot 示例</Term>：与其啰嗦地描述「我要城市、日期、方式……请用这种格式」，不如直接甩给它一个例子——「用户说『明天去上海』，你就回 `{"city":"上海","date":"明天"}`」。它一看就懂，照着这个格式和字段答。

这是「示范」胜过「讲道理」：一个好范例，抵得过一大段文字要求，还更稳。下一节 Demo 里你打开「few-shot」开关，会看到输出立刻对齐到范例的字段和格式。

## 第四味：让它按表格填，别写散文——结构化输出

最后一味，是要求模型 <Term def="要求模型按固定机器格式（最常见是 JSON）输出，而不是写自由文本散文，这样程序才能用 json.loads 之类的方法稳定地解析、提取出每个字段。">结构化输出</Term>。同一份订票需求，模型既可以写成一段散文「您是想后天坐高铁去广州对吧」，也可以吐一个 JSON `{"city":"广州", ...}`。对人来说散文更亲切，但对**程序**来说，散文是灾难——字段埋在句子里，只能靠正则去猜，换个措辞就崩。

JSON 则是程序的母语：一行 `json.loads` 就拿到一个字典，按 key 取值，稳。下一节第二个 Demo 会把「散文」和「JSON」摆一起，让你看程序从两者里提取字段的成功率天差地别。

{/* 4. 数学推导（spec §四）：math:false，整节省略。 */}

{/* 5. 交互 Demo（spec §五）：放 <PromptRecipeDemo /> 与 <StructuredOutputDemo />，各带「猜一猜」。 */}

## 动手一：三味配料，每加一味输出好一截

光读不如自己拨开关。下面这个 Demo 把「角色设定 / few-shot 示例 / 输出格式要求」做成三个开关，针对同一句固定的口语订票需求，每打开一味，下方「模型输出」就切到对应的示意输出，并给一个质量评级。

> 猜一猜：现在三个开关全关，模型在自由发挥。只打开「① 角色设定」一味，输出会变好多少？能直接被程序解析吗？先猜一个，再拨开关验证。

<PromptRecipeDemo />

你会看到一条清晰的阶梯：全关时跑题闲聊、没法用；加「角色」就聚焦了正事但还是散文；再加「few-shot」字段对齐了；三味全开才吐出干净的 JSON、程序一行就能解析。**每味配料补的洞各不相同**——角色管「别跑题」、few-shot 管「字段对齐」、格式要求管「能被程序读」。

## 动手二：散文 vs JSON，程序提取天差地别

第二个 Demo 把焦点放在最后一味。同一桩「抽取订票字段」的任务，用开关切换模型的两种输出：自由文本（散文）和 JSON 结构化。右侧「程序提取」面板会逐字段显示成功 ✓ 还是失败 ✗。

> 猜一猜：让模型写成散文「您是想后天坐高铁去广州，预算六百块上下」，程序想抠出 `type=高铁`、`budget=600` 这两个字段，能稳定抠到吗？关掉开关切到自由文本试试。

<StructuredOutputDemo />

JSON 那侧四个字段全绿——`json.loads` 拿到字典直接按 key 取。散文那侧，「坐高铁」对不上约定值、「六百块上下」根本不是数字，抠取一片飘红。这就是为什么：凡是要交给程序处理的输出，**必须**让模型按结构化格式填，别让它写散文。

{/* 6. 代码对照（spec §六，C 型重心，覆盖≥80%）：多段真实 Python——(a) 构造 messages(system+user)；(b) few-shot；(c) 结构化输出 + json.loads；(d) 提示模板 PROMPT.format。每段≤30 行、块间有解说、两块不相邻、API 关键点用 Callout。 */}

## 代码逐段拆解：把三味配料写成真实的 Python

讲了这么多，落到代码里到底长什么样？下面我们一步步把上面那个「订票需求抽取」的提示用 Python 写出来。聊天类 LLM 的接口几乎都长一个样：你传一个 `messages` 列表，每条是一个带 `role` 和 `content` 的字典。最朴素的提示就两条——一条 `system`（角色设定），一条 `user`（你的需求）：

```python
client = OpenAI()  # 任意兼容 OpenAI 风格的客户端

messages = [
    # 第一层：system —— 角色设定（小特的「岗位说明书」）
    {"role": "system", "content": "你是严谨的订票助理，只按事实回答，不确定就说不知道。"},
    # 第三层：user —— 你这一轮真正要它办的事
    {"role": "user", "content": "帮我订后天下午去广州的高铁票，预算别超 600。"},
]

resp = client.chat.completions.create(model="gpt-4o-mini", messages=messages)
print(resp.choices[0].message.content)
```

注意 `system` 和 `user` 是两条**独立**的消息，靠 `role` 区分。角色设定必须放进 `role: "system"`，而不是塞进 `user` 那句话里——这是把行为约束和具体请求分开的标准做法。

<Callout type="tip">
  **关键点**：`messages` 是个**列表**，顺序就是模型读到的顺序。`system`
  一般放第一条、全程生效；`user` 是当前请求。后面要加的 few-shot
  示例，就插在这两者**之间**。
</Callout>

现在加第二味——few-shot 示例。做法很直接：在 `system` 之后、真正的 `user` 之前，**塞进几对「示例 user → 示例 assistant」消息**，假装这段对话之前已经发生过。模型看到这几个范例，就会照着范例里 assistant 的格式来答：

```python
messages = [
    {"role": "system", "content": "你是严谨的订票助理，只按事实回答。"},
    # —— few-shot 示例：一对「示例输入 → 示例输出」，给模型打样 ——
    {"role": "user", "content": "明天去上海"},
    {"role": "assistant", "content": '{"city": "上海", "date": "明天", "type": "高铁", "budget": null}'},
    # —— 真正的用户消息（模型会照上面 assistant 的格式来答）——
    {"role": "user", "content": "帮我订后天下午去广州的高铁票，预算别超 600。"},
]
```

这就是 few-shot 的全部机巧：**用范例代替啰嗦的描述**。你没写一个字解释「请输出 city、date 字段」，但模型从那对示例里全看明白了。想更稳就多给一两对范例（few-shot 的 few 就是「几个」），但范例本身的质量要高、要覆盖典型情况。

<Callout type="tip">
  **关键点**：few-shot 的范例**必须自身就是你想要的标准答案**。示例 assistant
  里写的 JSON
  字段名、取值格式，就是模型会模仿的模板——范例错一个字段名，模型就跟着错。
</Callout>

第三味是结构化输出。光给范例还不够稳——得在 `system` 里**明确要求只输出 JSON**，很多 API 还提供 `response_format` 强制约束格式。两手都上，最保险。拿到回答后，用 `json.loads` 把它解析成 Python 字典：

```python
import json

messages = [
    {"role": "system", "content": "你是订票信息抽取器。只输出 JSON，不要任何多余文字。"},
    {"role": "user", "content": "明天去上海"},
    {"role": "assistant", "content": '{"city": "上海", "date": "明天", "type": "高铁", "budget": null}'},
    {"role": "user", "content": "帮我订后天下午去广州的高铁票，预算别超 600。"},
]

resp = client.chat.completions.create(
    model="gpt-4o-mini",
    messages=messages,
    response_format={"type": "json_object"},  # 强制模型输出合法 JSON
)
data = json.loads(resp.choices[0].message.content)  # → Python dict，按 key 取
print(data["city"], data["budget"])  # 广州 600
```

`json.loads` 把模型那段 JSON 文本变成真正的 Python 字典 `data`，于是 `data["city"]` 就稳稳地拿到「广州」。这正是动手二里 JSON 那侧「四字段全绿」的代码版本。

<Callout type="warn">
  **关键点**：`response_format={"type": "json_object"}` 能保证输出是**合法
  JSON**，但**不保证字段名和取值符合你的约定**——那靠 few-shot
  范例去锚定。两者配合：`response_format` 管「是 JSON」，few-shot 管「是对的 JSON」。
</Callout>

最后一招让这套提示能**复用**：把固定部分写成模板，变化部分用占位符，用 `str.format` 填进去。这样同一套角色 + few-shot，能套给无数条不同的用户需求：

```python
# 把固定的「角色 + 输出要求」写成模板，{request} 是每次变化的用户需求
SYSTEM_PROMPT = "你是订票信息抽取器。只输出 JSON，不要多余文字。"
USER_TEMPLATE = "请抽取这条订票需求的字段：{request}"

def build_messages(request: str) -> list[dict]:
    return [
        {"role": "system", "content": SYSTEM_PROMPT},
        {"role": "user", "content": "明天去上海"},
        {"role": "assistant", "content": '{"city": "上海", "date": "明天"}'},
        {"role": "user", "content": USER_TEMPLATE.format(request=request)},
    ]

# 同一套模板，喂不同需求复用
msgs = build_messages("帮我订后天下午去广州的高铁票，预算别超 600。")
```

`USER_TEMPLATE.format(request=...)` 把用户那句口语填进固定模板。这一步看着小，却是工程化的关键：提示一旦稳定，就把它**冻成模板**，只让真正变化的部分（用户需求）走占位符——而不是每次手拼字符串。这就是把「写好一个提示」变成「能复用的提示函数」。

{/* 7. 常见误区（spec §七，C 型 ≥3）：固定格式 现象→原因→修法；每个 ≤150 字。 */}

## 容易踩的坑

<Callout type="trap">
  **现象**：「角色设定写得越长越详细越好，我写了 500
  字的人设。」结果模型又慢又容易抓不住重点。**原因**：把「角色设定」当成「越多越灵」。它占的是宝贵的上下文窗口，堆太多反而稀释关键约束（上一章「迷失在中间」）。**修法**：角色设定只写**真正影响行为**的几句——身份、核心规则、输出要求。人设细节、客套话一律删掉，短而准。
</Callout>

<Callout type="trap">
  **现象**：「few-shot
  嘛，随便丢两个例子进去就行。」结果其中一个范例字段名写错了，模型输出全跟着错。**原因**：忘了模型是**照着范例模仿**的——范例就是它的标准答案模板。**修法**：few-shot
  的每个范例都当「正确答案」来精挑：字段名、格式、取值都得是你真正想要的，覆盖典型情况；宁可少给几个高质量的，也别堆一堆带瑕疵的。
</Callout>

<Callout type="trap">
  **现象**：「我在提示里写了『请输出 JSON』，应该就稳了吧？」结果模型时不时回一句「好的，这是 JSON：`{...}`」，前面那句寒暄把
  `json.loads` 干崩了。**原因**：把「自然语言要求」当成了「格式保证」。光靠嘴说，模型仍可能夹带多余文字。**修法**：双保险——`response_format`
  强制合法 JSON + few-shot 锚定字段；并且解析时 `try/except` 兜底，别裸调 `json.loads`。
</Callout>

<Callout type="trap">
  **现象**：「我把『你是严谨的订票助理，只输出 JSON』直接写进了 user
  那句话里，效果好像也行？」可换几轮对话后它就不听了。**原因**：把**行为约束写进了
  user 而非 system**。user
  消息是「这一轮的请求」，约束放这儿优先级低、易被后续消息冲淡。**修法**：身份和规则一律放
  `role: "system"`，全程生效；user 只放当前这一轮真正要办的事。
</Callout>

{/* 8. 小结与练习（spec §八）：小结 ≤5 条且与学习目标一一呼应；练习 3 题含 1 道独立实现题 + 1 道改 Demo 题，答案折叠。 */}

## 小结

- 提示是**三层结构**拼成的一整段：系统设定（角色）+ few-shot 示例 + 用户消息——大脑读到的是整段，不是你单打的那句
- **角色设定**（放 `system`）给小特发岗位说明书、定调、管「别跑题」；写短而准，别堆长人设
- **few-shot 示例**（塞在 system 与 user 之间的「示例 user→assistant」对）用范例代替啰嗦描述、管「字段对齐」；范例必须自身就是标准答案
- **结构化输出**（`response_format` + 提示要求 + `json.loads`）管「程序能稳定解析」；光靠嘴说不够，要强制 + 锚定 + 兜底
- 提示稳定后**冻成模板**，固定部分写死、变化部分用 `format` 占位符复用，把「写好一个提示」变成「能复用的提示函数」

<Exercises>

**练习 1（独立实现题）** 写一个完整的 Python 提示，把用户的口语订票需求抽取成 JSON。要求：① 带角色设定（放 `system`）；② 带至少一对 few-shot 示例；③ 要求并保证只输出 JSON，字段含 `city`、`date`、`type`、`budget`；④ 用 `json.loads` 把回答解析成字典，并打印 `city` 和 `budget`。给定用户需求是「下周一早上去杭州，高铁，预算 400 以内」。

<Answer>

```python
import json
from openai import OpenAI

client = OpenAI()

messages = [
    # ① 角色设定（system）：定身份 + 输出要求
    {"role": "system", "content": "你是订票信息抽取器。只输出 JSON，不要任何多余文字。"},
    # ② few-shot 示例：一对「输入 → 标准答案」，锚定字段名与格式
    {"role": "user", "content": "明天去上海，高铁，预算 600"},
    {"role": "assistant",
     "content": '{"city": "上海", "date": "明天", "type": "高铁", "budget": 600}'},
    # ③ 真正的用户需求
    {"role": "user", "content": "下周一早上去杭州，高铁，预算 400 以内"},
]

resp = client.chat.completions.create(
    model="gpt-4o-mini",
    messages=messages,
    response_format={"type": "json_object"},  # ③ 强制合法 JSON
)

# ④ 解析（用 try/except 兜底更稳）
try:
    data = json.loads(resp.choices[0].message.content)
    print(data["city"], data["budget"])  # 期望：杭州 400
except (json.JSONDecodeError, KeyError) as e:
    print("解析失败，需要重试或回退：", e)
```

要点回收：角色放 `system`（不塞进 user）、few-shot 范例自身就是标准答案（字段名 `city/date/type/budget` 锚定）、`response_format` 强制 JSON、`json.loads` + `try/except` 兜底。这四步正是本章三味配料 + 模板化的完整落地。

</Answer>

**练习 2（改 Demo 题）** 打开「动手一」的三味配料 Demo，只打开「① 角色设定」、关掉另两个，观察输出；再单独只打开「③ 输出格式要求」、关掉另两个，观察输出。两种「只开一味」的输出，哪个更接近「程序能直接用」？为什么仍然都不够好？

<Answer>

只开「③ 输出格式要求」的更接近能用——因为它至少吐出了 JSON，程序能 `json.loads`。只开「① 角色设定」的虽然不跑题了，但还是一段散文，字段埋在句子里，程序提取很脆。

但**两者都不够好**：只开角色，缺了「结构化」，根本不是机器格式；只开格式，缺了「few-shot 锚定」，字段名和取值各凭模型发挥（可能输出 `{"钱":"四百块以内"}` 这种），和你程序约定的 key 对不上。这正说明三味配料补的洞各不相同、缺一不可——角色管不跑题、few-shot 管字段对齐、格式要求管能被程序读，三味齐了才稳。

</Answer>

**练习 3（问答题）** 有人说：「我直接在 `user` 消息里写清楚『你是订票助理，请只输出 JSON，字段要有 city 和 date』，不就等于角色设定 + 格式要求了吗？干嘛非要分 `system` 和 `few-shot`？」请用本章概念反驳。

<Answer>

这种写法短期可能「看着也行」，但有两个洞：

**① 把行为约束写进 user，优先级低、易被冲淡。** `user` 消息是「这一轮的请求」，约束放这里，多轮对话后很容易被后续 user 消息盖过，模型就不听了。身份和规则应放 `role: "system"`，全程生效——这是本章「第四个坑」。

**② 用文字描述格式，远不如 few-shot 范例稳。** 「字段要有 city 和 date」是抽象描述，模型可能理解偏差（输出 `City`、漏字段、取值格式不对）。一对 `{"city": "上海", "date": "明天"}` 的范例直接把模板甩给它，照葫芦画瓢，比文字描述精确得多、稳得多。再加上 `response_format` 强制合法 JSON，才是「是 JSON 且是对的 JSON」的双保险。

所以分 `system` / few-shot / `user` 三层不是形式主义，而是分别解决「约束生效、字段对齐、格式合法」三个不同问题。

</Answer>

</Exercises>

{/* 10. 名词解释（spec §十）：与正文 <Term> 高亮一一对应（4 个，按首现顺序）；释义面向零基础；位置章末、出处声明之前。 */}

<Glossary>

<GlossaryItem term="角色设定（system prompt）">
  在提示最前面给 LLM
  设定的身份、职责和行为规则，相当于给它发一份「岗位说明书」。它不回答任何具体问题，只**定调**——后面不管你问什么，模型都照这份说明书的身份和规矩来答。一句「你是严谨的订票助理，只按事实回答」就能把它从话痨框成办事员。技术上放在
  `role: "system"`
  这条独立消息里，全程生效。详见本章「给小特发岗位说明书」一节。
</GlossaryItem>

<GlossaryItem term="提示的三层结构">
  一段完整提示由三层拼成：**① 系统设定（角色）+ ② few-shot 示例 + ③
  用户消息**。模型看到的是这三层拼起来的**整段**，而不是你单独打的那句话。你能调的不只是「问什么」，还有「配什么角色、给什么范例」——后两层正是把输出从「能用」逼到「好用」的关键。详见本章「提示的三层结构」一节与拼装动画。
</GlossaryItem>

<GlossaryItem term="few-shot 示例">
  在提示里塞进几个「**输入 → 输出**」的范例（few = 几个，shot =
  一次示范），让模型照着范例的格式、字段和风格来答，而不用你长篇大论地解释要求——**示范胜过讲道理**。技术上是在
  system 和真正的 user 之间，插入几对「示例 user → 示例
  assistant」消息。范例本身必须就是你想要的标准答案，模型会照着它模仿。详见本章「给几个范例」一节与配料
  Demo。
</GlossaryItem>

<GlossaryItem term="结构化输出">
  要求模型按**固定的机器格式**（最常见是
  JSON）输出，而不是写自由文本散文，这样**程序**才能用 `json.loads`
  之类的方法稳定地解析、提取出每个字段。散文对人亲切、对程序是灾难（字段埋在句子里只能靠正则猜）；JSON
  是程序的母语，一行就拿到字典按 key 取值。靠「提示要求 + `response_format` 强制
  + few-shot 锚定字段」三手保证。详见本章「让它按表格填」一节与散文 vs JSON
  Demo。
</GlossaryItem>

</Glossary>

{/* 9. 出处声明（spec §九）：原创改编，sourceUrl 为空字符串，Attribution 空标签自动处理。 */}

<Attribution />
