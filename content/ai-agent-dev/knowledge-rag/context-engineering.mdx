---
title: 上下文工程与压缩
type: D
section: 知识增强
order: 8
description: 上下文工程与压缩：在有限注意预算中选择、排序、压缩和按需加载上下文，并用位置扰动验证稳健性，以架构、轨迹与故障重放完成工程验收。
demo: true
math: false
sourceUrl: "https://arxiv.org/abs/2307.03172"
draft: false
qualityVersion: 2
practiceMode: simulation
sourceMode: independent-rewrite
officialUnitId: aiagentdev-09
---

import {
  ContextEngineeringModelLab,
  ContextEngineeringTraceLab,
  ContextEngineeringEvidenceLab,
} from "@/components/mdx/ai-agent-dev/v2/context-engineering";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  TermSequence,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释“上下文工程与压缩”如何在有限注意预算中选择、排序、压缩和按需加载上下文，并用位置扰动验证稳健性
- 能区分上下文工程、注意预算、lost in the middle、压缩、按需加载，并标出控制权、状态和副作用边界
- 能冻结输入与版本，沿以下证据定位首个分叉：token 预算、内容类别、位置、压缩映射、保留断言、引用与位置扰动结果
- 能注入“按消息年龄截断历史，把仍生效的安全约束和未解决错误一起删除”，完成阻断、恢复、复位和同输入重放

</Objectives>

{/* AI_AGENT_DEV_QUALITY_V2 */}

## 来源、课程身份与适用边界

“上下文工程与压缩”以[Anthropic 公开全文《Building effective agents》](https://www.anthropic.com/engineering/building-effective-agents)建立工程总纲，并用[《Lost in the Middle》原始论文](https://arxiv.org/abs/2307.03172)核对本章机制。

这不是一本名为《AI Agent 开发实战》的官方出版物，也不存在官方十四章目录。平台把公开工程文章、官方开发文档和原始论文重组为 14 个工程单元；下列 112 个节点是站内课程地图。正文、代码、图表、实验与练习均为独立教学重写，模型、API、协议或安全边界变化时必须重新验证。

## 本单元的八个工程坐标

- **上下文工程与压缩**：这是“在有限注意预算中选择、排序、压缩和按需加载上下文，并用位置扰动验证稳健性”的第 1 个工程坐标；必须进入机制解释、运行轨迹或故障证据，不能只停在目录。
- **桌子就那么大，该摆什么、怎么摆，是门讲究**：这是“在有限注意预算中选择、排序、压缩和按需加载上下文，并用位置扰动验证稳健性”的第 2 个工程坐标；必须进入机制解释、运行轨迹或故障证据，不能只停在目录。
- **上下文工程：在有限窗口里安排「放什么、放多少、怎么排」**：这是“在有限注意预算中选择、排序、压缩和按需加载上下文，并用位置扰动验证稳健性”的第 3 个工程坐标；必须进入机制解释、运行轨迹或故障证据，不能只停在目录。
- **上下文预算分配：窗口是一笔有限预算，分给好几样**：这是“在有限注意预算中选择、排序、压缩和按需加载上下文，并用位置扰动验证稳健性”的第 4 个工程坐标；必须进入机制解释、运行轨迹或故障证据，不能只停在目录。
- **压缩 / 摘要：太长了就浓缩成几句要点**：这是“在有限注意预算中选择、排序、压缩和按需加载上下文，并用位置扰动验证稳健性”的第 5 个工程坐标；必须进入机制解释、运行轨迹或故障证据，不能只停在目录。
- **lost-in-the-middle：注意力呈 U 形，重要信息要放首尾**：这是“在有限注意预算中选择、排序、压缩和按需加载上下文，并用位置扰动验证稳健性”的第 6 个工程坐标；必须进入机制解释、运行轨迹或故障证据，不能只停在目录。
- **动手一：拖五个滑块，给一笔窗口预算分家**：这是“在有限注意预算中选择、排序、压缩和按需加载上下文，并用位置扰动验证稳健性”的第 7 个工程坐标；必须进入机制解释、运行轨迹或故障证据，不能只停在目录。
- **动手二：把长对话压成要点，看省了多少、丢了什么**：这是“在有限注意预算中选择、排序、压缩和按需加载上下文，并用位置扰动验证稳健性”的第 8 个工程坐标；必须进入机制解释、运行轨迹或故障证据，不能只停在目录。

## 术语与运行合同

<TermSequence
  items={[
    {
      term: "上下文工程",
      def: "上下文工程：为每轮推理选择和维护高信号 token 的工程过程；在“上下文工程与压缩”中按以下证据核对：token 预算、内容类别、位置、压缩映射、保留断言、引用与位置扰动结果。",
    },
    {
      term: "注意预算",
      def: "注意预算：模型在长上下文中可靠利用信息的有限能力；在“上下文工程与压缩”中按以下证据核对：token 预算、内容类别、位置、压缩映射、保留断言、引用与位置扰动结果。",
    },
    {
      term: "lost in the middle",
      def: "lost in the middle：关键信息位于长上下文中部时利用率下降的现象；在“上下文工程与压缩”中按以下证据核对：token 预算、内容类别、位置、压缩映射、保留断言、引用与位置扰动结果。",
    },
    {
      term: "压缩",
      def: "压缩：保留关键事实并缩短表示的过程；在“上下文工程与压缩”中按以下证据核对：token 预算、内容类别、位置、压缩映射、保留断言、引用与位置扰动结果。",
    },
    {
      term: "按需加载",
      def: "按需加载：只在当前步骤需要时从外部取回内容；在“上下文工程与压缩”中按以下证据核对：token 预算、内容类别、位置、压缩映射、保留断言、引用与位置扰动结果。",
    },
  ]}
  suffix="。"
/>

本页不变量是：上下文重组后必须保留目标、约束、决定、开放问题和来源引用。任何“成功”结论都要保存token 预算、内容类别、位置、压缩映射、保留断言、引用与位置扰动结果；模型自评、最终措辞和单次 demo 都不能替代环境事实。

## 工程机制与反证实验

### 窗口容量不是有效容量

长上下文模型仍可能随位置和噪声出现性能梯度，不能只看标称 token 数。

动手验证：把同一事实放在首、中、尾三处测召回。

### 预算按价值而非来源平均分

系统指令、未解决错误和当前证据通常比陈旧工具原文更值得保留。

动手验证：删掉低价值结果后比较任务一致性。

### 压缩要能回指原始证据

摘要可能遗漏细节或引入错误，应保留来源 ID 和按需展开入口。

动手验证：从摘要中的每条决定回查原始事件。

### 按需检索减少污染

路径、查询和文档标识可先留在上下文，需要时再读取内容。

动手验证：比较一次性加载与逐步加载的 token、延迟和正确率。

## 从架构到故障重放

<Stepper>
  <Step title="1. 架构复杂度实验">
    在“上下文工程与压缩”中切换简单基线、受控工作流和自主循环，先判断“在有限注意预算中选择、排序、压缩和按需加载上下文，并用位置扰动验证稳健性”是否真的需要更高自主性，再比较延迟、成本、可观测性与风险。

    <ContextEngineeringModelLab />

  </Step>
  <Step title="2. 逐阶段运行轨迹">
    固定模型、任务、工具、权限和预算，沿盘点信息 → 分配预算 → 按需加载 → 压缩排序 → 扰动评测逐步推进；每个阶段只消费上一阶段已经验收的状态。

    <ContextEngineeringTraceLab />

  </Step>
  <Step title="3. 单故障、恢复与复位">
    注入“按消息年龄截断历史，把仍生效的安全约束和未解决错误一起删除”，在token 预算、内容类别、位置、压缩映射、保留断言、引用与位置扰动结果中标记首个分叉；撤销故障后重置控件，用完全相同的输入和权限重放。

    <ContextEngineeringEvidenceLab />

  </Step>
</Stepper>

## 最小可运行切片

```ts
function assembleContext(state: State, budget: number) {
  const mandatory = [state.goal, state.constraints, state.openIssues];
  const recent = state.events.slice(-6);
  const summary = compressWithCitations(state.events.slice(0, -6));
  const context = fitBudget([...mandatory, summary, ...recent], budget);
  assertFactsPreserved(state, context);
  return context;
}
```

切片只表达“在有限注意预算中选择、排序、压缩和按需加载上下文，并用位置扰动验证稳健性”的核心合同。生产实现还要补齐持久化、超时、密钥隔离、结构化日志、幂等和批量评测；如果不能重新取得token 预算、内容类别、位置、压缩映射、保留断言、引用与位置扰动结果，代码跑通也不能证明机制正确。

<Callout type="trap" title="本章首要反例">
  按消息年龄截断历史，把仍生效的安全约束和未解决错误一起删除。它会破坏“上下文重组后必须保留目标、约束、决定、开放问题和来源引用”；应从token
  预算、内容类别、位置、压缩映射、保留断言、引用与位置扰动结果定位首个分叉，而不是追加提示词掩盖运行时缺陷。
</Callout>

<Callout type="trap" title="结果正确也可能轨迹错误">
  “上下文工程与压缩”必须同时验收环境结果和完整轨迹。偶然得到正确文本，但发生越权调用、证据缺失、成本失控或错误停止，仍然是失败运行。
</Callout>

<Callout type="trap" title="升级后重新校准">
  模型、提示、工具、向量、协议或运行时变化都可能改变“在有限注意预算中选择、排序、压缩和按需加载上下文，并用位置扰动验证稳健性”的行为分布。升级后应在冻结任务集上重跑token
  预算、内容类别、位置、压缩映射、保留断言、引用与位置扰动结果。
</Callout>

## 小结

- 上下文工程在有限注意预算中选、排、压、按需加载
- 窗口容量不等于有效容量，存在 lost in the middle
- 预算按价值分配，系统指令与未解决错误优先保留
- 压缩须保留来源引用，可回指原始证据
- 重组后必保目标、约束、决定、开放问题与引用

## 练习与答案

<Exercises>

**问题 1：最小证明。** 怎样用正常、边界和单故障三类样本证明“上下文重组后必须保留目标、约束、决定、开放问题和来源引用”？

<Answer>
  冻结模型、任务、工具、权限和预算，手工写出三类预期，再保存token
  预算、内容类别、位置、压缩映射、保留断言、引用与位置扰动结果。任何未解释的首个分叉、未配对事件或失败副作用都应阻断通过。
</Answer>

**问题 2：节点覆盖。** 上下文工程与压缩、桌子就那么大，该摆什么、怎么摆，是门讲究、上下文工程：在有限窗口里安排「放什么、放多少、怎么排」、上下文预算分配：窗口是一笔有限预算，分给好几样、压缩 / 摘要：太长了就浓缩成几句要点、lost-in-the-middle：注意力呈 U 形，重要信息要放首尾、动手一：拖五个滑块，给一笔窗口预算分家、动手二：把长对话压成要点，看省了多少、丢了什么如何从目录词变成工程证据？

<Answer>
  把八个坐标分别映射到架构决策、状态轨迹和故障重放。每个坐标至少对应一个可观察状态、一个确定性判断或一个反例；只出现在列表里不算掌握。
</Answer>

**问题 3：恢复验收。** 怎样证明“按消息年龄截断历史，把仍生效的安全约束和未解决错误一起删除”已经修复？

<Answer>
  保存正常基线，注入故障并在token
  预算、内容类别、位置、压缩映射、保留断言、引用与位置扰动结果中标出首个差异；撤销后复位全部状态，用相同输入、版本、预算和权限重放。结果、轨迹与停止状态都回到基线才可交接。
</Answer>

</Exercises>

## 本章回顾

- “上下文工程与压缩”解决的是在有限注意预算中选择、排序、压缩和按需加载上下文，并用位置扰动验证稳健性。
- 核心不变量是上下文重组后必须保留目标、约束、决定、开放问题和来源引用。
- 首要反例是按消息年龄截断历史，把仍生效的安全约束和未解决错误一起删除。
- 最小证据包包含token 预算、内容类别、位置、压缩映射、保留断言、引用与位置扰动结果。

<Glossary>
  <GlossaryItem term="上下文工程">
    为每轮推理选择和维护高信号 token
    的工程过程。在“上下文工程与压缩”中必须能按以下证据重新定位：token
    预算、内容类别、位置、压缩映射、保留断言、引用与位置扰动结果。
  </GlossaryItem>
  <GlossaryItem term="注意预算">
    模型在长上下文中可靠利用信息的有限能力。在“上下文工程与压缩”中必须能按以下证据重新定位：token
    预算、内容类别、位置、压缩映射、保留断言、引用与位置扰动结果。
  </GlossaryItem>
  <GlossaryItem term="lost in the middle">
    关键信息位于长上下文中部时利用率下降的现象。在“上下文工程与压缩”中必须能按以下证据重新定位：token
    预算、内容类别、位置、压缩映射、保留断言、引用与位置扰动结果。
  </GlossaryItem>
  <GlossaryItem term="压缩">
    保留关键事实并缩短表示的过程。在“上下文工程与压缩”中必须能按以下证据重新定位：token
    预算、内容类别、位置、压缩映射、保留断言、引用与位置扰动结果。
  </GlossaryItem>
  <GlossaryItem term="按需加载">
    只在当前步骤需要时从外部取回内容。在“上下文工程与压缩”中必须能按以下证据重新定位：token
    预算、内容类别、位置、压缩映射、保留断言、引用与位置扰动结果。
  </GlossaryItem>
</Glossary>

## 阅读导航

[← RAG 检索增强生成](/learn/ai-agent-dev/knowledge-rag/rag) · [多智能体协作模式 →](/learn/ai-agent-dev/multi-agent/multi-agent-patterns)

<Attribution
  mode="independent-rewrite"
  sourceBasis="full-text-primary"
  workTitle="Anthropic Agent 工程资料与原始论文（站内十四单元开发课程改编）"
  adaptedUrl="https://www.anthropic.com/engineering/building-effective-agents"
/>
