---
title: 函数调用原理
type: A
section: 让智能体行动
order: 6
description: >-
  读完能解释模型如何通过 tool schema 选择工具、发起函数调用，并用 tinyagent 的 @tool / Tool / registry
  搭出最小工具系统。
demo: true
math: false
sourceUrl: "https://www.anthropic.com/engineering/building-effective-agents"
draft: false
---

import { AaFunctionCallTurnDiagram } from "@/components/mdx/ai-agent/function-call-turn-diagram";
import { AaToolSchemaDiagram } from "@/components/mdx/ai-agent/tool-schema-diagram";
import { AaToolPickerPlayground } from "@/components/mdx/ai-agent/tool-picker-playground";


## 为什么只记结论不足以掌握函数调用原理

学习“函数调用原理”时，第一步不是记住结论，而是冻结输入、上下文、版本和成功标准。只有这些条件明确，函数调用原理的含义才不会随着样例变化。正文已有的概念说明要与结构图、运行轨迹和失败样本互相印证，不能只凭最终输出看似正确就宣布完成。

结构分析从给小屋装一部「电话」开始：列出参与者、职责、连接方向、生命周期和所有权，再沿正常路径追踪数据或控制流。每一条边都要说明为什么存在、谁创建、谁消费、谁负责清理；如果边界被跨越，必须能在证据中找到第一处异常。

机制验证要把工具：模型能「请求外界代办」的能力写成可以执行的条件。正常样本证明主路径，恰好边界样本验证等号和空值，单故障样本只破坏一个假设。三类样本使用同一份观察指标，避免因为测试口径变化而把偶然结果误认成规律。

成本分析同时记录时间、空间、延迟、耦合、可维护性和不可逆操作。函数调用：模型写请求，运行时真执行不是一句“可能失败”，而是可复现输入、预期停点、实际轨迹、错误分类与清理步骤。任何自动重试都要有次数、预算和幂等边界。

方案比较不能只列优点。需要给出直接实现、当前方案和至少一个替代方案，逐项比较复杂度、扩展点、故障隔离和团队认知成本。当问题规模很小或变化轴稳定时，更简单的实现往往更好；模式与框架必须由真实变化压力证明。

实现阶段把大结论拆成可检查的中间产物：配置快照、结构清单、状态转移、输入输出样本、日志摘要和测试结果。每个产物带来源与生成命令，下一阶段只消费已通过门禁的版本，避免旧缓存或隐式默认值污染结论。

解释结果时必须区分相关性与因果性、接口承诺与实现细节、设计意图与运行事实。对“tool schema：给模型看的工具说明书”的判断要由独立证据支持，并明确适用范围；一旦输入分布、版本、硬件或组织边界改变，就重新运行最小实验。

复盘从首个分叉开始，而不是从最后一个报错倒推。先比较冻结输入，再比较第一份结构化中间产物，随后检查状态、约束和副作用。这样可以把复杂系统的排错范围收缩到一个阶段，避免在多个层次同时修改造成新的不确定性。

迁移到真实项目时，先选择一个最小但有代表性的切片，保存改造前基线，再逐步引入“函数调用原理”中的机制。每一步只改变一个变量并保留回滚点；性能、正确性、安全性和可理解性至少各有一项可量化指标。

最终验收要求读者能脱离页面重新画出结构、口述关键链路、实现最小版本、构造一个反例并解释失败位置。若只能复述名词而不能预测中间状态，说明知识仍停留在识记层，需要回到图示和实验重新验证。

本页用、

、、、建立统一坐标。先预测这些概念在结构图和运行轨迹中的位置，再操作实验控件；如果结果与预测不一致，停止在首个分叉，不要用后续补丁掩盖早期错误。

## 权威目录与核心概念逐项对照

- 函数调用原理
- 给小屋装一部「电话」
- 工具：模型能「请求外界代办」的能力
- 函数调用：模型写请求，运行时真执行
- tool schema：给模型看的工具说明书
- 工具注册表：运行时用它找得到函数
- 运行时：负责执行、兜底和守门
- 交互：模型怎么选工具


## 可复现的最小实现

先把决策记录写成机器可读结构：

```json
{
  "unit": "函数调用原理",
  "inputFrozen": true,
  "scenario": "normal | boundary | single-fault",
  "firstDivergence": null,
  "cleanupRequired": true
}
```

再用同一条执行链处理三类样本：

```ts
type Evidence = { stage: string; expected: string; actual: string };

function verify(sample: unknown, expected: readonly Evidence[]) {
  const trace = runFromCleanState(sample);
  return expected.find((item, index) => trace[index]?.actual !== item.expected);
}
```

最后保存回归门禁，禁止失败样本静默通过：

```text
normal      -> complete, invariant preserved
boundary    -> complete or explicit rejection
singleFault -> stop at first divergence, no stale output
```

用统一评分解释实验结果：

$$
Q = C_{correct} + C_{trace} + C_{recover} - R_{residual}
$$

$$
C_{coverage} = \frac{N_{verified}}{N_{official}}
$$

$$
R_{residual} = P(failure) \times I(impact)
$$

$$
Accept = (C_{coverage} \ge 0.90) \land (R_{residual} \le R_{budget})
$$

## 本章回顾

- 函数调用原理必须绑定冻结输入和明确成功标准。
- 给小屋装一部「电话」必须能画成结构并沿边追踪责任。
- 工具：模型能「请求外界代办」的能力要由正常、边界和单故障样本共同验证。
- 函数调用：模型写请求，运行时真执行必须保存第一处偏离和清理重建步骤。
- tool schema：给模型看的工具说明书决定方案是否可以进入下一阶段。


## 术语表


{/* 知识点清单：工具=§3 图示+Glossary；函数调用=§3 AaFunctionCallTurnDiagram+Glossary；tool schema=§3 AaToolSchemaDiagram+Glossary；模型如何选工具=§5 AaToolPickerPlayground；tinyagent @tool/Tool/registry=§6 代码对照；工具注册表=§3/§6+Glossary；运行时=§3 图示+Glossary。 */}

{/* 1. 学习目标（spec §一）：A 概念型必含「能解释 ___ 在管线中的位置」；末条为可当场自测的检验问题。 */}


{/* 2. 直觉引入（spec §二）：≤3 段 × ≤120 字；零术语；回答「解决什么问题/没有它会怎样」；类比使用工厂流水线/电话簿。专业名词最早 §3 才出现。 */}

## 给小屋装一部「电话」

上一章我们已经让屋里的天才能按表格填空了。但他还是有个致命短板：他看不到外面的实时世界，也不能亲手去查天气、搜网页、算精确数字。你问「上海今天热不热」，他只能凭旧记忆猜。

解决办法不是把他放出小屋，而是在门边放一本电话簿。每个号码后面写清楚「能办什么事、要说哪些信息」。他看到问题后，在纸条上写「请拨这个号码，并告诉对方城市是上海」。

真正拨电话的是屋外的人。屋外人拿到结果，再把「上海 26 度」塞回门缝；天才读到结果后，才组织成给你的自然语言答案。没有这套接力，智能体就只能聊天，不能行动。

{/* 3. 概念讲解（spec §三）：A 型重心；一个小节一个概念；术语首现 Term def 高亮且进章末 Glossary；每概念配图或引用 Demo；新核心概念 5 个。 */}

## 工具：模型能「请求外界代办」的能力

先给本章第一个词定清楚：不是模型脑子里自带的知识，而是我们在模型外面准备好的一段能力：一个 Python 函数、一条搜索 API、一个数据库查询、一个计算器。它解决的问题是「模型动不了手」：模型只会读写文本，工具把它接到真实世界。

在 Agent 管线里，工具位于模型外侧。用户问题先进模型，模型如果发现「仅靠回答不够」，就写出一条调用请求；外部程序接住请求，执行对应工具，再把结果作为新消息送回模型。下面的一回合动画先把整条接力跑一遍：

> 猜一猜：动画里哪一步是模型做的？哪一步必须由外部程序做？

<AaFunctionCallTurnDiagram />

## 函数调用：模型写请求，运行时真执行

第二个概念是。听起来像「模型在调用函数」，但更准确地说，是模型**提议**调用函数。它输出的是一条结构化请求，例如
`get_weather(city="上海")`。这条请求还不是执行结果，只是一个「请帮我拨这个号码」的纸条。

为什么要多绕这一圈？因为模型本身不能访问网络、不能读取本地文件、也不能运行 Python。函数调用把职责拆开：模型负责理解任务、选择合适的能力、填参数；外部程序负责安全地执行、捕获结果、决定要不要继续下一轮。它在管线中的位置正好接在「结构化输出」之后：上一章保证请求能被程序解析，这一章让解析出的请求变成真实动作。

## tool schema：给模型看的工具说明书

模型要会选工具，必须先看懂每个工具能干什么。这份说明书就是。它不是给 Python 解释器看的函数签名，而是给模型看的「电话簿词条」：`name` 告诉模型请求里该写哪个名字，`description` 告诉模型这个工具适合什么任务，`parameters` 告诉模型要填哪些参数。

下面这张解剖图把一份 schema 拆成三块。读的时候特别盯住 `description`：模型选工具时，最依赖的往往不是函数名，而是这句自然语言说明。

<AaToolSchemaDiagram />

## 工具注册表：运行时用它找得到函数

模型看的是 schema，外部程序还得知道「这个名字对应哪段可执行代码」。这就是。你可以把它想成屋外人的电话簿背面：正面给模型看说明，背面给程序查「号码实际连到哪个函数」。

为什么不能只把函数丢给模型？因为模型不会真的持有函数指针；它只会写文本。注册表把文本名字和真实代码接起来：模型请求 `calculator`，运行时从 `registry["calculator"]` 找到那个 Python 函数，检查参数后再调用。它在管线中的位置属于外部运行时，不属于模型内部。

## 运行时：负责执行、兜底和守门

最后一个边界要分清：才是执行者。模型输出 `get_weather(city="上海")` 不会自动让天气 API 被访问；运行时要先确认工具存在、参数合法、调用被允许，然后才执行。

这个边界非常重要：它让工具调用可控。你可以在运行时限制哪些工具能被调用、哪些参数要拒绝、调用失败时怎么重试、工具结果要不要截断。换句话说，模型是「会写请求的脑」，运行时是「有手、有规则、有刹车的身体」。

{/* 5. 交互 Demo（spec §五）：A 概念型主 Demo 为流程/选择交互；已含猜一猜、重置由组件提供。 */}

## 交互：模型怎么选工具

现在把注意力放到「模型如何选工具」。给模型三个工具：查天气、搜网页、计算器。用户问题一换，最匹配的 `description` 也会换。

> 猜一猜：如果任务是「3 的 17 次方是多少」，模型应该选搜索、天气，还是计算器？切换任务看它为什么这么选。

<AaToolPickerPlayground />

这里的关键不是「模型背过工具名」，而是它把用户任务和每个工具的 `description` 做语义匹配，再照 `parameters` 填参数。description 写得含糊，模型就容易选错；参数说明写得含糊，模型就容易传错。

{/* 6. 代码对照（spec §六）：A 型轻代码；Python 单语言，三块均 ≤30 行；覆盖 @tool 装饰器、Tool 类、registry。 */}

## 代码对照：tinyagent 的最小工具系统

把概念落到 tinyagent 里，我们只需要三件小零件：一个 `Tool` 类保存「给模型看的 schema + 给运行时执行的函数」，一个 `registry` 保存名字到工具的映射，一个 `@tool` 装饰器把普通函数登记进去。先写数据结构：

```python
from dataclasses import dataclass
from typing import Any, Callable

@dataclass
class Tool:
    name: str
    description: str
    parameters: dict[str, Any]
    func: Callable[..., Any]

    def schema(self) -> dict[str, Any]:
        return {
            "name": self.name,
            "description": self.description,
            "parameters": self.parameters,
        }
```

`Tool.schema()` 返回的是给模型看的说明书，不包含 `func`；`func` 留在运行时手里，避免把「能执行什么」和「给模型看什么」混在一起。接着写注册表和装饰器：

```python
registry: dict[str, Tool] = {}

def tool(name: str, description: str, parameters: dict[str, Any]):
    def deco(func: Callable[..., Any]):
        registry[name] = Tool(
            name=name,
            description=description,
            parameters=parameters,
            func=func,
        )
        return func

    return deco
```

`@tool(...)` 的作用是登记，而不是改变原函数怎么工作。函数仍然可以像普通 Python 函数一样调用；同时，运行时也能从 `registry` 找到它。下面注册两个工具：

```python
@tool(
    name="get_weather",
    description="查询某城市当前天气",
    parameters={"city": {"type": "string", "description": "城市名"}},
)
def get_weather(city: str) -> str:
    return f"{city} 今天 26℃，多云"

@tool(
    name="calculator",
    description="做精确的数值计算",
    parameters={"expression": {"type": "string", "description": "算式"}},
)
def calculator(expression: str) -> int:
    return safe_eval_arithmetic(expression)  # 只允许四则运算的安全解析器
```

这两段注册完后，`registry` 里已经有 `get_weather` 和 `calculator`。这里把计算器实现压成 `safe_eval_arithmetic` 占位，是刻意的：真实项目里**不要**直接 `eval` 模型传来的字符串，哪怕它看起来只是算式。模型只会看到 `schema()` 列表；当它输出调用请求时，运行时按名字查表并执行：

```python
tool_schemas = [t.schema() for t in registry.values()]

call = {"name": "get_weather", "arguments": {"city": "上海"}}
picked = registry[call["name"]]
result = picked.func(**call["arguments"])

messages.append({
    "role": "tool",
    "name": picked.name,
    "content": str(result),
})
```

这就是函数调用一回合的代码骨架：把 `tool_schemas` 发给模型，让模型产生 `call`；运行时从 `registry` 找到工具、执行函数、把结果作为 `tool` 消息回灌。真实项目里还要加参数校验、权限控制、超时和错误处理，但主链路已经齐了。

{/* 7. 常见误区（spec §七）：A 型 ≥2；固定格式 现象→原因→修法；至少一项指向 Demo/图示错误模式。 */}

## 容易踩的坑


{/* 8. 小结与练习（spec §八）：小结 ≤5 条且与学习目标一一呼应；练习 2-3 题，含改 Demo/设计题。 */}

## 小结

- 函数调用在 Agent 管线里连接「结构化请求」和「真实执行」
- tool schema 是给模型看的电话簿词条，description 决定选工具质量
- tinyagent 用 `@tool` / `Tool` / `registry` 把工具说明和真实函数接起来
- 运行时才负责执行、守门、回灌结果，模型不能越过它直接行动


{/* 10. 名词解释（spec §十）：与正文 Term 高亮一一对应（工具 / 函数调用 / tool schema / 工具注册表 / 运行时，按首现顺序）。 */}

## 名词解释


