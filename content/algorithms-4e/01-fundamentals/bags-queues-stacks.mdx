---
title: "1.3 Bags, Queues, and Stacks：访问顺序与表示选择"
type: C
section: "基础"
order: 2
description: "按官方1.3重建Bag、Queue、Stack API，扩缩容数组、链表实现、泛型迭代与Dijkstra双栈求值。"
demo: true
math: false
sourceUrl: "https://algs4.cs.princeton.edu/13stacks/"
draft: true
---

import { Algs4CollectionSemanticsLab, Algs4ResizingArrayLab, Algs4LinkedStackLab, Algs4LinkedQueueLab, Algs4IterationOrderLab, Algs4TwoStackEvaluateLab } from "@/components/mdx/algorithms-4e/diagrams/bags-queues-stacks";
import { Objectives, Callout, Attribution, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step } from "@/components/mdx/mdx-components";

<Objectives>

- 能解释Bag、Queue、Stack用不同访问顺序表达的algorithmic intent
- 能计算resizing array的capacity变化、linked endpoints与iterator输出顺序
- 能实现generic collection API、array与linked-list representations及iteration
- 能分析underflow、loitering、resize thrashing、empty-queue tail和generic array限制
- 能设计Dijkstra双栈client并按workload选择合适collection与representation

</Objectives>

## 从“同样存三个items，为什么不能只用一个List”开始

背包、队列与栈（<Term def="以不同insert、remove与iteration policies约束collection访问顺序的三种基础ADT。">背包、队列与栈</Term>，bags, queues, and stacks）都能保存items，却表达不同算法语义。Bag只承诺收集和遍历；Queue按first-in-first-out处理arrival；Stack按last-in-first-out处理nested或需要反向恢复的工作。

先预测依次加入A、B、C后，三者的foreach是否都应输出A、B、C。Queue是FIFO，通常如此；Stack的official iterator按LIFO输出C、B、A；Bag client根本不应依赖顺序。若用一个通用List却把访问policy留给每个caller，算法正确性会散落在index convention里，API无法替你守住order invariant。

官方1.3依次覆盖 **APIs、Array and resizing array implementations of collections、Linked lists、Linked-list implementations of collections、Iteration**。Applications还包括Dijkstra双栈算术表达式求值。这里从abstract policy走到两种representation，再回到client-visible iteration。

<Algs4CollectionSemanticsLab />

## 1.3.1 Collection APIs：把访问顺序写进operation names

集合API（<Term def="对generic items定义insert、remove、query、size与iteration behavior的public collection contract。">集合API</Term>，collection APIs）应让错误operation难以表达。Stack使用 `push/pop`，Queue使用 `enqueue/dequeue`，Bag只提供 `add`；如果都暴露 `remove(int index)`，policy就不再由ADT保证。

```java
public interface Stack<Item> {
    void push(Item item);
    Item pop();
    Item peek();
    boolean isEmpty();
    int size();
}

public interface Queue<Item> {
    void enqueue(Item item);
    Item dequeue();
    Item peek();
    boolean isEmpty();
    int size();
}
```

Official implementations是generic并实现 `Iterable<Item>`。Autoboxing允许 `Stack<Integer>` 接收int literals，但container实际保存Integer references；`Integer` identity与numeric value仍不能混淆。Empty removal属于underflow，current code抛 `NoSuchElementException`，client应在API层知道，而不是依赖null作为合法item与empty marker的二义值。

Bag常用于不需要removal的统计或graph adjacency collection；Queue用于BFS、event arrival；Stack用于DFS、parsing、backtracking和method-call-like nesting。选择不是性能表查一行，而是先把problem order semantics固定。

## 1.3.2 Resizing array：用几何扩缩容获得均摊常数

动态调整数组（<Term def="逻辑size与物理capacity分离，满时按常数倍扩容、稀疏到阈值时缩容的contiguous representation。">动态调整数组</Term>，resizing arrays）保持items在 `a[0..n-1]`。Push在 `n == a.length` 时分配double capacity并复制；pop清空slot，若size降到quarter capacity则halve。

<Algs4ResizingArrayLab />

```java
public void push(Item item) {
    if (n == a.length) resize(2 * a.length);
    a[n++] = item;
}

public Item pop() {
    if (isEmpty()) throw new NoSuchElementException("Stack underflow");
    Item item = a[n - 1];
    a[n - 1] = null;            // avoid loitering
    n--;
    if (n > 0 && n == a.length / 4) resize(a.length / 2);
    return item;
}
```

单次resize是linear copy，但capacity doubling意味着连续push中，每个旧item被复制的总次数受geometric series约束，push是amortized constant time。Pop若在half-full立即halve，下一次push可能马上double，交替操作造成resize thrashing；quarter-full才halve保留hysteresis，并让capacity维持在size的常数倍。

对象游离（<Term def="logical removal后underlying array仍保留object reference，使garbage collector无法回收已不可达业务item。">对象游离</Term>，loitering）解释为什么要把removed slot设为null。Logical size已经减小并不自动清除reference；长期stack若保留large objects会形成memory leak-like retention。

<Callout type="trap" title="只减n而不清空slot">
Client已无法通过API访问removed item，但array仍持有strong reference，garbage collector判定它可达。测试不仅要检查pop result与size，还应确认vacated slots被清理，尤其是long-lived buffers。
</Callout>

Java不能直接 `new Item[capacity]`，因为generic type erasure与reified arrays不兼容。Official code创建 `new Object[capacity]` 后做受控cast；安全依赖private array只通过Item-typed API进入，不能把它暴露成可写Object array。

## 1.3.3 Linked lists：节点与局部pointer rewiring

链表（<Term def="由item与next reference组成的nodes连接而成，logical order不要求contiguous storage的representation。">链表</Term>，linked lists）把capacity问题换成每item node overhead和pointer locality。Singly linked stack把top保存在 `first`，push在head插入，pop移除head：

```java
private Node<Item> first;
private int n;

public void push(Item item) {
    Node<Item> oldFirst = first;
    first = new Node<Item>();
    first.item = item;
    first.next = oldFirst;
    n++;
}

public Item pop() {
    if (isEmpty()) throw new NoSuchElementException("Stack underflow");
    Item item = first.item;
    first = first.next;
    n--;
    return item;
}
```

<Algs4LinkedStackLab />

每个operation只改constant number of references，因此worst-case constant time，不需要amortization。Representation invariant包括：`n == 0` iff `first == null`；沿next恰好走到n个nodes后到null；不存在cycle。只测push/pop outputs无法发现错误n或隐藏cycle，invariant checker应独立遍历。

## 1.3.4 Linked queue：first与last必须同步

Queue需要在tail插入、head删除，因此同时保存 `first` 与 `last`。Enqueue到empty queue时二者指向same new node；dequeue最后一个item后，`first` 变null，也必须把 `last` 设null。

<Algs4LinkedQueueLab />

```java
public void enqueue(Item item) {
    Node<Item> oldLast = last;
    last = new Node<Item>();
    last.item = item;
    if (isEmpty()) first = last;
    else oldLast.next = last;
    n++;
}

public Item dequeue() {
    if (isEmpty()) throw new NoSuchElementException("Queue underflow");
    Item item = first.item;
    first = first.next;
    n--;
    if (isEmpty()) last = null;
    return item;
}
```

如果遗漏最后一行，logical queue虽empty，`last` 仍引用removed node；下一次enqueue的empty branch会重设first/last，功能测试可能暂时通过，但representation不干净并保留旧object。应核查empty iff first和last都null，nonempty时last.next为null。

<Callout type="trap" title="只维护first却要求O(1) enqueue">
Singly linked list只有head pointer时，在tail enqueue必须每次遍历到末端，退化为linear time。要么维护last invariant，要么改变API或representation；不能在复杂度说明中假装有一个未保存的tail。
</Callout>

## 1.3.5 Iteration：暴露顺序，不暴露representation

迭代（<Term def="collection提供Iterator顺序访问items，使foreach client不依赖array indices或node pointers。">迭代</Term>，iteration）由 `Iterable<Item>` 与 `Iterator<Item>` 协作。Collection的 `iterator()` 创建独立cursor；`hasNext` 判断，`next` 返回item并推进。Array stack从 `n-1` 倒着走，linked stack从first沿next走，二者都给LIFO order。

```java
private class ReverseArrayIterator implements Iterator<Item> {
    private int i = n - 1;

    public boolean hasNext() { return i >= 0; }
    public Item next() {
        if (!hasNext()) throw new NoSuchElementException();
        return a[i--];
    }
}
```

<Algs4IterationOrderLab />

Iterator封装traversal mechanics，但order仍是behavior。Queue official iterator给FIFO；Stack给LIFO；Bag只保证visit all items，client不应把当前linked implementation的reverse insertion order当永久contract。Concurrent modification policy也要明确；本节minimal iterators不是fail-fast correctness guarantee。

## 1.3.6 Dijkstra双栈求值：ADT policy直接成为算法

Fully parenthesized arithmetic expression可用两个stacks单遍求值：operator压入ops，number压入vals；遇右括号时弹出一个operator和所需operands，计算后把result压回vals。LIFO正好匹配最近尚未完成的nested operation。

<Algs4TwoStackEvaluateLab />

```java
Stack<String> ops = new Stack<String>();
Stack<Double> vals = new Stack<Double>();

while (!StdIn.isEmpty()) {
    String token = StdIn.readString();
    if      (token.equals("(")) { }
    else if (token.equals("+")) ops.push(token);
    else if (token.equals("*")) ops.push(token);
    else if (token.equals(")")) {
        String op = ops.pop();
        double right = vals.pop();
        double left = vals.pop();
        vals.push(op.equals("+") ? left + right : left * right);
    }
    else vals.push(Double.parseDouble(token));
}
```

Official `Evaluate` 支持 `+ - * / sqrt`，并要求tokens由whitespace分开且每个operation fully parenthesized。Subtraction和division必须保留operand order：先pop的是right operand。若输入malformed，underflow或剩余items应被视为parse failure，不能只打印偶然top value。

## 统一验收：从访问policy到representation invariant

<Stepper>
  <Step title="先由problem确定collection semantics">
    <Algs4CollectionSemanticsLab />
    写出insert、remove、iteration与underflow behavior，禁止caller自行约定indices。
  </Step>
  <Step title="验证resizing thresholds与内存清理">
    <Algs4ResizingArrayLab />
    覆盖full push、quarter-full pop、empty、oscillation和vacated-slot nulling。
  </Step>
  <Step title="逐指针核查linked stack">
    <Algs4LinkedStackLab />
    每次push/pop后验证first、n、node count、termination与returned item。
  </Step>
  <Step title="同步queue的两个endpoints">
    <Algs4LinkedQueueLab />
    单独覆盖first enqueue、last dequeue和empty-to-nonempty transition。
  </Step>
  <Step title="用application验证访问顺序">
    <Algs4TwoStackEvaluateLab />
    对nested expressions追踪ops/vals，并验证noncommutative operand order与malformed input。
  </Step>
</Stepper>

## 本章回顾

1. Bags, queues, and stacks以API固定collection访问policy，而非只换operation names。
2. Collection APIs应说明generic item、underflow、size与iteration behavior。
3. Resizing arrays以doubling和quarter-full halving获得amortized constant operations与bounded spare space。
4. Removed array slots要null以避免loitering，generic arrays需要受控representation。
5. Linked stack只维护first；linked queue维护first/last并在empty transition同步清理。
6. Iteration隐藏traversal mechanics，但client-visible order仍需明确。
7. Dijkstra two-stack evaluation说明LIFO policy如何直接支撑nested expression algorithm。

<Exercises>

**问题 1：capacity为16、size为16时push一次会发生什么？之后size与capacity是多少？**

<Answer>先分配capacity 32的新array并复制16个items，再把新item写入index 16；最终size 17、capacity 32。该次是linear copy，但在连续push序列中为amortized constant。</Answer>

**问题 2：为什么pop在half-full时立即缩容可能很慢？**

<Answer>若size在half boundary两侧交替，pop触发halve后下一次push立即double，两次都复制linear数量items。等到quarter-full才halve提供hysteresis，使缩容后array至少half empty但不会马上再次扩容。</Answer>

**问题 3：linked queue删除唯一item后只设置 `first = null`，遗漏 `last = null` 有什么风险？**

<Answer>Representation invariant被破坏且last保留removed node，造成stale reference与内存保留；后续代码若错误依赖last也可能链接到旧chain。Empty state必须同时满足first和last为null。</Answer>

**问题 4：求值 `8 / 2` 时为何要先保存right再pop left？**

<Answer>Stack top是最近读到的2，即right operand；下一项8才是left。Division与subtraction不可交换，若直接按pop顺序计算会得到2/8而不是8/2。</Answer>

</Exercises>

## 名词解释

<Glossary>
  <GlossaryItem term="背包、队列与栈">分别表达收集、FIFO与LIFO访问策略的collection ADTs。</GlossaryItem>
  <GlossaryItem term="集合API">定义generic insert/remove/query/iteration behavior的public contract。</GlossaryItem>
  <GlossaryItem term="动态调整数组">按几何倍率扩缩capacity的contiguous collection representation。</GlossaryItem>
  <GlossaryItem term="对象游离">logical removal后stale reference仍阻止object被回收。</GlossaryItem>
  <GlossaryItem term="链表">以item和next组成nodes并通过references保持logical order的结构。</GlossaryItem>
  <GlossaryItem term="迭代">通过Iterator逐项访问collection而不暴露底层representation。</GlossaryItem>
</Glossary>

<Attribution
  name="Algorithms, 4th Edition · Section 1.3 Bags, Queues, and Stacks"
  href="https://algs4.cs.princeton.edu/13stacks/"
  author="Robert Sedgewick and Kevin Wayne"
  license="原书、官方页面与Java示例版权归Robert Sedgewick、Kevin Wayne及Addison-Wesley所有；本页依据作者官方1.3 synopsis及ResizingArrayStack、Stack、Queue、Evaluate代码独立重构教学内容"
/>
