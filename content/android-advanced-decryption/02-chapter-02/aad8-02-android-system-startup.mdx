---
title: "第2章 Android系统启动"
description: "沿init、Zygote、SystemServer和Launcher四段主链追踪Android 8.0从PID 1到应用图标可见的启动过程"
type: C
section: "Android进阶解密（Android 8.0源码 / 2018年初版）"
order: 2
draft: false
math: false
sourceUrl: "https://www.phei.com.cn/module/goods/wssd_content.jsp?bookid=52919"
demo: true
qualityVersion: 2
practiceMode: diagnosis
sourceMode: independent-rewrite
officialUnitId: aad8-02-android-system-startup
---

import { Objectives } from "@/components/mdx/objectives";
import { Term } from "@/components/mdx/term";
import { Step, Stepper } from "@/components/mdx/stepper";
import { Callout } from "@/components/mdx/callout";
import { Answer, Exercises } from "@/components/mdx/exercises";
import { Glossary, GlossaryItem } from "@/components/mdx/glossary";
import { Attribution } from "@/components/mdx/attribution";
import { Aad802AndroidSystemStartupDecisionLab, Aad802AndroidSystemStartupMechanismMap, Aad802AndroidSystemStartupFailureDiagram } from "@/components/mdx/android-advanced-decryption/aad8-02-android-system-startup";

# 第2章 Android系统启动

在《第2章 Android系统启动》中，本课程对应刘望舒《Android进阶解密》，电子工业出版社2018年10月初版，468页、702千字，ISBN 9787121348389。原书以Android 8.0系统源码为主；本页不会用Android 10后的ATMS、现代ART实现、Perfetto或新隐藏API政策替换原书链路。

沿init、Zygote、SystemServer和Launcher四段主链追踪Android 8.0从PID 1到应用图标可见的启动过程。源码实验固定AOSP Android 8.0与作者出版仓库提交 3860d9e1；后续版本只作为迁移差异，不改变正式目录分母。

<Objectives>

- 能解释“第2章 Android系统启动”全部正式目录节点，并定位Android 8.0中的入口类、函数与源码路径。
- 能绘制进程、线程、Binder/JNI/加载边界和关键状态转移，区分请求发出与系统完成。
- 能设计单变量故障实验，验证“每个阶段都明确谁创建下一进程、使用何种配置或IPC、何时进入消息循环，以及何种日志证明阶段完成”。
- 能写出包含版本、源码、日志/追踪、反例、停止、恢复、回退和复核人的独立交接记录。

</Objectives>

<Aad802AndroidSystemStartupMechanismMap />

<Aad802AndroidSystemStartupDecisionLab />

<Aad802AndroidSystemStartupFailureDiagram />

## 从一条可证伪的调用链开始

先预测：把启动描述成线性函数列表会忽略init action/service触发、Zygote socket、SystemServer服务依赖与Launcher查询包信息的跨进程边界。把预测写成入口、对象身份、进程/线程、状态和完成回调五列，再去读源码。若结果与预测不同，先修正模型，不要选择性截取调用栈。

原书面向应用开发者学习系统源码，重点不是抄完整函数，而是抓住跨层调用的关键节点。每个节点都要回答：谁发起、在哪个进程和线程执行、哪份状态被修改、后续如何被唤醒、失败如何返回、什么现象代表真正完成。

验收不变量是：每个阶段都明确谁创建下一进程、使用何种配置或IPC、何时进入消息循环，以及何种日志证明阶段完成。只有源码版本、符号关系、运行轨迹与最终状态四类证据一致，结论才可以进入下一章。

## 核心词汇与版本门

<Term def="init是本页第1个源码概念，必须同时说明Android 8.0定义、调用者、进程线程、状态、失败反例与证据">init</Term>、<Term def="init.rc是本页第2个源码概念，必须同时说明Android 8.0定义、调用者、进程线程、状态、失败反例与证据">init.rc</Term>、<Term def="Zygote是本页第3个源码概念，必须同时说明Android 8.0定义、调用者、进程线程、状态、失败反例与证据">Zygote</Term>、<Term def="SystemServer是本页第4个源码概念，必须同时说明Android 8.0定义、调用者、进程线程、状态、失败反例与证据">SystemServer</Term>、<Term def="Launcher是本页第5个源码概念，必须同时说明Android 8.0定义、调用者、进程线程、状态、失败反例与证据">Launcher</Term>

以上词汇都固定在Android 8.0语义。在《第2章 Android系统启动》中，Android 7.0只在原书明确比较AMS家族时出现；Android 9以后反射/隐藏API限制、Android 10的ActivityTaskManager、现代Profiler/Perfetto等只能写进迁移备注，不能改写本页正式链路。

## 原书目录核对清单

本页承担23个目录或复习节点，正文、图解、实验和题目必须能反向定位每一项：

- 2.1 init进程启动过程
- 2.1.1 引入init进程
- 2.1.2 init进程的入口函数
- 2.1.3 解析init.rc
- 2.1.4 解析Service类型语句
- 2.1.5 init启动Zygote
- 2.1.6 属性服务
- 2.1.7 init进程启动总结
- 2.2 Zygote进程启动过程
- 2.2.1 Zygote概述
- 2.2.2 Zygote启动脚本
- 2.2.3 Zygote进程启动过程介绍
- 2.2.4 Zygote进程启动总结
- 2.3 SystemServer处理过程
- 2.3.1 Zygote处理SystemServer进程
- 2.3.2 解析SystemServer进程
- 2.3.3 SystemServer进程总结
- 2.4 Launcher启动过程
- 2.4.1 Launcher概述
- 2.4.2 Launcher启动过程介绍
- 2.4.3 Launcher中应用图标显示过程
- 2.5 Android系统启动流程
- 2.6 本章小结

## 分步追踪：源码、运行与反例

<Stepper>
  <Step title="定位源码入口与责任边界">
    固定Android
    8.0标签，从公开入口向下追踪，不跳过Binder代理、JNI注册、ClassLoader查找或system_server状态对象。
  </Step>
  <Step title="注入一个失败变量">
    只改变进程未启动、Binder回调延迟、类先加载、token无效、帧负载或对象引用中的一个变量，先写预测再运行。
  </Step>
  <Step title="关闭独立证据门">
    对照正常与失败轨迹，核查源码符号、日志、状态、资源释放和最终业务结果，记录停止与回退条件。
    </Step>
</Stepper>

## 可复现实验

```text
kernel -> init(PID 1) -> app_process -> Zygote -> system_server -> Launcher
```

```text
on boot -> class core -> service zygote -> socket zygote -> start
```

```bash
adb shell ps -A | grep -E 'zygote|system_server|launcher'
adb shell getprop sys.boot_completed
```

动手试：在同一镜像、同一构建和同一输入上重复正常、冷启动/首次加载、热路径、单变量失败与恢复五轮。在《第2章 Android系统启动》中，记录PID/TID、调用入口、对象或token、状态前后值、耗时、异常/返回码和最终可见结果；性能章再记录P50/P95/P99，内存章保留GC Root路径。

<Callout type="trap" title="跨版本拼接调用链">
  类名相似不代表实现相同。Android
  8.0没有后来的ATMS体系，运行时、工具和隐藏API规则也不同；跨版本代码只能独立标注差异。
</Callout>

<Callout type="trap" title="把请求发出当成系统完成">
  Binder
  transact、fork返回、消息入队、补丁下载或View加入本地数组都只是中间状态。必须观察目标进程回调、系统状态、首帧或资源释放。
</Callout>

## 独立证据门

第2章 Android系统启动 的在《第2章 Android系统启动》中，最小证据包包含：出版社目录、Android版本、源码文件和符号、调用/状态图、PID/TID或加载器身份、一条失败注入、日志/追踪/堆证据、最终结果、已知限制、停止、恢复、回退、责任人与复核人。

## 练习

<Exercises>

**问题 1：为什么“第2章 Android系统启动”必须固定Android 8.0？**

<Answer>
  系统服务拆分、类名、单例入口、ART、工具和隐藏API约束都会演进。固定版本才能让目录、源码路径、作者样例和运行轨迹一一对应。
</Answer>

**问题 2：怎样构造“把启动描述成线性函数列表会忽略init action/service触发、Zygote socket、SystemServer服务依赖与Launcher查询包信息的跨进程边界”的最小反例？**

<Answer>
  固定镜像、构建、数据和操作，只改变一个进程、线程、token、加载顺序、帧负载或引用变量；比较入口、状态、回调、返回码与最终系统事实。
</Answer>

**问题 3：何时可以认为本页完成独立交接？**

<Answer>
  另一位读者无需口头补充即可定位全部目录节点，按相同版本复现正常与失败轨迹，并依据“每个阶段都明确谁创建下一进程、使用何种配置或IPC、何时进入消息循环，以及何种日志证明阶段完成”得到相同验收结论。
</Answer>

</Exercises>

## 本章回顾

“第2章 Android系统启动”的核心是沿init、Zygote、SystemServer和Launcher四段主链追踪Android 8.0从PID 1到应用图标可见的启动过程。把启动描述成线性函数列表会忽略init action/service触发、Zygote socket、SystemServer服务依赖与Launcher查询包信息的跨进程边界是必须保留的反证；源码阅读只有连接运行时的进程、线程、状态与最终结果，才从“知道类名”升级为“能诊断”。

<Glossary>
  <GlossaryItem term="init">
    init服务于“沿init、Zygote、SystemServer和Launcher四段主链追踪Android
    8.0从PID 1到应用图标可见的启动过程”。掌握标准是能定位Android
    8.0源码、画出运行轨迹、构造单变量反例，并用“每个阶段都明确谁创建下一进程、使用何种配置或IPC、何时进入消息循环，以及何种日志证明阶段完成”独立验收。
  </GlossaryItem>
  <GlossaryItem term="init.rc">
    init.rc服务于“沿init、Zygote、SystemServer和Launcher四段主链追踪Android
    8.0从PID 1到应用图标可见的启动过程”。掌握标准是能定位Android
    8.0源码、画出运行轨迹、构造单变量反例，并用“每个阶段都明确谁创建下一进程、使用何种配置或IPC、何时进入消息循环，以及何种日志证明阶段完成”独立验收。
  </GlossaryItem>
  <GlossaryItem term="Zygote">
    Zygote服务于“沿init、Zygote、SystemServer和Launcher四段主链追踪Android
    8.0从PID 1到应用图标可见的启动过程”。掌握标准是能定位Android
    8.0源码、画出运行轨迹、构造单变量反例，并用“每个阶段都明确谁创建下一进程、使用何种配置或IPC、何时进入消息循环，以及何种日志证明阶段完成”独立验收。
  </GlossaryItem>
  <GlossaryItem term="SystemServer">
    SystemServer服务于“沿init、Zygote、SystemServer和Launcher四段主链追踪Android
    8.0从PID 1到应用图标可见的启动过程”。掌握标准是能定位Android
    8.0源码、画出运行轨迹、构造单变量反例，并用“每个阶段都明确谁创建下一进程、使用何种配置或IPC、何时进入消息循环，以及何种日志证明阶段完成”独立验收。
  </GlossaryItem>
  <GlossaryItem term="Launcher">
    Launcher服务于“沿init、Zygote、SystemServer和Launcher四段主链追踪Android
    8.0从PID 1到应用图标可见的启动过程”。掌握标准是能定位Android
    8.0源码、画出运行轨迹、构造单变量反例，并用“每个阶段都明确谁创建下一进程、使用何种配置或IPC、何时进入消息循环，以及何种日志证明阶段完成”独立验收。
  </GlossaryItem>
</Glossary>

[← 上一页：第1章 Android系统架构](/learn/android-advanced-decryption/01-chapter-01/aad8-01-android-system-architecture) · [下一页：第3章 应用程序进程启动过程 →](/learn/android-advanced-decryption/03-chapter-03/aad8-03-app-process-startup)

## 原版目录概念补充核对

以下条目补齐官方目录中容易被示例主线掩盖的概念。它们不重复罗列目录，而是明确每项概念的机制、适用边界和验收证据。

### 2.1.1 引入init进程：机制、边界与证据

第2章 Android系统启动中的2.1.1 引入init进程要沿 Android 8.0 的真实启动或 IPC 链路解释：先定位入口源码与调用者，再标出进程、线程、Binder 对象、状态写入和完成回调。固定 AOSP 标签后，用 PID/TID、调用栈、关键日志与一个进程未就绪或回调延迟样本核对先后关系。

### 2.1.2 init进程的入口函数：机制、边界与证据

第2章 Android系统启动中的2.1.2 init进程的入口函数要沿 Android 8.0 的真实启动或 IPC 链路解释：先定位入口源码与调用者，再标出进程、线程、Binder 对象、状态写入和完成回调。固定 AOSP 标签后，用 PID/TID、调用栈、关键日志与一个进程未就绪或回调延迟样本核对先后关系。

### 2.1.3 解析init.rc：机制、边界与证据

第2章 Android系统启动中的2.1.3 解析init.rc要沿 Android 8.0 的真实启动或 IPC 链路解释：先定位入口源码与调用者，再标出进程、线程、Binder 对象、状态写入和完成回调。固定 AOSP 标签后，用 PID/TID、调用栈、关键日志与一个进程未就绪或回调延迟样本核对先后关系。

### 2.1.4 解析Service类型语句：机制、边界与证据

第2章 Android系统启动中的2.1.4 解析Service类型语句由 framework 对象、system_server 状态和应用进程回调共同完成，API 返回不等于系统状态已经落定。应跟踪 token/record、目标进程与线程、生命周期回调和最终窗口或组件状态，并用无效 token、进程重建或迟到回调验证拒绝与恢复路径。

### 2.1.5 init启动Zygote：机制、边界与证据

第2章 Android系统启动中的2.1.5 init启动Zygote要沿 Android 8.0 的真实启动或 IPC 链路解释：先定位入口源码与调用者，再标出进程、线程、Binder 对象、状态写入和完成回调。固定 AOSP 标签后，用 PID/TID、调用栈、关键日志与一个进程未就绪或回调延迟样本核对先后关系。

### 2.1.6 属性服务：机制、边界与证据

第2章 Android系统启动中的2.1.6 属性服务必须落回 Android 8.0 源码中的入口、对象、进程/线程、状态变化与完成点。固定版本和输入，先预测正常链路，再只注入一个失败条件，以源码符号、运行日志和最终系统状态三方核对。

### 2.1.7 init进程启动总结：机制、边界与证据

第2章 Android系统启动中的2.1.7 init进程启动总结要沿 Android 8.0 的真实启动或 IPC 链路解释：先定位入口源码与调用者，再标出进程、线程、Binder 对象、状态写入和完成回调。固定 AOSP 标签后，用 PID/TID、调用栈、关键日志与一个进程未就绪或回调延迟样本核对先后关系。

### 2.2 Zygote进程启动过程：机制、边界与证据

第2章 Android系统启动中的2.2 Zygote进程启动过程要沿 Android 8.0 的真实启动或 IPC 链路解释：先定位入口源码与调用者，再标出进程、线程、Binder 对象、状态写入和完成回调。固定 AOSP 标签后，用 PID/TID、调用栈、关键日志与一个进程未就绪或回调延迟样本核对先后关系。

### 2.2.1 Zygote概述：机制、边界与证据

第2章 Android系统启动中的2.2.1 Zygote概述要沿 Android 8.0 的真实启动或 IPC 链路解释：先定位入口源码与调用者，再标出进程、线程、Binder 对象、状态写入和完成回调。固定 AOSP 标签后，用 PID/TID、调用栈、关键日志与一个进程未就绪或回调延迟样本核对先后关系。

### 2.2.2 Zygote启动脚本：机制、边界与证据

第2章 Android系统启动中的2.2.2 Zygote启动脚本要沿 Android 8.0 的真实启动或 IPC 链路解释：先定位入口源码与调用者，再标出进程、线程、Binder 对象、状态写入和完成回调。固定 AOSP 标签后，用 PID/TID、调用栈、关键日志与一个进程未就绪或回调延迟样本核对先后关系。

### 2.2.3 Zygote进程启动过程介绍：机制、边界与证据

第2章 Android系统启动中的2.2.3 Zygote进程启动过程介绍要沿 Android 8.0 的真实启动或 IPC 链路解释：先定位入口源码与调用者，再标出进程、线程、Binder 对象、状态写入和完成回调。固定 AOSP 标签后，用 PID/TID、调用栈、关键日志与一个进程未就绪或回调延迟样本核对先后关系。

### 2.2.4 Zygote进程启动总结：机制、边界与证据

第2章 Android系统启动中的2.2.4 Zygote进程启动总结要沿 Android 8.0 的真实启动或 IPC 链路解释：先定位入口源码与调用者，再标出进程、线程、Binder 对象、状态写入和完成回调。固定 AOSP 标签后，用 PID/TID、调用栈、关键日志与一个进程未就绪或回调延迟样本核对先后关系。

### 2.3 SystemServer处理过程：机制、边界与证据

第2章 Android系统启动中的2.3 SystemServer处理过程要沿 Android 8.0 的真实启动或 IPC 链路解释：先定位入口源码与调用者，再标出进程、线程、Binder 对象、状态写入和完成回调。固定 AOSP 标签后，用 PID/TID、调用栈、关键日志与一个进程未就绪或回调延迟样本核对先后关系。

### 2.3.1 Zygote处理SystemServer进程：机制、边界与证据

第2章 Android系统启动中的2.3.1 Zygote处理SystemServer进程要沿 Android 8.0 的真实启动或 IPC 链路解释：先定位入口源码与调用者，再标出进程、线程、Binder 对象、状态写入和完成回调。固定 AOSP 标签后，用 PID/TID、调用栈、关键日志与一个进程未就绪或回调延迟样本核对先后关系。

### 2.3.2 解析SystemServer进程：机制、边界与证据

第2章 Android系统启动中的2.3.2 解析SystemServer进程要沿 Android 8.0 的真实启动或 IPC 链路解释：先定位入口源码与调用者，再标出进程、线程、Binder 对象、状态写入和完成回调。固定 AOSP 标签后，用 PID/TID、调用栈、关键日志与一个进程未就绪或回调延迟样本核对先后关系。

### 2.3.3 SystemServer进程总结：机制、边界与证据

第2章 Android系统启动中的2.3.3 SystemServer进程总结要沿 Android 8.0 的真实启动或 IPC 链路解释：先定位入口源码与调用者，再标出进程、线程、Binder 对象、状态写入和完成回调。固定 AOSP 标签后，用 PID/TID、调用栈、关键日志与一个进程未就绪或回调延迟样本核对先后关系。

### 2.4 Launcher启动过程：机制、边界与证据

第2章 Android系统启动中的2.4 Launcher启动过程要沿 Android 8.0 的真实启动或 IPC 链路解释：先定位入口源码与调用者，再标出进程、线程、Binder 对象、状态写入和完成回调。固定 AOSP 标签后，用 PID/TID、调用栈、关键日志与一个进程未就绪或回调延迟样本核对先后关系。

### 2.4.1 Launcher概述：机制、边界与证据

第2章 Android系统启动中的2.4.1 Launcher概述要沿 Android 8.0 的真实启动或 IPC 链路解释：先定位入口源码与调用者，再标出进程、线程、Binder 对象、状态写入和完成回调。固定 AOSP 标签后，用 PID/TID、调用栈、关键日志与一个进程未就绪或回调延迟样本核对先后关系。

### 2.4.2 Launcher启动过程介绍：机制、边界与证据

第2章 Android系统启动中的2.4.2 Launcher启动过程介绍要沿 Android 8.0 的真实启动或 IPC 链路解释：先定位入口源码与调用者，再标出进程、线程、Binder 对象、状态写入和完成回调。固定 AOSP 标签后，用 PID/TID、调用栈、关键日志与一个进程未就绪或回调延迟样本核对先后关系。

### 2.4.3 Launcher中应用图标显示过程：机制、边界与证据

第2章 Android系统启动中的2.4.3 Launcher中应用图标显示过程要沿 Android 8.0 的真实启动或 IPC 链路解释：先定位入口源码与调用者，再标出进程、线程、Binder 对象、状态写入和完成回调。固定 AOSP 标签后，用 PID/TID、调用栈、关键日志与一个进程未就绪或回调延迟样本核对先后关系。

### 2.5 Android系统启动流程：机制、边界与证据

第2章 Android系统启动中的2.5 Android系统启动流程必须落回 Android 8.0 源码中的入口、对象、进程/线程、状态变化与完成点。固定版本和输入，先预测正常链路，再只注入一个失败条件，以源码符号、运行日志和最终系统状态三方核对。

### 2.6 本章小结：机制、边界与证据

第2章 Android系统启动中的2.6 本章小结必须落回 Android 8.0 源码中的入口、对象、进程/线程、状态变化与完成点。固定版本和输入，先预测正常链路，再只注入一个失败条件，以源码符号、运行日志和最终系统状态三方核对。

<Attribution
  adaptedFrom="刘望舒《Android进阶解密》2018年初版（Android 8.0源码）"
  adaptedUrl="https://www.phei.com.cn/module/goods/wssd_content.jsp?bookid=52919"
  mode="independent-rewrite"
  sourceBasis="outline-only"
/>
