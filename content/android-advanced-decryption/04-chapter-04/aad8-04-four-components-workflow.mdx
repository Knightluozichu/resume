---
title: "第4章 四大组件的工作过程"
description: "以Activity、启动/绑定Service、动态广播和ContentProvider为四条端到端链路，连接ContextImpl、AMS、ApplicationThread与ActivityThread"
type: C
section: "Android进阶解密（Android 8.0源码 / 2018年初版）"
order: 4
draft: false
math: false
sourceUrl: "https://www.phei.com.cn/module/goods/wssd_content.jsp?bookid=52919"
demo: true
---

import { Objectives } from "@/components/mdx/objectives";
import { Term } from "@/components/mdx/term";
import { Step, Stepper } from "@/components/mdx/stepper";
import { Callout } from "@/components/mdx/callout";
import { Answer, Exercises } from "@/components/mdx/exercises";
import { Glossary, GlossaryItem } from "@/components/mdx/glossary";
import { Attribution } from "@/components/mdx/attribution";

# 第4章 四大组件的工作过程

本课程对应刘望舒《Android进阶解密》，电子工业出版社2018年10月初版，468页、702千字，ISBN 9787121348389。原书以Android 8.0系统源码为主；本页不会用Android 10后的ATMS、现代ART实现、Perfetto或新隐藏API政策替换原书链路。

以Activity、启动/绑定Service、动态广播和ContentProvider为四条端到端链路，连接ContextImpl、AMS、ApplicationThread与ActivityThread。源码实验固定AOSP Android 8.0与作者出版仓库提交 3860d9e1；后续版本只作为迁移差异，不改变正式目录分母。

<Objectives>

- 能解释“第4章 四大组件的工作过程”全部正式目录节点，并定位Android 8.0中的入口类、函数与源码路径。
- 能绘制进程、线程、Binder/JNI/加载边界和关键状态转移，区分请求发出与系统完成。
- 能设计单变量故障实验，验证“每个组件链都区分调用进程、system_server决策、目标进程调度和主线程回调，并能说明组件不存在或进程未启动时的分支”。
- 能写出包含版本、源码、日志/追踪、反例、停止、恢复、回退和复核人的独立交接记录。

</Objectives>

## 从一条可证伪的调用链开始

先预测：把startActivity、startService、sendBroadcast或query视为本地方法会遗漏Binder跳转、进程创建、调度队列与生命周期回调顺序。把预测写成入口、对象身份、进程/线程、状态和完成回调五列，再去读源码。若结果与预测不同，先修正模型，不要选择性截取调用栈。

原书面向应用开发者学习系统源码，重点不是抄完整函数，而是抓住跨层调用的关键节点。每个节点都要回答：谁发起、在哪个进程和线程执行、哪份状态被修改、后续如何被唤醒、失败如何返回、什么现象代表真正完成。

验收不变量是：每个组件链都区分调用进程、system_server决策、目标进程调度和主线程回调，并能说明组件不存在或进程未启动时的分支。只有源码版本、符号关系、运行轨迹与最终状态四类证据一致，结论才可以进入下一章。

## 核心词汇与版本门

<Term def="ApplicationThread是本页第1个源码概念，必须同时说明Android 8.0定义、调用者、进程线程、状态、失败反例与证据">ApplicationThread</Term>、<Term def="ActivityThread是本页第2个源码概念，必须同时说明Android 8.0定义、调用者、进程线程、状态、失败反例与证据">ActivityThread</Term>、<Term def="根Activity是本页第3个源码概念，必须同时说明Android 8.0定义、调用者、进程线程、状态、失败反例与证据">根Activity</Term>、<Term def="Service绑定是本页第4个源码概念，必须同时说明Android 8.0定义、调用者、进程线程、状态、失败反例与证据">Service绑定</Term>、<Term def="ContentProvider是本页第5个源码概念，必须同时说明Android 8.0定义、调用者、进程线程、状态、失败反例与证据">ContentProvider</Term>

以上词汇都固定在Android 8.0语义。Android 7.0只在原书明确比较AMS家族时出现；Android 9以后反射/隐藏API限制、Android 10的ActivityTaskManager、现代Profiler/Perfetto等只能写进迁移备注，不能改写本页正式链路。

## 原书目录逐节点重构

### 4.1 根Activity的启动过程

把“根Activity的启动过程”放回以Activity、启动/绑定Service、动态广播和ContentProvider为四条端到端链路，连接ContextImpl、AMS、ApplicationThread与ActivityThread的主链，明确入口、关键状态、跨进程或跨层边界与完成点。 阅读时先预测正常轨迹，再只注入一个失败变量，并以“每个组件链都区分调用进程、system_server决策、目标进程调度和主线程回调，并能说明组件不存在或进程未启动时的分支”核验源码、日志和最终状态。

本节采用“入口 -> 关键对象 -> 状态转移 -> 跨进程/Native边界 -> 完成回调”的五列追踪法。保持Android 8.0镜像、样例和操作不变，只改变一个进程、线程、加载顺序或资源压力变量，避免把相关性误写成因果。

#### 4.1.1 Launcher请求AMS过程

追踪AMS选择Home、启动Launcher及Launcher通过PackageManager查询可启动Activity并构造图标模型的过程。 阅读时先预测正常轨迹，再只注入一个失败变量，并以“每个组件链都区分调用进程、system_server决策、目标进程调度和主线程回调，并能说明组件不存在或进程未启动时的分支”核验源码、日志和最终状态。

**源码核查：** 记录版本、文件/符号、进程、线程、输入、状态变化、返回或回调；若只有概念图而没有一条运行证据，本节点不通过。

#### 4.1.2 AMS到ApplicationThread的调用过程

在客户端代理与system_server实体之间标出Binder边界，再用record、task、stack数据结构解释调度与任务归属。 阅读时先预测正常轨迹，再只注入一个失败变量，并以“每个组件链都区分调用进程、system_server决策、目标进程调度和主线程回调，并能说明组件不存在或进程未启动时的分支”核验源码、日志和最终状态。

**源码核查：** 记录版本、文件/符号、进程、线程、输入、状态变化、返回或回调；若只有概念图而没有一条运行证据，本节点不通过。

#### 4.1.3 ActivityThread启动Activity的过程

ApplicationThread是AMS回调应用进程的Binder端点，ActivityThread.H把调度切回主线程；必须同时记录Binder线程和main线程。 阅读时先预测正常轨迹，再只注入一个失败变量，并以“每个组件链都区分调用进程、system_server决策、目标进程调度和主线程回调，并能说明组件不存在或进程未启动时的分支”核验源码、日志和最终状态。

**源码核查：** 记录版本、文件/符号、进程、线程、输入、状态变化、返回或回调；若只有概念图而没有一条运行证据，本节点不通过。

#### 4.1.4 根Activity启动过程中涉及的进程

把“根Activity启动过程中涉及的进程”放回以Activity、启动/绑定Service、动态广播和ContentProvider为四条端到端链路，连接ContextImpl、AMS、ApplicationThread与ActivityThread的主链，明确入口、关键状态、跨进程或跨层边界与完成点。 阅读时先预测正常轨迹，再只注入一个失败变量，并以“每个组件链都区分调用进程、system_server决策、目标进程调度和主线程回调，并能说明组件不存在或进程未启动时的分支”核验源码、日志和最终状态。

**源码核查：** 记录版本、文件/符号、进程、线程、输入、状态变化、返回或回调；若只有概念图而没有一条运行证据，本节点不通过。

### 4.2 Service的启动过程

区分start产生的启动状态与bind产生的连接引用；跟踪AMS中的ServiceRecord、目标进程调度及onCreate/onStartCommand/onBind。 阅读时先预测正常轨迹，再只注入一个失败变量，并以“每个组件链都区分调用进程、system_server决策、目标进程调度和主线程回调，并能说明组件不存在或进程未启动时的分支”核验源码、日志和最终状态。

本节采用“入口 -> 关键对象 -> 状态转移 -> 跨进程/Native边界 -> 完成回调”的五列追踪法。保持Android 8.0镜像、样例和操作不变，只改变一个进程、线程、加载顺序或资源压力变量，避免把相关性误写成因果。

#### 4.2.1 ContextImpl到AMS的调用过程

在客户端代理与system_server实体之间标出Binder边界，再用record、task、stack数据结构解释调度与任务归属。 阅读时先预测正常轨迹，再只注入一个失败变量，并以“每个组件链都区分调用进程、system_server决策、目标进程调度和主线程回调，并能说明组件不存在或进程未启动时的分支”核验源码、日志和最终状态。

**源码核查：** 记录版本、文件/符号、进程、线程、输入、状态变化、返回或回调；若只有概念图而没有一条运行证据，本节点不通过。

#### 4.2.2 ActivityThread启动Service

ApplicationThread是AMS回调应用进程的Binder端点，ActivityThread.H把调度切回主线程；必须同时记录Binder线程和main线程。 阅读时先预测正常轨迹，再只注入一个失败变量，并以“每个组件链都区分调用进程、system_server决策、目标进程调度和主线程回调，并能说明组件不存在或进程未启动时的分支”核验源码、日志和最终状态。

**源码核查：** 记录版本、文件/符号、进程、线程、输入、状态变化、返回或回调；若只有概念图而没有一条运行证据，本节点不通过。

### 4.3 Service的绑定过程

区分start产生的启动状态与bind产生的连接引用；跟踪AMS中的ServiceRecord、目标进程调度及onCreate/onStartCommand/onBind。 阅读时先预测正常轨迹，再只注入一个失败变量，并以“每个组件链都区分调用进程、system_server决策、目标进程调度和主线程回调，并能说明组件不存在或进程未启动时的分支”核验源码、日志和最终状态。

本节采用“入口 -> 关键对象 -> 状态转移 -> 跨进程/Native边界 -> 完成回调”的五列追踪法。保持Android 8.0镜像、样例和操作不变，只改变一个进程、线程、加载顺序或资源压力变量，避免把相关性误写成因果。

#### 4.3.1 ContextImpl到AMS的调用过程

在客户端代理与system_server实体之间标出Binder边界，再用record、task、stack数据结构解释调度与任务归属。 阅读时先预测正常轨迹，再只注入一个失败变量，并以“每个组件链都区分调用进程、system_server决策、目标进程调度和主线程回调，并能说明组件不存在或进程未启动时的分支”核验源码、日志和最终状态。

**源码核查：** 记录版本、文件/符号、进程、线程、输入、状态变化、返回或回调；若只有概念图而没有一条运行证据，本节点不通过。

#### 4.3.2 Service的绑定过程

区分start产生的启动状态与bind产生的连接引用；跟踪AMS中的ServiceRecord、目标进程调度及onCreate/onStartCommand/onBind。 阅读时先预测正常轨迹，再只注入一个失败变量，并以“每个组件链都区分调用进程、system_server决策、目标进程调度和主线程回调，并能说明组件不存在或进程未启动时的分支”核验源码、日志和最终状态。

**源码核查：** 记录版本、文件/符号、进程、线程、输入、状态变化、返回或回调；若只有概念图而没有一条运行证据，本节点不通过。

### 4.4 广播的注册、发送和接收过程

区分动态注册表、Intent解析与BroadcastQueue分发，记录有序/并行语义、目标进程和接收完成。 阅读时先预测正常轨迹，再只注入一个失败变量，并以“每个组件链都区分调用进程、system_server决策、目标进程调度和主线程回调，并能说明组件不存在或进程未启动时的分支”核验源码、日志和最终状态。

本节采用“入口 -> 关键对象 -> 状态转移 -> 跨进程/Native边界 -> 完成回调”的五列追踪法。保持Android 8.0镜像、样例和操作不变，只改变一个进程、线程、加载顺序或资源压力变量，避免把相关性误写成因果。

#### 4.4.1 广播的注册过程

区分动态注册表、Intent解析与BroadcastQueue分发，记录有序/并行语义、目标进程和接收完成。 阅读时先预测正常轨迹，再只注入一个失败变量，并以“每个组件链都区分调用进程、system_server决策、目标进程调度和主线程回调，并能说明组件不存在或进程未启动时的分支”核验源码、日志和最终状态。

**源码核查：** 记录版本、文件/符号、进程、线程、输入、状态变化、返回或回调；若只有概念图而没有一条运行证据，本节点不通过。

#### 4.4.2 广播的发送和接收过程

区分动态注册表、Intent解析与BroadcastQueue分发，记录有序/并行语义、目标进程和接收完成。 阅读时先预测正常轨迹，再只注入一个失败变量，并以“每个组件链都区分调用进程、system_server决策、目标进程调度和主线程回调，并能说明组件不存在或进程未启动时的分支”核验源码、日志和最终状态。

**源码核查：** 记录版本、文件/符号、进程、线程、输入、状态变化、返回或回调；若只有概念图而没有一条运行证据，本节点不通过。

### 4.5 Content Provider的启动过程

调用前先经AMS获取或启动provider，目标进程发布IBinder端点后客户端才执行数据操作；关注authority与引用计数。 阅读时先预测正常轨迹，再只注入一个失败变量，并以“每个组件链都区分调用进程、system_server决策、目标进程调度和主线程回调，并能说明组件不存在或进程未启动时的分支”核验源码、日志和最终状态。

本节采用“入口 -> 关键对象 -> 状态转移 -> 跨进程/Native边界 -> 完成回调”的五列追踪法。保持Android 8.0镜像、样例和操作不变，只改变一个进程、线程、加载顺序或资源压力变量，避免把相关性误写成因果。

#### 4.5.1 query方法到AMS的调用过程

在客户端代理与system_server实体之间标出Binder边界，再用record、task、stack数据结构解释调度与任务归属。 阅读时先预测正常轨迹，再只注入一个失败变量，并以“每个组件链都区分调用进程、system_server决策、目标进程调度和主线程回调，并能说明组件不存在或进程未启动时的分支”核验源码、日志和最终状态。

**源码核查：** 记录版本、文件/符号、进程、线程、输入、状态变化、返回或回调；若只有概念图而没有一条运行证据，本节点不通过。

#### 4.5.2 AMS启动Content Provider的过程

在客户端代理与system_server实体之间标出Binder边界，再用record、task、stack数据结构解释调度与任务归属。 阅读时先预测正常轨迹，再只注入一个失败变量，并以“每个组件链都区分调用进程、system_server决策、目标进程调度和主线程回调，并能说明组件不存在或进程未启动时的分支”核验源码、日志和最终状态。

**源码核查：** 记录版本、文件/符号、进程、线程、输入、状态变化、返回或回调；若只有概念图而没有一条运行证据，本节点不通过。

### 4.6 本章小结

把“本章小结”放回以Activity、启动/绑定Service、动态广播和ContentProvider为四条端到端链路，连接ContextImpl、AMS、ApplicationThread与ActivityThread的主链，明确入口、关键状态、跨进程或跨层边界与完成点。 阅读时先预测正常轨迹，再只注入一个失败变量，并以“每个组件链都区分调用进程、system_server决策、目标进程调度和主线程回调，并能说明组件不存在或进程未启动时的分支”核验源码、日志和最终状态。

本节采用“入口 -> 关键对象 -> 状态转移 -> 跨进程/Native边界 -> 完成回调”的五列追踪法。保持Android 8.0镜像、样例和操作不变，只改变一个进程、线程、加载顺序或资源压力变量，避免把相关性误写成因果。

## 本页完整节点清单

本页承担18个目录或复习节点，正文、图解、实验和题目必须能反向定位每一项：

- 4.1 根Activity的启动过程
- 4.1.1 Launcher请求AMS过程
- 4.1.2 AMS到ApplicationThread的调用过程
- 4.1.3 ActivityThread启动Activity的过程
- 4.1.4 根Activity启动过程中涉及的进程
- 4.2 Service的启动过程
- 4.2.1 ContextImpl到AMS的调用过程
- 4.2.2 ActivityThread启动Service
- 4.3 Service的绑定过程
- 4.3.1 ContextImpl到AMS的调用过程
- 4.3.2 Service的绑定过程
- 4.4 广播的注册、发送和接收过程
- 4.4.1 广播的注册过程
- 4.4.2 广播的发送和接收过程
- 4.5 Content Provider的启动过程
- 4.5.1 query方法到AMS的调用过程
- 4.5.2 AMS启动Content Provider的过程
- 4.6 本章小结


## 分步追踪：源码、运行与反例

<Stepper>
  <Step title="定位源码入口与责任边界">
    固定Android
    8.0标签，从公开入口向下追踪，不跳过Binder代理、JNI注册、ClassLoader查找或system_server状态对象。
  </Step>
  <Step title="注入一个失败变量">
    只改变进程未启动、Binder回调延迟、类先加载、token无效、帧负载或对象引用中的一个变量，先写预测再运行。
  </Step>
  <Step title="关闭独立证据门">
    对照正常与失败轨迹，核查源码符号、日志、状态、资源释放和最终业务结果，记录停止与回退条件。
    </Step>
</Stepper>

## 可复现实验

```text
client -> ContextImpl -> AMS proxy -> system_server -> ApplicationThread -> ActivityThread.H
```

```text
bindService: request -> publish binder -> connected callback -> unbind
```

```bash
adb shell am start -W com.example/.MainActivity
adb shell dumpsys activity services com.example
```

动手试：在同一镜像、同一构建和同一输入上重复正常、冷启动/首次加载、热路径、单变量失败与恢复五轮。记录PID/TID、调用入口、对象或token、状态前后值、耗时、异常/返回码和最终可见结果；性能章再记录P50/P95/P99，内存章保留GC Root路径。

<Callout type="trap" title="跨版本拼接调用链">
  类名相似不代表实现相同。Android
  8.0没有后来的ATMS体系，运行时、工具和隐藏API规则也不同；跨版本代码只能独立标注差异。
</Callout>

<Callout type="trap" title="把请求发出当成系统完成">
  Binder
  transact、fork返回、消息入队、补丁下载或View加入本地数组都只是中间状态。必须观察目标进程回调、系统状态、首帧或资源释放。
</Callout>

## 独立证据门

最小证据包包含：出版社目录、Android版本、源码文件和符号、调用/状态图、PID/TID或加载器身份、一条失败注入、日志/追踪/堆证据、最终结果、已知限制、停止、恢复、回退、责任人与复核人。

## 练习

<Exercises>

**问题 1：为什么“第4章 四大组件的工作过程”必须固定Android 8.0？**

<Answer>
  系统服务拆分、类名、单例入口、ART、工具和隐藏API约束都会演进。固定版本才能让目录、源码路径、作者样例和运行轨迹一一对应。
</Answer>

**问题 2：怎样构造“把startActivity、startService、sendBroadcast或query视为本地方法会遗漏Binder跳转、进程创建、调度队列与生命周期回调顺序”的最小反例？**

<Answer>
  固定镜像、构建、数据和操作，只改变一个进程、线程、token、加载顺序、帧负载或引用变量；比较入口、状态、回调、返回码与最终系统事实。
</Answer>

**问题 3：何时可以认为本页完成独立交接？**

<Answer>
  另一位读者无需口头补充即可定位全部目录节点，按相同版本复现正常与失败轨迹，并依据“每个组件链都区分调用进程、system_server决策、目标进程调度和主线程回调，并能说明组件不存在或进程未启动时的分支”得到相同验收结论。
</Answer>

</Exercises>

## 本章回顾

“第4章 四大组件的工作过程”的核心是以Activity、启动/绑定Service、动态广播和ContentProvider为四条端到端链路，连接ContextImpl、AMS、ApplicationThread与ActivityThread。把startActivity、startService、sendBroadcast或query视为本地方法会遗漏Binder跳转、进程创建、调度队列与生命周期回调顺序是必须保留的反证；源码阅读只有连接运行时的进程、线程、状态与最终结果，才从“知道类名”升级为“能诊断”。

<Glossary>
  <GlossaryItem term="ApplicationThread">
    ApplicationThread服务于“以Activity、启动/绑定Service、动态广播和ContentProvider为四条端到端链路，连接ContextImpl、AMS、ApplicationThread与ActivityThread”。掌握标准是能定位Android
    8.0源码、画出运行轨迹、构造单变量反例，并用“每个组件链都区分调用进程、system_server决策、目标进程调度和主线程回调，并能说明组件不存在或进程未启动时的分支”独立验收。
  </GlossaryItem>
  <GlossaryItem term="ActivityThread">
    ActivityThread服务于“以Activity、启动/绑定Service、动态广播和ContentProvider为四条端到端链路，连接ContextImpl、AMS、ApplicationThread与ActivityThread”。掌握标准是能定位Android
    8.0源码、画出运行轨迹、构造单变量反例，并用“每个组件链都区分调用进程、system_server决策、目标进程调度和主线程回调，并能说明组件不存在或进程未启动时的分支”独立验收。
  </GlossaryItem>
  <GlossaryItem term="根Activity">
    根Activity服务于“以Activity、启动/绑定Service、动态广播和ContentProvider为四条端到端链路，连接ContextImpl、AMS、ApplicationThread与ActivityThread”。掌握标准是能定位Android
    8.0源码、画出运行轨迹、构造单变量反例，并用“每个组件链都区分调用进程、system_server决策、目标进程调度和主线程回调，并能说明组件不存在或进程未启动时的分支”独立验收。
  </GlossaryItem>
  <GlossaryItem term="Service绑定">
    Service绑定服务于“以Activity、启动/绑定Service、动态广播和ContentProvider为四条端到端链路，连接ContextImpl、AMS、ApplicationThread与ActivityThread”。掌握标准是能定位Android
    8.0源码、画出运行轨迹、构造单变量反例，并用“每个组件链都区分调用进程、system_server决策、目标进程调度和主线程回调，并能说明组件不存在或进程未启动时的分支”独立验收。
  </GlossaryItem>
  <GlossaryItem term="ContentProvider">
    ContentProvider服务于“以Activity、启动/绑定Service、动态广播和ContentProvider为四条端到端链路，连接ContextImpl、AMS、ApplicationThread与ActivityThread”。掌握标准是能定位Android
    8.0源码、画出运行轨迹、构造单变量反例，并用“每个组件链都区分调用进程、system_server决策、目标进程调度和主线程回调，并能说明组件不存在或进程未启动时的分支”独立验收。
  </GlossaryItem>
</Glossary>

[← 上一页：第3章 应用程序进程启动过程](/learn/android-advanced-decryption/03-chapter-03/aad8-03-app-process-startup) · [下一页：第5章 理解上下文Context →](/learn/android-advanced-decryption/05-chapter-05/aad8-05-context)

<Attribution
  adaptedFrom="刘望舒《Android进阶解密》2018年初版（Android 8.0源码）"
  adaptedUrl="https://www.phei.com.cn/module/goods/wssd_content.jsp?bookid=52919"
/>
