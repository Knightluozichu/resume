---
title: "第5章 理解上下文Context"
description: "沿Context、ContextWrapper与ContextImpl关系，比较Application、Activity和Service的Context创建、持有资源与生命周期"
type: C
section: "Android进阶解密（Android 8.0源码 / 2018年初版）"
order: 5
draft: true
math: false
sourceUrl: "https://www.phei.com.cn/module/goods/wssd_content.jsp?bookid=52919"
demo: true
---

# 第5章 理解上下文Context

本课程对应刘望舒《Android进阶解密》，电子工业出版社2018年10月初版，468页、702千字，ISBN 9787121348389。原书以Android 8.0系统源码为主；本页不会用Android 10后的ATMS、现代ART实现、Perfetto或新隐藏API政策替换原书链路。

沿Context、ContextWrapper与ContextImpl关系，比较Application、Activity和Service的Context创建、持有资源与生命周期。源码实验固定AOSP Android 8.0与作者出版仓库提交 3860d9e1；后续版本只作为迁移差异，不改变正式目录分母。

<Objectives>

- 能解释“第5章 理解上下文Context”全部正式目录节点，并定位Android 8.0中的入口类、函数与源码路径。
- 能绘制进程、线程、Binder/JNI/加载边界和关键状态转移，区分请求发出与系统完成。
- 能设计单变量故障实验，验证“每个Context引用都能说明base实现、主题/窗口能力、生命周期所有者与允许的操作；长生命周期对象不持有短生命周期Activity”。
- 能写出包含版本、源码、日志/追踪、反例、停止、恢复、回退和复核人的独立交接记录。

</Objectives>

## 从一条可证伪的调用链开始

先预测：把所有Context视为可互换会导致窗口token错误、主题丢失和Activity泄漏，也无法解释Application与Service的创建入口。把预测写成入口、对象身份、进程/线程、状态和完成回调五列，再去读源码。若结果与预测不同，先修正模型，不要选择性截取调用栈。

原书面向应用开发者学习系统源码，重点不是抄完整函数，而是抓住跨层调用的关键节点。每个节点都要回答：谁发起、在哪个进程和线程执行、哪份状态被修改、后续如何被唤醒、失败如何返回、什么现象代表真正完成。

验收不变量是：每个Context引用都能说明base实现、主题/窗口能力、生命周期所有者与允许的操作；长生命周期对象不持有短生命周期Activity。只有源码版本、符号关系、运行轨迹与最终状态四类证据一致，结论才可以进入下一章。

## 核心词汇与版本门

<Term def="Context是本页第1个源码概念，必须同时说明Android 8.0定义、调用者、进程线程、状态、失败反例与证据">Context</Term>、<Term def="ContextImpl是本页第2个源码概念，必须同时说明Android 8.0定义、调用者、进程线程、状态、失败反例与证据">ContextImpl</Term>、<Term def="ContextWrapper是本页第3个源码概念，必须同时说明Android 8.0定义、调用者、进程线程、状态、失败反例与证据">ContextWrapper</Term>、<Term def="Application Context是本页第4个源码概念，必须同时说明Android 8.0定义、调用者、进程线程、状态、失败反例与证据">Application Context</Term>、<Term def="ContextThemeWrapper是本页第5个源码概念，必须同时说明Android 8.0定义、调用者、进程线程、状态、失败反例与证据">ContextThemeWrapper</Term>

以上词汇都固定在Android 8.0语义。Android 7.0只在原书明确比较AMS家族时出现；Android 9以后反射/隐藏API限制、Android 10的ActivityTaskManager、现代Profiler/Perfetto等只能写进迁移备注，不能改写本页正式链路。

## 原书目录逐节点重构

### 5.1 Context的关联类

从ContextWrapper委托到ContextImpl，说明资源、包、系统服务与组件token从何而来，并按Application/Activity/Service生命周期选择引用。 阅读时先预测正常轨迹，再只注入一个失败变量，并以“每个Context引用都能说明base实现、主题/窗口能力、生命周期所有者与允许的操作；长生命周期对象不持有短生命周期Activity”核验源码、日志和最终状态。

本节采用“入口 -> 关键对象 -> 状态转移 -> 跨进程/Native边界 -> 完成回调”的五列追踪法。保持Android 8.0镜像、样例和操作不变，只改变一个进程、线程、加载顺序或资源压力变量，避免把相关性误写成因果。

### 5.2 Application Context的创建过程

从ContextWrapper委托到ContextImpl，说明资源、包、系统服务与组件token从何而来，并按Application/Activity/Service生命周期选择引用。 阅读时先预测正常轨迹，再只注入一个失败变量，并以“每个Context引用都能说明base实现、主题/窗口能力、生命周期所有者与允许的操作；长生命周期对象不持有短生命周期Activity”核验源码、日志和最终状态。

本节采用“入口 -> 关键对象 -> 状态转移 -> 跨进程/Native边界 -> 完成回调”的五列追踪法。保持Android 8.0镜像、样例和操作不变，只改变一个进程、线程、加载顺序或资源压力变量，避免把相关性误写成因果。

### 5.3 Application Context的获取过程

从ContextWrapper委托到ContextImpl，说明资源、包、系统服务与组件token从何而来，并按Application/Activity/Service生命周期选择引用。 阅读时先预测正常轨迹，再只注入一个失败变量，并以“每个Context引用都能说明base实现、主题/窗口能力、生命周期所有者与允许的操作；长生命周期对象不持有短生命周期Activity”核验源码、日志和最终状态。

本节采用“入口 -> 关键对象 -> 状态转移 -> 跨进程/Native边界 -> 完成回调”的五列追踪法。保持Android 8.0镜像、样例和操作不变，只改变一个进程、线程、加载顺序或资源压力变量，避免把相关性误写成因果。

### 5.4 Activity的Context创建过程

从ContextWrapper委托到ContextImpl，说明资源、包、系统服务与组件token从何而来，并按Application/Activity/Service生命周期选择引用。 阅读时先预测正常轨迹，再只注入一个失败变量，并以“每个Context引用都能说明base实现、主题/窗口能力、生命周期所有者与允许的操作；长生命周期对象不持有短生命周期Activity”核验源码、日志和最终状态。

本节采用“入口 -> 关键对象 -> 状态转移 -> 跨进程/Native边界 -> 完成回调”的五列追踪法。保持Android 8.0镜像、样例和操作不变，只改变一个进程、线程、加载顺序或资源压力变量，避免把相关性误写成因果。

### 5.5 Service的Context创建过程

区分start产生的启动状态与bind产生的连接引用；跟踪AMS中的ServiceRecord、目标进程调度及onCreate/onStartCommand/onBind。 阅读时先预测正常轨迹，再只注入一个失败变量，并以“每个Context引用都能说明base实现、主题/窗口能力、生命周期所有者与允许的操作；长生命周期对象不持有短生命周期Activity”核验源码、日志和最终状态。

本节采用“入口 -> 关键对象 -> 状态转移 -> 跨进程/Native边界 -> 完成回调”的五列追踪法。保持Android 8.0镜像、样例和操作不变，只改变一个进程、线程、加载顺序或资源压力变量，避免把相关性误写成因果。

### 5.6 本章小结

把“本章小结”放回沿Context、ContextWrapper与ContextImpl关系，比较Application、Activity和Service的Context创建、持有资源与生命周期的主链，明确入口、关键状态、跨进程或跨层边界与完成点。 阅读时先预测正常轨迹，再只注入一个失败变量，并以“每个Context引用都能说明base实现、主题/窗口能力、生命周期所有者与允许的操作；长生命周期对象不持有短生命周期Activity”核验源码、日志和最终状态。

本节采用“入口 -> 关键对象 -> 状态转移 -> 跨进程/Native边界 -> 完成回调”的五列追踪法。保持Android 8.0镜像、样例和操作不变，只改变一个进程、线程、加载顺序或资源压力变量，避免把相关性误写成因果。

## 本页完整节点清单

本页承担6个目录或复习节点，正文、图解、实验和题目必须能反向定位每一项：

- 5.1 Context的关联类
- 5.2 Application Context的创建过程
- 5.3 Application Context的获取过程
- 5.4 Activity的Context创建过程
- 5.5 Service的Context创建过程
- 5.6 本章小结

<Aad805ContextTraceLab />

## 分步追踪：源码、运行与反例

<Stepper>
  <Step title="定位源码入口与责任边界">
    固定Android
    8.0标签，从公开入口向下追踪，不跳过Binder代理、JNI注册、ClassLoader查找或system_server状态对象。
    <Aad805ContextTraceLab />
  </Step>
  <Step title="注入一个失败变量">
    只改变进程未启动、Binder回调延迟、类先加载、token无效、帧负载或对象引用中的一个变量，先写预测再运行。
    <Aad805ContextFaultLab />
  </Step>
  <Step title="关闭独立证据门">
    对照正常与失败轨迹，核查源码符号、日志、状态、资源释放和最终业务结果，记录停止与回退条件。
    </Step>
</Stepper>

## 可复现实验

```text
Context <- ContextWrapper <- Application / Service; ContextThemeWrapper <- Activity
```

```java
Context app = activity.getApplicationContext();
Context themed = activity;
```

```text
reference owner | context kind | required capability | release point
```

动手试：在同一镜像、同一构建和同一输入上重复正常、冷启动/首次加载、热路径、单变量失败与恢复五轮。记录PID/TID、调用入口、对象或token、状态前后值、耗时、异常/返回码和最终可见结果；性能章再记录P50/P95/P99，内存章保留GC Root路径。

<Callout type="trap" title="跨版本拼接调用链">
  类名相似不代表实现相同。Android
  8.0没有后来的ATMS体系，运行时、工具和隐藏API规则也不同；跨版本代码只能独立标注差异。
</Callout>

<Callout type="trap" title="把请求发出当成系统完成">
  Binder
  transact、fork返回、消息入队、补丁下载或View加入本地数组都只是中间状态。必须观察目标进程回调、系统状态、首帧或资源释放。
</Callout>

## 独立证据门

最小证据包包含：出版社目录、Android版本、源码文件和符号、调用/状态图、PID/TID或加载器身份、一条失败注入、日志/追踪/堆证据、最终结果、已知限制、停止、恢复、回退、责任人与复核人。

## 练习

<Exercises>

**问题 1：为什么“第5章 理解上下文Context”必须固定Android 8.0？**

<Answer>
  系统服务拆分、类名、单例入口、ART、工具和隐藏API约束都会演进。固定版本才能让目录、源码路径、作者样例和运行轨迹一一对应。
</Answer>

**问题 2：怎样构造“把所有Context视为可互换会导致窗口token错误、主题丢失和Activity泄漏，也无法解释Application与Service的创建入口”的最小反例？**

<Answer>
  固定镜像、构建、数据和操作，只改变一个进程、线程、token、加载顺序、帧负载或引用变量；比较入口、状态、回调、返回码与最终系统事实。
</Answer>

**问题 3：何时可以认为本页完成独立交接？**

<Answer>
  另一位读者无需口头补充即可定位全部目录节点，按相同版本复现正常与失败轨迹，并依据“每个Context引用都能说明base实现、主题/窗口能力、生命周期所有者与允许的操作；长生命周期对象不持有短生命周期Activity”得到相同验收结论。
</Answer>

</Exercises>

## 本章回顾

“第5章 理解上下文Context”的核心是沿Context、ContextWrapper与ContextImpl关系，比较Application、Activity和Service的Context创建、持有资源与生命周期。把所有Context视为可互换会导致窗口token错误、主题丢失和Activity泄漏，也无法解释Application与Service的创建入口是必须保留的反证；源码阅读只有连接运行时的进程、线程、状态与最终结果，才从“知道类名”升级为“能诊断”。

<Glossary>
  <GlossaryItem term="Context">
    Context服务于“沿Context、ContextWrapper与ContextImpl关系，比较Application、Activity和Service的Context创建、持有资源与生命周期”。掌握标准是能定位Android
    8.0源码、画出运行轨迹、构造单变量反例，并用“每个Context引用都能说明base实现、主题/窗口能力、生命周期所有者与允许的操作；长生命周期对象不持有短生命周期Activity”独立验收。
  </GlossaryItem>
  <GlossaryItem term="ContextImpl">
    ContextImpl服务于“沿Context、ContextWrapper与ContextImpl关系，比较Application、Activity和Service的Context创建、持有资源与生命周期”。掌握标准是能定位Android
    8.0源码、画出运行轨迹、构造单变量反例，并用“每个Context引用都能说明base实现、主题/窗口能力、生命周期所有者与允许的操作；长生命周期对象不持有短生命周期Activity”独立验收。
  </GlossaryItem>
  <GlossaryItem term="ContextWrapper">
    ContextWrapper服务于“沿Context、ContextWrapper与ContextImpl关系，比较Application、Activity和Service的Context创建、持有资源与生命周期”。掌握标准是能定位Android
    8.0源码、画出运行轨迹、构造单变量反例，并用“每个Context引用都能说明base实现、主题/窗口能力、生命周期所有者与允许的操作；长生命周期对象不持有短生命周期Activity”独立验收。
  </GlossaryItem>
  <GlossaryItem term="Application Context">
    Application
    Context服务于“沿Context、ContextWrapper与ContextImpl关系，比较Application、Activity和Service的Context创建、持有资源与生命周期”。掌握标准是能定位Android
    8.0源码、画出运行轨迹、构造单变量反例，并用“每个Context引用都能说明base实现、主题/窗口能力、生命周期所有者与允许的操作；长生命周期对象不持有短生命周期Activity”独立验收。
  </GlossaryItem>
  <GlossaryItem term="ContextThemeWrapper">
    ContextThemeWrapper服务于“沿Context、ContextWrapper与ContextImpl关系，比较Application、Activity和Service的Context创建、持有资源与生命周期”。掌握标准是能定位Android
    8.0源码、画出运行轨迹、构造单变量反例，并用“每个Context引用都能说明base实现、主题/窗口能力、生命周期所有者与允许的操作；长生命周期对象不持有短生命周期Activity”独立验收。
  </GlossaryItem>
</Glossary>

[← 上一页：第4章 四大组件的工作过程](/learn/android-advanced-decryption/04-chapter-04/aad8-04-four-components-workflow) · [下一页：第6章 理解ActivityManagerService →](/learn/android-advanced-decryption/06-chapter-06/aad8-06-activity-manager-service)

<Attribution
  adaptedFrom="刘望舒《Android进阶解密》2018年初版（Android 8.0源码）"
  adaptedUrl="https://www.phei.com.cn/module/goods/wssd_content.jsp?bookid=52919"
/>
