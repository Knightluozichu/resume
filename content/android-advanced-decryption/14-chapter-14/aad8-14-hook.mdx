---
title: "第14章 Hook技术"
description: "从代理模式和动态代理建立替换点，再分别Hook Activity与Context的startActivity调用链，验证代理注入与委托"
type: C
section: "Android进阶解密（Android 8.0源码 / 2018年初版）"
order: 14
draft: false
math: false
sourceUrl: "https://www.phei.com.cn/module/goods/wssd_content.jsp?bookid=52919"
demo: true
qualityVersion: 2
practiceMode: diagnosis
sourceMode: independent-rewrite
officialUnitId: aad8-14-hook
---

import { Objectives } from "@/components/mdx/objectives";
import { Term } from "@/components/mdx/term";
import { Step, Stepper } from "@/components/mdx/stepper";
import { Callout } from "@/components/mdx/callout";
import { Answer, Exercises } from "@/components/mdx/exercises";
import { Glossary, GlossaryItem } from "@/components/mdx/glossary";
import { Attribution } from "@/components/mdx/attribution";
import { Aad814HookDecisionLab, Aad814HookMechanismMap, Aad814HookFailureDiagram } from "@/components/mdx/android-advanced-decryption/aad8-14-hook";

# 第14章 Hook技术

在《第14章 Hook技术》中，本课程对应刘望舒《Android进阶解密》，电子工业出版社2018年10月初版，468页、702千字，ISBN 9787121348389。原书以Android 8.0系统源码为主；本页不会用Android 10后的ATMS、现代ART实现、Perfetto或新隐藏API政策替换原书链路。

从代理模式和动态代理建立替换点，再分别Hook Activity与Context的startActivity调用链，验证代理注入与委托。源码实验固定AOSP Android 8.0与作者出版仓库提交 3860d9e1；后续版本只作为迁移差异，不改变正式目录分母。

<Objectives>

- 能解释“第14章 Hook技术”全部正式目录节点，并定位Android 8.0中的入口类、函数与源码路径。
- 能绘制进程、线程、Binder/JNI/加载边界和关键状态转移，区分请求发出与系统完成。
- 能设计单变量故障实验，验证“Hook对象与原对象满足同一接口，未命中的调用完整委托，命中调用可记录/改写且能恢复原引用，不破坏并发与异常语义”。
- 能写出包含版本、源码、日志/追踪、反例、停止、恢复、回退和复核人的独立交接记录。

</Objectives>

<Aad814HookMechanismMap />

<Aad814HookDecisionLab />

<Aad814HookFailureDiagram />

## 从一条可证伪的调用链开始

先预测：按字段名反射替换单例却不校验Android 8.0版本，会因缓存位置或隐藏实现变化失效；吞掉未处理方法还会改变整个系统服务代理语义。把预测写成入口、对象身份、进程/线程、状态和完成回调五列，再去读源码。若结果与预测不同，先修正模型，不要选择性截取调用栈。

原书面向应用开发者学习系统源码，重点不是抄完整函数，而是抓住跨层调用的关键节点。每个节点都要回答：谁发起、在哪个进程和线程执行、哪份状态被修改、后续如何被唤醒、失败如何返回、什么现象代表真正完成。

验收不变量是：Hook对象与原对象满足同一接口，未命中的调用完整委托，命中调用可记录/改写且能恢复原引用，不破坏并发与异常语义。只有源码版本、符号关系、运行轨迹与最终状态四类证据一致，结论才可以进入下一章。

## 核心词汇与版本门

<Term def="Hook是本页第1个源码概念，必须同时说明Android 8.0定义、调用者、进程线程、状态、失败反例与证据">Hook</Term>、<Term def="代理模式是本页第2个源码概念，必须同时说明Android 8.0定义、调用者、进程线程、状态、失败反例与证据">代理模式</Term>、<Term def="动态代理是本页第3个源码概念，必须同时说明Android 8.0定义、调用者、进程线程、状态、失败反例与证据">动态代理</Term>、<Term def="IActivityManager是本页第4个源码概念，必须同时说明Android 8.0定义、调用者、进程线程、状态、失败反例与证据">IActivityManager</Term>、<Term def="Instrumentation是本页第5个源码概念，必须同时说明Android 8.0定义、调用者、进程线程、状态、失败反例与证据">Instrumentation</Term>

以上词汇都固定在Android 8.0语义。在《第14章 Hook技术》中，Android 7.0只在原书明确比较AMS家族时出现；Android 9以后反射/隐藏API限制、Android 10的ActivityTaskManager、现代Profiler/Perfetto等只能写进迁移备注，不能改写本页正式链路。

## 原书目录核对清单

本页承担10个目录或复习节点，正文、图解、实验和题目必须能反向定位每一项：

- 14.1 Hook技术概述
- 14.2 Hook技术分类
- 14.3 代理模式
- 14.3.1 代理模式简单实现
- 14.3.2 动态代理的简单实现
- 14.4 Hook startActivity方法
- 14.4.1 Hook Activity的startActivity方法
- 14.4.2 Hook Context的startActivity方法
- 14.4.3 Hook startActivity总结
- 14.5 本章小结

## 分步追踪：源码、运行与反例

<Stepper>
  <Step title="定位源码入口与责任边界">
    固定Android
    8.0标签，从公开入口向下追踪，不跳过Binder代理、JNI注册、ClassLoader查找或system_server状态对象。
  </Step>
  <Step title="注入一个失败变量">
    只改变进程未启动、Binder回调延迟、类先加载、token无效、帧负载或对象引用中的一个变量，先写预测再运行。
  </Step>
  <Step title="关闭独立证据门">
    对照正常与失败轨迹，核查源码符号、日志、状态、资源释放和最终业务结果，记录停止与回退条件。
    </Step>
</Stepper>

## 可复现实验

```java
Object proxy = Proxy.newProxyInstance(loader, new Class[]{IActivityManager.class}, handler);
```

```text
call -> proxy match? -> inspect/replace args -> original.invoke -> preserve result/exception
```

```text
install original reference | verify Android 8.0 field | invoke | restore | compare trace
```

动手试：在同一镜像、同一构建和同一输入上重复正常、冷启动/首次加载、热路径、单变量失败与恢复五轮。在《第14章 Hook技术》中，记录PID/TID、调用入口、对象或token、状态前后值、耗时、异常/返回码和最终可见结果；性能章再记录P50/P95/P99，内存章保留GC Root路径。

<Callout type="trap" title="跨版本拼接调用链">
  类名相似不代表实现相同。Android
  8.0没有后来的ATMS体系，运行时、工具和隐藏API规则也不同；跨版本代码只能独立标注差异。
</Callout>

<Callout type="trap" title="把请求发出当成系统完成">
  Binder
  transact、fork返回、消息入队、补丁下载或View加入本地数组都只是中间状态。必须观察目标进程回调、系统状态、首帧或资源释放。
</Callout>

## 独立证据门

- 节点覆盖Hook技术
- 最小切片贯通全链路
- 故障实验对比Hook对象与原对象满足同一接口，未命中的…
- 凭证据完成交接

## 练习

<Exercises>

**问题 1：为什么“第14章 Hook技术”必须固定Android 8.0？**

<Answer>
  系统服务拆分、类名、单例入口、ART、工具和隐藏API约束都会演进。固定版本才能让目录、源码路径、作者样例和运行轨迹一一对应。
</Answer>

**问题 2：怎样构造“按字段名反射替换单例却不校验Android 8.0版本，会因缓存位置或隐藏实现变化失效；吞掉未处理方法还会改变整个系统服务代理语义”的最小反例？**

<Answer>
  固定镜像、构建、数据和操作，只改变一个进程、线程、token、加载顺序、帧负载或引用变量；比较入口、状态、回调、返回码与最终系统事实。
</Answer>

**问题 3：何时可以认为本页完成独立交接？**

<Answer>
  另一位读者无需口头补充即可定位全部目录节点，按相同版本复现正常与失败轨迹，并依据“Hook对象与原对象满足同一接口，未命中的调用完整委托，命中调用可记录/改写且能恢复原引用，不破坏并发与异常语义”得到相同验收结论。
</Answer>

</Exercises>

## 本章回顾

“第14章 Hook技术”的核心是从代理模式和动态代理建立替换点，再分别Hook Activity与Context的startActivity调用链，验证代理注入与委托。按字段名反射替换单例却不校验Android 8.0版本，会因缓存位置或隐藏实现变化失效；吞掉未处理方法还会改变整个系统服务代理语义是必须保留的反证；源码阅读只有连接运行时的进程、线程、状态与最终结果，才从“知道类名”升级为“能诊断”。

<Glossary>
  <GlossaryItem term="Hook">
    Hook服务于“从代理模式和动态代理建立替换点，再分别Hook
    Activity与Context的startActivity调用链，验证代理注入与委托”。掌握标准是能定位Android
    8.0源码、画出运行轨迹、构造单变量反例，并用“Hook对象与原对象满足同一接口，未命中的调用完整委托，命中调用可记录/改写且能恢复原引用，不破坏并发与异常语义”独立验收。
  </GlossaryItem>
  <GlossaryItem term="代理模式">
    代理模式服务于“从代理模式和动态代理建立替换点，再分别Hook
    Activity与Context的startActivity调用链，验证代理注入与委托”。掌握标准是能定位Android
    8.0源码、画出运行轨迹、构造单变量反例，并用“Hook对象与原对象满足同一接口，未命中的调用完整委托，命中调用可记录/改写且能恢复原引用，不破坏并发与异常语义”独立验收。
  </GlossaryItem>
  <GlossaryItem term="动态代理">
    动态代理服务于“从代理模式和动态代理建立替换点，再分别Hook
    Activity与Context的startActivity调用链，验证代理注入与委托”。掌握标准是能定位Android
    8.0源码、画出运行轨迹、构造单变量反例，并用“Hook对象与原对象满足同一接口，未命中的调用完整委托，命中调用可记录/改写且能恢复原引用，不破坏并发与异常语义”独立验收。
  </GlossaryItem>
  <GlossaryItem term="IActivityManager">
    IActivityManager服务于“从代理模式和动态代理建立替换点，再分别Hook
    Activity与Context的startActivity调用链，验证代理注入与委托”。掌握标准是能定位Android
    8.0源码、画出运行轨迹、构造单变量反例，并用“Hook对象与原对象满足同一接口，未命中的调用完整委托，命中调用可记录/改写且能恢复原引用，不破坏并发与异常语义”独立验收。
  </GlossaryItem>
  <GlossaryItem term="Instrumentation">
    Instrumentation服务于“从代理模式和动态代理建立替换点，再分别Hook
    Activity与Context的startActivity调用链，验证代理注入与委托”。掌握标准是能定位Android
    8.0源码、画出运行轨迹、构造单变量反例，并用“Hook对象与原对象满足同一接口，未命中的调用完整委托，命中调用可记录/改写且能恢复原引用，不破坏并发与异常语义”独立验收。
  </GlossaryItem>
</Glossary>

[← 上一页：第13章 热修复原理](/learn/android-advanced-decryption/13-chapter-13/aad8-13-hotfix) · [下一页：第15章 插件化原理 →](/learn/android-advanced-decryption/15-chapter-15/aad8-15-pluginization)

## 原版目录概念补充核对

以下条目补齐官方目录中容易被示例主线掩盖的概念。它们不重复罗列目录，而是明确每项概念的机制、适用边界和验收证据。

### 14.2 Hook技术分类：机制、边界与证据

第14章 Hook技术中的14.2 Hook技术分类涉及 Java、Native、加载器身份与运行时内存边界；同名类型、引用或方法在不同加载器和线程中并不自动等价。用源码符号、类加载日志、JNI 引用计数、堆/GC 轨迹或正反加载案例验证对象身份、生命周期和释放条件。

### 14.3 代理模式：机制、边界与证据

第14章 Hook技术中的14.3 代理模式通过改变加载、查找或分发路径实现，必须区分 Android 8.0 的可行机制、隐藏实现依赖与现代平台限制。记录被替换的入口、原对象和代理对象身份、加载顺序与回滚点，再用补丁缺失、签名不符或进程重建样本确认失败不会静默污染状态。

### 14.3.1 代理模式简单实现：机制、边界与证据

第14章 Hook技术中的14.3.1 代理模式简单实现通过改变加载、查找或分发路径实现，必须区分 Android 8.0 的可行机制、隐藏实现依赖与现代平台限制。记录被替换的入口、原对象和代理对象身份、加载顺序与回滚点，再用补丁缺失、签名不符或进程重建样本确认失败不会静默污染状态。

### 14.3.2 动态代理的简单实现：机制、边界与证据

第14章 Hook技术中的14.3.2 动态代理的简单实现通过改变加载、查找或分发路径实现，必须区分 Android 8.0 的可行机制、隐藏实现依赖与现代平台限制。记录被替换的入口、原对象和代理对象身份、加载顺序与回滚点，再用补丁缺失、签名不符或进程重建样本确认失败不会静默污染状态。

### 14.4 Hook startActivity方法：机制、边界与证据

第14章 Hook技术中的14.4 Hook startActivity方法由 framework 对象、system_server 状态和应用进程回调共同完成，API 返回不等于系统状态已经落定。应跟踪 token/record、目标进程与线程、生命周期回调和最终窗口或组件状态，并用无效 token、进程重建或迟到回调验证拒绝与恢复路径。

### 14.4.1 Hook Activity的startActivity方法：机制、边界与证据

第14章 Hook技术中的14.4.1 Hook Activity的startActivity方法由 framework 对象、system_server 状态和应用进程回调共同完成，API 返回不等于系统状态已经落定。应跟踪 token/record、目标进程与线程、生命周期回调和最终窗口或组件状态，并用无效 token、进程重建或迟到回调验证拒绝与恢复路径。

### 14.4.2 Hook Context的startActivity方法：机制、边界与证据

第14章 Hook技术中的14.4.2 Hook Context的startActivity方法由 framework 对象、system_server 状态和应用进程回调共同完成，API 返回不等于系统状态已经落定。应跟踪 token/record、目标进程与线程、生命周期回调和最终窗口或组件状态，并用无效 token、进程重建或迟到回调验证拒绝与恢复路径。

### 14.4.3 Hook startActivity总结：机制、边界与证据

第14章 Hook技术中的14.4.3 Hook startActivity总结由 framework 对象、system_server 状态和应用进程回调共同完成，API 返回不等于系统状态已经落定。应跟踪 token/record、目标进程与线程、生命周期回调和最终窗口或组件状态，并用无效 token、进程重建或迟到回调验证拒绝与恢复路径。

### 14.5 本章小结：机制、边界与证据

第14章 Hook技术中的14.5 本章小结必须落回 Android 8.0 源码中的入口、对象、进程/线程、状态变化与完成点。固定版本和输入，先预测正常链路，再只注入一个失败条件，以源码符号、运行日志和最终系统状态三方核对。

<Attribution
  adaptedFrom="刘望舒《Android进阶解密》2018年初版（Android 8.0源码）"
  adaptedUrl="https://www.phei.com.cn/module/goods/wssd_content.jsp?bookid=52919"
  mode="independent-rewrite"
  sourceBasis="outline-only"
/>
