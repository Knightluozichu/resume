---
title: "第3章 View体系与自定义View"
description: "依据2017年首版完整目录覆盖29个节点：贯通坐标、六种滑动方式、属性动画、Scroller、事件分发、measure/layout/draw流程和四类自定义View"
type: C
section: "Android进阶之光（第1版 / Android 7.0时代）"
draft: true
math: false
sourceUrl: "https://www.bookschina.com/7533720.htm"
demo: true
order: 0
---

# 第3章 View体系与自定义View

本页依据刘望舒《Android进阶之光》独立重构，不复制原文。版本锁定电子工业出版社2017年7月首版、492页、ISBN 9787121315305；正式结构为11章、190个章/节/小节节点。

原书处在Android 5.0至7.0、Java与Support Library时代，包含AsyncTask、Volley、otto、RxJava早期版本、ButterKnife、Dagger 2和Data Binding。课程先复现历史机制，再以AndroidX、协程/Flow、现代架构组件作迁移对照。

<Objectives>

- 能解释“第3章 View体系与自定义View”全部29个正式节点的输入、线程、生命周期与可观察结果。
- 能实现“贯通坐标、六种滑动方式、属性动画、Scroller、事件分发、measure/layout/draw流程和四类自定义View”的最小可运行切片并保存版本、源码、样本和日志。
- 能区分正确结果、框架便利、隐藏成本和版本差异，并用MotionEvent序列、坐标换算、MeasureSpec、测量尺寸、布局坐标、绘制边界与父子拦截日志验收。
- 能设计反例推翻“只让自定义View在固定尺寸下看起来正确，不处理测量模式、padding、事件冲突和状态恢复”，写出2017年机制到现代Android的迁移与回退条件。

</Objectives>

## 从可观察行为开始

<Term def="由尺寸值和测量模式编码而成、父View传给子View的测量要求">MeasureSpec</Term>、<Term def="MotionEvent在Activity、ViewGroup与View之间分发、拦截和消费的协议">事件分发</Term>、<Term def="根据时间计算滚动偏移但不直接移动View的数值辅助器">Scroller</Term>、<Term def="随时间修改对象属性并通过更新回调驱动界面变化的动画体系">属性动画</Term>、<Term def="负责测量子项并按自定义规则放置它们的复合容器">自定义ViewGroup</Term>构成本页的分析词汇。先预测一次输入会穿过哪些对象、线程、队列、View或语言边界，再运行正常与失败样本；没有版本和原始证据的“源码原理”不能复查。

本单元主线是贯通坐标、六种滑动方式、属性动画、Scroller、事件分发、measure/layout/draw流程和四类自定义View。交互管线逐项选择目录节点，反例实验切换正常、配置变化、线程竞争、所有者销毁和版本漂移，证据门要求目录、行为、线程、生命周期与迁移全部通过。

<Aal17PipelineLab />

## 权威目录逐节点映射

### 第3章 View体系与自定义View

**章节合同 1/29。** 把本节点放进“贯通坐标、六种滑动方式、属性动画、Scroller、事件分发、measure/layout/draw流程和四类自定义View”主线：先确定输入与所有者，再追踪线程、View、网络、对象或源码调用，最后以MotionEvent序列、坐标换算、MeasureSpec、测量尺寸、布局坐标、绘制边界与父子拦截日志验收。只记API和类名不能证明掌握。

### 3.1 View与ViewGroup

**主题边界 2/29。** 把本节点放进“贯通坐标、六种滑动方式、属性动画、Scroller、事件分发、measure/layout/draw流程和四类自定义View”主线：先确定输入与所有者，再追踪线程、View、网络、对象或源码调用，最后以MotionEvent序列、坐标换算、MeasureSpec、测量尺寸、布局坐标、绘制边界与父子拦截日志验收。只记API和类名不能证明掌握。

### 3.2 坐标系

**主题边界 3/29。** 把本节点放进“贯通坐标、六种滑动方式、属性动画、Scroller、事件分发、measure/layout/draw流程和四类自定义View”主线：先确定输入与所有者，再追踪线程、View、网络、对象或源码调用，最后以MotionEvent序列、坐标换算、MeasureSpec、测量尺寸、布局坐标、绘制边界与父子拦截日志验收。只记API和类名不能证明掌握。

### 3.2.1 Android坐标系

**实现检查点 4/29。** 把本节点放进“贯通坐标、六种滑动方式、属性动画、Scroller、事件分发、measure/layout/draw流程和四类自定义View”主线：先确定输入与所有者，再追踪线程、View、网络、对象或源码调用，最后以MotionEvent序列、坐标换算、MeasureSpec、测量尺寸、布局坐标、绘制边界与父子拦截日志验收。只记API和类名不能证明掌握。

### 3.2.2 View坐标系

**实现检查点 5/29。** 把本节点放进“贯通坐标、六种滑动方式、属性动画、Scroller、事件分发、measure/layout/draw流程和四类自定义View”主线：先确定输入与所有者，再追踪线程、View、网络、对象或源码调用，最后以MotionEvent序列、坐标换算、MeasureSpec、测量尺寸、布局坐标、绘制边界与父子拦截日志验收。只记API和类名不能证明掌握。

### 3.3 View的滑动

**主题边界 6/29。** 把本节点放进“贯通坐标、六种滑动方式、属性动画、Scroller、事件分发、measure/layout/draw流程和四类自定义View”主线：先确定输入与所有者，再追踪线程、View、网络、对象或源码调用，最后以MotionEvent序列、坐标换算、MeasureSpec、测量尺寸、布局坐标、绘制边界与父子拦截日志验收。只记API和类名不能证明掌握。

### 3.3.1 layout()方法

**实现检查点 7/29。** 把本节点放进“贯通坐标、六种滑动方式、属性动画、Scroller、事件分发、measure/layout/draw流程和四类自定义View”主线：先确定输入与所有者，再追踪线程、View、网络、对象或源码调用，最后以MotionEvent序列、坐标换算、MeasureSpec、测量尺寸、布局坐标、绘制边界与父子拦截日志验收。只记API和类名不能证明掌握。

### 3.3.2 offsetLeftAndRight()与offsetTopAndBottom()

**实现检查点 8/29。** 把本节点放进“贯通坐标、六种滑动方式、属性动画、Scroller、事件分发、measure/layout/draw流程和四类自定义View”主线：先确定输入与所有者，再追踪线程、View、网络、对象或源码调用，最后以MotionEvent序列、坐标换算、MeasureSpec、测量尺寸、布局坐标、绘制边界与父子拦截日志验收。只记API和类名不能证明掌握。

### 3.3.3 LayoutParams（改变布局参数）

**实现检查点 9/29。** 把本节点放进“贯通坐标、六种滑动方式、属性动画、Scroller、事件分发、measure/layout/draw流程和四类自定义View”主线：先确定输入与所有者，再追踪线程、View、网络、对象或源码调用，最后以MotionEvent序列、坐标换算、MeasureSpec、测量尺寸、布局坐标、绘制边界与父子拦截日志验收。只记API和类名不能证明掌握。

### 3.3.4 动画

**实现检查点 10/29。** 把本节点放进“贯通坐标、六种滑动方式、属性动画、Scroller、事件分发、measure/layout/draw流程和四类自定义View”主线：先确定输入与所有者，再追踪线程、View、网络、对象或源码调用，最后以MotionEvent序列、坐标换算、MeasureSpec、测量尺寸、布局坐标、绘制边界与父子拦截日志验收。只记API和类名不能证明掌握。

### 3.3.5 scrollTo与scrollBy

**实现检查点 11/29。** 把本节点放进“贯通坐标、六种滑动方式、属性动画、Scroller、事件分发、measure/layout/draw流程和四类自定义View”主线：先确定输入与所有者，再追踪线程、View、网络、对象或源码调用，最后以MotionEvent序列、坐标换算、MeasureSpec、测量尺寸、布局坐标、绘制边界与父子拦截日志验收。只记API和类名不能证明掌握。

### 3.3.6 Scroller

**实现检查点 12/29。** 把本节点放进“贯通坐标、六种滑动方式、属性动画、Scroller、事件分发、measure/layout/draw流程和四类自定义View”主线：先确定输入与所有者，再追踪线程、View、网络、对象或源码调用，最后以MotionEvent序列、坐标换算、MeasureSpec、测量尺寸、布局坐标、绘制边界与父子拦截日志验收。只记API和类名不能证明掌握。

### 3.4 属性动画

**主题边界 13/29。** 把本节点放进“贯通坐标、六种滑动方式、属性动画、Scroller、事件分发、measure/layout/draw流程和四类自定义View”主线：先确定输入与所有者，再追踪线程、View、网络、对象或源码调用，最后以MotionEvent序列、坐标换算、MeasureSpec、测量尺寸、布局坐标、绘制边界与父子拦截日志验收。只记API和类名不能证明掌握。

### 3.5 解析Scroller

**主题边界 14/29。** 把本节点放进“贯通坐标、六种滑动方式、属性动画、Scroller、事件分发、measure/layout/draw流程和四类自定义View”主线：先确定输入与所有者，再追踪线程、View、网络、对象或源码调用，最后以MotionEvent序列、坐标换算、MeasureSpec、测量尺寸、布局坐标、绘制边界与父子拦截日志验收。只记API和类名不能证明掌握。

### 3.6 View的事件分发机制

**主题边界 15/29。** 把本节点放进“贯通坐标、六种滑动方式、属性动画、Scroller、事件分发、measure/layout/draw流程和四类自定义View”主线：先确定输入与所有者，再追踪线程、View、网络、对象或源码调用，最后以MotionEvent序列、坐标换算、MeasureSpec、测量尺寸、布局坐标、绘制边界与父子拦截日志验收。只记API和类名不能证明掌握。

### 3.6.1 源码解析Activity的构成

**实现检查点 16/29。** 把本节点放进“贯通坐标、六种滑动方式、属性动画、Scroller、事件分发、measure/layout/draw流程和四类自定义View”主线：先确定输入与所有者，再追踪线程、View、网络、对象或源码调用，最后以MotionEvent序列、坐标换算、MeasureSpec、测量尺寸、布局坐标、绘制边界与父子拦截日志验收。只记API和类名不能证明掌握。

### 3.6.2 源码解析View的事件分发机制

**实现检查点 17/29。** 把本节点放进“贯通坐标、六种滑动方式、属性动画、Scroller、事件分发、measure/layout/draw流程和四类自定义View”主线：先确定输入与所有者，再追踪线程、View、网络、对象或源码调用，最后以MotionEvent序列、坐标换算、MeasureSpec、测量尺寸、布局坐标、绘制边界与父子拦截日志验收。只记API和类名不能证明掌握。

### 3.7 View的工作流程

**主题边界 18/29。** 把本节点放进“贯通坐标、六种滑动方式、属性动画、Scroller、事件分发、measure/layout/draw流程和四类自定义View”主线：先确定输入与所有者，再追踪线程、View、网络、对象或源码调用，最后以MotionEvent序列、坐标换算、MeasureSpec、测量尺寸、布局坐标、绘制边界与父子拦截日志验收。只记API和类名不能证明掌握。

### 3.7.1 View的工作流程入口

**实现检查点 19/29。** 把本节点放进“贯通坐标、六种滑动方式、属性动画、Scroller、事件分发、measure/layout/draw流程和四类自定义View”主线：先确定输入与所有者，再追踪线程、View、网络、对象或源码调用，最后以MotionEvent序列、坐标换算、MeasureSpec、测量尺寸、布局坐标、绘制边界与父子拦截日志验收。只记API和类名不能证明掌握。

### 3.7.2 理解MeasureSpec

**实现检查点 20/29。** 把本节点放进“贯通坐标、六种滑动方式、属性动画、Scroller、事件分发、measure/layout/draw流程和四类自定义View”主线：先确定输入与所有者，再追踪线程、View、网络、对象或源码调用，最后以MotionEvent序列、坐标换算、MeasureSpec、测量尺寸、布局坐标、绘制边界与父子拦截日志验收。只记API和类名不能证明掌握。

### 3.7.3 View的measure流程

**实现检查点 21/29。** 把本节点放进“贯通坐标、六种滑动方式、属性动画、Scroller、事件分发、measure/layout/draw流程和四类自定义View”主线：先确定输入与所有者，再追踪线程、View、网络、对象或源码调用，最后以MotionEvent序列、坐标换算、MeasureSpec、测量尺寸、布局坐标、绘制边界与父子拦截日志验收。只记API和类名不能证明掌握。

### 3.7.4 View的layout流程

**实现检查点 22/29。** 把本节点放进“贯通坐标、六种滑动方式、属性动画、Scroller、事件分发、measure/layout/draw流程和四类自定义View”主线：先确定输入与所有者，再追踪线程、View、网络、对象或源码调用，最后以MotionEvent序列、坐标换算、MeasureSpec、测量尺寸、布局坐标、绘制边界与父子拦截日志验收。只记API和类名不能证明掌握。

### 3.7.5 View的draw流程

**实现检查点 23/29。** 把本节点放进“贯通坐标、六种滑动方式、属性动画、Scroller、事件分发、measure/layout/draw流程和四类自定义View”主线：先确定输入与所有者，再追踪线程、View、网络、对象或源码调用，最后以MotionEvent序列、坐标换算、MeasureSpec、测量尺寸、布局坐标、绘制边界与父子拦截日志验收。只记API和类名不能证明掌握。

### 3.8 自定义View

**主题边界 24/29。** 把本节点放进“贯通坐标、六种滑动方式、属性动画、Scroller、事件分发、measure/layout/draw流程和四类自定义View”主线：先确定输入与所有者，再追踪线程、View、网络、对象或源码调用，最后以MotionEvent序列、坐标换算、MeasureSpec、测量尺寸、布局坐标、绘制边界与父子拦截日志验收。只记API和类名不能证明掌握。

### 3.8.1 继承系统控件的自定义View

**实现检查点 25/29。** 把本节点放进“贯通坐标、六种滑动方式、属性动画、Scroller、事件分发、measure/layout/draw流程和四类自定义View”主线：先确定输入与所有者，再追踪线程、View、网络、对象或源码调用，最后以MotionEvent序列、坐标换算、MeasureSpec、测量尺寸、布局坐标、绘制边界与父子拦截日志验收。只记API和类名不能证明掌握。

### 3.8.2 继承View的自定义View

**实现检查点 26/29。** 把本节点放进“贯通坐标、六种滑动方式、属性动画、Scroller、事件分发、measure/layout/draw流程和四类自定义View”主线：先确定输入与所有者，再追踪线程、View、网络、对象或源码调用，最后以MotionEvent序列、坐标换算、MeasureSpec、测量尺寸、布局坐标、绘制边界与父子拦截日志验收。只记API和类名不能证明掌握。

### 3.8.3 自定义组合控件

**实现检查点 27/29。** 把本节点放进“贯通坐标、六种滑动方式、属性动画、Scroller、事件分发、measure/layout/draw流程和四类自定义View”主线：先确定输入与所有者，再追踪线程、View、网络、对象或源码调用，最后以MotionEvent序列、坐标换算、MeasureSpec、测量尺寸、布局坐标、绘制边界与父子拦截日志验收。只记API和类名不能证明掌握。

### 3.8.4 自定义ViewGroup

**实现检查点 28/29。** 把本节点放进“贯通坐标、六种滑动方式、属性动画、Scroller、事件分发、measure/layout/draw流程和四类自定义View”主线：先确定输入与所有者，再追踪线程、View、网络、对象或源码调用，最后以MotionEvent序列、坐标换算、MeasureSpec、测量尺寸、布局坐标、绘制边界与父子拦截日志验收。只记API和类名不能证明掌握。

### 3.9 本章小结

**主题边界 29/29。** 把本节点放进“贯通坐标、六种滑动方式、属性动画、Scroller、事件分发、measure/layout/draw流程和四类自定义View”主线：先确定输入与所有者，再追踪线程、View、网络、对象或源码调用，最后以MotionEvent序列、坐标换算、MeasureSpec、测量尺寸、布局坐标、绘制边界与父子拦截日志验收。只记API和类名不能证明掌握。

## 最小实现与边界

```kotlin
override fun onMeasure(widthSpec: Int, heightSpec: Int) {
    val desiredWidth = contentWidth + paddingLeft + paddingRight
    val desiredHeight = contentHeight + paddingTop + paddingBottom
    setMeasuredDimension(
        resolveSize(desiredWidth, widthSpec),
        resolveSize(desiredHeight, heightSpec)
    )
}
```

实现后用固定输入记录输出、线程名、生命周期、异常和资源释放。再构造错误输入、取消、旋转、后台切前台及低版本设备，验证便利框架没有隐藏所有权问题。

```kotlin
data class AndroidProbe(
    val apiLevel: Int,
    val ownerActive: Boolean,
    val callbackThread: String,
    val resultStable: Boolean
)
check(probe.ownerActive && probe.resultStable)
```

<Callout type="trap">**核心陷阱。** 只让自定义View在固定尺寸下看起来正确，不处理测量模式、padding、事件冲突和状态恢复。修复先恢复明确输入、所有者、线程和失败合同。</Callout>

<Callout type="trap">**历史版本错位。** 原书API按2017年语境讲解。现代AndroidX、协程、Flow、Hilt或Compose只进入迁移账本，不能删除原书正式目录。</Callout>

<Callout type="warning">抓包、日志、堆转储和媒体样本只使用合成数据；不得保存真实账号、令牌、联系人、消息或受版权保护媒体。</Callout>

## 证据解释

第一份证据是行为基线。固定Android版本、依赖版本、构建类型、输入数据和页面生命周期，完整记录“贯通坐标、六种滑动方式、属性动画、Scroller、事件分发、measure/layout/draw流程和四类自定义View”从入口到结果的顺序。除了成功输出，还要保存回调线程、对象身份、队列或订阅状态以及释放日志；否则无法判断升级后的差异来自业务代码、框架实现还是测试环境。

第二份证据是反例。针对“只让自定义View在固定尺寸下看起来正确，不处理测量模式、padding、事件冲突和状态恢复”构造至少一个可重复失败：在配置变化、快速重复输入、网络超时、后台切换或所有者销毁时观察错位、重复、泄漏和迟到回调。反例必须写明预期拒绝条件；只展示正常路径会掩盖框架最重要的所有权边界。

第三份证据是源码定位。先锁定仓库标签和库版本，再从公开入口沿实际调用追踪，不跨版本拼接类名。每次跨线程、跨进程或跨Java/Native边界，都记录调用方、被调用方、数据形态、错误返回和资源归属。MotionEvent序列、坐标换算、MeasureSpec、测量尺寸、布局坐标、绘制边界与父子拦截日志应能让另一位读者在相同版本上重建同一条链。

第四份证据是迁移对照。现代API解决的问题可能相同，但取消、缓存、状态恢复、线程和错误语义未必一致。迁移时保持输入和验收不变，一次只替换一个组件；新旧结果不一致时先解释差异，再决定适配或回退，不能用“新版推荐”跳过历史行为验证。

## 迁移账本

```text
原书节点: 第3章 View体系与自定义View
历史基线: Android 5.0—7.0 / Java / Support Library
现代候选: 一次只替换一个API或框架
不变量: 业务结果、线程、生命周期、错误与资源释放
证据: MotionEvent序列、坐标换算、MeasureSpec、测量尺寸、布局坐标、绘制边界与父子拦截日志
回退: 任一不变量失败即恢复历史实现
```

## 本章回顾

从一个真实输入复盘：它由谁接收、在哪个线程执行、状态和对象归谁所有、何时取消或释放、低版本与异常路径如何表现。然后用MotionEvent序列、坐标换算、MeasureSpec、测量尺寸、布局坐标、绘制边界与父子拦截日志重放；无法复现的结论退回实验阶段。

## 复习与独立交接

<Exercises>

**问题 1**：为什么本页必须保留29个目录节点？
<Answer>这些节点共同构成“贯通坐标、六种滑动方式、属性动画、Scroller、事件分发、measure/layout/draw流程和四类自定义View”的输入、机制、源码与验收链；删去正式节点会让调用或所有权证据断裂。</Answer>

**问题 2**：最小正确性合同是什么？
<Answer>相同版本、输入与生命周期下，业务结果、线程交付、错误和资源释放必须确定且可重放。</Answer>

**问题 3**：怎样构造最有效的反例？
<Answer>主动让“只让自定义View在固定尺寸下看起来正确，不处理测量模式、padding、事件冲突和状态恢复”成立，再运行取消、配置变化、所有者销毁或版本漂移场景；错位、泄漏或重复即失败。</Answer>

**问题 4**：为什么只读源码类名不够？
<Answer>类名不能证明运行版本、调用入口、线程、进程、对象所有权与错误路径；必须有可执行样本和原始日志。</Answer>

**问题 5**：怎样迁移而不改写原书？
<Answer>先保存2017年行为基线，再一次替换一个API或库，用同一输入、线程、生命周期与失败测试对照并记录回退。</Answer>

**问题 6**：独立交接需要什么？
<Answer>需要源码与版本锁、完整目录映射、MotionEvent序列、坐标换算、MeasureSpec、测量尺寸、布局坐标、绘制边界与父子拦截日志、失败测试、迁移账本和回退条件。</Answer>

</Exercises>

<Glossary>
  <GlossaryItem term="MeasureSpec">由尺寸值和测量模式编码而成、父View传给子View的测量要求。</GlossaryItem>
  <GlossaryItem term="事件分发">MotionEvent在Activity、ViewGroup与View之间分发、拦截和消费的协议。</GlossaryItem>
  <GlossaryItem term="Scroller">根据时间计算滚动偏移但不直接移动View的数值辅助器。</GlossaryItem>
  <GlossaryItem term="属性动画">随时间修改对象属性并通过更新回调驱动界面变化的动画体系。</GlossaryItem>
  <GlossaryItem term="自定义ViewGroup">负责测量子项并按自定义规则放置它们的复合容器。</GlossaryItem>
</Glossary>

[← 上一页：第2章 Material Design](/learn/android-advanced-light/02-material-design/aal-17-02-material-design) · [下一页：第4章 多线程编程 →](/learn/android-advanced-light/04-multithreading/aal-17-04-multithreading)

<Attribution adaptedFrom="刘望舒《Android进阶之光》" adaptedUrl="https://www.bookschina.com/7533720.htm" />
