---
title: "第5章 网络编程与网络框架"
description: "依据2017年首版完整目录覆盖22个节点：从分层、TCP握手挥手和HTTP报文，走到HttpClient、HttpURLConnection、Volley、OkHttp与Retrofit的用法和源码链"
type: C
section: "Android进阶之光（第1版 / Android 7.0时代）"
draft: false
math: false
sourceUrl: "https://www.bookschina.com/7533720.htm"
demo: true
order: 0
qualityVersion: 2
practiceMode: diagnosis
sourceMode: independent-rewrite
officialUnitId: aal-17-05-network-frameworks
---

import { Objectives } from "@/components/mdx/objectives";
import { Term } from "@/components/mdx/term";
import { Callout } from "@/components/mdx/callout";
import { Answer, Exercises } from "@/components/mdx/exercises";
import { Glossary, GlossaryItem } from "@/components/mdx/glossary";
import { Attribution } from "@/components/mdx/attribution";
import { Aal1705NetworkFrameworksDecisionLab, Aal1705NetworkFrameworksMechanismMap, Aal1705NetworkFrameworksFailureDiagram } from "@/components/mdx/android-advanced-light/aal-17-05-network-frameworks";

# 第5章 网络编程与网络框架

在《第5章 网络编程与网络框架》中，本页依据刘望舒《Android进阶之光》独立重构，不复制原文。版本锁定电子工业出版社2017年7月首版、492页、ISBN 9787121315305；正式结构为11章、190个章/节/小节节点。

在《第5章 网络编程与网络框架》中，原书处在Android 5.0至7.0、Java与Support Library时代，包含AsyncTask、Volley、otto、RxJava早期版本、ButterKnife、Dagger 2和Data Binding。课程先复现历史机制，再以AndroidX、协程/Flow、现代架构组件作迁移对照。

<Objectives>

- 能解释“第5章 网络编程与网络框架”全部22个正式节点的输入、线程、生命周期与可观察结果。
- 能实现“从分层、TCP握手挥手和HTTP报文，走到HttpClient、HttpURLConnection、Volley、OkHttp与Retrofit的用法和源码链”的最小可运行切片并保存版本、源码、样本和日志。
- 能区分正确结果、框架便利、隐藏成本和版本差异，并用抓包、请求响应报文、超时与取消、重试边界、连接复用、缓存、转换器和调用链日志验收。
- 能设计反例推翻“只比较网络库API是否简洁，不定义超时、取消、重试幂等、证书、错误体和生命周期所有权”，写出2017年机制到现代Android的迁移与回退条件。

</Objectives>

<Aal1705NetworkFrameworksMechanismMap />

<Aal1705NetworkFrameworksDecisionLab />

<Aal1705NetworkFrameworksFailureDiagram />

## 从可观察行为开始

<Term def="让多个请求共享符合条件的持久连接以减少握手成本的机制">连接复用</Term>、<Term def="同一请求重复执行仍保持预期资源状态不被重复放大的性质">请求幂等</Term>、<Term def="按顺序查看或修改请求响应并决定继续、短路或重试的责任链">拦截器链</Term>、<Term def="在HTTP消息体与应用对象之间进行编码解码的组件">转换器</Term>、<Term def="停止排队或执行中的网络工作并阻止结果交付给失效所有者的能力">调用取消</Term>构成本页的分析词汇。先预测一次输入会穿过哪些对象、线程、队列、View或语言边界，再运行正常与失败样本；没有版本和原始证据的“源码原理”不能复查。

本单元主线是从分层、TCP握手挥手和HTTP报文，走到HttpClient、HttpURLConnection、Volley、OkHttp与Retrofit的用法和源码链。交互管线逐项选择目录节点，反例实验切换正常、配置变化、线程竞争、所有者销毁和版本漂移，证据门要求目录、行为、线程、生命周期与迁移全部通过。

## 最小实现与边界

```kotlin
interface Api {
    @GET("users/{id}")
    fun user(@Path("id") id: String): Call<UserDto>
}

val client = OkHttpClient.Builder()
    .connectTimeout(10, TimeUnit.SECONDS)
    .build()
```

在《第5章 网络编程与网络框架》中，实现后用固定输入记录输出、线程名、生命周期、异常和资源释放。再构造错误输入、取消、旋转、后台切前台及低版本设备，验证便利框架没有隐藏所有权问题。

```kotlin
data class AndroidProbe(
    val apiLevel: Int,
    val ownerActive: Boolean,
    val callbackThread: String,
    val resultStable: Boolean
)
check(probe.ownerActive && probe.resultStable)
```

<Callout type="trap">

  **核心陷阱。** 只比较网络库API是否简洁，不定义超时、取消、重试幂等、证书、错误体和生命周期所有权。修复先恢复明确输入、所有者、线程和失败合同。

</Callout>
<Callout type="trap">

  **历史版本错位。** 在《第5章 网络编程与网络框架》中，原书API按2017年语境讲解。现代AndroidX、协程、Flow、Hilt或Compose只进入迁移账本，不能删除原书正式目录。

</Callout>
<Callout type="warning">

  抓包、日志、堆转储和媒体样本只使用合成数据；不得保存真实账号、令牌、联系人、消息或受版权保护媒体。

</Callout>
## 证据解释

第一份证据是行为基线。固定Android版本、依赖版本、构建类型、输入数据和页面生命周期，完整记录“从分层、TCP握手挥手和HTTP报文，走到HttpClient、HttpURLConnection、Volley、OkHttp与Retrofit的用法和源码链”从入口到结果的顺序。除了成功输出，还要保存回调线程、对象身份、队列或订阅状态以及释放日志；否则无法判断升级后的差异来自业务代码、框架实现还是测试环境。

第二份证据是反例。针对“只比较网络库API是否简洁，不定义超时、取消、重试幂等、证书、错误体和生命周期所有权”构造至少一个可重复失败：在配置变化、快速重复输入、网络超时、后台切换或所有者销毁时观察错位、重复、泄漏和迟到回调。反例必须写明预期拒绝条件；只展示正常路径会掩盖框架最重要的所有权边界。

第三份证据是源码定位。先锁定仓库标签和库版本，再从公开入口沿实际调用追踪，不跨版本拼接类名。每次跨线程、跨进程或跨Java/Native边界，都记录调用方、被调用方、数据形态、错误返回和资源归属。抓包、请求响应报文、超时与取消、重试边界、连接复用、缓存、转换器和调用链日志应能让另一位读者在相同版本上重建同一条链。

在《第5章 网络编程与网络框架》中，第四份证据是迁移对照。现代API解决的问题可能相同，但取消、缓存、状态恢复、线程和错误语义未必一致。迁移时保持输入和验收不变，一次只替换一个组件；新旧结果不一致时先解释差异，再决定适配或回退，不能用“新版推荐”跳过历史行为验证。

## 迁移账本

```text
原书节点: 第5章 网络编程与网络框架
历史基线: Android 5.0—7.0 / Java / Support Library
现代候选: 一次只替换一个API或框架
不变量: 业务结果、线程、生命周期、错误与资源释放
证据: 抓包、请求响应报文、超时与取消、重试边界、连接复用、缓存、转换器和调用链日志
回退: 任一不变量失败即恢复历史实现
```

## 本章回顾

从一个真实输入复盘：它由谁接收、在哪个线程执行、状态和对象归谁所有、何时取消或释放、低版本与异常路径如何表现。然后用抓包、请求响应报文、超时与取消、重试边界、连接复用、缓存、转换器和调用链日志重放；无法复现的结论退回实验阶段。

## 复习与独立交接

<Exercises>

**问题 1**：为什么本页必须保留22个目录节点？
<Answer>
  这些节点共同构成“从分层、TCP握手挥手和HTTP报文，走到HttpClient、HttpURLConnection、Volley、OkHttp与Retrofit的用法和源码链”的输入、机制、源码与验收链；删去正式节点会让调用或所有权证据断裂。
</Answer>
**问题 2**：最小正确性合同是什么？
<Answer>
  相同版本、输入与生命周期下，业务结果、线程交付、错误和资源释放必须确定且可重放。
</Answer>
**问题 3**：怎样构造最有效的反例？
<Answer>
  主动让“只比较网络库API是否简洁，不定义超时、取消、重试幂等、证书、错误体和生命周期所有权”成立，再运行取消、配置变化、所有者销毁或版本漂移场景；错位、泄漏或重复即失败。
</Answer>
**问题 4**：为什么只读源码类名不够？
<Answer>
  类名不能证明运行版本、调用入口、线程、进程、对象所有权与错误路径；必须有可执行样本和原始日志。
</Answer>
**问题 5**：怎样迁移而不改写原书？
<Answer>
  先保存2017年行为基线，再一次替换一个API或库，用同一输入、线程、生命周期与失败测试对照并记录回退。
</Answer>
**问题 6**：独立交接需要什么？
<Answer>
  需要源码与版本锁、完整目录映射、抓包、请求响应报文、超时与取消、重试边界、连接复用、缓存、转换器和调用链日志、失败测试、迁移账本和回退条件。
</Answer>
</Exercises>

<Glossary>
  <GlossaryItem term="连接复用">
    让多个请求共享符合条件的持久连接以减少握手成本的机制。
  </GlossaryItem>
  <GlossaryItem term="请求幂等">
    同一请求重复执行仍保持预期资源状态不被重复放大的性质。
  </GlossaryItem>
  <GlossaryItem term="拦截器链">
    按顺序查看或修改请求响应并决定继续、短路或重试的责任链。
  </GlossaryItem>
  <GlossaryItem term="转换器">
    在HTTP消息体与应用对象之间进行编码解码的组件。
  </GlossaryItem>
  <GlossaryItem term="调用取消">
    停止排队或执行中的网络工作并阻止结果交付给失效所有者的能力。
  </GlossaryItem>
</Glossary>

[← 上一页：第4章 多线程编程](/learn/android-advanced-light/04-multithreading/aal-17-04-multithreading) · [下一页：第6章 设计模式 →](/learn/android-advanced-light/06-design-patterns/aal-17-06-design-patterns)

## 原版目录概念补充核对

以下条目补齐官方目录中容易被示例主线掩盖的概念。它们不重复罗列目录，而是明确每项概念的机制、适用边界和验收证据。

### 5.1 网络分层：机制、边界与证据

第5章 网络编程与网络框架中的5.1 网络分层要明确请求身份、超时/取消、缓存键、重试幂等与响应解析边界。用可控服务端分别返回成功、慢响应、断网和错误数据，核对网络日志、缓存状态、线程切换和用户可见结果。

### 5.2 TCP的三次握手与四次挥手：机制、边界与证据

第5章 网络编程与网络框架中的5.2 TCP的三次握手与四次挥手应固定 Android 5.0–7.0、Java 与旧 Support Library 语境，写出输入、状态、线程、生命周期和失败结果。以最小实现和单变量反例保存日志、状态快照与迁移差异。

### 5.3 HTTP协议原理：机制、边界与证据

第5章 网络编程与网络框架中的5.3 HTTP协议原理要明确请求身份、超时/取消、缓存键、重试幂等与响应解析边界。用可控服务端分别返回成功、慢响应、断网和错误数据，核对网络日志、缓存状态、线程切换和用户可见结果。

### 5.3.1 HTTP简介：机制、边界与证据

第5章 网络编程与网络框架中的5.3.1 HTTP简介要明确请求身份、超时/取消、缓存键、重试幂等与响应解析边界。用可控服务端分别返回成功、慢响应、断网和错误数据，核对网络日志、缓存状态、线程切换和用户可见结果。

### 5.3.2 HTTP请求报文：机制、边界与证据

第5章 网络编程与网络框架中的5.3.2 HTTP请求报文要明确请求身份、超时/取消、缓存键、重试幂等与响应解析边界。用可控服务端分别返回成功、慢响应、断网和错误数据，核对网络日志、缓存状态、线程切换和用户可见结果。

### 5.3.3 HTTP响应报文：机制、边界与证据

第5章 网络编程与网络框架中的5.3.3 HTTP响应报文要明确请求身份、超时/取消、缓存键、重试幂等与响应解析边界。用可控服务端分别返回成功、慢响应、断网和错误数据，核对网络日志、缓存状态、线程切换和用户可见结果。

### 5.3.4 HTTP的消息报头：机制、边界与证据

第5章 网络编程与网络框架中的5.3.4 HTTP的消息报头涉及任务调度、共享状态、取消和生命周期所有权，回调成功一次不代表旋转或销毁后仍安全。记录线程、消息队列、owner 状态与资源释放，并注入取消、迟到回调或竞争复现失败路径。

### 5.3.5 抓包应用举例：机制、边界与证据

第5章 网络编程与网络框架中的5.3.5 抓包应用举例应固定 Android 5.0–7.0、Java 与旧 Support Library 语境，写出输入、状态、线程、生命周期和失败结果。以最小实现和单变量反例保存日志、状态快照与迁移差异。

### 5.4 HttpClient与HttpURLConnection：机制、边界与证据

第5章 网络编程与网络框架中的5.4 HttpClient与HttpURLConnection应固定 Android 5.0–7.0、Java 与旧 Support Library 语境，写出输入、状态、线程、生命周期和失败结果。以最小实现和单变量反例保存日志、状态快照与迁移差异。

### 5.4.1 HttpClient：机制、边界与证据

第5章 网络编程与网络框架中的5.4.1 HttpClient应固定 Android 5.0–7.0、Java 与旧 Support Library 语境，写出输入、状态、线程、生命周期和失败结果。以最小实现和单变量反例保存日志、状态快照与迁移差异。

### 5.4.2 HttpURLConnection：机制、边界与证据

第5章 网络编程与网络框架中的5.4.2 HttpURLConnection应固定 Android 5.0–7.0、Java 与旧 Support Library 语境，写出输入、状态、线程、生命周期和失败结果。以最小实现和单变量反例保存日志、状态快照与迁移差异。

### 5.5 解析Volley：机制、边界与证据

第5章 网络编程与网络框架中的5.5 解析Volley要明确请求身份、超时/取消、缓存键、重试幂等与响应解析边界。用可控服务端分别返回成功、慢响应、断网和错误数据，核对网络日志、缓存状态、线程切换和用户可见结果。

### 5.5.1 Volley基本用法：机制、边界与证据

第5章 网络编程与网络框架中的5.5.1 Volley基本用法要明确请求身份、超时/取消、缓存键、重试幂等与响应解析边界。用可控服务端分别返回成功、慢响应、断网和错误数据，核对网络日志、缓存状态、线程切换和用户可见结果。

### 5.5.2 源码解析Volley：机制、边界与证据

第5章 网络编程与网络框架中的5.5.2 源码解析Volley要明确请求身份、超时/取消、缓存键、重试幂等与响应解析边界。用可控服务端分别返回成功、慢响应、断网和错误数据，核对网络日志、缓存状态、线程切换和用户可见结果。

### 5.6 解析OkHttp：机制、边界与证据

第5章 网络编程与网络框架中的5.6 解析OkHttp要明确请求身份、超时/取消、缓存键、重试幂等与响应解析边界。用可控服务端分别返回成功、慢响应、断网和错误数据，核对网络日志、缓存状态、线程切换和用户可见结果。

### 5.6.1 OkHttp基本用法：机制、边界与证据

第5章 网络编程与网络框架中的5.6.1 OkHttp基本用法要明确请求身份、超时/取消、缓存键、重试幂等与响应解析边界。用可控服务端分别返回成功、慢响应、断网和错误数据，核对网络日志、缓存状态、线程切换和用户可见结果。

### 5.6.2 源码解析OkHttp：机制、边界与证据

第5章 网络编程与网络框架中的5.6.2 源码解析OkHttp要明确请求身份、超时/取消、缓存键、重试幂等与响应解析边界。用可控服务端分别返回成功、慢响应、断网和错误数据，核对网络日志、缓存状态、线程切换和用户可见结果。

### 5.7 解析Retrofit：机制、边界与证据

第5章 网络编程与网络框架中的5.7 解析Retrofit应固定 Android 5.0–7.0、Java 与旧 Support Library 语境，写出输入、状态、线程、生命周期和失败结果。以最小实现和单变量反例保存日志、状态快照与迁移差异。

### 5.7.1 Retrofit基本用法：机制、边界与证据

第5章 网络编程与网络框架中的5.7.1 Retrofit基本用法应固定 Android 5.0–7.0、Java 与旧 Support Library 语境，写出输入、状态、线程、生命周期和失败结果。以最小实现和单变量反例保存日志、状态快照与迁移差异。

### 5.7.2 源码解析Retrofit：机制、边界与证据

第5章 网络编程与网络框架中的5.7.2 源码解析Retrofit应固定 Android 5.0–7.0、Java 与旧 Support Library 语境，写出输入、状态、线程、生命周期和失败结果。以最小实现和单变量反例保存日志、状态快照与迁移差异。

### 5.8 本章小结：机制、边界与证据

第5章 网络编程与网络框架中的5.8 本章小结应固定 Android 5.0–7.0、Java 与旧 Support Library 语境，写出输入、状态、线程、生命周期和失败结果。以最小实现和单变量反例保存日志、状态快照与迁移差异。

<Attribution
  adaptedFrom="刘望舒《Android进阶之光》"
  adaptedUrl="https://www.bookschina.com/7533720.htm"
  mode="independent-rewrite"
  sourceBasis="outline-only"
/>
