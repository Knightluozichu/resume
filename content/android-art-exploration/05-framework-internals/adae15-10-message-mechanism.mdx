---
title: "第10章 Android的消息机制"
description: "依据得到电子书完整目录覆盖8个节点：拆解ThreadLocal、MessageQueue、Looper、Handler与主线程消息循环，验证消息入队、取出、分发和退出"
type: C
section: "Android开发艺术探索（2015 / Android 5.0 / Java）"
draft: false
math: false
sourceUrl: "https://www.dedao.cn/ebook/detail?id=ORa2P4NNLqmQPG2178z5gvkDndlOxWy6eq3ajK6BEVXYRrJpA9M4oybeZpqAKQgj"
demo: true
order: 2
qualityVersion: 2
practiceMode: diagnosis
sourceMode: independent-rewrite
officialUnitId: adae15-10-message-mechanism
---

import { Objectives } from "@/components/mdx/objectives";
import { Term } from "@/components/mdx/term";
import { Callout } from "@/components/mdx/callout";
import { Answer, Exercises } from "@/components/mdx/exercises";
import { Glossary, GlossaryItem } from "@/components/mdx/glossary";
import { Attribution } from "@/components/mdx/attribution";
import { Adae1510MessageMechanismDecisionLab, Adae1510MessageMechanismMechanismMap, Adae1510MessageMechanismFailureDiagram } from "@/components/mdx/android-art-exploration/adae15-10-message-mechanism";

# 第10章 Android的消息机制

在《第10章 Android的消息机制》中，本页依据任玉刚《Android开发艺术探索》完整目录独立重构，不复制原文。版本锁定电子工业出版社2015年9月第1版、507页、ISBN 9787121269394，原书机制以Android 5.0与Java应用层/源码分析为基线。

原书中的AsyncTask、IntentService、旧权限/后台/存储规则和部分framework实现具有历史边界。在《第10章 Android的消息机制》中，课程先准确解释出版时机制，再单独建立现代targetSdk迁移账本；不会用Compose、协程或当前Jetpack替换原书目录后仍声称一比一覆盖。

<Objectives>

- 能解释“第10章 Android的消息机制”全部正式节点的调用方、所有者、线程、进程、状态与Android 5.0边界。
- 能实现“拆解ThreadLocal、MessageQueue、Looper、Handler与主线程消息循环，验证消息入队、取出、分发和退出”的最小垂直切片，并保存源码、构建、操作、日志和断言。
- 能比较正常、边界、组件重建、进程退出、队列阻塞与外部失败，分析“在没有Looper的线程创建Handler，或让非静态Handler长期持有已销毁组件”。
- 能设计反例并凭线程局部表、消息队列时间线、Looper循环、Handler归属、主线程栈和泄漏实验完成独立交接。

</Objectives>

<Adae1510MessageMechanismMechanismMap />

<Adae1510MessageMechanismDecisionLab />

<Adae1510MessageMechanismFailureDiagram />

## 从framework因果链开始

Android API只是入口。在《第10章 Android的消息机制》中，每个实验先找<Term def="创建、持有并最终取消或释放Activity、View、Binder连接、线程任务与原生资源的明确主体">生命周期所有者</Term>，再画“应用调用 → framework代理 → Binder或消息队列 → 系统/目标对象 → 回调”的链路。<Term def="一次用户动作跨对象、线程与进程产生可观察结果的有序调用关系">调用因果链</Term>帮助区分源码事实和表面现象。

在《第10章 Android的消息机制》中，主线程、Binder线程池、HandlerThread、线程池和原生线程有不同责任。<Term def="回调实际执行的线程、队列顺序以及允许阻塞或触碰UI的约束">线程合同</Term>必须写在代码旁。状态也要分为界面暂态、可恢复实例状态、进程内缓存、文件/数据库事实和系统服务状态；进程单例从来不是持久事实源。

本章主线是拆解ThreadLocal、MessageQueue、Looper、Handler与主线程消息循环，验证消息入队、取出、分发和退出。所有节点都要生成<Term def="由固定环境、输入、操作、原始日志、时序、资源计数与断言组成的可重放结论">证据链</Term>，并声明<Term def="Android版本、targetSdk、设备实现、framework源码分支与依赖共同限定的结论范围">版本边界</Term>。正常截图不证明线程、重建、失败和释放正确。

## 最小可执行切片

在《第10章 Android的消息机制》中，下面的Java片段只抓住本章核心合同。先预测线程、状态和失败，再在匹配Android 5.0语境的样例工程中运行；代码所依赖的上下文和导入应在工程中显式声明。

```java
Handler main = new Handler(Looper.getMainLooper()) {
  @Override public void handleMessage(Message message) {
    if (message.what == MSG_RENDER) render((Model) message.obj);
  }
};

main.obtainMessage(MSG_RENDER, model).sendToTarget();
```

固定环境并保留构建、安装、组件与线程证据：

```bash
./gradlew --no-daemon clean test assembleDebug
adb install -r app/build/outputs/apk/debug/app-debug.apk
adb shell am force-stop org.example.art
adb shell monkey -p org.example.art 1
adb shell dumpsys activity processes > processes.txt
adb logcat -d -v threadtime > run.log
```

将机制写成能失败的行为合同：

```gherkin
Given a pinned JDK, Android plugin, device API, process state, permission state, and input
When the smallest scenario for "adae15-10-message-mechanism" crosses its owner, thread, or process boundary
Then callbacks, durable state, side effects, timing, and resource release match the prediction
And process death, owner destruction, malformed input, queue pressure, and repeated work stay observable
And modern targetSdk differences are recorded as migration evidence, not original behavior
```

在《第10章 Android的消息机制》中，三段材料分别约束源码机制、环境重放和用户可见结果。若日志没有线程名、进程号、输入标识和阶段，或测试只断言“没有崩溃”，就无法证明真正经过目标调用链。

## 四层源码与故障证明

在《第10章 Android的消息机制》中，**调用层。** 从“第10章 Android的消息机制”选择一次入口，写出应用对象、framework代理、系统服务或目标组件以及返回路径。对源码分析，记录Android版本、类名、方法与关键分支；对应用实验，记录用户动作、Intent或消息标识。先解释谁创建工作、谁持有引用、谁负责结束，再谈API细节。

在《第10章 Android的消息机制》中，**线程与进程层。** 在每个箭头上标线程和进程。主线程不能执行无界I/O或计算；Binder回调不自动处于主线程；Service也不自动提供后台线程。用线程名、进程号、队列长度和时间戳验证，分别注入进程退出、Binder死亡、队列饱和和组件销毁，检查迟到结果是否被丢弃。

在《第10章 Android的消息机制》中，**状态与资源层。** 列出可恢复事实、缓存和外部资源。旋转、后台回收、进程重建、列表复用与配置变化后，界面应由事实重建，而不是依赖旧对象。文件、Cursor、Bitmap、动画、Window、线程池和JNI引用都要有关闭或取消点；保存前后资源计数，避免只靠肉眼判断泄漏。

**迁移层。** 原书锁定Android 5.0。在《第10章 Android的消息机制》中，现代迁移一次只改变targetSdk、组件导出、后台限制、存储、权限、通知、构建插件或替代API之一，比较构建、调用链、用户行为、测试与回滚。新实现可以使用Jetpack或结构化并发，但必须保留原书机制说明，不能抹去历史API为何工作及为何后来受限。

围绕“在没有Looper的线程创建Handler，或让非静态Handler长期持有已销毁组件”预写一个推翻条件：状态丢失、错误线程、越权、ANR、泄漏、结果错位、重复副作用或原生崩溃中的任一项出现，都意味着当前实现不成立。最终交付线程局部表、消息队列时间线、Looper循环、Handler归属、主线程栈和泄漏实验，让他人无需口头提示即可复现。

<Callout type="trap">

  **本章核心陷阱。** 在没有Looper的线程创建Handler，或让非静态Handler长期持有已销毁组件。修复时先恢复所有者、线程/进程、状态与释放合同，再写能推翻实现的故障测试。

</Callout>
<Callout type="trap">

  在《第10章 Android的消息机制》中，**源码版本错位。** 在《第10章 Android的消息机制》中，framework实现会随Android版本变化。引用源码必须标版本与调用分支；现代API只能作为迁移对照，不能冒充2015年原书内容。

</Callout>
<Callout type="warning">

  在《第10章 Android的消息机制》中，IPC、崩溃、动态加载、反编译与JNI实验只使用自有样例和合成数据。日志、转储、APK与原生崩溃文件不得包含真实账号、令牌、位置或用户内容。

</Callout>
## 本章回顾

- 节点覆盖Android的消息机制
- 最小切片验证拆解ThreadLocal、Messa…
- 故障实验对比在没有Looper的线程创建Handler…
- 凭线程局部表、消息队列时间线、Looper循…完成交接

<Exercises>

**问题 1**：“第10章 Android的消息机制”覆盖哪些正式节点和机制主线？
<Answer>
  覆盖8个节点，从“第10章 Android的消息机制”到“10.3 主线程的消息循环”；主线是拆解ThreadLocal、MessageQueue、Looper、Handler与主线程消息循环，验证消息入队、取出、分发和退出。
</Answer>
**问题 2**：怎样建立本章最小可执行切片？
<Answer>
  固定Android版本、JDK、构建插件、设备、进程和输入，贯通一次用户动作、framework调用、线程/进程边界与可观察结果，并保存线程局部表、消息队列时间线、Looper循环、Handler归属、主线程栈和泄漏实验。
</Answer>
**问题 3**：本章最需要推翻的错误假设是什么？
<Answer>
  错误假设是“在没有Looper的线程创建Handler，或让非静态Handler长期持有已销毁组件”。保持其他条件不变，注入重建、进程退出、错误线程、队列压力、无效输入或资源未释放；异常即推翻。
</Answer>
**问题 4**：为什么一次正常运行不能证明framework机制？
<Answer>
  它没有证明真实调用分支、线程/进程、状态恢复、系统故障与释放。必须保存源码定位、时序、故障路径和资源计数。
</Answer>
**问题 5**：怎样迁移到现代targetSdk而不改写原书？
<Answer>
  在《第10章 Android的消息机制》中，先按Android 5.0保存原书行为基线，再一次只改变平台、targetSdk、插件、权限、后台、存储或替代API之一，记录行为、测试和回滚差异。
</Answer>
**问题 6**：达到独立交接标准需要哪些证据？
<Answer>
  需要环境和设备指纹、源码版本、构建产物、输入、线程/进程时序、状态快照、失败测试、资源释放、版本边界与线程局部表、消息队列时间线、Looper循环、Handler归属、主线程栈和泄漏实验。
</Answer>
</Exercises>

<Glossary>
  <GlossaryItem term="生命周期所有者">
    创建、持有并最终取消或释放Activity、View、Binder连接、线程任务与原生资源的明确主体。
  </GlossaryItem>
  <GlossaryItem term="调用因果链">
    一次用户动作跨对象、线程与进程产生可观察结果的有序调用关系。
  </GlossaryItem>
  <GlossaryItem term="线程合同">
    回调实际执行的线程、队列顺序以及允许阻塞或触碰UI的约束。
  </GlossaryItem>
  <GlossaryItem term="证据链">
    由固定环境、输入、操作、原始日志、时序、资源计数与断言组成的可重放结论。
  </GlossaryItem>
  <GlossaryItem term="版本边界">
    Android版本、targetSdk、设备实现、framework源码分支与依赖共同限定的结论范围。
  </GlossaryItem>
</Glossary>

[← 上一页：第9章 四大组件的工作过程](/learn/android-art-exploration/05-framework-internals/adae15-09-four-components) · [下一页：第11章 Android的线程和线程池 →](/learn/android-art-exploration/06-resources-performance/adae15-11-threads-pools)

## 原版目录概念补充核对

以下条目补齐官方目录中容易被示例主线掩盖的概念。它们不重复罗列目录，而是明确每项概念的机制、适用边界和验收证据。

### 10.1 Android的消息机制概述：机制、边界与证据

第10章 Android的消息机制中的10.1 Android的消息机制概述需要区分请求入口、system_server/消息队列状态、目标线程与完成回调。用 PID/TID、token/record、队列时序和最终组件状态交叉验证，并注入进程未启动或迟到消息。

### 10.2 Android的消息机制分析：机制、边界与证据

第10章 Android的消息机制中的10.2 Android的消息机制分析需要区分请求入口、system_server/消息队列状态、目标线程与完成回调。用 PID/TID、token/record、队列时序和最终组件状态交叉验证，并注入进程未启动或迟到消息。

### 10.2.1 ThreadLocal的工作原理：机制、边界与证据

第10章 Android的消息机制中的10.2.1 ThreadLocal的工作原理必须放回 Android 5.0/Java 基线的应用调用—framework 代理—Binder/消息队列—目标对象—回调链路。固定版本，先预测状态变化，再以源码符号、运行轨迹和一个失败样本复核。

### 10.2.2 消息队列的工作原理：机制、边界与证据

第10章 Android的消息机制中的10.2.2 消息队列的工作原理需要区分请求入口、system_server/消息队列状态、目标线程与完成回调。用 PID/TID、token/record、队列时序和最终组件状态交叉验证，并注入进程未启动或迟到消息。

### 10.2.3 Looper的工作原理：机制、边界与证据

第10章 Android的消息机制中的10.2.3 Looper的工作原理需要区分请求入口、system_server/消息队列状态、目标线程与完成回调。用 PID/TID、token/record、队列时序和最终组件状态交叉验证，并注入进程未启动或迟到消息。

### 10.2.4 Handler的工作原理：机制、边界与证据

第10章 Android的消息机制中的10.2.4 Handler的工作原理需要区分请求入口、system_server/消息队列状态、目标线程与完成回调。用 PID/TID、token/record、队列时序和最终组件状态交叉验证，并注入进程未启动或迟到消息。

<Attribution
  adaptedFrom="任玉刚《Android开发艺术探索》"
  adaptedUrl="https://www.dedao.cn/ebook/detail?id=ORa2P4NNLqmQPG2178z5gvkDndlOxWy6eq3ajK6BEVXYRrJpA9M4oybeZpqAKQgj"
  mode="independent-rewrite"
  sourceBasis="outline-only"
/>
