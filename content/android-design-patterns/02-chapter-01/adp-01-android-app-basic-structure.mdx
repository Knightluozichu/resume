---
title: "第1章 Android 应用的基本构成"
description: "以统一TODO规格比较MVP、MVVM，并解释平台约束、历史问题、库与语言的影响"
type: C
section: "第I部：理解应用设计"
order: 2
draft: false
math: false
sourceUrl: "https://peaks.cc/books/architecture_patterns"
demo: true
qualityVersion: 2
sourceMode: "independent-rewrite"
practiceMode: "diagnosis"
---

import { Objectives } from "@/components/mdx/objectives";
import { Term } from "@/components/mdx/term";
import { Step, Stepper } from "@/components/mdx/stepper";
import { Callout } from "@/components/mdx/callout";
import { Answer, Exercises } from "@/components/mdx/exercises";
import { Glossary, GlossaryItem } from "@/components/mdx/glossary";
import { Attribution } from "@/components/mdx/attribution";
import { Adp01AndroidAppBasicStructureLab } from "@/components/mdx/android-design-patterns/adp-01-android-app-basic-structure";
import {
  AndroidDecisionMap,
  AndroidFailureTimeline,
} from "@/components/mdx/android-design-patterns/android-architecture-lab";

# 第1章 Android 应用的基本构成

本页依据PEAKS最终商品页、官方样章完整目录和书中指定样例仓库重构。正式书由日高正博、小西裕介、藤原聖、吉岡毅、今井智章合著，2018年1月31日发行，224页；本课程严格保留出版时的Android技术语境。

本页位于 **第 I 部：理解应用设计**。 以统一TODO规格比较MVP、MVVM，并解释平台约束、历史问题、库与语言的影响。核心任务是把五屏TODO应用作为控制变量，从职责、依赖方向、生命周期与可测试性解释Android架构为何演进。先把结论写成可被反例推翻的预测，再阅读机制与案例。

<Objectives>

- 能解释“第1章 Android 应用的基本构成”全部正式目录节点，并画出角色、依赖方向、状态所有权和生命周期。
- 能比较至少两种设计选择，说明收益、代价、团队前提和不可采用的反例。
- 能设计旋转、后台、迟到回调、空数据或协作冲突实验，验证“TODO的新增、编辑、删除、本地保存和远端同步语义不随架构改变；改变的只能是职责分配与协作协议”。
- 能写出包含版本、代码快照、状态轨迹、测试结果、回退条件和责任人的交接记录。

</Objectives>

<Adp01AndroidAppBasicStructureLab />

<AndroidDecisionMap
  title="第1章 Android 应用的基本构成 的责任路径"
  input="固定 TODO 新增、编辑、删除、列表与持久化规格。"
  owner="比较 Presenter、ViewModel、View 与平台组件的状态写权限。"
  output="用恢复轨迹判断职责分配是否真正降低耦合。"
/>

<AndroidFailureTimeline
  scenario="第1章 Android 应用的基本构成 的失败验证"
  baseline="正常启动、加载并保存一条 TODO。"
  fault="旋转后让旧异步回调迟到。"
  guard="分离旧 View，并由唯一状态所有者接收结果。"
  verdict="新界面只渲染当前状态，旧实例不再被写入。"
/>

<Callout type="trap" title="第1章 Android 应用的基本构成 的反例">
  把 Activity 中的字段搬到一个名叫 ViewModel
  的类里，不会自动修正状态写权限和生命周期泄漏。
</Callout>

## 从一个可证伪的问题开始

先预测：如果只更换类名或框架，而没有改变责任、依赖方向和状态所有者，只把类改名为ViewModel或Presenter而不改变依赖方向，会继续留下Fat Activity、生命周期泄漏和无法隔离测试的问题。把预测落实为同一输入下的两条运行轨迹，不要把“能编译”“页面能打开”当成架构正确。

验收不变量是：TODO的新增、编辑、删除、本地保存和远端同步语义不随架构改变；改变的只能是职责分配与协作协议。若结论无法由代码快照、调用/状态轨迹、失败注入和团队交付事实共同支持，就只能记为待验证假设。

## 核心词汇与2018年边界

<Term def="MVP是本页第1个核心概念；必须同时说明职责、依赖方向、生命周期、失败反例与可观察证据">
  MVP
</Term>

<Term def="MVVM是本页第2个核心概念；必须同时说明职责、依赖方向、生命周期、失败反例与可观察证据">
  MVVM
</Term>

<Term def="Fat Activity是本页第3个核心概念；必须同时说明职责、依赖方向、生命周期、失败反例与可观察证据">
  Fat Activity
</Term>

<Term def="生命周期是本页第4个核心概念；必须同时说明职责、依赖方向、生命周期、失败反例与可观察证据">
  生命周期
</Term>

<Term def="Android Architecture Components是本页第5个核心概念；必须同时说明职责、依赖方向、生命周期、失败反例与可观察证据">
  Android Architecture Components
</Term>

以上词汇都要回答五个问题：谁拥有状态、谁可以修改、Android生命周期如何影响它、失败从哪里传播、怎样证明恢复正确。Compose、Flow、Hilt、Navigation Compose以及后来的Jetpack最佳实践可作为迁移对照，但不能改写2018年正式目录的含义。

## 原书目录逐节点重构

### 第 I 部：理解应用设计

第 I 部：理解应用设计 的复核需要落到实际对象：标明输入、状态所有者、调用或数据方向、生命周期边界和失败后的恢复结果，并保存一条能够推翻当前判断的反例；只有名称或类图不足以证明设计成立。

### 1.1 讨论所基于的应用与规格

固定新增、编辑、删除、列表、详情、统计和持久化行为，使架构比较只改变职责分配而不改变业务题目。

### 1.2 架构选择

先列约束和质量目标，再比较依赖方向、生命周期、测试替身、学习成本与迁移成本；不能从模式名称反推适用性。

### 1.3 Model-View-Presenter

Presenter接收用户意图并调用数据边界，再通过窄View契约显式渲染；测试应验证调用次序、空态、错误态和分离后的迟到回调。

### 1.4 Model-View-ViewModel

ViewModel暴露可观察页面状态，不持有具体View；View通过绑定或观察更新，瞬时导航与持久状态必须分开建模。

#### 1.4.1 数据绑定

布局声明状态到控件属性的映射，减少命令式查找与赋值；验收要覆盖空值、解绑、重建和重复观察。

#### 1.4.2 MVVM 的基础思想：Presentation Model

ViewModel暴露可观察页面状态，不持有具体View；View通过绑定或观察更新，瞬时导航与持久状态必须分开建模。

### 1.5 平台约束与复杂性

1.5 平台约束与复杂性 的复核需要落到实际对象：标明输入、状态所有者、调用或数据方向、生命周期边界和失败后的恢复结果，并保存一条能够推翻当前判断的反例；只有名称或类图不足以证明设计成立。

### 1.6 设计演进

1.6 设计演进 的复核需要落到实际对象：标明输入、状态所有者、调用或数据方向、生命周期边界和失败后的恢复结果，并保存一条能够推翻当前判断的反例；只有名称或类图不足以证明设计成立。

#### 1.6.1 Fat Activity 问题

Android组件只负责装配、生命周期桥接、渲染和转发意图，业务判断进入可替换的Presenter、ViewModel或Store。

#### 1.6.2 生命周期复杂化

逐一记录create、start、stop、配置变更和最终destroy，确认订阅、缓存、实例复用与释放都发生在正确边界。

#### 1.6.3 版本差异

1.6.3 版本差异 的复核需要落到实际对象：标明输入、状态所有者、调用或数据方向、生命周期边界和失败后的恢复结果，并保存一条能够推翻当前判断的反例；只有名称或类图不足以证明设计成立。

兼容层缓冲平台版本差异，但API可用性和实际设备行为仍需矩阵验证，不能只依赖编译通过。

#### 1.6.4 异步处理与后台执行

1.6.4 异步处理与后台执行 的复核需要落到实际对象：标明输入、状态所有者、调用或数据方向、生命周期边界和失败后的恢复结果，并保存一条能够推翻当前判断的反例；只有名称或类图不足以证明设计成立。

流式组合能显式表达异步链，但线程、取消、重试、错误和订阅释放仍需由架构边界负责。

#### 1.6.5 团队开发

1.6.5 团队开发 的复核需要落到实际对象：标明输入、状态所有者、调用或数据方向、生命周期边界和失败后的恢复结果，并保存一条能够推翻当前判断的反例；只有名称或类图不足以证明设计成立。

### 1.7 第三方库兴起

1.7 第三方库兴起 的复核需要落到实际对象：标明输入、状态所有者、调用或数据方向、生命周期边界和失败后的恢复结果，并保存一条能够推翻当前判断的反例；只有名称或类图不足以证明设计成立。

#### 1.7.1 Android Architecture Components

1.7.1 Android Architecture Components 的复核需要落到实际对象：标明输入、状态所有者、调用或数据方向、生命周期边界和失败后的恢复结果，并保存一条能够推翻当前判断的反例；只有名称或类图不足以证明设计成立。

#### 1.7.2 Android Support Library

1.7.2 Android Support Library 的复核需要落到实际对象：标明输入、状态所有者、调用或数据方向、生命周期边界和失败后的恢复结果，并保存一条能够推翻当前判断的反例；只有名称或类图不足以证明设计成立。

兼容层缓冲平台版本差异，但API可用性和实际设备行为仍需矩阵验证，不能只依赖编译通过。

#### 1.7.3 Dagger 2

依赖注入建立组合根和作用域，价值在于替换、生命周期对齐与依赖可见，而不是减少new关键字。

#### 1.7.4 Gson

网络和序列化库分别承担传输、接口映射和数据转换；超时、HTTP错误、字段演化与取消不能被统一成一个成功/失败布尔值。

#### 1.7.5 OkHttp

网络和序列化库分别承担传输、接口映射和数据转换；超时、HTTP错误、字段演化与取消不能被统一成一个成功/失败布尔值。

#### 1.7.6 PermissionsDispatcher

所有意图和结果都写成Action，经Dispatcher进入Store计算新状态，再由View观察；日志应能从Action重建状态变化。

#### 1.7.7 Retrofit 2

网络和序列化库分别承担传输、接口映射和数据转换；超时、HTTP错误、字段演化与取消不能被统一成一个成功/失败布尔值。

#### 1.7.8 RxJava 2

1.7.8 RxJava 2 的复核需要落到实际对象：标明输入、状态所有者、调用或数据方向、生命周期边界和失败后的恢复结果，并保存一条能够推翻当前判断的反例；只有名称或类图不足以证明设计成立。

流式组合能显式表达异步链，但线程、取消、重试、错误和订阅释放仍需由架构边界负责。

### 1.8 编程语言发展

语言能力会改变空值、函数、不可变性和样板成本，但最终成书未保留计划中的Kotlin正式章，本节只能说明语言影响的背景。

## 完整节点清单与覆盖门

本页承担下列23个目录或检索节点；正文、图解、实验和题目必须都能反向定位这些节点：

- 1.1 讨论所基于的应用与规格
- 1.2 架构选择
- 1.3 Model-View-Presenter
- 1.4 Model-View-ViewModel
- 1.4.1 数据绑定
- 1.4.2 MVVM 的基础思想：Presentation Model
- 1.5 平台约束与复杂性
- 1.6 设计演进
- 1.6.1 Fat Activity 问题
- 1.6.2 生命周期复杂化
- 1.6.3 版本差异
- 1.6.4 异步处理与后台执行
- 1.6.5 团队开发
- 1.7 第三方库兴起
- 1.7.1 Android Architecture Components
- 1.7.2 Android Support Library
- 1.7.3 Dagger 2
- 1.7.4 Gson
- 1.7.5 OkHttp
- 1.7.6 PermissionsDispatcher
- 1.7.7 Retrofit 2
- 1.7.8 RxJava 2
- 1.8 编程语言发展

## 分步推导：从结构到运行事实

<Stepper>
  <Step title="先画责任与依赖">
    固定业务输入，标明Android组件、Presenter/ViewModel/Store、数据边界与团队所有者。箭头表示调用或数据流，不能只画文件夹。
  </Step>
  <Step title="再制造单变量反例">
    保持样例不变，只改变生命周期、异步到达顺序、共享状态或团队协作中的一个变量。先写预测，再运行，不在看到结果后补理由。
  </Step>
  <Step title="最后关闭证据门">
    对照正常与失败轨迹，确认业务不变量、释放、恢复、回退和责任人。缺一项就不能把“偏好”升级为“决策”。
  </Step>
</Stepper>

## 可复现实验与记录格式

围绕“第1章 Android 应用的基本构成”，下面分别保存结构快照、运行轨迹和判定表；这些记录用于复现本页结论，而不是生产代码模板：

```java
interface TodoView { void render(TodoUiState state); }
interface TodoPresenter { void load(); void add(String title); }
```

```text
create -> start -> rotate -> recreate -> restore -> stop -> background
```

```text
same TODO specification | MVP trace | MVVM trace | observable difference
```

动手试（第1章 Android 应用的基本构成）：用同一TODO或章节案例分别运行正常、旋转/重建、后台、迟到成功、迟到失败五条路径。每条路径记录输入、对象实例、线程/调度、状态前后值、UI结果和释放点。若是团队案例，再增加变更文件数、评审往返和回退耗时。

<Callout type="trap" title="用现代结论覆盖历史版本">
  本页对应2018年成书与2017年前后样例。现代Android方案可放在迁移备注中比较，但不能替换正式节点、作者当时的约束或样例行为。
</Callout>

## 独立证据门

“第1章 Android 应用的基本构成”的“第1章 Android 应用的基本构成”的“第1章 Android 应用的基本构成”的“第1章 Android 应用的基本构成”的“第1章 Android 应用的基本构成”的“第1章 Android 应用的基本构成”的证据包至少包含：正式目录映射、来源URL、出版前代码提交、角色与依赖图、生命周期轨迹、一个单变量失败实验、测试输出、业务/团队指标、停止条件、回退步骤和复核人。图和代码必须使用相同角色命名，否则无法交叉验证。

## 练习

<Exercises>

**问题 1：在“第1章 Android 应用的基本构成”中，为什么要先固定业务规格或案例边界？**

<Answer>
  在“第1章 Android
  应用的基本构成”中，只有输入、功能和数据语义保持不变，差异才可归因于职责、依赖或生命周期策略；若同时改需求，就无法识别变化来源。
</Answer>

**问题 2：怎样构造“只把类改名为ViewModel或Presenter而不改变依赖方向，会继续留下Fat Activity、生命周期泄漏和无法隔离测试的问题”的最小反例？**

<Answer>
  固定版本、样例数据和操作序列，只引入一次旋转、后台、迟到回调、共享写入或协作冲突；比较对象身份、状态轨迹、UI结果、释放与最终业务事实。
</Answer>

**问题 3：何时可以认为“第1章 Android 应用的基本构成”已完成独立交接？**

<Answer>
  当另一位读者无需口头补充即可按版本记录复现实验，解释全部目录节点，观察正常与失败轨迹，并依据“TODO的新增、编辑、删除、本地保存和远端同步语义不随架构改变；改变的只能是职责分配与协作协议”独立得到同一通过或不通过结论。
</Answer>

</Exercises>

## 本章回顾

“第1章 Android 应用的基本构成”的核心不是记忆名词，而是把五屏TODO应用作为控制变量，从职责、依赖方向、生命周期与可测试性解释Android架构为何演进。最终结论必须同时经受平台生命周期、失败路径、既有代码和团队协作四类约束；只把类改名为ViewModel或Presenter而不改变依赖方向，会继续留下Fat Activity、生命周期泄漏和无法隔离测试的问题就是本页必须保留的反证边界。

<Glossary>
  <GlossaryItem term="MVP">
    MVP用于解释“把五屏TODO应用作为控制变量，从职责、依赖方向、生命周期与可测试性解释Android架构为何演进”。掌握标准不是记住名称，而是能画出责任与状态轨迹、构造单变量反例，并用“TODO的新增、编辑、删除、本地保存和远端同步语义不随架构改变；改变的只能是职责分配与协作协议”完成验收。
  </GlossaryItem>
  <GlossaryItem term="MVVM">
    MVVM用于解释“把五屏TODO应用作为控制变量，从职责、依赖方向、生命周期与可测试性解释Android架构为何演进”。掌握标准不是记住名称，而是能画出责任与状态轨迹、构造单变量反例，并用“TODO的新增、编辑、删除、本地保存和远端同步语义不随架构改变；改变的只能是职责分配与协作协议”完成验收。
  </GlossaryItem>
  <GlossaryItem term="Fat Activity">
    Fat
    Activity用于解释“把五屏TODO应用作为控制变量，从职责、依赖方向、生命周期与可测试性解释Android架构为何演进”。掌握标准不是记住名称，而是能画出责任与状态轨迹、构造单变量反例，并用“TODO的新增、编辑、删除、本地保存和远端同步语义不随架构改变；改变的只能是职责分配与协作协议”完成验收。
  </GlossaryItem>
  <GlossaryItem term="生命周期">
    生命周期用于解释“把五屏TODO应用作为控制变量，从职责、依赖方向、生命周期与可测试性解释Android架构为何演进”。掌握标准不是记住名称，而是能画出责任与状态轨迹、构造单变量反例，并用“TODO的新增、编辑、删除、本地保存和远端同步语义不随架构改变；改变的只能是职责分配与协作协议”完成验收。
  </GlossaryItem>
  <GlossaryItem term="Android Architecture Components">
    Android Architecture
    Components用于解释“把五屏TODO应用作为控制变量，从职责、依赖方向、生命周期与可测试性解释Android架构为何演进”。掌握标准不是记住名称，而是能画出责任与状态轨迹、构造单变量反例，并用“TODO的新增、编辑、删除、本地保存和远端同步语义不随架构改变；改变的只能是职责分配与协作协议”完成验收。
  </GlossaryItem>
</Glossary>

[← 上一页：前言](/learn/android-design-patterns/01-preface/adp-preface) · [下一页：第2章 MVVM 模式应用构成 →](/learn/android-design-patterns/03-chapter-02/adp-02-mvvm-application-structure)

<Attribution
  adaptedFrom="日高正博、小西裕介、藤原聖、吉岡毅、今井智章《Android 应用设计模式入门》2018年正式版"
  adaptedUrl="https://peaks.cc/books/architecture_patterns"
/>
