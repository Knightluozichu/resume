---
title: MVVM 模式应用构成
type: C
section: Android 架构与设计模式
order: 3
description: MVVM 用可观察状态替代手动回调，让 UI 随状态渲染而不是被命令驱动。
demo: true
math: false
sourceUrl: https://peaks.cc/architecture_patterns
draft: false
---

{/* 知识点清单：MVVM/ViewModel/Observable State/DataBinding；原书章节：第3章 MVVMパターンのアプリ構成。 */}

<Objectives>

- 能解释 ViewModel 不持有 View，View 只观察可公开状态。
- 能拖动图中的步骤，复述本章真实协作故事。
- 能回答：MVVM 用可观察状态替代手动回调，让 UI 随状态渲染而不是被命令驱动。

</Objectives>

## 先看本章故事

MVVM 用可观察状态替代手动回调，让 UI 随状态渲染而不是被命令驱动。 这章先不背模式名，而是把团队会争论的边界画出来：谁接收事件，谁做判断，谁保存状态，谁解释失败。

<AndroidDesignLab
  mode="aac"
  title="MVVM 模式应用构成 · 协作动画"
  story="用户输入标题"
/>

<Callout type="tip">
  <strong>猜一猜：</strong>如果“改动压力”拉高，本章哪条职责边界会先变脆？
</Callout>

## 分步动画：MVVM 模式应用构成

<Stepper>
  <Step title="1. 用户输入标题">
    <AndroidDesignLab
      mode="aac"
      title="MVVM 模式应用构成 · 用户输入标题"
      story="用户输入标题"
    />
  </Step>
  <Step title="2. ViewModel 更新 UiState">
    <AndroidDesignLab
      mode="aac"
      title="MVVM 模式应用构成 · ViewModel 更新 UiState"
      story="ViewModel 更新 UiState"
    />
  </Step>
  <Step title="3. 状态流发出错误或成功">
    <AndroidDesignLab
      mode="aac"
      title="MVVM 模式应用构成 · 状态流发出错误或成功"
      story="状态流发出错误或成功"
    />
  </Step>
  <Step title="4. View 根据状态自动渲染">
    <AndroidDesignLab
      mode="aac"
      title="MVVM 模式应用构成 · View 根据状态自动渲染"
      story="View 根据状态自动渲染"
    />
  </Step>
</Stepper>

## 核心概念

<Term def="MVVM 用可观察状态替代手动回调，让 UI 随状态渲染而不是被命令驱动。">
  MVVM
</Term>
要服务团队协作，而不是只服务当前开发者的手感。

<Term def="一条能被 review、测试和文档共同保护的代码边界。">责任边界</Term>
是本章的验收线：边界画不清，模式名再漂亮也没用。

## 常见误区

<Callout type="trap">
  <strong>现象 →</strong> ViewModel 拿 Context 或
  Fragment，导致生命周期和测试边界重新纠缠。 <strong>原因 →</strong>{" "}
  只讨论实现，没有讨论职责边界。 <strong>修法 →</strong>{" "}
  回到图里，把事件、状态、结果和失败路径都标出来。
</Callout>

<Callout type="trap">
  <strong>现象 →</strong> Stepper 能播放，但讲不出本章故事。{" "}
  <strong>原因 →</strong> 图只是高亮控件，没有绑定真实协作场景。{" "}
  <strong>修法 →</strong> 用“谁做决定、谁能测试、谁负责展示”重讲一遍。
</Callout>

## 小结与练习

- 本章核心：MVVM 用可观察状态替代手动回调，让 UI 随状态渲染而不是被命令驱动。
- 本章图型：aac，重点观察职责边界如何移动。
- 本章练习：把 MVP 的保存流程改成 ViewModel + StateFlow，并写状态断言测试。

<Exercises>
  <Answer question="本章 Stepper 的 4 步分别在解决什么协作问题？">
    用户输入标题 → ViewModel 更新 UiState → 状态流发出错误或成功 → View
    根据状态自动渲染。回答时要指出每一步的负责人和输出物。
  </Answer>
  <Answer question="本章最危险的误用是什么？">
    ViewModel 拿 Context 或 Fragment，导致生命周期和测试边界重新纠缠。
    修法是先画边界，再决定类、接口和测试替身。
  </Answer>
  <Answer question="把 MVP 的保存流程改成 ViewModel + StateFlow，并写状态断言测试。">
    用一张小图标出输入、判断、状态、输出和失败路径；再写一句 review
    规则保护这条边界。
  </Answer>
</Exercises>

## 名词解释

<Glossary>
  <GlossaryItem term="MVVM">
    MVVM 是 MVVM 模式应用构成 的关键概念；要能在图里指出它负责哪条协作边界。
  </GlossaryItem>
  <GlossaryItem term="ViewModel">
    ViewModel 是 MVVM 模式应用构成
    的关键概念；要能在图里指出它负责哪条协作边界。
  </GlossaryItem>
  <GlossaryItem term="Observable State">
    Observable State 是 MVVM 模式应用构成
    的关键概念；要能在图里指出它负责哪条协作边界。
  </GlossaryItem>
  <GlossaryItem term="DataBinding">
    DataBinding 是 MVVM 模式应用构成
    的关键概念；要能在图里指出它负责哪条协作边界。
  </GlossaryItem>
  <GlossaryItem term="责任边界">
    责任边界是代码中可以被讨论、测试、review 和文档共同保护的分界线。
  </GlossaryItem>
</Glossary>

<Attribution
  adaptedFrom="Android アプリ設計パターン入門"
  adaptedUrl="https://peaks.cc/architecture_patterns"
/>
