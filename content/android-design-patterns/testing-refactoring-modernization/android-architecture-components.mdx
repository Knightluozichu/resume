---
title: Android Architecture Components
type: C
section: 测试重构与现代化
order: 8
description: AAC 不是架构答案本身，而是让生命周期敏感的状态有稳定位置。
demo: true
math: false
sourceUrl: https://peaks.cc/architecture_patterns
draft: false
---

{/* 知识点清单：LifecycleOwner/ViewModelStore/Observer/SavedState；原书章节：第8章 Android Architecture Components。 */}

<Objectives>

- 能解释 Lifecycle、ViewModel、LiveData/DataBinding 如何配合处理生命周期与状态。
- 能拖动图中的步骤，复述本章真实协作故事。
- 能回答：AAC 不是架构答案本身，而是让生命周期敏感的状态有稳定位置。

</Objectives>

## 先看本章故事

AAC 不是架构答案本身，而是让生命周期敏感的状态有稳定位置。 这章先不背模式名，而是把团队会争论的边界画出来：谁接收事件，谁做判断，谁保存状态，谁解释失败。

<AndroidDesignLab
  mode="lifecycle"
  title="Android Architecture Components · 协作动画"
  story="打开页面并订阅数据"
/>

<Callout type="tip">
  <strong>猜一猜：</strong>如果“改动压力”拉高，本章哪条职责边界会先变脆？
</Callout>

## 分步动画：Android Architecture Components

<Stepper>
  <Step title="1. 打开页面并订阅数据">
    <AndroidDesignLab
      mode="lifecycle"
      title="Android Architecture Components · 打开页面并订阅数据"
      story="打开页面并订阅数据"
    />
  </Step>
  <Step title="2. 旋转屏幕后 View 销毁重建">
    <AndroidDesignLab
      mode="lifecycle"
      title="Android Architecture Components · 旋转屏幕后 View 销毁重建"
      story="旋转屏幕后 View 销毁重建"
    />
  </Step>
  <Step title="3. ViewModel 保留状态">
    <AndroidDesignLab
      mode="lifecycle"
      title="Android Architecture Components · ViewModel 保留状态"
      story="ViewModel 保留状态"
    />
  </Step>
  <Step title="4. Observer 重新订阅并恢复 UI">
    <AndroidDesignLab
      mode="lifecycle"
      title="Android Architecture Components · Observer 重新订阅并恢复 UI"
      story="Observer 重新订阅并恢复 UI"
    />
  </Step>
</Stepper>

## 核心概念

<Term def="AAC 不是架构答案本身，而是让生命周期敏感的状态有稳定位置。">
  LifecycleOwner
</Term>
要服务团队协作，而不是只服务当前开发者的手感。

<Term def="一条能被 review、测试和文档共同保护的代码边界。">责任边界</Term>
是本章的验收线：边界画不清，模式名再漂亮也没用。

## 常见误区

<Callout type="trap">
  <strong>现象 →</strong> AAC 等于架构，导致 LiveData 随便跨层传，边界反而更乱。{" "}
  <strong>原因 →</strong> 只讨论实现，没有讨论职责边界。 <strong>修法 →</strong>{" "}
  回到图里，把事件、状态、结果和失败路径都标出来。
</Callout>

<Callout type="trap">
  <strong>现象 →</strong> Stepper 能播放，但讲不出本章故事。{" "}
  <strong>原因 →</strong> 图只是高亮控件，没有绑定真实协作场景。{" "}
  <strong>修法 →</strong> 用“谁做决定、谁能测试、谁负责展示”重讲一遍。
</Callout>

## 小结与练习

- 本章核心：AAC 不是架构答案本身，而是让生命周期敏感的状态有稳定位置。
- 本章图型：lifecycle，重点观察职责边界如何移动。
- 本章练习：画出旋转屏幕后 ViewModel 保留、View 重建、Observer 重新订阅的流程。

<Exercises>
  <Answer question="本章 Stepper 的 4 步分别在解决什么协作问题？">
    打开页面并订阅数据 → 旋转屏幕后 View 销毁重建 → ViewModel 保留状态 →
    Observer 重新订阅并恢复 UI。回答时要指出每一步的负责人和输出物。
  </Answer>
  <Answer question="本章最危险的误用是什么？">
    AAC 等于架构，导致 LiveData 随便跨层传，边界反而更乱。
    修法是先画边界，再决定类、接口和测试替身。
  </Answer>
  <Answer question="画出旋转屏幕后 ViewModel 保留、View 重建、Observer 重新订阅的流程。">
    用一张小图标出输入、判断、状态、输出和失败路径；再写一句 review
    规则保护这条边界。
  </Answer>
</Exercises>

## 名词解释

<Glossary>
  <GlossaryItem term="LifecycleOwner">
    LifecycleOwner 是 Android Architecture Components
    的关键概念；要能在图里指出它负责哪条协作边界。
  </GlossaryItem>
  <GlossaryItem term="ViewModelStore">
    ViewModelStore 是 Android Architecture Components
    的关键概念；要能在图里指出它负责哪条协作边界。
  </GlossaryItem>
  <GlossaryItem term="Observer">
    Observer 是 Android Architecture Components
    的关键概念；要能在图里指出它负责哪条协作边界。
  </GlossaryItem>
  <GlossaryItem term="SavedState">
    SavedState 是 Android Architecture Components
    的关键概念；要能在图里指出它负责哪条协作边界。
  </GlossaryItem>
  <GlossaryItem term="责任边界">
    责任边界是代码中可以被讨论、测试、review 和文档共同保护的分界线。
  </GlossaryItem>
</Glossary>

<Attribution
  adaptedFrom="Android アプリ設計パターン入門"
  adaptedUrl="https://peaks.cc/architecture_patterns"
/>
