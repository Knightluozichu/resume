---
title: "第8章 图形"
description: "依据Apress/O'Reilly与中文版完整目录覆盖20个节点：从布局优化与层级工具进入OpenGL ES扩展、纹理压缩、Mipmap、多APK、着色、场景复杂度、消隐与功耗"
type: C
section: "Android应用性能优化（2012 / Android 4.0时代）"
draft: true
math: false
sourceUrl: "https://www.oreilly.com/library/view/pro-android-apps/9781430239994/"
demo: true
order: 0
---

# 第8章 图形

本页依据Hervé Guihot《Pro Android Apps Performance Optimization》独立重构，不复制原文。原版锁定Apress 2012年1月版、282页、ISBN 9781430239994；中文版锁定人民邮电出版社2012年10月第1版、白龙译、226页、ISBN 9787115272416。

版本语境是Dalvik、Eclipse/ADT、Android 3.x至4.0、早期NDK与ARM/NEON，包含AsyncTask、TraceView、DDMS、layoutopt和RenderScript。课程先解释历史机制，再以ART、Benchmark、Perfetto、现代NDK与替代计算API建立迁移账本；不会把新工具倒填成原书目录。

<Objectives>

- 能解释“第8章 图形”全部20个正式节点的输入、正确性、执行边界、资源成本与2012年版本限制。
- 能实现“从布局优化与层级工具进入OpenGL ES扩展、纹理压缩、Mipmap、多APK、着色、场景复杂度、消隐与功耗”的最小可重复实验，并保存源码、构建、设备、样本和原始测量。
- 能比较基线、优化、错误输入、设备差异和生命周期故障，分析“只追求更少View或更高纹理质量，忽略设备扩展、显存、填充率、场景复杂度与图形功耗的联合约束”。
- 能设计反例并凭布局层级、测绘制时间、纹理格式与显存、设备扩展、多APK选择、GPU帧与功耗对照完成独立复现与交接。

</Objectives>

## 从正确性与测量协议开始

<Term def="在相同输入、环境与可观察结果下代表优化前行为和资源成本的参照实现">性能基线</Term>先于技巧。优化不能改变业务结果、线程安全、精度、生命周期或错误语义。每个实验先写输出断言和允许误差，再决定测量墙钟时间、CPU时间、分配、GC、唤醒、网络或帧；只取最快一次会系统性夸大收益。

<Term def="规定设备状态、输入规模、预热、采样、统计和失败判据的可复现实验合同">测量协议</Term>要控制构建类型、Android版本、ABI、CPU调频、温度、电量和后台负载。<Term def="Java经JNI进入C/C++并承担数据转换、引用、异常、线程附着与ABI兼容的接口">原生边界</Term>有固定成本，只有热点工作量足够大时，原生计算收益才可能覆盖跨界开销。

内存优化依赖<Term def="让连续计算访问相邻数据并减少缓存未命中和不必要对象分配的布局性质">内存局部性</Term>，不只是“少用内存”。电池优化依赖<Term def="一次功能允许消耗的CPU、无线电、定位、传感器、图形与唤醒总成本">能耗预算</Term>，减少一段CPU时间却增加网络唤醒并不一定更省电。

本单元主线是从布局优化与层级工具进入OpenGL ES扩展、纹理压缩、Mipmap、多APK、着色、场景复杂度、消隐与功耗。交互管线连接Java、JNI/NDK、内存线程、设备图形与用户结果；实验面板切换基线、优化、错误结果、热状态和生命周期；证据门要求正确性、时间、资源、设备和版本全部可重放。

<Apo12PipelineLab />

## 权威目录逐节点映射

### 第8章 图形

**正式节点 1/20。** “第8章 图形”必须放进“从布局优化与层级工具进入OpenGL ES扩展、纹理压缩、Mipmap、多APK、着色、场景复杂度、消隐与功耗”的性能因果链：先固定输入与正确输出，再区分Java、JNI、原生、内存、线程、设备和图形边界，最后选择时间、分配、唤醒或帧证据。没有等价性测试和基线，任何更快数字都不能算优化。

为该节点准备未优化、优化和反例三组实现。固定设备、Android版本、ABI、CPU状态、构建类型与数据规模，完成预热和重复采样；记录原始结果、资源释放与回滚。现代工具可以重测，但必须单列与2012年机制的差异。

### 8.1 布局优化

**正式节点 2/20。** “8.1 布局优化”必须放进“从布局优化与层级工具进入OpenGL ES扩展、纹理压缩、Mipmap、多APK、着色、场景复杂度、消隐与功耗”的性能因果链：先固定输入与正确输出，再区分Java、JNI、原生、内存、线程、设备和图形边界，最后选择时间、分配、唤醒或帧证据。没有等价性测试和基线，任何更快数字都不能算优化。

为该节点准备未优化、优化和反例三组实现。固定设备、Android版本、ABI、CPU状态、构建类型与数据规模，完成预热和重复采样；记录原始结果、资源释放与回滚。现代工具可以重测，但必须单列与2012年机制的差异。

### 8.1.1 相对布局

**正式节点 3/20。** “8.1.1 相对布局”必须放进“从布局优化与层级工具进入OpenGL ES扩展、纹理压缩、Mipmap、多APK、着色、场景复杂度、消隐与功耗”的性能因果链：先固定输入与正确输出，再区分Java、JNI、原生、内存、线程、设备和图形边界，最后选择时间、分配、唤醒或帧证据。没有等价性测试和基线，任何更快数字都不能算优化。

为该节点准备未优化、优化和反例三组实现。固定设备、Android版本、ABI、CPU状态、构建类型与数据规模，完成预热和重复采样；记录原始结果、资源释放与回滚。现代工具可以重测，但必须单列与2012年机制的差异。

### 8.1.2 合并布局

**正式节点 4/20。** “8.1.2 合并布局”必须放进“从布局优化与层级工具进入OpenGL ES扩展、纹理压缩、Mipmap、多APK、着色、场景复杂度、消隐与功耗”的性能因果链：先固定输入与正确输出，再区分Java、JNI、原生、内存、线程、设备和图形边界，最后选择时间、分配、唤醒或帧证据。没有等价性测试和基线，任何更快数字都不能算优化。

为该节点准备未优化、优化和反例三组实现。固定设备、Android版本、ABI、CPU状态、构建类型与数据规模，完成预热和重复采样；记录原始结果、资源释放与回滚。现代工具可以重测，但必须单列与2012年机制的差异。

### 8.1.3 重用布局

**正式节点 5/20。** “8.1.3 重用布局”必须放进“从布局优化与层级工具进入OpenGL ES扩展、纹理压缩、Mipmap、多APK、着色、场景复杂度、消隐与功耗”的性能因果链：先固定输入与正确输出，再区分Java、JNI、原生、内存、线程、设备和图形边界，最后选择时间、分配、唤醒或帧证据。没有等价性测试和基线，任何更快数字都不能算优化。

为该节点准备未优化、优化和反例三组实现。固定设备、Android版本、ABI、CPU状态、构建类型与数据规模，完成预热和重复采样；记录原始结果、资源释放与回滚。现代工具可以重测，但必须单列与2012年机制的差异。

### 8.1.4 ViewStub

**正式节点 6/20。** “8.1.4 ViewStub”必须放进“从布局优化与层级工具进入OpenGL ES扩展、纹理压缩、Mipmap、多APK、着色、场景复杂度、消隐与功耗”的性能因果链：先固定输入与正确输出，再区分Java、JNI、原生、内存、线程、设备和图形边界，最后选择时间、分配、唤醒或帧证据。没有等价性测试和基线，任何更快数字都不能算优化。

为该节点准备未优化、优化和反例三组实现。固定设备、Android版本、ABI、CPU状态、构建类型与数据规模，完成预热和重复采样；记录原始结果、资源释放与回滚。现代工具可以重测，但必须单列与2012年机制的差异。

### 8.2 布局工具

**正式节点 7/20。** “8.2 布局工具”必须放进“从布局优化与层级工具进入OpenGL ES扩展、纹理压缩、Mipmap、多APK、着色、场景复杂度、消隐与功耗”的性能因果链：先固定输入与正确输出，再区分Java、JNI、原生、内存、线程、设备和图形边界，最后选择时间、分配、唤醒或帧证据。没有等价性测试和基线，任何更快数字都不能算优化。

为该节点准备未优化、优化和反例三组实现。固定设备、Android版本、ABI、CPU状态、构建类型与数据规模，完成预热和重复采样；记录原始结果、资源释放与回滚。现代工具可以重测，但必须单列与2012年机制的差异。

### 8.2.1 层级视图

**正式节点 8/20。** “8.2.1 层级视图”必须放进“从布局优化与层级工具进入OpenGL ES扩展、纹理压缩、Mipmap、多APK、着色、场景复杂度、消隐与功耗”的性能因果链：先固定输入与正确输出，再区分Java、JNI、原生、内存、线程、设备和图形边界，最后选择时间、分配、唤醒或帧证据。没有等价性测试和基线，任何更快数字都不能算优化。

为该节点准备未优化、优化和反例三组实现。固定设备、Android版本、ABI、CPU状态、构建类型与数据规模，完成预热和重复采样；记录原始结果、资源释放与回滚。现代工具可以重测，但必须单列与2012年机制的差异。

### 8.2.2 layoutopt

**正式节点 9/20。** “8.2.2 layoutopt”必须放进“从布局优化与层级工具进入OpenGL ES扩展、纹理压缩、Mipmap、多APK、着色、场景复杂度、消隐与功耗”的性能因果链：先固定输入与正确输出，再区分Java、JNI、原生、内存、线程、设备和图形边界，最后选择时间、分配、唤醒或帧证据。没有等价性测试和基线，任何更快数字都不能算优化。

为该节点准备未优化、优化和反例三组实现。固定设备、Android版本、ABI、CPU状态、构建类型与数据规模，完成预热和重复采样；记录原始结果、资源释放与回滚。现代工具可以重测，但必须单列与2012年机制的差异。

### 8.3 OpenGL ES

**正式节点 10/20。** “8.3 OpenGL ES”必须放进“从布局优化与层级工具进入OpenGL ES扩展、纹理压缩、Mipmap、多APK、着色、场景复杂度、消隐与功耗”的性能因果链：先固定输入与正确输出，再区分Java、JNI、原生、内存、线程、设备和图形边界，最后选择时间、分配、唤醒或帧证据。没有等价性测试和基线，任何更快数字都不能算优化。

为该节点准备未优化、优化和反例三组实现。固定设备、Android版本、ABI、CPU状态、构建类型与数据规模，完成预热和重复采样；记录原始结果、资源释放与回滚。现代工具可以重测，但必须单列与2012年机制的差异。

### 8.3.1 扩展

**正式节点 11/20。** “8.3.1 扩展”必须放进“从布局优化与层级工具进入OpenGL ES扩展、纹理压缩、Mipmap、多APK、着色、场景复杂度、消隐与功耗”的性能因果链：先固定输入与正确输出，再区分Java、JNI、原生、内存、线程、设备和图形边界，最后选择时间、分配、唤醒或帧证据。没有等价性测试和基线，任何更快数字都不能算优化。

为该节点准备未优化、优化和反例三组实现。固定设备、Android版本、ABI、CPU状态、构建类型与数据规模，完成预热和重复采样；记录原始结果、资源释放与回滚。现代工具可以重测，但必须单列与2012年机制的差异。

### 8.3.2 纹理压缩

**正式节点 12/20。** “8.3.2 纹理压缩”必须放进“从布局优化与层级工具进入OpenGL ES扩展、纹理压缩、Mipmap、多APK、着色、场景复杂度、消隐与功耗”的性能因果链：先固定输入与正确输出，再区分Java、JNI、原生、内存、线程、设备和图形边界，最后选择时间、分配、唤醒或帧证据。没有等价性测试和基线，任何更快数字都不能算优化。

为该节点准备未优化、优化和反例三组实现。固定设备、Android版本、ABI、CPU状态、构建类型与数据规模，完成预热和重复采样；记录原始结果、资源释放与回滚。现代工具可以重测，但必须单列与2012年机制的差异。

### 8.3.3 Mipmap

**正式节点 13/20。** “8.3.3 Mipmap”必须放进“从布局优化与层级工具进入OpenGL ES扩展、纹理压缩、Mipmap、多APK、着色、场景复杂度、消隐与功耗”的性能因果链：先固定输入与正确输出，再区分Java、JNI、原生、内存、线程、设备和图形边界，最后选择时间、分配、唤醒或帧证据。没有等价性测试和基线，任何更快数字都不能算优化。

为该节点准备未优化、优化和反例三组实现。固定设备、Android版本、ABI、CPU状态、构建类型与数据规模，完成预热和重复采样；记录原始结果、资源释放与回滚。现代工具可以重测，但必须单列与2012年机制的差异。

### 8.3.4 多APK

**正式节点 14/20。** “8.3.4 多APK”必须放进“从布局优化与层级工具进入OpenGL ES扩展、纹理压缩、Mipmap、多APK、着色、场景复杂度、消隐与功耗”的性能因果链：先固定输入与正确输出，再区分Java、JNI、原生、内存、线程、设备和图形边界，最后选择时间、分配、唤醒或帧证据。没有等价性测试和基线，任何更快数字都不能算优化。

为该节点准备未优化、优化和反例三组实现。固定设备、Android版本、ABI、CPU状态、构建类型与数据规模，完成预热和重复采样；记录原始结果、资源释放与回滚。现代工具可以重测，但必须单列与2012年机制的差异。

### 8.3.5 着色

**正式节点 15/20。** “8.3.5 着色”必须放进“从布局优化与层级工具进入OpenGL ES扩展、纹理压缩、Mipmap、多APK、着色、场景复杂度、消隐与功耗”的性能因果链：先固定输入与正确输出，再区分Java、JNI、原生、内存、线程、设备和图形边界，最后选择时间、分配、唤醒或帧证据。没有等价性测试和基线，任何更快数字都不能算优化。

为该节点准备未优化、优化和反例三组实现。固定设备、Android版本、ABI、CPU状态、构建类型与数据规模，完成预热和重复采样；记录原始结果、资源释放与回滚。现代工具可以重测，但必须单列与2012年机制的差异。

### 8.3.6 场景复杂性

**正式节点 16/20。** “8.3.6 场景复杂性”必须放进“从布局优化与层级工具进入OpenGL ES扩展、纹理压缩、Mipmap、多APK、着色、场景复杂度、消隐与功耗”的性能因果链：先固定输入与正确输出，再区分Java、JNI、原生、内存、线程、设备和图形边界，最后选择时间、分配、唤醒或帧证据。没有等价性测试和基线，任何更快数字都不能算优化。

为该节点准备未优化、优化和反例三组实现。固定设备、Android版本、ABI、CPU状态、构建类型与数据规模，完成预热和重复采样；记录原始结果、资源释放与回滚。现代工具可以重测，但必须单列与2012年机制的差异。

### 8.3.7 消隐

**正式节点 17/20。** “8.3.7 消隐”必须放进“从布局优化与层级工具进入OpenGL ES扩展、纹理压缩、Mipmap、多APK、着色、场景复杂度、消隐与功耗”的性能因果链：先固定输入与正确输出，再区分Java、JNI、原生、内存、线程、设备和图形边界，最后选择时间、分配、唤醒或帧证据。没有等价性测试和基线，任何更快数字都不能算优化。

为该节点准备未优化、优化和反例三组实现。固定设备、Android版本、ABI、CPU状态、构建类型与数据规模，完成预热和重复采样；记录原始结果、资源释放与回滚。现代工具可以重测，但必须单列与2012年机制的差异。

### 8.3.8 渲染模式

**正式节点 18/20。** “8.3.8 渲染模式”必须放进“从布局优化与层级工具进入OpenGL ES扩展、纹理压缩、Mipmap、多APK、着色、场景复杂度、消隐与功耗”的性能因果链：先固定输入与正确输出，再区分Java、JNI、原生、内存、线程、设备和图形边界，最后选择时间、分配、唤醒或帧证据。没有等价性测试和基线，任何更快数字都不能算优化。

为该节点准备未优化、优化和反例三组实现。固定设备、Android版本、ABI、CPU状态、构建类型与数据规模，完成预热和重复采样；记录原始结果、资源释放与回滚。现代工具可以重测，但必须单列与2012年机制的差异。

### 8.3.9 功耗管理

**正式节点 19/20。** “8.3.9 功耗管理”必须放进“从布局优化与层级工具进入OpenGL ES扩展、纹理压缩、Mipmap、多APK、着色、场景复杂度、消隐与功耗”的性能因果链：先固定输入与正确输出，再区分Java、JNI、原生、内存、线程、设备和图形边界，最后选择时间、分配、唤醒或帧证据。没有等价性测试和基线，任何更快数字都不能算优化。

为该节点准备未优化、优化和反例三组实现。固定设备、Android版本、ABI、CPU状态、构建类型与数据规模，完成预热和重复采样；记录原始结果、资源释放与回滚。现代工具可以重测，但必须单列与2012年机制的差异。

### 8.4 总结

**正式节点 20/20。** “8.4 总结”必须放进“从布局优化与层级工具进入OpenGL ES扩展、纹理压缩、Mipmap、多APK、着色、场景复杂度、消隐与功耗”的性能因果链：先固定输入与正确输出，再区分Java、JNI、原生、内存、线程、设备和图形边界，最后选择时间、分配、唤醒或帧证据。没有等价性测试和基线，任何更快数字都不能算优化。

为该节点准备未优化、优化和反例三组实现。固定设备、Android版本、ABI、CPU状态、构建类型与数据规模，完成预热和重复采样；记录原始结果、资源释放与回滚。现代工具可以重测，但必须单列与2012年机制的差异。

<Callout type="trap">**本章核心陷阱。** 只追求更少View或更高纹理质量，忽略设备扩展、显存、填充率、场景复杂度与图形功耗的联合约束。修复时先恢复等价性、测量、资源与生命周期合同，再写能推翻优化的测试。</Callout>

<Callout type="trap">**历史机制错位。** 原书处在Dalvik、早期NDK和Android 4.0时代。现代Profiler、Perfetto或替代API只能作迁移对照，不能删除NDK、OpenGL ES或RenderScript目录。</Callout>

<Callout type="warning">性能与电量实验只使用合成数据和自有设备。日志、trace、转储、数据库和原生崩溃文件不得包含真实账号、令牌或用户内容。</Callout>

## 本章回顾

本页从“第8章 图形”覆盖到“8.4 总结”，共20个正式节点。掌握标准是能沿“从布局优化与层级工具进入OpenGL ES扩展、纹理压缩、Mipmap、多APK、着色、场景复杂度、消隐与功耗”解释执行与资源因果链，运行等价、边界和故障实验，并让另一位开发者凭布局层级、测绘制时间、纹理格式与显存、设备扩展、多APK选择、GPU帧与功耗对照重放结论。

<Exercises>

**问题 1**：“第8章 图形”覆盖哪些正式节点与性能主线？
<Answer>覆盖20个节点，从“第8章 图形”到“8.4 总结”；主线是从布局优化与层级工具进入OpenGL ES扩展、纹理压缩、Mipmap、多APK、着色、场景复杂度、消隐与功耗。</Answer>

**问题 2**：怎样建立本章最小可重复实验？
<Answer>固定输入、正确输出、Release构建、设备、Android版本、ABI、电源和热状态，预热并重复交错采样，保存布局层级、测绘制时间、纹理格式与显存、设备扩展、多APK选择、GPU帧与功耗对照。</Answer>

**问题 3**：本章最需要推翻的错误假设是什么？
<Answer>错误假设是“只追求更少View或更高纹理质量，忽略设备扩展、显存、填充率、场景复杂度与图形功耗的联合约束”。只改变算法、边界、线程、设备能力、生命周期或测量方式之一，结果或资源偏离即推翻。</Answer>

**问题 4**：为什么一次更快数字不能证明优化？
<Answer>它没有证明结果等价、预热、样本分布、设备状态、跟踪开销、尾延迟与资源释放；必须保存原始样本和失败断言。</Answer>

**问题 5**：怎样迁移到现代Android而不改写原书？
<Answer>先保存2012年Dalvik、NDK、TraceView、AsyncTask和RenderScript机制，再一次只改变运行时、工具或API之一并比较行为。</Answer>

**问题 6**：达到独立交接标准需要什么？
<Answer>需要源码与输入、正确性断言、构建与设备指纹、预热采样协议、时间/内存/线程/能耗/图形证据、失败测试及布局层级、测绘制时间、纹理格式与显存、设备扩展、多APK选择、GPU帧与功耗对照。</Answer>

</Exercises>

<Glossary>
  <GlossaryItem term="性能基线">在相同输入、环境与可观察结果下代表优化前行为和资源成本的参照实现。</GlossaryItem>
  <GlossaryItem term="测量协议">规定设备状态、输入规模、预热、采样、统计和失败判据的可复现实验合同。</GlossaryItem>
  <GlossaryItem term="原生边界">Java经JNI进入C/C++并承担数据转换、引用、异常、线程附着与ABI兼容的接口。</GlossaryItem>
  <GlossaryItem term="内存局部性">让连续计算访问相邻数据并减少缓存未命中和不必要对象分配的布局性质。</GlossaryItem>
  <GlossaryItem term="能耗预算">一次功能允许消耗的CPU、无线电、定位、传感器、图形与唤醒总成本。</GlossaryItem>
</Glossary>

[← 上一页：第7章 延长电池续航时间](/learn/android-perf-optimization/07-battery/apo12-07-maximizing-battery-life) · [下一页：第9章 RenderScript →](/learn/android-perf-optimization/09-renderscript/apo12-09-renderscript)

<Attribution adaptedFrom="Hervé Guihot《Pro Android Apps Performance Optimization》" adaptedUrl="https://www.oreilly.com/library/view/pro-android-apps/9781430239994/" />
