---
title: 什么是架构
type: A
section: 架构入门
order: 1
description: 架构的定义、架构与设计的区别、架构决策的影响范围与架构师的角色。
demo: true
math: false
sourceUrl: >-
  https://www.pearson.com/en-gb/subject-catalog/p/domain-driven-design-tackling-complexity-in-the-heart-of-software/P200000009375
draft: false
---

import { AddArchitectureVsDesignDiagram } from "@/components/mdx/architecture-domain/diagrams/add-architecture-vs-design";
import {
  OfficialArchitectureDomainDesignWhatIsArchitectureMapLab,
  OfficialArchitectureDomainDesignWhatIsArchitectureExperimentLab,
  OfficialArchitectureDomainDesignWhatIsArchitectureEvidenceLab,
} from "@/components/mdx/architecture-domain-design/diagrams/what-is-architecture";
import {
  Objectives,
  Callout,
  Attribution,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
} from "@/components/mdx/mdx-components";

{/* OFFICIAL_COURSE_ENHANCEMENT */}

<Objectives>

- 能解释什么是架构的输入、核心结构、状态变化和输出合同
- 能绘制并修改什么是架构的最小模型，比较至少一种替代方案
- 能复现正常、边界和单故障样本，定位第一条证据分叉
- 能根据成本与风险判断方案适用范围，并完成清理重建

</Objectives>

## 为什么只记结论不足以掌握什么是架构

学习“什么是架构”时，第一步不是记住结论，而是冻结输入、上下文、版本和成功标准。只有这些条件明确，什么是架构的含义才不会随着样例变化。正文已有的概念说明要与结构图、运行轨迹和失败样本互相印证，不能只凭最终输出看似正确就宣布完成。

结构分析从核心概念开始：列出参与者、职责、连接方向、生命周期和所有权，再沿正常路径追踪数据或控制流。每一条边都要说明为什么存在、谁创建、谁消费、谁负责清理；如果边界被跨越，必须能在证据中找到第一处异常。

机制验证要把常见误区写成可以执行的条件。正常样本证明主路径，恰好边界样本验证等号和空值，单故障样本只破坏一个假设。三类样本使用同一份观察指标，避免因为测试口径变化而把偶然结果误认成规律。

成本分析同时记录时间、空间、延迟、耦合、可维护性和不可逆操作。小结与练习不是一句“可能失败”，而是可复现输入、预期停点、实际轨迹、错误分类与清理步骤。任何自动重试都要有次数、预算和幂等边界。

方案比较不能只列优点。需要给出直接实现、当前方案和至少一个替代方案，逐项比较复杂度、扩展点、故障隔离和团队认知成本。当问题规模很小或变化轴稳定时，更简单的实现往往更好；模式与框架必须由真实变化压力证明。

实现阶段把大结论拆成可检查的中间产物：配置快照、结构清单、状态转移、输入输出样本、日志摘要和测试结果。每个产物带来源与生成命令，下一阶段只消费已通过门禁的版本，避免旧缓存或隐式默认值污染结论。

解释结果时必须区分相关性与因果性、接口承诺与实现细节、设计意图与运行事实。对“名词解释”的判断要由独立证据支持，并明确适用范围；一旦输入分布、版本、硬件或组织边界改变，就重新运行最小实验。

复盘从首个分叉开始，而不是从最后一个报错倒推。先比较冻结输入，再比较第一份结构化中间产物，随后检查状态、约束和副作用。这样可以把复杂系统的排错范围收缩到一个阶段，避免在多个层次同时修改造成新的不确定性。

迁移到真实项目时，先选择一个最小但有代表性的切片，保存改造前基线，再逐步引入“什么是架构”中的机制。每一步只改变一个变量并保留回滚点；性能、正确性、安全性和可理解性至少各有一项可量化指标。

最终验收要求读者能脱离页面重新画出结构、口述关键链路、实现最小版本、构造一个反例并解释失败位置。若只能复述名词而不能预测中间状态，说明知识仍停留在识记层，需要回到图示和实验重新验证。

本页用<Term def="什么是架构连接什么是架构的机制、边界与可复查证据。">什么是架构</Term>、

<Term def="核心概念连接什么是架构的机制、边界与可复查证据。">核心概念</Term>、<Term def="常见误区连接什么是架构的机制、边界与可复查证据。">常见误区</Term>、<Term def="小结与练习连接什么是架构的机制、边界与可复查证据。">小结与练习</Term>、<Term def="名词解释连接什么是架构的机制、边界与可复查证据。">名词解释</Term>
建立统一坐标。先预测这些概念在结构图和运行轨迹中的位置，再操作实验控件；如果结果与预测不一致，停止在首个分叉，不要用后续补丁掩盖早期错误。

## 权威目录与核心概念逐项对照

- 什么是架构
- 核心概念
- 常见误区
- 小结与练习
- 名词解释

## 三视图实验：结构、变量与证据

<Stepper>
  <Step title="第一步：冻结结构并先预测">
    先预测参与者、连接方向与关键状态，再点击结构节点核对。
    <OfficialArchitectureDomainDesignWhatIsArchitectureMapLab />
  </Step>
  <Step title="第二步：只改变一个变量">
    保持其他条件不变，移动覆盖率或风险滑块，观察门禁变化。
    <OfficialArchitectureDomainDesignWhatIsArchitectureExperimentLab />
  </Step>
  <Step title="第三步：注入单故障并保存证据">
    注入一个边界故障，记录第一处偏离、拒绝原因和恢复条件。
    <OfficialArchitectureDomainDesignWhatIsArchitectureEvidenceLab />
  </Step>
</Stepper>

<Callout type="trap">
  <strong>误区：</strong>
  看到最终结果正确就认为机制正确。中间状态可能已经偏离，只是被后续默认值或重试偶然修补。应保存首个结构化产物并逐阶段比较。
</Callout>

<Callout type="trap">
  <strong>误区：</strong>
  为了使用模式或框架而制造抽象。若变化轴、失败边界和替代方案没有写清，新增层次只会扩大认知成本和故障面。
</Callout>

## 可复现的最小实现

先把决策记录写成机器可读结构：

```json
{
  "unit": "什么是架构",
  "inputFrozen": true,
  "scenario": "normal | boundary | single-fault",
  "firstDivergence": null,
  "cleanupRequired": true
}
```

再用同一条执行链处理三类样本：

```ts
type Evidence = { stage: string; expected: string; actual: string };

function verify(sample: unknown, expected: readonly Evidence[]) {
  const trace = runFromCleanState(sample);
  return expected.find((item, index) => trace[index]?.actual !== item.expected);
}
```

最后保存回归门禁，禁止失败样本静默通过：

```text
normal      -> complete, invariant preserved
boundary    -> complete or explicit rejection
singleFault -> stop at first divergence, no stale output
```

用统一评分解释实验结果：

$$
Q = C_{correct} + C_{trace} + C_{recover} - R_{residual}
$$

$$
C_{coverage} = \frac{N_{verified}}{N_{official}}
$$

$$
R_{residual} = P(failure) \times I(impact)
$$

$$
Accept = (C_{coverage} \ge 0.90) \land (R_{residual} \le R_{budget})
$$

## 本章回顾

- 什么是架构必须绑定冻结输入和明确成功标准。
- 核心概念必须能画成结构并沿边追踪责任。
- 常见误区要由正常、边界和单故障样本共同验证。
- 小结与练习必须保存第一处偏离和清理重建步骤。
- 名词解释决定方案是否可以进入下一阶段。

<Exercises>

**问题 1**：为“什么是架构”写出最小输入合同，并指出一个不能省略的边界条件。

<Answer>
  输入合同至少包含样本身份、版本、配置、成功标准和清理方式；边界条件必须能改变结构或状态，而不是只换一段描述。
</Answer>

**问题 2**：构造一个只破坏“常见误区”的单故障样本，第一条预期证据是什么？

<Answer>
  先冻结正常轨迹，只改变该条件；第一条证据应是最早依赖它的结构化中间产物，而不是最终错误消息。
</Answer>

**问题 3**：什么情况下不应采用本页方案？

<Answer>
  当变化轴稳定、直接实现更清晰、额外层次无法降低风险或提供独立测试边界时，应选择更简单方案并记录重新评估触发条件。
</Answer>

</Exercises>

## 术语表

<Glossary>
  <GlossaryItem term="什么是架构">
    什么是架构用于描述“什么是架构”中的输入、结构、约束或证据边界，必须能在实验记录中定位。
  </GlossaryItem>
  <GlossaryItem term="核心概念">
    核心概念用于描述“什么是架构”中的输入、结构、约束或证据边界，必须能在实验记录中定位。
  </GlossaryItem>
  <GlossaryItem term="常见误区">
    常见误区用于描述“什么是架构”中的输入、结构、约束或证据边界，必须能在实验记录中定位。
  </GlossaryItem>
  <GlossaryItem term="小结与练习">
    小结与练习用于描述“什么是架构”中的输入、结构、约束或证据边界，必须能在实验记录中定位。
  </GlossaryItem>
  <GlossaryItem term="名词解释">
    名词解释用于描述“什么是架构”中的输入、结构、约束或证据边界，必须能在实验记录中定位。
  </GlossaryItem>
</Glossary>

<Attribution
  adaptedFrom="站内双书专题《架构与领域设计》，依据Eric Evans《Domain-Driven Design》（2003）与Robert C. Martin《Clean Architecture》（2017） · 独立教学重构"
  adaptedUrl="https://www.pearson.com/en-gb/subject-catalog/p/domain-driven-design-tackling-complexity-in-the-heart-of-software/P200000009375"
/>

<Objectives>

- 能区分架构决策与设计决策，指出二者在影响范围上的差别
- 能说出架构关注的三件事：边界、依赖、组件关系
- 能回答：为什么架构决策难以修改，它和设计决策的根本差别在哪？

</Objectives>

## 什么是架构

架构这个词被用得太滥，以至于有必要先把它讲准。<Term def="系统的高层结构决策，规定组件如何划分、组件之间如何依赖、边界划在哪里，影响整个系统而非单个模块。">软件架构</Term>是那些「昂贵且难以更改」的决策集合——选哪种通信协议、把系统切成哪几个子系统、依赖方向朝哪边指。它不是某段代码长什么样，而是这些代码被组织成什么形状。

<AddArchitectureVsDesignDiagram />

架构与设计的分界线不在「高层 vs 低层」，而在「影响范围」。架构决策是系统级的：一旦定下「前端调后端、后端调数据库」这条依赖链，它就约束了成百上千个模块的协作方式。而设计决策是模块级的：某个类用策略模式还是工厂模式，只影响这一个模块内部。换掉一个设计模式只需重构一个文件；换掉一条架构依赖方向，则要撬动整个系统。

## 核心概念

架构真正关注三件事：**边界、依赖、组件关系**。<Term def="把系统切成若干可独立演化的部分的那条线，边界两侧的变化互不传染。">边界</Term>回答「在哪里切开」——把容易变动的部分和稳定的部分隔开，让前者的变化不传染后者。依赖回答「谁指向谁」——稳定的业务核心被依赖，易变的技术细节去依赖别人。组件关系回答「切开的各部分如何协作」——跨边界通信用什么方式，是直接调用还是通过接口。

<Term def="改变它需要牵动整个系统而非单个模块的决策，因此代价高昂、难以回退。">架构决策</Term>
之所以难以修改，正因为它是系统级的。一条依赖方向一旦定错，纠正它意味着同时改动上下游所有遵守旧方向的代码——这种「牵一发动全身」正是架构与设计的根本差别。架构师的角色因此不是画图者，而是决策者：他的核心产出不是
UML 图，而是这些「一旦定下就难以反悔」的取舍。

<Callout type="tip">
  <strong>建议 →</strong>{" "}
  判断一个决策算架构还是设计，问自己两个问题：它影响的是整个系统还是单个模块？改它的代价是撬动全局还是重构一处？前者是架构，后者是设计。
</Callout>

## 常见误区

<Callout type="trap">
  <strong>现象 →</strong>{" "}
  「架构就是画几张高层图，画完就交给开发去写代码。」结果图上的依赖方向没人遵守，系统慢慢长成大泥球。
  <strong>原因 →</strong>{" "}
  把架构当成了「产出文档」而非「做出并守护决策」。图画得再漂亮，依赖方向没被代码遵守就等于零。
  <strong>修法 →</strong> 架构师的产出是决策本身及其持续守护——通过 code
  review、依赖检查工具确保边界不被越过，而非画完图就撒手。
</Callout>

<Callout type="trap">
  <strong>现象 →</strong>{" "}
  「这个类用单例还是工厂？这是架构问题。」团队为这种小事开架构评审会，真正该定的系统级依赖方向却没人管。
  <strong>原因 →</strong> 把模块级的设计决策误当成系统级的架构决策，本末倒置。
  <strong>修法 →</strong>{" "}
  用影响范围划分：只影响单个模块的是设计，自己决定；约束整个系统协作方式的是架构，才需要评审。
</Callout>

## 小结与练习

- 架构 = 系统级的高层结构决策集合，是「昂贵且难以更改」的决策，不是某段代码的写法
- 架构与设计的分界在影响范围：架构系统级、难改；设计模块级、可局部重构
- 架构关注三件事：边界（在哪切）、依赖（谁指向谁）、组件关系（如何协作）
- 架构师是决策者与守护者，不是画图者；产出是决策及其持续守护

<Exercises>
  <Answer question="架构决策和设计决策的根本差别是什么？请各举一例。">
    根本差别在影响范围与修改代价。架构决策是系统级的，改它要撬动整个系统——例如把「前端直连数据库」改成「前端调后端
    API、后端调数据库」，要动所有直接访问数据库的地方。设计决策是模块级的，改它只重构一处——例如把某个类从单例改成工厂，只影响这个模块内部。
  </Answer>
  <Answer question="为什么说架构决策难以修改？这和它「系统级」的性质有什么关系？">
    架构决策约束的是整个系统的协作方式。一旦定下一条依赖方向或边界，成百上千个模块都按它协作；要纠正它，就得同时改动所有遵守旧方向的上下游代码，这种「牵一发动全身」让代价高到几乎无法回退。正因为它系统级，改一处等于改全局，所以才难以修改。
  </Answer>
</Exercises>

## 名词解释

<Glossary>
  <GlossaryItem term="软件架构">
    系统的高层结构决策集合，规定组件如何划分、组件之间如何依赖、边界划在哪里。它影响整个系统而非单个模块，因此代价高昂、难以更改。
  </GlossaryItem>
  <GlossaryItem term="边界">
    把系统切成若干可独立演化部分的那条线，边界两侧的变化互不传染。架构的核心任务之一就是决定边界划在哪里。
  </GlossaryItem>
  <GlossaryItem term="架构决策">
    改变它需要牵动整个系统而非单个模块的决策，因此代价高昂、难以回退。例如依赖方向、子系统划分、通信协议的选择。
  </GlossaryItem>
</Glossary>

<Attribution
  adaptedFrom="Clean Architecture (Robert C. Martin) & Domain-Driven Design (Eric Evans)"
  adaptedUrl="https://www.oreilly.com/"
/>
