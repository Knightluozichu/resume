---
title: SOLID 原则回顾
type: A
section: 架构原则
order: 2
description: 从架构角度重新审视 SOLID 五原则，重点讲 DIP 如何连接到整洁架构。
demo: true
math: false
sourceUrl: >-
  https://www.pearson.com/en-gb/subject-catalog/p/domain-driven-design-tackling-complexity-in-the-heart-of-software/P200000009375
draft: false
---

import { AddSolidPrinciplesDiagram } from "@/components/mdx/architecture-domain/diagrams/add-solid-principles";
import {
  OfficialArchitectureDomainDesignSolidPrinciplesMapLab,
  OfficialArchitectureDomainDesignSolidPrinciplesExperimentLab,
  OfficialArchitectureDomainDesignSolidPrinciplesEvidenceLab,
} from "@/components/mdx/architecture-domain-design/diagrams/solid-principles";
import {
  Objectives,
  Callout,
  Attribution,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
} from "@/components/mdx/mdx-components";

{/* OFFICIAL_COURSE_ENHANCEMENT */}

<Objectives>

- 能解释SOLID 原则回顾的输入、核心结构、状态变化和输出合同
- 能绘制并修改SOLID 原则回顾的最小模型，比较至少一种替代方案
- 能复现正常、边界和单故障样本，定位第一条证据分叉
- 能根据成本与风险判断方案适用范围，并完成清理重建

</Objectives>

## 为什么只记结论不足以掌握SOLID 原则回顾

学习“SOLID 原则回顾”时，第一步不是记住结论，而是冻结输入、上下文、版本和成功标准。只有这些条件明确，SOLID 原则回顾的含义才不会随着样例变化。正文已有的概念说明要与结构图、运行轨迹和失败样本互相印证，不能只凭最终输出看似正确就宣布完成。

结构分析从核心概念开始：列出参与者、职责、连接方向、生命周期和所有权，再沿正常路径追踪数据或控制流。每一条边都要说明为什么存在、谁创建、谁消费、谁负责清理；如果边界被跨越，必须能在证据中找到第一处异常。

机制验证要把常见误区写成可以执行的条件。正常样本证明主路径，恰好边界样本验证等号和空值，单故障样本只破坏一个假设。三类样本使用同一份观察指标，避免因为测试口径变化而把偶然结果误认成规律。

成本分析同时记录时间、空间、延迟、耦合、可维护性和不可逆操作。小结与练习不是一句“可能失败”，而是可复现输入、预期停点、实际轨迹、错误分类与清理步骤。任何自动重试都要有次数、预算和幂等边界。

方案比较不能只列优点。需要给出直接实现、当前方案和至少一个替代方案，逐项比较复杂度、扩展点、故障隔离和团队认知成本。当问题规模很小或变化轴稳定时，更简单的实现往往更好；模式与框架必须由真实变化压力证明。

实现阶段把大结论拆成可检查的中间产物：配置快照、结构清单、状态转移、输入输出样本、日志摘要和测试结果。每个产物带来源与生成命令，下一阶段只消费已通过门禁的版本，避免旧缓存或隐式默认值污染结论。

解释结果时必须区分相关性与因果性、接口承诺与实现细节、设计意图与运行事实。对“名词解释”的判断要由独立证据支持，并明确适用范围；一旦输入分布、版本、硬件或组织边界改变，就重新运行最小实验。

复盘从首个分叉开始，而不是从最后一个报错倒推。先比较冻结输入，再比较第一份结构化中间产物，随后检查状态、约束和副作用。这样可以把复杂系统的排错范围收缩到一个阶段，避免在多个层次同时修改造成新的不确定性。

迁移到真实项目时，先选择一个最小但有代表性的切片，保存改造前基线，再逐步引入“SOLID 原则回顾”中的机制。每一步只改变一个变量并保留回滚点；性能、正确性、安全性和可理解性至少各有一项可量化指标。

最终验收要求读者能脱离页面重新画出结构、口述关键链路、实现最小版本、构造一个反例并解释失败位置。若只能复述名词而不能预测中间状态，说明知识仍停留在识记层，需要回到图示和实验重新验证。

本页用<Term def="SOLID 原则回顾连接SOLID 原则回顾的机制、边界与可复查证据。">SOLID 原则回顾</Term>、

<Term def="核心概念连接SOLID 原则回顾的机制、边界与可复查证据。">核心概念</Term>、<Term def="常见误区连接SOLID 原则回顾的机制、边界与可复查证据。">常见误区</Term>、<Term def="小结与练习连接SOLID 原则回顾的机制、边界与可复查证据。">小结与练习</Term>、<Term def="名词解释连接SOLID 原则回顾的机制、边界与可复查证据。">名词解释</Term>
建立统一坐标。先预测这些概念在结构图和运行轨迹中的位置，再操作实验控件；如果结果与预测不一致，停止在首个分叉，不要用后续补丁掩盖早期错误。

## 权威目录与核心概念逐项对照

- SOLID 原则回顾
- 核心概念
- 常见误区
- 小结与练习
- 名词解释

## 三视图实验：结构、变量与证据

<Stepper>
  <Step title="第一步：冻结结构并先预测">
    先预测参与者、连接方向与关键状态，再点击结构节点核对。
    <OfficialArchitectureDomainDesignSolidPrinciplesMapLab />
  </Step>
  <Step title="第二步：只改变一个变量">
    保持其他条件不变，移动覆盖率或风险滑块，观察门禁变化。
    <OfficialArchitectureDomainDesignSolidPrinciplesExperimentLab />
  </Step>
  <Step title="第三步：注入单故障并保存证据">
    注入一个边界故障，记录第一处偏离、拒绝原因和恢复条件。
    <OfficialArchitectureDomainDesignSolidPrinciplesEvidenceLab />
  </Step>
</Stepper>

<Callout type="trap">
  <strong>误区：</strong>
  看到最终结果正确就认为机制正确。中间状态可能已经偏离，只是被后续默认值或重试偶然修补。应保存首个结构化产物并逐阶段比较。
</Callout>

<Callout type="trap">
  <strong>误区：</strong>
  为了使用模式或框架而制造抽象。若变化轴、失败边界和替代方案没有写清，新增层次只会扩大认知成本和故障面。
</Callout>

## 可复现的最小实现

先把决策记录写成机器可读结构：

```json
{
  "unit": "SOLID 原则回顾",
  "inputFrozen": true,
  "scenario": "normal | boundary | single-fault",
  "firstDivergence": null,
  "cleanupRequired": true
}
```

再用同一条执行链处理三类样本：

```ts
type Evidence = { stage: string; expected: string; actual: string };

function verify(sample: unknown, expected: readonly Evidence[]) {
  const trace = runFromCleanState(sample);
  return expected.find((item, index) => trace[index]?.actual !== item.expected);
}
```

最后保存回归门禁，禁止失败样本静默通过：

```text
normal      -> complete, invariant preserved
boundary    -> complete or explicit rejection
singleFault -> stop at first divergence, no stale output
```

用统一评分解释实验结果：

$$
Q = C_{correct} + C_{trace} + C_{recover} - R_{residual}
$$

$$
C_{coverage} = \frac{N_{verified}}{N_{official}}
$$

$$
R_{residual} = P(failure) \times I(impact)
$$

$$
Accept = (C_{coverage} \ge 0.90) \land (R_{residual} \le R_{budget})
$$

## 本章回顾

- SOLID 原则回顾必须绑定冻结输入和明确成功标准。
- 核心概念必须能画成结构并沿边追踪责任。
- 常见误区要由正常、边界和单故障样本共同验证。
- 小结与练习必须保存第一处偏离和清理重建步骤。
- 名词解释决定方案是否可以进入下一阶段。

<Exercises>

**问题 1**：为“SOLID 原则回顾”写出最小输入合同，并指出一个不能省略的边界条件。

<Answer>
  输入合同至少包含样本身份、版本、配置、成功标准和清理方式；边界条件必须能改变结构或状态，而不是只换一段描述。
</Answer>

**问题 2**：构造一个只破坏“常见误区”的单故障样本，第一条预期证据是什么？

<Answer>
  先冻结正常轨迹，只改变该条件；第一条证据应是最早依赖它的结构化中间产物，而不是最终错误消息。
</Answer>

**问题 3**：什么情况下不应采用本页方案？

<Answer>
  当变化轴稳定、直接实现更清晰、额外层次无法降低风险或提供独立测试边界时，应选择更简单方案并记录重新评估触发条件。
</Answer>

</Exercises>

## 术语表

<Glossary>
  <GlossaryItem term="SOLID 原则回顾">
    SOLID 原则回顾用于描述“SOLID
    原则回顾”中的输入、结构、约束或证据边界，必须能在实验记录中定位。
  </GlossaryItem>
  <GlossaryItem term="核心概念">
    核心概念用于描述“SOLID
    原则回顾”中的输入、结构、约束或证据边界，必须能在实验记录中定位。
  </GlossaryItem>
  <GlossaryItem term="常见误区">
    常见误区用于描述“SOLID
    原则回顾”中的输入、结构、约束或证据边界，必须能在实验记录中定位。
  </GlossaryItem>
  <GlossaryItem term="小结与练习">
    小结与练习用于描述“SOLID
    原则回顾”中的输入、结构、约束或证据边界，必须能在实验记录中定位。
  </GlossaryItem>
  <GlossaryItem term="名词解释">
    名词解释用于描述“SOLID
    原则回顾”中的输入、结构、约束或证据边界，必须能在实验记录中定位。
  </GlossaryItem>
</Glossary>

<Attribution
  adaptedFrom="站内双书专题《架构与领域设计》，依据Eric Evans《Domain-Driven Design》（2003）与Robert C. Martin《Clean Architecture》（2017） · 独立教学重构"
  adaptedUrl="https://www.pearson.com/en-gb/subject-catalog/p/domain-driven-design-tackling-complexity-in-the-heart-of-software/P200000009375"
/>

<Objectives>

- 能说出 SOLID 五原则各自解决什么问题
- 能从架构角度解释依赖倒置原则（DIP）为何是连接整洁架构的桥梁
- 能回答：违反开闭原则（OCP）会带来什么后果？

</Objectives>

## SOLID 原则回顾

SOLID 是五个设计原则的首字母缩写，你在设计模式书里已经见过它们。这里换一个视角——不再从「怎么写好一个类」看，而是从「怎么搭好一个系统」看。设计模式解决的是模块级问题，而架构要的是系统级的稳定性。SOLID 恰好是连接这两级的那座桥。

<AddSolidPrinciplesDiagram />

五原则各管一件事：<Term def="一个类应该只有一个引起它变化的原因，职责单一则改动不扩散。">单一职责（SRP）</Term>管「谁该变」——一个类只有一个变化的理由；<Term def="对扩展开放、对修改关闭，新增行为靠加新代码而非改老代码。">开闭原则（OCP）</Term>管「怎么加」——新增功能靠加代码，不改老代码；<Term def="子类型必须能替换掉父类型而不破坏程序正确性。">里氏替换（LSP）</Term>管「能不能换」——子类必须能无缝替换父类；<Term def="客户端不应被迫依赖它用不到的方法，接口要细化。">接口隔离（ISP）</Term>管「依赖多少」——只依赖你真正用到的方法；<Term def="高层模块不应依赖低层模块，二者都依赖抽象；抽象不依赖细节，细节依赖抽象。">依赖倒置（DIP）</Term>管「依赖朝哪指」——依赖抽象，不依赖具体。

## 核心概念

前四条仍停在模块层面，但 DIP 一旦放大到系统级，性质就变了。在模块级，DIP 说「高层类依赖接口而非实现类」；在架构级，DIP 说「业务核心不依赖数据库、框架这些细节，而是细节依赖核心定义的抽象」。这一跃迁，正是整洁架构「依赖向内」的力学源头——同心圆之所以能成立，全靠 DIP 把依赖箭头从「外层框架指向内层业务」翻转成「内层定义接口、外层实现接口」。

DIP 的特殊性在于：它是唯一一条直接规定「系统级依赖方向」的原则。SRP、OCP、LSP、ISP 都在帮你在模块内部把变化隔开，但隔开之后各模块该朝谁依赖？只有 DIP 给出了系统级答案——朝稳定的抽象指，朝业务核心指。所以本书把 DIP 单拎出来下一章细讲，它就是从 SOLID 走向整洁架构的那根引线。

<Callout type="tip">
  <strong>建议 →</strong> 把 SOLID 当作一个连续光谱而非五条孤立的规则：SRP
  切分职责 → OCP 隔离变化 → LSP 保证可替换 → ISP 收窄依赖 → DIP
  翻转方向。前四条是在「切割」，最后一条是在「转向」。
</Callout>

## 常见误区

<Callout type="trap">
  <strong>现象 →</strong> 违反 OCP：每次加一种新支付方式，都要打开原有的
  PaymentService 改 if-else，改着改着就引入了线上 bug。<strong>原因 →</strong>{" "}
  新增行为靠修改老代码而非扩展新类，老逻辑被反复触碰，回归风险累积。
  <strong>修法 →</strong> 定义 PaymentStrategy
  接口，每种支付方式做成独立实现类，新增支付只加新类、不改老类——这就是「对扩展开放、对修改关闭」。
</Callout>

<Callout type="trap">
  <strong>现象 →</strong> 「DIP
  就是处处用接口，所有类都抽象一下。」结果接口泛滥，系统多了一堆只有一个实现的空接口，理解成本暴增。
  <strong>原因 →</strong> 把 DIP
  的「依赖抽象」机械理解成「逢类必抽接口」，忽略了 DIP
  要翻转的是「跨稳定/易变边界」的依赖，同层内部无需硬抽。<strong>修法 →</strong>{" "}
  只在稳定层（业务核心）与易变层（框架、数据库）的边界上用 DIP
  翻转依赖，同层协作直接依赖即可。
</Callout>

## 小结与练习

- SOLID 五原则各管一件事：SRP 谁该变、OCP 怎么加、LSP 能否换、ISP 依赖多少、DIP 依赖朝哪指
- DIP 是唯一规定系统级依赖方向的原则，前四条切割变化，DIP 翻转方向
- DIP 从模块级「依赖接口」放大到架构级「细节依赖核心」，这就是整洁架构的力学源头
- 违反 OCP 会让新增功能变成反复修改老代码，回归风险不断累积

<Exercises>
  <Answer question="从架构角度，DIP 为什么比其他四条原则更特殊？">
    DIP 是唯一一条直接规定系统级依赖方向的原则。SRP、OCP、LSP、ISP
    都在模块内部隔开变化，但隔开后各模块该朝谁依赖，只有 DIP
    给出系统级答案：朝稳定的抽象指、朝业务核心指。把它从模块级放大到架构级，就得到「细节依赖核心」，这正是整洁架构依赖向内的力学源头。
  </Answer>
  <Answer question="违反开闭原则（OCP）会带来什么后果？请举例说明。">
    违反 OCP 意味着新增功能靠改老代码而非加新代码。例如 PaymentService 里用
    if-else
    区分支付方式，每加一种就要打开老代码改分支，反复触碰已上线逻辑，回归风险不断累积，最终改一处崩一片。遵守
    OCP 则把每种支付做成独立策略类，新增只加新类、不碰老类。
  </Answer>
</Exercises>

## 名词解释

<Glossary>
  <GlossaryItem term="单一职责（SRP）">
    一个类应该只有一个引起它变化的原因。职责单一，改动才不会扩散到无关的地方。
  </GlossaryItem>
  <GlossaryItem term="开闭原则（OCP）">
    对扩展开放、对修改关闭。新增行为靠加新代码而非改老代码，避免反复触碰已稳定逻辑。
  </GlossaryItem>
  <GlossaryItem term="依赖倒置（DIP）">
    高层模块不应依赖低层模块，二者都依赖抽象。在架构级，它翻转成「细节依赖核心」，是整洁架构依赖向内的源头。
  </GlossaryItem>
  <GlossaryItem term="里氏替换（LSP）">
    子类型必须能替换掉父类型而不破坏程序正确性，保证继承体系可安全替换。
  </GlossaryItem>
</Glossary>

<Attribution
  adaptedFrom="Clean Architecture (Robert C. Martin) & Domain-Driven Design (Eric Evans)"
  adaptedUrl="https://www.oreilly.com/"
/>
