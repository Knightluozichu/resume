---
title: 六边形架构
type: A
section: 架构实践
order: 11
description: 六边形架构（端口与适配器）、核心不依赖外部、测试可替换性与整洁架构的关系。
demo: false
math: false
sourceUrl: ""
draft: false
---

import { AddHexagonalArchitectureDiagram } from "@/components/mdx/architecture-domain/diagrams/add-hexagonal-architecture";
import { Objectives, Callout, Attribution, Glossary, GlossaryItem, Term, Exercises, Answer } from "@/components/mdx/mdx-components";

<Objectives>

- 能画出六边形架构图，标出核心、端口与适配器
- 能解释端口和适配器的区别
- 能回答：六边形架构和整洁架构的共同点是什么？

</Objectives>

## 六边形架构

六边形架构又叫「端口与适配器」，由 Alistair Cockburn 提出。它的主张和整洁架构同源：业务核心不依赖外部，外部反过来依赖核心。<Term def="以业务为核心、用端口定义交互接口、用适配器连接外部技术，核心不依赖任何外部的架构。">六边形架构</Term>画成图是一个六边形——中心是业务核心，六条边上是端口，每个端口外接一个适配器连向具体技术。

<AddHexagonalArchitectureDiagram />

关键概念是端口与适配器的分工。<Term def="核心定义的交互接口，规定核心需要什么能力或提供什么能力，与技术无关。">端口（Port）</Term>是核心定义的接口，它声明「我需要什么能力」或「我提供什么能力」，与技术无关。分两种： driving port（驱动端口，如「如何被调用」）和 driven port（被驱动端口，如「如何持久化」）。<Term def="端口的具体技术实现，把外部技术（数据库、Web 框架）接进来或把核心能力暴露出去。">适配器（Adapter）</Term>是端口的具体实现，把某种技术接进来——一个 REST 适配器实现 driving port 把 HTTP 请求转成对核心的调用，一个 MySQL 适配器实现 driven port 把核心的持久化需求落成 SQL。

## 核心概念

六边形架构的精髓是「核心不知道外部的存在」。业务核心只认识自己定义的端口，不认识 HTTP、不认识 MySQL、不认识 Kafka。适配器像插头一样插在端口上——换数据库就是换个适配器，核心一行不改。这与整洁架构「换最外圈框架而内层不动」的标准完全一致，只是表达方式不同：整洁架构用同心圆，六边形用端口+适配器。

这个结构带来一个巨大的红利：**测试可替换性**。因为核心只依赖端口，测试时可以用「假适配器」替换真实适配器——driving 端插一个测试驱动器模拟请求，driven 端插一个内存假仓储替代数据库。于是业务核心可以完全不碰数据库、不启 Web 服务器就跑完整测试，又快又稳。这就是六边形架构最实用的价值：可测试性是它比传统分层架构强得多的地方。

六边形架构和整洁架构的共同点，一句话：**业务核心不依赖外部，依赖方向一律朝内**。两者的差异只在表达：整洁架构按「职责分层」切成同心圆，六边形按「交互方向」切成端口。实质是同一种力学，所以很多团队把二者混用——用整洁架构的分层思路组织核心，用六边形的端口适配器组织边界，各取所长。

<Callout type="tip">
<strong>建议 →</strong> 想快速验证一个系统是不是真六边形，看测试：能不能不启数据库、不启 Web 服务器就把业务核心的测试全跑通？能，说明端口隔离到位；不能，说明核心某处偷偷依赖了具体技术，端口没守住。
</Callout>

## 常见误区

<Callout type="trap">
<strong>现象 →</strong> 「我们用了端口适配器」，但端口接口里满是 `HttpServletRequest`、`JpaEntity` 这种技术类型。<strong>原因 →</strong> 端口被技术细节污染，名义上是端口实际还是核心依赖外部。<strong>修法 →</strong> 端口只放与技术无关的领域类型（如 `Order`、`PlaceOrderCommand`），技术类型留在适配器里做转换，核心认识的永远是纯领域对象。
</Callout>

<Callout type="trap">
<strong>现象 →</strong> 把六边形架构和整洁架构当成对立的两套非要二选一，团队争论该用哪个。<strong>原因 →</strong> 只看到表达形式差异，没看到二者是同一种力学。<strong>修法 →</strong> 二者同源——业务核心不依赖外部、依赖朝内。可混用：用整洁架构分层组织核心，用六边形端口适配器组织边界，无需二选一。
</Callout>

## 小结与练习

- 六边形架构 = 业务核心 + 端口（接口）+ 适配器（技术实现），核心不认识任何外部技术
- 端口是核心定义的与技术无关的接口，适配器是端口的具体技术实现，像插头插在端口上
- 换技术就是换适配器、核心不动；测试可用假适配器替换真实适配器，核心免启数据库即可全测
- 与整洁架构同源：依赖朝内；差异只在表达——同心圆 vs 端口适配器，可混用

<Exercises>
  <Answer question="端口和适配器的区别是什么？请各举一例。">端口是核心定义的、与技术无关的接口，声明核心需要或提供什么能力；适配器是端口的具体技术实现，把某种技术接进来。例：核心定义一个 OrderRepository 端口（driven port，声明「我需要存取订单」），一个 MySQL 适配器实现它把存取落成 SQL；一个 RestController 适配器实现 driving port，把 HTTP 请求转成对核心下单方法的调用。</Answer>
  <Answer question="六边形架构和整洁架构的共同点是什么？为什么说二者可混用？">共同点是同一种力学：业务核心不依赖外部、依赖方向一律朝内——换最外圈技术而核心不动。差异只在表达：整洁架构按职责切成同心圆，六边形按交互方向切成端口适配器。因为实质相同，所以可混用：用整洁架构的分层思路组织核心内部，用六边形的端口适配器组织外部边界，各取所长，无需二选一。</Answer>
</Exercises>

## 名词解释

<Glossary>
  <GlossaryItem term="六边形架构">以业务为核心、用端口定义交互接口、用适配器连接外部技术的架构，核心不依赖任何外部，又称端口与适配器。</GlossaryItem>
  <GlossaryItem term="端口（Port）">核心定义的交互接口，规定核心需要或提供什么能力，与技术无关，分驱动端口与被驱动端口。</GlossaryItem>
  <GlossaryItem term="适配器（Adapter）">端口的具体技术实现，把外部技术（数据库、Web 框架）接进来或把核心能力暴露出去，像插头插在端口上。</GlossaryItem>
</Glossary>

<Attribution adaptedFrom="Clean Architecture (Robert C. Martin) & Domain-Driven Design (Eric Evans)" adaptedUrl="https://www.oreilly.com/" />
