---
title: 第6章：AUTOSAR ECU级开发之RTE与BSW（除MCAL外）
type: B
section: AUTOSAR规范与车用控制器软件开发（2018版）
order: 6
description: 第6章：AUTOSAR ECU级开发之RTE与BSW（除MCAL外）：以五段 AUTOSAR 工件链、逐节点解释、故障回退和同输入重放完成版本化工程验收。
demo: true
math: true
sourceUrl: "https://www.mycnbook.com/product/1201788824"
draft: false
qualityVersion: 2
practiceMode: simulation
sourceMode: independent-rewrite
officialUnitId: avc2-06-rte-bsw
---

import {
  Answer,
  Attribution,
  Callout,
  Exercises,
  Glossary,
  GlossaryItem,
  Objectives,
  Step,
  Stepper,
  TermSequence,
} from "@/components/mdx/mdx-components";
import {
  Avc206RteBswArtifactLab,
  Avc206RteBswTraceLab,
  Avc206RteBswFaultLab,
} from "@/components/mdx/autosar-vehicle-controller/v2/avc2-06-rte-bsw";

# 第6章：AUTOSAR ECU级开发之RTE与BSW（除MCAL外）

<Objectives>

- 能解释“第6章：AUTOSAR ECU级开发之RTE与BSW（除MCAL外）”为何要从 ECU Extract 驱动 CAN、EcuM、BswM、RTE 与 OS 配置，把合同阶段和生成阶段分开验收
- 能逐项把 第6章 AUTOSAR ECU级开发之RTE与BSW（除MCAL外）、6.1 ETAS RTA系列工具简介、6.1.1 RTA-BSW简介、6.1.2 RTA-RTE简介、6.1.3 RTA-OS简介、6.2 ETAS RTA系列工具入门、6.2.1 RTA系列工具安装方法、6.2.2 RTA系列工具界面说明、6.3 CAN通信协议栈概念与配置方法介绍、6.3.1 CAN通信协议栈概念、6.3.2 CAN通信协议栈配置方法、6.4 EcuM模块概念与配置方法介绍、6.5 BswM模块概念与配置方法介绍、6.6 BSW模块代码生成、6.7 服务软件组件与应用层软件组件端口连接、6.8 RTE配置与代码生成、6.8.1 RTE Contract阶段生成、6.8.2 RTE配置、6.8.3 RTE Generation阶段生成、6.9 AUTOSAR操作系统概念与配置方法介绍、6.9.1 AUTOSAR操作系统概念、6.9.2 RTA-OS工程创建、6.9.3 AUTOSAR操作系统配置方法、6.9.4 RTA-OS工程编译、6.10 本章小结 映射到五段工件链，并指出每段的输入、输出和所有者
- 能从“一致生成”追踪到可观察结果，持续验证“通信周期、模式、runnable 事件、任务映射和生成 API 对同一系统工件保持引用闭合，启动顺序不读未初始化服务”
- 能注入“先生成 RTE，再修改端口与任务映射却继续链接旧 RTE 源码，得到表面可编译的混合版本”，保存首个分岔，用同一输入回退并提交ECU Extract 哈希、BSW 配置引用、RTE contract 头文件、任务事件表、生成日志与链接映射

</Objectives>

## 为什么“ECU Extract”不能绕过“BSW 配置”

“第6章：AUTOSAR ECU级开发之RTE与BSW（除MCAL外）”的核心决策是：从 ECU Extract 驱动 CAN、EcuM、BswM、RTE 与 OS 配置，把合同阶段和生成阶段分开验收。本页先冻结场景、输入工件、AUTOSAR 版本和所有者，再运行转换；每一步都必须回答“改了什么、由谁生成、如何回退”。只要无法证明“通信周期、模式、runnable 事件、任务映射和生成 API 对同一系统工件保持引用闭合，启动顺序不读未初始化服务”，就不能用编译成功、目标板亮灯或工具无报错替代验收。

<Callout type="info" title="当前版本边界：R25-11">
  原书出版于 2018 年，本课程保留其 Classic
  Platform、车灯示例与当时工具链的教学范围；规范事实以截至 2026 年 7 月已发布的
  R25-11 核对。R26-11 计划于 2026 年 12
  月发布，本页不把计划版本提前写成现行规范。
</Callout>

## 来源、版次与独立重写边界

本站只能访问原书的公开书目信息和目录，不能把目录页当作原版全文。“第6章：AUTOSAR ECU级开发之RTE与BSW（除MCAL外）”的中文解释、工件模型、交互实验、故障、练习与答案均为独立教学重写；AUTOSAR、ISO、ETAS、MathWorks 与 NXP 的官方页面只用于核对各自直接承担的技术事实。

- [《AUTOSAR规范与车用控制器软件开发》2018 年版目录](https://www.mycnbook.com/product/1201788824)：针对“第6章：AUTOSAR ECU级开发之RTE与BSW（除MCAL外）”，只核对原书版次、十章与参考文献的目录边界，不把零售目录当作可访问的原书正文。
- [ETAS RTA-CAR Integration](https://www.etas.com/ww/en/products-services/vehicle-software-platform/autosar-classic-profile-rta-car/rta-car-details-integration/)：针对“第6章：AUTOSAR ECU级开发之RTE与BSW（除MCAL外）”，核对 ISOLAR-A、ISOLAR-B、RTA-RTE、RTA-BSW 与 RTA-OS 的当前产品职责，不复刻旧版菜单。
- [AUTOSAR Classic Platform](https://www.autosar.org/standards/classic-platform/)：针对“第6章：AUTOSAR ECU级开发之RTE与BSW（除MCAL外）”，核对应用层、RTE、BSW、VFB、方法论和 Classic Platform 当前发布版本。
- [AUTOSAR R25-11 RTE Requirements](https://www.autosar.org/fileadmin/standards/R25-11/CP/AUTOSAR_CP_RS_RTE.pdf)：针对“第6章：AUTOSAR ECU级开发之RTE与BSW（除MCAL外）”，核对 RTE 的接口、生成阶段和应用软件到基础软件之间的合同边界。
- [AUTOSAR Classic Platform R25-11 Release Overview](https://www.autosar.org/fileadmin/standards/R25-11/CP/AUTOSAR_CP_TR_ReleaseOverview.pdf)：针对“第6章：AUTOSAR ECU级开发之RTE与BSW（除MCAL外）”，给当前 Classic 规范主张绑定 R25-11 版本，避免无版本地引用会继续变化的标准。

## 五段工件链与观察语言

<TermSequence
  items={[
    {
      term: "ECU Extract",
      def: "第6章：AUTOSAR ECU级开发之RTE与BSW（除MCAL外）中的目标 ECU 的组件、通信与资源输入",
    },
    {
      term: "BSW 配置",
      def: "第6章：AUTOSAR ECU级开发之RTE与BSW（除MCAL外）中的CAN、EcuM、BswM 和服务模块",
    },
    {
      term: "RTE Contract",
      def: "第6章：AUTOSAR ECU级开发之RTE与BSW（除MCAL外）中的应用可编译的接口合同",
    },
    {
      term: "RTE Generation",
      def: "第6章：AUTOSAR ECU级开发之RTE与BSW（除MCAL外）中的完成映射后的 RTE 实现代码",
    },
    {
      term: "OS 构建",
      def: "第6章：AUTOSAR ECU级开发之RTE与BSW（除MCAL外）中的任务、事件、资源、优先级与可执行文件",
    },
  ]}
  suffix="。"
/>

| 阶段           | 可交付工件                           |
| -------------- | ------------------------------------ |
| ECU Extract    | 目标 ECU 的组件、通信与资源输入      |
| BSW 配置       | CAN、EcuM、BswM 和服务模块           |
| RTE Contract   | 应用可编译的接口合同                 |
| RTE Generation | 完成映射后的 RTE 实现代码            |
| OS 构建        | 任务、事件、资源、优先级与可执行文件 |

这条链不是固定工具菜单，而是“第6章：AUTOSAR ECU级开发之RTE与BSW（除MCAL外）”的追踪骨架。正常场景输入为“冻结 ECU Extract 后依次配置 BSW、生成 contract、映射 OS 再生成 RTE”；边界场景输入为“在 contract 后修改端口映射但复用旧生成目录”。二者都要从相同冻结条件开始，并以“ECU Extract 哈希、BSW 配置引用、RTE contract 头文件、任务事件表、生成日志与链接映射”判断结果。

## 正式目录逐项深读

### 第6章 AUTOSAR ECU级开发之RTE与BSW（除MCAL外）

“第6章 AUTOSAR ECU级开发之RTE与BSW（除MCAL外）”连接应用合同与 ECU 实现，但 contract 阶段和 generation 阶段的输入并不相同。本页要求保存两阶段输入哈希、API 差异和任务映射，并在“ECU Extract”拒绝新 ARXML 搭配旧生成目录。

### 6.1 ETAS RTA系列工具简介

“6.1 ETAS RTA系列工具简介”只承担可复现工具入口，不把 2018 年菜单路径当成永久知识。学习者需要记录工具名称与版本、插件、许可证、输入工件和生成日志，并以“通信周期、模式、runnable 事件、任务映射和生成 API 对同一系统工件保持引用闭合，启动顺序不读未初始化服务”验收结果；界面截图只能辅助定位，不能代替工件差异。

### 6.1.1 RTA-BSW简介

“6.1.1 RTA-BSW简介”在本页落到“RTE Contract”工件：应用可编译的接口合同。学习者要先写出该节点的输入、输出、所有者和失败条件，再用ECU Extract 哈希、BSW 配置引用、RTE contract 头文件、任务事件表、生成日志与链接映射核对它是否真正参与“从 ECU Extract 驱动 CAN、EcuM、BswM、RTE 与 OS 配置，把合同阶段和生成阶段分开验收”。

### 6.1.2 RTA-RTE简介

“6.1.2 RTA-RTE简介”连接应用合同与 ECU 实现，但 contract 阶段和 generation 阶段的输入并不相同。本页要求保存两阶段输入哈希、API 差异和任务映射，并在“RTE Generation”拒绝新 ARXML 搭配旧生成目录。

### 6.1.3 RTA-OS简介

“6.1.3 RTA-OS简介”决定行为何时执行以及由谁调度。应冻结 runnable 或任务的触发事件、周期、优先级、资源和最坏响应假设，再沿ECU Extract → BSW 配置 → RTE Contract → RTE Generation → OS 构建追踪一次执行，不能从生成成功反推时序正确。

### 6.2 ETAS RTA系列工具入门

“6.2 ETAS RTA系列工具入门”只承担可复现工具入口，不把 2018 年菜单路径当成永久知识。学习者需要记录工具名称与版本、插件、许可证、输入工件和生成日志，并以“通信周期、模式、runnable 事件、任务映射和生成 API 对同一系统工件保持引用闭合，启动顺序不读未初始化服务”验收结果；界面截图只能辅助定位，不能代替工件差异。

### 6.2.1 RTA系列工具安装方法

“6.2.1 RTA系列工具安装方法”只承担可复现工具入口，不把 2018 年菜单路径当成永久知识。学习者需要记录工具名称与版本、插件、许可证、输入工件和生成日志，并以“通信周期、模式、runnable 事件、任务映射和生成 API 对同一系统工件保持引用闭合，启动顺序不读未初始化服务”验收结果；界面截图只能辅助定位，不能代替工件差异。

### 6.2.2 RTA系列工具界面说明

“6.2.2 RTA系列工具界面说明”只承担可复现工具入口，不把 2018 年菜单路径当成永久知识。学习者需要记录工具名称与版本、插件、许可证、输入工件和生成日志，并以“通信周期、模式、runnable 事件、任务映射和生成 API 对同一系统工件保持引用闭合，启动顺序不读未初始化服务”验收结果；界面截图只能辅助定位，不能代替工件差异。

### 6.3 CAN通信协议栈概念与配置方法介绍

“6.3 CAN通信协议栈概念与配置方法介绍”首先是版本与范围节点。阅读它时应把原书 2018 年的工具链实践、当前 R25-11 已发布规范和 R26-11 计划版本分开记录，再判断“从 ECU Extract 驱动 CAN、EcuM、BswM、RTE 与 OS 配置，把合同阶段和生成阶段分开验收”中的哪些责任稳定、哪些界面会随版本变化。

### 6.3.1 CAN通信协议栈概念

“6.3.1 CAN通信协议栈概念”需要把逻辑信号、PDU、帧和驱动层分开，并记录长度、周期、超时、新鲜度、计数器、数据标识与 CRC 的适用位置。对“混合版本”的判断必须来自状态和故障模型，不能只看总线上出现了数据。

### 6.3.2 CAN通信协议栈配置方法

“6.3.2 CAN通信协议栈配置方法”需要把逻辑信号、PDU、帧和驱动层分开，并记录长度、周期、超时、新鲜度、计数器、数据标识与 CRC 的适用位置。对“混合版本”的判断必须来自状态和故障模型，不能只看总线上出现了数据。

### 6.4 EcuM模块概念与配置方法介绍

“6.4 EcuM模块概念与配置方法介绍”首先是版本与范围节点。阅读它时应把原书 2018 年的工具链实践、当前 R25-11 已发布规范和 R26-11 计划版本分开记录，再判断“从 ECU Extract 驱动 CAN、EcuM、BswM、RTE 与 OS 配置，把合同阶段和生成阶段分开验收”中的哪些责任稳定、哪些界面会随版本变化。

### 6.5 BswM模块概念与配置方法介绍

“6.5 BswM模块概念与配置方法介绍”首先是版本与范围节点。阅读它时应把原书 2018 年的工具链实践、当前 R25-11 已发布规范和 R26-11 计划版本分开记录，再判断“从 ECU Extract 驱动 CAN、EcuM、BswM、RTE 与 OS 配置，把合同阶段和生成阶段分开验收”中的哪些责任稳定、哪些界面会随版本变化。

### 6.6 BSW模块代码生成

“6.6 BSW模块代码生成”在本页落到“RTE Generation”工件：完成映射后的 RTE 实现代码。学习者要先写出该节点的输入、输出、所有者和失败条件，再用通信周期、模式、runnable 事件、任务映射和生成 API 对同一系统工件保持引用闭合，启动顺序不读未初始化服务核对它是否真正参与“从 ECU Extract 驱动 CAN、EcuM、BswM、RTE 与 OS 配置，把合同阶段和生成阶段分开验收”。

### 6.7 服务软件组件与应用层软件组件端口连接

“6.7 服务软件组件与应用层软件组件端口连接”属于可生成合同：数据语义、方向、初始值、更新方式、错误与调用关系都要在“OS 构建”工件中显式化。验收不只看名称相同，还要用ECU Extract 哈希、BSW 配置引用、RTE contract 头文件、任务事件表、生成日志与链接映射证明提供端与需要端的类型和生命周期兼容。

### 6.8 RTE配置与代码生成

“6.8 RTE配置与代码生成”连接应用合同与 ECU 实现，但 contract 阶段和 generation 阶段的输入并不相同。本页要求保存两阶段输入哈希、API 差异和任务映射，并在“ECU Extract”拒绝新 ARXML 搭配旧生成目录。

### 6.8.1 RTE Contract阶段生成

“6.8.1 RTE Contract阶段生成”连接应用合同与 ECU 实现，但 contract 阶段和 generation 阶段的输入并不相同。本页要求保存两阶段输入哈希、API 差异和任务映射，并在“BSW 配置”拒绝新 ARXML 搭配旧生成目录。

### 6.8.2 RTE配置

“6.8.2 RTE配置”连接应用合同与 ECU 实现，但 contract 阶段和 generation 阶段的输入并不相同。本页要求保存两阶段输入哈希、API 差异和任务映射，并在“RTE Contract”拒绝新 ARXML 搭配旧生成目录。

### 6.8.3 RTE Generation阶段生成

“6.8.3 RTE Generation阶段生成”连接应用合同与 ECU 实现，但 contract 阶段和 generation 阶段的输入并不相同。本页要求保存两阶段输入哈希、API 差异和任务映射，并在“RTE Generation”拒绝新 ARXML 搭配旧生成目录。

### 6.9 AUTOSAR操作系统概念与配置方法介绍

“6.9 AUTOSAR操作系统概念与配置方法介绍”首先是版本与范围节点。阅读它时应把原书 2018 年的工具链实践、当前 R25-11 已发布规范和 R26-11 计划版本分开记录，再判断“从 ECU Extract 驱动 CAN、EcuM、BswM、RTE 与 OS 配置，把合同阶段和生成阶段分开验收”中的哪些责任稳定、哪些界面会随版本变化。

### 6.9.1 AUTOSAR操作系统概念

“6.9.1 AUTOSAR操作系统概念”决定行为何时执行以及由谁调度。应冻结 runnable 或任务的触发事件、周期、优先级、资源和最坏响应假设，再沿ECU Extract → BSW 配置 → RTE Contract → RTE Generation → OS 构建追踪一次执行，不能从生成成功反推时序正确。

### 6.9.2 RTA-OS工程创建

“6.9.2 RTA-OS工程创建”决定行为何时执行以及由谁调度。应冻结 runnable 或任务的触发事件、周期、优先级、资源和最坏响应假设，再沿ECU Extract → BSW 配置 → RTE Contract → RTE Generation → OS 构建追踪一次执行，不能从生成成功反推时序正确。

### 6.9.3 AUTOSAR操作系统配置方法

“6.9.3 AUTOSAR操作系统配置方法”决定行为何时执行以及由谁调度。应冻结 runnable 或任务的触发事件、周期、优先级、资源和最坏响应假设，再沿ECU Extract → BSW 配置 → RTE Contract → RTE Generation → OS 构建追踪一次执行，不能从生成成功反推时序正确。

### 6.9.4 RTA-OS工程编译

“6.9.4 RTA-OS工程编译”决定行为何时执行以及由谁调度。应冻结 runnable 或任务的触发事件、周期、优先级、资源和最坏响应假设，再沿ECU Extract → BSW 配置 → RTE Contract → RTE Generation → OS 构建追踪一次执行，不能从生成成功反推时序正确。

### 6.10 本章小结

“6.10 本章小结”首先是版本与范围节点。阅读它时应把原书 2018 年的工具链实践、当前 R25-11 已发布规范和 R26-11 计划版本分开记录，再判断“从 ECU Extract 驱动 CAN、EcuM、BswM、RTE 与 OS 配置，把合同阶段和生成阶段分开验收”中的哪些责任稳定、哪些界面会随版本变化。

## 三个可重放实验

<Callout type="info" title="先写预测，再点击">
  先预测“一致生成”经过五段工件后的预期输出，再选择正式目录节点。若运行与预测不一致，保留差异并缩小主张，不允许移动输入边界。
</Callout>

<Stepper>
  <Step title="1. 目录节点与工件定位">
    切换“一致生成”与“混合版本”，再选择目录节点，确认它映射到哪个工件阶段。

    <Avc206RteBswArtifactLab />

  </Step>
  <Step title="2. 逐工件追踪">
    依次检查 ECU Extract → BSW 配置 → RTE Contract → RTE Generation → OS 构建，每一步只接受能维持“通信周期、模式、runnable 事件、任务映射和生成 API 对同一系统工件保持引用闭合，启动顺序不读未初始化服务”的转换。

    <Avc206RteBswTraceLab />

  </Step>
  <Step title="3. 单一故障、回退与同输入重放">
    注入“先生成 RTE，再修改端口与任务映射却继续链接旧 RTE 源码，得到表面可编译的混合版本”，定位第一个不一致工件；撤销故障后用完全相同的输入重放，确认中间状态与结果一起恢复。

    <Avc206RteBswFaultLab />

  </Step>
</Stepper>

## 工程验收矩阵

| 场景     | 输入                                                                            | 预期                                            | 拒绝条件                         |
| -------- | ------------------------------------------------------------------------------- | ----------------------------------------------- | -------------------------------- |
| 一致生成 | 冻结 ECU Extract 后依次配置 BSW、生成 contract、映射 OS 再生成 RTE              | API、任务和通信引用闭合，冷启动轨迹符合模式设计 | 工件版本或所有者不可追溯         |
| 混合版本 | 在 contract 后修改端口映射但复用旧生成目录                                      | 版本门禁识别输入哈希变化并要求清理后重新生成    | 只展示最终现象，没有首个分岔     |
| 故障恢复 | 先生成 RTE，再修改端口与任务映射却继续链接旧 RTE 源码，得到表面可编译的混合版本 | 回退后同输入轨迹恢复                            | 重置只清界面，没有恢复工件与状态 |

<Callout type="trap" title="误区：把工具成功当作系统正确">
  “第6章：AUTOSAR
  ECU级开发之RTE与BSW（除MCAL外）”中任何一个工具的绿色状态都只说明局部检查通过。跨工具的类型、引用、时序和硬件资源仍要用ECU
  Extract 哈希、BSW 配置引用、RTE contract
  头文件、任务事件表、生成日志与链接映射闭合。
</Callout>

<Callout type="trap" title="误区：把 2018 步骤当作当前规范">
  原书操作路径有明确时代边界。课程保留原始学习范围，但当前事实绑定
  R25-11；若工具菜单或生成选项变化，应迁移工件合同，而不是伪造旧界面。
</Callout>

<Callout type="trap" title="误区：采用 AUTOSAR 就等于功能安全">
  AUTOSAR 提供架构与可选安全机制，不自动证明 ISO 26262 合规、ASIL
  分解、独立性或诊断覆盖。涉及安全的结论必须另有安全需求、验证和残余风险证据。
</Callout>

## 本章小结

“第6章：AUTOSAR ECU级开发之RTE与BSW（除MCAL外）”的掌握标准不是记住 25 个目录标题，而是能解释“从 ECU Extract 驱动 CAN、EcuM、BswM、RTE 与 OS 配置，把合同阶段和生成阶段分开验收”，保持“通信周期、模式、runnable 事件、任务映射和生成 API 对同一系统工件保持引用闭合，启动顺序不读未初始化服务”，并在“先生成 RTE，再修改端口与任务映射却继续链接旧 RTE 源码，得到表面可编译的混合版本”发生时用ECU Extract 哈希、BSW 配置引用、RTE contract 头文件、任务事件表、生成日志与链接映射定位、回退和重放。

<Glossary>
  <GlossaryItem term="ECU Extract">
    目标 ECU 的组件、通信与资源输入。
  </GlossaryItem>
  <GlossaryItem term="BSW 配置">CAN、EcuM、BswM 和服务模块。</GlossaryItem>
  <GlossaryItem term="RTE Contract">应用可编译的接口合同。</GlossaryItem>
  <GlossaryItem term="RTE Generation">完成映射后的 RTE 实现代码。</GlossaryItem>
  <GlossaryItem term="OS 构建">
    任务、事件、资源、优先级与可执行文件。
  </GlossaryItem>
</Glossary>

<Exercises>

1. 怎样为“第6章：AUTOSAR ECU级开发之RTE与BSW（除MCAL外）”建立不依赖工具界面的最小正常基线？

<Answer>
  冻结“冻结 ECU Extract 后依次配置 BSW、生成 contract、映射 OS 再生成
  RTE”、规范和工具版本、五段工件及所有者；逐段记录输入输出，最后以“API、任务和通信引用闭合，冷启动轨迹符合模式设计”和ECU
  Extract 哈希、BSW 配置引用、RTE contract
  头文件、任务事件表、生成日志与链接映射验收。任何一步无法复算时停止向后生成。
</Answer>

2. 正式目录中的每个节点怎样进入工件、可视化与练习证据？

<Answer>
  1. 第6章 AUTOSAR ECU级开发之RTE与BSW（除MCAL外）：在“ECU Extract”核对目标 ECU
  的组件、通信与资源输入，证据进入ECU Extract 哈希、BSW 配置引用、RTE contract
  头文件、任务事件表、生成日志与链接映射。 2. 6.1 ETAS RTA系列工具简介：在“BSW
  配置”核对CAN、EcuM、BswM 和服务模块，证据进入ECU Extract 哈希、BSW
  配置引用、RTE contract 头文件、任务事件表、生成日志与链接映射。 3. 6.1.1
  RTA-BSW简介：在“RTE Contract”核对应用可编译的接口合同，证据进入ECU Extract
  哈希、BSW 配置引用、RTE contract 头文件、任务事件表、生成日志与链接映射。 4.
  6.1.2 RTA-RTE简介：在“RTE Generation”核对完成映射后的 RTE
  实现代码，证据进入ECU Extract 哈希、BSW 配置引用、RTE contract
  头文件、任务事件表、生成日志与链接映射。 5. 6.1.3 RTA-OS简介：在“OS
  构建”核对任务、事件、资源、优先级与可执行文件，证据进入ECU Extract 哈希、BSW
  配置引用、RTE contract 头文件、任务事件表、生成日志与链接映射。 6. 6.2 ETAS
  RTA系列工具入门：在“ECU Extract”核对目标 ECU
  的组件、通信与资源输入，证据进入ECU Extract 哈希、BSW 配置引用、RTE contract
  头文件、任务事件表、生成日志与链接映射。 7. 6.2.1 RTA系列工具安装方法：在“BSW
  配置”核对CAN、EcuM、BswM 和服务模块，证据进入ECU Extract 哈希、BSW
  配置引用、RTE contract 头文件、任务事件表、生成日志与链接映射。 8. 6.2.2
  RTA系列工具界面说明：在“RTE Contract”核对应用可编译的接口合同，证据进入ECU
  Extract 哈希、BSW 配置引用、RTE contract
  头文件、任务事件表、生成日志与链接映射。 9. 6.3
  CAN通信协议栈概念与配置方法介绍：在“RTE Generation”核对完成映射后的 RTE
  实现代码，证据进入ECU Extract 哈希、BSW 配置引用、RTE contract
  头文件、任务事件表、生成日志与链接映射。 10. 6.3.1 CAN通信协议栈概念：在“OS
  构建”核对任务、事件、资源、优先级与可执行文件，证据进入ECU Extract 哈希、BSW
  配置引用、RTE contract 头文件、任务事件表、生成日志与链接映射。 11. 6.3.2
  CAN通信协议栈配置方法：在“ECU Extract”核对目标 ECU
  的组件、通信与资源输入，证据进入ECU Extract 哈希、BSW 配置引用、RTE contract
  头文件、任务事件表、生成日志与链接映射。 12. 6.4
  EcuM模块概念与配置方法介绍：在“BSW 配置”核对CAN、EcuM、BswM
  和服务模块，证据进入ECU Extract 哈希、BSW 配置引用、RTE contract
  头文件、任务事件表、生成日志与链接映射。 13. 6.5
  BswM模块概念与配置方法介绍：在“RTE
  Contract”核对应用可编译的接口合同，证据进入ECU Extract 哈希、BSW 配置引用、RTE
  contract 头文件、任务事件表、生成日志与链接映射。 14. 6.6
  BSW模块代码生成：在“RTE Generation”核对完成映射后的 RTE 实现代码，证据进入ECU
  Extract 哈希、BSW 配置引用、RTE contract
  头文件、任务事件表、生成日志与链接映射。 15. 6.7
  服务软件组件与应用层软件组件端口连接：在“OS
  构建”核对任务、事件、资源、优先级与可执行文件，证据进入ECU Extract 哈希、BSW
  配置引用、RTE contract 头文件、任务事件表、生成日志与链接映射。 16. 6.8
  RTE配置与代码生成：在“ECU Extract”核对目标 ECU
  的组件、通信与资源输入，证据进入ECU Extract 哈希、BSW 配置引用、RTE contract
  头文件、任务事件表、生成日志与链接映射。 17. 6.8.1 RTE
  Contract阶段生成：在“BSW 配置”核对CAN、EcuM、BswM 和服务模块，证据进入ECU
  Extract 哈希、BSW 配置引用、RTE contract
  头文件、任务事件表、生成日志与链接映射。 18. 6.8.2 RTE配置：在“RTE
  Contract”核对应用可编译的接口合同，证据进入ECU Extract 哈希、BSW 配置引用、RTE
  contract 头文件、任务事件表、生成日志与链接映射。 19. 6.8.3 RTE
  Generation阶段生成：在“RTE Generation”核对完成映射后的 RTE
  实现代码，证据进入ECU Extract 哈希、BSW 配置引用、RTE contract
  头文件、任务事件表、生成日志与链接映射。 20. 6.9
  AUTOSAR操作系统概念与配置方法介绍：在“OS
  构建”核对任务、事件、资源、优先级与可执行文件，证据进入ECU Extract 哈希、BSW
  配置引用、RTE contract 头文件、任务事件表、生成日志与链接映射。 21. 6.9.1
  AUTOSAR操作系统概念：在“ECU Extract”核对目标 ECU
  的组件、通信与资源输入，证据进入ECU Extract 哈希、BSW 配置引用、RTE contract
  头文件、任务事件表、生成日志与链接映射。 22. 6.9.2 RTA-OS工程创建：在“BSW
  配置”核对CAN、EcuM、BswM 和服务模块，证据进入ECU Extract 哈希、BSW
  配置引用、RTE contract 头文件、任务事件表、生成日志与链接映射。 23. 6.9.3
  AUTOSAR操作系统配置方法：在“RTE Contract”核对应用可编译的接口合同，证据进入ECU
  Extract 哈希、BSW 配置引用、RTE contract
  头文件、任务事件表、生成日志与链接映射。 24. 6.9.4 RTA-OS工程编译：在“RTE
  Generation”核对完成映射后的 RTE 实现代码，证据进入ECU Extract 哈希、BSW
  配置引用、RTE contract 头文件、任务事件表、生成日志与链接映射。 25. 6.10
  本章小结：在“OS 构建”核对任务、事件、资源、优先级与可执行文件，证据进入ECU
  Extract 哈希、BSW 配置引用、RTE contract
  头文件、任务事件表、生成日志与链接映射。
</Answer>

3. 注入“先生成 RTE，再修改端口与任务映射却继续链接旧 RTE 源码，得到表面可编译的混合版本”后，怎样证明修复不是偶然？

<Answer>
  保存故障前的冻结输入和基线轨迹，定位首个偏离“通信周期、模式、runnable
  事件、任务映射和生成 API
  对同一系统工件保持引用闭合，启动顺序不读未初始化服务”的工件；修正受控源而非生成结果，清理下游工件后重新生成，再以同一输入重放“混合版本”。只有中间状态、最终结果和ECU
  Extract 哈希、BSW 配置引用、RTE contract
  头文件、任务事件表、生成日志与链接映射全部恢复才接受修复。
</Answer>

</Exercises>

<Attribution
  mode="independent-rewrite"
  sourceBasis="outline-only"
  workTitle="宋珂、王民、单忠伟、谭杨《AUTOSAR规范与车用控制器软件开发》（2018）公开目录"
  adaptedUrl="https://www.mycnbook.com/product/1201788824"
/>
