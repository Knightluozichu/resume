---
title: 定制视图与触摸事件
type: C
section: 高级UI与动画
order: 30
description: 从零实现自定义 View——掌握 onMeasure/onDraw/onTouchEvent 三大核心、Canvas 绘图 API、触摸手势处理与自定义属性。
demo: true
math: false
sourceUrl: "https://www.oreilly.com/library/view/android-programming-the/9780135257555/"
draft: false
---

import { BnrLifecycleLab, BnrFailureLab, BnrEvidenceLab } from "@/components/mdx/big-nerd-ranch-guide/diagrams/custom-views-touch";

{/* C 实战型（《Android 编程权威指南》第30章改编）：自定义 View。
   知识点清单：
   1) onMeasure/onDraw/onTouchEvent = 三部曲
   2) Canvas 绘图 API = 代码
   3) 自定义属性 attrs = XML
   4) 触摸手势 = 代码
   5) wrap_content 测量 = Callout
*/}

<Objectives>

- 能解释“第30章 Custom Views and Touch Events”全部正式节点的组件所有者、生命周期、线程、状态来源与Android版本边界。
- 能实现“把测量、触摸事件序列、Canvas绘制、手势和状态保存组织成自定义View合同”的最小垂直切片，并保存构建、设备操作、原始日志和断言。
- 能比较正常创建、旋转、后台、进程重建、权限拒绝与外部故障下的行为差异。
- 能设计推翻当前实现的反例，并凭DragAndDraw事件轨迹、绘制快照、旋转状态与无障碍测试完成独立交接。

</Objectives>

## 从零造轮子——为什么要写自定义 View

Android 自带的控件（Button、TextView、ImageView）能覆盖 80% 的需求。但当你需要做一个圆形进度条、一个可拖拽的色温调节器、一个手写签名区域——这些都没有现成的控件。答案只有一个：**自己画**。

自定义 View 是"造轮子"的技能。你不再依赖 XML 拼控件，而是直接跟 Canvas 打交道——想画什么就画什么，想怎么响应触摸就怎么响应。这是一个 Android 开发者从"组装工"变成"制造者"的分水岭。

用**画布与画笔**的类比：Android 的 View 系统就像给了你一张画布（`Canvas`）和一套画笔（`Paint`），你决定在上面画什么——圆形、矩形、文字、路径——然后决定用户的手指碰到画布时发生什么。

<Stepper>
  <Step title="① onMeasure 测量尺寸">
    父容器通过 `MeasureSpec`（包含模式 EXACTLY/AT_MOST/UNSPECIFIED 和尺寸值）将约束传给子 View。你需要覆写 `onMeasure()`，解析父容器的约束，计算自己需要的宽高，最后**必须**调用 `setMeasuredDimension()` 将结果上报——否则系统会抛出 `IllegalStateException`。
  </Step>
  <Step title="② onDraw 绘制内容">
    `onDraw(canvas: Canvas)` 是自定义 View 的灵魂。拿到 `Canvas` 对象后，配合 `Paint`（控制颜色、样式 FILL/STROKE、线宽、字号、抗锯齿），你可以调用 `drawRect`、`drawCircle`、`drawText`、`drawPath`、`drawBitmap` 等 API 在 View 坐标系中自由绘制。需要重绘时调用 `invalidate()`。
  </Step>
  <Step title="③ onTouchEvent 处理触摸">
    覆写 `onTouchEvent(event: MotionEvent)` 让 View 响应用户手指操作。根据 `event.action` 区分 ACTION_DOWN（按下）、ACTION_MOVE（移动）、ACTION_UP（抬起）。关键规则：`ACTION_DOWN` 必须返回 `true`，后续的 MOVE 和 UP 事件才会继续发给这个 View。对复杂手势，使用 `GestureDetector` 识别单击、双击、长按和快速滑动。
  </Step>
</Stepper>

## 1. 自定义 View 的三板斧：测量、绘制、触摸

每个自定义 View 都围绕三个核心方法：

| 方法 | 职责 | 类比 |
|---|---|---|
| `onMeasure()` | 测量自己应该多大 | 决定画布尺寸 |
| `onDraw()` | 把自己画出来 | 在画布上作画 |
| `onTouchEvent()` | 响应用户触摸 | 处理手指在画布上的动作 |

执行顺序：`onMeasure()` → `onLayout()` → `onDraw()`。父容器先 measure 孩子，再 layout 孩子，最后 draw 孩子。

### onMeasure——决定自己有多大

Android 用 `<Term def="父容器传递给子 View 的尺寸约束模式：精确值(EXACTLY)、上限(AT_MOST)、无限制(UNSPECIFIED)">MeasureSpec</Term>` 来描述父子之间的尺寸协商。每个 MeasureSpec 包含两个信息：**模式（mode）**和**尺寸（size）**。

三种模式：

```kotlin
override fun onMeasure(widthMeasureSpec: Int, heightMeasureSpec: Int) {
    // 1. 解析父容器给的约束
    val widthMode = MeasureSpec.getMode(widthMeasureSpec)
    val widthSize = MeasureSpec.getSize(widthMeasureSpec)
    val heightMode = MeasureSpec.getMode(heightMeasureSpec)
    val heightSize = MeasureSpec.getSize(heightMeasureSpec)

    // 2. 根据模式决定自己的尺寸
    val desiredWidth = when (widthMode) {
        MeasureSpec.EXACTLY -> widthSize         // 精确值：如 match_parent 或具体 dp
        MeasureSpec.AT_MOST -> min(200, widthSize) // 上限：如 wrap_content
        MeasureSpec.UNSPECIFIED -> 200            // 无限制：如 ScrollView 内的 View
        else -> 200
    }
    val desiredHeight = 200  // 简化：固定高度

    // 3. 必须调用 setMeasuredDimension
    setMeasuredDimension(desiredWidth, desiredHeight)
}
```

<Callout type="tip">
  关键理解：`MeasureSpec.EXACTLY` 对应 `match_parent` 或具体 dp 值，`MeasureSpec.AT_MOST` 对应 `wrap_content`。自定义 View 如果不处理 `wrap_content`，它就跟 `match_parent` 一个效果——这是最常见的自定义 View bug。
</Callout>

### onDraw——在 Canvas 上画一切

`onDraw(canvas: Canvas)` 是自定义 View 的灵魂。Canvas 提供了完整的 2D 绘图 API：

```kotlin
override fun onDraw(canvas: Canvas) {
    super.onDraw(canvas)

    val paint = Paint().apply {
        isAntiAlias = true  // 抗锯齿，让边缘平滑
    }

    // 1. 画矩形
    paint.color = Color.BLUE
    paint.style = Paint.Style.FILL
    canvas.drawRect(50f, 50f, 150f, 150f, paint)

    // 2. 画圆形
    paint.color = Color.RED
    paint.style = Paint.Style.STROKE  // 空心
    paint.strokeWidth = 8f
    canvas.drawCircle(300f, 100f, 60f, paint)

    // 3. 画文字
    paint.color = Color.BLACK
    paint.style = Paint.Style.FILL
    paint.textSize = 48f
    canvas.drawText("Hello View", 50f, 250f, paint)

    // 4. 画路径（直线 + 圆弧）
    paint.color = Color.GREEN
    paint.style = Paint.Style.STROKE
    paint.strokeWidth = 5f
    val path = Path().apply {
        moveTo(50f, 300f)
        lineTo(200f, 300f)
        lineTo(200f, 400f)
    }
    canvas.drawPath(path, paint)

    // 5. 画图片
    val bitmap = BitmapFactory.decodeResource(resources, R.drawable.ic_launcher)
    canvas.drawBitmap(bitmap, 300f, 250f, paint)
}
```

<Term def="Android 2D 绘图的核心类，提供 drawRect/drawCircle/drawText/drawPath/drawBitmap 等方法">Canvas</Term> 的方法全部"画在"View 的坐标系里——原点 (0, 0) 在 View 的左上角，x 轴向右，y 轴向下。

<Term def="保存绘图属性的对象——颜色、样式(FILL/STROKE)、线宽、字号、抗锯齿等">Paint</Term> 对象控制"怎么画"。每次改变 Paint 属性后，后续的 draw 调用都使用新属性。

### 坐标系变换

Canvas 支持平移、旋转、缩放——这对于做图表、仪表盘非常有用：

```kotlin
// 把原点平移到 View 中心
canvas.save()                       // 保存当前状态
canvas.translate(width / 2f, height / 2f)
canvas.rotate(45f)                  // 旋转 45 度
paint.color = Color.BLUE
canvas.drawRect(-50f, -50f, 50f, 50f, paint)  // 以新原点为中心画矩形
canvas.restore()                    // 恢复之前的状态
```

`save()` / `restore()` 是一对——`save()` 把当前变换矩阵和裁剪区域压入栈，`restore()` 恢复到压栈时的状态。

## 2. 触摸事件处理——让 View "活"起来

### MotionEvent 基础

当手指触碰到 View 时，Android 调用 `onTouchEvent(event: MotionEvent)`。每个事件包含：
- **动作类型**：`event.action`——`ACTION_DOWN`（按下）、`ACTION_MOVE`（移动）、`ACTION_UP`（抬起）
- **坐标**：`event.x` / `event.y`——相对于当前 View 左上角的坐标

```kotlin
override fun onTouchEvent(event: MotionEvent): Boolean {
    val x = event.x
    val y = event.y

    when (event.action) {
        MotionEvent.ACTION_DOWN -> {
            // 手指按下：记录起始位置
            startX = x
            startY = y
            return true  // 返回 true 表示"我要处理这个事件"，后续的事件也会发过来
        }
        MotionEvent.ACTION_MOVE -> {
            // 手指移动：计算偏移量
            dx = x - startX
            dy = y - startY
            invalidate()  // 触发重绘
        }
        MotionEvent.ACTION_UP -> {
            // 手指抬起：处理点击或动作结束
            performClick()
        }
    }
    return super.onTouchEvent(event)
}
```

<Callout type="trap">
  关键细节：`onTouchEvent` 返回 `true` 表示"我消费了这次 DOWN 事件，后续的 MOVE 和 UP 也要发给我"。返回 `false` 表示"我不处理，不要再发给我了"。忘记返回 true 是最常见的 bug——你写了 MOVE 的处理逻辑，但是永远收不到 MOVE 事件。
</Callout>

{/* 触摸事件序列动画：一次手势如何被拆成 DOWN → MOVE → MOVE → UP 的 MotionEvent 回调 */}

<TouchEventSequenceDiagram />

### GestureDetector——识别常见手势

自己从 MotionEvent 里识别双击、长按、快速滑动，太麻烦了。Android 提供了 `GestureDetector`：

```kotlin
class MyCustomView(context: Context, attrs: AttributeSet?) : View(context, attrs) {

    private val gestureDetector = GestureDetector(context, object : GestureDetector.SimpleOnGestureListener() {
        override fun onDown(e: MotionEvent): Boolean {
            return true  // 必须返回 true，否则所有手势都不触发
        }

        override fun onSingleTapUp(e: MotionEvent): Boolean {
            // 单击
            showToast("单击了")
            return true
        }

        override fun onDoubleTap(e: MotionEvent): Boolean {
            // 双击
            showToast("双击了")
            return true
        }

        override fun onLongPress(e: MotionEvent) {
            // 长按
            showToast("长按了")
        }

        override fun onFling(
            e1: MotionEvent?, e2: MotionEvent?,
            velocityX: Float, velocityY: Float
        ): Boolean {
            // 快速滑动
            if (abs(velocityX) > abs(velocityY)) {
                showToast("水平快速滑动")
            }
            return true
        }

        override fun onScroll(
            e1: MotionEvent?, e2: MotionEvent?,
            distanceX: Float, distanceY: Float
        ): Boolean {
            // 拖动
            scrollX += distanceX.toInt()
            invalidate()
            return true
        }
    })

    override fun onTouchEvent(event: MotionEvent): Boolean {
        // 先把事件传给 GestureDetector，再处理自己的逻辑
        val handled = gestureDetector.onTouchEvent(event)
        return handled || super.onTouchEvent(event)
    }
}
```

## 3. 自定义属性——让 View 可以像原生控件一样在 XML 配置

在 `res/values/attrs.xml` 中声明自定义属性：

```xml
<declare-styleable name="MyCustomView">
    <attr name="circleColor" format="color" />
    <attr name="circleRadius" format="dimension" />
    <attr name="labelText" format="string" />
</declare-styleable>
```

在 View 的构造方法中读取：

```kotlin
class MyCustomView(context: Context, attrs: AttributeSet?) : View(context, attrs) {
    var circleColor: Int = Color.BLUE
    var circleRadius: Float = 60f
    var labelText: String = ""

    init {
        context.obtainStyledAttributes(attrs, R.styleable.MyCustomView).apply {
            circleColor = getColor(R.styleable.MyCustomView_circleColor, Color.BLUE)
            circleRadius = getDimension(R.styleable.MyCustomView_circleRadius, 60f)
            labelText = getString(R.styleable.MyCustomView_labelText) ?: ""
            recycle()  // 用完后记得 recycle，否则内存泄漏
        }
    }
}
```

布局中这样使用：

```xml
<com.example.MyCustomView
    android:layout_width="200dp"
    android:layout_height="200dp"
    app:circleColor="#FF0000"
    app:circleRadius="40dp"
    app:labelText="自定义" />
```

## 4. requestLayout vs invalidate——什么时候该调用哪个

这是一个很容易搞混的问题：

| 方法 | 触发流程 | 何时调用 |
|---|---|---|
| `requestLayout()` | `onMeasure() → onLayout() → onDraw()` | View 的**尺寸**或**位置**发生变化 |
| `invalidate()` | 只触发 `onDraw()` | 只有**视觉内容**变化，尺寸没变 |

<Callout type="tip">
  简单判断法则：你改的内容如果会导致 View 变大或变小 → 调 `requestLayout()`。你只改了颜色、透明度、文本内容这些不影响尺寸的东西 → 调 `invalidate()`。不放心就用 `requestLayout()`——它能力更强但更费性能（因为它会触发整个 View 树的重新测量）。
</Callout>

## 容易踩的坑

<Callout type="trap">
  **现象**：自定义 View 设置了 `wrap_content`，但实际效果跟 `match_parent` 一样。**原因**：`onMeasure` 里没有处理 `MeasureSpec.AT_MOST` 模式。默认 View 的 `onMeasure` 在 `AT_MOST` 下也会填满父容器。**修法**：在 `onMeasure` 中判断 `AT_MOST` 模式时，`setMeasuredDimension` 使用内容实际需要的尺寸。
</Callout>

<Callout type="trap">
  **现象**：`onDraw` 里画的东西突然消失了，或被裁掉了。**原因**：View 的尺寸（由 `onMeasure` 决定）比 Canvas 画的区域小——Canvas 不会自动裁剪，但如果 View 在 ViewGroup 中被 clip，超出部分就不可见。**修法**：确保 `onMeasure` 给出的 `measuredWidth/measuredHeight` 足够大，或调用 `setWillNotDraw(false)`（默认 View 不会调用 onDraw，除非它是 ViewGroup 且默认不画自己）。
</Callout>

<Callout type="trap">
  **现象**：在 `onTouchEvent` 中修改了数据并调了 `invalidate()`，但界面不更新。**原因**：`onTouchEvent` 返回了 `false`——后续 MOVE 事件没发过来。或者你忘记调用 `super.onTouchEvent()`。**修法**：在 `ACTION_DOWN` 时返回 `true`，确保后续事件能收到。
</Callout>

## 小结

- 自定义 View 三大核心：`onMeasure`（测量尺寸）、`onDraw`（绘制内容）、`onTouchEvent`（处理触摸）
- MeasureSpec 三种模式：EXACTLY（精确值）、AT_MOST（上限/wrap_content）、UNSPECIFIED（无限制）
- Canvas API 提供 `drawRect`/`drawCircle`/`drawText`/`drawPath`/`drawBitmap`；Paint 控制颜色、样式、线宽等
- GestureDetector 封装了单击、双击、长按、快速滑动等常见手势识别
- 自定义属性通过 `declare-styleable` 声明，让 View 在 XML 里像原生控件一样配置
- `requestLayout()` 触发布局重测（尺寸变了）→ 重；`invalidate()` 只触发重绘（视觉变了）→ 快

<Exercises>

**问题 1**：“第30章 Custom Views and Touch Events”覆盖哪些正式节点和项目主线？
<Answer>覆盖9个节点，从“30. Custom Views and Touch Events”到“Challenge: Accessibility Support”。主线是把测量、触摸事件序列、Canvas绘制、手势和状态保存组织成自定义View合同，交付物是DragAndDraw事件轨迹、绘制快照、旋转状态与无障碍测试。</Answer>

**问题 2**：怎样建立本页最小可执行实验？
<Answer>固定JDK、SDK、Gradle、设备API、语言、权限和输入，完成一个从用户动作到可观察结果的垂直切片；保存干净构建、设备操作、原始日志、状态快照与断言。</Answer>

**问题 3**：为什么只在正常点击路径运行不能证明完成？
<Answer>它没有覆盖组件重建、进程死亡、异步取消、权限拒绝、外部输入失败、资源释放和版本变化。应逐项注入故障，并用DragAndDraw事件轨迹、绘制快照、旋转状态与无障碍测试核对。</Answer>

**问题 4**：怎样设计能推翻当前实现的反例？
<Answer>保持其他变量不变，分别注入旋转、后台回收、进程死亡、拒权、断网、无效输入与重复操作；任何状态丢失、越权、崩溃、泄漏或重复副作用都推翻完成结论。</Answer>

**问题 5**：从第4版语境迁移到现代目标SDK时如何控制变量？
<Answer>先按原书还原机制并锁定基线，再查询目标SDK合同；一次只迁移组件导出、存储、权限、后台、通知或依赖之一，保存构建、行为、测试和回滚差异。</Answer>

**问题 6**：本页达到独立交接标准需要什么？
<Answer>需要版本与设备指纹、源码提交、输入、构建命令、生命周期轨迹、原始日志、失败测试、安全边界与发布产物，让他人凭DragAndDraw事件轨迹、绘制快照、旋转状态与无障碍测试重放。</Answer>

</Exercises>

<Glossary>

<GlossaryItem term="MeasureSpec">
  父容器传给子 View 的尺寸约束。包含模式（EXACTLY/AT_MOST/UNSPECIFIED）和尺寸值。EXACTLY 对应 match_parent 或具体 dp；AT_MOST 对应 wrap_content。
</GlossaryItem>

<GlossaryItem term="Canvas">
  Android 2D 绘图的画布。提供 drawRect、drawCircle、drawText、drawPath、drawBitmap 等方法。支持 save/restore 保存和恢复变换状态。
</GlossaryItem>

<GlossaryItem term="Paint">
  画笔——保存绘制属性：颜色、样式（FILL/STROKE）、线宽、字号、抗锯齿开关等。每次修改 Paint 属性后，后续的 Canvas 绘制调用均使用新设置。
</GlossaryItem>

<GlossaryItem term="GestureDetector">
  手势识别器。封装了常见的触摸手势判断——单击 (onSingleTapUp)、双击 (onDoubleTap)、长按 (onLongPress)、快速滑动 (onFling)、拖动 (onScroll)。简化了从原始 MotionEvent 中识别手势的工作。
</GlossaryItem>

<GlossaryItem term="MotionEvent">
  描述一次触摸事件的对象。包含动作类型（ACTION_DOWN/MOVE/UP/CANCEL）和当前触摸点的坐标（x, y）。一次完整的点击序列从 DOWN 开始，中间可能经过若干 MOVE，最后以 UP 结束。
</GlossaryItem>

</Glossary>

<Attribution />


{/* BNR4_AUTHORITY_SUPPLEMENT */}

## 从生命周期、状态与证据开始

第4版不是API词典，而是六个逐步扩展的应用实验。每一章都要回答：用户从哪里进入，哪个Android组件拥有工作，回调在哪个线程发生，哪些事实必须跨旋转或进程死亡恢复，外部系统怎样失败，资源在何时释放。<Term def="负责创建、取消并最终释放组件、任务或资源的明确作用域">生命周期所有者</Term>把Activity、Fragment视图、ViewModel、Worker、线程与资源的寿命连接起来；没有所有者的异步任务就是潜在泄漏。

界面字段不是天然事实。<Term def="在配置变化或进程重建后，可从保存事实重新构造的最小用户状态">可恢复状态</Term>应根据数据大小与寿命放入saved instance state、ViewModel、数据库、文件或调度器，而不是全部塞进单例。Intent、文件、数据库、网络、系统服务和其他应用形成<Term def="跨边界交换数据时必须验证的action、类型、权限、大小、时序和失败约束">外部合同</Term>；正常响应只是合同中的一个样本。

本书以2019年第四版、Android 5.0至8.1及其后续兼容实践为语境。<Term def="Android版本、targetSdk、设备形态、权限与依赖共同限定的行为适用范围">版本边界</Term>要求先还原书中机制，再查询现代目标SDK的权限、后台、存储、通知和组件导出合同。<Term def="由环境指纹、输入、操作、日志、状态快照和断言组成的可重放结论">证据链</Term>则保证“在我的模拟器能跑”不会冒充可交付结论。

## 第4版正式目录逐节点映射

### 30. Custom Views and Touch Events

**正式节点 1/9。** 先说明“30. Custom Views and Touch Events”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“把测量、触摸事件序列、Canvas绘制、手势和状态保存组织成自定义View合同”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Setting Up the DragAndDraw Project

**正式节点 2/9。** 先说明“Setting Up the DragAndDraw Project”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“把测量、触摸事件序列、Canvas绘制、手势和状态保存组织成自定义View合同”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Creating a Custom View

**正式节点 3/9。** 先说明“Creating a Custom View”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“把测量、触摸事件序列、Canvas绘制、手势和状态保存组织成自定义View合同”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Handling Touch Events

**正式节点 4/9。** 先说明“Handling Touch Events”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“把测量、触摸事件序列、Canvas绘制、手势和状态保存组织成自定义View合同”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Rendering Inside onDraw(Canvas)

**正式节点 5/9。** 先说明“Rendering Inside onDraw(Canvas)”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“把测量、触摸事件序列、Canvas绘制、手势和状态保存组织成自定义View合同”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### For the More Curious: GestureDetector

**正式节点 6/9。** 先说明“For the More Curious: GestureDetector”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“把测量、触摸事件序列、Canvas绘制、手势和状态保存组织成自定义View合同”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Challenge: Saving State

**正式节点 7/9。** 先说明“Challenge: Saving State”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“把测量、触摸事件序列、Canvas绘制、手势和状态保存组织成自定义View合同”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Challenge: Rotating Boxes

**正式节点 8/9。** 先说明“Challenge: Rotating Boxes”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“把测量、触摸事件序列、Canvas绘制、手势和状态保存组织成自定义View合同”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Challenge: Accessibility Support

**正式节点 9/9。** 先说明“Challenge: Accessibility Support”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“把测量、触摸事件序列、Canvas绘制、手势和状态保存组织成自定义View合同”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

## 最小可执行切片

先把状态转换与Android副作用分开。下面的Kotlin骨架不替代本章代码，而是让旋转、进程恢复与失败输入可单测：

```kotlin
sealed interface ScreenState {
    data object Loading : ScreenState
    data class Ready(val key: String) : ScreenState
    data class Failed(val reason: String, val retryable: Boolean) : ScreenState
}

fun reduce(current: ScreenState, event: UiEvent): ScreenState = nextState(current, event)
```

固定JDK、SDK、Gradle、设备API、语言和输入，清理缓存后从命令行保存制品与日志：

```bash
./gradlew --no-daemon clean test assembleDebug
adb install -r app/build/outputs/apk/debug/app-debug.apk
adb shell am force-stop com.example.app
adb shell monkey -p com.example.app 1
adb logcat -d -v threadtime > run.log
```

先写行为断言，再执行用户操作和故障注入：

```gherkin
Given a pinned build, device API, locale, permission state, and input
When the owner is recreated after state saving or process death
Then durable facts reconstruct the same user-visible state
And cancelled work cannot update a destroyed owner
And denied permission, offline input, and malformed data remain recoverable
```

三段代码分别约束状态模型、环境重放和行为结果。真实项目还要加入静态检查、单元测试、仪器测试、无障碍检查和发布构建；日志只能支持断言，不能代替断言。

## 线程、状态与外部资源

主线程负责输入、生命周期分发和大部分UI工作，磁盘、网络、图片解码与长计算不能阻塞帧。后台结果返回前必须确认所有者仍有效；Fragment的生命周期与其View生命周期不是一回事。每个实现都应列出入口、线程、事实源、取消点、关闭责任和用户可见失败六项。

跨应用Intent、ContentProvider、WebView、网络响应、文件URI和深链都视为不可信输入。校验action、scheme、MIME、大小、授权与响应者；导出组件使用最小权限；日志、截图和测试数据库不得泄露令牌或个人数据。相机、音频、数据库、线程和回调注册必须在成功、异常与取消路径都释放。

性能以用户任务衡量：启动看可交互时刻，列表看掉帧与绑定分配，网络看超时和取消，后台工作看约束、电量与重复调度。每次比较固定设备温度、构建类型、数据规模和网络条件，先采基线再改变一个变量。

迁移现代目标SDK时，先在第4版语境中证明机制，再逐项处理组件导出、存储、通知、后台启动、权限、废弃API和依赖版本。每一步保存编译差异、行为差异、测试结果和回滚点；新API并不自动正确，旧API能编译也不代表仍符合平台政策。

## 三步验证实验

<Steps>
  <Step title="1. 建立目录节点与生命周期图">
    从“把测量、触摸事件序列、Canvas绘制、手势和状态保存组织成自定义View合同”选一个用户任务，标出入口、组件、所有者、回调线程、可恢复状态和销毁点。先预测旋转、后台和进程恢复后哪些对象消失、哪些事实必须重建。
    <BnrLifecycleLab />
  </Step>
  <Step title="2. 注入生命周期与外部故障">
    依次改变生命周期阶段、网络和权限，每次只改变一个条件；记录原始日志、界面状态、持久事实与资源计数，区分预期失败和未知状态。
    <BnrFailureLab />
  </Step>
  <Step title="3. 通过交付证据门">
    用DragAndDraw事件轨迹、绘制快照、旋转状态与无障碍测试核对版本设备、生命周期、持久状态、失败安全、测试和发布。缺少任何一项时，不得把正常点击路径视为完成。
    <BnrEvidenceLab />
  </Step>
</Steps>

<Callout type="trap">**核心陷阱。** 只复述类名与回调，未说明所有者、状态来源、线程、取消和失败后果。修复时回到用户任务和状态机，注入旋转、进程死亡、拒权、断网与无效输入。</Callout>

<Callout type="trap">**历史示例不等于当前平台政策。** 第4版准确反映出版时工具链；现代targetSdk可能改变组件导出、后台、存储、通知和权限。保留原书机制，再建立有证据的迁移层。</Callout>

<Callout type="warning">**真机与数据安全。** 使用测试账号、样本媒体和最小权限；清理日志、数据库与截图。签名密钥、API密钥、联系人和真实位置不得写入仓库或公开证据。</Callout>

## 本章回顾

本页从“30. Custom Views and Touch Events”覆盖到“Challenge: Accessibility Support”，共9个正式目录节点。掌握标准不是记住API，而是能沿“把测量、触摸事件序列、Canvas绘制、手势和状态保存组织成自定义View合同”解释组件与数据边界，运行正常、重建和失败实验，并让另一位开发者仅凭DragAndDraw事件轨迹、绘制快照、旋转状态与无障碍测试重放结论。

<Exercises>

**问题 1**：“第30章 Custom Views and Touch Events”覆盖哪些正式节点和项目主线？
<Answer>覆盖9个节点，从“30. Custom Views and Touch Events”到“Challenge: Accessibility Support”。主线是把测量、触摸事件序列、Canvas绘制、手势和状态保存组织成自定义View合同，交付物是DragAndDraw事件轨迹、绘制快照、旋转状态与无障碍测试。</Answer>

**问题 2**：怎样建立本页最小可执行实验？
<Answer>固定JDK、SDK、Gradle、设备API、语言、权限和输入，完成一个从用户动作到可观察结果的垂直切片；保存干净构建、设备操作、原始日志、状态快照与断言。</Answer>

**问题 3**：为什么只在正常点击路径运行不能证明完成？
<Answer>它没有覆盖组件重建、进程死亡、异步取消、权限拒绝、外部输入失败、资源释放和版本变化。应逐项注入故障，并用DragAndDraw事件轨迹、绘制快照、旋转状态与无障碍测试核对。</Answer>

**问题 4**：怎样设计能推翻当前实现的反例？
<Answer>保持其他变量不变，分别注入旋转、后台回收、进程死亡、拒权、断网、无效输入与重复操作；任何状态丢失、越权、崩溃、泄漏或重复副作用都推翻完成结论。</Answer>

**问题 5**：从第4版语境迁移到现代目标SDK时如何控制变量？
<Answer>先按原书还原机制并锁定基线，再查询目标SDK合同；一次只迁移组件导出、存储、权限、后台、通知或依赖之一，保存构建、行为、测试和回滚差异。</Answer>

**问题 6**：本页达到独立交接标准需要什么？
<Answer>需要版本与设备指纹、源码提交、输入、构建命令、生命周期轨迹、原始日志、失败测试、安全边界与发布产物，让他人凭DragAndDraw事件轨迹、绘制快照、旋转状态与无障碍测试重放。</Answer>

</Exercises>

<Glossary>
  <GlossaryItem term="生命周期所有者">负责创建、取消并最终释放组件、任务或资源的明确作用域。</GlossaryItem>
  <GlossaryItem term="可恢复状态">在配置变化或进程重建后，可从保存事实重新构造的最小用户状态。</GlossaryItem>
  <GlossaryItem term="外部合同">Intent、文件、数据库、网络、系统服务或其他应用与本组件交换数据时的可验证约束。</GlossaryItem>
  <GlossaryItem term="版本边界">Android版本、targetSdk、设备形态、权限与依赖共同限定的行为适用范围。</GlossaryItem>
  <GlossaryItem term="证据链">由环境指纹、输入、操作、日志、状态快照和断言组成的可重放结论。</GlossaryItem>
</Glossary>

[← 上一页：第29章 Browsing the Web and WebView](/learn/big-nerd-ranch-guide/advanced-ui-animation/webview) · [下一页：第31章 Property Animation →](/learn/big-nerd-ranch-guide/advanced-ui-animation/property-animation)

<Attribution adaptedFrom="Android Programming: The Big Nerd Ranch Guide, 4th Edition" adaptedUrl="https://www.oreilly.com/library/view/android-programming-the/9780135257555/" />
