---
title: 网页浏览
type: C
section: 高级UI与动画
order: 29
description: 掌握 WebView 的加载、JavaScript 交互、安全配置与性能优化——让 Android 应用内嵌完整的 Web 浏览能力。
demo: true
math: false
sourceUrl: "https://www.oreilly.com/library/view/android-programming-the/9780135257555/"
draft: false
---

import { BnrLifecycleLab, BnrFailureLab, BnrEvidenceLab } from "@/components/mdx/big-nerd-ranch-guide/diagrams/webview";

{/* C 实战型（《Android 编程权威指南》第29章改编）：WebView 的加载、交互、安全与性能。
   知识点清单：
   1) loadUrl/loadData = 代码
   2) WebViewClient/ChromeClient = 对照
   3) JS 双向通信 = 代码
   4) SSL 错误处理 = Callout
   5) 硬件加速与缓存 = Callout
*/}

<Objectives>

- 能解释“第29章 Browsing the Web and WebView”全部正式节点的组件所有者、生命周期、线程、状态来源与Android版本边界。
- 能实现“在隐式浏览器、WebView、自定义UI和Custom Tabs间按信任边界选择，并处理旋转与历史”的最小垂直切片，并保存构建、设备操作、原始日志和断言。
- 能比较正常创建、旋转、后台、进程重建、权限拒绝与外部故障下的行为差异。
- 能设计推翻当前实现的反例，并凭URL策略、WebViewClient/WebChromeClient边界、旋转状态、后退历史和注入风险测试完成独立交接。

</Objectives>

## 为什么需要 WebView——当原生 UI 不够用时

一个成熟的 App 几乎不可能只靠原生控件。展示运营活动页、加载隐私协议、嵌入在线客服、甚至直接打开第三方网站——这些场景的共同答案是：**在 App 里放一个浏览器**。

Android 把 Chromium 内核打包成了 `WebView` 控件：一个小巧但完整的浏览器引擎。你可以把它当成一个 `<iframe>` 用在 App 里，加载 URL、执行 JavaScript、跟原生代码双向通信。

如果没有 WebView，这些需求只能用原生代码从头画——运营页每次改版都得发 App 新版本，隐私协议更新也得走应用商店审核。WebView 给 Android 开发者开了后门：**不改 App 代码就能更新界面**。

本章从加载网页开始，一路讲到 JavaScript 双向通信和安全配置——这是所有商用 App 都绕不开的技能。

{/* WebView 集成解剖：中央核心 + loadUrl/WebSettings 作用、两个 Client 回调、addJavascriptInterface 双向桥 */}

<WebViewAnatomyDiagram />

<Stepper>
  <Step title="① WebViewClient 拦截 URL">
    页面链接被点击时，WebView 默认会跳转到系统浏览器。通过 `WebViewClient.shouldOverrideUrlLoading()` 拦截 URL 请求并返回 `false`，让链接留在 WebView 内部跳转，用户体验就像在使用一个内置浏览器。
  </Step>
  <Step title="② loadUrl 加载网页">
    拿到 WebView 实例后调用 `webView.loadUrl("https://...")` 即可加载远程页面；也可以用 `loadDataWithBaseURL()` 直接加载 HTML 字符串来显示富文本、隐私协议等本地内容。`WebSettings` 需显式开启 `javaScriptEnabled`。
  </Step>
  <Step title="③ addJavascriptInterface 暴露原生方法">
    在 Kotlin 端定义一个带 `@JavascriptInterface` 注解的类，通过 `webView.addJavascriptInterface(instance, "Android")` 将其注入 JS 全局作用域。之后 Web 页面里的 JavaScript 就能通过 `Android.方法名()` 直接调用原生代码。
  </Step>
  <Step title="④ evaluateJavascript 调用 JS">
    Android 到 JS 的桥梁是 `webView.evaluateJavascript("js代码")`——它在 WebView 的渲染线程异步执行 JS 字符串，结果通过回调以 JSON 格式返回。注意必须在 `onPageFinished` 之后调用，否则 JS 环境尚未就绪。
  </Step>
  <Step title="⑤ WebChromeClient 处理 alert/console">
    `WebChromeClient` 负责跟"浏览器外观"相关的回调：`onProgressChanged` 获取加载进度、`onReceivedTitle` 获取网页标题、`onJsAlert` 拦截 JS 的 `alert()` 弹窗并替换为原生 Dialog——让 WebView 的行为更像一个真正的浏览器。
  </Step>
</Stepper>

## 1. WebView 基础——加载网页

### 最简单的 WebView

在布局里放一个 `<WebView>`，然后 `findViewById` 拿到它，调用 `loadUrl()`：

```kotlin
// activity_main.xml
// <WebView android:id="@+id/webView" android:layout_width="match_parent"
//          android:layout_height="match_parent" />

class WebViewActivity : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_main)
        val webView = findViewById<WebView>(R.id.webView)
        webView.loadUrl("https://www.example.com")
    }
}
```

就这么简单——但这样写出来的 WebView 的行为会让你抓狂：点击网页里的链接？跳出到系统浏览器。页面加载成功/失败？没有任何回调。网页标题？拿不到。进度条？没有。

你需要两个 Client 来完成 WebView 的定制：`WebViewClient` 和 `WebChromeClient`。

### WebViewClient——控制页面加载

<Term def="拦截 URL 加载、处理页面请求和 SSL 错误的回调类">WebViewClient</Term> 负责页面导航相关的事件。最常用的三个回调：

```kotlin
webView.webViewClient = object : WebViewClient() {
    // 1. 拦截 URL 加载——让链接在 WebView 内部跳转，而不是跳系统浏览器
    override fun shouldOverrideUrlLoading(
        view: WebView?, request: WebResourceRequest?
    ): Boolean {
        // 返回 false 让 WebView 自己处理；返回 true 拦截
        return false
    }

    // 2. 页面开始加载
    override fun onPageStarted(view: WebView?, url: String?, favicon: Bitmap?) {
        progressBar.visibility = View.VISIBLE
    }

    // 3. 页面加载完成
    override fun onPageFinished(view: WebView?, url: String?) {
        progressBar.visibility = View.GONE
    }

    // 4. 处理 SSL 错误——这里绝不能草率 proceed()
    override fun onReceivedSslError(
        view: WebView?, handler: SslErrorHandler?, error: SslError?
    ) {
        // 生产环境应该提示用户并取消，而不是直接 proceed()
        handler?.cancel()
        showSslErrorDialog()
    }
}
```

<Callout type="trap">
  **关键安全红线的提醒**：`onReceivedSslError` 中的 `handler.proceed()` 会忽略证书错误继续加载——这在开发环境测试自签名证书时方便，但**生产环境绝对不能随意 `proceed()`**。否则等于把用户暴露在中间人攻击之下。正确做法：开发阶段仅在 BuildConfig.DEBUG 时 proceed，生产环境提示用户或拒绝加载。
</Callout>

### WebChromeClient——处理 UI 相关回调

<Term def="处理 WebView 中与浏览器 UI 相关的回调（进度条、标题、JS 对话框、文件选择等）">WebChromeClient</Term> 负责进度、标题、JS 对话框等"浏览器 UI"相关的事件：

```kotlin
webView.webChromeClient = object : WebChromeClient() {
    // 1. 获取加载进度（0-100）
    override fun onProgressChanged(view: WebView?, newProgress: Int) {
        progressBar.progress = newProgress
        if (newProgress == 100) progressBar.visibility = View.GONE
    }

    // 2. 获取网页标题
    override fun onReceivedTitle(view: WebView?, title: String?) {
        supportActionBar?.title = title
    }

    // 3. 处理 JavaScript alert()
    override fun onJsAlert(
        view: WebView?, url: String?, message: String?,
        result: JsResult?
    ): Boolean {
        AlertDialog.Builder(this@WebViewActivity)
            .setMessage(message)
            .setPositiveButton("OK") { _, _ -> result?.confirm() }
            .show()
        return true  // 返回 true 表示已处理，不要系统的默认弹窗
    }
}
```

### 加载本地 HTML 字符串

除了 `loadUrl()`，还可以用 `loadData()` 或 `loadDataWithBaseURL()` 直接加载 HTML 字符串——这在显示富文本、隐私协议等场景非常实用：

```kotlin
val htmlContent = """
    <html>
    <head><meta charset="utf-8"></head>
    <body>
        <h1>用户协议</h1>
        <p>这是用 WebView 直接渲染的 HTML 内容。</p>
    </body>
    </html>
""".trimIndent()

// loadData 可能乱码；推荐用 loadDataWithBaseURL 指定编码
webView.loadDataWithBaseURL(null, htmlContent, "text/html", "UTF-8", null)
```

## 2. JavaScript 交互——原生与 Web 的桥梁

这是 WebView 最有价值的部分：让 Android 代码和网页里的 JavaScript 可以互相调用。

### 方向一：Android 调用 JavaScript

用 `<Term def="在 WebView 的 JS 环境中执行一段 JavaScript 代码字符串并获取返回值">evaluateJavascript</Term>` 在 WebView 里执行 JS：

```kotlin
// Android → JS：调 JS 函数，拿返回值
webView.evaluateJavascript(
    "(function() { return document.title; })();"
) { result ->
    // result 是 JSON 字符串，比如 "\"我的网页标题\""
    val title = result.removeSurrounding("\"")
    titleTextView.text = title
}
```

`evaluateJavascript` 是异步的——它在 WebView 的渲染线程上执行 JS，结果通过回调返回。**不要在 `onPageFinished` 之前调用它**，否则 JS 环境还没准备好。

### 方向二：JavaScript 调用 Android

用 `<Term def="将 Java/Kotlin 对象暴露给 WebView 中的 JavaScript，使 JS 可以直接调用原生方法">addJavascriptInterface</Term>` 把 Kotlin 对象暴露给 JS：

```kotlin
// Kotlin 端：定义一个给 JS 调用的接口类
class WebAppInterface(private val context: Context) {
    @JavascriptInterface
    fun showToast(message: String) {
        Toast.makeText(context, message, Toast.LENGTH_SHORT).show()
    }

    @JavascriptInterface
    fun getDeviceInfo(): String {
        return "${Build.MANUFACTURER} ${Build.MODEL}"
    }
}

// 注册：把对象以 "Android" 为名字暴露到 JS 全局作用域
webView.addJavascriptInterface(WebAppInterface(this), "Android")
```

<Callout type="warn">
  **安全警告**：`@JavascriptInterface` 标记的方法会在 WebView 的线程池中执行（不在主线程），且只有 API 17+ 才需要该注解。API 16 及以下，**所有 public 方法**都能被 JS 调用——这是严重的安全漏洞。必须把 `targetSdkVersion` 设为 17 以上，且只暴露真正需要的方法。
</Callout>

JS 端这样调用：

```javascript
// Web 页面里的 JS 代码
document.getElementById("btn").onclick = function() {
    // Android 是 addJavascriptInterface 注册的名字
    Android.showToast("按钮被点了！");
    var info = Android.getDeviceInfo();
    console.log(info);
};
```

### WebView 设置——按需打开功能

WebView 默认**禁用 JavaScript**、禁用缩放、不保存表单数据。需要显式配置：

```kotlin
webView.settings.apply {
    javaScriptEnabled = true          // 开启 JavaScript（默认 false）
    domStorageEnabled = true          // 开启 DOM Storage（HTML5 localStorage）
    allowFileAccess = false           // 禁止访问文件系统（安全加固）
    allowContentAccess = false        // 禁止访问 content:// URI
    mixedContentMode = WebSettings.MIXED_CONTENT_NEVER_ALLOW // 禁止 HTTP/HTTPS 混用
    cacheMode = WebSettings.LOAD_DEFAULT // 使用默认缓存策略
    setSupportZoom(true)              // 允许缩放
    builtInZoomControls = true        // 内置缩放控件
    displayZoomControls = false       // 隐藏缩放按钮（手势缩放仍有效）
    useWideViewPort = true            // 支持 viewport 标签
    loadWithOverviewMode = true       // 缩放内容以适应屏幕
}
```

<Callout type="tip">
  性能关键：如果页面复杂，务必在 `AndroidManifest.xml` 中为 WebView 所在的 Activity 开启**硬件加速**：`android:hardwareAccelerated="true"`。大部分设备默认开启，但 Android 4.0 以下需要显式设置。没有硬件加速，大量 CSS 动画和 Canvas 绘制会卡到不能看。
</Callout>

## 3. 返回键处理——让 WebView 的返回先于 Activity

WebView 里用户浏览了好几页后按返回键——默认行为是直接关闭 Activity。这体验很差。应该让 WebView 先消费返回事件：

```kotlin
override fun onBackPressed() {
    if (webView.canGoBack()) {
        webView.goBack()   // 回到上一页
    } else {
        super.onBackPressed()  // 没有历史了就退出 Activity
    }
}
```

`canGoBack()` 检查是否有可返回的页面历史；`goBack()` 返回上一页。对应的还有 `canGoForward()` 和 `goForward()`——和普通浏览器完全一样。

## 代码完整示例

下面是一个配置齐全的 WebView Activity 完整代码：

```kotlin
class WebViewActivity : AppCompatActivity() {
    private lateinit var webView: WebView
    private lateinit var progressBar: ProgressBar

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_webview)

        webView = findViewById(R.id.webView)
        progressBar = findViewById(R.id.progressBar)

        configureWebView()
        webView.loadUrl("https://www.example.com")
    }

    private fun configureWebView() {
        // 基础设置
        webView.settings.apply {
            javaScriptEnabled = true
            domStorageEnabled = true
            allowFileAccess = false
            cacheMode = WebSettings.LOAD_DEFAULT
        }

        // 页面导航
        webView.webViewClient = object : WebViewClient() {
            override fun onPageStarted(view: WebView?, url: String?, favicon: Bitmap?) {
                progressBar.visibility = View.VISIBLE
            }
            override fun onPageFinished(view: WebView?, url: String?) {
                progressBar.visibility = View.GONE
            }
            override fun onReceivedSslError(
                view: WebView?, handler: SslErrorHandler?, error: SslError?
            ) {
                if (BuildConfig.DEBUG) handler?.proceed()
                else handler?.cancel()
            }
        }

        // UI 回调
        webView.webChromeClient = object : WebChromeClient() {
            override fun onProgressChanged(view: WebView?, newProgress: Int) {
                progressBar.progress = newProgress
            }
            override fun onReceivedTitle(view: WebView?, title: String?) {
                supportActionBar?.title = title
            }
        }

        // JavaScript 接口
        webView.addJavascriptInterface(
            WebAppInterface(this), "Android"
        )
    }

    override fun onBackPressed() {
        if (webView.canGoBack()) webView.goBack()
        else super.onBackPressed()
    }

    override fun onDestroy() {
        // 销毁 WebView，释放资源
        webView.destroy()
        super.onDestroy()
    }
}
```

## 容易踩的坑

<Callout type="trap">
  **现象**：WebView 加载 HTTPS 页面时一片空白，也没有错误提示。**原因**：Android 5.0+ 的 WebView 默认阻止混合内容（HTTPS 页面里嵌 HTTP 资源）。**修法**：`webView.settings.mixedContentMode = WebSettings.MIXED_CONTENT_COMPATIBILITY_MODE`（仅非生产环境），或让后端把所有资源改成 HTTPS。
</Callout>

<Callout type="trap">
  **现象**：内存泄漏——退出 WebView Activity 后内存不释放。**原因**：WebView 持有 Activity 的 Context 引用，且内部有独立的渲染线程和 JS 引擎。**修法**：①在 `onDestroy()` 中调用 `webView.destroy()`；②不要直接在布局 XML 中声明 WebView——用代码动态创建并传入 `applicationContext`（或用独立的进程）。
</Callout>

<Callout type="trap">
  **现象**：`evaluateJavascript` 调用后无回调，或 JS 报 "Android is not defined"。**原因**：在 `onPageFinished` 之前调了 `evaluateJavascript`——此时 JS 环境未就绪。或 `addJavascriptInterface` 在 `loadUrl` 之后调用。**修法**：`addJavascriptInterface` 必须在 `loadUrl` 之前调用；`evaluateJavascript` 必须在 `onPageFinished` 之后调用。
</Callout>

## 小结

- WebView 是 Android 内嵌的浏览器引擎——`loadUrl` 加载网页，`loadDataWithBaseURL` 加载 HTML 字符串
- `WebViewClient` 控制页面加载行为（URL 拦截、SSL 错误处理）；`WebChromeClient` 处理 UI 回调（进度、标题、JS 弹窗）
- Android→JS 用 `evaluateJavascript`；JS→Android 用 `addJavascriptInterface`（加 `@JavascriptInterface` 注解）
- 安全铁律：生产环境绝不 `handler.proceed()` SSL 错误；`allowFileAccess` 设为 false；只暴露必要方法给 JS
- 性能：开启硬件加速；`onDestroy` 中调 `webView.destroy()` 防止内存泄漏

<Exercises>

**问题 1**：“第29章 Browsing the Web and WebView”覆盖哪些正式节点和项目主线？
<Answer>覆盖10个节点，从“29. Browsing the Web and WebView”到“Challenge: Using the Back Button for Browser History”。主线是在隐式浏览器、WebView、自定义UI和Custom Tabs间按信任边界选择，并处理旋转与历史，交付物是URL策略、WebViewClient/WebChromeClient边界、旋转状态、后退历史和注入风险测试。</Answer>

**问题 2**：怎样建立本页最小可执行实验？
<Answer>固定JDK、SDK、Gradle、设备API、语言、权限和输入，完成一个从用户动作到可观察结果的垂直切片；保存干净构建、设备操作、原始日志、状态快照与断言。</Answer>

**问题 3**：为什么只在正常点击路径运行不能证明完成？
<Answer>它没有覆盖组件重建、进程死亡、异步取消、权限拒绝、外部输入失败、资源释放和版本变化。应逐项注入故障，并用URL策略、WebViewClient/WebChromeClient边界、旋转状态、后退历史和注入风险测试核对。</Answer>

**问题 4**：怎样设计能推翻当前实现的反例？
<Answer>保持其他变量不变，分别注入旋转、后台回收、进程死亡、拒权、断网、无效输入与重复操作；任何状态丢失、越权、崩溃、泄漏或重复副作用都推翻完成结论。</Answer>

**问题 5**：从第4版语境迁移到现代目标SDK时如何控制变量？
<Answer>先按原书还原机制并锁定基线，再查询目标SDK合同；一次只迁移组件导出、存储、权限、后台、通知或依赖之一，保存构建、行为、测试和回滚差异。</Answer>

**问题 6**：本页达到独立交接标准需要什么？
<Answer>需要版本与设备指纹、源码提交、输入、构建命令、生命周期轨迹、原始日志、失败测试、安全边界与发布产物，让他人凭URL策略、WebViewClient/WebChromeClient边界、旋转状态、后退历史和注入风险测试重放。</Answer>

</Exercises>

<Glossary>

<GlossaryItem term="WebViewClient">
  WebView 的导航管家。拦截 URL 加载（`shouldOverrideUrlLoading`）、处理页面开始/结束事件、处理 SSL 证书错误等。类比：浏览器地址栏的"逻辑层"。
</GlossaryItem>

<GlossaryItem term="WebChromeClient">
  WebView 的 UI 管家。负责进度条更新、标题获取、JavaScript alert/prompt 弹窗、文件选择器等跟"浏览器外观"相关的回调。类比：浏览器的标签页和弹窗控制。
</GlossaryItem>

<GlossaryItem term="evaluateJavascript">
  在 WebView 的 JS 引擎里异步执行一段 JS 代码字符串，结果通过回调返回（JSON 格式）。必须在 `onPageFinished` 之后调用，否则 JS 环境未就绪。
</GlossaryItem>

<GlossaryItem term="addJavascriptInterface">
  把一个 Kotlin/Java 对象绑定到 WebView 的 JS 全局作用域。JS 端通过绑定的名字（如 "Android"）直接调 Kotlin 方法。方法必须标记 `@JavascriptInterface`（API 17+ 的安全要求）。
</GlossaryItem>

<GlossaryItem term="混合内容 (Mixed Content)">
  一个 HTTPS 页面内部加载了 HTTP 协议的资源（图片、CSS、JS）。Android 5.0+ 默认阻止混合内容，因为 HTTP 资源可能被篡改，破坏 HTTPS 的安全性。
</GlossaryItem>

</Glossary>

<Attribution />


{/* BNR4_AUTHORITY_SUPPLEMENT */}

## 从生命周期、状态与证据开始

第4版不是API词典，而是六个逐步扩展的应用实验。每一章都要回答：用户从哪里进入，哪个Android组件拥有工作，回调在哪个线程发生，哪些事实必须跨旋转或进程死亡恢复，外部系统怎样失败，资源在何时释放。<Term def="负责创建、取消并最终释放组件、任务或资源的明确作用域">生命周期所有者</Term>把Activity、Fragment视图、ViewModel、Worker、线程与资源的寿命连接起来；没有所有者的异步任务就是潜在泄漏。

界面字段不是天然事实。<Term def="在配置变化或进程重建后，可从保存事实重新构造的最小用户状态">可恢复状态</Term>应根据数据大小与寿命放入saved instance state、ViewModel、数据库、文件或调度器，而不是全部塞进单例。Intent、文件、数据库、网络、系统服务和其他应用形成<Term def="跨边界交换数据时必须验证的action、类型、权限、大小、时序和失败约束">外部合同</Term>；正常响应只是合同中的一个样本。

本书以2019年第四版、Android 5.0至8.1及其后续兼容实践为语境。<Term def="Android版本、targetSdk、设备形态、权限与依赖共同限定的行为适用范围">版本边界</Term>要求先还原书中机制，再查询现代目标SDK的权限、后台、存储、通知和组件导出合同。<Term def="由环境指纹、输入、操作、日志、状态快照和断言组成的可重放结论">证据链</Term>则保证“在我的模拟器能跑”不会冒充可交付结论。

## 第4版正式目录逐节点映射

### 29. Browsing the Web and WebView

**正式节点 1/10。** 先说明“29. Browsing the Web and WebView”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“在隐式浏览器、WebView、自定义UI和Custom Tabs间按信任边界选择，并处理旋转与历史”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### One Last Bit of Flickr Data

**正式节点 2/10。** 先说明“One Last Bit of Flickr Data”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“在隐式浏览器、WebView、自定义UI和Custom Tabs间按信任边界选择，并处理旋转与历史”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### The Easy Way: Implicit Intents

**正式节点 3/10。** 先说明“The Easy Way: Implicit Intents”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“在隐式浏览器、WebView、自定义UI和Custom Tabs间按信任边界选择，并处理旋转与历史”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### The Harder Way: WebView

**正式节点 4/10。** 先说明“The Harder Way: WebView”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“在隐式浏览器、WebView、自定义UI和Custom Tabs间按信任边界选择，并处理旋转与历史”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Proper Rotation with WebView

**正式节点 5/10。** 先说明“Proper Rotation with WebView”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“在隐式浏览器、WebView、自定义UI和Custom Tabs间按信任边界选择，并处理旋转与历史”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### WebView vs a Custom UI

**正式节点 6/10。** 先说明“WebView vs a Custom UI”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“在隐式浏览器、WebView、自定义UI和Custom Tabs间按信任边界选择，并处理旋转与历史”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### For the More Curious: Injecting JavaScript Objects

**正式节点 7/10。** 先说明“For the More Curious: Injecting JavaScript Objects”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“在隐式浏览器、WebView、自定义UI和Custom Tabs间按信任边界选择，并处理旋转与历史”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### For the More Curious: WebView Updates

**正式节点 8/10。** 先说明“For the More Curious: WebView Updates”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“在隐式浏览器、WebView、自定义UI和Custom Tabs间按信任边界选择，并处理旋转与历史”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### For the More Curious: Chrome Custom Tabs (Another Easy Way)

**正式节点 9/10。** 先说明“For the More Curious: Chrome Custom Tabs (Another Easy Way)”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“在隐式浏览器、WebView、自定义UI和Custom Tabs间按信任边界选择，并处理旋转与历史”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Challenge: Using the Back Button for Browser History

**正式节点 10/10。** 先说明“Challenge: Using the Back Button for Browser History”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“在隐式浏览器、WebView、自定义UI和Custom Tabs间按信任边界选择，并处理旋转与历史”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

## 最小可执行切片

先把状态转换与Android副作用分开。下面的Kotlin骨架不替代本章代码，而是让旋转、进程恢复与失败输入可单测：

```kotlin
sealed interface ScreenState {
    data object Loading : ScreenState
    data class Ready(val key: String) : ScreenState
    data class Failed(val reason: String, val retryable: Boolean) : ScreenState
}

fun reduce(current: ScreenState, event: UiEvent): ScreenState = nextState(current, event)
```

固定JDK、SDK、Gradle、设备API、语言和输入，清理缓存后从命令行保存制品与日志：

```bash
./gradlew --no-daemon clean test assembleDebug
adb install -r app/build/outputs/apk/debug/app-debug.apk
adb shell am force-stop com.example.app
adb shell monkey -p com.example.app 1
adb logcat -d -v threadtime > run.log
```

先写行为断言，再执行用户操作和故障注入：

```gherkin
Given a pinned build, device API, locale, permission state, and input
When the owner is recreated after state saving or process death
Then durable facts reconstruct the same user-visible state
And cancelled work cannot update a destroyed owner
And denied permission, offline input, and malformed data remain recoverable
```

三段代码分别约束状态模型、环境重放和行为结果。真实项目还要加入静态检查、单元测试、仪器测试、无障碍检查和发布构建；日志只能支持断言，不能代替断言。

## 线程、状态与外部资源

主线程负责输入、生命周期分发和大部分UI工作，磁盘、网络、图片解码与长计算不能阻塞帧。后台结果返回前必须确认所有者仍有效；Fragment的生命周期与其View生命周期不是一回事。每个实现都应列出入口、线程、事实源、取消点、关闭责任和用户可见失败六项。

跨应用Intent、ContentProvider、WebView、网络响应、文件URI和深链都视为不可信输入。校验action、scheme、MIME、大小、授权与响应者；导出组件使用最小权限；日志、截图和测试数据库不得泄露令牌或个人数据。相机、音频、数据库、线程和回调注册必须在成功、异常与取消路径都释放。

性能以用户任务衡量：启动看可交互时刻，列表看掉帧与绑定分配，网络看超时和取消，后台工作看约束、电量与重复调度。每次比较固定设备温度、构建类型、数据规模和网络条件，先采基线再改变一个变量。

迁移现代目标SDK时，先在第4版语境中证明机制，再逐项处理组件导出、存储、通知、后台启动、权限、废弃API和依赖版本。每一步保存编译差异、行为差异、测试结果和回滚点；新API并不自动正确，旧API能编译也不代表仍符合平台政策。

## 三步验证实验

<Steps>
  <Step title="1. 建立目录节点与生命周期图">
    从“在隐式浏览器、WebView、自定义UI和Custom Tabs间按信任边界选择，并处理旋转与历史”选一个用户任务，标出入口、组件、所有者、回调线程、可恢复状态和销毁点。先预测旋转、后台和进程恢复后哪些对象消失、哪些事实必须重建。
    <BnrLifecycleLab />
  </Step>
  <Step title="2. 注入生命周期与外部故障">
    依次改变生命周期阶段、网络和权限，每次只改变一个条件；记录原始日志、界面状态、持久事实与资源计数，区分预期失败和未知状态。
    <BnrFailureLab />
  </Step>
  <Step title="3. 通过交付证据门">
    用URL策略、WebViewClient/WebChromeClient边界、旋转状态、后退历史和注入风险测试核对版本设备、生命周期、持久状态、失败安全、测试和发布。缺少任何一项时，不得把正常点击路径视为完成。
    <BnrEvidenceLab />
  </Step>
</Steps>

<Callout type="trap">**核心陷阱。** 只复述类名与回调，未说明所有者、状态来源、线程、取消和失败后果。修复时回到用户任务和状态机，注入旋转、进程死亡、拒权、断网与无效输入。</Callout>

<Callout type="trap">**历史示例不等于当前平台政策。** 第4版准确反映出版时工具链；现代targetSdk可能改变组件导出、后台、存储、通知和权限。保留原书机制，再建立有证据的迁移层。</Callout>

<Callout type="warning">**真机与数据安全。** 使用测试账号、样本媒体和最小权限；清理日志、数据库与截图。签名密钥、API密钥、联系人和真实位置不得写入仓库或公开证据。</Callout>

## 本章回顾

本页从“29. Browsing the Web and WebView”覆盖到“Challenge: Using the Back Button for Browser History”，共10个正式目录节点。掌握标准不是记住API，而是能沿“在隐式浏览器、WebView、自定义UI和Custom Tabs间按信任边界选择，并处理旋转与历史”解释组件与数据边界，运行正常、重建和失败实验，并让另一位开发者仅凭URL策略、WebViewClient/WebChromeClient边界、旋转状态、后退历史和注入风险测试重放结论。

<Exercises>

**问题 1**：“第29章 Browsing the Web and WebView”覆盖哪些正式节点和项目主线？
<Answer>覆盖10个节点，从“29. Browsing the Web and WebView”到“Challenge: Using the Back Button for Browser History”。主线是在隐式浏览器、WebView、自定义UI和Custom Tabs间按信任边界选择，并处理旋转与历史，交付物是URL策略、WebViewClient/WebChromeClient边界、旋转状态、后退历史和注入风险测试。</Answer>

**问题 2**：怎样建立本页最小可执行实验？
<Answer>固定JDK、SDK、Gradle、设备API、语言、权限和输入，完成一个从用户动作到可观察结果的垂直切片；保存干净构建、设备操作、原始日志、状态快照与断言。</Answer>

**问题 3**：为什么只在正常点击路径运行不能证明完成？
<Answer>它没有覆盖组件重建、进程死亡、异步取消、权限拒绝、外部输入失败、资源释放和版本变化。应逐项注入故障，并用URL策略、WebViewClient/WebChromeClient边界、旋转状态、后退历史和注入风险测试核对。</Answer>

**问题 4**：怎样设计能推翻当前实现的反例？
<Answer>保持其他变量不变，分别注入旋转、后台回收、进程死亡、拒权、断网、无效输入与重复操作；任何状态丢失、越权、崩溃、泄漏或重复副作用都推翻完成结论。</Answer>

**问题 5**：从第4版语境迁移到现代目标SDK时如何控制变量？
<Answer>先按原书还原机制并锁定基线，再查询目标SDK合同；一次只迁移组件导出、存储、权限、后台、通知或依赖之一，保存构建、行为、测试和回滚差异。</Answer>

**问题 6**：本页达到独立交接标准需要什么？
<Answer>需要版本与设备指纹、源码提交、输入、构建命令、生命周期轨迹、原始日志、失败测试、安全边界与发布产物，让他人凭URL策略、WebViewClient/WebChromeClient边界、旋转状态、后退历史和注入风险测试重放。</Answer>

</Exercises>

<Glossary>
  <GlossaryItem term="生命周期所有者">负责创建、取消并最终释放组件、任务或资源的明确作用域。</GlossaryItem>
  <GlossaryItem term="可恢复状态">在配置变化或进程重建后，可从保存事实重新构造的最小用户状态。</GlossaryItem>
  <GlossaryItem term="外部合同">Intent、文件、数据库、网络、系统服务或其他应用与本组件交换数据时的可验证约束。</GlossaryItem>
  <GlossaryItem term="版本边界">Android版本、targetSdk、设备形态、权限与依赖共同限定的行为适用范围。</GlossaryItem>
  <GlossaryItem term="证据链">由环境指纹、输入、操作、日志、状态快照和断言组成的可重放结论。</GlossaryItem>
</Glossary>

[← 上一页：第28章 Broadcast Intents](/learn/big-nerd-ranch-guide/background-network/broadcast-intents) · [下一页：第30章 Custom Views and Touch Events →](/learn/big-nerd-ranch-guide/advanced-ui-animation/custom-views-touch)

<Attribution adaptedFrom="Android Programming: The Big Nerd Ranch Guide, 4th Edition" adaptedUrl="https://www.oreilly.com/library/view/android-programming-the/9780135257555/" />
