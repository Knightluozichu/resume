---
title: Looper、Handler和HandlerThread
type: C
section: 后台与网络
order: 25
description: 深入理解 Android 消息队列机制，学会用 Handler 在任意两个线程之间传递消息，并掌握 Handler 内存泄漏的经典解法。
demo: true
math: false
sourceUrl: "https://www.oreilly.com/library/view/android-programming-the/9780135257555/"
draft: false
---

import { BnrLifecycleLab, BnrFailureLab, BnrEvidenceLab } from "@/components/mdx/big-nerd-ranch-guide/diagrams/looper-handler";

{/* C 实战型（《Android 编程权威指南》第25章改编）：重心在代码对照(§6)与常见误区(§7)。
   知识点清单：1)消息队列模型（Stepper动画掰碎）
   2)Looper/MessageQueue/Handler三角色（图示+Stepper）
   3)HandlerThread用法（完整代码）4)跨线程消息传递（代码对照）
   5)Handler内存泄漏与解法（static+WeakReference，Callout trap） */}

{/* 1. 学习目标（spec §一）：C 型必含「能改出 ___ 效果」 */}
<Objectives>

- 能解释“第25章 Loopers, Handlers, and HandlerThread”全部正式节点的组件所有者、生命周期、线程、状态来源与Android版本边界。
- 能实现“用MessageQueue、Looper、Handler和HandlerThread解释跨线程调度，并把下载器绑定到View生命周期”的最小垂直切片，并保存构建、设备操作、原始日志和断言。
- 能比较正常创建、旋转、后台、进程重建、权限拒绝与外部故障下的行为差异。
- 能设计推翻当前实现的反例，并凭缩略图下载状态机、消息轨迹、取消与缓存测试、StrictMode报告完成独立交接。

</Objectives>

{/* 2. 直觉引入（spec §二）：≤3 段 × ≤120 字；零术语；类比用工厂流水线 */}
## 为什么 Android 需要一套"消息队列"？

想象你是工厂的前台文员（主线程），忙得不可开交——接电话、接待客户、整理文件。每隔 30 秒还得去仓库跑一趟取货。你跑仓库的这 5 分钟里，电话没人接、客户干等着——整个前台停摆。老板让你改进：在仓库那边安排一个搬运工（后台线程），需要取货就把"取货申请单"塞进一个筐子。搬运工从筐子里一份份拿出来处理，货到了放回前台——前台照常运转，拿到货后才展示给客户。

这，就是 Android **消息队列**机制的直觉模型。筐子（MessageQueue）是线程间通信的中转站，搬运工（Looper）不停地从筐子里取任务处理，发申请单的（Handler）负责往筐子里投递消息。

没有这套机制，Android 的多线程通信会陷入混乱——你不知道该用什么方式安全地把数据从后台传到前台。

{/* 3. 概念讲解（spec §三） */}
## 消息队列三件套：Looper、MessageQueue、Handler

### 一条消息的完整生命

一条消息（Message）从发送到被处理，经历以下路径：

```
   Handler.sendMessage(msg)
        │
        ▼
   MessageQueue.enqueueMessage()      ← 消息入队，按时间戳排序
        │
        ▼
   Looper.loop()                      ← 无限循环，不停从队列取消息
        │
        ▼
   msg.target.dispatchMessage(msg)    ← target 就是发送它的 Handler
        │
        ▼
   Handler.handleMessage(msg)         ← 你在 handleMessage() 中写处理逻辑
```

{/* 消息循环三角色动画：Handler 入队 → Looper 从队头取出 → 分发 Handler.handleMessage 处理 → 队空阻塞 */}

<MessageLoopDiagram />

每个角色各司其职：

- <Term def="MessageQueue 是每个有 Looper 的线程内部的消息队列数据结构。它是一个按时间戳排序的优先队列——先到的不一定先处理，核心看时间戳。只负责存和取，不负责执行。">**MessageQueue（消息队列）**</Term>：一个按时间戳排序的优先队列。只有放消息和取消息两个操作，不负责执行。每个有 Looper 的线程自动带一个。

- <Term def="Looper 是一个线程的消息循环引擎。它调用 Looper.prepare() 初始化自己和 MessageQueue，然后调用 Looper.loop() 进入无限循环——不停地从 MessageQueue 取消息，分发给目标 Handler 的 handleMessage()。没有 Looper 的线程不能处理消息。">**Looper（消息泵）**</Term>：线程的消息循环引擎。`Looper.prepare()` 初始化消息队列，`Looper.loop()` 进入无限循环——不停地从消息队列里取消息，分发给目标 Handler。

- <Term def="Handler 是消息发送和处理的门面。它挂载在某个线程的 Looper 上——默认是创建它的那个线程的 Looper。通过它可以往关联线程的消息队列投递 Message 或 Runnable，消息处理回调 handleMessage() 也在关联线程上执行。">**Handler（消息收发器）**</Term>：消息发送和处理的门面。你通过它往关联线程的消息队列投递消息，消息到达时 `handleMessage()` 也在那个线程执行。

### 主线程为什么天然支持消息队列

你在主线程创建 Handler 不需要任何准备工作——因为 Android 在主线程启动时已经帮你调了 `Looper.prepareMainLooper()`。这也是为什么主线程永远不会退出：`Looper.loop()` 在无限循环，一旦退出，App 就死了。

```kotlin
// 主线程直接创建 Handler，不需要 prepare
val mainHandler = Handler(Looper.getMainLooper()) { msg ->
    when (msg.what) {
        1 -> textView.text = msg.obj as String
    }
    true
}
```

{/* 5. 交互 Demo（spec §五）：C 型用 Stepper 掰碎消息发送流程 */}
## 动手看：一条消息从"发出"到"被执行"

> 猜一猜：在后台线程创建一个不加 Looper 的 Handler，会怎么样？切到最后一步看答案。

<Stepper>
  <Step title="① 在后台线程准备消息循环">
    ```kotlin
    val backgroundThread = HandlerThread("BackgroundWorker").apply { start() }
    ```
    `HandlerThread` 是 Android 帮你封装好的——自带 Looper 和 MessageQueue 的后台线程。`start()` 后线程进入 `Looper.loop()` 循环，等待消息到来。
  </Step>
  <Step title="② 创建关联后台线程的 Handler">
    ```kotlin
    val backgroundHandler = Handler(backgroundThread.looper) { msg ->
        // 这里跑在 BackgroundWorker 线程，不是主线程！
        processData(msg.obj as ByteArray)
        true
    }
    ```
    Handler 构造时传入后台线程的 Looper，此后 `handleMessage()` 就在后台线程执行。
  </Step>
  <Step title="③ 从任意线程发送消息">
    ```kotlin
    // 主线程中发消息
    val msg = backgroundHandler.obtainMessage(1, byteArray)
    backgroundHandler.sendMessage(msg)

    // 或者用 post 发 Runnable
    backgroundHandler.post {
        // 这段代码跑在后台线程
    }
    ```
    `sendMessage()` 投递 Message，`post()` 投递 Runnable。两者最终都会被 `Looper.loop()` 取出并在后台线程执行。
  </Step>
  <Step title="④ 销毁消息循环">
    ```kotlin
    backgroundThread.quitSafely()
    ```
    跟普通 Thread 不同，HandlerThread 不会自己停止——`quitSafely()` 会处理完队列中剩余的消息再退出循环。
  </Step>
  <Step title="⑤ 常见错误：没有 Looper 就创建 Handler">
    ```kotlin
    Thread {
        // 直接创建 Handler → 崩溃！
        // RuntimeException: Can't create handler inside thread
        //     that has not called Looper.prepare()
        val handler = Handler()
    }.start()
    ```
    普通 Thread 没有 MessageQueue，没法创建 Handler。HandlerThread 或手动 `Looper.prepare()` 是解决之道。
  </Step>
</Stepper>

{/* 6. 代码对照（spec §六，C 型重心） */}
## 代码逐段拆解：跨线程通信的完整案例

### 场景：后台下载图片，主线程显示

```kotlin
class DownloadActivity : AppCompatActivity() {
    private lateinit var imageView: ImageView
    private lateinit var downloadThread: HandlerThread
    private lateinit var downloadHandler: Handler
    private val mainHandler = Handler(Looper.getMainLooper())

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_download)
        imageView = findViewById(R.id.imageView)

        // 1. 创建并启动带 Looper 的后台线程
        downloadThread = HandlerThread("ImageDownloader").apply { start() }
        downloadHandler = Handler(downloadThread.looper) { msg ->
            if (msg.what == MSG_DOWNLOAD) {
                val url = msg.obj as String
                val bitmap = downloadImage(url)      // 后台线程下载
                // 2. 下载完切回主线程更新 UI
                mainHandler.post {
                    imageView.setImageBitmap(bitmap)
                }
            }
            true
        }
    }

    private fun downloadImage(url: String): Bitmap {
        val client = OkHttpClient()
        val request = Request.Builder().url(url).build()
        val bytes = client.newCall(request).execute().body?.bytes()
        return BitmapFactory.decodeByteArray(bytes, 0, bytes?.size ?: 0)
    }

    companion object {
        private const val MSG_DOWNLOAD = 1
    }

    override fun onDestroy() {
        super.onDestroy()
        downloadThread.quitSafely()  // 3. 退出消息循环
    }
}
```

这段代码展示了经典的"双 Handler 模式"：
- `downloadHandler` 挂载在 HandlerThread，负责后台下载
- `mainHandler` 挂载在主线程，负责 UI 更新
- 两个 Handler 通过消息投递完成线程切换——这就是 Android 多线程通信的基本范式

{/* 7. 常见误区（spec §七，C 型 ≥3） */}
## 容易踩的坑

<Callout type="trap">
  **现象**：`Handler` 作为内部类创建，LeakCanary 报 "This Handler class should be static or leaks might occur"。
  **原因**：非静态内部类隐式持有外部 Activity 的引用。Handler 发送的延迟消息在到达前，Activity 已经销毁——但 Handler 还活着，Activity 的内存无法被 GC 回收。这就是经典的 Handler 内存泄漏。
  **修法**：将 Handler 声明为 `static` 内部类，用 `WeakReference<Activity>` 持有 Activity 引用。每次使用前检查 `get()` 是否为 null——如果 Activity 已被回收，直接丢弃消息不做处理。或者使用 `Handler.Callback` 接口模式，让 Handler 的回调通过接口注入，避免隐式持有外部引用。
</Callout>

<Callout type="trap">
  **现象**：HandlerThread 忘了 `quit()`，App 退出后进程仍然存活。
  **原因**：HandlerThread 的 `Looper.loop()` 是无限循环。Thread 的 `run()` 方法永远不会返回，即使 Activity 已经 `onDestroy()`。这个线程会一直占据内存直到进程被系统杀掉。
  **修法**：在 `onDestroy()` 或合适的生命周期回调中调用 `handlerThread.quitSafely()`。`quitSafely()` 跟 `quit()` 的区别：前者会等待队列中已存在的消息全部处理完再退出，后者直接丢弃所有待处理消息立即退出。
</Callout>

<Callout type="trap">
  **现象**：在 `handleMessage()` 里做了耗时操作，发现后续消息都被卡住了。
  **原因**：Looper 是单线程循环——同一时刻只处理一条消息。如果你在 `handleMessage()` 里睡 5 秒，那这 5 秒内队列里其他消息全在排队等待，包括主线程的 UI 绘制消息——界面直接卡死（老版 ANR ）。
  **修法**：`handleMessage()` 必须快速返回（&lt;16ms 最佳）。耗时任务应该投递到自己内部再起子线程处理，或使用 `WorkManager`（第 27 章）。不要把 Handler 的"线程切换"和"任务并行" 搞混了。
</Callout>

{/* 8. 小结与练习（spec §八） */}
## 小结

- Android 消息队列三件套：`Looper`（无限循环取消息）、`MessageQueue`（按时间排序存消息）、`Handler`（发送 + 处理消息）
- 主线程自带 Looper（`prepareMainLooper`），通过 Handler 可以实现任意线程之间的消息传递
- `HandlerThread` 是自带 Looper 的后台线程封装，适合需要排队执行的串行后台任务
- `static Handler + WeakReference<Activity>` 是解决 Handler 内存泄漏的经典范式
- 在 `handleMessage()` 里做耗时操作会阻塞整个消息队列——所有消息排队等，包括 UI 绘制

<Exercises>

**问题 1**：“第25章 Loopers, Handlers, and HandlerThread”覆盖哪些正式节点和项目主线？
<Answer>覆盖13个节点，从“25. Loopers, Handlers, and HandlerThread”到“Challenge: Preloading and Caching”。主线是用MessageQueue、Looper、Handler和HandlerThread解释跨线程调度，并把下载器绑定到View生命周期，交付物是缩略图下载状态机、消息轨迹、取消与缓存测试、StrictMode报告。</Answer>

**问题 2**：怎样建立本页最小可执行实验？
<Answer>固定JDK、SDK、Gradle、设备API、语言、权限和输入，完成一个从用户动作到可观察结果的垂直切片；保存干净构建、设备操作、原始日志、状态快照与断言。</Answer>

**问题 3**：为什么只在正常点击路径运行不能证明完成？
<Answer>它没有覆盖组件重建、进程死亡、异步取消、权限拒绝、外部输入失败、资源释放和版本变化。应逐项注入故障，并用缩略图下载状态机、消息轨迹、取消与缓存测试、StrictMode报告核对。</Answer>

**问题 4**：怎样设计能推翻当前实现的反例？
<Answer>保持其他变量不变，分别注入旋转、后台回收、进程死亡、拒权、断网、无效输入与重复操作；任何状态丢失、越权、崩溃、泄漏或重复副作用都推翻完成结论。</Answer>

**问题 5**：从第4版语境迁移到现代目标SDK时如何控制变量？
<Answer>先按原书还原机制并锁定基线，再查询目标SDK合同；一次只迁移组件导出、存储、权限、后台、通知或依赖之一，保存构建、行为、测试和回滚差异。</Answer>

**问题 6**：本页达到独立交接标准需要什么？
<Answer>需要版本与设备指纹、源码提交、输入、构建命令、生命周期轨迹、原始日志、失败测试、安全边界与发布产物，让他人凭缩略图下载状态机、消息轨迹、取消与缓存测试、StrictMode报告重放。</Answer>

</Exercises>

{/* 10. 名词解释（spec §十） */}
## 名词解释

<Glossary>
<GlossaryItem term="MessageQueue（消息队列）">
  每个有 Looper 的线程内部的消息存储结构。它是一个按时间戳排序的优先队列——一条消息什么时候该被执行，取决于它的 `when` 字段。只负责存和取，不负责执行。详见本章"消息队列三件套"一节。
</GlossaryItem>
<GlossaryItem term="Looper（消息泵）">
  线程的消息循环引擎。`Looper.prepare()` 创建 MessageQueue 并绑定当前线程，`Looper.loop()` 进入无限循环——不停从队列取消息，交给目标 Handler 处理。主线程的 Looper 由系统创建且永不退出。详见本章"消息队列三件套"一节。
</GlossaryItem>
<GlossaryItem term="Handler（消息收发器）">
  挂载在某个线程 Looper 上的消息发送和处理器。`sendMessage()` 往关联线程的消息队列投 Message，`handleMessage()` 在关联线程上执行。一个线程可以有多个 Handler。详见本章"消息队列三件套"一节。
</GlossaryItem>
<GlossaryItem term="HandlerThread">
  Android 封装好的自带 Looper + MessageQueue 的后台线程。不用手动 `Looper.prepare()` / `Looper.loop()`——`start()` 即进入消息循环。适合需要串行排队执行的后台任务。详见本章"HandlerThread"一节。
</GlossaryItem>
</Glossary>

<Attribution />


{/* BNR4_AUTHORITY_SUPPLEMENT */}

## 从生命周期、状态与证据开始

第4版不是API词典，而是六个逐步扩展的应用实验。每一章都要回答：用户从哪里进入，哪个Android组件拥有工作，回调在哪个线程发生，哪些事实必须跨旋转或进程死亡恢复，外部系统怎样失败，资源在何时释放。<Term def="负责创建、取消并最终释放组件、任务或资源的明确作用域">生命周期所有者</Term>把Activity、Fragment视图、ViewModel、Worker、线程与资源的寿命连接起来；没有所有者的异步任务就是潜在泄漏。

界面字段不是天然事实。<Term def="在配置变化或进程重建后，可从保存事实重新构造的最小用户状态">可恢复状态</Term>应根据数据大小与寿命放入saved instance state、ViewModel、数据库、文件或调度器，而不是全部塞进单例。Intent、文件、数据库、网络、系统服务和其他应用形成<Term def="跨边界交换数据时必须验证的action、类型、权限、大小、时序和失败约束">外部合同</Term>；正常响应只是合同中的一个样本。

本书以2019年第四版、Android 5.0至8.1及其后续兼容实践为语境。<Term def="Android版本、targetSdk、设备形态、权限与依赖共同限定的行为适用范围">版本边界</Term>要求先还原书中机制，再查询现代目标SDK的权限、后台、存储、通知和组件导出合同。<Term def="由环境指纹、输入、操作、日志、状态快照和断言组成的可重放结论">证据链</Term>则保证“在我的模拟器能跑”不会冒充可交付结论。

## 第4版正式目录逐节点映射

### 25. Loopers, Handlers, and HandlerThread

**正式节点 1/13。** 先说明“25. Loopers, Handlers, and HandlerThread”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“用MessageQueue、Looper、Handler和HandlerThread解释跨线程调度，并把下载器绑定到View生命周期”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Preparing RecyclerView to Display Images

**正式节点 2/13。** 先说明“Preparing RecyclerView to Display Images”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“用MessageQueue、Looper、Handler和HandlerThread解释跨线程调度，并把下载器绑定到View生命周期”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Preparing to Download Bytes from a URL

**正式节点 3/13。** 先说明“Preparing to Download Bytes from a URL”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“用MessageQueue、Looper、Handler和HandlerThread解释跨线程调度，并把下载器绑定到View生命周期”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Downloading Lots of Small Things

**正式节点 4/13。** 先说明“Downloading Lots of Small Things”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“用MessageQueue、Looper、Handler和HandlerThread解释跨线程调度，并把下载器绑定到View生命周期”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Assembling a Background Thread

**正式节点 5/13。** 先说明“Assembling a Background Thread”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“用MessageQueue、Looper、Handler和HandlerThread解释跨线程调度，并把下载器绑定到View生命周期”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Messages and Message Handlers

**正式节点 6/13。** 先说明“Messages and Message Handlers”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“用MessageQueue、Looper、Handler和HandlerThread解释跨线程调度，并把下载器绑定到View生命周期”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Listening to the View Lifecycle

**正式节点 7/13。** 先说明“Listening to the View Lifecycle”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“用MessageQueue、Looper、Handler和HandlerThread解释跨线程调度，并把下载器绑定到View生命周期”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Retained Fragments

**正式节点 8/13。** 先说明“Retained Fragments”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“用MessageQueue、Looper、Handler和HandlerThread解释跨线程调度，并把下载器绑定到View生命周期”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### For the More Curious: Solving the Image Downloading Problem

**正式节点 9/13。** 先说明“For the More Curious: Solving the Image Downloading Problem”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“用MessageQueue、Looper、Handler和HandlerThread解释跨线程调度，并把下载器绑定到View生命周期”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### For the More Curious: StrictMode

**正式节点 10/13。** 先说明“For the More Curious: StrictMode”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“用MessageQueue、Looper、Handler和HandlerThread解释跨线程调度，并把下载器绑定到View生命周期”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Challenge: Observing View LifecycleOwner LiveData

**正式节点 11/13。** 先说明“Challenge: Observing View LifecycleOwner LiveData”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“用MessageQueue、Looper、Handler和HandlerThread解释跨线程调度，并把下载器绑定到View生命周期”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Challenge: Improving ThumbnailDownloader's Lifecycle Awareness

**正式节点 12/13。** 先说明“Challenge: Improving ThumbnailDownloader's Lifecycle Awareness”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“用MessageQueue、Looper、Handler和HandlerThread解释跨线程调度，并把下载器绑定到View生命周期”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Challenge: Preloading and Caching

**正式节点 13/13。** 先说明“Challenge: Preloading and Caching”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“用MessageQueue、Looper、Handler和HandlerThread解释跨线程调度，并把下载器绑定到View生命周期”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

## 最小可执行切片

先把状态转换与Android副作用分开。下面的Kotlin骨架不替代本章代码，而是让旋转、进程恢复与失败输入可单测：

```kotlin
sealed interface ScreenState {
    data object Loading : ScreenState
    data class Ready(val key: String) : ScreenState
    data class Failed(val reason: String, val retryable: Boolean) : ScreenState
}

fun reduce(current: ScreenState, event: UiEvent): ScreenState = nextState(current, event)
```

固定JDK、SDK、Gradle、设备API、语言和输入，清理缓存后从命令行保存制品与日志：

```bash
./gradlew --no-daemon clean test assembleDebug
adb install -r app/build/outputs/apk/debug/app-debug.apk
adb shell am force-stop com.example.app
adb shell monkey -p com.example.app 1
adb logcat -d -v threadtime > run.log
```

先写行为断言，再执行用户操作和故障注入：

```gherkin
Given a pinned build, device API, locale, permission state, and input
When the owner is recreated after state saving or process death
Then durable facts reconstruct the same user-visible state
And cancelled work cannot update a destroyed owner
And denied permission, offline input, and malformed data remain recoverable
```

三段代码分别约束状态模型、环境重放和行为结果。真实项目还要加入静态检查、单元测试、仪器测试、无障碍检查和发布构建；日志只能支持断言，不能代替断言。

## 线程、状态与外部资源

主线程负责输入、生命周期分发和大部分UI工作，磁盘、网络、图片解码与长计算不能阻塞帧。后台结果返回前必须确认所有者仍有效；Fragment的生命周期与其View生命周期不是一回事。每个实现都应列出入口、线程、事实源、取消点、关闭责任和用户可见失败六项。

跨应用Intent、ContentProvider、WebView、网络响应、文件URI和深链都视为不可信输入。校验action、scheme、MIME、大小、授权与响应者；导出组件使用最小权限；日志、截图和测试数据库不得泄露令牌或个人数据。相机、音频、数据库、线程和回调注册必须在成功、异常与取消路径都释放。

性能以用户任务衡量：启动看可交互时刻，列表看掉帧与绑定分配，网络看超时和取消，后台工作看约束、电量与重复调度。每次比较固定设备温度、构建类型、数据规模和网络条件，先采基线再改变一个变量。

迁移现代目标SDK时，先在第4版语境中证明机制，再逐项处理组件导出、存储、通知、后台启动、权限、废弃API和依赖版本。每一步保存编译差异、行为差异、测试结果和回滚点；新API并不自动正确，旧API能编译也不代表仍符合平台政策。

## 三步验证实验

<Steps>
  <Step title="1. 建立目录节点与生命周期图">
    从“用MessageQueue、Looper、Handler和HandlerThread解释跨线程调度，并把下载器绑定到View生命周期”选一个用户任务，标出入口、组件、所有者、回调线程、可恢复状态和销毁点。先预测旋转、后台和进程恢复后哪些对象消失、哪些事实必须重建。
    <BnrLifecycleLab />
  </Step>
  <Step title="2. 注入生命周期与外部故障">
    依次改变生命周期阶段、网络和权限，每次只改变一个条件；记录原始日志、界面状态、持久事实与资源计数，区分预期失败和未知状态。
    <BnrFailureLab />
  </Step>
  <Step title="3. 通过交付证据门">
    用缩略图下载状态机、消息轨迹、取消与缓存测试、StrictMode报告核对版本设备、生命周期、持久状态、失败安全、测试和发布。缺少任何一项时，不得把正常点击路径视为完成。
    <BnrEvidenceLab />
  </Step>
</Steps>

<Callout type="trap">**核心陷阱。** 只复述类名与回调，未说明所有者、状态来源、线程、取消和失败后果。修复时回到用户任务和状态机，注入旋转、进程死亡、拒权、断网与无效输入。</Callout>

<Callout type="trap">**历史示例不等于当前平台政策。** 第4版准确反映出版时工具链；现代targetSdk可能改变组件导出、后台、存储、通知和权限。保留原书机制，再建立有证据的迁移层。</Callout>

<Callout type="warning">**真机与数据安全。** 使用测试账号、样本媒体和最小权限；清理日志、数据库与截图。签名密钥、API密钥、联系人和真实位置不得写入仓库或公开证据。</Callout>

## 本章回顾

本页从“25. Loopers, Handlers, and HandlerThread”覆盖到“Challenge: Preloading and Caching”，共13个正式目录节点。掌握标准不是记住API，而是能沿“用MessageQueue、Looper、Handler和HandlerThread解释跨线程调度，并把下载器绑定到View生命周期”解释组件与数据边界，运行正常、重建和失败实验，并让另一位开发者仅凭缩略图下载状态机、消息轨迹、取消与缓存测试、StrictMode报告重放结论。

<Exercises>

**问题 1**：“第25章 Loopers, Handlers, and HandlerThread”覆盖哪些正式节点和项目主线？
<Answer>覆盖13个节点，从“25. Loopers, Handlers, and HandlerThread”到“Challenge: Preloading and Caching”。主线是用MessageQueue、Looper、Handler和HandlerThread解释跨线程调度，并把下载器绑定到View生命周期，交付物是缩略图下载状态机、消息轨迹、取消与缓存测试、StrictMode报告。</Answer>

**问题 2**：怎样建立本页最小可执行实验？
<Answer>固定JDK、SDK、Gradle、设备API、语言、权限和输入，完成一个从用户动作到可观察结果的垂直切片；保存干净构建、设备操作、原始日志、状态快照与断言。</Answer>

**问题 3**：为什么只在正常点击路径运行不能证明完成？
<Answer>它没有覆盖组件重建、进程死亡、异步取消、权限拒绝、外部输入失败、资源释放和版本变化。应逐项注入故障，并用缩略图下载状态机、消息轨迹、取消与缓存测试、StrictMode报告核对。</Answer>

**问题 4**：怎样设计能推翻当前实现的反例？
<Answer>保持其他变量不变，分别注入旋转、后台回收、进程死亡、拒权、断网、无效输入与重复操作；任何状态丢失、越权、崩溃、泄漏或重复副作用都推翻完成结论。</Answer>

**问题 5**：从第4版语境迁移到现代目标SDK时如何控制变量？
<Answer>先按原书还原机制并锁定基线，再查询目标SDK合同；一次只迁移组件导出、存储、权限、后台、通知或依赖之一，保存构建、行为、测试和回滚差异。</Answer>

**问题 6**：本页达到独立交接标准需要什么？
<Answer>需要版本与设备指纹、源码提交、输入、构建命令、生命周期轨迹、原始日志、失败测试、安全边界与发布产物，让他人凭缩略图下载状态机、消息轨迹、取消与缓存测试、StrictMode报告重放。</Answer>

</Exercises>

<Glossary>
  <GlossaryItem term="**MessageQueue（消息队列）**">本页既有术语“**MessageQueue（消息队列）**”的上下文定义；验收时需说明其输入、所有者、生命周期与失败边界。</GlossaryItem>
  <GlossaryItem term="**Looper（消息泵）**">本页既有术语“**Looper（消息泵）**”的上下文定义；验收时需说明其输入、所有者、生命周期与失败边界。</GlossaryItem>
  <GlossaryItem term="**Handler（消息收发器）**">本页既有术语“**Handler（消息收发器）**”的上下文定义；验收时需说明其输入、所有者、生命周期与失败边界。</GlossaryItem>
  <GlossaryItem term="生命周期所有者">负责创建、取消并最终释放组件、任务或资源的明确作用域。</GlossaryItem>
  <GlossaryItem term="可恢复状态">在配置变化或进程重建后，可从保存事实重新构造的最小用户状态。</GlossaryItem>
  <GlossaryItem term="外部合同">Intent、文件、数据库、网络、系统服务或其他应用与本组件交换数据时的可验证约束。</GlossaryItem>
  <GlossaryItem term="版本边界">Android版本、targetSdk、设备形态、权限与依赖共同限定的行为适用范围。</GlossaryItem>
  <GlossaryItem term="证据链">由环境指纹、输入、操作、日志、状态快照和断言组成的可重放结论。</GlossaryItem>
</Glossary>

[← 上一页：第24章 HTTP and Background Tasks](/learn/big-nerd-ranch-guide/background-network/http-background) · [下一页：第26章 SearchView and SharedPreferences →](/learn/big-nerd-ranch-guide/background-network/search)

<Attribution adaptedFrom="Android Programming: The Big Nerd Ranch Guide, 4th Edition" adaptedUrl="https://www.oreilly.com/library/view/android-programming-the/9780135257555/" />
