---
title: 深入学习intent和任务
type: C
section: 后台与网络
order: 23
description: 掌握任务与返回栈的深层机制，能通过启动模式和Intent标志精确控制Activity的入栈行为。
demo: true
math: false
sourceUrl: "https://www.oreilly.com/library/view/android-programming-the/9780135257555/"
draft: false
qualityVersion: 2
practiceMode: simulation
sourceMode: independent-rewrite
---

import { BnrLifecycleLab, BnrFailureLab, BnrEvidenceLab } from "@/components/mdx/big-nerd-ranch-guide/diagrams/more-intents-tasks";

{/* C 实战型（《Android 编程权威指南》第23章改编）：重心在代码对照(§6)与常见误区(§7)，
   知识点清单：1)Task与返回栈（Stepper图示）2)四种启动模式（代码+图示对比）
   3)Intent标志位（代码对照）4)taskAffinity（代码演示）
   5)adb查看任务栈（实操演示） */}

{/* 1. 学习目标（spec §一）：C 型必含「能改出 ___ 效果」 */}
<Objectives>

- 能沿“深入学习intent和任务”的用户事件解释Android组件、状态所有者、线程与销毁边界。
- 能围绕“掌握任务与返回栈的深层机制，能通过启动模式和Intent标志精确控制Activity的入栈行为。”改出一个可运行结果，并用前后状态而非组件数量验收。
- 能在旋转、进程重建、拒权、离线或无效输入中选择适用反例，定位首个状态分叉。
- 能用Intent合同、解析目标、权限、返回结果和重复副作用独立重放结论，并标明第四版机制与现代targetSdk政策的边界。

</Objectives>

{/* 2. 直觉引入（spec §二）：≤3 段 × ≤120 字；零术语；类比用工厂流水线 */}
## 为什么需要理解"任务"？

你用过手机 App 的场景：从桌面点开微信 → 进聊天列表 → 点一个联系人 → 进去聊天。现在按返回键，你会逐层退出：回到聊天列表 → 再回到桌面。这个"一层层进去、一层层退出"的东西，就是 Android 的**任务返回栈**。

用**工厂流水线**类比：每个 App 就像一个独立车间，车间里有一叠待处理的工单（Activity 栈）。最上面的工单就是你现在看到的页面。来一个新工单（打开新页面），就压到最上面。处理完（按返回），就把最上面那张抽走，露出下一张。如果没有这套"工单叠放"机制，页面跳转会乱套——你不知道按返回键会回到哪里，页面可能在后台无限堆积占满内存。

这章要解决的核心问题：**当多个 App、多个页面交织跳转时，Android 怎么决定哪个页面进哪个栈？你作为开发者怎么精确控制这个行为？**

{/* 3. 概念讲解（spec §三） */}
## Task 和返回栈是怎么工作的

### Task 是什么

<Term def="Task 是 Android 用来组织一组相关 Activity 的容器，每个 Task 维护一个独立的返回栈（back stack）。用户感知到的就是一个「应用」——但实际上一个 App 可能跨越多个 Task，一个 Task 也可能包含来自不同 App 的 Activity。">Task（任务）</Term> 不是一个类或对象——它是一种抽象概念，是 Android 用来把一组 Activity 串成一条「用户操作路径」的容器。

每个 Task 内部维护一个**返回栈（Back Stack）**：后进先出（LIFO）。新 Activity 启动时被推到栈顶；用户按返回键时，栈顶 Activity 被弹出销毁，前一个 Activity 重新显示。

```
Task A 的返回栈：
┌────────────────────┐
│ ActivityC  (栈顶)   │ ← 用户当前看到的
├────────────────────┤
│ ActivityB           │
├────────────────────┤
│ ActivityA  (栈底)   │ ← Task 的根 Activity
└────────────────────┘
```

<Term def="启动模式（launch mode）是一种在 AndroidManifest 中配置的属性，控制 Activity 实例如何与 Task 和返回栈交互——是每次都新建、还是复用已有实例、还是独占一个 Task。">启动模式（Launch Mode）</Term> 和 <Term def="Intent 标志是一组在代码中动态设置的 flag（如 FLAG_ACTIVITY_NEW_TASK），可以更精细地控制 Activity 的启动行为，效果比 manifest 中声明的 launchMode 更灵活。">Intent 标志（Flag）</Term> 就是你控制这个"工单叠放"规则的两把钥匙——一个在清单文件中声明（静态），一个在代码中设置（动态）。

### 四种启动模式

Android 提供了四种启动模式，在 `AndroidManifest.xml` 中通过 `<activity>` 标签的 `android:launchMode` 属性指定：

| 启动模式 | 行为 | 典型场景 |
|---|---|---|
| `standard`（默认） | 每次启动都创建新实例，压入启动它的 Task 的栈顶 | 绝大多数普通页面 |
| `singleTop` | 如果要启动的 Activity 已在栈顶，则不创建新实例，直接复用（回调 `onNewIntent`）；否则行为同 standard | 搜索页面（避免重复打开） |
| `singleTask` | 系统中只保留一个实例。启动时清空它上面的所有 Activity，让它露出来成为栈顶。若不存在则创建 | App 主页面（MainActivity） |
| `singleInstance` | 跟 singleTask 类似，但更极端——这个 Activity 独占一个 Task，该 Task 中只有它一个 | 来电界面、闹钟响铃界面 |

{/* 四种 launchMode 对返回栈影响的并列对比静态图 */}

<LaunchModeDiagram />

{/* 5. 交互 Demo（spec §五）：C 型用 Stepper 分步演示四种模式的行为差异 */}
## 动手看：四种启动模式的行为区别

> 猜一猜：从 MainActivity(singleTask) 启动 DetailActivity(standard)，再从 DetailActivity 启动 MainActivity——返回栈里会有几个 MainActivity 实例？切到第 3 步看答案。

<Stepper>
  <Step title="① standard：每次都新建">
    假设当前栈是 `[Main → Detail]`。从 Detail 再次启动 Detail（standard）：系统创建一个新的 Detail 实例，栈变成 `[Main → Detail → Detail]`。按两次返回才能回到 Main。**这是最常见也最容易导致栈无限膨胀的模式。**
  </Step>
  <Step title="② singleTop：栈顶复用">
    栈为 `[Main → Search]`。从 Search 再次启动 Search（singleTop）：系统不创建新实例，而是调用 Search 的 `onNewIntent()`。栈保持 `[Main → Search]` 不变。**注意：如果 Search 不在栈顶（比如栈为 `[Main → Search → Detail]`）启动 Search，singleTop 跟 standard 一样——会创建新实例。**
  </Step>
  <Step title="③ singleTask：清栈复用">
    栈为 `[Main(singleTask) → Detail → Edit]`。从 Edit 启动 Main：系统把 Detail 和 Edit 全部出栈销毁，Main 回到栈顶。栈变成 `[Main]`。Main 的 `onNewIntent()` 被调用。**singleTask 是"整个 Task 里我只存在一个，且我永远在栈底附近"的保证。**
  </Step>
  <Step title="④ singleInstance：独占 Task">
    假设 App A 的栈是 `[A1 → A2]`。A2 启动来电界面 CallActivity（singleInstance）。系统创建一个全新的 Task B，CallActivity 独占其中。最近任务列表会出现两张"卡片"。按返回键：从 CallActivity 返回 → 回到 App A 的 A2（Task 切换，不是栈内返回）。
  </Step>
</Stepper>

{/* 6. 代码对照（spec §六，C 型重心——逐段讲解 ≥80% 关键代码） */}
## 代码逐段拆解：控制 Activity 的入栈

### 在 Manifest 中声明启动模式

```xml
<!-- AndroidManifest.xml -->
<activity
    android:name=".MainActivity"
    android:launchMode="singleTask">
    <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
    </intent-filter>
</activity>
```

这段代码把 MainActivity 设为 `singleTask`——意味着无论在 App 里的哪个页面，只要再次启动 MainActivity，它上面的所有 Activity 都会被清理掉。MainActivity 永远是这个 Task 的"根"或"唯一入口"。

### 用 Intent Flag 动态控制

有时你不想在 Manifest 里写死启动模式，而是在启动那一瞬间根据上下文决定行为。这时用 Intent flag：

```kotlin
// 从任何 Activity 中启动一个"新 Task"里的 Activity
val intent = Intent(this, TargetActivity::class.java).apply {
    flags = Intent.FLAG_ACTIVITY_NEW_TASK or Intent.FLAG_ACTIVITY_CLEAR_TOP
}
startActivity(intent)
```

`FLAG_ACTIVITY_NEW_TASK` 告诉系统：把 TargetActivity 放到一个新的 Task 里。`FLAG_ACTIVITY_CLEAR_TOP` 告诉系统：如果目标 Activity 已经存在于当前 Task 的栈中，把它上面的所有 Activity 清掉，然后复用这个实例。这两个 flag 组合起来的效果类似于 `singleTask`，但更灵活。

### taskAffinity：指定 Activity 在哪个 Task 里

```xml
<activity
    android:name=".SpecialActivity"
    android:taskAffinity="com.example.special"
    android:launchMode="singleTask" />
```

<Term def="taskAffinity 是 Activity 的一个属性，表示它「倾向于」属于哪个 Task。默认值等于应用的包名。当配合 FLAG_ACTIVITY_NEW_TASK 或 singleTask 使用时，taskAffinity 决定了 Activity 被放入哪个 Task——相同 affinity 的在一起，不同的则分到不同 Task。">`taskAffinity`（任务亲和性）</Term> 决定了 Activity "愿意"进入哪个 Task。默认情况下所有 Activity 的 taskAffinity 等于应用的包名，所以它们都在同一个 Task 里。

但如果你想让某个 Activity 进入一个独立的 Task（比如在最近任务列表里显示为一张单独的卡片），就用 `singleTask` + 独特的 `taskAffinity`。

```kotlin
// 用 adb 查看当前的任务栈结构
// 在终端执行：
// adb shell dumpsys activity activities | grep -A 5 "Stack #"
```

{/* 7. 常见误区（spec §七，C 型 ≥3） */}
## 容易踩的坑

<Callout type="trap">
  **现象**：把 MainActivity 设为 `singleTask`，发现从通知栏点通知进入 App 时，之前的页面全没了。
  **原因**：`singleTask` 的"清栈"行为不仅在你从 App 内部启动时生效，从外部（通知、deep link）启动同样会清空它上面的所有 Activity。用户正在填的表单、正在编辑的内容全部丢失。
  **修法**：如果目的是"不希望 MainActivity 有多个实例"而不是"每次都清空栈"，应该用 `singleTop` + `FLAG_ACTIVITY_SINGLE_TOP` 组合。或者在 `onNewIntent()` 中判断 intent 来源，决定是否真的需要清栈。
</Callout>

<Callout type="trap">
  **现象**：设置了 `FLAG_ACTIVITY_NEW_TASK` 但 Activity 还是进了当前 Task。
  **原因**：`FLAG_ACTIVITY_NEW_TASK` 单独使用时，只有当目标 Activity 的 `taskAffinity` 与当前 Task 不同时，才会真正创建新 Task。如果 taskAffinity 相同（默认都是包名），系统会把它放进当前 Task——等于没设。
  **修法**：真正需要新 Task 时，组合使用 `FLAG_ACTIVITY_NEW_TASK` + 为目标 Activity 设置不同的 `taskAffinity`。单独用 NEW_TASK 几乎没效果。
</Callout>

<Callout type="trap">
  **现象**：在 Service 或 BroadcastReceiver 等非 Activity 上下文中调用 `startActivity()` 时崩溃，错误为 `Calling startActivity() from outside of an Activity context`。
  **原因**：Android 要求从非 Activity 上下文启动 Activity 时，必须加 `FLAG_ACTIVITY_NEW_TASK`。因为 Service 没有自己的 Task，系统不知道该把新 Activity 放进哪个栈。
  **修法**：在 Service/BroadcastReceiver 中启动 Activity 时，永远加上 `intent.addFlags(Intent.FLAG_ACTIVITY_NEW_TASK)`。
</Callout>

{/* 8. 小结与练习（spec §八） */}
## 小结

- Task 是组织一组相关 Activity 的抽象容器，内部用返回栈（LIFO）管理页面跳转顺序
- 四种启动模式控制 Activity 实例的复用策略：`standard` 每次新建、`singleTop` 栈顶复用、`singleTask` 全栈唯一、`singleInstance` 独占 Task
- Intent flag 可动态覆盖启动模式：`NEW_TASK` 进入新 Task、`CLEAR_TOP` 清栈复用、`SINGLE_TOP` 等价于 singleTop
- `taskAffinity` 配合 `singleTask` 或 `NEW_TASK` 使用，决定 Activity 归属哪个 Task
- 从 Service/BroadcastReceiver 启动 Activity 必须加 `FLAG_ACTIVITY_NEW_TASK`

<Exercises>

**问题 1**：“第23章 More About Intents and Tasks”覆盖哪些正式节点和项目主线？

<Answer>覆盖9个节点，从“23. More About Intents and Tasks”到“Challenge: Icons”。主线是从隐式Intent解析到运行时显式Intent，观察任务、返回栈、Home角色和进程的不同边界，交付物是NerdLauncher、解析结果、任务栈轨迹、Home恢复和图标挑战。</Answer>

**问题 2**：怎样建立本页最小可执行实验？

<Answer>在“深入学习intent和任务”的验证中，固定JDK、SDK、Gradle、设备API、语言、权限和输入，完成一个从用户动作到可观察结果的垂直切片；保存干净构建、设备操作、原始日志、状态快照与断言。</Answer>

**问题 3**：为什么只在正常点击路径运行不能证明完成？

<Answer>它没有覆盖组件重建、进程死亡、异步取消、权限拒绝、外部输入失败、资源释放和版本变化。应逐项注入故障，并用NerdLauncher、解析结果、任务栈轨迹、Home恢复和图标挑战核对。</Answer>

**问题 4**：怎样设计能推翻当前实现的反例？

<Answer>在“深入学习intent和任务”的验证中，保持其他变量不变，分别注入旋转、后台回收、进程死亡、拒权、断网、无效输入与重复操作；任何状态丢失、越权、崩溃、泄漏或重复副作用都推翻完成结论。</Answer>

**问题 5**：从第4版语境迁移到现代目标SDK时如何控制变量？

<Answer>先按原书还原机制并锁定基线，再查询目标SDK合同；一次只迁移组件导出、存储、权限、后台、通知或依赖之一，保存构建、行为、测试和回滚差异。</Answer>

**问题 6**：本页达到独立交接标准需要什么？

<Answer>需要版本与设备指纹、源码提交、输入、构建命令、生命周期轨迹、原始日志、失败测试、安全边界与发布产物，让他人凭NerdLauncher、解析结果、任务栈轨迹、Home恢复和图标挑战重放。</Answer>

</Exercises>

{/* 10. 名词解释（spec §十） */}
## 名词解释

<Glossary>
<GlossaryItem term="Task（任务）">
  把一组相关的 Activity 串成一条「用户操作路径」的容器。你可以把它理解成一叠工单——最上面的工单是你当前看到的页面，按返回键就把最上面那张抽走。它是 Android 对"应用"这一用户概念的系统级抽象。详见本章"Task 是什么"一节。
</GlossaryItem>
<GlossaryItem term="启动模式（Launch Mode）">
  在 AndroidManifest 中声明的一种属性，控制 Activity 实例跟 Task、返回栈怎么互动。有四种：standard（每次都新建）、singleTop（栈顶复用）、singleTask（全栈唯一）、singleInstance（独占一个 Task）。详见本章"四种启动模式"一节。
</GlossaryItem>
<GlossaryItem term="Intent 标志（Flag）">
  在代码中通过 `intent.setFlags()` 设置的属性，可以动态控制 Activity 的启动行为。常见的如 FLAG_ACTIVITY_NEW_TASK（放进新 Task）、FLAG_ACTIVITY_CLEAR_TOP（清栈复用）。代码中设置的 flag 优先级高于 manifest 中声明的 launchMode。详见本章"Intent Flag"一节。
</GlossaryItem>
<GlossaryItem term="taskAffinity（任务亲和性）">
  Activity 的一种"归属感"属性，表示它倾向于进入哪个 Task。默认等于应用的包名。配合 singleTask 或 FLAG_ACTIVITY_NEW_TASK 使用时，决定 Activity 被分到哪个 Task。详见本章"taskAffinity"一节。
</GlossaryItem>
</Glossary>

{/* BNR4_QUALITY_V2 */}

“深入学习intent和任务”不使用未获授权的纸书正文；[InformIT出版信息](https://www.informit.com/store/android-programming-the-big-nerd-ranch-guide-9780135245125)与[授权电子版完整目录](https://www.oreilly.com/library/view/android-programming-the/9780135257555/)只用于确认第四版32章、269个正式目录节点和时代语境，[第四版官方勘误](https://github.com/bignerdranch/AndroidCourseResources/raw/master/4thEdition/Errata/4eAddendum.pdf)用于识别工具链变更。下列中文解释、图示、交互、代码与练习均为独立教学重写，平台行为再以[Android Developers](https://developer.android.com/develop)的一手文档复核。

## 为什么“深入学习intent和任务”必须回到可观察状态

“深入学习intent和任务”的学习结果不是记住类名，而是能预测“掌握任务与返回栈的深层机制，能通过启动模式和Intent标志精确控制Activity的入栈行为。”在一次输入、一次重建和一次失败中的不同状态，并指出哪条Android合同产生差异。

## 第四版机制逐项深读

### 23. More About Intents and Tasks

在“深入学习intent和任务”中，“23. More About Intents and Tasks”跨应用边界时只授予完成任务所需的URI与组件权限，并在日志中移除联系人等个人信息。

### Setting Up NerdLauncher

在“深入学习intent和任务”中，验证“Setting Up NerdLauncher”只改变一个生命周期或外部条件，保存操作、原始日志、状态快照和用户可见断言。

### Resolving an Implicit Intent

在“深入学习intent和任务”中，分析“Resolving an Implicit Intent”要区分Activity返回栈、系统task与进程，flags改变栈行为但不会自动提供业务幂等。

### Creating Explicit Intents at Runtime

在“深入学习intent和任务”中，分析“Creating Explicit Intents at Runtime”先冻结设备API、targetSdk和输入，再沿回调与数据流寻找首个状态分叉，不能只描述最终页面。

### Tasks and the Back Stack

在“深入学习intent和任务”中，分析“Tasks and the Back Stack”要区分Activity返回栈、系统task与进程，flags改变栈行为但不会自动提供业务幂等。

### Using NerdLauncher as a Home Screen

在“深入学习intent和任务”中，“Using NerdLauncher as a Home Screen”跨应用边界时只授予完成任务所需的URI与组件权限，并在日志中移除联系人等个人信息。

### For the More Curious: Processes vs Tasks

在“深入学习intent和任务”中，分析“For the More Curious: Processes vs Tasks”要区分Activity返回栈、系统task与进程，flags改变栈行为但不会自动提供业务幂等。

### For the More Curious: Concurrent Documents

在“深入学习intent和任务”中，“For the More Curious: Concurrent Documents”用于推翻“掌握任务与返回栈的深层机制，能通过启动模式和Intent标志精确控制Activity的入栈行为。”的顺利路径：先写预期，再引入一个边界输入、重建或平台差异，并用状态、日志与用户结果解释首个分叉。

### Challenge: Icons

在“深入学习intent和任务”中，分析“Challenge: Icons”要追踪资源ID、限定目录、密度缩放与属性解析，避免把预览器结果当成所有设备的合同。

## 可操作机制实验

“深入学习intent和任务”把“掌握任务与返回栈的深层机制，能通过启动模式和Intent标志精确控制Activity的入栈行为。在“深入学习intent和任务”的验证中，”拆成三个可观察层次：先选择目录节点并确认责任边界，再改变一个生命周期或外部条件，最后回到Intent合同、解析目标、权限、返回结果和重复副作用。点击后的文字、状态与轨迹必须同步变化，重置后回到相同基线。

<Steps>
  <Step title="1. 选择机制节点">
    选择一个正式节点，先预测它由谁拥有、在哪个线程执行、哪些状态需要恢复；右侧会显示本章专属的机制合同。

    <BnrLifecycleLab />

  </Step>
  <Step title="2. 注入一个边界条件">
    改变生命周期阶段、外部可用性或权限中的一项，观察回调、队列、数据或渲染结果怎样变化；不要用总风险分数代替因果。

    <BnrFailureLab />

  </Step>
  <Step title="3. 复位并核对证据">
    用Intent合同、解析目标、权限、返回结果和重复副作用核对预测，随后执行重置；若基线不能复现，实验本身不通过。

    <BnrEvidenceLab />

  </Step>
</Steps>

<Callout type="trap">在“深入学习intent和任务”中，正常点击成功只能证明一个样本。若重建、取消、无响应者、离线、旧schema或目标SDK变化会产生不同结果，就必须把该边界写进状态机与测试。</Callout>

<Callout type="trap">“深入学习intent和任务”若只留下最终截图，就无法判断旧所有者是否收到回调、后台任务是否重复、状态是否来自错误层或复位是否真正清空实验。</Callout>

## “深入学习intent和任务”验收回顾

“深入学习intent和任务”只有在Intent合同、解析目标、权限、返回结果和重复副作用能够从相同基线再次得到相同断言时才通过；第四版机制与现代平台政策分别记录，不用新API名称掩盖旧行为。

[← 上一页：XML drawable](/learn/big-nerd-ranch-guide/intent-data/xml-drawables) · [下一页：HTTP与后台任务 →](/learn/big-nerd-ranch-guide/background-network/http-background)

<Attribution
  mode="independent-rewrite"
  sourceBasis="authorized-sample"
  workTitle="Bill Phillips, Chris Stewart, Kristin Marsicano, Brian Gardner, Android Programming: The Big Nerd Ranch Guide, Fourth Edition"
  adaptedUrl="https://www.oreilly.com/library/view/android-programming-the/9780135257555/"
/>
