---
title: 搜索
type: C
section: 后台与网络
order: 26
description: 在 App 中添加搜索功能：使用 SearchView 控件、搜索建议提供器和 SharedPreferences 持久化搜索历史。
demo: true
math: false
sourceUrl: "https://www.oreilly.com/library/view/android-programming-the/9780135257555/"
draft: false
---

import { BnrLifecycleLab, BnrFailureLab, BnrEvidenceLab } from "@/components/mdx/big-nerd-ranch-guide/diagrams/search";

{/* C 实战型（《Android 编程权威指南》第26章改编）：重心在代码对照(§6)与常见误区(§7)。
   知识点清单：1)SearchView控件（XML+代码配置）2)searchable.xml配置
   3)搜索查询处理（onQueryTextSubmit）4)搜索建议（ContentProvider/SearchRecentSuggestionsProvider）
   5)SharedPreferences存储搜索历史（代码） */}

{/* 1. 学习目标（spec §一）：C 型必含「能改出 ___ 效果」 */}
<Objectives>

- 能解释“第26章 SearchView and SharedPreferences”全部正式节点的组件所有者、生命周期、线程、状态来源与Android版本边界。
- 能实现“把SearchView事件、查询持久化和Android KTX编辑连接成可恢复搜索体验”的最小垂直切片，并保存构建、设备操作、原始日志和断言。
- 能比较正常创建、旋转、后台、进程重建、权限拒绝与外部故障下的行为差异。
- 能设计推翻当前实现的反例，并凭查询状态表、SharedPreferences读写、进程恢复与界面打磨检查完成独立交接。

</Objectives>

{/* 2. 直觉引入（spec §二）：≤3 段 × ≤120 字；零术语 */}
## 为什么搜索不只是"一个输入框"？

你用手机 App 时几乎每天都在用它：微信搜聊天记录、设置里搜 Wi-Fi、地图搜地址。看起来只是一个输入框，但背后的用户期待远超一个普通的输入框——它应该在应用栏的位置、有语音输入按钮、输入时弹出历史建议、点击搜索后键盘自动收起且显示结果。

如果不用专门为搜索设计的控件，这些效果你都得自己造轮子：监听输入变化、维护历史列表、弹出建议弹窗、处理键盘收起……这就是为什么 Android 提供了一个完整的搜索框架——SearchView、SearchManager、建议提供器三者配合，把上面这些需求包装成一套开箱即用的方案。

这章要解决的核心问题：**怎么给你的 App 加一个专业级的搜索功能，而不是只扔一个 EditText 了事。**

{/* 3. 概念讲解（spec §三） */}
## SearchView 与搜索框架

### SearchView：应用栏里的专业搜索框

<Term def="SearchView 是 Android 提供的搜索专用输入控件。它不只是个带图标的输入框——它内建了搜索建议下拉列表、语音搜索按钮、自动收起键盘、以及跟 SearchManager 的深度集成。通常放在 Toolbar 或 ActionBar 中。">SearchView</Term> 是 Android 为搜索场景深度定制的一个控件。它比普通 EditText 多了这些能力：

- 搜索建议下拉列表（自动弹出、自动消失）
- 语音搜索按钮（如果你的设备支持）
- 与键盘的自动配合（提交后收起，不影响返回栈）
- 可配置的外观（图标化/展开、提示文字、搜索图标）

### searchable.xml：搜索配置宣言

Android 的搜索框架通过一个 XML 配置文件来声明搜索行为：

```xml
<!-- res/xml/searchable.xml -->
<?xml version="1.0" encoding="utf-8"?>
<searchable xmlns:android="http://schemas.android.com/apk/res/android"
    android:label="@string/app_name"
    android:hint="搜索联系人..."
    android:searchSuggestAuthority="com.example.app.SearchSuggestionProvider"
    android:searchSuggestSelection=" ?" />
```

这个文件告诉系统：这个 App 支持搜索、搜索建议由哪个 ContentProvider 提供、提示文字是什么。

### 处理搜索查询：onQueryTextSubmit

SearchView 通过 `OnQueryTextListener` 回调通知你搜索事件：

```kotlin
searchView.setOnQueryTextListener(object : SearchView.OnQueryTextListener {
    override fun onQueryTextSubmit(query: String?): Boolean {
        // 用户按下"搜索"按钮（键盘上的回车或放大镜图标）
        performSearch(query ?: "")
        return true
    }

    override fun onQueryTextChange(newText: String?): Boolean {
        // 用户每输入一个字符就回调一次——适合做即时过滤
        filterData(newText ?: "")
        return true
    }
})
```

`onQueryTextSubmit` 只在用户确认搜索时触发一次。`onQueryTextChange` 每次输入变化都触发——如果你的数据集很大，不要在这里做全量查询。

{/* 6. 代码对照（spec §六，C 型重心——逐段讲解） */}
## 代码逐段拆解：完整的搜索功能

### 第一步：在菜单中放置 SearchView

```xml
<!-- res/menu/fragment_main.xml -->
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/menu_item_search"
        android:title="搜索"
        app:actionViewClass="android.widget.SearchView"
        app:showAsAction="ifRoom|collapseActionView" />
</menu>
```

`app:actionViewClass="android.widget.SearchView"` 是关键——它告诉系统这个菜单项不是一个普通按钮，而是展开为一个 SearchView 控件。

### 第二步：在代码中获取并配置 SearchView

```kotlin
override fun onCreateOptionsMenu(menu: Menu, inflater: MenuInflater) {
    super.onCreateOptionsMenu(menu, inflater)
    inflater.inflate(R.menu.fragment_main, menu)

    val searchItem = menu.findItem(R.id.menu_item_search)
    val searchView = searchItem.actionView as SearchView

    searchView.apply {
        queryHint = "搜索联系人..."
        setOnQueryTextListener(object : SearchView.OnQueryTextListener {
            override fun onQueryTextSubmit(query: String?): Boolean {
                query?.let { performSearch(it) }
                return true
            }
            override fun onQueryTextChange(newText: String?): Boolean {
                newText?.let { filterList(it) }
                return true
            }
        })
    }
}
```

### 第三步：用 SharedPreferences 保存搜索历史

```kotlin
class SearchHistoryManager(context: Context) {
    private val prefs = context.getSharedPreferences("search_history", Context.MODE_PRIVATE)
    private val maxHistory = 10

    fun addQuery(query: String) {
        val currentList = getHistory().toMutableList()
        currentList.remove(query)  // 去重：已存在的先删掉
        currentList.add(0, query)  // 最新的放最前面
        if (currentList.size > maxHistory) {
            currentList.removeAt(currentList.lastIndex)
        }
        // 序列化为 JSON 字符串存入 SharedPreferences
        prefs.edit().putString(KEY_HISTORY, Gson().toJson(currentList)).apply()
    }

    fun getHistory(): List<String> {
        val json = prefs.getString(KEY_HISTORY, "[]") ?: "[]"
        val type = object : TypeToken<List<String>>() {}.type
        return Gson().fromJson(json, type)
    }

    companion object {
        private const val KEY_HISTORY = "queries"
    }
}
```

搜索历史的本质：一个去重 + 限制条数 + 按时间排序的列表，用 SharedPreferences 持久化。Gson 做序列化比手动拼接分隔符更可靠——你不会被用户输入的逗号或换行符搞崩解析逻辑。

<Stepper>
  <Step title="① 菜单声明 SearchView">
    在 `res/menu/` 中添加 `<item>`，设置 `app:actionViewClass="android.widget.SearchView"` 和 `app:showAsAction="ifRoom|collapseActionView"`。`actionViewClass` 是关键——它告诉系统把菜单项渲染成 SearchView 控件而非普通按钮。
  </Step>
  <Step title="② 在代码中配置 OnQueryTextListener">
    在 `onCreateOptionsMenu` 中通过 `menuItem.actionView as SearchView` 获取实例，调用 `setOnQueryTextListener` 绑定监听器。`onQueryTextSubmit` 在用户按搜索键时触发，`onQueryTextChange` 在每次输入变化时触发。
  </Step>
  <Step title="③ 过滤数据源">
    在 `onQueryTextChange` 回调中根据输入文字 `newText` 过滤本地数据（列表 `filter { }` 或 Room 数据库 `LIKE` 查询）。**建议加 300ms 防抖**——用 `handler.postDelayed` 延迟执行过滤，避免每输入一个字符都触发全量扫描。
  </Step>
  <Step title="④ 通知 Adapter 刷新界面">
    过滤结果通过 `adapter.submitList(filteredList)`（ListAdapter）或 `adapter.notifyDataSetChanged()`（RecyclerView.Adapter）更新列表。搜索提交（`onQueryTextSubmit`）后，调用 `searchView.clearFocus()` 和 `hideKeyboard()` 收起键盘，展示结果。
  </Step>
</Stepper>

{/* 搜索流程静态图：把上面四步串成「输入→监听→防抖→取数→刷新」整链路 */}

<SearchFlowDiagram />

{/* 7. 常见误区（spec §七，C 型 ≥3） */}
## 容易踩的坑

<Callout type="trap">
  **现象**：SearchView 展开后，按返回键没有收起 SearchView，而是退出了当前页面。
  **原因**：默认情况下 SearchView 展开不代表"进入了一个新界面"——按返回键会触发 Activity 的 `onBackPressed()`。如果你没有拦截处理，系统会按常规返回处理页面退出。
  **修法**：在 `onBackPressed()` 中检查 SearchView 是否展开：`if (!searchView.isIconified) { searchView.isIconified = true } else { super.onBackPressed() }`——先收起搜索框，已经收起状态才退出页面。
</Callout>

<Callout type="trap">
  **现象**：在 `onQueryTextChange()` 中做了耗时查询（如遍历大列表或数据库查询），输入时卡顿严重。
  **原因**：`onQueryTextChange` 在用户每输入一个字符时都会被调用。如果你在回调里遍历一个 1000 条数据的列表或执行 SQL 查询，每次按键都会阻塞主线程几十到几百毫秒。
  **修法**：用防抖（debounce）策略——将过滤操作延迟 300ms 执行，期间有新输入则重置计时器。或用 `RxJava / Flow.debounce()`。数据量大时应将查询移到后台线程（用上一章的 HandlerThread 模式）。
</Callout>

<Callout type="trap">
  **现象**：搜索历史用 SharedPreferences 直接频繁写入，旋转屏幕后历史丢失，或写入时 ANR。
  **原因**：SharedPreferences 的 `apply()` 虽然是异步写入磁盘，但首次加载时 `getSharedPreferences()` 会从磁盘解析 XML ——如果在主线程加载一个巨大的偏好文件，也造成卡顿。此外，`commit()` 方法是同步写盘，绝不能在主线程调。
  **修法**：始终用 `apply()` 而非 `commit()` 写入偏好。搜索历史这种"丢了无所谓、不影响核心业务"的数据，用 SharedPreferences 没问题。但如果是关键业务数据（如用户草稿），请用 Room 数据库。
</Callout>

{/* 8. 小结与练习（spec §八） */}
## 小结

- SearchView 是 Android 为搜索定制的控件，通过菜单配置 `app:actionViewClass` 集成到应用栏
- `searchable.xml` 声明搜索元数据（提示文字、建议提供器），`OnQueryTextListener` 处理查询提交和输入变化
- 搜索建议通过 ContentProvider 提供，`SearchRecentSuggestionsProvider` 可快速实现最近搜索记录
- SharedPreferences + Gson 序列化是一种轻量级的搜索历史存储方案——去重 + 限制条数 + 时间排序
- `onQueryTextChange` 回调频率极高，需要防抖或异步处理避免阻塞主线程

<Exercises>

**问题 1**：“第26章 SearchView and SharedPreferences”覆盖哪些正式节点和项目主线？
<Answer>覆盖7个节点，从“26. SearchView and SharedPreferences”到“Challenge: Polishing Your App Some More”。主线是把SearchView事件、查询持久化和Android KTX编辑连接成可恢复搜索体验，交付物是查询状态表、SharedPreferences读写、进程恢复与界面打磨检查。</Answer>

**问题 2**：怎样建立本页最小可执行实验？
<Answer>固定JDK、SDK、Gradle、设备API、语言、权限和输入，完成一个从用户动作到可观察结果的垂直切片；保存干净构建、设备操作、原始日志、状态快照与断言。</Answer>

**问题 3**：为什么只在正常点击路径运行不能证明完成？
<Answer>它没有覆盖组件重建、进程死亡、异步取消、权限拒绝、外部输入失败、资源释放和版本变化。应逐项注入故障，并用查询状态表、SharedPreferences读写、进程恢复与界面打磨检查核对。</Answer>

**问题 4**：怎样设计能推翻当前实现的反例？
<Answer>保持其他变量不变，分别注入旋转、后台回收、进程死亡、拒权、断网、无效输入与重复操作；任何状态丢失、越权、崩溃、泄漏或重复副作用都推翻完成结论。</Answer>

**问题 5**：从第4版语境迁移到现代目标SDK时如何控制变量？
<Answer>先按原书还原机制并锁定基线，再查询目标SDK合同；一次只迁移组件导出、存储、权限、后台、通知或依赖之一，保存构建、行为、测试和回滚差异。</Answer>

**问题 6**：本页达到独立交接标准需要什么？
<Answer>需要版本与设备指纹、源码提交、输入、构建命令、生命周期轨迹、原始日志、失败测试、安全边界与发布产物，让他人凭查询状态表、SharedPreferences读写、进程恢复与界面打磨检查重放。</Answer>

</Exercises>

{/* 10. 名词解释（spec §十） */}
## 名词解释

<Glossary>
<GlossaryItem term="SearchView">
  Android 为搜索场景深度定制的输入控件，内建搜索建议下拉列表、语音搜索按钮、与键盘自动配合。通常放在 Toolbar 中，通过 `app:actionViewClass="android.widget.SearchView"` 配置。详见本章"SearchView"一节。
</GlossaryItem>
<GlossaryItem term="SharedPreferences">
  Android 提供的一种轻量级键值对持久化存储方式。适合存储少量简单数据（如设置项、搜索历史），内部用 XML 文件存储。写入用 `apply()`（异步）或 `commit()`（同步，不推荐主线程）。详见本章"搜索历史"一节。
</GlossaryItem>
<GlossaryItem term="ContentProvider">
  Android 四大组件之一，用于管理结构化数据并提供跨应用/跨进程的数据访问接口。搜索建议就是一种特殊的 ContentProvider 应用——SearchView 通过 ContentResolver 调用 ContentProvider.query() 来获取建议列表。详见本章"搜索建议"一节。
</GlossaryItem>
</Glossary>

<Attribution />


{/* BNR4_AUTHORITY_SUPPLEMENT */}

## 从生命周期、状态与证据开始

第4版不是API词典，而是六个逐步扩展的应用实验。每一章都要回答：用户从哪里进入，哪个Android组件拥有工作，回调在哪个线程发生，哪些事实必须跨旋转或进程死亡恢复，外部系统怎样失败，资源在何时释放。<Term def="负责创建、取消并最终释放组件、任务或资源的明确作用域">生命周期所有者</Term>把Activity、Fragment视图、ViewModel、Worker、线程与资源的寿命连接起来；没有所有者的异步任务就是潜在泄漏。

界面字段不是天然事实。<Term def="在配置变化或进程重建后，可从保存事实重新构造的最小用户状态">可恢复状态</Term>应根据数据大小与寿命放入saved instance state、ViewModel、数据库、文件或调度器，而不是全部塞进单例。Intent、文件、数据库、网络、系统服务和其他应用形成<Term def="跨边界交换数据时必须验证的action、类型、权限、大小、时序和失败约束">外部合同</Term>；正常响应只是合同中的一个样本。

本书以2019年第四版、Android 5.0至8.1及其后续兼容实践为语境。<Term def="Android版本、targetSdk、设备形态、权限与依赖共同限定的行为适用范围">版本边界</Term>要求先还原书中机制，再查询现代目标SDK的权限、后台、存储、通知和组件导出合同。<Term def="由环境指纹、输入、操作、日志、状态快照和断言组成的可重放结论">证据链</Term>则保证“在我的模拟器能跑”不会冒充可交付结论。

## 第4版正式目录逐节点映射

### 26. SearchView and SharedPreferences

**正式节点 1/7。** 先说明“26. SearchView and SharedPreferences”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“把SearchView事件、查询持久化和Android KTX编辑连接成可恢复搜索体验”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Searching Flickr

**正式节点 2/7。** 先说明“Searching Flickr”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“把SearchView事件、查询持久化和Android KTX编辑连接成可恢复搜索体验”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Using SearchView

**正式节点 3/7。** 先说明“Using SearchView”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“把SearchView事件、查询持久化和Android KTX编辑连接成可恢复搜索体验”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Simple Persistence with SharedPreferences

**正式节点 4/7。** 先说明“Simple Persistence with SharedPreferences”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“把SearchView事件、查询持久化和Android KTX编辑连接成可恢复搜索体验”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Polishing Your App

**正式节点 5/7。** 先说明“Polishing Your App”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“把SearchView事件、查询持久化和Android KTX编辑连接成可恢复搜索体验”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Editing SharedPreferences with Android KTX

**正式节点 6/7。** 先说明“Editing SharedPreferences with Android KTX”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“把SearchView事件、查询持久化和Android KTX编辑连接成可恢复搜索体验”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Challenge: Polishing Your App Some More

**正式节点 7/7。** 先说明“Challenge: Polishing Your App Some More”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“把SearchView事件、查询持久化和Android KTX编辑连接成可恢复搜索体验”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

## 最小可执行切片

先把状态转换与Android副作用分开。下面的Kotlin骨架不替代本章代码，而是让旋转、进程恢复与失败输入可单测：

```kotlin
sealed interface ScreenState {
    data object Loading : ScreenState
    data class Ready(val key: String) : ScreenState
    data class Failed(val reason: String, val retryable: Boolean) : ScreenState
}

fun reduce(current: ScreenState, event: UiEvent): ScreenState = nextState(current, event)
```

固定JDK、SDK、Gradle、设备API、语言和输入，清理缓存后从命令行保存制品与日志：

```bash
./gradlew --no-daemon clean test assembleDebug
adb install -r app/build/outputs/apk/debug/app-debug.apk
adb shell am force-stop com.example.app
adb shell monkey -p com.example.app 1
adb logcat -d -v threadtime > run.log
```

先写行为断言，再执行用户操作和故障注入：

```gherkin
Given a pinned build, device API, locale, permission state, and input
When the owner is recreated after state saving or process death
Then durable facts reconstruct the same user-visible state
And cancelled work cannot update a destroyed owner
And denied permission, offline input, and malformed data remain recoverable
```

三段代码分别约束状态模型、环境重放和行为结果。真实项目还要加入静态检查、单元测试、仪器测试、无障碍检查和发布构建；日志只能支持断言，不能代替断言。

## 线程、状态与外部资源

主线程负责输入、生命周期分发和大部分UI工作，磁盘、网络、图片解码与长计算不能阻塞帧。后台结果返回前必须确认所有者仍有效；Fragment的生命周期与其View生命周期不是一回事。每个实现都应列出入口、线程、事实源、取消点、关闭责任和用户可见失败六项。

跨应用Intent、ContentProvider、WebView、网络响应、文件URI和深链都视为不可信输入。校验action、scheme、MIME、大小、授权与响应者；导出组件使用最小权限；日志、截图和测试数据库不得泄露令牌或个人数据。相机、音频、数据库、线程和回调注册必须在成功、异常与取消路径都释放。

性能以用户任务衡量：启动看可交互时刻，列表看掉帧与绑定分配，网络看超时和取消，后台工作看约束、电量与重复调度。每次比较固定设备温度、构建类型、数据规模和网络条件，先采基线再改变一个变量。

迁移现代目标SDK时，先在第4版语境中证明机制，再逐项处理组件导出、存储、通知、后台启动、权限、废弃API和依赖版本。每一步保存编译差异、行为差异、测试结果和回滚点；新API并不自动正确，旧API能编译也不代表仍符合平台政策。

## 三步验证实验

<Steps>
  <Step title="1. 建立目录节点与生命周期图">
    从“把SearchView事件、查询持久化和Android KTX编辑连接成可恢复搜索体验”选一个用户任务，标出入口、组件、所有者、回调线程、可恢复状态和销毁点。先预测旋转、后台和进程恢复后哪些对象消失、哪些事实必须重建。
    <BnrLifecycleLab />
  </Step>
  <Step title="2. 注入生命周期与外部故障">
    依次改变生命周期阶段、网络和权限，每次只改变一个条件；记录原始日志、界面状态、持久事实与资源计数，区分预期失败和未知状态。
    <BnrFailureLab />
  </Step>
  <Step title="3. 通过交付证据门">
    用查询状态表、SharedPreferences读写、进程恢复与界面打磨检查核对版本设备、生命周期、持久状态、失败安全、测试和发布。缺少任何一项时，不得把正常点击路径视为完成。
    <BnrEvidenceLab />
  </Step>
</Steps>

<Callout type="trap">**核心陷阱。** 只复述类名与回调，未说明所有者、状态来源、线程、取消和失败后果。修复时回到用户任务和状态机，注入旋转、进程死亡、拒权、断网与无效输入。</Callout>

<Callout type="trap">**历史示例不等于当前平台政策。** 第4版准确反映出版时工具链；现代targetSdk可能改变组件导出、后台、存储、通知和权限。保留原书机制，再建立有证据的迁移层。</Callout>

<Callout type="warning">**真机与数据安全。** 使用测试账号、样本媒体和最小权限；清理日志、数据库与截图。签名密钥、API密钥、联系人和真实位置不得写入仓库或公开证据。</Callout>

## 本章回顾

本页从“26. SearchView and SharedPreferences”覆盖到“Challenge: Polishing Your App Some More”，共7个正式目录节点。掌握标准不是记住API，而是能沿“把SearchView事件、查询持久化和Android KTX编辑连接成可恢复搜索体验”解释组件与数据边界，运行正常、重建和失败实验，并让另一位开发者仅凭查询状态表、SharedPreferences读写、进程恢复与界面打磨检查重放结论。

<Exercises>

**问题 1**：“第26章 SearchView and SharedPreferences”覆盖哪些正式节点和项目主线？
<Answer>覆盖7个节点，从“26. SearchView and SharedPreferences”到“Challenge: Polishing Your App Some More”。主线是把SearchView事件、查询持久化和Android KTX编辑连接成可恢复搜索体验，交付物是查询状态表、SharedPreferences读写、进程恢复与界面打磨检查。</Answer>

**问题 2**：怎样建立本页最小可执行实验？
<Answer>固定JDK、SDK、Gradle、设备API、语言、权限和输入，完成一个从用户动作到可观察结果的垂直切片；保存干净构建、设备操作、原始日志、状态快照与断言。</Answer>

**问题 3**：为什么只在正常点击路径运行不能证明完成？
<Answer>它没有覆盖组件重建、进程死亡、异步取消、权限拒绝、外部输入失败、资源释放和版本变化。应逐项注入故障，并用查询状态表、SharedPreferences读写、进程恢复与界面打磨检查核对。</Answer>

**问题 4**：怎样设计能推翻当前实现的反例？
<Answer>保持其他变量不变，分别注入旋转、后台回收、进程死亡、拒权、断网、无效输入与重复操作；任何状态丢失、越权、崩溃、泄漏或重复副作用都推翻完成结论。</Answer>

**问题 5**：从第4版语境迁移到现代目标SDK时如何控制变量？
<Answer>先按原书还原机制并锁定基线，再查询目标SDK合同；一次只迁移组件导出、存储、权限、后台、通知或依赖之一，保存构建、行为、测试和回滚差异。</Answer>

**问题 6**：本页达到独立交接标准需要什么？
<Answer>需要版本与设备指纹、源码提交、输入、构建命令、生命周期轨迹、原始日志、失败测试、安全边界与发布产物，让他人凭查询状态表、SharedPreferences读写、进程恢复与界面打磨检查重放。</Answer>

</Exercises>

<Glossary>
  <GlossaryItem term="生命周期所有者">负责创建、取消并最终释放组件、任务或资源的明确作用域。</GlossaryItem>
  <GlossaryItem term="可恢复状态">在配置变化或进程重建后，可从保存事实重新构造的最小用户状态。</GlossaryItem>
  <GlossaryItem term="外部合同">Intent、文件、数据库、网络、系统服务或其他应用与本组件交换数据时的可验证约束。</GlossaryItem>
  <GlossaryItem term="版本边界">Android版本、targetSdk、设备形态、权限与依赖共同限定的行为适用范围。</GlossaryItem>
  <GlossaryItem term="证据链">由环境指纹、输入、操作、日志、状态快照和断言组成的可重放结论。</GlossaryItem>
</Glossary>

[← 上一页：第25章 Loopers, Handlers, and HandlerThread](/learn/big-nerd-ranch-guide/background-network/looper-handler) · [下一页：第27章 WorkManager →](/learn/big-nerd-ranch-guide/background-network/workmanager)

<Attribution adaptedFrom="Android Programming: The Big Nerd Ranch Guide, 4th Edition" adaptedUrl="https://www.oreilly.com/library/view/android-programming-the/9780135257555/" />
