---
title: WorkManager
type: C
section: 后台与网络
order: 27
description: 使用 WorkManager 实现可靠的延迟后台任务：约束条件、任务链、输入输出、唯一任务，以及何时选择 WorkManager 而非 Thread 或 Service。
demo: true
math: false
sourceUrl: "https://www.oreilly.com/library/view/android-programming-the/9780135257555/"
draft: false
---

import { BnrLifecycleLab, BnrFailureLab, BnrEvidenceLab } from "@/components/mdx/big-nerd-ranch-guide/diagrams/workmanager";

{/* C 实战型（《Android 编程权威指南》第27章改编）：重心在代码对照(§6)与常见误区(§7)。
   知识点清单：1)WorkManager是什么（Stepper流程图）
   2)Worker类+dowork方法（代码）3)OneTimeWorkRequest/PeriodicWorkRequest（代码）
   4)Constraints约束（代码）5)Chaining工作链（代码）
   6)观察状态LiveData（代码）7)唯一任务enqueueUniqueWork（代码） */}

{/* 1. 学习目标（spec §一）：C 型必含「能改出 ___ 效果」 */}
<Objectives>

- 能解释“第27章 WorkManager”全部正式节点的组件所有者、生命周期、线程、状态来源与Android版本边界。
- 能实现“以Worker、约束、唯一调度、通知和用户控制表达可延期可靠工作”的最小垂直切片，并保存构建、设备操作、原始日志和断言。
- 能比较正常创建、旋转、后台、进程重建、权限拒绝与外部故障下的行为差异。
- 能设计推翻当前实现的反例，并凭轮询Worker、调度约束、重复任务控制、通知权限与取消测试完成独立交接。

</Objectives>

{/* 2. 直觉引入（spec §二）：≤3 段 × ≤120 字；零术语 */}
## 为什么需要"可靠的后台任务"？

你的 App 需要在后台做一件事：把用户编辑的笔记同步到服务器。如果用 `Thread { ... }` 来做，用户刚点完保存就关了 App、切了网络——线程直接被杀，同步中断，笔记丢失。你怎么办？自己写一套"重试机制"、"任务持久化存储"、"网络状态监听"？

Android 生态历史上出现过一堆解决方案——JobScheduler、AlarmManager、Firebase JobDispatcher、甚至老掉牙的 AsyncTask——它们各有各的坑：有的 API 版本限制、有的不保证执行、有的只对 Google Play Services 用户有效。

<Term def="WorkManager 是 Android Jetpack 中的后台任务调度库，专门解决「保证最终一定执行」这个硬需求。它能处理进程被杀后自动重试、根据网络/充电等条件决定执行时机、以及串联多个任务按顺序执行。">WorkManager</Term> 就是为了终结这场混乱而生的——它把"可靠地安排后台任务该什么时候做"这件事统一封装。你的任务不丢、不重复、在条件满足时按时执行——即使 App 被杀死、设备重启后也能恢复。

{/* 3. 概念讲解（spec §三） */}
## WorkManager 的工作机制

### 核心三角：Worker + WorkRequest + WorkManager

```kotlin
// 1. Worker：定义"做什么"
class SyncWorker(context: Context, params: WorkerParameters)
    : Worker(context, params) {
    override fun doWork(): Result {
        return try {
            syncNotesToServer()          // 执行同步逻辑
            Result.success()             // 成功 → 任务结束
        } catch (e: Exception) {
            Result.retry()               // 失败 → WorkManager 按退避策略自动重试
        }
    }
}

// 2. WorkRequest：定义"什么时候做"
val constraints = Constraints.Builder()
    .setRequiredNetworkType(NetworkType.CONNECTED)   // 需要网络
    .setRequiresCharging(true)                        // 需要充电
    .build()

val syncRequest = OneTimeWorkRequestBuilder<SyncWorker>()
    .setConstraints(constraints)
    .setInitialDelay(10, TimeUnit.MINUTES)           // 10 分钟后开始
    .build()

// 3. WorkManager：执行调度
WorkManager.getInstance(context).enqueue(syncRequest)
```

三者关系：`Worker` 定义任务逻辑（doWork），`WorkRequest` 定义任务的条件和时机（约束 + 延迟 + 重试），`WorkManager` 做统一调度（入队 + 选择执行时机 + 通知结果）。

{/* WorkManager 五段生命周期静态总览：定义 Worker → 约束 → 入队 → 保证执行 → 链式 */}

<WorkManagerDiagram />

<Term def="ONE_TIME 任务只执行一次；PERIODIC 任务按固定时间间隔重复执行——两者本质是 WorkRequest 的不同子类。" id="term-onetimeworkrequest">WorkRequest 有两种</Term>：`OneTimeWorkRequest`（执行一次）和 `PeriodicWorkRequest`（周期性重复，最小间隔 15 分钟）。

### 观察任务执行状态

WorkManager 提供 LiveData 接口让你观察任务进展：

```kotlin
WorkManager.getInstance(context)
    .getWorkInfoByIdLiveData(syncRequest.id)
    .observe(lifecycleOwner) { workInfo ->
        when (workInfo?.state) {
            WorkInfo.State.ENQUEUED -> showStatus("排队等待中...")
            WorkInfo.State.RUNNING -> showStatus("正在同步...")
            WorkInfo.State.SUCCEEDED -> showStatus("同步成功！")
            WorkInfo.State.FAILED -> showStatus("同步失败")
            WorkInfo.State.BLOCKED -> showStatus("等待前置任务完成...")
            else -> {}
        }
    }
```

{/* 5. 交互 Demo（spec §五）：C 型用 Stepper 掰碎任务生命周期 */}
## 动手看：一条 WorkRequest 的完整生命周期

> 猜一猜：如果用户在 SyncWorker 的 `doWork()` 执行期间杀掉了 App，同步会中断吗？

<Stepper>
  <Step title="① 入队（ENQUEUED）">
    `WorkManager.getInstance(context).enqueue(syncRequest)` 把任务注册到内部数据库。**这一步后任务已经持久化**——即使 App 进程被杀、设备重启，WorkManager 在下次启动时会从数据库中恢复任务。
  </Step>
  <Step title="② 等待约束（ENQUEUED）">
    任务被持久化后在后台等待。WorkManager 持续监测约束条件：`NetworkType.CONNECTED` 不满足 → 等待；`RequiresCharging` 不满足 → 继续等待。所有约束满足后才进入执行。
  </Step>
  <Step title="③ 开始执行（RUNNING）">
    WorkManager 内部使用一个后台线程池来执行 `Worker.doWork()`。`doWork()` 在后台线程运行——你不需要自己开线程。**关键：`doWork()` 的返回值必须在 10 分钟内返回，否则 Worker 被认为超时并重试。**
  </Step>
  <Step title="④ 成功（SUCCEEDED）或失败重试（FAILED → ENQUEUED）">
    `Result.success()` → 任务标记为 SUCCEEDED，从数据库中清理（或保留记录用于调试）。`Result.retry()` → 任务重新进入 ENQUEUED 状态，WorkManager 按退避算法（初始 30 秒，指数增长，最多 5 次）重新尝试执行。
  </Step>
  <Step title="⑤ 猜一猜的答案">
    用户在 Worker 执行期间杀进程：任务状态仍保存在 WorkManager 的数据库中。下次 App 启动时，WorkManager 会自动恢复未完成的 ENQUEUED/RUNNING 任务并重新执行——**这就是 WorkManager "可靠"的真正含义。**
  </Step>
</Stepper>

{/* 6. 代码对照（spec §六，C 型重心） */}
## 代码逐段拆解：任务链与数据传递

### 场景：先压缩图片，再上传到服务器

```kotlin
// 第 1 个 Worker：压缩图片
class CompressWorker(context: Context, params: WorkerParameters)
    : Worker(context, params) {
    override fun doWork(): Result {
        val imageUri = inputData.getString("IMAGE_URI") ?: return Result.failure()
        val compressedUri = compressImage(imageUri)

        // 输出压缩后的图片路径供下一个 Worker 使用
        val output = workDataOf("COMPRESSED_URI" to compressedUri)
        return Result.success(output)
    }
}

// 第 2 个 Worker：上传
class UploadWorker(context: Context, params: WorkerParameters)
    : Worker(context, params) {
    override fun doWork(): Result {
        val uri = inputData.getString("COMPRESSED_URI") ?: return Result.failure()
        return try {
            uploadToServer(uri)
            Result.success()
        } catch (e: Exception) {
            Result.retry()
        }
    }
}

// 串联执行：先压缩，成功后再上传
val inputData = workDataOf("IMAGE_URI" to photoUri.toString())

val compressRequest = OneTimeWorkRequestBuilder<CompressWorker>()
    .setInputData(inputData)
    .build()

val uploadRequest = OneTimeWorkRequestBuilder<UploadWorker>()
    .setConstraints(
        Constraints.Builder()
            .setRequiredNetworkType(NetworkType.CONNECTED)
            .build()
    )
    .build()

WorkManager.getInstance(context)
    .beginWith(compressRequest)     // 第一步：压缩
    .then(uploadRequest)            // 第二步：等压缩成功后上传
    .enqueue()
```

### 唯一任务：防止重复入队

```kotlin
WorkManager.getInstance(context)
    .enqueueUniqueWork(
        "daily-sync",                          // 唯一任务名
        ExistingWorkPolicy.REPLACE,            // 如果已存在同名任务，替换它
        syncRequest
    )
```

`ExistingWorkPolicy` 有三种策略：
- `REPLACE`：取消已有任务，用新的替代
- `KEEP`：如果已有任务在排队或执行，新的被丢弃
- `APPEND`：把新任务加在已有任务后面（形成隐式链）

{/* 7. 常见误区（spec §七，C 型 ≥3） */}
## 容易踩的坑

<Callout type="trap">
  **现象**：`PeriodicWorkRequest` 设了 5 分钟的间隔，测试时发现根本不按这个时间执行。
  **原因**：`PeriodicWorkRequest` 的**最小间隔是 15 分钟**——这是 WorkManager 为防止过度耗电设置的下限。你设置 5 分钟，系统自动提升到 15 分钟。此外，`PeriodicWorkRequest` 不保证精确时间——WorkManager 在间隔窗口内选择一个合适时机执行，而不是闹钟式的精确触发。
  **修法**：需要精确时间触发 = 这不是 WorkManager 的场景，用 `AlarmManager`。WorkManager 适合不严格要求精确时间的定期任务（如每 24 小时同步一次数据）。
</Callout>

<Callout type="trap">
  **现象**：Worker 的 `doWork()` 里执行了超过 10 分钟的任务，任务被标记为超时根本不重试。
  **原因**：WorkManager 2.3+ 要求 `doWork()` 在 10 分钟内返回。超时的 Worker 被视为失败并停止——不会像正常失败那样重试。
  **修法**：长时间任务用 `ListenableWorker`（Worker 的父类，支持异步），或用 `CoroutineWorker` + `setForeground()` 方法把任务提升为前台服务——后台限制更严格但支持长时间执行。超过 10 分钟的任务通常不是 WorkManager 的最佳场景。
</Callout>

<Callout type="trap">
  **现象**：用 WorkManager 做"立即执行"的任务，发现有时执行了几秒后才开始。
  **原因**：WorkManager 的设计目标是"可靠的延迟执行"，不是"立即后台执行"。任务的入队、约束检查、线程调度都有开销。它的优势是持久化重试和跨进程存活——而不是响应速度。
  **修法**：区分场景用工具：①需要立即后台执行 → `Coroutines` + `Dispatchers.IO`；②需要"保证执行，即使进程被杀" → WorkManager；③需要"长时间后台运行，用户可见" → `ForegroundService`。WorkManager 不替代 Thread/Coroutines，它解决的是"可靠性调度"问题。
</Callout>

{/* 8. 小结与练习（spec §八） */}
## 小结

- WorkManager 解决"可靠调度"：任务持久化在数据库中，进程被杀或设备重启后自动恢复
- 核心三角：`Worker`（定义做什么）→ `WorkRequest`（定义时机和条件）→ `WorkManager`（统一调度）
- `OneTimeWorkRequest` 一次执行，`PeriodicWorkRequest` 周期执行（最小间隔 15 分钟）
- `beginWith().then()` 构建任务链，`setInputData()` + `workDataOf()` 传递数据
- `enqueueUniqueWork()` 防止重复任务，`LiveData` + `getWorkInfoByIdLiveData()` 观察状态

<Exercises>

**问题 1**：“第27章 WorkManager”覆盖哪些正式节点和项目主线？
<Answer>覆盖6个节点，从“27. WorkManager”到“Providing User Control over Polling”。主线是以Worker、约束、唯一调度、通知和用户控制表达可延期可靠工作，交付物是轮询Worker、调度约束、重复任务控制、通知权限与取消测试。</Answer>

**问题 2**：怎样建立本页最小可执行实验？
<Answer>固定JDK、SDK、Gradle、设备API、语言、权限和输入，完成一个从用户动作到可观察结果的垂直切片；保存干净构建、设备操作、原始日志、状态快照与断言。</Answer>

**问题 3**：为什么只在正常点击路径运行不能证明完成？
<Answer>它没有覆盖组件重建、进程死亡、异步取消、权限拒绝、外部输入失败、资源释放和版本变化。应逐项注入故障，并用轮询Worker、调度约束、重复任务控制、通知权限与取消测试核对。</Answer>

**问题 4**：怎样设计能推翻当前实现的反例？
<Answer>保持其他变量不变，分别注入旋转、后台回收、进程死亡、拒权、断网、无效输入与重复操作；任何状态丢失、越权、崩溃、泄漏或重复副作用都推翻完成结论。</Answer>

**问题 5**：从第4版语境迁移到现代目标SDK时如何控制变量？
<Answer>先按原书还原机制并锁定基线，再查询目标SDK合同；一次只迁移组件导出、存储、权限、后台、通知或依赖之一，保存构建、行为、测试和回滚差异。</Answer>

**问题 6**：本页达到独立交接标准需要什么？
<Answer>需要版本与设备指纹、源码提交、输入、构建命令、生命周期轨迹、原始日志、失败测试、安全边界与发布产物，让他人凭轮询Worker、调度约束、重复任务控制、通知权限与取消测试重放。</Answer>

</Exercises>

{/* 10. 名词解释（spec §十） */}
## 名词解释

<Glossary>
<GlossaryItem term="WorkManager">
  Android Jetpack 中专门做"可靠后台任务调度"的库。任务存储在内部数据库中，进程被杀或重启后自动恢复。支持约束条件（网络、充电、空闲状态）、任务链、唯一任务、状态观察。是 JobScheduler / AlarmManager / AsyncTask 等老方案的系统性替代。详见本章"WorkManager 的工作机制"一节。
</GlossaryItem>
<GlossaryItem term="Worker">
  定义后台任务逻辑的抽象类。你需要继承 Worker 并实现 `doWork()` 方法，返回 `Result.success()`、`Result.failure()` 或 `Result.retry()`。`doWork()` 默认在后台线程执行，最长 10 分钟。详见本章"核心三角"一节。
</GlossaryItem>
<GlossaryItem term="OneTimeWorkRequest">
  只执行一次的后台任务请求。可以设置延迟（`setInitialDelay`）、约束条件（`setConstraints`）、输入数据（`setInputData`）。队列入队（enqueue）后由 WorkManager 统一调度执行时机。详见本章"核心三角"一节。
</GlossaryItem>
<GlossaryItem term="PeriodicWorkRequest">
  按固定时间间隔周期性重复执行的后台任务请求。**最小间隔 15 分钟**——这是 WorkManager 为防止过度耗电设置的系统下限。不保证精确执行时间，只在间隔窗口内选择一个合适时机执行。详见本章"容易踩的坑"一节。
</GlossaryItem>
</Glossary>

<Attribution />


{/* BNR4_AUTHORITY_SUPPLEMENT */}

## 从生命周期、状态与证据开始

第4版不是API词典，而是六个逐步扩展的应用实验。每一章都要回答：用户从哪里进入，哪个Android组件拥有工作，回调在哪个线程发生，哪些事实必须跨旋转或进程死亡恢复，外部系统怎样失败，资源在何时释放。<Term def="负责创建、取消并最终释放组件、任务或资源的明确作用域">生命周期所有者</Term>把Activity、Fragment视图、ViewModel、Worker、线程与资源的寿命连接起来；没有所有者的异步任务就是潜在泄漏。

界面字段不是天然事实。<Term def="在配置变化或进程重建后，可从保存事实重新构造的最小用户状态">可恢复状态</Term>应根据数据大小与寿命放入saved instance state、ViewModel、数据库、文件或调度器，而不是全部塞进单例。Intent、文件、数据库、网络、系统服务和其他应用形成<Term def="跨边界交换数据时必须验证的action、类型、权限、大小、时序和失败约束">外部合同</Term>；正常响应只是合同中的一个样本。

本书以2019年第四版、Android 5.0至8.1及其后续兼容实践为语境。<Term def="Android版本、targetSdk、设备形态、权限与依赖共同限定的行为适用范围">版本边界</Term>要求先还原书中机制，再查询现代目标SDK的权限、后台、存储、通知和组件导出合同。<Term def="由环境指纹、输入、操作、日志、状态快照和断言组成的可重放结论">证据链</Term>则保证“在我的模拟器能跑”不会冒充可交付结论。

## 第4版正式目录逐节点映射

### 27. WorkManager

**正式节点 1/6。** 先说明“27. WorkManager”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“以Worker、约束、唯一调度、通知和用户控制表达可延期可靠工作”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Creating a Worker

**正式节点 2/6。** 先说明“Creating a Worker”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“以Worker、约束、唯一调度、通知和用户控制表达可延期可靠工作”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Scheduling Work

**正式节点 3/6。** 先说明“Scheduling Work”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“以Worker、约束、唯一调度、通知和用户控制表达可延期可靠工作”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Checking for New Photos

**正式节点 4/6。** 先说明“Checking for New Photos”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“以Worker、约束、唯一调度、通知和用户控制表达可延期可靠工作”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Notifying the User

**正式节点 5/6。** 先说明“Notifying the User”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“以Worker、约束、唯一调度、通知和用户控制表达可延期可靠工作”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Providing User Control over Polling

**正式节点 6/6。** 先说明“Providing User Control over Polling”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“以Worker、约束、唯一调度、通知和用户控制表达可延期可靠工作”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

## 最小可执行切片

先把状态转换与Android副作用分开。下面的Kotlin骨架不替代本章代码，而是让旋转、进程恢复与失败输入可单测：

```kotlin
sealed interface ScreenState {
    data object Loading : ScreenState
    data class Ready(val key: String) : ScreenState
    data class Failed(val reason: String, val retryable: Boolean) : ScreenState
}

fun reduce(current: ScreenState, event: UiEvent): ScreenState = nextState(current, event)
```

固定JDK、SDK、Gradle、设备API、语言和输入，清理缓存后从命令行保存制品与日志：

```bash
./gradlew --no-daemon clean test assembleDebug
adb install -r app/build/outputs/apk/debug/app-debug.apk
adb shell am force-stop com.example.app
adb shell monkey -p com.example.app 1
adb logcat -d -v threadtime > run.log
```

先写行为断言，再执行用户操作和故障注入：

```gherkin
Given a pinned build, device API, locale, permission state, and input
When the owner is recreated after state saving or process death
Then durable facts reconstruct the same user-visible state
And cancelled work cannot update a destroyed owner
And denied permission, offline input, and malformed data remain recoverable
```

三段代码分别约束状态模型、环境重放和行为结果。真实项目还要加入静态检查、单元测试、仪器测试、无障碍检查和发布构建；日志只能支持断言，不能代替断言。

## 线程、状态与外部资源

主线程负责输入、生命周期分发和大部分UI工作，磁盘、网络、图片解码与长计算不能阻塞帧。后台结果返回前必须确认所有者仍有效；Fragment的生命周期与其View生命周期不是一回事。每个实现都应列出入口、线程、事实源、取消点、关闭责任和用户可见失败六项。

跨应用Intent、ContentProvider、WebView、网络响应、文件URI和深链都视为不可信输入。校验action、scheme、MIME、大小、授权与响应者；导出组件使用最小权限；日志、截图和测试数据库不得泄露令牌或个人数据。相机、音频、数据库、线程和回调注册必须在成功、异常与取消路径都释放。

性能以用户任务衡量：启动看可交互时刻，列表看掉帧与绑定分配，网络看超时和取消，后台工作看约束、电量与重复调度。每次比较固定设备温度、构建类型、数据规模和网络条件，先采基线再改变一个变量。

迁移现代目标SDK时，先在第4版语境中证明机制，再逐项处理组件导出、存储、通知、后台启动、权限、废弃API和依赖版本。每一步保存编译差异、行为差异、测试结果和回滚点；新API并不自动正确，旧API能编译也不代表仍符合平台政策。

## 三步验证实验

<Steps>
  <Step title="1. 建立目录节点与生命周期图">
    从“以Worker、约束、唯一调度、通知和用户控制表达可延期可靠工作”选一个用户任务，标出入口、组件、所有者、回调线程、可恢复状态和销毁点。先预测旋转、后台和进程恢复后哪些对象消失、哪些事实必须重建。
    <BnrLifecycleLab />
  </Step>
  <Step title="2. 注入生命周期与外部故障">
    依次改变生命周期阶段、网络和权限，每次只改变一个条件；记录原始日志、界面状态、持久事实与资源计数，区分预期失败和未知状态。
    <BnrFailureLab />
  </Step>
  <Step title="3. 通过交付证据门">
    用轮询Worker、调度约束、重复任务控制、通知权限与取消测试核对版本设备、生命周期、持久状态、失败安全、测试和发布。缺少任何一项时，不得把正常点击路径视为完成。
    <BnrEvidenceLab />
  </Step>
</Steps>

<Callout type="trap">**核心陷阱。** 只复述类名与回调，未说明所有者、状态来源、线程、取消和失败后果。修复时回到用户任务和状态机，注入旋转、进程死亡、拒权、断网与无效输入。</Callout>

<Callout type="trap">**历史示例不等于当前平台政策。** 第4版准确反映出版时工具链；现代targetSdk可能改变组件导出、后台、存储、通知和权限。保留原书机制，再建立有证据的迁移层。</Callout>

<Callout type="warning">**真机与数据安全。** 使用测试账号、样本媒体和最小权限；清理日志、数据库与截图。签名密钥、API密钥、联系人和真实位置不得写入仓库或公开证据。</Callout>

## 本章回顾

本页从“27. WorkManager”覆盖到“Providing User Control over Polling”，共6个正式目录节点。掌握标准不是记住API，而是能沿“以Worker、约束、唯一调度、通知和用户控制表达可延期可靠工作”解释组件与数据边界，运行正常、重建和失败实验，并让另一位开发者仅凭轮询Worker、调度约束、重复任务控制、通知权限与取消测试重放结论。

<Exercises>

**问题 1**：“第27章 WorkManager”覆盖哪些正式节点和项目主线？
<Answer>覆盖6个节点，从“27. WorkManager”到“Providing User Control over Polling”。主线是以Worker、约束、唯一调度、通知和用户控制表达可延期可靠工作，交付物是轮询Worker、调度约束、重复任务控制、通知权限与取消测试。</Answer>

**问题 2**：怎样建立本页最小可执行实验？
<Answer>固定JDK、SDK、Gradle、设备API、语言、权限和输入，完成一个从用户动作到可观察结果的垂直切片；保存干净构建、设备操作、原始日志、状态快照与断言。</Answer>

**问题 3**：为什么只在正常点击路径运行不能证明完成？
<Answer>它没有覆盖组件重建、进程死亡、异步取消、权限拒绝、外部输入失败、资源释放和版本变化。应逐项注入故障，并用轮询Worker、调度约束、重复任务控制、通知权限与取消测试核对。</Answer>

**问题 4**：怎样设计能推翻当前实现的反例？
<Answer>保持其他变量不变，分别注入旋转、后台回收、进程死亡、拒权、断网、无效输入与重复操作；任何状态丢失、越权、崩溃、泄漏或重复副作用都推翻完成结论。</Answer>

**问题 5**：从第4版语境迁移到现代目标SDK时如何控制变量？
<Answer>先按原书还原机制并锁定基线，再查询目标SDK合同；一次只迁移组件导出、存储、权限、后台、通知或依赖之一，保存构建、行为、测试和回滚差异。</Answer>

**问题 6**：本页达到独立交接标准需要什么？
<Answer>需要版本与设备指纹、源码提交、输入、构建命令、生命周期轨迹、原始日志、失败测试、安全边界与发布产物，让他人凭轮询Worker、调度约束、重复任务控制、通知权限与取消测试重放。</Answer>

</Exercises>

<Glossary>
  <GlossaryItem term="WorkRequest 有两种">本页既有术语“WorkRequest 有两种”的上下文定义；验收时需说明其输入、所有者、生命周期与失败边界。</GlossaryItem>
  <GlossaryItem term="生命周期所有者">负责创建、取消并最终释放组件、任务或资源的明确作用域。</GlossaryItem>
  <GlossaryItem term="可恢复状态">在配置变化或进程重建后，可从保存事实重新构造的最小用户状态。</GlossaryItem>
  <GlossaryItem term="外部合同">Intent、文件、数据库、网络、系统服务或其他应用与本组件交换数据时的可验证约束。</GlossaryItem>
  <GlossaryItem term="版本边界">Android版本、targetSdk、设备形态、权限与依赖共同限定的行为适用范围。</GlossaryItem>
  <GlossaryItem term="证据链">由环境指纹、输入、操作、日志、状态快照和断言组成的可重放结论。</GlossaryItem>
</Glossary>

[← 上一页：第26章 SearchView and SharedPreferences](/learn/big-nerd-ranch-guide/background-network/search) · [下一页：第28章 Broadcast Intents →](/learn/big-nerd-ranch-guide/background-network/broadcast-intents)

<Attribution adaptedFrom="Android Programming: The Big Nerd Ranch Guide, 4th Edition" adaptedUrl="https://www.oreilly.com/library/view/android-programming-the/9780135257555/" />
