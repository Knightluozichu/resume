---
title: Android应用的调试
type: C
section: 基础入门
order: 5
description: 掌握 Android Studio 调试工具——Logcat 日志过滤、断点调试、变量监视和堆栈追踪分析。
demo: true
math: false
sourceUrl: "https://www.oreilly.com/library/view/android-programming-the/9780135257555/"
draft: true
qualityVersion: 2
practiceMode: code
sourceMode: independent-rewrite
---

import { BnrLifecycleLab, BnrFailureLab, BnrEvidenceLab } from "@/components/mdx/big-nerd-ranch-guide/diagrams/debugging";

{/* 知识点清单：
   1) Logcat 过滤 level/tag/regex = Stepper
   2) 断点类型（普通/条件/异常）= 概念表
   3) 堆栈追踪读法 = 代码示例
   4) 复现→定位→验证 = Callout
   5) ANR 与主线程 = Callout
*/}

<Objectives>

- 能沿“Android应用的调试”的用户事件解释Android组件、状态所有者、线程与销毁边界。
- 能围绕“掌握 Android Studio 调试工具——Logcat 日志过滤、断点调试、变量监视和堆栈追踪分析。”改出一个可运行结果，并用前后状态而非组件数量验收。
- 能在旋转、进程重建、拒权、离线或无效输入中选择适用反例，定位首个状态分叉。
- 能用构建指纹、用户操作、状态快照、原始日志和行为断言独立重放结论，并标明第四版机制与现代targetSdk政策的边界。

</Objectives>

## Bug 来了——先看黑匣子

用户说「闪退」，你的第一步不是改代码，而是打开 <Term def="Android Studio 中显示系统和应用运行时日志的窗口，按时间与级别输出调试信息。">Logcat</Term>。配合断点和堆栈追踪，大多数问题能在十分钟内定位到文件和行号。

## Logcat 过滤

典型崩溃行：

```
E/AndroidRuntime: FATAL EXCEPTION: main
    java.lang.NullPointerException
    at com.example.MainActivity.onCreate(MainActivity.kt:42)
```

读法：**异常类型 → 你的包名 → 行号**。

{/* Logcat 知识点配图：一行日志拆成时间戳/PID-TID/级别/TAG/正文五段 + 级别图例与过滤方式 */}

<LogcatLineAnatomyDiagram />

<Stepper>
  <Step title="① 选择日志级别">
    在 Logcat 顶部下拉框将 Log Level 设为 **Error**（或 Warn+Error），过滤掉无关的 Debug 和 Verbose 输出。崩溃日志的优先级一定是 Error。
  </Step>
  <Step title="② 按包名过滤">
    在搜索框右侧选择 Package 过滤关键字为你的 `applicationId`（如 `com.example.criminalintent`），屏蔽系统和其他 App 的日志噪音。
  </Step>
  <Step title="③ 搜索 FATAL EXCEPTION">
    在搜索框输入 `FATAL EXCEPTION` 或 `AndroidRuntime`，定位崩溃的起始行。这一行包含异常类型和发生线程（通常是 main）。

    ```
    E/AndroidRuntime: FATAL EXCEPTION: main
        java.lang.NullPointerException
        at com.example.MainActivity.onCreate(MainActivity.kt:42)
    ```
  </Step>
  <Step title="④ 点击堆栈跳转">
    堆栈中第一个 `com.example` 开头的行即为你的崩溃点——点击蓝色文件名链接，Android Studio 直接跳转到 `MainActivity.kt:42`。若需要追踪变量值，在关键路径加 Log 打点。

    ```kotlin
    Log.d("MyTag", "clicked id=$id")
    Log.e("MyTag", "save failed", exception)
    ```
  </Step>
</Stepper>

<Callout type="tip">
  调试黄金法则：**复现 → 定位 → 验证**。先稳定复现，再 Logcat/断点定位，改完必须回归复现路径。
</Callout>

{/* 复现→定位→验证 知识点配图：调试闭环流程，验证未通过回边弹回复现 */}

<DebugLoopDiagram />

## 断点调试

| 类型 | 用途 |
|---|---|
| 普通断点 | 暂停查看变量 |
| 条件断点 | 如 `index == 99`，循环里只停一次 |
| 异常断点 | Run → View Breakpoints → Any exception，崩溃即停 |

## 堆栈追踪

从下往上读，**第一个你的包名**通常是根因：

```
at com.example.MainActivity.loadData(MainActivity.kt:42)  ← 根因
at com.example.MainActivity.onCreate(MainActivity.kt:18)
at android.app.Activity.performCreate(...)
```

<Callout type="trap">
  **ANR** 时看 Logcat 的 `ANR in` 和主线程堆栈——几乎总是主线程做了网络/磁盘 IO 或死锁。
</Callout>

<Callout type="trap">
  到处 `Log.d` 却不设 TAG 过滤 → 日志被淹没。统一 TAG 前缀（如 `CrimeApp`），Release 用 Timber 树关闭 Debug 日志。
</Callout>

<Callout type="trap">
  只看 `result.data` 不看 `resultCode` → 用户取消选择也被当成功。Activity Result 必须检查 `RESULT_OK`。
</Callout>

## 小结

- Crash → Logcat Error + 堆栈第一行自己的代码
- ANR → 主线程堆栈找阻塞点
- 断点：条件断点省时间，异常断点抓崩溃

<Exercises>

**问题 1**：“第5章 Debugging Android Apps”覆盖哪些正式节点和项目主线？

<Answer>覆盖5个节点，从“5. Debugging Android Apps”到“Challenge: Exploring the Profiler”。主线是从症状、异常栈和最后可信边界定位问题，并组合断点、Lint、Layout Inspector与Profiler，交付物是故障复现步骤、最小失败输入、栈帧解释和修复回归测试。</Answer>

**问题 2**：怎样建立本页最小可执行实验？

<Answer>在“Android应用的调试”的验证中，固定JDK、SDK、Gradle、设备API、语言、权限和输入，完成一个从用户动作到可观察结果的垂直切片；保存干净构建、设备操作、原始日志、状态快照与断言。</Answer>

**问题 3**：为什么只在正常点击路径运行不能证明完成？

<Answer>它没有覆盖组件重建、进程死亡、异步取消、权限拒绝、外部输入失败、资源释放和版本变化。应逐项注入故障，并用故障复现步骤、最小失败输入、栈帧解释和修复回归测试核对。</Answer>

**问题 4**：怎样设计能推翻当前实现的反例？

<Answer>在“Android应用的调试”的验证中，保持其他变量不变，分别注入旋转、后台回收、进程死亡、拒权、断网、无效输入与重复操作；任何状态丢失、越权、崩溃、泄漏或重复副作用都推翻完成结论。</Answer>

**问题 5**：从第4版语境迁移到现代目标SDK时如何控制变量？

<Answer>先按原书还原机制并锁定基线，再查询目标SDK合同；一次只迁移组件导出、存储、权限、后台、通知或依赖之一，保存构建、行为、测试和回滚差异。</Answer>

**问题 6**：本页达到独立交接标准需要什么？

<Answer>需要版本与设备指纹、源码提交、输入、构建命令、生命周期轨迹、原始日志、失败测试、安全边界与发布产物，让他人凭故障复现步骤、最小失败输入、栈帧解释和修复回归测试重放。</Answer>

</Exercises>

<Glossary>

<GlossaryItem term="Logcat">
  Android Studio 中显示系统和应用运行时日志的窗口，按时间与级别输出调试信息。排查闪退、异常的第一站。
</GlossaryItem>

<GlossaryItem term="ANR">
  Application Not Responding——主线程阻塞超约 5 秒，系统弹窗。常见原因：主线程 IO、死锁、过度布局。
</GlossaryItem>

<GlossaryItem term="堆栈追踪（Stack Trace）">
  异常时的方法调用链。从下往上读，定位第一个业务代码帧。
</GlossaryItem>

</Glossary>

{/* BNR4_QUALITY_V2 */}

“Android应用的调试”不使用未获授权的纸书正文；[InformIT出版信息](https://www.informit.com/store/android-programming-the-big-nerd-ranch-guide-9780135245125)与[授权电子版完整目录](https://www.oreilly.com/library/view/android-programming-the/9780135257555/)只用于确认第四版32章、269个正式目录节点和时代语境，[第四版官方勘误](https://github.com/bignerdranch/AndroidCourseResources/raw/master/4thEdition/Errata/4eAddendum.pdf)用于识别工具链变更。下列中文解释、图示、交互、代码与练习均为独立教学重写，平台行为再以[Android Developers](https://developer.android.com/develop)的一手文档复核。

## 为什么“Android应用的调试”必须回到可观察状态

“Android应用的调试”的学习结果不是记住类名，而是能预测“掌握 Android Studio 调试工具——Logcat 日志过滤、断点调试、变量监视和堆栈追踪分析。”在一次输入、一次重建和一次失败中的不同状态，并指出哪条Android合同产生差异。

## 第四版机制逐项深读

### 5. Debugging Android Apps

在“Android应用的调试”中，“5. Debugging Android Apps”从症状回溯首个异常帧、日志事件或状态分叉；修复证据必须包含可复现输入和失败前后的同一断言。

### Exceptions and Stack Traces

在“Android应用的调试”中，分析“Exceptions and Stack Traces”区分编译、运行时、布局、线程与资源问题，避免在后续连锁报错处停止定位。

### Android-Specific Debugging

在“Android应用的调试”中，“Android-Specific Debugging”从症状回溯首个异常帧、日志事件或状态分叉；修复证据必须包含可复现输入和失败前后的同一断言。

### Challenge: Exploring the Layout Inspector

在“Android应用的调试”中，“Challenge: Exploring the Layout Inspector”的视觉正确性必须与语义树一致；看得见但读屏无名称、焦点不可达或点击区过小仍判失败。

### Challenge: Exploring the Profiler

在“Android应用的调试”中，分析“Challenge: Exploring the Profiler”区分编译、运行时、布局、线程与资源问题，避免在后续连锁报错处停止定位。

## 可操作机制实验

“Android应用的调试”把“掌握 Android Studio 调试工具——Logcat 日志过滤、断点调试、变量监视和堆栈追踪分析。在“Android应用的调试”的验证中，”拆成三个可观察层次：先选择目录节点并确认责任边界，再改变一个生命周期或外部条件，最后回到构建指纹、用户操作、状态快照、原始日志和行为断言。点击后的文字、状态与轨迹必须同步变化，重置后回到相同基线。

<Steps>
  <Step title="1. 选择机制节点">
    选择一个正式节点，先预测它由谁拥有、在哪个线程执行、哪些状态需要恢复；右侧会显示本章专属的机制合同。

    <BnrLifecycleLab />

  </Step>
  <Step title="2. 注入一个边界条件">
    改变生命周期阶段、外部可用性或权限中的一项，观察回调、队列、数据或渲染结果怎样变化；不要用总风险分数代替因果。

    <BnrFailureLab />

  </Step>
  <Step title="3. 复位并核对证据">
    用构建指纹、用户操作、状态快照、原始日志和行为断言核对预测，随后执行重置；若基线不能复现，实验本身不通过。

    <BnrEvidenceLab />

  </Step>
</Steps>

<Callout type="trap">在“Android应用的调试”中，正常点击成功只能证明一个样本。若重建、取消、无响应者、离线、旧schema或目标SDK变化会产生不同结果，就必须把该边界写进状态机与测试。</Callout>

<Callout type="trap">“Android应用的调试”若只留下最终截图，就无法判断旧所有者是否收到回调、后台任务是否重复、状态是否来自错误层或复位是否真正清空实验。</Callout>

## “Android应用的调试”验收回顾

“Android应用的调试”只有在构建指纹、用户操作、状态快照、原始日志和行为断言能够从相同基线再次得到相同断言时才通过；第四版机制与现代平台政策分别记录，不用新API名称掩盖旧行为。

[← 上一页：UI状态的保存与恢复](/learn/big-nerd-ranch-guide/basics/ui-state-persistence) · [下一页：第二个Activity →](/learn/big-nerd-ranch-guide/basics/second-activity)

<Attribution
  mode="independent-rewrite"
  sourceBasis="authorized-sample"
  workTitle="Bill Phillips, Chris Stewart, Kristin Marsicano, Brian Gardner, Android Programming: The Big Nerd Ranch Guide, Fourth Edition"
  adaptedUrl="https://www.oreilly.com/library/view/android-programming-the/9780135257555/"
/>
