---
title: Android开发初体验
type: C
section: 基础入门
order: 1
description: 搭建 Android Studio 开发环境，创建并运行你的第一个 Android 应用，看懂项目结构中的每一部分。
demo: true
math: false
sourceUrl: "https://www.oreilly.com/library/view/android-programming-the/9780135257555/"
draft: true
qualityVersion: 2
practiceMode: code
sourceMode: independent-rewrite
---

import { BnrLifecycleLab, BnrFailureLab, BnrEvidenceLab } from "@/components/mdx/big-nerd-ranch-guide/diagrams/first-app";

{/* 知识点清单（动笔前先摊开，逐条标 viz 形态；术语首现用 <Term> 高亮，章末 <Glossary> 一一对应）：
   1) Android Studio 安装与项目创建 = Stepper 分步讲解
   2) 项目结构（manifests/java/res/Gradle Scripts）= 目录树示意图
   3) AndroidManifest.xml = 代码片段
   4) Activity 与布局文件 = 代码对照 + 图解
   5) 模拟器与真机运行 = Stepper 分步演示
   6) Gradle 构建系统 = 流程图
   7) APK 是什么 = 示意图
*/}

<Objectives>

- 能沿“Android开发初体验”的用户事件解释Android组件、状态所有者、线程与销毁边界。
- 能围绕“搭建 Android Studio 开发环境，创建并运行你的第一个 Android 应用，看懂项目结构中的每一部分。”改出一个可运行结果，并用前后状态而非组件数量验收。
- 能在旋转、进程重建、拒权、离线或无效输入中选择适用反例，定位首个状态分叉。
- 能用构建指纹、用户操作、状态快照、原始日志和行为断言独立重放结论，并标明第四版机制与现代targetSdk政策的边界。

</Objectives>

## 你的第一个 Android 应用——从零到跑起来

你以前用的手机 App，背后都是别人写的代码。这一章，你来做那个人——从下载工具、点几下按钮，到自己的手机上弹出一个新界面，全程不超过一小时。

这件事的核心是一条**工厂流水线**：你写 Kotlin 代码和 XML 布局（原料），经过一道叫 Gradle 的工位把所有东西打包压缩，最后输出一个叫 <Term def="Android Package，Android 应用的安装包文件，本质是一个 zip 压缩包，里面装着编译好的代码、资源文件、清单文件等所有应用运行需要的东西。">APK</Term> 的成品。把这个 APK 装进手机，点一下图标，你就看到了自己写的界面。

没有这条流水线会怎样？你写的几百行 Kotlin 代码永远只是一堆纯文本文件，手机一个字也不认识。这一章的每一节都在帮你把这条流水线走通一遍。

那条流水线的"成品"——APK——到底装了什么？把它剖开看一眼：

{/* APK 知识点：把 APK 画成剖开的 zip 容器，逐块标注用途 */}

<ApkAnatomyDiagram />

## 概念讲解

### Android Studio：你的工作台

<Term def="Google 官方提供的 Android 应用集成开发环境（IDE），基于 IntelliJ IDEA，内置了代码编辑器、模拟器、调试器、性能分析工具等所有开发 Android 应用需要的工具。">Android Studio</Term> 是你写 Android 应用的"主战场"。它集成了代码编辑器、模拟器、调试工具和 Gradle 构建系统——你不需要单独安装任何其他东西就能完整地开发、运行、调试一个 Android 应用。

下载安装 Android Studio 后，第一次启动会让你下载 Android SDK——这是一套包含 Android 各个版本系统镜像、开发工具的命令行工具集。**从 Android Studio 官网下载最新稳定版即可**，安装向导会自动帮你把 SDK 配好。

### 项目结构：三间房各管什么事

创建新项目时选"Empty Views Activity"，你会得到一个叫 `app` 的模块。打开左侧项目面板，把视图从 `Android` 切换到 `Project`，你会看到一个标准 Gradle 项目的完整目录树。核心的三个目录：

```
app/
├── manifests/
│   └── AndroidManifest.xml    ← 应用的"身份证"
├── java/
│   └── com.example.myapp/
│       └── MainActivity.kt    ← 你的 Kotlin 代码
└── res/
    ├── layout/
    │   └── activity_main.xml  ← 界面的样子
    ├── values/
    │   ├── strings.xml        ← 文字资源
    │   └── colors.xml         ← 颜色资源
    └── drawable/              ← 图片资源
```

{/* 项目结构知识点：目录树解剖图（每个节点右侧标注「装什么」） */}

<AndroidProjectStructureDiagram />

`manifests/` 里放的是 <Term def="每个 Android 应用必须包含的 XML 清单文件，向系统声明应用包含哪些组件（Activity、Service 等）、需要哪些权限、支持哪些设备特性。系统在安装应用前会先读这个文件。">AndroidManifest.xml</Term>——应用的"身份证"。它告诉 Android 系统：这个应用叫什么名字、图标是哪个、里面有哪些界面（Activity）、需要哪些权限（比如联网、拍照）。

`java/`（或 `kotlin/`）放你的 Kotlin 源代码。每个界面通常对应一个 <Term def="Android 四大组件之一，代表一个用户可以交互的屏幕。每个 Activity 负责一个界面，管理界面的创建、显示和用户交互。可以理解为应用中一个「页面」">Activity</Term> 类——`MainActivity` 就是你应用的第一个界面。

`res/` 是资源目录。`layout/` 里放布局 XML 文件，定义界面上按钮、文字摆在哪里、长什么样；`values/` 里放字符串、颜色等常量；`drawable/` 里放图片。这种把代码和资源**分开管理**的方式是 Android 的核心设计理念之一。

### Gradle：流水线的工位

你点"Run"按钮之后，背后真正干活的是 <Term def="Android 官方使用的自动化构建工具，负责把 Kotlin/Java 源码编译成字节码、再转换成 DEX 格式、打包资源文件、最终生成 APK。构建规则写在 build.gradle 文件里。">Gradle</Term>。它是把源码变成 APK 的**自动化流水线**：

1. **编译**：把 Kotlin 代码编译成 Java 字节码（`.class`），再转成 Android 专用的 DEX 格式（`.dex`）
2. **打包**：把 DEX 文件 + 编译后的资源 + AndroidManifest.xml 等打成一个未签名的 APK
3. **签名**：给 APK 加上数字签名（debug 模式下 Android Studio 自动用 debug 签名）
4. **安装**：通过 adb 把 APK 推送到模拟器或真机上安装

{/* Gradle 构建知识点：7 阶段流水线可控动画（依次点亮每个工位） */}

<GradleBuildPipelineDiagram />

Gradle 的配置写在 `build.gradle` 文件里。你有两个：项目根目录的（全局配置）和 `app/` 目录下的（当前模块配置）。`app/build.gradle` 里最重要的几项：

```kotlin
android {
    namespace = "com.example.myapp"
    compileSdk = 35

    defaultConfig {
        applicationId = "com.example.myapp"
        minSdk = 24
        targetSdk = 35
        versionCode = 1
        versionName = "1.0"
    }
}

dependencies {
    implementation("androidx.core:core-ktx:1.15.0")
    // ... 你的第三方库写在这里
}
```

`applicationId` 是应用的全球唯一标识，`minSdk` 是最低支持的 Android 版本，`dependencies` 里列出你依赖的第三方库。

## 动手：走通你的第一条流水线

下面创建一个新项目并运行它。每一步都配合实际操作。

> 猜一猜：点 Run 到手机上弹出界面，中间最快的步骤是哪一步？最慢的呢？做完下面全流程再看答案。

<Stepper>
  <Step title="① 创建新项目">
    打开 Android Studio → New Project → 选 "Empty Views Activity" → Next。
    在配置页填好：Name = "MyFirstApp"，Package name = "com.example.myfirstapp"（保持默认即可），Language = Kotlin，Minimum SDK = API 24。
    点 Finish，等待 Gradle 同步完成（底部状态栏显示 "Gradle sync finished"）。
  </Step>
  <Step title="② 认识 MainActivity 和布局文件">
    打开 `app/java/.../MainActivity.kt`，你会看到：

    ```kotlin
    class MainActivity : AppCompatActivity() {
        override fun onCreate(savedInstanceState: Bundle?) {
            super.onCreate(savedInstanceState)
            setContentView(R.layout.activity_main)
        }
    }
    ```

    `onCreate` 是 Activity 的入口，`setContentView(R.layout.activity_main)` 把布局文件加载进来。

    再打开 `app/res/layout/activity_main.xml`——这是界面的定义。里面有一个 `TextView`，显示 "Hello World!"。
  </Step>
  <Step title="③ 创建模拟器">
    点顶部工具栏的 Device Manager 图标（手机形状）→ Device Manager → 点 "+" → Create Virtual Device。
    选一个设备型号（推荐 Pixel 6），选系统镜像（推荐 Tiramisu API 33 或更高），一路 Next → Finish。
  </Step>
  <Step title="④ 运行！">
    工具栏上选中你刚创建的模拟器 → 点绿色三角形 ▶ Run。
    看底部 Build 面板：Gradle 开始编译、打包、安装——和上面讲的流水线完全一致。
    模拟器启动后，你会在屏幕上看到 "Hello World!"。
  </Step>
</Stepper>

<Callout type="tip">
  如果模拟器启动很慢，可以在 `app/build.gradle` 的 `android` 块里把 `compileSdk` 和 `targetSdk` 改成你已下载的最高版本（Tools → SDK Manager 查看）。另外，**首次运行 Gradle 会下载很多依赖**，耐心等它走完；第二次就快多了。
</Callout>

<Callout type="trap">
  **现象**：点 Run 后报错 `The application could not be installed: INSTALL_FAILED_INSUFFICIENT_STORAGE`。
  **原因**：模拟器存储空间不足——模拟器的系统分区或数据分区满了。
  **修法**：在 Device Manager 里找到该模拟器 → 点右侧下拉箭头 → Wipe Data，清除模拟器数据后重试。或者直接删掉这个模拟器重新建一个。
</Callout>

<Callout type="trap">
  **现象**：代码里 `R.layout.activity_main` 飘红，提示 `Unresolved reference: R`。
  **原因**：资源文件（比如布局 XML）有语法错误，导致 R 文件生成失败——Gradle 编译资源时出错，整个 R 类就没生成。
  **修法**：检查 `res/layout/` 下的 XML 文件有没有语法错误（最常见：标签忘闭合、属性名拼错）。打开 Build → Build Output 看详细错误信息。
</Callout>

## 代码逐段拆解

### MainActivity.kt：界面的大脑

```kotlin
package com.example.myfirstapp

import android.os.Bundle
import androidx.appcompat.app.AppCompatActivity

class MainActivity : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_main)
    }
}
```

逐行看：`package` 声明这个文件属于哪个包——和你在创建项目时填的包名一致。`import` 导入需要用到的类，`AppCompatActivity` 是 Android 官方兼容库提供的一个基类（兼容老版本 Android）。`class MainActivity : AppCompatActivity()` 声明你的 MainActivity **继承**自 AppCompatActivity——继承了它所有现成的功能，你只需要重写（override）你需要改的部分。

`onCreate(savedInstanceState: Bundle?)` 是 Activity 生命周期的第一个回调——系统创建你的界面时首先调它。`super.onCreate(savedInstanceState)` 先让父类把该做的初始化做了（**这条必须放第一行**），然后 `setContentView(R.layout.activity_main)` 把你写的布局 XML 加载进来，变成屏幕上可见的按钮和文字。

`R.layout.activity_main` 里的 `R` 是 Android 自动生成的一个类——你放进 `res/` 的每一个资源，Gradle 都会在 `R` 里生成一个对应的整数 ID，你的 Kotlin 代码通过这些 ID 来引用资源。

### activity_main.xml：界面的骨架

```xml
<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Hello World!"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
```

`ConstraintLayout` 是一种布局容器——它里面可以放各种控件（按钮、文字、图片），并通过"约束"来控制它们的位置。`android:layout_width="match_parent"` 意思是宽度撑满整个父容器（这里是屏幕）。`TextView` 是显示文字的控件，`wrap_content` 意思是"内容的尺寸多大我就多大"——文字有多宽，控件就多宽。

四个 `app:layout_constraint...` 属性把 TextView 的上下左右都约束在父容器中央——效果就是文字居中显示。

## 小结

- Android Studio + Gradle 构成一条自动化流水线：源码 → 编译 → DEX 转换 → 打包 → 签名 → APK
- 项目三间房：`manifests/` 是身份证，`java/` 是代码，`res/` 是资源——三个分开管理
- `MainActivity.onCreate()` 是应用的入口，`setContentView()` 把布局加载到屏幕
- `R.layout.activity_main` 是布局文件的 ID，Gradle 自动生成的 `R` 类管理所有资源引用
- 模拟器需要单独创建；首次运行 Gradle 较慢是正常现象

<Exercises>

**问题 1**：“第1章 Your First Android Application”覆盖哪些正式节点和项目主线？

<Answer>覆盖12个节点，从“1. Your First Android Application”到“Challenge: Customizing the Toast”。主线是从GeoQuiz最小切片理解资源、View对象、监听器、Toast、模拟器与构建产物，交付物是可重放的GeoQuiz构建日志、资源映射、界面断言和APK。</Answer>

**问题 2**：怎样建立本页最小可执行实验？

<Answer>在“Android开发初体验”的验证中，固定JDK、SDK、Gradle、设备API、语言、权限和输入，完成一个从用户动作到可观察结果的垂直切片；保存干净构建、设备操作、原始日志、状态快照与断言。</Answer>

**问题 3**：为什么只在正常点击路径运行不能证明完成？

<Answer>它没有覆盖组件重建、进程死亡、异步取消、权限拒绝、外部输入失败、资源释放和版本变化。应逐项注入故障，并用可重放的GeoQuiz构建日志、资源映射、界面断言和APK核对。</Answer>

**问题 4**：怎样设计能推翻当前实现的反例？

<Answer>在“Android开发初体验”的验证中，保持其他变量不变，分别注入旋转、后台回收、进程死亡、拒权、断网、无效输入与重复操作；任何状态丢失、越权、崩溃、泄漏或重复副作用都推翻完成结论。</Answer>

**问题 5**：从第4版语境迁移到现代目标SDK时如何控制变量？

<Answer>先按原书还原机制并锁定基线，再查询目标SDK合同；一次只迁移组件导出、存储、权限、后台、通知或依赖之一，保存构建、行为、测试和回滚差异。</Answer>

**问题 6**：本页达到独立交接标准需要什么？

<Answer>需要版本与设备指纹、源码提交、输入、构建命令、生命周期轨迹、原始日志、失败测试、安全边界与发布产物，让他人凭可重放的GeoQuiz构建日志、资源映射、界面断言和APK重放。</Answer>

</Exercises>

<Glossary>

<GlossaryItem term="APK">
  Android 应用的安装包文件，本质是一个 zip 压缩包。里面装着编译好的 .dex 字节码文件、res 目录下的所有资源、AndroidManifest.xml 清单文件、以及签名信息。把 APK 传进手机点安装，你的应用就出现在桌面上了。详见本章"你的第一个 Android 应用"一节。
</GlossaryItem>

<GlossaryItem term="Android Studio">
  Google 官方提供的 Android 集成开发环境，基于 JetBrains IntelliJ IDEA。集成了代码编辑、模拟器管理、Gradle 构建、Logcat 日志查看、Profiler 性能分析等所有开发必需的模块。免费下载使用。详见本章"Android Studio：你的工作台"一节。
</GlossaryItem>

<GlossaryItem term="AndroidManifest.xml">
  每个 Android 应用根目录下必须有的 XML 清单文件。它向 Android 系统声明：应用里有哪些组件（Activity、Service 等）、需要哪些权限（联网、相机等）、支持哪些设备特性、最低和目标 SDK 版本。系统在安装 APK 前会先解析这个文件。详见本章"项目结构"一节。
</GlossaryItem>

<GlossaryItem term="Activity">
  Android 四大应用组件之一，代表一个用户可以交互的屏幕。每一个界面（登录页、设置页、聊天列表等）都是一个 Activity。它管理界面的生命周期（创建→可见→暂停→销毁），处理用户输入。详见本章"MainActivity 与布局文件"一节。
</GlossaryItem>

<GlossaryItem term="Gradle">
  Android 官方使用的自动化构建工具。你点 Run 之后，Gradle 依次执行：编译 Kotlin → 转 DEX 字节码 → 打包资源和代码 → 签名 → 生成 APK。构建规则写在 build.gradle（Kotlin DSL）文件里。详见本章"Gradle：流水线的工位"一节。
</GlossaryItem>

</Glossary>

{/* BNR4_QUALITY_V2 */}

“Android开发初体验”不使用未获授权的纸书正文；[InformIT出版信息](https://www.informit.com/store/android-programming-the-big-nerd-ranch-guide-9780135245125)与[授权电子版完整目录](https://www.oreilly.com/library/view/android-programming-the/9780135257555/)只用于确认第四版32章、269个正式目录节点和时代语境，[第四版官方勘误](https://github.com/bignerdranch/AndroidCourseResources/raw/master/4thEdition/Errata/4eAddendum.pdf)用于识别工具链变更。下列中文解释、图示、交互、代码与练习均为独立教学重写，平台行为再以[Android Developers](https://developer.android.com/develop)的一手文档复核。

## 为什么“Android开发初体验”必须回到可观察状态

“Android开发初体验”的学习结果不是记住类名，而是能预测“搭建 Android Studio 开发环境，创建并运行你的第一个 Android 应用，看懂项目结构中的每一部分。”在一次输入、一次重建和一次失败中的不同状态，并指出哪条Android合同产生差异。

## 第四版机制逐项深读

### 1. Your First Android Application

在“Android开发初体验”中，“1. Your First Android Application”把源码、资源、清单和Gradle配置变成可安装APK；验证要从干净构建开始，保存variant、SDK、依赖与安装后的首帧，而不是只看IDE绿色三角。

### App Basics

在“Android开发初体验”中，验证“App Basics”只改变一个生命周期或外部条件，保存操作、原始日志、状态快照和用户可见断言。

### Creating an Android Project

在“Android开发初体验”中，“Creating an Android Project”把源码、资源、清单和Gradle配置变成可安装APK；验证要从干净构建开始，保存variant、SDK、依赖与安装后的首帧，而不是只看IDE绿色三角。

### Navigating in Android Studio

在“Android开发初体验”中，“Navigating in Android Studio”把源码、资源、清单和Gradle配置变成可安装APK；验证要从干净构建开始，保存variant、SDK、依赖与安装后的首帧，而不是只看IDE绿色三角。

### Laying Out the UI

在“Android开发初体验”中，“Laying Out the UI”若依赖第四版时期API，应分开说明原书机制与现代平台政策，并用Android官方文档核对迁移边界。

### From Layout XML to View Objects

在“Android开发初体验”中，“From Layout XML to View Objects”的视觉正确性必须与语义树一致；看得见但读屏无名称、焦点不可达或点击区过小仍判失败。

### Wiring Up Widgets

在“Android开发初体验”中，“Wiring Up Widgets”把约束、测量与布局落实为View树几何；用小屏、长文本与大字体验证，不能用单一模拟器截图证明适配。

### Making Toasts

在“Android开发初体验”中，分析“Making Toasts”先冻结设备API、targetSdk和输入，再沿回调与数据流寻找首个状态分叉，不能只描述最终页面。

### Running on the Emulator

在“Android开发初体验”中，“Running on the Emulator”服务于“搭建 Android Studio 开发环境，创建并运行你的第一个 Android 应用，看懂项目结构中的每一部分。”；解释要落到用户事件、Android所有者、状态变化、线程和可观察结果，并给出一个会推翻实现的反例。

### For the More Curious: The Android Build Process

在“Android开发初体验”中，“For the More Curious: The Android Build Process”把源码、资源、清单和Gradle配置变成可安装APK；验证要从干净构建开始，保存variant、SDK、依赖与安装后的首帧，而不是只看IDE绿色三角。

### Challenges

在“Android开发初体验”中，“Challenges”用于推翻“搭建 Android Studio 开发环境，创建并运行你的第一个 Android 应用，看懂项目结构中的每一部分。”的顺利路径：先写预期，再引入一个边界输入、重建或平台差异，并用状态、日志与用户结果解释首个分叉。

### Challenge: Customizing the Toast

在“Android开发初体验”中，“Challenge: Customizing the Toast”用于推翻“搭建 Android Studio 开发环境，创建并运行你的第一个 Android 应用，看懂项目结构中的每一部分。”的顺利路径：先写预期，再引入一个边界输入、重建或平台差异，并用状态、日志与用户结果解释首个分叉。

## 可操作机制实验

“Android开发初体验”把“搭建 Android Studio 开发环境，创建并运行你的第一个 Android 应用，看懂项目结构中的每一部分。在“Android开发初体验”的验证中，”拆成三个可观察层次：先选择目录节点并确认责任边界，再改变一个生命周期或外部条件，最后回到构建指纹、用户操作、状态快照、原始日志和行为断言。点击后的文字、状态与轨迹必须同步变化，重置后回到相同基线。

<Steps>
  <Step title="1. 选择机制节点">
    选择一个正式节点，先预测它由谁拥有、在哪个线程执行、哪些状态需要恢复；右侧会显示本章专属的机制合同。

    <BnrLifecycleLab />

  </Step>
  <Step title="2. 注入一个边界条件">
    改变生命周期阶段、外部可用性或权限中的一项，观察回调、队列、数据或渲染结果怎样变化；不要用总风险分数代替因果。

    <BnrFailureLab />

  </Step>
  <Step title="3. 复位并核对证据">
    用构建指纹、用户操作、状态快照、原始日志和行为断言核对预测，随后执行重置；若基线不能复现，实验本身不通过。

    <BnrEvidenceLab />

  </Step>
</Steps>

<Callout type="trap">在“Android开发初体验”中，正常点击成功只能证明一个样本。若重建、取消、无响应者、离线、旧schema或目标SDK变化会产生不同结果，就必须把该边界写进状态机与测试。</Callout>

<Callout type="trap">“Android开发初体验”若只留下最终截图，就无法判断旧所有者是否收到回调、后台任务是否重复、状态是否来自错误层或复位是否真正清空实验。</Callout>

## “Android开发初体验”验收回顾

“Android开发初体验”只有在构建指纹、用户操作、状态快照、原始日志和行为断言能够从相同基线再次得到相同断言时才通过；第四版机制与现代平台政策分别记录，不用新API名称掩盖旧行为。

[← 上一页：《Android Programming: The Big Nerd Ranch Guide（第4版）》权威学习地图](/learn/big-nerd-ranch-guide/00-official-learning-map/bnr4-official-learning-map) · [下一页：Android与MVC设计模式 →](/learn/big-nerd-ranch-guide/basics/android-mvc)

<Attribution
  mode="independent-rewrite"
  sourceBasis="authorized-sample"
  workTitle="Bill Phillips, Chris Stewart, Kristin Marsicano, Brian Gardner, Android Programming: The Big Nerd Ranch Guide, Fourth Edition"
  adaptedUrl="https://www.oreilly.com/library/view/android-programming-the/9780135257555/"
/>
