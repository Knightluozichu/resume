---
title: SDK版本与兼容性
type: C
section: 基础入门
order: 7
description: 理解 minSdk、targetSdk、compileSdk 的含义——如何在支持新 API 的同时兼容旧设备。
demo: true
math: false
sourceUrl: "https://www.oreilly.com/library/view/android-programming-the/9780135257555/"
draft: false
qualityVersion: 2
practiceMode: code
sourceMode: independent-rewrite
---

import { BnrLifecycleLab, BnrFailureLab, BnrEvidenceLab } from "@/components/mdx/big-nerd-ranch-guide/diagrams/sdk-compatibility";

{/* 知识点清单：
   1) compile / min / target 三版本 = 对照表
   2) Build.VERSION.SDK_INT 分支 = 代码
   3) AndroidX 向后移植 = Term
   4) targetSdk 行为变更 = Callout
   5) 权限与 API 级别 = Stepper
*/}

<Objectives>

- 能沿“SDK版本与兼容性”的用户事件解释Android组件、状态所有者、线程与销毁边界。
- 能围绕“理解 minSdk、targetSdk、compileSdk 的含义——如何在支持新 API 的同时兼容旧设备。”改出一个可运行结果，并用前后状态而非组件数量验收。
- 能在旋转、进程重建、拒权、离线或无效输入中选择适用反例，定位首个状态分叉。
- 能用构建指纹、用户操作、状态快照、原始日志和行为断言独立重放结论，并标明第四版机制与现代targetSdk政策的边界。

</Objectives>

## 你的 App 要跑在十年跨度上的设备

2015 年的手机和 2025 年的折叠屏，系统 API 差很多。<Term def="Gradle 中声明的最低可安装 Android 版本，低于此版本的设备无法从商店安装。">minSdk</Term> 决定「多老的机子能装」；<Term def="编译时使用的 SDK 版本，决定你能调用哪些 API 符号。">compileSdk</Term> 决定「你能写哪些新 API」；<Term def="向系统声明已适配的行为变更版本，影响权限、后台限制等策略。">targetSdk</Term> 告诉系统「请按哪一代规则管我」。

## 三个版本号

在 `build.gradle.kts`：

```kotlin
android {
    compileSdk = 35
    defaultConfig {
        minSdk = 26
        targetSdk = 35
    }
}
```

| 版本 | 含义 | 设低了会怎样 |
|---|---|---|
| compileSdk | 编译用 SDK | 无法用新 API / 编译警告 |
| minSdk | 最低安装版本 | 老设备装不了 |
| targetSdk | 行为兼容目标 | 系统用旧兼容模式，可能缺新安全特性 |

{/* 三版本钉在同一条 API Level 数轴上：min 装得上、target 按谁规则跑、compile 能写到多新 */}

<SdkVersionAxisDiagram />

## 运行时版本检查

```kotlin
if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.TIRAMISU) {
    registerForActivityResult(ActivityResultContracts.RequestPermission()) { granted ->
        if (granted) pickImage()
    }.launch(Manifest.permission.READ_MEDIA_IMAGES)
} else {
    // API 32 及以下用 READ_EXTERNAL_STORAGE
}
```

<Stepper>
  <Step title="① 查文档确认 API 级别">
    查阅 Android 官方文档，确认目标权限（如 `READ_MEDIA_IMAGES`）从哪个 API 级别引入（API 33 / TIRAMISU）。这决定了运行时分支的最低 SDK_INT 阈值。
  </Step>
  <Step title="② 根据 minSdk 写分支">
    如果你的 minSdk 低于权限引入的 API 级别（如 minSdk=26），必须用 `if (Build.VERSION.SDK_INT >= ...)` 做运行时分支，或使用 AndroidX 兼容 API。

    ```kotlin
    if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.TIRAMISU) {
        // API 33+：细粒度媒体权限
        requestPermissionLauncher.launch(Manifest.permission.READ_MEDIA_IMAGES)
    } else {
        // API 32 及以下：旧版存储权限
        requestPermissionLauncher.launch(Manifest.permission.READ_EXTERNAL_STORAGE)
    }
    ```
  </Step>
  <Step title="③ targetSdk 升至新权限要求">
    将 `targetSdk` 升级到至少等于新权限对应的 API 级别，并在 `AndroidManifest.xml` 中声明新权限、在代码中加入运行时申请逻辑。targetSdk 不够高时系统不会按新规则执行权限检查。
  </Step>
  <Step title="④ 在 minSdk 设备上回归">
    用 minSdk 版本的真机或模拟器跑完整流程——确保低版本走 else 分支时 UI 正常、权限弹窗显示、功能完整。
  </Step>
</Stepper>

<Callout type="tip">
  **AndroidX** 把新 API 行为 backport 到旧系统（如 `ContextCompat`、`ActivityCompat`）——优先用兼容 API，少写 if/else。
</Callout>

<Callout type="trap">
  targetSdk 长期不升 → Google Play 要求过期、用户设备上行为与新版系统不一致（后台限制、分区存储等突然「像坏了」）。
</Callout>

<Callout type="trap">
  compileSdk &lt; 使用的库要求 → 编译失败。升级 compileSdk 不等于必须升 minSdk，但需测 minSdk 设备。
</Callout>

## 小结

- compileSdk ≥ 你用的 API；minSdk = 业务决定的最低设备；targetSdk 尽量跟最新稳定版
- 新 API 用 SDK_INT 分支或 AndroidX
- 升 targetSdk 前读「行为变更」文档并全量测试

<Exercises>

**问题 1**：“第7章 Android SDK Versions and Compatibility”覆盖哪些正式节点和项目主线？

<Answer>覆盖6个节点，从“7. Android SDK Versions and Compatibility”到“Challenge: Limited Cheats”。主线是解释minSdk、targetSdk、compileSdk和运行时API检查各自约束，安全使用新API，交付物是SDK矩阵、文档证据、旧设备降级测试与版本报告。</Answer>

**问题 2**：怎样建立本页最小可执行实验？

<Answer>在“SDK版本与兼容性”的验证中，固定JDK、SDK、Gradle、设备API、语言、权限和输入，完成一个从用户动作到可观察结果的垂直切片；保存干净构建、设备操作、原始日志、状态快照与断言。</Answer>

**问题 3**：为什么只在正常点击路径运行不能证明完成？

<Answer>它没有覆盖组件重建、进程死亡、异步取消、权限拒绝、外部输入失败、资源释放和版本变化。应逐项注入故障，并用SDK矩阵、文档证据、旧设备降级测试与版本报告核对。</Answer>

**问题 4**：怎样设计能推翻当前实现的反例？

<Answer>在“SDK版本与兼容性”的验证中，保持其他变量不变，分别注入旋转、后台回收、进程死亡、拒权、断网、无效输入与重复操作；任何状态丢失、越权、崩溃、泄漏或重复副作用都推翻完成结论。</Answer>

**问题 5**：从第4版语境迁移到现代目标SDK时如何控制变量？

<Answer>先按原书还原机制并锁定基线，再查询目标SDK合同；一次只迁移组件导出、存储、权限、后台、通知或依赖之一，保存构建、行为、测试和回滚差异。</Answer>

**问题 6**：本页达到独立交接标准需要什么？

<Answer>需要版本与设备指纹、源码提交、输入、构建命令、生命周期轨迹、原始日志、失败测试、安全边界与发布产物，让他人凭SDK矩阵、文档证据、旧设备降级测试与版本报告重放。</Answer>

</Exercises>

<Glossary>

<GlossaryItem term="minSdk">
  应用支持的最低 Android 版本。低于此版本无法安装。
</GlossaryItem>

<GlossaryItem term="targetSdk">
  声明已测试的行为变更版本。影响权限模型、后台执行、存储访问等系统策略。
</GlossaryItem>

<GlossaryItem term="compileSdk">
  编译期 SDK 版本，决定可用的 API 与 lint 规则，不影响运行时「最低系统」。
</GlossaryItem>

</Glossary>

{/* BNR4_QUALITY_V2 */}

“SDK版本与兼容性”不使用未获授权的纸书正文；[InformIT出版信息](https://www.informit.com/store/android-programming-the-big-nerd-ranch-guide-9780135245125)与[授权电子版完整目录](https://www.oreilly.com/library/view/android-programming-the/9780135257555/)只用于确认第四版32章、269个正式目录节点和时代语境，[第四版官方勘误](https://github.com/bignerdranch/AndroidCourseResources/raw/master/4thEdition/Errata/4eAddendum.pdf)用于识别工具链变更。下列中文解释、图示、交互、代码与练习均为独立教学重写，平台行为再以[Android Developers](https://developer.android.com/develop)的一手文档复核。

## 为什么“SDK版本与兼容性”必须回到可观察状态

“SDK版本与兼容性”的学习结果不是记住类名，而是能预测“理解 minSdk、targetSdk、compileSdk 的含义——如何在支持新 API 的同时兼容旧设备。”在一次输入、一次重建和一次失败中的不同状态，并指出哪条Android合同产生差异。

## 第四版机制逐项深读

### 7. Android SDK Versions and Compatibility

在“SDK版本与兼容性”中，“7. Android SDK Versions and Compatibility”区分minSdk、targetSdk、compileSdk与运行设备版本；可调用性、行为变化和兼容库回退要分别验证，不能只凭编译通过。

### Android SDK Versions

在“SDK版本与兼容性”中，“Android SDK Versions”区分minSdk、targetSdk、compileSdk与运行设备版本；可调用性、行为变化和兼容库回退要分别验证，不能只凭编译通过。

### Compatibility and Android Programming

在“SDK版本与兼容性”中，“Compatibility and Android Programming”区分minSdk、targetSdk、compileSdk与运行设备版本；可调用性、行为变化和兼容库回退要分别验证，不能只凭编译通过。

### Using the Android Developer Documentation

在“SDK版本与兼容性”中，“Using the Android Developer Documentation”区分minSdk、targetSdk、compileSdk与运行设备版本；可调用性、行为变化和兼容库回退要分别验证，不能只凭编译通过。

### Challenge: Reporting the Device's Android Version

在“SDK版本与兼容性”中，“Challenge: Reporting the Device's Android Version”用于推翻“理解 minSdk、targetSdk、compileSdk 的含义——如何在支持新 API 的同时兼容旧设备。”的顺利路径：先写预期，再引入一个边界输入、重建或平台差异，并用状态、日志与用户结果解释首个分叉。

### Challenge: Limited Cheats

在“SDK版本与兼容性”中，“Challenge: Limited Cheats”区分minSdk、targetSdk、compileSdk与运行设备版本；可调用性、行为变化和兼容库回退要分别验证，不能只凭编译通过。

## 可操作机制实验

“SDK版本与兼容性”把“理解 minSdk、targetSdk、compileSdk 的含义——如何在支持新 API 的同时兼容旧设备。在“SDK版本与兼容性”的验证中，”拆成三个可观察层次：先选择目录节点并确认责任边界，再改变一个生命周期或外部条件，最后回到构建指纹、用户操作、状态快照、原始日志和行为断言。点击后的文字、状态与轨迹必须同步变化，重置后回到相同基线。

<Steps>
  <Step title="1. 选择机制节点">
    选择一个正式节点，先预测它由谁拥有、在哪个线程执行、哪些状态需要恢复；右侧会显示本章专属的机制合同。

    <BnrLifecycleLab />

  </Step>
  <Step title="2. 注入一个边界条件">
    改变生命周期阶段、外部可用性或权限中的一项，观察回调、队列、数据或渲染结果怎样变化；不要用总风险分数代替因果。

    <BnrFailureLab />

  </Step>
  <Step title="3. 复位并核对证据">
    用构建指纹、用户操作、状态快照、原始日志和行为断言核对预测，随后执行重置；若基线不能复现，实验本身不通过。

    <BnrEvidenceLab />

  </Step>
</Steps>

<Callout type="trap">在“SDK版本与兼容性”中，正常点击成功只能证明一个样本。若重建、取消、无响应者、离线、旧schema或目标SDK变化会产生不同结果，就必须把该边界写进状态机与测试。</Callout>

<Callout type="trap">“SDK版本与兼容性”若只留下最终截图，就无法判断旧所有者是否收到回调、后台任务是否重复、状态是否来自错误层或复位是否真正清空实验。</Callout>

## “SDK版本与兼容性”验收回顾

“SDK版本与兼容性”只有在构建指纹、用户操作、状态快照、原始日志和行为断言能够从相同基线再次得到相同断言时才通过；第四版机制与现代平台政策分别记录，不用新API名称掩盖旧行为。

[← 上一页：第二个Activity](/learn/big-nerd-ranch-guide/basics/second-activity) · [下一页：UI fragment与fragment管理器 →](/learn/big-nerd-ranch-guide/ui-fragment/ui-fragments)

<Attribution
  mode="independent-rewrite"
  sourceBasis="authorized-sample"
  workTitle="Bill Phillips, Chris Stewart, Kristin Marsicano, Brian Gardner, Android Programming: The Big Nerd Ranch Guide, Fourth Edition"
  adaptedUrl="https://www.oreilly.com/library/view/android-programming-the/9780135257555/"
/>
