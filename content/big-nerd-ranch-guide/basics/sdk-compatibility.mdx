---
title: SDK版本与兼容性
type: C
section: 基础入门
order: 7
description: 理解 minSdk、targetSdk、compileSdk 的含义——如何在支持新 API 的同时兼容旧设备。
demo: true
math: false
sourceUrl: "https://www.oreilly.com/library/view/android-programming-the/9780135257555/"
draft: false
---

import { BnrLifecycleLab, BnrFailureLab, BnrEvidenceLab } from "@/components/mdx/big-nerd-ranch-guide/diagrams/sdk-compatibility";

{/* 知识点清单：
   1) compile / min / target 三版本 = 对照表
   2) Build.VERSION.SDK_INT 分支 = 代码
   3) AndroidX 向后移植 = Term
   4) targetSdk 行为变更 = Callout
   5) 权限与 API 级别 = Stepper
*/}

<Objectives>

- 能解释“第7章 Android SDK Versions and Compatibility”全部正式节点的组件所有者、生命周期、线程、状态来源与Android版本边界。
- 能实现“解释minSdk、targetSdk、compileSdk和运行时API检查各自约束，安全使用新API”的最小垂直切片，并保存构建、设备操作、原始日志和断言。
- 能比较正常创建、旋转、后台、进程重建、权限拒绝与外部故障下的行为差异。
- 能设计推翻当前实现的反例，并凭SDK矩阵、文档证据、旧设备降级测试与版本报告完成独立交接。

</Objectives>

## 你的 App 要跑在十年跨度上的设备

2015 年的手机和 2025 年的折叠屏，系统 API 差很多。<Term def="Gradle 中声明的最低可安装 Android 版本，低于此版本的设备无法从商店安装。">minSdk</Term> 决定「多老的机子能装」；<Term def="编译时使用的 SDK 版本，决定你能调用哪些 API 符号。">compileSdk</Term> 决定「你能写哪些新 API」；<Term def="向系统声明已适配的行为变更版本，影响权限、后台限制等策略。">targetSdk</Term> 告诉系统「请按哪一代规则管我」。

## 三个版本号

在 `build.gradle.kts`：

```kotlin
android {
    compileSdk = 35
    defaultConfig {
        minSdk = 26
        targetSdk = 35
    }
}
```

| 版本 | 含义 | 设低了会怎样 |
|---|---|---|
| compileSdk | 编译用 SDK | 无法用新 API / 编译警告 |
| minSdk | 最低安装版本 | 老设备装不了 |
| targetSdk | 行为兼容目标 | 系统用旧兼容模式，可能缺新安全特性 |

{/* 三版本钉在同一条 API Level 数轴上：min 装得上、target 按谁规则跑、compile 能写到多新 */}

<SdkVersionAxisDiagram />

## 运行时版本检查

```kotlin
if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.TIRAMISU) {
    registerForActivityResult(ActivityResultContracts.RequestPermission()) { granted ->
        if (granted) pickImage()
    }.launch(Manifest.permission.READ_MEDIA_IMAGES)
} else {
    // API 32 及以下用 READ_EXTERNAL_STORAGE
}
```

<Stepper>
  <Step title="① 查文档确认 API 级别">
    查阅 Android 官方文档，确认目标权限（如 `READ_MEDIA_IMAGES`）从哪个 API 级别引入（API 33 / TIRAMISU）。这决定了运行时分支的最低 SDK_INT 阈值。
  </Step>
  <Step title="② 根据 minSdk 写分支">
    如果你的 minSdk 低于权限引入的 API 级别（如 minSdk=26），必须用 `if (Build.VERSION.SDK_INT >= ...)` 做运行时分支，或使用 AndroidX 兼容 API。

    ```kotlin
    if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.TIRAMISU) {
        // API 33+：细粒度媒体权限
        requestPermissionLauncher.launch(Manifest.permission.READ_MEDIA_IMAGES)
    } else {
        // API 32 及以下：旧版存储权限
        requestPermissionLauncher.launch(Manifest.permission.READ_EXTERNAL_STORAGE)
    }
    ```
  </Step>
  <Step title="③ targetSdk 升至新权限要求">
    将 `targetSdk` 升级到至少等于新权限对应的 API 级别，并在 `AndroidManifest.xml` 中声明新权限、在代码中加入运行时申请逻辑。targetSdk 不够高时系统不会按新规则执行权限检查。
  </Step>
  <Step title="④ 在 minSdk 设备上回归">
    用 minSdk 版本的真机或模拟器跑完整流程——确保低版本走 else 分支时 UI 正常、权限弹窗显示、功能完整。
  </Step>
</Stepper>

<Callout type="tip">
  **AndroidX** 把新 API 行为 backport 到旧系统（如 `ContextCompat`、`ActivityCompat`）——优先用兼容 API，少写 if/else。
</Callout>

<Callout type="trap">
  targetSdk 长期不升 → Google Play 要求过期、用户设备上行为与新版系统不一致（后台限制、分区存储等突然「像坏了」）。
</Callout>

<Callout type="trap">
  compileSdk &lt; 使用的库要求 → 编译失败。升级 compileSdk 不等于必须升 minSdk，但需测 minSdk 设备。
</Callout>

## 小结

- compileSdk ≥ 你用的 API；minSdk = 业务决定的最低设备；targetSdk 尽量跟最新稳定版
- 新 API 用 SDK_INT 分支或 AndroidX
- 升 targetSdk 前读「行为变更」文档并全量测试

<Exercises>

**问题 1**：“第7章 Android SDK Versions and Compatibility”覆盖哪些正式节点和项目主线？
<Answer>覆盖6个节点，从“7. Android SDK Versions and Compatibility”到“Challenge: Limited Cheats”。主线是解释minSdk、targetSdk、compileSdk和运行时API检查各自约束，安全使用新API，交付物是SDK矩阵、文档证据、旧设备降级测试与版本报告。</Answer>

**问题 2**：怎样建立本页最小可执行实验？
<Answer>固定JDK、SDK、Gradle、设备API、语言、权限和输入，完成一个从用户动作到可观察结果的垂直切片；保存干净构建、设备操作、原始日志、状态快照与断言。</Answer>

**问题 3**：为什么只在正常点击路径运行不能证明完成？
<Answer>它没有覆盖组件重建、进程死亡、异步取消、权限拒绝、外部输入失败、资源释放和版本变化。应逐项注入故障，并用SDK矩阵、文档证据、旧设备降级测试与版本报告核对。</Answer>

**问题 4**：怎样设计能推翻当前实现的反例？
<Answer>保持其他变量不变，分别注入旋转、后台回收、进程死亡、拒权、断网、无效输入与重复操作；任何状态丢失、越权、崩溃、泄漏或重复副作用都推翻完成结论。</Answer>

**问题 5**：从第4版语境迁移到现代目标SDK时如何控制变量？
<Answer>先按原书还原机制并锁定基线，再查询目标SDK合同；一次只迁移组件导出、存储、权限、后台、通知或依赖之一，保存构建、行为、测试和回滚差异。</Answer>

**问题 6**：本页达到独立交接标准需要什么？
<Answer>需要版本与设备指纹、源码提交、输入、构建命令、生命周期轨迹、原始日志、失败测试、安全边界与发布产物，让他人凭SDK矩阵、文档证据、旧设备降级测试与版本报告重放。</Answer>

</Exercises>

<Glossary>

<GlossaryItem term="minSdk">
  应用支持的最低 Android 版本。低于此版本无法安装。
</GlossaryItem>

<GlossaryItem term="targetSdk">
  声明已测试的行为变更版本。影响权限模型、后台执行、存储访问等系统策略。
</GlossaryItem>

<GlossaryItem term="compileSdk">
  编译期 SDK 版本，决定可用的 API 与 lint 规则，不影响运行时「最低系统」。
</GlossaryItem>

</Glossary>

<Attribution />


{/* BNR4_AUTHORITY_SUPPLEMENT */}

## 从生命周期、状态与证据开始

第4版不是API词典，而是六个逐步扩展的应用实验。每一章都要回答：用户从哪里进入，哪个Android组件拥有工作，回调在哪个线程发生，哪些事实必须跨旋转或进程死亡恢复，外部系统怎样失败，资源在何时释放。<Term def="负责创建、取消并最终释放组件、任务或资源的明确作用域">生命周期所有者</Term>把Activity、Fragment视图、ViewModel、Worker、线程与资源的寿命连接起来；没有所有者的异步任务就是潜在泄漏。

界面字段不是天然事实。<Term def="在配置变化或进程重建后，可从保存事实重新构造的最小用户状态">可恢复状态</Term>应根据数据大小与寿命放入saved instance state、ViewModel、数据库、文件或调度器，而不是全部塞进单例。Intent、文件、数据库、网络、系统服务和其他应用形成<Term def="跨边界交换数据时必须验证的action、类型、权限、大小、时序和失败约束">外部合同</Term>；正常响应只是合同中的一个样本。

本书以2019年第四版、Android 5.0至8.1及其后续兼容实践为语境。<Term def="Android版本、targetSdk、设备形态、权限与依赖共同限定的行为适用范围">版本边界</Term>要求先还原书中机制，再查询现代目标SDK的权限、后台、存储、通知和组件导出合同。<Term def="由环境指纹、输入、操作、日志、状态快照和断言组成的可重放结论">证据链</Term>则保证“在我的模拟器能跑”不会冒充可交付结论。

## 第4版正式目录逐节点映射

### 7. Android SDK Versions and Compatibility

**正式节点 1/6。** 先说明“7. Android SDK Versions and Compatibility”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“解释minSdk、targetSdk、compileSdk和运行时API检查各自约束，安全使用新API”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Android SDK Versions

**正式节点 2/6。** 先说明“Android SDK Versions”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“解释minSdk、targetSdk、compileSdk和运行时API检查各自约束，安全使用新API”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Compatibility and Android Programming

**正式节点 3/6。** 先说明“Compatibility and Android Programming”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“解释minSdk、targetSdk、compileSdk和运行时API检查各自约束，安全使用新API”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Using the Android Developer Documentation

**正式节点 4/6。** 先说明“Using the Android Developer Documentation”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“解释minSdk、targetSdk、compileSdk和运行时API检查各自约束，安全使用新API”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Challenge: Reporting the Device's Android Version

**正式节点 5/6。** 先说明“Challenge: Reporting the Device's Android Version”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“解释minSdk、targetSdk、compileSdk和运行时API检查各自约束，安全使用新API”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Challenge: Limited Cheats

**正式节点 6/6。** 先说明“Challenge: Limited Cheats”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“解释minSdk、targetSdk、compileSdk和运行时API检查各自约束，安全使用新API”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

## 最小可执行切片

先把状态转换与Android副作用分开。下面的Kotlin骨架不替代本章代码，而是让旋转、进程恢复与失败输入可单测：

```kotlin
sealed interface ScreenState {
    data object Loading : ScreenState
    data class Ready(val key: String) : ScreenState
    data class Failed(val reason: String, val retryable: Boolean) : ScreenState
}

fun reduce(current: ScreenState, event: UiEvent): ScreenState = nextState(current, event)
```

固定JDK、SDK、Gradle、设备API、语言和输入，清理缓存后从命令行保存制品与日志：

```bash
./gradlew --no-daemon clean test assembleDebug
adb install -r app/build/outputs/apk/debug/app-debug.apk
adb shell am force-stop com.example.app
adb shell monkey -p com.example.app 1
adb logcat -d -v threadtime > run.log
```

先写行为断言，再执行用户操作和故障注入：

```gherkin
Given a pinned build, device API, locale, permission state, and input
When the owner is recreated after state saving or process death
Then durable facts reconstruct the same user-visible state
And cancelled work cannot update a destroyed owner
And denied permission, offline input, and malformed data remain recoverable
```

三段代码分别约束状态模型、环境重放和行为结果。真实项目还要加入静态检查、单元测试、仪器测试、无障碍检查和发布构建；日志只能支持断言，不能代替断言。

## 线程、状态与外部资源

主线程负责输入、生命周期分发和大部分UI工作，磁盘、网络、图片解码与长计算不能阻塞帧。后台结果返回前必须确认所有者仍有效；Fragment的生命周期与其View生命周期不是一回事。每个实现都应列出入口、线程、事实源、取消点、关闭责任和用户可见失败六项。

跨应用Intent、ContentProvider、WebView、网络响应、文件URI和深链都视为不可信输入。校验action、scheme、MIME、大小、授权与响应者；导出组件使用最小权限；日志、截图和测试数据库不得泄露令牌或个人数据。相机、音频、数据库、线程和回调注册必须在成功、异常与取消路径都释放。

性能以用户任务衡量：启动看可交互时刻，列表看掉帧与绑定分配，网络看超时和取消，后台工作看约束、电量与重复调度。每次比较固定设备温度、构建类型、数据规模和网络条件，先采基线再改变一个变量。

迁移现代目标SDK时，先在第4版语境中证明机制，再逐项处理组件导出、存储、通知、后台启动、权限、废弃API和依赖版本。每一步保存编译差异、行为差异、测试结果和回滚点；新API并不自动正确，旧API能编译也不代表仍符合平台政策。

## 三步验证实验

<Steps>
  <Step title="1. 建立目录节点与生命周期图">
    从“解释minSdk、targetSdk、compileSdk和运行时API检查各自约束，安全使用新API”选一个用户任务，标出入口、组件、所有者、回调线程、可恢复状态和销毁点。先预测旋转、后台和进程恢复后哪些对象消失、哪些事实必须重建。
    <BnrLifecycleLab />
  </Step>
  <Step title="2. 注入生命周期与外部故障">
    依次改变生命周期阶段、网络和权限，每次只改变一个条件；记录原始日志、界面状态、持久事实与资源计数，区分预期失败和未知状态。
    <BnrFailureLab />
  </Step>
  <Step title="3. 通过交付证据门">
    用SDK矩阵、文档证据、旧设备降级测试与版本报告核对版本设备、生命周期、持久状态、失败安全、测试和发布。缺少任何一项时，不得把正常点击路径视为完成。
    <BnrEvidenceLab />
  </Step>
</Steps>

<Callout type="trap">**核心陷阱。** 只复述类名与回调，未说明所有者、状态来源、线程、取消和失败后果。修复时回到用户任务和状态机，注入旋转、进程死亡、拒权、断网与无效输入。</Callout>

<Callout type="trap">**历史示例不等于当前平台政策。** 第4版准确反映出版时工具链；现代targetSdk可能改变组件导出、后台、存储、通知和权限。保留原书机制，再建立有证据的迁移层。</Callout>

<Callout type="warning">**真机与数据安全。** 使用测试账号、样本媒体和最小权限；清理日志、数据库与截图。签名密钥、API密钥、联系人和真实位置不得写入仓库或公开证据。</Callout>

## 本章回顾

本页从“7. Android SDK Versions and Compatibility”覆盖到“Challenge: Limited Cheats”，共6个正式目录节点。掌握标准不是记住API，而是能沿“解释minSdk、targetSdk、compileSdk和运行时API检查各自约束，安全使用新API”解释组件与数据边界，运行正常、重建和失败实验，并让另一位开发者仅凭SDK矩阵、文档证据、旧设备降级测试与版本报告重放结论。

<Exercises>

**问题 1**：“第7章 Android SDK Versions and Compatibility”覆盖哪些正式节点和项目主线？
<Answer>覆盖6个节点，从“7. Android SDK Versions and Compatibility”到“Challenge: Limited Cheats”。主线是解释minSdk、targetSdk、compileSdk和运行时API检查各自约束，安全使用新API，交付物是SDK矩阵、文档证据、旧设备降级测试与版本报告。</Answer>

**问题 2**：怎样建立本页最小可执行实验？
<Answer>固定JDK、SDK、Gradle、设备API、语言、权限和输入，完成一个从用户动作到可观察结果的垂直切片；保存干净构建、设备操作、原始日志、状态快照与断言。</Answer>

**问题 3**：为什么只在正常点击路径运行不能证明完成？
<Answer>它没有覆盖组件重建、进程死亡、异步取消、权限拒绝、外部输入失败、资源释放和版本变化。应逐项注入故障，并用SDK矩阵、文档证据、旧设备降级测试与版本报告核对。</Answer>

**问题 4**：怎样设计能推翻当前实现的反例？
<Answer>保持其他变量不变，分别注入旋转、后台回收、进程死亡、拒权、断网、无效输入与重复操作；任何状态丢失、越权、崩溃、泄漏或重复副作用都推翻完成结论。</Answer>

**问题 5**：从第4版语境迁移到现代目标SDK时如何控制变量？
<Answer>先按原书还原机制并锁定基线，再查询目标SDK合同；一次只迁移组件导出、存储、权限、后台、通知或依赖之一，保存构建、行为、测试和回滚差异。</Answer>

**问题 6**：本页达到独立交接标准需要什么？
<Answer>需要版本与设备指纹、源码提交、输入、构建命令、生命周期轨迹、原始日志、失败测试、安全边界与发布产物，让他人凭SDK矩阵、文档证据、旧设备降级测试与版本报告重放。</Answer>

</Exercises>

<Glossary>
  <GlossaryItem term="生命周期所有者">负责创建、取消并最终释放组件、任务或资源的明确作用域。</GlossaryItem>
  <GlossaryItem term="可恢复状态">在配置变化或进程重建后，可从保存事实重新构造的最小用户状态。</GlossaryItem>
  <GlossaryItem term="外部合同">Intent、文件、数据库、网络、系统服务或其他应用与本组件交换数据时的可验证约束。</GlossaryItem>
  <GlossaryItem term="版本边界">Android版本、targetSdk、设备形态、权限与依赖共同限定的行为适用范围。</GlossaryItem>
  <GlossaryItem term="证据链">由环境指纹、输入、操作、日志、状态快照和断言组成的可重放结论。</GlossaryItem>
</Glossary>

[← 上一页：第6章 Your Second Activity](/learn/big-nerd-ranch-guide/basics/second-activity) · [下一页：第8章 UI Fragments and the Fragment Manager →](/learn/big-nerd-ranch-guide/ui-fragment/ui-fragments)

<Attribution adaptedFrom="Android Programming: The Big Nerd Ranch Guide, 4th Edition" adaptedUrl="https://www.oreilly.com/library/view/android-programming-the/9780135257555/" />
