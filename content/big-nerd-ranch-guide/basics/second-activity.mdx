---
title: 第二个Activity
type: C
section: 基础入门
order: 6
description: 学会启动第二个 Activity——显式 Intent、Activity 间传递数据、Activity Result API 接收返回结果。
demo: true
math: false
sourceUrl: "https://www.oreilly.com/library/view/android-programming-the/9780135257555/"
draft: true
qualityVersion: 2
practiceMode: code
sourceMode: independent-rewrite
---

import { BnrLifecycleLab, BnrFailureLab, BnrEvidenceLab } from "@/components/mdx/big-nerd-ranch-guide/diagrams/second-activity";

{/* 知识点清单：
   1) 显式 Intent 启动 = 代码对照
   2) putExtra / getXxxExtra = 传参
   3) Activity Result API = 现代回调
   4) launchMode 初识 = Callout
   5) 返回栈 = Stepper
*/}

<Objectives>

- 能沿“第二个Activity”的用户事件解释Android组件、状态所有者、线程与销毁边界。
- 能围绕“学会启动第二个 Activity——显式 Intent、Activity 间传递数据、Activity Result API 接收返回结果。”改出一个可运行结果，并用前后状态而非组件数量验收。
- 能在旋转、进程重建、拒权、离线或无效输入中选择适用反例，定位首个状态分叉。
- 能用Intent合同、解析目标、权限、返回结果和重复副作用独立重放结论，并标明第四版机制与现代targetSdk政策的边界。

</Objectives>

## 一个 App 里通常不止一个屏幕

列表页点进详情、设置页返回主界面——每个屏幕通常是一个 <Term def="Android 中承载单一界面的组件，通过 Intent 启动，在返回栈中按后进先出管理。">Activity</Term>。用 <Term def="显式指定目标组件类名的 Intent，用于应用内页面跳转。">显式 Intent</Term> 告诉系统「打开哪个 Activity」。

## 启动第二个 Activity

```kotlin
// CrimeListActivity.kt
val intent = Intent(this, CrimeDetailActivity::class.java)
startActivity(intent)
```

## 传递数据

```kotlin
intent.putExtra("crime_id", crime.id)
intent.putExtra("crime_title", crime.title)

// CrimeDetailActivity.kt
val crimeId = intent.getStringExtra("crime_id")
```

复杂对象用 `Parcelable`（Kotlin `@Parcelize`）或 `Serializable`，推荐 Parcelable。

## Activity Result API

```kotlin
private val detailLauncher = registerForActivityResult(
    ActivityResultContracts.StartActivityForResult()
) { result ->
    if (result.resultCode == Activity.RESULT_OK) {
        val updatedId = result.data?.getStringExtra("updated_id")
        // 刷新列表
    }
}

fun openDetail(crimeId: String) {
    val intent = Intent(this, CrimeDetailActivity::class.java)
    intent.putExtra("crime_id", crimeId)
    detailLauncher.launch(intent)
}

// 详情页返回
setResult(Activity.RESULT_OK, Intent().putExtra("updated_id", crime.id))
finish()
```

<Stepper>
  <Step title="① 列表启动详情">
    列表页调用 `detailLauncher.launch(intent)` 启动详情 Activity。intent 中携带 `crime_id` 等必要数据，详情页在 `onCreate` 中通过 `getStringExtra` 读取。

    ```kotlin
    val intent = Intent(this, CrimeDetailActivity::class.java)
    intent.putExtra("crime_id", crime.id)
    detailLauncher.launch(intent)
    ```
  </Step>
  <Step title="② 用户编辑后返回结果">
    用户在详情页编辑完成后，调用 `setResult` 设置返回码和数据，然后 `finish()` 关闭当前 Activity。

    ```kotlin
    setResult(Activity.RESULT_OK, Intent().putExtra("updated_id", crime.id))
    finish()
    ```
  </Step>
  <Step title="③ 列表接收回调并刷新">
    列表页的 `detailLauncher` 回调被触发，检查 `resultCode == RESULT_OK` 后从 `data` 中取出更新数据，刷新列表 UI。
  </Step>
</Stepper>

<Callout type="tip">
  新代码一律用 **Activity Result API**——`registerForActivityResult` 在 onCreate 之前注册，避免旋转后回调丢失；权限、拍照等同理用对应 Contract。
</Callout>

<Callout type="trap">
  忘记 `finish()` → 详情页留在返回栈，用户按 Back 又回到详情而非列表。只读详情也应在完成后 finish。
</Callout>

<Callout type="trap">
  `getStringExtra` 可能为 null——目标 Activity 被系统重建或未传参时崩溃。用 `requireNotNull` 或默认值，Manifest 里不要用 exported 接收外部 Intent 却不校验。
</Callout>

## 返回栈

为什么 `startActivity` 启动新 Activity 后按返回键能回到上一个？因为系统把同任务里的 Activity 放进一个<Term def="后进先出的 Activity 栈。按 Back 弹出当前 Activity，显示前一个。">返回栈</Term>（back stack，后进先出）：启动新 Activity = 压栈、它成为栈顶并显示在屏幕上；按返回键 = 栈顶出栈销毁，下面那个浮上来重新显示。**栈顶永远是用户当前看到的那一屏**——这也是上面那条「忘记 `finish()`」陷阱的根源：没 finish，详情页就一直赖在栈顶。

{/* 返回栈 push/pop 可控教学动画 */}

<BackStackDiagram />

## 小结

- 显式 Intent + `startActivity` / Result API
- putExtra / getXxxExtra 传简单数据
- setResult + finish 回传；检查 resultCode

<Exercises>

**问题 1**：“第6章 Your Second Activity”覆盖哪些正式节点和项目主线？

<Answer>覆盖7个节点，从“6. Your Second Activity”到“Challenge: Tracking Cheat Status by Question”。主线是以显式Intent和extra建立Activity边界，正确返回结果并防止作弊状态漏洞，交付物是双Activity合同、Intent extra表、结果回传与重建测试。</Answer>

**问题 2**：怎样建立本页最小可执行实验？

<Answer>在“第二个Activity”的验证中，固定JDK、SDK、Gradle、设备API、语言、权限和输入，完成一个从用户动作到可观察结果的垂直切片；保存干净构建、设备操作、原始日志、状态快照与断言。</Answer>

**问题 3**：为什么只在正常点击路径运行不能证明完成？

<Answer>它没有覆盖组件重建、进程死亡、异步取消、权限拒绝、外部输入失败、资源释放和版本变化。应逐项注入故障，并用双Activity合同、Intent extra表、结果回传与重建测试核对。</Answer>

**问题 4**：怎样设计能推翻当前实现的反例？

<Answer>在“第二个Activity”的验证中，保持其他变量不变，分别注入旋转、后台回收、进程死亡、拒权、断网、无效输入与重复操作；任何状态丢失、越权、崩溃、泄漏或重复副作用都推翻完成结论。</Answer>

**问题 5**：从第4版语境迁移到现代目标SDK时如何控制变量？

<Answer>先按原书还原机制并锁定基线，再查询目标SDK合同；一次只迁移组件导出、存储、权限、后台、通知或依赖之一，保存构建、行为、测试和回滚差异。</Answer>

**问题 6**：本页达到独立交接标准需要什么？

<Answer>需要版本与设备指纹、源码提交、输入、构建命令、生命周期轨迹、原始日志、失败测试、安全边界与发布产物，让他人凭双Activity合同、Intent extra表、结果回传与重建测试重放。</Answer>

</Exercises>

<Glossary>

<GlossaryItem term="Activity">
  Android 中承载单一界面的组件，通过 Intent 启动，在返回栈中按后进先出管理。
</GlossaryItem>

<GlossaryItem term="显式 Intent">
  通过 setClass/setComponent 指定目标 Activity 的 Intent，用于应用内导航。
</GlossaryItem>

<GlossaryItem term="Activity Result API">
  registerForActivityResult + ActivityResultContract，类型安全地启动 Activity 并接收结果。
</GlossaryItem>

<GlossaryItem term="返回栈">
  后进先出的 Activity 栈。按 Back 弹出当前 Activity，显示前一个。
</GlossaryItem>

</Glossary>

{/* BNR4_QUALITY_V2 */}

“第二个Activity”不使用未获授权的纸书正文；[InformIT出版信息](https://www.informit.com/store/android-programming-the-big-nerd-ranch-guide-9780135245125)与[授权电子版完整目录](https://www.oreilly.com/library/view/android-programming-the/9780135257555/)只用于确认第四版32章、269个正式目录节点和时代语境，[第四版官方勘误](https://github.com/bignerdranch/AndroidCourseResources/raw/master/4thEdition/Errata/4eAddendum.pdf)用于识别工具链变更。下列中文解释、图示、交互、代码与练习均为独立教学重写，平台行为再以[Android Developers](https://developer.android.com/develop)的一手文档复核。

## 为什么“第二个Activity”必须回到可观察状态

“第二个Activity”的学习结果不是记住类名，而是能预测“学会启动第二个 Activity——显式 Intent、Activity 间传递数据、Activity Result API 接收返回结果。”在一次输入、一次重建和一次失败中的不同状态，并指出哪条Android合同产生差异。

## 第四版机制逐项深读

### 6. Your Second Activity

在“第二个Activity”中，“6. Your Second Activity”通过显式Intent启动目标并以extras/result交换最小数据；发送方、接收方和进程重建都必须验证缺失、类型错误和重复返回。

### Setting Up a Second Activity

在“第二个Activity”中，“Setting Up a Second Activity”通过显式Intent启动目标并以extras/result交换最小数据；发送方、接收方和进程重建都必须验证缺失、类型错误和重复返回。

### Starting an Activity

在“第二个Activity”中，“Starting an Activity”通过显式Intent启动目标并以extras/result交换最小数据；发送方、接收方和进程重建都必须验证缺失、类型错误和重复返回。

### Passing Data Between Activities

在“第二个Activity”中，“Passing Data Between Activities”通过显式Intent启动目标并以extras/result交换最小数据；发送方、接收方和进程重建都必须验证缺失、类型错误和重复返回。

### How Android Sees Your Activities

在“第二个Activity”中，“How Android Sees Your Activities”若依赖第四版时期API，应分开说明原书机制与现代平台政策，并用Android官方文档核对迁移边界。

### Challenge: Closing Loopholes for Cheaters

在“第二个Activity”中，“Challenge: Closing Loopholes for Cheaters”用于推翻“学会启动第二个 Activity——显式 Intent、Activity 间传递数据、Activity Result API 接收返回结果。”的顺利路径：先写预期，再引入一个边界输入、重建或平台差异，并用状态、日志与用户结果解释首个分叉。

### Challenge: Tracking Cheat Status by Question

在“第二个Activity”中，“Challenge: Tracking Cheat Status by Question”用于推翻“学会启动第二个 Activity——显式 Intent、Activity 间传递数据、Activity Result API 接收返回结果。”的顺利路径：先写预期，再引入一个边界输入、重建或平台差异，并用状态、日志与用户结果解释首个分叉。

## 可操作机制实验

“第二个Activity”把“学会启动第二个 Activity——显式 Intent、Activity 间传递数据、Activity Result API 接收返回结果。在“第二个Activity”的验证中，”拆成三个可观察层次：先选择目录节点并确认责任边界，再改变一个生命周期或外部条件，最后回到Intent合同、解析目标、权限、返回结果和重复副作用。点击后的文字、状态与轨迹必须同步变化，重置后回到相同基线。

<Steps>
  <Step title="1. 选择机制节点">
    选择一个正式节点，先预测它由谁拥有、在哪个线程执行、哪些状态需要恢复；右侧会显示本章专属的机制合同。

    <BnrLifecycleLab />

  </Step>
  <Step title="2. 注入一个边界条件">
    改变生命周期阶段、外部可用性或权限中的一项，观察回调、队列、数据或渲染结果怎样变化；不要用总风险分数代替因果。

    <BnrFailureLab />

  </Step>
  <Step title="3. 复位并核对证据">
    用Intent合同、解析目标、权限、返回结果和重复副作用核对预测，随后执行重置；若基线不能复现，实验本身不通过。

    <BnrEvidenceLab />

  </Step>
</Steps>

<Callout type="trap">在“第二个Activity”中，正常点击成功只能证明一个样本。若重建、取消、无响应者、离线、旧schema或目标SDK变化会产生不同结果，就必须把该边界写进状态机与测试。</Callout>

<Callout type="trap">“第二个Activity”若只留下最终截图，就无法判断旧所有者是否收到回调、后台任务是否重复、状态是否来自错误层或复位是否真正清空实验。</Callout>

## “第二个Activity”验收回顾

“第二个Activity”只有在Intent合同、解析目标、权限、返回结果和重复副作用能够从相同基线再次得到相同断言时才通过；第四版机制与现代平台政策分别记录，不用新API名称掩盖旧行为。

[← 上一页：Android应用的调试](/learn/big-nerd-ranch-guide/basics/debugging) · [下一页：SDK版本与兼容性 →](/learn/big-nerd-ranch-guide/basics/sdk-compatibility)

<Attribution
  mode="independent-rewrite"
  sourceBasis="authorized-sample"
  workTitle="Bill Phillips, Chris Stewart, Kristin Marsicano, Brian Gardner, Android Programming: The Big Nerd Ranch Guide, Fourth Edition"
  adaptedUrl="https://www.oreilly.com/library/view/android-programming-the/9780135257555/"
/>
