---
title: UI状态的保存与恢复
type: C
section: 基础入门
order: 4
description: 掌握 onSaveInstanceState 和 ViewModel——让用户在旋转屏幕或进程被杀死后回到原来的界面状态。
demo: true
math: false
sourceUrl: "https://www.oreilly.com/library/view/android-programming-the/9780135257555/"
draft: false
qualityVersion: 2
practiceMode: simulation
sourceMode: independent-rewrite
---

{/* 知识点清单：
   1) 配置变更导致重建 = 直觉引入
   2) onSaveInstanceState / onCreate 恢复 = 代码对照
   3) ViewModel 存活范围 = Term + 对比表
   4) SavedStateHandle 结合 = Callout
   5) ViewModel 禁止持有 View = Callout
*/}

<Objectives>

- 能沿“UI状态的保存与恢复”的用户事件解释Android组件、状态所有者、线程与销毁边界。
- 能围绕“掌握 onSaveInstanceState 和 ViewModel——让用户在旋转屏幕或进程被杀死后回到原来的界面状态。”改出一个可运行结果，并用前后状态而非组件数量验收。
- 能在旋转、进程重建、拒权、离线或无效输入中选择适用反例，定位首个状态分叉。
- 能用实例ID、回调轨迹、旋转与进程恢复后的状态断言独立重放结论，并标明第四版机制与现代targetSdk政策的边界。

</Objectives>

## 转一下屏幕，表单全没了

用户填到一半旋转手机——输入框被清空，体验等于白填。原因是 <Term def="屏幕旋转、语言切换等导致 Activity 实例销毁并按新 Configuration 重建的过程。">配置变更</Term> 会重建 Activity，除非你把状态存到系统能带过去的地方。

两种主流方案：<Term def="把轻量 UI 状态写入 Bundle，随 Activity 实例一起保存与恢复。">onSaveInstanceState</Term>（轻量）和 <Term def="与 UI 控制器生命周期解耦、在配置变更时存活的组件，持有 UI 相关数据。">ViewModel</Term>（业务状态）。

## onSaveInstanceState

```kotlin
override fun onSaveInstanceState(outState: Bundle) {
    super.onSaveInstanceState(outState)
    outState.putString("user_input", editText.text.toString())
}

override fun onCreate(savedInstanceState: Bundle?) {
    super.onCreate(savedInstanceState)
    setContentView(R.layout.activity_main)
    savedInstanceState?.getString("user_input")?.let { editText.setText(it) }
}
```

Bundle 约 1MB 上限，只适合字符串、整型等可序列化小数据。

<Stepper>
  <Step title="① 用户输入">
    用户在 EditText 中输入内容（如 "Hello"），数据存储在 Activity 实例的内存中——此时尚未持久化，旋转就会被清空。
  </Step>
  <Step title="② 旋转触发 onSaveInstanceState">
    系统在销毁 Activity 前调用 onSaveInstanceState，将 UI 状态写入 Bundle。

    ```kotlin
    override fun onSaveInstanceState(outState: Bundle) {
        super.onSaveInstanceState(outState)
        outState.putString("user_input", editText.text.toString())
    }
    ```
  </Step>
  <Step title="③ 新 Activity onCreate 恢复">
    新 Activity 实例创建后，从 savedInstanceState 读取之前保存的数据并恢复到 EditText。

    ```kotlin
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_main)
        savedInstanceState?.getString("user_input")?.let { editText.setText(it) }
    }
    ```
  </Step>
</Stepper>

## ViewModel

```kotlin
class FormViewModel : ViewModel() {
    val userInput = MutableLiveData("")
}

class FormActivity : AppCompatActivity() {
    private val viewModel: FormViewModel by viewModels()

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        editText.setText(viewModel.userInput.value)
        editText.doAfterTextChanged { viewModel.userInput.value = it.toString() }
    }
}
```

旋转后仍是**同一个 ViewModel 实例**——不必手动 Bundle 也能保住数据。

{/* 配置变更下 Activity 重建 vs ViewModel 跨重建存活的对比动画 */}

<ConfigChangeViewModelDiagram />

| | onSaveInstanceState | ViewModel |
|---|---|---|
| 旋转 | 支持 | 支持 |
| 进程被杀 | 支持（Bundle 持久化） | 默认丢失 |
| 数据量 | 小 | 大 |
| 复杂对象 | 需序列化 | 直接持有 |

<Callout type="tip">
  推荐 **ViewModel + SavedStateHandle**：业务数据放 ViewModel，进程被杀也要恢复的 key 走 SavedStateHandle（底层仍是 Bundle）。
</Callout>

<Callout type="trap">
  ViewModel **绝不能**持有 Activity、Fragment 或 View 引用——否则旋转后旧 Activity 无法回收，内存泄漏。
</Callout>

<Callout type="trap">
  把 Bitmap 或大列表塞进 Bundle → `TransactionTooLargeException`。大对象放 Room/文件，Bundle 只存 ID。
</Callout>

## 小结

- 旋转：ViewModel 或 onSaveInstanceState
- 进程被杀：onSaveInstanceState / SavedStateHandle
- ViewModel 不碰 View 层引用

<Exercises>

**问题 1**：“第4章 Persisting UI State”覆盖哪些正式节点和项目主线？

<Answer>覆盖7个节点，从“4. Persisting UI State”到“For the More Curious: Avoiding a Half-Baked Solution”。主线是区分ViewModel跨配置存活与saved instance state跨进程恢复的不同合同，交付物是状态所有权表、旋转测试、进程死亡恢复测试和反例。</Answer>

**问题 2**：怎样建立本页最小可执行实验？

<Answer>在“UI状态的保存与恢复”的验证中，固定JDK、SDK、Gradle、设备API、语言、权限和输入，完成一个从用户动作到可观察结果的垂直切片；保存干净构建、设备操作、原始日志、状态快照与断言。</Answer>

**问题 3**：为什么只在正常点击路径运行不能证明完成？

<Answer>它没有覆盖组件重建、进程死亡、异步取消、权限拒绝、外部输入失败、资源释放和版本变化。应逐项注入故障，并用状态所有权表、旋转测试、进程死亡恢复测试和反例核对。</Answer>

**问题 4**：怎样设计能推翻当前实现的反例？

<Answer>在“UI状态的保存与恢复”的验证中，保持其他变量不变，分别注入旋转、后台回收、进程死亡、拒权、断网、无效输入与重复操作；任何状态丢失、越权、崩溃、泄漏或重复副作用都推翻完成结论。</Answer>

**问题 5**：从第4版语境迁移到现代目标SDK时如何控制变量？

<Answer>先按原书还原机制并锁定基线，再查询目标SDK合同；一次只迁移组件导出、存储、权限、后台、通知或依赖之一，保存构建、行为、测试和回滚差异。</Answer>

**问题 6**：本页达到独立交接标准需要什么？

<Answer>需要版本与设备指纹、源码提交、输入、构建命令、生命周期轨迹、原始日志、失败测试、安全边界与发布产物，让他人凭状态所有权表、旋转测试、进程死亡恢复测试和反例重放。</Answer>

</Exercises>

<Glossary>

<GlossaryItem term="配置变更">
  屏幕旋转、语言切换等导致 Activity 实例销毁并按新 Configuration 重建的过程。
</GlossaryItem>

<GlossaryItem term="onSaveInstanceState">
  系统在 Activity 可能被销毁前调用的回调，用于把 UI 状态写入 Bundle。
</GlossaryItem>

<GlossaryItem term="ViewModel">
  配置变更时存活的 UI 状态容器，由 ViewModelStore 管理，不持有 View 引用。
</GlossaryItem>

<GlossaryItem term="SavedStateHandle">
  ViewModel 构造参数，提供与 onSaveInstanceState 相同的 Bundle 能力，便于进程恢复。
</GlossaryItem>

</Glossary>

{/* BNR4_QUALITY_V2 */}

“UI状态的保存与恢复”不使用未获授权的纸书正文；[InformIT出版信息](https://www.informit.com/store/android-programming-the-big-nerd-ranch-guide-9780135245125)与[授权电子版完整目录](https://www.oreilly.com/library/view/android-programming-the/9780135257555/)只用于确认第四版32章、269个正式目录节点和时代语境，[第四版官方勘误](https://github.com/bignerdranch/AndroidCourseResources/raw/master/4thEdition/Errata/4eAddendum.pdf)用于识别工具链变更。下列中文解释、图示、交互、代码与练习均为独立教学重写，平台行为再以[Android Developers](https://developer.android.com/develop)的一手文档复核。

## 为什么“UI状态的保存与恢复”必须回到可观察状态

“UI状态的保存与恢复”的学习结果不是记住类名，而是能预测“掌握 onSaveInstanceState 和 ViewModel——让用户在旋转屏幕或进程被杀死后回到原来的界面状态。”在一次输入、一次重建和一次失败中的不同状态，并指出哪条Android合同产生差异。

## 第四版机制逐项深读

### 4. Persisting UI State

在“UI状态的保存与恢复”中，“4. Persisting UI State”要求把临时UI状态、配置期状态与进程死亡后需恢复的最小事实分层；ViewModel跨配置但不跨进程，saved state必须小且可序列化。

### Including the ViewModel Dependency

在“UI状态的保存与恢复”中，分析“Including the ViewModel Dependency”要画出事件进入、状态归约、数据源写入和UI重绘四条边，并指出配置变化时谁被重建。

### Adding a ViewModel

在“UI状态的保存与恢复”中，“Adding a ViewModel”按事实、界面表示和用户事件拆责任；状态所有者不能持有已销毁View，界面也不能绕过边界直接改持久数据。

### Saving Data Across Process Death

在“UI状态的保存与恢复”中，“Saving Data Across Process Death”要求把临时UI状态、配置期状态与进程死亡后需恢复的最小事实分层；ViewModel跨配置但不跨进程，saved state必须小且可序列化。

### ViewModel vs Saved Instance State

在“UI状态的保存与恢复”中，验证“ViewModel vs Saved Instance State”用同一事件序列比较旋转前后状态和副作用次数；界面相同但重复写入仍是不正确。

### For the More Curious: Jetpack, AndroidX, and Architecture Components

在“UI状态的保存与恢复”中，验证“For the More Curious: Jetpack, AndroidX, and Architecture Components”用同一事件序列比较旋转前后状态和副作用次数；界面相同但重复写入仍是不正确。

### For the More Curious: Avoiding a Half-Baked Solution

在“UI状态的保存与恢复”中，“For the More Curious: Avoiding a Half-Baked Solution”用于推翻“掌握 onSaveInstanceState 和 ViewModel——让用户在旋转屏幕或进程被杀死后回到原来的界面状态。”的顺利路径：先写预期，再引入一个边界输入、重建或平台差异，并用状态、日志与用户结果解释首个分叉。

<Callout type="trap">在“UI状态的保存与恢复”中，正常点击成功只能证明一个样本。若重建、取消、无响应者、离线、旧schema或目标SDK变化会产生不同结果，就必须把该边界写进状态机与测试。</Callout>

<Callout type="trap">“UI状态的保存与恢复”若只留下最终截图，就无法判断旧所有者是否收到回调、后台任务是否重复、状态是否来自错误层或复位是否真正清空实验。</Callout>

## “UI状态的保存与恢复”验收回顾

“UI状态的保存与恢复”只有在实例ID、回调轨迹、旋转与进程恢复后的状态断言能够从相同基线再次得到相同断言时才通过；第四版机制与现代平台政策分别记录，不用新API名称掩盖旧行为。

[← 上一页：activity的生命周期](/learn/big-nerd-ranch-guide/basics/activity-lifecycle) · [下一页：Android应用的调试 →](/learn/big-nerd-ranch-guide/basics/debugging)

<Attribution
  mode="independent-rewrite"
  sourceBasis="authorized-sample"
  workTitle="Bill Phillips, Chris Stewart, Kristin Marsicano, Brian Gardner, Android Programming: The Big Nerd Ranch Guide, Fourth Edition"
  adaptedUrl="https://www.oreilly.com/library/view/android-programming-the/9780135257555/"
/>
