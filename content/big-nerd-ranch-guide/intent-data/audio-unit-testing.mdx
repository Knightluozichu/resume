---
title: 音频播放与单元测试
type: C
section: Intent与数据
order: 20
description: 掌握 MediaPlayer 音频播放和 JUnit 单元测试——用 Mockito 隔离依赖，写出可测试的代码。
demo: true
math: false
sourceUrl: "https://www.oreilly.com/library/view/android-programming-the/9780135257555/"
draft: false
---

import { BnrLifecycleLab, BnrFailureLab, BnrEvidenceLab } from "@/components/mdx/big-nerd-ranch-guide/diagrams/audio-unit-testing";

{/* 知识点清单：
   1) MediaPlayer 生命周期 = 代码
   2) SoundPool 短音效 = 对比
   3) JUnit @Test/@Before = 代码
   4) Mockito mock = 代码
   5) release 防泄漏 = Callout
   6) Room 内存库测 DAO = Callout
*/}

<Objectives>

- 能解释“第20章 Unit Testing and Audio Playback”全部正式节点的组件所有者、生命周期、线程、状态来源与Android版本边界。
- 能实现“以SoundPool加载/播放/释放生命周期为被测边界，用测试替身核对对象交互和旋转行为”的最小垂直切片，并保存构建、设备操作、原始日志和断言。
- 能比较正常创建、旋转、后台、进程重建、权限拒绝与外部故障下的行为差异。
- 能设计推翻当前实现的反例，并凭音频资源清单、SoundPool状态机、单元测试、mock交互与资源释放证据完成独立交接。

</Objectives>

## 你改了 3 行代码，怎么确认没有破坏其他功能？

每次发版前手动点一遍所有页面？App 有 100 个页面怎么办？**自动化测试**是你最好的 QA。这章教你两个关键技术：MediaPlayer（最常用的音频播放）和 JUnit 单元测试（保证代码质量）。

## MediaPlayer 音频播放

```kotlin
class AudioPlayer(context: Context) {
    private var mediaPlayer: MediaPlayer? = null

    fun play(resId: Int) {
        release()  // 先释放旧的
        mediaPlayer = MediaPlayer.create(context, resId).apply {
            setOnCompletionListener { release() }
            start()
        }
    }

    fun pause() = mediaPlayer?.pause()
    fun stop() = mediaPlayer?.apply { stop(); prepareAsync() }

    fun release() {
        mediaPlayer?.release()
        mediaPlayer = null
    }
}
```

关键规则：**用完必须 release()**——MediaPlayer 持有系统音频资源，不释放会导致资源泄漏甚至其他 App 无声音。

对于短促音效（按钮点击声、通知铃声），用 **SoundPool** 替代 MediaPlayer：

```kotlin
val soundPool = SoundPool.Builder().setMaxStreams(3).build()
val soundId = soundPool.load(context, R.raw.click, 1)
soundPool.play(soundId, 1.0f, 1.0f, 1, 0, 1.0f)
```

SoundPool 预加载音频到内存，低延迟播放短音效——MediaPlayer 适合长音频（音乐/播客）。

<Stepper>
  <Step title="① create">
    `MediaPlayer.create(context, resId)` 是最简捷的创建方式——内部已调用 `setDataSource` + `prepare()`，创建后即可 `start()`。这也意味着它在 `create()` 阶段就可能因为资源不存在或格式不支持而抛异常。
  </Step>
  <Step title="② setDataSource + prepare">
    如果不用 `create()`，则手动 `setDataSource(path)` 指定音频来源（本地文件/网络 URI），然后调用 `prepare()`（同步，阻塞当前线程）或 `prepareAsync()`（异步，推荐）。异步准备完毕后通过 `OnPreparedListener` 回调通知。
  </Step>
  <Step title="③ start">
    调用 `start()` 开始播放。播放完成后触发 `OnCompletionListener`——在此回调中决定是循环播放（`seekTo(0)` + `start()`）还是释放资源（`release()`）。
  </Step>
  <Step title="④ pause / stop">
    `pause()` 暂停播放，之后可以 `start()` 从暂停位置恢复。`stop()` 停止并重置到 Idle 状态——之后若想再播必须重新 `prepare()`。用 `isPlaying()` 检查当前到底在播还是暂停。
  </Step>
  <Step title="⑤ release">
    调用 `release()` 彻底释放 MediaPlayer 持有的系统音频资源。**活动 onStop/onDestroy 中必须调用**——不释放会导致 AudioFlinger 资源泄漏，其他 App 可能无声音。每次 `create()` 新实例前也应先 `release()` 旧实例。
  </Step>
</Stepper>

## 单元测试基础

### JUnit 4 结构

```kotlin
class CalculatorTest {
    private lateinit var calculator: Calculator

    @Before
    fun setUp() {
        calculator = Calculator()
    }

    @Test
    fun `addition should return sum of two numbers`() {
        val result = calculator.add(2, 3)
        assertEquals(5, result)
    }

    @Test(expected = IllegalArgumentException::class)
    fun `division by zero should throw`() {
        calculator.divide(10, 0)
    }
}
```

### Mockito 模拟依赖

```kotlin
@Test
fun `loadData success should update LiveData`() {
    // 模拟 Repository
    val mockRepo = mock(Repository::class.java)
    `when`(mockRepo.fetchData()).thenReturn(listOf("a", "b"))

    val viewModel = MainViewModel(mockRepo)
    viewModel.loadData()

    // 验证 LiveData 结果
    assertEquals(listOf("a", "b"), viewModel.data.getOrAwaitValue())
}
```

Mockito 的 `mock()` 创建一个假对象，`when().thenReturn()` 控制它返回什么——让你在测试中隔离外部依赖（数据库、网络），只测 ViewModel 自身逻辑。

{/* 测试金字塔：单元测试为何放底层、如何用接口替身隔离 SoundPool 这类系统资源 */}

<TestPyramidDiagram />

## 容易踩的坑

<Callout type="trap">
  **现象**：MediaPlayer 播完一首歌再播下一首——无声或崩溃。**原因**：没有先 `release()` 旧的 MediaPlayer 就创建新的——系统音频资源耗尽。**修法**：每次 play 前先 `release()` 确保旧实例被清理。
</Callout>

<Callout type="trap">
  **现象**：测试跑到 Room DAO 那一层要 5 秒——因为真的在写数据库。**原因**：单元测试应该隔离数据库依赖。**修法**：Room DAO 的测试用 `Room.inMemoryDatabaseBuilder`（内存数据库），或直接 mock DAO 接口。
</Callout>

<Callout type="trap">
  **现象**：MediaPlayer 在 Activity destroy 后仍占音频焦点。**原因**：未在 onStop/onDestroy 调用 release()。**修法**：生命周期绑定点释放，或改用 ExoPlayer/Media3 更好管理。
</Callout>

## 小结

- MediaPlayer 适合长音频（音乐/播客），SoundPool 适合短音效（点击声）
- MediaPlayer 用完必须 release()，播放新音频前先释放旧实例
- JUnit：@Test 标记测试方法，@Before 准备环境，assertEquals 验证结果
- Mockito 模拟依赖 → 隔离外部因素，只测被测代码本身
- 单元测试测纯逻辑（ViewModel/Repository），Instrumentation 测试测 UI 交互

<Exercises>

**问题 1**：“第20章 Unit Testing and Audio Playback”覆盖哪些正式节点和项目主线？
<Answer>覆盖15个节点，从“20. Unit Testing and Audio Playback”到“Challenge: Play Sound Across Rotation”。主线是以SoundPool加载/播放/释放生命周期为被测边界，用测试替身核对对象交互和旋转行为，交付物是音频资源清单、SoundPool状态机、单元测试、mock交互与资源释放证据。</Answer>

**问题 2**：怎样建立本页最小可执行实验？
<Answer>固定JDK、SDK、Gradle、设备API、语言、权限和输入，完成一个从用户动作到可观察结果的垂直切片；保存干净构建、设备操作、原始日志、状态快照与断言。</Answer>

**问题 3**：为什么只在正常点击路径运行不能证明完成？
<Answer>它没有覆盖组件重建、进程死亡、异步取消、权限拒绝、外部输入失败、资源释放和版本变化。应逐项注入故障，并用音频资源清单、SoundPool状态机、单元测试、mock交互与资源释放证据核对。</Answer>

**问题 4**：怎样设计能推翻当前实现的反例？
<Answer>保持其他变量不变，分别注入旋转、后台回收、进程死亡、拒权、断网、无效输入与重复操作；任何状态丢失、越权、崩溃、泄漏或重复副作用都推翻完成结论。</Answer>

**问题 5**：从第4版语境迁移到现代目标SDK时如何控制变量？
<Answer>先按原书还原机制并锁定基线，再查询目标SDK合同；一次只迁移组件导出、存储、权限、后台、通知或依赖之一，保存构建、行为、测试和回滚差异。</Answer>

**问题 6**：本页达到独立交接标准需要什么？
<Answer>需要版本与设备指纹、源码提交、输入、构建命令、生命周期轨迹、原始日志、失败测试、安全边界与发布产物，让他人凭音频资源清单、SoundPool状态机、单元测试、mock交互与资源释放证据重放。</Answer>

</Exercises>

<Glossary>

<GlossaryItem term="SoundPool">
  短音效播放器——预加载音频到内存，低延迟播放（&lt;10ms）。适合按钮点击声、通知铃声、游戏音效。不适合长音频（音乐用 MediaPlayer）。
</GlossaryItem>

<GlossaryItem term="Mockito">
  Java/Kotlin 的 mock 测试框架。`mock()` 创建假对象，`when().thenReturn()` 控制行为，`verify()` 验证方法是否被调用。隔离依赖、专注测试被测代码。
</GlossaryItem>

</Glossary>

<Attribution />


{/* BNR4_AUTHORITY_SUPPLEMENT */}

## 从生命周期、状态与证据开始

第4版不是API词典，而是六个逐步扩展的应用实验。每一章都要回答：用户从哪里进入，哪个Android组件拥有工作，回调在哪个线程发生，哪些事实必须跨旋转或进程死亡恢复，外部系统怎样失败，资源在何时释放。<Term def="负责创建、取消并最终释放组件、任务或资源的明确作用域">生命周期所有者</Term>把Activity、Fragment视图、ViewModel、Worker、线程与资源的寿命连接起来；没有所有者的异步任务就是潜在泄漏。

界面字段不是天然事实。<Term def="在配置变化或进程重建后，可从保存事实重新构造的最小用户状态">可恢复状态</Term>应根据数据大小与寿命放入saved instance state、ViewModel、数据库、文件或调度器，而不是全部塞进单例。Intent、文件、数据库、网络、系统服务和其他应用形成<Term def="跨边界交换数据时必须验证的action、类型、权限、大小、时序和失败约束">外部合同</Term>；正常响应只是合同中的一个样本。

本书以2019年第四版、Android 5.0至8.1及其后续兼容实践为语境。<Term def="Android版本、targetSdk、设备形态、权限与依赖共同限定的行为适用范围">版本边界</Term>要求先还原书中机制，再查询现代目标SDK的权限、后台、存储、通知和组件导出合同。<Term def="由环境指纹、输入、操作、日志、状态快照和断言组成的可重放结论">证据链</Term>则保证“在我的模拟器能跑”不会冒充可交付结论。

## 第4版正式目录逐节点映射

### 20. Unit Testing and Audio Playback

**正式节点 1/15。** 先说明“20. Unit Testing and Audio Playback”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“以SoundPool加载/播放/释放生命周期为被测边界，用测试替身核对对象交互和旋转行为”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Creating a SoundPool

**正式节点 2/15。** 先说明“Creating a SoundPool”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“以SoundPool加载/播放/释放生命周期为被测边界，用测试替身核对对象交互和旋转行为”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Accessing Assets

**正式节点 3/15。** 先说明“Accessing Assets”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“以SoundPool加载/播放/释放生命周期为被测边界，用测试替身核对对象交互和旋转行为”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Loading Sounds

**正式节点 4/15。** 先说明“Loading Sounds”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“以SoundPool加载/播放/释放生命周期为被测边界，用测试替身核对对象交互和旋转行为”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Playing Sounds

**正式节点 5/15。** 先说明“Playing Sounds”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“以SoundPool加载/播放/释放生命周期为被测边界，用测试替身核对对象交互和旋转行为”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Test Dependencies

**正式节点 6/15。** 先说明“Test Dependencies”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“以SoundPool加载/播放/释放生命周期为被测边界，用测试替身核对对象交互和旋转行为”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Creating a Test Class

**正式节点 7/15。** 先说明“Creating a Test Class”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“以SoundPool加载/播放/释放生命周期为被测边界，用测试替身核对对象交互和旋转行为”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Setting Up Your Test

**正式节点 8/15。** 先说明“Setting Up Your Test”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“以SoundPool加载/播放/释放生命周期为被测边界，用测试替身核对对象交互和旋转行为”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Writing Tests

**正式节点 9/15。** 先说明“Writing Tests”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“以SoundPool加载/播放/释放生命周期为被测边界，用测试替身核对对象交互和旋转行为”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Data Binding Callbacks

**正式节点 10/15。** 先说明“Data Binding Callbacks”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“以SoundPool加载/播放/释放生命周期为被测边界，用测试替身核对对象交互和旋转行为”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Unloading Sounds

**正式节点 11/15。** 先说明“Unloading Sounds”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“以SoundPool加载/播放/释放生命周期为被测边界，用测试替身核对对象交互和旋转行为”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### For the More Curious: Integration Testing

**正式节点 12/15。** 先说明“For the More Curious: Integration Testing”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“以SoundPool加载/播放/释放生命周期为被测边界，用测试替身核对对象交互和旋转行为”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### For the More Curious: Mocks and Testing

**正式节点 13/15。** 先说明“For the More Curious: Mocks and Testing”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“以SoundPool加载/播放/释放生命周期为被测边界，用测试替身核对对象交互和旋转行为”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Challenge: Playback Speed Control

**正式节点 14/15。** 先说明“Challenge: Playback Speed Control”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“以SoundPool加载/播放/释放生命周期为被测边界，用测试替身核对对象交互和旋转行为”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Challenge: Play Sound Across Rotation

**正式节点 15/15。** 先说明“Challenge: Play Sound Across Rotation”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“以SoundPool加载/播放/释放生命周期为被测边界，用测试替身核对对象交互和旋转行为”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

## 最小可执行切片

先把状态转换与Android副作用分开。下面的Kotlin骨架不替代本章代码，而是让旋转、进程恢复与失败输入可单测：

```kotlin
sealed interface ScreenState {
    data object Loading : ScreenState
    data class Ready(val key: String) : ScreenState
    data class Failed(val reason: String, val retryable: Boolean) : ScreenState
}

fun reduce(current: ScreenState, event: UiEvent): ScreenState = nextState(current, event)
```

固定JDK、SDK、Gradle、设备API、语言和输入，清理缓存后从命令行保存制品与日志：

```bash
./gradlew --no-daemon clean test assembleDebug
adb install -r app/build/outputs/apk/debug/app-debug.apk
adb shell am force-stop com.example.app
adb shell monkey -p com.example.app 1
adb logcat -d -v threadtime > run.log
```

先写行为断言，再执行用户操作和故障注入：

```gherkin
Given a pinned build, device API, locale, permission state, and input
When the owner is recreated after state saving or process death
Then durable facts reconstruct the same user-visible state
And cancelled work cannot update a destroyed owner
And denied permission, offline input, and malformed data remain recoverable
```

三段代码分别约束状态模型、环境重放和行为结果。真实项目还要加入静态检查、单元测试、仪器测试、无障碍检查和发布构建；日志只能支持断言，不能代替断言。

## 线程、状态与外部资源

主线程负责输入、生命周期分发和大部分UI工作，磁盘、网络、图片解码与长计算不能阻塞帧。后台结果返回前必须确认所有者仍有效；Fragment的生命周期与其View生命周期不是一回事。每个实现都应列出入口、线程、事实源、取消点、关闭责任和用户可见失败六项。

跨应用Intent、ContentProvider、WebView、网络响应、文件URI和深链都视为不可信输入。校验action、scheme、MIME、大小、授权与响应者；导出组件使用最小权限；日志、截图和测试数据库不得泄露令牌或个人数据。相机、音频、数据库、线程和回调注册必须在成功、异常与取消路径都释放。

性能以用户任务衡量：启动看可交互时刻，列表看掉帧与绑定分配，网络看超时和取消，后台工作看约束、电量与重复调度。每次比较固定设备温度、构建类型、数据规模和网络条件，先采基线再改变一个变量。

迁移现代目标SDK时，先在第4版语境中证明机制，再逐项处理组件导出、存储、通知、后台启动、权限、废弃API和依赖版本。每一步保存编译差异、行为差异、测试结果和回滚点；新API并不自动正确，旧API能编译也不代表仍符合平台政策。

## 三步验证实验

<Steps>
  <Step title="1. 建立目录节点与生命周期图">
    从“以SoundPool加载/播放/释放生命周期为被测边界，用测试替身核对对象交互和旋转行为”选一个用户任务，标出入口、组件、所有者、回调线程、可恢复状态和销毁点。先预测旋转、后台和进程恢复后哪些对象消失、哪些事实必须重建。
    <BnrLifecycleLab />
  </Step>
  <Step title="2. 注入生命周期与外部故障">
    依次改变生命周期阶段、网络和权限，每次只改变一个条件；记录原始日志、界面状态、持久事实与资源计数，区分预期失败和未知状态。
    <BnrFailureLab />
  </Step>
  <Step title="3. 通过交付证据门">
    用音频资源清单、SoundPool状态机、单元测试、mock交互与资源释放证据核对版本设备、生命周期、持久状态、失败安全、测试和发布。缺少任何一项时，不得把正常点击路径视为完成。
    <BnrEvidenceLab />
  </Step>
</Steps>

<Callout type="trap">**核心陷阱。** 只复述类名与回调，未说明所有者、状态来源、线程、取消和失败后果。修复时回到用户任务和状态机，注入旋转、进程死亡、拒权、断网与无效输入。</Callout>

<Callout type="trap">**历史示例不等于当前平台政策。** 第4版准确反映出版时工具链；现代targetSdk可能改变组件导出、后台、存储、通知和权限。保留原书机制，再建立有证据的迁移层。</Callout>

<Callout type="warning">**真机与数据安全。** 使用测试账号、样本媒体和最小权限；清理日志、数据库与截图。签名密钥、API密钥、联系人和真实位置不得写入仓库或公开证据。</Callout>

## 本章回顾

本页从“20. Unit Testing and Audio Playback”覆盖到“Challenge: Play Sound Across Rotation”，共15个正式目录节点。掌握标准不是记住API，而是能沿“以SoundPool加载/播放/释放生命周期为被测边界，用测试替身核对对象交互和旋转行为”解释组件与数据边界，运行正常、重建和失败实验，并让另一位开发者仅凭音频资源清单、SoundPool状态机、单元测试、mock交互与资源释放证据重放结论。

<Exercises>

**问题 1**：“第20章 Unit Testing and Audio Playback”覆盖哪些正式节点和项目主线？
<Answer>覆盖15个节点，从“20. Unit Testing and Audio Playback”到“Challenge: Play Sound Across Rotation”。主线是以SoundPool加载/播放/释放生命周期为被测边界，用测试替身核对对象交互和旋转行为，交付物是音频资源清单、SoundPool状态机、单元测试、mock交互与资源释放证据。</Answer>

**问题 2**：怎样建立本页最小可执行实验？
<Answer>固定JDK、SDK、Gradle、设备API、语言、权限和输入，完成一个从用户动作到可观察结果的垂直切片；保存干净构建、设备操作、原始日志、状态快照与断言。</Answer>

**问题 3**：为什么只在正常点击路径运行不能证明完成？
<Answer>它没有覆盖组件重建、进程死亡、异步取消、权限拒绝、外部输入失败、资源释放和版本变化。应逐项注入故障，并用音频资源清单、SoundPool状态机、单元测试、mock交互与资源释放证据核对。</Answer>

**问题 4**：怎样设计能推翻当前实现的反例？
<Answer>保持其他变量不变，分别注入旋转、后台回收、进程死亡、拒权、断网、无效输入与重复操作；任何状态丢失、越权、崩溃、泄漏或重复副作用都推翻完成结论。</Answer>

**问题 5**：从第4版语境迁移到现代目标SDK时如何控制变量？
<Answer>先按原书还原机制并锁定基线，再查询目标SDK合同；一次只迁移组件导出、存储、权限、后台、通知或依赖之一，保存构建、行为、测试和回滚差异。</Answer>

**问题 6**：本页达到独立交接标准需要什么？
<Answer>需要版本与设备指纹、源码提交、输入、构建命令、生命周期轨迹、原始日志、失败测试、安全边界与发布产物，让他人凭音频资源清单、SoundPool状态机、单元测试、mock交互与资源释放证据重放。</Answer>

</Exercises>

<Glossary>
  <GlossaryItem term="生命周期所有者">负责创建、取消并最终释放组件、任务或资源的明确作用域。</GlossaryItem>
  <GlossaryItem term="可恢复状态">在配置变化或进程重建后，可从保存事实重新构造的最小用户状态。</GlossaryItem>
  <GlossaryItem term="外部合同">Intent、文件、数据库、网络、系统服务或其他应用与本组件交换数据时的可验证约束。</GlossaryItem>
  <GlossaryItem term="版本边界">Android版本、targetSdk、设备形态、权限与依赖共同限定的行为适用范围。</GlossaryItem>
  <GlossaryItem term="证据链">由环境指纹、输入、操作、日志、状态快照和断言组成的可重放结论。</GlossaryItem>
</Glossary>

[← 上一页：第19章 Data Binding and MVVM](/learn/big-nerd-ranch-guide/intent-data/data-binding-mvvm) · [下一页：第21章 Styles and Themes →](/learn/big-nerd-ranch-guide/intent-data/styles-themes)

<Attribution adaptedFrom="Android Programming: The Big Nerd Ranch Guide, 4th Edition" adaptedUrl="https://www.oreilly.com/library/view/android-programming-the/9780135257555/" />
