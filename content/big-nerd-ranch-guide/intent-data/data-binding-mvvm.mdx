---
title: 数据绑定与MVVM
type: C
section: Intent与数据
order: 19
description: 理解 MVVM 架构模式，使用 Data Binding Library 消除样板代码，让 View 和 ViewModel 自动同步。
demo: true
math: false
sourceUrl: "https://www.oreilly.com/library/view/android-programming-the/9780135257555/"
draft: false
---

import { BnrLifecycleLab, BnrFailureLab, BnrEvidenceLab } from "@/components/mdx/big-nerd-ranch-guide/diagrams/data-binding-mvvm";

{/* 知识点清单：
   1) MVVM 三层职责 = 对照表
   2) layout/data/variable = 代码对照
   3) lifecycleOwner + 绑定 = 代码对照
   4) ViewBinding vs DataBinding = Callout
   5) XML 不写业务逻辑 = Callout
*/}

<Objectives>

- 能解释“第19章 Data Binding and MVVM”全部正式节点的组件所有者、生命周期、线程、状态来源与Android版本边界。
- 能实现“区分MVVM view model与Jetpack ViewModel，通过绑定表达式、observable数据和asset加载建立BeatBox”的最小垂直切片，并保存构建、设备操作、原始日志和断言。
- 能比较正常创建、旋转、后台、进程重建、权限拒绝与外部故障下的行为差异。
- 能设计推翻当前实现的反例，并凭架构依赖图、绑定生成结果、asset清单、旋转与LiveData更新测试完成独立交接。

</Objectives>

## 你的 UI 代码为什么越来越臃肿？

一个典型的 Activity：要在 `onCreate` 里 `findViewById` 拿几十个 View，要手动 `setText` 更新数据，要手动 `setOnClickListener`。数据一变更，要记住更新所有相关的 View。漏了一个——那就是 bug。

MVVM + Data Binding 解决了这个痛点：**数据变，界面自动变**。你只管更新 ViewModel 里的字段，XML 布局自动反映最新数据。

## MVVM 三层

| 层 | 职责 | Kotlin 里是什么 |
|---|---|---|
| **Model** | 数据和业务逻辑 | Repository、Room Entity、网络请求 |
| **View** | UI 展示（XML + Activity/Fragment） | layout XML、Activity/Fragment |
| **ViewModel** | 为 View 准备数据、处理 UI 逻辑 | ViewModel 子类 + LiveData |

**核心原则**：ViewModel 不知道 View 的存在——它只暴露数据。View 观察 ViewModel 的数据变化，自动更新界面。

{/* MVVM 数据流闭环动画：一次点击如何流到 ViewModel，再经 DataBinding 自动推回 View */}

<MvvmDataFlowDiagram />

## Data Binding 实战

启用 Data Binding（build.gradle 加 `dataBinding { enabled = true }`），然后用 `<layout>` 包裹布局：

```xml
<layout xmlns:android="...">
    <data>
        <variable name="viewModel" type="com.example.MainViewModel" />
    </data>

    <LinearLayout ...>
        <TextView
            android:text="@{viewModel.userName}" />
        <Button
            android:onClick="@{() -> viewModel.onSaveClick()}" />
    </LinearLayout>
</layout>
```

Activity 端只需一行绑定：

```kotlin
val binding: ActivityMainBinding = DataBindingUtil.setContentView(this, R.layout.activity_main)
binding.lifecycleOwner = this  // 让 LiveData 自动感知生命周期
binding.viewModel = viewModel
```

### ViewModel

```kotlin
class MainViewModel : ViewModel() {
    private val _userName = MutableLiveData("张三")
    val userName: LiveData<String> = _userName

    fun onSaveClick() {
        _userName.value = "李四"  // 改数据 → View 自动更新！
    }
}
```

`LiveData` + Data Binding = 数据驱动 UI。ViewModel 只管改数据，XML 绑定自动反映——不用手动 `findViewById` + `setText`。

<Stepper>
  <Step title="① 创建 ViewModel">
    继承 `ViewModel()`，内部用 `MutableLiveData` 存数据，对外暴露不可变的 `LiveData`。当数据变化时（如 `_userName.value = "李四"`），LiveData 自动通知所有观察者。
  </Step>
  <Step title="② XML 加 layout/data/variable">
    用 `<layout>` 标签包裹根布局，`<data>` 块内声明 `<variable>` 指向 ViewModel 类。在 View 属性中用 `@\{viewModel.field\}` 表达式绑定数据，用 `@\{() -> viewModel.method()\}` 绑定点击事件。
  </Step>
  <Step title="③ Activity 设 binding + lifecycleOwner">
    `DataBindingUtil.setContentView(this, R.layout.activity_main)` 获取 Binding 对象，`binding.lifecycleOwner = this` 让 LiveData 自动感知 Activity 生命周期——只在 STARTED 以上状态更新 UI。最后 `binding.viewModel = viewModel` 把 ViewModel 传给 XML。
  </Step>
  <Step title="④ ViewModel 暴露 LiveData，View 自动更新">
    ViewModel 只管改 `_field.value` 的值，XML 中通过 `@\{viewModel.field\}` 绑定的 View 自动刷新——从此告别 `findViewById` + `setText`。
  </Step>
</Stepper>

## 容易踩的坑

<Callout type="trap">
  **现象**：Data Binding 的表达式里写了复杂逻辑，`@{viewModel.items.size > 0 ? View.VISIBLE : View.GONE}`——XML 变成了一堆三元表达式。**原因**：XML 只应该绑定数据，不应该写业务逻辑。**修法**：把逻辑放到 ViewModel 里——暴露 `val hasItems: LiveData<Boolean>`，XML 里只写 `@{viewModel.hasItems}`。
</Callout>

<Callout type="trap">
  **现象**：RecyclerView 的 Adapter 里也用 Data Binding——到处是 `binding.item = item`。**原因**：不是所有场景都适合 Data Binding。RecyclerView 的 item 更新频率高时，ViewBinding + ViewHolder 手动更新性能更好。**修法**：列表页面用 ViewBinding，单页面用 Data Binding——各取所长。
</Callout>

<Callout type="trap">
  **现象**：忘了 `binding.lifecycleOwner = this`，LiveData 绑定不更新。**原因**：Data Binding 需要 lifecycleOwner 才能感知 STARTED 以上生命周期。**修法**：setContentView 后立即设置 lifecycleOwner。
</Callout>

## 小结

- MVVM：Model（数据）、View（UI）、ViewModel（为View准备数据）——解耦可测试
- Data Binding 消除 findViewById 和手动 setText——数据变界面自动变
- LiveData + Data Binding = 生命周期安全的数据驱动 UI
- ViewBinding（轻量，只解决 findViewById）vs DataBinding（重量，支持表达式绑定）
- 列表场景用 ViewBinding，详情页用 DataBinding

<Exercises>

**问题 1**：“第19章 Data Binding and MVVM”覆盖哪些正式节点和项目主线？
<Answer>覆盖11个节点，从“19. Data Binding and MVVM”到“For the More Curious: LiveData and Data Binding”。主线是区分MVVM view model与Jetpack ViewModel，通过绑定表达式、observable数据和asset加载建立BeatBox，交付物是架构依赖图、绑定生成结果、asset清单、旋转与LiveData更新测试。</Answer>

**问题 2**：怎样建立本页最小可执行实验？
<Answer>固定JDK、SDK、Gradle、设备API、语言、权限和输入，完成一个从用户动作到可观察结果的垂直切片；保存干净构建、设备操作、原始日志、状态快照与断言。</Answer>

**问题 3**：为什么只在正常点击路径运行不能证明完成？
<Answer>它没有覆盖组件重建、进程死亡、异步取消、权限拒绝、外部输入失败、资源释放和版本变化。应逐项注入故障，并用架构依赖图、绑定生成结果、asset清单、旋转与LiveData更新测试核对。</Answer>

**问题 4**：怎样设计能推翻当前实现的反例？
<Answer>保持其他变量不变，分别注入旋转、后台回收、进程死亡、拒权、断网、无效输入与重复操作；任何状态丢失、越权、崩溃、泄漏或重复副作用都推翻完成结论。</Answer>

**问题 5**：从第4版语境迁移到现代目标SDK时如何控制变量？
<Answer>先按原书还原机制并锁定基线，再查询目标SDK合同；一次只迁移组件导出、存储、权限、后台、通知或依赖之一，保存构建、行为、测试和回滚差异。</Answer>

**问题 6**：本页达到独立交接标准需要什么？
<Answer>需要版本与设备指纹、源码提交、输入、构建命令、生命周期轨迹、原始日志、失败测试、安全边界与发布产物，让他人凭架构依赖图、绑定生成结果、asset清单、旋转与LiveData更新测试重放。</Answer>

</Exercises>

<Glossary>

<GlossaryItem term="MVVM">
  Model-View-ViewModel 架构模式。Model 管数据，View 管界面展示，ViewModel 为 View 准备数据并处理 UI 逻辑。View 观察 ViewModel 的数据变化，自动更新界面。
</GlossaryItem>

<GlossaryItem term="Data Binding">
  Android Jetpack 组件库的一部分。在 XML 布局中用 `@{}` 表达式直接绑定 ViewModel 数据，运行时自动同步。消除 findViewById 和手动 UI 更新。
</GlossaryItem>

</Glossary>

<Attribution />


{/* BNR4_AUTHORITY_SUPPLEMENT */}

## 从生命周期、状态与证据开始

第4版不是API词典，而是六个逐步扩展的应用实验。每一章都要回答：用户从哪里进入，哪个Android组件拥有工作，回调在哪个线程发生，哪些事实必须跨旋转或进程死亡恢复，外部系统怎样失败，资源在何时释放。<Term def="负责创建、取消并最终释放组件、任务或资源的明确作用域">生命周期所有者</Term>把Activity、Fragment视图、ViewModel、Worker、线程与资源的寿命连接起来；没有所有者的异步任务就是潜在泄漏。

界面字段不是天然事实。<Term def="在配置变化或进程重建后，可从保存事实重新构造的最小用户状态">可恢复状态</Term>应根据数据大小与寿命放入saved instance state、ViewModel、数据库、文件或调度器，而不是全部塞进单例。Intent、文件、数据库、网络、系统服务和其他应用形成<Term def="跨边界交换数据时必须验证的action、类型、权限、大小、时序和失败约束">外部合同</Term>；正常响应只是合同中的一个样本。

本书以2019年第四版、Android 5.0至8.1及其后续兼容实践为语境。<Term def="Android版本、targetSdk、设备形态、权限与依赖共同限定的行为适用范围">版本边界</Term>要求先还原书中机制，再查询现代目标SDK的权限、后台、存储、通知和组件导出合同。<Term def="由环境指纹、输入、操作、日志、状态快照和断言组成的可重放结论">证据链</Term>则保证“在我的模拟器能跑”不会冒充可交付结论。

## 第4版正式目录逐节点映射

### 19. Data Binding and MVVM

**正式节点 1/11。** 先说明“19. Data Binding and MVVM”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“区分MVVM view model与Jetpack ViewModel，通过绑定表达式、observable数据和asset加载建立BeatBox”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Different Architectures: Why Bother?

**正式节点 2/11。** 先说明“Different Architectures: Why Bother?”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“区分MVVM view model与Jetpack ViewModel，通过绑定表达式、observable数据和asset加载建立BeatBox”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### MVVM View Models vs Jetpack ViewModels

**正式节点 3/11。** 先说明“MVVM View Models vs Jetpack ViewModels”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“区分MVVM view model与Jetpack ViewModel，通过绑定表达式、observable数据和asset加载建立BeatBox”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Creating BeatBox

**正式节点 4/11。** 先说明“Creating BeatBox”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“区分MVVM view model与Jetpack ViewModel，通过绑定表达式、observable数据和asset加载建立BeatBox”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Implementing Simple Data Binding

**正式节点 5/11。** 先说明“Implementing Simple Data Binding”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“区分MVVM view model与Jetpack ViewModel，通过绑定表达式、observable数据和asset加载建立BeatBox”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Importing Assets

**正式节点 6/11。** 先说明“Importing Assets”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“区分MVVM view model与Jetpack ViewModel，通过绑定表达式、observable数据和asset加载建立BeatBox”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Accessing Assets

**正式节点 7/11。** 先说明“Accessing Assets”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“区分MVVM view model与Jetpack ViewModel，通过绑定表达式、observable数据和asset加载建立BeatBox”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Wiring Up Assets for Use

**正式节点 8/11。** 先说明“Wiring Up Assets for Use”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“区分MVVM view model与Jetpack ViewModel，通过绑定表达式、observable数据和asset加载建立BeatBox”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Binding to Data

**正式节点 9/11。** 先说明“Binding to Data”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“区分MVVM view model与Jetpack ViewModel，通过绑定表达式、observable数据和asset加载建立BeatBox”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### For the More Curious: More About Data Binding

**正式节点 10/11。** 先说明“For the More Curious: More About Data Binding”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“区分MVVM view model与Jetpack ViewModel，通过绑定表达式、observable数据和asset加载建立BeatBox”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### For the More Curious: LiveData and Data Binding

**正式节点 11/11。** 先说明“For the More Curious: LiveData and Data Binding”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“区分MVVM view model与Jetpack ViewModel，通过绑定表达式、observable数据和asset加载建立BeatBox”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

## 最小可执行切片

先把状态转换与Android副作用分开。下面的Kotlin骨架不替代本章代码，而是让旋转、进程恢复与失败输入可单测：

```kotlin
sealed interface ScreenState {
    data object Loading : ScreenState
    data class Ready(val key: String) : ScreenState
    data class Failed(val reason: String, val retryable: Boolean) : ScreenState
}

fun reduce(current: ScreenState, event: UiEvent): ScreenState = nextState(current, event)
```

固定JDK、SDK、Gradle、设备API、语言和输入，清理缓存后从命令行保存制品与日志：

```bash
./gradlew --no-daemon clean test assembleDebug
adb install -r app/build/outputs/apk/debug/app-debug.apk
adb shell am force-stop com.example.app
adb shell monkey -p com.example.app 1
adb logcat -d -v threadtime > run.log
```

先写行为断言，再执行用户操作和故障注入：

```gherkin
Given a pinned build, device API, locale, permission state, and input
When the owner is recreated after state saving or process death
Then durable facts reconstruct the same user-visible state
And cancelled work cannot update a destroyed owner
And denied permission, offline input, and malformed data remain recoverable
```

三段代码分别约束状态模型、环境重放和行为结果。真实项目还要加入静态检查、单元测试、仪器测试、无障碍检查和发布构建；日志只能支持断言，不能代替断言。

## 线程、状态与外部资源

主线程负责输入、生命周期分发和大部分UI工作，磁盘、网络、图片解码与长计算不能阻塞帧。后台结果返回前必须确认所有者仍有效；Fragment的生命周期与其View生命周期不是一回事。每个实现都应列出入口、线程、事实源、取消点、关闭责任和用户可见失败六项。

跨应用Intent、ContentProvider、WebView、网络响应、文件URI和深链都视为不可信输入。校验action、scheme、MIME、大小、授权与响应者；导出组件使用最小权限；日志、截图和测试数据库不得泄露令牌或个人数据。相机、音频、数据库、线程和回调注册必须在成功、异常与取消路径都释放。

性能以用户任务衡量：启动看可交互时刻，列表看掉帧与绑定分配，网络看超时和取消，后台工作看约束、电量与重复调度。每次比较固定设备温度、构建类型、数据规模和网络条件，先采基线再改变一个变量。

迁移现代目标SDK时，先在第4版语境中证明机制，再逐项处理组件导出、存储、通知、后台启动、权限、废弃API和依赖版本。每一步保存编译差异、行为差异、测试结果和回滚点；新API并不自动正确，旧API能编译也不代表仍符合平台政策。

## 三步验证实验

<Steps>
  <Step title="1. 建立目录节点与生命周期图">
    从“区分MVVM view model与Jetpack ViewModel，通过绑定表达式、observable数据和asset加载建立BeatBox”选一个用户任务，标出入口、组件、所有者、回调线程、可恢复状态和销毁点。先预测旋转、后台和进程恢复后哪些对象消失、哪些事实必须重建。
    <BnrLifecycleLab />
  </Step>
  <Step title="2. 注入生命周期与外部故障">
    依次改变生命周期阶段、网络和权限，每次只改变一个条件；记录原始日志、界面状态、持久事实与资源计数，区分预期失败和未知状态。
    <BnrFailureLab />
  </Step>
  <Step title="3. 通过交付证据门">
    用架构依赖图、绑定生成结果、asset清单、旋转与LiveData更新测试核对版本设备、生命周期、持久状态、失败安全、测试和发布。缺少任何一项时，不得把正常点击路径视为完成。
    <BnrEvidenceLab />
  </Step>
</Steps>

<Callout type="trap">**核心陷阱。** 只复述类名与回调，未说明所有者、状态来源、线程、取消和失败后果。修复时回到用户任务和状态机，注入旋转、进程死亡、拒权、断网与无效输入。</Callout>

<Callout type="trap">**历史示例不等于当前平台政策。** 第4版准确反映出版时工具链；现代targetSdk可能改变组件导出、后台、存储、通知和权限。保留原书机制，再建立有证据的迁移层。</Callout>

<Callout type="warning">**真机与数据安全。** 使用测试账号、样本媒体和最小权限；清理日志、数据库与截图。签名密钥、API密钥、联系人和真实位置不得写入仓库或公开证据。</Callout>

## 本章回顾

本页从“19. Data Binding and MVVM”覆盖到“For the More Curious: LiveData and Data Binding”，共11个正式目录节点。掌握标准不是记住API，而是能沿“区分MVVM view model与Jetpack ViewModel，通过绑定表达式、observable数据和asset加载建立BeatBox”解释组件与数据边界，运行正常、重建和失败实验，并让另一位开发者仅凭架构依赖图、绑定生成结果、asset清单、旋转与LiveData更新测试重放结论。

<Exercises>

**问题 1**：“第19章 Data Binding and MVVM”覆盖哪些正式节点和项目主线？
<Answer>覆盖11个节点，从“19. Data Binding and MVVM”到“For the More Curious: LiveData and Data Binding”。主线是区分MVVM view model与Jetpack ViewModel，通过绑定表达式、observable数据和asset加载建立BeatBox，交付物是架构依赖图、绑定生成结果、asset清单、旋转与LiveData更新测试。</Answer>

**问题 2**：怎样建立本页最小可执行实验？
<Answer>固定JDK、SDK、Gradle、设备API、语言、权限和输入，完成一个从用户动作到可观察结果的垂直切片；保存干净构建、设备操作、原始日志、状态快照与断言。</Answer>

**问题 3**：为什么只在正常点击路径运行不能证明完成？
<Answer>它没有覆盖组件重建、进程死亡、异步取消、权限拒绝、外部输入失败、资源释放和版本变化。应逐项注入故障，并用架构依赖图、绑定生成结果、asset清单、旋转与LiveData更新测试核对。</Answer>

**问题 4**：怎样设计能推翻当前实现的反例？
<Answer>保持其他变量不变，分别注入旋转、后台回收、进程死亡、拒权、断网、无效输入与重复操作；任何状态丢失、越权、崩溃、泄漏或重复副作用都推翻完成结论。</Answer>

**问题 5**：从第4版语境迁移到现代目标SDK时如何控制变量？
<Answer>先按原书还原机制并锁定基线，再查询目标SDK合同；一次只迁移组件导出、存储、权限、后台、通知或依赖之一，保存构建、行为、测试和回滚差异。</Answer>

**问题 6**：本页达到独立交接标准需要什么？
<Answer>需要版本与设备指纹、源码提交、输入、构建命令、生命周期轨迹、原始日志、失败测试、安全边界与发布产物，让他人凭架构依赖图、绑定生成结果、asset清单、旋转与LiveData更新测试重放。</Answer>

</Exercises>

<Glossary>
  <GlossaryItem term="生命周期所有者">负责创建、取消并最终释放组件、任务或资源的明确作用域。</GlossaryItem>
  <GlossaryItem term="可恢复状态">在配置变化或进程重建后，可从保存事实重新构造的最小用户状态。</GlossaryItem>
  <GlossaryItem term="外部合同">Intent、文件、数据库、网络、系统服务或其他应用与本组件交换数据时的可验证约束。</GlossaryItem>
  <GlossaryItem term="版本边界">Android版本、targetSdk、设备形态、权限与依赖共同限定的行为适用范围。</GlossaryItem>
  <GlossaryItem term="证据链">由环境指纹、输入、操作、日志、状态快照和断言组成的可重放结论。</GlossaryItem>
</Glossary>

[← 上一页：第18章 Accessibility](/learn/big-nerd-ranch-guide/intent-data/accessibility) · [下一页：第20章 Unit Testing and Audio Playback →](/learn/big-nerd-ranch-guide/intent-data/audio-unit-testing)

<Attribution adaptedFrom="Android Programming: The Big Nerd Ranch Guide, 4th Edition" adaptedUrl="https://www.oreilly.com/library/view/android-programming-the/9780135257555/" />
