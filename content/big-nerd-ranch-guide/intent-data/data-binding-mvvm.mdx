---
title: 数据绑定与MVVM
type: C
section: Intent与数据
order: 19
description: 理解 MVVM 架构模式，使用 Data Binding Library 消除样板代码，让 View 和 ViewModel 自动同步。
demo: true
math: false
sourceUrl: "https://www.oreilly.com/library/view/android-programming-the/9780135257555/"
draft: false
qualityVersion: 2
practiceMode: code
sourceMode: independent-rewrite
---

import { BnrLifecycleLab, BnrFailureLab, BnrEvidenceLab } from "@/components/mdx/big-nerd-ranch-guide/diagrams/data-binding-mvvm";

{/* 知识点清单：
   1) MVVM 三层职责 = 对照表
   2) layout/data/variable = 代码对照
   3) lifecycleOwner + 绑定 = 代码对照
   4) ViewBinding vs DataBinding = Callout
   5) XML 不写业务逻辑 = Callout
*/}

<Objectives>

- 能沿“数据绑定与MVVM”的用户事件解释Android组件、状态所有者、线程与销毁边界。
- 能围绕“理解 MVVM 架构模式，使用 Data Binding Library 消除样板代码，让 View 和 ViewModel 自动同步。”改出一个可运行结果，并用前后状态而非组件数量验收。
- 能在旋转、进程重建、拒权、离线或无效输入中选择适用反例，定位首个状态分叉。
- 能用构建指纹、用户操作、状态快照、原始日志和行为断言独立重放结论，并标明第四版机制与现代targetSdk政策的边界。

</Objectives>

## 你的 UI 代码为什么越来越臃肿？

一个典型的 Activity：要在 `onCreate` 里 `findViewById` 拿几十个 View，要手动 `setText` 更新数据，要手动 `setOnClickListener`。数据一变更，要记住更新所有相关的 View。漏了一个——那就是 bug。

MVVM + Data Binding 解决了这个痛点：**数据变，界面自动变**。你只管更新 ViewModel 里的字段，XML 布局自动反映最新数据。

## MVVM 三层

| 层 | 职责 | Kotlin 里是什么 |
|---|---|---|
| **Model** | 数据和业务逻辑 | Repository、Room Entity、网络请求 |
| **View** | UI 展示（XML + Activity/Fragment） | layout XML、Activity/Fragment |
| **ViewModel** | 为 View 准备数据、处理 UI 逻辑 | ViewModel 子类 + LiveData |

**核心原则**：ViewModel 不知道 View 的存在——它只暴露数据。View 观察 ViewModel 的数据变化，自动更新界面。

{/* MVVM 数据流闭环动画：一次点击如何流到 ViewModel，再经 DataBinding 自动推回 View */}

<MvvmDataFlowDiagram />

## Data Binding 实战

启用 Data Binding（build.gradle 加 `dataBinding { enabled = true }`），然后用 `<layout>` 包裹布局：

```xml
<layout xmlns:android="...">
    <data>
        <variable name="viewModel" type="com.example.MainViewModel" />
    </data>

    <LinearLayout ...>
        <TextView
            android:text="@{viewModel.userName}" />
        <Button
            android:onClick="@{() -> viewModel.onSaveClick()}" />
    </LinearLayout>
</layout>
```

Activity 端只需一行绑定：

```kotlin
val binding: ActivityMainBinding = DataBindingUtil.setContentView(this, R.layout.activity_main)
binding.lifecycleOwner = this  // 让 LiveData 自动感知生命周期
binding.viewModel = viewModel
```

### ViewModel

```kotlin
class MainViewModel : ViewModel() {
    private val _userName = MutableLiveData("张三")
    val userName: LiveData<String> = _userName

    fun onSaveClick() {
        _userName.value = "李四"  // 改数据 → View 自动更新！
    }
}
```

`LiveData` + Data Binding = 数据驱动 UI。ViewModel 只管改数据，XML 绑定自动反映——不用手动 `findViewById` + `setText`。

<Stepper>
  <Step title="① 创建 ViewModel">
    继承 `ViewModel()`，内部用 `MutableLiveData` 存数据，对外暴露不可变的 `LiveData`。当数据变化时（如 `_userName.value = "李四"`），LiveData 自动通知所有观察者。
  </Step>
  <Step title="② XML 加 layout/data/variable">
    用 `<layout>` 标签包裹根布局，`<data>` 块内声明 `<variable>` 指向 ViewModel 类。在 View 属性中用 `@\{viewModel.field\}` 表达式绑定数据，用 `@\{() -> viewModel.method()\}` 绑定点击事件。
  </Step>
  <Step title="③ Activity 设 binding + lifecycleOwner">
    `DataBindingUtil.setContentView(this, R.layout.activity_main)` 获取 Binding 对象，`binding.lifecycleOwner = this` 让 LiveData 自动感知 Activity 生命周期——只在 STARTED 以上状态更新 UI。最后 `binding.viewModel = viewModel` 把 ViewModel 传给 XML。
  </Step>
  <Step title="④ ViewModel 暴露 LiveData，View 自动更新">
    ViewModel 只管改 `_field.value` 的值，XML 中通过 `@\{viewModel.field\}` 绑定的 View 自动刷新——从此告别 `findViewById` + `setText`。
  </Step>
</Stepper>

## 容易踩的坑

<Callout type="trap">
  **现象**：Data Binding 的表达式里写了复杂逻辑，`@{viewModel.items.size > 0 ? View.VISIBLE : View.GONE}`——XML 变成了一堆三元表达式。**原因**：XML 只应该绑定数据，不应该写业务逻辑。**修法**：把逻辑放到 ViewModel 里——暴露 `val hasItems: LiveData<Boolean>`，XML 里只写 `@{viewModel.hasItems}`。
</Callout>

<Callout type="trap">
  **现象**：RecyclerView 的 Adapter 里也用 Data Binding——到处是 `binding.item = item`。**原因**：不是所有场景都适合 Data Binding。RecyclerView 的 item 更新频率高时，ViewBinding + ViewHolder 手动更新性能更好。**修法**：列表页面用 ViewBinding，单页面用 Data Binding——各取所长。
</Callout>

<Callout type="trap">
  **现象**：忘了 `binding.lifecycleOwner = this`，LiveData 绑定不更新。**原因**：Data Binding 需要 lifecycleOwner 才能感知 STARTED 以上生命周期。**修法**：setContentView 后立即设置 lifecycleOwner。
</Callout>

## 小结

- MVVM：Model（数据）、View（UI）、ViewModel（为View准备数据）——解耦可测试
- Data Binding 消除 findViewById 和手动 setText——数据变界面自动变
- LiveData + Data Binding = 生命周期安全的数据驱动 UI
- ViewBinding（轻量，只解决 findViewById）vs DataBinding（重量，支持表达式绑定）
- 列表场景用 ViewBinding，详情页用 DataBinding

<Exercises>

**问题 1**：“第19章 Data Binding and MVVM”覆盖哪些正式节点和项目主线？

<Answer>覆盖11个节点，从“19. Data Binding and MVVM”到“For the More Curious: LiveData and Data Binding”。主线是区分MVVM view model与Jetpack ViewModel，通过绑定表达式、observable数据和asset加载建立BeatBox，交付物是架构依赖图、绑定生成结果、asset清单、旋转与LiveData更新测试。</Answer>

**问题 2**：怎样建立本页最小可执行实验？

<Answer>在“数据绑定与MVVM”的验证中，固定JDK、SDK、Gradle、设备API、语言、权限和输入，完成一个从用户动作到可观察结果的垂直切片；保存干净构建、设备操作、原始日志、状态快照与断言。</Answer>

**问题 3**：为什么只在正常点击路径运行不能证明完成？

<Answer>它没有覆盖组件重建、进程死亡、异步取消、权限拒绝、外部输入失败、资源释放和版本变化。应逐项注入故障，并用架构依赖图、绑定生成结果、asset清单、旋转与LiveData更新测试核对。</Answer>

**问题 4**：怎样设计能推翻当前实现的反例？

<Answer>在“数据绑定与MVVM”的验证中，保持其他变量不变，分别注入旋转、后台回收、进程死亡、拒权、断网、无效输入与重复操作；任何状态丢失、越权、崩溃、泄漏或重复副作用都推翻完成结论。</Answer>

**问题 5**：从第4版语境迁移到现代目标SDK时如何控制变量？

<Answer>先按原书还原机制并锁定基线，再查询目标SDK合同；一次只迁移组件导出、存储、权限、后台、通知或依赖之一，保存构建、行为、测试和回滚差异。</Answer>

**问题 6**：本页达到独立交接标准需要什么？

<Answer>需要版本与设备指纹、源码提交、输入、构建命令、生命周期轨迹、原始日志、失败测试、安全边界与发布产物，让他人凭架构依赖图、绑定生成结果、asset清单、旋转与LiveData更新测试重放。</Answer>

</Exercises>

<Glossary>

<GlossaryItem term="MVVM">
  Model-View-ViewModel 架构模式。Model 管数据，View 管界面展示，ViewModel 为 View 准备数据并处理 UI 逻辑。View 观察 ViewModel 的数据变化，自动更新界面。
</GlossaryItem>

<GlossaryItem term="Data Binding">
  Android Jetpack 组件库的一部分。在 XML 布局中用 `@{}` 表达式直接绑定 ViewModel 数据，运行时自动同步。消除 findViewById 和手动 UI 更新。
</GlossaryItem>

</Glossary>

{/* BNR4_QUALITY_V2 */}

“数据绑定与MVVM”不使用未获授权的纸书正文；[InformIT出版信息](https://www.informit.com/store/android-programming-the-big-nerd-ranch-guide-9780135245125)与[授权电子版完整目录](https://www.oreilly.com/library/view/android-programming-the/9780135257555/)只用于确认第四版32章、269个正式目录节点和时代语境，[第四版官方勘误](https://github.com/bignerdranch/AndroidCourseResources/raw/master/4thEdition/Errata/4eAddendum.pdf)用于识别工具链变更。下列中文解释、图示、交互、代码与练习均为独立教学重写，平台行为再以[Android Developers](https://developer.android.com/develop)的一手文档复核。

## 为什么“数据绑定与MVVM”必须回到可观察状态

“数据绑定与MVVM”的学习结果不是记住类名，而是能预测“理解 MVVM 架构模式，使用 Data Binding Library 消除样板代码，让 View 和 ViewModel 自动同步。”在一次输入、一次重建和一次失败中的不同状态，并指出哪条Android合同产生差异。

## 第四版机制逐项深读

### 19. Data Binding and MVVM

在“数据绑定与MVVM”中，验证“19. Data Binding and MVVM”用同一事件序列比较旋转前后状态和副作用次数；界面相同但重复写入仍是不正确。

### Different Architectures: Why Bother?

在“数据绑定与MVVM”中，验证“Different Architectures: Why Bother?”用同一事件序列比较旋转前后状态和副作用次数；界面相同但重复写入仍是不正确。

### MVVM View Models vs Jetpack ViewModels

在“数据绑定与MVVM”中，“MVVM View Models vs Jetpack ViewModels”的价值由依赖方向与可测试状态转换证明，而不是由类名是否含Model、View或Repository决定。

### Creating BeatBox

在“数据绑定与MVVM”中，验证“Creating BeatBox”覆盖多指、取消、越界、旋转和辅助操作，比较模型框、Canvas输出与可访问性节点。

### Implementing Simple Data Binding

在“数据绑定与MVVM”中，“Implementing Simple Data Binding”按事实、界面表示和用户事件拆责任；状态所有者不能持有已销毁View，界面也不能绕过边界直接改持久数据。

### Importing Assets

在“数据绑定与MVVM”中，“Importing Assets”服务于“理解 MVVM 架构模式，使用 Data Binding Library 消除样板代码，让 View 和 ViewModel 自动同步。”；解释要落到用户事件、Android所有者、状态变化、线程和可观察结果，并给出一个会推翻实现的反例。

### Accessing Assets

在“数据绑定与MVVM”中，“Accessing Assets”若依赖第四版时期API，应分开说明原书机制与现代平台政策，并用Android官方文档核对迁移边界。

### Wiring Up Assets for Use

在“数据绑定与MVVM”中，“Wiring Up Assets for Use”服务于“理解 MVVM 架构模式，使用 Data Binding Library 消除样板代码，让 View 和 ViewModel 自动同步。”；解释要落到用户事件、Android所有者、状态变化、线程和可观察结果，并给出一个会推翻实现的反例。

### Binding to Data

在“数据绑定与MVVM”中，“Binding to Data”服务于“理解 MVVM 架构模式，使用 Data Binding Library 消除样板代码，让 View 和 ViewModel 自动同步。”；解释要落到用户事件、Android所有者、状态变化、线程和可观察结果，并给出一个会推翻实现的反例。

### For the More Curious: More About Data Binding

在“数据绑定与MVVM”中，验证“For the More Curious: More About Data Binding”用同一事件序列比较旋转前后状态和副作用次数；界面相同但重复写入仍是不正确。

### For the More Curious: LiveData and Data Binding

在“数据绑定与MVVM”中，分析“For the More Curious: LiveData and Data Binding”要画出事件进入、状态归约、数据源写入和UI重绘四条边，并指出配置变化时谁被重建。

## 可操作机制实验

“数据绑定与MVVM”把“理解 MVVM 架构模式，使用 Data Binding Library 消除样板代码，让 View 和 ViewModel 自动同步。在“数据绑定与MVVM”的验证中，”拆成三个可观察层次：先选择目录节点并确认责任边界，再改变一个生命周期或外部条件，最后回到构建指纹、用户操作、状态快照、原始日志和行为断言。点击后的文字、状态与轨迹必须同步变化，重置后回到相同基线。

<Steps>
  <Step title="1. 选择机制节点">
    选择一个正式节点，先预测它由谁拥有、在哪个线程执行、哪些状态需要恢复；右侧会显示本章专属的机制合同。

    <BnrLifecycleLab />

  </Step>
  <Step title="2. 注入一个边界条件">
    改变生命周期阶段、外部可用性或权限中的一项，观察回调、队列、数据或渲染结果怎样变化；不要用总风险分数代替因果。

    <BnrFailureLab />

  </Step>
  <Step title="3. 复位并核对证据">
    用构建指纹、用户操作、状态快照、原始日志和行为断言核对预测，随后执行重置；若基线不能复现，实验本身不通过。

    <BnrEvidenceLab />

  </Step>
</Steps>

<Callout type="trap">在“数据绑定与MVVM”中，正常点击成功只能证明一个样本。若重建、取消、无响应者、离线、旧schema或目标SDK变化会产生不同结果，就必须把该边界写进状态机与测试。</Callout>

<Callout type="trap">“数据绑定与MVVM”若只留下最终截图，就无法判断旧所有者是否收到回调、后台任务是否重复、状态是否来自错误层或复位是否真正清空实验。</Callout>

## “数据绑定与MVVM”验收回顾

“数据绑定与MVVM”只有在构建指纹、用户操作、状态快照、原始日志和行为断言能够从相同基线再次得到相同断言时才通过；第四版机制与现代平台政策分别记录，不用新API名称掩盖旧行为。

[← 上一页：Android辅助功能](/learn/big-nerd-ranch-guide/intent-data/accessibility) · [下一页：音频播放与单元测试 →](/learn/big-nerd-ranch-guide/intent-data/audio-unit-testing)

<Attribution
  mode="independent-rewrite"
  sourceBasis="authorized-sample"
  workTitle="Bill Phillips, Chris Stewart, Kristin Marsicano, Brian Gardner, Android Programming: The Big Nerd Ranch Guide, Fourth Edition"
  adaptedUrl="https://www.oreilly.com/library/view/android-programming-the/9780135257555/"
/>
