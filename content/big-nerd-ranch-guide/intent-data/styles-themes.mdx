---
title: 样式与主题
type: C
section: Intent与数据
order: 21
description: 用 Style 和 Theme 统一 App 视觉风格——理解主题继承、Material Design 属性，以及深色模式适配。
demo: true
math: false
sourceUrl: "https://www.oreilly.com/library/view/android-programming-the/9780135257555/"
draft: false
qualityVersion: 2
practiceMode: design
sourceMode: independent-rewrite
---

{/* 知识点清单：
   1) Style 复用 View 属性 = XML 示例
   2) Theme 作用 Application/Activity = Manifest
   3) Style 点记法继承 = 代码
   4) values-night 深色 = 目录结构
   5) 内联属性优先级 = Callout
   6) ?attr 系统色 = Callout
*/}

<Objectives>

- 能沿“样式与主题”的用户事件解释Android组件、状态所有者、线程与销毁边界。
- 能围绕“用 Style 和 Theme 统一 App 视觉风格——理解主题继承、Material Design 属性，以及深色模式适配。”改出一个可运行结果，并用前后状态而非组件数量验收。
- 能在旋转、进程重建、拒权、离线或无效输入中选择适用反例，定位首个状态分叉。
- 能用双视口、大字体、焦点顺序、状态语义和对比度截图独立重放结论，并标明第四版机制与现代targetSdk政策的边界。

</Objectives>

## 你的 App 看起来像打了补丁

同一个 App，按钮有的蓝色有的灰色，文字大小不统一——为什么？因为每个 View 的属性都是手写硬编码的，没有统一管理。Style 和 Theme 就是解决这个问题的——定义一套"视觉规则"，所有 View 照着来。

## Style——View 的"穿搭模板"

```xml
<!-- res/values/styles.xml -->
<style name="PrimaryButton" parent="Widget.Material3.Button">
    <item name="android:textColor">@color/white</item>
    <item name="backgroundTint">@color/primary</item>
    <item name="cornerRadius">8dp</item>
    <item name="android:textSize">16sp</item>
</style>
```

然后一个按钮只需引用这个 style：

```xml
<Button style="@style/PrimaryButton" android:text="登录" />
```

改了 style 里的 `cornerRadius` 从 8dp 到 12dp → 所有引用这个 style 的按钮自动变成圆角 12dp。**统一改一处，全 App 生效**。

### Style 继承

```xml
<!-- 继承父 style（点记法） -->
<style name="PrimaryButton.Outlined">
    <item name="backgroundTint">@android:color/transparent</item>
    <item name="strokeColor">@color/primary</item>
</style>
```

`PrimaryButton.Outlined` 继承 `PrimaryButton` 的所有属性，只覆盖要改的——描边按钮只有背景变透明+加描边，文字大小和圆角沿用父 style。

## Theme——整个 App 的"皮肤"

Theme 也是 Style，但作用于 Application 或 Activity 级别：

```xml
<!-- res/values/themes.xml -->
<style name="Theme.MyApp" parent="Theme.Material3.Light.NoActionBar">
    <item name="colorPrimary">@color/blue_500</item>
    <item name="colorOnPrimary">@color/white</item>
    <item name="android:statusBarColor">@color/blue_700</item>
</style>
```

在 Manifest 中应用：

```xml
<application android:theme="@style/Theme.MyApp">
```

{/* 样式继承链 vs 主题作用域 + 属性优先级对比图 */}

<StyleThemeInheritanceDiagram />

## 深色模式

```xml
<!-- res/values-night/themes.xml -->
<style name="Theme.MyApp" parent="Theme.Material3.Dark.NoActionBar">
    <item name="colorPrimary">@color/blue_200</item>
    <!-- 深色背景、浅色文字由父主题自动处理 -->
</style>
```

`values-night/` 目录——系统切换到深色模式时自动使用这里的资源。Material Design 3 的 Dark 主题帮你处理了大部分颜色映射，你只需要微调品牌色即可。

<Stepper>
  <Step title="① 定义 Theme">
    在 `res/values/themes.xml` 中定义 `<style name="Theme.MyApp" parent="Theme.Material3.Light.NoActionBar">`，设置 `colorPrimary`、`colorOnPrimary`、`statusBarColor` 等属性——这些颜色会渗透到所有 Material 组件中。
  </Step>
  <Step title="② Manifest 应用主题">
    在 `AndroidManifest.xml` 的 `<application>` 标签添加 `android:theme="@style/Theme.MyApp"`，全局生效。也可以单独给某个 `<activity>` 指定不同主题——但一般全局统一。
  </Step>
  <Step title="③ 提取常用 View 的 Style">
    把多个按钮/文本重复的 `android:textSize`、`backgroundTint`、`cornerRadius` 等属性提取到 `res/values/styles.xml` 中，View 上只保留 `style="@style/PrimaryButton"` 引用。用点记法（`PrimaryButton.Outlined`）实现样式继承。
  </Step>
  <Step title="④ 创建 values-night 深色主题">
    新建 `res/values-night/themes.xml`，定义同名 `Theme.MyApp` 但 parent 改为 `Theme.Material3.Dark.NoActionBar`。微调品牌色（如浅蓝 `@color/blue_200` 替代深蓝 `@color/blue_500`），让文字在暗色背景上可读。
  </Step>
</Stepper>

## 容易踩的坑

<Callout type="trap">
  **现象**：改了 style 里的某个属性，但 View 没变化。**原因**：View 上直接写了同名的 XML 属性——内联属性优先级高于 style。**修法**：把 view 上的硬编码属性移到 style 里管理，view 只保留 `style="@style/xxx"` 引用。
</Callout>

<Callout type="trap">
  **现象**：深色模式下文字看不清——白底白字。**原因**：只在 values/colors.xml 定义颜色，没有在 values-night/ 提供深色版本。**修法**：为每个需要适配的颜色提供 night 版本，或使用 `?attr/colorOnSurface` 等系统属性自动切换。
</Callout>

<Callout type="trap">
  **现象**：Material 组件颜色跟 Theme 不一致。**原因**：用了硬编码 `@color/xxx` 而非 `?attr/colorPrimary`。**修法**：主题相关色一律走 theme attribute，便于 night 与动态取色。
</Callout>

## 小结

- Style 复用 View 属性 → 消除 XML 重复、统一视觉
- Theme 是作用在 Application/Activity 级别的 Style → 整体皮肤
- Style 继承（点记法）→ 子 Style 覆盖少量属性，其余继承父
- 深色模式：values-night/ 目录提供暗色版本的资源和主题
- 内联属性优先级最高 → 统一管理时应去掉 View 上的硬编码

<Exercises>

**问题 1**：“第21章 Styles and Themes”覆盖哪些正式节点和项目主线？

<Answer>覆盖9个节点，从“21. Styles and Themes”到“For the More Curious: Accessing Theme Attributes”。主线是分清局部Style、层级继承、Theme与theme attribute的解析链，交付物是颜色与样式资源图、主题覆盖测试、按钮属性差异和继承追踪。</Answer>

**问题 2**：怎样建立本页最小可执行实验？

<Answer>在“样式与主题”的验证中，固定JDK、SDK、Gradle、设备API、语言、权限和输入，完成一个从用户动作到可观察结果的垂直切片；保存干净构建、设备操作、原始日志、状态快照与断言。</Answer>

**问题 3**：为什么只在正常点击路径运行不能证明完成？

<Answer>它没有覆盖组件重建、进程死亡、异步取消、权限拒绝、外部输入失败、资源释放和版本变化。应逐项注入故障，并用颜色与样式资源图、主题覆盖测试、按钮属性差异和继承追踪核对。</Answer>

**问题 4**：怎样设计能推翻当前实现的反例？

<Answer>在“样式与主题”的验证中，保持其他变量不变，分别注入旋转、后台回收、进程死亡、拒权、断网、无效输入与重复操作；任何状态丢失、越权、崩溃、泄漏或重复副作用都推翻完成结论。</Answer>

**问题 5**：从第4版语境迁移到现代目标SDK时如何控制变量？

<Answer>先按原书还原机制并锁定基线，再查询目标SDK合同；一次只迁移组件导出、存储、权限、后台、通知或依赖之一，保存构建、行为、测试和回滚差异。</Answer>

**问题 6**：本页达到独立交接标准需要什么？

<Answer>需要版本与设备指纹、源码提交、输入、构建命令、生命周期轨迹、原始日志、失败测试、安全边界与发布产物，让他人凭颜色与样式资源图、主题覆盖测试、按钮属性差异和继承追踪重放。</Answer>

</Exercises>

<Glossary>

<GlossaryItem term="Style">
  一组 View 属性的集合——定义在 res/values/styles.xml 中，通过 `style="@style/xxx"` 引用。复用 View 属性消除 XML 重复代码。
</GlossaryItem>

<GlossaryItem term="Theme">
  作用在 Application/Activity 级别的特殊 Style。定义全局视觉属性——主色、强调色、背景色、状态栏颜色等。决定整个 App 的视觉基调。
</GlossaryItem>

</Glossary>

{/* BNR4_QUALITY_V2 */}

“样式与主题”不使用未获授权的纸书正文；[InformIT出版信息](https://www.informit.com/store/android-programming-the-big-nerd-ranch-guide-9780135245125)与[授权电子版完整目录](https://www.oreilly.com/library/view/android-programming-the/9780135257555/)只用于确认第四版32章、269个正式目录节点和时代语境，[第四版官方勘误](https://github.com/bignerdranch/AndroidCourseResources/raw/master/4thEdition/Errata/4eAddendum.pdf)用于识别工具链变更。下列中文解释、图示、交互、代码与练习均为独立教学重写，平台行为再以[Android Developers](https://developer.android.com/develop)的一手文档复核。

## 为什么“样式与主题”必须回到可观察状态

“样式与主题”的学习结果不是记住类名，而是能预测“用 Style 和 Theme 统一 App 视觉风格——理解主题继承、Material Design 属性，以及深色模式适配。”在一次输入、一次重建和一次失败中的不同状态，并指出哪条Android合同产生差异。

## 第四版机制逐项深读

### 21. Styles and Themes

在“样式与主题”中，验证“21. Styles and Themes”在日夜主题、不同密度和字体缩放下比较像素边界、对比度与状态反馈。

### Color Resources

在“样式与主题”中，验证“Color Resources”在日夜主题、不同密度和字体缩放下比较像素边界、对比度与状态反馈。

### Styles

在“样式与主题”中，“Styles”通过资源限定符和主题属性把内容与设备配置解耦；最终值由资源匹配和主题继承共同决定。

### Themes

在“样式与主题”中，验证“Themes”在日夜主题、不同密度和字体缩放下比较像素边界、对比度与状态反馈。

### Adding Theme Colors

在“样式与主题”中，“Adding Theme Colors”通过资源限定符和主题属性把内容与设备配置解耦；最终值由资源匹配和主题继承共同决定。

### Overriding Theme Attributes

在“样式与主题”中，“Overriding Theme Attributes”的复用不能吞掉状态语义；pressed、focused、disabled与checked都要有可辨反馈。

### Modifying Button Attributes

在“样式与主题”中，分析“Modifying Button Attributes”先冻结设备API、targetSdk和输入，再沿回调与数据流寻找首个状态分叉，不能只描述最终页面。

### For the More Curious: More on Style Inheritance

在“样式与主题”中，验证“For the More Curious: More on Style Inheritance”在日夜主题、不同密度和字体缩放下比较像素边界、对比度与状态反馈。

### For the More Curious: Accessing Theme Attributes

在“样式与主题”中，分析“For the More Curious: Accessing Theme Attributes”要追踪资源ID、限定目录、密度缩放与属性解析，避免把预览器结果当成所有设备的合同。

<Callout type="trap">在“样式与主题”中，正常点击成功只能证明一个样本。若重建、取消、无响应者、离线、旧schema或目标SDK变化会产生不同结果，就必须把该边界写进状态机与测试。</Callout>

<Callout type="trap">“样式与主题”若只留下最终截图，就无法判断旧所有者是否收到回调、后台任务是否重复、状态是否来自错误层或复位是否真正清空实验。</Callout>

## “样式与主题”验收回顾

“样式与主题”只有在双视口、大字体、焦点顺序、状态语义和对比度截图能够从相同基线再次得到相同断言时才通过；第四版机制与现代平台政策分别记录，不用新API名称掩盖旧行为。

[← 上一页：音频播放与单元测试](/learn/big-nerd-ranch-guide/intent-data/audio-unit-testing) · [下一页：XML drawable →](/learn/big-nerd-ranch-guide/intent-data/xml-drawables)

<Attribution
  mode="independent-rewrite"
  sourceBasis="authorized-sample"
  workTitle="Bill Phillips, Chris Stewart, Kristin Marsicano, Brian Gardner, Android Programming: The Big Nerd Ranch Guide, Fourth Edition"
  adaptedUrl="https://www.oreilly.com/library/view/android-programming-the/9780135257555/"
/>
