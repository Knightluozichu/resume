---
title: 使用intent拍照
type: C
section: Intent与数据
order: 16
description: 学会通过隐式intent调用系统相机拍照、用FileProvider共享文件、处理拍照结果、以及应对Android 10+分区存储的挑战。
demo: true
math: false
sourceUrl: "https://www.oreilly.com/library/view/android-programming-the/9780135257555/"
draft: false
---

import { BnrLifecycleLab, BnrFailureLab, BnrEvidenceLab } from "@/components/mdx/big-nerd-ranch-guide/diagrams/taking-pictures";

{/* 知识点清单：
   1) MediaStore.ACTION_IMAGE_CAPTURE = 代码 walkthrough
   2) FileProvider 配置（manifest + file_paths.xml + authority）= XML + 代码
   3) 创建临时文件（getExternalFilesDir / createTempFile）= 代码
   4) ActivityResultContracts 处理返回 = 代码
   5) 全分辨率 vs 缩略图（EXTRA_OUTPUT / BitmapFactory）= 代码对比
   6) 相机权限（CAMERA / WRITE_EXTERNAL_STORAGE）= Manifest + 运行时
   7) content:// vs file:// URI = 概念 + 代码
   8) Android 10+ Scoped Storage = 说明 + MediaStore 替代
   9) CameraX 简要提及
*/}

<Objectives>

- 能解释“第16章 Taking Pictures with Intents”全部正式节点的组件所有者、生命周期、线程、状态来源与Android版本边界。
- 能实现“通过FileProvider授予最小URI权限，控制照片位置、相机Intent、位图缩放与设备能力”的最小垂直切片，并保存构建、设备操作、原始日志和断言。
- 能比较正常创建、旋转、后台、进程重建、权限拒绝与外部故障下的行为差异。
- 能设计推翻当前实现的反例，并凭照片文件合同、相机降级、缩略图内存测试和配置声明完成独立交接。

</Objectives>

## 为什么需要隐式intent拍照

很多 App 需要"拍一张照片"——用户头像、商品图、打卡记录。自己实现一个相机？你要处理对焦、曝光、闪光灯、前后摄像头切换、预览帧渲染……工程量巨大，而且每个厂商的相机行为还不一样。

Android 的标准做法是：**发出"我要拍照"的请求，让系统相机 App 帮你拍，拍完后把照片还给你**。就像**相机摄影**里的"借设备"——你不需要自己造一台相机，只需告诉专业摄影师"帮我拍一张"，他拍完把底片交给你。

这个看似简单的流程涉及三个关键技术：隐式 intent 调起相机、<Term def="ContentProvider 的特殊子类，用于在 App 之间安全共享文件。生成受权限保护的 content:// URI 替代 file:// URI，Android 7.0+ 跨 App 传文件的标准方式。">FileProvider</Term> 安全共享文件路径、以及 Activity Result API 接收拍照结果。

## 使用intent拍照是怎么工作的

### 调用系统相机：ACTION_IMAGE_CAPTURE

<Term def="Android 系统定义的标准拍照动作常量，值为 `android.media.action.IMAGE_CAPTURE`。发出带此 action 的隐式 intent，系统会调起已安装的相机 App 完成拍照。">MediaStore.ACTION_IMAGE_CAPTURE</Term> 是最经典的隐式 intent 之一。你发出"我要拍照"的请求，系统相机 App 响应并完成拍摄。

基本流程分四步：

```
你的App                    系统相机App
  │                            │
  │── ① 创建临时文件 ──────────│
  │── ② FileProvider 生成 URI ─│
  │── ③ 发出 ACTION_IMAGE_CAPTURE ──→ 打开相机、拍照
  │                            │
  │←── ④ 照片写入你指定的 URI ──│
  │── 显示照片                  │
```

{/* 拍照全链路 + FileProvider 安全交换文件的五步静态流程图 */}

<CameraIntentFlowDiagram />

### FileProvider：为什么不能直接用 file:// URI

Android 7.0（API 24）起，禁止用 `file://` URI 在 App 之间传文件。如果你把 `Uri.fromFile(photoFile)` 传给相机 App，运行时会抛出 `FileUriExposedException`。

原因很直观：别的 App 拿到你的文件系统绝对路径（如 `/data/data/com.myapp/cache/IMG_001.jpg`），就能直接读写你的私有目录——这是严重的安全漏洞。

FileProvider 把文件路径包装成 `content://` URI，并附带临时读写权限。相机 App 只能访问你授权的那一个文件，拍完后权限自动失效。

**Manifest 声明：**

```xml
<provider
    android:name="androidx.core.content.FileProvider"
    android:authorities="${applicationId}.fileprovider"
    android:exported="false"
    android:grantUriPermissions="true">
    <meta-data
        android:name="android.support.FILE_PROVIDER_PATHS"
        android:resource="@xml/file_paths" />
</provider>
```

**res/xml/file_paths.xml（定义可共享的目录范围）：**

```xml
<?xml version="1.0" encoding="utf-8"?>
<paths>
    <cache-path name="cache_photos" path="." />
    <external-files-path name="external_photos" path="Pictures/" />
</paths>
```

`cache-path` 对应 `context.cacheDir`，`external-files-path` 对应 `context.getExternalFilesDir(null)`——这两个都是 App 私有目录，其他 App 无法直接访问，但通过 FileProvider 可以临时授权。

**代码中获取 Content URI：**

<CodeTabs>
  <Tab label="Kotlin">
```kotlin
val photoFile = File.createTempFile("IMG_", ".jpg", cacheDir)
val photoUri = FileProvider.getUriForFile(
    this,
    "${packageName}.fileprovider",  // 与 manifest authorities 一致
    photoFile
)
```
  </Tab>
  <Tab label="Java">
```java
File photoFile = File.createTempFile("IMG_", ".jpg", getCacheDir());
Uri photoUri = FileProvider.getUriForFile(
    this,
    getPackageName() + ".fileprovider",
    photoFile
);
```
  </Tab>
</CodeTabs>

### 处理拍照结果：Activity Result API

旧写法 `onActivityResult` 已废弃。现代写法用 `registerForActivityResult`：

<CodeTabs>
  <Tab label="Kotlin">
```kotlin
private lateinit var photoUri: Uri

private val takePictureLauncher = registerForActivityResult(
    ActivityResultContracts.TakePicture()
) { success ->
    if (success) {
        // 照片已写入 photoUri 指向的文件
        binding.imageView.setImageURI(photoUri)
    }
}

fun takePhoto() {
    val photoFile = File.createTempFile("IMG_", ".jpg", cacheDir)
    photoUri = FileProvider.getUriForFile(
        this, "${packageName}.fileprovider", photoFile
    )
    takePictureLauncher.launch(photoUri)
}
```
  </Tab>
  <Tab label="Java">
```java
private Uri photoUri;

private final ActivityResultLauncher<Uri> takePictureLauncher =
    registerForActivityResult(new ActivityResultContracts.TakePicture(), success -> {
        if (success) {
            imageView.setImageURI(photoUri);
        }
    });

public void takePhoto() {
    File photoFile = File.createTempFile("IMG_", ".jpg", getCacheDir());
    photoUri = FileProvider.getUriForFile(
        this, getPackageName() + ".fileprovider", photoFile
    );
    takePictureLauncher.launch(photoUri);
}
```
  </Tab>
</CodeTabs>

`ActivityResultContracts.TakePicture()` 是 AndroidX 提供的专用 Contract——它自动处理 `ACTION_IMAGE_CAPTURE` intent 的发出和结果解析，比手动写 intent 更简洁。

### 全分辨率 vs 缩略图

不传 `MediaStore.EXTRA_OUTPUT` 时，相机返回 intent data 里的是一个压缩过的缩略图 `Bitmap`（通常只有几百像素）。传了 `EXTRA_OUTPUT` 并指定 FileProvider URI，相机 App 会把**原始全分辨率照片**写入你指定的文件。

<CodeTabs>
  <Tab label="Kotlin">
```kotlin
// 方式 A：缩略图（不推荐用于展示大图）
private val captureLauncher = registerForActivityResult(
    ActivityResultContracts.StartActivityForResult()
) { result ->
    val thumbnail = result.data?.extras?.get("data") as? Bitmap
    binding.imageView.setImageBitmap(thumbnail)  // 模糊的小图
}

// 方式 B：全分辨率（推荐）
private val takePictureLauncher = registerForActivityResult(
    ActivityResultContracts.TakePicture()
) { success ->
    if (success) {
        // 用 BitmapFactory 加载并缩放显示
        val bitmap = BitmapFactory.decodeFile(photoFile.absolutePath)
        binding.imageView.setImageBitmap(bitmap)
    }
}
```
  </Tab>
  <Tab label="Java">
```java
// 方式 B：全分辨率（推荐）
private final ActivityResultLauncher<Uri> takePictureLauncher =
    registerForActivityResult(new ActivityResultContracts.TakePicture(), success -> {
        if (success) {
            Bitmap bitmap = BitmapFactory.decodeFile(photoFile.getAbsolutePath());
            imageView.setImageBitmap(bitmap);
        }
    });
```
  </Tab>
</CodeTabs>

大图直接 `decodeFile` 可能 OOM。生产环境应加采样缩放：

```kotlin
fun decodeSampledBitmap(path: String, reqWidth: Int, reqHeight: Int): Bitmap {
    return BitmapFactory.Options().run {
        inJustDecodeBounds = true
        BitmapFactory.decodeFile(path, this)
        inSampleSize = calculateInSampleSize(this, reqWidth, reqHeight)
        inJustDecodeBounds = false
        BitmapFactory.decodeFile(path, this)
    }!!
}
```

### Android 10+ 分区存储（Scoped Storage）

<Term def="Android 10（API 29）引入的存储隔离机制。App 默认只能访问自己的私有目录和公共媒体集合（通过 MediaStore），不能随意读写外部存储的任意路径。">Scoped Storage</Term> 进一步收紧了存储访问：App 不能随意读写 `/sdcard/` 下的任意路径。

对拍照的影响：
- **App 私有目录**（`cacheDir`、`getExternalFilesDir`）不受 Scoped Storage 限制——FileProvider 共享这些目录里的文件完全合规
- 如果想把照片存到**公共相册**（让用户在系统图库里也能看到），需要用 `MediaStore.Images.Media` API 写入，而不是直接写文件路径
- `WRITE_EXTERNAL_STORAGE` 权限在 Android 10+ 对 App 私有目录已不需要；Android 13+ 改用细粒度媒体权限 `READ_MEDIA_IMAGES`

<Callout type="tip">
  如果只是"拍照→在 App 内展示"，用 `cacheDir` + FileProvider 就够了，不需要任何存储权限。只有要把照片存到公共相册时才需要 MediaStore API。
</Callout>

### CameraX：现代替代方案

如果需要**自定义相机界面**（实时滤镜、扫码框、连续拍摄），隐式 intent 就不够用了。Google 推荐的现代方案是 **CameraX** 库——基于 Camera2 API 封装，自动处理设备兼容性、生命周期绑定、权限管理。但学习曲线比隐式 intent 陡得多，适合有定制需求的场景。

## 动手：完成一个"拍照并显示"功能

> 猜一猜：如果忘了配置 FileProvider，在 Android 7.0 以上的设备上点"拍照"会发生什么？Logcat 里会出现什么异常类名？

<Stepper>
  <Step title="① 配置 FileProvider">
    在 `AndroidManifest.xml` 的 `<application>` 内添加 `<provider>` 标签，`authorities` 设为 `"${applicationId}.fileprovider"`。
    创建 `res/xml/file_paths.xml`，声明 `<cache-path name="cache_photos" path="." />`。
    效果：Gradle Sync 后，你的 App 具备了安全共享 cache 目录文件的能力。
  </Step>
  <Step title="② 创建临时文件并生成 Content URI">
    点击拍照按钮时，调用 `File.createTempFile("IMG_", ".jpg", cacheDir)` 在 App 私有缓存目录创建空文件。
    再用 `FileProvider.getUriForFile(context, authority, photoFile)` 生成 `content://com.myapp.fileprovider/cache_photos/IMG_xxx.jpg` 形式的 URI。
    把这个 URI 保存在成员变量 `photoUri` 里——后面显示照片要用。
  </Step>
  <Step title="③ 发出拍照 Intent">
    用 `ActivityResultContracts.TakePicture()` 注册 launcher，调用 `takePictureLauncher.launch(photoUri)`。
    系统弹出相机 App 界面，用户按下快门。
    相机 App 收到 `content://` URI 和临时写权限，把全分辨率 JPEG 写入你指定的文件。
  </Step>
  <Step title="④ 显示全分辨率照片">
    回调里 `success == true` 表示拍照成功。用 `binding.imageView.setImageURI(photoUri)` 快速预览，或用 `BitmapFactory.decodeFile` + 采样缩放加载高清图。
    屏幕上 ImageView 从占位图变为用户刚拍的照片。
  </Step>
</Stepper>

## 代码逐段拆解

### 完整拍照 Activity 示例

下面是一个可运行的最小实现，覆盖了 FileProvider 配置、权限声明、拍照启动和结果显示。

**AndroidManifest.xml 权限声明：**

```xml
<!-- 只有直接拨出电话才需要；隐式 intent 调相机通常不需要 CAMERA 权限 -->
<!-- Android 13+ 读取相册才需要 -->
<uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
```

**Activity 完整代码：**

<CodeTabs>
  <Tab label="Kotlin">
```kotlin
class PhotoActivity : AppCompatActivity() {
    private lateinit var binding: ActivityPhotoBinding
    private lateinit var photoFile: File
    private lateinit var photoUri: Uri

    private val takePictureLauncher = registerForActivityResult(
        ActivityResultContracts.TakePicture()
    ) { success ->
        if (success) {
            binding.imageView.setImageURI(photoUri)
        } else {
            Toast.makeText(this, "拍照取消", Toast.LENGTH_SHORT).show()
        }
    }

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        binding = ActivityPhotoBinding.inflate(layoutInflater)
        setContentView(binding.root)
        binding.btnTakePhoto.setOnClickListener { takePhoto() }
    }

    private fun takePhoto() {
        photoFile = File.createTempFile("IMG_", ".jpg", cacheDir)
        photoUri = FileProvider.getUriForFile(
            this, "${packageName}.fileprovider", photoFile
        )
        takePictureLauncher.launch(photoUri)
    }
}
```
  </Tab>
  <Tab label="Java">
```java
public class PhotoActivity extends AppCompatActivity {
    private ActivityPhotoBinding binding;
    private File photoFile;
    private Uri photoUri;

    private final ActivityResultLauncher<Uri> takePictureLauncher =
        registerForActivityResult(new ActivityResultContracts.TakePicture(), success -> {
            if (success) {
                binding.imageView.setImageURI(photoUri);
            } else {
                Toast.makeText(this, "拍照取消", Toast.LENGTH_SHORT).show();
            }
        });

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        binding = ActivityPhotoBinding.inflate(getLayoutInflater());
        setContentView(binding.getRoot());
        binding.btnTakePhoto.setOnClickListener(v -> takePhoto());
    }

    private void takePhoto() {
        try {
            photoFile = File.createTempFile("IMG_", ".jpg", getCacheDir());
            photoUri = FileProvider.getUriForFile(
                this, getPackageName() + ".fileprovider", photoFile
            );
            takePictureLauncher.launch(photoUri);
        } catch (IOException e) {
            Toast.makeText(this, "无法创建临时文件", Toast.LENGTH_SHORT).show();
        }
    }
}
```
  </Tab>
</CodeTabs>

关键点：`photoUri` 必须是成员变量——回调触发时 intent 的 data 里通常没有照片（用了 `EXTRA_OUTPUT` 后相机直接写文件），你必须靠之前保存的 URI 引用来读取照片。

### 手动 Intent 写法（理解原理用）

`ActivityResultContracts.TakePicture()` 底层其实就是下面这段代码：

<CodeTabs>
  <Tab label="Kotlin">
```kotlin
val captureIntent = Intent(MediaStore.ACTION_IMAGE_CAPTURE).apply {
    putExtra(MediaStore.EXTRA_OUTPUT, photoUri)
    addFlags(Intent.FLAG_GRANT_WRITE_URI_PERMISSION)
}
startActivityForResult(captureIntent, REQUEST_CODE)  // 旧 API，已废弃
```
  </Tab>
  <Tab label="Java">
```java
Intent captureIntent = new Intent(MediaStore.ACTION_IMAGE_CAPTURE);
captureIntent.putExtra(MediaStore.EXTRA_OUTPUT, photoUri);
captureIntent.addFlags(Intent.FLAG_GRANT_WRITE_URI_PERMISSION);
startActivityForResult(captureIntent, REQUEST_CODE);  // 旧 API，已废弃
```
  </Tab>
</CodeTabs>

`FLAG_GRANT_WRITE_URI_PERMISSION` 告诉系统：把对这个 URI 的写权限临时授予相机 App。没有这个 flag，相机 App 无法写入你指定的文件。

## 容易踩的坑

<Callout type="trap">
  **现象**：Android 7.0 以上点"拍照"崩溃，Logcat 报 `FileUriExposedException`。**原因**：用了 `Uri.fromFile(photoFile)` 生成 `file://` URI 传给相机 App。Android 7.0+ 禁止跨 App 传递 file:// URI。**修法**：改用 `FileProvider.getUriForFile()` 生成 `content://` URI，并在 Manifest 中正确配置 FileProvider。
</Callout>

<Callout type="trap">
  **现象**：拍照成功（`success == true`）但 ImageView 显示空白或旧图。**原因**：回调里从 `result.data` 取照片——用了 `EXTRA_OUTPUT` 后 intent data 里没有 Bitmap，照片写在你指定的文件里。**修法**：用成员变量 `photoUri` 或 `photoFile` 读取，不要依赖 `result.data?.extras?.get("data")`。
</Callout>

<Callout type="trap">
  **现象**：FileProvider 配置后仍然崩溃 `IllegalArgumentException: Failed to find configured root`。**原因**：`file_paths.xml` 里的 path 类型和实际文件所在目录不匹配——比如文件在 `cacheDir` 但 xml 里只声明了 `external-files-path`。**修法**：确认 `File.createTempFile` 的目录和 `file_paths.xml` 中的 `<cache-path>` / `<external-files-path>` 一致。
</Callout>

<Callout type="trap">
  **现象**：加载全分辨率照片时 App 崩溃 OOM（OutOfMemoryError）。**原因**：现代手机照片动辄 4000×3000 像素，直接 `BitmapFactory.decodeFile` 会占用 ~48MB 内存。**修法**：用 `BitmapFactory.Options.inSampleSize` 先采样再解码，或限制 ImageView 的显示尺寸后再加载。
</Callout>

## 小结

- 拍照三步：创建临时文件 → FileProvider 生成 content:// URI → 隐式 intent 调起相机
- FileProvider 替代 file:// URI，Android 7.0+ 跨 App 传文件的强制要求
- `EXTRA_OUTPUT` 拿全分辨率照片；不传只拿压缩缩略图
- 用 `registerForActivityResult(TakePicture())` 替代废弃的 `onActivityResult`
- Android 10+ Scoped Storage：App 私有目录不受限；存公共相册需 MediaStore API

<Exercises>

**问题 1**：“第16章 Taking Pictures with Intents”覆盖哪些正式节点和项目主线？
<Answer>覆盖8个节点，从“16. Taking Pictures with Intents”到“Challenge: Efficient Thumbnail Load”。主线是通过FileProvider授予最小URI权限，控制照片位置、相机Intent、位图缩放与设备能力，交付物是照片文件合同、相机降级、缩略图内存测试和配置声明。</Answer>

**问题 2**：怎样建立本页最小可执行实验？
<Answer>固定JDK、SDK、Gradle、设备API、语言、权限和输入，完成一个从用户动作到可观察结果的垂直切片；保存干净构建、设备操作、原始日志、状态快照与断言。</Answer>

**问题 3**：为什么只在正常点击路径运行不能证明完成？
<Answer>它没有覆盖组件重建、进程死亡、异步取消、权限拒绝、外部输入失败、资源释放和版本变化。应逐项注入故障，并用照片文件合同、相机降级、缩略图内存测试和配置声明核对。</Answer>

**问题 4**：怎样设计能推翻当前实现的反例？
<Answer>保持其他变量不变，分别注入旋转、后台回收、进程死亡、拒权、断网、无效输入与重复操作；任何状态丢失、越权、崩溃、泄漏或重复副作用都推翻完成结论。</Answer>

**问题 5**：从第4版语境迁移到现代目标SDK时如何控制变量？
<Answer>先按原书还原机制并锁定基线，再查询目标SDK合同；一次只迁移组件导出、存储、权限、后台、通知或依赖之一，保存构建、行为、测试和回滚差异。</Answer>

**问题 6**：本页达到独立交接标准需要什么？
<Answer>需要版本与设备指纹、源码提交、输入、构建命令、生命周期轨迹、原始日志、失败测试、安全边界与发布产物，让他人凭照片文件合同、相机降级、缩略图内存测试和配置声明重放。</Answer>

</Exercises>

<Glossary>

<GlossaryItem term="FileProvider">
  ContentProvider 的特殊子类，用于在 App 之间安全共享文件。生成受权限保护的 content:// URI 替代 file:// URI，可设置临时读写权限。Android 7.0+ 跨 App 传文件的标准方式。详见本章"FileProvider"一节。
</GlossaryItem>

<GlossaryItem term="MediaStore.ACTION_IMAGE_CAPTURE">
  Android 系统定义的标准拍照动作常量。发出带此 action 的隐式 intent，系统调起已安装的相机 App 完成拍照。配合 EXTRA_OUTPUT 可获取全分辨率照片。详见本章"调用系统相机"一节。
</GlossaryItem>

<GlossaryItem term="Scoped Storage">
  Android 10（API 29）引入的存储隔离机制。App 默认只能访问自己的私有目录和公共媒体集合（通过 MediaStore），不能随意读写外部存储的任意路径。详见本章"Android 10+ 分区存储"一节。
</GlossaryItem>

</Glossary>

<Attribution />


{/* BNR4_AUTHORITY_SUPPLEMENT */}

## 从生命周期、状态与证据开始

第4版不是API词典，而是六个逐步扩展的应用实验。每一章都要回答：用户从哪里进入，哪个Android组件拥有工作，回调在哪个线程发生，哪些事实必须跨旋转或进程死亡恢复，外部系统怎样失败，资源在何时释放。<Term def="负责创建、取消并最终释放组件、任务或资源的明确作用域">生命周期所有者</Term>把Activity、Fragment视图、ViewModel、Worker、线程与资源的寿命连接起来；没有所有者的异步任务就是潜在泄漏。

界面字段不是天然事实。<Term def="在配置变化或进程重建后，可从保存事实重新构造的最小用户状态">可恢复状态</Term>应根据数据大小与寿命放入saved instance state、ViewModel、数据库、文件或调度器，而不是全部塞进单例。Intent、文件、数据库、网络、系统服务和其他应用形成<Term def="跨边界交换数据时必须验证的action、类型、权限、大小、时序和失败约束">外部合同</Term>；正常响应只是合同中的一个样本。

本书以2019年第四版、Android 5.0至8.1及其后续兼容实践为语境。<Term def="Android版本、targetSdk、设备形态、权限与依赖共同限定的行为适用范围">版本边界</Term>要求先还原书中机制，再查询现代目标SDK的权限、后台、存储、通知和组件导出合同。<Term def="由环境指纹、输入、操作、日志、状态快照和断言组成的可重放结论">证据链</Term>则保证“在我的模拟器能跑”不会冒充可交付结论。

## 第4版正式目录逐节点映射

### 16. Taking Pictures with Intents

**正式节点 1/8。** 先说明“16. Taking Pictures with Intents”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“通过FileProvider授予最小URI权限，控制照片位置、相机Intent、位图缩放与设备能力”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### A Place for Your Photo

**正式节点 2/8。** 先说明“A Place for Your Photo”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“通过FileProvider授予最小URI权限，控制照片位置、相机Intent、位图缩放与设备能力”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### File Storage

**正式节点 3/8。** 先说明“File Storage”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“通过FileProvider授予最小URI权限，控制照片位置、相机Intent、位图缩放与设备能力”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Using a Camera Intent

**正式节点 4/8。** 先说明“Using a Camera Intent”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“通过FileProvider授予最小URI权限，控制照片位置、相机Intent、位图缩放与设备能力”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Scaling and Displaying Bitmaps

**正式节点 5/8。** 先说明“Scaling and Displaying Bitmaps”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“通过FileProvider授予最小URI权限，控制照片位置、相机Intent、位图缩放与设备能力”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Declaring Features

**正式节点 6/8。** 先说明“Declaring Features”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“通过FileProvider授予最小URI权限，控制照片位置、相机Intent、位图缩放与设备能力”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Challenge: Detail Display

**正式节点 7/8。** 先说明“Challenge: Detail Display”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“通过FileProvider授予最小URI权限，控制照片位置、相机Intent、位图缩放与设备能力”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Challenge: Efficient Thumbnail Load

**正式节点 8/8。** 先说明“Challenge: Efficient Thumbnail Load”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“通过FileProvider授予最小URI权限，控制照片位置、相机Intent、位图缩放与设备能力”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

## 最小可执行切片

先把状态转换与Android副作用分开。下面的Kotlin骨架不替代本章代码，而是让旋转、进程恢复与失败输入可单测：

```kotlin
sealed interface ScreenState {
    data object Loading : ScreenState
    data class Ready(val key: String) : ScreenState
    data class Failed(val reason: String, val retryable: Boolean) : ScreenState
}

fun reduce(current: ScreenState, event: UiEvent): ScreenState = nextState(current, event)
```

固定JDK、SDK、Gradle、设备API、语言和输入，清理缓存后从命令行保存制品与日志：

```bash
./gradlew --no-daemon clean test assembleDebug
adb install -r app/build/outputs/apk/debug/app-debug.apk
adb shell am force-stop com.example.app
adb shell monkey -p com.example.app 1
adb logcat -d -v threadtime > run.log
```

先写行为断言，再执行用户操作和故障注入：

```gherkin
Given a pinned build, device API, locale, permission state, and input
When the owner is recreated after state saving or process death
Then durable facts reconstruct the same user-visible state
And cancelled work cannot update a destroyed owner
And denied permission, offline input, and malformed data remain recoverable
```

三段代码分别约束状态模型、环境重放和行为结果。真实项目还要加入静态检查、单元测试、仪器测试、无障碍检查和发布构建；日志只能支持断言，不能代替断言。

## 线程、状态与外部资源

主线程负责输入、生命周期分发和大部分UI工作，磁盘、网络、图片解码与长计算不能阻塞帧。后台结果返回前必须确认所有者仍有效；Fragment的生命周期与其View生命周期不是一回事。每个实现都应列出入口、线程、事实源、取消点、关闭责任和用户可见失败六项。

跨应用Intent、ContentProvider、WebView、网络响应、文件URI和深链都视为不可信输入。校验action、scheme、MIME、大小、授权与响应者；导出组件使用最小权限；日志、截图和测试数据库不得泄露令牌或个人数据。相机、音频、数据库、线程和回调注册必须在成功、异常与取消路径都释放。

性能以用户任务衡量：启动看可交互时刻，列表看掉帧与绑定分配，网络看超时和取消，后台工作看约束、电量与重复调度。每次比较固定设备温度、构建类型、数据规模和网络条件，先采基线再改变一个变量。

迁移现代目标SDK时，先在第4版语境中证明机制，再逐项处理组件导出、存储、通知、后台启动、权限、废弃API和依赖版本。每一步保存编译差异、行为差异、测试结果和回滚点；新API并不自动正确，旧API能编译也不代表仍符合平台政策。

## 三步验证实验

<Steps>
  <Step title="1. 建立目录节点与生命周期图">
    从“通过FileProvider授予最小URI权限，控制照片位置、相机Intent、位图缩放与设备能力”选一个用户任务，标出入口、组件、所有者、回调线程、可恢复状态和销毁点。先预测旋转、后台和进程恢复后哪些对象消失、哪些事实必须重建。
    <BnrLifecycleLab />
  </Step>
  <Step title="2. 注入生命周期与外部故障">
    依次改变生命周期阶段、网络和权限，每次只改变一个条件；记录原始日志、界面状态、持久事实与资源计数，区分预期失败和未知状态。
    <BnrFailureLab />
  </Step>
  <Step title="3. 通过交付证据门">
    用照片文件合同、相机降级、缩略图内存测试和配置声明核对版本设备、生命周期、持久状态、失败安全、测试和发布。缺少任何一项时，不得把正常点击路径视为完成。
    <BnrEvidenceLab />
  </Step>
</Steps>

<Callout type="trap">**核心陷阱。** 只复述类名与回调，未说明所有者、状态来源、线程、取消和失败后果。修复时回到用户任务和状态机，注入旋转、进程死亡、拒权、断网与无效输入。</Callout>

<Callout type="trap">**历史示例不等于当前平台政策。** 第4版准确反映出版时工具链；现代targetSdk可能改变组件导出、后台、存储、通知和权限。保留原书机制，再建立有证据的迁移层。</Callout>

<Callout type="warning">**真机与数据安全。** 使用测试账号、样本媒体和最小权限；清理日志、数据库与截图。签名密钥、API密钥、联系人和真实位置不得写入仓库或公开证据。</Callout>

## 本章回顾

本页从“16. Taking Pictures with Intents”覆盖到“Challenge: Efficient Thumbnail Load”，共8个正式目录节点。掌握标准不是记住API，而是能沿“通过FileProvider授予最小URI权限，控制照片位置、相机Intent、位图缩放与设备能力”解释组件与数据边界，运行正常、重建和失败实验，并让另一位开发者仅凭照片文件合同、相机降级、缩略图内存测试和配置声明重放结论。

<Exercises>

**问题 1**：“第16章 Taking Pictures with Intents”覆盖哪些正式节点和项目主线？
<Answer>覆盖8个节点，从“16. Taking Pictures with Intents”到“Challenge: Efficient Thumbnail Load”。主线是通过FileProvider授予最小URI权限，控制照片位置、相机Intent、位图缩放与设备能力，交付物是照片文件合同、相机降级、缩略图内存测试和配置声明。</Answer>

**问题 2**：怎样建立本页最小可执行实验？
<Answer>固定JDK、SDK、Gradle、设备API、语言、权限和输入，完成一个从用户动作到可观察结果的垂直切片；保存干净构建、设备操作、原始日志、状态快照与断言。</Answer>

**问题 3**：为什么只在正常点击路径运行不能证明完成？
<Answer>它没有覆盖组件重建、进程死亡、异步取消、权限拒绝、外部输入失败、资源释放和版本变化。应逐项注入故障，并用照片文件合同、相机降级、缩略图内存测试和配置声明核对。</Answer>

**问题 4**：怎样设计能推翻当前实现的反例？
<Answer>保持其他变量不变，分别注入旋转、后台回收、进程死亡、拒权、断网、无效输入与重复操作；任何状态丢失、越权、崩溃、泄漏或重复副作用都推翻完成结论。</Answer>

**问题 5**：从第4版语境迁移到现代目标SDK时如何控制变量？
<Answer>先按原书还原机制并锁定基线，再查询目标SDK合同；一次只迁移组件导出、存储、权限、后台、通知或依赖之一，保存构建、行为、测试和回滚差异。</Answer>

**问题 6**：本页达到独立交接标准需要什么？
<Answer>需要版本与设备指纹、源码提交、输入、构建命令、生命周期轨迹、原始日志、失败测试、安全边界与发布产物，让他人凭照片文件合同、相机降级、缩略图内存测试和配置声明重放。</Answer>

</Exercises>

<Glossary>
  <GlossaryItem term="生命周期所有者">负责创建、取消并最终释放组件、任务或资源的明确作用域。</GlossaryItem>
  <GlossaryItem term="可恢复状态">在配置变化或进程重建后，可从保存事实重新构造的最小用户状态。</GlossaryItem>
  <GlossaryItem term="外部合同">Intent、文件、数据库、网络、系统服务或其他应用与本组件交换数据时的可验证约束。</GlossaryItem>
  <GlossaryItem term="版本边界">Android版本、targetSdk、设备形态、权限与依赖共同限定的行为适用范围。</GlossaryItem>
  <GlossaryItem term="证据链">由环境指纹、输入、操作、日志、状态快照和断言组成的可重放结论。</GlossaryItem>
</Glossary>

[← 上一页：第15章 Implicit Intents](/learn/big-nerd-ranch-guide/intent-data/implicit-intents) · [下一页：第17章 Localization →](/learn/big-nerd-ranch-guide/intent-data/localization)

<Attribution adaptedFrom="Android Programming: The Big Nerd Ranch Guide, 4th Edition" adaptedUrl="https://www.oreilly.com/library/view/android-programming-the/9780135257555/" />
