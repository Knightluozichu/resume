---
title: XML drawable
type: C
section: Intent与数据
order: 22
description: 用 XML 定义 shape、gradient、selector、layer-list 等 drawable——比 PNG 更省空间、自适应屏幕密度。
demo: true
math: false
sourceUrl: "https://www.oreilly.com/library/view/android-programming-the/9780135257555/"
draft: false
---

import { BnrLifecycleLab, BnrFailureLab, BnrEvidenceLab } from "@/components/mdx/big-nerd-ranch-guide/diagrams/xml-drawables";

{/* 知识点清单：
   1) shape/gradient/corners = XML
   2) selector 状态顺序 = Callout
   3) layer-list 叠层 = 概念
   4) vector 矢量 = Studio 导入
   5) 9-patch vs shape = Callout
*/}

<Objectives>

- 能解释“第22章 XML Drawables”全部正式节点的组件所有者、生命周期、线程、状态来源与Android版本边界。
- 能实现“组合shape、selector和layer-list表达状态化视觉，并比较mipmap与9-patch的用途”的最小垂直切片，并保存构建、设备操作、原始日志和断言。
- 能比较正常创建、旋转、后台、进程重建、权限拒绝与外部故障下的行为差异。
- 能设计推翻当前实现的反例，并凭统一按钮、状态矩阵、图层顺序、密度与拉伸测试完成独立交接。

</Objectives>

## 你的 App 塞满了 PNG 图片

按钮背景是一张 9-patch，圆角卡片是一张 PNG，选中的高亮又是一张图——你的 APK 越来越胖。XML drawable 在运行时由系统渲染，**一个 2KB 的 XML 文件能替代所有屏幕密度下的 PNG（xxhdpi/xxxhdpi 各一份，加起来几十 KB）**。

## Shape Drawable

```xml
<!-- res/drawable/rounded_background.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="#FFFFFF" />
    <corners android:radius="12dp" />
    <stroke android:width="1dp" android:color="#E0E0E0" />
</shape>
```

支持的 shape：`rectangle`、`oval`、`line`、`ring`。`gradient` 子元素可实现线性/径向/扫描渐变：

```xml
<gradient
    android:startColor="#FF6B6B"
    android:endColor="#FF8E53"
    android:angle="45" />
```

## Selector（State-List）

按钮按下变暗、禁用变灰——这些状态切换用 selector 一行代码搞定：

```xml
<!-- res/drawable/button_bg.xml -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 按下状态 -->
    <item android:state_pressed="true">
        <shape>
            <solid android:color="#1565C0" />
            <corners android:radius="8dp" />
        </shape>
    </item>
    <!-- 禁用状态 -->
    <item android:state_enabled="false">
        <shape>
            <solid android:color="#BDBDBD" />
            <corners android:radius="8dp" />
        </shape>
    </item>
    <!-- 默认 -->
    <item>
        <shape>
            <solid android:color="#2196F3" />
            <corners android:radius="8dp" />
        </shape>
    </item>
</selector>
```

Selector 按从上到下顺序匹配——第一个匹配的 `<item>` 生效。支持的状态：`state_pressed`、`state_focused`、`state_enabled`、`state_selected`、`state_checked` 等。

## Layer-List

把多个 drawable 叠起来：

```xml
<layer-list xmlns:android="...">
    <item>
        <shape><solid android:color="#FFF"/></shape>
    </item>
    <item android:top="2dp" android:bottom="2dp">
        <shape><solid android:color="#E0E0E0"/></shape>
    </item>
</layer-list>
```

{/* 四种常用 XML drawable 类型总览：shape / layer-list / selector / ripple */}

<XmlDrawableTypesDiagram />

## Vector Drawable

用 SVG 路径描述图标——任意缩放不变形、一个文件通吃所有屏幕：

```xml
<vector android:width="24dp" android:height="24dp"
    android:viewportWidth="24" android:viewportHeight="24">
    <path
        android:fillColor="#000"
        android:pathData="M12,2C6.48,2 2,6.48 2,12s4.48,10 10,10..." />
</vector>
```

Android Studio 内置 Vector Asset 工具——直接从 Material Icons 导入矢量图标。

<Stepper>
  <Step title="① 创建 shape drawable">
    在 `res/drawable/` 下新建 XML 文件，根元素 `<shape>` 设置 `android:shape="rectangle"`（或 `oval`/`line`/`ring`）。系统在运行时将 XML 解析为位图，自动适配当前屏幕密度。
  </Step>
  <Step title="② 加 gradient/corners/stroke">
    `<gradient>` 定义渐变（线性/径向/扫描），设 `startColor`、`endColor`、`angle`；`<corners>` 设 `android:radius` 控制圆角；`<stroke>` 设 `android:width` 和 `android:color` 添加描边。
  </Step>
  <Step title="③ 创建 selector 切换状态">
    `<selector>` 根元素，内含多个 `<item>`。按状态优先级从高到低排列：`state_pressed="true"` → `state_enabled="false"` → 默认（无条件）。系统按从上到下顺序匹配，第一个命中的 item 生效——**默认 item 必须放最后**。
  </Step>
  <Step title="④ 替换布局中的 @drawable 引用">
    把布局中 `android:background="@color/xxx"` 或 PNG 图片引用替换为 `android:background="@drawable/button_bg"`。XML drawable 一个文件通吃所有密度，比多套 PNG（xxhdpi/xxxhdpi）节省数十 KB APK 体积。
  </Step>
</Stepper>

## 容易踩的坑

<Callout type="trap">
  **现象**：shape 设了 corners radius，但 View 上看到的是直角。**原因**：View 本身有背景色覆盖了 drawable。**修法**：确保 `android:background="@drawable/rounded_bg"` 后的代码没有重新设 background 或 backgroundTint。
</Callout>

<Callout type="trap">
  **现象**：selector 里的状态切换不起作用——按下跟默认一样。**原因**：selector 中 `<item>` 的顺序错误——第一个匹配的 item 生效。如果默认 item 放在最前面，后面的状态永远不会被匹配。**修法**：默认 item 放最后。
</Callout>

<Callout type="trap">
  **现象**：vector 在 API 21 以下崩溃。**原因**：旧系统需 AppCompat 矢量兼容或 VectorDrawableCompat。**修法**：Gradle 开启 vectorDrawables.useSupportLibrary 或提供 png fallback。
</Callout>

## 小结

- XML drawable 替代 PNG → 省空间、自适应密度、运行时渲染
- shape 画基本图形；gradient 画渐变；corners 设圆角；stroke 加描边
- selector 根据 View 状态（按下/禁用/选中）自动切换 drawable
- layer-list 叠加多层 drawable；vector 用路径描述图标 → 无限缩放
- selector 顺序：默认 item 放最后，状态 item 放前面

<Exercises>

**问题 1**：“第22章 XML Drawables”覆盖哪些正式节点和项目主线？
<Answer>覆盖9个节点，从“22. XML Drawables”到“Challenge: Button Themes”。主线是组合shape、selector和layer-list表达状态化视觉，并比较mipmap与9-patch的用途，交付物是统一按钮、状态矩阵、图层顺序、密度与拉伸测试。</Answer>

**问题 2**：怎样建立本页最小可执行实验？
<Answer>固定JDK、SDK、Gradle、设备API、语言、权限和输入，完成一个从用户动作到可观察结果的垂直切片；保存干净构建、设备操作、原始日志、状态快照与断言。</Answer>

**问题 3**：为什么只在正常点击路径运行不能证明完成？
<Answer>它没有覆盖组件重建、进程死亡、异步取消、权限拒绝、外部输入失败、资源释放和版本变化。应逐项注入故障，并用统一按钮、状态矩阵、图层顺序、密度与拉伸测试核对。</Answer>

**问题 4**：怎样设计能推翻当前实现的反例？
<Answer>保持其他变量不变，分别注入旋转、后台回收、进程死亡、拒权、断网、无效输入与重复操作；任何状态丢失、越权、崩溃、泄漏或重复副作用都推翻完成结论。</Answer>

**问题 5**：从第4版语境迁移到现代目标SDK时如何控制变量？
<Answer>先按原书还原机制并锁定基线，再查询目标SDK合同；一次只迁移组件导出、存储、权限、后台、通知或依赖之一，保存构建、行为、测试和回滚差异。</Answer>

**问题 6**：本页达到独立交接标准需要什么？
<Answer>需要版本与设备指纹、源码提交、输入、构建命令、生命周期轨迹、原始日志、失败测试、安全边界与发布产物，让他人凭统一按钮、状态矩阵、图层顺序、密度与拉伸测试重放。</Answer>

</Exercises>

<Glossary>

<GlossaryItem term="Shape Drawable">
  用 XML 定义的几何图形（矩形/椭圆/线/环）。支持纯色(solid)、圆角(corners)、描边(stroke)、渐变(gradient)等属性。替代 PNG 背景图。
</GlossaryItem>

<GlossaryItem term="Selector（State-List）">
  根据 View 状态（按下/禁用/选中/聚焦）自动切换 drawable 的 XML 定义。创建按钮、列表项等交互反馈的标准方式。
</GlossaryItem>

<GlossaryItem term="Vector Drawable">
  用 XML 路径（pathData）描述的矢量图。与分辨率无关——任意缩放不失真，一个文件适用所有屏幕密度。
</GlossaryItem>

</Glossary>

<Attribution />


{/* BNR4_AUTHORITY_SUPPLEMENT */}

## 从生命周期、状态与证据开始

第4版不是API词典，而是六个逐步扩展的应用实验。每一章都要回答：用户从哪里进入，哪个Android组件拥有工作，回调在哪个线程发生，哪些事实必须跨旋转或进程死亡恢复，外部系统怎样失败，资源在何时释放。<Term def="负责创建、取消并最终释放组件、任务或资源的明确作用域">生命周期所有者</Term>把Activity、Fragment视图、ViewModel、Worker、线程与资源的寿命连接起来；没有所有者的异步任务就是潜在泄漏。

界面字段不是天然事实。<Term def="在配置变化或进程重建后，可从保存事实重新构造的最小用户状态">可恢复状态</Term>应根据数据大小与寿命放入saved instance state、ViewModel、数据库、文件或调度器，而不是全部塞进单例。Intent、文件、数据库、网络、系统服务和其他应用形成<Term def="跨边界交换数据时必须验证的action、类型、权限、大小、时序和失败约束">外部合同</Term>；正常响应只是合同中的一个样本。

本书以2019年第四版、Android 5.0至8.1及其后续兼容实践为语境。<Term def="Android版本、targetSdk、设备形态、权限与依赖共同限定的行为适用范围">版本边界</Term>要求先还原书中机制，再查询现代目标SDK的权限、后台、存储、通知和组件导出合同。<Term def="由环境指纹、输入、操作、日志、状态快照和断言组成的可重放结论">证据链</Term>则保证“在我的模拟器能跑”不会冒充可交付结论。

## 第4版正式目录逐节点映射

### 22. XML Drawables

**正式节点 1/9。** 先说明“22. XML Drawables”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“组合shape、selector和layer-list表达状态化视觉，并比较mipmap与9-patch的用途”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Making Uniform Buttons

**正式节点 2/9。** 先说明“Making Uniform Buttons”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“组合shape、selector和layer-list表达状态化视觉，并比较mipmap与9-patch的用途”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Shape Drawables

**正式节点 3/9。** 先说明“Shape Drawables”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“组合shape、selector和layer-list表达状态化视觉，并比较mipmap与9-patch的用途”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### State List Drawables

**正式节点 4/9。** 先说明“State List Drawables”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“组合shape、selector和layer-list表达状态化视觉，并比较mipmap与9-patch的用途”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Layer List Drawables

**正式节点 5/9。** 先说明“Layer List Drawables”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“组合shape、selector和layer-list表达状态化视觉，并比较mipmap与9-patch的用途”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### For the More Curious: Why Bother with XML Drawables?

**正式节点 6/9。** 先说明“For the More Curious: Why Bother with XML Drawables?”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“组合shape、selector和layer-list表达状态化视觉，并比较mipmap与9-patch的用途”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### For the More Curious: Mipmap Images

**正式节点 7/9。** 先说明“For the More Curious: Mipmap Images”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“组合shape、selector和layer-list表达状态化视觉，并比较mipmap与9-patch的用途”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### For the More Curious: 9-Patch Images

**正式节点 8/9。** 先说明“For the More Curious: 9-Patch Images”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“组合shape、selector和layer-list表达状态化视觉，并比较mipmap与9-patch的用途”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Challenge: Button Themes

**正式节点 9/9。** 先说明“Challenge: Button Themes”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“组合shape、selector和layer-list表达状态化视觉，并比较mipmap与9-patch的用途”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

## 最小可执行切片

先把状态转换与Android副作用分开。下面的Kotlin骨架不替代本章代码，而是让旋转、进程恢复与失败输入可单测：

```kotlin
sealed interface ScreenState {
    data object Loading : ScreenState
    data class Ready(val key: String) : ScreenState
    data class Failed(val reason: String, val retryable: Boolean) : ScreenState
}

fun reduce(current: ScreenState, event: UiEvent): ScreenState = nextState(current, event)
```

固定JDK、SDK、Gradle、设备API、语言和输入，清理缓存后从命令行保存制品与日志：

```bash
./gradlew --no-daemon clean test assembleDebug
adb install -r app/build/outputs/apk/debug/app-debug.apk
adb shell am force-stop com.example.app
adb shell monkey -p com.example.app 1
adb logcat -d -v threadtime > run.log
```

先写行为断言，再执行用户操作和故障注入：

```gherkin
Given a pinned build, device API, locale, permission state, and input
When the owner is recreated after state saving or process death
Then durable facts reconstruct the same user-visible state
And cancelled work cannot update a destroyed owner
And denied permission, offline input, and malformed data remain recoverable
```

三段代码分别约束状态模型、环境重放和行为结果。真实项目还要加入静态检查、单元测试、仪器测试、无障碍检查和发布构建；日志只能支持断言，不能代替断言。

## 线程、状态与外部资源

主线程负责输入、生命周期分发和大部分UI工作，磁盘、网络、图片解码与长计算不能阻塞帧。后台结果返回前必须确认所有者仍有效；Fragment的生命周期与其View生命周期不是一回事。每个实现都应列出入口、线程、事实源、取消点、关闭责任和用户可见失败六项。

跨应用Intent、ContentProvider、WebView、网络响应、文件URI和深链都视为不可信输入。校验action、scheme、MIME、大小、授权与响应者；导出组件使用最小权限；日志、截图和测试数据库不得泄露令牌或个人数据。相机、音频、数据库、线程和回调注册必须在成功、异常与取消路径都释放。

性能以用户任务衡量：启动看可交互时刻，列表看掉帧与绑定分配，网络看超时和取消，后台工作看约束、电量与重复调度。每次比较固定设备温度、构建类型、数据规模和网络条件，先采基线再改变一个变量。

迁移现代目标SDK时，先在第4版语境中证明机制，再逐项处理组件导出、存储、通知、后台启动、权限、废弃API和依赖版本。每一步保存编译差异、行为差异、测试结果和回滚点；新API并不自动正确，旧API能编译也不代表仍符合平台政策。

## 三步验证实验

<Steps>
  <Step title="1. 建立目录节点与生命周期图">
    从“组合shape、selector和layer-list表达状态化视觉，并比较mipmap与9-patch的用途”选一个用户任务，标出入口、组件、所有者、回调线程、可恢复状态和销毁点。先预测旋转、后台和进程恢复后哪些对象消失、哪些事实必须重建。
    <BnrLifecycleLab />
  </Step>
  <Step title="2. 注入生命周期与外部故障">
    依次改变生命周期阶段、网络和权限，每次只改变一个条件；记录原始日志、界面状态、持久事实与资源计数，区分预期失败和未知状态。
    <BnrFailureLab />
  </Step>
  <Step title="3. 通过交付证据门">
    用统一按钮、状态矩阵、图层顺序、密度与拉伸测试核对版本设备、生命周期、持久状态、失败安全、测试和发布。缺少任何一项时，不得把正常点击路径视为完成。
    <BnrEvidenceLab />
  </Step>
</Steps>

<Callout type="trap">**核心陷阱。** 只复述类名与回调，未说明所有者、状态来源、线程、取消和失败后果。修复时回到用户任务和状态机，注入旋转、进程死亡、拒权、断网与无效输入。</Callout>

<Callout type="trap">**历史示例不等于当前平台政策。** 第4版准确反映出版时工具链；现代targetSdk可能改变组件导出、后台、存储、通知和权限。保留原书机制，再建立有证据的迁移层。</Callout>

<Callout type="warning">**真机与数据安全。** 使用测试账号、样本媒体和最小权限；清理日志、数据库与截图。签名密钥、API密钥、联系人和真实位置不得写入仓库或公开证据。</Callout>

## 本章回顾

本页从“22. XML Drawables”覆盖到“Challenge: Button Themes”，共9个正式目录节点。掌握标准不是记住API，而是能沿“组合shape、selector和layer-list表达状态化视觉，并比较mipmap与9-patch的用途”解释组件与数据边界，运行正常、重建和失败实验，并让另一位开发者仅凭统一按钮、状态矩阵、图层顺序、密度与拉伸测试重放结论。

<Exercises>

**问题 1**：“第22章 XML Drawables”覆盖哪些正式节点和项目主线？
<Answer>覆盖9个节点，从“22. XML Drawables”到“Challenge: Button Themes”。主线是组合shape、selector和layer-list表达状态化视觉，并比较mipmap与9-patch的用途，交付物是统一按钮、状态矩阵、图层顺序、密度与拉伸测试。</Answer>

**问题 2**：怎样建立本页最小可执行实验？
<Answer>固定JDK、SDK、Gradle、设备API、语言、权限和输入，完成一个从用户动作到可观察结果的垂直切片；保存干净构建、设备操作、原始日志、状态快照与断言。</Answer>

**问题 3**：为什么只在正常点击路径运行不能证明完成？
<Answer>它没有覆盖组件重建、进程死亡、异步取消、权限拒绝、外部输入失败、资源释放和版本变化。应逐项注入故障，并用统一按钮、状态矩阵、图层顺序、密度与拉伸测试核对。</Answer>

**问题 4**：怎样设计能推翻当前实现的反例？
<Answer>保持其他变量不变，分别注入旋转、后台回收、进程死亡、拒权、断网、无效输入与重复操作；任何状态丢失、越权、崩溃、泄漏或重复副作用都推翻完成结论。</Answer>

**问题 5**：从第4版语境迁移到现代目标SDK时如何控制变量？
<Answer>先按原书还原机制并锁定基线，再查询目标SDK合同；一次只迁移组件导出、存储、权限、后台、通知或依赖之一，保存构建、行为、测试和回滚差异。</Answer>

**问题 6**：本页达到独立交接标准需要什么？
<Answer>需要版本与设备指纹、源码提交、输入、构建命令、生命周期轨迹、原始日志、失败测试、安全边界与发布产物，让他人凭统一按钮、状态矩阵、图层顺序、密度与拉伸测试重放。</Answer>

</Exercises>

<Glossary>
  <GlossaryItem term="生命周期所有者">负责创建、取消并最终释放组件、任务或资源的明确作用域。</GlossaryItem>
  <GlossaryItem term="可恢复状态">在配置变化或进程重建后，可从保存事实重新构造的最小用户状态。</GlossaryItem>
  <GlossaryItem term="外部合同">Intent、文件、数据库、网络、系统服务或其他应用与本组件交换数据时的可验证约束。</GlossaryItem>
  <GlossaryItem term="版本边界">Android版本、targetSdk、设备形态、权限与依赖共同限定的行为适用范围。</GlossaryItem>
  <GlossaryItem term="证据链">由环境指纹、输入、操作、日志、状态快照和断言组成的可重放结论。</GlossaryItem>
</Glossary>

[← 上一页：第21章 Styles and Themes](/learn/big-nerd-ranch-guide/intent-data/styles-themes) · [下一页：第23章 More About Intents and Tasks →](/learn/big-nerd-ranch-guide/background-network/more-intents-tasks)

<Attribution adaptedFrom="Android Programming: The Big Nerd Ranch Guide, 4th Edition" adaptedUrl="https://www.oreilly.com/library/view/android-programming-the/9780135257555/" />
