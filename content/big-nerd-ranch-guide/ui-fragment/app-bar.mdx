---
title: AppBar与菜单
type: C
section: UI与Fragment
order: 14
description: 掌握 Toolbar 替代 ActionBar——菜单资源、Options Menu、Overflow 和 Navigation Icon 的配置。
demo: true
math: false
sourceUrl: "https://www.oreilly.com/library/view/android-programming-the/9780135257555/"
draft: true
qualityVersion: 2
practiceMode: code
sourceMode: independent-rewrite
---

import { BnrLifecycleLab, BnrFailureLab, BnrEvidenceLab } from "@/components/mdx/big-nerd-ranch-guide/diagrams/app-bar";

{/* 知识点清单：
   1) Toolbar 替代 ActionBar = 布局 + setSupportActionBar
   2) menu XML + onCreateOptionsMenu = 代码对照
   3) onOptionsItemSelected = 点击处理
   4) Navigation Icon 与 Up = Stepper
   5) Material3 TopAppBar = Callout
*/}

<Objectives>

- 能沿“AppBar与菜单”的用户事件解释Android组件、状态所有者、线程与销毁边界。
- 能围绕“掌握 Toolbar 替代 ActionBar——菜单资源、Options Menu、Overflow 和 Navigation Icon 的配置。”改出一个可运行结果，并用前后状态而非组件数量验收。
- 能在旋转、进程重建、拒权、离线或无效输入中选择适用反例，定位首个状态分叉。
- 能用双视口、大字体、焦点顺序、状态语义和对比度截图独立重放结论，并标明第四版机制与现代targetSdk政策的边界。

</Objectives>

## 顶栏不只是标题——它是命令中枢

<Term def="Material Design 顶栏，可显示标题、导航图标和操作菜单，现代 App 多用 Toolbar 实现。">App Bar</Term>（Toolbar）承载标题、返回箭头和「更多」菜单。Fragment 时代默认 <Term def="旧版框架提供的顶栏，现推荐用 Toolbar + setSupportActionBar 替代。">ActionBar</Term> 不够灵活——把 Toolbar 放进布局，才能和 CoordinatorLayout、Drawer 配合。

## 启用 Toolbar

`res/layout/activity_main.xml`：

```xml
<com.google.android.material.appbar.MaterialToolbar
    android:id="@+id/toolbar"
    android:layout_width="match_parent"
    android:layout_height="?attr/actionBarSize"
    app:title="@string/app_name" />
```

```kotlin
setSupportActionBar(findViewById(R.id.toolbar))
supportActionBar?.setDisplayHomeAsUpEnabled(true)
```

主题使用 `NoActionBar` 变体，避免双顶栏。

## Options Menu

`res/menu/crime_list_menu.xml`：

```xml
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <item
        android:id="@+id/menu_add"
        android:icon="@drawable/ic_add"
        android:title="@string/add_crime"
        app:showAsAction="ifRoom" />
    <item
        android:id="@+id/menu_settings"
        android:title="@string/settings"
        app:showAsAction="never" />
</menu>
```

```kotlin
override fun onCreateOptionsMenu(menu: Menu): Boolean {
    menuInflater.inflate(R.menu.crime_list_menu, menu)
    return true
}

override fun onOptionsItemSelected(item: MenuItem): Boolean {
    return when (item.itemId) {
        R.id.menu_add -> { viewModel.addCrime(); true }
        R.id.menu_settings -> { startActivity(Intent(this, SettingsActivity::class.java)); true }
        android.R.id.home -> { onBackPressedDispatcher.onBackPressed(); true }
        else -> super.onOptionsItemSelected(item)
    }
}
```

`showAsAction="ifRoom"` 有空间时显示图标；`never` 进 Overflow（⋮）。

{/* AppBar（Toolbar）分区 + 菜单 showAsAction 落位 + 溢出菜单展开 + 三处关键回调的静态解剖图 */}

<AppBarMenuDiagram />

<Stepper>
  <Step title="① 列表页配置 Toolbar">
    在列表 Activity 的布局中放置 `MaterialToolbar`，主题使用 `NoActionBar` 变体避免双顶栏。通过 `setSupportActionBar(toolbar)` 将 Toolbar 设为 Activity 的 App Bar——列表页的 Toolbar 通常只显示标题和菜单项（如"添加"），不显示返回箭头（home 图标）。
  </Step>
  <Step title="② 详情页启用 Up 导航">
    在详情 Activity 中调用 `supportActionBar?.setDisplayHomeAsUpEnabled(true)`——Toolbar 左边出现一个向左的箭头（←），告诉用户"点这里可以返回上一级"。这是"向上导航"（Up）而非系统"返回"（Back）——Up 保证用户回到应用内的逻辑父页面。
  </Step>
  <Step title="③ 点击 ← 触发 onOptionsItemSelected">
    用户点击返回箭头时，系统回调 `onOptionsItemSelected(item)`，`item.itemId` 为 `android.R.id.home`。在回调中判断：如果用 Jetpack Navigation，调 `navController.navigateUp()` 或 `NavigationUI.navigateUp(navController, configuration)`；如果手动管理，调 `onBackPressedDispatcher.onBackPressed()` 或 `finish()`。必须 `return true` 表示事件已被消费。
  </Step>
  <Step title="④ 返回栈弹出，回到列表页">
    详情 Activity 从返回栈弹出（或 Fragment 详情页被 NavController 回退），列表页重新进入 `onResume` 状态。如果列表页之前被系统回收，此时自动重建并恢复滚动位置——用户感觉一切如初。
  </Step>
</Stepper>

<Callout type="tip">
  **Jetpack Navigation** 与 Toolbar 绑定：`setupWithNavController(toolbar, navController)` 自动处理标题与 Up，少写重复 home 处理。
</Callout>

<Callout type="trap">
  忘记 `return true` → 系统继续处理，可能触发默认行为或重复执行。已处理的 item 必须返回 true。
</Callout>

<Callout type="trap">
  Fragment 里要菜单：在 `onCreateView` 里 `setHasOptionsMenu(true)`（旧 API）或 MenuProvider（AndroidX Activity 1.4+），否则 `onCreateOptionsMenu` 不会调到 Fragment。
</Callout>

## 小结

- NoActionBar 主题 + 布局 Toolbar + setSupportActionBar
- menu XML + inflate + onOptionsItemSelected
- Home/Up 与返回栈一致；Navigation 组件可自动绑定

<Exercises>

**问题 1**：“第14章 The App Bar”覆盖哪些正式节点和项目主线？

<Answer>覆盖7个节点，从“14. The App Bar”到“Challenge: An Empty View for the RecyclerView”。主线是解释AppCompat应用栏、菜单资源、选择回调和Asset Studio在导航与动作层的边界，交付物是菜单状态表、图标资源、空列表视图与配置变化测试。</Answer>

**问题 2**：怎样建立本页最小可执行实验？

<Answer>在“AppBar与菜单”的验证中，固定JDK、SDK、Gradle、设备API、语言、权限和输入，完成一个从用户动作到可观察结果的垂直切片；保存干净构建、设备操作、原始日志、状态快照与断言。</Answer>

**问题 3**：为什么只在正常点击路径运行不能证明完成？

<Answer>它没有覆盖组件重建、进程死亡、异步取消、权限拒绝、外部输入失败、资源释放和版本变化。应逐项注入故障，并用菜单状态表、图标资源、空列表视图与配置变化测试核对。</Answer>

**问题 4**：怎样设计能推翻当前实现的反例？

<Answer>在“AppBar与菜单”的验证中，保持其他变量不变，分别注入旋转、后台回收、进程死亡、拒权、断网、无效输入与重复操作；任何状态丢失、越权、崩溃、泄漏或重复副作用都推翻完成结论。</Answer>

**问题 5**：从第4版语境迁移到现代目标SDK时如何控制变量？

<Answer>先按原书还原机制并锁定基线，再查询目标SDK合同；一次只迁移组件导出、存储、权限、后台、通知或依赖之一，保存构建、行为、测试和回滚差异。</Answer>

**问题 6**：本页达到独立交接标准需要什么？

<Answer>需要版本与设备指纹、源码提交、输入、构建命令、生命周期轨迹、原始日志、失败测试、安全边界与发布产物，让他人凭菜单状态表、图标资源、空列表视图与配置变化测试重放。</Answer>

</Exercises>

<Glossary>

<GlossaryItem term="App Bar">
  Material Design 顶栏，可显示标题、导航图标和操作菜单，现代 App 多用 Toolbar 实现。
</GlossaryItem>

<GlossaryItem term="ActionBar">
  旧版框架提供的顶栏，现推荐用 Toolbar + setSupportActionBar 替代。
</GlossaryItem>

<GlossaryItem term="Toolbar">
  可嵌入布局的顶栏控件，功能同 ActionBar 但可定制位置与样式。
</GlossaryItem>

<GlossaryItem term="Options Menu">
  Activity/Fragment 顶栏右侧的操作项集合，由 menu 资源定义。
</GlossaryItem>

<GlossaryItem term="Up navigation">
  应用内层级返回（←），不同于系统 Back——应弹出当前 Task 内上一级 Destination。
</GlossaryItem>

</Glossary>

{/* BNR4_QUALITY_V2 */}

“AppBar与菜单”不使用未获授权的纸书正文；[InformIT出版信息](https://www.informit.com/store/android-programming-the-big-nerd-ranch-guide-9780135245125)与[授权电子版完整目录](https://www.oreilly.com/library/view/android-programming-the/9780135257555/)只用于确认第四版32章、269个正式目录节点和时代语境，[第四版官方勘误](https://github.com/bignerdranch/AndroidCourseResources/raw/master/4thEdition/Errata/4eAddendum.pdf)用于识别工具链变更。下列中文解释、图示、交互、代码与练习均为独立教学重写，平台行为再以[Android Developers](https://developer.android.com/develop)的一手文档复核。

## 为什么“AppBar与菜单”必须回到可观察状态

“AppBar与菜单”的学习结果不是记住类名，而是能预测“掌握 Toolbar 替代 ActionBar——菜单资源、Options Menu、Overflow 和 Navigation Icon 的配置。”在一次输入、一次重建和一次失败中的不同状态，并指出哪条Android合同产生差异。

## 第四版机制逐项深读

### 14. The App Bar

在“AppBar与菜单”中，“14. The App Bar”把顶层动作、导航和溢出菜单接入当前界面状态；菜单创建与选择回调要在窄屏、无数据和权限受限时仍有可理解反馈。

### AppCompat Default App Bar

在“AppBar与菜单”中，“AppCompat Default App Bar”把顶层动作、导航和溢出菜单接入当前界面状态；菜单创建与选择回调要在窄屏、无数据和权限受限时仍有可理解反馈。

### Menus

在“AppBar与菜单”中，“Menus”把顶层动作、导航和溢出菜单接入当前界面状态；菜单创建与选择回调要在窄屏、无数据和权限受限时仍有可理解反馈。

### Using the Android Asset Studio

在“AppBar与菜单”中，“Using the Android Asset Studio”通过资源限定符和主题属性把内容与设备配置解耦；最终值由资源匹配和主题继承共同决定。

### For the More Curious: App Bar vs Action Bar vs Toolbar

在“AppBar与菜单”中，“For the More Curious: App Bar vs Action Bar vs Toolbar”把顶层动作、导航和溢出菜单接入当前界面状态；菜单创建与选择回调要在窄屏、无数据和权限受限时仍有可理解反馈。

### For the More Curious: Accessing the AppCompat App Bar

在“AppBar与菜单”中，“For the More Curious: Accessing the AppCompat App Bar”把顶层动作、导航和溢出菜单接入当前界面状态；菜单创建与选择回调要在窄屏、无数据和权限受限时仍有可理解反馈。

### Challenge: An Empty View for the RecyclerView

在“AppBar与菜单”中，验证“Challenge: An Empty View for the RecyclerView”用插入、删除、重排和多类型数据检查item身份、焦点与点击目标，记录首个错误绑定。

## 可操作机制实验

“AppBar与菜单”把“掌握 Toolbar 替代 ActionBar——菜单资源、Options Menu、Overflow 和 Navigation Icon 的配置。”拆成三个可观察层次：先选择目录节点并确认责任边界，再改变一个生命周期或外部条件，最后回到双视口、大字体、焦点顺序、状态语义和对比度截图。点击后的文字、状态与轨迹必须同步变化，重置后回到相同基线。

<Steps>
  <Step title="1. 选择机制节点">
    选择一个正式节点，先预测它由谁拥有、在哪个线程执行、哪些状态需要恢复；右侧会显示本章专属的机制合同。

    <BnrLifecycleLab />

  </Step>
  <Step title="2. 注入一个边界条件">
    改变生命周期阶段、外部可用性或权限中的一项，观察回调、队列、数据或渲染结果怎样变化；不要用总风险分数代替因果。

    <BnrFailureLab />

  </Step>
  <Step title="3. 复位并核对证据">
    用双视口、大字体、焦点顺序、状态语义和对比度截图核对预测，随后执行重置；若基线不能复现，实验本身不通过。

    <BnrEvidenceLab />

  </Step>
</Steps>

<Callout type="trap">在“AppBar与菜单”中，正常点击成功只能证明一个样本。若重建、取消、无响应者、离线、旧schema或目标SDK变化会产生不同结果，就必须把该边界写进状态机与测试。</Callout>

<Callout type="trap">“AppBar与菜单”若只留下最终截图，就无法判断旧所有者是否收到回调、后台任务是否重复、状态是否来自错误层或复位是否真正清空实验。</Callout>

## “AppBar与菜单”验收回顾

“AppBar与菜单”只有在双视口、大字体、焦点顺序、状态语义和对比度截图能够从相同基线再次得到相同断言时才通过；第四版机制与现代平台政策分别记录，不用新API名称掩盖旧行为。

[← 上一页：对话框](/learn/big-nerd-ranch-guide/ui-fragment/dialogs) · [下一页：隐式intent →](/learn/big-nerd-ranch-guide/intent-data/implicit-intents)

<Attribution
  mode="independent-rewrite"
  sourceBasis="authorized-sample"
  workTitle="Bill Phillips, Chris Stewart, Kristin Marsicano, Brian Gardner, Android Programming: The Big Nerd Ranch Guide, Fourth Edition"
  adaptedUrl="https://www.oreilly.com/library/view/android-programming-the/9780135257555/"
/>
