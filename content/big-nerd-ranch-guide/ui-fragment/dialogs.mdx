---
title: 对话框
type: C
section: UI与Fragment
order: 13
description: 掌握 AlertDialog 构建模式、DialogFragment 的配置变更安全机制、以及 FragmentResult API 的双向数据传递。
demo: true
math: false
sourceUrl: "https://www.oreilly.com/library/view/android-programming-the/9780135257555/"
draft: false
---

import { BnrLifecycleLab, BnrFailureLab, BnrEvidenceLab } from "@/components/mdx/big-nerd-ranch-guide/diagrams/dialogs";

{/* C 实战型：对话框完整用法。
   重心在概念讲解(§3)和代码对照(§6)，常见误区 ≥3 个，练习含实现题。 */}

{/* 知识点清单：
   1) AlertDialog.Builder 构建器模式 = 代码对照
   2) DialogFragment 配置变更安全 = Stepper 对比（普通 Dialog vs DialogFragment 旋转屏幕）
   3) DatePickerDialog & TimePickerDialog = 代码对照
   4) 自定义布局 Dialog = 代码对照
   5) FragmentResult API（双向数据传输） = 代码对照
   6) setTargetFragment（传统方式，已废弃） = 对比说明 */}

{/* 1. 学习目标。 */}

<Objectives>

- 能解释“第13章 Dialogs”全部正式节点的组件所有者、生命周期、线程、状态来源与Android版本边界。
- 能实现“用DialogFragment托管对话框生命周期，并通过明确目标和结果合同在Fragment间传值”的最小垂直切片，并保存构建、设备操作、原始日志和断言。
- 能比较正常创建、旋转、后台、进程重建、权限拒绝与外部故障下的行为差异。
- 能设计推翻当前实现的反例，并凭日期对话框、返回值协议、旋转恢复和更多对话框挑战完成独立交接。

</Objectives>

{/* 2. 直觉引入。 */}

## 为什么需要对话框？

你在用 App 时肯定见过这些场景：弹出一个窗口问"确定删除吗？"、选个日期、输个名字。这些都是对话框——一种悬浮在主界面之上的轻量级 UI。

对话框的核心价值：**打断用户当前操作，强制做出一个选择后再继续**。它不是"顺便问问你"的那种弱提醒（那是 Snackbar/Toast 的活），而是"你不回答我就不让你往下走"的强交互。

用得好，对话框让交互清晰且安全（删除前确认）。用不好，对话框满天飞就是用户体验灾难——所以 Android 提供了一套规范的对话框 API，把常见的模式（确认、输入、选择、日期）都帮你封装好了。

{/* 3. 概念讲解。 */}

## 对话框是怎么工作的

### AlertDialog：最通用的对话框

<Term def="AlertDialog 是 Android 最通用的对话框类。用 Builder 构建器模式（链式调用 setTitle、setMessage、setPositiveButton 等），最后 build() 创建、show() 显示。">AlertDialog</Term> 使用构建器模式——你先用 `AlertDialog.Builder` 一点点搭好对话框的样子（标题、内容、按钮），最后 `build()` 创建它。三个区域的按钮分工明确：

- **Positive 按钮**（确定/保存）——肯定操作，通常在右侧
- **Negative 按钮**（取消）——否定操作，Positive 左边
- **Neutral 按钮**（稍后提醒）——中立操作，最左边

<Callout type="tip">
  标准 Material Design 对话框建议最多三个按钮，且按钮文案用动词而非"是/否"——"删除"比"是"更能让用户明白点了会发生什么。
</Callout>

### DialogFragment：配置变更的安全网

为什么不能直接在 Activity 里 `AlertDialog.Builder(...).show()`？

问题出在**屏幕旋转**。当你旋转手机时，Activity 被销毁重建——但那个正在显示的对话框不属于 Activity 生命周期管理范围，重建后对话框消失了，用户之前的选择也丢了。DialogFragment 解决了这个问题：它本身是一个 Fragment，受 FragmentManager 管理，旋转屏幕时跟普通 Fragment 一样被保存和恢复。

- <Term def="DialogFragment 是一个特殊的 Fragment——它是对话框的「安全壳」。对话框的生命周期跟 DialogFragment 绑定，享受 FragmentManager 的自动保存和恢复。">DialogFragment</Term> 就是一个"包着对话框的 Fragment"——对话框的显示和隐藏跟 DialogFragment 的生命周期绑定，配置变更时自动保存恢复，你不需要做任何额外操作。

{/* DialogFragment：宿主失焦 + 对话框浮起 + 相对裸 AlertDialog 的优势 */}

<DialogFragmentDiagram />

### FragmentResult API：对话框怎么把结果传回来

对话框最常见的需求就是"用户选完了之后，发起方要知道选了什么"。传统做法用 `setTargetFragment()`（现已废弃）——发起方把 Fragment 设为目标，接收方通过 `onActivityResult` 接收。这套方案的耦合度太高了。

现代的做法是 FragmentResult API——可以理解为"广播机制"：

- 对话框里用 `setFragmentResult(requestKey, bundle)` 发出结果
- 发起方用 `setFragmentResultListener(requestKey)` 监听结果
- 双方通过 `requestKey`（一个字符串）配对——发的人和收的人用同一个 key，就跟对讲机共用同一个频道一样

```kotlin
// 发起方 (CrimeDetailFragment)
setFragmentResultListener("requestKey") { _, bundle ->
    val result = bundle.getString("selectedDate")
}

// 对话框方 (DatePickerFragment) 在用户选完日期后
setFragmentResult("requestKey", bundleOf("selectedDate" to date.toString()))
```

{/* 5. 交互 Demo：Stepper 分步展示 DialogFragment 弹出与回传 */}

<Stepper>
  <Step title="① 创建 DialogFragment 子类">
    新建一个继承 `DialogFragment()` 的类。它本身是一个 Fragment——受 FragmentManager 管理，旋转屏幕时自动保存恢复，不会像普通 Dialog 那样丢失。重写 `onCreateDialog()` 方法返回要显示的 Dialog 对象。
  </Step>
  <Step title="② onCreateDialog 构建 AlertDialog">
    在 `onCreateDialog()` 里用 `AlertDialog.Builder(requireContext())` 构建对话框：`.setTitle()` 设标题、`.setMessage()` 设内容、`.setPositiveButton("确定", listener)` 设肯定按钮。`listener` 里拿到用户输入或选择结果，打包成 Bundle 通过 `setFragmentResult(requestKey, bundle)` 发出。
  </Step>
  <Step title="③ show 显示对话框">
    在发起方代码中 `DatePickerFragment().show(childFragmentManager, "tag")` 显示对话框——使用 `childFragmentManager` 因为对话框是当前 Fragment 的子 Fragment。show 之前必须先注册 `setFragmentResultListener`，否则 listener 在结果发出后才注册，收不到数据。
  </Step>
  <Step title="④ FragmentResult 回传结果给发起方">
    发起方在 `show()` 之前调 `childFragmentManager.setFragmentResultListener(requestKey, viewLifecycleOwner) { _, bundle -> ... }`，用与对话框相同的 `requestKey` 配对。`viewLifecycleOwner` 确保在 View 销毁时自动解绑 listener，避免对已销毁 View 的无效更新。
  </Step>
</Stepper>

{/* 6. 代码对照。 */}

## 代码逐段拆解

### 第一步：创建带 DatePicker 的 DialogFragment

```kotlin
import androidx.fragment.app.DialogFragment
import android.app.DatePickerDialog
import java.util.Calendar

class DatePickerFragment : DialogFragment() {

    override fun onCreateDialog(savedInstanceState: Bundle?): Dialog {
        val calendar = Calendar.getInstance()
        val year = calendar.get(Calendar.YEAR)
        val month = calendar.get(Calendar.MONTH)
        val day = calendar.get(Calendar.DAY_OF_MONTH)

        return DatePickerDialog(
            requireContext(),
            null, // 实现 DatePickerDialog.OnDateSetListener 时设 null
            year, month, day
        )
    }
}
```

重写 `onCreateDialog()` 返回一个系统对话框——这是最简单的方式。

### 第二步：把选择的日期传回发起方

```kotlin
class DatePickerFragment : DialogFragment() {

    override fun onCreateDialog(savedInstanceState: Bundle?): Dialog {
        val calendar = Calendar.getInstance()

        return DatePickerDialog(
            requireContext(),
            { _, year, month, day ->
                val resultDate = GregorianCalendar(year, month, day).time
                val resultBundle = Bundle().apply {
                    putSerializable(KEY_DATE, resultDate)
                }
                setFragmentResult(REQUEST_KEY, resultBundle)
            },
            calendar.get(Calendar.YEAR),
            calendar.get(Calendar.MONTH),
            calendar.get(Calendar.DAY_OF_MONTH)
        )
    }

    companion object {
        const val REQUEST_KEY = "DATE_PICKER_RESULT"
        const val KEY_DATE = "selected_date"
    }
}
```

注意：`DatePickerDialog` 构造器的第二个参数就是回调——用户选完日期后触发。在这里把选好的日期打成 Bundle，用 `setFragmentResult` 发出去。

### 第三步：发起方监听结果

```kotlin
class CrimeDetailFragment : Fragment(R.layout.fragment_crime_detail) {

    override fun onViewCreated(view: View, savedInstanceState: Bundle?) {
        super.onViewCreated(view, savedInstanceState)

        childFragmentManager.setFragmentResultListener(
            DatePickerFragment.REQUEST_KEY,
            viewLifecycleOwner
        ) { _, bundle ->
            val date = bundle.getSerializable(DatePickerFragment.KEY_DATE) as Date
            updateDate(date)
        }
    }

    private fun showDatePicker() {
        DatePickerFragment().show(
            childFragmentManager,
            "DatePicker"
        )
    }
}
```

这里用的是 `childFragmentManager` 而不是 `supportFragmentManager`——因为对话框是当前 Fragment 的子 Fragment，用子 FragmentManager 管理。`viewLifecycleOwner` 确保只在 Fragment 的 View 活着的时候监听——View 销毁后自动取消监听，避免泄漏。

### 第四步：自定义布局的对话框

```kotlin
class InputDialogFragment : DialogFragment() {

    override fun onCreateDialog(savedInstanceState: Bundle?): Dialog {
        val view = LayoutInflater.from(requireContext())
            .inflate(R.layout.dialog_input, null, false)

        val editText = view.findViewById<EditText>(R.id.input_text)

        return AlertDialog.Builder(requireContext())
            .setView(view)
            .setTitle("输入嫌疑人姓名")
            .setPositiveButton("确定") { _, _ ->
                val result = editText.text.toString()
                setFragmentResult(
                    REQUEST_KEY,
                    Bundle().apply { putString(KEY_INPUT, result) }
                )
            }
            .setNegativeButton("取消", null)
            .create()
    }
}
```

`.setView(view)` 把自定义布局塞进对话框的"内容区域"。按钮处理中拿到的 `editText` 引用在回调触发时仍然有效（View 还没销毁）。

<Callout type="tip">
  自定义布局的 EditText 设置了 `text` 后，按键盘回车不会自动关闭对话框。如果想回车=确定，给 EditText 设 `setOnEditorActionListener` 监听 `IME_ACTION_DONE`，在监听器里手动调 `dialog.dismiss()`。
</Callout>

{/* 7. 常见误区：C 型 ≥3 个。 */}

## 容易踩的坑

<Callout type="trap">
  **现象 →** 旋转屏幕后对话框消失了，用户选的数据也丢了。**原因 →** 直接用 `AlertDialog.Builder().show()` 显示对话框——这个对话框不属于任何生命周期管理范围，Activity 重建时它被系统丢弃。**修法 →** 用 `DialogFragment` 包装——把它当成普通 Fragment，旋转屏幕时 FragmentManager 自动保存恢复。
</Callout>

<Callout type="trap">
  **现象 →** `setFragmentResultListener` 的回调没收到数据，但发起方设置了 listener。**原因 →** listener 的注册时机在对话框结果**发出之后**。FragmentResult API 只把结果发给当前已注册的 listener——你后注册的 listener 收不到已发过的结果。**修法 →** 在对话框 `show()` 之前注册 listener——在 `show()` 之前调 `setFragmentResultListener`。
</Callout>

<Callout type="trap">
  **现象 →** 用 `childFragmentManager` show DialogFragment 时崩溃：`IllegalStateException: Fragment already added`。**原因 →** 连续快速点击按钮创建了两个 DialogFragment 实例（同一个 tag），第二个 `show()` 时发现同 tag 的 Fragment 已存在。**修法 →** 在 `show()` 之前先检查 `isAdded` 或按钮防抖。
</Callout>

{/* 8. 小结与练习。 */}

## 小结

- AlertDialog 用 Builder 模式构建——setTitle/setMessage/setPositiveButton 链式调完后 build()
- DialogFragment 是对话框的"安全壳"——旋转屏幕自动保存恢复，FragmentManager 统一管理
- FragmentResult API 用 requestKey 配对，发起方 listener 必须在 show() 之前注册
- 自定义布局用 `setView(view)`，按钮回调中 View 引用仍然有效

<Exercises>

**问题 1**：“第13章 Dialogs”覆盖哪些正式节点和项目主线？
<Answer>覆盖4个节点，从“13. Dialogs”到“Challenge: More Dialogs”。主线是用DialogFragment托管对话框生命周期，并通过明确目标和结果合同在Fragment间传值，交付物是日期对话框、返回值协议、旋转恢复和更多对话框挑战。</Answer>

**问题 2**：怎样建立本页最小可执行实验？
<Answer>固定JDK、SDK、Gradle、设备API、语言、权限和输入，完成一个从用户动作到可观察结果的垂直切片；保存干净构建、设备操作、原始日志、状态快照与断言。</Answer>

**问题 3**：为什么只在正常点击路径运行不能证明完成？
<Answer>它没有覆盖组件重建、进程死亡、异步取消、权限拒绝、外部输入失败、资源释放和版本变化。应逐项注入故障，并用日期对话框、返回值协议、旋转恢复和更多对话框挑战核对。</Answer>

**问题 4**：怎样设计能推翻当前实现的反例？
<Answer>保持其他变量不变，分别注入旋转、后台回收、进程死亡、拒权、断网、无效输入与重复操作；任何状态丢失、越权、崩溃、泄漏或重复副作用都推翻完成结论。</Answer>

**问题 5**：从第4版语境迁移到现代目标SDK时如何控制变量？
<Answer>先按原书还原机制并锁定基线，再查询目标SDK合同；一次只迁移组件导出、存储、权限、后台、通知或依赖之一，保存构建、行为、测试和回滚差异。</Answer>

**问题 6**：本页达到独立交接标准需要什么？
<Answer>需要版本与设备指纹、源码提交、输入、构建命令、生命周期轨迹、原始日志、失败测试、安全边界与发布产物，让他人凭日期对话框、返回值协议、旋转恢复和更多对话框挑战重放。</Answer>

</Exercises>

{/* 10. 名词解释。 */}

## 名词解释

<Glossary>
  <GlossaryItem term="AlertDialog">Android 最通用的对话框类。通过 Builder 构建器模式（链式调用 setTitle/setMessage/setPositiveButton 等）构建，最后 show() 显示。它悬浮在 Activity 之上、遮挡背景——迫使用户做出选择。</GlossaryItem>
  <GlossaryItem term="DialogFragment">对话框的 Fragment 包装器。把对话框纳入 FragmentManager 管理——享受自动保存恢复、生命周期协同的好处。解决了普通 Dialog 旋转屏幕会消失的经典问题。</GlossaryItem>
  <GlossaryItem term="FragmentResult API">Fragment 间通信的现代方案。一对"请求 key" + Bundle 的配对机制：发送方 `setFragmentResult`，接收方 `setFragmentResultListener`。取代了已废弃的 `setTargetFragment`。</GlossaryItem>
</Glossary>

<Attribution />


{/* BNR4_AUTHORITY_SUPPLEMENT */}

## 从生命周期、状态与证据开始

第4版不是API词典，而是六个逐步扩展的应用实验。每一章都要回答：用户从哪里进入，哪个Android组件拥有工作，回调在哪个线程发生，哪些事实必须跨旋转或进程死亡恢复，外部系统怎样失败，资源在何时释放。<Term def="负责创建、取消并最终释放组件、任务或资源的明确作用域">生命周期所有者</Term>把Activity、Fragment视图、ViewModel、Worker、线程与资源的寿命连接起来；没有所有者的异步任务就是潜在泄漏。

界面字段不是天然事实。<Term def="在配置变化或进程重建后，可从保存事实重新构造的最小用户状态">可恢复状态</Term>应根据数据大小与寿命放入saved instance state、ViewModel、数据库、文件或调度器，而不是全部塞进单例。Intent、文件、数据库、网络、系统服务和其他应用形成<Term def="跨边界交换数据时必须验证的action、类型、权限、大小、时序和失败约束">外部合同</Term>；正常响应只是合同中的一个样本。

本书以2019年第四版、Android 5.0至8.1及其后续兼容实践为语境。<Term def="Android版本、targetSdk、设备形态、权限与依赖共同限定的行为适用范围">版本边界</Term>要求先还原书中机制，再查询现代目标SDK的权限、后台、存储、通知和组件导出合同。<Term def="由环境指纹、输入、操作、日志、状态快照和断言组成的可重放结论">证据链</Term>则保证“在我的模拟器能跑”不会冒充可交付结论。

## 第4版正式目录逐节点映射

### 13. Dialogs

**正式节点 1/4。** 先说明“13. Dialogs”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“用DialogFragment托管对话框生命周期，并通过明确目标和结果合同在Fragment间传值”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Creating a DialogFragment

**正式节点 2/4。** 先说明“Creating a DialogFragment”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“用DialogFragment托管对话框生命周期，并通过明确目标和结果合同在Fragment间传值”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Passing Data Between Two Fragments

**正式节点 3/4。** 先说明“Passing Data Between Two Fragments”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“用DialogFragment托管对话框生命周期，并通过明确目标和结果合同在Fragment间传值”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Challenge: More Dialogs

**正式节点 4/4。** 先说明“Challenge: More Dialogs”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“用DialogFragment托管对话框生命周期，并通过明确目标和结果合同在Fragment间传值”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

## 最小可执行切片

先把状态转换与Android副作用分开。下面的Kotlin骨架不替代本章代码，而是让旋转、进程恢复与失败输入可单测：

```kotlin
sealed interface ScreenState {
    data object Loading : ScreenState
    data class Ready(val key: String) : ScreenState
    data class Failed(val reason: String, val retryable: Boolean) : ScreenState
}

fun reduce(current: ScreenState, event: UiEvent): ScreenState = nextState(current, event)
```

固定JDK、SDK、Gradle、设备API、语言和输入，清理缓存后从命令行保存制品与日志：

```bash
./gradlew --no-daemon clean test assembleDebug
adb install -r app/build/outputs/apk/debug/app-debug.apk
adb shell am force-stop com.example.app
adb shell monkey -p com.example.app 1
adb logcat -d -v threadtime > run.log
```

先写行为断言，再执行用户操作和故障注入：

```gherkin
Given a pinned build, device API, locale, permission state, and input
When the owner is recreated after state saving or process death
Then durable facts reconstruct the same user-visible state
And cancelled work cannot update a destroyed owner
And denied permission, offline input, and malformed data remain recoverable
```

三段代码分别约束状态模型、环境重放和行为结果。真实项目还要加入静态检查、单元测试、仪器测试、无障碍检查和发布构建；日志只能支持断言，不能代替断言。

## 线程、状态与外部资源

主线程负责输入、生命周期分发和大部分UI工作，磁盘、网络、图片解码与长计算不能阻塞帧。后台结果返回前必须确认所有者仍有效；Fragment的生命周期与其View生命周期不是一回事。每个实现都应列出入口、线程、事实源、取消点、关闭责任和用户可见失败六项。

跨应用Intent、ContentProvider、WebView、网络响应、文件URI和深链都视为不可信输入。校验action、scheme、MIME、大小、授权与响应者；导出组件使用最小权限；日志、截图和测试数据库不得泄露令牌或个人数据。相机、音频、数据库、线程和回调注册必须在成功、异常与取消路径都释放。

性能以用户任务衡量：启动看可交互时刻，列表看掉帧与绑定分配，网络看超时和取消，后台工作看约束、电量与重复调度。每次比较固定设备温度、构建类型、数据规模和网络条件，先采基线再改变一个变量。

迁移现代目标SDK时，先在第4版语境中证明机制，再逐项处理组件导出、存储、通知、后台启动、权限、废弃API和依赖版本。每一步保存编译差异、行为差异、测试结果和回滚点；新API并不自动正确，旧API能编译也不代表仍符合平台政策。

## 三步验证实验

<Steps>
  <Step title="1. 建立目录节点与生命周期图">
    从“用DialogFragment托管对话框生命周期，并通过明确目标和结果合同在Fragment间传值”选一个用户任务，标出入口、组件、所有者、回调线程、可恢复状态和销毁点。先预测旋转、后台和进程恢复后哪些对象消失、哪些事实必须重建。
    <BnrLifecycleLab />
  </Step>
  <Step title="2. 注入生命周期与外部故障">
    依次改变生命周期阶段、网络和权限，每次只改变一个条件；记录原始日志、界面状态、持久事实与资源计数，区分预期失败和未知状态。
    <BnrFailureLab />
  </Step>
  <Step title="3. 通过交付证据门">
    用日期对话框、返回值协议、旋转恢复和更多对话框挑战核对版本设备、生命周期、持久状态、失败安全、测试和发布。缺少任何一项时，不得把正常点击路径视为完成。
    <BnrEvidenceLab />
  </Step>
</Steps>

<Callout type="trap">**核心陷阱。** 只复述类名与回调，未说明所有者、状态来源、线程、取消和失败后果。修复时回到用户任务和状态机，注入旋转、进程死亡、拒权、断网与无效输入。</Callout>

<Callout type="trap">**历史示例不等于当前平台政策。** 第4版准确反映出版时工具链；现代targetSdk可能改变组件导出、后台、存储、通知和权限。保留原书机制，再建立有证据的迁移层。</Callout>

<Callout type="warning">**真机与数据安全。** 使用测试账号、样本媒体和最小权限；清理日志、数据库与截图。签名密钥、API密钥、联系人和真实位置不得写入仓库或公开证据。</Callout>

## 本章回顾

本页从“13. Dialogs”覆盖到“Challenge: More Dialogs”，共4个正式目录节点。掌握标准不是记住API，而是能沿“用DialogFragment托管对话框生命周期，并通过明确目标和结果合同在Fragment间传值”解释组件与数据边界，运行正常、重建和失败实验，并让另一位开发者仅凭日期对话框、返回值协议、旋转恢复和更多对话框挑战重放结论。

<Exercises>

**问题 1**：“第13章 Dialogs”覆盖哪些正式节点和项目主线？
<Answer>覆盖4个节点，从“13. Dialogs”到“Challenge: More Dialogs”。主线是用DialogFragment托管对话框生命周期，并通过明确目标和结果合同在Fragment间传值，交付物是日期对话框、返回值协议、旋转恢复和更多对话框挑战。</Answer>

**问题 2**：怎样建立本页最小可执行实验？
<Answer>固定JDK、SDK、Gradle、设备API、语言、权限和输入，完成一个从用户动作到可观察结果的垂直切片；保存干净构建、设备操作、原始日志、状态快照与断言。</Answer>

**问题 3**：为什么只在正常点击路径运行不能证明完成？
<Answer>它没有覆盖组件重建、进程死亡、异步取消、权限拒绝、外部输入失败、资源释放和版本变化。应逐项注入故障，并用日期对话框、返回值协议、旋转恢复和更多对话框挑战核对。</Answer>

**问题 4**：怎样设计能推翻当前实现的反例？
<Answer>保持其他变量不变，分别注入旋转、后台回收、进程死亡、拒权、断网、无效输入与重复操作；任何状态丢失、越权、崩溃、泄漏或重复副作用都推翻完成结论。</Answer>

**问题 5**：从第4版语境迁移到现代目标SDK时如何控制变量？
<Answer>先按原书还原机制并锁定基线，再查询目标SDK合同；一次只迁移组件导出、存储、权限、后台、通知或依赖之一，保存构建、行为、测试和回滚差异。</Answer>

**问题 6**：本页达到独立交接标准需要什么？
<Answer>需要版本与设备指纹、源码提交、输入、构建命令、生命周期轨迹、原始日志、失败测试、安全边界与发布产物，让他人凭日期对话框、返回值协议、旋转恢复和更多对话框挑战重放。</Answer>

</Exercises>

<Glossary>
  <GlossaryItem term="生命周期所有者">负责创建、取消并最终释放组件、任务或资源的明确作用域。</GlossaryItem>
  <GlossaryItem term="可恢复状态">在配置变化或进程重建后，可从保存事实重新构造的最小用户状态。</GlossaryItem>
  <GlossaryItem term="外部合同">Intent、文件、数据库、网络、系统服务或其他应用与本组件交换数据时的可验证约束。</GlossaryItem>
  <GlossaryItem term="版本边界">Android版本、targetSdk、设备形态、权限与依赖共同限定的行为适用范围。</GlossaryItem>
  <GlossaryItem term="证据链">由环境指纹、输入、操作、日志、状态快照和断言组成的可重放结论。</GlossaryItem>
</Glossary>

[← 上一页：第12章 Fragment Navigation](/learn/big-nerd-ranch-guide/ui-fragment/fragment-navigation) · [下一页：第14章 The App Bar →](/learn/big-nerd-ranch-guide/ui-fragment/app-bar)

<Attribution adaptedFrom="Android Programming: The Big Nerd Ranch Guide, 4th Edition" adaptedUrl="https://www.oreilly.com/library/view/android-programming-the/9780135257555/" />
