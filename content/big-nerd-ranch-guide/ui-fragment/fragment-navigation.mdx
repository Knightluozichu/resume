---
title: Fragment Navigation
type: C
section: UI与Fragment
order: 12
description: 掌握 Jetpack Navigation 组件的导航图、Safe Args 类型安全传参、以及底部导航栏和返回栈管理。
demo: true
math: false
sourceUrl: "https://www.oreilly.com/library/view/android-programming-the/9780135257555/"
draft: false
---

import { BnrLifecycleLab, BnrFailureLab, BnrEvidenceLab } from "@/components/mdx/big-nerd-ranch-guide/diagrams/fragment-navigation";

{/* C 实战型：Jetpack Navigation 组件完整用法。
   重心在概念讲解(§3)和代码对照(§6)，常见误区 ≥3 个，练习含实现题。 */}

{/* 知识点清单：
   1) Navigation Graph XML（目的地/动作/参数） = 导航图可视化
   2) NavHostFragment（Fragment 容器） = Stepper 步骤图
   3) NavController.navigate() = 代码对照
   4) Safe Args 插件（类型安全参数传递） = 代码对照
   5) 深链接（Deep Link） = 代码对照
   6) NavigationUI（底部导航/应用栏集成） = 代码对照
   7) 返回栈管理（popUpTo / popUpToInclusive） = Stepper 示意图 */}

{/* 1. 学习目标。 */}

<Objectives>

- 能解释“第12章 Fragment Navigation”全部正式节点的组件所有者、生命周期、线程、状态来源与Android版本边界。
- 能实现“让单Activity拥有导航，以Fragment arguments表达稳定输入，以LiveData转换和数据库更新表达状态流”的最小垂直切片，并保存构建、设备操作、原始日志和断言。
- 能比较正常创建、旋转、后台、进程重建、权限拒绝与外部故障下的行为差异。
- 能设计推翻当前实现的反例，并凭导航栈、argument合同、数据库写入生命周期和差量刷新测试完成独立交接。

</Objectives>

{/* 2. 直觉引入。 */}

## 为什么导航需要专门的组件？

传统的 Fragment 导航是这样的：你手动 `supportFragmentManager.beginTransaction().replace(...).addToBackStack(...).commit()`。看起来还算清晰——直到你需要：
- 从 "列表页→详情页→编辑页" 三层跳转，按返回键要跳过编辑页直接回列表页
- 底部导航栏四个 Tab，每个 Tab 有自己独立的返回栈
- 从通知直接跳进"订单详情"这个深层页面（深链）

手动写这些逻辑时，不同开发者的回退栈行为千奇百怪——点两次返回键就崩溃是家常便饭。

Jetpack Navigation 组件把"从哪里来、到哪里去、怎么回"这件事统一管理。你画一张**导航图**（类似地铁线路图——每个站点是目的地，箭头是跳转动作），组件自动帮你处理 Fragment 切换和返回栈。Safe Args 插件还让你在页面间传参数时享受编译期类型检查——再也不会因为 key 拼错而运行时崩溃。

{/* 3. 概念讲解。 */}

## Navigation 是怎么工作的

### 三个核心角色

Navigation 组件由三个角色协同工作：

- <Term def="Navigation Graph 是一个 XML 文件，里面定义了一张「导航地图」——有哪些目的地（Fragment/Activity）、目的地之间怎么跳转（动作）、每个目的地需要什么参数。类比地铁线路图——站就是目的地，箭头就是怎么走。">Navigation Graph</Term> —— 一张"地铁线路图"。用 XML 描述：有哪些"站"（destination）、站之间怎么跳（action）、每个站需要什么"票"（argument）
- <Term def="NavHostFragment 是 Activity 布局里一个特殊的 Fragment 容器——它有「换站」的能力。你把 Navigation Graph 交给它，它就按图帮你切换 Fragment。">NavHostFragment</Term> —— 地铁的"站台"。它是 Activity 布局里一个特殊的 Fragment 容器，负责按导航图切换 Fragment
- <Term def="NavController 是导航的执行者。你调 navigate() 让它执行跳转，popBackStack() 让它返回上一站。同一个 Activity 只有一个 NavController。">NavController</Term> —— 地铁的"调度员"。你告诉它"去这一站"（`navigate()`），它实际执行 Fragment 事务并管理返回栈

{/* 导航图可视化：destination/action/navigate/popBackStack 与返回栈深度的可控动画 */}

<NavGraphDiagram />

### Safe Args：让传参在编译期就安全

传统传参方式：`arguments?.putString("KEY", value)`——如果接收方写成 `arguments?.getString("KAY")`（拼错了一个字母），编译没报错，运行时才会炸。Safe Args 是编译期生成的代码——你在导航图里声明参数，插件自动生成 `Directions` 类，调用时 IDE 有补全、编译期有检查。

{/* 5. 交互 Demo：Stepper 展示导航图到界面跳转 */}

> 猜一猜：如果你用 `navigate()` 连续跳转 5 个 Fragment，按返回键会怎样？需要按几下才能回到第一个？

<Stepper>
  <Step title="① 创建 Navigation Graph XML">
    在 `res/navigation/` 目录下创建如 `nav_graph.xml`，声明 `<navigation>` 根元素及 `app:startDestination`（第一个显示的页面）。用 `<fragment>` 标签定义每个目的地（`android:name` 指向 Fragment 全限定类名），在目的地内部用 `<argument>` 声明需要的参数及其类型。
  </Step>
  <Step title="② 添加 Action 定义跳转路径">
    在源 Fragment 标签内添加 `<action>` 子元素，指定 `android:id`（动作名如 `showCrimeDetail`）和 `app:destination`（目标 Fragment 的 ID）。如有需要，加上 `app:popUpTo` 和 `app:popUpToInclusive` 来清理返回栈——避免重复跳转导致栈帧堆积。
  </Step>
  <Step title="③ 在 Activity 布局中配置 NavHostFragment">
    在 Activity 布局 XML 里放置 `FragmentContainerView`，设 `android:name="androidx.navigation.fragment.NavHostFragment"`。通过 `app:navGraph="@navigation/nav_graph"` 绑定导航图，`app:defaultNavHost="true"` 让它拦截系统返回键——按返回键走 Navigation 的返回栈而非退出 Activity。
  </Step>
  <Step title="④ Safe Args 生成 Directions 类">
    在 `build.gradle` 中应用 Safe Args 插件后，编译项目——插件根据导航图中的 action 和 argument 自动生成 `XxxFragmentDirections` 类。这个类提供了带参数的 `actionXxx(param)` 方法，IDE 自动补全、编译期类型检查，参数 key 永远不会拼错。
  </Step>
  <Step title="⑤ navigate 执行跳转">
    在代码中调用 `findNavController().navigate(action)` 执行跳转，NavController 负责 Fragment 事务和返回栈管理。接收方用 `private val args: XxxFragmentArgs by navArgs()` 安全取出参数——类型确定、无需手动类型转换。
  </Step>
</Stepper>

{/* 6. 代码对照。 */}

## 代码逐段拆解

### 第一步：创建 Navigation Graph

```xml
<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/crimeListFragment">

    <fragment
        android:id="@+id/crimeListFragment"
        android:name="com.example.criminalintent.CrimeListFragment"
        android:label="案件列表"
        tools:layout="@layout/fragment_crime_list">
        <action
            android:id="@+id/showCrimeDetail"
            app:destination="@id/crimeDetailFragment" />
    </fragment>

    <fragment
        android:id="@+id/crimeDetailFragment"
        android:name="com.example.criminalintent.CrimeDetailFragment"
        android:label="案件详情"
        tools:layout="@layout/fragment_crime_detail">
        <argument
            android:name="crimeId"
            app:argType="java.util.UUID" />
    </fragment>
</navigation>
```

`app:startDestination` 指定第一个显示的页面——相当于地铁图的"起点站"。

### 第二步：在 Activity 布局中嵌入 NavHostFragment

```xml
<androidx.fragment.app.FragmentContainerView
    android:id="@+id/nav_host_fragment"
    android:name="androidx.navigation.fragment.NavHostFragment"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:defaultNavHost="true"
    app:navGraph="@navigation/nav_graph" />
```

`app:defaultNavHost="true"` 表示这个 NavHostFragment 会拦截系统的返回键——让它走 Navigation 的返回栈，不会直接退到桌面。

### 第三步：用 Safe Args 导航

Safe Args 插件会在编译期自动生成 `CrimeListFragmentDirections` 类：

```kotlin
class CrimeListFragment : Fragment(R.layout.fragment_crime_list) {

    override fun onViewCreated(view: View, savedInstanceState: Bundle?) {
        super.onViewCreated(view, savedInstanceState)

        // 点击条目 → 跳转详情页并传递 crimeId
        val action = CrimeListFragmentDirections.showCrimeDetail(crimeId)
        findNavController().navigate(action)
    }
}
```

接收方用 `navArgs()` 安全取出参数：

```kotlin
class CrimeDetailFragment : Fragment(R.layout.fragment_crime_detail) {

    private val args: CrimeDetailFragmentArgs by navArgs()

    override fun onViewCreated(view: View, savedInstanceState: Bundle?) {
        super.onViewCreated(view, savedInstanceState)
        val crimeId = args.crimeId // 编译期安全，IDE 自动补全
    }
}
```

<Callout type="tip">
  Safe Args 支持的类型：基本类型、Parcelable（用 `app:argType="全限定类名"`）、Enum（用 `app:argType="包名.枚举类名"`）、数组类型。对于自定义复杂类型（如 data class），记得实现 `Parcelable` 接口。
</Callout>

### 第四步：管理返回栈

```xml
<action
    android:id="@+id/showCrimeDetail"
    app:destination="@id/crimeDetailFragment"
    app:popUpTo="@id/crimeListFragment"
    app:popUpToInclusive="false" />
```

`popUpTo="@id/crimeListFragment"` 的意思是：导航到详情页之前，把返回栈里**从当前页到 crimeListFragment 之间所有页面**都弹出。`popUpToInclusive="false"` 表示 crimeListFragment 本身保留（不弹）。当你从编辑页点保存回到列表页时，返回栈就干净了——按返回键不会回到编辑页。

### 第五步：底部导航栏集成

```kotlin
class MainActivity : AppCompatActivity() {
    private lateinit var navController: NavController

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_main)

        val navHostFragment = supportFragmentManager
            .findFragmentById(R.id.nav_host_fragment) as NavHostFragment
        navController = navHostFragment.navController

        val bottomNav = findViewById<BottomNavigationView>(R.id.bottom_nav)
        bottomNav.setupWithNavController(navController)
    }
}
```

`setupWithNavController` 自动处理 Tab 点击 → 导航 + 选中状态同步 + 返回栈管理——一行代码搞定底部导航所有逻辑。

{/* 7. 常见误区：C 型 ≥3 个。 */}

## 容易踩的坑

<Callout type="trap">
  **现象 →** 按返回键直接退出 App 而不是回到上一个 Fragment。**原因 →** NavHostFragment 没设 `app:defaultNavHost="true"`，系统返回键被 Activity 直接拦截处理了。**修法 →** 在布局文件中的 FragmentContainerView 上加上 `app:defaultNavHost="true"`。
</Callout>

<Callout type="trap">
  **现象 →** 导航后返回栈里全是重复的 Fragment——从 A 到 B 再回来再过去，返回要按好几次。**原因 →** `navigate()` 每次都会往返回栈上加一个新实例，重复跳同一目的地会导致重复堆积。**修法 →** 在 action 上设 `app:popUpTo="@id/上一级"` + `app:popUpToInclusive="false"`——导航前先清理多余栈帧。
</Callout>

<Callout type="trap">
  **现象 →** Safe Args 生成的 Directions 类找不到了，IDE 标红。**原因 →** 改完导航图 XML 后没有重新构建项目（Build → Make Project）。Safe Args 插件需要 Gradle 构建才能生成代码。**修法 →** 执行 Build → Make Project 或在 Terminal 里跑 `./gradlew kaptDebugKotlin`。
</Callout>

{/* 8. 小结与练习。 */}

## 小结

- Navigation Graph 用 XML 描述"目的地地图"——哪些页面、怎么跳、带什么参数
- NavHostFragment 是容器，NavController 是调度员——navigate() 跳转，popBackStack() 返回
- Safe Args 把参数从运行时字符串变成编译期生成的强类型 Directions 类
- `popUpTo` 清理返回栈避免重复堆积；`defaultNavHost="true"` 拦截系统返回键

<Exercises>

**问题 1**：“第12章 Fragment Navigation”覆盖哪些正式节点和项目主线？
<Answer>覆盖8个节点，从“12. Fragment Navigation”到“Challenge: Efficient RecyclerView Reloading”。主线是让单Activity拥有导航，以Fragment arguments表达稳定输入，以LiveData转换和数据库更新表达状态流，交付物是导航栈、argument合同、数据库写入生命周期和差量刷新测试。</Answer>

**问题 2**：怎样建立本页最小可执行实验？
<Answer>固定JDK、SDK、Gradle、设备API、语言、权限和输入，完成一个从用户动作到可观察结果的垂直切片；保存干净构建、设备操作、原始日志、状态快照与断言。</Answer>

**问题 3**：为什么只在正常点击路径运行不能证明完成？
<Answer>它没有覆盖组件重建、进程死亡、异步取消、权限拒绝、外部输入失败、资源释放和版本变化。应逐项注入故障，并用导航栈、argument合同、数据库写入生命周期和差量刷新测试核对。</Answer>

**问题 4**：怎样设计能推翻当前实现的反例？
<Answer>保持其他变量不变，分别注入旋转、后台回收、进程死亡、拒权、断网、无效输入与重复操作；任何状态丢失、越权、崩溃、泄漏或重复副作用都推翻完成结论。</Answer>

**问题 5**：从第4版语境迁移到现代目标SDK时如何控制变量？
<Answer>先按原书还原机制并锁定基线，再查询目标SDK合同；一次只迁移组件导出、存储、权限、后台、通知或依赖之一，保存构建、行为、测试和回滚差异。</Answer>

**问题 6**：本页达到独立交接标准需要什么？
<Answer>需要版本与设备指纹、源码提交、输入、构建命令、生命周期轨迹、原始日志、失败测试、安全边界与发布产物，让他人凭导航栈、argument合同、数据库写入生命周期和差量刷新测试重放。</Answer>

</Exercises>

{/* 10. 名词解释。 */}

## 名词解释

<Glossary>
  <GlossaryItem term="Navigation Graph">一张描述"页面地图"的 XML 文件。声明所有目的地、它们之间的跳转动作、以及每个目的地需要的参数。可视化为地铁线路图——站是 Fragment，箭头是导航动作。</GlossaryItem>
  <GlossaryItem term="NavHostFragment">Activity 里的 Fragment 容器——但不是普通的容器。它持有 Navigation Graph，按图自动切换 Fragment。是整条地铁线的"站台"。</GlossaryItem>
  <GlossaryItem term="NavController">导航的执行引擎。你告诉它"去某站"，它处理 Fragment 事务和返回栈。通过 `findNavController()` 拿到。</GlossaryItem>
  <GlossaryItem term="Safe Args">Navigation 的编译期参数安全插件。你在导航图里声明参数类型，插件自动生成 Directions 类——类型检查从运行时提前到编译期，参数 key 永远不会拼错。</GlossaryItem>
</Glossary>

<Attribution />


{/* BNR4_AUTHORITY_SUPPLEMENT */}

## 从生命周期、状态与证据开始

第4版不是API词典，而是六个逐步扩展的应用实验。每一章都要回答：用户从哪里进入，哪个Android组件拥有工作，回调在哪个线程发生，哪些事实必须跨旋转或进程死亡恢复，外部系统怎样失败，资源在何时释放。<Term def="负责创建、取消并最终释放组件、任务或资源的明确作用域">生命周期所有者</Term>把Activity、Fragment视图、ViewModel、Worker、线程与资源的寿命连接起来；没有所有者的异步任务就是潜在泄漏。

界面字段不是天然事实。<Term def="在配置变化或进程重建后，可从保存事实重新构造的最小用户状态">可恢复状态</Term>应根据数据大小与寿命放入saved instance state、ViewModel、数据库、文件或调度器，而不是全部塞进单例。Intent、文件、数据库、网络、系统服务和其他应用形成<Term def="跨边界交换数据时必须验证的action、类型、权限、大小、时序和失败约束">外部合同</Term>；正常响应只是合同中的一个样本。

本书以2019年第四版、Android 5.0至8.1及其后续兼容实践为语境。<Term def="Android版本、targetSdk、设备形态、权限与依赖共同限定的行为适用范围">版本边界</Term>要求先还原书中机制，再查询现代目标SDK的权限、后台、存储、通知和组件导出合同。<Term def="由环境指纹、输入、操作、日志、状态快照和断言组成的可重放结论">证据链</Term>则保证“在我的模拟器能跑”不会冒充可交付结论。

## 第4版正式目录逐节点映射

### 12. Fragment Navigation

**正式节点 1/8。** 先说明“12. Fragment Navigation”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“让单Activity拥有导航，以Fragment arguments表达稳定输入，以LiveData转换和数据库更新表达状态流”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Single Activity: Fragment Boss

**正式节点 2/8。** 先说明“Single Activity: Fragment Boss”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“让单Activity拥有导航，以Fragment arguments表达稳定输入，以LiveData转换和数据库更新表达状态流”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Fragment Arguments

**正式节点 3/8。** 先说明“Fragment Arguments”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“让单Activity拥有导航，以Fragment arguments表达稳定输入，以LiveData转换和数据库更新表达状态流”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Using LiveData Transformations

**正式节点 4/8。** 先说明“Using LiveData Transformations”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“让单Activity拥有导航，以Fragment arguments表达稳定输入，以LiveData转换和数据库更新表达状态流”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Updating the Database

**正式节点 5/8。** 先说明“Updating the Database”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“让单Activity拥有导航，以Fragment arguments表达稳定输入，以LiveData转换和数据库更新表达状态流”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### For the More Curious: Why Use Fragment Arguments?

**正式节点 6/8。** 先说明“For the More Curious: Why Use Fragment Arguments?”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“让单Activity拥有导航，以Fragment arguments表达稳定输入，以LiveData转换和数据库更新表达状态流”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### For the More Curious: Navigation Architecture Component Library

**正式节点 7/8。** 先说明“For the More Curious: Navigation Architecture Component Library”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“让单Activity拥有导航，以Fragment arguments表达稳定输入，以LiveData转换和数据库更新表达状态流”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Challenge: Efficient RecyclerView Reloading

**正式节点 8/8。** 先说明“Challenge: Efficient RecyclerView Reloading”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“让单Activity拥有导航，以Fragment arguments表达稳定输入，以LiveData转换和数据库更新表达状态流”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

## 最小可执行切片

先把状态转换与Android副作用分开。下面的Kotlin骨架不替代本章代码，而是让旋转、进程恢复与失败输入可单测：

```kotlin
sealed interface ScreenState {
    data object Loading : ScreenState
    data class Ready(val key: String) : ScreenState
    data class Failed(val reason: String, val retryable: Boolean) : ScreenState
}

fun reduce(current: ScreenState, event: UiEvent): ScreenState = nextState(current, event)
```

固定JDK、SDK、Gradle、设备API、语言和输入，清理缓存后从命令行保存制品与日志：

```bash
./gradlew --no-daemon clean test assembleDebug
adb install -r app/build/outputs/apk/debug/app-debug.apk
adb shell am force-stop com.example.app
adb shell monkey -p com.example.app 1
adb logcat -d -v threadtime > run.log
```

先写行为断言，再执行用户操作和故障注入：

```gherkin
Given a pinned build, device API, locale, permission state, and input
When the owner is recreated after state saving or process death
Then durable facts reconstruct the same user-visible state
And cancelled work cannot update a destroyed owner
And denied permission, offline input, and malformed data remain recoverable
```

三段代码分别约束状态模型、环境重放和行为结果。真实项目还要加入静态检查、单元测试、仪器测试、无障碍检查和发布构建；日志只能支持断言，不能代替断言。

## 线程、状态与外部资源

主线程负责输入、生命周期分发和大部分UI工作，磁盘、网络、图片解码与长计算不能阻塞帧。后台结果返回前必须确认所有者仍有效；Fragment的生命周期与其View生命周期不是一回事。每个实现都应列出入口、线程、事实源、取消点、关闭责任和用户可见失败六项。

跨应用Intent、ContentProvider、WebView、网络响应、文件URI和深链都视为不可信输入。校验action、scheme、MIME、大小、授权与响应者；导出组件使用最小权限；日志、截图和测试数据库不得泄露令牌或个人数据。相机、音频、数据库、线程和回调注册必须在成功、异常与取消路径都释放。

性能以用户任务衡量：启动看可交互时刻，列表看掉帧与绑定分配，网络看超时和取消，后台工作看约束、电量与重复调度。每次比较固定设备温度、构建类型、数据规模和网络条件，先采基线再改变一个变量。

迁移现代目标SDK时，先在第4版语境中证明机制，再逐项处理组件导出、存储、通知、后台启动、权限、废弃API和依赖版本。每一步保存编译差异、行为差异、测试结果和回滚点；新API并不自动正确，旧API能编译也不代表仍符合平台政策。

## 三步验证实验

<Steps>
  <Step title="1. 建立目录节点与生命周期图">
    从“让单Activity拥有导航，以Fragment arguments表达稳定输入，以LiveData转换和数据库更新表达状态流”选一个用户任务，标出入口、组件、所有者、回调线程、可恢复状态和销毁点。先预测旋转、后台和进程恢复后哪些对象消失、哪些事实必须重建。
    <BnrLifecycleLab />
  </Step>
  <Step title="2. 注入生命周期与外部故障">
    依次改变生命周期阶段、网络和权限，每次只改变一个条件；记录原始日志、界面状态、持久事实与资源计数，区分预期失败和未知状态。
    <BnrFailureLab />
  </Step>
  <Step title="3. 通过交付证据门">
    用导航栈、argument合同、数据库写入生命周期和差量刷新测试核对版本设备、生命周期、持久状态、失败安全、测试和发布。缺少任何一项时，不得把正常点击路径视为完成。
    <BnrEvidenceLab />
  </Step>
</Steps>

<Callout type="trap">**核心陷阱。** 只复述类名与回调，未说明所有者、状态来源、线程、取消和失败后果。修复时回到用户任务和状态机，注入旋转、进程死亡、拒权、断网与无效输入。</Callout>

<Callout type="trap">**历史示例不等于当前平台政策。** 第4版准确反映出版时工具链；现代targetSdk可能改变组件导出、后台、存储、通知和权限。保留原书机制，再建立有证据的迁移层。</Callout>

<Callout type="warning">**真机与数据安全。** 使用测试账号、样本媒体和最小权限；清理日志、数据库与截图。签名密钥、API密钥、联系人和真实位置不得写入仓库或公开证据。</Callout>

## 本章回顾

本页从“12. Fragment Navigation”覆盖到“Challenge: Efficient RecyclerView Reloading”，共8个正式目录节点。掌握标准不是记住API，而是能沿“让单Activity拥有导航，以Fragment arguments表达稳定输入，以LiveData转换和数据库更新表达状态流”解释组件与数据边界，运行正常、重建和失败实验，并让另一位开发者仅凭导航栈、argument合同、数据库写入生命周期和差量刷新测试重放结论。

<Exercises>

**问题 1**：“第12章 Fragment Navigation”覆盖哪些正式节点和项目主线？
<Answer>覆盖8个节点，从“12. Fragment Navigation”到“Challenge: Efficient RecyclerView Reloading”。主线是让单Activity拥有导航，以Fragment arguments表达稳定输入，以LiveData转换和数据库更新表达状态流，交付物是导航栈、argument合同、数据库写入生命周期和差量刷新测试。</Answer>

**问题 2**：怎样建立本页最小可执行实验？
<Answer>固定JDK、SDK、Gradle、设备API、语言、权限和输入，完成一个从用户动作到可观察结果的垂直切片；保存干净构建、设备操作、原始日志、状态快照与断言。</Answer>

**问题 3**：为什么只在正常点击路径运行不能证明完成？
<Answer>它没有覆盖组件重建、进程死亡、异步取消、权限拒绝、外部输入失败、资源释放和版本变化。应逐项注入故障，并用导航栈、argument合同、数据库写入生命周期和差量刷新测试核对。</Answer>

**问题 4**：怎样设计能推翻当前实现的反例？
<Answer>保持其他变量不变，分别注入旋转、后台回收、进程死亡、拒权、断网、无效输入与重复操作；任何状态丢失、越权、崩溃、泄漏或重复副作用都推翻完成结论。</Answer>

**问题 5**：从第4版语境迁移到现代目标SDK时如何控制变量？
<Answer>先按原书还原机制并锁定基线，再查询目标SDK合同；一次只迁移组件导出、存储、权限、后台、通知或依赖之一，保存构建、行为、测试和回滚差异。</Answer>

**问题 6**：本页达到独立交接标准需要什么？
<Answer>需要版本与设备指纹、源码提交、输入、构建命令、生命周期轨迹、原始日志、失败测试、安全边界与发布产物，让他人凭导航栈、argument合同、数据库写入生命周期和差量刷新测试重放。</Answer>

</Exercises>

<Glossary>
  <GlossaryItem term="生命周期所有者">负责创建、取消并最终释放组件、任务或资源的明确作用域。</GlossaryItem>
  <GlossaryItem term="可恢复状态">在配置变化或进程重建后，可从保存事实重新构造的最小用户状态。</GlossaryItem>
  <GlossaryItem term="外部合同">Intent、文件、数据库、网络、系统服务或其他应用与本组件交换数据时的可验证约束。</GlossaryItem>
  <GlossaryItem term="版本边界">Android版本、targetSdk、设备形态、权限与依赖共同限定的行为适用范围。</GlossaryItem>
  <GlossaryItem term="证据链">由环境指纹、输入、操作、日志、状态快照和断言组成的可重放结论。</GlossaryItem>
</Glossary>

[← 上一页：第11章 Databases and the Room Library](/learn/big-nerd-ranch-guide/ui-fragment/room-database) · [下一页：第13章 Dialogs →](/learn/big-nerd-ranch-guide/ui-fragment/dialogs)

<Attribution adaptedFrom="Android Programming: The Big Nerd Ranch Guide, 4th Edition" adaptedUrl="https://www.oreilly.com/library/view/android-programming-the/9780135257555/" />
