---
title: Fragment Navigation
type: C
section: UI与Fragment
order: 12
description: Fragment Navigation：保留第四版正文机制，以所有者—状态—结果合同、事件轨迹和章专属故障完成可重放验收。
demo: true
math: false
sourceUrl: "https://www.oreilly.com/library/view/android-programming-the/9780135257555/"
draft: false
qualityVersion: 2
practiceMode: simulation
sourceMode: independent-rewrite
---

import {
  FragmentNavigationContractLab,
  FragmentNavigationLifecycleLab,
  FragmentNavigationFaultLab,
} from "@/components/mdx/big-nerd-ranch-guide/v2/fragment-navigation";

import { Objectives } from "@/components/mdx/objectives";
import { Term } from "@/components/mdx/term";
import { NavGraphDiagram } from "@/components/mdx/diagrams/nav-graph-diagram";
import { Step, Stepper } from "@/components/mdx/stepper";
import { Callout } from "@/components/mdx/callout";
import { Answer, Exercises } from "@/components/mdx/exercises";
import { Glossary, GlossaryItem } from "@/components/mdx/glossary";
import { Attribution } from "@/components/mdx/attribution";

{/* C 实战型：Jetpack Navigation 组件完整用法。
重心在概念讲解(§3)和代码对照(§6)，常见误区 ≥3 个，练习含实现题。 */}

{/* 知识点清单：

1.  Navigation Graph XML（目的地/动作/参数） = 导航图可视化
2.  NavHostFragment（Fragment 容器） = Stepper 步骤图
3.  NavController.navigate() = 代码对照
4.  Safe Args 插件（类型安全参数传递） = 代码对照
5.  深链接（Deep Link） = 代码对照
6.  NavigationUI（底部导航/应用栏集成） = 代码对照
7.  返回栈管理（popUpTo / popUpToInclusive） = Stepper 示意图 */}

{/* 1. 学习目标。 */}

<Objectives>

- 能沿“Fragment Navigation”的用户事件解释Android组件、状态所有者、线程与销毁边界。
- 能围绕“掌握 Jetpack Navigation 组件的导航图、Safe Args 类型安全传参、以及底部导航栏和返回栈管理。”改出一个可运行结果，并用前后状态而非组件数量验收。
- 能在旋转、进程重建、拒权、离线或无效输入中选择适用反例，定位首个状态分叉。
- 能用构建指纹、用户操作、状态快照、原始日志和行为断言独立重放结论，并标明第四版机制与现代targetSdk政策的边界。

</Objectives>

{/* 2. 直觉引入。 */}

## 为什么导航需要专门的组件？

传统的 Fragment 导航是这样的：你手动 `supportFragmentManager.beginTransaction().replace(...).addToBackStack(...).commit()`。看起来还算清晰——直到你需要：

- 从 "列表页→详情页→编辑页" 三层跳转，按返回键要跳过编辑页直接回列表页
- 底部导航栏四个 Tab，每个 Tab 有自己独立的返回栈
- 从通知直接跳进"订单详情"这个深层页面（深链）

手动写这些逻辑时，不同开发者的回退栈行为千奇百怪——点两次返回键就崩溃是家常便饭。

Jetpack Navigation 组件把"从哪里来、到哪里去、怎么回"这件事统一管理。你画一张**导航图**（类似地铁线路图——每个站点是目的地，箭头是跳转动作），组件自动帮你处理 Fragment 切换和返回栈。Safe Args 插件还让你在页面间传参数时享受编译期类型检查——再也不会因为 key 拼错而运行时崩溃。

{/* 3. 概念讲解。 */}

## Navigation 是怎么工作的

### 三个核心角色

Navigation 组件由三个角色协同工作：

- <Term def="Navigation Graph 是一个 XML 文件，里面定义了一张「导航地图」——有哪些目的地（Fragment/Activity）、目的地之间怎么跳转（动作）、每个目的地需要什么参数。类比地铁线路图——站就是目的地，箭头就是怎么走。">
    Navigation Graph
  </Term>
  —— 一张"地铁线路图"。用 XML
  描述：有哪些"站"（destination）、站之间怎么跳（action）、每个站需要什么"票"（argument）
- <Term def="NavHostFragment 是 Activity 布局里一个特殊的 Fragment 容器——它有「换站」的能力。你把 Navigation Graph 交给它，它就按图帮你切换 Fragment。">
    NavHostFragment
  </Term>
  —— 地铁的"站台"。它是 Activity 布局里一个特殊的 Fragment
  容器，负责按导航图切换 Fragment
- <Term def="NavController 是导航的执行者。你调 navigate() 让它执行跳转，popBackStack() 让它返回上一站。同一个 Activity 只有一个 NavController。">
    NavController
  </Term>
  —— 地铁的"调度员"。你告诉它"去这一站"（`navigate()`），它实际执行 Fragment
  事务并管理返回栈

{/* 导航图可视化：destination/action/navigate/popBackStack 与返回栈深度的可控动画 */}

<NavGraphDiagram />

### Safe Args：让传参在编译期就安全

传统传参方式：`arguments?.putString("KEY", value)`——如果接收方写成 `arguments?.getString("KAY")`（拼错了一个字母），编译没报错，运行时才会炸。Safe Args 是编译期生成的代码——你在导航图里声明参数，插件自动生成 `Directions` 类，调用时 IDE 有补全、编译期有检查。

{/* 5. 交互 Demo：Stepper 展示导航图到界面跳转 */}

> 猜一猜：如果你用 `navigate()` 连续跳转 5 个 Fragment，按返回键会怎样？需要按几下才能回到第一个？

<Stepper>
  <Step title="① 创建 Navigation Graph XML">
    在 `res/navigation/` 目录下创建如 `nav_graph.xml`，声明 `<navigation>` 根元素及 `app:startDestination`（第一个显示的页面）。用 `<fragment>` 标签定义每个目的地（`android:name` 指向 Fragment 全限定类名），在目的地内部用 `<argument>` 声明需要的参数及其类型。
  </Step>
  <Step title="② 添加 Action 定义跳转路径">
    在源 Fragment 标签内添加 `<action>` 子元素，指定 `android:id`（动作名如 `showCrimeDetail`）和 `app:destination`（目标 Fragment 的 ID）。如有需要，加上 `app:popUpTo` 和 `app:popUpToInclusive` 来清理返回栈——避免重复跳转导致栈帧堆积。
  </Step>
  <Step title="③ 在 Activity 布局中配置 NavHostFragment">
    在 Activity 布局 XML 里放置 `FragmentContainerView`，设 `android:name="androidx.navigation.fragment.NavHostFragment"`。通过 `app:navGraph="@navigation/nav_graph"` 绑定导航图，`app:defaultNavHost="true"` 让它拦截系统返回键——按返回键走 Navigation 的返回栈而非退出 Activity。
  </Step>
  <Step title="④ Safe Args 生成 Directions 类">
    在 `build.gradle` 中应用 Safe Args 插件后，编译项目——插件根据导航图中的 action 和 argument 自动生成 `XxxFragmentDirections` 类。这个类提供了带参数的 `actionXxx(param)` 方法，IDE 自动补全、编译期类型检查，参数 key 永远不会拼错。
  </Step>
  <Step title="⑤ navigate 执行跳转">
    在代码中调用 `findNavController().navigate(action)` 执行跳转，NavController 负责 Fragment 事务和返回栈管理。接收方用 `private val args: XxxFragmentArgs by navArgs()` 安全取出参数——类型确定、无需手动类型转换。
  </Step>
</Stepper>

{/* 6. 代码对照。 */}

## 代码逐段拆解

### 第一步：创建 Navigation Graph

```xml
<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/crimeListFragment">

    <fragment
        android:id="@+id/crimeListFragment"
        android:name="com.example.criminalintent.CrimeListFragment"
        android:label="案件列表"
        tools:layout="@layout/fragment_crime_list">
        <action
            android:id="@+id/showCrimeDetail"
            app:destination="@id/crimeDetailFragment" />
    </fragment>

    <fragment
        android:id="@+id/crimeDetailFragment"
        android:name="com.example.criminalintent.CrimeDetailFragment"
        android:label="案件详情"
        tools:layout="@layout/fragment_crime_detail">
        <argument
            android:name="crimeId"
            app:argType="java.util.UUID" />
    </fragment>
</navigation>
```

`app:startDestination` 指定第一个显示的页面——相当于地铁图的"起点站"。

### 第二步：在 Activity 布局中嵌入 NavHostFragment

```xml
<androidx.fragment.app.FragmentContainerView
    android:id="@+id/nav_host_fragment"
    android:name="androidx.navigation.fragment.NavHostFragment"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:defaultNavHost="true"
    app:navGraph="@navigation/nav_graph" />
```

`app:defaultNavHost="true"` 表示这个 NavHostFragment 会拦截系统的返回键——让它走 Navigation 的返回栈，不会直接退到桌面。

### 第三步：用 Safe Args 导航

Safe Args 插件会在编译期自动生成 `CrimeListFragmentDirections` 类：

```kotlin
class CrimeListFragment : Fragment(R.layout.fragment_crime_list) {

    override fun onViewCreated(view: View, savedInstanceState: Bundle?) {
        super.onViewCreated(view, savedInstanceState)

        // 点击条目 → 跳转详情页并传递 crimeId
        val action = CrimeListFragmentDirections.showCrimeDetail(crimeId)
        findNavController().navigate(action)
    }
}
```

接收方用 `navArgs()` 安全取出参数：

```kotlin
class CrimeDetailFragment : Fragment(R.layout.fragment_crime_detail) {

    private val args: CrimeDetailFragmentArgs by navArgs()

    override fun onViewCreated(view: View, savedInstanceState: Bundle?) {
        super.onViewCreated(view, savedInstanceState)
        val crimeId = args.crimeId // 编译期安全，IDE 自动补全
    }
}
```

<Callout type="tip">
  Safe Args 支持的类型：基本类型、Parcelable（用
  `app:argType="全限定类名"`）、Enum（用
  `app:argType="包名.枚举类名"`）、数组类型。对于自定义复杂类型（如 data
  class），记得实现 `Parcelable` 接口。
</Callout>

### 第四步：管理返回栈

```xml
<action
    android:id="@+id/showCrimeDetail"
    app:destination="@id/crimeDetailFragment"
    app:popUpTo="@id/crimeListFragment"
    app:popUpToInclusive="false" />
```

`popUpTo="@id/crimeListFragment"` 的意思是：导航到详情页之前，把返回栈里**从当前页到 crimeListFragment 之间所有页面**都弹出。`popUpToInclusive="false"` 表示 crimeListFragment 本身保留（不弹）。当你从编辑页点保存回到列表页时，返回栈就干净了——按返回键不会回到编辑页。

### 第五步：底部导航栏集成

```kotlin
class MainActivity : AppCompatActivity() {
    private lateinit var navController: NavController

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_main)

        val navHostFragment = supportFragmentManager
            .findFragmentById(R.id.nav_host_fragment) as NavHostFragment
        navController = navHostFragment.navController

        val bottomNav = findViewById<BottomNavigationView>(R.id.bottom_nav)
        bottomNav.setupWithNavController(navController)
    }
}
```

`setupWithNavController` 自动处理 Tab 点击 → 导航 + 选中状态同步 + 返回栈管理——一行代码搞定底部导航所有逻辑。

{/* 7. 常见误区：C 型 ≥3 个。 */}

## 容易踩的坑

<Callout type="trap">
  **现象 →** 按返回键直接退出 App 而不是回到上一个 Fragment。**原因 →**
  NavHostFragment 没设 `app:defaultNavHost="true"`，系统返回键被 Activity
  直接拦截处理了。**修法 →** 在布局文件中的 FragmentContainerView 上加上
  `app:defaultNavHost="true"`。
</Callout>

<Callout type="trap">
  **现象 →** 导航后返回栈里全是重复的 Fragment——从 A 到 B
  再回来再过去，返回要按好几次。**原因 →** `navigate()`
  每次都会往返回栈上加一个新实例，重复跳同一目的地会导致重复堆积。**修法 →** 在
  action 上设 `app:popUpTo="@id/上一级"` +
  `app:popUpToInclusive="false"`——导航前先清理多余栈帧。
</Callout>

<Callout type="trap">
  **现象 →** Safe Args 生成的 Directions 类找不到了，IDE 标红。**原因 →**
  改完导航图 XML 后没有重新构建项目（Build → Make Project）。Safe Args 插件需要
  Gradle 构建才能生成代码。**修法 →** 执行 Build → Make Project 或在 Terminal
  里跑 `./gradlew kaptDebugKotlin`。
</Callout>

{/* 8. 小结与练习。 */}

## 小结

- Navigation Graph 用 XML 描述"目的地地图"——哪些页面、怎么跳、带什么参数
- NavHostFragment 是容器，NavController 是调度员——navigate() 跳转，popBackStack() 返回
- Safe Args 把参数从运行时字符串变成编译期生成的强类型 Directions 类
- `popUpTo` 清理返回栈避免重复堆积；`defaultNavHost="true"` 拦截系统返回键

<Exercises>

**问题 1**：“第12章 Fragment Navigation”覆盖哪些正式节点和项目主线？

<Answer>
  覆盖8个节点，从“12. Fragment Navigation”到“Challenge: Efficient RecyclerView
  Reloading”。主线是让单Activity拥有导航，以Fragment
  arguments表达稳定输入，以LiveData转换和数据库更新表达状态流，交付物是导航栈、argument合同、数据库写入生命周期和差量刷新测试。
</Answer>

**问题 2**：怎样建立本页最小可执行实验？

<Answer>
  在“Fragment
  Navigation”的验证中，固定JDK、SDK、Gradle、设备API、语言、权限和输入，完成一个从用户动作到可观察结果的垂直切片；保存干净构建、设备操作、原始日志、状态快照与断言。
</Answer>

**问题 3**：为什么只在正常点击路径运行不能证明完成？

<Answer>
  它没有覆盖组件重建、进程死亡、异步取消、权限拒绝、外部输入失败、资源释放和版本变化。应逐项注入故障，并用导航栈、argument合同、数据库写入生命周期和差量刷新测试核对。
</Answer>

**问题 4**：怎样设计能推翻当前实现的反例？

<Answer>
  在“Fragment
  Navigation”的验证中，保持其他变量不变，分别注入旋转、后台回收、进程死亡、拒权、断网、无效输入与重复操作；任何状态丢失、越权、崩溃、泄漏或重复副作用都推翻完成结论。
</Answer>

**问题 5**：从第4版语境迁移到现代目标SDK时如何控制变量？

<Answer>
  先按原书还原机制并锁定基线，再查询目标SDK合同；一次只迁移组件导出、存储、权限、后台、通知或依赖之一，保存构建、行为、测试和回滚差异。
</Answer>

**问题 6**：本页达到独立交接标准需要什么？

<Answer>
  需要版本与设备指纹、源码提交、输入、构建命令、生命周期轨迹、原始日志、失败测试、安全边界与发布产物，让他人凭导航栈、argument合同、数据库写入生命周期和差量刷新测试重放。
</Answer>

</Exercises>

{/* 10. 名词解释。 */}

## 名词解释

<Glossary>
  <GlossaryItem term="Navigation Graph">
    一张描述"页面地图"的 XML
    文件。声明所有目的地、它们之间的跳转动作、以及每个目的地需要的参数。可视化为地铁线路图——站是
    Fragment，箭头是导航动作。
  </GlossaryItem>
  <GlossaryItem term="NavHostFragment">
    Activity 里的 Fragment 容器——但不是普通的容器。它持有 Navigation
    Graph，按图自动切换 Fragment。是整条地铁线的"站台"。
  </GlossaryItem>
  <GlossaryItem term="NavController">
    导航的执行引擎。你告诉它"去某站"，它处理 Fragment 事务和返回栈。通过
    `findNavController()` 拿到。
  </GlossaryItem>
  <GlossaryItem term="Safe Args">
    Navigation 的编译期参数安全插件。你在导航图里声明参数类型，插件自动生成
    Directions 类——类型检查从运行时提前到编译期，参数 key 永远不会拼错。
  </GlossaryItem>
</Glossary>

{/* BNR4_QUALITY_V2 */}

“Fragment Navigation”不使用未获授权的纸书正文；[InformIT出版信息](https://www.informit.com/store/android-programming-the-big-nerd-ranch-guide-9780135245125)与[授权电子版完整目录](https://www.oreilly.com/library/view/android-programming-the/9780135257555/)只用于确认第四版32章、269个正式目录节点和时代语境，[第四版官方勘误](https://github.com/bignerdranch/AndroidCourseResources/raw/master/4thEdition/Errata/4eAddendum.pdf)用于识别工具链变更。下列中文解释、图示、交互、代码与练习均为独立教学重写，平台行为再以[Android Developers](https://developer.android.com/develop)的一手文档复核。

## 为什么“Fragment Navigation”必须回到可观察状态

“Fragment Navigation”的学习结果不是记住类名，而是能预测“掌握 Jetpack Navigation 组件的导航图、Safe Args 类型安全传参、以及底部导航栏和返回栈管理。”在一次输入、一次重建和一次失败中的不同状态，并指出哪条Android合同产生差异。

## 第四版机制逐项深读

### 12. Fragment Navigation

在“Fragment Navigation”中，分析“12. Fragment Navigation”要标出事务提交、参数写入、目标结果和返回栈变化，避免用活动中的临时字段传递可恢复状态。

### Single Activity: Fragment Boss

在“Fragment Navigation”中，“Single Activity: Fragment Boss”由FragmentManager保存结构和返回栈，而Fragment实例与其View拥有不同生命周期；视图绑定必须在onDestroyView前释放。

### Fragment Arguments

在“Fragment Navigation”中，验证“Fragment Arguments”在旋转、后台恢复和快速连点下比较fragment列表、back stack与目标数据，只出现一次导航副作用。

### Using LiveData Transformations

在“Fragment Navigation”中，验证“Using LiveData Transformations”从空库、重复键、并发写与旧schema升级四条路径核对行数、约束、通知和回滚。

### Updating the Database

在“Fragment Navigation”中，验证“Updating the Database”从空库、重复键、并发写与旧schema升级四条路径核对行数、约束、通知和回滚。

### For the More Curious: Why Use Fragment Arguments?

在“Fragment Navigation”中，分析“For the More Curious: Why Use Fragment Arguments?”要标出事务提交、参数写入、目标结果和返回栈变化，避免用活动中的临时字段传递可恢复状态。

### For the More Curious: Navigation Architecture Component Library

在“Fragment Navigation”中，验证“For the More Curious: Navigation Architecture Component Library”用同一事件序列比较旋转前后状态和副作用次数；界面相同但重复写入仍是不正确。

### Challenge: Efficient RecyclerView Reloading

在“Fragment Navigation”中，“Challenge: Efficient RecyclerView Reloading”的性能由帧时间、绑定分配与diff范围共同决定，不以单次滑动的主观流畅度验收。

<Callout type="trap">
  在“Fragment
  Navigation”中，正常点击成功只能证明一个样本。若重建、取消、无响应者、离线、旧schema或目标SDK变化会产生不同结果，就必须把该边界写进状态机与测试。
</Callout>

<Callout type="trap">
  “Fragment
  Navigation”若只留下最终截图，就无法判断旧所有者是否收到回调、后台任务是否重复、状态是否来自错误层或复位是否真正清空实验。
</Callout>

## “Fragment Navigation”验收回顾

“Fragment Navigation”只有在构建指纹、用户操作、状态快照、原始日志和行为断言能够从相同基线再次得到相同断言时才通过；第四版机制与现代平台政策分别记录，不用新API名称掩盖旧行为。

[← 上一页：数据库与Room库](/learn/big-nerd-ranch-guide/ui-fragment/room-database) · [下一页：对话框 →](/learn/big-nerd-ranch-guide/ui-fragment/dialogs)

{/* BNR4_VISUAL_V2_START */}

## 章专属可重放状态实验

先预测“列表点击、详情编辑、返回、旋转与进程重建”发生后，FragmentManager、导航容器与 CrimeRepository应怎样改变目的地、参数、返回栈、选中 Crime ID 和更新结果；再操作三个实验。第四版示例与当前 Android 政策分别记录，实验不把新 API 名称倒填为原书内容。

### 实验一：所有者—状态—结果合同

选择任一正式目录节点和正常/边界场景，检查它是否真的进入本章状态合同。目录标题只有同时出现在解释、可视状态和交付证据中才算覆盖。

<FragmentNavigationContractLab />

### 实验二：事件与生命周期轨迹

沿五次转换逐步执行“列表点击、详情编辑、返回、旋转与进程重建”。每一步只允许FragmentManager、导航容器与 CrimeRepository按职责提交状态，并持续核对“目的地只接收稳定 ID，并从事实源恢复详情而非携带整份可变对象”。

<FragmentNavigationLifecycleLab />

### 实验三：章专属反例与同输入恢复

注入“状态保存后继续提交事务，旋转时抛出 state loss 或显示重复 Fragment”，保存第一个偏离点；撤销后以完全相同的 SDK、设备状态和用户事件重放。只有事务、back stack、argument、实例 ID 和数据库更新断言一起恢复才算修复。

<FragmentNavigationFaultLab />

{/* BNR4_VISUAL_V2_END */}

<Attribution
  mode="independent-rewrite"
  sourceBasis="authorized-sample"
  workTitle="Bill Phillips, Chris Stewart, Kristin Marsicano, Brian Gardner, Android Programming: The Big Nerd Ranch Guide, Fourth Edition"
  adaptedUrl="https://www.oreilly.com/library/view/android-programming-the/9780135257555/"
/>
