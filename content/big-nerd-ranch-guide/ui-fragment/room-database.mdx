---
title: 数据库与Room库
type: C
section: UI与Fragment
order: 11
description: 掌握 Room 持久化库的 Entity-DAO-Database 三层架构，能用 LiveData 和 Flow 实现响应式数据查询与更新。
demo: true
math: false
sourceUrl: "https://www.oreilly.com/library/view/android-programming-the/9780135257555/"
draft: false
---

import { BnrLifecycleLab, BnrFailureLab, BnrEvidenceLab } from "@/components/mdx/big-nerd-ranch-guide/diagrams/room-database";

{/* C 实战型：Room 数据库完整用法。
   重心在概念讲解(§3)和代码对照(§6)，常见误区 ≥3 个，练习含实现题。 */}

{/* 知识点清单：
   1) Room 是什么（SQLite 之上的抽象层） = Googl e官方架构图
   2) Entity 数据类（@Entity/@PrimaryKey/@ColumnInfo） = 代码对照
   3) DAO 接口（@Insert/@Query/@Update/@Delete） = 代码对照
   4) RoomDatabase 抽象类（@Database/singleton/companion） = 代码对照
   5) 响应式查询（LiveData / Flow） = 代码对照
   6) TypeConverters 处理非基本类型 = 代码对照
   7) Repository 模式 = 代码对照
   8) 数据库迁移策略 = Stepper 示意图 */}

{/* 1. 学习目标：C 型必含「能改出 ___ 效果」。 */}

<Objectives>

- 能解释“第11章 Databases and the Room Library”全部正式节点的组件所有者、生命周期、线程、状态来源与Android版本边界。
- 能实现“从Entity、DAO、Database、Repository、线程和LiveData建立单一事实源”的最小垂直切片，并保存构建、设备操作、原始日志和断言。
- 能比较正常创建、旋转、后台、进程重建、权限拒绝与外部故障下的行为差异。
- 能设计推翻当前实现的反例，并凭Room模式、DAO测试、线程检查、schema导出与迁移证据完成独立交接。

</Objectives>

{/* 2. 直觉引入。 */}

## 为什么不能直接裸写 SQLite？

Android 系统内置了一个叫 SQLite 的小型数据库。它能存数据、能查、能删——听起来够用了。但实际上裸写 SQLite 非常痛苦：

你写一条 SQL 语句→传一堆问号参数→拿到一个 Cursor（一个"数据库查询结果遍历器"）→手动读每一列→手动类型转换→用完记得关 Cursor→等 App 被系统杀死时数据没了。

整个过程全是胶水代码，而且没有编译期检查——你把列名写错了（`CrimTitle` vs `crimeTitle`），只有等 App 跑到那行崩溃时才知道。

这就引出 Room。Room 不是重写一个数据库引擎——它只是在 SQLite 上面加了三层抽象，把你从胶水代码里解放出来。它自动把 Kotlin 数据类和 SQL 语句绑定、自动做类型检查、自动在后台线程执行、还能跟 LiveData/Flow 配合实现"数据一变，界面自动刷新"。

{/* 3. 概念讲解。 */}

## Room 三层架构，逐层拆开看

### 第一层：Entity——数据库的"一行"长什么样

<Term def="Room 的 Entity 就是把一个 Kotlin data class 标记为数据库里的「一张表」。类的每个属性默认对应表里的一列。@PrimaryKey 标记主键列。">Entity</Term> 就是数据库里的一张表在 Kotlin 世界里的"镜像"。一个 `@Entity` 注解的 data class 对应表里的一行——类的属性就是表的各列。

### 第二层：DAO——"怎么查"和"怎么改"

<Term def="DAO（Data Access Object，数据访问对象）是一个被 @Dao 注解的接口。它定义了对数据库能做哪些操作——插入、查询、更新、删除。Room 在编译期为这个接口自动生成实现类。">DAO</Term> 就是你对数据库可以做什么的"菜单"。增、删、改、查都定义在这里，Room 会在编译期自动帮你生成实现代码——你只用写接口，不用写实现。

### 第三层：Database——把上面两层串起来

<Term def="RoomDatabase 是一个被 @Database 注解的抽象类——它是进入数据库的「大门」。它告诉 Room 有哪些 Entity、用哪个 DAO，并提供获取 DAO 实例的方法。通常用单例模式确保全局只有一个数据库实例。">Database</Term> 就是通往数据库的大门。它告诉 Room"我的数据库里有哪些表（entities）、有哪些 DAO"。它用单例模式——全局只有一个数据库实例，避免重复打开文件。

{/* Room 三层架构 + 数据流静态图：@Database 提供 @Dao、@Dao 操作 @Entity 对应的表、底层落到 SQLite */}

<RoomArchitectureDiagram />

{/* 5. 交互 Demo：Stepper 从空项目到 Room 运行 */}

> 猜一猜：如果在数据库已发布 App 后，你改了 Entity 的结构（比如加了一列），App 会怎样？继续猜——如果没写迁移策略又会怎样？

<Stepper>
  <Step title="① Entity 定义数据表结构">
    创建一个 `@Entity` 注解的 data class，用 `@PrimaryKey` 标记主键列。这个类就是数据库一张表的 Kotlin 镜像——它的每个属性自动对应表的一列，Room 在编译期生成建表 SQL：`CREATE TABLE Crime (...)`。
  </Step>
  <Step title="② DAO 接口定义数据操作">
    写一个 `@Dao` 注解的 interface，在里面声明增删改查方法——`@Query` 写 SQL 查询、`@Insert`/`@Update`/`@Delete` 分别对应写操作。Room 编译期自动生成实现类，你只写接口不写胶水代码。增删改方法加 `suspend` 让 Room 在后台线程执行。
  </Step>
  <Step title="③ Database 抽象类串联 Entity 与 DAO">
    创建一个 `@Database` 注解的 abstract class（继承 `RoomDatabase`），在注解参数里声明 `entities` 和 `version`。类内部声明 `abstract fun crimeDao(): CrimeDao` 抽象方法，Room 编译期自动实现。用 companion object + `@Volatile` + 双重检查锁实现线程安全的单例——`Room.databaseBuilder(context, CrimeDatabase::class.java, "crime-database").build()`。
  </Step>
  <Step title="④ ViewModel 通过 Repository 调用 DAO">
    ViewModel 不直接拿 DAO，而是通过 Repository 层：`CrimeRepository(crimeDao)` 封装所有数据操作方法，对外暴露 `LiveData<List<Crime>>`。ViewModel 持有 Repository 引用，UI 层观察 LiveData——数据一变，界面自动刷新。这层间接性让你将来换数据库引擎只需改 Repository。
  </Step>
</Stepper>

{/* 6. 代码对照：C 型重心。 */}

## 代码逐段拆解

### 第一步：定义 Crime 为 Entity

```kotlin
import androidx.room.Entity
import androidx.room.PrimaryKey
import java.util.Date
import java.util.UUID

@Entity
data class Crime(
    @PrimaryKey
    val id: UUID = UUID.randomUUID(),
    val title: String = "",
    val date: Date = Date(),
    val isSolved: Boolean = false,
    val suspect: String = ""
)
```

`@PrimaryKey` 标注 id 列是主键——Room 用它来唯一标识每一行。`UUID.randomUUID()` 作为默认值意味着每次 `Crime()` 自动生成全局唯一 ID。

<Callout type="warn">
  Room 默认只能存基本类型（Int, String, Boolean 等）和它们的装箱类型。`UUID` 和 `Date` 不是基本类型——需要用 TypeConverter 告诉 Room 怎么存、怎么取。
</Callout>

### 第二步：写 TypeConverter（非基本类型的"翻译官"）

```kotlin
import androidx.room.TypeConverter
import java.util.Date
import java.util.UUID

class CrimeTypeConverters {
    @TypeConverter
    fun fromUUID(uuid: UUID): String = uuid.toString()

    @TypeConverter
    fun toUUID(uuid: String): UUID = UUID.fromString(uuid)

    @TypeConverter
    fun fromDate(date: Date): Long = date.time

    @TypeConverter
    fun toDate(timestamp: Long): Date = Date(timestamp)
}
```

Room 在写数据时调用 `from` 方法（对象→可存类型），读数据时调用 `to` 方法（可存类型→对象）。对 UUID 来说：存的时候变成 String，取的时候从 String 重建 UUID。

### 第三步：定义 DAO——对数据库能做哪些操作

```kotlin
import androidx.lifecycle.LiveData
import androidx.room.*

@Dao
interface CrimeDao {
    @Query("SELECT * FROM crime ORDER BY date DESC")
    fun getCrimes(): LiveData<List<Crime>>

    @Query("SELECT * FROM crime WHERE id = (:id)")
    fun getCrime(id: UUID): LiveData<Crime?>

    @Insert(onConflict = OnConflictStrategy.REPLACE)
    suspend fun addCrime(crime: Crime)

    @Update
    suspend fun updateCrime(crime: Crime)

    @Delete
    suspend fun deleteCrime(crime: Crime)
}
```

注意三个细节：
- `LiveData<List<Crime>>` 返回值——Room 会把查询结果包在 LiveData 里。底层数据一变，所有观察这个 LiveData 的地方自动收到新数据
- `suspend` 关键字——增删改操作标记为挂起函数，Room 自动在后台线程执行，不阻塞主线程
- `(꞉id)` 是命名参数占位符——比 `?` 更可读，IDE 也能自动补全

<Callout type="tip">
  如果你用 Kotlin Flow 而不是 LiveData，DAO 方法返回 `Flow<List<Crime>>` 即可。Flow 的优势是可以用 Kotlin 标准库的 map/filter/combine 等操作符。
</Callout>

### 第四步：定义 Database 抽象类

```kotlin
import androidx.room.Database
import androidx.room.RoomDatabase
import androidx.room.TypeConverters

@Database(entities = [Crime::class], version = 1, exportSchema = false)
@TypeConverters(CrimeTypeConverters::class)
abstract class CrimeDatabase : RoomDatabase() {
    abstract fun crimeDao(): CrimeDao

    companion object {
        @Volatile
        private var INSTANCE: CrimeDatabase? = null

        fun getInstance(context: Context): CrimeDatabase =
            INSTANCE ?: synchronized(this) {
                INSTANCE ?: Room.databaseBuilder(
                    context.applicationContext,
                    CrimeDatabase::class.java,
                    "crime-database"
                ).build().also { INSTANCE = it }
            }
    }
}
```

`@Volatile` + 双重检查锁 = 线程安全的单例。`databaseBuilder` 三个参数：上下文、Database 类、数据库文件名。

### 第五步：Repository 模式——把数据访问封装一层

直接在 ViewModel 里操作 DAO 会让 ViewModel 和 Room 紧耦合。加一个 Repository 中间层：

```kotlin
class CrimeRepository(private val crimeDao: CrimeDao) {
    val crimes: LiveData<List<Crime>> = crimeDao.getCrimes()

    suspend fun addCrime(crime: Crime) {
        crimeDao.addCrime(crime)
    }

    suspend fun getCrime(id: UUID): Crime? {
        return crimeDao.getCrime(id).value
    }
}
```

这样的话，ViewModel 只知道 Repository，不知道 Room 存在——将来换数据库引擎只需改 Repository。

{/* 7. 常见误区：C 型 ≥3 个。 */}

## 容易踩的坑

<Callout type="trap">
  **现象 →** `java.lang.IllegalStateException: Cannot access database on the main thread`。**原因 →** Room 在主线程上执行数据库操作（增删改）。Room 默认不允许主线程数据库操作，因为 SQLite 的磁盘 IO 会卡 UI。**修法 →** 给 DAO 的增删改方法加 `suspend` 关键字，把调用放在协程里（如 `viewModelScope.launch`）。
</Callout>

<Callout type="trap">
  **现象 →** 新增一列后 App 一打开就 Crash：`Room cannot verify the data integrity`。**原因 →** Entity 改了（加了列），但 `@Database` 的 `version` 没升级，也没提供 Migration 策略。Room 检测到表结构变了但版本号没变，直接报错。**修法 →** `version` 从 1 升到 2，添加 Migration 对象——或开发阶段用 `fallbackToDestructiveMigration()` 直接删库重建（数据全丢，仅开发环境可用！）。
</Callout>

<Callout type="trap">
  **现象 →** `LiveData` 里的数据更新了，但 UI 没反应。**原因 →** 每次都 new 了一个新的 DAO 实例或新的 LiveData 对象，导致 ViewModel 观察的 LiveData 和实际更新的不是同一个实例。**修法 →** 确保 DAO 和 LiveData 在 Repository 初始化时创建且只创建一次，ViewModel 持有的是同一个 LiveData 引用。
</Callout>

{/* 8. 小结与练习。 */}

## 小结

- Room 用三层封装 SQLite：Entity（表结构）→ DAO（操作接口）→ Database（大门+单例）
- DAO 返回 LiveData/Flow 实现"数据变、UI 自动变"的响应式编程
- 非基本类型必须提供 TypeConverter 告诉 Room 如何序列化/反序列化
- Repository 层隔离数据源细节，让 ViewModel 与 Room 解耦
- 修改 Entity 时必须同步升级 version 并提供 Migration——或开发期用 `fallbackToDestructiveMigration()`（慎用）

<Exercises>

**问题 1**：“第11章 Databases and the Room Library”覆盖哪些正式节点和项目主线？
<Answer>覆盖10个节点，从“11. Databases and the Room Library”到“For the More Curious: Singletons”。主线是从Entity、DAO、Database、Repository、线程和LiveData建立单一事实源，交付物是Room模式、DAO测试、线程检查、schema导出与迁移证据。</Answer>

**问题 2**：怎样建立本页最小可执行实验？
<Answer>固定JDK、SDK、Gradle、设备API、语言、权限和输入，完成一个从用户动作到可观察结果的垂直切片；保存干净构建、设备操作、原始日志、状态快照与断言。</Answer>

**问题 3**：为什么只在正常点击路径运行不能证明完成？
<Answer>它没有覆盖组件重建、进程死亡、异步取消、权限拒绝、外部输入失败、资源释放和版本变化。应逐项注入故障，并用Room模式、DAO测试、线程检查、schema导出与迁移证据核对。</Answer>

**问题 4**：怎样设计能推翻当前实现的反例？
<Answer>保持其他变量不变，分别注入旋转、后台回收、进程死亡、拒权、断网、无效输入与重复操作；任何状态丢失、越权、崩溃、泄漏或重复副作用都推翻完成结论。</Answer>

**问题 5**：从第4版语境迁移到现代目标SDK时如何控制变量？
<Answer>先按原书还原机制并锁定基线，再查询目标SDK合同；一次只迁移组件导出、存储、权限、后台、通知或依赖之一，保存构建、行为、测试和回滚差异。</Answer>

**问题 6**：本页达到独立交接标准需要什么？
<Answer>需要版本与设备指纹、源码提交、输入、构建命令、生命周期轨迹、原始日志、失败测试、安全边界与发布产物，让他人凭Room模式、DAO测试、线程检查、schema导出与迁移证据重放。</Answer>

</Exercises>

{/* 10. 名词解释。 */}

## 名词解释

<Glossary>
  <GlossaryItem term="Entity">数据库里一张表的 Kotlin 镜像。一个 `@Entity` 注解的 data class 对应一行数据，类属性对应列。标记后 Room 就会自动生成建表 SQL。</GlossaryItem>
  <GlossaryItem term="DAO">数据库操作菜单。一个 `@Dao` 注解的接口，里面声明增删改查方法，Room 在编译期自动生成实现。不用手写一行胶水代码。</GlossaryItem>
  <GlossaryItem term="Database">通往数据库的大门。一个 `@Database` 注解的抽象类，声明有哪些 Entity 和 DAO。它管理数据库文件的打开和关闭。</GlossaryItem>
  <GlossaryItem term="TypeConverter">非基本类型的"翻译官"。Room 默认只认 Int、String、Boolean。遇到 UUID、Date 等自定义类型时，TypeConverter 告诉 Room 存成什么格式、取出来怎么重建。</GlossaryItem>
  <GlossaryItem term="Migration">数据库结构升级的"施工方案"。当你改了 Entity（加列、改类型），Room 需要一份指令告诉它怎么从旧表结构变成新表结构。</GlossaryItem>
</Glossary>

<Attribution />


{/* BNR4_AUTHORITY_SUPPLEMENT */}

## 从生命周期、状态与证据开始

第4版不是API词典，而是六个逐步扩展的应用实验。每一章都要回答：用户从哪里进入，哪个Android组件拥有工作，回调在哪个线程发生，哪些事实必须跨旋转或进程死亡恢复，外部系统怎样失败，资源在何时释放。<Term def="负责创建、取消并最终释放组件、任务或资源的明确作用域">生命周期所有者</Term>把Activity、Fragment视图、ViewModel、Worker、线程与资源的寿命连接起来；没有所有者的异步任务就是潜在泄漏。

界面字段不是天然事实。<Term def="在配置变化或进程重建后，可从保存事实重新构造的最小用户状态">可恢复状态</Term>应根据数据大小与寿命放入saved instance state、ViewModel、数据库、文件或调度器，而不是全部塞进单例。Intent、文件、数据库、网络、系统服务和其他应用形成<Term def="跨边界交换数据时必须验证的action、类型、权限、大小、时序和失败约束">外部合同</Term>；正常响应只是合同中的一个样本。

本书以2019年第四版、Android 5.0至8.1及其后续兼容实践为语境。<Term def="Android版本、targetSdk、设备形态、权限与依赖共同限定的行为适用范围">版本边界</Term>要求先还原书中机制，再查询现代目标SDK的权限、后台、存储、通知和组件导出合同。<Term def="由环境指纹、输入、操作、日志、状态快照和断言组成的可重放结论">证据链</Term>则保证“在我的模拟器能跑”不会冒充可交付结论。

## 第4版正式目录逐节点映射

### 11. Databases and the Room Library

**正式节点 1/10。** 先说明“11. Databases and the Room Library”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“从Entity、DAO、Database、Repository、线程和LiveData建立单一事实源”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Room Architecture Component Library

**正式节点 2/10。** 先说明“Room Architecture Component Library”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“从Entity、DAO、Database、Repository、线程和LiveData建立单一事实源”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Creating a Database

**正式节点 3/10。** 先说明“Creating a Database”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“从Entity、DAO、Database、Repository、线程和LiveData建立单一事实源”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Defining a Data Access Object

**正式节点 4/10。** 先说明“Defining a Data Access Object”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“从Entity、DAO、Database、Repository、线程和LiveData建立单一事实源”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Accessing the Database Using the Repository Pattern

**正式节点 5/10。** 先说明“Accessing the Database Using the Repository Pattern”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“从Entity、DAO、Database、Repository、线程和LiveData建立单一事实源”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Testing Queries

**正式节点 6/10。** 先说明“Testing Queries”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“从Entity、DAO、Database、Repository、线程和LiveData建立单一事实源”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Application Threads

**正式节点 7/10。** 先说明“Application Threads”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“从Entity、DAO、Database、Repository、线程和LiveData建立单一事实源”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Using LiveData

**正式节点 8/10。** 先说明“Using LiveData”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“从Entity、DAO、Database、Repository、线程和LiveData建立单一事实源”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Challenge: Addressing the Schema Warning

**正式节点 9/10。** 先说明“Challenge: Addressing the Schema Warning”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“从Entity、DAO、Database、Repository、线程和LiveData建立单一事实源”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### For the More Curious: Singletons

**正式节点 10/10。** 先说明“For the More Curious: Singletons”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“从Entity、DAO、Database、Repository、线程和LiveData建立单一事实源”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

## 最小可执行切片

先把状态转换与Android副作用分开。下面的Kotlin骨架不替代本章代码，而是让旋转、进程恢复与失败输入可单测：

```kotlin
sealed interface ScreenState {
    data object Loading : ScreenState
    data class Ready(val key: String) : ScreenState
    data class Failed(val reason: String, val retryable: Boolean) : ScreenState
}

fun reduce(current: ScreenState, event: UiEvent): ScreenState = nextState(current, event)
```

固定JDK、SDK、Gradle、设备API、语言和输入，清理缓存后从命令行保存制品与日志：

```bash
./gradlew --no-daemon clean test assembleDebug
adb install -r app/build/outputs/apk/debug/app-debug.apk
adb shell am force-stop com.example.app
adb shell monkey -p com.example.app 1
adb logcat -d -v threadtime > run.log
```

先写行为断言，再执行用户操作和故障注入：

```gherkin
Given a pinned build, device API, locale, permission state, and input
When the owner is recreated after state saving or process death
Then durable facts reconstruct the same user-visible state
And cancelled work cannot update a destroyed owner
And denied permission, offline input, and malformed data remain recoverable
```

三段代码分别约束状态模型、环境重放和行为结果。真实项目还要加入静态检查、单元测试、仪器测试、无障碍检查和发布构建；日志只能支持断言，不能代替断言。

## 线程、状态与外部资源

主线程负责输入、生命周期分发和大部分UI工作，磁盘、网络、图片解码与长计算不能阻塞帧。后台结果返回前必须确认所有者仍有效；Fragment的生命周期与其View生命周期不是一回事。每个实现都应列出入口、线程、事实源、取消点、关闭责任和用户可见失败六项。

跨应用Intent、ContentProvider、WebView、网络响应、文件URI和深链都视为不可信输入。校验action、scheme、MIME、大小、授权与响应者；导出组件使用最小权限；日志、截图和测试数据库不得泄露令牌或个人数据。相机、音频、数据库、线程和回调注册必须在成功、异常与取消路径都释放。

性能以用户任务衡量：启动看可交互时刻，列表看掉帧与绑定分配，网络看超时和取消，后台工作看约束、电量与重复调度。每次比较固定设备温度、构建类型、数据规模和网络条件，先采基线再改变一个变量。

迁移现代目标SDK时，先在第4版语境中证明机制，再逐项处理组件导出、存储、通知、后台启动、权限、废弃API和依赖版本。每一步保存编译差异、行为差异、测试结果和回滚点；新API并不自动正确，旧API能编译也不代表仍符合平台政策。

## 三步验证实验

<Steps>
  <Step title="1. 建立目录节点与生命周期图">
    从“从Entity、DAO、Database、Repository、线程和LiveData建立单一事实源”选一个用户任务，标出入口、组件、所有者、回调线程、可恢复状态和销毁点。先预测旋转、后台和进程恢复后哪些对象消失、哪些事实必须重建。
    <BnrLifecycleLab />
  </Step>
  <Step title="2. 注入生命周期与外部故障">
    依次改变生命周期阶段、网络和权限，每次只改变一个条件；记录原始日志、界面状态、持久事实与资源计数，区分预期失败和未知状态。
    <BnrFailureLab />
  </Step>
  <Step title="3. 通过交付证据门">
    用Room模式、DAO测试、线程检查、schema导出与迁移证据核对版本设备、生命周期、持久状态、失败安全、测试和发布。缺少任何一项时，不得把正常点击路径视为完成。
    <BnrEvidenceLab />
  </Step>
</Steps>

<Callout type="trap">**核心陷阱。** 只复述类名与回调，未说明所有者、状态来源、线程、取消和失败后果。修复时回到用户任务和状态机，注入旋转、进程死亡、拒权、断网与无效输入。</Callout>

<Callout type="trap">**历史示例不等于当前平台政策。** 第4版准确反映出版时工具链；现代targetSdk可能改变组件导出、后台、存储、通知和权限。保留原书机制，再建立有证据的迁移层。</Callout>

<Callout type="warning">**真机与数据安全。** 使用测试账号、样本媒体和最小权限；清理日志、数据库与截图。签名密钥、API密钥、联系人和真实位置不得写入仓库或公开证据。</Callout>

## 本章回顾

本页从“11. Databases and the Room Library”覆盖到“For the More Curious: Singletons”，共10个正式目录节点。掌握标准不是记住API，而是能沿“从Entity、DAO、Database、Repository、线程和LiveData建立单一事实源”解释组件与数据边界，运行正常、重建和失败实验，并让另一位开发者仅凭Room模式、DAO测试、线程检查、schema导出与迁移证据重放结论。

<Exercises>

**问题 1**：“第11章 Databases and the Room Library”覆盖哪些正式节点和项目主线？
<Answer>覆盖10个节点，从“11. Databases and the Room Library”到“For the More Curious: Singletons”。主线是从Entity、DAO、Database、Repository、线程和LiveData建立单一事实源，交付物是Room模式、DAO测试、线程检查、schema导出与迁移证据。</Answer>

**问题 2**：怎样建立本页最小可执行实验？
<Answer>固定JDK、SDK、Gradle、设备API、语言、权限和输入，完成一个从用户动作到可观察结果的垂直切片；保存干净构建、设备操作、原始日志、状态快照与断言。</Answer>

**问题 3**：为什么只在正常点击路径运行不能证明完成？
<Answer>它没有覆盖组件重建、进程死亡、异步取消、权限拒绝、外部输入失败、资源释放和版本变化。应逐项注入故障，并用Room模式、DAO测试、线程检查、schema导出与迁移证据核对。</Answer>

**问题 4**：怎样设计能推翻当前实现的反例？
<Answer>保持其他变量不变，分别注入旋转、后台回收、进程死亡、拒权、断网、无效输入与重复操作；任何状态丢失、越权、崩溃、泄漏或重复副作用都推翻完成结论。</Answer>

**问题 5**：从第4版语境迁移到现代目标SDK时如何控制变量？
<Answer>先按原书还原机制并锁定基线，再查询目标SDK合同；一次只迁移组件导出、存储、权限、后台、通知或依赖之一，保存构建、行为、测试和回滚差异。</Answer>

**问题 6**：本页达到独立交接标准需要什么？
<Answer>需要版本与设备指纹、源码提交、输入、构建命令、生命周期轨迹、原始日志、失败测试、安全边界与发布产物，让他人凭Room模式、DAO测试、线程检查、schema导出与迁移证据重放。</Answer>

</Exercises>

<Glossary>
  <GlossaryItem term="生命周期所有者">负责创建、取消并最终释放组件、任务或资源的明确作用域。</GlossaryItem>
  <GlossaryItem term="可恢复状态">在配置变化或进程重建后，可从保存事实重新构造的最小用户状态。</GlossaryItem>
  <GlossaryItem term="外部合同">Intent、文件、数据库、网络、系统服务或其他应用与本组件交换数据时的可验证约束。</GlossaryItem>
  <GlossaryItem term="版本边界">Android版本、targetSdk、设备形态、权限与依赖共同限定的行为适用范围。</GlossaryItem>
  <GlossaryItem term="证据链">由环境指纹、输入、操作、日志、状态快照和断言组成的可重放结论。</GlossaryItem>
</Glossary>

[← 上一页：第10章 Creating User Interfaces with Layouts and Widgets](/learn/big-nerd-ranch-guide/ui-fragment/layouts-widgets) · [下一页：第12章 Fragment Navigation →](/learn/big-nerd-ranch-guide/ui-fragment/fragment-navigation)

<Attribution adaptedFrom="Android Programming: The Big Nerd Ranch Guide, 4th Edition" adaptedUrl="https://www.oreilly.com/library/view/android-programming-the/9780135257555/" />
