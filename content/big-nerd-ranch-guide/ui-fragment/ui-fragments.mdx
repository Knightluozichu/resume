---
title: UI fragment与fragment管理器
type: C
section: UI与Fragment
order: 8
description: 掌握 Fragment 的生命周期、FragmentManager 的事务操作、以及 Fragment 与 Activity 的通信模式。
demo: true
math: false
sourceUrl: "https://www.oreilly.com/library/view/android-programming-the/9780135257555/"
draft: false
---

import { BnrLifecycleLab, BnrFailureLab, BnrEvidenceLab } from "@/components/mdx/big-nerd-ranch-guide/diagrams/ui-fragments";

{/* C 实战型：Fragment 核心概念与代码实现。
   重心在概念讲解(§3)和代码对照(§6)，常见误区 ≥3 个，练习含实现题。 */}

{/* 知识点清单（动笔前摊开，术语首现用 <Term> 高亮，章末 <Glossary> 一一对应）：
   1) Fragment 是什么、为什么存在 = Stepper 示意图（Activity 内嵌 Fragment 的布局层次）
   2) Fragment 生命周期 vs Activity 生命周期 = 生命周期对比图（并行时间线）
   3) FragmentManager & FragmentTransaction = 代码对照（add/replace/remove 的 Kotlin 示例）
   4) Fragment.onCreateView 与布局绑定 = 代码对照（layout inflater 用法）
   5) Fragment 参数传递（Arguments Bundle）= 代码对照（newInstance 工厂模式）
   6) Fragment 与 Activity 通信模式 = 代码对照（接口回调 vs 共享 ViewModel） */}

{/* 1. 学习目标（spec §一）：C 型必含「能改出 ___ 效果」；末条检验问题。 */}

<Objectives>

- 能解释“第8章 UI Fragments and the Fragment Manager”全部正式节点的组件所有者、生命周期、线程、状态来源与Android版本边界。
- 能实现“建立Activity宿主、FragmentManager、事务及Fragment视图生命周期的所有权图”的最小垂直切片，并保存构建、设备操作、原始日志和断言。
- 能比较正常创建、旋转、后台、进程重建、权限拒绝与外部故障下的行为差异。
- 能设计推翻当前实现的反例，并凭CriminalIntent骨架、Fragment事务轨迹、重建与泄漏测试完成独立交接。

</Objectives>

{/* 2. 直觉引入（spec §二）：≤3 段 × ≤120 字；零术语。 */}

## 为什么需要 Fragment？

想象你在写一个平板应用：左边是列表，右边是详情。如果只有 Activity 这一个"页面"单位，你只能把列表和详情硬塞进同一个布局里——代码会像千层饼一样越堆越厚，想复用其中一部分到别的页面？基本不可能。

Fragment 就是来解决这个痛点的：它把 UI 拆成一个个可以**灵活组装、独立管理**的"积木块"。一个 Activity 可以容纳多个 Fragment，Fragment 自己也可以被不同的 Activity 复用。平板上的列表+详情布局，用两个 Fragment 各管各的，代码清爽，复用也轻松。

没有 Fragment 会怎样？大屏适配会变成噩梦——你要么为每种屏幕尺寸写一套 Activity，要么在一个 Activity 里写满 `if (isTablet)`。Fragment 让 UI 模块化成为可能。

{/* 3. 概念讲解（spec §三）：一节一概念；术语首现 <Term def> 高亮且进章末 Glossary。 */}

## Fragment 是怎么工作的

### Fragment 是什么

一个 Fragment 就是一个可复用的 UI 片段，它有自己的布局、自己的生命周期、自己的事件处理逻辑，但它**必须寄生在一个宿主 Activity 里面**才能显示。

Fragment 和 Activity 的关系可以类比为"乐高积木"和"底板"：<Term def="Activity 是 Android 里代表一个屏幕页面的组件，Fragment 必须附着在 Activity 上才能显示。">Activity</Term> 是底板，<Term def="Fragment 是 Android 里一个可复用的 UI 片段，必须由 Activity 承载，拥有自己的生命周期和布局。">Fragment</Term> 是拼在上面的积木块。底板负责在屏幕上占一个位置，积木块负责填充具体内容。

### Fragment 的生命周期

Fragment 有自己的生命周期，但它的生命周期**被宿主 Activity 的生命周期牵着走**。Activity 暂停，里面的 Fragment 也会暂停；Activity 销毁，Fragment 也会跟着销毁。

下面是 Fragment 生命周期中最常打交道的方法：

| 方法 | 触发时机 | 典型的活 |
|------|----------|---------|
| `onAttach()` | Fragment 被绑定到 Activity | 获取宿主引用 |
| `onCreate()` | Fragment 被创建 | 初始化非 UI 数据 |
| `onCreateView()` | Fragment 即将渲染布局 | **inflate 布局文件** |
| `onViewCreated()` | 布局创建完毕后 | findViewById、设置监听器 |
| `onStart()` | Fragment 即将可见 | 注册观察者 |
| `onResume()` | Fragment 获得焦点 | 开始动画、相机预览 |
| `onPause()` | Fragment 失去焦点 | 暂停动画、释放相机 |
| `onStop()` | Fragment 不可见 | 注销观察者 |
| `onDestroyView()` | Fragment 布局被销毁 | 清理 View 引用 |
| `onDestroy()` | Fragment 被销毁 | 释放所有资源 |
| `onDetach()` | Fragment 与 Activity 解绑 | 取消宿主引用 |

**核心记忆**：`onCreateView()` 创建 View，`onViewCreated()` 操作 View，`onDestroyView()` 清理 View。

### FragmentManager：谁来管 Fragment

你不需要手动管理 Fragment 的生死——交给 FragmentManager。它是一个"Fragment 管家"，负责：

- 把 Fragment 加入 Activity（`add()`）
- 把 Fragment 换成另一个（`replace()`）
- 把 Fragment 从屏幕上去掉（`remove()`）
- 管理回退栈（按返回键回到上一个 Fragment）

所有这些操作都通过 FragmentTransaction 执行，而且遵循"打包-提交"模式：把想做的操作一条条写进事务，最后 `commit()` 一次性执行。

{/* 5. 交互 Demo：Stepper 分步展示 Fragment 添加到 Activity 的完整流程 */}

<Stepper>
  <Step title="① newInstance 创建 Fragment">
    通过工厂方法 `newInstance(crimeId)` 创建 Fragment 实例，将参数封装进 `arguments` Bundle——确保系统重建 Fragment 时参数不丢失。调用方只需传业务参数，不用关心 Bundle 内部细节。
  </Step>
  <Step title="② FragmentManager.beginTransaction 开启事务">
    拿到 `supportFragmentManager` 后调用 `beginTransaction()`，开启一个 Fragment 事务——后续所有 add/replace/remove 操作都在这个事务里排队，最后一次性提交。事务保证操作的原子性：要么全做，要么全不做。
  </Step>
  <Step title="③ add 或 replace 放入容器">
    `add(R.id.fragment_container, fragment)` 把 Fragment 加到 Activity 布局预留的 `FrameLayout` 插槽中——首次添加用 add；`replace()` 则是先移除容器内已有 Fragment，再放入新 Fragment，相当于"换一块积木"。
  </Step>
  <Step title="④ commit 提交事务">
    调用 `commit()` 将事务提交给主线程排队执行。如果需要支持返回键回退，在 commit 前调用 `addToBackStack(null)`——把它压入回退栈，按返回键时 FragmentManager 自动逆向操作回到上一个 Fragment。
  </Step>
</Stepper>

{/* 6. 代码对照（spec §六，C 型重心）：逐段讲解 ≥80% 关键代码。 */}

## 代码逐段拆解

### 第一步：创建 Fragment 类

```kotlin
import androidx.fragment.app.Fragment

class CrimeFragment : Fragment(R.layout.fragment_crime) {
    // 构造器里直接传布局 ID，省掉重写 onCreateView
}
```

使用 `Fragment(R.layout.fragment_crime)` 这个构造函数是 Fragment 1.1.0 引入的便捷方式。它内部自动帮你做了 `LayoutInflater.inflate()` 的事情。等价于传统写法：

```kotlin
class CrimeFragment : Fragment() {
    override fun onCreateView(
        inflater: LayoutInflater,
        container: ViewGroup?,
        savedInstanceState: Bundle?
    ): View? {
        return inflater.inflate(R.layout.fragment_crime, container, false)
    }
}
```

<Callout type="warn">
  注意 `inflate()` 第三个参数必须传 `false`——意思是"不要自动把 inflate 出来的 View 加到 container 里"。FragmentManager 会自己决定什么时候加，你代劳的话会报 `IllegalStateException`。
</Callout>

### 第二步：用 FragmentManager 把 Fragment 放到 Activity 里

```kotlin
class MainActivity : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_main)

        val fragmentManager = supportFragmentManager
        val fragmentTransaction = fragmentManager.beginTransaction()

        fragmentTransaction
            .add(R.id.fragment_container, CrimeFragment())
            .commit()
    }
}
```

`R.id.fragment_container` 是你在 Activity 布局里预留的"Fragment 插槽"——通常是一个 `FrameLayout`。

{/* FragmentManager 事务可视化：add → replace(addToBackStack) → popBackStack 一次完整往返 */}

<FragmentTransactionDiagram />

### 第三步：用 Arguments 传参数给 Fragment

Fragment 的构造函数不能带参数（系统重建 Fragment 时会用无参构造器）。正确的传参方式是用 `Bundle`，这叫做 **Fragment Arguments 模式**：

```kotlin
class CrimeFragment : Fragment() {

    companion object {
        private const val ARG_CRIME_ID = "crime_id"

        fun newInstance(crimeId: UUID): CrimeFragment {
            val args = Bundle().apply {
                putSerializable(ARG_CRIME_ID, crimeId)
            }
            return CrimeFragment().apply {
                arguments = args
            }
        }
    }

    private val crimeId: UUID by lazy {
        arguments?.getSerializable(ARG_CRIME_ID) as UUID
    }

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        // arguments 在 onCreate 里已经可用
    }
}
```

`newInstance()` 是一个工厂方法——把传参逻辑封装进去，外部调用者只需 `CrimeFragment.newInstance(someId)`，不用关心内部怎么传的 Bundle。

### 第四步：Fragment 与 Activity 通信

**方案一：接口回调（经典方式）**

```kotlin
class CrimeFragment : Fragment() {
    interface Callbacks {
        fun onCrimeSelected(crimeId: UUID)
    }

    private var callbacks: Callbacks? = null

    override fun onAttach(context: Context) {
        super.onAttach(context)
        callbacks = context as? Callbacks
    }

    override fun onDetach() {
        super.onDetach()
        callbacks = null
    }
}
```

**方案二：共享 ViewModel（现代推荐方式）**

```kotlin
class CrimeFragment : Fragment() {
    private val sharedViewModel: CrimeViewModel by activityViewModels()
}
```

`by activityViewModels()` 拿到的 ViewModel 跟宿主 Activity 是同一个实例，所以同一 Activity 里的所有 Fragment 天然共享同一个 ViewModel——这是最干净的数据共享方案。

{/* 7. 常见误区（spec §七，C 型 ≥3 个）。 */}

## 容易踩的坑

<Callout type="trap">
  **现象 →** 旋转屏幕后 Fragment 上的数据显示了旧内容。**原因 →** Activity 重建时默认重建 Fragment，你没处理 Arguments 或 ViewModel 的保留。**修法 →** 用 ViewModel 跨重建保留数据；Arguments Bundle 会自动保留，标准参数用 Arguments 传即可。
</Callout>

<Callout type="trap">
  **现象 →** `FragmentTransaction.commit()` 后界面没变，还报了 `IllegalStateException`。**原因 →** 你在 `onCreate()` 或 `onStart()` 里提交事务，但此时 Activity 还没恢复完，状态不能丢。**修法 →** 改为 `commitAllowingStateLoss()`（风险——丢失状态），或者把事务推迟到 `onResumeFragments()` 里执行。
</Callout>

<Callout type="trap">
  **现象 →** `findViewById()` 在 Fragment 里返回 null。**原因 →** 你在 `onCreateView()` 里调了 `findViewById()`，但 View 还没创建完毕。**修法 →** 把 View 操作移到 `onViewCreated()` 里——那时 `view` 已经是个可用的 View 对象了。
</Callout>

{/* 8. 小结与练习（spec §八）：小结呼应学习目标，C 型含独立实现题。 */}

## 小结

- Fragment 是可复用的 UI 模块，寄生在 Activity 里，有自己独立但受控的生命周期
- FragmentManager + FragmentTransaction 是操作 Fragment 的唯一入口（add/replace/remove）
- 参数传递用 Arguments Bundle + newInstance 工厂模式，不要用自定义构造器
- Fragment 与 Activity 通信推荐用共享 ViewModel；经典回调也可行但耦合度高

<Exercises>

**问题 1**：“第8章 UI Fragments and the Fragment Manager”覆盖哪些正式节点和项目主线？
<Answer>覆盖8个节点，从“8. UI Fragments and the Fragment Manager”到“Application Architecture with Fragments”。主线是建立Activity宿主、FragmentManager、事务及Fragment视图生命周期的所有权图，交付物是CriminalIntent骨架、Fragment事务轨迹、重建与泄漏测试。</Answer>

**问题 2**：怎样建立本页最小可执行实验？
<Answer>固定JDK、SDK、Gradle、设备API、语言、权限和输入，完成一个从用户动作到可观察结果的垂直切片；保存干净构建、设备操作、原始日志、状态快照与断言。</Answer>

**问题 3**：为什么只在正常点击路径运行不能证明完成？
<Answer>它没有覆盖组件重建、进程死亡、异步取消、权限拒绝、外部输入失败、资源释放和版本变化。应逐项注入故障，并用CriminalIntent骨架、Fragment事务轨迹、重建与泄漏测试核对。</Answer>

**问题 4**：怎样设计能推翻当前实现的反例？
<Answer>保持其他变量不变，分别注入旋转、后台回收、进程死亡、拒权、断网、无效输入与重复操作；任何状态丢失、越权、崩溃、泄漏或重复副作用都推翻完成结论。</Answer>

**问题 5**：从第4版语境迁移到现代目标SDK时如何控制变量？
<Answer>先按原书还原机制并锁定基线，再查询目标SDK合同；一次只迁移组件导出、存储、权限、后台、通知或依赖之一，保存构建、行为、测试和回滚差异。</Answer>

**问题 6**：本页达到独立交接标准需要什么？
<Answer>需要版本与设备指纹、源码提交、输入、构建命令、生命周期轨迹、原始日志、失败测试、安全边界与发布产物，让他人凭CriminalIntent骨架、Fragment事务轨迹、重建与泄漏测试重放。</Answer>

</Exercises>

{/* 10. 名词解释（spec §十）：与正文 Term 高亮一一对应。 */}

## 名词解释

<Glossary>
  <GlossaryItem term="Activity">Android 里代表一个全屏页面的组件。像一个舞台，Fragment 是上面的演员——舞台提供场地，演员负责表演。</GlossaryItem>
  <GlossaryItem term="Fragment">寄生在 Activity 里的可复用 UI 模块。有自己的布局、生命周期和逻辑，但不能独立显示——必须有一个宿主 Activity。</GlossaryItem>
  <GlossaryItem term="FragmentManager">Fragment 的管家。负责把 Fragment 往 Activity 里加、换、删，管理回退栈。通过 `supportFragmentManager` 获取。</GlossaryItem>
  <GlossaryItem term="FragmentTransaction">FragmentManager 执行操作时的一个"打包事务"。把要做的 add/replace/remove 等操作写到事务里，最后 commit 一次性提交。像银行转账——要么全做，要么全不做。</GlossaryItem>
  <GlossaryItem term="Bundle">Android 里的"键值对背包"，用于在组件间传递数据。Fragment 用 Bundle（Arguments）接收参数，因为构造器不能带参数。</GlossaryItem>
</Glossary>

{/* 9. 出处声明（spec §九）。 */}

<Attribution />


{/* BNR4_AUTHORITY_SUPPLEMENT */}

## 从生命周期、状态与证据开始

第4版不是API词典，而是六个逐步扩展的应用实验。每一章都要回答：用户从哪里进入，哪个Android组件拥有工作，回调在哪个线程发生，哪些事实必须跨旋转或进程死亡恢复，外部系统怎样失败，资源在何时释放。<Term def="负责创建、取消并最终释放组件、任务或资源的明确作用域">生命周期所有者</Term>把Activity、Fragment视图、ViewModel、Worker、线程与资源的寿命连接起来；没有所有者的异步任务就是潜在泄漏。

界面字段不是天然事实。<Term def="在配置变化或进程重建后，可从保存事实重新构造的最小用户状态">可恢复状态</Term>应根据数据大小与寿命放入saved instance state、ViewModel、数据库、文件或调度器，而不是全部塞进单例。Intent、文件、数据库、网络、系统服务和其他应用形成<Term def="跨边界交换数据时必须验证的action、类型、权限、大小、时序和失败约束">外部合同</Term>；正常响应只是合同中的一个样本。

本书以2019年第四版、Android 5.0至8.1及其后续兼容实践为语境。<Term def="Android版本、targetSdk、设备形态、权限与依赖共同限定的行为适用范围">版本边界</Term>要求先还原书中机制，再查询现代目标SDK的权限、后台、存储、通知和组件导出合同。<Term def="由环境指纹、输入、操作、日志、状态快照和断言组成的可重放结论">证据链</Term>则保证“在我的模拟器能跑”不会冒充可交付结论。

## 第4版正式目录逐节点映射

### 8. UI Fragments and the Fragment Manager

**正式节点 1/8。** 先说明“8. UI Fragments and the Fragment Manager”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“建立Activity宿主、FragmentManager、事务及Fragment视图生命周期的所有权图”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### The Need for UI Flexibility

**正式节点 2/8。** 先说明“The Need for UI Flexibility”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“建立Activity宿主、FragmentManager、事务及Fragment视图生命周期的所有权图”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Introducing Fragments

**正式节点 3/8。** 先说明“Introducing Fragments”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“建立Activity宿主、FragmentManager、事务及Fragment视图生命周期的所有权图”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Starting CriminalIntent

**正式节点 4/8。** 先说明“Starting CriminalIntent”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“建立Activity宿主、FragmentManager、事务及Fragment视图生命周期的所有权图”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Creating a Data Class

**正式节点 5/8。** 先说明“Creating a Data Class”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“建立Activity宿主、FragmentManager、事务及Fragment视图生命周期的所有权图”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Creating a UI Fragment

**正式节点 6/8。** 先说明“Creating a UI Fragment”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“建立Activity宿主、FragmentManager、事务及Fragment视图生命周期的所有权图”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Hosting a UI Fragment

**正式节点 7/8。** 先说明“Hosting a UI Fragment”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“建立Activity宿主、FragmentManager、事务及Fragment视图生命周期的所有权图”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

### Application Architecture with Fragments

**正式节点 8/8。** 先说明“Application Architecture with Fragments”解决的用户任务与平台合同，再标出组件所有者、回调线程、可恢复状态、外部输入和销毁点。实现必须服务于“建立Activity宿主、FragmentManager、事务及Fragment视图生命周期的所有权图”，并留下可在模拟器或真机重放的输入、操作、日志与断言。

先预测旋转、后台、进程重建、权限拒绝、断网或目标SDK变化时会怎样，再只改变一个条件。若只能在正常点击路径运行，尚未掌握该节点。

## 最小可执行切片

先把状态转换与Android副作用分开。下面的Kotlin骨架不替代本章代码，而是让旋转、进程恢复与失败输入可单测：

```kotlin
sealed interface ScreenState {
    data object Loading : ScreenState
    data class Ready(val key: String) : ScreenState
    data class Failed(val reason: String, val retryable: Boolean) : ScreenState
}

fun reduce(current: ScreenState, event: UiEvent): ScreenState = nextState(current, event)
```

固定JDK、SDK、Gradle、设备API、语言和输入，清理缓存后从命令行保存制品与日志：

```bash
./gradlew --no-daemon clean test assembleDebug
adb install -r app/build/outputs/apk/debug/app-debug.apk
adb shell am force-stop com.example.app
adb shell monkey -p com.example.app 1
adb logcat -d -v threadtime > run.log
```

先写行为断言，再执行用户操作和故障注入：

```gherkin
Given a pinned build, device API, locale, permission state, and input
When the owner is recreated after state saving or process death
Then durable facts reconstruct the same user-visible state
And cancelled work cannot update a destroyed owner
And denied permission, offline input, and malformed data remain recoverable
```

三段代码分别约束状态模型、环境重放和行为结果。真实项目还要加入静态检查、单元测试、仪器测试、无障碍检查和发布构建；日志只能支持断言，不能代替断言。

## 线程、状态与外部资源

主线程负责输入、生命周期分发和大部分UI工作，磁盘、网络、图片解码与长计算不能阻塞帧。后台结果返回前必须确认所有者仍有效；Fragment的生命周期与其View生命周期不是一回事。每个实现都应列出入口、线程、事实源、取消点、关闭责任和用户可见失败六项。

跨应用Intent、ContentProvider、WebView、网络响应、文件URI和深链都视为不可信输入。校验action、scheme、MIME、大小、授权与响应者；导出组件使用最小权限；日志、截图和测试数据库不得泄露令牌或个人数据。相机、音频、数据库、线程和回调注册必须在成功、异常与取消路径都释放。

性能以用户任务衡量：启动看可交互时刻，列表看掉帧与绑定分配，网络看超时和取消，后台工作看约束、电量与重复调度。每次比较固定设备温度、构建类型、数据规模和网络条件，先采基线再改变一个变量。

迁移现代目标SDK时，先在第4版语境中证明机制，再逐项处理组件导出、存储、通知、后台启动、权限、废弃API和依赖版本。每一步保存编译差异、行为差异、测试结果和回滚点；新API并不自动正确，旧API能编译也不代表仍符合平台政策。

## 三步验证实验

<Steps>
  <Step title="1. 建立目录节点与生命周期图">
    从“建立Activity宿主、FragmentManager、事务及Fragment视图生命周期的所有权图”选一个用户任务，标出入口、组件、所有者、回调线程、可恢复状态和销毁点。先预测旋转、后台和进程恢复后哪些对象消失、哪些事实必须重建。
    <BnrLifecycleLab />
  </Step>
  <Step title="2. 注入生命周期与外部故障">
    依次改变生命周期阶段、网络和权限，每次只改变一个条件；记录原始日志、界面状态、持久事实与资源计数，区分预期失败和未知状态。
    <BnrFailureLab />
  </Step>
  <Step title="3. 通过交付证据门">
    用CriminalIntent骨架、Fragment事务轨迹、重建与泄漏测试核对版本设备、生命周期、持久状态、失败安全、测试和发布。缺少任何一项时，不得把正常点击路径视为完成。
    <BnrEvidenceLab />
  </Step>
</Steps>

<Callout type="trap">**核心陷阱。** 只复述类名与回调，未说明所有者、状态来源、线程、取消和失败后果。修复时回到用户任务和状态机，注入旋转、进程死亡、拒权、断网与无效输入。</Callout>

<Callout type="trap">**历史示例不等于当前平台政策。** 第4版准确反映出版时工具链；现代targetSdk可能改变组件导出、后台、存储、通知和权限。保留原书机制，再建立有证据的迁移层。</Callout>

<Callout type="warning">**真机与数据安全。** 使用测试账号、样本媒体和最小权限；清理日志、数据库与截图。签名密钥、API密钥、联系人和真实位置不得写入仓库或公开证据。</Callout>

## 本章回顾

本页从“8. UI Fragments and the Fragment Manager”覆盖到“Application Architecture with Fragments”，共8个正式目录节点。掌握标准不是记住API，而是能沿“建立Activity宿主、FragmentManager、事务及Fragment视图生命周期的所有权图”解释组件与数据边界，运行正常、重建和失败实验，并让另一位开发者仅凭CriminalIntent骨架、Fragment事务轨迹、重建与泄漏测试重放结论。

<Exercises>

**问题 1**：“第8章 UI Fragments and the Fragment Manager”覆盖哪些正式节点和项目主线？
<Answer>覆盖8个节点，从“8. UI Fragments and the Fragment Manager”到“Application Architecture with Fragments”。主线是建立Activity宿主、FragmentManager、事务及Fragment视图生命周期的所有权图，交付物是CriminalIntent骨架、Fragment事务轨迹、重建与泄漏测试。</Answer>

**问题 2**：怎样建立本页最小可执行实验？
<Answer>固定JDK、SDK、Gradle、设备API、语言、权限和输入，完成一个从用户动作到可观察结果的垂直切片；保存干净构建、设备操作、原始日志、状态快照与断言。</Answer>

**问题 3**：为什么只在正常点击路径运行不能证明完成？
<Answer>它没有覆盖组件重建、进程死亡、异步取消、权限拒绝、外部输入失败、资源释放和版本变化。应逐项注入故障，并用CriminalIntent骨架、Fragment事务轨迹、重建与泄漏测试核对。</Answer>

**问题 4**：怎样设计能推翻当前实现的反例？
<Answer>保持其他变量不变，分别注入旋转、后台回收、进程死亡、拒权、断网、无效输入与重复操作；任何状态丢失、越权、崩溃、泄漏或重复副作用都推翻完成结论。</Answer>

**问题 5**：从第4版语境迁移到现代目标SDK时如何控制变量？
<Answer>先按原书还原机制并锁定基线，再查询目标SDK合同；一次只迁移组件导出、存储、权限、后台、通知或依赖之一，保存构建、行为、测试和回滚差异。</Answer>

**问题 6**：本页达到独立交接标准需要什么？
<Answer>需要版本与设备指纹、源码提交、输入、构建命令、生命周期轨迹、原始日志、失败测试、安全边界与发布产物，让他人凭CriminalIntent骨架、Fragment事务轨迹、重建与泄漏测试重放。</Answer>

</Exercises>

<Glossary>
  <GlossaryItem term="生命周期所有者">负责创建、取消并最终释放组件、任务或资源的明确作用域。</GlossaryItem>
  <GlossaryItem term="可恢复状态">在配置变化或进程重建后，可从保存事实重新构造的最小用户状态。</GlossaryItem>
  <GlossaryItem term="外部合同">Intent、文件、数据库、网络、系统服务或其他应用与本组件交换数据时的可验证约束。</GlossaryItem>
  <GlossaryItem term="版本边界">Android版本、targetSdk、设备形态、权限与依赖共同限定的行为适用范围。</GlossaryItem>
  <GlossaryItem term="证据链">由环境指纹、输入、操作、日志、状态快照和断言组成的可重放结论。</GlossaryItem>
</Glossary>

[← 上一页：第7章 Android SDK Versions and Compatibility](/learn/big-nerd-ranch-guide/basics/sdk-compatibility) · [下一页：第9章 Displaying Lists with RecyclerView →](/learn/big-nerd-ranch-guide/ui-fragment/recyclerview)

<Attribution adaptedFrom="Android Programming: The Big Nerd Ranch Guide, 4th Edition" adaptedUrl="https://www.oreilly.com/library/view/android-programming-the/9780135257555/" />
