---
title: "第 9 章 Painting Textures：从 Base Texture 到角色细节"
type: A
section: "玩转 Blender 第 3 版 · UV、纹理与材质"
order: 9
description: "覆盖主要纹理流程、Blender Texture Paint、Base Texture、纹理元素、外部软件绘制和 Blender 中回看。"
demo: true
math: false
sourceUrl: "https://www.pearson.com/en-gb/subject-catalog/p/learning-blender/P200000000666/9780136411758"
draft: true
---

import {
  Objectives,
  Callout,
  Attribution,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
} from "@/components/mdx/mdx-components";
import {
  Bl309PaintingTexturesMapLab,
  Bl309PaintingTexturesExperimentLab,
  Bl309PaintingTexturesEvidenceLab,
} from "@/components/mdx/blender-3d/diagrams/bl3-09-painting-textures";

<Objectives>

- 能设计可保存且可追溯的纹理绘制槽

- 能实现从基调、材质分区到角色细节的分层纹理

- 能比较 Blender 与外部绘画软件之间的色彩和 UV 结果

</Objectives>

## 问题：为什么视口里已经画完，关闭文件后纹理仍可能丢失

先预测：在 Texture Paint 中画出颜色但没有保存 Image Datablock，保存 .blend 是否保证像素写入磁盘？不保证。第九章把绘画拆成 UV、Image、Material Slot、Paint Slot、笔刷和外部文件链，先建立 Base Texture，再添加面部、服装与配件细节，并在 Blender 中回看。

<Bl309PaintingTexturesMapLab />

## 原书边界与官方小节

本页只覆盖 Pearson 第三版目录中的以下原始单元，不用现代功能清单替换作者的角色项目顺序：

- **Defining the Main Workflow**
- **Texture Painting in Blender**
- **Creating the Base Texture**
- **Understanding the Elements of a Texture**
- **Texturing in Other Software；Seeing the Painted Character in Blender**

这些小节按官方目录唯一归属。本站重新组织解释、实验和证据，不逐字复制原文；导读、总复习和现代迁移不重复计算原章覆盖。

## Image Datablock、磁盘文件与材质引用

Image Datablock 是 Blender 内存中的图像对象，Material Node 引用它，Texture Paint 写入其像素；磁盘文件是独立持久化载体。Save .blend、Save Image 和 Pack Resources 是不同动作。Base Texture 先分配皮肤、布料、金属等大色块，再用明度、色相、粗糙变化和磨损表达材质层级；细节尺度必须匹配最终镜头。

<Bl309PaintingTexturesExperimentLab />

## 五个核心术语与责任边界

<Term def="材质中被 Texture Paint 作为目标的图像通道。">Paint Slot</Term>
必须落到本章资产链：输入来自哪里、由谁修改、输出供谁消费，以及失败时哪个信号最先偏离。

<Term def="Blender 内存中保存尺寸、色彩空间和像素引用的图像数据。">Image Datablock</Term>
必须落到本章资产链：输入来自哪里、由谁修改、输出供谁消费，以及失败时哪个信号最先偏离。

<Term def="在细节前建立材质分区和主色关系的基础纹理。">Base Texture</Term>
必须落到本章资产链：输入来自哪里、由谁修改、输出供谁消费，以及失败时哪个信号最先偏离。

<Term def="规定图像数值如何解释为颜色或线性数据的设置。">Color Space</Term>
必须落到本章资产链：输入来自哪里、由谁修改、输出供谁消费，以及失败时哪个信号最先偏离。

<Term def="嵌入 .blend、减少外部路径丢失但增加文件体积的资源。">Packed Resource</Term>
必须落到本章资产链：输入来自哪里、由谁修改、输出供谁消费，以及失败时哪个信号最先偏离。

术语若不能落到具体 .blend、Datablock、图像、骨骼、Action、Track 或 Render 上，就仍停留在记忆层。读者应能写出正常值、边界值和故意失败时的观测差异。

## 关键对象与数据流

为 UV 创建固定分辨率图像并立即命名、保存，连接到材质 Base Color 并设为 Paint Slot。先填材质大区，再在独立外部图层或版本文件添加色差、接缝、污渍和角色特征。每次绘制后执行 Save Image，重新打开 .blend 验证路径；导入外部修改时检查色彩空间、Alpha 和 UV 对齐。

每次修改前先保存输入版本，修改后记录输出文件、统计和截图。最终画面正确但中间资产不符合预测，也不能立即签发，因为它可能依赖隐藏偏好、未保存图像、错误链接或偶然缓存。

## 三步可重复实验

动手前先预测：只改变当前步骤的一项输入后，哪个对象、拓扑、像素、骨骼、轨迹或渲染结果应最先变化？实验期间固定 Blender 版本、插件、资产版本、相机和输出设置。

<Stepper>
  <Step title="建立绘画目标">
    <Bl309PaintingTexturesMapLab />

创建和保存命名图像，连接材质节点并选中正确 Paint Slot，先画 UV 校验标记。

```python
img = bpy.data.images.get('Jim_BaseColor')
print(img.filepath, img.size[:], img.is_dirty)
```

  </Step>
  <Step title="从基调到细节">
    <Bl309PaintingTexturesExperimentLab />

先分材质大色块和明度，再按角色设计添加面部、服装、配件和适量磨损。

```text
UV -> image -> paint slot -> pixels -> save image -> material -> viewport proof
```

  </Step>
  <Step title="验证外部往返">
    <Bl309PaintingTexturesEvidenceLab />

导出 UV、在外部软件分层编辑、回 Blender 重载，检查色彩空间、Alpha、路径和未保存状态。

```python
for image in bpy.data.images:
    if image.is_dirty: print('UNSAVED', image.name)
```

  </Step>
</Stepper>

## 正常、边界与失败样本

本章至少保存一个正常样本、一个极端边界和一个故意破坏配置。正常样本证明主路径，边界样本说明适用范围，失败样本证明诊断方法确实能抓到错误。

<Callout type="trap">
  **因果陷阱**：保存 .blend 不等于保存所有外部 Image
  像素，未保存图像可能在重开后丢失。
</Callout>

<Callout type="trap">
  **版本陷阱**：把颜色贴图和粗糙度/遮罩都设为同一 sRGB
  解释，会让非颜色数据数值失真。
</Callout>

## 从 Blender 2.83 LTS 到当前版本

Texture Paint 界面和画笔系统会变化，但 Image Datablock、Paint Slot、磁盘持久化、色彩空间和材质引用链稳定。现代外部 PBR 工具可补充流程，本章仍需证明 Base Texture 和 Blender 回看。

迁移账本采用五列：原书载体、稳定不变量、当前实现、不可等价处、证据。允许菜单和工具变化，不允许抹去原始问题、项目依赖或章节身份。

## 验收证据

验收包含图像名称、尺寸、色彩空间、文件路径、Paint Slot、Base Texture、细节版本、外部往返对照、未保存图像检查和重开文件结果。目标镜头尺寸下不应出现无意义高频细节。

证据包同时记录文件路径、Datablock 名称、版本、参数、复现步骤、预期和实际结果。交给另一位读者后不需要猜隐藏状态即可重放，才算掌握而不只是看过。

## 小结

- 纹理绘制目标是 Image Datablock 与材质槽的组合
- 保存项目和保存图像是不同操作
- Base Texture 先建立材质和明度层级
- 外部往返必须验证路径、色彩空间和 UV

<Exercises>

**问题 1** 哪些图通常用 Non-Color？

<Answer>粗糙度、金属度、法线、遮罩等数值数据。</Answer>

**问题 2** Packed Resource 的代价？

<Answer>降低外部路径丢失风险，但增加 .blend 体积并可能隐藏源文件更新。</Answer>

**问题 3** 外部软件回写后最先检查什么？

<Answer>路径重载、分辨率、色彩空间、Alpha 和 UV 对齐。</Answer>

</Exercises>

<Glossary>
  <GlossaryItem term="Paint Slot">
    材质中被 Texture Paint 作为目标的图像通道。
  </GlossaryItem>
  <GlossaryItem term="Image Datablock">
    Blender 内存中保存尺寸、色彩空间和像素引用的图像数据。
  </GlossaryItem>
  <GlossaryItem term="Base Texture">
    在细节前建立材质分区和主色关系的基础纹理。
  </GlossaryItem>
  <GlossaryItem term="Color Space">
    规定图像数值如何解释为颜色或线性数据的设置。
  </GlossaryItem>
  <GlossaryItem term="Packed Resource">
    嵌入 .blend、减少外部路径丢失但增加文件体积的资源。
  </GlossaryItem>
</Glossary>

<Attribution
  adaptedFrom="Oliver Villar《Learning Blender》第 3 版 Pearson 官方目录与中文版身份资料"
  adaptedUrl="https://www.pearson.com/en-gb/subject-catalog/p/learning-blender/P200000000666/9780136411758"
/>
