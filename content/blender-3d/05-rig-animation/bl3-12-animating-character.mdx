---
title: "第 12 章 Animating Your Character：姿态、编辑器与行走循环"
type: A
section: "玩转 Blender 第 3 版 · 绑定与动画"
order: 12
description: "覆盖使用角色 Rig、摆姿态、动画编辑器和 Jim 行走循环。"
demo: true
math: false
sourceUrl: "https://www.pearson.com/en-gb/subject-catalog/p/learning-blender/P200000000666/9780136411758"
draft: true
---

import {
  Objectives,
  Callout,
  Attribution,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能设计剪影清晰且重心正确的关键姿态

- 能区分 Timeline、Dope Sheet、Graph Editor 与 NLA 的职责

- 能实现无跳帧、无脚滑且可循环的 Walk Cycle

</Objectives>

## 问题：为什么左右脚姿态齐全的行走循环仍可能没有重量感

先预测：只画 Contact 左、Contact 右两个对称姿态并线性插值，角色会自然走路吗？不会。行走需要 Contact、Down、Passing、Up 等姿态的重心起伏、髋肩反向、脚掌滚动和时间节奏。第十二章先正确使用 Rig 和动画编辑器，再把 Jim 的行走循环制作成可复用 Action。

## 原书边界与官方小节

本页只覆盖 Pearson 第三版目录中的以下原始单元，不用现代功能清单替换作者的角色项目顺序：

- **Using the Character's Rig**
- **Posing the Character**
- **Working with Animation Editors**
- **Animating a Walk Cycle**

这些小节按官方目录唯一归属。本站重新组织解释、实验和证据，不逐字复制原文；导读、总复习和现代迁移不重复计算原章覆盖。

## 关键姿态、时间间隔与曲线切线

Pose 定义某一帧的空间关系，Timing 决定姿态间隔，Spacing 由 F-Curve 描述帧间位移。Timeline 定位与播放，Dope Sheet/Action Editor 管理关键帧和 Action，Graph Editor 调属性曲线，NLA 组合多个 Action。Walk Cycle 的根和脚接触必须闭合，首尾重复帧只用于播放边界，不能造成双停顿。

## 五个核心术语与责任边界

<Term def="定义动作叙事、重心和轮廓的关键姿态。">Key Pose</Term>
必须落到本章资产链：输入来自哪里、由谁修改、输出供谁消费，以及失败时哪个信号最先偏离。

<Term def="保存一组对象或骨骼动画 F-Curves 的可复用数据块。">Action</Term>
必须落到本章资产链：输入来自哪里、由谁修改、输出供谁消费，以及失败时哪个信号最先偏离。

<Term def="按时间查看和编辑多个通道关键帧的动画编辑器。">Dope Sheet</Term>
必须落到本章资产链：输入来自哪里、由谁修改、输出供谁消费，以及失败时哪个信号最先偏离。

<Term def="描述属性值随帧变化及插值切线的曲线。">F-Curve</Term>
必须落到本章资产链：输入来自哪里、由谁修改、输出供谁消费，以及失败时哪个信号最先偏离。

<Term def="首尾连续、可重复播放的行走动作周期。">Walk Cycle</Term>
必须落到本章资产链：输入来自哪里、由谁修改、输出供谁消费，以及失败时哪个信号最先偏离。

术语若不能落到具体 .blend、Datablock、图像、骨骼、Action、Track 或 Render 上，就仍停留在记忆层。读者应能写出正常值、边界值和故意失败时的观测差异。

## 关键对象与数据流

先在 Reference Pose 上确认控制器归零和旋转模式，依次建立左右 Contact、Down、Passing、Up 姿态，优先脚、髋、躯干再处理手臂和头。用 Dope Sheet 调整体 Timing，用 Graph Editor 清理髋部垂直与脚位移曲线。循环播放时检查首尾速度、脚接触区世界位移和重心轨迹，再把 Action 推入 NLA 做路径或镜头测试。

每次修改前先保存输入版本，修改后记录输出文件、统计和截图。最终画面正确但中间资产不符合预测，也不能立即签发，因为它可能依赖隐藏偏好、未保存图像、错误链接或偶然缓存。

## 正常、边界与失败样本

本章至少保存一个正常样本、一个极端边界和一个故意破坏配置。正常样本证明主路径，边界样本说明适用范围，失败样本证明诊断方法确实能抓到错误。

<Callout type="trap">
  **因果陷阱**：只复制左右姿态而不处理重心起伏和脚掌滚动，会得到机械滑步。
</Callout>

<Callout type="trap">
  **版本陷阱**：循环首帧和末帧都完整保留在播放范围内，会让相同姿态停留两帧。
</Callout>

## 从 Blender 2.83 LTS 到当前版本

Timeline、Dope Sheet、Graph Editor、Action 与 NLA 概念稳定。当前 Blender 的 Pose Library 和 Asset Browser 可复用姿态，但不替代关键姿态、Timing、Spacing 和循环验收；现代库工作流应标记为补充。

迁移账本采用五列：原书载体、稳定不变量、当前实现、不可等价处、证据。允许菜单和工具变化，不允许抹去原始问题、项目依赖或章节身份。

## 验收证据

验收包含八个关键姿态、Dope Sheet 时序、髋/脚 F-Curves、Action 帧范围、循环首尾值与切线、脚滑误差和至少三周期播放。另一场景能复用同一 Action。

证据包同时记录文件路径、Datablock 名称、版本、参数、复现步骤、预期和实际结果。交给另一位读者后不需要猜隐藏状态即可重放，才算掌握而不只是看过。

## 小结

- 动画质量由姿态、Timing 和 Spacing共同决定
- 四类动画编辑器承担不同层级职责
- 行走循环需要完整重心与脚掌阶段
- 循环验收必须量化脚滑和首尾连续性

<Exercises>

**问题 1** 脚滑怎样量化？

<Answer>脚应接触地面的帧区间内测量脚控制器世界位置变化。</Answer>

**问题 2** 为什么末帧可能不纳入循环播放？

<Answer>它与首帧相同，双重采样会产生停顿。</Answer>

**问题 3** NLA 的作用？

<Answer>非破坏组合、重复和混合多个 Actions。</Answer>

</Exercises>

<Glossary>
  <GlossaryItem term="Key Pose">
    定义动作叙事、重心和轮廓的关键姿态。
  </GlossaryItem>
  <GlossaryItem term="Action">
    保存一组对象或骨骼动画 F-Curves 的可复用数据块。
  </GlossaryItem>
  <GlossaryItem term="Dope Sheet">
    按时间查看和编辑多个通道关键帧的动画编辑器。
  </GlossaryItem>
  <GlossaryItem term="F-Curve">
    描述属性值随帧变化及插值切线的曲线。
  </GlossaryItem>
  <GlossaryItem term="Walk Cycle">
    首尾连续、可重复播放的行走动作周期。
  </GlossaryItem>
</Glossary>

<Attribution
  adaptedFrom="Oliver Villar《Learning Blender》第 3 版 Pearson 官方目录与中文版身份资料"
  adaptedUrl="https://www.pearson.com/en-gb/subject-catalog/p/learning-blender/P200000000666/9780136411758"
/>
