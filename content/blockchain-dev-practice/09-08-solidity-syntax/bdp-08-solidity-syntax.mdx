---
title: "第8章 Solidity语法详解"
type: C
section: "区块链开发实战（2018版）"
order: 9
description: "从类型、数据位置、函数、事件、继承、库与异常处理推导合约状态和安全边界。"
demo: true
math: true
sourceUrl: "https://www.dedao.cn/ebook/detail?id=bBVDEXGGLn7eB51b8NjVRqDoQJPMk3aKmq0adYrXmAxE4Ov92lgzK6ZypxLqdQjp"
draft: false
---

import {
  Bdp08SoliditySyntaxFlowLab,
  Bdp08SoliditySyntaxExperimentLab,
  Bdp08SoliditySyntaxEvidenceLab,
} from "@/components/mdx/blockchain-dev-practice/diagrams/bdp-08-solidity-syntax";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释值类型、引用类型、storage各自负责的对象、输入与假设边界
- 能推导状态转移、资源或价值约束、确认条件与证据门禁四组表达式
- 能实现并运行三段最小代码，追踪正常、边界和单故障样本的首个差异
- 能设计独立复核并证明“能为一个含数组、映射、结构体、事件和外部支付的合约标注数据位置、权限、状态变化和失败点，并写出正常与攻击测试。”

</Objectives>

## 为什么从可重放状态开始

Solidity语法最终映射到存储布局、内存生命周期、调用上下文和EVM控制流。类型、可见性、数据位置和调用顺序共同决定资产是否守恒；历史语法必须与编译器版本绑定。

区块链开发最容易产生的错觉，是界面显示成功、命令返回哈希或节点高度增加，就认为整条链路已经正确。实际上，源码可能由错误编译器构建，客户端可能连接错误网络，签名可能授权了不同字节，交易可能只进入内存池，执行可能回滚，索引器也可能展示尚未确认或已经被重组撤销的数据。只有把前状态、输入、验证依据、候选输出、回执、确认与后状态连起来，才能区分“请求被看见”和“状态已按预期提交”。

本页因此先定义最小对象和不变量，再运行工具。读者应在实验前写出预期状态和拒绝原因，运行后保存原始协议对象，而不是只保存截图或格式化摘要。正常样本证明主路径，边界样本验证零值、最大值、恰好余额或资源上限，单故障样本验证实现能在首个不满足条件处停止。三个样本共享版本、网络、账户和观察点，差异才有可解释性。

每次重放还应生成一份机器可读记录：书目来源、提交或源码摘要、运行时版本、网络标识、账户或钱包上下文、固定输入、原始输出、错误对象、前后状态摘要与时间顺序。复核者从空目录读取这份记录，不依赖操作者浏览器缓存或终端历史；若缺失字段、节点未同步或结果绑定到不同区块哈希，门禁应明确失败而不是自动补猜。

2018年版还带来一层额外要求：概念与历史工具必须分开。Geth、TestRPC、browser-solidity、Truffle、Solidity和比特币节点的命令及默认配置已经演进，直接复制可能失败，也可能在错误或不安全的上下文中运行。历史实验在无真实资产的隔离环境中复现原行为；现代迁移另列替代工具、行为差异、验证方法和回退点，不能用今天的成功结果改写原书当时的系统假设。

## 版次、对象与验收合同

<Term def="值类型是第8章 Solidity语法详解的第1个复核坐标，必须绑定对象、版本、状态与失败证据。">
  值类型
</Term>
、
<Term def="引用类型是第8章 Solidity语法详解的第2个复核坐标，必须绑定对象、版本、状态与失败证据。">
  引用类型
</Term>
、
<Term def="storage是第8章 Solidity语法详解的第3个复核坐标，必须绑定对象、版本、状态与失败证据。">
  storage
</Term>
、
<Term def="可见性是第8章 Solidity语法详解的第4个复核坐标，必须绑定对象、版本、状态与失败证据。">
  可见性
</Term>
、
<Term def="异常回滚是第8章 Solidity语法详解的第5个复核坐标，必须绑定对象、版本、状态与失败证据。">
  异常回滚
</Term>
共同构成本页复核坐标。从类型、数据位置、函数、事件、继承、库与异常处理推导合约状态和安全边界。

本书锁定吴寿鹤、冯翔、刘涛、周广益《区块链开发实战：以太坊关键技术与案例分析》，机械工业出版社，2018年6月，227页，ISBN 9787111599562。得到电子书公开前言、11章和附录A至C的完整目录；京东交叉核对三部分结构和目录页码，天珑书店交叉核对作者、出版社、2018年6月、227页与ISBN。全书共有185个正式目录层级，学习地图与总复习不计入分母。本文是独立教学重构，不复制原书正文。

Solidity语法最终映射到存储布局、内存生命周期、调用上下文和EVM控制流。类型、可见性、数据位置和调用顺序共同决定资产是否守恒；历史语法必须与编译器版本绑定。

<Callout type="info">
  **验收命题：**
  能为一个含数组、映射、结构体、事件和外部支付的合约标注数据位置、权限、状态变化和失败点，并写出正常与攻击测试。
</Callout>

## 核心状态模型

$$
cost(T)=gasUsed(T) \times effectiveGasPrice(T)
$$

$$
address = last_{20}(keccak256(RLP(sender,nonce)))
$$

$$
S_{t+1} = apply(S_t, T_t), \quad valid(T_t,S_t)=1
$$

$$
gate = source \times version \times replay \times boundary \times failureEvidence
$$

第一式描述本页资源、价值或身份约束；第二式给出地址、确认或账本链接等派生关系。状态转移式要求候选请求先通过当前状态验证，再原子地产生后状态；失败必须保留原状态。证据门禁采用乘法语义，来源、版本、重放、边界或失败证据任一缺失都不能发布。

以一个最小样本固定前状态、调用者、输入、节点或编译器版本与确认阈值。正常样本走完整成功路径；边界样本取零值、最大值、恰好余额或恰好Gas；单故障样本只改变签名、nonce、网络身份、区块链接、权限或响应字段中的一项。三者使用同一观察合同，才能把差异归因到目标规则。

## 三段可运行骨架

```typescript
type Trace = { before: string; input: string; after?: string; error?: string };

function evidenceGate(trace: Trace) {
  const complete = Boolean(
    trace.before && trace.input && (trace.after || trace.error),
  );
  return { complete, trace, unit: "bdp-08-solidity-syntax" };
}
```

```python
def apply_transition(state, request, verify):
    if not verify(state, request):
        return {"accepted": False, "state": state, "reason": "invalid"}
    candidate = state.copy()
    candidate[request["key"]] = request["value"]
    return {"accepted": True, "state": candidate, "reason": None}
```

```solidity
mapping(address => uint256) private credit;

function withdraw() external {
    uint256 amount = credit[msg.sender];
    require(amount > 0, "no-credit");
    credit[msg.sender] = 0;
    (bool ok, ) = msg.sender.call{value: amount}("");
    require(ok, "transfer-failed");
}
```

第一段要求成功和失败都形成结构化轨迹；第二段验证失败不污染原状态；第三段把接口、合约或确认落到可执行边界。生产实现还要补充密码库、并发、持久化、超时、整数范围与密钥管理，教学骨架不应直接连接真实资产。

## 正式目录逐项讲解

### 第8章 Solidity语法详解

“第8章 Solidity语法详解”位于本页正式目录坐标 1/44。Solidity语法最终映射到存储布局、内存生命周期、调用上下文和EVM控制流。类型、可见性、数据位置和调用顺序共同决定资产是否守恒；历史语法必须与编译器版本绑定。

复核时记录前状态、输入、调用者或验证者、候选输出、拒绝原因和后状态；正常、边界与单故障样本除目标变量外保持一致。通过标准是“能为一个含数组、映射、结构体、事件和外部支付的合约标注数据位置、权限、状态变化和失败点，并写出正常与攻击测试。”，并保留原始响应、状态摘要、版本和首个失败点。

### 8.1 注释

“8.1 注释”位于本页正式目录坐标 2/44。Solidity语法最终映射到存储布局、内存生命周期、调用上下文和EVM控制流。类型、可见性、数据位置和调用顺序共同决定资产是否守恒；历史语法必须与编译器版本绑定。

复核时记录前状态、输入、调用者或验证者、候选输出、拒绝原因和后状态；正常、边界与单故障样本除目标变量外保持一致。通过标准是“能为一个含数组、映射、结构体、事件和外部支付的合约标注数据位置、权限、状态变化和失败点，并写出正常与攻击测试。”，并保留原始响应、状态摘要、版本和首个失败点。

### 8.2 整型和布尔型

“8.2 整型和布尔型”位于本页正式目录坐标 3/44。整数宽度决定范围和存储，布尔量用于表达状态门禁。2018年编译器的算术默认行为与现代版本不同；边界测试必须覆盖零、最大值、加减溢出和错误类型转换。

复核时记录前状态、输入、调用者或验证者、候选输出、拒绝原因和后状态；正常、边界与单故障样本除目标变量外保持一致。通过标准是“能为一个含数组、映射、结构体、事件和外部支付的合约标注数据位置、权限、状态变化和失败点，并写出正常与攻击测试。”，并保留原始响应、状态摘要、版本和首个失败点。

### 8.2.1 整型

“8.2.1 整型”位于本页正式目录坐标 4/44。整数宽度决定范围和存储，布尔量用于表达状态门禁。2018年编译器的算术默认行为与现代版本不同；边界测试必须覆盖零、最大值、加减溢出和错误类型转换。

复核时记录前状态、输入、调用者或验证者、候选输出、拒绝原因和后状态；正常、边界与单故障样本除目标变量外保持一致。通过标准是“能为一个含数组、映射、结构体、事件和外部支付的合约标注数据位置、权限、状态变化和失败点，并写出正常与攻击测试。”，并保留原始响应、状态摘要、版本和首个失败点。

### 8.2.2 布尔型

“8.2.2 布尔型”位于本页正式目录坐标 5/44。整数宽度决定范围和存储，布尔量用于表达状态门禁。2018年编译器的算术默认行为与现代版本不同；边界测试必须覆盖零、最大值、加减溢出和错误类型转换。

复核时记录前状态、输入、调用者或验证者、候选输出、拒绝原因和后状态；正常、边界与单故障样本除目标变量外保持一致。通过标准是“能为一个含数组、映射、结构体、事件和外部支付的合约标注数据位置、权限、状态变化和失败点，并写出正常与攻击测试。”，并保留原始响应、状态摘要、版本和首个失败点。

### 8.3 地址

“8.3 地址”位于本页正式目录坐标 6/44。地址标识外部账户或合约账户，但不携带类型和业务身份。转账前要验证零地址、调用者、目标代码、chain ID与返回路径，不能把地址格式正确当作收款人真实。

复核时记录前状态、输入、调用者或验证者、候选输出、拒绝原因和后状态；正常、边界与单故障样本除目标变量外保持一致。通过标准是“能为一个含数组、映射、结构体、事件和外部支付的合约标注数据位置、权限、状态变化和失败点，并写出正常与攻击测试。”，并保留原始响应、状态摘要、版本和首个失败点。

### 8.4 字节数组

“8.4 字节数组”位于本页正式目录坐标 7/44。固定长度字节适合摘要等定长值，动态bytes携带长度和可变内容。二者在存储、内存、ABI编码和Gas上的行为不同；比较前先明确字节序和编码。

复核时记录前状态、输入、调用者或验证者、候选输出、拒绝原因和后状态；正常、边界与单故障样本除目标变量外保持一致。通过标准是“能为一个含数组、映射、结构体、事件和外部支付的合约标注数据位置、权限、状态变化和失败点，并写出正常与攻击测试。”，并保留原始响应、状态摘要、版本和首个失败点。

### 8.4.1 固定长字节数组

“8.4.1 固定长字节数组”位于本页正式目录坐标 8/44。固定长度字节适合摘要等定长值，动态bytes携带长度和可变内容。二者在存储、内存、ABI编码和Gas上的行为不同；比较前先明确字节序和编码。

复核时记录前状态、输入、调用者或验证者、候选输出、拒绝原因和后状态；正常、边界与单故障样本除目标变量外保持一致。通过标准是“能为一个含数组、映射、结构体、事件和外部支付的合约标注数据位置、权限、状态变化和失败点，并写出正常与攻击测试。”，并保留原始响应、状态摘要、版本和首个失败点。

### 8.4.2 动态长度字节数组

“8.4.2 动态长度字节数组”位于本页正式目录坐标 9/44。固定长度字节适合摘要等定长值，动态bytes携带长度和可变内容。二者在存储、内存、ABI编码和Gas上的行为不同；比较前先明确字节序和编码。

复核时记录前状态、输入、调用者或验证者、候选输出、拒绝原因和后状态；正常、边界与单故障样本除目标变量外保持一致。通过标准是“能为一个含数组、映射、结构体、事件和外部支付的合约标注数据位置、权限、状态变化和失败点，并写出正常与攻击测试。”，并保留原始响应、状态摘要、版本和首个失败点。

### 8.5 类型转换和类型推断

“8.5 类型转换和类型推断”位于本页正式目录坐标 10/44。显式与隐式转换可能截断宽度、改变符号或丢失地址语义。安全做法是在边界处断言范围，保留原始值与转换结果，并让编译器版本进入测试矩阵。

复核时记录前状态、输入、调用者或验证者、候选输出、拒绝原因和后状态；正常、边界与单故障样本除目标变量外保持一致。通过标准是“能为一个含数组、映射、结构体、事件和外部支付的合约标注数据位置、权限、状态变化和失败点，并写出正常与攻击测试。”，并保留原始响应、状态摘要、版本和首个失败点。

### 8.5.1 类型转换

“8.5.1 类型转换”位于本页正式目录坐标 11/44。显式与隐式转换可能截断宽度、改变符号或丢失地址语义。安全做法是在边界处断言范围，保留原始值与转换结果，并让编译器版本进入测试矩阵。

复核时记录前状态、输入、调用者或验证者、候选输出、拒绝原因和后状态；正常、边界与单故障样本除目标变量外保持一致。通过标准是“能为一个含数组、映射、结构体、事件和外部支付的合约标注数据位置、权限、状态变化和失败点，并写出正常与攻击测试。”，并保留原始响应、状态摘要、版本和首个失败点。

### 8.5.2 类型推断

“8.5.2 类型推断”位于本页正式目录坐标 12/44。显式与隐式转换可能截断宽度、改变符号或丢失地址语义。安全做法是在边界处断言范围，保留原始值与转换结果，并让编译器版本进入测试矩阵。

复核时记录前状态、输入、调用者或验证者、候选输出、拒绝原因和后状态；正常、边界与单故障样本除目标变量外保持一致。通过标准是“能为一个含数组、映射、结构体、事件和外部支付的合约标注数据位置、权限、状态变化和失败点，并写出正常与攻击测试。”，并保留原始响应、状态摘要、版本和首个失败点。

### 8.6 时间单位和货币单位

“8.6 时间单位和货币单位”位于本页正式目录坐标 13/44。单位后缀是编译期数值换算，不提供真实时钟或价格稳定性。区块时间可被有限操纵，wei到ether的显示换算也不能使用浮点数处理资产。

复核时记录前状态、输入、调用者或验证者、候选输出、拒绝原因和后状态；正常、边界与单故障样本除目标变量外保持一致。通过标准是“能为一个含数组、映射、结构体、事件和外部支付的合约标注数据位置、权限、状态变化和失败点，并写出正常与攻击测试。”，并保留原始响应、状态摘要、版本和首个失败点。

### 8.6.1 时间单位

“8.6.1 时间单位”位于本页正式目录坐标 14/44。单位后缀是编译期数值换算，不提供真实时钟或价格稳定性。区块时间可被有限操纵，wei到ether的显示换算也不能使用浮点数处理资产。

复核时记录前状态、输入、调用者或验证者、候选输出、拒绝原因和后状态；正常、边界与单故障样本除目标变量外保持一致。通过标准是“能为一个含数组、映射、结构体、事件和外部支付的合约标注数据位置、权限、状态变化和失败点，并写出正常与攻击测试。”，并保留原始响应、状态摘要、版本和首个失败点。

### 8.6.2 货币单位

“8.6.2 货币单位”位于本页正式目录坐标 15/44。单位后缀是编译期数值换算，不提供真实时钟或价格稳定性。区块时间可被有限操纵，wei到ether的显示换算也不能使用浮点数处理资产。

复核时记录前状态、输入、调用者或验证者、候选输出、拒绝原因和后状态；正常、边界与单故障样本除目标变量外保持一致。通过标准是“能为一个含数组、映射、结构体、事件和外部支付的合约标注数据位置、权限、状态变化和失败点，并写出正常与攻击测试。”，并保留原始响应、状态摘要、版本和首个失败点。

### 8.7 数组和多维数组

“8.7 数组和多维数组”位于本页正式目录坐标 16/44。数组保存可枚举序列，映射提供键到值的稀疏存储但不能天然枚举。删除、长度、默认零值和多维索引会影响状态不变量与Gas，遍历长度必须设上限。

复核时记录前状态、输入、调用者或验证者、候选输出、拒绝原因和后状态；正常、边界与单故障样本除目标变量外保持一致。通过标准是“能为一个含数组、映射、结构体、事件和外部支付的合约标注数据位置、权限、状态变化和失败点，并写出正常与攻击测试。”，并保留原始响应、状态摘要、版本和首个失败点。

### 8.7.1 数组

“8.7.1 数组”位于本页正式目录坐标 17/44。数组保存可枚举序列，映射提供键到值的稀疏存储但不能天然枚举。删除、长度、默认零值和多维索引会影响状态不变量与Gas，遍历长度必须设上限。

复核时记录前状态、输入、调用者或验证者、候选输出、拒绝原因和后状态；正常、边界与单故障样本除目标变量外保持一致。通过标准是“能为一个含数组、映射、结构体、事件和外部支付的合约标注数据位置、权限、状态变化和失败点，并写出正常与攻击测试。”，并保留原始响应、状态摘要、版本和首个失败点。

### 8.7.2 多维数组

“8.7.2 多维数组”位于本页正式目录坐标 18/44。数组保存可枚举序列，映射提供键到值的稀疏存储但不能天然枚举。删除、长度、默认零值和多维索引会影响状态不变量与Gas，遍历长度必须设上限。

复核时记录前状态、输入、调用者或验证者、候选输出、拒绝原因和后状态；正常、边界与单故障样本除目标变量外保持一致。通过标准是“能为一个含数组、映射、结构体、事件和外部支付的合约标注数据位置、权限、状态变化和失败点，并写出正常与攻击测试。”，并保留原始响应、状态摘要、版本和首个失败点。

### 8.8 映射/字典

“8.8 映射/字典”位于本页正式目录坐标 19/44。数组保存可枚举序列，映射提供键到值的稀疏存储但不能天然枚举。删除、长度、默认零值和多维索引会影响状态不变量与Gas，遍历长度必须设上限。

复核时记录前状态、输入、调用者或验证者、候选输出、拒绝原因和后状态；正常、边界与单故障样本除目标变量外保持一致。通过标准是“能为一个含数组、映射、结构体、事件和外部支付的合约标注数据位置、权限、状态变化和失败点，并写出正常与攻击测试。”，并保留原始响应、状态摘要、版本和首个失败点。

### 8.9 结构体与枚举

“8.9 结构体与枚举”位于本页正式目录坐标 20/44。结构体把相关字段组织为状态对象，枚举限制离散阶段。状态机要明确允许迁移，不能只靠枚举名称；升级或ABI交互还要保持字段布局和取值兼容。

复核时记录前状态、输入、调用者或验证者、候选输出、拒绝原因和后状态；正常、边界与单故障样本除目标变量外保持一致。通过标准是“能为一个含数组、映射、结构体、事件和外部支付的合约标注数据位置、权限、状态变化和失败点，并写出正常与攻击测试。”，并保留原始响应、状态摘要、版本和首个失败点。

### 8.9.1 结构体

“8.9.1 结构体”位于本页正式目录坐标 21/44。结构体把相关字段组织为状态对象，枚举限制离散阶段。状态机要明确允许迁移，不能只靠枚举名称；升级或ABI交互还要保持字段布局和取值兼容。

复核时记录前状态、输入、调用者或验证者、候选输出、拒绝原因和后状态；正常、边界与单故障样本除目标变量外保持一致。通过标准是“能为一个含数组、映射、结构体、事件和外部支付的合约标注数据位置、权限、状态变化和失败点，并写出正常与攻击测试。”，并保留原始响应、状态摘要、版本和首个失败点。

### 8.9.2 枚举

“8.9.2 枚举”位于本页正式目录坐标 22/44。结构体把相关字段组织为状态对象，枚举限制离散阶段。状态机要明确允许迁移，不能只靠枚举名称；升级或ABI交互还要保持字段布局和取值兼容。

复核时记录前状态、输入、调用者或验证者、候选输出、拒绝原因和后状态；正常、边界与单故障样本除目标变量外保持一致。通过标准是“能为一个含数组、映射、结构体、事件和外部支付的合约标注数据位置、权限、状态变化和失败点，并写出正常与攻击测试。”，并保留原始响应、状态摘要、版本和首个失败点。

### 8.10 全局变量

“8.10 全局变量”位于本页正式目录坐标 23/44。msg、block和tx上下文提供调用者、金额和区块信息，但部分值可被调用链或出块者影响。授权优先使用明确身份，业务随机性和长期时间保证不能直接依赖区块字段。

复核时记录前状态、输入、调用者或验证者、候选输出、拒绝原因和后状态；正常、边界与单故障样本除目标变量外保持一致。通过标准是“能为一个含数组、映射、结构体、事件和外部支付的合约标注数据位置、权限、状态变化和失败点，并写出正常与攻击测试。”，并保留原始响应、状态摘要、版本和首个失败点。

### 8.11 控制结构

“8.11 控制结构”位于本页正式目录坐标 24/44。控制流决定哪些状态更新和外部调用发生。函数签名、可见性、可变性、修饰器和返回值共同构成接口；循环必须有资源上限，检查应早于不可控副作用。

复核时记录前状态、输入、调用者或验证者、候选输出、拒绝原因和后状态；正常、边界与单故障样本除目标变量外保持一致。通过标准是“能为一个含数组、映射、结构体、事件和外部支付的合约标注数据位置、权限、状态变化和失败点，并写出正常与攻击测试。”，并保留原始响应、状态摘要、版本和首个失败点。

### 8.12 函数

“8.12 函数”位于本页正式目录坐标 25/44。控制流决定哪些状态更新和外部调用发生。函数签名、可见性、可变性、修饰器和返回值共同构成接口；循环必须有资源上限，检查应早于不可控副作用。

复核时记录前状态、输入、调用者或验证者、候选输出、拒绝原因和后状态；正常、边界与单故障样本除目标变量外保持一致。通过标准是“能为一个含数组、映射、结构体、事件和外部支付的合约标注数据位置、权限、状态变化和失败点，并写出正常与攻击测试。”，并保留原始响应、状态摘要、版本和首个失败点。

### 8.13 事件

“8.13 事件”位于本页正式目录坐标 26/44。事件是交易日志，便于前端与索引器重建视图，但合约不能读取历史日志，重组也会撤销事件。关键状态必须保存在存储中，索引器按区块哈希处理确认和回滚。

复核时记录前状态、输入、调用者或验证者、候选输出、拒绝原因和后状态；正常、边界与单故障样本除目标变量外保持一致。通过标准是“能为一个含数组、映射、结构体、事件和外部支付的合约标注数据位置、权限、状态变化和失败点，并写出正常与攻击测试。”，并保留原始响应、状态摘要、版本和首个失败点。

### 8.14 合约

“8.14 合约”位于本页正式目录坐标 27/44。合约是由交易触发的确定性状态机。调用者、输入、前状态、代码和执行环境共同决定后状态；权限、外部调用、资源上限和失败回滚必须显式验证。

复核时记录前状态、输入、调用者或验证者、候选输出、拒绝原因和后状态；正常、边界与单故障样本除目标变量外保持一致。通过标准是“能为一个含数组、映射、结构体、事件和外部支付的合约标注数据位置、权限、状态变化和失败点，并写出正常与攻击测试。”，并保留原始响应、状态摘要、版本和首个失败点。

### 8.15 继承

“8.15 继承”位于本页正式目录坐标 28/44。继承复用实现，抽象合约保留未实现行为，接口只声明外部合同。线性化、覆盖与存储布局都可能改变实际调用；测试应通过最终字节码和调用轨迹验证分派。

复核时记录前状态、输入、调用者或验证者、候选输出、拒绝原因和后状态；正常、边界与单故障样本除目标变量外保持一致。通过标准是“能为一个含数组、映射、结构体、事件和外部支付的合约标注数据位置、权限、状态变化和失败点，并写出正常与攻击测试。”，并保留原始响应、状态摘要、版本和首个失败点。

### 8.16 抽象合约

“8.16 抽象合约”位于本页正式目录坐标 29/44。合约是由交易触发的确定性状态机。调用者、输入、前状态、代码和执行环境共同决定后状态；权限、外部调用、资源上限和失败回滚必须显式验证。

复核时记录前状态、输入、调用者或验证者、候选输出、拒绝原因和后状态；正常、边界与单故障样本除目标变量外保持一致。通过标准是“能为一个含数组、映射、结构体、事件和外部支付的合约标注数据位置、权限、状态变化和失败点，并写出正常与攻击测试。”，并保留原始响应、状态摘要、版本和首个失败点。

### 8.17 接口

“8.17 接口”位于本页正式目录坐标 30/44。继承复用实现，抽象合约保留未实现行为，接口只声明外部合同。线性化、覆盖与存储布局都可能改变实际调用；测试应通过最终字节码和调用轨迹验证分派。

复核时记录前状态、输入、调用者或验证者、候选输出、拒绝原因和后状态；正常、边界与单故障样本除目标变量外保持一致。通过标准是“能为一个含数组、映射、结构体、事件和外部支付的合约标注数据位置、权限、状态变化和失败点，并写出正常与攻击测试。”，并保留原始响应、状态摘要、版本和首个失败点。

### 8.18 库

“8.18 库”位于本页正式目录坐标 31/44。库与源文件拆分减少重复，却引入链接地址、依赖版本和命名解析边界。using for只是方法式语法绑定，不改变底层参数；构建应锁定全部源摘要。

复核时记录前状态、输入、调用者或验证者、候选输出、拒绝原因和后状态；正常、边界与单故障样本除目标变量外保持一致。通过标准是“能为一个含数组、映射、结构体、事件和外部支付的合约标注数据位置、权限、状态变化和失败点，并写出正常与攻击测试。”，并保留原始响应、状态摘要、版本和首个失败点。

### 8.19 Using for

“8.19 Using for”位于本页正式目录坐标 32/44。库与源文件拆分减少重复，却引入链接地址、依赖版本和命名解析边界。using for只是方法式语法绑定，不改变底层参数；构建应锁定全部源摘要。

复核时记录前状态、输入、调用者或验证者、候选输出、拒绝原因和后状态；正常、边界与单故障样本除目标变量外保持一致。通过标准是“能为一个含数组、映射、结构体、事件和外部支付的合约标注数据位置、权限、状态变化和失败点，并写出正常与攻击测试。”，并保留原始响应、状态摘要、版本和首个失败点。

### 8.20 引入其他源文件

“8.20 引入其他源文件”位于本页正式目录坐标 33/44。库与源文件拆分减少重复，却引入链接地址、依赖版本和命名解析边界。using for只是方法式语法绑定，不改变底层参数；构建应锁定全部源摘要。

复核时记录前状态、输入、调用者或验证者、候选输出、拒绝原因和后状态；正常、边界与单故障样本除目标变量外保持一致。通过标准是“能为一个含数组、映射、结构体、事件和外部支付的合约标注数据位置、权限、状态变化和失败点，并写出正常与攻击测试。”，并保留原始响应、状态摘要、版本和首个失败点。

### 8.21 状态变量/局部变量

“8.21 状态变量/局部变量”位于本页正式目录坐标 34/44。storage持久化链上状态，memory服务单次调用，calldata是只读外部输入。引用赋值可能复制也可能别名；审计要标注每个引用的生命周期和写入目标。

复核时记录前状态、输入、调用者或验证者、候选输出、拒绝原因和后状态；正常、边界与单故障样本除目标变量外保持一致。通过标准是“能为一个含数组、映射、结构体、事件和外部支付的合约标注数据位置、权限、状态变化和失败点，并写出正常与攻击测试。”，并保留原始响应、状态摘要、版本和首个失败点。

### 8.22 数据位置

“8.22 数据位置”位于本页正式目录坐标 35/44。storage持久化链上状态，memory服务单次调用，calldata是只读外部输入。引用赋值可能复制也可能别名；审计要标注每个引用的生命周期和写入目标。

复核时记录前状态、输入、调用者或验证者、候选输出、拒绝原因和后状态；正常、边界与单故障样本除目标变量外保持一致。通过标准是“能为一个含数组、映射、结构体、事件和外部支付的合约标注数据位置、权限、状态变化和失败点，并写出正常与攻击测试。”，并保留原始响应、状态摘要、版本和首个失败点。

### 8.22.1 数据位置概述

“8.22.1 数据位置概述”位于本页正式目录坐标 36/44。storage持久化链上状态，memory服务单次调用，calldata是只读外部输入。引用赋值可能复制也可能别名；审计要标注每个引用的生命周期和写入目标。

复核时记录前状态、输入、调用者或验证者、候选输出、拒绝原因和后状态；正常、边界与单故障样本除目标变量外保持一致。通过标准是“能为一个含数组、映射、结构体、事件和外部支付的合约标注数据位置、权限、状态变化和失败点，并写出正常与攻击测试。”，并保留原始响应、状态摘要、版本和首个失败点。

### 8.22.2 数据位置之间相互转换

“8.22.2 数据位置之间相互转换”位于本页正式目录坐标 37/44。storage持久化链上状态，memory服务单次调用，calldata是只读外部输入。引用赋值可能复制也可能别名；审计要标注每个引用的生命周期和写入目标。

复核时记录前状态、输入、调用者或验证者、候选输出、拒绝原因和后状态；正常、边界与单故障样本除目标变量外保持一致。通过标准是“能为一个含数组、映射、结构体、事件和外部支付的合约标注数据位置、权限、状态变化和失败点，并写出正常与攻击测试。”，并保留原始响应、状态摘要、版本和首个失败点。

### 8.23 异常处理

“8.23 异常处理”位于本页正式目录坐标 38/44。异常使当前调用路径的状态更新回滚并消耗已用Gas。入口先验证权限、范围和状态可减少无效工作；外部调用失败还要明确传播、捕获或补偿策略。

复核时记录前状态、输入、调用者或验证者、候选输出、拒绝原因和后状态；正常、边界与单故障样本除目标变量外保持一致。通过标准是“能为一个含数组、映射、结构体、事件和外部支付的合约标注数据位置、权限、状态变化和失败点，并写出正常与攻击测试。”，并保留原始响应、状态摘要、版本和首个失败点。

### 8.24 编写安全solidity智能合约最佳实践

“8.24 编写安全solidity智能合约最佳实践”位于本页正式目录坐标 39/44。合约是由交易触发的确定性状态机。调用者、输入、前状态、代码和执行环境共同决定后状态；权限、外部调用、资源上限和失败回滚必须显式验证。

复核时记录前状态、输入、调用者或验证者、候选输出、拒绝原因和后状态；正常、边界与单故障样本除目标变量外保持一致。通过标准是“能为一个含数组、映射、结构体、事件和外部支付的合约标注数据位置、权限、状态变化和失败点，并写出正常与攻击测试。”，并保留原始响应、状态摘要、版本和首个失败点。

### 8.24.1 尽早抛出异常

“8.24.1 尽早抛出异常”位于本页正式目录坐标 40/44。异常使当前调用路径的状态更新回滚并消耗已用Gas。入口先验证权限、范围和状态可减少无效工作；外部调用失败还要明确传播、捕获或补偿策略。

复核时记录前状态、输入、调用者或验证者、候选输出、拒绝原因和后状态；正常、边界与单故障样本除目标变量外保持一致。通过标准是“能为一个含数组、映射、结构体、事件和外部支付的合约标注数据位置、权限、状态变化和失败点，并写出正常与攻击测试。”，并保留原始响应、状态摘要、版本和首个失败点。

### 8.24.2 结构化函数代码顺序

“8.24.2 结构化函数代码顺序”位于本页正式目录坐标 41/44。控制流决定哪些状态更新和外部调用发生。函数签名、可见性、可变性、修饰器和返回值共同构成接口；循环必须有资源上限，检查应早于不可控副作用。

复核时记录前状态、输入、调用者或验证者、候选输出、拒绝原因和后状态；正常、边界与单故障样本除目标变量外保持一致。通过标准是“能为一个含数组、映射、结构体、事件和外部支付的合约标注数据位置、权限、状态变化和失败点，并写出正常与攻击测试。”，并保留原始响应、状态摘要、版本和首个失败点。

### 8.24.3 在支付时使用pull模式而不是push模式

“8.24.3 在支付时使用pull模式而不是push模式”位于本页正式目录坐标 42/44。pull payment先记可提余额，再由收款人主动提取，把单个接收方失败与主流程隔离。实现仍要先清零状态再外部调用，并防止重入和重复提取。

复核时记录前状态、输入、调用者或验证者、候选输出、拒绝原因和后状态；正常、边界与单故障样本除目标变量外保持一致。通过标准是“能为一个含数组、映射、结构体、事件和外部支付的合约标注数据位置、权限、状态变化和失败点，并写出正常与攻击测试。”，并保留原始响应、状态摘要、版本和首个失败点。

### 8.24.4 整数上溢和下溢

“8.24.4 整数上溢和下溢”位于本页正式目录坐标 43/44。2018年Solidity整数运算可能静默环绕，攻击者可借边界值破坏余额和计数。历史重放用显式安全算术，现代版本同时测试checked与unchecked边界。

复核时记录前状态、输入、调用者或验证者、候选输出、拒绝原因和后状态；正常、边界与单故障样本除目标变量外保持一致。通过标准是“能为一个含数组、映射、结构体、事件和外部支付的合约标注数据位置、权限、状态变化和失败点，并写出正常与攻击测试。”，并保留原始响应、状态摘要、版本和首个失败点。

### 8.25 本章小结

“8.25 本章小结”位于本页正式目录坐标 44/44。“第8章 Solidity语法详解”的收束不是名词列表，而是把对象、授权、执行顺序、状态不变量和版本边界连接成一条可重放轨迹。

以“能为一个含数组、映射、结构体、事件和外部支付的合约标注数据位置、权限、状态变化和失败点，并写出正常与攻击测试。”作为通过标准；只展示成功截图、最终余额或交易哈希都不构成完整证据。通过标准是“能为一个含数组、映射、结构体、事件和外部支付的合约标注数据位置、权限、状态变化和失败点，并写出正常与攻击测试。”，并保留原始响应、状态摘要、版本和首个失败点。

## 正常、边界与故障矩阵

| 样本   | 只改变的变量                      | 预期结果               | 必存证据                   |
| ------ | --------------------------------- | ---------------------- | -------------------------- |
| 正常   | 合法输入与足额资源                | 状态提交并达到确认阈值 | 前后状态、哈希、回执、版本 |
| 边界   | 零值、最大值或恰好阈值            | 按明确规则接受或拒绝   | 边界值、判断分支、资源消耗 |
| 单故障 | 签名、nonce、网络、权限或链接一项 | 提交前拒绝且原状态不变 | 错误对象、首个差异、回退点 |

测试先手算预期，再运行实现，并从独立进程读取最终状态。若前端、客户端、节点和链上状态给出不同答案，以原始协议对象和固定区块哈希为复核坐标；不得用刷新页面、提高超时或重复广播掩盖不一致。

## 三步交互实验

<Stepper>
  <Step title="追踪处理流水线">
    先预测当前阶段的输入、验证依据与候选状态，再拖动阶段滑块定位首个分叉。
    <Bdp08SoliditySyntaxFlowLab />
  </Step>
  <Step title="改变资源与节点条件">
    一次只调整节点数或资源预算，观察可用性、确认与执行门槛怎样变化。
    <Bdp08SoliditySyntaxExperimentLab />
  </Step>
  <Step title="注入单一故障">
    注入签名、网络、接口或状态故障，要求证据门禁在提交前给出拒绝原因和回退点。
    <Bdp08SoliditySyntaxEvidenceLab />
  </Step>
</Stepper>

## 常见失败与回退

<Callout type="trap">
  **把交易哈希当成功：**
  哈希只标识候选交易。还要检查节点接收、入块、执行状态、事件、后状态和确认深度。
</Callout>

<Callout type="trap">
  **把2018年命令当当前接口：**
  原书中的Geth、TestRPC、browser-solidity、Truffle、Solidity和比特币参数属于历史版本。重放使用隔离环境，现代部署使用当前文档与安全默认值。
</Callout>

<Callout type="tip">
  **回退顺序：** 书目与软件版本 -> 网络和创世身份 -> 编码与签名 -> 请求或交易规则 -> 执行与回执 -> 确认与索引视图。首个差异未解释前不比较最终余额。
</Callout>

## 本章回顾

从类型、数据位置、函数、事件、继承、库与异常处理推导合约状态和安全边界。 Solidity语法最终映射到存储布局、内存生命周期、调用上下文和EVM控制流。类型、可见性、数据位置和调用顺序共同决定资产是否守恒；历史语法必须与编译器版本绑定。 掌握标准不是记住命令或界面，而是能为一个含数组、映射、结构体、事件和外部支付的合约标注数据位置、权限、状态变化和失败点，并写出正常与攻击测试。。发布前必须由独立观察者从来源和固定配置重放正常、边界及单故障样本。

<Glossary>

<GlossaryItem term="值类型">
  “值类型”用于定位第8章
  Solidity语法详解的第1类证据；定义必须包含输入、验证规则、状态变化、版本和拒绝路径。
</GlossaryItem>

<GlossaryItem term="引用类型">
  “引用类型”用于定位第8章
  Solidity语法详解的第2类证据；定义必须包含输入、验证规则、状态变化、版本和拒绝路径。
</GlossaryItem>

<GlossaryItem term="storage">
  “storage”用于定位第8章
  Solidity语法详解的第3类证据；定义必须包含输入、验证规则、状态变化、版本和拒绝路径。
</GlossaryItem>

<GlossaryItem term="可见性">
  “可见性”用于定位第8章
  Solidity语法详解的第4类证据；定义必须包含输入、验证规则、状态变化、版本和拒绝路径。
</GlossaryItem>

<GlossaryItem term="异常回滚">
  “异常回滚”用于定位第8章
  Solidity语法详解的第5类证据；定义必须包含输入、验证规则、状态变化、版本和拒绝路径。
</GlossaryItem>

</Glossary>

<Exercises>

1. 如何为第8章 Solidity语法详解建立最小可重放基线？

<Answer>
  固定2018年版目录来源、软件版本、网络身份、前状态、调用者、输入、验证规则和确认条件，保存每一步判断、输出与拒绝原因。
</Answer>

2. 如何证明“能为一个含数组、映射、结构体、事件和外部支付的合约标注数据位置、权限、状态变化和失败点，并写出正常与攻击测试。”不是一次偶然成功？

<Answer>
  先手算最小实例，再对同一合同执行正常、边界和单故障样本；由独立进程读取原始对象和后状态，逐项一致才通过。
</Answer>

3. 修复第8章 Solidity语法详解后怎样回归？

<Answer>
  先重放原失败，再运行相邻边界和正常基线，按版本、网络、编码、签名、接口、执行、状态和确认定位首个差异，全部不变量通过后发布。
</Answer>

</Exercises>

## 前后导航

- [上一页：第7章 Solidity IDE和Solidity快速入门](/learn/blockchain-dev-practice/08-07-solidity-ide-quickstart/bdp-07-solidity-ide-quickstart)
- [下一页：第9章 Solidity合约编译、部署](/learn/blockchain-dev-practice/10-09-contract-compile-deploy/bdp-09-contract-compile-deploy)

<Attribution
  adaptedFrom="吴寿鹤、冯翔、刘涛、周广益《区块链开发实战：以太坊关键技术与案例分析》，机械工业出版社，2018年6月，227页，ISBN 9787111599562 · 基于完整目录185个正式层级的独立教学重构"
  adaptedUrl="https://www.dedao.cn/ebook/detail?id=bBVDEXGGLn7eB51b8NjVRqDoQJPMk3aKmq0adYrXmAxE4Ov92lgzK6ZypxLqdQjp"
/>
