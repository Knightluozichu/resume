---
title: "第9章 Solidity合约编译、部署"
type: C
section: "区块链开发实战（2018版）"
order: 10
description: "把源代码、编译器、ABI、字节码、部署交易、合约地址和调用回执连成证据链。"
demo: true
math: true
sourceUrl: "https://www.dedao.cn/ebook/detail?id=bBVDEXGGLn7eB51b8NjVRqDoQJPMk3aKmq0adYrXmAxE4Ov92lgzK6ZypxLqdQjp"
draft: false
---

import {
  Bdp09ContractCompileDeployFlowLab,
  Bdp09ContractCompileDeployExperimentLab,
  Bdp09ContractCompileDeployEvidenceLab,
} from "@/components/mdx/blockchain-dev-practice/diagrams/bdp-09-contract-compile-deploy";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释solc、ABI、字节码各自负责的对象、输入与假设边界
- 能推导状态转移、资源或价值约束、确认条件与证据门禁四组表达式
- 能实现并运行三段最小代码，追踪正常、边界和单故障样本的首个差异
- 能设计独立复核并证明“能锁定solc版本复现字节码，部署到隔离链，校验地址上的运行时代码，并区分只读调用、状态交易和失败回执。”

</Objectives>

## 为什么从可重放状态开始

部署不是上传源文件，而是用固定编译器把源码转成创建字节码，由签名交易执行构造逻辑并产生地址。源码、设置、ABI、字节码、交易和运行时代码必须可相互验证。

区块链开发最容易产生的错觉，是界面显示成功、命令返回哈希或节点高度增加，就认为整条链路已经正确。实际上，源码可能由错误编译器构建，客户端可能连接错误网络，签名可能授权了不同字节，交易可能只进入内存池，执行可能回滚，索引器也可能展示尚未确认或已经被重组撤销的数据。只有把前状态、输入、验证依据、候选输出、回执、确认与后状态连起来，才能区分“请求被看见”和“状态已按预期提交”。

本页因此先定义最小对象和不变量，再运行工具。读者应在实验前写出预期状态和拒绝原因，运行后保存原始协议对象，而不是只保存截图或格式化摘要。正常样本证明主路径，边界样本验证零值、最大值、恰好余额或资源上限，单故障样本验证实现能在首个不满足条件处停止。三个样本共享版本、网络、账户和观察点，差异才有可解释性。

每次重放还应生成一份机器可读记录：书目来源、提交或源码摘要、运行时版本、网络标识、账户或钱包上下文、固定输入、原始输出、错误对象、前后状态摘要与时间顺序。复核者从空目录读取这份记录，不依赖操作者浏览器缓存或终端历史；若缺失字段、节点未同步或结果绑定到不同区块哈希，门禁应明确失败而不是自动补猜。

2018年版还带来一层额外要求：概念与历史工具必须分开。Geth、TestRPC、browser-solidity、Truffle、Solidity和比特币节点的命令及默认配置已经演进，直接复制可能失败，也可能在错误或不安全的上下文中运行。历史实验在无真实资产的隔离环境中复现原行为；现代迁移另列替代工具、行为差异、验证方法和回退点，不能用今天的成功结果改写原书当时的系统假设。

## 版次、对象与验收合同

<Term def="solc是第9章 Solidity合约编译、部署的第1个复核坐标，必须绑定对象、版本、状态与失败证据。">
  solc
</Term>
、
<Term def="ABI是第9章 Solidity合约编译、部署的第2个复核坐标，必须绑定对象、版本、状态与失败证据。">
  ABI
</Term>
、
<Term def="字节码是第9章 Solidity合约编译、部署的第3个复核坐标，必须绑定对象、版本、状态与失败证据。">
  字节码
</Term>
、
<Term def="部署交易是第9章 Solidity合约编译、部署的第4个复核坐标，必须绑定对象、版本、状态与失败证据。">
  部署交易
</Term>
、
<Term def="合约地址是第9章 Solidity合约编译、部署的第5个复核坐标，必须绑定对象、版本、状态与失败证据。">
  合约地址
</Term>
共同构成本页复核坐标。把源代码、编译器、ABI、字节码、部署交易、合约地址和调用回执连成证据链。

本书锁定吴寿鹤、冯翔、刘涛、周广益《区块链开发实战：以太坊关键技术与案例分析》，机械工业出版社，2018年6月，227页，ISBN 9787111599562。得到电子书公开前言、11章和附录A至C的完整目录；京东交叉核对三部分结构和目录页码，天珑书店交叉核对作者、出版社、2018年6月、227页与ISBN。全书共有185个正式目录层级，学习地图与总复习不计入分母。本文是独立教学重构，不复制原书正文。

部署不是上传源文件，而是用固定编译器把源码转成创建字节码，由签名交易执行构造逻辑并产生地址。源码、设置、ABI、字节码、交易和运行时代码必须可相互验证。

<Callout type="info">
  **验收命题：**
  能锁定solc版本复现字节码，部署到隔离链，校验地址上的运行时代码，并区分只读调用、状态交易和失败回执。
</Callout>

## 核心状态模型

$$
cost(T)=gasUsed(T) \times effectiveGasPrice(T)
$$

$$
address = last_{20}(keccak256(RLP(sender,nonce)))
$$

$$
S_{t+1} = apply(S_t, T_t), \quad valid(T_t,S_t)=1
$$

$$
gate = source \times version \times replay \times boundary \times failureEvidence
$$

第一式描述本页资源、价值或身份约束；第二式给出地址、确认或账本链接等派生关系。状态转移式要求候选请求先通过当前状态验证，再原子地产生后状态；失败必须保留原状态。证据门禁采用乘法语义，来源、版本、重放、边界或失败证据任一缺失都不能发布。

以一个最小样本固定前状态、调用者、输入、节点或编译器版本与确认阈值。正常样本走完整成功路径；边界样本取零值、最大值、恰好余额或恰好Gas；单故障样本只改变签名、nonce、网络身份、区块链接、权限或响应字段中的一项。三者使用同一观察合同，才能把差异归因到目标规则。

## 三段可运行骨架

```typescript
type Trace = { before: string; input: string; after?: string; error?: string };

function evidenceGate(trace: Trace) {
  const complete = Boolean(
    trace.before && trace.input && (trace.after || trace.error),
  );
  return { complete, trace, unit: "bdp-09-contract-compile-deploy" };
}
```

```python
def apply_transition(state, request, verify):
    if not verify(state, request):
        return {"accepted": False, "state": state, "reason": "invalid"}
    candidate = state.copy()
    candidate[request["key"]] = request["value"]
    return {"accepted": True, "state": candidate, "reason": None}
```

```solidity
mapping(address => uint256) private credit;

function withdraw() external {
    uint256 amount = credit[msg.sender];
    require(amount > 0, "no-credit");
    credit[msg.sender] = 0;
    (bool ok, ) = msg.sender.call{value: amount}("");
    require(ok, "transfer-failed");
}
```

第一段要求成功和失败都形成结构化轨迹；第二段验证失败不污染原状态；第三段把接口、合约或确认落到可执行边界。生产实现还要补充密码库、并发、持久化、超时、整数范围与密钥管理，教学骨架不应直接连接真实资产。

## 正式目录逐项讲解

### 第9章 Solidity合约编译、部署

“第9章 Solidity合约编译、部署”位于本页正式目录坐标 1/9。合约是由交易触发的确定性状态机。调用者、输入、前状态、代码和执行环境共同决定后状态；权限、外部调用、资源上限和失败回滚必须显式验证。

复核时记录前状态、输入、调用者或验证者、候选输出、拒绝原因和后状态；正常、边界与单故障样本除目标变量外保持一致。通过标准是“能锁定solc版本复现字节码，部署到隔离链，校验地址上的运行时代码，并区分只读调用、状态交易和失败回执。”，并保留原始响应、状态摘要、版本和首个失败点。

### 9.1 编译合约

“9.1 编译合约”位于本页正式目录坐标 2/9。合约是由交易触发的确定性状态机。调用者、输入、前状态、代码和执行环境共同决定后状态；权限、外部调用、资源上限和失败回滚必须显式验证。

复核时记录前状态、输入、调用者或验证者、候选输出、拒绝原因和后状态；正常、边界与单故障样本除目标变量外保持一致。通过标准是“能锁定solc版本复现字节码，部署到隔离链，校验地址上的运行时代码，并区分只读调用、状态交易和失败回执。”，并保留原始响应、状态摘要、版本和首个失败点。

### 9.1.1 安装solc编译工具

“9.1.1 安装solc编译工具”位于本页正式目录坐标 3/9。平台安装路径不同，但验收合同一致：来源可信、摘要匹配、版本明确、架构正确、数据目录隔离、端口最小暴露。包管理器安装成功不能替代这些检查。

复核时记录前状态、输入、调用者或验证者、候选输出、拒绝原因和后状态；正常、边界与单故障样本除目标变量外保持一致。通过标准是“能锁定solc版本复现字节码，部署到隔离链，校验地址上的运行时代码，并区分只读调用、状态交易和失败回执。”，并保留原始响应、状态摘要、版本和首个失败点。

### 9.1.2 开始编译合约

“9.1.2 开始编译合约”位于本页正式目录坐标 4/9。合约是由交易触发的确定性状态机。调用者、输入、前状态、代码和执行环境共同决定后状态；权限、外部调用、资源上限和失败回滚必须显式验证。

复核时记录前状态、输入、调用者或验证者、候选输出、拒绝原因和后状态；正常、边界与单故障样本除目标变量外保持一致。通过标准是“能锁定solc版本复现字节码，部署到隔离链，校验地址上的运行时代码，并区分只读调用、状态交易和失败回执。”，并保留原始响应、状态摘要、版本和首个失败点。

### 9.2 部署合约

“9.2 部署合约”位于本页正式目录坐标 5/9。合约是由交易触发的确定性状态机。调用者、输入、前状态、代码和执行环境共同决定后状态；权限、外部调用、资源上限和失败回滚必须显式验证。

复核时记录前状态、输入、调用者或验证者、候选输出、拒绝原因和后状态；正常、边界与单故障样本除目标变量外保持一致。通过标准是“能锁定solc版本复现字节码，部署到隔离链，校验地址上的运行时代码，并区分只读调用、状态交易和失败回执。”，并保留原始响应、状态摘要、版本和首个失败点。

### 9.2.1 启动以太坊geth节点

“9.2.1 启动以太坊geth节点”位于本页正式目录坐标 6/9。部署不是上传源文件，而是用固定编译器把源码转成创建字节码，由签名交易执行构造逻辑并产生地址。源码、设置、ABI、字节码、交易和运行时代码必须可相互验证。

复核时记录前状态、输入、调用者或验证者、候选输出、拒绝原因和后状态；正常、边界与单故障样本除目标变量外保持一致。通过标准是“能锁定solc版本复现字节码，部署到隔离链，校验地址上的运行时代码，并区分只读调用、状态交易和失败回执。”，并保留原始响应、状态摘要、版本和首个失败点。

### 9.2.2 部署智能合约

“9.2.2 部署智能合约”位于本页正式目录坐标 7/9。合约是由交易触发的确定性状态机。调用者、输入、前状态、代码和执行环境共同决定后状态；权限、外部调用、资源上限和失败回滚必须显式验证。

复核时记录前状态、输入、调用者或验证者、候选输出、拒绝原因和后状态；正常、边界与单故障样本除目标变量外保持一致。通过标准是“能锁定solc版本复现字节码，部署到隔离链，校验地址上的运行时代码，并区分只读调用、状态交易和失败回执。”，并保留原始响应、状态摘要、版本和首个失败点。

### 9.3 调用合约

“9.3 调用合约”位于本页正式目录坐标 8/9。合约是由交易触发的确定性状态机。调用者、输入、前状态、代码和执行环境共同决定后状态；权限、外部调用、资源上限和失败回滚必须显式验证。

复核时记录前状态、输入、调用者或验证者、候选输出、拒绝原因和后状态；正常、边界与单故障样本除目标变量外保持一致。通过标准是“能锁定solc版本复现字节码，部署到隔离链，校验地址上的运行时代码，并区分只读调用、状态交易和失败回执。”，并保留原始响应、状态摘要、版本和首个失败点。

### 9.4 本章小结

“9.4 本章小结”位于本页正式目录坐标 9/9。“第9章 Solidity合约编译、部署”的收束不是名词列表，而是把对象、授权、执行顺序、状态不变量和版本边界连接成一条可重放轨迹。

以“能锁定solc版本复现字节码，部署到隔离链，校验地址上的运行时代码，并区分只读调用、状态交易和失败回执。”作为通过标准；只展示成功截图、最终余额或交易哈希都不构成完整证据。通过标准是“能锁定solc版本复现字节码，部署到隔离链，校验地址上的运行时代码，并区分只读调用、状态交易和失败回执。”，并保留原始响应、状态摘要、版本和首个失败点。

## 正常、边界与故障矩阵

| 样本   | 只改变的变量                      | 预期结果               | 必存证据                   |
| ------ | --------------------------------- | ---------------------- | -------------------------- |
| 正常   | 合法输入与足额资源                | 状态提交并达到确认阈值 | 前后状态、哈希、回执、版本 |
| 边界   | 零值、最大值或恰好阈值            | 按明确规则接受或拒绝   | 边界值、判断分支、资源消耗 |
| 单故障 | 签名、nonce、网络、权限或链接一项 | 提交前拒绝且原状态不变 | 错误对象、首个差异、回退点 |

测试先手算预期，再运行实现，并从独立进程读取最终状态。若前端、客户端、节点和链上状态给出不同答案，以原始协议对象和固定区块哈希为复核坐标；不得用刷新页面、提高超时或重复广播掩盖不一致。

## 三步交互实验

<Stepper>
  <Step title="追踪处理流水线">
    先预测当前阶段的输入、验证依据与候选状态，再拖动阶段滑块定位首个分叉。
    <Bdp09ContractCompileDeployFlowLab />
  </Step>
  <Step title="改变资源与节点条件">
    一次只调整节点数或资源预算，观察可用性、确认与执行门槛怎样变化。
    <Bdp09ContractCompileDeployExperimentLab />
  </Step>
  <Step title="注入单一故障">
    注入签名、网络、接口或状态故障，要求证据门禁在提交前给出拒绝原因和回退点。
    <Bdp09ContractCompileDeployEvidenceLab />
  </Step>
</Stepper>

## 常见失败与回退

<Callout type="trap">
  **把交易哈希当成功：**
  哈希只标识候选交易。还要检查节点接收、入块、执行状态、事件、后状态和确认深度。
</Callout>

<Callout type="trap">
  **把2018年命令当当前接口：**
  原书中的Geth、TestRPC、browser-solidity、Truffle、Solidity和比特币参数属于历史版本。重放使用隔离环境，现代部署使用当前文档与安全默认值。
</Callout>

<Callout type="tip">
  **回退顺序：** 书目与软件版本 -> 网络和创世身份 -> 编码与签名 -> 请求或交易规则 -> 执行与回执 -> 确认与索引视图。首个差异未解释前不比较最终余额。
</Callout>

## 本章回顾

把源代码、编译器、ABI、字节码、部署交易、合约地址和调用回执连成证据链。 部署不是上传源文件，而是用固定编译器把源码转成创建字节码，由签名交易执行构造逻辑并产生地址。源码、设置、ABI、字节码、交易和运行时代码必须可相互验证。 掌握标准不是记住命令或界面，而是能锁定solc版本复现字节码，部署到隔离链，校验地址上的运行时代码，并区分只读调用、状态交易和失败回执。。发布前必须由独立观察者从来源和固定配置重放正常、边界及单故障样本。

<Glossary>

<GlossaryItem term="solc">
  “solc”用于定位第9章
  Solidity合约编译、部署的第1类证据；定义必须包含输入、验证规则、状态变化、版本和拒绝路径。
</GlossaryItem>

<GlossaryItem term="ABI">
  “ABI”用于定位第9章
  Solidity合约编译、部署的第2类证据；定义必须包含输入、验证规则、状态变化、版本和拒绝路径。
</GlossaryItem>

<GlossaryItem term="字节码">
  “字节码”用于定位第9章
  Solidity合约编译、部署的第3类证据；定义必须包含输入、验证规则、状态变化、版本和拒绝路径。
</GlossaryItem>

<GlossaryItem term="部署交易">
  “部署交易”用于定位第9章
  Solidity合约编译、部署的第4类证据；定义必须包含输入、验证规则、状态变化、版本和拒绝路径。
</GlossaryItem>

<GlossaryItem term="合约地址">
  “合约地址”用于定位第9章
  Solidity合约编译、部署的第5类证据；定义必须包含输入、验证规则、状态变化、版本和拒绝路径。
</GlossaryItem>

</Glossary>

<Exercises>

1. 如何为第9章 Solidity合约编译、部署建立最小可重放基线？

<Answer>
  固定2018年版目录来源、软件版本、网络身份、前状态、调用者、输入、验证规则和确认条件，保存每一步判断、输出与拒绝原因。
</Answer>

2. 如何证明“能锁定solc版本复现字节码，部署到隔离链，校验地址上的运行时代码，并区分只读调用、状态交易和失败回执。”不是一次偶然成功？

<Answer>
  先手算最小实例，再对同一合同执行正常、边界和单故障样本；由独立进程读取原始对象和后状态，逐项一致才通过。
</Answer>

3. 修复第9章 Solidity合约编译、部署后怎样回归？

<Answer>
  先重放原失败，再运行相邻边界和正常基线，按版本、网络、编码、签名、接口、执行、状态和确认定位首个差异，全部不变量通过后发布。
</Answer>

</Exercises>

## 前后导航

- [上一页：第8章 Solidity语法详解](/learn/blockchain-dev-practice/09-08-solidity-syntax/bdp-08-solidity-syntax)
- [下一页：第10章 Truffle详解](/learn/blockchain-dev-practice/11-10-truffle/bdp-10-truffle)

<Attribution
  adaptedFrom="吴寿鹤、冯翔、刘涛、周广益《区块链开发实战：以太坊关键技术与案例分析》，机械工业出版社，2018年6月，227页，ISBN 9787111599562 · 基于完整目录185个正式层级的独立教学重构"
  adaptedUrl="https://www.dedao.cn/ebook/detail?id=bBVDEXGGLn7eB51b8NjVRqDoQJPMk3aKmq0adYrXmAxE4Ov92lgzK6ZypxLqdQjp"
/>
