---
title: "第1章 初识区块链"
type: C
section: "白话区块链（2017版）"
order: 3
description: "从共同记账的生活类比进入区块、P2P、共识、脚本与比特币实现，建立可验证而非口号式的区块链定义。"
demo: true
math: false
sourceUrl: "https://e.dangdang.com/products/1901310198.html"
draft: true
---

import {
  Bp01FirstBlockchainFlowLab,
  Bp01FirstBlockchainExperimentLab,
  Bp01FirstBlockchainEvidenceLab,
} from "@/components/mdx/blockchain-plain/diagrams/bp-01-first-blockchain";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释公共账本、哈希链与P2P节点在账本状态转移中的职责和假设边界
- 能推导区块链接、交易授权、确认权重与证据门禁四个表达式
- 能实现三段最小代码并追踪输入、验证、排序、执行、提交和拒绝路径
- 能设计正常、边界和单故障样本，独立证明“能追踪一笔UTXO交易从钱包签名、节点验证、矿工打包到后续区块确认的完整状态变化。”

</Objectives>

## 为什么从可验证状态开始

<Term def="公共账本是第1章 初识区块链的第1个复核坐标，必须绑定输入、规则、状态和失败边界。">公共账本</Term>、<Term def="哈希链是第1章 初识区块链的第2个复核坐标，必须能够从独立证据重放。">哈希链</Term>、<Term def="P2P节点是第1章 初识区块链的第3个复核坐标，不能用最终结果替代过程。">P2P节点</Term>、<Term def="UTXO是第1章 初识区块链的第4个复核坐标，要求正常与失败样本使用同一合同。">UTXO</Term>
和
<Term def="独立验证是第1章 初识区块链的第5个复核坐标，发布前必须说明不可证明的部分。">独立验证</Term>
共同构成本页坐标。从共同记账的生活类比进入区块、P2P、共识、脚本与比特币实现，建立可验证而非口号式的区块链定义。

本书锁定蒋勇、文延、嘉文著《白话区块链》，机械工业出版社2017年10月版，236页，ISBN 9787111582984。当当云阅读给出技术审校、前言、9章全部编号节与小节以及后记的完整目录；得到与天珑书店分别交叉核对出版方、作者、日期、页数和ISBN。本文是独立教学重构，不复制原书正文。

区块链不是“广播后大家自然相信”，而是候选状态经过本地规则验证、网络传播、排序或共识，再提交为节点认可历史。任何结论都要回答五个问题：谁能发起，谁能验证，按什么顺序，失败时保留哪个状态，观察者凭什么复核。

<Callout type="info">
  **验收命题：**
  能追踪一笔UTXO交易从钱包签名、节点验证、矿工打包到后续区块确认的完整状态变化。
</Callout>

## 区块、交易与证据不变量

$$
H_i = hash(H_{i-1} + merkle(T_i) + time_i + nonce_i)
$$

$$
valid(T) = signature(T, publicKey) and unspent(inputs) and valueIn >= valueOut
$$

$$
confirmed(T, k) = valid(T) and depth(T) >= k and canonical(block(T))
$$

$$
gate = source * replay * boundary * failureEvidence
$$

第一式把新区块绑定到前块摘要与交易集合；任一字段改变都会改变摘要，但只有独立验证和共识选择才能决定候选链是否被接受。第二式把签名授权、未花费约束和价值守恒分开，签名正确不代表输入尚未消费。第三式说明确认依赖规范链和深度，不能把“收到交易”写成“最终结算”。第四式要求来源、重放、边界与失败证据同时存在，任一为零都不能发布。

取三笔UTXO：A拥有7与4，拟向B支付8并找零3。合法交易必须引用两笔输入、由A签名并产生8与3两个输出；若仅引用7、重复引用同一输入或把找零写成4，节点应在进入候选区块前拒绝。把合法交易放入区块后修改金额，重算Merkle根和区块摘要虽可让单块内部一致，却会破坏后续链接和累计共识证据。

## 三段可运行骨架

```python
def verify_transaction(tx, utxos, verify_signature):
    inputs = [utxos.get(ref) for ref in tx["inputs"]]
    if None in inputs or len(set(tx["inputs"])) != len(tx["inputs"]):
        return False
    return verify_signature(tx) and sum(inputs) >= sum(tx["outputs"])
```

```python
def append_block(chain, block, hash_block, valid_tx):
    if block["previous"] != hash_block(chain[-1]):
        return {"accepted": False, "reason": "broken-link"}
    if not all(valid_tx(tx) for tx in block["transactions"]):
        return {"accepted": False, "reason": "invalid-transaction"}
    return {"accepted": True, "chain": [*chain, block]}
```

```python
def evidence_gate(run):
    required = {"source", "version", "input", "trace", "output", "failure"}
    missing = sorted(required.difference(run))
    return {"pass": not missing, "missing": missing}
```

第一段验证交易引用、重复输入、签名和价值约束；生产实现还要检查脚本、费用、锁定时间与整数范围。第二段先检查前块链接再检查交易，失败时不修改原链。第三段对实验记录做结构门禁，使成功轨迹和失败证据使用同一版本合同。

## 最小实例与重放顺序

先冻结创世状态、账户或UTXO集合、验证函数、网络成员、排序规则、确认条件和软件版本。按“构造交易 -> 本地签名 -> 邻居独立验证 -> 候选池 -> 打包或排序 -> 区块验证 -> 状态提交”记录每一步输入输出。只保存最后余额，会漏掉重复消费、错误签名被缓存、无效区块短暂传播或回滚未清理等问题。

正常样本使用两笔输入支付并找零；边界样本让输入恰好等于输出；单故障样本只破坏签名、前块摘要、背书数量或读版本中的一项。三组样本除该变量外保持相同，才能把拒绝原因归因到目标规则。随机网络实验保存种子、消息顺序和超时，不以一次收敛证明协议活性。

## 2017年版本边界与现代迁移

原书记录的是2017年前后的比特币客户端、以太坊钱包、Fabric版本与当时的扩容和安全事件。今天重放时保留概念坐标和状态不变量，但不照抄已经失效的下载地址、网络参数、命令或默认配置。历史截图用于解释接口，不作为当前安全保证。

迁移到现代实现时建立对应表：原书术语、历史行为、当前替代、兼容差异、验证方法和回退方案。以太坊共识变更、Fabric架构演进或钱包弃用必须显式披露；不能因为现代工具运行成功，就声称原书命令仍然有效，也不能用今天的特性改写当年的设计动机。

## 正式目录逐项讲解

### 第1章 初识区块链

“第1章 初识区块链”位于本页正式目录坐标 1/18。共同账本的关键不是人人拥有一份文件，而是每个节点依据同一验证规则独立接受状态转移。广播只传播候选数据，验证、排序和确认才把候选写入本地认可历史。

复核时为“第1章 初识区块链”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 1.1 例说区块链

“1.1 例说区块链”位于本页正式目录坐标 2/18。共同账本的关键不是人人拥有一份文件，而是每个节点依据同一验证规则独立接受状态转移。广播只传播候选数据，验证、排序和确认才把候选写入本地认可历史。

复核时为“1.1 例说区块链”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 1.1.1 从一本账本说起

“1.1.1 从一本账本说起”位于本页正式目录坐标 3/18。共同账本的关键不是人人拥有一份文件，而是每个节点依据同一验证规则独立接受状态转移。广播只传播候选数据，验证、排序和确认才把候选写入本地认可历史。

复核时为“1.1.1 从一本账本说起”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 1.1.2 区块链技术理念

“1.1.2 区块链技术理念”位于本页正式目录坐标 4/18。共同账本的关键不是人人拥有一份文件，而是每个节点依据同一验证规则独立接受状态转移。广播只传播候选数据，验证、排序和确认才把候选写入本地认可历史。

复核时为“1.1.2 区块链技术理念”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 1.1.3 一般工作流程

“1.1.3 一般工作流程”位于本页正式目录坐标 5/18。共同账本的关键不是人人拥有一份文件，而是每个节点依据同一验证规则独立接受状态转移。广播只传播候选数据，验证、排序和确认才把候选写入本地认可历史。

复核时为“1.1.3 一般工作流程”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 1.2 区块链技术栈

“1.2 区块链技术栈”位于本页正式目录坐标 6/18。把系统拆成数据结构、密码、P2P传播、共识、激励、脚本或合约和应用接口。公有、联盟与私有的差异首先来自准入和失败模型，而不是界面上是否显示区块。

复核时为“1.2 区块链技术栈”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 1.3 区块链分类与架构

“1.3 区块链分类与架构”位于本页正式目录坐标 7/18。把系统拆成数据结构、密码、P2P传播、共识、激励、脚本或合约和应用接口。公有、联盟与私有的差异首先来自准入和失败模型，而不是界面上是否显示区块。

复核时为“1.3 区块链分类与架构”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 1.3.1 区块链架构

“1.3.1 区块链架构”位于本页正式目录坐标 8/18。把系统拆成数据结构、密码、P2P传播、共识、激励、脚本或合约和应用接口。公有、联盟与私有的差异首先来自准入和失败模型，而不是界面上是否显示区块。

复核时为“1.3.1 区块链架构”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 1.3.2 区块链分类

“1.3.2 区块链分类”位于本页正式目录坐标 9/18。把系统拆成数据结构、密码、P2P传播、共识、激励、脚本或合约和应用接口。公有、联盟与私有的差异首先来自准入和失败模型，而不是界面上是否显示区块。

复核时为“1.3.2 区块链分类”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 1.4 一切源自比特币

“1.4 一切源自比特币”位于本页正式目录坐标 10/18。比特币把UTXO所有权条件、交易签名、区块工作量和最长累计工作链组合起来。钱包构造并签名候选交易，全节点独立验证，矿工只能选择合法交易排序，不能替其他所有者签名。

复核时为“1.4 一切源自比特币”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 1.4.1 比特币技术论文介绍

“1.4.1 比特币技术论文介绍”位于本页正式目录坐标 11/18。比特币把UTXO所有权条件、交易签名、区块工作量和最长累计工作链组合起来。钱包构造并签名候选交易，全节点独立验证，矿工只能选择合法交易排序，不能替其他所有者签名。

复核时为“1.4.1 比特币技术论文介绍”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 1.4.2 比特币核心程序：中本聪客户端

“1.4.2 比特币核心程序：中本聪客户端”位于本页正式目录坐标 12/18。比特币把UTXO所有权条件、交易签名、区块工作量和最长累计工作链组合起来。钱包构造并签名候选交易，全节点独立验证，矿工只能选择合法交易排序，不能替其他所有者签名。

复核时为“1.4.2 比特币核心程序：中本聪客户端”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 1.4.3 比特币的发行：挖矿

“1.4.3 比特币的发行：挖矿”位于本页正式目录坐标 13/18。比特币把UTXO所有权条件、交易签名、区块工作量和最长累计工作链组合起来。钱包构造并签名候选交易，全节点独立验证，矿工只能选择合法交易排序，不能替其他所有者签名。

复核时为“1.4.3 比特币的发行：挖矿”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 1.4.4 比特币钱包：核心钱包与轻钱包

“1.4.4 比特币钱包：核心钱包与轻钱包”位于本页正式目录坐标 14/18。比特币把UTXO所有权条件、交易签名、区块工作量和最长累计工作链组合起来。钱包构造并签名候选交易，全节点独立验证，矿工只能选择合法交易排序，不能替其他所有者签名。

复核时为“1.4.4 比特币钱包：核心钱包与轻钱包”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 1.4.5 比特币账户模型：UTXO

“1.4.5 比特币账户模型：UTXO”位于本页正式目录坐标 15/18。比特币把UTXO所有权条件、交易签名、区块工作量和最长累计工作链组合起来。钱包构造并签名候选交易，全节点独立验证，矿工只能选择合法交易排序，不能替其他所有者签名。

复核时为“1.4.5 比特币账户模型：UTXO”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 1.4.6 动手编译比特币源码

“1.4.6 动手编译比特币源码”位于本页正式目录坐标 16/18。比特币把UTXO所有权条件、交易签名、区块工作量和最长累计工作链组合起来。钱包构造并签名候选交易，全节点独立验证，矿工只能选择合法交易排序，不能替其他所有者签名。

复核时为“1.4.6 动手编译比特币源码”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 1.5 区块链的技术意义

“1.5 区块链的技术意义”位于本页正式目录坐标 17/18。技术意义在于把多方状态转移的验证权从单一数据库管理员扩散到协议参与者，同时付出重复存储、共识延迟和治理升级成本；它不是对所有数据库问题的默认替代。

复核时为“1.5 区块链的技术意义”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 1.6 知识点导图

“1.6 知识点导图”位于本页正式目录坐标 18/18。知识点导图用于检查依赖：从状态对象指向授权证据、传播、排序、执行和风险；它是回放索引，不替代定义、公式、代码和失败样本。每条箭头都应能落到一个可观察输入输出。

复核时为“1.6 知识点导图”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

## 六类复核证据

### 状态与所有权合同

列出链上对象的唯一标识、所有者或解锁条件、当前版本和允许的状态转移。UTXO模型检查引用与未花费性，账户模型检查发送者、nonce与余额；两者不能共用一个含糊的“余额正确”断言。

### 密码与编码合同

记录哈希和签名算法、参数、密钥来源、随机数、序列化与地址校验。编码只解决传输表示，哈希只提供摘要，签名只证明相应私钥授权；真实性、保密性与业务合法性需要额外证据。

### 网络与共识合同

保存节点身份、连接、消息、传播顺序、故障模型、候选集合和规范历史选择。共识安全性与活性分别验证；暂时不出块和两个节点接受冲突最终状态是不同故障，不能只用平均TPS描述。

### 执行与平台合同

合约或链码记录前状态、调用者、输入、读写集、资源消耗、事件和后状态。失败执行必须原子回滚，外部服务和预言机响应进入证据，避免把链外副作用误当成虚拟机可回滚状态。

### 攻击与恢复合同

每个风险写出资产、攻击者能力、入口、首个可观测异常、影响和恢复边界。私钥丢失、多数重组、合约权限泄漏与数据增长的恢复手段不同；“不可篡改”不能替代备份、暂停、升级和治理。

### 发布与独立复核合同

独立复核者从空目录读取来源与配置，先手算最小状态，再运行正常、边界和单故障样本。哈希、签名、事件、余额、拒绝原因和最终状态逐项一致后才发布；任何缺失证据都进入隔离清单而非静默跳过。

## 三步交互实验

<Stepper>
  <Step title="追踪状态链">
    先预测当前阶段应看到的输入、验证依据和后状态，再拖动滑块逐步检查首个分叉。
    <Bp01FirstBlockchainFlowLab />
  </Step>
  <Step title="改变网络条件">
    一次只调整节点规模或确认阈值，观察吞吐、最终性和验证成本如何一起变化。
    <Bp01FirstBlockchainExperimentLab />
  </Step>
  <Step title="注入单一故障">
    注入签名、链接、共识或状态故障，要求门禁在提交前给出拒绝原因与回退点。
    <Bp01FirstBlockchainEvidenceLab />
  </Step>
</Stepper>

## 常见失败与回退

<Callout type="trap">
  **把广播当确认：**
  节点看见交易或区块只代表收到候选数据。没有本地验证、规范历史和确认条件，不能把它记为不可逆结算。
</Callout>

<Callout type="trap">
  **把历史命令当当前接口：**
  2017年的钱包、以太坊和Fabric工具链已经演进。直接复制旧命令既可能失败，也可能连接错误网络；先建立版本映射和隔离环境。
</Callout>

<Callout type="tip">
  **回退顺序：** 书目版本 -> 序列化与哈希 -> 密钥和签名 -> 交易规则 -> 网络消息 -> 共识选择 -> 状态提交 -> 业务断言。首个差异未解释前不比较最终指标。
</Callout>

## 本章回顾

从共同记账的生活类比进入区块、P2P、共识、脚本与比特币实现，建立可验证而非口号式的区块链定义。 掌握标准不是背诵项目名或算法缩写，而是能冻结状态、授权、传播、排序、执行、证据和失败边界。通过标准为：能追踪一笔UTXO交易从钱包签名、节点验证、矿工打包到后续区块确认的完整状态变化。

<Glossary>

<GlossaryItem term="公共账本">
  “公共账本”是第1章
  初识区块链的第1个复核坐标；定义必须绑定对象、输入、验证规则、状态变化、版本和失败证据。
</GlossaryItem>

<GlossaryItem term="哈希链">
  “哈希链”是第1章
  初识区块链的第2个复核坐标；定义必须绑定对象、输入、验证规则、状态变化、版本和失败证据。
</GlossaryItem>

<GlossaryItem term="P2P节点">
  “P2P节点”是第1章
  初识区块链的第3个复核坐标；定义必须绑定对象、输入、验证规则、状态变化、版本和失败证据。
</GlossaryItem>

<GlossaryItem term="UTXO">
  “UTXO”是第1章
  初识区块链的第4个复核坐标；定义必须绑定对象、输入、验证规则、状态变化、版本和失败证据。
</GlossaryItem>

<GlossaryItem term="独立验证">
  “独立验证”是第1章
  初识区块链的第5个复核坐标；定义必须绑定对象、输入、验证规则、状态变化、版本和失败证据。
</GlossaryItem>

</Glossary>

<Exercises>

1. 如何为第1章 初识区块链建立最小可重放基线？

<Answer>
  固定2017年版目录来源、创世状态、对象模型、验证规则、节点、消息顺序、软件版本和确认条件，保存每一步输入、判断、输出与拒绝原因。
</Answer>

2. 如何证明“能追踪一笔UTXO交易从钱包签名、节点验证、矿工打包到后续区块确认的完整状态变化。”不是一次偶然成功？

<Answer>
  先手算最小实例，再对同一合同执行正常、边界和单故障样本；由独立进程重放哈希、签名、排序和状态，逐项一致才通过。
</Answer>

3. 修复第1章 初识区块链后怎样回归？

<Answer>
  先重放原失败，再运行相邻边界和正常基线，按来源、序列化、签名、交易、消息、共识与状态定位首个差异，全部不变量通过后才发布。
</Answer>

</Exercises>

## 前后导航

- [上一页：前言](/learn/blockchain-plain/02-preface/bp-preface)
- [下一页：第2章 区块链应用发展](/learn/blockchain-plain/04-02-application-development/bp-02-application-development)

<Attribution
  adaptedFrom="蒋勇、文延、嘉文《白话区块链》，机械工业出版社，2017年10月，236页，ISBN 9787111582984 · 基于完整目录119个正式层级的独立教学重构"
  adaptedUrl="https://e.dangdang.com/products/1901310198.html"
/>
