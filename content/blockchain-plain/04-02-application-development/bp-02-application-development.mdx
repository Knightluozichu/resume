---
title: "第2章 区块链应用发展"
type: C
section: "白话区块链（2017版）"
order: 4
description: "比较数字货币、智能合约、结算、分布式文件、公证溯源与供应链金融，区分链上证明和现实履约。"
demo: true
math: false
sourceUrl: "https://e.dangdang.com/products/1901310198.html"
draft: false
---


## 为什么从可验证状态开始

、、、和
共同构成本页坐标。比较数字货币、智能合约、结算、分布式文件、公证溯源与供应链金融，区分链上证明和现实履约。

本书锁定蒋勇、文延、嘉文著《白话区块链》，机械工业出版社2017年10月版，236页，ISBN 9787111582984。当当云阅读给出技术审校、前言、9章全部编号节与小节以及后记的完整目录；得到与天珑书店分别交叉核对出版方、作者、日期、页数和ISBN。本文是独立教学重构，不复制原书正文。

区块链不是“广播后大家自然相信”，而是候选状态经过本地规则验证、网络传播、排序或共识，再提交为节点认可历史。任何结论都要回答五个问题：谁能发起，谁能验证，按什么顺序，失败时保留哪个状态，观察者凭什么复核。


## 区块、交易与证据不变量

$$
H_i = hash(H_{i-1} + merkle(T_i) + time_i + nonce_i)
$$

$$
valid(T) = signature(T, publicKey) and unspent(inputs) and valueIn >= valueOut
$$

$$
confirmed(T, k) = valid(T) and depth(T) >= k and canonical(block(T))
$$

$$
gate = source * replay * boundary * failureEvidence
$$

第一式把新区块绑定到前块摘要与交易集合；任一字段改变都会改变摘要，但只有独立验证和共识选择才能决定候选链是否被接受。第二式把签名授权、未花费约束和价值守恒分开，签名正确不代表输入尚未消费。第三式说明确认依赖规范链和深度，不能把“收到交易”写成“最终结算”。第四式要求来源、重放、边界与失败证据同时存在，任一为零都不能发布。

取三笔UTXO：A拥有7与4，拟向B支付8并找零3。合法交易必须引用两笔输入、由A签名并产生8与3两个输出；若仅引用7、重复引用同一输入或把找零写成4，节点应在进入候选区块前拒绝。把合法交易放入区块后修改金额，重算Merkle根和区块摘要虽可让单块内部一致，却会破坏后续链接和累计共识证据。

## 三段可运行骨架

以下代码展示应用比较的两层视角：核心逻辑层验证链上状态转移，证据门禁层区分链上证明与链外履约的边界。

<CodeTabs>
  <Tab label="Python / 核心逻辑">

```python
def verify_transaction(tx, utxos, verify_signature):
    inputs = [utxos.get(ref) for ref in tx["inputs"]]
    if None in inputs or len(set(tx["inputs"])) != len(tx["inputs"]):
        return False
    return verify_signature(tx) and sum(inputs) >= sum(tx["outputs"])

def append_block(chain, block, hash_block, valid_tx):
    if block["previous"] != hash_block(chain[-1]):
        return {"accepted": False, "reason": "broken-link"}
    if not all(valid_tx(tx) for tx in block["transactions"]):
        return {"accepted": False, "reason": "invalid-transaction"}
    return {"accepted": True, "chain": [*chain, block]}
```

  </Tab>
  <Tab label="Python / 证据门禁">

```python
def evidence_gate(run):
    required = {"source", "version", "input", "trace", "output", "failure"}
    missing = sorted(required.difference(run))
    return {"pass": not missing, "missing": missing}
```

  </Tab>
</CodeTabs>

第一段验证交易引用、重复输入、签名和价值约束；生产实现还要检查脚本、费用、锁定时间与整数范围。第二段先检查前块链接再检查交易，失败时不修改原链。第三段对实验记录做结构门禁，使成功轨迹和失败证据使用同一版本合同。

## 最小实例与重放顺序

先冻结创世状态、账户或UTXO集合、验证函数、网络成员、排序规则、确认条件和软件版本。按“构造交易 -> 本地签名 -> 邻居独立验证 -> 候选池 -> 打包或排序 -> 区块验证 -> 状态提交”记录每一步输入输出。只保存最后余额，会漏掉重复消费、错误签名被缓存、无效区块短暂传播或回滚未清理等问题。

正常样本使用两笔输入支付并找零；边界样本让输入恰好等于输出；单故障样本只破坏签名、前块摘要、背书数量或读版本中的一项。三组样本除该变量外保持相同，才能把拒绝原因归因到目标规则。随机网络实验保存种子、消息顺序和超时，不以一次收敛证明协议活性。

## 2017年版本边界与现代迁移

原书记录的是2017年前后的比特币客户端、以太坊钱包、Fabric版本与当时的扩容和安全事件。今天重放时保留概念坐标和状态不变量，但不照抄已经失效的下载地址、网络参数、命令或默认配置。历史截图用于解释接口，不作为当前安全保证。

迁移到现代实现时建立对应表：原书术语、历史行为、当前替代、兼容差异、验证方法和回退方案。以太坊共识变更、Fabric架构演进或钱包弃用必须显式披露；不能因为现代工具运行成功，就声称原书命令仍然有效，也不能用今天的特性改写当年的设计动机。

## 正式目录逐项讲解

### 第2章 区块链应用发展

“第2章 区块链应用发展”位于本页正式目录坐标 1/19。这一目录项要落到状态对象、授权规则、传播顺序和失败边界。先写最小输入输出，再用正常、边界与单故障样本验证，避免只记名词而无法判断实现是否正确。

复核时为“第2章 区块链应用发展”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 2.1 比特币及其朋友圈：加密数字货币

“2.1 比特币及其朋友圈：加密数字货币”位于本页正式目录坐标 2/19。不同数字货币改变区块参数、哈希工作、隐私模型或治理路径。比较时必须固定安全预算、节点可验证成本、确认定义和隐私泄漏面，不能只比较币名、TPS或市场表现。

复核时为“2.1 比特币及其朋友圈：加密数字货币”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 2.1.1 以太坊

“2.1.1 以太坊”位于本页正式目录坐标 3/19。以太坊把交易扩展为确定性状态机调用。执行者从同一前状态、字节码和输入得到同一后状态；Gas限制资源，回滚保护原子性，但代码确定性并不保证业务意图、外部价格或权限配置正确。

复核时为“2.1.1 以太坊”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 2.1.2 比特币现金

“2.1.2 比特币现金”位于本页正式目录坐标 4/19。不同数字货币改变区块参数、哈希工作、隐私模型或治理路径。比较时必须固定安全预算、节点可验证成本、确认定义和隐私泄漏面，不能只比较币名、TPS或市场表现。

复核时为“2.1.2 比特币现金”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 2.1.3 莱特币

“2.1.3 莱特币”位于本页正式目录坐标 5/19。不同数字货币改变区块参数、哈希工作、隐私模型或治理路径。比较时必须固定安全预算、节点可验证成本、确认定义和隐私泄漏面，不能只比较币名、TPS或市场表现。

复核时为“2.1.3 莱特币”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 2.1.4 零币

“2.1.4 零币”位于本页正式目录坐标 6/19。不同数字货币改变区块参数、哈希工作、隐私模型或治理路径。比较时必须固定安全预算、节点可验证成本、确认定义和隐私泄漏面，不能只比较币名、TPS或市场表现。

复核时为“2.1.4 零币”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 2.1.5 数字货币发展总结

“2.1.5 数字货币发展总结”位于本页正式目录坐标 7/19。不同数字货币改变区块参数、哈希工作、隐私模型或治理路径。比较时必须固定安全预算、节点可验证成本、确认定义和隐私泄漏面，不能只比较币名、TPS或市场表现。

复核时为“2.1.5 数字货币发展总结”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 2.2 区块链扩展应用：智能合约

“2.2 区块链扩展应用：智能合约”位于本页正式目录坐标 8/19。以太坊把交易扩展为确定性状态机调用。执行者从同一前状态、字节码和输入得到同一后状态；Gas限制资源，回滚保护原子性，但代码确定性并不保证业务意图、外部价格或权限配置正确。

复核时为“2.2 区块链扩展应用：智能合约”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 2.2.1 比特币中包含的合约思想

“2.2.1 比特币中包含的合约思想”位于本页正式目录坐标 9/19。这一目录项要落到状态对象、授权规则、传播顺序和失败边界。先写最小输入输出，再用正常、边界与单故障样本验证，避免只记名词而无法判断实现是否正确。

复核时为“2.2.1 比特币中包含的合约思想”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 2.2.2 以太坊中图灵完备的合约支持

“2.2.2 以太坊中图灵完备的合约支持”位于本页正式目录坐标 10/19。以太坊把交易扩展为确定性状态机调用。执行者从同一前状态、字节码和输入得到同一后状态；Gas限制资源，回滚保护原子性，但代码确定性并不保证业务意图、外部价格或权限配置正确。

复核时为“2.2.2 以太坊中图灵完备的合约支持”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 2.3 交易结算

“2.3 交易结算”位于本页正式目录坐标 11/19。清算计算各方净头寸，结算完成资产最终交割。链上确认只覆盖协议内状态；法币账户、司法冻结和对手方信用仍在链外，因此需要明确最终性与追索规则。

复核时为“2.3 交易结算”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 2.3.1 银行结算清算

“2.3.1 银行结算清算”位于本页正式目录坐标 12/19。清算计算各方净头寸，结算完成资产最终交割。链上确认只覆盖协议内状态；法币账户、司法冻结和对手方信用仍在链外，因此需要明确最终性与追索规则。

复核时为“2.3.1 银行结算清算”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 2.3.2 瑞波：开放支付网络

“2.3.2 瑞波：开放支付网络”位于本页正式目录坐标 13/19。清算计算各方净头寸，结算完成资产最终交割。链上确认只覆盖协议内状态；法币账户、司法冻结和对手方信用仍在链外，因此需要明确最终性与追索规则。

复核时为“2.3.2 瑞波：开放支付网络”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 2.4 IPFS：星际文件系统

“2.4 IPFS：星际文件系统”位于本页正式目录坐标 14/19。内容寻址可证明取回数据与给定摘要一致，时间戳可证明某摘要曾出现，却不能自动证明传感器、仓单或商品标签真实。链外资产必须通过身份、审计、预言机、争议和法律执行闭环。

复核时为“2.4 IPFS：星际文件系统”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 2.5 公证防伪溯源

“2.5 公证防伪溯源”位于本页正式目录坐标 15/19。内容寻址可证明取回数据与给定摘要一致，时间戳可证明某摘要曾出现，却不能自动证明传感器、仓单或商品标签真实。链外资产必须通过身份、审计、预言机、争议和法律执行闭环。

复核时为“2.5 公证防伪溯源”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 2.6 供应链金融

“2.6 供应链金融”位于本页正式目录坐标 16/19。内容寻址可证明取回数据与给定摘要一致，时间戳可证明某摘要曾出现，却不能自动证明传感器、仓单或商品标签真实。链外资产必须通过身份、审计、预言机、争议和法律执行闭环。

复核时为“2.6 供应链金融”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 2.7 区块链基础设施：可编程社会

“2.7 区块链基础设施：可编程社会”位于本页正式目录坐标 17/19。内容寻址可证明取回数据与给定摘要一致，时间戳可证明某摘要曾出现，却不能自动证明传感器、仓单或商品标签真实。链外资产必须通过身份、审计、预言机、争议和法律执行闭环。

复核时为“2.7 区块链基础设施：可编程社会”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 2.8 链内资产与链外资产

“2.8 链内资产与链外资产”位于本页正式目录坐标 18/19。内容寻址可证明取回数据与给定摘要一致，时间戳可证明某摘要曾出现，却不能自动证明传感器、仓单或商品标签真实。链外资产必须通过身份、审计、预言机、争议和法律执行闭环。

复核时为“2.8 链内资产与链外资产”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 2.9 知识点导图

“2.9 知识点导图”位于本页正式目录坐标 19/19。知识点导图用于检查依赖：从状态对象指向授权证据、传播、排序、执行和风险；它是回放索引，不替代定义、公式、代码和失败样本。每条箭头都应能落到一个可观察输入输出。

复核时为“2.9 知识点导图”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

## 六类复核证据

### 状态与所有权合同

列出链上对象的唯一标识、所有者或解锁条件、当前版本和允许的状态转移。UTXO模型检查引用与未花费性，账户模型检查发送者、nonce与余额；两者不能共用一个含糊的“余额正确”断言。

### 密码与编码合同

记录哈希和签名算法、参数、密钥来源、随机数、序列化与地址校验。编码只解决传输表示，哈希只提供摘要，签名只证明相应私钥授权；真实性、保密性与业务合法性需要额外证据。

### 网络与共识合同

保存节点身份、连接、消息、传播顺序、故障模型、候选集合和规范历史选择。共识安全性与活性分别验证；暂时不出块和两个节点接受冲突最终状态是不同故障，不能只用平均TPS描述。

### 执行与平台合同

合约或链码记录前状态、调用者、输入、读写集、资源消耗、事件和后状态。失败执行必须原子回滚，外部服务和预言机响应进入证据，避免把链外副作用误当成虚拟机可回滚状态。

### 攻击与恢复合同

每个风险写出资产、攻击者能力、入口、首个可观测异常、影响和恢复边界。私钥丢失、多数重组、合约权限泄漏与数据增长的恢复手段不同；“不可篡改”不能替代备份、暂停、升级和治理。

### 发布与独立复核合同

独立复核者从空目录读取来源与配置，先手算最小状态，再运行正常、边界和单故障样本。哈希、签名、事件、余额、拒绝原因和最终状态逐项一致后才发布；任何缺失证据都进入隔离清单而非静默跳过。

## 容易踩的坑

**现象 →** 把商品溯源数据写入区块链就声称商品保真。
**原因 →** 内容寻址只证明取回一致，不证明传感器或标签真实。
**修法 →** 链外资产须通过身份、审计、预言机、争议和法律执行闭环。你可以亲自试一下：把一条假仓单写入IPFS，观察链上摘要仍验证通过。

**现象 →** 只看TPS就判定某数字货币更优。
**原因 →** 未固定安全预算、确认定义和隐私泄漏面。
**修法 →** 比较时固定节点可验证成本、安全预算和确认定义。

**现象 →** 链上确认后认为法币也已到账。
**原因 →** 链上确认只覆盖协议内状态，法币账户在链外。
**修法 →** 明确最终性与追索规则，区分清算与结算。

## 小结

- 数字货币比较须固定安全预算、确认定义和隐私泄漏面
- 智能合约确定性不保证业务意图或权限配置正确
- 链上确认只覆盖协议内状态，法币追索仍在链外
- 内容寻址证明取回一致，不证明链外事实真实
- 通过标准：能为业务场景画出链上状态、链外事实与争议处置

<Exercises>

**问题 1：** IPFS的内容寻址能证明什么，不能证明什么？

<Answer>

IPFS的内容寻址能证明取回的数据与给定摘要一致（数据完整性），也能通过时间戳证明某摘要曾出现。但不能自动证明传感器读数、仓单记录或商品标签的真实性——这些是链外事实，需要身份、审计、预言机和法律执行来闭环。

</Answer>

**问题 2（独立实现）：** 独立实现一个链上链外边界检查器，输入应用场景描述，输出哪些是链上可验证状态、哪些是链外事实，并标注每个链外事实所需的信任假设。

<Answer>

```python
def boundary_checker(scenario):
    on_chain = []
    off_chain = []
    for item in scenario:
        if item["type"] in ("state_transfer", "signature", "block_hash"):
            on_chain.append(item["name"])
        else:
            off_chain.append({
                "name": item["name"],
                "trust_assumption": item.get("trust", "unspecified"),
            })
    return {"on_chain": on_chain, "off_chain": off_chain}

# 测试：溯源场景
scenario = [
    {"name": "仓单上链哈希", "type": "block_hash"},
    {"name": "传感器温度读数", "type": "oracle", "trust": "传感器未被篡改"},
    {"name": "商品物理交接", "type": "physical", "trust": "物流方诚实"},
    {"name": "所有权转移签名", "type": "signature"},
]
result = boundary_checker(scenario)
assert "仓单上链哈希" in result["on_chain"]
assert "所有权转移签名" in result["on_chain"]
assert any(o["name"] == "传感器温度读数" for o in result["off_chain"])
```

</Answer>

</Exercises>

## 本章回顾

比较数字货币、智能合约、结算、分布式文件、公证溯源与供应链金融，区分链上证明和现实履约。 掌握标准不是背诵项目名或算法缩写，而是能冻结状态、授权、传播、排序、执行、证据和失败边界。通过标准为：能为一个业务场景画出参与者、链上状态、链外事实、信任假设与争议处置，不用上链代替真实性证明。


## 前后导航

- [上一页：第1章 初识区块链](/learn/blockchain-plain/03-01-first-blockchain/bp-01-first-blockchain)
- [下一页：第3章 区块链骨骼：密码算法](/learn/blockchain-plain/05-03-cryptography/bp-03-cryptography)

<Attribution
  mode="independent-rewrite"
  sourceBasis="publisher-catalog"
  workTitle="蒋勇、文颜成、李浩、李耀军《白话区块链》机械工业出版社 / 2017"
  adaptedUrl="https://e.dangdang.com/products/1901310198.html"
/>

