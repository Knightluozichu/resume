---
title: "第3章 区块链骨骼：密码算法"
type: C
section: "白话区块链（2017版）"
order: 5
description: "把哈希、公开密钥、数字签名与Base系列编码放回完整性、授权和人机传递三类不同职责。"
demo: true
math: false
sourceUrl: "https://e.dangdang.com/products/1901310198.html"
draft: false
---


## 为什么从可验证状态开始

、、、和
共同构成本页坐标。把哈希、公开密钥、数字签名与Base系列编码放回完整性、授权和人机传递三类不同职责。

本书锁定蒋勇、文延、嘉文著《白话区块链》，机械工业出版社2017年10月版，236页，ISBN 9787111582984。当当云阅读给出技术审校、前言、9章全部编号节与小节以及后记的完整目录；得到与天珑书店分别交叉核对出版方、作者、日期、页数和ISBN。本文是独立教学重构，不复制原书正文。

区块链不是“广播后大家自然相信”，而是候选状态经过本地规则验证、网络传播、排序或共识，再提交为节点认可历史。任何结论都要回答五个问题：谁能发起，谁能验证，按什么顺序，失败时保留哪个状态，观察者凭什么复核。


## 区块、交易与证据不变量

$$
H_i = hash(H_{i-1} + merkle(T_i) + time_i + nonce_i)
$$

$$
valid(T) = signature(T, publicKey) and unspent(inputs) and valueIn >= valueOut
$$

$$
confirmed(T, k) = valid(T) and depth(T) >= k and canonical(block(T))
$$

$$
gate = source * replay * boundary * failureEvidence
$$

第一式把新区块绑定到前块摘要与交易集合；任一字段改变都会改变摘要，但只有独立验证和共识选择才能决定候选链是否被接受。第二式把签名授权、未花费约束和价值守恒分开，签名正确不代表输入尚未消费。第三式说明确认依赖规范链和深度，不能把“收到交易”写成“最终结算”。第四式要求来源、重放、边界与失败证据同时存在，任一为零都不能发布。

取三笔UTXO：A拥有7与4，拟向B支付8并找零3。合法交易必须引用两笔输入、由A签名并产生8与3两个输出；若仅引用7、重复引用同一输入或把找零写成4，节点应在进入候选区块前拒绝。把合法交易放入区块后修改金额，重算Merkle根和区块摘要虽可让单块内部一致，却会破坏后续链接和累计共识证据。

## 三段可运行骨架

以下三段骨架将哈希链接、签名验证与证据门禁串成可运行的最小闭环，帮助你把密码学概念落到代码验证。

<CodeTabs>
<Tab label="Python / 核心逻辑">

```python
def verify_transaction(tx, utxos, verify_signature):
    inputs = [utxos.get(ref) for ref in tx["inputs"]]
    if None in inputs or len(set(tx["inputs"])) != len(tx["inputs"]):
        return False
    return verify_signature(tx) and sum(inputs) >= sum(tx["outputs"])

def append_block(chain, block, hash_block, valid_tx):
    if block["previous"] != hash_block(chain[-1]):
        return {"accepted": False, "reason": "broken-link"}
    if not all(valid_tx(tx) for tx in block["transactions"]):
        return {"accepted": False, "reason": "invalid-transaction"}
    return {"accepted": True, "chain": [*chain, block]}
```

</Tab>
<Tab label="Python / 证据门禁">

```python
def evidence_gate(run):
    required = {"source", "version", "input", "trace", "output", "failure"}
    missing = sorted(required.difference(run))
    return {"pass": not missing, "missing": missing}
```

</Tab>
</CodeTabs>

第一段验证交易引用、重复输入、签名和价值约束；生产实现还要检查脚本、费用、锁定时间与整数范围。第二段先检查前块链接再检查交易，失败时不修改原链。第三段对实验记录做结构门禁，使成功轨迹和失败证据使用同一版本合同。

## 最小实例与重放顺序

先冻结创世状态、账户或UTXO集合、验证函数、网络成员、排序规则、确认条件和软件版本。按“构造交易 -> 本地签名 -> 邻居独立验证 -> 候选池 -> 打包或排序 -> 区块验证 -> 状态提交”记录每一步输入输出。只保存最后余额，会漏掉重复消费、错误签名被缓存、无效区块短暂传播或回滚未清理等问题。

正常样本使用两笔输入支付并找零；边界样本让输入恰好等于输出；单故障样本只破坏签名、前块摘要、背书数量或读版本中的一项。三组样本除该变量外保持相同，才能把拒绝原因归因到目标规则。随机网络实验保存种子、消息顺序和超时，不以一次收敛证明协议活性。

## 2017年版本边界与现代迁移

原书记录的是2017年前后的比特币客户端、以太坊钱包、Fabric版本与当时的扩容和安全事件。今天重放时保留概念坐标和状态不变量，但不照抄已经失效的下载地址、网络参数、命令或默认配置。历史截图用于解释接口，不作为当前安全保证。

迁移到现代实现时建立对应表：原书术语、历史行为、当前替代、兼容差异、验证方法和回退方案。以太坊共识变更、Fabric架构演进或钱包弃用必须显式披露；不能因为现代工具运行成功，就声称原书命令仍然有效，也不能用今天的特性改写当年的设计动机。

## 正式目录逐项讲解

### 第3章 区块链骨骼：密码算法

“第3章 区块链骨骼：密码算法”位于本页正式目录坐标 1/15。公私钥算法把授权能力绑定到私钥，把公开验证交给公钥。区块链交易通常依赖签名而非用公钥加密账本；算法名称、参数、随机数质量与密钥生命周期必须一并审计。

复核时为“第3章 区块链骨骼：密码算法”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 3.1 哈希算法

“3.1 哈希算法”位于本页正式目录坐标 2/15。哈希把任意输入压缩成固定摘要，用于完整性和链接；Base64、Base58只是可逆表示，Base58Check额外加入校验和以发现抄录错误。三者都不提供保密性，也不能替代数字签名。

复核时为“3.1 哈希算法”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 3.1.1 什么是哈希计算

“3.1.1 什么是哈希计算”位于本页正式目录坐标 3/15。哈希把任意输入压缩成固定摘要，用于完整性和链接；Base64、Base58只是可逆表示，Base58Check额外加入校验和以发现抄录错误。三者都不提供保密性，也不能替代数字签名。

复核时为“3.1.1 什么是哈希计算”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 3.1.2 哈希算法的种类

“3.1.2 哈希算法的种类”位于本页正式目录坐标 4/15。哈希把任意输入压缩成固定摘要，用于完整性和链接；Base64、Base58只是可逆表示，Base58Check额外加入校验和以发现抄录错误。三者都不提供保密性，也不能替代数字签名。

复核时为“3.1.2 哈希算法的种类”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 3.1.3 区块链中的哈希算法

“3.1.3 区块链中的哈希算法”位于本页正式目录坐标 5/15。哈希把任意输入压缩成固定摘要，用于完整性和链接；Base64、Base58只是可逆表示，Base58Check额外加入校验和以发现抄录错误。三者都不提供保密性，也不能替代数字签名。

复核时为“3.1.3 区块链中的哈希算法”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 3.2 公开密钥算法

“3.2 公开密钥算法”位于本页正式目录坐标 6/15。公私钥算法把授权能力绑定到私钥，把公开验证交给公钥。区块链交易通常依赖签名而非用公钥加密账本；算法名称、参数、随机数质量与密钥生命周期必须一并审计。

复核时为“3.2 公开密钥算法”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 3.2.1 两把钥匙：公钥和私钥

“3.2.1 两把钥匙：公钥和私钥”位于本页正式目录坐标 7/15。公私钥算法把授权能力绑定到私钥，把公开验证交给公钥。区块链交易通常依赖签名而非用公钥加密账本；算法名称、参数、随机数质量与密钥生命周期必须一并审计。

复核时为“3.2.1 两把钥匙：公钥和私钥”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 3.2.2 RSA算法

“3.2.2 RSA算法”位于本页正式目录坐标 8/15。公私钥算法把授权能力绑定到私钥，把公开验证交给公钥。区块链交易通常依赖签名而非用公钥加密账本；算法名称、参数、随机数质量与密钥生命周期必须一并审计。

复核时为“3.2.2 RSA算法”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 3.2.3 椭圆曲线密码算法

“3.2.3 椭圆曲线密码算法”位于本页正式目录坐标 9/15。公私钥算法把授权能力绑定到私钥，把公开验证交给公钥。区块链交易通常依赖签名而非用公钥加密账本；算法名称、参数、随机数质量与密钥生命周期必须一并审计。

复核时为“3.2.3 椭圆曲线密码算法”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 3.3 编码/解码算法

“3.3 编码/解码算法”位于本页正式目录坐标 10/15。哈希把任意输入压缩成固定摘要，用于完整性和链接；Base64、Base58只是可逆表示，Base58Check额外加入校验和以发现抄录错误。三者都不提供保密性，也不能替代数字签名。

复核时为“3.3 编码/解码算法”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 3.3.1 Base64

“3.3.1 Base64”位于本页正式目录坐标 11/15。哈希把任意输入压缩成固定摘要，用于完整性和链接；Base64、Base58只是可逆表示，Base58Check额外加入校验和以发现抄录错误。三者都不提供保密性，也不能替代数字签名。

复核时为“3.3.1 Base64”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 3.3.2 Base58

“3.3.2 Base58”位于本页正式目录坐标 12/15。哈希把任意输入压缩成固定摘要，用于完整性和链接；Base64、Base58只是可逆表示，Base58Check额外加入校验和以发现抄录错误。三者都不提供保密性，也不能替代数字签名。

复核时为“3.3.2 Base58”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 3.3.3 Base58Check

“3.3.3 Base58Check”位于本页正式目录坐标 13/15。哈希把任意输入压缩成固定摘要，用于完整性和链接；Base64、Base58只是可逆表示，Base58Check额外加入校验和以发现抄录错误。三者都不提供保密性，也不能替代数字签名。

复核时为“3.3.3 Base58Check”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 3.4 应用场景

“3.4 应用场景”位于本页正式目录坐标 14/15。这一目录项要落到状态对象、授权规则、传播顺序和失败边界。先写最小输入输出，再用正常、边界与单故障样本验证，避免只记名词而无法判断实现是否正确。

复核时为“3.4 应用场景”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 3.5 知识点导图

“3.5 知识点导图”位于本页正式目录坐标 15/15。知识点导图用于检查依赖：从状态对象指向授权证据、传播、排序、执行和风险；它是回放索引，不替代定义、公式、代码和失败样本。每条箭头都应能落到一个可观察输入输出。

复核时为“3.5 知识点导图”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

## 六类复核证据

### 状态与所有权合同

列出链上对象的唯一标识、所有者或解锁条件、当前版本和允许的状态转移。UTXO模型检查引用与未花费性，账户模型检查发送者、nonce与余额；两者不能共用一个含糊的“余额正确”断言。

### 密码与编码合同

记录哈希和签名算法、参数、密钥来源、随机数、序列化与地址校验。编码只解决传输表示，哈希只提供摘要，签名只证明相应私钥授权；真实性、保密性与业务合法性需要额外证据。

### 网络与共识合同

保存节点身份、连接、消息、传播顺序、故障模型、候选集合和规范历史选择。共识安全性与活性分别验证；暂时不出块和两个节点接受冲突最终状态是不同故障，不能只用平均TPS描述。

### 执行与平台合同

合约或链码记录前状态、调用者、输入、读写集、资源消耗、事件和后状态。失败执行必须原子回滚，外部服务和预言机响应进入证据，避免把链外副作用误当成虚拟机可回滚状态。

### 攻击与恢复合同

每个风险写出资产、攻击者能力、入口、首个可观测异常、影响和恢复边界。私钥丢失、多数重组、合约权限泄漏与数据增长的恢复手段不同；“不可篡改”不能替代备份、暂停、升级和治理。

### 发布与独立复核合同

独立复核者从空目录读取来源与配置，先手算最小状态，再运行正常、边界和单故障样本。哈希、签名、事件、余额、拒绝原因和最终状态逐项一致后才发布；任何缺失证据都进入隔离清单而非静默跳过。

## 容易踩的坑

**现象 →** 把Base58编码后的私钥存在公开文件里，以为编码就等于加密。
**原因 →** Base58只是可逆表示，任何人都能解码还原原文，它不提供任何保密性。
**修法 →** 私钥必须用加密方案（如AES）或硬件钱包保护，编码仅用于人机传递和抄录校验。

**现象 →** 交易签名验证通过，节点却拒绝该交易——你可以亲手用一段Python代码复现这个翻车：只验签名不查UTXO是否未花费，双花交易签名完全正确却被拒绝。
**原因 →** 签名只证明私钥授权，不代表输入尚未消费；价值守恒和未花费约束是独立检查项。
**修法 →** 验证函数必须同时检查签名、UTXO存在性、重复引用和价值守恒，四项缺一不可。

**现象 →** 修改区块内交易金额后重算Merkle根和区块摘要，单块内部看起来一致，整条链却断裂。
**原因 →** 后续区块引用的是修改前的摘要，改一个字段就破坏了哈希链接的完整性。
**修法 →** 任何字段变更必须从该区块起重算所有后续摘要，并重新经过共识确认，不能只做局部修补。

## 小结

- 哈希提供完整性校验，不提供保密性或身份证明
- 公私钥把授权绑定到私钥，公钥负责公开验证
- 数字签名证明私钥授权，但不代表交易业务合法
- Base64/Base58是可逆编码，Base58Check加校验和防抄录错误
- 编码不是加密，哈希不是签名，三者职责不可混用

<Exercises>

**问题 1**

给定一个区块包含 `previous_hash`、`merkle_root`、`timestamp` 和 `nonce` 四个字段，请独立实现一个函数 `compute_block_hash(block)` 计算区块摘要，并实现 `verify_chain(chain)` 验证整条链的哈希链接完整性。要求：篡改任意一个区块的任意字段后，`verify_chain` 必须返回 `False`。

<Answer>

关键点：将四个字段按固定顺序拼接后哈希，验证时逐块检查 `block["previous_hash"] == hash(prev_block)`。篡改字段会导致当前块摘要变化，从而与后一块的 `previous_hash` 不匹配。

</Answer>

**问题 2**

Base58Check 在 Base58 的基础上增加了4字节校验和。请解释为什么 Base58Check 能检测出单字符抄录错误，而普通 Base58 不能。如果两个字符被同时抄错且恰好抵消，校验和是否仍然可靠？

<Answer>

Base58Check 对原始数据做两次SHA-256后取前4字节作为校验和，任何单字符变化都会改变校验和值从而被检测。若两个字符同时出错且恰好使校验和不变，概率仅为 1/2^32，在实际抄录场景中可忽略不计，但不为零。

</Answer>

</Exercises>

## 本章回顾

把哈希、公开密钥、数字签名与Base系列编码放回完整性、授权和人机传递三类不同职责。 掌握标准不是背诵项目名或算法缩写，而是能冻结状态、授权、传播、排序、执行、证据和失败边界。通过标准为：能手算简化哈希链并实现签名验签与校验和，明确编码不是加密、哈希也不证明签名者身份。


## 前后导航

- [上一页：第2章 区块链应用发展](/learn/blockchain-plain/04-02-application-development/bp-02-application-development)
- [下一页：第4章 区块链灵魂：共识算法](/learn/blockchain-plain/06-04-consensus/bp-04-consensus)

<Attribution
  mode="independent-rewrite"
  sourceBasis="publisher-catalog"
  workTitle="蒋勇、文颜成、李浩、李耀军《白话区块链》机械工业出版社 / 2017"
  adaptedUrl="https://e.dangdang.com/products/1901310198.html"
/>

