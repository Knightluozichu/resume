---
title: "第4章 区块链灵魂：共识算法"
type: C
section: "白话区块链（2017版）"
order: 6
description: "从失败模型和网络假设比较Paxos、Raft、PBFT、PoW、PoS与DPoS，拆开安全性、活性和最终性。"
demo: true
math: false
sourceUrl: "https://e.dangdang.com/products/1901310198.html"
draft: false
---


## 为什么从可验证状态开始

、、、和
共同构成本页坐标。从失败模型和网络假设比较Paxos、Raft、PBFT、PoW、PoS与DPoS，拆开安全性、活性和最终性。

本书锁定蒋勇、文延、嘉文著《白话区块链》，机械工业出版社2017年10月版，236页，ISBN 9787111582984。当当云阅读给出技术审校、前言、9章全部编号节与小节以及后记的完整目录；得到与天珑书店分别交叉核对出版方、作者、日期、页数和ISBN。本文是独立教学重构，不复制原书正文。

区块链不是“广播后大家自然相信”，而是候选状态经过本地规则验证、网络传播、排序或共识，再提交为节点认可历史。任何结论都要回答五个问题：谁能发起，谁能验证，按什么顺序，失败时保留哪个状态，观察者凭什么复核。


## 区块、交易与证据不变量

$$
H_i = hash(H_{i-1} + merkle(T_i) + time_i + nonce_i)
$$

$$
valid(T) = signature(T, publicKey) and unspent(inputs) and valueIn >= valueOut
$$

$$
confirmed(T, k) = valid(T) and depth(T) >= k and canonical(block(T))
$$

$$
gate = source * replay * boundary * failureEvidence
$$

第一式把新区块绑定到前块摘要与交易集合；任一字段改变都会改变摘要，但只有独立验证和共识选择才能决定候选链是否被接受。第二式把签名授权、未花费约束和价值守恒分开，签名正确不代表输入尚未消费。第三式说明确认依赖规范链和深度，不能把“收到交易”写成“最终结算”。第四式要求来源、重放、边界与失败证据同时存在，任一为零都不能发布。

取三笔UTXO：A拥有7与4，拟向B支付8并找零3。合法交易必须引用两笔输入、由A签名并产生8与3两个输出；若仅引用7、重复引用同一输入或把找零写成4，节点应在进入候选区块前拒绝。把合法交易放入区块后修改金额，重算Merkle根和区块摘要虽可让单块内部一致，却会破坏后续链接和累计共识证据。

## 三段可运行骨架

以下三段骨架从交易验证、区块追加到证据门禁构成最小闭环，帮助你在共识实验中定位安全性与活性的边界。

<CodeTabs>
<Tab label="Python / 核心逻辑">

```python
def verify_transaction(tx, utxos, verify_signature):
    inputs = [utxos.get(ref) for ref in tx["inputs"]]
    if None in inputs or len(set(tx["inputs"])) != len(tx["inputs"]):
        return False
    return verify_signature(tx) and sum(inputs) >= sum(tx["outputs"])

def append_block(chain, block, hash_block, valid_tx):
    if block["previous"] != hash_block(chain[-1]):
        return {"accepted": False, "reason": "broken-link"}
    if not all(valid_tx(tx) for tx in block["transactions"]):
        return {"accepted": False, "reason": "invalid-transaction"}
    return {"accepted": True, "chain": [*chain, block]}
```

</Tab>
<Tab label="Python / 证据门禁">

```python
def evidence_gate(run):
    required = {"source", "version", "input", "trace", "output", "failure"}
    missing = sorted(required.difference(run))
    return {"pass": not missing, "missing": missing}
```

</Tab>
</CodeTabs>

第一段验证交易引用、重复输入、签名和价值约束；生产实现还要检查脚本、费用、锁定时间与整数范围。第二段先检查前块链接再检查交易，失败时不修改原链。第三段对实验记录做结构门禁，使成功轨迹和失败证据使用同一版本合同。

## 最小实例与重放顺序

先冻结创世状态、账户或UTXO集合、验证函数、网络成员、排序规则、确认条件和软件版本。按“构造交易 -> 本地签名 -> 邻居独立验证 -> 候选池 -> 打包或排序 -> 区块验证 -> 状态提交”记录每一步输入输出。只保存最后余额，会漏掉重复消费、错误签名被缓存、无效区块短暂传播或回滚未清理等问题。

正常样本使用两笔输入支付并找零；边界样本让输入恰好等于输出；单故障样本只破坏签名、前块摘要、背书数量或读版本中的一项。三组样本除该变量外保持相同，才能把拒绝原因归因到目标规则。随机网络实验保存种子、消息顺序和超时，不以一次收敛证明协议活性。

## 2017年版本边界与现代迁移

原书记录的是2017年前后的比特币客户端、以太坊钱包、Fabric版本与当时的扩容和安全事件。今天重放时保留概念坐标和状态不变量，但不照抄已经失效的下载地址、网络参数、命令或默认配置。历史截图用于解释接口，不作为当前安全保证。

迁移到现代实现时建立对应表：原书术语、历史行为、当前替代、兼容差异、验证方法和回退方案。以太坊共识变更、Fabric架构演进或钱包弃用必须显式披露；不能因为现代工具运行成功，就声称原书命令仍然有效，也不能用今天的特性改写当年的设计动机。

## 正式目录逐项讲解

### 第4章 区块链灵魂：共识算法

“第4章 区块链灵魂：共识算法”位于本页正式目录坐标 1/14。共识结论依赖故障类型、网络时序和成员身份。Paxos与Raft主要处理崩溃故障，PBFT显式容忍有限恶意副本；FLP和CAP提醒读者先写假设，不能把一个协议名称当作全部保证。

复核时为“第4章 区块链灵魂：共识算法”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 4.1 分布式系统的一致性

“4.1 分布式系统的一致性”位于本页正式目录坐标 2/14。共识结论依赖故障类型、网络时序和成员身份。Paxos与Raft主要处理崩溃故障，PBFT显式容忍有限恶意副本；FLP和CAP提醒读者先写假设，不能把一个协议名称当作全部保证。

复核时为“4.1 分布式系统的一致性”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 4.1.1 一致性问题

“4.1.1 一致性问题”位于本页正式目录坐标 3/14。共识结论依赖故障类型、网络时序和成员身份。Paxos与Raft主要处理崩溃故障，PBFT显式容忍有限恶意副本；FLP和CAP提醒读者先写假设，不能把一个协议名称当作全部保证。

复核时为“4.1.1 一致性问题”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 4.1.2 两个原理：FLP与CAP

“4.1.2 两个原理：FLP与CAP”位于本页正式目录坐标 4/14。共识结论依赖故障类型、网络时序和成员身份。Paxos与Raft主要处理崩溃故障，PBFT显式容忍有限恶意副本；FLP和CAP提醒读者先写假设，不能把一个协议名称当作全部保证。

复核时为“4.1.2 两个原理：FLP与CAP”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 4.1.3 拜占庭将军问题

“4.1.3 拜占庭将军问题”位于本页正式目录坐标 5/14。共识结论依赖故障类型、网络时序和成员身份。Paxos与Raft主要处理崩溃故障，PBFT显式容忍有限恶意副本；FLP和CAP提醒读者先写假设，不能把一个协议名称当作全部保证。

复核时为“4.1.3 拜占庭将军问题”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 4.1.4 共识算法的目的

“4.1.4 共识算法的目的”位于本页正式目录坐标 6/14。共识结论依赖故障类型、网络时序和成员身份。Paxos与Raft主要处理崩溃故障，PBFT显式容忍有限恶意副本；FLP和CAP提醒读者先写假设，不能把一个协议名称当作全部保证。

复核时为“4.1.4 共识算法的目的”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 4.2 Paxos算法

“4.2 Paxos算法”位于本页正式目录坐标 7/14。共识结论依赖故障类型、网络时序和成员身份。Paxos与Raft主要处理崩溃故障，PBFT显式容忍有限恶意副本；FLP和CAP提醒读者先写假设，不能把一个协议名称当作全部保证。

复核时为“4.2 Paxos算法”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 4.3 Raft算法

“4.3 Raft算法”位于本页正式目录坐标 8/14。共识结论依赖故障类型、网络时序和成员身份。Paxos与Raft主要处理崩溃故障，PBFT显式容忍有限恶意副本；FLP和CAP提醒读者先写假设，不能把一个协议名称当作全部保证。

复核时为“4.3 Raft算法”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 4.4 PBFT算法

“4.4 PBFT算法”位于本页正式目录坐标 9/14。共识结论依赖故障类型、网络时序和成员身份。Paxos与Raft主要处理崩溃故障，PBFT显式容忍有限恶意副本；FLP和CAP提醒读者先写假设，不能把一个协议名称当作全部保证。

复核时为“4.4 PBFT算法”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 4.5 工作量证明——PoW

“4.5 工作量证明——PoW”位于本页正式目录坐标 10/14。PoW用外部计算成本约束历史重写，PoS用可罚没权益约束验证者，DPoS再引入委托代表。三者必须同时比较准入、集中化、惩罚、最终性和治理，不以单一能耗或速度指标下结论。

复核时为“4.5 工作量证明——PoW”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 4.6 股权权益证明——PoS

“4.6 股权权益证明——PoS”位于本页正式目录坐标 11/14。PoW用外部计算成本约束历史重写，PoS用可罚没权益约束验证者，DPoS再引入委托代表。三者必须同时比较准入、集中化、惩罚、最终性和治理，不以单一能耗或速度指标下结论。

复核时为“4.6 股权权益证明——PoS”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 4.7 委托权益人证明机制——DPoS

“4.7 委托权益人证明机制——DPoS”位于本页正式目录坐标 12/14。PoW用外部计算成本约束历史重写，PoS用可罚没权益约束验证者，DPoS再引入委托代表。三者必须同时比较准入、集中化、惩罚、最终性和治理，不以单一能耗或速度指标下结论。

复核时为“4.7 委托权益人证明机制——DPoS”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 4.8 共识算法的社会学探讨

“4.8 共识算法的社会学探讨”位于本页正式目录坐标 13/14。共识结论依赖故障类型、网络时序和成员身份。Paxos与Raft主要处理崩溃故障，PBFT显式容忍有限恶意副本；FLP和CAP提醒读者先写假设，不能把一个协议名称当作全部保证。

复核时为“4.8 共识算法的社会学探讨”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 4.9 知识点导图

“4.9 知识点导图”位于本页正式目录坐标 14/14。知识点导图用于检查依赖：从状态对象指向授权证据、传播、排序、执行和风险；它是回放索引，不替代定义、公式、代码和失败样本。每条箭头都应能落到一个可观察输入输出。

复核时为“4.9 知识点导图”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

## 六类复核证据

### 状态与所有权合同

列出链上对象的唯一标识、所有者或解锁条件、当前版本和允许的状态转移。UTXO模型检查引用与未花费性，账户模型检查发送者、nonce与余额；两者不能共用一个含糊的“余额正确”断言。

### 密码与编码合同

记录哈希和签名算法、参数、密钥来源、随机数、序列化与地址校验。编码只解决传输表示，哈希只提供摘要，签名只证明相应私钥授权；真实性、保密性与业务合法性需要额外证据。

### 网络与共识合同

保存节点身份、连接、消息、传播顺序、故障模型、候选集合和规范历史选择。共识安全性与活性分别验证；暂时不出块和两个节点接受冲突最终状态是不同故障，不能只用平均TPS描述。

### 执行与平台合同

合约或链码记录前状态、调用者、输入、读写集、资源消耗、事件和后状态。失败执行必须原子回滚，外部服务和预言机响应进入证据，避免把链外副作用误当成虚拟机可回滚状态。

### 攻击与恢复合同

每个风险写出资产、攻击者能力、入口、首个可观测异常、影响和恢复边界。私钥丢失、多数重组、合约权限泄漏与数据增长的恢复手段不同；“不可篡改”不能替代备份、暂停、升级和治理。

### 发布与独立复核合同

独立复核者从空目录读取来源与配置，先手算最小状态，再运行正常、边界和单故障样本。哈希、签名、事件、余额、拒绝原因和最终状态逐项一致后才发布；任何缺失证据都进入隔离清单而非静默跳过。

## 容易踩的坑

**现象 →** 在需要容忍恶意节点的场景中用Raft替代PBFT，以为都是共识算法就能安全——你可以亲手翻车：让一个Raft节点伪造日志条目，其他节点因为不验证签名而直接接受。
**原因 →** Raft只处理崩溃故障，假设所有节点诚实；PBFT显式验证每条消息签名，才能容忍有限拜占庭副本。
**修法 →** 先确定故障模型（崩溃 vs 拜占庭），再选协议；恶意场景必须使用带消息签名验证的BFT类协议。

**现象 →** 网络分区期间PoW暂时不出块，立刻判定"共识失败"并触发人工干预。
**原因 →** PoW在分区时牺牲活性（暂时不出块）来保证安全性（不会接受冲突账本），这是设计预期而非故障。
**修法 →** 区分安全性和活性两个指标，分区恢复后活性自动恢复才算正常行为；只有长期不出块才需告警。

**现象 →** 用单一TPS指标比较PoW和PBFT，得出"PBFT更快所以更好"的结论。
**原因 →** TPS只反映吞吐，忽略了准入门槛、去中心化程度、最终性延迟、惩罚机制和治理模型等维度。
**修法 →** 至少同时比较准入、集中化、惩罚、最终性和治理五个维度，不以单一速度或能耗指标下结论。

## 小结

- 共识结论依赖故障模型、网络时序和成员身份三类假设
- Paxos/Raft处理崩溃故障，PBFT显式容忍有限恶意副本
- FLP证明异步网络中无法同时保证安全性和活性
- PoW用计算成本约束重写，PoS用可罚没权益约束验证者
- 安全性和活性必须分别验证，不能只用TPS下结论

<Exercises>

**问题 1**

假设系统有 n=3f+1 个节点，其中最多 f 个为拜占庭节点。请独立实现一个简化的PBFT投票函数 `check_endorsements(endorsements, n, f)`，当且仅当收集到至少 2f+1 个有效签名背书时返回 `True`。要求：构造 f 个恶意签名和 2f 个诚实签名，验证函数返回 `True`；再减少一个诚实签名，验证返回 `False`。

<Answer>

关键点：2f+1 是PBFT的法定人数门槛，保证至少 f+1 个诚实节点参与，使两个冲突的法定人数集合必然相交。函数需检查签名数量是否 >= 2f+1 且签名来自不同节点。

</Answer>

**问题 2**

给定一个网络分区场景：网络被分为两个不连通的子集，各子集内部通信正常。请分别分析在此场景下 PoW 和 PBFT 各自失去的是安全性还是活性，以及分区恢复后会发生什么。

<Answer>

PoW在分区时失去活性（可能两侧都暂不出块或各自出块），但分区恢复后通过最长链规则选择一条链，保证最终安全性。PBFT在分区时如果任一侧不足 2f+1 则失去活性；如果一侧恰好达到 2f+1 则保持安全性和活性，另一侧暂停。

</Answer>

</Exercises>

## 本章回顾

从失败模型和网络假设比较Paxos、Raft、PBFT、PoW、PoS与DPoS，拆开安全性、活性和最终性。 掌握标准不是背诵项目名或算法缩写，而是能冻结状态、授权、传播、排序、执行、证据和失败边界。通过标准为：能在节点宕机、消息延迟和恶意节点三种样本中判断协议保证什么、暂时失去什么以及何时恢复。


## 前后导航

- [上一页：第3章 区块链骨骼：密码算法](/learn/blockchain-plain/05-03-cryptography/bp-03-cryptography)
- [下一页：第5章 区块链扩展：扩容、侧链和闪电网络](/learn/blockchain-plain/07-05-scaling-sidechains-lightning/bp-05-scaling-sidechains-lightning)

<Attribution
  mode="independent-rewrite"
  sourceBasis="publisher-catalog"
  workTitle="蒋勇、文颜成、李浩、李耀军《白话区块链》机械工业出版社 / 2017"
  adaptedUrl="https://e.dangdang.com/products/1901310198.html"
/>

