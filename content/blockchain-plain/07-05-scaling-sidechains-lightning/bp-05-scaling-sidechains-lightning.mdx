---
title: "第5章 区块链扩展：扩容、侧链和闪电网络"
type: C
section: "白话区块链（2017版）"
order: 7
description: "比较区块容量、侧链锚定、支付通道与多链交互的吞吐收益和新增信任边界。"
demo: true
math: false
sourceUrl: "https://e.dangdang.com/products/1901310198.html"
draft: false
---


## 为什么从可验证状态开始

、、、和
共同构成本页坐标。比较区块容量、侧链锚定、支付通道与多链交互的吞吐收益和新增信任边界。

本书锁定蒋勇、文延、嘉文著《白话区块链》，机械工业出版社2017年10月版，236页，ISBN 9787111582984。当当云阅读给出技术审校、前言、9章全部编号节与小节以及后记的完整目录；得到与天珑书店分别交叉核对出版方、作者、日期、页数和ISBN。本文是独立教学重构，不复制原书正文。

区块链不是“广播后大家自然相信”，而是候选状态经过本地规则验证、网络传播、排序或共识，再提交为节点认可历史。任何结论都要回答五个问题：谁能发起，谁能验证，按什么顺序，失败时保留哪个状态，观察者凭什么复核。


## 区块、交易与证据不变量

$$
H_i = hash(H_{i-1} + merkle(T_i) + time_i + nonce_i)
$$

$$
valid(T) = signature(T, publicKey) and unspent(inputs) and valueIn >= valueOut
$$

$$
confirmed(T, k) = valid(T) and depth(T) >= k and canonical(block(T))
$$

$$
gate = source * replay * boundary * failureEvidence
$$

第一式把新区块绑定到前块摘要与交易集合；任一字段改变都会改变摘要，但只有独立验证和共识选择才能决定候选链是否被接受。第二式把签名授权、未花费约束和价值守恒分开，签名正确不代表输入尚未消费。第三式说明确认依赖规范链和深度，不能把“收到交易”写成“最终结算”。第四式要求来源、重放、边界与失败证据同时存在，任一为零都不能发布。

取三笔UTXO：A拥有7与4，拟向B支付8并找零3。合法交易必须引用两笔输入、由A签名并产生8与3两个输出；若仅引用7、重复引用同一输入或把找零写成4，节点应在进入候选区块前拒绝。把合法交易放入区块后修改金额，重算Merkle根和区块摘要虽可让单块内部一致，却会破坏后续链接和累计共识证据。

## 三段可运行骨架

以下三段骨架覆盖交易验证、区块追加与证据门禁，帮助你在扩容实验中核对吞吐提升是否改变了验证边界。

<CodeTabs>
<Tab label="Python / 核心逻辑">

```python
def verify_transaction(tx, utxos, verify_signature):
    inputs = [utxos.get(ref) for ref in tx["inputs"]]
    if None in inputs or len(set(tx["inputs"])) != len(tx["inputs"]):
        return False
    return verify_signature(tx) and sum(inputs) >= sum(tx["outputs"])

def append_block(chain, block, hash_block, valid_tx):
    if block["previous"] != hash_block(chain[-1]):
        return {"accepted": False, "reason": "broken-link"}
    if not all(valid_tx(tx) for tx in block["transactions"]):
        return {"accepted": False, "reason": "invalid-transaction"}
    return {"accepted": True, "chain": [*chain, block]}
```

</Tab>
<Tab label="Python / 证据门禁">

```python
def evidence_gate(run):
    required = {"source", "version", "input", "trace", "output", "failure"}
    missing = sorted(required.difference(run))
    return {"pass": not missing, "missing": missing}
```

</Tab>
</CodeTabs>

第一段验证交易引用、重复输入、签名和价值约束；生产实现还要检查脚本、费用、锁定时间与整数范围。第二段先检查前块链接再检查交易，失败时不修改原链。第三段对实验记录做结构门禁，使成功轨迹和失败证据使用同一版本合同。

## 最小实例与重放顺序

先冻结创世状态、账户或UTXO集合、验证函数、网络成员、排序规则、确认条件和软件版本。按“构造交易 -> 本地签名 -> 邻居独立验证 -> 候选池 -> 打包或排序 -> 区块验证 -> 状态提交”记录每一步输入输出。只保存最后余额，会漏掉重复消费、错误签名被缓存、无效区块短暂传播或回滚未清理等问题。

正常样本使用两笔输入支付并找零；边界样本让输入恰好等于输出；单故障样本只破坏签名、前块摘要、背书数量或读版本中的一项。三组样本除该变量外保持相同，才能把拒绝原因归因到目标规则。随机网络实验保存种子、消息顺序和超时，不以一次收敛证明协议活性。

## 2017年版本边界与现代迁移

原书记录的是2017年前后的比特币客户端、以太坊钱包、Fabric版本与当时的扩容和安全事件。今天重放时保留概念坐标和状态不变量，但不照抄已经失效的下载地址、网络参数、命令或默认配置。历史截图用于解释接口，不作为当前安全保证。

迁移到现代实现时建立对应表：原书术语、历史行为、当前替代、兼容差异、验证方法和回退方案。以太坊共识变更、Fabric架构演进或钱包弃用必须显式披露；不能因为现代工具运行成功，就声称原书命令仍然有效，也不能用今天的特性改写当年的设计动机。

## 正式目录逐项讲解

### 第5章 区块链扩展：扩容、侧链和闪电网络

“第5章 区块链扩展：扩容、侧链和闪电网络”位于本页正式目录坐标 1/6。扩容改变数据量、验证位置或结算频率。侧链需要锚定和退出假设，闪电网络需要通道资金、路由和在线监控，多链交互需要验证对方最终性；吞吐提升不会免费继承主链全部安全性。

复核时为“第5章 区块链扩展：扩容、侧链和闪电网络”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 5.1 比特币区块扩容

“5.1 比特币区块扩容”位于本页正式目录坐标 2/6。扩容改变数据量、验证位置或结算频率。侧链需要锚定和退出假设，闪电网络需要通道资金、路由和在线监控，多链交互需要验证对方最终性；吞吐提升不会免费继承主链全部安全性。

复核时为“5.1 比特币区块扩容”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 5.2 侧链技术

“5.2 侧链技术”位于本页正式目录坐标 3/6。扩容改变数据量、验证位置或结算频率。侧链需要锚定和退出假设，闪电网络需要通道资金、路由和在线监控，多链交互需要验证对方最终性；吞吐提升不会免费继承主链全部安全性。

复核时为“5.2 侧链技术”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 5.3 闪电网络的设计

“5.3 闪电网络的设计”位于本页正式目录坐标 4/6。扩容改变数据量、验证位置或结算频率。侧链需要锚定和退出假设，闪电网络需要通道资金、路由和在线监控，多链交互需要验证对方最终性；吞吐提升不会免费继承主链全部安全性。

复核时为“5.3 闪电网络的设计”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 5.4 多链：区块链应用的扩展交互

“5.4 多链：区块链应用的扩展交互”位于本页正式目录坐标 5/6。扩容改变数据量、验证位置或结算频率。侧链需要锚定和退出假设，闪电网络需要通道资金、路由和在线监控，多链交互需要验证对方最终性；吞吐提升不会免费继承主链全部安全性。

复核时为“5.4 多链：区块链应用的扩展交互”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 5.5 知识点导图

“5.5 知识点导图”位于本页正式目录坐标 6/6。知识点导图用于检查依赖：从状态对象指向授权证据、传播、排序、执行和风险；它是回放索引，不替代定义、公式、代码和失败样本。每条箭头都应能落到一个可观察输入输出。

复核时为“5.5 知识点导图”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

## 六类复核证据

### 状态与所有权合同

列出链上对象的唯一标识、所有者或解锁条件、当前版本和允许的状态转移。UTXO模型检查引用与未花费性，账户模型检查发送者、nonce与余额；两者不能共用一个含糊的“余额正确”断言。

### 密码与编码合同

记录哈希和签名算法、参数、密钥来源、随机数、序列化与地址校验。编码只解决传输表示，哈希只提供摘要，签名只证明相应私钥授权；真实性、保密性与业务合法性需要额外证据。

### 网络与共识合同

保存节点身份、连接、消息、传播顺序、故障模型、候选集合和规范历史选择。共识安全性与活性分别验证；暂时不出块和两个节点接受冲突最终状态是不同故障，不能只用平均TPS描述。

### 执行与平台合同

合约或链码记录前状态、调用者、输入、读写集、资源消耗、事件和后状态。失败执行必须原子回滚，外部服务和预言机响应进入证据，避免把链外副作用误当成虚拟机可回滚状态。

### 攻击与恢复合同

每个风险写出资产、攻击者能力、入口、首个可观测异常、影响和恢复边界。私钥丢失、多数重组、合约权限泄漏与数据增长的恢复手段不同；“不可篡改”不能替代备份、暂停、升级和治理。

### 发布与独立复核合同

独立复核者从空目录读取来源与配置，先手算最小状态，再运行正常、边界和单故障样本。哈希、签名、事件、余额、拒绝原因和最终状态逐项一致后才发布；任何缺失证据都进入隔离清单而非静默跳过。

## 容易踩的坑

**现象 →** 把资产从主链转移到侧链后，侧链锚定节点集体作恶，资产无法退出——你可以亲手翻车：模拟3个锚定节点中2个被控制，用户提交退出请求被拒绝。
**原因 →** 侧链安全性依赖锚定机制和退出假设，吞吐提升不会免费继承主链的全部安全性。
**修法 →** 评估侧链时必须显式列出锚定信任模型、退出窗口和争议机制，不能只看TPS提升。

**现象 →** 闪电网络用户长期离线，回来发现通道余额被对手用旧状态关闭，资金被盗。
**原因 →** 闪电网络要求参与者在线监控通道状态并及时提交惩罚交易，离线期间旧状态可能被恶意提交。
**修法 →** 部署看塔（Watchtower）服务代理监控，或确保在争议窗口内上线提交最新状态。

**现象 →** 闪电网络支付路由显示成功，但对方声称未收到，以为支付已完成。
**原因 →** "收到交易"不等于"最终结算"，闪电网络支付在通道关闭并上链确认前并非最终到账。
**修法 →** 区分路由成功与通道结算两个阶段，大额支付应在主链确认后才视为最终完成。

## 小结

- 扩容改变数据量、验证位置或结算频率，不免费继承主链安全
- 侧链需要锚定和退出假设，吞吐提升伴随信任边界转移
- 闪电网络需要通道资金、路由和在线监控
- 多链交互必须验证对方最终性，不能假设跨链即时结算
- "收到交易"不等于"最终结算"，确认依赖深度和规范链

<Exercises>

**问题 1**

假设Alice和Bob通过闪电网络建立支付通道，初始余额分别为 Alice=5、Bob=3。请独立实现一个函数 `update_channel(state, sender, amount)` 更新链下余额状态，并实现 `close_channel(state)` 生成分配最终余额的关闭交易。要求：构造一个旧状态（Alice=4, Bob=4）和当前状态（Alice=2, Bob=6），验证关闭交易使用的是最新状态而非旧状态。

<Answer>

关键点：通道状态需带版本号或序列号，关闭时双方签名最新状态；惩罚机制保证提交旧状态者被罚没。`update_channel` 检查发送方余额充足后更新双方余额和版本号，`close_channel` 输出当前余额分配并锁定争议窗口。

</Answer>

**问题 2**

将比特币区块大小从1MB提升到2MB，理论吞吐翻倍。请分析这一扩容方案把哪些成本转移到了哪里（验证、存储、传播、数据可用性），以及为什么不能说"吞吐翻倍且安全性不变"。

<Answer>

区块翻倍后：全节点存储和传播带宽翻倍，轻节点SPV验证的欺诈证明数据可用性风险增加，出块传播延迟可能导致更高孤儿率。安全性虽然共识机制不变，但网络分叉概率上升、全节点门槛提高导致去中心化程度下降，因此不能说安全性完全不变。

</Answer>

</Exercises>

## 本章回顾

比较区块容量、侧链锚定、支付通道与多链交互的吞吐收益和新增信任边界。 掌握标准不是背诵项目名或算法缩写，而是能冻结状态、授权、传播、排序、执行、证据和失败边界。通过标准为：能计算吞吐、确认、通道资金与退出窗口，并指出扩容方案把验证、数据可用性或流动性转移到了哪里。


## 前后导航

- [上一页：第4章 区块链灵魂：共识算法](/learn/blockchain-plain/06-04-consensus/bp-04-consensus)
- [下一页：第6章 区块链开发平台：以太坊](/learn/blockchain-plain/08-06-ethereum/bp-06-ethereum)

<Attribution
  mode="independent-rewrite"
  sourceBasis="publisher-catalog"
  workTitle="蒋勇、文颜成、李浩、李耀军《白话区块链》机械工业出版社 / 2017"
  adaptedUrl="https://e.dangdang.com/products/1901310198.html"
/>

