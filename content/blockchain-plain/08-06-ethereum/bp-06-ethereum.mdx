---
title: "第6章 区块链开发平台：以太坊"
type: C
section: "白话区块链（2017版）"
order: 8
description: "以2017年书中平台为历史坐标，讲清账户状态、交易、Gas、EVM、测试链和代币合约的执行证据。"
demo: true
math: false
sourceUrl: "https://e.dangdang.com/products/1901310198.html"
draft: false
---


## 为什么从可验证状态开始

、、、和
共同构成本页坐标。以2017年书中平台为历史坐标，讲清账户状态、交易、Gas、EVM、测试链和代币合约的执行证据。

本书锁定蒋勇、文延、嘉文著《白话区块链》，机械工业出版社2017年10月版，236页，ISBN 9787111582984。当当云阅读给出技术审校、前言、9章全部编号节与小节以及后记的完整目录；得到与天珑书店分别交叉核对出版方、作者、日期、页数和ISBN。本文是独立教学重构，不复制原书正文。

区块链不是“广播后大家自然相信”，而是候选状态经过本地规则验证、网络传播、排序或共识，再提交为节点认可历史。任何结论都要回答五个问题：谁能发起，谁能验证，按什么顺序，失败时保留哪个状态，观察者凭什么复核。


## 区块、交易与证据不变量

$$
H_i = hash(H_{i-1} + merkle(T_i) + time_i + nonce_i)
$$

$$
valid(T) = signature(T, publicKey) and unspent(inputs) and valueIn >= valueOut
$$

$$
confirmed(T, k) = valid(T) and depth(T) >= k and canonical(block(T))
$$

$$
gate = source * replay * boundary * failureEvidence
$$

第一式把新区块绑定到前块摘要与交易集合；任一字段改变都会改变摘要，但只有独立验证和共识选择才能决定候选链是否被接受。第二式把签名授权、未花费约束和价值守恒分开，签名正确不代表输入尚未消费。第三式说明确认依赖规范链和深度，不能把“收到交易”写成“最终结算”。第四式要求来源、重放、边界与失败证据同时存在，任一为零都不能发布。

取三笔UTXO：A拥有7与4，拟向B支付8并找零3。合法交易必须引用两笔输入、由A签名并产生8与3两个输出；若仅引用7、重复引用同一输入或把找零写成4，节点应在进入候选区块前拒绝。把合法交易放入区块后修改金额，重算Merkle根和区块摘要虽可让单块内部一致，却会破坏后续链接和累计共识证据。

## 三段可运行骨架

以下三段骨架把交易验证、区块追加与证据门禁串成最小闭环，帮助你在以太坊实验中重放状态转移与失败回滚。

<CodeTabs>
<Tab label="Python / 核心逻辑">

```python
def verify_transaction(tx, utxos, verify_signature):
    inputs = [utxos.get(ref) for ref in tx["inputs"]]
    if None in inputs or len(set(tx["inputs"])) != len(tx["inputs"]):
        return False
    return verify_signature(tx) and sum(inputs) >= sum(tx["outputs"])

def append_block(chain, block, hash_block, valid_tx):
    if block["previous"] != hash_block(chain[-1]):
        return {"accepted": False, "reason": "broken-link"}
    if not all(valid_tx(tx) for tx in block["transactions"]):
        return {"accepted": False, "reason": "invalid-transaction"}
    return {"accepted": True, "chain": [*chain, block]}
```

</Tab>
<Tab label="Python / 证据门禁">

```python
def evidence_gate(run):
    required = {"source", "version", "input", "trace", "output", "failure"}
    missing = sorted(required.difference(run))
    return {"pass": not missing, "missing": missing}
```

</Tab>
</CodeTabs>

第一段验证交易引用、重复输入、签名和价值约束；生产实现还要检查脚本、费用、锁定时间与整数范围。第二段先检查前块链接再检查交易，失败时不修改原链。第三段对实验记录做结构门禁，使成功轨迹和失败证据使用同一版本合同。

## 最小实例与重放顺序

先冻结创世状态、账户或UTXO集合、验证函数、网络成员、排序规则、确认条件和软件版本。按“构造交易 -> 本地签名 -> 邻居独立验证 -> 候选池 -> 打包或排序 -> 区块验证 -> 状态提交”记录每一步输入输出。只保存最后余额，会漏掉重复消费、错误签名被缓存、无效区块短暂传播或回滚未清理等问题。

正常样本使用两笔输入支付并找零；边界样本让输入恰好等于输出；单故障样本只破坏签名、前块摘要、背书数量或读版本中的一项。三组样本除该变量外保持相同，才能把拒绝原因归因到目标规则。随机网络实验保存种子、消息顺序和超时，不以一次收敛证明协议活性。

## 2017年版本边界与现代迁移

原书记录的是2017年前后的比特币客户端、以太坊钱包、Fabric版本与当时的扩容和安全事件。今天重放时保留概念坐标和状态不变量，但不照抄已经失效的下载地址、网络参数、命令或默认配置。历史截图用于解释接口，不作为当前安全保证。

迁移到现代实现时建立对应表：原书术语、历史行为、当前替代、兼容差异、验证方法和回退方案。以太坊共识变更、Fabric架构演进或钱包弃用必须显式披露；不能因为现代工具运行成功，就声称原书命令仍然有效，也不能用今天的特性改写当年的设计动机。

## 正式目录逐项讲解

### 第6章 区块链开发平台：以太坊

“第6章 区块链开发平台：以太坊”位于本页正式目录坐标 1/10。以太坊把交易扩展为确定性状态机调用。执行者从同一前状态、字节码和输入得到同一后状态；Gas限制资源，回滚保护原子性，但代码确定性并不保证业务意图、外部价格或权限配置正确。

复核时为“第6章 区块链开发平台：以太坊”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 6.1 项目介绍

“6.1 项目介绍”位于本页正式目录坐标 2/10。平台实验先冻结客户端、网络标识、创世配置、账户和合约版本。书中2017年的钱包和命令只作为历史坐标，今天的实验使用隔离测试环境，但仍复核同一交易输入、状态转移和失败回滚。

复核时为“6.1 项目介绍”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 6.1.1 项目背景

“6.1.1 项目背景”位于本页正式目录坐标 3/10。平台实验先冻结客户端、网络标识、创世配置、账户和合约版本。书中2017年的钱包和命令只作为历史坐标，今天的实验使用隔离测试环境，但仍复核同一交易输入、状态转移和失败回滚。

复核时为“6.1.1 项目背景”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 6.1.2 以太坊组成

“6.1.2 以太坊组成”位于本页正式目录坐标 4/10。以太坊把交易扩展为确定性状态机调用。执行者从同一前状态、字节码和输入得到同一后状态；Gas限制资源，回滚保护原子性，但代码确定性并不保证业务意图、外部价格或权限配置正确。

复核时为“6.1.2 以太坊组成”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 6.1.3 关键概念

“6.1.3 关键概念”位于本页正式目录坐标 5/10。平台实验先冻结客户端、网络标识、创世配置、账户和合约版本。书中2017年的钱包和命令只作为历史坐标，今天的实验使用隔离测试环境，但仍复核同一交易输入、状态转移和失败回滚。

复核时为“6.1.3 关键概念”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 6.1.4 官方钱包使用

“6.1.4 官方钱包使用”位于本页正式目录坐标 6/10。平台实验先冻结客户端、网络标识、创世配置、账户和合约版本。书中2017年的钱包和命令只作为历史坐标，今天的实验使用隔离测试环境，但仍复核同一交易输入、状态转移和失败回滚。

复核时为“6.1.4 官方钱包使用”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 6.2 以太坊应用

“6.2 以太坊应用”位于本页正式目录坐标 7/10。以太坊把交易扩展为确定性状态机调用。执行者从同一前状态、字节码和输入得到同一后状态；Gas限制资源，回滚保护原子性，但代码确定性并不保证业务意图、外部价格或权限配置正确。

复核时为“6.2 以太坊应用”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 6.2.1 测试链与私链

“6.2.1 测试链与私链”位于本页正式目录坐标 8/10。平台实验先冻结客户端、网络标识、创世配置、账户和合约版本。书中2017年的钱包和命令只作为历史坐标，今天的实验使用隔离测试环境，但仍复核同一交易输入、状态转移和失败回滚。

复核时为“6.2.1 测试链与私链”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 6.2.2 编写一个代币合约

“6.2.2 编写一个代币合约”位于本页正式目录坐标 9/10。以太坊把交易扩展为确定性状态机调用。执行者从同一前状态、字节码和输入得到同一后状态；Gas限制资源，回滚保护原子性，但代码确定性并不保证业务意图、外部价格或权限配置正确。

复核时为“6.2.2 编写一个代币合约”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

### 6.3 知识点导图

“6.3 知识点导图”位于本页正式目录坐标 10/10。知识点导图用于检查依赖：从状态对象指向授权证据、传播、排序、执行和风险；它是回放索引，不替代定义、公式、代码和失败样本。每条箭头都应能落到一个可观察输入输出。

复核时为“6.3 知识点导图”保存前状态、输入、授权或验证依据、候选输出、拒绝原因和后状态。先手算一个最小实例，再运行实现；若两者不同，停在首个哈希、签名、规则、消息、排序或状态差异处，不以最终余额相近掩盖中间错误。

## 六类复核证据

### 状态与所有权合同

列出链上对象的唯一标识、所有者或解锁条件、当前版本和允许的状态转移。UTXO模型检查引用与未花费性，账户模型检查发送者、nonce与余额；两者不能共用一个含糊的“余额正确”断言。

### 密码与编码合同

记录哈希和签名算法、参数、密钥来源、随机数、序列化与地址校验。编码只解决传输表示，哈希只提供摘要，签名只证明相应私钥授权；真实性、保密性与业务合法性需要额外证据。

### 网络与共识合同

保存节点身份、连接、消息、传播顺序、故障模型、候选集合和规范历史选择。共识安全性与活性分别验证；暂时不出块和两个节点接受冲突最终状态是不同故障，不能只用平均TPS描述。

### 执行与平台合同

合约或链码记录前状态、调用者、输入、读写集、资源消耗、事件和后状态。失败执行必须原子回滚，外部服务和预言机响应进入证据，避免把链外副作用误当成虚拟机可回滚状态。

### 攻击与恢复合同

每个风险写出资产、攻击者能力、入口、首个可观测异常、影响和恢复边界。私钥丢失、多数重组、合约权限泄漏与数据增长的恢复手段不同；“不可篡改”不能替代备份、暂停、升级和治理。

### 发布与独立复核合同

独立复核者从空目录读取来源与配置，先手算最小状态，再运行正常、边界和单故障样本。哈希、签名、事件、余额、拒绝原因和最终状态逐项一致后才发布；任何缺失证据都进入隔离清单而非静默跳过。

## 容易踩的坑

**现象 →** 调用合约时Gas不足导致交易回滚，但发起方账户余额仍然减少——你可以亲手翻车：设置Gas Limit为10000调用一个消耗50000 Gas的合约，交易失败但Gas费照扣。
**原因 →** EVM在Gas耗尽时回滚状态变更，但已消耗的Gas用于补偿矿工计算成本，不退还。
**修法 →** 估算Gas时预留20%余量，使用 `eth_estimateGas` 后再手动增加缓冲，避免因边界波动导致回滚。

**现象 →** 照书中2017年的步骤安装以太坊钱包和客户端，命令报错或网络参数不匹配。
**原因 →** 以太坊共识已从PoW迁移到PoS，旧版钱包和命令已失效，网络标识和创世配置均已变更。
**修法 →** 建立历史命令与现代替代的对应表，实验使用隔离测试环境，不照抄已失效的下载地址和默认配置。

**现象 →** 合约部署成功且单元测试通过，上线后却被攻击者利用权限漏洞转走全部资金。
**原因 →** 代码确定性只保证相同输入得到相同输出，不保证业务意图、权限配置或外部价格数据正确。
**修法 →** 上线前做权限审计和形式化验证，检查所有 `external` 和 `public` 函数的调用者限制，预言机数据需设价格偏差熔断。

## 小结

- 以太坊把交易扩展为确定性状态机调用
- Gas限制资源消耗，回滚保护原子性
- 代码确定性不保证业务意图或权限配置正确
- 2017年钱包和命令仅作历史坐标，不可直接照用
- 测试链实验需冻结客户端、网络标识和创世配置

<Exercises>

**问题 1**

请独立实现一个简化的EVM调用函数 `execute_call(state, caller, to, value, gas_limit, bytecode)`，要求：扣除调用者Gas预算，执行状态变更，Gas耗尽时回滚全部状态变更并返回 `out-of-gas`，执行成功时返回后状态和实际Gas消耗。要求：构造一个会消耗全部Gas的循环字节码和一个正常转账调用，验证前者回滚、后者状态更新。

<Answer>

关键点：维护一个 `gas_remaining` 计数器，每条指令扣费；Gas耗尽时恢复到调用前状态快照（深拷贝），成功时提交变更并返回 `gas_used = gas_limit - gas_remaining`。状态对象至少包含账户余额和nonce。

</Answer>

**问题 2**

给定一笔代币合约的 `transfer(from, to, amount)` 交易，请列出哪些字段进入执行证据（前状态、调用者、输入、读写集、事件、后状态），哪些属于2017年的历史工具产物（旧钱包界面截图、旧网络参数）。

<Answer>

执行证据：调用者地址、`to` 合约地址、`transfer` 函数选择器和参数、from余额前值、to余额前值、Transfer事件日志、from和to余额后值、Gas消耗。历史产物：2017年Mist钱包界面截图、旧测试网ChainID、已弃用的geth命令行参数——这些仅作历史坐标，不进入执行证据。

</Answer>

</Exercises>

## 本章回顾

以2017年书中平台为历史坐标，讲清账户状态、交易、Gas、EVM、测试链和代币合约的执行证据。 掌握标准不是背诵项目名或算法缩写，而是能冻结状态、授权、传播、排序、执行、证据和失败边界。通过标准为：能从交易输入重放一次合约状态转移，核对余额、Gas、事件与失败回滚，并标明历史工具已失效的部分。


## 前后导航

- [上一页：第5章 区块链扩展：扩容、侧链和闪电网络](/learn/blockchain-plain/07-05-scaling-sidechains-lightning/bp-05-scaling-sidechains-lightning)
- [下一页：第7章 区块链开发平台：超级账本](/learn/blockchain-plain/09-07-hyperledger/bp-07-hyperledger)

<Attribution
  mode="independent-rewrite"
  sourceBasis="publisher-catalog"
  workTitle="蒋勇、文颜成、李浩、李耀军《白话区块链》机械工业出版社 / 2017"
  adaptedUrl="https://e.dangdang.com/products/1901310198.html"
/>

