---
title: 字符串和格式化输入/输出
type: C
section: C基础
order: 4
description: 掌握C语言中字符串的本质——字符数组+空字符，以及scanf/printf的格式化输入输出，包含常用格式说明的完整用法
demo: true
math: false
sourceUrl: "https://www.informit.com/store/c-primer-plus-9780321928429"
draft: false
qualityVersion: 2
practiceMode: simulation
sourceMode: independent-rewrite
officialUnitId: cpr-04
---

import { Objectives } from "@/components/mdx/objectives";
import { Term } from "@/components/mdx/term";
import { PrintfFormatFlagsDiagram } from "@/components/mdx/diagrams/printf-format-flags-diagram";
import { Callout } from "@/components/mdx/callout";
import { Step, Stepper } from "@/components/mdx/stepper";
import { ScanfInputDiagram } from "@/components/mdx/diagrams/scanf-input-diagram";
import { IOMemoryBufferDiagram } from "@/components/mdx/diagrams/io-memory-buffer-diagram";
import { Answer, Exercises } from "@/components/mdx/exercises";
import { Glossary, GlossaryItem } from "@/components/mdx/glossary";
import { Attribution } from "@/components/mdx/attribution";
import { StringsIoDecisionLab, StringsIoMechanismMap, StringsIoFailureDiagram } from "@/components/mdx/c-primer-plus/strings-io";

<Objectives>

- 能区分字符数组容量、字符串首个空字符前的长度与指针大小，并说明何时 `sizeof array == strlen(array) + 1`
- 能用 printf 的各种格式标志（宽度、精度、对齐、前缀）精确控制输出格式
- 能回答：用 scanf("%d", &n) 读完一个整数后紧接着用 getchar()，为什么读到的不是下一个字符而是换行符？

</Objectives>

<StringsIoMechanismMap />

<StringsIoDecisionLab />

<StringsIoFailureDiagram />

## 为什么 C 的输入输出这么"啰嗦"？

想象你要给一个自动化仓库发指令："把一个红色盒子放到 3 号架第 2 层"。这条指令必须精确到每个细节——什么颜色、哪个架子、哪一层，错一个仓库机器人就放不对。

在 C 语言里，输入和输出就是这样的：电脑不会"猜"你想把数据存到哪、想用什么格式显示。你必须告诉它：**"这个数字是整数（%d）、存在变量 n 的内存地址（&n）"**。每个细节都得自己说清楚，少说一个就出问题。

**如果没有这种精确控制呢？** 那 printf 输出 3.14159 时你不知道该显示 2 位还是 10 位小数，scanf 不知道把读到的 42 存到哪个变量——数据就在缓冲区里漂着，进不了你的程序，你也看不见正确的输出。这一章就教你掌握 C 输入输出的"格式标签"，把数据在程序和外部世界之间精确搬运。

## C 字符串的本质——char 数组加一个看不见的尾巴

在 C 语言里没有独立的字符串类型。<Term def="由字符序列和终止空字符组成的数据约定。处理字符串的函数必须能在可访问对象边界内找到首个空字符，否则调用不满足接口前提。">字符串（string）</Term> 依赖末尾的 <Term def="数值为零的字符，写作 \0，用作 C 字符串终止标记；它不同于字符 '0'，也不依赖执行字符集是否为 ASCII。">空字符（null character）</Term> `\0` 判断结束。

字符串字面值（双引号包围的文本，如 `"Hello"`）在编译时就被处理成带有 `\0` 的字符数组。你写 `char str[] = "Hello";`，编译器自动分配 6 个字节而不是 5 个——多出来的那一个就是末尾的 `\0`。

<Term def={'string.h 中的函数，从给定地址开始查找到首个空字符并返回此前字符数。调用者必须提供有效且已终止的字符串；它不知道数组容量。'}>strlen</Term> 对 `"Hello"` 返回 5，而 `sizeof "Hello"` 返回包含终止字符的数组大小 6。这个“差 1”只适用于数组恰好由该字符串初始化且首个空字符位于最后一格的情形；数组有额外容量、内部更早出现空字符或表达式已退化为指针时都不成立。

```c
#include <stdio.h>
#include <string.h>
int main(void) {
    char str[] = "Hello";

    printf("strlen(str) = %zu\n", strlen(str));  // 输出 5
    printf("sizeof(str) = %zu\n", sizeof(str));  // 输出 6

    // sizeof 算上了 C 自动给你加的那个 \0！
    return 0;
}
```

记住这条**铁律**：C 语言的字符串必须靠 `\0` 判断结束——没有 `\0` 的不是字符串，只是普通字符数组。

### printf 的格式说明——百分号后面那短短几个字符的全套能力

上一章我们已经用 `printf()` 输出过数据，但 `printf` 的能力远不止 `%d` 和 `%f`。<Term def="printf 格式串中以 % 开头的指令，如 %d、%f、%.2f。它告诉 printf：后面的参数是什么类型、要怎么显示。">格式说明符（format specifier）</Term> 的通式是 `%[标志][宽度][.精度]类型`，由三个可选的控制字段和一个必选的类型字段组成。

<PrintfFormatFlagsDiagram />

来逐一看每个字段的作用：

* **类型（必选）：** 告诉 printf 参数是什么类型——`d`（有符号整数）、`u`（无符号整数）、`f`（浮点数）、`c`（字符）、`s`（字符串）、`x`（十六进制）。
* **宽度（可选）：** `%10d` 表示总宽度至少 10 个字符——不够的用空格填满。数字默认右对齐，加 `-`（`%-10d`）变成左对齐。
* **.精度（可选）：** `%.2f` 指浮点数保留 2 位小数；`%.5s` 指字符串最多打印前 5 个字符。
* **标志（可选）：** `-` 左对齐、`+` 强制显示正号、`#` 给八/十六进制加前缀（如 `%#x` → `0xff`）、`0` 用 0 补足宽度而不是空格。

下面这段代码把上面所有玩法串在一起：

```c
#include <stdio.h>
int main(void) {
    int  n = 42;
    double pi = 3.1415926535;

    printf("默认:    |%d|\n", n);      // |42|
    printf("宽度10:  |%10d|\n", n);    // |        42|  ⟵ 右对齐
    printf("左对齐:  |%-10d|\n", n);   // |42        |  ⟵ 左对齐
    printf("补零:    |%010d|\n", n);   // |0000000042|  ⟵ 左边填0
    printf("带符号:  |%+d|\n", n);     // |+42|        ⟵ 强制显示正号
    printf("十六进制:|%#x|\n", 255);   // |0xff|       ⟵ 0x前缀
    printf("浮点默认:|%f|\n", pi);     // |3.141593|   ⟵ 默认6位
    printf("精确2位: |%.2f|\n", pi);   // |3.14|       ⟵ 保留2位
    printf("宽10精2: |%10.2f|\n", pi); // |      3.14| ⟵ 宽度+精度组合
    printf("左宽10精2:|%-10.2f|\n", pi);//|3.14      | ⟵ 组合标志

    return 0;
}
```

<Callout type="tip">
  格式说明的组合顺序是固定的：`%` → 标志 → 宽度 → `.精度` → 类型。记住这个顺序就不会写错——`%-10.2f` 是对的，`%.2-10f` 是错的。
</Callout>

### scanf 的用法——每次输入都是一次"格式匹配"

如果说 `printf` 是把数据按你指定的格式"编码"成文字输出，那 `scanf()` 就是反过来——从输入里按格式"解码"数据存入变量。<Term def={'C 标准库的格式化输入函数。与 printf 相反：它按格式说明从标准输入中读取数据，存到指定变量的地址里。需要包含 stdio.h 头文件。'}>scanf</Term> 的用法跟 `printf` 相似——也是用格式说明来告诉电脑"输入里的数据长什么样"。

<Callout type="warn">
  **scanf 最大的不同：你必须传递变量的地址，而不是变量的值！** 在变量名前加 <Term def="一元运算符 &，放在变量名前表示取该变量的内存地址。scanf 需要用地址来知道把读到的数据写到哪。">取地址运算符（&）</Term>——`scanf("%d", &n)` 中的 `&n` 就是变量 `n` 在内存中的地址。忘记写 `&` 是 scanf 的经典翻车现场——程序崩溃或输出乱七八糟。
</Callout>

下面用 Stepper 一步步看 scanf 到底怎么工作的——每一步都有对应的图示：

## 动手：scanf 从键盘到变量的三步

> 猜一猜：`scanf("%d %f", &n, &f);` 输入 `42 3.14` 回车后，`n` 是 42、`f` 是 3.14——但回车符去哪了？它留在输入缓冲区里了！通过下面 Stepper 一步步揭晓。

<Stepper>
  <Step title="第一步：键盘输入进入缓冲区">
    <ScanfInputDiagram step={1} />
    当你敲 `42 3.14↵`（↵ = 回车键），这些字符首先进入一个叫**输入缓冲区**（stdin buffer）的临时存储区。scanf 不是直接从键盘读的——它是从这个缓冲区里读。缓冲区是一个先到先取的队列：你按顺序输入的字符在这里排好队，等 scanf 来取。
  </Step>
  <Step title="第二步：scanf 按格式说明匹配">
    <ScanfInputDiagram step={2} />
    `scanf("%d %f", &n, &f)` 的格式串 `"%d %f"` 告诉 scanf：
    - 先跳过空白字符（空格、回车、制表符）
    - 读一个整数 → 存到 `&n` 指向的地址（n 变成 42）
    - 再跳过空白
    - 读一个浮点数 → 存到 `&f` 指向的地址（f 变成 3.14）
    scanf 的关键规则：**格式说明 `%d` 遇到第一个不能识别为数字的字符就停**——这里读到空格就停止读整数，下一个 `%f` 从空格后面的 `3.14` 接着读。
  </Step>
  <Step title="第三步：缓冲区残留——经典 bug 来源">
    <ScanfInputDiagram step={3} />
    扫描完成后，行尾换行通常仍在流中。下一次 `%d`、`%f`、`%s` 会自动跳过前导空白，但 `%c`、`%[` 和 `getchar()` 不会，因此它们可能立即取得该换行。
    读取单个非空白字符可用 `scanf(" %c", &ch)`；需要可靠处理整行时应统一用 `fgets`，再用 `strtol` 等函数解析。单独调用一次 `getchar()` 只消费一个字符，不能保证清理含多余输入的整行。
  </Step>
</Stepper>

### printf / scanf 与缓冲区——不能只看表面

<IOMemoryBufferDiagram />

缓冲区也影响输出可见时机。正常程序终止会关闭并刷新输出流；但没有换行的交互提示可能在程序等待输入时仍留在缓冲区。终端上的 `stdout` 常为行缓冲但不是无条件保证，需立即显示提示时调用 `fflush(stdout)`。

不要把“换行必刷新”或“所有 scanf 都不吃换行”当成规则。应根据流缓冲模式、具体转换说明是否跳过空白以及函数返回值判断状态。

## 代码详解：四个核心场景逐段拆解

### 场景一：声明和输出字符串——字符串的三种"写法"

```c
#include <stdio.h>
#include <string.h>
int main(void) {
    // 方式①：{} 逐字符初始化（C 会自动加 \0）
    char s1[] = {'H', 'i', '\0'};   // "Hi"——手动写 \0

    // 方式②："" 字符串字面值（编译器自动加 \0）
    char s2[] = "World!";           // "World!"——自动加 \0

    // 方式③：char 指针指向字面值
    const char *s3 = "Hello";       // 指针指向只读的"Hello\0"

    printf("%s %s %s\n", s1, s2, s3);   // 输出 Hi World! Hello
    printf("strlen(s2) = %zu\n", strlen(s2));  // 6
    return 0;
}
```

三种方式有细微差别：方式②创建可修改数组，其存储期取决于声明位置；方式③指向字符串字面量。通过该指针修改字面量的行为未定义，无论目标平台是否把它放在只读段。`%s` 会从地址读取直到首个空字符，因此调用者必须保证对象边界内存在终止符。

### 场景二：printf 格式组合——宽度、精度、标志一起上

```c
#include <stdio.h>
int main(void) {
    double price = 12.50;
    int    count = 3;

    // 记账风格：左对齐标签 + 右对齐金额
    printf("%-20s %8.2f\n", "单价:", price);       // 单价:            12.50
    printf("%-20s %8d\n",   "数量:", count);       // 数量:                3
    printf("%-20s %8.2f\n", "总价:", price * count);// 总价:            37.50

    return 0;
}
```

`%-20s` 中 `-` 表示左对齐，`20` 是宽度——标签左边贴着显示，右边用空格补齐到 20。`%8.2f` 中 `8` 是总宽度（含小数点和小数位），`.2` 是 2 位小数——金额 37.50 刚好占满 8 个字符宽度，右边对齐。这套格式化组合在打印表格时非常实用。

### 场景三：scanf 读取混合类型——每一个百分号都在"扫描"

```c
#include <stdio.h>
int main(void) {
    int  age;
    float height;
    char name[20];

    printf("请输入：姓名 年龄 身高\n");
    if (scanf("%19s %d %f", name, &age, &height) != 3) {
        fputs("输入格式错误\n", stderr);
        return 1;
    }

    printf("%s，%d 岁，身高 %.2f 米\n", name, age, height);
    return 0;
}
```

在此函数实参中 `name` 转换为首元素指针，所以不写 `&name`；`age` 和 `height` 则传对象地址。`%19s` 最多写入 19 个非空白字符并追加空字符，匹配 `name[20]` 的容量。返回值 3 表示三个转换都成功；不检查返回值就可能继续读取未赋值对象。

### 场景四：gets 的危险与 fgets 的安全替代

```c
#include <stdio.h>
int main(void) {
    char line[20];

    // ❌ gets(line)——已从 C11 标准删除，永不使用！
    // gets 不知道数组有多大，用户输入 100 个字符会把栈冲垮

    // ✅ 安全替代：fgets
    printf("请输入一行文字：");
    fgets(line, sizeof(line), stdin);   // 最多读 sizeof(line)-1 个字符
    fputs(line, stdout);                // 输出读到的内容（fgets 会保留末尾 \n）

    return 0;
}
```

`gets()` 在 C11 中被删除，因为接口无法获知目标容量。`fgets()` 接受数组、最大写入数量和输入流，成功时最多存 `n-1` 个字符并追加空字符。若换行在容量内会一并保存；长行只读到一段，EOF 前最后一行也可能没有换行。调用者必须检查返回值并决定是否继续读取剩余片段。

<Callout type="trap">
  `fgets` 只有在本次读取遇到换行且容量足够时才保存 `\n`。可先确认返回值，再用 `strcspn` 去掉已读到的换行；若数组中没有换行且尚未到 EOF，说明输入行可能被截成多段，需要继续读取或报告过长。
</Callout>

## 容易踩的坑

<Callout type="trap">
  **现象**：`char str[5] = "Hello";` 可能只得到五个非空字符；再以 `%s` 输出时行为不可预测。
  **原因**：初始化器有五个可见字符和一个终止符，而数组只有五格。C 允许字符数组在大小不足终止符时省略它，但结果不是有效 C 字符串；`%s` 随后越界查找空字符会产生未定义行为。
  **修法**：声明字符数组时永远给 `\0` 留一个位置——`char str[6] = "Hello";` 或者直接不写大小让编译器算：`char str[] = "Hello";`。
</Callout>

<Callout type="trap">
  **现象**：`int n; scanf("%d", n);` 程序直接崩溃——段错误（Segmentation Fault）。
  **原因**：`%d` 要求对应参数是 `int *`，实际却传了经默认提升后的 `int` 值，且该值还未初始化。可变参数类型不匹配会产生未定义行为，不保证表现为段错误。
  **修法**：给 scanf 传地址——`scanf("%d", &n);`。唯一的例外是数组名和指针：`char name[20]; scanf("%s", name);` 不需要 `&`，因为数组名本身就是地址。
</Callout>

<Callout type="trap">
  **现象**：`scanf("%d", &n); char ch; scanf("%c", &ch);` ——第一次输完 `42` 回车后，`ch` 瞬间变成了 `\n`，根本不等你输入。
  **原因**：scanf 在第一轮把 `42` 读走，但回车符 `\n` 留在缓冲区。第二轮 `scanf("%c", &ch)` 立刻从缓冲区读到了这个残留的 `\n`——%c 不跳过空白。这就是经典的"输入被跳过"现象。
  **修法**：若目标是下一个非空白字符，在 `%c` 前加空格；若目标是下一整行，统一用 `fgets` 读取并解析。不要假设一次 `getchar()` 能清理任意残留输入。
</Callout>

<Callout type="trap">
  **现象**：`printf("进度 %d%", 80);` 或 `printf("值: %d\n", 3.14159);` 的输出不可依赖。
  **原因**：格式串以孤立 `%` 结束，或转换说明要求的类型与提升后的实参类型不匹配，行为未定义；不能用固定字节读取方式解释结果。
  **修法**：第一条把 `%d%` 改成 `%d%%`。第二条 `printf("值: %f\n", 3.14159);`——类型和格式说明必须一一对应：int 用 %d、double 用 %f、char* 用 %s。
</Callout>

## 小结

- C 字符串以空字符终止；`strlen` 找首个终止符，`sizeof` 查询数组或指针本身，两者只在特定数组初始化场景差 1
- printf 的格式通式 %[标志][宽度][.精度]类型 —— 组合这些字段就能完成日常所有输出格式需求
- scanf 必须传变量地址（&），从输入缓冲区按格式匹配读取——跟 printf 方向相反但格式体系相同
- 输入残留要按目标协议处理：非空白字符可在 `%c` 前加空格，整行输入应统一用 `fgets` 并检查完整性
- 永远不用 gets——用 fgets(name, sizeof(name), stdin) 安全替代；puts/fputs 做行输出

<Exercises>

**问题 1（改代码型）** 下面程序至少有七处错误或隐患。找出并修正，且检查每次输入的返回值。

```c
#include <stdio.h>
int main(void) {
    char name[5] = "Xiaow";
    int age;
    float score;
    char grade;

    gets(name);
    scanf("%f", score);
    scanf("%c", &grade);
    printf("姓名: %s 年龄: %c 成绩: %d\n", name, age, score);
    return 0;
}
```

<Answer>

七处问题：
① `char name[5]="Xiaow"`——五个可见字符还需要一个终止空字符，数组太小。改成容量更大的数组。
② `gets(name)`——gets 已从 C11 删除且极度危险。改正为 `fgets(name, sizeof(name), stdin);`。
③ `scanf("%f", score)`——漏了 `&`，scanf 需要变量地址。改正为 `scanf("%f", &score);`。
④ `age` 从未成功读取便被输出，读取其不确定值会导致未定义行为。
⑤ `scanf("%c", &grade)` 会读到前一次转换留下的空白，可按需求改成 `" %c"`。
⑥ `printf` 里 `%c` 对应 `age`，类型不匹配，应使用 `%d`。
⑦ `printf` 里 `%d` 对应经提升后的 `score`，应使用 `%f`。

修正后：

```c
#include <stdio.h>
#include <string.h>
int main(void) {
    char name[20];
    int age;
    float score;
    char grade;

    printf("请输入姓名：");
    if (!fgets(name, sizeof(name), stdin)) return 1;
    name[strcspn(name, "\n")] = '\0';

    printf("请输入年龄和成绩：");
    if (scanf("%d %f", &age, &score) != 2) return 1;

    printf("请输入等级：");
    if (scanf(" %c", &grade) != 1) return 1;

    printf("姓名: %s 年龄: %d 成绩: %.1f 等级: %c\n", name, age, score, grade);
    return 0;
}
```

</Answer>

**问题 2（问答型）** 读下面程序，写出前五行结果，并说明为什么第六行没有可移植答案。

```c
#include <stdio.h>
#include <string.h>
int main(void) {
    char str[] = "CPro";
    printf("① strlen = %zu\n", strlen(str));
    printf("② sizeof = %zu\n", sizeof(str));
    printf("③ |%10s|\n", str);
    printf("④ |%-10s|\n", str);
    printf("⑤ str[4] = %d\n", str[4]);
    printf("⑥ str[5] = %d\n", str[5]);
    return 0;
}
```

<Answer>

① 输出 `strlen = 4`——"CPro" 有 4 个字符 C/P/r/o，\0 不计入 strlen。
② 输出 `sizeof = 5`——数组 `str` 分配了 5 个字节（C/P/r/o/\0），sizeof 算上 \0。
③ 输出 `|      CPro|`——%10s 总宽度 10，字符串只有 4 字符，默认右对齐，左边填 6 个空格。
④ 输出 `|CPro      |`——%-10s 左对齐，右边填 6 个空格。
⑤ 输出 `str[4] = 0`——数组第 5 个元素是数值为零的空字符，与执行字符集是否 ASCII 无关。
⑥ 输出 `str[5] = ...`——**这句有 bug！** 数组只有 5 个元素（下标 0~4），`str[5]` 越界访问，行为未定义——可能输出随机值，也可能崩溃。

</Answer>

**问题 3（综合实现型）** 用 64 字节数组读取一行，检查是否完整，去掉已读换行，再按默认、右对齐和左对齐格式输出。

<Answer>

```c
#include <stdio.h>
#include <string.h>
int main(void) {
    char line[64];

    printf("请输入一行文字（最多 63 字符）：");
    if (!fgets(line, sizeof(line), stdin)) return 1;
    size_t cut = strcspn(line, "\n");
    int complete = line[cut] == '\n';
    line[cut] = '\0';

    printf("\n你输入了 %zu 个字符，完整行=%s\n",
           strlen(line), complete ? "yes" : "no");

    printf("默认输出:  |%s|\n", line);
    printf("右对齐50:  |%50s|\n", line);
    printf("左对齐30:  |%-30s|\n", line);

    return 0;
}
```

`strcspn` 找到首个换行或字符串末尾。若没有读到换行，可能是 EOF 前最后一行，也可能只是长行的第一段；生产代码还应结合 `feof(stdin)` 决定是否继续读取。

</Answer>

</Exercises>

## 名词解释

<Glossary>

<GlossaryItem term="字符串（string）">
  C 语言中存文本的方式——就是 char 类型的字符一个接一个在内存里排好队，然后队尾放一个空字符 \0 当"终点站"标记。C 没有专门的 string 类型，所谓的"字符串"本质上就是 char 数组——只是多了一个 \0 结尾。详见本章「C 字符串的本质」和 CStringMemoryDiagram 图示。
</GlossaryItem>

<GlossaryItem term="空字符（null character）">
  数值为零的字符，写作 `\0`，用于终止 C 字符串；它与字符 `'0'` 不同，也不依赖 ASCII。
</GlossaryItem>

<GlossaryItem term="strlen">
  从给定字符串起点查找到首个空字符并返回此前字符数。它不知道数组容量；`sizeof` 与它只在特定数组初始化情形下恰好差 1。
</GlossaryItem>

<GlossaryItem term="printf()">
  C 标准库里最常用的输出函数（`<stdio.h>`）。它像一个填空题的模板引擎：格式串里的普通文字原样输出，百分号 % 开头的格式说明被替换成后面参数的值。百分号之后控制三个维度——宽度（占几位）、精度（留几位小数）、类型（int/float/char）——合起来就是 `%[标志][宽度][.精度]类型`。详见本章「printf 的格式说明」一节和 PrintfFormatFlagsDiagram 图示。
</GlossaryItem>

<GlossaryItem term="格式说明符（format specifier）">
  printf 和 scanf 中以 % 开头、以类型字母结尾的指令。%d 表示"这里要填一个有符号整数"，%f 表示"这里要填一个浮点数"。不同函数对格式说明符的规则不同——比如 scanf 里 double 用 %lf，但 printf 里 double 和 float 都用 %f。详见本章「printf 的格式说明」和 PrintfFormatFlagsDiagram 图示。
</GlossaryItem>

<GlossaryItem term="scanf">
  按格式从输入流执行转换并写入目标对象的函数。数值转换传对象地址；数组表达式在函数实参中通常转换为首元素指针。应限制字符串字段宽度并检查成功转换项数。
</GlossaryItem>

<GlossaryItem term="取地址运算符（&）">
  返回对象地址的一元运算符。`scanf` 的 `%d` 等转换要求匹配的对象指针；传值而非地址会造成可变参数类型不匹配和未定义行为。
</GlossaryItem>

<GlossaryItem term="输入缓冲区（input buffer）">
  标准 I/O 实现为输入流维护的暂存状态。格式转换只消费匹配所需字符；未消费字符会供后续读取使用，而不同转换对前导空白的处理不同。
</GlossaryItem>

</Glossary>

## 原版目录概念补充核对

以下条目补齐官方目录中容易被示例主线掩盖的概念。它们不重复罗列目录，而是明确每项概念的机制、适用边界和验收证据。

### 常量与预处理器：机制、边界与证据

在《字符串和格式化输入/输出》的官方单元 cpr-04 中，常量与预处理器连接本章第 3 组知识约束。学习时要同时说明它接受什么输入、改变什么状态、在何种边界失效；再以本章示例的编译诊断、固定输入输出或失败用例复核结论，不能只记术语名称。

<Attribution
  adaptedFrom="C Primer Plus, Sixth Edition, Chapter 4: Character Strings and Formatted Input/Output"
  adaptedUrl="https://www.informit.com/store/c-primer-plus-9780321928429"
  mode="independent-rewrite"
  sourceBasis="outline-only"
/>
