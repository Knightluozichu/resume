---
title: "三维几何、层次场景与相机"
type: B
section: "计算机图形学与渲染"
order: 4
description: "承载第三版 Ch3、Ch6、Ch7、Ch11、Ch13、Ch16：Dürer renderer、三维数学、层次模型、变换、相机与平台"
demo: true
math: true
sourceUrl: "https://www.informit.com/store/computer-graphics-principles-and-practice-9780133373707"
draft: true
---

import { Cgp3dGraphicsDiagram, CgpHierarchyDiagram, CgpCameraPlatformDiagram } from "@/components/mdx/cg-principles-practice/diagrams/cgp-3d-graphics";
import { CgpStepFlowDiagram } from "@/components/mdx/cg-principles-practice/diagrams/cgp-step-flow";
import { Objectives, Callout, Attribution, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step } from "@/components/mdx/mdx-components";

<Objectives>

- 能从 Dürer window 推导 pinhole projection，并指出 clipping、visibility 与 shading 缺失时的限制
- 能使用 point/vector/plane、dot/cross、barycentric 与 frame 解决三维几何问题
- 能计算层次模型的 world transform、normal transform 与 camera/view transform
- 能比较 fixed-function、programmable API、scene framework 与 game engine 的控制和责任边界

</Objectives>

## 直觉：相机不是把 z 删除掉

先预测：两条平行于成像平面的线在照片上一定汇聚吗？父节点缩放后，子节点绕“自己的轴”旋转还是绕被缩放的轴旋转？答案取决于几何关系和 frame composition，不是“3D 乘一个投影矩阵”就结束。

Ch3 An Ancient Renderer Made Modern 从 Dürer 的观察窗建立第一台 renderer；Ch6 用 fixed-function 3D 与 hierarchical modeling 组织场景；Ch7 给出 2-space/3-space 数学；Ch11/13 完成三维变换和 camera；Ch16 比较承载这些责任的平台。

<Cgp3dGraphicsDiagram />

## Ch3 An Ancient Renderer Made Modern

Dürer 装置把 eye $e$、画面平面与三维点 $p$ 连成一条 ray，交点是图像位置。若 eye 在原点、image plane 为 $z=-n$，相机空间点 $(x,y,z)$ 的平面坐标为：

$$
x_p=-n\frac{x}{z},\qquad y_p=-n\frac{y}{z}.
$$

这是相似三角形，不是“模拟人眼”这一句话。平行于 view direction 的线会趋向 vanishing point；平行于 image plane 的线仍可保持平行。

<Term def="从相机中心穿过图像样本形成射线，并取射线与场景几何的最近有效交点来确定覆盖的成像方法">光线投射</Term>可直接解决 visibility；若只投影 mesh vertices 再连 wireframe，会遇到：视点后方点、跨 near plane 边、隐藏面、透视插值与遮挡顺序。

完整最小 renderer 需要 model/camera transform、clip、coverage/ray intersection、nearest visibility 和 shading。中间量应显示 camera-space geometry、clip decision、projected coordinates 与 selected hit。

## Ch7 Essential Mathematics and the Geometry of 2-Space and 3-Space

point 属于 affine space，vector 表示点之差。dot product 给长度/投影/夹角；cross product 给与两个向量垂直的有向面积向量：

$$
a\cdot b=\|a\|\|b\|\cos\theta,
\qquad \|a\times b\|=\|a\|\|b\|\sin\theta.
$$

<Term def="由一个原点和一组有序基向量定义的坐标参照；几何对象本身不变，坐标随参照系改变">坐标标架</Term>把抽象 point/vector 变成数值。basis 非正交时不能用转置代替 inverse；handedness 决定 cross、winding 和 rotation 正方向。

平面可写为 $n\cdot x+d=0$。三角形中的 point 用 barycentric coordinates：

$$
p=\lambda_0p_0+\lambda_1p_1+\lambda_2p_2,
\quad \lambda_0+\lambda_1+\lambda_2=1.
$$

全部 $\lambda_i\ge0$ 表示在闭三角形内，但 edge inclusion 要统一，否则相邻 triangle 会产生 double hit 或 crack。退化 triangle 的面积接近零时必须有明确 policy。

<Term def="用仿射组合表达点相对 simplex 顶点的位置，常用于三角形包含测试和属性插值的坐标">重心坐标</Term>在投影后插值 texture/depth 时还要区分 screen-linear 与 perspective-correct。

## Ch6 Fixed-Function 3D Graphics and Hierarchical Modeling

<CgpHierarchyDiagram />

fixed-function pipeline 通过 transform、lighting、texture、blend 等预定义状态完成常见渲染。它的历史价值是暴露 stage/state；现代 programmable pipeline 把部分 stage 交给 shader，但 depth/raster/blend 等固定功能仍存在，不能把“fixed-function”误解成整个 GPU 不可编程。

<Term def="以父子节点组织局部变换和对象，使父状态变化递归影响后代 world transform 的建模方式">层次建模</Term>中：

$$
M_{world(child)}=M_{world(parent)}M_{local(child)}.
$$

robot arm 的 shoulder、elbow、hand 各在局部 frame 建模。matrix stack 的 push/multiply/draw/pop 是 legacy API 的一种遍历实现；现代 ECS/scene graph 仍需同样的 parent-before-child update、dirty propagation、cycle prevention 与 bounds refresh。

```cpp
void updateWorld(Node& node, const Mat4& parentFromLocal) {
    node.worldFromLocal = parentFromLocal * node.parentFromLocal;
    node.worldBounds = transformBounds(node.worldFromLocal, node.localBounds);
    for (Node* child : node.children) updateWorld(*child, node.worldFromLocal);
}
```

非均匀 parent scale 会改变 child basis，旋转后可能产生 shear；如果设计要求 rigid joint，应分离 scale 或对 transform components 设约束。

## Ch11 Transformations in Three Dimensions

三维 affine transform 用 $4\times4$ 齐次矩阵。translation、scale、shear 与 reflection 直接推广；rotation 更复杂：可用 axis-angle、matrix 或 unit quaternion。

Rodrigues 公式围绕单位轴 $u$ 旋转 vector $v$：

$$
R_u(\theta)v=v\cos\theta+(u\times v)\sin\theta+u(u\cdot v)(1-\cos\theta).
$$

<Term def="以单位四元数表示三维旋转，便于稳定组合和球面插值，但仍需固定乘法与标架约定">旋转四元数</Term>避免欧拉角参数化的 gimbal lock，不表示“任何插值都自动正确”。两个姿态需处理 antipodal equivalence、shortest path、normalization 和 coordinate convention。

normal 不是普通 direction。若 model 线性部分为 $A$，切平面向量变为 $At$，保持垂直要求：

$$
n'=(A^{-1})^Tn.
$$

纯 rotation 时等于 $An$；nonuniform scale/shear 时直接乘 $A$ 会错。singular transform 没有 normal inverse，资产和 runtime 必须拒绝或降级。

## Ch13 Camera Specifications and Transformations

<Term def="由相机位置、朝向、视场或成像范围、aspect、near/far 与投影类型组成的观察规格">相机规格</Term>先定义 camera world pose，再取 inverse 得到 view-from-world。`lookAt(eye,target,upHint)` 必须处理 eye≈target、up 与 forward 平行，以及 handedness。

**Perspective camera（透视相机）**以 eye/center of projection 和 frustum 表达近大远小；**orthographic camera（正交相机）**以平行投射方向和 box 表达与深度无关的图像尺度。二者都需要 view transform、clipping 和 depth，只是 projection 几何不同。

透视投影将 view frustum 映到 clip volume。projection 输出 homogeneous clip coordinates，先按 clip inequalities 裁剪，再除以 $w$ 得 NDC，最后 viewport 映射到 pixels/depth。正交投影通常 $w$ 保持常数，但仍执行通用 homogeneous divide；不是“完全不做除法”。

矩阵具体符号依 handedness、forward axis、NDC z range、reversed-Z 和 storage。不能把 `w=-z` 当所有 API 的定义。应以已知 near/far/corner points 做端到端测试。

perspective depth 对 view distance 非线性，传统映射近处精度高；near plane 过小会浪费精度。reversed-Z 配合 floating depth 和合适 projection 可改善远距离精度，但 compare/clear/linearization 全链要一致。

## Ch16 Survey of Real-Time 3D Graphics Platforms

<CgpCameraPlatformDiagram />

<Term def="平台向应用暴露的数据模型、生命周期和绘制能力；抽象厚度决定应用与平台各自承担的场景、资源和调度责任">图形平台抽象</Term>可分层比较：

- thin graphics API：显式 resource/pipeline/command/sync，控制强，应用责任大；
- programmable rendering framework：封装 shader/material/pass/asset，保留较多渲染控制；
- scene graph/CAD/visualization toolkit：提供 hierarchy、camera、selection、data model；
- game engine：再加入 editor、animation、physics、streaming、platform build；
- browser/mobile/UI platform：受 sandbox、power、form factor 与 integration 约束。

平台不是按“越厚越高级”排序。选择要根据 performance predictability、content scale、tooling、portability、debuggability、team 与 product lifecycle，用代表性 vertical slice 测试。

## 三步三维验收

<Stepper>
<Step title="第一步：固定 frame、handedness 与 clip 约定">

显示 model/world/view basis、camera frustum、near/far、NDC range、winding 与 depth policy。

<CgpStepFlowDiagram caption="空间约定必须先于投影矩阵抄写。" stage={1} stages={["Specify：frames + handedness + vector side + clip/depth + camera"]} />

</Step>
<Step title="第二步：逐级观察层次与投影">

显示 local/world bounds、parent composition、camera-space vertices、clip result、NDC、viewport 与 selected ray hit。

<CgpStepFlowDiagram caption="Dürer 几何、scene hierarchy 和 GPU pipeline 用同一批中间量连接。" stage={2} stages={["Specify：frames + handedness + vector side + clip/depth + camera", "Inspect：hierarchy → world → view → clip → NDC → visibility"]} />

</Step>
<Step title="第三步：运行解析、退化与平台测试">

验证 known points、inverse round-trip、normal perpendicularity、near clipping、degenerate basis，并跨两种平台比较 output 与时间。

<CgpStepFlowDiagram caption="数学正确、边界稳健和平台可迁移同时验收。" stage={3} stages={["Specify：frames + handedness + vector side + clip/depth + camera", "Inspect：hierarchy → world → view → clip → NDC → visibility", "Validate：analytic geometry + degenerates + image reference + platform cost"]} />

</Step>
</Stepper>

## 常见误区

<Callout type="trap">
**现象 →** 只投影 vertices 后连线，认为 renderer 已完成。**原因 →** 忽略 clipping、coverage、nearest visibility 与 perspective interpolation。**修法 →** 用跨 near-plane triangle、互相遮挡面和可视 hit 中间量验收。
</Callout>

<Callout type="trap">
**现象 →** 把 `w=-z`、NDC `[-1,1]` 和矩阵下标复制到所有 API。**原因 →** 把某个 convention 当投影原理。**修法 →** 先声明 handedness/depth/vector/storage，用 near/far/corners 单元测试矩阵。
</Callout>

<Callout type="trap">
**现象 →** 父节点 nonuniform scale 后子节点法线、高光和关节旋转异常。**原因 →** direction/normal/rigid pose 都直接乘同一矩阵。**修法 →** normal 用 inverse transpose，关节限制 rigid transform，检查 determinant/shear。
</Callout>

## 练习

<Exercises>

**问题 1：Dürer renderer 到完整 renderer 还缺什么？**

<Answer>
点投影只给图像交点；还需处理相机后/near plane clipping、primitive coverage 或 ray intersection、最近可见 surface、属性插值、shading 和 framebuffer/display。每步都要可视中间结果。
</Answer>

**问题 2：为什么 view matrix 是 camera world transform 的 inverse？**

<Answer>
camera pose 把 camera-local 坐标放到 world；渲染需要把 world 点表达在 camera frame，因此应用逆映射。若 camera worldFromView 为刚体，inverse 可由 rotation transpose 和相反平移构造。
</Answer>

**问题 3：怎样验证 normal matrix？**

<Answer>
选择与 tangent 垂直的 normal，对 nonuniform scale/shear 变换 tangent，并用 inverse-transpose 变换 normal；检查 dot 仍接近零。再覆盖 pure rotation、reflection 和 near-singular case。
</Answer>

**问题 4：如何比较 thin API 与 engine？**

<Answer>
实现相同 vertical slice，比较资源/场景控制、工具与开发时间、CPU/GPU/内存、调试证据、平台覆盖和 fallback。不能只用 feature list 或“更底层更快”下结论。
</Answer>

</Exercises>

## 小结

Ch3、6、7、11、13、16 把针孔几何推进为完整三维系统：数学定义 frame 与 query，hierarchy 组合对象，camera 组织观察，clip/visibility 生成图像，平台分配应用与框架责任。通过标准是空间约定、中间量、退化输入和平台证据完整。

<Glossary>
<GlossaryItem term="光线投射">从相机经图像样本查询最近场景交点的成像方法。</GlossaryItem>
<GlossaryItem term="坐标标架">由原点和有序基定义的坐标参照。</GlossaryItem>
<GlossaryItem term="重心坐标">以 simplex 顶点 affine combination 表示位置的坐标。</GlossaryItem>
<GlossaryItem term="层次建模">以父子局部变换递归构造 world pose 的建模方式。</GlossaryItem>
<GlossaryItem term="旋转四元数">用于组合和插值三维旋转的单位四元数。</GlossaryItem>
<GlossaryItem term="相机规格">位置、朝向、投影范围和 depth 参数组成的观察定义。</GlossaryItem>
<GlossaryItem term="图形平台抽象">平台与应用之间关于场景、资源、绘制和生命周期的责任合同。</GlossaryItem>
</Glossary>

<Attribution adaptedFrom="Computer Graphics: Principles and Practice, Third Edition, Chapters 3, 6, 7, 11, 13 and 16" adaptedUrl="https://www.informit.com/store/computer-graphics-principles-and-practice-9780133373707" />
