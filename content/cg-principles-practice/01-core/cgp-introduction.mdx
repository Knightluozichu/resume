---
title: "图形学导论、视觉感知与交互"
type: B
section: "计算机图形学与渲染"
order: 1
description: "承载第三版 Ch1、Ch5、Ch21：图形学问题空间、人类视觉系统、2D/3D 交互映射与评价"
demo: true
math: true
sourceUrl: "https://www.informit.com/store/computer-graphics-principles-and-practice-9780133373707"
draft: true
---

import { CgpIntroductionDiagram, CgpPerceptionDiagram, CgpInteractionDiagram } from "@/components/mdx/cg-principles-practice/diagrams/cgp-introduction";
import { CgpStepFlowDiagram } from "@/components/mdx/cg-principles-practice/diagrams/cgp-step-flow";
import { Objectives, Callout, Attribution, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step } from "@/components/mdx/mdx-components";

<Objectives>

- 能依据 Ch1 Introduction 划分 modeling、rendering、animation、interaction 与 display 的职责
- 能依据 Ch5 An Introduction to Human Visual Perception 分析亮度、颜色、空间频率、深度和运动线索
- 能依据 Ch21 Interaction Techniques 比较 multitouch、arcball/trackball 与 Unicam 的自由度映射
- 能设计同时测量任务成功率、运动轨迹、感知误差和系统延迟的交互实验

</Objectives>

## 直觉：图像只有被观察和操纵时才完成任务

先预测：两个像素的物理 radiance 相同，观察者一定觉得一样亮吗？鼠标向右拖动，三维物体就一定绕世界 Y 轴旋转吗？都不一定。背景、适应、显示编码和交互映射会改变结果。

Ch1 定义问题与抽象，Ch5 定义观察者，Ch21 定义从用户动作到图形状态的闭环。三章共同要求：不能只证明“渲染器输出了像素”，还要证明这些像素可感知、可解释、可操纵。

<CgpIntroductionDiagram />

## Ch1 Introduction：图形学的输入输出不止模型和图像

<Term def="研究如何用计算系统表示、生成、操纵与显示视觉信息，并支持人与视觉模型交互的领域">计算机图形学</Term>包括：

- modeling：选择 shape、material、light、camera、motion 的表示；
- rendering：求 visibility 与 radiance/image samples；
- animation/simulation：生成随时间变化的状态；
- interaction：把用户意图映射为 scene/view/action；
- display：把编码信号通过设备转成观察者看到的光。

其中 **Graphics Pipeline（图形管线）** 把 application、geometry/visibility、sampling/shading 与 display-oriented output 组织为可并行的阶段；它是一种重要抽象，不是图形学的全部定义。

图像处理、视觉与图形学会共享 sampling、optimization、learning 和 geometry，不能只用“模型→图像 / 图像→模型”完全切开。例如 inverse rendering 同时估计 geometry/material/light，neural rendering 也跨越表示与生成。

### 抽象是带损失的合同

<Term def="隐藏实现细节并暴露稳定操作的接口；它同时决定使用者能表达什么、不能表达什么以及错误出现在哪里">图形抽象</Term>可以是 immediate 2D canvas、retained scene graph、raster pipeline、ray query、material graph 或 game engine。抽象越厚，常见任务越快；但控制、可观测性与性能预测可能更弱。

选择抽象要列出：目标任务、数据规模、更新时间、质量/延迟预算、平台和 debug needs。不能因为某 API 流行就断言它代表图形学原理。

一个帧系统可写成 producer-consumer：

$$
S_{t+1}=Update(S_t, input_t),\qquad I_t=Render(S_{t+1}, camera_t),
$$

但 input sampling、simulation、render 和 display 可能跨帧重叠；交互延迟必须沿时间线测量，而非只看 renderer GPU time。

## Ch5 An Introduction to Human Visual Perception

<CgpPerceptionDiagram />

眼睛把光学图像投到 retina；rods 对低照度与周边运动敏感，cones 支持明视觉与颜色。之后的神经处理强调对比、边缘、结构和变化。我们感知的不是逐像素 radiance 副本。

<Term def="观察者对局部亮度差异相对于适应背景的敏感程度；同一绝对差异在不同背景下可见性不同">对比敏感度</Term>说明误差阈值依 spatial frequency、luminance、eccentricity、motion 与 adaptation。简化 Weber contrast：

$$
C_W=\frac{L_{target}-L_{background}}{L_{background}}.
$$

它只适合一定条件，不能代替 HDR、暗视觉或复杂图案的完整模型。

### 深度、颜色与运动线索

- depth：occlusion、relative size、perspective、texture gradient、shading、stereo disparity、motion parallax；
- color：由 spectrum、cone response、adaptation 与 context 共同形成，metamer 说明不同光谱可产生相同颜色感知；
- motion：temporal sampling、persistence、追踪与 optic flow 共同影响平滑和延迟；
- attention：显著性可引导观察，但任务与先验会改变注视位置。

视觉角决定屏幕特征在视网膜上的尺度：

$$
\theta=2\arctan\left(\frac{s}{2d}\right),
$$

其中 $s$ 是物理尺寸，$d$ 是观察距离。因此“1 像素误差”在手机、投影和头显上的可见程度不同。

<Term def="当两个不同光谱在给定观察条件下产生相同颜色匹配响应时，它们构成的同色异谱现象">同色异谱</Term>提醒我们 RGB 是观察/设备相关表示，不是完整 spectrum。感知实验必须固定 display calibration、ambient light、view distance 与 adaptation time。

## Ch21 Interaction Techniques：把设备动作映射到任务自由度

<CgpInteractionDiagram />

<Term def="把鼠标、触控、笔、控制器或手势样本转换为对象、相机或界面参数变化的规则及反馈闭环">交互映射</Term>至少说明 input space、controlled degrees of freedom、gain/constraint、selection、feedback 和 clutch/recovery。

### Multitouch 2D manipulation

两个触点从 $p_1,p_2$ 移到 $q_1,q_2$，可估计 translation、rotation 与 uniform scale：

$$
s=\frac{\|q_2-q_1\|}{\|p_2-p_1\|},\qquad
\phi=angle(q_2-q_1)-angle(p_2-p_1).
$$

变换围绕手势 centroid 组合；触点接近时方向不稳定，需要最小距离、filter 与 gesture-state transition。不能每帧重新选择触点而让对象跳变。

### Arcball、trackball 与 Unicam

arcball 将二维 cursor 投到虚拟球/双曲延拓，起止向量 $u,v$ 产生旋转轴 $u\times v$ 与角度 $\arccos(u\cdot v)$。它提供路径相关的三维旋转感；必须明确 object-space 还是 camera-space composition，并保证 quaternion normalization。

trackball 是一类“像滚动球”的映射，具体投影/增益实现不同，不能把名称当统一算法。Unicam 通过单鼠标在 rotate、pan、dolly 等 mode 间切换，降低同时控制 6-DOF 的困难，但 mode inference、focus point 与 transition feedback 决定可用性。

<Term def="为交互动作选择参照对象、坐标系、旋转中心或关注点，从而决定相同输入产生何种空间运动的上下文">交互参照系</Term>错误会表现为物体突然绕远处旋转、camera roll、drag 方向反直觉。debug view 应显示 pivot、basis、ray/pick point 与 active mode。

## 从感知目标到交互评价

交互技术不是只看“拖得动”。一个可复现实验要固定 task、target、device、display、latency 与 participant protocol，记录：

- completion time、success/error、path length、overshoot 与 mode error；
- end-to-end input-to-photon latency 与 dropped samples；
- subjective workload/comfort 和可学习性；
- 不同视距、尺寸、对比、色觉和运动能力下的 accessibility。

Fitts' law 可作为 pointing task 的描述模型：

$$
MT=a+b\log_2\left(1+\frac{D}{W}\right),
$$

但参数来自设备与用户实验，不是 UI 自动通过的公式。三维 selection、occlusion、depth 与 clutch 还会违反简单一维假设。

## 三步闭环实验

<Stepper>
<Step title="第一步：定义任务、观察条件与参照系">

固定 display/view distance、lighting、target、device、input coordinate、controlled DOF、pivot 和成功条件。

<CgpStepFlowDiagram caption="先让任务和输入输出空间可复现。" stage={1} stages={["Specify：observer + display + task + device + spaces + DOF"]} />

</Step>
<Step title="第二步：记录完整时间线与中间量">

显示 raw/filtered input、pick ray、pivot、active mode、scene state、render/present timestamp 与最终轨迹。

<CgpStepFlowDiagram caption="反馈错误与渲染延迟必须能分开归因。" stage={2} stages={["Specify：observer + display + task + device + spaces + DOF", "Trace：samples → mapping → state → render → present → perception"]} />

</Step>
<Step title="第三步：比较技术与边界人群">

对 multitouch、arcball/trackball、Unicam 做 counterbalanced task；比较速度、错误、路径、延迟与主观负担。

<CgpStepFlowDiagram caption="交互结论来自任务数据和观察条件，不来自作者偏好。" stage={3} stages={["Specify：observer + display + task + device + spaces + DOF", "Trace：samples → mapping → state → render → present → perception", "Validate：performance + errors + latency + workload + accessibility"]} />

</Step>
</Stepper>

## 常见误区

<Callout type="trap">
**现象 →** 把图形学定义成“模型到图像”，忽略 animation、interaction 与 display。**原因 →** 只从离线 renderer 看领域。**修法 →** 画 scene/input/render/display 闭环，并为每条边写 representation 与 latency。
</Callout>

<Callout type="trap">
**现象 →** 认为 GPU 每个像素完全独立，所以任何 shader 都会线性加速。**原因 →** 忽略 SIMD/SIMT 分组、memory locality、divergence、synchronization 与 fixed units。**修法 →** 将此处只作为 throughput 动机，硬件细节交由 Ch38 实测。
</Callout>

<Callout type="trap">
**现象 →** 鼠标 drag 能旋转就宣布 arcball 完成。**原因 →** 没有定义 sphere mapping、pivot、composition space、边界投影与输入延迟。**修法 →** 可视化球面向量和 quaternion，用往返轨迹、极端 cursor 与任务实验验收。
</Callout>

## 练习

<Exercises>

**问题 1：为什么相同 radiance 的 patch 可能看起来亮度不同？**

<Answer>
感知受周围 luminance、adaptation、spatial frequency、glare、display 与观察位置影响。应在校准条件下测 contrast/visibility，而不是把 framebuffer 数值直接等同主观亮度。
</Answer>

**问题 2：如何区分交互 mapping 错误与 rendering latency？**

<Answer>
记录硬件 input timestamp、raw/filtered sample、mapped state、frame submission、present/scanout；用离线轨迹验证 mapping 数值，再比较 state-change 到 photon 的时间。mapping 错会产生错误方向/幅度，latency 则是正确状态延后显示。
</Answer>

**问题 3：arcball 为什么适合 quaternion？**

<Answer>
球面起止向量直接定义最短轴角旋转，可转换为单位 quaternion 后稳定组合，避免欧拉角万向节锁。仍需规范化、处理近反向向量，并声明左/右乘和 object/camera space。
</Answer>

**问题 4：怎样选择厚 engine abstraction 还是薄 graphics API？**

<Answer>
按目标功能、平台、团队、内容工具、控制需求、性能可预测性、调试与生命周期比较。用一个代表性 vertical slice 测量开发成本和运行证据，不应把抽象厚度本身当优劣。
</Answer>

</Exercises>

## 小结

Ch1、Ch5、Ch21 把图形学定义为观察者参与的交互系统：抽象组织表示和算法，视觉系统决定可见价值，交互技术把意图映射为状态。完成标准是任务、空间、观察条件、时间线和实验数据全部可复查。

<Glossary>
<GlossaryItem term="计算机图形学">表示、生成、操纵和显示视觉信息并支持交互的计算领域。</GlossaryItem>
<GlossaryItem term="图形抽象">隐藏实现并暴露操作，同时限定表达、控制与可观测性的接口。</GlossaryItem>
<GlossaryItem term="对比敏感度">观察者对相对背景亮度差和空间频率的检测能力。</GlossaryItem>
<GlossaryItem term="同色异谱">不同光谱在给定观察条件下产生相同颜色匹配的现象。</GlossaryItem>
<GlossaryItem term="交互映射">从设备样本到对象、相机或界面参数变化的规则。</GlossaryItem>
<GlossaryItem term="交互参照系">决定输入围绕哪个 pivot、basis 和空间解释的上下文。</GlossaryItem>
</Glossary>

<Attribution adaptedFrom="Computer Graphics: Principles and Practice, Third Edition, Chapters 1, 5 and 21" adaptedUrl="https://www.informit.com/store/computer-graphics-principles-and-practice-9780133373707" />
