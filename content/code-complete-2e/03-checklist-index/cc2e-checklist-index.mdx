---
title: "核对表目录"
type: B
section: "代码大全（第2版·2006中文版）"
order: 3
description: "核对表目录：从无上下文的清单推进到能拒绝缺证据结论的任务门禁，让每次勾选都有条件、证据和回归路径。"
demo: true
math: true
sourceUrl: "https://book.douban.com/reading/10145819/"
qualityVersion: 2
practiceMode: design
sourceMode: independent-rewrite
officialUnitId: cc2e-checklist-index
draft: false
---

import { Objectives } from "@/components/mdx/objectives";
import { Callout } from "@/components/mdx/callout";
import { Term } from "@/components/mdx/term";
import { Exercises, Answer } from "@/components/mdx/exercises";
import { Glossary, GlossaryItem } from "@/components/mdx/glossary";
import { Attribution } from "@/components/mdx/attribution";
import { Cc2eChecklistIndexLab } from "@/components/mdx/code-complete-2e/v2/cc2e-checklist-index";

<Objectives>

- 能从任务类型和进入条件定位合适的核对表，而不是把一张清单用于所有问题。
- 能为每个条目写出判断条件、证据位置、拒绝理由和回归检查。
- 能区分“全部勾选”和“证据充分”，并通过故障与复位实验验证门禁是否有效。

</Objectives>

## 为什么需要这一机制

核对表目录的价值不在于让勾选数量变大，而在于让任务进入正确的检查路径。一个没有上下文的清单会鼓励机械勾选；一个有门禁的目录会先确认任务类型，再规定逐项证据、责任人和拒绝条件。缺少证据的条目必须能阻止结论通过。

<Callout type="info" title="范围与事实来源">
  本页范围依据[2006 中文第 2 版公开试读目录](https://book.douban.com/reading/10145819/)，软件质量与工程过程边界参考[IEEE SWEBOK](https://www.computer.org/education/bodies-of-knowledge/software-engineering/v4)和[ISO/IEC/IEEE 29119 软件测试标准](https://www.iso.org/standard/81291.html)。本文是独立教学重写。
</Callout>

## 先避开三个清单误区

<Callout type="trap" title="误区一：勾选数量就是质量">
  现象 → 只追求完成率。原因 → 条件和证据没有绑定到条目。修法 → 每项必须附可定位的结果或拒绝理由。
</Callout>

<Callout type="trap" title="误区二：一张清单适用于所有任务">
  现象 → 用发布检查表判断探索性原型。原因 → 任务类型和进入条件被忽略。修法 → 先定位任务，再选择适用的检查路径。
</Callout>

<Callout type="trap" title="误区三：通过一次就永远通过">
  现象 → 代码或环境改变后不回归。原因 → 没有版本、输入和观察窗口。修法 → 固定基线，改变一个条件并在故障后重放。
</Callout>

## 核心合同与操作术语

门禁可以写成：所有条目都必须同时满足 condition 和 evidence。<Term def="决定使用哪组检查项的交付活动、风险类型和当前阶段">任务类型</Term>定义问题；<Term def="允许检查开始并规定输入、版本和责任人的前置事实">进入条件</Term>防止清单被滥用。

<Term def="按条目判断并为每项留下结果、证据和责任人的过程">逐项核对</Term>把抽象建议变成动作；<Term def="能让别人定位结果的测试输出、变更记录、截图或审查链接">证据位置</Term>让勾选可复查；<Term def="说明为何拒绝通过以及修订后如何重新检查的记录">拒绝与回归</Term>使失败也成为流程的一部分。

## 目录节点逐项深读

### 核对表目录

核对表目录的工程任务是把构建任务映射到门禁。先写任务、输入、版本和检查目的，再从目录选表；每一项同时写状态、证据位置和责任人。一个没有证据的勾选必须拒绝，修复后从相同基线回归，而不是直接改成通过。

## 最小可重放实现

~~~text
baseline = freeze(task, version, input)
checklist = locate_by_task_type(task)
for item in checklist:
    result = evaluate(item.condition, baseline)
    record(result, item.evidenceLocation, owner)
reject_if_missing_evidence()
reset()
assert replay(baseline) == baseline.trace
~~~

正常轨迹验证全链路；边界轨迹改变任务类型或进入条件；故障轨迹注入没有证据的勾选；复位轨迹重新定位清单并回归。门禁的输出应包含通过、拒绝和下一步动作。

## 先预测，再操作证据实验

先预测任务类型、证据位置或拒绝条件改变后哪一个节点会先变化，再切换一个场景。组件显示的是清单门禁模型，不代表项目的自动批准。

<Cc2eChecklistIndexLab />

## 练习与答案

<Exercises>

**问题 1：选择清单。** 一个探索性原型准备转入发布，请写出任务类型、进入条件和三条必须有证据的检查项。

<Answer>
  任务类型是发布准备而不是探索；进入条件包括固定版本、目标环境和回滚负责人。检查项可包括关键路径测试、配置差异和回滚演练，每项都要附运行结果或记录位置。
</Answer>

**问题 2：拒绝无证据勾选。** 团队已经勾完所有项目，但某一条“边界条件已验证”没有输出。门禁应如何处理？

<Answer>
  保持拒绝状态，记录缺少的输入、预期和证据位置；补一个最小边界实验后再回归。完成率不能覆盖这个空洞。
</Answer>

**问题 3：设计回归。** 清单通过后接口版本改变，哪些条件必须重新固定？

<Answer>
  固定新版本、相同输入、环境、观察窗口和判定标准，重新执行受影响条目及其依赖回归；记录首个偏离和未受影响的证据，不能只复制旧勾选。
</Answer>

</Exercises>

## 术语表

<Glossary>
<GlossaryItem term="任务类型">决定采用哪一组检查项的交付活动和风险。</GlossaryItem>
<GlossaryItem term="进入条件">允许检查开始的版本、输入、环境和责任人约束。</GlossaryItem>
<GlossaryItem term="逐项核对">对每个条目给出判断、证据和责任人的过程。</GlossaryItem>
<GlossaryItem term="证据位置">别人可以定位结果的输出、记录或审查链接。</GlossaryItem>
<GlossaryItem term="拒绝与回归">缺证据时拒绝，通过修订后从同一基线重新检查。</GlossaryItem>
</Glossary>

## 本页小结

核对表目录把“勾选”变成有条件的门禁：先定位任务，再逐项判断，附上证据位置，缺证据就拒绝，修复后从同一基线回归。这样清单才是降低遗漏的工具，而不是制造完成率的装饰。

<Attribution
  mode="independent-rewrite"
  sourceBasis="outline-only"
  workTitle="Steve McConnell, Code Complete, Second Edition"
  adaptedUrl="https://book.douban.com/reading/10145819/"
/>
