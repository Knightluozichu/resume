---
title: "第6章：可以工作的类"
type: B
section: "代码大全（第2版·2006中文版）"
order: 13
description: "第6章：可以工作的类：从“类只是数据与函数容器”推进到“类在生命周期内守住抽象合同”，以目录节点、专属机制实验和故障复位验收。"
demo: true
math: true
sourceUrl: "https://book.douban.com/reading/10145819/"
qualityVersion: 2
practiceMode: code
sourceMode: independent-rewrite
draft: true
---

import { Cc2e06WorkingClassesMechanismLab } from "@/components/mdx/code-complete-2e/diagrams/cc2e-06-working-classes";
import {
  Objectives,
  Callout,
  Term,
  Glossary,
  GlossaryItem,
  Exercises,
  Answer,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能说明 第6章：可以工作的类 怎样把“类只是数据与函数容器”推进为“类在生命周期内守住抽象合同”
- 能沿 领域概念 → ADT契约 → 类接口 → 实现封装 → 使用验证 重建对象、状态、决定和证据
- 能使用本页合同完成一次代码修改
- 能注入“公开可变字段或半初始化对象让调用者绕过不变量”，找到首差并确认重置恢复基线

</Objectives>

## 为什么需要这一机制

第6章：可以工作的类 解决的不是一个可背诵口号，而是从 **类只是数据与函数容器** 到 **类在生命周期内守住抽象合同** 的可观察变化。它的核心机制是：可工作的类用不变量约束状态，以最小接口暴露稳定能力，通过封装、组合或受控继承管理变化。

学习 第6章：可以工作的类 时先预测 领域概念、ADT契约、类接口、实现封装、使用验证 的输入与输出，再改变一个直接条件。最终结果即使看似正确，只要中间状态违反合同、故障不能隔离或重置不能回到同一基线，本页结论就不通过。

<Callout type="info" title="范围与事实来源">
  第6章：可以工作的类 由 [2006 中文第 2
  版公开试读目录](https://book.douban.com/reading/10145819/)
  限定范围，技术机制依据 [C++ Core
  Guidelines](https://isocpp.github.io/CppCoreGuidelines/CppCoreGuidelines)
  独立重写。目录核对只证明主题坐标；本页不宣称取得、翻译或复现原书正文、插图、表格、核对表与代码。
</Callout>

## 核心合同

$$
valid(object)\iff invariant(constructor)\land invariant(after\ public\ call)
$$

第6章：可以工作的类 的合同变量必须绑定到同一版本、同一输入、同一单位和同一观察窗口。先由合同推出预期，再观察 类接口 与 使用验证；如果只是修改阈值来迎合结果，实验失去裁决能力。

## 目录节点到四级证据

以下节点逐项给出“出现—解释—视觉/实验—练习验证”证据。第6章：可以工作的类 不把目录词频当作覆盖率；每一项都必须能在专属机制链中指出状态变化，并在章末练习清单中被复核。

### 第6章 可以工作的类

在 **第6章：可以工作的类** 中，目录节点 **第6章 可以工作的类** 落在“领域概念”检查点。这里的技术含义是：用构造和公开操作守住对象不变量，优先组合稳定职责；它影响“抽象质量”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **第6章 可以工作的类** 时，尝试绕过接口制造半有效对象，确认不变量不可被外部破坏。先写预期，再运行正常值、恰好边界和一个故障；若出现“公开可变字段或半初始化对象让调用者绕过不变量”，就在 第6章：可以工作的类 的 **领域概念** 保存首个偏离、拒绝结果和复位后的重放证据。

### 6.1 类的基础：抽象数据类型

在 **第6章：可以工作的类** 中，目录节点 **6.1 类的基础：抽象数据类型** 落在“ADT契约”检查点。这里的技术含义是：用构造和公开操作守住对象不变量，优先组合稳定职责；它影响“封装边界”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **6.1 类的基础：抽象数据类型** 时，尝试绕过接口制造半有效对象，确认不变量不可被外部破坏。先写预期，再运行正常值、恰好边界和一个故障；若出现“公开可变字段或半初始化对象让调用者绕过不变量”，就在 第6章：可以工作的类 的 **ADT契约** 保存首个偏离、拒绝结果和复位后的重放证据。

### 需要用到ADT的例子

在 **第6章：可以工作的类** 中，目录节点 **需要用到ADT的例子** 落在“类接口”检查点。这里的技术含义是：用构造和公开操作守住对象不变量，优先组合稳定职责；它影响“组合”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **需要用到ADT的例子** 时，尝试绕过接口制造半有效对象，确认不变量不可被外部破坏。先写预期，再运行正常值、恰好边界和一个故障；若出现“公开可变字段或半初始化对象让调用者绕过不变量”，就在 第6章：可以工作的类 的 **类接口** 保存首个偏离、拒绝结果和复位后的重放证据。

### 使用ADT的益处

在 **第6章：可以工作的类** 中，目录节点 **使用ADT的益处** 落在“实现封装”检查点。这里的技术含义是：用构造和公开操作守住对象不变量，优先组合稳定职责；它影响“继承”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **使用ADT的益处** 时，尝试绕过接口制造半有效对象，确认不变量不可被外部破坏。先写预期，再运行正常值、恰好边界和一个故障；若出现“公开可变字段或半初始化对象让调用者绕过不变量”，就在 第6章：可以工作的类 的 **实现封装** 保存首个偏离、拒绝结果和复位后的重放证据。

### 更多的ADT示例

在 **第6章：可以工作的类** 中，目录节点 **更多的ADT示例** 落在“使用验证”检查点。这里的技术含义是：用构造和公开操作守住对象不变量，优先组合稳定职责；它影响“包组织”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **更多的ADT示例** 时，尝试绕过接口制造半有效对象，确认不变量不可被外部破坏。先写预期，再运行正常值、恰好边界和一个故障；若出现“公开可变字段或半初始化对象让调用者绕过不变量”，就在 第6章：可以工作的类 的 **使用验证** 保存首个偏离、拒绝结果和复位后的重放证据。

### 在非面向对象环境中用ADT处理多份数据实例

在 **第6章：可以工作的类** 中，目录节点 **在非面向对象环境中用ADT处理多份数据实例** 落在“领域概念”检查点。这里的技术含义是：用构造和公开操作守住对象不变量，优先组合稳定职责；它影响“抽象质量”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **在非面向对象环境中用ADT处理多份数据实例** 时，尝试绕过接口制造半有效对象，确认不变量不可被外部破坏。先写预期，再运行正常值、恰好边界和一个故障；若出现“公开可变字段或半初始化对象让调用者绕过不变量”，就在 第6章：可以工作的类 的 **领域概念** 保存首个偏离、拒绝结果和复位后的重放证据。

### ADT和类

在 **第6章：可以工作的类** 中，目录节点 **ADT和类** 落在“ADT契约”检查点。这里的技术含义是：用构造和公开操作守住对象不变量，优先组合稳定职责；它影响“封装边界”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **ADT和类** 时，尝试绕过接口制造半有效对象，确认不变量不可被外部破坏。先写预期，再运行正常值、恰好边界和一个故障；若出现“公开可变字段或半初始化对象让调用者绕过不变量”，就在 第6章：可以工作的类 的 **ADT契约** 保存首个偏离、拒绝结果和复位后的重放证据。

### 6.2 良好的类接口

在 **第6章：可以工作的类** 中，目录节点 **6.2 良好的类接口** 落在“类接口”检查点。这里的技术含义是：用构造和公开操作守住对象不变量，优先组合稳定职责；它影响“组合”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **6.2 良好的类接口** 时，尝试绕过接口制造半有效对象，确认不变量不可被外部破坏。先写预期，再运行正常值、恰好边界和一个故障；若出现“公开可变字段或半初始化对象让调用者绕过不变量”，就在 第6章：可以工作的类 的 **类接口** 保存首个偏离、拒绝结果和复位后的重放证据。

### 好的抽象

在 **第6章：可以工作的类** 中，目录节点 **好的抽象** 落在“实现封装”检查点。这里的技术含义是：把本质复杂度隔离在有名字的职责和接口之后，避免跨层泄漏；它影响“继承”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **好的抽象** 时，改变一个需求，统计需要理解和修改的独立边界数。先写预期，再运行正常值、恰好边界和一个故障；若出现“公开可变字段或半初始化对象让调用者绕过不变量”，就在 第6章：可以工作的类 的 **实现封装** 保存首个偏离、拒绝结果和复位后的重放证据。

### 良好的封装

在 **第6章：可以工作的类** 中，目录节点 **良好的封装** 落在“使用验证”检查点。这里的技术含义是：用构造和公开操作守住对象不变量，优先组合稳定职责；它影响“包组织”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **良好的封装** 时，尝试绕过接口制造半有效对象，确认不变量不可被外部破坏。先写预期，再运行正常值、恰好边界和一个故障；若出现“公开可变字段或半初始化对象让调用者绕过不变量”，就在 第6章：可以工作的类 的 **使用验证** 保存首个偏离、拒绝结果和复位后的重放证据。

### 6.3 有关设计和实现的问题

在 **第6章：可以工作的类** 中，目录节点 **6.3 有关设计和实现的问题** 落在“领域概念”检查点。这里的技术含义是：把本质复杂度隔离在有名字的职责和接口之后，避免跨层泄漏；它影响“抽象质量”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **6.3 有关设计和实现的问题** 时，改变一个需求，统计需要理解和修改的独立边界数。先写预期，再运行正常值、恰好边界和一个故障；若出现“公开可变字段或半初始化对象让调用者绕过不变量”，就在 第6章：可以工作的类 的 **领域概念** 保存首个偏离、拒绝结果和复位后的重放证据。

### 包含（“有一个……”的关系）

在 **第6章：可以工作的类** 中，目录节点 **包含（“有一个……”的关系）** 落在“ADT契约”检查点。这里的技术含义是：把“包含（“有一个……”的关系）”放回 可工作的类用不变量约束状态，以最小接口暴露稳定能力，通过封装、组合或受控继承管理变化；它影响“封装边界”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **包含（“有一个……”的关系）** 时，固定其余条件，只改变与“包含（“有一个……”的关系）”直接相连的一个输入或决定。先写预期，再运行正常值、恰好边界和一个故障；若出现“公开可变字段或半初始化对象让调用者绕过不变量”，就在 第6章：可以工作的类 的 **ADT契约** 保存首个偏离、拒绝结果和复位后的重放证据。

### 继承（“是一个……”关系）

在 **第6章：可以工作的类** 中，目录节点 **继承（“是一个……”关系）** 落在“类接口”检查点。这里的技术含义是：用构造和公开操作守住对象不变量，优先组合稳定职责；它影响“组合”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **继承（“是一个……”关系）** 时，尝试绕过接口制造半有效对象，确认不变量不可被外部破坏。先写预期，再运行正常值、恰好边界和一个故障；若出现“公开可变字段或半初始化对象让调用者绕过不变量”，就在 第6章：可以工作的类 的 **类接口** 保存首个偏离、拒绝结果和复位后的重放证据。

### 成员函数和数据成员

在 **第6章：可以工作的类** 中，目录节点 **成员函数和数据成员** 落在“实现封装”检查点。这里的技术含义是：把单一职责、前置条件、后置条件和错误语义放在调用边界；它影响“继承”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **成员函数和数据成员** 时，给最小、最大和非法参数各运行一次，并检查返回与状态副作用。先写预期，再运行正常值、恰好边界和一个故障；若出现“公开可变字段或半初始化对象让调用者绕过不变量”，就在 第6章：可以工作的类 的 **实现封装** 保存首个偏离、拒绝结果和复位后的重放证据。

### 构造函数

在 **第6章：可以工作的类** 中，目录节点 **构造函数** 落在“使用验证”检查点。这里的技术含义是：把单一职责、前置条件、后置条件和错误语义放在调用边界；它影响“包组织”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **构造函数** 时，给最小、最大和非法参数各运行一次，并检查返回与状态副作用。先写预期，再运行正常值、恰好边界和一个故障；若出现“公开可变字段或半初始化对象让调用者绕过不变量”，就在 第6章：可以工作的类 的 **使用验证** 保存首个偏离、拒绝结果和复位后的重放证据。

### 6.4 创建类的原因

在 **第6章：可以工作的类** 中，目录节点 **6.4 创建类的原因** 落在“领域概念”检查点。这里的技术含义是：用构造和公开操作守住对象不变量，优先组合稳定职责；它影响“抽象质量”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **6.4 创建类的原因** 时，尝试绕过接口制造半有效对象，确认不变量不可被外部破坏。先写预期，再运行正常值、恰好边界和一个故障；若出现“公开可变字段或半初始化对象让调用者绕过不变量”，就在 第6章：可以工作的类 的 **领域概念** 保存首个偏离、拒绝结果和复位后的重放证据。

### 应该避免的类

在 **第6章：可以工作的类** 中，目录节点 **应该避免的类** 落在“ADT契约”检查点。这里的技术含义是：用构造和公开操作守住对象不变量，优先组合稳定职责；它影响“封装边界”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **应该避免的类** 时，尝试绕过接口制造半有效对象，确认不变量不可被外部破坏。先写预期，再运行正常值、恰好边界和一个故障；若出现“公开可变字段或半初始化对象让调用者绕过不变量”，就在 第6章：可以工作的类 的 **ADT契约** 保存首个偏离、拒绝结果和复位后的重放证据。

### 总结：创建类的理由

在 **第6章：可以工作的类** 中，目录节点 **总结：创建类的理由** 落在“类接口”检查点。这里的技术含义是：用构造和公开操作守住对象不变量，优先组合稳定职责；它影响“组合”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **总结：创建类的理由** 时，尝试绕过接口制造半有效对象，确认不变量不可被外部破坏。先写预期，再运行正常值、恰好边界和一个故障；若出现“公开可变字段或半初始化对象让调用者绕过不变量”，就在 第6章：可以工作的类 的 **类接口** 保存首个偏离、拒绝结果和复位后的重放证据。

### 与具体编程语言相关的问题

在 **第6章：可以工作的类** 中，目录节点 **与具体编程语言相关的问题** 落在“实现封装”检查点。这里的技术含义是：把“与具体编程语言相关的问题”放回 可工作的类用不变量约束状态，以最小接口暴露稳定能力，通过封装、组合或受控继承管理变化；它影响“继承”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **与具体编程语言相关的问题** 时，固定其余条件，只改变与“与具体编程语言相关的问题”直接相连的一个输入或决定。先写预期，再运行正常值、恰好边界和一个故障；若出现“公开可变字段或半初始化对象让调用者绕过不变量”，就在 第6章：可以工作的类 的 **实现封装** 保存首个偏离、拒绝结果和复位后的重放证据。

### 6.6 超越类：包

在 **第6章：可以工作的类** 中，目录节点 **6.6 超越类：包** 落在“使用验证”检查点。这里的技术含义是：用构造和公开操作守住对象不变量，优先组合稳定职责；它影响“包组织”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **6.6 超越类：包** 时，尝试绕过接口制造半有效对象，确认不变量不可被外部破坏。先写预期，再运行正常值、恰好边界和一个故障；若出现“公开可变字段或半初始化对象让调用者绕过不变量”，就在 第6章：可以工作的类 的 **使用验证** 保存首个偏离、拒绝结果和复位后的重放证据。

### 更多资源

在 **第6章：可以工作的类** 中，目录节点 **更多资源** 落在“领域概念”检查点。这里的技术含义是：只选择能补足当前能力缺口且可转为实践产物的后续材料；它影响“抽象质量”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **更多资源** 时，为每个来源预先写出要解决的问题和截止复核日期。先写预期，再运行正常值、恰好边界和一个故障；若出现“公开可变字段或半初始化对象让调用者绕过不变量”，就在 第6章：可以工作的类 的 **领域概念** 保存首个偏离、拒绝结果和复位后的重放证据。

### 关键点

在 **第6章：可以工作的类** 中，目录节点 **关键点** 落在“ADT契约”检查点。这里的技术含义是：把本单元结论改写为带前提、反例和证据位置的决策规则；它影响“封装边界”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **关键点** 时，构造规则不适用的最小反例并说明退出条件。先写预期，再运行正常值、恰好边界和一个故障；若出现“公开可变字段或半初始化对象让调用者绕过不变量”，就在 第6章：可以工作的类 的 **ADT契约** 保存首个偏离、拒绝结果和复位后的重放证据。

## 最小可重放实现

```text
// 第6章：可以工作的类：同一输入验证基线、故障和复位
const baseline = run_cc2e_06_working_classes(fixedCase());
assertContract(baseline);
assertRejected(run_cc2e_06_working_classes(faultCase()));
assert.deepEqual(resetAndRun(fixedCase()), baseline);
```

这段 第6章：可以工作的类 验证草图表达独立教学合同，不复制原书代码。真实执行应保存输入、环境、节点轨迹、最终结果、拒绝理由和复位结果，使第二位读者能从干净状态重放。

## 专属因果实验

先预测改变“抽象质量”或“封装边界”后，第6章：可以工作的类 的哪一个节点最先变化。切换基线、边界和注入故障，运行并保存证据；最后点击“重置实验”，确认滑块、场景、节点、压力和结果文字全部恢复。

<Cc2e06WorkingClassesMechanismLab />

## 故障诊断与误区

<Callout type="trap" title="误区：公开可变字段或半初始化对象让调用者绕过不变量">
  这个错误会让 第6章：可以工作的类
  无法从“类只是数据与函数容器”稳定推进到“类在生命周期内守住抽象合同”。诊断沿
  领域概念 → ADT契约 → 类接口 → 实现封装 → 使用验证
  前进，只在第一次出现预期与实际不一致的节点停下，不用最终症状倒猜原因。
</Callout>

<Callout type="trap" title="误区：一个指标可以代替全部证据">
  第6章：可以工作的类
  的单一数值可能被样本、缓存、默认值、测量窗口或偶然次序掩盖。至少同时保存机制轨迹、边界结果、故障拒绝和复位重放，任何一项缺失都不能用平均分补偿。
</Callout>

<Callout type="tip" title="恢复顺序">
  第6章：可以工作的类 的恢复顺序是：还原输入和环境，清理派生状态，重建
  ADT契约，重跑 类接口，核对 使用验证，再注入同一故障确认拒绝可重复。
</Callout>

## 术语与边界

第6章：可以工作的类 的五个操作术语是 <Term def="领域概念 是 第6章：可以工作的类 中可观察、可失败且可复位的机制边界">领域概念</Term>、<Term def="ADT契约 是 第6章：可以工作的类 中可观察、可失败且可复位的机制边界">ADT契约</Term>、<Term def="类接口 是 第6章：可以工作的类 中可观察、可失败且可复位的机制边界">类接口</Term>、<Term def="实现封装 是 第6章：可以工作的类 中可观察、可失败且可复位的机制边界">实现封装</Term>、<Term def="使用验证 是 第6章：可以工作的类 中可观察、可失败且可复位的机制边界">使用验证</Term>。每个术语必须能指向实验控件、节点或状态文本；若只能指向目录标题，说明解释和验证仍未完成。

<Glossary>

<GlossaryItem term="领域概念">
  领域概念 是 第6章：可以工作的类 的第 1
  个机制检查点；它的记录包含进入条件、状态变化、退出条件与故障表现。
</GlossaryItem>

<GlossaryItem term="ADT契约">
  ADT契约 是 第6章：可以工作的类 的第 2
  个机制检查点；它的记录包含进入条件、状态变化、退出条件与故障表现。
</GlossaryItem>

<GlossaryItem term="类接口">
  类接口 是 第6章：可以工作的类 的第 3
  个机制检查点；它的记录包含进入条件、状态变化、退出条件与故障表现。
</GlossaryItem>

<GlossaryItem term="实现封装">
  实现封装 是 第6章：可以工作的类 的第 4
  个机制检查点；它的记录包含进入条件、状态变化、退出条件与故障表现。
</GlossaryItem>

<GlossaryItem term="使用验证">
  使用验证 是 第6章：可以工作的类 的第 5
  个机制检查点；它的记录包含进入条件、状态变化、退出条件与故障表现。
</GlossaryItem>

</Glossary>

## 本页小结

第6章：可以工作的类 的完成标准是能解释“可工作的类用不变量约束状态，以最小接口暴露稳定能力，通过封装、组合或受控继承管理变化”，用核心合同推出预期，沿五个专属节点观察实际，诊断“公开可变字段或半初始化对象让调用者绕过不变量”，并证明复位后同一输入重建基线轨迹。

<Exercises>

1. 第6章：可以工作的类 的五节点链中，哪个状态最能区分“类只是数据与函数容器”和“类在生命周期内守住抽象合同”？请给出最小输入和恰好边界。

<Answer>
  先固定 领域概念，只改变与 ADT契约 相连的一个条件；最早在 类接口
  出现且能由合同预测的差异，就是区分两种状态的证据。边界值要同时记录接受或拒绝以及
  使用验证 的最终状态。
</Answer>

2. 使用 第6章：可以工作的类 的核心合同做一次手算、代码断言、状态推演、故障诊断或方案比较，并说明每个量对应的真实对象。

<Answer>
  把输入值、单位、版本和观察窗口写入基线，先求预期，再与实验轨迹比较。若量纲、分母、对象身份或执行顺序发生变化，应重建模型；不能只修改阈值让本轮通过。
</Answer>

3. 如何逐项证明 第6章：可以工作的类 的目录范围不只是标题出现？覆盖清单：第6章 可以工作的类（领域概念）；6.1 类的基础：抽象数据类型（ADT契约）；需要用到ADT的例子（类接口）；使用ADT的益处（实现封装）；更多的ADT示例（使用验证）；在非面向对象环境中用ADT处理多份数据实例（领域概念）；ADT和类（ADT契约）；6.2 良好的类接口（类接口）；好的抽象（实现封装）；良好的封装（使用验证）；6.3 有关设计和实现的问题（领域概念）；包含（“有一个……”的关系）（ADT契约）；继承（“是一个……”关系）（类接口）；成员函数和数据成员（实现封装）；构造函数（使用验证）；6.4 创建类的原因（领域概念）；应该避免的类（ADT契约）；总结：创建类的理由（类接口）；与具体编程语言相关的问题（实现封装）；6.6 超越类：包（使用验证）；更多资源（领域概念）；关键点（ADT契约）。

<Answer>
  对清单每项找到正文解释，指出它位于五节点链的哪一步，在专属实验中改变一个直接变量，并写下正常、边界、故障与复位结果。四类证据任缺一类，该目录节点仍保持未通过。
</Answer>

</Exercises>

<Attribution
  adaptedFrom="《代码大全（第2版）》2006 中文版公开试读目录用于界定 第6章：可以工作的类 的范围；技术讲解、机制图、实验、实现草图和练习依据 C++ Core Guidelines 独立重写；未使用或声称复现原书正文、插图、表格、核对表和代码"
  adaptedUrl="https://book.douban.com/reading/10145819/"
/>
