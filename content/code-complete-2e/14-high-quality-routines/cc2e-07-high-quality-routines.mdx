---
title: "第7章：高质量的子程序"
type: B
section: "代码大全（第2版·2006中文版）"
order: 14
description: "第7章：高质量的子程序：从“函数只是可复用代码段”推进到“调用边界压缩可靠推理”，以目录节点、专属机制实验和故障复位验收。"
demo: true
math: true
sourceUrl: "https://book.douban.com/reading/10145819/"
qualityVersion: 2
practiceMode: code
sourceMode: independent-rewrite
draft: false
---


## 为什么需要这一机制

第7章：高质量的子程序 解决的不是一个可背诵口号，而是从 **函数只是可复用代码段** 到 **调用边界压缩可靠推理** 的可观察变化。它的核心机制是：高质量子程序承担一个可命名职责，参数表达输入输出合同，控制路径和错误语义足够局部，调用者无需阅读实现即可正确使用。

学习 第7章：高质量的子程序 时先预测 调用意图、职责边界、参数契约、实现路径、调用证据 的输入与输出，再改变一个直接条件。最终结果即使看似正确，只要中间状态违反合同、故障不能隔离或重置不能回到同一基线，本页结论就不通过。


## 核心合同

$$
routine=precondition\rightarrow single\ responsibility\rightarrow postcondition
$$

第7章：高质量的子程序 的合同变量必须绑定到同一版本、同一输入、同一单位和同一观察窗口。先由合同推出预期，再观察 参数契约 与 调用证据；如果只是修改阈值来迎合结果，实验失去裁决能力。

## 目录节点到四级证据

以下节点逐项给出“出现—解释—视觉/实验—练习验证”证据。第7章：高质量的子程序 不把目录词频当作覆盖率；每一项都必须能在专属机制链中指出状态变化，并在章末练习清单中被复核。

### 第7章 高质量的子程序

在 **第7章：高质量的子程序** 中，目录节点 **第7章 高质量的子程序** 落在“调用意图”检查点。这里的技术含义是：把单一职责、前置条件、后置条件和错误语义放在调用边界；它影响“创建理由”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **第7章 高质量的子程序** 时，给最小、最大和非法参数各运行一次，并检查返回与状态副作用。先写预期，再运行正常值、恰好边界和一个故障；若出现“布尔开关、隐式全局状态和混合层次让一个函数承担多个原因变化”，就在 第7章：高质量的子程序 的 **调用意图** 保存首个偏离、拒绝结果和复位后的重放证据。

### 7.1 创建子程序的正当理由

在 **第7章：高质量的子程序** 中，目录节点 **7.1 创建子程序的正当理由** 落在“职责边界”检查点。这里的技术含义是：把单一职责、前置条件、后置条件和错误语义放在调用边界；它影响“层次设计”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **7.1 创建子程序的正当理由** 时，给最小、最大和非法参数各运行一次，并检查返回与状态副作用。先写预期，再运行正常值、恰好边界和一个故障；若出现“布尔开关、隐式全局状态和混合层次让一个函数承担多个原因变化”，就在 第7章：高质量的子程序 的 **职责边界** 保存首个偏离、拒绝结果和复位后的重放证据。

### 似乎过于简单而没必要写成子程序的操作

在 **第7章：高质量的子程序** 中，目录节点 **似乎过于简单而没必要写成子程序的操作** 落在“参数契约”检查点。这里的技术含义是：把单一职责、前置条件、后置条件和错误语义放在调用边界；它影响“命名”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **似乎过于简单而没必要写成子程序的操作** 时，给最小、最大和非法参数各运行一次，并检查返回与状态副作用。先写预期，再运行正常值、恰好边界和一个故障；若出现“布尔开关、隐式全局状态和混合层次让一个函数承担多个原因变化”，就在 第7章：高质量的子程序 的 **参数契约** 保存首个偏离、拒绝结果和复位后的重放证据。

### 总结：创建子程序的理由

在 **第7章：高质量的子程序** 中，目录节点 **总结：创建子程序的理由** 落在“实现路径”检查点。这里的技术含义是：把单一职责、前置条件、后置条件和错误语义放在调用边界；它影响“长度”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **总结：创建子程序的理由** 时，给最小、最大和非法参数各运行一次，并检查返回与状态副作用。先写预期，再运行正常值、恰好边界和一个故障；若出现“布尔开关、隐式全局状态和混合层次让一个函数承担多个原因变化”，就在 第7章：高质量的子程序 的 **实现路径** 保存首个偏离、拒绝结果和复位后的重放证据。

### 7.2 在子程序层上设计

在 **第7章：高质量的子程序** 中，目录节点 **7.2 在子程序层上设计** 落在“调用证据”检查点。这里的技术含义是：把单一职责、前置条件、后置条件和错误语义放在调用边界；它影响“参数返回”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **7.2 在子程序层上设计** 时，给最小、最大和非法参数各运行一次，并检查返回与状态副作用。先写预期，再运行正常值、恰好边界和一个故障；若出现“布尔开关、隐式全局状态和混合层次让一个函数承担多个原因变化”，就在 第7章：高质量的子程序 的 **调用证据** 保存首个偏离、拒绝结果和复位后的重放证据。

### 7.3 好的子程序名字

在 **第7章：高质量的子程序** 中，目录节点 **7.3 好的子程序名字** 落在“调用意图”检查点。这里的技术含义是：让名称表达问题域角色、单位和状态，而不是实现类型或临时历史；它影响“创建理由”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **7.3 好的子程序名字** 时，交换两个同类型但不同单位的值，检查名称和类型能否阻止误用。先写预期，再运行正常值、恰好边界和一个故障；若出现“布尔开关、隐式全局状态和混合层次让一个函数承担多个原因变化”，就在 第7章：高质量的子程序 的 **调用意图** 保存首个偏离、拒绝结果和复位后的重放证据。

### 7.4 子程序可以写多长

在 **第7章：高质量的子程序** 中，目录节点 **7.4 子程序可以写多长** 落在“职责边界”检查点。这里的技术含义是：把单一职责、前置条件、后置条件和错误语义放在调用边界；它影响“层次设计”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **7.4 子程序可以写多长** 时，给最小、最大和非法参数各运行一次，并检查返回与状态副作用。先写预期，再运行正常值、恰好边界和一个故障；若出现“布尔开关、隐式全局状态和混合层次让一个函数承担多个原因变化”，就在 第7章：高质量的子程序 的 **职责边界** 保存首个偏离、拒绝结果和复位后的重放证据。

### 7.5 如何使用子程序参数

在 **第7章：高质量的子程序** 中，目录节点 **7.5 如何使用子程序参数** 落在“参数契约”检查点。这里的技术含义是：把单一职责、前置条件、后置条件和错误语义放在调用边界；它影响“命名”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **7.5 如何使用子程序参数** 时，给最小、最大和非法参数各运行一次，并检查返回与状态副作用。先写预期，再运行正常值、恰好边界和一个故障；若出现“布尔开关、隐式全局状态和混合层次让一个函数承担多个原因变化”，就在 第7章：高质量的子程序 的 **参数契约** 保存首个偏离、拒绝结果和复位后的重放证据。

### 7.6 使用函数时要特别考虑的问题

在 **第7章：高质量的子程序** 中，目录节点 **7.6 使用函数时要特别考虑的问题** 落在“实现路径”检查点。这里的技术含义是：把单一职责、前置条件、后置条件和错误语义放在调用边界；它影响“长度”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **7.6 使用函数时要特别考虑的问题** 时，给最小、最大和非法参数各运行一次，并检查返回与状态副作用。先写预期，再运行正常值、恰好边界和一个故障；若出现“布尔开关、隐式全局状态和混合层次让一个函数承担多个原因变化”，就在 第7章：高质量的子程序 的 **实现路径** 保存首个偏离、拒绝结果和复位后的重放证据。

### 什么时候使用函数，什么时候使用过程

在 **第7章：高质量的子程序** 中，目录节点 **什么时候使用函数，什么时候使用过程** 落在“调用证据”检查点。这里的技术含义是：把单一职责、前置条件、后置条件和错误语义放在调用边界；它影响“参数返回”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **什么时候使用函数，什么时候使用过程** 时，给最小、最大和非法参数各运行一次，并检查返回与状态副作用。先写预期，再运行正常值、恰好边界和一个故障；若出现“布尔开关、隐式全局状态和混合层次让一个函数承担多个原因变化”，就在 第7章：高质量的子程序 的 **调用证据** 保存首个偏离、拒绝结果和复位后的重放证据。

### 设置函数的返回值

在 **第7章：高质量的子程序** 中，目录节点 **设置函数的返回值** 落在“调用意图”检查点。这里的技术含义是：把单一职责、前置条件、后置条件和错误语义放在调用边界；它影响“创建理由”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **设置函数的返回值** 时，给最小、最大和非法参数各运行一次，并检查返回与状态副作用。先写预期，再运行正常值、恰好边界和一个故障；若出现“布尔开关、隐式全局状态和混合层次让一个函数承担多个原因变化”，就在 第7章：高质量的子程序 的 **调用意图** 保存首个偏离、拒绝结果和复位后的重放证据。

### 7.7 宏子程序和内联子程序

在 **第7章：高质量的子程序** 中，目录节点 **7.7 宏子程序和内联子程序** 落在“职责边界”检查点。这里的技术含义是：把单一职责、前置条件、后置条件和错误语义放在调用边界；它影响“层次设计”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **7.7 宏子程序和内联子程序** 时，给最小、最大和非法参数各运行一次，并检查返回与状态副作用。先写预期，再运行正常值、恰好边界和一个故障；若出现“布尔开关、隐式全局状态和混合层次让一个函数承担多个原因变化”，就在 第7章：高质量的子程序 的 **职责边界** 保存首个偏离、拒绝结果和复位后的重放证据。

### 宏子程序在使用上的限制

在 **第7章：高质量的子程序** 中，目录节点 **宏子程序在使用上的限制** 落在“参数契约”检查点。这里的技术含义是：把单一职责、前置条件、后置条件和错误语义放在调用边界；它影响“命名”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **宏子程序在使用上的限制** 时，给最小、最大和非法参数各运行一次，并检查返回与状态副作用。先写预期，再运行正常值、恰好边界和一个故障；若出现“布尔开关、隐式全局状态和混合层次让一个函数承担多个原因变化”，就在 第7章：高质量的子程序 的 **参数契约** 保存首个偏离、拒绝结果和复位后的重放证据。

### 内联子程序

在 **第7章：高质量的子程序** 中，目录节点 **内联子程序** 落在“实现路径”检查点。这里的技术含义是：把单一职责、前置条件、后置条件和错误语义放在调用边界；它影响“长度”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **内联子程序** 时，给最小、最大和非法参数各运行一次，并检查返回与状态副作用。先写预期，再运行正常值、恰好边界和一个故障；若出现“布尔开关、隐式全局状态和混合层次让一个函数承担多个原因变化”，就在 第7章：高质量的子程序 的 **实现路径** 保存首个偏离、拒绝结果和复位后的重放证据。

### 关键点

在 **第7章：高质量的子程序** 中，目录节点 **关键点** 落在“调用证据”检查点。这里的技术含义是：把本单元结论改写为带前提、反例和证据位置的决策规则；它影响“参数返回”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **关键点** 时，构造规则不适用的最小反例并说明退出条件。先写预期，再运行正常值、恰好边界和一个故障；若出现“布尔开关、隐式全局状态和混合层次让一个函数承担多个原因变化”，就在 第7章：高质量的子程序 的 **调用证据** 保存首个偏离、拒绝结果和复位后的重放证据。

## 最小可重放实现

```text
// 第7章：高质量的子程序：同一输入验证基线、故障和复位
const baseline = run_cc2e_07_high_quality_routines(fixedCase());
assertContract(baseline);
assertRejected(run_cc2e_07_high_quality_routines(faultCase()));
assert.deepEqual(resetAndRun(fixedCase()), baseline);
```

这段 第7章：高质量的子程序 验证草图表达独立教学合同，不复制原书代码。真实执行应保存输入、环境、节点轨迹、最终结果、拒绝理由和复位结果，使第二位读者能从干净状态重放。

## 专属因果实验

先预测改变“创建理由”或“层次设计”后，第7章：高质量的子程序 的哪一个节点最先变化。切换基线、边界和注入故障，运行并保存证据；最后点击“重置实验”，确认滑块、场景、节点、压力和结果文字全部恢复。


## 故障诊断与误区


## 术语与边界

第7章：高质量的子程序 的五个操作术语是、、、、。每个术语必须能指向实验控件、节点或状态文本；若只能指向目录标题，说明解释和验证仍未完成。


## 本页小结

第7章：高质量的子程序 的完成标准是能解释“高质量子程序承担一个可命名职责，参数表达输入输出合同，控制路径和错误语义足够局部，调用者无需阅读实现即可正确使用”，用核心合同推出预期，沿五个专属节点观察实际，诊断“布尔开关、隐式全局状态和混合层次让一个函数承担多个原因变化”，并证明复位后同一输入重建基线轨迹。


