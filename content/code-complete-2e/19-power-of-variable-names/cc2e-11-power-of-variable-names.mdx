---
title: "第11章：变量名的力量"
type: B
section: "代码大全（第2版·2006中文版）"
order: 19
description: "让变量名面向问题域并适配作用域，针对索引、状态、临时值、布尔、枚举与常量建立一致规则，同时拒绝误导性缩写和前缀。"
demo: true
math: true
sourceUrl: "https://book.douban.com/reading/10145819/"
draft: false
---

import {
  Cc2e11PowerOfVariableNamesStructureLab,
  Cc2e11PowerOfVariableNamesTestLab,
  Cc2e11PowerOfVariableNamesEvidenceLab,
} from "@/components/mdx/code-complete-2e/diagrams/cc2e-11-power-of-variable-names";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第11章：变量名的力量在49个正式单元和685个目录节点中的职责与前后依赖
- 能绘制“领域概念 → 语义命名 → 规则一致 → 作用域阅读 → 维护判断”因果链，并实现最小代码或项目验证
- 能计算复杂度、缺陷检出、变更风险与反馈时间，并比较正常、边界、单故障
- 能设计独立复核并证明“能对一段含糊命名代码建立重命名计划，证明新名称表达领域、类型与状态而不重复实现细节。”

</Objectives>

## 为什么这一单元不可压缩

本页依据史蒂夫·迈克康奈尔《代码大全（第2版）》，金戈、汤凌、陈硕、张菲译，裘宗燕审校，电子工业出版社，2006年3月，ISBN 9787121022982的公开完整试读目录，独立重构 **第11章 变量名的力量**。让变量名面向问题域并适配作用域，针对索引、状态、临时值、布尔、枚举与常量建立一致规则，同时拒绝误导性缩写和前缀。 本课程不复制原书正文、插图、表格、核对表或代码示例，而把目录主题转写成新的模型、代码实验、边界样本与复核证据。

中文版目录包含5个前置单元、7个部分、35章、参考文献和索引，共49个正式单元、685个目录节点。中文书目页登记944页；北京化工大学图书馆MARC登记“37,914页”，属于前置页与正文分页口径差异。课程以作者、译者、出版社、出版日期、ISBN和完整目录共同锁定版本，不因页数差异删减内容。

原版由Microsoft Press于2004年出版，共960页，ISBN 9780735619678。语言、工具和平台示例有历史边界，但管理复杂度、明确契约、开发者测试、增量集成和面向人阅读仍需在当前项目重新验证；现代框架名称不能倒灌成原书目录内容。

## 版次、对象与验收合同

<Term def="变量名是第11章：变量名的力量的第1个复核坐标，必须绑定代码对象、版本、输入、边界和可观察证据。">
  变量名
</Term>
、
<Term def="问题域是第11章：变量名的力量的第2个复核坐标，必须绑定代码对象、版本、输入、边界和可观察证据。">
  问题域
</Term>
、
<Term def="命名规则是第11章：变量名的力量的第3个复核坐标，必须绑定代码对象、版本、输入、边界和可观察证据。">
  命名规则
</Term>
、
<Term def="语义前缀是第11章：变量名的力量的第4个复核坐标，必须绑定代码对象、版本、输入、边界和可观察证据。">
  语义前缀
</Term>
、
<Term def="缩写是第11章：变量名的力量的第5个复核坐标，必须绑定代码对象、版本、输入、边界和可观察证据。">
  缩写
</Term>
共同构成本页验收坐标。每次实验固定需求、语言和编译器版本、输入数据、初始代码、测试与测量环境，只改变一个结构、类型、控制或过程条件。

<Callout type="info">
  **验收命题：**
  能对一段含糊命名代码建立重命名计划，证明新名称表达领域、类型与状态而不重复实现细节。
</Callout>

## 四个可核查模型

控制流复杂度用独立路径近似：

$$
V(G)=E-N+2P
$$

缺陷移除效能必须固定发现阶段和分母：

$$
DRE=removedBeforeRelease/(removedBeforeRelease+escaped)
$$

变更风险同时取决于传播范围、状态和测试缺口：

$$
risk=fanout * mutableState * uncoveredPaths
$$

发布采用乘法门禁，任一项缺失不能被平均分掩盖：

$$
release=contract * readableDesign * tests * integration * independentReview
$$

复杂度不是质量结论，覆盖率也不是正确性证明。公式输入必须来自同一版本和场景，并保存单位、采样窗口、阈值来源及无法测量的假设。

## 三段最小可运行骨架

```typescript
type ConstructionSnapshot = {
  edition: "2006-CN-2E";
  unit: "cc2e-11-power-of-variable-names";
  sourceHash: string;
  paths: number;
  tests: string[];
  evidence: string[];
};

export function releaseGate(snapshot: ConstructionSnapshot) {
  return (
    snapshot.sourceHash.length > 0 &&
    snapshot.tests.length >= 3 &&
    snapshot.evidence.length >= 3
  );
}
```

```python
def boundary_cases(low, high):
    if low > high:
        raise ValueError("range")
    return [low - 1, low, low + 1, high - 1, high, high + 1]
```

```python
def apply_one_change(baseline, change, validate):
    if len(change) != 1:
        return {"accepted": False, "state": baseline, "reason": "not-single-change"}
    candidate = dict(baseline, **change)
    return ({"accepted": True, "state": candidate, "reason": None}
            if validate(candidate) else
            {"accepted": False, "state": baseline, "reason": "gate-rejected"})
```

第一段冻结源代码与证据，第二段系统产生边界邻域，第三段保证拒绝时不留下半更新状态。三段都要补充正常、恰好边界和单故障预期，失败时先比较首差而不是最终输出。

## 正式目录逐项讲解

### 第11章 变量名的力量

变量应尽晚定义、可靠初始化、局部使用并尽早结束生命周期。追踪定义到最后使用的跨度，消除隐式状态和一变量多用途。

复核动作：把“第11章 变量名的力量”放入第11章 变量名的力量的“领域概念 → 语义命名 → 规则一致 → 作用域阅读 → 维护判断”链路，只改变一个结构、数据或工具条件，保存预期、实际、首差与回退，并让第二位读者重放。

### 11.1 选择好变量名的注意事项

变量应尽晚定义、可靠初始化、局部使用并尽早结束生命周期。追踪定义到最后使用的跨度，消除隐式状态和一变量多用途。

复核动作：把“11.1 选择好变量名的注意事项”放入第11章 变量名的力量的“领域概念 → 语义命名 → 规则一致 → 作用域阅读 → 维护判断”链路，只改变一个结构、数据或工具条件，保存预期、实际、首差与回退，并让第二位读者重放。

### 最重要的命名注意事项

名称首先表达问题域含义，再补充必要的类型或状态信息。用调用场景检验可读性，拒绝依赖记忆的缩写、误导范围和重复实现细节的前缀。

复核动作：把“最重要的命名注意事项”放入第11章 变量名的力量的“领域概念 → 语义命名 → 规则一致 → 作用域阅读 → 维护判断”链路，只改变一个结构、数据或工具条件，保存预期、实际、首差与回退，并让第二位读者重放。

### 以问题为导向

以问题为导向承担第11章 变量名的力量中的一个正式目录坐标。先说明它解决的构建问题、输入、输出、适用范围和与相邻主题的依赖，再把建议改写为可以失败的判断。

最小实验固定2006年中文第2版语义边界和同一代码基线，只改变一个条件，记录复杂度、测试、缺陷或反馈时间的首个变化；未覆盖的语言与项目差异必须显式保留。

### 最适当的名字长度

名称首先表达问题域含义，再补充必要的类型或状态信息。用调用场景检验可读性，拒绝依赖记忆的缩写、误导范围和重复实现细节的前缀。

复核动作：把“最适当的名字长度”放入第11章 变量名的力量的“领域概念 → 语义命名 → 规则一致 → 作用域阅读 → 维护判断”链路，只改变一个结构、数据或工具条件，保存预期、实际、首差与回退，并让第二位读者重放。

### 变量名字的效果范围

变量应尽晚定义、可靠初始化、局部使用并尽早结束生命周期。追踪定义到最后使用的跨度，消除隐式状态和一变量多用途。

复核动作：把“变量名字的效果范围”放入第11章 变量名的力量的“领域概念 → 语义命名 → 规则一致 → 作用域阅读 → 维护判断”链路，只改变一个结构、数据或工具条件，保存预期、实际、首差与回退，并让第二位读者重放。

### 变量名字中的计算值限定词

变量应尽晚定义、可靠初始化、局部使用并尽早结束生命周期。追踪定义到最后使用的跨度，消除隐式状态和一变量多用途。

复核动作：把“变量名字中的计算值限定词”放入第11章 变量名的力量的“领域概念 → 语义命名 → 规则一致 → 作用域阅读 → 维护判断”链路，只改变一个结构、数据或工具条件，保存预期、实际、首差与回退，并让第二位读者重放。

### 变量名字中的常用反义词

变量应尽晚定义、可靠初始化、局部使用并尽早结束生命周期。追踪定义到最后使用的跨度，消除隐式状态和一变量多用途。

复核动作：把“变量名字中的常用反义词”放入第11章 变量名的力量的“领域概念 → 语义命名 → 规则一致 → 作用域阅读 → 维护判断”链路，只改变一个结构、数据或工具条件，保存预期、实际、首差与回退，并让第二位读者重放。

### 11.2 为特定类型的数据命名

代码单元应表达一个一致职责和稳定契约。检查调用者需要知道什么、哪些状态必须隐藏、参数和返回怎样约束，以及替换实现时哪些测试保持不变。

复核动作：把“11.2 为特定类型的数据命名”放入第11章 变量名的力量的“领域概念 → 语义命名 → 规则一致 → 作用域阅读 → 维护判断”链路，只改变一个结构、数据或工具条件，保存预期、实际、首差与回退，并让第二位读者重放。

### 为循环索引命名

名称首先表达问题域含义，再补充必要的类型或状态信息。用调用场景检验可读性，拒绝依赖记忆的缩写、误导范围和重复实现细节的前缀。

复核动作：把“为循环索引命名”放入第11章 变量名的力量的“领域概念 → 语义命名 → 规则一致 → 作用域阅读 → 维护判断”链路，只改变一个结构、数据或工具条件，保存预期、实际、首差与回退，并让第二位读者重放。

### 为状态变量命名

变量应尽晚定义、可靠初始化、局部使用并尽早结束生命周期。追踪定义到最后使用的跨度，消除隐式状态和一变量多用途。

复核动作：把“为状态变量命名”放入第11章 变量名的力量的“领域概念 → 语义命名 → 规则一致 → 作用域阅读 → 维护判断”链路，只改变一个结构、数据或工具条件，保存预期、实际、首差与回退，并让第二位读者重放。

### 为临时变量命名

变量应尽晚定义、可靠初始化、局部使用并尽早结束生命周期。追踪定义到最后使用的跨度，消除隐式状态和一变量多用途。

复核动作：把“为临时变量命名”放入第11章 变量名的力量的“领域概念 → 语义命名 → 规则一致 → 作用域阅读 → 维护判断”链路，只改变一个结构、数据或工具条件，保存预期、实际、首差与回退，并让第二位读者重放。

### 为布尔变量命名

变量应尽晚定义、可靠初始化、局部使用并尽早结束生命周期。追踪定义到最后使用的跨度，消除隐式状态和一变量多用途。

复核动作：把“为布尔变量命名”放入第11章 变量名的力量的“领域概念 → 语义命名 → 规则一致 → 作用域阅读 → 维护判断”链路，只改变一个结构、数据或工具条件，保存预期、实际、首差与回退，并让第二位读者重放。

### 为枚举类型命名

代码单元应表达一个一致职责和稳定契约。检查调用者需要知道什么、哪些状态必须隐藏、参数和返回怎样约束，以及替换实现时哪些测试保持不变。

复核动作：把“为枚举类型命名”放入第11章 变量名的力量的“领域概念 → 语义命名 → 规则一致 → 作用域阅读 → 维护判断”链路，只改变一个结构、数据或工具条件，保存预期、实际、首差与回退，并让第二位读者重放。

### 为常量命名

名称首先表达问题域含义，再补充必要的类型或状态信息。用调用场景检验可读性，拒绝依赖记忆的缩写、误导范围和重复实现细节的前缀。

复核动作：把“为常量命名”放入第11章 变量名的力量的“领域概念 → 语义命名 → 规则一致 → 作用域阅读 → 维护判断”链路，只改变一个结构、数据或工具条件，保存预期、实际、首差与回退，并让第二位读者重放。

### 11.3 命名规则的力量

名称首先表达问题域含义，再补充必要的类型或状态信息。用调用场景检验可读性，拒绝依赖记忆的缩写、误导范围和重复实现细节的前缀。

复核动作：把“11.3 命名规则的力量”放入第11章 变量名的力量的“领域概念 → 语义命名 → 规则一致 → 作用域阅读 → 维护判断”链路，只改变一个结构、数据或工具条件，保存预期、实际、首差与回退，并让第二位读者重放。

### 为什么要有规则？

为什么要有规则？承担第11章 变量名的力量中的一个正式目录坐标。先说明它解决的构建问题、输入、输出、适用范围和与相邻主题的依赖，再把建议改写为可以失败的判断。

最小实验固定2006年中文第2版语义边界和同一代码基线，只改变一个条件，记录复杂度、测试、缺陷或反馈时间的首个变化；未覆盖的语言与项目差异必须显式保留。

### 何时采用命名规则

名称首先表达问题域含义，再补充必要的类型或状态信息。用调用场景检验可读性，拒绝依赖记忆的缩写、误导范围和重复实现细节的前缀。

复核动作：把“何时采用命名规则”放入第11章 变量名的力量的“领域概念 → 语义命名 → 规则一致 → 作用域阅读 → 维护判断”链路，只改变一个结构、数据或工具条件，保存预期、实际、首差与回退，并让第二位读者重放。

### 正式程度

正式程度承担第11章 变量名的力量中的一个正式目录坐标。先说明它解决的构建问题、输入、输出、适用范围和与相邻主题的依赖，再把建议改写为可以失败的判断。

最小实验固定2006年中文第2版语义边界和同一代码基线，只改变一个条件，记录复杂度、测试、缺陷或反馈时间的首个变化；未覆盖的语言与项目差异必须显式保留。

### 11.4 非正式命名规则

名称首先表达问题域含义，再补充必要的类型或状态信息。用调用场景检验可读性，拒绝依赖记忆的缩写、误导范围和重复实现细节的前缀。

复核动作：把“11.4 非正式命名规则”放入第11章 变量名的力量的“领域概念 → 语义命名 → 规则一致 → 作用域阅读 → 维护判断”链路，只改变一个结构、数据或工具条件，保存预期、实际、首差与回退，并让第二位读者重放。

### 语言无关规则的指导原则

语言无关规则的指导原则承担第11章 变量名的力量中的一个正式目录坐标。先说明它解决的构建问题、输入、输出、适用范围和与相邻主题的依赖，再把建议改写为可以失败的判断。

最小实验固定2006年中文第2版语义边界和同一代码基线，只改变一个条件，记录复杂度、测试、缺陷或反馈时间的首个变化；未覆盖的语言与项目差异必须显式保留。

### 语言相关规则的指导原则

语言相关规则的指导原则承担第11章 变量名的力量中的一个正式目录坐标。先说明它解决的构建问题、输入、输出、适用范围和与相邻主题的依赖，再把建议改写为可以失败的判断。

最小实验固定2006年中文第2版语义边界和同一代码基线，只改变一个条件，记录复杂度、测试、缺陷或反馈时间的首个变化；未覆盖的语言与项目差异必须显式保留。

### 混合语言编程的注意事项

混合语言编程的注意事项承担第11章 变量名的力量中的一个正式目录坐标。先说明它解决的构建问题、输入、输出、适用范围和与相邻主题的依赖，再把建议改写为可以失败的判断。

最小实验固定2006年中文第2版语义边界和同一代码基线，只改变一个条件，记录复杂度、测试、缺陷或反馈时间的首个变化；未覆盖的语言与项目差异必须显式保留。

### 命名规则示例

名称首先表达问题域含义，再补充必要的类型或状态信息。用调用场景检验可读性，拒绝依赖记忆的缩写、误导范围和重复实现细节的前缀。

复核动作：把“命名规则示例”放入第11章 变量名的力量的“领域概念 → 语义命名 → 规则一致 → 作用域阅读 → 维护判断”链路，只改变一个结构、数据或工具条件，保存预期、实际、首差与回退，并让第二位读者重放。

### 11.5 标准前缀

名称首先表达问题域含义，再补充必要的类型或状态信息。用调用场景检验可读性，拒绝依赖记忆的缩写、误导范围和重复实现细节的前缀。

复核动作：把“11.5 标准前缀”放入第11章 变量名的力量的“领域概念 → 语义命名 → 规则一致 → 作用域阅读 → 维护判断”链路，只改变一个结构、数据或工具条件，保存预期、实际、首差与回退，并让第二位读者重放。

### 用户自定义类型缩写

代码单元应表达一个一致职责和稳定契约。检查调用者需要知道什么、哪些状态必须隐藏、参数和返回怎样约束，以及替换实现时哪些测试保持不变。

复核动作：把“用户自定义类型缩写”放入第11章 变量名的力量的“领域概念 → 语义命名 → 规则一致 → 作用域阅读 → 维护判断”链路，只改变一个结构、数据或工具条件，保存预期、实际、首差与回退，并让第二位读者重放。

### 语义前缀

名称首先表达问题域含义，再补充必要的类型或状态信息。用调用场景检验可读性，拒绝依赖记忆的缩写、误导范围和重复实现细节的前缀。

复核动作：把“语义前缀”放入第11章 变量名的力量的“领域概念 → 语义命名 → 规则一致 → 作用域阅读 → 维护判断”链路，只改变一个结构、数据或工具条件，保存预期、实际、首差与回退，并让第二位读者重放。

### 标准前缀的优点

名称首先表达问题域含义，再补充必要的类型或状态信息。用调用场景检验可读性，拒绝依赖记忆的缩写、误导范围和重复实现细节的前缀。

复核动作：把“标准前缀的优点”放入第11章 变量名的力量的“领域概念 → 语义命名 → 规则一致 → 作用域阅读 → 维护判断”链路，只改变一个结构、数据或工具条件，保存预期、实际、首差与回退，并让第二位读者重放。

### 11.6 创建具备可读性的短名称

11.6 创建具备可读性的短名称承担第11章 变量名的力量中的一个正式目录坐标。先说明它解决的构建问题、输入、输出、适用范围和与相邻主题的依赖，再把建议改写为可以失败的判断。

最小实验固定2006年中文第2版语义边界和同一代码基线，只改变一个条件，记录复杂度、测试、缺陷或反馈时间的首个变化；未覆盖的语言与项目差异必须显式保留。

### 一般的缩写指导原则

名称首先表达问题域含义，再补充必要的类型或状态信息。用调用场景检验可读性，拒绝依赖记忆的缩写、误导范围和重复实现细节的前缀。

复核动作：把“一般的缩写指导原则”放入第11章 变量名的力量的“领域概念 → 语义命名 → 规则一致 → 作用域阅读 → 维护判断”链路，只改变一个结构、数据或工具条件，保存预期、实际、首差与回退，并让第二位读者重放。

### 语音缩写

名称首先表达问题域含义，再补充必要的类型或状态信息。用调用场景检验可读性，拒绝依赖记忆的缩写、误导范围和重复实现细节的前缀。

复核动作：把“语音缩写”放入第11章 变量名的力量的“领域概念 → 语义命名 → 规则一致 → 作用域阅读 → 维护判断”链路，只改变一个结构、数据或工具条件，保存预期、实际、首差与回退，并让第二位读者重放。

### 有关缩写的评论

名称首先表达问题域含义，再补充必要的类型或状态信息。用调用场景检验可读性，拒绝依赖记忆的缩写、误导范围和重复实现细节的前缀。

复核动作：把“有关缩写的评论”放入第11章 变量名的力量的“领域概念 → 语义命名 → 规则一致 → 作用域阅读 → 维护判断”链路，只改变一个结构、数据或工具条件，保存预期、实际、首差与回退，并让第二位读者重放。

### 11.7 应该避免的名称

11.7 应该避免的名称承担第11章 变量名的力量中的一个正式目录坐标。先说明它解决的构建问题、输入、输出、适用范围和与相邻主题的依赖，再把建议改写为可以失败的判断。

最小实验固定2006年中文第2版语义边界和同一代码基线，只改变一个条件，记录复杂度、测试、缺陷或反馈时间的首个变化；未覆盖的语言与项目差异必须显式保留。

### 关键点

关键点用于把本单元收束为少量可执行判断。复核时把每条判断改写成带对象、前置条件、失败样本和证据位置的门禁，不能只背结论。

复核动作：把“关键点”放入第11章 变量名的力量的“领域概念 → 语义命名 → 规则一致 → 作用域阅读 → 维护判断”链路，只改变一个结构、数据或工具条件，保存预期、实际、首差与回退，并让第二位读者重放。

## 从目录建议到构建因果链

先画五节点链：领域概念 → 语义命名 → 规则一致 → 作用域阅读 → 维护判断。每条边注明传递的是需求、抽象、类型、状态、控制、构件、缺陷还是证据；每个节点写出进入条件、退出条件、所有者和失败回退。没有对象或状态变化的箭头只是排版连接。

再以问题导向、长度、特定类型、命名规则、避免名称作为检查点。正常样本证明主路径可运行；边界样本选择恰好类型范围、控制端点、复杂度阈值、测试覆盖或构建容量；单故障样本只改变一个输入、分支、依赖、工具或构件。三类样本共享其余条件，才能归因。

书中示例语言与当代项目不同时，先保留原建议解决的问题，再比较类型系统、内存模型、工具自动化和发布环境。后续语言特性可以降低次要负担，但不能替代领域建模、接口选择和失败验证。

## 最小证据包与独立复核

证据包包含ISBN与目录坐标、需求与代码哈希、语言和工具版本、输入数据、复杂度、静态分析、测试设计、失败日志、性能基线、集成轨迹、回退状态和结论。覆盖率、格式化结果或最终截图只能辅助定位，不能单独证明质量。

独立复核者只接收冻结输入、代码基线和验收条件，不接收原作者的操作顺序或期待答案。复核者先预测五个节点，再运行同一数据；结果不一致时比较首个分叉、原始日志和环境差异。

## 跨语言与时代迁移审查

把2006年的构建建议迁移到当前语言时，先保存原建议解决的风险，再分别记录今天由类型系统、标准库、编译器、静态分析、测试框架或持续集成自动承担的部分。自动化已经覆盖的机械步骤可以缩减，但它的配置、盲区和失败模式必须进入证据包；仍涉及领域抽象、接口语义、数据边界和取舍的部分，不能交给工具默认值静默决定。

迁移记录至少比较四栏：原版问题、当前机制、仍存风险、验证样本。若一种现代特性声称让某条建议“过时”，就构造关闭该特性、误配该特性和越过其检查边界的样本；只有能说明风险确实被消除而非转移，才可调整学习权重。这样既不机械照搬旧语言技巧，也不因工具更新而丢失软件构建的因果问题。

## 构建验证卡

每次应用目录建议都填写同一张验证卡：问题必须可失败；基线必须包含版本、输入和测量；改动一次只改变一个变量；证据要能定位首差；复核由未参与实现的人完成。缺少任一栏，结论只能保留为待验证假设。

| 字段 | 必答问题                     | 拒绝条件                   |
| ---- | ---------------------------- | -------------------------- |
| 问题 | 哪个构建风险需要降低         | 只有风格偏好，没有失败条件 |
| 基线 | 代码、输入、工具和指标是什么 | 分母、单位或版本不一致     |
| 改动 | 唯一改变了哪个结构或过程     | 同时重写、调优和换工具     |
| 证据 | 首差、测试、性能与回退在哪里 | 只有最终成功截图           |
| 复核 | 谁在相同输入下重建结果       | 依赖实现者操作提示         |

## 正常、边界与单故障矩阵

| 样本   | 只改变的变量                   | 预期结果                     | 必存证据                   |
| ------ | ------------------------------ | ---------------------------- | -------------------------- |
| 正常   | 合法输入、清晰契约和稳定工具链 | 主路径闭合且指标有余量       | 代码哈希、输入、测试与轨迹 |
| 边界   | 恰好范围、端点、复杂度或容量   | 明确接受或拒绝，不产生半状态 | 阈值、分支、原状态与断言   |
| 单故障 | 一个输入、依赖、分支或构件失败 | 在首个异常边界隔离并回退     | 首差、错误策略、恢复与回归 |

操作前先预测。若一次同时重构、调优、换语言和升级工具，就无法判断改进来源，必须重置基线。

## 三步交互实验

<Stepper>
  <Step title="追踪结构、数据与控制边界">
    选择节点，检查对象、状态、决定和证据怎样跨边界传播。
    <Cc2e11PowerOfVariableNamesStructureLab />
  </Step>
  <Step title="调整复杂度、测试与变更范围">
    一次只改变一个参数，观察认知负担、缺陷暴露和变更风险。
    <Cc2e11PowerOfVariableNamesTestLab />
  </Step>
  <Step title="注入一个故障并保存证据">
    比较正常、恰好边界与单故障，要求门禁拒绝无证据结论。
    <Cc2e11PowerOfVariableNamesEvidenceLab />
  </Step>
</Stepper>

## 常见失败与回退

<Callout type="trap">
  **把规则变成风格口号：**
  没有问题、边界和反例的“最佳实践”无法裁决。回到代码基线，说明风险、唯一改动与可观察结果。
</Callout>

<Callout type="trap">
  **用现代工具覆盖原问题：**
  自动格式化、类型检查和生成式工具能减少部分机械工作，却不自动保证抽象、契约和测试。记录工具解决了什么，以及仍需人判断什么。
</Callout>

## 本章回顾

让变量名面向问题域并适配作用域，针对索引、状态、临时值、布尔、枚举与常量建立一致规则，同时拒绝误导性缩写和前缀。 完成“能对一段含糊命名代码建立重命名计划，证明新名称表达领域、类型与状态而不重复实现细节。”后，还要让独立复核者在相同边界重建结构、测试、首差和回退。一次通过不能代表所有输入，能解释何时失败和怎样恢复才是可迁移的构建知识。

<Glossary>

<GlossaryItem term="变量名">
  变量名用于定位第11章：变量名的力量中的第1类结构、数据、质量或过程变量；使用时必须写出适用边界、失败条件和复核方法。
</GlossaryItem>

<GlossaryItem term="问题域">
  问题域用于定位第11章：变量名的力量中的第2类结构、数据、质量或过程变量；使用时必须写出适用边界、失败条件和复核方法。
</GlossaryItem>

<GlossaryItem term="命名规则">
  命名规则用于定位第11章：变量名的力量中的第3类结构、数据、质量或过程变量；使用时必须写出适用边界、失败条件和复核方法。
</GlossaryItem>

<GlossaryItem term="语义前缀">
  语义前缀用于定位第11章：变量名的力量中的第4类结构、数据、质量或过程变量；使用时必须写出适用边界、失败条件和复核方法。
</GlossaryItem>

<GlossaryItem term="缩写">
  缩写用于定位第11章：变量名的力量中的第5类结构、数据、质量或过程变量；使用时必须写出适用边界、失败条件和复核方法。
</GlossaryItem>

</Glossary>

<Exercises>

1. 如何为第11章：变量名的力量建立最小可重放基线？

<Answer>
  固定2006年中文第2版目录坐标、需求、代码哈希、语言与工具、输入和测试，先画“领域概念
  → 语义命名 → 规则一致 → 作用域阅读 →
  维护判断”预期，再保存正常、边界与单故障轨迹。
</Answer>

2. 怎样证明“能对一段含糊命名代码建立重命名计划，证明新名称表达领域、类型与状态而不重复实现细节。”不是一次偶然成功？

<Answer>
  先手算复杂度、边界、缺陷检出或反馈预算，再让独立复核者只依据输入和验收合同重建；双方必须在相同首差拒绝故障，并保留相同回退状态。
</Answer>

3. 修复一个失败后怎样回归？

<Answer>
  先重放原失败，再运行相邻边界和正常基线；逐节点检查契约、类型、控制、测试、工具与恢复证据，全部通过后才进入下一页。
</Answer>

</Exercises>

## 前后导航

- [上一页：第10章：使用变量的一般事项](/learn/code-complete-2e/18-general-variable-use/cc2e-10-general-variable-use)
- [下一页：第12章：基本数据类型](/learn/code-complete-2e/20-fundamental-data-types/cc2e-12-fundamental-data-types)

<Attribution
  adaptedFrom="史蒂夫·迈克康奈尔《代码大全（第2版）》，金戈、汤凌、陈硕、张菲译，裘宗燕审校，电子工业出版社，2006年3月，ISBN 9787121022982 · 公开试读目录含5个前置单元、7个部分、35章、参考文献与索引共49个正式单元和685个目录节点 · 基于公开目录独立中文教学重构，不复制原书正文、插图、表格、核对表或代码示例"
  adaptedUrl="https://book.douban.com/reading/10145819/"
/>
