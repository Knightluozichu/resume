---
title: "第12章：基本数据类型"
type: B
section: "代码大全（第2版·2006中文版）"
order: 20
description: "第12章：基本数据类型：从“类型只影响存储大小”推进到“表示范围和业务合同一致”，以目录节点、专属机制实验和故障复位验收。"
demo: true
math: true
sourceUrl: "https://book.douban.com/reading/10145819/"
qualityVersion: 2
practiceMode: calculation
sourceMode: independent-rewrite
draft: false
---


## 为什么需要这一机制

第12章：基本数据类型 解决的不是一个可背诵口号，而是从 **类型只影响存储大小** 到 **表示范围和业务合同一致** 的可观察变化。它的核心机制是：基本类型选择必须覆盖业务范围、精度、编码和运算规则，边界样本验证溢出、舍入、枚举和常量语义。

学习 第12章：基本数据类型 时先预测 业务量、类型选择、范围约束、运算规则、边界测试 的输入与输出，再改变一个直接条件。最终结果即使看似正确，只要中间状态违反合同、故障不能隔离或重置不能回到同一基线，本页结论就不通过。


## 核心合同

$$
valid\ type\iff domain\ range\subseteq representation\ range
$$

第12章：基本数据类型 的合同变量必须绑定到同一版本、同一输入、同一单位和同一观察窗口。先由合同推出预期，再观察 范围约束 与 边界测试；如果只是修改阈值来迎合结果，实验失去裁决能力。

## 目录节点到四级证据

以下节点逐项给出“出现—解释—视觉/实验—练习验证”证据。第12章：基本数据类型 不把目录词频当作覆盖率；每一项都必须能在专属机制链中指出状态变化，并在章末练习清单中被复核。

### 第12章 基本数据类型

在 **第12章：基本数据类型** 中，目录节点 **第12章 基本数据类型** 落在“业务量”检查点。这里的技术含义是：用构造和公开操作守住对象不变量，优先组合稳定职责；它影响“数值规则”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **第12章 基本数据类型** 时，尝试绕过接口制造半有效对象，确认不变量不可被外部破坏。先写预期，再运行正常值、恰好边界和一个故障；若出现“用浮点表示精确金额，或在定宽整数乘法后才检查溢出”，就在 第12章：基本数据类型 的 **业务量** 保存首个偏离、拒绝结果和复位后的重放证据。

### 12.1 使用数的普遍规则

在 **第12章：基本数据类型** 中，目录节点 **12.1 使用数的普遍规则** 落在“类型选择”检查点。这里的技术含义是：把“12.1 使用数的普遍规则”放回 基本类型选择必须覆盖业务范围、精度、编码和运算规则，边界样本验证溢出、舍入、枚举和常量语义；它影响“精度”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **12.1 使用数的普遍规则** 时，固定其余条件，只改变与“12.1 使用数的普遍规则”直接相连的一个输入或决定。先写预期，再运行正常值、恰好边界和一个故障；若出现“用浮点表示精确金额，或在定宽整数乘法后才检查溢出”，就在 第12章：基本数据类型 的 **类型选择** 保存首个偏离、拒绝结果和复位后的重放证据。

### 12.2 整数

在 **第12章：基本数据类型** 中，目录节点 **12.2 整数** 落在“范围约束”检查点。这里的技术含义是：依据业务范围、精度、编码和运算规则选择表示；它影响“文本编码”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **12.2 整数** 时，测试最小值、最大值、舍入点、溢出邻域和非法编码。先写预期，再运行正常值、恰好边界和一个故障；若出现“用浮点表示精确金额，或在定宽整数乘法后才检查溢出”，就在 第12章：基本数据类型 的 **范围约束** 保存首个偏离、拒绝结果和复位后的重放证据。

### 12.3 浮点数

在 **第12章：基本数据类型** 中，目录节点 **12.3 浮点数** 落在“运算规则”检查点。这里的技术含义是：依据业务范围、精度、编码和运算规则选择表示；它影响“枚举常量”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **12.3 浮点数** 时，测试最小值、最大值、舍入点、溢出邻域和非法编码。先写预期，再运行正常值、恰好边界和一个故障；若出现“用浮点表示精确金额，或在定宽整数乘法后才检查溢出”，就在 第12章：基本数据类型 的 **运算规则** 保存首个偏离、拒绝结果和复位后的重放证据。

### 12.4 字符和字符串

在 **第12章：基本数据类型** 中，目录节点 **12.4 字符和字符串** 落在“边界测试”检查点。这里的技术含义是：依据业务范围、精度、编码和运算规则选择表示；它影响“自定义类型”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **12.4 字符和字符串** 时，测试最小值、最大值、舍入点、溢出邻域和非法编码。先写预期，再运行正常值、恰好边界和一个故障；若出现“用浮点表示精确金额，或在定宽整数乘法后才检查溢出”，就在 第12章：基本数据类型 的 **边界测试** 保存首个偏离、拒绝结果和复位后的重放证据。

### C中的字符串

在 **第12章：基本数据类型** 中，目录节点 **C中的字符串** 落在“业务量”检查点。这里的技术含义是：依据业务范围、精度、编码和运算规则选择表示；它影响“数值规则”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **C中的字符串** 时，测试最小值、最大值、舍入点、溢出邻域和非法编码。先写预期，再运行正常值、恰好边界和一个故障；若出现“用浮点表示精确金额，或在定宽整数乘法后才检查溢出”，就在 第12章：基本数据类型 的 **业务量** 保存首个偏离、拒绝结果和复位后的重放证据。

### 12.5 布尔变量

在 **第12章：基本数据类型** 中，目录节点 **12.5 布尔变量** 落在“类型选择”检查点。这里的技术含义是：使分支互斥且覆盖输入域，命名复杂谓词并明确默认路径；它影响“精度”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **12.5 布尔变量** 时，列出条件真值表和边界邻域，寻找重叠或没有归属的输入。先写预期，再运行正常值、恰好边界和一个故障；若出现“用浮点表示精确金额，或在定宽整数乘法后才检查溢出”，就在 第12章：基本数据类型 的 **类型选择** 保存首个偏离、拒绝结果和复位后的重放证据。

### 12.6 枚举类型

在 **第12章：基本数据类型** 中，目录节点 **12.6 枚举类型** 落在“范围约束”检查点。这里的技术含义是：用构造和公开操作守住对象不变量，优先组合稳定职责；它影响“文本编码”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **12.6 枚举类型** 时，尝试绕过接口制造半有效对象，确认不变量不可被外部破坏。先写预期，再运行正常值、恰好边界和一个故障；若出现“用浮点表示精确金额，或在定宽整数乘法后才检查溢出”，就在 第12章：基本数据类型 的 **范围约束** 保存首个偏离、拒绝结果和复位后的重放证据。

### 如果你的语言里没有枚举类型

在 **第12章：基本数据类型** 中，目录节点 **如果你的语言里没有枚举类型** 落在“运算规则”检查点。这里的技术含义是：用构造和公开操作守住对象不变量，优先组合稳定职责；它影响“枚举常量”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **如果你的语言里没有枚举类型** 时，尝试绕过接口制造半有效对象，确认不变量不可被外部破坏。先写预期，再运行正常值、恰好边界和一个故障；若出现“用浮点表示精确金额，或在定宽整数乘法后才检查溢出”，就在 第12章：基本数据类型 的 **运算规则** 保存首个偏离、拒绝结果和复位后的重放证据。

### 12.7 命名常量

在 **第12章：基本数据类型** 中，目录节点 **12.7 命名常量** 落在“边界测试”检查点。这里的技术含义是：让名称表达问题域角色、单位和状态，而不是实现类型或临时历史；它影响“自定义类型”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **12.7 命名常量** 时，交换两个同类型但不同单位的值，检查名称和类型能否阻止误用。先写预期，再运行正常值、恰好边界和一个故障；若出现“用浮点表示精确金额，或在定宽整数乘法后才检查溢出”，就在 第12章：基本数据类型 的 **边界测试** 保存首个偏离、拒绝结果和复位后的重放证据。

### 12.8 数组

在 **第12章：基本数据类型** 中，目录节点 **12.8 数组** 落在“业务量”检查点。这里的技术含义是：声明所有权、别名、边界、可变性和释放责任；它影响“数值规则”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **12.8 数组** 时，制造空引用、悬空引用、越界索引与并发写入各一个反例。先写预期，再运行正常值、恰好边界和一个故障；若出现“用浮点表示精确金额，或在定宽整数乘法后才检查溢出”，就在 第12章：基本数据类型 的 **业务量** 保存首个偏离、拒绝结果和复位后的重放证据。

### 12.9 创建你自己的类型（类型别名）

在 **第12章：基本数据类型** 中，目录节点 **12.9 创建你自己的类型（类型别名）** 落在“类型选择”检查点。这里的技术含义是：用构造和公开操作守住对象不变量，优先组合稳定职责；它影响“精度”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **12.9 创建你自己的类型（类型别名）** 时，尝试绕过接口制造半有效对象，确认不变量不可被外部破坏。先写预期，再运行正常值、恰好边界和一个故障；若出现“用浮点表示精确金额，或在定宽整数乘法后才检查溢出”，就在 第12章：基本数据类型 的 **类型选择** 保存首个偏离、拒绝结果和复位后的重放证据。

### 为什么创建自己的类型的示例是用Pascal和Ada写的？

在 **第12章：基本数据类型** 中，目录节点 **为什么创建自己的类型的示例是用Pascal和Ada写的？** 落在“范围约束”检查点。这里的技术含义是：用构造和公开操作守住对象不变量，优先组合稳定职责；它影响“文本编码”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **为什么创建自己的类型的示例是用Pascal和Ada写的？** 时，尝试绕过接口制造半有效对象，确认不变量不可被外部破坏。先写预期，再运行正常值、恰好边界和一个故障；若出现“用浮点表示精确金额，或在定宽整数乘法后才检查溢出”，就在 第12章：基本数据类型 的 **范围约束** 保存首个偏离、拒绝结果和复位后的重放证据。

### 创建自定义数据类型的指导原则

在 **第12章：基本数据类型** 中，目录节点 **创建自定义数据类型的指导原则** 落在“运算规则”检查点。这里的技术含义是：用构造和公开操作守住对象不变量，优先组合稳定职责；它影响“枚举常量”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **创建自定义数据类型的指导原则** 时，尝试绕过接口制造半有效对象，确认不变量不可被外部破坏。先写预期，再运行正常值、恰好边界和一个故障；若出现“用浮点表示精确金额，或在定宽整数乘法后才检查溢出”，就在 第12章：基本数据类型 的 **运算规则** 保存首个偏离、拒绝结果和复位后的重放证据。

### 关键点

在 **第12章：基本数据类型** 中，目录节点 **关键点** 落在“边界测试”检查点。这里的技术含义是：把本单元结论改写为带前提、反例和证据位置的决策规则；它影响“自定义类型”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **关键点** 时，构造规则不适用的最小反例并说明退出条件。先写预期，再运行正常值、恰好边界和一个故障；若出现“用浮点表示精确金额，或在定宽整数乘法后才检查溢出”，就在 第12章：基本数据类型 的 **边界测试** 保存首个偏离、拒绝结果和复位后的重放证据。

## 最小可重放实现

```text
# 第12章：基本数据类型：先建立参考模型，再核对边界
expected = reference_cc2e_12_fundamental_data_types(fixed_input)
actual = run_cc2e_12_fundamental_data_types(fixed_input)
assert actual == expected
assert reset_and_run(fixed_input) == expected
```

这段 第12章：基本数据类型 验证草图表达独立教学合同，不复制原书代码。真实执行应保存输入、环境、节点轨迹、最终结果、拒绝理由和复位结果，使第二位读者能从干净状态重放。

## 专属因果实验

先预测改变“数值规则”或“精度”后，第12章：基本数据类型 的哪一个节点最先变化。切换基线、边界和注入故障，运行并保存证据；最后点击“重置实验”，确认滑块、场景、节点、压力和结果文字全部恢复。


## 故障诊断与误区


## 术语与边界

第12章：基本数据类型 的五个操作术语是、、、、。每个术语必须能指向实验控件、节点或状态文本；若只能指向目录标题，说明解释和验证仍未完成。


## 本页小结

第12章：基本数据类型 的完成标准是能解释“基本类型选择必须覆盖业务范围、精度、编码和运算规则，边界样本验证溢出、舍入、枚举和常量语义”，用核心合同推出预期，沿五个专属节点观察实际，诊断“用浮点表示精确金额，或在定宽整数乘法后才检查溢出”，并证明复位后同一输入重建基线轨迹。


