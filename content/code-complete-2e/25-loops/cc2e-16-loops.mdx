---
title: "第16章：控制循环"
type: B
section: "代码大全（第2版·2006中文版）"
order: 25
description: "第16章：控制循环：从“重复执行某段代码”推进到“边界与终止都能推导”，以目录节点、专属机制实验和故障复位验收。"
demo: true
math: true
sourceUrl: "https://book.douban.com/reading/10145819/"
qualityVersion: 2
practiceMode: calculation
sourceMode: independent-rewrite
draft: true
---

import { Cc2e16LoopsMechanismLab } from "@/components/mdx/code-complete-2e/diagrams/cc2e-16-loops";
import {
  Objectives,
  Callout,
  Term,
  Glossary,
  GlossaryItem,
  Exercises,
  Answer,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能说明 第16章：控制循环 怎样把“重复执行某段代码”推进为“边界与终止都能推导”
- 能沿 循环前状态 → 进入条件 → 单次不变量 → 退出条件 → 循环后状态 重建对象、状态、决定和证据
- 能使用本页合同完成一次计算
- 能注入“索引与集合长度错一位，或循环体不保证向退出条件推进”，找到首差并确认重置恢复基线

</Objectives>

## 为什么需要这一机制

第16章：控制循环 解决的不是一个可背诵口号，而是从 **重复执行某段代码** 到 **边界与终止都能推导** 的可观察变化。它的核心机制是：循环用初始化、继续条件、不变量、进展量和退出后置条件建立证明，并对零次、一次、末端和越界邻域测试。

学习 第16章：控制循环 时先预测 循环前状态、进入条件、单次不变量、退出条件、循环后状态 的输入与输出，再改变一个直接条件。最终结果即使看似正确，只要中间状态违反合同、故障不能隔离或重置不能回到同一基线，本页结论就不通过。

<Callout type="info" title="范围与事实来源">
  第16章：控制循环 由 [2006 中文第 2
  版公开试读目录](https://book.douban.com/reading/10145819/)
  限定范围，技术机制依据 [C++ Core
  Guidelines](https://isocpp.github.io/CppCoreGuidelines/CppCoreGuidelines)
  独立重写。目录核对只证明主题坐标；本页不宣称取得、翻译或复现原书正文、插图、表格、核对表与代码。
</Callout>

## 核心合同

$$
invariant\land guard\land progress\Rightarrow postcondition
$$

第16章：控制循环 的合同变量必须绑定到同一版本、同一输入、同一单位和同一观察窗口。先由合同推出预期，再观察 单次不变量 与 循环后状态；如果只是修改阈值来迎合结果，实验失去裁决能力。

## 目录节点到四级证据

以下节点逐项给出“出现—解释—视觉/实验—练习验证”证据。第16章：控制循环 不把目录词频当作覆盖率；每一项都必须能在专属机制链中指出状态变化，并在章末练习清单中被复核。

### 第16章 控制循环

在 **第16章：控制循环** 中，目录节点 **第16章 控制循环** 落在“循环前状态”检查点。这里的技术含义是：写出循环前状态、不变量、进展量和退出后置条件；它影响“循环种类”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **第16章 控制循环** 时，运行零次、一次、末端一次和越界一步四种样本。先写预期，再运行正常值、恰好边界和一个故障；若出现“索引与集合长度错一位，或循环体不保证向退出条件推进”，就在 第16章：控制循环 的 **循环前状态** 保存首个偏离、拒绝结果和复位后的重放证据。

### 16.1 选择循环的种类

在 **第16章：控制循环** 中，目录节点 **16.1 选择循环的种类** 落在“进入条件”检查点。这里的技术含义是：用构造和公开操作守住对象不变量，优先组合稳定职责；它影响“控制”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **16.1 选择循环的种类** 时，尝试绕过接口制造半有效对象，确认不变量不可被外部破坏。先写预期，再运行正常值、恰好边界和一个故障；若出现“索引与集合长度错一位，或循环体不保证向退出条件推进”，就在 第16章：控制循环 的 **进入条件** 保存首个偏离、拒绝结果和复位后的重放证据。

### 什么时候使用while循环

在 **第16章：控制循环** 中，目录节点 **什么时候使用while循环** 落在“单次不变量”检查点。这里的技术含义是：写出循环前状态、不变量、进展量和退出后置条件；它影响“端点”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **什么时候使用while循环** 时，运行零次、一次、末端一次和越界一步四种样本。先写预期，再运行正常值、恰好边界和一个故障；若出现“索引与集合长度错一位，或循环体不保证向退出条件推进”，就在 第16章：控制循环 的 **单次不变量** 保存首个偏离、拒绝结果和复位后的重放证据。

### 什么时候用带退出的循环

在 **第16章：控制循环** 中，目录节点 **什么时候用带退出的循环** 落在“退出条件”检查点。这里的技术含义是：写出循环前状态、不变量、进展量和退出后置条件；它影响“由内而外”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **什么时候用带退出的循环** 时，运行零次、一次、末端一次和越界一步四种样本。先写预期，再运行正常值、恰好边界和一个故障；若出现“索引与集合长度错一位，或循环体不保证向退出条件推进”，就在 第16章：控制循环 的 **退出条件** 保存首个偏离、拒绝结果和复位后的重放证据。

### 何时使用for循环

在 **第16章：控制循环** 中，目录节点 **何时使用for循环** 落在“循环后状态”检查点。这里的技术含义是：写出循环前状态、不变量、进展量和退出后置条件；它影响“数组关系”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **何时使用for循环** 时，运行零次、一次、末端一次和越界一步四种样本。先写预期，再运行正常值、恰好边界和一个故障；若出现“索引与集合长度错一位，或循环体不保证向退出条件推进”，就在 第16章：控制循环 的 **循环后状态** 保存首个偏离、拒绝结果和复位后的重放证据。

### 何时使用foreach循环

在 **第16章：控制循环** 中，目录节点 **何时使用foreach循环** 落在“循环前状态”检查点。这里的技术含义是：写出循环前状态、不变量、进展量和退出后置条件；它影响“循环种类”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **何时使用foreach循环** 时，运行零次、一次、末端一次和越界一步四种样本。先写预期，再运行正常值、恰好边界和一个故障；若出现“索引与集合长度错一位，或循环体不保证向退出条件推进”，就在 第16章：控制循环 的 **循环前状态** 保存首个偏离、拒绝结果和复位后的重放证据。

### 16.2 循环控制

在 **第16章：控制循环** 中，目录节点 **16.2 循环控制** 落在“进入条件”检查点。这里的技术含义是：写出循环前状态、不变量、进展量和退出后置条件；它影响“控制”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **16.2 循环控制** 时，运行零次、一次、末端一次和越界一步四种样本。先写预期，再运行正常值、恰好边界和一个故障；若出现“索引与集合长度错一位，或循环体不保证向退出条件推进”，就在 第16章：控制循环 的 **进入条件** 保存首个偏离、拒绝结果和复位后的重放证据。

### 进入循环

在 **第16章：控制循环** 中，目录节点 **进入循环** 落在“单次不变量”检查点。这里的技术含义是：写出循环前状态、不变量、进展量和退出后置条件；它影响“端点”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **进入循环** 时，运行零次、一次、末端一次和越界一步四种样本。先写预期，再运行正常值、恰好边界和一个故障；若出现“索引与集合长度错一位，或循环体不保证向退出条件推进”，就在 第16章：控制循环 的 **单次不变量** 保存首个偏离、拒绝结果和复位后的重放证据。

### 处理好循环体

在 **第16章：控制循环** 中，目录节点 **处理好循环体** 落在“退出条件”检查点。这里的技术含义是：写出循环前状态、不变量、进展量和退出后置条件；它影响“由内而外”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **处理好循环体** 时，运行零次、一次、末端一次和越界一步四种样本。先写预期，再运行正常值、恰好边界和一个故障；若出现“索引与集合长度错一位，或循环体不保证向退出条件推进”，就在 第16章：控制循环 的 **退出条件** 保存首个偏离、拒绝结果和复位后的重放证据。

### 退出循环

在 **第16章：控制循环** 中，目录节点 **退出循环** 落在“循环后状态”检查点。这里的技术含义是：写出循环前状态、不变量、进展量和退出后置条件；它影响“数组关系”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **退出循环** 时，运行零次、一次、末端一次和越界一步四种样本。先写预期，再运行正常值、恰好边界和一个故障；若出现“索引与集合长度错一位，或循环体不保证向退出条件推进”，就在 第16章：控制循环 的 **循环后状态** 保存首个偏离、拒绝结果和复位后的重放证据。

### 检查端点

在 **第16章：控制循环** 中，目录节点 **检查端点** 落在“循环前状态”检查点。这里的技术含义是：明确角色、准备材料、缺陷分类和修订责任，讨论产物而非个人；它影响“循环种类”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **检查端点** 时，让未参与实现者仅凭合同复查并比较独立发现。先写预期，再运行正常值、恰好边界和一个故障；若出现“索引与集合长度错一位，或循环体不保证向退出条件推进”，就在 第16章：控制循环 的 **循环前状态** 保存首个偏离、拒绝结果和复位后的重放证据。

### 使用循环变量

在 **第16章：控制循环** 中，目录节点 **使用循环变量** 落在“进入条件”检查点。这里的技术含义是：写出循环前状态、不变量、进展量和退出后置条件；它影响“控制”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **使用循环变量** 时，运行零次、一次、末端一次和越界一步四种样本。先写预期，再运行正常值、恰好边界和一个故障；若出现“索引与集合长度错一位，或循环体不保证向退出条件推进”，就在 第16章：控制循环 的 **进入条件** 保存首个偏离、拒绝结果和复位后的重放证据。

### 循环应该有多长

在 **第16章：控制循环** 中，目录节点 **循环应该有多长** 落在“单次不变量”检查点。这里的技术含义是：写出循环前状态、不变量、进展量和退出后置条件；它影响“端点”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **循环应该有多长** 时，运行零次、一次、末端一次和越界一步四种样本。先写预期，再运行正常值、恰好边界和一个故障；若出现“索引与集合长度错一位，或循环体不保证向退出条件推进”，就在 第16章：控制循环 的 **单次不变量** 保存首个偏离、拒绝结果和复位后的重放证据。

### 16.3 轻松创建循环——由内而外

在 **第16章：控制循环** 中，目录节点 **16.3 轻松创建循环——由内而外** 落在“退出条件”检查点。这里的技术含义是：写出循环前状态、不变量、进展量和退出后置条件；它影响“由内而外”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **16.3 轻松创建循环——由内而外** 时，运行零次、一次、末端一次和越界一步四种样本。先写预期，再运行正常值、恰好边界和一个故障；若出现“索引与集合长度错一位，或循环体不保证向退出条件推进”，就在 第16章：控制循环 的 **退出条件** 保存首个偏离、拒绝结果和复位后的重放证据。

### 16.4 循环和数组的关系

在 **第16章：控制循环** 中，目录节点 **16.4 循环和数组的关系** 落在“循环后状态”检查点。这里的技术含义是：写出循环前状态、不变量、进展量和退出后置条件；它影响“数组关系”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **16.4 循环和数组的关系** 时，运行零次、一次、末端一次和越界一步四种样本。先写预期，再运行正常值、恰好边界和一个故障；若出现“索引与集合长度错一位，或循环体不保证向退出条件推进”，就在 第16章：控制循环 的 **循环后状态** 保存首个偏离、拒绝结果和复位后的重放证据。

### 关键点

在 **第16章：控制循环** 中，目录节点 **关键点** 落在“循环前状态”检查点。这里的技术含义是：把本单元结论改写为带前提、反例和证据位置的决策规则；它影响“循环种类”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **关键点** 时，构造规则不适用的最小反例并说明退出条件。先写预期，再运行正常值、恰好边界和一个故障；若出现“索引与集合长度错一位，或循环体不保证向退出条件推进”，就在 第16章：控制循环 的 **循环前状态** 保存首个偏离、拒绝结果和复位后的重放证据。

## 最小可重放实现

```text
# 第16章：控制循环：先建立参考模型，再核对边界
expected = reference_cc2e_16_loops(fixed_input)
actual = run_cc2e_16_loops(fixed_input)
assert actual == expected
assert reset_and_run(fixed_input) == expected
```

这段 第16章：控制循环 验证草图表达独立教学合同，不复制原书代码。真实执行应保存输入、环境、节点轨迹、最终结果、拒绝理由和复位结果，使第二位读者能从干净状态重放。

## 专属因果实验

先预测改变“循环种类”或“控制”后，第16章：控制循环 的哪一个节点最先变化。切换基线、边界和注入故障，运行并保存证据；最后点击“重置实验”，确认滑块、场景、节点、压力和结果文字全部恢复。

<Cc2e16LoopsMechanismLab />

## 故障诊断与误区

<Callout
  type="trap"
  title="误区：索引与集合长度错一位，或循环体不保证向退出条件推进"
>
  这个错误会让 第16章：控制循环
  无法从“重复执行某段代码”稳定推进到“边界与终止都能推导”。诊断沿 循环前状态 →
  进入条件 → 单次不变量 → 退出条件 → 循环后状态
  前进，只在第一次出现预期与实际不一致的节点停下，不用最终症状倒猜原因。
</Callout>

<Callout type="trap" title="误区：一个指标可以代替全部证据">
  第16章：控制循环
  的单一数值可能被样本、缓存、默认值、测量窗口或偶然次序掩盖。至少同时保存机制轨迹、边界结果、故障拒绝和复位重放，任何一项缺失都不能用平均分补偿。
</Callout>

<Callout type="tip" title="恢复顺序">
  第16章：控制循环 的恢复顺序是：还原输入和环境，清理派生状态，重建
  进入条件，重跑 单次不变量，核对 循环后状态，再注入同一故障确认拒绝可重复。
</Callout>

## 术语与边界

第16章：控制循环 的五个操作术语是 <Term def="循环前状态 是 第16章：控制循环 中可观察、可失败且可复位的机制边界">循环前状态</Term>、<Term def="进入条件 是 第16章：控制循环 中可观察、可失败且可复位的机制边界">进入条件</Term>、<Term def="单次不变量 是 第16章：控制循环 中可观察、可失败且可复位的机制边界">单次不变量</Term>、<Term def="退出条件 是 第16章：控制循环 中可观察、可失败且可复位的机制边界">退出条件</Term>、<Term def="循环后状态 是 第16章：控制循环 中可观察、可失败且可复位的机制边界">循环后状态</Term>。每个术语必须能指向实验控件、节点或状态文本；若只能指向目录标题，说明解释和验证仍未完成。

<Glossary>

<GlossaryItem term="循环前状态">
  循环前状态 是 第16章：控制循环 的第 1
  个机制检查点；它的记录包含进入条件、状态变化、退出条件与故障表现。
</GlossaryItem>

<GlossaryItem term="进入条件">
  进入条件 是 第16章：控制循环 的第 2
  个机制检查点；它的记录包含进入条件、状态变化、退出条件与故障表现。
</GlossaryItem>

<GlossaryItem term="单次不变量">
  单次不变量 是 第16章：控制循环 的第 3
  个机制检查点；它的记录包含进入条件、状态变化、退出条件与故障表现。
</GlossaryItem>

<GlossaryItem term="退出条件">
  退出条件 是 第16章：控制循环 的第 4
  个机制检查点；它的记录包含进入条件、状态变化、退出条件与故障表现。
</GlossaryItem>

<GlossaryItem term="循环后状态">
  循环后状态 是 第16章：控制循环 的第 5
  个机制检查点；它的记录包含进入条件、状态变化、退出条件与故障表现。
</GlossaryItem>

</Glossary>

## 本页小结

第16章：控制循环 的完成标准是能解释“循环用初始化、继续条件、不变量、进展量和退出后置条件建立证明，并对零次、一次、末端和越界邻域测试”，用核心合同推出预期，沿五个专属节点观察实际，诊断“索引与集合长度错一位，或循环体不保证向退出条件推进”，并证明复位后同一输入重建基线轨迹。

<Exercises>

1. 第16章：控制循环 的五节点链中，哪个状态最能区分“重复执行某段代码”和“边界与终止都能推导”？请给出最小输入和恰好边界。

<Answer>
  先固定 循环前状态，只改变与 进入条件 相连的一个条件；最早在 单次不变量
  出现且能由合同预测的差异，就是区分两种状态的证据。边界值要同时记录接受或拒绝以及
  循环后状态 的最终状态。
</Answer>

2. 使用 第16章：控制循环 的核心合同做一次手算、代码断言、状态推演、故障诊断或方案比较，并说明每个量对应的真实对象。

<Answer>
  把输入值、单位、版本和观察窗口写入基线，先求预期，再与实验轨迹比较。若量纲、分母、对象身份或执行顺序发生变化，应重建模型；不能只修改阈值让本轮通过。
</Answer>

3. 如何逐项证明 第16章：控制循环 的目录范围不只是标题出现？覆盖清单：第16章 控制循环（循环前状态）；16.1 选择循环的种类（进入条件）；什么时候使用while循环（单次不变量）；什么时候用带退出的循环（退出条件）；何时使用for循环（循环后状态）；何时使用foreach循环（循环前状态）；16.2 循环控制（进入条件）；进入循环（单次不变量）；处理好循环体（退出条件）；退出循环（循环后状态）；检查端点（循环前状态）；使用循环变量（进入条件）；循环应该有多长（单次不变量）；16.3 轻松创建循环——由内而外（退出条件）；16.4 循环和数组的关系（循环后状态）；关键点（循环前状态）。

<Answer>
  对清单每项找到正文解释，指出它位于五节点链的哪一步，在专属实验中改变一个直接变量，并写下正常、边界、故障与复位结果。四类证据任缺一类，该目录节点仍保持未通过。
</Answer>

</Exercises>

<Attribution
  adaptedFrom="《代码大全（第2版）》2006 中文版公开试读目录用于界定 第16章：控制循环 的范围；技术讲解、机制图、实验、实现草图和练习依据 C++ Core Guidelines 独立重写；未使用或声称复现原书正文、插图、表格、核对表和代码"
  adaptedUrl="https://book.douban.com/reading/10145819/"
/>
