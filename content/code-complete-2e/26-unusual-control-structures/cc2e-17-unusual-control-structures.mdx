---
title: "第17章：不常见的控制结构"
type: B
section: "代码大全（第2版·2006中文版）"
order: 26
description: "第17章：不常见的控制结构：从“特殊结构天然危险或天然简洁”推进到“以路径证据判断结构取舍”，以目录节点、专属机制实验和故障复位验收。"
demo: true
math: true
sourceUrl: "https://book.douban.com/reading/10145819/"
qualityVersion: 2
practiceMode: diagnosis
sourceMode: independent-rewrite
draft: false
---

import { Cc2e17UnusualControlStructuresMechanismLab } from "@/components/mdx/code-complete-2e/diagrams/cc2e-17-unusual-control-structures";
import {
  Objectives,
  Callout,
  Term,
  Glossary,
  GlossaryItem,
  Exercises,
  Answer,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能说明 第17章：不常见的控制结构 怎样把“特殊结构天然危险或天然简洁”推进为“以路径证据判断结构取舍”
- 能沿 控制目标 → 候选结构 → 路径展开 → 退出清理 → 复杂度复核 重建对象、状态、决定和证据
- 能使用本页合同完成一次故障诊断
- 能注入“递归未严格缩小问题，或提前返回绕过清理和不变量恢复”，找到首差并确认重置恢复基线

</Objectives>

## 为什么需要这一机制

第17章：不常见的控制结构 解决的不是一个可背诵口号，而是从 **特殊结构天然危险或天然简洁** 到 **以路径证据判断结构取舍** 的可观察变化。它的核心机制是：多返回、递归和跳转只有在缩短错误路径或直接表达问题结构时才使用，并显式证明资源清理、终止与可读性收益。

学习 第17章：不常见的控制结构 时先预测 控制目标、候选结构、路径展开、退出清理、复杂度复核 的输入与输出，再改变一个直接条件。最终结果即使看似正确，只要中间状态违反合同、故障不能隔离或重置不能回到同一基线，本页结论就不通过。

<Callout type="info" title="范围与事实来源">
  第17章：不常见的控制结构 由 [2006 中文第 2
  版公开试读目录](https://book.douban.com/reading/10145819/)
  限定范围，技术机制依据 [C++ Core
  Guidelines](https://isocpp.github.io/CppCoreGuidelines/CppCoreGuidelines)
  独立重写。目录核对只证明主题坐标；本页不宣称取得、翻译或复现原书正文、插图、表格、核对表与代码。
</Callout>

## 核心合同

$$
T(n)=T(smaller(n))+combine(n)
$$

第17章：不常见的控制结构 的合同变量必须绑定到同一版本、同一输入、同一单位和同一观察窗口。先由合同推出预期，再观察 路径展开 与 复杂度复核；如果只是修改阈值来迎合结果，实验失去裁决能力。

## 目录节点到四级证据

以下节点逐项给出“出现—解释—视觉/实验—练习验证”证据。第17章：不常见的控制结构 不把目录词频当作覆盖率；每一项都必须能在专属机制链中指出状态变化，并在章末练习清单中被复核。

### 第17章 不常见的控制结构

在 **第17章：不常见的控制结构** 中，目录节点 **第17章 不常见的控制结构** 落在“控制目标”检查点。这里的技术含义是：比较路径缩短与清理责任，不按语法标签预判结构优劣；它影响“多个返回”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **第17章 不常见的控制结构** 时，展开所有退出边，检查资源释放和后置条件是否一致。先写预期，再运行正常值、恰好边界和一个故障；若出现“递归未严格缩小问题，或提前返回绕过清理和不变量恢复”，就在 第17章：不常见的控制结构 的 **控制目标** 保存首个偏离、拒绝结果和复位后的重放证据。

### 17.1 子程序中的多个返回

在 **第17章：不常见的控制结构** 中，目录节点 **17.1 子程序中的多个返回** 落在“候选结构”检查点。这里的技术含义是：把单一职责、前置条件、后置条件和错误语义放在调用边界；它影响“递归”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **17.1 子程序中的多个返回** 时，给最小、最大和非法参数各运行一次，并检查返回与状态副作用。先写预期，再运行正常值、恰好边界和一个故障；若出现“递归未严格缩小问题，或提前返回绕过清理和不变量恢复”，就在 第17章：不常见的控制结构 的 **候选结构** 保存首个偏离、拒绝结果和复位后的重放证据。

### 17.2 递归

在 **第17章：不常见的控制结构** 中，目录节点 **17.2 递归** 落在“路径展开”检查点。这里的技术含义是：每次调用严格缩小问题并保证可达基线，返回阶段只合并子结果；它影响“goto争论”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **17.2 递归** 时，构造无法缩小的输入，确认终止保护先于栈耗尽生效。先写预期，再运行正常值、恰好边界和一个故障；若出现“递归未严格缩小问题，或提前返回绕过清理和不变量恢复”，就在 第17章：不常见的控制结构 的 **路径展开** 保存首个偏离、拒绝结果和复位后的重放证据。

### 递归的例子

在 **第17章：不常见的控制结构** 中，目录节点 **递归的例子** 落在“退出清理”检查点。这里的技术含义是：每次调用严格缩小问题并保证可达基线，返回阶段只合并子结果；它影响“错误处理”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **递归的例子** 时，构造无法缩小的输入，确认终止保护先于栈耗尽生效。先写预期，再运行正常值、恰好边界和一个故障；若出现“递归未严格缩小问题，或提前返回绕过清理和不变量恢复”，就在 第17章：不常见的控制结构 的 **退出清理** 保存首个偏离、拒绝结果和复位后的重放证据。

### 使用递归的技巧

在 **第17章：不常见的控制结构** 中，目录节点 **使用递归的技巧** 落在“复杂度复核”检查点。这里的技术含义是：每次调用严格缩小问题并保证可达基线，返回阶段只合并子结果；它影响“使用原则”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **使用递归的技巧** 时，构造无法缩小的输入，确认终止保护先于栈耗尽生效。先写预期，再运行正常值、恰好边界和一个故障；若出现“递归未严格缩小问题，或提前返回绕过清理和不变量恢复”，就在 第17章：不常见的控制结构 的 **复杂度复核** 保存首个偏离、拒绝结果和复位后的重放证据。

### 17.3 goto

在 **第17章：不常见的控制结构** 中，目录节点 **17.3 goto** 落在“控制目标”检查点。这里的技术含义是：比较路径缩短与清理责任，不按语法标签预判结构优劣；它影响“多个返回”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **17.3 goto** 时，展开所有退出边，检查资源释放和后置条件是否一致。先写预期，再运行正常值、恰好边界和一个故障；若出现“递归未严格缩小问题，或提前返回绕过清理和不变量恢复”，就在 第17章：不常见的控制结构 的 **控制目标** 保存首个偏离、拒绝结果和复位后的重放证据。

### 反对goto的论点

在 **第17章：不常见的控制结构** 中，目录节点 **反对goto的论点** 落在“候选结构”检查点。这里的技术含义是：比较路径缩短与清理责任，不按语法标签预判结构优劣；它影响“递归”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **反对goto的论点** 时，展开所有退出边，检查资源释放和后置条件是否一致。先写预期，再运行正常值、恰好边界和一个故障；若出现“递归未严格缩小问题，或提前返回绕过清理和不变量恢复”，就在 第17章：不常见的控制结构 的 **候选结构** 保存首个偏离、拒绝结果和复位后的重放证据。

### 支持goto的观点

在 **第17章：不常见的控制结构** 中，目录节点 **支持goto的观点** 落在“路径展开”检查点。这里的技术含义是：比较路径缩短与清理责任，不按语法标签预判结构优劣；它影响“goto争论”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **支持goto的观点** 时，展开所有退出边，检查资源释放和后置条件是否一致。先写预期，再运行正常值、恰好边界和一个故障；若出现“递归未严格缩小问题，或提前返回绕过清理和不变量恢复”，就在 第17章：不常见的控制结构 的 **路径展开** 保存首个偏离、拒绝结果和复位后的重放证据。

### 关于goto的虚假辩论

在 **第17章：不常见的控制结构** 中，目录节点 **关于goto的虚假辩论** 落在“退出清理”检查点。这里的技术含义是：比较路径缩短与清理责任，不按语法标签预判结构优劣；它影响“错误处理”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **关于goto的虚假辩论** 时，展开所有退出边，检查资源释放和后置条件是否一致。先写预期，再运行正常值、恰好边界和一个故障；若出现“递归未严格缩小问题，或提前返回绕过清理和不变量恢复”，就在 第17章：不常见的控制结构 的 **退出清理** 保存首个偏离、拒绝结果和复位后的重放证据。

### 错误处理和goto

在 **第17章：不常见的控制结构** 中，目录节点 **错误处理和goto** 落在“复杂度复核”检查点。这里的技术含义是：区分外部可恢复错误与内部不可能状态，在最近信任边界拒绝；它影响“使用原则”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **错误处理和goto** 时，注入一个坏输入和一个内部不变量破坏，确认两者走不同处置路径。先写预期，再运行正常值、恰好边界和一个故障；若出现“递归未严格缩小问题，或提前返回绕过清理和不变量恢复”，就在 第17章：不常见的控制结构 的 **复杂度复核** 保存首个偏离、拒绝结果和复位后的重放证据。

### goto和在else子句中的共享代码

在 **第17章：不常见的控制结构** 中，目录节点 **goto和在else子句中的共享代码** 落在“控制目标”检查点。这里的技术含义是：比较路径缩短与清理责任，不按语法标签预判结构优劣；它影响“多个返回”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **goto和在else子句中的共享代码** 时，展开所有退出边，检查资源释放和后置条件是否一致。先写预期，再运行正常值、恰好边界和一个故障；若出现“递归未严格缩小问题，或提前返回绕过清理和不变量恢复”，就在 第17章：不常见的控制结构 的 **控制目标** 保存首个偏离、拒绝结果和复位后的重放证据。

### goto使用原则总结

在 **第17章：不常见的控制结构** 中，目录节点 **goto使用原则总结** 落在“候选结构”检查点。这里的技术含义是：比较路径缩短与清理责任，不按语法标签预判结构优劣；它影响“递归”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **goto使用原则总结** 时，展开所有退出边，检查资源释放和后置条件是否一致。先写预期，再运行正常值、恰好边界和一个故障；若出现“递归未严格缩小问题，或提前返回绕过清理和不变量恢复”，就在 第17章：不常见的控制结构 的 **候选结构** 保存首个偏离、拒绝结果和复位后的重放证据。

### 17.4 对不常见控制结构的看法

在 **第17章：不常见的控制结构** 中，目录节点 **17.4 对不常见控制结构的看法** 落在“路径展开”检查点。这里的技术含义是：比较路径缩短与清理责任，不按语法标签预判结构优劣；它影响“goto争论”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **17.4 对不常见控制结构的看法** 时，展开所有退出边，检查资源释放和后置条件是否一致。先写预期，再运行正常值、恰好边界和一个故障；若出现“递归未严格缩小问题，或提前返回绕过清理和不变量恢复”，就在 第17章：不常见的控制结构 的 **路径展开** 保存首个偏离、拒绝结果和复位后的重放证据。

### 其他资源

在 **第17章：不常见的控制结构** 中，目录节点 **其他资源** 落在“退出清理”检查点。这里的技术含义是：把“其他资源”放回 多返回、递归和跳转只有在缩短错误路径或直接表达问题结构时才使用，并显式证明资源清理、终止与可读性收益；它影响“错误处理”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **其他资源** 时，固定其余条件，只改变与“其他资源”直接相连的一个输入或决定。先写预期，再运行正常值、恰好边界和一个故障；若出现“递归未严格缩小问题，或提前返回绕过清理和不变量恢复”，就在 第17章：不常见的控制结构 的 **退出清理** 保存首个偏离、拒绝结果和复位后的重放证据。

### 关键点

在 **第17章：不常见的控制结构** 中，目录节点 **关键点** 落在“复杂度复核”检查点。这里的技术含义是：把本单元结论改写为带前提、反例和证据位置的决策规则；它影响“使用原则”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **关键点** 时，构造规则不适用的最小反例并说明退出条件。先写预期，再运行正常值、恰好边界和一个故障；若出现“递归未严格缩小问题，或提前返回绕过清理和不变量恢复”，就在 第17章：不常见的控制结构 的 **复杂度复核** 保存首个偏离、拒绝结果和复位后的重放证据。

## 最小可重放实现

```text
# 第17章：不常见的控制结构：保留竞争假设，只用单变量探针排除
hypotheses = enumerate_causes(reproducible_failure)
while len(hypotheses) > 1:
    hypotheses = distinguish_with_one_probe(hypotheses)
assert replay_after_fix() == baseline_trace
```

这段 第17章：不常见的控制结构 验证草图表达独立教学合同，不复制原书代码。真实执行应保存输入、环境、节点轨迹、最终结果、拒绝理由和复位结果，使第二位读者能从干净状态重放。

## 专属因果实验

先预测改变“多个返回”或“递归”后，第17章：不常见的控制结构 的哪一个节点最先变化。切换基线、边界和注入故障，运行并保存证据；最后点击“重置实验”，确认滑块、场景、节点、压力和结果文字全部恢复。

<Cc2e17UnusualControlStructuresMechanismLab />

## 故障诊断与误区

<Callout
  type="trap"
  title="误区：递归未严格缩小问题，或提前返回绕过清理和不变量恢复"
>
  这个错误会让 第17章：不常见的控制结构
  无法从“特殊结构天然危险或天然简洁”稳定推进到“以路径证据判断结构取舍”。诊断沿
  控制目标 → 候选结构 → 路径展开 → 退出清理 → 复杂度复核
  前进，只在第一次出现预期与实际不一致的节点停下，不用最终症状倒猜原因。
</Callout>

<Callout type="trap" title="误区：一个指标可以代替全部证据">
  第17章：不常见的控制结构
  的单一数值可能被样本、缓存、默认值、测量窗口或偶然次序掩盖。至少同时保存机制轨迹、边界结果、故障拒绝和复位重放，任何一项缺失都不能用平均分补偿。
</Callout>

<Callout type="tip" title="恢复顺序">
  第17章：不常见的控制结构 的恢复顺序是：还原输入和环境，清理派生状态，重建
  候选结构，重跑 路径展开，核对 复杂度复核，再注入同一故障确认拒绝可重复。
</Callout>

## 术语与边界

第17章：不常见的控制结构 的五个操作术语是 <Term def="控制目标 是 第17章：不常见的控制结构 中可观察、可失败且可复位的机制边界">控制目标</Term>、<Term def="候选结构 是 第17章：不常见的控制结构 中可观察、可失败且可复位的机制边界">候选结构</Term>、<Term def="路径展开 是 第17章：不常见的控制结构 中可观察、可失败且可复位的机制边界">路径展开</Term>、<Term def="退出清理 是 第17章：不常见的控制结构 中可观察、可失败且可复位的机制边界">退出清理</Term>、<Term def="复杂度复核 是 第17章：不常见的控制结构 中可观察、可失败且可复位的机制边界">复杂度复核</Term>。每个术语必须能指向实验控件、节点或状态文本；若只能指向目录标题，说明解释和验证仍未完成。

<Glossary>

<GlossaryItem term="控制目标">
  控制目标 是 第17章：不常见的控制结构 的第 1
  个机制检查点；它的记录包含进入条件、状态变化、退出条件与故障表现。
</GlossaryItem>

<GlossaryItem term="候选结构">
  候选结构 是 第17章：不常见的控制结构 的第 2
  个机制检查点；它的记录包含进入条件、状态变化、退出条件与故障表现。
</GlossaryItem>

<GlossaryItem term="路径展开">
  路径展开 是 第17章：不常见的控制结构 的第 3
  个机制检查点；它的记录包含进入条件、状态变化、退出条件与故障表现。
</GlossaryItem>

<GlossaryItem term="退出清理">
  退出清理 是 第17章：不常见的控制结构 的第 4
  个机制检查点；它的记录包含进入条件、状态变化、退出条件与故障表现。
</GlossaryItem>

<GlossaryItem term="复杂度复核">
  复杂度复核 是 第17章：不常见的控制结构 的第 5
  个机制检查点；它的记录包含进入条件、状态变化、退出条件与故障表现。
</GlossaryItem>

</Glossary>

## 本页小结

第17章：不常见的控制结构 的完成标准是能解释“多返回、递归和跳转只有在缩短错误路径或直接表达问题结构时才使用，并显式证明资源清理、终止与可读性收益”，用核心合同推出预期，沿五个专属节点观察实际，诊断“递归未严格缩小问题，或提前返回绕过清理和不变量恢复”，并证明复位后同一输入重建基线轨迹。

<Exercises>

1. 第17章：不常见的控制结构 的五节点链中，哪个状态最能区分“特殊结构天然危险或天然简洁”和“以路径证据判断结构取舍”？请给出最小输入和恰好边界。

<Answer>
  先固定 控制目标，只改变与 候选结构 相连的一个条件；最早在 路径展开
  出现且能由合同预测的差异，就是区分两种状态的证据。边界值要同时记录接受或拒绝以及
  复杂度复核 的最终状态。
</Answer>

2. 使用 第17章：不常见的控制结构 的核心合同做一次手算、代码断言、状态推演、故障诊断或方案比较，并说明每个量对应的真实对象。

<Answer>
  把输入值、单位、版本和观察窗口写入基线，先求预期，再与实验轨迹比较。若量纲、分母、对象身份或执行顺序发生变化，应重建模型；不能只修改阈值让本轮通过。
</Answer>

3. 如何逐项证明 第17章：不常见的控制结构 的目录范围不只是标题出现？覆盖清单：第17章 不常见的控制结构（控制目标）；17.1 子程序中的多个返回（候选结构）；17.2 递归（路径展开）；递归的例子（退出清理）；使用递归的技巧（复杂度复核）；17.3 goto（控制目标）；反对goto的论点（候选结构）；支持goto的观点（路径展开）；关于goto的虚假辩论（退出清理）；错误处理和goto（复杂度复核）；goto和在else子句中的共享代码（控制目标）；goto使用原则总结（候选结构）；17.4 对不常见控制结构的看法（路径展开）；其他资源（退出清理）；关键点（复杂度复核）。

<Answer>
  对清单每项找到正文解释，指出它位于五节点链的哪一步，在专属实验中改变一个直接变量，并写下正常、边界、故障与复位结果。四类证据任缺一类，该目录节点仍保持未通过。
</Answer>

</Exercises>

<Attribution
  adaptedFrom="《代码大全（第2版）》2006 中文版公开试读目录用于界定 第17章：不常见的控制结构 的范围；技术讲解、机制图、实验、实现草图和练习依据 C++ Core Guidelines 独立重写；未使用或声称复现原书正文、插图、表格、核对表和代码"
  adaptedUrl="https://book.douban.com/reading/10145819/"
/>
