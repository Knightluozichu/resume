---
title: "第17章：不常见的控制结构"
type: B
section: "代码大全（第2版·2006中文版）"
order: 26
description: "评估多个返回、递归与goto的真实控制成本，依据可读性、错误处理和状态路径选择，而不是按教条全盘禁止或接受。"
demo: true
math: true
sourceUrl: "https://book.douban.com/reading/10145819/"
draft: false
---

import {
  Cc2e17UnusualControlStructuresStructureLab,
  Cc2e17UnusualControlStructuresTestLab,
  Cc2e17UnusualControlStructuresEvidenceLab,
} from "@/components/mdx/code-complete-2e/diagrams/cc2e-17-unusual-control-structures";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第17章：不常见的控制结构在49个正式单元和685个目录节点中的职责与前后依赖
- 能绘制“控制目标 → 候选结构 → 路径展开 → 退出清理 → 复杂度复核”因果链，并实现最小代码或项目验证
- 能计算复杂度、缺陷检出、变更风险与反馈时间，并比较正常、边界、单故障
- 能设计独立复核并证明“能将同一逻辑分别用常规结构和一种不常见结构实现，比较路径复杂度、清理正确性和可测试性后作有证据选择。”

</Objectives>

## 为什么这一单元不可压缩

本页依据史蒂夫·迈克康奈尔《代码大全（第2版）》，金戈、汤凌、陈硕、张菲译，裘宗燕审校，电子工业出版社，2006年3月，ISBN 9787121022982的公开完整试读目录，独立重构 **第17章 不常见的控制结构**。评估多个返回、递归与goto的真实控制成本，依据可读性、错误处理和状态路径选择，而不是按教条全盘禁止或接受。 本课程不复制原书正文、插图、表格、核对表或代码示例，而把目录主题转写成新的模型、代码实验、边界样本与复核证据。

中文版目录包含5个前置单元、7个部分、35章、参考文献和索引，共49个正式单元、685个目录节点。中文书目页登记944页；北京化工大学图书馆MARC登记“37,914页”，属于前置页与正文分页口径差异。课程以作者、译者、出版社、出版日期、ISBN和完整目录共同锁定版本，不因页数差异删减内容。

原版由Microsoft Press于2004年出版，共960页，ISBN 9780735619678。语言、工具和平台示例有历史边界，但管理复杂度、明确契约、开发者测试、增量集成和面向人阅读仍需在当前项目重新验证；现代框架名称不能倒灌成原书目录内容。

## 版次、对象与验收合同

<Term def="多个返回是第17章：不常见的控制结构的第1个复核坐标，必须绑定代码对象、版本、输入、边界和可观察证据。">
  多个返回
</Term>
、
<Term def="递归是第17章：不常见的控制结构的第2个复核坐标，必须绑定代码对象、版本、输入、边界和可观察证据。">
  递归
</Term>
、
<Term def="goto是第17章：不常见的控制结构的第3个复核坐标，必须绑定代码对象、版本、输入、边界和可观察证据。">
  goto
</Term>
、
<Term def="错误处理是第17章：不常见的控制结构的第4个复核坐标，必须绑定代码对象、版本、输入、边界和可观察证据。">
  错误处理
</Term>
、
<Term def="控制路径是第17章：不常见的控制结构的第5个复核坐标，必须绑定代码对象、版本、输入、边界和可观察证据。">
  控制路径
</Term>
共同构成本页验收坐标。每次实验固定需求、语言和编译器版本、输入数据、初始代码、测试与测量环境，只改变一个结构、类型、控制或过程条件。

<Callout type="info">
  **验收命题：**
  能将同一逻辑分别用常规结构和一种不常见结构实现，比较路径复杂度、清理正确性和可测试性后作有证据选择。
</Callout>

## 四个可核查模型

控制流复杂度用独立路径近似：

$$
V(G)=E-N+2P
$$

缺陷移除效能必须固定发现阶段和分母：

$$
DRE=removedBeforeRelease/(removedBeforeRelease+escaped)
$$

变更风险同时取决于传播范围、状态和测试缺口：

$$
risk=fanout * mutableState * uncoveredPaths
$$

发布采用乘法门禁，任一项缺失不能被平均分掩盖：

$$
release=contract * readableDesign * tests * integration * independentReview
$$

复杂度不是质量结论，覆盖率也不是正确性证明。公式输入必须来自同一版本和场景，并保存单位、采样窗口、阈值来源及无法测量的假设。

## 三段最小可运行骨架

```typescript
type ConstructionSnapshot = {
  edition: "2006-CN-2E";
  unit: "cc2e-17-unusual-control-structures";
  sourceHash: string;
  paths: number;
  tests: string[];
  evidence: string[];
};

export function releaseGate(snapshot: ConstructionSnapshot) {
  return (
    snapshot.sourceHash.length > 0 &&
    snapshot.tests.length >= 3 &&
    snapshot.evidence.length >= 3
  );
}
```

```python
def boundary_cases(low, high):
    if low > high:
        raise ValueError("range")
    return [low - 1, low, low + 1, high - 1, high, high + 1]
```

```python
def apply_one_change(baseline, change, validate):
    if len(change) != 1:
        return {"accepted": False, "state": baseline, "reason": "not-single-change"}
    candidate = dict(baseline, **change)
    return ({"accepted": True, "state": candidate, "reason": None}
            if validate(candidate) else
            {"accepted": False, "state": baseline, "reason": "gate-rejected"})
```

第一段冻结源代码与证据，第二段系统产生边界邻域，第三段保证拒绝时不留下半更新状态。三段都要补充正常、恰好边界和单故障预期，失败时先比较首差而不是最终输出。

## 正式目录逐项讲解

### 第17章 不常见的控制结构

数据表示必须明确范围、精度、编码、所有权、生命周期和非法状态。构造最小值、最大值、空值、越界和别名样本，验证类型选择真的约束错误。

复核动作：把“第17章 不常见的控制结构”放入第17章 不常见的控制结构的“控制目标 → 候选结构 → 路径展开 → 退出清理 → 复杂度复核”链路，只改变一个结构、数据或工具条件，保存预期、实际、首差与回退，并让第二位读者重放。

### 17.1 子程序中的多个返回

代码单元应表达一个一致职责和稳定契约。检查调用者需要知道什么、哪些状态必须隐藏、参数和返回怎样约束，以及替换实现时哪些测试保持不变。

复核动作：把“17.1 子程序中的多个返回”放入第17章 不常见的控制结构的“控制目标 → 候选结构 → 路径展开 → 退出清理 → 复杂度复核”链路，只改变一个结构、数据或工具条件，保存预期、实际、首差与回退，并让第二位读者重放。

### 17.2 递归

控制流要突出正常路径、退出条件和不变量。先画分支与循环状态，再减少否定、重复和深层嵌套；用零次、一次、边界和默认分支覆盖证明完整。

复核动作：把“17.2 递归”放入第17章 不常见的控制结构的“控制目标 → 候选结构 → 路径展开 → 退出清理 → 复杂度复核”链路，只改变一个结构、数据或工具条件，保存预期、实际、首差与回退，并让第二位读者重放。

### 递归的例子

控制流要突出正常路径、退出条件和不变量。先画分支与循环状态，再减少否定、重复和深层嵌套；用零次、一次、边界和默认分支覆盖证明完整。

复核动作：把“递归的例子”放入第17章 不常见的控制结构的“控制目标 → 候选结构 → 路径展开 → 退出清理 → 复杂度复核”链路，只改变一个结构、数据或工具条件，保存预期、实际、首差与回退，并让第二位读者重放。

### 使用递归的技巧

控制流要突出正常路径、退出条件和不变量。先画分支与循环状态，再减少否定、重复和深层嵌套；用零次、一次、边界和默认分支覆盖证明完整。

复核动作：把“使用递归的技巧”放入第17章 不常见的控制结构的“控制目标 → 候选结构 → 路径展开 → 退出清理 → 复杂度复核”链路，只改变一个结构、数据或工具条件，保存预期、实际、首差与回退，并让第二位读者重放。

### 17.3 goto

控制流要突出正常路径、退出条件和不变量。先画分支与循环状态，再减少否定、重复和深层嵌套；用零次、一次、边界和默认分支覆盖证明完整。

复核动作：把“17.3 goto”放入第17章 不常见的控制结构的“控制目标 → 候选结构 → 路径展开 → 退出清理 → 复杂度复核”链路，只改变一个结构、数据或工具条件，保存预期、实际、首差与回退，并让第二位读者重放。

### 反对goto的论点

控制流要突出正常路径、退出条件和不变量。先画分支与循环状态，再减少否定、重复和深层嵌套；用零次、一次、边界和默认分支覆盖证明完整。

复核动作：把“反对goto的论点”放入第17章 不常见的控制结构的“控制目标 → 候选结构 → 路径展开 → 退出清理 → 复杂度复核”链路，只改变一个结构、数据或工具条件，保存预期、实际、首差与回退，并让第二位读者重放。

### 支持goto的观点

控制流要突出正常路径、退出条件和不变量。先画分支与循环状态，再减少否定、重复和深层嵌套；用零次、一次、边界和默认分支覆盖证明完整。

复核动作：把“支持goto的观点”放入第17章 不常见的控制结构的“控制目标 → 候选结构 → 路径展开 → 退出清理 → 复杂度复核”链路，只改变一个结构、数据或工具条件，保存预期、实际、首差与回退，并让第二位读者重放。

### 关于goto的虚假辩论

控制流要突出正常路径、退出条件和不变量。先画分支与循环状态，再减少否定、重复和深层嵌套；用零次、一次、边界和默认分支覆盖证明完整。

复核动作：把“关于goto的虚假辩论”放入第17章 不常见的控制结构的“控制目标 → 候选结构 → 路径展开 → 退出清理 → 复杂度复核”链路，只改变一个结构、数据或工具条件，保存预期、实际、首差与回退，并让第二位读者重放。

### 错误处理和goto

把外部不可信输入、内部程序错误和可恢复运行故障分层处理。边界校验、断言、异常与隔离区各有责任，任何失败都要留下首差和安全状态。

复核动作：把“错误处理和goto”放入第17章 不常见的控制结构的“控制目标 → 候选结构 → 路径展开 → 退出清理 → 复杂度复核”链路，只改变一个结构、数据或工具条件，保存预期、实际、首差与回退，并让第二位读者重放。

### goto和在else子句中的共享代码

控制流要突出正常路径、退出条件和不变量。先画分支与循环状态，再减少否定、重复和深层嵌套；用零次、一次、边界和默认分支覆盖证明完整。

复核动作：把“goto和在else子句中的共享代码”放入第17章 不常见的控制结构的“控制目标 → 候选结构 → 路径展开 → 退出清理 → 复杂度复核”链路，只改变一个结构、数据或工具条件，保存预期、实际、首差与回退，并让第二位读者重放。

### goto使用原则总结

控制流要突出正常路径、退出条件和不变量。先画分支与循环状态，再减少否定、重复和深层嵌套；用零次、一次、边界和默认分支覆盖证明完整。

复核动作：把“goto使用原则总结”放入第17章 不常见的控制结构的“控制目标 → 候选结构 → 路径展开 → 退出清理 → 复杂度复核”链路，只改变一个结构、数据或工具条件，保存预期、实际、首差与回退，并让第二位读者重放。

### 17.4 对不常见控制结构的看法

数据表示必须明确范围、精度、编码、所有权、生命周期和非法状态。构造最小值、最大值、空值、越界和别名样本，验证类型选择真的约束错误。

复核动作：把“17.4 对不常见控制结构的看法”放入第17章 不常见的控制结构的“控制目标 → 候选结构 → 路径展开 → 退出清理 → 复杂度复核”链路，只改变一个结构、数据或工具条件，保存预期、实际、首差与回退，并让第二位读者重放。

### 其他资源

其他资源承担第17章 不常见的控制结构中的一个正式目录坐标。先说明它解决的构建问题、输入、输出、适用范围和与相邻主题的依赖，再把建议改写为可以失败的判断。

最小实验固定2006年中文第2版语义边界和同一代码基线，只改变一个条件，记录复杂度、测试、缺陷或反馈时间的首个变化；未覆盖的语言与项目差异必须显式保留。

### 关键点

关键点用于把本单元收束为少量可执行判断。复核时把每条判断改写成带对象、前置条件、失败样本和证据位置的门禁，不能只背结论。

复核动作：把“关键点”放入第17章 不常见的控制结构的“控制目标 → 候选结构 → 路径展开 → 退出清理 → 复杂度复核”链路，只改变一个结构、数据或工具条件，保存预期、实际、首差与回退，并让第二位读者重放。

## 从目录建议到构建因果链

先画五节点链：控制目标 → 候选结构 → 路径展开 → 退出清理 → 复杂度复核。每条边注明传递的是需求、抽象、类型、状态、控制、构件、缺陷还是证据；每个节点写出进入条件、退出条件、所有者和失败回退。没有对象或状态变化的箭头只是排版连接。

再以多个返回、递归、goto争论、错误处理、使用原则作为检查点。正常样本证明主路径可运行；边界样本选择恰好类型范围、控制端点、复杂度阈值、测试覆盖或构建容量；单故障样本只改变一个输入、分支、依赖、工具或构件。三类样本共享其余条件，才能归因。

书中示例语言与当代项目不同时，先保留原建议解决的问题，再比较类型系统、内存模型、工具自动化和发布环境。后续语言特性可以降低次要负担，但不能替代领域建模、接口选择和失败验证。

## 最小证据包与独立复核

证据包包含ISBN与目录坐标、需求与代码哈希、语言和工具版本、输入数据、复杂度、静态分析、测试设计、失败日志、性能基线、集成轨迹、回退状态和结论。覆盖率、格式化结果或最终截图只能辅助定位，不能单独证明质量。

独立复核者只接收冻结输入、代码基线和验收条件，不接收原作者的操作顺序或期待答案。复核者先预测五个节点，再运行同一数据；结果不一致时比较首个分叉、原始日志和环境差异。

## 跨语言与时代迁移审查

把2006年的构建建议迁移到当前语言时，先保存原建议解决的风险，再分别记录今天由类型系统、标准库、编译器、静态分析、测试框架或持续集成自动承担的部分。自动化已经覆盖的机械步骤可以缩减，但它的配置、盲区和失败模式必须进入证据包；仍涉及领域抽象、接口语义、数据边界和取舍的部分，不能交给工具默认值静默决定。

迁移记录至少比较四栏：原版问题、当前机制、仍存风险、验证样本。若一种现代特性声称让某条建议“过时”，就构造关闭该特性、误配该特性和越过其检查边界的样本；只有能说明风险确实被消除而非转移，才可调整学习权重。这样既不机械照搬旧语言技巧，也不因工具更新而丢失软件构建的因果问题。

## 构建验证卡

每次应用目录建议都填写同一张验证卡：问题必须可失败；基线必须包含版本、输入和测量；改动一次只改变一个变量；证据要能定位首差；复核由未参与实现的人完成。缺少任一栏，结论只能保留为待验证假设。

| 字段 | 必答问题                     | 拒绝条件                   |
| ---- | ---------------------------- | -------------------------- |
| 问题 | 哪个构建风险需要降低         | 只有风格偏好，没有失败条件 |
| 基线 | 代码、输入、工具和指标是什么 | 分母、单位或版本不一致     |
| 改动 | 唯一改变了哪个结构或过程     | 同时重写、调优和换工具     |
| 证据 | 首差、测试、性能与回退在哪里 | 只有最终成功截图           |
| 复核 | 谁在相同输入下重建结果       | 依赖实现者操作提示         |

## 正常、边界与单故障矩阵

| 样本   | 只改变的变量                   | 预期结果                     | 必存证据                   |
| ------ | ------------------------------ | ---------------------------- | -------------------------- |
| 正常   | 合法输入、清晰契约和稳定工具链 | 主路径闭合且指标有余量       | 代码哈希、输入、测试与轨迹 |
| 边界   | 恰好范围、端点、复杂度或容量   | 明确接受或拒绝，不产生半状态 | 阈值、分支、原状态与断言   |
| 单故障 | 一个输入、依赖、分支或构件失败 | 在首个异常边界隔离并回退     | 首差、错误策略、恢复与回归 |

操作前先预测。若一次同时重构、调优、换语言和升级工具，就无法判断改进来源，必须重置基线。

## 三步交互实验

<Stepper>
  <Step title="追踪结构、数据与控制边界">
    选择节点，检查对象、状态、决定和证据怎样跨边界传播。
    <Cc2e17UnusualControlStructuresStructureLab />
  </Step>
  <Step title="调整复杂度、测试与变更范围">
    一次只改变一个参数，观察认知负担、缺陷暴露和变更风险。
    <Cc2e17UnusualControlStructuresTestLab />
  </Step>
  <Step title="注入一个故障并保存证据">
    比较正常、恰好边界与单故障，要求门禁拒绝无证据结论。
    <Cc2e17UnusualControlStructuresEvidenceLab />
  </Step>
</Stepper>

## 常见失败与回退

<Callout type="trap">
  **把规则变成风格口号：**
  没有问题、边界和反例的“最佳实践”无法裁决。回到代码基线，说明风险、唯一改动与可观察结果。
</Callout>

<Callout type="trap">
  **用现代工具覆盖原问题：**
  自动格式化、类型检查和生成式工具能减少部分机械工作，却不自动保证抽象、契约和测试。记录工具解决了什么，以及仍需人判断什么。
</Callout>

## 本章回顾

评估多个返回、递归与goto的真实控制成本，依据可读性、错误处理和状态路径选择，而不是按教条全盘禁止或接受。 完成“能将同一逻辑分别用常规结构和一种不常见结构实现，比较路径复杂度、清理正确性和可测试性后作有证据选择。”后，还要让独立复核者在相同边界重建结构、测试、首差和回退。一次通过不能代表所有输入，能解释何时失败和怎样恢复才是可迁移的构建知识。

<Glossary>

<GlossaryItem term="多个返回">
  多个返回用于定位第17章：不常见的控制结构中的第1类结构、数据、质量或过程变量；使用时必须写出适用边界、失败条件和复核方法。
</GlossaryItem>

<GlossaryItem term="递归">
  递归用于定位第17章：不常见的控制结构中的第2类结构、数据、质量或过程变量；使用时必须写出适用边界、失败条件和复核方法。
</GlossaryItem>

<GlossaryItem term="goto">
  goto用于定位第17章：不常见的控制结构中的第3类结构、数据、质量或过程变量；使用时必须写出适用边界、失败条件和复核方法。
</GlossaryItem>

<GlossaryItem term="错误处理">
  错误处理用于定位第17章：不常见的控制结构中的第4类结构、数据、质量或过程变量；使用时必须写出适用边界、失败条件和复核方法。
</GlossaryItem>

<GlossaryItem term="控制路径">
  控制路径用于定位第17章：不常见的控制结构中的第5类结构、数据、质量或过程变量；使用时必须写出适用边界、失败条件和复核方法。
</GlossaryItem>

</Glossary>

<Exercises>

1. 如何为第17章：不常见的控制结构建立最小可重放基线？

<Answer>
  固定2006年中文第2版目录坐标、需求、代码哈希、语言与工具、输入和测试，先画“控制目标
  → 候选结构 → 路径展开 → 退出清理 →
  复杂度复核”预期，再保存正常、边界与单故障轨迹。
</Answer>

2. 怎样证明“能将同一逻辑分别用常规结构和一种不常见结构实现，比较路径复杂度、清理正确性和可测试性后作有证据选择。”不是一次偶然成功？

<Answer>
  先手算复杂度、边界、缺陷检出或反馈预算，再让独立复核者只依据输入和验收合同重建；双方必须在相同首差拒绝故障，并保留相同回退状态。
</Answer>

3. 修复一个失败后怎样回归？

<Answer>
  先重放原失败，再运行相邻边界和正常基线；逐节点检查契约、类型、控制、测试、工具与恢复证据，全部通过后才进入下一页。
</Answer>

</Exercises>

## 前后导航

- [上一页：第16章：控制循环](/learn/code-complete-2e/25-loops/cc2e-16-loops)
- [下一页：第18章：表驱动方法](/learn/code-complete-2e/27-table-driven-methods/cc2e-18-table-driven-methods)

<Attribution
  adaptedFrom="史蒂夫·迈克康奈尔《代码大全（第2版）》，金戈、汤凌、陈硕、张菲译，裘宗燕审校，电子工业出版社，2006年3月，ISBN 9787121022982 · 公开试读目录含5个前置单元、7个部分、35章、参考文献与索引共49个正式单元和685个目录节点 · 基于公开目录独立中文教学重构，不复制原书正文、插图、表格、核对表或代码示例"
  adaptedUrl="https://book.douban.com/reading/10145819/"
/>
