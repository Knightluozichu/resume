---
title: "第5部分：代码改进"
type: B
section: "代码大全（第2版·2006中文版）"
order: 29
description: "第5部分代码改进：从问题定位出发，单变量重构，比较性能与边界，并用回归反馈证明改进。"
demo: true
math: true
sourceUrl: "https://book.douban.com/reading/10145819/"
qualityVersion: 2
practiceMode: code
sourceMode: independent-rewrite
officialUnitId: cc2e-part-05-code-improvement
draft: false
---

import { Objectives } from "@/components/mdx/objectives";
import { Callout } from "@/components/mdx/callout";
import { Term } from "@/components/mdx/term";
import { Exercises, Answer } from "@/components/mdx/exercises";
import { Glossary, GlossaryItem } from "@/components/mdx/glossary";
import { Attribution } from "@/components/mdx/attribution";
import { Cc2ePart05CodeImprovementLab } from "@/components/mdx/code-complete-2e/v2/cc2e-part-05-code-improvement";

<Objectives>

- 能先定位可复现的问题，再决定是重构、调优还是补防御。
- 能把一次改进限制为可比较的单变量变更，并同时记录收益、代价和边界。
- 能用回归反馈确认行为契约没有被破坏，而不是只凭局部结果宣称改进。

</Objectives>

## 为什么需要这一部分

代码改进不是随意重写，而是从一个可复现的问题出发改变一个条件，再比较前后结果。重构关注结构，性能调优关注资源和观察窗口，防御关注异常边界；三者都需要基线、对照和回归。

<Callout type="info" title="范围与事实来源">
  本部分范围依据[2006 中文第 2 版公开试读目录](https://book.douban.com/reading/10145819/)，软件质量与性能测量边界交叉核对[IEEE SWEBOK](https://www.computer.org/education/bodies-of-knowledge/software-engineering/v4)和[NIST Engineering Statistics Handbook](https://www.itl.nist.gov/div898/handbook/)。本文是独立教学重写。
</Callout>

## 先避开三个改进误区

<Callout type="trap" title="误区一：没有基线也能判断更好">
  现象 → 改完后凭感觉接受。原因 → 输入、版本和指标不固定。修法 → 先冻结基线和观察窗口。
</Callout>

<Callout type="trap" title="误区二：局部更快就是整体改进">
  现象 → 只看一次局部耗时。原因 → 忽略内存、可维护性和尾延迟。修法 → 明确目标函数和被接受的代价。
</Callout>

<Callout type="trap" title="误区三：重构可以跳过回归">
  现象 → 认为只改结构就不会改变行为。原因 → 隐藏契约没有被测试。修法 → 保留正常、边界和故障回归。
</Callout>

## 核心合同与操作术语

<Term def="由输入、版本、观察窗口和首个偏离组成的可复现问题记录">问题定位</Term>是起点；<Term def="不改变外部行为而改善结构、职责或表达的单变量变更">重构</Term>要保持契约。<Term def="在固定负载和指标下比较资源、延迟或吞吐的取舍">性能取舍</Term>不能脱离环境；<Term def="对无效、异常和边界输入规定拒绝或恢复行为">防御</Term>保留失败边界；<Term def="修订后重新执行原始路径及受影响边界的记录">回归反馈</Term>裁决是否真的变好。

## 目录节点逐项深读

### 第5部分 代码改进

本部分的阅读合同是：定位问题，冻结基线，只改一个条件，比较结果与代价，最后回归。若改进同时改变接口、数据、环境和指标，结果不能归因，必须拆成多个实验。

## 最小可重放实现

~~~text
baseline = freeze(version, input, metric, observationWindow)
deviation = reproduce_problem(baseline)
candidate = change_one_thing(baseline)
compare(candidate, baseline, cost, boundary)
record(firstDeviation, regression, decision)
~~~

正常轨迹验证原行为；边界轨迹改变规模或负载；故障轨迹让改进破坏契约；复位轨迹从原始基线重跑，避免在已污染的环境上得出结论。

## 先预测，再操作证据实验

先预测问题定位、重构、性能或回归节点的首个变化，再只切换一个场景。专属面板表达改进的因果链，不输出真实项目性能承诺。

<Cc2ePart05CodeImprovementLab />

## 练习与答案

<Exercises>

**问题 1：建立基线。** 你要优化一个慢查询，最少要固定哪些条件？

<Answer>
  固定数据库版本、数据快照、查询输入、索引配置、并发负载、观察窗口和指标，先记录正常与边界样本，再一次只改变查询或索引策略。
</Answer>

**问题 2：区分重构和行为变化。** 重命名并移动函数后测试失败，如何判断是不是无害重构？

<Answer>
  从失败输入追踪首个偏离，比较接口、异常、顺序和副作用；如果外部行为改变，就不是纯重构，应明确修订契约并补回归。
</Answer>

**问题 3：处理代价。** 延迟下降但内存翻倍，如何写出可交接判断？

<Answer>
  写明目标、收益、内存代价、负载边界和接受条件；让第二位实践者在同一基线重放，并由系统约束决定是否接受，而非只看一个更好的数字。
</Answer>

</Exercises>

## 术语表

<Glossary>
<GlossaryItem term="问题定位">包含输入、版本、窗口和首个偏离的可复现问题记录。</GlossaryItem>
<GlossaryItem term="重构">保持外部行为、改善结构的单变量变更。</GlossaryItem>
<GlossaryItem term="性能取舍">在固定指标和负载下比较收益与资源代价。</GlossaryItem>
<GlossaryItem term="防御">对异常和边界输入规定拒绝或恢复行为。</GlossaryItem>
<GlossaryItem term="回归反馈">修订后重新执行原路径和受影响边界的记录。</GlossaryItem>
</Glossary>

## 本页小结

代码改进靠基线和对照说话：先定位，再单变量改变，比较收益与代价，保留防御边界，最后用回归反馈裁决。没有可复现基线，就没有可靠的“改进”。

<Attribution
  mode="independent-rewrite"
  sourceBasis="outline-only"
  workTitle="Steve McConnell, Code Complete, Second Edition"
  adaptedUrl="https://book.douban.com/reading/10145819/"
/>
