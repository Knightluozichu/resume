---
title: "第22章：开发者测试"
type: B
section: "代码大全（第2版·2006中文版）"
order: 32
description: "第22章：开发者测试：从“运行过就是测试”推进到“测试能稳定暴露指定故障”，以目录节点、专属机制实验和故障复位验收。"
demo: true
math: true
sourceUrl: "https://book.douban.com/reading/10145819/"
qualityVersion: 2
practiceMode: code
sourceMode: independent-rewrite
draft: true
---

import { Cc2e22DeveloperTestingMechanismLab } from "@/components/mdx/code-complete-2e/diagrams/cc2e-22-developer-testing";
import {
  Objectives,
  Callout,
  Term,
  Glossary,
  GlossaryItem,
  Exercises,
  Answer,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能说明 第22章：开发者测试 怎样把“运行过就是测试”推进为“测试能稳定暴露指定故障”
- 能沿 测试目标 → 用例设计 → 脚手架执行 → 覆盖观察 → 回归记录 重建对象、状态、决定和证据
- 能使用本页合同完成一次代码修改
- 能注入“只写会通过的正常样例，或断言最终输出却不定位首差”，找到首差并确认重置恢复基线

</Objectives>

## 为什么需要这一机制

第22章：开发者测试 解决的不是一个可背诵口号，而是从 **运行过就是测试** 到 **测试能稳定暴露指定故障** 的可观察变化。它的核心机制是：开发者测试从需求和代码结构导出正常、边界、错误与独立路径用例，脚手架保证隔离、可重复和失败可诊断。

学习 第22章：开发者测试 时先预测 测试目标、用例设计、脚手架执行、覆盖观察、回归记录 的输入与输出，再改变一个直接条件。最终结果即使看似正确，只要中间状态违反合同、故障不能隔离或重置不能回到同一基线，本页结论就不通过。

<Callout type="info" title="范围与事实来源">
  第22章：开发者测试 由 [2006 中文第 2
  版公开试读目录](https://book.douban.com/reading/10145819/)
  限定范围，技术机制依据 [NIST SP 800-218 Secure Software Development
  Framework](https://csrc.nist.gov/pubs/sp/800/218/final)
  独立重写。目录核对只证明主题坐标；本页不宣称取得、翻译或复现原书正文、插图、表格、核对表与代码。
</Callout>

## 核心合同

$$
tests=equivalence\ classes+boundaries+error\ paths+independent\ paths
$$

第22章：开发者测试 的合同变量必须绑定到同一版本、同一输入、同一单位和同一观察窗口。先由合同推出预期，再观察 脚手架执行 与 回归记录；如果只是修改阈值来迎合结果，实验失去裁决能力。

## 目录节点到四级证据

以下节点逐项给出“出现—解释—视觉/实验—练习验证”证据。第22章：开发者测试 不把目录词频当作覆盖率；每一项都必须能在专属机制链中指出状态变化，并在章末练习清单中被复核。

### 第22章 开发者测试

在 **第22章：开发者测试** 中，目录节点 **第22章 开发者测试** 落在“测试目标”检查点。这里的技术含义是：从合同、等价类、边界和独立路径导出可重复断言；它影响“测试角色”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **第22章 开发者测试** 时，先故意破坏目标行为，确认测试确实由红转绿。先写预期，再运行正常值、恰好边界和一个故障；若出现“只写会通过的正常样例，或断言最终输出却不定位首差”，就在 第22章：开发者测试 的 **测试目标** 保存首个偏离、拒绝结果和复位后的重放证据。

### 22.1 开发者测试在软件质量中的角色

在 **第22章：开发者测试** 中，目录节点 **22.1 开发者测试在软件质量中的角色** 落在“用例设计”检查点。这里的技术含义是：把质量属性分开定义，再选择预防、发现和修复证据；它影响“测试优先”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **22.1 开发者测试在软件质量中的角色** 时，让一个指标改善而另一个恶化，检查门禁能否阻止平均分掩盖。先写预期，再运行正常值、恰好边界和一个故障；若出现“只写会通过的正常样例，或断言最终输出却不定位首差”，就在 第22章：开发者测试 的 **用例设计** 保存首个偏离、拒绝结果和复位后的重放证据。

### 构造中测试

在 **第22章：开发者测试** 中，目录节点 **构造中测试** 落在“脚手架执行”检查点。这里的技术含义是：从合同、等价类、边界和独立路径导出可重复断言；它影响“技巧”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **构造中测试** 时，先故意破坏目标行为，确认测试确实由红转绿。先写预期，再运行正常值、恰好边界和一个故障；若出现“只写会通过的正常样例，或断言最终输出却不定位首差”，就在 第22章：开发者测试 的 **脚手架执行** 保存首个偏离、拒绝结果和复位后的重放证据。

### 22.2 推荐的开发者测试方法

在 **第22章：开发者测试** 中，目录节点 **22.2 推荐的开发者测试方法** 落在“覆盖观察”检查点。这里的技术含义是：从合同、等价类、边界和独立路径导出可重复断言；它影响“典型错误”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **22.2 推荐的开发者测试方法** 时，先故意破坏目标行为，确认测试确实由红转绿。先写预期，再运行正常值、恰好边界和一个故障；若出现“只写会通过的正常样例，或断言最终输出却不定位首差”，就在 第22章：开发者测试 的 **覆盖观察** 保存首个偏离、拒绝结果和复位后的重放证据。

### 先测试还是后测试

在 **第22章：开发者测试** 中，目录节点 **先测试还是后测试** 落在“回归记录”检查点。这里的技术含义是：从合同、等价类、边界和独立路径导出可重复断言；它影响“工具过程”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **先测试还是后测试** 时，先故意破坏目标行为，确认测试确实由红转绿。先写预期，再运行正常值、恰好边界和一个故障；若出现“只写会通过的正常样例，或断言最终输出却不定位首差”，就在 第22章：开发者测试 的 **回归记录** 保存首个偏离、拒绝结果和复位后的重放证据。

### 开发者测试的局限性

在 **第22章：开发者测试** 中，目录节点 **开发者测试的局限性** 落在“测试目标”检查点。这里的技术含义是：从合同、等价类、边界和独立路径导出可重复断言；它影响“测试角色”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **开发者测试的局限性** 时，先故意破坏目标行为，确认测试确实由红转绿。先写预期，再运行正常值、恰好边界和一个故障；若出现“只写会通过的正常样例，或断言最终输出却不定位首差”，就在 第22章：开发者测试 的 **测试目标** 保存首个偏离、拒绝结果和复位后的重放证据。

### 22.3 测试技巧锦囊

在 **第22章：开发者测试** 中，目录节点 **22.3 测试技巧锦囊** 落在“用例设计”检查点。这里的技术含义是：从合同、等价类、边界和独立路径导出可重复断言；它影响“测试优先”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **22.3 测试技巧锦囊** 时，先故意破坏目标行为，确认测试确实由红转绿。先写预期，再运行正常值、恰好边界和一个故障；若出现“只写会通过的正常样例，或断言最终输出却不定位首差”，就在 第22章：开发者测试 的 **用例设计** 保存首个偏离、拒绝结果和复位后的重放证据。

### 不完整的测试

在 **第22章：开发者测试** 中，目录节点 **不完整的测试** 落在“脚手架执行”检查点。这里的技术含义是：从合同、等价类、边界和独立路径导出可重复断言；它影响“技巧”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **不完整的测试** 时，先故意破坏目标行为，确认测试确实由红转绿。先写预期，再运行正常值、恰好边界和一个故障；若出现“只写会通过的正常样例，或断言最终输出却不定位首差”，就在 第22章：开发者测试 的 **脚手架执行** 保存首个偏离、拒绝结果和复位后的重放证据。

### 结构化的基础测试

在 **第22章：开发者测试** 中，目录节点 **结构化的基础测试** 落在“覆盖观察”检查点。这里的技术含义是：声明所有权、别名、边界、可变性和释放责任；它影响“典型错误”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **结构化的基础测试** 时，制造空引用、悬空引用、越界索引与并发写入各一个反例。先写预期，再运行正常值、恰好边界和一个故障；若出现“只写会通过的正常样例，或断言最终输出却不定位首差”，就在 第22章：开发者测试 的 **覆盖观察** 保存首个偏离、拒绝结果和复位后的重放证据。

### 数据流测试

在 **第22章：开发者测试** 中，目录节点 **数据流测试** 落在“回归记录”检查点。这里的技术含义是：从合同、等价类、边界和独立路径导出可重复断言；它影响“工具过程”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **数据流测试** 时，先故意破坏目标行为，确认测试确实由红转绿。先写预期，再运行正常值、恰好边界和一个故障；若出现“只写会通过的正常样例，或断言最终输出却不定位首差”，就在 第22章：开发者测试 的 **回归记录** 保存首个偏离、拒绝结果和复位后的重放证据。

### 等价类划分

在 **第22章：开发者测试** 中，目录节点 **等价类划分** 落在“测试目标”检查点。这里的技术含义是：用构造和公开操作守住对象不变量，优先组合稳定职责；它影响“测试角色”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **等价类划分** 时，尝试绕过接口制造半有效对象，确认不变量不可被外部破坏。先写预期，再运行正常值、恰好边界和一个故障；若出现“只写会通过的正常样例，或断言最终输出却不定位首差”，就在 第22章：开发者测试 的 **测试目标** 保存首个偏离、拒绝结果和复位后的重放证据。

### 猜测错误

在 **第22章：开发者测试** 中，目录节点 **猜测错误** 落在“用例设计”检查点。这里的技术含义是：把“猜测错误”放回 开发者测试从需求和代码结构导出正常、边界、错误与独立路径用例，脚手架保证隔离、可重复和失败可诊断；它影响“测试优先”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **猜测错误** 时，固定其余条件，只改变与“猜测错误”直接相连的一个输入或决定。先写预期，再运行正常值、恰好边界和一个故障；若出现“只写会通过的正常样例，或断言最终输出却不定位首差”，就在 第22章：开发者测试 的 **用例设计** 保存首个偏离、拒绝结果和复位后的重放证据。

### 边界值分析

在 **第22章：开发者测试** 中，目录节点 **边界值分析** 落在“脚手架执行”检查点。这里的技术含义是：把“边界值分析”放回 开发者测试从需求和代码结构导出正常、边界、错误与独立路径用例，脚手架保证隔离、可重复和失败可诊断；它影响“技巧”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **边界值分析** 时，固定其余条件，只改变与“边界值分析”直接相连的一个输入或决定。先写预期，再运行正常值、恰好边界和一个故障；若出现“只写会通过的正常样例，或断言最终输出却不定位首差”，就在 第22章：开发者测试 的 **脚手架执行** 保存首个偏离、拒绝结果和复位后的重放证据。

### 几类坏数据

在 **第22章：开发者测试** 中，目录节点 **几类坏数据** 落在“覆盖观察”检查点。这里的技术含义是：用构造和公开操作守住对象不变量，优先组合稳定职责；它影响“典型错误”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **几类坏数据** 时，尝试绕过接口制造半有效对象，确认不变量不可被外部破坏。先写预期，再运行正常值、恰好边界和一个故障；若出现“只写会通过的正常样例，或断言最终输出却不定位首差”，就在 第22章：开发者测试 的 **覆盖观察** 保存首个偏离、拒绝结果和复位后的重放证据。

### 几类好数据

在 **第22章：开发者测试** 中，目录节点 **几类好数据** 落在“回归记录”检查点。这里的技术含义是：用构造和公开操作守住对象不变量，优先组合稳定职责；它影响“工具过程”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **几类好数据** 时，尝试绕过接口制造半有效对象，确认不变量不可被外部破坏。先写预期，再运行正常值、恰好边界和一个故障；若出现“只写会通过的正常样例，或断言最终输出却不定位首差”，就在 第22章：开发者测试 的 **回归记录** 保存首个偏离、拒绝结果和复位后的重放证据。

### 采用容易手工检查的测试用例

在 **第22章：开发者测试** 中，目录节点 **采用容易手工检查的测试用例** 落在“测试目标”检查点。这里的技术含义是：从合同、等价类、边界和独立路径导出可重复断言；它影响“测试角色”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **采用容易手工检查的测试用例** 时，先故意破坏目标行为，确认测试确实由红转绿。先写预期，再运行正常值、恰好边界和一个故障；若出现“只写会通过的正常样例，或断言最终输出却不定位首差”，就在 第22章：开发者测试 的 **测试目标** 保存首个偏离、拒绝结果和复位后的重放证据。

### 22.4 典型错误

在 **第22章：开发者测试** 中，目录节点 **22.4 典型错误** 落在“用例设计”检查点。这里的技术含义是：把“22.4 典型错误”放回 开发者测试从需求和代码结构导出正常、边界、错误与独立路径用例，脚手架保证隔离、可重复和失败可诊断；它影响“测试优先”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **22.4 典型错误** 时，固定其余条件，只改变与“22.4 典型错误”直接相连的一个输入或决定。先写预期，再运行正常值、恰好边界和一个故障；若出现“只写会通过的正常样例，或断言最终输出却不定位首差”，就在 第22章：开发者测试 的 **用例设计** 保存首个偏离、拒绝结果和复位后的重放证据。

### 哪些类包含最多的错误？

在 **第22章：开发者测试** 中，目录节点 **哪些类包含最多的错误？** 落在“脚手架执行”检查点。这里的技术含义是：用构造和公开操作守住对象不变量，优先组合稳定职责；它影响“技巧”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **哪些类包含最多的错误？** 时，尝试绕过接口制造半有效对象，确认不变量不可被外部破坏。先写预期，再运行正常值、恰好边界和一个故障；若出现“只写会通过的正常样例，或断言最终输出却不定位首差”，就在 第22章：开发者测试 的 **脚手架执行** 保存首个偏离、拒绝结果和复位后的重放证据。

### 错误的分类

在 **第22章：开发者测试** 中，目录节点 **错误的分类** 落在“覆盖观察”检查点。这里的技术含义是：用构造和公开操作守住对象不变量，优先组合稳定职责；它影响“典型错误”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **错误的分类** 时，尝试绕过接口制造半有效对象，确认不变量不可被外部破坏。先写预期，再运行正常值、恰好边界和一个故障；若出现“只写会通过的正常样例，或断言最终输出却不定位首差”，就在 第22章：开发者测试 的 **覆盖观察** 保存首个偏离、拒绝结果和复位后的重放证据。

### 不完善的构造过程引发错误所占的比例

在 **第22章：开发者测试** 中，目录节点 **不完善的构造过程引发错误所占的比例** 落在“回归记录”检查点。这里的技术含义是：把“不完善的构造过程引发错误所占的比例”放回 开发者测试从需求和代码结构导出正常、边界、错误与独立路径用例，脚手架保证隔离、可重复和失败可诊断；它影响“工具过程”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **不完善的构造过程引发错误所占的比例** 时，固定其余条件，只改变与“不完善的构造过程引发错误所占的比例”直接相连的一个输入或决定。先写预期，再运行正常值、恰好边界和一个故障；若出现“只写会通过的正常样例，或断言最终输出却不定位首差”，就在 第22章：开发者测试 的 **回归记录** 保存首个偏离、拒绝结果和复位后的重放证据。

### 你期望能发现多少错误

在 **第22章：开发者测试** 中，目录节点 **你期望能发现多少错误** 落在“测试目标”检查点。这里的技术含义是：把“你期望能发现多少错误”放回 开发者测试从需求和代码结构导出正常、边界、错误与独立路径用例，脚手架保证隔离、可重复和失败可诊断；它影响“测试角色”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **你期望能发现多少错误** 时，固定其余条件，只改变与“你期望能发现多少错误”直接相连的一个输入或决定。先写预期，再运行正常值、恰好边界和一个故障；若出现“只写会通过的正常样例，或断言最终输出却不定位首差”，就在 第22章：开发者测试 的 **测试目标** 保存首个偏离、拒绝结果和复位后的重放证据。

### 测试本身的错误

在 **第22章：开发者测试** 中，目录节点 **测试本身的错误** 落在“用例设计”检查点。这里的技术含义是：从合同、等价类、边界和独立路径导出可重复断言；它影响“测试优先”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **测试本身的错误** 时，先故意破坏目标行为，确认测试确实由红转绿。先写预期，再运行正常值、恰好边界和一个故障；若出现“只写会通过的正常样例，或断言最终输出却不定位首差”，就在 第22章：开发者测试 的 **用例设计** 保存首个偏离、拒绝结果和复位后的重放证据。

### 22.5 测试支持工具

在 **第22章：开发者测试** 中，目录节点 **22.5 测试支持工具** 落在“脚手架执行”检查点。这里的技术含义是：从合同、等价类、边界和独立路径导出可重复断言；它影响“技巧”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **22.5 测试支持工具** 时，先故意破坏目标行为，确认测试确实由红转绿。先写预期，再运行正常值、恰好边界和一个故障；若出现“只写会通过的正常样例，或断言最终输出却不定位首差”，就在 第22章：开发者测试 的 **脚手架执行** 保存首个偏离、拒绝结果和复位后的重放证据。

### 为测试各个类构造脚手架

在 **第22章：开发者测试** 中，目录节点 **为测试各个类构造脚手架** 落在“覆盖观察”检查点。这里的技术含义是：用构造和公开操作守住对象不变量，优先组合稳定职责；它影响“典型错误”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **为测试各个类构造脚手架** 时，尝试绕过接口制造半有效对象，确认不变量不可被外部破坏。先写预期，再运行正常值、恰好边界和一个故障；若出现“只写会通过的正常样例，或断言最终输出却不定位首差”，就在 第22章：开发者测试 的 **覆盖观察** 保存首个偏离、拒绝结果和复位后的重放证据。

### Diff工具

在 **第22章：开发者测试** 中，目录节点 **Diff工具** 落在“回归记录”检查点。这里的技术含义是：使分支互斥且覆盖输入域，命名复杂谓词并明确默认路径；它影响“工具过程”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **Diff工具** 时，列出条件真值表和边界邻域，寻找重叠或没有归属的输入。先写预期，再运行正常值、恰好边界和一个故障；若出现“只写会通过的正常样例，或断言最终输出却不定位首差”，就在 第22章：开发者测试 的 **回归记录** 保存首个偏离、拒绝结果和复位后的重放证据。

### 测试数据生成器

在 **第22章：开发者测试** 中，目录节点 **测试数据生成器** 落在“测试目标”检查点。这里的技术含义是：从合同、等价类、边界和独立路径导出可重复断言；它影响“测试角色”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **测试数据生成器** 时，先故意破坏目标行为，确认测试确实由红转绿。先写预期，再运行正常值、恰好边界和一个故障；若出现“只写会通过的正常样例，或断言最终输出却不定位首差”，就在 第22章：开发者测试 的 **测试目标** 保存首个偏离、拒绝结果和复位后的重放证据。

### 覆盖率监视器

在 **第22章：开发者测试** 中，目录节点 **覆盖率监视器** 落在“用例设计”检查点。这里的技术含义是：从合同、等价类、边界和独立路径导出可重复断言；它影响“测试优先”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **覆盖率监视器** 时，先故意破坏目标行为，确认测试确实由红转绿。先写预期，再运行正常值、恰好边界和一个故障；若出现“只写会通过的正常样例，或断言最终输出却不定位首差”，就在 第22章：开发者测试 的 **用例设计** 保存首个偏离、拒绝结果和复位后的重放证据。

### 数据记录器/日志记录器

在 **第22章：开发者测试** 中，目录节点 **数据记录器/日志记录器** 落在“脚手架执行”检查点。这里的技术含义是：把“数据记录器/日志记录器”放回 开发者测试从需求和代码结构导出正常、边界、错误与独立路径用例，脚手架保证隔离、可重复和失败可诊断；它影响“技巧”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **数据记录器/日志记录器** 时，固定其余条件，只改变与“数据记录器/日志记录器”直接相连的一个输入或决定。先写预期，再运行正常值、恰好边界和一个故障；若出现“只写会通过的正常样例，或断言最终输出却不定位首差”，就在 第22章：开发者测试 的 **脚手架执行** 保存首个偏离、拒绝结果和复位后的重放证据。

### 符号调试工具

在 **第22章：开发者测试** 中，目录节点 **符号调试工具** 落在“覆盖观察”检查点。这里的技术含义是：稳定复现并用最小区分实验排除竞争假设；它影响“典型错误”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **符号调试工具** 时，保留相同输入，只改变一个怀疑条件并记录首差。先写预期，再运行正常值、恰好边界和一个故障；若出现“只写会通过的正常样例，或断言最终输出却不定位首差”，就在 第22章：开发者测试 的 **覆盖观察** 保存首个偏离、拒绝结果和复位后的重放证据。

### 系统干扰器

在 **第22章：开发者测试** 中，目录节点 **系统干扰器** 落在“回归记录”检查点。这里的技术含义是：把“系统干扰器”放回 开发者测试从需求和代码结构导出正常、边界、错误与独立路径用例，脚手架保证隔离、可重复和失败可诊断；它影响“工具过程”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **系统干扰器** 时，固定其余条件，只改变与“系统干扰器”直接相连的一个输入或决定。先写预期，再运行正常值、恰好边界和一个故障；若出现“只写会通过的正常样例，或断言最终输出却不定位首差”，就在 第22章：开发者测试 的 **回归记录** 保存首个偏离、拒绝结果和复位后的重放证据。

### 错误数据库

在 **第22章：开发者测试** 中，目录节点 **错误数据库** 落在“测试目标”检查点。这里的技术含义是：把“错误数据库”放回 开发者测试从需求和代码结构导出正常、边界、错误与独立路径用例，脚手架保证隔离、可重复和失败可诊断；它影响“测试角色”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **错误数据库** 时，固定其余条件，只改变与“错误数据库”直接相连的一个输入或决定。先写预期，再运行正常值、恰好边界和一个故障；若出现“只写会通过的正常样例，或断言最终输出却不定位首差”，就在 第22章：开发者测试 的 **测试目标** 保存首个偏离、拒绝结果和复位后的重放证据。

### 22.6 改善测试过程

在 **第22章：开发者测试** 中，目录节点 **22.6 改善测试过程** 落在“用例设计”检查点。这里的技术含义是：从合同、等价类、边界和独立路径导出可重复断言；它影响“测试优先”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **22.6 改善测试过程** 时，先故意破坏目标行为，确认测试确实由红转绿。先写预期，再运行正常值、恰好边界和一个故障；若出现“只写会通过的正常样例，或断言最终输出却不定位首差”，就在 第22章：开发者测试 的 **用例设计** 保存首个偏离、拒绝结果和复位后的重放证据。

### 有计划的测试

在 **第22章：开发者测试** 中，目录节点 **有计划的测试** 落在“脚手架执行”检查点。这里的技术含义是：从合同、等价类、边界和独立路径导出可重复断言；它影响“技巧”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **有计划的测试** 时，先故意破坏目标行为，确认测试确实由红转绿。先写预期，再运行正常值、恰好边界和一个故障；若出现“只写会通过的正常样例，或断言最终输出却不定位首差”，就在 第22章：开发者测试 的 **脚手架执行** 保存首个偏离、拒绝结果和复位后的重放证据。

### 重新测试（回归测试）

在 **第22章：开发者测试** 中，目录节点 **重新测试（回归测试）** 落在“覆盖观察”检查点。这里的技术含义是：从合同、等价类、边界和独立路径导出可重复断言；它影响“典型错误”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **重新测试（回归测试）** 时，先故意破坏目标行为，确认测试确实由红转绿。先写预期，再运行正常值、恰好边界和一个故障；若出现“只写会通过的正常样例，或断言最终输出却不定位首差”，就在 第22章：开发者测试 的 **覆盖观察** 保存首个偏离、拒绝结果和复位后的重放证据。

### 自动化测试

在 **第22章：开发者测试** 中，目录节点 **自动化测试** 落在“回归记录”检查点。这里的技术含义是：从合同、等价类、边界和独立路径导出可重复断言；它影响“工具过程”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **自动化测试** 时，先故意破坏目标行为，确认测试确实由红转绿。先写预期，再运行正常值、恰好边界和一个故障；若出现“只写会通过的正常样例，或断言最终输出却不定位首差”，就在 第22章：开发者测试 的 **回归记录** 保存首个偏离、拒绝结果和复位后的重放证据。

### 22.7 保留测试记录

在 **第22章：开发者测试** 中，目录节点 **22.7 保留测试记录** 落在“测试目标”检查点。这里的技术含义是：从合同、等价类、边界和独立路径导出可重复断言；它影响“测试角色”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **22.7 保留测试记录** 时，先故意破坏目标行为，确认测试确实由红转绿。先写预期，再运行正常值、恰好边界和一个故障；若出现“只写会通过的正常样例，或断言最终输出却不定位首差”，就在 第22章：开发者测试 的 **测试目标** 保存首个偏离、拒绝结果和复位后的重放证据。

### 个人测试记录

在 **第22章：开发者测试** 中，目录节点 **个人测试记录** 落在“用例设计”检查点。这里的技术含义是：从合同、等价类、边界和独立路径导出可重复断言；它影响“测试优先”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **个人测试记录** 时，先故意破坏目标行为，确认测试确实由红转绿。先写预期，再运行正常值、恰好边界和一个故障；若出现“只写会通过的正常样例，或断言最终输出却不定位首差”，就在 第22章：开发者测试 的 **用例设计** 保存首个偏离、拒绝结果和复位后的重放证据。

### 推荐读物

在 **第22章：开发者测试** 中，目录节点 **推荐读物** 落在“脚手架执行”检查点。这里的技术含义是：把“推荐读物”放回 开发者测试从需求和代码结构导出正常、边界、错误与独立路径用例，脚手架保证隔离、可重复和失败可诊断；它影响“技巧”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **推荐读物** 时，固定其余条件，只改变与“推荐读物”直接相连的一个输入或决定。先写预期，再运行正常值、恰好边界和一个故障；若出现“只写会通过的正常样例，或断言最终输出却不定位首差”，就在 第22章：开发者测试 的 **脚手架执行** 保存首个偏离、拒绝结果和复位后的重放证据。

### 测试

在 **第22章：开发者测试** 中，目录节点 **测试** 落在“覆盖观察”检查点。这里的技术含义是：从合同、等价类、边界和独立路径导出可重复断言；它影响“典型错误”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **测试** 时，先故意破坏目标行为，确认测试确实由红转绿。先写预期，再运行正常值、恰好边界和一个故障；若出现“只写会通过的正常样例，或断言最终输出却不定位首差”，就在 第22章：开发者测试 的 **覆盖观察** 保存首个偏离、拒绝结果和复位后的重放证据。

### 测试脚手架

在 **第22章：开发者测试** 中，目录节点 **测试脚手架** 落在“回归记录”检查点。这里的技术含义是：从合同、等价类、边界和独立路径导出可重复断言；它影响“工具过程”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **测试脚手架** 时，先故意破坏目标行为，确认测试确实由红转绿。先写预期，再运行正常值、恰好边界和一个故障；若出现“只写会通过的正常样例，或断言最终输出却不定位首差”，就在 第22章：开发者测试 的 **回归记录** 保存首个偏离、拒绝结果和复位后的重放证据。

### 测试优先的开发

在 **第22章：开发者测试** 中，目录节点 **测试优先的开发** 落在“测试目标”检查点。这里的技术含义是：从合同、等价类、边界和独立路径导出可重复断言；它影响“测试角色”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **测试优先的开发** 时，先故意破坏目标行为，确认测试确实由红转绿。先写预期，再运行正常值、恰好边界和一个故障；若出现“只写会通过的正常样例，或断言最终输出却不定位首差”，就在 第22章：开发者测试 的 **测试目标** 保存首个偏离、拒绝结果和复位后的重放证据。

### 相关标准

在 **第22章：开发者测试** 中，目录节点 **相关标准** 落在“用例设计”检查点。这里的技术含义是：把“相关标准”放回 开发者测试从需求和代码结构导出正常、边界、错误与独立路径用例，脚手架保证隔离、可重复和失败可诊断；它影响“测试优先”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **相关标准** 时，固定其余条件，只改变与“相关标准”直接相连的一个输入或决定。先写预期，再运行正常值、恰好边界和一个故障；若出现“只写会通过的正常样例，或断言最终输出却不定位首差”，就在 第22章：开发者测试 的 **用例设计** 保存首个偏离、拒绝结果和复位后的重放证据。

### 关键点

在 **第22章：开发者测试** 中，目录节点 **关键点** 落在“脚手架执行”检查点。这里的技术含义是：把本单元结论改写为带前提、反例和证据位置的决策规则；它影响“技巧”时，必须同时声明对象、输入、状态、输出和适用边界，不能用标题出现代替解释。

验证 **关键点** 时，构造规则不适用的最小反例并说明退出条件。先写预期，再运行正常值、恰好边界和一个故障；若出现“只写会通过的正常样例，或断言最终输出却不定位首差”，就在 第22章：开发者测试 的 **脚手架执行** 保存首个偏离、拒绝结果和复位后的重放证据。

## 最小可重放实现

```text
// 第22章：开发者测试：同一输入验证基线、故障和复位
const baseline = run_cc2e_22_developer_testing(fixedCase());
assertContract(baseline);
assertRejected(run_cc2e_22_developer_testing(faultCase()));
assert.deepEqual(resetAndRun(fixedCase()), baseline);
```

这段 第22章：开发者测试 验证草图表达独立教学合同，不复制原书代码。真实执行应保存输入、环境、节点轨迹、最终结果、拒绝理由和复位结果，使第二位读者能从干净状态重放。

## 专属因果实验

先预测改变“测试角色”或“测试优先”后，第22章：开发者测试 的哪一个节点最先变化。切换基线、边界和注入故障，运行并保存证据；最后点击“重置实验”，确认滑块、场景、节点、压力和结果文字全部恢复。

<Cc2e22DeveloperTestingMechanismLab />

## 故障诊断与误区

<Callout
  type="trap"
  title="误区：只写会通过的正常样例，或断言最终输出却不定位首差"
>
  这个错误会让 第22章：开发者测试
  无法从“运行过就是测试”稳定推进到“测试能稳定暴露指定故障”。诊断沿 测试目标 →
  用例设计 → 脚手架执行 → 覆盖观察 → 回归记录
  前进，只在第一次出现预期与实际不一致的节点停下，不用最终症状倒猜原因。
</Callout>

<Callout type="trap" title="误区：一个指标可以代替全部证据">
  第22章：开发者测试
  的单一数值可能被样本、缓存、默认值、测量窗口或偶然次序掩盖。至少同时保存机制轨迹、边界结果、故障拒绝和复位重放，任何一项缺失都不能用平均分补偿。
</Callout>

<Callout type="tip" title="恢复顺序">
  第22章：开发者测试 的恢复顺序是：还原输入和环境，清理派生状态，重建
  用例设计，重跑 脚手架执行，核对 回归记录，再注入同一故障确认拒绝可重复。
</Callout>

## 术语与边界

第22章：开发者测试 的五个操作术语是 <Term def="测试目标 是 第22章：开发者测试 中可观察、可失败且可复位的机制边界">测试目标</Term>、<Term def="用例设计 是 第22章：开发者测试 中可观察、可失败且可复位的机制边界">用例设计</Term>、<Term def="脚手架执行 是 第22章：开发者测试 中可观察、可失败且可复位的机制边界">脚手架执行</Term>、<Term def="覆盖观察 是 第22章：开发者测试 中可观察、可失败且可复位的机制边界">覆盖观察</Term>、<Term def="回归记录 是 第22章：开发者测试 中可观察、可失败且可复位的机制边界">回归记录</Term>。每个术语必须能指向实验控件、节点或状态文本；若只能指向目录标题，说明解释和验证仍未完成。

<Glossary>

<GlossaryItem term="测试目标">
  测试目标 是 第22章：开发者测试 的第 1
  个机制检查点；它的记录包含进入条件、状态变化、退出条件与故障表现。
</GlossaryItem>

<GlossaryItem term="用例设计">
  用例设计 是 第22章：开发者测试 的第 2
  个机制检查点；它的记录包含进入条件、状态变化、退出条件与故障表现。
</GlossaryItem>

<GlossaryItem term="脚手架执行">
  脚手架执行 是 第22章：开发者测试 的第 3
  个机制检查点；它的记录包含进入条件、状态变化、退出条件与故障表现。
</GlossaryItem>

<GlossaryItem term="覆盖观察">
  覆盖观察 是 第22章：开发者测试 的第 4
  个机制检查点；它的记录包含进入条件、状态变化、退出条件与故障表现。
</GlossaryItem>

<GlossaryItem term="回归记录">
  回归记录 是 第22章：开发者测试 的第 5
  个机制检查点；它的记录包含进入条件、状态变化、退出条件与故障表现。
</GlossaryItem>

</Glossary>

## 本页小结

第22章：开发者测试 的完成标准是能解释“开发者测试从需求和代码结构导出正常、边界、错误与独立路径用例，脚手架保证隔离、可重复和失败可诊断”，用核心合同推出预期，沿五个专属节点观察实际，诊断“只写会通过的正常样例，或断言最终输出却不定位首差”，并证明复位后同一输入重建基线轨迹。

<Exercises>

1. 第22章：开发者测试 的五节点链中，哪个状态最能区分“运行过就是测试”和“测试能稳定暴露指定故障”？请给出最小输入和恰好边界。

<Answer>
  先固定 测试目标，只改变与 用例设计 相连的一个条件；最早在 脚手架执行
  出现且能由合同预测的差异，就是区分两种状态的证据。边界值要同时记录接受或拒绝以及
  回归记录 的最终状态。
</Answer>

2. 使用 第22章：开发者测试 的核心合同做一次手算、代码断言、状态推演、故障诊断或方案比较，并说明每个量对应的真实对象。

<Answer>
  把输入值、单位、版本和观察窗口写入基线，先求预期，再与实验轨迹比较。若量纲、分母、对象身份或执行顺序发生变化，应重建模型；不能只修改阈值让本轮通过。
</Answer>

3. 如何逐项证明 第22章：开发者测试 的目录范围不只是标题出现？覆盖清单：第22章 开发者测试（测试目标）；22.1 开发者测试在软件质量中的角色（用例设计）；构造中测试（脚手架执行）；22.2 推荐的开发者测试方法（覆盖观察）；先测试还是后测试（回归记录）；开发者测试的局限性（测试目标）；22.3 测试技巧锦囊（用例设计）；不完整的测试（脚手架执行）；结构化的基础测试（覆盖观察）；数据流测试（回归记录）；等价类划分（测试目标）；猜测错误（用例设计）；边界值分析（脚手架执行）；几类坏数据（覆盖观察）；几类好数据（回归记录）；采用容易手工检查的测试用例（测试目标）；22.4 典型错误（用例设计）；哪些类包含最多的错误？（脚手架执行）；错误的分类（覆盖观察）；不完善的构造过程引发错误所占的比例（回归记录）；你期望能发现多少错误（测试目标）；测试本身的错误（用例设计）；22.5 测试支持工具（脚手架执行）；为测试各个类构造脚手架（覆盖观察）；Diff工具（回归记录）；测试数据生成器（测试目标）；覆盖率监视器（用例设计）；数据记录器/日志记录器（脚手架执行）；符号调试工具（覆盖观察）；系统干扰器（回归记录）；错误数据库（测试目标）；22.6 改善测试过程（用例设计）；有计划的测试（脚手架执行）；重新测试（回归测试）（覆盖观察）；自动化测试（回归记录）；22.7 保留测试记录（测试目标）；个人测试记录（用例设计）；推荐读物（脚手架执行）；测试（覆盖观察）；测试脚手架（回归记录）；测试优先的开发（测试目标）；相关标准（用例设计）；关键点（脚手架执行）。

<Answer>
  对清单每项找到正文解释，指出它位于五节点链的哪一步，在专属实验中改变一个直接变量，并写下正常、边界、故障与复位结果。四类证据任缺一类，该目录节点仍保持未通过。
</Answer>

</Exercises>

<Attribution
  adaptedFrom="《代码大全（第2版）》2006 中文版公开试读目录用于界定 第22章：开发者测试 的范围；技术讲解、机制图、实验、实现草图和练习依据 NIST SP 800-218 Secure Software Development Framework 独立重写；未使用或声称复现原书正文、插图、表格、核对表和代码"
  adaptedUrl="https://book.douban.com/reading/10145819/"
/>
