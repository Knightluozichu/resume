---
title: "第26章：代码调整方法"
type: B
section: "代码大全（第2版·2006中文版）"
order: 36
description: "在测量证明热点后，评估逻辑、循环、数据变换、表达式、子程序和低级语言技术，警惕编译器与平台变化使微优化失效。"
demo: true
math: true
sourceUrl: "https://book.douban.com/reading/10145819/"
draft: false
---

import {
  Cc2e26CodeTuningTechniquesStructureLab,
  Cc2e26CodeTuningTechniquesTestLab,
  Cc2e26CodeTuningTechniquesEvidenceLab,
} from "@/components/mdx/code-complete-2e/diagrams/cc2e-26-code-tuning-techniques";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第26章：代码调整方法在49个正式单元和685个目录节点中的职责与前后依赖
- 能绘制“已证热点 → 技术选择 → 单点改写 → 基准复测 → 可移植性审查”因果链，并实现最小代码或项目验证
- 能计算复杂度、缺陷检出、变更风险与反馈时间，并比较正常、边界、单故障
- 能设计独立复核并证明“能对同一热点比较至少两种调整技术，保存编译器、硬件和数据分布，证明收益稳定且没有破坏正确性。”

</Objectives>

## 为什么这一单元不可压缩

本页依据史蒂夫·迈克康奈尔《代码大全（第2版）》，金戈、汤凌、陈硕、张菲译，裘宗燕审校，电子工业出版社，2006年3月，ISBN 9787121022982的公开完整试读目录，独立重构 **第26章 代码调整方法**。在测量证明热点后，评估逻辑、循环、数据变换、表达式、子程序和低级语言技术，警惕编译器与平台变化使微优化失效。 本课程不复制原书正文、插图、表格、核对表或代码示例，而把目录主题转写成新的模型、代码实验、边界样本与复核证据。

中文版目录包含5个前置单元、7个部分、35章、参考文献和索引，共49个正式单元、685个目录节点。中文书目页登记944页；北京化工大学图书馆MARC登记“37,914页”，属于前置页与正文分页口径差异。课程以作者、译者、出版社、出版日期、ISBN和完整目录共同锁定版本，不因页数差异删减内容。

原版由Microsoft Press于2004年出版，共960页，ISBN 9780735619678。语言、工具和平台示例有历史边界，但管理复杂度、明确契约、开发者测试、增量集成和面向人阅读仍需在当前项目重新验证；现代框架名称不能倒灌成原书目录内容。

## 版次、对象与验收合同

<Term def="惰性求值是第26章：代码调整方法的第1个复核坐标，必须绑定代码对象、版本、输入、边界和可观察证据。">
  惰性求值
</Term>
、
<Term def="循环展开是第26章：代码调整方法的第2个复核坐标，必须绑定代码对象、版本、输入、边界和可观察证据。">
  循环展开
</Term>
、
<Term def="缓存是第26章：代码调整方法的第3个复核坐标，必须绑定代码对象、版本、输入、边界和可观察证据。">
  缓存
</Term>
、
<Term def="强度削弱是第26章：代码调整方法的第4个复核坐标，必须绑定代码对象、版本、输入、边界和可观察证据。">
  强度削弱
</Term>
、
<Term def="内联是第26章：代码调整方法的第5个复核坐标，必须绑定代码对象、版本、输入、边界和可观察证据。">
  内联
</Term>
共同构成本页验收坐标。每次实验固定需求、语言和编译器版本、输入数据、初始代码、测试与测量环境，只改变一个结构、类型、控制或过程条件。

<Callout type="info">
  **验收命题：**
  能对同一热点比较至少两种调整技术，保存编译器、硬件和数据分布，证明收益稳定且没有破坏正确性。
</Callout>

## 四个可核查模型

控制流复杂度用独立路径近似：

$$
V(G)=E-N+2P
$$

缺陷移除效能必须固定发现阶段和分母：

$$
DRE=removedBeforeRelease/(removedBeforeRelease+escaped)
$$

变更风险同时取决于传播范围、状态和测试缺口：

$$
risk=fanout * mutableState * uncoveredPaths
$$

发布采用乘法门禁，任一项缺失不能被平均分掩盖：

$$
release=contract * readableDesign * tests * integration * independentReview
$$

复杂度不是质量结论，覆盖率也不是正确性证明。公式输入必须来自同一版本和场景，并保存单位、采样窗口、阈值来源及无法测量的假设。

## 三段最小可运行骨架

```typescript
type ConstructionSnapshot = {
  edition: "2006-CN-2E";
  unit: "cc2e-26-code-tuning-techniques";
  sourceHash: string;
  paths: number;
  tests: string[];
  evidence: string[];
};

export function releaseGate(snapshot: ConstructionSnapshot) {
  return (
    snapshot.sourceHash.length > 0 &&
    snapshot.tests.length >= 3 &&
    snapshot.evidence.length >= 3
  );
}
```

```python
def boundary_cases(low, high):
    if low > high:
        raise ValueError("range")
    return [low - 1, low, low + 1, high - 1, high, high + 1]
```

```python
def apply_one_change(baseline, change, validate):
    if len(change) != 1:
        return {"accepted": False, "state": baseline, "reason": "not-single-change"}
    candidate = dict(baseline, **change)
    return ({"accepted": True, "state": candidate, "reason": None}
            if validate(candidate) else
            {"accepted": False, "state": baseline, "reason": "gate-rejected"})
```

第一段冻结源代码与证据，第二段系统产生边界邻域，第三段保证拒绝时不留下半更新状态。三段都要补充正常、恰好边界和单故障预期，失败时先比较首差而不是最终输出。

## 正式目录逐项讲解

### 第26章 代码调整方法

性能工作从目标负载和可重复基线开始，只优化测量证明的热点。每次改动后同时复测正确性、收益稳定性、可读性和平台依赖。

复核动作：把“第26章 代码调整方法”放入第26章 代码调整方法的“已证热点 → 技术选择 → 单点改写 → 基准复测 → 可移植性审查”链路，只改变一个结构、数据或工具条件，保存预期、实际、首差与回退，并让第二位读者重放。

### 26.1 逻辑

26.1 逻辑承担第26章 代码调整方法中的一个正式目录坐标。先说明它解决的构建问题、输入、输出、适用范围和与相邻主题的依赖，再把建议改写为可以失败的判断。

最小实验固定2006年中文第2版语义边界和同一代码基线，只改变一个条件，记录复杂度、测试、缺陷或反馈时间的首个变化；未覆盖的语言与项目差异必须显式保留。

### 在知道答案后停止判断

在知道答案后停止判断承担第26章 代码调整方法中的一个正式目录坐标。先说明它解决的构建问题、输入、输出、适用范围和与相邻主题的依赖，再把建议改写为可以失败的判断。

最小实验固定2006年中文第2版语义边界和同一代码基线，只改变一个条件，记录复杂度、测试、缺陷或反馈时间的首个变化；未覆盖的语言与项目差异必须显式保留。

### 按照出现频率来调整判断顺序

性能工作从目标负载和可重复基线开始，只优化测量证明的热点。每次改动后同时复测正确性、收益稳定性、可读性和平台依赖。

复核动作：把“按照出现频率来调整判断顺序”放入第26章 代码调整方法的“已证热点 → 技术选择 → 单点改写 → 基准复测 → 可移植性审查”链路，只改变一个结构、数据或工具条件，保存预期、实际、首差与回退，并让第二位读者重放。

### 相似逻辑结构之间的性能比较

数据表示必须明确范围、精度、编码、所有权、生命周期和非法状态。构造最小值、最大值、空值、越界和别名样本，验证类型选择真的约束错误。

复核动作：把“相似逻辑结构之间的性能比较”放入第26章 代码调整方法的“已证热点 → 技术选择 → 单点改写 → 基准复测 → 可移植性审查”链路，只改变一个结构、数据或工具条件，保存预期、实际、首差与回退，并让第二位读者重放。

### 用查找表替代复杂表达式

设计主题的核心是控制认知与变化传播：明确抽象层、隐藏决策、限制依赖并保留替代方案。比较方案时同时记录复杂度、可测试性和未来变更成本。

复核动作：把“用查找表替代复杂表达式”放入第26章 代码调整方法的“已证热点 → 技术选择 → 单点改写 → 基准复测 → 可移植性审查”链路，只改变一个结构、数据或工具条件，保存预期、实际、首差与回退，并让第二位读者重放。

### 使用惰性求值

使用惰性求值承担第26章 代码调整方法中的一个正式目录坐标。先说明它解决的构建问题、输入、输出、适用范围和与相邻主题的依赖，再把建议改写为可以失败的判断。

最小实验固定2006年中文第2版语义边界和同一代码基线，只改变一个条件，记录复杂度、测试、缺陷或反馈时间的首个变化；未覆盖的语言与项目差异必须显式保留。

### 26.2 循环

控制流要突出正常路径、退出条件和不变量。先画分支与循环状态，再减少否定、重复和深层嵌套；用零次、一次、边界和默认分支覆盖证明完整。

复核动作：把“26.2 循环”放入第26章 代码调整方法的“已证热点 → 技术选择 → 单点改写 → 基准复测 → 可移植性审查”链路，只改变一个结构、数据或工具条件，保存预期、实际、首差与回退，并让第二位读者重放。

### 将判断外提（Unswitching）

将判断外提（Unswitching）承担第26章 代码调整方法中的一个正式目录坐标。先说明它解决的构建问题、输入、输出、适用范围和与相邻主题的依赖，再把建议改写为可以失败的判断。

最小实验固定2006年中文第2版语义边界和同一代码基线，只改变一个条件，记录复杂度、测试、缺陷或反馈时间的首个变化；未覆盖的语言与项目差异必须显式保留。

### 合并循环

控制流要突出正常路径、退出条件和不变量。先画分支与循环状态，再减少否定、重复和深层嵌套；用零次、一次、边界和默认分支覆盖证明完整。

复核动作：把“合并循环”放入第26章 代码调整方法的“已证热点 → 技术选择 → 单点改写 → 基准复测 → 可移植性审查”链路，只改变一个结构、数据或工具条件，保存预期、实际、首差与回退，并让第二位读者重放。

### 展开

展开承担第26章 代码调整方法中的一个正式目录坐标。先说明它解决的构建问题、输入、输出、适用范围和与相邻主题的依赖，再把建议改写为可以失败的判断。

最小实验固定2006年中文第2版语义边界和同一代码基线，只改变一个条件，记录复杂度、测试、缺陷或反馈时间的首个变化；未覆盖的语言与项目差异必须显式保留。

### 尽可能减少再循环内部做的工作

控制流要突出正常路径、退出条件和不变量。先画分支与循环状态，再减少否定、重复和深层嵌套；用零次、一次、边界和默认分支覆盖证明完整。

复核动作：把“尽可能减少再循环内部做的工作”放入第26章 代码调整方法的“已证热点 → 技术选择 → 单点改写 → 基准复测 → 可移植性审查”链路，只改变一个结构、数据或工具条件，保存预期、实际、首差与回退，并让第二位读者重放。

### 哨兵值

哨兵值承担第26章 代码调整方法中的一个正式目录坐标。先说明它解决的构建问题、输入、输出、适用范围和与相邻主题的依赖，再把建议改写为可以失败的判断。

最小实验固定2006年中文第2版语义边界和同一代码基线，只改变一个条件，记录复杂度、测试、缺陷或反馈时间的首个变化；未覆盖的语言与项目差异必须显式保留。

### 把最忙的循环放在最内层

控制流要突出正常路径、退出条件和不变量。先画分支与循环状态，再减少否定、重复和深层嵌套；用零次、一次、边界和默认分支覆盖证明完整。

复核动作：把“把最忙的循环放在最内层”放入第26章 代码调整方法的“已证热点 → 技术选择 → 单点改写 → 基准复测 → 可移植性审查”链路，只改变一个结构、数据或工具条件，保存预期、实际、首差与回退，并让第二位读者重放。

### 削减强度

削减强度承担第26章 代码调整方法中的一个正式目录坐标。先说明它解决的构建问题、输入、输出、适用范围和与相邻主题的依赖，再把建议改写为可以失败的判断。

最小实验固定2006年中文第2版语义边界和同一代码基线，只改变一个条件，记录复杂度、测试、缺陷或反馈时间的首个变化；未覆盖的语言与项目差异必须显式保留。

### 26.3 数据变换

26.3 数据变换承担第26章 代码调整方法中的一个正式目录坐标。先说明它解决的构建问题、输入、输出、适用范围和与相邻主题的依赖，再把建议改写为可以失败的判断。

最小实验固定2006年中文第2版语义边界和同一代码基线，只改变一个条件，记录复杂度、测试、缺陷或反馈时间的首个变化；未覆盖的语言与项目差异必须显式保留。

### 使用整型数而不是浮点数

数据表示必须明确范围、精度、编码、所有权、生命周期和非法状态。构造最小值、最大值、空值、越界和别名样本，验证类型选择真的约束错误。

复核动作：把“使用整型数而不是浮点数”放入第26章 代码调整方法的“已证热点 → 技术选择 → 单点改写 → 基准复测 → 可移植性审查”链路，只改变一个结构、数据或工具条件，保存预期、实际、首差与回退，并让第二位读者重放。

### 数组维度尽可能少

数据表示必须明确范围、精度、编码、所有权、生命周期和非法状态。构造最小值、最大值、空值、越界和别名样本，验证类型选择真的约束错误。

复核动作：把“数组维度尽可能少”放入第26章 代码调整方法的“已证热点 → 技术选择 → 单点改写 → 基准复测 → 可移植性审查”链路，只改变一个结构、数据或工具条件，保存预期、实际、首差与回退，并让第二位读者重放。

### 尽可能减少数组引用

数据表示必须明确范围、精度、编码、所有权、生命周期和非法状态。构造最小值、最大值、空值、越界和别名样本，验证类型选择真的约束错误。

复核动作：把“尽可能减少数组引用”放入第26章 代码调整方法的“已证热点 → 技术选择 → 单点改写 → 基准复测 → 可移植性审查”链路，只改变一个结构、数据或工具条件，保存预期、实际、首差与回退，并让第二位读者重放。

### 使用辅助索引

反向入口的价值在于快速定位原始上下文。记录术语或编号、目标章节、版本、数据口径和交叉引用，不能脱离正文引用孤立条目。

复核动作：把“使用辅助索引”放入第26章 代码调整方法的“已证热点 → 技术选择 → 单点改写 → 基准复测 → 可移植性审查”链路，只改变一个结构、数据或工具条件，保存预期、实际、首差与回退，并让第二位读者重放。

### 使用缓存机制

性能工作从目标负载和可重复基线开始，只优化测量证明的热点。每次改动后同时复测正确性、收益稳定性、可读性和平台依赖。

复核动作：把“使用缓存机制”放入第26章 代码调整方法的“已证热点 → 技术选择 → 单点改写 → 基准复测 → 可移植性审查”链路，只改变一个结构、数据或工具条件，保存预期、实际、首差与回退，并让第二位读者重放。

### 26.4 表达式

26.4 表达式承担第26章 代码调整方法中的一个正式目录坐标。先说明它解决的构建问题、输入、输出、适用范围和与相邻主题的依赖，再把建议改写为可以失败的判断。

最小实验固定2006年中文第2版语义边界和同一代码基线，只改变一个条件，记录复杂度、测试、缺陷或反馈时间的首个变化；未覆盖的语言与项目差异必须显式保留。

### 利用代数恒等式

利用代数恒等式承担第26章 代码调整方法中的一个正式目录坐标。先说明它解决的构建问题、输入、输出、适用范围和与相邻主题的依赖，再把建议改写为可以失败的判断。

最小实验固定2006年中文第2版语义边界和同一代码基线，只改变一个条件，记录复杂度、测试、缺陷或反馈时间的首个变化；未覆盖的语言与项目差异必须显式保留。

### 削弱运算强度

削弱运算强度承担第26章 代码调整方法中的一个正式目录坐标。先说明它解决的构建问题、输入、输出、适用范围和与相邻主题的依赖，再把建议改写为可以失败的判断。

最小实验固定2006年中文第2版语义边界和同一代码基线，只改变一个条件，记录复杂度、测试、缺陷或反馈时间的首个变化；未覆盖的语言与项目差异必须显式保留。

### 编译时初始化

变量应尽晚定义、可靠初始化、局部使用并尽早结束生命周期。追踪定义到最后使用的跨度，消除隐式状态和一变量多用途。

复核动作：把“编译时初始化”放入第26章 代码调整方法的“已证热点 → 技术选择 → 单点改写 → 基准复测 → 可移植性审查”链路，只改变一个结构、数据或工具条件，保存预期、实际、首差与回退，并让第二位读者重放。

### 小心系统函数

代码单元应表达一个一致职责和稳定契约。检查调用者需要知道什么、哪些状态必须隐藏、参数和返回怎样约束，以及替换实现时哪些测试保持不变。

复核动作：把“小心系统函数”放入第26章 代码调整方法的“已证热点 → 技术选择 → 单点改写 → 基准复测 → 可移植性审查”链路，只改变一个结构、数据或工具条件，保存预期、实际、首差与回退，并让第二位读者重放。

### 使用正确的常量类型

代码单元应表达一个一致职责和稳定契约。检查调用者需要知道什么、哪些状态必须隐藏、参数和返回怎样约束，以及替换实现时哪些测试保持不变。

复核动作：把“使用正确的常量类型”放入第26章 代码调整方法的“已证热点 → 技术选择 → 单点改写 → 基准复测 → 可移植性审查”链路，只改变一个结构、数据或工具条件，保存预期、实际、首差与回退，并让第二位读者重放。

### 预先算出结果

预先算出结果承担第26章 代码调整方法中的一个正式目录坐标。先说明它解决的构建问题、输入、输出、适用范围和与相邻主题的依赖，再把建议改写为可以失败的判断。

最小实验固定2006年中文第2版语义边界和同一代码基线，只改变一个条件，记录复杂度、测试、缺陷或反馈时间的首个变化；未覆盖的语言与项目差异必须显式保留。

### 删除公共子表达式

删除公共子表达式承担第26章 代码调整方法中的一个正式目录坐标。先说明它解决的构建问题、输入、输出、适用范围和与相邻主题的依赖，再把建议改写为可以失败的判断。

最小实验固定2006年中文第2版语义边界和同一代码基线，只改变一个条件，记录复杂度、测试、缺陷或反馈时间的首个变化；未覆盖的语言与项目差异必须显式保留。

### 26.5 子程序

代码单元应表达一个一致职责和稳定契约。检查调用者需要知道什么、哪些状态必须隐藏、参数和返回怎样约束，以及替换实现时哪些测试保持不变。

复核动作：把“26.5 子程序”放入第26章 代码调整方法的“已证热点 → 技术选择 → 单点改写 → 基准复测 → 可移植性审查”链路，只改变一个结构、数据或工具条件，保存预期、实际、首差与回退，并让第二位读者重放。

### 将函数重写为内联

代码单元应表达一个一致职责和稳定契约。检查调用者需要知道什么、哪些状态必须隐藏、参数和返回怎样约束，以及替换实现时哪些测试保持不变。

复核动作：把“将函数重写为内联”放入第26章 代码调整方法的“已证热点 → 技术选择 → 单点改写 → 基准复测 → 可移植性审查”链路，只改变一个结构、数据或工具条件，保存预期、实际、首差与回退，并让第二位读者重放。

### 26.6 用低级语言重写代码

性能工作从目标负载和可重复基线开始，只优化测量证明的热点。每次改动后同时复测正确性、收益稳定性、可读性和平台依赖。

复核动作：把“26.6 用低级语言重写代码”放入第26章 代码调整方法的“已证热点 → 技术选择 → 单点改写 → 基准复测 → 可移植性审查”链路，只改变一个结构、数据或工具条件，保存预期、实际、首差与回退，并让第二位读者重放。

### 26.7 变得越多，事情反而更没变

26.7 变得越多，事情反而更没变承担第26章 代码调整方法中的一个正式目录坐标。先说明它解决的构建问题、输入、输出、适用范围和与相邻主题的依赖，再把建议改写为可以失败的判断。

最小实验固定2006年中文第2版语义边界和同一代码基线，只改变一个条件，记录复杂度、测试、缺陷或反馈时间的首个变化；未覆盖的语言与项目差异必须显式保留。

### 推荐读物

扩展资料必须按问题、版本与证据类型选择。建立“当前缺口—来源—实践任务—复核产物”链接，避免收集书目却不改变构建能力。

复核动作：把“推荐读物”放入第26章 代码调整方法的“已证热点 → 技术选择 → 单点改写 → 基准复测 → 可移植性审查”链路，只改变一个结构、数据或工具条件，保存预期、实际、首差与回退，并让第二位读者重放。

### 关键点

关键点用于把本单元收束为少量可执行判断。复核时把每条判断改写成带对象、前置条件、失败样本和证据位置的门禁，不能只背结论。

复核动作：把“关键点”放入第26章 代码调整方法的“已证热点 → 技术选择 → 单点改写 → 基准复测 → 可移植性审查”链路，只改变一个结构、数据或工具条件，保存预期、实际、首差与回退，并让第二位读者重放。

## 从目录建议到构建因果链

先画五节点链：已证热点 → 技术选择 → 单点改写 → 基准复测 → 可移植性审查。每条边注明传递的是需求、抽象、类型、状态、控制、构件、缺陷还是证据；每个节点写出进入条件、退出条件、所有者和失败回退。没有对象或状态变化的箭头只是排版连接。

再以逻辑、循环、数据、表达式、低级改写作为检查点。正常样本证明主路径可运行；边界样本选择恰好类型范围、控制端点、复杂度阈值、测试覆盖或构建容量；单故障样本只改变一个输入、分支、依赖、工具或构件。三类样本共享其余条件，才能归因。

书中示例语言与当代项目不同时，先保留原建议解决的问题，再比较类型系统、内存模型、工具自动化和发布环境。后续语言特性可以降低次要负担，但不能替代领域建模、接口选择和失败验证。

## 最小证据包与独立复核

证据包包含ISBN与目录坐标、需求与代码哈希、语言和工具版本、输入数据、复杂度、静态分析、测试设计、失败日志、性能基线、集成轨迹、回退状态和结论。覆盖率、格式化结果或最终截图只能辅助定位，不能单独证明质量。

独立复核者只接收冻结输入、代码基线和验收条件，不接收原作者的操作顺序或期待答案。复核者先预测五个节点，再运行同一数据；结果不一致时比较首个分叉、原始日志和环境差异。

## 跨语言与时代迁移审查

把2006年的构建建议迁移到当前语言时，先保存原建议解决的风险，再分别记录今天由类型系统、标准库、编译器、静态分析、测试框架或持续集成自动承担的部分。自动化已经覆盖的机械步骤可以缩减，但它的配置、盲区和失败模式必须进入证据包；仍涉及领域抽象、接口语义、数据边界和取舍的部分，不能交给工具默认值静默决定。

迁移记录至少比较四栏：原版问题、当前机制、仍存风险、验证样本。若一种现代特性声称让某条建议“过时”，就构造关闭该特性、误配该特性和越过其检查边界的样本；只有能说明风险确实被消除而非转移，才可调整学习权重。这样既不机械照搬旧语言技巧，也不因工具更新而丢失软件构建的因果问题。

## 构建验证卡

每次应用目录建议都填写同一张验证卡：问题必须可失败；基线必须包含版本、输入和测量；改动一次只改变一个变量；证据要能定位首差；复核由未参与实现的人完成。缺少任一栏，结论只能保留为待验证假设。

| 字段 | 必答问题                     | 拒绝条件                   |
| ---- | ---------------------------- | -------------------------- |
| 问题 | 哪个构建风险需要降低         | 只有风格偏好，没有失败条件 |
| 基线 | 代码、输入、工具和指标是什么 | 分母、单位或版本不一致     |
| 改动 | 唯一改变了哪个结构或过程     | 同时重写、调优和换工具     |
| 证据 | 首差、测试、性能与回退在哪里 | 只有最终成功截图           |
| 复核 | 谁在相同输入下重建结果       | 依赖实现者操作提示         |

## 正常、边界与单故障矩阵

| 样本   | 只改变的变量                   | 预期结果                     | 必存证据                   |
| ------ | ------------------------------ | ---------------------------- | -------------------------- |
| 正常   | 合法输入、清晰契约和稳定工具链 | 主路径闭合且指标有余量       | 代码哈希、输入、测试与轨迹 |
| 边界   | 恰好范围、端点、复杂度或容量   | 明确接受或拒绝，不产生半状态 | 阈值、分支、原状态与断言   |
| 单故障 | 一个输入、依赖、分支或构件失败 | 在首个异常边界隔离并回退     | 首差、错误策略、恢复与回归 |

操作前先预测。若一次同时重构、调优、换语言和升级工具，就无法判断改进来源，必须重置基线。

## 三步交互实验

<Stepper>
  <Step title="追踪结构、数据与控制边界">
    选择节点，检查对象、状态、决定和证据怎样跨边界传播。
    <Cc2e26CodeTuningTechniquesStructureLab />
  </Step>
  <Step title="调整复杂度、测试与变更范围">
    一次只改变一个参数，观察认知负担、缺陷暴露和变更风险。
    <Cc2e26CodeTuningTechniquesTestLab />
  </Step>
  <Step title="注入一个故障并保存证据">
    比较正常、恰好边界与单故障，要求门禁拒绝无证据结论。
    <Cc2e26CodeTuningTechniquesEvidenceLab />
  </Step>
</Stepper>

## 常见失败与回退

<Callout type="trap">
  **把规则变成风格口号：**
  没有问题、边界和反例的“最佳实践”无法裁决。回到代码基线，说明风险、唯一改动与可观察结果。
</Callout>

<Callout type="trap">
  **用现代工具覆盖原问题：**
  自动格式化、类型检查和生成式工具能减少部分机械工作，却不自动保证抽象、契约和测试。记录工具解决了什么，以及仍需人判断什么。
</Callout>

## 本章回顾

在测量证明热点后，评估逻辑、循环、数据变换、表达式、子程序和低级语言技术，警惕编译器与平台变化使微优化失效。 完成“能对同一热点比较至少两种调整技术，保存编译器、硬件和数据分布，证明收益稳定且没有破坏正确性。”后，还要让独立复核者在相同边界重建结构、测试、首差和回退。一次通过不能代表所有输入，能解释何时失败和怎样恢复才是可迁移的构建知识。

<Glossary>

<GlossaryItem term="惰性求值">
  惰性求值用于定位第26章：代码调整方法中的第1类结构、数据、质量或过程变量；使用时必须写出适用边界、失败条件和复核方法。
</GlossaryItem>

<GlossaryItem term="循环展开">
  循环展开用于定位第26章：代码调整方法中的第2类结构、数据、质量或过程变量；使用时必须写出适用边界、失败条件和复核方法。
</GlossaryItem>

<GlossaryItem term="缓存">
  缓存用于定位第26章：代码调整方法中的第3类结构、数据、质量或过程变量；使用时必须写出适用边界、失败条件和复核方法。
</GlossaryItem>

<GlossaryItem term="强度削弱">
  强度削弱用于定位第26章：代码调整方法中的第4类结构、数据、质量或过程变量；使用时必须写出适用边界、失败条件和复核方法。
</GlossaryItem>

<GlossaryItem term="内联">
  内联用于定位第26章：代码调整方法中的第5类结构、数据、质量或过程变量；使用时必须写出适用边界、失败条件和复核方法。
</GlossaryItem>

</Glossary>

<Exercises>

1. 如何为第26章：代码调整方法建立最小可重放基线？

<Answer>
  固定2006年中文第2版目录坐标、需求、代码哈希、语言与工具、输入和测试，先画“已证热点
  → 技术选择 → 单点改写 → 基准复测 →
  可移植性审查”预期，再保存正常、边界与单故障轨迹。
</Answer>

2. 怎样证明“能对同一热点比较至少两种调整技术，保存编译器、硬件和数据分布，证明收益稳定且没有破坏正确性。”不是一次偶然成功？

<Answer>
  先手算复杂度、边界、缺陷检出或反馈预算，再让独立复核者只依据输入和验收合同重建；双方必须在相同首差拒绝故障，并保留相同回退状态。
</Answer>

3. 修复一个失败后怎样回归？

<Answer>
  先重放原失败，再运行相邻边界和正常基线；逐节点检查契约、类型、控制、测试、工具与恢复证据，全部通过后才进入下一页。
</Answer>

</Exercises>

## 前后导航

- [上一页：第25章：代码调整策略](/learn/code-complete-2e/35-code-tuning-strategies/cc2e-25-code-tuning-strategies)
- [下一页：第6部分：系统考虑](/learn/code-complete-2e/37-system-considerations/cc2e-part-06-system-considerations)

<Attribution
  adaptedFrom="史蒂夫·迈克康奈尔《代码大全（第2版）》，金戈、汤凌、陈硕、张菲译，裘宗燕审校，电子工业出版社，2006年3月，ISBN 9787121022982 · 公开试读目录含5个前置单元、7个部分、35章、参考文献与索引共49个正式单元和685个目录节点 · 基于公开目录独立中文教学重构，不复制原书正文、插图、表格、核对表或代码示例"
  adaptedUrl="https://book.douban.com/reading/10145819/"
/>
