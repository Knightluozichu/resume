---
title: "第7部分：软件工艺"
type: B
section: "代码大全（第2版·2006中文版）"
order: 42
description: "从布局、自说明代码和个人性格上升到软件工艺原则与持续学习，连接代码可读性、专业判断和成长。"
demo: true
math: true
sourceUrl: "https://book.douban.com/reading/10145819/"
draft: false
---

import {
  Cc2ePart07SoftwareCraftsmanshipStructureLab,
  Cc2ePart07SoftwareCraftsmanshipTestLab,
  Cc2ePart07SoftwareCraftsmanshipEvidenceLab,
} from "@/components/mdx/code-complete-2e/diagrams/cc2e-part-07-software-craftsmanship";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第7部分：软件工艺在49个正式单元和685个目录节点中的职责与前后依赖
- 能绘制“代码布局 → 说明意图 → 个人习惯 → 工艺判断 → 持续学习”因果链，并实现最小代码或项目验证
- 能计算复杂度、缺陷检出、变更风险与反馈时间，并比较正常、边界、单故障
- 能设计独立复核并证明“能用第31至35章建立代码风格、文档、个人实践和持续学习的共同改进计划。”

</Objectives>

## 为什么这一单元不可压缩

本页依据史蒂夫·迈克康奈尔《代码大全（第2版）》，金戈、汤凌、陈硕、张菲译，裘宗燕审校，电子工业出版社，2006年3月，ISBN 9787121022982的公开完整试读目录，独立重构 **第7部分 软件工艺**。从布局、自说明代码和个人性格上升到软件工艺原则与持续学习，连接代码可读性、专业判断和成长。 本课程不复制原书正文、插图、表格、核对表或代码示例，而把目录主题转写成新的模型、代码实验、边界样本与复核证据。

中文版目录包含5个前置单元、7个部分、35章、参考文献和索引，共49个正式单元、685个目录节点。中文书目页登记944页；北京化工大学图书馆MARC登记“37,914页”，属于前置页与正文分页口径差异。课程以作者、译者、出版社、出版日期、ISBN和完整目录共同锁定版本，不因页数差异删减内容。

原版由Microsoft Press于2004年出版，共960页，ISBN 9780735619678。语言、工具和平台示例有历史边界，但管理复杂度、明确契约、开发者测试、增量集成和面向人阅读仍需在当前项目重新验证；现代框架名称不能倒灌成原书目录内容。

## 版次、对象与验收合同

<Term def="布局是第7部分：软件工艺的第1个复核坐标，必须绑定代码对象、版本、输入、边界和可观察证据。">
  布局
</Term>
、
<Term def="自说明代码是第7部分：软件工艺的第2个复核坐标，必须绑定代码对象、版本、输入、边界和可观察证据。">
  自说明代码
</Term>
、
<Term def="个人性格是第7部分：软件工艺的第3个复核坐标，必须绑定代码对象、版本、输入、边界和可观察证据。">
  个人性格
</Term>
、
<Term def="软件工艺是第7部分：软件工艺的第4个复核坐标，必须绑定代码对象、版本、输入、边界和可观察证据。">
  软件工艺
</Term>
、
<Term def="学习计划是第7部分：软件工艺的第5个复核坐标，必须绑定代码对象、版本、输入、边界和可观察证据。">
  学习计划
</Term>
共同构成本页验收坐标。每次实验固定需求、语言和编译器版本、输入数据、初始代码、测试与测量环境，只改变一个结构、类型、控制或过程条件。

<Callout type="info">
  **验收命题：**
  能用第31至35章建立代码风格、文档、个人实践和持续学习的共同改进计划。
</Callout>

## 四个可核查模型

控制流复杂度用独立路径近似：

$$
V(G)=E-N+2P
$$

缺陷移除效能必须固定发现阶段和分母：

$$
DRE=removedBeforeRelease/(removedBeforeRelease+escaped)
$$

变更风险同时取决于传播范围、状态和测试缺口：

$$
risk=fanout * mutableState * uncoveredPaths
$$

发布采用乘法门禁，任一项缺失不能被平均分掩盖：

$$
release=contract * readableDesign * tests * integration * independentReview
$$

复杂度不是质量结论，覆盖率也不是正确性证明。公式输入必须来自同一版本和场景，并保存单位、采样窗口、阈值来源及无法测量的假设。

## 三段最小可运行骨架

```typescript
type ConstructionSnapshot = {
  edition: "2006-CN-2E";
  unit: "cc2e-part-07-software-craftsmanship";
  sourceHash: string;
  paths: number;
  tests: string[];
  evidence: string[];
};

export function releaseGate(snapshot: ConstructionSnapshot) {
  return (
    snapshot.sourceHash.length > 0 &&
    snapshot.tests.length >= 3 &&
    snapshot.evidence.length >= 3
  );
}
```

```python
def boundary_cases(low, high):
    if low > high:
        raise ValueError("range")
    return [low - 1, low, low + 1, high - 1, high, high + 1]
```

```python
def apply_one_change(baseline, change, validate):
    if len(change) != 1:
        return {"accepted": False, "state": baseline, "reason": "not-single-change"}
    candidate = dict(baseline, **change)
    return ({"accepted": True, "state": candidate, "reason": None}
            if validate(candidate) else
            {"accepted": False, "state": baseline, "reason": "gate-rejected"})
```

第一段冻结源代码与证据，第二段系统产生边界邻域，第三段保证拒绝时不留下半更新状态。三段都要补充正常、恰好边界和单故障预期，失败时先比较首差而不是最终输出。

## 正式目录逐项讲解

### 第7部分 软件工艺

软件工艺不是固定教条，而是在原则、情境和实验之间作有证据的判断。记录被放弃方案、折中成本和改变决定的条件。

复核动作：把“第7部分 软件工艺”放入第7部分 软件工艺的“代码布局 → 说明意图 → 个人习惯 → 工艺判断 → 持续学习”链路，只改变一个结构、数据或工具条件，保存预期、实际、首差与回退，并让第二位读者重放。

## 从目录建议到构建因果链

先画五节点链：代码布局 → 说明意图 → 个人习惯 → 工艺判断 → 持续学习。每条边注明传递的是需求、抽象、类型、状态、控制、构件、缺陷还是证据；每个节点写出进入条件、退出条件、所有者和失败回退。没有对象或状态变化的箭头只是排版连接。

再以风格、文档、品格、工艺、资料作为检查点。正常样本证明主路径可运行；边界样本选择恰好类型范围、控制端点、复杂度阈值、测试覆盖或构建容量；单故障样本只改变一个输入、分支、依赖、工具或构件。三类样本共享其余条件，才能归因。

书中示例语言与当代项目不同时，先保留原建议解决的问题，再比较类型系统、内存模型、工具自动化和发布环境。后续语言特性可以降低次要负担，但不能替代领域建模、接口选择和失败验证。

## 最小证据包与独立复核

证据包包含ISBN与目录坐标、需求与代码哈希、语言和工具版本、输入数据、复杂度、静态分析、测试设计、失败日志、性能基线、集成轨迹、回退状态和结论。覆盖率、格式化结果或最终截图只能辅助定位，不能单独证明质量。

独立复核者只接收冻结输入、代码基线和验收条件，不接收原作者的操作顺序或期待答案。复核者先预测五个节点，再运行同一数据；结果不一致时比较首个分叉、原始日志和环境差异。

## 跨语言与时代迁移审查

把2006年的构建建议迁移到当前语言时，先保存原建议解决的风险，再分别记录今天由类型系统、标准库、编译器、静态分析、测试框架或持续集成自动承担的部分。自动化已经覆盖的机械步骤可以缩减，但它的配置、盲区和失败模式必须进入证据包；仍涉及领域抽象、接口语义、数据边界和取舍的部分，不能交给工具默认值静默决定。

迁移记录至少比较四栏：原版问题、当前机制、仍存风险、验证样本。若一种现代特性声称让某条建议“过时”，就构造关闭该特性、误配该特性和越过其检查边界的样本；只有能说明风险确实被消除而非转移，才可调整学习权重。这样既不机械照搬旧语言技巧，也不因工具更新而丢失软件构建的因果问题。

## 构建验证卡

每次应用目录建议都填写同一张验证卡：问题必须可失败；基线必须包含版本、输入和测量；改动一次只改变一个变量；证据要能定位首差；复核由未参与实现的人完成。缺少任一栏，结论只能保留为待验证假设。

| 字段 | 必答问题                     | 拒绝条件                   |
| ---- | ---------------------------- | -------------------------- |
| 问题 | 哪个构建风险需要降低         | 只有风格偏好，没有失败条件 |
| 基线 | 代码、输入、工具和指标是什么 | 分母、单位或版本不一致     |
| 改动 | 唯一改变了哪个结构或过程     | 同时重写、调优和换工具     |
| 证据 | 首差、测试、性能与回退在哪里 | 只有最终成功截图           |
| 复核 | 谁在相同输入下重建结果       | 依赖实现者操作提示         |

## 正常、边界与单故障矩阵

| 样本   | 只改变的变量                   | 预期结果                     | 必存证据                   |
| ------ | ------------------------------ | ---------------------------- | -------------------------- |
| 正常   | 合法输入、清晰契约和稳定工具链 | 主路径闭合且指标有余量       | 代码哈希、输入、测试与轨迹 |
| 边界   | 恰好范围、端点、复杂度或容量   | 明确接受或拒绝，不产生半状态 | 阈值、分支、原状态与断言   |
| 单故障 | 一个输入、依赖、分支或构件失败 | 在首个异常边界隔离并回退     | 首差、错误策略、恢复与回归 |

操作前先预测。若一次同时重构、调优、换语言和升级工具，就无法判断改进来源，必须重置基线。

## 三步交互实验

<Stepper>
  <Step title="追踪结构、数据与控制边界">
    选择节点，检查对象、状态、决定和证据怎样跨边界传播。
    <Cc2ePart07SoftwareCraftsmanshipStructureLab />
  </Step>
  <Step title="调整复杂度、测试与变更范围">
    一次只改变一个参数，观察认知负担、缺陷暴露和变更风险。
    <Cc2ePart07SoftwareCraftsmanshipTestLab />
  </Step>
  <Step title="注入一个故障并保存证据">
    比较正常、恰好边界与单故障，要求门禁拒绝无证据结论。
    <Cc2ePart07SoftwareCraftsmanshipEvidenceLab />
  </Step>
</Stepper>

## 常见失败与回退

<Callout type="trap">
  **把规则变成风格口号：**
  没有问题、边界和反例的“最佳实践”无法裁决。回到代码基线，说明风险、唯一改动与可观察结果。
</Callout>

<Callout type="trap">
  **用现代工具覆盖原问题：**
  自动格式化、类型检查和生成式工具能减少部分机械工作，却不自动保证抽象、契约和测试。记录工具解决了什么，以及仍需人判断什么。
</Callout>

## 本章回顾

从布局、自说明代码和个人性格上升到软件工艺原则与持续学习，连接代码可读性、专业判断和成长。 完成“能用第31至35章建立代码风格、文档、个人实践和持续学习的共同改进计划。”后，还要让独立复核者在相同边界重建结构、测试、首差和回退。一次通过不能代表所有输入，能解释何时失败和怎样恢复才是可迁移的构建知识。

<Glossary>

<GlossaryItem term="布局">
  布局用于定位第7部分：软件工艺中的第1类结构、数据、质量或过程变量；使用时必须写出适用边界、失败条件和复核方法。
</GlossaryItem>

<GlossaryItem term="自说明代码">
  自说明代码用于定位第7部分：软件工艺中的第2类结构、数据、质量或过程变量；使用时必须写出适用边界、失败条件和复核方法。
</GlossaryItem>

<GlossaryItem term="个人性格">
  个人性格用于定位第7部分：软件工艺中的第3类结构、数据、质量或过程变量；使用时必须写出适用边界、失败条件和复核方法。
</GlossaryItem>

<GlossaryItem term="软件工艺">
  软件工艺用于定位第7部分：软件工艺中的第4类结构、数据、质量或过程变量；使用时必须写出适用边界、失败条件和复核方法。
</GlossaryItem>

<GlossaryItem term="学习计划">
  学习计划用于定位第7部分：软件工艺中的第5类结构、数据、质量或过程变量；使用时必须写出适用边界、失败条件和复核方法。
</GlossaryItem>

</Glossary>

<Exercises>

1. 如何为第7部分：软件工艺建立最小可重放基线？

<Answer>
  固定2006年中文第2版目录坐标、需求、代码哈希、语言与工具、输入和测试，先画“代码布局
  → 说明意图 → 个人习惯 → 工艺判断 →
  持续学习”预期，再保存正常、边界与单故障轨迹。
</Answer>

2. 怎样证明“能用第31至35章建立代码风格、文档、个人实践和持续学习的共同改进计划。”不是一次偶然成功？

<Answer>
  先手算复杂度、边界、缺陷检出或反馈预算，再让独立复核者只依据输入和验收合同重建；双方必须在相同首差拒绝故障，并保留相同回退状态。
</Answer>

3. 修复一个失败后怎样回归？

<Answer>
  先重放原失败，再运行相邻边界和正常基线；逐节点检查契约、类型、控制、测试、工具与恢复证据，全部通过后才进入下一页。
</Answer>

</Exercises>

## 前后导航

- [上一页：第30章：编程工具](/learn/code-complete-2e/41-programming-tools/cc2e-30-programming-tools)
- [下一页：第31章：布局与风格](/learn/code-complete-2e/43-layout-and-style/cc2e-31-layout-and-style)

<Attribution
  adaptedFrom="史蒂夫·迈克康奈尔《代码大全（第2版）》，金戈、汤凌、陈硕、张菲译，裘宗燕审校，电子工业出版社，2006年3月，ISBN 9787121022982 · 公开试读目录含5个前置单元、7个部分、35章、参考文献与索引共49个正式单元和685个目录节点 · 基于公开目录独立中文教学重构，不复制原书正文、插图、表格、核对表或代码示例"
  adaptedUrl="https://book.douban.com/reading/10145819/"
/>
