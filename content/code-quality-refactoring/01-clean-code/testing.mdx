---
title: 单元测试
type: A
section: 整洁代码
order: 6
description: 测试金字塔、AAA 模式与 TDD 红绿重构——为什么测试是重构的安全网。
demo: true
math: false
sourceUrl: >-
  https://www.pearson.com/en-us/subject-catalog/p/clean-code-a-handbook-of-agile-software-craftsmanship/P200000009044/9780132350884
draft: false
---

import { CqrTestPyramidDiagram } from "@/components/mdx/code-quality/diagrams/test-pyramid-diagram";
import {
  Objectives,
  Callout,
  Attribution,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释单元测试的输入、核心结构、状态变化和输出合同
- 能绘制并修改单元测试的最小模型，比较至少一种替代方案
- 能复现正常、边界和单故障样本，定位第一条证据分叉
- 能根据成本与风险判断方案适用范围，并完成清理重建

</Objectives>

## 为什么只记结论不足以掌握单元测试

学习“单元测试”时，第一步不是记住结论，而是冻结输入、上下文、版本和成功标准。只有这些条件明确，单元测试的含义才不会随着样例变化。正文已有的概念说明要与结构图、运行轨迹和失败样本互相印证，不能只凭最终输出看似正确就宣布完成。

结构分析从痛点场景：没有测试，改代码像拆炸弹开始：列出参与者、职责、连接方向、生命周期和所有权，再沿正常路径追踪数据或控制流。每一条边都要说明为什么存在、谁创建、谁消费、谁负责清理；如果边界被跨越，必须能在证据中找到第一处异常。

机制验证要把解决方案：测试金字塔写成可以执行的条件。正常样本证明主路径，恰好边界样本验证等号和空值，单故障样本只破坏一个假设。三类样本使用同一份观察指标，避免因为测试口径变化而把偶然结果误认成规律。

成本分析同时记录时间、空间、延迟、耦合、可维护性和不可逆操作。AAA 模式：一个测试的三段式不是一句“可能失败”，而是可复现输入、预期停点、实际轨迹、错误分类与清理步骤。任何自动重试都要有次数、预算和幂等边界。

方案比较不能只列优点。需要给出直接实现、当前方案和至少一个替代方案，逐项比较复杂度、扩展点、故障隔离和团队认知成本。当问题规模很小或变化轴稳定时，更简单的实现往往更好；模式与框架必须由真实变化压力证明。

实现阶段把大结论拆成可检查的中间产物：配置快照、结构清单、状态转移、输入输出样本、日志摘要和测试结果。每个产物带来源与生成命令，下一阶段只消费已通过门禁的版本，避免旧缓存或隐式默认值污染结论。

解释结果时必须区分相关性与因果性、接口承诺与实现细节、设计意图与运行事实。对“TDD：红绿重构”的判断要由独立证据支持，并明确适用范围；一旦输入分布、版本、硬件或组织边界改变，就重新运行最小实验。

复盘从首个分叉开始，而不是从最后一个报错倒推。先比较冻结输入，再比较第一份结构化中间产物，随后检查状态、约束和副作用。这样可以把复杂系统的排错范围收缩到一个阶段，避免在多个层次同时修改造成新的不确定性。

迁移到真实项目时，先选择一个最小但有代表性的切片，保存改造前基线，再逐步引入“单元测试”中的机制。每一步只改变一个变量并保留回滚点；性能、正确性、安全性和可理解性至少各有一项可量化指标。

最终验收要求读者能脱离页面重新画出结构、口述关键链路、实现最小版本、构造一个反例并解释失败位置。若只能复述名词而不能预测中间状态，说明知识仍停留在识记层，需要回到图示和实验重新验证。

本页用<Term def="单元测试连接单元测试的机制、边界与可复查证据。">单元测试</Term>、

<Term def="痛点场景：没有测试，改代码像拆炸弹连接单元测试的机制、边界与可复查证据。">痛点场景：没有测试，改代码像拆炸弹</Term>、<Term def="解决方案：测试金字塔连接单元测试的机制、边界与可复查证据。">解决方案：测试金字塔</Term>、<Term def="AAA 模式：一个测试的三段式连接单元测试的机制、边界与可复查证据。">AAA 模式：一个测试的三段式</Term>、<Term def="TDD：红绿重构连接单元测试的机制、边界与可复查证据。">TDD：红绿重构</Term>
建立统一坐标。先预测这些概念在结构图和运行轨迹中的位置，再操作实验控件；如果结果与预测不一致，停止在首个分叉，不要用后续补丁掩盖早期错误。

## 权威目录与核心概念逐项对照

- 单元测试
- 痛点场景：没有测试，改代码像拆炸弹
- 解决方案：测试金字塔
- AAA 模式：一个测试的三段式
- TDD：红绿重构
- 常见误区
- 误区 1：测试覆盖率越高越好
- 误区 2：一个测试测一堆东西

<Callout type="trap">
  <strong>误区：</strong>
  看到最终结果正确就认为机制正确。中间状态可能已经偏离，只是被后续默认值或重试偶然修补。应保存首个结构化产物并逐阶段比较。
</Callout>

<Callout type="trap">
  <strong>误区：</strong>
  为了使用模式或框架而制造抽象。若变化轴、失败边界和替代方案没有写清，新增层次只会扩大认知成本和故障面。
</Callout>

## 可复现的最小实现

先把决策记录写成机器可读结构：

```json
{
  "unit": "单元测试",
  "inputFrozen": true,
  "scenario": "normal | boundary | single-fault",
  "firstDivergence": null,
  "cleanupRequired": true
}
```

再用同一条执行链处理三类样本：

```ts
type Evidence = { stage: string; expected: string; actual: string };

function verify(sample: unknown, expected: readonly Evidence[]) {
  const trace = runFromCleanState(sample);
  return expected.find((item, index) => trace[index]?.actual !== item.expected);
}
```

最后保存回归门禁，禁止失败样本静默通过：

```text
normal      -> complete, invariant preserved
boundary    -> complete or explicit rejection
singleFault -> stop at first divergence, no stale output
```

用统一评分解释实验结果：

$$
Q = C_{correct} + C_{trace} + C_{recover} - R_{residual}
$$

$$
C_{coverage} = \frac{N_{verified}}{N_{official}}
$$

$$
R_{residual} = P(failure) \times I(impact)
$$

$$
Accept = (C_{coverage} \ge 0.90) \land (R_{residual} \le R_{budget})
$$

## 本章回顾

- 单元测试必须绑定冻结输入和明确成功标准。
- 痛点场景：没有测试，改代码像拆炸弹必须能画成结构并沿边追踪责任。
- 解决方案：测试金字塔要由正常、边界和单故障样本共同验证。
- AAA 模式：一个测试的三段式必须保存第一处偏离和清理重建步骤。
- TDD：红绿重构决定方案是否可以进入下一阶段。

<Exercises>

**问题 1**：为“单元测试”写出最小输入合同，并指出一个不能省略的边界条件。

<Answer>
  输入合同至少包含样本身份、版本、配置、成功标准和清理方式；边界条件必须能改变结构或状态，而不是只换一段描述。
</Answer>

**问题 2**：构造一个只破坏“解决方案：测试金字塔”的单故障样本，第一条预期证据是什么？

<Answer>
  先冻结正常轨迹，只改变该条件；第一条证据应是最早依赖它的结构化中间产物，而不是最终错误消息。
</Answer>

**问题 3**：什么情况下不应采用本页方案？

<Answer>
  当变化轴稳定、直接实现更清晰、额外层次无法降低风险或提供独立测试边界时，应选择更简单方案并记录重新评估触发条件。
</Answer>

</Exercises>

## 术语表

<Glossary>
  <GlossaryItem term="单元测试">
    单元测试用于描述“单元测试”中的输入、结构、约束或证据边界，必须能在实验记录中定位。
  </GlossaryItem>
  <GlossaryItem term="痛点场景：没有测试，改代码像拆炸弹">
    痛点场景：没有测试，改代码像拆炸弹用于描述“单元测试”中的输入、结构、约束或证据边界，必须能在实验记录中定位。
  </GlossaryItem>
  <GlossaryItem term="解决方案：测试金字塔">
    解决方案：测试金字塔用于描述“单元测试”中的输入、结构、约束或证据边界，必须能在实验记录中定位。
  </GlossaryItem>
  <GlossaryItem term="AAA 模式：一个测试的三段式">
    AAA
    模式：一个测试的三段式用于描述“单元测试”中的输入、结构、约束或证据边界，必须能在实验记录中定位。
  </GlossaryItem>
  <GlossaryItem term="TDD：红绿重构">
    TDD：红绿重构用于描述“单元测试”中的输入、结构、约束或证据边界，必须能在实验记录中定位。
  </GlossaryItem>
</Glossary>

<Attribution
  adaptedFrom="站内双书专题《代码质量与重构》，主线采用Robert C. Martin《Clean Code》第一版（2008）与Martin Fowler《Refactoring》第二版（2018） · 独立教学重构"
  adaptedUrl="https://www.pearson.com/en-us/subject-catalog/p/clean-code-a-handbook-of-agile-software-craftsmanship/P200000009044/9780132350884"
/>

<Objectives>

- 能画出测试金字塔，并解释为什么底层多、顶层少
- 能用 AAA 模式写一个清晰的单元测试
- 能回答：TDD 的「红绿重构」三步分别是什么？没测试为什么不能重构？

</Objectives>

## 痛点场景：没有测试，改代码像拆炸弹

你要改一段核心逻辑，没有测试。你小心翼翼地改，改完手动点几下界面，「看起来没坏」就上线了，结果三天后用户报错——你改的地方影响了另一个角落的功能。没有测试，每次改动都是赌博：**你不知道自己有没有改坏什么**。测试不是负担，是让你敢改代码的底气——没有测试网兜底，谁敢动老代码？

<CqrTestPyramidDiagram />

## 解决方案：测试金字塔

测试分层，像金字塔：底层多、顶层少。

- **单元测试（塔底，多）**：测单个函数/类，快、稳、多。跑一次几毫秒，应该有成百上千个
- **集成测试（中间，少）**：测模块间协作，慢一些。验证「拼起来能不能工作」
- **端到端测试（塔顶，极少）**：测完整用户流程，最慢、最脆。少量关键路径即可

<Callout type="tip">
  为什么金字塔要「底大顶小」？端到端测试慢且脆（界面一变就挂），用它覆盖所有分支不现实。单元测试快且准，能精确定位是哪个函数错了。把精力压在塔底，塔顶只守关键路径。
</Callout>

## AAA 模式：一个测试的三段式

每个单元测试都该是清晰的三段：

```typescript
test("打九折后总价正确", () => {
  // Arrange（准备）：搭好测试场景
  const cart = new Cart([{ price: 100, qty: 2 }]);

  // Act（执行）：调用被测函数
  const total = cart.totalWithDiscount(0.9);

  // Assert（断言）：验证结果
  expect(total).toBe(180);
});
```

- **Arrange**：准备输入和依赖，这是最长的一段
- **Act**：执行被测逻辑，通常一行
- **Assert**：断言结果，一个测试只断言一个核心行为

## TDD：红绿重构

测试驱动开发的循环：**红 → 绿 → 重构**。

1. **红**：先写一个会失败的测试（因为功能还没实现）
2. **绿**：写最少的代码让测试通过——别过度设计，能过就行
3. **重构**：测试还绿着，放心地优化代码结构

<Callout type="warn">
  TDD
  的「重构」这一步常被跳过——很多人绿了就跑去写下个测试。但正是这一步让代码保持整洁。测试保护你改结构，跳过重构等于放弃了
  TDD 一半的价值。
</Callout>

## 常见误区

### 误区 1：测试覆盖率越高越好

**现象**：追求 100% 覆盖率，写了一堆测 getter/setter 的无用测试。**原因**：把覆盖率当成了质量指标。**修法**：覆盖率的重点在「关键逻辑覆盖到了没有」，而非「行数全到了」。一个边界条件没测到，比 100% 覆盖 getter 危险得多。

### 误区 2：一个测试测一堆东西

**现象**：一个 test 里 arrange 了三个场景、assert 了十个结果。**原因**：图省事把相关测试堆一起。**修法**：一个测试只验证一个行为。挂了能立刻知道是哪个行为出错——堆在一起，挂了你还要排查是哪段出问题。

### 误区 3：测试依赖执行顺序

**现象**：测试 A 跑完留下数据，测试 B 依赖它，单独跑 B 就挂。**原因**：测试间有隐式依赖。**修法**：每个测试独立——自己准备、自己清理。任何顺序跑都应该全绿，否则测试不可信。

## 小结

- 测试金字塔：单元多、集成少、端到端极少——把精力压在塔底
- AAA 模式：Arrange 准备、Act 执行、Assert 断言，一个测试一个行为
- TDD 循环：红（写失败测试）→ 绿（最少代码通过）→ 重构（绿着改结构）
- 测试是重构的安全网：没测试别动老代码

下一步：进入「类与组织」，把视角从函数抬到类。

<Attribution
  adaptedFrom="Clean Code, Robert C. Martin & Refactoring, Martin Fowler"
  adaptedUrl="https://www.oreilly.com/"
/>

<Glossary>

<GlossaryItem term="测试金字塔">

测试分层策略：底层单元测试多而快，顶层端到端测试少而慢，形状像金字塔。倒金字塔（端到端多、单元少）会导致测试又慢又脆。

</GlossaryItem>

<GlossaryItem term="AAA 模式">

单元测试的三段结构：Arrange（准备场景）、Act（执行被测逻辑）、Assert（断言结果）。让每个测试结构统一、易读。

</GlossaryItem>

<GlossaryItem term="TDD 红绿重构">

测试驱动开发的三步循环：先写失败测试（红），再写最少代码让它通过（绿），最后在测试保护下优化结构（重构）。「重构」一步常被跳过，但它正是 TDD 保持代码整洁的关键。

</GlossaryItem>

</Glossary>
