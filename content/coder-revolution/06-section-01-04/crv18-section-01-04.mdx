---
title: "1.4 CPU阿甘"
type: B
section: "码农翻身（2018年第1版）"
order: 7
description: "沿取指、译码、执行、缓存访存、流水线提交追踪指令吞吐，用 CPI、依赖和停顿证据解释 CPU 性能。"
demo: true
math: true
sourceUrl: "https://www.phei.com.cn/module/goods/wssd_content.jsp?bookid=52126"
qualityVersion: 2
practiceMode: calculation
sourceMode: independent-rewrite
officialUnitId: crv18-section-01-04
draft: false
---

import { Objectives } from "@/components/mdx/objectives";
import { Term } from "@/components/mdx/term";
import { Callout } from "@/components/mdx/callout";
import { Step, Stepper } from "@/components/mdx/stepper";
import { Answer, Exercises } from "@/components/mdx/exercises";
import { Glossary, GlossaryItem } from "@/components/mdx/glossary";
import { Attribution } from "@/components/mdx/attribution";
import {
  Crv18Section0104EvidenceDiagram,
  Crv18Section0104Lab,
  Crv18Section0104PipelineDiagram,
} from "@/components/mdx/coder-revolution/diagrams/crv18-section-01-04-diagram";

<Objectives>

- 能沿取指、译码、执行、访存和提交追踪指令的阶段状态与结果
- 能用指令数、CPI、周期时间、缓存命中率和流水线停顿解释性能，而不是只看主频
- 能在数据依赖、缓存未命中和分支预测失败场景中定位首个停顿并重放验证

</Objectives>

## 1.4 CPU阿甘

本页依据刘欣《码农翻身》（2018 年第 1 版）及出版社公开书目信息，独立重构 **1.4 CPU阿甘**。正文、代码、图示、实验和练习都是本课程重新设计的教学材料，不复制原书正文、插图、练习答案或代码。

CPU 的工作不是“主频越高就越快”。一条指令经历取指、译码、执行、可能的访存和提交；流水线让不同指令的阶段重叠，但数据依赖、缓存未命中和分支改变会插入停顿或清空。性能解释必须把指令数、CPI、周期时间和停顿原因放回同一条轨迹。

## 三个会让 CPU 模型失真的陷阱

<Callout type="trap" title="只用主频判断性能">

- **现象**：两个程序或两颗 CPU 只比较 GHz，就宣布谁更快。
- **原因**：忽略指令数、每条指令周期数、缓存行为、依赖和分支停顿。
- **修法**：记录 `T_cpu = instruction_count × CPI × cycle_time` 的每个量，并把额外停顿归因到阶段证据。

</Callout>

<Callout type="trap" title="把流水线重叠当成每条指令零成本">

- **现象**：看到五级流水线，就把五条指令都当成同一周期完成。
- **原因**：混淆吞吐量和单条指令延迟，忽略依赖、结构冲突、分支和访存等待。
- **修法**：画出每条指令的阶段占用和停顿周期，区分首条延迟、稳态吞吐与排空成本。

</Callout>

<Callout type="trap" title="把缓存命中当成内存访问的恒定成本">

- **现象**：每次加载都使用同一个延迟，或者看到缓存未命中只说“CPU 变慢”。
- **原因**：没有记录访问层级、局部性、未命中类型和等待是否阻塞依赖。
- **修法**：保存地址、缓存层级、命中/未命中、填充时间和受影响的流水线阶段。

</Callout>

## 五个目录节点到机制证据

### 1.4 CPU阿甘

<Term def="通过取指、译码、执行、访存和提交阶段推进指令，并用周期、CPI、缓存和停顿证据解释结果的 CPU 执行模型。">
  1.4 CPU阿甘
</Term>
不是把人物故事换成硬件名词，而是固定一条最小指令序列，说明每个阶段的输入、输出和阻塞条件。

性能合同可写成：

$$
T_{cpu}=instruction\_count\times CPI\times cycle\_time
$$

如果结果相同但其中一次访存越过缓存层级、依赖被错误忽略或提交顺序不成立，仍应拒绝该实现。最终时间是结果，阶段轨迹才是解释。

### 启动

<Term def="根据程序计数器取出下一条指令，并把指令与地址带入流水线的阶段。">
  启动
</Term>
对应取指的机制证据。记录程序计数器、指令地址、取指缓存结果和下一地址；分支或未命中改变路径时，必须说明预取或等待如何处理。

### 运行

<Term def="指令在译码、执行、访存和提交阶段推进，阶段之间通过操作数、控制信号和结果传递连接。">
  运行
</Term>
对应指令执行的阶段证据。记录操作数就绪、功能单元、依赖、访存结果和提交顺序，不能只用最终寄存器值推断中间过程。

### 新装备：缓存

<Term def="利用局部性把近期或相邻数据放在更快层级，并用命中、未命中和填充事件决定访存等待的层级。">
  新装备：缓存
</Term>
对应访存的层级证据。缓存命中缩短常见路径，未命中可能等待更低层或主存；记录地址、层级、替换与填充，才能解释
CPI 变化。

### 自我提升：流水线

<Term def="让不同指令的取指、译码、执行、访存和提交阶段重叠，并在依赖、分支或资源冲突时插入停顿、转发或清空。">
  自我提升：流水线
</Term>
对应重叠执行的证据。每一周期都应能回答哪些指令占用哪些阶段、哪个条件阻塞了推进，以及提交是否保持架构顺序。

<Crv18Section0104PipelineDiagram />

## 最小可重放实现

```text
expected = instruction_count * cpi * cycle_time
trace = run_pipeline(fixed_program, cache="baseline")
assert trace.committed_value == reference_value
assert trace.cycles == expected_cycles
assert reset(trace) == run_pipeline(fixed_program, cache="baseline")
```

这段草图只表达阶段轨迹与性能合同，不复制书中叙事或代码。实际复核应保存程序版本、指令数、周期、CPI、程序计数器、阶段占用、缓存事件、停顿原因、提交结果和复位结果。

## 五步复核一条指令序列

<Stepper>

<Step title="1. 固定程序、指令数与性能基线">

冻结指令序列、输入、程序计数器、缓存初始状态和周期时间。先按公式预测周期，再观察第一条指令的取指。

<Crv18Section0104PipelineDiagram />

</Step>

<Step title="2. 检查译码与执行依赖">

记录操作数、功能单元、数据依赖和转发路径。让后一条指令依赖前一条结果，确认停顿发生在执行前的哪个阶段。

<Crv18Section0104EvidenceDiagram />

</Step>

<Step title="3. 注入缓存命中与未命中">

固定访问地址并切换缓存状态，保存命中层级、未命中延迟、填充事件和受阻指令。不要把所有内存访问归为同一延迟。

<Crv18Section0104Lab />

</Step>

<Step title="4. 观察分支与流水线停顿">

改变分支结果或预测结果，记录取指路径、清空、重新取指和提交顺序；找最先发生的错误路径，而不是只看总周期。

<Crv18Section0104EvidenceDiagram />

</Step>

<Step title="5. 提交、计算周期并复位">

核对架构状态、总周期、指令数、CPI 和停顿分解。点击复位后用相同程序确认寄存器、缓存选择和阶段轨迹恢复基线。

<Crv18Section0104Lab />

</Step>

</Stepper>

<Crv18Section0104Lab />

## 正常、边界与故障证据

<Crv18Section0104EvidenceDiagram />

| 样本 | 只改变的变量                           | 预期判定                         | 必存证据                         |
| ---- | -------------------------------------- | -------------------------------- | -------------------------------- |
| 正常 | 独立指令、缓存命中、分支路径稳定       | 阶段推进，提交结果与基线一致     | 指令数、周期、CPI、阶段轨迹      |
| 边界 | 数据依赖、缓存容量、流水线填充或排空   | 在相应阶段停顿并给出额外周期     | 依赖、层级、停顿周期、路径       |
| 故障 | 一次错误转发、未命中处理或分支清空遗漏 | 首个阶段违约可定位，修复后可重放 | 首差、操作数、缓存事件、提交结果 |

## 故障诊断：先找第一个多出的周期

1. **核对算式输入**：确认指令数、CPI、周期时间和测量区间，排除把初始化或排空混入基线。
2. **核对阶段依赖**：按周期排列取指、译码、执行、访存和提交，找第一个操作数未就绪或资源冲突。
3. **核对缓存事件**：确认访问地址、命中层级、未命中类型、填充和阻塞范围，区分容量、冲突和冷启动。
4. **核对路径与提交**：检查分支预测、清空、重新取指和提交顺序；修复后从同一缓存基线重放。

## 术语表

<Glossary>
  <GlossaryItem term="1.4 CPU阿甘">
    通过阶段推进、缓存层级和停顿证据解释指令执行与 CPU 时间的模型。
  </GlossaryItem>
  <GlossaryItem term="启动">
    根据程序计数器取出下一条指令并带入流水线的阶段。
  </GlossaryItem>
  <GlossaryItem term="运行">
    指令在译码、执行、访存和提交阶段推进的过程。
  </GlossaryItem>
  <GlossaryItem term="新装备：缓存">
    利用局部性把数据放在更快层级，并用命中与未命中决定访存等待的机制。
  </GlossaryItem>
  <GlossaryItem term="自我提升：流水线">
    让多条指令的阶段重叠，并在依赖、分支或资源冲突时插入控制动作的机制。
  </GlossaryItem>
</Glossary>

## 练习

<Exercises>

**问题 1：** 两颗 CPU 主频相同，程序运行时间不同，应该补充哪些证据？

<Answer>

补充指令数、总周期、CPI、周期时间、缓存命中/未命中、分支停顿和流水线排空证据。用 `T_cpu = instruction_count × CPI × cycle_time` 分解差异，不能用主频单独解释性能。

</Answer>

**问题 2：** 后一条指令依赖前一条结果，为什么流水线仍可能出现停顿？

<Answer>

阶段重叠不等于操作数立即可用。记录生产指令的执行和写回时刻、消费指令的译码/执行时刻、转发路径以及插入的气泡；若结果尚未到达，就暂停消费或选择合法转发，随后验证提交顺序。

</Answer>

**问题 3：** 一次缓存未命中使总周期增加，怎样判断它是否阻塞了整条流水线？

<Answer>

保存访问地址、未命中层级、填充延迟、等待指令和同期阶段占用。如果独立指令继续推进，可能是局部阻塞；如果依赖链和取指都停住，记录阻塞范围与排空/恢复，再用命中基线比较额外周期。

</Answer>

</Exercises>

<Attribution
  sources={[
    {
      label: "本书版本与目录范围依据：中国电子工业出版社《码农翻身》书目页",
      url: "https://www.phei.com.cn/module/goods/wssd_content.jsp?bookid=52126",
    },
    {
      label: "指令语义、程序计数器与架构状态边界参考：RISC-V ISA 规范",
      url: "https://docs.riscv.org/reference/isa/v20240411/unpriv/",
    },
    {
      label: "作者公开文章与技术写作范围交叉核对：刘欣博客",
      url: "https://liuxiaochen.blog.csdn.net/",
    },
  ]}
  note="本页仅使用公开书目信息、RISC-V 公开规范与作者公开资料作为范围依据；正文、代码、图示、实验和练习均为独立重写。"
  license="出版社公开书目信息与公开标准资料；教学内容为本项目独立创作。"
/>

## 本页小结

1.4 CPU阿甘的关键不是把 CPU 想成更快的跑步者，而是能沿取指、译码、执行、访存、提交追踪阶段，说明缓存和流水线怎样改变等待。完成标准是用指令数、CPI、周期时间和停顿证据解释总周期，在依赖、未命中或分支故障中定位首差，并证明复位后结果和轨迹重新一致。
