---
title: "1.4 CPU阿甘"
type: B
section: "码农翻身（2018年第1版）"
order: 7
description: "1.4 CPU阿甘：从“一条指令走完才开始下一条”推进到“阶段重叠且能解释停顿来源”，用专属机制图、实验和故障诊断验证。"
demo: true
math: true
sourceUrl: "https://www.phei.com.cn/module/goods/wssd_content.jsp?bookid=52126"
qualityVersion: 2
practiceMode: calculation
sourceMode: independent-rewrite
draft: false
---


## 为什么需要这一机制

1.4 CPU阿甘 不能停在故事类比。真实系统要解决的变化是从 **一条指令走完才开始下一条** 到 **阶段重叠且能解释停顿来源**；可执行机制是：CPU 以取指、译码、执行和提交推进指令；缓存缩短常见访存，流水线重叠不同指令阶段但会被依赖和分支打断。

对 1.4 CPU阿甘 的验收从同一份最小输入开始，先预测 取指、译码、执行、访存缓存、提交结果 各自会看到什么，再运行正常、边界和故障场景。故事帮助记住矛盾，状态、协议、类型、构建或反馈证据才决定解释是否成立。


## 核心合同

$$
T_{cpu}=instruction\_count\times CPI\times cycle\_time
$$

在 1.4 CPU阿甘 中，公式或状态表达式里的量必须绑定到具体对象、单位、版本或生命周期。先给基线输入算出预期，再逐步观察；如果最终结果碰巧相同，但中途已违反 执行 或 访存缓存，仍应拒绝该实现。

## 目录节点到机制证据

### 1.4 CPU阿甘

在 **1.4 CPU阿甘** 中，目录坐标 **1.4 CPU阿甘** 对应“取指”这一步。它不是故事情节的复述，而是要求读者解释：CPU 以取指、译码、执行和提交推进指令；缓存缩短常见访存，流水线重叠不同指令阶段但会被依赖和分支打断。

复核 **1.4 CPU阿甘** 时，为 1.4 CPU阿甘 固定一个最小输入，先写出预期状态和输出，再改变与“取指”直接相连的一个条件。若出现“只用主频判断性能，忽略指令数、CPI、缓存未命中和分支停顿”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

### 启动

在 **1.4 CPU阿甘** 中，目录坐标 **启动** 对应“译码”这一步。它不是故事情节的复述，而是要求读者解释：CPU 以取指、译码、执行和提交推进指令；缓存缩短常见访存，流水线重叠不同指令阶段但会被依赖和分支打断。

复核 **启动** 时，为 1.4 CPU阿甘 固定一个最小输入，先写出预期状态和输出，再改变与“译码”直接相连的一个条件。若出现“只用主频判断性能，忽略指令数、CPI、缓存未命中和分支停顿”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

### 运行

在 **1.4 CPU阿甘** 中，目录坐标 **运行** 对应“执行”这一步。它不是故事情节的复述，而是要求读者解释：CPU 以取指、译码、执行和提交推进指令；缓存缩短常见访存，流水线重叠不同指令阶段但会被依赖和分支打断。

复核 **运行** 时，为 1.4 CPU阿甘 固定一个最小输入，先写出预期状态和输出，再改变与“执行”直接相连的一个条件。若出现“只用主频判断性能，忽略指令数、CPI、缓存未命中和分支停顿”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

### 新装备：缓存

在 **1.4 CPU阿甘** 中，目录坐标 **新装备：缓存** 对应“访存缓存”这一步。它不是故事情节的复述，而是要求读者解释：CPU 以取指、译码、执行和提交推进指令；缓存缩短常见访存，流水线重叠不同指令阶段但会被依赖和分支打断。

复核 **新装备：缓存** 时，为 1.4 CPU阿甘 固定一个最小输入，先写出预期状态和输出，再改变与“访存缓存”直接相连的一个条件。若出现“只用主频判断性能，忽略指令数、CPI、缓存未命中和分支停顿”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

### 自我提升：流水线

在 **1.4 CPU阿甘** 中，目录坐标 **自我提升：流水线** 对应“提交结果”这一步。它不是故事情节的复述，而是要求读者解释：CPU 以取指、译码、执行和提交推进指令；缓存缩短常见访存，流水线重叠不同指令阶段但会被依赖和分支打断。

复核 **自我提升：流水线** 时，为 1.4 CPU阿甘 固定一个最小输入，先写出预期状态和输出，再改变与“提交结果”直接相连的一个条件。若出现“只用主频判断性能，忽略指令数、CPI、缓存未命中和分支停顿”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

## 最小可重放实现

```text
# 1.4 CPU阿甘：先手算合同，再核对逐阶段轨迹
expected = calculate_reference(fixed_input)
trace = run_datapath(fixed_input)
assert trace.output == expected
assert run_datapath(fixed_input).output == expected
```

这段实现草图只表达 1.4 CPU阿甘 的验证合同，不复制书中叙事或代码。实际运行时应保存输入、环境、每个节点状态、最终结果和复位结果，使另一位读者能够从干净状态重放。

## 专属因果实验

先预测改变一个控制量后，1.4 CPU阿甘 的哪一节点最先变化。依次切换基线、边界和注入故障，保存一次轨迹；随后点击“重置实验”，确认控件、节点选择、压力值和状态文本全部恢复。


## 故障诊断


## 术语与边界

1.4 CPU阿甘 使用五个可验证术语：、、、、。术语若不能指向本页实验中的控件、节点或状态，就仍然只是目录名词。


## 本页小结

1.4 CPU阿甘 的关键不是记住故事，而是能解释“CPU 以取指、译码、执行和提交推进指令；缓存缩短常见访存，流水线重叠不同指令阶段但会被依赖和分支打断”。完成标准是用本页合同推出预期，沿五个专属节点观察实际，诊断“只用主频判断性能，忽略指令数、CPI、缓存未命中和分支停顿”，并证明复位后的同一输入重新得到基线轨迹。


