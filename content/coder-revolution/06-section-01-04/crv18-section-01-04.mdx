---
title: "1.4 CPU阿甘"
type: B
section: "码农翻身（2018年第1版）"
order: 7
description: "1.4 CPU阿甘：从“一条指令走完才开始下一条”推进到“阶段重叠且能解释停顿来源”，用专属机制图、实验和故障诊断验证。"
demo: true
math: true
sourceUrl: "https://www.phei.com.cn/module/goods/wssd_content.jsp?bookid=52126"
qualityVersion: 2
practiceMode: calculation
sourceMode: independent-rewrite
draft: true
---

import { Crv18Section0104Lab } from "@/components/mdx/coder-revolution/diagrams/crv18-section-01-04";
import {
  Objectives,
  Callout,
  Term,
  Glossary,
  GlossaryItem,
  Exercises,
  Answer,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释 1.4 CPU阿甘 为什么把“一条指令走完才开始下一条”推进为“阶段重叠且能解释停顿来源”
- 能沿 取指 → 译码 → 执行 → 访存缓存 → 提交结果 说清对象、状态和边界
- 能用本页公式或状态合同完成一次手算、代码、模拟、诊断或方案判断
- 能注入“只用主频判断性能，忽略指令数、CPI、缓存未命中和分支停顿”，定位最早偏离点并验证复位

</Objectives>

## 为什么需要这一机制

1.4 CPU阿甘 不能停在故事类比。真实系统要解决的变化是从 **一条指令走完才开始下一条** 到 **阶段重叠且能解释停顿来源**；可执行机制是：CPU 以取指、译码、执行和提交推进指令；缓存缩短常见访存，流水线重叠不同指令阶段但会被依赖和分支打断。

对 1.4 CPU阿甘 的验收从同一份最小输入开始，先预测 取指、译码、执行、访存缓存、提交结果 各自会看到什么，再运行正常、边界和故障场景。故事帮助记住矛盾，状态、协议、类型、构建或反馈证据才决定解释是否成立。

<Callout type="info" title="范围与事实来源">
  本页按电子工业出版社公开目录定位 1.4 CPU阿甘 的问题范围，并依据 [RISC-V
  unprivileged ISA
  specification](https://docs.riscv.org/reference/isa/unpriv/unpriv-index.html)
  独立重写技术机制。目录只证明主题坐标，不代表取得或复现原书正文。
</Callout>

## 核心合同

$$
T_{cpu}=instruction\_count\times CPI\times cycle\_time
$$

在 1.4 CPU阿甘 中，公式或状态表达式里的量必须绑定到具体对象、单位、版本或生命周期。先给基线输入算出预期，再逐步观察；如果最终结果碰巧相同，但中途已违反 执行 或 访存缓存，仍应拒绝该实现。

## 目录节点到机制证据

### 1.4 CPU阿甘

在 **1.4 CPU阿甘** 中，目录坐标 **1.4 CPU阿甘** 对应“取指”这一步。它不是故事情节的复述，而是要求读者解释：CPU 以取指、译码、执行和提交推进指令；缓存缩短常见访存，流水线重叠不同指令阶段但会被依赖和分支打断。

复核 **1.4 CPU阿甘** 时，为 1.4 CPU阿甘 固定一个最小输入，先写出预期状态和输出，再改变与“取指”直接相连的一个条件。若出现“只用主频判断性能，忽略指令数、CPI、缓存未命中和分支停顿”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

### 启动

在 **1.4 CPU阿甘** 中，目录坐标 **启动** 对应“译码”这一步。它不是故事情节的复述，而是要求读者解释：CPU 以取指、译码、执行和提交推进指令；缓存缩短常见访存，流水线重叠不同指令阶段但会被依赖和分支打断。

复核 **启动** 时，为 1.4 CPU阿甘 固定一个最小输入，先写出预期状态和输出，再改变与“译码”直接相连的一个条件。若出现“只用主频判断性能，忽略指令数、CPI、缓存未命中和分支停顿”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

### 运行

在 **1.4 CPU阿甘** 中，目录坐标 **运行** 对应“执行”这一步。它不是故事情节的复述，而是要求读者解释：CPU 以取指、译码、执行和提交推进指令；缓存缩短常见访存，流水线重叠不同指令阶段但会被依赖和分支打断。

复核 **运行** 时，为 1.4 CPU阿甘 固定一个最小输入，先写出预期状态和输出，再改变与“执行”直接相连的一个条件。若出现“只用主频判断性能，忽略指令数、CPI、缓存未命中和分支停顿”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

### 新装备：缓存

在 **1.4 CPU阿甘** 中，目录坐标 **新装备：缓存** 对应“访存缓存”这一步。它不是故事情节的复述，而是要求读者解释：CPU 以取指、译码、执行和提交推进指令；缓存缩短常见访存，流水线重叠不同指令阶段但会被依赖和分支打断。

复核 **新装备：缓存** 时，为 1.4 CPU阿甘 固定一个最小输入，先写出预期状态和输出，再改变与“访存缓存”直接相连的一个条件。若出现“只用主频判断性能，忽略指令数、CPI、缓存未命中和分支停顿”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

### 自我提升：流水线

在 **1.4 CPU阿甘** 中，目录坐标 **自我提升：流水线** 对应“提交结果”这一步。它不是故事情节的复述，而是要求读者解释：CPU 以取指、译码、执行和提交推进指令；缓存缩短常见访存，流水线重叠不同指令阶段但会被依赖和分支打断。

复核 **自我提升：流水线** 时，为 1.4 CPU阿甘 固定一个最小输入，先写出预期状态和输出，再改变与“提交结果”直接相连的一个条件。若出现“只用主频判断性能，忽略指令数、CPI、缓存未命中和分支停顿”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

## 最小可重放实现

```text
# 1.4 CPU阿甘：先手算合同，再核对逐阶段轨迹
expected = calculate_reference(fixed_input)
trace = run_datapath(fixed_input)
assert trace.output == expected
assert run_datapath(fixed_input).output == expected
```

这段实现草图只表达 1.4 CPU阿甘 的验证合同，不复制书中叙事或代码。实际运行时应保存输入、环境、每个节点状态、最终结果和复位结果，使另一位读者能够从干净状态重放。

## 专属因果实验

先预测改变一个控制量后，1.4 CPU阿甘 的哪一节点最先变化。依次切换基线、边界和注入故障，保存一次轨迹；随后点击“重置实验”，确认控件、节点选择、压力值和状态文本全部恢复。

<Crv18Section0104Lab />

## 故障诊断

<Callout
  type="trap"
  title="误区：只用主频判断性能，忽略指令数、CPI、缓存未命中和分支停顿"
>
  该错误会破坏 1.4 CPU阿甘
  从“一条指令走完才开始下一条”到“阶段重叠且能解释停顿来源”的转换。诊断时按 取指
  → 译码 → 执行 → 访存缓存 → 提交结果
  前进，只在首次出现预期与实际不一致的位置停下。
</Callout>

<Callout type="trap" title="误区：最终成功就算机制正确">
  1.4 CPU阿甘
  的最终输出可能被缓存、重试、默认值或偶然时序掩盖。必须同时保存中间状态、拒绝语义和复位重放；缺少任一项都不能据此推广到更大规模。
</Callout>

<Callout type="tip" title="恢复顺序">
  1.4 CPU阿甘 的恢复顺序是：还原输入与环境 → 清理派生状态 → 重建当前边界 →
  重跑机制链 → 对照公式或合同 → 再次注入同一故障确认可重复拒绝。
</Callout>

## 术语与边界

1.4 CPU阿甘 使用五个可验证术语：<Term def="取指 是 1.4 CPU阿甘 机制链中的可观察状态或边界，必须用实际输入和轨迹验证">取指</Term>、<Term def="译码 是 1.4 CPU阿甘 机制链中的可观察状态或边界，必须用实际输入和轨迹验证">译码</Term>、<Term def="执行 是 1.4 CPU阿甘 机制链中的可观察状态或边界，必须用实际输入和轨迹验证">执行</Term>、<Term def="访存缓存 是 1.4 CPU阿甘 机制链中的可观察状态或边界，必须用实际输入和轨迹验证">访存缓存</Term>、<Term def="提交结果 是 1.4 CPU阿甘 机制链中的可观察状态或边界，必须用实际输入和轨迹验证">提交结果</Term>。术语若不能指向本页实验中的控件、节点或状态，就仍然只是目录名词。

<Glossary>

<GlossaryItem term="取指">
  取指 位于 1.4 CPU阿甘 的第 1
  个检查点；验收记录必须包含它的输入、状态、输出和故障表现。
</GlossaryItem>

<GlossaryItem term="译码">
  译码 位于 1.4 CPU阿甘 的第 2
  个检查点；验收记录必须包含它的输入、状态、输出和故障表现。
</GlossaryItem>

<GlossaryItem term="执行">
  执行 位于 1.4 CPU阿甘 的第 3
  个检查点；验收记录必须包含它的输入、状态、输出和故障表现。
</GlossaryItem>

<GlossaryItem term="访存缓存">
  访存缓存 位于 1.4 CPU阿甘 的第 4
  个检查点；验收记录必须包含它的输入、状态、输出和故障表现。
</GlossaryItem>

<GlossaryItem term="提交结果">
  提交结果 位于 1.4 CPU阿甘 的第 5
  个检查点；验收记录必须包含它的输入、状态、输出和故障表现。
</GlossaryItem>

</Glossary>

## 本页小结

1.4 CPU阿甘 的关键不是记住故事，而是能解释“CPU 以取指、译码、执行和提交推进指令；缓存缩短常见访存，流水线重叠不同指令阶段但会被依赖和分支打断”。完成标准是用本页合同推出预期，沿五个专属节点观察实际，诊断“只用主频判断性能，忽略指令数、CPI、缓存未命中和分支停顿”，并证明复位后的同一输入重新得到基线轨迹。

<Exercises>

1. 1.4 CPU阿甘 的五节点链中，哪个状态最能区分“一条指令走完才开始下一条”和“阶段重叠且能解释停顿来源”？请给出一个最小样本。

<Answer>
  先检查 取指 的输入身份，再观察 译码 与
  执行。只改变一个条件时，最早发生可解释变化的节点就是区分两种状态的证据；目录节点逐项核对为：1.4
  CPU阿甘（检查 取指）；启动（检查 译码）；运行（检查 执行）；新装备：缓存（检查
  访存缓存）；自我提升：流水线（检查 提交结果）。
</Answer>

2. 使用本页核心合同完成一次手算、状态推演或代码断言，并说明每个量对应哪个真实对象。

<Answer>
  为 1.4 CPU阿甘 写下输入值、单位、版本和边界，按公式得到预期，再与实验中的 执行
  和 提交结果
  比较。若发生隐式单位转换、状态复用或顺序变化，必须先修正模型，不能只调整答案。
</Answer>

3. 注入“只用主频判断性能，忽略指令数、CPI、缓存未命中和分支停顿”后，怎样证明恢复动作有效而非偶然成功？

<Answer>
  保存故障前基线与最早偏离点，执行本页恢复顺序，再用完全相同输入运行两次。1.4
  CPU阿甘 只有在控件状态、五节点轨迹、最终输出和拒绝结果都可重复时才算恢复完成。
</Answer>

</Exercises>

<Attribution />
