---
title: "1.5 我是一个进程"
type: B
section: "码农翻身（2018年第1版）"
order: 8
description: "1.5 我是一个进程：从“程序等同于一段磁盘文件”推进到“运行实例拥有地址映射与资源生命周期”，用专属机制图、实验和故障诊断验证。"
demo: true
math: true
sourceUrl: "https://www.phei.com.cn/module/goods/wssd_content.jsp?bookid=52126"
qualityVersion: 2
practiceMode: simulation
sourceMode: independent-rewrite
draft: false
---


## 为什么需要这一机制

1.5 我是一个进程 不能停在故事类比。真实系统要解决的变化是从 **程序等同于一段磁盘文件** 到 **运行实例拥有地址映射与资源生命周期**；可执行机制是：进程提供隔离的虚拟地址空间和资源身份，页表把虚拟页映射到物理页，线程则在该进程资源内执行。

对 1.5 我是一个进程 的验收从同一份最小输入开始，先预测 装载程序、创建进程、映射页面、调度线程、退出回收 各自会看到什么，再运行正常、边界和故障场景。故事帮助记住矛盾，状态、协议、类型、构建或反馈证据才决定解释是否成立。


## 核心合同

$$
virtual\_address = VPN\times page\_size + offset
$$

在 1.5 我是一个进程 中，公式或状态表达式里的量必须绑定到具体对象、单位、版本或生命周期。先给基线输入算出预期，再逐步观察；如果最终结果碰巧相同，但中途已违反 映射页面 或 调度线程，仍应拒绝该实现。

## 目录节点到机制证据

### 1.5 我是一个进程

在 **1.5 我是一个进程** 中，目录坐标 **1.5 我是一个进程** 对应“装载程序”这一步。它不是故事情节的复述，而是要求读者解释：进程提供隔离的虚拟地址空间和资源身份，页表把虚拟页映射到物理页，线程则在该进程资源内执行。

复核 **1.5 我是一个进程** 时，为 1.5 我是一个进程 固定一个最小输入，先写出预期状态和输出，再改变与“装载程序”直接相连的一个条件。若出现“把指针值当成物理地址，或让两个进程未经共享映射直接访问同一虚拟地址”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

### 批处理系统

在 **1.5 我是一个进程** 中，目录坐标 **批处理系统** 对应“创建进程”这一步。它不是故事情节的复述，而是要求读者解释：进程提供隔离的虚拟地址空间和资源身份，页表把虚拟页映射到物理页，线程则在该进程资源内执行。

复核 **批处理系统** 时，为 1.5 我是一个进程 固定一个最小输入，先写出预期状态和输出，再改变与“创建进程”直接相连的一个条件。若出现“把指针值当成物理地址，或让两个进程未经共享映射直接访问同一虚拟地址”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

### 多道程序

在 **1.5 我是一个进程** 中，目录坐标 **多道程序** 对应“映射页面”这一步。它不是故事情节的复述，而是要求读者解释：进程提供隔离的虚拟地址空间和资源身份，页表把虚拟页映射到物理页，线程则在该进程资源内执行。

复核 **多道程序** 时，为 1.5 我是一个进程 固定一个最小输入，先写出预期状态和输出，再改变与“映射页面”直接相连的一个条件。若出现“把指针值当成物理地址，或让两个进程未经共享映射直接访问同一虚拟地址”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

### 地址重定位

在 **1.5 我是一个进程** 中，目录坐标 **地址重定位** 对应“调度线程”这一步。它不是故事情节的复述，而是要求读者解释：进程提供隔离的虚拟地址空间和资源身份，页表把虚拟页映射到物理页，线程则在该进程资源内执行。

复核 **地址重定位** 时，为 1.5 我是一个进程 固定一个最小输入，先写出预期状态和输出，再改变与“调度线程”直接相连的一个条件。若出现“把指针值当成物理地址，或让两个进程未经共享映射直接访问同一虚拟地址”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

### 分时系统

在 **1.5 我是一个进程** 中，目录坐标 **分时系统** 对应“退出回收”这一步。它不是故事情节的复述，而是要求读者解释：进程提供隔离的虚拟地址空间和资源身份，页表把虚拟页映射到物理页，线程则在该进程资源内执行。

复核 **分时系统** 时，为 1.5 我是一个进程 固定一个最小输入，先写出预期状态和输出，再改变与“退出回收”直接相连的一个条件。若出现“把指针值当成物理地址，或让两个进程未经共享映射直接访问同一虚拟地址”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

### 分块装入内存

在 **1.5 我是一个进程** 中，目录坐标 **分块装入内存** 对应“装载程序”这一步。它不是故事情节的复述，而是要求读者解释：进程提供隔离的虚拟地址空间和资源身份，页表把虚拟页映射到物理页，线程则在该进程资源内执行。

复核 **分块装入内存** 时，为 1.5 我是一个进程 固定一个最小输入，先写出预期状态和输出，再改变与“装载程序”直接相连的一个条件。若出现“把指针值当成物理地址，或让两个进程未经共享映射直接访问同一虚拟地址”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

### 虚拟内存：分页

在 **1.5 我是一个进程** 中，目录坐标 **虚拟内存：分页** 对应“创建进程”这一步。它不是故事情节的复述，而是要求读者解释：进程提供隔离的虚拟地址空间和资源身份，页表把虚拟页映射到物理页，线程则在该进程资源内执行。

复核 **虚拟内存：分页** 时，为 1.5 我是一个进程 固定一个最小输入，先写出预期状态和输出，再改变与“创建进程”直接相连的一个条件。若出现“把指针值当成物理地址，或让两个进程未经共享映射直接访问同一虚拟地址”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

### 分段 分页

在 **1.5 我是一个进程** 中，目录坐标 **分段 分页** 对应“映射页面”这一步。它不是故事情节的复述，而是要求读者解释：进程提供隔离的虚拟地址空间和资源身份，页表把虚拟页映射到物理页，线程则在该进程资源内执行。

复核 **分段 分页** 时，为 1.5 我是一个进程 固定一个最小输入，先写出预期状态和输出，再改变与“映射页面”直接相连的一个条件。若出现“把指针值当成物理地址，或让两个进程未经共享映射直接访问同一虚拟地址”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

### 程序的装载

在 **1.5 我是一个进程** 中，目录坐标 **程序的装载** 对应“调度线程”这一步。它不是故事情节的复述，而是要求读者解释：进程提供隔离的虚拟地址空间和资源身份，页表把虚拟页映射到物理页，线程则在该进程资源内执行。

复核 **程序的装载** 时，为 1.5 我是一个进程 固定一个最小输入，先写出预期状态和输出，再改变与“调度线程”直接相连的一个条件。若出现“把指针值当成物理地址，或让两个进程未经共享映射直接访问同一虚拟地址”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

### 线程

在 **1.5 我是一个进程** 中，目录坐标 **线程** 对应“退出回收”这一步。它不是故事情节的复述，而是要求读者解释：进程提供隔离的虚拟地址空间和资源身份，页表把虚拟页映射到物理页，线程则在该进程资源内执行。

复核 **线程** 时，为 1.5 我是一个进程 固定一个最小输入，先写出预期状态和输出，再改变与“退出回收”直接相连的一个条件。若出现“把指针值当成物理地址，或让两个进程未经共享映射直接访问同一虚拟地址”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

## 最小可重放实现

```text
# 1.5 我是一个进程：用事件轨迹验证状态转换
state = baseline_state()
for event in scenario:
    state = transition(state, event)
    assert invariant(state)
assert reset(state) == baseline_state()
```

这段实现草图只表达 1.5 我是一个进程 的验证合同，不复制书中叙事或代码。实际运行时应保存输入、环境、每个节点状态、最终结果和复位结果，使另一位读者能够从干净状态重放。

## 专属因果实验

先预测改变一个控制量后，1.5 我是一个进程 的哪一节点最先变化。依次切换基线、边界和注入故障，保存一次轨迹；随后点击“重置实验”，确认控件、节点选择、压力值和状态文本全部恢复。


## 故障诊断


## 术语与边界

1.5 我是一个进程 使用五个可验证术语：、、、、。术语若不能指向本页实验中的控件、节点或状态，就仍然只是目录名词。


## 本页小结

1.5 我是一个进程 的关键不是记住故事，而是能解释“进程提供隔离的虚拟地址空间和资源身份，页表把虚拟页映射到物理页，线程则在该进程资源内执行”。完成标准是用本页合同推出预期，沿五个专属节点观察实际，诊断“把指针值当成物理地址，或让两个进程未经共享映射直接访问同一虚拟地址”，并证明复位后的同一输入重新得到基线轨迹。


