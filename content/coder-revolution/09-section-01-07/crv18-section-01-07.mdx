---
title: "1.7 我是一个键盘"
type: B
section: "码农翻身（2018年第1版）"
order: 10
description: "1.7 我是一个键盘：从“CPU 持续等待每个设备”推进到“事件通知与批量搬运减少忙等”，用专属机制图、实验和故障诊断验证。"
demo: true
math: true
sourceUrl: "https://www.phei.com.cn/module/goods/wssd_content.jsp?bookid=52126"
qualityVersion: 2
practiceMode: calculation
sourceMode: independent-rewrite
draft: false
---

import { Crv18Section0107Lab } from "@/components/mdx/coder-revolution/diagrams/crv18-section-01-07";
import {
  Objectives,
  Callout,
  Term,
  Glossary,
  GlossaryItem,
  Exercises,
  Answer,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释 1.7 我是一个键盘 为什么把“CPU 持续等待每个设备”推进为“事件通知与批量搬运减少忙等”
- 能沿 按键扫描 → 控制器缓冲 → 中断通知 → 驱动读取 → 进程消费 说清对象、状态和边界
- 能用本页公式或状态合同完成一次手算、代码、模拟、诊断或方案判断
- 能注入“把中断与 DMA 都解释成设备直接执行应用代码，遗漏驱动和内核的边界”，定位最早偏离点并验证复位

</Objectives>

## 为什么需要这一机制

1.7 我是一个键盘 不能停在故事类比。真实系统要解决的变化是从 **CPU 持续等待每个设备** 到 **事件通知与批量搬运减少忙等**；可执行机制是：键盘经控制器和总线产生输入；轮询由 CPU 主动检查，中断在事件到来时通知，DMA 则让大块数据不必逐字节占用 CPU。

对 1.7 我是一个键盘 的验收从同一份最小输入开始，先预测 按键扫描、控制器缓冲、中断通知、驱动读取、进程消费 各自会看到什么，再运行正常、边界和故障场景。故事帮助记住矛盾，状态、协议、类型、构建或反馈证据才决定解释是否成立。

<Callout type="info" title="范围与事实来源">
  本页按电子工业出版社公开目录定位 1.7 我是一个键盘 的问题范围，并依据 [Linux
  kernel memory-management
  documentation](https://docs.kernel.org/admin-guide/mm/concepts.html)
  独立重写技术机制。目录只证明主题坐标，不代表取得或复现原书正文。
</Callout>

## 核心合同

$$
CPU\_cost_{poll}=checks\times cost_{check}
$$

在 1.7 我是一个键盘 中，公式或状态表达式里的量必须绑定到具体对象、单位、版本或生命周期。先给基线输入算出预期，再逐步观察；如果最终结果碰巧相同，但中途已违反 中断通知 或 驱动读取，仍应拒绝该实现。

## 目录节点到机制证据

### 1.7 我是一个键盘

在 **1.7 我是一个键盘** 中，目录坐标 **1.7 我是一个键盘** 对应“按键扫描”这一步。它不是故事情节的复述，而是要求读者解释：键盘经控制器和总线产生输入；轮询由 CPU 主动检查，中断在事件到来时通知，DMA 则让大块数据不必逐字节占用 CPU。

复核 **1.7 我是一个键盘** 时，为 1.7 我是一个键盘 固定一个最小输入，先写出预期状态和输出，再改变与“按键扫描”直接相连的一个条件。若出现“把中断与 DMA 都解释成设备直接执行应用代码，遗漏驱动和内核的边界”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

### 二等公民

在 **1.7 我是一个键盘** 中，目录坐标 **二等公民** 对应“控制器缓冲”这一步。它不是故事情节的复述，而是要求读者解释：键盘经控制器和总线产生输入；轮询由 CPU 主动检查，中断在事件到来时通知，DMA 则让大块数据不必逐字节占用 CPU。

复核 **二等公民** 时，为 1.7 我是一个键盘 固定一个最小输入，先写出预期状态和输出，再改变与“控制器缓冲”直接相连的一个条件。若出现“把中断与 DMA 都解释成设备直接执行应用代码，遗漏驱动和内核的边界”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

### 总线和端口

在 **1.7 我是一个键盘** 中，目录坐标 **总线和端口** 对应“中断通知”这一步。它不是故事情节的复述，而是要求读者解释：键盘经控制器和总线产生输入；轮询由 CPU 主动检查，中断在事件到来时通知，DMA 则让大块数据不必逐字节占用 CPU。

复核 **总线和端口** 时，为 1.7 我是一个键盘 固定一个最小输入，先写出预期状态和输出，再改变与“中断通知”直接相连的一个条件。若出现“把中断与 DMA 都解释成设备直接执行应用代码，遗漏驱动和内核的边界”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

### 轮询（程序式I/O）

在 **1.7 我是一个键盘** 中，目录坐标 **轮询（程序式I/O）** 对应“驱动读取”这一步。它不是故事情节的复述，而是要求读者解释：键盘经控制器和总线产生输入；轮询由 CPU 主动检查，中断在事件到来时通知，DMA 则让大块数据不必逐字节占用 CPU。

复核 **轮询（程序式I/O）** 时，为 1.7 我是一个键盘 固定一个最小输入，先写出预期状态和输出，再改变与“驱动读取”直接相连的一个条件。若出现“把中断与 DMA 都解释成设备直接执行应用代码，遗漏驱动和内核的边界”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

### 中断

在 **1.7 我是一个键盘** 中，目录坐标 **中断** 对应“进程消费”这一步。它不是故事情节的复述，而是要求读者解释：键盘经控制器和总线产生输入；轮询由 CPU 主动检查，中断在事件到来时通知，DMA 则让大块数据不必逐字节占用 CPU。

复核 **中断** 时，为 1.7 我是一个键盘 固定一个最小输入，先写出预期状态和输出，再改变与“进程消费”直接相连的一个条件。若出现“把中断与 DMA 都解释成设备直接执行应用代码，遗漏驱动和内核的边界”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

### DMA

在 **1.7 我是一个键盘** 中，目录坐标 **DMA** 对应“按键扫描”这一步。它不是故事情节的复述，而是要求读者解释：键盘经控制器和总线产生输入；轮询由 CPU 主动检查，中断在事件到来时通知，DMA 则让大块数据不必逐字节占用 CPU。

复核 **DMA** 时，为 1.7 我是一个键盘 固定一个最小输入，先写出预期状态和输出，再改变与“按键扫描”直接相连的一个条件。若出现“把中断与 DMA 都解释成设备直接执行应用代码，遗漏驱动和内核的边界”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

## 最小可重放实现

```text
# 1.7 我是一个键盘：先手算合同，再核对逐阶段轨迹
expected = calculate_reference(fixed_input)
trace = run_datapath(fixed_input)
assert trace.output == expected
assert run_datapath(fixed_input).output == expected
```

这段实现草图只表达 1.7 我是一个键盘 的验证合同，不复制书中叙事或代码。实际运行时应保存输入、环境、每个节点状态、最终结果和复位结果，使另一位读者能够从干净状态重放。

## 专属因果实验

先预测改变一个控制量后，1.7 我是一个键盘 的哪一节点最先变化。依次切换基线、边界和注入故障，保存一次轨迹；随后点击“重置实验”，确认控件、节点选择、压力值和状态文本全部恢复。

<Crv18Section0107Lab />

## 故障诊断

<Callout
  type="trap"
  title="误区：把中断与 DMA 都解释成设备直接执行应用代码，遗漏驱动和内核的边界"
>
  该错误会破坏 1.7 我是一个键盘 从“CPU
  持续等待每个设备”到“事件通知与批量搬运减少忙等”的转换。诊断时按 按键扫描 →
  控制器缓冲 → 中断通知 → 驱动读取 → 进程消费
  前进，只在首次出现预期与实际不一致的位置停下。
</Callout>

<Callout type="trap" title="误区：最终成功就算机制正确">
  1.7 我是一个键盘
  的最终输出可能被缓存、重试、默认值或偶然时序掩盖。必须同时保存中间状态、拒绝语义和复位重放；缺少任一项都不能据此推广到更大规模。
</Callout>

<Callout type="tip" title="恢复顺序">
  1.7 我是一个键盘 的恢复顺序是：还原输入与环境 → 清理派生状态 → 重建当前边界 →
  重跑机制链 → 对照公式或合同 → 再次注入同一故障确认可重复拒绝。
</Callout>

## 术语与边界

1.7 我是一个键盘 使用五个可验证术语：<Term def="按键扫描 是 1.7 我是一个键盘 机制链中的可观察状态或边界，必须用实际输入和轨迹验证">按键扫描</Term>、<Term def="控制器缓冲 是 1.7 我是一个键盘 机制链中的可观察状态或边界，必须用实际输入和轨迹验证">控制器缓冲</Term>、<Term def="中断通知 是 1.7 我是一个键盘 机制链中的可观察状态或边界，必须用实际输入和轨迹验证">中断通知</Term>、<Term def="驱动读取 是 1.7 我是一个键盘 机制链中的可观察状态或边界，必须用实际输入和轨迹验证">驱动读取</Term>、<Term def="进程消费 是 1.7 我是一个键盘 机制链中的可观察状态或边界，必须用实际输入和轨迹验证">进程消费</Term>。术语若不能指向本页实验中的控件、节点或状态，就仍然只是目录名词。

<Glossary>

<GlossaryItem term="按键扫描">
  按键扫描 位于 1.7 我是一个键盘 的第 1
  个检查点；验收记录必须包含它的输入、状态、输出和故障表现。
</GlossaryItem>

<GlossaryItem term="控制器缓冲">
  控制器缓冲 位于 1.7 我是一个键盘 的第 2
  个检查点；验收记录必须包含它的输入、状态、输出和故障表现。
</GlossaryItem>

<GlossaryItem term="中断通知">
  中断通知 位于 1.7 我是一个键盘 的第 3
  个检查点；验收记录必须包含它的输入、状态、输出和故障表现。
</GlossaryItem>

<GlossaryItem term="驱动读取">
  驱动读取 位于 1.7 我是一个键盘 的第 4
  个检查点；验收记录必须包含它的输入、状态、输出和故障表现。
</GlossaryItem>

<GlossaryItem term="进程消费">
  进程消费 位于 1.7 我是一个键盘 的第 5
  个检查点；验收记录必须包含它的输入、状态、输出和故障表现。
</GlossaryItem>

</Glossary>

## 本页小结

1.7 我是一个键盘 的关键不是记住故事，而是能解释“键盘经控制器和总线产生输入；轮询由 CPU 主动检查，中断在事件到来时通知，DMA 则让大块数据不必逐字节占用 CPU”。完成标准是用本页合同推出预期，沿五个专属节点观察实际，诊断“把中断与 DMA 都解释成设备直接执行应用代码，遗漏驱动和内核的边界”，并证明复位后的同一输入重新得到基线轨迹。

<Exercises>

1. 1.7 我是一个键盘 的五节点链中，哪个状态最能区分“CPU 持续等待每个设备”和“事件通知与批量搬运减少忙等”？请给出一个最小样本。

<Answer>
  先检查 按键扫描 的输入身份，再观察 控制器缓冲 与
  中断通知。只改变一个条件时，最早发生可解释变化的节点就是区分两种状态的证据；目录节点逐项核对为：1.7
  我是一个键盘（检查 按键扫描）；二等公民（检查 控制器缓冲）；总线和端口（检查
  中断通知）；轮询（程序式I/O）（检查 驱动读取）；中断（检查
  进程消费）；DMA（检查 按键扫描）。
</Answer>

2. 使用本页核心合同完成一次手算、状态推演或代码断言，并说明每个量对应哪个真实对象。

<Answer>
  为 1.7 我是一个键盘 写下输入值、单位、版本和边界，按公式得到预期，再与实验中的
  中断通知 和 进程消费
  比较。若发生隐式单位转换、状态复用或顺序变化，必须先修正模型，不能只调整答案。
</Answer>

3. 注入“把中断与 DMA 都解释成设备直接执行应用代码，遗漏驱动和内核的边界”后，怎样证明恢复动作有效而非偶然成功？

<Answer>
  保存故障前基线与最早偏离点，执行本页恢复顺序，再用完全相同输入运行两次。1.7
  我是一个键盘
  只有在控件状态、五节点轨迹、最终输出和拒绝结果都可重复时才算恢复完成。
</Answer>

</Exercises>

<Attribution />
