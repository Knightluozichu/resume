---
title: "1.12 编程世界的那把锁"
type: B
section: "码农翻身（2018年第1版）"
order: 15
description: "1.12 编程世界的那把锁：从“多个线程任意改写共享变量”推进到“同步原语声明访问顺序与容量”，用专属机制图、实验和故障诊断验证。"
demo: true
math: true
sourceUrl: "https://www.phei.com.cn/module/goods/wssd_content.jsp?bookid=52126"
qualityVersion: 2
practiceMode: simulation
sourceMode: independent-rewrite
draft: true
---

import { Crv18Section0112Lab } from "@/components/mdx/coder-revolution/diagrams/crv18-section-01-12";
import {
  Objectives,
  Callout,
  Term,
  Glossary,
  GlossaryItem,
  Exercises,
  Answer,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释 1.12 编程世界的那把锁 为什么把“多个线程任意改写共享变量”推进为“同步原语声明访问顺序与容量”
- 能沿 请求进入 → 获取许可 → 修改共享态 → 发布可见性 → 释放许可 说清对象、状态和边界
- 能用本页公式或状态合同完成一次手算、代码、模拟、诊断或方案判断
- 能注入“在异常路径漏掉解锁或 release，使后续线程永久等待”，定位最早偏离点并验证复位

</Objectives>

## 为什么需要这一机制

1.12 编程世界的那把锁 不能停在故事类比。真实系统要解决的变化是从 **多个线程任意改写共享变量** 到 **同步原语声明访问顺序与容量**；可执行机制是：互斥锁保护临界区，信号量以许可数约束同时进入者；正确性取决于共享状态的原子边界与先行发生关系。

对 1.12 编程世界的那把锁 的验收从同一份最小输入开始，先预测 请求进入、获取许可、修改共享态、发布可见性、释放许可 各自会看到什么，再运行正常、边界和故障场景。故事帮助记住矛盾，状态、协议、类型、构建或反馈证据才决定解释是否成立。

<Callout type="info" title="范围与事实来源">
  本页按电子工业出版社公开目录定位 1.12 编程世界的那把锁 的问题范围，并依据
  [Linux man-pages: POSIX threads
  overview](https://man7.org/linux/man-pages/man7/pthreads.7.html)
  独立重写技术机制。目录只证明主题坐标，不代表取得或复现原书正文。
</Callout>

## 核心合同

$$
permits_{after}=permits_{before}-acquire+release
$$

在 1.12 编程世界的那把锁 中，公式或状态表达式里的量必须绑定到具体对象、单位、版本或生命周期。先给基线输入算出预期，再逐步观察；如果最终结果碰巧相同，但中途已违反 修改共享态 或 发布可见性，仍应拒绝该实现。

## 目录节点到机制证据

### 1.12 编程世界的那把锁

在 **1.12 编程世界的那把锁** 中，目录坐标 **1.12 编程世界的那把锁** 对应“请求进入”这一步。它不是故事情节的复述，而是要求读者解释：互斥锁保护临界区，信号量以许可数约束同时进入者；正确性取决于共享状态的原子边界与先行发生关系。

复核 **1.12 编程世界的那把锁** 时，为 1.12 编程世界的那把锁 固定一个最小输入，先写出预期状态和输出，再改变与“请求进入”直接相连的一个条件。若出现“在异常路径漏掉解锁或 release，使后续线程永久等待”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

### 共享变量惹的祸

在 **1.12 编程世界的那把锁** 中，目录坐标 **共享变量惹的祸** 对应“获取许可”这一步。它不是故事情节的复述，而是要求读者解释：互斥锁保护临界区，信号量以许可数约束同时进入者；正确性取决于共享状态的原子边界与先行发生关系。

复核 **共享变量惹的祸** 时，为 1.12 编程世界的那把锁 固定一个最小输入，先写出预期状态和输出，再改变与“获取许可”直接相连的一个条件。若出现“在异常路径漏掉解锁或 release，使后续线程永久等待”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

### 争抢吧，线程

在 **1.12 编程世界的那把锁** 中，目录坐标 **争抢吧，线程** 对应“修改共享态”这一步。它不是故事情节的复述，而是要求读者解释：互斥锁保护临界区，信号量以许可数约束同时进入者；正确性取决于共享状态的原子边界与先行发生关系。

复核 **争抢吧，线程** 时，为 1.12 编程世界的那把锁 固定一个最小输入，先写出预期状态和输出，再改变与“修改共享态”直接相连的一个条件。若出现“在异常路径漏掉解锁或 release，使后续线程永久等待”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

### 改进

在 **1.12 编程世界的那把锁** 中，目录坐标 **改进** 对应“发布可见性”这一步。它不是故事情节的复述，而是要求读者解释：互斥锁保护临界区，信号量以许可数约束同时进入者；正确性取决于共享状态的原子边界与先行发生关系。

复核 **改进** 时，为 1.12 编程世界的那把锁 固定一个最小输入，先写出预期状态和输出，再改变与“发布可见性”直接相连的一个条件。若出现“在异常路径漏掉解锁或 release，使后续线程永久等待”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

### 信号量

在 **1.12 编程世界的那把锁** 中，目录坐标 **信号量** 对应“释放许可”这一步。它不是故事情节的复述，而是要求读者解释：互斥锁保护临界区，信号量以许可数约束同时进入者；正确性取决于共享状态的原子边界与先行发生关系。

复核 **信号量** 时，为 1.12 编程世界的那把锁 固定一个最小输入，先写出预期状态和输出，再改变与“释放许可”直接相连的一个条件。若出现“在异常路径漏掉解锁或 release，使后续线程永久等待”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

## 最小可重放实现

```text
# 1.12 编程世界的那把锁：用事件轨迹验证状态转换
state = baseline_state()
for event in scenario:
    state = transition(state, event)
    assert invariant(state)
assert reset(state) == baseline_state()
```

这段实现草图只表达 1.12 编程世界的那把锁 的验证合同，不复制书中叙事或代码。实际运行时应保存输入、环境、每个节点状态、最终结果和复位结果，使另一位读者能够从干净状态重放。

## 专属因果实验

先预测改变一个控制量后，1.12 编程世界的那把锁 的哪一节点最先变化。依次切换基线、边界和注入故障，保存一次轨迹；随后点击“重置实验”，确认控件、节点选择、压力值和状态文本全部恢复。

<Crv18Section0112Lab />

## 故障诊断

<Callout
  type="trap"
  title="误区：在异常路径漏掉解锁或 release，使后续线程永久等待"
>
  该错误会破坏 1.12 编程世界的那把锁
  从“多个线程任意改写共享变量”到“同步原语声明访问顺序与容量”的转换。诊断时按
  请求进入 → 获取许可 → 修改共享态 → 发布可见性 → 释放许可
  前进，只在首次出现预期与实际不一致的位置停下。
</Callout>

<Callout type="trap" title="误区：最终成功就算机制正确">
  1.12 编程世界的那把锁
  的最终输出可能被缓存、重试、默认值或偶然时序掩盖。必须同时保存中间状态、拒绝语义和复位重放；缺少任一项都不能据此推广到更大规模。
</Callout>

<Callout type="tip" title="恢复顺序">
  1.12 编程世界的那把锁 的恢复顺序是：还原输入与环境 → 清理派生状态 →
  重建当前边界 → 重跑机制链 → 对照公式或合同 → 再次注入同一故障确认可重复拒绝。
</Callout>

## 术语与边界

1.12 编程世界的那把锁 使用五个可验证术语：<Term def="请求进入 是 1.12 编程世界的那把锁 机制链中的可观察状态或边界，必须用实际输入和轨迹验证">请求进入</Term>、<Term def="获取许可 是 1.12 编程世界的那把锁 机制链中的可观察状态或边界，必须用实际输入和轨迹验证">获取许可</Term>、<Term def="修改共享态 是 1.12 编程世界的那把锁 机制链中的可观察状态或边界，必须用实际输入和轨迹验证">修改共享态</Term>、<Term def="发布可见性 是 1.12 编程世界的那把锁 机制链中的可观察状态或边界，必须用实际输入和轨迹验证">发布可见性</Term>、<Term def="释放许可 是 1.12 编程世界的那把锁 机制链中的可观察状态或边界，必须用实际输入和轨迹验证">释放许可</Term>。术语若不能指向本页实验中的控件、节点或状态，就仍然只是目录名词。

<Glossary>

<GlossaryItem term="请求进入">
  请求进入 位于 1.12 编程世界的那把锁 的第 1
  个检查点；验收记录必须包含它的输入、状态、输出和故障表现。
</GlossaryItem>

<GlossaryItem term="获取许可">
  获取许可 位于 1.12 编程世界的那把锁 的第 2
  个检查点；验收记录必须包含它的输入、状态、输出和故障表现。
</GlossaryItem>

<GlossaryItem term="修改共享态">
  修改共享态 位于 1.12 编程世界的那把锁 的第 3
  个检查点；验收记录必须包含它的输入、状态、输出和故障表现。
</GlossaryItem>

<GlossaryItem term="发布可见性">
  发布可见性 位于 1.12 编程世界的那把锁 的第 4
  个检查点；验收记录必须包含它的输入、状态、输出和故障表现。
</GlossaryItem>

<GlossaryItem term="释放许可">
  释放许可 位于 1.12 编程世界的那把锁 的第 5
  个检查点；验收记录必须包含它的输入、状态、输出和故障表现。
</GlossaryItem>

</Glossary>

## 本页小结

1.12 编程世界的那把锁 的关键不是记住故事，而是能解释“互斥锁保护临界区，信号量以许可数约束同时进入者；正确性取决于共享状态的原子边界与先行发生关系”。完成标准是用本页合同推出预期，沿五个专属节点观察实际，诊断“在异常路径漏掉解锁或 release，使后续线程永久等待”，并证明复位后的同一输入重新得到基线轨迹。

<Exercises>

1. 1.12 编程世界的那把锁 的五节点链中，哪个状态最能区分“多个线程任意改写共享变量”和“同步原语声明访问顺序与容量”？请给出一个最小样本。

<Answer>
  先检查 请求进入 的输入身份，再观察 获取许可 与
  修改共享态。只改变一个条件时，最早发生可解释变化的节点就是区分两种状态的证据；目录节点逐项核对为：1.12
  编程世界的那把锁（检查 请求进入）；共享变量惹的祸（检查
  获取许可）；争抢吧，线程（检查 修改共享态）；改进（检查
  发布可见性）；信号量（检查 释放许可）。
</Answer>

2. 使用本页核心合同完成一次手算、状态推演或代码断言，并说明每个量对应哪个真实对象。

<Answer>
  为 1.12 编程世界的那把锁
  写下输入值、单位、版本和边界，按公式得到预期，再与实验中的 修改共享态 和
  释放许可
  比较。若发生隐式单位转换、状态复用或顺序变化，必须先修正模型，不能只调整答案。
</Answer>

3. 注入“在异常路径漏掉解锁或 release，使后续线程永久等待”后，怎样证明恢复动作有效而非偶然成功？

<Answer>
  保存故障前基线与最早偏离点，执行本页恢复顺序，再用完全相同输入运行两次。1.12
  编程世界的那把锁
  只有在控件状态、五节点轨迹、最终输出和拒绝结果都可重复时才算恢复完成。
</Answer>

</Exercises>

<Attribution />
