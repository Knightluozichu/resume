---
title: "1.13 绕不开的加法器"
type: B
section: "码农翻身（2018年第1版）"
order: 16
description: "1.13 绕不开的加法器：从“加法被当成单个不可拆黑盒”推进到“布尔门和进位链解释整数加法”，用专属机制图、实验和故障诊断验证。"
demo: true
math: true
sourceUrl: "https://www.phei.com.cn/module/goods/wssd_content.jsp?bookid=52126"
qualityVersion: 2
practiceMode: calculation
sourceMode: independent-rewrite
draft: false
---


## 为什么需要这一机制

1.13 绕不开的加法器 不能停在故事类比。真实系统要解决的变化是从 **加法被当成单个不可拆黑盒** 到 **布尔门和进位链解释整数加法**；可执行机制是：半加器用 XOR 产生和、AND 产生进位；全加器再合并输入进位，多个全加器串接构成多位加法。

对 1.13 绕不开的加法器 的验收从同一份最小输入开始，先预测 输入位、半加和、生成进位、合并进位、输出多位 各自会看到什么，再运行正常、边界和故障场景。故事帮助记住矛盾，状态、协议、类型、构建或反馈证据才决定解释是否成立。


## 核心合同

$$
sum=a\oplus b\oplus c_{in},\quad c_{out}=ab+c_{in}(a\oplus b)
$$

在 1.13 绕不开的加法器 中，公式或状态表达式里的量必须绑定到具体对象、单位、版本或生命周期。先给基线输入算出预期，再逐步观察；如果最终结果碰巧相同，但中途已违反 生成进位 或 合并进位，仍应拒绝该实现。

## 目录节点到机制证据

### 1.13 绕不开的加法器

在 **1.13 绕不开的加法器** 中，目录坐标 **1.13 绕不开的加法器** 对应“输入位”这一步。它不是故事情节的复述，而是要求读者解释：半加器用 XOR 产生和、AND 产生进位；全加器再合并输入进位，多个全加器串接构成多位加法。

复核 **1.13 绕不开的加法器** 时，为 1.13 绕不开的加法器 固定一个最小输入，先写出预期状态和输出，再改变与“输入位”直接相连的一个条件。若出现“只计算每一位的 XOR 而未把低位进位送到高位，遇到 1+1 即失败”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

## 最小可重放实现

```text
# 1.13 绕不开的加法器：先手算合同，再核对逐阶段轨迹
expected = calculate_reference(fixed_input)
trace = run_datapath(fixed_input)
assert trace.output == expected
assert run_datapath(fixed_input).output == expected
```

这段实现草图只表达 1.13 绕不开的加法器 的验证合同，不复制书中叙事或代码。实际运行时应保存输入、环境、每个节点状态、最终结果和复位结果，使另一位读者能够从干净状态重放。

## 专属因果实验

先预测改变一个控制量后，1.13 绕不开的加法器 的哪一节点最先变化。依次切换基线、边界和注入故障，保存一次轨迹；随后点击“重置实验”，确认控件、节点选择、压力值和状态文本全部恢复。


## 故障诊断


## 术语与边界

1.13 绕不开的加法器 使用五个可验证术语：、、、、。术语若不能指向本页实验中的控件、节点或状态，就仍然只是目录名词。


## 本页小结

1.13 绕不开的加法器 的关键不是记住故事，而是能解释“半加器用 XOR 产生和、AND 产生进位；全加器再合并输入进位，多个全加器串接构成多位加法”。完成标准是用本页合同推出预期，沿五个专属节点观察实际，诊断“只计算每一位的 XOR 而未把低位进位送到高位，遇到 1+1 即失败”，并证明复位后的同一输入重新得到基线轨迹。


