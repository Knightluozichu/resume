---
title: "1.14 递归那点事儿"
type: B
section: "码农翻身（2018年第1版）"
order: 17
description: "1.14 递归那点事儿：从“函数只能由循环重复”推进到“调用栈承载分解与回溯”，用专属机制图、实验和故障诊断验证。"
demo: true
math: true
sourceUrl: "https://www.phei.com.cn/module/goods/wssd_content.jsp?bookid=52126"
qualityVersion: 2
practiceMode: calculation
sourceMode: independent-rewrite
draft: false
---


## 为什么需要这一机制

1.14 递归那点事儿 不能停在故事类比。真实系统要解决的变化是从 **函数只能由循环重复** 到 **调用栈承载分解与回溯**；可执行机制是：递归调用必须有可达基线，每次调用创建独立栈帧并缩小问题；返回值沿调用链反向合并。

对 1.14 递归那点事儿 的验收从同一份最小输入开始，先预测 检查基线、缩小输入、压入栈帧、得到子结果、返回合并 各自会看到什么，再运行正常、边界和故障场景。故事帮助记住矛盾，状态、协议、类型、构建或反馈证据才决定解释是否成立。


## 核心合同

$$
T(n)=T(n-1)+O(1),\quad S(n)=O(n)
$$

在 1.14 递归那点事儿 中，公式或状态表达式里的量必须绑定到具体对象、单位、版本或生命周期。先给基线输入算出预期，再逐步观察；如果最终结果碰巧相同，但中途已违反 压入栈帧 或 得到子结果，仍应拒绝该实现。

## 目录节点到机制证据

### 1.14 递归那点事儿

在 **1.14 递归那点事儿** 中，目录坐标 **1.14 递归那点事儿** 对应“检查基线”这一步。它不是故事情节的复述，而是要求读者解释：递归调用必须有可达基线，每次调用创建独立栈帧并缩小问题；返回值沿调用链反向合并。

复核 **1.14 递归那点事儿** 时，为 1.14 递归那点事儿 固定一个最小输入，先写出预期状态和输出，再改变与“检查基线”直接相连的一个条件。若出现“递归分支没有严格逼近基线，造成无限递归或栈溢出”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

## 最小可重放实现

```text
// 1.14 递归那点事儿：比较语义，不比较口号
const input = fixedCase();
const expected = referenceModel(input);
const actual = runProgram(input);
assert.deepEqual(actual, expected);
assert.deepEqual(resetAndRun(input), expected);
```

这段实现草图只表达 1.14 递归那点事儿 的验证合同，不复制书中叙事或代码。实际运行时应保存输入、环境、每个节点状态、最终结果和复位结果，使另一位读者能够从干净状态重放。

## 专属因果实验

先预测改变一个控制量后，1.14 递归那点事儿 的哪一节点最先变化。依次切换基线、边界和注入故障，保存一次轨迹；随后点击“重置实验”，确认控件、节点选择、压力值和状态文本全部恢复。


## 故障诊断


## 术语与边界

1.14 递归那点事儿 使用五个可验证术语：、、、、。术语若不能指向本页实验中的控件、节点或状态，就仍然只是目录名词。


## 本页小结

1.14 递归那点事儿 的关键不是记住故事，而是能解释“递归调用必须有可达基线，每次调用创建独立栈帧并缩小问题；返回值沿调用链反向合并”。完成标准是用本页合同推出预期，沿五个专属节点观察实际，诊断“递归分支没有严格逼近基线，造成无限递归或栈溢出”，并证明复位后的同一输入重新得到基线轨迹。


