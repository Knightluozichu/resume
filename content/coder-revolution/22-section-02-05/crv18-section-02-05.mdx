---
title: "2.5 Java帝国之宫廷内斗"
type: B
section: "码农翻身（2018年第1版）"
order: 23
description: "把跨资源事务拆成 JDBC 参与者、JTA 协调、prepare 投票、全局决定和恢复，理解原子性与可用性的代价。"
demo: true
math: true
sourceUrl: "https://www.phei.com.cn/module/goods/wssd_content.jsp?bookid=52126"
qualityVersion: 2
practiceMode: diagnosis
sourceMode: independent-rewrite
officialUnitId: crv18-section-02-05
draft: false
---

import { Objectives } from "@/components/mdx/objectives";
import { Term } from "@/components/mdx/term";
import { Callout } from "@/components/mdx/callout";
import { Step, Stepper } from "@/components/mdx/stepper";
import { Answer, Exercises } from "@/components/mdx/exercises";
import { Glossary, GlossaryItem } from "@/components/mdx/glossary";
import { Attribution } from "@/components/mdx/attribution";
import {
  Crv18Section0205EvidenceDiagram,
  Crv18Section0205TwoPhaseLab,
  Crv18Section0205TwoPhaseDiagram,
} from "@/components/mdx/coder-revolution/diagrams/crv18-section-02-05-diagram";

<Objectives>

- 能沿 JTA 事务、协调者、JDBC 参与者和两个资源追踪 prepare/commit/rollback 状态
- 能解释两阶段提交如何换取全局原子决定，以及为什么会引入锁、阻塞和恢复日志
- 能用正常、边界和单一故障样本定位投票拒绝、协调者失联、重试重复和基本可用降级

</Objectives>

## 2.5 Java帝国之宫廷内斗

本页依据刘欣《码农翻身》（2018 年第 1 版）及出版社公开书目信息，独立重构 **2.5 Java帝国之宫廷内斗**。正文、代码、图示、实验和练习都是本课程重新设计的教学材料，不复制原书正文、插图、练习答案或代码。

跨两个数据库、消息资源或其他可参与资源写入时，各自本地提交不足以证明全局原子性。JTA 为应用提供全局事务边界，协调者先让每个 JDBC 参与者执行并准备，再根据所有投票广播 commit 或 rollback。它减少“半边成功”的概率，却要求参与者在不确定时保留锁和 prepared 状态，因而增加阻塞、超时和恢复复杂度。

<Crv18Section0205TwoPhaseDiagram />

## 三个会让全局事务失真的陷阱

<Callout type="trap" title="把 prepare 当成已经提交">

- **现象**：参与者返回 prepared 后，应用立即释放锁或对外发送“成功”事件。
- **原因**：prepare 只是承诺可以提交，最终决定尚未到达；资源仍可能需要保持恢复所需状态。
- **修法**：区分 prepared、committed、rolled back 和 in-doubt，外部成功通知绑定最终决定或幂等事件。

</Callout>

<Callout type="trap" title="把 JTA 当成所有副作用的事务">

- **现象**：数据库回滚了，但邮件、文件、缓存或远程 HTTP 已经产生不可撤销副作用。
- **原因**：JTA 只协调已登记的事务资源，未登记的系统不自动参与两阶段协议。
- **修法**：外部副作用使用 outbox、幂等键、补偿动作或明确的最终一致性合同。

</Callout>

<Callout type="trap" title="用基本可用掩盖未知决定">

- **现象**：协调者失联时，服务直接放行后续写入，导致同一业务键出现两种状态。
- **原因**：把“系统还能响应”误当成“事务决定已经知道”；一致性与可用性之间的取舍没有写进策略。
- **修法**：对 in-doubt 事务选择阻塞、只读、隔离或补偿，并记录恢复来源；不能静默猜测 commit。

</Callout>

## 十个目录节点到协议证据

### JDBC大臣

<Term def="代表登记在全局事务中的 JDBC 连接与本地数据库资源，负责执行 SQL、准备、锁定和报告投票。">
  JDBC大臣
</Term>
不是一个会自动服从命令的对象。参与者必须能保存 prepare 状态、响应
commit/rollback、报告失败，并把本地日志和锁纳入恢复。连接池、自动提交和隔离级别会影响它能否诚实投票。

### 密谋

<Term def="协调者与参与者在第一阶段交换本地可提交条件、写入预备日志并等待全局决定的协议阶段。">
  密谋
</Term>
可理解为 prepare 阶段的内部协商。参与者要检查约束、锁和日志；返回 yes
代表“我可以按协议提交”，不是“业务已经完成”。任何一个 no 都足以让协调者选择回滚。

### 两阶段提交

<Term def="先收集所有参与者 prepare 投票，再广播统一 commit 或 rollback 决定的分布式原子提交协议。">
  两阶段提交
</Term>
的第一阶段保证协调者知道谁能提交，第二阶段传播唯一决定。它不能消灭协调者、网络或参与者故障；prepare
后失联会留下 in-doubt 与阻塞窗口，因此必须有超时、日志和恢复协议。

### JTA

<Term def="Java 应用通过统一事务接口声明全局事务、登记资源、提交或回滚的协调抽象。">
  JTA
</Term>
把应用和具体事务管理器解耦，但事务管理器仍需知道资源、超时、传播和恢复。JTA
的提交结果不覆盖未登记的外部服务，也不自动让本地非事务操作具备原子性。

### 塞翁失马，焉知非福

<Term def="一次资源或性能损失可能暴露更深的边界问题，要求把失败样本转化为协议与恢复证据。">
  塞翁失马，焉知非福
</Term>
不是为故障找乐观解释，而是提醒保留失败轨迹：某次超时也许揭示锁没有释放，某次回滚也许揭示事件发送早于提交。修复要让同一故障重放时得到更清晰的拒绝或恢复。

### 基本可用

<Term def="在全局决定未知、资源部分不可达或一致性代价过高时，明确允许哪些只读、排队、隔离或降级行为。">
  基本可用
</Term>
不是无条件放行。设计时先声明业务可接受的降级：读旧快照、排队等待、拒绝写入或进入补偿；每种选择都要标注可能的陈旧、重复或延迟，并能在恢复后对账。

### 走漏风声

<Term def="协调决定、参与者日志或外部事件在错误时机泄露，导致未提交状态被下游当成最终事实。">
  走漏风声
</Term>
提示观察通知顺序。提交事件应在最终决定可确认后发送，或携带幂等键与可追踪状态；prepare、锁等待和本地中间值不应冒充业务成功。

### 宫廷激辩

<Term def="围绕原子性、可用性、延迟、锁和补偿的工程取舍进行可验证讨论，而不是用单一指标决定协议。">
  宫廷激辩
</Term>
要求把争论变成场景表：哪些资源必须原子，哪些可以最终一致，协调者失联时是否阻塞，业务能接受多长恢复时间。没有这些输入，“选择
2PC”或“拒绝 2PC”都只是口号。

### 2.5 Java帝国之宫廷内斗

这场“内斗”的工程事实是多个事务参与者共享一个全局决定，却各自拥有本地锁、日志和失败模式。**2.5 Java帝国之宫廷内斗**不是在赞美或否定 JTA，而是训练读者沿 prepare、决定、释放和恢复验证原子性，以及诚实记录可用性代价。

## 最小两阶段提交合同

```java
enum Vote { YES, NO }
enum Decision { COMMIT, ROLLBACK, IN_DOUBT }

record Participant(String id, Vote vote, boolean prepared, boolean released) {}

static Decision decide(List<Participant> participants, boolean coordinatorAlive) {
  if (!coordinatorAlive) return Decision.IN_DOUBT;
  return participants.stream().allMatch(p -> p.vote() == Vote.YES && p.prepared())
      ? Decision.COMMIT
      : Decision.ROLLBACK;
}
```

合同把 `prepared`、最终 `Decision` 和 `released` 分开。协调者存活且所有参与者都准备好，才可广播 commit；任何 no 选择 rollback；协调者失联时只能记录 in-doubt，不能凭超时猜测成功。外部副作用还需要独立的幂等或补偿设计。

## 四步复核一个全局事务

<Stepper>

<Step title="1. 开启 JTA 事务并登记参与者">

列出事务边界、JDBC 连接、资源身份、超时和传播方式。正常样本登记两个参与者；边界样本缺少一个资源或混入未登记副作用，观察合同如何拒绝。

<Crv18Section0205TwoPhaseDiagram />

</Step>

<Step title="2. 执行本地写入并准备投票">

每个参与者检查约束、写日志、持有必要锁并返回 yes/no。保存 SQL、版本、锁、日志和投票；prepare 不是提交，不能提前释放或通知业务成功。

<Crv18Section0205EvidenceDiagram />

</Step>

<Step title="3. 广播统一决定">

协调者持久化决定后广播 commit 或 rollback，参与者执行、确认并释放资源。模拟一个 no、一个超时和一个重复决定，检查决定幂等。

<Crv18Section0205TwoPhaseLab />

</Step>

<Step title="4. 处理失联与基本可用策略">

在 prepare 后让协调者或参与者失联，记录 in-doubt、锁和恢复日志。选择阻塞、只读、排队或补偿前，先写出一致性和业务可用性承诺，再从干净状态重放。

<Crv18Section0205EvidenceDiagram />

</Step>

</Stepper>

<Crv18Section0205TwoPhaseLab />

## 正常、边界与单一故障证据

<Crv18Section0205EvidenceDiagram />

| 样本     | 只改变的变量                       | 预期判定                       | 必存证据                     |
| -------- | ---------------------------------- | ------------------------------ | ---------------------------- |
| 正常     | 所有参与者 prepare=yes，协调者可达 | 统一 commit 后释放资源         | 投票、决定日志、确认、锁释放 |
| 边界     | 一个参与者 no、超时或重复决定      | 全局 rollback 或幂等确认       | 参与者状态、超时、重试与版本 |
| 单一故障 | prepare 后协调者失联               | in-doubt 可恢复，不猜测 commit | 日志、锁、恢复来源、降级策略 |

## 故障诊断：先确认决定是否存在

1. **登记层**：确认 JTA 事务是否包含所有需要原子的资源，外部邮件/HTTP/缓存是否另有合同。
2. **投票层**：查看每个参与者的本地约束、日志、锁和 prepare 结果；一个 no 不应被转换为 yes。
3. **决定层**：查协调者的持久化决定和广播确认；区分 rollback、commit 与 in-doubt，不能以连接超时推断结果。
4. **恢复层**：根据日志恢复参与者，释放锁并执行幂等补偿；基本可用策略必须保留一致性和陈旧性说明。

如果数据库都已提交但消息未发出，检查 outbox；如果一个参与者持锁等待，先查协调者决定日志；如果重复提交，检查决定幂等和客户端重试键。每个故障都要能从唯一业务身份重放。

## 最小全局事务证据包与反例

证据包包含全局事务 ID、JTA 传播、参与者列表、连接/资源身份、SQL、投票、prepare 日志、协调决定、广播确认、锁、超时、重试、外部副作用和恢复结果。敏感数据可脱敏，但不能删除决定链和参与者身份。

反例一是参与者全部 prepare 后协调者失联，系统为了“基本可用”直接放行同一资源的下一次写入；反例二是数据库回滚但邮件已发送。保留未知状态，再用决定日志、outbox 或补偿重放，证明修复没有把未知变成猜测。

## 术语表

<Glossary>
  <GlossaryItem term="JDBC大臣">
    登记在全局事务中的 JDBC 参与者，负责本地 SQL、prepare、决定确认和资源释放。
  </GlossaryItem>
  <GlossaryItem term="密谋">
    参与者检查本地条件并写预备日志、返回投票的 prepare 阶段。
  </GlossaryItem>
  <GlossaryItem term="两阶段提交">
    先收集 prepare 投票，再广播统一 commit 或 rollback 的协议。
  </GlossaryItem>
  <GlossaryItem term="JTA">
    Java 应用声明全局事务、登记资源并交由事务管理器协调的抽象。
  </GlossaryItem>
  <GlossaryItem term="塞翁失马，焉知非福">
    将失败样本转化为协议、恢复和可重放证据的复盘视角。
  </GlossaryItem>
  <GlossaryItem term="基本可用">
    在决定未知或资源不可达时明确允许的只读、排队、隔离或补偿策略。
  </GlossaryItem>
  <GlossaryItem term="走漏风声">
    未提交状态或中间决定过早泄露，被下游误当成最终业务事实。
  </GlossaryItem>
  <GlossaryItem term="宫廷激辩">
    围绕原子性、可用性、延迟、锁和补偿做场景化工程取舍。
  </GlossaryItem>
</Glossary>

## 练习

<Exercises>

**问题 1：** 两个参与者都返回 prepare=yes，但协调者在广播决定前宕机。系统为什么不能直接当作已提交？

<Answer>

prepare 只代表参与者承诺可以提交，最终决定可能还没有持久化或广播。查协调者日志；若没有决定，保持 in-doubt 并按恢复协议处理，不能依据超时猜测 commit。锁和资源状态要在决定明确后释放。

</Answer>

**问题 2：** JTA 事务回滚了数据库，但邮件已经发出。JTA 哪里没有覆盖？

<Answer>

邮件服务不是登记在该 JTA 事务中的资源，数据库回滚不会撤销外部副作用。用事务外盒子在数据库提交后发布、为邮件设置幂等键，或设计可补偿动作，并在证据中区分数据库决定与邮件状态。

</Answer>

**问题 3：** 为了保持基本可用，协调者失联时允许读写同一订单。怎样证明这不是静默破坏一致性？

<Answer>

先定义可接受的陈旧、排队、只读或隔离策略，给未知事务标记唯一状态；新写入必须避免覆盖 in-doubt 版本，并保留日志与对账路径。恢复后按决定日志或补偿重放，不能把“服务响应了”当作原子提交证明。

</Answer>

</Exercises>

<Attribution
  sources={[
    {
      label: "本书版本与目录范围依据：中国电子工业出版社《码农翻身》书目页",
      url: "https://www.phei.com.cn/module/goods/wssd_content.jsp?bookid=52126",
    },
    {
      label: "事务、两阶段提交参与者与恢复边界参考：PostgreSQL 官方文档",
      url: "https://www.postgresql.org/docs/current/two-phase.html",
    },
    {
      label: "作者公开文章与技术写作范围交叉核对：刘欣博客",
      url: "https://liuxiaochen.blog.csdn.net/",
    },
  ]}
  note="本页仅使用公开书目信息、PostgreSQL 技术文档与作者公开写作范围作为边界；正文、代码、图示、实验和练习均为独立重写。"
  license="出版社公开书目信息与 PostgreSQL 文档链接；教学内容为本项目独立创作。"
/>

## 本页小结

2.5 Java帝国之宫廷内斗的关键不是把 JTA 当成魔法，而是能沿参与者投票、协调决定、锁、日志和恢复区分 commit、rollback 与 in-doubt。完成标准是知道两阶段提交换来的原子性代价，并为基本可用和未登记副作用设计明确的降级、幂等或补偿合同。
