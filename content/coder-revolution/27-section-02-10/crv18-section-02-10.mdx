---
title: "2.10 Java帝国之泛型"
type: B
section: "码农翻身（2018年第1版）"
order: 28
description: "2.10 Java帝国之泛型：从“集合以 Object 接口依赖强制转换”推进到“类型参数把错误提前到编译期”，用专属机制图、实验和故障诊断验证。"
demo: true
math: true
sourceUrl: "https://www.phei.com.cn/module/goods/wssd_content.jsp?bookid=52126"
qualityVersion: 2
practiceMode: calculation
sourceMode: independent-rewrite
draft: false
---

import { Crv18Section0210Lab } from "@/components/mdx/coder-revolution/diagrams/crv18-section-02-10";
import {
  Objectives,
  Callout,
  Term,
  Glossary,
  GlossaryItem,
  Exercises,
  Answer,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释 2.10 Java帝国之泛型 为什么把“集合以 Object 接口依赖强制转换”推进为“类型参数把错误提前到编译期”
- 能沿 声明类型参 → 推断实参 → 检查边界 → 擦除桥接 → 运行使用 说清对象、状态和边界
- 能用本页公式或状态合同完成一次手算、代码、模拟、诊断或方案判断
- 能注入“把 List[Integer] 当成 List[Number] 写入 Double，破坏原容器元素合同”，定位最早偏离点并验证复位

</Objectives>

## 为什么需要这一机制

2.10 Java帝国之泛型 不能停在故事类比。真实系统要解决的变化是从 **集合以 Object 接口依赖强制转换** 到 **类型参数把错误提前到编译期**；可执行机制是：Java 泛型在编译期检查参数化类型，多数信息经擦除映射到边界类型；泛型类默认不因类型实参继承而协变。

对 2.10 Java帝国之泛型 的验收从同一份最小输入开始，先预测 声明类型参、推断实参、检查边界、擦除桥接、运行使用 各自会看到什么，再运行正常、边界和故障场景。故事帮助记住矛盾，状态、协议、类型、构建或反馈证据才决定解释是否成立。

<Callout type="info" title="范围与事实来源">
  本页按电子工业出版社公开目录定位 2.10 Java帝国之泛型 的问题范围，并依据 [Java
  language and VM specifications](https://docs.oracle.com/javase/specs/)
  独立重写技术机制。目录只证明主题坐标，不代表取得或复现原书正文。
</Callout>

## 核心合同

$$
List_{Integer}\not<:List_{Number}
$$

在 2.10 Java帝国之泛型 中，公式或状态表达式里的量必须绑定到具体对象、单位、版本或生命周期。先给基线输入算出预期，再逐步观察；如果最终结果碰巧相同，但中途已违反 检查边界 或 擦除桥接，仍应拒绝该实现。

## 目录节点到机制证据

### 2.10 Java帝国之泛型

在 **2.10 Java帝国之泛型** 中，目录坐标 **2.10 Java帝国之泛型** 对应“声明类型参”这一步。它不是故事情节的复述，而是要求读者解释：Java 泛型在编译期检查参数化类型，多数信息经擦除映射到边界类型；泛型类默认不因类型实参继承而协变。

复核 **2.10 Java帝国之泛型** 时，为 2.10 Java帝国之泛型 固定一个最小输入，先写出预期状态和输出，再改变与“声明类型参”直接相连的一个条件。若出现“把 List[Integer] 当成 List[Number] 写入 Double，破坏原容器元素合同”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

### 新王登基

在 **2.10 Java帝国之泛型** 中，目录坐标 **新王登基** 对应“推断实参”这一步。它不是故事情节的复述，而是要求读者解释：Java 泛型在编译期检查参数化类型，多数信息经擦除映射到边界类型；泛型类默认不因类型实参继承而协变。

复核 **新王登基** 时，为 2.10 Java帝国之泛型 固定一个最小输入，先写出预期状态和输出，再改变与“推断实参”直接相连的一个条件。若出现“把 List[Integer] 当成 List[Number] 写入 Double，破坏原容器元素合同”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

### C 使者

在 **2.10 Java帝国之泛型** 中，目录坐标 **C 使者** 对应“检查边界”这一步。它不是故事情节的复述，而是要求读者解释：Java 泛型在编译期检查参数化类型，多数信息经擦除映射到边界类型；泛型类默认不因类型实参继承而协变。

复核 **C 使者** 时，为 2.10 Java帝国之泛型 固定一个最小输入，先写出预期状态和输出，再改变与“检查边界”直接相连的一个条件。若出现“把 List[Integer] 当成 List[Number] 写入 Double，破坏原容器元素合同”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

### 泛型实现

在 **2.10 Java帝国之泛型** 中，目录坐标 **泛型实现** 对应“擦除桥接”这一步。它不是故事情节的复述，而是要求读者解释：Java 泛型在编译期检查参数化类型，多数信息经擦除映射到边界类型；泛型类默认不因类型实参继承而协变。

复核 **泛型实现** 时，为 2.10 Java帝国之泛型 固定一个最小输入，先写出预期状态和输出，再改变与“擦除桥接”直接相连的一个条件。若出现“把 List[Integer] 当成 List[Number] 写入 Double，破坏原容器元素合同”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

### 泛型方法

在 **2.10 Java帝国之泛型** 中，目录坐标 **泛型方法** 对应“运行使用”这一步。它不是故事情节的复述，而是要求读者解释：Java 泛型在编译期检查参数化类型，多数信息经擦除映射到边界类型；泛型类默认不因类型实参继承而协变。

复核 **泛型方法** 时，为 2.10 Java帝国之泛型 固定一个最小输入，先写出预期状态和输出，再改变与“运行使用”直接相连的一个条件。若出现“把 List[Integer] 当成 List[Number] 写入 Double，破坏原容器元素合同”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

### 泛型和继承

在 **2.10 Java帝国之泛型** 中，目录坐标 **泛型和继承** 对应“声明类型参”这一步。它不是故事情节的复述，而是要求读者解释：Java 泛型在编译期检查参数化类型，多数信息经擦除映射到边界类型；泛型类默认不因类型实参继承而协变。

复核 **泛型和继承** 时，为 2.10 Java帝国之泛型 固定一个最小输入，先写出预期状态和输出，再改变与“声明类型参”直接相连的一个条件。若出现“把 List[Integer] 当成 List[Number] 写入 Double，破坏原容器元素合同”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

## 最小可重放实现

```text
// 2.10 Java帝国之泛型：接口行为与运行时边界一起验证
var baseline = runCase("baseline");
assertContract(baseline);
var rejected = runCase("fault");
assertTrue(rejected.isRejected());
assertEquals(baseline, resetAndRun());
```

这段实现草图只表达 2.10 Java帝国之泛型 的验证合同，不复制书中叙事或代码。实际运行时应保存输入、环境、每个节点状态、最终结果和复位结果，使另一位读者能够从干净状态重放。

## 专属因果实验

先预测改变一个控制量后，2.10 Java帝国之泛型 的哪一节点最先变化。依次切换基线、边界和注入故障，保存一次轨迹；随后点击“重置实验”，确认控件、节点选择、压力值和状态文本全部恢复。

<Crv18Section0210Lab />

## 故障诊断

<Callout
  type="trap"
  title="误区：把 List[Integer] 当成 List[Number] 写入 Double，破坏原容器元素合同"
>
  该错误会破坏 2.10 Java帝国之泛型 从“集合以 Object
  接口依赖强制转换”到“类型参数把错误提前到编译期”的转换。诊断时按 声明类型参 →
  推断实参 → 检查边界 → 擦除桥接 → 运行使用
  前进，只在首次出现预期与实际不一致的位置停下。
</Callout>

<Callout type="trap" title="误区：最终成功就算机制正确">
  2.10 Java帝国之泛型
  的最终输出可能被缓存、重试、默认值或偶然时序掩盖。必须同时保存中间状态、拒绝语义和复位重放；缺少任一项都不能据此推广到更大规模。
</Callout>

<Callout type="tip" title="恢复顺序">
  2.10 Java帝国之泛型 的恢复顺序是：还原输入与环境 → 清理派生状态 → 重建当前边界
  → 重跑机制链 → 对照公式或合同 → 再次注入同一故障确认可重复拒绝。
</Callout>

## 术语与边界

2.10 Java帝国之泛型 使用五个可验证术语：<Term def="声明类型参 是 2.10 Java帝国之泛型 机制链中的可观察状态或边界，必须用实际输入和轨迹验证">声明类型参</Term>、<Term def="推断实参 是 2.10 Java帝国之泛型 机制链中的可观察状态或边界，必须用实际输入和轨迹验证">推断实参</Term>、<Term def="检查边界 是 2.10 Java帝国之泛型 机制链中的可观察状态或边界，必须用实际输入和轨迹验证">检查边界</Term>、<Term def="擦除桥接 是 2.10 Java帝国之泛型 机制链中的可观察状态或边界，必须用实际输入和轨迹验证">擦除桥接</Term>、<Term def="运行使用 是 2.10 Java帝国之泛型 机制链中的可观察状态或边界，必须用实际输入和轨迹验证">运行使用</Term>。术语若不能指向本页实验中的控件、节点或状态，就仍然只是目录名词。

<Glossary>

<GlossaryItem term="声明类型参">
  声明类型参 位于 2.10 Java帝国之泛型 的第 1
  个检查点；验收记录必须包含它的输入、状态、输出和故障表现。
</GlossaryItem>

<GlossaryItem term="推断实参">
  推断实参 位于 2.10 Java帝国之泛型 的第 2
  个检查点；验收记录必须包含它的输入、状态、输出和故障表现。
</GlossaryItem>

<GlossaryItem term="检查边界">
  检查边界 位于 2.10 Java帝国之泛型 的第 3
  个检查点；验收记录必须包含它的输入、状态、输出和故障表现。
</GlossaryItem>

<GlossaryItem term="擦除桥接">
  擦除桥接 位于 2.10 Java帝国之泛型 的第 4
  个检查点；验收记录必须包含它的输入、状态、输出和故障表现。
</GlossaryItem>

<GlossaryItem term="运行使用">
  运行使用 位于 2.10 Java帝国之泛型 的第 5
  个检查点；验收记录必须包含它的输入、状态、输出和故障表现。
</GlossaryItem>

</Glossary>

## 本页小结

2.10 Java帝国之泛型 的关键不是记住故事，而是能解释“Java 泛型在编译期检查参数化类型，多数信息经擦除映射到边界类型；泛型类默认不因类型实参继承而协变”。完成标准是用本页合同推出预期，沿五个专属节点观察实际，诊断“把 List[Integer] 当成 List[Number] 写入 Double，破坏原容器元素合同”，并证明复位后的同一输入重新得到基线轨迹。

<Exercises>

1. 2.10 Java帝国之泛型 的五节点链中，哪个状态最能区分“集合以 Object 接口依赖强制转换”和“类型参数把错误提前到编译期”？请给出一个最小样本。

<Answer>
  先检查 声明类型参 的输入身份，再观察 推断实参 与
  检查边界。只改变一个条件时，最早发生可解释变化的节点就是区分两种状态的证据；目录节点逐项核对为：2.10
  Java帝国之泛型（检查 声明类型参）；新王登基（检查 推断实参）；C 使者（检查
  检查边界）；泛型实现（检查 擦除桥接）；泛型方法（检查
  运行使用）；泛型和继承（检查 声明类型参）。
</Answer>

2. 使用本页核心合同完成一次手算、状态推演或代码断言，并说明每个量对应哪个真实对象。

<Answer>
  为 2.10 Java帝国之泛型
  写下输入值、单位、版本和边界，按公式得到预期，再与实验中的 检查边界 和
  运行使用
  比较。若发生隐式单位转换、状态复用或顺序变化，必须先修正模型，不能只调整答案。
</Answer>

3. 注入“把 List[Integer] 当成 List[Number] 写入 Double，破坏原容器元素合同”后，怎样证明恢复动作有效而非偶然成功？

<Answer>
  保存故障前基线与最早偏离点，执行本页恢复顺序，再用完全相同输入运行两次。2.10
  Java帝国之泛型
  只有在控件状态、五节点轨迹、最终输出和拒绝结果都可重复时才算恢复完成。
</Answer>

</Exercises>

<Attribution />
