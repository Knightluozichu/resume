---
title: "2.13 加锁还是不加锁，这是一个问题"
type: B
section: "码农翻身（2018年第1版）"
order: 31
description: "2.13 加锁还是不加锁，这是一个问题：从“所有共享更新都由互斥锁串行”推进到“条件原子更新减少阻塞但需要重试策略”，用专属机制图、实验和故障诊断验证。"
demo: true
math: true
sourceUrl: "https://www.phei.com.cn/module/goods/wssd_content.jsp?bookid=52126"
qualityVersion: 2
practiceMode: simulation
sourceMode: independent-rewrite
draft: false
---

import { Crv18Section0213Lab } from "@/components/mdx/coder-revolution/diagrams/crv18-section-02-13";
import {
  Objectives,
  Callout,
  Term,
  Glossary,
  GlossaryItem,
  Exercises,
  Answer,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释 2.13 加锁还是不加锁，这是一个问题 为什么把“所有共享更新都由互斥锁串行”推进为“条件原子更新减少阻塞但需要重试策略”
- 能沿 读取旧值 → 计算新值 → 执行CAS → 检查失败 → 退避重试 说清对象、状态和边界
- 能用本页公式或状态合同完成一次手算、代码、模拟、诊断或方案判断
- 能注入“值从 A 变为 B 又回到 A，单看数值的 CAS 未察觉版本变化而触发 ABA”，定位最早偏离点并验证复位

</Objectives>

## 为什么需要这一机制

2.13 加锁还是不加锁，这是一个问题 不能停在故事类比。真实系统要解决的变化是从 **所有共享更新都由互斥锁串行** 到 **条件原子更新减少阻塞但需要重试策略**；可执行机制是：CAS 仅在当前值等于期望值时原子更新，失败方重读并重试；无锁不等于每个线程都有有限等待上界。

对 2.13 加锁还是不加锁，这是一个问题 的验收从同一份最小输入开始，先预测 读取旧值、计算新值、执行CAS、检查失败、退避重试 各自会看到什么，再运行正常、边界和故障场景。故事帮助记住矛盾，状态、协议、类型、构建或反馈证据才决定解释是否成立。

<Callout type="info" title="范围与事实来源">
  本页按电子工业出版社公开目录定位 2.13 加锁还是不加锁，这是一个问题
  的问题范围，并依据 [Java language and VM
  specifications](https://docs.oracle.com/javase/specs/)
  独立重写技术机制。目录只证明主题坐标，不代表取得或复现原书正文。
</Callout>

## 核心合同

$$
CAS(addr,expected,new)\Rightarrow old=expected
$$

在 2.13 加锁还是不加锁，这是一个问题 中，公式或状态表达式里的量必须绑定到具体对象、单位、版本或生命周期。先给基线输入算出预期，再逐步观察；如果最终结果碰巧相同，但中途已违反 执行CAS 或 检查失败，仍应拒绝该实现。

## 目录节点到机制证据

### 2.13 加锁还是不加锁，这是一个问题

在 **2.13 加锁还是不加锁，这是一个问题** 中，目录坐标 **2.13 加锁还是不加锁，这是一个问题** 对应“读取旧值”这一步。它不是故事情节的复述，而是要求读者解释：CAS 仅在当前值等于期望值时原子更新，失败方重读并重试；无锁不等于每个线程都有有限等待上界。

复核 **2.13 加锁还是不加锁，这是一个问题** 时，为 2.13 加锁还是不加锁，这是一个问题 固定一个最小输入，先写出预期状态和输出，再改变与“读取旧值”直接相连的一个条件。若出现“值从 A 变为 B 又回到 A，单看数值的 CAS 未察觉版本变化而触发 ABA”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

### 互斥锁

在 **2.13 加锁还是不加锁，这是一个问题** 中，目录坐标 **互斥锁** 对应“计算新值”这一步。它不是故事情节的复述，而是要求读者解释：CAS 仅在当前值等于期望值时原子更新，失败方重读并重试；无锁不等于每个线程都有有限等待上界。

复核 **互斥锁** 时，为 2.13 加锁还是不加锁，这是一个问题 固定一个最小输入，先写出预期状态和输出，再改变与“计算新值”直接相连的一个条件。若出现“值从 A 变为 B 又回到 A，单看数值的 CAS 未察觉版本变化而触发 ABA”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

### 要不要加锁

在 **2.13 加锁还是不加锁，这是一个问题** 中，目录坐标 **要不要加锁** 对应“执行CAS”这一步。它不是故事情节的复述，而是要求读者解释：CAS 仅在当前值等于期望值时原子更新，失败方重读并重试；无锁不等于每个线程都有有限等待上界。

复核 **要不要加锁** 时，为 2.13 加锁还是不加锁，这是一个问题 固定一个最小输入，先写出预期状态和输出，再改变与“执行CAS”直接相连的一个条件。若出现“值从 A 变为 B 又回到 A，单看数值的 CAS 未察觉版本变化而触发 ABA”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

### CAS的扩展

在 **2.13 加锁还是不加锁，这是一个问题** 中，目录坐标 **CAS的扩展** 对应“检查失败”这一步。它不是故事情节的复述，而是要求读者解释：CAS 仅在当前值等于期望值时原子更新，失败方重读并重试；无锁不等于每个线程都有有限等待上界。

复核 **CAS的扩展** 时，为 2.13 加锁还是不加锁，这是一个问题 固定一个最小输入，先写出预期状态和输出，再改变与“检查失败”直接相连的一个条件。若出现“值从 A 变为 B 又回到 A，单看数值的 CAS 未察觉版本变化而触发 ABA”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

## 最小可重放实现

```text
# 2.13 加锁还是不加锁，这是一个问题：用事件轨迹验证状态转换
state = baseline_state()
for event in scenario:
    state = transition(state, event)
    assert invariant(state)
assert reset(state) == baseline_state()
```

这段实现草图只表达 2.13 加锁还是不加锁，这是一个问题 的验证合同，不复制书中叙事或代码。实际运行时应保存输入、环境、每个节点状态、最终结果和复位结果，使另一位读者能够从干净状态重放。

## 专属因果实验

先预测改变一个控制量后，2.13 加锁还是不加锁，这是一个问题 的哪一节点最先变化。依次切换基线、边界和注入故障，保存一次轨迹；随后点击“重置实验”，确认控件、节点选择、压力值和状态文本全部恢复。

<Crv18Section0213Lab />

## 故障诊断

<Callout
  type="trap"
  title="误区：值从 A 变为 B 又回到 A，单看数值的 CAS 未察觉版本变化而触发 ABA"
>
  该错误会破坏 2.13 加锁还是不加锁，这是一个问题
  从“所有共享更新都由互斥锁串行”到“条件原子更新减少阻塞但需要重试策略”的转换。诊断时按
  读取旧值 → 计算新值 → 执行CAS → 检查失败 → 退避重试
  前进，只在首次出现预期与实际不一致的位置停下。
</Callout>

<Callout type="trap" title="误区：最终成功就算机制正确">
  2.13 加锁还是不加锁，这是一个问题
  的最终输出可能被缓存、重试、默认值或偶然时序掩盖。必须同时保存中间状态、拒绝语义和复位重放；缺少任一项都不能据此推广到更大规模。
</Callout>

<Callout type="tip" title="恢复顺序">
  2.13 加锁还是不加锁，这是一个问题 的恢复顺序是：还原输入与环境 → 清理派生状态
  → 重建当前边界 → 重跑机制链 → 对照公式或合同 →
  再次注入同一故障确认可重复拒绝。
</Callout>

## 术语与边界

2.13 加锁还是不加锁，这是一个问题 使用五个可验证术语：<Term def="读取旧值 是 2.13 加锁还是不加锁，这是一个问题 机制链中的可观察状态或边界，必须用实际输入和轨迹验证">读取旧值</Term>、<Term def="计算新值 是 2.13 加锁还是不加锁，这是一个问题 机制链中的可观察状态或边界，必须用实际输入和轨迹验证">计算新值</Term>、<Term def="执行CAS 是 2.13 加锁还是不加锁，这是一个问题 机制链中的可观察状态或边界，必须用实际输入和轨迹验证">执行CAS</Term>、<Term def="检查失败 是 2.13 加锁还是不加锁，这是一个问题 机制链中的可观察状态或边界，必须用实际输入和轨迹验证">检查失败</Term>、<Term def="退避重试 是 2.13 加锁还是不加锁，这是一个问题 机制链中的可观察状态或边界，必须用实际输入和轨迹验证">退避重试</Term>。术语若不能指向本页实验中的控件、节点或状态，就仍然只是目录名词。

<Glossary>

<GlossaryItem term="读取旧值">
  读取旧值 位于 2.13 加锁还是不加锁，这是一个问题 的第 1
  个检查点；验收记录必须包含它的输入、状态、输出和故障表现。
</GlossaryItem>

<GlossaryItem term="计算新值">
  计算新值 位于 2.13 加锁还是不加锁，这是一个问题 的第 2
  个检查点；验收记录必须包含它的输入、状态、输出和故障表现。
</GlossaryItem>

<GlossaryItem term="执行CAS">
  执行CAS 位于 2.13 加锁还是不加锁，这是一个问题 的第 3
  个检查点；验收记录必须包含它的输入、状态、输出和故障表现。
</GlossaryItem>

<GlossaryItem term="检查失败">
  检查失败 位于 2.13 加锁还是不加锁，这是一个问题 的第 4
  个检查点；验收记录必须包含它的输入、状态、输出和故障表现。
</GlossaryItem>

<GlossaryItem term="退避重试">
  退避重试 位于 2.13 加锁还是不加锁，这是一个问题 的第 5
  个检查点；验收记录必须包含它的输入、状态、输出和故障表现。
</GlossaryItem>

</Glossary>

## 本页小结

2.13 加锁还是不加锁，这是一个问题 的关键不是记住故事，而是能解释“CAS 仅在当前值等于期望值时原子更新，失败方重读并重试；无锁不等于每个线程都有有限等待上界”。完成标准是用本页合同推出预期，沿五个专属节点观察实际，诊断“值从 A 变为 B 又回到 A，单看数值的 CAS 未察觉版本变化而触发 ABA”，并证明复位后的同一输入重新得到基线轨迹。

<Exercises>

1. 2.13 加锁还是不加锁，这是一个问题 的五节点链中，哪个状态最能区分“所有共享更新都由互斥锁串行”和“条件原子更新减少阻塞但需要重试策略”？请给出一个最小样本。

<Answer>
  先检查 读取旧值 的输入身份，再观察 计算新值 与
  执行CAS。只改变一个条件时，最早发生可解释变化的节点就是区分两种状态的证据；目录节点逐项核对为：2.13
  加锁还是不加锁，这是一个问题（检查 读取旧值）；互斥锁（检查
  计算新值）；要不要加锁（检查 执行CAS）；CAS的扩展（检查 检查失败）。
</Answer>

2. 使用本页核心合同完成一次手算、状态推演或代码断言，并说明每个量对应哪个真实对象。

<Answer>
  为 2.13 加锁还是不加锁，这是一个问题
  写下输入值、单位、版本和边界，按公式得到预期，再与实验中的 执行CAS 和 退避重试
  比较。若发生隐式单位转换、状态复用或顺序变化，必须先修正模型，不能只调整答案。
</Answer>

3. 注入“值从 A 变为 B 又回到 A，单看数值的 CAS 未察觉版本变化而触发 ABA”后，怎样证明恢复动作有效而非偶然成功？

<Answer>
  保存故障前基线与最早偏离点，执行本页恢复顺序，再用完全相同输入运行两次。2.13
  加锁还是不加锁，这是一个问题
  只有在控件状态、五节点轨迹、最终输出和拒绝结果都可重复时才算恢复完成。
</Answer>

</Exercises>

<Attribution />
