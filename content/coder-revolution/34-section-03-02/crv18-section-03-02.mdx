---
title: "3.2 两个程序的爱情故事"
type: B
section: "码农翻身（2018年第1版）"
order: 35
description: "3.2 两个程序的爱情故事：从“两个函数在同一内存直接调用”推进到“序列化消息穿越不可靠网络边界”，用专属机制图、实验和故障诊断验证。"
demo: true
math: true
sourceUrl: "https://www.phei.com.cn/module/goods/wssd_content.jsp?bookid=52126"
qualityVersion: 2
practiceMode: simulation
sourceMode: independent-rewrite
draft: false
---


## 为什么需要这一机制

3.2 两个程序的爱情故事 不能停在故事类比。真实系统要解决的变化是从 **两个函数在同一内存直接调用** 到 **序列化消息穿越不可靠网络边界**；可执行机制是：客户端和服务器是独立进程，借助 socket 与应用协议交换带边界的消息；网络会延迟、重复、丢失或中断连接。

对 3.2 两个程序的爱情故事 的验收从同一份最小输入开始，先预测 构造请求、连接服务、传输消息、处理状态、接收响应 各自会看到什么，再运行正常、边界和故障场景。故事帮助记住矛盾，状态、协议、类型、构建或反馈证据才决定解释是否成立。


## 核心合同

$$
response=f(request,state)
$$

在 3.2 两个程序的爱情故事 中，公式或状态表达式里的量必须绑定到具体对象、单位、版本或生命周期。先给基线输入算出预期，再逐步观察；如果最终结果碰巧相同，但中途已违反 传输消息 或 处理状态，仍应拒绝该实现。

## 目录节点到机制证据

### 3.2 两个程序的爱情故事

在 **3.2 两个程序的爱情故事** 中，目录坐标 **3.2 两个程序的爱情故事** 对应“构造请求”这一步。它不是故事情节的复述，而是要求读者解释：客户端和服务器是独立进程，借助 socket 与应用协议交换带边界的消息；网络会延迟、重复、丢失或中断连接。

复核 **3.2 两个程序的爱情故事** 时，为 3.2 两个程序的爱情故事 固定一个最小输入，先写出预期状态和输出，再改变与“构造请求”直接相连的一个条件。若出现“客户端超时后重试非幂等请求，服务器实际已成功而产生两次副作用”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

### 好感

在 **3.2 两个程序的爱情故事** 中，目录坐标 **好感** 对应“连接服务”这一步。它不是故事情节的复述，而是要求读者解释：客户端和服务器是独立进程，借助 socket 与应用协议交换带边界的消息；网络会延迟、重复、丢失或中断连接。

复核 **好感** 时，为 3.2 两个程序的爱情故事 固定一个最小输入，先写出预期状态和输出，再改变与“连接服务”直接相连的一个条件。若出现“客户端超时后重试非幂等请求，服务器实际已成功而产生两次副作用”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

### 分离

在 **3.2 两个程序的爱情故事** 中，目录坐标 **分离** 对应“传输消息”这一步。它不是故事情节的复述，而是要求读者解释：客户端和服务器是独立进程，借助 socket 与应用协议交换带边界的消息；网络会延迟、重复、丢失或中断连接。

复核 **分离** 时，为 3.2 两个程序的爱情故事 固定一个最小输入，先写出预期状态和输出，再改变与“传输消息”直接相连的一个条件。若出现“客户端超时后重试非幂等请求，服务器实际已成功而产生两次副作用”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

### 网络

在 **3.2 两个程序的爱情故事** 中，目录坐标 **网络** 对应“处理状态”这一步。它不是故事情节的复述，而是要求读者解释：客户端和服务器是独立进程，借助 socket 与应用协议交换带边界的消息；网络会延迟、重复、丢失或中断连接。

复核 **网络** 时，为 3.2 两个程序的爱情故事 固定一个最小输入，先写出预期状态和输出，再改变与“处理状态”直接相连的一个条件。若出现“客户端超时后重试非幂等请求，服务器实际已成功而产生两次副作用”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

### Web

在 **3.2 两个程序的爱情故事** 中，目录坐标 **Web** 对应“接收响应”这一步。它不是故事情节的复述，而是要求读者解释：客户端和服务器是独立进程，借助 socket 与应用协议交换带边界的消息；网络会延迟、重复、丢失或中断连接。

复核 **Web** 时，为 3.2 两个程序的爱情故事 固定一个最小输入，先写出预期状态和输出，再改变与“接收响应”直接相连的一个条件。若出现“客户端超时后重试非幂等请求，服务器实际已成功而产生两次副作用”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

## 最小可重放实现

```text
# 3.2 两个程序的爱情故事：每个报文都保留身份、顺序与结果
trace = protocol_run(fixed_payload, scenario="baseline")
assert trace.first_error is None
assert protocol_run(fixed_payload, scenario="fault").rejected
assert protocol_run(fixed_payload) == trace
```

这段实现草图只表达 3.2 两个程序的爱情故事 的验证合同，不复制书中叙事或代码。实际运行时应保存输入、环境、每个节点状态、最终结果和复位结果，使另一位读者能够从干净状态重放。

## 专属因果实验

先预测改变一个控制量后，3.2 两个程序的爱情故事 的哪一节点最先变化。依次切换基线、边界和注入故障，保存一次轨迹；随后点击“重置实验”，确认控件、节点选择、压力值和状态文本全部恢复。


## 故障诊断


## 术语与边界

3.2 两个程序的爱情故事 使用五个可验证术语：、、、、。术语若不能指向本页实验中的控件、节点或状态，就仍然只是目录名词。


## 本页小结

3.2 两个程序的爱情故事 的关键不是记住故事，而是能解释“客户端和服务器是独立进程，借助 socket 与应用协议交换带边界的消息；网络会延迟、重复、丢失或中断连接”。完成标准是用本页合同推出预期，沿五个专属节点观察实际，诊断“客户端超时后重试非幂等请求，服务器实际已成功而产生两次副作用”，并证明复位后的同一输入重新得到基线轨迹。


