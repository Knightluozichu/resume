---
title: "3.5 从密码到token，一个有关授权的故事"
type: B
section: "码农翻身（2018年第1版）"
order: 38
description: "3.5 从密码到token，一个有关授权的故事：从“把用户密码交给每个第三方应用”推进到“授权服务器签发有范围和期限的令牌”，用专属机制图、实验和故障诊断验证。"
demo: true
math: true
sourceUrl: "https://www.phei.com.cn/module/goods/wssd_content.jsp?bookid=52126"
qualityVersion: 2
practiceMode: simulation
sourceMode: independent-rewrite
draft: false
---


## 为什么需要这一机制

3.5 从密码到token，一个有关授权的故事 不能停在故事类比。真实系统要解决的变化是从 **把用户密码交给每个第三方应用** 到 **授权服务器签发有范围和期限的令牌**；可执行机制是：OAuth 授权码流程让资源所有者在授权服务器登录，客户端以授权码换取访问令牌；客户端不应取得用户密码。

对 3.5 从密码到token，一个有关授权的故事 的验收从同一份最小输入开始，先预测 发起授权、用户同意、返回授权码、后端换令牌、携令牌访问 各自会看到什么，再运行正常、边界和故障场景。故事帮助记住矛盾，状态、协议、类型、构建或反馈证据才决定解释是否成立。


## 核心合同

$$
access\ token = exchange(code,client,redirect\ URI)
$$

在 3.5 从密码到token，一个有关授权的故事 中，公式或状态表达式里的量必须绑定到具体对象、单位、版本或生命周期。先给基线输入算出预期，再逐步观察；如果最终结果碰巧相同，但中途已违反 返回授权码 或 后端换令牌，仍应拒绝该实现。

## 目录节点到机制证据

### 3.5 从密码到token，一个有关授权的故事

在 **3.5 从密码到token，一个有关授权的故事** 中，目录坐标 **3.5 从密码到token，一个有关授权的故事** 对应“发起授权”这一步。它不是故事情节的复述，而是要求读者解释：OAuth 授权码流程让资源所有者在授权服务器登录，客户端以授权码换取访问令牌；客户端不应取得用户密码。

复核 **3.5 从密码到token，一个有关授权的故事** 时，为 3.5 从密码到token，一个有关授权的故事 固定一个最小输入，先写出预期状态和输出，再改变与“发起授权”直接相连的一个条件。若出现“攻击者截获可复用授权码，客户端又未绑定 redirect URI 或 PKCE 校验值”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

### 我把密码献给你

在 **3.5 从密码到token，一个有关授权的故事** 中，目录坐标 **我把密码献给你** 对应“用户同意”这一步。它不是故事情节的复述，而是要求读者解释：OAuth 授权码流程让资源所有者在授权服务器登录，客户端以授权码换取访问令牌；客户端不应取得用户密码。

复核 **我把密码献给你** 时，为 3.5 从密码到token，一个有关授权的故事 固定一个最小输入，先写出预期状态和输出，再改变与“用户同意”直接相连的一个条件。若出现“攻击者截获可复用授权码，客户端又未绑定 redirect URI 或 PKCE 校验值”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

### token

在 **3.5 从密码到token，一个有关授权的故事** 中，目录坐标 **token** 对应“返回授权码”这一步。它不是故事情节的复述，而是要求读者解释：OAuth 授权码流程让资源所有者在授权服务器登录，客户端以授权码换取访问令牌；客户端不应取得用户密码。

复核 **token** 时，为 3.5 从密码到token，一个有关授权的故事 固定一个最小输入，先写出预期状态和输出，再改变与“返回授权码”直接相连的一个条件。若出现“攻击者截获可复用授权码，客户端又未绑定 redirect URI 或 PKCE 校验值”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

### 授权码 token

在 **3.5 从密码到token，一个有关授权的故事** 中，目录坐标 **授权码 token** 对应“后端换令牌”这一步。它不是故事情节的复述，而是要求读者解释：OAuth 授权码流程让资源所有者在授权服务器登录，客户端以授权码换取访问令牌；客户端不应取得用户密码。

复核 **授权码 token** 时，为 3.5 从密码到token，一个有关授权的故事 固定一个最小输入，先写出预期状态和输出，再改变与“后端换令牌”直接相连的一个条件。若出现“攻击者截获可复用授权码，客户端又未绑定 redirect URI 或 PKCE 校验值”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

### 后记

在 **3.5 从密码到token，一个有关授权的故事** 中，目录坐标 **后记** 对应“携令牌访问”这一步。它不是故事情节的复述，而是要求读者解释：OAuth 授权码流程让资源所有者在授权服务器登录，客户端以授权码换取访问令牌；客户端不应取得用户密码。

复核 **后记** 时，为 3.5 从密码到token，一个有关授权的故事 固定一个最小输入，先写出预期状态和输出，再改变与“携令牌访问”直接相连的一个条件。若出现“攻击者截获可复用授权码，客户端又未绑定 redirect URI 或 PKCE 校验值”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

## 最小可重放实现

```text
# 3.5 从密码到token，一个有关授权的故事：用同一 request_id 串联跨层证据
response = request(fixed_case, request_id="case-01")
assert response.contract_ok
assert inject_fault("one-boundary").first_deviation
assert reset_and_request(fixed_case) == response
```

这段实现草图只表达 3.5 从密码到token，一个有关授权的故事 的验证合同，不复制书中叙事或代码。实际运行时应保存输入、环境、每个节点状态、最终结果和复位结果，使另一位读者能够从干净状态重放。

## 专属因果实验

先预测改变一个控制量后，3.5 从密码到token，一个有关授权的故事 的哪一节点最先变化。依次切换基线、边界和注入故障，保存一次轨迹；随后点击“重置实验”，确认控件、节点选择、压力值和状态文本全部恢复。


## 故障诊断


## 术语与边界

3.5 从密码到token，一个有关授权的故事 使用五个可验证术语：、、、、。术语若不能指向本页实验中的控件、节点或状态，就仍然只是目录名词。


## 本页小结

3.5 从密码到token，一个有关授权的故事 的关键不是记住故事，而是能解释“OAuth 授权码流程让资源所有者在授权服务器登录，客户端以授权码换取访问令牌；客户端不应取得用户密码”。完成标准是用本页合同推出预期，沿五个专属节点观察实际，诊断“攻击者截获可复用授权码，客户端又未绑定 redirect URI 或 PKCE 校验值”，并证明复位后的同一输入重新得到基线轨迹。


