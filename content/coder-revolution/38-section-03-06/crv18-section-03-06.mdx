---
title: "3.6 后端风云"
type: B
section: "码农翻身（2018年第1版）"
order: 39
description: "3.6 后端风云：从“单库单进程承担全部请求”推进到“按状态边界拆分容量与可用性职责”，用专属机制图、实验和故障诊断验证。"
demo: true
math: true
sourceUrl: "https://www.phei.com.cn/module/goods/wssd_content.jsp?bookid=52126"
qualityVersion: 2
practiceMode: diagnosis
sourceMode: independent-rewrite
draft: false
---


## 为什么需要这一机制

3.6 后端风云 不能停在故事类比。真实系统要解决的变化是从 **单库单进程承担全部请求** 到 **按状态边界拆分容量与可用性职责**；可执行机制是：后端扩展需要分别处理缓存、分片、副本、负载均衡和故障转移；一致性哈希减少节点变化时的键迁移，却不自动解决副本一致性。

对 3.6 后端风云 的验收从同一份最小输入开始，先预测 请求入口、缓存命中、选择分片、读写副本、故障转移 各自会看到什么，再运行正常、边界和故障场景。故事帮助记住矛盾，状态、协议、类型、构建或反馈证据才决定解释是否成立。


## 核心合同

$$
shard=hash(key)\bmod N\quad(N\ fixed)
$$

在 3.6 后端风云 中，公式或状态表达式里的量必须绑定到具体对象、单位、版本或生命周期。先给基线输入算出预期，再逐步观察；如果最终结果碰巧相同，但中途已违反 选择分片 或 读写副本，仍应拒绝该实现。

## 目录节点到机制证据

### 3.6 后端风云

在 **3.6 后端风云** 中，目录坐标 **3.6 后端风云** 对应“请求入口”这一步。它不是故事情节的复述，而是要求读者解释：后端扩展需要分别处理缓存、分片、副本、负载均衡和故障转移；一致性哈希减少节点变化时的键迁移，却不自动解决副本一致性。

复核 **3.6 后端风云** 时，为 3.6 后端风云 固定一个最小输入，先写出预期状态和输出，再改变与“请求入口”直接相连的一个条件。若出现“扩容时直接改变取模分母，绝大多数键重新映射并造成缓存穿透或数据找不到”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

### 数据库老头儿

在 **3.6 后端风云** 中，目录坐标 **数据库老头儿** 对应“缓存命中”这一步。它不是故事情节的复述，而是要求读者解释：后端扩展需要分别处理缓存、分片、副本、负载均衡和故障转移；一致性哈希减少节点变化时的键迁移，却不自动解决副本一致性。

复核 **数据库老头儿** 时，为 3.6 后端风云 固定一个最小输入，先写出预期状态和输出，再改变与“缓存命中”直接相连的一个条件。若出现“扩容时直接改变取模分母，绝大多数键重新映射并造成缓存穿透或数据找不到”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

### 危机

在 **3.6 后端风云** 中，目录坐标 **危机** 对应“选择分片”这一步。它不是故事情节的复述，而是要求读者解释：后端扩展需要分别处理缓存、分片、副本、负载均衡和故障转移；一致性哈希减少节点变化时的键迁移，却不自动解决副本一致性。

复核 **危机** 时，为 3.6 后端风云 固定一个最小输入，先写出预期状态和输出，再改变与“选择分片”直接相连的一个条件。若出现“扩容时直接改变取模分母，绝大多数键重新映射并造成缓存穿透或数据找不到”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

### 党委扩大会议

在 **3.6 后端风云** 中，目录坐标 **党委扩大会议** 对应“读写副本”这一步。它不是故事情节的复述，而是要求读者解释：后端扩展需要分别处理缓存、分片、副本、负载均衡和故障转移；一致性哈希减少节点变化时的键迁移，却不自动解决副本一致性。

复核 **党委扩大会议** 时，为 3.6 后端风云 固定一个最小输入，先写出预期状态和输出，再改变与“读写副本”直接相连的一个条件。若出现“扩容时直接改变取模分母，绝大多数键重新映射并造成缓存穿透或数据找不到”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

### 分家

在 **3.6 后端风云** 中，目录坐标 **分家** 对应“故障转移”这一步。它不是故事情节的复述，而是要求读者解释：后端扩展需要分别处理缓存、分片、副本、负载均衡和故障转移；一致性哈希减少节点变化时的键迁移，却不自动解决副本一致性。

复核 **分家** 时，为 3.6 后端风云 固定一个最小输入，先写出预期状态和输出，再改变与“故障转移”直接相连的一个条件。若出现“扩容时直接改变取模分母，绝大多数键重新映射并造成缓存穿透或数据找不到”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

### Redis

在 **3.6 后端风云** 中，目录坐标 **Redis** 对应“请求入口”这一步。它不是故事情节的复述，而是要求读者解释：后端扩展需要分别处理缓存、分片、副本、负载均衡和故障转移；一致性哈希减少节点变化时的键迁移，却不自动解决副本一致性。

复核 **Redis** 时，为 3.6 后端风云 固定一个最小输入，先写出预期状态和输出，再改变与“请求入口”直接相连的一个条件。若出现“扩容时直接改变取模分母，绝大多数键重新映射并造成缓存穿透或数据找不到”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

### 余数算法

在 **3.6 后端风云** 中，目录坐标 **余数算法** 对应“缓存命中”这一步。它不是故事情节的复述，而是要求读者解释：后端扩展需要分别处理缓存、分片、副本、负载均衡和故障转移；一致性哈希减少节点变化时的键迁移，却不自动解决副本一致性。

复核 **余数算法** 时，为 3.6 后端风云 固定一个最小输入，先写出预期状态和输出，再改变与“缓存命中”直接相连的一个条件。若出现“扩容时直接改变取模分母，绝大多数键重新映射并造成缓存穿透或数据找不到”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

### 一致性Hash算法

在 **3.6 后端风云** 中，目录坐标 **一致性Hash算法** 对应“选择分片”这一步。它不是故事情节的复述，而是要求读者解释：后端扩展需要分别处理缓存、分片、副本、负载均衡和故障转移；一致性哈希减少节点变化时的键迁移，却不自动解决副本一致性。

复核 **一致性Hash算法** 时，为 3.6 后端风云 固定一个最小输入，先写出预期状态和输出，再改变与“选择分片”直接相连的一个条件。若出现“扩容时直接改变取模分母，绝大多数键重新映射并造成缓存穿透或数据找不到”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

### Hash槽 （Hash Slot）

在 **3.6 后端风云** 中，目录坐标 **Hash槽 （Hash Slot）** 对应“读写副本”这一步。它不是故事情节的复述，而是要求读者解释：后端扩展需要分别处理缓存、分片、副本、负载均衡和故障转移；一致性哈希减少节点变化时的键迁移，却不自动解决副本一致性。

复核 **Hash槽 （Hash Slot）** 时，为 3.6 后端风云 固定一个最小输入，先写出预期状态和输出，再改变与“读写副本”直接相连的一个条件。若出现“扩容时直接改变取模分母，绝大多数键重新映射并造成缓存穿透或数据找不到”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

### 故障转移

在 **3.6 后端风云** 中，目录坐标 **故障转移** 对应“故障转移”这一步。它不是故事情节的复述，而是要求读者解释：后端扩展需要分别处理缓存、分片、副本、负载均衡和故障转移；一致性哈希减少节点变化时的键迁移，却不自动解决副本一致性。

复核 **故障转移** 时，为 3.6 后端风云 固定一个最小输入，先写出预期状态和输出，再改变与“故障转移”直接相连的一个条件。若出现“扩容时直接改变取模分母，绝大多数键重新映射并造成缓存穿透或数据找不到”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

### 高可用的Nginx

在 **3.6 后端风云** 中，目录坐标 **高可用的Nginx** 对应“请求入口”这一步。它不是故事情节的复述，而是要求读者解释：后端扩展需要分别处理缓存、分片、副本、负载均衡和故障转移；一致性哈希减少节点变化时的键迁移，却不自动解决副本一致性。

复核 **高可用的Nginx** 时，为 3.6 后端风云 固定一个最小输入，先写出预期状态和输出，再改变与“请求入口”直接相连的一个条件。若出现“扩容时直接改变取模分母，绝大多数键重新映射并造成缓存穿透或数据找不到”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

### 高可用的Tomcat

在 **3.6 后端风云** 中，目录坐标 **高可用的Tomcat** 对应“缓存命中”这一步。它不是故事情节的复述，而是要求读者解释：后端扩展需要分别处理缓存、分片、副本、负载均衡和故障转移；一致性哈希减少节点变化时的键迁移，却不自动解决副本一致性。

复核 **高可用的Tomcat** 时，为 3.6 后端风云 固定一个最小输入，先写出预期状态和输出，再改变与“缓存命中”直接相连的一个条件。若出现“扩容时直接改变取模分母，绝大多数键重新映射并造成缓存穿透或数据找不到”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

### 数据库的读写分离

在 **3.6 后端风云** 中，目录坐标 **数据库的读写分离** 对应“选择分片”这一步。它不是故事情节的复述，而是要求读者解释：后端扩展需要分别处理缓存、分片、副本、负载均衡和故障转移；一致性哈希减少节点变化时的键迁移，却不自动解决副本一致性。

复核 **数据库的读写分离** 时，为 3.6 后端风云 固定一个最小输入，先写出预期状态和输出，再改变与“选择分片”直接相连的一个条件。若出现“扩容时直接改变取模分母，绝大多数键重新映射并造成缓存穿透或数据找不到”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

## 最小可重放实现

```text
# 3.6 后端风云：提交、重试与回滚共享同一业务身份
result = execute_transaction(case_id, scenario="baseline")
assert result.committed and invariant(result.state)
fault = execute_transaction(case_id, scenario="fault")
assert fault.rolled_back or fault.safe_to_retry
```

这段实现草图只表达 3.6 后端风云 的验证合同，不复制书中叙事或代码。实际运行时应保存输入、环境、每个节点状态、最终结果和复位结果，使另一位读者能够从干净状态重放。

## 专属因果实验

先预测改变一个控制量后，3.6 后端风云 的哪一节点最先变化。依次切换基线、边界和注入故障，保存一次轨迹；随后点击“重置实验”，确认控件、节点选择、压力值和状态文本全部恢复。


## 故障诊断


## 术语与边界

3.6 后端风云 使用五个可验证术语：、、、、。术语若不能指向本页实验中的控件、节点或状态，就仍然只是目录名词。


## 本页小结

3.6 后端风云 的关键不是记住故事，而是能解释“后端扩展需要分别处理缓存、分片、副本、负载均衡和故障转移；一致性哈希减少节点变化时的键迁移，却不自动解决副本一致性”。完成标准是用本页合同推出预期，沿五个专属节点观察实际，诊断“扩容时直接改变取模分母，绝大多数键重新映射并造成缓存穿透或数据找不到”，并证明复位后的同一输入重新得到基线轨迹。


