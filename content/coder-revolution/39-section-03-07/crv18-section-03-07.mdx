---
title: "3.7 我是一个函数"
type: B
section: "码农翻身（2018年第1版）"
order: 40
description: "3.7 我是一个函数：从“逻辑散落在可跳转语句中”推进到“命名调用边界压缩并复用推理”，用专属机制图、实验和故障诊断验证。"
demo: true
math: true
sourceUrl: "https://www.phei.com.cn/module/goods/wssd_content.jsp?bookid=52126"
qualityVersion: 2
practiceMode: code
sourceMode: independent-rewrite
draft: true
---

import { Crv18Section0307Lab } from "@/components/mdx/coder-revolution/diagrams/crv18-section-03-07";
import {
  Objectives,
  Callout,
  Term,
  Glossary,
  GlossaryItem,
  Exercises,
  Answer,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释 3.7 我是一个函数 为什么把“逻辑散落在可跳转语句中”推进为“命名调用边界压缩并复用推理”
- 能沿 绑定参数 → 建立栈帧 → 执行函数体 → 返回或抛错 → 释放栈帧 说清对象、状态和边界
- 能用本页公式或状态合同完成一次手算、代码、模拟、诊断或方案判断
- 能注入“函数暗中修改全局状态，使同样输入在不同调用次序下产生不同结果”，定位最早偏离点并验证复位

</Objectives>

## 为什么需要这一机制

3.7 我是一个函数 不能停在故事类比。真实系统要解决的变化是从 **逻辑散落在可跳转语句中** 到 **命名调用边界压缩并复用推理**；可执行机制是：函数把参数、局部状态、返回值与异常封装成调用合同；调用栈保存活动记录，纯函数还能用相同输入重放结果。

对 3.7 我是一个函数 的验收从同一份最小输入开始，先预测 绑定参数、建立栈帧、执行函数体、返回或抛错、释放栈帧 各自会看到什么，再运行正常、边界和故障场景。故事帮助记住矛盾，状态、协议、类型、构建或反馈证据才决定解释是否成立。

<Callout type="info" title="范围与事实来源">
  本页按电子工业出版社公开目录定位 3.7 我是一个函数 的问题范围，并依据
  [ECMAScript language specification](https://tc39.es/ecma262/)
  独立重写技术机制。目录只证明主题坐标，不代表取得或复现原书正文。
</Callout>

## 核心合同

$$
y=f(x)\quad\text{with declared pre/post conditions}
$$

在 3.7 我是一个函数 中，公式或状态表达式里的量必须绑定到具体对象、单位、版本或生命周期。先给基线输入算出预期，再逐步观察；如果最终结果碰巧相同，但中途已违反 执行函数体 或 返回或抛错，仍应拒绝该实现。

## 目录节点到机制证据

### 3.7 我是一个函数

在 **3.7 我是一个函数** 中，目录坐标 **3.7 我是一个函数** 对应“绑定参数”这一步。它不是故事情节的复述，而是要求读者解释：函数把参数、局部状态、返回值与异常封装成调用合同；调用栈保存活动记录，纯函数还能用相同输入重放结果。

复核 **3.7 我是一个函数** 时，为 3.7 我是一个函数 固定一个最小输入，先写出预期状态和输出，再改变与“绑定参数”直接相连的一个条件。若出现“函数暗中修改全局状态，使同样输入在不同调用次序下产生不同结果”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

## 最小可重放实现

```text
// 3.7 我是一个函数：比较语义，不比较口号
const input = fixedCase();
const expected = referenceModel(input);
const actual = runProgram(input);
assert.deepEqual(actual, expected);
assert.deepEqual(resetAndRun(input), expected);
```

这段实现草图只表达 3.7 我是一个函数 的验证合同，不复制书中叙事或代码。实际运行时应保存输入、环境、每个节点状态、最终结果和复位结果，使另一位读者能够从干净状态重放。

## 专属因果实验

先预测改变一个控制量后，3.7 我是一个函数 的哪一节点最先变化。依次切换基线、边界和注入故障，保存一次轨迹；随后点击“重置实验”，确认控件、节点选择、压力值和状态文本全部恢复。

<Crv18Section0307Lab />

## 故障诊断

<Callout
  type="trap"
  title="误区：函数暗中修改全局状态，使同样输入在不同调用次序下产生不同结果"
>
  该错误会破坏 3.7 我是一个函数
  从“逻辑散落在可跳转语句中”到“命名调用边界压缩并复用推理”的转换。诊断时按
  绑定参数 → 建立栈帧 → 执行函数体 → 返回或抛错 → 释放栈帧
  前进，只在首次出现预期与实际不一致的位置停下。
</Callout>

<Callout type="trap" title="误区：最终成功就算机制正确">
  3.7 我是一个函数
  的最终输出可能被缓存、重试、默认值或偶然时序掩盖。必须同时保存中间状态、拒绝语义和复位重放；缺少任一项都不能据此推广到更大规模。
</Callout>

<Callout type="tip" title="恢复顺序">
  3.7 我是一个函数 的恢复顺序是：还原输入与环境 → 清理派生状态 → 重建当前边界 →
  重跑机制链 → 对照公式或合同 → 再次注入同一故障确认可重复拒绝。
</Callout>

## 术语与边界

3.7 我是一个函数 使用五个可验证术语：<Term def="绑定参数 是 3.7 我是一个函数 机制链中的可观察状态或边界，必须用实际输入和轨迹验证">绑定参数</Term>、<Term def="建立栈帧 是 3.7 我是一个函数 机制链中的可观察状态或边界，必须用实际输入和轨迹验证">建立栈帧</Term>、<Term def="执行函数体 是 3.7 我是一个函数 机制链中的可观察状态或边界，必须用实际输入和轨迹验证">执行函数体</Term>、<Term def="返回或抛错 是 3.7 我是一个函数 机制链中的可观察状态或边界，必须用实际输入和轨迹验证">返回或抛错</Term>、<Term def="释放栈帧 是 3.7 我是一个函数 机制链中的可观察状态或边界，必须用实际输入和轨迹验证">释放栈帧</Term>。术语若不能指向本页实验中的控件、节点或状态，就仍然只是目录名词。

<Glossary>

<GlossaryItem term="绑定参数">
  绑定参数 位于 3.7 我是一个函数 的第 1
  个检查点；验收记录必须包含它的输入、状态、输出和故障表现。
</GlossaryItem>

<GlossaryItem term="建立栈帧">
  建立栈帧 位于 3.7 我是一个函数 的第 2
  个检查点；验收记录必须包含它的输入、状态、输出和故障表现。
</GlossaryItem>

<GlossaryItem term="执行函数体">
  执行函数体 位于 3.7 我是一个函数 的第 3
  个检查点；验收记录必须包含它的输入、状态、输出和故障表现。
</GlossaryItem>

<GlossaryItem term="返回或抛错">
  返回或抛错 位于 3.7 我是一个函数 的第 4
  个检查点；验收记录必须包含它的输入、状态、输出和故障表现。
</GlossaryItem>

<GlossaryItem term="释放栈帧">
  释放栈帧 位于 3.7 我是一个函数 的第 5
  个检查点；验收记录必须包含它的输入、状态、输出和故障表现。
</GlossaryItem>

</Glossary>

## 本页小结

3.7 我是一个函数 的关键不是记住故事，而是能解释“函数把参数、局部状态、返回值与异常封装成调用合同；调用栈保存活动记录，纯函数还能用相同输入重放结果”。完成标准是用本页合同推出预期，沿五个专属节点观察实际，诊断“函数暗中修改全局状态，使同样输入在不同调用次序下产生不同结果”，并证明复位后的同一输入重新得到基线轨迹。

<Exercises>

1. 3.7 我是一个函数 的五节点链中，哪个状态最能区分“逻辑散落在可跳转语句中”和“命名调用边界压缩并复用推理”？请给出一个最小样本。

<Answer>
  先检查 绑定参数 的输入身份，再观察 建立栈帧 与
  执行函数体。只改变一个条件时，最早发生可解释变化的节点就是区分两种状态的证据；目录节点逐项核对为：3.7
  我是一个函数（检查 绑定参数）。
</Answer>

2. 使用本页核心合同完成一次手算、状态推演或代码断言，并说明每个量对应哪个真实对象。

<Answer>
  为 3.7 我是一个函数 写下输入值、单位、版本和边界，按公式得到预期，再与实验中的
  执行函数体 和 释放栈帧
  比较。若发生隐式单位转换、状态复用或顺序变化，必须先修正模型，不能只调整答案。
</Answer>

3. 注入“函数暗中修改全局状态，使同样输入在不同调用次序下产生不同结果”后，怎样证明恢复动作有效而非偶然成功？

<Answer>
  保存故障前基线与最早偏离点，执行本页恢复顺序，再用完全相同输入运行两次。3.7
  我是一个函数
  只有在控件状态、五节点轨迹、最终输出和拒绝结果都可重复时才算恢复完成。
</Answer>

</Exercises>

<Attribution />
