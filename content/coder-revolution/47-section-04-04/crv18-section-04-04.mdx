---
title: "4.4 敏捷下的单元测试"
type: B
section: "码农翻身（2018年第1版）"
order: 48
description: "4.4 敏捷下的单元测试：从“功能完成后由人手工点一遍”推进到“每次小变更立即得到可定位反馈”，用专属机制图、实验和故障诊断验证。"
demo: true
math: true
sourceUrl: "https://www.phei.com.cn/module/goods/wssd_content.jsp?bookid=52126"
qualityVersion: 2
practiceMode: code
sourceMode: independent-rewrite
draft: true
---

import { Crv18Section0404Lab } from "@/components/mdx/coder-revolution/diagrams/crv18-section-04-04";
import {
  Objectives,
  Callout,
  Term,
  Glossary,
  GlossaryItem,
  Exercises,
  Answer,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释 4.4 敏捷下的单元测试 为什么把“功能完成后由人手工点一遍”推进为“每次小变更立即得到可定位反馈”
- 能沿 安排夹具 → 执行单元 → 断言结果 → 清理状态 → 回归重放 说清对象、状态和边界
- 能用本页公式或状态合同完成一次手算、代码、模拟、诊断或方案判断
- 能注入“测试依赖真实时间、网络或共享数据库，导致相同代码随机红绿”，定位最早偏离点并验证复位

</Objectives>

## 为什么需要这一机制

4.4 敏捷下的单元测试 不能停在故事类比。真实系统要解决的变化是从 **功能完成后由人手工点一遍** 到 **每次小变更立即得到可定位反馈**；可执行机制是：单元测试在隔离边界内安排输入、执行行为并断言结果；敏捷迭代依赖快速、确定、可重复的反馈而非测试数量。

对 4.4 敏捷下的单元测试 的验收从同一份最小输入开始，先预测 安排夹具、执行单元、断言结果、清理状态、回归重放 各自会看到什么，再运行正常、边界和故障场景。故事帮助记住矛盾，状态、协议、类型、构建或反馈证据才决定解释是否成立。

<Callout type="info" title="范围与事实来源">
  本页按电子工业出版社公开目录定位 4.4 敏捷下的单元测试 的问题范围，并依据
  [JUnit current user guide](https://docs.junit.org/current/user-guide/)
  独立重写技术机制。目录只证明主题坐标，不代表取得或复现原书正文。
</Callout>

## 核心合同

$$
test=arrange+act+assert
$$

在 4.4 敏捷下的单元测试 中，公式或状态表达式里的量必须绑定到具体对象、单位、版本或生命周期。先给基线输入算出预期，再逐步观察；如果最终结果碰巧相同，但中途已违反 断言结果 或 清理状态，仍应拒绝该实现。

## 目录节点到机制证据

### 4.4 敏捷下的单元测试

在 **4.4 敏捷下的单元测试** 中，目录坐标 **4.4 敏捷下的单元测试** 对应“安排夹具”这一步。它不是故事情节的复述，而是要求读者解释：单元测试在隔离边界内安排输入、执行行为并断言结果；敏捷迭代依赖快速、确定、可重复的反馈而非测试数量。

复核 **4.4 敏捷下的单元测试** 时，为 4.4 敏捷下的单元测试 固定一个最小输入，先写出预期状态和输出，再改变与“安排夹具”直接相连的一个条件。若出现“测试依赖真实时间、网络或共享数据库，导致相同代码随机红绿”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

### 敏捷运动

在 **4.4 敏捷下的单元测试** 中，目录坐标 **敏捷运动** 对应“执行单元”这一步。它不是故事情节的复述，而是要求读者解释：单元测试在隔离边界内安排输入、执行行为并断言结果；敏捷迭代依赖快速、确定、可重复的反馈而非测试数量。

复核 **敏捷运动** 时，为 4.4 敏捷下的单元测试 固定一个最小输入，先写出预期状态和输出，再改变与“执行单元”直接相连的一个条件。若出现“测试依赖真实时间、网络或共享数据库，导致相同代码随机红绿”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

### 困惑

在 **4.4 敏捷下的单元测试** 中，目录坐标 **困惑** 对应“断言结果”这一步。它不是故事情节的复述，而是要求读者解释：单元测试在隔离边界内安排输入、执行行为并断言结果；敏捷迭代依赖快速、确定、可重复的反馈而非测试数量。

复核 **困惑** 时，为 4.4 敏捷下的单元测试 固定一个最小输入，先写出预期状态和输出，再改变与“断言结果”直接相连的一个条件。若出现“测试依赖真实时间、网络或共享数据库，导致相同代码随机红绿”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

### 讨论

在 **4.4 敏捷下的单元测试** 中，目录坐标 **讨论** 对应“清理状态”这一步。它不是故事情节的复述，而是要求读者解释：单元测试在隔离边界内安排输入、执行行为并断言结果；敏捷迭代依赖快速、确定、可重复的反馈而非测试数量。

复核 **讨论** 时，为 4.4 敏捷下的单元测试 固定一个最小输入，先写出预期状态和输出，再改变与“清理状态”直接相连的一个条件。若出现“测试依赖真实时间、网络或共享数据库，导致相同代码随机红绿”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

### 一年以后

在 **4.4 敏捷下的单元测试** 中，目录坐标 **一年以后** 对应“回归重放”这一步。它不是故事情节的复述，而是要求读者解释：单元测试在隔离边界内安排输入、执行行为并断言结果；敏捷迭代依赖快速、确定、可重复的反馈而非测试数量。

复核 **一年以后** 时，为 4.4 敏捷下的单元测试 固定一个最小输入，先写出预期状态和输出，再改变与“回归重放”直接相连的一个条件。若出现“测试依赖真实时间、网络或共享数据库，导致相同代码随机红绿”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

## 最小可重放实现

```text
# 4.4 敏捷下的单元测试：变更必须有独立、可重放的验证
baseline = verify(clean_checkout())
changed = verify(apply_one_change())
assert changed.expected_delta_only
assert verify(revert_change()) == baseline
```

这段实现草图只表达 4.4 敏捷下的单元测试 的验证合同，不复制书中叙事或代码。实际运行时应保存输入、环境、每个节点状态、最终结果和复位结果，使另一位读者能够从干净状态重放。

## 专属因果实验

先预测改变一个控制量后，4.4 敏捷下的单元测试 的哪一节点最先变化。依次切换基线、边界和注入故障，保存一次轨迹；随后点击“重置实验”，确认控件、节点选择、压力值和状态文本全部恢复。

<Crv18Section0404Lab />

## 故障诊断

<Callout
  type="trap"
  title="误区：测试依赖真实时间、网络或共享数据库，导致相同代码随机红绿"
>
  该错误会破坏 4.4 敏捷下的单元测试
  从“功能完成后由人手工点一遍”到“每次小变更立即得到可定位反馈”的转换。诊断时按
  安排夹具 → 执行单元 → 断言结果 → 清理状态 → 回归重放
  前进，只在首次出现预期与实际不一致的位置停下。
</Callout>

<Callout type="trap" title="误区：最终成功就算机制正确">
  4.4 敏捷下的单元测试
  的最终输出可能被缓存、重试、默认值或偶然时序掩盖。必须同时保存中间状态、拒绝语义和复位重放；缺少任一项都不能据此推广到更大规模。
</Callout>

<Callout type="tip" title="恢复顺序">
  4.4 敏捷下的单元测试 的恢复顺序是：还原输入与环境 → 清理派生状态 →
  重建当前边界 → 重跑机制链 → 对照公式或合同 → 再次注入同一故障确认可重复拒绝。
</Callout>

## 术语与边界

4.4 敏捷下的单元测试 使用五个可验证术语：<Term def="安排夹具 是 4.4 敏捷下的单元测试 机制链中的可观察状态或边界，必须用实际输入和轨迹验证">安排夹具</Term>、<Term def="执行单元 是 4.4 敏捷下的单元测试 机制链中的可观察状态或边界，必须用实际输入和轨迹验证">执行单元</Term>、<Term def="断言结果 是 4.4 敏捷下的单元测试 机制链中的可观察状态或边界，必须用实际输入和轨迹验证">断言结果</Term>、<Term def="清理状态 是 4.4 敏捷下的单元测试 机制链中的可观察状态或边界，必须用实际输入和轨迹验证">清理状态</Term>、<Term def="回归重放 是 4.4 敏捷下的单元测试 机制链中的可观察状态或边界，必须用实际输入和轨迹验证">回归重放</Term>。术语若不能指向本页实验中的控件、节点或状态，就仍然只是目录名词。

<Glossary>

<GlossaryItem term="安排夹具">
  安排夹具 位于 4.4 敏捷下的单元测试 的第 1
  个检查点；验收记录必须包含它的输入、状态、输出和故障表现。
</GlossaryItem>

<GlossaryItem term="执行单元">
  执行单元 位于 4.4 敏捷下的单元测试 的第 2
  个检查点；验收记录必须包含它的输入、状态、输出和故障表现。
</GlossaryItem>

<GlossaryItem term="断言结果">
  断言结果 位于 4.4 敏捷下的单元测试 的第 3
  个检查点；验收记录必须包含它的输入、状态、输出和故障表现。
</GlossaryItem>

<GlossaryItem term="清理状态">
  清理状态 位于 4.4 敏捷下的单元测试 的第 4
  个检查点；验收记录必须包含它的输入、状态、输出和故障表现。
</GlossaryItem>

<GlossaryItem term="回归重放">
  回归重放 位于 4.4 敏捷下的单元测试 的第 5
  个检查点；验收记录必须包含它的输入、状态、输出和故障表现。
</GlossaryItem>

</Glossary>

## 本页小结

4.4 敏捷下的单元测试 的关键不是记住故事，而是能解释“单元测试在隔离边界内安排输入、执行行为并断言结果；敏捷迭代依赖快速、确定、可重复的反馈而非测试数量”。完成标准是用本页合同推出预期，沿五个专属节点观察实际，诊断“测试依赖真实时间、网络或共享数据库，导致相同代码随机红绿”，并证明复位后的同一输入重新得到基线轨迹。

<Exercises>

1. 4.4 敏捷下的单元测试 的五节点链中，哪个状态最能区分“功能完成后由人手工点一遍”和“每次小变更立即得到可定位反馈”？请给出一个最小样本。

<Answer>
  先检查 安排夹具 的输入身份，再观察 执行单元 与
  断言结果。只改变一个条件时，最早发生可解释变化的节点就是区分两种状态的证据；目录节点逐项核对为：4.4
  敏捷下的单元测试（检查 安排夹具）；敏捷运动（检查 执行单元）；困惑（检查
  断言结果）；讨论（检查 清理状态）；一年以后（检查 回归重放）。
</Answer>

2. 使用本页核心合同完成一次手算、状态推演或代码断言，并说明每个量对应哪个真实对象。

<Answer>
  为 4.4 敏捷下的单元测试
  写下输入值、单位、版本和边界，按公式得到预期，再与实验中的 断言结果 和
  回归重放
  比较。若发生隐式单位转换、状态复用或顺序变化，必须先修正模型，不能只调整答案。
</Answer>

3. 注入“测试依赖真实时间、网络或共享数据库，导致相同代码随机红绿”后，怎样证明恢复动作有效而非偶然成功？

<Answer>
  保存故障前基线与最早偏离点，执行本页恢复顺序，再用完全相同输入运行两次。4.4
  敏捷下的单元测试
  只有在控件状态、五节点轨迹、最终输出和拒绝结果都可重复时才算恢复完成。
</Answer>

</Exercises>

<Attribution />
