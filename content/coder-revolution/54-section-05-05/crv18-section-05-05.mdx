---
title: "5.5 命令式编程 VS 声明式编程"
type: B
section: "码农翻身（2018年第1版）"
order: 55
description: "5.5 命令式编程 VS 声明式编程：从“所有问题都手写逐步控制流”推进到“把策略选择交给有全局信息的执行器”，用专属机制图、实验和故障诊断验证。"
demo: true
math: true
sourceUrl: "https://www.phei.com.cn/module/goods/wssd_content.jsp?bookid=52126"
qualityVersion: 2
practiceMode: code
sourceMode: independent-rewrite
draft: false
---

import { Crv18Section0505Lab } from "@/components/mdx/coder-revolution/diagrams/crv18-section-05-05";
import {
  Objectives,
  Callout,
  Term,
  Glossary,
  GlossaryItem,
  Exercises,
  Answer,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释 5.5 命令式编程 VS 声明式编程 为什么把“所有问题都手写逐步控制流”推进为“把策略选择交给有全局信息的执行器”
- 能沿 描述目标 → 解析约束 → 生成计划 → 执行计划 → 核对结果 说清对象、状态和边界
- 能用本页公式或状态合同完成一次手算、代码、模拟、诊断或方案判断
- 能注入“认为声明式描述没有执行成本，忽略查询计划、求解策略和数据规模”，定位最早偏离点并验证复位

</Objectives>

## 为什么需要这一机制

5.5 命令式编程 VS 声明式编程 不能停在故事类比。真实系统要解决的变化是从 **所有问题都手写逐步控制流** 到 **把策略选择交给有全局信息的执行器**；可执行机制是：命令式程序给出状态变化步骤，声明式程序描述期望关系或结果并把求解策略交给执行器；两者可在不同层次组合。

对 5.5 命令式编程 VS 声明式编程 的验收从同一份最小输入开始，先预测 描述目标、解析约束、生成计划、执行计划、核对结果 各自会看到什么，再运行正常、边界和故障场景。故事帮助记住矛盾，状态、协议、类型、构建或反馈证据才决定解释是否成立。

<Callout type="info" title="范围与事实来源">
  本页按电子工业出版社公开目录定位 5.5 命令式编程 VS 声明式编程
  的问题范围，并依据 [PostgreSQL current
  documentation](https://www.postgresql.org/docs/current/)
  独立重写技术机制。目录只证明主题坐标，不代表取得或复现原书正文。
</Callout>

## 核心合同

$$
declarative:\ result=arg\ satisfy(constraints)
$$

在 5.5 命令式编程 VS 声明式编程 中，公式或状态表达式里的量必须绑定到具体对象、单位、版本或生命周期。先给基线输入算出预期，再逐步观察；如果最终结果碰巧相同，但中途已违反 生成计划 或 执行计划，仍应拒绝该实现。

## 目录节点到机制证据

### 5.5 命令式编程 VS 声明式编程

在 **5.5 命令式编程 VS 声明式编程** 中，目录坐标 **5.5 命令式编程 VS 声明式编程** 对应“描述目标”这一步。它不是故事情节的复述，而是要求读者解释：命令式程序给出状态变化步骤，声明式程序描述期望关系或结果并把求解策略交给执行器；两者可在不同层次组合。

复核 **5.5 命令式编程 VS 声明式编程** 时，为 5.5 命令式编程 VS 声明式编程 固定一个最小输入，先写出预期状态和输出，再改变与“描述目标”直接相连的一个条件。若出现“认为声明式描述没有执行成本，忽略查询计划、求解策略和数据规模”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

### 一则小故事

在 **5.5 命令式编程 VS 声明式编程** 中，目录坐标 **一则小故事** 对应“解析约束”这一步。它不是故事情节的复述，而是要求读者解释：命令式程序给出状态变化步骤，声明式程序描述期望关系或结果并把求解策略交给执行器；两者可在不同层次组合。

复核 **一则小故事** 时，为 5.5 命令式编程 VS 声明式编程 固定一个最小输入，先写出预期状态和输出，再改变与“解析约束”直接相连的一个条件。若出现“认为声明式描述没有执行成本，忽略查询计划、求解策略和数据规模”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

### 命令式编程

在 **5.5 命令式编程 VS 声明式编程** 中，目录坐标 **命令式编程** 对应“生成计划”这一步。它不是故事情节的复述，而是要求读者解释：命令式程序给出状态变化步骤，声明式程序描述期望关系或结果并把求解策略交给执行器；两者可在不同层次组合。

复核 **命令式编程** 时，为 5.5 命令式编程 VS 声明式编程 固定一个最小输入，先写出预期状态和输出，再改变与“生成计划”直接相连的一个条件。若出现“认为声明式描述没有执行成本，忽略查询计划、求解策略和数据规模”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

### 声明式编程

在 **5.5 命令式编程 VS 声明式编程** 中，目录坐标 **声明式编程** 对应“执行计划”这一步。它不是故事情节的复述，而是要求读者解释：命令式程序给出状态变化步骤，声明式程序描述期望关系或结果并把求解策略交给执行器；两者可在不同层次组合。

复核 **声明式编程** 时，为 5.5 命令式编程 VS 声明式编程 固定一个最小输入，先写出预期状态和输出，再改变与“执行计划”直接相连的一个条件。若出现“认为声明式描述没有执行成本，忽略查询计划、求解策略和数据规模”，应记录最早偏离的节点、拒绝结果以及恢复后的重放结果。

## 最小可重放实现

```text
// 5.5 命令式编程 VS 声明式编程：比较语义，不比较口号
const input = fixedCase();
const expected = referenceModel(input);
const actual = runProgram(input);
assert.deepEqual(actual, expected);
assert.deepEqual(resetAndRun(input), expected);
```

这段实现草图只表达 5.5 命令式编程 VS 声明式编程 的验证合同，不复制书中叙事或代码。实际运行时应保存输入、环境、每个节点状态、最终结果和复位结果，使另一位读者能够从干净状态重放。

## 专属因果实验

先预测改变一个控制量后，5.5 命令式编程 VS 声明式编程 的哪一节点最先变化。依次切换基线、边界和注入故障，保存一次轨迹；随后点击“重置实验”，确认控件、节点选择、压力值和状态文本全部恢复。

<Crv18Section0505Lab />

## 故障诊断

<Callout
  type="trap"
  title="误区：认为声明式描述没有执行成本，忽略查询计划、求解策略和数据规模"
>
  该错误会破坏 5.5 命令式编程 VS 声明式编程
  从“所有问题都手写逐步控制流”到“把策略选择交给有全局信息的执行器”的转换。诊断时按
  描述目标 → 解析约束 → 生成计划 → 执行计划 → 核对结果
  前进，只在首次出现预期与实际不一致的位置停下。
</Callout>

<Callout type="trap" title="误区：最终成功就算机制正确">
  5.5 命令式编程 VS 声明式编程
  的最终输出可能被缓存、重试、默认值或偶然时序掩盖。必须同时保存中间状态、拒绝语义和复位重放；缺少任一项都不能据此推广到更大规模。
</Callout>

<Callout type="tip" title="恢复顺序">
  5.5 命令式编程 VS 声明式编程 的恢复顺序是：还原输入与环境 → 清理派生状态 →
  重建当前边界 → 重跑机制链 → 对照公式或合同 → 再次注入同一故障确认可重复拒绝。
</Callout>

## 术语与边界

5.5 命令式编程 VS 声明式编程 使用五个可验证术语：<Term def="描述目标 是 5.5 命令式编程 VS 声明式编程 机制链中的可观察状态或边界，必须用实际输入和轨迹验证">描述目标</Term>、<Term def="解析约束 是 5.5 命令式编程 VS 声明式编程 机制链中的可观察状态或边界，必须用实际输入和轨迹验证">解析约束</Term>、<Term def="生成计划 是 5.5 命令式编程 VS 声明式编程 机制链中的可观察状态或边界，必须用实际输入和轨迹验证">生成计划</Term>、<Term def="执行计划 是 5.5 命令式编程 VS 声明式编程 机制链中的可观察状态或边界，必须用实际输入和轨迹验证">执行计划</Term>、<Term def="核对结果 是 5.5 命令式编程 VS 声明式编程 机制链中的可观察状态或边界，必须用实际输入和轨迹验证">核对结果</Term>。术语若不能指向本页实验中的控件、节点或状态，就仍然只是目录名词。

<Glossary>

<GlossaryItem term="描述目标">
  描述目标 位于 5.5 命令式编程 VS 声明式编程 的第 1
  个检查点；验收记录必须包含它的输入、状态、输出和故障表现。
</GlossaryItem>

<GlossaryItem term="解析约束">
  解析约束 位于 5.5 命令式编程 VS 声明式编程 的第 2
  个检查点；验收记录必须包含它的输入、状态、输出和故障表现。
</GlossaryItem>

<GlossaryItem term="生成计划">
  生成计划 位于 5.5 命令式编程 VS 声明式编程 的第 3
  个检查点；验收记录必须包含它的输入、状态、输出和故障表现。
</GlossaryItem>

<GlossaryItem term="执行计划">
  执行计划 位于 5.5 命令式编程 VS 声明式编程 的第 4
  个检查点；验收记录必须包含它的输入、状态、输出和故障表现。
</GlossaryItem>

<GlossaryItem term="核对结果">
  核对结果 位于 5.5 命令式编程 VS 声明式编程 的第 5
  个检查点；验收记录必须包含它的输入、状态、输出和故障表现。
</GlossaryItem>

</Glossary>

## 本页小结

5.5 命令式编程 VS 声明式编程 的关键不是记住故事，而是能解释“命令式程序给出状态变化步骤，声明式程序描述期望关系或结果并把求解策略交给执行器；两者可在不同层次组合”。完成标准是用本页合同推出预期，沿五个专属节点观察实际，诊断“认为声明式描述没有执行成本，忽略查询计划、求解策略和数据规模”，并证明复位后的同一输入重新得到基线轨迹。

<Exercises>

1. 5.5 命令式编程 VS 声明式编程 的五节点链中，哪个状态最能区分“所有问题都手写逐步控制流”和“把策略选择交给有全局信息的执行器”？请给出一个最小样本。

<Answer>
  先检查 描述目标 的输入身份，再观察 解析约束 与
  生成计划。只改变一个条件时，最早发生可解释变化的节点就是区分两种状态的证据；目录节点逐项核对为：5.5
  命令式编程 VS 声明式编程（检查 描述目标）；一则小故事（检查
  解析约束）；命令式编程（检查 生成计划）；声明式编程（检查 执行计划）。
</Answer>

2. 使用本页核心合同完成一次手算、状态推演或代码断言，并说明每个量对应哪个真实对象。

<Answer>
  为 5.5 命令式编程 VS 声明式编程
  写下输入值、单位、版本和边界，按公式得到预期，再与实验中的 生成计划 和
  核对结果
  比较。若发生隐式单位转换、状态复用或顺序变化，必须先修正模型，不能只调整答案。
</Answer>

3. 注入“认为声明式描述没有执行成本，忽略查询计划、求解策略和数据规模”后，怎样证明恢复动作有效而非偶然成功？

<Answer>
  保存故障前基线与最早偏离点，执行本页恢复顺序，再用完全相同输入运行两次。5.5
  命令式编程 VS 声明式编程
  只有在控件状态、五节点轨迹、最终输出和拒绝结果都可重复时才算恢复完成。
</Answer>

</Exercises>

<Attribution />
