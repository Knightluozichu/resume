---
title: 分行从上到下打印二叉树
type: C
section: 树
order: 6
description: 在层序遍历基础上记录每层节点数量，将二叉树按层分组输出。
demo: false
math: true
sourceUrl: https://github.com/zhedahht/CodingInterviews
draft: false
---

<Objectives>

- 在普通层序遍历基础上识别“层边界”。
- 掌握使用 `levelSize` 固定当前层节点数的写法。
- 能解释为什么不能边遍历边直接使用动态变化的队列长度。
- 能输出二维数组，每一行对应二叉树的一层。

</Objectives>

## 核心思路

普通层序遍历只要求一个线性输出；分行打印要求知道每一层在哪里结束。

最稳的方式是在处理每一层前先记录：

```text
levelSize = queue.length - head
```

这个数量就是当前层剩余节点数。随后只循环 `levelSize` 次，把这一层的节点收集成 `level` 数组，并把下一层孩子放入队列。当前层处理完后，`level` 就是一行。

## 代码对照

<CodeTabs>
  <Tab label="C++">
```cpp
#include <queue>
#include <vector>

struct TreeNode {
    int val;
    TreeNode* left;
    TreeNode* right;
};

std::vector<std::vector<int>> printByLines(TreeNode* root) {
    std::vector<std::vector<int>> result;
    if (root == nullptr) return result;

    std::queue<TreeNode*> q;
    q.push(root);

    while (!q.empty()) {
        int levelSize = static_cast<int>(q.size());
        std::vector<int> level;

        for (int i = 0; i < levelSize; ++i) {
            TreeNode* node = q.front();
            q.pop();
            level.push_back(node->val);

            if (node->left != nullptr) q.push(node->left);
            if (node->right != nullptr) q.push(node->right);
        }

        result.push_back(level);
    }

    return result;
}
```
  </Tab>
  <Tab label="TypeScript">
```typescript
interface TreeNode {
  val: number;
  left: TreeNode | null;
  right: TreeNode | null;
}

function levelOrderByLines(root: TreeNode | null): number[][] {
  if (!root) return [];

  const result: number[][] = [];
  const queue: TreeNode[] = [root];
  let head = 0;

  while (head < queue.length) {
    const levelSize = queue.length - head;
    const level: number[] = [];

    for (let i = 0; i < levelSize; i++) {
      const node = queue[head++];
      level.push(node.val);

      if (node.left) queue.push(node.left);
      if (node.right) queue.push(node.right);
    }

    result.push(level);
  }

  return result;
}
```
  </Tab>
</CodeTabs>

<Callout type="trap">
  <strong>现象 →</strong> 一层里混入了下一层节点。
  <strong>原因 →</strong> 循环条件直接使用动态变化的 `queue.length`，孩子入队后当前层长度被扩大。
  <strong>修法 →</strong> 每层开始前先缓存 `levelSize`，本层只处理这固定数量的节点。
</Callout>

<Attribution
  adaptedFrom="Coding Interviews, Second Edition"
  adaptedUrl="https://github.com/zhedahht/CodingInterviews"
/>
