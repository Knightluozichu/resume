---
title: "动画、交互、全局光照与数据可视化"
type: B
section: "计算机图形学与渲染"
order: 8
description: "从时域状态与输入反馈，到阴影/延迟、ray/path tracing、radiosity、算法建模和 volume rendering"
demo: true
math: true
sourceUrl: "https://www.pearson.com/en-gb/subject-catalog/p/computer-graphics-with-open-gl-pearson-new-international-edition/P200000004424/9781292037196"
draft: true
---

import {
  Cg4AdvancedRenderingDiagram,
  Cg4AdvancedMethodsDiagram,
} from "@/components/mdx/computer-graphics-4e/diagrams/cg4-advanced-rendering";
import { Cg4StepFlowDiagram } from "@/components/mdx/computer-graphics-4e/diagrams/cg4-step-flow";
import {
  Objectives,
  Callout,
  Attribution,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能描述 animation state、interaction events、rendering/transport 与 visualization encoding 构成的反馈闭环
- 能实现 keyframe/fixed-step 更新、ID/ray picking、shadow/deferred 和 volume compositing 的最小探针
- 能推导 path tracing estimator、radiosity patch equation 与 emission-absorption 前向合成
- 能比较 shadow map、deferred、path tracing、radiosity 和 volume rendering 的偏差、方差、带宽及数据边界

</Objectives>

## 高级主题是应用闭环

原书后半部的 **Computer Animation / 计算机动画**、**Interactive Input Methods and Graphical User Interfaces / 交互输入与图形用户界面**、**Global Illumination / 全局光照**、**Algorithmic Modeling / 算法建模**和 **Visualization of Data Sets / 数据集可视化**共同回答：内容如何随时间产生，用户如何改变它，光/数据如何到达图像，图像又如何被解释。

<Term def="模型/数据状态随时间更新，经用户输入反馈、渲染和显示，再影响下一次输入与状态的循环">图形应用闭环</Term>不能只以一张最终截图验收。动画需要时域稳定，交互需要坐标和延迟正确，GI 需要估计误差，visualization 需要保持数据语义。

<Cg4AdvancedRenderingDiagram />

## Computer Animation：状态是时间函数

<Term def="在给定时间记录对象变换、形状、材质或其他属性，并在相邻记录间插值生成动画的样本">Keyframe</Term>动画把状态写成 $x(t)$。位置线性插值：

$$
\mathbf p(t)=(1-u)\mathbf p_0+u\mathbf p_1,
\qquad u=\frac{t-t_0}{t_1-t_0}.
$$

Rotation 不宜直接逐分量插值 Euler angles；quaternion slerp 选择单位球上的弧，并处理 $q$与 $-q$等价和 shortest path。Scale、morph weights、camera parameters 也应按语义插值，而不是所有属性统一 lerp。

物理/程序动画应将 simulation timestep 与 display frame 分开。Fixed timestep accumulator：

```cpp
accumulator += clamp(realDeltaTime, 0.0, maxFrameDelta);
while (accumulator >= fixedDt) {
  previousState = currentState;
  simulate(currentState, fixedDt);
  accumulator -= fixedDt;
}
double alpha = accumulator / fixedDt;
render(interpolate(previousState, currentState, alpha));
```

它提高不同帧率下的可复现性，但网络、碰撞和随机数还需 deterministic policy。Motion blur/TAA 需要保存 previous transforms；teleport/animation reset 时必须使历史失效。

## Interaction 与 GUI：屏幕输入要回到场景

<Term def="把鼠标、触摸、手柄等设备事件经窗口/viewport/相机坐标映射为对象选择或操作语义的过程">交互拾取</Term>有两条常见路径：

1. **ID buffer picking**：绘制 object/primitive ID 到整数 attachment，点击时读取对应 pixel；需处理 DPI、viewport origin、MSAA resolve 和异步 readback。
2. **Ray picking**：把 screen point unproject 成 world ray，与 BVH/geometry 求交；需使用与渲染一致的 view/projection/depth conventions。

```cpp
Ray screenRay(Vec2 cursorLogical, const Viewport& vp, const Camera& camera) {
  Vec2 framebufferPixel = logicalToFramebuffer(cursorLogical, vp.dpiScale);
  Vec2 ndc = framebufferToNdc(framebufferPixel, vp);
  Vec4 nearH = inverse(camera.viewProjection) * clipPoint(ndc, camera.nearNdc);
  Vec4 farH  = inverse(camera.viewProjection) * clipPoint(ndc, camera.farNdc);
  Vec3 nearP = nearH.xyz / nearH.w;
  Vec3 farP = farH.xyz / farH.w;
  return {nearP, normalize(farP - nearP)};
}
```

GUI 还要管理 focus、capture、keyboard navigation、accessibility 和 event ordering。Picking hit 只是输入，不应在 render callback 中随意改 scene graph；使用 command/state update 边界能保持动画、undo 和多线程一致。

## Shadow Map 是 Direct Visibility 近似

<Term def="从光源视角离散记录最近深度，再将相机可见点投影到该深度图比较的直接光可见性算法">Shadow mapping</Term>估计的是光源到表面的 visibility，不是 global illumination。

```glsl
float hardShadow(vec4 lightClip, sampler2D shadowDepth, float receiverBias) {
  vec3 p = lightClip.xyz / lightClip.w;
  vec2 uv = p.xy * 0.5 + 0.5;
  float receiver = p.z * 0.5 + 0.5;
  if (any(lessThan(uv, vec2(0))) || any(greaterThan(uv, vec2(1)))) return 0.0;
  float blocker = texture(shadowDepth, uv).r;
  return receiver - receiverBias > blocker ? 1.0 : 0.0;
}
```

Bias 取决于 depth convention、slope、normal、resolution 和 projection，固定 0.005 不是通用答案。过小产生 self-shadow acne，过大产生 Peter Panning。PCF 是对离散 comparisons 过滤，产生软边近似；真实 area-light penumbra 随 blocker/receiver geometry 变化。

## Forward/Deferred 是数据调度选择

<Term def="先保存相机可见 opaque surface 的材质/几何属性，再以屏幕空间 light passes 求光的架构">Deferred shading</Term>可避免对被遮挡表面执行昂贵多光源着色，并方便 light volumes/tiles，但成本是 G-buffer memory bandwidth、格式精度、MSAA 和 material variation。

“前向 $O(geometry\times lights)$、延迟 $O(geometry+pixels\times lights)$”只是粗略口号。Forward+/clustered 也会筛 light lists；deferred 每 pixel 仍可能遍历很多 lights。真实工作取决于 visible samples、light overlap、material cost、overdraw 和 bandwidth。

G-buffer通常保存最近 opaque layer，不代表 deferred “无法支持透明”。透明层可在 opaque deferred 后用 forward/clustered pass，或采用 OIT/multi-layer structures；只是单层 G-buffer不能直接表示多个透明 surfaces。

## Global Illumination 与渲染方程

<Term def="同时考虑直接和一次或多次间接光传输，使表面收到其他表面反射/发射贡献的计算">全局光照</Term>目标由 rendering equation 表达：

$$
L_o(x,\omega_o)=L_e(x,\omega_o)+
\int_{\Omega^+}f_r(x,\omega_i,\omega_o)L_i(x,\omega_i)
(\mathbf n\cdot\omega_i)d\omega_i.
$$

Ray tracing 只是沿 ray 求最近交点/可见性，不自动物理精确。Whitted ray tracing 递归镜面反射/折射；path tracing 用 Monte Carlo 随机路径估计更一般积分。对半球积分采样 $\omega_i\sim p$：

$$
\hat L_o= L_e+\frac1N\sum_{k=1}^{N}
\frac{f_r(\omega_k,\omega_o)L_i(\omega_k)
(\mathbf n\cdot\omega_k)}{p(\omega_k)}.
$$

Estimator 是否 unbiased/consistent、variance 大小、PDF 是否覆盖非零 integrand、Russian roulette 和 MIS 权重都影响正确性。更多 samples 降低随机噪声约 $1/\sqrt N$，不修复 bias 或错误 BRDF。

## Radiosity：Diffuse Patch 之间的能量

<Term def="把表面离散为 diffuse patches，用 form factors 建立各 patch 发射与反射能量平衡的全局光照方法">Radiosity</Term>方程：

$$
B_i=E_i+\rho_i\sum_jF_{ij}B_j,
$$

其中 $F_{ij}$是 patch i 到 j 的 form factor，包含几何、距离、朝向和 visibility。封闭系统应近似满足 $\sum_jF_{ij}=1$，互易关系 $A_iF_{ij}=A_jF_{ji}$是数值检查。

Radiosity 适合 diffuse、相对静态场景，可预计算 view-independent patch radiosity；离散误差、form-factor 成本和动态更新是边界。它与 path tracing 不是“旧/新同一算法”，而是不同表示和求解策略。

## Algorithmic Modeling：生成结构也要可验证

Fractal/L-system、particle system、procedural field 和 subdivision 从规则生成内容。验收不只看复杂：

- random seed 与参数可复现；
- topology/bounds/level of detail 不爆炸；
- 粒子 timestep、spawn/death 和 conservation 按模型定义；
- procedural frequency 与采样率匹配，避免空间/时间 aliasing；
- 输出 mesh/field 与后续 visibility/lighting 的数据合同一致。

算法建模可驱动动画和可视化，例如 L-system 生长、streamline particles、implicit field isosurface。

## Visualization of Data Sets：颜色必须表达数据

<Term def="把 scalar、vector、tensor 或 volume data 映射为位置、几何、颜色、opacity、glyph 或动画，以支持任务判断的过程">数据可视化</Term>的 transfer function 不只是美术渐变。Color map 应匹配 ordered/diverging/categorical data，标出单位和范围，避免 rainbow 产生假边界；缺失/越界值需显式编码。

Scalar field 可用 contour/isosurface；vector field 可用 glyph、streamline/particle；volume data 可用 slice、isosurface 或 direct volume rendering。

Emission-absorption ray marching 的离散 front-to-back 合成：

$$
\mathbf C\leftarrow\mathbf C+(1-A)\alpha_i\mathbf c_i,
\qquad
A\leftarrow A+(1-A)\alpha_i.
$$

当 A 接近 1 可 early terminate；空空间可跳跃。但 opacity 依 step length，改变采样步长时要做 opacity correction，否则同一体数据会变浓/变淡。

```glsl
vec4 integrateVolume(Ray ray) {
  vec3 color = vec3(0.0);
  float opacity = 0.0;
  for (float t = ray.tMin; t < ray.tMax && opacity < 0.995; t += stepSize) {
    float scalar = sampleVolume(ray.origin + t * ray.direction);
    vec4 sampleValue = transferFunction(scalar);
    float alpha = correctOpacity(sampleValue.a, stepSize, referenceStep);
    color += (1.0 - opacity) * alpha * sampleValue.rgb;
    opacity += (1.0 - opacity) * alpha;
  }
  return vec4(color, opacity);
}
```

Volume interpolation、gradient normal、transfer function、sampling rate 和 compositing order 都应在合成数据上验证。好看的云状图不证明数值位置和单位正确。

<Cg4AdvancedMethodsDiagram />

## 三步建立综合场景

<Stepper>
<Step title="第一步：冻结时间与交互状态">

用 fixed-step 更新 keyframe/procedural/particle state，记录 previous/current transforms；验证 DPI/viewport/unprojection 和 picking IDs。

<Cg4StepFlowDiagram
  caption="时间与输入先可复现，后续图像差异才可解释。"
  stage={1}
  stages={["State：fixed time、keyframes、procedural seed、input/picking coordinates"]}
/>

</Step>
<Step title="第二步：验证 transport paths">

分别输出 direct visibility、G-buffer、path samples/radiance 和 radiosity form-factor checks，与简单 reference scene 对照。

<Cg4StepFlowDiagram
  caption="每种 transport 方法保存自己的偏差、方差和可见性证据。"
  stage={2}
  stages={[
    "State：fixed time、keyframes、procedural seed、input/picking coordinates",
    "Transport：shadow/deferred、path estimator、radiosity conservation",
  ]}
/>

</Step>
<Step title="第三步：验证可视编码">

用解析 scalar/vector/volume fields 检查 transfer function、isosurface、ray integration 和 step-size invariance，并记录交互任务结果。

<Cg4StepFlowDiagram
  caption="可视化验收同时检查图像、数值位置、单位和用户任务。"
  stage={3}
  stages={[
    "State：fixed time、keyframes、procedural seed、input/picking coordinates",
    "Transport：shadow/deferred、path estimator、radiosity conservation",
    "Visualization：known fields、transfer semantics、volume convergence、tasks",
  ]}
/>

</Step>
</Stepper>

## 常见误区

<Callout type="trap">
**现象 →** 不同刷新率下动画速度或粒子轨迹不同，TAA 历史不断拖影。
**原因 →** Simulation 直接使用不受限 variable frame delta，且 reset/teleport 未使 previous state 失效。
**修法 →** Fixed-step accumulator + render interpolation；定义 history reset 和 deterministic seed 策略。
</Callout>

<Callout type="trap">
**现象 →** Path tracer 有噪声就无限加 samples，但均值仍偏亮。
**原因 →** 把 variance 当唯一问题，PDF、BRDF normalization、MIS 或颜色空间存在 bias。
**修法 →** 用解析场景和 furnace 检查 estimator 均值，再观察 $1/\sqrt N$收敛；先修 bias 后降 variance。
</Callout>

<Callout type="trap">
**现象 →** Volume step size 减半后整体更不透明，研究者误以为数据变化。
**原因 →** 每 sample alpha 未按步长校正，采样次数翻倍导致累计 opacity 改变。
**修法 →** 将 transfer opacity解释为 reference length extinction并做 step correction；对均匀 volume 验证步长收敛。
</Callout>

## 练习

<Exercises>

1. **动画：** 60 Hz 和 144 Hz 显示器运行同一粒子模拟，怎样保证核心状态近似一致又渲染平滑？

<Answer>
用固定 simulation dt（如 1/120s）累积 real time并执行整数步；渲染在 previous/current fixed states 间按剩余 accumulator 插值。随机数使用确定 seed/顺序，限制单帧最大 catch-up 防 spiral。显示刷新只改变插值采样，不改变模拟步长。
</Answer>

2. **GI：** “使用 ray tracing 所以结果物理精确”为何错误？

<Answer>
Ray tracing只提供沿 ray 的求交/可见性。若只追一次 shadow/reflection、使用非守能 BRDF、错误 light units/PDF、遗漏路径或 tone/color错误，结果仍不物理。Path tracing还需正确 estimator、sampling/PDF、MIS、termination和收敛验证。
</Answer>

3. **Volume：** Front-to-back 累积当前 A=0.4，本 sample alpha=0.25、color=(1,0,0)。本次增加多少 color 和 opacity？

<Answer>
未覆盖比例为 1-A=0.6。Color 增量为 $0.6×0.25×(1,0,0)=(0.15,0,0)$；opacity 增量为 0.6×0.25=0.15，新 A=0.55。前提是 alpha 已按当前 step length 校正。
</Answer>

</Exercises>

## 本章回顾

- Animation/interaction 把时间和输入映射为可复现状态，再进入 rendering 与反馈；
- Shadow map估 direct visibility，deferred 重排 surface/light 数据，不等于 GI 或“不能透明”；
- Ray tracing 是求交框架，path tracing/radiosity 以不同 estimator/表示求全局光传输；
- Data visualization 必须保留单位、范围和 transfer semantics，volume integration需对 step size 收敛。

## 术语表

<Glossary>
<GlossaryItem term="图形应用闭环">状态随时间/输入更新，经渲染显示后再影响下一轮输入与状态的循环。</GlossaryItem>
<GlossaryItem term="Keyframe">在指定时间记录变换、形状或材质等属性、供相邻状态插值的动画样本。</GlossaryItem>
<GlossaryItem term="交互拾取">把设备事件和屏幕坐标映射为场景对象选择/操作语义的过程。</GlossaryItem>
<GlossaryItem term="Shadow mapping">从光源记录离散深度并在相机 pass 比较的直接光可见性算法。</GlossaryItem>
<GlossaryItem term="Deferred shading">先保存相机可见 opaque surface 属性，再用屏幕空间 light passes 求光的架构。</GlossaryItem>
<GlossaryItem term="全局光照">考虑直接和一次或多次间接反射/发射贡献的光传输计算。</GlossaryItem>
<GlossaryItem term="Radiosity">以 diffuse patches 和 form factors 求能量平衡的全局光照方法。</GlossaryItem>
<GlossaryItem term="数据可视化">把多类数据映射为位置、几何、颜色、opacity、glyph 或动画以支持判断的过程。</GlossaryItem>
</Glossary>

<Attribution adaptedFrom="Computer Graphics with OpenGL, 4th Edition" adaptedUrl="https://www.pearson.com/en-gb/subject-catalog/p/computer-graphics-with-open-gl-pearson-new-international-edition/P200000004424/9781292037196" />
