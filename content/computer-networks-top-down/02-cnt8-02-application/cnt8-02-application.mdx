---
title: "第2章 应用层"
description: "从进程通信与运输服务需求进入HTTP、电子邮件、DNS、P2P、视频/CDN和UDP/TCP套接字编程"
type: A
section: "计算机网络：自顶向下方法（原书第8版）"
order: 2
draft: false
math: false
sourceUrl: "https://www.cmpedu.com/books/book/5606311.htm"
demo: true
---

import {
  Cnt802ApplicationMapLab,
  Cnt802ApplicationExperimentLab,
  Cnt802ApplicationEvidenceLab,
} from "@/components/mdx/computer-networks-top-down/diagrams/cnt8-02-application";

# 第2章 应用层

本课程锁定James F. Kurose、Keith W. Ross著，陈鸣译《计算机网络：自顶向下方法（原书第8版）》，机械工业出版社，2022年10月，ISBN 9787111712367；英文原版Pearson第8版，2020年出版、2021版权，ISBN 9780135928615。机械工业出版社官方中文目录与Pearson、作者官方第8版目录共同确认8章结构；正式分母为8个章标题和全部编号节/小节，共218个节点。课后习题、编程作业、Wireshark实验、人物专访和参考文献作为学习支持保留说明，但不重复计入核心节点。

<Objectives>

- 能解释“第2章 应用层”全部节点，并把术语放回端到端协议路径。
- 能绘制通信实体、报文次序、状态机、表项和逐层封装。
- 能设计单变量实验，验证“每个应用协议都能说明通信进程、寻址、报文格式与次序、运输服务、状态位置、缓存或复制策略以及失败语义”。
- 能写出含版本边界、性能、故障、恢复与复核人的独立证据包。

</Objectives>

## 从一条可证伪的端到端路径开始

先预测：只记端口号而不区分应用状态、运输连接、缓存一致性和命名依赖，会在代理、CDN、DNS或并发连接处误判。把预测写成“应用意图、运输状态、数据平面、控制平面、链路/无线、安全”六行，再运行协议或抓包。若结果与预测不同，先定位首个偏差，不把后续连锁症状冒充根因。

本页主问题是：从进程通信与运输服务需求进入HTTP、电子邮件、DNS、P2P、视频/CDN和UDP/TCP套接字编程。每个箭头要注明通信实体、报文、地址或标识、状态变化、定时器以及证据位置；每个性能结论要注明负载、路径、窗口和瓶颈假设。

验收不变量是：每个应用协议都能说明通信进程、寻址、报文格式与次序、运输服务、状态位置、缓存或复制策略以及失败语义。一次ping、一个HTTP状态码或一个接口up都只证明局部事实，不能独立证明名称解析、双向运输、转发、移动性与身份安全全部成立。

## 核心词汇与第8版边界

<Term def="应用进程与运输层之间发送和接收报文的编程接口">套接字</Term>、
<Term def="定义Web客户与服务器交换请求和响应报文语义的应用层协议">HTTP</Term>、
<Term def="把主机名等名称映射为资源记录并由分层服务器协作提供的目录服务">
  DNS
</Term>
、<Term def="把内容副本部署到多个边缘位置并按策略选择交付节点的系统">CDN</Term>
、
<Term def="端系统同时承担客户与服务方角色并直接贡献分发能力的体系结构">
  P2P
</Term>

这些词汇固定在原书第8版语境。HTTP/2、QUIC/HTTP/3演化、CUBIC/BBR、SDN、NETCONF/YANG、4G/5G和WPA3语境属于本版；第9版重排或新增内容只能另行比较，不能修改218个节点分母。

<Cnt802ApplicationMapLab />

## 核心机制深读

### 先写应用需求再选运输服务

可靠性、吞吐、时延和安全需求不同，选择TCP或UDP不能只按习惯。协议还要定义消息边界、错误、重试、幂等和状态归属。

动手试：先画预期报文时间线与状态变化，再抓取一次正常流和一次单变量故障流。标出首个偏差，避免用最终症状倒推任意根因。

### HTTP无状态不等于系统无状态

HTTP请求可独立解释，但Cookie、会话存储、缓存验证器和后端数据库保存业务状态。持续连接减少握手，HTTP/2用多路复用缓解应用层队头问题。

动手试：先画预期报文时间线与状态变化，再抓取一次正常流和一次单变量故障流。标出首个偏差，避免用最终症状倒推任意根因。

### DNS是一棵分布式委派树

递归解析器迭代访问根、顶级域和权威服务器，并按TTL缓存资源记录。故障分析要区分查询超时、否定应答、旧缓存和权威数据错误。

动手试：先画预期报文时间线与状态变化，再抓取一次正常流和一次单变量故障流。标出首个偏差，避免用最终症状倒推任意根因。

### 内容分发同时处理容量与位置

DASH让客户按测得带宽选分片码率，CDN把副本靠近用户并进行请求映射；P2P则让新对等方同时增加需求与上传能力。

动手试：先画预期报文时间线与状态变化，再抓取一次正常流和一次单变量故障流。标出首个偏差，避免用最终症状倒推任意根因。

### 套接字程序必须验证消息边界

UDP保留数据报边界但不保证到达和次序；TCP提供字节流，单次读取不对应单条应用消息。正确程序要循环读写并定义定界或长度字段。

动手试：先画预期报文时间线与状态变化，再抓取一次正常流和一次单变量故障流。标出首个偏差，避免用最终症状倒推任意根因。

## 原书目录逐节点重构

### 第2章 应用层

把“应用层”放回“从进程通信与运输服务需求进入HTTP、电子邮件、DNS、P2P、视频/CDN和UDP/TCP套接字编程”的因果链，写出通信实体、输入状态、报文或算法、输出状态、性能边界和失败语义。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“应用层”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“应用层”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

### 2.1 网络应用原理

把“网络应用原理”放回“从进程通信与运输服务需求进入HTTP、电子邮件、DNS、P2P、视频/CDN和UDP/TCP套接字编程”的因果链，写出通信实体、输入状态、报文或算法、输出状态、性能边界和失败语义。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“网络应用原理”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“网络应用原理”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 2.1.1 网络应用体系结构

把“网络应用体系结构”放回“从进程通信与运输服务需求进入HTTP、电子邮件、DNS、P2P、视频/CDN和UDP/TCP套接字编程”的因果链，写出通信实体、输入状态、报文或算法、输出状态、性能边界和失败语义。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“网络应用体系结构”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“网络应用体系结构”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 2.1.2 进程通信

把“进程通信”放回“从进程通信与运输服务需求进入HTTP、电子邮件、DNS、P2P、视频/CDN和UDP/TCP套接字编程”的因果链，写出通信实体、输入状态、报文或算法、输出状态、性能边界和失败语义。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“进程通信”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“进程通信”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 2.1.3 可供应用程序使用的运输服务

把“可供应用程序使用的运输服务”放回“从进程通信与运输服务需求进入HTTP、电子邮件、DNS、P2P、视频/CDN和UDP/TCP套接字编程”的因果链，写出通信实体、输入状态、报文或算法、输出状态、性能边界和失败语义。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“可供应用程序使用的运输服务”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“可供应用程序使用的运输服务”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 2.1.4 因特网提供的运输服务

把“因特网提供的运输服务”放回“从进程通信与运输服务需求进入HTTP、电子邮件、DNS、P2P、视频/CDN和UDP/TCP套接字编程”的因果链，写出通信实体、输入状态、报文或算法、输出状态、性能边界和失败语义。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“因特网提供的运输服务”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“因特网提供的运输服务”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 2.1.5 应用层协议

把“应用层协议”放回“从进程通信与运输服务需求进入HTTP、电子邮件、DNS、P2P、视频/CDN和UDP/TCP套接字编程”的因果链，写出通信实体、输入状态、报文或算法、输出状态、性能边界和失败语义。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“应用层协议”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“应用层协议”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 2.1.6 本书涉及的网络应用

把“本书涉及的网络应用”放回“从进程通信与运输服务需求进入HTTP、电子邮件、DNS、P2P、视频/CDN和UDP/TCP套接字编程”的因果链，写出通信实体、输入状态、报文或算法、输出状态、性能边界和失败语义。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“本书涉及的网络应用”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“本书涉及的网络应用”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

### 2.2 Web和HTTP

沿请求方法、目标、首部、状态码、连接复用和缓存验证器画出客户与服务器状态；区分应用语义和运输连接。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“Web和HTTP”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“Web和HTTP”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 2.2.1 HTTP概述

沿请求方法、目标、首部、状态码、连接复用和缓存验证器画出客户与服务器状态；区分应用语义和运输连接。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“HTTP概述”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“HTTP概述”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 2.2.2 非持续连接和持续连接

把“非持续连接和持续连接”放回“从进程通信与运输服务需求进入HTTP、电子邮件、DNS、P2P、视频/CDN和UDP/TCP套接字编程”的因果链，写出通信实体、输入状态、报文或算法、输出状态、性能边界和失败语义。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“非持续连接和持续连接”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“非持续连接和持续连接”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 2.2.3 HTTP报文格式

沿请求方法、目标、首部、状态码、连接复用和缓存验证器画出客户与服务器状态；区分应用语义和运输连接。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“HTTP报文格式”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“HTTP报文格式”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 2.2.4 用户与服务器的交互：cookie

沿请求方法、目标、首部、状态码、连接复用和缓存验证器画出客户与服务器状态；区分应用语义和运输连接。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“用户与服务器的交互：cookie”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“用户与服务器的交互：cookie”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 2.2.5 Web缓存

沿请求方法、目标、首部、状态码、连接复用和缓存验证器画出客户与服务器状态；区分应用语义和运输连接。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“Web缓存”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“Web缓存”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 2.2.6 HTTP/2

沿请求方法、目标、首部、状态码、连接复用和缓存验证器画出客户与服务器状态；区分应用语义和运输连接。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“HTTP/2”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“HTTP/2”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

### 2.3 因特网中的电子邮件

把“因特网中的电子邮件”放回“从进程通信与运输服务需求进入HTTP、电子邮件、DNS、P2P、视频/CDN和UDP/TCP套接字编程”的因果链，写出通信实体、输入状态、报文或算法、输出状态、性能边界和失败语义。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“因特网中的电子邮件”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“因特网中的电子邮件”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 2.3.1 SMTP

把“SMTP”放回“从进程通信与运输服务需求进入HTTP、电子邮件、DNS、P2P、视频/CDN和UDP/TCP套接字编程”的因果链，写出通信实体、输入状态、报文或算法、输出状态、性能边界和失败语义。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“SMTP”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“SMTP”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 2.3.2 邮件报文格式

把“邮件报文格式”放回“从进程通信与运输服务需求进入HTTP、电子邮件、DNS、P2P、视频/CDN和UDP/TCP套接字编程”的因果链，写出通信实体、输入状态、报文或算法、输出状态、性能边界和失败语义。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“邮件报文格式”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“邮件报文格式”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 2.3.3 邮件访问协议

把“邮件访问协议”放回“从进程通信与运输服务需求进入HTTP、电子邮件、DNS、P2P、视频/CDN和UDP/TCP套接字编程”的因果链，写出通信实体、输入状态、报文或算法、输出状态、性能边界和失败语义。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“邮件访问协议”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“邮件访问协议”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

### 2.4 DNS：因特网的目录服务

记录查询名、类型、递归/迭代角色、权威来源、响应码、TTL和缓存命中；用查询与应答报文验证，不只看最终IP。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“DNS：因特网的目录服务”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“DNS：因特网的目录服务”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 2.4.1 DNS提供的服务

记录查询名、类型、递归/迭代角色、权威来源、响应码、TTL和缓存命中；用查询与应答报文验证，不只看最终IP。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“DNS提供的服务”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“DNS提供的服务”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 2.4.2 DNS工作机理概述

记录查询名、类型、递归/迭代角色、权威来源、响应码、TTL和缓存命中；用查询与应答报文验证，不只看最终IP。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“DNS工作机理概述”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“DNS工作机理概述”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 2.4.3 DNS记录和报文

记录查询名、类型、递归/迭代角色、权威来源、响应码、TTL和缓存命中；用查询与应答报文验证，不只看最终IP。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“DNS记录和报文”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“DNS记录和报文”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

### 2.5 P2P文件分发

把“P2P文件分发”放回“从进程通信与运输服务需求进入HTTP、电子邮件、DNS、P2P、视频/CDN和UDP/TCP套接字编程”的因果链，写出通信实体、输入状态、报文或算法、输出状态、性能边界和失败语义。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“P2P文件分发”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“P2P文件分发”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

### 2.6 视频流和内容分发网

把“视频流和内容分发网”放回“从进程通信与运输服务需求进入HTTP、电子邮件、DNS、P2P、视频/CDN和UDP/TCP套接字编程”的因果链，写出通信实体、输入状态、报文或算法、输出状态、性能边界和失败语义。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“视频流和内容分发网”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“视频流和内容分发网”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 2.6.1 因特网视频

把“因特网视频”放回“从进程通信与运输服务需求进入HTTP、电子邮件、DNS、P2P、视频/CDN和UDP/TCP套接字编程”的因果链，写出通信实体、输入状态、报文或算法、输出状态、性能边界和失败语义。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“因特网视频”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“因特网视频”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 2.6.2 HTTP流和DASH

沿请求方法、目标、首部、状态码、连接复用和缓存验证器画出客户与服务器状态；区分应用语义和运输连接。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“HTTP流和DASH”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“HTTP流和DASH”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 2.6.3 内容分发网

把“内容分发网”放回“从进程通信与运输服务需求进入HTTP、电子邮件、DNS、P2P、视频/CDN和UDP/TCP套接字编程”的因果链，写出通信实体、输入状态、报文或算法、输出状态、性能边界和失败语义。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“内容分发网”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“内容分发网”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 2.6.4 学习案例：Netflix和YouTube

把“学习案例：Netflix和YouTube”放回“从进程通信与运输服务需求进入HTTP、电子邮件、DNS、P2P、视频/CDN和UDP/TCP套接字编程”的因果链，写出通信实体、输入状态、报文或算法、输出状态、性能边界和失败语义。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“学习案例：Netflix和YouTube”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“学习案例：Netflix和YouTube”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

### 2.7 套接字编程：生成网络应用

把“套接字编程：生成网络应用”放回“从进程通信与运输服务需求进入HTTP、电子邮件、DNS、P2P、视频/CDN和UDP/TCP套接字编程”的因果链，写出通信实体、输入状态、报文或算法、输出状态、性能边界和失败语义。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“套接字编程：生成网络应用”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“套接字编程：生成网络应用”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 2.7.1 UDP套接字编程

明确数据报边界、校验和、无连接状态与应用自担的可靠性；用丢失、重复和乱序反例验证应用协议。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“UDP套接字编程”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“UDP套接字编程”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 2.7.2 TCP套接字编程

用序号、确认号、定时器、发送/接收窗口和状态转换解释交付；注入单个丢包或乱序，比较预期与抓包。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“TCP套接字编程”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“TCP套接字编程”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

### 2.8 小结

把“小结”放回“从进程通信与运输服务需求进入HTTP、电子邮件、DNS、P2P、视频/CDN和UDP/TCP套接字编程”的因果链，写出通信实体、输入状态、报文或算法、输出状态、性能边界和失败语义。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“小结”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“小结”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

## 本页完整节点清单

本页承担33个原书正式节点。正文、图解、实验和题目必须能反向定位每项：

- 第2章 应用层
- 2.1 网络应用原理
- 2.1.1 网络应用体系结构
- 2.1.2 进程通信
- 2.1.3 可供应用程序使用的运输服务
- 2.1.4 因特网提供的运输服务
- 2.1.5 应用层协议
- 2.1.6 本书涉及的网络应用
- 2.2 Web和HTTP
- 2.2.1 HTTP概述
- 2.2.2 非持续连接和持续连接
- 2.2.3 HTTP报文格式
- 2.2.4 用户与服务器的交互：cookie
- 2.2.5 Web缓存
- 2.2.6 HTTP/2
- 2.3 因特网中的电子邮件
- 2.3.1 SMTP
- 2.3.2 邮件报文格式
- 2.3.3 邮件访问协议
- 2.4 DNS：因特网的目录服务
- 2.4.1 DNS提供的服务
- 2.4.2 DNS工作机理概述
- 2.4.3 DNS记录和报文
- 2.5 P2P文件分发
- 2.6 视频流和内容分发网
- 2.6.1 因特网视频
- 2.6.2 HTTP流和DASH
- 2.6.3 内容分发网
- 2.6.4 学习案例：Netflix和YouTube
- 2.7 套接字编程：生成网络应用
- 2.7.1 UDP套接字编程
- 2.7.2 TCP套接字编程
- 2.8 小结

<Stepper>
  <Step title="建立逐层正常路径">
    从应用消息向下标出运输端点、IP与转发表、下一跳链路、无线或接入状态以及安全保护边界。
    <Cnt802ApplicationMapLab />
  </Step>
  <Step title="只改变一个网络变量">
    固定其他条件，改变时延、瓶颈、丢包、表项、协议状态或信任条件，先写预测再执行。
    <Cnt802ApplicationExperimentLab />
  </Step>
  <Step title="关闭独立证据门">
    对照正常与失败时间线，确认首个偏差、告警、恢复、版次边界和第三方复核结果。
    <Cnt802ApplicationEvidenceLab />
  </Step>
</Stepper>

## 可复现实验记录

```text
GET /index.html HTTP/1.1
Host: example.test
If-None-Match: "v7"
Connection: keep-alive
```

```text
name -> recursive resolver -> root -> TLD -> authoritative
record = {name, type, value, ttl}
cache_expiry = received_at + ttl
```

```text
message = length_prefix + payload
while bytes_read < expected_length:
  read_more_from_tcp_stream()
```

动手试：保存一次正常流、一次性能边界和一次单变量故障。至少记录应用消息、套接字/运输状态、路由与邻居表、链路或无线事件、安全握手、统一时间戳和恢复步骤。

<Callout type="trap" title="把局部可达当成端到端正确">
  ICMP可达不证明DNS、目标端口、TLS身份、HTTP语义或返回路径正确。必须沿协议栈逐层关闭证据缺口。
</Callout>

<Callout type="trap" title="混入其他版次的目录">
  本课程以中英文官方第8版目录为分母。第7版退役主题与第9版重排、新增内容可做版本比较，但不能替换本版正式节点。
</Callout>

## 独立证据门

<Cnt802ApplicationEvidenceLab />

最小证据包包含：第8版节点、正常报文时间线、地址与端口、协议状态、路由/邻居/链路表项、性能条件、单变量故障、安全策略、恢复、偏差、责任人与复核人。

## 练习

<Exercises>

**问题 1：为什么“第2章 应用层”必须固定第8版？**

<Answer>
  固定版次才能让8章、218个节点、协议语境和实验资源一一对应。第7版退役材料和第9版新目录只能独立比较，不能覆盖本页正式分母。
</Answer>

**问题 2：怎样构造“只记端口号而不区分应用状态、运输连接、缓存一致性和命名依赖，会在代理、CDN、DNS或并发连接处误判”的最小反例？**

<Answer>
  固定应用、路径和其余协议状态，只改变一个报文、表项、定时器、链路或信任条件；比较逐层状态、首个偏差、告警、性能和恢复。
</Answer>

**问题 3：何时可以认为本页完成独立交接？**

<Answer>
  另一位学习者无需口头补充即可定位全部33个节点，重建正常路径，复现单变量故障，并依据“每个应用协议都能说明通信进程、寻址、报文格式与次序、运输服务、状态位置、缓存或复制策略以及失败语义”得到相同结论。
</Answer>

</Exercises>

## 本章回顾

“第2章 应用层”的核心是从进程通信与运输服务需求进入HTTP、电子邮件、DNS、P2P、视频/CDN和UDP/TCP套接字编程。真正掌握不是背缩写，而是能把应用意图、报文、状态、表项、性能、故障与安全证据连成可证伪的端到端模型。

<Glossary>
  <GlossaryItem term="套接字">
    应用进程与运输层之间发送和接收报文的编程接口。掌握标准是能在报文、状态机或表项中定位，并构造一个最小失败反例。
  </GlossaryItem>
  <GlossaryItem term="HTTP">
    定义Web客户与服务器交换请求和响应报文语义的应用层协议。掌握标准是能在报文、状态机或表项中定位，并构造一个最小失败反例。
  </GlossaryItem>
  <GlossaryItem term="DNS">
    把主机名等名称映射为资源记录并由分层服务器协作提供的目录服务。掌握标准是能在报文、状态机或表项中定位，并构造一个最小失败反例。
  </GlossaryItem>
  <GlossaryItem term="CDN">
    把内容副本部署到多个边缘位置并按策略选择交付节点的系统。掌握标准是能在报文、状态机或表项中定位，并构造一个最小失败反例。
  </GlossaryItem>
  <GlossaryItem term="P2P">
    端系统同时承担客户与服务方角色并直接贡献分发能力的体系结构。掌握标准是能在报文、状态机或表项中定位，并构造一个最小失败反例。
  </GlossaryItem>
</Glossary>

[← 上一页：第1章 计算机网络和因特网](/learn/computer-networks-top-down/01-cnt8-01-internet/cnt8-01-internet) · [下一页：第3章 运输层 →](/learn/computer-networks-top-down/03-cnt8-03-transport/cnt8-03-transport)

<Attribution
  adaptedFrom="Kurose与Ross《计算机网络：自顶向下方法》原书第8版"
  adaptedUrl="https://www.cmpedu.com/books/book/5606311.htm"
/>
