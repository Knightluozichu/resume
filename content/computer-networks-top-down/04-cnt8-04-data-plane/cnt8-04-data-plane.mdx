---
title: "第4章 网络层：数据平面"
description: "分离转发与路由，剖析路由器输入、交换、输出、排队和调度，并掌握IPv4编址、NAT、IPv6、泛化转发、SDN与中间盒"
type: A
section: "计算机网络：自顶向下方法（原书第8版）"
order: 4
draft: true
math: false
sourceUrl: "https://www.cmpedu.com/books/book/5606311.htm"
demo: true
---

# 第4章 网络层：数据平面

本课程锁定James F. Kurose、Keith W. Ross著，陈鸣译《计算机网络：自顶向下方法（原书第8版）》，机械工业出版社，2022年10月，ISBN 9787111712367；英文原版Pearson第8版，2020年出版、2021版权，ISBN 9780135928615。机械工业出版社官方中文目录与Pearson、作者官方第8版目录共同确认8章结构；正式分母为8个章标题和全部编号节/小节，共218个节点。课后习题、编程作业、Wireshark实验、人物专访和参考文献作为学习支持保留说明，但不重复计入核心节点。

<Objectives>

- 能解释“第4章 网络层：数据平面”全部节点，并把术语放回端到端协议路径。
- 能绘制通信实体、报文次序、状态机、表项和逐层封装。
- 能设计单变量实验，验证“任一分组的本地处理都能由匹配字段、转发表项、队列和调度动作解释，地址转换与返回路径保持可逆状态”。
- 能写出含版本边界、性能、故障、恢复与复核人的独立证据包。

</Objectives>

## 从一条可证伪的端到端路径开始

先预测：只查看路由表而忽略输入匹配、交换能力、输出队列、ACL/NAT和中间盒状态，会把数据平面丢包错误归因于路由协议。把预测写成“应用意图、运输状态、数据平面、控制平面、链路/无线、安全”六行，再运行协议或抓包。若结果与预测不同，先定位首个偏差，不把后续连锁症状冒充根因。

本页主问题是：分离转发与路由，剖析路由器输入、交换、输出、排队和调度，并掌握IPv4编址、NAT、IPv6、泛化转发、SDN与中间盒。每个箭头要注明通信实体、报文、地址或标识、状态变化、定时器以及证据位置；每个性能结论要注明负载、路径、窗口和瓶颈假设。

验收不变量是：任一分组的本地处理都能由匹配字段、转发表项、队列和调度动作解释，地址转换与返回路径保持可逆状态。一次ping、一个HTTP状态码或一个接口up都只证明局部事实，不能独立证明名称解析、双向运输、转发、移动性与身份安全全部成立。

## 核心词汇与第8版边界

<Term def="路由器在本地依据转发表把到达分组移到适当输出端口的动作">转发</Term>、<Term def="在多个目的前缀中选择位数最长且匹配目标地址的规则">最长前缀匹配</Term>、<Term def="在边界改写地址或端口并维护映射状态的网络地址转换机制">NAT</Term>、<Term def="按多个首部字段匹配并执行转发、丢弃、改写等动作的模型">泛化转发</Term>、<Term def="执行传统IP转发之外网络功能的路径内设备或功能实体">中间盒</Term>

这些词汇固定在原书第8版语境。HTTP/2、QUIC/HTTP/3演化、CUBIC/BBR、SDN、NETCONF/YANG、4G/5G和WPA3语境属于本版；第9版重排或新增内容只能另行比较，不能修改218个节点分母。

## 核心机制深读

### 数据平面逐包快，控制平面全网慢

路由器输入端口解析首部并查表，交换结构把分组送往输出端口；控制平面负责形成规则。两者分离后才能定位是规则错误还是执行拥塞。

动手试：先画预期报文时间线与状态变化，再抓取一次正常流和一次单变量故障流。标出首个偏差，避免用最终症状倒推任意根因。

### 队列位置决定观察到的故障

输入排队可能发生队头阻塞，输出到达率超过链路服务率会积压和丢包。调度算法决定不同流的延迟、公平与隔离。

动手试：先画预期报文时间线与状态变化，再抓取一次正常流和一次单变量故障流。标出首个偏差，避免用最终症状倒推任意根因。

### IPv4编址是一组前缀约束

子网由接口间无需路由器即可互达来定义，CIDR用前缀长度聚合地址。DHCP提供配置，NAT把私网端点映射到外部标识。

动手试：先画预期报文时间线与状态变化，再抓取一次正常流和一次单变量故障流。标出首个偏差，避免用最终症状倒推任意根因。

### IPv6不是把地址加长这么简单

固定基础首部、扩展首部和不同的分片责任改变处理方式；迁移常借助双栈或隧道，需要分别验证路径MTU和策略。

动手试：先画预期报文时间线与状态变化，再抓取一次正常流和一次单变量故障流。标出首个偏差，避免用最终症状倒推任意根因。

### 匹配加动作统一多种路径功能

OpenFlow式规则可匹配入端口和多层首部并执行转发、丢弃、改写或送控制器。中间盒则提醒我们真实路径还包含防火墙、均衡和转换状态。

动手试：先画预期报文时间线与状态变化，再抓取一次正常流和一次单变量故障流。标出首个偏差，避免用最终症状倒推任意根因。

## 原书目录逐节点重构

### 第4章 网络层：数据平面

把“网络层：数据平面”放回“分离转发与路由，剖析路由器输入、交换、输出、排队和调度，并掌握IPv4编址、NAT、IPv6、泛化转发、SDN与中间盒”的因果链，写出通信实体、输入状态、报文或算法、输出状态、性能边界和失败语义。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“网络层：数据平面”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“网络层：数据平面”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

### 4.1 网络层概述

把“网络层概述”放回“分离转发与路由，剖析路由器输入、交换、输出、排队和调度，并掌握IPv4编址、NAT、IPv6、泛化转发、SDN与中间盒”的因果链，写出通信实体、输入状态、报文或算法、输出状态、性能边界和失败语义。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“网络层概述”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“网络层概述”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 4.1.1 转发和路由选择：数据平面和控制平面

从输入解析、表项匹配、交换、输出队列到调度逐步定位；保存命中规则、队列计数和丢弃原因。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“转发和路由选择：数据平面和控制平面”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“转发和路由选择：数据平面和控制平面”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 4.1.2 网络服务模型

把“网络服务模型”放回“分离转发与路由，剖析路由器输入、交换、输出、排队和调度，并掌握IPv4编址、NAT、IPv6、泛化转发、SDN与中间盒”的因果链，写出通信实体、输入状态、报文或算法、输出状态、性能边界和失败语义。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“网络服务模型”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“网络服务模型”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

### 4.2 路由器工作原理

从输入解析、表项匹配、交换、输出队列到调度逐步定位；保存命中规则、队列计数和丢弃原因。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“路由器工作原理”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“路由器工作原理”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 4.2.1 输入端口处理和基于目的地转发

从输入解析、表项匹配、交换、输出队列到调度逐步定位；保存命中规则、队列计数和丢弃原因。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“输入端口处理和基于目的地转发”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“输入端口处理和基于目的地转发”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 4.2.2 交换

把“交换”放回“分离转发与路由，剖析路由器输入、交换、输出、排队和调度，并掌握IPv4编址、NAT、IPv6、泛化转发、SDN与中间盒”的因果链，写出通信实体、输入状态、报文或算法、输出状态、性能边界和失败语义。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“交换”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“交换”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 4.2.3 输出端口处理

把“输出端口处理”放回“分离转发与路由，剖析路由器输入、交换、输出、排队和调度，并掌握IPv4编址、NAT、IPv6、泛化转发、SDN与中间盒”的因果链，写出通信实体、输入状态、报文或算法、输出状态、性能边界和失败语义。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“输出端口处理”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“输出端口处理”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 4.2.4 何处出现排队

从输入解析、表项匹配、交换、输出队列到调度逐步定位；保存命中规则、队列计数和丢弃原因。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“何处出现排队”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“何处出现排队”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 4.2.5 分组调度

从输入解析、表项匹配、交换、输出队列到调度逐步定位；保存命中规则、队列计数和丢弃原因。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“分组调度”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“分组调度”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

### 4.3 网际协议：IPv4、寻址、IPv6及其他

写出源/目的地址、前缀、下一跳、转换前后元组、方向和返回状态；正反向抓包必须能互相解释。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“网际协议：IPv4、寻址、IPv6及其他”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“网际协议：IPv4、寻址、IPv6及其他”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 4.3.1 IPv4数据报格式

写出源/目的地址、前缀、下一跳、转换前后元组、方向和返回状态；正反向抓包必须能互相解释。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“IPv4数据报格式”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“IPv4数据报格式”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 4.3.2 IPv4编址

写出源/目的地址、前缀、下一跳、转换前后元组、方向和返回状态；正反向抓包必须能互相解释。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“IPv4编址”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“IPv4编址”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 4.3.3 网络地址转换

把“网络地址转换”放回“分离转发与路由，剖析路由器输入、交换、输出、排队和调度，并掌握IPv4编址、NAT、IPv6、泛化转发、SDN与中间盒”的因果链，写出通信实体、输入状态、报文或算法、输出状态、性能边界和失败语义。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“网络地址转换”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“网络地址转换”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 4.3.4 IPv6

写出源/目的地址、前缀、下一跳、转换前后元组、方向和返回状态；正反向抓包必须能互相解释。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“IPv6”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“IPv6”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

### 4.4 泛化转发和SDN

从输入解析、表项匹配、交换、输出队列到调度逐步定位；保存命中规则、队列计数和丢弃原因。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“泛化转发和SDN”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“泛化转发和SDN”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 4.4.1 匹配

把“匹配”放回“分离转发与路由，剖析路由器输入、交换、输出、排队和调度，并掌握IPv4编址、NAT、IPv6、泛化转发、SDN与中间盒”的因果链，写出通信实体、输入状态、报文或算法、输出状态、性能边界和失败语义。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“匹配”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“匹配”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 4.4.2 操作

把“操作”放回“分离转发与路由，剖析路由器输入、交换、输出、排队和调度，并掌握IPv4编址、NAT、IPv6、泛化转发、SDN与中间盒”的因果链，写出通信实体、输入状态、报文或算法、输出状态、性能边界和失败语义。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“操作”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“操作”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 4.4.3 运行中的匹配加操作的OpenFlow例子

关联意图、控制器或管理器状态、事务/协议消息、设备配置与运行状态，并验证失败时回退和告警。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“运行中的匹配加操作的OpenFlow例子”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“运行中的匹配加操作的OpenFlow例子”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

### 4.5 中间盒

从输入解析、表项匹配、交换、输出队列到调度逐步定位；保存命中规则、队列计数和丢弃原因。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“中间盒”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“中间盒”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

### 4.6 小结

把“小结”放回“分离转发与路由，剖析路由器输入、交换、输出、排队和调度，并掌握IPv4编址、NAT、IPv6、泛化转发、SDN与中间盒”的因果链，写出通信实体、输入状态、报文或算法、输出状态、性能边界和失败语义。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“小结”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“小结”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

## 本页完整节点清单

本页承担21个原书正式节点。正文、图解、实验和题目必须能反向定位每项：

- 第4章 网络层：数据平面
- 4.1 网络层概述
- 4.1.1 转发和路由选择：数据平面和控制平面
- 4.1.2 网络服务模型
- 4.2 路由器工作原理
- 4.2.1 输入端口处理和基于目的地转发
- 4.2.2 交换
- 4.2.3 输出端口处理
- 4.2.4 何处出现排队
- 4.2.5 分组调度
- 4.3 网际协议：IPv4、寻址、IPv6及其他
- 4.3.1 IPv4数据报格式
- 4.3.2 IPv4编址
- 4.3.3 网络地址转换
- 4.3.4 IPv6
- 4.4 泛化转发和SDN
- 4.4.1 匹配
- 4.4.2 操作
- 4.4.3 运行中的匹配加操作的OpenFlow例子
- 4.5 中间盒
- 4.6 小结

<Stepper>
  <Step title="建立逐层正常路径">
    从应用消息向下标出运输端点、IP与转发表、下一跳链路、无线或接入状态以及安全保护边界。
    </Step>
  <Step title="只改变一个网络变量">
    固定其他条件，改变时延、瓶颈、丢包、表项、协议状态或信任条件，先写预测再执行。
    </Step>
  <Step title="关闭独立证据门">
    对照正常与失败时间线，确认首个偏差、告警、恢复、版次边界和第三方复核结果。
    </Step>
</Stepper>

## 可复现实验记录

```text
destination = 10.23.7.41
candidates = [10.0.0.0/8, 10.23.0.0/16, 10.23.7.0/24]
selected = longest_matching_prefix(candidates)
```

```text
match: in_port=3, ipv4_dst=10.0.4.0/24, tcp_dst=443
action: decrement_ttl, set_queue=gold, output=7
```

```text
inside = 10.0.0.8:53000
public = 203.0.113.7:62001
return traffic must match the same NAT state
```

动手试：保存一次正常流、一次性能边界和一次单变量故障。至少记录应用消息、套接字/运输状态、路由与邻居表、链路或无线事件、安全握手、统一时间戳和恢复步骤。

<Callout type="trap" title="把局部可达当成端到端正确">
  ICMP可达不证明DNS、目标端口、TLS身份、HTTP语义或返回路径正确。必须沿协议栈逐层关闭证据缺口。
</Callout>

<Callout type="trap" title="混入其他版次的目录">
  本课程以中英文官方第8版目录为分母。第7版退役主题与第9版重排、新增内容可做版本比较，但不能替换本版正式节点。
</Callout>

## 独立证据门

最小证据包包含：第8版节点、正常报文时间线、地址与端口、协议状态、路由/邻居/链路表项、性能条件、单变量故障、安全策略、恢复、偏差、责任人与复核人。

## 练习

<Exercises>

**问题 1：为什么“第4章 网络层：数据平面”必须固定第8版？**

<Answer>
  固定版次才能让8章、218个节点、协议语境和实验资源一一对应。第7版退役材料和第9版新目录只能独立比较，不能覆盖本页正式分母。
</Answer>

**问题 2：怎样构造“只查看路由表而忽略输入匹配、交换能力、输出队列、ACL/NAT和中间盒状态，会把数据平面丢包错误归因于路由协议”的最小反例？**

<Answer>
  固定应用、路径和其余协议状态，只改变一个报文、表项、定时器、链路或信任条件；比较逐层状态、首个偏差、告警、性能和恢复。
</Answer>

**问题 3：何时可以认为本页完成独立交接？**

<Answer>
  另一位学习者无需口头补充即可定位全部21个节点，重建正常路径，复现单变量故障，并依据“任一分组的本地处理都能由匹配字段、转发表项、队列和调度动作解释，地址转换与返回路径保持可逆状态”得到相同结论。
</Answer>

</Exercises>

## 本章回顾

“第4章 网络层：数据平面”的核心是分离转发与路由，剖析路由器输入、交换、输出、排队和调度，并掌握IPv4编址、NAT、IPv6、泛化转发、SDN与中间盒。真正掌握不是背缩写，而是能把应用意图、报文、状态、表项、性能、故障与安全证据连成可证伪的端到端模型。

<Glossary>
  <GlossaryItem term="转发">
    路由器在本地依据转发表把到达分组移到适当输出端口的动作。掌握标准是能在报文、状态机或表项中定位，并构造一个最小失败反例。
  </GlossaryItem>
  <GlossaryItem term="最长前缀匹配">
    在多个目的前缀中选择位数最长且匹配目标地址的规则。掌握标准是能在报文、状态机或表项中定位，并构造一个最小失败反例。
  </GlossaryItem>
  <GlossaryItem term="NAT">
    在边界改写地址或端口并维护映射状态的网络地址转换机制。掌握标准是能在报文、状态机或表项中定位，并构造一个最小失败反例。
  </GlossaryItem>
  <GlossaryItem term="泛化转发">
    按多个首部字段匹配并执行转发、丢弃、改写等动作的模型。掌握标准是能在报文、状态机或表项中定位，并构造一个最小失败反例。
  </GlossaryItem>
  <GlossaryItem term="中间盒">
    执行传统IP转发之外网络功能的路径内设备或功能实体。掌握标准是能在报文、状态机或表项中定位，并构造一个最小失败反例。
  </GlossaryItem>
</Glossary>

[← 上一页：第3章 运输层](/learn/computer-networks-top-down/03-cnt8-03-transport/cnt8-03-transport) · [下一页：第5章 网络层：控制平面 →](/learn/computer-networks-top-down/05-cnt8-05-control-plane/cnt8-05-control-plane)

<Attribution
  adaptedFrom="Kurose与Ross《计算机网络：自顶向下方法》原书第8版"
  adaptedUrl="https://www.cmpedu.com/books/book/5606311.htm"
/>
