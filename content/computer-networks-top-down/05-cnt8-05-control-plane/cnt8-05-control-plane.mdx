---
title: "第5章 网络层：控制平面"
description: "从链路状态与距离向量算法进入OSPF、BGP、SDN控制器、ICMP以及SNMP和NETCONF/YANG网络管理"
type: A
section: "计算机网络：自顶向下方法（原书第8版）"
order: 5
draft: false
math: false
sourceUrl: "https://www.cmpedu.com/books/book/5606311.htm"
demo: true
---

import {
  Cnt805ControlPlaneMapLab,
  Cnt805ControlPlaneExperimentLab,
  Cnt805ControlPlaneEvidenceLab,
} from "@/components/mdx/computer-networks-top-down/diagrams/cnt8-05-control-plane";

# 第5章 网络层：控制平面

本课程锁定James F. Kurose、Keith W. Ross著，陈鸣译《计算机网络：自顶向下方法（原书第8版）》，机械工业出版社，2022年10月，ISBN 9787111712367；英文原版Pearson第8版，2020年出版、2021版权，ISBN 9780135928615。机械工业出版社官方中文目录与Pearson、作者官方第8版目录共同确认8章结构；正式分母为8个章标题和全部编号节/小节，共218个节点。课后习题、编程作业、Wireshark实验、人物专访和参考文献作为学习支持保留说明，但不重复计入核心节点。

<Objectives>

- 能解释“第5章 网络层：控制平面”全部节点，并把术语放回端到端协议路径。
- 能绘制通信实体、报文次序、状态机、表项和逐层封装。
- 能设计单变量实验，验证“每条转发规则都有可追溯的拓扑或策略输入、算法选择、控制消息、收敛状态、管理配置和故障证据”。
- 能写出含版本边界、性能、故障、恢复与复核人的独立证据包。

</Objectives>

## 从一条可证伪的端到端路径开始

先预测：把最短路径等同于域间最佳路由，或只看控制邻居不验证数据平面，会忽略BGP策略、收敛瞬态和配置漂移。把预测写成“应用意图、运输状态、数据平面、控制平面、链路/无线、安全”六行，再运行协议或抓包。若结果与预测不同，先定位首个偏差，不把后续连锁症状冒充根因。

本页主问题是：从链路状态与距离向量算法进入OSPF、BGP、SDN控制器、ICMP以及SNMP和NETCONF/YANG网络管理。每个箭头要注明通信实体、报文、地址或标识、状态变化、定时器以及证据位置；每个性能结论要注明负载、路径、窗口和瓶颈假设。

验收不变量是：每条转发规则都有可追溯的拓扑或策略输入、算法选择、控制消息、收敛状态、管理配置和故障证据。一次ping、一个HTTP状态码或一个接口up都只证明局部事实，不能独立证明名称解析、双向运输、转发、移动性与身份安全全部成立。

## 核心词汇与第8版边界

<Term def="让节点获得拓扑与链路代价后独立计算最短路径的路由方法">链路状态</Term>、<Term def="节点与邻居迭代交换目的距离并按Bellman-Ford关系更新的路由方法">距离向量</Term>、<Term def="在自治系统内部传播链路状态并计算路由的域内协议">OSPF</Term>、<Term def="在自治系统之间通告前缀可达性和路径属性并执行策略的协议">BGP</Term>、<Term def="描述网络配置与状态数据结构及约束的数据建模语言">YANG</Term>

这些词汇固定在原书第8版语境。HTTP/2、QUIC/HTTP/3演化、CUBIC/BBR、SDN、NETCONF/YANG、4G/5G和WPA3语境属于本版；第9版重排或新增内容只能另行比较，不能修改218个节点分母。

<Cnt805ControlPlaneMapLab />

## 核心机制深读

### 算法输入决定路由含义

Dijkstra在已知全图上计算最低代价树，距离向量依赖邻居迭代。两者都要说明代价、更新触发、收敛与环路风险，而不是只背步骤。

动手试：先画预期报文时间线与状态变化，再抓取一次正常流和一次单变量故障流。标出首个偏差，避免用最终症状倒推任意根因。

### OSPF与BGP解决不同范围的问题

OSPF在一个自治系统内按链路状态和层次区域收敛；BGP跨自治系统传播AS路径和属性，策略通常比最短距离更重要。

动手试：先画预期报文时间线与状态变化，再抓取一次正常流和一次单变量故障流。标出首个偏差，避免用最终症状倒推任意根因。

### 任播与策略选择都依赖路由

多个站点通告同一前缀可实现IP任播，但用户到达哪个站点取决于域间策略与路径状态；切换结果必须从控制通告和实际数据流双向验证。

动手试：先画预期报文时间线与状态变化，再抓取一次正常流和一次单变量故障流。标出首个偏差，避免用最终症状倒推任意根因。

### SDN把控制逻辑显式分层

控制器维护网络状态并向控制应用提供抽象，南向协议与设备交互。逻辑集中不意味着物理单点，可靠性与一致性仍需设计。

动手试：先画预期报文时间线与状态变化，再抓取一次正常流和一次单变量故障流。标出首个偏差，避免用最终症状倒推任意根因。

### 管理面是可运营性的闭环

ICMP报告网络层事实，SNMP读取与通知管理对象，NETCONF执行配置事务，YANG约束数据模型。配置、状态、告警与回退要使用同一设备身份和时间线。

动手试：先画预期报文时间线与状态变化，再抓取一次正常流和一次单变量故障流。标出首个偏差，避免用最终症状倒推任意根因。

## 原书目录逐节点重构

### 第5章 网络层：控制平面

把“网络层：控制平面”放回“从链路状态与距离向量算法进入OSPF、BGP、SDN控制器、ICMP以及SNMP和NETCONF/YANG网络管理”的因果链，写出通信实体、输入状态、报文或算法、输出状态、性能边界和失败语义。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“网络层：控制平面”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“网络层：控制平面”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

### 5.1 概述

把“概述”放回“从链路状态与距离向量算法进入OSPF、BGP、SDN控制器、ICMP以及SNMP和NETCONF/YANG网络管理”的因果链，写出通信实体、输入状态、报文或算法、输出状态、性能边界和失败语义。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“概述”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“概述”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

### 5.2 路由选择算法

分开记录拓扑/策略输入、控制通告、选路结果、安装表项与数据流；注入单链路或单通告变化观察收敛。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“路由选择算法”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“路由选择算法”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 5.2.1 链路状态路由选择算法

分开记录拓扑/策略输入、控制通告、选路结果、安装表项与数据流；注入单链路或单通告变化观察收敛。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“链路状态路由选择算法”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“链路状态路由选择算法”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 5.2.2 距离向量路由选择算法

分开记录拓扑/策略输入、控制通告、选路结果、安装表项与数据流；注入单链路或单通告变化观察收敛。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“距离向量路由选择算法”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“距离向量路由选择算法”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

### 5.3 因特网中自治系统内部的路由选择：OSPF

分开记录拓扑/策略输入、控制通告、选路结果、安装表项与数据流；注入单链路或单通告变化观察收敛。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“因特网中自治系统内部的路由选择：OSPF”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“因特网中自治系统内部的路由选择：OSPF”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

### 5.4 ISP之间的路由选择：BGP

分开记录拓扑/策略输入、控制通告、选路结果、安装表项与数据流；注入单链路或单通告变化观察收敛。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“ISP之间的路由选择：BGP”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“ISP之间的路由选择：BGP”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 5.4.1 BGP的作用

分开记录拓扑/策略输入、控制通告、选路结果、安装表项与数据流；注入单链路或单通告变化观察收敛。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“BGP的作用”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“BGP的作用”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 5.4.2 通告BGP路由信息

分开记录拓扑/策略输入、控制通告、选路结果、安装表项与数据流；注入单链路或单通告变化观察收敛。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“通告BGP路由信息”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“通告BGP路由信息”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 5.4.3 确定好的路由

把“确定好的路由”放回“从链路状态与距离向量算法进入OSPF、BGP、SDN控制器、ICMP以及SNMP和NETCONF/YANG网络管理”的因果链，写出通信实体、输入状态、报文或算法、输出状态、性能边界和失败语义。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“确定好的路由”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“确定好的路由”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 5.4.4 IP任播

把“IP任播”放回“从链路状态与距离向量算法进入OSPF、BGP、SDN控制器、ICMP以及SNMP和NETCONF/YANG网络管理”的因果链，写出通信实体、输入状态、报文或算法、输出状态、性能边界和失败语义。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“IP任播”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“IP任播”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 5.4.5 路由选择策略

分开记录拓扑/策略输入、控制通告、选路结果、安装表项与数据流；注入单链路或单通告变化观察收敛。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“路由选择策略”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“路由选择策略”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 5.4.6 拼装在一起：在因特网中呈现

把“拼装在一起：在因特网中呈现”放回“从链路状态与距离向量算法进入OSPF、BGP、SDN控制器、ICMP以及SNMP和NETCONF/YANG网络管理”的因果链，写出通信实体、输入状态、报文或算法、输出状态、性能边界和失败语义。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“拼装在一起：在因特网中呈现”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“拼装在一起：在因特网中呈现”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

### 5.5 SDN控制平面

关联意图、控制器或管理器状态、事务/协议消息、设备配置与运行状态，并验证失败时回退和告警。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“SDN控制平面”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“SDN控制平面”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 5.5.1 SDN控制平面：SDN控制器和SDN网络控制应用程序

关联意图、控制器或管理器状态、事务/协议消息、设备配置与运行状态，并验证失败时回退和告警。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“SDN控制平面：SDN控制器和SDN网络控制应用程序”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“SDN控制平面：SDN控制器和SDN网络控制应用程序”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 5.5.2 OpenFlow协议

关联意图、控制器或管理器状态、事务/协议消息、设备配置与运行状态，并验证失败时回退和告警。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“OpenFlow协议”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“OpenFlow协议”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 5.5.3 数据平面和控制平面交互的例子

把“数据平面和控制平面交互的例子”放回“从链路状态与距离向量算法进入OSPF、BGP、SDN控制器、ICMP以及SNMP和NETCONF/YANG网络管理”的因果链，写出通信实体、输入状态、报文或算法、输出状态、性能边界和失败语义。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“数据平面和控制平面交互的例子”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“数据平面和控制平面交互的例子”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 5.5.4 SDN的过去与未来

关联意图、控制器或管理器状态、事务/协议消息、设备配置与运行状态，并验证失败时回退和告警。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“SDN的过去与未来”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“SDN的过去与未来”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

### 5.6 ICMP：因特网控制报文协议

把“ICMP：因特网控制报文协议”放回“从链路状态与距离向量算法进入OSPF、BGP、SDN控制器、ICMP以及SNMP和NETCONF/YANG网络管理”的因果链，写出通信实体、输入状态、报文或算法、输出状态、性能边界和失败语义。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“ICMP：因特网控制报文协议”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“ICMP：因特网控制报文协议”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

### 5.7 网络管理、SNMP和NETCONF/YANG

关联意图、控制器或管理器状态、事务/协议消息、设备配置与运行状态，并验证失败时回退和告警。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“网络管理、SNMP和NETCONF/YANG”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“网络管理、SNMP和NETCONF/YANG”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 5.7.1 网络管理框架

关联意图、控制器或管理器状态、事务/协议消息、设备配置与运行状态，并验证失败时回退和告警。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“网络管理框架”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“网络管理框架”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 5.7.2 简单网络管理协议和管理信息库

关联意图、控制器或管理器状态、事务/协议消息、设备配置与运行状态，并验证失败时回退和告警。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“简单网络管理协议和管理信息库”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“简单网络管理协议和管理信息库”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 5.7.3 NETCONF和YANG

关联意图、控制器或管理器状态、事务/协议消息、设备配置与运行状态，并验证失败时回退和告警。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“NETCONF和YANG”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“NETCONF和YANG”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

### 5.8 小结

把“小结”放回“从链路状态与距离向量算法进入OSPF、BGP、SDN控制器、ICMP以及SNMP和NETCONF/YANG网络管理”的因果链，写出通信实体、输入状态、报文或算法、输出状态、性能边界和失败语义。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“小结”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“小结”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

## 本页完整节点清单

本页承担24个原书正式节点。正文、图解、实验和题目必须能反向定位每项：

- 第5章 网络层：控制平面
- 5.1 概述
- 5.2 路由选择算法
- 5.2.1 链路状态路由选择算法
- 5.2.2 距离向量路由选择算法
- 5.3 因特网中自治系统内部的路由选择：OSPF
- 5.4 ISP之间的路由选择：BGP
- 5.4.1 BGP的作用
- 5.4.2 通告BGP路由信息
- 5.4.3 确定好的路由
- 5.4.4 IP任播
- 5.4.5 路由选择策略
- 5.4.6 拼装在一起：在因特网中呈现
- 5.5 SDN控制平面
- 5.5.1 SDN控制平面：SDN控制器和SDN网络控制应用程序
- 5.5.2 OpenFlow协议
- 5.5.3 数据平面和控制平面交互的例子
- 5.5.4 SDN的过去与未来
- 5.6 ICMP：因特网控制报文协议
- 5.7 网络管理、SNMP和NETCONF/YANG
- 5.7.1 网络管理框架
- 5.7.2 简单网络管理协议和管理信息库
- 5.7.3 NETCONF和YANG
- 5.8 小结

<Stepper>
  <Step title="建立逐层正常路径">
    从应用消息向下标出运输端点、IP与转发表、下一跳链路、无线或接入状态以及安全保护边界。
    <Cnt805ControlPlaneMapLab />
  </Step>
  <Step title="只改变一个网络变量">
    固定其他条件，改变时延、瓶颈、丢包、表项、协议状态或信任条件，先写预测再执行。
    <Cnt805ControlPlaneExperimentLab />
  </Step>
  <Step title="关闭独立证据门">
    对照正常与失败时间线，确认首个偏差、告警、恢复、版次边界和第三方复核结果。
    <Cnt805ControlPlaneEvidenceLab />
  </Step>
</Stepper>

## 可复现实验记录

```text
D_x(y) = min_v { c(x,v) + D_v(y) }
update only after validating neighbor and metric
```

```text
prefix = 203.0.113.0/24
as_path = [64510, 64496]
policy = prefer_customer_then_shorter_path
```

```text
<edit-config target='candidate'>...</edit-config>
validate()
commit_confirmed(timeout=120)
verify_state_or_rollback()
```

动手试：保存一次正常流、一次性能边界和一次单变量故障。至少记录应用消息、套接字/运输状态、路由与邻居表、链路或无线事件、安全握手、统一时间戳和恢复步骤。

<Callout type="trap" title="把局部可达当成端到端正确">
  ICMP可达不证明DNS、目标端口、TLS身份、HTTP语义或返回路径正确。必须沿协议栈逐层关闭证据缺口。
</Callout>

<Callout type="trap" title="混入其他版次的目录">
  本课程以中英文官方第8版目录为分母。第7版退役主题与第9版重排、新增内容可做版本比较，但不能替换本版正式节点。
</Callout>

## 独立证据门

<Cnt805ControlPlaneEvidenceLab />

最小证据包包含：第8版节点、正常报文时间线、地址与端口、协议状态、路由/邻居/链路表项、性能条件、单变量故障、安全策略、恢复、偏差、责任人与复核人。

## 练习

<Exercises>

**问题 1：为什么“第5章 网络层：控制平面”必须固定第8版？**

<Answer>
  固定版次才能让8章、218个节点、协议语境和实验资源一一对应。第7版退役材料和第9版新目录只能独立比较，不能覆盖本页正式分母。
</Answer>

**问题 2：怎样构造“把最短路径等同于域间最佳路由，或只看控制邻居不验证数据平面，会忽略BGP策略、收敛瞬态和配置漂移”的最小反例？**

<Answer>
  固定应用、路径和其余协议状态，只改变一个报文、表项、定时器、链路或信任条件；比较逐层状态、首个偏差、告警、性能和恢复。
</Answer>

**问题 3：何时可以认为本页完成独立交接？**

<Answer>
  另一位学习者无需口头补充即可定位全部24个节点，重建正常路径，复现单变量故障，并依据“每条转发规则都有可追溯的拓扑或策略输入、算法选择、控制消息、收敛状态、管理配置和故障证据”得到相同结论。
</Answer>

</Exercises>

## 本章回顾

“第5章 网络层：控制平面”的核心是从链路状态与距离向量算法进入OSPF、BGP、SDN控制器、ICMP以及SNMP和NETCONF/YANG网络管理。真正掌握不是背缩写，而是能把应用意图、报文、状态、表项、性能、故障与安全证据连成可证伪的端到端模型。

<Glossary>
  <GlossaryItem term="链路状态">
    让节点获得拓扑与链路代价后独立计算最短路径的路由方法。掌握标准是能在报文、状态机或表项中定位，并构造一个最小失败反例。
  </GlossaryItem>
  <GlossaryItem term="距离向量">
    节点与邻居迭代交换目的距离并按Bellman-Ford关系更新的路由方法。掌握标准是能在报文、状态机或表项中定位，并构造一个最小失败反例。
  </GlossaryItem>
  <GlossaryItem term="OSPF">
    在自治系统内部传播链路状态并计算路由的域内协议。掌握标准是能在报文、状态机或表项中定位，并构造一个最小失败反例。
  </GlossaryItem>
  <GlossaryItem term="BGP">
    在自治系统之间通告前缀可达性和路径属性并执行策略的协议。掌握标准是能在报文、状态机或表项中定位，并构造一个最小失败反例。
  </GlossaryItem>
  <GlossaryItem term="YANG">
    描述网络配置与状态数据结构及约束的数据建模语言。掌握标准是能在报文、状态机或表项中定位，并构造一个最小失败反例。
  </GlossaryItem>
</Glossary>

[← 上一页：第4章 网络层：数据平面](/learn/computer-networks-top-down/04-cnt8-04-data-plane/cnt8-04-data-plane) · [下一页：第6章 链路层和局域网 →](/learn/computer-networks-top-down/06-cnt8-06-link-lans/cnt8-06-link-lans)

<Attribution
  adaptedFrom="Kurose与Ross《计算机网络：自顶向下方法》原书第8版"
  adaptedUrl="https://www.cmpedu.com/books/book/5606311.htm"
/>
