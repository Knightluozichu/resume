---
title: "第6章 链路层和局域网"
description: "掌握链路层服务、差错检测、共享信道协议、ARP与以太网交换、VLAN、链路虚拟化、数据中心网络和完整Web请求回顾"
type: A
section: "计算机网络：自顶向下方法（原书第8版）"
order: 6
draft: false
math: false
sourceUrl: "https://www.cmpedu.com/books/book/5606311.htm"
demo: true
---

import { Objectives } from "@/components/mdx/objectives";
import { Term } from "@/components/mdx/term";
import { Step, Stepper } from "@/components/mdx/stepper";
import { Callout } from "@/components/mdx/callout";
import { Answer, Exercises } from "@/components/mdx/exercises";
import { Glossary, GlossaryItem } from "@/components/mdx/glossary";
import { Attribution } from "@/components/mdx/attribution";

# 第6章 链路层和局域网

本课程锁定James F. Kurose、Keith W. Ross著，陈鸣译《计算机网络：自顶向下方法（原书第8版）》，机械工业出版社，2022年10月，ISBN 9787111712367；英文原版Pearson第8版，2020年出版、2021版权，ISBN 9780135928615。机械工业出版社官方中文目录与Pearson、作者官方第8版目录共同确认8章结构；正式分母为8个章标题和全部编号节/小节，共218个节点。课后习题、编程作业、Wireshark实验、人物专访和参考文献作为学习支持保留说明，但不重复计入核心节点。

<Objectives>

- 能解释“第6章 链路层和局域网”全部节点，并把术语放回端到端协议路径。
- 能绘制通信实体、报文次序、状态机、表项和逐层封装。
- 能设计单变量实验，验证“同一链路上的帧交付能由成帧、差错检测、介质访问、MAC学习、VLAN边界和下一跳解析共同解释”。
- 能写出含版本边界、性能、故障、恢复与复核人的独立证据包。

</Objectives>

## 从一条可证伪的端到端路径开始

先预测：把IP子网、VLAN、广播域和交换表混成同一概念，会在ARP、Trunk、环路和未知单播处产生错误设计。把预测写成“应用意图、运输状态、数据平面、控制平面、链路/无线、安全”六行，再运行协议或抓包。若结果与预测不同，先定位首个偏差，不把后续连锁症状冒充根因。

本页主问题是：掌握链路层服务、差错检测、共享信道协议、ARP与以太网交换、VLAN、链路虚拟化、数据中心网络和完整Web请求回顾。每个箭头要注明通信实体、报文、地址或标识、状态变化、定时器以及证据位置；每个性能结论要注明负载、路径、窗口和瓶颈假设。

验收不变量是：同一链路上的帧交付能由成帧、差错检测、介质访问、MAC学习、VLAN边界和下一跳解析共同解释。一次ping、一个HTTP状态码或一个接口up都只证明局部事实，不能独立证明名称解析、双向运输、转发、移动性与身份安全全部成立。

## 核心词汇与第8版边界

<Term def="把比特串视为多项式并用生成多项式余数检测突发错误的方法">CRC</Term>、<Term def="协调多个节点共享同一广播信道发送机会的规则">多路访问协议</Term>、<Term def="链路层接口用于局部帧交付的标识">MAC地址</Term>、<Term def="在IPv4链路上把下一跳IP地址解析为MAC地址的协议">ARP</Term>、<Term def="在交换基础设施上逻辑划分广播域与端口成员的机制">VLAN</Term>

这些词汇固定在原书第8版语境。HTTP/2、QUIC/HTTP/3演化、CUBIC/BBR、SDN、NETCONF/YANG、4G/5G和WPA3语境属于本版；第9版重排或新增内容只能另行比较，不能修改218个节点分母。

## 核心机制深读

### 差错检测提供概率保证

奇偶校验、检验和和CRC覆盖能力不同。检测到错误通常丢弃帧，是否重传由链路或更高层决定；不能把检测等同于纠正。

动手试：先画预期报文时间线与状态变化，再抓取一次正常流和一次单变量故障流。标出首个偏差，避免用最终症状倒推任意根因。

### 共享信道协议在效率与公平间权衡

信道划分避免碰撞但空闲时浪费，随机接入适合突发负载但会冲突，轮流协议协调发送却引入控制开销和故障点。

动手试：先画预期报文时间线与状态变化，再抓取一次正常流和一次单变量故障流。标出首个偏差，避免用最终症状倒推任意根因。

### 交换机从源地址学习

交换机按入帧源MAC更新表项，再按目的MAC选择转发、过滤或泛洪。ARP解析的是当前链路下一跳，不是远端服务器最终MAC。

动手试：先画预期报文时间线与状态变化，再抓取一次正常流和一次单变量故障流。标出首个偏差，避免用最终症状倒推任意根因。

### VLAN把广播边界与物理位置解耦

接入口属于一个VLAN，Trunk用标签承载多个VLAN。端口成员、允许列表、原生VLAN与三层网关必须成套验证。

动手试：先画预期报文时间线与状态变化，再抓取一次正常流和一次单变量故障流。标出首个偏差，避免用最终症状倒推任意根因。

### Web请求把全书前六章串起来

主机先用DHCP获得配置，再经DNS定位服务、ARP解析下一跳、域内和域间路由前进，最后以TCP和HTTP交换应用数据。每一步都产生可抓取证据。

动手试：先画预期报文时间线与状态变化，再抓取一次正常流和一次单变量故障流。标出首个偏差，避免用最终症状倒推任意根因。

## 原书目录逐节点重构

### 第6章 链路层和局域网

把“链路层和局域网”放回“掌握链路层服务、差错检测、共享信道协议、ARP与以太网交换、VLAN、链路虚拟化、数据中心网络和完整Web请求回顾”的因果链，写出通信实体、输入状态、报文或算法、输出状态、性能边界和失败语义。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“链路层和局域网”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“链路层和局域网”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

### 6.1 链路层概述

把“链路层概述”放回“掌握链路层服务、差错检测、共享信道协议、ARP与以太网交换、VLAN、链路虚拟化、数据中心网络和完整Web请求回顾”的因果链，写出通信实体、输入状态、报文或算法、输出状态、性能边界和失败语义。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“链路层概述”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“链路层概述”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 6.1.1 链路层提供的服务

把“链路层提供的服务”放回“掌握链路层服务、差错检测、共享信道协议、ARP与以太网交换、VLAN、链路虚拟化、数据中心网络和完整Web请求回顾”的因果链，写出通信实体、输入状态、报文或算法、输出状态、性能边界和失败语义。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“链路层提供的服务”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“链路层提供的服务”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 6.1.2 链路层在何处实现

把“链路层在何处实现”放回“掌握链路层服务、差错检测、共享信道协议、ARP与以太网交换、VLAN、链路虚拟化、数据中心网络和完整Web请求回顾”的因果链，写出通信实体、输入状态、报文或算法、输出状态、性能边界和失败语义。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“链路层在何处实现”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“链路层在何处实现”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

### 6.2 差错检测和纠正技术

固定VLAN与链路，核对帧地址、邻居解析、交换学习、介质访问和差错结果；防止把局部链路身份误当端到端身份。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“差错检测和纠正技术”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“差错检测和纠正技术”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 6.2.1 奇偶校验

把“奇偶校验”放回“掌握链路层服务、差错检测、共享信道协议、ARP与以太网交换、VLAN、链路虚拟化、数据中心网络和完整Web请求回顾”的因果链，写出通信实体、输入状态、报文或算法、输出状态、性能边界和失败语义。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“奇偶校验”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“奇偶校验”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 6.2.2 检验和方法

把“检验和方法”放回“掌握链路层服务、差错检测、共享信道协议、ARP与以太网交换、VLAN、链路虚拟化、数据中心网络和完整Web请求回顾”的因果链，写出通信实体、输入状态、报文或算法、输出状态、性能边界和失败语义。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“检验和方法”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“检验和方法”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 6.2.3 循环冗余检测

把“循环冗余检测”放回“掌握链路层服务、差错检测、共享信道协议、ARP与以太网交换、VLAN、链路虚拟化、数据中心网络和完整Web请求回顾”的因果链，写出通信实体、输入状态、报文或算法、输出状态、性能边界和失败语义。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“循环冗余检测”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“循环冗余检测”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

### 6.3 多路访问链路和协议

固定VLAN与链路，核对帧地址、邻居解析、交换学习、介质访问和差错结果；防止把局部链路身份误当端到端身份。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“多路访问链路和协议”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“多路访问链路和协议”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 6.3.1 信道划分协议

把“信道划分协议”放回“掌握链路层服务、差错检测、共享信道协议、ARP与以太网交换、VLAN、链路虚拟化、数据中心网络和完整Web请求回顾”的因果链，写出通信实体、输入状态、报文或算法、输出状态、性能边界和失败语义。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“信道划分协议”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“信道划分协议”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 6.3.2 随机接入协议

把“随机接入协议”放回“掌握链路层服务、差错检测、共享信道协议、ARP与以太网交换、VLAN、链路虚拟化、数据中心网络和完整Web请求回顾”的因果链，写出通信实体、输入状态、报文或算法、输出状态、性能边界和失败语义。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“随机接入协议”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“随机接入协议”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 6.3.3 轮流协议

把“轮流协议”放回“掌握链路层服务、差错检测、共享信道协议、ARP与以太网交换、VLAN、链路虚拟化、数据中心网络和完整Web请求回顾”的因果链，写出通信实体、输入状态、报文或算法、输出状态、性能边界和失败语义。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“轮流协议”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“轮流协议”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 6.3.4 DOCSIS：用于电缆因特网接入的链路层协议

把“DOCSIS：用于电缆因特网接入的链路层协议”放回“掌握链路层服务、差错检测、共享信道协议、ARP与以太网交换、VLAN、链路虚拟化、数据中心网络和完整Web请求回顾”的因果链，写出通信实体、输入状态、报文或算法、输出状态、性能边界和失败语义。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“DOCSIS：用于电缆因特网接入的链路层协议”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“DOCSIS：用于电缆因特网接入的链路层协议”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

### 6.4 交换局域网

固定VLAN与链路，核对帧地址、邻居解析、交换学习、介质访问和差错结果；防止把局部链路身份误当端到端身份。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“交换局域网”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“交换局域网”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 6.4.1 链路层寻址和ARP

固定VLAN与链路，核对帧地址、邻居解析、交换学习、介质访问和差错结果；防止把局部链路身份误当端到端身份。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“链路层寻址和ARP”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“链路层寻址和ARP”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 6.4.2 以太网

固定VLAN与链路，核对帧地址、邻居解析、交换学习、介质访问和差错结果；防止把局部链路身份误当端到端身份。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“以太网”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“以太网”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 6.4.3 链路层交换机

把“链路层交换机”放回“掌握链路层服务、差错检测、共享信道协议、ARP与以太网交换、VLAN、链路虚拟化、数据中心网络和完整Web请求回顾”的因果链，写出通信实体、输入状态、报文或算法、输出状态、性能边界和失败语义。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“链路层交换机”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“链路层交换机”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 6.4.4 虚拟局域网

把“虚拟局域网”放回“掌握链路层服务、差错检测、共享信道协议、ARP与以太网交换、VLAN、链路虚拟化、数据中心网络和完整Web请求回顾”的因果链，写出通信实体、输入状态、报文或算法、输出状态、性能边界和失败语义。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“虚拟局域网”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“虚拟局域网”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

### 6.5 链路虚拟化：网络作为链路层

把“链路虚拟化：网络作为链路层”放回“掌握链路层服务、差错检测、共享信道协议、ARP与以太网交换、VLAN、链路虚拟化、数据中心网络和完整Web请求回顾”的因果链，写出通信实体、输入状态、报文或算法、输出状态、性能边界和失败语义。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“链路虚拟化：网络作为链路层”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“链路虚拟化：网络作为链路层”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

### 6.6 数据中心网络

把“数据中心网络”放回“掌握链路层服务、差错检测、共享信道协议、ARP与以太网交换、VLAN、链路虚拟化、数据中心网络和完整Web请求回顾”的因果链，写出通信实体、输入状态、报文或算法、输出状态、性能边界和失败语义。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“数据中心网络”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“数据中心网络”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 6.6.1 数据中心体系结构

把“数据中心体系结构”放回“掌握链路层服务、差错检测、共享信道协议、ARP与以太网交换、VLAN、链路虚拟化、数据中心网络和完整Web请求回顾”的因果链，写出通信实体、输入状态、报文或算法、输出状态、性能边界和失败语义。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“数据中心体系结构”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“数据中心体系结构”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 6.6.2 数据中心网络的发展趋势

把“数据中心网络的发展趋势”放回“掌握链路层服务、差错检测、共享信道协议、ARP与以太网交换、VLAN、链路虚拟化、数据中心网络和完整Web请求回顾”的因果链，写出通信实体、输入状态、报文或算法、输出状态、性能边界和失败语义。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“数据中心网络的发展趋势”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“数据中心网络的发展趋势”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

### 6.7 回顾：Web页面请求的历程

把“回顾：Web页面请求的历程”放回“掌握链路层服务、差错检测、共享信道协议、ARP与以太网交换、VLAN、链路虚拟化、数据中心网络和完整Web请求回顾”的因果链，写出通信实体、输入状态、报文或算法、输出状态、性能边界和失败语义。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“回顾：Web页面请求的历程”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“回顾：Web页面请求的历程”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 6.7.1 准备：DHCP、UDP、IP和以太网

明确数据报边界、校验和、无连接状态与应用自担的可靠性；用丢失、重复和乱序反例验证应用协议。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“准备：DHCP、UDP、IP和以太网”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“准备：DHCP、UDP、IP和以太网”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 6.7.2 仍在准备：DNS和ARP

记录查询名、类型、递归/迭代角色、权威来源、响应码、TTL和缓存命中；用查询与应答报文验证，不只看最终IP。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“仍在准备：DNS和ARP”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“仍在准备：DNS和ARP”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 6.7.3 仍在准备：域内路由选择到DNS服务器

记录查询名、类型、递归/迭代角色、权威来源、响应码、TTL和缓存命中；用查询与应答报文验证，不只看最终IP。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“仍在准备：域内路由选择到DNS服务器”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“仍在准备：域内路由选择到DNS服务器”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 6.7.4 Web客户-服务器交互：TCP和HTTP

沿请求方法、目标、首部、状态码、连接复用和缓存验证器画出客户与服务器状态；区分应用语义和运输连接。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“Web客户-服务器交互：TCP和HTTP”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“Web客户-服务器交互：TCP和HTTP”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

### 6.8 小结

把“小结”放回“掌握链路层服务、差错检测、共享信道协议、ARP与以太网交换、VLAN、链路虚拟化、数据中心网络和完整Web请求回顾”的因果链，写出通信实体、输入状态、报文或算法、输出状态、性能边界和失败语义。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“小结”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“小结”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

## 本页完整节点清单

本页承担28个原书正式节点。正文、图解、实验和题目必须能反向定位每项：

- 第6章 链路层和局域网
- 6.1 链路层概述
- 6.1.1 链路层提供的服务
- 6.1.2 链路层在何处实现
- 6.2 差错检测和纠正技术
- 6.2.1 奇偶校验
- 6.2.2 检验和方法
- 6.2.3 循环冗余检测
- 6.3 多路访问链路和协议
- 6.3.1 信道划分协议
- 6.3.2 随机接入协议
- 6.3.3 轮流协议
- 6.3.4 DOCSIS：用于电缆因特网接入的链路层协议
- 6.4 交换局域网
- 6.4.1 链路层寻址和ARP
- 6.4.2 以太网
- 6.4.3 链路层交换机
- 6.4.4 虚拟局域网
- 6.5 链路虚拟化：网络作为链路层
- 6.6 数据中心网络
- 6.6.1 数据中心体系结构
- 6.6.2 数据中心网络的发展趋势
- 6.7 回顾：Web页面请求的历程
- 6.7.1 准备：DHCP、UDP、IP和以太网
- 6.7.2 仍在准备：DNS和ARP
- 6.7.3 仍在准备：域内路由选择到DNS服务器
- 6.7.4 Web客户-服务器交互：TCP和HTTP
- 6.8 小结

<Stepper>
  <Step title="建立逐层正常路径">
    从应用消息向下标出运输端点、IP与转发表、下一跳链路、无线或接入状态以及安全保护边界。
    </Step>
  <Step title="只改变一个网络变量">
    固定其他条件，改变时延、瓶颈、丢包、表项、协议状态或信任条件，先写预测再执行。
    </Step>
  <Step title="关闭独立证据门">
    对照正常与失败时间线，确认首个偏差、告警、恢复、版次边界和第三方复核结果。
    </Step>
</Stepper>

## 可复现实验记录

```text
frame = dst_mac | src_mac | type | payload | fcs
receiver_remainder = polynomial_division(frame_bits, generator)
```

```text
on_frame(port, src, dst, vlan):
  learn(src, port, vlan)
  forward_or_flood(dst, vlan)
```

```text
DHCP -> DNS -> ARP -> Ethernet -> IP routing -> TCP -> HTTP
verify each transition with packet and table evidence
```

动手试：保存一次正常流、一次性能边界和一次单变量故障。至少记录应用消息、套接字/运输状态、路由与邻居表、链路或无线事件、安全握手、统一时间戳和恢复步骤。

<Callout type="trap" title="把局部可达当成端到端正确">
  ICMP可达不证明DNS、目标端口、TLS身份、HTTP语义或返回路径正确。必须沿协议栈逐层关闭证据缺口。
</Callout>

<Callout type="trap" title="混入其他版次的目录">
  本课程以中英文官方第8版目录为分母。第7版退役主题与第9版重排、新增内容可做版本比较，但不能替换本版正式节点。
</Callout>

## 独立证据门

最小证据包包含：第8版节点、正常报文时间线、地址与端口、协议状态、路由/邻居/链路表项、性能条件、单变量故障、安全策略、恢复、偏差、责任人与复核人。

## 练习

<Exercises>

**问题 1：为什么“第6章 链路层和局域网”必须固定第8版？**

<Answer>
  固定版次才能让8章、218个节点、协议语境和实验资源一一对应。第7版退役材料和第9版新目录只能独立比较，不能覆盖本页正式分母。
</Answer>

**问题 2：怎样构造“把IP子网、VLAN、广播域和交换表混成同一概念，会在ARP、Trunk、环路和未知单播处产生错误设计”的最小反例？**

<Answer>
  固定应用、路径和其余协议状态，只改变一个报文、表项、定时器、链路或信任条件；比较逐层状态、首个偏差、告警、性能和恢复。
</Answer>

**问题 3：何时可以认为本页完成独立交接？**

<Answer>
  另一位学习者无需口头补充即可定位全部28个节点，重建正常路径，复现单变量故障，并依据“同一链路上的帧交付能由成帧、差错检测、介质访问、MAC学习、VLAN边界和下一跳解析共同解释”得到相同结论。
</Answer>

</Exercises>

## 本章回顾

“第6章 链路层和局域网”的核心是掌握链路层服务、差错检测、共享信道协议、ARP与以太网交换、VLAN、链路虚拟化、数据中心网络和完整Web请求回顾。真正掌握不是背缩写，而是能把应用意图、报文、状态、表项、性能、故障与安全证据连成可证伪的端到端模型。

<Glossary>
  <GlossaryItem term="CRC">
    把比特串视为多项式并用生成多项式余数检测突发错误的方法。掌握标准是能在报文、状态机或表项中定位，并构造一个最小失败反例。
  </GlossaryItem>
  <GlossaryItem term="多路访问协议">
    协调多个节点共享同一广播信道发送机会的规则。掌握标准是能在报文、状态机或表项中定位，并构造一个最小失败反例。
  </GlossaryItem>
  <GlossaryItem term="MAC地址">
    链路层接口用于局部帧交付的标识。掌握标准是能在报文、状态机或表项中定位，并构造一个最小失败反例。
  </GlossaryItem>
  <GlossaryItem term="ARP">
    在IPv4链路上把下一跳IP地址解析为MAC地址的协议。掌握标准是能在报文、状态机或表项中定位，并构造一个最小失败反例。
  </GlossaryItem>
  <GlossaryItem term="VLAN">
    在交换基础设施上逻辑划分广播域与端口成员的机制。掌握标准是能在报文、状态机或表项中定位，并构造一个最小失败反例。
  </GlossaryItem>
</Glossary>

[← 上一页：第5章 网络层：控制平面](/learn/computer-networks-top-down/05-cnt8-05-control-plane/cnt8-05-control-plane) · [下一页：第7章 无线网络和移动网络 →](/learn/computer-networks-top-down/07-cnt8-07-wireless-mobile/cnt8-07-wireless-mobile)

<Attribution
  adaptedFrom="Kurose与Ross《计算机网络：自顶向下方法》原书第8版"
  adaptedUrl="https://www.cmpedu.com/books/book/5606311.htm"
/>
