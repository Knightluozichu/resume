---
title: "第8章 计算机网络中的安全"
description: "从威胁模型和密码学原理建立完整性、签名、端点鉴别、邮件、TLS、IPsec、无线/蜂窝鉴别、防火墙与入侵检测"
type: A
section: "计算机网络：自顶向下方法（原书第8版）"
order: 8
draft: false
math: false
sourceUrl: "https://www.cmpedu.com/books/book/5606311.htm"
demo: true
---

import {
  Cnt808SecurityMapLab,
  Cnt808SecurityExperimentLab,
  Cnt808SecurityEvidenceLab,
} from "@/components/mdx/computer-networks-top-down/diagrams/cnt8-08-security";

# 第8章 计算机网络中的安全

本课程锁定James F. Kurose、Keith W. Ross著，陈鸣译《计算机网络：自顶向下方法（原书第8版）》，机械工业出版社，2022年10月，ISBN 9787111712367；英文原版Pearson第8版，2020年出版、2021版权，ISBN 9780135928615。机械工业出版社官方中文目录与Pearson、作者官方第8版目录共同确认8章结构；正式分母为8个章标题和全部编号节/小节，共218个节点。课后习题、编程作业、Wireshark实验、人物专访和参考文献作为学习支持保留说明，但不重复计入核心节点。

<Objectives>

- 能解释“第8章 计算机网络中的安全”全部节点，并把术语放回端到端协议路径。
- 能绘制通信实体、报文次序、状态机、表项和逐层封装。
- 能设计单变量实验，验证“每项安全结论都明确保护目标、信任根、密钥归属、握手新鲜性、保护范围、失败模式和可审计证据”。
- 能写出含版本边界、性能、故障、恢复与复核人的独立证据包。

</Objectives>

## 从一条可证伪的端到端路径开始

先预测：把加密等同于完整安全，或不验证证书身份、随机数、密钥方向和策略边界，会留下重放、中间人和错误放行。把预测写成“应用意图、运输状态、数据平面、控制平面、链路/无线、安全”六行，再运行协议或抓包。若结果与预测不同，先定位首个偏差，不把后续连锁症状冒充根因。

本页主问题是：从威胁模型和密码学原理建立完整性、签名、端点鉴别、邮件、TLS、IPsec、无线/蜂窝鉴别、防火墙与入侵检测。每个箭头要注明通信实体、报文、地址或标识、状态变化、定时器以及证据位置；每个性能结论要注明负载、路径、窗口和瓶颈假设。

验收不变量是：每项安全结论都明确保护目标、信任根、密钥归属、握手新鲜性、保护范围、失败模式和可审计证据。一次ping、一个HTTP状态码或一个接口up都只证明局部事实，不能独立证明名称解析、双向运输、转发、移动性与身份安全全部成立。

## 核心词汇与第8版边界

<Term def="使未获授权的观察者无法理解受保护信息的安全属性">机密性</Term>、<Term def="使用共享密钥验证报文完整性和来源真实性的短标签">报文鉴别码</Term>、<Term def="用私钥生成并可由公钥验证来源与完整性的密码学结果">数字签名</Term>、<Term def="在可靠运输之上协商身份与密钥并保护应用字节流的安全协议">TLS</Term>、<Term def="在网络层使用安全关联保护IP通信的协议族">IPsec</Term>

这些词汇固定在原书第8版语境。HTTP/2、QUIC/HTTP/3演化、CUBIC/BBR、SDN、NETCONF/YANG、4G/5G和WPA3语境属于本版；第9版重排或新增内容只能另行比较，不能修改218个节点分母。

<Cnt808SecurityMapLab />

## 核心机制深读

### 先写威胁模型再选密码机制

窃听、篡改、伪装、重放和拒绝服务需要不同控制。对称密码高效，公钥密码解决不同的密钥与身份问题，散列本身不提供来源鉴别。

动手试：先画预期报文时间线与状态变化，再抓取一次正常流和一次单变量故障流。标出首个偏差，避免用最终症状倒推任意根因。

### 完整性、鉴别与不可否认要分开

MAC依赖共享秘密，数字签名使用非对称密钥。证书把身份绑定到公钥，但验证者还必须检查信任链、名称、有效期和撤销语境。

动手试：先画预期报文时间线与状态变化，再抓取一次正常流和一次单变量故障流。标出首个偏差，避免用最终症状倒推任意根因。

### 鉴别协议必须证明新鲜性

仅重放旧口令或加密结果不能证明对方当前在线。随机数挑战、会话绑定和方向性密钥可降低重放与反射风险。

动手试：先画预期报文时间线与状态变化，再抓取一次正常流和一次单变量故障流。标出首个偏差，避免用最终症状倒推任意根因。

### TLS与IPsec保护范围不同

TLS保护特定应用连接并由应用身份驱动；IPsec用安全关联在网络层保护数据报，隧道模式可构建VPN。两者都需要密钥协商、序号和策略。

动手试：先画预期报文时间线与状态变化，再抓取一次正常流和一次单变量故障流。标出首个偏差，避免用最终症状倒推任意根因。

### 运行安全需要策略与检测闭环

防火墙按地址、端口、状态或应用规则控制流量，IDS检查事件与行为。无线和蜂窝接入还要完成双向鉴别与密钥协商，日志必须统一时间。

动手试：先画预期报文时间线与状态变化，再抓取一次正常流和一次单变量故障流。标出首个偏差，避免用最终症状倒推任意根因。

## 原书目录逐节点重构

### 第8章 计算机网络中的安全

先声明威胁、信任根、身份和密钥，再核对握手新鲜性、保护范围、策略命中与拒绝路径；成功连接不等于身份正确。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“计算机网络中的安全”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“计算机网络中的安全”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

### 8.1 什么是网络安全

先声明威胁、信任根、身份和密钥，再核对握手新鲜性、保护范围、策略命中与拒绝路径；成功连接不等于身份正确。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“什么是网络安全”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“什么是网络安全”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

### 8.2 密码学原理

先声明威胁、信任根、身份和密钥，再核对握手新鲜性、保护范围、策略命中与拒绝路径；成功连接不等于身份正确。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“密码学原理”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“密码学原理”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 8.2.1 对称密钥密码体制

先声明威胁、信任根、身份和密钥，再核对握手新鲜性、保护范围、策略命中与拒绝路径；成功连接不等于身份正确。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“对称密钥密码体制”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“对称密钥密码体制”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 8.2.2 公开密钥加密

把“公开密钥加密”放回“从威胁模型和密码学原理建立完整性、签名、端点鉴别、邮件、TLS、IPsec、无线/蜂窝鉴别、防火墙与入侵检测”的因果链，写出通信实体、输入状态、报文或算法、输出状态、性能边界和失败语义。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“公开密钥加密”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“公开密钥加密”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

### 8.3 报文完整性和数字签名

先声明威胁、信任根、身份和密钥，再核对握手新鲜性、保护范围、策略命中与拒绝路径；成功连接不等于身份正确。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“报文完整性和数字签名”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“报文完整性和数字签名”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 8.3.1 密码散列函数

先声明威胁、信任根、身份和密钥，再核对握手新鲜性、保护范围、策略命中与拒绝路径；成功连接不等于身份正确。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“密码散列函数”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“密码散列函数”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 8.3.2 报文鉴别码

先声明威胁、信任根、身份和密钥，再核对握手新鲜性、保护范围、策略命中与拒绝路径；成功连接不等于身份正确。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“报文鉴别码”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“报文鉴别码”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 8.3.3 数字签名

先声明威胁、信任根、身份和密钥，再核对握手新鲜性、保护范围、策略命中与拒绝路径；成功连接不等于身份正确。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“数字签名”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“数字签名”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

### 8.4 端点鉴别

先声明威胁、信任根、身份和密钥，再核对握手新鲜性、保护范围、策略命中与拒绝路径；成功连接不等于身份正确。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“端点鉴别”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“端点鉴别”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

### 8.5 安全电子邮件

先声明威胁、信任根、身份和密钥，再核对握手新鲜性、保护范围、策略命中与拒绝路径；成功连接不等于身份正确。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“安全电子邮件”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“安全电子邮件”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 8.5.1 安全电子邮件概述

先声明威胁、信任根、身份和密钥，再核对握手新鲜性、保护范围、策略命中与拒绝路径；成功连接不等于身份正确。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“安全电子邮件概述”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“安全电子邮件概述”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 8.5.2 PGP

把“PGP”放回“从威胁模型和密码学原理建立完整性、签名、端点鉴别、邮件、TLS、IPsec、无线/蜂窝鉴别、防火墙与入侵检测”的因果链，写出通信实体、输入状态、报文或算法、输出状态、性能边界和失败语义。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“PGP”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“PGP”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

### 8.6 使TCP连接安全：TLS

用序号、确认号、定时器、发送/接收窗口和状态转换解释交付；注入单个丢包或乱序，比较预期与抓包。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“使TCP连接安全：TLS”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“使TCP连接安全：TLS”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 8.6.1 宏观描述

把“宏观描述”放回“从威胁模型和密码学原理建立完整性、签名、端点鉴别、邮件、TLS、IPsec、无线/蜂窝鉴别、防火墙与入侵检测”的因果链，写出通信实体、输入状态、报文或算法、输出状态、性能边界和失败语义。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“宏观描述”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“宏观描述”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 8.6.2 更完整的描述

把“更完整的描述”放回“从威胁模型和密码学原理建立完整性、签名、端点鉴别、邮件、TLS、IPsec、无线/蜂窝鉴别、防火墙与入侵检测”的因果链，写出通信实体、输入状态、报文或算法、输出状态、性能边界和失败语义。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“更完整的描述”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“更完整的描述”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

### 8.7 网络层安全性：IPsec和虚拟专用网

先声明威胁、信任根、身份和密钥，再核对握手新鲜性、保护范围、策略命中与拒绝路径；成功连接不等于身份正确。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“网络层安全性：IPsec和虚拟专用网”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“网络层安全性：IPsec和虚拟专用网”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 8.7.1 IPsec和虚拟专用网

先声明威胁、信任根、身份和密钥，再核对握手新鲜性、保护范围、策略命中与拒绝路径；成功连接不等于身份正确。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“IPsec和虚拟专用网”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“IPsec和虚拟专用网”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 8.7.2 AH协议和ESP协议

把“AH协议和ESP协议”放回“从威胁模型和密码学原理建立完整性、签名、端点鉴别、邮件、TLS、IPsec、无线/蜂窝鉴别、防火墙与入侵检测”的因果链，写出通信实体、输入状态、报文或算法、输出状态、性能边界和失败语义。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“AH协议和ESP协议”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“AH协议和ESP协议”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 8.7.3 安全关联

先声明威胁、信任根、身份和密钥，再核对握手新鲜性、保护范围、策略命中与拒绝路径；成功连接不等于身份正确。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“安全关联”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“安全关联”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 8.7.4 IPsec数据报

先声明威胁、信任根、身份和密钥，再核对握手新鲜性、保护范围、策略命中与拒绝路径；成功连接不等于身份正确。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“IPsec数据报”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“IPsec数据报”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 8.7.5 IKE：IPsec中的密钥管理

先声明威胁、信任根、身份和密钥，再核对握手新鲜性、保护范围、策略命中与拒绝路径；成功连接不等于身份正确。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“IKE：IPsec中的密钥管理”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“IKE：IPsec中的密钥管理”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

### 8.8 实现安全的无线局域网和4G/5G蜂窝网络

保存关联或附着、无线质量、调度、核心网承载、位置和切换时间线；只改变一个接入或移动变量。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“实现安全的无线局域网和4G/5G蜂窝网络”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“实现安全的无线局域网和4G/5G蜂窝网络”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 8.8.1 802.11无线局域网中的鉴别和密钥协商

保存关联或附着、无线质量、调度、核心网承载、位置和切换时间线；只改变一个接入或移动变量。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“802.11无线局域网中的鉴别和密钥协商”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“802.11无线局域网中的鉴别和密钥协商”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 8.8.2 4G/5G蜂窝网络中的鉴别和密钥协商

保存关联或附着、无线质量、调度、核心网承载、位置和切换时间线；只改变一个接入或移动变量。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“4G/5G蜂窝网络中的鉴别和密钥协商”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“4G/5G蜂窝网络中的鉴别和密钥协商”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

### 8.9 运行安全性：防火墙和入侵检测系统

先声明威胁、信任根、身份和密钥，再核对握手新鲜性、保护范围、策略命中与拒绝路径；成功连接不等于身份正确。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“运行安全性：防火墙和入侵检测系统”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“运行安全性：防火墙和入侵检测系统”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 8.9.1 防火墙

先声明威胁、信任根、身份和密钥，再核对握手新鲜性、保护范围、策略命中与拒绝路径；成功连接不等于身份正确。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“防火墙”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“防火墙”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

#### 8.9.2 入侵检测系统

先声明威胁、信任根、身份和密钥，再核对握手新鲜性、保护范围、策略命中与拒绝路径；成功连接不等于身份正确。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“入侵检测系统”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“入侵检测系统”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

### 8.10 小结

把“小结”放回“从威胁模型和密码学原理建立完整性、签名、端点鉴别、邮件、TLS、IPsec、无线/蜂窝鉴别、防火墙与入侵检测”的因果链，写出通信实体、输入状态、报文或算法、输出状态、性能边界和失败语义。 验收时先给出正常路径预测，再只改变一个变量，保存带统一时间戳的报文、表项、指标和日志；若实际结果不同，修正模型并说明“小结”如何影响上一层服务。

**可复现核查：** 固定正常端到端路径，只改变一个与“小结”有关的报文、状态、表项、链路或安全变量；保存预期、实际、首个偏差和恢复结果。

## 本页完整节点清单

本页承担29个原书正式节点。正文、图解、实验和题目必须能反向定位每项：

- 第8章 计算机网络中的安全
- 8.1 什么是网络安全
- 8.2 密码学原理
- 8.2.1 对称密钥密码体制
- 8.2.2 公开密钥加密
- 8.3 报文完整性和数字签名
- 8.3.1 密码散列函数
- 8.3.2 报文鉴别码
- 8.3.3 数字签名
- 8.4 端点鉴别
- 8.5 安全电子邮件
- 8.5.1 安全电子邮件概述
- 8.5.2 PGP
- 8.6 使TCP连接安全：TLS
- 8.6.1 宏观描述
- 8.6.2 更完整的描述
- 8.7 网络层安全性：IPsec和虚拟专用网
- 8.7.1 IPsec和虚拟专用网
- 8.7.2 AH协议和ESP协议
- 8.7.3 安全关联
- 8.7.4 IPsec数据报
- 8.7.5 IKE：IPsec中的密钥管理
- 8.8 实现安全的无线局域网和4G/5G蜂窝网络
- 8.8.1 802.11无线局域网中的鉴别和密钥协商
- 8.8.2 4G/5G蜂窝网络中的鉴别和密钥协商
- 8.9 运行安全性：防火墙和入侵检测系统
- 8.9.1 防火墙
- 8.9.2 入侵检测系统
- 8.10 小结

<Stepper>
  <Step title="建立逐层正常路径">
    从应用消息向下标出运输端点、IP与转发表、下一跳链路、无线或接入状态以及安全保护边界。
    <Cnt808SecurityMapLab />
  </Step>
  <Step title="只改变一个网络变量">
    固定其他条件，改变时延、瓶颈、丢包、表项、协议状态或信任条件，先写预测再执行。
    <Cnt808SecurityExperimentLab />
  </Step>
  <Step title="关闭独立证据门">
    对照正常与失败时间线，确认首个偏差、告警、恢复、版次边界和第三方复核结果。
    <Cnt808SecurityEvidenceLab />
  </Step>
</Stepper>

## 可复现实验记录

```text
security_goal | threat | trust_anchor | key_owner | protected_bytes | failure_action
```

```text
client_hello -> server_identity + key_agreement
verify certificate name and chain
derive directional traffic keys
protect application records
```

```text
policy: allow src_zone=app dst_zone=db tcp/5432 state=new
evidence: rule_id + flow_log + denied_negative_test + reviewer
```

动手试：保存一次正常流、一次性能边界和一次单变量故障。至少记录应用消息、套接字/运输状态、路由与邻居表、链路或无线事件、安全握手、统一时间戳和恢复步骤。

<Callout type="trap" title="把局部可达当成端到端正确">
  ICMP可达不证明DNS、目标端口、TLS身份、HTTP语义或返回路径正确。必须沿协议栈逐层关闭证据缺口。
</Callout>

<Callout type="trap" title="混入其他版次的目录">
  本课程以中英文官方第8版目录为分母。第7版退役主题与第9版重排、新增内容可做版本比较，但不能替换本版正式节点。
</Callout>

## 独立证据门

<Cnt808SecurityEvidenceLab />

最小证据包包含：第8版节点、正常报文时间线、地址与端口、协议状态、路由/邻居/链路表项、性能条件、单变量故障、安全策略、恢复、偏差、责任人与复核人。

## 练习

<Exercises>

**问题 1：为什么“第8章 计算机网络中的安全”必须固定第8版？**

<Answer>
  固定版次才能让8章、218个节点、协议语境和实验资源一一对应。第7版退役材料和第9版新目录只能独立比较，不能覆盖本页正式分母。
</Answer>

**问题 2：怎样构造“把加密等同于完整安全，或不验证证书身份、随机数、密钥方向和策略边界，会留下重放、中间人和错误放行”的最小反例？**

<Answer>
  固定应用、路径和其余协议状态，只改变一个报文、表项、定时器、链路或信任条件；比较逐层状态、首个偏差、告警、性能和恢复。
</Answer>

**问题 3：何时可以认为本页完成独立交接？**

<Answer>
  另一位学习者无需口头补充即可定位全部29个节点，重建正常路径，复现单变量故障，并依据“每项安全结论都明确保护目标、信任根、密钥归属、握手新鲜性、保护范围、失败模式和可审计证据”得到相同结论。
</Answer>

</Exercises>

## 本章回顾

“第8章 计算机网络中的安全”的核心是从威胁模型和密码学原理建立完整性、签名、端点鉴别、邮件、TLS、IPsec、无线/蜂窝鉴别、防火墙与入侵检测。真正掌握不是背缩写，而是能把应用意图、报文、状态、表项、性能、故障与安全证据连成可证伪的端到端模型。

<Glossary>
  <GlossaryItem term="机密性">
    使未获授权的观察者无法理解受保护信息的安全属性。掌握标准是能在报文、状态机或表项中定位，并构造一个最小失败反例。
  </GlossaryItem>
  <GlossaryItem term="报文鉴别码">
    使用共享密钥验证报文完整性和来源真实性的短标签。掌握标准是能在报文、状态机或表项中定位，并构造一个最小失败反例。
  </GlossaryItem>
  <GlossaryItem term="数字签名">
    用私钥生成并可由公钥验证来源与完整性的密码学结果。掌握标准是能在报文、状态机或表项中定位，并构造一个最小失败反例。
  </GlossaryItem>
  <GlossaryItem term="TLS">
    在可靠运输之上协商身份与密钥并保护应用字节流的安全协议。掌握标准是能在报文、状态机或表项中定位，并构造一个最小失败反例。
  </GlossaryItem>
  <GlossaryItem term="IPsec">
    在网络层使用安全关联保护IP通信的协议族。掌握标准是能在报文、状态机或表项中定位，并构造一个最小失败反例。
  </GlossaryItem>
</Glossary>

[← 上一页：第7章 无线网络和移动网络](/learn/computer-networks-top-down/07-cnt8-07-wireless-mobile/cnt8-07-wireless-mobile) · [下一页：原书第8版总复习与端到端诊断 →](/learn/computer-networks-top-down/09-cnt8-official-final-review/cnt8-official-final-review)

<Attribution
  adaptedFrom="Kurose与Ross《计算机网络：自顶向下方法》原书第8版"
  adaptedUrl="https://www.cmpedu.com/books/book/5606311.htm"
/>
