---
title: "第3章 整数函数"
type: C
section: "具体数学2e · 整数与数论"
order: 3
description: "以floor、ceiling和mod处理离散边界、分桶、递推与带取整求和。"
demo: true
math: true
sourceUrl: "https://www.informit.com/store/concrete-mathematics-a-foundation-for-computer-science-9780201558029"
draft: false
---

import { Objectives, Callout, Attribution, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step } from "@/components/mdx/mdx-components";
import { Cm2IntegerFunctionsObjectLab, Cm2IntegerFunctionsDerivationLab, Cm2IntegerFunctionsEvidenceLab } from "@/components/mdx/concrete-mathematics/diagrams/cm2-integer-functions";

<Objectives>
- 能解释下取整与上取整的对象、索引和边界约定
- 能推导取整谱到模运算的关键等式并标注每次变换
- 能实现取整和的最小程序，用反例和误差界验收
</Objectives>

## 先做预测

先预测索引平移、初值改变、参数取边界或n增大时，精确值与近似值如何变化。以floor、ceiling和mod处理离散边界、分桶、递推与带取整求和。 本页用<Term def="floor x是小于等于x的最大整数，精确表达完整块数量。">下取整</Term>、<Term def="ceiling x是大于等于x的最小整数，精确表达覆盖需求。">上取整</Term>、<Term def="floor与ceiling通过负号、平移和分数部分形成可转换恒等式。">取整谱</Term>、<Term def="a mod m给出除法余数；负数语义必须固定到数学约定。">模运算</Term>和<Term def="通过格点计数、互补区域或分块计算包含floor的求和。">取整和</Term>建立对象、变换、证书与误差闭环。

<Cm2IntegerFunctionsObjectLab />

## 第二版权威目录定位

本页一一对应Graham、Knuth和Patashnik《Concrete Mathematics》第二版“第3章 整数函数”。出版社目录给出9个正式章节；作者官网确认1994年第二版、ISBN与657页，并说明新增机械求和内容。

### 原书小节覆盖

1. Floors and Ceilings
2. Floor/Ceiling Applications
3. Floor/Ceiling Recurrences
4. mod: The Binary Operation
5. Floor/Ceiling Sums
6. Exercises

### 核心对象

1. **下取整**：floor x是小于等于x的最大整数，精确表达完整块数量。
2. **上取整**：ceiling x是大于等于x的最小整数，精确表达覆盖需求。
3. **取整谱**：floor与ceiling通过负号、平移和分数部分形成可转换恒等式。
4. **模运算**：a mod m给出除法余数；负数语义必须固定到数学约定。
5. **取整和**：通过格点计数、互补区域或分块计算包含floor的求和。

## 先固定离散契约

公式前先写索引集合、初值、参数范围和边界约定。递推、取整、组合数和渐近式最常见的错误不是代数，而是少一项、从错误下标开始或在不合法参数上继续使用恒等式。

$$
\mathcal P=(\text{index domain},\text{initial values},\text{parameters},\text{boundary convention})
$$

精确问题应先枚举最小规模：空集合、n为零、第一非平凡值和正好落在边界的值。小例子不能替代理论，但能尽早暴露符号和约定不一致。

## 可核查推导

$$
\lfloor x\rfloor\le x<\lfloor x\rfloor+1,\qquad \lceil x\rceil=-\lfloor-x\rfloor
$$

下取整定义给出唯一相邻整数夹逼。对负x应用该夹逼并乘负一，不等号反向，得到上取整与负下取整的对偶关系。 每次换元都同步改上下界，每次交换求和都说明有限性或收敛条件，每次从精确式转为渐近式都保留余项。

```python
from math import floor,ceil
for x in (-2.3,-1,0,2.7):
    assert floor(x)<=x<floor(x)+1
    assert ceil(x)==-floor(-x)
```

代码与手算必须在至少五个连续规模上相符。只测一个点可能让两个不同序列偶然相等；还应加入边界和随机小规模穷举。

## 对象与变换链

**下取整**：floor x是小于等于x的最大整数，精确表达完整块数量。

**上取整**：ceiling x是大于等于x的最小整数，精确表达覆盖需求。

**取整谱**：floor与ceiling通过负号、平移和分数部分形成可转换恒等式。

具体数学的核心是把离散对象在多种表示间切换：递推转求和，求和转差分，组合对象转生成函数，概率计数转指示变量，精确式转带余项的渐近式。每一步都要能逆向检查。

<Cm2IntegerFunctionsDerivationLab />

## 边界、反例与误差

**模运算**：a mod m给出除法余数；负数语义必须固定到数学约定。

**取整和**：通过格点计数、互补区域或分块计算包含floor的求和。

边界集至少包括空和、空积、零规模、负参数、参数恰在端点、取模负数、重复根、发散级数和小n渐近失真。失败应明确指出哪个前提不成立。

$$
e(n)=\text{exact}(n)-\text{approx}(n),\qquad |e(n)|\le B(n)
$$

```python
audit={"exact":"enumerate small n","boundary":"zero and endpoint","counterexample":"认为floor负数等于向零截断，导致负坐标分桶错误。","certificate":"replay transformations"}
assert set(audit)=={"exact","boundary","counterexample","certificate"}
```

## 算法案例

分页、环形缓冲和任务分片必须明确整数除法与负余数语义；用边界正好整除、少一和多一三组输入验证floor或ceiling选择。 报告保存精确输入、索引域、中间恒等式、机械证书或组合解释、程序输出与误差界，保证他人能从头重算。

<Cm2IntegerFunctionsEvidenceLab />

```python
evidence=[("下取整","definition"),("取整谱","derivation"),("取整和","certificate")]
assert len({name for name,_ in evidence})==3
```

## 分步实验

<Stepper>
  <Step title="枚举对象"><Cm2IntegerFunctionsObjectLab /></Step>
  <Step title="变换公式"><Cm2IntegerFunctionsDerivationLab /></Step>
  <Step title="保存证书"><Cm2IntegerFunctionsEvidenceLab /></Step>
</Stepper>

1. 写出索引域、初值和空对象约定，手算最小非平凡样例。
2. 一次只做一种变换，保存边界项和前后等价关系。
3. 删除一个假设构造反例，解释首个失败步骤。
4. 对近似结论计算残差与误差界，并标明何时进入渐近区间。

## 验收证书

$$
\operatorname{accept}=\text{exact examples}\land\text{derivation}\land\text{counterexample}\land\text{error budget}
$$

证书拒绝孤立闭式。它应包含问题对象、精确关系、推导步骤、初值、机械或组合证据、小规模重放、反例和误差预算。

## 常见误区

<Callout type="trap" title="边界项丢失">认为floor负数等于向零截断，导致负坐标分桶错误。 修复时重新列出索引集合与初值。</Callout>

<Callout type="trap" title="公式脱离对象">把编程语言的负数余数规则直接当作数学mod而未写适配。 应补充组合解释、证书、反例与误差界。</Callout>

## 本章回顾

本章覆盖下取整、上取整、取整谱、模运算、取整和。掌握标准是能从对象建立精确式，逐步变换并保留边界，给出可重放证书，再说明近似误差和反例。

<Exercises>
**问题1：下取整与上取整如何协作？**
<Answer>floor x是小于等于x的最大整数，精确表达完整块数量。 ceiling x是大于等于x的最小整数，精确表达覆盖需求。 回答时写清索引和边界。</Answer>

**问题2：如何检查取整谱的推导？**
<Answer>下取整定义给出唯一相邻整数夹逼。对负x应用该夹逼并乘负一，不等号反向，得到上取整与负下取整的对偶关系。 用连续小规模值逐项对照。</Answer>

**问题3：怎样验收取整和？**
<Answer>通过格点计数、互补区域或分块计算包含floor的求和。 同时保存正常例、边界例、反例和误差界。</Answer>
</Exercises>

## 术语表

<Glossary>
  <GlossaryItem term="下取整" definition="floor x是小于等于x的最大整数，精确表达完整块数量。" />
  <GlossaryItem term="上取整" definition="ceiling x是大于等于x的最小整数，精确表达覆盖需求。" />
  <GlossaryItem term="取整谱" definition="floor与ceiling通过负号、平移和分数部分形成可转换恒等式。" />
  <GlossaryItem term="模运算" definition="a mod m给出除法余数；负数语义必须固定到数学约定。" />
  <GlossaryItem term="取整和" definition="通过格点计数、互补区域或分块计算包含floor的求和。" />
  <GlossaryItem term="离散证书" definition="能重放恒等式、递推或机械求和结果的边界、初值和中间关系。" />
</Glossary>

<Attribution adaptedFrom="Ronald L. Graham, Donald E. Knuth, Oren Patashnik · Concrete Mathematics, Second Edition · 第3章 整数函数" adaptedUrl="https://www.informit.com/store/concrete-mathematics-a-foundation-for-computer-science-9780201558029" />
