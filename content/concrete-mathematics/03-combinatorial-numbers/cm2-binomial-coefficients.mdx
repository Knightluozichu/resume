---
title: "第5章 二项式系数"
type: C
section: "具体数学2e · 组合数与特殊数"
order: 5
description: "从基本恒等式、吸收与卷积技巧进入超几何项、变换、部分和与机械求和。"
demo: true
math: true
sourceUrl: "https://www.informit.com/store/concrete-mathematics-a-foundation-for-computer-science-9780201558029"
draft: true
---

import { Objectives, Callout, Attribution, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step } from "@/components/mdx/mdx-components";
import { Cm2BinomialCoefficientsObjectLab, Cm2BinomialCoefficientsDerivationLab, Cm2BinomialCoefficientsEvidenceLab } from "@/components/mdx/concrete-mathematics/diagrams/cm2-binomial-coefficients";

<Objectives>
- 能解释二项式系数与Pascal恒等式的对象、索引和边界约定
- 能推导Vandermonde卷积到超几何项的关键等式并标注每次变换
- 能实现求和证书的最小程序，用反例和误差界验收
</Objectives>

## 先做预测

先预测索引平移、初值改变、参数取边界或n增大时，精确值与近似值如何变化。从基本恒等式、吸收与卷积技巧进入超几何项、变换、部分和与机械求和。 本页用<Term def="n选k计数k元素子集，并通过广义定义扩展到更多参数。">二项式系数</Term>、<Term def="按是否包含指定元素分组，得到相邻两项之和。">Pascal恒等式</Term>、<Term def="按从两个集合分别选取多少元素对同一对象双重计数。">Vandermonde卷积</Term>、<Term def="相邻项比值是索引的有理函数，适合机械化求和。">超几何项</Term>和<Term def="把目标和写成望远镜差分或参数递推，提供可检查证明。">求和证书</Term>建立对象、变换、证书与误差闭环。

<Cm2BinomialCoefficientsObjectLab />

## 第二版权威目录定位

本页一一对应Graham、Knuth和Patashnik《Concrete Mathematics》第二版“第5章 二项式系数”。出版社目录给出9个正式章节；作者官网确认1994年第二版、ISBN与657页，并说明新增机械求和内容。

### 原书小节覆盖

1. Basic Identities
2. Basic Practice
3. Tricks of the Trade
4. Generating Functions
5. Hypergeometric Functions
6. Hypergeometric Transformations
7. Partial Hypergeometric Sums
8. Mechanical Summation
9. Exercises

### 核心对象

1. **二项式系数**：n选k计数k元素子集，并通过广义定义扩展到更多参数。
2. **Pascal恒等式**：按是否包含指定元素分组，得到相邻两项之和。
3. **Vandermonde卷积**：按从两个集合分别选取多少元素对同一对象双重计数。
4. **超几何项**：相邻项比值是索引的有理函数，适合机械化求和。
5. **求和证书**：把目标和写成望远镜差分或参数递推，提供可检查证明。

## 先固定离散契约

公式前先写索引集合、初值、参数范围和边界约定。递推、取整、组合数和渐近式最常见的错误不是代数，而是少一项、从错误下标开始或在不合法参数上继续使用恒等式。

$$
\mathcal P=(\text{index domain},\text{initial values},\text{parameters},\text{boundary convention})
$$

精确问题应先枚举最小规模：空集合、n为零、第一非平凡值和正好落在边界的值。小例子不能替代理论，但能尽早暴露符号和约定不一致。

## 可核查推导

$$
\binom nk=\binom{n-1}k+\binom{n-1}{k-1},\qquad \sum_k\binom rk\binom s{n-k}=\binom{r+s}n
$$

从r加s个元素中选n个，可直接计数得到右侧；也可按从前r个元素中选k个分类并对所有k求和，得到左侧。两种方法计数同一集合。 每次换元都同步改上下界，每次交换求和都说明有限性或收敛条件，每次从精确式转为渐近式都保留余项。

```python
from math import comb
def vandermonde(r,s,n):
    return sum(comb(r,k)*comb(s,n-k) for k in range(max(0,n-s),min(r,n)+1))
assert vandermonde(5,7,4)==comb(12,4)
```

代码与手算必须在至少五个连续规模上相符。只测一个点可能让两个不同序列偶然相等；还应加入边界和随机小规模穷举。

## 对象与变换链

**二项式系数**：n选k计数k元素子集，并通过广义定义扩展到更多参数。

**Pascal恒等式**：按是否包含指定元素分组，得到相邻两项之和。

**Vandermonde卷积**：按从两个集合分别选取多少元素对同一对象双重计数。

具体数学的核心是把离散对象在多种表示间切换：递推转求和，求和转差分，组合对象转生成函数，概率计数转指示变量，精确式转带余项的渐近式。每一步都要能逆向检查。

<Cm2BinomialCoefficientsDerivationLab />

## 边界、反例与误差

**超几何项**：相邻项比值是索引的有理函数，适合机械化求和。

**求和证书**：把目标和写成望远镜差分或参数递推，提供可检查证明。

边界集至少包括空和、空积、零规模、负参数、参数恰在端点、取模负数、重复根、发散级数和小n渐近失真。失败应明确指出哪个前提不成立。

$$
e(n)=\text{exact}(n)-\text{approx}(n),\qquad |e(n)|\le B(n)
$$

```python
audit={"exact":"enumerate small n","boundary":"zero and endpoint","counterexample":"把组合定义域限制在教科书正整数后，又在变换中静默使用广义二项式。","certificate":"replay transformations"}
assert set(audit)=={"exact","boundary","counterexample","certificate"}
```

## 算法案例

组合恒等式优先寻找双重计数解释，再用符号变换或机械证书补充。程序验收覆盖k小于零、大于n和边界n为零。 报告保存精确输入、索引域、中间恒等式、机械证书或组合解释、程序输出与误差界，保证他人能从头重算。

<Cm2BinomialCoefficientsEvidenceLab />

```python
evidence=[("二项式系数","definition"),("Vandermonde卷积","derivation"),("求和证书","certificate")]
assert len({name for name,_ in evidence})==3
```

## 分步实验

<Stepper>
  <Step title="枚举对象"><Cm2BinomialCoefficientsObjectLab /></Step>
  <Step title="变换公式"><Cm2BinomialCoefficientsDerivationLab /></Step>
  <Step title="保存证书"><Cm2BinomialCoefficientsEvidenceLab /></Step>
</Stepper>

1. 写出索引域、初值和空对象约定，手算最小非平凡样例。
2. 一次只做一种变换，保存边界项和前后等价关系。
3. 删除一个假设构造反例，解释首个失败步骤。
4. 对近似结论计算残差与误差界，并标明何时进入渐近区间。

## 验收证书

$$
\operatorname{accept}=\text{exact examples}\land\text{derivation}\land\text{counterexample}\land\text{error budget}
$$

证书拒绝孤立闭式。它应包含问题对象、精确关系、推导步骤、初值、机械或组合证据、小规模重放、反例和误差预算。

## 常见误区

<Callout type="trap" title="边界项丢失">把组合定义域限制在教科书正整数后，又在变换中静默使用广义二项式。 修复时重新列出索引集合与初值。</Callout>

<Callout type="trap" title="公式脱离对象">机械求和只输出闭式，不保存望远镜证书或递推初值。 应补充组合解释、证书、反例与误差界。</Callout>

## 本章回顾

本章覆盖二项式系数、Pascal恒等式、Vandermonde卷积、超几何项、求和证书。掌握标准是能从对象建立精确式，逐步变换并保留边界，给出可重放证书，再说明近似误差和反例。

<Exercises>
**问题1：二项式系数与Pascal恒等式如何协作？**
<Answer>n选k计数k元素子集，并通过广义定义扩展到更多参数。 按是否包含指定元素分组，得到相邻两项之和。 回答时写清索引和边界。</Answer>

**问题2：如何检查Vandermonde卷积的推导？**
<Answer>从r加s个元素中选n个，可直接计数得到右侧；也可按从前r个元素中选k个分类并对所有k求和，得到左侧。两种方法计数同一集合。 用连续小规模值逐项对照。</Answer>

**问题3：怎样验收求和证书？**
<Answer>把目标和写成望远镜差分或参数递推，提供可检查证明。 同时保存正常例、边界例、反例和误差界。</Answer>
</Exercises>

## 术语表

<Glossary>
  <GlossaryItem term="二项式系数" definition="n选k计数k元素子集，并通过广义定义扩展到更多参数。" />
  <GlossaryItem term="Pascal恒等式" definition="按是否包含指定元素分组，得到相邻两项之和。" />
  <GlossaryItem term="Vandermonde卷积" definition="按从两个集合分别选取多少元素对同一对象双重计数。" />
  <GlossaryItem term="超几何项" definition="相邻项比值是索引的有理函数，适合机械化求和。" />
  <GlossaryItem term="求和证书" definition="把目标和写成望远镜差分或参数递推，提供可检查证明。" />
  <GlossaryItem term="离散证书" definition="能重放恒等式、递推或机械求和结果的边界、初值和中间关系。" />
</Glossary>

<Attribution adaptedFrom="Ronald L. Graham, Donald E. Knuth, Oren Patashnik · Concrete Mathematics, Second Edition · 第5章 二项式系数" adaptedUrl="https://www.informit.com/store/concrete-mathematics-a-foundation-for-computer-science-9780201558029" />
