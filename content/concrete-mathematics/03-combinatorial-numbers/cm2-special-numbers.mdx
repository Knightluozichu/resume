---
title: "第6章 特殊数"
type: C
section: "具体数学2e · 组合数与特殊数"
order: 6
description: "统一理解Stirling数、Eulerian数、调和数、Bernoulli数、Fibonacci数与continuant。"
demo: true
math: true
sourceUrl: "https://www.informit.com/store/concrete-mathematics-a-foundation-for-computer-science-9780201558029"
draft: false
---

import { Objectives, Callout, Attribution, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step } from "@/components/mdx/mdx-components";
import { Cm2SpecialNumbersObjectLab, Cm2SpecialNumbersDerivationLab, Cm2SpecialNumbersEvidenceLab } from "@/components/mdx/concrete-mathematics/diagrams/cm2-special-numbers";

<Objectives>
- 能解释Stirling数与Eulerian数的对象、索引和边界约定
- 能推导调和数到Bernoulli数的关键等式并标注每次变换
- 能实现Fibonacci与continuant的最小程序，用反例和误差界验收
</Objectives>

## 先做预测

先预测索引平移、初值改变、参数取边界或n增大时，精确值与近似值如何变化。统一理解Stirling数、Eulerian数、调和数、Bernoulli数、Fibonacci数与continuant。 本页用<Term def="第一类连接排列循环与升降阶乘，第二类计数集合划分。">Stirling数</Term>、<Term def="按上升或下降次数细分排列，并形成多项式恒等式。">Eulerian数</Term>、<Term def="前n个倒数之和，常出现在平均复杂度和随机过程。">调和数</Term>、<Term def="通过生成函数定义并编码幂和与Euler求和公式。">Bernoulli数</Term>和<Term def="二阶递推、矩阵幂和连分数行列式共享同一结构。">Fibonacci与continuant</Term>建立对象、变换、证书与误差闭环。

<Cm2SpecialNumbersObjectLab />

## 第二版权威目录定位

本页一一对应Graham、Knuth和Patashnik《Concrete Mathematics》第二版“第6章 特殊数”。出版社目录给出9个正式章节；作者官网确认1994年第二版、ISBN与657页，并说明新增机械求和内容。

### 原书小节覆盖

1. Stirling Numbers
2. Eulerian Numbers
3. Harmonic Numbers
4. Harmonic Summation
5. Bernoulli Numbers
6. Fibonacci Numbers
7. Continuants
8. Exercises

### 核心对象

1. **Stirling数**：第一类连接排列循环与升降阶乘，第二类计数集合划分。
2. **Eulerian数**：按上升或下降次数细分排列，并形成多项式恒等式。
3. **调和数**：前n个倒数之和，常出现在平均复杂度和随机过程。
4. **Bernoulli数**：通过生成函数定义并编码幂和与Euler求和公式。
5. **Fibonacci与continuant**：二阶递推、矩阵幂和连分数行列式共享同一结构。

## 先固定离散契约

公式前先写索引集合、初值、参数范围和边界约定。递推、取整、组合数和渐近式最常见的错误不是代数，而是少一项、从错误下标开始或在不合法参数上继续使用恒等式。

$$
\mathcal P=(\text{index domain},\text{initial values},\text{parameters},\text{boundary convention})
$$

精确问题应先枚举最小规模：空集合、n为零、第一非平凡值和正好落在边界的值。小例子不能替代理论，但能尽早暴露符号和约定不一致。

## 可核查推导

$$
\left\{\!{n\atop k}\!\right\}=k\left\{\!{n-1\atop k}\!\right\}+\left\{\!{n-1\atop k-1}\!\right\},\qquad F_n=F_{n-1}+F_{n-2}
$$

划分n元素到k个非空块：新元素要么加入k个已有块之一，要么单独成块并把其余元素划为k减1块。两种互斥情况相加得到Stirling第二类递推。 每次换元都同步改上下界，每次交换求和都说明有限性或收敛条件，每次从精确式转为渐近式都保留余项。

```python
def stirling2(n,k):
    if n==k==0:return 1
    if n==0 or k==0:return 0
    return k*stirling2(n-1,k)+stirling2(n-1,k-1)
assert stirling2(5,2)==15
```

代码与手算必须在至少五个连续规模上相符。只测一个点可能让两个不同序列偶然相等；还应加入边界和随机小规模穷举。

## 对象与变换链

**Stirling数**：第一类连接排列循环与升降阶乘，第二类计数集合划分。

**Eulerian数**：按上升或下降次数细分排列，并形成多项式恒等式。

**调和数**：前n个倒数之和，常出现在平均复杂度和随机过程。

具体数学的核心是把离散对象在多种表示间切换：递推转求和，求和转差分，组合对象转生成函数，概率计数转指示变量，精确式转带余项的渐近式。每一步都要能逆向检查。

<Cm2SpecialNumbersDerivationLab />

## 边界、反例与误差

**Bernoulli数**：通过生成函数定义并编码幂和与Euler求和公式。

**Fibonacci与continuant**：二阶递推、矩阵幂和连分数行列式共享同一结构。

边界集至少包括空和、空积、零规模、负参数、参数恰在端点、取模负数、重复根、发散级数和小n渐近失真。失败应明确指出哪个前提不成立。

$$
e(n)=\text{exact}(n)-\text{approx}(n),\qquad |e(n)|\le B(n)
$$

```python
audit={"exact":"enumerate small n","boundary":"zero and endpoint","counterexample":"把两类Stirling数的对象和符号混用。","certificate":"replay transformations"}
assert set(audit)=={"exact","boundary","counterexample","certificate"}
```

## 算法案例

不要把特殊数当表格记忆；为每个序列保留组合对象、递推、生成函数和前几项，用两种定义交叉验收。 报告保存精确输入、索引域、中间恒等式、机械证书或组合解释、程序输出与误差界，保证他人能从头重算。

<Cm2SpecialNumbersEvidenceLab />

```python
evidence=[("Stirling数","definition"),("调和数","derivation"),("Fibonacci与continuant","certificate")]
assert len({name for name,_ in evidence})==3
```

## 分步实验

<Stepper>
  <Step title="枚举对象"><Cm2SpecialNumbersObjectLab /></Step>
  <Step title="变换公式"><Cm2SpecialNumbersDerivationLab /></Step>
  <Step title="保存证书"><Cm2SpecialNumbersEvidenceLab /></Step>
</Stepper>

1. 写出索引域、初值和空对象约定，手算最小非平凡样例。
2. 一次只做一种变换，保存边界项和前后等价关系。
3. 删除一个假设构造反例，解释首个失败步骤。
4. 对近似结论计算残差与误差界，并标明何时进入渐近区间。

## 验收证书

$$
\operatorname{accept}=\text{exact examples}\land\text{derivation}\land\text{counterexample}\land\text{error budget}
$$

证书拒绝孤立闭式。它应包含问题对象、精确关系、推导步骤、初值、机械或组合证据、小规模重放、反例和误差预算。

## 常见误区

<Callout type="trap" title="边界项丢失">把两类Stirling数的对象和符号混用。 修复时重新列出索引集合与初值。</Callout>

<Callout type="trap" title="公式脱离对象">使用Bernoulli数时未声明B1符号约定，导致公式整体差一项。 应补充组合解释、证书、反例与误差界。</Callout>

## 本章回顾

本章覆盖Stirling数、Eulerian数、调和数、Bernoulli数、Fibonacci与continuant。掌握标准是能从对象建立精确式，逐步变换并保留边界，给出可重放证书，再说明近似误差和反例。

<Exercises>
**问题1：Stirling数与Eulerian数如何协作？**
<Answer>第一类连接排列循环与升降阶乘，第二类计数集合划分。 按上升或下降次数细分排列，并形成多项式恒等式。 回答时写清索引和边界。</Answer>

**问题2：如何检查调和数的推导？**
<Answer>划分n元素到k个非空块：新元素要么加入k个已有块之一，要么单独成块并把其余元素划为k减1块。两种互斥情况相加得到Stirling第二类递推。 用连续小规模值逐项对照。</Answer>

**问题3：怎样验收Fibonacci与continuant？**
<Answer>二阶递推、矩阵幂和连分数行列式共享同一结构。 同时保存正常例、边界例、反例和误差界。</Answer>
</Exercises>

## 术语表

<Glossary>
  <GlossaryItem term="Stirling数" definition="第一类连接排列循环与升降阶乘，第二类计数集合划分。" />
  <GlossaryItem term="Eulerian数" definition="按上升或下降次数细分排列，并形成多项式恒等式。" />
  <GlossaryItem term="调和数" definition="前n个倒数之和，常出现在平均复杂度和随机过程。" />
  <GlossaryItem term="Bernoulli数" definition="通过生成函数定义并编码幂和与Euler求和公式。" />
  <GlossaryItem term="Fibonacci与continuant" definition="二阶递推、矩阵幂和连分数行列式共享同一结构。" />
  <GlossaryItem term="离散证书" definition="能重放恒等式、递推或机械求和结果的边界、初值和中间关系。" />
</Glossary>

<Attribution adaptedFrom="Ronald L. Graham, Donald E. Knuth, Oren Patashnik · Concrete Mathematics, Second Edition · 第6章 特殊数" adaptedUrl="https://www.informit.com/store/concrete-mathematics-a-foundation-for-computer-science-9780201558029" />
