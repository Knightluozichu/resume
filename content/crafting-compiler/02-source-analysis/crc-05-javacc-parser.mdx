---
title: 第5章 基于JavaCC的解析器描述
type: C
section: 第1部分 · 代码分析
order: 5
description: >-
  第5章
  基于JavaCC的解析器描述围绕基于EBNF语法的描述、语法的二义性和token的超前扫描推进cbc，实现从C♭源代码到中间产物、机器代码或运行时证据的可复查转换。
  本页覆盖3个正式目录层级，以章专属编译探针、首错定位和清理重建验收。
demo: true
math: false
sourceUrl: "https://i.loveruby.net/ja/stdcompiler/toc.html"
draft: true
qualityVersion: 2
practiceMode: code
sourceMode: independent-rewrite
---

import {
  Crc05JavaccParserMapLab,
  Crc05JavaccParserExperimentLab,
  Crc05JavaccParserEvidenceLab,
} from "@/components/mdx/crafting-compiler/diagrams/crc-05-javacc-parser";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第5章 基于JavaCC的解析器描述全部3个正式目录节点在C♭到进程链中的输入、输出和时代边界
- 能运行“EBNF与前看决策台”，一次只改变前看深度或产生式族
- 能修改章专属C♭、JavaCC、IR、IA-32汇编或ELF样本，并保存最早结构化分叉
- 能触发“JAVACODE黑盒出现在选择点导致不可预测分支”，清理派生产物后证明“每个选择点有可解释前缀，合法串唯一选择且非法串在最早位置失败”

</Objectives>

{/* CRAFTING_COMPILER_QUALITY_V2 */}

## 为什么从“EBNF与前看决策台”开始

第5章 基于JavaCC的解析器描述沿“production → expansion choice → LOOKAHEAD → action”转换信息。最终进程返回正确值只能证明一条执行路径；EBNF与前看决策台保存token、AST、绑定、类型、IR、汇编、符号、重定位或加载轨迹中的适用层，让错误停在第一处分叉。

第5章 基于JavaCC的解析器描述固定C♭源码、cbc提交、JDK与JavaCC、GNU as/ld/binutils、IA-32目标、链接参数和运行环境，只改变前看深度或产生式族。本页交互只移动production → expansion choice → LOOKAHEAD → action中的真实对象和状态，不生成置信、风险或证据分数。

## 来源、版本与独立重写边界

第5章 基于JavaCC的解析器描述以青木峰郎维护的[日文原版完整详细目录](https://i.loveruby.net/ja/stdcompiler/toc.html)限定4部分、22章、86个编号小节、附录和116个正式层级；本站未取得原书正文授权，中文解释、代码、图示、实验和答案均为独立教学重写。

第5章 基于JavaCC的解析器描述用[JavaCC完整语法文档](https://javacc.github.io/javacc/documentation/grammar.html)、[ELF规范](https://refspecs.linuxfoundation.org/elf/elf.pdf)、GNU [readelf](https://sourceware.org/binutils/docs/binutils/readelf.html)、[as](https://sourceware.org/binutils/docs/as/)与[ld](https://sourceware.org/binutils/docs/ld/)文档核对技术事实。原书主线是JavaCC、Linux/IA-32、GNU工具与ELF；现代JDK、x86-64、PIE和新版binutils默认值必须单独记录，不能改写原书时代坐标。

## 本章合同与可运行探针

<Term def="production → expansion choice → LOOKAHEAD → action是第5章 基于JavaCC的解析器描述连接冻结输入与可推翻编译结论的章专属坐标，必须记录源码、工具版本、目标与原始产物。">production → expansion choice → LOOKAHEAD → action</Term>、<Term def="前看深度是第5章 基于JavaCC的解析器描述连接产出结构与可推翻编译结论的章专属坐标，必须记录源码、工具版本、目标与原始产物。">前看深度</Term>、<Term def="产生式族是第5章 基于JavaCC的解析器描述连接断言不变量与可推翻编译结论的章专属坐标，必须记录源码、工具版本、目标与原始产物。">产生式族</Term>、<Term def="LOOKAHEAD轨迹与ParseException是第5章 基于JavaCC的解析器描述连接触发首错与可推翻编译结论的章专属坐标，必须记录源码、工具版本、目标与原始产物。">LOOKAHEAD轨迹与ParseException</Term>、<Term def="文法选择点清单是第5章 基于JavaCC的解析器描述连接清理重建与可推翻编译结论的章专属坐标，必须记录源码、工具版本、目标与原始产物。">文法选择点清单</Term>。

第5章 基于JavaCC的解析器描述的通过不变量是“每个选择点有可解释前缀，合法串唯一选择且非法串在最早位置失败”。实验档案保存源码与期望、cbc提交、工具版本、目标ABI、命令与退出码、所有适用中间产物、标准错误、第一处分叉、清理记录和回归结果。

```bash
javacc -DEBUG_LOOKAHEAD=true Parser.jj
java ParserProbe cases.txt
```

<Callout type="warning" title="历史IA-32工具链必须隔离">
  第5章
  基于JavaCC的解析器描述涉及旧JDK、旧JavaCC、32位链接器、任意目标文件或加载器变量时，在容器或离线虚拟机中运行；固定资源与超时，禁止执行不可信二进制，先检查ELF架构、入口和依赖。
</Callout>

## 先预测，再操作三层实验

<Stepper>
  <Step title="1. 结构与所有权">
    选择目录节点，标出生产者、数据结构、消费者和禁止越层的结论。
    <Crc05JavaccParserMapLab />
  </Step>
  <Step title="2. 单变量转换">
    固定工具链与样本，只切换前看深度或产生式族。
    <Crc05JavaccParserExperimentLab />
  </Step>
  <Step title="3. 首错与重建">
    注入“JAVACODE黑盒出现在选择点导致不可预测分支”，保存首处分叉，清理后从源码重建。
    <Crc05JavaccParserEvidenceLab />
  </Step>
</Stepper>

## 官方目录逐项深读

### 第5章 基于JavaCC的解析器描述

**四级证据 1/3。** 前端槽1用合法、最长匹配、歧义、截断和非法字符样本核对“第5章 基于JavaCC的解析器描述”的token、前看、树形与源位置；固定产生式族，只改变前看深度，以LOOKAHEAD轨迹与ParseException找到第一处分叉并清理重建。

### 5.1 基于EBNF语法的描述

**四级证据 2/3。** 前端槽2用合法、最长匹配、歧义、截断和非法字符样本核对“5.1 基于EBNF语法的描述”的token、前看、树形与源位置；固定产生式族，只改变前看深度，以LOOKAHEAD轨迹与ParseException找到第一处分叉并清理重建。

### 5.2 语法的二义性和token的超前扫描

**四级证据 3/3。** 前端槽3用合法、最长匹配、歧义、截断和非法字符样本核对“5.2 语法的二义性和token的超前扫描”的token、前看、树形与源位置；固定产生式族，只改变前看深度，以LOOKAHEAD轨迹与ParseException找到第一处分叉并清理重建。

## 三个必须主动触发的误区

<Callout type="trap" title="中间产物不能省略">
  第5章
  基于JavaCC的解析器描述不允许用最终返回值平均掉token、AST、绑定、类型、IR、汇编或ELF层失败；缺少适用中间证据即退回。
</Callout>

<Callout type="trap" title="章专属失败样本">
  第5章
  基于JavaCC的解析器描述主动触发“生成成功不等于生成的Java可编译或文法正确”，再注入“JAVACODE黑盒出现在选择点导致不可预测分支”。若LOOKAHEAD轨迹与ParseException没有变化，先验证阳性对照和采集链，不能复用旧构建。
</Callout>

<Callout type="trap" title="机制与现代默认值分开">
  第5章
  基于JavaCC的解析器描述的IA-32、非PIE或旧工具行为必须显式冻结；x86-64、PIE、不同JavaCC/JDK和binutils版本的结果只能作为带版本的对照。
</Callout>

## 练习、答案与116层级验证

<Exercises>

**问题1：因果隔离。** 怎样测量前看深度对LOOKAHEAD轨迹与ParseException的影响？

<Answer>
  第5章
  基于JavaCC的解析器描述固定源码、cbc提交、目标、产生式族和工具链，只把前看深度从“局部2”切到“语义前看”；比较首个结构化差分、退出码和下游停止位置，再复位并重建基线。
</Answer>

**问题2：四级覆盖。** 怎样证明本页3个目录节点不是标题复述？

<Answer>
  1. 第5章
  基于JavaCC的解析器描述：前端槽1用合法、最长匹配、歧义、截断和非法字符样本核对“第5章
  基于JavaCC的解析器描述”的token、前看、树形与源位置；用LOOKAHEAD轨迹与ParseException断言。
  2. 5.1
  基于EBNF语法的描述：前端槽2用合法、最长匹配、歧义、截断和非法字符样本核对“5.1
  基于EBNF语法的描述”的token、前看、树形与源位置；用文法选择点清单断言。 3. 5.2
  语法的二义性和token的超前扫描：前端槽3用合法、最长匹配、歧义、截断和非法字符样本核对“5.2
  语法的二义性和token的超前扫描”的token、前看、树形与源位置；用LOOKAHEAD轨迹与ParseException断言。
</Answer>

**问题3：修复证明。** 怎样证明“JAVACODE黑盒出现在选择点导致不可预测分支”已经修复？

<Answer>
  第5章
  基于JavaCC的解析器描述沿冻结输入、产出结构、断言不变量、触发首错、清理重建定位首错，只改最小条件；删除旧token、AST、IR、汇编、目标文件与可执行文件，以同一输入重跑，直到“每个选择点有可解释前缀，合法串唯一选择且非法串在最早位置失败”恢复且文法选择点清单可由另一位读者复现。
</Answer>

</Exercises>

<Glossary>
  <GlossaryItem term="production → expansion choice → LOOKAHEAD → action">
    production → expansion choice → LOOKAHEAD → action是第5章
    基于JavaCC的解析器描述连接冻结输入与可推翻编译结论的章专属坐标，必须记录源码、工具版本、目标与原始产物。
  </GlossaryItem>
  <GlossaryItem term="前看深度">
    前看深度是第5章
    基于JavaCC的解析器描述连接产出结构与可推翻编译结论的章专属坐标，必须记录源码、工具版本、目标与原始产物。
  </GlossaryItem>
  <GlossaryItem term="产生式族">
    产生式族是第5章
    基于JavaCC的解析器描述连接断言不变量与可推翻编译结论的章专属坐标，必须记录源码、工具版本、目标与原始产物。
  </GlossaryItem>
  <GlossaryItem term="LOOKAHEAD轨迹与ParseException">
    LOOKAHEAD轨迹与ParseException是第5章
    基于JavaCC的解析器描述连接触发首错与可推翻编译结论的章专属坐标，必须记录源码、工具版本、目标与原始产物。
  </GlossaryItem>
  <GlossaryItem term="文法选择点清单">
    文法选择点清单是第5章
    基于JavaCC的解析器描述连接清理重建与可推翻编译结论的章专属坐标，必须记录源码、工具版本、目标与原始产物。
  </GlossaryItem>
</Glossary>

<Attribution
  mode="independent-rewrite"
  sourceBasis="outline-only"
  workTitle="青木峰郎《ふつうのコンパイラをつくろう》/ 中文版《自制编译器》"
  adaptedUrl="https://i.loveruby.net/ja/stdcompiler/toc.html"
/>
