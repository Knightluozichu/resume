---
title: 第12章 x86架构的概要
type: C
section: 第3部分 · x86与调用约定
order: 12
description: >-
  第12章
  x86架构的概要围绕计算机的系统结构、x86系列CPU的历史、IA-32的概要、数据的表现形式和格式推进cbc，实现从C♭源代码到中间产物、机器代码或运行时证据的可复查转换。
  本页覆盖6个正式目录层级，以章专属编译探针、首错定位和清理重建验收。
demo: true
math: false
sourceUrl: "https://i.loveruby.net/ja/stdcompiler/toc.html"
draft: false
qualityVersion: 2
practiceMode: code
sourceMode: independent-rewrite
---

import {
  Crc12X86OverviewMapLab,
  Crc12X86OverviewExperimentLab,
  Crc12X86OverviewEvidenceLab,
} from "@/components/mdx/crafting-compiler/diagrams/crc-12-x86-overview";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第12章 x86架构的概要全部6个正式目录节点在C♭到进程链中的输入、输出和时代边界
- 能运行“IA-32数据与寄存器台”，一次只改变数据宽度或存放位置
- 能修改章专属C♭、JavaCC、IR、IA-32汇编或ELF样本，并保存最早结构化分叉
- 能触发“把x86-64寄存器和ABI默认值混入IA-32主线”，清理派生产物后证明“操作数宽度、符号扩展、对齐和寄存器破坏符合冻结的IA-32目标”

</Objectives>

{/* CRAFTING_COMPILER_QUALITY_V2 */}

## 为什么从“IA-32数据与寄存器台”开始

第12章 x86架构的概要沿“C♭type → size/alignment → register/memory operand”转换信息。最终进程返回正确值只能证明一条执行路径；IA-32数据与寄存器台保存token、AST、绑定、类型、IR、汇编、符号、重定位或加载轨迹中的适用层，让错误停在第一处分叉。

第12章 x86架构的概要固定C♭源码、cbc提交、JDK与JavaCC、GNU as/ld/binutils、IA-32目标、链接参数和运行环境，只改变数据宽度或存放位置。本页交互只移动C♭type → size/alignment → register/memory operand中的真实对象和状态，不生成置信、风险或证据分数。

## 来源、版本与独立重写边界

第12章 x86架构的概要以青木峰郎维护的[日文原版完整详细目录](https://i.loveruby.net/ja/stdcompiler/toc.html)限定4部分、22章、86个编号小节、附录和116个正式层级；本站未取得原书正文授权，中文解释、代码、图示、实验和答案均为独立教学重写。

第12章 x86架构的概要用[JavaCC完整语法文档](https://javacc.github.io/javacc/documentation/grammar.html)、[ELF规范](https://refspecs.linuxfoundation.org/elf/elf.pdf)、GNU [readelf](https://sourceware.org/binutils/docs/binutils/readelf.html)、[as](https://sourceware.org/binutils/docs/as/)与[ld](https://sourceware.org/binutils/docs/ld/)文档核对技术事实。原书主线是JavaCC、Linux/IA-32、GNU工具与ELF；现代JDK、x86-64、PIE和新版binutils默认值必须单独记录，不能改写原书时代坐标。

## 本章合同与可运行探针

<Term def="C♭type → size/alignment → register/memory operand是第12章 x86架构的概要连接冻结输入与可推翻编译结论的章专属坐标，必须记录源码、工具版本、目标与原始产物。">C♭type → size/alignment → register/memory operand</Term>、<Term def="数据宽度是第12章 x86架构的概要连接产出结构与可推翻编译结论的章专属坐标，必须记录源码、工具版本、目标与原始产物。">数据宽度</Term>、<Term def="存放位置是第12章 x86架构的概要连接断言不变量与可推翻编译结论的章专属坐标，必须记录源码、工具版本、目标与原始产物。">存放位置</Term>、<Term def="编码字节、寄存器读写与宽度是第12章 x86架构的概要连接触发首错与可推翻编译结论的章专属坐标，必须记录源码、工具版本、目标与原始产物。">编码字节、寄存器读写与宽度</Term>、<Term def="IA-32目标契约是第12章 x86架构的概要连接清理重建与可推翻编译结论的章专属坐标，必须记录源码、工具版本、目标与原始产物。">IA-32目标契约</Term>。

第12章 x86架构的概要的通过不变量是“操作数宽度、符号扩展、对齐和寄存器破坏符合冻结的IA-32目标”。实验档案保存源码与期望、cbc提交、工具版本、目标ABI、命令与退出码、所有适用中间产物、标准错误、第一处分叉、清理记录和回归结果。

```bash
as --32 -o probe.o probe.s
objdump -drwC -Mintel probe.o
```

<Callout type="warning" title="历史IA-32工具链必须隔离">
  第12章
  x86架构的概要涉及旧JDK、旧JavaCC、32位链接器、任意目标文件或加载器变量时，在容器或离线虚拟机中运行；固定资源与超时，禁止执行不可信二进制，先检查ELF架构、入口和依赖。
</Callout>

## 先预测，再操作三层实验

<Stepper>
  <Step title="1. 结构与所有权">
    选择目录节点，标出生产者、数据结构、消费者和禁止越层的结论。
    <Crc12X86OverviewMapLab />
  </Step>
  <Step title="2. 单变量转换">
    固定工具链与样本，只切换数据宽度或存放位置。
    <Crc12X86OverviewExperimentLab />
  </Step>
  <Step title="3. 首错与重建">
    注入“把x86-64寄存器和ABI默认值混入IA-32主线”，保存首处分叉，清理后从源码重建。
    <Crc12X86OverviewEvidenceLab />
  </Step>
</Stepper>

## 官方目录逐项深读

### 第3部分 汇编代码

**四级证据 1/6。** 机器槽1把“第3部分 汇编代码”落实到IA-32宽度、寄存器、栈效应、调用约定与反汇编，用哨兵和差分样本验证；固定存放位置，只改变数据宽度，以编码字节、寄存器读写与宽度找到第一处分叉并清理重建。

### 第12章 x86架构的概要

**四级证据 2/6。** 机器槽2把“第12章 x86架构的概要”落实到IA-32宽度、寄存器、栈效应、调用约定与反汇编，用哨兵和差分样本验证；固定存放位置，只改变数据宽度，以编码字节、寄存器读写与宽度找到第一处分叉并清理重建。

### 12.1 计算机的系统结构

**四级证据 3/6。** 流水线槽3将“12.1 计算机的系统结构”映射为输入、结构化产物、错误合同、第一处分叉和清理后重建断言；固定存放位置，只改变数据宽度，以编码字节、寄存器读写与宽度找到第一处分叉并清理重建。

### 12.2 x86系列CPU的历史

**四级证据 4/6。** 机器槽4把“12.2 x86系列CPU的历史”落实到IA-32宽度、寄存器、栈效应、调用约定与反汇编，用哨兵和差分样本验证；固定存放位置，只改变数据宽度，以编码字节、寄存器读写与宽度找到第一处分叉并清理重建。

### 12.3 IA-32的概要

**四级证据 5/6。** 流水线槽5将“12.3 IA-32的概要”映射为输入、结构化产物、错误合同、第一处分叉和清理后重建断言；固定存放位置，只改变数据宽度，以编码字节、寄存器读写与宽度找到第一处分叉并清理重建。

### 12.4 数据的表现形式和格式

**四级证据 6/6。** 流水线槽6将“12.4 数据的表现形式和格式”映射为输入、结构化产物、错误合同、第一处分叉和清理后重建断言；固定存放位置，只改变数据宽度，以编码字节、寄存器读写与宽度找到第一处分叉并清理重建。

## 三个必须主动触发的误区

<Callout type="trap" title="中间产物不能省略">
  第12章
  x86架构的概要不允许用最终返回值平均掉token、AST、绑定、类型、IR、汇编或ELF层失败；缺少适用中间证据即退回。
</Callout>

<Callout type="trap" title="章专属失败样本">
  第12章
  x86架构的概要主动触发“架构历史说明不能替代具体ABI合同”，再注入“把x86-64寄存器和ABI默认值混入IA-32主线”。若编码字节、寄存器读写与宽度没有变化，先验证阳性对照和采集链，不能复用旧构建。
</Callout>

<Callout type="trap" title="机制与现代默认值分开">
  第12章
  x86架构的概要的IA-32、非PIE或旧工具行为必须显式冻结；x86-64、PIE、不同JavaCC/JDK和binutils版本的结果只能作为带版本的对照。
</Callout>

## 练习、答案与116层级验证

<Exercises>

**问题1：因果隔离。** 怎样测量数据宽度对编码字节、寄存器读写与宽度的影响？

<Answer>
  第12章
  x86架构的概要固定源码、cbc提交、目标、存放位置和工具链，只把数据宽度从“16位”切到“32位”；比较首个结构化差分、退出码和下游停止位置，再复位并重建基线。
</Answer>

**问题2：四级覆盖。** 怎样证明本页6个目录节点不是标题复述？

<Answer>
  1. 第3部分 汇编代码：机器槽1把“第3部分
  汇编代码”落实到IA-32宽度、寄存器、栈效应、调用约定与反汇编，用哨兵和差分样本验证；用编码字节、寄存器读写与宽度断言。
  2. 第12章 x86架构的概要：机器槽2把“第12章
  x86架构的概要”落实到IA-32宽度、寄存器、栈效应、调用约定与反汇编，用哨兵和差分样本验证；用IA-32目标契约断言。
  3. 12.1 计算机的系统结构：流水线槽3将“12.1
  计算机的系统结构”映射为输入、结构化产物、错误合同、第一处分叉和清理后重建断言；用编码字节、寄存器读写与宽度断言。
  4. 12.2 x86系列CPU的历史：机器槽4把“12.2
  x86系列CPU的历史”落实到IA-32宽度、寄存器、栈效应、调用约定与反汇编，用哨兵和差分样本验证；用IA-32目标契约断言。
  5. 12.3 IA-32的概要：流水线槽5将“12.3
  IA-32的概要”映射为输入、结构化产物、错误合同、第一处分叉和清理后重建断言；用编码字节、寄存器读写与宽度断言。
  6. 12.4 数据的表现形式和格式：流水线槽6将“12.4
  数据的表现形式和格式”映射为输入、结构化产物、错误合同、第一处分叉和清理后重建断言；用IA-32目标契约断言。
</Answer>

**问题3：修复证明。** 怎样证明“把x86-64寄存器和ABI默认值混入IA-32主线”已经修复？

<Answer>
  第12章
  x86架构的概要沿冻结输入、产出结构、断言不变量、触发首错、清理重建定位首错，只改最小条件；删除旧token、AST、IR、汇编、目标文件与可执行文件，以同一输入重跑，直到“操作数宽度、符号扩展、对齐和寄存器破坏符合冻结的IA-32目标”恢复且IA-32目标契约可由另一位读者复现。
</Answer>

</Exercises>

<Glossary>
  <GlossaryItem term="C♭type → size/alignment → register/memory operand">
    C♭type → size/alignment → register/memory operand是第12章
    x86架构的概要连接冻结输入与可推翻编译结论的章专属坐标，必须记录源码、工具版本、目标与原始产物。
  </GlossaryItem>
  <GlossaryItem term="数据宽度">
    数据宽度是第12章
    x86架构的概要连接产出结构与可推翻编译结论的章专属坐标，必须记录源码、工具版本、目标与原始产物。
  </GlossaryItem>
  <GlossaryItem term="存放位置">
    存放位置是第12章
    x86架构的概要连接断言不变量与可推翻编译结论的章专属坐标，必须记录源码、工具版本、目标与原始产物。
  </GlossaryItem>
  <GlossaryItem term="编码字节、寄存器读写与宽度">
    编码字节、寄存器读写与宽度是第12章
    x86架构的概要连接触发首错与可推翻编译结论的章专属坐标，必须记录源码、工具版本、目标与原始产物。
  </GlossaryItem>
  <GlossaryItem term="IA-32目标契约">
    IA-32目标契约是第12章
    x86架构的概要连接清理重建与可推翻编译结论的章专属坐标，必须记录源码、工具版本、目标与原始产物。
  </GlossaryItem>
</Glossary>

<Attribution
  mode="independent-rewrite"
  sourceBasis="outline-only"
  workTitle="青木峰郎《ふつうのコンパイラをつくろう》/ 中文版《自制编译器》"
  adaptedUrl="https://i.loveruby.net/ja/stdcompiler/toc.html"
/>
