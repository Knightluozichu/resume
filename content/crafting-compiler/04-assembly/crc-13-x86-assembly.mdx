---
title: 第13章 x86汇编器编程
type: C
section: 第3部分 · x86与调用约定
order: 13
description: >-
  第13章
  x86汇编器编程围绕基于GNU汇编器的编程、GNU汇编器的语法、传输指令、算术运算指令、位运算指令、流程的控制推进cbc，实现从C♭源代码到中间产物、机器代码或运行时证据的可复查转换。
  本页覆盖7个正式目录层级，以章专属编译探针、首错定位和清理重建验收。
demo: true
math: false
sourceUrl: "https://i.loveruby.net/ja/stdcompiler/toc.html"
draft: true
qualityVersion: 2
practiceMode: code
sourceMode: independent-rewrite
---

import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第13章 x86汇编器编程全部7个正式目录节点在C♭到进程链中的输入、输出和时代边界
- 能运行“GNU as指令编码台”，一次只改变指令族或操作数边界
- 能修改章专属C♭、JavaCC、IR、IA-32汇编或ELF样本，并保存最早结构化分叉
- 能触发“源/目的次序或宽度后缀错误却因测试值偶然相同”，清理派生产物后证明“汇编、反汇编、标志位和预期状态逐指令一致”

</Objectives>

{/* CRAFTING_COMPILER_QUALITY_V2 */}

## 为什么从“GNU as指令编码台”开始

第13章 x86汇编器编程沿“AT&T syntax → instruction/operand → machine bytes → flags”转换信息。最终进程返回正确值只能证明一条执行路径；GNU as指令编码台保存token、AST、绑定、类型、IR、汇编、符号、重定位或加载轨迹中的适用层，让错误停在第一处分叉。

第13章 x86汇编器编程固定C♭源码、cbc提交、JDK与JavaCC、GNU as/ld/binutils、IA-32目标、链接参数和运行环境，只改变指令族或操作数边界。本页交互只移动AT&T syntax → instruction/operand → machine bytes → flags中的真实对象和状态，不生成置信、风险或证据分数。

## 来源、版本与独立重写边界

第13章 x86汇编器编程以青木峰郎维护的[日文原版完整详细目录](https://i.loveruby.net/ja/stdcompiler/toc.html)限定4部分、22章、86个编号小节、附录和116个正式层级；本站未取得原书正文授权，中文解释、代码、图示、实验和答案均为独立教学重写。

第13章 x86汇编器编程用[JavaCC完整语法文档](https://javacc.github.io/javacc/documentation/grammar.html)、[ELF规范](https://refspecs.linuxfoundation.org/elf/elf.pdf)、GNU [readelf](https://sourceware.org/binutils/docs/binutils/readelf.html)、[as](https://sourceware.org/binutils/docs/as/)与[ld](https://sourceware.org/binutils/docs/ld/)文档核对技术事实。原书主线是JavaCC、Linux/IA-32、GNU工具与ELF；现代JDK、x86-64、PIE和新版binutils默认值必须单独记录，不能改写原书时代坐标。

## 本章合同与可运行探针

<Term def="AT&T syntax → instruction/operand → machine bytes → flags是第13章 x86汇编器编程连接冻结输入与可推翻编译结论的章专属坐标，必须记录源码、工具版本、目标与原始产物。">AT&T syntax → instruction/operand → machine bytes → flags</Term>、<Term def="指令族是第13章 x86汇编器编程连接产出结构与可推翻编译结论的章专属坐标，必须记录源码、工具版本、目标与原始产物。">指令族</Term>、<Term def="操作数边界是第13章 x86汇编器编程连接断言不变量与可推翻编译结论的章专属坐标，必须记录源码、工具版本、目标与原始产物。">操作数边界</Term>、<Term def="机器码、操作数与EFLAGS是第13章 x86汇编器编程连接触发首错与可推翻编译结论的章专属坐标，必须记录源码、工具版本、目标与原始产物。">机器码、操作数与EFLAGS</Term>、<Term def="指令级对照表是第13章 x86汇编器编程连接清理重建与可推翻编译结论的章专属坐标，必须记录源码、工具版本、目标与原始产物。">指令级对照表</Term>。

第13章 x86汇编器编程的通过不变量是“汇编、反汇编、标志位和预期状态逐指令一致”。实验档案保存源码与期望、cbc提交、工具版本、目标ABI、命令与退出码、所有适用中间产物、标准错误、第一处分叉、清理记录和回归结果。

```bash
as --32 -o probe.o probe.s
objdump -drwC probe.o
```

<Callout type="warning" title="历史IA-32工具链必须隔离">
  第13章
  x86汇编器编程涉及旧JDK、旧JavaCC、32位链接器、任意目标文件或加载器变量时，在容器或离线虚拟机中运行；固定资源与超时，禁止执行不可信二进制，先检查ELF架构、入口和依赖。
</Callout>

## 官方目录逐项深读

### 第13章 x86汇编器编程

**四级证据 1/7。** 机器槽1把“第13章 x86汇编器编程”落实到IA-32宽度、寄存器、栈效应、调用约定与反汇编，用哨兵和差分样本验证；固定操作数边界，只改变指令族，以机器码、操作数与EFLAGS找到第一处分叉并清理重建。

### 13.1 基于GNU汇编器的编程

**四级证据 2/7。** 机器槽2把“13.1 基于GNU汇编器的编程”落实到IA-32宽度、寄存器、栈效应、调用约定与反汇编，用哨兵和差分样本验证；固定操作数边界，只改变指令族，以机器码、操作数与EFLAGS找到第一处分叉并清理重建。

### 13.2 GNU汇编器的语法

**四级证据 3/7。** 前端槽3用合法、最长匹配、歧义、截断和非法字符样本核对“13.2 GNU汇编器的语法”的token、前看、树形与源位置；固定操作数边界，只改变指令族，以机器码、操作数与EFLAGS找到第一处分叉并清理重建。

### 13.3 传输指令

**四级证据 4/7。** 机器槽4把“13.3 传输指令”落实到IA-32宽度、寄存器、栈效应、调用约定与反汇编，用哨兵和差分样本验证；固定操作数边界，只改变指令族，以机器码、操作数与EFLAGS找到第一处分叉并清理重建。

### 13.4 算术运算指令

**四级证据 5/7。** 机器槽5把“13.4 算术运算指令”落实到IA-32宽度、寄存器、栈效应、调用约定与反汇编，用哨兵和差分样本验证；固定操作数边界，只改变指令族，以机器码、操作数与EFLAGS找到第一处分叉并清理重建。

### 13.5 位运算指令

**四级证据 6/7。** 机器槽6把“13.5 位运算指令”落实到IA-32宽度、寄存器、栈效应、调用约定与反汇编，用哨兵和差分样本验证；固定操作数边界，只改变指令族，以机器码、操作数与EFLAGS找到第一处分叉并清理重建。

### 13.6 流程的控制

**四级证据 7/7。** 流水线槽7将“13.6 流程的控制”映射为输入、结构化产物、错误合同、第一处分叉和清理后重建断言；固定操作数边界，只改变指令族，以机器码、操作数与EFLAGS找到第一处分叉并清理重建。

## 三个必须主动触发的误区

<Callout type="trap" title="中间产物不能省略">
  第13章
  x86汇编器编程不允许用最终返回值平均掉token、AST、绑定、类型、IR、汇编或ELF层失败；缺少适用中间证据即退回。
</Callout>

<Callout type="trap" title="章专属失败样本">
  第13章
  x86汇编器编程主动触发“汇编器接受指令不证明它实现了预期语义”，再注入“源/目的次序或宽度后缀错误却因测试值偶然相同”。若机器码、操作数与EFLAGS没有变化，先验证阳性对照和采集链，不能复用旧构建。
</Callout>

<Callout type="trap" title="机制与现代默认值分开">
  第13章
  x86汇编器编程的IA-32、非PIE或旧工具行为必须显式冻结；x86-64、PIE、不同JavaCC/JDK和binutils版本的结果只能作为带版本的对照。
</Callout>

## 练习、答案与116层级验证

<Exercises>

**问题1：因果隔离。** 怎样测量指令族对机器码、操作数与EFLAGS的影响？

<Answer>
  第13章
  x86汇编器编程固定源码、cbc提交、目标、操作数边界和工具链，只把指令族从“算术/位运算”切到“控制转移”；比较首个结构化差分、退出码和下游停止位置，再复位并重建基线。
</Answer>

**问题2：四级覆盖。** 怎样证明本页7个目录节点不是标题复述？

<Answer>
  1. 第13章 x86汇编器编程：机器槽1把“第13章
  x86汇编器编程”落实到IA-32宽度、寄存器、栈效应、调用约定与反汇编，用哨兵和差分样本验证；用机器码、操作数与EFLAGS断言。
  2. 13.1 基于GNU汇编器的编程：机器槽2把“13.1
  基于GNU汇编器的编程”落实到IA-32宽度、寄存器、栈效应、调用约定与反汇编，用哨兵和差分样本验证；用指令级对照表断言。
  3. 13.2
  GNU汇编器的语法：前端槽3用合法、最长匹配、歧义、截断和非法字符样本核对“13.2
  GNU汇编器的语法”的token、前看、树形与源位置；用机器码、操作数与EFLAGS断言。 4.
  13.3 传输指令：机器槽4把“13.3
  传输指令”落实到IA-32宽度、寄存器、栈效应、调用约定与反汇编，用哨兵和差分样本验证；用指令级对照表断言。
  5. 13.4 算术运算指令：机器槽5把“13.4
  算术运算指令”落实到IA-32宽度、寄存器、栈效应、调用约定与反汇编，用哨兵和差分样本验证；用机器码、操作数与EFLAGS断言。
  6. 13.5 位运算指令：机器槽6把“13.5
  位运算指令”落实到IA-32宽度、寄存器、栈效应、调用约定与反汇编，用哨兵和差分样本验证；用指令级对照表断言。
  7. 13.6 流程的控制：流水线槽7将“13.6
  流程的控制”映射为输入、结构化产物、错误合同、第一处分叉和清理后重建断言；用机器码、操作数与EFLAGS断言。
</Answer>

**问题3：修复证明。** 怎样证明“源/目的次序或宽度后缀错误却因测试值偶然相同”已经修复？

<Answer>
  第13章
  x86汇编器编程沿冻结输入、产出结构、断言不变量、触发首错、清理重建定位首错，只改最小条件；删除旧token、AST、IR、汇编、目标文件与可执行文件，以同一输入重跑，直到“汇编、反汇编、标志位和预期状态逐指令一致”恢复且指令级对照表可由另一位读者复现。
</Answer>

</Exercises>

<Glossary>
  <GlossaryItem term="AT&T syntax → instruction/operand → machine bytes → flags">
    AT&T syntax → instruction/operand → machine bytes → flags是第13章
    x86汇编器编程连接冻结输入与可推翻编译结论的章专属坐标，必须记录源码、工具版本、目标与原始产物。
  </GlossaryItem>
  <GlossaryItem term="指令族">
    指令族是第13章
    x86汇编器编程连接产出结构与可推翻编译结论的章专属坐标，必须记录源码、工具版本、目标与原始产物。
  </GlossaryItem>
  <GlossaryItem term="操作数边界">
    操作数边界是第13章
    x86汇编器编程连接断言不变量与可推翻编译结论的章专属坐标，必须记录源码、工具版本、目标与原始产物。
  </GlossaryItem>
  <GlossaryItem term="机器码、操作数与EFLAGS">
    机器码、操作数与EFLAGS是第13章
    x86汇编器编程连接触发首错与可推翻编译结论的章专属坐标，必须记录源码、工具版本、目标与原始产物。
  </GlossaryItem>
  <GlossaryItem term="指令级对照表">
    指令级对照表是第13章
    x86汇编器编程连接清理重建与可推翻编译结论的章专属坐标，必须记录源码、工具版本、目标与原始产物。
  </GlossaryItem>
</Glossary>

<Attribution
  mode="independent-rewrite"
  sourceBasis="outline-only"
  workTitle="青木峰郎《ふつうのコンパイラをつくろう》/ 中文版《自制编译器》"
  adaptedUrl="https://i.loveruby.net/ja/stdcompiler/toc.html"
/>
