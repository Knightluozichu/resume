---
title: 第15章 编译表达式和语句
type: C
section: 第3部分 · 代码生成与优化
order: 15
description: >-
  第15章
  编译表达式和语句围绕确认编译结果、x86汇编的对象与DSL、cbc的x86汇编DSL、CodeGenerator类的概要、编译单纯的表达式、编译二元运算推进cbc，实现从C♭源代码到中间产物、机器代码或运行时证据的可复查转换。
  本页覆盖9个正式目录层级，以章专属编译探针、首错定位和清理重建验收。
demo: true
math: false
sourceUrl: "https://i.loveruby.net/ja/stdcompiler/toc.html"
draft: false
qualityVersion: 2
practiceMode: code
sourceMode: independent-rewrite
---

import {
  Crc15CompileExpressionsStatementsMapLab,
  Crc15CompileExpressionsStatementsExperimentLab,
  Crc15CompileExpressionsStatementsEvidenceLab,
} from "@/components/mdx/crafting-compiler/diagrams/crc-15-compile-expressions-statements";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第15章 编译表达式和语句全部9个正式目录节点在C♭到进程链中的输入、输出和时代边界
- 能运行“IR到指令选择台”，一次只改变IR类别或比较基线
- 能修改章专属C♭、JavaCC、IR、IA-32汇编或ELF样本，并保存最早结构化分叉
- 能触发“只比较汇编文本格式或只比较最终返回值”，清理派生产物后证明“每个IR节点选择合法模式，标签闭合且反汇编语义与执行结果一致”

</Objectives>

{/* CRAFTING_COMPILER_QUALITY_V2 */}

## 为什么从“IR到指令选择台”开始

第15章 编译表达式和语句沿“IR node → x86 DSL → labels/operands → assembly”转换信息。最终进程返回正确值只能证明一条执行路径；IR到指令选择台保存token、AST、绑定、类型、IR、汇编、符号、重定位或加载轨迹中的适用层，让错误停在第一处分叉。

第15章 编译表达式和语句固定C♭源码、cbc提交、JDK与JavaCC、GNU as/ld/binutils、IA-32目标、链接参数和运行环境，只改变IR类别或比较基线。本页交互只移动IR node → x86 DSL → labels/operands → assembly中的真实对象和状态，不生成置信、风险或证据分数。

## 来源、版本与独立重写边界

第15章 编译表达式和语句以青木峰郎维护的[日文原版完整详细目录](https://i.loveruby.net/ja/stdcompiler/toc.html)限定4部分、22章、86个编号小节、附录和116个正式层级；本站未取得原书正文授权，中文解释、代码、图示、实验和答案均为独立教学重写。

第15章 编译表达式和语句用[JavaCC完整语法文档](https://javacc.github.io/javacc/documentation/grammar.html)、[ELF规范](https://refspecs.linuxfoundation.org/elf/elf.pdf)、GNU [readelf](https://sourceware.org/binutils/docs/binutils/readelf.html)、[as](https://sourceware.org/binutils/docs/as/)与[ld](https://sourceware.org/binutils/docs/ld/)文档核对技术事实。原书主线是JavaCC、Linux/IA-32、GNU工具与ELF；现代JDK、x86-64、PIE和新版binutils默认值必须单独记录，不能改写原书时代坐标。

## 本章合同与可运行探针

<Term def="IR node → x86 DSL → labels/operands → assembly是第15章 编译表达式和语句连接冻结输入与可推翻编译结论的章专属坐标，必须记录源码、工具版本、目标与原始产物。">IR node → x86 DSL → labels/operands → assembly</Term>、<Term def="IR类别是第15章 编译表达式和语句连接产出结构与可推翻编译结论的章专属坐标，必须记录源码、工具版本、目标与原始产物。">IR类别</Term>、<Term def="比较基线是第15章 编译表达式和语句连接断言不变量与可推翻编译结论的章专属坐标，必须记录源码、工具版本、目标与原始产物。">比较基线</Term>、<Term def="IR—指令对应与运行断言是第15章 编译表达式和语句连接触发首错与可推翻编译结论的章专属坐标，必须记录源码、工具版本、目标与原始产物。">IR—指令对应与运行断言</Term>、<Term def="代码生成差分包是第15章 编译表达式和语句连接清理重建与可推翻编译结论的章专属坐标，必须记录源码、工具版本、目标与原始产物。">代码生成差分包</Term>。

第15章 编译表达式和语句的通过不变量是“每个IR节点选择合法模式，标签闭合且反汇编语义与执行结果一致”。实验档案保存源码与期望、cbc提交、工具版本、目标ABI、命令与退出码、所有适用中间产物、标准错误、第一处分叉、清理记录和回归结果。

```bash
./cbc -S samples/codegen.cb
as --32 -o codegen.o codegen.s
objdump -drwC codegen.o
```

<Callout type="warning" title="历史IA-32工具链必须隔离">
  第15章
  编译表达式和语句涉及旧JDK、旧JavaCC、32位链接器、任意目标文件或加载器变量时，在容器或离线虚拟机中运行；固定资源与超时，禁止执行不可信二进制，先检查ELF架构、入口和依赖。
</Callout>

## 先预测，再操作三层实验

<Stepper>
  <Step title="1. 结构与所有权">
    选择目录节点，标出生产者、数据结构、消费者和禁止越层的结论。
    <Crc15CompileExpressionsStatementsMapLab />
  </Step>
  <Step title="2. 单变量转换">
    固定工具链与样本，只切换IR类别或比较基线。
    <Crc15CompileExpressionsStatementsExperimentLab />
  </Step>
  <Step title="3. 首错与重建">
    注入“只比较汇编文本格式或只比较最终返回值”，保存首处分叉，清理后从源码重建。
    <Crc15CompileExpressionsStatementsEvidenceLab />
  </Step>
</Stepper>

## 官方目录逐项深读

### 第15章 编译表达式和语句

**四级证据 1/9。** 机器槽1把“第15章 编译表达式和语句”落实到IA-32宽度、寄存器、栈效应、调用约定与反汇编，用哨兵和差分样本验证；固定比较基线，只改变IR类别，以IR—指令对应与运行断言找到第一处分叉并清理重建。

### 15.1 确认编译结果

**四级证据 2/9。** 流水线槽2将“15.1 确认编译结果”映射为输入、结构化产物、错误合同、第一处分叉和清理后重建断言；固定比较基线，只改变IR类别，以IR—指令对应与运行断言找到第一处分叉并清理重建。

### 15.2 x86汇编的对象与DSL

**四级证据 3/9。** 机器槽3把“15.2 x86汇编的对象与DSL”落实到IA-32宽度、寄存器、栈效应、调用约定与反汇编，用哨兵和差分样本验证；固定比较基线，只改变IR类别，以IR—指令对应与运行断言找到第一处分叉并清理重建。

### 15.3 cbc的x86汇编DSL

**四级证据 4/9。** 机器槽4把“15.3 cbc的x86汇编DSL”落实到IA-32宽度、寄存器、栈效应、调用约定与反汇编，用哨兵和差分样本验证；固定比较基线，只改变IR类别，以IR—指令对应与运行断言找到第一处分叉并清理重建。

### 15.4 CodeGenerator类的概要

**四级证据 5/9。** 流水线槽5将“15.4 CodeGenerator类的概要”映射为输入、结构化产物、错误合同、第一处分叉和清理后重建断言；固定比较基线，只改变IR类别，以IR—指令对应与运行断言找到第一处分叉并清理重建。

### 15.5 编译单纯的表达式

**四级证据 6/9。** 机器槽6把“15.5 编译单纯的表达式”落实到IA-32宽度、寄存器、栈效应、调用约定与反汇编，用哨兵和差分样本验证；固定比较基线，只改变IR类别，以IR—指令对应与运行断言找到第一处分叉并清理重建。

### 15.6 编译二元运算

**四级证据 7/9。** 流水线槽7将“15.6 编译二元运算”映射为输入、结构化产物、错误合同、第一处分叉和清理后重建断言；固定比较基线，只改变IR类别，以IR—指令对应与运行断言找到第一处分叉并清理重建。

### 15.7 引用变量和赋值

**四级证据 8/9。** 语义槽8为“15.7 引用变量和赋值”保存节点ID、声明绑定、类型/值类别、求值顺序和最小错误，失败后禁止产生伪IR；固定比较基线，只改变IR类别，以IR—指令对应与运行断言找到第一处分叉并清理重建。

### 15.8 编译jump语句

**四级证据 9/9。** 机器槽9把“15.8 编译jump语句”落实到IA-32宽度、寄存器、栈效应、调用约定与反汇编，用哨兵和差分样本验证；固定比较基线，只改变IR类别，以IR—指令对应与运行断言找到第一处分叉并清理重建。

## 三个必须主动触发的误区

<Callout type="trap" title="中间产物不能省略">
  第15章
  编译表达式和语句不允许用最终返回值平均掉token、AST、绑定、类型、IR、汇编或ELF层失败；缺少适用中间证据即退回。
</Callout>

<Callout type="trap" title="章专属失败样本">
  第15章
  编译表达式和语句主动触发“文本相似不等于机器语义相同”，再注入“只比较汇编文本格式或只比较最终返回值”。若IR—指令对应与运行断言没有变化，先验证阳性对照和采集链，不能复用旧构建。
</Callout>

<Callout type="trap" title="机制与现代默认值分开">
  第15章
  编译表达式和语句的IA-32、非PIE或旧工具行为必须显式冻结；x86-64、PIE、不同JavaCC/JDK和binutils版本的结果只能作为带版本的对照。
</Callout>

## 练习、答案与116层级验证

<Exercises>

**问题1：因果隔离。** 怎样测量IR类别对IR—指令对应与运行断言的影响？

<Answer>
  第15章
  编译表达式和语句固定源码、cbc提交、目标、比较基线和工具链，只把IR类别从“赋值”切到“跳转”；比较首个结构化差分、退出码和下游停止位置，再复位并重建基线。
</Answer>

**问题2：四级覆盖。** 怎样证明本页9个目录节点不是标题复述？

<Answer>
  1. 第15章 编译表达式和语句：机器槽1把“第15章
  编译表达式和语句”落实到IA-32宽度、寄存器、栈效应、调用约定与反汇编，用哨兵和差分样本验证；用IR—指令对应与运行断言断言。
  2. 15.1 确认编译结果：流水线槽2将“15.1
  确认编译结果”映射为输入、结构化产物、错误合同、第一处分叉和清理后重建断言；用代码生成差分包断言。
  3. 15.2 x86汇编的对象与DSL：机器槽3把“15.2
  x86汇编的对象与DSL”落实到IA-32宽度、寄存器、栈效应、调用约定与反汇编，用哨兵和差分样本验证；用IR—指令对应与运行断言断言。
  4. 15.3 cbc的x86汇编DSL：机器槽4把“15.3
  cbc的x86汇编DSL”落实到IA-32宽度、寄存器、栈效应、调用约定与反汇编，用哨兵和差分样本验证；用代码生成差分包断言。
  5. 15.4 CodeGenerator类的概要：流水线槽5将“15.4
  CodeGenerator类的概要”映射为输入、结构化产物、错误合同、第一处分叉和清理后重建断言；用IR—指令对应与运行断言断言。
  6. 15.5 编译单纯的表达式：机器槽6把“15.5
  编译单纯的表达式”落实到IA-32宽度、寄存器、栈效应、调用约定与反汇编，用哨兵和差分样本验证；用代码生成差分包断言。
  7. 15.6 编译二元运算：流水线槽7将“15.6
  编译二元运算”映射为输入、结构化产物、错误合同、第一处分叉和清理后重建断言；用IR—指令对应与运行断言断言。
  8. 15.7 引用变量和赋值：语义槽8为“15.7
  引用变量和赋值”保存节点ID、声明绑定、类型/值类别、求值顺序和最小错误，失败后禁止产生伪IR；用代码生成差分包断言。
  9. 15.8 编译jump语句：机器槽9把“15.8
  编译jump语句”落实到IA-32宽度、寄存器、栈效应、调用约定与反汇编，用哨兵和差分样本验证；用IR—指令对应与运行断言断言。
</Answer>

**问题3：修复证明。** 怎样证明“只比较汇编文本格式或只比较最终返回值”已经修复？

<Answer>
  第15章
  编译表达式和语句沿冻结输入、产出结构、断言不变量、触发首错、清理重建定位首错，只改最小条件；删除旧token、AST、IR、汇编、目标文件与可执行文件，以同一输入重跑，直到“每个IR节点选择合法模式，标签闭合且反汇编语义与执行结果一致”恢复且代码生成差分包可由另一位读者复现。
</Answer>

</Exercises>

<Glossary>
  <GlossaryItem term="IR node → x86 DSL → labels/operands → assembly">
    IR node → x86 DSL → labels/operands → assembly是第15章
    编译表达式和语句连接冻结输入与可推翻编译结论的章专属坐标，必须记录源码、工具版本、目标与原始产物。
  </GlossaryItem>
  <GlossaryItem term="IR类别">
    IR类别是第15章
    编译表达式和语句连接产出结构与可推翻编译结论的章专属坐标，必须记录源码、工具版本、目标与原始产物。
  </GlossaryItem>
  <GlossaryItem term="比较基线">
    比较基线是第15章
    编译表达式和语句连接断言不变量与可推翻编译结论的章专属坐标，必须记录源码、工具版本、目标与原始产物。
  </GlossaryItem>
  <GlossaryItem term="IR—指令对应与运行断言">
    IR—指令对应与运行断言是第15章
    编译表达式和语句连接触发首错与可推翻编译结论的章专属坐标，必须记录源码、工具版本、目标与原始产物。
  </GlossaryItem>
  <GlossaryItem term="代码生成差分包">
    代码生成差分包是第15章
    编译表达式和语句连接清理重建与可推翻编译结论的章专属坐标，必须记录源码、工具版本、目标与原始产物。
  </GlossaryItem>
</Glossary>

<Attribution
  mode="independent-rewrite"
  sourceBasis="outline-only"
  workTitle="青木峰郎《ふつうのコンパイラをつくろう》/ 中文版《自制编译器》"
  adaptedUrl="https://i.loveruby.net/ja/stdcompiler/toc.html"
/>
