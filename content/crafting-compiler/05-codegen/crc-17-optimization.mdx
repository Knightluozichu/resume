---
title: "第17章 优化的方法"
type: C
section: "第3部分 · 代码生成与优化"
order: 17
description: "第17章 优化的方法围绕什么是优化、优化的分类、cbc中的优化、更深层的优化推进cbc，实现从C♭源代码到中间产物、机器代码或运行时证据的可复查转换。 覆盖5个正式目录层级。"
demo: true
math: false
sourceUrl: "https://i.loveruby.net/ja/stdcompiler/toc.html"
draft: false
---

import {
  Crc17OptimizationMapLab,
  Crc17OptimizationExperimentLab,
  Crc17OptimizationEvidenceLab,
} from "@/components/mdx/crafting-compiler/diagrams/crc-17-optimization";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第17章 优化的方法在cbc端到端编译链中的职责，并定位每个正式目录项对应的数据结构与产物
- 能描述“冻结语义基线 → 构造CFG → 收集数据流 → 执行局部改写 → 验证守卫 → 基准与差分”各阶段的输入、输出、不变量和首个失败位置
- 能实现或复现本章最小转换，并保存AST、绑定、IR、汇编、ELF或加载器证据
- 能比较正常、边界、失败与回归轨迹并判断“每个优化有前置条件、改写规则和失效反例，优化前后返回值、内存副作用、未定义行为边界与诊断保持一致。”是否成立

</Objectives>

## 为什么不能只看最终程序是否运行

<Term def="什么是优化连接冻结语义基线阶段的输入合同、中间表示、失败位置和下游证据。">
  什么是优化
</Term>
、
<Term def="优化的分类连接构造CFG阶段的输入合同、中间表示、失败位置和下游证据。">
  优化的分类
</Term>
、
<Term def="cbc中的优化连接收集数据流阶段的输入合同、中间表示、失败位置和下游证据。">
  cbc中的优化
</Term>
、
<Term def="更深层的优化连接执行局部改写阶段的输入合同、中间表示、失败位置和下游证据。">
  更深层的优化
</Term>
、
<Term def="优化分类连接验证守卫阶段的输入合同、中间表示、失败位置和下游证据。">
  优化分类
</Term>
共同构成本页坐标。第17章
优化的方法围绕什么是优化、优化的分类、cbc中的优化、更深层的优化推进cbc，实现从C♭源代码到中间产物、机器代码或运行时证据的可复查转换。

可执行文件返回正确值只证明一条样本路径。扫描器可能吞掉字符，解析器可能生成错误结合性的树，引用消解可能绑定到错误作用域，IR可能重复副作用，代码生成器可能碰巧使用未破坏的寄存器，链接器也可能依赖当前地址布局。因此必须保存每层可独立检查的产物。

## cbc端到端实验台账

每章先固定工具链：C♭样例、cbc源码提交、JDK与JavaCC版本、目标三元组、GNU as/ld版本、编译与链接参数。台账从源文件开始，依次记录token、AST、引用与类型绑定、IR、汇编文本、目标文件节/符号/重定位、最终程序头、动态依赖和运行结果。任何结论都要指向第一处分叉，而非只指向最终崩溃。

样本至少分四组：最小合法程序证明主路径，边界程序覆盖空声明、深嵌套、整数宽度、栈对齐与零长度段，故障程序只引入一个未闭合token、二义文法、未定义引用、类型冲突、非法左值、寄存器破坏或未解析符号，回归程序则组合指针运算、函数调用、全局变量与控制流。

诊断对象保留阶段、源位置、实际输入、期望集合、相关声明或符号、根因和恢复建议。前端失败后不得生成伪AST，语义失败后不得继续生成IR，汇编或链接失败后不得以旧目标文件冒充新结果。每次运行前清理生成目录，并把命令、退出状态和原始标准错误一起保存。

对机器层的验证同时从两个方向进行：自顶向下从C♭语义预测IR、寄存器、栈帧和符号；自底向上用objdump、readelf、nm、ldd与加载器调试输出还原实际结果。两条路径在调用约定、数据宽度、重定位类型和段权限上会合，才能证明转换正确。

每次提交附带变更契约：新增或修改哪些token、产生式、节点、语义规则、IR、指令模式、ELF条目或链接行为，哪些旧接口保持不变，失败时如何回退。生成快照可以进入版本比较，但只有解释结构变化并通过全量样例差分后才能更新基线。

正式目录项还要落到可追溯矩阵：每一项至少关联一个输入样本、一条预期不变量、一份原始产物和一个失败对照；跨章节接口则同时记录生产者与消费者。这样既能发现“页面提到了概念却没有验证”的空洞覆盖，也能在cbc实现变化后只重放受影响样本，并从差分证据判断是预期演进还是语义回归。

## 六阶段证据链

### 1. 冻结语义基线

围绕什么是优化保存输入、结构化中间状态与输出。进入“构造CFG”前，先断言当前检查点成立并记录首个偏离。

### 2. 构造CFG

围绕优化的分类保存输入、结构化中间状态与输出。进入“收集数据流”前，先断言当前检查点成立并记录首个偏离。

### 3. 收集数据流

围绕cbc中的优化保存输入、结构化中间状态与输出。进入“执行局部改写”前，先断言当前检查点成立并记录首个偏离。

### 4. 执行局部改写

围绕更深层的优化保存输入、结构化中间状态与输出。进入“验证守卫”前，先断言当前检查点成立并记录首个偏离。

### 5. 验证守卫

围绕优化分类保存输入、结构化中间状态与输出。进入“基准与差分”前，先断言当前检查点成立并记录首个偏离。

### 6. 基准与差分

围绕什么是优化保存输入、结构化中间状态与输出。进入“冻结语义基线”前，先断言当前检查点成立并记录首个偏离。

## 可运行骨架

```text
x = 2 * 8  -> x = 16
y = x + 0  -> y = x
only when volatility and overflow contracts permit
```

```bash
cbc -O0 -S bench.cb -o baseline.s
cbc -O1 -S bench.cb -o optimized.s
diff -u baseline.result optimized.result
```

```text
basic blocks -> CFG edges -> reaching definitions -> guarded rewrite
```

<Callout type="trap">
  **平台边界：** 本书以JavaCC、Linux/IA-32、GNU汇编器和ELF为主线。现代x86-64
  ABI、PIE默认值、JDK模块和新版binutils可能改变命令或产物；先记录版本并区分书中机制与当前平台默认值。
</Callout>

## 正式目录逐项讲解

### 第17章 优化的方法

“第17章 优化的方法”要求先冻结可观察语义和未定义行为边界，再写前置条件、改写规则、反例、差分结果和性能测量。在本章中，“什么是优化”服务于“冻结语义基线”：固定C♭源码、cbc版本、Java/Javacc版本、x86目标与链接参数，同时保存结构化中间产物、标准错误和退出状态。

验证只改变一个token、产生式、声明、类型、IR节点、寄存器约束、符号可见性或装载地址，并在进入“构造CFG”前停住第一条AST、绑定、IR、汇编、ELF或运行轨迹分叉。修复后以同一输入重建全部下游产物，不能只手工修改最终汇编或可执行文件。

### 17.1 什么是优化

“17.1 什么是优化”要求先冻结可观察语义和未定义行为边界，再写前置条件、改写规则、反例、差分结果和性能测量。在本章中，“优化的分类”服务于“构造CFG”：固定C♭源码、cbc版本、Java/Javacc版本、x86目标与链接参数，同时保存结构化中间产物、标准错误和退出状态。

验证只改变一个token、产生式、声明、类型、IR节点、寄存器约束、符号可见性或装载地址，并在进入“收集数据流”前停住第一条AST、绑定、IR、汇编、ELF或运行轨迹分叉。修复后以同一输入重建全部下游产物，不能只手工修改最终汇编或可执行文件。

### 17.2 优化的分类

“17.2 优化的分类”要求先冻结可观察语义和未定义行为边界，再写前置条件、改写规则、反例、差分结果和性能测量。在本章中，“cbc中的优化”服务于“收集数据流”：固定C♭源码、cbc版本、Java/Javacc版本、x86目标与链接参数，同时保存结构化中间产物、标准错误和退出状态。

验证只改变一个token、产生式、声明、类型、IR节点、寄存器约束、符号可见性或装载地址，并在进入“执行局部改写”前停住第一条AST、绑定、IR、汇编、ELF或运行轨迹分叉。修复后以同一输入重建全部下游产物，不能只手工修改最终汇编或可执行文件。

### 17.3 cbc中的优化

“17.3 cbc中的优化”要求先冻结可观察语义和未定义行为边界，再写前置条件、改写规则、反例、差分结果和性能测量。在本章中，“更深层的优化”服务于“执行局部改写”：固定C♭源码、cbc版本、Java/Javacc版本、x86目标与链接参数，同时保存结构化中间产物、标准错误和退出状态。

验证只改变一个token、产生式、声明、类型、IR节点、寄存器约束、符号可见性或装载地址，并在进入“验证守卫”前停住第一条AST、绑定、IR、汇编、ELF或运行轨迹分叉。修复后以同一输入重建全部下游产物，不能只手工修改最终汇编或可执行文件。

### 17.4 更深层的优化

“17.4 更深层的优化”要求先冻结可观察语义和未定义行为边界，再写前置条件、改写规则、反例、差分结果和性能测量。在本章中，“优化分类”服务于“验证守卫”：固定C♭源码、cbc版本、Java/Javacc版本、x86目标与链接参数，同时保存结构化中间产物、标准错误和退出状态。

验证只改变一个token、产生式、声明、类型、IR节点、寄存器约束、符号可见性或装载地址，并在进入“基准与差分”前停住第一条AST、绑定、IR、汇编、ELF或运行轨迹分叉。修复后以同一输入重建全部下游产物，不能只手工修改最终汇编或可执行文件。

## 分步视觉验证

先预测中间产物，再运行转换；若不同，停在最早的AST、绑定、IR、汇编或ELF分叉。

<Stepper>
  <Step title="画编译产物链">
    先预测什么是优化、优化的分类、cbc中的优化、更深层的优化、优化分类的输入、输出、所有权和下游消费者。
    <Crc17OptimizationMapLab />
  </Step>
  <Step title="运行单变量编译">
    固定工具链和C♭样例，只改变一个前端、语义、机器或链接条件。
    <Crc17OptimizationExperimentLab />
  </Step>
  <Step title="核对第一处分叉">
    保存原始中间产物，清理后从源文件重建并重放历史样本。
    <Crc17OptimizationEvidenceLab />
  </Step>
</Stepper>

## 常见误区

<Callout type="trap">
  **误区一：**
  只保留最终汇编或可执行文件。没有AST、语义绑定、IR、符号和重定位，无法判断错误属于哪一层。
</Callout>

<Callout type="trap">
  **误区二：**
  把IA-32示例直接外推到x86-64，或把当前gcc/ld默认PIE行为当作C♭语言与ELF标准保证。
</Callout>

## 四类样本与验收

| 样本 | 只改变什么                  | 必查证据               | 通过条件     |
| ---- | --------------------------- | ---------------------- | ------------ |
| 正常 | 一个最小语言或工具链功能    | 全部中间产物与退出状态 | 与预测一致   |
| 边界 | 宽度、嵌套、对齐或布局      | 第一条边界分支         | 边界可解释   |
| 故障 | 一个token、绑定、类型或符号 | 最早诊断与下游停止     | 不产出伪结果 |
| 回归 | 相同C♭样例集                | AST到进程的差分        | 旧语义保持   |

## 练习

<Exercises>

**问题 1** 为什么不能只凭最终程序运行成功验收第17章 优化的方法？

<Answer>
  最终结果会隐藏错误AST、错误绑定、重复副作用、寄存器破坏、错误重定位和偶然地址布局。必须逐层保存结构化产物并定位第一处分叉。
</Answer>

**问题 2** 如何把“收集数据流”变成可回归实验？

<Answer>
  固定C♭源码、cbc提交、JavaCC、目标ABI和GNU工具版本，预测什么是优化与优化的分类的中间状态，只改变一个条件并保留合法、边界、故障和跨阶段样本。
</Answer>

**问题 3** 本页最小通过条件是什么？

<Answer>
  每个优化有前置条件、改写规则和失效反例，优化前后返回值、内存副作用、未定义行为边界与诊断保持一致。
  修复后必须清理旧产物，从源文件重建，并让全部历史样本回到基线。
</Answer>

</Exercises>

## 小结

- 什么是优化：连接“冻结语义基线”的输入、中间产物、诊断与下游证据。
- 优化的分类：连接“构造CFG”的输入、中间产物、诊断与下游证据。
- cbc中的优化：连接“收集数据流”的输入、中间产物、诊断与下游证据。
- 更深层的优化：连接“执行局部改写”的输入、中间产物、诊断与下游证据。
- 优化分类：连接“验证守卫”的输入、中间产物、诊断与下游证据。
- 本页5个正式目录层级全部进入cbc端到端可复查链。

## 术语表

<Glossary>
  <GlossaryItem term="什么是优化">
    什么是优化用于解释“冻结语义基线”，需要同时指出输入、结构化产物、第一处失败和重建方法。
  </GlossaryItem>
  <GlossaryItem term="优化的分类">
    优化的分类用于解释“构造CFG”，需要同时指出输入、结构化产物、第一处失败和重建方法。
  </GlossaryItem>
  <GlossaryItem term="cbc中的优化">
    cbc中的优化用于解释“收集数据流”，需要同时指出输入、结构化产物、第一处失败和重建方法。
  </GlossaryItem>
  <GlossaryItem term="更深层的优化">
    更深层的优化用于解释“执行局部改写”，需要同时指出输入、结构化产物、第一处失败和重建方法。
  </GlossaryItem>
  <GlossaryItem term="优化分类">
    优化分类用于解释“验证守卫”，需要同时指出输入、结构化产物、第一处失败和重建方法。
  </GlossaryItem>
</Glossary>

<Attribution
  adaptedFrom="青木峰郎著，严圣逸、绝云译《自制编译器》，人民邮电出版社，2016-06，472页，ISBN 9787115422187 · 作者详细目录与中文版完整目录的独立教学重构"
  adaptedUrl="https://i.loveruby.net/ja/stdcompiler/toc.html"
/>
