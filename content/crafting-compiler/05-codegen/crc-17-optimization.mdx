---
title: 第17章 优化的方法
type: C
section: 第3部分 · 代码生成与优化
order: 17
description: >-
  第17章 优化的方法围绕什么是优化、优化的分类、cbc中的优化、更深层的优化推进cbc，实现从C♭源代码到中间产物、机器代码或运行时证据的可复查转换。
  本页覆盖5个正式目录层级，以章专属编译探针、首错定位和清理重建验收。
demo: true
math: false
sourceUrl: "https://i.loveruby.net/ja/stdcompiler/toc.html"
draft: true
qualityVersion: 2
practiceMode: code
sourceMode: independent-rewrite
---

import {
  Crc17OptimizationMapLab,
  Crc17OptimizationExperimentLab,
  Crc17OptimizationEvidenceLab,
} from "@/components/mdx/crafting-compiler/diagrams/crc-17-optimization";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第17章 优化的方法全部5个正式目录节点在C♭到进程链中的输入、输出和时代边界
- 能运行“语义保持优化台”，一次只改变优化层或观察指标
- 能修改章专属C♭、JavaCC、IR、IA-32汇编或ELF样本，并保存最早结构化分叉
- 能触发“删去看似无用但有副作用或未定义行为边界的表达式”，清理派生产物后证明“优化前后可观察语义一致，收益在固定样本与目标上可复现”

</Objectives>

{/* CRAFTING_COMPILER_QUALITY_V2 */}

## 为什么从“语义保持优化台”开始

第17章 优化的方法沿“baseline IR/asm → one transform → differential tests”转换信息。最终进程返回正确值只能证明一条执行路径；语义保持优化台保存token、AST、绑定、类型、IR、汇编、符号、重定位或加载轨迹中的适用层，让错误停在第一处分叉。

第17章 优化的方法固定C♭源码、cbc提交、JDK与JavaCC、GNU as/ld/binutils、IA-32目标、链接参数和运行环境，只改变优化层或观察指标。本页交互只移动baseline IR/asm → one transform → differential tests中的真实对象和状态，不生成置信、风险或证据分数。

## 来源、版本与独立重写边界

第17章 优化的方法以青木峰郎维护的[日文原版完整详细目录](https://i.loveruby.net/ja/stdcompiler/toc.html)限定4部分、22章、86个编号小节、附录和116个正式层级；本站未取得原书正文授权，中文解释、代码、图示、实验和答案均为独立教学重写。

第17章 优化的方法用[JavaCC完整语法文档](https://javacc.github.io/javacc/documentation/grammar.html)、[ELF规范](https://refspecs.linuxfoundation.org/elf/elf.pdf)、GNU [readelf](https://sourceware.org/binutils/docs/binutils/readelf.html)、[as](https://sourceware.org/binutils/docs/as/)与[ld](https://sourceware.org/binutils/docs/ld/)文档核对技术事实。原书主线是JavaCC、Linux/IA-32、GNU工具与ELF；现代JDK、x86-64、PIE和新版binutils默认值必须单独记录，不能改写原书时代坐标。

## 本章合同与可运行探针

<Term def="baseline IR/asm → one transform → differential tests是第17章 优化的方法连接冻结输入与可推翻编译结论的章专属坐标，必须记录源码、工具版本、目标与原始产物。">baseline IR/asm → one transform → differential tests</Term>、<Term def="优化层是第17章 优化的方法连接产出结构与可推翻编译结论的章专属坐标，必须记录源码、工具版本、目标与原始产物。">优化层</Term>、<Term def="观察指标是第17章 优化的方法连接断言不变量与可推翻编译结论的章专属坐标，必须记录源码、工具版本、目标与原始产物。">观察指标</Term>、<Term def="差分结果、代码尺寸与失败样本是第17章 优化的方法连接触发首错与可推翻编译结论的章专属坐标，必须记录源码、工具版本、目标与原始产物。">差分结果、代码尺寸与失败样本</Term>、<Term def="单优化变换证据包是第17章 优化的方法连接清理重建与可推翻编译结论的章专属坐标，必须记录源码、工具版本、目标与原始产物。">单优化变换证据包</Term>。

第17章 优化的方法的通过不变量是“优化前后可观察语义一致，收益在固定样本与目标上可复现”。实验档案保存源码与期望、cbc提交、工具版本、目标ABI、命令与退出码、所有适用中间产物、标准错误、第一处分叉、清理记录和回归结果。

```bash
./regress --baseline no-opt --candidate one-pass
size -A before.o after.o
```

<Callout type="warning" title="历史IA-32工具链必须隔离">
  第17章
  优化的方法涉及旧JDK、旧JavaCC、32位链接器、任意目标文件或加载器变量时，在容器或离线虚拟机中运行；固定资源与超时，禁止执行不可信二进制，先检查ELF架构、入口和依赖。
</Callout>

## 先预测，再操作三层实验

<Stepper>
  <Step title="1. 结构与所有权">
    选择目录节点，标出生产者、数据结构、消费者和禁止越层的结论。
    <Crc17OptimizationMapLab />
  </Step>
  <Step title="2. 单变量转换">
    固定工具链与样本，只切换优化层或观察指标。
    <Crc17OptimizationExperimentLab />
  </Step>
  <Step title="3. 首错与重建">
    注入“删去看似无用但有副作用或未定义行为边界的表达式”，保存首处分叉，清理后从源码重建。
    <Crc17OptimizationEvidenceLab />
  </Step>
</Stepper>

## 官方目录逐项深读

### 第17章 优化的方法

**四级证据 1/5。** 机器槽1把“第17章 优化的方法”落实到IA-32宽度、寄存器、栈效应、调用约定与反汇编，用哨兵和差分样本验证；固定观察指标，只改变优化层，以差分结果、代码尺寸与失败样本找到第一处分叉并清理重建。

### 17.1 什么是优化

**四级证据 2/5。** 机器槽2把“17.1 什么是优化”落实到IA-32宽度、寄存器、栈效应、调用约定与反汇编，用哨兵和差分样本验证；固定观察指标，只改变优化层，以差分结果、代码尺寸与失败样本找到第一处分叉并清理重建。

### 17.2 优化的分类

**四级证据 3/5。** 机器槽3把“17.2 优化的分类”落实到IA-32宽度、寄存器、栈效应、调用约定与反汇编，用哨兵和差分样本验证；固定观察指标，只改变优化层，以差分结果、代码尺寸与失败样本找到第一处分叉并清理重建。

### 17.3 cbc中的优化

**四级证据 4/5。** 机器槽4把“17.3 cbc中的优化”落实到IA-32宽度、寄存器、栈效应、调用约定与反汇编，用哨兵和差分样本验证；固定观察指标，只改变优化层，以差分结果、代码尺寸与失败样本找到第一处分叉并清理重建。

### 17.4 更深层的优化

**四级证据 5/5。** 机器槽5把“17.4 更深层的优化”落实到IA-32宽度、寄存器、栈效应、调用约定与反汇编，用哨兵和差分样本验证；固定观察指标，只改变优化层，以差分结果、代码尺寸与失败样本找到第一处分叉并清理重建。

## 三个必须主动触发的误区

<Callout type="trap" title="中间产物不能省略">
  第17章
  优化的方法不允许用最终返回值平均掉token、AST、绑定、类型、IR、汇编或ELF层失败；缺少适用中间证据即退回。
</Callout>

<Callout type="trap" title="章专属失败样本">
  第17章
  优化的方法主动触发“更短汇编不是正确优化的充分条件”，再注入“删去看似无用但有副作用或未定义行为边界的表达式”。若差分结果、代码尺寸与失败样本没有变化，先验证阳性对照和采集链，不能复用旧构建。
</Callout>

<Callout type="trap" title="机制与现代默认值分开">
  第17章
  优化的方法的IA-32、非PIE或旧工具行为必须显式冻结；x86-64、PIE、不同JavaCC/JDK和binutils版本的结果只能作为带版本的对照。
</Callout>

## 练习、答案与116层级验证

<Exercises>

**问题1：因果隔离。** 怎样测量优化层对差分结果、代码尺寸与失败样本的影响？

<Answer>
  第17章
  优化的方法固定源码、cbc提交、目标、观察指标和工具链，只把优化层从“控制流”切到“机器相关”；比较首个结构化差分、退出码和下游停止位置，再复位并重建基线。
</Answer>

**问题2：四级覆盖。** 怎样证明本页5个目录节点不是标题复述？

<Answer>
  1. 第17章 优化的方法：机器槽1把“第17章
  优化的方法”落实到IA-32宽度、寄存器、栈效应、调用约定与反汇编，用哨兵和差分样本验证；用差分结果、代码尺寸与失败样本断言。
  2. 17.1 什么是优化：机器槽2把“17.1
  什么是优化”落实到IA-32宽度、寄存器、栈效应、调用约定与反汇编，用哨兵和差分样本验证；用单优化变换证据包断言。
  3. 17.2 优化的分类：机器槽3把“17.2
  优化的分类”落实到IA-32宽度、寄存器、栈效应、调用约定与反汇编，用哨兵和差分样本验证；用差分结果、代码尺寸与失败样本断言。
  4. 17.3 cbc中的优化：机器槽4把“17.3
  cbc中的优化”落实到IA-32宽度、寄存器、栈效应、调用约定与反汇编，用哨兵和差分样本验证；用单优化变换证据包断言。
  5. 17.4 更深层的优化：机器槽5把“17.4
  更深层的优化”落实到IA-32宽度、寄存器、栈效应、调用约定与反汇编，用哨兵和差分样本验证；用差分结果、代码尺寸与失败样本断言。
</Answer>

**问题3：修复证明。** 怎样证明“删去看似无用但有副作用或未定义行为边界的表达式”已经修复？

<Answer>
  第17章
  优化的方法沿冻结输入、产出结构、断言不变量、触发首错、清理重建定位首错，只改最小条件；删除旧token、AST、IR、汇编、目标文件与可执行文件，以同一输入重跑，直到“优化前后可观察语义一致，收益在固定样本与目标上可复现”恢复且单优化变换证据包可由另一位读者复现。
</Answer>

</Exercises>

<Glossary>
  <GlossaryItem term="baseline IR/asm → one transform → differential tests">
    baseline IR/asm → one transform → differential tests是第17章
    优化的方法连接冻结输入与可推翻编译结论的章专属坐标，必须记录源码、工具版本、目标与原始产物。
  </GlossaryItem>
  <GlossaryItem term="优化层">
    优化层是第17章
    优化的方法连接产出结构与可推翻编译结论的章专属坐标，必须记录源码、工具版本、目标与原始产物。
  </GlossaryItem>
  <GlossaryItem term="观察指标">
    观察指标是第17章
    优化的方法连接断言不变量与可推翻编译结论的章专属坐标，必须记录源码、工具版本、目标与原始产物。
  </GlossaryItem>
  <GlossaryItem term="差分结果、代码尺寸与失败样本">
    差分结果、代码尺寸与失败样本是第17章
    优化的方法连接触发首错与可推翻编译结论的章专属坐标，必须记录源码、工具版本、目标与原始产物。
  </GlossaryItem>
  <GlossaryItem term="单优化变换证据包">
    单优化变换证据包是第17章
    优化的方法连接清理重建与可推翻编译结论的章专属坐标，必须记录源码、工具版本、目标与原始产物。
  </GlossaryItem>
</Glossary>

<Attribution
  mode="independent-rewrite"
  sourceBasis="outline-only"
  workTitle="青木峰郎《ふつうのコンパイラをつくろう》/ 中文版《自制编译器》"
  adaptedUrl="https://i.loveruby.net/ja/stdcompiler/toc.html"
/>
