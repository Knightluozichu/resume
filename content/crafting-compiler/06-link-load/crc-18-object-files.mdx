---
title: "第18章 生成目标文件"
type: C
section: "第4部分 · 链接和加载"
order: 18
description: "第18章 生成目标文件围绕ELF文件的结构、全局变量及其在ELF文件中的表示、编译全局变量、生成目标文件推进cbc，实现从C♭源代码到中间产物、机器代码或运行时证据的可复查转换。 覆盖6个正式目录层级。"
demo: true
math: false
sourceUrl: "https://i.loveruby.net/ja/stdcompiler/toc.html"
draft: false
---

import {
  Crc18ObjectFilesMapLab,
  Crc18ObjectFilesExperimentLab,
  Crc18ObjectFilesEvidenceLab,
} from "@/components/mdx/crafting-compiler/diagrams/crc-18-object-files";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第18章 生成目标文件在cbc端到端编译链中的职责，并定位每个正式目录项对应的数据结构与产物
- 能描述“列出ELF节 → 布局全局符号 → 生成重定位 → 写入目标文件 → 反汇编节内容 → 核对符号表”各阶段的输入、输出、不变量和首个失败位置
- 能实现或复现本章最小转换，并保存AST、绑定、IR、汇编、ELF或加载器证据
- 能比较正常、边界、失败与回归轨迹并判断“代码、数据、BSS、符号和重定位条目在ELF中相互引用一致，未解析地址保留为正确类型的重定位而不是写入伪地址。”是否成立

</Objectives>

## 为什么不能只看最终程序是否运行

<Term def="ELF文件的结构连接列出ELF节阶段的输入合同、中间表示、失败位置和下游证据。">
  ELF文件的结构
</Term>
、
<Term def="全局变量及其在ELF文件中的表示连接布局全局符号阶段的输入合同、中间表示、失败位置和下游证据。">
  全局变量及其在ELF文件中的表示
</Term>
、
<Term def="编译全局变量连接生成重定位阶段的输入合同、中间表示、失败位置和下游证据。">
  编译全局变量
</Term>
、
<Term def="生成目标文件连接写入目标文件阶段的输入合同、中间表示、失败位置和下游证据。">
  生成目标文件
</Term>
、
<Term def="ELF连接反汇编节内容阶段的输入合同、中间表示、失败位置和下游证据。">
  ELF
</Term>
共同构成本页坐标。第18章
生成目标文件围绕ELF文件的结构、全局变量及其在ELF文件中的表示、编译全局变量、生成目标文件推进cbc，实现从C♭源代码到中间产物、机器代码或运行时证据的可复查转换。

可执行文件返回正确值只证明一条样本路径。扫描器可能吞掉字符，解析器可能生成错误结合性的树，引用消解可能绑定到错误作用域，IR可能重复副作用，代码生成器可能碰巧使用未破坏的寄存器，链接器也可能依赖当前地址布局。因此必须保存每层可独立检查的产物。

## cbc端到端实验台账

每章先固定工具链：C♭样例、cbc源码提交、JDK与JavaCC版本、目标三元组、GNU as/ld版本、编译与链接参数。台账从源文件开始，依次记录token、AST、引用与类型绑定、IR、汇编文本、目标文件节/符号/重定位、最终程序头、动态依赖和运行结果。任何结论都要指向第一处分叉，而非只指向最终崩溃。

样本至少分四组：最小合法程序证明主路径，边界程序覆盖空声明、深嵌套、整数宽度、栈对齐与零长度段，故障程序只引入一个未闭合token、二义文法、未定义引用、类型冲突、非法左值、寄存器破坏或未解析符号，回归程序则组合指针运算、函数调用、全局变量与控制流。

诊断对象保留阶段、源位置、实际输入、期望集合、相关声明或符号、根因和恢复建议。前端失败后不得生成伪AST，语义失败后不得继续生成IR，汇编或链接失败后不得以旧目标文件冒充新结果。每次运行前清理生成目录，并把命令、退出状态和原始标准错误一起保存。

对机器层的验证同时从两个方向进行：自顶向下从C♭语义预测IR、寄存器、栈帧和符号；自底向上用objdump、readelf、nm、ldd与加载器调试输出还原实际结果。两条路径在调用约定、数据宽度、重定位类型和段权限上会合，才能证明转换正确。

每次提交附带变更契约：新增或修改哪些token、产生式、节点、语义规则、IR、指令模式、ELF条目或链接行为，哪些旧接口保持不变，失败时如何回退。生成快照可以进入版本比较，但只有解释结构变化并通过全量样例差分后才能更新基线。

正式目录项还要落到可追溯矩阵：每一项至少关联一个输入样本、一条预期不变量、一份原始产物和一个失败对照；跨章节接口则同时记录生产者与消费者。这样既能发现“页面提到了概念却没有验证”的空洞覆盖，也能在cbc实现变化后只重放受影响样本，并从差分证据判断是预期演进还是语义回归。

## 六阶段证据链

### 1. 列出ELF节

围绕ELF文件的结构保存输入、结构化中间状态与输出。进入“布局全局符号”前，先断言当前检查点成立并记录首个偏离。

### 2. 布局全局符号

围绕全局变量及其在ELF文件中的表示保存输入、结构化中间状态与输出。进入“生成重定位”前，先断言当前检查点成立并记录首个偏离。

### 3. 生成重定位

围绕编译全局变量保存输入、结构化中间状态与输出。进入“写入目标文件”前，先断言当前检查点成立并记录首个偏离。

### 4. 写入目标文件

围绕生成目标文件保存输入、结构化中间状态与输出。进入“反汇编节内容”前，先断言当前检查点成立并记录首个偏离。

### 5. 反汇编节内容

围绕ELF保存输入、结构化中间状态与输出。进入“核对符号表”前，先断言当前检查点成立并记录首个偏离。

### 6. 核对符号表

围绕ELF文件的结构保存输入、结构化中间状态与输出。进入“列出ELF节”前，先断言当前检查点成立并记录首个偏离。

## 可运行骨架

```bash
readelf -h -S -s -r sample.o
objdump -drwC sample.o
```

```text
.text -> machine code
.data -> initialized globals
.bss  -> zero-filled globals
.rel.text -> unresolved addresses
```

```bash
nm -n sample.o
size -A sample.o
```

<Callout type="trap">
  **平台边界：** 本书以JavaCC、Linux/IA-32、GNU汇编器和ELF为主线。现代x86-64
  ABI、PIE默认值、JDK模块和新版binutils可能改变命令或产物；先记录版本并区分书中机制与当前平台默认值。
</Callout>

## 正式目录逐项讲解

### 第4部分 链接和加载

“第4部分 链接和加载”要求沿符号解析、库选择、重定位、段映射、GOT/PLT和入口转移保存原始ELF及加载器轨迹。在本章中，“ELF文件的结构”服务于“列出ELF节”：固定C♭源码、cbc版本、Java/Javacc版本、x86目标与链接参数，同时保存结构化中间产物、标准错误和退出状态。

验证只改变一个token、产生式、声明、类型、IR节点、寄存器约束、符号可见性或装载地址，并在进入“布局全局符号”前停住第一条AST、绑定、IR、汇编、ELF或运行轨迹分叉。修复后以同一输入重建全部下游产物，不能只手工修改最终汇编或可执行文件。

### 第18章 生成目标文件

“第18章 生成目标文件”要求把代码、数据、BSS、符号和重定位映射到ELF节与程序头，并用readelf、nm和objdump交叉检查。在本章中，“全局变量及其在ELF文件中的表示”服务于“布局全局符号”：固定C♭源码、cbc版本、Java/Javacc版本、x86目标与链接参数，同时保存结构化中间产物、标准错误和退出状态。

验证只改变一个token、产生式、声明、类型、IR节点、寄存器约束、符号可见性或装载地址，并在进入“生成重定位”前停住第一条AST、绑定、IR、汇编、ELF或运行轨迹分叉。修复后以同一输入重建全部下游产物，不能只手工修改最终汇编或可执行文件。

### 18.1 ELF文件的结构

“18.1 ELF文件的结构”要求把代码、数据、BSS、符号和重定位映射到ELF节与程序头，并用readelf、nm和objdump交叉检查。在本章中，“编译全局变量”服务于“生成重定位”：固定C♭源码、cbc版本、Java/Javacc版本、x86目标与链接参数，同时保存结构化中间产物、标准错误和退出状态。

验证只改变一个token、产生式、声明、类型、IR节点、寄存器约束、符号可见性或装载地址，并在进入“写入目标文件”前停住第一条AST、绑定、IR、汇编、ELF或运行轨迹分叉。修复后以同一输入重建全部下游产物，不能只手工修改最终汇编或可执行文件。

### 18.2 全局变量及其在ELF文件中的表示

“18.2 全局变量及其在ELF文件中的表示”要求建立作用域和类型表，将每个名称与类型使用绑定到唯一声明，并在IR生成前汇总最小语义诊断。在本章中，“生成目标文件”服务于“写入目标文件”：固定C♭源码、cbc版本、Java/Javacc版本、x86目标与链接参数，同时保存结构化中间产物、标准错误和退出状态。

验证只改变一个token、产生式、声明、类型、IR节点、寄存器约束、符号可见性或装载地址，并在进入“反汇编节内容”前停住第一条AST、绑定、IR、汇编、ELF或运行轨迹分叉。修复后以同一输入重建全部下游产物，不能只手工修改最终汇编或可执行文件。

### 18.3 编译全局变量

“18.3 编译全局变量”要求建立作用域和类型表，将每个名称与类型使用绑定到唯一声明，并在IR生成前汇总最小语义诊断。在本章中，“ELF”服务于“反汇编节内容”：固定C♭源码、cbc版本、Java/Javacc版本、x86目标与链接参数，同时保存结构化中间产物、标准错误和退出状态。

验证只改变一个token、产生式、声明、类型、IR节点、寄存器约束、符号可见性或装载地址，并在进入“核对符号表”前停住第一条AST、绑定、IR、汇编、ELF或运行轨迹分叉。修复后以同一输入重建全部下游产物，不能只手工修改最终汇编或可执行文件。

### 18.4 生成目标文件

“18.4 生成目标文件”要求把代码、数据、BSS、符号和重定位映射到ELF节与程序头，并用readelf、nm和objdump交叉检查。在本章中，“ELF文件的结构”服务于“核对符号表”：固定C♭源码、cbc版本、Java/Javacc版本、x86目标与链接参数，同时保存结构化中间产物、标准错误和退出状态。

验证只改变一个token、产生式、声明、类型、IR节点、寄存器约束、符号可见性或装载地址，并在进入“列出ELF节”前停住第一条AST、绑定、IR、汇编、ELF或运行轨迹分叉。修复后以同一输入重建全部下游产物，不能只手工修改最终汇编或可执行文件。

## 分步视觉验证

先预测中间产物，再运行转换；若不同，停在最早的AST、绑定、IR、汇编或ELF分叉。

<Stepper>
  <Step title="画编译产物链">
    先预测ELF文件的结构、全局变量及其在ELF文件中的表示、编译全局变量、生成目标文件、ELF的输入、输出、所有权和下游消费者。
    <Crc18ObjectFilesMapLab />
  </Step>
  <Step title="运行单变量编译">
    固定工具链和C♭样例，只改变一个前端、语义、机器或链接条件。
    <Crc18ObjectFilesExperimentLab />
  </Step>
  <Step title="核对第一处分叉">
    保存原始中间产物，清理后从源文件重建并重放历史样本。
    <Crc18ObjectFilesEvidenceLab />
  </Step>
</Stepper>

## 常见误区

<Callout type="trap">
  **误区一：**
  只保留最终汇编或可执行文件。没有AST、语义绑定、IR、符号和重定位，无法判断错误属于哪一层。
</Callout>

<Callout type="trap">
  **误区二：**
  把IA-32示例直接外推到x86-64，或把当前gcc/ld默认PIE行为当作C♭语言与ELF标准保证。
</Callout>

## 四类样本与验收

| 样本 | 只改变什么                  | 必查证据               | 通过条件     |
| ---- | --------------------------- | ---------------------- | ------------ |
| 正常 | 一个最小语言或工具链功能    | 全部中间产物与退出状态 | 与预测一致   |
| 边界 | 宽度、嵌套、对齐或布局      | 第一条边界分支         | 边界可解释   |
| 故障 | 一个token、绑定、类型或符号 | 最早诊断与下游停止     | 不产出伪结果 |
| 回归 | 相同C♭样例集                | AST到进程的差分        | 旧语义保持   |

## 练习

<Exercises>

**问题 1** 为什么不能只凭最终程序运行成功验收第18章 生成目标文件？

<Answer>
  最终结果会隐藏错误AST、错误绑定、重复副作用、寄存器破坏、错误重定位和偶然地址布局。必须逐层保存结构化产物并定位第一处分叉。
</Answer>

**问题 2** 如何把“生成重定位”变成可回归实验？

<Answer>
  固定C♭源码、cbc提交、JavaCC、目标ABI和GNU工具版本，预测ELF文件的结构与全局变量及其在ELF文件中的表示的中间状态，只改变一个条件并保留合法、边界、故障和跨阶段样本。
</Answer>

**问题 3** 本页最小通过条件是什么？

<Answer>
  代码、数据、BSS、符号和重定位条目在ELF中相互引用一致，未解析地址保留为正确类型的重定位而不是写入伪地址。
  修复后必须清理旧产物，从源文件重建，并让全部历史样本回到基线。
</Answer>

</Exercises>

## 小结

- ELF文件的结构：连接“列出ELF节”的输入、中间产物、诊断与下游证据。
- 全局变量及其在ELF文件中的表示：连接“布局全局符号”的输入、中间产物、诊断与下游证据。
- 编译全局变量：连接“生成重定位”的输入、中间产物、诊断与下游证据。
- 生成目标文件：连接“写入目标文件”的输入、中间产物、诊断与下游证据。
- ELF：连接“反汇编节内容”的输入、中间产物、诊断与下游证据。
- 本页6个正式目录层级全部进入cbc端到端可复查链。

## 术语表

<Glossary>
  <GlossaryItem term="ELF文件的结构">
    ELF文件的结构用于解释“列出ELF节”，需要同时指出输入、结构化产物、第一处失败和重建方法。
  </GlossaryItem>
  <GlossaryItem term="全局变量及其在ELF文件中的表示">
    全局变量及其在ELF文件中的表示用于解释“布局全局符号”，需要同时指出输入、结构化产物、第一处失败和重建方法。
  </GlossaryItem>
  <GlossaryItem term="编译全局变量">
    编译全局变量用于解释“生成重定位”，需要同时指出输入、结构化产物、第一处失败和重建方法。
  </GlossaryItem>
  <GlossaryItem term="生成目标文件">
    生成目标文件用于解释“写入目标文件”，需要同时指出输入、结构化产物、第一处失败和重建方法。
  </GlossaryItem>
  <GlossaryItem term="ELF">
    ELF用于解释“反汇编节内容”，需要同时指出输入、结构化产物、第一处失败和重建方法。
  </GlossaryItem>
</Glossary>

<Attribution
  adaptedFrom="青木峰郎著，严圣逸、绝云译《自制编译器》，人民邮电出版社，2016-06，472页，ISBN 9787115422187 · 作者详细目录与中文版完整目录的独立教学重构"
  adaptedUrl="https://i.loveruby.net/ja/stdcompiler/toc.html"
/>
