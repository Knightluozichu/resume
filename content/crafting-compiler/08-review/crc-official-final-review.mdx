---
title: "《自制编译器》全书总复习"
type: C
section: "总复习 · C♭到进程"
order: 24
description: "用源代码、AST、语义绑定、IR、汇编、ELF、链接和加载八条证据线贯通cbc的完整编译与执行环境。 覆盖27个正式目录层级。"
demo: true
math: false
sourceUrl: "https://i.loveruby.net/ja/stdcompiler/toc.html"
draft: false
---

import {
  CrcOfficialFinalReviewMapLab,
  CrcOfficialFinalReviewExperimentLab,
  CrcOfficialFinalReviewEvidenceLab,
} from "@/components/mdx/crafting-compiler/diagrams/crc-official-final-review";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释《自制编译器》全书总复习在cbc端到端编译链中的职责，并定位每个正式目录项对应的数据结构与产物
- 能描述“核对4部分 → 完成代码分析 → 生成AST与IR → 生成x86汇编 → 链接加载ELF → 回归C♭程序”各阶段的输入、输出、不变量和首个失败位置
- 能实现或复现本章最小转换，并保存AST、绑定、IR、汇编、ELF或加载器证据
- 能比较正常、边界、失败与回归轨迹并判断“同一C♭程序从源码到进程的每层产物可独立解释，故障停在最早责任层，清理后可由记录完整重建。”是否成立

</Objectives>

## 为什么不能只看最终程序是否运行

<Term def="扫描解析连接核对4部分阶段的输入合同、中间表示、失败位置和下游证据。">
  扫描解析
</Term>
、
<Term def="语义与IR连接完成代码分析阶段的输入合同、中间表示、失败位置和下游证据。">
  语义与IR
</Term>
、
<Term def="x86代码生成连接生成AST与IR阶段的输入合同、中间表示、失败位置和下游证据。">
  x86代码生成
</Term>
、
<Term def="ELF链接连接生成x86汇编阶段的输入合同、中间表示、失败位置和下游证据。">
  ELF链接
</Term>
、
<Term def="加载与PIC连接链接加载ELF阶段的输入合同、中间表示、失败位置和下游证据。">
  加载与PIC
</Term>
共同构成本页坐标。用源代码、AST、语义绑定、IR、汇编、ELF、链接和加载八条证据线贯通cbc的完整编译与执行环境。

可执行文件返回正确值只证明一条样本路径。扫描器可能吞掉字符，解析器可能生成错误结合性的树，引用消解可能绑定到错误作用域，IR可能重复副作用，代码生成器可能碰巧使用未破坏的寄存器，链接器也可能依赖当前地址布局。因此必须保存每层可独立检查的产物。

## cbc端到端实验台账

每章先固定工具链：C♭样例、cbc源码提交、JDK与JavaCC版本、目标三元组、GNU as/ld版本、编译与链接参数。台账从源文件开始，依次记录token、AST、引用与类型绑定、IR、汇编文本、目标文件节/符号/重定位、最终程序头、动态依赖和运行结果。任何结论都要指向第一处分叉，而非只指向最终崩溃。

样本至少分四组：最小合法程序证明主路径，边界程序覆盖空声明、深嵌套、整数宽度、栈对齐与零长度段，故障程序只引入一个未闭合token、二义文法、未定义引用、类型冲突、非法左值、寄存器破坏或未解析符号，回归程序则组合指针运算、函数调用、全局变量与控制流。

诊断对象保留阶段、源位置、实际输入、期望集合、相关声明或符号、根因和恢复建议。前端失败后不得生成伪AST，语义失败后不得继续生成IR，汇编或链接失败后不得以旧目标文件冒充新结果。每次运行前清理生成目录，并把命令、退出状态和原始标准错误一起保存。

对机器层的验证同时从两个方向进行：自顶向下从C♭语义预测IR、寄存器、栈帧和符号；自底向上用objdump、readelf、nm、ldd与加载器调试输出还原实际结果。两条路径在调用约定、数据宽度、重定位类型和段权限上会合，才能证明转换正确。

每次提交附带变更契约：新增或修改哪些token、产生式、节点、语义规则、IR、指令模式、ELF条目或链接行为，哪些旧接口保持不变，失败时如何回退。生成快照可以进入版本比较，但只有解释结构变化并通过全量样例差分后才能更新基线。

正式目录项还要落到可追溯矩阵：每一项至少关联一个输入样本、一条预期不变量、一份原始产物和一个失败对照；跨章节接口则同时记录生产者与消费者。这样既能发现“页面提到了概念却没有验证”的空洞覆盖，也能在cbc实现变化后只重放受影响样本，并从差分证据判断是预期演进还是语义回归。

## 六阶段证据链

### 1. 核对4部分

围绕扫描解析保存输入、结构化中间状态与输出。进入“完成代码分析”前，先断言当前检查点成立并记录首个偏离。

### 2. 完成代码分析

围绕语义与IR保存输入、结构化中间状态与输出。进入“生成AST与IR”前，先断言当前检查点成立并记录首个偏离。

### 3. 生成AST与IR

围绕x86代码生成保存输入、结构化中间状态与输出。进入“生成x86汇编”前，先断言当前检查点成立并记录首个偏离。

### 4. 生成x86汇编

围绕ELF链接保存输入、结构化中间状态与输出。进入“链接加载ELF”前，先断言当前检查点成立并记录首个偏离。

### 5. 链接加载ELF

围绕加载与PIC保存输入、结构化中间状态与输出。进入“回归C♭程序”前，先断言当前检查点成立并记录首个偏离。

### 6. 回归C♭程序

围绕扫描解析保存输入、结构化中间状态与输出。进入“核对4部分”前，先断言当前检查点成立并记录首个偏离。

## 可运行骨架

```text
代码分析 -> AST/语义/IR -> x86汇编 -> ELF目标文件 -> 链接 -> 加载执行
```

```bash
find samples -name '*.cb' -print0 | xargs -0 -n1 ./test-one
```

```text
source | token | AST | semantic | IR | asm | object | executable | process
```

<Callout type="trap">
  **平台边界：** 本书以JavaCC、Linux/IA-32、GNU汇编器和ELF为主线。现代x86-64
  ABI、PIE默认值、JDK模块和新版binutils可能改变命令或产物；先记录版本并区分书中机制与当前平台默认值。
</Callout>

## 正式目录逐项讲解

### 第1章 开始制作编译器

“第1章 开始制作编译器”要求给出输入、输出、中间表示、错误合同和清理条件，沿cbc端到端流水线停在第一处分叉。在本章中，“扫描解析”服务于“核对4部分”：固定C♭源码、cbc版本、Java/Javacc版本、x86目标与链接参数，同时保存结构化中间产物、标准错误和退出状态。

验证只改变一个token、产生式、声明、类型、IR节点、寄存器约束、符号可见性或装载地址，并在进入“完成代码分析”前停住第一条AST、绑定、IR、汇编、ELF或运行轨迹分叉。修复后以同一输入重建全部下游产物，不能只手工修改最终汇编或可执行文件。

### 第2章 C♭和cbc

“第2章 C♭和cbc”要求给出输入、输出、中间表示、错误合同和清理条件，沿cbc端到端流水线停在第一处分叉。在本章中，“语义与IR”服务于“完成代码分析”：固定C♭源码、cbc版本、Java/Javacc版本、x86目标与链接参数，同时保存结构化中间产物、标准错误和退出状态。

验证只改变一个token、产生式、声明、类型、IR节点、寄存器约束、符号可见性或装载地址，并在进入“生成AST与IR”前停住第一条AST、绑定、IR、汇编、ELF或运行轨迹分叉。修复后以同一输入重建全部下游产物，不能只手工修改最终汇编或可执行文件。

### 第1部分 代码分析

“第1部分 代码分析”要求给出输入、输出、中间表示、错误合同和清理条件，沿cbc端到端流水线停在第一处分叉。在本章中，“x86代码生成”服务于“生成AST与IR”：固定C♭源码、cbc版本、Java/Javacc版本、x86目标与链接参数，同时保存结构化中间产物、标准错误和退出状态。

验证只改变一个token、产生式、声明、类型、IR节点、寄存器约束、符号可见性或装载地址，并在进入“生成x86汇编”前停住第一条AST、绑定、IR、汇编、ELF或运行轨迹分叉。修复后以同一输入重建全部下游产物，不能只手工修改最终汇编或可执行文件。

### 第3章 语法分析的概要

“第3章 语法分析的概要”要求写出token、跳过规则、EBNF、前看条件与错误位置，并用合法、歧义、截断和非法字符样本证明分析边界。在本章中，“ELF链接”服务于“生成x86汇编”：固定C♭源码、cbc版本、Java/Javacc版本、x86目标与链接参数，同时保存结构化中间产物、标准错误和退出状态。

验证只改变一个token、产生式、声明、类型、IR节点、寄存器约束、符号可见性或装载地址，并在进入“链接加载ELF”前停住第一条AST、绑定、IR、汇编、ELF或运行轨迹分叉。修复后以同一输入重建全部下游产物，不能只手工修改最终汇编或可执行文件。

### 第4章 词法分析

“第4章 词法分析”要求写出token、跳过规则、EBNF、前看条件与错误位置，并用合法、歧义、截断和非法字符样本证明分析边界。在本章中，“加载与PIC”服务于“链接加载ELF”：固定C♭源码、cbc版本、Java/Javacc版本、x86目标与链接参数，同时保存结构化中间产物、标准错误和退出状态。

验证只改变一个token、产生式、声明、类型、IR节点、寄存器约束、符号可见性或装载地址，并在进入“回归C♭程序”前停住第一条AST、绑定、IR、汇编、ELF或运行轨迹分叉。修复后以同一输入重建全部下游产物，不能只手工修改最终汇编或可执行文件。

### 第5章 基于JavaCC的解析器描述

“第5章 基于JavaCC的解析器描述”要求写出token、跳过规则、EBNF、前看条件与错误位置，并用合法、歧义、截断和非法字符样本证明分析边界。在本章中，“扫描解析”服务于“回归C♭程序”：固定C♭源码、cbc版本、Java/Javacc版本、x86目标与链接参数，同时保存结构化中间产物、标准错误和退出状态。

验证只改变一个token、产生式、声明、类型、IR节点、寄存器约束、符号可见性或装载地址，并在进入“核对4部分”前停住第一条AST、绑定、IR、汇编、ELF或运行轨迹分叉。修复后以同一输入重建全部下游产物，不能只手工修改最终汇编或可执行文件。

### 第6章 语法分析

“第6章 语法分析”要求写出token、跳过规则、EBNF、前看条件与错误位置，并用合法、歧义、截断和非法字符样本证明分析边界。在本章中，“语义与IR”服务于“核对4部分”：固定C♭源码、cbc版本、Java/Javacc版本、x86目标与链接参数，同时保存结构化中间产物、标准错误和退出状态。

验证只改变一个token、产生式、声明、类型、IR节点、寄存器约束、符号可见性或装载地址，并在进入“完成代码分析”前停住第一条AST、绑定、IR、汇编、ELF或运行轨迹分叉。修复后以同一输入重建全部下游产物，不能只手工修改最终汇编或可执行文件。

### 第2部分 抽象语法树和中间代码

“第2部分 抽象语法树和中间代码”要求区分解析器临时状态与持久AST，核对节点类型、父子关系、运算结合性、声明边界和源位置。在本章中，“x86代码生成”服务于“完成代码分析”：固定C♭源码、cbc版本、Java/Javacc版本、x86目标与链接参数，同时保存结构化中间产物、标准错误和退出状态。

验证只改变一个token、产生式、声明、类型、IR节点、寄存器约束、符号可见性或装载地址，并在进入“生成AST与IR”前停住第一条AST、绑定、IR、汇编、ELF或运行轨迹分叉。修复后以同一输入重建全部下游产物，不能只手工修改最终汇编或可执行文件。

### 第7章 JavaCC的action和抽象语法树

“第7章 JavaCC的action和抽象语法树”要求写出token、跳过规则、EBNF、前看条件与错误位置，并用合法、歧义、截断和非法字符样本证明分析边界。在本章中，“ELF链接”服务于“生成AST与IR”：固定C♭源码、cbc版本、Java/Javacc版本、x86目标与链接参数，同时保存结构化中间产物、标准错误和退出状态。

验证只改变一个token、产生式、声明、类型、IR节点、寄存器约束、符号可见性或装载地址，并在进入“生成x86汇编”前停住第一条AST、绑定、IR、汇编、ELF或运行轨迹分叉。修复后以同一输入重建全部下游产物，不能只手工修改最终汇编或可执行文件。

### 第8章 抽象语法树的生成

“第8章 抽象语法树的生成”要求区分解析器临时状态与持久AST，核对节点类型、父子关系、运算结合性、声明边界和源位置。在本章中，“加载与PIC”服务于“生成x86汇编”：固定C♭源码、cbc版本、Java/Javacc版本、x86目标与链接参数，同时保存结构化中间产物、标准错误和退出状态。

验证只改变一个token、产生式、声明、类型、IR节点、寄存器约束、符号可见性或装载地址，并在进入“链接加载ELF”前停住第一条AST、绑定、IR、汇编、ELF或运行轨迹分叉。修复后以同一输入重建全部下游产物，不能只手工修改最终汇编或可执行文件。

### 第9章 语义分析（1）引用的消解

“第9章 语义分析（1）引用的消解”要求建立作用域和类型表，将每个名称与类型使用绑定到唯一声明，并在IR生成前汇总最小语义诊断。在本章中，“扫描解析”服务于“链接加载ELF”：固定C♭源码、cbc版本、Java/Javacc版本、x86目标与链接参数，同时保存结构化中间产物、标准错误和退出状态。

验证只改变一个token、产生式、声明、类型、IR节点、寄存器约束、符号可见性或装载地址，并在进入“回归C♭程序”前停住第一条AST、绑定、IR、汇编、ELF或运行轨迹分叉。修复后以同一输入重建全部下游产物，不能只手工修改最终汇编或可执行文件。

### 第10章 语义分析（2）静态类型检查

“第10章 语义分析（2）静态类型检查”要求建立作用域和类型表，将每个名称与类型使用绑定到唯一声明，并在IR生成前汇总最小语义诊断。在本章中，“语义与IR”服务于“回归C♭程序”：固定C♭源码、cbc版本、Java/Javacc版本、x86目标与链接参数，同时保存结构化中间产物、标准错误和退出状态。

验证只改变一个token、产生式、声明、类型、IR节点、寄存器约束、符号可见性或装载地址，并在进入“核对4部分”前停住第一条AST、绑定、IR、汇编、ELF或运行轨迹分叉。修复后以同一输入重建全部下游产物，不能只手工修改最终汇编或可执行文件。

### 第11章 中间代码的转换

“第11章 中间代码的转换”要求显式化求值顺序、地址和值、跳转标签和副作用次数，确保复杂表达式降低后不重复求值左值。在本章中，“x86代码生成”服务于“核对4部分”：固定C♭源码、cbc版本、Java/Javacc版本、x86目标与链接参数，同时保存结构化中间产物、标准错误和退出状态。

验证只改变一个token、产生式、声明、类型、IR节点、寄存器约束、符号可见性或装载地址，并在进入“完成代码分析”前停住第一条AST、绑定、IR、汇编、ELF或运行轨迹分叉。修复后以同一输入重建全部下游产物，不能只手工修改最终汇编或可执行文件。

### 第3部分 汇编代码

“第3部分 汇编代码”要求记录寄存器读写、操作数宽度、栈效应、调用约定、对齐与返回路径，再用反汇编逐条核对。在本章中，“ELF链接”服务于“完成代码分析”：固定C♭源码、cbc版本、Java/Javacc版本、x86目标与链接参数，同时保存结构化中间产物、标准错误和退出状态。

验证只改变一个token、产生式、声明、类型、IR节点、寄存器约束、符号可见性或装载地址，并在进入“生成AST与IR”前停住第一条AST、绑定、IR、汇编、ELF或运行轨迹分叉。修复后以同一输入重建全部下游产物，不能只手工修改最终汇编或可执行文件。

### 第12章 x86架构的概要

“第12章 x86架构的概要”要求记录寄存器读写、操作数宽度、栈效应、调用约定、对齐与返回路径，再用反汇编逐条核对。在本章中，“加载与PIC”服务于“生成AST与IR”：固定C♭源码、cbc版本、Java/Javacc版本、x86目标与链接参数，同时保存结构化中间产物、标准错误和退出状态。

验证只改变一个token、产生式、声明、类型、IR节点、寄存器约束、符号可见性或装载地址，并在进入“生成x86汇编”前停住第一条AST、绑定、IR、汇编、ELF或运行轨迹分叉。修复后以同一输入重建全部下游产物，不能只手工修改最终汇编或可执行文件。

### 第13章 x86汇编器编程

“第13章 x86汇编器编程”要求记录寄存器读写、操作数宽度、栈效应、调用约定、对齐与返回路径，再用反汇编逐条核对。在本章中，“扫描解析”服务于“生成x86汇编”：固定C♭源码、cbc版本、Java/Javacc版本、x86目标与链接参数，同时保存结构化中间产物、标准错误和退出状态。

验证只改变一个token、产生式、声明、类型、IR节点、寄存器约束、符号可见性或装载地址，并在进入“链接加载ELF”前停住第一条AST、绑定、IR、汇编、ELF或运行轨迹分叉。修复后以同一输入重建全部下游产物，不能只手工修改最终汇编或可执行文件。

### 第14章 函数和变量

“第14章 函数和变量”要求建立作用域和类型表，将每个名称与类型使用绑定到唯一声明，并在IR生成前汇总最小语义诊断。在本章中，“语义与IR”服务于“链接加载ELF”：固定C♭源码、cbc版本、Java/Javacc版本、x86目标与链接参数，同时保存结构化中间产物、标准错误和退出状态。

验证只改变一个token、产生式、声明、类型、IR节点、寄存器约束、符号可见性或装载地址，并在进入“回归C♭程序”前停住第一条AST、绑定、IR、汇编、ELF或运行轨迹分叉。修复后以同一输入重建全部下游产物，不能只手工修改最终汇编或可执行文件。

### 第15章 编译表达式和语句

“第15章 编译表达式和语句”要求从IR节点选择指令模式，标记虚拟栈和标签修补，比较生成汇编、目标文件与同源参考编译结果。在本章中，“x86代码生成”服务于“回归C♭程序”：固定C♭源码、cbc版本、Java/Javacc版本、x86目标与链接参数，同时保存结构化中间产物、标准错误和退出状态。

验证只改变一个token、产生式、声明、类型、IR节点、寄存器约束、符号可见性或装载地址，并在进入“核对4部分”前停住第一条AST、绑定、IR、汇编、ELF或运行轨迹分叉。修复后以同一输入重建全部下游产物，不能只手工修改最终汇编或可执行文件。

### 第16章 分配栈帧

“第16章 分配栈帧”要求记录寄存器读写、操作数宽度、栈效应、调用约定、对齐与返回路径，再用反汇编逐条核对。在本章中，“ELF链接”服务于“核对4部分”：固定C♭源码、cbc版本、Java/Javacc版本、x86目标与链接参数，同时保存结构化中间产物、标准错误和退出状态。

验证只改变一个token、产生式、声明、类型、IR节点、寄存器约束、符号可见性或装载地址，并在进入“完成代码分析”前停住第一条AST、绑定、IR、汇编、ELF或运行轨迹分叉。修复后以同一输入重建全部下游产物，不能只手工修改最终汇编或可执行文件。

### 第17章 优化的方法

“第17章 优化的方法”要求先冻结可观察语义和未定义行为边界，再写前置条件、改写规则、反例、差分结果和性能测量。在本章中，“加载与PIC”服务于“完成代码分析”：固定C♭源码、cbc版本、Java/Javacc版本、x86目标与链接参数，同时保存结构化中间产物、标准错误和退出状态。

验证只改变一个token、产生式、声明、类型、IR节点、寄存器约束、符号可见性或装载地址，并在进入“生成AST与IR”前停住第一条AST、绑定、IR、汇编、ELF或运行轨迹分叉。修复后以同一输入重建全部下游产物，不能只手工修改最终汇编或可执行文件。

### 第4部分 链接和加载

“第4部分 链接和加载”要求沿符号解析、库选择、重定位、段映射、GOT/PLT和入口转移保存原始ELF及加载器轨迹。在本章中，“扫描解析”服务于“生成AST与IR”：固定C♭源码、cbc版本、Java/Javacc版本、x86目标与链接参数，同时保存结构化中间产物、标准错误和退出状态。

验证只改变一个token、产生式、声明、类型、IR节点、寄存器约束、符号可见性或装载地址，并在进入“生成x86汇编”前停住第一条AST、绑定、IR、汇编、ELF或运行轨迹分叉。修复后以同一输入重建全部下游产物，不能只手工修改最终汇编或可执行文件。

### 第18章 生成目标文件

“第18章 生成目标文件”要求把代码、数据、BSS、符号和重定位映射到ELF节与程序头，并用readelf、nm和objdump交叉检查。在本章中，“语义与IR”服务于“生成x86汇编”：固定C♭源码、cbc版本、Java/Javacc版本、x86目标与链接参数，同时保存结构化中间产物、标准错误和退出状态。

验证只改变一个token、产生式、声明、类型、IR节点、寄存器约束、符号可见性或装载地址，并在进入“链接加载ELF”前停住第一条AST、绑定、IR、汇编、ELF或运行轨迹分叉。修复后以同一输入重建全部下游产物，不能只手工修改最终汇编或可执行文件。

### 第19章 链接和库

“第19章 链接和库”要求沿符号解析、库选择、重定位、段映射、GOT/PLT和入口转移保存原始ELF及加载器轨迹。在本章中，“x86代码生成”服务于“链接加载ELF”：固定C♭源码、cbc版本、Java/Javacc版本、x86目标与链接参数，同时保存结构化中间产物、标准错误和退出状态。

验证只改变一个token、产生式、声明、类型、IR节点、寄存器约束、符号可见性或装载地址，并在进入“回归C♭程序”前停住第一条AST、绑定、IR、汇编、ELF或运行轨迹分叉。修复后以同一输入重建全部下游产物，不能只手工修改最终汇编或可执行文件。

### 第20章 加载程序

“第20章 加载程序”要求沿符号解析、库选择、重定位、段映射、GOT/PLT和入口转移保存原始ELF及加载器轨迹。在本章中，“ELF链接”服务于“回归C♭程序”：固定C♭源码、cbc版本、Java/Javacc版本、x86目标与链接参数，同时保存结构化中间产物、标准错误和退出状态。

验证只改变一个token、产生式、声明、类型、IR节点、寄存器约束、符号可见性或装载地址，并在进入“核对4部分”前停住第一条AST、绑定、IR、汇编、ELF或运行轨迹分叉。修复后以同一输入重建全部下游产物，不能只手工修改最终汇编或可执行文件。

### 第21章 生成地址无关代码

“第21章 生成地址无关代码”要求沿符号解析、库选择、重定位、段映射、GOT/PLT和入口转移保存原始ELF及加载器轨迹。在本章中，“加载与PIC”服务于“核对4部分”：固定C♭源码、cbc版本、Java/Javacc版本、x86目标与链接参数，同时保存结构化中间产物、标准错误和退出状态。

验证只改变一个token、产生式、声明、类型、IR节点、寄存器约束、符号可见性或装载地址，并在进入“完成代码分析”前停住第一条AST、绑定、IR、汇编、ELF或运行轨迹分叉。修复后以同一输入重建全部下游产物，不能只手工修改最终汇编或可执行文件。

### 第22章 扩展阅读

“第22章 扩展阅读”要求注明主题、版本、取得位置、最小复现和正文回链，避免把当前工具链行为误写成稳定ABI保证。在本章中，“扫描解析”服务于“完成代码分析”：固定C♭源码、cbc版本、Java/Javacc版本、x86目标与链接参数，同时保存结构化中间产物、标准错误和退出状态。

验证只改变一个token、产生式、声明、类型、IR节点、寄存器约束、符号可见性或装载地址，并在进入“生成AST与IR”前停住第一条AST、绑定、IR、汇编、ELF或运行轨迹分叉。修复后以同一输入重建全部下游产物，不能只手工修改最终汇编或可执行文件。

### 附录

“附录”要求给出输入、输出、中间表示、错误合同和清理条件，沿cbc端到端流水线停在第一处分叉。在本章中，“语义与IR”服务于“生成AST与IR”：固定C♭源码、cbc版本、Java/Javacc版本、x86目标与链接参数，同时保存结构化中间产物、标准错误和退出状态。

验证只改变一个token、产生式、声明、类型、IR节点、寄存器约束、符号可见性或装载地址，并在进入“生成x86汇编”前停住第一条AST、绑定、IR、汇编、ELF或运行轨迹分叉。修复后以同一输入重建全部下游产物，不能只手工修改最终汇编或可执行文件。

## 分步视觉验证

先预测中间产物，再运行转换；若不同，停在最早的AST、绑定、IR、汇编或ELF分叉。

<Stepper>
  <Step title="画编译产物链">
    先预测扫描解析、语义与IR、x86代码生成、ELF链接、加载与PIC的输入、输出、所有权和下游消费者。
    <CrcOfficialFinalReviewMapLab />
  </Step>
  <Step title="运行单变量编译">
    固定工具链和C♭样例，只改变一个前端、语义、机器或链接条件。
    <CrcOfficialFinalReviewExperimentLab />
  </Step>
  <Step title="核对第一处分叉">
    保存原始中间产物，清理后从源文件重建并重放历史样本。
    <CrcOfficialFinalReviewEvidenceLab />
  </Step>
</Stepper>

## 常见误区

<Callout type="trap">
  **误区一：**
  只保留最终汇编或可执行文件。没有AST、语义绑定、IR、符号和重定位，无法判断错误属于哪一层。
</Callout>

<Callout type="trap">
  **误区二：**
  把IA-32示例直接外推到x86-64，或把当前gcc/ld默认PIE行为当作C♭语言与ELF标准保证。
</Callout>

## 四类样本与验收

| 样本 | 只改变什么                  | 必查证据               | 通过条件     |
| ---- | --------------------------- | ---------------------- | ------------ |
| 正常 | 一个最小语言或工具链功能    | 全部中间产物与退出状态 | 与预测一致   |
| 边界 | 宽度、嵌套、对齐或布局      | 第一条边界分支         | 边界可解释   |
| 故障 | 一个token、绑定、类型或符号 | 最早诊断与下游停止     | 不产出伪结果 |
| 回归 | 相同C♭样例集                | AST到进程的差分        | 旧语义保持   |

## 练习

<Exercises>

**问题 1** 为什么不能只凭最终程序运行成功验收《自制编译器》全书总复习？

<Answer>
  最终结果会隐藏错误AST、错误绑定、重复副作用、寄存器破坏、错误重定位和偶然地址布局。必须逐层保存结构化产物并定位第一处分叉。
</Answer>

**问题 2** 如何把“生成AST与IR”变成可回归实验？

<Answer>
  固定C♭源码、cbc提交、JavaCC、目标ABI和GNU工具版本，预测扫描解析与语义与IR的中间状态，只改变一个条件并保留合法、边界、故障和跨阶段样本。
</Answer>

**问题 3** 本页最小通过条件是什么？

<Answer>
  同一C♭程序从源码到进程的每层产物可独立解释，故障停在最早责任层，清理后可由记录完整重建。
  修复后必须清理旧产物，从源文件重建，并让全部历史样本回到基线。
</Answer>

</Exercises>

## 小结

- 扫描解析：连接“核对4部分”的输入、中间产物、诊断与下游证据。
- 语义与IR：连接“完成代码分析”的输入、中间产物、诊断与下游证据。
- x86代码生成：连接“生成AST与IR”的输入、中间产物、诊断与下游证据。
- ELF链接：连接“生成x86汇编”的输入、中间产物、诊断与下游证据。
- 加载与PIC：连接“链接加载ELF”的输入、中间产物、诊断与下游证据。
- 本页27个正式目录层级全部进入cbc端到端可复查链。

## 术语表

<Glossary>
  <GlossaryItem term="扫描解析">
    扫描解析用于解释“核对4部分”，需要同时指出输入、结构化产物、第一处失败和重建方法。
  </GlossaryItem>
  <GlossaryItem term="语义与IR">
    语义与IR用于解释“完成代码分析”，需要同时指出输入、结构化产物、第一处失败和重建方法。
  </GlossaryItem>
  <GlossaryItem term="x86代码生成">
    x86代码生成用于解释“生成AST与IR”，需要同时指出输入、结构化产物、第一处失败和重建方法。
  </GlossaryItem>
  <GlossaryItem term="ELF链接">
    ELF链接用于解释“生成x86汇编”，需要同时指出输入、结构化产物、第一处失败和重建方法。
  </GlossaryItem>
  <GlossaryItem term="加载与PIC">
    加载与PIC用于解释“链接加载ELF”，需要同时指出输入、结构化产物、第一处失败和重建方法。
  </GlossaryItem>
</Glossary>

<Attribution
  adaptedFrom="青木峰郎著，严圣逸、绝云译《自制编译器》，人民邮电出版社，2016-06，472页，ISBN 9787115422187 · 作者详细目录与中文版完整目录的独立教学重构"
  adaptedUrl="https://i.loveruby.net/ja/stdcompiler/toc.html"
/>
