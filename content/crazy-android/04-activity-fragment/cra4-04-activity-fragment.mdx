---
title: "第4章 深入理解Activity与Fragment"
description: "依据电子工业出版社第4版完整目录，覆盖34个本页节点：把Activity、Fragment、Intent结果、加载模式、事务、生命周期和导航组织成可恢复页面流"
type: C
section: "疯狂Android讲义（第4版 / Android 9 系列 / Java）"
draft: false
math: false
sourceUrl: "https://phei.com.cn/module/goods/wssd_content.jsp?bookid=53581"
demo: true
order: 0
qualityVersion: 2
practiceMode: diagnosis
sourceMode: independent-rewrite
officialUnitId: cra4-04-activity-fragment
---

import { Objectives } from "@/components/mdx/objectives";
import { Term } from "@/components/mdx/term";
import { Callout } from "@/components/mdx/callout";
import { Answer, Exercises } from "@/components/mdx/exercises";
import { Glossary, GlossaryItem } from "@/components/mdx/glossary";
import { Attribution } from "@/components/mdx/attribution";
import { Cra404ActivityFragmentDecisionLab, Cra404ActivityFragmentMechanismMap, Cra404ActivityFragmentFailureDiagram } from "@/components/mdx/crazy-android/cra4-04-activity-fragment";

# 第4章 深入理解Activity与Fragment

在《第4章 深入理解Activity与Fragment》中，本页依据电子工业出版社《疯狂Android讲义》第4版完整目录独立重构，不复制原文。版本锁定李刚、2019年3月、764页、ISBN 9787121360091，以Android 9 系列、Android Studio与Java为基线；现代项目必须另建迁移层，不能无说明地把Compose或最新targetSdk规则写回原书语境。

<Objectives>

- 能解释“第4章 深入理解Activity与Fragment”全部正式节点的组件所有者、生命周期、线程、权限和Android 9 系列版本边界。
- 能实现“把Activity、Fragment、Intent结果、加载模式、事务、生命周期和导航组织成可恢复页面流”的最小垂直切片，并保存构建、设备操作、原始日志和断言。
- 能比较正常运行、旋转、后台、进程重建、权限拒绝与外部故障下的行为差异。
- 能设计推翻当前实现的反例，并凭生命周期图、任务栈、Fragment事务日志、旋转与进程恢复测试完成独立交接。

</Objectives>

<Cra404ActivityFragmentMechanismMap />

<Cra404ActivityFragmentDecisionLab />

<Cra404ActivityFragmentFailureDiagram />

## 从项目合同与生命周期开始

在《第4章 深入理解Activity与Fragment》中，第4版以近百个实例和两个综合项目组织知识，不是零散API列表。每个节点都要回答用户从哪里进入、哪个组件拥有工作、回调在哪个线程发生、哪些事实必须恢复、需要什么权限、外部系统如何失败，以及资源何时释放。<Term def="负责创建、取消并最终释放组件、任务或资源的明确作用域">生命周期所有者</Term>连接Activity、Fragment、View、Service、Receiver、线程与媒体资源。

在《第4章 深入理解Activity与Fragment》中，界面字段和进程单例不是可靠事实源。<Term def="配置变化或进程重建后能从保存事实重新构造的最小用户状态">可恢复状态</Term>要按大小和寿命放入Bundle、文件、SQLite或服务端。Intent、Provider、网络、定位、地图和系统服务构成<Term def="跨组件或系统边界交换数据时必须验证的类型、权限、大小、时序与失败约束">外部合同</Term>，成功响应只是其中一个样本。

在《第4章 深入理解Activity与Fragment》中，本书准确反映Android 9时代。<Term def="Android版本、targetSdk、设备能力、权限与依赖共同限定的行为适用范围">版本边界</Term>要求先复现原书机制，再迁移组件导出、分区存储、通知、后台启动、位置和废弃API。<Term def="由环境指纹、输入、操作、日志、状态快照和断言组成的可重放结论">证据链</Term>保证示例不是只在作者机器上可用。

## 最小可执行切片

先把业务状态转换与Android副作用分开，便于在设备之外验证不变量：

```java
sealed interface ScreenState permits Loading, Ready, Failed {}
record Loading() implements ScreenState {}
record Ready(String key) implements ScreenState {}
record Failed(String reason, boolean retryable) implements ScreenState {}

ScreenState reduce(ScreenState current, UiEvent event) { return nextState(current, event); }
```

固定环境并从命令行保留构建和设备证据：

```bash
./gradlew --no-daemon clean test assembleDebug
adb install -r app/build/outputs/apk/debug/app-debug.apk
adb shell am force-stop org.example.app
adb shell monkey -p org.example.app 1
adb logcat -d -v threadtime > run.log
```

先写行为合同，再执行用户动作与故障注入：

```gherkin
Given a pinned JDK, SDK, Gradle build, device API, locale, permission state, and input
When the owner is recreated after state saving or process death
Then durable facts reconstruct the same user-visible state
And cancelled work cannot update a destroyed owner
And denied permission, offline input, malformed data, and missing hardware remain recoverable
```

在《第4章 深入理解Activity与Fragment》中，三段代码分别约束状态模型、环境重放和行为结果。第4版源码使用当时Java/Android API；这里的状态骨架只说明测试边界，不冒充原书代码。真实项目还需要静态检查、仪器测试、无障碍检查和发布构建。

## 线程、权限、状态与资源

在《第4章 深入理解Activity与Fragment》中，主线程负责输入、生命周期分发和UI；文件、SQLite、网络、图片解码、OpenGL准备与长计算不得阻塞帧。后台结果返回前确认所有者仍有效，Service并不自动拥有后台线程，Receiver也不能无限运行。每个实现列出入口、线程、事实源、权限、取消点、关闭责任和用户可见失败。

在《第4章 深入理解Activity与Fragment》中，Intent、Provider、WebView、JSON、文件URI、地图Key、位置和传感器样本都按不可信输入处理。在《第4章 深入理解Activity与Fragment》中，校验action、scheme、MIME、大小、授权、时效和响应者；导出组件最小授权；日志、截图与数据库不得泄露联系人、短信、位置、令牌或签名密钥。

在《第4章 深入理解Activity与Fragment》中，性能以用户任务衡量：界面看首帧和掉帧，列表看回收与分配，媒体和图形看帧时间与内存，网络看超时和取消，传感器看采样与耗电，后台工作看约束与重复调度。比较时固定设备、构建类型、数据规模和网络条件。

在《第4章 深入理解Activity与Fragment》中，现代迁移一次改变一个条件。在《第4章 深入理解Activity与Fragment》中，AsyncTask、IntentService、旧Fragment、外部存储、后台Service、隐式广播、HTTP明文、旧定位和地图SDK都需要查当前合同；先保存Android 9基线，再迁移并保留行为差异、测试和回滚。

<Callout type="trap">

  在《第4章 深入理解Activity与Fragment》中，**核心陷阱。** 在《第4章 深入理解Activity与Fragment》中，把525个目录条目压成“UI、数据、服务、网络”几个概览页，会直接遗漏Intent、资源、Provider、OpenGL、桌面、传感器、GPS、地图和两个项目。必须逐节点映射到可运行实验与证据。

</Callout>
<Callout type="trap">

  在《第4章 深入理解Activity与Fragment》中，**历史示例不等于当前平台政策。** Android 9时代可运行的后台、存储、广播、位置和组件配置，在现代targetSdk下可能被限制。保留原书机制，再建立有证据的迁移层。

</Callout>
<Callout type="warning">

  真机实验使用测试账号、样本媒体与最小权限；联系人、短信、位置、地图Key、签名密钥和服务端凭据不得写入仓库或公开日志。

</Callout>
## 本章回顾

本页从“第4章 深入理解Activity与Fragment”覆盖到“4.7 本章小结”，共34个正式目录条目。掌握标准是能沿“把Activity、Fragment、Intent结果、加载模式、事务、生命周期和导航组织成可恢复页面流”解释组件与数据边界，运行正常、重建与失败实验，并让另一位开发者仅凭生命周期图、任务栈、Fragment事务日志、旋转与进程恢复测试重放结论。

<Exercises>

**问题 1**：“第4章 深入理解Activity与Fragment”覆盖哪些正式节点和主线？
<Answer>
  覆盖34个本页节点，从“第4章 深入理解Activity与Fragment”到“4.7 本章小结”。主线是把Activity、Fragment、Intent结果、加载模式、事务、生命周期和导航组织成可恢复页面流，交付生命周期图、任务栈、Fragment事务日志、旋转与进程恢复测试。
</Answer>
**问题 2**：怎样建立本页最小可执行实验？
<Answer>
  在《第4章 深入理解Activity与Fragment》中，固定JDK、Android SDK、Gradle、设备API、语言、权限和输入，完成从用户动作到可观察结果的垂直切片；保存干净构建、设备操作、原始日志、状态快照与断言。
</Answer>
**问题 3**：为什么只在正常路径运行不能证明完成？
<Answer>
  它没有覆盖组件重建、进程死亡、线程取消、权限拒绝、外部输入失败、资源释放和版本变化。应逐项注入故障，并用生命周期图、任务栈、Fragment事务日志、旋转与进程恢复测试核对。
</Answer>
**问题 4**：怎样设计能推翻实现的反例？
<Answer>
  在《第4章 深入理解Activity与Fragment》中，保持其他变量不变，分别注入旋转、后台回收、进程死亡、拒权、断网、无效输入与重复操作；状态丢失、越权、崩溃、泄漏或重复副作用都推翻完成结论。
</Answer>
**问题 5**：从Android 9迁移到现代targetSdk时如何控制变量？
<Answer>
  在《第4章 深入理解Activity与Fragment》中，先按第4版锁定Java与Android 9行为基线，再查询当前平台合同；一次只迁移组件导出、存储、权限、后台、通知、位置或依赖之一，保存构建、行为、测试与回滚差异。
</Answer>
**问题 6**：达到独立交接标准需要什么？
<Answer>
  需要版本与设备指纹、源码提交、输入、构建命令、生命周期轨迹、原始日志、失败测试、安全边界与发布制品，让他人凭生命周期图、任务栈、Fragment事务日志、旋转与进程恢复测试重放。
</Answer>
</Exercises>

<Glossary>
  <GlossaryItem term="生命周期所有者">
    负责创建、取消并最终释放组件、任务或资源的明确作用域。
  </GlossaryItem>
  <GlossaryItem term="可恢复状态">
    配置变化或进程重建后能从保存事实重新构造的最小用户状态。
  </GlossaryItem>
  <GlossaryItem term="外部合同">
    跨组件或系统边界交换数据时必须验证的类型、权限、大小、时序与失败约束。
  </GlossaryItem>
  <GlossaryItem term="版本边界">
    Android版本、targetSdk、设备能力、权限与依赖共同限定的行为适用范围。
  </GlossaryItem>
  <GlossaryItem term="证据链">
    由环境指纹、输入、操作、日志、状态快照和断言组成的可重放结论。
  </GlossaryItem>
</Glossary>

[← 上一页：第3章 Android事件机制](/learn/crazy-android/03-event-mechanism/cra4-03-event-mechanism) · [下一页：第5章 使用Intent和IntentFilter通信 →](/learn/crazy-android/05-intent-filter/cra4-05-intent-filter)

## 原版目录概念补充核对

以下条目补齐官方目录中容易被示例主线掩盖的概念。它们不重复罗列目录，而是明确每项概念的机制、适用边界和验收证据。

### 4.1 建立、配置和使用Activity：机制、边界与证据

第4章 深入理解Activity与Fragment中的4.1 建立、配置和使用Activity涉及实例、任务栈、保存状态和生命周期所有权，回调出现不等于用户任务已经恢复。保存实例标识、Intent、栈位置与状态来源，用旋转、后台回收、进程重建和重复导航核对恢复与释放。

### 4.1.1 高级Activity：机制、边界与证据

第4章 深入理解Activity与Fragment中的4.1.1 高级Activity涉及实例、任务栈、保存状态和生命周期所有权，回调出现不等于用户任务已经恢复。保存实例标识、Intent、栈位置与状态来源，用旋转、后台回收、进程重建和重复导航核对恢复与释放。

### 实例：用LauncherActivity开发启动Activity的列表：机制、边界与证据

第4章 深入理解Activity与Fragment中的实例：用LauncherActivity开发启动Activity的列表涉及实例、任务栈、保存状态和生命周期所有权，回调出现不等于用户任务已经恢复。保存实例标识、Intent、栈位置与状态来源，用旋转、后台回收、进程重建和重复导航核对恢复与释放。

### 实例：使用ExpandableListActivity实现可展开的Activity：机制、边界与证据

第4章 深入理解Activity与Fragment中的实例：使用ExpandableListActivity实现可展开的Activity由 View 树、MeasureSpec、布局结果、绘制、输入焦点和可恢复状态共同决定。固定屏幕、字体和数据后改变一个尺寸、配置或复用条件，保存层级、回调顺序、无障碍语义与帧结果，避免只看静态截图。

### 实例：PreferenceActivity结合PreferenceFragment实现参数设置界面：机制、边界与证据

第4章 深入理解Activity与Fragment中的实例：PreferenceActivity结合PreferenceFragment实现参数设置界面由 View 树、MeasureSpec、布局结果、绘制、输入焦点和可恢复状态共同决定。固定屏幕、字体和数据后改变一个尺寸、配置或复用条件，保存层级、回调顺序、无障碍语义与帧结果，避免只看静态截图。

### 4.1.2 配置Activity：机制、边界与证据

第4章 深入理解Activity与Fragment中的4.1.2 配置Activity涉及实例、任务栈、保存状态和生命周期所有权，回调出现不等于用户任务已经恢复。保存实例标识、Intent、栈位置与状态来源，用旋转、后台回收、进程重建和重复导航核对恢复与释放。

### 4.1.3 启动、关闭Activity：机制、边界与证据

第4章 深入理解Activity与Fragment中的4.1.3 启动、关闭Activity涉及实例、任务栈、保存状态和生命周期所有权，回调出现不等于用户任务已经恢复。保存实例标识、Intent、栈位置与状态来源，用旋转、后台回收、进程重建和重复导航核对恢复与释放。

### 4.1.4 使用Bundle在Activity之间交换数据：机制、边界与证据

第4章 深入理解Activity与Fragment中的4.1.4 使用Bundle在Activity之间交换数据涉及实例、任务栈、保存状态和生命周期所有权，回调出现不等于用户任务已经恢复。保存实例标识、Intent、栈位置与状态来源，用旋转、后台回收、进程重建和重复导航核对恢复与释放。

### 实例：用第二个Activity处理注册信息：机制、边界与证据

第4章 深入理解Activity与Fragment中的实例：用第二个Activity处理注册信息涉及实例、任务栈、保存状态和生命周期所有权，回调出现不等于用户任务已经恢复。保存实例标识、Intent、栈位置与状态来源，用旋转、后台回收、进程重建和重复导航核对恢复与释放。

### 4.1.5 启动其他Activity并返回结果：机制、边界与证据

第4章 深入理解Activity与Fragment中的4.1.5 启动其他Activity并返回结果涉及实例、任务栈、保存状态和生命周期所有权，回调出现不等于用户任务已经恢复。保存实例标识、Intent、栈位置与状态来源，用旋转、后台回收、进程重建和重复导航核对恢复与释放。

### 实例：用第二个Activity让用户选择信息：机制、边界与证据

第4章 深入理解Activity与Fragment中的实例：用第二个Activity让用户选择信息涉及实例、任务栈、保存状态和生命周期所有权，回调出现不等于用户任务已经恢复。保存实例标识、Intent、栈位置与状态来源，用旋转、后台回收、进程重建和重复导航核对恢复与释放。

### 4.2 Activity的回调机制：机制、边界与证据

第4章 深入理解Activity与Fragment中的4.2 Activity的回调机制要沿输入源、分发、拦截、消费和状态更新解释时序，同一事件不能被未知对象重复处理。记录 MotionEvent/KeyEvent、目标 View、线程和消费返回值，并用取消、父级拦截或快速重复输入验证边界。

### 4.3 Activity的生命周期：机制、边界与证据

第4章 深入理解Activity与Fragment中的4.3 Activity的生命周期涉及实例、任务栈、保存状态和生命周期所有权，回调出现不等于用户任务已经恢复。保存实例标识、Intent、栈位置与状态来源，用旋转、后台回收、进程重建和重复导航核对恢复与释放。

### 4.3.1 Activity的生命周期演示：机制、边界与证据

第4章 深入理解Activity与Fragment中的4.3.1 Activity的生命周期演示涉及实例、任务栈、保存状态和生命周期所有权，回调出现不等于用户任务已经恢复。保存实例标识、Intent、栈位置与状态来源，用旋转、后台回收、进程重建和重复导航核对恢复与释放。

### 4.3.2 Activity与Servlet的相似性和区别：机制、边界与证据

第4章 深入理解Activity与Fragment中的4.3.2 Activity与Servlet的相似性和区别涉及实例、任务栈、保存状态和生命周期所有权，回调出现不等于用户任务已经恢复。保存实例标识、Intent、栈位置与状态来源，用旋转、后台回收、进程重建和重复导航核对恢复与释放。

### 4.4 Activity的4种加载模式：机制、边界与证据

第4章 深入理解Activity与Fragment中的4.4 Activity的4种加载模式涉及实例、任务栈、保存状态和生命周期所有权，回调出现不等于用户任务已经恢复。保存实例标识、Intent、栈位置与状态来源，用旋转、后台回收、进程重建和重复导航核对恢复与释放。

### 4.4.1 standard模式：机制、边界与证据

第4章 深入理解Activity与Fragment中的4.4.1 standard模式要在 Android 9 系列/Java 基线上写清入口、组件 owner、线程、可恢复状态、权限和外部失败。固定设备与输入，只改变一个生命周期或平台条件，用原始日志、状态快照和最终用户结果复核。

### 4.4.2 singleTop模式：机制、边界与证据

第4章 深入理解Activity与Fragment中的4.4.2 singleTop模式要在 Android 9 系列/Java 基线上写清入口、组件 owner、线程、可恢复状态、权限和外部失败。固定设备与输入，只改变一个生命周期或平台条件，用原始日志、状态快照和最终用户结果复核。

### 4.4.3 singleTask模式：机制、边界与证据

第4章 深入理解Activity与Fragment中的4.4.3 singleTask模式要在 Android 9 系列/Java 基线上写清入口、组件 owner、线程、可恢复状态、权限和外部失败。固定设备与输入，只改变一个生命周期或平台条件，用原始日志、状态快照和最终用户结果复核。

### 4.4.4 singleInstance模式：机制、边界与证据

第4章 深入理解Activity与Fragment中的4.4.4 singleInstance模式要在 Android 9 系列/Java 基线上写清入口、组件 owner、线程、可恢复状态、权限和外部失败。固定设备与输入，只改变一个生命周期或平台条件，用原始日志、状态快照和最终用户结果复核。

### 4.5 Android 9升级的Fragment：机制、边界与证据

第4章 深入理解Activity与Fragment中的4.5 Android 9升级的Fragment涉及实例、任务栈、保存状态和生命周期所有权，回调出现不等于用户任务已经恢复。保存实例标识、Intent、栈位置与状态来源，用旋转、后台回收、进程重建和重复导航核对恢复与释放。

### 4.5.1 Fragment概述及其设计初衷：机制、边界与证据

第4章 深入理解Activity与Fragment中的4.5.1 Fragment概述及其设计初衷涉及实例、任务栈、保存状态和生命周期所有权，回调出现不等于用户任务已经恢复。保存实例标识、Intent、栈位置与状态来源，用旋转、后台回收、进程重建和重复导航核对恢复与释放。

### 4.5.2 创建Fragment：机制、边界与证据

第4章 深入理解Activity与Fragment中的4.5.2 创建Fragment涉及实例、任务栈、保存状态和生命周期所有权，回调出现不等于用户任务已经恢复。保存实例标识、Intent、栈位置与状态来源，用旋转、后台回收、进程重建和重复导航核对恢复与释放。

### 实例：开发显示图书详情的Fragment：机制、边界与证据

第4章 深入理解Activity与Fragment中的实例：开发显示图书详情的Fragment涉及实例、任务栈、保存状态和生命周期所有权，回调出现不等于用户任务已经恢复。保存实例标识、Intent、栈位置与状态来源，用旋转、后台回收、进程重建和重复导航核对恢复与释放。

### 实例：创建ListFragment：机制、边界与证据

第4章 深入理解Activity与Fragment中的实例：创建ListFragment由 View 树、MeasureSpec、布局结果、绘制、输入焦点和可恢复状态共同决定。固定屏幕、字体和数据后改变一个尺寸、配置或复用条件，保存层级、回调顺序、无障碍语义与帧结果，避免只看静态截图。

### 4.5.3 Fragment与Activity通信：机制、边界与证据

第4章 深入理解Activity与Fragment中的4.5.3 Fragment与Activity通信涉及实例、任务栈、保存状态和生命周期所有权，回调出现不等于用户任务已经恢复。保存实例标识、Intent、栈位置与状态来源，用旋转、后台回收、进程重建和重复导航核对恢复与释放。

### 4.5.4 Fragment管理与Fragment事务：机制、边界与证据

第4章 深入理解Activity与Fragment中的4.5.4 Fragment管理与Fragment事务涉及实例、任务栈、保存状态和生命周期所有权，回调出现不等于用户任务已经恢复。保存实例标识、Intent、栈位置与状态来源，用旋转、后台回收、进程重建和重复导航核对恢复与释放。

### 实例：开发兼顾屏幕分辨率的应用：机制、边界与证据

第4章 深入理解Activity与Fragment中的实例：开发兼顾屏幕分辨率的应用由资源限定符、设备配置、主题继承和运行时解析共同决定，文件存在不等于会被选中。固定 APK 后改变语言、密度、方向、夜间模式或 API，记录资源 ID、匹配路径和最终像素，并检查缺省回退。

### 4.6 Fragment的生命周期：机制、边界与证据

第4章 深入理解Activity与Fragment中的4.6 Fragment的生命周期涉及实例、任务栈、保存状态和生命周期所有权，回调出现不等于用户任务已经恢复。保存实例标识、Intent、栈位置与状态来源，用旋转、后台回收、进程重建和重复导航核对恢复与释放。

### 4.7 管理Fragment导航：机制、边界与证据

第4章 深入理解Activity与Fragment中的4.7 管理Fragment导航涉及实例、任务栈、保存状态和生命周期所有权，回调出现不等于用户任务已经恢复。保存实例标识、Intent、栈位置与状态来源，用旋转、后台回收、进程重建和重复导航核对恢复与释放。

### 实例：结合ViewPager实现分页导航：机制、边界与证据

第4章 深入理解Activity与Fragment中的实例：结合ViewPager实现分页导航由 View 树、MeasureSpec、布局结果、绘制、输入焦点和可恢复状态共同决定。固定屏幕、字体和数据后改变一个尺寸、配置或复用条件，保存层级、回调顺序、无障碍语义与帧结果，避免只看静态截图。

### 实例：结合TabLayout实现Tab导航：机制、边界与证据

第4章 深入理解Activity与Fragment中的实例：结合TabLayout实现Tab导航要在 Android 9 系列/Java 基线上写清入口、组件 owner、线程、可恢复状态、权限和外部失败。固定设备与输入，只改变一个生命周期或平台条件，用原始日志、状态快照和最终用户结果复核。

<Attribution
  adaptedFrom="李刚《疯狂Android讲义》第4版"
  adaptedUrl="https://phei.com.cn/module/goods/wssd_content.jsp?bookid=53581"
  mode="independent-rewrite"
  sourceBasis="outline-only"
/>
