---
title: "第 12 章 并发编程"
type: A
section: "程序间的交互和通信 · 并发编程"
order: 12
description: "比较进程、I/O 多路复用和线程三种并发模型，以线程内存模型、进度图和信号量分析互斥、调度、竞争、可重入与死锁。覆盖34个正式目录坐标。"
demo: true
math: false
sourceUrl: "https://csapp.cs.cmu.edu/3e/pieces/preface3e.pdf"
draft: false
---


## 直觉起点

、、、、共同构成本页的观察坐标。比较进程、I/O
多路复用和线程三种并发模型，以线程内存模型、进度图和信号量分析互斥、调度、竞争、可重入与死锁。
先预测下一状态，再运行最小样本；最终输出正确并不证明中间制品、地址、资源和并发关系都正确。

## 六阶段因果链

### 1. 划分并发任务

先标出逻辑流和共享状态，再用上下文、同步边和生命周期解释交错；安全性、活性和资源回收要分别验证。 对“第12章 并发编程”的学习不能停在定义：先在“划分并发任务”阶段写出输入和预测，再推进到“选择进程事件或线程”并记录机器可见状态。实验固定源码、工具链、输入和环境，只改变一个条件；若实际轨迹偏离预测，就保存首个分叉、责任接口与恢复结果。

### 2. 选择进程事件或线程

先标出逻辑流和共享状态，再用上下文、同步边和生命周期解释交错；安全性、活性和资源回收要分别验证。 对“12.1 基于进程的并发编程”的学习不能停在定义：先在“选择进程事件或线程”阶段写出输入和预测，再推进到“标记共享状态”并记录机器可见状态。实验固定源码、工具链、输入和环境，只改变一个条件；若实际轨迹偏离预测，就保存首个分叉、责任接口与恢复结果。

### 3. 标记共享状态

先标出逻辑流和共享状态，再用上下文、同步边和生命周期解释交错；安全性、活性和资源回收要分别验证。 对“12.1.1 基于进程的并发服务器”的学习不能停在定义：先在“标记共享状态”阶段写出输入和预测，再推进到“建立同步顺序”并记录机器可见状态。实验固定源码、工具链、输入和环境，只改变一个条件；若实际轨迹偏离预测，就保存首个分叉、责任接口与恢复结果。

### 4. 建立同步顺序

先标出逻辑流和共享状态，再用上下文、同步边和生命周期解释交错；安全性、活性和资源回收要分别验证。 对“12.1.2 进程的优劣”的学习不能停在定义：先在“建立同步顺序”阶段写出输入和预测，再推进到“验证活性”并记录机器可见状态。实验固定源码、工具链、输入和环境，只改变一个条件；若实际轨迹偏离预测，就保存首个分叉、责任接口与恢复结果。

### 5. 验证活性

先标出逻辑流和共享状态，再用上下文、同步边和生命周期解释交错；安全性、活性和资源回收要分别验证。 对“12.2 基于I/O多路复用的并发编程”的学习不能停在定义：先在“验证活性”阶段写出输入和预测，再推进到“回收全部资源”并记录机器可见状态。实验固定源码、工具链、输入和环境，只改变一个条件；若实际轨迹偏离预测，就保存首个分叉、责任接口与恢复结果。

### 6. 回收全部资源

先标出逻辑流和共享状态，再用上下文、同步边和生命周期解释交错；安全性、活性和资源回收要分别验证。 对“12.2.1 基于I/O多路复用的并发事件驱动服务器”的学习不能停在定义：先在“回收全部资源”阶段写出输入和预测，再推进到“划分并发任务”并记录机器可见状态。实验固定源码、工具链、输入和环境，只改变一个条件；若实际轨迹偏离预测，就保存首个分叉、责任接口与恢复结果。

## 核心机制深挖

比较进程、I/O 多路复用和线程三种并发模型，以线程内存模型、进度图和信号量分析互斥、调度、竞争、可重入与死锁。 本页不把系统描述成若干孤立名词，而把每个概念放回“划分并发任务 → 选择进程事件或线程 → 标记共享状态 → 建立同步顺序 → 验证活性 → 回收全部资源”的状态机。输入包括源码、位模式、目标制品、地址、文件或网络字节；状态包括程序计数器、寄存器、缓存与页表、内核对象和同步关系；输出必须同时说明正常值、资源所有权和下一阶段可以依赖的承诺。

本页签发的不变量是：**共享变量的每次冲突访问都有同步顺序；互斥保护不变量，信号量维护资源计数，锁顺序保证系统既安全又能继续前进。**。不变量必须在转换前后都成立。只展示一次成功运行最多证明样本可用，不能证明边界安全；因此每项机制都配正常、边界、失败和恢复四条轨迹，并把“谁检测、谁清理、谁重试、谁向上报告”写进证据账本。

用三种模型实现同一 echo 服务，再用进度图枚举交错，注入竞争与锁顺序反转并观察吞吐、隔离和回收。 运行前先把预期写成状态快照，运行后逐字段比较。计时实验需要预热、重复和稳定输入；地址实验要记录位宽与权限；并发实验要记录同步边而非依赖偶然调度。这样才能把现象还原成可复查的因果链。

重复运行共享计数、生产者消费者和双锁样本，区分数据竞争、饥饿与死锁，并用同步证据而非睡眠延迟修复。 故障移除后必须用完全相同的输入重放，确认制品、机器状态、外部输出和清理结果一起恢复。若只修正最终输出，却留下描述符、子进程、堆块、锁或错误映射，验收仍然失败。

中文第三版采用 x86-64、Linux 和 Y86-64 教学处理器。现代 CPU、内核和工具链会改变微结构、默认安全机制与性能数值，但固定宽度表示、指令语义、缓存局部性、符号重定位、进程地址空间、字节流接口和同步不变量仍然成立。现代对照必须标明差异，不能覆盖原书问题身份。

## 正式目录逐项讲解

### 第12章 并发编程

先标出逻辑流和共享状态，再用上下文、同步边和生命周期解释交错；安全性、活性和资源回收要分别验证。 对“第12章 并发编程”的学习不能停在定义：先在“划分并发任务”阶段写出输入和预测，再推进到“选择进程事件或线程”并记录机器可见状态。实验固定源码、工具链、输入和环境，只改变一个条件；若实际轨迹偏离预测，就保存首个分叉、责任接口与恢复结果。

### 12.1 基于进程的并发编程

先标出逻辑流和共享状态，再用上下文、同步边和生命周期解释交错；安全性、活性和资源回收要分别验证。 对“12.1 基于进程的并发编程”的学习不能停在定义：先在“选择进程事件或线程”阶段写出输入和预测，再推进到“标记共享状态”并记录机器可见状态。实验固定源码、工具链、输入和环境，只改变一个条件；若实际轨迹偏离预测，就保存首个分叉、责任接口与恢复结果。

### 12.1.1 基于进程的并发服务器

先标出逻辑流和共享状态，再用上下文、同步边和生命周期解释交错；安全性、活性和资源回收要分别验证。 对“12.1.1 基于进程的并发服务器”的学习不能停在定义：先在“标记共享状态”阶段写出输入和预测，再推进到“建立同步顺序”并记录机器可见状态。实验固定源码、工具链、输入和环境，只改变一个条件；若实际轨迹偏离预测，就保存首个分叉、责任接口与恢复结果。

### 12.1.2 进程的优劣

先标出逻辑流和共享状态，再用上下文、同步边和生命周期解释交错；安全性、活性和资源回收要分别验证。 对“12.1.2 进程的优劣”的学习不能停在定义：先在“建立同步顺序”阶段写出输入和预测，再推进到“验证活性”并记录机器可见状态。实验固定源码、工具链、输入和环境，只改变一个条件；若实际轨迹偏离预测，就保存首个分叉、责任接口与恢复结果。

### 12.2 基于I/O多路复用的并发编程

先标出逻辑流和共享状态，再用上下文、同步边和生命周期解释交错；安全性、活性和资源回收要分别验证。 对“12.2 基于I/O多路复用的并发编程”的学习不能停在定义：先在“验证活性”阶段写出输入和预测，再推进到“回收全部资源”并记录机器可见状态。实验固定源码、工具链、输入和环境，只改变一个条件；若实际轨迹偏离预测，就保存首个分叉、责任接口与恢复结果。

### 12.2.1 基于I/O多路复用的并发事件驱动服务器

先标出逻辑流和共享状态，再用上下文、同步边和生命周期解释交错；安全性、活性和资源回收要分别验证。 对“12.2.1 基于I/O多路复用的并发事件驱动服务器”的学习不能停在定义：先在“回收全部资源”阶段写出输入和预测，再推进到“划分并发任务”并记录机器可见状态。实验固定源码、工具链、输入和环境，只改变一个条件；若实际轨迹偏离预测，就保存首个分叉、责任接口与恢复结果。

### 12.2.2 I/O多路复用的优劣

把字节流接口与应用协议分层，循环处理短计数、分段到达、EOF 和错误返回；每个描述符与连接都要有明确所有者。 对“12.2.2 I/O多路复用的优劣”的学习不能停在定义：先在“划分并发任务”阶段写出输入和预测，再推进到“选择进程事件或线程”并记录机器可见状态。实验固定源码、工具链、输入和环境，只改变一个条件；若实际轨迹偏离预测，就保存首个分叉、责任接口与恢复结果。

### 12.3 基于线程的并发编程

先标出逻辑流和共享状态，再用上下文、同步边和生命周期解释交错；安全性、活性和资源回收要分别验证。 对“12.3 基于线程的并发编程”的学习不能停在定义：先在“选择进程事件或线程”阶段写出输入和预测，再推进到“标记共享状态”并记录机器可见状态。实验固定源码、工具链、输入和环境，只改变一个条件；若实际轨迹偏离预测，就保存首个分叉、责任接口与恢复结果。

### 12.3.1 线程执行模型

先标出逻辑流和共享状态，再用上下文、同步边和生命周期解释交错；安全性、活性和资源回收要分别验证。 对“12.3.1 线程执行模型”的学习不能停在定义：先在“标记共享状态”阶段写出输入和预测，再推进到“建立同步顺序”并记录机器可见状态。实验固定源码、工具链、输入和环境，只改变一个条件；若实际轨迹偏离预测，就保存首个分叉、责任接口与恢复结果。

### 12.3.2 Posix线程

先标出逻辑流和共享状态，再用上下文、同步边和生命周期解释交错；安全性、活性和资源回收要分别验证。 对“12.3.2 Posix线程”的学习不能停在定义：先在“建立同步顺序”阶段写出输入和预测，再推进到“验证活性”并记录机器可见状态。实验固定源码、工具链、输入和环境，只改变一个条件；若实际轨迹偏离预测，就保存首个分叉、责任接口与恢复结果。

### 12.3.3 创建线程

先标出逻辑流和共享状态，再用上下文、同步边和生命周期解释交错；安全性、活性和资源回收要分别验证。 对“12.3.3 创建线程”的学习不能停在定义：先在“验证活性”阶段写出输入和预测，再推进到“回收全部资源”并记录机器可见状态。实验固定源码、工具链、输入和环境，只改变一个条件；若实际轨迹偏离预测，就保存首个分叉、责任接口与恢复结果。

### 12.3.4 终止线程

先标出逻辑流和共享状态，再用上下文、同步边和生命周期解释交错；安全性、活性和资源回收要分别验证。 对“12.3.4 终止线程”的学习不能停在定义：先在“回收全部资源”阶段写出输入和预测，再推进到“划分并发任务”并记录机器可见状态。实验固定源码、工具链、输入和环境，只改变一个条件；若实际轨迹偏离预测，就保存首个分叉、责任接口与恢复结果。

### 12.3.5 回收已终止的线程

先标出逻辑流和共享状态，再用上下文、同步边和生命周期解释交错；安全性、活性和资源回收要分别验证。 对“12.3.5 回收已终止的线程”的学习不能停在定义：先在“划分并发任务”阶段写出输入和预测，再推进到“选择进程事件或线程”并记录机器可见状态。实验固定源码、工具链、输入和环境，只改变一个条件；若实际轨迹偏离预测，就保存首个分叉、责任接口与恢复结果。

### 12.3.6 分离线程

先标出逻辑流和共享状态，再用上下文、同步边和生命周期解释交错；安全性、活性和资源回收要分别验证。 对“12.3.6 分离线程”的学习不能停在定义：先在“选择进程事件或线程”阶段写出输入和预测，再推进到“标记共享状态”并记录机器可见状态。实验固定源码、工具链、输入和环境，只改变一个条件；若实际轨迹偏离预测，就保存首个分叉、责任接口与恢复结果。

### 12.3.7 初始化线程

先标出逻辑流和共享状态，再用上下文、同步边和生命周期解释交错；安全性、活性和资源回收要分别验证。 对“12.3.7 初始化线程”的学习不能停在定义：先在“标记共享状态”阶段写出输入和预测，再推进到“建立同步顺序”并记录机器可见状态。实验固定源码、工具链、输入和环境，只改变一个条件；若实际轨迹偏离预测，就保存首个分叉、责任接口与恢复结果。

### 12.3.8 基于线程的并发服务器

先标出逻辑流和共享状态，再用上下文、同步边和生命周期解释交错；安全性、活性和资源回收要分别验证。 对“12.3.8 基于线程的并发服务器”的学习不能停在定义：先在“建立同步顺序”阶段写出输入和预测，再推进到“验证活性”并记录机器可见状态。实验固定源码、工具链、输入和环境，只改变一个条件；若实际轨迹偏离预测，就保存首个分叉、责任接口与恢复结果。

### 12.4 多线程程序中的共享变量

先标出逻辑流和共享状态，再用上下文、同步边和生命周期解释交错；安全性、活性和资源回收要分别验证。 对“12.4 多线程程序中的共享变量”的学习不能停在定义：先在“验证活性”阶段写出输入和预测，再推进到“回收全部资源”并记录机器可见状态。实验固定源码、工具链、输入和环境，只改变一个条件；若实际轨迹偏离预测，就保存首个分叉、责任接口与恢复结果。

### 12.4.1 线程内存模型

先标出逻辑流和共享状态，再用上下文、同步边和生命周期解释交错；安全性、活性和资源回收要分别验证。 对“12.4.1 线程内存模型”的学习不能停在定义：先在“回收全部资源”阶段写出输入和预测，再推进到“划分并发任务”并记录机器可见状态。实验固定源码、工具链、输入和环境，只改变一个条件；若实际轨迹偏离预测，就保存首个分叉、责任接口与恢复结果。

### 12.4.2 将变量映射到内存

比较进程、I/O 多路复用和线程三种并发模型，以线程内存模型、进度图和信号量分析互斥、调度、竞争、可重入与死锁。 对“12.4.2 将变量映射到内存”的学习不能停在定义：先在“划分并发任务”阶段写出输入和预测，再推进到“选择进程事件或线程”并记录机器可见状态。实验固定源码、工具链、输入和环境，只改变一个条件；若实际轨迹偏离预测，就保存首个分叉、责任接口与恢复结果。

### 12.4.3 共享变量

比较进程、I/O 多路复用和线程三种并发模型，以线程内存模型、进度图和信号量分析互斥、调度、竞争、可重入与死锁。 对“12.4.3 共享变量”的学习不能停在定义：先在“选择进程事件或线程”阶段写出输入和预测，再推进到“标记共享状态”并记录机器可见状态。实验固定源码、工具链、输入和环境，只改变一个条件；若实际轨迹偏离预测，就保存首个分叉、责任接口与恢复结果。

### 12.5 用信号量同步线程

先标出逻辑流和共享状态，再用上下文、同步边和生命周期解释交错；安全性、活性和资源回收要分别验证。 对“12.5 用信号量同步线程”的学习不能停在定义：先在“标记共享状态”阶段写出输入和预测，再推进到“建立同步顺序”并记录机器可见状态。实验固定源码、工具链、输入和环境，只改变一个条件；若实际轨迹偏离预测，就保存首个分叉、责任接口与恢复结果。

### 12.5.1 进度图

比较进程、I/O 多路复用和线程三种并发模型，以线程内存模型、进度图和信号量分析互斥、调度、竞争、可重入与死锁。 对“12.5.1 进度图”的学习不能停在定义：先在“建立同步顺序”阶段写出输入和预测，再推进到“验证活性”并记录机器可见状态。实验固定源码、工具链、输入和环境，只改变一个条件；若实际轨迹偏离预测，就保存首个分叉、责任接口与恢复结果。

### 12.5.2 信号量

先标出逻辑流和共享状态，再用上下文、同步边和生命周期解释交错；安全性、活性和资源回收要分别验证。 对“12.5.2 信号量”的学习不能停在定义：先在“验证活性”阶段写出输入和预测，再推进到“回收全部资源”并记录机器可见状态。实验固定源码、工具链、输入和环境，只改变一个条件；若实际轨迹偏离预测，就保存首个分叉、责任接口与恢复结果。

### 12.5.3 使用信号量实现互斥

先标出逻辑流和共享状态，再用上下文、同步边和生命周期解释交错；安全性、活性和资源回收要分别验证。 对“12.5.3 使用信号量实现互斥”的学习不能停在定义：先在“回收全部资源”阶段写出输入和预测，再推进到“划分并发任务”并记录机器可见状态。实验固定源码、工具链、输入和环境，只改变一个条件；若实际轨迹偏离预测，就保存首个分叉、责任接口与恢复结果。

### 12.5.4 利用信号量来调度共享资源

先标出逻辑流和共享状态，再用上下文、同步边和生命周期解释交错；安全性、活性和资源回收要分别验证。 对“12.5.4 利用信号量来调度共享资源”的学习不能停在定义：先在“划分并发任务”阶段写出输入和预测，再推进到“选择进程事件或线程”并记录机器可见状态。实验固定源码、工具链、输入和环境，只改变一个条件；若实际轨迹偏离预测，就保存首个分叉、责任接口与恢复结果。

### 12.5.5 综合：基于预线程化的并发服务器

先标出逻辑流和共享状态，再用上下文、同步边和生命周期解释交错；安全性、活性和资源回收要分别验证。 对“12.5.5 综合：基于预线程化的并发服务器”的学习不能停在定义：先在“选择进程事件或线程”阶段写出输入和预测，再推进到“标记共享状态”并记录机器可见状态。实验固定源码、工具链、输入和环境，只改变一个条件；若实际轨迹偏离预测，就保存首个分叉、责任接口与恢复结果。

### 12.6 使用线程提高并行性

先标出逻辑流和共享状态，再用上下文、同步边和生命周期解释交错；安全性、活性和资源回收要分别验证。 对“12.6 使用线程提高并行性”的学习不能停在定义：先在“标记共享状态”阶段写出输入和预测，再推进到“建立同步顺序”并记录机器可见状态。实验固定源码、工具链、输入和环境，只改变一个条件；若实际轨迹偏离预测，就保存首个分叉、责任接口与恢复结果。

### 12.7 其他并发问题

先标出逻辑流和共享状态，再用上下文、同步边和生命周期解释交错；安全性、活性和资源回收要分别验证。 对“12.7 其他并发问题”的学习不能停在定义：先在“建立同步顺序”阶段写出输入和预测，再推进到“验证活性”并记录机器可见状态。实验固定源码、工具链、输入和环境，只改变一个条件；若实际轨迹偏离预测，就保存首个分叉、责任接口与恢复结果。

### 12.7.1 线程安全

先标出逻辑流和共享状态，再用上下文、同步边和生命周期解释交错；安全性、活性和资源回收要分别验证。 对“12.7.1 线程安全”的学习不能停在定义：先在“验证活性”阶段写出输入和预测，再推进到“回收全部资源”并记录机器可见状态。实验固定源码、工具链、输入和环境，只改变一个条件；若实际轨迹偏离预测，就保存首个分叉、责任接口与恢复结果。

### 12.7.2 可重入性

比较进程、I/O 多路复用和线程三种并发模型，以线程内存模型、进度图和信号量分析互斥、调度、竞争、可重入与死锁。 对“12.7.2 可重入性”的学习不能停在定义：先在“回收全部资源”阶段写出输入和预测，再推进到“划分并发任务”并记录机器可见状态。实验固定源码、工具链、输入和环境，只改变一个条件；若实际轨迹偏离预测，就保存首个分叉、责任接口与恢复结果。

### 12.7.3 在线程化的程序中使用已存在的库函数

先标出逻辑流和共享状态，再用上下文、同步边和生命周期解释交错；安全性、活性和资源回收要分别验证。 对“12.7.3 在线程化的程序中使用已存在的库函数”的学习不能停在定义：先在“划分并发任务”阶段写出输入和预测，再推进到“选择进程事件或线程”并记录机器可见状态。实验固定源码、工具链、输入和环境，只改变一个条件；若实际轨迹偏离预测，就保存首个分叉、责任接口与恢复结果。

### 12.7.4 竞争

比较进程、I/O 多路复用和线程三种并发模型，以线程内存模型、进度图和信号量分析互斥、调度、竞争、可重入与死锁。 对“12.7.4 竞争”的学习不能停在定义：先在“选择进程事件或线程”阶段写出输入和预测，再推进到“标记共享状态”并记录机器可见状态。实验固定源码、工具链、输入和环境，只改变一个条件；若实际轨迹偏离预测，就保存首个分叉、责任接口与恢复结果。

### 12.7.5 死锁

先标出逻辑流和共享状态，再用上下文、同步边和生命周期解释交错；安全性、活性和资源回收要分别验证。 对“12.7.5 死锁”的学习不能停在定义：先在“标记共享状态”阶段写出输入和预测，再推进到“建立同步顺序”并记录机器可见状态。实验固定源码、工具链、输入和环境，只改变一个条件；若实际轨迹偏离预测，就保存首个分叉、责任接口与恢复结果。

### 12.8 小结

这一小结不是术语列表，而是检查本章不变量“共享变量的每次冲突访问都有同步顺序；互斥保护不变量，信号量维护资源计数，锁顺序保证系统既安全又能继续前进。”是否已由正常、边界、失败和恢复四类样本共同支持。 对“12.8 小结”的学习不能停在定义：先在“建立同步顺序”阶段写出输入和预测，再推进到“验证活性”并记录机器可见状态。实验固定源码、工具链、输入和环境，只改变一个条件；若实际轨迹偏离预测，就保存首个分叉、责任接口与恢复结果。

## 状态与失败矩阵

| 观察层     | 正常轨迹             | 边界样本             | 失败证据             | 恢复条件               |
| ---------- | -------------------- | -------------------- | -------------------- | ---------------------- |
| 表示与制品 | 位宽、格式、符号明确 | 溢出、截断、缺失定义 | 字节、节与重定位位置 | 使用同一可追溯制品重放 |
| 机器与存储 | 指令和地址映射有效   | 冒险、未命中、缺页   | 首个寄存器或地址分叉 | 重置到已知机器状态     |
| 进程与资源 | 所有权和生命周期闭合 | 短计数、信号、容量   | 系统调用和资源快照   | 逆序清理后重试         |
| 通信与并发 | 协议和同步边完整     | 断连、交错、竞争     | 字节流、锁与进度图   | 相同输入可重复通过     |

先预测：如果在“标记共享状态”注入失败，哪一个状态字段应最先变化，后续哪一阶段必须停止提交副作用？完成预测后再操作实验控件。

## 可重放实验记录

```text
trace = []
for stage in ["划分并发任务", "选择进程事件或线程", "标记共享状态", "建立同步顺序", "验证活性", "回收全部资源"]:
  expected = predict(stage, state)
  actual = execute(stage, state)
  trace.append({ stage, expected, actual, owner, cleanup })
  stop_at_first_divergence(expected, actual)
```

```yaml
book: 深入理解计算机系统
edition: 中文第三版-2016-12
page: cap-12-concurrent-programming
formal_scope: 第 12 章 并发编程
sample: normal | boundary | failure | recovery
predict_before_run: true
capture_first_divergence: true
replay_same_input: true
```

```text
freeze source, toolchain, input, and environment
record bytes, addresses, machine state, resources, and synchronization
change exactly one condition
stop at the first divergent boundary
remove the fault and replay the same input
verify output and cleanup together
```

## 常见误区与故障注入


## 四类样本与验收

| 样本 | 注入方式                   | 必查证据               | 通过条件     |
| ---- | -------------------------- | ---------------------- | ------------ |
| 正常 | 固定源码、工具链和输入     | 全阶段状态与输出       | 与预测一致   |
| 边界 | 极值、空输入、低容量       | 范围、终止与资源状态   | 不越界不泄漏 |
| 失败 | 错地址、缺依赖、断连或竞争 | 首个错误与责任层       | 失败被隔离   |
| 恢复 | 删除故障后重放             | 制品、状态、输出和清理 | 回到受控结果 |

## 练习


## 小结

- 并发流：连接“划分并发任务”的输入与状态。
- I/O 多路复用：连接“选择进程事件或线程”的责任接口。
- 线程：连接“标记共享状态”的边界条件。
- 信号量：连接“建立同步顺序”的失败传播。
- 死锁：连接“验证活性”的恢复证据。
- 34个正式目录坐标已进入本页或全书运行链，不以泛化主题替代原书章节。
- 最终输出只是证据之一，制品、机器状态、资源和清理共同决定是否通过。

## 术语表


