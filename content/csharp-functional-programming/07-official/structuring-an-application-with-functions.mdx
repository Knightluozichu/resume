---
title: "Chapter 7. Structuring an application with functions"
type: C
section: "Functional C# 1e · Becoming functional"
order: 7
description: "覆盖partial application、method resolution、currying、partial-friendly API、application modularization与fold，把dependency wiring变成typed function construction。"
demo: true
math: false
sourceUrl: "https://livebook.manning.com/book/functional-programming-in-c-sharp/chapter-7"
draft: true
---

import { CfpApplicationShapeLab, CfpFoldArchitectureLab, CfpPartialFriendlyApiLab } from "@/components/mdx/csharp-functional-programming/diagrams/structuring-an-application-with-functions";

<Objectives>

- 能区分partial application与currying，推导固定stable dependencies后得到的workflow signature
- 能分析C# overload/method-group inference与parameter order，设计partial-application-friendly API
- 能设计composition root、module functions和Fold reduction，复现empty/list/state aggregation semantics

</Objectives>

## 为什么Application Structure可以是Function Construction

传统application用mutable service objects保存dependencies；functional approach也需要dependency wiring，只是把dependencies作为parameters，再在startup部分应用，得到只接request的function。最终handler仍可交给framework，core无需service locator。

先预测：partial application是否等于currying；任何method group都可自动partial；dependency放最后是否一样好；closure捕获singleton是否没有lifetime；Aggregate无seed是否总安全。答案都是否。

<Term def="固定multi-argument function的一部分arguments，产生接收剩余arguments的新function。">partial application</Term>

## Partial application: supplying arguments piecemeal

给`(Config, Store, Request) -> Response`先传Config/Store，得到`Request -> Response`。稳定dependencies在composition root固定，变化data在runtime传入。Closure保存dependency references，因此lifetime必须不短于result function；不要把request-scoped resource捕获进singleton handler。

```csharp
Func<Request, Response> BuildHandler(Config config, IStore store) =>
    request => Handle(config, store, request);
```

<Stepper>
  <Step title="切换partial、currying、method group与closure"><CfpApplicationShapeLab /></Step>
  <Step title="写出每次application后的type">从multi-arg到剩余signature，标captured dependency及其scope。</Step>
  <Step title="用scope与substitution验收">替换store/policy，销毁request scope，确认handler不保留短生命周期对象。</Step>
</Stepper>

## Overcoming the quirks of method resolution

C# method group不是独立value，需target delegate决定overload/generic inference。Partial helper遇overloaded method时可能ambiguous；用显式`Func<...>`、adapter lambda或domain-named wrapper固定signature。不要用大量casts掩盖API shape不清。

新增overload可能让原本可编译的method group在rebuild后变得ambiguous；参数改名也会破坏named arguments。对公共composition boundary定义稳定delegate alias和named adapter，并用compile fixture覆盖旧调用形状，API演进时先证明resolution仍唯一。

<Term def="Method group需要target delegate、overload与generic inference共同确定唯一callable signature的边界。">method-resolution boundary</Term>

## Curried functions: optimized for partial application

Currying把`(A,B,C)->R`转换为`A->B->C->R`，每次application自然返回下一function。Partial application是使用行为，currying是function representation transformation。C#原生method通常uncurried，library helper可Curry/Apply；过度nested Func会损害debug/readability。

```csharp
Func<TaxPolicy, Func<Currency, Func<Order, Price>>> price =
    tax => currency => order => Price.Calculate(order, tax, currency);

Func<Order, Price> priceUsd = price(usTax)(Currency.Usd);
```

<Term def="把n-argument function转换为一连串single-argument functions的representation transformation。">curried function shape</Term>

## Creating a partial-application-friendly API

参数顺序优先stable dependency/config，再放per-call data。避免optional flags和同type primitive；给overload提供one canonical function shape。Async/cancellation通常属于per-call context，不能在startup固定一个过期token。

<Stepper>
  <Step title="切换dependency-first、data-first、overload与named function"><CfpPartialFriendlyApiLab /></Step>
  <Step title="按change frequency与lifetime排序">Startup config、singleton policy、request scope和per-item data分别标出。</Step>
  <Step title="编译composition call sites">检查type inference、方法名可读性、token传递和旧consumer compatibility。</Step>
</Stepper>

<Callout type="trap" title="把所有Dependencies都Capture就等于Dependency Injection">
Capture可能把scoped resource提升到singleton lifetime，也隐藏Dispose/cancellation。Composition root只固定lifetime-compatible factories/policies；短scope在per-call shell创建并释放。
</Callout>

## Modularizing and composing an application

Module可以暴露builders/functions：ValidationModule.Build(config)、Workflow.Build(validate, decide, execute)。每个module返回最小delegate contract，composition root集中选择implementations。Domain workflow不依赖container APIs，test传入pure/fake functions。

```csharp
var validate = Validation.Build(rules);
var decide = Pricing.Build(taxPolicy);
var execute = Execution.Build(storeFactory, clock);
var handler = Workflow.Build(validate, decide, execute);
```

<Term def="Startup处集中选择implementations、匹配lifetimes并部分应用dependencies，产出最终entry functions的位置。">functional composition root</Term>

## Reducing a list to a single value

Fold/Aggregate以seed和step把sequence归约为一个value。Seed定义empty result与accumulator type；step应明确ordering、associativity与effect。Sum、state replay、function composition都可视为fold。无seed Aggregate对empty partial，不适合需要total behavior的public workflow。

<Stepper>
  <Step title="切换sum、state、compose与empty"><CfpFoldArchitectureLab /></Step>
  <Step title="写出seed和step law">Identity是什么，step是否associative，ordering是否业务语义，能否parallel。</Step>
  <Step title="用empty/one/many/reorder验收">验证empty totality、single identity、many result与parallel/reorder是否允许。</Step>
</Stepper>

<Callout type="trap" title="Aggregate只是一个更Functional的For Loop">
Fold的价值是显式seed/state transition与laws；若step修改external state或依赖order却未说明，只是把imperative loop藏进lambda。返回new accumulator并测empty/reorder。
</Callout>

<Term def="从seed开始，用step逐项产生新accumulator并最终得到单一value的sequence reduction。">fold reduction</Term>

## 本章回顾：在Startup构造Behavior，在Runtime提供Data

1. Partial application固定部分arguments；currying改变multi-arg representation。
2. C# method groups需target type，overload ambiguity用adapter/canonical signature解决。
3. Dependency-first order让composition root自然构造request handlers。
4. Captured dependencies必须满足function lifetime，scoped resources使用factory/per-call owner。
5. Fold以seed和step表达total reduction及其laws。

<Exercises>

**问题 1：为什么不应在singleton handler中capture DbContext？**

<Answer>DbContext通常scoped且非线程安全，closure延长lifetime并跨requests共享。Capture scope factory/repository function，每次call创建/dispose context；测试并发、scope end、fault/cancel cleanup。</Answer>

**问题 2：Overloaded Parse无法传给Map时如何修？**

<Answer>用显式`Func<string, Result>` target或named adapter `ParseOrderId`选定overload，避免散落casts。若public API频繁ambiguous，提供canonical non-overloaded function shape并做compile cases。</Answer>

**问题 3：Event fold为什么必须有initial state？**

<Answer>Empty stream也需定义aggregate state；seed使fold total并明确version/default。测试empty、one/many、invalid event order和snapshot+tail equivalence；无seed Aggregate会在empty抛异常。</Answer>

</Exercises>

## 术语表

<Glossary>
  <GlossaryItem term="partial application" definition="固定部分arguments并返回剩余signature。" />
  <GlossaryItem term="method-resolution boundary" definition="Target delegate决定method group overload/inference的边界。" />
  <GlossaryItem term="curried function shape" definition="A-to-B-to-C-to-R的nested single-argument表示。" />
  <GlossaryItem term="functional composition root" definition="集中wiring/lifetime并构造entry functions的位置。" />
  <GlossaryItem term="fold reduction" definition="以seed和step把sequence归约为single value。" />
</Glossary>

<Attribution adaptedFrom="Functional Programming in C#, First Edition, Chapter 7: Structuring an application with functions" adaptedUrl="https://livebook.manning.com/book/functional-programming-in-c-sharp/chapter-7" />
