---
title: "第 1 章 引言"
type: A
section: "CSS 揭秘 · 方法论"
order: 1
description: "把 CSS 技巧视为受规范、回退路径和维护成本约束的问题求解，而不是记忆一次性代码片段。"
demo: true
math: false
sourceUrl: "https://www.oreilly.com/library/view/css-secrets/9781449372736/toc.html"
draft: false
---


## 直觉起点

、、、、共同构成本页的观察坐标。把 CSS
技巧视为受规范、回退路径和维护成本约束的问题求解，而不是记忆一次性代码片段。本页覆盖
3 个公开目录条目。《CSS
揭秘》的价值不只是给出效果代码，而是展示怎样从限制条件中找到更少依赖的机制；所以每个示例都必须同时保留基线、增强和失败解释。

## 六阶段解题链

### 1. 定义视觉问题

先写视觉与交互契约，不写具体属性。固定 DOM、文本、视口、字体和输入方式，预测无增强样式时应保留的内容、尺寸、焦点和阅读顺序。

### 2. 调查标准能力

查明目标效果属于盒生成、背景绘制、边框、滤镜、字体、布局还是合成阶段。只选能直接解释目标的机制，并注明规范能力与实现边界。

### 3. 建立最小基线

构造最小代码，删除重复数值、镜像状态和额外 DOM。先预测计算值与绘制区域，再运行正常样本，记录首个产生目标效果的声明。

### 4. 组合属性副作用

只改变一个变量，例如容器宽度、文本长度、背景、字体、输入方式或动态偏好。停止在首个偏离点，不用更多覆盖把失败截图修平。

### 5. 设计回退路径

关闭关键能力，检查自然回退是否仍可读、可操作、可布局。增强方案不能反向破坏基线，也不能以颜色、动画或装饰替代语义。

### 6. 删除重复约束

清除故障后重放同一 DOM，确认没有旧内联样式、重复常量、隐藏焦点或层叠残留。把通过条件写成以后可以自动或人工复查的证据。

## 核心机制深挖

把 CSS 技巧视为受规范、回退路径和维护成本约束的问题求解，而不是记忆一次性代码片段。 解题时先区分**结构事实、计算样式、绘制结果和交互状态**：结构决定语义与可访问名称，级联和布局产生计算值与使用值，背景、边框、阴影、滤镜再参与绘制，动画与输入只改变状态。把这些阶段混在一起，往往会用多余 DOM 修背景、用固定尺寸修布局、用不可见原生控件修状态，最终难以维护。

本页签发不变量是：**解决方案必须说明依赖的规范能力、基线表现、增强效果与失败时的可用状态。** 对每个方案都要问四个问题：不支持高级能力时剩下什么；内容变长时谁负责尺寸；键盘与触摸是否仍能完成任务；删除故障声明后是否回到同一基线。只有四问都有可复现答案，技巧才从演示升级为工程方案。

## 标准与实现的决策记录

遇到浏览器结果不一致时，先保存一个只含必要 DOM 和声明的复现页，再把现象归入三类。第一类是**规范允许多个结果**，此时作者不能把其中一个结果当成契约，应增加明确约束；第二类是**规范已经定义但实现尚未支持**，此时用自然基线和功能查询组织渐进增强；第三类是**实现偏离规范**，此时记录浏览器版本、最小触发条件和删除条件，临时规避代码必须集中且可被测试。三类问题不能都用浏览器嗅探处理，否则旧补丁会在实现修复后继续干扰正确路径。

DRY 也不是单纯追求更少字符，而是让同一设计事实只由一个来源表达。颜色可由 `currentColor` 传播，尺度可用 `em` 跟随字号，共享间距可进入自定义属性，组件状态则应由真实控件属性驱动。若两个数值当前相等但代表不同约束，不应为了表面复用强行绑定；反之，若同一颜色或半径散落在多条规则中，即使代码仍能运行，也已经形成修改时可能分叉的风险。

每个技巧最后都要留下简短决策记录：目标问题、采用机制、拒绝的备选方案、最低可用基线、增强检测方式、边界样本和未来删除条件。这样读者看到的不是脱离上下文的属性组合，而是一条可以迁移到新问题的推理链；浏览器能力变化时，也能从记录中判断应保留原方案、替换为现代属性，还是删除已经失效的兼容层。

## 出版社目录逐项讲解

### 第1章 引言

本章先建立全书的求解方法：从真实问题出发，查询规范与兼容边界，保留简单基线，再把属性的既有副作用组合成更轻、更稳的方案。重点不是收集技巧，而是学会解释为什么一个方案满足 DRY、可维护、可扩展和标准兼容。

证据记录从“定义视觉问题”开始：先写出未增强时的可用结果，再只加入一个 CSS 能力，比较计算样式、绘制边界、内容语义、输入状态和失败回退。若方案需要复制尺寸、隐藏真实控件、依赖固定内容或无法解释的层叠补丁，就回到最小样本重做。

### Web 标准：是敌还是友

标准既限制可承诺的行为，也提供跨实现的共同语言。先区分规范已定义、实现尚未支持和实现存在缺陷三类情况；用功能查询与自然回退渐进增强，不按某个浏览器截图推断标准，也不把厂商前缀当永久接口。

证据记录从“调查标准能力”开始：先写出未增强时的可用结果，再只加入一个 CSS 能力，比较计算样式、绘制边界、内容语义、输入状态和失败回退。若方案需要复制尺寸、隐藏真实控件、依赖固定内容或无法解释的层叠补丁，就回到最小样本重做。

### CSS 编码技巧

编码时优先使用相对单位、currentColor、inherit 和可复用的自定义属性，让样式从上下文获得颜色与尺度。减少重复数值、避免覆盖链和不必要的简写重置；每次优化都要比较规则数量、依赖关系、失败表现和阅读成本。

证据记录从“建立最小基线”开始：先写出未增强时的可用结果，再只加入一个 CSS 能力，比较计算样式、绘制边界、内容语义、输入状态和失败回退。若方案需要复制尺寸、隐藏真实控件、依赖固定内容或无法解释的层叠补丁，就回到最小样本重做。

## 方案比较矩阵

| 方案       | 依赖                       | 优点             | 主要风险             | 验收重点                |
| ---------- | -------------------------- | ---------------- | -------------------- | ----------------------- |
| 基线       | 正常流、实体颜色、原生语义 | 最稳定、自然回退 | 视觉较简单           | 无 CSS 增强仍可读可操作 |
| 单属性增强 | 圆角、阴影、渐变或相对单位 | 代码少、意图清楚 | 绘制区域判断错误     | 改背景、尺寸和内容      |
| 多层组合   | 多背景、伪元素、滤镜或变换 | 表达复杂效果     | 层叠、裁剪和性能     | 关闭一层仍能定位根因    |
| 动态增强   | 状态选择器、过渡、动画     | 反馈连续         | 键盘、减少动态和中断 | 快速反转与静态回退      |

## 最小可运行实验

```css
.button {
  color: #2563eb;
  border: 1px solid currentColor;
  padding: 0.45em 0.8em;
  background: transparent;
}
.button:focus-visible {
  outline: 0.18em solid currentColor;
  outline-offset: 0.18em;
}
```

```html
<article class="secret component" data-enhanced>
  <button class="action" type="button">验证基线与增强</button>
  <p>用长内容、窄容器、键盘和能力缺失重放同一结构。</p>
</article>
```

```yaml
book: CSS 揭秘
page: csec-01-introduction
sample: baseline | enhanced | failure | recovery
toc_entries: 3
semantic_content_preserved: true
fallback_operable: true
first_divergence: null
```

## 常见误区与故障注入


## 四类样本与验收

| 样本 | 注入方式                             | 必查证据                 | 通过条件               |
| ---- | ------------------------------------ | ------------------------ | ---------------------- |
| 基线 | 关闭高级绘制或动画能力               | 内容、原生语义、正常流   | 信息与操作完整         |
| 增强 | 只开启目标机制                       | 计算值、绘制区、声明数量 | 目标效果由最少机制产生 |
| 失败 | 窄宽、长内容、键盘、换背景或快速反转 | 首个偏离点               | 能解释并移除根因       |
| 恢复 | 删除故障后重放同一 DOM               | 状态、尺寸、焦点、层叠   | 无残留覆盖且基线一致   |

目录证据：第1章 引言、Web 标准：是敌还是友、CSS 编码技巧。本页不复制原书正文，而是按公开目录独立重构问题、机制、实验与验收，确保每个标题都有可执行学习证据。

## 练习


## 小结

- Web 标准：连接“定义视觉问题”与规范依赖。
- DRY：连接“调查标准能力”与最小基线。
- 回退：连接“建立最小基线”与增强机制。
- 相对单位：连接“组合属性副作用”与边界故障。
- currentColor：连接“设计回退路径”与恢复证据。
- 3 个本页目录条目已经全部进入问题、机制、实现与验收链。
- 现代便利写法只作对照，原书 2015 年方案的机制身份与回退路径保持清楚。

## 术语表


