---
title: "第 5 章 字体排印"
type: A
section: "CSS 揭秘 · 排印与体验"
order: 5
description: "把字体度量、语言、行框和装饰绘制作为同一排印系统，先保证可读文本，再渐进增强连字、下划线和路径效果。"
demo: true
math: false
sourceUrl: "https://www.oreilly.com/library/view/css-secrets/9781449372736/toc.html"
draft: true
---

import {
  Csec05TypographyMapLab,
  Csec05TypographyExperimentLab,
  Csec05TypographyEvidenceLab,
} from "@/components/mdx/css-secrets/diagrams/csec-05-typography";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第 5 章 字体排印的核心问题，并说出它在原书 8 章、47 个攻略体系中的位置
- 能描述本页公开目录条目的问题、CSS 机制、最小实现与自然回退
- 能实现一个正常样本，并用能力缺失、窄宽、长内容或输入变化暴露失败边界
- 能比较基线、增强、失败和恢复四条轨迹，判断方案是否 DRY、可维护且符合标准

</Objectives>

## 直觉起点

<Term def="hyphens 是“第 5 章 字体排印”的第 1 个观察量，对应“声明语言与字体”；验收时同时记录规范依赖、基线、增强、边界和回退。">hyphens</Term>、<Term def="white-space 是“第 5 章 字体排印”的第 2 个观察量，对应“稳定行框节奏”；验收时同时记录规范依赖、基线、增强、边界和回退。">white-space</Term>、<Term def="tab-size 是“第 5 章 字体排印”的第 3 个观察量，对应“控制断行空白”；验收时同时记录规范依赖、基线、增强、边界和回退。">tab-size</Term>、<Term def="font-variant 是“第 5 章 字体排印”的第 4 个观察量，对应“启用字形特性”；验收时同时记录规范依赖、基线、增强、边界和回退。">font-variant</Term>、<Term def="text-decoration 是“第 5 章 字体排印”的第 5 个观察量，对应“叠加文本装饰”；验收时同时记录规范依赖、基线、增强、边界和回退。">text-decoration</Term>
共同构成本页的观察坐标。把字体度量、语言、行框和装饰绘制作为同一排印系统，先保证可读文本，再渐进增强连字、下划线和路径效果。本页覆盖
10 个公开目录条目。《CSS
揭秘》的价值不只是给出效果代码，而是展示怎样从限制条件中找到更少依赖的机制；所以每个示例都必须同时保留基线、增强和失败解释。

<Csec05TypographyMapLab />

## 六阶段解题链

### 1. 声明语言与字体

先写视觉与交互契约，不写具体属性。固定 DOM、文本、视口、字体和输入方式，预测无增强样式时应保留的内容、尺寸、焦点和阅读顺序。

### 2. 稳定行框节奏

查明目标效果属于盒生成、背景绘制、边框、滤镜、字体、布局还是合成阶段。只选能直接解释目标的机制，并注明规范能力与实现边界。

### 3. 控制断行空白

构造最小代码，删除重复数值、镜像状态和额外 DOM。先预测计算值与绘制区域，再运行正常样本，记录首个产生目标效果的声明。

### 4. 启用字形特性

只改变一个变量，例如容器宽度、文本长度、背景、字体、输入方式或动态偏好。停止在首个偏离点，不用更多覆盖把失败截图修平。

### 5. 叠加文本装饰

关闭关键能力，检查自然回退是否仍可读、可操作、可布局。增强方案不能反向破坏基线，也不能以颜色、动画或装饰替代语义。

### 6. 验证复制与缩放

清除故障后重放同一 DOM，确认没有旧内联样式、重复常量、隐藏焦点或层叠残留。把通过条件写成以后可以自动或人工复查的证据。

## 核心机制深挖

把字体度量、语言、行框和装饰绘制作为同一排印系统，先保证可读文本，再渐进增强连字、下划线和路径效果。 解题时先区分**结构事实、计算样式、绘制结果和交互状态**：结构决定语义与可访问名称，级联和布局产生计算值与使用值，背景、边框、阴影、滤镜再参与绘制，动画与输入只改变状态。把这些阶段混在一起，往往会用多余 DOM 修背景、用固定尺寸修布局、用不可见原生控件修状态，最终难以维护。

本页签发不变量是：**排印增强不能损害文本语义、复制顺序、语言断行和缩放可读性，缺少高级字体能力时仍保留正文。** 对每个方案都要问四个问题：不支持高级能力时剩下什么；内容变长时谁负责尺寸；键盘与触摸是否仍能完成任务；删除故障声明后是否回到同一基线。只有四问都有可复现答案，技巧才从演示升级为工程方案。

## 出版社目录逐项讲解

### 第5章 字体排印

本章把文本内容、语言元数据、字体字形、行框和装饰层连成证据链。CSS 可以增强断词、连字和下划线，但不可把关键文字只放在伪元素或路径效果里；复制、朗读和缩放始终是验收条件。

证据记录从“声明语言与字体”开始：先写出未增强时的可用结果，再只加入一个 CSS 能力，比较计算样式、绘制边界、内容语义、输入状态和失败回退。若方案需要复制尺寸、隐藏真实控件、依赖固定内容或无法解释的层叠补丁，就回到最小样本重做。

### 20 连字符断行

hyphens: auto 依赖正确的 lang 属性和浏览器词典，在窄列中减少参差空白。手动软连字符只适合少量专有词；测试不同语言、复制结果和不支持词典的环境，回退应是普通换行而非截断。

证据记录从“稳定行框节奏”开始：先写出未增强时的可用结果，再只加入一个 CSS 能力，比较计算样式、绘制边界、内容语义、输入状态和失败回退。若方案需要复制尺寸、隐藏真实控件、依赖固定内容或无法解释的层叠补丁，就回到最小样本重做。

### 21 插入换行

描述列表等结构可用伪元素生成换行，但生成内容不应承载核心语义。更稳的方法是让真实结构决定分组，再以 display 或 white-space 控制布局；关闭样式后顺序仍需可读，窄宽下不产生孤立标点。

证据记录从“控制断行空白”开始：先写出未增强时的可用结果，再只加入一个 CSS 能力，比较计算样式、绘制边界、内容语义、输入状态和失败回退。若方案需要复制尺寸、隐藏真实控件、依赖固定内容或无法解释的层叠补丁，就回到最小样本重做。

### 22 文本行的斑马条纹

重复线性渐变以两倍 line-height 为周期，可在背景上交替标记文本行。行高与背景周期必须共享变量，字体缩放或段内不同字号会破坏对齐；该效果只作阅读辅助，不能替代真实表格行语义。

证据记录从“启用字形特性”开始：先写出未增强时的可用结果，再只加入一个 CSS 能力，比较计算样式、绘制边界、内容语义、输入状态和失败回退。若方案需要复制尺寸、隐藏真实控件、依赖固定内容或无法解释的层叠补丁，就回到最小样本重做。

### 23 调整 tab 的宽度

tab-size 控制制表符占据的列宽，适合代码和预格式化文本。它只在保留空白的上下文中可见，应结合 white-space: pre；不要用 tab 对齐普通正文，测试不同字体和复制到编辑器后的内容一致性。

证据记录从“叠加文本装饰”开始：先写出未增强时的可用结果，再只加入一个 CSS 能力，比较计算样式、绘制边界、内容语义、输入状态和失败回退。若方案需要复制尺寸、隐藏真实控件、依赖固定内容或无法解释的层叠补丁，就回到最小样本重做。

### 24 连字

font-variant-ligatures 以语义化方式启用常见或酌情连字，实际结果取决于字体是否提供对应 OpenType 字形。代码、账号等需要字符逐个可辨的内容应关闭连字；更换回退字体时不能改变字符串本身。

证据记录从“验证复制与缩放”开始：先写出未增强时的可用结果，再只加入一个 CSS 能力，比较计算样式、绘制边界、内容语义、输入状态和失败回退。若方案需要复制尺寸、隐藏真实控件、依赖固定内容或无法解释的层叠补丁，就回到最小样本重做。

### 25 华丽的 & 符号

可用 unicode-range 的 @font-face 只为 & 字符选择装饰字体，其他正文保持原字体。字体加载失败时符号仍是同一文本字符；要检查基线、字重与行高，避免以伪元素复制出屏幕阅读器重复内容。

证据记录从“声明语言与字体”开始：先写出未增强时的可用结果，再只加入一个 CSS 能力，比较计算样式、绘制边界、内容语义、输入状态和失败回退。若方案需要复制尺寸、隐藏真实控件、依赖固定内容或无法解释的层叠补丁，就回到最小样本重做。

### 26 自定义下划线

背景渐变可以精确控制线条粗细、位置与断开，但现代 text-decoration 已提供 thickness、offset 和 skip-ink。优先使用语义装饰属性，渐变作为兼容方案；多行、不同下行字母和高对比模式都要检查。

证据记录从“稳定行框节奏”开始：先写出未增强时的可用结果，再只加入一个 CSS 能力，比较计算样式、绘制边界、内容语义、输入状态和失败回退。若方案需要复制尺寸、隐藏真实控件、依赖固定内容或无法解释的层叠补丁，就回到最小样本重做。

### 27 现实中的文字效果

凸版、空心、发光等效果通常由多层 text-shadow 模拟，每层偏移代表光源与表面关系。先保证无阴影文字的对比度，再逐层增加效果；阴影数量、模糊半径和动画会影响绘制成本。

证据记录从“控制断行空白”开始：先写出未增强时的可用结果，再只加入一个 CSS 能力，比较计算样式、绘制边界、内容语义、输入状态和失败回退。若方案需要复制尺寸、隐藏真实控件、依赖固定内容或无法解释的层叠补丁，就回到最小样本重做。

### 28 环形文字

环形文字需要把字符沿圆周定位，SVG textPath 能保持字符串与路径关系，逐字符旋转则需要脚本分割。路径效果不能破坏阅读顺序和复制文本；小屏应提供直线排版回退，不强迫正文绕圈阅读。

证据记录从“启用字形特性”开始：先写出未增强时的可用结果，再只加入一个 CSS 能力，比较计算样式、绘制边界、内容语义、输入状态和失败回退。若方案需要复制尺寸、隐藏真实控件、依赖固定内容或无法解释的层叠补丁，就回到最小样本重做。

## 方案比较矩阵

| 方案       | 依赖                       | 优点             | 主要风险             | 验收重点                |
| ---------- | -------------------------- | ---------------- | -------------------- | ----------------------- |
| 基线       | 正常流、实体颜色、原生语义 | 最稳定、自然回退 | 视觉较简单           | 无 CSS 增强仍可读可操作 |
| 单属性增强 | 圆角、阴影、渐变或相对单位 | 代码少、意图清楚 | 绘制区域判断错误     | 改背景、尺寸和内容      |
| 多层组合   | 多背景、伪元素、滤镜或变换 | 表达复杂效果     | 层叠、裁剪和性能     | 关闭一层仍能定位根因    |
| 动态增强   | 状态选择器、过渡、动画     | 反馈连续         | 键盘、减少动态和中断 | 快速反转与静态回退      |

## 分步可视实验

<Stepper>
  <Step title="1. 建立问题与基线">
    先预测 DOM、正常流、可访问名称和无增强结果，再选择原书目录中的目标攻略。
    <Csec05TypographyMapLab />
  </Step>
  <Step title="2. 注入单一能力与故障">
    只切换一个绘制或布局能力，改变容器压力，观察首个计算值、边界或交互偏离点。
    <Csec05TypographyExperimentLab />
  </Step>
  <Step title="3. 删除故障并签发证据">
    恢复同一输入，核对视觉、内容、焦点、尺寸和回退轨迹都收敛，再保留最少声明。
    <Csec05TypographyEvidenceLab />
  </Step>
</Stepper>

## 最小可运行实验

```css
.prose {
  hyphens: auto;
  line-height: 1.65;
  font-variant-ligatures: common-ligatures;
}
.prose a {
  text-decoration-thickness: 0.1em;
  text-underline-offset: 0.16em;
  text-decoration-skip-ink: auto;
}
```

```html
<article class="secret component" data-enhanced>
  <button class="action" type="button">验证基线与增强</button>
  <p>用长内容、窄容器、键盘和能力缺失重放同一结构。</p>
</article>
```

```yaml
book: CSS 揭秘
page: csec-05-typography
sample: baseline | enhanced | failure | recovery
toc_entries: 10
semantic_content_preserved: true
fallback_operable: true
first_divergence: null
```

## 常见误区与故障注入

<Callout type="trap">
  **误区一：看到效果相同就认定方案等价。** 固定尺寸、额外
  DOM、隐藏溢出和无法解释的高层级可能暂时生成同一截图，却会在内容、字体、输入或能力缺失时分叉。必须比较机制与恢复轨迹。
</Callout>

<Callout type="trap">
  **误区二：把 2026 年的便利属性直接冒充 2015 年原书攻略。**
  现代替代方案可以放入比较矩阵，但必须先解释原书使用的渐变、阴影、伪元素、变换或选择器机制，并明确两者的回退与兼容边界。
</Callout>

## 四类样本与验收

| 样本 | 注入方式                             | 必查证据                 | 通过条件               |
| ---- | ------------------------------------ | ------------------------ | ---------------------- |
| 基线 | 关闭高级绘制或动画能力               | 内容、原生语义、正常流   | 信息与操作完整         |
| 增强 | 只开启目标机制                       | 计算值、绘制区、声明数量 | 目标效果由最少机制产生 |
| 失败 | 窄宽、长内容、键盘、换背景或快速反转 | 首个偏离点               | 能解释并移除根因       |
| 恢复 | 删除故障后重放同一 DOM               | 状态、尺寸、焦点、层叠   | 无残留覆盖且基线一致   |

<Csec05TypographyEvidenceLab />

目录证据：第5章 字体排印、20 连字符断行、21 插入换行、22 文本行的斑马条纹、23 调整 tab 的宽度、24 连字、25 华丽的 & 符号、26 自定义下划线、27 现实中的文字效果、28 环形文字。本页不复制原书正文，而是按公开目录独立重构问题、机制、实验与验收，确保每个标题都有可执行学习证据。

## 练习

<Exercises>

**问题 1** 为什么不能只用最终截图验收第 5 章 字体排印？

<Answer>
  截图不显示语义、能力依赖、计算值、命中区、焦点和回退路径。必须关闭关键能力并改变内容、宽度和输入方式，找到首个偏离点，再确认恢复后回到同一基线。
</Answer>

**问题 2** 如何把本页公开目录转换为可执行证据？

<Answer>
  依次登记“第5章 字体排印、20 连字符断行、21 插入换行、22 文本行的斑马条纹、23
  调整 tab 的宽度、24 连字、25 华丽的 & 符号、26 自定义下划线、27
  现实中的文字效果、28
  环形文字”，为每项写出问题约束、规范机制、最小代码、自然回退、边界样本与通过条件。每次只改变一个变量，不能用额外覆盖掩盖失败。
</Answer>

**问题 3** 本页的最小通过条件是什么？

<Answer>
  排印增强不能损害文本语义、复制顺序、语言断行和缩放可读性，缺少高级字体能力时仍保留正文。
  同时，基线、增强、失败和恢复四条轨迹都能复现，删除故障声明后不存在重复常量、隐藏焦点、错误裁剪或状态跳变。
</Answer>

</Exercises>

## 小结

- hyphens：连接“声明语言与字体”与规范依赖。
- white-space：连接“稳定行框节奏”与最小基线。
- tab-size：连接“控制断行空白”与增强机制。
- font-variant：连接“启用字形特性”与边界故障。
- text-decoration：连接“叠加文本装饰”与恢复证据。
- 10 个本页目录条目已经全部进入问题、机制、实现与验收链。
- 现代便利写法只作对照，原书 2015 年方案的机制身份与回退路径保持清楚。

## 术语表

<Glossary>
  <GlossaryItem term="hyphens">
    hyphens
    对应“声明语言与字体”。它不是孤立属性记忆，而是连接问题、机制、最小实现、能力检测和失败恢复的证据节点。
  </GlossaryItem>
  <GlossaryItem term="white-space">
    white-space
    对应“稳定行框节奏”。它不是孤立属性记忆，而是连接问题、机制、最小实现、能力检测和失败恢复的证据节点。
  </GlossaryItem>
  <GlossaryItem term="tab-size">
    tab-size
    对应“控制断行空白”。它不是孤立属性记忆，而是连接问题、机制、最小实现、能力检测和失败恢复的证据节点。
  </GlossaryItem>
  <GlossaryItem term="font-variant">
    font-variant
    对应“启用字形特性”。它不是孤立属性记忆，而是连接问题、机制、最小实现、能力检测和失败恢复的证据节点。
  </GlossaryItem>
  <GlossaryItem term="text-decoration">
    text-decoration
    对应“叠加文本装饰”。它不是孤立属性记忆，而是连接问题、机制、最小实现、能力检测和失败恢复的证据节点。
  </GlossaryItem>
</Glossary>

<Attribution
  adaptedFrom="Lea Verou · CSS Secrets · official O'Reilly table of contents; CSS魔法译《CSS揭秘》中文目录"
  adaptedUrl="https://www.oreilly.com/library/view/css-secrets/9781449372736/toc.html"
/>
