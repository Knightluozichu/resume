---
title: "第 2 章 需提前了解的术语和概念"
type: A
section: "CSS 世界 · 世界观与术语"
order: 2
description: "读懂 CSS 必须区分属性、值、关键字、变量、长度单位、功能符和声明，并承认规范中的未定义行为；没有定义的结果不能当作跨浏览器契约。"
demo: true
math: false
sourceUrl: "https://www.dedao.cn/ebook/detail?id=V5R16yPmaYOMqGRAv82jkX4KDe175w771mwrbx6pNgznl9VZPLJQyEBodb89mqoO"
draft: true
---

import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第 2 章 需提前了解的术语和概念的核心主张，并把它放回原书 12 章 CSS 2.1 体系
- 能按出版社目录逐项描述 3 个标题的盒、包含块、计算值和使用值
- 能实现本页最小样例，定位流、尺寸、内联、定位、层叠或绘制的首个偏离点
- 能比较正常、边界、失败和恢复四类样本，判断布局是否仍保持流体契约

</Objectives>

## 直觉起点

<Term def="属性 是“第 2 章 需提前了解的术语和概念”的第 1 个核心观察量，位于“拆分规则与声明”阶段；必须同时说明指定值、计算值、使用值、实际盒与失败边界。">属性</Term>、<Term def="关键字 是“第 2 章 需提前了解的术语和概念”的第 2 个核心观察量，位于“辨认属性和值”阶段；必须同时说明指定值、计算值、使用值、实际盒与失败边界。">关键字</Term>、<Term def="初始值 是“第 2 章 需提前了解的术语和概念”的第 3 个核心观察量，位于“解析关键字和单位”阶段；必须同时说明指定值、计算值、使用值、实际盒与失败边界。">初始值</Term>、<Term def="继承 是“第 2 章 需提前了解的术语和概念”的第 4 个核心观察量，位于“确定初始与继承”阶段；必须同时说明指定值、计算值、使用值、实际盒与失败边界。">继承</Term>、<Term def="未定义行为 是“第 2 章 需提前了解的术语和概念”的第 5 个核心观察量，位于“标记未定义行为”阶段；必须同时说明指定值、计算值、使用值、实际盒与失败边界。">未定义行为</Term>
共同构成本页的观察坐标。读懂 CSS
必须区分属性、值、关键字、变量、长度单位、功能符和声明，并承认规范中的未定义行为；没有定义的结果不能当作跨浏览器契约。
CSS
的困难不在属性名称，而在多个声明经过级联、继承和格式化后才产生最终使用值；同一截图可能由完全不同的盒树、固定尺寸或偶然层叠得到，必须向前追踪原因。

## 六阶段机制链

### 1. 拆分规则与声明

固定 DOM、样式表、视口、字体和内容，先预测生成盒、包含块、格式化上下文、计算值、使用尺寸、行框和层叠位置。一旦轨迹不同，保存首个偏离点，不用最终像素掩盖过程。

### 2. 辨认属性和值

只改变一个声明或环境变量，记录生成盒、包含块、格式化上下文、计算值、使用尺寸、行框和层叠位置。一旦轨迹不同，保存首个偏离点，不用最终像素掩盖过程。

### 3. 解析关键字和单位

只改变一个声明或环境变量，记录生成盒、包含块、格式化上下文、计算值、使用尺寸、行框和层叠位置。一旦轨迹不同，保存首个偏离点，不用最终像素掩盖过程。

### 4. 确定初始与继承

只改变一个声明或环境变量，记录生成盒、包含块、格式化上下文、计算值、使用尺寸、行框和层叠位置。一旦轨迹不同，保存首个偏离点，不用最终像素掩盖过程。

### 5. 标记未定义行为

只改变一个声明或环境变量，记录生成盒、包含块、格式化上下文、计算值、使用尺寸、行框和层叠位置。一旦轨迹不同，保存首个偏离点，不用最终像素掩盖过程。

### 6. 建立可移植测试

只改变一个声明或环境变量，记录生成盒、包含块、格式化上下文、计算值、使用尺寸、行框和层叠位置。恢复后重放同一输入，并确认窄宽、长内容和方向变化不会暴露旧补丁。

## 核心机制深挖

CSS 声明只有在语法有效、计算值可确定、使用值能落到具体格式化上下文时才产生可见结果。浏览器容错会丢弃无效声明，但不会替作者补上未写明的布局意图。 任何依赖未定义顺序、错误语法容错或单一浏览器偶然表现的样式，都必须通过更明确的结构和声明改写，不能用一次截图证明正确。 实验时把浏览器开发者工具中的盒模型、计算样式和层叠上下文当成观测工具，而不是答案来源；答案必须能从规范规则和当前 DOM 结构推导出来。先删除非必要覆盖，再逐条加入约束，这样才能分辨默认流在做什么、哪条声明真正改变了结果。

规范把某个结果标为未定义，不等于浏览器可以随意违反已经定义的其他规则，而是作者不能依赖该处得到唯一、稳定且可移植的结果。判读时先圈定未定义边界，再确认边界外的声明解析、继承、盒生成和使用值仍各自成立。随后用至少两个浏览器、两个视口和一组长内容复现；只要结果分叉，就把依赖改写成明确的 DOM 关系或声明，而不是记录某个版本的偶然表现。这样形成的测试关注可承诺的行为，也允许实现细节在规范许可范围内变化。

## 边界记录模板

每次实验建立六列证据：**DOM 与内容、生成盒与格式化上下文、包含块、计算值与使用值、首个偏离点、恢复结果**。尺寸页增加视口与固有尺寸，内联页增加字体度量和基线，定位页增加脱流空间与四向偏移，层叠页增加上下文树，文本页增加字体回退，界面页增加键盘与书写方向。若最终外观相同但固定宽高、隐藏溢出、不可见焦点或错误层叠仍存在，依然失败。

## 出版社目录逐项讲解

### 第2章 需提前了解的术语和概念

**第2章 需提前了解的术语和概念** 要解决的是：把规范词汇映射到指定值、计算值、使用值和实际值，拒绝依赖未定义结果。先预测元素在“拆分规则与声明”前后会生成哪些盒、读取哪个包含块，并记录“属性”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 2.1 务必了解的CSS世界的专业术语

**2.1 务必了解的CSS世界的专业术语** 要解决的是：把规范词汇映射到指定值、计算值、使用值和实际值，拒绝依赖未定义结果。先预测元素在“辨认属性和值”前后会生成哪些盒、读取哪个包含块，并记录“关键字”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 2.2 了解CSS世界中的“未定义行为”

**2.2 了解CSS世界中的“未定义行为”** 要解决的是：把规范词汇映射到指定值、计算值、使用值和实际值，拒绝依赖未定义结果。先预测元素在“解析关键字和单位”前后会生成哪些盒、读取哪个包含块，并记录“初始值”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

## 最小可运行实验

```css:
root {
  color: #222;
}
.note {
  color: inherit;
  width: auto;
  min-width: 0;
}
.note::before {
  content: attr(data-label);
}
```

```yaml
book: CSS 世界
page: csw-02-terms-undefined-behavior
public_toc_entries: 3
sample: normal | boundary | failure | recovery
first_divergence: null
box_tree_explained: true
used_values_explained: true
fluid_contract_preserved: true
```

```text
freeze DOM + stylesheet + viewport + fonts
predict generated boxes + containing blocks
change exactly one variable
stop at the first divergent used value
restore and replay the same DOM
assert fluid layout across content and directions
```

## 常见误区与故障注入

<Callout type="trap">
  **误区一：记住“第 2 章
  需提前了解的术语和概念”的属性结论，却不展开从“拆分规则与声明”到“建立可移植测试”的过程。**
  相同截图可能依赖固定高度、偶然字体、错误包含块或过大的
  z-index，必须保存盒树与使用值证据。
</Callout>

<Callout type="trap">
  **误区二：把 Flex、Grid、transform 和 animation 塞进《CSS 世界》。**
  作者明确把本书定位为 CSS 2.1 体系；这些 CSS3 及后续能力属于《CSS
  新世界》，目录忠实度和现代替代方案应分开记录。
</Callout>

## 四类样本与验收

| 样本 | 注入方式                             | 必查证据                       | 通过条件               |
| ---- | ------------------------------------ | ------------------------------ | ---------------------- |
| 正常 | 标准字体、常规内容和宽视口           | 盒、包含块、使用值、行框、层级 | 六阶段按预期完成       |
| 边界 | 窄视口、长内容、替换元素、字体缺失   | min/max、溢出、基线和回退      | 不依赖固定像素补丁     |
| 失败 | 单一脱流、错误层叠、隐藏焦点或错方向 | 首个偏离点                     | 能定位并解释根因       |
| 恢复 | 删除故障声明后重放同一 DOM           | 盒树和计算样式                 | 无旧覆盖且流体契约恢复 |

本页签发不变量是：**读懂 CSS 必须区分属性、值、关键字、变量、长度单位、功能符和声明，并承认规范中的未定义行为；没有定义的结果不能当作跨浏览器契约。** 单次截图不能替代盒生成、格式化上下文、包含块、计算值、使用值与绘制顺序的完整轨迹。

## 练习

<Exercises>

**问题 1** 为什么不能只以最终截图正确作为第 2 章 需提前了解的术语和概念的验收？

<Answer>
  截图会隐藏固定尺寸、错误包含块、字体偶然命中、溢出裁剪和层叠补丁。必须改变视口、内容、字体和流向，比较盒树、使用值与首个偏离点。
</Answer>

**问题 2** 如何把本页出版社目录变成可执行学习证据？

<Answer>
  按公开顺序登记“第2章 需提前了解的术语和概念、2.1
  务必了解的CSS世界的专业术语、2.2
  了解CSS世界中的“未定义行为””，为每项写出生成盒、包含块、格式化上下文、计算值、使用值、失败和恢复条件，再用同一
  DOM 重放正常与反例。
</Answer>

**问题 3** 本页的最小通过条件是什么？

<Answer>
  任何依赖未定义顺序、错误语法容错或单一浏览器偶然表现的样式，都必须通过更明确的结构和声明改写，不能用一次截图证明正确。
  同时，正常、边界、失败和恢复轨迹都能复现，删除故障声明后不存在旧覆盖、旧层叠或隐藏溢出。
</Answer>

</Exercises>

## 小结

- **属性**：对应“拆分规则与声明”阶段的核心观察量。
- **关键字**：对应“辨认属性和值”阶段的核心观察量。
- **初始值**：对应“解析关键字和单位”阶段的核心观察量。
- **继承**：对应“确定初始与继承”阶段的核心观察量。
- **未定义行为**：对应“标记未定义行为”阶段的核心观察量。
- 3 个公开目录条目已经全部映射到本页证据链。
- 盒树、使用值和边界样本共同决定是否通过，最终截图不是唯一证据。

## 术语表

<Glossary>
  <GlossaryItem term="属性">
    属性 是本页第 1
    个核心观察量，位于“拆分规则与声明”阶段。验收时同时记录包含块、格式化上下文、计算值、使用值与绘制结果。
  </GlossaryItem>
  <GlossaryItem term="关键字">
    关键字 是本页第 2
    个核心观察量，位于“辨认属性和值”阶段。验收时同时记录包含块、格式化上下文、计算值、使用值与绘制结果。
  </GlossaryItem>
  <GlossaryItem term="初始值">
    初始值 是本页第 3
    个核心观察量，位于“解析关键字和单位”阶段。验收时同时记录包含块、格式化上下文、计算值、使用值与绘制结果。
  </GlossaryItem>
  <GlossaryItem term="继承">
    继承 是本页第 4
    个核心观察量，位于“确定初始与继承”阶段。验收时同时记录包含块、格式化上下文、计算值、使用值与绘制结果。
  </GlossaryItem>
  <GlossaryItem term="未定义行为">
    未定义行为 是本页第 5
    个核心观察量，位于“标记未定义行为”阶段。验收时同时记录包含块、格式化上下文、计算值、使用值与绘制结果。
  </GlossaryItem>
</Glossary>

## 版本与改编边界

本单元以张鑫旭著《CSS 世界》为身份基线。详细目录来自人民邮电出版社出版电子版页面，CSS 2.1 定位由作者官网和图书官网交叉确认；正文是独立教学重构，不复制原书段落。现代浏览器兼容状态需另行按当前 CSS 规范和浏览器文档核验。

<Attribution
  adaptedFrom="张鑫旭著，《CSS 世界》，人民邮电出版社，2017 年 12 月，328 页，ISBN 9787115470669。本页按出版社公开目录与作者官网独立教学重构。"
  adaptedUrl="https://www.dedao.cn/ebook/detail?id=V5R16yPmaYOMqGRAv82jkX4KDe175w771mwrbx6pNgznl9VZPLJQyEBodb89mqoO"
/>
