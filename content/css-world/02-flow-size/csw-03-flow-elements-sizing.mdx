---
title: "第 3 章 流、元素与基本尺寸"
type: A
section: "CSS 世界 · 流与尺寸"
order: 3
description: "块级元素通过外在块级盒参与流、通过内在容器盒承载内容；width:auto 的充分利用可用宽度、收缩包裹、最小宽度和超出容器四种表现，以及内联盒和幽灵空白节点，共同决定基本尺寸。"
demo: true
math: false
sourceUrl: "https://www.dedao.cn/ebook/detail?id=V5R16yPmaYOMqGRAv82jkX4KDe175w771mwrbx6pNgznl9VZPLJQyEBodb89mqoO"
draft: false
---

import {
  Csw03FlowElementsSizingMapLab,
  Csw03FlowElementsSizingExperimentLab,
  Csw03FlowElementsSizingEvidenceLab,
} from "@/components/mdx/css-world/diagrams/csw-03-flow-elements-sizing";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第 3 章 流、元素与基本尺寸的核心主张，并把它放回原书 12 章 CSS 2.1 体系
- 能按出版社目录逐项描述 21 个标题的盒、包含块、计算值和使用值
- 能实现本页最小样例，定位流、尺寸、内联、定位、层叠或绘制的首个偏离点
- 能比较正常、边界、失败和恢复四类样本，判断布局是否仍保持流体契约

</Objectives>

## 直觉起点

<Term def="块级元素 是“第 3 章 流、元素与基本尺寸”的第 1 个核心观察量，位于“辨认外在与内在盒”阶段；必须同时说明指定值、计算值、使用值、实际盒与失败边界。">
  块级元素
</Term>
、
<Term def="width:auto 是“第 3 章 流、元素与基本尺寸”的第 2 个核心观察量，位于“求解 width:auto”阶段；必须同时说明指定值、计算值、使用值、实际盒与失败边界。">
  width:auto
</Term>
、
<Term def="宽度分离 是“第 3 章 流、元素与基本尺寸”的第 3 个核心观察量，位于“应用宽度分离”阶段；必须同时说明指定值、计算值、使用值、实际盒与失败边界。">
  宽度分离
</Term>
、
<Term def="内联盒模型 是“第 3 章 流、元素与基本尺寸”的第 4 个核心观察量，位于“约束最小最大尺寸”阶段；必须同时说明指定值、计算值、使用值、实际盒与失败边界。">
  内联盒模型
</Term>
、
<Term def="幽灵空白节点 是“第 3 章 流、元素与基本尺寸”的第 5 个核心观察量，位于“建立内联盒模型”阶段；必须同时说明指定值、计算值、使用值、实际盒与失败边界。">
  幽灵空白节点
</Term>
共同构成本页的观察坐标。块级元素通过外在块级盒参与流、通过内在容器盒承载内容；width:auto
的充分利用可用宽度、收缩包裹、最小宽度和超出容器四种表现，以及内联盒和幽灵空白节点，共同决定基本尺寸。
CSS
的困难不在属性名称，而在多个声明经过级联、继承和格式化后才产生最终使用值；同一截图可能由完全不同的盒树、固定尺寸或偶然层叠得到，必须向前追踪原因。

<Csw03FlowElementsSizingMapLab />

## 六阶段机制链

### 1. 辨认外在与内在盒

固定 DOM、样式表、视口、字体和内容，先预测生成盒、包含块、格式化上下文、计算值、使用尺寸、行框和层叠位置。一旦轨迹不同，保存首个偏离点，不用最终像素掩盖过程。

### 2. 求解 width:auto

只改变一个声明或环境变量，记录生成盒、包含块、格式化上下文、计算值、使用尺寸、行框和层叠位置。一旦轨迹不同，保存首个偏离点，不用最终像素掩盖过程。

### 3. 应用宽度分离

只改变一个声明或环境变量，记录生成盒、包含块、格式化上下文、计算值、使用尺寸、行框和层叠位置。一旦轨迹不同，保存首个偏离点，不用最终像素掩盖过程。

### 4. 约束最小最大尺寸

只改变一个声明或环境变量，记录生成盒、包含块、格式化上下文、计算值、使用尺寸、行框和层叠位置。一旦轨迹不同，保存首个偏离点，不用最终像素掩盖过程。

### 5. 建立内联盒模型

只改变一个声明或环境变量，记录生成盒、包含块、格式化上下文、计算值、使用尺寸、行框和层叠位置。一旦轨迹不同，保存首个偏离点，不用最终像素掩盖过程。

### 6. 排查幽灵空白

只改变一个声明或环境变量，记录生成盒、包含块、格式化上下文、计算值、使用尺寸、行框和层叠位置。恢复后重放同一输入，并确认窄宽、长内容和方向变化不会暴露旧补丁。

## 核心机制深挖

width:auto 不是简单的 100%。块级非替换元素常表现为充分利用可用宽度，浮动和绝对定位可能收缩包裹，连续长词会触发最小内容约束，负外边距还能使外部尺寸超过容器。 先让外部尺寸由流决定，再在内层用 margin 或 padding 分离内容；只有替换元素、明确业务约束或格式化上下文要求时才固定宽高。 实验时把浏览器开发者工具中的盒模型、计算样式和层叠上下文当成观测工具，而不是答案来源；答案必须能从规范规则和当前 DOM 结构推导出来。先删除非必要覆盖，再逐条加入约束，这样才能分辨默认流在做什么、哪条声明真正改变了结果。

## 边界记录模板

每次实验建立六列证据：**DOM 与内容、生成盒与格式化上下文、包含块、计算值与使用值、首个偏离点、恢复结果**。尺寸页增加视口与固有尺寸，内联页增加字体度量和基线，定位页增加脱流空间与四向偏移，层叠页增加上下文树，文本页增加字体回退，界面页增加键盘与书写方向。若最终外观相同但固定宽高、隐藏溢出、不可见焦点或错误层叠仍存在，依然失败。

## 出版社目录逐项讲解

### 第3章 流、元素与基本尺寸

**第3章 流、元素与基本尺寸** 要解决的是：确定外在盒、内在盒、包含块与 auto/百分比计算，避免把声明值直接当成最终使用尺寸。先预测元素在“辨认外在与内在盒”前后会生成哪些盒、读取哪个包含块，并记录“块级元素”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 3.1 块级元素

**3.1 块级元素** 要解决的是：确定外在盒、内在盒、包含块与 auto/百分比计算，避免把声明值直接当成最终使用尺寸。先预测元素在“求解 width:auto”前后会生成哪些盒、读取哪个包含块，并记录“width:auto”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 3.1.1 为什么list-item元素会出现项目符号

**3.1.1 为什么list-item元素会出现项目符号** 要解决的是：把该目录项放回正常流、盒生成、尺寸计算和格式化上下文，记录输入、约束与浏览器使用值。先预测元素在“应用宽度分离”前后会生成哪些盒、读取哪个包含块，并记录“宽度分离”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 3.1.2 display:inline-table的盒子是怎样组成的

**3.1.2 display:inline-table的盒子是怎样组成的** 要解决的是：比较盒是否生成、空间是否保留、后代可见性、事件命中与可访问性。先预测元素在“约束最小最大尺寸”前后会生成哪些盒、读取哪个包含块，并记录“内联盒模型”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 3.1.3 width/height作用在哪个盒子上

**3.1.3 width/height作用在哪个盒子上** 要解决的是：确定外在盒、内在盒、包含块与 auto/百分比计算，避免把声明值直接当成最终使用尺寸。先预测元素在“建立内联盒模型”前后会生成哪些盒、读取哪个包含块，并记录“幽灵空白节点”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 3.2 width/height作用的具体细节

**3.2 width/height作用的具体细节** 要解决的是：确定外在盒、内在盒、包含块与 auto/百分比计算，避免把声明值直接当成最终使用尺寸。先预测元素在“排查幽灵空白”前后会生成哪些盒、读取哪个包含块，并记录“块级元素”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 3.2.1 深藏不露的width:auto

**3.2.1 深藏不露的width:auto** 要解决的是：确定外在盒、内在盒、包含块与 auto/百分比计算，避免把声明值直接当成最终使用尺寸。先预测元素在“辨认外在与内在盒”前后会生成哪些盒、读取哪个包含块，并记录“width:auto”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 3.2.2 width值作用的细节

**3.2.2 width值作用的细节** 要解决的是：确定外在盒、内在盒、包含块与 auto/百分比计算，避免把声明值直接当成最终使用尺寸。先预测元素在“求解 width:auto”前后会生成哪些盒、读取哪个包含块，并记录“宽度分离”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 3.2.3 CSS流体布局下的宽度分离原则

**3.2.3 CSS流体布局下的宽度分离原则** 要解决的是：把该目录项放回正常流、盒生成、尺寸计算和格式化上下文，记录输入、约束与浏览器使用值。先预测元素在“应用宽度分离”前后会生成哪些盒、读取哪个包含块，并记录“内联盒模型”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 3.2.4 改变width/height作用细节的box-sizing

**3.2.4 改变width/height作用细节的box-sizing** 要解决的是：确定外在盒、内在盒、包含块与 auto/百分比计算，避免把声明值直接当成最终使用尺寸。先预测元素在“约束最小最大尺寸”前后会生成哪些盒、读取哪个包含块，并记录“幽灵空白节点”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 3.2.5 相对简单而单纯的height:auto

**3.2.5 相对简单而单纯的height:auto** 要解决的是：确定外在盒、内在盒、包含块与 auto/百分比计算，避免把声明值直接当成最终使用尺寸。先预测元素在“建立内联盒模型”前后会生成哪些盒、读取哪个包含块，并记录“块级元素”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 3.2.6 关于height:100%

**3.2.6 关于height:100%** 要解决的是：确定外在盒、内在盒、包含块与 auto/百分比计算，避免把声明值直接当成最终使用尺寸。先预测元素在“排查幽灵空白”前后会生成哪些盒、读取哪个包含块，并记录“width:auto”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 3.3 CSS min-width/max-width和min-height/max-height二三事

**3.3 CSS min-width/max-width和min-height/max-height二三事** 要解决的是：确定外在盒、内在盒、包含块与 auto/百分比计算，避免把声明值直接当成最终使用尺寸。先预测元素在“辨认外在与内在盒”前后会生成哪些盒、读取哪个包含块，并记录“宽度分离”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 3.3.1 为流体而生的min-width/max-width

**3.3.1 为流体而生的min-width/max-width** 要解决的是：确定外在盒、内在盒、包含块与 auto/百分比计算，避免把声明值直接当成最终使用尺寸。先预测元素在“求解 width:auto”前后会生成哪些盒、读取哪个包含块，并记录“内联盒模型”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 3.3.2 与众不同的初始值

**3.3.2 与众不同的初始值** 要解决的是：把该目录项放回正常流、盒生成、尺寸计算和格式化上下文，记录输入、约束与浏览器使用值。先预测元素在“应用宽度分离”前后会生成哪些盒、读取哪个包含块，并记录“幽灵空白节点”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 3.3.3 超越!important，超越最大

**3.3.3 超越!important，超越最大** 要解决的是：把该目录项放回正常流、盒生成、尺寸计算和格式化上下文，记录输入、约束与浏览器使用值。先预测元素在“约束最小最大尺寸”前后会生成哪些盒、读取哪个包含块，并记录“块级元素”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 3.3.4 任意高度元素的展开收起动画技术

**3.3.4 任意高度元素的展开收起动画技术** 要解决的是：把该目录项放回正常流、盒生成、尺寸计算和格式化上下文，记录输入、约束与浏览器使用值。先预测元素在“建立内联盒模型”前后会生成哪些盒、读取哪个包含块，并记录“width:auto”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 3.4 内联元素

**3.4 内联元素** 要解决的是：画出内容区域、行内盒、行框盒与基线，按字体度量解释垂直位置。先预测元素在“排查幽灵空白”前后会生成哪些盒、读取哪个包含块，并记录“宽度分离”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 3.4.1 哪些元素是内联元素

**3.4.1 哪些元素是内联元素** 要解决的是：画出内容区域、行内盒、行框盒与基线，按字体度量解释垂直位置。先预测元素在“辨认外在与内在盒”前后会生成哪些盒、读取哪个包含块，并记录“内联盒模型”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 3.4.2 内联世界深入的基础——内联盒模型

**3.4.2 内联世界深入的基础——内联盒模型** 要解决的是：画出内容区域、行内盒、行框盒与基线，按字体度量解释垂直位置。先预测元素在“求解 width:auto”前后会生成哪些盒、读取哪个包含块，并记录“幽灵空白节点”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 3.4.3 幽灵空白节点

**3.4.3 幽灵空白节点** 要解决的是：把该目录项放回正常流、盒生成、尺寸计算和格式化上下文，记录输入、约束与浏览器使用值。先预测元素在“应用宽度分离”前后会生成哪些盒、读取哪个包含块，并记录“块级元素”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

## 分步可视实验

<Stepper>
  <Step title="1. 辨认外在与内在盒与求解 width:auto">
    先预测盒树和默认流，再运行正常样本保存基线。
    <Csw03FlowElementsSizingMapLab />
  </Step>
  <Step title="2. 应用宽度分离与约束最小最大尺寸">
    改变容器、内容或单一声明，在首个无法满足契约的位置停止。
    <Csw03FlowElementsSizingExperimentLab />
  </Step>
  <Step title="3. 建立内联盒模型与排查幽灵空白">
    清除覆盖并重放同一 DOM，核对尺寸、行框、层级与绘制全部收敛。
    <Csw03FlowElementsSizingEvidenceLab />
  </Step>
</Stepper>

## 最小可运行实验

```css
.panel {
  width: auto;
  min-width: 0;
  max-width: 40rem;
  margin-inline: auto;
}
.panel__content {
  margin-inline: 1rem;
}
.inline-demo {
  line-height: 1.5;
}
```

```yaml
book: CSS 世界
page: csw-03-flow-elements-sizing
public_toc_entries: 21
sample: normal | boundary | failure | recovery
first_divergence: null
box_tree_explained: true
used_values_explained: true
fluid_contract_preserved: true
```

```text
freeze DOM + stylesheet + viewport + fonts
predict generated boxes + containing blocks
change exactly one variable
stop at the first divergent used value
restore and replay the same DOM
assert fluid layout across content and directions
```

## 常见误区与故障注入

<Callout type="trap">
  **误区一：记住“第 3 章
  流、元素与基本尺寸”的属性结论，却不展开从“辨认外在与内在盒”到“排查幽灵空白”的过程。**
  相同截图可能依赖固定高度、偶然字体、错误包含块或过大的
  z-index，必须保存盒树与使用值证据。
</Callout>

<Callout type="trap">
  **误区二：把 Flex、Grid、transform 和 animation 塞进《CSS 世界》。**
  作者明确把本书定位为 CSS 2.1 体系；这些 CSS3 及后续能力属于《CSS
  新世界》，目录忠实度和现代替代方案应分开记录。
</Callout>

## 四类样本与验收

| 样本 | 注入方式                             | 必查证据                       | 通过条件               |
| ---- | ------------------------------------ | ------------------------------ | ---------------------- |
| 正常 | 标准字体、常规内容和宽视口           | 盒、包含块、使用值、行框、层级 | 六阶段按预期完成       |
| 边界 | 窄视口、长内容、替换元素、字体缺失   | min/max、溢出、基线和回退      | 不依赖固定像素补丁     |
| 失败 | 单一脱流、错误层叠、隐藏焦点或错方向 | 首个偏离点                     | 能定位并解释根因       |
| 恢复 | 删除故障声明后重放同一 DOM           | 盒树和计算样式                 | 无旧覆盖且流体契约恢复 |

<Csw03FlowElementsSizingEvidenceLab />

本页签发不变量是：**块级元素通过外在块级盒参与流、通过内在容器盒承载内容；width:auto 的充分利用可用宽度、收缩包裹、最小宽度和超出容器四种表现，以及内联盒和幽灵空白节点，共同决定基本尺寸。** 单次截图不能替代盒生成、格式化上下文、包含块、计算值、使用值与绘制顺序的完整轨迹。

## 练习

<Exercises>

**问题 1** 为什么不能只以最终截图正确作为第 3 章 流、元素与基本尺寸的验收？

<Answer>
  截图会隐藏固定尺寸、错误包含块、字体偶然命中、溢出裁剪和层叠补丁。必须改变视口、内容、字体和流向，比较盒树、使用值与首个偏离点。
</Answer>

**问题 2** 如何把本页出版社目录变成可执行学习证据？

<Answer>
  按公开顺序登记“第3章 流、元素与基本尺寸、3.1 块级元素、3.1.1
  为什么list-item元素会出现项目符号、3.1.2
  display:inline-table的盒子是怎样组成的、3.1.3
  width/height作用在哪个盒子上、3.2 width/height作用的具体细节、3.2.1
  深藏不露的width:auto、3.2.2 width值作用的细节、3.2.3
  CSS流体布局下的宽度分离原则、3.2.4 改变width/height作用细节的box-sizing、3.2.5
  相对简单而单纯的height:auto、3.2.6 关于height:100%、3.3 CSS
  min-width/max-width和min-height/max-height二三事、3.3.1
  为流体而生的min-width/max-width、3.3.2 与众不同的初始值、3.3.3
  超越!important，超越最大、3.3.4 任意高度元素的展开收起动画技术、3.4
  内联元素、3.4.1 哪些元素是内联元素、3.4.2
  内联世界深入的基础——内联盒模型、3.4.3
  幽灵空白节点”，为每项写出生成盒、包含块、格式化上下文、计算值、使用值、失败和恢复条件，再用同一
  DOM 重放正常与反例。
</Answer>

**问题 3** 本页的最小通过条件是什么？

<Answer>
  先让外部尺寸由流决定，再在内层用 margin 或 padding
  分离内容；只有替换元素、明确业务约束或格式化上下文要求时才固定宽高。
  同时，正常、边界、失败和恢复轨迹都能复现，删除故障声明后不存在旧覆盖、旧层叠或隐藏溢出。
</Answer>

</Exercises>

## 小结

- **块级元素**：对应“辨认外在与内在盒”阶段的核心观察量。
- **width:auto**：对应“求解 width:auto”阶段的核心观察量。
- **宽度分离**：对应“应用宽度分离”阶段的核心观察量。
- **内联盒模型**：对应“约束最小最大尺寸”阶段的核心观察量。
- **幽灵空白节点**：对应“建立内联盒模型”阶段的核心观察量。
- 21 个公开目录条目已经全部映射到本页证据链。
- 盒树、使用值和边界样本共同决定是否通过，最终截图不是唯一证据。

## 术语表

<Glossary>
  <GlossaryItem term="块级元素">
    块级元素 是本页第 1
    个核心观察量，位于“辨认外在与内在盒”阶段。验收时同时记录包含块、格式化上下文、计算值、使用值与绘制结果。
  </GlossaryItem>
  <GlossaryItem term="width:auto">
    width:auto 是本页第 2 个核心观察量，位于“求解
    width:auto”阶段。验收时同时记录包含块、格式化上下文、计算值、使用值与绘制结果。
  </GlossaryItem>
  <GlossaryItem term="宽度分离">
    宽度分离 是本页第 3
    个核心观察量，位于“应用宽度分离”阶段。验收时同时记录包含块、格式化上下文、计算值、使用值与绘制结果。
  </GlossaryItem>
  <GlossaryItem term="内联盒模型">
    内联盒模型 是本页第 4
    个核心观察量，位于“约束最小最大尺寸”阶段。验收时同时记录包含块、格式化上下文、计算值、使用值与绘制结果。
  </GlossaryItem>
  <GlossaryItem term="幽灵空白节点">
    幽灵空白节点 是本页第 5
    个核心观察量，位于“建立内联盒模型”阶段。验收时同时记录包含块、格式化上下文、计算值、使用值与绘制结果。
  </GlossaryItem>
</Glossary>

## 版本与改编边界

本单元以张鑫旭著《CSS 世界》为身份基线。详细目录来自人民邮电出版社出版电子版页面，CSS 2.1 定位由作者官网和图书官网交叉确认；正文是独立教学重构，不复制原书段落。现代浏览器兼容状态需另行按当前 CSS 规范和浏览器文档核验。

<Attribution
  adaptedFrom="张鑫旭著，《CSS 世界》，人民邮电出版社，2017 年 12 月，328 页，ISBN 9787115470669。本页按出版社公开目录与作者官网独立教学重构。"
  adaptedUrl="https://www.dedao.cn/ebook/detail?id=V5R16yPmaYOMqGRAv82jkX4KDe175w771mwrbx6pNgznl9VZPLJQyEBodb89mqoO"
/>
