---
title: "第 4 章 盒尺寸四大家族"
type: A
section: "CSS 世界 · 流与尺寸"
order: 4
description: "content、padding、margin 和 border 分别控制内容生成、内边空间、外部尺寸与边界图形；百分比基准、替换元素、外边距合并和 margin:auto 分配必须放回格式化上下文理解。"
demo: true
math: false
sourceUrl: "https://www.dedao.cn/ebook/detail?id=V5R16yPmaYOMqGRAv82jkX4KDe175w771mwrbx6pNgznl9VZPLJQyEBodb89mqoO"
draft: true
---

import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第 4 章 盒尺寸四大家族的核心主张，并把它放回原书 12 章 CSS 2.1 体系
- 能按出版社目录逐项描述 22 个标题的盒、包含块、计算值和使用值
- 能实现本页最小样例，定位流、尺寸、内联、定位、层叠或绘制的首个偏离点
- 能比较正常、边界、失败和恢复四类样本，判断布局是否仍保持流体契约

</Objectives>

## 直觉起点

<Term def="替换元素 是“第 4 章 盒尺寸四大家族”的第 1 个核心观察量，位于“确定 content 来源”阶段；必须同时说明指定值、计算值、使用值、实际盒与失败边界。">替换元素</Term>、<Term def="padding 是“第 4 章 盒尺寸四大家族”的第 2 个核心观察量，位于“计算 padding 空间”阶段；必须同时说明指定值、计算值、使用值、实际盒与失败边界。">padding</Term>、<Term def="margin 合并 是“第 4 章 盒尺寸四大家族”的第 3 个核心观察量，位于“求解 margin 外部尺寸”阶段；必须同时说明指定值、计算值、使用值、实际盒与失败边界。">margin 合并</Term>、<Term def="margin:auto 是“第 4 章 盒尺寸四大家族”的第 4 个核心观察量，位于“处理 margin 合并”阶段；必须同时说明指定值、计算值、使用值、实际盒与失败边界。">margin:auto</Term>、<Term def="border 是“第 4 章 盒尺寸四大家族”的第 5 个核心观察量，位于“分配 margin:auto”阶段；必须同时说明指定值、计算值、使用值、实际盒与失败边界。">border</Term>
共同构成本页的观察坐标。content、padding、margin 和 border
分别控制内容生成、内边空间、外部尺寸与边界图形；百分比基准、替换元素、外边距合并和
margin:auto 分配必须放回格式化上下文理解。 CSS
的困难不在属性名称，而在多个声明经过级联、继承和格式化后才产生最终使用值；同一截图可能由完全不同的盒树、固定尺寸或偶然层叠得到，必须向前追踪原因。

## 六阶段机制链

### 1. 确定 content 来源

固定 DOM、样式表、视口、字体和内容，先预测生成盒、包含块、格式化上下文、计算值、使用尺寸、行框和层叠位置。一旦轨迹不同，保存首个偏离点，不用最终像素掩盖过程。

### 2. 计算 padding 空间

只改变一个声明或环境变量，记录生成盒、包含块、格式化上下文、计算值、使用尺寸、行框和层叠位置。一旦轨迹不同，保存首个偏离点，不用最终像素掩盖过程。

### 3. 求解 margin 外部尺寸

只改变一个声明或环境变量，记录生成盒、包含块、格式化上下文、计算值、使用尺寸、行框和层叠位置。一旦轨迹不同，保存首个偏离点，不用最终像素掩盖过程。

### 4. 处理 margin 合并

只改变一个声明或环境变量，记录生成盒、包含块、格式化上下文、计算值、使用尺寸、行框和层叠位置。一旦轨迹不同，保存首个偏离点，不用最终像素掩盖过程。

### 5. 分配 margin:auto

只改变一个声明或环境变量，记录生成盒、包含块、格式化上下文、计算值、使用尺寸、行框和层叠位置。一旦轨迹不同，保存首个偏离点，不用最终像素掩盖过程。

### 6. 绘制 border 边界

只改变一个声明或环境变量，记录生成盒、包含块、格式化上下文、计算值、使用尺寸、行框和层叠位置。恢复后重放同一输入，并确认窄宽、长内容和方向变化不会暴露旧补丁。

## 核心机制深挖

padding 和 margin 的百分比在 CSS 2.1 中通常相对包含块宽度计算，这使垂直方向百分比也能形成稳定比例。margin 合并只发生在块级正常流的特定边界，auto 则分配剩余空间。 每次尺寸计算都写出内容盒、内边距、边框和外部尺寸四层结果；合并、auto 或替换元素参与时，必须额外记录包含块和格式化上下文。 实验时把浏览器开发者工具中的盒模型、计算样式和层叠上下文当成观测工具，而不是答案来源；答案必须能从规范规则和当前 DOM 结构推导出来。先删除非必要覆盖，再逐条加入约束，这样才能分辨默认流在做什么、哪条声明真正改变了结果。

## 边界记录模板

每次实验建立六列证据：**DOM 与内容、生成盒与格式化上下文、包含块、计算值与使用值、首个偏离点、恢复结果**。尺寸页增加视口与固有尺寸，内联页增加字体度量和基线，定位页增加脱流空间与四向偏移，层叠页增加上下文树，文本页增加字体回退，界面页增加键盘与书写方向。若最终外观相同但固定宽高、隐藏溢出、不可见焦点或错误层叠仍存在，依然失败。

## 出版社目录逐项讲解

### 第4章 盒尺寸四大家族

**第4章 盒尺寸四大家族** 要解决的是：确定外在盒、内在盒、包含块与 auto/百分比计算，避免把声明值直接当成最终使用尺寸。先预测元素在“确定 content 来源”前后会生成哪些盒、读取哪个包含块，并记录“替换元素”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 4.1 深入理解content

**4.1 深入理解content** 要解决的是：区分生成内容、替换内容和固有尺寸，跟踪内容如何参与盒尺寸与可访问语义。先预测元素在“计算 padding 空间”前后会生成哪些盒、读取哪个包含块，并记录“padding”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 4.1.1 content与替换元素

**4.1.1 content与替换元素** 要解决的是：区分生成内容、替换内容和固有尺寸，跟踪内容如何参与盒尺寸与可访问语义。先预测元素在“求解 margin 外部尺寸”前后会生成哪些盒、读取哪个包含块，并记录“margin 合并”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 4.1.2 content内容生成技术

**4.1.2 content内容生成技术** 要解决的是：区分生成内容、替换内容和固有尺寸，跟踪内容如何参与盒尺寸与可访问语义。先预测元素在“处理 margin 合并”前后会生成哪些盒、读取哪个包含块，并记录“margin:auto”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 4.2 温和的padding属性

**4.2 温和的padding属性** 要解决的是：核对内边空间、百分比基准、内置样式和背景绘制范围。先预测元素在“分配 margin:auto”前后会生成哪些盒、读取哪个包含块，并记录“border”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 4.2.1 padding与元素的尺寸

**4.2.1 padding与元素的尺寸** 要解决的是：确定外在盒、内在盒、包含块与 auto/百分比计算，避免把声明值直接当成最终使用尺寸。先预测元素在“绘制 border 边界”前后会生成哪些盒、读取哪个包含块，并记录“替换元素”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 4.2.2 padding的百分比值

**4.2.2 padding的百分比值** 要解决的是：核对内边空间、百分比基准、内置样式和背景绘制范围。先预测元素在“确定 content 来源”前后会生成哪些盒、读取哪个包含块，并记录“padding”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 4.2.3 标签元素内置的padding

**4.2.3 标签元素内置的padding** 要解决的是：核对内边空间、百分比基准、内置样式和背景绘制范围。先预测元素在“计算 padding 空间”前后会生成哪些盒、读取哪个包含块，并记录“margin 合并”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 4.2.4 padding与图形绘制

**4.2.4 padding与图形绘制** 要解决的是：核对内边空间、百分比基准、内置样式和背景绘制范围。先预测元素在“求解 margin 外部尺寸”前后会生成哪些盒、读取哪个包含块，并记录“margin:auto”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 4.3 激进的margin属性

**4.3 激进的margin属性** 要解决的是：区分外部尺寸、百分比基准、合并、auto 分配与失效条件。先预测元素在“处理 margin 合并”前后会生成哪些盒、读取哪个包含块，并记录“border”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 4.3.1 margin与元素尺寸以及相关布局

**4.3.1 margin与元素尺寸以及相关布局** 要解决的是：确定外在盒、内在盒、包含块与 auto/百分比计算，避免把声明值直接当成最终使用尺寸。先预测元素在“分配 margin:auto”前后会生成哪些盒、读取哪个包含块，并记录“替换元素”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 4.3.2 margin的百分比值

**4.3.2 margin的百分比值** 要解决的是：区分外部尺寸、百分比基准、合并、auto 分配与失效条件。先预测元素在“绘制 border 边界”前后会生成哪些盒、读取哪个包含块，并记录“padding”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 4.3.3 正确看待CSS世界里的margin合并

**4.3.3 正确看待CSS世界里的margin合并** 要解决的是：区分外部尺寸、百分比基准、合并、auto 分配与失效条件。先预测元素在“确定 content 来源”前后会生成哪些盒、读取哪个包含块，并记录“margin 合并”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 4.3.4 深入理解CSS中的margin:auto

**4.3.4 深入理解CSS中的margin:auto** 要解决的是：区分外部尺寸、百分比基准、合并、auto 分配与失效条件。先预测元素在“计算 padding 空间”前后会生成哪些盒、读取哪个包含块，并记录“margin:auto”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 4.3.5 margin无效情形解析

**4.3.5 margin无效情形解析** 要解决的是：区分外部尺寸、百分比基准、合并、auto 分配与失效条件。先预测元素在“求解 margin 外部尺寸”前后会生成哪些盒、读取哪个包含块，并记录“border”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 4.4 功勋卓越的border属性

**4.4 功勋卓越的border属性** 要解决的是：分析边框宽度、样式、颜色、透明技巧及其对可见尺寸和图形构建的影响。先预测元素在“处理 margin 合并”前后会生成哪些盒、读取哪个包含块，并记录“替换元素”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 4.4.1 为什么border-width不支持百分比值

**4.4.1 为什么border-width不支持百分比值** 要解决的是：确定外在盒、内在盒、包含块与 auto/百分比计算，避免把声明值直接当成最终使用尺寸。先预测元素在“分配 margin:auto”前后会生成哪些盒、读取哪个包含块，并记录“padding”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 4.4.2 了解各种border-style类型

**4.4.2 了解各种border-style类型** 要解决的是：分析边框宽度、样式、颜色、透明技巧及其对可见尺寸和图形构建的影响。先预测元素在“绘制 border 边界”前后会生成哪些盒、读取哪个包含块，并记录“margin 合并”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 4.4.3 border-color和color

**4.4.3 border-color和color** 要解决的是：分析边框宽度、样式、颜色、透明技巧及其对可见尺寸和图形构建的影响。先预测元素在“确定 content 来源”前后会生成哪些盒、读取哪个包含块，并记录“margin:auto”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 4.4.4 border与透明边框技巧

**4.4.4 border与透明边框技巧** 要解决的是：分析边框宽度、样式、颜色、透明技巧及其对可见尺寸和图形构建的影响。先预测元素在“计算 padding 空间”前后会生成哪些盒、读取哪个包含块，并记录“border”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 4.4.5 border与图形构建

**4.4.5 border与图形构建** 要解决的是：分析边框宽度、样式、颜色、透明技巧及其对可见尺寸和图形构建的影响。先预测元素在“求解 margin 外部尺寸”前后会生成哪些盒、读取哪个包含块，并记录“替换元素”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 4.4.6 border等高布局技术

**4.4.6 border等高布局技术** 要解决的是：分析边框宽度、样式、颜色、透明技巧及其对可见尺寸和图形构建的影响。先预测元素在“处理 margin 合并”前后会生成哪些盒、读取哪个包含块，并记录“padding”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

## 最小可运行实验

```css
.avatar {
  box-sizing: border-box;
  width: 6rem;
  padding: 0.5rem;
  border: 0.25rem solid currentColor;
  margin-inline: auto;
}
.triangle {
  width: 0;
  border: 0.75rem solid transparent;
  border-top-color: currentColor;
}
```

```yaml
book: CSS 世界
page: csw-04-box-dimensions
public_toc_entries: 22
sample: normal | boundary | failure | recovery
first_divergence: null
box_tree_explained: true
used_values_explained: true
fluid_contract_preserved: true
```

```text
freeze DOM + stylesheet + viewport + fonts
predict generated boxes + containing blocks
change exactly one variable
stop at the first divergent used value
restore and replay the same DOM
assert fluid layout across content and directions
```

## 常见误区与故障注入

<Callout type="trap">
  **误区一：记住“第 4 章 盒尺寸四大家族”的属性结论，却不展开从“确定 content
  来源”到“绘制 border 边界”的过程。**
  相同截图可能依赖固定高度、偶然字体、错误包含块或过大的
  z-index，必须保存盒树与使用值证据。
</Callout>

<Callout type="trap">
  **误区二：把 Flex、Grid、transform 和 animation 塞进《CSS 世界》。**
  作者明确把本书定位为 CSS 2.1 体系；这些 CSS3 及后续能力属于《CSS
  新世界》，目录忠实度和现代替代方案应分开记录。
</Callout>

## 四类样本与验收

| 样本 | 注入方式                             | 必查证据                       | 通过条件               |
| ---- | ------------------------------------ | ------------------------------ | ---------------------- |
| 正常 | 标准字体、常规内容和宽视口           | 盒、包含块、使用值、行框、层级 | 六阶段按预期完成       |
| 边界 | 窄视口、长内容、替换元素、字体缺失   | min/max、溢出、基线和回退      | 不依赖固定像素补丁     |
| 失败 | 单一脱流、错误层叠、隐藏焦点或错方向 | 首个偏离点                     | 能定位并解释根因       |
| 恢复 | 删除故障声明后重放同一 DOM           | 盒树和计算样式                 | 无旧覆盖且流体契约恢复 |

本页签发不变量是：**content、padding、margin 和 border 分别控制内容生成、内边空间、外部尺寸与边界图形；百分比基准、替换元素、外边距合并和 margin:auto 分配必须放回格式化上下文理解。** 单次截图不能替代盒生成、格式化上下文、包含块、计算值、使用值与绘制顺序的完整轨迹。

## 练习

<Exercises>

**问题 1** 为什么不能只以最终截图正确作为第 4 章 盒尺寸四大家族的验收？

<Answer>
  截图会隐藏固定尺寸、错误包含块、字体偶然命中、溢出裁剪和层叠补丁。必须改变视口、内容、字体和流向，比较盒树、使用值与首个偏离点。
</Answer>

**问题 2** 如何把本页出版社目录变成可执行学习证据？

<Answer>
  按公开顺序登记“第4章 盒尺寸四大家族、4.1 深入理解content、4.1.1
  content与替换元素、4.1.2 content内容生成技术、4.2 温和的padding属性、4.2.1
  padding与元素的尺寸、4.2.2 padding的百分比值、4.2.3
  标签元素内置的padding、4.2.4 padding与图形绘制、4.3 激进的margin属性、4.3.1
  margin与元素尺寸以及相关布局、4.3.2 margin的百分比值、4.3.3
  正确看待CSS世界里的margin合并、4.3.4 深入理解CSS中的margin:auto、4.3.5
  margin无效情形解析、4.4 功勋卓越的border属性、4.4.1
  为什么border-width不支持百分比值、4.4.2 了解各种border-style类型、4.4.3
  border-color和color、4.4.4 border与透明边框技巧、4.4.5 border与图形构建、4.4.6
  border等高布局技术”，为每项写出生成盒、包含块、格式化上下文、计算值、使用值、失败和恢复条件，再用同一
  DOM 重放正常与反例。
</Answer>

**问题 3** 本页的最小通过条件是什么？

<Answer>
  每次尺寸计算都写出内容盒、内边距、边框和外部尺寸四层结果；合并、auto
  或替换元素参与时，必须额外记录包含块和格式化上下文。
  同时，正常、边界、失败和恢复轨迹都能复现，删除故障声明后不存在旧覆盖、旧层叠或隐藏溢出。
</Answer>

</Exercises>

## 小结

- **替换元素**：对应“确定 content 来源”阶段的核心观察量。
- **padding**：对应“计算 padding 空间”阶段的核心观察量。
- **margin 合并**：对应“求解 margin 外部尺寸”阶段的核心观察量。
- **margin:auto**：对应“处理 margin 合并”阶段的核心观察量。
- **border**：对应“分配 margin:auto”阶段的核心观察量。
- 22 个公开目录条目已经全部映射到本页证据链。
- 盒树、使用值和边界样本共同决定是否通过，最终截图不是唯一证据。

## 术语表

<Glossary>
  <GlossaryItem term="替换元素">
    替换元素 是本页第 1 个核心观察量，位于“确定 content
    来源”阶段。验收时同时记录包含块、格式化上下文、计算值、使用值与绘制结果。
  </GlossaryItem>
  <GlossaryItem term="padding">
    padding 是本页第 2 个核心观察量，位于“计算 padding
    空间”阶段。验收时同时记录包含块、格式化上下文、计算值、使用值与绘制结果。
  </GlossaryItem>
  <GlossaryItem term="margin 合并">
    margin 合并 是本页第 3 个核心观察量，位于“求解 margin
    外部尺寸”阶段。验收时同时记录包含块、格式化上下文、计算值、使用值与绘制结果。
  </GlossaryItem>
  <GlossaryItem term="margin:auto">
    margin:auto 是本页第 4 个核心观察量，位于“处理 margin
    合并”阶段。验收时同时记录包含块、格式化上下文、计算值、使用值与绘制结果。
  </GlossaryItem>
  <GlossaryItem term="border">
    border 是本页第 5 个核心观察量，位于“分配
    margin:auto”阶段。验收时同时记录包含块、格式化上下文、计算值、使用值与绘制结果。
  </GlossaryItem>
</Glossary>

## 版本与改编边界

本单元以张鑫旭著《CSS 世界》为身份基线。详细目录来自人民邮电出版社出版电子版页面，CSS 2.1 定位由作者官网和图书官网交叉确认；正文是独立教学重构，不复制原书段落。现代浏览器兼容状态需另行按当前 CSS 规范和浏览器文档核验。

<Attribution
  adaptedFrom="张鑫旭著，《CSS 世界》，人民邮电出版社，2017 年 12 月，328 页，ISBN 9787115470669。本页按出版社公开目录与作者官网独立教学重构。"
  adaptedUrl="https://www.dedao.cn/ebook/detail?id=V5R16yPmaYOMqGRAv82jkX4KDe175w771mwrbx6pNgznl9VZPLJQyEBodb89mqoO"
/>
