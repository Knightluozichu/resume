---
title: "第 5 章 内联元素与流"
type: A
section: "CSS 世界 · 流与尺寸"
order: 5
description: "内联格式化由字符度量、行内盒、行框盒和基线共同决定；line-height 形成行框高度，vertical-align 改变基线对齐，两者联动解释图片底部空隙、垂直居中和大值特性。"
demo: true
math: false
sourceUrl: "https://www.dedao.cn/ebook/detail?id=V5R16yPmaYOMqGRAv82jkX4KDe175w771mwrbx6pNgznl9VZPLJQyEBodb89mqoO"
draft: false
---

import {
  Csw05InlineFlowMapLab,
  Csw05InlineFlowExperimentLab,
  Csw05InlineFlowEvidenceLab,
} from "@/components/mdx/css-world/diagrams/csw-05-inline-flow";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第 5 章 内联元素与流的核心主张，并把它放回原书 12 章 CSS 2.1 体系
- 能按出版社目录逐项描述 19 个标题的盒、包含块、计算值和使用值
- 能实现本页最小样例，定位流、尺寸、内联、定位、层叠或绘制的首个偏离点
- 能比较正常、边界、失败和恢复四类样本，判断布局是否仍保持流体契约

</Objectives>

## 直觉起点

<Term def="x-height 是“第 5 章 内联元素与流”的第 1 个核心观察量，位于“读取字体 x-height”阶段；必须同时说明指定值、计算值、使用值、实际盒与失败边界。">x-height</Term>、<Term def="行内盒 是“第 5 章 内联元素与流”的第 2 个核心观察量，位于“建立内容区域”阶段；必须同时说明指定值、计算值、使用值、实际盒与失败边界。">行内盒</Term>、<Term def="行框盒 是“第 5 章 内联元素与流”的第 3 个核心观察量，位于“计算行内盒”阶段；必须同时说明指定值、计算值、使用值、实际盒与失败边界。">行框盒</Term>、<Term def="line-height 是“第 5 章 内联元素与流”的第 4 个核心观察量，位于“形成行框盒”阶段；必须同时说明指定值、计算值、使用值、实际盒与失败边界。">line-height</Term>、<Term def="vertical-align 是“第 5 章 内联元素与流”的第 5 个核心观察量，位于“应用 line-height”阶段；必须同时说明指定值、计算值、使用值、实际盒与失败边界。">vertical-align</Term>
共同构成本页的观察坐标。内联格式化由字符度量、行内盒、行框盒和基线共同决定；line-height
形成行框高度，vertical-align
改变基线对齐，两者联动解释图片底部空隙、垂直居中和大值特性。 CSS
的困难不在属性名称，而在多个声明经过级联、继承和格式化后才产生最终使用值；同一截图可能由完全不同的盒树、固定尺寸或偶然层叠得到，必须向前追踪原因。

<Csw05InlineFlowMapLab />

## 六阶段机制链

### 1. 读取字体 x-height

固定 DOM、样式表、视口、字体和内容，先预测生成盒、包含块、格式化上下文、计算值、使用尺寸、行框和层叠位置。一旦轨迹不同，保存首个偏离点，不用最终像素掩盖过程。

### 2. 建立内容区域

只改变一个声明或环境变量，记录生成盒、包含块、格式化上下文、计算值、使用尺寸、行框和层叠位置。一旦轨迹不同，保存首个偏离点，不用最终像素掩盖过程。

### 3. 计算行内盒

只改变一个声明或环境变量，记录生成盒、包含块、格式化上下文、计算值、使用尺寸、行框和层叠位置。一旦轨迹不同，保存首个偏离点，不用最终像素掩盖过程。

### 4. 形成行框盒

只改变一个声明或环境变量，记录生成盒、包含块、格式化上下文、计算值、使用尺寸、行框和层叠位置。一旦轨迹不同，保存首个偏离点，不用最终像素掩盖过程。

### 5. 应用 line-height

只改变一个声明或环境变量，记录生成盒、包含块、格式化上下文、计算值、使用尺寸、行框和层叠位置。一旦轨迹不同，保存首个偏离点，不用最终像素掩盖过程。

### 6. 按基线 vertical-align

只改变一个声明或环境变量，记录生成盒、包含块、格式化上下文、计算值、使用尺寸、行框和层叠位置。恢复后重放同一输入，并确认窄宽、长内容和方向变化不会暴露旧补丁。

## 核心机制深挖

一行的高度由其中最高的行内盒决定，不等于某个元素的 height。图片默认按基线对齐，基线下方要保留字母下行空间，因此常出现底部空隙；vertical-align 只有在内联或表格单元上下文才生效。 调垂直位置前先画出基线、x-height、行内盒和行框盒；若用固定 top 或负 margin 试错而说不清基线，方案不可签发。 实验时把浏览器开发者工具中的盒模型、计算样式和层叠上下文当成观测工具，而不是答案来源；答案必须能从规范规则和当前 DOM 结构推导出来。先删除非必要覆盖，再逐条加入约束，这样才能分辨默认流在做什么、哪条声明真正改变了结果。

## 边界记录模板

每次实验建立六列证据：**DOM 与内容、生成盒与格式化上下文、包含块、计算值与使用值、首个偏离点、恢复结果**。尺寸页增加视口与固有尺寸，内联页增加字体度量和基线，定位页增加脱流空间与四向偏移，层叠页增加上下文树，文本页增加字体回退，界面页增加键盘与书写方向。若最终外观相同但固定宽高、隐藏溢出、不可见焦点或错误层叠仍存在，依然失败。

## 出版社目录逐项讲解

### 第5章 内联元素与流

**第5章 内联元素与流** 要解决的是：画出内容区域、行内盒、行框盒与基线，按字体度量解释垂直位置。先预测元素在“读取字体 x-height”前后会生成哪些盒、读取哪个包含块，并记录“x-height”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 5.1 字母x——CSS世界中隐匿的举足轻重的角色

**5.1 字母x——CSS世界中隐匿的举足轻重的角色** 要解决的是：把该目录项放回正常流、盒生成、尺寸计算和格式化上下文，记录输入、约束与浏览器使用值。先预测元素在“建立内容区域”前后会生成哪些盒、读取哪个包含块，并记录“行内盒”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 5.1.1 字母x与CSS世界的基线

**5.1.1 字母x与CSS世界的基线** 要解决的是：画出内容区域、行内盒、行框盒与基线，按字体度量解释垂直位置。先预测元素在“计算行内盒”前后会生成哪些盒、读取哪个包含块，并记录“行框盒”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 5.1.2 字母x与CSS中的x-height

**5.1.2 字母x与CSS中的x-height** 要解决的是：确定外在盒、内在盒、包含块与 auto/百分比计算，避免把声明值直接当成最终使用尺寸。先预测元素在“形成行框盒”前后会生成哪些盒、读取哪个包含块，并记录“line-height”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 5.1.3 字母x与CSS中的ex

**5.1.3 字母x与CSS中的ex** 要解决的是：把该目录项放回正常流、盒生成、尺寸计算和格式化上下文，记录输入、约束与浏览器使用值。先预测元素在“应用 line-height”前后会生成哪些盒、读取哪个包含块，并记录“vertical-align”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 5.2 内联元素的基石line-height

**5.2 内联元素的基石line-height** 要解决的是：确定外在盒、内在盒、包含块与 auto/百分比计算，避免把声明值直接当成最终使用尺寸。先预测元素在“按基线 vertical-align”前后会生成哪些盒、读取哪个包含块，并记录“x-height”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 5.2.1 内联元素的高度之本——line-height

**5.2.1 内联元素的高度之本——line-height** 要解决的是：确定外在盒、内在盒、包含块与 auto/百分比计算，避免把声明值直接当成最终使用尺寸。先预测元素在“读取字体 x-height”前后会生成哪些盒、读取哪个包含块，并记录“行内盒”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 5.2.2 为什么line-height可以让内联元素“垂直居中”

**5.2.2 为什么line-height可以让内联元素“垂直居中”** 要解决的是：确定外在盒、内在盒、包含块与 auto/百分比计算，避免把声明值直接当成最终使用尺寸。先预测元素在“建立内容区域”前后会生成哪些盒、读取哪个包含块，并记录“行框盒”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 5.2.3 深入line-height的各类属性值

**5.2.3 深入line-height的各类属性值** 要解决的是：确定外在盒、内在盒、包含块与 auto/百分比计算，避免把声明值直接当成最终使用尺寸。先预测元素在“计算行内盒”前后会生成哪些盒、读取哪个包含块，并记录“line-height”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 5.2.4 内联元素line-height的“大值特性”

**5.2.4 内联元素line-height的“大值特性”** 要解决的是：确定外在盒、内在盒、包含块与 auto/百分比计算，避免把声明值直接当成最终使用尺寸。先预测元素在“形成行框盒”前后会生成哪些盒、读取哪个包含块，并记录“vertical-align”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 5.3 line-height的好朋友vertical-align

**5.3 line-height的好朋友vertical-align** 要解决的是：确定外在盒、内在盒、包含块与 auto/百分比计算，避免把声明值直接当成最终使用尺寸。先预测元素在“应用 line-height”前后会生成哪些盒、读取哪个包含块，并记录“x-height”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 5.3.1 vertical-align家族基本认识

**5.3.1 vertical-align家族基本认识** 要解决的是：画出内容区域、行内盒、行框盒与基线，按字体度量解释垂直位置。先预测元素在“按基线 vertical-align”前后会生成哪些盒、读取哪个包含块，并记录“行内盒”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 5.3.2 vertical-align作用的前提

**5.3.2 vertical-align作用的前提** 要解决的是：画出内容区域、行内盒、行框盒与基线，按字体度量解释垂直位置。先预测元素在“读取字体 x-height”前后会生成哪些盒、读取哪个包含块，并记录“行框盒”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 5.3.3 vertical-align和line-height之间的关系

**5.3.3 vertical-align和line-height之间的关系** 要解决的是：确定外在盒、内在盒、包含块与 auto/百分比计算，避免把声明值直接当成最终使用尺寸。先预测元素在“建立内容区域”前后会生成哪些盒、读取哪个包含块，并记录“line-height”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 5.3.4 深入理解vertical-align线性类属性值

**5.3.4 深入理解vertical-align线性类属性值** 要解决的是：画出内容区域、行内盒、行框盒与基线，按字体度量解释垂直位置。先预测元素在“计算行内盒”前后会生成哪些盒、读取哪个包含块，并记录“vertical-align”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 5.3.5 深入理解vertical-align文本类属性值

**5.3.5 深入理解vertical-align文本类属性值** 要解决的是：画出内容区域、行内盒、行框盒与基线，按字体度量解释垂直位置。先预测元素在“形成行框盒”前后会生成哪些盒、读取哪个包含块，并记录“x-height”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 5.3.6 简单了解vertical-align上标下标类属性值

**5.3.6 简单了解vertical-align上标下标类属性值** 要解决的是：画出内容区域、行内盒、行框盒与基线，按字体度量解释垂直位置。先预测元素在“应用 line-height”前后会生成哪些盒、读取哪个包含块，并记录“行内盒”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 5.3.7 无处不在的vertical-align

**5.3.7 无处不在的vertical-align** 要解决的是：画出内容区域、行内盒、行框盒与基线，按字体度量解释垂直位置。先预测元素在“按基线 vertical-align”前后会生成哪些盒、读取哪个包含块，并记录“行框盒”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 5.3.8 基于vertical-align属性的水平垂直居中弹框

**5.3.8 基于vertical-align属性的水平垂直居中弹框** 要解决的是：画出内容区域、行内盒、行框盒与基线，按字体度量解释垂直位置。先预测元素在“读取字体 x-height”前后会生成哪些盒、读取哪个包含块，并记录“line-height”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

## 分步可视实验

<Stepper>
  <Step title="1. 读取字体 x-height与建立内容区域">
    先预测盒树和默认流，再运行正常样本保存基线。
    <Csw05InlineFlowMapLab />
  </Step>
  <Step title="2. 计算行内盒与形成行框盒">
    改变容器、内容或单一声明，在首个无法满足契约的位置停止。
    <Csw05InlineFlowExperimentLab />
  </Step>
  <Step title="3. 应用 line-height与按基线 vertical-align">
    清除覆盖并重放同一 DOM，核对尺寸、行框、层级与绘制全部收敛。
    <Csw05InlineFlowEvidenceLab />
  </Step>
</Stepper>

## 最小可运行实验

```css
.icon-text {
  font-size: 16px;
  line-height: 1.5;
}
.icon-text > img {
  width: 1em;
  height: 1em;
  vertical-align: -0.15em;
}
```

```yaml
book: CSS 世界
page: csw-05-inline-flow
public_toc_entries: 19
sample: normal | boundary | failure | recovery
first_divergence: null
box_tree_explained: true
used_values_explained: true
fluid_contract_preserved: true
```

```text
freeze DOM + stylesheet + viewport + fonts
predict generated boxes + containing blocks
change exactly one variable
stop at the first divergent used value
restore and replay the same DOM
assert fluid layout across content and directions
```

## 常见误区与故障注入

<Callout type="trap">
  **误区一：记住“第 5 章 内联元素与流”的属性结论，却不展开从“读取字体
  x-height”到“按基线 vertical-align”的过程。**
  相同截图可能依赖固定高度、偶然字体、错误包含块或过大的
  z-index，必须保存盒树与使用值证据。
</Callout>

<Callout type="trap">
  **误区二：把 Flex、Grid、transform 和 animation 塞进《CSS 世界》。**
  作者明确把本书定位为 CSS 2.1 体系；这些 CSS3 及后续能力属于《CSS
  新世界》，目录忠实度和现代替代方案应分开记录。
</Callout>

## 四类样本与验收

| 样本 | 注入方式                             | 必查证据                       | 通过条件               |
| ---- | ------------------------------------ | ------------------------------ | ---------------------- |
| 正常 | 标准字体、常规内容和宽视口           | 盒、包含块、使用值、行框、层级 | 六阶段按预期完成       |
| 边界 | 窄视口、长内容、替换元素、字体缺失   | min/max、溢出、基线和回退      | 不依赖固定像素补丁     |
| 失败 | 单一脱流、错误层叠、隐藏焦点或错方向 | 首个偏离点                     | 能定位并解释根因       |
| 恢复 | 删除故障声明后重放同一 DOM           | 盒树和计算样式                 | 无旧覆盖且流体契约恢复 |

<Csw05InlineFlowEvidenceLab />

本页签发不变量是：**内联格式化由字符度量、行内盒、行框盒和基线共同决定；line-height 形成行框高度，vertical-align 改变基线对齐，两者联动解释图片底部空隙、垂直居中和大值特性。** 单次截图不能替代盒生成、格式化上下文、包含块、计算值、使用值与绘制顺序的完整轨迹。

## 练习

<Exercises>

**问题 1** 为什么不能只以最终截图正确作为第 5 章 内联元素与流的验收？

<Answer>
  截图会隐藏固定尺寸、错误包含块、字体偶然命中、溢出裁剪和层叠补丁。必须改变视口、内容、字体和流向，比较盒树、使用值与首个偏离点。
</Answer>

**问题 2** 如何把本页出版社目录变成可执行学习证据？

<Answer>
  按公开顺序登记“第5章 内联元素与流、5.1
  字母x——CSS世界中隐匿的举足轻重的角色、5.1.1 字母x与CSS世界的基线、5.1.2
  字母x与CSS中的x-height、5.1.3 字母x与CSS中的ex、5.2
  内联元素的基石line-height、5.2.1 内联元素的高度之本——line-height、5.2.2
  为什么line-height可以让内联元素“垂直居中”、5.2.3
  深入line-height的各类属性值、5.2.4 内联元素line-height的“大值特性”、5.3
  line-height的好朋友vertical-align、5.3.1 vertical-align家族基本认识、5.3.2
  vertical-align作用的前提、5.3.3 vertical-align和line-height之间的关系、5.3.4
  深入理解vertical-align线性类属性值、5.3.5
  深入理解vertical-align文本类属性值、5.3.6
  简单了解vertical-align上标下标类属性值、5.3.7 无处不在的vertical-align、5.3.8
  基于vertical-align属性的水平垂直居中弹框”，为每项写出生成盒、包含块、格式化上下文、计算值、使用值、失败和恢复条件，再用同一
  DOM 重放正常与反例。
</Answer>

**问题 3** 本页的最小通过条件是什么？

<Answer>
  调垂直位置前先画出基线、x-height、行内盒和行框盒；若用固定 top 或负 margin
  试错而说不清基线，方案不可签发。
  同时，正常、边界、失败和恢复轨迹都能复现，删除故障声明后不存在旧覆盖、旧层叠或隐藏溢出。
</Answer>

</Exercises>

## 小结

- **x-height**：对应“读取字体 x-height”阶段的核心观察量。
- **行内盒**：对应“建立内容区域”阶段的核心观察量。
- **行框盒**：对应“计算行内盒”阶段的核心观察量。
- **line-height**：对应“形成行框盒”阶段的核心观察量。
- **vertical-align**：对应“应用 line-height”阶段的核心观察量。
- 19 个公开目录条目已经全部映射到本页证据链。
- 盒树、使用值和边界样本共同决定是否通过，最终截图不是唯一证据。

## 术语表

<Glossary>
  <GlossaryItem term="x-height">
    x-height 是本页第 1 个核心观察量，位于“读取字体
    x-height”阶段。验收时同时记录包含块、格式化上下文、计算值、使用值与绘制结果。
  </GlossaryItem>
  <GlossaryItem term="行内盒">
    行内盒 是本页第 2
    个核心观察量，位于“建立内容区域”阶段。验收时同时记录包含块、格式化上下文、计算值、使用值与绘制结果。
  </GlossaryItem>
  <GlossaryItem term="行框盒">
    行框盒 是本页第 3
    个核心观察量，位于“计算行内盒”阶段。验收时同时记录包含块、格式化上下文、计算值、使用值与绘制结果。
  </GlossaryItem>
  <GlossaryItem term="line-height">
    line-height 是本页第 4
    个核心观察量，位于“形成行框盒”阶段。验收时同时记录包含块、格式化上下文、计算值、使用值与绘制结果。
  </GlossaryItem>
  <GlossaryItem term="vertical-align">
    vertical-align 是本页第 5 个核心观察量，位于“应用
    line-height”阶段。验收时同时记录包含块、格式化上下文、计算值、使用值与绘制结果。
  </GlossaryItem>
</Glossary>

## 版本与改编边界

本单元以张鑫旭著《CSS 世界》为身份基线。详细目录来自人民邮电出版社出版电子版页面，CSS 2.1 定位由作者官网和图书官网交叉确认；正文是独立教学重构，不复制原书段落。现代浏览器兼容状态需另行按当前 CSS 规范和浏览器文档核验。

<Attribution
  adaptedFrom="张鑫旭著，《CSS 世界》，人民邮电出版社，2017 年 12 月，328 页，ISBN 9787115470669。本页按出版社公开目录与作者官网独立教学重构。"
  adaptedUrl="https://www.dedao.cn/ebook/detail?id=V5R16yPmaYOMqGRAv82jkX4KDe175w771mwrbx6pNgznl9VZPLJQyEBodb89mqoO"
/>
