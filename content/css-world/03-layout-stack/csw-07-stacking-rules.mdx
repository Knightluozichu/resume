---
title: "第 7 章 CSS 世界的层叠规则"
type: A
section: "CSS 世界 · 流、定位与层叠"
order: 7
description: "z-index 只在层叠上下文中比较层叠水平；背景、负值、块级盒、浮动、内联内容、定位元素等按规定层叠顺序绘制，子上下文整体受父上下文约束。"
demo: true
math: false
sourceUrl: "https://www.dedao.cn/ebook/detail?id=V5R16yPmaYOMqGRAv82jkX4KDe175w771mwrbx6pNgznl9VZPLJQyEBodb89mqoO"
draft: false
---


## 直觉起点

、、、、共同构成本页的观察坐标。z-index
只在层叠上下文中比较层叠水平；背景、负值、块级盒、浮动、内联内容、定位元素等按规定层叠顺序绘制，子上下文整体受父上下文约束。
CSS
的困难不在属性名称，而在多个声明经过级联、继承和格式化后才产生最终使用值；同一截图可能由完全不同的盒树、固定尺寸或偶然层叠得到，必须向前追踪原因。

## 六阶段机制链

### 1. 识别层叠上下文

固定 DOM、样式表、视口、字体和内容，先预测生成盒、包含块、格式化上下文、计算值、使用尺寸、行框和层叠位置。一旦轨迹不同，保存首个偏离点，不用最终像素掩盖过程。

### 2. 列出层叠水平

只改变一个声明或环境变量，记录生成盒、包含块、格式化上下文、计算值、使用尺寸、行框和层叠位置。一旦轨迹不同，保存首个偏离点，不用最终像素掩盖过程。

### 3. 按顺序绘制

只改变一个声明或环境变量，记录生成盒、包含块、格式化上下文、计算值、使用尺寸、行框和层叠位置。一旦轨迹不同，保存首个偏离点，不用最终像素掩盖过程。

### 4. 比较同级 z-index

只改变一个声明或环境变量，记录生成盒、包含块、格式化上下文、计算值、使用尺寸、行框和层叠位置。一旦轨迹不同，保存首个偏离点，不用最终像素掩盖过程。

### 5. 封装子上下文

只改变一个声明或环境变量，记录生成盒、包含块、格式化上下文、计算值、使用尺寸、行框和层叠位置。一旦轨迹不同，保存首个偏离点，不用最终像素掩盖过程。

### 6. 限制层级变量

只改变一个声明或环境变量，记录生成盒、包含块、格式化上下文、计算值、使用尺寸、行框和层叠位置。恢复后重放同一输入，并确认窄宽、长内容和方向变化不会暴露旧补丁。

## 核心机制深挖

层叠上下文像原子分组：内部元素先按自己的层叠水平排序，整个分组再作为一个单位参与父级排序。因此子元素设置极大 z-index 也不能越过父上下文的同级兄弟。 排查遮挡时先列上下文树，再比较同一父级下的层叠水平；禁止通过不断增大数字绕过结构问题，组件只使用有限语义层级。 实验时把浏览器开发者工具中的盒模型、计算样式和层叠上下文当成观测工具，而不是答案来源；答案必须能从规范规则和当前 DOM 结构推导出来。先删除非必要覆盖，再逐条加入约束，这样才能分辨默认流在做什么、哪条声明真正改变了结果。

## 边界记录模板

每次实验建立六列证据：**DOM 与内容、生成盒与格式化上下文、包含块、计算值与使用值、首个偏离点、恢复结果**。尺寸页增加视口与固有尺寸，内联页增加字体度量和基线，定位页增加脱流空间与四向偏移，层叠页增加上下文树，文本页增加字体回退，界面页增加键盘与书写方向。若最终外观相同但固定宽高、隐藏溢出、不可见焦点或错误层叠仍存在，依然失败。

## 出版社目录逐项讲解

### 第7章 CSS世界的层叠规则

**第7章 CSS世界的层叠规则** 要解决的是：先建立层叠上下文树，再在同级比较层叠水平和绘制顺序。先预测元素在“识别层叠上下文”前后会生成哪些盒、读取哪个包含块，并记录“层叠上下文”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 7.1 z-index只是CSS层叠规则中的一叶小舟

**7.1 z-index只是CSS层叠规则中的一叶小舟** 要解决的是：先建立层叠上下文树，再在同级比较层叠水平和绘制顺序。先预测元素在“列出层叠水平”前后会生成哪些盒、读取哪个包含块，并记录“层叠水平”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 7.2 理解CSS世界的层叠上下文和层叠水平

**7.2 理解CSS世界的层叠上下文和层叠水平** 要解决的是：先建立层叠上下文树，再在同级比较层叠水平和绘制顺序。先预测元素在“按顺序绘制”前后会生成哪些盒、读取哪个包含块，并记录“层叠顺序”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 7.2.1 什么是层叠上下文

**7.2.1 什么是层叠上下文** 要解决的是：先建立层叠上下文树，再在同级比较层叠水平和绘制顺序。先预测元素在“比较同级 z-index”前后会生成哪些盒、读取哪个包含块，并记录“z-index”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 7.2.2 什么是层叠水平

**7.2.2 什么是层叠水平** 要解决的是：先建立层叠上下文树，再在同级比较层叠水平和绘制顺序。先预测元素在“封装子上下文”前后会生成哪些盒、读取哪个包含块，并记录“负层级”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 7.3 理解元素的层叠顺序

**7.3 理解元素的层叠顺序** 要解决的是：先建立层叠上下文树，再在同级比较层叠水平和绘制顺序。先预测元素在“限制层级变量”前后会生成哪些盒、读取哪个包含块，并记录“层叠上下文”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 7.4 务必牢记的层叠准则

**7.4 务必牢记的层叠准则** 要解决的是：先建立层叠上下文树，再在同级比较层叠水平和绘制顺序。先预测元素在“识别层叠上下文”前后会生成哪些盒、读取哪个包含块，并记录“层叠水平”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 7.5 深入了解层叠上下文

**7.5 深入了解层叠上下文** 要解决的是：先建立层叠上下文树，再在同级比较层叠水平和绘制顺序。先预测元素在“列出层叠水平”前后会生成哪些盒、读取哪个包含块，并记录“层叠顺序”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 7.5.1 层叠上下文的特性

**7.5.1 层叠上下文的特性** 要解决的是：先建立层叠上下文树，再在同级比较层叠水平和绘制顺序。先预测元素在“按顺序绘制”前后会生成哪些盒、读取哪个包含块，并记录“z-index”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 7.5.2 层叠上下文的创建

**7.5.2 层叠上下文的创建** 要解决的是：先建立层叠上下文树，再在同级比较层叠水平和绘制顺序。先预测元素在“比较同级 z-index”前后会生成哪些盒、读取哪个包含块，并记录“负层级”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 7.5.3 层叠上下文与层叠顺序

**7.5.3 层叠上下文与层叠顺序** 要解决的是：先建立层叠上下文树，再在同级比较层叠水平和绘制顺序。先预测元素在“封装子上下文”前后会生成哪些盒、读取哪个包含块，并记录“层叠上下文”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 7.6 z-index负值深入理解

**7.6 z-index负值深入理解** 要解决的是：先建立层叠上下文树，再在同级比较层叠水平和绘制顺序。先预测元素在“限制层级变量”前后会生成哪些盒、读取哪个包含块，并记录“层叠水平”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 7.7 z-index“不犯二”准则

**7.7 z-index“不犯二”准则** 要解决的是：先建立层叠上下文树，再在同级比较层叠水平和绘制顺序。先预测元素在“识别层叠上下文”前后会生成哪些盒、读取哪个包含块，并记录“层叠顺序”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

## 最小可运行实验

```css
.shell {
  position: relative;
  z-index: 0;
}
.content {
  position: relative;
  z-index: 1;
}
.overlay {
  position: fixed;
  z-index: 20;
}
.modal {
  position: fixed;
  z-index: 30;
}
/* 子元素再大，也无法越过父层叠上下文的同级顺序。 */
```

```yaml
book: CSS 世界
page: csw-07-stacking-rules
public_toc_entries: 13
sample: normal | boundary | failure | recovery
first_divergence: null
box_tree_explained: true
used_values_explained: true
fluid_contract_preserved: true
```

```text
freeze DOM + stylesheet + viewport + fonts
predict generated boxes + containing blocks
change exactly one variable
stop at the first divergent used value
restore and replay the same DOM
assert fluid layout across content and directions
```

## 常见误区与故障注入


## 四类样本与验收

| 样本 | 注入方式                             | 必查证据                       | 通过条件               |
| ---- | ------------------------------------ | ------------------------------ | ---------------------- |
| 正常 | 标准字体、常规内容和宽视口           | 盒、包含块、使用值、行框、层级 | 六阶段按预期完成       |
| 边界 | 窄视口、长内容、替换元素、字体缺失   | min/max、溢出、基线和回退      | 不依赖固定像素补丁     |
| 失败 | 单一脱流、错误层叠、隐藏焦点或错方向 | 首个偏离点                     | 能定位并解释根因       |
| 恢复 | 删除故障声明后重放同一 DOM           | 盒树和计算样式                 | 无旧覆盖且流体契约恢复 |

本页签发不变量是：**z-index 只在层叠上下文中比较层叠水平；背景、负值、块级盒、浮动、内联内容、定位元素等按规定层叠顺序绘制，子上下文整体受父上下文约束。** 单次截图不能替代盒生成、格式化上下文、包含块、计算值、使用值与绘制顺序的完整轨迹。

## 练习


## 小结

- **层叠上下文**：对应“识别层叠上下文”阶段的核心观察量。
- **层叠水平**：对应“列出层叠水平”阶段的核心观察量。
- **层叠顺序**：对应“按顺序绘制”阶段的核心观察量。
- **z-index**：对应“比较同级 z-index”阶段的核心观察量。
- **负层级**：对应“封装子上下文”阶段的核心观察量。
- 13 个公开目录条目已经全部映射到本页证据链。
- 盒树、使用值和边界样本共同决定是否通过，最终截图不是唯一证据。

## 术语表


## 版本与改编边界

本单元以张鑫旭著《CSS 世界》为身份基线。详细目录来自人民邮电出版社出版电子版页面，CSS 2.1 定位由作者官网和图书官网交叉确认；正文是独立教学重构，不复制原书段落。现代浏览器兼容状态需另行按当前 CSS 规范和浏览器文档核验。

