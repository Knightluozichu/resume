---
title: "第 9 章 元素的装饰与美化"
type: A
section: "CSS 世界 · 文本与装饰"
order: 9
description: "color 提供可继承的前景色并通过 currentColor 联动边框，background 在边框盒内分层绘制颜色与图像；位置百分比、重复、固定附着和资源加载都有独立计算规则。"
demo: true
math: false
sourceUrl: "https://www.dedao.cn/ebook/detail?id=V5R16yPmaYOMqGRAv82jkX4KDe175w771mwrbx6pNgznl9VZPLJQyEBodb89mqoO"
draft: false
---

import {
  Csw09DecorationMapLab,
  Csw09DecorationExperimentLab,
  Csw09DecorationEvidenceLab,
} from "@/components/mdx/css-world/diagrams/csw-09-decoration";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第 9 章 元素的装饰与美化的核心主张，并把它放回原书 12 章 CSS 2.1 体系
- 能按出版社目录逐项描述 15 个标题的盒、包含块、计算值和使用值
- 能实现本页最小样例，定位流、尺寸、内联、定位、层叠或绘制的首个偏离点
- 能比较正常、边界、失败和恢复四类样本，判断布局是否仍保持流体契约

</Objectives>

## 直觉起点

<Term def="currentColor 是“第 9 章 元素的装饰与美化”的第 1 个核心观察量，位于“解析颜色值”阶段；必须同时说明指定值、计算值、使用值、实际盒与失败边界。">
  currentColor
</Term>
、
<Term def="background-color 是“第 9 章 元素的装饰与美化”的第 2 个核心观察量，位于“传播 currentColor”阶段；必须同时说明指定值、计算值、使用值、实际盒与失败边界。">
  background-color
</Term>
、
<Term def="background-position 是“第 9 章 元素的装饰与美化”的第 3 个核心观察量，位于“铺设背景色”阶段；必须同时说明指定值、计算值、使用值、实际盒与失败边界。">
  background-position
</Term>
、
<Term def="background-repeat 是“第 9 章 元素的装饰与美化”的第 4 个核心观察量，位于“定位背景图”阶段；必须同时说明指定值、计算值、使用值、实际盒与失败边界。">
  background-repeat
</Term>
、
<Term def="背景附着 是“第 9 章 元素的装饰与美化”的第 5 个核心观察量，位于“应用重复与附着”阶段；必须同时说明指定值、计算值、使用值、实际盒与失败边界。">
  背景附着
</Term>
共同构成本页的观察坐标。color 提供可继承的前景色并通过 currentColor
联动边框，background
在边框盒内分层绘制颜色与图像；位置百分比、重复、固定附着和资源加载都有独立计算规则。
CSS
的困难不在属性名称，而在多个声明经过级联、继承和格式化后才产生最终使用值；同一截图可能由完全不同的盒树、固定尺寸或偶然层叠得到，必须向前追踪原因。

<Csw09DecorationMapLab />

## 六阶段机制链

### 1. 解析颜色值

固定 DOM、样式表、视口、字体和内容，先预测生成盒、包含块、格式化上下文、计算值、使用尺寸、行框和层叠位置。一旦轨迹不同，保存首个偏离点，不用最终像素掩盖过程。

### 2. 传播 currentColor

只改变一个声明或环境变量，记录生成盒、包含块、格式化上下文、计算值、使用尺寸、行框和层叠位置。一旦轨迹不同，保存首个偏离点，不用最终像素掩盖过程。

### 3. 铺设背景色

只改变一个声明或环境变量，记录生成盒、包含块、格式化上下文、计算值、使用尺寸、行框和层叠位置。一旦轨迹不同，保存首个偏离点，不用最终像素掩盖过程。

### 4. 定位背景图

只改变一个声明或环境变量，记录生成盒、包含块、格式化上下文、计算值、使用尺寸、行框和层叠位置。一旦轨迹不同，保存首个偏离点，不用最终像素掩盖过程。

### 5. 应用重复与附着

只改变一个声明或环境变量，记录生成盒、包含块、格式化上下文、计算值、使用尺寸、行框和层叠位置。一旦轨迹不同，保存首个偏离点，不用最终像素掩盖过程。

### 6. 验证隐藏和兼容状态

只改变一个声明或环境变量，记录生成盒、包含块、格式化上下文、计算值、使用尺寸、行框和层叠位置。恢复后重放同一输入，并确认窄宽、长内容和方向变化不会暴露旧补丁。

## 核心机制深挖

背景位置百分比按容器剩余空间，也就是容器尺寸减背景图尺寸计算，不是直接取容器百分比。背景色永远在所有背景图下方，currentColor 则把前景色变成可复用颜色变量。 装饰不能改变内容可读性和盒尺寸契约；资源失败、透明颜色、系统主题和高对比模式下，仍要保留可辨认边界与文字。 实验时把浏览器开发者工具中的盒模型、计算样式和层叠上下文当成观测工具，而不是答案来源；答案必须能从规范规则和当前 DOM 结构推导出来。先删除非必要覆盖，再逐条加入约束，这样才能分辨默认流在做什么、哪条声明真正改变了结果。

## 边界记录模板

每次实验建立六列证据：**DOM 与内容、生成盒与格式化上下文、包含块、计算值与使用值、首个偏离点、恢复结果**。尺寸页增加视口与固有尺寸，内联页增加字体度量和基线，定位页增加脱流空间与四向偏移，层叠页增加上下文树，文本页增加字体回退，界面页增加键盘与书写方向。若最终外观相同但固定宽高、隐藏溢出、不可见焦点或错误层叠仍存在，依然失败。

## 出版社目录逐项讲解

### 第9章 元素的装饰与美化

**第9章 元素的装饰与美化** 要解决的是：把该目录项放回正常流、盒生成、尺寸计算和格式化上下文，记录输入、约束与浏览器使用值。先预测元素在“解析颜色值”前后会生成哪些盒、读取哪个包含块，并记录“currentColor”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 9.1 CSS世界的color很单调

**9.1 CSS世界的color很单调** 要解决的是：分离前景色继承、背景分层、图像位置、重复、附着和资源失败状态。先预测元素在“传播 currentColor”前后会生成哪些盒、读取哪个包含块，并记录“background-color”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 9.1.1 少得可怜的颜色关键字

**9.1.1 少得可怜的颜色关键字** 要解决的是：分离前景色继承、背景分层、图像位置、重复、附着和资源失败状态。先预测元素在“铺设背景色”前后会生成哪些盒、读取哪个包含块，并记录“background-position”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 9.1.2 不支持的transparent关键字

**9.1.2 不支持的transparent关键字** 要解决的是：把该目录项放回正常流、盒生成、尺寸计算和格式化上下文，记录输入、约束与浏览器使用值。先预测元素在“定位背景图”前后会生成哪些盒、读取哪个包含块，并记录“background-repeat”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 9.1.3 不支持的currentColor变量

**9.1.3 不支持的currentColor变量** 要解决的是：把该目录项放回正常流、盒生成、尺寸计算和格式化上下文，记录输入、约束与浏览器使用值。先预测元素在“应用重复与附着”前后会生成哪些盒、读取哪个包含块，并记录“背景附着”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 9.1.4 不支持的rgba颜色和hsla颜色

**9.1.4 不支持的rgba颜色和hsla颜色** 要解决的是：分离前景色继承、背景分层、图像位置、重复、附着和资源失败状态。先预测元素在“验证隐藏和兼容状态”前后会生成哪些盒、读取哪个包含块，并记录“currentColor”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 9.1.5 支持却鸡肋的系统颜色

**9.1.5 支持却鸡肋的系统颜色** 要解决的是：分离前景色继承、背景分层、图像位置、重复、附着和资源失败状态。先预测元素在“解析颜色值”前后会生成哪些盒、读取哪个包含块，并记录“background-color”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 9.2 CSS世界的background很单调

**9.2 CSS世界的background很单调** 要解决的是：分离前景色继承、背景分层、图像位置、重复、附着和资源失败状态。先预测元素在“传播 currentColor”前后会生成哪些盒、读取哪个包含块，并记录“background-position”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 9.2.1 隐藏元素的background-image到底加不加载

**9.2.1 隐藏元素的background-image到底加不加载** 要解决的是：分离前景色继承、背景分层、图像位置、重复、附着和资源失败状态。先预测元素在“铺设背景色”前后会生成哪些盒、读取哪个包含块，并记录“background-repeat”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 9.2.2 与众不同的background-position百分比计算方式

**9.2.2 与众不同的background-position百分比计算方式** 要解决的是：确认脱流程度、实际包含块、四向偏移、流体尺寸与定位约束。先预测元素在“定位背景图”前后会生成哪些盒、读取哪个包含块，并记录“背景附着”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 9.2.3 background-repeat与渲染性能

**9.2.3 background-repeat与渲染性能** 要解决的是：分离前景色继承、背景分层、图像位置、重复、附着和资源失败状态。先预测元素在“应用重复与附着”前后会生成哪些盒、读取哪个包含块，并记录“currentColor”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 9.2.4 外强中干的background-attachment:fixed

**9.2.4 外强中干的background-attachment:fixed** 要解决的是：确认脱流程度、实际包含块、四向偏移、流体尺寸与定位约束。先预测元素在“验证隐藏和兼容状态”前后会生成哪些盒、读取哪个包含块，并记录“background-color”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 9.2.5 background-color背景色永远是最低的

**9.2.5 background-color背景色永远是最低的** 要解决的是：分离前景色继承、背景分层、图像位置、重复、附着和资源失败状态。先预测元素在“解析颜色值”前后会生成哪些盒、读取哪个包含块，并记录“background-position”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 9.2.6 利用多背景的属性hack小技巧

**9.2.6 利用多背景的属性hack小技巧** 要解决的是：分离前景色继承、背景分层、图像位置、重复、附着和资源失败状态。先预测元素在“传播 currentColor”前后会生成哪些盒、读取哪个包含块，并记录“background-repeat”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 9.2.7 渐变背景和rgba背景色的兼容处理

**9.2.7 渐变背景和rgba背景色的兼容处理** 要解决的是：分离前景色继承、背景分层、图像位置、重复、附着和资源失败状态。先预测元素在“铺设背景色”前后会生成哪些盒、读取哪个包含块，并记录“背景附着”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

## 分步可视实验

<Stepper>
  <Step title="1. 解析颜色值与传播 currentColor">
    先预测盒树和默认流，再运行正常样本保存基线。
    <Csw09DecorationMapLab />
  </Step>
  <Step title="2. 铺设背景色与定位背景图">
    改变容器、内容或单一声明，在首个无法满足契约的位置停止。
    <Csw09DecorationExperimentLab />
  </Step>
  <Step title="3. 应用重复与附着与验证隐藏和兼容状态">
    清除覆盖并重放同一 DOM，核对尺寸、行框、层级与绘制全部收敛。
    <Csw09DecorationEvidenceLab />
  </Step>
</Stepper>

## 最小可运行实验

```css
.notice {
  color: #0f766e;
  border: 1px solid currentColor;
  background-color: #f0fdfa;
  background-image: linear-gradient(
    90deg,
    currentColor 0 0.25rem,
    transparent 0.25rem
  );
  background-repeat: no-repeat;
}
```

```yaml
book: CSS 世界
page: csw-09-decoration
public_toc_entries: 15
sample: normal | boundary | failure | recovery
first_divergence: null
box_tree_explained: true
used_values_explained: true
fluid_contract_preserved: true
```

```text
freeze DOM + stylesheet + viewport + fonts
predict generated boxes + containing blocks
change exactly one variable
stop at the first divergent used value
restore and replay the same DOM
assert fluid layout across content and directions
```

## 常见误区与故障注入

<Callout type="trap">
  **误区一：记住“第 9 章
  元素的装饰与美化”的属性结论，却不展开从“解析颜色值”到“验证隐藏和兼容状态”的过程。**
  相同截图可能依赖固定高度、偶然字体、错误包含块或过大的
  z-index，必须保存盒树与使用值证据。
</Callout>

<Callout type="trap">
  **误区二：把 Flex、Grid、transform 和 animation 塞进《CSS 世界》。**
  作者明确把本书定位为 CSS 2.1 体系；这些 CSS3 及后续能力属于《CSS
  新世界》，目录忠实度和现代替代方案应分开记录。
</Callout>

## 四类样本与验收

| 样本 | 注入方式                             | 必查证据                       | 通过条件               |
| ---- | ------------------------------------ | ------------------------------ | ---------------------- |
| 正常 | 标准字体、常规内容和宽视口           | 盒、包含块、使用值、行框、层级 | 六阶段按预期完成       |
| 边界 | 窄视口、长内容、替换元素、字体缺失   | min/max、溢出、基线和回退      | 不依赖固定像素补丁     |
| 失败 | 单一脱流、错误层叠、隐藏焦点或错方向 | 首个偏离点                     | 能定位并解释根因       |
| 恢复 | 删除故障声明后重放同一 DOM           | 盒树和计算样式                 | 无旧覆盖且流体契约恢复 |

<Csw09DecorationEvidenceLab />

本页签发不变量是：**color 提供可继承的前景色并通过 currentColor 联动边框，background 在边框盒内分层绘制颜色与图像；位置百分比、重复、固定附着和资源加载都有独立计算规则。** 单次截图不能替代盒生成、格式化上下文、包含块、计算值、使用值与绘制顺序的完整轨迹。

## 练习

<Exercises>

**问题 1** 为什么不能只以最终截图正确作为第 9 章 元素的装饰与美化的验收？

<Answer>
  截图会隐藏固定尺寸、错误包含块、字体偶然命中、溢出裁剪和层叠补丁。必须改变视口、内容、字体和流向，比较盒树、使用值与首个偏离点。
</Answer>

**问题 2** 如何把本页出版社目录变成可执行学习证据？

<Answer>
  按公开顺序登记“第9章 元素的装饰与美化、9.1 CSS世界的color很单调、9.1.1
  少得可怜的颜色关键字、9.1.2 不支持的transparent关键字、9.1.3
  不支持的currentColor变量、9.1.4 不支持的rgba颜色和hsla颜色、9.1.5
  支持却鸡肋的系统颜色、9.2 CSS世界的background很单调、9.2.1
  隐藏元素的background-image到底加不加载、9.2.2
  与众不同的background-position百分比计算方式、9.2.3
  background-repeat与渲染性能、9.2.4
  外强中干的background-attachment:fixed、9.2.5
  background-color背景色永远是最低的、9.2.6 利用多背景的属性hack小技巧、9.2.7
  渐变背景和rgba背景色的兼容处理”，为每项写出生成盒、包含块、格式化上下文、计算值、使用值、失败和恢复条件，再用同一
  DOM 重放正常与反例。
</Answer>

**问题 3** 本页的最小通过条件是什么？

<Answer>
  装饰不能改变内容可读性和盒尺寸契约；资源失败、透明颜色、系统主题和高对比模式下，仍要保留可辨认边界与文字。
  同时，正常、边界、失败和恢复轨迹都能复现，删除故障声明后不存在旧覆盖、旧层叠或隐藏溢出。
</Answer>

</Exercises>

## 小结

- **currentColor**：对应“解析颜色值”阶段的核心观察量。
- **background-color**：对应“传播 currentColor”阶段的核心观察量。
- **background-position**：对应“铺设背景色”阶段的核心观察量。
- **background-repeat**：对应“定位背景图”阶段的核心观察量。
- **背景附着**：对应“应用重复与附着”阶段的核心观察量。
- 15 个公开目录条目已经全部映射到本页证据链。
- 盒树、使用值和边界样本共同决定是否通过，最终截图不是唯一证据。

## 术语表

<Glossary>
  <GlossaryItem term="currentColor">
    currentColor 是本页第 1
    个核心观察量，位于“解析颜色值”阶段。验收时同时记录包含块、格式化上下文、计算值、使用值与绘制结果。
  </GlossaryItem>
  <GlossaryItem term="background-color">
    background-color 是本页第 2 个核心观察量，位于“传播
    currentColor”阶段。验收时同时记录包含块、格式化上下文、计算值、使用值与绘制结果。
  </GlossaryItem>
  <GlossaryItem term="background-position">
    background-position 是本页第 3
    个核心观察量，位于“铺设背景色”阶段。验收时同时记录包含块、格式化上下文、计算值、使用值与绘制结果。
  </GlossaryItem>
  <GlossaryItem term="background-repeat">
    background-repeat 是本页第 4
    个核心观察量，位于“定位背景图”阶段。验收时同时记录包含块、格式化上下文、计算值、使用值与绘制结果。
  </GlossaryItem>
  <GlossaryItem term="背景附着">
    背景附着 是本页第 5
    个核心观察量，位于“应用重复与附着”阶段。验收时同时记录包含块、格式化上下文、计算值、使用值与绘制结果。
  </GlossaryItem>
</Glossary>

## 版本与改编边界

本单元以张鑫旭著《CSS 世界》为身份基线。详细目录来自人民邮电出版社出版电子版页面，CSS 2.1 定位由作者官网和图书官网交叉确认；正文是独立教学重构，不复制原书段落。现代浏览器兼容状态需另行按当前 CSS 规范和浏览器文档核验。

<Attribution
  adaptedFrom="张鑫旭著，《CSS 世界》，人民邮电出版社，2017 年 12 月，328 页，ISBN 9787115470669。本页按出版社公开目录与作者官网独立教学重构。"
  adaptedUrl="https://www.dedao.cn/ebook/detail?id=V5R16yPmaYOMqGRAv82jkX4KDe175w771mwrbx6pNgznl9VZPLJQyEBodb89mqoO"
/>
