---
title: "第 11 章 用户界面样式"
type: A
section: "CSS 世界 · 界面与流向"
order: 11
description: "outline 在边框之外绘制且不占布局空间，是键盘焦点的重要反馈；cursor 只表达可用交互意图，不能替代语义元素、真实状态和可访问名称。"
demo: true
math: false
sourceUrl: "https://www.dedao.cn/ebook/detail?id=V5R16yPmaYOMqGRAv82jkX4KDe175w771mwrbx6pNgznl9VZPLJQyEBodb89mqoO"
draft: true
---

import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第 11 章 用户界面样式的核心主张，并把它放回原书 12 章 CSS 2.1 体系
- 能按出版社目录逐项描述 7 个标题的盒、包含块、计算值和使用值
- 能实现本页最小样例，定位流、尺寸、内联、定位、层叠或绘制的首个偏离点
- 能比较正常、边界、失败和恢复四类样本，判断布局是否仍保持流体契约

</Objectives>

## 直觉起点

<Term def="outline 是“第 11 章 用户界面样式”的第 1 个核心观察量，位于“保留原生焦点”阶段；必须同时说明指定值、计算值、使用值、实际盒与失败边界。">outline</Term>、<Term def="焦点可见 是“第 11 章 用户界面样式”的第 2 个核心观察量，位于“绘制 outline”阶段；必须同时说明指定值、计算值、使用值、实际盒与失败边界。">焦点可见</Term>、<Term def="outline-offset 是“第 11 章 用户界面样式”的第 3 个核心观察量，位于“处理偏移与圆角”阶段；必须同时说明指定值、计算值、使用值、实际盒与失败边界。">outline-offset</Term>、<Term def="cursor 是“第 11 章 用户界面样式”的第 4 个核心观察量，位于“选择系统 cursor”阶段；必须同时说明指定值、计算值、使用值、实际盒与失败边界。">cursor</Term>、<Term def="指针回退 是“第 11 章 用户界面样式”的第 5 个核心观察量，位于“提供自定义回退”阶段；必须同时说明指定值、计算值、使用值、实际盒与失败边界。">指针回退</Term>
共同构成本页的观察坐标。outline
在边框之外绘制且不占布局空间，是键盘焦点的重要反馈；cursor
只表达可用交互意图，不能替代语义元素、真实状态和可访问名称。 CSS
的困难不在属性名称，而在多个声明经过级联、继承和格式化后才产生最终使用值；同一截图可能由完全不同的盒树、固定尺寸或偶然层叠得到，必须向前追踪原因。

## 六阶段机制链

### 1. 保留原生焦点

固定 DOM、样式表、视口、字体和内容，先预测生成盒、包含块、格式化上下文、计算值、使用尺寸、行框和层叠位置。一旦轨迹不同，保存首个偏离点，不用最终像素掩盖过程。

### 2. 绘制 outline

只改变一个声明或环境变量，记录生成盒、包含块、格式化上下文、计算值、使用尺寸、行框和层叠位置。一旦轨迹不同，保存首个偏离点，不用最终像素掩盖过程。

### 3. 处理偏移与圆角

只改变一个声明或环境变量，记录生成盒、包含块、格式化上下文、计算值、使用尺寸、行框和层叠位置。一旦轨迹不同，保存首个偏离点，不用最终像素掩盖过程。

### 4. 选择系统 cursor

只改变一个声明或环境变量，记录生成盒、包含块、格式化上下文、计算值、使用尺寸、行框和层叠位置。一旦轨迹不同，保存首个偏离点，不用最终像素掩盖过程。

### 5. 提供自定义回退

只改变一个声明或环境变量，记录生成盒、包含块、格式化上下文、计算值、使用尺寸、行框和层叠位置。一旦轨迹不同，保存首个偏离点，不用最终像素掩盖过程。

### 6. 核对键盘与指针

只改变一个声明或环境变量，记录生成盒、包含块、格式化上下文、计算值、使用尺寸、行框和层叠位置。恢复后重放同一输入，并确认窄宽、长内容和方向变化不会暴露旧补丁。

## 核心机制深挖

outline 不参与盒模型，因此不会像 border 一样改变尺寸；它可以跨越碎片绘制，最适合焦点反馈。cursor URL 必须提供通用关键字回退，而且指针图形不等于控件真的可用。 任何移除 outline 的规则都必须提供对比度和几何位置同等清晰的 focus-visible 替代；鼠标、键盘和触摸路径应表达同一状态。 实验时把浏览器开发者工具中的盒模型、计算样式和层叠上下文当成观测工具，而不是答案来源；答案必须能从规范规则和当前 DOM 结构推导出来。先删除非必要覆盖，再逐条加入约束，这样才能分辨默认流在做什么、哪条声明真正改变了结果。

## 边界记录模板

每次实验建立六列证据：**DOM 与内容、生成盒与格式化上下文、包含块、计算值与使用值、首个偏离点、恢复结果**。尺寸页增加视口与固有尺寸，内联页增加字体度量和基线，定位页增加脱流空间与四向偏移，层叠页增加上下文树，文本页增加字体回退，界面页增加键盘与书写方向。若最终外观相同但固定宽高、隐藏溢出、不可见焦点或错误层叠仍存在，依然失败。

## 出版社目录逐项讲解

### 第11章 用户界面样式

**第11章 用户界面样式** 要解决的是：把该目录项放回正常流、盒生成、尺寸计算和格式化上下文，记录输入、约束与浏览器使用值。先预测元素在“保留原生焦点”前后会生成哪些盒、读取哪个包含块，并记录“outline”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 11.1 和border形似的outline属性

**11.1 和border形似的outline属性** 要解决的是：分析边框宽度、样式、颜色、透明技巧及其对可见尺寸和图形构建的影响。先预测元素在“绘制 outline”前后会生成哪些盒、读取哪个包含块，并记录“焦点可见”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 11.1.1 万万不可在全局设置outline:0 none

**11.1.1 万万不可在全局设置outline:0 none** 要解决的是：保证焦点反馈不占空间且清晰可见，并为指针样式保留语义和回退。先预测元素在“处理偏移与圆角”前后会生成哪些盒、读取哪个包含块，并记录“outline-offset”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 11.1.2 真正的不占据空间的outline及其应用

**11.1.2 真正的不占据空间的outline及其应用** 要解决的是：保证焦点反馈不占空间且清晰可见，并为指针样式保留语义和回退。先预测元素在“选择系统 cursor”前后会生成哪些盒、读取哪个包含块，并记录“cursor”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 11.2 光标属性cursor

**11.2 光标属性cursor** 要解决的是：保证焦点反馈不占空间且清晰可见，并为指针样式保留语义和回退。先预测元素在“提供自定义回退”前后会生成哪些盒、读取哪个包含块，并记录“指针回退”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 11.2.1 琳琅满目的cursor属性值

**11.2.1 琳琅满目的cursor属性值** 要解决的是：保证焦点反馈不占空间且清晰可见，并为指针样式保留语义和回退。先预测元素在“核对键盘与指针”前后会生成哪些盒、读取哪个包含块，并记录“outline”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 11.2.2 自定义光标

**11.2.2 自定义光标** 要解决的是：保证焦点反馈不占空间且清晰可见，并为指针样式保留语义和回退。先预测元素在“保留原生焦点”前后会生成哪些盒、读取哪个包含块，并记录“焦点可见”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

## 最小可运行实验

```css
.action:focus-visible {
  outline: 3px solid #2563eb;
  outline-offset: 3px;
}
[aria-busy="true"] {
  cursor: progress;
}
[aria-disabled="true"] {
  cursor: not-allowed;
}
```

```yaml
book: CSS 世界
page: csw-11-user-interface
public_toc_entries: 7
sample: normal | boundary | failure | recovery
first_divergence: null
box_tree_explained: true
used_values_explained: true
fluid_contract_preserved: true
```

```text
freeze DOM + stylesheet + viewport + fonts
predict generated boxes + containing blocks
change exactly one variable
stop at the first divergent used value
restore and replay the same DOM
assert fluid layout across content and directions
```

## 常见误区与故障注入

<Callout type="trap">
  **误区一：记住“第 11 章
  用户界面样式”的属性结论，却不展开从“保留原生焦点”到“核对键盘与指针”的过程。**
  相同截图可能依赖固定高度、偶然字体、错误包含块或过大的
  z-index，必须保存盒树与使用值证据。
</Callout>

<Callout type="trap">
  **误区二：把 Flex、Grid、transform 和 animation 塞进《CSS 世界》。**
  作者明确把本书定位为 CSS 2.1 体系；这些 CSS3 及后续能力属于《CSS
  新世界》，目录忠实度和现代替代方案应分开记录。
</Callout>

## 四类样本与验收

| 样本 | 注入方式                             | 必查证据                       | 通过条件               |
| ---- | ------------------------------------ | ------------------------------ | ---------------------- |
| 正常 | 标准字体、常规内容和宽视口           | 盒、包含块、使用值、行框、层级 | 六阶段按预期完成       |
| 边界 | 窄视口、长内容、替换元素、字体缺失   | min/max、溢出、基线和回退      | 不依赖固定像素补丁     |
| 失败 | 单一脱流、错误层叠、隐藏焦点或错方向 | 首个偏离点                     | 能定位并解释根因       |
| 恢复 | 删除故障声明后重放同一 DOM           | 盒树和计算样式                 | 无旧覆盖且流体契约恢复 |

本页签发不变量是：**outline 在边框之外绘制且不占布局空间，是键盘焦点的重要反馈；cursor 只表达可用交互意图，不能替代语义元素、真实状态和可访问名称。** 单次截图不能替代盒生成、格式化上下文、包含块、计算值、使用值与绘制顺序的完整轨迹。

## 练习

<Exercises>

**问题 1** 为什么不能只以最终截图正确作为第 11 章 用户界面样式的验收？

<Answer>
  截图会隐藏固定尺寸、错误包含块、字体偶然命中、溢出裁剪和层叠补丁。必须改变视口、内容、字体和流向，比较盒树、使用值与首个偏离点。
</Answer>

**问题 2** 如何把本页出版社目录变成可执行学习证据？

<Answer>
  按公开顺序登记“第11章 用户界面样式、11.1 和border形似的outline属性、11.1.1
  万万不可在全局设置outline:0 none、11.1.2
  真正的不占据空间的outline及其应用、11.2 光标属性cursor、11.2.1
  琳琅满目的cursor属性值、11.2.2
  自定义光标”，为每项写出生成盒、包含块、格式化上下文、计算值、使用值、失败和恢复条件，再用同一
  DOM 重放正常与反例。
</Answer>

**问题 3** 本页的最小通过条件是什么？

<Answer>
  任何移除 outline 的规则都必须提供对比度和几何位置同等清晰的 focus-visible
  替代；鼠标、键盘和触摸路径应表达同一状态。
  同时，正常、边界、失败和恢复轨迹都能复现，删除故障声明后不存在旧覆盖、旧层叠或隐藏溢出。
</Answer>

</Exercises>

## 小结

- **outline**：对应“保留原生焦点”阶段的核心观察量。
- **焦点可见**：对应“绘制 outline”阶段的核心观察量。
- **outline-offset**：对应“处理偏移与圆角”阶段的核心观察量。
- **cursor**：对应“选择系统 cursor”阶段的核心观察量。
- **指针回退**：对应“提供自定义回退”阶段的核心观察量。
- 7 个公开目录条目已经全部映射到本页证据链。
- 盒树、使用值和边界样本共同决定是否通过，最终截图不是唯一证据。

## 术语表

<Glossary>
  <GlossaryItem term="outline">
    outline 是本页第 1
    个核心观察量，位于“保留原生焦点”阶段。验收时同时记录包含块、格式化上下文、计算值、使用值与绘制结果。
  </GlossaryItem>
  <GlossaryItem term="焦点可见">
    焦点可见 是本页第 2 个核心观察量，位于“绘制
    outline”阶段。验收时同时记录包含块、格式化上下文、计算值、使用值与绘制结果。
  </GlossaryItem>
  <GlossaryItem term="outline-offset">
    outline-offset 是本页第 3
    个核心观察量，位于“处理偏移与圆角”阶段。验收时同时记录包含块、格式化上下文、计算值、使用值与绘制结果。
  </GlossaryItem>
  <GlossaryItem term="cursor">
    cursor 是本页第 4 个核心观察量，位于“选择系统
    cursor”阶段。验收时同时记录包含块、格式化上下文、计算值、使用值与绘制结果。
  </GlossaryItem>
  <GlossaryItem term="指针回退">
    指针回退 是本页第 5
    个核心观察量，位于“提供自定义回退”阶段。验收时同时记录包含块、格式化上下文、计算值、使用值与绘制结果。
  </GlossaryItem>
</Glossary>

## 版本与改编边界

本单元以张鑫旭著《CSS 世界》为身份基线。详细目录来自人民邮电出版社出版电子版页面，CSS 2.1 定位由作者官网和图书官网交叉确认；正文是独立教学重构，不复制原书段落。现代浏览器兼容状态需另行按当前 CSS 规范和浏览器文档核验。

<Attribution
  adaptedFrom="张鑫旭著，《CSS 世界》，人民邮电出版社，2017 年 12 月，328 页，ISBN 9787115470669。本页按出版社公开目录与作者官网独立教学重构。"
  adaptedUrl="https://www.dedao.cn/ebook/detail?id=V5R16yPmaYOMqGRAv82jkX4KDe175w771mwrbx6pNgznl9VZPLJQyEBodb89mqoO"
/>
