---
title: "第 12 章 流向的改变"
type: A
section: "CSS 世界 · 界面与流向"
order: 12
description: "direction 与 unicode-bidi 控制双向文本嵌入，writing-mode 改变块流和内联流方向；尺寸、边距、对齐与滚动应优先使用逻辑轴推理，而不是把横排物理方向硬编码。"
demo: true
math: false
sourceUrl: "https://www.dedao.cn/ebook/detail?id=V5R16yPmaYOMqGRAv82jkX4KDe175w771mwrbx6pNgznl9VZPLJQyEBodb89mqoO"
draft: true
---

import {
  Csw12WritingDirectionsMapLab,
  Csw12WritingDirectionsExperimentLab,
  Csw12WritingDirectionsEvidenceLab,
} from "@/components/mdx/css-world/diagrams/csw-12-writing-directions";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第 12 章 流向的改变的核心主张，并把它放回原书 12 章 CSS 2.1 体系
- 能按出版社目录逐项描述 8 个标题的盒、包含块、计算值和使用值
- 能实现本页最小样例，定位流、尺寸、内联、定位、层叠或绘制的首个偏离点
- 能比较正常、边界、失败和恢复四类样本，判断布局是否仍保持流体契约

</Objectives>

## 直觉起点

<Term def="direction 是“第 12 章 流向的改变”的第 1 个核心观察量，位于“确定文字书写方向”阶段；必须同时说明指定值、计算值、使用值、实际盒与失败边界。">direction</Term>、<Term def="unicode-bidi 是“第 12 章 流向的改变”的第 2 个核心观察量，位于“应用双向算法”阶段；必须同时说明指定值、计算值、使用值、实际盒与失败边界。">unicode-bidi</Term>、<Term def="writing-mode 是“第 12 章 流向的改变”的第 3 个核心观察量，位于“设置块流方向”阶段；必须同时说明指定值、计算值、使用值、实际盒与失败边界。">writing-mode</Term>、<Term def="内联轴 是“第 12 章 流向的改变”的第 4 个核心观察量，位于“重映射内联与块轴”阶段；必须同时说明指定值、计算值、使用值、实际盒与失败边界。">内联轴</Term>、<Term def="块轴 是“第 12 章 流向的改变”的第 5 个核心观察量，位于“改写逻辑尺寸”阶段；必须同时说明指定值、计算值、使用值、实际盒与失败边界。">块轴</Term>
共同构成本页的观察坐标。direction 与 unicode-bidi 控制双向文本嵌入，writing-mode
改变块流和内联流方向；尺寸、边距、对齐与滚动应优先使用逻辑轴推理，而不是把横排物理方向硬编码。
CSS
的困难不在属性名称，而在多个声明经过级联、继承和格式化后才产生最终使用值；同一截图可能由完全不同的盒树、固定尺寸或偶然层叠得到，必须向前追踪原因。

<Csw12WritingDirectionsMapLab />

## 六阶段机制链

### 1. 确定文字书写方向

固定 DOM、样式表、视口、字体和内容，先预测生成盒、包含块、格式化上下文、计算值、使用尺寸、行框和层叠位置。一旦轨迹不同，保存首个偏离点，不用最终像素掩盖过程。

### 2. 应用双向算法

只改变一个声明或环境变量，记录生成盒、包含块、格式化上下文、计算值、使用尺寸、行框和层叠位置。一旦轨迹不同，保存首个偏离点，不用最终像素掩盖过程。

### 3. 设置块流方向

只改变一个声明或环境变量，记录生成盒、包含块、格式化上下文、计算值、使用尺寸、行框和层叠位置。一旦轨迹不同，保存首个偏离点，不用最终像素掩盖过程。

### 4. 重映射内联与块轴

只改变一个声明或环境变量，记录生成盒、包含块、格式化上下文、计算值、使用尺寸、行框和层叠位置。一旦轨迹不同，保存首个偏离点，不用最终像素掩盖过程。

### 5. 改写逻辑尺寸

只改变一个声明或环境变量，记录生成盒、包含块、格式化上下文、计算值、使用尺寸、行框和层叠位置。一旦轨迹不同，保存首个偏离点，不用最终像素掩盖过程。

### 6. 验证混合语言

只改变一个声明或环境变量，记录生成盒、包含块、格式化上下文、计算值、使用尺寸、行框和层叠位置。恢复后重放同一输入，并确认窄宽、长内容和方向变化不会暴露旧补丁。

## 核心机制深挖

writing-mode 不只是旋转文字，它交换块轴和内联轴，使 width、height、margin 与定位的物理含义变得脆弱。direction 还参与表格列、滚动起点和双向文本顺序。 至少用横排中英文、RTL 文本和竖排样本验收；若切换书写模式后需要大量 left、right、top、bottom 覆盖，应改用逻辑属性重建。 实验时把浏览器开发者工具中的盒模型、计算样式和层叠上下文当成观测工具，而不是答案来源；答案必须能从规范规则和当前 DOM 结构推导出来。先删除非必要覆盖，再逐条加入约束，这样才能分辨默认流在做什么、哪条声明真正改变了结果。

## 边界记录模板

每次实验建立六列证据：**DOM 与内容、生成盒与格式化上下文、包含块、计算值与使用值、首个偏离点、恢复结果**。尺寸页增加视口与固有尺寸，内联页增加字体度量和基线，定位页增加脱流空间与四向偏移，层叠页增加上下文树，文本页增加字体回退，界面页增加键盘与书写方向。若最终外观相同但固定宽高、隐藏溢出、不可见焦点或错误层叠仍存在，依然失败。

## 出版社目录逐项讲解

### 第12章 流向的改变

**第12章 流向的改变** 要解决的是：按双向算法、书写模式、内联轴和块轴重算尺寸、顺序与对齐。先预测元素在“确定文字书写方向”前后会生成哪些盒、读取哪个包含块，并记录“direction”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 12.1 改变水平流向的direction

**12.1 改变水平流向的direction** 要解决的是：按双向算法、书写模式、内联轴和块轴重算尺寸、顺序与对齐。先预测元素在“应用双向算法”前后会生成哪些盒、读取哪个包含块，并记录“unicode-bidi”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 12.1.1 direction简介

**12.1.1 direction简介** 要解决的是：按双向算法、书写模式、内联轴和块轴重算尺寸、顺序与对齐。先预测元素在“设置块流方向”前后会生成哪些盒、读取哪个包含块，并记录“writing-mode”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 12.1.2 direction的黄金搭档unicode-bidi

**12.1.2 direction的黄金搭档unicode-bidi** 要解决的是：按双向算法、书写模式、内联轴和块轴重算尺寸、顺序与对齐。先预测元素在“重映射内联与块轴”前后会生成哪些盒、读取哪个包含块，并记录“内联轴”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 12.2 改变CSS世界纵横规则的writing-mode

**12.2 改变CSS世界纵横规则的writing-mode** 要解决的是：按双向算法、书写模式、内联轴和块轴重算尺寸、顺序与对齐。先预测元素在“改写逻辑尺寸”前后会生成哪些盒、读取哪个包含块，并记录“块轴”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 12.2.1 writing-mode原本的作用

**12.2.1 writing-mode原本的作用** 要解决的是：按双向算法、书写模式、内联轴和块轴重算尺寸、顺序与对齐。先预测元素在“验证混合语言”前后会生成哪些盒、读取哪个包含块，并记录“direction”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 12.2.2 writing-mode不经意改变了哪些规则

**12.2.2 writing-mode不经意改变了哪些规则** 要解决的是：按双向算法、书写模式、内联轴和块轴重算尺寸、顺序与对齐。先预测元素在“确定文字书写方向”前后会生成哪些盒、读取哪个包含块，并记录“unicode-bidi”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

### 12.2.3 writing-mode和direction的关系

**12.2.3 writing-mode和direction的关系** 要解决的是：按双向算法、书写模式、内联轴和块轴重算尺寸、顺序与对齐。先预测元素在“应用双向算法”前后会生成哪些盒、读取哪个包含块，并记录“writing-mode”的计算值与使用值。正常样本只保留最小声明；边界样本改变容器宽度、内容长度、字体或书写方向；失败样本只引入一个脱流、溢出或层叠条件。清除故障后重放同一 DOM，只有盒树、尺寸、行框、层级和绘制结果都可解释才通过。

## 分步可视实验

<Stepper>
  <Step title="1. 确定文字书写方向与应用双向算法">
    先预测盒树和默认流，再运行正常样本保存基线。
    <Csw12WritingDirectionsMapLab />
  </Step>
  <Step title="2. 设置块流方向与重映射内联与块轴">
    改变容器、内容或单一声明，在首个无法满足契约的位置停止。
    <Csw12WritingDirectionsExperimentLab />
  </Step>
  <Step title="3. 改写逻辑尺寸与验证混合语言">
    清除覆盖并重放同一 DOM，核对尺寸、行框、层级与绘制全部收敛。
    <Csw12WritingDirectionsEvidenceLab />
  </Step>
</Stepper>

## 最小可运行实验

```css
.article {
  writing-mode: vertical-rl;
  direction: ltr;
  inline-size: 24rem;
  max-block-size: 40rem;
  padding-inline: 1rem;
  border-block-start: 1px solid currentColor;
}
```

```yaml
book: CSS 世界
page: csw-12-writing-directions
public_toc_entries: 8
sample: normal | boundary | failure | recovery
first_divergence: null
box_tree_explained: true
used_values_explained: true
fluid_contract_preserved: true
```

```text
freeze DOM + stylesheet + viewport + fonts
predict generated boxes + containing blocks
change exactly one variable
stop at the first divergent used value
restore and replay the same DOM
assert fluid layout across content and directions
```

## 常见误区与故障注入

<Callout type="trap">
  **误区一：记住“第 12 章
  流向的改变”的属性结论，却不展开从“确定文字书写方向”到“验证混合语言”的过程。**
  相同截图可能依赖固定高度、偶然字体、错误包含块或过大的
  z-index，必须保存盒树与使用值证据。
</Callout>

<Callout type="trap">
  **误区二：把 Flex、Grid、transform 和 animation 塞进《CSS 世界》。**
  作者明确把本书定位为 CSS 2.1 体系；这些 CSS3 及后续能力属于《CSS
  新世界》，目录忠实度和现代替代方案应分开记录。
</Callout>

## 四类样本与验收

| 样本 | 注入方式                             | 必查证据                       | 通过条件               |
| ---- | ------------------------------------ | ------------------------------ | ---------------------- |
| 正常 | 标准字体、常规内容和宽视口           | 盒、包含块、使用值、行框、层级 | 六阶段按预期完成       |
| 边界 | 窄视口、长内容、替换元素、字体缺失   | min/max、溢出、基线和回退      | 不依赖固定像素补丁     |
| 失败 | 单一脱流、错误层叠、隐藏焦点或错方向 | 首个偏离点                     | 能定位并解释根因       |
| 恢复 | 删除故障声明后重放同一 DOM           | 盒树和计算样式                 | 无旧覆盖且流体契约恢复 |

<Csw12WritingDirectionsEvidenceLab />

本页签发不变量是：**direction 与 unicode-bidi 控制双向文本嵌入，writing-mode 改变块流和内联流方向；尺寸、边距、对齐与滚动应优先使用逻辑轴推理，而不是把横排物理方向硬编码。** 单次截图不能替代盒生成、格式化上下文、包含块、计算值、使用值与绘制顺序的完整轨迹。

## 练习

<Exercises>

**问题 1** 为什么不能只以最终截图正确作为第 12 章 流向的改变的验收？

<Answer>
  截图会隐藏固定尺寸、错误包含块、字体偶然命中、溢出裁剪和层叠补丁。必须改变视口、内容、字体和流向，比较盒树、使用值与首个偏离点。
</Answer>

**问题 2** 如何把本页出版社目录变成可执行学习证据？

<Answer>
  按公开顺序登记“第12章 流向的改变、12.1 改变水平流向的direction、12.1.1
  direction简介、12.1.2 direction的黄金搭档unicode-bidi、12.2
  改变CSS世界纵横规则的writing-mode、12.2.1 writing-mode原本的作用、12.2.2
  writing-mode不经意改变了哪些规则、12.2.3
  writing-mode和direction的关系”，为每项写出生成盒、包含块、格式化上下文、计算值、使用值、失败和恢复条件，再用同一
  DOM 重放正常与反例。
</Answer>

**问题 3** 本页的最小通过条件是什么？

<Answer>
  至少用横排中英文、RTL 文本和竖排样本验收；若切换书写模式后需要大量
  left、right、top、bottom 覆盖，应改用逻辑属性重建。
  同时，正常、边界、失败和恢复轨迹都能复现，删除故障声明后不存在旧覆盖、旧层叠或隐藏溢出。
</Answer>

</Exercises>

## 小结

- **direction**：对应“确定文字书写方向”阶段的核心观察量。
- **unicode-bidi**：对应“应用双向算法”阶段的核心观察量。
- **writing-mode**：对应“设置块流方向”阶段的核心观察量。
- **内联轴**：对应“重映射内联与块轴”阶段的核心观察量。
- **块轴**：对应“改写逻辑尺寸”阶段的核心观察量。
- 8 个公开目录条目已经全部映射到本页证据链。
- 盒树、使用值和边界样本共同决定是否通过，最终截图不是唯一证据。

## 术语表

<Glossary>
  <GlossaryItem term="direction">
    direction 是本页第 1
    个核心观察量，位于“确定文字书写方向”阶段。验收时同时记录包含块、格式化上下文、计算值、使用值与绘制结果。
  </GlossaryItem>
  <GlossaryItem term="unicode-bidi">
    unicode-bidi 是本页第 2
    个核心观察量，位于“应用双向算法”阶段。验收时同时记录包含块、格式化上下文、计算值、使用值与绘制结果。
  </GlossaryItem>
  <GlossaryItem term="writing-mode">
    writing-mode 是本页第 3
    个核心观察量，位于“设置块流方向”阶段。验收时同时记录包含块、格式化上下文、计算值、使用值与绘制结果。
  </GlossaryItem>
  <GlossaryItem term="内联轴">
    内联轴 是本页第 4
    个核心观察量，位于“重映射内联与块轴”阶段。验收时同时记录包含块、格式化上下文、计算值、使用值与绘制结果。
  </GlossaryItem>
  <GlossaryItem term="块轴">
    块轴 是本页第 5
    个核心观察量，位于“改写逻辑尺寸”阶段。验收时同时记录包含块、格式化上下文、计算值、使用值与绘制结果。
  </GlossaryItem>
</Glossary>

## 版本与改编边界

本单元以张鑫旭著《CSS 世界》为身份基线。详细目录来自人民邮电出版社出版电子版页面，CSS 2.1 定位由作者官网和图书官网交叉确认；正文是独立教学重构，不复制原书段落。现代浏览器兼容状态需另行按当前 CSS 规范和浏览器文档核验。

<Attribution
  adaptedFrom="张鑫旭著，《CSS 世界》，人民邮电出版社，2017 年 12 月，328 页，ISBN 9787115470669。本页按出版社公开目录与作者官网独立教学重构。"
  adaptedUrl="https://www.dedao.cn/ebook/detail?id=V5R16yPmaYOMqGRAv82jkX4KDe175w771mwrbx6pNgznl9VZPLJQyEBodb89mqoO"
/>
