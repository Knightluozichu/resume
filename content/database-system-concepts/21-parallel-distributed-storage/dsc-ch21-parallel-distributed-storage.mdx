---
title: "第21章 并行与分布式存储"
description: "依据原书第7版完整目录覆盖8个节点：用分区、复制、并行索引和分布式文件组织大规模数据"
type: C
section: "数据库系统概念（原书第7版 / 2021）"
draft: false
math: false
sourceUrl: "https://www.db-book.com/"
demo: true
order: 0
qualityVersion: 2
practiceMode: simulation
sourceMode: independent-rewrite
---

import {
  DscCh21ParallelDistributedStorageArchitectureLab,
  DscCh21ParallelDistributedStorageExperimentLab,
  DscCh21ParallelDistributedStorageEvidenceLab,
} from "@/components/mdx/database-system-concepts/diagrams/dsc-ch21-parallel-distributed-storage";

# 第21章 并行与分布式存储

本课程对应Abraham Silberschatz、Henry F. Korth、S. 在“第21章 并行与分布式存储”的证据链中，Sudarshan著，杨冬青、李红燕、张金波等译《数据库系统概念（原书第7版）》，机械工业出版社2021年6月出版，820页，ISBN 9787111681816。原版第7版由McGraw-Hill于2019年发布。课程按原书11部分、32章与附录A逐项重构，不复制原文。

中文版第7版纸书正文收录第1-23章；第24-26章由中文版提供中文在线内容；第27-32章是原版官网英文在线章节；附录A在纸书中。本页媒介为“纸书正文”。
全课程另设学习地图与总复习，共35页；正式知识分母仍是32章与附录A。本页属于“第八部分 并行与分布式数据库”，依据正式目录覆盖8个节点。

“第21章 并行与分布式存储”没有使用未获授权的中文纸书正文；以 [作者官网第 7 版完整目录](https://www.db-book.com/toc-dir/toc.pdf) 界定 32 章与附录 A，以 [作者公开在线章节](https://www.db-book.com/online-chapters-dir/index.html)、[大学模式实验](https://www.db-book.com/university-lab-dir/exercises-dir/) 和 [McGraw-Hill 第 7 版变更说明](https://www.mheducation.com/unitas/highered/changes/silberschatz-database-system-concepts-7e.pdf) 核对可公开内容。中文解释、SQL、图示、实验与练习均为独立教学重写；实现行为再以 [PostgreSQL 官方文档](https://www.postgresql.org/docs/current/) 等一手规范复核。

<Objectives>

- 能解释“第21章 并行与分布式存储”的8个正式节点，并把它们连接到“用分区、复制、并行索引和分布式文件组织大规模数据”主线。
- 能复现分片函数、倾斜直方图、复制故障实验和再平衡计划，记录输入版本、配置、预期、实际结果与失败边界。
- 能比较至少两种实现或故障条件，证明“每条记录的放置规则确定，复制版本收敛，节点故障与热点下仍满足目标可用性”。
- 能设计反例推翻“只按平均数据量分区，忽略热键、访问局部性和再平衡期间的双写一致性”，并给出修复、回退和独立验收条件。

</Objectives>

## 从一次可观察状态变化开始

先预测：给定一份固定的大学数据库、一个操作和一个故障时刻，哪些逻辑行、物理页、锁、版本或日志记录应该变化，哪些绝不能变化？不要先运行再解释。把预测写成状态表，然后用独立查询、执行统计、页或日志轨迹核对。若预测与观察不一致，先检查模型和实验边界，而不是立即改配置。

本页主问题是“用分区、复制、并行索引和分布式文件组织大规模数据”。交付不以读完为准，而以另一位学习者能从分片函数、倾斜直方图、复制故障实验和再平衡计划复现同一结论为准。最小正确性合同是：**每条记录的放置规则确定，复制版本收敛，节点故障与热点下仍满足目标可用性**。

## 核心词汇与系统边界

<Term def="分区是“用分区、复制、并行索引和分布式文件组织大规模数据”中的第1个核心概念；必须由状态、机制、反例和证据共同定义">分区</Term>、<Term def="数据倾斜是“用分区、复制、并行索引和分布式文件组织大规模数据”中的第2个核心概念；必须由状态、机制、反例和证据共同定义">数据倾斜</Term>、<Term def="复制是“用分区、复制、并行索引和分布式文件组织大规模数据”中的第3个核心概念；必须由状态、机制、反例和证据共同定义">复制</Term>、<Term def="分布式文件系统是“用分区、复制、并行索引和分布式文件组织大规模数据”中的第4个核心概念；必须由状态、机制、反例和证据共同定义">分布式文件系统</Term>、<Term def="键值存储是“用分区、复制、并行索引和分布式文件组织大规模数据”中的第5个核心概念；必须由状态、机制、反例和证据共同定义">键值存储</Term>
构成本页词汇表。每个词都要回答四个问题：它约束什么状态，由哪个模块执行，在哪个故障或并发边界会失效，用什么证据发现失效。只给一句名词解释不构成掌握。

<DscCh21ParallelDistributedStorageArchitectureLab />

## 第七版机制逐项深读

### 21.1 概述

在“第21章 并行与分布式存储”中，“21.1 概述”组织“依据原书第7版完整目录覆盖8个节点：用分区、复制、并行索引和分布式文件组织大规模数据”的语义、结构、执行和证据；若不能给出一个状态变化与独立对账，本节点仍未完成。

### 21.2 数据分区

在“第21章 并行与分布式存储”中，分析“21.2 数据分区”要标出本地读取、重分区、网络交换和聚合边界，区分可交换操作与必须保序的状态。

### 21.3 处理分区倾斜

在“第21章 并行与分布式存储”中，“21.3 处理分区倾斜”把数据切分后并行处理；键分布决定负载，验收要测中位分区与最热分区而非只看总吞吐。

### 21.4 复制

在“第21章 并行与分布式存储”中，“21.4 复制”在多个节点间复制状态或决定；安全性要求不产生冲突决定，活性则依赖可达法定人数与故障假设。

### 21.5 并行索引

在“第21章 并行与分布式存储”中，“21.5 并行索引”的物理算法可以变化，但逻辑语义不可漂移；证据应同时保存手算结果和数据库返回结果。

### 21.6 分布式文件系统

在“第21章 并行与分布式存储”中，验证“21.6 分布式文件系统”固定操作，只改变消息延迟或单节点失效，记录决定、重试、恢复和客户端可见结果。

### 21.7 并行键值存储

在“第21章 并行与分布式存储”中，“21.7 并行键值存储”用最小属性集唯一标识元组，并通过外码把引用限制到被参照键；验证要覆盖重复、NULL、更新与删除四条路径。

### 21.8 小结

在“第21章 并行与分布式存储”中，“21.8 小结”组织“依据原书第7版完整目录覆盖8个节点：用分区、复制、并行索引和分布式文件组织大规模数据”的语义、结构、执行和证据；若不能给出一个状态变化与独立对账，本节点仍未完成。
## 机制推演：从语义到物理证据

范围分区支持区间访问但可能形成热点，哈希分区均匀却破坏范围局部性。复制提高读取与容错能力，同时需要传播和冲突规则；并行索引与文件系统还要把元数据和块故障纳入设计。

推演时始终区分五层。在“第21章 并行与分布式存储”的证据链中，第一层是业务不变量，说明哪些数据库状态合法；第二层是逻辑模型与查询语义，说明结果应该是什么；第三层是物理计划和数据结构，说明系统怎样得到结果；第四层是并发、日志与复制协议，说明交错和故障后仍保留哪些承诺；第五层是观测证据，说明我们怎样知道前四层真的成立。性能优化只能改变第三层和部分第四层的实现，不能悄悄改变前两层。

对每个节点建立因果链：输入版本与配置 → 操作或调度 → 中间状态 → 可观察输出 → 独立对账。在“第21章 并行与分布式存储”的证据链中，若结论涉及性能，报告中位数、尾延迟、吞吐、I/O和等待，而不是只截一条最快记录；若涉及正确性，至少准备一个应成功样本和一个应失败样本，并记录错误类别或恢复终点。

## 最小可重放实验

```sql
-- 固定大学模式版本，并用独立计数建立输入基线
SELECT 'department' AS relation_name, COUNT(*) AS rows_n FROM department
UNION ALL
SELECT 'student', COUNT(*) FROM student
UNION ALL
SELECT 'takes', COUNT(*) FROM takes;
```

先在纸面写出预计关系数、目标键、异常类型和恢复终点。再执行本章实验，并把每一步与“每条记录的放置规则确定，复制版本收敛，节点故障与热点下仍满足目标可用性”对照。查询正确不等于机制解释正确，执行成功也不等于故障后仍正确。

```text
book=数据库系统概念（原书第7版 / 2021）
unit=dsc-ch21-parallel-distributed-storage
medium=纸书正文
baseline=university-schema-v1
prediction=执行前写明结果、状态变化与失败终点
artifact=分片函数、倾斜直方图、复制故障实验和再平衡计划
invariant=每条记录的放置规则确定，复制版本收敛，节点故障与热点下仍满足目标可用性
```

```sql
-- 实验后用不同于待测操作的查询核对引用完整性与孤立记录
SELECT COUNT(*) AS orphan_takes
FROM takes t
LEFT JOIN student s ON s.ID = t.ID
WHERE s.ID IS NULL;
```

## 三步交互实验

<Stepper>
  <Step title="1. 建立结构模型">
    选择正式目录节点，沿语义、计划、状态与证据四层定位责任模块。

    <DscCh21ParallelDistributedStorageArchitectureLab />

  </Step>
  <Step title="2. 注入规模或故障">
    先预测正常、边界与失败场景，再改变数据规模和故障位置比较结果。

    <DscCh21ParallelDistributedStorageExperimentLab />

  </Step>
  <Step title="3. 通过证据门">
    登记版本、基线、计划、状态、反例与回退，六项齐备后才交付。

    <DscCh21ParallelDistributedStorageEvidenceLab />

  </Step>
</Stepper>

<Callout type="trap">
  **建模陷阱。**
  只按平均数据量分区，忽略热键、访问局部性和再平衡期间的双写一致性。先恢复业务不变量、输入版本和最小反例，再选择结构或协议；不要让偶然成功的样例替代证明。
</Callout>

<Callout type="trap">
  **证据陷阱。**
  只保存最终截图无法区分逻辑错误、计划变化、缓存命中、并发交错和恢复副作用。必须同时保存预期、完整输入、操作顺序、关键中间状态和独立对账。
</Callout>

<Callout type="warning">
  写入、并发、崩溃、恢复与分布式实验只在隔离样例库进行。先保存重置脚本，规定影响行数、等待时间、磁盘空间和停止条件。
</Callout>

## 证据解释与交接

语义证据证明结果定义没有漂移。保存关系模式、约束、查询文本、参数、隔离级别和预期行集；涉及NULL、重复、顺序或聚集时，单独列出处理规则。任何实现比较都必须共享同一语义合同。

执行证据证明机制判断可以复核。保存计划、实际行数、缓冲命中、I/O、锁或版本、日志位置和错误状态中与本章相关的部分。计划估计与实际偏差本身就是结果，不应通过只截取计划名称隐藏。

失败证据证明边界真实存在。每次只注入一个变量：空输入、重复键、倾斜、并发冲突、进程崩溃、存储丢失或网络分区。明确失败前最后一个持久状态、恢复后第一个可用状态，以及是否需要人工介入。

交接证据让另一位学习者无需口头补充就能复现。最少包括版本卡、大学模式加载与重置脚本、预测表、分片函数、倾斜直方图、复制故障实验和再平衡计划、正常与失败轨迹、独立对账、已知限制和回退条件。若更换DBMS或版本，先重跑基线再比较。

## 本章回顾

重新完成“用分区、复制、并行索引和分布式文件组织大规模数据”：先声明不变量和输入版本，按8个目录节点建立模型，手算正常路径，注入一个边界或故障，收集语义与执行证据，最后由独立查询对账。最终交付分片函数、倾斜直方图、复制故障实验和再平衡计划，并能证明“每条记录的放置规则确定，复制版本收敛，节点故障与热点下仍满足目标可用性”。

## 复习与独立验收

<Exercises>
**问题 1**：为什么“第21章 并行与分布式存储”必须覆盖8个目录节点？

<Answer>这些节点共同组成“用分区、复制、并行索引和分布式文件组织大规模数据”的语义、结构、执行、故障和证据链；缺项会让分片函数、倾斜直方图、复制故障实验和再平衡计划无法独立复现。</Answer>

**问题 2**：本页最小正确性合同是什么？

<Answer>
  每条记录的放置规则确定，复制版本收敛，节点故障与热点下仍满足目标可用性。它必须由固定输入、正常轨迹、失败反例和独立对账共同证明。
</Answer>

**问题 3**：怎样构造能推翻常见错误的最小反例？

<Answer>
  把“只按平均数据量分区，忽略热键、访问局部性和再平衡期间的双写一致性”写成待证假设，只改变一个数据、并发或故障变量，比较前后状态与独立对账结果。
</Answer>

**问题 4**：为什么执行成功不能证明数据库设计正确？

<Answer>
  执行成功只说明当前输入没有触发可见错误，不能证明所有合法调度、NULL、重复、故障和恢复边界都保持不变量；还需反例与机制证据。
</Answer>

**问题 5**：性能结论至少需要哪些数据？

<Answer>
  需要固定工作负载与数据规模、预热策略、重复次数、延迟分布、吞吐、I/O或等待、执行计划和正确性对账，不能只报告一次最快值。
</Answer>

**问题 6**：独立交接至少包含什么？

<Answer>需要版本与配置、大学模式基线、预测表、分片函数、倾斜直方图、复制故障实验和再平衡计划、正常与失败轨迹、独立对账、已知限制和回退条件。</Answer>
</Exercises>

<Glossary>
  <GlossaryItem term="分区">
    分区在本页中以“每条记录的放置规则确定，复制版本收敛，节点故障与热点下仍满足目标可用性”为正确性边界，并由可重放实验验证。
  </GlossaryItem>
  <GlossaryItem term="数据倾斜">
    数据倾斜在本页中以“每条记录的放置规则确定，复制版本收敛，节点故障与热点下仍满足目标可用性”为正确性边界，并由可重放实验验证。
  </GlossaryItem>
  <GlossaryItem term="复制">
    复制在本页中以“每条记录的放置规则确定，复制版本收敛，节点故障与热点下仍满足目标可用性”为正确性边界，并由可重放实验验证。
  </GlossaryItem>
  <GlossaryItem term="分布式文件系统">
    分布式文件系统在本页中以“每条记录的放置规则确定，复制版本收敛，节点故障与热点下仍满足目标可用性”为正确性边界，并由可重放实验验证。
  </GlossaryItem>
  <GlossaryItem term="键值存储">
    键值存储在本页中以“每条记录的放置规则确定，复制版本收敛，节点故障与热点下仍满足目标可用性”为正确性边界，并由可重放实验验证。
  </GlossaryItem>
</Glossary>

[← 上一页：第20章 数据库系统体系结构](/learn/database-system-concepts/20-architectures/dsc-ch20-architectures) · [下一页：第22章 并行与分布式查询处理 →](/learn/database-system-concepts/22-parallel-distributed-query/dsc-ch22-parallel-distributed-query)

<Attribution
  mode="independent-rewrite"
  sourceBasis="authorized-sample"
  workTitle="Abraham Silberschatz, Henry F. Korth, S. Sudarshan, Database System Concepts, Seventh Edition"
  adaptedUrl="https://www.db-book.com/"
/>
