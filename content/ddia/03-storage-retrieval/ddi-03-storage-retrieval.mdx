---
title: "第3章 数据存储与检索"
description: "依据2018中文第1版完整目录：沿写路径、读路径和后台维护理解日志结构、B树、列存与物化聚合的成本模型"
type: C
section: "数据密集型应用系统设计（第1版 / 2018）"
draft: false
math: false
sourceUrl: "https://www.oreilly.com/library/view/designing-data-intensive-applications/9781491903063/"
demo: true
order: 0
---

import { Objectives } from "@/components/mdx/objectives";
import { Term } from "@/components/mdx/term";
import { Callout } from "@/components/mdx/callout";
import { Answer, Exercises } from "@/components/mdx/exercises";
import { Glossary, GlossaryItem } from "@/components/mdx/glossary";
import { Attribution } from "@/components/mdx/attribution";

# 第3章 数据存储与检索

本课程对应Martin Kleppmann著，赵军平、吕云松、耿煜、李三平译《数据密集型应用系统设计》第1版，中国电力出版社2018年9月出版，纸书519页，ISBN 9787519821968。英文原版由O'Reilly于2017年3月出版，616页，ISBN 9781491903063。

O'Reilly第1版完整目录确认3部分、12章与术语表。英文第2版由Martin Kleppmann与Chris Riccomini合著，于2026年出版并重排目录；当前课程对应2018中文第1版，不把第2版静默混入。课程独立重构核心知识、图示与实验，不复制原文。本页逐项覆盖15个正式章/节节点。

<Objectives>

- 能解释“第3章 数据存储与检索”的15个页面节点，并连接到“沿写路径、读路径和后台维护理解日志结构、B树、列存与物化聚合的成本模型”主线。
- 能绘制权威状态、派生状态、消息与故障边界，复现写读路径图、索引基准、放大系数、压缩对照与恢复验证。
- 能比较正常、压力和部分故障历史，证明“存储布局在崩溃恢复、并发写和真实分布下返回正确结果，读写空间放大与尾延迟可度量”。
- 能设计反例推翻“只比较单次查询平均耗时，不计压实、缓存、写放大、恢复和空间回收”，给出停止、恢复和回退条件。

</Objectives>

## 从一个不可判定的用户结果开始

先预测：客户端超时后，这次写入究竟未发生、已提交还是仍可能提交？先写用户不变量、允许陈旧窗口、尾延迟和恢复目标，再选择模型、协议或存储结构。不要先看监控图再编故事；先固定输入和预期，运行后才允许修正。

本页主问题是“沿写路径、读路径和后台维护理解日志结构、B树、列存与物化聚合的成本模型”。最终交付不是概念摘要，而是写读路径图、索引基准、放大系数、压缩对照与恢复验证，由独立复跑证明：**存储布局在崩溃恢复、并发写和真实分布下返回正确结果，读写空间放大与尾延迟可度量**。

## 核心词汇与运行边界

<Term def="追加日志是“沿写路径、读路径和后台维护理解日志结构、B树、列存与物化聚合的成本模型”中的第1个核心概念，必须由状态、顺序、故障与证据定义">追加日志</Term>、<Term def="LSM树是“沿写路径、读路径和后台维护理解日志结构、B树、列存与物化聚合的成本模型”中的第2个核心概念，必须由状态、顺序、故障与证据定义">LSM树</Term>、<Term def="B树是“沿写路径、读路径和后台维护理解日志结构、B树、列存与物化聚合的成本模型”中的第3个核心概念，必须由状态、顺序、故障与证据定义">B树</Term>、<Term def="写放大是“沿写路径、读路径和后台维护理解日志结构、B树、列存与物化聚合的成本模型”中的第4个核心概念，必须由状态、顺序、故障与证据定义">写放大</Term>、<Term def="列式存储是“沿写路径、读路径和后台维护理解日志结构、B树、列存与物化聚合的成本模型”中的第5个核心概念，必须由状态、顺序、故障与证据定义">列式存储</Term>
构成本页共同语言。每个词都必须回答：谁观察、观察哪个状态、顺序依据是什么、部分故障时会看到什么，以及哪条证据能推翻解释。


## 正式目录逐节点重构

### 驱动数据库的数据结构

**目录节点 1/15。** 索引用额外空间和维护成本换取更短查找路径。

把“驱动数据库的数据结构”放回“沿写路径、读路径和后台维护理解日志结构、B树、列存与物化聚合的成本模型”主线：上游是“用户结果与本章问题”，下游是“哈希索引”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“存储布局在崩溃恢复、并发写和真实分布下返回正确结果，读写空间放大与尾延迟可度量”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 哈希索引

**目录节点 2/15。** 内存哈希表指向追加日志偏移，段合并回收旧值。

把“哈希索引”放回“沿写路径、读路径和后台维护理解日志结构、B树、列存与物化聚合的成本模型”主线：上游是“驱动数据库的数据结构”，下游是“SSTable与LSM树”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“存储布局在崩溃恢复、并发写和真实分布下返回正确结果，读写空间放大与尾延迟可度量”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### SSTable与LSM树

**目录节点 3/15。** 有序不可变段、稀疏索引和压实共同控制放大。

把“SSTable与LSM树”放回“沿写路径、读路径和后台维护理解日志结构、B树、列存与物化聚合的成本模型”主线：上游是“哈希索引”，下游是“B树”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“存储布局在崩溃恢复、并发写和真实分布下返回正确结果，读写空间放大与尾延迟可度量”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### B树

**目录节点 4/15。** 固定页面和高扇出提供稳定读，分裂、WAL与锁保证更新。

把“B树”放回“沿写路径、读路径和后台维护理解日志结构、B树、列存与物化聚合的成本模型”主线：上游是“SSTable与LSM树”，下游是“比较B树和LSM树”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“存储布局在崩溃恢复、并发写和真实分布下返回正确结果，读写空间放大与尾延迟可度量”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 比较B树和LSM树

**目录节点 5/15。** 两者在顺序写、随机页更新、压实和读放大间取舍。

把“比较B树和LSM树”放回“沿写路径、读路径和后台维护理解日志结构、B树、列存与物化聚合的成本模型”主线：上游是“B树”，下游是“其他索引结构”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“存储布局在崩溃恢复、并发写和真实分布下返回正确结果，读写空间放大与尾延迟可度量”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 其他索引结构

**目录节点 6/15。** 二级、覆盖、多列和全文索引服务不同谓词并承担维护成本。

把“其他索引结构”放回“沿写路径、读路径和后台维护理解日志结构、B树、列存与物化聚合的成本模型”主线：上游是“比较B树和LSM树”，下游是“事务处理还是分析处理”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“存储布局在崩溃恢复、并发写和真实分布下返回正确结果，读写空间放大与尾延迟可度量”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 事务处理还是分析处理

**目录节点 7/15。** OLTP小范围低延迟，OLAP大范围扫描，访问模式相反。

把“事务处理还是分析处理”放回“沿写路径、读路径和后台维护理解日志结构、B树、列存与物化聚合的成本模型”主线：上游是“其他索引结构”，下游是“数据仓库”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“存储布局在崩溃恢复、并发写和真实分布下返回正确结果，读写空间放大与尾延迟可度量”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 数据仓库

**目录节点 8/15。** 仓库隔离分析资源，同时要保存来源、更新时点和质量。

把“数据仓库”放回“沿写路径、读路径和后台维护理解日志结构、B树、列存与物化聚合的成本模型”主线：上游是“事务处理还是分析处理”，下游是“星型与雪花型Schema”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“存储布局在崩溃恢复、并发写和真实分布下返回正确结果，读写空间放大与尾延迟可度量”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 星型与雪花型Schema

**目录节点 9/15。** 事实和维度的规范化程度决定连接与重复成本。

把“星型与雪花型Schema”放回“沿写路径、读路径和后台维护理解日志结构、B树、列存与物化聚合的成本模型”主线：上游是“数据仓库”，下游是“列式存储”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“存储布局在崩溃恢复、并发写和真实分布下返回正确结果，读写空间放大与尾延迟可度量”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 列式存储

**目录节点 10/15。** 只读所需列利于压缩和向量化，整行随机更新更昂贵。

把“列式存储”放回“沿写路径、读路径和后台维护理解日志结构、B树、列存与物化聚合的成本模型”主线：上游是“星型与雪花型Schema”，下游是“列压缩”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“存储布局在崩溃恢复、并发写和真实分布下返回正确结果，读写空间放大与尾延迟可度量”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 列压缩

**目录节点 11/15。** 位图、游程和字典编码利用低基数与重复值。

把“列压缩”放回“沿写路径、读路径和后台维护理解日志结构、B树、列存与物化聚合的成本模型”主线：上游是“列式存储”，下游是“列存中的排序顺序”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“存储布局在崩溃恢复、并发写和真实分布下返回正确结果，读写空间放大与尾延迟可度量”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 列存中的排序顺序

**目录节点 12/15。** 排序可减少扫描和改善压缩，也限制其他查询的局部性。

把“列存中的排序顺序”放回“沿写路径、读路径和后台维护理解日志结构、B树、列存与物化聚合的成本模型”主线：上游是“列压缩”，下游是“写入列式存储”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“存储布局在崩溃恢复、并发写和真实分布下返回正确结果，读写空间放大与尾延迟可度量”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 写入列式存储

**目录节点 13/15。** 内存增量吸收写入，后台合并后读取要协调新旧视图。

把“写入列式存储”放回“沿写路径、读路径和后台维护理解日志结构、B树、列存与物化聚合的成本模型”主线：上游是“列存中的排序顺序”，下游是“聚合：数据立方体与物化视图”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“存储布局在崩溃恢复、并发写和真实分布下返回正确结果，读写空间放大与尾延迟可度量”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 聚合：数据立方体与物化视图

**目录节点 14/15。** 预计算用刷新与存储成本换取查询速度。

把“聚合：数据立方体与物化视图”放回“沿写路径、读路径和后台维护理解日志结构、B树、列存与物化聚合的成本模型”主线：上游是“写入列式存储”，下游是“小结”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“存储布局在崩溃恢复、并发写和真实分布下返回正确结果，读写空间放大与尾延迟可度量”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 小结

**目录节点 15/15。** 评估必须包含前台路径、后台工作、恢复和结果正确性。

把“小结”放回“沿写路径、读路径和后台维护理解日志结构、B树、列存与物化聚合的成本模型”主线：上游是“聚合：数据立方体与物化视图”，下游是“本章交付与独立验收”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“存储布局在崩溃恢复、并发写和真实分布下返回正确结果，读写空间放大与尾延迟可度量”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

## 机制推演：状态、顺序与失败

第一步固定状态所有权。写出哪个数据集是权威事实，哪些只是缓存、索引、仓库或UI等派生视图；再写每次转换的输入版本、输出版本和提交点。没有所有权就无法区分恢复、覆盖和冲突，也无法判断“沿写路径、读路径和后台维护理解日志结构、B树、列存与物化聚合的成本模型”中的结果由谁负责。

第二步展开时间线。客户端发送、节点接收、本地持久化、复制或派生、确认和后续读取是不同事件；超时只提供观察者的等待上界，不提供远端提交事实。为每个事件记录逻辑顺序，墙钟仅作诊断。

第三步施加负载与故障。正常路径不能证明队列增长、热点、进程暂停、消息重复、节点崩溃或网络分区时仍满足“存储布局在崩溃恢复、并发写和真实分布下返回正确结果，读写空间放大与尾延迟可度量”。最小实验一次只改变一个因素，并保留能复盘状态转移的原始历史。

第四步从外部核对。服务自报成功、任务完成或副本在线都属于被测路径内部证据；必须用业务标识、数量、约束、校验和或独立读取核对。最终材料“写读路径图、索引基准、放大系数、压缩对照与恢复验证”还要包含未覆盖边界与回退。

<Callout type="trap">
  **语义陷阱。**
  只比较单次查询平均耗时，不计压实、缓存、写放大、恢复和空间回收。先恢复用户不变量、提交点和故障模型，再比较方案；产品名、节点状态和平均指标不能替代保证。
</Callout>

<Callout type="trap">
  **时间陷阱。**
  客户端时间、服务器日志时间和逻辑提交顺序不同。时钟偏差、进程暂停、排队和重试会制造矛盾记录；先按消息与操作标识建立偏序，再讨论因果。
</Callout>

<Callout type="warning">
  崩溃、分区、数据回放和并发写实验只在隔离环境执行。规定最大数据量、错误预算、停止条件、清理方式和恢复负责人。
</Callout>

## 证据解释与独立交接

契约证据说明什么必须成立：记录业务不变量、请求身份、成功定义、允许陈旧度、顺序模型和SLO。任何模糊词都改写成可观察历史，例如高可用要说明分区哪侧可写，一致要说明允许哪些并发历史。

机制证据说明为什么可能成立：保存数据模型、分区与副本映射、事务或日志位置、消息顺序、版本和配置。只截最终仪表盘会丢失中间状态，无法区分修复、掩盖和错误重试。

负载证据说明在什么规模成立：记录请求分布、热点、并发、数据量、缓存状态、P50/P95/P99、吞吐和资源等待。平均值会隐藏最慢分区、锁队列、压实、检查点与网络抖动。

故障证据说明边界在哪里：在确认、持久化、复制或派生前后分别注入故障，观察重试、选举、回放与补偿。最后用外部读者对账，确认没有丢失、重复、顺序颠倒或约束破坏。

## 本章回顾

重新完成“沿写路径、读路径和后台维护理解日志结构、B树、列存与物化聚合的成本模型”：固定版本和目录，写出用户不变量与权威状态，展开消息时间线，运行基线，再以单一故障寻找反例。最终交付写读路径图、索引基准、放大系数、压缩对照与恢复验证，证明“存储布局在崩溃恢复、并发写和真实分布下返回正确结果，读写空间放大与尾延迟可度量”，并保留未解决风险。

## 复习与生产验收

<Exercises>
**问题 1**：为什么“第3章 数据存储与检索”必须覆盖15个页面节点？
<Answer>这些节点组成“沿写路径、读路径和后台维护理解日志结构、B树、列存与物化聚合的成本模型”的状态、顺序、故障与证据链；缺项会使写读路径图、索引基准、放大系数、压缩对照与恢复验证无法复现。</Answer>

**问题 2**：本页最小运行不变量是什么？

<Answer>
  存储布局在崩溃恢复、并发写和真实分布下返回正确结果，读写空间放大与尾延迟可度量。它必须由固定输入、正常历史、压力数据、故障反例和独立业务对账共同证明。
</Answer>

**问题 3**：怎样为“第3章 数据存储与检索”构造最小反例？

<Answer>
  把“只比较单次查询平均耗时，不计压实、缓存、写放大、恢复和空间回收”写成假设，保持版本、数据和请求不变，只改变延迟、并发、崩溃点或重试之一。
</Answer>

**问题 4**：为什么超时不能直接解释为失败？

<Answer>
  超时只表示观察者没按时收到回复；远端可能未处理、已提交或仍执行。必须用操作标识、幂等规则、提交记录和独立读取判断。
</Answer>

**问题 5**：怎样验证本页性能结论？

<Answer>
  固定数据和请求，重复测量吞吐、错误、P50/P95/P99与资源等待，同时核对语义，防止用丢请求或弱化保证换取数字。
</Answer>

**问题 6**：独立交接至少包含什么？

<Answer>需要版本目录、契约输入、写读路径图、索引基准、放大系数、压缩对照与恢复验证、正常与故障历史、业务对账、停止条件、恢复和回退责任人。</Answer>
</Exercises>

<Glossary>
  <GlossaryItem term="追加日志">
    追加日志在本页指向“沿写路径、读路径和后台维护理解日志结构、B树、列存与物化聚合的成本模型”中的具体状态和保证；边界由“存储布局在崩溃恢复、并发写和真实分布下返回正确结果，读写空间放大与尾延迟可度量”与失败实验共同限定。
  </GlossaryItem>
  <GlossaryItem term="LSM树">
    LSM树在本页指向“沿写路径、读路径和后台维护理解日志结构、B树、列存与物化聚合的成本模型”中的具体状态和保证；边界由“存储布局在崩溃恢复、并发写和真实分布下返回正确结果，读写空间放大与尾延迟可度量”与失败实验共同限定。
  </GlossaryItem>
  <GlossaryItem term="B树">
    B树在本页指向“沿写路径、读路径和后台维护理解日志结构、B树、列存与物化聚合的成本模型”中的具体状态和保证；边界由“存储布局在崩溃恢复、并发写和真实分布下返回正确结果，读写空间放大与尾延迟可度量”与失败实验共同限定。
  </GlossaryItem>
  <GlossaryItem term="写放大">
    写放大在本页指向“沿写路径、读路径和后台维护理解日志结构、B树、列存与物化聚合的成本模型”中的具体状态和保证；边界由“存储布局在崩溃恢复、并发写和真实分布下返回正确结果，读写空间放大与尾延迟可度量”与失败实验共同限定。
  </GlossaryItem>
  <GlossaryItem term="列式存储">
    列式存储在本页指向“沿写路径、读路径和后台维护理解日志结构、B树、列存与物化聚合的成本模型”中的具体状态和保证；边界由“存储布局在崩溃恢复、并发写和真实分布下返回正确结果，读写空间放大与尾延迟可度量”与失败实验共同限定。
  </GlossaryItem>
</Glossary>

[← 上一页：第2章 数据模型与查询语言](/learn/ddia/02-data-models-query-languages/ddi-02-data-models-query-languages) · [下一页：第4章 数据编码与演化 →](/learn/ddia/04-encoding-evolution/ddi-04-encoding-evolution)

<Attribution
  adaptedFrom="《数据密集型应用系统设计》第1版"
  adaptedUrl="https://www.oreilly.com/library/view/designing-data-intensive-applications/9781491903063/"
/>
