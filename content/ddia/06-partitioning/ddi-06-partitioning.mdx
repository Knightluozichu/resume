---
title: "第6章 分区"
description: "依据2018中文第1版完整目录：按键范围或哈希分散数据与请求，控制热点、二级索引、再平衡和路由变化"
type: C
section: "数据密集型应用系统设计（第1版 / 2018）"
draft: false
math: false
sourceUrl: "https://www.oreilly.com/library/view/designing-data-intensive-applications/9781491903063/"
demo: true
order: 0
---

import {
  Ddi06PartitioningArchitectureLab,
  Ddi06PartitioningFailureLab,
  Ddi06PartitioningEvidenceLab,
} from "@/components/mdx/ddia/diagrams/ddi-06-partitioning";

# 第6章 分区

本课程对应Martin Kleppmann著，赵军平、吕云松、耿煜、李三平译《数据密集型应用系统设计》第1版，中国电力出版社2018年9月出版，纸书519页，ISBN 9787519821968。英文原版由O'Reilly于2017年3月出版，616页，ISBN 9781491903063。

O'Reilly第1版完整目录确认3部分、12章与术语表。英文第2版由Martin Kleppmann与Chris Riccomini合著，于2026年出版并重排目录；当前课程对应2018中文第1版，不把第2版静默混入。课程独立重构核心知识、图示与实验，不复制原文。本页逐项覆盖14个正式章/节节点。

<Objectives>

- 能解释“第6章 分区”的14个页面节点，并连接到“按键范围或哈希分散数据与请求，控制热点、二级索引、再平衡和路由变化”主线。
- 能绘制权威状态、派生状态、消息与故障边界，复现键分布直方图、分区映射、再平衡演练、路由追踪与跨分区报告。
- 能比较正常、压力和部分故障历史，证明“归属变化期间没有丢失、重复拥有或错误路由，热点与再平衡成本受控”。
- 能设计反例推翻“假设哈希天然均匀、自动再平衡天然安全，让热点键或全局索引拖垮集群”，给出停止、恢复和回退条件。

</Objectives>

## 从一个不可判定的用户结果开始

先预测：客户端超时后，这次写入究竟未发生、已提交还是仍可能提交？先写用户不变量、允许陈旧窗口、尾延迟和恢复目标，再选择模型、协议或存储结构。不要先看监控图再编故事；先固定输入和预期，运行后才允许修正。

本页主问题是“按键范围或哈希分散数据与请求，控制热点、二级索引、再平衡和路由变化”。最终交付不是概念摘要，而是键分布直方图、分区映射、再平衡演练、路由追踪与跨分区报告，由独立复跑证明：**归属变化期间没有丢失、重复拥有或错误路由，热点与再平衡成本受控**。

## 核心词汇与运行边界

<Term def="分区是“按键范围或哈希分散数据与请求，控制热点、二级索引、再平衡和路由变化”中的第1个核心概念，必须由状态、顺序、故障与证据定义">分区</Term>、<Term def="键范围是“按键范围或哈希分散数据与请求，控制热点、二级索引、再平衡和路由变化”中的第2个核心概念，必须由状态、顺序、故障与证据定义">键范围</Term>、<Term def="哈希是“按键范围或哈希分散数据与请求，控制热点、二级索引、再平衡和路由变化”中的第3个核心概念，必须由状态、顺序、故障与证据定义">哈希</Term>、<Term def="热点是“按键范围或哈希分散数据与请求，控制热点、二级索引、再平衡和路由变化”中的第4个核心概念，必须由状态、顺序、故障与证据定义">热点</Term>、<Term def="再平衡是“按键范围或哈希分散数据与请求，控制热点、二级索引、再平衡和路由变化”中的第5个核心概念，必须由状态、顺序、故障与证据定义">再平衡</Term>
构成本页共同语言。每个词都必须回答：谁观察、观察哪个状态、顺序依据是什么、部分故障时会看到什么，以及哪条证据能推翻解释。

<Ddi06PartitioningArchitectureLab />

## 正式目录逐节点重构

### 分区与复制

**目录节点 1/14。** 每个分区还可有多个副本，需要同时追踪归属和角色。

把“分区与复制”放回“按键范围或哈希分散数据与请求，控制热点、二级索引、再平衡和路由变化”主线：上游是“用户结果与本章问题”，下游是“键值数据的分区”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“归属变化期间没有丢失、重复拥有或错误路由，热点与再平衡成本受控”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 键值数据的分区

**目录节点 2/14。** 数据量均匀不代表请求、存储或计算负载均匀。

把“键值数据的分区”放回“按键范围或哈希分散数据与请求，控制热点、二级索引、再平衡和路由变化”主线：上游是“分区与复制”，下游是“按键范围分区”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“归属变化期间没有丢失、重复拥有或错误路由，热点与再平衡成本受控”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 按键范围分区

**目录节点 3/14。** 支持范围扫描，但顺序时间键可能集中最新写入。

把“按键范围分区”放回“按键范围或哈希分散数据与请求，控制热点、二级索引、再平衡和路由变化”主线：上游是“键值数据的分区”，下游是“按键哈希分区”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“归属变化期间没有丢失、重复拥有或错误路由，热点与再平衡成本受控”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 按键哈希分区

**目录节点 4/14。** 打散相邻键却分散范围查询，并影响迁移。

把“按键哈希分区”放回“按键范围或哈希分散数据与请求，控制热点、二级索引、再平衡和路由变化”主线：上游是“按键范围分区”，下游是“偏斜负载与热点缓解”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“归属变化期间没有丢失、重复拥有或错误路由，热点与再平衡成本受控”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 偏斜负载与热点缓解

**目录节点 5/14。** 大租户和名人键需要拆分、随机前缀或应用聚合。

把“偏斜负载与热点缓解”放回“按键范围或哈希分散数据与请求，控制热点、二级索引、再平衡和路由变化”主线：上游是“按键哈希分区”，下游是“分区与二级索引”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“归属变化期间没有丢失、重复拥有或错误路由，热点与再平衡成本受控”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 分区与二级索引

**目录节点 6/14。** 索引键不同于分区键时要在广播读和跨分区写间取舍。

把“分区与二级索引”放回“按键范围或哈希分散数据与请求，控制热点、二级索引、再平衡和路由变化”主线：上游是“偏斜负载与热点缓解”，下游是“按文档分区的二级索引”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“归属变化期间没有丢失、重复拥有或错误路由，热点与再平衡成本受控”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 按文档分区的二级索引

**目录节点 7/14。** 本地索引写入简单，查询需散射并汇总。

把“按文档分区的二级索引”放回“按键范围或哈希分散数据与请求，控制热点、二级索引、再平衡和路由变化”主线：上游是“分区与二级索引”，下游是“按词项分区的二级索引”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“归属变化期间没有丢失、重复拥有或错误路由，热点与再平衡成本受控”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 按词项分区的二级索引

**目录节点 8/14。** 全局索引定向读取，却让更新触及多个分区。

把“按词项分区的二级索引”放回“按键范围或哈希分散数据与请求，控制热点、二级索引、再平衡和路由变化”主线：上游是“按文档分区的二级索引”，下游是“分区再平衡”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“归属变化期间没有丢失、重复拥有或错误路由，热点与再平衡成本受控”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 分区再平衡

**目录节点 9/14。** 迁移时仍要服务读写并避免所有权歧义。

把“分区再平衡”放回“按键范围或哈希分散数据与请求，控制热点、二级索引、再平衡和路由变化”主线：上游是“按词项分区的二级索引”，下游是“再平衡策略”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“归属变化期间没有丢失、重复拥有或错误路由，热点与再平衡成本受控”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 再平衡策略

**目录节点 10/14。** 固定、动态范围和按节点比例分区的迁移粒度不同。

把“再平衡策略”放回“按键范围或哈希分散数据与请求，控制热点、二级索引、再平衡和路由变化”主线：上游是“分区再平衡”，下游是“自动或手动再平衡操作”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“归属变化期间没有丢失、重复拥有或错误路由，热点与再平衡成本受控”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 自动或手动再平衡操作

**目录节点 11/14。** 自动化需限速、故障门与人工批准避免扩大事故。

把“自动或手动再平衡操作”放回“按键范围或哈希分散数据与请求，控制热点、二级索引、再平衡和路由变化”主线：上游是“再平衡策略”，下游是“请求路由”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“归属变化期间没有丢失、重复拥有或错误路由，热点与再平衡成本受控”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 请求路由

**目录节点 12/14。** 旧映射需要版本、重定向和幂等重试。

把“请求路由”放回“按键范围或哈希分散数据与请求，控制热点、二级索引、再平衡和路由变化”主线：上游是“自动或手动再平衡操作”，下游是“并行查询执行”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“归属变化期间没有丢失、重复拥有或错误路由，热点与再平衡成本受控”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 并行查询执行

**目录节点 13/14。** 最慢分区、扇出与部分失败决定尾延迟和完整性。

把“并行查询执行”放回“按键范围或哈希分散数据与请求，控制热点、二级索引、再平衡和路由变化”主线：上游是“请求路由”，下游是“小结”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“归属变化期间没有丢失、重复拥有或错误路由，热点与再平衡成本受控”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 小结

**目录节点 14/14。** 容量问题被转化为分布、元数据、迁移和扇出问题。

把“小结”放回“按键范围或哈希分散数据与请求，控制热点、二级索引、再平衡和路由变化”主线：上游是“并行查询执行”，下游是“本章交付与独立验收”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“归属变化期间没有丢失、重复拥有或错误路由，热点与再平衡成本受控”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

## 机制推演：状态、顺序与失败

第一步固定状态所有权。写出哪个数据集是权威事实，哪些只是缓存、索引、仓库或UI等派生视图；再写每次转换的输入版本、输出版本和提交点。没有所有权就无法区分恢复、覆盖和冲突，也无法判断“按键范围或哈希分散数据与请求，控制热点、二级索引、再平衡和路由变化”中的结果由谁负责。

第二步展开时间线。客户端发送、节点接收、本地持久化、复制或派生、确认和后续读取是不同事件；超时只提供观察者的等待上界，不提供远端提交事实。为每个事件记录逻辑顺序，墙钟仅作诊断。

第三步施加负载与故障。正常路径不能证明队列增长、热点、进程暂停、消息重复、节点崩溃或网络分区时仍满足“归属变化期间没有丢失、重复拥有或错误路由，热点与再平衡成本受控”。最小实验一次只改变一个因素，并保留能复盘状态转移的原始历史。

第四步从外部核对。服务自报成功、任务完成或副本在线都属于被测路径内部证据；必须用业务标识、数量、约束、校验和或独立读取核对。最终材料“键分布直方图、分区映射、再平衡演练、路由追踪与跨分区报告”还要包含未覆盖边界与回退。

## 最小可重放实验

```text
book=数据密集型应用系统设计（第1版 / 2018）
unit=ddi-06-partitioning
question=按键范围或哈希分散数据与请求，控制热点、二级索引、再平衡和路由变化
invariant=归属变化期间没有丢失、重复拥有或错误路由，热点与再平衡成本受控
artifact=键分布直方图、分区映射、再平衡演练、路由追踪与跨分区报告
```

```pseudo
given fixed_version, fixed_dataset, fixed_request_history
predict user_result, state_transitions, p99, failure_endpoint
run baseline and capture ordered_events
change exactly one of latency, concurrency, crash_point, retry_policy
assert business_invariant with an independent_reader
```

```yaml
evidence:
  input: schema, distribution, request ids, dependency versions
  execution: ordered events, queue depth, resource percentiles
  failure: injection point, timeout, retry, leader or offset changes
  result: accepted, committed, visible, reconciled, recovered
  decision: pass, reject, rollback, unresolved
```

## 三步交互实验

<Stepper>
  <Step title="1. 固定数据流和目录节点">
    选择正式节点与部署形态，标出请求、权威状态和派生视图。
    <Ddi06PartitioningArchitectureLab />
  </Step>
  <Step title="2. 注入延迟、并发与分区">
    先预测P99、陈旧窗口、可用性与冲突风险，再改变单一条件。
    <Ddi06PartitioningFailureLab />
  </Step>
  <Step title="3. 通过独立证据门">
    六类证据齐备后才能宣布机制、性能、恢复或一致性结论成立。
    <Ddi06PartitioningEvidenceLab />
  </Step>
</Stepper>

<Callout type="trap">
  **语义陷阱。**
  假设哈希天然均匀、自动再平衡天然安全，让热点键或全局索引拖垮集群。先恢复用户不变量、提交点和故障模型，再比较方案；产品名、节点状态和平均指标不能替代保证。
</Callout>

<Callout type="trap">
  **时间陷阱。**
  客户端时间、服务器日志时间和逻辑提交顺序不同。时钟偏差、进程暂停、排队和重试会制造矛盾记录；先按消息与操作标识建立偏序，再讨论因果。
</Callout>

<Callout type="warning">
  崩溃、分区、数据回放和并发写实验只在隔离环境执行。规定最大数据量、错误预算、停止条件、清理方式和恢复负责人。
</Callout>

## 证据解释与独立交接

契约证据说明什么必须成立：记录业务不变量、请求身份、成功定义、允许陈旧度、顺序模型和SLO。任何模糊词都改写成可观察历史，例如高可用要说明分区哪侧可写，一致要说明允许哪些并发历史。

机制证据说明为什么可能成立：保存数据模型、分区与副本映射、事务或日志位置、消息顺序、版本和配置。只截最终仪表盘会丢失中间状态，无法区分修复、掩盖和错误重试。

负载证据说明在什么规模成立：记录请求分布、热点、并发、数据量、缓存状态、P50/P95/P99、吞吐和资源等待。平均值会隐藏最慢分区、锁队列、压实、检查点与网络抖动。

故障证据说明边界在哪里：在确认、持久化、复制或派生前后分别注入故障，观察重试、选举、回放与补偿。最后用外部读者对账，确认没有丢失、重复、顺序颠倒或约束破坏。

## 本章回顾

重新完成“按键范围或哈希分散数据与请求，控制热点、二级索引、再平衡和路由变化”：固定版本和目录，写出用户不变量与权威状态，展开消息时间线，运行基线，再以单一故障寻找反例。最终交付键分布直方图、分区映射、再平衡演练、路由追踪与跨分区报告，证明“归属变化期间没有丢失、重复拥有或错误路由，热点与再平衡成本受控”，并保留未解决风险。

## 复习与生产验收

<Exercises>
**问题 1**：为什么“第6章 分区”必须覆盖14个页面节点？
<Answer>这些节点组成“按键范围或哈希分散数据与请求，控制热点、二级索引、再平衡和路由变化”的状态、顺序、故障与证据链；缺项会使键分布直方图、分区映射、再平衡演练、路由追踪与跨分区报告无法复现。</Answer>

**问题 2**：本页最小运行不变量是什么？

<Answer>
  归属变化期间没有丢失、重复拥有或错误路由，热点与再平衡成本受控。它必须由固定输入、正常历史、压力数据、故障反例和独立业务对账共同证明。
</Answer>

**问题 3**：怎样为“第6章 分区”构造最小反例？

<Answer>
  把“假设哈希天然均匀、自动再平衡天然安全，让热点键或全局索引拖垮集群”写成假设，保持版本、数据和请求不变，只改变延迟、并发、崩溃点或重试之一。
</Answer>

**问题 4**：为什么超时不能直接解释为失败？

<Answer>
  超时只表示观察者没按时收到回复；远端可能未处理、已提交或仍执行。必须用操作标识、幂等规则、提交记录和独立读取判断。
</Answer>

**问题 5**：怎样验证本页性能结论？

<Answer>
  固定数据和请求，重复测量吞吐、错误、P50/P95/P99与资源等待，同时核对语义，防止用丢请求或弱化保证换取数字。
</Answer>

**问题 6**：独立交接至少包含什么？

<Answer>需要版本目录、契约输入、键分布直方图、分区映射、再平衡演练、路由追踪与跨分区报告、正常与故障历史、业务对账、停止条件、恢复和回退责任人。</Answer>
</Exercises>

<Glossary>
  <GlossaryItem term="分区">
    分区在本页指向“按键范围或哈希分散数据与请求，控制热点、二级索引、再平衡和路由变化”中的具体状态和保证；边界由“归属变化期间没有丢失、重复拥有或错误路由，热点与再平衡成本受控”与失败实验共同限定。
  </GlossaryItem>
  <GlossaryItem term="键范围">
    键范围在本页指向“按键范围或哈希分散数据与请求，控制热点、二级索引、再平衡和路由变化”中的具体状态和保证；边界由“归属变化期间没有丢失、重复拥有或错误路由，热点与再平衡成本受控”与失败实验共同限定。
  </GlossaryItem>
  <GlossaryItem term="哈希">
    哈希在本页指向“按键范围或哈希分散数据与请求，控制热点、二级索引、再平衡和路由变化”中的具体状态和保证；边界由“归属变化期间没有丢失、重复拥有或错误路由，热点与再平衡成本受控”与失败实验共同限定。
  </GlossaryItem>
  <GlossaryItem term="热点">
    热点在本页指向“按键范围或哈希分散数据与请求，控制热点、二级索引、再平衡和路由变化”中的具体状态和保证；边界由“归属变化期间没有丢失、重复拥有或错误路由，热点与再平衡成本受控”与失败实验共同限定。
  </GlossaryItem>
  <GlossaryItem term="再平衡">
    再平衡在本页指向“按键范围或哈希分散数据与请求，控制热点、二级索引、再平衡和路由变化”中的具体状态和保证；边界由“归属变化期间没有丢失、重复拥有或错误路由，热点与再平衡成本受控”与失败实验共同限定。
  </GlossaryItem>
</Glossary>

[← 上一页：第5章 复制](/learn/ddia/05-replication/ddi-05-replication) · [下一页：第7章 事务 →](/learn/ddia/07-transactions/ddi-07-transactions)

<Attribution
  adaptedFrom="《数据密集型应用系统设计》第1版"
  adaptedUrl="https://www.oreilly.com/library/view/designing-data-intensive-applications/9781491903063/"
/>
