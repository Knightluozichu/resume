---
title: "第8章 分布式系统的麻烦"
description: "依据2018中文第1版完整目录：承认网络、时钟和进程只能提供不完整信息，用超时、仲裁与隔离令牌管理不确定性"
type: C
section: "数据密集型应用系统设计（第1版 / 2018）"
draft: false
math: false
sourceUrl: "https://www.oreilly.com/library/view/designing-data-intensive-applications/9781491903063/"
demo: true
order: 0
---

import {
  Ddi08TroubleDistributedSystemsArchitectureLab,
  Ddi08TroubleDistributedSystemsFailureLab,
  Ddi08TroubleDistributedSystemsEvidenceLab,
} from "@/components/mdx/ddia/diagrams/ddi-08-trouble-distributed-systems";

# 第8章 分布式系统的麻烦

本课程对应Martin Kleppmann著，赵军平、吕云松、耿煜、李三平译《数据密集型应用系统设计》第1版，中国电力出版社2018年9月出版，纸书519页，ISBN 9787519821968。英文原版由O'Reilly于2017年3月出版，616页，ISBN 9781491903063。

O'Reilly第1版完整目录确认3部分、12章与术语表。英文第2版由Martin Kleppmann与Chris Riccomini合著，于2026年出版并重排目录；当前课程对应2018中文第1版，不把第2版静默混入。课程独立重构核心知识、图示与实验，不复制原文。本页逐项覆盖17个正式章/节节点。

<Objectives>

- 能解释“第8章 分布式系统的麻烦”的17个页面节点，并连接到“承认网络、时钟和进程只能提供不完整信息，用超时、仲裁与隔离令牌管理不确定性”主线。
- 能绘制权威状态、派生状态、消息与故障边界，复现故障时间线、延迟分布、时钟误差、进程暂停实验与隔离验证。
- 能比较正常、压力和部分故障历史，证明“节点不把本地时间、沉默或单方观察误当全局事实，过期参与者不能破坏资源”。
- 能设计反例推翻“把超时等同对方死亡，把墙钟当严格顺序，并让恢复后的旧主继续写资源”，给出停止、恢复和回退条件。

</Objectives>

## 从一个不可判定的用户结果开始

先预测：客户端超时后，这次写入究竟未发生、已提交还是仍可能提交？先写用户不变量、允许陈旧窗口、尾延迟和恢复目标，再选择模型、协议或存储结构。不要先看监控图再编故事；先固定输入和预期，运行后才允许修正。

本页主问题是“承认网络、时钟和进程只能提供不完整信息，用超时、仲裁与隔离令牌管理不确定性”。最终交付不是概念摘要，而是故障时间线、延迟分布、时钟误差、进程暂停实验与隔离验证，由独立复跑证明：**节点不把本地时间、沉默或单方观察误当全局事实，过期参与者不能破坏资源**。

## 核心词汇与运行边界

<Term def="部分失效是“承认网络、时钟和进程只能提供不完整信息，用超时、仲裁与隔离令牌管理不确定性”中的第1个核心概念，必须由状态、顺序、故障与证据定义">部分失效</Term>、<Term def="超时是“承认网络、时钟和进程只能提供不完整信息，用超时、仲裁与隔离令牌管理不确定性”中的第2个核心概念，必须由状态、顺序、故障与证据定义">超时</Term>、<Term def="单调时钟是“承认网络、时钟和进程只能提供不完整信息，用超时、仲裁与隔离令牌管理不确定性”中的第3个核心概念，必须由状态、顺序、故障与证据定义">单调时钟</Term>、<Term def="进程暂停是“承认网络、时钟和进程只能提供不完整信息，用超时、仲裁与隔离令牌管理不确定性”中的第4个核心概念，必须由状态、顺序、故障与证据定义">进程暂停</Term>、<Term def="隔离令牌是“承认网络、时钟和进程只能提供不完整信息，用超时、仲裁与隔离令牌管理不确定性”中的第5个核心概念，必须由状态、顺序、故障与证据定义">隔离令牌</Term>
构成本页共同语言。每个词都必须回答：谁观察、观察哪个状态、顺序依据是什么、部分故障时会看到什么，以及哪条证据能推翻解释。

<Ddi08TroubleDistributedSystemsArchitectureLab />

## 正式目录逐节点重构

### 故障与部分失效

**目录节点 1/17。** 部分组件继续工作时，观察者难以区分慢、丢包和宕机。

把“故障与部分失效”放回“承认网络、时钟和进程只能提供不完整信息，用超时、仲裁与隔离令牌管理不确定性”主线：上游是“用户结果与本章问题”，下游是“云计算与超级计算”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“节点不把本地时间、沉默或单方观察误当全局事实，过期参与者不能破坏资源”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 云计算与超级计算

**目录节点 2/17。** 商品云和受控超算采用不同故障模型与恢复目标。

把“云计算与超级计算”放回“承认网络、时钟和进程只能提供不完整信息，用超时、仲裁与隔离令牌管理不确定性”主线：上游是“故障与部分失效”，下游是“不可靠的网络”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“节点不把本地时间、沉默或单方观察误当全局事实，过期参与者不能破坏资源”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 不可靠的网络

**目录节点 3/17。** 消息可能延迟、丢失、重复和重排。

把“不可靠的网络”放回“承认网络、时钟和进程只能提供不完整信息，用超时、仲裁与隔离令牌管理不确定性”主线：上游是“云计算与超级计算”，下游是“实践中的网络故障”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“节点不把本地时间、沉默或单方观察误当全局事实，过期参与者不能破坏资源”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 实践中的网络故障

**目录节点 4/17。** 链路、队列、路由和配置都会造成局部分区。

把“实践中的网络故障”放回“承认网络、时钟和进程只能提供不完整信息，用超时、仲裁与隔离令牌管理不确定性”主线：上游是“不可靠的网络”，下游是“检测故障”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“节点不把本地时间、沉默或单方观察误当全局事实，过期参与者不能破坏资源”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 检测故障

**目录节点 5/17。** 拒绝、回复和第三方观测只是线索，不是完美事实。

把“检测故障”放回“承认网络、时钟和进程只能提供不完整信息，用超时、仲裁与隔离令牌管理不确定性”主线：上游是“实践中的网络故障”，下游是“超时与无界延迟”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“节点不把本地时间、沉默或单方观察误当全局事实，过期参与者不能破坏资源”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 超时与无界延迟

**目录节点 6/17。** 超时是策略，过短误判，过长拖延恢复。

把“超时与无界延迟”放回“承认网络、时钟和进程只能提供不完整信息，用超时、仲裁与隔离令牌管理不确定性”主线：上游是“检测故障”，下游是“同步网络与异步网络”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“节点不把本地时间、沉默或单方观察误当全局事实，过期参与者不能破坏资源”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 同步网络与异步网络

**目录节点 7/17。** 通用云环境不能依赖固定网络延迟上界。

把“同步网络与异步网络”放回“承认网络、时钟和进程只能提供不完整信息，用超时、仲裁与隔离令牌管理不确定性”主线：上游是“超时与无界延迟”，下游是“不可靠的时钟”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“节点不把本地时间、沉默或单方观察误当全局事实，过期参与者不能破坏资源”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 不可靠的时钟

**目录节点 8/17。** 时钟漂移和同步误差限制全球时间推理。

把“不可靠的时钟”放回“承认网络、时钟和进程只能提供不完整信息，用超时、仲裁与隔离令牌管理不确定性”主线：上游是“同步网络与异步网络”，下游是“单调时钟与时刻时钟”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“节点不把本地时间、沉默或单方观察误当全局事实，过期参与者不能破坏资源”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 单调时钟与时刻时钟

**目录节点 9/17。** 持续时间应使用不会因校时倒退的单调时钟。

把“单调时钟与时刻时钟”放回“承认网络、时钟和进程只能提供不完整信息，用超时、仲裁与隔离令牌管理不确定性”主线：上游是“不可靠的时钟”，下游是“时钟同步与准确性”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“节点不把本地时间、沉默或单方观察误当全局事实，过期参与者不能破坏资源”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 时钟同步与准确性

**目录节点 10/17。** 应用若依赖时间顺序，应传播误差范围。

把“时钟同步与准确性”放回“承认网络、时钟和进程只能提供不完整信息，用超时、仲裁与隔离令牌管理不确定性”主线：上游是“单调时钟与时刻时钟”，下游是“依赖同步时钟”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“节点不把本地时间、沉默或单方观察误当全局事实，过期参与者不能破坏资源”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 依赖同步时钟

**目录节点 11/17。** 最后写入胜利和租约忽略误差会丢写或重叠。

把“依赖同步时钟”放回“承认网络、时钟和进程只能提供不完整信息，用超时、仲裁与隔离令牌管理不确定性”主线：上游是“时钟同步与准确性”，下游是“进程暂停”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“节点不把本地时间、沉默或单方观察误当全局事实，过期参与者不能破坏资源”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 进程暂停

**目录节点 12/17。** GC、抢占和休眠会让进程错过租约却不自知。

把“进程暂停”放回“承认网络、时钟和进程只能提供不完整信息，用超时、仲裁与隔离令牌管理不确定性”主线：上游是“依赖同步时钟”，下游是“知识、真相与谎言”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“节点不把本地时间、沉默或单方观察误当全局事实，过期参与者不能破坏资源”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 知识、真相与谎言

**目录节点 13/17。** 节点知识来自可能过期的消息和本地状态。

把“知识、真相与谎言”放回“承认网络、时钟和进程只能提供不完整信息，用超时、仲裁与隔离令牌管理不确定性”主线：上游是“进程暂停”，下游是“真相由多数定义”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“节点不把本地时间、沉默或单方观察误当全局事实，过期参与者不能破坏资源”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 真相由多数定义

**目录节点 14/17。** 多数只在已声明成员集和协议假设中有效。

把“真相由多数定义”放回“承认网络、时钟和进程只能提供不完整信息，用超时、仲裁与隔离令牌管理不确定性”主线：上游是“知识、真相与谎言”，下游是“拜占庭故障”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“节点不把本地时间、沉默或单方观察误当全局事实，过期参与者不能破坏资源”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 拜占庭故障

**目录节点 15/17。** 任意或矛盾行为需要更强身份、协议与副本数。

把“拜占庭故障”放回“承认网络、时钟和进程只能提供不完整信息，用超时、仲裁与隔离令牌管理不确定性”主线：上游是“真相由多数定义”，下游是“系统模型与现实”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“节点不把本地时间、沉默或单方观察误当全局事实，过期参与者不能破坏资源”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 系统模型与现实

**目录节点 16/17。** 必须写清同步、故障类型、安全性和活性假设。

把“系统模型与现实”放回“承认网络、时钟和进程只能提供不完整信息，用超时、仲裁与隔离令牌管理不确定性”主线：上游是“拜占庭故障”，下游是“小结”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“节点不把本地时间、沉默或单方观察误当全局事实，过期参与者不能破坏资源”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 小结

**目录节点 17/17。** 稳健协议从不确定性出发并限制错误推断后果。

把“小结”放回“承认网络、时钟和进程只能提供不完整信息，用超时、仲裁与隔离令牌管理不确定性”主线：上游是“系统模型与现实”，下游是“本章交付与独立验收”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“节点不把本地时间、沉默或单方观察误当全局事实，过期参与者不能破坏资源”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

## 机制推演：状态、顺序与失败

第一步固定状态所有权。写出哪个数据集是权威事实，哪些只是缓存、索引、仓库或UI等派生视图；再写每次转换的输入版本、输出版本和提交点。没有所有权就无法区分恢复、覆盖和冲突，也无法判断“承认网络、时钟和进程只能提供不完整信息，用超时、仲裁与隔离令牌管理不确定性”中的结果由谁负责。

第二步展开时间线。客户端发送、节点接收、本地持久化、复制或派生、确认和后续读取是不同事件；超时只提供观察者的等待上界，不提供远端提交事实。为每个事件记录逻辑顺序，墙钟仅作诊断。

第三步施加负载与故障。正常路径不能证明队列增长、热点、进程暂停、消息重复、节点崩溃或网络分区时仍满足“节点不把本地时间、沉默或单方观察误当全局事实，过期参与者不能破坏资源”。最小实验一次只改变一个因素，并保留能复盘状态转移的原始历史。

第四步从外部核对。服务自报成功、任务完成或副本在线都属于被测路径内部证据；必须用业务标识、数量、约束、校验和或独立读取核对。最终材料“故障时间线、延迟分布、时钟误差、进程暂停实验与隔离验证”还要包含未覆盖边界与回退。

## 最小可重放实验

```text
book=数据密集型应用系统设计（第1版 / 2018）
unit=ddi-08-trouble-distributed-systems
question=承认网络、时钟和进程只能提供不完整信息，用超时、仲裁与隔离令牌管理不确定性
invariant=节点不把本地时间、沉默或单方观察误当全局事实，过期参与者不能破坏资源
artifact=故障时间线、延迟分布、时钟误差、进程暂停实验与隔离验证
```

```pseudo
given fixed_version, fixed_dataset, fixed_request_history
predict user_result, state_transitions, p99, failure_endpoint
run baseline and capture ordered_events
change exactly one of latency, concurrency, crash_point, retry_policy
assert business_invariant with an independent_reader
```

```yaml
evidence:
  input: schema, distribution, request ids, dependency versions
  execution: ordered events, queue depth, resource percentiles
  failure: injection point, timeout, retry, leader or offset changes
  result: accepted, committed, visible, reconciled, recovered
  decision: pass, reject, rollback, unresolved
```

## 三步交互实验

<Stepper>
  <Step title="1. 固定数据流和目录节点">
    选择正式节点与部署形态，标出请求、权威状态和派生视图。
    <Ddi08TroubleDistributedSystemsArchitectureLab />
  </Step>
  <Step title="2. 注入延迟、并发与分区">
    先预测P99、陈旧窗口、可用性与冲突风险，再改变单一条件。
    <Ddi08TroubleDistributedSystemsFailureLab />
  </Step>
  <Step title="3. 通过独立证据门">
    六类证据齐备后才能宣布机制、性能、恢复或一致性结论成立。
    <Ddi08TroubleDistributedSystemsEvidenceLab />
  </Step>
</Stepper>

<Callout type="trap">
  **语义陷阱。**
  把超时等同对方死亡，把墙钟当严格顺序，并让恢复后的旧主继续写资源。先恢复用户不变量、提交点和故障模型，再比较方案；产品名、节点状态和平均指标不能替代保证。
</Callout>

<Callout type="trap">
  **时间陷阱。**
  客户端时间、服务器日志时间和逻辑提交顺序不同。时钟偏差、进程暂停、排队和重试会制造矛盾记录；先按消息与操作标识建立偏序，再讨论因果。
</Callout>

<Callout type="warning">
  崩溃、分区、数据回放和并发写实验只在隔离环境执行。规定最大数据量、错误预算、停止条件、清理方式和恢复负责人。
</Callout>

## 证据解释与独立交接

契约证据说明什么必须成立：记录业务不变量、请求身份、成功定义、允许陈旧度、顺序模型和SLO。任何模糊词都改写成可观察历史，例如高可用要说明分区哪侧可写，一致要说明允许哪些并发历史。

机制证据说明为什么可能成立：保存数据模型、分区与副本映射、事务或日志位置、消息顺序、版本和配置。只截最终仪表盘会丢失中间状态，无法区分修复、掩盖和错误重试。

负载证据说明在什么规模成立：记录请求分布、热点、并发、数据量、缓存状态、P50/P95/P99、吞吐和资源等待。平均值会隐藏最慢分区、锁队列、压实、检查点与网络抖动。

故障证据说明边界在哪里：在确认、持久化、复制或派生前后分别注入故障，观察重试、选举、回放与补偿。最后用外部读者对账，确认没有丢失、重复、顺序颠倒或约束破坏。

## 本章回顾

重新完成“承认网络、时钟和进程只能提供不完整信息，用超时、仲裁与隔离令牌管理不确定性”：固定版本和目录，写出用户不变量与权威状态，展开消息时间线，运行基线，再以单一故障寻找反例。最终交付故障时间线、延迟分布、时钟误差、进程暂停实验与隔离验证，证明“节点不把本地时间、沉默或单方观察误当全局事实，过期参与者不能破坏资源”，并保留未解决风险。

## 复习与生产验收

<Exercises>
**问题 1**：为什么“第8章 分布式系统的麻烦”必须覆盖17个页面节点？
<Answer>这些节点组成“承认网络、时钟和进程只能提供不完整信息，用超时、仲裁与隔离令牌管理不确定性”的状态、顺序、故障与证据链；缺项会使故障时间线、延迟分布、时钟误差、进程暂停实验与隔离验证无法复现。</Answer>

**问题 2**：本页最小运行不变量是什么？

<Answer>
  节点不把本地时间、沉默或单方观察误当全局事实，过期参与者不能破坏资源。它必须由固定输入、正常历史、压力数据、故障反例和独立业务对账共同证明。
</Answer>

**问题 3**：怎样为“第8章 分布式系统的麻烦”构造最小反例？

<Answer>
  把“把超时等同对方死亡，把墙钟当严格顺序，并让恢复后的旧主继续写资源”写成假设，保持版本、数据和请求不变，只改变延迟、并发、崩溃点或重试之一。
</Answer>

**问题 4**：为什么超时不能直接解释为失败？

<Answer>
  超时只表示观察者没按时收到回复；远端可能未处理、已提交或仍执行。必须用操作标识、幂等规则、提交记录和独立读取判断。
</Answer>

**问题 5**：怎样验证本页性能结论？

<Answer>
  固定数据和请求，重复测量吞吐、错误、P50/P95/P99与资源等待，同时核对语义，防止用丢请求或弱化保证换取数字。
</Answer>

**问题 6**：独立交接至少包含什么？

<Answer>需要版本目录、契约输入、故障时间线、延迟分布、时钟误差、进程暂停实验与隔离验证、正常与故障历史、业务对账、停止条件、恢复和回退责任人。</Answer>
</Exercises>

<Glossary>
  <GlossaryItem term="部分失效">
    部分失效在本页指向“承认网络、时钟和进程只能提供不完整信息，用超时、仲裁与隔离令牌管理不确定性”中的具体状态和保证；边界由“节点不把本地时间、沉默或单方观察误当全局事实，过期参与者不能破坏资源”与失败实验共同限定。
  </GlossaryItem>
  <GlossaryItem term="超时">
    超时在本页指向“承认网络、时钟和进程只能提供不完整信息，用超时、仲裁与隔离令牌管理不确定性”中的具体状态和保证；边界由“节点不把本地时间、沉默或单方观察误当全局事实，过期参与者不能破坏资源”与失败实验共同限定。
  </GlossaryItem>
  <GlossaryItem term="单调时钟">
    单调时钟在本页指向“承认网络、时钟和进程只能提供不完整信息，用超时、仲裁与隔离令牌管理不确定性”中的具体状态和保证；边界由“节点不把本地时间、沉默或单方观察误当全局事实，过期参与者不能破坏资源”与失败实验共同限定。
  </GlossaryItem>
  <GlossaryItem term="进程暂停">
    进程暂停在本页指向“承认网络、时钟和进程只能提供不完整信息，用超时、仲裁与隔离令牌管理不确定性”中的具体状态和保证；边界由“节点不把本地时间、沉默或单方观察误当全局事实，过期参与者不能破坏资源”与失败实验共同限定。
  </GlossaryItem>
  <GlossaryItem term="隔离令牌">
    隔离令牌在本页指向“承认网络、时钟和进程只能提供不完整信息，用超时、仲裁与隔离令牌管理不确定性”中的具体状态和保证；边界由“节点不把本地时间、沉默或单方观察误当全局事实，过期参与者不能破坏资源”与失败实验共同限定。
  </GlossaryItem>
</Glossary>

[← 上一页：第7章 事务](/learn/ddia/07-transactions/ddi-07-transactions) · [下一页：第9章 一致性与共识 →](/learn/ddia/09-consistency-consensus/ddi-09-consistency-consensus)

<Attribution
  adaptedFrom="《数据密集型应用系统设计》第1版"
  adaptedUrl="https://www.oreilly.com/library/view/designing-data-intensive-applications/9781491903063/"
/>
