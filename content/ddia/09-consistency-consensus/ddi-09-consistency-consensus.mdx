---
title: "第9章 一致性与共识"
description: "依据2018中文第1版完整目录：用线性一致、因果与全序广播描述可见顺序，再理解原子提交、共识与成员协调"
type: C
section: "数据密集型应用系统设计（第1版 / 2018）"
draft: false
math: false
sourceUrl: "https://www.oreilly.com/library/view/designing-data-intensive-applications/9781491903063/"
demo: true
order: 0
---

import {
  Ddi09ConsistencyConsensusArchitectureLab,
  Ddi09ConsistencyConsensusFailureLab,
  Ddi09ConsistencyConsensusEvidenceLab,
} from "@/components/mdx/ddia/diagrams/ddi-09-consistency-consensus";

# 第9章 一致性与共识

本课程对应Martin Kleppmann著，赵军平、吕云松、耿煜、李三平译《数据密集型应用系统设计》第1版，中国电力出版社2018年9月出版，纸书519页，ISBN 9787519821968。英文原版由O'Reilly于2017年3月出版，616页，ISBN 9781491903063。

O'Reilly第1版完整目录确认3部分、12章与术语表。英文第2版由Martin Kleppmann与Chris Riccomini合著，于2026年出版并重排目录；当前课程对应2018中文第1版，不把第2版静默混入。课程独立重构核心知识、图示与实验，不复制原文。本页逐项覆盖16个正式章/节节点。

<Objectives>

- 能解释“第9章 一致性与共识”的16个页面节点，并连接到“用线性一致、因果与全序广播描述可见顺序，再理解原子提交、共识与成员协调”主线。
- 能绘制权威状态、派生状态、消息与故障边界，复现历史检查、因果图、共识日志、成员变更与提交恢复记录。
- 能比较正常、压力和部分故障历史，证明“决定满足顺序与唯一性，任期和提交证据可追溯，少数故障不产生两个已提交事实”。
- 能设计反例推翻“把副本收敛等同线性一致，把两阶段提交等同容错共识，只测稳定领导者”，给出停止、恢复和回退条件。

</Objectives>

## 从一个不可判定的用户结果开始

先预测：客户端超时后，这次写入究竟未发生、已提交还是仍可能提交？先写用户不变量、允许陈旧窗口、尾延迟和恢复目标，再选择模型、协议或存储结构。不要先看监控图再编故事；先固定输入和预期，运行后才允许修正。

本页主问题是“用线性一致、因果与全序广播描述可见顺序，再理解原子提交、共识与成员协调”。最终交付不是概念摘要，而是历史检查、因果图、共识日志、成员变更与提交恢复记录，由独立复跑证明：**决定满足顺序与唯一性，任期和提交证据可追溯，少数故障不产生两个已提交事实**。

## 核心词汇与运行边界

<Term def="线性一致性是“用线性一致、因果与全序广播描述可见顺序，再理解原子提交、共识与成员协调”中的第1个核心概念，必须由状态、顺序、故障与证据定义">
  线性一致性
</Term>
、
<Term def="因果关系是“用线性一致、因果与全序广播描述可见顺序，再理解原子提交、共识与成员协调”中的第2个核心概念，必须由状态、顺序、故障与证据定义">
  因果关系
</Term>
、
<Term def="全序广播是“用线性一致、因果与全序广播描述可见顺序，再理解原子提交、共识与成员协调”中的第3个核心概念，必须由状态、顺序、故障与证据定义">
  全序广播
</Term>
、
<Term def="两阶段提交是“用线性一致、因果与全序广播描述可见顺序，再理解原子提交、共识与成员协调”中的第4个核心概念，必须由状态、顺序、故障与证据定义">
  两阶段提交
</Term>
、
<Term def="共识是“用线性一致、因果与全序广播描述可见顺序，再理解原子提交、共识与成员协调”中的第5个核心概念，必须由状态、顺序、故障与证据定义">
  共识
</Term>
构成本页共同语言。每个词都必须回答：谁观察、观察哪个状态、顺序依据是什么、部分故障时会看到什么，以及哪条证据能推翻解释。

<Ddi09ConsistencyConsensusArchitectureLab />

## 正式目录逐节点重构

### 一致性保证

**目录节点 1/16。** 模型必须说明读取允许观察哪些写入和顺序。

把“一致性保证”放回“用线性一致、因果与全序广播描述可见顺序，再理解原子提交、共识与成员协调”主线：上游是“用户结果与本章问题”，下游是“线性一致性”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“决定满足顺序与唯一性，任期和提交证据可追溯，少数故障不产生两个已提交事实”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 线性一致性

**目录节点 2/16。** 操作在调用与返回之间某点原子生效并尊重实时先后。

把“线性一致性”放回“用线性一致、因果与全序广播描述可见顺序，再理解原子提交、共识与成员协调”主线：上游是“一致性保证”，下游是“什么使系统线性一致”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“决定满足顺序与唯一性，任期和提交证据可追溯，少数故障不产生两个已提交事实”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 什么使系统线性一致

**目录节点 3/16。** 应通过并发历史寻找合法线性化顺序。

把“什么使系统线性一致”放回“用线性一致、因果与全序广播描述可见顺序，再理解原子提交、共识与成员协调”主线：上游是“线性一致性”，下游是“依赖线性一致性”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“决定满足顺序与唯一性，任期和提交证据可追溯，少数故障不产生两个已提交事实”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 依赖线性一致性

**目录节点 4/16。** 选主、唯一约束和锁依赖单一最新事实。

把“依赖线性一致性”放回“用线性一致、因果与全序广播描述可见顺序，再理解原子提交、共识与成员协调”主线：上游是“什么使系统线性一致”，下游是“实现线性一致系统”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“决定满足顺序与唯一性，任期和提交证据可追溯，少数故障不产生两个已提交事实”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 实现线性一致系统

**目录节点 5/16。** 单主、共识复制或严格法定人数都有协调成本。

把“实现线性一致系统”放回“用线性一致、因果与全序广播描述可见顺序，再理解原子提交、共识与成员协调”主线：上游是“依赖线性一致性”，下游是“线性一致性的代价”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“决定满足顺序与唯一性，任期和提交证据可追溯，少数故障不产生两个已提交事实”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 线性一致性的代价

**目录节点 6/16。** 网络分区时保护单一顺序的一侧必须拒绝操作。

把“线性一致性的代价”放回“用线性一致、因果与全序广播描述可见顺序，再理解原子提交、共识与成员协调”主线：上游是“实现线性一致系统”，下游是“顺序保证”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“决定满足顺序与唯一性，任期和提交证据可追溯，少数故障不产生两个已提交事实”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 顺序保证

**目录节点 7/16。** 每键、因果和全局顺序的协调范围不同。

把“顺序保证”放回“用线性一致、因果与全序广播描述可见顺序，再理解原子提交、共识与成员协调”主线：上游是“线性一致性的代价”，下游是“顺序与因果关系”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“决定满足顺序与唯一性，任期和提交证据可追溯，少数故障不产生两个已提交事实”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 顺序与因果关系

**目录节点 8/16。** 有依赖的事件需保序，并发事件不必强加实时顺序。

把“顺序与因果关系”放回“用线性一致、因果与全序广播描述可见顺序，再理解原子提交、共识与成员协调”主线：上游是“顺序保证”，下游是“序列号排序”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“决定满足顺序与唯一性，任期和提交证据可追溯，少数故障不产生两个已提交事实”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 序列号排序

**目录节点 9/16。** 逻辑编号表达先后，但编号本身不证明提交。

把“序列号排序”放回“用线性一致、因果与全序广播描述可见顺序，再理解原子提交、共识与成员协调”主线：上游是“顺序与因果关系”，下游是“全序广播”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“决定满足顺序与唯一性，任期和提交证据可追溯，少数故障不产生两个已提交事实”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 全序广播

**目录节点 10/16。** 正确节点以相同顺序交付不重复、不凭空的消息。

把“全序广播”放回“用线性一致、因果与全序广播描述可见顺序，再理解原子提交、共识与成员协调”主线：上游是“序列号排序”，下游是“分布式事务与共识”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“决定满足顺序与唯一性，任期和提交证据可追溯，少数故障不产生两个已提交事实”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 分布式事务与共识

**目录节点 11/16。** 原子决定和领导者日志相关，却有不同恢复边界。

把“分布式事务与共识”放回“用线性一致、因果与全序广播描述可见顺序，再理解原子提交、共识与成员协调”主线：上游是“全序广播”，下游是“原子提交与两阶段提交”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“决定满足顺序与唯一性，任期和提交证据可追溯，少数故障不产生两个已提交事实”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 原子提交与两阶段提交

**目录节点 12/16。** 参与者准备后可能因协调者故障而阻塞。

把“原子提交与两阶段提交”放回“用线性一致、因果与全序广播描述可见顺序，再理解原子提交、共识与成员协调”主线：上游是“分布式事务与共识”，下游是“实践中的分布式事务”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“决定满足顺序与唯一性，任期和提交证据可追溯，少数故障不产生两个已提交事实”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 实践中的分布式事务

**目录节点 13/16。** XA扩大故障边界，业务补偿是另一种选择。

把“实践中的分布式事务”放回“用线性一致、因果与全序广播描述可见顺序，再理解原子提交、共识与成员协调”主线：上游是“原子提交与两阶段提交”，下游是“容错共识”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“决定满足顺序与唯一性，任期和提交证据可追溯，少数故障不产生两个已提交事实”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 容错共识

**目录节点 14/16。** 任期和多数交集阻止两个冲突值同时提交。

把“容错共识”放回“用线性一致、因果与全序广播描述可见顺序，再理解原子提交、共识与成员协调”主线：上游是“实践中的分布式事务”，下游是“成员与协调服务”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“决定满足顺序与唯一性，任期和提交证据可追溯，少数故障不产生两个已提交事实”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 成员与协调服务

**目录节点 15/16。** 租约和通知仍需处理会话过期与隔离令牌。

把“成员与协调服务”放回“用线性一致、因果与全序广播描述可见顺序，再理解原子提交、共识与成员协调”主线：上游是“容错共识”，下游是“小结”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“决定满足顺序与唯一性，任期和提交证据可追溯，少数故障不产生两个已提交事实”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 小结

**目录节点 16/16。** 按应用不变量选择顺序模型并以故障历史证明。

把“小结”放回“用线性一致、因果与全序广播描述可见顺序，再理解原子提交、共识与成员协调”主线：上游是“成员与协调服务”，下游是“本章交付与独立验收”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“决定满足顺序与唯一性，任期和提交证据可追溯，少数故障不产生两个已提交事实”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

## 机制推演：状态、顺序与失败

第一步固定状态所有权。写出哪个数据集是权威事实，哪些只是缓存、索引、仓库或UI等派生视图；再写每次转换的输入版本、输出版本和提交点。没有所有权就无法区分恢复、覆盖和冲突，也无法判断“用线性一致、因果与全序广播描述可见顺序，再理解原子提交、共识与成员协调”中的结果由谁负责。

第二步展开时间线。客户端发送、节点接收、本地持久化、复制或派生、确认和后续读取是不同事件；超时只提供观察者的等待上界，不提供远端提交事实。为每个事件记录逻辑顺序，墙钟仅作诊断。

第三步施加负载与故障。正常路径不能证明队列增长、热点、进程暂停、消息重复、节点崩溃或网络分区时仍满足“决定满足顺序与唯一性，任期和提交证据可追溯，少数故障不产生两个已提交事实”。最小实验一次只改变一个因素，并保留能复盘状态转移的原始历史。

第四步从外部核对。服务自报成功、任务完成或副本在线都属于被测路径内部证据；必须用业务标识、数量、约束、校验和或独立读取核对。最终材料“历史检查、因果图、共识日志、成员变更与提交恢复记录”还要包含未覆盖边界与回退。

## 最小可重放实验

```text
book=数据密集型应用系统设计（第1版 / 2018）
unit=ddi-09-consistency-consensus
question=用线性一致、因果与全序广播描述可见顺序，再理解原子提交、共识与成员协调
invariant=决定满足顺序与唯一性，任期和提交证据可追溯，少数故障不产生两个已提交事实
artifact=历史检查、因果图、共识日志、成员变更与提交恢复记录
```

```pseudo
given fixed_version, fixed_dataset, fixed_request_history
predict user_result, state_transitions, p99, failure_endpoint
run baseline and capture ordered_events
change exactly one of latency, concurrency, crash_point, retry_policy
assert business_invariant with an independent_reader
```

```yaml
evidence:
  input: schema, distribution, request ids, dependency versions
  execution: ordered events, queue depth, resource percentiles
  failure: injection point, timeout, retry, leader or offset changes
  result: accepted, committed, visible, reconciled, recovered
  decision: pass, reject, rollback, unresolved
```

## 三步交互实验

<Stepper>
  <Step title="1. 固定数据流和目录节点">
    选择正式节点与部署形态，标出请求、权威状态和派生视图。
    <Ddi09ConsistencyConsensusArchitectureLab />
  </Step>
  <Step title="2. 注入延迟、并发与分区">
    先预测P99、陈旧窗口、可用性与冲突风险，再改变单一条件。
    <Ddi09ConsistencyConsensusFailureLab />
  </Step>
  <Step title="3. 通过独立证据门">
    六类证据齐备后才能宣布机制、性能、恢复或一致性结论成立。
    <Ddi09ConsistencyConsensusEvidenceLab />
  </Step>
</Stepper>

<Callout type="trap">
  **语义陷阱。**
  把副本收敛等同线性一致，把两阶段提交等同容错共识，只测稳定领导者。先恢复用户不变量、提交点和故障模型，再比较方案；产品名、节点状态和平均指标不能替代保证。
</Callout>

<Callout type="trap">
  **时间陷阱。**
  客户端时间、服务器日志时间和逻辑提交顺序不同。时钟偏差、进程暂停、排队和重试会制造矛盾记录；先按消息与操作标识建立偏序，再讨论因果。
</Callout>

<Callout type="warning">
  崩溃、分区、数据回放和并发写实验只在隔离环境执行。规定最大数据量、错误预算、停止条件、清理方式和恢复负责人。
</Callout>

## 证据解释与独立交接

契约证据说明什么必须成立：记录业务不变量、请求身份、成功定义、允许陈旧度、顺序模型和SLO。任何模糊词都改写成可观察历史，例如高可用要说明分区哪侧可写，一致要说明允许哪些并发历史。

机制证据说明为什么可能成立：保存数据模型、分区与副本映射、事务或日志位置、消息顺序、版本和配置。只截最终仪表盘会丢失中间状态，无法区分修复、掩盖和错误重试。

负载证据说明在什么规模成立：记录请求分布、热点、并发、数据量、缓存状态、P50/P95/P99、吞吐和资源等待。平均值会隐藏最慢分区、锁队列、压实、检查点与网络抖动。

故障证据说明边界在哪里：在确认、持久化、复制或派生前后分别注入故障，观察重试、选举、回放与补偿。最后用外部读者对账，确认没有丢失、重复、顺序颠倒或约束破坏。

## 本章回顾

重新完成“用线性一致、因果与全序广播描述可见顺序，再理解原子提交、共识与成员协调”：固定版本和目录，写出用户不变量与权威状态，展开消息时间线，运行基线，再以单一故障寻找反例。最终交付历史检查、因果图、共识日志、成员变更与提交恢复记录，证明“决定满足顺序与唯一性，任期和提交证据可追溯，少数故障不产生两个已提交事实”，并保留未解决风险。

## 复习与生产验收

<Exercises>
**问题 1**：为什么“第9章 一致性与共识”必须覆盖16个页面节点？
<Answer>这些节点组成“用线性一致、因果与全序广播描述可见顺序，再理解原子提交、共识与成员协调”的状态、顺序、故障与证据链；缺项会使历史检查、因果图、共识日志、成员变更与提交恢复记录无法复现。</Answer>

**问题 2**：本页最小运行不变量是什么？

<Answer>
  决定满足顺序与唯一性，任期和提交证据可追溯，少数故障不产生两个已提交事实。它必须由固定输入、正常历史、压力数据、故障反例和独立业务对账共同证明。
</Answer>

**问题 3**：怎样为“第9章 一致性与共识”构造最小反例？

<Answer>
  把“把副本收敛等同线性一致，把两阶段提交等同容错共识，只测稳定领导者”写成假设，保持版本、数据和请求不变，只改变延迟、并发、崩溃点或重试之一。
</Answer>

**问题 4**：为什么超时不能直接解释为失败？

<Answer>
  超时只表示观察者没按时收到回复；远端可能未处理、已提交或仍执行。必须用操作标识、幂等规则、提交记录和独立读取判断。
</Answer>

**问题 5**：怎样验证本页性能结论？

<Answer>
  固定数据和请求，重复测量吞吐、错误、P50/P95/P99与资源等待，同时核对语义，防止用丢请求或弱化保证换取数字。
</Answer>

**问题 6**：独立交接至少包含什么？

<Answer>需要版本目录、契约输入、历史检查、因果图、共识日志、成员变更与提交恢复记录、正常与故障历史、业务对账、停止条件、恢复和回退责任人。</Answer>
</Exercises>

<Glossary>
  <GlossaryItem term="线性一致性">
    线性一致性在本页指向“用线性一致、因果与全序广播描述可见顺序，再理解原子提交、共识与成员协调”中的具体状态和保证；边界由“决定满足顺序与唯一性，任期和提交证据可追溯，少数故障不产生两个已提交事实”与失败实验共同限定。
  </GlossaryItem>
  <GlossaryItem term="因果关系">
    因果关系在本页指向“用线性一致、因果与全序广播描述可见顺序，再理解原子提交、共识与成员协调”中的具体状态和保证；边界由“决定满足顺序与唯一性，任期和提交证据可追溯，少数故障不产生两个已提交事实”与失败实验共同限定。
  </GlossaryItem>
  <GlossaryItem term="全序广播">
    全序广播在本页指向“用线性一致、因果与全序广播描述可见顺序，再理解原子提交、共识与成员协调”中的具体状态和保证；边界由“决定满足顺序与唯一性，任期和提交证据可追溯，少数故障不产生两个已提交事实”与失败实验共同限定。
  </GlossaryItem>
  <GlossaryItem term="两阶段提交">
    两阶段提交在本页指向“用线性一致、因果与全序广播描述可见顺序，再理解原子提交、共识与成员协调”中的具体状态和保证；边界由“决定满足顺序与唯一性，任期和提交证据可追溯，少数故障不产生两个已提交事实”与失败实验共同限定。
  </GlossaryItem>
  <GlossaryItem term="共识">
    共识在本页指向“用线性一致、因果与全序广播描述可见顺序，再理解原子提交、共识与成员协调”中的具体状态和保证；边界由“决定满足顺序与唯一性，任期和提交证据可追溯，少数故障不产生两个已提交事实”与失败实验共同限定。
  </GlossaryItem>
</Glossary>

[← 上一页：第8章 分布式系统的麻烦](/learn/ddia/08-trouble-distributed-systems/ddi-08-trouble-distributed-systems) · [下一页：第10章 批处理 →](/learn/ddia/10-batch-processing/ddi-10-batch-processing)

<Attribution
  adaptedFrom="《数据密集型应用系统设计》第1版"
  adaptedUrl="https://www.oreilly.com/library/view/designing-data-intensive-applications/9781491903063/"
/>
