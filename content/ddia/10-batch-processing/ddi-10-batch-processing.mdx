---
title: "第10章 批处理"
description: "依据2018中文第1版完整目录：把不可变输入转为可重跑的派生输出，从Unix管道走向MapReduce、连接和数据流"
type: C
section: "数据密集型应用系统设计（第1版 / 2018）"
draft: false
math: false
sourceUrl: "https://www.oreilly.com/library/view/designing-data-intensive-applications/9781491903063/"
demo: true
order: 0
---

import {
  Ddi10BatchProcessingArchitectureLab,
  Ddi10BatchProcessingFailureLab,
  Ddi10BatchProcessingEvidenceLab,
} from "@/components/mdx/ddia/diagrams/ddi-10-batch-processing";

# 第10章 批处理

本课程对应Martin Kleppmann著，赵军平、吕云松、耿煜、李三平译《数据密集型应用系统设计》第1版，中国电力出版社2018年9月出版，纸书519页，ISBN 9787519821968。英文原版由O'Reilly于2017年3月出版，616页，ISBN 9781491903063。

O'Reilly第1版完整目录确认3部分、12章与术语表。英文第2版由Martin Kleppmann与Chris Riccomini合著，于2026年出版并重排目录；当前课程对应2018中文第1版，不把第2版静默混入。课程独立重构核心知识、图示与实验，不复制原文。本页逐项覆盖14个正式章/节节点。

<Objectives>

- 能解释“第10章 批处理”的14个页面节点，并连接到“把不可变输入转为可重跑的派生输出，从Unix管道走向MapReduce、连接和数据流”主线。
- 能绘制权威状态、派生状态、消息与故障边界，复现输入快照、批处理DAG、连接计划、重跑对账和输出发布记录。
- 能比较正常、压力和部分故障历史，证明“相同版本输入与代码可重建结果，失败任务不污染最终输出，来源和完成边界可审计”。
- 能设计反例推翻“直接修改线上派生状态，把任务成功当结果正确，忽略倾斜键和部分输出”，给出停止、恢复和回退条件。

</Objectives>

## 从一个不可判定的用户结果开始

先预测：客户端超时后，这次写入究竟未发生、已提交还是仍可能提交？先写用户不变量、允许陈旧窗口、尾延迟和恢复目标，再选择模型、协议或存储结构。不要先看监控图再编故事；先固定输入和预期，运行后才允许修正。

本页主问题是“把不可变输入转为可重跑的派生输出，从Unix管道走向MapReduce、连接和数据流”。最终交付不是概念摘要，而是输入快照、批处理DAG、连接计划、重跑对账和输出发布记录，由独立复跑证明：**相同版本输入与代码可重建结果，失败任务不污染最终输出，来源和完成边界可审计**。

## 核心词汇与运行边界

<Term def="不可变输入是“把不可变输入转为可重跑的派生输出，从Unix管道走向MapReduce、连接和数据流”中的第1个核心概念，必须由状态、顺序、故障与证据定义">
  不可变输入
</Term>
、
<Term def="MapReduce是“把不可变输入转为可重跑的派生输出，从Unix管道走向MapReduce、连接和数据流”中的第2个核心概念，必须由状态、顺序、故障与证据定义">
  MapReduce
</Term>
、
<Term def="Shuffle是“把不可变输入转为可重跑的派生输出，从Unix管道走向MapReduce、连接和数据流”中的第3个核心概念，必须由状态、顺序、故障与证据定义">
  Shuffle
</Term>
、
<Term def="数据流是“把不可变输入转为可重跑的派生输出，从Unix管道走向MapReduce、连接和数据流”中的第4个核心概念，必须由状态、顺序、故障与证据定义">
  数据流
</Term>
、
<Term def="物化是“把不可变输入转为可重跑的派生输出，从Unix管道走向MapReduce、连接和数据流”中的第5个核心概念，必须由状态、顺序、故障与证据定义">
  物化
</Term>
构成本页共同语言。每个词都必须回答：谁观察、观察哪个状态、顺序依据是什么、部分故障时会看到什么，以及哪条证据能推翻解释。

<Ddi10BatchProcessingArchitectureLab />

## 正式目录逐节点重构

### 使用Unix工具进行批处理

**目录节点 1/14。** 文件和标准流让小工具可观察、可替换地组合。

把“使用Unix工具进行批处理”放回“把不可变输入转为可重跑的派生输出，从Unix管道走向MapReduce、连接和数据流”主线：上游是“用户结果与本章问题”，下游是“简单日志分析”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“相同版本输入与代码可重建结果，失败任务不污染最终输出，来源和完成边界可审计”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 简单日志分析

**目录节点 2/14。** 固定输入、确定变换和独立输出构成可重放基线。

把“简单日志分析”放回“把不可变输入转为可重跑的派生输出，从Unix管道走向MapReduce、连接和数据流”主线：上游是“使用Unix工具进行批处理”，下游是“Unix哲学”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“相同版本输入与代码可重建结果，失败任务不污染最终输出，来源和完成边界可审计”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### Unix哲学

**目录节点 3/14。** 单一职责和文本接口降低耦合但受单机和弱类型限制。

把“Unix哲学”放回“把不可变输入转为可重跑的派生输出，从Unix管道走向MapReduce、连接和数据流”主线：上游是“简单日志分析”，下游是“MapReduce与分布式文件系统”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“相同版本输入与代码可重建结果，失败任务不污染最终输出，来源和完成边界可审计”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### MapReduce与分布式文件系统

**目录节点 4/14。** Map、Shuffle与Reduce围绕数据局部性和分区执行。

把“MapReduce与分布式文件系统”放回“把不可变输入转为可重跑的派生输出，从Unix管道走向MapReduce、连接和数据流”主线：上游是“Unix哲学”，下游是“MapReduce作业执行”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“相同版本输入与代码可重建结果，失败任务不污染最终输出，来源和完成边界可审计”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### MapReduce作业执行

**目录节点 5/14。** 任务重试与原子输出避免半完成结果发布。

把“MapReduce作业执行”放回“把不可变输入转为可重跑的派生输出，从Unix管道走向MapReduce、连接和数据流”主线：上游是“MapReduce与分布式文件系统”，下游是“Reduce端连接与分组”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“相同版本输入与代码可重建结果，失败任务不污染最终输出，来源和完成边界可审计”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### Reduce端连接与分组

**目录节点 6/14。** 通用连接需要网络Shuffle并受热点键拖累。

把“Reduce端连接与分组”放回“把不可变输入转为可重跑的派生输出，从Unix管道走向MapReduce、连接和数据流”主线：上游是“MapReduce作业执行”，下游是“Map端连接”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“相同版本输入与代码可重建结果，失败任务不污染最终输出，来源和完成边界可审计”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### Map端连接

**目录节点 7/14。** 兼容分区或小表广播可减少Shuffle但提高前置要求。

把“Map端连接”放回“把不可变输入转为可重跑的派生输出，从Unix管道走向MapReduce、连接和数据流”主线：上游是“Reduce端连接与分组”，下游是“批处理工作流的输出”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“相同版本输入与代码可重建结果，失败任务不污染最终输出，来源和完成边界可审计”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 批处理工作流的输出

**目录节点 8/14。** 版本化输出和切换指针便于对比与回滚。

把“批处理工作流的输出”放回“把不可变输入转为可重跑的派生输出，从Unix管道走向MapReduce、连接和数据流”主线：上游是“Map端连接”，下游是“比较Hadoop与分布式数据库”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“相同版本输入与代码可重建结果，失败任务不污染最终输出，来源和完成边界可审计”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 比较Hadoop与分布式数据库

**目录节点 9/14。** 高吞吐扫描和低延迟索引服务不同工作负载。

把“比较Hadoop与分布式数据库”放回“把不可变输入转为可重跑的派生输出，从Unix管道走向MapReduce、连接和数据流”主线：上游是“批处理工作流的输出”，下游是“超越MapReduce”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“相同版本输入与代码可重建结果，失败任务不污染最终输出，来源和完成边界可审计”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 超越MapReduce

**目录节点 10/14。** DAG引擎整体优化算子并减少不必要写盘。

把“超越MapReduce”放回“把不可变输入转为可重跑的派生输出，从Unix管道走向MapReduce、连接和数据流”主线：上游是“比较Hadoop与分布式数据库”，下游是“中间状态的物化”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“相同版本输入与代码可重建结果，失败任务不污染最终输出，来源和完成边界可审计”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 中间状态的物化

**目录节点 11/14。** 物化提供故障边界，流水线减少I/O但扩大重算。

把“中间状态的物化”放回“把不可变输入转为可重跑的派生输出，从Unix管道走向MapReduce、连接和数据流”主线：上游是“超越MapReduce”，下游是“图与迭代处理”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“相同版本输入与代码可重建结果，失败任务不污染最终输出，来源和完成边界可审计”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 图与迭代处理

**目录节点 12/14。** 反复传播状态需要增量或图专用调度。

把“图与迭代处理”放回“把不可变输入转为可重跑的派生输出，从Unix管道走向MapReduce、连接和数据流”主线：上游是“中间状态的物化”，下游是“高级API与语言”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“相同版本输入与代码可重建结果，失败任务不污染最终输出，来源和完成边界可审计”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 高级API与语言

**目录节点 13/14。** 声明式算子让优化器选择连接和执行计划。

把“高级API与语言”放回“把不可变输入转为可重跑的派生输出，从Unix管道走向MapReduce、连接和数据流”主线：上游是“图与迭代处理”，下游是“小结”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“相同版本输入与代码可重建结果，失败任务不污染最终输出，来源和完成边界可审计”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 小结

**目录节点 14/14。** 吞吐优化不能牺牲输入版本、原子输出和对账。

把“小结”放回“把不可变输入转为可重跑的派生输出，从Unix管道走向MapReduce、连接和数据流”主线：上游是“高级API与语言”，下游是“本章交付与独立验收”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“相同版本输入与代码可重建结果，失败任务不污染最终输出，来源和完成边界可审计”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

## 机制推演：状态、顺序与失败

第一步固定状态所有权。写出哪个数据集是权威事实，哪些只是缓存、索引、仓库或UI等派生视图；再写每次转换的输入版本、输出版本和提交点。没有所有权就无法区分恢复、覆盖和冲突，也无法判断“把不可变输入转为可重跑的派生输出，从Unix管道走向MapReduce、连接和数据流”中的结果由谁负责。

第二步展开时间线。客户端发送、节点接收、本地持久化、复制或派生、确认和后续读取是不同事件；超时只提供观察者的等待上界，不提供远端提交事实。为每个事件记录逻辑顺序，墙钟仅作诊断。

第三步施加负载与故障。正常路径不能证明队列增长、热点、进程暂停、消息重复、节点崩溃或网络分区时仍满足“相同版本输入与代码可重建结果，失败任务不污染最终输出，来源和完成边界可审计”。最小实验一次只改变一个因素，并保留能复盘状态转移的原始历史。

第四步从外部核对。服务自报成功、任务完成或副本在线都属于被测路径内部证据；必须用业务标识、数量、约束、校验和或独立读取核对。最终材料“输入快照、批处理DAG、连接计划、重跑对账和输出发布记录”还要包含未覆盖边界与回退。

## 最小可重放实验

```text
book=数据密集型应用系统设计（第1版 / 2018）
unit=ddi-10-batch-processing
question=把不可变输入转为可重跑的派生输出，从Unix管道走向MapReduce、连接和数据流
invariant=相同版本输入与代码可重建结果，失败任务不污染最终输出，来源和完成边界可审计
artifact=输入快照、批处理DAG、连接计划、重跑对账和输出发布记录
```

```pseudo
given fixed_version, fixed_dataset, fixed_request_history
predict user_result, state_transitions, p99, failure_endpoint
run baseline and capture ordered_events
change exactly one of latency, concurrency, crash_point, retry_policy
assert business_invariant with an independent_reader
```

```yaml
evidence:
  input: schema, distribution, request ids, dependency versions
  execution: ordered events, queue depth, resource percentiles
  failure: injection point, timeout, retry, leader or offset changes
  result: accepted, committed, visible, reconciled, recovered
  decision: pass, reject, rollback, unresolved
```

## 三步交互实验

<Stepper>
  <Step title="1. 固定数据流和目录节点">
    选择正式节点与部署形态，标出请求、权威状态和派生视图。
    <Ddi10BatchProcessingArchitectureLab />
  </Step>
  <Step title="2. 注入延迟、并发与分区">
    先预测P99、陈旧窗口、可用性与冲突风险，再改变单一条件。
    <Ddi10BatchProcessingFailureLab />
  </Step>
  <Step title="3. 通过独立证据门">
    六类证据齐备后才能宣布机制、性能、恢复或一致性结论成立。
    <Ddi10BatchProcessingEvidenceLab />
  </Step>
</Stepper>

<Callout type="trap">
  **语义陷阱。**
  直接修改线上派生状态，把任务成功当结果正确，忽略倾斜键和部分输出。先恢复用户不变量、提交点和故障模型，再比较方案；产品名、节点状态和平均指标不能替代保证。
</Callout>

<Callout type="trap">
  **时间陷阱。**
  客户端时间、服务器日志时间和逻辑提交顺序不同。时钟偏差、进程暂停、排队和重试会制造矛盾记录；先按消息与操作标识建立偏序，再讨论因果。
</Callout>

<Callout type="warning">
  崩溃、分区、数据回放和并发写实验只在隔离环境执行。规定最大数据量、错误预算、停止条件、清理方式和恢复负责人。
</Callout>

## 证据解释与独立交接

契约证据说明什么必须成立：记录业务不变量、请求身份、成功定义、允许陈旧度、顺序模型和SLO。任何模糊词都改写成可观察历史，例如高可用要说明分区哪侧可写，一致要说明允许哪些并发历史。

机制证据说明为什么可能成立：保存数据模型、分区与副本映射、事务或日志位置、消息顺序、版本和配置。只截最终仪表盘会丢失中间状态，无法区分修复、掩盖和错误重试。

负载证据说明在什么规模成立：记录请求分布、热点、并发、数据量、缓存状态、P50/P95/P99、吞吐和资源等待。平均值会隐藏最慢分区、锁队列、压实、检查点与网络抖动。

故障证据说明边界在哪里：在确认、持久化、复制或派生前后分别注入故障，观察重试、选举、回放与补偿。最后用外部读者对账，确认没有丢失、重复、顺序颠倒或约束破坏。

## 本章回顾

重新完成“把不可变输入转为可重跑的派生输出，从Unix管道走向MapReduce、连接和数据流”：固定版本和目录，写出用户不变量与权威状态，展开消息时间线，运行基线，再以单一故障寻找反例。最终交付输入快照、批处理DAG、连接计划、重跑对账和输出发布记录，证明“相同版本输入与代码可重建结果，失败任务不污染最终输出，来源和完成边界可审计”，并保留未解决风险。

## 复习与生产验收

<Exercises>
**问题 1**：为什么“第10章 批处理”必须覆盖14个页面节点？
<Answer>这些节点组成“把不可变输入转为可重跑的派生输出，从Unix管道走向MapReduce、连接和数据流”的状态、顺序、故障与证据链；缺项会使输入快照、批处理DAG、连接计划、重跑对账和输出发布记录无法复现。</Answer>

**问题 2**：本页最小运行不变量是什么？

<Answer>
  相同版本输入与代码可重建结果，失败任务不污染最终输出，来源和完成边界可审计。它必须由固定输入、正常历史、压力数据、故障反例和独立业务对账共同证明。
</Answer>

**问题 3**：怎样为“第10章 批处理”构造最小反例？

<Answer>
  把“直接修改线上派生状态，把任务成功当结果正确，忽略倾斜键和部分输出”写成假设，保持版本、数据和请求不变，只改变延迟、并发、崩溃点或重试之一。
</Answer>

**问题 4**：为什么超时不能直接解释为失败？

<Answer>
  超时只表示观察者没按时收到回复；远端可能未处理、已提交或仍执行。必须用操作标识、幂等规则、提交记录和独立读取判断。
</Answer>

**问题 5**：怎样验证本页性能结论？

<Answer>
  固定数据和请求，重复测量吞吐、错误、P50/P95/P99与资源等待，同时核对语义，防止用丢请求或弱化保证换取数字。
</Answer>

**问题 6**：独立交接至少包含什么？

<Answer>需要版本目录、契约输入、输入快照、批处理DAG、连接计划、重跑对账和输出发布记录、正常与故障历史、业务对账、停止条件、恢复和回退责任人。</Answer>
</Exercises>

<Glossary>
  <GlossaryItem term="不可变输入">
    不可变输入在本页指向“把不可变输入转为可重跑的派生输出，从Unix管道走向MapReduce、连接和数据流”中的具体状态和保证；边界由“相同版本输入与代码可重建结果，失败任务不污染最终输出，来源和完成边界可审计”与失败实验共同限定。
  </GlossaryItem>
  <GlossaryItem term="MapReduce">
    MapReduce在本页指向“把不可变输入转为可重跑的派生输出，从Unix管道走向MapReduce、连接和数据流”中的具体状态和保证；边界由“相同版本输入与代码可重建结果，失败任务不污染最终输出，来源和完成边界可审计”与失败实验共同限定。
  </GlossaryItem>
  <GlossaryItem term="Shuffle">
    Shuffle在本页指向“把不可变输入转为可重跑的派生输出，从Unix管道走向MapReduce、连接和数据流”中的具体状态和保证；边界由“相同版本输入与代码可重建结果，失败任务不污染最终输出，来源和完成边界可审计”与失败实验共同限定。
  </GlossaryItem>
  <GlossaryItem term="数据流">
    数据流在本页指向“把不可变输入转为可重跑的派生输出，从Unix管道走向MapReduce、连接和数据流”中的具体状态和保证；边界由“相同版本输入与代码可重建结果，失败任务不污染最终输出，来源和完成边界可审计”与失败实验共同限定。
  </GlossaryItem>
  <GlossaryItem term="物化">
    物化在本页指向“把不可变输入转为可重跑的派生输出，从Unix管道走向MapReduce、连接和数据流”中的具体状态和保证；边界由“相同版本输入与代码可重建结果，失败任务不污染最终输出，来源和完成边界可审计”与失败实验共同限定。
  </GlossaryItem>
</Glossary>

[← 上一页：第9章 一致性与共识](/learn/ddia/09-consistency-consensus/ddi-09-consistency-consensus) · [下一页：第11章 流处理 →](/learn/ddia/11-stream-processing/ddi-11-stream-processing)

<Attribution
  adaptedFrom="《数据密集型应用系统设计》第1版"
  adaptedUrl="https://www.oreilly.com/library/view/designing-data-intensive-applications/9781491903063/"
/>
