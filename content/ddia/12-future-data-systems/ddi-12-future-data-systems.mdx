---
title: "第12章 数据系统的未来"
description: "依据2018中文第1版完整目录：以数据流组合专用工具，并把端到端正确性、验证、隐私和伦理纳入设计"
type: C
section: "数据密集型应用系统设计（第1版 / 2018）"
draft: false
math: false
sourceUrl: "https://www.oreilly.com/library/view/designing-data-intensive-applications/9781491903063/"
demo: true
order: 0
---

import { Objectives } from "@/components/mdx/objectives";
import { Term } from "@/components/mdx/term";
import { Callout } from "@/components/mdx/callout";
import { Answer, Exercises } from "@/components/mdx/exercises";
import { Glossary, GlossaryItem } from "@/components/mdx/glossary";
import { Attribution } from "@/components/mdx/attribution";

# 第12章 数据系统的未来

本课程对应Martin Kleppmann著，赵军平、吕云松、耿煜、李三平译《数据密集型应用系统设计》第1版，中国电力出版社2018年9月出版，纸书519页，ISBN 9787519821968。英文原版由O'Reilly于2017年3月出版，616页，ISBN 9781491903063。

O'Reilly第1版完整目录确认3部分、12章与术语表。英文第2版由Martin Kleppmann与Chris Riccomini合著，于2026年出版并重排目录；当前课程对应2018中文第1版，不把第2版静默混入。课程独立重构核心知识、图示与实验，不复制原文。本页逐项覆盖16个正式章/节节点。

<Objectives>

- 能解释“第12章 数据系统的未来”的16个页面节点，并连接到“以数据流组合专用工具，并把端到端正确性、验证、隐私和伦理纳入设计”主线。
- 能绘制权威状态、派生状态、消息与故障边界，复现数据血缘、视图重建、约束验证、审计日志与隐私影响评估。
- 能比较正常、压力和部分故障历史，证明“权威输入、派生关系和约束可追踪可重建，系统及时且完整，用户风险有责任边界”。
- 能设计反例推翻“堆叠工具却没有权威源和恢复路径，以低延迟替代正确性，把预测能力当采集许可”，给出停止、恢复和回退条件。

</Objectives>

## 从一个不可判定的用户结果开始

先预测：客户端超时后，这次写入究竟未发生、已提交还是仍可能提交？先写用户不变量、允许陈旧窗口、尾延迟和恢复目标，再选择模型、协议或存储结构。不要先看监控图再编故事；先固定输入和预期，运行后才允许修正。

本页主问题是“以数据流组合专用工具，并把端到端正确性、验证、隐私和伦理纳入设计”。最终交付不是概念摘要，而是数据血缘、视图重建、约束验证、审计日志与隐私影响评估，由独立复跑证明：**权威输入、派生关系和约束可追踪可重建，系统及时且完整，用户风险有责任边界**。

## 核心词汇与运行边界

<Term def="数据集成是“以数据流组合专用工具，并把端到端正确性、验证、隐私和伦理纳入设计”中的第1个核心概念，必须由状态、顺序、故障与证据定义">数据集成</Term>、<Term def="派生数据是“以数据流组合专用工具，并把端到端正确性、验证、隐私和伦理纳入设计”中的第2个核心概念，必须由状态、顺序、故障与证据定义">派生数据</Term>、<Term def="数据流是“以数据流组合专用工具，并把端到端正确性、验证、隐私和伦理纳入设计”中的第3个核心概念，必须由状态、顺序、故障与证据定义">数据流</Term>、<Term def="端到端原则是“以数据流组合专用工具，并把端到端正确性、验证、隐私和伦理纳入设计”中的第4个核心概念，必须由状态、顺序、故障与证据定义">端到端原则</Term>、<Term def="完整性是“以数据流组合专用工具，并把端到端正确性、验证、隐私和伦理纳入设计”中的第5个核心概念，必须由状态、顺序、故障与证据定义">完整性</Term>
构成本页共同语言。每个词都必须回答：谁观察、观察哪个状态、顺序依据是什么、部分故障时会看到什么，以及哪条证据能推翻解释。


## 正式目录逐节点重构

### 数据集成

**目录节点 1/16。** 事务、搜索、分析与缓存需要明确多份表示之间的关系。

把“数据集成”放回“以数据流组合专用工具，并把端到端正确性、验证、隐私和伦理纳入设计”主线：上游是“用户结果与本章问题”，下游是“通过派生数据组合专用工具”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“权威输入、派生关系和约束可追踪可重建，系统及时且完整，用户风险有责任边界”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 通过派生数据组合专用工具

**目录节点 2/16。** 权威状态经变更流产生可丢弃重建的派生视图。

把“通过派生数据组合专用工具”放回“以数据流组合专用工具，并把端到端正确性、验证、隐私和伦理纳入设计”主线：上游是“数据集成”，下游是“批处理与流处理”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“权威输入、派生关系和约束可追踪可重建，系统及时且完整，用户风险有责任边界”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 批处理与流处理

**目录节点 3/16。** 全量重算与增量维护应共享逻辑避免双实现分叉。

把“批处理与流处理”放回“以数据流组合专用工具，并把端到端正确性、验证、隐私和伦理纳入设计”主线：上游是“通过派生数据组合专用工具”，下游是“拆解数据库”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“权威输入、派生关系和约束可追踪可重建，系统及时且完整，用户风险有责任边界”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 拆解数据库

**目录节点 4/16。** 索引、日志和物化能力拆分后，事务与运维边界也显露。

把“拆解数据库”放回“以数据流组合专用工具，并把端到端正确性、验证、隐私和伦理纳入设计”主线：上游是“批处理与流处理”，下游是“组合数据存储技术”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“权威输入、派生关系和约束可追踪可重建，系统及时且完整，用户风险有责任边界”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 组合数据存储技术

**目录节点 5/16。** 必须明确权威状态、Schema、顺序、幂等和重建。

把“组合数据存储技术”放回“以数据流组合专用工具，并把端到端正确性、验证、隐私和伦理纳入设计”主线：上游是“拆解数据库”，下游是“围绕数据流设计应用”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“权威输入、派生关系和约束可追踪可重建，系统及时且完整，用户风险有责任边界”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 围绕数据流设计应用

**目录节点 6/16。** 订阅状态变化可解耦读写模型，也会形成反馈环。

把“围绕数据流设计应用”放回“以数据流组合专用工具，并把端到端正确性、验证、隐私和伦理纳入设计”主线：上游是“组合数据存储技术”，下游是“观察派生状态”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“权威输入、派生关系和约束可追踪可重建，系统及时且完整，用户风险有责任边界”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 观察派生状态

**目录节点 7/16。** 界面是数据流末端视图，应表达处理中、失败与不确定。

把“观察派生状态”放回“以数据流组合专用工具，并把端到端正确性、验证、隐私和伦理纳入设计”主线：上游是“围绕数据流设计应用”，下游是“追求正确性”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“权威输入、派生关系和约束可追踪可重建，系统及时且完整，用户风险有责任边界”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 追求正确性

**目录节点 8/16。** 恰好一次标签不能替代约束、审计与外部副作用设计。

把“追求正确性”放回“以数据流组合专用工具，并把端到端正确性、验证、隐私和伦理纳入设计”主线：上游是“观察派生状态”，下游是“数据库的端到端原则”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“权威输入、派生关系和约束可追踪可重建，系统及时且完整，用户风险有责任边界”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 数据库的端到端原则

**目录节点 9/16。** 应用需携带操作标识并在业务边界消除重复。

把“数据库的端到端原则”放回“以数据流组合专用工具，并把端到端正确性、验证、隐私和伦理纳入设计”主线：上游是“追求正确性”，下游是“执行约束”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“权威输入、派生关系和约束可追踪可重建，系统及时且完整，用户风险有责任边界”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 执行约束

**目录节点 10/16。** 串行化、所有权和补偿提供不同即时性与冲突体验。

把“执行约束”放回“以数据流组合专用工具，并把端到端正确性、验证、隐私和伦理纳入设计”主线：上游是“数据库的端到端原则”，下游是“及时性与完整性”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“权威输入、派生关系和约束可追踪可重建，系统及时且完整，用户风险有责任边界”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 及时性与完整性

**目录节点 11/16。** 何时可见与何时可信是两个独立契约。

把“及时性与完整性”放回“以数据流组合专用工具，并把端到端正确性、验证、隐私和伦理纳入设计”主线：上游是“执行约束”，下游是“信任但要验证”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“权威输入、派生关系和约束可追踪可重建，系统及时且完整，用户风险有责任边界”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 信任但要验证

**目录节点 12/16。** 独立重算、校验和与不变量扫描发现沉默损坏。

把“信任但要验证”放回“以数据流组合专用工具，并把端到端正确性、验证、隐私和伦理纳入设计”主线：上游是“及时性与完整性”，下游是“做正确的事”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“权威输入、派生关系和约束可追踪可重建，系统及时且完整，用户风险有责任边界”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 做正确的事

**目录节点 13/16。** 技术正确还需评估权力、伤害、申诉和退出。

把“做正确的事”放回“以数据流组合专用工具，并把端到端正确性、验证、隐私和伦理纳入设计”主线：上游是“信任但要验证”，下游是“预测分析”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“权威输入、派生关系和约束可追踪可重建，系统及时且完整，用户风险有责任边界”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 预测分析

**目录节点 14/16。** 相关性、漂移和反馈环会改变被预测的人和环境。

把“预测分析”放回“以数据流组合专用工具，并把端到端正确性、验证、隐私和伦理纳入设计”主线：上游是“做正确的事”，下游是“隐私与跟踪”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“权威输入、派生关系和约束可追踪可重建，系统及时且完整，用户风险有责任边界”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 隐私与跟踪

**目录节点 15/16。** 采集应有目的、最小范围、保留期限和访问控制。

把“隐私与跟踪”放回“以数据流组合专用工具，并把端到端正确性、验证、隐私和伦理纳入设计”主线：上游是“预测分析”，下游是“小结”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“权威输入、派生关系和约束可追踪可重建，系统及时且完整，用户风险有责任边界”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 小结

**目录节点 16/16。** 未来系统应可组合、可验证并对社会后果负责。

把“小结”放回“以数据流组合专用工具，并把端到端正确性、验证、隐私和伦理纳入设计”主线：上游是“隐私与跟踪”，下游是“本章交付与独立验收”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“权威输入、派生关系和约束可追踪可重建，系统及时且完整，用户风险有责任边界”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

## 机制推演：状态、顺序与失败

第一步固定状态所有权。写出哪个数据集是权威事实，哪些只是缓存、索引、仓库或UI等派生视图；再写每次转换的输入版本、输出版本和提交点。没有所有权就无法区分恢复、覆盖和冲突，也无法判断“以数据流组合专用工具，并把端到端正确性、验证、隐私和伦理纳入设计”中的结果由谁负责。

第二步展开时间线。客户端发送、节点接收、本地持久化、复制或派生、确认和后续读取是不同事件；超时只提供观察者的等待上界，不提供远端提交事实。为每个事件记录逻辑顺序，墙钟仅作诊断。

第三步施加负载与故障。正常路径不能证明队列增长、热点、进程暂停、消息重复、节点崩溃或网络分区时仍满足“权威输入、派生关系和约束可追踪可重建，系统及时且完整，用户风险有责任边界”。最小实验一次只改变一个因素，并保留能复盘状态转移的原始历史。

第四步从外部核对。服务自报成功、任务完成或副本在线都属于被测路径内部证据；必须用业务标识、数量、约束、校验和或独立读取核对。最终材料“数据血缘、视图重建、约束验证、审计日志与隐私影响评估”还要包含未覆盖边界与回退。

<Callout type="trap">
  **语义陷阱。**
  堆叠工具却没有权威源和恢复路径，以低延迟替代正确性，把预测能力当采集许可。先恢复用户不变量、提交点和故障模型，再比较方案；产品名、节点状态和平均指标不能替代保证。
</Callout>

<Callout type="trap">
  **时间陷阱。**
  客户端时间、服务器日志时间和逻辑提交顺序不同。时钟偏差、进程暂停、排队和重试会制造矛盾记录；先按消息与操作标识建立偏序，再讨论因果。
</Callout>

<Callout type="warning">
  崩溃、分区、数据回放和并发写实验只在隔离环境执行。规定最大数据量、错误预算、停止条件、清理方式和恢复负责人。
</Callout>

## 证据解释与独立交接

契约证据说明什么必须成立：记录业务不变量、请求身份、成功定义、允许陈旧度、顺序模型和SLO。任何模糊词都改写成可观察历史，例如高可用要说明分区哪侧可写，一致要说明允许哪些并发历史。

机制证据说明为什么可能成立：保存数据模型、分区与副本映射、事务或日志位置、消息顺序、版本和配置。只截最终仪表盘会丢失中间状态，无法区分修复、掩盖和错误重试。

负载证据说明在什么规模成立：记录请求分布、热点、并发、数据量、缓存状态、P50/P95/P99、吞吐和资源等待。平均值会隐藏最慢分区、锁队列、压实、检查点与网络抖动。

故障证据说明边界在哪里：在确认、持久化、复制或派生前后分别注入故障，观察重试、选举、回放与补偿。最后用外部读者对账，确认没有丢失、重复、顺序颠倒或约束破坏。

## 本章回顾

重新完成“以数据流组合专用工具，并把端到端正确性、验证、隐私和伦理纳入设计”：固定版本和目录，写出用户不变量与权威状态，展开消息时间线，运行基线，再以单一故障寻找反例。最终交付数据血缘、视图重建、约束验证、审计日志与隐私影响评估，证明“权威输入、派生关系和约束可追踪可重建，系统及时且完整，用户风险有责任边界”，并保留未解决风险。

## 复习与生产验收

<Exercises>
**问题 1**：为什么“第12章 数据系统的未来”必须覆盖16个页面节点？
<Answer>这些节点组成“以数据流组合专用工具，并把端到端正确性、验证、隐私和伦理纳入设计”的状态、顺序、故障与证据链；缺项会使数据血缘、视图重建、约束验证、审计日志与隐私影响评估无法复现。</Answer>

**问题 2**：本页最小运行不变量是什么？

<Answer>
  权威输入、派生关系和约束可追踪可重建，系统及时且完整，用户风险有责任边界。它必须由固定输入、正常历史、压力数据、故障反例和独立业务对账共同证明。
</Answer>

**问题 3**：怎样为“第12章 数据系统的未来”构造最小反例？

<Answer>
  把“堆叠工具却没有权威源和恢复路径，以低延迟替代正确性，把预测能力当采集许可”写成假设，保持版本、数据和请求不变，只改变延迟、并发、崩溃点或重试之一。
</Answer>

**问题 4**：为什么超时不能直接解释为失败？

<Answer>
  超时只表示观察者没按时收到回复；远端可能未处理、已提交或仍执行。必须用操作标识、幂等规则、提交记录和独立读取判断。
</Answer>

**问题 5**：怎样验证本页性能结论？

<Answer>
  固定数据和请求，重复测量吞吐、错误、P50/P95/P99与资源等待，同时核对语义，防止用丢请求或弱化保证换取数字。
</Answer>

**问题 6**：独立交接至少包含什么？

<Answer>需要版本目录、契约输入、数据血缘、视图重建、约束验证、审计日志与隐私影响评估、正常与故障历史、业务对账、停止条件、恢复和回退责任人。</Answer>
</Exercises>

<Glossary>
  <GlossaryItem term="数据集成">
    数据集成在本页指向“以数据流组合专用工具，并把端到端正确性、验证、隐私和伦理纳入设计”中的具体状态和保证；边界由“权威输入、派生关系和约束可追踪可重建，系统及时且完整，用户风险有责任边界”与失败实验共同限定。
  </GlossaryItem>
  <GlossaryItem term="派生数据">
    派生数据在本页指向“以数据流组合专用工具，并把端到端正确性、验证、隐私和伦理纳入设计”中的具体状态和保证；边界由“权威输入、派生关系和约束可追踪可重建，系统及时且完整，用户风险有责任边界”与失败实验共同限定。
  </GlossaryItem>
  <GlossaryItem term="数据流">
    数据流在本页指向“以数据流组合专用工具，并把端到端正确性、验证、隐私和伦理纳入设计”中的具体状态和保证；边界由“权威输入、派生关系和约束可追踪可重建，系统及时且完整，用户风险有责任边界”与失败实验共同限定。
  </GlossaryItem>
  <GlossaryItem term="端到端原则">
    端到端原则在本页指向“以数据流组合专用工具，并把端到端正确性、验证、隐私和伦理纳入设计”中的具体状态和保证；边界由“权威输入、派生关系和约束可追踪可重建，系统及时且完整，用户风险有责任边界”与失败实验共同限定。
  </GlossaryItem>
  <GlossaryItem term="完整性">
    完整性在本页指向“以数据流组合专用工具，并把端到端正确性、验证、隐私和伦理纳入设计”中的具体状态和保证；边界由“权威输入、派生关系和约束可追踪可重建，系统及时且完整，用户风险有责任边界”与失败实验共同限定。
  </GlossaryItem>
</Glossary>

[← 上一页：第11章 流处理](/learn/ddia/11-stream-processing/ddi-11-stream-processing) · [下一页：术语表 →](/learn/ddia/13-glossary/ddi-glossary)

<Attribution
  adaptedFrom="《数据密集型应用系统设计》第1版"
  adaptedUrl="https://www.oreilly.com/library/view/designing-data-intensive-applications/9781491903063/"
/>
