---
title: "术语表"
description: "依据2018中文第1版完整目录：把跨12章术语绑定到可观察语义、失败边界和相邻概念，形成设计评审共同语言"
type: C
section: "数据密集型应用系统设计（第1版 / 2018）"
draft: false
math: false
sourceUrl: "https://www.oreilly.com/library/view/designing-data-intensive-applications/9781491903063/"
demo: true
order: 0
---

import {
  DdiGlossaryArchitectureLab,
  DdiGlossaryFailureLab,
  DdiGlossaryEvidenceLab,
} from "@/components/mdx/ddia/diagrams/ddi-glossary";

# 术语表

本课程对应Martin Kleppmann著，赵军平、吕云松、耿煜、李三平译《数据密集型应用系统设计》第1版，中国电力出版社2018年9月出版，纸书519页，ISBN 9787519821968。英文原版由O'Reilly于2017年3月出版，616页，ISBN 9781491903063。

O'Reilly第1版完整目录确认3部分、12章与术语表。英文第2版由Martin Kleppmann与Chris Riccomini合著，于2026年出版并重排目录；当前课程对应2018中文第1版，不把第2版静默混入。课程独立重构核心知识、图示与实验，不复制原文。本页对应原书术语表正式单元，并重构30个跨章高频词。

<Objectives>

- 能解释“术语表”的30个页面节点，并连接到“把跨12章术语绑定到可观察语义、失败边界和相邻概念，形成设计评审共同语言”主线。
- 能绘制权威状态、派生状态、消息与故障边界，复现30项术语卡、概念依赖图、保证对照表与评审记录。
- 能比较正常、压力和部分故障历史，证明“同一术语在架构、代码、监控和演练中含义一致，任何保证都声明范围与反例”。
- 能设计反例推翻“用最终一致、高可用、恰好一次等标签代替可检验定义”，给出停止、恢复和回退条件。

</Objectives>

## 从一个不可判定的用户结果开始

先预测：客户端超时后，这次写入究竟未发生、已提交还是仍可能提交？先写用户不变量、允许陈旧窗口、尾延迟和恢复目标，再选择模型、协议或存储结构。不要先看监控图再编故事；先固定输入和预期，运行后才允许修正。

本页主问题是“把跨12章术语绑定到可观察语义、失败边界和相邻概念，形成设计评审共同语言”。最终交付不是概念摘要，而是30项术语卡、概念依赖图、保证对照表与评审记录，由独立复跑证明：**同一术语在架构、代码、监控和演练中含义一致，任何保证都声明范围与反例**。

## 核心词汇与运行边界

<Term def="可用性是“把跨12章术语绑定到可观察语义、失败边界和相邻概念，形成设计评审共同语言”中的第1个核心概念，必须由状态、顺序、故障与证据定义">可用性</Term>、<Term def="因果关系是“把跨12章术语绑定到可观察语义、失败边界和相邻概念，形成设计评审共同语言”中的第2个核心概念，必须由状态、顺序、故障与证据定义">因果关系</Term>、<Term def="线性一致性是“把跨12章术语绑定到可观察语义、失败边界和相邻概念，形成设计评审共同语言”中的第3个核心概念，必须由状态、顺序、故障与证据定义">线性一致性</Term>、<Term def="幂等是“把跨12章术语绑定到可观察语义、失败边界和相邻概念，形成设计评审共同语言”中的第4个核心概念，必须由状态、顺序、故障与证据定义">幂等</Term>、<Term def="水位线是“把跨12章术语绑定到可观察语义、失败边界和相邻概念，形成设计评审共同语言”中的第5个核心概念，必须由状态、顺序、故障与证据定义">水位线</Term>
构成本页共同语言。每个词都必须回答：谁观察、观察哪个状态、顺序依据是什么、部分故障时会看到什么，以及哪条证据能推翻解释。

<DdiGlossaryArchitectureLab />

## 正式目录逐节点重构

### 可用性

**目录节点 1/30。** 规定条件下接受并完成请求的比例，需同时声明分母、错误和窗口。

把“可用性”放回“把跨12章术语绑定到可观察语义、失败边界和相邻概念，形成设计评审共同语言”主线：上游是“用户结果与本章问题”，下游是“拜占庭故障”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“同一术语在架构、代码、监控和演练中含义一致，任何保证都声明范围与反例”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 拜占庭故障

**目录节点 2/30。** 节点产生任意或矛盾行为的故障模型。

把“拜占庭故障”放回“把跨12章术语绑定到可观察语义、失败边界和相邻概念，形成设计评审共同语言”主线：上游是“可用性”，下游是“因果关系”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“同一术语在架构、代码、监控和演练中含义一致，任何保证都声明范围与反例”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 因果关系

**目录节点 3/30。** 一个事件可能影响另一个事件形成的偏序。

把“因果关系”放回“把跨12章术语绑定到可观察语义、失败边界和相邻概念，形成设计评审共同语言”主线：上游是“拜占庭故障”，下游是“变更数据捕获”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“同一术语在架构、代码、监控和演练中含义一致，任何保证都声明范围与反例”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 变更数据捕获

**目录节点 4/30。** 从提交日志抽取变化并以快照接缝构造事件流。

把“变更数据捕获”放回“把跨12章术语绑定到可观察语义、失败边界和相邻概念，形成设计评审共同语言”主线：上游是“因果关系”，下游是“一致性模型”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“同一术语在架构、代码、监控和演练中含义一致，任何保证都声明范围与反例”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 一致性模型

**目录节点 5/30。** 定义读取允许观察到哪些写入和顺序。

把“一致性模型”放回“把跨12章术语绑定到可观察语义、失败边界和相邻概念，形成设计评审共同语言”主线：上游是“变更数据捕获”，下游是“共识”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“同一术语在架构、代码、监控和演练中含义一致，任何保证都声明范围与反例”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 共识

**目录节点 6/30。** 多个节点对单一值或日志顺序作出不可分叉决定。

把“共识”放回“把跨12章术语绑定到可观察语义、失败边界和相邻概念，形成设计评审共同语言”主线：上游是“一致性模型”，下游是“派生数据”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“同一术语在架构、代码、监控和演练中含义一致，任何保证都声明范围与反例”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 派生数据

**目录节点 7/30。** 可由权威数据通过确定过程重建的数据。

把“派生数据”放回“把跨12章术语绑定到可观察语义、失败边界和相邻概念，形成设计评审共同语言”主线：上游是“共识”，下游是“编码”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“同一术语在架构、代码、监控和演练中含义一致，任何保证都声明范围与反例”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 编码

**目录节点 8/30。** 把内存结构转换为可持久化或传输字节。

把“编码”放回“把跨12章术语绑定到可观察语义、失败边界和相邻概念，形成设计评审共同语言”主线：上游是“派生数据”，下游是“事件时间”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“同一术语在架构、代码、监控和演练中含义一致，任何保证都声明范围与反例”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 事件时间

**目录节点 9/30。** 业务事件实际发生时间，而非处理器观察时间。

把“事件时间”放回“把跨12章术语绑定到可观察语义、失败边界和相邻概念，形成设计评审共同语言”主线：上游是“编码”，下游是“隔离令牌”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“同一术语在架构、代码、监控和演练中含义一致，任何保证都声明范围与反例”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 隔离令牌

**目录节点 10/30。** 单调任期编号，使资源拒绝过期参与者写入。

把“隔离令牌”放回“把跨12章术语绑定到可观察语义、失败边界和相邻概念，形成设计评审共同语言”主线：上游是“事件时间”，下游是“幂等”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“同一术语在架构、代码、监控和演练中含义一致，任何保证都声明范围与反例”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 幂等

**目录节点 11/30。** 同一操作执行多次仍有等价可观察结果。

把“幂等”放回“把跨12章术语绑定到可观察语义、失败边界和相邻概念，形成设计评审共同语言”主线：上游是“隔离令牌”，下游是“线性一致性”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“同一术语在架构、代码、监控和演练中含义一致，任何保证都声明范围与反例”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 线性一致性

**目录节点 12/30。** 操作在调用与返回之间原子生效并尊重实时顺序。

把“线性一致性”放回“把跨12章术语绑定到可观察语义、失败边界和相邻概念，形成设计评审共同语言”主线：上游是“幂等”，下游是“LSM树”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“同一术语在架构、代码、监控和演练中含义一致，任何保证都声明范围与反例”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### LSM树

**目录节点 13/30。** 以有序不可变段、内存表和压实组织数据。

把“LSM树”放回“把跨12章术语绑定到可观察语义、失败边界和相邻概念，形成设计评审共同语言”主线：上游是“线性一致性”，下游是“物化视图”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“同一术语在架构、代码、监控和演练中含义一致，任何保证都声明范围与反例”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 物化视图

**目录节点 14/30。** 预先计算并保存查询结果的派生结构。

把“物化视图”放回“把跨12章术语绑定到可观察语义、失败边界和相邻概念，形成设计评审共同语言”主线：上游是“LSM树”，下游是“单调时钟”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“同一术语在架构、代码、监控和演练中含义一致，任何保证都声明范围与反例”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 单调时钟

**目录节点 15/30。** 不会因校时向后跳变、适合测持续时间的时钟。

把“单调时钟”放回“把跨12章术语绑定到可观察语义、失败边界和相邻概念，形成设计评审共同语言”主线：上游是“物化视图”，下游是“多版本并发控制”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“同一术语在架构、代码、监控和演练中含义一致，任何保证都声明范围与反例”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 多版本并发控制

**目录节点 16/30。** 保存多个版本并按快照决定可见性。

把“多版本并发控制”放回“把跨12章术语绑定到可观察语义、失败边界和相邻概念，形成设计评审共同语言”主线：上游是“单调时钟”，下游是“分区”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“同一术语在架构、代码、监控和演练中含义一致，任何保证都声明范围与反例”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 分区

**目录节点 17/30。** 按键范围、哈希或业务规则把数据分散。

把“分区”放回“把跨12章术语绑定到可观察语义、失败边界和相邻概念，形成设计评审共同语言”主线：上游是“多版本并发控制”，下游是“法定人数”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“同一术语在架构、代码、监控和演练中含义一致，任何保证都声明范围与反例”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 法定人数

**目录节点 18/30。** 操作必须获得的副本票数，交集不自动等于线性一致。

把“法定人数”放回“把跨12章术语绑定到可观察语义、失败边界和相邻概念，形成设计评审共同语言”主线：上游是“分区”，下游是“读己之写”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“同一术语在架构、代码、监控和演练中含义一致，任何保证都声明范围与反例”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 读己之写

**目录节点 19/30。** 成功写入后，后续读取不会看不到该写入。

把“读己之写”放回“把跨12章术语绑定到可观察语义、失败边界和相邻概念，形成设计评审共同语言”主线：上游是“法定人数”，下游是“复制”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“同一术语在架构、代码、监控和演练中含义一致，任何保证都声明范围与反例”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 复制

**目录节点 20/30。** 在多机保存副本并传播变化。

把“复制”放回“把跨12章术语绑定到可观察语义、失败边界和相邻概念，形成设计评审共同语言”主线：上游是“读己之写”，下游是“Schema演化”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“同一术语在架构、代码、监控和演练中含义一致，任何保证都声明范围与反例”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### Schema演化

**目录节点 21/30。** 结构变化时保持新旧读写者兼容的规则。

把“Schema演化”放回“把跨12章术语绑定到可观察语义、失败边界和相邻概念，形成设计评审共同语言”主线：上游是“复制”，下游是“可串行化”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“同一术语在架构、代码、监控和演练中含义一致，任何保证都声明范围与反例”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 可串行化

**目录节点 22/30。** 并发结果等价于某种串行执行顺序。

把“可串行化”放回“把跨12章术语绑定到可观察语义、失败边界和相邻概念，形成设计评审共同语言”主线：上游是“Schema演化”，下游是“快照隔离”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“同一术语在架构、代码、监控和演练中含义一致，任何保证都声明范围与反例”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 快照隔离

**目录节点 23/30。** 事务从一致快照读取并检测特定写冲突。

把“快照隔离”放回“把跨12章术语绑定到可观察语义、失败边界和相邻概念，形成设计评审共同语言”主线：上游是“可串行化”，下游是“SSTable”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“同一术语在架构、代码、监控和演练中含义一致，任何保证都声明范围与反例”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### SSTable

**目录节点 24/30。** 按键排序且写成后不原地修改的字符串表。

把“SSTable”放回“把跨12章术语绑定到可观察语义、失败边界和相邻概念，形成设计评审共同语言”主线：上游是“快照隔离”，下游是“流处理”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“同一术语在架构、代码、监控和演练中含义一致，任何保证都声明范围与反例”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 流处理

**目录节点 25/30。** 对持续到达的无界事件执行变换。

把“流处理”放回“把跨12章术语绑定到可观察语义、失败边界和相邻概念，形成设计评审共同语言”主线：上游是“SSTable”，下游是“事务”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“同一术语在架构、代码、监控和演练中含义一致，任何保证都声明范围与反例”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 事务

**目录节点 26/30。** 把一组读写组织为提交或中止单元。

把“事务”放回“把跨12章术语绑定到可观察语义、失败边界和相邻概念，形成设计评审共同语言”主线：上游是“流处理”，下游是“两阶段提交”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“同一术语在架构、代码、监控和演练中含义一致，任何保证都声明范围与反例”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 两阶段提交

**目录节点 27/30。** 先准备参与者再发布提交或中止决定。

把“两阶段提交”放回“把跨12章术语绑定到可观察语义、失败边界和相邻概念，形成设计评审共同语言”主线：上游是“事务”，下游是“版本向量”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“同一术语在架构、代码、监控和演练中含义一致，任何保证都声明范围与反例”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 版本向量

**目录节点 28/30。** 按副本逻辑计数表达覆盖或并发关系。

把“版本向量”放回“把跨12章术语绑定到可观察语义、失败边界和相邻概念，形成设计评审共同语言”主线：上游是“两阶段提交”，下游是“水位线”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“同一术语在架构、代码、监控和演练中含义一致，任何保证都声明范围与反例”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 水位线

**目录节点 29/30。** 流处理器对事件时间进度的估计。

把“水位线”放回“把跨12章术语绑定到可观察语义、失败边界和相邻概念，形成设计评审共同语言”主线：上游是“版本向量”，下游是“写偏差”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“同一术语在架构、代码、监控和演练中含义一致，任何保证都声明范围与反例”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

### 写偏差

**目录节点 30/30。** 并发事务更新不同对象却破坏共同不变量。

把“写偏差”放回“把跨12章术语绑定到可观察语义、失败边界和相邻概念，形成设计评审共同语言”主线：上游是“水位线”，下游是“本章交付与独立验收”。先写权威输入、局部状态、跨边界消息和用户可见结果，再标出延迟、并发、崩溃或重试在哪条边上改变观察。若只能复述术语，不能预测状态转移或构造失败历史，就还没有掌握这个节点。

实验保持版本、数据分布和请求不变，只改变一个机制或故障条件；同时记录正常结果、尾延迟、消息顺序、恢复终点与独立对账。用“同一术语在架构、代码、监控和演练中含义一致，任何保证都声明范围与反例”判断是否通过，不能用进程存活、平均耗时或一次演示成功代替系统语义。

## 机制推演：状态、顺序与失败

第一步固定状态所有权。写出哪个数据集是权威事实，哪些只是缓存、索引、仓库或UI等派生视图；再写每次转换的输入版本、输出版本和提交点。没有所有权就无法区分恢复、覆盖和冲突，也无法判断“把跨12章术语绑定到可观察语义、失败边界和相邻概念，形成设计评审共同语言”中的结果由谁负责。

第二步展开时间线。客户端发送、节点接收、本地持久化、复制或派生、确认和后续读取是不同事件；超时只提供观察者的等待上界，不提供远端提交事实。为每个事件记录逻辑顺序，墙钟仅作诊断。

第三步施加负载与故障。正常路径不能证明队列增长、热点、进程暂停、消息重复、节点崩溃或网络分区时仍满足“同一术语在架构、代码、监控和演练中含义一致，任何保证都声明范围与反例”。最小实验一次只改变一个因素，并保留能复盘状态转移的原始历史。

第四步从外部核对。服务自报成功、任务完成或副本在线都属于被测路径内部证据；必须用业务标识、数量、约束、校验和或独立读取核对。最终材料“30项术语卡、概念依赖图、保证对照表与评审记录”还要包含未覆盖边界与回退。

## 最小可重放实验

```text
book=数据密集型应用系统设计（第1版 / 2018）
unit=ddi-glossary
question=把跨12章术语绑定到可观察语义、失败边界和相邻概念，形成设计评审共同语言
invariant=同一术语在架构、代码、监控和演练中含义一致，任何保证都声明范围与反例
artifact=30项术语卡、概念依赖图、保证对照表与评审记录
```

```pseudo
given fixed_version, fixed_dataset, fixed_request_history
predict user_result, state_transitions, p99, failure_endpoint
run baseline and capture ordered_events
change exactly one of latency, concurrency, crash_point, retry_policy
assert business_invariant with an independent_reader
```

```yaml
evidence:
  input: schema, distribution, request ids, dependency versions
  execution: ordered events, queue depth, resource percentiles
  failure: injection point, timeout, retry, leader or offset changes
  result: accepted, committed, visible, reconciled, recovered
  decision: pass, reject, rollback, unresolved
```

## 三步交互实验

<Stepper>
  <Step title="1. 固定数据流和目录节点">
    选择正式节点与部署形态，标出请求、权威状态和派生视图。
    <DdiGlossaryArchitectureLab />
  </Step>
  <Step title="2. 注入延迟、并发与分区">
    先预测P99、陈旧窗口、可用性与冲突风险，再改变单一条件。
    <DdiGlossaryFailureLab />
  </Step>
  <Step title="3. 通过独立证据门">
    六类证据齐备后才能宣布机制、性能、恢复或一致性结论成立。
    <DdiGlossaryEvidenceLab />
  </Step>
</Stepper>

<Callout type="trap">
  **语义陷阱。**
  用最终一致、高可用、恰好一次等标签代替可检验定义。先恢复用户不变量、提交点和故障模型，再比较方案；产品名、节点状态和平均指标不能替代保证。
</Callout>

<Callout type="trap">
  **时间陷阱。**
  客户端时间、服务器日志时间和逻辑提交顺序不同。时钟偏差、进程暂停、排队和重试会制造矛盾记录；先按消息与操作标识建立偏序，再讨论因果。
</Callout>

<Callout type="warning">
  崩溃、分区、数据回放和并发写实验只在隔离环境执行。规定最大数据量、错误预算、停止条件、清理方式和恢复负责人。
</Callout>

## 证据解释与独立交接

契约证据说明什么必须成立：记录业务不变量、请求身份、成功定义、允许陈旧度、顺序模型和SLO。任何模糊词都改写成可观察历史，例如高可用要说明分区哪侧可写，一致要说明允许哪些并发历史。

机制证据说明为什么可能成立：保存数据模型、分区与副本映射、事务或日志位置、消息顺序、版本和配置。只截最终仪表盘会丢失中间状态，无法区分修复、掩盖和错误重试。

负载证据说明在什么规模成立：记录请求分布、热点、并发、数据量、缓存状态、P50/P95/P99、吞吐和资源等待。平均值会隐藏最慢分区、锁队列、压实、检查点与网络抖动。

故障证据说明边界在哪里：在确认、持久化、复制或派生前后分别注入故障，观察重试、选举、回放与补偿。最后用外部读者对账，确认没有丢失、重复、顺序颠倒或约束破坏。

## 本章回顾

重新完成“把跨12章术语绑定到可观察语义、失败边界和相邻概念，形成设计评审共同语言”：固定版本和目录，写出用户不变量与权威状态，展开消息时间线，运行基线，再以单一故障寻找反例。最终交付30项术语卡、概念依赖图、保证对照表与评审记录，证明“同一术语在架构、代码、监控和演练中含义一致，任何保证都声明范围与反例”，并保留未解决风险。

## 复习与生产验收

<Exercises>
**问题 1**：为什么“术语表”必须覆盖30个页面节点？
<Answer>这些节点组成“把跨12章术语绑定到可观察语义、失败边界和相邻概念，形成设计评审共同语言”的状态、顺序、故障与证据链；缺项会使30项术语卡、概念依赖图、保证对照表与评审记录无法复现。</Answer>

**问题 2**：本页最小运行不变量是什么？

<Answer>
  同一术语在架构、代码、监控和演练中含义一致，任何保证都声明范围与反例。它必须由固定输入、正常历史、压力数据、故障反例和独立业务对账共同证明。
</Answer>

**问题 3**：怎样为“术语表”构造最小反例？

<Answer>
  把“用最终一致、高可用、恰好一次等标签代替可检验定义”写成假设，保持版本、数据和请求不变，只改变延迟、并发、崩溃点或重试之一。
</Answer>

**问题 4**：为什么超时不能直接解释为失败？

<Answer>
  超时只表示观察者没按时收到回复；远端可能未处理、已提交或仍执行。必须用操作标识、幂等规则、提交记录和独立读取判断。
</Answer>

**问题 5**：怎样验证本页性能结论？

<Answer>
  固定数据和请求，重复测量吞吐、错误、P50/P95/P99与资源等待，同时核对语义，防止用丢请求或弱化保证换取数字。
</Answer>

**问题 6**：独立交接至少包含什么？

<Answer>需要版本目录、契约输入、30项术语卡、概念依赖图、保证对照表与评审记录、正常与故障历史、业务对账、停止条件、恢复和回退责任人。</Answer>
</Exercises>

<Glossary>
  <GlossaryItem term="可用性">
    可用性在本页指向“把跨12章术语绑定到可观察语义、失败边界和相邻概念，形成设计评审共同语言”中的具体状态和保证；边界由“同一术语在架构、代码、监控和演练中含义一致，任何保证都声明范围与反例”与失败实验共同限定。
  </GlossaryItem>
  <GlossaryItem term="因果关系">
    因果关系在本页指向“把跨12章术语绑定到可观察语义、失败边界和相邻概念，形成设计评审共同语言”中的具体状态和保证；边界由“同一术语在架构、代码、监控和演练中含义一致，任何保证都声明范围与反例”与失败实验共同限定。
  </GlossaryItem>
  <GlossaryItem term="线性一致性">
    线性一致性在本页指向“把跨12章术语绑定到可观察语义、失败边界和相邻概念，形成设计评审共同语言”中的具体状态和保证；边界由“同一术语在架构、代码、监控和演练中含义一致，任何保证都声明范围与反例”与失败实验共同限定。
  </GlossaryItem>
  <GlossaryItem term="幂等">
    幂等在本页指向“把跨12章术语绑定到可观察语义、失败边界和相邻概念，形成设计评审共同语言”中的具体状态和保证；边界由“同一术语在架构、代码、监控和演练中含义一致，任何保证都声明范围与反例”与失败实验共同限定。
  </GlossaryItem>
  <GlossaryItem term="水位线">
    水位线在本页指向“把跨12章术语绑定到可观察语义、失败边界和相邻概念，形成设计评审共同语言”中的具体状态和保证；边界由“同一术语在架构、代码、监控和演练中含义一致，任何保证都声明范围与反例”与失败实验共同限定。
  </GlossaryItem>
</Glossary>

[← 上一页：第12章 数据系统的未来](/learn/ddia/12-future-data-systems/ddi-12-future-data-systems) · [下一页：第1版全书总复习 →](/learn/ddia/99-final-review/ddi-official-final-review)

<Attribution
  adaptedFrom="《数据密集型应用系统设计》第1版"
  adaptedUrl="https://www.oreilly.com/library/view/designing-data-intensive-applications/9781491903063/"
/>
