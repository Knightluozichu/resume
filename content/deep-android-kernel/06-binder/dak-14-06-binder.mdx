---
title: "第6章 进程间通信——Binder"
description: "依据2014年首版完整目录覆盖20个节点：从智能指针与Parcel进入Binder驱动协议、ServiceManager、客户端、AIDL和匿名服务"
type: C
section: "深入理解Android内核设计思想（第1版 / Android 4.3）"
draft: false
math: false
sourceUrl: "https://www.yami.com/zh/p/android/3118057821"
demo: true
order: 0
qualityVersion: 2
practiceMode: diagnosis
sourceMode: independent-rewrite
officialUnitId: dak-14-06-binder
---

import { Objectives } from "@/components/mdx/objectives";
import { Term } from "@/components/mdx/term";
import { Callout } from "@/components/mdx/callout";
import { Answer, Exercises } from "@/components/mdx/exercises";
import { Glossary, GlossaryItem } from "@/components/mdx/glossary";
import { Attribution } from "@/components/mdx/attribution";
import { Dak1406BinderDecisionLab, Dak1406BinderMechanismMap, Dak1406BinderFailureDiagram } from "@/components/mdx/deep-android-kernel/dak-14-06-binder";

# 第6章 进程间通信——Binder

在《第6章 进程间通信——Binder》中，本页依据林学森《深入理解Android内核设计思想》独立重构，不复制原文。版本锁定人民邮电出版社2014年5月首版、687页、ISBN 9787115348418，面向Android 4.3以上；全书为4篇22章、323个章/节/小节节点。

在《第6章 进程间通信——Binder》中，课程先复现首版Make构建、Binder、SurfaceFlinger、WMS、View、AudioFlinger、Ant与旧工具链，再用现代AOSP/Soong、Treble、ART和新调试工具建立迁移账本，绝不把第2版新增的虚拟机、安全与Gradle章节倒填进首版。

<Objectives>

- 能解释“第6章 进程间通信——Binder”全部20个正式节点的入口、线程进程、数据与资源所有权。
- 能实现“从智能指针与Parcel进入Binder驱动协议、ServiceManager、客户端、AIDL和匿名服务”的最小复现实验并保存版本卡、命令、产物、状态和日志。
- 能区分源码事实、运行证据与版本变化，并用sp/wp引用计数、Parcel边界、binder_open/mmap/ioctl、事务码、线程池与死亡通知验收。
- 能设计反例推翻“只画代理和服务两端，遗漏驱动映射、Parcel校验、线程池、引用计数和死亡处理”，写出首版机制到现代Android的迁移和回退条件。

</Objectives>

<Dak1406BinderMechanismMap />

<Dak1406BinderDecisionLab />

<Dak1406BinderFailureDiagram />

## 从版本与断点开始

在《第6章 进程间通信——Binder》中，<Term def="记录AOSP标签、仓库、构建目标、文件路径和提交位置的溯源信息">源码版本卡</Term>、<Term def="从公开入口沿函数、线程、进程和语言边界到实际执行点的有序路径">调用链</Term>、<Term def="规定进程、对象、缓冲区、文件描述符和线程由谁创建、共享与释放的责任">资源所有权</Term>、<Term def="在跨线程、跨进程或跨层边界保存输入输出与身份信息的可复查位置">证据断点</Term>、<Term def="逐项记录首版机制、现代变化、行为差异、验证证据和回退条件的清单">迁移账本</Term>构成本页的分析协议。先预测入口会跨过哪些线程、进程、语言、驱动或硬件抽象边界，再在每个边界保存身份和输入输出；没有标签与证据断点的流程图不能复查。

本单元主线是从智能指针与Parcel进入Binder驱动协议、ServiceManager、客户端、AIDL和匿名服务。交互管线逐项选择目录节点；反例实验切换基线、错误版本、线程停滞、所有者丢失和数据损坏；证据门要求目录、版本、调用、所有权与迁移全部通过。

## 证据解释

在《第6章 进程间通信——Binder》中，第一份证据是版本证据：AOSP标签、repo manifest、主机工具链、产品目标和产物哈希必须同时保存。Android源码长期重构，同名类和服务在不同分支可能承担不同职责；跨版本拼接得到的“完整流程”实际上无法编译，也无法在单一设备上运行。

第二份证据是调用证据。针对“从智能指针与Parcel进入Binder驱动协议、ServiceManager、客户端、AIDL和匿名服务”，从真实公开入口前向追踪，每次跨线程、跨进程、跨Java/Native、跨HAL/驱动边界都记录双方、数据形态、事务或函数编号和错误返回。sp/wp引用计数、Parcel边界、binder_open/mmap/ioctl、事务码、线程池与死亡通知要能让另一位读者在相同标签上停在相同断点。

在《第6章 进程间通信——Binder》中，第三份证据是所有权证据。成功路径之外，主动让调用方退出、服务重启、缓冲区耗尽、输入损坏或超时发生，观察对象、引用计数、文件描述符、线程和映射是否释放。只画“谁调用谁”而不回答“谁负责释放”，无法解释系统泄漏、死锁和迟到回调。

在《第6章 进程间通信——Binder》中，第四份证据是迁移对照。现代AOSP可能以Soong、Treble、AIDL稳定接口、新渲染链或新调试工具替代首版机制。迁移时保持功能结果和故障样本不变，一次只替换一层；若线程、错误、性能或资源语义变化，先记录差异和回退，再更新结论。

```text
历史节点: 第6章 进程间通信——Binder
首版基线: Android 4.3 / 2014年第1版
现代候选: 待当前AOSP标签验证
不变量: 功能、线程进程、错误、资源释放
证据: sp/wp引用计数、Parcel边界、binder_open/mmap/ioctl、事务码、线程池与死亡通知
回退: 任一不变量失败即恢复首版路径
```

## 本章回顾

从一次真实输入开始，依次回答源码属于哪个标签、入口在哪里、跨过哪些边界、每层保存什么状态、错误如何返回、资源由谁释放。再用sp/wp引用计数、Parcel边界、binder_open/mmap/ioctl、事务码、线程池与死亡通知重放；无法在单一版本复现的结论退回研究阶段。

## 复习与独立交接

- 20个节点覆盖进程间通信——Binder
- 最小切片验证从智能指针与Parcel进入Binde…
- 故障实验对比正常与故障边界
- 凭证据完成交接

<Exercises>
**问题 1**：为什么必须保留20个正式节点？
<Answer>
  这些节点共同组成“从智能指针与Parcel进入Binder驱动协议、ServiceManager、客户端、AIDL和匿名服务”的完整入口、机制、边界与证据链，删去节点会造成调用或所有权断裂。
</Answer>
**问题 2**：最小正确性合同是什么？
<Answer>
  同一源码标签、构建目标和输入下，功能结果、线程进程、错误与资源释放必须确定且可重放。
</Answer>
**问题 3**：怎样构造有效反例？
<Answer>
  主动让“只画代理和服务两端，遗漏驱动映射、Parcel校验、线程池、引用计数和死亡处理”成立，再注入超时、服务退出、数据损坏或版本错配；出现死锁、泄漏、错位或不可复现即失败。
</Answer>
**问题 4**：为什么类名和静态流程图不够？
<Answer>
  它们不能证明标签、运行入口、线程进程、对象身份、错误路径和释放结果，必须结合构建产物与运行日志。
</Answer>
**问题 5**：怎样迁移而不改写首版？
<Answer>
  先保存Android 4.3首版行为基线，再一次替换一层，用相同输入和故障测试比较并记录差异与回退。
</Answer>
**问题 6**：独立交接需要什么？
<Answer>
  需要源码版本卡、完整目录映射、构建命令与产物、sp/wp引用计数、Parcel边界、binder_open/mmap/ioctl、事务码、线程池与死亡通知、失败实验、迁移账本和回退条件。
</Answer>
</Exercises>

<Glossary>
  <GlossaryItem term="源码版本卡">
    记录AOSP标签、仓库、构建目标、文件路径和提交位置的溯源信息。
  </GlossaryItem>
  <GlossaryItem term="调用链">
    从公开入口沿函数、线程、进程和语言边界到实际执行点的有序路径。
  </GlossaryItem>
  <GlossaryItem term="资源所有权">
    规定进程、对象、缓冲区、文件描述符和线程由谁创建、共享与释放的责任。
  </GlossaryItem>
  <GlossaryItem term="证据断点">
    在跨线程、跨进程或跨层边界保存输入输出与身份信息的可复查位置。
  </GlossaryItem>
  <GlossaryItem term="迁移账本">
    逐项记录首版机制、现代变化、行为差异、验证证据和回退条件的清单。
  </GlossaryItem>
</Glossary>

[← 上一页：第5章 Android进程/线程管理](/learn/deep-android-kernel/05-process-thread/dak-14-05-process-thread) · [下一页：第7章 Android启动过程简析 →](/learn/deep-android-kernel/07-boot/dak-14-07-boot)

## 原版目录概念补充核对

以下条目补齐官方目录中容易被示例主线掩盖的概念。它们不重复罗列目录，而是明确每项概念的机制、适用边界和验收证据。

### 6.1 智能指针：机制、边界与证据

第6章 进程间通信——Binder中的6.1 智能指针跨越代理、Parcel、Binder 驱动、线程池与服务对象，Java/C++ 引用外观不能代表同一身份。保存 PID/TID、事务码、缓冲区、引用计数和死亡通知，注入服务退出、大事务或畸形数据确认错误返回与清理。

### 6.1.1 智能指针的设计理念：机制、边界与证据

第6章 进程间通信——Binder中的6.1.1 智能指针的设计理念跨越代理、Parcel、Binder 驱动、线程池与服务对象，Java/C++ 引用外观不能代表同一身份。保存 PID/TID、事务码、缓冲区、引用计数和死亡通知，注入服务退出、大事务或畸形数据确认错误返回与清理。

### 6.1.2 强指针sp：机制、边界与证据

第6章 进程间通信——Binder中的6.1.2 强指针sp跨越代理、Parcel、Binder 驱动、线程池与服务对象，Java/C++ 引用外观不能代表同一身份。保存 PID/TID、事务码、缓冲区、引用计数和死亡通知，注入服务退出、大事务或畸形数据确认错误返回与清理。

### 6.1.3 弱指针wp：机制、边界与证据

第6章 进程间通信——Binder中的6.1.3 弱指针wp跨越代理、Parcel、Binder 驱动、线程池与服务对象，Java/C++ 引用外观不能代表同一身份。保存 PID/TID、事务码、缓冲区、引用计数和死亡通知，注入服务退出、大事务或畸形数据确认错误返回与清理。

### 6.2 进程间的数据传递载体——Parcel：机制、边界与证据

第6章 进程间通信——Binder中的6.2 进程间的数据传递载体——Parcel要从 Linux 进程/线程、地址空间、调度和同步原语连接到 Android 运行结果。记录 PID/TID、优先级、等待点和资源 owner，用竞争、阻塞、退出或内存压力样本验证时序与释放。

### 6.3 Binder驱动与协议：机制、边界与证据

第6章 进程间通信——Binder中的6.3 Binder驱动与协议跨越代理、Parcel、Binder 驱动、线程池与服务对象，Java/C++ 引用外观不能代表同一身份。保存 PID/TID、事务码、缓冲区、引用计数和死亡通知，注入服务退出、大事务或畸形数据确认错误返回与清理。

### 6.3.1 打开Binder驱动——binder_open：机制、边界与证据

第6章 进程间通信——Binder中的6.3.1 打开Binder驱动——binder_open跨越代理、Parcel、Binder 驱动、线程池与服务对象，Java/C++ 引用外观不能代表同一身份。保存 PID/TID、事务码、缓冲区、引用计数和死亡通知，注入服务退出、大事务或畸形数据确认错误返回与清理。

### 6.3.2 binder_mmap：机制、边界与证据

第6章 进程间通信——Binder中的6.3.2 binder_mmap必须锁定 Android 4.3 首版坐标，从真实入口跟踪线程、进程、语言与驱动边界，并标出对象和缓冲区所有权。使用同一构建和输入运行正常与单变量失败路径，保存版本卡、断点、日志和释放结果。

### 6.3.3 binder_ioctl：机制、边界与证据

第6章 进程间通信——Binder中的6.3.3 binder_ioctl必须锁定 Android 4.3 首版坐标，从真实入口跟踪线程、进程、语言与驱动边界，并标出对象和缓冲区所有权。使用同一构建和输入运行正常与单变量失败路径，保存版本卡、断点、日志和释放结果。

### 6.4 DNS服务器——ServiceManager（Binder Server）：机制、边界与证据

第6章 进程间通信——Binder中的6.4 DNS服务器——ServiceManager（Binder Server）跨越代理、Parcel、Binder 驱动、线程池与服务对象，Java/C++ 引用外观不能代表同一身份。保存 PID/TID、事务码、缓冲区、引用计数和死亡通知，注入服务退出、大事务或畸形数据确认错误返回与清理。

### 6.4.1 ServiceManager的启动：机制、边界与证据

第6章 进程间通信——Binder中的6.4.1 ServiceManager的启动跨越代理、Parcel、Binder 驱动、线程池与服务对象，Java/C++ 引用外观不能代表同一身份。保存 PID/TID、事务码、缓冲区、引用计数和死亡通知，注入服务退出、大事务或畸形数据确认错误返回与清理。

### 6.4.2 ServiceManager的构建：机制、边界与证据

第6章 进程间通信——Binder中的6.4.2 ServiceManager的构建跨越代理、Parcel、Binder 驱动、线程池与服务对象，Java/C++ 引用外观不能代表同一身份。保存 PID/TID、事务码、缓冲区、引用计数和死亡通知，注入服务退出、大事务或畸形数据确认错误返回与清理。

### 6.4.3 获取ServiceManager服务——设计思考：机制、边界与证据

第6章 进程间通信——Binder中的6.4.3 获取ServiceManager服务——设计思考跨越代理、Parcel、Binder 驱动、线程池与服务对象，Java/C++ 引用外观不能代表同一身份。保存 PID/TID、事务码、缓冲区、引用计数和死亡通知，注入服务退出、大事务或畸形数据确认错误返回与清理。

### 6.4.4 ServiceManagerProxy：机制、边界与证据

第6章 进程间通信——Binder中的6.4.4 ServiceManagerProxy跨越代理、Parcel、Binder 驱动、线程池与服务对象，Java/C++ 引用外观不能代表同一身份。保存 PID/TID、事务码、缓冲区、引用计数和死亡通知，注入服务退出、大事务或畸形数据确认错误返回与清理。

### 6.4.5 IBinder和BpBinder：机制、边界与证据

第6章 进程间通信——Binder中的6.4.5 IBinder和BpBinder跨越代理、Parcel、Binder 驱动、线程池与服务对象，Java/C++ 引用外观不能代表同一身份。保存 PID/TID、事务码、缓冲区、引用计数和死亡通知，注入服务退出、大事务或畸形数据确认错误返回与清理。

### 6.4.6 ProcessState和IPCThreadState：机制、边界与证据

第6章 进程间通信——Binder中的6.4.6 ProcessState和IPCThreadState跨越代理、Parcel、Binder 驱动、线程池与服务对象，Java/C++ 引用外观不能代表同一身份。保存 PID/TID、事务码、缓冲区、引用计数和死亡通知，注入服务退出、大事务或畸形数据确认错误返回与清理。

### 6.5 Binder客户端——Binder Client：机制、边界与证据

第6章 进程间通信——Binder中的6.5 Binder客户端——Binder Client跨越代理、Parcel、Binder 驱动、线程池与服务对象，Java/C++ 引用外观不能代表同一身份。保存 PID/TID、事务码、缓冲区、引用计数和死亡通知，注入服务退出、大事务或畸形数据确认错误返回与清理。

### 6.6 Android接口描述语言——AIDL：机制、边界与证据

第6章 进程间通信——Binder中的6.6 Android接口描述语言——AIDL跨越代理、Parcel、Binder 驱动、线程池与服务对象，Java/C++ 引用外观不能代表同一身份。保存 PID/TID、事务码、缓冲区、引用计数和死亡通知，注入服务退出、大事务或畸形数据确认错误返回与清理。

### 6.7 匿名Binder Server：机制、边界与证据

第6章 进程间通信——Binder中的6.7 匿名Binder Server跨越代理、Parcel、Binder 驱动、线程池与服务对象，Java/C++ 引用外观不能代表同一身份。保存 PID/TID、事务码、缓冲区、引用计数和死亡通知，注入服务退出、大事务或畸形数据确认错误返回与清理。

<Attribution
  adaptedFrom="林学森《深入理解Android内核设计思想》"
  adaptedUrl="https://www.yami.com/zh/p/android/3118057821"
  mode="independent-rewrite"
  sourceBasis="outline-only"
/>
