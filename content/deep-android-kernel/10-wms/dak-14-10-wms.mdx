---
title: "第10章 GUI系统之窗口管理员——WMS"
description: "依据2014年首版完整目录覆盖28个节点：从WMS职责、窗口属性与添加流程进入Surface、布局、启动窗口和动画"
type: C
section: "深入理解Android内核设计思想（第1版 / Android 4.3）"
draft: false
math: false
sourceUrl: "https://www.yami.com/zh/p/android/3118057821"
demo: true
order: 0
---

import { Objectives } from "@/components/mdx/objectives";
import { Term } from "@/components/mdx/term";
import { Callout } from "@/components/mdx/callout";
import { Answer, Exercises } from "@/components/mdx/exercises";
import { Glossary, GlossaryItem } from "@/components/mdx/glossary";
import { Attribution } from "@/components/mdx/attribution";

# 第10章 GUI系统之窗口管理员——WMS

本页依据林学森《深入理解Android内核设计思想》独立重构，不复制原文。版本锁定人民邮电出版社2014年5月首版、687页、ISBN 9787115348418，面向Android 4.3以上；全书为4篇22章、323个章/节/小节节点。

课程先复现首版Make构建、Binder、SurfaceFlinger、WMS、View、AudioFlinger、Ant与旧工具链，再用现代AOSP/Soong、Treble、ART和新调试工具建立迁移账本，绝不把第2版新增的虚拟机、安全与Gradle章节倒填进首版。

<Objectives>

- 能解释“第10章 GUI系统之窗口管理员——WMS”全部28个正式节点的入口、线程进程、数据与资源所有权。
- 能实现“从WMS职责、窗口属性与添加流程进入Surface、布局、启动窗口和动画”的最小复现实验并保存版本卡、命令、产物、状态和日志。
- 能区分源码事实、运行证据与版本变化，并用WindowToken、类型层级、LayoutParams、add/relayout、Surface跨进程、窗口帧与动画状态验收。
- 能设计反例推翻“只在应用侧解释窗口，遗漏WMS策略、Token、Surface分配和启动窗口替换时序”，写出首版机制到现代Android的迁移和回退条件。

</Objectives>

## 从版本与断点开始

<Term def="记录AOSP标签、仓库、构建目标、文件路径和提交位置的溯源信息">源码版本卡</Term>、<Term def="从公开入口沿函数、线程、进程和语言边界到实际执行点的有序路径">调用链</Term>、<Term def="规定进程、对象、缓冲区、文件描述符和线程由谁创建、共享与释放的责任">资源所有权</Term>、<Term def="在跨线程、跨进程或跨层边界保存输入输出与身份信息的可复查位置">证据断点</Term>、<Term def="逐项记录首版机制、现代变化、行为差异、验证证据和回退条件的清单">迁移账本</Term>构成本页的分析协议。先预测入口会跨过哪些线程、进程、语言、驱动或硬件抽象边界，再在每个边界保存身份和输入输出；没有标签与证据断点的流程图不能复查。

本单元主线是从WMS职责、窗口属性与添加流程进入Surface、布局、启动窗口和动画。交互管线逐项选择目录节点；反例实验切换基线、错误版本、线程停滞、所有者丢失和数据损坏；证据门要求目录、版本、调用、所有权与迁移全部通过。


## 权威目录逐节点映射

### 第10章 GUI系统之窗口管理员——WMS

**章节合同 1/28。** 将本节点放入“从WMS职责、窗口属性与添加流程进入Surface、布局、启动窗口和动画”主线。先锁定AOSP标签与入口，再记录线程、进程、对象、缓冲区或文件的所有权，最后用WindowToken、类型层级、LayoutParams、add/relayout、Surface跨进程、窗口帧与动画状态验证成功和失败路径。

### 10.1 窗口管理员——WMS综述

**机制边界 2/28。** 将本节点放入“从WMS职责、窗口属性与添加流程进入Surface、布局、启动窗口和动画”主线。先锁定AOSP标签与入口，再记录线程、进程、对象、缓冲区或文件的所有权，最后用WindowToken、类型层级、LayoutParams、add/relayout、Surface跨进程、窗口帧与动画状态验证成功和失败路径。

### 10.1.1 WMS的启动

**源码检查点 3/28。** 将本节点放入“从WMS职责、窗口属性与添加流程进入Surface、布局、启动窗口和动画”主线。先锁定AOSP标签与入口，再记录线程、进程、对象、缓冲区或文件的所有权，最后用WindowToken、类型层级、LayoutParams、add/relayout、Surface跨进程、窗口帧与动画状态验证成功和失败路径。

### 10.1.2 WMS的基础功能

**源码检查点 4/28。** 将本节点放入“从WMS职责、窗口属性与添加流程进入Surface、布局、启动窗口和动画”主线。先锁定AOSP标签与入口，再记录线程、进程、对象、缓冲区或文件的所有权，最后用WindowToken、类型层级、LayoutParams、add/relayout、Surface跨进程、窗口帧与动画状态验证成功和失败路径。

### 10.1.3 WMS的工作方式

**源码检查点 5/28。** 将本节点放入“从WMS职责、窗口属性与添加流程进入Surface、布局、启动窗口和动画”主线。先锁定AOSP标签与入口，再记录线程、进程、对象、缓冲区或文件的所有权，最后用WindowToken、类型层级、LayoutParams、add/relayout、Surface跨进程、窗口帧与动画状态验证成功和失败路径。

### 10.1.4 WMS、AMS与Activity间的联系

**源码检查点 6/28。** 将本节点放入“从WMS职责、窗口属性与添加流程进入Surface、布局、启动窗口和动画”主线。先锁定AOSP标签与入口，再记录线程、进程、对象、缓冲区或文件的所有权，最后用WindowToken、类型层级、LayoutParams、add/relayout、Surface跨进程、窗口帧与动画状态验证成功和失败路径。

### 10.2 窗口属性

**机制边界 7/28。** 将本节点放入“从WMS职责、窗口属性与添加流程进入Surface、布局、启动窗口和动画”主线。先锁定AOSP标签与入口，再记录线程、进程、对象、缓冲区或文件的所有权，最后用WindowToken、类型层级、LayoutParams、add/relayout、Surface跨进程、窗口帧与动画状态验证成功和失败路径。

### 10.2.1 窗口类型与层级

**源码检查点 8/28。** 将本节点放入“从WMS职责、窗口属性与添加流程进入Surface、布局、启动窗口和动画”主线。先锁定AOSP标签与入口，再记录线程、进程、对象、缓冲区或文件的所有权，最后用WindowToken、类型层级、LayoutParams、add/relayout、Surface跨进程、窗口帧与动画状态验证成功和失败路径。

### 10.2.2 窗口策略（Window Policy）

**源码检查点 9/28。** 将本节点放入“从WMS职责、窗口属性与添加流程进入Surface、布局、启动窗口和动画”主线。先锁定AOSP标签与入口，再记录线程、进程、对象、缓冲区或文件的所有权，最后用WindowToken、类型层级、LayoutParams、add/relayout、Surface跨进程、窗口帧与动画状态验证成功和失败路径。

### 10.2.3 窗口属性（LayoutParams）

**源码检查点 10/28。** 将本节点放入“从WMS职责、窗口属性与添加流程进入Surface、布局、启动窗口和动画”主线。先锁定AOSP标签与入口，再记录线程、进程、对象、缓冲区或文件的所有权，最后用WindowToken、类型层级、LayoutParams、add/relayout、Surface跨进程、窗口帧与动画状态验证成功和失败路径。

### 10.3 窗口的添加过程

**机制边界 11/28。** 将本节点放入“从WMS职责、窗口属性与添加流程进入Surface、布局、启动窗口和动画”主线。先锁定AOSP标签与入口，再记录线程、进程、对象、缓冲区或文件的所有权，最后用WindowToken、类型层级、LayoutParams、add/relayout、Surface跨进程、窗口帧与动画状态验证成功和失败路径。

### 10.3.1 系统窗口的添加过程

**源码检查点 12/28。** 将本节点放入“从WMS职责、窗口属性与添加流程进入Surface、布局、启动窗口和动画”主线。先锁定AOSP标签与入口，再记录线程、进程、对象、缓冲区或文件的所有权，最后用WindowToken、类型层级、LayoutParams、add/relayout、Surface跨进程、窗口帧与动画状态验证成功和失败路径。

### 10.3.2 Activity窗口的添加过程

**源码检查点 13/28。** 将本节点放入“从WMS职责、窗口属性与添加流程进入Surface、布局、启动窗口和动画”主线。先锁定AOSP标签与入口，再记录线程、进程、对象、缓冲区或文件的所有权，最后用WindowToken、类型层级、LayoutParams、add/relayout、Surface跨进程、窗口帧与动画状态验证成功和失败路径。

### 10.3.3 窗口添加实例

**源码检查点 14/28。** 将本节点放入“从WMS职责、窗口属性与添加流程进入Surface、布局、启动窗口和动画”主线。先锁定AOSP标签与入口，再记录线程、进程、对象、缓冲区或文件的所有权，最后用WindowToken、类型层级、LayoutParams、add/relayout、Surface跨进程、窗口帧与动画状态验证成功和失败路径。

### 10.4 Surface管理

**机制边界 15/28。** 将本节点放入“从WMS职责、窗口属性与添加流程进入Surface、布局、启动窗口和动画”主线。先锁定AOSP标签与入口，再记录线程、进程、对象、缓冲区或文件的所有权，最后用WindowToken、类型层级、LayoutParams、add/relayout、Surface跨进程、窗口帧与动画状态验证成功和失败路径。

### 10.4.1 Surface申请流程（relayout）

**源码检查点 16/28。** 将本节点放入“从WMS职责、窗口属性与添加流程进入Surface、布局、启动窗口和动画”主线。先锁定AOSP标签与入口，再记录线程、进程、对象、缓冲区或文件的所有权，最后用WindowToken、类型层级、LayoutParams、add/relayout、Surface跨进程、窗口帧与动画状态验证成功和失败路径。

### 10.4.2 Surface的跨进程传递

**源码检查点 17/28。** 将本节点放入“从WMS职责、窗口属性与添加流程进入Surface、布局、启动窗口和动画”主线。先锁定AOSP标签与入口，再记录线程、进程、对象、缓冲区或文件的所有权，最后用WindowToken、类型层级、LayoutParams、add/relayout、Surface跨进程、窗口帧与动画状态验证成功和失败路径。

### 10.4.3 Surface的业务操作

**源码检查点 18/28。** 将本节点放入“从WMS职责、窗口属性与添加流程进入Surface、布局、启动窗口和动画”主线。先锁定AOSP标签与入口，再记录线程、进程、对象、缓冲区或文件的所有权，最后用WindowToken、类型层级、LayoutParams、add/relayout、Surface跨进程、窗口帧与动画状态验证成功和失败路径。

### 10.5 performLayoutAndPlaceSurfacesLockedInner

**机制边界 19/28。** 将本节点放入“从WMS职责、窗口属性与添加流程进入Surface、布局、启动窗口和动画”主线。先锁定AOSP标签与入口，再记录线程、进程、对象、缓冲区或文件的所有权，最后用WindowToken、类型层级、LayoutParams、add/relayout、Surface跨进程、窗口帧与动画状态验证成功和失败路径。

### 10.6 窗口大小的计算过程

**机制边界 20/28。** 将本节点放入“从WMS职责、窗口属性与添加流程进入Surface、布局、启动窗口和动画”主线。先锁定AOSP标签与入口，再记录线程、进程、对象、缓冲区或文件的所有权，最后用WindowToken、类型层级、LayoutParams、add/relayout、Surface跨进程、窗口帧与动画状态验证成功和失败路径。

### 10.7 启动窗口的添加与销毁

**机制边界 21/28。** 将本节点放入“从WMS职责、窗口属性与添加流程进入Surface、布局、启动窗口和动画”主线。先锁定AOSP标签与入口，再记录线程、进程、对象、缓冲区或文件的所有权，最后用WindowToken、类型层级、LayoutParams、add/relayout、Surface跨进程、窗口帧与动画状态验证成功和失败路径。

### 10.7.1 启动窗口的添加

**源码检查点 22/28。** 将本节点放入“从WMS职责、窗口属性与添加流程进入Surface、布局、启动窗口和动画”主线。先锁定AOSP标签与入口，再记录线程、进程、对象、缓冲区或文件的所有权，最后用WindowToken、类型层级、LayoutParams、add/relayout、Surface跨进程、窗口帧与动画状态验证成功和失败路径。

### 10.7.2 启动窗口的销毁

**源码检查点 23/28。** 将本节点放入“从WMS职责、窗口属性与添加流程进入Surface、布局、启动窗口和动画”主线。先锁定AOSP标签与入口，再记录线程、进程、对象、缓冲区或文件的所有权，最后用WindowToken、类型层级、LayoutParams、add/relayout、Surface跨进程、窗口帧与动画状态验证成功和失败路径。

### 10.8 窗口动画

**机制边界 24/28。** 将本节点放入“从WMS职责、窗口属性与添加流程进入Surface、布局、启动窗口和动画”主线。先锁定AOSP标签与入口，再记录线程、进程、对象、缓冲区或文件的所有权，最后用WindowToken、类型层级、LayoutParams、add/relayout、Surface跨进程、窗口帧与动画状态验证成功和失败路径。

### 10.8.1 窗口动画类型

**源码检查点 25/28。** 将本节点放入“从WMS职责、窗口属性与添加流程进入Surface、布局、启动窗口和动画”主线。先锁定AOSP标签与入口，再记录线程、进程、对象、缓冲区或文件的所有权，最后用WindowToken、类型层级、LayoutParams、add/relayout、Surface跨进程、窗口帧与动画状态验证成功和失败路径。

### 10.8.2 动画流程跟踪——WindowStateAnimator

**源码检查点 26/28。** 将本节点放入“从WMS职责、窗口属性与添加流程进入Surface、布局、启动窗口和动画”主线。先锁定AOSP标签与入口，再记录线程、进程、对象、缓冲区或文件的所有权，最后用WindowToken、类型层级、LayoutParams、add/relayout、Surface跨进程、窗口帧与动画状态验证成功和失败路径。

### 10.8.3 AppWindowAnimator

**源码检查点 27/28。** 将本节点放入“从WMS职责、窗口属性与添加流程进入Surface、布局、启动窗口和动画”主线。先锁定AOSP标签与入口，再记录线程、进程、对象、缓冲区或文件的所有权，最后用WindowToken、类型层级、LayoutParams、add/relayout、Surface跨进程、窗口帧与动画状态验证成功和失败路径。

### 10.8.4 动画的执行过程

**源码检查点 28/28。** 将本节点放入“从WMS职责、窗口属性与添加流程进入Surface、布局、启动窗口和动画”主线。先锁定AOSP标签与入口，再记录线程、进程、对象、缓冲区或文件的所有权，最后用WindowToken、类型层级、LayoutParams、add/relayout、Surface跨进程、窗口帧与动画状态验证成功和失败路径。

<Callout type="trap">**核心陷阱。** 只在应用侧解释窗口，遗漏WMS策略、Token、Surface分配和启动窗口替换时序。修复先恢复版本、入口、边界与所有权证据。</Callout>

<Callout type="trap">**版次错位。** 首版只有22章。第2版新增Android虚拟机、安全机制和IDE/Gradle等内容只能列入版本差异，不能伪装成首版目录。</Callout>

<Callout type="warning">刷机、Root、串口和系统映像实验仅在可恢复测试设备或模拟器上进行。日志、媒体、系统分区与密钥不得来自真实用户设备。</Callout>

## 证据解释

第一份证据是版本证据：AOSP标签、repo manifest、主机工具链、产品目标和产物哈希必须同时保存。Android源码长期重构，同名类和服务在不同分支可能承担不同职责；跨版本拼接得到的“完整流程”实际上无法编译，也无法在单一设备上运行。

第二份证据是调用证据。针对“从WMS职责、窗口属性与添加流程进入Surface、布局、启动窗口和动画”，从真实公开入口前向追踪，每次跨线程、跨进程、跨Java/Native、跨HAL/驱动边界都记录双方、数据形态、事务或函数编号和错误返回。WindowToken、类型层级、LayoutParams、add/relayout、Surface跨进程、窗口帧与动画状态要能让另一位读者在相同标签上停在相同断点。

第三份证据是所有权证据。成功路径之外，主动让调用方退出、服务重启、缓冲区耗尽、输入损坏或超时发生，观察对象、引用计数、文件描述符、线程和映射是否释放。只画“谁调用谁”而不回答“谁负责释放”，无法解释系统泄漏、死锁和迟到回调。

第四份证据是迁移对照。现代AOSP可能以Soong、Treble、AIDL稳定接口、新渲染链或新调试工具替代首版机制。迁移时保持功能结果和故障样本不变，一次只替换一层；若线程、错误、性能或资源语义变化，先记录差异和回退，再更新结论。

```text
历史节点: 第10章 GUI系统之窗口管理员——WMS
首版基线: Android 4.3 / 2014年第1版
现代候选: 待当前AOSP标签验证
不变量: 功能、线程进程、错误、资源释放
证据: WindowToken、类型层级、LayoutParams、add/relayout、Surface跨进程、窗口帧与动画状态
回退: 任一不变量失败即恢复首版路径
```

## 本章回顾

从一次真实输入开始，依次回答源码属于哪个标签、入口在哪里、跨过哪些边界、每层保存什么状态、错误如何返回、资源由谁释放。再用WindowToken、类型层级、LayoutParams、add/relayout、Surface跨进程、窗口帧与动画状态重放；无法在单一版本复现的结论退回研究阶段。

## 复习与独立交接

<Exercises>
**问题 1**：为什么必须保留28个正式节点？
<Answer>这些节点共同组成“从WMS职责、窗口属性与添加流程进入Surface、布局、启动窗口和动画”的完整入口、机制、边界与证据链，删去节点会造成调用或所有权断裂。</Answer>

**问题 2**：最小正确性合同是什么？
<Answer>同一源码标签、构建目标和输入下，功能结果、线程进程、错误与资源释放必须确定且可重放。</Answer>

**问题 3**：怎样构造有效反例？
<Answer>主动让“只在应用侧解释窗口，遗漏WMS策略、Token、Surface分配和启动窗口替换时序”成立，再注入超时、服务退出、数据损坏或版本错配；出现死锁、泄漏、错位或不可复现即失败。</Answer>

**问题 4**：为什么类名和静态流程图不够？
<Answer>它们不能证明标签、运行入口、线程进程、对象身份、错误路径和释放结果，必须结合构建产物与运行日志。</Answer>

**问题 5**：怎样迁移而不改写首版？
<Answer>先保存Android 4.3首版行为基线，再一次替换一层，用相同输入和故障测试比较并记录差异与回退。</Answer>

**问题 6**：独立交接需要什么？
<Answer>需要源码版本卡、完整目录映射、构建命令与产物、WindowToken、类型层级、LayoutParams、add/relayout、Surface跨进程、窗口帧与动画状态、失败实验、迁移账本和回退条件。</Answer>
</Exercises>

<Glossary>
  <GlossaryItem term="源码版本卡">记录AOSP标签、仓库、构建目标、文件路径和提交位置的溯源信息。</GlossaryItem>
  <GlossaryItem term="调用链">从公开入口沿函数、线程、进程和语言边界到实际执行点的有序路径。</GlossaryItem>
  <GlossaryItem term="资源所有权">规定进程、对象、缓冲区、文件描述符和线程由谁创建、共享与释放的责任。</GlossaryItem>
  <GlossaryItem term="证据断点">在跨线程、跨进程或跨层边界保存输入输出与身份信息的可复查位置。</GlossaryItem>
  <GlossaryItem term="迁移账本">逐项记录首版机制、现代变化、行为差异、验证证据和回退条件的清单。</GlossaryItem>
</Glossary>

[← 上一页：第9章 GUI系统之SurfaceFlinger](/learn/deep-android-kernel/09-surfaceflinger/dak-14-09-surfaceflinger) · [下一页：第11章 GUI系统之View体系 →](/learn/deep-android-kernel/11-view/dak-14-11-view)

<Attribution adaptedFrom="林学森《深入理解Android内核设计思想》" adaptedUrl="https://www.yami.com/zh/p/android/3118057821" />
