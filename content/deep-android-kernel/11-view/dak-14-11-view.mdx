---
title: "第11章 GUI系统之View体系"
description: "依据2014年首版完整目录覆盖21个节点：追踪View Tree创建、WMS注册、ViewRoot遍历、属性、Canvas、draw、触摸投递和动画"
type: C
section: "深入理解Android内核设计思想（第1版 / Android 4.3）"
draft: false
math: false
sourceUrl: "https://www.yami.com/zh/p/android/3118057821"
demo: true
order: 0
---

import { Dak14PipelineLab, Dak14ExperimentLab, Dak14EvidenceLab } from "@/components/mdx/deep-android-kernel/diagrams/dak-14-11-view";

# 第11章 GUI系统之View体系

本页依据林学森《深入理解Android内核设计思想》独立重构，不复制原文。版本锁定人民邮电出版社2014年5月首版、687页、ISBN 9787115348418，面向Android 4.3以上；全书为4篇22章、323个章/节/小节节点。

课程先复现首版Make构建、Binder、SurfaceFlinger、WMS、View、AudioFlinger、Ant与旧工具链，再用现代AOSP/Soong、Treble、ART和新调试工具建立迁移账本，绝不把第2版新增的虚拟机、安全与Gradle章节倒填进首版。

<Objectives>

- 能解释“第11章 GUI系统之View体系”全部21个正式节点的入口、线程进程、数据与资源所有权。
- 能实现“追踪View Tree创建、WMS注册、ViewRoot遍历、属性、Canvas、draw、触摸投递和动画”的最小复现实验并保存版本卡、命令、产物、状态和日志。
- 能区分源码事实、运行证据与版本变化，并用DecorView树、ViewRootImpl、traversal调度、measure/layout/draw、Canvas提交与TouchEvent路径验收。
- 能设计反例推翻“只看View.onDraw，忽略窗口注册、遍历触发、父子测量、Surface提交和事件消费”，写出首版机制到现代Android的迁移和回退条件。

</Objectives>

## 从版本与断点开始

<Term def="记录AOSP标签、仓库、构建目标、文件路径和提交位置的溯源信息">源码版本卡</Term>、<Term def="从公开入口沿函数、线程、进程和语言边界到实际执行点的有序路径">调用链</Term>、<Term def="规定进程、对象、缓冲区、文件描述符和线程由谁创建、共享与释放的责任">资源所有权</Term>、<Term def="在跨线程、跨进程或跨层边界保存输入输出与身份信息的可复查位置">证据断点</Term>、<Term def="逐项记录首版机制、现代变化、行为差异、验证证据和回退条件的清单">迁移账本</Term>构成本页的分析协议。先预测入口会跨过哪些线程、进程、语言、驱动或硬件抽象边界，再在每个边界保存身份和输入输出；没有标签与证据断点的流程图不能复查。

本单元主线是追踪View Tree创建、WMS注册、ViewRoot遍历、属性、Canvas、draw、触摸投递和动画。交互管线逐项选择目录节点；反例实验切换基线、错误版本、线程停滞、所有者丢失和数据损坏；证据门要求目录、版本、调用、所有权与迁移全部通过。

<Dak14PipelineLab />

## 权威目录逐节点映射

### 第11章 让你的界面炫彩起来的GUI系统之View体系

**章节合同 1/21。** 将本节点放入“追踪View Tree创建、WMS注册、ViewRoot遍历、属性、Canvas、draw、触摸投递和动画”主线。先锁定AOSP标签与入口，再记录线程、进程、对象、缓冲区或文件的所有权，最后用DecorView树、ViewRootImpl、traversal调度、measure/layout/draw、Canvas提交与TouchEvent路径验证成功和失败路径。

### 11.1 应用程序中的View框架

**机制边界 2/21。** 将本节点放入“追踪View Tree创建、WMS注册、ViewRoot遍历、属性、Canvas、draw、触摸投递和动画”主线。先锁定AOSP标签与入口，再记录线程、进程、对象、缓冲区或文件的所有权，最后用DecorView树、ViewRootImpl、traversal调度、measure/layout/draw、Canvas提交与TouchEvent路径验证成功和失败路径。

### 11.2 Activity中View Tree的创建过程

**机制边界 3/21。** 将本节点放入“追踪View Tree创建、WMS注册、ViewRoot遍历、属性、Canvas、draw、触摸投递和动画”主线。先锁定AOSP标签与入口，再记录线程、进程、对象、缓冲区或文件的所有权，最后用DecorView树、ViewRootImpl、traversal调度、measure/layout/draw、Canvas提交与TouchEvent路径验证成功和失败路径。

### 11.3 在WMS中注册窗口

**机制边界 4/21。** 将本节点放入“追踪View Tree创建、WMS注册、ViewRoot遍历、属性、Canvas、draw、触摸投递和动画”主线。先锁定AOSP标签与入口，再记录线程、进程、对象、缓冲区或文件的所有权，最后用DecorView树、ViewRootImpl、traversal调度、measure/layout/draw、Canvas提交与TouchEvent路径验证成功和失败路径。

### 11.4 ViewRoot的基本工作方式

**机制边界 5/21。** 将本节点放入“追踪View Tree创建、WMS注册、ViewRoot遍历、属性、Canvas、draw、触摸投递和动画”主线。先锁定AOSP标签与入口，再记录线程、进程、对象、缓冲区或文件的所有权，最后用DecorView树、ViewRootImpl、traversal调度、measure/layout/draw、Canvas提交与TouchEvent路径验证成功和失败路径。

### 11.5 View Tree的遍历时机

**机制边界 6/21。** 将本节点放入“追踪View Tree创建、WMS注册、ViewRoot遍历、属性、Canvas、draw、触摸投递和动画”主线。先锁定AOSP标签与入口，再记录线程、进程、对象、缓冲区或文件的所有权，最后用DecorView树、ViewRootImpl、traversal调度、measure/layout/draw、Canvas提交与TouchEvent路径验证成功和失败路径。

### 11.6 View Tree的遍历流程

**机制边界 7/21。** 将本节点放入“追踪View Tree创建、WMS注册、ViewRoot遍历、属性、Canvas、draw、触摸投递和动画”主线。先锁定AOSP标签与入口，再记录线程、进程、对象、缓冲区或文件的所有权，最后用DecorView树、ViewRootImpl、traversal调度、measure/layout/draw、Canvas提交与TouchEvent路径验证成功和失败路径。

### 11.7 View和ViewGroup属性

**机制边界 8/21。** 将本节点放入“追踪View Tree创建、WMS注册、ViewRoot遍历、属性、Canvas、draw、触摸投递和动画”主线。先锁定AOSP标签与入口，再记录线程、进程、对象、缓冲区或文件的所有权，最后用DecorView树、ViewRootImpl、traversal调度、measure/layout/draw、Canvas提交与TouchEvent路径验证成功和失败路径。

### 11.7.1 View的基本属性

**源码检查点 9/21。** 将本节点放入“追踪View Tree创建、WMS注册、ViewRoot遍历、属性、Canvas、draw、触摸投递和动画”主线。先锁定AOSP标签与入口，再记录线程、进程、对象、缓冲区或文件的所有权，最后用DecorView树、ViewRootImpl、traversal调度、measure/layout/draw、Canvas提交与TouchEvent路径验证成功和失败路径。

### 11.7.2 ViewGroup的属性

**源码检查点 10/21。** 将本节点放入“追踪View Tree创建、WMS注册、ViewRoot遍历、属性、Canvas、draw、触摸投递和动画”主线。先锁定AOSP标签与入口，再记录线程、进程、对象、缓冲区或文件的所有权，最后用DecorView树、ViewRootImpl、traversal调度、measure/layout/draw、Canvas提交与TouchEvent路径验证成功和失败路径。

### 11.7.3 View、ViewGroup和ViewParent

**源码检查点 11/21。** 将本节点放入“追踪View Tree创建、WMS注册、ViewRoot遍历、属性、Canvas、draw、触摸投递和动画”主线。先锁定AOSP标签与入口，再记录线程、进程、对象、缓冲区或文件的所有权，最后用DecorView树、ViewRootImpl、traversal调度、measure/layout/draw、Canvas提交与TouchEvent路径验证成功和失败路径。

### 11.7.4 Callback接口

**源码检查点 12/21。** 将本节点放入“追踪View Tree创建、WMS注册、ViewRoot遍历、属性、Canvas、draw、触摸投递和动画”主线。先锁定AOSP标签与入口，再记录线程、进程、对象、缓冲区或文件的所有权，最后用DecorView树、ViewRootImpl、traversal调度、measure/layout/draw、Canvas提交与TouchEvent路径验证成功和失败路径。

### 11.8 作画工具集——Canvas

**机制边界 13/21。** 将本节点放入“追踪View Tree创建、WMS注册、ViewRoot遍历、属性、Canvas、draw、触摸投递和动画”主线。先锁定AOSP标签与入口，再记录线程、进程、对象、缓冲区或文件的所有权，最后用DecorView树、ViewRootImpl、traversal调度、measure/layout/draw、Canvas提交与TouchEvent路径验证成功和失败路径。

### 11.8.1 绘制UI——Skia

**源码检查点 14/21。** 将本节点放入“追踪View Tree创建、WMS注册、ViewRoot遍历、属性、Canvas、draw、触摸投递和动画”主线。先锁定AOSP标签与入口，再记录线程、进程、对象、缓冲区或文件的所有权，最后用DecorView树、ViewRootImpl、traversal调度、measure/layout/draw、Canvas提交与TouchEvent路径验证成功和失败路径。

### 11.8.2 数据中介——Surface.lockCanvas

**源码检查点 15/21。** 将本节点放入“追踪View Tree创建、WMS注册、ViewRoot遍历、属性、Canvas、draw、触摸投递和动画”主线。先锁定AOSP标签与入口，再记录线程、进程、对象、缓冲区或文件的所有权，最后用DecorView树、ViewRootImpl、traversal调度、measure/layout/draw、Canvas提交与TouchEvent路径验证成功和失败路径。

### 11.8.3 解锁并提交结果——unlockCanvasAndPost

**源码检查点 16/21。** 将本节点放入“追踪View Tree创建、WMS注册、ViewRoot遍历、属性、Canvas、draw、触摸投递和动画”主线。先锁定AOSP标签与入口，再记录线程、进程、对象、缓冲区或文件的所有权，最后用DecorView树、ViewRootImpl、traversal调度、measure/layout/draw、Canvas提交与TouchEvent路径验证成功和失败路径。

### 11.9 draw和onDraw

**机制边界 17/21。** 将本节点放入“追踪View Tree创建、WMS注册、ViewRoot遍历、属性、Canvas、draw、触摸投递和动画”主线。先锁定AOSP标签与入口，再记录线程、进程、对象、缓冲区或文件的所有权，最后用DecorView树、ViewRootImpl、traversal调度、measure/layout/draw、Canvas提交与TouchEvent路径验证成功和失败路径。

### 11.10 View中的消息传递

**机制边界 18/21。** 将本节点放入“追踪View Tree创建、WMS注册、ViewRoot遍历、属性、Canvas、draw、触摸投递和动画”主线。先锁定AOSP标签与入口，再记录线程、进程、对象、缓冲区或文件的所有权，最后用DecorView树、ViewRootImpl、traversal调度、measure/layout/draw、Canvas提交与TouchEvent路径验证成功和失败路径。

### 11.10.1 View中TouchEvent的投递流程

**源码检查点 19/21。** 将本节点放入“追踪View Tree创建、WMS注册、ViewRoot遍历、属性、Canvas、draw、触摸投递和动画”主线。先锁定AOSP标签与入口，再记录线程、进程、对象、缓冲区或文件的所有权，最后用DecorView树、ViewRootImpl、traversal调度、measure/layout/draw、Canvas提交与TouchEvent路径验证成功和失败路径。

### 11.10.2 ViewGroup中TouchEvent的投递流程

**源码检查点 20/21。** 将本节点放入“追踪View Tree创建、WMS注册、ViewRoot遍历、属性、Canvas、draw、触摸投递和动画”主线。先锁定AOSP标签与入口，再记录线程、进程、对象、缓冲区或文件的所有权，最后用DecorView树、ViewRootImpl、traversal调度、measure/layout/draw、Canvas提交与TouchEvent路径验证成功和失败路径。

### 11.11 View动画

**机制边界 21/21。** 将本节点放入“追踪View Tree创建、WMS注册、ViewRoot遍历、属性、Canvas、draw、触摸投递和动画”主线。先锁定AOSP标签与入口，再记录线程、进程、对象、缓冲区或文件的所有权，最后用DecorView树、ViewRootImpl、traversal调度、measure/layout/draw、Canvas提交与TouchEvent路径验证成功和失败路径。

## 最小复现实验

```bash
repo init -u https://android.googlesource.com/platform/manifest -b android-4.3_r1
repo sync -c
source build/envsetup.sh
lunch aosp_arm-eng
# 为“第11章 让你的界面炫彩起来的GUI系统之View体系”保存入口、进程、线程、对象和失败日志
```

先固定源码标签、主机工具链、产品目标和设备映像，再执行本章最小路径。每次实验只改变一个输入，输出中至少包含进程/线程身份、关键对象或缓冲区、返回值和释放结果。

```text
checkpoint=dak-14-11-view
source_tag=android-4.3_r1
entry=第11章 让你的界面炫彩起来的GUI系统之View体系
owner=待实测
success_evidence=DecorView树、ViewRootImpl、traversal调度、measure/layout/draw、Canvas提交与TouchEvent路径
failure=主动注入超时、损坏输入或所有者退出
```

## 三步复现实验

<Stepper>
  <Step title="1. 锁定版本并画边界">逐个连接21个目录节点，标出仓库、文件、入口、线程进程与资源所有者；先预测失败断点。<Dak14PipelineLab /></Step>
  <Step title="2. 控制变量注入故障">切换基线、错误版本、线程停滞、所有者丢失或数据损坏，一次只改变一个条件。<Dak14ExperimentLab /></Step>
  <Step title="3. 通过源码证据门">用DecorView树、ViewRootImpl、traversal调度、measure/layout/draw、Canvas提交与TouchEvent路径核对目录、版本、调用、所有权和迁移；缺项时不得交付流程结论。<Dak14EvidenceLab /></Step>
</Stepper>

<Callout type="trap">**核心陷阱。** 只看View.onDraw，忽略窗口注册、遍历触发、父子测量、Surface提交和事件消费。修复先恢复版本、入口、边界与所有权证据。</Callout>

<Callout type="trap">**版次错位。** 首版只有22章。第2版新增Android虚拟机、安全机制和IDE/Gradle等内容只能列入版本差异，不能伪装成首版目录。</Callout>

<Callout type="warning">刷机、Root、串口和系统映像实验仅在可恢复测试设备或模拟器上进行。日志、媒体、系统分区与密钥不得来自真实用户设备。</Callout>

## 证据解释

第一份证据是版本证据：AOSP标签、repo manifest、主机工具链、产品目标和产物哈希必须同时保存。Android源码长期重构，同名类和服务在不同分支可能承担不同职责；跨版本拼接得到的“完整流程”实际上无法编译，也无法在单一设备上运行。

第二份证据是调用证据。针对“追踪View Tree创建、WMS注册、ViewRoot遍历、属性、Canvas、draw、触摸投递和动画”，从真实公开入口前向追踪，每次跨线程、跨进程、跨Java/Native、跨HAL/驱动边界都记录双方、数据形态、事务或函数编号和错误返回。DecorView树、ViewRootImpl、traversal调度、measure/layout/draw、Canvas提交与TouchEvent路径要能让另一位读者在相同标签上停在相同断点。

第三份证据是所有权证据。成功路径之外，主动让调用方退出、服务重启、缓冲区耗尽、输入损坏或超时发生，观察对象、引用计数、文件描述符、线程和映射是否释放。只画“谁调用谁”而不回答“谁负责释放”，无法解释系统泄漏、死锁和迟到回调。

第四份证据是迁移对照。现代AOSP可能以Soong、Treble、AIDL稳定接口、新渲染链或新调试工具替代首版机制。迁移时保持功能结果和故障样本不变，一次只替换一层；若线程、错误、性能或资源语义变化，先记录差异和回退，再更新结论。

```text
历史节点: 第11章 让你的界面炫彩起来的GUI系统之View体系
首版基线: Android 4.3 / 2014年第1版
现代候选: 待当前AOSP标签验证
不变量: 功能、线程进程、错误、资源释放
证据: DecorView树、ViewRootImpl、traversal调度、measure/layout/draw、Canvas提交与TouchEvent路径
回退: 任一不变量失败即恢复首版路径
```

## 本章回顾

从一次真实输入开始，依次回答源码属于哪个标签、入口在哪里、跨过哪些边界、每层保存什么状态、错误如何返回、资源由谁释放。再用DecorView树、ViewRootImpl、traversal调度、measure/layout/draw、Canvas提交与TouchEvent路径重放；无法在单一版本复现的结论退回研究阶段。

## 复习与独立交接

<Exercises>
**问题 1**：为什么必须保留21个正式节点？
<Answer>这些节点共同组成“追踪View Tree创建、WMS注册、ViewRoot遍历、属性、Canvas、draw、触摸投递和动画”的完整入口、机制、边界与证据链，删去节点会造成调用或所有权断裂。</Answer>

**问题 2**：最小正确性合同是什么？
<Answer>同一源码标签、构建目标和输入下，功能结果、线程进程、错误与资源释放必须确定且可重放。</Answer>

**问题 3**：怎样构造有效反例？
<Answer>主动让“只看View.onDraw，忽略窗口注册、遍历触发、父子测量、Surface提交和事件消费”成立，再注入超时、服务退出、数据损坏或版本错配；出现死锁、泄漏、错位或不可复现即失败。</Answer>

**问题 4**：为什么类名和静态流程图不够？
<Answer>它们不能证明标签、运行入口、线程进程、对象身份、错误路径和释放结果，必须结合构建产物与运行日志。</Answer>

**问题 5**：怎样迁移而不改写首版？
<Answer>先保存Android 4.3首版行为基线，再一次替换一层，用相同输入和故障测试比较并记录差异与回退。</Answer>

**问题 6**：独立交接需要什么？
<Answer>需要源码版本卡、完整目录映射、构建命令与产物、DecorView树、ViewRootImpl、traversal调度、measure/layout/draw、Canvas提交与TouchEvent路径、失败实验、迁移账本和回退条件。</Answer>
</Exercises>

<Glossary>
  <GlossaryItem term="源码版本卡">记录AOSP标签、仓库、构建目标、文件路径和提交位置的溯源信息。</GlossaryItem>
  <GlossaryItem term="调用链">从公开入口沿函数、线程、进程和语言边界到实际执行点的有序路径。</GlossaryItem>
  <GlossaryItem term="资源所有权">规定进程、对象、缓冲区、文件描述符和线程由谁创建、共享与释放的责任。</GlossaryItem>
  <GlossaryItem term="证据断点">在跨线程、跨进程或跨层边界保存输入输出与身份信息的可复查位置。</GlossaryItem>
  <GlossaryItem term="迁移账本">逐项记录首版机制、现代变化、行为差异、验证证据和回退条件的清单。</GlossaryItem>
</Glossary>

[← 上一页：第10章 GUI系统之窗口管理员——WMS](/learn/deep-android-kernel/10-wms/dak-14-10-wms) · [下一页：第12章 InputManagerService与输入事件 →](/learn/deep-android-kernel/12-input/dak-14-12-input)

<Attribution adaptedFrom="林学森《深入理解Android内核设计思想》" adaptedUrl="https://www.yami.com/zh/p/android/3118057821" />
