---
title: 卷I 第2章 深入理解JNI
description: >-
  卷I 第2章
  深入理解JNI：以MediaScanner贯通Java声明、JNI注册、类型转换、回调与异常边界，以android-2.2_r1源码、全节点机制图和故障恢复证据完成验收。
type: C
section: 深入Android三卷 · 卷I Native基础
draft: false
math: false
sourceUrl: "https://hzbook-group.iteye.com/group/wiki/3128-Android-internals"
demo: true
order: 2
qualityVersion: 2
practiceMode: diagnosis
sourceMode: independent-rewrite
---


{/* DEEP_ANDROID_VOLUMES_QUALITY_V2 */}

## 从一个可失败的问题开始

卷I 第2章 深入理解JNI不是类名清单，而是在android-2.2_r1中回答：以MediaScanner贯通Java声明、JNI注册、类型转换、回调与异常边界。先预测请求经过的线程、进程与对象所有者，再运行正常样本；如果库加载日志、JNINativeMethod表、JNIEnv线程身份、局部/全局引用和异常状态不能指出首个变化，本页结论仍不可交接。

本章不变量是在android-2.2_r1固定输入下，以MediaScanner贯通Java声明、JNI注册、类型转换、回调与异常边界的入口身份、状态拥有者、成功结果、错误返回和资源释放均可重复观察。将“只记住函数命名规则，忽略线程附着、引用寿命与异常清理”作为反例，必须在同一版本、同一输入下看到确定的错误或状态分叉，并能恢复到基线。

## 来源范围、历史标签与当前迁移

卷I 第2章 深入理解JNI以[卷I出版社合法公开样章](https://hzbook-group.iteye.com/group/wiki/3128-Android-internals)核定可见正文、目录与Android 2.2范围；样章以外不宣称复现原书正文。

[AOSP版本与构建号表](https://source.android.com/docs/setup/reference/build-numbers)用于核对2.2/API 8、4.0.1/API 14、4.2.2/API 17及正式源码标签；[android-2.2_r1源码树](https://android.googlesource.com/platform/frameworks/base/+/android-2.2_r1/)提供本页历史机制的一手坐标。当前[Android架构文档](https://source.android.com/docs/core/architecture)只用于迁移对照，ART、Treble、稳定AIDL、模块化系统服务和现代构建工具不得倒填为三卷历史实现。

## 六个机制检查点

、、、、、。
这六项把以MediaScanner贯通Java声明、JNI注册、类型转换、回调与异常边界落到库加载日志、JNINativeMethod表、JNIEnv线程身份、局部/全局引用和异常状态，防止目录词出现却没有运行解释。

## 正式目录逐项深读

### 第2章 深入理解JNI

第2章 深入理解JNI在“以MediaScanner贯通Java声明、JNI注册、类型转换、回调与异常边界”中的责任要翻译成一个可推翻的运行合同：明确入口、线程或进程、状态拥有者、正常结果和首个错误返回，再由库加载日志、JNINativeMethod表、JNIEnv线程身份、局部/全局引用和异常状态复核。

处理 第2章 深入理解JNI 时先执行“锁定历史基线”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“复现正常轨迹”后仅改变一个条件，观察库加载日志、JNINativeMethod表、JNIEnv线程身份、局部/全局引用和异常状态中哪一项先偏离。

该节点的四级证据是：保留 第2章 深入理解JNI 这一目录坐标；解释它如何参与以MediaScanner贯通Java声明、JNI注册、类型转换、回调与异常边界；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭库加载日志、JNINativeMethod表、JNIEnv线程身份、局部/全局引用和异常状态恢复同输入结果。

### 2.1 JNI概述

2.1 JNI概述在“以MediaScanner贯通Java声明、JNI注册、类型转换、回调与异常边界”中的责任是界定分析分母和结论边界。先列出它包含的入口、对象和退出条件，再用库加载日志、JNINativeMethod表、JNIEnv线程身份、局部/全局引用和异常状态排除只凭类名或流程图得出的结论。

处理 2.1 JNI概述 时先执行“复现正常轨迹”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“注入单一故障”后仅改变一个条件，观察库加载日志、JNINativeMethod表、JNIEnv线程身份、局部/全局引用和异常状态中哪一项先偏离。

该节点的四级证据是：保留 2.1 JNI概述 这一目录坐标；解释它如何参与以MediaScanner贯通Java声明、JNI注册、类型转换、回调与异常边界；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭库加载日志、JNINativeMethod表、JNIEnv线程身份、局部/全局引用和异常状态恢复同输入结果。

### 2.2 学习JNI的实例：MediaScanner

2.2 学习JNI的实例：MediaScanner在“以MediaScanner贯通Java声明、JNI注册、类型转换、回调与异常边界”中的责任要追踪数据从入口到持久化或观察者回调的完整闭环。固定URI、文件或账户样本，记录事务、远端资源、通知与关闭结果，再用重复输入验证幂等和泄漏。

处理 2.2 学习JNI的实例：MediaScanner 时先执行“注入单一故障”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“定位首个分叉”后仅改变一个条件，观察库加载日志、JNINativeMethod表、JNIEnv线程身份、局部/全局引用和异常状态中哪一项先偏离。

该节点的四级证据是：保留 2.2 学习JNI的实例：MediaScanner 这一目录坐标；解释它如何参与以MediaScanner贯通Java声明、JNI注册、类型转换、回调与异常边界；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭库加载日志、JNINativeMethod表、JNIEnv线程身份、局部/全局引用和异常状态恢复同输入结果。

### 2.3 Java层的MediaScanner分析

2.3 Java层的MediaScanner分析在“以MediaScanner贯通Java声明、JNI注册、类型转换、回调与异常边界”中的责任要追踪数据从入口到持久化或观察者回调的完整闭环。固定URI、文件或账户样本，记录事务、远端资源、通知与关闭结果，再用重复输入验证幂等和泄漏。

处理 2.3 Java层的MediaScanner分析 时先执行“定位首个分叉”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“恢复同输入重放”后仅改变一个条件，观察库加载日志、JNINativeMethod表、JNIEnv线程身份、局部/全局引用和异常状态中哪一项先偏离。

该节点的四级证据是：保留 2.3 Java层的MediaScanner分析 这一目录坐标；解释它如何参与以MediaScanner贯通Java声明、JNI注册、类型转换、回调与异常边界；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭库加载日志、JNINativeMethod表、JNIEnv线程身份、局部/全局引用和异常状态恢复同输入结果。

### 2.3.1 加载JNI库

2.3.1 加载JNI库在“以MediaScanner贯通Java声明、JNI注册、类型转换、回调与异常边界”中的责任要翻译成一个可推翻的运行合同：明确入口、线程或进程、状态拥有者、正常结果和首个错误返回，再由库加载日志、JNINativeMethod表、JNIEnv线程身份、局部/全局引用和异常状态复核。

处理 2.3.1 加载JNI库 时先执行“恢复同输入重放”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“锁定历史基线”后仅改变一个条件，观察库加载日志、JNINativeMethod表、JNIEnv线程身份、局部/全局引用和异常状态中哪一项先偏离。

该节点的四级证据是：保留 2.3.1 加载JNI库 这一目录坐标；解释它如何参与以MediaScanner贯通Java声明、JNI注册、类型转换、回调与异常边界；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭库加载日志、JNINativeMethod表、JNIEnv线程身份、局部/全局引用和异常状态恢复同输入结果。

### 2.3.2 Java的native函数和总结

2.3.2 Java的native函数和总结在“以MediaScanner贯通Java声明、JNI注册、类型转换、回调与异常边界”中的责任是界定分析分母和结论边界。先列出它包含的入口、对象和退出条件，再用库加载日志、JNINativeMethod表、JNIEnv线程身份、局部/全局引用和异常状态排除只凭类名或流程图得出的结论。

处理 2.3.2 Java的native函数和总结 时先执行“锁定历史基线”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“复现正常轨迹”后仅改变一个条件，观察库加载日志、JNINativeMethod表、JNIEnv线程身份、局部/全局引用和异常状态中哪一项先偏离。

该节点的四级证据是：保留 2.3.2 Java的native函数和总结 这一目录坐标；解释它如何参与以MediaScanner贯通Java声明、JNI注册、类型转换、回调与异常边界；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭库加载日志、JNINativeMethod表、JNIEnv线程身份、局部/全局引用和异常状态恢复同输入结果。

### 2.4 JNI层MediaScanner的分析

2.4 JNI层MediaScanner的分析在“以MediaScanner贯通Java声明、JNI注册、类型转换、回调与异常边界”中的责任要追踪数据从入口到持久化或观察者回调的完整闭环。固定URI、文件或账户样本，记录事务、远端资源、通知与关闭结果，再用重复输入验证幂等和泄漏。

处理 2.4 JNI层MediaScanner的分析 时先执行“复现正常轨迹”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“注入单一故障”后仅改变一个条件，观察库加载日志、JNINativeMethod表、JNIEnv线程身份、局部/全局引用和异常状态中哪一项先偏离。

该节点的四级证据是：保留 2.4 JNI层MediaScanner的分析 这一目录坐标；解释它如何参与以MediaScanner贯通Java声明、JNI注册、类型转换、回调与异常边界；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭库加载日志、JNINativeMethod表、JNIEnv线程身份、局部/全局引用和异常状态恢复同输入结果。

### 2.4.1 注册JNI函数

2.4.1 注册JNI函数在“以MediaScanner贯通Java声明、JNI注册、类型转换、回调与异常边界”中的责任要翻译成一个可推翻的运行合同：明确入口、线程或进程、状态拥有者、正常结果和首个错误返回，再由库加载日志、JNINativeMethod表、JNIEnv线程身份、局部/全局引用和异常状态复核。

处理 2.4.1 注册JNI函数 时先执行“注入单一故障”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“定位首个分叉”后仅改变一个条件，观察库加载日志、JNINativeMethod表、JNIEnv线程身份、局部/全局引用和异常状态中哪一项先偏离。

该节点的四级证据是：保留 2.4.1 注册JNI函数 这一目录坐标；解释它如何参与以MediaScanner贯通Java声明、JNI注册、类型转换、回调与异常边界；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭库加载日志、JNINativeMethod表、JNIEnv线程身份、局部/全局引用和异常状态恢复同输入结果。

### 2.4.2 数据类型转换

2.4.2 数据类型转换在“以MediaScanner贯通Java声明、JNI注册、类型转换、回调与异常边界”中的责任要翻译成一个可推翻的运行合同：明确入口、线程或进程、状态拥有者、正常结果和首个错误返回，再由库加载日志、JNINativeMethod表、JNIEnv线程身份、局部/全局引用和异常状态复核。

处理 2.4.2 数据类型转换 时先执行“定位首个分叉”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“恢复同输入重放”后仅改变一个条件，观察库加载日志、JNINativeMethod表、JNIEnv线程身份、局部/全局引用和异常状态中哪一项先偏离。

该节点的四级证据是：保留 2.4.2 数据类型转换 这一目录坐标；解释它如何参与以MediaScanner贯通Java声明、JNI注册、类型转换、回调与异常边界；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭库加载日志、JNINativeMethod表、JNIEnv线程身份、局部/全局引用和异常状态恢复同输入结果。

### 2.4.3 JNIEnv介绍

2.4.3 JNIEnv介绍在“以MediaScanner贯通Java声明、JNI注册、类型转换、回调与异常边界”中的责任要翻译成一个可推翻的运行合同：明确入口、线程或进程、状态拥有者、正常结果和首个错误返回，再由库加载日志、JNINativeMethod表、JNIEnv线程身份、局部/全局引用和异常状态复核。

处理 2.4.3 JNIEnv介绍 时先执行“恢复同输入重放”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“锁定历史基线”后仅改变一个条件，观察库加载日志、JNINativeMethod表、JNIEnv线程身份、局部/全局引用和异常状态中哪一项先偏离。

该节点的四级证据是：保留 2.4.3 JNIEnv介绍 这一目录坐标；解释它如何参与以MediaScanner贯通Java声明、JNI注册、类型转换、回调与异常边界；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭库加载日志、JNINativeMethod表、JNIEnv线程身份、局部/全局引用和异常状态恢复同输入结果。

### 2.4.4 通过JNIEnv操作jobject

2.4.4 通过JNIEnv操作jobject在“以MediaScanner贯通Java声明、JNI注册、类型转换、回调与异常边界”中的责任要翻译成一个可推翻的运行合同：明确入口、线程或进程、状态拥有者、正常结果和首个错误返回，再由库加载日志、JNINativeMethod表、JNIEnv线程身份、局部/全局引用和异常状态复核。

处理 2.4.4 通过JNIEnv操作jobject 时先执行“锁定历史基线”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“复现正常轨迹”后仅改变一个条件，观察库加载日志、JNINativeMethod表、JNIEnv线程身份、局部/全局引用和异常状态中哪一项先偏离。

该节点的四级证据是：保留 2.4.4 通过JNIEnv操作jobject 这一目录坐标；解释它如何参与以MediaScanner贯通Java声明、JNI注册、类型转换、回调与异常边界；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭库加载日志、JNINativeMethod表、JNIEnv线程身份、局部/全局引用和异常状态恢复同输入结果。

### 2.4.5 jstring介绍

2.4.5 jstring介绍在“以MediaScanner贯通Java声明、JNI注册、类型转换、回调与异常边界”中的责任要翻译成一个可推翻的运行合同：明确入口、线程或进程、状态拥有者、正常结果和首个错误返回，再由库加载日志、JNINativeMethod表、JNIEnv线程身份、局部/全局引用和异常状态复核。

处理 2.4.5 jstring介绍 时先执行“复现正常轨迹”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“注入单一故障”后仅改变一个条件，观察库加载日志、JNINativeMethod表、JNIEnv线程身份、局部/全局引用和异常状态中哪一项先偏离。

该节点的四级证据是：保留 2.4.5 jstring介绍 这一目录坐标；解释它如何参与以MediaScanner贯通Java声明、JNI注册、类型转换、回调与异常边界；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭库加载日志、JNINativeMethod表、JNIEnv线程身份、局部/全局引用和异常状态恢复同输入结果。

### 2.4.6 JNI类型签名介绍

2.4.6 JNI类型签名介绍在“以MediaScanner贯通Java声明、JNI注册、类型转换、回调与异常边界”中的责任要翻译成一个可推翻的运行合同：明确入口、线程或进程、状态拥有者、正常结果和首个错误返回，再由库加载日志、JNINativeMethod表、JNIEnv线程身份、局部/全局引用和异常状态复核。

处理 2.4.6 JNI类型签名介绍 时先执行“注入单一故障”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“定位首个分叉”后仅改变一个条件，观察库加载日志、JNINativeMethod表、JNIEnv线程身份、局部/全局引用和异常状态中哪一项先偏离。

该节点的四级证据是：保留 2.4.6 JNI类型签名介绍 这一目录坐标；解释它如何参与以MediaScanner贯通Java声明、JNI注册、类型转换、回调与异常边界；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭库加载日志、JNINativeMethod表、JNIEnv线程身份、局部/全局引用和异常状态恢复同输入结果。

### 2.4.7 垃圾回收

2.4.7 垃圾回收在“以MediaScanner贯通Java声明、JNI注册、类型转换、回调与异常边界”中的责任要翻译成一个可推翻的运行合同：明确入口、线程或进程、状态拥有者、正常结果和首个错误返回，再由库加载日志、JNINativeMethod表、JNIEnv线程身份、局部/全局引用和异常状态复核。

处理 2.4.7 垃圾回收 时先执行“定位首个分叉”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“恢复同输入重放”后仅改变一个条件，观察库加载日志、JNINativeMethod表、JNIEnv线程身份、局部/全局引用和异常状态中哪一项先偏离。

该节点的四级证据是：保留 2.4.7 垃圾回收 这一目录坐标；解释它如何参与以MediaScanner贯通Java声明、JNI注册、类型转换、回调与异常边界；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭库加载日志、JNINativeMethod表、JNIEnv线程身份、局部/全局引用和异常状态恢复同输入结果。

### 2.4.8 JNI中的异常处理

2.4.8 JNI中的异常处理在“以MediaScanner贯通Java声明、JNI注册、类型转换、回调与异常边界”中的责任要翻译成一个可推翻的运行合同：明确入口、线程或进程、状态拥有者、正常结果和首个错误返回，再由库加载日志、JNINativeMethod表、JNIEnv线程身份、局部/全局引用和异常状态复核。

处理 2.4.8 JNI中的异常处理 时先执行“恢复同输入重放”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“锁定历史基线”后仅改变一个条件，观察库加载日志、JNINativeMethod表、JNIEnv线程身份、局部/全局引用和异常状态中哪一项先偏离。

该节点的四级证据是：保留 2.4.8 JNI中的异常处理 这一目录坐标；解释它如何参与以MediaScanner贯通Java声明、JNI注册、类型转换、回调与异常边界；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭库加载日志、JNINativeMethod表、JNIEnv线程身份、局部/全局引用和异常状态恢复同输入结果。

### 2.5 本章小结

2.5 本章小结在“以MediaScanner贯通Java声明、JNI注册、类型转换、回调与异常边界”中的责任是界定分析分母和结论边界。先列出它包含的入口、对象和退出条件，再用库加载日志、JNINativeMethod表、JNIEnv线程身份、局部/全局引用和异常状态排除只凭类名或流程图得出的结论。

处理 2.5 本章小结 时先执行“锁定历史基线”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“复现正常轨迹”后仅改变一个条件，观察库加载日志、JNINativeMethod表、JNIEnv线程身份、局部/全局引用和异常状态中哪一项先偏离。

该节点的四级证据是：保留 2.5 本章小结 这一目录坐标；解释它如何参与以MediaScanner贯通Java声明、JNI注册、类型转换、回调与异常边界；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭库加载日志、JNINativeMethod表、JNIEnv线程身份、局部/全局引用和异常状态恢复同输入结果。

## 章专属复现实验

```bash
mkdir -p aosp-android-2.2_r1
cd aosp-android-2.2_r1
repo init -u https://android.googlesource.com/platform/manifest -b android-2.2_r1
repo sync -c

# 只在可恢复的同版本模拟器或测试机采集
adb logcat -d -s MediaScannerJNI
adb shell ps
```

运行前保存主机工具链、manifest与产品目标；这些历史分支通常需要隔离的旧工具链环境。诊断命令只在可恢复模拟器或测试机执行，账号、媒体与设备标识使用隔离样本；现代设备输出不得冒充android-2.2_r1结果。


## 练习、答案与四级证据


## 术语复核与本章回顾


完成卷I 第2章 深入理解JNI意味着：能解释以MediaScanner贯通Java声明、JNI注册、类型转换、回调与异常边界，能操作全节点图与诊断切片，能制造只记住函数命名规则，忽略线程附着、引用寿命与异常清理，还能凭库加载日志、JNINativeMethod表、JNIEnv线程身份、局部/全局引用和异常状态恢复、复位和交接，而不是只在目录中找到关键词。

## 阅读导航

[← 上一页：卷I 第1章 阅读前的准备工作](/learn/deep-android-volumes/01-v1-01/dav-v1-01-preparation) · [下一页：卷I 第3章 深入理解init →](/learn/deep-android-volumes/03-v1-03/dav-v1-03-init)

