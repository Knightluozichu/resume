---
title: 卷I 第3章 深入理解init
description: >-
  卷I 第3章 深入理解init：从PID
  1解析init.rc、启动service并建立属性服务，以android-2.2_r1源码、全节点机制图和故障恢复证据完成验收。
type: C
section: 深入Android三卷 · 卷I Native基础
draft: false
math: false
sourceUrl: "https://hzbook-group.iteye.com/group/wiki/3128-Android-internals"
demo: true
order: 3
qualityVersion: 2
practiceMode: diagnosis
sourceMode: independent-rewrite
---


{/* DEEP_ANDROID_VOLUMES_QUALITY_V2 */}

## 从一个可失败的问题开始

卷I 第3章 深入理解init不是类名清单，而是在android-2.2_r1中回答：从PID 1解析init.rc、启动service并建立属性服务。先预测请求经过的线程、进程与对象所有者，再运行正常样本；如果action/service解析表、进程父子关系、property socket请求、重启策略与失败日志不能指出首个变化，本页结论仍不可交接。

本章不变量是在android-2.2_r1固定输入下，从PID 1解析init.rc、启动service并建立属性服务的入口身份、状态拥有者、成功结果、错误返回和资源释放均可重复观察。将“把init.rc当静态脚本，忽略事件队列、服务状态和属性权限”作为反例，必须在同一版本、同一输入下看到确定的错误或状态分叉，并能恢复到基线。

## 来源范围、历史标签与当前迁移

卷I 第3章 深入理解init以[卷I出版社合法公开样章](https://hzbook-group.iteye.com/group/wiki/3128-Android-internals)核定可见正文、目录与Android 2.2范围；样章以外不宣称复现原书正文。

[AOSP版本与构建号表](https://source.android.com/docs/setup/reference/build-numbers)用于核对2.2/API 8、4.0.1/API 14、4.2.2/API 17及正式源码标签；[android-2.2_r1源码树](https://android.googlesource.com/platform/system/core/+/android-2.2_r1/)提供本页历史机制的一手坐标。当前[Android架构文档](https://source.android.com/docs/core/architecture)只用于迁移对照，ART、Treble、稳定AIDL、模块化系统服务和现代构建工具不得倒填为三卷历史实现。

## 六个机制检查点

、、、、、。
这六项把从PID
1解析init.rc、启动service并建立属性服务落到action/service解析表、进程父子关系、property
socket请求、重启策略与失败日志，防止目录词出现却没有运行解释。

## 正式目录逐项深读

### 第3章 深入理解init

第3章 深入理解init在“从PID 1解析init.rc、启动service并建立属性服务”中的责任必须落到创建者、触发条件和服务状态。用同一配置分别触发成功与重启路径，核对PID、属性、退出码和父子关系，防止把声明文件误当顺序脚本。

处理 第3章 深入理解init 时先执行“锁定历史基线”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“复现正常轨迹”后仅改变一个条件，观察action/service解析表、进程父子关系、property socket请求、重启策略与失败日志中哪一项先偏离。

该节点的四级证据是：保留 第3章 深入理解init 这一目录坐标；解释它如何参与从PID 1解析init.rc、启动service并建立属性服务；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭action/service解析表、进程父子关系、property socket请求、重启策略与失败日志恢复同输入结果。

### 3.1 概述

3.1 概述在“从PID 1解析init.rc、启动service并建立属性服务”中的责任是界定分析分母和结论边界。先列出它包含的入口、对象和退出条件，再用action/service解析表、进程父子关系、property socket请求、重启策略与失败日志排除只凭类名或流程图得出的结论。

处理 3.1 概述 时先执行“复现正常轨迹”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“注入单一故障”后仅改变一个条件，观察action/service解析表、进程父子关系、property socket请求、重启策略与失败日志中哪一项先偏离。

该节点的四级证据是：保留 3.1 概述 这一目录坐标；解释它如何参与从PID 1解析init.rc、启动service并建立属性服务；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭action/service解析表、进程父子关系、property socket请求、重启策略与失败日志恢复同输入结果。

### 3.2 init分析

3.2 init分析在“从PID 1解析init.rc、启动service并建立属性服务”中的责任必须落到创建者、触发条件和服务状态。用同一配置分别触发成功与重启路径，核对PID、属性、退出码和父子关系，防止把声明文件误当顺序脚本。

处理 3.2 init分析 时先执行“注入单一故障”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“定位首个分叉”后仅改变一个条件，观察action/service解析表、进程父子关系、property socket请求、重启策略与失败日志中哪一项先偏离。

该节点的四级证据是：保留 3.2 init分析 这一目录坐标；解释它如何参与从PID 1解析init.rc、启动service并建立属性服务；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭action/service解析表、进程父子关系、property socket请求、重启策略与失败日志恢复同输入结果。

### 3.2.1 解析配置文件

3.2.1 解析配置文件在“从PID 1解析init.rc、启动service并建立属性服务”中的责任要翻译成一个可推翻的运行合同：明确入口、线程或进程、状态拥有者、正常结果和首个错误返回，再由action/service解析表、进程父子关系、property socket请求、重启策略与失败日志复核。

处理 3.2.1 解析配置文件 时先执行“定位首个分叉”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“恢复同输入重放”后仅改变一个条件，观察action/service解析表、进程父子关系、property socket请求、重启策略与失败日志中哪一项先偏离。

该节点的四级证据是：保留 3.2.1 解析配置文件 这一目录坐标；解释它如何参与从PID 1解析init.rc、启动service并建立属性服务；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭action/service解析表、进程父子关系、property socket请求、重启策略与失败日志恢复同输入结果。

### 3.2.2 解析service

3.2.2 解析service在“从PID 1解析init.rc、启动service并建立属性服务”中的责任要连接system_server记录、调度队列和目标进程回调。保存记录对象与进程身份，制造超时或死亡后检查队列推进、死亡清理和后续请求是否恢复。

处理 3.2.2 解析service 时先执行“恢复同输入重放”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“锁定历史基线”后仅改变一个条件，观察action/service解析表、进程父子关系、property socket请求、重启策略与失败日志中哪一项先偏离。

该节点的四级证据是：保留 3.2.2 解析service 这一目录坐标；解释它如何参与从PID 1解析init.rc、启动service并建立属性服务；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭action/service解析表、进程父子关系、property socket请求、重启策略与失败日志恢复同输入结果。

### 3.2.3 init控制service

3.2.3 init控制service在“从PID 1解析init.rc、启动service并建立属性服务”中的责任要连接system_server记录、调度队列和目标进程回调。保存记录对象与进程身份，制造超时或死亡后检查队列推进、死亡清理和后续请求是否恢复。

处理 3.2.3 init控制service 时先执行“锁定历史基线”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“复现正常轨迹”后仅改变一个条件，观察action/service解析表、进程父子关系、property socket请求、重启策略与失败日志中哪一项先偏离。

该节点的四级证据是：保留 3.2.3 init控制service 这一目录坐标；解释它如何参与从PID 1解析init.rc、启动service并建立属性服务；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭action/service解析表、进程父子关系、property socket请求、重启策略与失败日志恢复同输入结果。

### 3.2.4 属性服务

3.2.4 属性服务在“从PID 1解析init.rc、启动service并建立属性服务”中的责任必须落到创建者、触发条件和服务状态。用同一配置分别触发成功与重启路径，核对PID、属性、退出码和父子关系，防止把声明文件误当顺序脚本。

处理 3.2.4 属性服务 时先执行“复现正常轨迹”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“注入单一故障”后仅改变一个条件，观察action/service解析表、进程父子关系、property socket请求、重启策略与失败日志中哪一项先偏离。

该节点的四级证据是：保留 3.2.4 属性服务 这一目录坐标；解释它如何参与从PID 1解析init.rc、启动service并建立属性服务；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭action/service解析表、进程父子关系、property socket请求、重启策略与失败日志恢复同输入结果。

### 3.3 本章小结

3.3 本章小结在“从PID 1解析init.rc、启动service并建立属性服务”中的责任是界定分析分母和结论边界。先列出它包含的入口、对象和退出条件，再用action/service解析表、进程父子关系、property socket请求、重启策略与失败日志排除只凭类名或流程图得出的结论。

处理 3.3 本章小结 时先执行“注入单一故障”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“定位首个分叉”后仅改变一个条件，观察action/service解析表、进程父子关系、property socket请求、重启策略与失败日志中哪一项先偏离。

该节点的四级证据是：保留 3.3 本章小结 这一目录坐标；解释它如何参与从PID 1解析init.rc、启动service并建立属性服务；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭action/service解析表、进程父子关系、property socket请求、重启策略与失败日志恢复同输入结果。

## 章专属复现实验

```bash
mkdir -p aosp-android-2.2_r1
cd aosp-android-2.2_r1
repo init -u https://android.googlesource.com/platform/manifest -b android-2.2_r1
repo sync -c

# 只在可恢复的同版本模拟器或测试机采集
adb shell getprop init.svc.zygote
adb shell ps
```

运行前保存主机工具链、manifest与产品目标；这些历史分支通常需要隔离的旧工具链环境。诊断命令只在可恢复模拟器或测试机执行，账号、媒体与设备标识使用隔离样本；现代设备输出不得冒充android-2.2_r1结果。


## 练习、答案与四级证据


## 术语复核与本章回顾


完成卷I 第3章 深入理解init意味着：能解释从PID 1解析init.rc、启动service并建立属性服务，能操作全节点图与诊断切片，能制造把init.rc当静态脚本，忽略事件队列、服务状态和属性权限，还能凭action/service解析表、进程父子关系、property socket请求、重启策略与失败日志恢复、复位和交接，而不是只在目录中找到关键词。

## 阅读导航

[← 上一页：卷I 第2章 深入理解JNI](/learn/deep-android-volumes/02-v1-02/dav-v1-02-jni) · [下一页：卷I 第4章 深入理解zygote →](/learn/deep-android-volumes/04-v1-04/dav-v1-04-zygote)

