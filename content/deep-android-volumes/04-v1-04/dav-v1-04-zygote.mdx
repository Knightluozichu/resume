---
title: 卷I 第4章 深入理解zygote
description: >-
  卷I 第4章
  深入理解zygote：追踪app_process进入Java世界、SystemServer诞生与应用进程fork，以android-2.2_r1源码、全节点机制图和故障恢复证据完成验收。
type: C
section: 深入Android三卷 · 卷I Native基础
draft: true
math: false
sourceUrl: "https://hzbook-group.iteye.com/group/wiki/3128-Android-internals"
demo: true
order: 4
qualityVersion: 2
practiceMode: simulation
sourceMode: independent-rewrite
---

import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能在android-2.2_r1单一历史标签内解释追踪app_process进入Java世界、SystemServer诞生与应用进程fork，不跨卷拼接文件与接口身份。
- 能操作全节点机制图，让本页 19 个坐标都具备出现、解释、实验和练习四级证据。
- 能使用章专属诊断切片保存入口、PID/TID、对象或状态、正常结果、错误返回与资源释放。
- 能主动制造“只画init到Zygote箭头，遗漏预加载、fork分支和system_server职责”，再凭Zygote socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时定位、恢复、复位并以同输入重放。

</Objectives>

{/* DEEP_ANDROID_VOLUMES_QUALITY_V2 */}

## 从一个可失败的问题开始

卷I 第4章 深入理解zygote不是类名清单，而是在android-2.2_r1中回答：追踪app_process进入Java世界、SystemServer诞生与应用进程fork。先预测请求经过的线程、进程与对象所有者，再运行正常样本；如果Zygote socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时不能指出首个变化，本页结论仍不可交接。

本章不变量是在android-2.2_r1固定输入下，追踪app_process进入Java世界、SystemServer诞生与应用进程fork的入口身份、状态拥有者、成功结果、错误返回和资源释放均可重复观察。将“只画init到Zygote箭头，遗漏预加载、fork分支和system_server职责”作为反例，必须在同一版本、同一输入下看到确定的错误或状态分叉，并能恢复到基线。

## 来源范围、历史标签与当前迁移

卷I 第4章 深入理解zygote以[卷I出版社合法公开样章](https://hzbook-group.iteye.com/group/wiki/3128-Android-internals)核定可见正文、目录与Android 2.2范围；样章以外不宣称复现原书正文。

[AOSP版本与构建号表](https://source.android.com/docs/setup/reference/build-numbers)用于核对2.2/API 8、4.0.1/API 14、4.2.2/API 17及正式源码标签；[android-2.2_r1源码树](https://android.googlesource.com/platform/frameworks/base/+/android-2.2_r1/)提供本页历史机制的一手坐标。当前[Android架构文档](https://source.android.com/docs/core/architecture)只用于迁移对照，ART、Treble、稳定AIDL、模块化系统服务和现代构建工具不得倒填为三卷历史实现。

## 六个机制检查点

<Term def="初始输入在本页用于锁定android-2.2_r1的事实坐标与允许输入。">初始输入</Term>、<Term def="调用入口在本页用于标出追踪app_process进入Java世界、SystemServer诞生与应用进程fork由谁创建、修改与释放状态。">调用入口</Term>、<Term def="状态迁移在本页用于保存能复现正常路径的对象身份和时序。">状态迁移</Term>、<Term def="可见反馈在本页用于暴露只画init到Zygote箭头，遗漏预加载、fork分支和system_server职责发生的第一个边界。">可见反馈</Term>、<Term def="故障注入在本页用于声明故障后重新接收请求所需的恢复条件。">故障注入</Term>、<Term def="复位重放在本页用于让独立读者凭Zygote socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时得到同一结论。">复位重放</Term>。
这六项把追踪app_process进入Java世界、SystemServer诞生与应用进程fork落到Zygote
socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时，防止目录词出现却没有运行解释。

## 正式目录逐项深读

### 第4章 深入理解zygote

第4章 深入理解zygote在“追踪app_process进入Java世界、SystemServer诞生与应用进程fork”中的责任处在预加载、进程分裂或系统服务启动边界。比较fork前后PID、共享页与分支职责，并在子进程失败时确认父进程和system_server状态没有被静默污染。

处理 第4章 深入理解zygote 时先执行“固定输入与所有者”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“触发系统请求”后仅改变一个条件，观察Zygote socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时中哪一项先偏离。

该节点的四级证据是：保留 第4章 深入理解zygote 这一目录坐标；解释它如何参与追踪app_process进入Java世界、SystemServer诞生与应用进程fork；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭Zygote socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时恢复同输入结果。

### 4.1 概述

4.1 概述在“追踪app_process进入Java世界、SystemServer诞生与应用进程fork”中的责任是界定分析分母和结论边界。先列出它包含的入口、对象和退出条件，再用Zygote socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时排除只凭类名或流程图得出的结论。

处理 4.1 概述 时先执行“触发系统请求”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“推进状态机”后仅改变一个条件，观察Zygote socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时中哪一项先偏离。

该节点的四级证据是：保留 4.1 概述 这一目录坐标；解释它如何参与追踪app_process进入Java世界、SystemServer诞生与应用进程fork；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭Zygote socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时恢复同输入结果。

### 4.2 zygote分析

4.2 zygote分析在“追踪app_process进入Java世界、SystemServer诞生与应用进程fork”中的责任处在预加载、进程分裂或系统服务启动边界。比较fork前后PID、共享页与分支职责，并在子进程失败时确认父进程和system_server状态没有被静默污染。

处理 4.2 zygote分析 时先执行“推进状态机”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“观察反馈与释放”后仅改变一个条件，观察Zygote socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时中哪一项先偏离。

该节点的四级证据是：保留 4.2 zygote分析 这一目录坐标；解释它如何参与追踪app_process进入Java世界、SystemServer诞生与应用进程fork；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭Zygote socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时恢复同输入结果。

### 4.2.1 AppRuntime分析

4.2.1 AppRuntime分析在“追踪app_process进入Java世界、SystemServer诞生与应用进程fork”中的责任要翻译成一个可推翻的运行合同：明确入口、线程或进程、状态拥有者、正常结果和首个错误返回，再由Zygote socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时复核。

处理 4.2.1 AppRuntime分析 时先执行“观察反馈与释放”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“复位后再次执行”后仅改变一个条件，观察Zygote socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时中哪一项先偏离。

该节点的四级证据是：保留 4.2.1 AppRuntime分析 这一目录坐标；解释它如何参与追踪app_process进入Java世界、SystemServer诞生与应用进程fork；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭Zygote socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时恢复同输入结果。

### 4.2.2 Welcome to Java World

4.2.2 Welcome to Java World在“追踪app_process进入Java世界、SystemServer诞生与应用进程fork”中的责任要翻译成一个可推翻的运行合同：明确入口、线程或进程、状态拥有者、正常结果和首个错误返回，再由Zygote socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时复核。

处理 4.2.2 Welcome to Java World 时先执行“复位后再次执行”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“固定输入与所有者”后仅改变一个条件，观察Zygote socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时中哪一项先偏离。

该节点的四级证据是：保留 4.2.2 Welcome to Java World 这一目录坐标；解释它如何参与追踪app_process进入Java世界、SystemServer诞生与应用进程fork；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭Zygote socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时恢复同输入结果。

### 4.2.3 关于zygote的总结

4.2.3 关于zygote的总结在“追踪app_process进入Java世界、SystemServer诞生与应用进程fork”中的责任是界定分析分母和结论边界。先列出它包含的入口、对象和退出条件，再用Zygote socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时排除只凭类名或流程图得出的结论。

处理 4.2.3 关于zygote的总结 时先执行“固定输入与所有者”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“触发系统请求”后仅改变一个条件，观察Zygote socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时中哪一项先偏离。

该节点的四级证据是：保留 4.2.3 关于zygote的总结 这一目录坐标；解释它如何参与追踪app_process进入Java世界、SystemServer诞生与应用进程fork；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭Zygote socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时恢复同输入结果。

### 4.3 SystemServer分析

4.3 SystemServer分析在“追踪app_process进入Java世界、SystemServer诞生与应用进程fork”中的责任处在预加载、进程分裂或系统服务启动边界。比较fork前后PID、共享页与分支职责，并在子进程失败时确认父进程和system_server状态没有被静默污染。

处理 4.3 SystemServer分析 时先执行“触发系统请求”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“推进状态机”后仅改变一个条件，观察Zygote socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时中哪一项先偏离。

该节点的四级证据是：保留 4.3 SystemServer分析 这一目录坐标；解释它如何参与追踪app_process进入Java世界、SystemServer诞生与应用进程fork；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭Zygote socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时恢复同输入结果。

### 4.3.1 SystemServer的诞生

4.3.1 SystemServer的诞生在“追踪app_process进入Java世界、SystemServer诞生与应用进程fork”中的责任处在预加载、进程分裂或系统服务启动边界。比较fork前后PID、共享页与分支职责，并在子进程失败时确认父进程和system_server状态没有被静默污染。

处理 4.3.1 SystemServer的诞生 时先执行“推进状态机”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“观察反馈与释放”后仅改变一个条件，观察Zygote socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时中哪一项先偏离。

该节点的四级证据是：保留 4.3.1 SystemServer的诞生 这一目录坐标；解释它如何参与追踪app_process进入Java世界、SystemServer诞生与应用进程fork；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭Zygote socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时恢复同输入结果。

### 4.3.2 SystemServer的重要使命

4.3.2 SystemServer的重要使命在“追踪app_process进入Java世界、SystemServer诞生与应用进程fork”中的责任处在预加载、进程分裂或系统服务启动边界。比较fork前后PID、共享页与分支职责，并在子进程失败时确认父进程和system_server状态没有被静默污染。

处理 4.3.2 SystemServer的重要使命 时先执行“观察反馈与释放”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“复位后再次执行”后仅改变一个条件，观察Zygote socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时中哪一项先偏离。

该节点的四级证据是：保留 4.3.2 SystemServer的重要使命 这一目录坐标；解释它如何参与追踪app_process进入Java世界、SystemServer诞生与应用进程fork；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭Zygote socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时恢复同输入结果。

### 4.3.3 关于SystemServer的总结

4.3.3 关于SystemServer的总结在“追踪app_process进入Java世界、SystemServer诞生与应用进程fork”中的责任是界定分析分母和结论边界。先列出它包含的入口、对象和退出条件，再用Zygote socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时排除只凭类名或流程图得出的结论。

处理 4.3.3 关于SystemServer的总结 时先执行“复位后再次执行”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“固定输入与所有者”后仅改变一个条件，观察Zygote socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时中哪一项先偏离。

该节点的四级证据是：保留 4.3.3 关于SystemServer的总结 这一目录坐标；解释它如何参与追踪app_process进入Java世界、SystemServer诞生与应用进程fork；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭Zygote socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时恢复同输入结果。

### 4.4 zygote的分裂

4.4 zygote的分裂在“追踪app_process进入Java世界、SystemServer诞生与应用进程fork”中的责任处在预加载、进程分裂或系统服务启动边界。比较fork前后PID、共享页与分支职责，并在子进程失败时确认父进程和system_server状态没有被静默污染。

处理 4.4 zygote的分裂 时先执行“固定输入与所有者”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“触发系统请求”后仅改变一个条件，观察Zygote socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时中哪一项先偏离。

该节点的四级证据是：保留 4.4 zygote的分裂 这一目录坐标；解释它如何参与追踪app_process进入Java世界、SystemServer诞生与应用进程fork；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭Zygote socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时恢复同输入结果。

### 4.4.1 ActivityManagerService发送请求

4.4.1 ActivityManagerService发送请求在“追踪app_process进入Java世界、SystemServer诞生与应用进程fork”中的责任要连接system_server记录、调度队列和目标进程回调。保存记录对象与进程身份，制造超时或死亡后检查队列推进、死亡清理和后续请求是否恢复。

处理 4.4.1 ActivityManagerService发送请求 时先执行“触发系统请求”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“推进状态机”后仅改变一个条件，观察Zygote socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时中哪一项先偏离。

该节点的四级证据是：保留 4.4.1 ActivityManagerService发送请求 这一目录坐标；解释它如何参与追踪app_process进入Java世界、SystemServer诞生与应用进程fork；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭Zygote socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时恢复同输入结果。

### 4.4.2 有求必应之响应请求

4.4.2 有求必应之响应请求在“追踪app_process进入Java世界、SystemServer诞生与应用进程fork”中的责任要翻译成一个可推翻的运行合同：明确入口、线程或进程、状态拥有者、正常结果和首个错误返回，再由Zygote socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时复核。

处理 4.4.2 有求必应之响应请求 时先执行“推进状态机”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“观察反馈与释放”后仅改变一个条件，观察Zygote socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时中哪一项先偏离。

该节点的四级证据是：保留 4.4.2 有求必应之响应请求 这一目录坐标；解释它如何参与追踪app_process进入Java世界、SystemServer诞生与应用进程fork；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭Zygote socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时恢复同输入结果。

### 4.4.3 关于zygote分裂的总结

4.4.3 关于zygote分裂的总结在“追踪app_process进入Java世界、SystemServer诞生与应用进程fork”中的责任是界定分析分母和结论边界。先列出它包含的入口、对象和退出条件，再用Zygote socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时排除只凭类名或流程图得出的结论。

处理 4.4.3 关于zygote分裂的总结 时先执行“观察反馈与释放”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“复位后再次执行”后仅改变一个条件，观察Zygote socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时中哪一项先偏离。

该节点的四级证据是：保留 4.4.3 关于zygote分裂的总结 这一目录坐标；解释它如何参与追踪app_process进入Java世界、SystemServer诞生与应用进程fork；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭Zygote socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时恢复同输入结果。

### 4.5 拓展思考

4.5 拓展思考在“追踪app_process进入Java世界、SystemServer诞生与应用进程fork”中的责任是界定分析分母和结论边界。先列出它包含的入口、对象和退出条件，再用Zygote socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时排除只凭类名或流程图得出的结论。

处理 4.5 拓展思考 时先执行“复位后再次执行”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“固定输入与所有者”后仅改变一个条件，观察Zygote socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时中哪一项先偏离。

该节点的四级证据是：保留 4.5 拓展思考 这一目录坐标；解释它如何参与追踪app_process进入Java世界、SystemServer诞生与应用进程fork；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭Zygote socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时恢复同输入结果。

### 4.5.1 虚拟机heapsize的限制

4.5.1 虚拟机heapsize的限制在“追踪app_process进入Java世界、SystemServer诞生与应用进程fork”中的责任处在预加载、进程分裂或系统服务启动边界。比较fork前后PID、共享页与分支职责，并在子进程失败时确认父进程和system_server状态没有被静默污染。

处理 4.5.1 虚拟机heapsize的限制 时先执行“固定输入与所有者”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“触发系统请求”后仅改变一个条件，观察Zygote socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时中哪一项先偏离。

该节点的四级证据是：保留 4.5.1 虚拟机heapsize的限制 这一目录坐标；解释它如何参与追踪app_process进入Java世界、SystemServer诞生与应用进程fork；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭Zygote socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时恢复同输入结果。

### 4.5.2 开机速度优化

4.5.2 开机速度优化在“追踪app_process进入Java世界、SystemServer诞生与应用进程fork”中的责任要翻译成一个可推翻的运行合同：明确入口、线程或进程、状态拥有者、正常结果和首个错误返回，再由Zygote socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时复核。

处理 4.5.2 开机速度优化 时先执行“触发系统请求”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“推进状态机”后仅改变一个条件，观察Zygote socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时中哪一项先偏离。

该节点的四级证据是：保留 4.5.2 开机速度优化 这一目录坐标；解释它如何参与追踪app_process进入Java世界、SystemServer诞生与应用进程fork；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭Zygote socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时恢复同输入结果。

### 4.5.3 Watchdog分析

4.5.3 Watchdog分析在“追踪app_process进入Java世界、SystemServer诞生与应用进程fork”中的责任处在预加载、进程分裂或系统服务启动边界。比较fork前后PID、共享页与分支职责，并在子进程失败时确认父进程和system_server状态没有被静默污染。

处理 4.5.3 Watchdog分析 时先执行“推进状态机”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“观察反馈与释放”后仅改变一个条件，观察Zygote socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时中哪一项先偏离。

该节点的四级证据是：保留 4.5.3 Watchdog分析 这一目录坐标；解释它如何参与追踪app_process进入Java世界、SystemServer诞生与应用进程fork；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭Zygote socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时恢复同输入结果。

### 4.6 本章小结

4.6 本章小结在“追踪app_process进入Java世界、SystemServer诞生与应用进程fork”中的责任是界定分析分母和结论边界。先列出它包含的入口、对象和退出条件，再用Zygote socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时排除只凭类名或流程图得出的结论。

处理 4.6 本章小结 时先执行“观察反馈与释放”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“复位后再次执行”后仅改变一个条件，观察Zygote socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时中哪一项先偏离。

该节点的四级证据是：保留 4.6 本章小结 这一目录坐标；解释它如何参与追踪app_process进入Java世界、SystemServer诞生与应用进程fork；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭Zygote socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时恢复同输入结果。

## 章专属复现实验

```bash
mkdir -p aosp-android-2.2_r1
cd aosp-android-2.2_r1
repo init -u https://android.googlesource.com/platform/manifest -b android-2.2_r1
repo sync -c

# 只在可恢复的同版本模拟器或测试机采集
adb shell ps
adb logcat -d -s Zygote SystemServer
```

运行前保存主机工具链、manifest与产品目标；这些历史分支通常需要隔离的旧工具链环境。诊断命令只在可恢复模拟器或测试机执行，账号、媒体与设备标识使用隔离样本；现代设备输出不得冒充android-2.2_r1结果。

<Callout type="trap" title="本章核心陷阱">
  只画init到Zygote箭头，遗漏预加载、fork分支和system_server职责。先恢复android-2.2_r1的真实入口、对象身份和失败返回，再讨论当前Android迁移。
</Callout>

<Callout type="trap" title="跨卷同名不等于同一实现">
  卷I 第4章
  深入理解zygote只在自己的历史标签中成立；Binder、MessageQueue、SystemServer、Audio或窗口机制跨卷复用名称时，必须分别核对文件路径、线程责任和错误语义。
</Callout>

<Callout type="warning" title="系统实验安全边界">
  刷机、Root、系统映像、gdb、getevent与输入注入仅用于可恢复测试环境。先备份，再记录回滚条件；任何真实用户数据都不得进入截图、日志或练习答案。
</Callout>

## 练习、答案与四级证据

<Exercises>

**问题 1：历史边界。** 怎样证明本页没有把当前Android实现倒灌到 android-2.2_r1？

<Answer>
  从[官方标签表](https://source.android.com/docs/setup/reference/build-numbers)锁定版本，只引用[该标签源码](https://android.googlesource.com/platform/frameworks/base/+/android-2.2_r1/)解释历史文件与接口；当前架构另列迁移记录。若同名对象路径、职责或返回语义改变，就保留差异而不是拼接流程。
</Answer>

**问题 2：逐节点证据。** 本页 19 个正式坐标如何通过出现、解释、实验与练习四级验收？

<Answer>
  - **第4章
  深入理解zygote**：在“固定输入与所有者”保存入口与状态，注入只画init到Zygote箭头，遗漏预加载、fork分支和system_server职责后恢复，并以Zygote
  socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时重放。 -
  **4.1
  概述**：在“触发系统请求”保存入口与状态，注入只画init到Zygote箭头，遗漏预加载、fork分支和system_server职责后恢复，并以Zygote
  socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时重放。 -
  **4.2
  zygote分析**：在“推进状态机”保存入口与状态，注入只画init到Zygote箭头，遗漏预加载、fork分支和system_server职责后恢复，并以Zygote
  socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时重放。 -
  **4.2.1
  AppRuntime分析**：在“观察反馈与释放”保存入口与状态，注入只画init到Zygote箭头，遗漏预加载、fork分支和system_server职责后恢复，并以Zygote
  socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时重放。 -
  **4.2.2 Welcome to Java
  World**：在“复位后再次执行”保存入口与状态，注入只画init到Zygote箭头，遗漏预加载、fork分支和system_server职责后恢复，并以Zygote
  socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时重放。 -
  **4.2.3
  关于zygote的总结**：在“固定输入与所有者”保存入口与状态，注入只画init到Zygote箭头，遗漏预加载、fork分支和system_server职责后恢复，并以Zygote
  socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时重放。 -
  **4.3
  SystemServer分析**：在“触发系统请求”保存入口与状态，注入只画init到Zygote箭头，遗漏预加载、fork分支和system_server职责后恢复，并以Zygote
  socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时重放。 -
  **4.3.1
  SystemServer的诞生**：在“推进状态机”保存入口与状态，注入只画init到Zygote箭头，遗漏预加载、fork分支和system_server职责后恢复，并以Zygote
  socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时重放。 -
  **4.3.2
  SystemServer的重要使命**：在“观察反馈与释放”保存入口与状态，注入只画init到Zygote箭头，遗漏预加载、fork分支和system_server职责后恢复，并以Zygote
  socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时重放。 -
  **4.3.3
  关于SystemServer的总结**：在“复位后再次执行”保存入口与状态，注入只画init到Zygote箭头，遗漏预加载、fork分支和system_server职责后恢复，并以Zygote
  socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时重放。 -
  **4.4
  zygote的分裂**：在“固定输入与所有者”保存入口与状态，注入只画init到Zygote箭头，遗漏预加载、fork分支和system_server职责后恢复，并以Zygote
  socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时重放。 -
  **4.4.1
  ActivityManagerService发送请求**：在“触发系统请求”保存入口与状态，注入只画init到Zygote箭头，遗漏预加载、fork分支和system_server职责后恢复，并以Zygote
  socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时重放。 -
  **4.4.2
  有求必应之响应请求**：在“推进状态机”保存入口与状态，注入只画init到Zygote箭头，遗漏预加载、fork分支和system_server职责后恢复，并以Zygote
  socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时重放。 -
  **4.4.3
  关于zygote分裂的总结**：在“观察反馈与释放”保存入口与状态，注入只画init到Zygote箭头，遗漏预加载、fork分支和system_server职责后恢复，并以Zygote
  socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时重放。 -
  **4.5
  拓展思考**：在“复位后再次执行”保存入口与状态，注入只画init到Zygote箭头，遗漏预加载、fork分支和system_server职责后恢复，并以Zygote
  socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时重放。 -
  **4.5.1
  虚拟机heapsize的限制**：在“固定输入与所有者”保存入口与状态，注入只画init到Zygote箭头，遗漏预加载、fork分支和system_server职责后恢复，并以Zygote
  socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时重放。 -
  **4.5.2
  开机速度优化**：在“触发系统请求”保存入口与状态，注入只画init到Zygote箭头，遗漏预加载、fork分支和system_server职责后恢复，并以Zygote
  socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时重放。 -
  **4.5.3
  Watchdog分析**：在“推进状态机”保存入口与状态，注入只画init到Zygote箭头，遗漏预加载、fork分支和system_server职责后恢复，并以Zygote
  socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时重放。 -
  **4.6
  本章小结**：在“观察反馈与释放”保存入口与状态，注入只画init到Zygote箭头，遗漏预加载、fork分支和system_server职责后恢复，并以Zygote
  socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时重放。
</Answer>

**问题 3：故障恢复。** 面对只画init到Zygote箭头，遗漏预加载、fork分支和system_server职责，如何确认修复而不是掩盖症状？

<Answer>
  先保存Zygote
  socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时中的正常基线，再只注入一个错误条件并记录首个分叉。恢复真实标签、状态所有者或消费路径后，以完全相同输入重放；只有在android-2.2_r1固定输入下，追踪app_process进入Java世界、SystemServer诞生与应用进程fork的入口身份、状态拥有者、成功结果、错误返回和资源释放均可重复观察重新成立且实验可重置，才算通过。
</Answer>

</Exercises>

## 术语复核与本章回顾

<Glossary>
  <GlossaryItem term="初始输入">
    初始输入在本页用于锁定android-2.2_r1的事实坐标与允许输入。
  </GlossaryItem>
  <GlossaryItem term="调用入口">
    调用入口在本页用于标出追踪app_process进入Java世界、SystemServer诞生与应用进程fork由谁创建、修改与释放状态。
  </GlossaryItem>
  <GlossaryItem term="状态迁移">
    状态迁移在本页用于保存能复现正常路径的对象身份和时序。
  </GlossaryItem>
  <GlossaryItem term="可见反馈">
    可见反馈在本页用于暴露只画init到Zygote箭头，遗漏预加载、fork分支和system_server职责发生的第一个边界。
  </GlossaryItem>
  <GlossaryItem term="故障注入">
    故障注入在本页用于声明故障后重新接收请求所需的恢复条件。
  </GlossaryItem>
  <GlossaryItem term="复位重放">
    复位重放在本页用于让独立读者凭Zygote
    socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时得到同一结论。
  </GlossaryItem>
</Glossary>

完成卷I 第4章 深入理解zygote意味着：能解释追踪app_process进入Java世界、SystemServer诞生与应用进程fork，能操作全节点图与诊断切片，能制造只画init到Zygote箭头，遗漏预加载、fork分支和system_server职责，还能凭Zygote socket、预加载集合、fork前后PID与页共享、AMS请求、Watchdog和启动耗时恢复、复位和交接，而不是只在目录中找到关键词。

## 阅读导航

[← 上一页：卷I 第3章 深入理解init](/learn/deep-android-volumes/03-v1-03/dav-v1-03-init) · [下一页：卷I 第5章 深入理解常见类 →](/learn/deep-android-volumes/05-v1-05/dav-v1-05-common-classes)

<Attribution
  mode="independent-rewrite"
  sourceBasis="authorized-sample"
  workTitle="邓凡平《深入理解Android：卷I》（机械工业出版社，2011，Android 2.2）"
  adaptedUrl="https://hzbook-group.iteye.com/group/wiki/3128-Android-internals"
/>
