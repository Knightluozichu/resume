---
title: 卷I 第5章 深入理解常见类
description: >-
  卷I 第5章
  深入理解常见类：用RefBase、sp/wp、Thread、同步类、Looper和Handler建立对象与任务寿命模型，以android-2.2_r1源码、全节点机制图和故障恢复证据完成验收。
type: C
section: 深入Android三卷 · 卷I Native基础
draft: false
math: false
sourceUrl: "https://hzbook-group.iteye.com/group/wiki/3128-Android-internals"
demo: true
order: 5
qualityVersion: 2
practiceMode: diagnosis
sourceMode: independent-rewrite
---


{/* DEEP_ANDROID_VOLUMES_QUALITY_V2 */}

## 从一个可失败的问题开始

卷I 第5章 深入理解常见类不是类名清单，而是在android-2.2_r1中回答：用RefBase、sp/wp、Thread、同步类、Looper和Handler建立对象与任务寿命模型。先预测请求经过的线程、进程与对象所有者，再运行正常样本；如果强弱计数轨迹、首次引用/最后引用回调、线程状态、锁顺序、Message入队与唤醒时间不能指出首个变化，本页结论仍不可交接。

本章不变量是在android-2.2_r1固定输入下，用RefBase、sp/wp、Thread、同步类、Looper和Handler建立对象与任务寿命模型的入口身份、状态拥有者、成功结果、错误返回和资源释放均可重复观察。将“把sp等同标准智能指针，或把Handler误认为自动创建工作线程”作为反例，必须在同一版本、同一输入下看到确定的错误或状态分叉，并能恢复到基线。

## 来源范围、历史标签与当前迁移

卷I 第5章 深入理解常见类以[卷I出版社合法公开样章](https://hzbook-group.iteye.com/group/wiki/3128-Android-internals)核定可见正文、目录与Android 2.2范围；样章以外不宣称复现原书正文。

[AOSP版本与构建号表](https://source.android.com/docs/setup/reference/build-numbers)用于核对2.2/API 8、4.0.1/API 14、4.2.2/API 17及正式源码标签；[android-2.2_r1源码树](https://android.googlesource.com/platform/frameworks/base/+/android-2.2_r1/)提供本页历史机制的一手坐标。当前[Android架构文档](https://source.android.com/docs/core/architecture)只用于迁移对照，ART、Treble、稳定AIDL、模块化系统服务和现代构建工具不得倒填为三卷历史实现。

## 六个机制检查点

、、、、、。
这六项把用RefBase、sp/wp、Thread、同步类、Looper和Handler建立对象与任务寿命模型落到强弱计数轨迹、首次引用/最后引用回调、线程状态、锁顺序、Message入队与唤醒时间，防止目录词出现却没有运行解释。

## 正式目录逐项深读

### 第5章 深入理解常见类

第5章 深入理解常见类在“用RefBase、sp/wp、Thread、同步类、Looper和Handler建立对象与任务寿命模型”中的责任要翻译成一个可推翻的运行合同：明确入口、线程或进程、状态拥有者、正常结果和首个错误返回，再由强弱计数轨迹、首次引用/最后引用回调、线程状态、锁顺序、Message入队与唤醒时间复核。

处理 第5章 深入理解常见类 时先执行“锁定历史基线”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“复现正常轨迹”后仅改变一个条件，观察强弱计数轨迹、首次引用/最后引用回调、线程状态、锁顺序、Message入队与唤醒时间中哪一项先偏离。

该节点的四级证据是：保留 第5章 深入理解常见类 这一目录坐标；解释它如何参与用RefBase、sp/wp、Thread、同步类、Looper和Handler建立对象与任务寿命模型；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭强弱计数轨迹、首次引用/最后引用回调、线程状态、锁顺序、Message入队与唤醒时间恢复同输入结果。

### 5.1 概述

5.1 概述在“用RefBase、sp/wp、Thread、同步类、Looper和Handler建立对象与任务寿命模型”中的责任是界定分析分母和结论边界。先列出它包含的入口、对象和退出条件，再用强弱计数轨迹、首次引用/最后引用回调、线程状态、锁顺序、Message入队与唤醒时间排除只凭类名或流程图得出的结论。

处理 5.1 概述 时先执行“复现正常轨迹”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“注入单一故障”后仅改变一个条件，观察强弱计数轨迹、首次引用/最后引用回调、线程状态、锁顺序、Message入队与唤醒时间中哪一项先偏离。

该节点的四级证据是：保留 5.1 概述 这一目录坐标；解释它如何参与用RefBase、sp/wp、Thread、同步类、Looper和Handler建立对象与任务寿命模型；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭强弱计数轨迹、首次引用/最后引用回调、线程状态、锁顺序、Message入队与唤醒时间恢复同输入结果。

### 5.2 以“三板斧”揭秘RefBase、sp和wp

5.2 以“三板斧”揭秘RefBase、sp和wp在“用RefBase、sp/wp、Thread、同步类、Looper和Handler建立对象与任务寿命模型”中的责任要翻译成一个可推翻的运行合同：明确入口、线程或进程、状态拥有者、正常结果和首个错误返回，再由强弱计数轨迹、首次引用/最后引用回调、线程状态、锁顺序、Message入队与唤醒时间复核。

处理 5.2 以“三板斧”揭秘RefBase、sp和wp 时先执行“注入单一故障”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“定位首个分叉”后仅改变一个条件，观察强弱计数轨迹、首次引用/最后引用回调、线程状态、锁顺序、Message入队与唤醒时间中哪一项先偏离。

该节点的四级证据是：保留 5.2 以“三板斧”揭秘RefBase、sp和wp 这一目录坐标；解释它如何参与用RefBase、sp/wp、Thread、同步类、Looper和Handler建立对象与任务寿命模型；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭强弱计数轨迹、首次引用/最后引用回调、线程状态、锁顺序、Message入队与唤醒时间恢复同输入结果。

### 5.2.1 第一板斧——初识影子对象

5.2.1 第一板斧——初识影子对象在“用RefBase、sp/wp、Thread、同步类、Looper和Handler建立对象与任务寿命模型”中的责任是界定分析分母和结论边界。先列出它包含的入口、对象和退出条件，再用强弱计数轨迹、首次引用/最后引用回调、线程状态、锁顺序、Message入队与唤醒时间排除只凭类名或流程图得出的结论。

处理 5.2.1 第一板斧——初识影子对象 时先执行“定位首个分叉”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“恢复同输入重放”后仅改变一个条件，观察强弱计数轨迹、首次引用/最后引用回调、线程状态、锁顺序、Message入队与唤醒时间中哪一项先偏离。

该节点的四级证据是：保留 5.2.1 第一板斧——初识影子对象 这一目录坐标；解释它如何参与用RefBase、sp/wp、Thread、同步类、Looper和Handler建立对象与任务寿命模型；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭强弱计数轨迹、首次引用/最后引用回调、线程状态、锁顺序、Message入队与唤醒时间恢复同输入结果。

### 5.2.2 第二板斧——由弱生强

5.2.2 第二板斧——由弱生强在“用RefBase、sp/wp、Thread、同步类、Looper和Handler建立对象与任务寿命模型”中的责任要翻译成一个可推翻的运行合同：明确入口、线程或进程、状态拥有者、正常结果和首个错误返回，再由强弱计数轨迹、首次引用/最后引用回调、线程状态、锁顺序、Message入队与唤醒时间复核。

处理 5.2.2 第二板斧——由弱生强 时先执行“恢复同输入重放”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“锁定历史基线”后仅改变一个条件，观察强弱计数轨迹、首次引用/最后引用回调、线程状态、锁顺序、Message入队与唤醒时间中哪一项先偏离。

该节点的四级证据是：保留 5.2.2 第二板斧——由弱生强 这一目录坐标；解释它如何参与用RefBase、sp/wp、Thread、同步类、Looper和Handler建立对象与任务寿命模型；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭强弱计数轨迹、首次引用/最后引用回调、线程状态、锁顺序、Message入队与唤醒时间恢复同输入结果。

### 5.2.3 第三板斧——破解生死魔咒

5.2.3 第三板斧——破解生死魔咒在“用RefBase、sp/wp、Thread、同步类、Looper和Handler建立对象与任务寿命模型”中的责任要翻译成一个可推翻的运行合同：明确入口、线程或进程、状态拥有者、正常结果和首个错误返回，再由强弱计数轨迹、首次引用/最后引用回调、线程状态、锁顺序、Message入队与唤醒时间复核。

处理 5.2.3 第三板斧——破解生死魔咒 时先执行“锁定历史基线”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“复现正常轨迹”后仅改变一个条件，观察强弱计数轨迹、首次引用/最后引用回调、线程状态、锁顺序、Message入队与唤醒时间中哪一项先偏离。

该节点的四级证据是：保留 5.2.3 第三板斧——破解生死魔咒 这一目录坐标；解释它如何参与用RefBase、sp/wp、Thread、同步类、Looper和Handler建立对象与任务寿命模型；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭强弱计数轨迹、首次引用/最后引用回调、线程状态、锁顺序、Message入队与唤醒时间恢复同输入结果。

### 5.2.4 轻量级的引用计数控制类LightRefBase

5.2.4 轻量级的引用计数控制类LightRefBase在“用RefBase、sp/wp、Thread、同步类、Looper和Handler建立对象与任务寿命模型”中的责任要翻译成一个可推翻的运行合同：明确入口、线程或进程、状态拥有者、正常结果和首个错误返回，再由强弱计数轨迹、首次引用/最后引用回调、线程状态、锁顺序、Message入队与唤醒时间复核。

处理 5.2.4 轻量级的引用计数控制类LightRefBase 时先执行“复现正常轨迹”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“注入单一故障”后仅改变一个条件，观察强弱计数轨迹、首次引用/最后引用回调、线程状态、锁顺序、Message入队与唤醒时间中哪一项先偏离。

该节点的四级证据是：保留 5.2.4 轻量级的引用计数控制类LightRefBase 这一目录坐标；解释它如何参与用RefBase、sp/wp、Thread、同步类、Looper和Handler建立对象与任务寿命模型；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭强弱计数轨迹、首次引用/最后引用回调、线程状态、锁顺序、Message入队与唤醒时间恢复同输入结果。

### 5.2.5 题外话——三板斧的来历

5.2.5 题外话——三板斧的来历在“用RefBase、sp/wp、Thread、同步类、Looper和Handler建立对象与任务寿命模型”中的责任要翻译成一个可推翻的运行合同：明确入口、线程或进程、状态拥有者、正常结果和首个错误返回，再由强弱计数轨迹、首次引用/最后引用回调、线程状态、锁顺序、Message入队与唤醒时间复核。

处理 5.2.5 题外话——三板斧的来历 时先执行“注入单一故障”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“定位首个分叉”后仅改变一个条件，观察强弱计数轨迹、首次引用/最后引用回调、线程状态、锁顺序、Message入队与唤醒时间中哪一项先偏离。

该节点的四级证据是：保留 5.2.5 题外话——三板斧的来历 这一目录坐标；解释它如何参与用RefBase、sp/wp、Thread、同步类、Looper和Handler建立对象与任务寿命模型；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭强弱计数轨迹、首次引用/最后引用回调、线程状态、锁顺序、Message入队与唤醒时间恢复同输入结果。

### 5.3 Thread类及常用同步类分析

5.3 Thread类及常用同步类分析在“用RefBase、sp/wp、Thread、同步类、Looper和Handler建立对象与任务寿命模型”中的责任由队列头时间、消费线程和唤醒源共同决定。固定消息与时钟后延迟一次消费，比较入队、唤醒、执行和释放四个时间点，不能把容器结构当调度语义。

处理 5.3 Thread类及常用同步类分析 时先执行“定位首个分叉”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“恢复同输入重放”后仅改变一个条件，观察强弱计数轨迹、首次引用/最后引用回调、线程状态、锁顺序、Message入队与唤醒时间中哪一项先偏离。

该节点的四级证据是：保留 5.3 Thread类及常用同步类分析 这一目录坐标；解释它如何参与用RefBase、sp/wp、Thread、同步类、Looper和Handler建立对象与任务寿命模型；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭强弱计数轨迹、首次引用/最后引用回调、线程状态、锁顺序、Message入队与唤醒时间恢复同输入结果。

### 5.3.1 一个变量引发的思考

5.3.1 一个变量引发的思考在“用RefBase、sp/wp、Thread、同步类、Looper和Handler建立对象与任务寿命模型”中的责任要翻译成一个可推翻的运行合同：明确入口、线程或进程、状态拥有者、正常结果和首个错误返回，再由强弱计数轨迹、首次引用/最后引用回调、线程状态、锁顺序、Message入队与唤醒时间复核。

处理 5.3.1 一个变量引发的思考 时先执行“恢复同输入重放”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“锁定历史基线”后仅改变一个条件，观察强弱计数轨迹、首次引用/最后引用回调、线程状态、锁顺序、Message入队与唤醒时间中哪一项先偏离。

该节点的四级证据是：保留 5.3.1 一个变量引发的思考 这一目录坐标；解释它如何参与用RefBase、sp/wp、Thread、同步类、Looper和Handler建立对象与任务寿命模型；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭强弱计数轨迹、首次引用/最后引用回调、线程状态、锁顺序、Message入队与唤醒时间恢复同输入结果。

### 5.3.2 常用同步类

5.3.2 常用同步类在“用RefBase、sp/wp、Thread、同步类、Looper和Handler建立对象与任务寿命模型”中的责任要翻译成一个可推翻的运行合同：明确入口、线程或进程、状态拥有者、正常结果和首个错误返回，再由强弱计数轨迹、首次引用/最后引用回调、线程状态、锁顺序、Message入队与唤醒时间复核。

处理 5.3.2 常用同步类 时先执行“锁定历史基线”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“复现正常轨迹”后仅改变一个条件，观察强弱计数轨迹、首次引用/最后引用回调、线程状态、锁顺序、Message入队与唤醒时间中哪一项先偏离。

该节点的四级证据是：保留 5.3.2 常用同步类 这一目录坐标；解释它如何参与用RefBase、sp/wp、Thread、同步类、Looper和Handler建立对象与任务寿命模型；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭强弱计数轨迹、首次引用/最后引用回调、线程状态、锁顺序、Message入队与唤醒时间恢复同输入结果。

### 5.4 Looper和Handler类分析

5.4 Looper和Handler类分析在“用RefBase、sp/wp、Thread、同步类、Looper和Handler建立对象与任务寿命模型”中的责任由队列头时间、消费线程和唤醒源共同决定。固定消息与时钟后延迟一次消费，比较入队、唤醒、执行和释放四个时间点，不能把容器结构当调度语义。

处理 5.4 Looper和Handler类分析 时先执行“复现正常轨迹”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“注入单一故障”后仅改变一个条件，观察强弱计数轨迹、首次引用/最后引用回调、线程状态、锁顺序、Message入队与唤醒时间中哪一项先偏离。

该节点的四级证据是：保留 5.4 Looper和Handler类分析 这一目录坐标；解释它如何参与用RefBase、sp/wp、Thread、同步类、Looper和Handler建立对象与任务寿命模型；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭强弱计数轨迹、首次引用/最后引用回调、线程状态、锁顺序、Message入队与唤醒时间恢复同输入结果。

### 5.4.1 Looper类分析

5.4.1 Looper类分析在“用RefBase、sp/wp、Thread、同步类、Looper和Handler建立对象与任务寿命模型”中的责任由队列头时间、消费线程和唤醒源共同决定。固定消息与时钟后延迟一次消费，比较入队、唤醒、执行和释放四个时间点，不能把容器结构当调度语义。

处理 5.4.1 Looper类分析 时先执行“注入单一故障”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“定位首个分叉”后仅改变一个条件，观察强弱计数轨迹、首次引用/最后引用回调、线程状态、锁顺序、Message入队与唤醒时间中哪一项先偏离。

该节点的四级证据是：保留 5.4.1 Looper类分析 这一目录坐标；解释它如何参与用RefBase、sp/wp、Thread、同步类、Looper和Handler建立对象与任务寿命模型；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭强弱计数轨迹、首次引用/最后引用回调、线程状态、锁顺序、Message入队与唤醒时间恢复同输入结果。

### 5.4.2 Handler分析

5.4.2 Handler分析在“用RefBase、sp/wp、Thread、同步类、Looper和Handler建立对象与任务寿命模型”中的责任由队列头时间、消费线程和唤醒源共同决定。固定消息与时钟后延迟一次消费，比较入队、唤醒、执行和释放四个时间点，不能把容器结构当调度语义。

处理 5.4.2 Handler分析 时先执行“定位首个分叉”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“恢复同输入重放”后仅改变一个条件，观察强弱计数轨迹、首次引用/最后引用回调、线程状态、锁顺序、Message入队与唤醒时间中哪一项先偏离。

该节点的四级证据是：保留 5.4.2 Handler分析 这一目录坐标；解释它如何参与用RefBase、sp/wp、Thread、同步类、Looper和Handler建立对象与任务寿命模型；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭强弱计数轨迹、首次引用/最后引用回调、线程状态、锁顺序、Message入队与唤醒时间恢复同输入结果。

### 5.4.3 Looper和Handler的同步关系

5.4.3 Looper和Handler的同步关系在“用RefBase、sp/wp、Thread、同步类、Looper和Handler建立对象与任务寿命模型”中的责任由队列头时间、消费线程和唤醒源共同决定。固定消息与时钟后延迟一次消费，比较入队、唤醒、执行和释放四个时间点，不能把容器结构当调度语义。

处理 5.4.3 Looper和Handler的同步关系 时先执行“恢复同输入重放”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“锁定历史基线”后仅改变一个条件，观察强弱计数轨迹、首次引用/最后引用回调、线程状态、锁顺序、Message入队与唤醒时间中哪一项先偏离。

该节点的四级证据是：保留 5.4.3 Looper和Handler的同步关系 这一目录坐标；解释它如何参与用RefBase、sp/wp、Thread、同步类、Looper和Handler建立对象与任务寿命模型；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭强弱计数轨迹、首次引用/最后引用回调、线程状态、锁顺序、Message入队与唤醒时间恢复同输入结果。

### 5.4.4 HandlerThread介绍

5.4.4 HandlerThread介绍在“用RefBase、sp/wp、Thread、同步类、Looper和Handler建立对象与任务寿命模型”中的责任由队列头时间、消费线程和唤醒源共同决定。固定消息与时钟后延迟一次消费，比较入队、唤醒、执行和释放四个时间点，不能把容器结构当调度语义。

处理 5.4.4 HandlerThread介绍 时先执行“锁定历史基线”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“复现正常轨迹”后仅改变一个条件，观察强弱计数轨迹、首次引用/最后引用回调、线程状态、锁顺序、Message入队与唤醒时间中哪一项先偏离。

该节点的四级证据是：保留 5.4.4 HandlerThread介绍 这一目录坐标；解释它如何参与用RefBase、sp/wp、Thread、同步类、Looper和Handler建立对象与任务寿命模型；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭强弱计数轨迹、首次引用/最后引用回调、线程状态、锁顺序、Message入队与唤醒时间恢复同输入结果。

### 5.5 本章小结

5.5 本章小结在“用RefBase、sp/wp、Thread、同步类、Looper和Handler建立对象与任务寿命模型”中的责任是界定分析分母和结论边界。先列出它包含的入口、对象和退出条件，再用强弱计数轨迹、首次引用/最后引用回调、线程状态、锁顺序、Message入队与唤醒时间排除只凭类名或流程图得出的结论。

处理 5.5 本章小结 时先执行“复现正常轨迹”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“注入单一故障”后仅改变一个条件，观察强弱计数轨迹、首次引用/最后引用回调、线程状态、锁顺序、Message入队与唤醒时间中哪一项先偏离。

该节点的四级证据是：保留 5.5 本章小结 这一目录坐标；解释它如何参与用RefBase、sp/wp、Thread、同步类、Looper和Handler建立对象与任务寿命模型；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭强弱计数轨迹、首次引用/最后引用回调、线程状态、锁顺序、Message入队与唤醒时间恢复同输入结果。

## 章专属复现实验

```bash
mkdir -p aosp-android-2.2_r1
cd aosp-android-2.2_r1
repo init -u https://android.googlesource.com/platform/manifest -b android-2.2_r1
repo sync -c

# 只在可恢复的同版本模拟器或测试机采集
adb shell ps
adb logcat -d -v threadtime
```

运行前保存主机工具链、manifest与产品目标；这些历史分支通常需要隔离的旧工具链环境。诊断命令只在可恢复模拟器或测试机执行，账号、媒体与设备标识使用隔离样本；现代设备输出不得冒充android-2.2_r1结果。


## 练习、答案与四级证据


## 术语复核与本章回顾


完成卷I 第5章 深入理解常见类意味着：能解释用RefBase、sp/wp、Thread、同步类、Looper和Handler建立对象与任务寿命模型，能操作全节点图与诊断切片，能制造把sp等同标准智能指针，或把Handler误认为自动创建工作线程，还能凭强弱计数轨迹、首次引用/最后引用回调、线程状态、锁顺序、Message入队与唤醒时间恢复、复位和交接，而不是只在目录中找到关键词。

## 阅读导航

[← 上一页：卷I 第4章 深入理解zygote](/learn/deep-android-volumes/04-v1-04/dav-v1-04-zygote) · [下一页：卷I 第6章 深入理解Binder →](/learn/deep-android-volumes/06-v1-06/dav-v1-06-binder-native)

