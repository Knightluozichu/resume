---
title: 卷I 第6章 深入理解Binder
description: >-
  卷I 第6章
  深入理解Binder：从MediaServer进入ProcessState、IPCThreadState、驱动协议、ServiceManager与服务客户端，以android-2.2_r1源码、全节点机制图和故障恢复证据完成验收。
type: C
section: 深入Android三卷 · 卷I Native基础
draft: false
math: false
sourceUrl: "https://hzbook-group.iteye.com/group/wiki/3128-Android-internals"
demo: true
order: 6
qualityVersion: 2
practiceMode: diagnosis
sourceMode: independent-rewrite
---


{/* DEEP_ANDROID_VOLUMES_QUALITY_V2 */}

## 从一个可失败的问题开始

卷I 第6章 深入理解Binder不是类名清单，而是在android-2.2_r1中回答：从MediaServer进入ProcessState、IPCThreadState、驱动协议、ServiceManager与服务客户端。先预测请求经过的线程、进程与对象所有者，再运行正常样本；如果设备节点、mmap区域、BC/BR命令、handle/node/ref、线程池、死亡通知和Parcel边界不能指出首个变化，本页结论仍不可交接。

本章不变量是在android-2.2_r1固定输入下，从MediaServer进入ProcessState、IPCThreadState、驱动协议、ServiceManager与服务客户端的入口身份、状态拥有者、成功结果、错误返回和资源释放均可重复观察。将“只讲一次拷贝或Proxy/Stub，遗漏服务发现、线程池与引用语义”作为反例，必须在同一版本、同一输入下看到确定的错误或状态分叉，并能恢复到基线。

## 来源范围、历史标签与当前迁移

卷I 第6章 深入理解Binder以[卷I出版社合法公开样章](https://hzbook-group.iteye.com/group/wiki/3128-Android-internals)核定可见正文、目录与Android 2.2范围；样章以外不宣称复现原书正文。

[AOSP版本与构建号表](https://source.android.com/docs/setup/reference/build-numbers)用于核对2.2/API 8、4.0.1/API 14、4.2.2/API 17及正式源码标签；[android-2.2_r1源码树](https://android.googlesource.com/platform/frameworks/base/+/android-2.2_r1/)提供本页历史机制的一手坐标。当前[Android架构文档](https://source.android.com/docs/core/architecture)只用于迁移对照，ART、Treble、稳定AIDL、模块化系统服务和现代构建工具不得倒填为三卷历史实现。

## 六个机制检查点

、、、、、。
这六项把从MediaServer进入ProcessState、IPCThreadState、驱动协议、ServiceManager与服务客户端落到设备节点、mmap区域、BC/BR命令、handle/node/ref、线程池、死亡通知和Parcel边界，防止目录词出现却没有运行解释。

## 正式目录逐项深读

### 第6章 深入理解Binder

第6章 深入理解Binder在“从MediaServer进入ProcessState、IPCThreadState、驱动协议、ServiceManager与服务客户端”中的责任要沿代理、事务数据、驱动或服务端线程追踪一次请求。记录调用方与接收方身份、同步语义、句柄或对象引用，并用死亡或错误事务验证回收。

处理 第6章 深入理解Binder 时先执行“锁定历史基线”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“复现正常轨迹”后仅改变一个条件，观察设备节点、mmap区域、BC/BR命令、handle/node/ref、线程池、死亡通知和Parcel边界中哪一项先偏离。

该节点的四级证据是：保留 第6章 深入理解Binder 这一目录坐标；解释它如何参与从MediaServer进入ProcessState、IPCThreadState、驱动协议、ServiceManager与服务客户端；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭设备节点、mmap区域、BC/BR命令、handle/node/ref、线程池、死亡通知和Parcel边界恢复同输入结果。

### 6.1 概述

6.1 概述在“从MediaServer进入ProcessState、IPCThreadState、驱动协议、ServiceManager与服务客户端”中的责任是界定分析分母和结论边界。先列出它包含的入口、对象和退出条件，再用设备节点、mmap区域、BC/BR命令、handle/node/ref、线程池、死亡通知和Parcel边界排除只凭类名或流程图得出的结论。

处理 6.1 概述 时先执行“复现正常轨迹”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“注入单一故障”后仅改变一个条件，观察设备节点、mmap区域、BC/BR命令、handle/node/ref、线程池、死亡通知和Parcel边界中哪一项先偏离。

该节点的四级证据是：保留 6.1 概述 这一目录坐标；解释它如何参与从MediaServer进入ProcessState、IPCThreadState、驱动协议、ServiceManager与服务客户端；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭设备节点、mmap区域、BC/BR命令、handle/node/ref、线程池、死亡通知和Parcel边界恢复同输入结果。

### 6.2 庖丁解MediaServer

6.2 庖丁解MediaServer在“从MediaServer进入ProcessState、IPCThreadState、驱动协议、ServiceManager与服务客户端”中的责任要翻译成一个可推翻的运行合同：明确入口、线程或进程、状态拥有者、正常结果和首个错误返回，再由设备节点、mmap区域、BC/BR命令、handle/node/ref、线程池、死亡通知和Parcel边界复核。

处理 6.2 庖丁解MediaServer 时先执行“注入单一故障”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“定位首个分叉”后仅改变一个条件，观察设备节点、mmap区域、BC/BR命令、handle/node/ref、线程池、死亡通知和Parcel边界中哪一项先偏离。

该节点的四级证据是：保留 6.2 庖丁解MediaServer 这一目录坐标；解释它如何参与从MediaServer进入ProcessState、IPCThreadState、驱动协议、ServiceManager与服务客户端；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭设备节点、mmap区域、BC/BR命令、handle/node/ref、线程池、死亡通知和Parcel边界恢复同输入结果。

### 6.2.1 MediaServer的入口函数

6.2.1 MediaServer的入口函数在“从MediaServer进入ProcessState、IPCThreadState、驱动协议、ServiceManager与服务客户端”中的责任要翻译成一个可推翻的运行合同：明确入口、线程或进程、状态拥有者、正常结果和首个错误返回，再由设备节点、mmap区域、BC/BR命令、handle/node/ref、线程池、死亡通知和Parcel边界复核。

处理 6.2.1 MediaServer的入口函数 时先执行“定位首个分叉”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“恢复同输入重放”后仅改变一个条件，观察设备节点、mmap区域、BC/BR命令、handle/node/ref、线程池、死亡通知和Parcel边界中哪一项先偏离。

该节点的四级证据是：保留 6.2.1 MediaServer的入口函数 这一目录坐标；解释它如何参与从MediaServer进入ProcessState、IPCThreadState、驱动协议、ServiceManager与服务客户端；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭设备节点、mmap区域、BC/BR命令、handle/node/ref、线程池、死亡通知和Parcel边界恢复同输入结果。

### 6.2.2 独一无二的ProcessState

6.2.2 独一无二的ProcessState在“从MediaServer进入ProcessState、IPCThreadState、驱动协议、ServiceManager与服务客户端”中的责任要连接system_server记录、调度队列和目标进程回调。保存记录对象与进程身份，制造超时或死亡后检查队列推进、死亡清理和后续请求是否恢复。

处理 6.2.2 独一无二的ProcessState 时先执行“恢复同输入重放”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“锁定历史基线”后仅改变一个条件，观察设备节点、mmap区域、BC/BR命令、handle/node/ref、线程池、死亡通知和Parcel边界中哪一项先偏离。

该节点的四级证据是：保留 6.2.2 独一无二的ProcessState 这一目录坐标；解释它如何参与从MediaServer进入ProcessState、IPCThreadState、驱动协议、ServiceManager与服务客户端；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭设备节点、mmap区域、BC/BR命令、handle/node/ref、线程池、死亡通知和Parcel边界恢复同输入结果。

### 6.2.3 时空穿越魔术——defaultServiceManager

6.2.3 时空穿越魔术——defaultServiceManager在“从MediaServer进入ProcessState、IPCThreadState、驱动协议、ServiceManager与服务客户端”中的责任要沿代理、事务数据、驱动或服务端线程追踪一次请求。记录调用方与接收方身份、同步语义、句柄或对象引用，并用死亡或错误事务验证回收。

处理 6.2.3 时空穿越魔术——defaultServiceManager 时先执行“锁定历史基线”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“复现正常轨迹”后仅改变一个条件，观察设备节点、mmap区域、BC/BR命令、handle/node/ref、线程池、死亡通知和Parcel边界中哪一项先偏离。

该节点的四级证据是：保留 6.2.3 时空穿越魔术——defaultServiceManager 这一目录坐标；解释它如何参与从MediaServer进入ProcessState、IPCThreadState、驱动协议、ServiceManager与服务客户端；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭设备节点、mmap区域、BC/BR命令、handle/node/ref、线程池、死亡通知和Parcel边界恢复同输入结果。

### 6.2.4 注册MediaPlayerService

6.2.4 注册MediaPlayerService在“从MediaServer进入ProcessState、IPCThreadState、驱动协议、ServiceManager与服务客户端”中的责任横跨窗口身份、缓冲区所有权与合成时序。以同一帧记录生产、提交、层级、消费和移除，改变Z序或所有者后检查旧Surface是否仍可见或占用资源。

处理 6.2.4 注册MediaPlayerService 时先执行“复现正常轨迹”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“注入单一故障”后仅改变一个条件，观察设备节点、mmap区域、BC/BR命令、handle/node/ref、线程池、死亡通知和Parcel边界中哪一项先偏离。

该节点的四级证据是：保留 6.2.4 注册MediaPlayerService 这一目录坐标；解释它如何参与从MediaServer进入ProcessState、IPCThreadState、驱动协议、ServiceManager与服务客户端；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭设备节点、mmap区域、BC/BR命令、handle/node/ref、线程池、死亡通知和Parcel边界恢复同输入结果。

### 6.2.5 StartThreadPool和joinThreadPool分析

6.2.5 StartThreadPool和joinThreadPool分析在“从MediaServer进入ProcessState、IPCThreadState、驱动协议、ServiceManager与服务客户端”中的责任由队列头时间、消费线程和唤醒源共同决定。固定消息与时钟后延迟一次消费，比较入队、唤醒、执行和释放四个时间点，不能把容器结构当调度语义。

处理 6.2.5 StartThreadPool和joinThreadPool分析 时先执行“注入单一故障”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“定位首个分叉”后仅改变一个条件，观察设备节点、mmap区域、BC/BR命令、handle/node/ref、线程池、死亡通知和Parcel边界中哪一项先偏离。

该节点的四级证据是：保留 6.2.5 StartThreadPool和joinThreadPool分析 这一目录坐标；解释它如何参与从MediaServer进入ProcessState、IPCThreadState、驱动协议、ServiceManager与服务客户端；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭设备节点、mmap区域、BC/BR命令、handle/node/ref、线程池、死亡通知和Parcel边界恢复同输入结果。

### 6.2.6 你彻底明白了吗

6.2.6 你彻底明白了吗在“从MediaServer进入ProcessState、IPCThreadState、驱动协议、ServiceManager与服务客户端”中的责任是界定分析分母和结论边界。先列出它包含的入口、对象和退出条件，再用设备节点、mmap区域、BC/BR命令、handle/node/ref、线程池、死亡通知和Parcel边界排除只凭类名或流程图得出的结论。

处理 6.2.6 你彻底明白了吗 时先执行“定位首个分叉”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“恢复同输入重放”后仅改变一个条件，观察设备节点、mmap区域、BC/BR命令、handle/node/ref、线程池、死亡通知和Parcel边界中哪一项先偏离。

该节点的四级证据是：保留 6.2.6 你彻底明白了吗 这一目录坐标；解释它如何参与从MediaServer进入ProcessState、IPCThreadState、驱动协议、ServiceManager与服务客户端；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭设备节点、mmap区域、BC/BR命令、handle/node/ref、线程池、死亡通知和Parcel边界恢复同输入结果。

### 6.3 服务总管ServiceManager

6.3 服务总管ServiceManager在“从MediaServer进入ProcessState、IPCThreadState、驱动协议、ServiceManager与服务客户端”中的责任要沿代理、事务数据、驱动或服务端线程追踪一次请求。记录调用方与接收方身份、同步语义、句柄或对象引用，并用死亡或错误事务验证回收。

处理 6.3 服务总管ServiceManager 时先执行“恢复同输入重放”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“锁定历史基线”后仅改变一个条件，观察设备节点、mmap区域、BC/BR命令、handle/node/ref、线程池、死亡通知和Parcel边界中哪一项先偏离。

该节点的四级证据是：保留 6.3 服务总管ServiceManager 这一目录坐标；解释它如何参与从MediaServer进入ProcessState、IPCThreadState、驱动协议、ServiceManager与服务客户端；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭设备节点、mmap区域、BC/BR命令、handle/node/ref、线程池、死亡通知和Parcel边界恢复同输入结果。

### 6.3.1 ServiceManager的原理

6.3.1 ServiceManager的原理在“从MediaServer进入ProcessState、IPCThreadState、驱动协议、ServiceManager与服务客户端”中的责任要沿代理、事务数据、驱动或服务端线程追踪一次请求。记录调用方与接收方身份、同步语义、句柄或对象引用，并用死亡或错误事务验证回收。

处理 6.3.1 ServiceManager的原理 时先执行“锁定历史基线”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“复现正常轨迹”后仅改变一个条件，观察设备节点、mmap区域、BC/BR命令、handle/node/ref、线程池、死亡通知和Parcel边界中哪一项先偏离。

该节点的四级证据是：保留 6.3.1 ServiceManager的原理 这一目录坐标；解释它如何参与从MediaServer进入ProcessState、IPCThreadState、驱动协议、ServiceManager与服务客户端；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭设备节点、mmap区域、BC/BR命令、handle/node/ref、线程池、死亡通知和Parcel边界恢复同输入结果。

### 6.3.2 服务的注册

6.3.2 服务的注册在“从MediaServer进入ProcessState、IPCThreadState、驱动协议、ServiceManager与服务客户端”中的责任必须落到创建者、触发条件和服务状态。用同一配置分别触发成功与重启路径，核对PID、属性、退出码和父子关系，防止把声明文件误当顺序脚本。

处理 6.3.2 服务的注册 时先执行“复现正常轨迹”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“注入单一故障”后仅改变一个条件，观察设备节点、mmap区域、BC/BR命令、handle/node/ref、线程池、死亡通知和Parcel边界中哪一项先偏离。

该节点的四级证据是：保留 6.3.2 服务的注册 这一目录坐标；解释它如何参与从MediaServer进入ProcessState、IPCThreadState、驱动协议、ServiceManager与服务客户端；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭设备节点、mmap区域、BC/BR命令、handle/node/ref、线程池、死亡通知和Parcel边界恢复同输入结果。

### 6.3.3 ServiceManager存在的意义

6.3.3 ServiceManager存在的意义在“从MediaServer进入ProcessState、IPCThreadState、驱动协议、ServiceManager与服务客户端”中的责任要沿代理、事务数据、驱动或服务端线程追踪一次请求。记录调用方与接收方身份、同步语义、句柄或对象引用，并用死亡或错误事务验证回收。

处理 6.3.3 ServiceManager存在的意义 时先执行“注入单一故障”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“定位首个分叉”后仅改变一个条件，观察设备节点、mmap区域、BC/BR命令、handle/node/ref、线程池、死亡通知和Parcel边界中哪一项先偏离。

该节点的四级证据是：保留 6.3.3 ServiceManager存在的意义 这一目录坐标；解释它如何参与从MediaServer进入ProcessState、IPCThreadState、驱动协议、ServiceManager与服务客户端；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭设备节点、mmap区域、BC/BR命令、handle/node/ref、线程池、死亡通知和Parcel边界恢复同输入结果。

### 6.4 MediaPlayerService和它的Client

6.4 MediaPlayerService和它的Client在“从MediaServer进入ProcessState、IPCThreadState、驱动协议、ServiceManager与服务客户端”中的责任横跨窗口身份、缓冲区所有权与合成时序。以同一帧记录生产、提交、层级、消费和移除，改变Z序或所有者后检查旧Surface是否仍可见或占用资源。

处理 6.4 MediaPlayerService和它的Client 时先执行“定位首个分叉”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“恢复同输入重放”后仅改变一个条件，观察设备节点、mmap区域、BC/BR命令、handle/node/ref、线程池、死亡通知和Parcel边界中哪一项先偏离。

该节点的四级证据是：保留 6.4 MediaPlayerService和它的Client 这一目录坐标；解释它如何参与从MediaServer进入ProcessState、IPCThreadState、驱动协议、ServiceManager与服务客户端；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭设备节点、mmap区域、BC/BR命令、handle/node/ref、线程池、死亡通知和Parcel边界恢复同输入结果。

### 6.4.1 查询ServiceManager

6.4.1 查询ServiceManager在“从MediaServer进入ProcessState、IPCThreadState、驱动协议、ServiceManager与服务客户端”中的责任要沿代理、事务数据、驱动或服务端线程追踪一次请求。记录调用方与接收方身份、同步语义、句柄或对象引用，并用死亡或错误事务验证回收。

处理 6.4.1 查询ServiceManager 时先执行“恢复同输入重放”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“锁定历史基线”后仅改变一个条件，观察设备节点、mmap区域、BC/BR命令、handle/node/ref、线程池、死亡通知和Parcel边界中哪一项先偏离。

该节点的四级证据是：保留 6.4.1 查询ServiceManager 这一目录坐标；解释它如何参与从MediaServer进入ProcessState、IPCThreadState、驱动协议、ServiceManager与服务客户端；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭设备节点、mmap区域、BC/BR命令、handle/node/ref、线程池、死亡通知和Parcel边界恢复同输入结果。

### 6.4.2 子承父业

6.4.2 子承父业在“从MediaServer进入ProcessState、IPCThreadState、驱动协议、ServiceManager与服务客户端”中的责任要翻译成一个可推翻的运行合同：明确入口、线程或进程、状态拥有者、正常结果和首个错误返回，再由设备节点、mmap区域、BC/BR命令、handle/node/ref、线程池、死亡通知和Parcel边界复核。

处理 6.4.2 子承父业 时先执行“锁定历史基线”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“复现正常轨迹”后仅改变一个条件，观察设备节点、mmap区域、BC/BR命令、handle/node/ref、线程池、死亡通知和Parcel边界中哪一项先偏离。

该节点的四级证据是：保留 6.4.2 子承父业 这一目录坐标；解释它如何参与从MediaServer进入ProcessState、IPCThreadState、驱动协议、ServiceManager与服务客户端；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭设备节点、mmap区域、BC/BR命令、handle/node/ref、线程池、死亡通知和Parcel边界恢复同输入结果。

### 6.5 拓展思考

6.5 拓展思考在“从MediaServer进入ProcessState、IPCThreadState、驱动协议、ServiceManager与服务客户端”中的责任是界定分析分母和结论边界。先列出它包含的入口、对象和退出条件，再用设备节点、mmap区域、BC/BR命令、handle/node/ref、线程池、死亡通知和Parcel边界排除只凭类名或流程图得出的结论。

处理 6.5 拓展思考 时先执行“复现正常轨迹”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“注入单一故障”后仅改变一个条件，观察设备节点、mmap区域、BC/BR命令、handle/node/ref、线程池、死亡通知和Parcel边界中哪一项先偏离。

该节点的四级证据是：保留 6.5 拓展思考 这一目录坐标；解释它如何参与从MediaServer进入ProcessState、IPCThreadState、驱动协议、ServiceManager与服务客户端；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭设备节点、mmap区域、BC/BR命令、handle/node/ref、线程池、死亡通知和Parcel边界恢复同输入结果。

### 6.5.1 Binder和线程的关系

6.5.1 Binder和线程的关系在“从MediaServer进入ProcessState、IPCThreadState、驱动协议、ServiceManager与服务客户端”中的责任要沿代理、事务数据、驱动或服务端线程追踪一次请求。记录调用方与接收方身份、同步语义、句柄或对象引用，并用死亡或错误事务验证回收。

处理 6.5.1 Binder和线程的关系 时先执行“注入单一故障”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“定位首个分叉”后仅改变一个条件，观察设备节点、mmap区域、BC/BR命令、handle/node/ref、线程池、死亡通知和Parcel边界中哪一项先偏离。

该节点的四级证据是：保留 6.5.1 Binder和线程的关系 这一目录坐标；解释它如何参与从MediaServer进入ProcessState、IPCThreadState、驱动协议、ServiceManager与服务客户端；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭设备节点、mmap区域、BC/BR命令、handle/node/ref、线程池、死亡通知和Parcel边界恢复同输入结果。

### 6.5.2 死亡通知

6.5.2 死亡通知在“从MediaServer进入ProcessState、IPCThreadState、驱动协议、ServiceManager与服务客户端”中的责任连接系统服务状态、SystemUI进程和系统窗口。记录图标或disable位的发送者、Binder回调、窗口变化与重启恢复，不能按普通Activity页面解释。

处理 6.5.2 死亡通知 时先执行“定位首个分叉”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“恢复同输入重放”后仅改变一个条件，观察设备节点、mmap区域、BC/BR命令、handle/node/ref、线程池、死亡通知和Parcel边界中哪一项先偏离。

该节点的四级证据是：保留 6.5.2 死亡通知 这一目录坐标；解释它如何参与从MediaServer进入ProcessState、IPCThreadState、驱动协议、ServiceManager与服务客户端；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭设备节点、mmap区域、BC/BR命令、handle/node/ref、线程池、死亡通知和Parcel边界恢复同输入结果。

### 6.5.3 匿名Service

6.5.3 匿名Service在“从MediaServer进入ProcessState、IPCThreadState、驱动协议、ServiceManager与服务客户端”中的责任要连接system_server记录、调度队列和目标进程回调。保存记录对象与进程身份，制造超时或死亡后检查队列推进、死亡清理和后续请求是否恢复。

处理 6.5.3 匿名Service 时先执行“恢复同输入重放”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“锁定历史基线”后仅改变一个条件，观察设备节点、mmap区域、BC/BR命令、handle/node/ref、线程池、死亡通知和Parcel边界中哪一项先偏离。

该节点的四级证据是：保留 6.5.3 匿名Service 这一目录坐标；解释它如何参与从MediaServer进入ProcessState、IPCThreadState、驱动协议、ServiceManager与服务客户端；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭设备节点、mmap区域、BC/BR命令、handle/node/ref、线程池、死亡通知和Parcel边界恢复同输入结果。

### 6.6 学以致用

6.6 学以致用在“从MediaServer进入ProcessState、IPCThreadState、驱动协议、ServiceManager与服务客户端”中的责任要翻译成一个可推翻的运行合同：明确入口、线程或进程、状态拥有者、正常结果和首个错误返回，再由设备节点、mmap区域、BC/BR命令、handle/node/ref、线程池、死亡通知和Parcel边界复核。

处理 6.6 学以致用 时先执行“锁定历史基线”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“复现正常轨迹”后仅改变一个条件，观察设备节点、mmap区域、BC/BR命令、handle/node/ref、线程池、死亡通知和Parcel边界中哪一项先偏离。

该节点的四级证据是：保留 6.6 学以致用 这一目录坐标；解释它如何参与从MediaServer进入ProcessState、IPCThreadState、驱动协议、ServiceManager与服务客户端；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭设备节点、mmap区域、BC/BR命令、handle/node/ref、线程池、死亡通知和Parcel边界恢复同输入结果。

### 6.6.1 纯Native的Service

6.6.1 纯Native的Service在“从MediaServer进入ProcessState、IPCThreadState、驱动协议、ServiceManager与服务客户端”中的责任要连接system_server记录、调度队列和目标进程回调。保存记录对象与进程身份，制造超时或死亡后检查队列推进、死亡清理和后续请求是否恢复。

处理 6.6.1 纯Native的Service 时先执行“复现正常轨迹”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“注入单一故障”后仅改变一个条件，观察设备节点、mmap区域、BC/BR命令、handle/node/ref、线程池、死亡通知和Parcel边界中哪一项先偏离。

该节点的四级证据是：保留 6.6.1 纯Native的Service 这一目录坐标；解释它如何参与从MediaServer进入ProcessState、IPCThreadState、驱动协议、ServiceManager与服务客户端；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭设备节点、mmap区域、BC/BR命令、handle/node/ref、线程池、死亡通知和Parcel边界恢复同输入结果。

### 6.6.2 AIDL

6.6.2 AIDL在“从MediaServer进入ProcessState、IPCThreadState、驱动协议、ServiceManager与服务客户端”中的责任要沿代理、事务数据、驱动或服务端线程追踪一次请求。记录调用方与接收方身份、同步语义、句柄或对象引用，并用死亡或错误事务验证回收。

处理 6.6.2 AIDL 时先执行“注入单一故障”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“定位首个分叉”后仅改变一个条件，观察设备节点、mmap区域、BC/BR命令、handle/node/ref、线程池、死亡通知和Parcel边界中哪一项先偏离。

该节点的四级证据是：保留 6.6.2 AIDL 这一目录坐标；解释它如何参与从MediaServer进入ProcessState、IPCThreadState、驱动协议、ServiceManager与服务客户端；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭设备节点、mmap区域、BC/BR命令、handle/node/ref、线程池、死亡通知和Parcel边界恢复同输入结果。

### 6.7 本章小结

6.7 本章小结在“从MediaServer进入ProcessState、IPCThreadState、驱动协议、ServiceManager与服务客户端”中的责任是界定分析分母和结论边界。先列出它包含的入口、对象和退出条件，再用设备节点、mmap区域、BC/BR命令、handle/node/ref、线程池、死亡通知和Parcel边界排除只凭类名或流程图得出的结论。

处理 6.7 本章小结 时先执行“定位首个分叉”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“恢复同输入重放”后仅改变一个条件，观察设备节点、mmap区域、BC/BR命令、handle/node/ref、线程池、死亡通知和Parcel边界中哪一项先偏离。

该节点的四级证据是：保留 6.7 本章小结 这一目录坐标；解释它如何参与从MediaServer进入ProcessState、IPCThreadState、驱动协议、ServiceManager与服务客户端；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭设备节点、mmap区域、BC/BR命令、handle/node/ref、线程池、死亡通知和Parcel边界恢复同输入结果。

## 章专属复现实验

```bash
mkdir -p aosp-android-2.2_r1
cd aosp-android-2.2_r1
repo init -u https://android.googlesource.com/platform/manifest -b android-2.2_r1
repo sync -c

# 只在可恢复的同版本模拟器或测试机采集
adb shell service list
adb shell ls -l /dev/binder
```

运行前保存主机工具链、manifest与产品目标；这些历史分支通常需要隔离的旧工具链环境。诊断命令只在可恢复模拟器或测试机执行，账号、媒体与设备标识使用隔离样本；现代设备输出不得冒充android-2.2_r1结果。


## 练习、答案与四级证据


## 术语复核与本章回顾


完成卷I 第6章 深入理解Binder意味着：能解释从MediaServer进入ProcessState、IPCThreadState、驱动协议、ServiceManager与服务客户端，能操作全节点图与诊断切片，能制造只讲一次拷贝或Proxy/Stub，遗漏服务发现、线程池与引用语义，还能凭设备节点、mmap区域、BC/BR命令、handle/node/ref、线程池、死亡通知和Parcel边界恢复、复位和交接，而不是只在目录中找到关键词。

## 阅读导航

[← 上一页：卷I 第5章 深入理解常见类](/learn/deep-android-volumes/05-v1-05/dav-v1-05-common-classes) · [下一页：卷I 第7章 深入理解Audio系统 →](/learn/deep-android-volumes/07-v1-07/dav-v1-07-audio-native)

