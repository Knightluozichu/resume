---
title: 卷I 第9章 深入理解Vold和Rild
description: >-
  卷I 第9章
  深入理解Vold和Rild：比较存储热插拔的Netlink事件链与基带请求的异步RIL事件链，以android-2.2_r1源码、全节点机制图和故障恢复证据完成验收。
type: C
section: 深入Android三卷 · 卷I 媒体与设备
draft: false
math: false
sourceUrl: 'https://hzbook-group.iteye.com/group/wiki/3128-Android-internals'
demo: true
order: 3
qualityVersion: 2
practiceMode: diagnosis
sourceMode: independent-rewrite
---
import { DavSeriesPipelineLab, DavSeriesExperimentLab, DavSeriesEvidenceLab } from "@/components/mdx/deep-android-volumes/diagrams/dav-v1-09-vold-rild";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能在android-2.2_r1单一历史标签内解释比较存储热插拔的Netlink事件链与基带请求的异步RIL事件链，不跨卷拼接文件与接口身份。
- 能操作全节点机制图，让本页 22 个坐标都具备出现、解释、实验和练习四级证据。
- 能使用章专属诊断切片保存入口、PID/TID、对象或状态、正常结果、错误返回与资源释放。
- 能主动制造“把Vold和Rild并列成两个守护进程名称，未区分同步存储状态机与异步射频协议”，再凭uevent序列、Volume状态、CommandListener会话、RIL请求号、回调线程和超时恢复定位、恢复、复位并以同输入重放。

</Objectives>

{/* DEEP_ANDROID_VOLUMES_QUALITY_V2 */}

## 从一个可失败的问题开始

卷I 第9章 深入理解Vold和Rild不是类名清单，而是在android-2.2_r1中回答：比较存储热插拔的Netlink事件链与基带请求的异步RIL事件链。先预测请求经过的线程、进程与对象所有者，再运行正常样本；如果uevent序列、Volume状态、CommandListener会话、RIL请求号、回调线程和超时恢复不能指出首个变化，本页结论仍不可交接。

本章不变量是在android-2.2_r1固定输入下，比较存储热插拔的Netlink事件链与基带请求的异步RIL事件链的入口身份、状态拥有者、成功结果、错误返回和资源释放均可重复观察。将“把Vold和Rild并列成两个守护进程名称，未区分同步存储状态机与异步射频协议”作为反例，必须在同一版本、同一输入下看到确定的错误或状态分叉，并能恢复到基线。

## 来源范围、历史标签与当前迁移

卷I 第9章 深入理解Vold和Rild以[卷I出版社合法公开样章](https://hzbook-group.iteye.com/group/wiki/3128-Android-internals)核定可见正文、目录与Android 2.2范围；样章以外不宣称复现原书正文。

[AOSP版本与构建号表](https://source.android.com/docs/setup/reference/build-numbers)用于核对2.2/API 8、4.0.1/API 14、4.2.2/API 17及正式源码标签；[android-2.2_r1源码树](https://android.googlesource.com/platform/system/core/+/android-2.2_r1/)提供本页历史机制的一手坐标。当前[Android架构文档](https://source.android.com/docs/core/architecture)只用于迁移对照，ART、Treble、稳定AIDL、模块化系统服务和现代构建工具不得倒填为三卷历史实现。

## 六个机制检查点

<Term def="版本基线在本页用于锁定android-2.2_r1的事实坐标与允许输入。">版本基线</Term>、<Term def="正常轨迹在本页用于标出比较存储热插拔的Netlink事件链与基带请求的异步RIL事件链由谁创建、修改与释放状态。">正常轨迹</Term>、<Term def="首错断点在本页用于保存能复现正常路径的对象身份和时序。">首错断点</Term>、<Term def="对象所有权在本页用于暴露把Vold和Rild并列成两个守护进程名称，未区分同步存储状态机与异步射频协议发生的第一个边界。">对象所有权</Term>、<Term def="恢复条件在本页用于声明故障后重新接收请求所需的恢复条件。">恢复条件</Term>、<Term def="回归证据在本页用于让独立读者凭uevent序列、Volume状态、CommandListener会话、RIL请求号、回调线程和超时恢复得到同一结论。">回归证据</Term>。这六项把比较存储热插拔的Netlink事件链与基带请求的异步RIL事件链落到uevent序列、Volume状态、CommandListener会话、RIL请求号、回调线程和超时恢复，防止目录词出现却没有运行解释。

## 先预测，再操作三个专属实验

围绕卷I 第9章 深入理解Vold和Rild，先预测哪一个对象最先改变、“把Vold和Rild并列成两个守护进程名称，未区分同步存储状态机与异步射频协议”应在哪个边界暴露，以及恢复后uevent序列、Volume状态、CommandListener会话、RIL请求号、回调线程和超时恢复中的哪份证据必须回到原值；然后按全节点、版本因果、故障恢复的顺序操作。

<Stepper>
  <Step title="1. 全节点机制与可执行探针">
    从卷I 第9章 深入理解Vold和Rild的 22 个正式坐标中选择任一节点与处理阶段，比较章专属机制说明和探针；列表完整保留，不截断节点。

    <DavSeriesPipelineLab />

  </Step>
  <Step title="2. 同输入版本与故障边界">
    保持比较存储热插拔的Netlink事件链与基带请求的异步RIL事件链的输入不变，只切换正常、标签错配、所有者死亡或线程停滞，解释可见结果为何变化。

    <DavSeriesExperimentLab />

  </Step>
  <Step title="3. 基线、首错、恢复与复位">
    保存卷I 第9章 深入理解Vold和Rild的首个分叉后恢复android-2.2_r1合同，以同输入重放；点击重置，确认状态和输出都回到初始值。

    <DavSeriesEvidenceLab />

  </Step>
</Stepper>

## 正式目录逐项深读

下面保留 22 个正式目录坐标，但不重复套用实验流程。每个条目只回答它在比较存储热插拔的Netlink事件链与基带请求的异步RIL事件链中的独立责任；共同的定位、单变量注错、恢复和重放动作统一由上方实验与下方练习验收。

### 第9章 深入理解Vold和Rild

第9章 深入理解Vold和Rild在“比较存储热插拔的Netlink事件链与基带请求的异步RIL事件链”中的责任必须分清内核事件、守护进程状态机和异步基带协议。给事件或请求编号，保存状态迁移、回调线程、超时和恢复，禁止因两个守护进程并列就假定相同语义。

### 9.1 概述

9.1 概述在“比较存储热插拔的Netlink事件链与基带请求的异步RIL事件链”中的责任是界定分析分母和结论边界。先列出它包含的入口、对象和退出条件，再用uevent序列、Volume状态、CommandListener会话、RIL请求号、回调线程和超时恢复排除只凭类名或流程图得出的结论。

### 9.2 Vold的原理与机制分析

9.2 Vold的原理与机制分析在“比较存储热插拔的Netlink事件链与基带请求的异步RIL事件链”中的责任必须分清内核事件、守护进程状态机和异步基带协议。给事件或请求编号，保存状态迁移、回调线程、超时和恢复，禁止因两个守护进程并列就假定相同语义。

### 9.2.1 Netlink和Uevent介绍

9.2.1 Netlink和Uevent介绍在“比较存储热插拔的Netlink事件链与基带请求的异步RIL事件链”中的责任必须分清内核事件、守护进程状态机和异步基带协议。给事件或请求编号，保存状态迁移、回调线程、超时和恢复，禁止因两个守护进程并列就假定相同语义。

### 9.2.2 初识Vold

9.2.2 初识Vold在“比较存储热插拔的Netlink事件链与基带请求的异步RIL事件链”中的责任是界定分析分母和结论边界。先列出它包含的入口、对象和退出条件，再用uevent序列、Volume状态、CommandListener会话、RIL请求号、回调线程和超时恢复排除只凭类名或流程图得出的结论。

### 9.2.3 NetlinkManager模块分析

9.2.3 NetlinkManager模块分析在“比较存储热插拔的Netlink事件链与基带请求的异步RIL事件链”中的责任必须分清内核事件、守护进程状态机和异步基带协议。给事件或请求编号，保存状态迁移、回调线程、超时和恢复，禁止因两个守护进程并列就假定相同语义。

### 9.2.4 VolumeManager模块分析

9.2.4 VolumeManager模块分析在“比较存储热插拔的Netlink事件链与基带请求的异步RIL事件链”中的责任必须分清内核事件、守护进程状态机和异步基带协议。给事件或请求编号，保存状态迁移、回调线程、超时和恢复，禁止因两个守护进程并列就假定相同语义。

### 9.2.5 CommandListener模块分析

9.2.5 CommandListener模块分析在“比较存储热插拔的Netlink事件链与基带请求的异步RIL事件链”中的责任必须分清内核事件、守护进程状态机和异步基带协议。给事件或请求编号，保存状态迁移、回调线程、超时和恢复，禁止因两个守护进程并列就假定相同语义。

### 9.2.6 Vold实例分析

9.2.6 Vold实例分析在“比较存储热插拔的Netlink事件链与基带请求的异步RIL事件链”中的责任必须分清内核事件、守护进程状态机和异步基带协议。给事件或请求编号，保存状态迁移、回调线程、超时和恢复，禁止因两个守护进程并列就假定相同语义。

### 9.2.7 关于Vold的总结

9.2.7 关于Vold的总结在“比较存储热插拔的Netlink事件链与基带请求的异步RIL事件链”中的责任是界定分析分母和结论边界。先列出它包含的入口、对象和退出条件，再用uevent序列、Volume状态、CommandListener会话、RIL请求号、回调线程和超时恢复排除只凭类名或流程图得出的结论。

### 9.3 Rild的原理与机制分析

9.3 Rild的原理与机制分析在“比较存储热插拔的Netlink事件链与基带请求的异步RIL事件链”中的责任必须分清内核事件、守护进程状态机和异步基带协议。给事件或请求编号，保存状态迁移、回调线程、超时和恢复，禁止因两个守护进程并列就假定相同语义。

### 9.3.1 初识Rild

9.3.1 初识Rild在“比较存储热插拔的Netlink事件链与基带请求的异步RIL事件链”中的责任是界定分析分母和结论边界。先列出它包含的入口、对象和退出条件，再用uevent序列、Volume状态、CommandListener会话、RIL请求号、回调线程和超时恢复排除只凭类名或流程图得出的结论。

### 9.3.2 RIL_startEventLoop分析

9.3.2 RIL_startEventLoop分析在“比较存储热插拔的Netlink事件链与基带请求的异步RIL事件链”中的责任必须分清内核事件、守护进程状态机和异步基带协议。给事件或请求编号，保存状态迁移、回调线程、超时和恢复，禁止因两个守护进程并列就假定相同语义。

### 9.3.3 RIL_Init分析

9.3.3 RIL_Init分析在“比较存储热插拔的Netlink事件链与基带请求的异步RIL事件链”中的责任必须分清内核事件、守护进程状态机和异步基带协议。给事件或请求编号，保存状态迁移、回调线程、超时和恢复，禁止因两个守护进程并列就假定相同语义。

### 9.3.4 RIL_register分析

9.3.4 RIL_register分析在“比较存储热插拔的Netlink事件链与基带请求的异步RIL事件链”中的责任必须分清内核事件、守护进程状态机和异步基带协议。给事件或请求编号，保存状态迁移、回调线程、超时和恢复，禁止因两个守护进程并列就假定相同语义。

### 9.3.5 关于Rild main函数的总结

9.3.5 关于Rild main函数的总结在“比较存储热插拔的Netlink事件链与基带请求的异步RIL事件链”中的责任是界定分析分母和结论边界。先列出它包含的入口、对象和退出条件，再用uevent序列、Volume状态、CommandListener会话、RIL请求号、回调线程和超时恢复排除只凭类名或流程图得出的结论。

### 9.3.6 Rild实例分析

9.3.6 Rild实例分析在“比较存储热插拔的Netlink事件链与基带请求的异步RIL事件链”中的责任必须分清内核事件、守护进程状态机和异步基带协议。给事件或请求编号，保存状态迁移、回调线程、超时和恢复，禁止因两个守护进程并列就假定相同语义。

### 9.3.7 关于Rild的总结

9.3.7 关于Rild的总结在“比较存储热插拔的Netlink事件链与基带请求的异步RIL事件链”中的责任是界定分析分母和结论边界。先列出它包含的入口、对象和退出条件，再用uevent序列、Volume状态、CommandListener会话、RIL请求号、回调线程和超时恢复排除只凭类名或流程图得出的结论。

### 9.4 拓展思考

9.4 拓展思考在“比较存储热插拔的Netlink事件链与基带请求的异步RIL事件链”中的责任是界定分析分母和结论边界。先列出它包含的入口、对象和退出条件，再用uevent序列、Volume状态、CommandListener会话、RIL请求号、回调线程和超时恢复排除只凭类名或流程图得出的结论。

### 9.4.1 嵌入式系统的存储知识介绍

9.4.1 嵌入式系统的存储知识介绍在“比较存储热插拔的Netlink事件链与基带请求的异步RIL事件链”中的责任必须分清内核事件、守护进程状态机和异步基带协议。给事件或请求编号，保存状态迁移、回调线程、超时和恢复，禁止因两个守护进程并列就假定相同语义。

### 9.4.2 Rild和Phone的改进探讨

9.4.2 Rild和Phone的改进探讨在“比较存储热插拔的Netlink事件链与基带请求的异步RIL事件链”中的责任必须分清内核事件、守护进程状态机和异步基带协议。给事件或请求编号，保存状态迁移、回调线程、超时和恢复，禁止因两个守护进程并列就假定相同语义。

### 9.5 本章小结

9.5 本章小结在“比较存储热插拔的Netlink事件链与基带请求的异步RIL事件链”中的责任是界定分析分母和结论边界。先列出它包含的入口、对象和退出条件，再用uevent序列、Volume状态、CommandListener会话、RIL请求号、回调线程和超时恢复排除只凭类名或流程图得出的结论。

## 章专属复现实验

```bash
mkdir -p aosp-android-2.2_r1
cd aosp-android-2.2_r1
repo init -u https://android.googlesource.com/platform/manifest -b android-2.2_r1
repo sync -c

# 只在可恢复的同版本模拟器或测试机采集
adb logcat -d -b radio
adb logcat -d -s vold
```

复现卷I 第9章 深入理解Vold和Rild前先保存主机工具链、manifest与产品目标；android-2.2_r1历史分支通常需要隔离的旧工具链环境。与比较存储热插拔的Netlink事件链与基带请求的异步RIL事件链有关的诊断命令只在可恢复模拟器或测试机执行，账号、媒体与设备标识使用隔离样本；现代设备输出不得冒充android-2.2_r1结果。

<Callout type="trap" title="本章核心陷阱">
  把Vold和Rild并列成两个守护进程名称，未区分同步存储状态机与异步射频协议。先恢复android-2.2_r1的真实入口、对象身份和失败返回，再讨论当前Android迁移。
</Callout>

<Callout type="trap" title="跨卷同名不等于同一实现">
  卷I 第9章 深入理解Vold和Rild只在自己的历史标签中成立；Binder、MessageQueue、SystemServer、Audio或窗口机制跨卷复用名称时，必须分别核对文件路径、线程责任和错误语义。
</Callout>

<Callout type="warning" title="系统实验安全边界">
  卷I 第9章 深入理解Vold和Rild涉及的刷机、Root、系统映像、gdb、getevent与输入注入仅用于可恢复测试环境。先备份，再记录回滚条件；任何真实用户数据都不得进入截图、日志或练习答案。
</Callout>

## 练习、答案与四级证据

<Exercises>

**问题 1：历史边界。** 怎样证明本页没有把当前Android实现倒灌到 android-2.2_r1？

<Answer>
  从[官方标签表](https://source.android.com/docs/setup/reference/build-numbers)锁定版本，只引用[该标签源码](https://android.googlesource.com/platform/system/core/+/android-2.2_r1/)解释历史文件与接口；当前架构另列迁移记录。若同名对象路径、职责或返回语义改变，就保留差异而不是拼接流程。
</Answer>

**问题 2：逐节点证据。** 本页 22 个正式坐标如何通过出现、解释、实验与练习四级验收？

<Answer>
  - **第9章 深入理解Vold和Rild**：在“锁定历史基线”保存入口与状态，注入把Vold和Rild并列成两个守护进程名称，未区分同步存储状态机与异步射频协议后恢复，并以uevent序列、Volume状态、CommandListener会话、RIL请求号、回调线程和超时恢复重放。
  - **9.1 概述**：在“复现正常轨迹”保存入口与状态，注入把Vold和Rild并列成两个守护进程名称，未区分同步存储状态机与异步射频协议后恢复，并以uevent序列、Volume状态、CommandListener会话、RIL请求号、回调线程和超时恢复重放。
  - **9.2 Vold的原理与机制分析**：在“注入单一故障”保存入口与状态，注入把Vold和Rild并列成两个守护进程名称，未区分同步存储状态机与异步射频协议后恢复，并以uevent序列、Volume状态、CommandListener会话、RIL请求号、回调线程和超时恢复重放。
  - **9.2.1 Netlink和Uevent介绍**：在“定位首个分叉”保存入口与状态，注入把Vold和Rild并列成两个守护进程名称，未区分同步存储状态机与异步射频协议后恢复，并以uevent序列、Volume状态、CommandListener会话、RIL请求号、回调线程和超时恢复重放。
  - **9.2.2 初识Vold**：在“恢复同输入重放”保存入口与状态，注入把Vold和Rild并列成两个守护进程名称，未区分同步存储状态机与异步射频协议后恢复，并以uevent序列、Volume状态、CommandListener会话、RIL请求号、回调线程和超时恢复重放。
  - **9.2.3 NetlinkManager模块分析**：在“锁定历史基线”保存入口与状态，注入把Vold和Rild并列成两个守护进程名称，未区分同步存储状态机与异步射频协议后恢复，并以uevent序列、Volume状态、CommandListener会话、RIL请求号、回调线程和超时恢复重放。
  - **9.2.4 VolumeManager模块分析**：在“复现正常轨迹”保存入口与状态，注入把Vold和Rild并列成两个守护进程名称，未区分同步存储状态机与异步射频协议后恢复，并以uevent序列、Volume状态、CommandListener会话、RIL请求号、回调线程和超时恢复重放。
  - **9.2.5 CommandListener模块分析**：在“注入单一故障”保存入口与状态，注入把Vold和Rild并列成两个守护进程名称，未区分同步存储状态机与异步射频协议后恢复，并以uevent序列、Volume状态、CommandListener会话、RIL请求号、回调线程和超时恢复重放。
  - **9.2.6 Vold实例分析**：在“定位首个分叉”保存入口与状态，注入把Vold和Rild并列成两个守护进程名称，未区分同步存储状态机与异步射频协议后恢复，并以uevent序列、Volume状态、CommandListener会话、RIL请求号、回调线程和超时恢复重放。
  - **9.2.7 关于Vold的总结**：在“恢复同输入重放”保存入口与状态，注入把Vold和Rild并列成两个守护进程名称，未区分同步存储状态机与异步射频协议后恢复，并以uevent序列、Volume状态、CommandListener会话、RIL请求号、回调线程和超时恢复重放。
  - **9.3 Rild的原理与机制分析**：在“锁定历史基线”保存入口与状态，注入把Vold和Rild并列成两个守护进程名称，未区分同步存储状态机与异步射频协议后恢复，并以uevent序列、Volume状态、CommandListener会话、RIL请求号、回调线程和超时恢复重放。
  - **9.3.1 初识Rild**：在“复现正常轨迹”保存入口与状态，注入把Vold和Rild并列成两个守护进程名称，未区分同步存储状态机与异步射频协议后恢复，并以uevent序列、Volume状态、CommandListener会话、RIL请求号、回调线程和超时恢复重放。
  - **9.3.2 RIL_startEventLoop分析**：在“注入单一故障”保存入口与状态，注入把Vold和Rild并列成两个守护进程名称，未区分同步存储状态机与异步射频协议后恢复，并以uevent序列、Volume状态、CommandListener会话、RIL请求号、回调线程和超时恢复重放。
  - **9.3.3 RIL_Init分析**：在“定位首个分叉”保存入口与状态，注入把Vold和Rild并列成两个守护进程名称，未区分同步存储状态机与异步射频协议后恢复，并以uevent序列、Volume状态、CommandListener会话、RIL请求号、回调线程和超时恢复重放。
  - **9.3.4 RIL_register分析**：在“恢复同输入重放”保存入口与状态，注入把Vold和Rild并列成两个守护进程名称，未区分同步存储状态机与异步射频协议后恢复，并以uevent序列、Volume状态、CommandListener会话、RIL请求号、回调线程和超时恢复重放。
  - **9.3.5 关于Rild main函数的总结**：在“锁定历史基线”保存入口与状态，注入把Vold和Rild并列成两个守护进程名称，未区分同步存储状态机与异步射频协议后恢复，并以uevent序列、Volume状态、CommandListener会话、RIL请求号、回调线程和超时恢复重放。
  - **9.3.6 Rild实例分析**：在“复现正常轨迹”保存入口与状态，注入把Vold和Rild并列成两个守护进程名称，未区分同步存储状态机与异步射频协议后恢复，并以uevent序列、Volume状态、CommandListener会话、RIL请求号、回调线程和超时恢复重放。
  - **9.3.7 关于Rild的总结**：在“注入单一故障”保存入口与状态，注入把Vold和Rild并列成两个守护进程名称，未区分同步存储状态机与异步射频协议后恢复，并以uevent序列、Volume状态、CommandListener会话、RIL请求号、回调线程和超时恢复重放。
  - **9.4 拓展思考**：在“定位首个分叉”保存入口与状态，注入把Vold和Rild并列成两个守护进程名称，未区分同步存储状态机与异步射频协议后恢复，并以uevent序列、Volume状态、CommandListener会话、RIL请求号、回调线程和超时恢复重放。
  - **9.4.1 嵌入式系统的存储知识介绍**：在“恢复同输入重放”保存入口与状态，注入把Vold和Rild并列成两个守护进程名称，未区分同步存储状态机与异步射频协议后恢复，并以uevent序列、Volume状态、CommandListener会话、RIL请求号、回调线程和超时恢复重放。
  - **9.4.2 Rild和Phone的改进探讨**：在“锁定历史基线”保存入口与状态，注入把Vold和Rild并列成两个守护进程名称，未区分同步存储状态机与异步射频协议后恢复，并以uevent序列、Volume状态、CommandListener会话、RIL请求号、回调线程和超时恢复重放。
  - **9.5 本章小结**：在“复现正常轨迹”保存入口与状态，注入把Vold和Rild并列成两个守护进程名称，未区分同步存储状态机与异步射频协议后恢复，并以uevent序列、Volume状态、CommandListener会话、RIL请求号、回调线程和超时恢复重放。
</Answer>

**问题 3：故障恢复。** 面对把Vold和Rild并列成两个守护进程名称，未区分同步存储状态机与异步射频协议，如何确认修复而不是掩盖症状？

<Answer>
  先保存uevent序列、Volume状态、CommandListener会话、RIL请求号、回调线程和超时恢复中的正常基线，再只注入一个错误条件并记录首个分叉。恢复真实标签、状态所有者或消费路径后，以完全相同输入重放；只有在android-2.2_r1固定输入下，比较存储热插拔的Netlink事件链与基带请求的异步RIL事件链的入口身份、状态拥有者、成功结果、错误返回和资源释放均可重复观察重新成立且实验可重置，才算通过。
</Answer>

</Exercises>

## 术语复核与本章回顾

<Glossary>
  <GlossaryItem term="版本基线">
    版本基线在本页用于锁定android-2.2_r1的事实坐标与允许输入。
  </GlossaryItem>
  <GlossaryItem term="正常轨迹">
    正常轨迹在本页用于标出比较存储热插拔的Netlink事件链与基带请求的异步RIL事件链由谁创建、修改与释放状态。
  </GlossaryItem>
  <GlossaryItem term="首错断点">
    首错断点在本页用于保存能复现正常路径的对象身份和时序。
  </GlossaryItem>
  <GlossaryItem term="对象所有权">
    对象所有权在本页用于暴露把Vold和Rild并列成两个守护进程名称，未区分同步存储状态机与异步射频协议发生的第一个边界。
  </GlossaryItem>
  <GlossaryItem term="恢复条件">
    恢复条件在本页用于声明故障后重新接收请求所需的恢复条件。
  </GlossaryItem>
  <GlossaryItem term="回归证据">
    回归证据在本页用于让独立读者凭uevent序列、Volume状态、CommandListener会话、RIL请求号、回调线程和超时恢复得到同一结论。
  </GlossaryItem>
</Glossary>

完成卷I 第9章 深入理解Vold和Rild意味着：能解释比较存储热插拔的Netlink事件链与基带请求的异步RIL事件链，能操作全节点图与诊断切片，能制造把Vold和Rild并列成两个守护进程名称，未区分同步存储状态机与异步射频协议，还能凭uevent序列、Volume状态、CommandListener会话、RIL请求号、回调线程和超时恢复恢复、复位和交接，而不是只在目录中找到关键词。

## 阅读导航

[← 上一页：卷I 第8章 深入理解Surface系统](/learn/deep-android-volumes/08-v1-08/dav-v1-08-surface) · [下一页：卷I 第10章 深入理解MediaScanner →](/learn/deep-android-volumes/10-v1-10/dav-v1-10-media-scanner)

<Attribution
  mode="independent-rewrite"
  sourceBasis="authorized-sample"
  workTitle="邓凡平《深入理解Android：卷I》（机械工业出版社，2011，Android 2.2）"
  adaptedUrl="https://hzbook-group.iteye.com/group/wiki/3128-Android-internals"
/>
