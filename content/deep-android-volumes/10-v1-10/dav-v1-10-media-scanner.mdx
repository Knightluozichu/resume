---
title: 卷I 第10章 深入理解MediaScanner
description: >-
  卷I 第10章
  深入理解MediaScanner：贯通广播接收、扫描服务、Java/JNI/PV解析器与媒体数据库写入，以android-2.2_r1源码、全节点机制图和故障恢复证据完成验收。
type: C
section: 深入Android三卷 · 卷I 媒体与设备
draft: false
math: false
sourceUrl: "https://hzbook-group.iteye.com/group/wiki/3128-Android-internals"
demo: true
order: 4
qualityVersion: 2
practiceMode: diagnosis
sourceMode: independent-rewrite
---


{/* DEEP_ANDROID_VOLUMES_QUALITY_V2 */}

## 从一个可失败的问题开始

卷I 第10章 深入理解MediaScanner不是类名清单，而是在android-2.2_r1中回答：贯通广播接收、扫描服务、Java/JNI/PV解析器与媒体数据库写入。先预测请求经过的线程、进程与对象所有者，再运行正常样本；如果挂载广播、扫描任务、文件元数据、JNI回调、数据库行、重复扫描和损坏文件样本不能指出首个变化，本页结论仍不可交接。

本章不变量是在android-2.2_r1固定输入下，贯通广播接收、扫描服务、Java/JNI/PV解析器与媒体数据库写入的入口身份、状态拥有者、成功结果、错误返回和资源释放均可重复观察。将“只调用scanFile验证成功路径，忽略目录扫描、去重、异常文件与数据库事务”作为反例，必须在同一版本、同一输入下看到确定的错误或状态分叉，并能恢复到基线。

## 来源范围、历史标签与当前迁移

卷I 第10章 深入理解MediaScanner以[卷I出版社合法公开样章](https://hzbook-group.iteye.com/group/wiki/3128-Android-internals)核定可见正文、目录与Android 2.2范围；样章以外不宣称复现原书正文。

[AOSP版本与构建号表](https://source.android.com/docs/setup/reference/build-numbers)用于核对2.2/API 8、4.0.1/API 14、4.2.2/API 17及正式源码标签；[android-2.2_r1源码树](https://android.googlesource.com/platform/frameworks/base/+/android-2.2_r1/)提供本页历史机制的一手坐标。当前[Android架构文档](https://source.android.com/docs/core/architecture)只用于迁移对照，ART、Treble、稳定AIDL、模块化系统服务和现代构建工具不得倒填为三卷历史实现。

## 六个机制检查点

、、、、、。
这六项把贯通广播接收、扫描服务、Java/JNI/PV解析器与媒体数据库写入落到挂载广播、扫描任务、文件元数据、JNI回调、数据库行、重复扫描和损坏文件样本，防止目录词出现却没有运行解释。

## 正式目录逐项深读

### 第10章 深入理解MediaScanner

第10章 深入理解MediaScanner在“贯通广播接收、扫描服务、Java/JNI/PV解析器与媒体数据库写入”中的责任要追踪数据从入口到持久化或观察者回调的完整闭环。固定URI、文件或账户样本，记录事务、远端资源、通知与关闭结果，再用重复输入验证幂等和泄漏。

处理 第10章 深入理解MediaScanner 时先执行“锁定历史基线”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“复现正常轨迹”后仅改变一个条件，观察挂载广播、扫描任务、文件元数据、JNI回调、数据库行、重复扫描和损坏文件样本中哪一项先偏离。

该节点的四级证据是：保留 第10章 深入理解MediaScanner 这一目录坐标；解释它如何参与贯通广播接收、扫描服务、Java/JNI/PV解析器与媒体数据库写入；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭挂载广播、扫描任务、文件元数据、JNI回调、数据库行、重复扫描和损坏文件样本恢复同输入结果。

### 10.1 概述

10.1 概述在“贯通广播接收、扫描服务、Java/JNI/PV解析器与媒体数据库写入”中的责任是界定分析分母和结论边界。先列出它包含的入口、对象和退出条件，再用挂载广播、扫描任务、文件元数据、JNI回调、数据库行、重复扫描和损坏文件样本排除只凭类名或流程图得出的结论。

处理 10.1 概述 时先执行“复现正常轨迹”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“注入单一故障”后仅改变一个条件，观察挂载广播、扫描任务、文件元数据、JNI回调、数据库行、重复扫描和损坏文件样本中哪一项先偏离。

该节点的四级证据是：保留 10.1 概述 这一目录坐标；解释它如何参与贯通广播接收、扫描服务、Java/JNI/PV解析器与媒体数据库写入；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭挂载广播、扫描任务、文件元数据、JNI回调、数据库行、重复扫描和损坏文件样本恢复同输入结果。

### 10.2 android.process.media分析

10.2 android.process.media分析在“贯通广播接收、扫描服务、Java/JNI/PV解析器与媒体数据库写入”中的责任要连接system_server记录、调度队列和目标进程回调。保存记录对象与进程身份，制造超时或死亡后检查队列推进、死亡清理和后续请求是否恢复。

处理 10.2 android.process.media分析 时先执行“注入单一故障”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“定位首个分叉”后仅改变一个条件，观察挂载广播、扫描任务、文件元数据、JNI回调、数据库行、重复扫描和损坏文件样本中哪一项先偏离。

该节点的四级证据是：保留 10.2 android.process.media分析 这一目录坐标；解释它如何参与贯通广播接收、扫描服务、Java/JNI/PV解析器与媒体数据库写入；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭挂载广播、扫描任务、文件元数据、JNI回调、数据库行、重复扫描和损坏文件样本恢复同输入结果。

### 10.2.1 MediaScannerReceiver模块分析

10.2.1 MediaScannerReceiver模块分析在“贯通广播接收、扫描服务、Java/JNI/PV解析器与媒体数据库写入”中的责任要追踪数据从入口到持久化或观察者回调的完整闭环。固定URI、文件或账户样本，记录事务、远端资源、通知与关闭结果，再用重复输入验证幂等和泄漏。

处理 10.2.1 MediaScannerReceiver模块分析 时先执行“定位首个分叉”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“恢复同输入重放”后仅改变一个条件，观察挂载广播、扫描任务、文件元数据、JNI回调、数据库行、重复扫描和损坏文件样本中哪一项先偏离。

该节点的四级证据是：保留 10.2.1 MediaScannerReceiver模块分析 这一目录坐标；解释它如何参与贯通广播接收、扫描服务、Java/JNI/PV解析器与媒体数据库写入；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭挂载广播、扫描任务、文件元数据、JNI回调、数据库行、重复扫描和损坏文件样本恢复同输入结果。

### 10.2.2 MediaScannerService模块分析

10.2.2 MediaScannerService模块分析在“贯通广播接收、扫描服务、Java/JNI/PV解析器与媒体数据库写入”中的责任要追踪数据从入口到持久化或观察者回调的完整闭环。固定URI、文件或账户样本，记录事务、远端资源、通知与关闭结果，再用重复输入验证幂等和泄漏。

处理 10.2.2 MediaScannerService模块分析 时先执行“恢复同输入重放”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“锁定历史基线”后仅改变一个条件，观察挂载广播、扫描任务、文件元数据、JNI回调、数据库行、重复扫描和损坏文件样本中哪一项先偏离。

该节点的四级证据是：保留 10.2.2 MediaScannerService模块分析 这一目录坐标；解释它如何参与贯通广播接收、扫描服务、Java/JNI/PV解析器与媒体数据库写入；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭挂载广播、扫描任务、文件元数据、JNI回调、数据库行、重复扫描和损坏文件样本恢复同输入结果。

### 10.2.3 android.process.media媒体扫描流程总结

10.2.3 android.process.media媒体扫描流程总结在“贯通广播接收、扫描服务、Java/JNI/PV解析器与媒体数据库写入”中的责任是界定分析分母和结论边界。先列出它包含的入口、对象和退出条件，再用挂载广播、扫描任务、文件元数据、JNI回调、数据库行、重复扫描和损坏文件样本排除只凭类名或流程图得出的结论。

处理 10.2.3 android.process.media媒体扫描流程总结 时先执行“锁定历史基线”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“复现正常轨迹”后仅改变一个条件，观察挂载广播、扫描任务、文件元数据、JNI回调、数据库行、重复扫描和损坏文件样本中哪一项先偏离。

该节点的四级证据是：保留 10.2.3 android.process.media媒体扫描流程总结 这一目录坐标；解释它如何参与贯通广播接收、扫描服务、Java/JNI/PV解析器与媒体数据库写入；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭挂载广播、扫描任务、文件元数据、JNI回调、数据库行、重复扫描和损坏文件样本恢复同输入结果。

### 10.3 MediaScanner分析

10.3 MediaScanner分析在“贯通广播接收、扫描服务、Java/JNI/PV解析器与媒体数据库写入”中的责任要追踪数据从入口到持久化或观察者回调的完整闭环。固定URI、文件或账户样本，记录事务、远端资源、通知与关闭结果，再用重复输入验证幂等和泄漏。

处理 10.3 MediaScanner分析 时先执行“复现正常轨迹”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“注入单一故障”后仅改变一个条件，观察挂载广播、扫描任务、文件元数据、JNI回调、数据库行、重复扫描和损坏文件样本中哪一项先偏离。

该节点的四级证据是：保留 10.3 MediaScanner分析 这一目录坐标；解释它如何参与贯通广播接收、扫描服务、Java/JNI/PV解析器与媒体数据库写入；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭挂载广播、扫描任务、文件元数据、JNI回调、数据库行、重复扫描和损坏文件样本恢复同输入结果。

### 10.3.1 Java层分析

10.3.1 Java层分析在“贯通广播接收、扫描服务、Java/JNI/PV解析器与媒体数据库写入”中的责任要翻译成一个可推翻的运行合同：明确入口、线程或进程、状态拥有者、正常结果和首个错误返回，再由挂载广播、扫描任务、文件元数据、JNI回调、数据库行、重复扫描和损坏文件样本复核。

处理 10.3.1 Java层分析 时先执行“注入单一故障”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“定位首个分叉”后仅改变一个条件，观察挂载广播、扫描任务、文件元数据、JNI回调、数据库行、重复扫描和损坏文件样本中哪一项先偏离。

该节点的四级证据是：保留 10.3.1 Java层分析 这一目录坐标；解释它如何参与贯通广播接收、扫描服务、Java/JNI/PV解析器与媒体数据库写入；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭挂载广播、扫描任务、文件元数据、JNI回调、数据库行、重复扫描和损坏文件样本恢复同输入结果。

### 10.3.2 JNI层分析

10.3.2 JNI层分析在“贯通广播接收、扫描服务、Java/JNI/PV解析器与媒体数据库写入”中的责任要翻译成一个可推翻的运行合同：明确入口、线程或进程、状态拥有者、正常结果和首个错误返回，再由挂载广播、扫描任务、文件元数据、JNI回调、数据库行、重复扫描和损坏文件样本复核。

处理 10.3.2 JNI层分析 时先执行“定位首个分叉”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“恢复同输入重放”后仅改变一个条件，观察挂载广播、扫描任务、文件元数据、JNI回调、数据库行、重复扫描和损坏文件样本中哪一项先偏离。

该节点的四级证据是：保留 10.3.2 JNI层分析 这一目录坐标；解释它如何参与贯通广播接收、扫描服务、Java/JNI/PV解析器与媒体数据库写入；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭挂载广播、扫描任务、文件元数据、JNI回调、数据库行、重复扫描和损坏文件样本恢复同输入结果。

### 10.3.3 PVMediaScanner分析

10.3.3 PVMediaScanner分析在“贯通广播接收、扫描服务、Java/JNI/PV解析器与媒体数据库写入”中的责任要追踪数据从入口到持久化或观察者回调的完整闭环。固定URI、文件或账户样本，记录事务、远端资源、通知与关闭结果，再用重复输入验证幂等和泄漏。

处理 10.3.3 PVMediaScanner分析 时先执行“恢复同输入重放”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“锁定历史基线”后仅改变一个条件，观察挂载广播、扫描任务、文件元数据、JNI回调、数据库行、重复扫描和损坏文件样本中哪一项先偏离。

该节点的四级证据是：保留 10.3.3 PVMediaScanner分析 这一目录坐标；解释它如何参与贯通广播接收、扫描服务、Java/JNI/PV解析器与媒体数据库写入；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭挂载广播、扫描任务、文件元数据、JNI回调、数据库行、重复扫描和损坏文件样本恢复同输入结果。

### 10.3.4 关于MediaScanner的总结

10.3.4 关于MediaScanner的总结在“贯通广播接收、扫描服务、Java/JNI/PV解析器与媒体数据库写入”中的责任是界定分析分母和结论边界。先列出它包含的入口、对象和退出条件，再用挂载广播、扫描任务、文件元数据、JNI回调、数据库行、重复扫描和损坏文件样本排除只凭类名或流程图得出的结论。

处理 10.3.4 关于MediaScanner的总结 时先执行“锁定历史基线”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“复现正常轨迹”后仅改变一个条件，观察挂载广播、扫描任务、文件元数据、JNI回调、数据库行、重复扫描和损坏文件样本中哪一项先偏离。

该节点的四级证据是：保留 10.3.4 关于MediaScanner的总结 这一目录坐标；解释它如何参与贯通广播接收、扫描服务、Java/JNI/PV解析器与媒体数据库写入；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭挂载广播、扫描任务、文件元数据、JNI回调、数据库行、重复扫描和损坏文件样本恢复同输入结果。

### 10.4 拓展思考

10.4 拓展思考在“贯通广播接收、扫描服务、Java/JNI/PV解析器与媒体数据库写入”中的责任是界定分析分母和结论边界。先列出它包含的入口、对象和退出条件，再用挂载广播、扫描任务、文件元数据、JNI回调、数据库行、重复扫描和损坏文件样本排除只凭类名或流程图得出的结论。

处理 10.4 拓展思考 时先执行“复现正常轨迹”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“注入单一故障”后仅改变一个条件，观察挂载广播、扫描任务、文件元数据、JNI回调、数据库行、重复扫描和损坏文件样本中哪一项先偏离。

该节点的四级证据是：保留 10.4 拓展思考 这一目录坐标；解释它如何参与贯通广播接收、扫描服务、Java/JNI/PV解析器与媒体数据库写入；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭挂载广播、扫描任务、文件元数据、JNI回调、数据库行、重复扫描和损坏文件样本恢复同输入结果。

### 10.4.1 MediaScannerConnection介绍

10.4.1 MediaScannerConnection介绍在“贯通广播接收、扫描服务、Java/JNI/PV解析器与媒体数据库写入”中的责任要追踪数据从入口到持久化或观察者回调的完整闭环。固定URI、文件或账户样本，记录事务、远端资源、通知与关闭结果，再用重复输入验证幂等和泄漏。

处理 10.4.1 MediaScannerConnection介绍 时先执行“注入单一故障”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“定位首个分叉”后仅改变一个条件，观察挂载广播、扫描任务、文件元数据、JNI回调、数据库行、重复扫描和损坏文件样本中哪一项先偏离。

该节点的四级证据是：保留 10.4.1 MediaScannerConnection介绍 这一目录坐标；解释它如何参与贯通广播接收、扫描服务、Java/JNI/PV解析器与媒体数据库写入；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭挂载广播、扫描任务、文件元数据、JNI回调、数据库行、重复扫描和损坏文件样本恢复同输入结果。

### 10.4.2 我问你答

10.4.2 我问你答在“贯通广播接收、扫描服务、Java/JNI/PV解析器与媒体数据库写入”中的责任要翻译成一个可推翻的运行合同：明确入口、线程或进程、状态拥有者、正常结果和首个错误返回，再由挂载广播、扫描任务、文件元数据、JNI回调、数据库行、重复扫描和损坏文件样本复核。

处理 10.4.2 我问你答 时先执行“定位首个分叉”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“恢复同输入重放”后仅改变一个条件，观察挂载广播、扫描任务、文件元数据、JNI回调、数据库行、重复扫描和损坏文件样本中哪一项先偏离。

该节点的四级证据是：保留 10.4.2 我问你答 这一目录坐标；解释它如何参与贯通广播接收、扫描服务、Java/JNI/PV解析器与媒体数据库写入；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭挂载广播、扫描任务、文件元数据、JNI回调、数据库行、重复扫描和损坏文件样本恢复同输入结果。

### 10.5 本章小结

10.5 本章小结在“贯通广播接收、扫描服务、Java/JNI/PV解析器与媒体数据库写入”中的责任是界定分析分母和结论边界。先列出它包含的入口、对象和退出条件，再用挂载广播、扫描任务、文件元数据、JNI回调、数据库行、重复扫描和损坏文件样本排除只凭类名或流程图得出的结论。

处理 10.5 本章小结 时先执行“恢复同输入重放”：在 android-2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“锁定历史基线”后仅改变一个条件，观察挂载广播、扫描任务、文件元数据、JNI回调、数据库行、重复扫描和损坏文件样本中哪一项先偏离。

该节点的四级证据是：保留 10.5 本章小结 这一目录坐标；解释它如何参与贯通广播接收、扫描服务、Java/JNI/PV解析器与媒体数据库写入；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭挂载广播、扫描任务、文件元数据、JNI回调、数据库行、重复扫描和损坏文件样本恢复同输入结果。

## 章专属复现实验

```bash
mkdir -p aosp-android-2.2_r1
cd aosp-android-2.2_r1
repo init -u https://android.googlesource.com/platform/manifest -b android-2.2_r1
repo sync -c

# 只在可恢复的同版本模拟器或测试机采集
adb logcat -d -s MediaScanner MediaProvider
adb shell dumpsys activity services
```

运行前保存主机工具链、manifest与产品目标；这些历史分支通常需要隔离的旧工具链环境。诊断命令只在可恢复模拟器或测试机执行，账号、媒体与设备标识使用隔离样本；现代设备输出不得冒充android-2.2_r1结果。


## 练习、答案与四级证据


## 术语复核与本章回顾


完成卷I 第10章 深入理解MediaScanner意味着：能解释贯通广播接收、扫描服务、Java/JNI/PV解析器与媒体数据库写入，能操作全节点图与诊断切片，能制造只调用scanFile验证成功路径，忽略目录扫描、去重、异常文件与数据库事务，还能凭挂载广播、扫描任务、文件元数据、JNI回调、数据库行、重复扫描和损坏文件样本恢复、复位和交接，而不是只在目录中找到关键词。

## 阅读导航

[← 上一页：卷I 第9章 深入理解Vold和Rild](/learn/deep-android-volumes/09-v1-09/dav-v1-09-vold-rild) · [下一页：卷II 第1章 搭建Android源码工作环境 →](/learn/deep-android-volumes/11-v2-01/dav-v2-01-source-environment)

