---
title: 卷II 第1章 搭建Android源码工作环境
description: >-
  卷II 第1章 搭建Android源码工作环境：在Android
  4.0.1上建立源码下载、编译与system_process调试基线，以android-4.0.1_r1源码、全节点机制图和故障恢复证据完成验收。
type: C
section: 深入Android三卷 · 卷II Java基础
draft: true
math: false
sourceUrl: "https://www.sanmin.com.tw/product/index/003084876"
demo: true
order: 1
qualityVersion: 2
practiceMode: code
sourceMode: independent-rewrite
---

import {
  DavSeriesPipelineLab,
  DavSeriesExperimentLab,
  DavSeriesEvidenceLab,
} from "@/components/mdx/deep-android-volumes/diagrams/dav-v2-01-source-environment";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能在android-4.0.1_r1单一历史标签内解释在Android 4.0.1上建立源码下载、编译与system_process调试基线，不跨卷拼接文件与接口身份。
- 能操作全节点机制图，让本页 7 个坐标都具备出现、解释、实验和练习四级证据。
- 能使用章专属诊断切片保存入口、PID/TID、对象或状态、正常结果、错误返回与资源释放。
- 能主动制造“沿用卷I 2.2路径或用现代IDE截图代替4.0.1可运行证据”，再凭manifest标签、编译目标、framework产物、Eclipse符号、调试端口与断点线程定位、恢复、复位并以同输入重放。

</Objectives>

{/* DEEP_ANDROID_VOLUMES_QUALITY_V2 */}

## 从一个可失败的问题开始

卷II 第1章 搭建Android源码工作环境不是类名清单，而是在android-4.0.1_r1中回答：在Android 4.0.1上建立源码下载、编译与system_process调试基线。先预测请求经过的线程、进程与对象所有者，再运行正常样本；如果manifest标签、编译目标、framework产物、Eclipse符号、调试端口与断点线程不能指出首个变化，本页结论仍不可交接。

本章不变量是在android-4.0.1_r1固定输入下，在Android 4.0.1上建立源码下载、编译与system_process调试基线的入口身份、状态拥有者、成功结果、错误返回和资源释放均可重复观察。将“沿用卷I 2.2路径或用现代IDE截图代替4.0.1可运行证据”作为反例，必须在同一版本、同一输入下看到确定的错误或状态分叉，并能恢复到基线。

## 来源范围、历史标签与当前迁移

卷II 第1章 搭建Android源码工作环境只以[卷II公开书目与详细目录](https://www.sanmin.com.tw/product/index/003084876)界定Android 4.0.1学习范围；未取得原书正文，以下机制解释不沿用原作表述。

[AOSP版本与构建号表](https://source.android.com/docs/setup/reference/build-numbers)用于核对2.2/API 8、4.0.1/API 14、4.2.2/API 17及正式源码标签；[android-4.0.1_r1源码树](https://android.googlesource.com/platform/manifest/+/android-4.0.1_r1/)提供本页历史机制的一手坐标。当前[Android架构文档](https://source.android.com/docs/core/architecture)只用于迁移对照，ART、Treble、稳定AIDL、模块化系统服务和现代构建工具不得倒填为三卷历史实现。

## 六个机制检查点

<Term def="源码标签在本页用于锁定android-4.0.1_r1的事实坐标与允许输入。">源码标签</Term>、<Term def="构建目标在本页用于标出在Android 4.0.1上建立源码下载、编译与system_process调试基线由谁创建、修改与释放状态。">构建目标</Term>、<Term def="符号入口在本页用于保存能复现正常路径的对象身份和时序。">符号入口</Term>、<Term def="运行产物在本页用于暴露沿用卷I 2.2路径或用现代IDE截图代替4.0.1可运行证据发生的第一个边界。">运行产物</Term>、<Term def="错误日志在本页用于声明故障后重新接收请求所需的恢复条件。">错误日志</Term>、<Term def="复现脚本在本页用于让独立读者凭manifest标签、编译目标、framework产物、Eclipse符号、调试端口与断点线程得到同一结论。">复现脚本</Term>。
这六项把在Android
4.0.1上建立源码下载、编译与system_process调试基线落到manifest标签、编译目标、framework产物、Eclipse符号、调试端口与断点线程，防止目录词出现却没有运行解释。

## 先预测，再操作三个专属实验

预测本页哪一个对象最先改变、故障应在哪个边界暴露、恢复后哪份证据必须回到原值；然后按全节点、版本因果、故障恢复的顺序操作。

<Stepper>
  <Step title="1. 全节点机制与可执行探针">
    选择任一目录节点与处理阶段，比较章专属机制说明和探针；列表完整保留，不截断节点。

    <DavSeriesPipelineLab />

  </Step>
  <Step title="2. 同输入版本与故障边界">
    保持输入不变，只切换正常、标签错配、所有者死亡或线程停滞，解释可见结果为何变化。

    <DavSeriesExperimentLab />

  </Step>
  <Step title="3. 基线、首错、恢复与复位">
    保存首个分叉后恢复历史合同，以同输入重放；点击重置，确认状态和输出都回到初始值。

    <DavSeriesEvidenceLab />

  </Step>
</Stepper>

## 正式目录逐项深读

### 第1章 搭建Android源码工作环境

第1章 搭建Android源码工作环境在“在Android 4.0.1上建立源码下载、编译与system_process调试基线”中的责任是把源码标签、主机工具链、产品目标和运行映像绑定成可复现坐标。产物、符号与设备指纹必须都指向android-4.0.1_r1，否则立即停止比较。

处理 第1章 搭建Android源码工作环境 时先执行“锁定工具链”：在 android-4.0.1_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“定位源码入口”后仅改变一个条件，观察manifest标签、编译目标、framework产物、Eclipse符号、调试端口与断点线程中哪一项先偏离。

该节点的四级证据是：保留 第1章 搭建Android源码工作环境 这一目录坐标；解释它如何参与在Android 4.0.1上建立源码下载、编译与system_process调试基线；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭manifest标签、编译目标、framework产物、Eclipse符号、调试端口与断点线程恢复同输入结果。

### 1.1 Android系统架构

1.1 Android系统架构在“在Android 4.0.1上建立源码下载、编译与system_process调试基线”中的责任要翻译成一个可推翻的运行合同：明确入口、线程或进程、状态拥有者、正常结果和首个错误返回，再由manifest标签、编译目标、framework产物、Eclipse符号、调试端口与断点线程复核。

处理 1.1 Android系统架构 时先执行“定位源码入口”：在 android-4.0.1_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“运行最小切片”后仅改变一个条件，观察manifest标签、编译目标、framework产物、Eclipse符号、调试端口与断点线程中哪一项先偏离。

该节点的四级证据是：保留 1.1 Android系统架构 这一目录坐标；解释它如何参与在Android 4.0.1上建立源码下载、编译与system_process调试基线；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭manifest标签、编译目标、framework产物、Eclipse符号、调试端口与断点线程恢复同输入结果。

### 1.2 搭建开发环境

1.2 搭建开发环境在“在Android 4.0.1上建立源码下载、编译与system_process调试基线”中的责任是把源码标签、主机工具链、产品目标和运行映像绑定成可复现坐标。产物、符号与设备指纹必须都指向android-4.0.1_r1，否则立即停止比较。

处理 1.2 搭建开发环境 时先执行“运行最小切片”：在 android-4.0.1_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“保存构建诊断”后仅改变一个条件，观察manifest标签、编译目标、framework产物、Eclipse符号、调试端口与断点线程中哪一项先偏离。

该节点的四级证据是：保留 1.2 搭建开发环境 这一目录坐标；解释它如何参与在Android 4.0.1上建立源码下载、编译与system_process调试基线；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭manifest标签、编译目标、framework产物、Eclipse符号、调试端口与断点线程恢复同输入结果。

### 1.2.1 下载源码

1.2.1 下载源码在“在Android 4.0.1上建立源码下载、编译与system_process调试基线”中的责任是把源码标签、主机工具链、产品目标和运行映像绑定成可复现坐标。产物、符号与设备指纹必须都指向android-4.0.1_r1，否则立即停止比较。

处理 1.2.1 下载源码 时先执行“保存构建诊断”：在 android-4.0.1_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“回归重放”后仅改变一个条件，观察manifest标签、编译目标、framework产物、Eclipse符号、调试端口与断点线程中哪一项先偏离。

该节点的四级证据是：保留 1.2.1 下载源码 这一目录坐标；解释它如何参与在Android 4.0.1上建立源码下载、编译与system_process调试基线；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭manifest标签、编译目标、framework产物、Eclipse符号、调试端口与断点线程恢复同输入结果。

### 1.2.2 编译源码

1.2.2 编译源码在“在Android 4.0.1上建立源码下载、编译与system_process调试基线”中的责任是把源码标签、主机工具链、产品目标和运行映像绑定成可复现坐标。产物、符号与设备指纹必须都指向android-4.0.1_r1，否则立即停止比较。

处理 1.2.2 编译源码 时先执行“回归重放”：在 android-4.0.1_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“锁定工具链”后仅改变一个条件，观察manifest标签、编译目标、framework产物、Eclipse符号、调试端口与断点线程中哪一项先偏离。

该节点的四级证据是：保留 1.2.2 编译源码 这一目录坐标；解释它如何参与在Android 4.0.1上建立源码下载、编译与system_process调试基线；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭manifest标签、编译目标、framework产物、Eclipse符号、调试端口与断点线程恢复同输入结果。

### 1.2.3 利用Eclipse调试system_process

1.2.3 利用Eclipse调试system_process在“在Android 4.0.1上建立源码下载、编译与system_process调试基线”中的责任是把源码标签、主机工具链、产品目标和运行映像绑定成可复现坐标。产物、符号与设备指纹必须都指向android-4.0.1_r1，否则立即停止比较。

处理 1.2.3 利用Eclipse调试system_process 时先执行“锁定工具链”：在 android-4.0.1_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“定位源码入口”后仅改变一个条件，观察manifest标签、编译目标、framework产物、Eclipse符号、调试端口与断点线程中哪一项先偏离。

该节点的四级证据是：保留 1.2.3 利用Eclipse调试system_process 这一目录坐标；解释它如何参与在Android 4.0.1上建立源码下载、编译与system_process调试基线；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭manifest标签、编译目标、framework产物、Eclipse符号、调试端口与断点线程恢复同输入结果。

### 1.3 本章小结

1.3 本章小结在“在Android 4.0.1上建立源码下载、编译与system_process调试基线”中的责任是界定分析分母和结论边界。先列出它包含的入口、对象和退出条件，再用manifest标签、编译目标、framework产物、Eclipse符号、调试端口与断点线程排除只凭类名或流程图得出的结论。

处理 1.3 本章小结 时先执行“定位源码入口”：在 android-4.0.1_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“运行最小切片”后仅改变一个条件，观察manifest标签、编译目标、framework产物、Eclipse符号、调试端口与断点线程中哪一项先偏离。

该节点的四级证据是：保留 1.3 本章小结 这一目录坐标；解释它如何参与在Android 4.0.1上建立源码下载、编译与system_process调试基线；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭manifest标签、编译目标、framework产物、Eclipse符号、调试端口与断点线程恢复同输入结果。

## 章专属复现实验

```bash
mkdir -p aosp-android-4.0.1_r1
cd aosp-android-4.0.1_r1
repo init -u https://android.googlesource.com/platform/manifest -b android-4.0.1_r1
repo sync -c

# 只在可恢复的同版本模拟器或测试机采集
adb shell getprop ro.build.fingerprint
adb shell ps
```

运行前保存主机工具链、manifest与产品目标；这些历史分支通常需要隔离的旧工具链环境。诊断命令只在可恢复模拟器或测试机执行，账号、媒体与设备标识使用隔离样本；现代设备输出不得冒充android-4.0.1_r1结果。

<Callout type="trap" title="本章核心陷阱">
  沿用卷I
  2.2路径或用现代IDE截图代替4.0.1可运行证据。先恢复android-4.0.1_r1的真实入口、对象身份和失败返回，再讨论当前Android迁移。
</Callout>

<Callout type="trap" title="跨卷同名不等于同一实现">
  卷II 第1章
  搭建Android源码工作环境只在自己的历史标签中成立；Binder、MessageQueue、SystemServer、Audio或窗口机制跨卷复用名称时，必须分别核对文件路径、线程责任和错误语义。
</Callout>

<Callout type="warning" title="系统实验安全边界">
  刷机、Root、系统映像、gdb、getevent与输入注入仅用于可恢复测试环境。先备份，再记录回滚条件；任何真实用户数据都不得进入截图、日志或练习答案。
</Callout>

## 练习、答案与四级证据

<Exercises>

**问题 1：历史边界。** 怎样证明本页没有把当前Android实现倒灌到 android-4.0.1_r1？

<Answer>
  从[官方标签表](https://source.android.com/docs/setup/reference/build-numbers)锁定版本，只引用[该标签源码](https://android.googlesource.com/platform/manifest/+/android-4.0.1_r1/)解释历史文件与接口；当前架构另列迁移记录。若同名对象路径、职责或返回语义改变，就保留差异而不是拼接流程。
</Answer>

**问题 2：逐节点证据。** 本页 7 个正式坐标如何通过出现、解释、实验与练习四级验收？

<Answer>
  - **第1章 搭建Android源码工作环境**：在“锁定工具链”保存入口与状态，注入沿用卷I
  2.2路径或用现代IDE截图代替4.0.1可运行证据后恢复，并以manifest标签、编译目标、framework产物、Eclipse符号、调试端口与断点线程重放。
  - **1.1 Android系统架构**：在“定位源码入口”保存入口与状态，注入沿用卷I
  2.2路径或用现代IDE截图代替4.0.1可运行证据后恢复，并以manifest标签、编译目标、framework产物、Eclipse符号、调试端口与断点线程重放。
  - **1.2 搭建开发环境**：在“运行最小切片”保存入口与状态，注入沿用卷I
  2.2路径或用现代IDE截图代替4.0.1可运行证据后恢复，并以manifest标签、编译目标、framework产物、Eclipse符号、调试端口与断点线程重放。
  - **1.2.1 下载源码**：在“保存构建诊断”保存入口与状态，注入沿用卷I
  2.2路径或用现代IDE截图代替4.0.1可运行证据后恢复，并以manifest标签、编译目标、framework产物、Eclipse符号、调试端口与断点线程重放。
  - **1.2.2 编译源码**：在“回归重放”保存入口与状态，注入沿用卷I
  2.2路径或用现代IDE截图代替4.0.1可运行证据后恢复，并以manifest标签、编译目标、framework产物、Eclipse符号、调试端口与断点线程重放。
  - **1.2.3
  利用Eclipse调试system_process**：在“锁定工具链”保存入口与状态，注入沿用卷I
  2.2路径或用现代IDE截图代替4.0.1可运行证据后恢复，并以manifest标签、编译目标、framework产物、Eclipse符号、调试端口与断点线程重放。
  - **1.3 本章小结**：在“定位源码入口”保存入口与状态，注入沿用卷I
  2.2路径或用现代IDE截图代替4.0.1可运行证据后恢复，并以manifest标签、编译目标、framework产物、Eclipse符号、调试端口与断点线程重放。
</Answer>

**问题 3：故障恢复。** 面对沿用卷I 2.2路径或用现代IDE截图代替4.0.1可运行证据，如何确认修复而不是掩盖症状？

<Answer>
  先保存manifest标签、编译目标、framework产物、Eclipse符号、调试端口与断点线程中的正常基线，再只注入一个错误条件并记录首个分叉。恢复真实标签、状态所有者或消费路径后，以完全相同输入重放；只有在android-4.0.1_r1固定输入下，在Android
  4.0.1上建立源码下载、编译与system_process调试基线的入口身份、状态拥有者、成功结果、错误返回和资源释放均可重复观察重新成立且实验可重置，才算通过。
</Answer>

</Exercises>

## 术语复核与本章回顾

<Glossary>
  <GlossaryItem term="源码标签">
    源码标签在本页用于锁定android-4.0.1_r1的事实坐标与允许输入。
  </GlossaryItem>
  <GlossaryItem term="构建目标">
    构建目标在本页用于标出在Android
    4.0.1上建立源码下载、编译与system_process调试基线由谁创建、修改与释放状态。
  </GlossaryItem>
  <GlossaryItem term="符号入口">
    符号入口在本页用于保存能复现正常路径的对象身份和时序。
  </GlossaryItem>
  <GlossaryItem term="运行产物">
    运行产物在本页用于暴露沿用卷I
    2.2路径或用现代IDE截图代替4.0.1可运行证据发生的第一个边界。
  </GlossaryItem>
  <GlossaryItem term="错误日志">
    错误日志在本页用于声明故障后重新接收请求所需的恢复条件。
  </GlossaryItem>
  <GlossaryItem term="复现脚本">
    复现脚本在本页用于让独立读者凭manifest标签、编译目标、framework产物、Eclipse符号、调试端口与断点线程得到同一结论。
  </GlossaryItem>
</Glossary>

完成卷II 第1章 搭建Android源码工作环境意味着：能解释在Android 4.0.1上建立源码下载、编译与system_process调试基线，能操作全节点图与诊断切片，能制造沿用卷I 2.2路径或用现代IDE截图代替4.0.1可运行证据，还能凭manifest标签、编译目标、framework产物、Eclipse符号、调试端口与断点线程恢复、复位和交接，而不是只在目录中找到关键词。

## 阅读导航

[← 上一页：卷I 第10章 深入理解MediaScanner](/learn/deep-android-volumes/10-v1-10/dav-v1-10-media-scanner) · [下一页：卷II 第2章 深入理解Java Binder和MessageQueue →](/learn/deep-android-volumes/12-v2-02/dav-v2-02-java-binder-messagequeue)

<Attribution
  mode="independent-rewrite"
  sourceBasis="outline-only"
  workTitle="邓凡平《深入理解Android：卷II》（机械工业出版社，2012，Android 4.0.1）"
  adaptedUrl="https://www.sanmin.com.tw/product/index/003084876"
/>
