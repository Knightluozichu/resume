---
title: 卷III 第4章 深入理解WindowManagerService
description: >-
  卷III 第4章
  深入理解WindowManagerService：以窗口管理结构、Z序、布局与动画交替循环解释WMS，以android-4.2.2_r1源码、全节点机制图和故障恢复证据完成验收。
type: C
section: 深入Android三卷 · 卷III UI系统
draft: false
math: false
sourceUrl: 'https://discx.libtop.com/book/7031561197875'
demo: true
order: 2
qualityVersion: 2
practiceMode: simulation
sourceMode: independent-rewrite
---
import { DavSeriesPipelineLab, DavSeriesExperimentLab, DavSeriesEvidenceLab } from "@/components/mdx/deep-android-volumes/diagrams/dav-v3-04-window-manager-service";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能在android-4.2.2_r1单一历史标签内解释以窗口管理结构、Z序、布局与动画交替循环解释WMS，不跨卷拼接文件与接口身份。
- 能操作全节点机制图，让本页 29 个坐标都具备出现、解释、实验和练习四级证据。
- 能使用章专属诊断切片保存入口、PID/TID、对象或状态、正常结果、错误返回与资源释放。
- 能主动制造“把WMS当View布局器，忽略窗口令牌、显示层级、Surface和动画状态”，再凭WindowToken/WindowState/DisplayContent关系、layer、frame、Surface事务、动画时间线与移除状态定位、恢复、复位并以同输入重放。

</Objectives>

{/* DEEP_ANDROID_VOLUMES_QUALITY_V2 */}

## 从一个可失败的问题开始

卷III 第4章 深入理解WindowManagerService不是类名清单，而是在android-4.2.2_r1中回答：以窗口管理结构、Z序、布局与动画交替循环解释WMS。先预测请求经过的线程、进程与对象所有者，再运行正常样本；如果WindowToken/WindowState/DisplayContent关系、layer、frame、Surface事务、动画时间线与移除状态不能指出首个变化，本页结论仍不可交接。

本章不变量是在android-4.2.2_r1固定输入下，以窗口管理结构、Z序、布局与动画交替循环解释WMS的入口身份、状态拥有者、成功结果、错误返回和资源释放均可重复观察。将“把WMS当View布局器，忽略窗口令牌、显示层级、Surface和动画状态”作为反例，必须在同一版本、同一输入下看到确定的错误或状态分叉，并能恢复到基线。

## 来源范围、历史标签与当前迁移

卷III 第4章 深入理解WindowManagerService以[卷III图书馆书目](https://discx.libtop.com/book/7031561197875)和[零售书目](https://www.yami.com/zh/p/android-3/3118023101)交叉核对Android 4.2.2目录；未取得原书正文，不作忠实性宣称。

[AOSP版本与构建号表](https://source.android.com/docs/setup/reference/build-numbers)用于核对2.2/API 8、4.0.1/API 14、4.2.2/API 17及正式源码标签；[android-4.2.2_r1源码树](https://android.googlesource.com/platform/frameworks/base/+/android-4.2.2_r1/)提供本页历史机制的一手坐标。当前[Android架构文档](https://source.android.com/docs/core/architecture)只用于迁移对照，ART、Treble、稳定AIDL、模块化系统服务和现代构建工具不得倒填为三卷历史实现。

## 六个机制检查点

<Term def="初始输入在本页用于锁定android-4.2.2_r1的事实坐标与允许输入。">初始输入</Term>、<Term def="调用入口在本页用于标出以窗口管理结构、Z序、布局与动画交替循环解释WMS由谁创建、修改与释放状态。">调用入口</Term>、<Term def="状态迁移在本页用于保存能复现正常路径的对象身份和时序。">状态迁移</Term>、<Term def="可见反馈在本页用于暴露把WMS当View布局器，忽略窗口令牌、显示层级、Surface和动画状态发生的第一个边界。">可见反馈</Term>、<Term def="故障注入在本页用于声明故障后重新接收请求所需的恢复条件。">故障注入</Term>、<Term def="复位重放在本页用于让独立读者凭WindowToken/WindowState/DisplayContent关系、layer、frame、Surface事务、动画时间线与移除状态得到同一结论。">复位重放</Term>。这六项把以窗口管理结构、Z序、布局与动画交替循环解释WMS落到WindowToken/WindowState/DisplayContent关系、layer、frame、Surface事务、动画时间线与移除状态，防止目录词出现却没有运行解释。

## 先预测，再操作三个专属实验

围绕卷III 第4章 深入理解WindowManagerService，先预测哪一个对象最先改变、“把WMS当View布局器，忽略窗口令牌、显示层级、Surface和动画状态”应在哪个边界暴露，以及恢复后WindowToken/WindowState/DisplayContent关系、layer、frame、Surface事务、动画时间线与移除状态中的哪份证据必须回到原值；然后按全节点、版本因果、故障恢复的顺序操作。

<Stepper>
  <Step title="1. 全节点机制与可执行探针">
    从卷III 第4章 深入理解WindowManagerService的 29 个正式坐标中选择任一节点与处理阶段，比较章专属机制说明和探针；列表完整保留，不截断节点。

    <DavSeriesPipelineLab />

  </Step>
  <Step title="2. 同输入版本与故障边界">
    保持以窗口管理结构、Z序、布局与动画交替循环解释WMS的输入不变，只切换正常、标签错配、所有者死亡或线程停滞，解释可见结果为何变化。

    <DavSeriesExperimentLab />

  </Step>
  <Step title="3. 基线、首错、恢复与复位">
    保存卷III 第4章 深入理解WindowManagerService的首个分叉后恢复android-4.2.2_r1合同，以同输入重放；点击重置，确认状态和输出都回到初始值。

    <DavSeriesEvidenceLab />

  </Step>
</Stepper>

## 正式目录逐项深读

下面保留 29 个正式目录坐标，但不重复套用实验流程。每个条目只回答它在以窗口管理结构、Z序、布局与动画交替循环解释WMS中的独立责任；共同的定位、单变量注错、恢复和重放动作统一由上方实验与下方练习验收。

### 第4章 深入理解WindowManagerService

第4章 深入理解WindowManagerService在“以窗口管理结构、Z序、布局与动画交替循环解释WMS”中的责任横跨窗口身份、缓冲区所有权与合成时序。以同一帧记录生产、提交、层级、消费和移除，改变Z序或所有者后检查旧Surface是否仍可见或占用资源。

### 4.1 初识WindowManagerService

4.1 初识WindowManagerService在“以窗口管理结构、Z序、布局与动画交替循环解释WMS”中的责任是界定分析分母和结论边界。先列出它包含的入口、对象和退出条件，再用WindowToken/WindowState/DisplayContent关系、layer、frame、Surface事务、动画时间线与移除状态排除只凭类名或流程图得出的结论。

### 4.1.1 一个从命令行启动的动画窗口

4.1.1 一个从命令行启动的动画窗口在“以窗口管理结构、Z序、布局与动画交替循环解释WMS”中的责任横跨窗口身份、缓冲区所有权与合成时序。以同一帧记录生产、提交、层级、消费和移除，改变Z序或所有者后检查旧Surface是否仍可见或占用资源。

### 4.1.2 WMS的构成

4.1.2 WMS的构成在“以窗口管理结构、Z序、布局与动画交替循环解释WMS”中的责任横跨窗口身份、缓冲区所有权与合成时序。以同一帧记录生产、提交、层级、消费和移除，改变Z序或所有者后检查旧Surface是否仍可见或占用资源。

### 4.1.3 初识WMS的小结

4.1.3 初识WMS的小结在“以窗口管理结构、Z序、布局与动画交替循环解释WMS”中的责任是界定分析分母和结论边界。先列出它包含的入口、对象和退出条件，再用WindowToken/WindowState/DisplayContent关系、layer、frame、Surface事务、动画时间线与移除状态排除只凭类名或流程图得出的结论。

### 4.2 WMS的窗口管理结构

4.2 WMS的窗口管理结构在“以窗口管理结构、Z序、布局与动画交替循环解释WMS”中的责任横跨窗口身份、缓冲区所有权与合成时序。以同一帧记录生产、提交、层级、消费和移除，改变Z序或所有者后检查旧Surface是否仍可见或占用资源。

### 4.2.1 理解WindowToken

4.2.1 理解WindowToken在“以窗口管理结构、Z序、布局与动画交替循环解释WMS”中的责任横跨窗口身份、缓冲区所有权与合成时序。以同一帧记录生产、提交、层级、消费和移除，改变Z序或所有者后检查旧Surface是否仍可见或占用资源。

### 4.2.2 理解WindowState

4.2.2 理解WindowState在“以窗口管理结构、Z序、布局与动画交替循环解释WMS”中的责任横跨窗口身份、缓冲区所有权与合成时序。以同一帧记录生产、提交、层级、消费和移除，改变Z序或所有者后检查旧Surface是否仍可见或占用资源。

### 4.2.3 理解DisplayContent

4.2.3 理解DisplayContent在“以窗口管理结构、Z序、布局与动画交替循环解释WMS”中的责任横跨窗口身份、缓冲区所有权与合成时序。以同一帧记录生产、提交、层级、消费和移除，改变Z序或所有者后检查旧Surface是否仍可见或占用资源。

### 4.3 理解窗口的显示次序

4.3 理解窗口的显示次序在“以窗口管理结构、Z序、布局与动画交替循环解释WMS”中的责任横跨窗口身份、缓冲区所有权与合成时序。以同一帧记录生产、提交、层级、消费和移除，改变Z序或所有者后检查旧Surface是否仍可见或占用资源。

### 4.3.1 主序、子序和窗口类型

4.3.1 主序、子序和窗口类型在“以窗口管理结构、Z序、布局与动画交替循环解释WMS”中的责任横跨窗口身份、缓冲区所有权与合成时序。以同一帧记录生产、提交、层级、消费和移除，改变Z序或所有者后检查旧Surface是否仍可见或占用资源。

### 4.3.2 通过主序与子序确定窗口的次序

4.3.2 通过主序与子序确定窗口的次序在“以窗口管理结构、Z序、布局与动画交替循环解释WMS”中的责任横跨窗口身份、缓冲区所有权与合成时序。以同一帧记录生产、提交、层级、消费和移除，改变Z序或所有者后检查旧Surface是否仍可见或占用资源。

### 4.3.3 更新显示次序到Surface

4.3.3 更新显示次序到Surface在“以窗口管理结构、Z序、布局与动画交替循环解释WMS”中的责任横跨窗口身份、缓冲区所有权与合成时序。以同一帧记录生产、提交、层级、消费和移除，改变Z序或所有者后检查旧Surface是否仍可见或占用资源。

### 4.3.4 关于显示次序的小结

4.3.4 关于显示次序的小结在“以窗口管理结构、Z序、布局与动画交替循环解释WMS”中的责任是界定分析分母和结论边界。先列出它包含的入口、对象和退出条件，再用WindowToken/WindowState/DisplayContent关系、layer、frame、Surface事务、动画时间线与移除状态排除只凭类名或流程图得出的结论。

### 4.4 窗口的布局

4.4 窗口的布局在“以窗口管理结构、Z序、布局与动画交替循环解释WMS”中的责任横跨窗口身份、缓冲区所有权与合成时序。以同一帧记录生产、提交、层级、消费和移除，改变Z序或所有者后检查旧Surface是否仍可见或占用资源。

### 4.4.1 从relayoutWindow()开始

4.4.1 从relayoutWindow()开始在“以窗口管理结构、Z序、布局与动画交替循环解释WMS”中的责任横跨窗口身份、缓冲区所有权与合成时序。以同一帧记录生产、提交、层级、消费和移除，改变Z序或所有者后检查旧Surface是否仍可见或占用资源。

### 4.4.2 布局操作的外围代码分析

4.4.2 布局操作的外围代码分析在“以窗口管理结构、Z序、布局与动画交替循环解释WMS”中的责任横跨窗口身份、缓冲区所有权与合成时序。以同一帧记录生产、提交、层级、消费和移除，改变Z序或所有者后检查旧Surface是否仍可见或占用资源。

### 4.4.3 初探performLayoutAndPlaceSurfacesLockedInner()

4.4.3 初探performLayoutAndPlaceSurfacesLockedInner()在“以窗口管理结构、Z序、布局与动画交替循环解释WMS”中的责任横跨窗口身份、缓冲区所有权与合成时序。以同一帧记录生产、提交、层级、消费和移除，改变Z序或所有者后检查旧Surface是否仍可见或占用资源。

### 4.4.4 布局的前期处理

4.4.4 布局的前期处理在“以窗口管理结构、Z序、布局与动画交替循环解释WMS”中的责任横跨窗口身份、缓冲区所有权与合成时序。以同一帧记录生产、提交、层级、消费和移除，改变Z序或所有者后检查旧Surface是否仍可见或占用资源。

### 4.4.5 布局DisplayContent

4.4.5 布局DisplayContent在“以窗口管理结构、Z序、布局与动画交替循环解释WMS”中的责任横跨窗口身份、缓冲区所有权与合成时序。以同一帧记录生产、提交、层级、消费和移除，改变Z序或所有者后检查旧Surface是否仍可见或占用资源。

### 4.4.6 布局的最终阶段

4.4.6 布局的最终阶段在“以窗口管理结构、Z序、布局与动画交替循环解释WMS”中的责任横跨窗口身份、缓冲区所有权与合成时序。以同一帧记录生产、提交、层级、消费和移除，改变Z序或所有者后检查旧Surface是否仍可见或占用资源。

### 4.5 WMS的动画系统

4.5 WMS的动画系统在“以窗口管理结构、Z序、布局与动画交替循环解释WMS”中的责任横跨窗口身份、缓冲区所有权与合成时序。以同一帧记录生产、提交、层级、消费和移除，改变Z序或所有者后检查旧Surface是否仍可见或占用资源。

### 4.5.1 Android动画原理简介

4.5.1 Android动画原理简介在“以窗口管理结构、Z序、布局与动画交替循环解释WMS”中的责任横跨窗口身份、缓冲区所有权与合成时序。以同一帧记录生产、提交、层级、消费和移除，改变Z序或所有者后检查旧Surface是否仍可见或占用资源。

### 4.5.2 WMS的动画系统框架

4.5.2 WMS的动画系统框架在“以窗口管理结构、Z序、布局与动画交替循环解释WMS”中的责任横跨窗口身份、缓冲区所有权与合成时序。以同一帧记录生产、提交、层级、消费和移除，改变Z序或所有者后检查旧Surface是否仍可见或占用资源。

### 4.5.3 WindowAnimator分析

4.5.3 WindowAnimator分析在“以窗口管理结构、Z序、布局与动画交替循环解释WMS”中的责任横跨窗口身份、缓冲区所有权与合成时序。以同一帧记录生产、提交、层级、消费和移除，改变Z序或所有者后检查旧Surface是否仍可见或占用资源。

### 4.5.4 深入理解窗口动画

4.5.4 深入理解窗口动画在“以窗口管理结构、Z序、布局与动画交替循环解释WMS”中的责任横跨窗口身份、缓冲区所有权与合成时序。以同一帧记录生产、提交、层级、消费和移除，改变Z序或所有者后检查旧Surface是否仍可见或占用资源。

### 4.5.5 交替运行的布局系统与动画系统

4.5.5 交替运行的布局系统与动画系统在“以窗口管理结构、Z序、布局与动画交替循环解释WMS”中的责任横跨窗口身份、缓冲区所有权与合成时序。以同一帧记录生产、提交、层级、消费和移除，改变Z序或所有者后检查旧Surface是否仍可见或占用资源。

### 4.5.6 动画系统总结

4.5.6 动画系统总结在“以窗口管理结构、Z序、布局与动画交替循环解释WMS”中的责任是界定分析分母和结论边界。先列出它包含的入口、对象和退出条件，再用WindowToken/WindowState/DisplayContent关系、layer、frame、Surface事务、动画时间线与移除状态排除只凭类名或流程图得出的结论。

### 4.6 本章小结

4.6 本章小结在“以窗口管理结构、Z序、布局与动画交替循环解释WMS”中的责任是界定分析分母和结论边界。先列出它包含的入口、对象和退出条件，再用WindowToken/WindowState/DisplayContent关系、layer、frame、Surface事务、动画时间线与移除状态排除只凭类名或流程图得出的结论。

## 章专属复现实验

```bash
mkdir -p aosp-android-4.2.2_r1
cd aosp-android-4.2.2_r1
repo init -u https://android.googlesource.com/platform/manifest -b android-4.2.2_r1
repo sync -c

# 只在可恢复的同版本模拟器或测试机采集
adb shell dumpsys window
adb shell dumpsys SurfaceFlinger
```

复现卷III 第4章 深入理解WindowManagerService前先保存主机工具链、manifest与产品目标；android-4.2.2_r1历史分支通常需要隔离的旧工具链环境。与以窗口管理结构、Z序、布局与动画交替循环解释WMS有关的诊断命令只在可恢复模拟器或测试机执行，账号、媒体与设备标识使用隔离样本；现代设备输出不得冒充android-4.2.2_r1结果。

<Callout type="trap" title="本章核心陷阱">
  把WMS当View布局器，忽略窗口令牌、显示层级、Surface和动画状态。先恢复android-4.2.2_r1的真实入口、对象身份和失败返回，再讨论当前Android迁移。
</Callout>

<Callout type="trap" title="跨卷同名不等于同一实现">
  卷III 第4章 深入理解WindowManagerService只在自己的历史标签中成立；Binder、MessageQueue、SystemServer、Audio或窗口机制跨卷复用名称时，必须分别核对文件路径、线程责任和错误语义。
</Callout>

<Callout type="warning" title="系统实验安全边界">
  卷III 第4章 深入理解WindowManagerService涉及的刷机、Root、系统映像、gdb、getevent与输入注入仅用于可恢复测试环境。先备份，再记录回滚条件；任何真实用户数据都不得进入截图、日志或练习答案。
</Callout>

## 练习、答案与四级证据

<Exercises>

**问题 1：历史边界。** 怎样证明本页没有把当前Android实现倒灌到 android-4.2.2_r1？

<Answer>
  从[官方标签表](https://source.android.com/docs/setup/reference/build-numbers)锁定版本，只引用[该标签源码](https://android.googlesource.com/platform/frameworks/base/+/android-4.2.2_r1/)解释历史文件与接口；当前架构另列迁移记录。若同名对象路径、职责或返回语义改变，就保留差异而不是拼接流程。
</Answer>

**问题 2：逐节点证据。** 本页 29 个正式坐标如何通过出现、解释、实验与练习四级验收？

<Answer>
  - **第4章 深入理解WindowManagerService**：在“固定输入与所有者”保存入口与状态，注入把WMS当View布局器，忽略窗口令牌、显示层级、Surface和动画状态后恢复，并以WindowToken/WindowState/DisplayContent关系、layer、frame、Surface事务、动画时间线与移除状态重放。
  - **4.1 初识WindowManagerService**：在“触发系统请求”保存入口与状态，注入把WMS当View布局器，忽略窗口令牌、显示层级、Surface和动画状态后恢复，并以WindowToken/WindowState/DisplayContent关系、layer、frame、Surface事务、动画时间线与移除状态重放。
  - **4.1.1 一个从命令行启动的动画窗口**：在“推进状态机”保存入口与状态，注入把WMS当View布局器，忽略窗口令牌、显示层级、Surface和动画状态后恢复，并以WindowToken/WindowState/DisplayContent关系、layer、frame、Surface事务、动画时间线与移除状态重放。
  - **4.1.2 WMS的构成**：在“观察反馈与释放”保存入口与状态，注入把WMS当View布局器，忽略窗口令牌、显示层级、Surface和动画状态后恢复，并以WindowToken/WindowState/DisplayContent关系、layer、frame、Surface事务、动画时间线与移除状态重放。
  - **4.1.3 初识WMS的小结**：在“复位后再次执行”保存入口与状态，注入把WMS当View布局器，忽略窗口令牌、显示层级、Surface和动画状态后恢复，并以WindowToken/WindowState/DisplayContent关系、layer、frame、Surface事务、动画时间线与移除状态重放。
  - **4.2 WMS的窗口管理结构**：在“固定输入与所有者”保存入口与状态，注入把WMS当View布局器，忽略窗口令牌、显示层级、Surface和动画状态后恢复，并以WindowToken/WindowState/DisplayContent关系、layer、frame、Surface事务、动画时间线与移除状态重放。
  - **4.2.1 理解WindowToken**：在“触发系统请求”保存入口与状态，注入把WMS当View布局器，忽略窗口令牌、显示层级、Surface和动画状态后恢复，并以WindowToken/WindowState/DisplayContent关系、layer、frame、Surface事务、动画时间线与移除状态重放。
  - **4.2.2 理解WindowState**：在“推进状态机”保存入口与状态，注入把WMS当View布局器，忽略窗口令牌、显示层级、Surface和动画状态后恢复，并以WindowToken/WindowState/DisplayContent关系、layer、frame、Surface事务、动画时间线与移除状态重放。
  - **4.2.3 理解DisplayContent**：在“观察反馈与释放”保存入口与状态，注入把WMS当View布局器，忽略窗口令牌、显示层级、Surface和动画状态后恢复，并以WindowToken/WindowState/DisplayContent关系、layer、frame、Surface事务、动画时间线与移除状态重放。
  - **4.3 理解窗口的显示次序**：在“复位后再次执行”保存入口与状态，注入把WMS当View布局器，忽略窗口令牌、显示层级、Surface和动画状态后恢复，并以WindowToken/WindowState/DisplayContent关系、layer、frame、Surface事务、动画时间线与移除状态重放。
  - **4.3.1 主序、子序和窗口类型**：在“固定输入与所有者”保存入口与状态，注入把WMS当View布局器，忽略窗口令牌、显示层级、Surface和动画状态后恢复，并以WindowToken/WindowState/DisplayContent关系、layer、frame、Surface事务、动画时间线与移除状态重放。
  - **4.3.2 通过主序与子序确定窗口的次序**：在“触发系统请求”保存入口与状态，注入把WMS当View布局器，忽略窗口令牌、显示层级、Surface和动画状态后恢复，并以WindowToken/WindowState/DisplayContent关系、layer、frame、Surface事务、动画时间线与移除状态重放。
  - **4.3.3 更新显示次序到Surface**：在“推进状态机”保存入口与状态，注入把WMS当View布局器，忽略窗口令牌、显示层级、Surface和动画状态后恢复，并以WindowToken/WindowState/DisplayContent关系、layer、frame、Surface事务、动画时间线与移除状态重放。
  - **4.3.4 关于显示次序的小结**：在“观察反馈与释放”保存入口与状态，注入把WMS当View布局器，忽略窗口令牌、显示层级、Surface和动画状态后恢复，并以WindowToken/WindowState/DisplayContent关系、layer、frame、Surface事务、动画时间线与移除状态重放。
  - **4.4 窗口的布局**：在“复位后再次执行”保存入口与状态，注入把WMS当View布局器，忽略窗口令牌、显示层级、Surface和动画状态后恢复，并以WindowToken/WindowState/DisplayContent关系、layer、frame、Surface事务、动画时间线与移除状态重放。
  - **4.4.1 从relayoutWindow()开始**：在“固定输入与所有者”保存入口与状态，注入把WMS当View布局器，忽略窗口令牌、显示层级、Surface和动画状态后恢复，并以WindowToken/WindowState/DisplayContent关系、layer、frame、Surface事务、动画时间线与移除状态重放。
  - **4.4.2 布局操作的外围代码分析**：在“触发系统请求”保存入口与状态，注入把WMS当View布局器，忽略窗口令牌、显示层级、Surface和动画状态后恢复，并以WindowToken/WindowState/DisplayContent关系、layer、frame、Surface事务、动画时间线与移除状态重放。
  - <strong className="break-words [overflow-wrap:anywhere]">4.4.3 初探performLayoutAndPlaceSurfacesLockedInner()</strong>：在“推进状态机”保存入口与状态，注入把WMS当View布局器，忽略窗口令牌、显示层级、Surface和动画状态后恢复，并以WindowToken/WindowState/DisplayContent关系、layer、frame、Surface事务、动画时间线与移除状态重放。
  - **4.4.4 布局的前期处理**：在“观察反馈与释放”保存入口与状态，注入把WMS当View布局器，忽略窗口令牌、显示层级、Surface和动画状态后恢复，并以WindowToken/WindowState/DisplayContent关系、layer、frame、Surface事务、动画时间线与移除状态重放。
  - **4.4.5 布局DisplayContent**：在“复位后再次执行”保存入口与状态，注入把WMS当View布局器，忽略窗口令牌、显示层级、Surface和动画状态后恢复，并以WindowToken/WindowState/DisplayContent关系、layer、frame、Surface事务、动画时间线与移除状态重放。
  - **4.4.6 布局的最终阶段**：在“固定输入与所有者”保存入口与状态，注入把WMS当View布局器，忽略窗口令牌、显示层级、Surface和动画状态后恢复，并以WindowToken/WindowState/DisplayContent关系、layer、frame、Surface事务、动画时间线与移除状态重放。
  - **4.5 WMS的动画系统**：在“触发系统请求”保存入口与状态，注入把WMS当View布局器，忽略窗口令牌、显示层级、Surface和动画状态后恢复，并以WindowToken/WindowState/DisplayContent关系、layer、frame、Surface事务、动画时间线与移除状态重放。
  - **4.5.1 Android动画原理简介**：在“推进状态机”保存入口与状态，注入把WMS当View布局器，忽略窗口令牌、显示层级、Surface和动画状态后恢复，并以WindowToken/WindowState/DisplayContent关系、layer、frame、Surface事务、动画时间线与移除状态重放。
  - **4.5.2 WMS的动画系统框架**：在“观察反馈与释放”保存入口与状态，注入把WMS当View布局器，忽略窗口令牌、显示层级、Surface和动画状态后恢复，并以WindowToken/WindowState/DisplayContent关系、layer、frame、Surface事务、动画时间线与移除状态重放。
  - **4.5.3 WindowAnimator分析**：在“复位后再次执行”保存入口与状态，注入把WMS当View布局器，忽略窗口令牌、显示层级、Surface和动画状态后恢复，并以WindowToken/WindowState/DisplayContent关系、layer、frame、Surface事务、动画时间线与移除状态重放。
  - **4.5.4 深入理解窗口动画**：在“固定输入与所有者”保存入口与状态，注入把WMS当View布局器，忽略窗口令牌、显示层级、Surface和动画状态后恢复，并以WindowToken/WindowState/DisplayContent关系、layer、frame、Surface事务、动画时间线与移除状态重放。
  - **4.5.5 交替运行的布局系统与动画系统**：在“触发系统请求”保存入口与状态，注入把WMS当View布局器，忽略窗口令牌、显示层级、Surface和动画状态后恢复，并以WindowToken/WindowState/DisplayContent关系、layer、frame、Surface事务、动画时间线与移除状态重放。
  - **4.5.6 动画系统总结**：在“推进状态机”保存入口与状态，注入把WMS当View布局器，忽略窗口令牌、显示层级、Surface和动画状态后恢复，并以WindowToken/WindowState/DisplayContent关系、layer、frame、Surface事务、动画时间线与移除状态重放。
  - **4.6 本章小结**：在“观察反馈与释放”保存入口与状态，注入把WMS当View布局器，忽略窗口令牌、显示层级、Surface和动画状态后恢复，并以WindowToken/WindowState/DisplayContent关系、layer、frame、Surface事务、动画时间线与移除状态重放。
</Answer>

**问题 3：故障恢复。** 面对把WMS当View布局器，忽略窗口令牌、显示层级、Surface和动画状态，如何确认修复而不是掩盖症状？

<Answer>
  先保存WindowToken/WindowState/DisplayContent关系、layer、frame、Surface事务、动画时间线与移除状态中的正常基线，再只注入一个错误条件并记录首个分叉。恢复真实标签、状态所有者或消费路径后，以完全相同输入重放；只有在android-4.2.2_r1固定输入下，以窗口管理结构、Z序、布局与动画交替循环解释WMS的入口身份、状态拥有者、成功结果、错误返回和资源释放均可重复观察重新成立且实验可重置，才算通过。
</Answer>

</Exercises>

## 术语复核与本章回顾

- 节点覆盖dav-v3-04-window-…
- 最小切片贯通全链路
- 故障实验对比正常与故障边界
- 凭证据完成交接

<Glossary>
  <GlossaryItem term="初始输入">
    初始输入在本页用于锁定android-4.2.2_r1的事实坐标与允许输入。
  </GlossaryItem>
  <GlossaryItem term="调用入口">
    调用入口在本页用于标出以窗口管理结构、Z序、布局与动画交替循环解释WMS由谁创建、修改与释放状态。
  </GlossaryItem>
  <GlossaryItem term="状态迁移">
    状态迁移在本页用于保存能复现正常路径的对象身份和时序。
  </GlossaryItem>
  <GlossaryItem term="可见反馈">
    可见反馈在本页用于暴露把WMS当View布局器，忽略窗口令牌、显示层级、Surface和动画状态发生的第一个边界。
  </GlossaryItem>
  <GlossaryItem term="故障注入">
    故障注入在本页用于声明故障后重新接收请求所需的恢复条件。
  </GlossaryItem>
  <GlossaryItem term="复位重放">
    复位重放在本页用于让独立读者凭WindowToken/WindowState/DisplayContent关系、layer、frame、Surface事务、动画时间线与移除状态得到同一结论。
  </GlossaryItem>
</Glossary>

完成卷III 第4章 深入理解WindowManagerService意味着：能解释以窗口管理结构、Z序、布局与动画交替循环解释WMS，能操作全节点图与诊断切片，能制造把WMS当View布局器，忽略窗口令牌、显示层级、Surface和动画状态，还能凭WindowToken/WindowState/DisplayContent关系、layer、frame、Surface事务、动画时间线与移除状态恢复、复位和交接，而不是只在目录中找到关键词。

## 阅读导航

[← 上一页：卷III 第3章 深入理解AudioService](/learn/deep-android-volumes/21-v3-03/dav-v3-03-audio-service) · [下一页：卷III 第5章 深入理解Android输入系统 →](/learn/deep-android-volumes/23-v3-05/dav-v3-05-input-system)

<Attribution
  mode="independent-rewrite"
  sourceBasis="outline-only"
  workTitle="张大伟《深入理解Android：卷III》（机械工业出版社，2015，Android 4.2.2）"
  adaptedUrl="https://discx.libtop.com/book/7031561197875"
/>
