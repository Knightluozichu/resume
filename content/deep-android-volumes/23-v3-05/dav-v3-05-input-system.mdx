---
title: 卷III 第5章 深入理解Android输入系统
description: >-
  卷III 第5章
  深入理解Android输入系统：贯通Linux事件、EventHub、InputReader、InputDispatcher、InputChannel到窗口反馈，以android-4.2.2_r1源码、全节点机制图和故障恢复证据完成验收。
type: C
section: 深入Android三卷 · 卷III UI系统
draft: true
math: false
sourceUrl: "https://discx.libtop.com/book/7031561197875"
demo: true
order: 3
qualityVersion: 2
practiceMode: diagnosis
sourceMode: independent-rewrite
---

import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能在android-4.2.2_r1单一历史标签内解释贯通Linux事件、EventHub、InputReader、InputDispatcher、InputChannel到窗口反馈，不跨卷拼接文件与接口身份。
- 能操作全节点机制图，让本页 28 个坐标都具备出现、解释、实验和练习四级证据。
- 能使用章专属诊断切片保存入口、PID/TID、对象或状态、正常结果、错误返回与资源释放。
- 能主动制造“只追踪触摸到View，遗漏设备发现、策略过滤、反馈和超时状态”，再凭event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时定位、恢复、复位并以同输入重放。

</Objectives>

{/* DEEP_ANDROID_VOLUMES_QUALITY_V2 */}

## 从一个可失败的问题开始

卷III 第5章 深入理解Android输入系统不是类名清单，而是在android-4.2.2_r1中回答：贯通Linux事件、EventHub、InputReader、InputDispatcher、InputChannel到窗口反馈。先预测请求经过的线程、进程与对象所有者，再运行正常样本；如果event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时不能指出首个变化，本页结论仍不可交接。

本章不变量是在android-4.2.2_r1固定输入下，贯通Linux事件、EventHub、InputReader、InputDispatcher、InputChannel到窗口反馈的入口身份、状态拥有者、成功结果、错误返回和资源释放均可重复观察。将“只追踪触摸到View，遗漏设备发现、策略过滤、反馈和超时状态”作为反例，必须在同一版本、同一输入下看到确定的错误或状态分叉，并能恢复到基线。

## 来源范围、历史标签与当前迁移

卷III 第5章 深入理解Android输入系统以[卷III图书馆书目](https://discx.libtop.com/book/7031561197875)和[零售书目](https://www.yami.com/zh/p/android-3/3118023101)交叉核对Android 4.2.2目录；未取得原书正文，不作忠实性宣称。

[AOSP版本与构建号表](https://source.android.com/docs/setup/reference/build-numbers)用于核对2.2/API 8、4.0.1/API 14、4.2.2/API 17及正式源码标签；[android-4.2.2_r1源码树](https://android.googlesource.com/platform/frameworks/base/+/android-4.2.2_r1/)提供本页历史机制的一手坐标。当前[Android架构文档](https://source.android.com/docs/core/architecture)只用于迁移对照，ART、Treble、稳定AIDL、模块化系统服务和现代构建工具不得倒填为三卷历史实现。

## 六个机制检查点

<Term def="版本基线在本页用于锁定android-4.2.2_r1的事实坐标与允许输入。">版本基线</Term>、<Term def="正常轨迹在本页用于标出贯通Linux事件、EventHub、InputReader、InputDispatcher、InputChannel到窗口反馈由谁创建、修改与释放状态。">正常轨迹</Term>、<Term def="首错断点在本页用于保存能复现正常路径的对象身份和时序。">首错断点</Term>、<Term def="对象所有权在本页用于暴露只追踪触摸到View，遗漏设备发现、策略过滤、反馈和超时状态发生的第一个边界。">对象所有权</Term>、<Term def="恢复条件在本页用于声明故障后重新接收请求所需的恢复条件。">恢复条件</Term>、<Term def="回归证据在本页用于让独立读者凭event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时得到同一结论。">回归证据</Term>。
这六项把贯通Linux事件、EventHub、InputReader、InputDispatcher、InputChannel到窗口反馈落到event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时，防止目录词出现却没有运行解释。

## 正式目录逐项深读

### 第5章 深入理解Android输入系统

第5章 深入理解Android输入系统在“贯通Linux事件、EventHub、InputReader、InputDispatcher、InputChannel到窗口反馈”中的责任要翻译成一个可推翻的运行合同：明确入口、线程或进程、状态拥有者、正常结果和首个错误返回，再由event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时复核。

处理 第5章 深入理解Android输入系统 时先执行“锁定历史基线”：在 android-4.2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“复现正常轨迹”后仅改变一个条件，观察event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时中哪一项先偏离。

该节点的四级证据是：保留 第5章 深入理解Android输入系统 这一目录坐标；解释它如何参与贯通Linux事件、EventHub、InputReader、InputDispatcher、InputChannel到窗口反馈；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时恢复同输入结果。

### 5.1 初识Android输入系统

5.1 初识Android输入系统在“贯通Linux事件、EventHub、InputReader、InputDispatcher、InputChannel到窗口反馈”中的责任是界定分析分母和结论边界。先列出它包含的入口、对象和退出条件，再用event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时排除只凭类名或流程图得出的结论。

处理 5.1 初识Android输入系统 时先执行“复现正常轨迹”：在 android-4.2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“注入单一故障”后仅改变一个条件，观察event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时中哪一项先偏离。

该节点的四级证据是：保留 5.1 初识Android输入系统 这一目录坐标；解释它如何参与贯通Linux事件、EventHub、InputReader、InputDispatcher、InputChannel到窗口反馈；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时恢复同输入结果。

### 5.1.1 getevent与sendevent工具

5.1.1 getevent与sendevent工具在“贯通Linux事件、EventHub、InputReader、InputDispatcher、InputChannel到窗口反馈”中的责任是把源码标签、主机工具链、产品目标和运行映像绑定成可复现坐标。产物、符号与设备指纹必须都指向android-4.2.2_r1，否则立即停止比较。

处理 5.1.1 getevent与sendevent工具 时先执行“注入单一故障”：在 android-4.2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“定位首个分叉”后仅改变一个条件，观察event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时中哪一项先偏离。

该节点的四级证据是：保留 5.1.1 getevent与sendevent工具 这一目录坐标；解释它如何参与贯通Linux事件、EventHub、InputReader、InputDispatcher、InputChannel到窗口反馈；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时恢复同输入结果。

### 5.1.2 Android输入系统简介

5.1.2 Android输入系统简介在“贯通Linux事件、EventHub、InputReader、InputDispatcher、InputChannel到窗口反馈”中的责任要翻译成一个可推翻的运行合同：明确入口、线程或进程、状态拥有者、正常结果和首个错误返回，再由event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时复核。

处理 5.1.2 Android输入系统简介 时先执行“定位首个分叉”：在 android-4.2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“恢复同输入重放”后仅改变一个条件，观察event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时中哪一项先偏离。

该节点的四级证据是：保留 5.1.2 Android输入系统简介 这一目录坐标；解释它如何参与贯通Linux事件、EventHub、InputReader、InputDispatcher、InputChannel到窗口反馈；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时恢复同输入结果。

### 5.1.3 IMS的构成

5.1.3 IMS的构成在“贯通Linux事件、EventHub、InputReader、InputDispatcher、InputChannel到窗口反馈”中的责任要翻译成一个可推翻的运行合同：明确入口、线程或进程、状态拥有者、正常结果和首个错误返回，再由event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时复核。

处理 5.1.3 IMS的构成 时先执行“恢复同输入重放”：在 android-4.2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“锁定历史基线”后仅改变一个条件，观察event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时中哪一项先偏离。

该节点的四级证据是：保留 5.1.3 IMS的构成 这一目录坐标；解释它如何参与贯通Linux事件、EventHub、InputReader、InputDispatcher、InputChannel到窗口反馈；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时恢复同输入结果。

### 5.2 原始事件的读取与加工

5.2 原始事件的读取与加工在“贯通Linux事件、EventHub、InputReader、InputDispatcher、InputChannel到窗口反馈”中的责任要翻译成一个可推翻的运行合同：明确入口、线程或进程、状态拥有者、正常结果和首个错误返回，再由event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时复核。

处理 5.2 原始事件的读取与加工 时先执行“锁定历史基线”：在 android-4.2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“复现正常轨迹”后仅改变一个条件，观察event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时中哪一项先偏离。

该节点的四级证据是：保留 5.2 原始事件的读取与加工 这一目录坐标；解释它如何参与贯通Linux事件、EventHub、InputReader、InputDispatcher、InputChannel到窗口反馈；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时恢复同输入结果。

### 5.2.1 基础知识：INotify与Epoll

5.2.1 基础知识：INotify与Epoll在“贯通Linux事件、EventHub、InputReader、InputDispatcher、InputChannel到窗口反馈”中的责任由队列头时间、消费线程和唤醒源共同决定。固定消息与时钟后延迟一次消费，比较入队、唤醒、执行和释放四个时间点，不能把容器结构当调度语义。

处理 5.2.1 基础知识：INotify与Epoll 时先执行“复现正常轨迹”：在 android-4.2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“注入单一故障”后仅改变一个条件，观察event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时中哪一项先偏离。

该节点的四级证据是：保留 5.2.1 基础知识：INotify与Epoll 这一目录坐标；解释它如何参与贯通Linux事件、EventHub、InputReader、InputDispatcher、InputChannel到窗口反馈；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时恢复同输入结果。

### 5.2.2 InputReader的总体流程

5.2.2 InputReader的总体流程在“贯通Linux事件、EventHub、InputReader、InputDispatcher、InputChannel到窗口反馈”中的责任从设备事件进入读取、策略、派发与完成反馈。给同一输入事件绑定序号，核对焦点窗口、通道、finish信号和ANR计时，丢失反馈即不能宣称处理完成。

处理 5.2.2 InputReader的总体流程 时先执行“注入单一故障”：在 android-4.2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“定位首个分叉”后仅改变一个条件，观察event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时中哪一项先偏离。

该节点的四级证据是：保留 5.2.2 InputReader的总体流程 这一目录坐标；解释它如何参与贯通Linux事件、EventHub、InputReader、InputDispatcher、InputChannel到窗口反馈；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时恢复同输入结果。

### 5.2.3 深入理解EventHub

5.2.3 深入理解EventHub在“贯通Linux事件、EventHub、InputReader、InputDispatcher、InputChannel到窗口反馈”中的责任从设备事件进入读取、策略、派发与完成反馈。给同一输入事件绑定序号，核对焦点窗口、通道、finish信号和ANR计时，丢失反馈即不能宣称处理完成。

处理 5.2.3 深入理解EventHub 时先执行“定位首个分叉”：在 android-4.2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“恢复同输入重放”后仅改变一个条件，观察event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时中哪一项先偏离。

该节点的四级证据是：保留 5.2.3 深入理解EventHub 这一目录坐标；解释它如何参与贯通Linux事件、EventHub、InputReader、InputDispatcher、InputChannel到窗口反馈；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时恢复同输入结果。

### 5.2.4 深入理解InputReader

5.2.4 深入理解InputReader在“贯通Linux事件、EventHub、InputReader、InputDispatcher、InputChannel到窗口反馈”中的责任从设备事件进入读取、策略、派发与完成反馈。给同一输入事件绑定序号，核对焦点窗口、通道、finish信号和ANR计时，丢失反馈即不能宣称处理完成。

处理 5.2.4 深入理解InputReader 时先执行“恢复同输入重放”：在 android-4.2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“锁定历史基线”后仅改变一个条件，观察event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时中哪一项先偏离。

该节点的四级证据是：保留 5.2.4 深入理解InputReader 这一目录坐标；解释它如何参与贯通Linux事件、EventHub、InputReader、InputDispatcher、InputChannel到窗口反馈；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时恢复同输入结果。

### 5.2.5 原始事件的读取与加工总结

5.2.5 原始事件的读取与加工总结在“贯通Linux事件、EventHub、InputReader、InputDispatcher、InputChannel到窗口反馈”中的责任是界定分析分母和结论边界。先列出它包含的入口、对象和退出条件，再用event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时排除只凭类名或流程图得出的结论。

处理 5.2.5 原始事件的读取与加工总结 时先执行“锁定历史基线”：在 android-4.2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“复现正常轨迹”后仅改变一个条件，观察event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时中哪一项先偏离。

该节点的四级证据是：保留 5.2.5 原始事件的读取与加工总结 这一目录坐标；解释它如何参与贯通Linux事件、EventHub、InputReader、InputDispatcher、InputChannel到窗口反馈；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时恢复同输入结果。

### 5.3 输入事件的派发

5.3 输入事件的派发在“贯通Linux事件、EventHub、InputReader、InputDispatcher、InputChannel到窗口反馈”中的责任要翻译成一个可推翻的运行合同：明确入口、线程或进程、状态拥有者、正常结果和首个错误返回，再由event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时复核。

处理 5.3 输入事件的派发 时先执行“复现正常轨迹”：在 android-4.2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“注入单一故障”后仅改变一个条件，观察event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时中哪一项先偏离。

该节点的四级证据是：保留 5.3 输入事件的派发 这一目录坐标；解释它如何参与贯通Linux事件、EventHub、InputReader、InputDispatcher、InputChannel到窗口反馈；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时恢复同输入结果。

### 5.3.1 通用事件派发流程

5.3.1 通用事件派发流程在“贯通Linux事件、EventHub、InputReader、InputDispatcher、InputChannel到窗口反馈”中的责任要翻译成一个可推翻的运行合同：明确入口、线程或进程、状态拥有者、正常结果和首个错误返回，再由event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时复核。

处理 5.3.1 通用事件派发流程 时先执行“注入单一故障”：在 android-4.2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“定位首个分叉”后仅改变一个条件，观察event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时中哪一项先偏离。

该节点的四级证据是：保留 5.3.1 通用事件派发流程 这一目录坐标；解释它如何参与贯通Linux事件、EventHub、InputReader、InputDispatcher、InputChannel到窗口反馈；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时恢复同输入结果。

### 5.3.2 按键事件的派发

5.3.2 按键事件的派发在“贯通Linux事件、EventHub、InputReader、InputDispatcher、InputChannel到窗口反馈”中的责任从设备事件进入读取、策略、派发与完成反馈。给同一输入事件绑定序号，核对焦点窗口、通道、finish信号和ANR计时，丢失反馈即不能宣称处理完成。

处理 5.3.2 按键事件的派发 时先执行“定位首个分叉”：在 android-4.2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“恢复同输入重放”后仅改变一个条件，观察event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时中哪一项先偏离。

该节点的四级证据是：保留 5.3.2 按键事件的派发 这一目录坐标；解释它如何参与贯通Linux事件、EventHub、InputReader、InputDispatcher、InputChannel到窗口反馈；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时恢复同输入结果。

### 5.3.3 DispatcherPolicy与InputFilter

5.3.3 DispatcherPolicy与InputFilter在“贯通Linux事件、EventHub、InputReader、InputDispatcher、InputChannel到窗口反馈”中的责任从设备事件进入读取、策略、派发与完成反馈。给同一输入事件绑定序号，核对焦点窗口、通道、finish信号和ANR计时，丢失反馈即不能宣称处理完成。

处理 5.3.3 DispatcherPolicy与InputFilter 时先执行“恢复同输入重放”：在 android-4.2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“锁定历史基线”后仅改变一个条件，观察event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时中哪一项先偏离。

该节点的四级证据是：保留 5.3.3 DispatcherPolicy与InputFilter 这一目录坐标；解释它如何参与贯通Linux事件、EventHub、InputReader、InputDispatcher、InputChannel到窗口反馈；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时恢复同输入结果。

### 5.3.4 输入事件的派发总结

5.3.4 输入事件的派发总结在“贯通Linux事件、EventHub、InputReader、InputDispatcher、InputChannel到窗口反馈”中的责任是界定分析分母和结论边界。先列出它包含的入口、对象和退出条件，再用event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时排除只凭类名或流程图得出的结论。

处理 5.3.4 输入事件的派发总结 时先执行“锁定历史基线”：在 android-4.2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“复现正常轨迹”后仅改变一个条件，观察event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时中哪一项先偏离。

该节点的四级证据是：保留 5.3.4 输入事件的派发总结 这一目录坐标；解释它如何参与贯通Linux事件、EventHub、InputReader、InputDispatcher、InputChannel到窗口反馈；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时恢复同输入结果。

### 5.4 输入事件的发送、接收与反馈

5.4 输入事件的发送、接收与反馈在“贯通Linux事件、EventHub、InputReader、InputDispatcher、InputChannel到窗口反馈”中的责任要翻译成一个可推翻的运行合同：明确入口、线程或进程、状态拥有者、正常结果和首个错误返回，再由event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时复核。

处理 5.4 输入事件的发送、接收与反馈 时先执行“复现正常轨迹”：在 android-4.2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“注入单一故障”后仅改变一个条件，观察event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时中哪一项先偏离。

该节点的四级证据是：保留 5.4 输入事件的发送、接收与反馈 这一目录坐标；解释它如何参与贯通Linux事件、EventHub、InputReader、InputDispatcher、InputChannel到窗口反馈；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时恢复同输入结果。

### 5.4.1 深入理解InputChannel

5.4.1 深入理解InputChannel在“贯通Linux事件、EventHub、InputReader、InputDispatcher、InputChannel到窗口反馈”中的责任从设备事件进入读取、策略、派发与完成反馈。给同一输入事件绑定序号，核对焦点窗口、通道、finish信号和ANR计时，丢失反馈即不能宣称处理完成。

处理 5.4.1 深入理解InputChannel 时先执行“注入单一故障”：在 android-4.2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“定位首个分叉”后仅改变一个条件，观察event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时中哪一项先偏离。

该节点的四级证据是：保留 5.4.1 深入理解InputChannel 这一目录坐标；解释它如何参与贯通Linux事件、EventHub、InputReader、InputDispatcher、InputChannel到窗口反馈；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时恢复同输入结果。

### 5.4.2 连接InputDispatcher和窗口

5.4.2 连接InputDispatcher和窗口在“贯通Linux事件、EventHub、InputReader、InputDispatcher、InputChannel到窗口反馈”中的责任横跨窗口身份、缓冲区所有权与合成时序。以同一帧记录生产、提交、层级、消费和移除，改变Z序或所有者后检查旧Surface是否仍可见或占用资源。

处理 5.4.2 连接InputDispatcher和窗口 时先执行“定位首个分叉”：在 android-4.2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“恢复同输入重放”后仅改变一个条件，观察event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时中哪一项先偏离。

该节点的四级证据是：保留 5.4.2 连接InputDispatcher和窗口 这一目录坐标；解释它如何参与贯通Linux事件、EventHub、InputReader、InputDispatcher、InputChannel到窗口反馈；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时恢复同输入结果。

### 5.4.3 事件的发送

5.4.3 事件的发送在“贯通Linux事件、EventHub、InputReader、InputDispatcher、InputChannel到窗口反馈”中的责任要翻译成一个可推翻的运行合同：明确入口、线程或进程、状态拥有者、正常结果和首个错误返回，再由event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时复核。

处理 5.4.3 事件的发送 时先执行“恢复同输入重放”：在 android-4.2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“锁定历史基线”后仅改变一个条件，观察event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时中哪一项先偏离。

该节点的四级证据是：保留 5.4.3 事件的发送 这一目录坐标；解释它如何参与贯通Linux事件、EventHub、InputReader、InputDispatcher、InputChannel到窗口反馈；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时恢复同输入结果。

### 5.4.4 事件的接收

5.4.4 事件的接收在“贯通Linux事件、EventHub、InputReader、InputDispatcher、InputChannel到窗口反馈”中的责任要翻译成一个可推翻的运行合同：明确入口、线程或进程、状态拥有者、正常结果和首个错误返回，再由event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时复核。

处理 5.4.4 事件的接收 时先执行“锁定历史基线”：在 android-4.2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“复现正常轨迹”后仅改变一个条件，观察event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时中哪一项先偏离。

该节点的四级证据是：保留 5.4.4 事件的接收 这一目录坐标；解释它如何参与贯通Linux事件、EventHub、InputReader、InputDispatcher、InputChannel到窗口反馈；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时恢复同输入结果。

### 5.4.5 事件的反馈与发送循环

5.4.5 事件的反馈与发送循环在“贯通Linux事件、EventHub、InputReader、InputDispatcher、InputChannel到窗口反馈”中的责任要翻译成一个可推翻的运行合同：明确入口、线程或进程、状态拥有者、正常结果和首个错误返回，再由event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时复核。

处理 5.4.5 事件的反馈与发送循环 时先执行“复现正常轨迹”：在 android-4.2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“注入单一故障”后仅改变一个条件，观察event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时中哪一项先偏离。

该节点的四级证据是：保留 5.4.5 事件的反馈与发送循环 这一目录坐标；解释它如何参与贯通Linux事件、EventHub、InputReader、InputDispatcher、InputChannel到窗口反馈；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时恢复同输入结果。

### 5.4.6 输入事件的发送、接收与反馈总结

5.4.6 输入事件的发送、接收与反馈总结在“贯通Linux事件、EventHub、InputReader、InputDispatcher、InputChannel到窗口反馈”中的责任是界定分析分母和结论边界。先列出它包含的入口、对象和退出条件，再用event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时排除只凭类名或流程图得出的结论。

处理 5.4.6 输入事件的发送、接收与反馈总结 时先执行“注入单一故障”：在 android-4.2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“定位首个分叉”后仅改变一个条件，观察event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时中哪一项先偏离。

该节点的四级证据是：保留 5.4.6 输入事件的发送、接收与反馈总结 这一目录坐标；解释它如何参与贯通Linux事件、EventHub、InputReader、InputDispatcher、InputChannel到窗口反馈；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时恢复同输入结果。

### 5.5 关于输入系统的其他重要话题

5.5 关于输入系统的其他重要话题在“贯通Linux事件、EventHub、InputReader、InputDispatcher、InputChannel到窗口反馈”中的责任要翻译成一个可推翻的运行合同：明确入口、线程或进程、状态拥有者、正常结果和首个错误返回，再由event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时复核。

处理 5.5 关于输入系统的其他重要话题 时先执行“定位首个分叉”：在 android-4.2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“恢复同输入重放”后仅改变一个条件，观察event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时中哪一项先偏离。

该节点的四级证据是：保留 5.5 关于输入系统的其他重要话题 这一目录坐标；解释它如何参与贯通Linux事件、EventHub、InputReader、InputDispatcher、InputChannel到窗口反馈；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时恢复同输入结果。

### 5.5.1 输入事件ANR的产生

5.5.1 输入事件ANR的产生在“贯通Linux事件、EventHub、InputReader、InputDispatcher、InputChannel到窗口反馈”中的责任要连接system_server记录、调度队列和目标进程回调。保存记录对象与进程身份，制造超时或死亡后检查队列推进、死亡清理和后续请求是否恢复。

处理 5.5.1 输入事件ANR的产生 时先执行“恢复同输入重放”：在 android-4.2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“锁定历史基线”后仅改变一个条件，观察event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时中哪一项先偏离。

该节点的四级证据是：保留 5.5.1 输入事件ANR的产生 这一目录坐标；解释它如何参与贯通Linux事件、EventHub、InputReader、InputDispatcher、InputChannel到窗口反馈；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时恢复同输入结果。

### 5.5.2 焦点窗口的确定

5.5.2 焦点窗口的确定在“贯通Linux事件、EventHub、InputReader、InputDispatcher、InputChannel到窗口反馈”中的责任要区分控制策略与音频数据平面。保存流、设备、缓冲区指针、处理线程和回调结果，注入欠载或路由变化后检查声音之外的状态恢复。

处理 5.5.2 焦点窗口的确定 时先执行“锁定历史基线”：在 android-4.2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“复现正常轨迹”后仅改变一个条件，观察event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时中哪一项先偏离。

该节点的四级证据是：保留 5.5.2 焦点窗口的确定 这一目录坐标；解释它如何参与贯通Linux事件、EventHub、InputReader、InputDispatcher、InputChannel到窗口反馈；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时恢复同输入结果。

### 5.5.3 以软件方式模拟用户操作

5.5.3 以软件方式模拟用户操作在“贯通Linux事件、EventHub、InputReader、InputDispatcher、InputChannel到窗口反馈”中的责任要翻译成一个可推翻的运行合同：明确入口、线程或进程、状态拥有者、正常结果和首个错误返回，再由event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时复核。

处理 5.5.3 以软件方式模拟用户操作 时先执行“复现正常轨迹”：在 android-4.2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“注入单一故障”后仅改变一个条件，观察event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时中哪一项先偏离。

该节点的四级证据是：保留 5.5.3 以软件方式模拟用户操作 这一目录坐标；解释它如何参与贯通Linux事件、EventHub、InputReader、InputDispatcher、InputChannel到窗口反馈；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时恢复同输入结果。

### 5.6 本章小结

5.6 本章小结在“贯通Linux事件、EventHub、InputReader、InputDispatcher、InputChannel到窗口反馈”中的责任是界定分析分母和结论边界。先列出它包含的入口、对象和退出条件，再用event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时排除只凭类名或流程图得出的结论。

处理 5.6 本章小结 时先执行“注入单一故障”：在 android-4.2.2_r1 中定位真实入口，把PID/TID、对象身份和输入写入记录。进入“定位首个分叉”后仅改变一个条件，观察event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时中哪一项先偏离。

该节点的四级证据是：保留 5.6 本章小结 这一目录坐标；解释它如何参与贯通Linux事件、EventHub、InputReader、InputDispatcher、InputChannel到窗口反馈；在全节点机制图中选中它并操作阶段反馈；最后在练习中凭event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时恢复同输入结果。

## 章专属复现实验

```bash
mkdir -p aosp-android-4.2.2_r1
cd aosp-android-4.2.2_r1
repo init -u https://android.googlesource.com/platform/manifest -b android-4.2.2_r1
repo sync -c

# 只在可恢复的同版本模拟器或测试机采集
adb shell getevent
adb shell dumpsys input
```

运行前保存主机工具链、manifest与产品目标；这些历史分支通常需要隔离的旧工具链环境。诊断命令只在可恢复模拟器或测试机执行，账号、媒体与设备标识使用隔离样本；现代设备输出不得冒充android-4.2.2_r1结果。

<Callout type="trap" title="本章核心陷阱">
  只追踪触摸到View，遗漏设备发现、策略过滤、反馈和超时状态。先恢复android-4.2.2_r1的真实入口、对象身份和失败返回，再讨论当前Android迁移。
</Callout>

<Callout type="trap" title="跨卷同名不等于同一实现">
  卷III 第5章
  深入理解Android输入系统只在自己的历史标签中成立；Binder、MessageQueue、SystemServer、Audio或窗口机制跨卷复用名称时，必须分别核对文件路径、线程责任和错误语义。
</Callout>

<Callout type="warning" title="系统实验安全边界">
  刷机、Root、系统映像、gdb、getevent与输入注入仅用于可恢复测试环境。先备份，再记录回滚条件；任何真实用户数据都不得进入截图、日志或练习答案。
</Callout>

## 练习、答案与四级证据

<Exercises>

**问题 1：历史边界。** 怎样证明本页没有把当前Android实现倒灌到 android-4.2.2_r1？

<Answer>
  从[官方标签表](https://source.android.com/docs/setup/reference/build-numbers)锁定版本，只引用[该标签源码](https://android.googlesource.com/platform/frameworks/base/+/android-4.2.2_r1/)解释历史文件与接口；当前架构另列迁移记录。若同名对象路径、职责或返回语义改变，就保留差异而不是拼接流程。
</Answer>

**问题 2：逐节点证据。** 本页 28 个正式坐标如何通过出现、解释、实验与练习四级验收？

<Answer>
  - **第5章
  深入理解Android输入系统**：在“锁定历史基线”保存入口与状态，注入只追踪触摸到View，遗漏设备发现、策略过滤、反馈和超时状态后恢复，并以event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时重放。
  - **5.1
  初识Android输入系统**：在“复现正常轨迹”保存入口与状态，注入只追踪触摸到View，遗漏设备发现、策略过滤、反馈和超时状态后恢复，并以event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时重放。
  - **5.1.1
  getevent与sendevent工具**：在“注入单一故障”保存入口与状态，注入只追踪触摸到View，遗漏设备发现、策略过滤、反馈和超时状态后恢复，并以event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时重放。
  - **5.1.2
  Android输入系统简介**：在“定位首个分叉”保存入口与状态，注入只追踪触摸到View，遗漏设备发现、策略过滤、反馈和超时状态后恢复，并以event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时重放。
  - **5.1.3
  IMS的构成**：在“恢复同输入重放”保存入口与状态，注入只追踪触摸到View，遗漏设备发现、策略过滤、反馈和超时状态后恢复，并以event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时重放。
  - **5.2
  原始事件的读取与加工**：在“锁定历史基线”保存入口与状态，注入只追踪触摸到View，遗漏设备发现、策略过滤、反馈和超时状态后恢复，并以event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时重放。
  - **5.2.1
  基础知识：INotify与Epoll**：在“复现正常轨迹”保存入口与状态，注入只追踪触摸到View，遗漏设备发现、策略过滤、反馈和超时状态后恢复，并以event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时重放。
  - **5.2.2
  InputReader的总体流程**：在“注入单一故障”保存入口与状态，注入只追踪触摸到View，遗漏设备发现、策略过滤、反馈和超时状态后恢复，并以event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时重放。
  - **5.2.3
  深入理解EventHub**：在“定位首个分叉”保存入口与状态，注入只追踪触摸到View，遗漏设备发现、策略过滤、反馈和超时状态后恢复，并以event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时重放。
  - **5.2.4
  深入理解InputReader**：在“恢复同输入重放”保存入口与状态，注入只追踪触摸到View，遗漏设备发现、策略过滤、反馈和超时状态后恢复，并以event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时重放。
  - **5.2.5
  原始事件的读取与加工总结**：在“锁定历史基线”保存入口与状态，注入只追踪触摸到View，遗漏设备发现、策略过滤、反馈和超时状态后恢复，并以event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时重放。
  - **5.3
  输入事件的派发**：在“复现正常轨迹”保存入口与状态，注入只追踪触摸到View，遗漏设备发现、策略过滤、反馈和超时状态后恢复，并以event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时重放。
  - **5.3.1
  通用事件派发流程**：在“注入单一故障”保存入口与状态，注入只追踪触摸到View，遗漏设备发现、策略过滤、反馈和超时状态后恢复，并以event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时重放。
  - **5.3.2
  按键事件的派发**：在“定位首个分叉”保存入口与状态，注入只追踪触摸到View，遗漏设备发现、策略过滤、反馈和超时状态后恢复，并以event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时重放。
  - **5.3.3
  DispatcherPolicy与InputFilter**：在“恢复同输入重放”保存入口与状态，注入只追踪触摸到View，遗漏设备发现、策略过滤、反馈和超时状态后恢复，并以event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时重放。
  - **5.3.4
  输入事件的派发总结**：在“锁定历史基线”保存入口与状态，注入只追踪触摸到View，遗漏设备发现、策略过滤、反馈和超时状态后恢复，并以event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时重放。
  - **5.4
  输入事件的发送、接收与反馈**：在“复现正常轨迹”保存入口与状态，注入只追踪触摸到View，遗漏设备发现、策略过滤、反馈和超时状态后恢复，并以event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时重放。
  - **5.4.1
  深入理解InputChannel**：在“注入单一故障”保存入口与状态，注入只追踪触摸到View，遗漏设备发现、策略过滤、反馈和超时状态后恢复，并以event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时重放。
  - **5.4.2
  连接InputDispatcher和窗口**：在“定位首个分叉”保存入口与状态，注入只追踪触摸到View，遗漏设备发现、策略过滤、反馈和超时状态后恢复，并以event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时重放。
  - **5.4.3
  事件的发送**：在“恢复同输入重放”保存入口与状态，注入只追踪触摸到View，遗漏设备发现、策略过滤、反馈和超时状态后恢复，并以event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时重放。
  - **5.4.4
  事件的接收**：在“锁定历史基线”保存入口与状态，注入只追踪触摸到View，遗漏设备发现、策略过滤、反馈和超时状态后恢复，并以event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时重放。
  - **5.4.5
  事件的反馈与发送循环**：在“复现正常轨迹”保存入口与状态，注入只追踪触摸到View，遗漏设备发现、策略过滤、反馈和超时状态后恢复，并以event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时重放。
  - **5.4.6
  输入事件的发送、接收与反馈总结**：在“注入单一故障”保存入口与状态，注入只追踪触摸到View，遗漏设备发现、策略过滤、反馈和超时状态后恢复，并以event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时重放。
  - **5.5
  关于输入系统的其他重要话题**：在“定位首个分叉”保存入口与状态，注入只追踪触摸到View，遗漏设备发现、策略过滤、反馈和超时状态后恢复，并以event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时重放。
  - **5.5.1
  输入事件ANR的产生**：在“恢复同输入重放”保存入口与状态，注入只追踪触摸到View，遗漏设备发现、策略过滤、反馈和超时状态后恢复，并以event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时重放。
  - **5.5.2
  焦点窗口的确定**：在“锁定历史基线”保存入口与状态，注入只追踪触摸到View，遗漏设备发现、策略过滤、反馈和超时状态后恢复，并以event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时重放。
  - **5.5.3
  以软件方式模拟用户操作**：在“复现正常轨迹”保存入口与状态，注入只追踪触摸到View，遗漏设备发现、策略过滤、反馈和超时状态后恢复，并以event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时重放。
  - **5.6
  本章小结**：在“注入单一故障”保存入口与状态，注入只追踪触摸到View，遗漏设备发现、策略过滤、反馈和超时状态后恢复，并以event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时重放。
</Answer>

**问题 3：故障恢复。** 面对只追踪触摸到View，遗漏设备发现、策略过滤、反馈和超时状态，如何确认修复而不是掩盖症状？

<Answer>
  先保存event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时中的正常基线，再只注入一个错误条件并记录首个分叉。恢复真实标签、状态所有者或消费路径后，以完全相同输入重放；只有在android-4.2.2_r1固定输入下，贯通Linux事件、EventHub、InputReader、InputDispatcher、InputChannel到窗口反馈的入口身份、状态拥有者、成功结果、错误返回和资源释放均可重复观察重新成立且实验可重置，才算通过。
</Answer>

</Exercises>

## 术语复核与本章回顾

<Glossary>
  <GlossaryItem term="版本基线">
    版本基线在本页用于锁定android-4.2.2_r1的事实坐标与允许输入。
  </GlossaryItem>
  <GlossaryItem term="正常轨迹">
    正常轨迹在本页用于标出贯通Linux事件、EventHub、InputReader、InputDispatcher、InputChannel到窗口反馈由谁创建、修改与释放状态。
  </GlossaryItem>
  <GlossaryItem term="首错断点">
    首错断点在本页用于保存能复现正常路径的对象身份和时序。
  </GlossaryItem>
  <GlossaryItem term="对象所有权">
    对象所有权在本页用于暴露只追踪触摸到View，遗漏设备发现、策略过滤、反馈和超时状态发生的第一个边界。
  </GlossaryItem>
  <GlossaryItem term="恢复条件">
    恢复条件在本页用于声明故障后重新接收请求所需的恢复条件。
  </GlossaryItem>
  <GlossaryItem term="回归证据">
    回归证据在本页用于让独立读者凭event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时得到同一结论。
  </GlossaryItem>
</Glossary>

完成卷III 第5章 深入理解Android输入系统意味着：能解释贯通Linux事件、EventHub、InputReader、InputDispatcher、InputChannel到窗口反馈，能操作全节点图与诊断切片，能制造只追踪触摸到View，遗漏设备发现、策略过滤、反馈和超时状态，还能凭event设备、原始/映射事件、焦点窗口、派发队列、socket通道、finish信号、ANR计时恢复、复位和交接，而不是只在目录中找到关键词。

## 阅读导航

[← 上一页：卷III 第4章 深入理解WindowManagerService](/learn/deep-android-volumes/22-v3-04/dav-v3-04-window-manager-service) · [下一页：卷III 第6章 深入理解控件系统 →](/learn/deep-android-volumes/24-v3-06/dav-v3-06-view-system)

<Attribution
  mode="independent-rewrite"
  sourceBasis="outline-only"
  workTitle="张大伟《深入理解Android：卷III》（机械工业出版社，2015，Android 4.2.2）"
  adaptedUrl="https://discx.libtop.com/book/7031561197875"
/>
