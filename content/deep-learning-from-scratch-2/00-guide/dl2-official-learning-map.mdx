---
title: "《深度学习入门2：自制框架》权威学习地图"
type: B
section: "导读"
order: 0
description: "《深度学习入门2：自制框架》权威学习地图覆盖60个权威目录层级，以可执行框架合同、数学推导、交互实验和失败对照重建核心。"
demo: true
math: true
sourceUrl: "https://www.oreilly.co.jp/books/9784873119069/toc.html"
draft: false
---

import {
  Dl2OfficialLearningMapMapLab,
  Dl2OfficialLearningMapExperimentLab,
  Dl2OfficialLearningMapEvidenceLab,
} from "@/components/mdx/deep-learning-from-scratch-2/diagrams/dl2-official-learning-map";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释《深度学习入门2：自制框架》权威学习地图新增的接口、对象关系、数学规则和适用边界
- 能计算四个核心方程中的forward值、shape、局部导数或参数更新
- 能复现三个互动视图，并定位第一条图连接、数值、梯度或模式证据分叉
- 能判断“5个阶段、60个步骤、207个编号小节和3个附录全部可导航，任何便利API都不破坏计算图、shape、梯度与模式合同。”是否成立

</Objectives>

## 为什么先冻结框架合同

<Term def="Define-by-Run是本页第1个核查坐标，用于连接框架接口、数值状态、计算图和失败边界。">
  Define-by-Run
</Term>
、
<Term def="Variable是本页第2个核查坐标，用于连接框架接口、数值状态、计算图和失败边界。">
  Variable
</Term>
、
<Term def="Function是本页第3个核查坐标，用于连接框架接口、数值状态、计算图和失败边界。">
  Function
</Term>
、
<Term def="高阶导数是本页第4个核查坐标，用于连接框架接口、数值状态、计算图和失败边界。">
  高阶导数
</Term>
、
<Term def="Layer是本页第5个核查坐标，用于连接框架接口、数值状态、计算图和失败边界。">
  Layer
</Term>
、
<Term def="DeZero是本页第6个核查坐标，用于连接框架接口、数值状态、计算图和失败边界。">
  DeZero
</Term>
共同构成本页坐标。按官方60步从Variable和Function出发，完成自动微分、自然接口、高阶导数、神经网络以及GPU/CNN/RNN挑战。

自制框架的难点不是写出一个能返回数字的函数，而是让自然Python表达式、动态图、反向传播、内存和模式在同一合同下工作。实验先冻结Python/NumPy版本、输入与参数数组、对象身份、随机种子和上一版测试，再预测新能力应最先改变的中间量。

每次forward保存输出值、shape、dtype、creator和generation；每次backward保存上游梯度、局部导数、输入梯度和参数梯度。若最终损失下降但对象连接、梯度累加或模式切换错误，训练结果不能采信。修复后清空grad、图、优化器与循环状态，从原始输入完整重放。

## 数学骨架

$$
y=f(x)
$$

$$
\bar{x}=\bar{y}\frac{\partial y}{\partial x}
$$

$$
Y=XW+b
$$

$$
\theta_{t+1}=\theta_t-\eta\nabla L
$$

方程进入代码前先声明输入输出shape、轴和对象所有权。解析梯度与中心差分比较相对误差，参数更新后用新forward独立重算；高阶、GPU或时序模式还要记录是否建图、设备和状态边界。

## 核心机制

1. 第1阶段 自动微分包含10步，每一步只引入一项可测试能力，并把上一版本作为回归基线。
2. 第2阶段 用自然的代码表达包含14步，每一步只引入一项可测试能力，并把上一版本作为回归基线。
3. 第3阶段 实现高阶导数包含12步，每一步只引入一项可测试能力，并把上一版本作为回归基线。
4. 第4阶段 创建神经网络包含15步，每一步只引入一项可测试能力，并把上一版本作为回归基线。
5. 第5阶段 DeZero高级挑战包含9步，每一步只引入一项可测试能力，并把上一版本作为回归基线。
6. 路线以可运行框架为连续产物，不是彼此孤立的算法文章。每一步开始前保存上一版测试，结束后增加最小功能和失败用例。
7. 最终DeZero贯通动态图、反向传播、高阶导数、参数层、优化器、数据加载、GPU、卷积和循环网络，所有能力共享同一Variable/Function核心。

## DeZero实现合同

1. **对象合同：** Variable只承载数据、梯度与图连接，Function负责数值变换与局部导数；便利API最终委托核心函数，不能绕过creator设置。
2. **shape合同：** forward与backward逐轴对称。前向广播、求和、转置或reshape后，反向必须恢复每个输入的原shape，并对重复路径累加。
3. **顺序合同：** 动态图按实际运行创建，反向按generation拓扑逆序；同一函数只处理一次，同一变量可从多条路径接收贡献。
4. **模式合同：** no_grad、create_graph、train/test与设备上下文使用成对进入和恢复，异常退出也不能污染全局状态。
5. **资源合同：** 不需要的中间梯度及时释放，weakref打破循环引用；保存加载、CPU/GPU和DataLoader都保留稳定键、dtype与样本身份。
6. **测试合同：** 每个新函数至少有forward例、解析backward、数值梯度、shape边界和失败样本；上一版本测试必须继续通过。

## 权威目录逐项讲解

### 步骤1 作为“箱子”的变量

Variable把ndarray、grad、creator和generation绑定为一个可追踪对象。 数据为空、标量数组和多维数组都要有明确处理，grad初始化与原数据shape一致。 验收时保存输入、输出、creator或状态、局部梯度和失败边界，并核对“5个阶段、60个步骤、207个编号小节和3个附录全部可导航，任何便利API都不破坏计算图、shape、梯度与模式合同。”是否成立。

先预测该单元改变后最早变化的是对象连接、forward值、shape、generation、内存、梯度还是模式，再运行固定小样本。若末端结果合理但首个证据没有出现，应停止并从上一个通过测试的DeZero版本重放。

### 步骤2 创建变量的函数

Variable把ndarray、grad、creator和generation绑定为一个可追踪对象。 数据为空、标量数组和多维数组都要有明确处理，grad初始化与原数据shape一致。 验收时保存输入、输出、creator或状态、局部梯度和失败边界，并核对“5个阶段、60个步骤、207个编号小节和3个附录全部可导航，任何便利API都不破坏计算图、shape、梯度与模式合同。”是否成立。

先预测该单元改变后最早变化的是对象连接、forward值、shape、generation、内存、梯度还是模式，再运行固定小样本。若末端结果合理但首个证据没有出现，应停止并从上一个通过测试的DeZero版本重放。

### 步骤3 函数的连续调用

Function调用先标准化输入，再执行forward，包装输出并设置creator；backward按输入顺序返回局部梯度。 函数连续调用形成动态图，必须用对象身份连接而不是复制数值，否则回溯会失去来源。 验收时保存输入、输出、creator或状态、局部梯度和失败边界，并核对“5个阶段、60个步骤、207个编号小节和3个附录全部可导航，任何便利API都不破坏计算图、shape、梯度与模式合同。”是否成立。

先预测该单元改变后最早变化的是对象连接、forward值、shape、generation、内存、梯度还是模式，再运行固定小样本。若末端结果合理但首个证据没有出现，应停止并从上一个通过测试的DeZero版本重放。

### 步骤4 数值微分

中心差分逐坐标扰动并恢复原值，比较相对误差而非只看绝对误差。 单元测试覆盖forward、backward、shape、dtype、边界输入和异常路径，失败应定位到最小函数。 验收时保存输入、输出、creator或状态、局部梯度和失败边界，并核对“5个阶段、60个步骤、207个编号小节和3个附录全部可导航，任何便利API都不破坏计算图、shape、梯度与模式合同。”是否成立。

先预测该单元改变后最早变化的是对象连接、forward值、shape、generation、内存、梯度还是模式，再运行固定小样本。若末端结果合理但首个证据没有出现，应停止并从上一个通过测试的DeZero版本重放。

### 步骤5 反向传播的理论知识

反向从输出grad=1开始，按creator逆序把上游梯度乘局部导数。 分支图在Variable处累加梯度，批量和广播还要把贡献还原到输入shape。 验收时保存输入、输出、creator或状态、局部梯度和失败边界，并核对“5个阶段、60个步骤、207个编号小节和3个附录全部可导航，任何便利API都不破坏计算图、shape、梯度与模式合同。”是否成立。

先预测该单元改变后最早变化的是对象连接、forward值、shape、generation、内存、梯度还是模式，再运行固定小样本。若末端结果合理但首个证据没有出现，应停止并从上一个通过测试的DeZero版本重放。

### 步骤6 手动进行反向传播

反向从输出grad=1开始，按creator逆序把上游梯度乘局部导数。 分支图在Variable处累加梯度，批量和广播还要把贡献还原到输入shape。 验收时保存输入、输出、creator或状态、局部梯度和失败边界，并核对“5个阶段、60个步骤、207个编号小节和3个附录全部可导航，任何便利API都不破坏计算图、shape、梯度与模式合同。”是否成立。

先预测该单元改变后最早变化的是对象连接、forward值、shape、generation、内存、梯度还是模式，再运行固定小样本。若末端结果合理但首个证据没有出现，应停止并从上一个通过测试的DeZero版本重放。

### 步骤7 反向传播的自动化

反向从输出grad=1开始，按creator逆序把上游梯度乘局部导数。 分支图在Variable处累加梯度，批量和广播还要把贡献还原到输入shape。 验收时保存输入、输出、creator或状态、局部梯度和失败边界，并核对“5个阶段、60个步骤、207个编号小节和3个附录全部可导航，任何便利API都不破坏计算图、shape、梯度与模式合同。”是否成立。

先预测该单元改变后最早变化的是对象连接、forward值、shape、generation、内存、梯度还是模式，再运行固定小样本。若末端结果合理但首个证据没有出现，应停止并从上一个通过测试的DeZero版本重放。

### 步骤8 从递归到循环

循环工作表避免深图递归栈溢出，generation保证拓扑逆序。 候选函数用集合去重，每处理一个函数就把其输入creator加入队列。 验收时保存输入、输出、creator或状态、局部梯度和失败边界，并核对“5个阶段、60个步骤、207个编号小节和3个附录全部可导航，任何便利API都不破坏计算图、shape、梯度与模式合同。”是否成立。

先预测该单元改变后最早变化的是对象连接、forward值、shape、generation、内存、梯度还是模式，再运行固定小样本。若末端结果合理但首个证据没有出现，应停止并从上一个通过测试的DeZero版本重放。

### 步骤9 让函数更易用

Function调用先标准化输入，再执行forward，包装输出并设置creator；backward按输入顺序返回局部梯度。 函数连续调用形成动态图，必须用对象身份连接而不是复制数值，否则回溯会失去来源。 验收时保存输入、输出、creator或状态、局部梯度和失败边界，并核对“5个阶段、60个步骤、207个编号小节和3个附录全部可导航，任何便利API都不破坏计算图、shape、梯度与模式合同。”是否成立。

先预测该单元改变后最早变化的是对象连接、forward值、shape、generation、内存、梯度还是模式，再运行固定小样本。若末端结果合理但首个证据没有出现，应停止并从上一个通过测试的DeZero版本重放。

### 步骤10 测试

中心差分逐坐标扰动并恢复原值，比较相对误差而非只看绝对误差。 单元测试覆盖forward、backward、shape、dtype、边界输入和异常路径，失败应定位到最小函数。 验收时保存输入、输出、creator或状态、局部梯度和失败边界，并核对“5个阶段、60个步骤、207个编号小节和3个附录全部可导航，任何便利API都不破坏计算图、shape、梯度与模式合同。”是否成立。

先预测该单元改变后最早变化的是对象连接、forward值、shape、generation、内存、梯度还是模式，再运行固定小样本。若末端结果合理但首个证据没有出现，应停止并从上一个通过测试的DeZero版本重放。

### 步骤11 可变长参数（正向传播篇）

inputs与outputs都用元组保存，forward参数与backward返回值严格按位置配对。 单输入函数仍走统一序列接口，便利包装只在边界解包，不破坏核心合同。 验收时保存输入、输出、creator或状态、局部梯度和失败边界，并核对“5个阶段、60个步骤、207个编号小节和3个附录全部可导航，任何便利API都不破坏计算图、shape、梯度与模式合同。”是否成立。

先预测该单元改变后最早变化的是对象连接、forward值、shape、generation、内存、梯度还是模式，再运行固定小样本。若末端结果合理但首个证据没有出现，应停止并从上一个通过测试的DeZero版本重放。

### 步骤12 可变长参数（改进篇）

inputs与outputs都用元组保存，forward参数与backward返回值严格按位置配对。 单输入函数仍走统一序列接口，便利包装只在边界解包，不破坏核心合同。 验收时保存输入、输出、creator或状态、局部梯度和失败边界，并核对“5个阶段、60个步骤、207个编号小节和3个附录全部可导航，任何便利API都不破坏计算图、shape、梯度与模式合同。”是否成立。

先预测该单元改变后最早变化的是对象连接、forward值、shape、generation、内存、梯度还是模式，再运行固定小样本。若末端结果合理但首个证据没有出现，应停止并从上一个通过测试的DeZero版本重放。

### 步骤13 可变长参数（反向传播篇）

反向从输出grad=1开始，按creator逆序把上游梯度乘局部导数。 分支图在Variable处累加梯度，批量和广播还要把贡献还原到输入shape。 验收时保存输入、输出、creator或状态、局部梯度和失败边界，并核对“5个阶段、60个步骤、207个编号小节和3个附录全部可导航，任何便利API都不破坏计算图、shape、梯度与模式合同。”是否成立。

先预测该单元改变后最早变化的是对象连接、forward值、shape、generation、内存、梯度还是模式，再运行固定小样本。若末端结果合理但首个证据没有出现，应停止并从上一个通过测试的DeZero版本重放。

### 步骤14 重复使用同一个变量

Variable把ndarray、grad、creator和generation绑定为一个可追踪对象。 数据为空、标量数组和多维数组都要有明确处理，grad初始化与原数据shape一致。 验收时保存输入、输出、creator或状态、局部梯度和失败边界，并核对“5个阶段、60个步骤、207个编号小节和3个附录全部可导航，任何便利API都不破坏计算图、shape、梯度与模式合同。”是否成立。

先预测该单元改变后最早变化的是对象连接、forward值、shape、generation、内存、梯度还是模式，再运行固定小样本。若末端结果合理但首个证据没有出现，应停止并从上一个通过测试的DeZero版本重放。

### 步骤15 复杂的计算图（理论篇）

循环工作表避免深图递归栈溢出，generation保证拓扑逆序。 候选函数用集合去重，每处理一个函数就把其输入creator加入队列。 验收时保存输入、输出、creator或状态、局部梯度和失败边界，并核对“5个阶段、60个步骤、207个编号小节和3个附录全部可导航，任何便利API都不破坏计算图、shape、梯度与模式合同。”是否成立。

先预测该单元改变后最早变化的是对象连接、forward值、shape、generation、内存、梯度还是模式，再运行固定小样本。若末端结果合理但首个证据没有出现，应停止并从上一个通过测试的DeZero版本重放。

### 步骤16 复杂的计算图（实现篇）

循环工作表避免深图递归栈溢出，generation保证拓扑逆序。 候选函数用集合去重，每处理一个函数就把其输入creator加入队列。 验收时保存输入、输出、creator或状态、局部梯度和失败边界，并核对“5个阶段、60个步骤、207个编号小节和3个附录全部可导航，任何便利API都不破坏计算图、shape、梯度与模式合同。”是否成立。

先预测该单元改变后最早变化的是对象连接、forward值、shape、generation、内存、梯度还是模式，再运行固定小样本。若末端结果合理但首个证据没有出现，应停止并从上一个通过测试的DeZero版本重放。

### 步骤17 内存管理和循环引用

循环工作表避免深图递归栈溢出，generation保证拓扑逆序。 候选函数用集合去重，每处理一个函数就把其输入creator加入队列。 验收时保存输入、输出、creator或状态、局部梯度和失败边界，并核对“5个阶段、60个步骤、207个编号小节和3个附录全部可导航，任何便利API都不破坏计算图、shape、梯度与模式合同。”是否成立。

先预测该单元改变后最早变化的是对象连接、forward值、shape、generation、内存、梯度还是模式，再运行固定小样本。若末端结果合理但首个证据没有出现，应停止并从上一个通过测试的DeZero版本重放。

### 步骤18 减少内存使用量的模式

Function强引用inputs以供backward，outputs使用weakref避免Variable与Function形成不可及时释放的环。 用对象存活数和峰值内存验证修复，同时确认反向前所需节点不会过早回收。 验收时保存输入、输出、creator或状态、局部梯度和失败边界，并核对“5个阶段、60个步骤、207个编号小节和3个附录全部可导航，任何便利API都不破坏计算图、shape、梯度与模式合同。”是否成立。

先预测该单元改变后最早变化的是对象连接、forward值、shape、generation、内存、梯度还是模式，再运行固定小样本。若末端结果合理但首个证据没有出现，应停止并从上一个通过测试的DeZero版本重放。

### 步骤19 让变量更易用

Variable把ndarray、grad、creator和generation绑定为一个可追踪对象。 数据为空、标量数组和多维数组都要有明确处理，grad初始化与原数据shape一致。 验收时保存输入、输出、creator或状态、局部梯度和失败边界，并核对“5个阶段、60个步骤、207个编号小节和3个附录全部可导航，任何便利API都不破坏计算图、shape、梯度与模式合同。”是否成立。

先预测该单元改变后最早变化的是对象连接、forward值、shape、generation、内存、梯度还是模式，再运行固定小样本。若末端结果合理但首个证据没有出现，应停止并从上一个通过测试的DeZero版本重放。

### 步骤20 运算符重载（1）

Python运算符最终委托DeZero函数，使自然表达式仍建立动态图。 反向运算符处理左操作数为标量或ndarray的分派优先级，并把常数转成同设备数组。 验收时保存输入、输出、creator或状态、局部梯度和失败边界，并核对“5个阶段、60个步骤、207个编号小节和3个附录全部可导航，任何便利API都不破坏计算图、shape、梯度与模式合同。”是否成立。

先预测该单元改变后最早变化的是对象连接、forward值、shape、generation、内存、梯度还是模式，再运行固定小样本。若末端结果合理但首个证据没有出现，应停止并从上一个通过测试的DeZero版本重放。

### 步骤21 运算符重载（2）

Python运算符最终委托DeZero函数，使自然表达式仍建立动态图。 反向运算符处理左操作数为标量或ndarray的分派优先级，并把常数转成同设备数组。 验收时保存输入、输出、creator或状态、局部梯度和失败边界，并核对“5个阶段、60个步骤、207个编号小节和3个附录全部可导航，任何便利API都不破坏计算图、shape、梯度与模式合同。”是否成立。

先预测该单元改变后最早变化的是对象连接、forward值、shape、generation、内存、梯度还是模式，再运行固定小样本。若末端结果合理但首个证据没有出现，应停止并从上一个通过测试的DeZero版本重放。

### 步骤22 运算符重载（3）

Python运算符最终委托DeZero函数，使自然表达式仍建立动态图。 反向运算符处理左操作数为标量或ndarray的分派优先级，并把常数转成同设备数组。 验收时保存输入、输出、creator或状态、局部梯度和失败边界，并核对“5个阶段、60个步骤、207个编号小节和3个附录全部可导航，任何便利API都不破坏计算图、shape、梯度与模式合同。”是否成立。

先预测该单元改变后最早变化的是对象连接、forward值、shape、generation、内存、梯度还是模式，再运行固定小样本。若末端结果合理但首个证据没有出现，应停止并从上一个通过测试的DeZero版本重放。

### 步骤23 打包

核心类、functions、utils、layers、models与optimizers按依赖方向拆分，顶层只暴露稳定入口。 包内导入避免循环依赖，最小导入测试确认新进程不依赖交互环境残留。 验收时保存输入、输出、creator或状态、局部梯度和失败边界，并核对“5个阶段、60个步骤、207个编号小节和3个附录全部可导航，任何便利API都不破坏计算图、shape、梯度与模式合同。”是否成立。

先预测该单元改变后最早变化的是对象连接、forward值、shape、generation、内存、梯度还是模式，再运行固定小样本。若末端结果合理但首个证据没有出现，应停止并从上一个通过测试的DeZero版本重放。

### 步骤24 复杂函数的求导

Function调用先标准化输入，再执行forward，包装输出并设置creator；backward按输入顺序返回局部梯度。 函数连续调用形成动态图，必须用对象身份连接而不是复制数值，否则回溯会失去来源。 验收时保存输入、输出、creator或状态、局部梯度和失败边界，并核对“5个阶段、60个步骤、207个编号小节和3个附录全部可导航，任何便利API都不破坏计算图、shape、梯度与模式合同。”是否成立。

先预测该单元改变后最早变化的是对象连接、forward值、shape、generation、内存、梯度还是模式，再运行固定小样本。若末端结果合理但首个证据没有出现，应停止并从上一个通过测试的DeZero版本重放。

### 步骤25 计算图的可视化（1）

变量节点显示名称、shape和dtype，函数节点显示类名，边按真实输入输出方向连接。 节点ID使用对象身份防止同值对象合并，DOT转图失败时保留文本便于诊断。 验收时保存输入、输出、creator或状态、局部梯度和失败边界，并核对“5个阶段、60个步骤、207个编号小节和3个附录全部可导航，任何便利API都不破坏计算图、shape、梯度与模式合同。”是否成立。

先预测该单元改变后最早变化的是对象连接、forward值、shape、generation、内存、梯度还是模式，再运行固定小样本。若末端结果合理但首个证据没有出现，应停止并从上一个通过测试的DeZero版本重放。

### 步骤26 计算图的可视化（2）

变量节点显示名称、shape和dtype，函数节点显示类名，边按真实输入输出方向连接。 节点ID使用对象身份防止同值对象合并，DOT转图失败时保留文本便于诊断。 验收时保存输入、输出、creator或状态、局部梯度和失败边界，并核对“5个阶段、60个步骤、207个编号小节和3个附录全部可导航，任何便利API都不破坏计算图、shape、梯度与模式合同。”是否成立。

先预测该单元改变后最早变化的是对象连接、forward值、shape、generation、内存、梯度还是模式，再运行固定小样本。若末端结果合理但首个证据没有出现，应停止并从上一个通过测试的DeZero版本重放。

### 步骤27 泰勒展开的导数

级数实现逐项累加并设置截断阈值，比较近似值、项数与解析函数。 周期函数的高阶导数按固定循环变化，是验证create_graph和多次backward的透明样本。 验收时保存输入、输出、creator或状态、局部梯度和失败边界，并核对“5个阶段、60个步骤、207个编号小节和3个附录全部可导航，任何便利API都不破坏计算图、shape、梯度与模式合同。”是否成立。

先预测该单元改变后最早变化的是对象连接、forward值、shape、generation、内存、梯度还是模式，再运行固定小样本。若末端结果合理但首个证据没有出现，应停止并从上一个通过测试的DeZero版本重放。

### 步骤28 函数优化

Function调用先标准化输入，再执行forward，包装输出并设置creator；backward按输入顺序返回局部梯度。 函数连续调用形成动态图，必须用对象身份连接而不是复制数值，否则回溯会失去来源。 验收时保存输入、输出、creator或状态、局部梯度和失败边界，并核对“5个阶段、60个步骤、207个编号小节和3个附录全部可导航，任何便利API都不破坏计算图、shape、梯度与模式合同。”是否成立。

先预测该单元改变后最早变化的是对象连接、forward值、shape、generation、内存、梯度还是模式，再运行固定小样本。若末端结果合理但首个证据没有出现，应停止并从上一个通过测试的DeZero版本重放。

### 步骤29 使用牛顿法进行优化（手动计算）

牛顿步用二阶导数按局部曲率缩放一阶梯度，在一维凸邻域可快速收敛。 曲率接近0或符号错误时步长不可靠，应设置阻尼、停止条件和梯度下降对照。 验收时保存输入、输出、creator或状态、局部梯度和失败边界，并核对“5个阶段、60个步骤、207个编号小节和3个附录全部可导航，任何便利API都不破坏计算图、shape、梯度与模式合同。”是否成立。

先预测该单元改变后最早变化的是对象连接、forward值、shape、generation、内存、梯度还是模式，再运行固定小样本。若末端结果合理但首个证据没有出现，应停止并从上一个通过测试的DeZero版本重放。

### 步骤30 高阶导数（准备篇）

backward内部调用DeZero函数并在create_graph开启时记录新图，梯度因此仍可求导。 二阶导数和梯度惩罚会显著增加图与内存，普通训练应关闭建图模式。 验收时保存输入、输出、creator或状态、局部梯度和失败边界，并核对“5个阶段、60个步骤、207个编号小节和3个附录全部可导航，任何便利API都不破坏计算图、shape、梯度与模式合同。”是否成立。

先预测该单元改变后最早变化的是对象连接、forward值、shape、generation、内存、梯度还是模式，再运行固定小样本。若末端结果合理但首个证据没有出现，应停止并从上一个通过测试的DeZero版本重放。

### 步骤31 高阶导数（理论篇）

backward内部调用DeZero函数并在create_graph开启时记录新图，梯度因此仍可求导。 二阶导数和梯度惩罚会显著增加图与内存，普通训练应关闭建图模式。 验收时保存输入、输出、creator或状态、局部梯度和失败边界，并核对“5个阶段、60个步骤、207个编号小节和3个附录全部可导航，任何便利API都不破坏计算图、shape、梯度与模式合同。”是否成立。

先预测该单元改变后最早变化的是对象连接、forward值、shape、generation、内存、梯度还是模式，再运行固定小样本。若末端结果合理但首个证据没有出现，应停止并从上一个通过测试的DeZero版本重放。

### 步骤32 高阶导数（实现篇）

backward内部调用DeZero函数并在create_graph开启时记录新图，梯度因此仍可求导。 二阶导数和梯度惩罚会显著增加图与内存，普通训练应关闭建图模式。 验收时保存输入、输出、creator或状态、局部梯度和失败边界，并核对“5个阶段、60个步骤、207个编号小节和3个附录全部可导航，任何便利API都不破坏计算图、shape、梯度与模式合同。”是否成立。

先预测该单元改变后最早变化的是对象连接、forward值、shape、generation、内存、梯度还是模式，再运行固定小样本。若末端结果合理但首个证据没有出现，应停止并从上一个通过测试的DeZero版本重放。

### 步骤33 使用牛顿法进行优化（自动计算）

牛顿步用二阶导数按局部曲率缩放一阶梯度，在一维凸邻域可快速收敛。 曲率接近0或符号错误时步长不可靠，应设置阻尼、停止条件和梯度下降对照。 验收时保存输入、输出、creator或状态、局部梯度和失败边界，并核对“5个阶段、60个步骤、207个编号小节和3个附录全部可导航，任何便利API都不破坏计算图、shape、梯度与模式合同。”是否成立。

先预测该单元改变后最早变化的是对象连接、forward值、shape、generation、内存、梯度还是模式，再运行固定小样本。若末端结果合理但首个证据没有出现，应停止并从上一个通过测试的DeZero版本重放。

### 步骤34 sin函数的高阶导数

Function调用先标准化输入，再执行forward，包装输出并设置creator；backward按输入顺序返回局部梯度。 函数连续调用形成动态图，必须用对象身份连接而不是复制数值，否则回溯会失去来源。 验收时保存输入、输出、creator或状态、局部梯度和失败边界，并核对“5个阶段、60个步骤、207个编号小节和3个附录全部可导航，任何便利API都不破坏计算图、shape、梯度与模式合同。”是否成立。

先预测该单元改变后最早变化的是对象连接、forward值、shape、generation、内存、梯度还是模式，再运行固定小样本。若末端结果合理但首个证据没有出现，应停止并从上一个通过测试的DeZero版本重放。

### 步骤35 高阶导数的计算图

backward内部调用DeZero函数并在create_graph开启时记录新图，梯度因此仍可求导。 二阶导数和梯度惩罚会显著增加图与内存，普通训练应关闭建图模式。 验收时保存输入、输出、creator或状态、局部梯度和失败边界，并核对“5个阶段、60个步骤、207个编号小节和3个附录全部可导航，任何便利API都不破坏计算图、shape、梯度与模式合同。”是否成立。

先预测该单元改变后最早变化的是对象连接、forward值、shape、generation、内存、梯度还是模式，再运行固定小样本。若末端结果合理但首个证据没有出现，应停止并从上一个通过测试的DeZero版本重放。

### 步骤36 DeZero的其他用途

backward内部调用DeZero函数并在create_graph开启时记录新图，梯度因此仍可求导。 二阶导数和梯度惩罚会显著增加图与内存，普通训练应关闭建图模式。 验收时保存输入、输出、creator或状态、局部梯度和失败边界，并核对“5个阶段、60个步骤、207个编号小节和3个附录全部可导航，任何便利API都不破坏计算图、shape、梯度与模式合同。”是否成立。

先预测该单元改变后最早变化的是对象连接、forward值、shape、generation、内存、梯度还是模式，再运行固定小样本。若末端结果合理但首个证据没有出现，应停止并从上一个通过测试的DeZero版本重放。

### 步骤37 处理张量

reshape backward恢复输入shape，transpose backward应用逆轴置换，元素数量和轴语义都要核对。 标量、向量、矩阵和高阶张量走同一Variable接口，但每个算子的shape规则必须显式。 验收时保存输入、输出、creator或状态、局部梯度和失败边界，并核对“5个阶段、60个步骤、207个编号小节和3个附录全部可导航，任何便利API都不破坏计算图、shape、梯度与模式合同。”是否成立。

先预测该单元改变后最早变化的是对象连接、forward值、shape、generation、内存、梯度还是模式，再运行固定小样本。若末端结果合理但首个证据没有出现，应停止并从上一个通过测试的DeZero版本重放。

### 步骤38 改变形状的函数

Function调用先标准化输入，再执行forward，包装输出并设置creator；backward按输入顺序返回局部梯度。 函数连续调用形成动态图，必须用对象身份连接而不是复制数值，否则回溯会失去来源。 验收时保存输入、输出、creator或状态、局部梯度和失败边界，并核对“5个阶段、60个步骤、207个编号小节和3个附录全部可导航，任何便利API都不破坏计算图、shape、梯度与模式合同。”是否成立。

先预测该单元改变后最早变化的是对象连接、forward值、shape、generation、内存、梯度还是模式，再运行固定小样本。若末端结果合理但首个证据没有出现，应停止并从上一个通过测试的DeZero版本重放。

### 步骤39 求和的函数

Function调用先标准化输入，再执行forward，包装输出并设置creator；backward按输入顺序返回局部梯度。 函数连续调用形成动态图，必须用对象身份连接而不是复制数值，否则回溯会失去来源。 验收时保存输入、输出、creator或状态、局部梯度和失败边界，并核对“5个阶段、60个步骤、207个编号小节和3个附录全部可导航，任何便利API都不破坏计算图、shape、梯度与模式合同。”是否成立。

先预测该单元改变后最早变化的是对象连接、forward值、shape、generation、内存、梯度还是模式，再运行固定小样本。若末端结果合理但首个证据没有出现，应停止并从上一个通过测试的DeZero版本重放。

### 步骤40 进行广播的函数

Function调用先标准化输入，再执行forward，包装输出并设置creator；backward按输入顺序返回局部梯度。 函数连续调用形成动态图，必须用对象身份连接而不是复制数值，否则回溯会失去来源。 验收时保存输入、输出、creator或状态、局部梯度和失败边界，并核对“5个阶段、60个步骤、207个编号小节和3个附录全部可导航，任何便利API都不破坏计算图、shape、梯度与模式合同。”是否成立。

先预测该单元改变后最早变化的是对象连接、forward值、shape、generation、内存、梯度还是模式，再运行固定小样本。若末端结果合理但首个证据没有出现，应停止并从上一个通过测试的DeZero版本重放。

### 步骤41 矩阵的乘积

matmul要求左末轴与右倒数第二轴匹配，批次维按规则广播。 二维情形dX=dY W^T、dW=X^T dY，并用中心差分核对两侧shape。 验收时保存输入、输出、creator或状态、局部梯度和失败边界，并核对“5个阶段、60个步骤、207个编号小节和3个附录全部可导航，任何便利API都不破坏计算图、shape、梯度与模式合同。”是否成立。

先预测该单元改变后最早变化的是对象连接、forward值、shape、generation、内存、梯度还是模式，再运行固定小样本。若末端结果合理但首个证据没有出现，应停止并从上一个通过测试的DeZero版本重放。

### 步骤42 线性回归

玩具数据先固定生成公式和噪声，再用线性层与均方误差学习斜率偏置。 损失按批量平均，参数更新后独立重算，拟合曲线与残差分布并列检查。 验收时保存输入、输出、creator或状态、局部梯度和失败边界，并核对“5个阶段、60个步骤、207个编号小节和3个附录全部可导航，任何便利API都不破坏计算图、shape、梯度与模式合同。”是否成立。

先预测该单元改变后最早变化的是对象连接、forward值、shape、generation、内存、梯度还是模式，再运行固定小样本。若末端结果合理但首个证据没有出现，应停止并从上一个通过测试的DeZero版本重放。

### 步骤43 神经网络

linear执行仿射变换，激活引入非线性，多层组合由Model统一调用。 隐藏宽度、输出宽度和任务标签一致，逐层保存shape与激活范围。 验收时保存输入、输出、creator或状态、局部梯度和失败边界，并核对“5个阶段、60个步骤、207个编号小节和3个附录全部可导航，任何便利API都不破坏计算图、shape、梯度与模式合同。”是否成立。

先预测该单元改变后最早变化的是对象连接、forward值、shape、generation、内存、梯度还是模式，再运行固定小样本。若末端结果合理但首个证据没有出现，应停止并从上一个通过测试的DeZero版本重放。

### 步骤44 汇总参数的层

Parameter是可训练Variable标记，Layer通过**setattr**登记参数和子层。 params递归遍历且按对象身份去重，使共享参数只更新和保存一次。 验收时保存输入、输出、creator或状态、局部梯度和失败边界，并核对“5个阶段、60个步骤、207个编号小节和3个附录全部可导航，任何便利API都不破坏计算图、shape、梯度与模式合同。”是否成立。

先预测该单元改变后最早变化的是对象连接、forward值、shape、generation、内存、梯度还是模式，再运行固定小样本。若末端结果合理但首个证据没有出现，应停止并从上一个通过测试的DeZero版本重放。

### 步骤45 汇总层的层

Parameter是可训练Variable标记，Layer通过**setattr**登记参数和子层。 params递归遍历且按对象身份去重，使共享参数只更新和保存一次。 验收时保存输入、输出、creator或状态、局部梯度和失败边界，并核对“5个阶段、60个步骤、207个编号小节和3个附录全部可导航，任何便利API都不破坏计算图、shape、梯度与模式合同。”是否成立。

先预测该单元改变后最早变化的是对象连接、forward值、shape、generation、内存、梯度还是模式，再运行固定小样本。若末端结果合理但首个证据没有出现，应停止并从上一个通过测试的DeZero版本重放。

### 步骤46 通过Optimizer更新参数

Optimizer持有目标Layer，setup后遍历有效梯度，hooks可在update前执行裁剪或正则。 SGD只做参数减学习率乘梯度，复杂优化器的状态按参数身份独立保存。 验收时保存输入、输出、creator或状态、局部梯度和失败边界，并核对“5个阶段、60个步骤、207个编号小节和3个附录全部可导航，任何便利API都不破坏计算图、shape、梯度与模式合同。”是否成立。

先预测该单元改变后最早变化的是对象连接、forward值、shape、generation、内存、梯度还是模式，再运行固定小样本。若末端结果合理但首个证据没有出现，应停止并从上一个通过测试的DeZero版本重放。

### 步骤47 softmax函数和交叉熵误差

Function调用先标准化输入，再执行forward，包装输出并设置creator；backward按输入顺序返回局部梯度。 函数连续调用形成动态图，必须用对象身份连接而不是复制数值，否则回溯会失去来源。 验收时保存输入、输出、creator或状态、局部梯度和失败边界，并核对“5个阶段、60个步骤、207个编号小节和3个附录全部可导航，任何便利API都不破坏计算图、shape、梯度与模式合同。”是否成立。

先预测该单元改变后最早变化的是对象连接、forward值、shape、generation、内存、梯度还是模式，再运行固定小样本。若末端结果合理但首个证据没有出现，应停止并从上一个通过测试的DeZero版本重放。

### 步骤48 多分类

螺旋数据线性不可分，MLP输出每类logit并用交叉熵训练。 决策边界可视化之外还要保存批次损失、类别概率和独立准确率。 验收时保存输入、输出、creator或状态、局部梯度和失败边界，并核对“5个阶段、60个步骤、207个编号小节和3个附录全部可导航，任何便利API都不破坏计算图、shape、梯度与模式合同。”是否成立。

先预测该单元改变后最早变化的是对象连接、forward值、shape、generation、内存、梯度还是模式，再运行固定小样本。若末端结果合理但首个证据没有出现，应停止并从上一个通过测试的DeZero版本重放。

### 步骤49 Dataset类和预处理

Dataset只定义长度与单样本访问，transform分别处理输入和标签；DataLoader负责索引、打乱与批量堆叠。 epoch边界、最后不足批次和随机种子明确，训练与测试Loader的shuffle和模式不同。 验收时保存输入、输出、creator或状态、局部梯度和失败边界，并核对“5个阶段、60个步骤、207个编号小节和3个附录全部可导航，任何便利API都不破坏计算图、shape、梯度与模式合同。”是否成立。

先预测该单元改变后最早变化的是对象连接、forward值、shape、generation、内存、梯度还是模式，再运行固定小样本。若末端结果合理但首个证据没有出现，应停止并从上一个通过测试的DeZero版本重放。

### 步骤50 用于取出小批量数据的DataLoader

Dataset只定义长度与单样本访问，transform分别处理输入和标签；DataLoader负责索引、打乱与批量堆叠。 epoch边界、最后不足批次和随机种子明确，训练与测试Loader的shuffle和模式不同。 验收时保存输入、输出、creator或状态、局部梯度和失败边界，并核对“5个阶段、60个步骤、207个编号小节和3个附录全部可导航，任何便利API都不破坏计算图、shape、梯度与模式合同。”是否成立。

先预测该单元改变后最早变化的是对象连接、forward值、shape、generation、内存、梯度还是模式，再运行固定小样本。若末端结果合理但首个证据没有出现，应停止并从上一个通过测试的DeZero版本重放。

### 步骤51 MNIST的训练

Dataset只定义长度与单样本访问，transform分别处理输入和标签；DataLoader负责索引、打乱与批量堆叠。 epoch边界、最后不足批次和随机种子明确，训练与测试Loader的shuffle和模式不同。 验收时保存输入、输出、creator或状态、局部梯度和失败边界，并核对“5个阶段、60个步骤、207个编号小节和3个附录全部可导航，任何便利API都不破坏计算图、shape、梯度与模式合同。”是否成立。

先预测该单元改变后最早变化的是对象连接、forward值、shape、generation、内存、梯度还是模式，再运行固定小样本。若末端结果合理但首个证据没有出现，应停止并从上一个通过测试的DeZero版本重放。

### 步骤52 支持GPU

get_array_module按数据设备选择后端，as_numpy与as_cupy显式迁移并记录成本。 同一Function的全部输入和参数必须同设备，CPU/GPU结果在容差内对照。 验收时保存输入、输出、creator或状态、局部梯度和失败边界，并核对“5个阶段、60个步骤、207个编号小节和3个附录全部可导航，任何便利API都不破坏计算图、shape、梯度与模式合同。”是否成立。

先预测该单元改变后最早变化的是对象连接、forward值、shape、generation、内存、梯度还是模式，再运行固定小样本。若末端结果合理但首个证据没有出现，应停止并从上一个通过测试的DeZero版本重放。

### 步骤53 模型的保存和加载

递归参数名形成稳定键，保存前转为NumPy，加载时核对缺失、额外键与shape。 往返测试比较原模型和新模型的参数摘要与固定输入预测，不能只确认文件存在。 验收时保存输入、输出、creator或状态、局部梯度和失败边界，并核对“5个阶段、60个步骤、207个编号小节和3个附录全部可导航，任何便利API都不破坏计算图、shape、梯度与模式合同。”是否成立。

先预测该单元改变后最早变化的是对象连接、forward值、shape、generation、内存、梯度还是模式，再运行固定小样本。若末端结果合理但首个证据没有出现，应停止并从上一个通过测试的DeZero版本重放。

### 步骤54 Dropout和测试模式

中心差分逐坐标扰动并恢复原值，比较相对误差而非只看绝对误差。 单元测试覆盖forward、backward、shape、dtype、边界输入和异常路径，失败应定位到最小函数。 验收时保存输入、输出、creator或状态、局部梯度和失败边界，并核对“5个阶段、60个步骤、207个编号小节和3个附录全部可导航，任何便利API都不破坏计算图、shape、梯度与模式合同。”是否成立。

先预测该单元改变后最早变化的是对象连接、forward值、shape、generation、内存、梯度还是模式，再运行固定小样本。若末端结果合理但首个证据没有出现，应停止并从上一个通过测试的DeZero版本重放。

### 步骤55 CNN的机制（1）

卷积输出尺寸由输入、核、padding和stride计算，非整数覆盖视为合同错误。 滤波器在空间位置共享参数，多通道贡献求和，偏置按输出通道广播。 验收时保存输入、输出、creator或状态、局部梯度和失败边界，并核对“5个阶段、60个步骤、207个编号小节和3个附录全部可导航，任何便利API都不破坏计算图、shape、梯度与模式合同。”是否成立。

先预测该单元改变后最早变化的是对象连接、forward值、shape、generation、内存、梯度还是模式，再运行固定小样本。若末端结果合理但首个证据没有出现，应停止并从上一个通过测试的DeZero版本重放。

### 步骤56 CNN的机制（2）

卷积输出尺寸由输入、核、padding和stride计算，非整数覆盖视为合同错误。 滤波器在空间位置共享参数，多通道贡献求和，偏置按输出通道广播。 验收时保存输入、输出、creator或状态、局部梯度和失败边界，并核对“5个阶段、60个步骤、207个编号小节和3个附录全部可导航，任何便利API都不破坏计算图、shape、梯度与模式合同。”是否成立。

先预测该单元改变后最早变化的是对象连接、forward值、shape、generation、内存、梯度还是模式，再运行固定小样本。若末端结果合理但首个证据没有出现，应停止并从上一个通过测试的DeZero版本重放。

### 步骤57 conv2d函数和pooling函数

Function调用先标准化输入，再执行forward，包装输出并设置creator；backward按输入顺序返回局部梯度。 函数连续调用形成动态图，必须用对象身份连接而不是复制数值，否则回溯会失去来源。 验收时保存输入、输出、creator或状态、局部梯度和失败边界，并核对“5个阶段、60个步骤、207个编号小节和3个附录全部可导航，任何便利API都不破坏计算图、shape、梯度与模式合同。”是否成立。

先预测该单元改变后最早变化的是对象连接、forward值、shape、generation、内存、梯度还是模式，再运行固定小样本。若末端结果合理但首个证据没有出现，应停止并从上一个通过测试的DeZero版本重放。

### 步骤58 具有代表性的CNN（VGG16）

卷积输出尺寸由输入、核、padding和stride计算，非整数覆盖视为合同错误。 滤波器在空间位置共享参数，多通道贡献求和，偏置按输出通道广播。 验收时保存输入、输出、creator或状态、局部梯度和失败边界，并核对“5个阶段、60个步骤、207个编号小节和3个附录全部可导航，任何便利API都不破坏计算图、shape、梯度与模式合同。”是否成立。

先预测该单元改变后最早变化的是对象连接、forward值、shape、generation、内存、梯度还是模式，再运行固定小样本。若末端结果合理但首个证据没有出现，应停止并从上一个通过测试的DeZero版本重放。

### 步骤59 使用RNN处理时间序列数据

RNN层缓存上一时刻h，使同一参数跨时间复用；序列边界必须reset_state。 截断反向用unchain_backward切断旧图但保留数值状态，控制内存和梯度跨度。 验收时保存输入、输出、creator或状态、局部梯度和失败边界，并核对“5个阶段、60个步骤、207个编号小节和3个附录全部可导航，任何便利API都不破坏计算图、shape、梯度与模式合同。”是否成立。

先预测该单元改变后最早变化的是对象连接、forward值、shape、generation、内存、梯度还是模式，再运行固定小样本。若末端结果合理但首个证据没有出现，应停止并从上一个通过测试的DeZero版本重放。

### 步骤60 LSTM与数据加载器

递归参数名形成稳定键，保存前转为NumPy，加载时核对缺失、额外键与shape。 往返测试比较原模型和新模型的参数摘要与固定输入预测，不能只确认文件存在。 验收时保存输入、输出、creator或状态、局部梯度和失败边界，并核对“5个阶段、60个步骤、207个编号小节和3个附录全部可导航，任何便利API都不破坏计算图、shape、梯度与模式合同。”是否成立。

先预测该单元改变后最早变化的是对象连接、forward值、shape、generation、内存、梯度还是模式，再运行固定小样本。若末端结果合理但首个证据没有出现，应停止并从上一个通过测试的DeZero版本重放。

## 三步交互实验

先预测单变量变化会最早影响对象图、forward、shape、梯度还是独立测试，再切换基线、边界和失败模式。

<Stepper>
  <Step title="画出对象与计算图">
    标出Variable、Function、creator、参数、数据或循环状态的所有权与连接。
    <Dl2OfficialLearningMapMapLab />
  </Step>
  <Step title="运行单变量实验">
    固定输入、参数和上一版代码，只改变一个接口、shape、模式或设备条件。
    <Dl2OfficialLearningMapExperimentLab />
  </Step>
  <Step title="核对独立证据">
    比较forward、解析/数值梯度、对象存活、更新与重放测试，定位第一处分叉。
    <Dl2OfficialLearningMapEvidenceLab />
  </Step>
</Stepper>

## 常见失败与边界

<Callout type="trap">
  **便利接口绕过核心图：**
  运算结果数值正确，但输出未设置creator，后续backward在中途停止。所有运算符和包装函数必须委托Function调用。
</Callout>

<Callout type="trap">
  **只看训练终点：**
  错误广播、梯度覆盖、旧缓存、错误模式或测试泄漏可能暂时得到较低损失；必须核对最早中间证据。
</Callout>

<Callout type="tip">
  **最小重放顺序：**
  在标量或极小数组上先手算forward与局部导数，再做中心差分、组合图和长训练。首错出现后清空全部派生状态。
</Callout>

## 本章回顾

按官方60步从Variable和Function出发，完成自动微分、自然接口、高阶导数、神经网络以及GPU/CNN/RNN挑战。 验收底线是：5个阶段、60个步骤、207个编号小节和3个附录全部可导航，任何便利API都不破坏计算图、shape、梯度与模式合同。。完成标准是能预测首个中间变化、重放实现、解释失败样本，并证明新能力没有破坏此前测试。

<Glossary>

<GlossaryItem term="Define-by-Run">
  “Define-by-Run”对应“冻结输入与版本”：记录对象身份、shape、前向缓存、局部梯度、模式和数值容差；仅有最终输出不足以证明框架正确。
</GlossaryItem>

<GlossaryItem term="Variable">
  “Variable”对应“执行forward”：记录对象身份、shape、前向缓存、局部梯度、模式和数值容差；仅有最终输出不足以证明框架正确。
</GlossaryItem>

<GlossaryItem term="Function">
  “Function”对应“保存图与shape”：记录对象身份、shape、前向缓存、局部梯度、模式和数值容差；仅有最终输出不足以证明框架正确。
</GlossaryItem>

<GlossaryItem term="高阶导数">
  “高阶导数”对应“执行backward”：记录对象身份、shape、前向缓存、局部梯度、模式和数值容差；仅有最终输出不足以证明框架正确。
</GlossaryItem>

<GlossaryItem term="Layer">
  “Layer”对应“核对数值梯度”：记录对象身份、shape、前向缓存、局部梯度、模式和数值容差；仅有最终输出不足以证明框架正确。
</GlossaryItem>

<GlossaryItem term="DeZero">
  “DeZero”对应“独立重放测试”：记录对象身份、shape、前向缓存、局部梯度、模式和数值容差；仅有最终输出不足以证明框架正确。
</GlossaryItem>

</Glossary>

<Exercises>

1. 如何为《深度学习入门2：自制框架》权威学习地图建立最小可手算的forward/backward测试？

<Answer>
固定一个标量或极小数组、对象身份和上一版实现，按“冻结输入与版本 -> 执行forward -> 保存图与shape -> 执行backward -> 核对数值梯度 -> 独立重放测试”保存中间证据。手算四个方程，并用中心差分比较解析梯度；shape、creator、generation和模式都应符合合同。
</Answer>

2. 如何证明新便利接口没有绕过DeZero计算图或改变梯度？

<Answer>
  让核心函数与便利写法共享同一输入，逐项比较输出、creator链、generation、解析梯度和数值梯度。两条路径必须在容差内一致，并继续通过所有旧测试。
</Answer>

3. 为“5个阶段、60个步骤、207个编号小节和3个附录全部可导航，任何便利API都不破坏计算图、shape、梯度与模式合同。”构造失败样本并指出停止位置。

<Answer>
  可以故意断开creator、覆盖梯度、交换shape轴、复用旧图、忘记恢复模式或混用CPU/GPU。运行前预测第一条被破坏的证据，在该处停止，清空grad、缓存、优化器和状态后重放。
</Answer>

</Exercises>

<Attribution
  adaptedFrom="斋藤康毅著、郑明智译《深度学习入门2：自制框架》，人民邮电出版社，2023年3月，ISBN 9787115607515；原版《ゼロから作るDeep Learning ❸》，O'Reilly Japan，2020年4月20日，552页，ISBN 9784873119069 · 基于官方5阶段、60步骤、207个编号小节与3个附录完整目录的独立教学重构"
  adaptedUrl="https://www.oreilly.co.jp/books/9784873119069/toc.html"
/>
