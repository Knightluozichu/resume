---
title: 步骤8 从递归到循环
type: B
section: 第1阶段 · 自动微分
order: 8
description: >-
  建立Variable/Function、计算图、链式法则、自动反向与梯度测试，并逐项覆盖步骤8 从递归到循环、8.1 现在的Variable类、8.2
  使用循环实现、8.3 代码验证；用计算图合同、单故障反向轨迹和框架增量验收门完成独立复核。
demo: true
math: false
sourceUrl: "https://www.oreilly.co.jp/books/9784873119069/"
qualityVersion: 2
practiceMode: simulation
sourceMode: independent-rewrite
draft: false
officialUnitId: dl2-step-08
---

import {
  Objectives,
  Term,
  Callout,
  Stepper,
  Step,
  Exercises,
  Answer,
  Glossary,
  GlossaryItem,
  Attribution,
} from "@/components/mdx/mdx-components";
import {
  Dl2Step08RecursionToLoopGraphContractLab,
  Dl2Step08RecursionToLoopBackwardTraceLab,
  Dl2Step08RecursionToLoopFrameworkGateLab,
} from "@/components/mdx/deep-learning-from-scratch-2/v2/dl2-step-08-recursion-to-loop";

<Objectives>

- 能说明“步骤8 从递归到循环”如何建立Variable/Function、计算图、链式法则、自动反向与梯度测试，并逐项覆盖步骤8 从递归到循环、8.1 现在的Variable类、8.2 使用循环实现、8.3 代码验证，并区分2020年原版、2023年中文译本与当前扩展
- 能先预测“怎样让对象身份、creator边和反向传播顺序形成可自动验证的最小计算图？ 当前步骤新增的最小能力和失败边界分别是什么？”“步骤8 从递归到循环”会改变哪个对象、图边、梯度、资源或模式状态，再逐步复核
- 能注入“creator边或局部导数错误，但只凭一个标量结果看似正确；在步骤8 从递归到循环验收中只注入这一处”，用“Variable.data/grad/creator、Function输入输出、局部导数、拓扑顺序和梯度检查一致；步骤8 从递归到循环的新增能力不得破坏此前步骤”决定接受、回退或拒绝框架增量

</Objectives>

## 为什么从这个框架问题开始

“步骤8 从递归到循环”必须交付一个可单独回退的框架增量，不能把后续完整DeZero倒灌进当前步骤。 “步骤8 从递归到循环”的贯穿任务是：从上一步DeZero快照出发，只实现“步骤8 从递归到循环”，再用参考图与单故障图做差分回归。 动手前先写下哪个对象、图边、shape、梯度、资源或模式状态会变化；运行后补理由不算预测。

围绕“怎样让对象身份、creator边和反向传播顺序形成可自动验证的最小计算图？ 当前步骤新增的最小能力和失败边界分别是什么？”“步骤8 从递归到循环”建立参考、故障与恢复路径。只有它守住“Variable.data/grad/creator、Function输入输出、局部导数、拓扑顺序和梯度检查一致；步骤8 从递归到循环的新增能力不得破坏此前步骤”并交付dl2-step-08代码补丁、对象/边快照、输入输出shape、前向缓存、梯度轨迹、资源状态、回归测试与失败复现，代码、对象图或训练结果才构成自制框架证据。

## 书目、275个原版层级与版本边界

“步骤8 从递归到循环”以[O’Reilly Japan原版官方书页](https://www.oreilly.co.jp/books/9784873119069/)核对斋藤康毅著《ゼロから作るDeep Learning ❸ ―フレームワーク編》于2020年4月20日出版、552页、ISBN 9784873119069。对“步骤8 从递归到循环”而言，出版社同页给出的5个阶段、60个步骤与编号小节构成完整目录边界；本站逐项统计5个阶段标题、60个步骤、207个编号小节和附录A至C，共275个正式目录层级。“步骤8 从递归到循环”再以[出版社官方代码仓库](https://github.com/oreilly-japan/deep-learning-from-scratch-3)核对逐步实现，以[作者公开样章](https://koki0702.github.io/dezero-book/)核对教学顺序，以[官方勘误](https://github.com/oreilly-japan/deep-learning-from-scratch-3/wiki/Errata)识别已知错误。

“步骤8 从递归到循环”没有使用未获授权的原书完整正文，只以出版社完整目录、官方代码、作者公开样章和勘误限定范围；中文解释、代码实验、交互、练习与答案均为独立教学重写。“步骤8 从递归到循环”的中文译本书目信息仅用于核对郑明智译《深度学习入门2：自制框架》、人民邮电出版社2023年3月、ISBN 9787115607515，不用来证明日文原版正文。“步骤8 从递归到循环”以2020年日文原版与2023年中文译本目录为内容边界；当前Python/NumPy/CuPy兼容修正和后续框架只作带时间标签的独立核验。

“步骤8 从递归到循环”另以[技术核对 1](https://www.oreilly.co.jp/books/9784873119069/)、[技术核对 2](https://github.com/oreilly-japan/deep-learning-from-scratch-3)、[技术核对 3](https://docs.scipy.org/doc/scipy/reference/generated/scipy.optimize.check_grad.html)核对算法定义、实现语义或数值诊断；这些外部资料能验证本页技术事实，不能反向证明原书采用了本站表述。

## 原版目录层级与实现机制

### 步骤8 从递归到循环

<Term def="从递归到循环对应原版目录坐标“步骤8 从递归到循环”，在“步骤8 从递归到循环”中用于声明本步相对上一快照新增的框架能力与依赖，并受对象身份、图边、dtype/shape、梯度、资源生命周期与版本边界约束。">
  从递归到循环
</Term>

**原版坐标 1/4。** 在“步骤8 从递归到循环”的坐标1中，目录项「步骤8 从递归到循环」用于声明本步相对上一快照新增的框架能力与依赖；先冻结代码快照、对象图与shape，再以前置API、代码差分、对象/边快照、回归用例和回退点复核，出现把后续完整实现倒灌到当前步骤时撤回结论。

### 8.1 现在的Variable类

<Term def="现在的Variable类对应原版目录坐标“8·1 现在的Variable类”，在“步骤8 从递归到循环”中用于用Variable与Function对象记录前向边并按链式法则传播梯度，并受对象身份、图边、dtype/shape、梯度、资源生命周期与版本边界约束。">
  现在的Variable类
</Term>

**原版坐标 2/4。** 目录项「8·1 现在的Variable类」进入“步骤8 从递归到循环”后要回答第2个框架问题：它怎样用Variable与Function对象记录前向边并按链式法则传播梯度、改变哪个对象、图边、梯度或资源状态、由哪些对象身份、data/grad/creator、inputs/outputs、generation、缓存和局部导数证明，并如何排除边丢失、对象覆盖或错误拓扑顺序。

### 8.2 使用循环实现

<Term def="使用循环实现对应原版目录坐标“8·2 使用循环实现”，在“步骤8 从递归到循环”中用于把目录坐标转为有对象、图边、形状、梯度、资源和回归的框架增量合同，并受对象身份、图边、dtype/shape、梯度、资源生命周期与版本边界约束。">
  使用循环实现
</Term>

**原版坐标 3/4。** 围绕“怎样让对象身份、creator边和反向传播顺序形成可自动验证的最小计算图？ 当前步骤新增的最小能力和失败边界分别是什么？”“步骤8 从递归到循环”在原版层级3把「8·2 使用循环实现」落实为把目录坐标转为有对象、图边、形状、梯度、资源和回归的框架增量合同；复核者先读取代码快照、输入输出对象、图边、张量shape、缓存、梯度、资源状态、反例和时间边界，不能接受只复述代码或模型名称。

### 8.3 代码验证

<Term def="代码验证对应原版目录坐标“8·3 代码验证”，在“步骤8 从递归到循环”中用于把目录坐标转为有对象、图边、形状、梯度、资源和回归的框架增量合同，并受对象身份、图边、dtype/shape、梯度、资源生命周期与版本边界约束。">
  代码验证
</Term>

**原版坐标 4/4。** 对“步骤8 从递归到循环”而言，目录项「8·3 代码验证」的最小框架增量合同是把目录坐标转为有对象、图边、形状、梯度、资源和回归的框架增量合同，第4次检查保存代码快照、输入输出对象、图边、张量shape、缓存、梯度、资源状态、反例和时间边界；若产生只复述代码或模型名称，就返回上一步快照。

## 先预测，再操作三个步骤专属框架实验

<Callout type="info" title="先写出哪个对象、图边或梯度状态会先变化">
  对“步骤8
  从递归到循环”先冻结代码快照、对象身份、图边、dtype/shape、顺序、容差和种子，再操作计算图合同、反向轨迹和框架验收门；结果与预测不一致时修改假设，不删除失败运行。
</Callout>

<Stepper>
  <Step title="1. 对象、图边与前向合同">
    固定“从上一步DeZero快照出发，只实现“步骤8 从递归到循环”，再用参考图与单故障图做差分回归。”，在参考与反例间切换，逐阶段查看“对象合同、前向连边、局部导数、反向调度、梯度测试”的对象输入、连边动作、输出和身份/shape检查。

    <Dl2Step08RecursionToLoopGraphContractLab />

  </Step>
  <Step title="2. 参考与单故障反向轨迹">
    保持代码快照、对象图、输入shape和初值不变，只注入“creator边或局部导数错误，但只凭一个标量结果看似正确；在步骤8 从递归到循环验收中只注入这一处”，定位第一处偏离“Variable.data/grad/creator、Function输入输出、局部导数、拓扑顺序和梯度检查一致；步骤8 从递归到循环的新增能力不得破坏此前步骤”的对象、边、梯度或资源状态。

    <Dl2Step08RecursionToLoopBackwardTraceLab />

  </Step>
  <Step title="3. 框架增量证据验收门">
    分别锁定前置API、对象图、反向梯度、资源/模式状态与历史回归，展开dl2-step-08代码补丁、对象/边快照、输入输出shape、前向缓存、梯度轨迹、资源状态、回归测试与失败复现后决定是否接受本步增量。

    <Dl2Step08RecursionToLoopFrameworkGateLab />

  </Step>
</Stepper>

<Callout
  type="trap"
  title="本页单一故障：creator边或局部导数错误，但只凭一个标量结果看似正确；在步骤8 从递归到循环验收中只注入这一处"
>
  “步骤8
  从递归到循环”遇到该故障时保持其余代码快照、对象身份、图边、dtype/shape、顺序、容差和种子不变，沿五阶段寻找最早偏离；最终示例看似可运行不能证明“Variable.data/grad/creator、Function输入输出、局部导数、拓扑顺序和梯度检查一致；步骤8
  从递归到循环的新增能力不得破坏此前步骤”。
</Callout>

<Callout type="trap" title="能运行不等于计算图与梯度语义正确">
  从上一步DeZero快照出发，只实现“步骤8
  从递归到循环”，再用参考图与单故障图做差分回归。
  的代码只说明当前样例没有报错；“步骤8
  从递归到循环”仍需对象身份、图边、generation顺序、dtype/shape、前向缓存、梯度对照、资源状态和边界反例。
</Callout>

<Callout type="trap" title="当前兼容层不能冒充2020年原版">
  “步骤8
  从递归到循环”引用现行文档是为了核对计算语义；当前Python、NumPy、CuPy兼容修正和后续框架能力必须单列时间标签，不能倒填2020年日文原版或2023年中文译本。
</Callout>

## 步骤8 从递归到循环的可重放实现协议

| 阶段     | 允许动作                                                                         | 必留证据                                                     | 拒绝条件                                                                                    |
| -------- | -------------------------------------------------------------------------------- | ------------------------------------------------------------ | ------------------------------------------------------------------------------------------- |
| 对象合同 | 在“步骤8 从递归到循环”执行对象合同，只改变声明的对象、图边、算子、资源或模式状态 | 代码快照、前置API、对象身份、dtype/shape与版本               | 前置快照或对象图不可追溯                                                                    |
| 前向连边 | 在“步骤8 从递归到循环”执行前向连边，只改变声明的对象、图边、算子、资源或模式状态 | 输入输出对象、creator/generation、缓存、梯度、参数与资源状态 | creator边或局部导数错误，但只凭一个标量结果看似正确；在步骤8 从递归到循环验收中只注入这一处 |
| 局部导数 | 在“步骤8 从递归到循环”执行局部导数，只改变声明的对象、图边、算子、资源或模式状态 | 输入输出对象、creator/generation、缓存、梯度、参数与资源状态 | creator边或局部导数错误，但只凭一个标量结果看似正确；在步骤8 从递归到循环验收中只注入这一处 |
| 反向调度 | 在“步骤8 从递归到循环”执行反向调度，只改变声明的对象、图边、算子、资源或模式状态 | 输入输出对象、creator/generation、缓存、梯度、参数与资源状态 | creator边或局部导数错误，但只凭一个标量结果看似正确；在步骤8 从递归到循环验收中只注入这一处 |
| 梯度测试 | 在“步骤8 从递归到循环”执行梯度测试，只改变声明的对象、图边、算子、资源或模式状态 | 梯度对照、资源释放、跨设备/模式回归、反例与复现              | 无法重放或缺少历史回归                                                                      |

```yaml
unit: "dl2-step-08"
question: "怎样让对象身份、creator边和反向传播顺序形成可自动验证的最小计算图？ 当前步骤新增的最小能力和失败边界分别是什么？"
scenario: "从上一步DeZero快照出发，只实现“步骤8 从递归到循环”，再用参考图与单故障图做差分回归。"
stages: ["对象合同", "前向连边", "局部导数", "反向调度", "梯度测试"]
invariant: "Variable.data/grad/creator、Function输入输出、局部导数、拓扑顺序和梯度检查一致；步骤8 从递归到循环的新增能力不得破坏此前步骤"
fault: "creator边或局部导数错误，但只凭一个标量结果看似正确；在步骤8 从递归到循环验收中只注入这一处"
evidence: "dl2-step-08代码补丁、对象/边快照、输入输出shape、前向缓存、梯度轨迹、资源状态、回归测试与失败复现"
reset: restore_case_stage_trace_mode_step_gates_and_artifact
```

该协议要求“步骤8 从递归到循环”在相同代码快照、对象图、dtype/shape、初值、顺序、容差和种子下重放。重置后若案例、阶段、轨迹模式、步骤、框架门或证据包没有回到基线，交互状态已经污染比较。

## 本页回顾

掌握“步骤8 从递归到循环”不是背诵最终代码或API，而是能围绕“怎样让对象身份、creator边和反向传播顺序形成可自动验证的最小计算图？ 当前步骤新增的最小能力和失败边界分别是什么？”重建前置快照、对象、图边、前向、梯度、资源与回归证据，并用“Variable.data/grad/creator、Function输入输出、局部导数、拓扑顺序和梯度检查一致；步骤8 从递归到循环的新增能力不得破坏此前步骤”拒绝“creator边或局部导数错误，但只凭一个标量结果看似正确；在步骤8 从递归到循环验收中只注入这一处”。最终交付为dl2-step-08代码补丁、对象/边快照、输入输出shape、前向缓存、梯度轨迹、资源状态、回归测试与失败复现

## 练习与答案

<Exercises>

1. **问题 1：框架增量合同。** “步骤8 从递归到循环”为什么必须先冻结代码快照、对象图、dtype/shape、初值、顺序、容差和种子？

<Answer>
  从上一步DeZero快照出发，只实现“步骤8
  从递归到循环”，再用参考图与单故障图做差分回归。
  若这些条件变化，相同输出可能来自不同对象身份、图边、缓存、梯度、资源或随机轨迹；“步骤8
  从递归到循环”先冻结合同，才能把观测连接到当前步骤新增的单一框架机制。
</Answer>

2. **问题 2：目录逐项覆盖。** 怎样证明原版目录坐标已经进入机制、交互和练习？

<Answer>
  1. “步骤8 从递归到循环”的目录项「步骤8
  从递归到循环」：以“声明本步相对上一快照新增的框架能力与依赖”解释实现作用，用“前置API、代码差分、对象/边快照、回归用例和回退点”提供复核。
  2. “步骤8 从递归到循环”的目录项「8·1
  现在的Variable类」：以“用Variable与Function对象记录前向边并按链式法则传播梯度”解释实现作用，用“对象身份、data/grad/creator、inputs/outputs、generation、缓存和局部导数”提供复核。
  3. “步骤8 从递归到循环”的目录项「8·2
  使用循环实现」：以“把目录坐标转为有对象、图边、形状、梯度、资源和回归的框架增量合同”解释实现作用，用“代码快照、输入输出对象、图边、张量shape、缓存、梯度、资源状态、反例和时间边界”提供复核。
  4. “步骤8 从递归到循环”的目录项「8·3
  代码验证」：以“把目录坐标转为有对象、图边、形状、梯度、资源和回归的框架增量合同”解释实现作用，用“代码快照、输入输出对象、图边、张量shape、缓存、梯度、资源状态、反例和时间边界”提供复核。
</Answer>

3. **问题 3：故障恢复。** 怎样证明“creator边或局部导数错误，但只凭一个标量结果看似正确；在步骤8 从递归到循环验收中只注入这一处”已经被修正？

<Answer>
  为“步骤8
  从递归到循环”复用同一代码快照、对象图、dtype/shape、初值、顺序、容差和种子，重放参考路径后只注入“creator边或局部导数错误，但只凭一个标量结果看似正确；在步骤8
  从递归到循环验收中只注入这一处”；记录最早偏离，撤销故障再运行。只有计算图合同、反向轨迹、框架门和dl2-step-08代码补丁、对象/边快照、输入输出shape、前向缓存、梯度轨迹、资源状态、回归测试与失败复现重新满足“Variable.data/grad/creator、Function输入输出、局部导数、拓扑顺序和梯度检查一致；步骤8
  从递归到循环的新增能力不得破坏此前步骤”，修正才可提交。
</Answer>

</Exercises>

## 名词解释

<Glossary>
  <GlossaryItem term="从递归到循环">
    检索键 dl2-A 对应目录坐标「步骤8 从递归到循环」；在“步骤8
    从递归到循环”中用于声明本步相对上一快照新增的框架能力与依赖，需要连接原版范围、对象/图边、shape、梯度轨迹、资源状态与回归证据。
  </GlossaryItem>
  <GlossaryItem term="现在的Variable类">
    检索键 dl2-B 对应目录坐标「8·1 现在的Variable类」；在“步骤8
    从递归到循环”中用于用Variable与Function对象记录前向边并按链式法则传播梯度，需要连接原版范围、对象/图边、shape、梯度轨迹、资源状态与回归证据。
  </GlossaryItem>
  <GlossaryItem term="使用循环实现">
    检索键 dl2-C 对应目录坐标「8·2 使用循环实现」；在“步骤8
    从递归到循环”中用于把目录坐标转为有对象、图边、形状、梯度、资源和回归的框架增量合同，需要连接原版范围、对象/图边、shape、梯度轨迹、资源状态与回归证据。
  </GlossaryItem>
  <GlossaryItem term="代码验证">
    检索键 dl2-D 对应目录坐标「8·3 代码验证」；在“步骤8
    从递归到循环”中用于把目录坐标转为有对象、图边、形状、梯度、资源和回归的框架增量合同，需要连接原版范围、对象/图边、shape、梯度轨迹、资源状态与回归证据。
  </GlossaryItem>
</Glossary>

<Attribution
  mode="independent-rewrite"
  sourceBasis="outline-plus-official-code-and-author-sample"
  workTitle="斋藤康毅著《ゼロから作るDeep Learning ❸ ―フレームワーク編》"
  adaptedUrl="https://www.oreilly.co.jp/books/9784873119069/"
/>
