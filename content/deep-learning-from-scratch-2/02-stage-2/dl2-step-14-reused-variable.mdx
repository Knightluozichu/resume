---
title: "步骤14 重复使用同一个变量"
type: B
section: "第2阶段 · 自然代码"
order: 14
description: "步骤14 重复使用同一个变量：从“每个变量假定只有一条路径”到“分支梯度累加且可显式清零”，用独立计算、代码和故障注入验证。"
demo: true
math: true
sourceUrl: "https://www.oreilly.co.jp/books/9784873119069/"
qualityVersion: 2
practiceMode: code
sourceMode: independent-rewrite
draft: false
---


## 为什么这一步不能跳过

步骤14 重复使用同一个变量 的增量不是 API 名称，而是把 **每个变量假定只有一条路径** 变为 **分支梯度累加且可显式清零**。它的核心机制是：同一 Variable 经多条路径复用时必须累加梯度，cleargrad 则在新一轮反传前清空历史贡献。

这一步验收时固定输入数组、随机种子、DeZero 前一步版本与运行设备。先写出预期的 forward、shape、creator 或状态变化，再执行实现；backward 后清空派生梯度并从相同输入重放。这样才能区分本步能力与缓存、旧图或随机性的偶然结果。


## 数学与状态合同

$$
g_x\leftarrow g_x+g_x^{(new)}
$$

对 步骤14 重复使用同一个变量，公式中的每个量都要绑定到实际数组或对象：记录 dtype、shape、设备、creator/generation 或训练模式。若发生广播、归约、转置、时间递推或设备迁移，反向结果必须回到每个输入原有的语义边界。

## 目录单元到教学证据

### 步骤14 重复使用同一个变量

在 **步骤14 重复使用同一个变量** 中，**步骤14 重复使用同一个变量** 把“每个变量假定只有一条路径”推进到“分支梯度累加且可显式清零”。本单元的可观察机制是：同一 Variable 经多条路径复用时必须累加梯度，cleargrad 则在新一轮反传前清空历史贡献。

核对 **步骤14 重复使用同一个变量** 时先记录“输入对象”的输入、输出、shape、对象连接或运行模式，再注入反例“覆盖而非累加会漏掉分支贡献，不清空又会把两次训练的梯度混在一起”。如果最终数值看似合理但这一处首先分叉，实验必须停止并恢复基线。

### 14.1 问题的原因

在 **步骤14 重复使用同一个变量** 中，**14.1 问题的原因** 把“每个变量假定只有一条路径”推进到“分支梯度累加且可显式清零”。本单元的可观察机制是：同一 Variable 经多条路径复用时必须累加梯度，cleargrad 则在新一轮反传前清空历史贡献。

核对 **14.1 问题的原因** 时先记录“Function调用”的输入、输出、shape、对象连接或运行模式，再注入反例“覆盖而非累加会漏掉分支贡献，不清空又会把两次训练的梯度混在一起”。如果最终数值看似合理但这一处首先分叉，实验必须停止并恢复基线。

### 14.2 解决方案

在 **步骤14 重复使用同一个变量** 中，**14.2 解决方案** 把“每个变量假定只有一条路径”推进到“分支梯度累加且可显式清零”。本单元的可观察机制是：同一 Variable 经多条路径复用时必须累加梯度，cleargrad 则在新一轮反传前清空历史贡献。

核对 **14.2 解决方案** 时先记录“输出连接”的输入、输出、shape、对象连接或运行模式，再注入反例“覆盖而非累加会漏掉分支贡献，不清空又会把两次训练的梯度混在一起”。如果最终数值看似合理但这一处首先分叉，实验必须停止并恢复基线。

### 14.3 重置导数

在 **步骤14 重复使用同一个变量** 中，**14.3 重置导数** 把“每个变量假定只有一条路径”推进到“分支梯度累加且可显式清零”。本单元的可观察机制是：同一 Variable 经多条路径复用时必须累加梯度，cleargrad 则在新一轮反传前清空历史贡献。

核对 **14.3 重置导数** 时先记录“反向调度”的输入、输出、shape、对象连接或运行模式，再注入反例“覆盖而非累加会漏掉分支贡献，不清空又会把两次训练的梯度混在一起”。如果最终数值看似合理但这一处首先分叉，实验必须停止并恢复基线。

## 最小实现草图

```python
# 重复使用同一个变量：自然语法仍必须经过 Function 图节点
x = Variable(np.array(2.0))
y = target(x)
assert y.creator is not None
y.backward()
```

这段草图不复制原书实现；它只保留 步骤14 重复使用同一个变量 的可验证接口。实际练习要补齐输入构造、断言和失败样本，并用作者官方仓的对应 step 文件核对行为边界，而不是照抄代码行。

## 单变量因果实验

先预测改变一个输入后，输入对象 → Function调用 → 输出连接 → 反向调度 → 梯度核对 中哪一项最先变化。操作基线、边界和故障三种场景；保存证据后点击“重置实验”，确认所有状态回到初始值。


## 故障诊断


## 本步小结

步骤14 重复使用同一个变量 的完成标准是：能从“每个变量假定只有一条路径”解释到“分支梯度累加且可显式清零”，能用本页数学与状态合同算出或验证关键量，并能诊断“覆盖而非累加会漏掉分支贡献，不清空又会把两次训练的梯度混在一起”。只出现目录词、只跑通最终 loss 或只展示一张通用计算图都不算通过。

本步需要准确使用这些术语：、、、。它们必须落到本页实验的数值、shape、对象连接或运行状态上。


