---
title: "步骤27 泰勒展开的导数"
type: B
section: "第3阶段 · 高阶导数"
order: 27
description: "步骤27 泰勒展开的导数：从“只能调用 NumPy 的整体 sin”到“级数实现展示近似与图规模取舍”，用独立计算、代码和故障注入验证。"
demo: true
math: true
sourceUrl: "https://www.oreilly.co.jp/books/9784873119069/"
qualityVersion: 2
practiceMode: calculation
sourceMode: independent-rewrite
draft: false
---


## 为什么这一步不能跳过

步骤27 泰勒展开的导数 的增量不是 API 名称，而是把 **只能调用 NumPy 的整体 sin** 变为 **级数实现展示近似与图规模取舍**。它的核心机制是：sin 的泰勒级数逐项累加并以项绝对值阈值停止，自动微分图保留每一项的导数。

这一步验收时固定输入数组、随机种子、DeZero 前一步版本与运行设备。先写出预期的 forward、shape、creator 或状态变化，再执行实现；backward 后清空派生梯度并从相同输入重放。这样才能区分本步能力与缓存、旧图或随机性的偶然结果。


## 数学与状态合同

$$
\sin x=\sum_{i=0}^{\infty}\frac{(-1)^ix^{2i+1}}{(2i+1)!}
$$

对 步骤27 泰勒展开的导数，公式中的每个量都要绑定到实际数组或对象：记录 dtype、shape、设备、creator/generation 或训练模式。若发生广播、归约、转置、时间递推或设备迁移，反向结果必须回到每个输入原有的语义边界。

## 目录单元到教学证据

### 步骤27 泰勒展开的导数

在 **步骤27 泰勒展开的导数** 中，**步骤27 泰勒展开的导数** 把“只能调用 NumPy 的整体 sin”推进到“级数实现展示近似与图规模取舍”。本单元的可观察机制是：sin 的泰勒级数逐项累加并以项绝对值阈值停止，自动微分图保留每一项的导数。

核对 **步骤27 泰勒展开的导数** 时先记录“固定输入”的输入、输出、shape、对象连接或运行模式，再注入反例“固定迭代次数过少会产生近似误差，过多则制造冗长计算图”。如果最终数值看似合理但这一处首先分叉，实验必须停止并恢复基线。

### 27.1 sin函数的实现

在 **步骤27 泰勒展开的导数** 中，**27.1 sin函数的实现** 把“只能调用 NumPy 的整体 sin”推进到“级数实现展示近似与图规模取舍”。本单元的可观察机制是：sin 的泰勒级数逐项累加并以项绝对值阈值停止，自动微分图保留每一项的导数。

核对 **27.1 sin函数的实现** 时先记录“前向函数”的输入、输出、shape、对象连接或运行模式，再注入反例“固定迭代次数过少会产生近似误差，过多则制造冗长计算图”。如果最终数值看似合理但这一处首先分叉，实验必须停止并恢复基线。

### 27.2 泰勒展开的理论知识

在 **步骤27 泰勒展开的导数** 中，**27.2 泰勒展开的理论知识** 把“只能调用 NumPy 的整体 sin”推进到“级数实现展示近似与图规模取舍”。本单元的可观察机制是：sin 的泰勒级数逐项累加并以项绝对值阈值停止，自动微分图保留每一项的导数。

核对 **27.2 泰勒展开的理论知识** 时先记录“局部导数”的输入、输出、shape、对象连接或运行模式，再注入反例“固定迭代次数过少会产生近似误差，过多则制造冗长计算图”。如果最终数值看似合理但这一处首先分叉，实验必须停止并恢复基线。

### 27.3 泰勒展开的实现

在 **步骤27 泰勒展开的导数** 中，**27.3 泰勒展开的实现** 把“只能调用 NumPy 的整体 sin”推进到“级数实现展示近似与图规模取舍”。本单元的可观察机制是：sin 的泰勒级数逐项累加并以项绝对值阈值停止，自动微分图保留每一项的导数。

核对 **27.3 泰勒展开的实现** 时先记录“链式组合”的输入、输出、shape、对象连接或运行模式，再注入反例“固定迭代次数过少会产生近似误差，过多则制造冗长计算图”。如果最终数值看似合理但这一处首先分叉，实验必须停止并恢复基线。

### 27.4 计算图的可视化

在 **步骤27 泰勒展开的导数** 中，**27.4 计算图的可视化** 把“只能调用 NumPy 的整体 sin”推进到“级数实现展示近似与图规模取舍”。本单元的可观察机制是：sin 的泰勒级数逐项累加并以项绝对值阈值停止，自动微分图保留每一项的导数。

核对 **27.4 计算图的可视化** 时先记录“数值检验”的输入、输出、shape、对象连接或运行模式，再注入反例“固定迭代次数过少会产生近似误差，过多则制造冗长计算图”。如果最终数值看似合理但这一处首先分叉，实验必须停止并恢复基线。

## 最小实现草图

```python
# 泰勒展开的导数：解析梯度必须由独立数值差分核对
x = Variable(np.array(0.5))
y = target(x)
y.backward(create_graph=False)
assert relative_error(x.grad, central_diff(target, x)) < 1e-4
```

这段草图不复制原书实现；它只保留 步骤27 泰勒展开的导数 的可验证接口。实际练习要补齐输入构造、断言和失败样本，并用作者官方仓的对应 step 文件核对行为边界，而不是照抄代码行。

## 单变量因果实验

先预测改变一个输入后，固定输入 → 前向函数 → 局部导数 → 链式组合 → 数值检验 中哪一项最先变化。操作基线、边界和故障三种场景；保存证据后点击“重置实验”，确认所有状态回到初始值。


## 故障诊断


## 本步小结

步骤27 泰勒展开的导数 的完成标准是：能从“只能调用 NumPy 的整体 sin”解释到“级数实现展示近似与图规模取舍”，能用本页数学与状态合同算出或验证关键量，并能诊断“固定迭代次数过少会产生近似误差，过多则制造冗长计算图”。只出现目录词、只跑通最终 loss 或只展示一张通用计算图都不算通过。

本步需要准确使用这些术语：、、、、。它们必须落到本页实验的数值、shape、对象连接或运行状态上。


