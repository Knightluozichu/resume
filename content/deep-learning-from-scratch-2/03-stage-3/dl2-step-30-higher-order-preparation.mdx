---
title: "步骤30 高阶导数（准备篇）"
type: B
section: "第3阶段 · 高阶导数"
order: 30
description: "步骤30 高阶导数（准备篇）：从“梯度只是不可微 ndarray”到“梯度也成为可追踪 Variable”，用独立计算、代码和故障注入验证。"
demo: true
math: true
sourceUrl: "https://www.oreilly.co.jp/books/9784873119069/"
qualityVersion: 2
practiceMode: calculation
sourceMode: independent-rewrite
draft: false
---


## 为什么这一步不能跳过

步骤30 高阶导数（准备篇） 的增量不是 API 名称，而是把 **梯度只是不可微 ndarray** 变为 **梯度也成为可追踪 Variable**。它的核心机制是：把 grad 从 ndarray 升级为 Variable，并让 backward 运算也通过 DeZero Function 执行，为高阶图建模。

这一步验收时固定输入数组、随机种子、DeZero 前一步版本与运行设备。先写出预期的 forward、shape、creator 或状态变化，再执行实现；backward 后清空派生梯度并从相同输入重放。这样才能区分本步能力与缓存、旧图或随机性的偶然结果。


## 数学与状态合同

$$
g_x=Variable(g_y)\cdot 2x
$$

对 步骤30 高阶导数（准备篇），公式中的每个量都要绑定到实际数组或对象：记录 dtype、shape、设备、creator/generation 或训练模式。若发生广播、归约、转置、时间递推或设备迁移，反向结果必须回到每个输入原有的语义边界。

## 目录单元到教学证据

### 步骤30 高阶导数（准备篇）

在 **步骤30 高阶导数（准备篇）** 中，**步骤30 高阶导数（准备篇）** 把“梯度只是不可微 ndarray”推进到“梯度也成为可追踪 Variable”。本单元的可观察机制是：把 grad 从 ndarray 升级为 Variable，并让 backward 运算也通过 DeZero Function 执行，为高阶图建模。

核对 **步骤30 高阶导数（准备篇）** 时先记录“固定输入”的输入、输出、shape、对象连接或运行模式，再注入反例“backward 中混入 NumPy 运算会切断梯度本身的 creator”。如果最终数值看似合理但这一处首先分叉，实验必须停止并恢复基线。

### 30.1 确认工作①：Variable实例变量

在 **步骤30 高阶导数（准备篇）** 中，**30.1 确认工作①：Variable实例变量** 把“梯度只是不可微 ndarray”推进到“梯度也成为可追踪 Variable”。本单元的可观察机制是：把 grad 从 ndarray 升级为 Variable，并让 backward 运算也通过 DeZero Function 执行，为高阶图建模。

核对 **30.1 确认工作①：Variable实例变量** 时先记录“前向函数”的输入、输出、shape、对象连接或运行模式，再注入反例“backward 中混入 NumPy 运算会切断梯度本身的 creator”。如果最终数值看似合理但这一处首先分叉，实验必须停止并恢复基线。

### 30.2 确认工作②：Function类

在 **步骤30 高阶导数（准备篇）** 中，**30.2 确认工作②：Function类** 把“梯度只是不可微 ndarray”推进到“梯度也成为可追踪 Variable”。本单元的可观察机制是：把 grad 从 ndarray 升级为 Variable，并让 backward 运算也通过 DeZero Function 执行，为高阶图建模。

核对 **30.2 确认工作②：Function类** 时先记录“局部导数”的输入、输出、shape、对象连接或运行模式，再注入反例“backward 中混入 NumPy 运算会切断梯度本身的 creator”。如果最终数值看似合理但这一处首先分叉，实验必须停止并恢复基线。

### 30.3 确认工作③：Variable类的反向传播

在 **步骤30 高阶导数（准备篇）** 中，**30.3 确认工作③：Variable类的反向传播** 把“梯度只是不可微 ndarray”推进到“梯度也成为可追踪 Variable”。本单元的可观察机制是：把 grad 从 ndarray 升级为 Variable，并让 backward 运算也通过 DeZero Function 执行，为高阶图建模。

核对 **30.3 确认工作③：Variable类的反向传播** 时先记录“链式组合”的输入、输出、shape、对象连接或运行模式，再注入反例“backward 中混入 NumPy 运算会切断梯度本身的 creator”。如果最终数值看似合理但这一处首先分叉，实验必须停止并恢复基线。

## 最小实现草图

```python
# 高阶导数（准备篇）：解析梯度必须由独立数值差分核对
x = Variable(np.array(0.5))
y = target(x)
y.backward(create_graph=True)
assert relative_error(x.grad, central_diff(target, x)) < 1e-4
```

这段草图不复制原书实现；它只保留 步骤30 高阶导数（准备篇） 的可验证接口。实际练习要补齐输入构造、断言和失败样本，并用作者官方仓的对应 step 文件核对行为边界，而不是照抄代码行。

## 单变量因果实验

先预测改变一个输入后，固定输入 → 前向函数 → 局部导数 → 链式组合 → 数值检验 中哪一项最先变化。操作基线、边界和故障三种场景；保存证据后点击“重置实验”，确认所有状态回到初始值。


## 故障诊断


## 本步小结

步骤30 高阶导数（准备篇） 的完成标准是：能从“梯度只是不可微 ndarray”解释到“梯度也成为可追踪 Variable”，能用本页数学与状态合同算出或验证关键量，并能诊断“backward 中混入 NumPy 运算会切断梯度本身的 creator”。只出现目录词、只跑通最终 loss 或只展示一张通用计算图都不算通过。

本步需要准确使用这些术语：、、、。它们必须落到本页实验的数值、shape、对象连接或运行状态上。


