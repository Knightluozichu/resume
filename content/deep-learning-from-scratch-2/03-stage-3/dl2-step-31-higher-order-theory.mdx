---
title: "步骤31 高阶导数（理论篇）"
type: B
section: "第3阶段 · 高阶导数"
order: 31
description: "步骤31 高阶导数（理论篇）：从“反向传播只产生数值梯度”到“按需建立导数的计算图”，用独立计算、代码和故障注入验证。"
demo: true
math: true
sourceUrl: "https://www.oreilly.co.jp/books/9784873119069/"
qualityVersion: 2
practiceMode: calculation
sourceMode: independent-rewrite
draft: false
---


## 为什么这一步不能跳过

步骤31 高阶导数（理论篇） 的增量不是 API 名称，而是把 **反向传播只产生数值梯度** 变为 **按需建立导数的计算图**。它的核心机制是：create_graph 控制反向传播期间是否构建新计算图，一阶 backward 的运算可再接受 backward。

这一步验收时固定输入数组、随机种子、DeZero 前一步版本与运行设备。先写出预期的 forward、shape、creator 或状态变化，再执行实现；backward 后清空派生梯度并从相同输入重放。这样才能区分本步能力与缓存、旧图或随机性的偶然结果。


## 数学与状态合同

$$
y=f(x),\;g=f'(x),\;g.backward()=f''(x)
$$

对 步骤31 高阶导数（理论篇），公式中的每个量都要绑定到实际数组或对象：记录 dtype、shape、设备、creator/generation 或训练模式。若发生广播、归约、转置、时间递推或设备迁移，反向结果必须回到每个输入原有的语义边界。

## 目录单元到教学证据

### 步骤31 高阶导数（理论篇）

在 **步骤31 高阶导数（理论篇）** 中，**步骤31 高阶导数（理论篇）** 把“反向传播只产生数值梯度”推进到“按需建立导数的计算图”。本单元的可观察机制是：create_graph 控制反向传播期间是否构建新计算图，一阶 backward 的运算可再接受 backward。

核对 **步骤31 高阶导数（理论篇）** 时先记录“固定输入”的输入、输出、shape、对象连接或运行模式，再注入反例“无条件建反向图会浪费内存，无条件关闭则无法求二阶导”。如果最终数值看似合理但这一处首先分叉，实验必须停止并恢复基线。

### 31.1 在反向传播时进行的计算

在 **步骤31 高阶导数（理论篇）** 中，**31.1 在反向传播时进行的计算** 把“反向传播只产生数值梯度”推进到“按需建立导数的计算图”。本单元的可观察机制是：create_graph 控制反向传播期间是否构建新计算图，一阶 backward 的运算可再接受 backward。

核对 **31.1 在反向传播时进行的计算** 时先记录“前向函数”的输入、输出、shape、对象连接或运行模式，再注入反例“无条件建反向图会浪费内存，无条件关闭则无法求二阶导”。如果最终数值看似合理但这一处首先分叉，实验必须停止并恢复基线。

### 31.2 创建反向传播的计算图的方法

在 **步骤31 高阶导数（理论篇）** 中，**31.2 创建反向传播的计算图的方法** 把“反向传播只产生数值梯度”推进到“按需建立导数的计算图”。本单元的可观察机制是：create_graph 控制反向传播期间是否构建新计算图，一阶 backward 的运算可再接受 backward。

核对 **31.2 创建反向传播的计算图的方法** 时先记录“局部导数”的输入、输出、shape、对象连接或运行模式，再注入反例“无条件建反向图会浪费内存，无条件关闭则无法求二阶导”。如果最终数值看似合理但这一处首先分叉，实验必须停止并恢复基线。

## 最小实现草图

```python
# 高阶导数（理论篇）：解析梯度必须由独立数值差分核对
x = Variable(np.array(0.5))
y = target(x)
y.backward(create_graph=True)
assert relative_error(x.grad, central_diff(target, x)) < 1e-4
```

这段草图不复制原书实现；它只保留 步骤31 高阶导数（理论篇） 的可验证接口。实际练习要补齐输入构造、断言和失败样本，并用作者官方仓的对应 step 文件核对行为边界，而不是照抄代码行。

## 单变量因果实验

先预测改变一个输入后，固定输入 → 前向函数 → 局部导数 → 链式组合 → 数值检验 中哪一项最先变化。操作基线、边界和故障三种场景；保存证据后点击“重置实验”，确认所有状态回到初始值。


## 故障诊断


## 本步小结

步骤31 高阶导数（理论篇） 的完成标准是：能从“反向传播只产生数值梯度”解释到“按需建立导数的计算图”，能用本页数学与状态合同算出或验证关键量，并能诊断“无条件建反向图会浪费内存，无条件关闭则无法求二阶导”。只出现目录词、只跑通最终 loss 或只展示一张通用计算图都不算通过。

本步需要准确使用这些术语：、、。它们必须落到本页实验的数值、shape、对象连接或运行状态上。


