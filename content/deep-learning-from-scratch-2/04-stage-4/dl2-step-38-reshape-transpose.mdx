---
title: "步骤38 改变形状的函数"
type: B
section: "第4阶段 · 神经网络"
order: 38
description: "步骤38 改变形状的函数：从“张量形状固定”到“可微形状变换保存逆操作信息”，用独立计算、代码和故障注入验证。"
demo: true
math: true
sourceUrl: "https://www.oreilly.co.jp/books/9784873119069/"
qualityVersion: 2
practiceMode: calculation
sourceMode: independent-rewrite
draft: false
---

import { Dl2Step38ReshapeTransposeLab } from "@/components/mdx/deep-learning-from-scratch-2/diagrams/dl2-step-38-reshape-transpose";
import {
  Objectives,
  Callout,
  Term,
  Glossary,
  GlossaryItem,
  Exercises,
  Answer,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能说明 步骤38 改变形状的函数 相对前一步新增的责任：可微形状变换保存逆操作信息
- 能依据本页数学与状态合同完成推导或验证，并写清输入、输出与 shape
- 能运行本页最小代码草图，观察 输入shape、轴变换、前向shape、反向还原、梯度shape 的因果链
- 能注入“只保存输出 shape 无法在 backward 还原输入布局”并在首个错误证据处停止

</Objectives>

## 为什么这一步不能跳过

步骤38 改变形状的函数 的增量不是 API 名称，而是把 **张量形状固定** 变为 **可微形状变换保存逆操作信息**。它的核心机制是：reshape/transpose 前向只改变视图或轴序，反向必须恢复输入原 shape 或逆置换。

这一步验收时固定输入数组、随机种子、DeZero 前一步版本与运行设备。先写出预期的 forward、shape、creator 或状态变化，再执行实现；backward 后清空派生梯度并从相同输入重放。这样才能区分本步能力与缓存、旧图或随机性的偶然结果。

<Callout type="info" title="本步硬合同">
  前向证据、反向证据和重置后重放必须同时一致。只保存输出 shape 无法在 backward
  还原输入布局 时，即使最终 loss 或输出合理，也应判定 步骤38 改变形状的函数
  失败。
</Callout>

## 数学与状态合同

$$
g_x=reshape(g_y,shape(x))
$$

对 步骤38 改变形状的函数，公式中的每个量都要绑定到实际数组或对象：记录 dtype、shape、设备、creator/generation 或训练模式。若发生广播、归约、转置、时间递推或设备迁移，反向结果必须回到每个输入原有的语义边界。

## 目录单元到教学证据

### 步骤38 改变形状的函数

在 **步骤38 改变形状的函数** 中，**步骤38 改变形状的函数** 把“张量形状固定”推进到“可微形状变换保存逆操作信息”。本单元的可观察机制是：reshape/transpose 前向只改变视图或轴序，反向必须恢复输入原 shape 或逆置换。

核对 **步骤38 改变形状的函数** 时先记录“输入shape”的输入、输出、shape、对象连接或运行模式，再注入反例“只保存输出 shape 无法在 backward 还原输入布局”。如果最终数值看似合理但这一处首先分叉，实验必须停止并恢复基线。

### 38.1 reshape函数的实现

在 **步骤38 改变形状的函数** 中，**38.1 reshape函数的实现** 把“张量形状固定”推进到“可微形状变换保存逆操作信息”。本单元的可观察机制是：reshape/transpose 前向只改变视图或轴序，反向必须恢复输入原 shape 或逆置换。

核对 **38.1 reshape函数的实现** 时先记录“轴变换”的输入、输出、shape、对象连接或运行模式，再注入反例“只保存输出 shape 无法在 backward 还原输入布局”。如果最终数值看似合理但这一处首先分叉，实验必须停止并恢复基线。

### 38.2 从Variable对象调用reshape

在 **步骤38 改变形状的函数** 中，**38.2 从Variable对象调用reshape** 把“张量形状固定”推进到“可微形状变换保存逆操作信息”。本单元的可观察机制是：reshape/transpose 前向只改变视图或轴序，反向必须恢复输入原 shape 或逆置换。

核对 **38.2 从Variable对象调用reshape** 时先记录“前向shape”的输入、输出、shape、对象连接或运行模式，再注入反例“只保存输出 shape 无法在 backward 还原输入布局”。如果最终数值看似合理但这一处首先分叉，实验必须停止并恢复基线。

### 38.3 矩阵的转置

在 **步骤38 改变形状的函数** 中，**38.3 矩阵的转置** 把“张量形状固定”推进到“可微形状变换保存逆操作信息”。本单元的可观察机制是：reshape/transpose 前向只改变视图或轴序，反向必须恢复输入原 shape 或逆置换。

核对 **38.3 矩阵的转置** 时先记录“反向还原”的输入、输出、shape、对象连接或运行模式，再注入反例“只保存输出 shape 无法在 backward 还原输入布局”。如果最终数值看似合理但这一处首先分叉，实验必须停止并恢复基线。

### 38.4 实际的transpose函数（补充内容）

在 **步骤38 改变形状的函数** 中，**38.4 实际的transpose函数（补充内容）** 把“张量形状固定”推进到“可微形状变换保存逆操作信息”。本单元的可观察机制是：reshape/transpose 前向只改变视图或轴序，反向必须恢复输入原 shape 或逆置换。

核对 **38.4 实际的transpose函数（补充内容）** 时先记录“梯度shape”的输入、输出、shape、对象连接或运行模式，再注入反例“只保存输出 shape 无法在 backward 还原输入布局”。如果最终数值看似合理但这一处首先分叉，实验必须停止并恢复基线。

## 最小实现草图

```python
# 改变形状的函数：前向与反向都记录 shape
x = Variable(np.arange(24).reshape(2, 3, 4))
y = target(x)
y.backward()
assert x.grad.shape == x.shape
```

这段草图不复制原书实现；它只保留 步骤38 改变形状的函数 的可验证接口。实际练习要补齐输入构造、断言和失败样本，并用作者官方仓的对应 step 文件核对行为边界，而不是照抄代码行。

## 单变量因果实验

先预测改变一个输入后，输入shape → 轴变换 → 前向shape → 反向还原 → 梯度shape 中哪一项最先变化。操作基线、边界和故障三种场景；保存证据后点击“重置实验”，确认所有状态回到初始值。

<Dl2Step38ReshapeTransposeLab />

## 故障诊断

<Callout
  type="trap"
  title="误区：只保存输出 shape 无法在 backward 还原输入布局"
>
  这会破坏 步骤38 改变形状的函数 的“可微形状变换保存逆操作信息”。先检查
  输入shape，再沿 轴变换、前向shape、反向还原、梯度shape
  顺序定位第一处分叉；不要用最终输出掩盖局部错误。
</Callout>

<Callout type="tip" title="恢复顺序">
  恢复固定输入与模式 → 清空 grad/缓存/隐藏状态 → 重建当前计算图 → 重跑 forward →
  重跑 backward → 与独立公式或 shape 断言比较。
</Callout>

## 本步小结

步骤38 改变形状的函数 的完成标准是：能从“张量形状固定”解释到“可微形状变换保存逆操作信息”，能用本页数学与状态合同算出或验证关键量，并能诊断“只保存输出 shape 无法在 backward 还原输入布局”。只出现目录词、只跑通最终 loss 或只展示一张通用计算图都不算通过。

本步需要准确使用这些术语：<Term def="在 步骤38 改变形状的函数 中对应“输入shape”的可验证对象或状态">步骤38 改变形状的函数</Term>、<Term def="在 步骤38 改变形状的函数 中对应“轴变换”的可验证对象或状态">38.1 reshape函数的实现</Term>、<Term def="在 步骤38 改变形状的函数 中对应“前向shape”的可验证对象或状态">38.2 从Variable对象调用reshape</Term>、<Term def="在 步骤38 改变形状的函数 中对应“反向还原”的可验证对象或状态">38.3 矩阵的转置</Term>、<Term def="在 步骤38 改变形状的函数 中对应“梯度shape”的可验证对象或状态">38.4 实际的transpose函数（补充内容）</Term>。它们必须落到本页实验的数值、shape、对象连接或运行状态上。

<Glossary>

<GlossaryItem term="步骤38 改变形状的函数">
  在 步骤38 改变形状的函数 中，步骤38 改变形状的函数
  对应“输入shape”；验收时必须给出实际数值、shape 或对象状态，不能只复述名称。
</GlossaryItem>

<GlossaryItem term="38.1 reshape函数的实现">
  在 步骤38 改变形状的函数 中，38.1 reshape函数的实现
  对应“轴变换”；验收时必须给出实际数值、shape 或对象状态，不能只复述名称。
</GlossaryItem>

<GlossaryItem term="38.2 从Variable对象调用reshape">
  在 步骤38 改变形状的函数 中，38.2 从Variable对象调用reshape
  对应“前向shape”；验收时必须给出实际数值、shape 或对象状态，不能只复述名称。
</GlossaryItem>

<GlossaryItem term="38.3 矩阵的转置">
  在 步骤38 改变形状的函数 中，38.3 矩阵的转置
  对应“反向还原”；验收时必须给出实际数值、shape 或对象状态，不能只复述名称。
</GlossaryItem>

<GlossaryItem term="38.4 实际的transpose函数（补充内容）">
  在 步骤38 改变形状的函数 中，38.4 实际的transpose函数（补充内容）
  对应“梯度shape”；验收时必须给出实际数值、shape 或对象状态，不能只复述名称。
</GlossaryItem>

</Glossary>

<Exercises>

1. 步骤38 改变形状的函数 相对前一步改变了哪个合同？请用一个最小输入指出最先变化的证据。

<Answer>
  它把“张量形状固定”推进为“可微形状变换保存逆操作信息”。固定输入后先检查 输入shape，再核对 轴变换 与 前向shape；机制应符合“reshape/transpose 前向只改变视图或轴序，反向必须恢复输入原 shape 或逆置换”。

目录单元逐项验收：步骤38 改变形状的函数（核对 输入shape）；38.1 reshape函数的实现（核对 轴变换）；38.2 从Variable对象调用reshape（核对 前向shape）；38.3 矩阵的转置（核对 反向还原）；38.4 实际的transpose函数（补充内容）（核对 梯度shape）。每项都要写下预期、实测与复位后的重放证据。

</Answer>

2. 使用本页数学与状态合同做一次手算或 shape 推演，并说明代码中对应的数组或对象。

<Answer>
  先给公式中的输入标注数值、shape 与设备，得到预期结果后再运行最小草图。步骤38
  改变形状的函数 的 forward 结果和
  backward/状态证据必须同时匹配，不能只比较一个最终标量。
</Answer>

3. 注入“只保存输出 shape 无法在 backward 还原输入布局”后，如何证明重置真的恢复了实验？

<Answer>
  保存故障前的输入、输入shape
  和最终状态；注入故障定位首错后执行本页恢复顺序。重置后的
  forward、梯度/shape、模式与证据轨迹都应回到基线，随后再次运行得到相同结果。
</Answer>

</Exercises>

<Attribution />
