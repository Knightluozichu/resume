---
title: "步骤43 神经网络"
type: B
section: "第4阶段 · 神经网络"
order: 43
description: "步骤43 神经网络：从“线性回归只能拟合直线”到“非线性网络拟合弯曲决策面”，用独立计算、代码和故障注入验证。"
demo: true
math: true
sourceUrl: "https://www.oreilly.co.jp/books/9784873119069/"
qualityVersion: 2
practiceMode: code
sourceMode: independent-rewrite
draft: false
---


## 为什么这一步不能跳过

步骤43 神经网络 的增量不是 API 名称，而是把 **线性回归只能拟合直线** 变为 **非线性网络拟合弯曲决策面**。它的核心机制是：Linear 与 sigmoid 组合出两层网络，非线性激活使模型超越单一仿射映射。

这一步验收时固定输入数组、随机种子、DeZero 前一步版本与运行设备。先写出预期的 forward、shape、creator 或状态变化，再执行实现；backward 后清空派生梯度并从相同输入重放。这样才能区分本步能力与缓存、旧图或随机性的偶然结果。


## 数学与状态合同

$$
y=W_2\sigma(W_1x+b_1)+b_2
$$

对 步骤43 神经网络，公式中的每个量都要绑定到实际数组或对象：记录 dtype、shape、设备、creator/generation 或训练模式。若发生广播、归约、转置、时间递推或设备迁移，反向结果必须回到每个输入原有的语义边界。

## 目录单元到教学证据

### 步骤43 神经网络

在 **步骤43 神经网络** 中，**步骤43 神经网络** 把“线性回归只能拟合直线”推进到“非线性网络拟合弯曲决策面”。本单元的可观察机制是：Linear 与 sigmoid 组合出两层网络，非线性激活使模型超越单一仿射映射。

核对 **步骤43 神经网络** 时先记录“取批数据”的输入、输出、shape、对象连接或运行模式，再注入反例“省略激活函数时多层线性层仍等价于单层线性变换”。如果最终数值看似合理但这一处首先分叉，实验必须停止并恢复基线。

### 43.1 DeZero中的linear函数

在 **步骤43 神经网络** 中，**43.1 DeZero中的linear函数** 把“线性回归只能拟合直线”推进到“非线性网络拟合弯曲决策面”。本单元的可观察机制是：Linear 与 sigmoid 组合出两层网络，非线性激活使模型超越单一仿射映射。

核对 **43.1 DeZero中的linear函数** 时先记录“模型前向”的输入、输出、shape、对象连接或运行模式，再注入反例“省略激活函数时多层线性层仍等价于单层线性变换”。如果最终数值看似合理但这一处首先分叉，实验必须停止并恢复基线。

### 43.2 非线性数据集

在 **步骤43 神经网络** 中，**43.2 非线性数据集** 把“线性回归只能拟合直线”推进到“非线性网络拟合弯曲决策面”。本单元的可观察机制是：Linear 与 sigmoid 组合出两层网络，非线性激活使模型超越单一仿射映射。

核对 **43.2 非线性数据集** 时先记录“计算损失”的输入、输出、shape、对象连接或运行模式，再注入反例“省略激活函数时多层线性层仍等价于单层线性变换”。如果最终数值看似合理但这一处首先分叉，实验必须停止并恢复基线。

### 43.3 激活函数和神经网络

在 **步骤43 神经网络** 中，**43.3 激活函数和神经网络** 把“线性回归只能拟合直线”推进到“非线性网络拟合弯曲决策面”。本单元的可观察机制是：Linear 与 sigmoid 组合出两层网络，非线性激活使模型超越单一仿射映射。

核对 **43.3 激活函数和神经网络** 时先记录“反向传播”的输入、输出、shape、对象连接或运行模式，再注入反例“省略激活函数时多层线性层仍等价于单层线性变换”。如果最终数值看似合理但这一处首先分叉，实验必须停止并恢复基线。

### 43.4 神经网络的实现

在 **步骤43 神经网络** 中，**43.4 神经网络的实现** 把“线性回归只能拟合直线”推进到“非线性网络拟合弯曲决策面”。本单元的可观察机制是：Linear 与 sigmoid 组合出两层网络，非线性激活使模型超越单一仿射映射。

核对 **43.4 神经网络的实现** 时先记录“参数更新”的输入、输出、shape、对象连接或运行模式，再注入反例“省略激活函数时多层线性层仍等价于单层线性变换”。如果最终数值看似合理但这一处首先分叉，实验必须停止并恢复基线。

## 最小实现草图

```python
# 神经网络：每轮都从干净梯度开始
model.cleargrads()
pred = model(batch_x)
loss = objective(pred, batch_t)
loss.backward()
optimizer.update()
```

这段草图不复制原书实现；它只保留 步骤43 神经网络 的可验证接口。实际练习要补齐输入构造、断言和失败样本，并用作者官方仓的对应 step 文件核对行为边界，而不是照抄代码行。

## 单变量因果实验

先预测改变一个输入后，取批数据 → 模型前向 → 计算损失 → 反向传播 → 参数更新 中哪一项最先变化。操作基线、边界和故障三种场景；保存证据后点击“重置实验”，确认所有状态回到初始值。


## 故障诊断


## 本步小结

步骤43 神经网络 的完成标准是：能从“线性回归只能拟合直线”解释到“非线性网络拟合弯曲决策面”，能用本页数学与状态合同算出或验证关键量，并能诊断“省略激活函数时多层线性层仍等价于单层线性变换”。只出现目录词、只跑通最终 loss 或只展示一张通用计算图都不算通过。

本步需要准确使用这些术语：、、、、。它们必须落到本页实验的数值、shape、对象连接或运行状态上。


