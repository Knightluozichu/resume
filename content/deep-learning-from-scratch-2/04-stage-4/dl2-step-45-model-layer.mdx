---
title: "步骤45 汇总层的层"
type: B
section: "第4阶段 · 神经网络"
order: 45
description: "步骤45 汇总层的层：从“单层能管理参数”到“嵌套模型递归汇总所有层”，用独立计算、代码和故障注入验证。"
demo: true
math: true
sourceUrl: "https://www.oreilly.co.jp/books/9784873119069/"
qualityVersion: 2
practiceMode: code
sourceMode: independent-rewrite
draft: false
---


## 为什么这一步不能跳过

步骤45 汇总层的层 的增量不是 API 名称，而是把 **单层能管理参数** 变为 **嵌套模型递归汇总所有层**。它的核心机制是：Model 继承 Layer 并可嵌套子层，MLP 根据尺寸列表构造任意深度线性层序列。

这一步验收时固定输入数组、随机种子、DeZero 前一步版本与运行设备。先写出预期的 forward、shape、creator 或状态变化，再执行实现；backward 后清空派生梯度并从相同输入重放。这样才能区分本步能力与缓存、旧图或随机性的偶然结果。


## 数学与状态合同

$$
h_{k+1}=\sigma(L_k(h_k))
$$

对 步骤45 汇总层的层，公式中的每个量都要绑定到实际数组或对象：记录 dtype、shape、设备、creator/generation 或训练模式。若发生广播、归约、转置、时间递推或设备迁移，反向结果必须回到每个输入原有的语义边界。

## 目录单元到教学证据

### 步骤45 汇总层的层

在 **步骤45 汇总层的层** 中，**步骤45 汇总层的层** 把“单层能管理参数”推进到“嵌套模型递归汇总所有层”。本单元的可观察机制是：Model 继承 Layer 并可嵌套子层，MLP 根据尺寸列表构造任意深度线性层序列。

核对 **步骤45 汇总层的层** 时先记录“取批数据”的输入、输出、shape、对象连接或运行模式，再注入反例“子层不注册为属性时递归参数遍历会漏掉整层权重”。如果最终数值看似合理但这一处首先分叉，实验必须停止并恢复基线。

### 45.1 扩展Layer类

在 **步骤45 汇总层的层** 中，**45.1 扩展Layer类** 把“单层能管理参数”推进到“嵌套模型递归汇总所有层”。本单元的可观察机制是：Model 继承 Layer 并可嵌套子层，MLP 根据尺寸列表构造任意深度线性层序列。

核对 **45.1 扩展Layer类** 时先记录“模型前向”的输入、输出、shape、对象连接或运行模式，再注入反例“子层不注册为属性时递归参数遍历会漏掉整层权重”。如果最终数值看似合理但这一处首先分叉，实验必须停止并恢复基线。

### 45.2 Model类

在 **步骤45 汇总层的层** 中，**45.2 Model类** 把“单层能管理参数”推进到“嵌套模型递归汇总所有层”。本单元的可观察机制是：Model 继承 Layer 并可嵌套子层，MLP 根据尺寸列表构造任意深度线性层序列。

核对 **45.2 Model类** 时先记录“计算损失”的输入、输出、shape、对象连接或运行模式，再注入反例“子层不注册为属性时递归参数遍历会漏掉整层权重”。如果最终数值看似合理但这一处首先分叉，实验必须停止并恢复基线。

### 45.3 使用Model来解决问题

在 **步骤45 汇总层的层** 中，**45.3 使用Model来解决问题** 把“单层能管理参数”推进到“嵌套模型递归汇总所有层”。本单元的可观察机制是：Model 继承 Layer 并可嵌套子层，MLP 根据尺寸列表构造任意深度线性层序列。

核对 **45.3 使用Model来解决问题** 时先记录“反向传播”的输入、输出、shape、对象连接或运行模式，再注入反例“子层不注册为属性时递归参数遍历会漏掉整层权重”。如果最终数值看似合理但这一处首先分叉，实验必须停止并恢复基线。

### 45.4 MLP类

在 **步骤45 汇总层的层** 中，**45.4 MLP类** 把“单层能管理参数”推进到“嵌套模型递归汇总所有层”。本单元的可观察机制是：Model 继承 Layer 并可嵌套子层，MLP 根据尺寸列表构造任意深度线性层序列。

核对 **45.4 MLP类** 时先记录“参数更新”的输入、输出、shape、对象连接或运行模式，再注入反例“子层不注册为属性时递归参数遍历会漏掉整层权重”。如果最终数值看似合理但这一处首先分叉，实验必须停止并恢复基线。

## 最小实现草图

```python
# 汇总层的层：每轮都从干净梯度开始
model.cleargrads()
pred = model(batch_x)
loss = objective(pred, batch_t)
loss.backward()
optimizer.update()
```

这段草图不复制原书实现；它只保留 步骤45 汇总层的层 的可验证接口。实际练习要补齐输入构造、断言和失败样本，并用作者官方仓的对应 step 文件核对行为边界，而不是照抄代码行。

## 单变量因果实验

先预测改变一个输入后，取批数据 → 模型前向 → 计算损失 → 反向传播 → 参数更新 中哪一项最先变化。操作基线、边界和故障三种场景；保存证据后点击“重置实验”，确认所有状态回到初始值。


## 故障诊断


## 本步小结

步骤45 汇总层的层 的完成标准是：能从“单层能管理参数”解释到“嵌套模型递归汇总所有层”，能用本页数学与状态合同算出或验证关键量，并能诊断“子层不注册为属性时递归参数遍历会漏掉整层权重”。只出现目录词、只跑通最终 loss 或只展示一张通用计算图都不算通过。

本步需要准确使用这些术语：、、、、。它们必须落到本页实验的数值、shape、对象连接或运行状态上。


