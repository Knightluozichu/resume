---
title: "步骤49 Dataset类和预处理"
type: B
section: "第4阶段 · 神经网络"
order: 49
description: "步骤49 Dataset类和预处理：从“训练代码直接持有完整数组”到“数据访问与预处理形成稳定接口”，用独立计算、代码和故障注入验证。"
demo: true
math: true
sourceUrl: "https://www.oreilly.co.jp/books/9784873119069/"
qualityVersion: 2
practiceMode: code
sourceMode: independent-rewrite
draft: true
---

import { Dl2Step49DatasetPreprocessLab } from "@/components/mdx/deep-learning-from-scratch-2/diagrams/dl2-step-49-dataset-preprocess";
import {
  Objectives,
  Callout,
  Term,
  Glossary,
  GlossaryItem,
  Exercises,
  Answer,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能说明 步骤49 Dataset类和预处理 相对前一步新增的责任：数据访问与预处理形成稳定接口
- 能依据本页数学与状态合同完成推导或验证，并写清输入、输出与 shape
- 能运行本页最小代码草图，观察 取批数据、模型前向、计算损失、反向传播、参数更新 的因果链
- 能注入“原地修改共享原始数组会让不同 epoch 的样本反复预处理”并在首个错误证据处停止

</Objectives>

## 为什么这一步不能跳过

步骤49 Dataset类和预处理 的增量不是 API 名称，而是把 **训练代码直接持有完整数组** 变为 **数据访问与预处理形成稳定接口**。它的核心机制是：Dataset 把原始数据、标签与 transform/target_transform 延迟组合，避免预处理污染源数据。

这一步验收时固定输入数组、随机种子、DeZero 前一步版本与运行设备。先写出预期的 forward、shape、creator 或状态变化，再执行实现；backward 后清空派生梯度并从相同输入重放。这样才能区分本步能力与缓存、旧图或随机性的偶然结果。

<Callout type="info" title="本步硬合同">
  前向证据、反向证据和重置后重放必须同时一致。原地修改共享原始数组会让不同 epoch
  的样本反复预处理 时，即使最终 loss 或输出合理，也应判定 步骤49
  Dataset类和预处理 失败。
</Callout>

## 数学与状态合同

$$
sample_i=(transform(x_i),target\_transform(t_i))
$$

对 步骤49 Dataset类和预处理，公式中的每个量都要绑定到实际数组或对象：记录 dtype、shape、设备、creator/generation 或训练模式。若发生广播、归约、转置、时间递推或设备迁移，反向结果必须回到每个输入原有的语义边界。

## 目录单元到教学证据

### 步骤49 Dataset类和预处理

在 **步骤49 Dataset类和预处理** 中，**步骤49 Dataset类和预处理** 把“训练代码直接持有完整数组”推进到“数据访问与预处理形成稳定接口”。本单元的可观察机制是：Dataset 把原始数据、标签与 transform/target_transform 延迟组合，避免预处理污染源数据。

核对 **步骤49 Dataset类和预处理** 时先记录“取批数据”的输入、输出、shape、对象连接或运行模式，再注入反例“原地修改共享原始数组会让不同 epoch 的样本反复预处理”。如果最终数值看似合理但这一处首先分叉，实验必须停止并恢复基线。

### 49.1 Dataset类的实现

在 **步骤49 Dataset类和预处理** 中，**49.1 Dataset类的实现** 把“训练代码直接持有完整数组”推进到“数据访问与预处理形成稳定接口”。本单元的可观察机制是：Dataset 把原始数据、标签与 transform/target_transform 延迟组合，避免预处理污染源数据。

核对 **49.1 Dataset类的实现** 时先记录“模型前向”的输入、输出、shape、对象连接或运行模式，再注入反例“原地修改共享原始数组会让不同 epoch 的样本反复预处理”。如果最终数值看似合理但这一处首先分叉，实验必须停止并恢复基线。

### 49.2 大型数据集的情况

在 **步骤49 Dataset类和预处理** 中，**49.2 大型数据集的情况** 把“训练代码直接持有完整数组”推进到“数据访问与预处理形成稳定接口”。本单元的可观察机制是：Dataset 把原始数据、标签与 transform/target_transform 延迟组合，避免预处理污染源数据。

核对 **49.2 大型数据集的情况** 时先记录“计算损失”的输入、输出、shape、对象连接或运行模式，再注入反例“原地修改共享原始数组会让不同 epoch 的样本反复预处理”。如果最终数值看似合理但这一处首先分叉，实验必须停止并恢复基线。

### 49.3 数据的连接

在 **步骤49 Dataset类和预处理** 中，**49.3 数据的连接** 把“训练代码直接持有完整数组”推进到“数据访问与预处理形成稳定接口”。本单元的可观察机制是：Dataset 把原始数据、标签与 transform/target_transform 延迟组合，避免预处理污染源数据。

核对 **49.3 数据的连接** 时先记录“反向传播”的输入、输出、shape、对象连接或运行模式，再注入反例“原地修改共享原始数组会让不同 epoch 的样本反复预处理”。如果最终数值看似合理但这一处首先分叉，实验必须停止并恢复基线。

### 49.4 用于训练的代码

在 **步骤49 Dataset类和预处理** 中，**49.4 用于训练的代码** 把“训练代码直接持有完整数组”推进到“数据访问与预处理形成稳定接口”。本单元的可观察机制是：Dataset 把原始数据、标签与 transform/target_transform 延迟组合，避免预处理污染源数据。

核对 **49.4 用于训练的代码** 时先记录“参数更新”的输入、输出、shape、对象连接或运行模式，再注入反例“原地修改共享原始数组会让不同 epoch 的样本反复预处理”。如果最终数值看似合理但这一处首先分叉，实验必须停止并恢复基线。

### 49.5 数据集的预处理

在 **步骤49 Dataset类和预处理** 中，**49.5 数据集的预处理** 把“训练代码直接持有完整数组”推进到“数据访问与预处理形成稳定接口”。本单元的可观察机制是：Dataset 把原始数据、标签与 transform/target_transform 延迟组合，避免预处理污染源数据。

核对 **49.5 数据集的预处理** 时先记录“取批数据”的输入、输出、shape、对象连接或运行模式，再注入反例“原地修改共享原始数组会让不同 epoch 的样本反复预处理”。如果最终数值看似合理但这一处首先分叉，实验必须停止并恢复基线。

## 最小实现草图

```python
# Dataset类和预处理：每轮都从干净梯度开始
model.cleargrads()
pred = model(batch_x)
loss = objective(pred, batch_t)
loss.backward()
optimizer.update()
```

这段草图不复制原书实现；它只保留 步骤49 Dataset类和预处理 的可验证接口。实际练习要补齐输入构造、断言和失败样本，并用作者官方仓的对应 step 文件核对行为边界，而不是照抄代码行。

## 单变量因果实验

先预测改变一个输入后，取批数据 → 模型前向 → 计算损失 → 反向传播 → 参数更新 中哪一项最先变化。操作基线、边界和故障三种场景；保存证据后点击“重置实验”，确认所有状态回到初始值。

<Dl2Step49DatasetPreprocessLab />

## 故障诊断

<Callout
  type="trap"
  title="误区：原地修改共享原始数组会让不同 epoch 的样本反复预处理"
>
  这会破坏 步骤49 Dataset类和预处理 的“数据访问与预处理形成稳定接口”。先检查
  取批数据，再沿 模型前向、计算损失、反向传播、参数更新
  顺序定位第一处分叉；不要用最终输出掩盖局部错误。
</Callout>

<Callout type="tip" title="恢复顺序">
  恢复固定输入与模式 → 清空 grad/缓存/隐藏状态 → 重建当前计算图 → 重跑 forward →
  重跑 backward → 与独立公式或 shape 断言比较。
</Callout>

## 本步小结

步骤49 Dataset类和预处理 的完成标准是：能从“训练代码直接持有完整数组”解释到“数据访问与预处理形成稳定接口”，能用本页数学与状态合同算出或验证关键量，并能诊断“原地修改共享原始数组会让不同 epoch 的样本反复预处理”。只出现目录词、只跑通最终 loss 或只展示一张通用计算图都不算通过。

本步需要准确使用这些术语：<Term def="在 步骤49 Dataset类和预处理 中对应“取批数据”的可验证对象或状态">步骤49 Dataset类和预处理</Term>、<Term def="在 步骤49 Dataset类和预处理 中对应“模型前向”的可验证对象或状态">49.1 Dataset类的实现</Term>、<Term def="在 步骤49 Dataset类和预处理 中对应“计算损失”的可验证对象或状态">49.2 大型数据集的情况</Term>、<Term def="在 步骤49 Dataset类和预处理 中对应“反向传播”的可验证对象或状态">49.3 数据的连接</Term>、<Term def="在 步骤49 Dataset类和预处理 中对应“参数更新”的可验证对象或状态">49.4 用于训练的代码</Term>。它们必须落到本页实验的数值、shape、对象连接或运行状态上。

<Glossary>

<GlossaryItem term="步骤49 Dataset类和预处理">
  在 步骤49 Dataset类和预处理 中，步骤49 Dataset类和预处理
  对应“取批数据”；验收时必须给出实际数值、shape 或对象状态，不能只复述名称。
</GlossaryItem>

<GlossaryItem term="49.1 Dataset类的实现">
  在 步骤49 Dataset类和预处理 中，49.1 Dataset类的实现
  对应“模型前向”；验收时必须给出实际数值、shape 或对象状态，不能只复述名称。
</GlossaryItem>

<GlossaryItem term="49.2 大型数据集的情况">
  在 步骤49 Dataset类和预处理 中，49.2 大型数据集的情况
  对应“计算损失”；验收时必须给出实际数值、shape 或对象状态，不能只复述名称。
</GlossaryItem>

<GlossaryItem term="49.3 数据的连接">
  在 步骤49 Dataset类和预处理 中，49.3 数据的连接
  对应“反向传播”；验收时必须给出实际数值、shape 或对象状态，不能只复述名称。
</GlossaryItem>

<GlossaryItem term="49.4 用于训练的代码">
  在 步骤49 Dataset类和预处理 中，49.4 用于训练的代码
  对应“参数更新”；验收时必须给出实际数值、shape 或对象状态，不能只复述名称。
</GlossaryItem>

</Glossary>

<Exercises>

1. 步骤49 Dataset类和预处理 相对前一步改变了哪个合同？请用一个最小输入指出最先变化的证据。

<Answer>
  它把“训练代码直接持有完整数组”推进为“数据访问与预处理形成稳定接口”。固定输入后先检查 取批数据，再核对 模型前向 与 计算损失；机制应符合“Dataset 把原始数据、标签与 transform/target_transform 延迟组合，避免预处理污染源数据”。

目录单元逐项验收：步骤49 Dataset类和预处理（核对 取批数据）；49.1 Dataset类的实现（核对 模型前向）；49.2 大型数据集的情况（核对 计算损失）；49.3 数据的连接（核对 反向传播）；49.4 用于训练的代码（核对 参数更新）；49.5 数据集的预处理（核对 取批数据）。每项都要写下预期、实测与复位后的重放证据。

</Answer>

2. 使用本页数学与状态合同做一次手算或 shape 推演，并说明代码中对应的数组或对象。

<Answer>
  先给公式中的输入标注数值、shape 与设备，得到预期结果后再运行最小草图。步骤49
  Dataset类和预处理 的 forward 结果和
  backward/状态证据必须同时匹配，不能只比较一个最终标量。
</Answer>

3. 注入“原地修改共享原始数组会让不同 epoch 的样本反复预处理”后，如何证明重置真的恢复了实验？

<Answer>
  保存故障前的输入、取批数据
  和最终状态；注入故障定位首错后执行本页恢复顺序。重置后的
  forward、梯度/shape、模式与证据轨迹都应回到基线，随后再次运行得到相同结果。
</Answer>

</Exercises>

<Attribution />
