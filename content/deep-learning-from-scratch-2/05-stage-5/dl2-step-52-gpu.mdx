---
title: "步骤52 支持GPU"
type: B
section: "第5阶段 · 高级挑战"
order: 52
description: "步骤52 支持GPU：从“运算后端固定为 CPU NumPy”到“同一图按设备一致性运行”，用独立计算、代码和故障注入验证。"
demo: true
math: true
sourceUrl: "https://www.oreilly.co.jp/books/9784873119069/"
qualityVersion: 2
practiceMode: code
sourceMode: independent-rewrite
draft: false
---


## 为什么这一步不能跳过

步骤52 支持GPU 的增量不是 API 名称，而是把 **运算后端固定为 CPU NumPy** 变为 **同一图按设备一致性运行**。它的核心机制是：cuda 模块抽象 NumPy/CuPy，Variable、Layer 与 DataLoader 迁移数组且同一次运算必须处于同一设备。

这一步验收时固定输入数组、随机种子、DeZero 前一步版本与运行设备。先写出预期的 forward、shape、creator 或状态变化，再执行实现；backward 后清空派生梯度并从相同输入重放。这样才能区分本步能力与缓存、旧图或随机性的偶然结果。


## 数学与状态合同

$$
xp=cuda.get\_array\_module(x)
$$

对 步骤52 支持GPU，公式中的每个量都要绑定到实际数组或对象：记录 dtype、shape、设备、creator/generation 或训练模式。若发生广播、归约、转置、时间递推或设备迁移，反向结果必须回到每个输入原有的语义边界。

## 目录单元到教学证据

### 第5阶段 DeZero高级挑战

在 **步骤52 支持GPU** 中，**第5阶段 DeZero高级挑战** 把“运算后端固定为 CPU NumPy”推进到“同一图按设备一致性运行”。本单元的可观察机制是：cuda 模块抽象 NumPy/CuPy，Variable、Layer 与 DataLoader 迁移数组且同一次运算必须处于同一设备。

核对 **第5阶段 DeZero高级挑战** 时先记录“固定版本”的输入、输出、shape、对象连接或运行模式，再注入反例“CPU ndarray 与 GPU CuPy 数组混算会报错或触发昂贵隐式传输”。如果最终数值看似合理但这一处首先分叉，实验必须停止并恢复基线。

### 步骤52 支持GPU

在 **步骤52 支持GPU** 中，**步骤52 支持GPU** 把“运算后端固定为 CPU NumPy”推进到“同一图按设备一致性运行”。本单元的可观察机制是：cuda 模块抽象 NumPy/CuPy，Variable、Layer 与 DataLoader 迁移数组且同一次运算必须处于同一设备。

核对 **步骤52 支持GPU** 时先记录“选择设备”的输入、输出、shape、对象连接或运行模式，再注入反例“CPU ndarray 与 GPU CuPy 数组混算会报错或触发昂贵隐式传输”。如果最终数值看似合理但这一处首先分叉，实验必须停止并恢复基线。

### 52.1 CuPy的安装和使用方法

在 **步骤52 支持GPU** 中，**52.1 CuPy的安装和使用方法** 把“运算后端固定为 CPU NumPy”推进到“同一图按设备一致性运行”。本单元的可观察机制是：cuda 模块抽象 NumPy/CuPy，Variable、Layer 与 DataLoader 迁移数组且同一次运算必须处于同一设备。

核对 **52.1 CuPy的安装和使用方法** 时先记录“迁移状态”的输入、输出、shape、对象连接或运行模式，再注入反例“CPU ndarray 与 GPU CuPy 数组混算会报错或触发昂贵隐式传输”。如果最终数值看似合理但这一处首先分叉，实验必须停止并恢复基线。

### 52.2 cuda模块

在 **步骤52 支持GPU** 中，**52.2 cuda模块** 把“运算后端固定为 CPU NumPy”推进到“同一图按设备一致性运行”。本单元的可观察机制是：cuda 模块抽象 NumPy/CuPy，Variable、Layer 与 DataLoader 迁移数组且同一次运算必须处于同一设备。

核对 **52.2 cuda模块** 时先记录“执行模式”的输入、输出、shape、对象连接或运行模式，再注入反例“CPU ndarray 与 GPU CuPy 数组混算会报错或触发昂贵隐式传输”。如果最终数值看似合理但这一处首先分叉，实验必须停止并恢复基线。

### 52.3 向Variable/Layer/DataLoader类添加代码

在 **步骤52 支持GPU** 中，**52.3 向Variable/Layer/DataLoader类添加代码** 把“运算后端固定为 CPU NumPy”推进到“同一图按设备一致性运行”。本单元的可观察机制是：cuda 模块抽象 NumPy/CuPy，Variable、Layer 与 DataLoader 迁移数组且同一次运算必须处于同一设备。

核对 **52.3 向Variable/Layer/DataLoader类添加代码** 时先记录“重放核对”的输入、输出、shape、对象连接或运行模式，再注入反例“CPU ndarray 与 GPU CuPy 数组混算会报错或触发昂贵隐式传输”。如果最终数值看似合理但这一处首先分叉，实验必须停止并恢复基线。

### 52.4 函数的相应修改

在 **步骤52 支持GPU** 中，**52.4 函数的相应修改** 把“运算后端固定为 CPU NumPy”推进到“同一图按设备一致性运行”。本单元的可观察机制是：cuda 模块抽象 NumPy/CuPy，Variable、Layer 与 DataLoader 迁移数组且同一次运算必须处于同一设备。

核对 **52.4 函数的相应修改** 时先记录“固定版本”的输入、输出、shape、对象连接或运行模式，再注入反例“CPU ndarray 与 GPU CuPy 数组混算会报错或触发昂贵隐式传输”。如果最终数值看似合理但这一处首先分叉，实验必须停止并恢复基线。

### 52.5 在GPU上训练MNIST

在 **步骤52 支持GPU** 中，**52.5 在GPU上训练MNIST** 把“运算后端固定为 CPU NumPy”推进到“同一图按设备一致性运行”。本单元的可观察机制是：cuda 模块抽象 NumPy/CuPy，Variable、Layer 与 DataLoader 迁移数组且同一次运算必须处于同一设备。

核对 **52.5 在GPU上训练MNIST** 时先记录“选择设备”的输入、输出、shape、对象连接或运行模式，再注入反例“CPU ndarray 与 GPU CuPy 数组混算会报错或触发昂贵隐式传输”。如果最终数值看似合理但这一处首先分叉，实验必须停止并恢复基线。

## 最小实现草图

```python
# 支持GPU：版本、设备与模式必须成套记录
x = to_selected_device(batch)
with runtime_mode(training=False):
    y = target(x)
assert same_device(x, y)
```

这段草图不复制原书实现；它只保留 步骤52 支持GPU 的可验证接口。实际练习要补齐输入构造、断言和失败样本，并用作者官方仓的对应 step 文件核对行为边界，而不是照抄代码行。

## 单变量因果实验

先预测改变一个输入后，固定版本 → 选择设备 → 迁移状态 → 执行模式 → 重放核对 中哪一项最先变化。操作基线、边界和故障三种场景；保存证据后点击“重置实验”，确认所有状态回到初始值。


## 故障诊断


## 本步小结

步骤52 支持GPU 的完成标准是：能从“运算后端固定为 CPU NumPy”解释到“同一图按设备一致性运行”，能用本页数学与状态合同算出或验证关键量，并能诊断“CPU ndarray 与 GPU CuPy 数组混算会报错或触发昂贵隐式传输”。只出现目录词、只跑通最终 loss 或只展示一张通用计算图都不算通过。

本步需要准确使用这些术语：、、、、。它们必须落到本页实验的数值、shape、对象连接或运行状态上。


