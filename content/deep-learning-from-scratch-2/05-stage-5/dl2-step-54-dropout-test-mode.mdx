---
title: "步骤54 Dropout和测试模式"
type: B
section: "第5阶段 · 高级挑战"
order: 54
description: "步骤54 Dropout和测试模式：从“训练与推理使用同一随机前向”到“test_mode 明确切换确定性推理”，用独立计算、代码和故障注入验证。"
demo: true
math: true
sourceUrl: "https://www.oreilly.co.jp/books/9784873119069/"
qualityVersion: 2
practiceMode: code
sourceMode: independent-rewrite
draft: false
---


## 为什么这一步不能跳过

步骤54 Dropout和测试模式 的增量不是 API 名称，而是把 **训练与推理使用同一随机前向** 变为 **test_mode 明确切换确定性推理**。它的核心机制是：inverted dropout 在训练时用掩码并除以保留率，测试模式直接返回输入以保持期望一致。

这一步验收时固定输入数组、随机种子、DeZero 前一步版本与运行设备。先写出预期的 forward、shape、creator 或状态变化，再执行实现；backward 后清空派生梯度并从相同输入重放。这样才能区分本步能力与缓存、旧图或随机性的偶然结果。


## 数学与状态合同

$$
y=\frac{m\odot x}{1-p},\quad m\sim Bernoulli(1-p)
$$

对 步骤54 Dropout和测试模式，公式中的每个量都要绑定到实际数组或对象：记录 dtype、shape、设备、creator/generation 或训练模式。若发生广播、归约、转置、时间递推或设备迁移，反向结果必须回到每个输入原有的语义边界。

## 目录单元到教学证据

### 步骤54 Dropout和测试模式

在 **步骤54 Dropout和测试模式** 中，**步骤54 Dropout和测试模式** 把“训练与推理使用同一随机前向”推进到“test_mode 明确切换确定性推理”。本单元的可观察机制是：inverted dropout 在训练时用掩码并除以保留率，测试模式直接返回输入以保持期望一致。

核对 **步骤54 Dropout和测试模式** 时先记录“固定版本”的输入、输出、shape、对象连接或运行模式，再注入反例“测试时仍随机丢弃或训练时不缩放都会造成输出分布漂移”。如果最终数值看似合理但这一处首先分叉，实验必须停止并恢复基线。

### 54.1 什么是Dropout

在 **步骤54 Dropout和测试模式** 中，**54.1 什么是Dropout** 把“训练与推理使用同一随机前向”推进到“test_mode 明确切换确定性推理”。本单元的可观察机制是：inverted dropout 在训练时用掩码并除以保留率，测试模式直接返回输入以保持期望一致。

核对 **54.1 什么是Dropout** 时先记录“选择设备”的输入、输出、shape、对象连接或运行模式，再注入反例“测试时仍随机丢弃或训练时不缩放都会造成输出分布漂移”。如果最终数值看似合理但这一处首先分叉，实验必须停止并恢复基线。

### 54.2 Inverted Dropout

在 **步骤54 Dropout和测试模式** 中，**54.2 Inverted Dropout** 把“训练与推理使用同一随机前向”推进到“test_mode 明确切换确定性推理”。本单元的可观察机制是：inverted dropout 在训练时用掩码并除以保留率，测试模式直接返回输入以保持期望一致。

核对 **54.2 Inverted Dropout** 时先记录“迁移状态”的输入、输出、shape、对象连接或运行模式，再注入反例“测试时仍随机丢弃或训练时不缩放都会造成输出分布漂移”。如果最终数值看似合理但这一处首先分叉，实验必须停止并恢复基线。

### 54.3 增加测试模式

在 **步骤54 Dropout和测试模式** 中，**54.3 增加测试模式** 把“训练与推理使用同一随机前向”推进到“test_mode 明确切换确定性推理”。本单元的可观察机制是：inverted dropout 在训练时用掩码并除以保留率，测试模式直接返回输入以保持期望一致。

核对 **54.3 增加测试模式** 时先记录“执行模式”的输入、输出、shape、对象连接或运行模式，再注入反例“测试时仍随机丢弃或训练时不缩放都会造成输出分布漂移”。如果最终数值看似合理但这一处首先分叉，实验必须停止并恢复基线。

### 54.4 Dropout的实现

在 **步骤54 Dropout和测试模式** 中，**54.4 Dropout的实现** 把“训练与推理使用同一随机前向”推进到“test_mode 明确切换确定性推理”。本单元的可观察机制是：inverted dropout 在训练时用掩码并除以保留率，测试模式直接返回输入以保持期望一致。

核对 **54.4 Dropout的实现** 时先记录“重放核对”的输入、输出、shape、对象连接或运行模式，再注入反例“测试时仍随机丢弃或训练时不缩放都会造成输出分布漂移”。如果最终数值看似合理但这一处首先分叉，实验必须停止并恢复基线。

## 最小实现草图

```python
# Dropout和测试模式：版本、设备与模式必须成套记录
x = to_selected_device(batch)
with runtime_mode(training=False):
    y = target(x)
assert same_device(x, y)
```

这段草图不复制原书实现；它只保留 步骤54 Dropout和测试模式 的可验证接口。实际练习要补齐输入构造、断言和失败样本，并用作者官方仓的对应 step 文件核对行为边界，而不是照抄代码行。

## 单变量因果实验

先预测改变一个输入后，固定版本 → 选择设备 → 迁移状态 → 执行模式 → 重放核对 中哪一项最先变化。操作基线、边界和故障三种场景；保存证据后点击“重置实验”，确认所有状态回到初始值。


## 故障诊断


## 本步小结

步骤54 Dropout和测试模式 的完成标准是：能从“训练与推理使用同一随机前向”解释到“test_mode 明确切换确定性推理”，能用本页数学与状态合同算出或验证关键量，并能诊断“测试时仍随机丢弃或训练时不缩放都会造成输出分布漂移”。只出现目录词、只跑通最终 loss 或只展示一张通用计算图都不算通过。

本步需要准确使用这些术语：、、、、。它们必须落到本页实验的数值、shape、对象连接或运行状态上。


