---
title: "步骤56 CNN的机制（2）"
type: B
section: "第5阶段 · 高级挑战"
order: 56
description: "步骤56 CNN的机制（2）：从“二维单通道示意”到“批量多通道卷积与池化语义完整”，用独立计算、代码和故障注入验证。"
demo: true
math: true
sourceUrl: "https://www.oreilly.co.jp/books/9784873119069/"
qualityVersion: 2
practiceMode: calculation
sourceMode: independent-rewrite
draft: false
---


## 为什么这一步不能跳过

步骤56 CNN的机制（2） 的增量不是 API 名称，而是把 **二维单通道示意** 变为 **批量多通道卷积与池化语义完整**。它的核心机制是：CNN 张量按 NCHW 组织，卷积核连接输入/输出通道，池化在局部窗口归约且通常无参数。

这一步验收时固定输入数组、随机种子、DeZero 前一步版本与运行设备。先写出预期的 forward、shape、creator 或状态变化，再执行实现；backward 后清空派生梯度并从相同输入重放。这样才能区分本步能力与缓存、旧图或随机性的偶然结果。


## 数学与状态合同

$$
X\in\mathbb{R}^{N\times C\times H\times W}
$$

对 步骤56 CNN的机制（2），公式中的每个量都要绑定到实际数组或对象：记录 dtype、shape、设备、creator/generation 或训练模式。若发生广播、归约、转置、时间递推或设备迁移，反向结果必须回到每个输入原有的语义边界。

## 目录单元到教学证据

### 步骤56 CNN的机制（2）

在 **步骤56 CNN的机制（2）** 中，**步骤56 CNN的机制（2）** 把“二维单通道示意”推进到“批量多通道卷积与池化语义完整”。本单元的可观察机制是：CNN 张量按 NCHW 组织，卷积核连接输入/输出通道，池化在局部窗口归约且通常无参数。

核对 **步骤56 CNN的机制（2）** 时先记录“输入NCHW”的输入、输出、shape、对象连接或运行模式，再注入反例“混用 NHWC/NCHW 或漏掉 batch/channel 轴会让卷积结果静默错位”。如果最终数值看似合理但这一处首先分叉，实验必须停止并恢复基线。

### 56.1 三阶张量

在 **步骤56 CNN的机制（2）** 中，**56.1 三阶张量** 把“二维单通道示意”推进到“批量多通道卷积与池化语义完整”。本单元的可观察机制是：CNN 张量按 NCHW 组织，卷积核连接输入/输出通道，池化在局部窗口归约且通常无参数。

核对 **56.1 三阶张量** 时先记录“核与步幅”的输入、输出、shape、对象连接或运行模式，再注入反例“混用 NHWC/NCHW 或漏掉 batch/channel 轴会让卷积结果静默错位”。如果最终数值看似合理但这一处首先分叉，实验必须停止并恢复基线。

### 56.2 结合方块进行思考

在 **步骤56 CNN的机制（2）** 中，**56.2 结合方块进行思考** 把“二维单通道示意”推进到“批量多通道卷积与池化语义完整”。本单元的可观察机制是：CNN 张量按 NCHW 组织，卷积核连接输入/输出通道，池化在局部窗口归约且通常无参数。

核对 **56.2 结合方块进行思考** 时先记录“窗口展开”的输入、输出、shape、对象连接或运行模式，再注入反例“混用 NHWC/NCHW 或漏掉 batch/channel 轴会让卷积结果静默错位”。如果最终数值看似合理但这一处首先分叉，实验必须停止并恢复基线。

### 56.3 小批量处理

在 **步骤56 CNN的机制（2）** 中，**56.3 小批量处理** 把“二维单通道示意”推进到“批量多通道卷积与池化语义完整”。本单元的可观察机制是：CNN 张量按 NCHW 组织，卷积核连接输入/输出通道，池化在局部窗口归约且通常无参数。

核对 **56.3 小批量处理** 时先记录“输出特征”的输入、输出、shape、对象连接或运行模式，再注入反例“混用 NHWC/NCHW 或漏掉 batch/channel 轴会让卷积结果静默错位”。如果最终数值看似合理但这一处首先分叉，实验必须停止并恢复基线。

### 56.4 池化层

在 **步骤56 CNN的机制（2）** 中，**56.4 池化层** 把“二维单通道示意”推进到“批量多通道卷积与池化语义完整”。本单元的可观察机制是：CNN 张量按 NCHW 组织，卷积核连接输入/输出通道，池化在局部窗口归约且通常无参数。

核对 **56.4 池化层** 时先记录“反向散射”的输入、输出、shape、对象连接或运行模式，再注入反例“混用 NHWC/NCHW 或漏掉 batch/channel 轴会让卷积结果静默错位”。如果最终数值看似合理但这一处首先分叉，实验必须停止并恢复基线。

## 最小实现草图

```python
# CNN的机制（2）：先算输出尺寸，再执行空间运算
out_h = (in_h + 2 * pad - kernel) // stride + 1
y = spatial_op(x, kernel=kernel, stride=stride, pad=pad)
assert y.shape[-2] == out_h
```

这段草图不复制原书实现；它只保留 步骤56 CNN的机制（2） 的可验证接口。实际练习要补齐输入构造、断言和失败样本，并用作者官方仓的对应 step 文件核对行为边界，而不是照抄代码行。

## 单变量因果实验

先预测改变一个输入后，输入NCHW → 核与步幅 → 窗口展开 → 输出特征 → 反向散射 中哪一项最先变化。操作基线、边界和故障三种场景；保存证据后点击“重置实验”，确认所有状态回到初始值。


## 故障诊断


## 本步小结

步骤56 CNN的机制（2） 的完成标准是：能从“二维单通道示意”解释到“批量多通道卷积与池化语义完整”，能用本页数学与状态合同算出或验证关键量，并能诊断“混用 NHWC/NCHW 或漏掉 batch/channel 轴会让卷积结果静默错位”。只出现目录词、只跑通最终 loss 或只展示一张通用计算图都不算通过。

本步需要准确使用这些术语：、、、、。它们必须落到本页实验的数值、shape、对象连接或运行状态上。


