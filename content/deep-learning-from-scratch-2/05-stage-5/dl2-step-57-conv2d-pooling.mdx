---
title: "步骤57 conv2d函数和pooling函数"
type: B
section: "第5阶段 · 高级挑战"
order: 57
description: "步骤57 conv2d函数和pooling函数：从“只有卷积几何理论”到“conv2d/Conv2d/pooling 成为可微函数”，用独立计算、代码和故障注入验证。"
demo: true
math: true
sourceUrl: "https://www.oreilly.co.jp/books/9784873119069/"
qualityVersion: 2
practiceMode: calculation
sourceMode: independent-rewrite
draft: false
---


## 为什么这一步不能跳过

步骤57 conv2d函数和pooling函数 的增量不是 API 名称，而是把 **只有卷积几何理论** 变为 **conv2d/Conv2d/pooling 成为可微函数**。它的核心机制是：im2col 把滑动窗口展开为矩阵，conv2d 转成矩阵乘法；pooling backward 按 argmax 散射梯度。

这一步验收时固定输入数组、随机种子、DeZero 前一步版本与运行设备。先写出预期的 forward、shape、creator 或状态变化，再执行实现；backward 后清空派生梯度并从相同输入重放。这样才能区分本步能力与缓存、旧图或随机性的偶然结果。


## 数学与状态合同

$$
Y_{col}=X_{col}W_{col}
$$

对 步骤57 conv2d函数和pooling函数，公式中的每个量都要绑定到实际数组或对象：记录 dtype、shape、设备、creator/generation 或训练模式。若发生广播、归约、转置、时间递推或设备迁移，反向结果必须回到每个输入原有的语义边界。

## 目录单元到教学证据

### 步骤57 conv2d函数和pooling函数

在 **步骤57 conv2d函数和pooling函数** 中，**步骤57 conv2d函数和pooling函数** 把“只有卷积几何理论”推进到“conv2d/Conv2d/pooling 成为可微函数”。本单元的可观察机制是：im2col 把滑动窗口展开为矩阵，conv2d 转成矩阵乘法；pooling backward 按 argmax 散射梯度。

核对 **步骤57 conv2d函数和pooling函数** 时先记录“输入NCHW”的输入、输出、shape、对象连接或运行模式，再注入反例“col2im 重叠区域不累加或 pooling 未保存 argmax 会得到错误输入梯度”。如果最终数值看似合理但这一处首先分叉，实验必须停止并恢复基线。

### 57.1 使用im2col展开

在 **步骤57 conv2d函数和pooling函数** 中，**57.1 使用im2col展开** 把“只有卷积几何理论”推进到“conv2d/Conv2d/pooling 成为可微函数”。本单元的可观察机制是：im2col 把滑动窗口展开为矩阵，conv2d 转成矩阵乘法；pooling backward 按 argmax 散射梯度。

核对 **57.1 使用im2col展开** 时先记录“核与步幅”的输入、输出、shape、对象连接或运行模式，再注入反例“col2im 重叠区域不累加或 pooling 未保存 argmax 会得到错误输入梯度”。如果最终数值看似合理但这一处首先分叉，实验必须停止并恢复基线。

### 57.2 conv2d函数的实现

在 **步骤57 conv2d函数和pooling函数** 中，**57.2 conv2d函数的实现** 把“只有卷积几何理论”推进到“conv2d/Conv2d/pooling 成为可微函数”。本单元的可观察机制是：im2col 把滑动窗口展开为矩阵，conv2d 转成矩阵乘法；pooling backward 按 argmax 散射梯度。

核对 **57.2 conv2d函数的实现** 时先记录“窗口展开”的输入、输出、shape、对象连接或运行模式，再注入反例“col2im 重叠区域不累加或 pooling 未保存 argmax 会得到错误输入梯度”。如果最终数值看似合理但这一处首先分叉，实验必须停止并恢复基线。

### 57.3 Conv2d层的实现

在 **步骤57 conv2d函数和pooling函数** 中，**57.3 Conv2d层的实现** 把“只有卷积几何理论”推进到“conv2d/Conv2d/pooling 成为可微函数”。本单元的可观察机制是：im2col 把滑动窗口展开为矩阵，conv2d 转成矩阵乘法；pooling backward 按 argmax 散射梯度。

核对 **57.3 Conv2d层的实现** 时先记录“输出特征”的输入、输出、shape、对象连接或运行模式，再注入反例“col2im 重叠区域不累加或 pooling 未保存 argmax 会得到错误输入梯度”。如果最终数值看似合理但这一处首先分叉，实验必须停止并恢复基线。

### 57.4 pooling函数的实现

在 **步骤57 conv2d函数和pooling函数** 中，**57.4 pooling函数的实现** 把“只有卷积几何理论”推进到“conv2d/Conv2d/pooling 成为可微函数”。本单元的可观察机制是：im2col 把滑动窗口展开为矩阵，conv2d 转成矩阵乘法；pooling backward 按 argmax 散射梯度。

核对 **57.4 pooling函数的实现** 时先记录“反向散射”的输入、输出、shape、对象连接或运行模式，再注入反例“col2im 重叠区域不累加或 pooling 未保存 argmax 会得到错误输入梯度”。如果最终数值看似合理但这一处首先分叉，实验必须停止并恢复基线。

## 最小实现草图

```python
# conv2d函数和pooling函数：先算输出尺寸，再执行空间运算
out_h = (in_h + 2 * pad - kernel) // stride + 1
y = spatial_op(x, kernel=kernel, stride=stride, pad=pad)
assert y.shape[-2] == out_h
```

这段草图不复制原书实现；它只保留 步骤57 conv2d函数和pooling函数 的可验证接口。实际练习要补齐输入构造、断言和失败样本，并用作者官方仓的对应 step 文件核对行为边界，而不是照抄代码行。

## 单变量因果实验

先预测改变一个输入后，输入NCHW → 核与步幅 → 窗口展开 → 输出特征 → 反向散射 中哪一项最先变化。操作基线、边界和故障三种场景；保存证据后点击“重置实验”，确认所有状态回到初始值。


## 故障诊断


## 本步小结

步骤57 conv2d函数和pooling函数 的完成标准是：能从“只有卷积几何理论”解释到“conv2d/Conv2d/pooling 成为可微函数”，能用本页数学与状态合同算出或验证关键量，并能诊断“col2im 重叠区域不累加或 pooling 未保存 argmax 会得到错误输入梯度”。只出现目录词、只跑通最终 loss 或只展示一张通用计算图都不算通过。

本步需要准确使用这些术语：、、、、。它们必须落到本页实验的数值、shape、对象连接或运行状态上。


