---
title: "步骤59 使用RNN处理时间序列数据"
type: B
section: "第5阶段 · 高级挑战"
order: 59
description: "步骤59 使用RNN处理时间序列数据：从“样本之间没有持久状态”到“隐藏状态表达时间依赖并可显式截断”，用独立计算、代码和故障注入验证。"
demo: true
math: true
sourceUrl: "https://www.oreilly.co.jp/books/9784873119069/"
qualityVersion: 2
practiceMode: code
sourceMode: independent-rewrite
draft: true
---

import { Dl2Step59RnnLab } from "@/components/mdx/deep-learning-from-scratch-2/diagrams/dl2-step-59-rnn";
import {
  Objectives,
  Callout,
  Term,
  Glossary,
  GlossaryItem,
  Exercises,
  Answer,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能说明 步骤59 使用RNN处理时间序列数据 相对前一步新增的责任：隐藏状态表达时间依赖并可显式截断
- 能依据本页数学与状态合同完成推导或验证，并写清输入、输出与 shape
- 能运行本页最小代码草图，观察 时间批次、输入投影、状态递推、截断反传、序列预测 的因果链
- 能注入“epoch 或独立序列之间不 reset_state 会把无关样本串成同一时间线”并在首个错误证据处停止

</Objectives>

## 为什么这一步不能跳过

步骤59 使用RNN处理时间序列数据 的增量不是 API 名称，而是把 **样本之间没有持久状态** 变为 **隐藏状态表达时间依赖并可显式截断**。它的核心机制是：RNN Layer 保存上一时刻 h，时间展开形成跨步计算图；截断反传时 unchain_backward 切断旧历史。

这一步验收时固定输入数组、随机种子、DeZero 前一步版本与运行设备。先写出预期的 forward、shape、creator 或状态变化，再执行实现；backward 后清空派生梯度并从相同输入重放。这样才能区分本步能力与缓存、旧图或随机性的偶然结果。

<Callout type="info" title="本步硬合同">
  前向证据、反向证据和重置后重放必须同时一致。epoch 或独立序列之间不 reset_state
  会把无关样本串成同一时间线 时，即使最终 loss 或输出合理，也应判定 步骤59
  使用RNN处理时间序列数据 失败。
</Callout>

## 数学与状态合同

$$
h_t=\tanh(x_tW_x+h_{t-1}W_h+b)
$$

对 步骤59 使用RNN处理时间序列数据，公式中的每个量都要绑定到实际数组或对象：记录 dtype、shape、设备、creator/generation 或训练模式。若发生广播、归约、转置、时间递推或设备迁移，反向结果必须回到每个输入原有的语义边界。

## 目录单元到教学证据

### 步骤59 使用RNN处理时间序列数据

在 **步骤59 使用RNN处理时间序列数据** 中，**步骤59 使用RNN处理时间序列数据** 把“样本之间没有持久状态”推进到“隐藏状态表达时间依赖并可显式截断”。本单元的可观察机制是：RNN Layer 保存上一时刻 h，时间展开形成跨步计算图；截断反传时 unchain_backward 切断旧历史。

核对 **步骤59 使用RNN处理时间序列数据** 时先记录“时间批次”的输入、输出、shape、对象连接或运行模式，再注入反例“epoch 或独立序列之间不 reset_state 会把无关样本串成同一时间线”。如果最终数值看似合理但这一处首先分叉，实验必须停止并恢复基线。

### 59.1 RNN层的实现

在 **步骤59 使用RNN处理时间序列数据** 中，**59.1 RNN层的实现** 把“样本之间没有持久状态”推进到“隐藏状态表达时间依赖并可显式截断”。本单元的可观察机制是：RNN Layer 保存上一时刻 h，时间展开形成跨步计算图；截断反传时 unchain_backward 切断旧历史。

核对 **59.1 RNN层的实现** 时先记录“输入投影”的输入、输出、shape、对象连接或运行模式，再注入反例“epoch 或独立序列之间不 reset_state 会把无关样本串成同一时间线”。如果最终数值看似合理但这一处首先分叉，实验必须停止并恢复基线。

### 59.2 RNN模型的实现

在 **步骤59 使用RNN处理时间序列数据** 中，**59.2 RNN模型的实现** 把“样本之间没有持久状态”推进到“隐藏状态表达时间依赖并可显式截断”。本单元的可观察机制是：RNN Layer 保存上一时刻 h，时间展开形成跨步计算图；截断反传时 unchain_backward 切断旧历史。

核对 **59.2 RNN模型的实现** 时先记录“状态递推”的输入、输出、shape、对象连接或运行模式，再注入反例“epoch 或独立序列之间不 reset_state 会把无关样本串成同一时间线”。如果最终数值看似合理但这一处首先分叉，实验必须停止并恢复基线。

### 59.3 切断连接的方法

在 **步骤59 使用RNN处理时间序列数据** 中，**59.3 切断连接的方法** 把“样本之间没有持久状态”推进到“隐藏状态表达时间依赖并可显式截断”。本单元的可观察机制是：RNN Layer 保存上一时刻 h，时间展开形成跨步计算图；截断反传时 unchain_backward 切断旧历史。

核对 **59.3 切断连接的方法** 时先记录“截断反传”的输入、输出、shape、对象连接或运行模式，再注入反例“epoch 或独立序列之间不 reset_state 会把无关样本串成同一时间线”。如果最终数值看似合理但这一处首先分叉，实验必须停止并恢复基线。

### 59.4 正弦波的预测

在 **步骤59 使用RNN处理时间序列数据** 中，**59.4 正弦波的预测** 把“样本之间没有持久状态”推进到“隐藏状态表达时间依赖并可显式截断”。本单元的可观察机制是：RNN Layer 保存上一时刻 h，时间展开形成跨步计算图；截断反传时 unchain_backward 切断旧历史。

核对 **59.4 正弦波的预测** 时先记录“序列预测”的输入、输出、shape、对象连接或运行模式，再注入反例“epoch 或独立序列之间不 reset_state 会把无关样本串成同一时间线”。如果最终数值看似合理但这一处首先分叉，实验必须停止并恢复基线。

## 最小实现草图

```python
# 使用RNN处理时间序列数据：序列边界决定何时保留或重置状态
model.reset_state()
for x_t, t_t in sequence_loader:
    loss = loss + objective(model(x_t), t_t)
loss.backward()
model.unchain_backward()
```

这段草图不复制原书实现；它只保留 步骤59 使用RNN处理时间序列数据 的可验证接口。实际练习要补齐输入构造、断言和失败样本，并用作者官方仓的对应 step 文件核对行为边界，而不是照抄代码行。

## 单变量因果实验

先预测改变一个输入后，时间批次 → 输入投影 → 状态递推 → 截断反传 → 序列预测 中哪一项最先变化。操作基线、边界和故障三种场景；保存证据后点击“重置实验”，确认所有状态回到初始值。

<Dl2Step59RnnLab />

## 故障诊断

<Callout
  type="trap"
  title="误区：epoch 或独立序列之间不 reset_state 会把无关样本串成同一时间线"
>
  这会破坏 步骤59 使用RNN处理时间序列数据
  的“隐藏状态表达时间依赖并可显式截断”。先检查 时间批次，再沿
  输入投影、状态递推、截断反传、序列预测
  顺序定位第一处分叉；不要用最终输出掩盖局部错误。
</Callout>

<Callout type="tip" title="恢复顺序">
  恢复固定输入与模式 → 清空 grad/缓存/隐藏状态 → 重建当前计算图 → 重跑 forward →
  重跑 backward → 与独立公式或 shape 断言比较。
</Callout>

## 本步小结

步骤59 使用RNN处理时间序列数据 的完成标准是：能从“样本之间没有持久状态”解释到“隐藏状态表达时间依赖并可显式截断”，能用本页数学与状态合同算出或验证关键量，并能诊断“epoch 或独立序列之间不 reset_state 会把无关样本串成同一时间线”。只出现目录词、只跑通最终 loss 或只展示一张通用计算图都不算通过。

本步需要准确使用这些术语：<Term def="在 步骤59 使用RNN处理时间序列数据 中对应“时间批次”的可验证对象或状态">步骤59 使用RNN处理时间序列数据</Term>、<Term def="在 步骤59 使用RNN处理时间序列数据 中对应“输入投影”的可验证对象或状态">59.1 RNN层的实现</Term>、<Term def="在 步骤59 使用RNN处理时间序列数据 中对应“状态递推”的可验证对象或状态">59.2 RNN模型的实现</Term>、<Term def="在 步骤59 使用RNN处理时间序列数据 中对应“截断反传”的可验证对象或状态">59.3 切断连接的方法</Term>、<Term def="在 步骤59 使用RNN处理时间序列数据 中对应“序列预测”的可验证对象或状态">59.4 正弦波的预测</Term>。它们必须落到本页实验的数值、shape、对象连接或运行状态上。

<Glossary>

<GlossaryItem term="步骤59 使用RNN处理时间序列数据">
  在 步骤59 使用RNN处理时间序列数据 中，步骤59 使用RNN处理时间序列数据
  对应“时间批次”；验收时必须给出实际数值、shape 或对象状态，不能只复述名称。
</GlossaryItem>

<GlossaryItem term="59.1 RNN层的实现">
  在 步骤59 使用RNN处理时间序列数据 中，59.1 RNN层的实现
  对应“输入投影”；验收时必须给出实际数值、shape 或对象状态，不能只复述名称。
</GlossaryItem>

<GlossaryItem term="59.2 RNN模型的实现">
  在 步骤59 使用RNN处理时间序列数据 中，59.2 RNN模型的实现
  对应“状态递推”；验收时必须给出实际数值、shape 或对象状态，不能只复述名称。
</GlossaryItem>

<GlossaryItem term="59.3 切断连接的方法">
  在 步骤59 使用RNN处理时间序列数据 中，59.3 切断连接的方法
  对应“截断反传”；验收时必须给出实际数值、shape 或对象状态，不能只复述名称。
</GlossaryItem>

<GlossaryItem term="59.4 正弦波的预测">
  在 步骤59 使用RNN处理时间序列数据 中，59.4 正弦波的预测
  对应“序列预测”；验收时必须给出实际数值、shape 或对象状态，不能只复述名称。
</GlossaryItem>

</Glossary>

<Exercises>

1. 步骤59 使用RNN处理时间序列数据 相对前一步改变了哪个合同？请用一个最小输入指出最先变化的证据。

<Answer>
  它把“样本之间没有持久状态”推进为“隐藏状态表达时间依赖并可显式截断”。固定输入后先检查 时间批次，再核对 输入投影 与 状态递推；机制应符合“RNN Layer 保存上一时刻 h，时间展开形成跨步计算图；截断反传时 unchain_backward 切断旧历史”。

目录单元逐项验收：步骤59 使用RNN处理时间序列数据（核对 时间批次）；59.1 RNN层的实现（核对 输入投影）；59.2 RNN模型的实现（核对 状态递推）；59.3 切断连接的方法（核对 截断反传）；59.4 正弦波的预测（核对 序列预测）。每项都要写下预期、实测与复位后的重放证据。

</Answer>

2. 使用本页数学与状态合同做一次手算或 shape 推演，并说明代码中对应的数组或对象。

<Answer>
  先给公式中的输入标注数值、shape 与设备，得到预期结果后再运行最小草图。步骤59
  使用RNN处理时间序列数据 的 forward 结果和
  backward/状态证据必须同时匹配，不能只比较一个最终标量。
</Answer>

3. 注入“epoch 或独立序列之间不 reset_state 会把无关样本串成同一时间线”后，如何证明重置真的恢复了实验？

<Answer>
  保存故障前的输入、时间批次
  和最终状态；注入故障定位首错后执行本页恢复顺序。重置后的
  forward、梯度/shape、模式与证据轨迹都应回到基线，随后再次运行得到相同结果。
</Answer>

</Exercises>

<Attribution />
