---
title: "附录C 在Google Colaboratory上运行"
type: B
section: "附录"
order: 63
description: "附录C 在Google Colaboratory上运行覆盖1个权威目录层级，以可执行框架合同、数学推导、交互实验和失败对照重建核心。"
demo: true
math: true
sourceUrl: "https://www.oreilly.co.jp/books/9784873119069/toc.html"
draft: false
---

import {
  Dl2AppendixCColabMapLab,
  Dl2AppendixCColabExperimentLab,
  Dl2AppendixCColabEvidenceLab,
} from "@/components/mdx/deep-learning-from-scratch-2/diagrams/dl2-appendix-c-colab";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释附录C 在Google Colaboratory上运行新增的接口、对象关系、数学规则和适用边界
- 能计算四个核心方程中的forward值、shape、局部导数或参数更新
- 能复现三个互动视图，并定位第一条图连接、数值、梯度或模式证据分叉
- 能判断“云端环境的版本、设备、路径和数据可从干净运行时完整恢复。”是否成立

</Objectives>

## 为什么先冻结框架合同

<Term def="Colaboratory是本页第1个核查坐标，用于连接框架接口、数值状态、计算图和失败边界。">
  Colaboratory
</Term>
、
<Term def="运行时是本页第2个核查坐标，用于连接框架接口、数值状态、计算图和失败边界。">
  运行时
</Term>
、
<Term def="依赖是本页第3个核查坐标，用于连接框架接口、数值状态、计算图和失败边界。">
  依赖
</Term>
、
<Term def="GPU是本页第4个核查坐标，用于连接框架接口、数值状态、计算图和失败边界。">
  GPU
</Term>
、
<Term def="路径是本页第5个核查坐标，用于连接框架接口、数值状态、计算图和失败边界。">
  路径
</Term>
、
<Term def="复现是本页第6个核查坐标，用于连接框架接口、数值状态、计算图和失败边界。">
  复现
</Term>
共同构成本页坐标。附录C 在Google
Colaboratory上运行补足主线实现边界，并用最小失败样本验证状态、shape与反向语义。

自制框架的难点不是写出一个能返回数字的函数，而是让自然Python表达式、动态图、反向传播、内存和模式在同一合同下工作。实验先冻结Python/NumPy版本、输入与参数数组、对象身份、随机种子和上一版测试，再预测新能力应最先改变的中间量。

每次forward保存输出值、shape、dtype、creator和generation；每次backward保存上游梯度、局部导数、输入梯度和参数梯度。若最终损失下降但对象连接、梯度累加或模式切换错误，训练结果不能采信。修复后清空grad、图、优化器与循环状态，从原始输入完整重放。

## 数学骨架

$$
H_o=\left\lfloor\frac{H+2P-F}{S}\right\rfloor+1
$$

$$
y=\frac{m\odot x}{1-p}
$$

$$
h_t=\tanh(x_tW_x+h_{t-1}W_h+b)
$$

$$
c_t=f_t\odot c_{t-1}+i_t\odot g_t
$$

方程进入代码前先声明输入输出shape、轴和对象所有权。解析梯度与中心差分比较相对误差，参数更新后用新forward独立重算；高阶、GPU或时序模式还要记录是否建图、设备和状态边界。

## 核心机制

1. Colab环境记录运行时、依赖版本、GPU类型和工作目录，安装后从干净内核导入DeZero。
2. 挂载数据和保存产物路径显式，断开运行时后仍能按清单恢复。
3. GPU支持通过统一cuda模块选择NumPy或CuPy，Variable、Layer、DataLoader和函数必须在同一设备上工作。设备转换要显式，不能隐式复制掩盖成本。
4. 模型保存先把参数递归扁平化，以稳定键名写出ndarray；加载时核对键集合、shape和dtype。往返后同输入应得到相同预测。
5. 训练/测试模式影响Dropout等随机层。Inverted Dropout在训练时按保留率缩放，推理直接通过；模式必须由上下文显式控制。
6. CNN处理空间局部结构，RNN/LSTM处理时间状态。两者都要固定批次与特征轴，保存窗口或时间步状态，并明确何处切断反向连接。

## DeZero实现合同

1. **对象合同：** Variable只承载数据、梯度与图连接，Function负责数值变换与局部导数；便利API最终委托核心函数，不能绕过creator设置。
2. **shape合同：** forward与backward逐轴对称。前向广播、求和、转置或reshape后，反向必须恢复每个输入的原shape，并对重复路径累加。
3. **顺序合同：** 动态图按实际运行创建，反向按generation拓扑逆序；同一函数只处理一次，同一变量可从多条路径接收贡献。
4. **模式合同：** no_grad、create_graph、train/test与设备上下文使用成对进入和恢复，异常退出也不能污染全局状态。
5. **资源合同：** 不需要的中间梯度及时释放，weakref打破循环引用；保存加载、CPU/GPU和DataLoader都保留稳定键、dtype与样本身份。
6. **测试合同：** 每个新函数至少有forward例、解析backward、数值梯度、shape边界和失败样本；上一版本测试必须继续通过。

## 从干净运行时验证可复现性

Colab 的临时运行时会在断开后丢失内存、已安装包和本地文件，因此“当前单元格能跑”不等于环境可恢复。验收从全新运行时开始，依次记录 Python、NumPy/CuPy、CUDA 与 GPU 型号，安装固定版本的依赖，导入打包后的 DeZero，再运行 CPU 基线、自动微分梯度检查和一个极小训练循环。每一步输出版本、设备、随机种子和工作目录，避免依赖旧会话中的隐式状态。

数据、权重与图像产物分为临时路径和持久路径。挂载云盘后要核对文件哈希、参数键、shape与dtype；保存模型后重启运行时并重新加载，用同一固定输入比较预测和参数摘要。GPU不可用时应明确降级到CPU而不是静默跳过测试，CPU/GPU都可用时则在合理浮点容差内比较forward与梯度。这样才能证明笔记本既可演示，也可从零重放。

最终把安装、测试、训练和保存拆成可独立重跑的单元格，并从上到下执行一次“全部运行”。任何依赖乱序执行才存在的变量、挂载路径或缓存都视为失败，修复后重新启动运行时复验。

复验结果还应附带完整环境摘要。

## 权威目录逐项讲解

### 附录C 在Google Colaboratory上运行

Colab环境记录运行时、依赖版本、GPU类型和工作目录，安装后从干净内核导入DeZero。 挂载数据和保存产物路径显式，断开运行时后仍能按清单恢复。 验收时保存输入、输出、creator或状态、局部梯度和失败边界，并核对“云端环境的版本、设备、路径和数据可从干净运行时完整恢复。”是否成立。

先预测该单元改变后最早变化的是对象连接、forward值、shape、generation、内存、梯度还是模式，再运行固定小样本。若末端结果合理但首个证据没有出现，应停止并从上一个通过测试的DeZero版本重放。

## 三步交互实验

先预测单变量变化会最早影响对象图、forward、shape、梯度还是独立测试，再切换基线、边界和失败模式。

<Stepper>
  <Step title="画出对象与计算图">
    标出Variable、Function、creator、参数、数据或循环状态的所有权与连接。
    <Dl2AppendixCColabMapLab />
  </Step>
  <Step title="运行单变量实验">
    固定输入、参数和上一版代码，只改变一个接口、shape、模式或设备条件。
    <Dl2AppendixCColabExperimentLab />
  </Step>
  <Step title="核对独立证据">
    比较forward、解析/数值梯度、对象存活、更新与重放测试，定位第一处分叉。
    <Dl2AppendixCColabEvidenceLab />
  </Step>
</Stepper>

## 常见失败与边界

<Callout type="trap">
  **便利接口绕过核心图：**
  运算结果数值正确，但输出未设置creator，后续backward在中途停止。所有运算符和包装函数必须委托Function调用。
</Callout>

<Callout type="trap">
  **只看训练终点：**
  错误广播、梯度覆盖、旧缓存、错误模式或测试泄漏可能暂时得到较低损失；必须核对最早中间证据。
</Callout>

<Callout type="tip">
  **最小重放顺序：**
  在标量或极小数组上先手算forward与局部导数，再做中心差分、组合图和长训练。首错出现后清空全部派生状态。
</Callout>

## 本章回顾

附录C 在Google Colaboratory上运行补足主线实现边界，并用最小失败样本验证状态、shape与反向语义。 验收底线是：云端环境的版本、设备、路径和数据可从干净运行时完整恢复。。完成标准是能预测首个中间变化、重放实现、解释失败样本，并证明新能力没有破坏此前测试。

<Glossary>

<GlossaryItem term="Colaboratory">
  “Colaboratory”对应“冻结输入与版本”：记录对象身份、shape、前向缓存、局部梯度、模式和数值容差；仅有最终输出不足以证明框架正确。
</GlossaryItem>

<GlossaryItem term="运行时">
  “运行时”对应“执行forward”：记录对象身份、shape、前向缓存、局部梯度、模式和数值容差；仅有最终输出不足以证明框架正确。
</GlossaryItem>

<GlossaryItem term="依赖">
  “依赖”对应“保存图与shape”：记录对象身份、shape、前向缓存、局部梯度、模式和数值容差；仅有最终输出不足以证明框架正确。
</GlossaryItem>

<GlossaryItem term="GPU">
  “GPU”对应“执行backward”：记录对象身份、shape、前向缓存、局部梯度、模式和数值容差；仅有最终输出不足以证明框架正确。
</GlossaryItem>

<GlossaryItem term="路径">
  “路径”对应“核对数值梯度”：记录对象身份、shape、前向缓存、局部梯度、模式和数值容差；仅有最终输出不足以证明框架正确。
</GlossaryItem>

<GlossaryItem term="复现">
  “复现”对应“独立重放测试”：记录对象身份、shape、前向缓存、局部梯度、模式和数值容差；仅有最终输出不足以证明框架正确。
</GlossaryItem>

</Glossary>

<Exercises>

1. 如何为附录C 在Google Colaboratory上运行建立最小可手算的forward/backward测试？

<Answer>
固定一个标量或极小数组、对象身份和上一版实现，按“冻结输入与版本 -> 执行forward -> 保存图与shape -> 执行backward -> 核对数值梯度 -> 独立重放测试”保存中间证据。手算四个方程，并用中心差分比较解析梯度；shape、creator、generation和模式都应符合合同。
</Answer>

2. 如何证明新便利接口没有绕过DeZero计算图或改变梯度？

<Answer>
  让核心函数与便利写法共享同一输入，逐项比较输出、creator链、generation、解析梯度和数值梯度。两条路径必须在容差内一致，并继续通过所有旧测试。
</Answer>

3. 为“云端环境的版本、设备、路径和数据可从干净运行时完整恢复。”构造失败样本并指出停止位置。

<Answer>
  可以故意断开creator、覆盖梯度、交换shape轴、复用旧图、忘记恢复模式或混用CPU/GPU。运行前预测第一条被破坏的证据，在该处停止，清空grad、缓存、优化器和状态后重放。
</Answer>

</Exercises>

<Attribution
  adaptedFrom="斋藤康毅著、郑明智译《深度学习入门2：自制框架》，人民邮电出版社，2023年3月，ISBN 9787115607515；原版《ゼロから作るDeep Learning ❸》，O'Reilly Japan，2020年4月20日，552页，ISBN 9784873119069 · 基于官方5阶段、60步骤、207个编号小节与3个附录完整目录的独立教学重构"
  adaptedUrl="https://www.oreilly.co.jp/books/9784873119069/toc.html"
/>
