---
title: "训练与评估"
type: C
section: "人工智能与机器学习"
order: 8
description: "损失函数、数据集、训练循环与精度评估的完整实现"
demo: true
math: false
sourceUrl: ""
draft: false
---

import { Dl2TrainingEvaluationDiagram } from "@/components/mdx/deep-learning-from-scratch-2/diagrams/dl2-training-evaluation";
import { Objectives, Callout, Glossary, GlossaryItem, Term, Exercises, Answer } from "@/components/mdx/mdx-components";

<Objectives>

- 掌握损失函数的实现：均方误差与 softmax 交叉熵
- 理解 DataLoader 与 Dataset 的数据管道设计
- 掌握完整训练循环的组装：前向→损失→反向→更新
- 掌握模型评估方法：精度计算与测试集验证

</Objectives>

## 训练流程全景

训练一个神经网络就是把前面所有零件组装起来，形成一个完整的工作流。

<Dl2TrainingEvaluationDiagram />

```text
数据 ──→ 模型(前向) ──→ 损失 ──→ 反向传播 ──→ 优化器更新
  ↑                                                    │
  └────────────── 下一轮 mini-batch ←─────────────────┘
```

## 损失函数

### 均方误差（MSE）

用于回归任务：

```python
class MeanSquaredError(Function):
    def forward(self, x0, x1):
        diff = x0 - x1
        y = (diff ** 2).sum() / len(diff)
        return y

    def backward(self, gy):
        x0, x1 = self.inputs
        diff = x0.data - x1.data
        gx0 = gy * diff * (2. / len(diff))
        gx1 = -gx0
        return gx0, gx1

def mean_squared_error(x0, x1):
    return MeanSquaredError()(x0, x1)
```

### Softmax 交叉熵

用于多分类任务，是深度学习最常用的损失函数：

```python
class SoftmaxCrossEntropy(Function):
    def forward(self, x, t):
        # x: 预测得分 (N, C), t: 标签 (N,) 整数
        N = x.shape[0]
        # softmax（数值稳定）
        x = x - x.max(axis=1, keepdims=True)
        exp_x = np.exp(x)
        softmax_x = exp_x / exp_x.sum(axis=1, keepdims=True)
        # 交叉熵：-log(softmax_x[t])
        log_z = np.log(softmax_x[np.arange(N), t] + 1e-10)
        y = -log_z.sum() / N
        return y

    def backward(self, gy):
        x, t = self.inputs
        N = x.shape[0]
        # 重算 softmax
        x = x.data - x.data.max(axis=1, keepdims=True)
        exp_x = np.exp(x)
        softmax_x = exp_x / exp_x.sum(axis=1, keepdims=True)
        # 梯度：(softmax - onehot) / N
        gx = softmax_x.copy()
        gx[np.arange(N), t.data] -= 1
        gx = gx * gy / N
        return gx

def softmax_cross_entropy(x, t):
    return SoftmaxCrossEntropy()(x, t)
```

<Callout type="info">

**梯度简洁性**：softmax + 交叉熵的反向传播梯度为 `(softmax - onehot) / N`，形式极其简洁——预测正确时梯度接近 0，预测错误时梯度大。这是将 softmax 和交叉熵合并为一个 Function 的原因。

</Callout>

## 数据管道

### Dataset 基类

```python
class Dataset:
    """数据集抽象基类"""
    def __init__(self, train=True, transform=None):
        self.train = train
        self.transform = transform
        self.data = None
        self.label = None
        self.prepare()

    def prepare(self):
        """子类重写：加载/生成数据"""
        pass

    def __getitem__(self, index):
        if self.label is None:
            return self.data[index], None
        return self.data[index], self.label[index]

    def __len__(self):
        return len(self.data)
```

### DataLoader

```python
class DataLoader:
    """mini-batch 数据加载器"""
    def __init__(self, dataset, batch_size, shuffle=True):
        self.dataset = dataset
        self.batch_size = batch_size
        self.shuffle = shuffle
        self.data_size = len(dataset)
        self.max_iter = self.data_size // batch_size

    def __iter__(self):
        self.iteration = 0
        if self.shuffle:
            self.index = np.random.permutation(self.data_size)
        else:
            self.index = np.arange(self.data_size)
        return self

    def __next__(self):
        if self.iteration >= self.max_iter:
            raise StopIteration
        i = self.iteration
        batch_index = self.index[i*self.batch_size:(i+1)*self.batch_size]
        batch = [self.dataset[i] for i in batch_index]
        x = np.array([item[0] for item in batch])
        t = np.array([item[1] for item in batch])
        self.iteration += 1
        return x, t
```

## 完整训练循环

```python
# 1. 准备数据
train_set = SpiralDataset(train=True)
test_set = SpiralDataset(train=False)
train_loader = DataLoader(train_set, batch_size=30, shuffle=True)

# 2. 定义模型和优化器
model = MLP((100, 50, 3))           # 3 类分类
optimizer = Adam().setup(model)

# 3. 训练循环
max_epoch = 100
for epoch in range(max_epoch):
    for x, t in train_loader:
        # 前向
        y = model(x)
        # 损失
        loss = softmax_cross_entropy(y, t)
        # 清零 + 反向
        model.cleargrads()
        loss.backward()
        # 更新
        optimizer.update()

    # 每个 epoch 评估
    if epoch % 10 == 0:
        acc = accuracy(model, test_set)
        print(f'epoch {epoch}, loss {loss.data:.4f}, acc {acc:.4f}')
```

## 精度评估

```python
def accuracy(model, dataset):
    """计算模型在数据集上的分类精度"""
    x, t = dataset.data, dataset.label
    y = model(x)                       # 前向传播（不需要梯度）
    pred = y.data.argmax(axis=1)       # 取最大值索引为预测类别
    correct = (pred == t).sum()
    total = len(t)
    return correct / total
```

<Callout type="tip">

**评估时不需要梯度**：精度评估只做前向传播，不需要反向传播。在 DeZero 中可以通过 `using_config('enable_backprop', False)` 禁用计算图构建，节省内存。这与 PyTorch 的 `torch.no_grad()` 作用一致。

</Callout>

## 训练监控指标

| 指标 | 计算方式 | 意义 |
|------|---------|------|
| 训练损失 | 每 epoch 平均 loss | 模型拟合程度 |
| 训练精度 | 训练集上的 accuracy | 是否学会训练数据 |
| 测试精度 | 测试集上的 accuracy | 泛化能力 |
| 过拟合检测 | 训练精度高但测试精度低 | 模型记住了训练数据但没学到规律 |

<Glossary>

<GlossaryItem term="softmax 交叉熵">
多分类任务的标准损失函数。forward 先做 softmax 转概率，再算交叉熵 `-log(p[t])`。backward 梯度为 `(softmax - onehot) / N`，形式简洁——预测正确梯度小、错误梯度大。合并 softmax 和交叉熵为一个 Function 是因为分开实现时 softmax 的 backward 形式复杂，合并后大幅简化。
</GlossaryItem>

<GlossaryItem term="均方误差（MSE）">
回归任务的损失函数。`L = (1/N) * Σ(y_pred - y_true)^2`。backward 梯度为 `2*(y_pred - y_true)/N`。适用于输出连续值的回归任务，不适合分类（分类用交叉熵）。
</GlossaryItem>

<GlossaryItem term="Dataset">
数据集抽象基类。子类重写 prepare() 加载/生成数据，存储到 self.data 和 self.label。支持 `__getitem__` 索引访问和 `__len__` 长度查询。可配合 transform 做数据预处理。
</GlossaryItem>

<GlossaryItem term="DataLoader">
mini-batch 数据加载器。接收 Dataset 和 batch_size，`__iter__`/`__next__` 实现迭代器协议，每次返回一个 batch 的 (x, t)。支持 shuffle 打乱顺序。是训练循环的数据来源。
</GlossaryItem>

<GlossaryItem term="训练循环">
神经网络训练的核心流程：①前向——`y = model(x)` 算预测。②损失——`loss = criterion(y, t)` 算误差。③清零——`model.cleargrads()` 清旧梯度。④反向——`loss.backward()` 算新梯度。⑤更新——`optimizer.update()` 用梯度更新参数。外层按 epoch 循环，内层按 mini-batch 循环。
</GlossaryItem>

<GlossaryItem term="精度（accuracy）">
分类模型的评估指标。`正确预测数 / 总样本数`。评估时只做前向传播，取 argmax 为预测类别，与真实标签对比。DeZero 可通过 `using_config('enable_backprop', False)` 禁用计算图构建以节省内存，对应 PyTorch 的 `torch.no_grad()`。
</GlossaryItem>

</Glossary>

<Exercises>

### 问题 1

为什么 softmax 交叉熵要合并为一个 Function 实现？写出 forward 和 backward。

<Answer>

softmax 交叉熵合并为一个 Function 实现的原因：如果分开实现（先 softmax 再交叉熵），softmax 的 backward 涉及雅可比矩阵，形式复杂。合并后，利用 softmax + 交叉熵的导数有简洁形式 `(softmax - onehot) / N`，大幅简化计算。forward 实现：接收预测得分 x(N,C) 和标签 t(N,)。先做数值稳定的 softmax（减最大值），再算交叉熵 `y = -sum(log(softmax[arange(N), t] + 1e-10)) / N`，加 1e-10 防 log(0)。backward 实现：重算 softmax，梯度 `gx = softmax.copy(); gx[arange(N), t] -= 1; gx = gx * gy / N`。即 softmax 概率减去 onehot 标签（预测正确的类别减 1），再乘以上游梯度 gy 除以 N。梯度含义：预测正确（softmax 接近 1）时梯度接近 0，预测错误（softmax 接近 0）时梯度大，能快速纠正错误预测。
</Answer>

### 问题 2

DataLoader 如何实现 mini-batch 数据加载？写出核心逻辑。

<Answer>

DataLoader 实现 mini-batch 数据加载的核心逻辑：`__init__` 接收 dataset、batch_size、shuffle 参数，计算 data_size 和 max_iter（总 batch 数）。`__iter__` 初始化 iteration=0，若 shuffle 则生成随机排列的 index 数组，否则用顺序 index。`__next__` 检查 iteration 是否超过 max_iter，若是则 raise StopIteration。否则取当前 batch 的 index 切片 `batch_index = self.index[i*batch_size:(i+1)*batch_size]`，从 dataset 中按 batch_index 取出数据，组装为 x（特征数组）和 t（标签数组），iteration+=1 后返回 (x, t)。关键设计：①shuffle 用 `np.random.permutation` 打乱顺序，每个 epoch 重新打乱。②index 数组在 `__iter__` 时生成，保证每个 epoch 的打乱不同。③batch_index 切片处理最后一个 batch 不足 batch_size 的情况（max_iter = data_size // batch_size 向下取整，丢弃尾部不足一个 batch 的数据）。
</Answer>

### 问题 3

写出完整的训练循环，说明每一步的作用。

<Answer>

完整训练循环：`for epoch in range(max_epoch): for x, t in train_loader: y = model(x); loss = softmax_cross_entropy(y, t); model.cleargrads(); loss.backward(); optimizer.update(); if epoch % 10 == 0: acc = accuracy(model, test_set); print(...)`。每一步作用：①`for x, t in train_loader`——DataLoader 按 mini-batch 迭代，每次返回一个 batch 的特征和标签。②`y = model(x)`——前向传播，模型对输入 x 计算预测值 y。③`loss = softmax_cross_entropy(y, t)`——计算预测值 y 与真实标签 t 之间的损失。④`model.cleargrads()`——清零所有参数的梯度，防止上一轮的梯度残留累加。⑤`loss.backward()`——反向传播，沿计算图从 loss 回溯到所有参数，填充梯度。⑥`optimizer.update()`——优化器根据梯度更新所有参数的 data。⑦定期评估——每若干 epoch 在测试集上算精度，监控训练进度和泛化能力。外层 epoch 循环控制训练轮数，内层 batch 循环遍历所有数据。
</Answer>

### 问题 4

如何检测过拟合？DeZero 中评估时如何禁用计算图构建以节省内存？

<Answer>

过拟合检测方法：对比训练精度和测试精度。如果训练精度持续升高但测试精度停滞或下降，说明模型过拟合——记住了训练数据的细节（包括噪声）但没有学到通用规律。具体监控：每个 epoch 记录训练损失、训练精度和测试精度。训练精度高（如 99%）但测试精度低（如 70%）是典型过拟合信号。应对方法：增加数据量、正则化（L2）、Dropout、早停（在测试精度开始下降时停止训练）。DeZero 中评估时禁用计算图构建：用 `with using_config('enable_backprop', False):` 上下文管理器包裹评估代码。在该上下文内，Function.__call__ 检测到 enable_backprop 为 False，跳过 creator 设置和 inputs/outputs 保存，不构建计算图。这样评估（只做前向）时不占用计算图内存，与 PyTorch 的 `with torch.no_grad():` 作用一致。实现原理是 DeZero 的 Config 类支持全局配置，Function.__call__ 中 `if Config.enable_backprop: output.set_creator(self)` 做条件判断。
</Answer>

</Exercises>
