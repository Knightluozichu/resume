---
title: "第4章 神经网络的学习"
type: B
section: "前向与学习"
order: 4
description: "第4章 神经网络的学习覆盖22个权威目录层级，以数学推导、数组实现、交互实验和失败对照重建核心。"
demo: true
math: true
sourceUrl: "https://www.oreilly.co.jp/books/9784873117584/toc.html"
draft: false
---

import {
  Dls04NeuralNetworkLearningMapLab,
  Dls04NeuralNetworkLearningExperimentLab,
  Dls04NeuralNetworkLearningEvidenceLab,
} from "@/components/mdx/deep-learning-from-scratch/diagrams/dls-04-neural-network-learning";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第4章 神经网络的学习中的模型对象、数据流、数学前提和失效边界
- 能计算四个核心方程中的shape、概率、损失、局部导数或更新量
- 能复现三个互动视图，并定位第一条前向、反向或独立评价证据分叉
- 能判断“参数更新只能读取训练批次；数值梯度使用中心差分并逐参数恢复原值，测试数据只用于最终泛化评价。”是否成立

</Objectives>

## 为什么从可核查的中间量开始

<Term def="训练数据是本页第1个核查坐标，用于连接数学对象、数组实现、中间证据与失效边界。">训练数据</Term>、<Term def="测试数据是本页第2个核查坐标，用于连接数学对象、数组实现、中间证据与失效边界。">测试数据</Term>、<Term def="损失函数是本页第3个核查坐标，用于连接数学对象、数组实现、中间证据与失效边界。">损失函数</Term>、<Term def="mini-batch是本页第4个核查坐标，用于连接数学对象、数组实现、中间证据与失效边界。">mini-batch</Term>、<Term def="数值微分是本页第5个核查坐标，用于连接数学对象、数组实现、中间证据与失效边界。">数值微分</Term>、<Term def="梯度下降是本页第6个核查坐标，用于连接数学对象、数组实现、中间证据与失效边界。">梯度下降</Term>
共同构成本页坐标。从训练/测试隔离、损失函数和mini-batch进入数值微分与梯度下降，完整实现两层网络的参数学习和独立评价。

“从零实现”的价值不是拒绝成熟框架，而是让每个数学对象都对应一个可检查的数组，让每次forward、loss、backward和update都能解释。实验先冻结样本身份、输入与标签shape、参数初值、随机种子、数据折分、浮点精度和停止规则；运行前先预测第一处会改变的中间量，运行后立即与手算或枚举基线比较。

本页沿“隔离数据折分 -> 计算批次损失 -> 中心差分梯度 -> 执行参数更新 -> 重算训练目标 -> 独立评价测试”组织证据。最终准确率、损失曲线或漂亮图像都只是末端现象；若shape、概率归一、局部导数、梯度尺度或数据隔离在更早位置失败，末端结果不能采信。修复后清空前向缓存、梯度、优化器状态与派生特征，从原始输入完整重放。

## 数学骨架与shape合同

$$
E=\frac12\sum_k(y_k-t_k)^2
$$

$$
L=-\frac1N\sum_{n=1}^{N}\log y_{n,t_n}
$$

$$
\frac{\partial f}{\partial x_i}\approx\frac{f(x_i+h)-f(x_i-h)}{2h}
$$

$$
\theta_{t+1}=\theta_t-\eta\nabla_{\theta}L(\theta_t)
$$

方程实现前先声明标量、向量、矩阵或四维张量的轴语义，再核对有限值、归一化、导数符号和更新尺度。相同终点可能由错误广播、标签错位、批量分母或缓存污染偶然产生；所以验收以最早分叉为准，并保留更新前后的独立重算结果。

## 核心机制

1. 数据驱动学习让模型从样本调整权重，而不是人为为每类图像编写规则。训练集用于拟合参数，测试集模拟未见数据；预处理统计、模型选择和停止条件也只能由训练与验证产生。
2. 损失函数把整张预测向量压缩成可比较标量。均方误差适合连续差异，交叉熵直接惩罚正确类别概率；损失需要连续可微，因为离散准确率对微小参数变化通常不敏感。
3. mini-batch用随机子集近似全数据损失和梯度。抽样索引、batch大小和归一化分母必须记录；标签既可能是one-hot向量也可能是类别索引，交叉熵实现要显式区分。
4. 数值微分用中心差分避免单侧差分的大误差，h过大产生截断误差，过小产生浮点消减。它计算昂贵但透明，适合作为少量参数上的梯度真值，而非大网络的正式训练算法。
5. 梯度指向损失增长最快方向，负梯度用于下降。学习率过大导致震荡或发散，过小收敛缓慢；每步应保存更新前损失、梯度范数、参数变化和更新后独立重算的损失。
6. 两层网络类统一params、predict、loss、accuracy、numerical_gradient和gradient接口。训练循环抽批、求梯度、更新参数并按周期评价；测试准确率只报告，不反向影响训练决定。

## 从原书路线定位本页

全书权威路线为：第1章 Python入门 -> 第2章 感知机 -> 第3章 神经网络 -> 第4章 神经网络的学习 -> 第5章 误差反向传播法 -> 第6章 与学习相关的技巧 -> 第7章 卷积神经网络 -> 第8章 深度学习 -> 附录A Softmax-with-Loss层的计算图。当前页面不是把主题任意重组，而是在这条依赖链上恢复原书位置。前置章节提供数组、表示和可微目标，后续章节使用这里的中间证据继续推导；跨章引用时必须写明复用的是数学定义、代码接口还是实验状态。

对照实验只改变一个变量：shape、偏置、激活、batch、学习率、初始化、网络深度或任务输出。保持输入、参数、随机状态和评价预算不变，先写出按方程应最先变化的量，再比较基线与边界模式。这样才能区分机制收益、实现错误和随机波动。

## 权威目录逐项讲解

### 第4章 神经网络的学习

“神经网络的学习”放回本章主线分析其输入、数学变换、输出和边界。复现时保存关键中间量，并用最小样本验证“参数更新只能读取训练批次；数值梯度使用中心差分并逐参数恢复原值，测试数据只用于最终泛化评价。”。

验收“第4章 神经网络的学习”时先写出预期的输入、输出和第一处可观察变化，再运行一个固定小样本。若终点看似合理但该中间证据未出现，应停止并检查轴、广播、归一化、缓存、梯度或数据隔离。

### 4.1 从数据中学习

训练数据产生参数和所有数据依赖决策，测试数据只估计冻结模型的泛化；预处理、停止规则或阈值若读取测试信息，就构成泄漏。

验收“4.1 从数据中学习”时先写出预期的输入、输出和第一处可观察变化，再运行一个固定小样本。若终点看似合理但该中间证据未出现，应停止并检查轴、广播、归一化、缓存、梯度或数据隔离。

### 4.1.1 数据驱动

训练数据产生参数和所有数据依赖决策，测试数据只估计冻结模型的泛化；预处理、停止规则或阈值若读取测试信息，就构成泄漏。

验收“4.1.1 数据驱动”时先写出预期的输入、输出和第一处可观察变化，再运行一个固定小样本。若终点看似合理但该中间证据未出现，应停止并检查轴、广播、归一化、缓存、梯度或数据隔离。

### 4.1.2 训练数据和测试数据

训练数据产生参数和所有数据依赖决策，测试数据只估计冻结模型的泛化；预处理、停止规则或阈值若读取测试信息，就构成泄漏。

验收“4.1.2 训练数据和测试数据”时先写出预期的输入、输出和第一处可观察变化，再运行一个固定小样本。若终点看似合理但该中间证据未出现，应停止并检查轴、广播、归一化、缓存、梯度或数据隔离。

### 4.2 损失函数

这一单元建立从交互验证到可复现脚本的编程基础：明确对象类型、控制流、函数输入输出与类状态，并把一次性试算保存为可重复执行的最小程序。

验收“4.2 损失函数”时先写出预期的输入、输出和第一处可观察变化，再运行一个固定小样本。若终点看似合理但该中间证据未出现，应停止并检查轴、广播、归一化、缓存、梯度或数据隔离。

### 4.2.1 均方误差

把预测质量变成连续标量目标，并明确按样本求和还是平均。损失提供参数变化方向，离散准确率只能作为评价，不能替代可微目标。

验收“4.2.1 均方误差”时先写出预期的输入、输出和第一处可观察变化，再运行一个固定小样本。若终点看似合理但该中间证据未出现，应停止并检查轴、广播、归一化、缓存、梯度或数据隔离。

### 4.2.2 交叉熵误差

把预测质量变成连续标量目标，并明确按样本求和还是平均。损失提供参数变化方向，离散准确率只能作为评价，不能替代可微目标。

验收“4.2.2 交叉熵误差”时先写出预期的输入、输出和第一处可观察变化，再运行一个固定小样本。若终点看似合理但该中间证据未出现，应停止并检查轴、广播、归一化、缓存、梯度或数据隔离。

### 4.2.3 mini-batch学习

保存抽样索引、批量大小和归一化分母，用小批量梯度近似全数据梯度；标签表示和类别轴必须显式，避免同一公式在索引标签与one-hot标签间错用。

验收“4.2.3 mini-batch学习”时先写出预期的输入、输出和第一处可观察变化，再运行一个固定小样本。若终点看似合理但该中间证据未出现，应停止并检查轴、广播、归一化、缓存、梯度或数据隔离。

### 4.2.4 mini-batch版交叉熵误差的实现

把预测质量变成连续标量目标，并明确按样本求和还是平均。损失提供参数变化方向，离散准确率只能作为评价，不能替代可微目标。

验收“4.2.4 mini-batch版交叉熵误差的实现”时先写出预期的输入、输出和第一处可观察变化，再运行一个固定小样本。若终点看似合理但该中间证据未出现，应停止并检查轴、广播、归一化、缓存、梯度或数据隔离。

### 4.2.5 为何要设定损失函数

这一单元建立从交互验证到可复现脚本的编程基础：明确对象类型、控制流、函数输入输出与类状态，并把一次性试算保存为可重复执行的最小程序。

验收“4.2.5 为何要设定损失函数”时先写出预期的输入、输出和第一处可观察变化，再运行一个固定小样本。若终点看似合理但该中间证据未出现，应停止并检查轴、广播、归一化、缓存、梯度或数据隔离。

### 4.3 数值微分

用中心差分建立小规模真值，逐坐标恢复参数原值；负梯度给出局部下降方向，学习率决定步长，更新后必须重新forward计算新损失。

验收“4.3 数值微分”时先写出预期的输入、输出和第一处可观察变化，再运行一个固定小样本。若终点看似合理但该中间证据未出现，应停止并检查轴、广播、归一化、缓存、梯度或数据隔离。

### 4.3.1 导数

用中心差分建立小规模真值，逐坐标恢复参数原值；负梯度给出局部下降方向，学习率决定步长，更新后必须重新forward计算新损失。

验收“4.3.1 导数”时先写出预期的输入、输出和第一处可观察变化，再运行一个固定小样本。若终点看似合理但该中间证据未出现，应停止并检查轴、广播、归一化、缓存、梯度或数据隔离。

### 4.3.2 数值微分的例子

用中心差分建立小规模真值，逐坐标恢复参数原值；负梯度给出局部下降方向，学习率决定步长，更新后必须重新forward计算新损失。

验收“4.3.2 数值微分的例子”时先写出预期的输入、输出和第一处可观察变化，再运行一个固定小样本。若终点看似合理但该中间证据未出现，应停止并检查轴、广播、归一化、缓存、梯度或数据隔离。

### 4.3.3 偏导数

用中心差分建立小规模真值，逐坐标恢复参数原值；负梯度给出局部下降方向，学习率决定步长，更新后必须重新forward计算新损失。

验收“4.3.3 偏导数”时先写出预期的输入、输出和第一处可观察变化，再运行一个固定小样本。若终点看似合理但该中间证据未出现，应停止并检查轴、广播、归一化、缓存、梯度或数据隔离。

### 4.4 梯度

用中心差分建立小规模真值，逐坐标恢复参数原值；负梯度给出局部下降方向，学习率决定步长，更新后必须重新forward计算新损失。

验收“4.4 梯度”时先写出预期的输入、输出和第一处可观察变化，再运行一个固定小样本。若终点看似合理但该中间证据未出现，应停止并检查轴、广播、归一化、缓存、梯度或数据隔离。

### 4.4.1 梯度法

用中心差分建立小规模真值，逐坐标恢复参数原值；负梯度给出局部下降方向，学习率决定步长，更新后必须重新forward计算新损失。

验收“4.4.1 梯度法”时先写出预期的输入、输出和第一处可观察变化，再运行一个固定小样本。若终点看似合理但该中间证据未出现，应停止并检查轴、广播、归一化、缓存、梯度或数据隔离。

### 4.4.2 神经网络的梯度

用中心差分建立小规模真值，逐坐标恢复参数原值；负梯度给出局部下降方向，学习率决定步长，更新后必须重新forward计算新损失。

验收“4.4.2 神经网络的梯度”时先写出预期的输入、输出和第一处可观察变化，再运行一个固定小样本。若终点看似合理但该中间证据未出现，应停止并检查轴、广播、归一化、缓存、梯度或数据隔离。

### 4.5 学习算法的实现

把predict、loss、gradient与accuracy组织成一致接口，训练循环只使用训练批次更新；评价阶段切换为只读并保留独立预测。

验收“4.5 学习算法的实现”时先写出预期的输入、输出和第一处可观察变化，再运行一个固定小样本。若终点看似合理但该中间证据未出现，应停止并检查轴、广播、归一化、缓存、梯度或数据隔离。

### 4.5.1 2层神经网络的类

这一单元建立从交互验证到可复现脚本的编程基础：明确对象类型、控制流、函数输入输出与类状态，并把一次性试算保存为可重复执行的最小程序。

验收“4.5.1 2层神经网络的类”时先写出预期的输入、输出和第一处可观察变化，再运行一个固定小样本。若终点看似合理但该中间证据未出现，应停止并检查轴、广播、归一化、缓存、梯度或数据隔离。

### 4.5.2 mini-batch的实现

保存抽样索引、批量大小和归一化分母，用小批量梯度近似全数据梯度；标签表示和类别轴必须显式，避免同一公式在索引标签与one-hot标签间错用。

验收“4.5.2 mini-batch的实现”时先写出预期的输入、输出和第一处可观察变化，再运行一个固定小样本。若终点看似合理但该中间证据未出现，应停止并检查轴、广播、归一化、缓存、梯度或数据隔离。

### 4.5.3 基于测试数据的评价

训练数据产生参数和所有数据依赖决策，测试数据只估计冻结模型的泛化；预处理、停止规则或阈值若读取测试信息，就构成泄漏。

验收“4.5.3 基于测试数据的评价”时先写出预期的输入、输出和第一处可观察变化，再运行一个固定小样本。若终点看似合理但该中间证据未出现，应停止并检查轴、广播、归一化、缓存、梯度或数据隔离。

### 4.6 小结

把本章的输入、核心变换、中间证据和失效边界重新连成闭环；验收重点是能从冻结输入重放到“参数更新只能读取训练批次；数值梯度使用中心差分并逐参数恢复原值，测试数据只用于最终泛化评价。”，而不是只复述术语。

验收“4.6 小结”时先写出预期的输入、输出和第一处可观察变化，再运行一个固定小样本。若终点看似合理但该中间证据未出现，应停止并检查轴、广播、归一化、缓存、梯度或数据隔离。

## 三步交互实验

先预测控制变量变化会如何影响shape、前向值、损失、梯度与独立证据，再切换基线、边界和失败模式。三个视图共享同一冻结输入，但分别强调结构、单变量扰动和验收残差。

<Stepper>
  <Step title="建立结构地图">
    标出输入、核心变换、中间状态、输出、反向路径与失败边界。
    <Dls04NeuralNetworkLearningMapLab />
  </Step>
  <Step title="运行单变量实验">
    固定样本、参数和预算，只改变一个shape、步长、深度或噪声条件，并预测第一处分叉。
    <Dls04NeuralNetworkLearningExperimentLab />
  </Step>
  <Step title="核对独立证据">
    并排比较前向摘要、梯度残差、训练目标与验证证据；首错出现后从冻结输入重放。
    <Dls04NeuralNetworkLearningEvidenceLab />
  </Step>
</Stepper>

## 常见失败与边界

<Callout type="trap">
  **用准确率直接求梯度：**
  argmax和计数在大多数参数邻域不变，无法提供有效下降方向。
</Callout>

<Callout type="trap">
  **查看测试曲线决定轮次：** 这会把测试集变成验证集，使最终泛化估计偏乐观。
</Callout>

<Callout type="tip">
  **最小重放顺序：**
  先在可枚举样本上手算，再核对程序中间量，最后扩大批次或模型。任何非有限值、shape漂移、概率不归一、梯度检查失败或测试泄漏都应立即停止。
</Callout>

## 本章回顾

从训练/测试隔离、损失函数和mini-batch进入数值微分与梯度下降，完整实现两层网络的参数学习和独立评价。 验收底线是：参数更新只能读取训练批次；数值梯度使用中心差分并逐参数恢复原值，测试数据只用于最终泛化评价。。学习完成的标准不是记住名称，而是能从冻结输入预测中间状态，解释单变量为何改变答案，并在失败样本中指出第一处违背前提的位置。

<Glossary>

<GlossaryItem term="训练数据">
  “训练数据”对应“隔离数据折分”：记录定义、shape或标量范围、前向状态、反向证据、数值容差和失败边界，不能只保留最终分数。
</GlossaryItem>

<GlossaryItem term="测试数据">
  “测试数据”对应“计算批次损失”：记录定义、shape或标量范围、前向状态、反向证据、数值容差和失败边界，不能只保留最终分数。
</GlossaryItem>

<GlossaryItem term="损失函数">
  “损失函数”对应“中心差分梯度”：记录定义、shape或标量范围、前向状态、反向证据、数值容差和失败边界，不能只保留最终分数。
</GlossaryItem>

<GlossaryItem term="mini-batch">
  “mini-batch”对应“执行参数更新”：记录定义、shape或标量范围、前向状态、反向证据、数值容差和失败边界，不能只保留最终分数。
</GlossaryItem>

<GlossaryItem term="数值微分">
  “数值微分”对应“重算训练目标”：记录定义、shape或标量范围、前向状态、反向证据、数值容差和失败边界，不能只保留最终分数。
</GlossaryItem>

<GlossaryItem term="梯度下降">
  “梯度下降”对应“独立评价测试”：记录定义、shape或标量范围、前向状态、反向证据、数值容差和失败边界，不能只保留最终分数。
</GlossaryItem>

</Glossary>

<Exercises>

1. 如何为第4章 神经网络的学习建立最小、可手算且不泄漏测试信息的复现实验？

<Answer>
先冻结输入、标签、shape、参数、随机种子和数据折分，再按“隔离数据折分 -> 计算批次损失 -> 中心差分梯度 -> 执行参数更新 -> 重算训练目标 -> 独立评价测试”保存中间状态。用极小样本手算四个方程，解析结果与枚举或中心差分对照；所有模型选择完成后测试集只使用一次。
</Answer>

2. 如何证明单变量改动造成的结果来自本页机制，而不是广播、缓存或随机波动？

<Answer>
  保持样本、初值、批次顺序、预算和评价协议不变，只替换一个条件。运行前写出按方程应最先改变的shape、概率、损失或梯度；该证据不符合预期时先修复实现，不采信末端分数。
</Answer>

3. 为“参数更新只能读取训练批次；数值梯度使用中心差分并逐参数恢复原值，测试数据只用于最终泛化评价。”构造一个失败样本，并说明应在哪里停止。

<Answer>
  可以故意交换样本轴与特征轴、制造错误广播、去掉softmax稳定化、复用旧缓存、漏除batch大小或让测试信息参与选择。先预测第一条被破坏的中间证据，在该处停止并保存原始状态；清空派生产物后从冻结输入重放。
</Answer>

</Exercises>

<Attribution
  adaptedFrom="斋藤康毅著、陆宇杰译《深度学习入门：基于Python的理论与实现》，人民邮电出版社，2018年7月，ISBN 9787115485588；原版《ゼロから作るDeep Learning》，O'Reilly Japan，2016年，320页，ISBN 9784873117584 · 基于O'Reilly Japan官方8章、202个编号节/小节和附录A完整目录的独立教学重构"
  adaptedUrl="https://www.oreilly.co.jp/books/9784873117584/toc.html"
/>
