---
title: "步骤2 最大似然估计"
type: B
section: "步骤2 最大似然估计"
order: 2
description: "步骤2 最大似然估计逐项覆盖权威目录，以公式、最小张量、交互实验和失败对照重建生成模型证据链。"
demo: true
math: true
sourceUrl: "https://www.oreilly.co.jp/books/9784814400591/"
draft: true
---

import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释步骤2 最大似然估计中的分布假设、目标函数、参数更新与采样边界
- 能推导并计算四个核心方程中的密度、似然、ELBO、梯度或噪声目标
- 能复现三个互动视图，并定位第一条密度、责任度、梯度或采样证据分叉
- 能判断“训练样本、拟合参数、对数似然和新生成样本必须分开；参数只能由训练集估计，评价不能回读答案。”是否成立并设计单一故障样本

</Objectives>

## 为什么先冻结概率合同

<Term def="生成式模型是本页第1个证据坐标，用于连接分布、目标、更新、采样与失败边界。">生成式模型</Term>、<Term def="总体是本页第2个证据坐标，用于连接分布、目标、更新、采样与失败边界。">总体</Term>、<Term def="样本是本页第3个证据坐标，用于连接分布、目标、更新、采样与失败边界。">样本</Term>、<Term def="似然是本页第4个证据坐标，用于连接分布、目标、更新、采样与失败边界。">似然</Term>、<Term def="对数似然是本页第5个证据坐标，用于连接分布、目标、更新、采样与失败边界。">对数似然</Term>、<Term def="最大似然估计是本页第6个证据坐标，用于连接分布、目标、更新、采样与失败边界。">最大似然估计</Term>
共同构成本页坐标。把观测数据看作未知总体的样本，以似然和对数似然估计正态分布参数，再检验生成与概率计算。

生成模型同时包含数据分布、随机采样、优化目标和参数化近似。结果看似合理，仍可能来自训练测试泄漏、错误归一化、广播shape、旧参数责任度、未停止的梯度或随机种子挑选。实验前必须冻结数据版本、预处理、shape、参数初值、优化预算、随机种子和评价协议。

本页遵循“声明联合分布 -> 写条件分解 -> 手算最小样本 -> 运行单变量实验 -> 独立重算目标 -> 注入失败并重放”。第一处分叉出现后停止扩大训练，从原始样本和空派生状态复现，不能用最终图片替代过程证据。

## 数学骨架

$$
L(\theta)=\prod_{i=1}^{N}p(x_i;\theta)
$$

$$
\ell(\theta)=\sum_{i=1}^{N}\log p(x_i;\theta)
$$

$$
\hat{\mu}=\frac{1}{N}\sum_{i=1}^{N}x_i
$$

$$
\hat{\sigma}^2=\frac{1}{N}\sum_{i=1}^{N}(x_i-\hat{\mu})^2
$$

每个方程进入实现前都声明求和或期望的轴、张量shape、条件变量、参数版本和随机来源。概率必须归一，方差必须为正，目标中的常量项能否省略要说明；采样分支和可微分支应显式区分。

## 可复现证据工作表

第一栏保存数据版本、样本索引、原始值、预处理输出、批次shape和随机种子。第二栏记录分布名称、条件变量、参数版本、密度或对数密度、归一化轴与数值稳定策略。这样才能判断问题来自数据、分布假设还是实现。

第三栏保存更新前参数、每个损失项、梯度范数、优化器状态和更新后参数。潜变量模型还要记录后验参数、采样噪声、责任度或时间步。第四栏以冻结模型和未见种子重做采样与评价，严禁复用训练期缓存。

失败对照只改变一个条件，例如交换样本轴与特征轴、在E步骤使用新参数、漏掉KL项、让目标噪声错位或在引导时混用不同时间步。检测器应在预先声明的中间证据报警；若只能从最终视觉质量发现问题，工作表仍不合格。

## 权威目录逐项讲解

### 步骤2 最大似然估计

把“步骤2 最大似然估计”放回“数据 -> 分布假设 -> 目标函数 -> 参数更新 -> 独立采样”的证据链。先写清输入的shape、条件变量和随机来源，再说明生成式模型如何改变样本；任何结论都必须能由保存的中间量重算，不能只凭最终图像是否好看判断。

最小实验固定四到八个样本、参数初值和随机种子，先预测本节变更后最早发生变化的密度、责任度、ELBO、梯度、噪声或采样轨迹。针对“步骤2 最大似然估计”，验收条件是“训练样本、拟合参数、对数似然和新生成样本必须分开；参数只能由训练集估计，评价不能回读答案。”。若第一条预期证据没有变化，就停止扩大模型，从原始样本与空优化器状态重放。

### 2.1 生成式模型的基础知识

把“2.1 生成式模型的基础知识”放回“数据 -> 分布假设 -> 目标函数 -> 参数更新 -> 独立采样”的证据链。先写清输入的shape、条件变量和随机来源，再说明总体如何改变似然；任何结论都必须能由保存的中间量重算，不能只凭最终图像是否好看判断。

最小实验固定四到八个样本、参数初值和随机种子，先预测本节变更后最早发生变化的密度、责任度、ELBO、梯度、噪声或采样轨迹。针对“2.1 生成式模型的基础知识”，验收条件是“训练样本、拟合参数、对数似然和新生成样本必须分开；参数只能由训练集估计，评价不能回读答案。”。若第一条预期证据没有变化，就停止扩大模型，从原始样本与空优化器状态重放。

### 2.1.1 什么是生成式模型

把“2.1.1 什么是生成式模型”放回“数据 -> 分布假设 -> 目标函数 -> 参数更新 -> 独立采样”的证据链。先写清输入的shape、条件变量和随机来源，再说明样本如何改变对数似然；任何结论都必须能由保存的中间量重算，不能只凭最终图像是否好看判断。

最小实验固定四到八个样本、参数初值和随机种子，先预测本节变更后最早发生变化的密度、责任度、ELBO、梯度、噪声或采样轨迹。针对“2.1.1 什么是生成式模型”，验收条件是“训练样本、拟合参数、对数似然和新生成样本必须分开；参数只能由训练集估计，评价不能回读答案。”。若第一条预期证据没有变化，就停止扩大模型，从原始样本与空优化器状态重放。

### 2.1.2 总体和样本

把“2.1.2 总体和样本”放回“数据 -> 分布假设 -> 目标函数 -> 参数更新 -> 独立采样”的证据链。先写清输入的shape、条件变量和随机来源，再说明似然如何改变最大似然估计；任何结论都必须能由保存的中间量重算，不能只凭最终图像是否好看判断。

最小实验固定四到八个样本、参数初值和随机种子，先预测本节变更后最早发生变化的密度、责任度、ELBO、梯度、噪声或采样轨迹。针对“2.1.2 总体和样本”，验收条件是“训练样本、拟合参数、对数似然和新生成样本必须分开；参数只能由训练集估计，评价不能回读答案。”。若第一条预期证据没有变化，就停止扩大模型，从原始样本与空优化器状态重放。

### 2.2 使用真实数据实现生成式模型

把“2.2 使用真实数据实现生成式模型”放回“数据 -> 分布假设 -> 目标函数 -> 参数更新 -> 独立采样”的证据链。先写清输入的shape、条件变量和随机来源，再说明对数似然如何改变生成式模型；任何结论都必须能由保存的中间量重算，不能只凭最终图像是否好看判断。

最小实验固定四到八个样本、参数初值和随机种子，先预测本节变更后最早发生变化的密度、责任度、ELBO、梯度、噪声或采样轨迹。针对“2.2 使用真实数据实现生成式模型”，验收条件是“训练样本、拟合参数、对数似然和新生成样本必须分开；参数只能由训练集估计，评价不能回读答案。”。若第一条预期证据没有变化，就停止扩大模型，从原始样本与空优化器状态重放。

### 2.2.1 读取身高数据集

把“2.2.1 读取身高数据集”放回“数据 -> 分布假设 -> 目标函数 -> 参数更新 -> 独立采样”的证据链。先写清输入的shape、条件变量和随机来源，再说明最大似然估计如何改变总体；任何结论都必须能由保存的中间量重算，不能只凭最终图像是否好看判断。

最小实验固定四到八个样本、参数初值和随机种子，先预测本节变更后最早发生变化的密度、责任度、ELBO、梯度、噪声或采样轨迹。针对“2.2.1 读取身高数据集”，验收条件是“训练样本、拟合参数、对数似然和新生成样本必须分开；参数只能由训练集估计，评价不能回读答案。”。若第一条预期证据没有变化，就停止扩大模型，从原始样本与空优化器状态重放。

### 2.2.2 基于正态分布的生成式模型

把“2.2.2 基于正态分布的生成式模型”放回“数据 -> 分布假设 -> 目标函数 -> 参数更新 -> 独立采样”的证据链。先写清输入的shape、条件变量和随机来源，再说明生成式模型如何改变样本；任何结论都必须能由保存的中间量重算，不能只凭最终图像是否好看判断。

最小实验固定四到八个样本、参数初值和随机种子，先预测本节变更后最早发生变化的密度、责任度、ELBO、梯度、噪声或采样轨迹。针对“2.2.2 基于正态分布的生成式模型”，验收条件是“训练样本、拟合参数、对数似然和新生成样本必须分开；参数只能由训练集估计，评价不能回读答案。”。若第一条预期证据没有变化，就停止扩大模型，从原始样本与空优化器状态重放。

### 2.3 最大似然估计的理论知识

把“2.3 最大似然估计的理论知识”放回“数据 -> 分布假设 -> 目标函数 -> 参数更新 -> 独立采样”的证据链。先写清输入的shape、条件变量和随机来源，再说明总体如何改变似然；任何结论都必须能由保存的中间量重算，不能只凭最终图像是否好看判断。

最小实验固定四到八个样本、参数初值和随机种子，先预测本节变更后最早发生变化的密度、责任度、ELBO、梯度、噪声或采样轨迹。针对“2.3 最大似然估计的理论知识”，验收条件是“训练样本、拟合参数、对数似然和新生成样本必须分开；参数只能由训练集估计，评价不能回读答案。”。若第一条预期证据没有变化，就停止扩大模型，从原始样本与空优化器状态重放。

### 2.3.1 似然的最大化

把“2.3.1 似然的最大化”放回“数据 -> 分布假设 -> 目标函数 -> 参数更新 -> 独立采样”的证据链。先写清输入的shape、条件变量和随机来源，再说明样本如何改变对数似然；任何结论都必须能由保存的中间量重算，不能只凭最终图像是否好看判断。

最小实验固定四到八个样本、参数初值和随机种子，先预测本节变更后最早发生变化的密度、责任度、ELBO、梯度、噪声或采样轨迹。针对“2.3.1 似然的最大化”，验收条件是“训练样本、拟合参数、对数似然和新生成样本必须分开；参数只能由训练集估计，评价不能回读答案。”。若第一条预期证据没有变化，就停止扩大模型，从原始样本与空优化器状态重放。

### 2.3.2 利用导数求最大值

把“2.3.2 利用导数求最大值”放回“数据 -> 分布假设 -> 目标函数 -> 参数更新 -> 独立采样”的证据链。先写清输入的shape、条件变量和随机来源，再说明似然如何改变最大似然估计；任何结论都必须能由保存的中间量重算，不能只凭最终图像是否好看判断。

最小实验固定四到八个样本、参数初值和随机种子，先预测本节变更后最早发生变化的密度、责任度、ELBO、梯度、噪声或采样轨迹。针对“2.3.2 利用导数求最大值”，验收条件是“训练样本、拟合参数、对数似然和新生成样本必须分开；参数只能由训练集估计，评价不能回读答案。”。若第一条预期证据没有变化，就停止扩大模型，从原始样本与空优化器状态重放。

### 2.3.3 正态分布的最大似然估计

把“2.3.3 正态分布的最大似然估计”放回“数据 -> 分布假设 -> 目标函数 -> 参数更新 -> 独立采样”的证据链。先写清输入的shape、条件变量和随机来源，再说明对数似然如何改变生成式模型；任何结论都必须能由保存的中间量重算，不能只凭最终图像是否好看判断。

最小实验固定四到八个样本、参数初值和随机种子，先预测本节变更后最早发生变化的密度、责任度、ELBO、梯度、噪声或采样轨迹。针对“2.3.3 正态分布的最大似然估计”，验收条件是“训练样本、拟合参数、对数似然和新生成样本必须分开；参数只能由训练集估计，评价不能回读答案。”。若第一条预期证据没有变化，就停止扩大模型，从原始样本与空优化器状态重放。

### 2.4 生成式模型的用途

把“2.4 生成式模型的用途”放回“数据 -> 分布假设 -> 目标函数 -> 参数更新 -> 独立采样”的证据链。先写清输入的shape、条件变量和随机来源，再说明最大似然估计如何改变总体；任何结论都必须能由保存的中间量重算，不能只凭最终图像是否好看判断。

最小实验固定四到八个样本、参数初值和随机种子，先预测本节变更后最早发生变化的密度、责任度、ELBO、梯度、噪声或采样轨迹。针对“2.4 生成式模型的用途”，验收条件是“训练样本、拟合参数、对数似然和新生成样本必须分开；参数只能由训练集估计，评价不能回读答案。”。若第一条预期证据没有变化，就停止扩大模型，从原始样本与空优化器状态重放。

### 2.4.1 新数据的生成

把“2.4.1 新数据的生成”放回“数据 -> 分布假设 -> 目标函数 -> 参数更新 -> 独立采样”的证据链。先写清输入的shape、条件变量和随机来源，再说明生成式模型如何改变样本；任何结论都必须能由保存的中间量重算，不能只凭最终图像是否好看判断。

最小实验固定四到八个样本、参数初值和随机种子，先预测本节变更后最早发生变化的密度、责任度、ELBO、梯度、噪声或采样轨迹。针对“2.4.1 新数据的生成”，验收条件是“训练样本、拟合参数、对数似然和新生成样本必须分开；参数只能由训练集估计，评价不能回读答案。”。若第一条预期证据没有变化，就停止扩大模型，从原始样本与空优化器状态重放。

### 2.4.2 概率的计算

把“2.4.2 概率的计算”放回“数据 -> 分布假设 -> 目标函数 -> 参数更新 -> 独立采样”的证据链。先写清输入的shape、条件变量和随机来源，再说明总体如何改变似然；任何结论都必须能由保存的中间量重算，不能只凭最终图像是否好看判断。

最小实验固定四到八个样本、参数初值和随机种子，先预测本节变更后最早发生变化的密度、责任度、ELBO、梯度、噪声或采样轨迹。针对“2.4.2 概率的计算”，验收条件是“训练样本、拟合参数、对数似然和新生成样本必须分开；参数只能由训练集估计，评价不能回读答案。”。若第一条预期证据没有变化，就停止扩大模型，从原始样本与空优化器状态重放。

## 七类实现证据

### 数据合同

数据合同要求保留原始输入、shape、随机种子、参数版本和关键中间量。正常路径与故障路径只改变一个条件，并在运行前写下首个应分叉的观测值。对步骤2 最大似然估计而言，必须同时核对数值、方向、归一化条件和边界样本，避免一个聚合指标掩盖局部错误。

回归时从完全相同的初值运行两次：第一次关闭故障得到基线，第二次只注入一种错位。检测器要在密度、责任度、ELBO、梯度、噪声或单步采样处报警；若直到最终图片才发现差异，说明证据粒度仍然不足。

### 分布合同

分布合同要求保留原始输入、shape、随机种子、参数版本和关键中间量。正常路径与故障路径只改变一个条件，并在运行前写下首个应分叉的观测值。对步骤2 最大似然估计而言，必须同时核对数值、方向、归一化条件和边界样本，避免一个聚合指标掩盖局部错误。

回归时从完全相同的初值运行两次：第一次关闭故障得到基线，第二次只注入一种错位。检测器要在密度、责任度、ELBO、梯度、噪声或单步采样处报警；若直到最终图片才发现差异，说明证据粒度仍然不足。

### 目标合同

目标合同要求保留原始输入、shape、随机种子、参数版本和关键中间量。正常路径与故障路径只改变一个条件，并在运行前写下首个应分叉的观测值。对步骤2 最大似然估计而言，必须同时核对数值、方向、归一化条件和边界样本，避免一个聚合指标掩盖局部错误。

回归时从完全相同的初值运行两次：第一次关闭故障得到基线，第二次只注入一种错位。检测器要在密度、责任度、ELBO、梯度、噪声或单步采样处报警；若直到最终图片才发现差异，说明证据粒度仍然不足。

### 更新合同

更新合同要求保留原始输入、shape、随机种子、参数版本和关键中间量。正常路径与故障路径只改变一个条件，并在运行前写下首个应分叉的观测值。对步骤2 最大似然估计而言，必须同时核对数值、方向、归一化条件和边界样本，避免一个聚合指标掩盖局部错误。

回归时从完全相同的初值运行两次：第一次关闭故障得到基线，第二次只注入一种错位。检测器要在密度、责任度、ELBO、梯度、噪声或单步采样处报警；若直到最终图片才发现差异，说明证据粒度仍然不足。

### 采样合同

采样合同要求保留原始输入、shape、随机种子、参数版本和关键中间量。正常路径与故障路径只改变一个条件，并在运行前写下首个应分叉的观测值。对步骤2 最大似然估计而言，必须同时核对数值、方向、归一化条件和边界样本，避免一个聚合指标掩盖局部错误。

回归时从完全相同的初值运行两次：第一次关闭故障得到基线，第二次只注入一种错位。检测器要在密度、责任度、ELBO、梯度、噪声或单步采样处报警；若直到最终图片才发现差异，说明证据粒度仍然不足。

### 评价合同

评价合同要求保留原始输入、shape、随机种子、参数版本和关键中间量。正常路径与故障路径只改变一个条件，并在运行前写下首个应分叉的观测值。对步骤2 最大似然估计而言，必须同时核对数值、方向、归一化条件和边界样本，避免一个聚合指标掩盖局部错误。

回归时从完全相同的初值运行两次：第一次关闭故障得到基线，第二次只注入一种错位。检测器要在密度、责任度、ELBO、梯度、噪声或单步采样处报警；若直到最终图片才发现差异，说明证据粒度仍然不足。

### 回退合同

回退合同要求保留原始输入、shape、随机种子、参数版本和关键中间量。正常路径与故障路径只改变一个条件，并在运行前写下首个应分叉的观测值。对步骤2 最大似然估计而言，必须同时核对数值、方向、归一化条件和边界样本，避免一个聚合指标掩盖局部错误。

回归时从完全相同的初值运行两次：第一次关闭故障得到基线，第二次只注入一种错位。检测器要在密度、责任度、ELBO、梯度、噪声或单步采样处报警；若直到最终图片才发现差异，说明证据粒度仍然不足。

## 常见失败与边界

<Callout type="trap">
  **把好看的样本当成正确性证明：**
  单个随机种子、训练数据记忆或评价泄漏都能制造漂亮结果；必须核对密度、目标、梯度与独立采样。
</Callout>

<Callout type="trap">
  **shape和参数版本错位：**
  广播可能让错误计算继续运行，EM新旧参数混用或扩散时间步错位也可能让损失下降但目标失真。
</Callout>

<Callout type="tip">
  **最小重放顺序：**
  先用一维分布或两三个像素手算整条链，再扩大维度与网络；每次只放大一个变量。
</Callout>

## 本章回顾

把观测数据看作未知总体的样本，以似然和对数似然估计正态分布参数，再检验生成与概率计算。验收底线是：训练样本、拟合参数、对数似然和新生成样本必须分开；参数只能由训练集估计，评价不能回读答案。。掌握标准是能从固定数据预测首个变化、手算最小分布、复现实验、解释失败样本，并证明独立评价不读取训练状态。

<Glossary>

<GlossaryItem term="生成式模型">
  “生成式模型”对应证据链第1站。记录定义、条件变量、shape、参数版本、数值容差和失败边界；只展示最终生成结果不足以证明这一环节正确。
</GlossaryItem>

<GlossaryItem term="总体">
  “总体”对应证据链第2站。记录定义、条件变量、shape、参数版本、数值容差和失败边界；只展示最终生成结果不足以证明这一环节正确。
</GlossaryItem>

<GlossaryItem term="样本">
  “样本”对应证据链第3站。记录定义、条件变量、shape、参数版本、数值容差和失败边界；只展示最终生成结果不足以证明这一环节正确。
</GlossaryItem>

<GlossaryItem term="似然">
  “似然”对应证据链第4站。记录定义、条件变量、shape、参数版本、数值容差和失败边界；只展示最终生成结果不足以证明这一环节正确。
</GlossaryItem>

<GlossaryItem term="对数似然">
  “对数似然”对应证据链第5站。记录定义、条件变量、shape、参数版本、数值容差和失败边界；只展示最终生成结果不足以证明这一环节正确。
</GlossaryItem>

<GlossaryItem term="最大似然估计">
  “最大似然估计”对应证据链第6站。记录定义、条件变量、shape、参数版本、数值容差和失败边界；只展示最终生成结果不足以证明这一环节正确。
</GlossaryItem>

</Glossary>

<Exercises>

1. 如何为步骤2 最大似然估计建立可手算、可重放的最小概率链？

<Answer>
  选择一维或二维小样本，固定分布、参数初值和随机种子，逐步写出密度、目标、梯度、更新前后参数与采样值，再从原始输入独立重放。
</Answer>

2. 如何证明“训练样本、拟合参数、对数似然和新生成样本必须分开；参数只能由训练集估计，评价不能回读答案。”不是由单个漂亮样本碰巧满足？

<Answer>
  同时运行正常路径和只破坏一个shape、参数版本、KL项、时间步或随机噪声的失败路径。检测器必须在预测的首个中间量区分两者，并跨固定评价种子报告分布统计。
</Answer>

3. 修复步骤2 最大似然估计后应按什么顺序回归？

<Answer>
  清空批次缓存、梯度、优化器和采样状态，从固定配置重放：先查数据与shape，再查密度和目标，然后查梯度与参数版本，最后才跑长期训练和独立采样。
</Answer>

</Exercises>

<Attribution
  adaptedFrom="斋藤康毅著、郑明智译《深度学习入门5：生成模型》，人民邮电出版社，2025年6月，278页，ISBN 9787115669773；原版《ゼロから作るDeep Learning ❺ ―生成モデル編》，O'Reilly Japan，2024年4月10日，336页，ISBN 9784814400591 · 基于官方10个步骤、47个一级节、120个二级节、4个附录与14个附录节完整目录的独立教学重构"
  adaptedUrl="https://www.oreilly.co.jp/books/9784814400591/"
/>
