---
title: "步骤9 扩散模型的实现"
type: B
section: "步骤9 扩散模型的实现"
order: 9
description: "步骤9 扩散模型的实现逐项覆盖权威目录，以公式、最小张量、交互实验和失败对照重建生成模型证据链。"
demo: true
math: true
sourceUrl: "https://www.oreilly.co.jp/books/9784814400591/"
draft: false
---

import {
  Dlg09DiffusionImplementationMapLab,
  Dlg09DiffusionImplementationExperimentLab,
  Dlg09DiffusionImplementationEvidenceLab,
} from "@/components/mdx/deep-learning-gen-models/diagrams/dlg-09-diffusion-implementation";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释步骤9 扩散模型的实现中的分布假设、目标函数、参数更新与采样边界
- 能推导并计算四个核心方程中的密度、似然、ELBO、梯度或噪声目标
- 能复现三个互动视图，并定位第一条密度、责任度、梯度或采样证据分叉
- 能判断“U-Net各尺度shape、时间嵌入广播、随机时间步、噪声标签和逆向方差必须在最小批次上逐项断言。”是否成立并设计单一故障样本

</Objectives>

## 为什么先冻结概率合同

<Term def="U-Net是本页第1个证据坐标，用于连接分布、目标、更新、采样与失败边界。">U-Net</Term>、<Term def="跳跃连接是本页第2个证据坐标，用于连接分布、目标、更新、采样与失败边界。">跳跃连接</Term>、<Term def="位置编码是本页第3个证据坐标，用于连接分布、目标、更新、采样与失败边界。">位置编码</Term>、<Term def="Diffuser是本页第4个证据坐标，用于连接分布、目标、更新、采样与失败边界。">Diffuser</Term>、<Term def="单步去噪是本页第5个证据坐标，用于连接分布、目标、更新、采样与失败边界。">单步去噪</Term>、<Term def="训练循环是本页第6个证据坐标，用于连接分布、目标、更新、采样与失败边界。">训练循环</Term>
共同构成本页坐标。实现带时间嵌入的U-Net、前向加噪和逐步去噪，把理论中的张量、日程与采样边界落到代码。

生成模型同时包含数据分布、随机采样、优化目标和参数化近似。结果看似合理，仍可能来自训练测试泄漏、错误归一化、广播shape、旧参数责任度、未停止的梯度或随机种子挑选。实验前必须冻结数据版本、预处理、shape、参数初值、优化预算、随机种子和评价协议。

本页遵循“声明联合分布 -> 写条件分解 -> 手算最小样本 -> 运行单变量实验 -> 独立重算目标 -> 注入失败并重放”。第一处分叉出现后停止扩大训练，从原始样本和空派生状态复现，不能用最终图片替代过程证据。

## 数学骨架

$$
PE(t,2i)=\sin(t/10000^{2i/d})
$$

$$
PE(t,2i+1)=\cos(t/10000^{2i/d})
$$

$$
\mathbf{x}_t=\sqrt{\bar{\alpha}_t}\mathbf{x}_0+\sqrt{1-\bar{\alpha}_t}\boldsymbol{\epsilon}
$$

$$
\mathbf{x}_{t-1}=\mu_\theta(\mathbf{x}_t,t)+\sigma_t\mathbf{z}
$$

每个方程进入实现前都声明求和或期望的轴、张量shape、条件变量、参数版本和随机来源。概率必须归一，方差必须为正，目标中的常量项能否省略要说明；采样分支和可微分支应显式区分。

## 可复现证据工作表

第一栏保存数据版本、样本索引、原始值、预处理输出、批次shape和随机种子。第二栏记录分布名称、条件变量、参数版本、密度或对数密度、归一化轴与数值稳定策略。这样才能判断问题来自数据、分布假设还是实现。

第三栏保存更新前参数、每个损失项、梯度范数、优化器状态和更新后参数。潜变量模型还要记录后验参数、采样噪声、责任度或时间步。第四栏以冻结模型和未见种子重做采样与评价，严禁复用训练期缓存。

失败对照只改变一个条件，例如交换样本轴与特征轴、在E步骤使用新参数、漏掉KL项、让目标噪声错位或在引导时混用不同时间步。检测器应在预先声明的中间证据报警；若只能从最终视觉质量发现问题，工作表仍不合格。

## 权威目录逐项讲解

### 步骤9 扩散模型的实现

把“步骤9 扩散模型的实现”放回“数据 -> 分布假设 -> 目标函数 -> 参数更新 -> 独立采样”的证据链。先写清输入的shape、条件变量和随机来源，再说明U-Net如何改变位置编码；任何结论都必须能由保存的中间量重算，不能只凭最终图像是否好看判断。

最小实验固定四到八个样本、参数初值和随机种子，先预测本节变更后最早发生变化的密度、责任度、ELBO、梯度、噪声或采样轨迹。针对“步骤9 扩散模型的实现”，验收条件是“U-Net各尺度shape、时间嵌入广播、随机时间步、噪声标签和逆向方差必须在最小批次上逐项断言。”。若第一条预期证据没有变化，就停止扩大模型，从原始样本与空优化器状态重放。

### 9.1 U-Net

把“9.1 U-Net”放回“数据 -> 分布假设 -> 目标函数 -> 参数更新 -> 独立采样”的证据链。先写清输入的shape、条件变量和随机来源，再说明跳跃连接如何改变Diffuser；任何结论都必须能由保存的中间量重算，不能只凭最终图像是否好看判断。

最小实验固定四到八个样本、参数初值和随机种子，先预测本节变更后最早发生变化的密度、责任度、ELBO、梯度、噪声或采样轨迹。针对“9.1 U-Net”，验收条件是“U-Net各尺度shape、时间嵌入广播、随机时间步、噪声标签和逆向方差必须在最小批次上逐项断言。”。若第一条预期证据没有变化，就停止扩大模型，从原始样本与空优化器状态重放。

### 9.1.1 什么是U-Net

把“9.1.1 什么是U-Net”放回“数据 -> 分布假设 -> 目标函数 -> 参数更新 -> 独立采样”的证据链。先写清输入的shape、条件变量和随机来源，再说明位置编码如何改变单步去噪；任何结论都必须能由保存的中间量重算，不能只凭最终图像是否好看判断。

最小实验固定四到八个样本、参数初值和随机种子，先预测本节变更后最早发生变化的密度、责任度、ELBO、梯度、噪声或采样轨迹。针对“9.1.1 什么是U-Net”，验收条件是“U-Net各尺度shape、时间嵌入广播、随机时间步、噪声标签和逆向方差必须在最小批次上逐项断言。”。若第一条预期证据没有变化，就停止扩大模型，从原始样本与空优化器状态重放。

### 9.1.2 U-Net的实现

把“9.1.2 U-Net的实现”放回“数据 -> 分布假设 -> 目标函数 -> 参数更新 -> 独立采样”的证据链。先写清输入的shape、条件变量和随机来源，再说明Diffuser如何改变训练循环；任何结论都必须能由保存的中间量重算，不能只凭最终图像是否好看判断。

最小实验固定四到八个样本、参数初值和随机种子，先预测本节变更后最早发生变化的密度、责任度、ELBO、梯度、噪声或采样轨迹。针对“9.1.2 U-Net的实现”，验收条件是“U-Net各尺度shape、时间嵌入广播、随机时间步、噪声标签和逆向方差必须在最小批次上逐项断言。”。若第一条预期证据没有变化，就停止扩大模型，从原始样本与空优化器状态重放。

### 9.2 正弦波位置编码

把“9.2 正弦波位置编码”放回“数据 -> 分布假设 -> 目标函数 -> 参数更新 -> 独立采样”的证据链。先写清输入的shape、条件变量和随机来源，再说明单步去噪如何改变U-Net；任何结论都必须能由保存的中间量重算，不能只凭最终图像是否好看判断。

最小实验固定四到八个样本、参数初值和随机种子，先预测本节变更后最早发生变化的密度、责任度、ELBO、梯度、噪声或采样轨迹。针对“9.2 正弦波位置编码”，验收条件是“U-Net各尺度shape、时间嵌入广播、随机时间步、噪声标签和逆向方差必须在最小批次上逐项断言。”。若第一条预期证据没有变化，就停止扩大模型，从原始样本与空优化器状态重放。

### 9.2.1 什么是正弦波位置编码

把“9.2.1 什么是正弦波位置编码”放回“数据 -> 分布假设 -> 目标函数 -> 参数更新 -> 独立采样”的证据链。先写清输入的shape、条件变量和随机来源，再说明训练循环如何改变跳跃连接；任何结论都必须能由保存的中间量重算，不能只凭最终图像是否好看判断。

最小实验固定四到八个样本、参数初值和随机种子，先预测本节变更后最早发生变化的密度、责任度、ELBO、梯度、噪声或采样轨迹。针对“9.2.1 什么是正弦波位置编码”，验收条件是“U-Net各尺度shape、时间嵌入广播、随机时间步、噪声标签和逆向方差必须在最小批次上逐项断言。”。若第一条预期证据没有变化，就停止扩大模型，从原始样本与空优化器状态重放。

### 9.2.2 正弦波位置编码的实现

把“9.2.2 正弦波位置编码的实现”放回“数据 -> 分布假设 -> 目标函数 -> 参数更新 -> 独立采样”的证据链。先写清输入的shape、条件变量和随机来源，再说明U-Net如何改变位置编码；任何结论都必须能由保存的中间量重算，不能只凭最终图像是否好看判断。

最小实验固定四到八个样本、参数初值和随机种子，先预测本节变更后最早发生变化的密度、责任度、ELBO、梯度、噪声或采样轨迹。针对“9.2.2 正弦波位置编码的实现”，验收条件是“U-Net各尺度shape、时间嵌入广播、随机时间步、噪声标签和逆向方差必须在最小批次上逐项断言。”。若第一条预期证据没有变化，就停止扩大模型，从原始样本与空优化器状态重放。

### 9.2.3 组合到U-Net中

把“9.2.3 组合到U-Net中”放回“数据 -> 分布假设 -> 目标函数 -> 参数更新 -> 独立采样”的证据链。先写清输入的shape、条件变量和随机来源，再说明跳跃连接如何改变Diffuser；任何结论都必须能由保存的中间量重算，不能只凭最终图像是否好看判断。

最小实验固定四到八个样本、参数初值和随机种子，先预测本节变更后最早发生变化的密度、责任度、ELBO、梯度、噪声或采样轨迹。针对“9.2.3 组合到U-Net中”，验收条件是“U-Net各尺度shape、时间嵌入广播、随机时间步、噪声标签和逆向方差必须在最小批次上逐项断言。”。若第一条预期证据没有变化，就停止扩大模型，从原始样本与空优化器状态重放。

### 9.3 扩散过程

把“9.3 扩散过程”放回“数据 -> 分布假设 -> 目标函数 -> 参数更新 -> 独立采样”的证据链。先写清输入的shape、条件变量和随机来源，再说明位置编码如何改变单步去噪；任何结论都必须能由保存的中间量重算，不能只凭最终图像是否好看判断。

最小实验固定四到八个样本、参数初值和随机种子，先预测本节变更后最早发生变化的密度、责任度、ELBO、梯度、噪声或采样轨迹。针对“9.3 扩散过程”，验收条件是“U-Net各尺度shape、时间嵌入广播、随机时间步、噪声标签和逆向方差必须在最小批次上逐项断言。”。若第一条预期证据没有变化，就停止扩大模型，从原始样本与空优化器状态重放。

### 9.3.1 从 $q(x_t \mid x_{t-1})$ 采样

把“9.3.1 从 $q(x_t \mid x_{t-1})$ 采样”放回“数据 -> 分布假设 -> 目标函数 -> 参数更新 -> 独立采样”的证据链。先写清输入的shape、条件变量和随机来源，再说明Diffuser如何改变训练循环；任何结论都必须能由保存的中间量重算，不能只凭最终图像是否好看判断。

最小实验固定四到八个样本、参数初值和随机种子，先预测本节变更后最早发生变化的密度、责任度、ELBO、梯度、噪声或采样轨迹。针对“9.3.1 从 $q(x_t \mid x_{t-1})$ 采样”，验收条件是“U-Net各尺度shape、时间嵌入广播、随机时间步、噪声标签和逆向方差必须在最小批次上逐项断言。”。若第一条预期证据没有变化，就停止扩大模型，从原始样本与空优化器状态重放。

### 9.3.2 图像的扩散过程

把“9.3.2 图像的扩散过程”放回“数据 -> 分布假设 -> 目标函数 -> 参数更新 -> 独立采样”的证据链。先写清输入的shape、条件变量和随机来源，再说明单步去噪如何改变U-Net；任何结论都必须能由保存的中间量重算，不能只凭最终图像是否好看判断。

最小实验固定四到八个样本、参数初值和随机种子，先预测本节变更后最早发生变化的密度、责任度、ELBO、梯度、噪声或采样轨迹。针对“9.3.2 图像的扩散过程”，验收条件是“U-Net各尺度shape、时间嵌入广播、随机时间步、噪声标签和逆向方差必须在最小批次上逐项断言。”。若第一条预期证据没有变化，就停止扩大模型，从原始样本与空优化器状态重放。

### 9.3.3 从q(x_t | x_0)采样

把“9.3.3 从q(x_t | x_0)采样”放回“数据 -> 分布假设 -> 目标函数 -> 参数更新 -> 独立采样”的证据链。先写清输入的shape、条件变量和随机来源，再说明训练循环如何改变跳跃连接；任何结论都必须能由保存的中间量重算，不能只凭最终图像是否好看判断。

最小实验固定四到八个样本、参数初值和随机种子，先预测本节变更后最早发生变化的密度、责任度、ELBO、梯度、噪声或采样轨迹。针对“9.3.3 从q(x_t | x_0)采样”，验收条件是“U-Net各尺度shape、时间嵌入广播、随机时间步、噪声标签和逆向方差必须在最小批次上逐项断言。”。若第一条预期证据没有变化，就停止扩大模型，从原始样本与空优化器状态重放。

### 9.3.4 Diffuser类的实现

把“9.3.4 Diffuser类的实现”放回“数据 -> 分布假设 -> 目标函数 -> 参数更新 -> 独立采样”的证据链。先写清输入的shape、条件变量和随机来源，再说明U-Net如何改变位置编码；任何结论都必须能由保存的中间量重算，不能只凭最终图像是否好看判断。

最小实验固定四到八个样本、参数初值和随机种子，先预测本节变更后最早发生变化的密度、责任度、ELBO、梯度、噪声或采样轨迹。针对“9.3.4 Diffuser类的实现”，验收条件是“U-Net各尺度shape、时间嵌入广播、随机时间步、噪声标签和逆向方差必须在最小批次上逐项断言。”。若第一条预期证据没有变化，就停止扩大模型，从原始样本与空优化器状态重放。

### 9.4 数据生成

把“9.4 数据生成”放回“数据 -> 分布假设 -> 目标函数 -> 参数更新 -> 独立采样”的证据链。先写清输入的shape、条件变量和随机来源，再说明跳跃连接如何改变Diffuser；任何结论都必须能由保存的中间量重算，不能只凭最终图像是否好看判断。

最小实验固定四到八个样本、参数初值和随机种子，先预测本节变更后最早发生变化的密度、责任度、ELBO、梯度、噪声或采样轨迹。针对“9.4 数据生成”，验收条件是“U-Net各尺度shape、时间嵌入广播、随机时间步、噪声标签和逆向方差必须在最小批次上逐项断言。”。若第一条预期证据没有变化，就停止扩大模型，从原始样本与空优化器状态重放。

### 9.4.1 单步去噪处理

把“9.4.1 单步去噪处理”放回“数据 -> 分布假设 -> 目标函数 -> 参数更新 -> 独立采样”的证据链。先写清输入的shape、条件变量和随机来源，再说明位置编码如何改变单步去噪；任何结论都必须能由保存的中间量重算，不能只凭最终图像是否好看判断。

最小实验固定四到八个样本、参数初值和随机种子，先预测本节变更后最早发生变化的密度、责任度、ELBO、梯度、噪声或采样轨迹。针对“9.4.1 单步去噪处理”，验收条件是“U-Net各尺度shape、时间嵌入广播、随机时间步、噪声标签和逆向方差必须在最小批次上逐项断言。”。若第一条预期证据没有变化，就停止扩大模型，从原始样本与空优化器状态重放。

### 9.4.2 数据生成的实现

把“9.4.2 数据生成的实现”放回“数据 -> 分布假设 -> 目标函数 -> 参数更新 -> 独立采样”的证据链。先写清输入的shape、条件变量和随机来源，再说明Diffuser如何改变训练循环；任何结论都必须能由保存的中间量重算，不能只凭最终图像是否好看判断。

最小实验固定四到八个样本、参数初值和随机种子，先预测本节变更后最早发生变化的密度、责任度、ELBO、梯度、噪声或采样轨迹。针对“9.4.2 数据生成的实现”，验收条件是“U-Net各尺度shape、时间嵌入广播、随机时间步、噪声标签和逆向方差必须在最小批次上逐项断言。”。若第一条预期证据没有变化，就停止扩大模型，从原始样本与空优化器状态重放。

### 9.5 扩散模型的学习（实现篇）

把“9.5 扩散模型的学习（实现篇）”放回“数据 -> 分布假设 -> 目标函数 -> 参数更新 -> 独立采样”的证据链。先写清输入的shape、条件变量和随机来源，再说明单步去噪如何改变U-Net；任何结论都必须能由保存的中间量重算，不能只凭最终图像是否好看判断。

最小实验固定四到八个样本、参数初值和随机种子，先预测本节变更后最早发生变化的密度、责任度、ELBO、梯度、噪声或采样轨迹。针对“9.5 扩散模型的学习（实现篇）”，验收条件是“U-Net各尺度shape、时间嵌入广播、随机时间步、噪声标签和逆向方差必须在最小批次上逐项断言。”。若第一条预期证据没有变化，就停止扩大模型，从原始样本与空优化器状态重放。

### 9.5.1 扩散模型的学习代码

把“9.5.1 扩散模型的学习代码”放回“数据 -> 分布假设 -> 目标函数 -> 参数更新 -> 独立采样”的证据链。先写清输入的shape、条件变量和随机来源，再说明训练循环如何改变跳跃连接；任何结论都必须能由保存的中间量重算，不能只凭最终图像是否好看判断。

最小实验固定四到八个样本、参数初值和随机种子，先预测本节变更后最早发生变化的密度、责任度、ELBO、梯度、噪声或采样轨迹。针对“9.5.1 扩散模型的学习代码”，验收条件是“U-Net各尺度shape、时间嵌入广播、随机时间步、噪声标签和逆向方差必须在最小批次上逐项断言。”。若第一条预期证据没有变化，就停止扩大模型，从原始样本与空优化器状态重放。

### 9.5.2 学习结果

把“9.5.2 学习结果”放回“数据 -> 分布假设 -> 目标函数 -> 参数更新 -> 独立采样”的证据链。先写清输入的shape、条件变量和随机来源，再说明U-Net如何改变位置编码；任何结论都必须能由保存的中间量重算，不能只凭最终图像是否好看判断。

最小实验固定四到八个样本、参数初值和随机种子，先预测本节变更后最早发生变化的密度、责任度、ELBO、梯度、噪声或采样轨迹。针对“9.5.2 学习结果”，验收条件是“U-Net各尺度shape、时间嵌入广播、随机时间步、噪声标签和逆向方差必须在最小批次上逐项断言。”。若第一条预期证据没有变化，就停止扩大模型，从原始样本与空优化器状态重放。

## 七类实现证据

### 数据合同

数据合同要求保留原始输入、shape、随机种子、参数版本和关键中间量。正常路径与故障路径只改变一个条件，并在运行前写下首个应分叉的观测值。对步骤9 扩散模型的实现而言，必须同时核对数值、方向、归一化条件和边界样本，避免一个聚合指标掩盖局部错误。

回归时从完全相同的初值运行两次：第一次关闭故障得到基线，第二次只注入一种错位。检测器要在密度、责任度、ELBO、梯度、噪声或单步采样处报警；若直到最终图片才发现差异，说明证据粒度仍然不足。

### 分布合同

分布合同要求保留原始输入、shape、随机种子、参数版本和关键中间量。正常路径与故障路径只改变一个条件，并在运行前写下首个应分叉的观测值。对步骤9 扩散模型的实现而言，必须同时核对数值、方向、归一化条件和边界样本，避免一个聚合指标掩盖局部错误。

回归时从完全相同的初值运行两次：第一次关闭故障得到基线，第二次只注入一种错位。检测器要在密度、责任度、ELBO、梯度、噪声或单步采样处报警；若直到最终图片才发现差异，说明证据粒度仍然不足。

### 目标合同

目标合同要求保留原始输入、shape、随机种子、参数版本和关键中间量。正常路径与故障路径只改变一个条件，并在运行前写下首个应分叉的观测值。对步骤9 扩散模型的实现而言，必须同时核对数值、方向、归一化条件和边界样本，避免一个聚合指标掩盖局部错误。

回归时从完全相同的初值运行两次：第一次关闭故障得到基线，第二次只注入一种错位。检测器要在密度、责任度、ELBO、梯度、噪声或单步采样处报警；若直到最终图片才发现差异，说明证据粒度仍然不足。

### 更新合同

更新合同要求保留原始输入、shape、随机种子、参数版本和关键中间量。正常路径与故障路径只改变一个条件，并在运行前写下首个应分叉的观测值。对步骤9 扩散模型的实现而言，必须同时核对数值、方向、归一化条件和边界样本，避免一个聚合指标掩盖局部错误。

回归时从完全相同的初值运行两次：第一次关闭故障得到基线，第二次只注入一种错位。检测器要在密度、责任度、ELBO、梯度、噪声或单步采样处报警；若直到最终图片才发现差异，说明证据粒度仍然不足。

### 采样合同

采样合同要求保留原始输入、shape、随机种子、参数版本和关键中间量。正常路径与故障路径只改变一个条件，并在运行前写下首个应分叉的观测值。对步骤9 扩散模型的实现而言，必须同时核对数值、方向、归一化条件和边界样本，避免一个聚合指标掩盖局部错误。

回归时从完全相同的初值运行两次：第一次关闭故障得到基线，第二次只注入一种错位。检测器要在密度、责任度、ELBO、梯度、噪声或单步采样处报警；若直到最终图片才发现差异，说明证据粒度仍然不足。

### 评价合同

评价合同要求保留原始输入、shape、随机种子、参数版本和关键中间量。正常路径与故障路径只改变一个条件，并在运行前写下首个应分叉的观测值。对步骤9 扩散模型的实现而言，必须同时核对数值、方向、归一化条件和边界样本，避免一个聚合指标掩盖局部错误。

回归时从完全相同的初值运行两次：第一次关闭故障得到基线，第二次只注入一种错位。检测器要在密度、责任度、ELBO、梯度、噪声或单步采样处报警；若直到最终图片才发现差异，说明证据粒度仍然不足。

### 回退合同

回退合同要求保留原始输入、shape、随机种子、参数版本和关键中间量。正常路径与故障路径只改变一个条件，并在运行前写下首个应分叉的观测值。对步骤9 扩散模型的实现而言，必须同时核对数值、方向、归一化条件和边界样本，避免一个聚合指标掩盖局部错误。

回归时从完全相同的初值运行两次：第一次关闭故障得到基线，第二次只注入一种错位。检测器要在密度、责任度、ELBO、梯度、噪声或单步采样处报警；若直到最终图片才发现差异，说明证据粒度仍然不足。

## 三步交互实验

先预测单变量变化会最早影响概率密度、对数似然、责任度、ELBO、梯度、噪声还是采样轨迹，再切换基线、边界和故障模式。

<Stepper>
  <Step title="画出分布与参数图">
    标出观测、潜变量、条件分布、参数、目标和采样方向，逐边注明shape与参数版本。
    <Dlg09DiffusionImplementationMapLab />
  </Step>
  <Step title="运行单变量实验">
    固定其余条件，只改变噪声强度、潜变量维度、优化步长或采样步数中的一个变量。
    <Dlg09DiffusionImplementationExperimentLab />
  </Step>
  <Step title="核对独立证据">
    比较正常与故障路径、训练目标与独立重算，定位第一条不一致并给出回退点。
    <Dlg09DiffusionImplementationEvidenceLab />
  </Step>
</Stepper>

## 常见失败与边界

<Callout type="trap">
  **把好看的样本当成正确性证明：**
  单个随机种子、训练数据记忆或评价泄漏都能制造漂亮结果；必须核对密度、目标、梯度与独立采样。
</Callout>

<Callout type="trap">
  **shape和参数版本错位：**
  广播可能让错误计算继续运行，EM新旧参数混用或扩散时间步错位也可能让损失下降但目标失真。
</Callout>

<Callout type="tip">
  **最小重放顺序：**
  先用一维分布或两三个像素手算整条链，再扩大维度与网络；每次只放大一个变量。
</Callout>

## 本章回顾

实现带时间嵌入的U-Net、前向加噪和逐步去噪，把理论中的张量、日程与采样边界落到代码。验收底线是：U-Net各尺度shape、时间嵌入广播、随机时间步、噪声标签和逆向方差必须在最小批次上逐项断言。。掌握标准是能从固定数据预测首个变化、手算最小分布、复现实验、解释失败样本，并证明独立评价不读取训练状态。

<Glossary>

<GlossaryItem term="U-Net">
  “U-Net”对应证据链第1站。记录定义、条件变量、shape、参数版本、数值容差和失败边界；只展示最终生成结果不足以证明这一环节正确。
</GlossaryItem>

<GlossaryItem term="跳跃连接">
  “跳跃连接”对应证据链第2站。记录定义、条件变量、shape、参数版本、数值容差和失败边界；只展示最终生成结果不足以证明这一环节正确。
</GlossaryItem>

<GlossaryItem term="位置编码">
  “位置编码”对应证据链第3站。记录定义、条件变量、shape、参数版本、数值容差和失败边界；只展示最终生成结果不足以证明这一环节正确。
</GlossaryItem>

<GlossaryItem term="Diffuser">
  “Diffuser”对应证据链第4站。记录定义、条件变量、shape、参数版本、数值容差和失败边界；只展示最终生成结果不足以证明这一环节正确。
</GlossaryItem>

<GlossaryItem term="单步去噪">
  “单步去噪”对应证据链第5站。记录定义、条件变量、shape、参数版本、数值容差和失败边界；只展示最终生成结果不足以证明这一环节正确。
</GlossaryItem>

<GlossaryItem term="训练循环">
  “训练循环”对应证据链第6站。记录定义、条件变量、shape、参数版本、数值容差和失败边界；只展示最终生成结果不足以证明这一环节正确。
</GlossaryItem>

</Glossary>

<Exercises>

1. 如何为步骤9 扩散模型的实现建立可手算、可重放的最小概率链？

<Answer>
  选择一维或二维小样本，固定分布、参数初值和随机种子，逐步写出密度、目标、梯度、更新前后参数与采样值，再从原始输入独立重放。
</Answer>

2. 如何证明“U-Net各尺度shape、时间嵌入广播、随机时间步、噪声标签和逆向方差必须在最小批次上逐项断言。”不是由单个漂亮样本碰巧满足？

<Answer>
  同时运行正常路径和只破坏一个shape、参数版本、KL项、时间步或随机噪声的失败路径。检测器必须在预测的首个中间量区分两者，并跨固定评价种子报告分布统计。
</Answer>

3. 修复步骤9 扩散模型的实现后应按什么顺序回归？

<Answer>
  清空批次缓存、梯度、优化器和采样状态，从固定配置重放：先查数据与shape，再查密度和目标，然后查梯度与参数版本，最后才跑长期训练和独立采样。
</Answer>

</Exercises>

<Attribution
  adaptedFrom="斋藤康毅著、郑明智译《深度学习入门5：生成模型》，人民邮电出版社，2025年6月，278页，ISBN 9787115669773；原版《ゼロから作るDeep Learning ❺ ―生成モデル編》，O'Reilly Japan，2024年4月10日，336页，ISBN 9784814400591 · 基于官方10个步骤、47个一级节、120个二级节、4个附录与14个附录节完整目录的独立教学重构"
  adaptedUrl="https://www.oreilly.co.jp/books/9784814400591/"
/>
