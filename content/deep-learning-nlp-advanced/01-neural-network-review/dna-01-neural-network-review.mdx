---
title: "第1章 神经网络的复习"
type: B
section: "第1章 神经网络的复习"
order: 1
description: "第1章 神经网络的复习逐项覆盖权威目录，以公式、从零实现、交互实验和失败对照重建核心知识。"
demo: true
math: true
sourceUrl: "https://www.oreilly.co.jp/books/9784873118369/"
draft: true
---

import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第1章 神经网络的复习的对象、数据流、公式与适用边界
- 能推导并计算四个核心方程中的shape、概率、状态或梯度
- 能复现三个互动视图，并定位第一条计数、状态、梯度或对齐证据分叉
- 能判断“每一层的forward与backward必须保持输入输出shape、批量轴和参数梯度一致；最终损失下降不能替代局部梯度与数值梯度核对。”是否成立并设计失败样本

</Objectives>

## 为什么从可观测证据开始

<Term def="张量形状是本页第1个证据坐标，用于连接公式、shape、中间状态与失败边界。">张量形状</Term>、<Term def="Affine层是本页第2个证据坐标，用于连接公式、shape、中间状态与失败边界。">Affine层</Term>、<Term def="Softmax是本页第3个证据坐标，用于连接公式、shape、中间状态与失败边界。">Softmax</Term>、<Term def="交叉熵是本页第4个证据坐标，用于连接公式、shape、中间状态与失败边界。">交叉熵</Term>、<Term def="计算图是本页第5个证据坐标，用于连接公式、shape、中间状态与失败边界。">计算图</Term>、<Term def="梯度检验是本页第6个证据坐标，用于连接公式、shape、中间状态与失败边界。">梯度检验</Term>
共同构成本页坐标。先把矩阵形状、层接口、损失、反向传播和参数更新串成可复现训练闭环，后续所有NLP模型都复用这套层式实现。

自然语言模型容易产生“输出看起来合理”的错觉，但词表错位、广播轴错误、旧隐藏状态、错误采样、训练/推理路径混用都可能暂时给出低损失。实验开始前固定语料切片、词ID映射、批量与时间轴、参数、随机种子、模式和上一版结果；运行前写下首个应变化的中间量。

本页采用“定义对象 -> 写shape与公式 -> 手算最小样本 -> 运行交互实验 -> 比较独立证据 -> 注入失败并重放”的顺序。任何末端改进都要能沿证据链回到输入；首处分叉出现后停止扩大实验，清空缓存、梯度和循环状态，从固定输入重放。

## 数学骨架

$$
Y=XW+b
$$

$$
y_k=\frac{\exp(s_k)}{\sum_j\exp(s_j)}
$$

$$
L=-\frac{1}{N}\sum_{n=1}^{N}\log y_{n,t_n}
$$

$$
\theta_{t+1}=\theta_t-\eta\nabla_{\theta}L
$$

每个方程进入代码前都声明批量轴、词表轴、时间轴和隐藏维。概率要核对归一化轴，查表要核对重复ID梯度，循环状态要核对跨批次边界，反向传播要与中心差分或可手算局部导数比较。

## 可复现证据工作表

第一栏记录原始文本、分词结果、词ID、批量索引和时间偏移，任何预处理变化都生成新的版本号。第二栏逐层写输入shape、参数shape、输出shape与归一化轴；查表层另记重复ID，循环层另记初始状态与截断边界，Attention另记mask覆盖位置。这样能在模型开始训练前发现大多数轴错位。

第三栏保存可独立重算的数值证据：计数模型保留共现行与PPMI项，预测模型保留logit与目标概率，循环模型保留每步隐藏状态与梯度范数，生成模型保留教师强制和自回归两条轨迹。第四栏写通过阈值与失败样本，明确哪一个中间量应最先报警，而不是等最终损失或文本输出暴露问题。

重放时从原始文本重新构造词表和小批量，重新初始化参数、优化器、随机采样器与循环状态；随后按同一顺序计算forward、局部梯度、参数更新和独立评价。若修复只在保留旧缓存时有效，或换一个固定随机种子就消失，应判为状态污染而不是算法改进。

## 核心实现合同

1. **输入合同：** 冻结语料版本、分词与词ID映射；批量、时间、词表和隐藏维不靠上下文猜测。
2. **前向合同：** 保存每层输入输出、shape、概率归一化轴、查表ID、循环状态或对齐权重。
3. **反向合同：** 共享参数与重复ID贡献必须累加；截断只切反向图，padding和无效位置不得贡献梯度。
4. **状态合同：** 训练、验证、生成和独立重放使用明确初始状态；Dropout、采样器和缓存不得跨模式泄漏。
5. **评价合同：** 损失、困惑度、近邻、精确匹配与可视化都从保存的预测重新计算，不读取训练期临时值。
6. **失败合同：** 至少注入一个错误轴、错位ID、旧状态、错误mask或采样泄漏，并证明检测器在首个中间量处报警。

## 权威目录逐项讲解

### 第1章 神经网络的复习

把该小节放回本章主链，先声明输入、输出、状态和评价标准，再用最小样本核对中间证据。实现应能解释为什么得到该结果、在哪个边界失效，以及如何从首个分叉点重放。 本页验收合同是“每一层的forward与backward必须保持输入输出shape、批量轴和参数梯度一致；最终损失下降不能替代局部梯度与数值梯度核对。”针对“第1章 神经网络的复习”，至少保存一组正常样本和一组故意破坏轴、状态、采样或模式的失败样本。

先预测“第1章 神经网络的复习”改变后最早变化的是计数、shape、概率、隐藏状态、梯度还是对齐权重，再运行固定样本。若最终指标改善但第一条预期证据没有出现，应停止并从原始输入、参数、随机种子和空状态完整重放。

### 1.1 数学和Python的复习

把该小节放回本章主链，先声明输入、输出、状态和评价标准，再用最小样本核对中间证据。实现应能解释为什么得到该结果、在哪个边界失效，以及如何从首个分叉点重放。 本页验收合同是“每一层的forward与backward必须保持输入输出shape、批量轴和参数梯度一致；最终损失下降不能替代局部梯度与数值梯度核对。”针对“1.1 数学和Python的复习”，至少保存一组正常样本和一组故意破坏轴、状态、采样或模式的失败样本。

先预测“1.1 数学和Python的复习”改变后最早变化的是计数、shape、概率、隐藏状态、梯度还是对齐权重，再运行固定样本。若最终指标改善但第一条预期证据没有出现，应停止并从原始输入、参数、随机种子和空状态完整重放。

### 1.1.1 向量和矩阵

先在纸上标出每个轴与shape，再执行逐元素运算、广播或矩阵乘积。forward通过后，用上游梯度逐轴还原输入梯度；广播轴必须求和，不能依赖NumPy碰巧给出可运行结果。 本页验收合同是“每一层的forward与backward必须保持输入输出shape、批量轴和参数梯度一致；最终损失下降不能替代局部梯度与数值梯度核对。”针对“1.1.1 向量和矩阵”，至少保存一组正常样本和一组故意破坏轴、状态、采样或模式的失败样本。

先预测“1.1.1 向量和矩阵”改变后最早变化的是计数、shape、概率、隐藏状态、梯度还是对齐权重，再运行固定样本。若最终指标改善但第一条预期证据没有出现，应停止并从原始输入、参数、随机种子和空状态完整重放。

### 1.1.2 矩阵的对应元素的运算

先在纸上标出每个轴与shape，再执行逐元素运算、广播或矩阵乘积。forward通过后，用上游梯度逐轴还原输入梯度；广播轴必须求和，不能依赖NumPy碰巧给出可运行结果。 本页验收合同是“每一层的forward与backward必须保持输入输出shape、批量轴和参数梯度一致；最终损失下降不能替代局部梯度与数值梯度核对。”针对“1.1.2 矩阵的对应元素的运算”，至少保存一组正常样本和一组故意破坏轴、状态、采样或模式的失败样本。

先预测“1.1.2 矩阵的对应元素的运算”改变后最早变化的是计数、shape、概率、隐藏状态、梯度还是对齐权重，再运行固定样本。若最终指标改善但第一条预期证据没有出现，应停止并从原始输入、参数、随机种子和空状态完整重放。

### 1.1.3 广播

先在纸上标出每个轴与shape，再执行逐元素运算、广播或矩阵乘积。forward通过后，用上游梯度逐轴还原输入梯度；广播轴必须求和，不能依赖NumPy碰巧给出可运行结果。 本页验收合同是“每一层的forward与backward必须保持输入输出shape、批量轴和参数梯度一致；最终损失下降不能替代局部梯度与数值梯度核对。”针对“1.1.3 广播”，至少保存一组正常样本和一组故意破坏轴、状态、采样或模式的失败样本。

先预测“1.1.3 广播”改变后最早变化的是计数、shape、概率、隐藏状态、梯度还是对齐权重，再运行固定样本。若最终指标改善但第一条预期证据没有出现，应停止并从原始输入、参数、随机种子和空状态完整重放。

### 1.1.4 向量内积和矩阵乘积

先在纸上标出每个轴与shape，再执行逐元素运算、广播或矩阵乘积。forward通过后，用上游梯度逐轴还原输入梯度；广播轴必须求和，不能依赖NumPy碰巧给出可运行结果。 本页验收合同是“每一层的forward与backward必须保持输入输出shape、批量轴和参数梯度一致；最终损失下降不能替代局部梯度与数值梯度核对。”针对“1.1.4 向量内积和矩阵乘积”，至少保存一组正常样本和一组故意破坏轴、状态、采样或模式的失败样本。

先预测“1.1.4 向量内积和矩阵乘积”改变后最早变化的是计数、shape、概率、隐藏状态、梯度还是对齐权重，再运行固定样本。若最终指标改善但第一条预期证据没有出现，应停止并从原始输入、参数、随机种子和空状态完整重放。

### 1.1.5 矩阵的形状检查

先在纸上标出每个轴与shape，再执行逐元素运算、广播或矩阵乘积。forward通过后，用上游梯度逐轴还原输入梯度；广播轴必须求和，不能依赖NumPy碰巧给出可运行结果。 本页验收合同是“每一层的forward与backward必须保持输入输出shape、批量轴和参数梯度一致；最终损失下降不能替代局部梯度与数值梯度核对。”针对“1.1.5 矩阵的形状检查”，至少保存一组正常样本和一组故意破坏轴、状态、采样或模式的失败样本。

先预测“1.1.5 矩阵的形状检查”改变后最早变化的是计数、shape、概率、隐藏状态、梯度还是对齐权重，再运行固定样本。若最终指标改善但第一条预期证据没有出现，应停止并从原始输入、参数、随机种子和空状态完整重放。

### 1.2 神经网络的推理

把该小节放回本章主链，先声明输入、输出、状态和评价标准，再用最小样本核对中间证据。实现应能解释为什么得到该结果、在哪个边界失效，以及如何从首个分叉点重放。 本页验收合同是“每一层的forward与backward必须保持输入输出shape、批量轴和参数梯度一致；最终损失下降不能替代局部梯度与数值梯度核对。”针对“1.2 神经网络的推理”，至少保存一组正常样本和一组故意破坏轴、状态、采样或模式的失败样本。

先预测“1.2 神经网络的推理”改变后最早变化的是计数、shape、概率、隐藏状态、梯度还是对齐权重，再运行固定样本。若最终指标改善但第一条预期证据没有出现，应停止并从原始输入、参数、随机种子和空状态完整重放。

### 1.2.1 神经网络的推理的全貌图

把该小节放回本章主链，先声明输入、输出、状态和评价标准，再用最小样本核对中间证据。实现应能解释为什么得到该结果、在哪个边界失效，以及如何从首个分叉点重放。 本页验收合同是“每一层的forward与backward必须保持输入输出shape、批量轴和参数梯度一致；最终损失下降不能替代局部梯度与数值梯度核对。”针对“1.2.1 神经网络的推理的全貌图”，至少保存一组正常样本和一组故意破坏轴、状态、采样或模式的失败样本。

先预测“1.2.1 神经网络的推理的全貌图”改变后最早变化的是计数、shape、概率、隐藏状态、梯度还是对齐权重，再运行固定样本。若最终指标改善但第一条预期证据没有出现，应停止并从原始输入、参数、随机种子和空状态完整重放。

### 1.2.2 层的类化及正向传播的实现

把该小节放回本章主链，先声明输入、输出、状态和评价标准，再用最小样本核对中间证据。实现应能解释为什么得到该结果、在哪个边界失效，以及如何从首个分叉点重放。 本页验收合同是“每一层的forward与backward必须保持输入输出shape、批量轴和参数梯度一致；最终损失下降不能替代局部梯度与数值梯度核对。”针对“1.2.2 层的类化及正向传播的实现”，至少保存一组正常样本和一组故意破坏轴、状态、采样或模式的失败样本。

先预测“1.2.2 层的类化及正向传播的实现”改变后最早变化的是计数、shape、概率、隐藏状态、梯度还是对齐权重，再运行固定样本。若最终指标改善但第一条预期证据没有出现，应停止并从原始输入、参数、随机种子和空状态完整重放。

### 1.3 神经网络的学习

把该小节放回本章主链，先声明输入、输出、状态和评价标准，再用最小样本核对中间证据。实现应能解释为什么得到该结果、在哪个边界失效，以及如何从首个分叉点重放。 本页验收合同是“每一层的forward与backward必须保持输入输出shape、批量轴和参数梯度一致；最终损失下降不能替代局部梯度与数值梯度核对。”针对“1.3 神经网络的学习”，至少保存一组正常样本和一组故意破坏轴、状态、采样或模式的失败样本。

先预测“1.3 神经网络的学习”改变后最早变化的是计数、shape、概率、隐藏状态、梯度还是对齐权重，再运行固定样本。若最终指标改善但第一条预期证据没有出现，应停止并从原始输入、参数、随机种子和空状态完整重放。

### 1.3.1 损失函数

把局部函数拆成可手算节点，保存forward输入与输出，backward只接收上游梯度并返回同shape贡献。解析梯度要与中心差分比较相对误差，更新后必须重新forward，禁止复用旧损失。 本页验收合同是“每一层的forward与backward必须保持输入输出shape、批量轴和参数梯度一致；最终损失下降不能替代局部梯度与数值梯度核对。”针对“1.3.1 损失函数”，至少保存一组正常样本和一组故意破坏轴、状态、采样或模式的失败样本。

先预测“1.3.1 损失函数”改变后最早变化的是计数、shape、概率、隐藏状态、梯度还是对齐权重，再运行固定样本。若最终指标改善但第一条预期证据没有出现，应停止并从原始输入、参数、随机种子和空状态完整重放。

### 1.3.2 导数和梯度

把局部函数拆成可手算节点，保存forward输入与输出，backward只接收上游梯度并返回同shape贡献。解析梯度要与中心差分比较相对误差，更新后必须重新forward，禁止复用旧损失。 本页验收合同是“每一层的forward与backward必须保持输入输出shape、批量轴和参数梯度一致；最终损失下降不能替代局部梯度与数值梯度核对。”针对“1.3.2 导数和梯度”，至少保存一组正常样本和一组故意破坏轴、状态、采样或模式的失败样本。

先预测“1.3.2 导数和梯度”改变后最早变化的是计数、shape、概率、隐藏状态、梯度还是对齐权重，再运行固定样本。若最终指标改善但第一条预期证据没有出现，应停止并从原始输入、参数、随机种子和空状态完整重放。

### 1.3.3 链式法则

把局部函数拆成可手算节点，保存forward输入与输出，backward只接收上游梯度并返回同shape贡献。解析梯度要与中心差分比较相对误差，更新后必须重新forward，禁止复用旧损失。 本页验收合同是“每一层的forward与backward必须保持输入输出shape、批量轴和参数梯度一致；最终损失下降不能替代局部梯度与数值梯度核对。”针对“1.3.3 链式法则”，至少保存一组正常样本和一组故意破坏轴、状态、采样或模式的失败样本。

先预测“1.3.3 链式法则”改变后最早变化的是计数、shape、概率、隐藏状态、梯度还是对齐权重，再运行固定样本。若最终指标改善但第一条预期证据没有出现，应停止并从原始输入、参数、随机种子和空状态完整重放。

### 1.3.4 计算图

把局部函数拆成可手算节点，保存forward输入与输出，backward只接收上游梯度并返回同shape贡献。解析梯度要与中心差分比较相对误差，更新后必须重新forward，禁止复用旧损失。 本页验收合同是“每一层的forward与backward必须保持输入输出shape、批量轴和参数梯度一致；最终损失下降不能替代局部梯度与数值梯度核对。”针对“1.3.4 计算图”，至少保存一组正常样本和一组故意破坏轴、状态、采样或模式的失败样本。

先预测“1.3.4 计算图”改变后最早变化的是计数、shape、概率、隐藏状态、梯度还是对齐权重，再运行固定样本。若最终指标改善但第一条预期证据没有出现，应停止并从原始输入、参数、随机种子和空状态完整重放。

### 1.3.5 梯度的推导和反向传播的实现

把局部函数拆成可手算节点，保存forward输入与输出，backward只接收上游梯度并返回同shape贡献。解析梯度要与中心差分比较相对误差，更新后必须重新forward，禁止复用旧损失。 本页验收合同是“每一层的forward与backward必须保持输入输出shape、批量轴和参数梯度一致；最终损失下降不能替代局部梯度与数值梯度核对。”针对“1.3.5 梯度的推导和反向传播的实现”，至少保存一组正常样本和一组故意破坏轴、状态、采样或模式的失败样本。

先预测“1.3.5 梯度的推导和反向传播的实现”改变后最早变化的是计数、shape、概率、隐藏状态、梯度还是对齐权重，再运行固定样本。若最终指标改善但第一条预期证据没有出现，应停止并从原始输入、参数、随机种子和空状态完整重放。

### 1.3.6 权重的更新

把局部函数拆成可手算节点，保存forward输入与输出，backward只接收上游梯度并返回同shape贡献。解析梯度要与中心差分比较相对误差，更新后必须重新forward，禁止复用旧损失。 本页验收合同是“每一层的forward与backward必须保持输入输出shape、批量轴和参数梯度一致；最终损失下降不能替代局部梯度与数值梯度核对。”针对“1.3.6 权重的更新”，至少保存一组正常样本和一组故意破坏轴、状态、采样或模式的失败样本。

先预测“1.3.6 权重的更新”改变后最早变化的是计数、shape、概率、隐藏状态、梯度还是对齐权重，再运行固定样本。若最终指标改善但第一条预期证据没有出现，应停止并从原始输入、参数、随机种子和空状态完整重放。

### 1.4 使用神经网络解决问题

把该小节放回本章主链，先声明输入、输出、状态和评价标准，再用最小样本核对中间证据。实现应能解释为什么得到该结果、在哪个边界失效，以及如何从首个分叉点重放。 本页验收合同是“每一层的forward与backward必须保持输入输出shape、批量轴和参数梯度一致；最终损失下降不能替代局部梯度与数值梯度核对。”针对“1.4 使用神经网络解决问题”，至少保存一组正常样本和一组故意破坏轴、状态、采样或模式的失败样本。

先预测“1.4 使用神经网络解决问题”改变后最早变化的是计数、shape、概率、隐藏状态、梯度还是对齐权重，再运行固定样本。若最终指标改善但第一条预期证据没有出现，应停止并从原始输入、参数、随机种子和空状态完整重放。

### 1.4.1 螺旋状数据集

把该小节放回本章主链，先声明输入、输出、状态和评价标准，再用最小样本核对中间证据。实现应能解释为什么得到该结果、在哪个边界失效，以及如何从首个分叉点重放。 本页验收合同是“每一层的forward与backward必须保持输入输出shape、批量轴和参数梯度一致；最终损失下降不能替代局部梯度与数值梯度核对。”针对“1.4.1 螺旋状数据集”，至少保存一组正常样本和一组故意破坏轴、状态、采样或模式的失败样本。

先预测“1.4.1 螺旋状数据集”改变后最早变化的是计数、shape、概率、隐藏状态、梯度还是对齐权重，再运行固定样本。若最终指标改善但第一条预期证据没有出现，应停止并从原始输入、参数、随机种子和空状态完整重放。

### 1.4.2 神经网络的实现

把该小节放回本章主链，先声明输入、输出、状态和评价标准，再用最小样本核对中间证据。实现应能解释为什么得到该结果、在哪个边界失效，以及如何从首个分叉点重放。 本页验收合同是“每一层的forward与backward必须保持输入输出shape、批量轴和参数梯度一致；最终损失下降不能替代局部梯度与数值梯度核对。”针对“1.4.2 神经网络的实现”，至少保存一组正常样本和一组故意破坏轴、状态、采样或模式的失败样本。

先预测“1.4.2 神经网络的实现”改变后最早变化的是计数、shape、概率、隐藏状态、梯度还是对齐权重，再运行固定样本。若最终指标改善但第一条预期证据没有出现，应停止并从原始输入、参数、随机种子和空状态完整重放。

### 1.4.3 学习用的代码

把该小节放回本章主链，先声明输入、输出、状态和评价标准，再用最小样本核对中间证据。实现应能解释为什么得到该结果、在哪个边界失效，以及如何从首个分叉点重放。 本页验收合同是“每一层的forward与backward必须保持输入输出shape、批量轴和参数梯度一致；最终损失下降不能替代局部梯度与数值梯度核对。”针对“1.4.3 学习用的代码”，至少保存一组正常样本和一组故意破坏轴、状态、采样或模式的失败样本。

先预测“1.4.3 学习用的代码”改变后最早变化的是计数、shape、概率、隐藏状态、梯度还是对齐权重，再运行固定样本。若最终指标改善但第一条预期证据没有出现，应停止并从原始输入、参数、随机种子和空状态完整重放。

### 1.4.4 Trainer类

把该小节放回本章主链，先声明输入、输出、状态和评价标准，再用最小样本核对中间证据。实现应能解释为什么得到该结果、在哪个边界失效，以及如何从首个分叉点重放。 本页验收合同是“每一层的forward与backward必须保持输入输出shape、批量轴和参数梯度一致；最终损失下降不能替代局部梯度与数值梯度核对。”针对“1.4.4 Trainer类”，至少保存一组正常样本和一组故意破坏轴、状态、采样或模式的失败样本。

先预测“1.4.4 Trainer类”改变后最早变化的是计数、shape、概率、隐藏状态、梯度还是对齐权重，再运行固定样本。若最终指标改善但第一条预期证据没有出现，应停止并从原始输入、参数、随机种子和空状态完整重放。

### 1.5 计算的高速化

Embedding前向是按ID取行，反向必须用散射加法处理重复ID。负采样将一个全词表分类变成一正多负的Sigmoid损失，需记录采样分布、随机种子、负样本数和目标排除规则。 本页验收合同是“每一层的forward与backward必须保持输入输出shape、批量轴和参数梯度一致；最终损失下降不能替代局部梯度与数值梯度核对。”针对“1.5 计算的高速化”，至少保存一组正常样本和一组故意破坏轴、状态、采样或模式的失败样本。

先预测“1.5 计算的高速化”改变后最早变化的是计数、shape、概率、隐藏状态、梯度还是对齐权重，再运行固定样本。若最终指标改善但第一条预期证据没有出现，应停止并从原始输入、参数、随机种子和空状态完整重放。

### 1.5.1 位精度

把该小节放回本章主链，先声明输入、输出、状态和评价标准，再用最小样本核对中间证据。实现应能解释为什么得到该结果、在哪个边界失效，以及如何从首个分叉点重放。 本页验收合同是“每一层的forward与backward必须保持输入输出shape、批量轴和参数梯度一致；最终损失下降不能替代局部梯度与数值梯度核对。”针对“1.5.1 位精度”，至少保存一组正常样本和一组故意破坏轴、状态、采样或模式的失败样本。

先预测“1.5.1 位精度”改变后最早变化的是计数、shape、概率、隐藏状态、梯度还是对齐权重，再运行固定样本。若最终指标改善但第一条预期证据没有出现，应停止并从原始输入、参数、随机种子和空状态完整重放。

### 1.5.2 GPU（CuPy）

把该小节放回本章主链，先声明输入、输出、状态和评价标准，再用最小样本核对中间证据。实现应能解释为什么得到该结果、在哪个边界失效，以及如何从首个分叉点重放。 本页验收合同是“每一层的forward与backward必须保持输入输出shape、批量轴和参数梯度一致；最终损失下降不能替代局部梯度与数值梯度核对。”针对“1.5.2 GPU（CuPy）”，至少保存一组正常样本和一组故意破坏轴、状态、采样或模式的失败样本。

先预测“1.5.2 GPU（CuPy）”改变后最早变化的是计数、shape、概率、隐藏状态、梯度还是对齐权重，再运行固定样本。若最终指标改善但第一条预期证据没有出现，应停止并从原始输入、参数、随机种子和空状态完整重放。

### 1.6 小结

把该小节放回本章主链，先声明输入、输出、状态和评价标准，再用最小样本核对中间证据。实现应能解释为什么得到该结果、在哪个边界失效，以及如何从首个分叉点重放。 本页验收合同是“每一层的forward与backward必须保持输入输出shape、批量轴和参数梯度一致；最终损失下降不能替代局部梯度与数值梯度核对。”针对“1.6 小结”，至少保存一组正常样本和一组故意破坏轴、状态、采样或模式的失败样本。

先预测“1.6 小结”改变后最早变化的是计数、shape、概率、隐藏状态、梯度还是对齐权重，再运行固定样本。若最终指标改善但第一条预期证据没有出现，应停止并从原始输入、参数、随机种子和空状态完整重放。

## 常见失败与边界

<Callout type="trap">
  **最终输出掩盖中间错误：**
  词向量近邻、生成文本或验证损失看似合理，但词ID、归一化轴、隐藏状态或mask已经错位。必须先查最早的中间证据。
</Callout>

<Callout type="trap">
  **重放没有清空派生状态：**
  旧梯度、RNN状态、采样缓存、Dropout模式或预处理词表进入新实验，导致修复前后不可比较。重放必须从原始语料和空状态开始。
</Callout>

<Callout type="tip">
  **最小调试顺序：**
  先用可手算的两三个词或两三个时间步验证shape与公式，再扩大到PTB等真实语料；第一条证据通过后才检查下一条。
</Callout>

## 本章回顾

先把矩阵形状、层接口、损失、反向传播和参数更新串成可复现训练闭环，后续所有NLP模型都复用这套层式实现。 验收底线是：每一层的forward与backward必须保持输入输出shape、批量轴和参数梯度一致；最终损失下降不能替代局部梯度与数值梯度核对。。掌握标准不是记住模型名称，而是能预测首个变化、手算最小例、复现实验、解释失败样本，并证明修复没有污染其他阶段。

<Glossary>

<GlossaryItem term="张量形状">
  “张量形状”对应证据链第1站。记录它的定义、输入输出shape、可观测中间量、容差与失败边界；只报告最终准确率、损失或样例输出不足以证明该环节正确。
</GlossaryItem>

<GlossaryItem term="Affine层">
  “Affine层”对应证据链第2站。记录它的定义、输入输出shape、可观测中间量、容差与失败边界；只报告最终准确率、损失或样例输出不足以证明该环节正确。
</GlossaryItem>

<GlossaryItem term="Softmax">
  “Softmax”对应证据链第3站。记录它的定义、输入输出shape、可观测中间量、容差与失败边界；只报告最终准确率、损失或样例输出不足以证明该环节正确。
</GlossaryItem>

<GlossaryItem term="交叉熵">
  “交叉熵”对应证据链第4站。记录它的定义、输入输出shape、可观测中间量、容差与失败边界；只报告最终准确率、损失或样例输出不足以证明该环节正确。
</GlossaryItem>

<GlossaryItem term="计算图">
  “计算图”对应证据链第5站。记录它的定义、输入输出shape、可观测中间量、容差与失败边界；只报告最终准确率、损失或样例输出不足以证明该环节正确。
</GlossaryItem>

<GlossaryItem term="梯度检验">
  “梯度检验”对应证据链第6站。记录它的定义、输入输出shape、可观测中间量、容差与失败边界；只报告最终准确率、损失或样例输出不足以证明该环节正确。
</GlossaryItem>

</Glossary>

<Exercises>

1. 如何为第1章 神经网络的复习建立一个可手算、可重放的最小实验？

<Answer>
  选取两三个词或时间步，冻结词表、参数、随机种子和初始状态；先写四个方程与shape，再保存forward中间量、局部梯度和独立评价。运行前预测首个变化，运行后从原始输入重算并比较。
</Answer>

2. 如何证明“每一层的forward与backward必须保持输入输出shape、批量轴和参数梯度一致；最终损失下降不能替代局部梯度与数值梯度核对。”不是由最终指标碰巧满足？

<Answer>
  同时比较正常路径和故意破坏一个轴、ID、mask、状态或采样条件的失败路径。检测器应在预测的首个中间量处区分两者；若只有末端损失不同，证据粒度仍然不足。
</Answer>

3. 修复第1章 神经网络的复习的实现后，应该按什么顺序回归？

<Answer>
  清空梯度、缓存、循环状态与采样器，从固定原始输入重放：先查shape和索引，再查概率与状态，然后查解析/数值梯度，最后才跑训练、困惑度、生成或可视化，并确认上一阶段基线不退化。
</Answer>

</Exercises>

<Attribution
  adaptedFrom="斋藤康毅著、陆宇杰译《深度学习进阶：自然语言处理》，人民邮电出版社，2020年10月，ISBN 9787115547644；原版《ゼロから作るDeep Learning ❷ ―自然言語処理編》，O'Reilly Japan，2018年7月21日，432页，ISBN 9784873118369 · 基于官方8章、46个一级节、125个二级节、3个附录与9个附录节完整目录的独立教学重构"
  adaptedUrl="https://www.oreilly.co.jp/books/9784873118369/"
/>
