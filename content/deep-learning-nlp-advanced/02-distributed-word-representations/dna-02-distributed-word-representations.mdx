---
title: "第2章 自然语言和单词的分布式表示"
type: B
section: "第2章 自然语言和单词的分布式表示"
order: 2
description: "第2章 自然语言和单词的分布式表示逐项覆盖权威目录，以公式、从零实现、交互实验和失败对照重建核心知识。"
demo: true
math: true
sourceUrl: "https://www.oreilly.co.jp/books/9784873118369/"
draft: false
---


## 为什么从可观测证据开始

、、、、、共同构成本页坐标。把文本转成稳定词ID和共现统计，以分布式假设、PPMI与SVD建立可解释的稠密词向量基线。

自然语言模型容易产生“输出看起来合理”的错觉，但词表错位、广播轴错误、旧隐藏状态、错误采样、训练/推理路径混用都可能暂时给出低损失。实验开始前固定语料切片、词ID映射、批量与时间轴、参数、随机种子、模式和上一版结果；运行前写下首个应变化的中间量。

本页采用“定义对象 -> 写shape与公式 -> 手算最小样本 -> 运行交互实验 -> 比较独立证据 -> 注入失败并重放”的顺序。任何末端改进都要能沿证据链回到输入；首处分叉出现后停止扩大实验，清空缓存、梯度和循环状态，从固定输入重放。

## 数学骨架

$$
C_{ij}=\sum_t\mathbf{1}[w_t=i]\sum_{\substack{-c\leq r\leq c\\r\neq0}}\mathbf{1}[w_{t+r}=j]
$$

$$
\operatorname{sim}(x,y)=\frac{x\cdot y}{\lVert x\rVert\lVert y\rVert}
$$

$$
\operatorname{PMI}(i,j)=\log_2\frac{C_{ij}N}{C_iC_j}
$$

$$
M\approx U_k\Sigma_kV_k^{\mathsf T}
$$

每个方程进入代码前都声明批量轴、词表轴、时间轴和隐藏维。概率要核对归一化轴，查表要核对重复ID梯度，循环状态要核对跨批次边界，反向传播要与中心差分或可手算局部导数比较。

## 可复现证据工作表

第一栏记录原始文本、分词结果、词ID、批量索引和时间偏移，任何预处理变化都生成新的版本号。第二栏逐层写输入shape、参数shape、输出shape与归一化轴；查表层另记重复ID，循环层另记初始状态与截断边界，Attention另记mask覆盖位置。这样能在模型开始训练前发现大多数轴错位。

第三栏保存可独立重算的数值证据：计数模型保留共现行与PPMI项，预测模型保留logit与目标概率，循环模型保留每步隐藏状态与梯度范数，生成模型保留教师强制和自回归两条轨迹。第四栏写通过阈值与失败样本，明确哪一个中间量应最先报警，而不是等最终损失或文本输出暴露问题。

重放时从原始文本重新构造词表和小批量，重新初始化参数、优化器、随机采样器与循环状态；随后按同一顺序计算forward、局部梯度、参数更新和独立评价。若修复只在保留旧缓存时有效，或换一个固定随机种子就消失，应判为状态污染而不是算法改进。

## 核心实现合同

1. **输入合同：** 冻结语料版本、分词与词ID映射；批量、时间、词表和隐藏维不靠上下文猜测。
2. **前向合同：** 保存每层输入输出、shape、概率归一化轴、查表ID、循环状态或对齐权重。
3. **反向合同：** 共享参数与重复ID贡献必须累加；截断只切反向图，padding和无效位置不得贡献梯度。
4. **状态合同：** 训练、验证、生成和独立重放使用明确初始状态；Dropout、采样器和缓存不得跨模式泄漏。
5. **评价合同：** 损失、困惑度、近邻、精确匹配与可视化都从保存的预测重新计算，不读取训练期临时值。
6. **失败合同：** 至少注入一个错误轴、错位ID、旧状态、错误mask或采样泄漏，并证明检测器在首个中间量处报警。

## 权威目录逐项讲解

### 第2章 自然语言和单词的分布式表示

把该小节放回本章主链，先声明输入、输出、状态和评价标准，再用最小样本核对中间证据。实现应能解释为什么得到该结果、在哪个边界失效，以及如何从首个分叉点重放。 本页验收合同是“窗口、词表、计数方向、零频平滑和降维维数必须固定；相似词结果要能追溯到共现行、PPMI值与奇异向量。”针对“第2章 自然语言和单词的分布式表示”，至少保存一组正常样本和一组故意破坏轴、状态、采样或模式的失败样本。

先预测“第2章 自然语言和单词的分布式表示”改变后最早变化的是计数、shape、概率、隐藏状态、梯度还是对齐权重，再运行固定样本。若最终指标改善但第一条预期证据没有出现，应停止并从原始输入、参数、随机种子和空状态完整重放。

### 2.1 什么是自然语言处理

先冻结分词规则、大小写、标点、词ID映射和语料版本，再区分单词字符串、词义节点与上下文实例。规则知识库能给出显式关系，但覆盖、维护和语境消歧是它的边界。 本页验收合同是“窗口、词表、计数方向、零频平滑和降维维数必须固定；相似词结果要能追溯到共现行、PPMI值与奇异向量。”针对“2.1 什么是自然语言处理”，至少保存一组正常样本和一组故意破坏轴、状态、采样或模式的失败样本。

先预测“2.1 什么是自然语言处理”改变后最早变化的是计数、shape、概率、隐藏状态、梯度还是对齐权重，再运行固定样本。若最终指标改善但第一条预期证据没有出现，应停止并从原始输入、参数、随机种子和空状态完整重放。

### 2.1.1 单词的含义

先冻结分词规则、大小写、标点、词ID映射和语料版本，再区分单词字符串、词义节点与上下文实例。规则知识库能给出显式关系，但覆盖、维护和语境消歧是它的边界。 本页验收合同是“窗口、词表、计数方向、零频平滑和降维维数必须固定；相似词结果要能追溯到共现行、PPMI值与奇异向量。”针对“2.1.1 单词的含义”，至少保存一组正常样本和一组故意破坏轴、状态、采样或模式的失败样本。

先预测“2.1.1 单词的含义”改变后最早变化的是计数、shape、概率、隐藏状态、梯度还是对齐权重，再运行固定样本。若最终指标改善但第一条预期证据没有出现，应停止并从原始输入、参数、随机种子和空状态完整重放。

### 2.2 同义词词典

先冻结分词规则、大小写、标点、词ID映射和语料版本，再区分单词字符串、词义节点与上下文实例。规则知识库能给出显式关系，但覆盖、维护和语境消歧是它的边界。 本页验收合同是“窗口、词表、计数方向、零频平滑和降维维数必须固定；相似词结果要能追溯到共现行、PPMI值与奇异向量。”针对“2.2 同义词词典”，至少保存一组正常样本和一组故意破坏轴、状态、采样或模式的失败样本。

先预测“2.2 同义词词典”改变后最早变化的是计数、shape、概率、隐藏状态、梯度还是对齐权重，再运行固定样本。若最终指标改善但第一条预期证据没有出现，应停止并从原始输入、参数、随机种子和空状态完整重放。

### 2.2.1 WordNet

先冻结分词规则、大小写、标点、词ID映射和语料版本，再区分单词字符串、词义节点与上下文实例。规则知识库能给出显式关系，但覆盖、维护和语境消歧是它的边界。 本页验收合同是“窗口、词表、计数方向、零频平滑和降维维数必须固定；相似词结果要能追溯到共现行、PPMI值与奇异向量。”针对“2.2.1 WordNet”，至少保存一组正常样本和一组故意破坏轴、状态、采样或模式的失败样本。

先预测“2.2.1 WordNet”改变后最早变化的是计数、shape、概率、隐藏状态、梯度还是对齐权重，再运行固定样本。若最终指标改善但第一条预期证据没有出现，应停止并从原始输入、参数、随机种子和空状态完整重放。

### 2.2.2 同义词词典的问题

先冻结分词规则、大小写、标点、词ID映射和语料版本，再区分单词字符串、词义节点与上下文实例。规则知识库能给出显式关系，但覆盖、维护和语境消歧是它的边界。 本页验收合同是“窗口、词表、计数方向、零频平滑和降维维数必须固定；相似词结果要能追溯到共现行、PPMI值与奇异向量。”针对“2.2.2 同义词词典的问题”，至少保存一组正常样本和一组故意破坏轴、状态、采样或模式的失败样本。

先预测“2.2.2 同义词词典的问题”改变后最早变化的是计数、shape、概率、隐藏状态、梯度还是对齐权重，再运行固定样本。若最终指标改善但第一条预期证据没有出现，应停止并从原始输入、参数、随机种子和空状态完整重放。

### 2.3 基于计数的方法

以窗口扫描构造共现行，手算一个中心词的左右邻居计数，再比较余弦、PMI/PPMI和SVD前后的近邻。高频词偏置、零频与截断维数必须作为显式实验变量。 本页验收合同是“窗口、词表、计数方向、零频平滑和降维维数必须固定；相似词结果要能追溯到共现行、PPMI值与奇异向量。”针对“2.3 基于计数的方法”，至少保存一组正常样本和一组故意破坏轴、状态、采样或模式的失败样本。

先预测“2.3 基于计数的方法”改变后最早变化的是计数、shape、概率、隐藏状态、梯度还是对齐权重，再运行固定样本。若最终指标改善但第一条预期证据没有出现，应停止并从原始输入、参数、随机种子和空状态完整重放。

### 2.3.1 基于Python的语料库的预处理

先冻结分词规则、大小写、标点、词ID映射和语料版本，再区分单词字符串、词义节点与上下文实例。规则知识库能给出显式关系，但覆盖、维护和语境消歧是它的边界。 本页验收合同是“窗口、词表、计数方向、零频平滑和降维维数必须固定；相似词结果要能追溯到共现行、PPMI值与奇异向量。”针对“2.3.1 基于Python的语料库的预处理”，至少保存一组正常样本和一组故意破坏轴、状态、采样或模式的失败样本。

先预测“2.3.1 基于Python的语料库的预处理”改变后最早变化的是计数、shape、概率、隐藏状态、梯度还是对齐权重，再运行固定样本。若最终指标改善但第一条预期证据没有出现，应停止并从原始输入、参数、随机种子和空状态完整重放。

### 2.3.2 单词的分布式表示

把该小节放回本章主链，先声明输入、输出、状态和评价标准，再用最小样本核对中间证据。实现应能解释为什么得到该结果、在哪个边界失效，以及如何从首个分叉点重放。 本页验收合同是“窗口、词表、计数方向、零频平滑和降维维数必须固定；相似词结果要能追溯到共现行、PPMI值与奇异向量。”针对“2.3.2 单词的分布式表示”，至少保存一组正常样本和一组故意破坏轴、状态、采样或模式的失败样本。

先预测“2.3.2 单词的分布式表示”改变后最早变化的是计数、shape、概率、隐藏状态、梯度还是对齐权重，再运行固定样本。若最终指标改善但第一条预期证据没有出现，应停止并从原始输入、参数、随机种子和空状态完整重放。

### 2.3.3 分布式假设

以窗口扫描构造共现行，手算一个中心词的左右邻居计数，再比较余弦、PMI/PPMI和SVD前后的近邻。高频词偏置、零频与截断维数必须作为显式实验变量。 本页验收合同是“窗口、词表、计数方向、零频平滑和降维维数必须固定；相似词结果要能追溯到共现行、PPMI值与奇异向量。”针对“2.3.3 分布式假设”，至少保存一组正常样本和一组故意破坏轴、状态、采样或模式的失败样本。

先预测“2.3.3 分布式假设”改变后最早变化的是计数、shape、概率、隐藏状态、梯度还是对齐权重，再运行固定样本。若最终指标改善但第一条预期证据没有出现，应停止并从原始输入、参数、随机种子和空状态完整重放。

### 2.3.4 共现矩阵

先在纸上标出每个轴与shape，再执行逐元素运算、广播或矩阵乘积。forward通过后，用上游梯度逐轴还原输入梯度；广播轴必须求和，不能依赖NumPy碰巧给出可运行结果。 本页验收合同是“窗口、词表、计数方向、零频平滑和降维维数必须固定；相似词结果要能追溯到共现行、PPMI值与奇异向量。”针对“2.3.4 共现矩阵”，至少保存一组正常样本和一组故意破坏轴、状态、采样或模式的失败样本。

先预测“2.3.4 共现矩阵”改变后最早变化的是计数、shape、概率、隐藏状态、梯度还是对齐权重，再运行固定样本。若最终指标改善但第一条预期证据没有出现，应停止并从原始输入、参数、随机种子和空状态完整重放。

### 2.3.5 向量间的相似度

先在纸上标出每个轴与shape，再执行逐元素运算、广播或矩阵乘积。forward通过后，用上游梯度逐轴还原输入梯度；广播轴必须求和，不能依赖NumPy碰巧给出可运行结果。 本页验收合同是“窗口、词表、计数方向、零频平滑和降维维数必须固定；相似词结果要能追溯到共现行、PPMI值与奇异向量。”针对“2.3.5 向量间的相似度”，至少保存一组正常样本和一组故意破坏轴、状态、采样或模式的失败样本。

先预测“2.3.5 向量间的相似度”改变后最早变化的是计数、shape、概率、隐藏状态、梯度还是对齐权重，再运行固定样本。若最终指标改善但第一条预期证据没有出现，应停止并从原始输入、参数、随机种子和空状态完整重放。

### 2.3.6 相似单词的排序

以窗口扫描构造共现行，手算一个中心词的左右邻居计数，再比较余弦、PMI/PPMI和SVD前后的近邻。高频词偏置、零频与截断维数必须作为显式实验变量。 本页验收合同是“窗口、词表、计数方向、零频平滑和降维维数必须固定；相似词结果要能追溯到共现行、PPMI值与奇异向量。”针对“2.3.6 相似单词的排序”，至少保存一组正常样本和一组故意破坏轴、状态、采样或模式的失败样本。

先预测“2.3.6 相似单词的排序”改变后最早变化的是计数、shape、概率、隐藏状态、梯度还是对齐权重，再运行固定样本。若最终指标改善但第一条预期证据没有出现，应停止并从原始输入、参数、随机种子和空状态完整重放。

### 2.4 基于计数的方法的改进

以窗口扫描构造共现行，手算一个中心词的左右邻居计数，再比较余弦、PMI/PPMI和SVD前后的近邻。高频词偏置、零频与截断维数必须作为显式实验变量。 本页验收合同是“窗口、词表、计数方向、零频平滑和降维维数必须固定；相似词结果要能追溯到共现行、PPMI值与奇异向量。”针对“2.4 基于计数的方法的改进”，至少保存一组正常样本和一组故意破坏轴、状态、采样或模式的失败样本。

先预测“2.4 基于计数的方法的改进”改变后最早变化的是计数、shape、概率、隐藏状态、梯度还是对齐权重，再运行固定样本。若最终指标改善但第一条预期证据没有出现，应停止并从原始输入、参数、随机种子和空状态完整重放。

### 2.4.1 点互信息

以窗口扫描构造共现行，手算一个中心词的左右邻居计数，再比较余弦、PMI/PPMI和SVD前后的近邻。高频词偏置、零频与截断维数必须作为显式实验变量。 本页验收合同是“窗口、词表、计数方向、零频平滑和降维维数必须固定；相似词结果要能追溯到共现行、PPMI值与奇异向量。”针对“2.4.1 点互信息”，至少保存一组正常样本和一组故意破坏轴、状态、采样或模式的失败样本。

先预测“2.4.1 点互信息”改变后最早变化的是计数、shape、概率、隐藏状态、梯度还是对齐权重，再运行固定样本。若最终指标改善但第一条预期证据没有出现，应停止并从原始输入、参数、随机种子和空状态完整重放。

### 2.4.2 降维

以窗口扫描构造共现行，手算一个中心词的左右邻居计数，再比较余弦、PMI/PPMI和SVD前后的近邻。高频词偏置、零频与截断维数必须作为显式实验变量。 本页验收合同是“窗口、词表、计数方向、零频平滑和降维维数必须固定；相似词结果要能追溯到共现行、PPMI值与奇异向量。”针对“2.4.2 降维”，至少保存一组正常样本和一组故意破坏轴、状态、采样或模式的失败样本。

先预测“2.4.2 降维”改变后最早变化的是计数、shape、概率、隐藏状态、梯度还是对齐权重，再运行固定样本。若最终指标改善但第一条预期证据没有出现，应停止并从原始输入、参数、随机种子和空状态完整重放。

### 2.4.3 基于SVD的降维

以窗口扫描构造共现行，手算一个中心词的左右邻居计数，再比较余弦、PMI/PPMI和SVD前后的近邻。高频词偏置、零频与截断维数必须作为显式实验变量。 本页验收合同是“窗口、词表、计数方向、零频平滑和降维维数必须固定；相似词结果要能追溯到共现行、PPMI值与奇异向量。”针对“2.4.3 基于SVD的降维”，至少保存一组正常样本和一组故意破坏轴、状态、采样或模式的失败样本。

先预测“2.4.3 基于SVD的降维”改变后最早变化的是计数、shape、概率、隐藏状态、梯度还是对齐权重，再运行固定样本。若最终指标改善但第一条预期证据没有出现，应停止并从原始输入、参数、随机种子和空状态完整重放。

### 2.4.4 PTB数据集

以窗口扫描构造共现行，手算一个中心词的左右邻居计数，再比较余弦、PMI/PPMI和SVD前后的近邻。高频词偏置、零频与截断维数必须作为显式实验变量。 本页验收合同是“窗口、词表、计数方向、零频平滑和降维维数必须固定；相似词结果要能追溯到共现行、PPMI值与奇异向量。”针对“2.4.4 PTB数据集”，至少保存一组正常样本和一组故意破坏轴、状态、采样或模式的失败样本。

先预测“2.4.4 PTB数据集”改变后最早变化的是计数、shape、概率、隐藏状态、梯度还是对齐权重，再运行固定样本。若最终指标改善但第一条预期证据没有出现，应停止并从原始输入、参数、随机种子和空状态完整重放。

### 2.4.5 基于PTB数据集的评价

以窗口扫描构造共现行，手算一个中心词的左右邻居计数，再比较余弦、PMI/PPMI和SVD前后的近邻。高频词偏置、零频与截断维数必须作为显式实验变量。 本页验收合同是“窗口、词表、计数方向、零频平滑和降维维数必须固定；相似词结果要能追溯到共现行、PPMI值与奇异向量。”针对“2.4.5 基于PTB数据集的评价”，至少保存一组正常样本和一组故意破坏轴、状态、采样或模式的失败样本。

先预测“2.4.5 基于PTB数据集的评价”改变后最早变化的是计数、shape、概率、隐藏状态、梯度还是对齐权重，再运行固定样本。若最终指标改善但第一条预期证据没有出现，应停止并从原始输入、参数、随机种子和空状态完整重放。

### 2.5 小结

把该小节放回本章主链，先声明输入、输出、状态和评价标准，再用最小样本核对中间证据。实现应能解释为什么得到该结果、在哪个边界失效，以及如何从首个分叉点重放。 本页验收合同是“窗口、词表、计数方向、零频平滑和降维维数必须固定；相似词结果要能追溯到共现行、PPMI值与奇异向量。”针对“2.5 小结”，至少保存一组正常样本和一组故意破坏轴、状态、采样或模式的失败样本。

先预测“2.5 小结”改变后最早变化的是计数、shape、概率、隐藏状态、梯度还是对齐权重，再运行固定样本。若最终指标改善但第一条预期证据没有出现，应停止并从原始输入、参数、随机种子和空状态完整重放。

## 常见失败与边界


## 本章回顾

把文本转成稳定词ID和共现统计，以分布式假设、PPMI与SVD建立可解释的稠密词向量基线。 验收底线是：窗口、词表、计数方向、零频平滑和降维维数必须固定；相似词结果要能追溯到共现行、PPMI值与奇异向量。。掌握标准不是记住模型名称，而是能预测首个变化、手算最小例、复现实验、解释失败样本，并证明修复没有污染其他阶段。


