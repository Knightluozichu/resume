---
title: "第6章 Gated RNN"
type: B
section: "第6章 Gated RNN"
order: 6
description: "第6章 Gated RNN逐项覆盖权威目录，以公式、从零实现、交互实验和失败对照重建核心知识。"
demo: true
math: true
sourceUrl: "https://www.oreilly.co.jp/books/9784873118369/"
draft: false
---

import {
  Dna06GatedRnnMapLab,
  Dna06GatedRnnExperimentLab,
  Dna06GatedRnnEvidenceLab,
} from "@/components/mdx/deep-learning-nlp-advanced/diagrams/dna-06-gated-rnn";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第6章 Gated RNN的对象、数据流、公式与适用边界
- 能推导并计算四个核心方程中的shape、概率、状态或梯度
- 能复现三个互动视图，并定位第一条计数、状态、梯度或对齐证据分叉
- 能判断“门值、候选记忆、单元状态和隐藏状态必须分开记录；Dropout仅作用于正确路径，训练/测试模式及共享权重的梯度累加要可验证。”是否成立并设计失败样本

</Objectives>

## 为什么从可观测证据开始

<Term def="梯度消失是本页第1个证据坐标，用于连接公式、shape、中间状态与失败边界。">
  梯度消失
</Term>
、
<Term def="梯度裁剪是本页第2个证据坐标，用于连接公式、shape、中间状态与失败边界。">
  梯度裁剪
</Term>
、
<Term def="LSTM是本页第3个证据坐标，用于连接公式、shape、中间状态与失败边界。">
  LSTM
</Term>
、
<Term def="遗忘门是本页第4个证据坐标，用于连接公式、shape、中间状态与失败边界。">
  遗忘门
</Term>
、
<Term def="Dropout是本页第5个证据坐标，用于连接公式、shape、中间状态与失败边界。">
  Dropout
</Term>
、
<Term def="权重共享是本页第6个证据坐标，用于连接公式、shape、中间状态与失败边界。">
  权重共享
</Term>
共同构成本页坐标。从时间连乘导致的梯度消失/爆炸出发，构造LSTM记忆单元与三类门，并组合多层、Dropout和权重共享。

自然语言模型容易产生“输出看起来合理”的错觉，但词表错位、广播轴错误、旧隐藏状态、错误采样、训练/推理路径混用都可能暂时给出低损失。实验开始前固定语料切片、词ID映射、批量与时间轴、参数、随机种子、模式和上一版结果；运行前写下首个应变化的中间量。

本页采用“定义对象 -> 写shape与公式 -> 手算最小样本 -> 运行交互实验 -> 比较独立证据 -> 注入失败并重放”的顺序。任何末端改进都要能沿证据链回到输入；首处分叉出现后停止扩大实验，清空缓存、梯度和循环状态，从固定输入重放。

## 数学骨架

$$
f_t=\sigma(x_tW_f+h_{t-1}U_f+b_f)
$$

$$
i_t=\sigma(x_tW_i+h_{t-1}U_i+b_i),\quad g_t=\tanh(x_tW_g+h_{t-1}U_g+b_g)
$$

$$
c_t=f_t\odot c_{t-1}+i_t\odot g_t
$$

$$
h_t=o_t\odot\tanh(c_t)
$$

每个方程进入代码前都声明批量轴、词表轴、时间轴和隐藏维。概率要核对归一化轴，查表要核对重复ID梯度，循环状态要核对跨批次边界，反向传播要与中心差分或可手算局部导数比较。

## 可复现证据工作表

第一栏记录原始文本、分词结果、词ID、批量索引和时间偏移，任何预处理变化都生成新的版本号。第二栏逐层写输入shape、参数shape、输出shape与归一化轴；查表层另记重复ID，循环层另记初始状态与截断边界，Attention另记mask覆盖位置。这样能在模型开始训练前发现大多数轴错位。

第三栏保存可独立重算的数值证据：计数模型保留共现行与PPMI项，预测模型保留logit与目标概率，循环模型保留每步隐藏状态与梯度范数，生成模型保留教师强制和自回归两条轨迹。第四栏写通过阈值与失败样本，明确哪一个中间量应最先报警，而不是等最终损失或文本输出暴露问题。

重放时从原始文本重新构造词表和小批量，重新初始化参数、优化器、随机采样器与循环状态；随后按同一顺序计算forward、局部梯度、参数更新和独立评价。若修复只在保留旧缓存时有效，或换一个固定随机种子就消失，应判为状态污染而不是算法改进。

## 核心实现合同

1. **输入合同：** 冻结语料版本、分词与词ID映射；批量、时间、词表和隐藏维不靠上下文猜测。
2. **前向合同：** 保存每层输入输出、shape、概率归一化轴、查表ID、循环状态或对齐权重。
3. **反向合同：** 共享参数与重复ID贡献必须累加；截断只切反向图，padding和无效位置不得贡献梯度。
4. **状态合同：** 训练、验证、生成和独立重放使用明确初始状态；Dropout、采样器和缓存不得跨模式泄漏。
5. **评价合同：** 损失、困惑度、近邻、精确匹配与可视化都从保存的预测重新计算，不读取训练期临时值。
6. **失败合同：** 至少注入一个错误轴、错位ID、旧状态、错误mask或采样泄漏，并证明检测器在首个中间量处报警。

## 权威目录逐项讲解

### 第6章 Gated RNN

沿时间展开共享参数，逐步保存输入、隐藏状态、输出和局部梯度。Truncated BPTT只截断反向图而保留必要的前向状态；批量序列应从不同偏移连续读取，困惑度由平均负对数似然独立重算。 本页验收合同是“门值、候选记忆、单元状态和隐藏状态必须分开记录；Dropout仅作用于正确路径，训练/测试模式及共享权重的梯度累加要可验证。”针对“第6章 Gated RNN”，至少保存一组正常样本和一组故意破坏轴、状态、采样或模式的失败样本。

先预测“第6章 Gated RNN”改变后最早变化的是计数、shape、概率、隐藏状态、梯度还是对齐权重，再运行固定样本。若最终指标改善但第一条预期证据没有出现，应停止并从原始输入、参数、随机种子和空状态完整重放。

### 6.1 RNN的问题

沿时间展开共享参数，逐步保存输入、隐藏状态、输出和局部梯度。Truncated BPTT只截断反向图而保留必要的前向状态；批量序列应从不同偏移连续读取，困惑度由平均负对数似然独立重算。 本页验收合同是“门值、候选记忆、单元状态和隐藏状态必须分开记录；Dropout仅作用于正确路径，训练/测试模式及共享权重的梯度累加要可验证。”针对“6.1 RNN的问题”，至少保存一组正常样本和一组故意破坏轴、状态、采样或模式的失败样本。

先预测“6.1 RNN的问题”改变后最早变化的是计数、shape、概率、隐藏状态、梯度还是对齐权重，再运行固定样本。若最终指标改善但第一条预期证据没有出现，应停止并从原始输入、参数、随机种子和空状态完整重放。

### 6.1.1 RNN的复习

沿时间展开共享参数，逐步保存输入、隐藏状态、输出和局部梯度。Truncated BPTT只截断反向图而保留必要的前向状态；批量序列应从不同偏移连续读取，困惑度由平均负对数似然独立重算。 本页验收合同是“门值、候选记忆、单元状态和隐藏状态必须分开记录；Dropout仅作用于正确路径，训练/测试模式及共享权重的梯度累加要可验证。”针对“6.1.1 RNN的复习”，至少保存一组正常样本和一组故意破坏轴、状态、采样或模式的失败样本。

先预测“6.1.1 RNN的复习”改变后最早变化的是计数、shape、概率、隐藏状态、梯度还是对齐权重，再运行固定样本。若最终指标改善但第一条预期证据没有出现，应停止并从原始输入、参数、随机种子和空状态完整重放。

### 6.1.2 梯度消失和梯度爆炸

把局部函数拆成可手算节点，保存forward输入与输出，backward只接收上游梯度并返回同shape贡献。解析梯度要与中心差分比较相对误差，更新后必须重新forward，禁止复用旧损失。 本页验收合同是“门值、候选记忆、单元状态和隐藏状态必须分开记录；Dropout仅作用于正确路径，训练/测试模式及共享权重的梯度累加要可验证。”针对“6.1.2 梯度消失和梯度爆炸”，至少保存一组正常样本和一组故意破坏轴、状态、采样或模式的失败样本。

先预测“6.1.2 梯度消失和梯度爆炸”改变后最早变化的是计数、shape、概率、隐藏状态、梯度还是对齐权重，再运行固定样本。若最终指标改善但第一条预期证据没有出现，应停止并从原始输入、参数、随机种子和空状态完整重放。

### 6.1.3 梯度消失和梯度爆炸的原因

把局部函数拆成可手算节点，保存forward输入与输出，backward只接收上游梯度并返回同shape贡献。解析梯度要与中心差分比较相对误差，更新后必须重新forward，禁止复用旧损失。 本页验收合同是“门值、候选记忆、单元状态和隐藏状态必须分开记录；Dropout仅作用于正确路径，训练/测试模式及共享权重的梯度累加要可验证。”针对“6.1.3 梯度消失和梯度爆炸的原因”，至少保存一组正常样本和一组故意破坏轴、状态、采样或模式的失败样本。

先预测“6.1.3 梯度消失和梯度爆炸的原因”改变后最早变化的是计数、shape、概率、隐藏状态、梯度还是对齐权重，再运行固定样本。若最终指标改善但第一条预期证据没有出现，应停止并从原始输入、参数、随机种子和空状态完整重放。

### 6.1.4 梯度爆炸的对策

把局部函数拆成可手算节点，保存forward输入与输出，backward只接收上游梯度并返回同shape贡献。解析梯度要与中心差分比较相对误差，更新后必须重新forward，禁止复用旧损失。 本页验收合同是“门值、候选记忆、单元状态和隐藏状态必须分开记录；Dropout仅作用于正确路径，训练/测试模式及共享权重的梯度累加要可验证。”针对“6.1.4 梯度爆炸的对策”，至少保存一组正常样本和一组故意破坏轴、状态、采样或模式的失败样本。

先预测“6.1.4 梯度爆炸的对策”改变后最早变化的是计数、shape、概率、隐藏状态、梯度还是对齐权重，再运行固定样本。若最终指标改善但第一条预期证据没有出现，应停止并从原始输入、参数、随机种子和空状态完整重放。

### 6.2 梯度消失和LSTM

把局部函数拆成可手算节点，保存forward输入与输出，backward只接收上游梯度并返回同shape贡献。解析梯度要与中心差分比较相对误差，更新后必须重新forward，禁止复用旧损失。 本页验收合同是“门值、候选记忆、单元状态和隐藏状态必须分开记录；Dropout仅作用于正确路径，训练/测试模式及共享权重的梯度累加要可验证。”针对“6.2 梯度消失和LSTM”，至少保存一组正常样本和一组故意破坏轴、状态、采样或模式的失败样本。

先预测“6.2 梯度消失和LSTM”改变后最早变化的是计数、shape、概率、隐藏状态、梯度还是对齐权重，再运行固定样本。若最终指标改善但第一条预期证据没有出现，应停止并从原始输入、参数、随机种子和空状态完整重放。

### 6.2.1 LSTM的接口

分别记录遗忘门、输入门、输出门、候选记忆、单元状态与隐藏状态。检查门值范围和单元状态的加法路径，再用长序列梯度范数、裁剪阈值、训练/测试Dropout差异证明修复有效。 本页验收合同是“门值、候选记忆、单元状态和隐藏状态必须分开记录；Dropout仅作用于正确路径，训练/测试模式及共享权重的梯度累加要可验证。”针对“6.2.1 LSTM的接口”，至少保存一组正常样本和一组故意破坏轴、状态、采样或模式的失败样本。

先预测“6.2.1 LSTM的接口”改变后最早变化的是计数、shape、概率、隐藏状态、梯度还是对齐权重，再运行固定样本。若最终指标改善但第一条预期证据没有出现，应停止并从原始输入、参数、随机种子和空状态完整重放。

### 6.2.2 LSTM层的结构

分别记录遗忘门、输入门、输出门、候选记忆、单元状态与隐藏状态。检查门值范围和单元状态的加法路径，再用长序列梯度范数、裁剪阈值、训练/测试Dropout差异证明修复有效。 本页验收合同是“门值、候选记忆、单元状态和隐藏状态必须分开记录；Dropout仅作用于正确路径，训练/测试模式及共享权重的梯度累加要可验证。”针对“6.2.2 LSTM层的结构”，至少保存一组正常样本和一组故意破坏轴、状态、采样或模式的失败样本。

先预测“6.2.2 LSTM层的结构”改变后最早变化的是计数、shape、概率、隐藏状态、梯度还是对齐权重，再运行固定样本。若最终指标改善但第一条预期证据没有出现，应停止并从原始输入、参数、随机种子和空状态完整重放。

### 6.2.3 输出门

分别记录遗忘门、输入门、输出门、候选记忆、单元状态与隐藏状态。检查门值范围和单元状态的加法路径，再用长序列梯度范数、裁剪阈值、训练/测试Dropout差异证明修复有效。 本页验收合同是“门值、候选记忆、单元状态和隐藏状态必须分开记录；Dropout仅作用于正确路径，训练/测试模式及共享权重的梯度累加要可验证。”针对“6.2.3 输出门”，至少保存一组正常样本和一组故意破坏轴、状态、采样或模式的失败样本。

先预测“6.2.3 输出门”改变后最早变化的是计数、shape、概率、隐藏状态、梯度还是对齐权重，再运行固定样本。若最终指标改善但第一条预期证据没有出现，应停止并从原始输入、参数、随机种子和空状态完整重放。

### 6.2.4 遗忘门

分别记录遗忘门、输入门、输出门、候选记忆、单元状态与隐藏状态。检查门值范围和单元状态的加法路径，再用长序列梯度范数、裁剪阈值、训练/测试Dropout差异证明修复有效。 本页验收合同是“门值、候选记忆、单元状态和隐藏状态必须分开记录；Dropout仅作用于正确路径，训练/测试模式及共享权重的梯度累加要可验证。”针对“6.2.4 遗忘门”，至少保存一组正常样本和一组故意破坏轴、状态、采样或模式的失败样本。

先预测“6.2.4 遗忘门”改变后最早变化的是计数、shape、概率、隐藏状态、梯度还是对齐权重，再运行固定样本。若最终指标改善但第一条预期证据没有出现，应停止并从原始输入、参数、随机种子和空状态完整重放。

### 6.2.5 新的记忆单元

分别记录遗忘门、输入门、输出门、候选记忆、单元状态与隐藏状态。检查门值范围和单元状态的加法路径，再用长序列梯度范数、裁剪阈值、训练/测试Dropout差异证明修复有效。 本页验收合同是“门值、候选记忆、单元状态和隐藏状态必须分开记录；Dropout仅作用于正确路径，训练/测试模式及共享权重的梯度累加要可验证。”针对“6.2.5 新的记忆单元”，至少保存一组正常样本和一组故意破坏轴、状态、采样或模式的失败样本。

先预测“6.2.5 新的记忆单元”改变后最早变化的是计数、shape、概率、隐藏状态、梯度还是对齐权重，再运行固定样本。若最终指标改善但第一条预期证据没有出现，应停止并从原始输入、参数、随机种子和空状态完整重放。

### 6.2.6 输入门

分别记录遗忘门、输入门、输出门、候选记忆、单元状态与隐藏状态。检查门值范围和单元状态的加法路径，再用长序列梯度范数、裁剪阈值、训练/测试Dropout差异证明修复有效。 本页验收合同是“门值、候选记忆、单元状态和隐藏状态必须分开记录；Dropout仅作用于正确路径，训练/测试模式及共享权重的梯度累加要可验证。”针对“6.2.6 输入门”，至少保存一组正常样本和一组故意破坏轴、状态、采样或模式的失败样本。

先预测“6.2.6 输入门”改变后最早变化的是计数、shape、概率、隐藏状态、梯度还是对齐权重，再运行固定样本。若最终指标改善但第一条预期证据没有出现，应停止并从原始输入、参数、随机种子和空状态完整重放。

### 6.2.7 LSTM的梯度的流动

把局部函数拆成可手算节点，保存forward输入与输出，backward只接收上游梯度并返回同shape贡献。解析梯度要与中心差分比较相对误差，更新后必须重新forward，禁止复用旧损失。 本页验收合同是“门值、候选记忆、单元状态和隐藏状态必须分开记录；Dropout仅作用于正确路径，训练/测试模式及共享权重的梯度累加要可验证。”针对“6.2.7 LSTM的梯度的流动”，至少保存一组正常样本和一组故意破坏轴、状态、采样或模式的失败样本。

先预测“6.2.7 LSTM的梯度的流动”改变后最早变化的是计数、shape、概率、隐藏状态、梯度还是对齐权重，再运行固定样本。若最终指标改善但第一条预期证据没有出现，应停止并从原始输入、参数、随机种子和空状态完整重放。

### 6.3 LSTM的实现

分别记录遗忘门、输入门、输出门、候选记忆、单元状态与隐藏状态。检查门值范围和单元状态的加法路径，再用长序列梯度范数、裁剪阈值、训练/测试Dropout差异证明修复有效。 本页验收合同是“门值、候选记忆、单元状态和隐藏状态必须分开记录；Dropout仅作用于正确路径，训练/测试模式及共享权重的梯度累加要可验证。”针对“6.3 LSTM的实现”，至少保存一组正常样本和一组故意破坏轴、状态、采样或模式的失败样本。

先预测“6.3 LSTM的实现”改变后最早变化的是计数、shape、概率、隐藏状态、梯度还是对齐权重，再运行固定样本。若最终指标改善但第一条预期证据没有出现，应停止并从原始输入、参数、随机种子和空状态完整重放。

### 6.3.1 Time LSTM的实现

沿时间展开共享参数，逐步保存输入、隐藏状态、输出和局部梯度。Truncated BPTT只截断反向图而保留必要的前向状态；批量序列应从不同偏移连续读取，困惑度由平均负对数似然独立重算。 本页验收合同是“门值、候选记忆、单元状态和隐藏状态必须分开记录；Dropout仅作用于正确路径，训练/测试模式及共享权重的梯度累加要可验证。”针对“6.3.1 Time LSTM的实现”，至少保存一组正常样本和一组故意破坏轴、状态、采样或模式的失败样本。

先预测“6.3.1 Time LSTM的实现”改变后最早变化的是计数、shape、概率、隐藏状态、梯度还是对齐权重，再运行固定样本。若最终指标改善但第一条预期证据没有出现，应停止并从原始输入、参数、随机种子和空状态完整重放。

### 6.4 使用LSTM的语言模型

沿时间展开共享参数，逐步保存输入、隐藏状态、输出和局部梯度。Truncated BPTT只截断反向图而保留必要的前向状态；批量序列应从不同偏移连续读取，困惑度由平均负对数似然独立重算。 本页验收合同是“门值、候选记忆、单元状态和隐藏状态必须分开记录；Dropout仅作用于正确路径，训练/测试模式及共享权重的梯度累加要可验证。”针对“6.4 使用LSTM的语言模型”，至少保存一组正常样本和一组故意破坏轴、状态、采样或模式的失败样本。

先预测“6.4 使用LSTM的语言模型”改变后最早变化的是计数、shape、概率、隐藏状态、梯度还是对齐权重，再运行固定样本。若最终指标改善但第一条预期证据没有出现，应停止并从原始输入、参数、随机种子和空状态完整重放。

### 6.5 进一步改进RNNLM

沿时间展开共享参数，逐步保存输入、隐藏状态、输出和局部梯度。Truncated BPTT只截断反向图而保留必要的前向状态；批量序列应从不同偏移连续读取，困惑度由平均负对数似然独立重算。 本页验收合同是“门值、候选记忆、单元状态和隐藏状态必须分开记录；Dropout仅作用于正确路径，训练/测试模式及共享权重的梯度累加要可验证。”针对“6.5 进一步改进RNNLM”，至少保存一组正常样本和一组故意破坏轴、状态、采样或模式的失败样本。

先预测“6.5 进一步改进RNNLM”改变后最早变化的是计数、shape、概率、隐藏状态、梯度还是对齐权重，再运行固定样本。若最终指标改善但第一条预期证据没有出现，应停止并从原始输入、参数、随机种子和空状态完整重放。

### 6.5.1 LSTM层的多层化

分别记录遗忘门、输入门、输出门、候选记忆、单元状态与隐藏状态。检查门值范围和单元状态的加法路径，再用长序列梯度范数、裁剪阈值、训练/测试Dropout差异证明修复有效。 本页验收合同是“门值、候选记忆、单元状态和隐藏状态必须分开记录；Dropout仅作用于正确路径，训练/测试模式及共享权重的梯度累加要可验证。”针对“6.5.1 LSTM层的多层化”，至少保存一组正常样本和一组故意破坏轴、状态、采样或模式的失败样本。

先预测“6.5.1 LSTM层的多层化”改变后最早变化的是计数、shape、概率、隐藏状态、梯度还是对齐权重，再运行固定样本。若最终指标改善但第一条预期证据没有出现，应停止并从原始输入、参数、随机种子和空状态完整重放。

### 6.5.2 基于Dropout抑制过拟合

分别记录遗忘门、输入门、输出门、候选记忆、单元状态与隐藏状态。检查门值范围和单元状态的加法路径，再用长序列梯度范数、裁剪阈值、训练/测试Dropout差异证明修复有效。 本页验收合同是“门值、候选记忆、单元状态和隐藏状态必须分开记录；Dropout仅作用于正确路径，训练/测试模式及共享权重的梯度累加要可验证。”针对“6.5.2 基于Dropout抑制过拟合”，至少保存一组正常样本和一组故意破坏轴、状态、采样或模式的失败样本。

先预测“6.5.2 基于Dropout抑制过拟合”改变后最早变化的是计数、shape、概率、隐藏状态、梯度还是对齐权重，再运行固定样本。若最终指标改善但第一条预期证据没有出现，应停止并从原始输入、参数、随机种子和空状态完整重放。

### 6.5.3 权重共享

分别记录遗忘门、输入门、输出门、候选记忆、单元状态与隐藏状态。检查门值范围和单元状态的加法路径，再用长序列梯度范数、裁剪阈值、训练/测试Dropout差异证明修复有效。 本页验收合同是“门值、候选记忆、单元状态和隐藏状态必须分开记录；Dropout仅作用于正确路径，训练/测试模式及共享权重的梯度累加要可验证。”针对“6.5.3 权重共享”，至少保存一组正常样本和一组故意破坏轴、状态、采样或模式的失败样本。

先预测“6.5.3 权重共享”改变后最早变化的是计数、shape、概率、隐藏状态、梯度还是对齐权重，再运行固定样本。若最终指标改善但第一条预期证据没有出现，应停止并从原始输入、参数、随机种子和空状态完整重放。

### 6.5.4 更好的RNNLM的实现

沿时间展开共享参数，逐步保存输入、隐藏状态、输出和局部梯度。Truncated BPTT只截断反向图而保留必要的前向状态；批量序列应从不同偏移连续读取，困惑度由平均负对数似然独立重算。 本页验收合同是“门值、候选记忆、单元状态和隐藏状态必须分开记录；Dropout仅作用于正确路径，训练/测试模式及共享权重的梯度累加要可验证。”针对“6.5.4 更好的RNNLM的实现”，至少保存一组正常样本和一组故意破坏轴、状态、采样或模式的失败样本。

先预测“6.5.4 更好的RNNLM的实现”改变后最早变化的是计数、shape、概率、隐藏状态、梯度还是对齐权重，再运行固定样本。若最终指标改善但第一条预期证据没有出现，应停止并从原始输入、参数、随机种子和空状态完整重放。

### 6.5.5 前沿研究

分别记录遗忘门、输入门、输出门、候选记忆、单元状态与隐藏状态。检查门值范围和单元状态的加法路径，再用长序列梯度范数、裁剪阈值、训练/测试Dropout差异证明修复有效。 本页验收合同是“门值、候选记忆、单元状态和隐藏状态必须分开记录；Dropout仅作用于正确路径，训练/测试模式及共享权重的梯度累加要可验证。”针对“6.5.5 前沿研究”，至少保存一组正常样本和一组故意破坏轴、状态、采样或模式的失败样本。

先预测“6.5.5 前沿研究”改变后最早变化的是计数、shape、概率、隐藏状态、梯度还是对齐权重，再运行固定样本。若最终指标改善但第一条预期证据没有出现，应停止并从原始输入、参数、随机种子和空状态完整重放。

### 6.6 小结

把该小节放回本章主链，先声明输入、输出、状态和评价标准，再用最小样本核对中间证据。实现应能解释为什么得到该结果、在哪个边界失效，以及如何从首个分叉点重放。 本页验收合同是“门值、候选记忆、单元状态和隐藏状态必须分开记录；Dropout仅作用于正确路径，训练/测试模式及共享权重的梯度累加要可验证。”针对“6.6 小结”，至少保存一组正常样本和一组故意破坏轴、状态、采样或模式的失败样本。

先预测“6.6 小结”改变后最早变化的是计数、shape、概率、隐藏状态、梯度还是对齐权重，再运行固定样本。若最终指标改善但第一条预期证据没有出现，应停止并从原始输入、参数、随机种子和空状态完整重放。

## 三步交互实验

先预测单变量变化会最早影响词ID、矩阵shape、采样分数、循环状态、梯度还是Attention热力图，再切换基线、边界和失败模式。

<Stepper>
  <Step title="画出对象与数据流">
    标出语料、词ID、参数矩阵、时间状态、损失和评价证据之间的方向与shape。
    <Dna06GatedRnnMapLab />
  </Step>
  <Step title="运行单变量实验">
    固定其余输入，只改变窗口、截断长度、门值、负样本数或注意力温度中的一个变量。
    <Dna06GatedRnnExperimentLab />
  </Step>
  <Step title="核对独立证据">
    对比预测与观测、正常与故障路径，定位第一条不一致证据并给出回退点。
    <Dna06GatedRnnEvidenceLab />
  </Step>
</Stepper>

## 常见失败与边界

<Callout type="trap">
  **最终输出掩盖中间错误：**
  词向量近邻、生成文本或验证损失看似合理，但词ID、归一化轴、隐藏状态或mask已经错位。必须先查最早的中间证据。
</Callout>

<Callout type="trap">
  **重放没有清空派生状态：**
  旧梯度、RNN状态、采样缓存、Dropout模式或预处理词表进入新实验，导致修复前后不可比较。重放必须从原始语料和空状态开始。
</Callout>

<Callout type="tip">
  **最小调试顺序：**
  先用可手算的两三个词或两三个时间步验证shape与公式，再扩大到PTB等真实语料；第一条证据通过后才检查下一条。
</Callout>

## 本章回顾

从时间连乘导致的梯度消失/爆炸出发，构造LSTM记忆单元与三类门，并组合多层、Dropout和权重共享。 验收底线是：门值、候选记忆、单元状态和隐藏状态必须分开记录；Dropout仅作用于正确路径，训练/测试模式及共享权重的梯度累加要可验证。。掌握标准不是记住模型名称，而是能预测首个变化、手算最小例、复现实验、解释失败样本，并证明修复没有污染其他阶段。

<Glossary>

<GlossaryItem term="梯度消失">
  “梯度消失”对应证据链第1站。记录它的定义、输入输出shape、可观测中间量、容差与失败边界；只报告最终准确率、损失或样例输出不足以证明该环节正确。
</GlossaryItem>

<GlossaryItem term="梯度裁剪">
  “梯度裁剪”对应证据链第2站。记录它的定义、输入输出shape、可观测中间量、容差与失败边界；只报告最终准确率、损失或样例输出不足以证明该环节正确。
</GlossaryItem>

<GlossaryItem term="LSTM">
  “LSTM”对应证据链第3站。记录它的定义、输入输出shape、可观测中间量、容差与失败边界；只报告最终准确率、损失或样例输出不足以证明该环节正确。
</GlossaryItem>

<GlossaryItem term="遗忘门">
  “遗忘门”对应证据链第4站。记录它的定义、输入输出shape、可观测中间量、容差与失败边界；只报告最终准确率、损失或样例输出不足以证明该环节正确。
</GlossaryItem>

<GlossaryItem term="Dropout">
  “Dropout”对应证据链第5站。记录它的定义、输入输出shape、可观测中间量、容差与失败边界；只报告最终准确率、损失或样例输出不足以证明该环节正确。
</GlossaryItem>

<GlossaryItem term="权重共享">
  “权重共享”对应证据链第6站。记录它的定义、输入输出shape、可观测中间量、容差与失败边界；只报告最终准确率、损失或样例输出不足以证明该环节正确。
</GlossaryItem>

</Glossary>

<Exercises>

1. 如何为第6章 Gated RNN建立一个可手算、可重放的最小实验？

<Answer>
  选取两三个词或时间步，冻结词表、参数、随机种子和初始状态；先写四个方程与shape，再保存forward中间量、局部梯度和独立评价。运行前预测首个变化，运行后从原始输入重算并比较。
</Answer>

2. 如何证明“门值、候选记忆、单元状态和隐藏状态必须分开记录；Dropout仅作用于正确路径，训练/测试模式及共享权重的梯度累加要可验证。”不是由最终指标碰巧满足？

<Answer>
  同时比较正常路径和故意破坏一个轴、ID、mask、状态或采样条件的失败路径。检测器应在预测的首个中间量处区分两者；若只有末端损失不同，证据粒度仍然不足。
</Answer>

3. 修复第6章 Gated RNN的实现后，应该按什么顺序回归？

<Answer>
  清空梯度、缓存、循环状态与采样器，从固定原始输入重放：先查shape和索引，再查概率与状态，然后查解析/数值梯度，最后才跑训练、困惑度、生成或可视化，并确认上一阶段基线不退化。
</Answer>

</Exercises>

<Attribution
  adaptedFrom="斋藤康毅著、陆宇杰译《深度学习进阶：自然语言处理》，人民邮电出版社，2020年10月，ISBN 9787115547644；原版《ゼロから作るDeep Learning ❷ ―自然言語処理編》，O'Reilly Japan，2018年7月21日，432页，ISBN 9784873118369 · 基于官方8章、46个一级节、125个二级节、3个附录与9个附录节完整目录的独立教学重构"
  adaptedUrl="https://www.oreilly.co.jp/books/9784873118369/"
/>
