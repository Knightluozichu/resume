---
title: 附录B 运行WordNet
type: B
section: 附录
order: 10
description: >-
  安装并运行WordNet，区分同义词网络与分布式表示的证据来源，并逐项覆盖附录B 运行WordNet、B.1 NLTK的安装、B.2
  使用WordNet获得同义词、B.3 WordNet和单词网络、B.4 基于WordNet的语义相似度；用表示账本、单故障序列轨迹和独立评价验收门完成复核。
demo: true
math: false
sourceUrl: "https://www.oreilly.co.jp/books/9784873118369/"
draft: false
qualityVersion: 2
practiceMode: simulation
sourceMode: independent-rewrite
officialUnitId: dna-app-b
---

import {
  Objectives,
  Term,
  Callout,
  Stepper,
  Step,
  Exercises,
  Answer,
  Glossary,
  GlossaryItem,
  Attribution,
} from "@/components/mdx/mdx-components";
import {
  DnaAppendixBWordnetRepresentationLedgerLab,
  DnaAppendixBWordnetSequenceTraceLab,
  DnaAppendixBWordnetEvaluationGateLab,
} from "@/components/mdx/deep-learning-nlp-advanced/v2/dna-appendix-b-wordnet";

<Objectives>

- 能说明“附录B 运行WordNet”如何安装并运行WordNet，区分同义词网络与分布式表示的证据来源，并逐项覆盖附录B 运行WordNet、B.1 NLTK的安装、B.2 使用WordNet获得同义词、B.3 WordNet和单词网络、B.4 基于WordNet的语义相似度，并区分2018年原版、2020年中文译本与当前扩展
- 能先预测“怎样让synset、关系边和路径相似度在固定WordNet版本上可重放？ 本章各目录坐标如何汇合为同一证据链？”“附录B 运行WordNet”会改变哪个词ID、表示、时序状态、梯度、采样或评价结果，再逐步复核
- 能注入“混合不同词性或语料版本，仍把路径距离解释为通用语义真值；在附录B 运行WordNet验收中只注入这一处”，用“NLTK/WordNet版本、词性、synset ID、关系类型与相似度定义一致；附录B 运行WordNet的结论不得越过原版目录与数据边界”决定接受、回退或拒绝NLP结论

</Objectives>

## 为什么从这个NLP问题开始

“附录B 运行WordNet”必须把表示、序列状态和评价条件写成可重放合同，不能用最终指标替代中间机制。 “附录B 运行WordNet”的贯穿任务是：为“附录B 运行WordNet”冻结语料/数据、词表、shape、时序切分、随机种子与代码版本，再对照参考路径和单故障路径。 动手前先写下哪个词ID、表示、shape、时序状态、梯度、采样或评价结果会变化；运行后补理由不算预测。

围绕“怎样让synset、关系边和路径相似度在固定WordNet版本上可重放？ 本章各目录坐标如何汇合为同一证据链？”“附录B 运行WordNet”建立参考、故障与恢复路径。只有它守住“NLTK/WordNet版本、词性、synset ID、关系类型与相似度定义一致；附录B 运行WordNet的结论不得越过原版目录与数据边界”并交付dna-app-b语料与词表快照、张量/时序账本、前向缓存、梯度轨迹、评价结果、反例与失败复现，近邻、序列输出、注意力图或指标才构成NLP证据。

## 书目、191个原版层级与版本边界

“附录B 运行WordNet”以[O’Reilly Japan原版官方书页](https://www.oreilly.co.jp/books/9784873118369/)核对斋藤康毅著《ゼロから作るDeep Learning ❷ ―自然言語処理編》于2018年7月21日出版、432页、ISBN 9784873118369。对“附录B 运行WordNet”而言，出版社同页给出的8章与附录A至C构成完整目录边界；本站逐项统计8个章标题、46个一级节、125个二级节、3个附录标题和9个附录节，共191个正式目录层级。“附录B 运行WordNet”再以[出版社官方代码仓库](https://github.com/oreilly-japan/deep-learning-from-scratch-2)核对ch01至ch08实现，以[官方勘误](https://github.com/oreilly-japan/deep-learning-from-scratch-2/wiki/errata)识别已知错误。

“附录B 运行WordNet”没有使用未获授权的原书完整正文，只以出版社完整目录、官方代码和勘误限定范围；中文解释、代码实验、交互、练习与答案均为独立教学重写。“附录B 运行WordNet”的[中文授权书目页](https://www.oreilly.com/library/view/shen-du-xue-xi-jin-jie-zi-ran-yu-yan-chu-li/9787115547644/)仅用于核对陆宇杰译《深度学习进阶：自然语言处理》、人民邮电出版社2020年10月、ISBN 9787115547644，不用来证明日文原版正文。“附录B 运行WordNet”以2018年日文原版与2020年中文译本目录为内容边界；当前大模型、现代Transformer变体与依赖兼容修正只作带时间标签的独立核验。

“附录B 运行WordNet”另以[技术核对 1](https://www.oreilly.co.jp/books/9784873118369/)、[技术核对 2](https://github.com/oreilly-japan/deep-learning-from-scratch-2)、[技术核对 3](https://wordnet.princeton.edu/)核对算法定义、实现语义或数值诊断；这些外部资料能验证本页技术事实，不能反向证明原书采用了本站表述。

## 原版目录层级与实现机制

### 附录B 运行WordNet

<Term def="运行WordNet对应原版目录坐标“附录B 运行WordNet”，在“附录B 运行WordNet”中用于声明本章在表示—序列—评价链中的职责与范围，并受语料/数据、词表、dtype/shape、时序状态、梯度、评价协议与版本边界约束。">
  运行WordNet
</Term>

**原版坐标 1/5。** 在“附录B 运行WordNet”的坐标1中，目录项「附录B 运行WordNet」用于声明本章在表示—序列—评价链中的职责与范围；先冻结语料/数据、词表、时序与shape，再以原版坐标、输入数据、核心状态、下游接口、反例和时间边界复核，出现用当前热点重排原版或只罗列术语时撤回结论。

### B.1 NLTK的安装

<Term def="B.1 NLTK的安装对应原版目录坐标“B·1 NLTK的安装”，在“附录B 运行WordNet”中用于在固定WordNet资源上查询synset、关系边和路径相似度，并受语料/数据、词表、dtype/shape、时序状态、梯度、评价协议与版本边界约束。">
  B.1 NLTK的安装
</Term>

**原版坐标 2/5。** 目录项「B·1 NLTK的安装」进入“附录B 运行WordNet”后要回答第2个NLP问题：它怎样在固定WordNet资源上查询synset、关系边和路径相似度、改变哪个词ID、表示、时序状态、梯度或评价结果、由哪些资源版本、词性、synset ID、关系类型、路径与相似度定义证明，并如何排除混合词性或把词典路径当作普适语义真值。

### B.2 使用WordNet获得同义词

<Term def="B.2 使用WordNet获得同义词对应原版目录坐标“B·2 使用WordNet获得同义词”，在“附录B 运行WordNet”中用于把固定语料变换为可比较的词ID、共现统计与低维表示，并受语料/数据、词表、dtype/shape、时序状态、梯度、评价协议与版本边界约束。">
  B.2 使用WordNet获得同义词
</Term>

**原版坐标 3/5。** 围绕“怎样让synset、关系边和路径相似度在固定WordNet版本上可重放？ 本章各目录坐标如何汇合为同一证据链？”“附录B 运行WordNet”在原版层级3把「B·2 使用WordNet获得同义词」落实为把固定语料变换为可比较的词ID、共现统计与低维表示；复核者先读取语料版本、token/ID映射、窗口、共现矩阵、PPMI、SVD维度和近邻结果，不能接受词表漂移、语料泄漏或不同维度下直接比较相似度。

### B.3 WordNet和单词网络

<Term def="B.3 WordNet和单词网络对应原版目录坐标“B·3 WordNet和单词网络”，在“附录B 运行WordNet”中用于把固定语料变换为可比较的词ID、共现统计与低维表示，并受语料/数据、词表、dtype/shape、时序状态、梯度、评价协议与版本边界约束。">
  B.3 WordNet和单词网络
</Term>

**原版坐标 4/5。** 对“附录B 运行WordNet”而言，目录项「B·3 WordNet和单词网络」的最小NLP实验合同是把固定语料变换为可比较的词ID、共现统计与低维表示，第4次检查保存语料版本、token/ID映射、窗口、共现矩阵、PPMI、SVD维度和近邻结果；若产生词表漂移、语料泄漏或不同维度下直接比较相似度，就返回同一语料与词表快照。

### B.4 基于WordNet的语义相似度

<Term def="B.4 基于WordNet的语义相似度对应原版目录坐标“B·4 基于WordNet的语义相似度”，在“附录B 运行WordNet”中用于把固定语料变换为可比较的词ID、共现统计与低维表示，并受语料/数据、词表、dtype/shape、时序状态、梯度、评价协议与版本边界约束。">
  B.4 基于WordNet的语义相似度
</Term>

**原版坐标 5/5。** 第5个正式坐标「B·4 基于WordNet的语义相似度」服务于安装并运行WordNet，区分同义词网络与分布式表示的证据来源，并逐项覆盖附录B 运行WordNet、B.1 NLTK的安装、B.2 使用WordNet获得同义词、B.3 WordNet和单词网络、B.4 基于WordNet的语义相似度，需要以语料版本、token/ID映射、窗口、共现矩阵、PPMI、SVD维度和近邻结果呈现把固定语料变换为可比较的词ID、共现统计与低维表示；词表漂移、语料泄漏或不同维度下直接比较相似度会破坏“NLTK/WordNet版本、词性、synset ID、关系类型与相似度定义一致；附录B 运行WordNet的结论不得越过原版目录与数据边界”。

## 先预测，再操作三个章节专属NLP实验

<Callout type="info" title="先写出哪个词ID、表示或序列状态会先变化">
  对“附录B
  运行WordNet”先冻结语料/数据、词表、切分、dtype/shape、时间顺序、容差和种子，再操作表示账本、序列轨迹和评价验收门；结果与预测不一致时修改假设，不删除失败运行。
</Callout>

<Stepper>
  <Step title="1. 语料、表示与shape账本">
    固定“为“附录B 运行WordNet”冻结语料/数据、词表、shape、时序切分、随机种子与代码版本，再对照参考路径和单故障路径。”，在参考与反例间切换，逐阶段查看“锁定资源、查询synset、遍历关系、计算相似度、比较边界”的输入词ID、表示/算子动作、输出和shape检查。

    <DnaAppendixBWordnetRepresentationLedgerLab />

  </Step>
  <Step title="2. 参考与单故障序列轨迹">
    保持语料/数据、词表、输入shape、时间切分和初值不变，只注入“混合不同词性或语料版本，仍把路径距离解释为通用语义真值；在附录B 运行WordNet验收中只注入这一处”，定位第一处偏离“NLTK/WordNet版本、词性、synset ID、关系类型与相似度定义一致；附录B 运行WordNet的结论不得越过原版目录与数据边界”的表示、状态、梯度、采样或评价结果。

    <DnaAppendixBWordnetSequenceTraceLab />

  </Step>
  <Step title="3. 独立评价与历史边界验收门">
    分别锁定数据/词表、表示shape、时序/梯度、随机采样与独立评价，展开dna-app-b语料与词表快照、张量/时序账本、前向缓存、梯度轨迹、评价结果、反例与失败复现后决定是否接受本章结论。

    <DnaAppendixBWordnetEvaluationGateLab />

  </Step>
</Stepper>

<Callout
  type="trap"
  title="本页单一故障：混合不同词性或语料版本，仍把路径距离解释为通用语义真值；在附录B 运行WordNet验收中只注入这一处"
>
  “附录B
  运行WordNet”遇到该故障时保持其余语料/数据、词表、切分、dtype/shape、时间顺序、容差和种子不变，沿五阶段寻找最早偏离；最终指标看似合理不能证明“NLTK/WordNet版本、词性、synset
  ID、关系类型与相似度定义一致；附录B
  运行WordNet的结论不得越过原版目录与数据边界”。
</Callout>

<Callout type="trap" title="能训练不等于表示、序列与评价语义正确">
  为“附录B
  运行WordNet”冻结语料/数据、词表、shape、时序切分、随机种子与代码版本，再对照参考路径和单故障路径。
  的代码只说明当前样例没有报错；“附录B
  运行WordNet”仍需词ID映射、dtype/shape、时序状态、梯度对照、采样记录、独立评价和边界反例。
</Callout>

<Callout type="trap" title="当前大模型不能冒充2018年原版">
  “附录B
  运行WordNet”引用现行文档是为了核对技术语义；当前大模型、现代Transformer变体与依赖兼容修正必须单列时间标签，不能倒填2018年日文原版或2020年中文译本；Transformer只属于原版8.5.2。
</Callout>

## 附录B 运行WordNet的可重放实现协议

| 阶段       | 允许动作                                                                          | 必留证据                                                   | 拒绝条件                                                                                      |
| ---------- | --------------------------------------------------------------------------------- | ---------------------------------------------------------- | --------------------------------------------------------------------------------------------- |
| 锁定资源   | 在“附录B 运行WordNet”执行锁定资源，只改变声明的表示、序列、梯度、采样或评价状态   | 语料/数据哈希、词表、切分、dtype/shape与版本               | 语料、词表或切分不可追溯                                                                      |
| 查询synset | 在“附录B 运行WordNet”执行查询synset，只改变声明的表示、序列、梯度、采样或评价状态 | 词ID、输入输出shape、表示/隐藏状态、缓存、梯度、采样与损失 | 混合不同词性或语料版本，仍把路径距离解释为通用语义真值；在附录B 运行WordNet验收中只注入这一处 |
| 遍历关系   | 在“附录B 运行WordNet”执行遍历关系，只改变声明的表示、序列、梯度、采样或评价状态   | 词ID、输入输出shape、表示/隐藏状态、缓存、梯度、采样与损失 | 混合不同词性或语料版本，仍把路径距离解释为通用语义真值；在附录B 运行WordNet验收中只注入这一处 |
| 计算相似度 | 在“附录B 运行WordNet”执行计算相似度，只改变声明的表示、序列、梯度、采样或评价状态 | 词ID、输入输出shape、表示/隐藏状态、缓存、梯度、采样与损失 | 混合不同词性或语料版本，仍把路径距离解释为通用语义真值；在附录B 运行WordNet验收中只注入这一处 |
| 比较边界   | 在“附录B 运行WordNet”执行比较边界，只改变声明的表示、序列、梯度、采样或评价状态   | 独立评价、反例、随机性、2018/当前边界与复现                | 无法重放或缺少独立评价                                                                        |

```yaml
unit: "dna-app-b"
question: "怎样让synset、关系边和路径相似度在固定WordNet版本上可重放？ 本章各目录坐标如何汇合为同一证据链？"
scenario: "为“附录B 运行WordNet”冻结语料/数据、词表、shape、时序切分、随机种子与代码版本，再对照参考路径和单故障路径。"
stages: ["锁定资源", "查询synset", "遍历关系", "计算相似度", "比较边界"]
invariant: "NLTK/WordNet版本、词性、synset ID、关系类型与相似度定义一致；附录B 运行WordNet的结论不得越过原版目录与数据边界"
fault: "混合不同词性或语料版本，仍把路径距离解释为通用语义真值；在附录B 运行WordNet验收中只注入这一处"
evidence: "dna-app-b语料与词表快照、张量/时序账本、前向缓存、梯度轨迹、评价结果、反例与失败复现"
reset: restore_case_stage_trace_mode_step_gates_and_artifact
```

该协议要求“附录B 运行WordNet”在相同语料/数据、词表、切分、dtype/shape、初值、时间顺序、容差和种子下重放。重置后若案例、阶段、轨迹模式、步骤、评价门或证据包没有回到基线，交互状态已经污染比较。

## 本页回顾

掌握“附录B 运行WordNet”不是背诵模型名称或最终指标，而是能围绕“怎样让synset、关系边和路径相似度在固定WordNet版本上可重放？ 本章各目录坐标如何汇合为同一证据链？”重建语料/数据、词表、表示、时序状态、梯度、采样与评价证据，并用“NLTK/WordNet版本、词性、synset ID、关系类型与相似度定义一致；附录B 运行WordNet的结论不得越过原版目录与数据边界”拒绝“混合不同词性或语料版本，仍把路径距离解释为通用语义真值；在附录B 运行WordNet验收中只注入这一处”。最终交付为dna-app-b语料与词表快照、张量/时序账本、前向缓存、梯度轨迹、评价结果、反例与失败复现

## 练习与答案

<Exercises>

1. **问题 1：NLP实验合同。** “附录B 运行WordNet”为什么必须先冻结语料/数据、词表、切分、dtype/shape、初值、时间顺序、容差和种子？

<Answer>
  为“附录B
  运行WordNet”冻结语料/数据、词表、shape、时序切分、随机种子与代码版本，再对照参考路径和单故障路径。
  若这些条件变化，相同输出可能来自不同词ID、表示、隐藏状态、梯度、采样或评价轨迹；“附录B
  运行WordNet”先冻结合同，才能把观测连接到单一NLP机制。
</Answer>

2. **问题 2：目录逐项覆盖。** 怎样证明原版目录坐标已经进入机制、交互和练习？

<Answer>
  1. “附录B 运行WordNet”的目录项「附录B
  运行WordNet」：以“声明本章在表示—序列—评价链中的职责与范围”解释实现作用，用“原版坐标、输入数据、核心状态、下游接口、反例和时间边界”提供复核。
  2. “附录B 运行WordNet”的目录项「B·1
  NLTK的安装」：以“在固定WordNet资源上查询synset、关系边和路径相似度”解释实现作用，用“资源版本、词性、synset
  ID、关系类型、路径与相似度定义”提供复核。 3. “附录B 运行WordNet”的目录项「B·2
  使用WordNet获得同义词」：以“把固定语料变换为可比较的词ID、共现统计与低维表示”解释实现作用，用“语料版本、token/ID映射、窗口、共现矩阵、PPMI、SVD维度和近邻结果”提供复核。
  4. “附录B 运行WordNet”的目录项「B·3
  WordNet和单词网络」：以“把固定语料变换为可比较的词ID、共现统计与低维表示”解释实现作用，用“语料版本、token/ID映射、窗口、共现矩阵、PPMI、SVD维度和近邻结果”提供复核。
  5. “附录B 运行WordNet”的目录项「B·4
  基于WordNet的语义相似度」：以“把固定语料变换为可比较的词ID、共现统计与低维表示”解释实现作用，用“语料版本、token/ID映射、窗口、共现矩阵、PPMI、SVD维度和近邻结果”提供复核。
</Answer>

3. **问题 3：故障恢复。** 怎样证明“混合不同词性或语料版本，仍把路径距离解释为通用语义真值；在附录B 运行WordNet验收中只注入这一处”已经被修正？

<Answer>
  为“附录B
  运行WordNet”复用同一语料/数据、词表、切分、dtype/shape、初值、时间顺序、容差和种子，重放参考路径后只注入“混合不同词性或语料版本，仍把路径距离解释为通用语义真值；在附录B
  运行WordNet验收中只注入这一处”；记录最早偏离，撤销故障再运行。只有表示账本、序列轨迹、评价门和dna-app-b语料与词表快照、张量/时序账本、前向缓存、梯度轨迹、评价结果、反例与失败复现重新满足“NLTK/WordNet版本、词性、synset
  ID、关系类型与相似度定义一致；附录B
  运行WordNet的结论不得越过原版目录与数据边界”，修正才可提交。
</Answer>

</Exercises>

## 小结

- WordNet 是词汇语义网络
- 同义词集组织词义
- 上下位关系构成层次
- NLTK 接口便捷查询
- 词典资源辅助词义研究

## 名词解释

<Glossary>
  <GlossaryItem term="运行WordNet">
    检索键 dna-A 对应目录坐标「附录B 运行WordNet」；在“附录B
    运行WordNet”中用于声明本章在表示—序列—评价链中的职责与范围，需要连接原版范围、语料/词表、shape、时序/梯度轨迹、采样与评价证据。
  </GlossaryItem>
  <GlossaryItem term="B.1 NLTK的安装">
    检索键 dna-B 对应目录坐标「B·1 NLTK的安装」；在“附录B
    运行WordNet”中用于在固定WordNet资源上查询synset、关系边和路径相似度，需要连接原版范围、语料/词表、shape、时序/梯度轨迹、采样与评价证据。
  </GlossaryItem>
  <GlossaryItem term="B.2 使用WordNet获得同义词">
    检索键 dna-C 对应目录坐标「B·2 使用WordNet获得同义词」；在“附录B
    运行WordNet”中用于把固定语料变换为可比较的词ID、共现统计与低维表示，需要连接原版范围、语料/词表、shape、时序/梯度轨迹、采样与评价证据。
  </GlossaryItem>
  <GlossaryItem term="B.3 WordNet和单词网络">
    检索键 dna-D 对应目录坐标「B·3 WordNet和单词网络」；在“附录B
    运行WordNet”中用于把固定语料变换为可比较的词ID、共现统计与低维表示，需要连接原版范围、语料/词表、shape、时序/梯度轨迹、采样与评价证据。
  </GlossaryItem>
  <GlossaryItem term="B.4 基于WordNet的语义相似度">
    检索键 dna-E 对应目录坐标「B·4 基于WordNet的语义相似度」；在“附录B
    运行WordNet”中用于把固定语料变换为可比较的词ID、共现统计与低维表示，需要连接原版范围、语料/词表、shape、时序/梯度轨迹、采样与评价证据。
  </GlossaryItem>
</Glossary>

<Attribution
  mode="independent-rewrite"
  sourceBasis="outline-plus-official-code-and-errata"
  workTitle="斋藤康毅著《ゼロから作るDeep Learning ❷ ―自然言語処理編》"
  adaptedUrl="https://www.oreilly.co.jp/books/9784873118369/"
/>
