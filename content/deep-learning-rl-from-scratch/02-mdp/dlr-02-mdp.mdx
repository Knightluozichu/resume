---
title: "第2章 马尔可夫决策过程"
type: B
section: "第2章 马尔可夫决策过程"
order: 2
description: "第2章 马尔可夫决策过程逐项覆盖权威目录，以公式、最小轨迹、交互实验和失败对照重建核心。"
demo: true
math: true
sourceUrl: "https://www.oreilly.co.jp/books/9784873119755/"
draft: false
---


## 为什么先冻结交互合同

、、、、、共同构成本页坐标。把智能体与环境的交互形式化为状态、动作、转移、奖励和策略，并以折扣收益定义优化目标。

强化学习同时改变数据分布和决策策略，奖励上升可能来自随机幸运、环境状态泄漏、错误终止、自举目标漂移或评估仍在探索。实验开始前固定环境版本、种子、初始状态分布、策略参数、训练预算和评估协议，并写下哪一条中间证据应最先变化。

本页遵循“定义MDP -> 写条件变量与公式 -> 手算最小轨迹 -> 运行单变量实验 -> 对照独立评价 -> 注入失败并重放”。首处分叉出现后停止扩大训练，从环境reset和空派生状态开始复现，不能用最终奖励替代过程证据。

## 数学骨架

$$
P(s'\mid s,a)
$$

$$
r(s,a,s')=\mathbb{E}[R_{t+1}\mid S_t=s,A_t=a,S_{t+1}=s']
$$

$$
G_t=\sum_{k=0}^{\infty}\gamma^kR_{t+k+1}
$$

$$
v_\pi(s)=\mathbb{E}_\pi[G_t\mid S_t=s]
$$

每个方程进入实现前都声明时间索引、条件分布、终止mask和更新前旧值。期望、max、采样动作和目标动作必须区分，神经网络目标要停止梯度，概率比和策略梯度要核对支持集与归一化。

## 可复现证据工作表

第一栏保存环境版本、reset种子、初始状态、动作空间、奖励定义、终止与截断标志。第二栏按时间记录状态、行为策略概率、动作、奖励、下一状态和目标策略概率；模型型方法再记转移分布，无模型方法保留原始样本身份。

第三栏保存更新前价值、目标值、TD误差、学习率、更新后价值与策略概率。函数近似还要记录网络参数版本、回放索引、目标网络同步时刻和梯度范数。第四栏写独立评价协议：关闭探索和训练随机层，使用固定未见种子，从环境reset重新生成轨迹。

失败对照只改变一个条件，例如错置终止mask、复用旧状态、把SARSA目标换成max、让目标网络参与梯度或使用不覆盖目标策略的行为策略。检测器应在预先声明的第一条证据处报警；若只能从最终奖励发现问题，工作表仍不合格。

## 核心实现合同

1. **环境合同：** reset、step、终止、截断和奖励时刻明确，训练与评估不能共享未重置状态。
2. **策略合同：** 动作概率归一，探索动作与目标动作分别记录；随机种子只用于复现，不用于挑选好结果。
3. **目标合同：** 收益、贝尔曼目标、TD目标或优势从保存轨迹独立重算，终止后自举为零。
4. **更新合同：** 使用更新前旧值；表格重复访问和网络批量梯度正确聚合，目标分支按算法要求停止梯度。
5. **评价合同：** 多个固定种子报告均值、波动和训练预算，评估关闭探索、Dropout与参数更新。
6. **回退合同：** 首错出现后清空环境、回放、梯度、优化器和目标网络缓存，从原始配置完整重放。

## 权威目录逐项讲解

### 第2章 马尔可夫决策过程

把该小节放回状态、动作、奖励、目标和更新主链，先声明输入输出，再用最小轨迹核对中间证据与失败边界。 针对“第2章 马尔可夫决策过程”，运行前预测最先变化的转移、价值、TD误差、梯度或策略概率；验收合同为“状态必须含足够的马尔可夫信息；转移与奖励的条件变量、终止边界、折扣因子和策略归一化轴必须明确。”。

先固定环境、策略、参数和随机种子，手算一个最小轨迹。若累计奖励看似改善但首个预期证据没有变化，应停止训练，清空回放、梯度、优化器与环境状态后完整重放。

### 2.1 什么是MDP

先画智能体与环境边界，声明状态是否满足马尔可夫性、动作集合、转移条件、奖励时刻和终止标志。用一条两三步轨迹手算折扣收益，核对策略在动作轴上归一。 针对“2.1 什么是MDP”，运行前预测最先变化的转移、价值、TD误差、梯度或策略概率；验收合同为“状态必须含足够的马尔可夫信息；转移与奖励的条件变量、终止边界、折扣因子和策略归一化轴必须明确。”。

先固定环境、策略、参数和随机种子，手算一个最小轨迹。若累计奖励看似改善但首个预期证据没有变化，应停止训练，清空回放、梯度、优化器与环境状态后完整重放。

### 2.1.1 MDP的具体例子

先画智能体与环境边界，声明状态是否满足马尔可夫性、动作集合、转移条件、奖励时刻和终止标志。用一条两三步轨迹手算折扣收益，核对策略在动作轴上归一。 针对“2.1.1 MDP的具体例子”，运行前预测最先变化的转移、价值、TD误差、梯度或策略概率；验收合同为“状态必须含足够的马尔可夫信息；转移与奖励的条件变量、终止边界、折扣因子和策略归一化轴必须明确。”。

先固定环境、策略、参数和随机种子，手算一个最小轨迹。若累计奖励看似改善但首个预期证据没有变化，应停止训练，清空回放、梯度、优化器与环境状态后完整重放。

### 2.1.2 智能体与环境的互动

把该小节放回状态、动作、奖励、目标和更新主链，先声明输入输出，再用最小轨迹核对中间证据与失败边界。 针对“2.1.2 智能体与环境的互动”，运行前预测最先变化的转移、价值、TD误差、梯度或策略概率；验收合同为“状态必须含足够的马尔可夫信息；转移与奖励的条件变量、终止边界、折扣因子和策略归一化轴必须明确。”。

先固定环境、策略、参数和随机种子，手算一个最小轨迹。若累计奖励看似改善但首个预期证据没有变化，应停止训练，清空回放、梯度、优化器与环境状态后完整重放。

### 2.2 环境和智能体的数学表示

把该小节放回状态、动作、奖励、目标和更新主链，先声明输入输出，再用最小轨迹核对中间证据与失败边界。 针对“2.2 环境和智能体的数学表示”，运行前预测最先变化的转移、价值、TD误差、梯度或策略概率；验收合同为“状态必须含足够的马尔可夫信息；转移与奖励的条件变量、终止边界、折扣因子和策略归一化轴必须明确。”。

先固定环境、策略、参数和随机种子，手算一个最小轨迹。若累计奖励看似改善但首个预期证据没有变化，应停止训练，清空回放、梯度、优化器与环境状态后完整重放。

### 2.2.1 状态转移

先画智能体与环境边界，声明状态是否满足马尔可夫性、动作集合、转移条件、奖励时刻和终止标志。用一条两三步轨迹手算折扣收益，核对策略在动作轴上归一。 针对“2.2.1 状态转移”，运行前预测最先变化的转移、价值、TD误差、梯度或策略概率；验收合同为“状态必须含足够的马尔可夫信息；转移与奖励的条件变量、终止边界、折扣因子和策略归一化轴必须明确。”。

先固定环境、策略、参数和随机种子，手算一个最小轨迹。若累计奖励看似改善但首个预期证据没有变化，应停止训练，清空回放、梯度、优化器与环境状态后完整重放。

### 2.2.2 奖励函数

先画智能体与环境边界，声明状态是否满足马尔可夫性、动作集合、转移条件、奖励时刻和终止标志。用一条两三步轨迹手算折扣收益，核对策略在动作轴上归一。 针对“2.2.2 奖励函数”，运行前预测最先变化的转移、价值、TD误差、梯度或策略概率；验收合同为“状态必须含足够的马尔可夫信息；转移与奖励的条件变量、终止边界、折扣因子和策略归一化轴必须明确。”。

先固定环境、策略、参数和随机种子，手算一个最小轨迹。若累计奖励看似改善但首个预期证据没有变化，应停止训练，清空回放、梯度、优化器与环境状态后完整重放。

### 2.2.3 智能体的策略

先画智能体与环境边界，声明状态是否满足马尔可夫性、动作集合、转移条件、奖励时刻和终止标志。用一条两三步轨迹手算折扣收益，核对策略在动作轴上归一。 针对“2.2.3 智能体的策略”，运行前预测最先变化的转移、价值、TD误差、梯度或策略概率；验收合同为“状态必须含足够的马尔可夫信息；转移与奖励的条件变量、终止边界、折扣因子和策略归一化轴必须明确。”。

先固定环境、策略、参数和随机种子，手算一个最小轨迹。若累计奖励看似改善但首个预期证据没有变化，应停止训练，清空回放、梯度、优化器与环境状态后完整重放。

### 2.3 MDP的目标

先画智能体与环境边界，声明状态是否满足马尔可夫性、动作集合、转移条件、奖励时刻和终止标志。用一条两三步轨迹手算折扣收益，核对策略在动作轴上归一。 针对“2.3 MDP的目标”，运行前预测最先变化的转移、价值、TD误差、梯度或策略概率；验收合同为“状态必须含足够的马尔可夫信息；转移与奖励的条件变量、终止边界、折扣因子和策略归一化轴必须明确。”。

先固定环境、策略、参数和随机种子，手算一个最小轨迹。若累计奖励看似改善但首个预期证据没有变化，应停止训练，清空回放、梯度、优化器与环境状态后完整重放。

### 2.3.1 回合制任务和连续性任务

先画智能体与环境边界，声明状态是否满足马尔可夫性、动作集合、转移条件、奖励时刻和终止标志。用一条两三步轨迹手算折扣收益，核对策略在动作轴上归一。 针对“2.3.1 回合制任务和连续性任务”，运行前预测最先变化的转移、价值、TD误差、梯度或策略概率；验收合同为“状态必须含足够的马尔可夫信息；转移与奖励的条件变量、终止边界、折扣因子和策略归一化轴必须明确。”。

先固定环境、策略、参数和随机种子，手算一个最小轨迹。若累计奖励看似改善但首个预期证据没有变化，应停止训练，清空回放、梯度、优化器与环境状态后完整重放。

### 2.3.2 收益

先画智能体与环境边界，声明状态是否满足马尔可夫性、动作集合、转移条件、奖励时刻和终止标志。用一条两三步轨迹手算折扣收益，核对策略在动作轴上归一。 针对“2.3.2 收益”，运行前预测最先变化的转移、价值、TD误差、梯度或策略概率；验收合同为“状态必须含足够的马尔可夫信息；转移与奖励的条件变量、终止边界、折扣因子和策略归一化轴必须明确。”。

先固定环境、策略、参数和随机种子，手算一个最小轨迹。若累计奖励看似改善但首个预期证据没有变化，应停止训练，清空回放、梯度、优化器与环境状态后完整重放。

### 2.3.3 状态价值函数

把收益拆成一步奖励与下一状态剩余收益，分别对策略动作和环境转移求期望。最优方程才使用max；固定策略评价使用期望，二者混用会在第一轮备份就出现可检测差异。 针对“2.3.3 状态价值函数”，运行前预测最先变化的转移、价值、TD误差、梯度或策略概率；验收合同为“状态必须含足够的马尔可夫信息；转移与奖励的条件变量、终止边界、折扣因子和策略归一化轴必须明确。”。

先固定环境、策略、参数和随机种子，手算一个最小轨迹。若累计奖励看似改善但首个预期证据没有变化，应停止训练，清空回放、梯度、优化器与环境状态后完整重放。

### 2.3.4 最优策略和最优价值函数

先画智能体与环境边界，声明状态是否满足马尔可夫性、动作集合、转移条件、奖励时刻和终止标志。用一条两三步轨迹手算折扣收益，核对策略在动作轴上归一。 针对“2.3.4 最优策略和最优价值函数”，运行前预测最先变化的转移、价值、TD误差、梯度或策略概率；验收合同为“状态必须含足够的马尔可夫信息；转移与奖励的条件变量、终止边界、折扣因子和策略归一化轴必须明确。”。

先固定环境、策略、参数和随机种子，手算一个最小轨迹。若累计奖励看似改善但首个预期证据没有变化，应停止训练，清空回放、梯度、优化器与环境状态后完整重放。

### 2.4 MDP的例子

先画智能体与环境边界，声明状态是否满足马尔可夫性、动作集合、转移条件、奖励时刻和终止标志。用一条两三步轨迹手算折扣收益，核对策略在动作轴上归一。 针对“2.4 MDP的例子”，运行前预测最先变化的转移、价值、TD误差、梯度或策略概率；验收合同为“状态必须含足够的马尔可夫信息；转移与奖励的条件变量、终止边界、折扣因子和策略归一化轴必须明确。”。

先固定环境、策略、参数和随机种子，手算一个最小轨迹。若累计奖励看似改善但首个预期证据没有变化，应停止训练，清空回放、梯度、优化器与环境状态后完整重放。

### 2.4.1 回溯线图

先画智能体与环境边界，声明状态是否满足马尔可夫性、动作集合、转移条件、奖励时刻和终止标志。用一条两三步轨迹手算折扣收益，核对策略在动作轴上归一。 针对“2.4.1 回溯线图”，运行前预测最先变化的转移、价值、TD误差、梯度或策略概率；验收合同为“状态必须含足够的马尔可夫信息；转移与奖励的条件变量、终止边界、折扣因子和策略归一化轴必须明确。”。

先固定环境、策略、参数和随机种子，手算一个最小轨迹。若累计奖励看似改善但首个预期证据没有变化，应停止训练，清空回放、梯度、优化器与环境状态后完整重放。

### 2.4.2 找出最优策略

先画智能体与环境边界，声明状态是否满足马尔可夫性、动作集合、转移条件、奖励时刻和终止标志。用一条两三步轨迹手算折扣收益，核对策略在动作轴上归一。 针对“2.4.2 找出最优策略”，运行前预测最先变化的转移、价值、TD误差、梯度或策略概率；验收合同为“状态必须含足够的马尔可夫信息；转移与奖励的条件变量、终止边界、折扣因子和策略归一化轴必须明确。”。

先固定环境、策略、参数和随机种子，手算一个最小轨迹。若累计奖励看似改善但首个预期证据没有变化，应停止训练，清空回放、梯度、优化器与环境状态后完整重放。

### 2.5 小结

把该小节放回状态、动作、奖励、目标和更新主链，先声明输入输出，再用最小轨迹核对中间证据与失败边界。 针对“2.5 小结”，运行前预测最先变化的转移、价值、TD误差、梯度或策略概率；验收合同为“状态必须含足够的马尔可夫信息；转移与奖励的条件变量、终止边界、折扣因子和策略归一化轴必须明确。”。

先固定环境、策略、参数和随机种子，手算一个最小轨迹。若累计奖励看似改善但首个预期证据没有变化，应停止训练，清空回放、梯度、优化器与环境状态后完整重放。

## 常见失败与边界


## 本章回顾

把智能体与环境的交互形式化为状态、动作、转移、奖励和策略，并以折扣收益定义优化目标。 验收底线是：状态必须含足够的马尔可夫信息；转移与奖励的条件变量、终止边界、折扣因子和策略归一化轴必须明确。。掌握标准是能从固定环境预测首个变化、手算最小轨迹、复现实验、解释失败样本，并证明独立评价不读取训练期状态。


