---
title: "第8章 DQN"
type: B
section: "第8章 DQN"
order: 8
description: "第8章 DQN逐项覆盖权威目录，以公式、最小轨迹、交互实验和失败对照重建核心。"
demo: true
math: true
sourceUrl: "https://www.oreilly.co.jp/books/9784873119755/"
draft: false
---


## 为什么先冻结交互合同

、、、、、共同构成本页坐标。以经验回放打破时间相关、目标网络稳定自举，再扩展到Atari、Double、优先回放与Dueling结构。

强化学习同时改变数据分布和决策策略，奖励上升可能来自随机幸运、环境状态泄漏、错误终止、自举目标漂移或评估仍在探索。实验开始前固定环境版本、种子、初始状态分布、策略参数、训练预算和评估协议，并写下哪一条中间证据应最先变化。

本页遵循“定义MDP -> 写条件变量与公式 -> 手算最小轨迹 -> 运行单变量实验 -> 对照独立评价 -> 注入失败并重放”。首处分叉出现后停止扩大训练，从环境reset和空派生状态开始复现，不能用最终奖励替代过程证据。

## 数学骨架

$$
y=r+\gamma(1-d)\max_{a'}Q(s',a';\theta^-)
$$

$$
L=\mathbb{E}_{(s,a,r,s')\sim D}[(y-Q(s,a;\theta))^2]
$$

$$
a^*=\arg\max_aQ(s',a;\theta),\quad y_{DDQN}=r+\gamma Q(s',a^*;\theta^-)
$$

$$
Q(s,a)=V(s)+A(s,a)-\frac{1}{|\mathcal{A}|}\sum_{a'}A(s,a')
$$

每个方程进入实现前都声明时间索引、条件分布、终止mask和更新前旧值。期望、max、采样动作和目标动作必须区分，神经网络目标要停止梯度，概率比和策略梯度要核对支持集与归一化。

## 可复现证据工作表

第一栏保存环境版本、reset种子、初始状态、动作空间、奖励定义、终止与截断标志。第二栏按时间记录状态、行为策略概率、动作、奖励、下一状态和目标策略概率；模型型方法再记转移分布，无模型方法保留原始样本身份。

第三栏保存更新前价值、目标值、TD误差、学习率、更新后价值与策略概率。函数近似还要记录网络参数版本、回放索引、目标网络同步时刻和梯度范数。第四栏写独立评价协议：关闭探索和训练随机层，使用固定未见种子，从环境reset重新生成轨迹。

失败对照只改变一个条件，例如错置终止mask、复用旧状态、把SARSA目标换成max、让目标网络参与梯度或使用不覆盖目标策略的行为策略。检测器应在预先声明的第一条证据处报警；若只能从最终奖励发现问题，工作表仍不合格。

## 核心实现合同

1. **环境合同：** reset、step、终止、截断和奖励时刻明确，训练与评估不能共享未重置状态。
2. **策略合同：** 动作概率归一，探索动作与目标动作分别记录；随机种子只用于复现，不用于挑选好结果。
3. **目标合同：** 收益、贝尔曼目标、TD目标或优势从保存轨迹独立重算，终止后自举为零。
4. **更新合同：** 使用更新前旧值；表格重复访问和网络批量梯度正确聚合，目标分支按算法要求停止梯度。
5. **评价合同：** 多个固定种子报告均值、波动和训练预算，评估关闭探索、Dropout与参数更新。
6. **回退合同：** 首错出现后清空环境、回放、梯度、优化器和目标网络缓存，从原始配置完整重放。

## 权威目录逐项讲解

### 第8章 DQN

为回放项保存状态、动作、奖励、下一状态、终止标志和插入版本；目标网络按明确周期同步。Double DQN分离动作选择与评价，优先回放还要记录采样概率与重要性权重。 针对“第8章 DQN”，运行前预测最先变化的转移、价值、TD误差、梯度或策略概率；验收合同为“回放样本身份、终止mask、在线/目标参数版本、同步时刻、动作选择与动作评价网络必须分开保存。”。

先固定环境、策略、参数和随机种子，手算一个最小轨迹。若累计奖励看似改善但首个预期证据没有变化，应停止训练，清空回放、梯度、优化器与环境状态后完整重放。

### 8.1 OpenAI Gym

为回放项保存状态、动作、奖励、下一状态、终止标志和插入版本；目标网络按明确周期同步。Double DQN分离动作选择与评价，优先回放还要记录采样概率与重要性权重。 针对“8.1 OpenAI Gym”，运行前预测最先变化的转移、价值、TD误差、梯度或策略概率；验收合同为“回放样本身份、终止mask、在线/目标参数版本、同步时刻、动作选择与动作评价网络必须分开保存。”。

先固定环境、策略、参数和随机种子，手算一个最小轨迹。若累计奖励看似改善但首个预期证据没有变化，应停止训练，清空回放、梯度、优化器与环境状态后完整重放。

### 8.1.1 OpenAI Gym的基础知识

为回放项保存状态、动作、奖励、下一状态、终止标志和插入版本；目标网络按明确周期同步。Double DQN分离动作选择与评价，优先回放还要记录采样概率与重要性权重。 针对“8.1.1 OpenAI Gym的基础知识”，运行前预测最先变化的转移、价值、TD误差、梯度或策略概率；验收合同为“回放样本身份、终止mask、在线/目标参数版本、同步时刻、动作选择与动作评价网络必须分开保存。”。

先固定环境、策略、参数和随机种子，手算一个最小轨迹。若累计奖励看似改善但首个预期证据没有变化，应停止训练，清空回放、梯度、优化器与环境状态后完整重放。

### 8.1.2 随机智能体

把该小节放回状态、动作、奖励、目标和更新主链，先声明输入输出，再用最小轨迹核对中间证据与失败边界。 针对“8.1.2 随机智能体”，运行前预测最先变化的转移、价值、TD误差、梯度或策略概率；验收合同为“回放样本身份、终止mask、在线/目标参数版本、同步时刻、动作选择与动作评价网络必须分开保存。”。

先固定环境、策略、参数和随机种子，手算一个最小轨迹。若累计奖励看似改善但首个预期证据没有变化，应停止训练，清空回放、梯度、优化器与环境状态后完整重放。

### 8.2 DQN的核心技术

为回放项保存状态、动作、奖励、下一状态、终止标志和插入版本；目标网络按明确周期同步。Double DQN分离动作选择与评价，优先回放还要记录采样概率与重要性权重。 针对“8.2 DQN的核心技术”，运行前预测最先变化的转移、价值、TD误差、梯度或策略概率；验收合同为“回放样本身份、终止mask、在线/目标参数版本、同步时刻、动作选择与动作评价网络必须分开保存。”。

先固定环境、策略、参数和随机种子，手算一个最小轨迹。若累计奖励看似改善但首个预期证据没有变化，应停止训练，清空回放、梯度、优化器与环境状态后完整重放。

### 8.2.1 经验回放（Experience Replay）

为回放项保存状态、动作、奖励、下一状态、终止标志和插入版本；目标网络按明确周期同步。Double DQN分离动作选择与评价，优先回放还要记录采样概率与重要性权重。 针对“8.2.1 经验回放（Experience Replay）”，运行前预测最先变化的转移、价值、TD误差、梯度或策略概率；验收合同为“回放样本身份、终止mask、在线/目标参数版本、同步时刻、动作选择与动作评价网络必须分开保存。”。

先固定环境、策略、参数和随机种子，手算一个最小轨迹。若累计奖励看似改善但首个预期证据没有变化，应停止训练，清空回放、梯度、优化器与环境状态后完整重放。

### 8.2.2 经验回放的实现

为回放项保存状态、动作、奖励、下一状态、终止标志和插入版本；目标网络按明确周期同步。Double DQN分离动作选择与评价，优先回放还要记录采样概率与重要性权重。 针对“8.2.2 经验回放的实现”，运行前预测最先变化的转移、价值、TD误差、梯度或策略概率；验收合同为“回放样本身份、终止mask、在线/目标参数版本、同步时刻、动作选择与动作评价网络必须分开保存。”。

先固定环境、策略、参数和随机种子，手算一个最小轨迹。若累计奖励看似改善但首个预期证据没有变化，应停止训练，清空回放、梯度、优化器与环境状态后完整重放。

### 8.2.3 目标网络（Target Network）

为回放项保存状态、动作、奖励、下一状态、终止标志和插入版本；目标网络按明确周期同步。Double DQN分离动作选择与评价，优先回放还要记录采样概率与重要性权重。 针对“8.2.3 目标网络（Target Network）”，运行前预测最先变化的转移、价值、TD误差、梯度或策略概率；验收合同为“回放样本身份、终止mask、在线/目标参数版本、同步时刻、动作选择与动作评价网络必须分开保存。”。

先固定环境、策略、参数和随机种子，手算一个最小轨迹。若累计奖励看似改善但首个预期证据没有变化，应停止训练，清空回放、梯度、优化器与环境状态后完整重放。

### 8.2.4 目标网络的实现

为回放项保存状态、动作、奖励、下一状态、终止标志和插入版本；目标网络按明确周期同步。Double DQN分离动作选择与评价，优先回放还要记录采样概率与重要性权重。 针对“8.2.4 目标网络的实现”，运行前预测最先变化的转移、价值、TD误差、梯度或策略概率；验收合同为“回放样本身份、终止mask、在线/目标参数版本、同步时刻、动作选择与动作评价网络必须分开保存。”。

先固定环境、策略、参数和随机种子，手算一个最小轨迹。若累计奖励看似改善但首个预期证据没有变化，应停止训练，清空回放、梯度、优化器与环境状态后完整重放。

### 8.2.5 运行DQN

为回放项保存状态、动作、奖励、下一状态、终止标志和插入版本；目标网络按明确周期同步。Double DQN分离动作选择与评价，优先回放还要记录采样概率与重要性权重。 针对“8.2.5 运行DQN”，运行前预测最先变化的转移、价值、TD误差、梯度或策略概率；验收合同为“回放样本身份、终止mask、在线/目标参数版本、同步时刻、动作选择与动作评价网络必须分开保存。”。

先固定环境、策略、参数和随机种子，手算一个最小轨迹。若累计奖励看似改善但首个预期证据没有变化，应停止训练，清空回放、梯度、优化器与环境状态后完整重放。

### 8.3 DQN与Atari

为回放项保存状态、动作、奖励、下一状态、终止标志和插入版本；目标网络按明确周期同步。Double DQN分离动作选择与评价，优先回放还要记录采样概率与重要性权重。 针对“8.3 DQN与Atari”，运行前预测最先变化的转移、价值、TD误差、梯度或策略概率；验收合同为“回放样本身份、终止mask、在线/目标参数版本、同步时刻、动作选择与动作评价网络必须分开保存。”。

先固定环境、策略、参数和随机种子，手算一个最小轨迹。若累计奖励看似改善但首个预期证据没有变化，应停止训练，清空回放、梯度、优化器与环境状态后完整重放。

### 8.3.1 Atari的游戏环境

为回放项保存状态、动作、奖励、下一状态、终止标志和插入版本；目标网络按明确周期同步。Double DQN分离动作选择与评价，优先回放还要记录采样概率与重要性权重。 针对“8.3.1 Atari的游戏环境”，运行前预测最先变化的转移、价值、TD误差、梯度或策略概率；验收合同为“回放样本身份、终止mask、在线/目标参数版本、同步时刻、动作选择与动作评价网络必须分开保存。”。

先固定环境、策略、参数和随机种子，手算一个最小轨迹。若累计奖励看似改善但首个预期证据没有变化，应停止训练，清空回放、梯度、优化器与环境状态后完整重放。

### 8.3.2 预处理

先用玩具回归验证网络forward、loss、backward与优化器，再让网络按状态输出全部动作Q值。只索引已执行动作的预测参与当前损失，目标值停止梯度并从保存的转移独立重算。 针对“8.3.2 预处理”，运行前预测最先变化的转移、价值、TD误差、梯度或策略概率；验收合同为“回放样本身份、终止mask、在线/目标参数版本、同步时刻、动作选择与动作评价网络必须分开保存。”。

先固定环境、策略、参数和随机种子，手算一个最小轨迹。若累计奖励看似改善但首个预期证据没有变化，应停止训练，清空回放、梯度、优化器与环境状态后完整重放。

### 8.3.3 CNN

为回放项保存状态、动作、奖励、下一状态、终止标志和插入版本；目标网络按明确周期同步。Double DQN分离动作选择与评价，优先回放还要记录采样概率与重要性权重。 针对“8.3.3 CNN”，运行前预测最先变化的转移、价值、TD误差、梯度或策略概率；验收合同为“回放样本身份、终止mask、在线/目标参数版本、同步时刻、动作选择与动作评价网络必须分开保存。”。

先固定环境、策略、参数和随机种子，手算一个最小轨迹。若累计奖励看似改善但首个预期证据没有变化，应停止训练，清空回放、梯度、优化器与环境状态后完整重放。

### 8.3.4 其他的技巧

把该小节放回状态、动作、奖励、目标和更新主链，先声明输入输出，再用最小轨迹核对中间证据与失败边界。 针对“8.3.4 其他的技巧”，运行前预测最先变化的转移、价值、TD误差、梯度或策略概率；验收合同为“回放样本身份、终止mask、在线/目标参数版本、同步时刻、动作选择与动作评价网络必须分开保存。”。

先固定环境、策略、参数和随机种子，手算一个最小轨迹。若累计奖励看似改善但首个预期证据没有变化，应停止训练，清空回放、梯度、优化器与环境状态后完整重放。

### 8.4 DQN的扩展

为回放项保存状态、动作、奖励、下一状态、终止标志和插入版本；目标网络按明确周期同步。Double DQN分离动作选择与评价，优先回放还要记录采样概率与重要性权重。 针对“8.4 DQN的扩展”，运行前预测最先变化的转移、价值、TD误差、梯度或策略概率；验收合同为“回放样本身份、终止mask、在线/目标参数版本、同步时刻、动作选择与动作评价网络必须分开保存。”。

先固定环境、策略、参数和随机种子，手算一个最小轨迹。若累计奖励看似改善但首个预期证据没有变化，应停止训练，清空回放、梯度、优化器与环境状态后完整重放。

### 8.4.1 Double DQN

为回放项保存状态、动作、奖励、下一状态、终止标志和插入版本；目标网络按明确周期同步。Double DQN分离动作选择与评价，优先回放还要记录采样概率与重要性权重。 针对“8.4.1 Double DQN”，运行前预测最先变化的转移、价值、TD误差、梯度或策略概率；验收合同为“回放样本身份、终止mask、在线/目标参数版本、同步时刻、动作选择与动作评价网络必须分开保存。”。

先固定环境、策略、参数和随机种子，手算一个最小轨迹。若累计奖励看似改善但首个预期证据没有变化，应停止训练，清空回放、梯度、优化器与环境状态后完整重放。

### 8.4.2 优先级经验回放

为回放项保存状态、动作、奖励、下一状态、终止标志和插入版本；目标网络按明确周期同步。Double DQN分离动作选择与评价，优先回放还要记录采样概率与重要性权重。 针对“8.4.2 优先级经验回放”，运行前预测最先变化的转移、价值、TD误差、梯度或策略概率；验收合同为“回放样本身份、终止mask、在线/目标参数版本、同步时刻、动作选择与动作评价网络必须分开保存。”。

先固定环境、策略、参数和随机种子，手算一个最小轨迹。若累计奖励看似改善但首个预期证据没有变化，应停止训练，清空回放、梯度、优化器与环境状态后完整重放。

### 8.4.3 Dueling DQN

为回放项保存状态、动作、奖励、下一状态、终止标志和插入版本；目标网络按明确周期同步。Double DQN分离动作选择与评价，优先回放还要记录采样概率与重要性权重。 针对“8.4.3 Dueling DQN”，运行前预测最先变化的转移、价值、TD误差、梯度或策略概率；验收合同为“回放样本身份、终止mask、在线/目标参数版本、同步时刻、动作选择与动作评价网络必须分开保存。”。

先固定环境、策略、参数和随机种子，手算一个最小轨迹。若累计奖励看似改善但首个预期证据没有变化，应停止训练，清空回放、梯度、优化器与环境状态后完整重放。

### 8.5 小结

把该小节放回状态、动作、奖励、目标和更新主链，先声明输入输出，再用最小轨迹核对中间证据与失败边界。 针对“8.5 小结”，运行前预测最先变化的转移、价值、TD误差、梯度或策略概率；验收合同为“回放样本身份、终止mask、在线/目标参数版本、同步时刻、动作选择与动作评价网络必须分开保存。”。

先固定环境、策略、参数和随机种子，手算一个最小轨迹。若累计奖励看似改善但首个预期证据没有变化，应停止训练，清空回放、梯度、优化器与环境状态后完整重放。

## 常见失败与边界


## 本章回顾

以经验回放打破时间相关、目标网络稳定自举，再扩展到Atari、Double、优先回放与Dueling结构。 验收底线是：回放样本身份、终止mask、在线/目标参数版本、同步时刻、动作选择与动作评价网络必须分开保存。。掌握标准是能从固定环境预测首个变化、手算最小轨迹、复现实验、解释失败样本，并证明独立评价不读取训练期状态。


