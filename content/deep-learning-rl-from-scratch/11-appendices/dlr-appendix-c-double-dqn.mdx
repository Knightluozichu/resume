---
title: "附录C Double DQN的理解"
type: B
section: "附录"
order: 13
description: "附录C Double DQN的理解逐项覆盖权威目录，以公式、最小轨迹、交互实验和失败对照重建核心。"
demo: true
math: true
sourceUrl: "https://www.oreilly.co.jp/books/9784873119755/"
draft: true
---

import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释附录C Double DQN的理解中的状态、动作、目标、更新规则与适用边界
- 能推导并计算四个核心方程中的收益、价值、TD目标或策略梯度
- 能复现三个互动视图，并定位第一条转移、价值、梯度或策略证据分叉
- 能判断“同一下一状态的各动作估计、argmax来源、评价网络版本和目标值必须保存，禁止两步都悄悄使用同一网络。”是否成立并设计失败样本

</Objectives>

## 为什么先冻结交互合同

<Term def="过大估计是本页第1个证据坐标，用于连接轨迹、公式、更新与失败边界。">过大估计</Term>、<Term def="max偏差是本页第2个证据坐标，用于连接轨迹、公式、更新与失败边界。">max偏差</Term>、<Term def="动作选择是本页第3个证据坐标，用于连接轨迹、公式、更新与失败边界。">动作选择</Term>、<Term def="动作评价是本页第4个证据坐标，用于连接轨迹、公式、更新与失败边界。">动作评价</Term>、<Term def="在线网络是本页第5个证据坐标，用于连接轨迹、公式、更新与失败边界。">在线网络</Term>、<Term def="目标网络是本页第6个证据坐标，用于连接轨迹、公式、更新与失败边界。">目标网络</Term>
共同构成本页坐标。用带噪Q估计说明max算子为何产生过大估计，再分离在线网络选动作与目标网络评动作。

强化学习同时改变数据分布和决策策略，奖励上升可能来自随机幸运、环境状态泄漏、错误终止、自举目标漂移或评估仍在探索。实验开始前固定环境版本、种子、初始状态分布、策略参数、训练预算和评估协议，并写下哪一条中间证据应最先变化。

本页遵循“定义MDP -> 写条件变量与公式 -> 手算最小轨迹 -> 运行单变量实验 -> 对照独立评价 -> 注入失败并重放”。首处分叉出现后停止扩大训练，从环境reset和空派生状态开始复现，不能用最终奖励替代过程证据。

## 数学骨架

$$
\mathbb{E}[\max_a\hat{Q}(s,a)]\geq\max_aQ(s,a)
$$

$$
a^*=\arg\max_aQ(s',a;\theta)
$$

$$
y=r+\gamma Q(s',a^*;\theta^-)
$$

$$
L=(y-Q(s,a;\theta))^2
$$

每个方程进入实现前都声明时间索引、条件分布、终止mask和更新前旧值。期望、max、采样动作和目标动作必须区分，神经网络目标要停止梯度，概率比和策略梯度要核对支持集与归一化。

## 可复现证据工作表

第一栏保存环境版本、reset种子、初始状态、动作空间、奖励定义、终止与截断标志。第二栏按时间记录状态、行为策略概率、动作、奖励、下一状态和目标策略概率；模型型方法再记转移分布，无模型方法保留原始样本身份。

第三栏保存更新前价值、目标值、TD误差、学习率、更新后价值与策略概率。函数近似还要记录网络参数版本、回放索引、目标网络同步时刻和梯度范数。第四栏写独立评价协议：关闭探索和训练随机层，使用固定未见种子，从环境reset重新生成轨迹。

失败对照只改变一个条件，例如错置终止mask、复用旧状态、把SARSA目标换成max、让目标网络参与梯度或使用不覆盖目标策略的行为策略。检测器应在预先声明的第一条证据处报警；若只能从最终奖励发现问题，工作表仍不合格。

## 附录推导检查

**逐项展开：** 先把公式中的随机变量、条件分布和求和范围全部写开。在只有两个状态、两个动作、两步时域的环境中枚举每条可能轨迹，逐条计算轨迹概率、奖励、折扣项和更新权重。枚举总概率应为1，按定义直接求得的期望要与公式化简结果一致；这一步能发现索引错一位、终止后继续自举或行为策略概率漏乘。

**极限与边界：** 分别检查折扣为0和接近1、探索率为0和正值、一步与完整回合、在线网络与目标网络相同和不同等边界。每个边界都先写出预期退化形式，再运行实现；例如一步收益应退化为TD目标，完整回合且不自举时应退化为蒙特卡罗收益。极限不一致时，不应继续用大环境平均奖励解释。

**模拟对照：** 用固定种子生成大量独立小轨迹，把样本均值、方差和有效样本量与枚举真值比较。重要性采样记录权重分布，n步方法记录不同n的偏差与方差，Double DQN记录选择网络和评价网络，策略梯度记录解析期望与有限差分。样本曲线只用于验证收敛到理论值，不能反过来替代理论合同。

**回归判据：** 保存一组正常环境和一组单故障环境。修复后从reset重放，两者必须在预定的第一条中间证据处分离；正常路径满足数值容差，故障路径明确报警。最后更换一组未参与调试的固定种子复核，确认结果不依赖旧轨迹、缓存、目标网络或偶然动作序列。

## 核心实现合同

1. **环境合同：** reset、step、终止、截断和奖励时刻明确，训练与评估不能共享未重置状态。
2. **策略合同：** 动作概率归一，探索动作与目标动作分别记录；随机种子只用于复现，不用于挑选好结果。
3. **目标合同：** 收益、贝尔曼目标、TD目标或优势从保存轨迹独立重算，终止后自举为零。
4. **更新合同：** 使用更新前旧值；表格重复访问和网络批量梯度正确聚合，目标分支按算法要求停止梯度。
5. **评价合同：** 多个固定种子报告均值、波动和训练预算，评估关闭探索、Dropout与参数更新。
6. **回退合同：** 首错出现后清空环境、回放、梯度、优化器和目标网络缓存，从原始配置完整重放。

## 权威目录逐项讲解

### 附录C Double DQN的理解

为回放项保存状态、动作、奖励、下一状态、终止标志和插入版本；目标网络按明确周期同步。Double DQN分离动作选择与评价，优先回放还要记录采样概率与重要性权重。 针对“附录C Double DQN的理解”，运行前预测最先变化的转移、价值、TD误差、梯度或策略概率；验收合同为“同一下一状态的各动作估计、argmax来源、评价网络版本和目标值必须保存，禁止两步都悄悄使用同一网络。”。

先固定环境、策略、参数和随机种子，手算一个最小轨迹。若累计奖励看似改善但首个预期证据没有变化，应停止训练，清空回放、梯度、优化器与环境状态后完整重放。

### C.1 什么是过大估计

在极小轨迹上逐项展开目标与概率比，保存每一项来源和终止截断。解析式与枚举或模拟均值比较；若估计依赖未覆盖动作、错误max来源或动作相关基线，应立即判定失败。 针对“C.1 什么是过大估计”，运行前预测最先变化的转移、价值、TD误差、梯度或策略概率；验收合同为“同一下一状态的各动作估计、argmax来源、评价网络版本和目标值必须保存，禁止两步都悄悄使用同一网络。”。

先固定环境、策略、参数和随机种子，手算一个最小轨迹。若累计奖励看似改善但首个预期证据没有变化，应停止训练，清空回放、梯度、优化器与环境状态后完整重放。

### C.2 过大估计的解决方法

在极小轨迹上逐项展开目标与概率比，保存每一项来源和终止截断。解析式与枚举或模拟均值比较；若估计依赖未覆盖动作、错误max来源或动作相关基线，应立即判定失败。 针对“C.2 过大估计的解决方法”，运行前预测最先变化的转移、价值、TD误差、梯度或策略概率；验收合同为“同一下一状态的各动作估计、argmax来源、评价网络版本和目标值必须保存，禁止两步都悄悄使用同一网络。”。

先固定环境、策略、参数和随机种子，手算一个最小轨迹。若累计奖励看似改善但首个预期证据没有变化，应停止训练，清空回放、梯度、优化器与环境状态后完整重放。

## 常见失败与边界

<Callout type="trap">
  **把高奖励当成正确性证明：**
  单个种子的幸运轨迹、评估探索或状态泄漏都能制造高分；必须核对转移、目标、更新和独立种子。
</Callout>

<Callout type="trap">
  **终止与自举错位：**
  回合结束后仍加入下一状态价值，或把时间截断误当自然终止，会系统性污染TD目标与策略比较。
</Callout>

<Callout type="tip">
  **最小重放顺序：**
  先用两三个状态手算整条轨迹和一次更新，再扩大到GridWorld与神经网络；每步只放大一个变量。
</Callout>

## 本章回顾

用带噪Q估计说明max算子为何产生过大估计，再分离在线网络选动作与目标网络评动作。 验收底线是：同一下一状态的各动作估计、argmax来源、评价网络版本和目标值必须保存，禁止两步都悄悄使用同一网络。。掌握标准是能从固定环境预测首个变化、手算最小轨迹、复现实验、解释失败样本，并证明独立评价不读取训练期状态。

<Glossary>

<GlossaryItem term="过大估计">
  “过大估计”对应证据链第1站。记录定义、条件变量、shape、更新前后值、容差和失败边界；只报告累计奖励不足以证明这一环节正确。
</GlossaryItem>

<GlossaryItem term="max偏差">
  “max偏差”对应证据链第2站。记录定义、条件变量、shape、更新前后值、容差和失败边界；只报告累计奖励不足以证明这一环节正确。
</GlossaryItem>

<GlossaryItem term="动作选择">
  “动作选择”对应证据链第3站。记录定义、条件变量、shape、更新前后值、容差和失败边界；只报告累计奖励不足以证明这一环节正确。
</GlossaryItem>

<GlossaryItem term="动作评价">
  “动作评价”对应证据链第4站。记录定义、条件变量、shape、更新前后值、容差和失败边界；只报告累计奖励不足以证明这一环节正确。
</GlossaryItem>

<GlossaryItem term="在线网络">
  “在线网络”对应证据链第5站。记录定义、条件变量、shape、更新前后值、容差和失败边界；只报告累计奖励不足以证明这一环节正确。
</GlossaryItem>

<GlossaryItem term="目标网络">
  “目标网络”对应证据链第6站。记录定义、条件变量、shape、更新前后值、容差和失败边界；只报告累计奖励不足以证明这一环节正确。
</GlossaryItem>

</Glossary>

<Exercises>

1. 如何为附录C Double DQN的理解建立可手算、可重放的最小轨迹？

<Answer>
  选择两三个状态与动作，固定转移、奖励、策略、折扣和随机种子，逐步写出动作概率、奖励、收益或TD目标、更新前后价值，再从reset独立重放。
</Answer>

2. 如何证明“同一下一状态的各动作估计、argmax来源、评价网络版本和目标值必须保存，禁止两步都悄悄使用同一网络。”不是由单次高奖励碰巧满足？

<Answer>
  同时运行正常路径和只破坏一个终止mask、目标动作、概率比或网络版本的失败路径。检测器必须在预测的首个中间量区分两者，并跨固定评估种子报告均值与波动。
</Answer>

3. 修复附录C Double DQN的理解后应按什么顺序回归？

<Answer>
  清空环境、回放、梯度、优化器与目标缓存，从固定配置重放：先查转移与终止，再查目标与更新，然后查梯度和策略分布，最后才跑长期训练与独立评价。
</Answer>

</Exercises>

<Attribution
  adaptedFrom="斋藤康毅著、郑明智译《深度学习入门4：强化学习》，人民邮电出版社，2024年8月，ISBN 9787115649171；原版《ゼロから作るDeep Learning ❹ ―強化学習編》，O'Reilly Japan，2022年4月6日，376页，ISBN 9784873119755 · 基于官方10章、57个一级节、128个二级节、4个附录与6个附录节完整目录的独立教学重构"
  adaptedUrl="https://www.oreilly.co.jp/books/9784873119755/"
/>
