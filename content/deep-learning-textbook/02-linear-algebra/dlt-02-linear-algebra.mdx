---
title: "第2章 线性代数"
type: B
section: "第2章 线性代数"
order: 2
description: "第2章 线性代数逐项覆盖权威目录，以公式、最小张量、交互实验和失败对照重建深度学习证据链。"
demo: true
math: true
sourceUrl: "https://www.deeplearningbook.org/contents/TOC.html"
draft: false
---

import {
  Dlt02LinearAlgebraMapLab,
  Dlt02LinearAlgebraExperimentLab,
  Dlt02LinearAlgebraEvidenceLab,
} from "@/components/mdx/deep-learning-textbook/diagrams/dlt-02-linear-algebra";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第2章 线性代数中的输入、表示、目标、梯度和适用边界
- 能推导并计算四个核心方程中的shape、概率、损失或梯度
- 能复现三个互动视图，并定位第一条激活、目标、梯度或评价证据分叉
- 能判断“每个张量的轴、基、秩和dtype必须记录；矩阵乘法、逐元素乘法、转置和广播不得依靠形状碰巧通过。”是否成立并设计单一故障样本

</Objectives>

## 为什么先冻结模型合同

<Term def="张量是本页第1个证据坐标，用于连接输入、表示、目标、梯度与评价。">张量</Term>、<Term def="矩阵乘法是本页第2个证据坐标，用于连接输入、表示、目标、梯度与评价。">矩阵乘法</Term>、<Term def="范数是本页第3个证据坐标，用于连接输入、表示、目标、梯度与评价。">范数</Term>、<Term def="特征分解是本页第4个证据坐标，用于连接输入、表示、目标、梯度与评价。">特征分解</Term>、<Term def="奇异值分解是本页第5个证据坐标，用于连接输入、表示、目标、梯度与评价。">奇异值分解</Term>、<Term def="主成分分析是本页第6个证据坐标，用于连接输入、表示、目标、梯度与评价。">主成分分析</Term>
共同构成本页坐标。把深度学习的shape、线性变换与子空间语言统一起来，并以特征分解、SVD和PCA连接几何与数值计算。

深度学习系统同时包含数据处理、参数化表示、随机训练、近似推断和评价。指标上升仍可能来自数据泄漏、广播shape、训练评价模式错位、旧优化器状态或挑选随机种子。实验前必须冻结数据版本、切分身份、预处理、轴语义、参数初值、预算、随机种子和评价协议。

本页遵循“声明输入与shape -> 写前向和目标 -> 手算最小张量 -> 运行单变量实验 -> 独立重算梯度 -> 注入失败并重放”。第一处分叉出现后停止扩大训练，从原始样本和空派生状态复现，不能用最终分数替代过程证据。

## 数学骨架

$$
A\mathbf{x}=\mathbf{b}
$$

$$
\|\mathbf{x}\|_2=\sqrt{\sum_i x_i^2}
$$

$$
A=Q\Lambda Q^T
$$

$$
A=U\Sigma V^T
$$

每个方程进入实现前都声明求和或期望的轴、张量shape、条件分布、参数版本和随机来源。矩阵乘法与广播、训练与评价、数据项与正则项、精确推断与近似推断必须显式区分。

## 可复现证据工作表

第一栏保存数据版本、样本索引、原始值、切分身份、预处理输出、批次shape和随机种子。第二栏记录每层输入输出、参数版本、激活统计、mask与归一化轴，使前向错误能在进入损失前被发现。

第三栏保存每个损失项、局部导数、参数梯度、梯度范数、优化器状态和更新后参数。概率模型还要记录联合分解、后验近似、采样链或配分函数估计。第四栏用冻结模型和未见样本重做评价，禁止读取训练缓存。

失败对照只改变一个条件，例如交换批次轴与特征轴、漏掉mask、复用旧隐藏状态、把训练模式带入评价或让验证集参与梯度。检测器应在预先声明的中间证据报警；若只能从最终指标发现问题，工作表仍不合格。

## 权威目录逐项讲解

### 第1部分 应用数学与机器学习基础

把“第1部分 应用数学与机器学习基础”放回“数据 -> 表示 -> 目标 -> 梯度 -> 独立评价”的主链。先写清输入输出shape、条件变量和参数版本，再说明张量如何影响范数；每个结论都必须能从保存的中间量重算。

最小实验固定两到八个样本、参数初值和随机种子，先预测本节变化后最早分叉的激活、损失、梯度、后验或评价切片。针对“第1部分 应用数学与机器学习基础”，验收条件是“每个张量的轴、基、秩和dtype必须记录；矩阵乘法、逐元素乘法、转置和广播不得依靠形状碰巧通过。”。若首个预期证据没有变化，就停止扩大模型，从原始输入和空优化器状态重放。

### 第2章 线性代数

把“第2章 线性代数”放回“数据 -> 表示 -> 目标 -> 梯度 -> 独立评价”的主链。先写清输入输出shape、条件变量和参数版本，再说明矩阵乘法如何影响特征分解；每个结论都必须能从保存的中间量重算。

最小实验固定两到八个样本、参数初值和随机种子，先预测本节变化后最早分叉的激活、损失、梯度、后验或评价切片。针对“第2章 线性代数”，验收条件是“每个张量的轴、基、秩和dtype必须记录；矩阵乘法、逐元素乘法、转置和广播不得依靠形状碰巧通过。”。若首个预期证据没有变化，就停止扩大模型，从原始输入和空优化器状态重放。

### 2.1 标量、向量、矩阵和张量

把“2.1 标量、向量、矩阵和张量”放回“数据 -> 表示 -> 目标 -> 梯度 -> 独立评价”的主链。先写清输入输出shape、条件变量和参数版本，再说明范数如何影响奇异值分解；每个结论都必须能从保存的中间量重算。

最小实验固定两到八个样本、参数初值和随机种子，先预测本节变化后最早分叉的激活、损失、梯度、后验或评价切片。针对“2.1 标量、向量、矩阵和张量”，验收条件是“每个张量的轴、基、秩和dtype必须记录；矩阵乘法、逐元素乘法、转置和广播不得依靠形状碰巧通过。”。若首个预期证据没有变化，就停止扩大模型，从原始输入和空优化器状态重放。

### 2.2 矩阵和向量相乘

把“2.2 矩阵和向量相乘”放回“数据 -> 表示 -> 目标 -> 梯度 -> 独立评价”的主链。先写清输入输出shape、条件变量和参数版本，再说明特征分解如何影响主成分分析；每个结论都必须能从保存的中间量重算。

最小实验固定两到八个样本、参数初值和随机种子，先预测本节变化后最早分叉的激活、损失、梯度、后验或评价切片。针对“2.2 矩阵和向量相乘”，验收条件是“每个张量的轴、基、秩和dtype必须记录；矩阵乘法、逐元素乘法、转置和广播不得依靠形状碰巧通过。”。若首个预期证据没有变化，就停止扩大模型，从原始输入和空优化器状态重放。

### 2.3 单位矩阵和逆矩阵

把“2.3 单位矩阵和逆矩阵”放回“数据 -> 表示 -> 目标 -> 梯度 -> 独立评价”的主链。先写清输入输出shape、条件变量和参数版本，再说明奇异值分解如何影响张量；每个结论都必须能从保存的中间量重算。

最小实验固定两到八个样本、参数初值和随机种子，先预测本节变化后最早分叉的激活、损失、梯度、后验或评价切片。针对“2.3 单位矩阵和逆矩阵”，验收条件是“每个张量的轴、基、秩和dtype必须记录；矩阵乘法、逐元素乘法、转置和广播不得依靠形状碰巧通过。”。若首个预期证据没有变化，就停止扩大模型，从原始输入和空优化器状态重放。

### 2.4 线性相关和生成子空间

把“2.4 线性相关和生成子空间”放回“数据 -> 表示 -> 目标 -> 梯度 -> 独立评价”的主链。先写清输入输出shape、条件变量和参数版本，再说明主成分分析如何影响矩阵乘法；每个结论都必须能从保存的中间量重算。

最小实验固定两到八个样本、参数初值和随机种子，先预测本节变化后最早分叉的激活、损失、梯度、后验或评价切片。针对“2.4 线性相关和生成子空间”，验收条件是“每个张量的轴、基、秩和dtype必须记录；矩阵乘法、逐元素乘法、转置和广播不得依靠形状碰巧通过。”。若首个预期证据没有变化，就停止扩大模型，从原始输入和空优化器状态重放。

### 2.5 范数

把“2.5 范数”放回“数据 -> 表示 -> 目标 -> 梯度 -> 独立评价”的主链。先写清输入输出shape、条件变量和参数版本，再说明张量如何影响范数；每个结论都必须能从保存的中间量重算。

最小实验固定两到八个样本、参数初值和随机种子，先预测本节变化后最早分叉的激活、损失、梯度、后验或评价切片。针对“2.5 范数”，验收条件是“每个张量的轴、基、秩和dtype必须记录；矩阵乘法、逐元素乘法、转置和广播不得依靠形状碰巧通过。”。若首个预期证据没有变化，就停止扩大模型，从原始输入和空优化器状态重放。

### 2.6 特殊类型的矩阵和向量

把“2.6 特殊类型的矩阵和向量”放回“数据 -> 表示 -> 目标 -> 梯度 -> 独立评价”的主链。先写清输入输出shape、条件变量和参数版本，再说明矩阵乘法如何影响特征分解；每个结论都必须能从保存的中间量重算。

最小实验固定两到八个样本、参数初值和随机种子，先预测本节变化后最早分叉的激活、损失、梯度、后验或评价切片。针对“2.6 特殊类型的矩阵和向量”，验收条件是“每个张量的轴、基、秩和dtype必须记录；矩阵乘法、逐元素乘法、转置和广播不得依靠形状碰巧通过。”。若首个预期证据没有变化，就停止扩大模型，从原始输入和空优化器状态重放。

### 2.7 特征分解

把“2.7 特征分解”放回“数据 -> 表示 -> 目标 -> 梯度 -> 独立评价”的主链。先写清输入输出shape、条件变量和参数版本，再说明范数如何影响奇异值分解；每个结论都必须能从保存的中间量重算。

最小实验固定两到八个样本、参数初值和随机种子，先预测本节变化后最早分叉的激活、损失、梯度、后验或评价切片。针对“2.7 特征分解”，验收条件是“每个张量的轴、基、秩和dtype必须记录；矩阵乘法、逐元素乘法、转置和广播不得依靠形状碰巧通过。”。若首个预期证据没有变化，就停止扩大模型，从原始输入和空优化器状态重放。

### 2.8 奇异值分解

把“2.8 奇异值分解”放回“数据 -> 表示 -> 目标 -> 梯度 -> 独立评价”的主链。先写清输入输出shape、条件变量和参数版本，再说明特征分解如何影响主成分分析；每个结论都必须能从保存的中间量重算。

最小实验固定两到八个样本、参数初值和随机种子，先预测本节变化后最早分叉的激活、损失、梯度、后验或评价切片。针对“2.8 奇异值分解”，验收条件是“每个张量的轴、基、秩和dtype必须记录；矩阵乘法、逐元素乘法、转置和广播不得依靠形状碰巧通过。”。若首个预期证据没有变化，就停止扩大模型，从原始输入和空优化器状态重放。

### 2.9 Moore-Penrose伪逆

把“2.9 Moore-Penrose伪逆”放回“数据 -> 表示 -> 目标 -> 梯度 -> 独立评价”的主链。先写清输入输出shape、条件变量和参数版本，再说明奇异值分解如何影响张量；每个结论都必须能从保存的中间量重算。

最小实验固定两到八个样本、参数初值和随机种子，先预测本节变化后最早分叉的激活、损失、梯度、后验或评价切片。针对“2.9 Moore-Penrose伪逆”，验收条件是“每个张量的轴、基、秩和dtype必须记录；矩阵乘法、逐元素乘法、转置和广播不得依靠形状碰巧通过。”。若首个预期证据没有变化，就停止扩大模型，从原始输入和空优化器状态重放。

### 2.10 迹运算

把“2.10 迹运算”放回“数据 -> 表示 -> 目标 -> 梯度 -> 独立评价”的主链。先写清输入输出shape、条件变量和参数版本，再说明主成分分析如何影响矩阵乘法；每个结论都必须能从保存的中间量重算。

最小实验固定两到八个样本、参数初值和随机种子，先预测本节变化后最早分叉的激活、损失、梯度、后验或评价切片。针对“2.10 迹运算”，验收条件是“每个张量的轴、基、秩和dtype必须记录；矩阵乘法、逐元素乘法、转置和广播不得依靠形状碰巧通过。”。若首个预期证据没有变化，就停止扩大模型，从原始输入和空优化器状态重放。

### 2.11 行列式

把“2.11 行列式”放回“数据 -> 表示 -> 目标 -> 梯度 -> 独立评价”的主链。先写清输入输出shape、条件变量和参数版本，再说明张量如何影响范数；每个结论都必须能从保存的中间量重算。

最小实验固定两到八个样本、参数初值和随机种子，先预测本节变化后最早分叉的激活、损失、梯度、后验或评价切片。针对“2.11 行列式”，验收条件是“每个张量的轴、基、秩和dtype必须记录；矩阵乘法、逐元素乘法、转置和广播不得依靠形状碰巧通过。”。若首个预期证据没有变化，就停止扩大模型，从原始输入和空优化器状态重放。

### 2.12 示例：主成分分析

把“2.12 示例：主成分分析”放回“数据 -> 表示 -> 目标 -> 梯度 -> 独立评价”的主链。先写清输入输出shape、条件变量和参数版本，再说明矩阵乘法如何影响特征分解；每个结论都必须能从保存的中间量重算。

最小实验固定两到八个样本、参数初值和随机种子，先预测本节变化后最早分叉的激活、损失、梯度、后验或评价切片。针对“2.12 示例：主成分分析”，验收条件是“每个张量的轴、基、秩和dtype必须记录；矩阵乘法、逐元素乘法、转置和广播不得依靠形状碰巧通过。”。若首个预期证据没有变化，就停止扩大模型，从原始输入和空优化器状态重放。

## 七类实现证据

### 数据合同

数据合同要求保留原始输入、轴语义、dtype、随机种子、参数版本和关键中间量。正常路径与故障路径只改变一个条件，并在运行前写下首个应分叉的观测值。对第2章 线性代数而言，必须同时核对数值、方向、归一化条件和边界样本，避免聚合指标掩盖局部错误。

回归时从相同初值运行基线和单一故障。检测器要在激活、损失、梯度、后验、采样或数据切片处报警；若直到最终准确率或样本观感才发现差异，说明证据粒度仍然不足。

### shape合同

shape合同要求保留原始输入、轴语义、dtype、随机种子、参数版本和关键中间量。正常路径与故障路径只改变一个条件，并在运行前写下首个应分叉的观测值。对第2章 线性代数而言，必须同时核对数值、方向、归一化条件和边界样本，避免聚合指标掩盖局部错误。

回归时从相同初值运行基线和单一故障。检测器要在激活、损失、梯度、后验、采样或数据切片处报警；若直到最终准确率或样本观感才发现差异，说明证据粒度仍然不足。

### 前向合同

前向合同要求保留原始输入、轴语义、dtype、随机种子、参数版本和关键中间量。正常路径与故障路径只改变一个条件，并在运行前写下首个应分叉的观测值。对第2章 线性代数而言，必须同时核对数值、方向、归一化条件和边界样本，避免聚合指标掩盖局部错误。

回归时从相同初值运行基线和单一故障。检测器要在激活、损失、梯度、后验、采样或数据切片处报警；若直到最终准确率或样本观感才发现差异，说明证据粒度仍然不足。

### 目标合同

目标合同要求保留原始输入、轴语义、dtype、随机种子、参数版本和关键中间量。正常路径与故障路径只改变一个条件，并在运行前写下首个应分叉的观测值。对第2章 线性代数而言，必须同时核对数值、方向、归一化条件和边界样本，避免聚合指标掩盖局部错误。

回归时从相同初值运行基线和单一故障。检测器要在激活、损失、梯度、后验、采样或数据切片处报警；若直到最终准确率或样本观感才发现差异，说明证据粒度仍然不足。

### 梯度合同

梯度合同要求保留原始输入、轴语义、dtype、随机种子、参数版本和关键中间量。正常路径与故障路径只改变一个条件，并在运行前写下首个应分叉的观测值。对第2章 线性代数而言，必须同时核对数值、方向、归一化条件和边界样本，避免聚合指标掩盖局部错误。

回归时从相同初值运行基线和单一故障。检测器要在激活、损失、梯度、后验、采样或数据切片处报警；若直到最终准确率或样本观感才发现差异，说明证据粒度仍然不足。

### 评价合同

评价合同要求保留原始输入、轴语义、dtype、随机种子、参数版本和关键中间量。正常路径与故障路径只改变一个条件，并在运行前写下首个应分叉的观测值。对第2章 线性代数而言，必须同时核对数值、方向、归一化条件和边界样本，避免聚合指标掩盖局部错误。

回归时从相同初值运行基线和单一故障。检测器要在激活、损失、梯度、后验、采样或数据切片处报警；若直到最终准确率或样本观感才发现差异，说明证据粒度仍然不足。

### 回退合同

回退合同要求保留原始输入、轴语义、dtype、随机种子、参数版本和关键中间量。正常路径与故障路径只改变一个条件，并在运行前写下首个应分叉的观测值。对第2章 线性代数而言，必须同时核对数值、方向、归一化条件和边界样本，避免聚合指标掩盖局部错误。

回归时从相同初值运行基线和单一故障。检测器要在激活、损失、梯度、后验、采样或数据切片处报警；若直到最终准确率或样本观感才发现差异，说明证据粒度仍然不足。

## 三步交互实验

先预测单变量变化会最早影响shape、激活、损失、梯度、后验还是评价切片，再切换基线、边界和故障模式。

<Stepper>
  <Step title="画出模型与shape图">
    标出输入、表示、参数、目标、梯度和评价方向，逐边注明shape与参数版本。
    <Dlt02LinearAlgebraMapLab />
  </Step>
  <Step title="运行单变量实验">
    固定其余条件，只改变深度、容量、扰动强度或优化步长中的一个变量。
    <Dlt02LinearAlgebraExperimentLab />
  </Step>
  <Step title="核对独立证据">
    比较正常与故障路径、自动微分与独立重算，定位第一条不一致并给出回退点。
    <Dlt02LinearAlgebraEvidenceLab />
  </Step>
</Stepper>

## 常见失败与边界

<Callout type="trap">
  **把高指标当成正确性证明：**
  数据泄漏、训练模式或单个随机种子都能制造高分；必须核对前向、目标、梯度和独立切片。
</Callout>

<Callout type="trap">
  **shape和状态版本错位：**
  广播可能让错误继续运行，旧隐藏状态或优化器统计也会让损失下降但实验合同失真。
</Callout>

<Callout type="tip">
  **最小重放顺序：**
  先用一层网络或两三个变量手算整条链，再扩大维度与深度；每次只放大一个变量。
</Callout>

## 本章回顾

把深度学习的shape、线性变换与子空间语言统一起来，并以特征分解、SVD和PCA连接几何与数值计算。验收底线是：每个张量的轴、基、秩和dtype必须记录；矩阵乘法、逐元素乘法、转置和广播不得依靠形状碰巧通过。。掌握标准是能从固定数据预测首个变化、手算最小张量、复现实验、解释失败样本，并证明独立评价不读取训练状态。

<Glossary>

<GlossaryItem term="张量">
  “张量”对应证据链第1站。记录定义、shape、条件变量、参数版本、容差和失败边界；只报告最终分数不足以证明这一环节正确。
</GlossaryItem>

<GlossaryItem term="矩阵乘法">
  “矩阵乘法”对应证据链第2站。记录定义、shape、条件变量、参数版本、容差和失败边界；只报告最终分数不足以证明这一环节正确。
</GlossaryItem>

<GlossaryItem term="范数">
  “范数”对应证据链第3站。记录定义、shape、条件变量、参数版本、容差和失败边界；只报告最终分数不足以证明这一环节正确。
</GlossaryItem>

<GlossaryItem term="特征分解">
  “特征分解”对应证据链第4站。记录定义、shape、条件变量、参数版本、容差和失败边界；只报告最终分数不足以证明这一环节正确。
</GlossaryItem>

<GlossaryItem term="奇异值分解">
  “奇异值分解”对应证据链第5站。记录定义、shape、条件变量、参数版本、容差和失败边界；只报告最终分数不足以证明这一环节正确。
</GlossaryItem>

<GlossaryItem term="主成分分析">
  “主成分分析”对应证据链第6站。记录定义、shape、条件变量、参数版本、容差和失败边界；只报告最终分数不足以证明这一环节正确。
</GlossaryItem>

</Glossary>

<Exercises>

1. 如何为第2章 线性代数建立可手算、可重放的最小模型链？

<Answer>
  选择一层或一个小图，固定输入、参数初值和随机种子，逐步写出shape、激活、损失、局部导数、参数梯度和更新后值，再从原始输入独立重放。
</Answer>

2. 如何证明“每个张量的轴、基、秩和dtype必须记录；矩阵乘法、逐元素乘法、转置和广播不得依靠形状碰巧通过。”不是由单次高指标碰巧满足？

<Answer>
  同时运行正常路径和只破坏一个shape、mask、模式、状态版本或数据切分的失败路径。检测器必须在预测的首个中间量区分两者，并跨固定评价切片报告结果。
</Answer>

3. 修复第2章 线性代数后应按什么顺序回归？

<Answer>
  清空批次缓存、隐藏状态、梯度和优化器，从固定配置重放：先查数据与shape，再查前向和目标，然后查梯度与参数版本，最后才跑长期训练和独立评价。
</Answer>

</Exercises>

<Attribution
  adaptedFrom="Ian Goodfellow、Yoshua Bengio、Aaron Courville著，赵申剑、黎彧君、符天凡、李凯译《深度学习》，人民邮电出版社，2017年7月，500页，ISBN 9787115461476；原版Deep Learning，MIT Press，2016年11月18日，800页，ISBN 9780262035613 · 基于官方3部分、20章与164个编号一级节完整核心目录的独立教学重构"
  adaptedUrl="https://www.deeplearningbook.org/contents/TOC.html"
/>
