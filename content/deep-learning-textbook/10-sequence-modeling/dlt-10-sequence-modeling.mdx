---
title: 第10章 序列建模：循环和递归网络
type: B
section: 第10章 序列建模：循环和递归网络
order: 10
description: 把展开图、循环状态、双向结构、编码解码、门控与长期依赖统一到时间合同；用依赖地图、计算轨迹和独立证据门交付时间状态表、展开计算图与泄漏反例报告
demo: true
math: true
sourceUrl: "https://www.deeplearningbook.org/"
draft: false
qualityVersion: 2
practiceMode: simulation
sourceMode: independent-rewrite
officialUnitId: dlt-10
---

import {
  Objectives,
  Term,
  Callout,
  Stepper,
  Step,
  Exercises,
  Answer,
  Glossary,
  GlossaryItem,
  Attribution,
} from "@/components/mdx/mdx-components";
import {
  Dlt10SequenceModelingDependencyMapLab,
  Dlt10SequenceModelingComputationTraceLab,
  Dlt10SequenceModelingEvidenceGateLab,
} from "@/components/mdx/deep-learning-textbook/v2/dlt-10-sequence-modeling";

<Objectives>

- 能说明“第10章 序列建模：循环和递归网络”如何把展开图、循环状态、双向结构、编码解码、门控与长期依赖统一到时间合同，并区分原版、中文目录、开放访问许可和当前独立重写
- 能先预测“怎样沿时间展开状态与梯度，区分教师驱动、推断循环和长期依赖故障？”会改变哪个输入、shape、状态、目标、梯度、估计或评估结果，再操作三类证据视图
- 能只注入“训练时读取未来标签，推断时却宣称模型只依赖历史输入”，定位首个偏离“时间轴、初始状态、掩码、教师信号、梯度截断和解码协议必须一致”的状态，并从同一快照完成恢复

</Objectives>

## 为什么从这个问题开始

“第10章 序列建模：循环和递归网络”围绕“怎样沿时间展开状态与梯度，区分教师驱动、推断循环和长期依赖故障？”建立贯穿任务：对同一序列任务重放RNN、门控单元和编码解码路径。先写下哪个输入、shape、状态、目标、梯度、估计或评估会先变化，再运行参考、故障和恢复路径；运行后补理由不算预测。只有“第10章 序列建模：循环和递归网络”守住“时间轴、初始状态、掩码、教师信号、梯度截断和解码协议必须一致”并交付时间状态表、展开计算图与泄漏反例报告，最终分数、似然、重构、样本或部署结果才构成机制证据。

## 书目、353个正式坐标与许可边界

“第10章 序列建模：循环和递归网络”以[MIT Press书目页](https://mitpress.mit.edu/9780262035613/deep-learning/)核对Ian Goodfellow、Yoshua Bengio、Aaron Courville著 _Deep Learning_：精装ISBN 9780262035613，2016年11月18日出版，800页；MIT Press同时把[作者托管版](https://www.deeplearningbook.org/)列为开放访问资源，并标注[CC BY-NC-ND 4.0](https://creativecommons.org/licenses/by-nc-nd/4.0/)。开放阅读不等于允许演绎，“第10章 序列建模：循环和递归网络”不复制、翻译或改写受保护正文，只把原版公开目录与可定位章节用作范围和事实核对。

“第10章 序列建模：循环和递归网络”以[作者官方目录](https://www.deeplearningbook.org/contents/TOC.html)核对3个部分、20章和164个一级节，再以[人民邮电出版社官方中文书页](https://detail.youzan.com/show/goods?alias=275704xsv5xoh&from_source=gbox_seo)核对166个二级节、中文书名、ISBN 9787115461476和2017年8月出版信息；合计353个正式目录坐标。出版社页面中“有效有效”、缺空格等排版噪声已按英文原版标题规范化，但没有新增原版不存在的章节。

“第10章 序列建模：循环和递归网络”的中文讲解、状态图、数值实验、练习与答案均为独立教学重写；2016年以后出现的框架行为或研究进展必须带当前时间标签，不能倒填为原书原话。[章专属核对 1](https://docs.pytorch.org/docs/stable/nn.html)、[章专属核对 2](https://www.deeplearningbook.org/)只用于核对本页算法、API或实验边界，不能反向证明原书采用本站表述。

## 原版目录层级与可验证机制

### 第10章 序列建模：循环和递归网络

<Term def="序列建模对应正式目录坐标“第10章 序列建模：循环和递归网络”，在“第10章 序列建模：循环和递归网络”中用于沿时间展开状态、输入依赖、门控和梯度传播，并受数据、shape、目标、随机性、评估与版本边界约束。">
  序列建模
</Term>

**正式坐标 1/24。** 目录原文键 `第10章 序列建模：循环和递归网络`。在“第10章 序列建模：循环和递归网络”的第1个坐标中，「第10章 序列建模：循环和递归网络」通过沿时间展开状态、输入依赖、门控和梯度传播推进时间状态、门控与长期依赖；复核者保存时间索引、状态、掩码、教师信号、梯度与解码轨迹，一旦出现训练读取未来信息或推断协议与训练不一致就撤回结论。

### 10·1 展开计算图

<Term def="展开计算图对应正式目录坐标“10·1 展开计算图”，在“第10章 序列建模：循环和递归网络”中用于把前向状态、局部导数和反向梯度连接为计算图，并受数据、shape、目标、随机性、评估与版本边界约束。">
  展开计算图
</Term>

**正式坐标 2/24。** 目录原文键 `10.1 展开计算图`。围绕“怎样沿时间展开状态与梯度，区分教师驱动、推断循环和长期依赖故障？”，“第10章 序列建模：循环和递归网络”在坐标2把「10·1 展开计算图」落实为把前向状态、局部导数和反向梯度连接为计算图；只有张量shape、节点值、局部导数、梯度与有限差分可复算且反例排除广播或缩放错误让梯度数值存在但语义不正确，本节点才算掌握。

### 10·2 循环神经网络

<Term def="循环神经网络对应正式目录坐标“10·2 循环神经网络”，在“第10章 序列建模：循环和递归网络”中用于沿时间展开状态、输入依赖、门控和梯度传播，并受数据、shape、目标、随机性、评估与版本边界约束。">
  循环神经网络
</Term>

**正式坐标 3/24。** 目录原文键 `10.2 循环神经网络`。“第10章 序列建模：循环和递归网络”的原版节点3「10·2 循环神经网络」不能停在名词解释：它要沿时间展开状态、输入依赖、门控和梯度传播，交付时间索引、状态、掩码、教师信号、梯度与解码轨迹，并用训练读取未来信息或推断协议与训练不一致作为单一反事实检查。

### 10·2·1 导师驱动过程和输出循环网络

<Term def="导师驱动过程和输出循环网络对应正式目录坐标“10·2·1 导师驱动过程和输出循环网络”，在“第10章 序列建模：循环和递归网络”中用于沿时间展开状态、输入依赖、门控和梯度传播，并受数据、shape、目标、随机性、评估与版本边界约束。">
  导师驱动过程和输出循环网络
</Term>

**正式坐标 4/24。** 目录原文键 `10.2.1 导师驱动过程和输出循环网络`。对“第10章 序列建模：循环和递归网络”而言，「10·2·1 导师驱动过程和输出循环网络」在第4次检查中改变可观察状态，因为它负责沿时间展开状态、输入依赖、门控和梯度传播；时间索引、状态、掩码、教师信号、梯度与解码轨迹必须与“时间轴、初始状态、掩码、教师信号、梯度截断和解码协议必须一致”对齐，不能接受训练读取未来信息或推断协议与训练不一致。

### 10·2·2 计算循环神经网络的梯度

<Term def="计算循环神经网络的梯度对应正式目录坐标“10·2·2 计算循环神经网络的梯度”，在“第10章 序列建模：循环和递归网络”中用于跟踪浮点、梯度、曲率、状态缓冲和参数更新，并受数据、shape、目标、随机性、评估与版本边界约束。">
  计算循环神经网络的梯度
</Term>

**正式坐标 5/24。** 目录原文键 `10.2.2 计算循环神经网络的梯度`。目录原文「10·2·2 计算循环神经网络的梯度」进入“第10章 序列建模：循环和递归网络”后形成第5个实验合同：先跟踪浮点、梯度、曲率、状态缓冲和参数更新，再保存dtype、条件数、梯度核对、步长、更新差与停止理由；若发生只看目标下降而忽略数值不稳、状态错位或泛化失败，从同一输入快照恢复。

### 10·2·3 作为有向图模型的循环网络

<Term def="作为有向图模型的循环网络对应正式目录坐标“10·2·3 作为有向图模型的循环网络”，在“第10章 序列建模：循环和递归网络”中用于沿时间展开状态、输入依赖、门控和梯度传播，并受数据、shape、目标、随机性、评估与版本边界约束。">
  作为有向图模型的循环网络
</Term>

**正式坐标 6/24。** 目录原文键 `10.2.3 作为有向图模型的循环网络`。在“第10章 序列建模：循环和递归网络”的第6个坐标中，「10·2·3 作为有向图模型的循环网络」通过沿时间展开状态、输入依赖、门控和梯度传播推进时间状态、门控与长期依赖；复核者保存时间索引、状态、掩码、教师信号、梯度与解码轨迹，一旦出现训练读取未来信息或推断协议与训练不一致就撤回结论。

### 10·2·4 基于上下文的RNN序列建模

<Term def="基于上下文的RNN序列建模对应正式目录坐标“10·2·4 基于上下文的RNN序列建模”，在“第10章 序列建模：循环和递归网络”中用于沿时间展开状态、输入依赖、门控和梯度传播，并受数据、shape、目标、随机性、评估与版本边界约束。">
  基于上下文的RNN序列建模
</Term>

**正式坐标 7/24。** 目录原文键 `10.2.4 基于上下文的RNN序列建模`。围绕“怎样沿时间展开状态与梯度，区分教师驱动、推断循环和长期依赖故障？”，“第10章 序列建模：循环和递归网络”在坐标7把「10·2·4 基于上下文的RNN序列建模」落实为沿时间展开状态、输入依赖、门控和梯度传播；只有时间索引、状态、掩码、教师信号、梯度与解码轨迹可复算且反例排除训练读取未来信息或推断协议与训练不一致，本节点才算掌握。

### 10·3 双向RNN

<Term def="双向RNN对应正式目录坐标“10·3 双向RNN”，在“第10章 序列建模：循环和递归网络”中用于沿时间展开状态、输入依赖、门控和梯度传播，并受数据、shape、目标、随机性、评估与版本边界约束。">
  双向RNN
</Term>

**正式坐标 8/24。** 目录原文键 `10.3 双向RNN`。“第10章 序列建模：循环和递归网络”的原版节点8「10·3 双向RNN」不能停在名词解释：它要沿时间展开状态、输入依赖、门控和梯度传播，交付时间索引、状态、掩码、教师信号、梯度与解码轨迹，并用训练读取未来信息或推断协议与训练不一致作为单一反事实检查。

### 10·4 编码器-解码器序列到序列架构

<Term def="编码器-解码器序列到序列架构对应正式目录坐标“10·4 编码器-解码器序列到序列架构”，在“第10章 序列建模：循环和递归网络”中用于沿时间展开状态、输入依赖、门控和梯度传播，并受数据、shape、目标、随机性、评估与版本边界约束。">
  编码器-解码器序列到序列架构
</Term>

**正式坐标 9/24。** 目录原文键 `10.4 编码器-解码器序列到序列架构`。对“第10章 序列建模：循环和递归网络”而言，「10·4 编码器-解码器序列到序列架构」在第9次检查中改变可观察状态，因为它负责沿时间展开状态、输入依赖、门控和梯度传播；时间索引、状态、掩码、教师信号、梯度与解码轨迹必须与“时间轴、初始状态、掩码、教师信号、梯度截断和解码协议必须一致”对齐，不能接受训练读取未来信息或推断协议与训练不一致。

### 10·5 深度循环网络

<Term def="深度循环网络对应正式目录坐标“10·5 深度循环网络”，在“第10章 序列建模：循环和递归网络”中用于沿时间展开状态、输入依赖、门控和梯度传播，并受数据、shape、目标、随机性、评估与版本边界约束。">
  深度循环网络
</Term>

**正式坐标 10/24。** 目录原文键 `10.5 深度循环网络`。目录原文「10·5 深度循环网络」进入“第10章 序列建模：循环和递归网络”后形成第10个实验合同：先沿时间展开状态、输入依赖、门控和梯度传播，再保存时间索引、状态、掩码、教师信号、梯度与解码轨迹；若发生训练读取未来信息或推断协议与训练不一致，从同一输入快照恢复。

### 10·6 递归神经网络

<Term def="递归神经网络对应正式目录坐标“10·6 递归神经网络”，在“第10章 序列建模：循环和递归网络”中用于把“递归神经网络”放进时间状态、门控与长期依赖的章专属计算链，并受数据、shape、目标、随机性、评估与版本边界约束。">
  递归神经网络
</Term>

**正式坐标 11/24。** 目录原文键 `10.6 递归神经网络`。在“第10章 序列建模：循环和递归网络”的第11个坐标中，「10·6 递归神经网络」通过把“递归神经网络”放进时间状态、门控与长期依赖的章专属计算链推进时间状态、门控与长期依赖；复核者保存第10章 序列建模：循环和递归网络的输入、状态变化、输出、反例与边界，一旦出现只复述“递归神经网络”名称而没有可观察状态和独立证据就撤回结论。

### 10·7 长期依赖的挑战

<Term def="长期依赖的挑战对应正式目录坐标“10·7 长期依赖的挑战”，在“第10章 序列建模：循环和递归网络”中用于沿时间展开状态、输入依赖、门控和梯度传播，并受数据、shape、目标、随机性、评估与版本边界约束。">
  长期依赖的挑战
</Term>

**正式坐标 12/24。** 目录原文键 `10.7 长期依赖的挑战`。围绕“怎样沿时间展开状态与梯度，区分教师驱动、推断循环和长期依赖故障？”，“第10章 序列建模：循环和递归网络”在坐标12把「10·7 长期依赖的挑战」落实为沿时间展开状态、输入依赖、门控和梯度传播；只有时间索引、状态、掩码、教师信号、梯度与解码轨迹可复算且反例排除训练读取未来信息或推断协议与训练不一致，本节点才算掌握。

### 10·8 Echo State网络

<Term def="Echo State网络对应正式目录坐标“10·8 Echo State网络”，在“第10章 序列建模：循环和递归网络”中用于把“Echo State网络”放进时间状态、门控与长期依赖的章专属计算链，并受数据、shape、目标、随机性、评估与版本边界约束。">
  Echo State网络
</Term>

**正式坐标 13/24。** 目录原文键 `10.8 Echo State网络`。“第10章 序列建模：循环和递归网络”的原版节点13「10·8 Echo State网络」不能停在名词解释：它要把“Echo State网络”放进时间状态、门控与长期依赖的章专属计算链，交付第10章 序列建模：循环和递归网络的输入、状态变化、输出、反例与边界，并用只复述“Echo State网络”名称而没有可观察状态和独立证据作为单一反事实检查。

### 10·9 渗漏单元和其他多时间尺度策略

<Term def="渗漏单元和其他多时间尺度策略对应正式目录坐标“10·9 渗漏单元和其他多时间尺度策略”，在“第10章 序列建模：循环和递归网络”中用于沿时间展开状态、输入依赖、门控和梯度传播，并受数据、shape、目标、随机性、评估与版本边界约束。">
  渗漏单元和其他多时间尺度策略
</Term>

**正式坐标 14/24。** 目录原文键 `10.9 渗漏单元和其他多时间尺度策略`。对“第10章 序列建模：循环和递归网络”而言，「10·9 渗漏单元和其他多时间尺度策略」在第14次检查中改变可观察状态，因为它负责沿时间展开状态、输入依赖、门控和梯度传播；时间索引、状态、掩码、教师信号、梯度与解码轨迹必须与“时间轴、初始状态、掩码、教师信号、梯度截断和解码协议必须一致”对齐，不能接受训练读取未来信息或推断协议与训练不一致。

### 10·9·1 时间维度的跳跃连接

<Term def="时间维度的跳跃连接对应正式目录坐标“10·9·1 时间维度的跳跃连接”，在“第10章 序列建模：循环和递归网络”中用于沿时间展开状态、输入依赖、门控和梯度传播，并受数据、shape、目标、随机性、评估与版本边界约束。">
  时间维度的跳跃连接
</Term>

**正式坐标 15/24。** 目录原文键 `10.9.1 时间维度的跳跃连接`。目录原文「10·9·1 时间维度的跳跃连接」进入“第10章 序列建模：循环和递归网络”后形成第15个实验合同：先沿时间展开状态、输入依赖、门控和梯度传播，再保存时间索引、状态、掩码、教师信号、梯度与解码轨迹；若发生训练读取未来信息或推断协议与训练不一致，从同一输入快照恢复。

### 10·9·2 渗漏单元和一系列不同时间尺度

<Term def="渗漏单元和一系列不同时间尺度对应正式目录坐标“10·9·2 渗漏单元和一系列不同时间尺度”，在“第10章 序列建模：循环和递归网络”中用于沿时间展开状态、输入依赖、门控和梯度传播，并受数据、shape、目标、随机性、评估与版本边界约束。">
  渗漏单元和一系列不同时间尺度
</Term>

**正式坐标 16/24。** 目录原文键 `10.9.2 渗漏单元和一系列不同时间尺度`。在“第10章 序列建模：循环和递归网络”的第16个坐标中，「10·9·2 渗漏单元和一系列不同时间尺度」通过沿时间展开状态、输入依赖、门控和梯度传播推进时间状态、门控与长期依赖；复核者保存时间索引、状态、掩码、教师信号、梯度与解码轨迹，一旦出现训练读取未来信息或推断协议与训练不一致就撤回结论。

### 10·9·3 删除连接

<Term def="删除连接对应正式目录坐标“10·9·3 删除连接”，在“第10章 序列建模：循环和递归网络”中用于把“删除连接”放进时间状态、门控与长期依赖的章专属计算链，并受数据、shape、目标、随机性、评估与版本边界约束。">
  删除连接
</Term>

**正式坐标 17/24。** 目录原文键 `10.9.3 删除连接`。围绕“怎样沿时间展开状态与梯度，区分教师驱动、推断循环和长期依赖故障？”，“第10章 序列建模：循环和递归网络”在坐标17把「10·9·3 删除连接」落实为把“删除连接”放进时间状态、门控与长期依赖的章专属计算链；只有第10章 序列建模：循环和递归网络的输入、状态变化、输出、反例与边界可复算且反例排除只复述“删除连接”名称而没有可观察状态和独立证据，本节点才算掌握。

### 10·10 长短期记忆和其他门控RNN

<Term def="长短期记忆和其他门控RNN对应正式目录坐标“10·10 长短期记忆和其他门控RNN”，在“第10章 序列建模：循环和递归网络”中用于沿时间展开状态、输入依赖、门控和梯度传播，并受数据、shape、目标、随机性、评估与版本边界约束。">
  长短期记忆和其他门控RNN
</Term>

**正式坐标 18/24。** 目录原文键 `10.10 长短期记忆和其他门控RNN`。“第10章 序列建模：循环和递归网络”的原版节点18「10·10 长短期记忆和其他门控RNN」不能停在名词解释：它要沿时间展开状态、输入依赖、门控和梯度传播，交付时间索引、状态、掩码、教师信号、梯度与解码轨迹，并用训练读取未来信息或推断协议与训练不一致作为单一反事实检查。

### 10·10·1 LSTM

<Term def="LSTM对应正式目录坐标“10·10·1 LSTM”，在“第10章 序列建模：循环和递归网络”中用于沿时间展开状态、输入依赖、门控和梯度传播，并受数据、shape、目标、随机性、评估与版本边界约束。">
  LSTM
</Term>

**正式坐标 19/24。** 目录原文键 `10.10.1 LSTM`。对“第10章 序列建模：循环和递归网络”而言，「10·10·1 LSTM」在第19次检查中改变可观察状态，因为它负责沿时间展开状态、输入依赖、门控和梯度传播；时间索引、状态、掩码、教师信号、梯度与解码轨迹必须与“时间轴、初始状态、掩码、教师信号、梯度截断和解码协议必须一致”对齐，不能接受训练读取未来信息或推断协议与训练不一致。

### 10·10·2 其他门控RNN

<Term def="其他门控RNN对应正式目录坐标“10·10·2 其他门控RNN”，在“第10章 序列建模：循环和递归网络”中用于沿时间展开状态、输入依赖、门控和梯度传播，并受数据、shape、目标、随机性、评估与版本边界约束。">
  其他门控RNN
</Term>

**正式坐标 20/24。** 目录原文键 `10.10.2 其他门控RNN`。目录原文「10·10·2 其他门控RNN」进入“第10章 序列建模：循环和递归网络”后形成第20个实验合同：先沿时间展开状态、输入依赖、门控和梯度传播，再保存时间索引、状态、掩码、教师信号、梯度与解码轨迹；若发生训练读取未来信息或推断协议与训练不一致，从同一输入快照恢复。

### 10·11 优化长期依赖

<Term def="优化长期依赖对应正式目录坐标“10·11 优化长期依赖”，在“第10章 序列建模：循环和递归网络”中用于跟踪浮点、梯度、曲率、状态缓冲和参数更新，并受数据、shape、目标、随机性、评估与版本边界约束。">
  优化长期依赖
</Term>

**正式坐标 21/24。** 目录原文键 `10.11 优化长期依赖`。在“第10章 序列建模：循环和递归网络”的第21个坐标中，「10·11 优化长期依赖」通过跟踪浮点、梯度、曲率、状态缓冲和参数更新推进时间状态、门控与长期依赖；复核者保存dtype、条件数、梯度核对、步长、更新差与停止理由，一旦出现只看目标下降而忽略数值不稳、状态错位或泛化失败就撤回结论。

### 10·11·1 截断梯度

<Term def="截断梯度对应正式目录坐标“10·11·1 截断梯度”，在“第10章 序列建模：循环和递归网络”中用于跟踪浮点、梯度、曲率、状态缓冲和参数更新，并受数据、shape、目标、随机性、评估与版本边界约束。">
  截断梯度
</Term>

**正式坐标 22/24。** 目录原文键 `10.11.1 截断梯度`。围绕“怎样沿时间展开状态与梯度，区分教师驱动、推断循环和长期依赖故障？”，“第10章 序列建模：循环和递归网络”在坐标22把「10·11·1 截断梯度」落实为跟踪浮点、梯度、曲率、状态缓冲和参数更新；只有dtype、条件数、梯度核对、步长、更新差与停止理由可复算且反例排除只看目标下降而忽略数值不稳、状态错位或泛化失败，本节点才算掌握。

### 10·11·2 引导信息流的正则化

<Term def="引导信息流的正则化对应正式目录坐标“10·11·2 引导信息流的正则化”，在“第10章 序列建模：循环和递归网络”中用于声明随机变量、支持集、分布和信息量的概率合同，并受数据、shape、目标、随机性、评估与版本边界约束。">
  引导信息流的正则化
</Term>

**正式坐标 23/24。** 目录原文键 `10.11.2 引导信息流的正则化`。“第10章 序列建模：循环和递归网络”的原版节点23「10·11·2 引导信息流的正则化」不能停在名词解释：它要声明随机变量、支持集、分布和信息量的概率合同，交付归一化、条件方向、样本统计、对数口径与误差，并用把密度、质量、样本频率或信息量混为同一量作为单一反事实检查。

### 10·12 显式记忆

<Term def="显式记忆对应正式目录坐标“10·12 显式记忆”，在“第10章 序列建模：循环和递归网络”中用于沿时间展开状态、输入依赖、门控和梯度传播，并受数据、shape、目标、随机性、评估与版本边界约束。">
  显式记忆
</Term>

**正式坐标 24/24。** 目录原文键 `10.12 显式记忆`。对“第10章 序列建模：循环和递归网络”而言，「10·12 显式记忆」在第24次检查中改变可观察状态，因为它负责沿时间展开状态、输入依赖、门控和梯度传播；时间索引、状态、掩码、教师信号、梯度与解码轨迹必须与“时间轴、初始状态、掩码、教师信号、梯度截断和解码协议必须一致”对齐，不能接受训练读取未来信息或推断协议与训练不一致。

## 先预测，再操作三个章专属实验

<Callout type="info" title="先写出哪个状态会先变化">
  对“第10章
  序列建模：循环和递归网络”先冻结对同一序列任务重放RNN、门控单元和编码解码路径的数据、shape、目标、预算、随机性和评估口径，再操作依赖地图、计算轨迹与证据门；结果与预测不同就修改假设，不删除失败运行。
</Callout>

<Stepper>
  <Step title="1. 原版坐标、依赖与状态">
    为“第10章 序列建模：循环和递归网络”选择一个正式目录坐标，在参考合同与单一反例间切换，逐阶段核对输入、操作、输出和检查条件。

    <Dlt10SequenceModelingDependencyMapLab />

  </Step>
  <Step title="2. 参考与故障计算轨迹">
    对“第10章 序列建模：循环和递归网络”保持对同一序列任务重放RNN、门控单元和编码解码路径不变，只注入“训练时读取未来标签，推断时却宣称模型只依赖历史输入”，逐步定位首个偏离“时间轴、初始状态、掩码、教师信号、梯度截断和解码协议必须一致”的位置。

    <Dlt10SequenceModelingComputationTraceLab />

  </Step>
  <Step title="3. 独立证据与交付门">
    在“第10章 序列建模：循环和递归网络”的基线、单故障和恢复案例间切换，展开来源、计算、目标和独立评估门后再决定是否交付。

    <Dlt10SequenceModelingEvidenceGateLab />

  </Step>
</Stepper>

<Callout
  type="trap"
  title="本页单一故障：训练时读取未来标签，推断时却宣称模型只依赖历史输入"
>
  “第10章
  序列建模：循环和递归网络”遇到该故障时必须保持其余输入、版本、预算和随机序列不变；最终分数偶尔更高不能替代首个状态分岔和恢复证据。
</Callout>

<Callout type="trap" title="代理目标不等于最终结论">
  “第10章
  序列建模：循环和递归网络”中的训练损失、下界、重构误差、链内统计或精选样本只回答各自合同；它们不能自动证明泛化、精确似然、因果关系或部署可靠性。
</Callout>

<Callout type="trap" title="开放访问不等于允许演绎">
  “第10章 序列建模：循环和递归网络”可以定位作者托管原版和MIT Press元数据，但CC
  BY-NC-ND不授权翻译或改写原书正文；本站内容必须保持独立表达、技术来源和时间边界。
</Callout>

## 第10章 序列建模：循环和递归网络的可重放协议

| 阶段                                         | 允许动作                                                                                         | 必留证据                                                | 拒绝条件                                                                                                  |
| -------------------------------------------- | ------------------------------------------------------------------------------------------------ | ------------------------------------------------------- | --------------------------------------------------------------------------------------------------------- |
| 第10章 序列建模：循环和递归网络 · 输入与角色 | 冻结时间状态、门控与长期依赖所需的数据角色、版本和shape                                          | 第10章 序列建模：循环和递归网络的输入合同与基线快照     | 未满足“第10章 序列建模：循环和递归网络的来源、角色、单位、shape和可见性没有越界”                          |
| 第10章 序列建模：循环和递归网络 · 计算与状态 | 执行把展开图、循环状态、双向结构、编码解码、门控与长期依赖统一到时间合同的最小计算并保存中间状态 | 第10章 序列建模：循环和递归网络的参考轨迹与单故障轨迹   | 未满足“第10章 序列建模：循环和递归网络每一步状态变化都能由输入、公式或算法操作复算”                       |
| 第10章 序列建模：循环和递归网络 · 目标与更新 | 比较目标分量、梯度、估计或选择决定                                                               | 第10章 序列建模：循环和递归网络的更新前后差异与首个分岔 | 未满足“第10章 序列建模：循环和递归网络没有把代理目标、训练分数或偶然样本当作最终结论”                     |
| 第10章 序列建模：循环和递归网络 · 独立评估   | 重放预测、反例、恢复和边界检查                                                                   | 第10章 序列建模：循环和递归网络的接受、回退或拒绝理由   | 未满足“第10章 序列建模：循环和递归网络满足“时间轴、初始状态、掩码、教师信号、梯度截断和解码协议必须一致”” |

```yaml
unit: "dlt-10"
question: "怎样沿时间展开状态与梯度，区分教师驱动、推断循环和长期依赖故障？"
scenario: "对同一序列任务重放RNN、门控单元和编码解码路径"
invariant: "时间轴、初始状态、掩码、教师信号、梯度截断和解码协议必须一致"
fault: "训练时读取未来标签，推断时却宣称模型只依赖历史输入"
evidence: "时间状态表、展开计算图与泄漏反例报告"
reset: restore_concept_mode_trace_step_case_gates_and_artifact
```

“第10章 序列建模：循环和递归网络”要求从同一输入、版本、预算和随机状态重放参考、故障与恢复路径。重置后若目录选择、模式、轨迹步骤、案例、证据门或交付包没有回到基线，本次比较已经混入状态泄漏。

## 本页回顾

掌握“第10章 序列建模：循环和递归网络”不是背诵术语或抄公式，而是能围绕“怎样沿时间展开状态与梯度，区分教师驱动、推断循环和长期依赖故障？”重建输入、shape、计算、目标、更新、随机性与独立评估，并用“时间轴、初始状态、掩码、教师信号、梯度截断和解码协议必须一致”拒绝“训练时读取未来标签，推断时却宣称模型只依赖历史输入”。最终交付为时间状态表、展开计算图与泄漏反例报告

## 练习与答案

<Exercises>

1. **问题 1：实验合同。** “第10章 序列建模：循环和递归网络”为什么必须先冻结输入、版本、shape、目标、预算、随机性和评估口径？

<Answer>
  “第10章
  序列建模：循环和递归网络”若同时改变这些条件，相同输出可能来自不同数据、计算、目标或选择路径；先冻结合同，才能把观测连接到单一机制并定位首差。
</Answer>

2. **问题 2：目录逐项覆盖。** 怎样证明“第10章 序列建模：循环和递归网络”的正式目录坐标已经进入机制、交互和练习？

<Answer>
  1. “第10章 序列建模：循环和递归网络”的目录项「第10章
  序列建模：循环和递归网络」：以“沿时间展开状态、输入依赖、门控和梯度传播”解释作用，用“时间索引、状态、掩码、教师信号、梯度与解码轨迹”提供复核。
  2. “第10章 序列建模：循环和递归网络”的目录项「10·1
  展开计算图」：以“把前向状态、局部导数和反向梯度连接为计算图”解释作用，用“张量shape、节点值、局部导数、梯度与有限差分”提供复核。
  3. “第10章 序列建模：循环和递归网络”的目录项「10·2
  循环神经网络」：以“沿时间展开状态、输入依赖、门控和梯度传播”解释作用，用“时间索引、状态、掩码、教师信号、梯度与解码轨迹”提供复核。
  4. “第10章 序列建模：循环和递归网络”的目录项「10·2·1
  导师驱动过程和输出循环网络」：以“沿时间展开状态、输入依赖、门控和梯度传播”解释作用，用“时间索引、状态、掩码、教师信号、梯度与解码轨迹”提供复核。
  5. “第10章 序列建模：循环和递归网络”的目录项「10·2·2
  计算循环神经网络的梯度」：以“跟踪浮点、梯度、曲率、状态缓冲和参数更新”解释作用，用“dtype、条件数、梯度核对、步长、更新差与停止理由”提供复核。
  6. “第10章 序列建模：循环和递归网络”的目录项「10·2·3
  作为有向图模型的循环网络」：以“沿时间展开状态、输入依赖、门控和梯度传播”解释作用，用“时间索引、状态、掩码、教师信号、梯度与解码轨迹”提供复核。
  7. “第10章 序列建模：循环和递归网络”的目录项「10·2·4
  基于上下文的RNN序列建模」：以“沿时间展开状态、输入依赖、门控和梯度传播”解释作用，用“时间索引、状态、掩码、教师信号、梯度与解码轨迹”提供复核。
  8. “第10章 序列建模：循环和递归网络”的目录项「10·3
  双向RNN」：以“沿时间展开状态、输入依赖、门控和梯度传播”解释作用，用“时间索引、状态、掩码、教师信号、梯度与解码轨迹”提供复核。
  9. “第10章 序列建模：循环和递归网络”的目录项「10·4
  编码器-解码器序列到序列架构」：以“沿时间展开状态、输入依赖、门控和梯度传播”解释作用，用“时间索引、状态、掩码、教师信号、梯度与解码轨迹”提供复核。
  10. “第10章 序列建模：循环和递归网络”的目录项「10·5
  深度循环网络」：以“沿时间展开状态、输入依赖、门控和梯度传播”解释作用，用“时间索引、状态、掩码、教师信号、梯度与解码轨迹”提供复核。
  11. “第10章 序列建模：循环和递归网络”的目录项「10·6
  递归神经网络」：以“把“递归神经网络”放进时间状态、门控与长期依赖的章专属计算链”解释作用，用“第10章
  序列建模：循环和递归网络的输入、状态变化、输出、反例与边界”提供复核。 12.
  “第10章 序列建模：循环和递归网络”的目录项「10·7
  长期依赖的挑战」：以“沿时间展开状态、输入依赖、门控和梯度传播”解释作用，用“时间索引、状态、掩码、教师信号、梯度与解码轨迹”提供复核。
  13. “第10章 序列建模：循环和递归网络”的目录项「10·8 Echo
  State网络」：以“把“Echo
  State网络”放进时间状态、门控与长期依赖的章专属计算链”解释作用，用“第10章
  序列建模：循环和递归网络的输入、状态变化、输出、反例与边界”提供复核。 14.
  “第10章 序列建模：循环和递归网络”的目录项「10·9
  渗漏单元和其他多时间尺度策略」：以“沿时间展开状态、输入依赖、门控和梯度传播”解释作用，用“时间索引、状态、掩码、教师信号、梯度与解码轨迹”提供复核。
  15. “第10章 序列建模：循环和递归网络”的目录项「10·9·1
  时间维度的跳跃连接」：以“沿时间展开状态、输入依赖、门控和梯度传播”解释作用，用“时间索引、状态、掩码、教师信号、梯度与解码轨迹”提供复核。
  16. “第10章 序列建模：循环和递归网络”的目录项「10·9·2
  渗漏单元和一系列不同时间尺度」：以“沿时间展开状态、输入依赖、门控和梯度传播”解释作用，用“时间索引、状态、掩码、教师信号、梯度与解码轨迹”提供复核。
  17. “第10章 序列建模：循环和递归网络”的目录项「10·9·3
  删除连接」：以“把“删除连接”放进时间状态、门控与长期依赖的章专属计算链”解释作用，用“第10章
  序列建模：循环和递归网络的输入、状态变化、输出、反例与边界”提供复核。 18.
  “第10章 序列建模：循环和递归网络”的目录项「10·10
  长短期记忆和其他门控RNN」：以“沿时间展开状态、输入依赖、门控和梯度传播”解释作用，用“时间索引、状态、掩码、教师信号、梯度与解码轨迹”提供复核。
  19. “第10章 序列建模：循环和递归网络”的目录项「10·10·1
  LSTM」：以“沿时间展开状态、输入依赖、门控和梯度传播”解释作用，用“时间索引、状态、掩码、教师信号、梯度与解码轨迹”提供复核。
  20. “第10章 序列建模：循环和递归网络”的目录项「10·10·2
  其他门控RNN」：以“沿时间展开状态、输入依赖、门控和梯度传播”解释作用，用“时间索引、状态、掩码、教师信号、梯度与解码轨迹”提供复核。
  21. “第10章 序列建模：循环和递归网络”的目录项「10·11
  优化长期依赖」：以“跟踪浮点、梯度、曲率、状态缓冲和参数更新”解释作用，用“dtype、条件数、梯度核对、步长、更新差与停止理由”提供复核。
  22. “第10章 序列建模：循环和递归网络”的目录项「10·11·1
  截断梯度」：以“跟踪浮点、梯度、曲率、状态缓冲和参数更新”解释作用，用“dtype、条件数、梯度核对、步长、更新差与停止理由”提供复核。
  23. “第10章 序列建模：循环和递归网络”的目录项「10·11·2
  引导信息流的正则化」：以“声明随机变量、支持集、分布和信息量的概率合同”解释作用，用“归一化、条件方向、样本统计、对数口径与误差”提供复核。
  24. “第10章 序列建模：循环和递归网络”的目录项「10·12
  显式记忆」：以“沿时间展开状态、输入依赖、门控和梯度传播”解释作用，用“时间索引、状态、掩码、教师信号、梯度与解码轨迹”提供复核。
</Answer>

3. **问题 3：故障恢复。** 怎样证明“训练时读取未来标签，推断时却宣称模型只依赖历史输入”已经被修正？

<Answer>
  为“第10章
  序列建模：循环和递归网络”复用同一输入、版本、预算和随机序列，重放参考路径后只注入“训练时读取未来标签，推断时却宣称模型只依赖历史输入”；记录首个偏离，撤销故障再运行。只有依赖地图、计算轨迹、证据门和时间状态表、展开计算图与泄漏反例报告重新满足“时间轴、初始状态、掩码、教师信号、梯度截断和解码协议必须一致”，修正才可提交。
</Answer>

</Exercises>

## 名词解释

<Glossary>
  <GlossaryItem term="序列建模">
    检索键 dlt-A 对应目录坐标「第10章 序列建模：循环和递归网络」；在“第10章
    序列建模：循环和递归网络”中用于沿时间展开状态、输入依赖、门控和梯度传播，需要连接原版范围、计算状态、独立证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="展开计算图">
    检索键 dlt-B 对应目录坐标「10·1 展开计算图」；在“第10章
    序列建模：循环和递归网络”中用于把前向状态、局部导数和反向梯度连接为计算图，需要连接原版范围、计算状态、独立证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="循环神经网络">
    检索键 dlt-C 对应目录坐标「10·2 循环神经网络」；在“第10章
    序列建模：循环和递归网络”中用于沿时间展开状态、输入依赖、门控和梯度传播，需要连接原版范围、计算状态、独立证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="导师驱动过程和输出循环网络">
    检索键 dlt-D 对应目录坐标「10·2·1 导师驱动过程和输出循环网络」；在“第10章
    序列建模：循环和递归网络”中用于沿时间展开状态、输入依赖、门控和梯度传播，需要连接原版范围、计算状态、独立证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="计算循环神经网络的梯度">
    检索键 dlt-E 对应目录坐标「10·2·2 计算循环神经网络的梯度」；在“第10章
    序列建模：循环和递归网络”中用于跟踪浮点、梯度、曲率、状态缓冲和参数更新，需要连接原版范围、计算状态、独立证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="作为有向图模型的循环网络">
    检索键 dlt-F 对应目录坐标「10·2·3 作为有向图模型的循环网络」；在“第10章
    序列建模：循环和递归网络”中用于沿时间展开状态、输入依赖、门控和梯度传播，需要连接原版范围、计算状态、独立证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="基于上下文的RNN序列建模">
    检索键 dlt-G 对应目录坐标「10·2·4 基于上下文的RNN序列建模」；在“第10章
    序列建模：循环和递归网络”中用于沿时间展开状态、输入依赖、门控和梯度传播，需要连接原版范围、计算状态、独立证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="双向RNN">
    检索键 dlt-H 对应目录坐标「10·3 双向RNN」；在“第10章
    序列建模：循环和递归网络”中用于沿时间展开状态、输入依赖、门控和梯度传播，需要连接原版范围、计算状态、独立证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="编码器-解码器序列到序列架构">
    检索键 dlt-I 对应目录坐标「10·4 编码器-解码器序列到序列架构」；在“第10章
    序列建模：循环和递归网络”中用于沿时间展开状态、输入依赖、门控和梯度传播，需要连接原版范围、计算状态、独立证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="深度循环网络">
    检索键 dlt-J 对应目录坐标「10·5 深度循环网络」；在“第10章
    序列建模：循环和递归网络”中用于沿时间展开状态、输入依赖、门控和梯度传播，需要连接原版范围、计算状态、独立证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="递归神经网络">
    检索键 dlt-K 对应目录坐标「10·6 递归神经网络」；在“第10章
    序列建模：循环和递归网络”中用于把“递归神经网络”放进时间状态、门控与长期依赖的章专属计算链，需要连接原版范围、计算状态、独立证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="长期依赖的挑战">
    检索键 dlt-L 对应目录坐标「10·7 长期依赖的挑战」；在“第10章
    序列建模：循环和递归网络”中用于沿时间展开状态、输入依赖、门控和梯度传播，需要连接原版范围、计算状态、独立证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="Echo State网络">
    检索键 dlt-M 对应目录坐标「10·8 Echo State网络」；在“第10章
    序列建模：循环和递归网络”中用于把“Echo
    State网络”放进时间状态、门控与长期依赖的章专属计算链，需要连接原版范围、计算状态、独立证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="渗漏单元和其他多时间尺度策略">
    检索键 dlt-N 对应目录坐标「10·9 渗漏单元和其他多时间尺度策略」；在“第10章
    序列建模：循环和递归网络”中用于沿时间展开状态、输入依赖、门控和梯度传播，需要连接原版范围、计算状态、独立证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="时间维度的跳跃连接">
    检索键 dlt-O 对应目录坐标「10·9·1 时间维度的跳跃连接」；在“第10章
    序列建模：循环和递归网络”中用于沿时间展开状态、输入依赖、门控和梯度传播，需要连接原版范围、计算状态、独立证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="渗漏单元和一系列不同时间尺度">
    检索键 dlt-P 对应目录坐标「10·9·2 渗漏单元和一系列不同时间尺度」；在“第10章
    序列建模：循环和递归网络”中用于沿时间展开状态、输入依赖、门控和梯度传播，需要连接原版范围、计算状态、独立证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="删除连接">
    检索键 dlt-Q 对应目录坐标「10·9·3 删除连接」；在“第10章
    序列建模：循环和递归网络”中用于把“删除连接”放进时间状态、门控与长期依赖的章专属计算链，需要连接原版范围、计算状态、独立证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="长短期记忆和其他门控RNN">
    检索键 dlt-R 对应目录坐标「10·10 长短期记忆和其他门控RNN」；在“第10章
    序列建模：循环和递归网络”中用于沿时间展开状态、输入依赖、门控和梯度传播，需要连接原版范围、计算状态、独立证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="LSTM">
    检索键 dlt-S 对应目录坐标「10·10·1 LSTM」；在“第10章
    序列建模：循环和递归网络”中用于沿时间展开状态、输入依赖、门控和梯度传播，需要连接原版范围、计算状态、独立证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="其他门控RNN">
    检索键 dlt-T 对应目录坐标「10·10·2 其他门控RNN」；在“第10章
    序列建模：循环和递归网络”中用于沿时间展开状态、输入依赖、门控和梯度传播，需要连接原版范围、计算状态、独立证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="优化长期依赖">
    检索键 dlt-U 对应目录坐标「10·11 优化长期依赖」；在“第10章
    序列建模：循环和递归网络”中用于跟踪浮点、梯度、曲率、状态缓冲和参数更新，需要连接原版范围、计算状态、独立证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="截断梯度">
    检索键 dlt-V 对应目录坐标「10·11·1 截断梯度」；在“第10章
    序列建模：循环和递归网络”中用于跟踪浮点、梯度、曲率、状态缓冲和参数更新，需要连接原版范围、计算状态、独立证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="引导信息流的正则化">
    检索键 dlt-W 对应目录坐标「10·11·2 引导信息流的正则化」；在“第10章
    序列建模：循环和递归网络”中用于声明随机变量、支持集、分布和信息量的概率合同，需要连接原版范围、计算状态、独立证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="显式记忆">
    检索键 dlt-X 对应目录坐标「10·12 显式记忆」；在“第10章
    序列建模：循环和递归网络”中用于沿时间展开状态、输入依赖、门控和梯度传播，需要连接原版范围、计算状态、独立证据和不适用边界。
  </GlossaryItem>
</Glossary>

<Attribution
  mode="independent-rewrite"
  sourceBasis="full-open-access-non-derivative-plus-publisher-toc"
  workTitle="Ian Goodfellow、Yoshua Bengio、Aaron Courville著《Deep Learning》"
  adaptedUrl="https://www.deeplearningbook.org/"
/>
