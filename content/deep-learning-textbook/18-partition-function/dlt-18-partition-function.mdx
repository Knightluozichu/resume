---
title: 第18章 直面配分函数
type: B
section: 第18章 直面配分函数
order: 18
description: 比较似然梯度、对比散度、伪似然、得分匹配与配分函数估计；用依赖地图、计算轨迹和独立证据门交付正负相梯度、近似偏差与配分函数估计报告
demo: true
math: true
sourceUrl: "https://www.deeplearningbook.org/"
draft: false
qualityVersion: 2
practiceMode: simulation
sourceMode: independent-rewrite
officialUnitId: dlt-18
---

import {
  Objectives,
  Term,
  Callout,
  Stepper,
  Step,
  Exercises,
  Answer,
  Glossary,
  GlossaryItem,
  Attribution,
} from "@/components/mdx/mdx-components";
import {
  Dlt18PartitionFunctionDependencyMapLab,
  Dlt18PartitionFunctionComputationTraceLab,
  Dlt18PartitionFunctionEvidenceGateLab,
} from "@/components/mdx/deep-learning-textbook/v2/dlt-18-partition-function";

<Objectives>

- 能说明“第18章 直面配分函数”如何比较似然梯度、对比散度、伪似然、得分匹配与配分函数估计，并区分原版、中文目录、开放访问许可和当前独立重写
- 能先预测“怎样在未知配分函数下区分目标近似、梯度估计和归一化估计的误差？”会改变哪个输入、shape、状态、目标、梯度、估计或评估结果，再操作三类证据视图
- 能只注入“短链对比散度目标下降被当作精确对数似然上升”，定位首个偏离“能量尺度、负相采样、链状态、近似目标和评估估计器必须一致”的状态，并从同一快照完成恢复

</Objectives>

## 为什么从这个问题开始

“第18章 直面配分函数”围绕“怎样在未知配分函数下区分目标近似、梯度估计和归一化估计的误差？”建立贯穿任务：训练并评估一个基于能量的模型。先写下哪个输入、shape、状态、目标、梯度、估计或评估会先变化，再运行参考、故障和恢复路径；运行后补理由不算预测。只有“第18章 直面配分函数”守住“能量尺度、负相采样、链状态、近似目标和评估估计器必须一致”并交付正负相梯度、近似偏差与配分函数估计报告，最终分数、似然、重构、样本或部署结果才构成机制证据。

## 书目、353个正式坐标与许可边界

“第18章 直面配分函数”以[MIT Press书目页](https://mitpress.mit.edu/9780262035613/deep-learning/)核对Ian Goodfellow、Yoshua Bengio、Aaron Courville著 _Deep Learning_：精装ISBN 9780262035613，2016年11月18日出版，800页；MIT Press同时把[作者托管版](https://www.deeplearningbook.org/)列为开放访问资源，并标注[CC BY-NC-ND 4.0](https://creativecommons.org/licenses/by-nc-nd/4.0/)。开放阅读不等于允许演绎，“第18章 直面配分函数”不复制、翻译或改写受保护正文，只把原版公开目录与可定位章节用作范围和事实核对。

“第18章 直面配分函数”以[作者官方目录](https://www.deeplearningbook.org/contents/TOC.html)核对3个部分、20章和164个一级节，再以[人民邮电出版社官方中文书页](https://detail.youzan.com/show/goods?alias=275704xsv5xoh&from_source=gbox_seo)核对166个二级节、中文书名、ISBN 9787115461476和2017年8月出版信息；合计353个正式目录坐标。出版社页面中“有效有效”、缺空格等排版噪声已按英文原版标题规范化，但没有新增原版不存在的章节。

“第18章 直面配分函数”的中文讲解、状态图、数值实验、练习与答案均为独立教学重写；2016年以后出现的框架行为或研究进展必须带当前时间标签，不能倒填为原书原话。[章专属核对 1](https://docs.scipy.org/doc/scipy/reference/stats.html)、[章专属核对 2](https://www.deeplearningbook.org/)只用于核对本页算法、API或实验边界，不能反向证明原书采用本站表述。

## 原版目录层级与可验证机制

### 第18章 直面配分函数

<Term def="直面配分函数对应正式目录坐标“第18章 直面配分函数”，在“第18章 直面配分函数”中用于区分能量、归一化、正负相与近似训练目标，并受数据、shape、目标、随机性、评估与版本边界约束。">
  直面配分函数
</Term>

**正式坐标 1/10。** 目录原文键 `第18章 直面配分函数`。在“第18章 直面配分函数”的第1个坐标中，「第18章 直面配分函数」通过区分能量、归一化、正负相与近似训练目标推进能量、负相与配分函数；复核者保存能量、正负相梯度、链状态、近似偏差与归一化估计，一旦出现代理目标改善被写成精确似然改善就撤回结论。

### 18·1 对数似然梯度

<Term def="对数似然梯度对应正式目录坐标“18·1 对数似然梯度”，在“第18章 直面配分函数”中用于跟踪浮点、梯度、曲率、状态缓冲和参数更新，并受数据、shape、目标、随机性、评估与版本边界约束。">
  对数似然梯度
</Term>

**正式坐标 2/10。** 目录原文键 `18.1 对数似然梯度`。围绕“怎样在未知配分函数下区分目标近似、梯度估计和归一化估计的误差？”，“第18章 直面配分函数”在坐标2把「18·1 对数似然梯度」落实为跟踪浮点、梯度、曲率、状态缓冲和参数更新；只有dtype、条件数、梯度核对、步长、更新差与停止理由可复算且反例排除只看目标下降而忽略数值不稳、状态错位或泛化失败，本节点才算掌握。

### 18·2 随机最大似然和对比散度

<Term def="随机最大似然和对比散度对应正式目录坐标“18·2 随机最大似然和对比散度”，在“第18章 直面配分函数”中用于建立任务、数据角色、估计、选择与泛化协议，并受数据、shape、目标、随机性、评估与版本边界约束。">
  随机最大似然和对比散度
</Term>

**正式坐标 3/10。** 目录原文键 `18.2 随机最大似然和对比散度`。“第18章 直面配分函数”的原版节点3「18·2 随机最大似然和对比散度」不能停在名词解释：它要建立任务、数据角色、估计、选择与泛化协议，交付实体切分、候选比较、偏差—方差、选择日志与封存测试，并用训练、验证和测试角色泄漏后仍报告独立泛化作为单一反事实检查。

### 18·3 伪似然

<Term def="伪似然对应正式目录坐标“18·3 伪似然”，在“第18章 直面配分函数”中用于建立任务、数据角色、估计、选择与泛化协议，并受数据、shape、目标、随机性、评估与版本边界约束。">
  伪似然
</Term>

**正式坐标 4/10。** 目录原文键 `18.3 伪似然`。对“第18章 直面配分函数”而言，「18·3 伪似然」在第4次检查中改变可观察状态，因为它负责建立任务、数据角色、估计、选择与泛化协议；实体切分、候选比较、偏差—方差、选择日志与封存测试必须与“能量尺度、负相采样、链状态、近似目标和评估估计器必须一致”对齐，不能接受训练、验证和测试角色泄漏后仍报告独立泛化。

### 18·4 得分匹配和比率匹配

<Term def="得分匹配和比率匹配对应正式目录坐标“18·4 得分匹配和比率匹配”，在“第18章 直面配分函数”中用于区分能量、归一化、正负相与近似训练目标，并受数据、shape、目标、随机性、评估与版本边界约束。">
  得分匹配和比率匹配
</Term>

**正式坐标 5/10。** 目录原文键 `18.4 得分匹配和比率匹配`。目录原文「18·4 得分匹配和比率匹配」进入“第18章 直面配分函数”后形成第5个实验合同：先区分能量、归一化、正负相与近似训练目标，再保存能量、正负相梯度、链状态、近似偏差与归一化估计；若发生代理目标改善被写成精确似然改善，从同一输入快照恢复。

### 18·5 去噪得分匹配

<Term def="去噪得分匹配对应正式目录坐标“18·5 去噪得分匹配”，在“第18章 直面配分函数”中用于区分能量、归一化、正负相与近似训练目标，并受数据、shape、目标、随机性、评估与版本边界约束。">
  去噪得分匹配
</Term>

**正式坐标 6/10。** 目录原文键 `18.5 去噪得分匹配`。在“第18章 直面配分函数”的第6个坐标中，「18·5 去噪得分匹配」通过区分能量、归一化、正负相与近似训练目标推进能量、负相与配分函数；复核者保存能量、正负相梯度、链状态、近似偏差与归一化估计，一旦出现代理目标改善被写成精确似然改善就撤回结论。

### 18·6 噪声对比估计

<Term def="噪声对比估计对应正式目录坐标“18·6 噪声对比估计”，在“第18章 直面配分函数”中用于建立任务、数据角色、估计、选择与泛化协议，并受数据、shape、目标、随机性、评估与版本边界约束。">
  噪声对比估计
</Term>

**正式坐标 7/10。** 目录原文键 `18.6 噪声对比估计`。围绕“怎样在未知配分函数下区分目标近似、梯度估计和归一化估计的误差？”，“第18章 直面配分函数”在坐标7把「18·6 噪声对比估计」落实为建立任务、数据角色、估计、选择与泛化协议；只有实体切分、候选比较、偏差—方差、选择日志与封存测试可复算且反例排除训练、验证和测试角色泄漏后仍报告独立泛化，本节点才算掌握。

### 18·7 估计配分函数

<Term def="估计配分函数对应正式目录坐标“18·7 估计配分函数”，在“第18章 直面配分函数”中用于建立任务、数据角色、估计、选择与泛化协议，并受数据、shape、目标、随机性、评估与版本边界约束。">
  估计配分函数
</Term>

**正式坐标 8/10。** 目录原文键 `18.7 估计配分函数`。“第18章 直面配分函数”的原版节点8「18·7 估计配分函数」不能停在名词解释：它要建立任务、数据角色、估计、选择与泛化协议，交付实体切分、候选比较、偏差—方差、选择日志与封存测试，并用训练、验证和测试角色泄漏后仍报告独立泛化作为单一反事实检查。

### 18·7·1 退火重要采样

<Term def="退火重要采样对应正式目录坐标“18·7·1 退火重要采样”，在“第18章 直面配分函数”中用于用采样、权重、自相关和跨链诊断估计目标量，并受数据、shape、目标、随机性、评估与版本边界约束。">
  退火重要采样
</Term>

**正式坐标 9/10。** 目录原文键 `18.7.1 退火重要采样`。对“第18章 直面配分函数”而言，「18·7·1 退火重要采样」在第9次检查中改变可观察状态，因为它负责用采样、权重、自相关和跨链诊断估计目标量；链轨迹、权重、有效样本量、自相关与多链诊断必须与“能量尺度、负相采样、链状态、近似目标和评估估计器必须一致”对齐，不能接受单链困在一个峰却因样本数多而宣称收敛。

### 18·7·2 桥式采样

<Term def="桥式采样对应正式目录坐标“18·7·2 桥式采样”，在“第18章 直面配分函数”中用于用采样、权重、自相关和跨链诊断估计目标量，并受数据、shape、目标、随机性、评估与版本边界约束。">
  桥式采样
</Term>

**正式坐标 10/10。** 目录原文键 `18.7.2 桥式采样`。目录原文「18·7·2 桥式采样」进入“第18章 直面配分函数”后形成第10个实验合同：先用采样、权重、自相关和跨链诊断估计目标量，再保存链轨迹、权重、有效样本量、自相关与多链诊断；若发生单链困在一个峰却因样本数多而宣称收敛，从同一输入快照恢复。

## 先预测，再操作三个章专属实验

<Callout type="info" title="先写出哪个状态会先变化">
  对“第18章
  直面配分函数”先冻结训练并评估一个基于能量的模型的数据、shape、目标、预算、随机性和评估口径，再操作依赖地图、计算轨迹与证据门；结果与预测不同就修改假设，不删除失败运行。
</Callout>

<Stepper>
  <Step title="1. 原版坐标、依赖与状态">
    为“第18章 直面配分函数”选择一个正式目录坐标，在参考合同与单一反例间切换，逐阶段核对输入、操作、输出和检查条件。

    <Dlt18PartitionFunctionDependencyMapLab />

  </Step>
  <Step title="2. 参考与故障计算轨迹">
    对“第18章 直面配分函数”保持训练并评估一个基于能量的模型不变，只注入“短链对比散度目标下降被当作精确对数似然上升”，逐步定位首个偏离“能量尺度、负相采样、链状态、近似目标和评估估计器必须一致”的位置。

    <Dlt18PartitionFunctionComputationTraceLab />

  </Step>
  <Step title="3. 独立证据与交付门">
    在“第18章 直面配分函数”的基线、单故障和恢复案例间切换，展开来源、计算、目标和独立评估门后再决定是否交付。

    <Dlt18PartitionFunctionEvidenceGateLab />

  </Step>
</Stepper>

<Callout
  type="trap"
  title="本页单一故障：短链对比散度目标下降被当作精确对数似然上升"
>
  “第18章
  直面配分函数”遇到该故障时必须保持其余输入、版本、预算和随机序列不变；最终分数偶尔更高不能替代首个状态分岔和恢复证据。
</Callout>

<Callout type="trap" title="代理目标不等于最终结论">
  “第18章
  直面配分函数”中的训练损失、下界、重构误差、链内统计或精选样本只回答各自合同；它们不能自动证明泛化、精确似然、因果关系或部署可靠性。
</Callout>

<Callout type="trap" title="开放访问不等于允许演绎">
  “第18章 直面配分函数”可以定位作者托管原版和MIT Press元数据，但CC
  BY-NC-ND不授权翻译或改写原书正文；本站内容必须保持独立表达、技术来源和时间边界。
</Callout>

## 第18章 直面配分函数的可重放协议

| 阶段                             | 允许动作                                                                           | 必留证据                                    | 拒绝条件                                                                                  |
| -------------------------------- | ---------------------------------------------------------------------------------- | ------------------------------------------- | ----------------------------------------------------------------------------------------- |
| 第18章 直面配分函数 · 输入与角色 | 冻结能量、负相与配分函数所需的数据角色、版本和shape                                | 第18章 直面配分函数的输入合同与基线快照     | 未满足“第18章 直面配分函数的来源、角色、单位、shape和可见性没有越界”                      |
| 第18章 直面配分函数 · 计算与状态 | 执行比较似然梯度、对比散度、伪似然、得分匹配与配分函数估计的最小计算并保存中间状态 | 第18章 直面配分函数的参考轨迹与单故障轨迹   | 未满足“第18章 直面配分函数每一步状态变化都能由输入、公式或算法操作复算”                   |
| 第18章 直面配分函数 · 目标与更新 | 比较目标分量、梯度、估计或选择决定                                                 | 第18章 直面配分函数的更新前后差异与首个分岔 | 未满足“第18章 直面配分函数没有把代理目标、训练分数或偶然样本当作最终结论”                 |
| 第18章 直面配分函数 · 独立评估   | 重放预测、反例、恢复和边界检查                                                     | 第18章 直面配分函数的接受、回退或拒绝理由   | 未满足“第18章 直面配分函数满足“能量尺度、负相采样、链状态、近似目标和评估估计器必须一致”” |

```yaml
unit: "dlt-18"
question: "怎样在未知配分函数下区分目标近似、梯度估计和归一化估计的误差？"
scenario: "训练并评估一个基于能量的模型"
invariant: "能量尺度、负相采样、链状态、近似目标和评估估计器必须一致"
fault: "短链对比散度目标下降被当作精确对数似然上升"
evidence: "正负相梯度、近似偏差与配分函数估计报告"
reset: restore_concept_mode_trace_step_case_gates_and_artifact
```

“第18章 直面配分函数”要求从同一输入、版本、预算和随机状态重放参考、故障与恢复路径。重置后若目录选择、模式、轨迹步骤、案例、证据门或交付包没有回到基线，本次比较已经混入状态泄漏。

## 本页回顾

掌握“第18章 直面配分函数”不是背诵术语或抄公式，而是能围绕“怎样在未知配分函数下区分目标近似、梯度估计和归一化估计的误差？”重建输入、shape、计算、目标、更新、随机性与独立评估，并用“能量尺度、负相采样、链状态、近似目标和评估估计器必须一致”拒绝“短链对比散度目标下降被当作精确对数似然上升”。最终交付为正负相梯度、近似偏差与配分函数估计报告

## 练习与答案

<Exercises>

1. **问题 1：实验合同。** “第18章 直面配分函数”为什么必须先冻结输入、版本、shape、目标、预算、随机性和评估口径？

<Answer>
  “第18章
  直面配分函数”若同时改变这些条件，相同输出可能来自不同数据、计算、目标或选择路径；先冻结合同，才能把观测连接到单一机制并定位首差。
</Answer>

2. **问题 2：目录逐项覆盖。** 怎样证明“第18章 直面配分函数”的正式目录坐标已经进入机制、交互和练习？

<Answer>
  1. “第18章 直面配分函数”的目录项「第18章
  直面配分函数」：以“区分能量、归一化、正负相与近似训练目标”解释作用，用“能量、正负相梯度、链状态、近似偏差与归一化估计”提供复核。
  2. “第18章 直面配分函数”的目录项「18·1
  对数似然梯度」：以“跟踪浮点、梯度、曲率、状态缓冲和参数更新”解释作用，用“dtype、条件数、梯度核对、步长、更新差与停止理由”提供复核。
  3. “第18章 直面配分函数”的目录项「18·2
  随机最大似然和对比散度」：以“建立任务、数据角色、估计、选择与泛化协议”解释作用，用“实体切分、候选比较、偏差—方差、选择日志与封存测试”提供复核。
  4. “第18章 直面配分函数”的目录项「18·3
  伪似然」：以“建立任务、数据角色、估计、选择与泛化协议”解释作用，用“实体切分、候选比较、偏差—方差、选择日志与封存测试”提供复核。
  5. “第18章 直面配分函数”的目录项「18·4
  得分匹配和比率匹配」：以“区分能量、归一化、正负相与近似训练目标”解释作用，用“能量、正负相梯度、链状态、近似偏差与归一化估计”提供复核。
  6. “第18章 直面配分函数”的目录项「18·5
  去噪得分匹配」：以“区分能量、归一化、正负相与近似训练目标”解释作用，用“能量、正负相梯度、链状态、近似偏差与归一化估计”提供复核。
  7. “第18章 直面配分函数”的目录项「18·6
  噪声对比估计」：以“建立任务、数据角色、估计、选择与泛化协议”解释作用，用“实体切分、候选比较、偏差—方差、选择日志与封存测试”提供复核。
  8. “第18章 直面配分函数”的目录项「18·7
  估计配分函数」：以“建立任务、数据角色、估计、选择与泛化协议”解释作用，用“实体切分、候选比较、偏差—方差、选择日志与封存测试”提供复核。
  9. “第18章 直面配分函数”的目录项「18·7·1
  退火重要采样」：以“用采样、权重、自相关和跨链诊断估计目标量”解释作用，用“链轨迹、权重、有效样本量、自相关与多链诊断”提供复核。
  10. “第18章 直面配分函数”的目录项「18·7·2
  桥式采样」：以“用采样、权重、自相关和跨链诊断估计目标量”解释作用，用“链轨迹、权重、有效样本量、自相关与多链诊断”提供复核。
</Answer>

3. **问题 3：故障恢复。** 怎样证明“短链对比散度目标下降被当作精确对数似然上升”已经被修正？

<Answer>
  为“第18章
  直面配分函数”复用同一输入、版本、预算和随机序列，重放参考路径后只注入“短链对比散度目标下降被当作精确对数似然上升”；记录首个偏离，撤销故障再运行。只有依赖地图、计算轨迹、证据门和正负相梯度、近似偏差与配分函数估计报告重新满足“能量尺度、负相采样、链状态、近似目标和评估估计器必须一致”，修正才可提交。
</Answer>

</Exercises>

## 名词解释

<Glossary>
  <GlossaryItem term="直面配分函数">
    检索键 dlt-A 对应目录坐标「第18章 直面配分函数」；在“第18章
    直面配分函数”中用于区分能量、归一化、正负相与近似训练目标，需要连接原版范围、计算状态、独立证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="对数似然梯度">
    检索键 dlt-B 对应目录坐标「18·1 对数似然梯度」；在“第18章
    直面配分函数”中用于跟踪浮点、梯度、曲率、状态缓冲和参数更新，需要连接原版范围、计算状态、独立证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="随机最大似然和对比散度">
    检索键 dlt-C 对应目录坐标「18·2 随机最大似然和对比散度」；在“第18章
    直面配分函数”中用于建立任务、数据角色、估计、选择与泛化协议，需要连接原版范围、计算状态、独立证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="伪似然">
    检索键 dlt-D 对应目录坐标「18·3 伪似然」；在“第18章
    直面配分函数”中用于建立任务、数据角色、估计、选择与泛化协议，需要连接原版范围、计算状态、独立证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="得分匹配和比率匹配">
    检索键 dlt-E 对应目录坐标「18·4 得分匹配和比率匹配」；在“第18章
    直面配分函数”中用于区分能量、归一化、正负相与近似训练目标，需要连接原版范围、计算状态、独立证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="去噪得分匹配">
    检索键 dlt-F 对应目录坐标「18·5 去噪得分匹配」；在“第18章
    直面配分函数”中用于区分能量、归一化、正负相与近似训练目标，需要连接原版范围、计算状态、独立证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="噪声对比估计">
    检索键 dlt-G 对应目录坐标「18·6 噪声对比估计」；在“第18章
    直面配分函数”中用于建立任务、数据角色、估计、选择与泛化协议，需要连接原版范围、计算状态、独立证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="估计配分函数">
    检索键 dlt-H 对应目录坐标「18·7 估计配分函数」；在“第18章
    直面配分函数”中用于建立任务、数据角色、估计、选择与泛化协议，需要连接原版范围、计算状态、独立证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="退火重要采样">
    检索键 dlt-I 对应目录坐标「18·7·1 退火重要采样」；在“第18章
    直面配分函数”中用于用采样、权重、自相关和跨链诊断估计目标量，需要连接原版范围、计算状态、独立证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="桥式采样">
    检索键 dlt-J 对应目录坐标「18·7·2 桥式采样」；在“第18章
    直面配分函数”中用于用采样、权重、自相关和跨链诊断估计目标量，需要连接原版范围、计算状态、独立证据和不适用边界。
  </GlossaryItem>
</Glossary>

<Attribution
  mode="independent-rewrite"
  sourceBasis="full-open-access-non-derivative-plus-publisher-toc"
  workTitle="Ian Goodfellow、Yoshua Bengio、Aaron Courville著《Deep Learning》"
  adaptedUrl="https://www.deeplearningbook.org/"
/>
