---
title: 第8章 带基线的策略梯度方法
type: C
section: 第8章 带基线的策略梯度方法
order: 8
description: 证明基线不改变期望策略梯度，并比较REINFORCE基线与A2C的方差和自举；用环境合同、更新轨迹和独立评估门交付基线前后梯度均值/方差、优势和价值残差
demo: true
math: true
sourceUrl: "https://github.com/DeepRLChinese/DeepRL-Chinese"
draft: false
qualityVersion: 2
practiceMode: simulation
sourceMode: independent-rewrite
officialUnitId: drl-08
---

import {
  Objectives,
  Term,
  Callout,
  Stepper,
  Step,
  Exercises,
  Answer,
  Glossary,
  GlossaryItem,
  Attribution,
} from "@/components/mdx/mdx-components";
import {
  Drl08PolicyGradientBaselineEnvironmentContractLab,
  Drl08PolicyGradientBaselineReturnUpdateTraceLab,
  Drl08PolicyGradientBaselineEvaluationGateLab,
} from "@/components/mdx/deep-reinforcement-learning/v2/drl-08-policy-gradient-baseline";

<Objectives>

- 能说明“第8章 带基线的策略梯度方法”如何证明基线不改变期望策略梯度，并比较REINFORCE基线与A2C的方差和自举，并区分原版目录、作者代码映射、独立技术来源和本站重写
- 能先预测“基线减去什么、是否依赖动作、优势目标在哪里停止梯度？”会改变哪一个环境、轨迹、回报、目标、策略或评估状态，再操作三类交互证据
- 能只注入“让基线直接依赖当前动作却仍按无偏基线解释”，定位首个偏离“行为策略、回报、状态基线、停止梯度与价值目标角色明确分离”的状态，并从同一快照完成恢复

</Objectives>

## 为什么从这个问题开始

“第8章 带基线的策略梯度方法”围绕“基线减去什么、是否依赖动作、优势目标在哪里停止梯度？”建立贯穿任务：在同一轨迹上比较无基线、状态基线和A2C。先写下哪个环境、轨迹、回报、目标、策略或评估状态会最先变化，再运行参考、故障和恢复路径；运行后补理由不算预测。只有守住“行为策略、回报、状态基线、停止梯度与价值目标角色明确分离”并交付基线前后梯度均值/方差、优势和价值残差，训练回报、策略价值、搜索统计或应用收益才构成机制证据。

## 原版书目、272个正式坐标与访问边界

“第8章 带基线的策略梯度方法”以[作者官方代码仓库](https://github.com/DeepRLChinese/DeepRL-Chinese)核对王树森、黎彧君、张志华著《深度强化学习》的第1至19章及主要算法—实现映射，以[馆藏书目](https://lib2.buct.edu.cn/bookInfo_01h1336186.html)核对人民邮电出版社、2022年11月、294页和ISBN 9787115600691，再以[发行数字版完整目录](https://online.anyflip.com/sckew/oxjt/)核对5个部分、19章、233个编号节/小节、附录A、附录B与B.1至B.13，合计272个正式目录层级。

作者仓库验证章节和算法实现确实对应，但仓库页面未提供可据以复用代码、图表或书稿的明确许可；发行数字版用于核对目录范围，不作为复制正文的授权。“第8章 带基线的策略梯度方法”不翻译、摘编或改写原书正文，也不复制仓库代码；中文讲解、状态轨迹、反例、交互、练习与答案均为独立教学重写。

[本页独立核对 1](http://incompleteideas.net/book/the-book-2nd.html)、[本页独立核对 2](https://github.com/DeepRLChinese/DeepRL-Chinese)只用于独立核对本页算法、环境接口或实验边界，不能反向证明原书使用本站表述。Gymnasium、PyTorch、PettingZoo等当前API行为按2026-07-30核对并显式视为当前实现，不倒填为2022年原书内容。

## 原版目录层级与可验证机制

### 第8章 带基线的策略梯度方法

<Term def="带基线的策略梯度方法对应正式目录坐标“第8章 带基线的策略梯度方法”，在“第8章 带基线的策略梯度方法”中用于建立环境、轨迹、终止、策略、回报和价值的时间合同，并受环境、轨迹、策略快照、随机性、评估与版本边界约束。">
  带基线的策略梯度方法
</Term>

**正式坐标 1/13。** 原版目录键 `第8章 带基线的策略梯度方法`。在“第8章 带基线的策略梯度方法”的第1个正式坐标中，「第8章 带基线的策略梯度方法」通过建立环境、轨迹、终止、策略、回报和价值的时间合同推进基线、优势与方差控制；复核者保存转移记录、策略概率、终止/截断标记、回报与价值残差，出现混淆状态与观测、终止与截断或行为策略与目标策略就撤回结论。

### 8·1 策略梯度中的基线

<Term def="策略梯度中的基线对应正式目录坐标“8.1 策略梯度中的基线”，在“第8章 带基线的策略梯度方法”中用于建立环境、轨迹、终止、策略、回报和价值的时间合同，并受环境、轨迹、策略快照、随机性、评估与版本边界约束。">
  策略梯度中的基线
</Term>

**正式坐标 2/13。** 原版目录键 `8.1 策略梯度中的基线`。围绕“基线减去什么、是否依赖动作、优势目标在哪里停止梯度？”，“第8章 带基线的策略梯度方法”在坐标2把「8·1 策略梯度中的基线」落实为建立环境、轨迹、终止、策略、回报和价值的时间合同；只有转移记录、策略概率、终止/截断标记、回报与价值残差可重放且反例排除混淆状态与观测、终止与截断或行为策略与目标策略，本节点才算掌握。

### 8·1·1 基线的引入

<Term def="基线的引入对应正式目录坐标“8.1.1 基线的引入”，在“第8章 带基线的策略梯度方法”中用于从轨迹概率、回报、优势、概率比与KL约束推导策略更新，并受环境、轨迹、策略快照、随机性、评估与版本边界约束。">
  基线的引入
</Term>

**正式坐标 3/13。** 原版目录键 `8.1.1 基线的引入`。“第8章 带基线的策略梯度方法”的目录节点3「8·1·1 基线的引入」不能停在术语复述：它要从轨迹概率、回报、优势、概率比与KL约束推导策略更新，交付动作对数概率、回报、基线、优势、梯度、KL与接受规则，并把用新策略重算旧轨迹、动作依赖基线或越过置信域设为单一反事实。

### 8·1·2 基线的直观解释

<Term def="基线的直观解释对应正式目录坐标“8.1.2 基线的直观解释”，在“第8章 带基线的策略梯度方法”中用于从轨迹概率、回报、优势、概率比与KL约束推导策略更新，并受环境、轨迹、策略快照、随机性、评估与版本边界约束。">
  基线的直观解释
</Term>

**正式坐标 4/13。** 原版目录键 `8.1.2 基线的直观解释`。对“第8章 带基线的策略梯度方法”而言，「8·1·2 基线的直观解释」在第4次检查中改变可观察状态，因为它负责从轨迹概率、回报、优势、概率比与KL约束推导策略更新；动作对数概率、回报、基线、优势、梯度、KL与接受规则必须与“行为策略、回报、状态基线、停止梯度与价值目标角色明确分离”对齐，不能接受用新策略重算旧轨迹、动作依赖基线或越过置信域。

### 8·2 带基线的REINFORCE算法

<Term def="带基线的REINFORCE算法对应正式目录坐标“8.2 带基线的REINFORCE算法”，在“第8章 带基线的策略梯度方法”中用于从轨迹概率、回报、优势、概率比与KL约束推导策略更新，并受环境、轨迹、策略快照、随机性、评估与版本边界约束。">
  带基线的REINFORCE算法
</Term>

**正式坐标 5/13。** 原版目录键 `8.2 带基线的REINFORCE算法`。在“第8章 带基线的策略梯度方法”的第5个正式坐标中，「8·2 带基线的REINFORCE算法」通过从轨迹概率、回报、优势、概率比与KL约束推导策略更新推进基线、优势与方差控制；复核者保存动作对数概率、回报、基线、优势、梯度、KL与接受规则，出现用新策略重算旧轨迹、动作依赖基线或越过置信域就撤回结论。

### 8·2·1 策略网络和价值网络

<Term def="策略网络和价值网络对应正式目录坐标“8.2.1 策略网络和价值网络”，在“第8章 带基线的策略梯度方法”中用于建立环境、轨迹、终止、策略、回报和价值的时间合同，并受环境、轨迹、策略快照、随机性、评估与版本边界约束。">
  策略网络和价值网络
</Term>

**正式坐标 6/13。** 原版目录键 `8.2.1 策略网络和价值网络`。围绕“基线减去什么、是否依赖动作、优势目标在哪里停止梯度？”，“第8章 带基线的策略梯度方法”在坐标6把「8·2·1 策略网络和价值网络」落实为建立环境、轨迹、终止、策略、回报和价值的时间合同；只有转移记录、策略概率、终止/截断标记、回报与价值残差可重放且反例排除混淆状态与观测、终止与截断或行为策略与目标策略，本节点才算掌握。

### 8·2·2 算法推导

<Term def="算法推导对应正式目录坐标“8.2.2 算法推导”，在“第8章 带基线的策略梯度方法”中用于把“算法推导”放进基线、优势与方差控制的环境—轨迹—目标—更新链，并受环境、轨迹、策略快照、随机性、评估与版本边界约束。">
  算法推导
</Term>

**正式坐标 7/13。** 原版目录键 `8.2.2 算法推导`。“第8章 带基线的策略梯度方法”的目录节点7「8·2·2 算法推导」不能停在术语复述：它要把“算法推导”放进基线、优势与方差控制的环境—轨迹—目标—更新链，交付第8章 带基线的策略梯度方法的输入角色、中间状态、策略快照、反例与独立评估，并把只复述“算法推导”名称而没有可观察状态、单一故障和恢复证据设为单一反事实。

### 8·2·3 训练流程

<Term def="训练流程对应正式目录坐标“8.2.3 训练流程”，在“第8章 带基线的策略梯度方法”中用于把“训练流程”放进基线、优势与方差控制的环境—轨迹—目标—更新链，并受环境、轨迹、策略快照、随机性、评估与版本边界约束。">
  训练流程
</Term>

**正式坐标 8/13。** 原版目录键 `8.2.3 训练流程`。对“第8章 带基线的策略梯度方法”而言，「8·2·3 训练流程」在第8次检查中改变可观察状态，因为它负责把“训练流程”放进基线、优势与方差控制的环境—轨迹—目标—更新链；第8章 带基线的策略梯度方法的输入角色、中间状态、策略快照、反例与独立评估必须与“行为策略、回报、状态基线、停止梯度与价值目标角色明确分离”对齐，不能接受只复述“训练流程”名称而没有可观察状态、单一故障和恢复证据。

### 8·3 advantage actor-critic

<Term def="advantage actor-critic对应正式目录坐标“8.3 advantage actor-critic”，在“第8章 带基线的策略梯度方法”中用于从轨迹概率、回报、优势、概率比与KL约束推导策略更新，并受环境、轨迹、策略快照、随机性、评估与版本边界约束。">
  advantage actor-critic
</Term>

**正式坐标 9/13。** 原版目录键 `8.3 advantage actor-critic`。在“第8章 带基线的策略梯度方法”的第9个正式坐标中，「8·3 advantage actor-critic」通过从轨迹概率、回报、优势、概率比与KL约束推导策略更新推进基线、优势与方差控制；复核者保存动作对数概率、回报、基线、优势、梯度、KL与接受规则，出现用新策略重算旧轨迹、动作依赖基线或越过置信域就撤回结论。

### 8·3·1 算法推导

<Term def="算法推导对应正式目录坐标“8.3.1 算法推导”，在“第8章 带基线的策略梯度方法”中用于把“算法推导”放进基线、优势与方差控制的环境—轨迹—目标—更新链，并受环境、轨迹、策略快照、随机性、评估与版本边界约束。">
  算法推导
</Term>

**正式坐标 10/13。** 原版目录键 `8.3.1 算法推导`。围绕“基线减去什么、是否依赖动作、优势目标在哪里停止梯度？”，“第8章 带基线的策略梯度方法”在坐标10把「8·3·1 算法推导」落实为把“算法推导”放进基线、优势与方差控制的环境—轨迹—目标—更新链；只有第8章 带基线的策略梯度方法的输入角色、中间状态、策略快照、反例与独立评估可重放且反例排除只复述“算法推导”名称而没有可观察状态、单一故障和恢复证据，本节点才算掌握。

### 8·3·2 训练流程

<Term def="训练流程对应正式目录坐标“8.3.2 训练流程”，在“第8章 带基线的策略梯度方法”中用于把“训练流程”放进基线、优势与方差控制的环境—轨迹—目标—更新链，并受环境、轨迹、策略快照、随机性、评估与版本边界约束。">
  训练流程
</Term>

**正式坐标 11/13。** 原版目录键 `8.3.2 训练流程`。“第8章 带基线的策略梯度方法”的目录节点11「8·3·2 训练流程」不能停在术语复述：它要把“训练流程”放进基线、优势与方差控制的环境—轨迹—目标—更新链，交付第8章 带基线的策略梯度方法的输入角色、中间状态、策略快照、反例与独立评估，并把只复述“训练流程”名称而没有可观察状态、单一故障和恢复证据设为单一反事实。

### 8·3·3 用目标网络改进训练

<Term def="用目标网络改进训练对应正式目录坐标“8.3.3 用目标网络改进训练”，在“第8章 带基线的策略梯度方法”中用于构造可复算的TD目标并区分同策略、异策略、采样和目标网络角色，并受环境、轨迹、策略快照、随机性、评估与版本边界约束。">
  用目标网络改进训练
</Term>

**正式坐标 12/13。** 原版目录键 `8.3.3 用目标网络改进训练`。对“第8章 带基线的策略梯度方法”而言，「8·3·3 用目标网络改进训练」在第12次检查中改变可观察状态，因为它负责构造可复算的TD目标并区分同策略、异策略、采样和目标网络角色；回放索引、下一动作、TD目标、误差、优先级和参数快照必须与“行为策略、回报、状态基线、停止梯度与价值目标角色明确分离”对齐，不能接受目标网络泄漏、策略角色错位或多技巧同时变化。

### 8·4 证明带基线的策略梯度定理

<Term def="证明带基线的策略梯度定理对应正式目录坐标“8.4 证明带基线的策略梯度定理”，在“第8章 带基线的策略梯度方法”中用于建立环境、轨迹、终止、策略、回报和价值的时间合同，并受环境、轨迹、策略快照、随机性、评估与版本边界约束。">
  证明带基线的策略梯度定理
</Term>

**正式坐标 13/13。** 原版目录键 `8.4 证明带基线的策略梯度定理`。在“第8章 带基线的策略梯度方法”的第13个正式坐标中，「8·4 证明带基线的策略梯度定理」通过建立环境、轨迹、终止、策略、回报和价值的时间合同推进基线、优势与方差控制；复核者保存转移记录、策略概率、终止/截断标记、回报与价值残差，出现混淆状态与观测、终止与截断或行为策略与目标策略就撤回结论。

## 先预测，再操作三个章专属实验

<Callout type="info" title="先写出哪个状态会最先变化">
  对“第8章
  带基线的策略梯度方法”先冻结在同一轨迹上比较无基线、状态基线和A2C的环境、轨迹、策略/对手快照、预算、随机性和评估口径，再操作三类实验；结果与预测不同就修改假设，不删除失败运行。
</Callout>

<Stepper>
  <Step title="1. 环境与轨迹合同">
    为“第8章 带基线的策略梯度方法”选择正式目录坐标，在参考合同与单一反事实间切换，逐角色核对输入、状态变化、证据与即时裁决。

    <Drl08PolicyGradientBaselineEnvironmentContractLab />

  </Step>
  <Step title="2. 回报、目标与更新轨迹">
    保持“第8章 带基线的策略梯度方法”的在同一轨迹上比较无基线、状态基线和A2C不变，只注入“让基线直接依赖当前动作却仍按无偏基线解释”，逐步定位首个偏离“行为策略、回报、状态基线、停止梯度与价值目标角色明确分离”的位置。

    <Drl08PolicyGradientBaselineReturnUpdateTraceLab />

  </Step>
  <Step title="3. 异策略与多智能体评估门">
    在“第8章 带基线的策略梯度方法”的基线、单故障和恢复案例间切换，展开结构、环境、更新和独立评估门后再决定是否交付。

    <Drl08PolicyGradientBaselineEvaluationGateLab />

  </Step>
</Stepper>

<Callout
  type="trap"
  title="本页单一故障：让基线直接依赖当前动作却仍按无偏基线解释"
>
  “第8章
  带基线的策略梯度方法”遇到该故障时必须保持其余环境、轨迹、策略/对手版本、预算和随机序列不变；训练回报偶尔更高不能替代首个状态分岔与恢复证据。
</Callout>

<Callout type="trap" title="训练回报不等于独立策略价值">
  “第8章
  带基线的策略梯度方法”的TD误差、代理目标、训练回报、搜索值或同代对战只回答各自合同；它们不能自动证明未见种子、冻结对手、真实用户或安全部署上的收益。
</Callout>

<Callout type="trap" title="可访问目录与代码映射不等于复用许可">
  “第8章
  带基线的策略梯度方法”可以用作者仓库和发行版目录核对范围，但未发现可授权本站复制代码、图表或正文的明确许可；当前内容必须保持独立表达、独立技术核对与时间边界。
</Callout>

## 第8章 带基线的策略梯度方法的可重放协议

| 阶段                                    | 允许动作                                                                                           | 必留证据                                                   | 拒绝条件                                                                                         |
| --------------------------------------- | -------------------------------------------------------------------------------------------------- | ---------------------------------------------------------- | ------------------------------------------------------------------------------------------------ |
| 第8章 带基线的策略梯度方法 · 环境与角色 | 冻结基线、优势与方差控制所需的环境版本、观测/状态、动作、奖励和数据角色                            | 第8章 带基线的策略梯度方法的环境合同、策略快照与基线轨迹   | 未满足“第8章 带基线的策略梯度方法的角色、时间索引、shape、终止和可见性没有错位”                  |
| 第8章 带基线的策略梯度方法 · 回报与目标 | 按证明基线不改变期望策略梯度，并比较REINFORCE基线与A2C的方差和自举构造回报、目标、估计量或搜索统计 | 第8章 带基线的策略梯度方法的逐步回报、目标分量与中间状态   | 未满足“第8章 带基线的策略梯度方法的目标可由同一轨迹、公式和随机状态复算”                         |
| 第8章 带基线的策略梯度方法 · 更新与策略 | 执行一次受控更新并记录策略、价值、梯度或联合决策的变化                                             | 第8章 带基线的策略梯度方法的更新前后差、首个分岔和恢复路径 | 未满足“第8章 带基线的策略梯度方法没有把代理损失、单次回报或训练内统计当作最终结论”               |
| 第8章 带基线的策略梯度方法 · 独立评估   | 重放基线、单故障、恢复和边界案例                                                                   | 第8章 带基线的策略梯度方法的接受、回退或拒绝理由           | 未满足“第8章 带基线的策略梯度方法满足“行为策略、回报、状态基线、停止梯度与价值目标角色明确分离”” |

```yaml
unit: "drl-08"
question: "基线减去什么、是否依赖动作、优势目标在哪里停止梯度？"
scenario: "在同一轨迹上比较无基线、状态基线和A2C"
invariant: "行为策略、回报、状态基线、停止梯度与价值目标角色明确分离"
fault: "让基线直接依赖当前动作却仍按无偏基线解释"
evidence: "基线前后梯度均值/方差、优势和价值残差"
reset: restore_concept_mode_stage_trace_step_case_gates_and_artifact
```

“第8章 带基线的策略梯度方法”要求从同一环境、轨迹、策略/对手版本、预算和随机状态重放参考、故障与恢复路径。重置后若目录选择、合同模式、阶段、轨迹步骤、案例、证据门或交付包没有回到基线，本次比较已经混入状态泄漏。

## 本页回顾

掌握“第8章 带基线的策略梯度方法”不是背算法名或抄训练循环，而是围绕“基线减去什么、是否依赖动作、优势目标在哪里停止梯度？”重建环境、轨迹、回报、目标、更新、策略/对手快照与独立评估，并用“行为策略、回报、状态基线、停止梯度与价值目标角色明确分离”拒绝“让基线直接依赖当前动作却仍按无偏基线解释”。最终交付为基线前后梯度均值/方差、优势和价值残差。

## 练习与答案

<Exercises>

1. **问题 1：实验合同。** “第8章 带基线的策略梯度方法”为什么必须先冻结环境、轨迹、策略/对手版本、预算、随机性和评估口径？

<Answer>
  若同时改变这些条件，相同回报可能来自不同状态分布、行为策略、目标、更新或对手；先冻结合同，才能把观测连接到单一机制并定位首差。
</Answer>

2. **问题 2：目录逐项覆盖。** 怎样证明“第8章 带基线的策略梯度方法”的正式目录坐标已经进入机制、交互和练习？

<Answer>
  1. “第8章 带基线的策略梯度方法”的目录项「第8章
  带基线的策略梯度方法」：以“建立环境、轨迹、终止、策略、回报和价值的时间合同”解释作用，用“转移记录、策略概率、终止/截断标记、回报与价值残差”提供复核。
  2. “第8章 带基线的策略梯度方法”的目录项「8·1
  策略梯度中的基线」：以“建立环境、轨迹、终止、策略、回报和价值的时间合同”解释作用，用“转移记录、策略概率、终止/截断标记、回报与价值残差”提供复核。
  3. “第8章 带基线的策略梯度方法”的目录项「8·1·1
  基线的引入」：以“从轨迹概率、回报、优势、概率比与KL约束推导策略更新”解释作用，用“动作对数概率、回报、基线、优势、梯度、KL与接受规则”提供复核。
  4. “第8章 带基线的策略梯度方法”的目录项「8·1·2
  基线的直观解释」：以“从轨迹概率、回报、优势、概率比与KL约束推导策略更新”解释作用，用“动作对数概率、回报、基线、优势、梯度、KL与接受规则”提供复核。
  5. “第8章 带基线的策略梯度方法”的目录项「8·2
  带基线的REINFORCE算法」：以“从轨迹概率、回报、优势、概率比与KL约束推导策略更新”解释作用，用“动作对数概率、回报、基线、优势、梯度、KL与接受规则”提供复核。
  6. “第8章 带基线的策略梯度方法”的目录项「8·2·1
  策略网络和价值网络」：以“建立环境、轨迹、终止、策略、回报和价值的时间合同”解释作用，用“转移记录、策略概率、终止/截断标记、回报与价值残差”提供复核。
  7. “第8章 带基线的策略梯度方法”的目录项「8·2·2
  算法推导」：以“把“算法推导”放进基线、优势与方差控制的环境—轨迹—目标—更新链”解释作用，用“第8章
  带基线的策略梯度方法的输入角色、中间状态、策略快照、反例与独立评估”提供复核。
  8. “第8章 带基线的策略梯度方法”的目录项「8·2·3
  训练流程」：以“把“训练流程”放进基线、优势与方差控制的环境—轨迹—目标—更新链”解释作用，用“第8章
  带基线的策略梯度方法的输入角色、中间状态、策略快照、反例与独立评估”提供复核。
  9. “第8章 带基线的策略梯度方法”的目录项「8·3 advantage
  actor-critic」：以“从轨迹概率、回报、优势、概率比与KL约束推导策略更新”解释作用，用“动作对数概率、回报、基线、优势、梯度、KL与接受规则”提供复核。
  10. “第8章 带基线的策略梯度方法”的目录项「8·3·1
  算法推导」：以“把“算法推导”放进基线、优势与方差控制的环境—轨迹—目标—更新链”解释作用，用“第8章
  带基线的策略梯度方法的输入角色、中间状态、策略快照、反例与独立评估”提供复核。
  11. “第8章 带基线的策略梯度方法”的目录项「8·3·2
  训练流程」：以“把“训练流程”放进基线、优势与方差控制的环境—轨迹—目标—更新链”解释作用，用“第8章
  带基线的策略梯度方法的输入角色、中间状态、策略快照、反例与独立评估”提供复核。
  12. “第8章 带基线的策略梯度方法”的目录项「8·3·3
  用目标网络改进训练」：以“构造可复算的TD目标并区分同策略、异策略、采样和目标网络角色”解释作用，用“回放索引、下一动作、TD目标、误差、优先级和参数快照”提供复核。
  13. “第8章 带基线的策略梯度方法”的目录项「8·4
  证明带基线的策略梯度定理」：以“建立环境、轨迹、终止、策略、回报和价值的时间合同”解释作用，用“转移记录、策略概率、终止/截断标记、回报与价值残差”提供复核。
</Answer>

3. **问题 3：故障恢复。** 怎样证明“让基线直接依赖当前动作却仍按无偏基线解释”已经被修正？

<Answer>
  为“第8章
  带基线的策略梯度方法”复用同一环境、轨迹、策略/对手快照、预算和随机流，重放参考路径后只注入该故障；记录首个偏离，撤销故障再运行。只有环境合同、更新轨迹、独立评估和基线前后梯度均值/方差、优势和价值残差重新满足“行为策略、回报、状态基线、停止梯度与价值目标角色明确分离”，修正才可提交。
</Answer>

</Exercises>

## 名词解释

<Glossary>
  <GlossaryItem term="带基线的策略梯度方法">
    检索键 drl-A 对应正式目录坐标「第8章 带基线的策略梯度方法」；在“第8章
    带基线的策略梯度方法”中用于建立环境、轨迹、终止、策略、回报和价值的时间合同，需要连接结构范围、轨迹状态、更新证据、独立评估与不适用边界。
  </GlossaryItem>
  <GlossaryItem term="策略梯度中的基线">
    检索键 drl-B 对应正式目录坐标「8·1 策略梯度中的基线」；在“第8章
    带基线的策略梯度方法”中用于建立环境、轨迹、终止、策略、回报和价值的时间合同，需要连接结构范围、轨迹状态、更新证据、独立评估与不适用边界。
  </GlossaryItem>
  <GlossaryItem term="基线的引入">
    检索键 drl-C 对应正式目录坐标「8·1·1 基线的引入」；在“第8章
    带基线的策略梯度方法”中用于从轨迹概率、回报、优势、概率比与KL约束推导策略更新，需要连接结构范围、轨迹状态、更新证据、独立评估与不适用边界。
  </GlossaryItem>
  <GlossaryItem term="基线的直观解释">
    检索键 drl-D 对应正式目录坐标「8·1·2 基线的直观解释」；在“第8章
    带基线的策略梯度方法”中用于从轨迹概率、回报、优势、概率比与KL约束推导策略更新，需要连接结构范围、轨迹状态、更新证据、独立评估与不适用边界。
  </GlossaryItem>
  <GlossaryItem term="带基线的REINFORCE算法">
    检索键 drl-E 对应正式目录坐标「8·2 带基线的REINFORCE算法」；在“第8章
    带基线的策略梯度方法”中用于从轨迹概率、回报、优势、概率比与KL约束推导策略更新，需要连接结构范围、轨迹状态、更新证据、独立评估与不适用边界。
  </GlossaryItem>
  <GlossaryItem term="策略网络和价值网络">
    检索键 drl-F 对应正式目录坐标「8·2·1 策略网络和价值网络」；在“第8章
    带基线的策略梯度方法”中用于建立环境、轨迹、终止、策略、回报和价值的时间合同，需要连接结构范围、轨迹状态、更新证据、独立评估与不适用边界。
  </GlossaryItem>
  <GlossaryItem term="算法推导">
    检索键 drl-G 对应正式目录坐标「8·2·2 算法推导」；在“第8章
    带基线的策略梯度方法”中用于把“算法推导”放进基线、优势与方差控制的环境—轨迹—目标—更新链，需要连接结构范围、轨迹状态、更新证据、独立评估与不适用边界。
  </GlossaryItem>
  <GlossaryItem term="训练流程">
    检索键 drl-H 对应正式目录坐标「8·2·3 训练流程」；在“第8章
    带基线的策略梯度方法”中用于把“训练流程”放进基线、优势与方差控制的环境—轨迹—目标—更新链，需要连接结构范围、轨迹状态、更新证据、独立评估与不适用边界。
  </GlossaryItem>
  <GlossaryItem term="advantage actor-critic">
    检索键 drl-I 对应正式目录坐标「8·3 advantage actor-critic」；在“第8章
    带基线的策略梯度方法”中用于从轨迹概率、回报、优势、概率比与KL约束推导策略更新，需要连接结构范围、轨迹状态、更新证据、独立评估与不适用边界。
  </GlossaryItem>
  <GlossaryItem term="算法推导">
    检索键 drl-J 对应正式目录坐标「8·3·1 算法推导」；在“第8章
    带基线的策略梯度方法”中用于把“算法推导”放进基线、优势与方差控制的环境—轨迹—目标—更新链，需要连接结构范围、轨迹状态、更新证据、独立评估与不适用边界。
  </GlossaryItem>
  <GlossaryItem term="训练流程">
    检索键 drl-K 对应正式目录坐标「8·3·2 训练流程」；在“第8章
    带基线的策略梯度方法”中用于把“训练流程”放进基线、优势与方差控制的环境—轨迹—目标—更新链，需要连接结构范围、轨迹状态、更新证据、独立评估与不适用边界。
  </GlossaryItem>
  <GlossaryItem term="用目标网络改进训练">
    检索键 drl-L 对应正式目录坐标「8·3·3 用目标网络改进训练」；在“第8章
    带基线的策略梯度方法”中用于构造可复算的TD目标并区分同策略、异策略、采样和目标网络角色，需要连接结构范围、轨迹状态、更新证据、独立评估与不适用边界。
  </GlossaryItem>
  <GlossaryItem term="证明带基线的策略梯度定理">
    检索键 drl-M 对应正式目录坐标「8·4 证明带基线的策略梯度定理」；在“第8章
    带基线的策略梯度方法”中用于建立环境、轨迹、终止、策略、回报和价值的时间合同，需要连接结构范围、轨迹状态、更新证据、独立评估与不适用边界。
  </GlossaryItem>
</Glossary>

<Attribution
  mode="independent-rewrite"
  sourceBasis="author-official-code-map-plus-issued-edition-detailed-toc"
  workTitle="王树森、黎彧君、张志华著《深度强化学习》"
  adaptedUrl="https://github.com/DeepRLChinese/DeepRL-Chinese"
/>
