---
title: "第8章 带基线的策略梯度方法"
type: C
section: "第8章 带基线的策略梯度方法"
order: 8
description: "第8章 带基线的策略梯度方法逐项覆盖权威目录，以公式、轨迹、交互实验和失败对照重建深度强化学习证据链。"
demo: true
math: true
sourceUrl: "https://github.com/DeepRLChinese/DeepRL-Chinese"
draft: true
---

import { Objectives, Callout, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step, Attribution } from "@/components/mdx/mdx-components";

<Objectives>

- 能解释基线、优势函数、方差缩减在本章中的输入、输出与参数版本
- 能推导并计算固定轨迹上的四个核心方程，将每一项映射到实现状态
- 能实现并运行三种交互视图，定位终止、采样、目标或策略版本的首个证据分叉
- 能设计正常样本和单一故障样本，证明“枚举一个状态的全部动作，验证基线项期望梯度为零，并分别记录actor与critic损失、梯度和参数版本。”

</Objectives>

## 为什么先冻结环境、轨迹与参数版本

<Term def="基线是第8章 带基线的策略梯度方法的第1个可执行证据坐标。">基线</Term>、<Term def="优势函数是第8章 带基线的策略梯度方法的第2个可执行证据坐标。">优势函数</Term>、<Term def="方差缩减是第8章 带基线的策略梯度方法的第3个可执行证据坐标。">方差缩减</Term>、<Term def="REINFORCE是第8章 带基线的策略梯度方法的第4个可执行证据坐标。">REINFORCE</Term>、<Term def="A2C是第8章 带基线的策略梯度方法的第5个可执行证据坐标。">A2C</Term>、<Term def="目标网络是第8章 带基线的策略梯度方法的第6个可执行证据坐标。">目标网络</Term>共同构成本页坐标。证明只依赖状态的基线不改变策略梯度期望，再用价值网络构造优势信号，降低REINFORCE的方差。

阅读时把环境状态、采样数据、学习目标、优化参数和评价策略分成五份账。每一条转移至少保存状态、动作、奖励、下一状态、终止与截断标记、行为概率和采集时的参数版本；每一次更新保存目标值、损失、梯度范数、更新前后参数摘要和随机种子。这样才能判断改进来自正确算法，而不是信息泄漏、状态污染或评价仍在学习。

<Callout type="info">
  **验收命题：** 枚举一个状态的全部动作，验证基线项期望梯度为零，并分别记录actor与critic损失、梯度和参数版本。
</Callout>

## 数学骨架

$$
A^\pi(s,a)=Q^\pi(s,a)-V^\pi(s)
$$

$$
\mathbb E_{a\sim\pi}[b(s)\nabla\log\pi(a|s)]=0
$$

$$
\widehat g=\sum_t(G_t-V_\phi(S_t))\nabla\log\pi_\theta(A_t|S_t)
$$

$$
L_V=\frac12(G_t-V_\phi(S_t))^2
$$

公式必须落实为shape与版本合同。奖励是标量，动作可以是离散索引或连续向量，终止转移的未来项固定为零；行为策略负责产生数据，目标策略负责定义学习目标，在线参数与冻结参数不能靠变量名猜测。先用一个两状态、两动作或两智能体样例手算，再运行批量训练。

## 最小实现骨架

```python
def record_transition(env, policy, observation):
    action, logp, policy_version = policy.sample(observation)
    next_observation, reward, terminated, truncated, info = env.step(action)
    return {
        "observation": observation,
        "action": action,
        "reward": float(reward),
        "next_observation": next_observation,
        "terminated": bool(terminated),
        "truncated": bool(truncated),
        "behavior_logp": float(logp),
        "policy_version": policy_version,
    }
```

```python
def one_step_target(transition, bootstrap_value, gamma):
    future = 0.0 if transition["terminated"] else bootstrap_value
    return transition["reward"] + gamma * future
```

```python
def audit_update(before, target, loss, gradient, after):
    assert target.isfinite().all()
    assert loss.isfinite().all()
    assert gradient.isfinite().all()
    return {"before": before.digest(), "target": target.detach(),
            "grad_norm": gradient.norm(), "after": after.digest()}
```

这三段代码不替代具体算法，而是固定共同边界：环境只产生事实，目标函数只读取冻结转移，优化器只在审计通过后更新。章节中的DQN、SARSA、策略梯度、A3C、MADDPG或MCTS都必须映射回这套生命周期。

## 常见失败与回退

<Callout type="trap">
  **本章高风险失败：** 基线依赖当前动作却未做修正、优势未停止梯度，或共享网络时把价值损失权重设得过大淹没策略信号。
</Callout>

<Callout type="trap">
  **最终曲线不是充分证据：** 奖励上升可能与错误终止、评价继续训练、数据泄漏、错误动作缩放或挑选随机种子同时出现。
</Callout>

<Callout type="tip">
  **回退顺序：** 环境转移 → 轨迹边界 → 手算目标 → 前向输出 → 梯度 → 参数更新 → 冻结评价。第一处不一致未消除前，不增加网络、worker或训练步数。
</Callout>

## 本章回顾

证明只依赖状态的基线不改变策略梯度期望，再用价值网络构造优势信号，降低REINFORCE的方差。 最终掌握标准不是复述算法名，而是能说明每个量来自哪里、由哪版策略产生、在哪个边界归零、如何手算，以及故障样本应在哪一步被发现。通过标准是：枚举一个状态的全部动作，验证基线项期望梯度为零，并分别记录actor与critic损失、梯度和参数版本。

<Glossary>

<GlossaryItem term="基线">
  “基线”是本页证据链第1站。记录定义、输入来源、参数版本、手算中间值、数值容差和失败边界；只看到最终回报不能证明这一站正确。
</GlossaryItem>

<GlossaryItem term="优势函数">
  “优势函数”是本页证据链第2站。记录定义、输入来源、参数版本、手算中间值、数值容差和失败边界；只看到最终回报不能证明这一站正确。
</GlossaryItem>

<GlossaryItem term="方差缩减">
  “方差缩减”是本页证据链第3站。记录定义、输入来源、参数版本、手算中间值、数值容差和失败边界；只看到最终回报不能证明这一站正确。
</GlossaryItem>

<GlossaryItem term="REINFORCE">
  “REINFORCE”是本页证据链第4站。记录定义、输入来源、参数版本、手算中间值、数值容差和失败边界；只看到最终回报不能证明这一站正确。
</GlossaryItem>

<GlossaryItem term="A2C">
  “A2C”是本页证据链第5站。记录定义、输入来源、参数版本、手算中间值、数值容差和失败边界；只看到最终回报不能证明这一站正确。
</GlossaryItem>

<GlossaryItem term="目标网络">
  “目标网络”是本页证据链第6站。记录定义、输入来源、参数版本、手算中间值、数值容差和失败边界；只看到最终回报不能证明这一站正确。
</GlossaryItem>

</Glossary>

<Exercises>

1. 如何为第8章 带基线的策略梯度方法建立最小可手算样例？

<Answer>
  只保留两个状态、两个动作、两步轨迹或两个智能体，固定奖励、折扣、参数和随机种子；先写预期中间值，再运行一次更新并比较状态、目标、梯度与参数摘要。
</Answer>

2. 如何证明“枚举一个状态的全部动作，验证基线项期望梯度为零，并分别记录actor与critic损失、梯度和参数版本。”不是一次高回报偶然满足？

<Answer>
  用同一输入并排运行正常路径与只破坏一个终止标记、策略版本、采样概率、动作边界或梯度符号的路径。检测器必须在预期首个中间量区分两者，并在独立评价中保持结论。
</Answer>

3. 修复第8章 带基线的策略梯度方法后按什么顺序回归？

<Answer>
  先验证环境与轨迹，再核对目标与前向，然后核对梯度和更新，最后冻结策略做多随机种子评价；任何早期证据失败都停止后续长训练。
</Answer>

</Exercises>

## 前后导航

- [第7章 策略梯度方法](/learn/deep-reinforcement-learning/03-policy-learning/drl-07-policy-gradient)
- [第9章 策略学习高级技巧](/learn/deep-reinforcement-learning/03-policy-learning/drl-09-advanced-policy-learning)

<Attribution adaptedFrom="王树森、黎彧君、张志华著《深度强化学习》，人民邮电出版社，2022年11月，294页，ISBN 9787115600691 · 基于5部分、19章、233个编号节/小节和2个附录完整目录的独立教学重构" adaptedUrl="https://github.com/DeepRLChinese/DeepRL-Chinese" />
