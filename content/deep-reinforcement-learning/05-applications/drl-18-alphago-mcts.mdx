---
title: "第18章 AlphaGo与蒙特卡洛树搜索"
type: C
section: "第18章 AlphaGo与蒙特卡洛树搜索"
order: 18
description: "第18章 AlphaGo与蒙特卡洛树搜索逐项覆盖权威目录，以公式、轨迹、交互实验和失败对照重建深度强化学习证据链。"
demo: true
math: true
sourceUrl: "https://github.com/DeepRLChinese/DeepRL-Chinese"
draft: true
---

import { Objectives, Callout, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step, Attribution } from "@/components/mdx/mdx-components";

<Objectives>

- 能解释围棋状态、MCTS、选择在本章中的输入、输出与参数版本
- 能推导并计算固定轨迹上的四个核心方程，将每一项映射到实现状态
- 能实现并运行三种交互视图，定位终止、采样、目标或策略版本的首个证据分叉
- 能设计正常样本和单一故障样本，证明“在小型棋盘记录每个节点的访问次数、先验、均值和探索项；固定网络输出后手算一次完整MCTS模拟。”

</Objectives>

## 为什么先冻结环境、轨迹与参数版本

<Term def="围棋状态是第18章 AlphaGo与蒙特卡洛树搜索的第1个可执行证据坐标。">围棋状态</Term>、<Term def="MCTS是第18章 AlphaGo与蒙特卡洛树搜索的第2个可执行证据坐标。">MCTS</Term>、<Term def="选择是第18章 AlphaGo与蒙特卡洛树搜索的第3个可执行证据坐标。">选择</Term>、<Term def="扩展是第18章 AlphaGo与蒙特卡洛树搜索的第4个可执行证据坐标。">扩展</Term>、<Term def="评估是第18章 AlphaGo与蒙特卡洛树搜索的第5个可执行证据坐标。">评估</Term>、<Term def="回传是第18章 AlphaGo与蒙特卡洛树搜索的第6个可执行证据坐标。">回传</Term>共同构成本页坐标。把策略先验、价值估计与树搜索结合，逐步解释选择、扩展、评估和回传，并比较监督预训练与纯自博弈。

阅读时把环境状态、采样数据、学习目标、优化参数和评价策略分成五份账。每一条转移至少保存状态、动作、奖励、下一状态、终止与截断标记、行为概率和采集时的参数版本；每一次更新保存目标值、损失、梯度范数、更新前后参数摘要和随机种子。这样才能判断改进来自正确算法，而不是信息泄漏、状态污染或评价仍在学习。

<Callout type="info">
  **验收命题：** 在小型棋盘记录每个节点的访问次数、先验、均值和探索项；固定网络输出后手算一次完整MCTS模拟。
</Callout>

## 数学骨架

$$
a^*=\arg\max_a[Q(s,a)+cP(s,a)\frac{\sqrt{N(s)}}{1+N(s,a)}]
$$

$$
Q(s,a)=W(s,a)/N(s,a)
$$

$$
\pi(a|s)=N(s,a)^{1/\tau}/\sum_bN(s,b)^{1/\tau}
$$

$$
L=(z-v_\theta(s))^2-\pi^\top\log p_\theta(\cdot|s)+c\|\theta\|^2
$$

公式必须落实为shape与版本合同。奖励是标量，动作可以是离散索引或连续向量，终止转移的未来项固定为零；行为策略负责产生数据，目标策略负责定义学习目标，在线参数与冻结参数不能靠变量名猜测。先用一个两状态、两动作或两智能体样例手算，再运行批量训练。

## 最小实现骨架

```python
def record_transition(env, policy, observation):
    action, logp, policy_version = policy.sample(observation)
    next_observation, reward, terminated, truncated, info = env.step(action)
    return {
        "observation": observation,
        "action": action,
        "reward": float(reward),
        "next_observation": next_observation,
        "terminated": bool(terminated),
        "truncated": bool(truncated),
        "behavior_logp": float(logp),
        "policy_version": policy_version,
    }
```

```python
def one_step_target(transition, bootstrap_value, gamma):
    future = 0.0 if transition["terminated"] else bootstrap_value
    return transition["reward"] + gamma * future
```

```python
def audit_update(before, target, loss, gradient, after):
    assert target.isfinite().all()
    assert loss.isfinite().all()
    assert gradient.isfinite().all()
    return {"before": before.digest(), "target": target.detach(),
            "grad_norm": gradient.norm(), "after": after.digest()}
```

这三段代码不替代具体算法，而是固定共同边界：环境只产生事实，目标函数只读取冻结转移，优化器只在审计通过后更新。章节中的DQN、SARSA、策略梯度、A3C、MADDPG或MCTS都必须映射回这套生命周期。

## 常见失败与回退

<Callout type="trap">
  **本章高风险失败：** 决策时按Q值而非访问次数选子、回传时玩家视角符号错误，或训练目标读取搜索后的未来信息。
</Callout>

<Callout type="trap">
  **最终曲线不是充分证据：** 奖励上升可能与错误终止、评价继续训练、数据泄漏、错误动作缩放或挑选随机种子同时出现。
</Callout>

<Callout type="tip">
  **回退顺序：** 环境转移 → 轨迹边界 → 手算目标 → 前向输出 → 梯度 → 参数更新 → 冻结评价。第一处不一致未消除前，不增加网络、worker或训练步数。
</Callout>

## 本章回顾

把策略先验、价值估计与树搜索结合，逐步解释选择、扩展、评估和回传，并比较监督预训练与纯自博弈。 最终掌握标准不是复述算法名，而是能说明每个量来自哪里、由哪版策略产生、在哪个边界归零、如何手算，以及故障样本应在哪一步被发现。通过标准是：在小型棋盘记录每个节点的访问次数、先验、均值和探索项；固定网络输出后手算一次完整MCTS模拟。

<Glossary>

<GlossaryItem term="围棋状态">
  “围棋状态”是本页证据链第1站。记录定义、输入来源、参数版本、手算中间值、数值容差和失败边界；只看到最终回报不能证明这一站正确。
</GlossaryItem>

<GlossaryItem term="MCTS">
  “MCTS”是本页证据链第2站。记录定义、输入来源、参数版本、手算中间值、数值容差和失败边界；只看到最终回报不能证明这一站正确。
</GlossaryItem>

<GlossaryItem term="选择">
  “选择”是本页证据链第3站。记录定义、输入来源、参数版本、手算中间值、数值容差和失败边界；只看到最终回报不能证明这一站正确。
</GlossaryItem>

<GlossaryItem term="扩展">
  “扩展”是本页证据链第4站。记录定义、输入来源、参数版本、手算中间值、数值容差和失败边界；只看到最终回报不能证明这一站正确。
</GlossaryItem>

<GlossaryItem term="评估">
  “评估”是本页证据链第5站。记录定义、输入来源、参数版本、手算中间值、数值容差和失败边界；只看到最终回报不能证明这一站正确。
</GlossaryItem>

<GlossaryItem term="回传">
  “回传”是本页证据链第6站。记录定义、输入来源、参数版本、手算中间值、数值容差和失败边界；只看到最终回报不能证明这一站正确。
</GlossaryItem>

</Glossary>

<Exercises>

1. 如何为第18章 AlphaGo与蒙特卡洛树搜索建立最小可手算样例？

<Answer>
  只保留两个状态、两个动作、两步轨迹或两个智能体，固定奖励、折扣、参数和随机种子；先写预期中间值，再运行一次更新并比较状态、目标、梯度与参数摘要。
</Answer>

2. 如何证明“在小型棋盘记录每个节点的访问次数、先验、均值和探索项；固定网络输出后手算一次完整MCTS模拟。”不是一次高回报偶然满足？

<Answer>
  用同一输入并排运行正常路径与只破坏一个终止标记、策略版本、采样概率、动作边界或梯度符号的路径。检测器必须在预期首个中间量区分两者，并在独立评价中保持结论。
</Answer>

3. 修复第18章 AlphaGo与蒙特卡洛树搜索后按什么顺序回归？

<Answer>
  先验证环境与轨迹，再核对目标与前向，然后核对梯度和更新，最后冻结策略做多随机种子评价；任何早期证据失败都停止后续长训练。
</Answer>

</Exercises>

## 前后导航

- [第17章 注意力机制与多智能体强化学习](/learn/deep-reinforcement-learning/04-multi-agent/drl-17-attention-marl)
- [第19章 现实世界中的应用](/learn/deep-reinforcement-learning/05-applications/drl-19-real-world-applications)

<Attribution adaptedFrom="王树森、黎彧君、张志华著《深度强化学习》，人民邮电出版社，2022年11月，294页，ISBN 9787115600691 · 基于5部分、19章、233个编号节/小节和2个附录完整目录的独立教学重构" adaptedUrl="https://github.com/DeepRLChinese/DeepRL-Chinese" />
