---
title: "《深度强化学习》全书总复习"
type: D
section: "《深度强化学习》全书总复习"
order: 22
description: "《深度强化学习》全书总复习逐项覆盖权威目录，以公式、轨迹、交互实验和失败对照重建深度强化学习证据链。"
demo: true
math: true
sourceUrl: "https://github.com/DeepRLChinese/DeepRL-Chinese"
draft: false
---

import { DrlOfficialFinalReviewMapLab, DrlOfficialFinalReviewTraceLab, DrlOfficialFinalReviewAuditLab } from "@/components/mdx/deep-reinforcement-learning/diagrams/drl-official-final-review";
import { Objectives, Callout, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step, Attribution } from "@/components/mdx/mdx-components";

<Objectives>

- 能解释环境合同、采样证据、价值目标在本章中的输入、输出与参数版本
- 能推导并计算固定轨迹上的四个核心方程，将每一项映射到实现状态
- 能实现并运行三种交互视图，定位终止、采样、目标或策略版本的首个证据分叉
- 能设计正常样本和单一故障样本，证明“随机抽取价值学习、策略学习、多智能体和应用各一章，均能手算首个目标、复现实验、注入单一故障并定位第一处证据分叉。”

</Objectives>

## 为什么先冻结环境、轨迹与参数版本

<Term def="环境合同是《深度强化学习》全书总复习的第1个可执行证据坐标。">环境合同</Term>、<Term def="采样证据是《深度强化学习》全书总复习的第2个可执行证据坐标。">采样证据</Term>、<Term def="价值目标是《深度强化学习》全书总复习的第3个可执行证据坐标。">价值目标</Term>、<Term def="策略梯度是《深度强化学习》全书总复习的第4个可执行证据坐标。">策略梯度</Term>、<Term def="多智能体是《深度强化学习》全书总复习的第5个可执行证据坐标。">多智能体</Term>、<Term def="独立评价是《深度强化学习》全书总复习的第6个可执行证据坐标。">独立评价</Term>共同构成本页坐标。用环境、数据、目标、优化、策略版本和评价六本账串联十九章，要求每个算法都能回到同一条可重放轨迹。

阅读时把环境状态、采样数据、学习目标、优化参数和评价策略分成五份账。每一条转移至少保存状态、动作、奖励、下一状态、终止与截断标记、行为概率和采集时的参数版本；每一次更新保存目标值、损失、梯度范数、更新前后参数摘要和随机种子。这样才能判断改进来自正确算法，而不是信息泄漏、状态污染或评价仍在学习。

<Callout type="info">
  **验收命题：** 随机抽取价值学习、策略学习、多智能体和应用各一章，均能手算首个目标、复现实验、注入单一故障并定位第一处证据分叉。
</Callout>

## 数学骨架

$$
\tau=(S_0,A_0,R_1,\ldots,S_T)
$$

$$
y_t=R_{t+1}+\gamma(1-d_t)\operatorname{bootstrap}(S_{t+1})
$$

$$
\theta_{k+1}=\operatorname{update}(\theta_k,D_k,y_k)
$$

$$
\text{claim}\Rightarrow\text{trace}+\text{counterexample}+\text{replay}
$$

公式必须落实为shape与版本合同。奖励是标量，动作可以是离散索引或连续向量，终止转移的未来项固定为零；行为策略负责产生数据，目标策略负责定义学习目标，在线参数与冻结参数不能靠变量名猜测。先用一个两状态、两动作或两智能体样例手算，再运行批量训练。

## 最小实现骨架

```python
def record_transition(env, policy, observation):
    action, logp, policy_version = policy.sample(observation)
    next_observation, reward, terminated, truncated, info = env.step(action)
    return {
        "observation": observation,
        "action": action,
        "reward": float(reward),
        "next_observation": next_observation,
        "terminated": bool(terminated),
        "truncated": bool(truncated),
        "behavior_logp": float(logp),
        "policy_version": policy_version,
    }
```

```python
def one_step_target(transition, bootstrap_value, gamma):
    future = 0.0 if transition["terminated"] else bootstrap_value
    return transition["reward"] + gamma * future
```

```python
def audit_update(before, target, loss, gradient, after):
    assert target.isfinite().all()
    assert loss.isfinite().all()
    assert gradient.isfinite().all()
    return {"before": before.digest(), "target": target.detach(),
            "grad_norm": gradient.norm(), "after": after.digest()}
```

这三段代码不替代具体算法，而是固定共同边界：环境只产生事实，目标函数只读取冻结转移，优化器只在审计通过后更新。章节中的DQN、SARSA、策略梯度、A3C、MADDPG或MCTS都必须映射回这套生命周期。

## 固定轨迹手算实验

取一条完全冻结的转移：当前状态为s0，动作是a0，即时奖励为2，下一状态为s1，折扣率为0.9，更新前的下一状态估计为4，当前预测为3。若该转移没有终止，一步目标是2加0.9乘4，等于5.6；TD误差是5.6减3，等于2.6。学习率取0.1时，表格预测更新为3.26。若把同一转移改成真正终止，目标必须退化为2，误差变成负1，未来估计4不得进入目标。这个成对样本能直接检出终止掩码是否放错位置。

随后只改变行为策略版本：旧策略对a0的概率为0.4，新策略为0.5，重要性比率应为1.25。数据本身的奖励和终止事实不变，只有依赖策略分布的校正项变化；若价值目标也随当前策略参数无条件漂移，说明实现混入了错误依赖。再把动作换为非法边界值，环境层应在产生学习目标前拒绝，而不是靠损失函数事后吸收。

最后从全新进程独立重放：恢复相同环境、参数快照、轨迹与随机数序列，首个目标、首个梯度和更新后摘要应逐项一致。浮点并行实现可使用预先声明的相对与绝对容差，但不能放宽到足以掩盖符号、终止或策略版本错误。完成这组三层对照后，才扩大批量、网络规模、worker数量或搜索次数。

还可以把两个非终止状态的策略评价写成线性方程组。先固定策略和转移矩阵P，再列出向量方程V等于r加gamma乘P乘V；直接求解得到基准值，同时从零向量开始反复应用贝尔曼算子。每轮保存无穷范数残差、相邻迭代差和状态次序，迭代结果应在容差内接近直接解。交换状态编号后若数值语义改变，说明矩阵行列或奖励索引接错；把某状态改成终止后若该行仍保留未来价值，说明终止边界没有进入算子。这个小实验同时核对公式、数据布局、收敛条件和实现版本。

验收记录还应写明折扣率小于1时压缩映射提供的收敛保证，以及无折扣持续任务为何需要额外条件。不要把一次残差下降当成普遍收敛证明；至少比较不同初值、不同状态排列和解析基准，并保留达到容差所需的迭代次数。

在提交结果前，再用独立脚本读取保存的转移表并重新组装P与r，不复用训练代码中的矩阵对象。两条实现路径应得到同一方程残差；若只有共享对象的路径通过，就仍然无法排除原地更新、别名引用或索引顺序造成的共同错误。

## 权威目录逐项讲解

### 《深度强化学习》全书总复习

“《深度强化学习》全书总复习”在本章中承担从输入合同到采样证据的连接作用，需要同时保留定义、计算和验证边界。 将它放回本章主线“用环境、数据、目标、优化、策略版本和评价六本账串联十九章，要求每个算法都能回到同一条可重放轨迹。”中，最小证据应记录输入状态、动作或样本来源，手算首个中间量，再与实现输出逐项比对。

针对“《深度强化学习》全书总复习”，先写出该项使用的状态、动作、随机变量或参数版本，再回答它如何改变目标量。正常路径之外增加一个只破坏终止标记、策略版本、shape、采样概率或梯度符号的失败样本；检测器应在最终回报变化之前定位首个分叉。

### 第1章 机器学习基础

答案应重建该章的已知条件、推导或程序状态，给出中间值、容差和反例，而不是只复述最终结论。 将它放回本章主线“用环境、数据、目标、优化、策略版本和评价六本账串联十九章，要求每个算法都能回到同一条可重放轨迹。”中，最小证据应记录输入状态、动作或样本来源，手算首个中间量，再与实现输出逐项比对。

针对“第1章 机器学习基础”，先写出该项使用的状态、动作、随机变量或参数版本，再回答它如何改变目标量。正常路径之外增加一个只破坏终止标记、策略版本、shape、采样概率或梯度符号的失败样本；检测器应在最终回报变化之前定位首个分叉。

### 第2章 蒙特卡洛方法

答案应重建该章的已知条件、推导或程序状态，给出中间值、容差和反例，而不是只复述最终结论。 将它放回本章主线“用环境、数据、目标、优化、策略版本和评价六本账串联十九章，要求每个算法都能回到同一条可重放轨迹。”中，最小证据应记录输入状态、动作或样本来源，手算首个中间量，再与实现输出逐项比对。

针对“第2章 蒙特卡洛方法”，先写出该项使用的状态、动作、随机变量或参数版本，再回答它如何改变目标量。正常路径之外增加一个只破坏终止标记、策略版本、shape、采样概率或梯度符号的失败样本；检测器应在最终回报变化之前定位首个分叉。

### 第3章 强化学习基本概念

答案应重建该章的已知条件、推导或程序状态，给出中间值、容差和反例，而不是只复述最终结论。 将它放回本章主线“用环境、数据、目标、优化、策略版本和评价六本账串联十九章，要求每个算法都能回到同一条可重放轨迹。”中，最小证据应记录输入状态、动作或样本来源，手算首个中间量，再与实现输出逐项比对。

针对“第3章 强化学习基本概念”，先写出该项使用的状态、动作、随机变量或参数版本，再回答它如何改变目标量。正常路径之外增加一个只破坏终止标记、策略版本、shape、采样概率或梯度符号的失败样本；检测器应在最终回报变化之前定位首个分叉。

### 第4章 DQN与Q学习

用神经网络一次输出各动作Q值，把TD目标视为监督标签；本章先推导基本形式，高级稳定技巧留到第6章。 将它放回本章主线“用环境、数据、目标、优化、策略版本和评价六本账串联十九章，要求每个算法都能回到同一条可重放轨迹。”中，最小证据应记录输入状态、动作或样本来源，手算首个中间量，再与实现输出逐项比对。

针对“第4章 DQN与Q学习”，先写出该项使用的状态、动作、随机变量或参数版本，再回答它如何改变目标量。正常路径之外增加一个只破坏终止标记、策略版本、shape、采样概率或梯度符号的失败样本；检测器应在最终回报变化之前定位首个分叉。

### 第5章 SARSA算法

目标中的下一动作来自行为策略，名称对应状态、动作、奖励、下一状态、下一动作五元组，体现同策略学习。 将它放回本章主线“用环境、数据、目标、优化、策略版本和评价六本账串联十九章，要求每个算法都能回到同一条可重放轨迹。”中，最小证据应记录输入状态、动作或样本来源，手算首个中间量，再与实现输出逐项比对。

针对“第5章 SARSA算法”，先写出该项使用的状态、动作、随机变量或参数版本，再回答它如何改变目标量。正常路径之外增加一个只破坏终止标记、策略版本、shape、采样概率或梯度符号的失败样本；检测器应在最终回报变化之前定位首个分叉。

### 第6章 价值学习高级技巧

答案应重建该章的已知条件、推导或程序状态，给出中间值、容差和反例，而不是只复述最终结论。 将它放回本章主线“用环境、数据、目标、优化、策略版本和评价六本账串联十九章，要求每个算法都能回到同一条可重放轨迹。”中，最小证据应记录输入状态、动作或样本来源，手算首个中间量，再与实现输出逐项比对。

针对“第6章 价值学习高级技巧”，先写出该项使用的状态、动作、随机变量或参数版本，再回答它如何改变目标量。正常路径之外增加一个只破坏终止标记、策略版本、shape、采样概率或梯度符号的失败样本；检测器应在最终回报变化之前定位首个分叉。

### 第7章 策略梯度方法

答案应重建该章的已知条件、推导或程序状态，给出中间值、容差和反例，而不是只复述最终结论。 将它放回本章主线“用环境、数据、目标、优化、策略版本和评价六本账串联十九章，要求每个算法都能回到同一条可重放轨迹。”中，最小证据应记录输入状态、动作或样本来源，手算首个中间量，再与实现输出逐项比对。

针对“第7章 策略梯度方法”，先写出该项使用的状态、动作、随机变量或参数版本，再回答它如何改变目标量。正常路径之外增加一个只破坏终止标记、策略版本、shape、采样概率或梯度符号的失败样本；检测器应在最终回报变化之前定位首个分叉。

### 第8章 带基线的策略梯度方法

从回报减去只依赖状态的基线不会改变期望梯度，却能显著降低方差；依赖动作的基线通常会引入偏差。 将它放回本章主线“用环境、数据、目标、优化、策略版本和评价六本账串联十九章，要求每个算法都能回到同一条可重放轨迹。”中，最小证据应记录输入状态、动作或样本来源，手算首个中间量，再与实现输出逐项比对。

针对“第8章 带基线的策略梯度方法”，先写出该项使用的状态、动作、随机变量或参数版本，再回答它如何改变目标量。正常路径之外增加一个只破坏终止标记、策略版本、shape、采样概率或梯度符号的失败样本；检测器应在最终回报变化之前定位首个分叉。

### 第9章 策略学习高级技巧

答案应重建该章的已知条件、推导或程序状态，给出中间值、容差和反例，而不是只复述最终结论。 将它放回本章主线“用环境、数据、目标、优化、策略版本和评价六本账串联十九章，要求每个算法都能回到同一条可重放轨迹。”中，最小证据应记录输入状态、动作或样本来源，手算首个中间量，再与实现输出逐项比对。

针对“第9章 策略学习高级技巧”，先写出该项使用的状态、动作、随机变量或参数版本，再回答它如何改变目标量。正常路径之外增加一个只破坏终止标记、策略版本、shape、采样概率或梯度符号的失败样本；检测器应在最终回报变化之前定位首个分叉。

### 第10章 连续控制

答案应重建该章的已知条件、推导或程序状态，给出中间值、容差和反例，而不是只复述最终结论。 将它放回本章主线“用环境、数据、目标、优化、策略版本和评价六本账串联十九章，要求每个算法都能回到同一条可重放轨迹。”中，最小证据应记录输入状态、动作或样本来源，手算首个中间量，再与实现输出逐项比对。

针对“第10章 连续控制”，先写出该项使用的状态、动作、随机变量或参数版本，再回答它如何改变目标量。正常路径之外增加一个只破坏终止标记、策略版本、shape、采样概率或梯度符号的失败样本；检测器应在最终回报变化之前定位首个分叉。

### 第11章 对状态的不完全观测

隐藏状态压缩历史以弥补单帧观测缺失，训练批次必须保留时间顺序、初始隐藏状态和有效位置掩码。 将它放回本章主线“用环境、数据、目标、优化、策略版本和评价六本账串联十九章，要求每个算法都能回到同一条可重放轨迹。”中，最小证据应记录输入状态、动作或样本来源，手算首个中间量，再与实现输出逐项比对。

针对“第11章 对状态的不完全观测”，先写出该项使用的状态、动作、随机变量或参数版本，再回答它如何改变目标量。正常路径之外增加一个只破坏终止标记、策略版本、shape、采样概率或梯度符号的失败样本；检测器应在最终回报变化之前定位首个分叉。

### 第12章 模仿学习

答案应重建该章的已知条件、推导或程序状态，给出中间值、容差和反例，而不是只复述最终结论。 将它放回本章主线“用环境、数据、目标、优化、策略版本和评价六本账串联十九章，要求每个算法都能回到同一条可重放轨迹。”中，最小证据应记录输入状态、动作或样本来源，手算首个中间量，再与实现输出逐项比对。

针对“第12章 模仿学习”，先写出该项使用的状态、动作、随机变量或参数版本，再回答它如何改变目标量。正常路径之外增加一个只破坏终止标记、策略版本、shape、采样概率或梯度符号的失败样本；检测器应在最终回报变化之前定位首个分叉。

### 第13章 并行计算

worker负责采样与局部梯度，参数端负责聚合；同步受慢worker约束，异步则引入参数陈旧度。 将它放回本章主线“用环境、数据、目标、优化、策略版本和评价六本账串联十九章，要求每个算法都能回到同一条可重放轨迹。”中，最小证据应记录输入状态、动作或样本来源，手算首个中间量，再与实现输出逐项比对。

针对“第13章 并行计算”，先写出该项使用的状态、动作、随机变量或参数版本，再回答它如何改变目标量。正常路径之外增加一个只破坏终止标记、策略版本、shape、采样概率或梯度符号的失败样本；检测器应在最终回报变化之前定位首个分叉。

### 第14章 多智能体系统

其他智能体同时学习使环境非平稳，训练阶段可借助联合信息，但分散执行只能使用部署时可得观测。 将它放回本章主线“用环境、数据、目标、优化、策略版本和评价六本账串联十九章，要求每个算法都能回到同一条可重放轨迹。”中，最小证据应记录输入状态、动作或样本来源，手算首个中间量，再与实现输出逐项比对。

针对“第14章 多智能体系统”，先写出该项使用的状态、动作、随机变量或参数版本，再回答它如何改变目标量。正常路径之外增加一个只破坏终止标记、策略版本、shape、采样概率或梯度符号的失败样本；检测器应在最终回报变化之前定位首个分叉。

### 第15章 完全合作关系设定下的多智能体强化学习

其他智能体同时学习使环境非平稳，训练阶段可借助联合信息，但分散执行只能使用部署时可得观测。 将它放回本章主线“用环境、数据、目标、优化、策略版本和评价六本账串联十九章，要求每个算法都能回到同一条可重放轨迹。”中，最小证据应记录输入状态、动作或样本来源，手算首个中间量，再与实现输出逐项比对。

针对“第15章 完全合作关系设定下的多智能体强化学习”，先写出该项使用的状态、动作、随机变量或参数版本，再回答它如何改变目标量。正常路径之外增加一个只破坏终止标记、策略版本、shape、采样概率或梯度符号的失败样本；检测器应在最终回报变化之前定位首个分叉。

### 第16章 非合作关系设定下的多智能体强化学习

其他智能体同时学习使环境非平稳，训练阶段可借助联合信息，但分散执行只能使用部署时可得观测。 将它放回本章主线“用环境、数据、目标、优化、策略版本和评价六本账串联十九章，要求每个算法都能回到同一条可重放轨迹。”中，最小证据应记录输入状态、动作或样本来源，手算首个中间量，再与实现输出逐项比对。

针对“第16章 非合作关系设定下的多智能体强化学习”，先写出该项使用的状态、动作、随机变量或参数版本，再回答它如何改变目标量。正常路径之外增加一个只破坏终止标记、策略版本、shape、采样概率或梯度符号的失败样本；检测器应在最终回报变化之前定位首个分叉。

### 第17章 注意力机制与多智能体强化学习

其他智能体同时学习使环境非平稳，训练阶段可借助联合信息，但分散执行只能使用部署时可得观测。 将它放回本章主线“用环境、数据、目标、优化、策略版本和评价六本账串联十九章，要求每个算法都能回到同一条可重放轨迹。”中，最小证据应记录输入状态、动作或样本来源，手算首个中间量，再与实现输出逐项比对。

针对“第17章 注意力机制与多智能体强化学习”，先写出该项使用的状态、动作、随机变量或参数版本，再回答它如何改变目标量。正常路径之外增加一个只破坏终止标记、策略版本、shape、采样概率或梯度符号的失败样本；检测器应在最终回报变化之前定位首个分叉。

### 第18章 AlphaGo与蒙特卡洛树搜索

树搜索把策略先验与价值估计转换为访问次数分布，每次模拟必须完整执行选择、扩展、评估和回传。 将它放回本章主线“用环境、数据、目标、优化、策略版本和评价六本账串联十九章，要求每个算法都能回到同一条可重放轨迹。”中，最小证据应记录输入状态、动作或样本来源，手算首个中间量，再与实现输出逐项比对。

针对“第18章 AlphaGo与蒙特卡洛树搜索”，先写出该项使用的状态、动作、随机变量或参数版本，再回答它如何改变目标量。正常路径之外增加一个只破坏终止标记、策略版本、shape、采样概率或梯度符号的失败样本；检测器应在最终回报变化之前定位首个分叉。

### 第19章 现实世界中的应用

先确认动作会改变未来状态与数据分布，再定义长期目标和安全约束；否则监督学习或优化方法可能更合适。 将它放回本章主线“用环境、数据、目标、优化、策略版本和评价六本账串联十九章，要求每个算法都能回到同一条可重放轨迹。”中，最小证据应记录输入状态、动作或样本来源，手算首个中间量，再与实现输出逐项比对。

针对“第19章 现实世界中的应用”，先写出该项使用的状态、动作、随机变量或参数版本，再回答它如何改变目标量。正常路径之外增加一个只破坏终止标记、策略版本、shape、采样概率或梯度符号的失败样本；检测器应在最终回报变化之前定位首个分叉。

### 附录A 贝尔曼方程

利用回报的一步递归和条件期望得到价值不动点；固定策略使用加权平均，最优控制使用动作最大化。 将它放回本章主线“用环境、数据、目标、优化、策略版本和评价六本账串联十九章，要求每个算法都能回到同一条可重放轨迹。”中，最小证据应记录输入状态、动作或样本来源，手算首个中间量，再与实现输出逐项比对。

针对“附录A 贝尔曼方程”，先写出该项使用的状态、动作、随机变量或参数版本，再回答它如何改变目标量。正常路径之外增加一个只破坏终止标记、策略版本、shape、采样概率或梯度符号的失败样本；检测器应在最终回报变化之前定位首个分叉。

### 附录B 习题答案

“习题答案”在本章中承担从输入合同到多智能体的连接作用，需要同时保留定义、计算和验证边界。 将它放回本章主线“用环境、数据、目标、优化、策略版本和评价六本账串联十九章，要求每个算法都能回到同一条可重放轨迹。”中，最小证据应记录输入状态、动作或样本来源，手算首个中间量，再与实现输出逐项比对。

针对“附录B 习题答案”，先写出该项使用的状态、动作、随机变量或参数版本，再回答它如何改变目标量。正常路径之外增加一个只破坏终止标记、策略版本、shape、采样概率或梯度符号的失败样本；检测器应在最终回报变化之前定位首个分叉。

## 六类独立证据

### 环境与状态证据

固定环境版本、包装器顺序、动作缩放、随机种子和初始状态。分别记录终止与时间上限截断；评价环境重新创建，不能继承训练episode的隐藏状态、回放缓存或归一化统计写权限。

### 轨迹与采样证据

保存轨迹边界、行为策略概率、采集参数版本和采样索引。回放或重要性采样时记录抽样概率，序列模型保留时间顺序与有效掩码，多智能体数据保留智能体身份和联合动作。

### 目标与自举证据

先手算即时奖励、折扣、终止掩码和bootstrap值，再构造目标。在线网络、目标网络、旧策略、当前策略和对手策略都使用明确版本号；同一步不得读取更新后的参数重新生成目标。

### 梯度与优化证据

对一个参数执行中心有限差分，检查梯度符号、量级和相对误差。共享骨干时分别保存各损失对参数的贡献；裁剪前后梯度范数、优化器状态与实际学习率都进入账本。

### 对照与消融证据

基线与改进使用相同环境步数、评价轨迹和随机种子集合。每次只改变一个算法机制；报告均值、离散程度和失败率，不从多个种子中只挑最好曲线。

### 冻结评价证据

评价关闭探索噪声和参数更新，固定策略快照与环境集合，单独统计回报、约束、稳定性和样本成本。应用场景还要经过离线、影子流量和受控线上实验，不能直接从模拟回报跳到生产结论。

## 三步交互实验

<Stepper>
  <Step title="画出对象与版本图">
    推动阶段滑块，确认环境、转移、目标、更新和冻结评价使用哪一版状态与参数。
    <DrlOfficialFinalReviewMapLab />
  </Step>
  <Step title="重放折扣轨迹">
    调整折扣率与采样噪声，观察远期奖励贡献如何变化，并预测首个受影响的中间量。
    <DrlOfficialFinalReviewTraceLab />
  </Step>
  <Step title="注入单一故障">
    切换终止或策略版本故障，要求审计指标在最终回报之前暴露状态、目标或重放不一致。
    <DrlOfficialFinalReviewAuditLab />
  </Step>
</Stepper>

## 常见失败与回退

<Callout type="trap">
  **本章高风险失败：** 只背算法名称和最终性能，不记录转移来源、目标参数版本、行为概率、终止语义或部署时的信息边界。
</Callout>

<Callout type="trap">
  **最终曲线不是充分证据：** 奖励上升可能与错误终止、评价继续训练、数据泄漏、错误动作缩放或挑选随机种子同时出现。
</Callout>

<Callout type="tip">
  **回退顺序：** 环境转移 → 轨迹边界 → 手算目标 → 前向输出 → 梯度 → 参数更新 → 冻结评价。第一处不一致未消除前，不增加网络、worker或训练步数。
</Callout>

## 本章回顾

用环境、数据、目标、优化、策略版本和评价六本账串联十九章，要求每个算法都能回到同一条可重放轨迹。 最终掌握标准不是复述算法名，而是能说明每个量来自哪里、由哪版策略产生、在哪个边界归零、如何手算，以及故障样本应在哪一步被发现。通过标准是：随机抽取价值学习、策略学习、多智能体和应用各一章，均能手算首个目标、复现实验、注入单一故障并定位第一处证据分叉。

<Glossary>

<GlossaryItem term="环境合同">
  “环境合同”是本页证据链第1站。记录定义、输入来源、参数版本、手算中间值、数值容差和失败边界；只看到最终回报不能证明这一站正确。
</GlossaryItem>

<GlossaryItem term="采样证据">
  “采样证据”是本页证据链第2站。记录定义、输入来源、参数版本、手算中间值、数值容差和失败边界；只看到最终回报不能证明这一站正确。
</GlossaryItem>

<GlossaryItem term="价值目标">
  “价值目标”是本页证据链第3站。记录定义、输入来源、参数版本、手算中间值、数值容差和失败边界；只看到最终回报不能证明这一站正确。
</GlossaryItem>

<GlossaryItem term="策略梯度">
  “策略梯度”是本页证据链第4站。记录定义、输入来源、参数版本、手算中间值、数值容差和失败边界；只看到最终回报不能证明这一站正确。
</GlossaryItem>

<GlossaryItem term="多智能体">
  “多智能体”是本页证据链第5站。记录定义、输入来源、参数版本、手算中间值、数值容差和失败边界；只看到最终回报不能证明这一站正确。
</GlossaryItem>

<GlossaryItem term="独立评价">
  “独立评价”是本页证据链第6站。记录定义、输入来源、参数版本、手算中间值、数值容差和失败边界；只看到最终回报不能证明这一站正确。
</GlossaryItem>

</Glossary>

<Exercises>

1. 如何为《深度强化学习》全书总复习建立最小可手算样例？

<Answer>
  只保留两个状态、两个动作、两步轨迹或两个智能体，固定奖励、折扣、参数和随机种子；先写预期中间值，再运行一次更新并比较状态、目标、梯度与参数摘要。
</Answer>

2. 如何证明“随机抽取价值学习、策略学习、多智能体和应用各一章，均能手算首个目标、复现实验、注入单一故障并定位第一处证据分叉。”不是一次高回报偶然满足？

<Answer>
  用同一输入并排运行正常路径与只破坏一个终止标记、策略版本、采样概率、动作边界或梯度符号的路径。检测器必须在预期首个中间量区分两者，并在独立评价中保持结论。
</Answer>

3. 修复《深度强化学习》全书总复习后按什么顺序回归？

<Answer>
  先验证环境与轨迹，再核对目标与前向，然后核对梯度和更新，最后冻结策略做多随机种子评价；任何早期证据失败都停止后续长训练。
</Answer>

</Exercises>

## 前后导航

- [附录B 习题答案](/learn/deep-reinforcement-learning/06-appendices/drl-appendix-b-exercise-answers)

<Attribution adaptedFrom="王树森、黎彧君、张志华著《深度强化学习》，人民邮电出版社，2022年11月，294页，ISBN 9787115600691 · 基于5部分、19章、233个编号节/小节和2个附录完整目录的独立教学重构" adaptedUrl="https://github.com/DeepRLChinese/DeepRL-Chinese" />
