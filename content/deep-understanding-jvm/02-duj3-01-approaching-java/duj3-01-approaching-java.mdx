---
title: "第1章 走近Java"
description: "依据周志明《深入理解Java虚拟机》第3版完整目录独立重构：从Java体系和JVM演进走到可重复的OpenJDK 12源码构建，建立阅读虚拟机实现的入口"
type: C
section: "深入理解Java虚拟机（第3版 / 2019年JDK 12基线）"
draft: false
math: true
sourceUrl: "https://github.com/fenixsoft/jvm_book"
demo: true
order: 0
---

import { Duj3StructureLab, Duj3ExecutionLab, Duj3EvidenceLab } from "@/components/mdx/deep-understanding-jvm/diagrams/duj3-01-approaching-java";

# 第1章 走近Java

本页对应第3版正式结构中的 **第1章 走近Java**。课程不复制原文，而是按目录逐节点独立重构核心知识，把规范、HotSpot实现、工具输出和可复现实验连成证据链。当前任务是：从Java体系和JVM演进走到可重复的OpenJDK 12源码构建，建立阅读虚拟机实现的入口。

<Callout type="info">**版本与来源边界。** 本课程锁定周志明《深入理解Java虚拟机：JVM高级特性与最佳实践》第3版，机械工业出版社，2019年，纸本正文521页，ISBN 9787111641247。作者仓库确认第3版样例代码与持续勘误；出版社版式电子目录确认5个部分、13章与附录A-E。原书成文于2019年中期，以JDK 12和JDK 13预览版为时间边界。后续JDK的行为、参数和工具必须重新查一手资料，不能冒充原书内容。</Callout>

## 可验收学习目标

<Objectives>
- 能解释本页全部26个正式节点的概念层次、前置关系、版本边界与失败条件。
- 能实现一个固定JDK、JVM参数、工作量和停止条件的最小可重复实验。
- 能比较规范承诺、HotSpot实现、工具观测与应用证据，指出哪些结论不可跨版本外推。
- 能设计反例、资源隔离、回滚和交接记录，让另一位读者独立重放并挑战结论。
</Objectives>

本页交付 **JVM家族比较表、JDK 12构建清单、构建日志、镜像验证与调试断点**。首要陷阱是“用今天的发行版默认行为回填2019年的技术语境，或构建成功却不记录源码提交、boot JDK与依赖”；所有实验都必须先预测、再运行、最后解释偏差。

## 官方目录逐节点复刻

### 第1章 走近Java

**正式节点 1/26。** 先界定规范、实现、发行版和版本，再用最小案例把概念连接到可观察行为；保存输入、命令、输出和反例，避免只背结论。本节点必须服务于“从Java体系和JVM演进走到可重复的OpenJDK 12源码构建，建立阅读虚拟机实现的入口”，并写入JVM家族比较表、JDK 12构建清单、构建日志、镜像验证与调试断点。

先预测一个能推翻当前理解的结果，再运行基线与单变量对照。验收说明JDK、JVM实现、发行版、参数、工作量和采集时刻；若只能复述术语或命令，尚未覆盖“第1章 走近Java”的核心知识。

### 1.1 概述

**正式节点 2/26。** 先界定规范、实现、发行版和版本，再用最小案例把概念连接到可观察行为；保存输入、命令、输出和反例，避免只背结论。本节点必须服务于“从Java体系和JVM演进走到可重复的OpenJDK 12源码构建，建立阅读虚拟机实现的入口”，并写入JVM家族比较表、JDK 12构建清单、构建日志、镜像验证与调试断点。

先预测一个能推翻当前理解的结果，再运行基线与单变量对照。验收说明JDK、JVM实现、发行版、参数、工作量和采集时刻；若只能复述术语或命令，尚未覆盖“1.1 概述”的核心知识。

### 1.2 Java技术体系

**正式节点 3/26。** 先界定规范、实现、发行版和版本，再用最小案例把概念连接到可观察行为；保存输入、命令、输出和反例，避免只背结论。本节点必须服务于“从Java体系和JVM演进走到可重复的OpenJDK 12源码构建，建立阅读虚拟机实现的入口”，并写入JVM家族比较表、JDK 12构建清单、构建日志、镜像验证与调试断点。

先预测一个能推翻当前理解的结果，再运行基线与单变量对照。验收说明JDK、JVM实现、发行版、参数、工作量和采集时刻；若只能复述术语或命令，尚未覆盖“1.2 Java技术体系”的核心知识。

### 1.3 Java发展史

**正式节点 4/26。** 把事实放回当时的日期、源码和产品边界，区分规范、实现与预测；历史判断用一手发布记录复核，不能用今天结果改写当时证据。本节点必须服务于“从Java体系和JVM演进走到可重复的OpenJDK 12源码构建，建立阅读虚拟机实现的入口”，并写入JVM家族比较表、JDK 12构建清单、构建日志、镜像验证与调试断点。

先预测一个能推翻当前理解的结果，再运行基线与单变量对照。验收说明JDK、JVM实现、发行版、参数、工作量和采集时刻；若只能复述术语或命令，尚未覆盖“1.3 Java发展史”的核心知识。

### 1.4 Java虚拟机家族

**正式节点 5/26。** 把事实放回当时的日期、源码和产品边界，区分规范、实现与预测；历史判断用一手发布记录复核，不能用今天结果改写当时证据。本节点必须服务于“从Java体系和JVM演进走到可重复的OpenJDK 12源码构建，建立阅读虚拟机实现的入口”，并写入JVM家族比较表、JDK 12构建清单、构建日志、镜像验证与调试断点。

先预测一个能推翻当前理解的结果，再运行基线与单变量对照。验收说明JDK、JVM实现、发行版、参数、工作量和采集时刻；若只能复述术语或命令，尚未覆盖“1.4 Java虚拟机家族”的核心知识。

### 1.4.1 虚拟机始祖：Sun Classic/Exact VM

**正式节点 6/26。** 把十六进制偏移、Class表项、常量池索引与javap输出互相核对，并逐条记录局部变量表和操作数栈变化。本节点必须服务于“从Java体系和JVM演进走到可重复的OpenJDK 12源码构建，建立阅读虚拟机实现的入口”，并写入JVM家族比较表、JDK 12构建清单、构建日志、镜像验证与调试断点。

先预测一个能推翻当前理解的结果，再运行基线与单变量对照。验收说明JDK、JVM实现、发行版、参数、工作量和采集时刻；若只能复述术语或命令，尚未覆盖“1.4.1 虚拟机始祖：Sun Classic/Exact VM”的核心知识。

### 1.4.2 武林盟主：HotSpot VM

**正式节点 7/26。** 先界定规范、实现、发行版和版本，再用最小案例把概念连接到可观察行为；保存输入、命令、输出和反例，避免只背结论。本节点必须服务于“从Java体系和JVM演进走到可重复的OpenJDK 12源码构建，建立阅读虚拟机实现的入口”，并写入JVM家族比较表、JDK 12构建清单、构建日志、镜像验证与调试断点。

先预测一个能推翻当前理解的结果，再运行基线与单变量对照。验收说明JDK、JVM实现、发行版、参数、工作量和采集时刻；若只能复述术语或命令，尚未覆盖“1.4.2 武林盟主：HotSpot VM”的核心知识。

### 1.4.3 小家碧玉：Mobile/Embedded VM

**正式节点 8/26。** 先界定规范、实现、发行版和版本，再用最小案例把概念连接到可观察行为；保存输入、命令、输出和反例，避免只背结论。本节点必须服务于“从Java体系和JVM演进走到可重复的OpenJDK 12源码构建，建立阅读虚拟机实现的入口”，并写入JVM家族比较表、JDK 12构建清单、构建日志、镜像验证与调试断点。

先预测一个能推翻当前理解的结果，再运行基线与单变量对照。验收说明JDK、JVM实现、发行版、参数、工作量和采集时刻；若只能复述术语或命令，尚未覆盖“1.4.3 小家碧玉：Mobile/Embedded VM”的核心知识。

### 1.4.4 天下第二：BEA JRockit/IBM J9 VM

**正式节点 9/26。** 先界定规范、实现、发行版和版本，再用最小案例把概念连接到可观察行为；保存输入、命令、输出和反例，避免只背结论。本节点必须服务于“从Java体系和JVM演进走到可重复的OpenJDK 12源码构建，建立阅读虚拟机实现的入口”，并写入JVM家族比较表、JDK 12构建清单、构建日志、镜像验证与调试断点。

先预测一个能推翻当前理解的结果，再运行基线与单变量对照。验收说明JDK、JVM实现、发行版、参数、工作量和采集时刻；若只能复述术语或命令，尚未覆盖“1.4.4 天下第二：BEA JRockit/IBM J9 VM”的核心知识。

### 1.4.5 软硬合璧：BEA Liquid VM/Azul VM

**正式节点 10/26。** 先界定规范、实现、发行版和版本，再用最小案例把概念连接到可观察行为；保存输入、命令、输出和反例，避免只背结论。本节点必须服务于“从Java体系和JVM演进走到可重复的OpenJDK 12源码构建，建立阅读虚拟机实现的入口”，并写入JVM家族比较表、JDK 12构建清单、构建日志、镜像验证与调试断点。

先预测一个能推翻当前理解的结果，再运行基线与单变量对照。验收说明JDK、JVM实现、发行版、参数、工作量和采集时刻；若只能复述术语或命令，尚未覆盖“1.4.5 软硬合璧：BEA Liquid VM/Azul VM”的核心知识。

### 1.4.6 挑战者：Apache Harmony/Google Android Dalvik VM

**正式节点 11/26。** 先界定规范、实现、发行版和版本，再用最小案例把概念连接到可观察行为；保存输入、命令、输出和反例，避免只背结论。本节点必须服务于“从Java体系和JVM演进走到可重复的OpenJDK 12源码构建，建立阅读虚拟机实现的入口”，并写入JVM家族比较表、JDK 12构建清单、构建日志、镜像验证与调试断点。

先预测一个能推翻当前理解的结果，再运行基线与单变量对照。验收说明JDK、JVM实现、发行版、参数、工作量和采集时刻；若只能复述术语或命令，尚未覆盖“1.4.6 挑战者：Apache Harmony/Google Android Dalvik VM”的核心知识。

### 1.4.7 没有成功，但并非失败：Microsoft JVM及其他

**正式节点 12/26。** 先界定规范、实现、发行版和版本，再用最小案例把概念连接到可观察行为；保存输入、命令、输出和反例，避免只背结论。本节点必须服务于“从Java体系和JVM演进走到可重复的OpenJDK 12源码构建，建立阅读虚拟机实现的入口”，并写入JVM家族比较表、JDK 12构建清单、构建日志、镜像验证与调试断点。

先预测一个能推翻当前理解的结果，再运行基线与单变量对照。验收说明JDK、JVM实现、发行版、参数、工作量和采集时刻；若只能复述术语或命令，尚未覆盖“1.4.7 没有成功，但并非失败：Microsoft JVM及其他”的核心知识。

### 1.4.8 百家争鸣

**正式节点 13/26。** 先界定规范、实现、发行版和版本，再用最小案例把概念连接到可观察行为；保存输入、命令、输出和反例，避免只背结论。本节点必须服务于“从Java体系和JVM演进走到可重复的OpenJDK 12源码构建，建立阅读虚拟机实现的入口”，并写入JVM家族比较表、JDK 12构建清单、构建日志、镜像验证与调试断点。

先预测一个能推翻当前理解的结果，再运行基线与单变量对照。验收说明JDK、JVM实现、发行版、参数、工作量和采集时刻；若只能复述术语或命令，尚未覆盖“1.4.8 百家争鸣”的核心知识。

### 1.5 展望Java技术的未来

**正式节点 14/26。** 把事实放回当时的日期、源码和产品边界，区分规范、实现与预测；历史判断用一手发布记录复核，不能用今天结果改写当时证据。本节点必须服务于“从Java体系和JVM演进走到可重复的OpenJDK 12源码构建，建立阅读虚拟机实现的入口”，并写入JVM家族比较表、JDK 12构建清单、构建日志、镜像验证与调试断点。

先预测一个能推翻当前理解的结果，再运行基线与单变量对照。验收说明JDK、JVM实现、发行版、参数、工作量和采集时刻；若只能复述术语或命令，尚未覆盖“1.5 展望Java技术的未来”的核心知识。

### 1.5.1 无语言倾向

**正式节点 15/26。** 先界定规范、实现、发行版和版本，再用最小案例把概念连接到可观察行为；保存输入、命令、输出和反例，避免只背结论。本节点必须服务于“从Java体系和JVM演进走到可重复的OpenJDK 12源码构建，建立阅读虚拟机实现的入口”，并写入JVM家族比较表、JDK 12构建清单、构建日志、镜像验证与调试断点。

先预测一个能推翻当前理解的结果，再运行基线与单变量对照。验收说明JDK、JVM实现、发行版、参数、工作量和采集时刻；若只能复述术语或命令，尚未覆盖“1.5.1 无语言倾向”的核心知识。

### 1.5.2 新一代即时编译器

**正式节点 16/26。** 区分前端、解释、JIT和AOT阶段，保存脱糖字节码、编译事件与IR；用预热、基准和去优化反例限制结论。本节点必须服务于“从Java体系和JVM演进走到可重复的OpenJDK 12源码构建，建立阅读虚拟机实现的入口”，并写入JVM家族比较表、JDK 12构建清单、构建日志、镜像验证与调试断点。

先预测一个能推翻当前理解的结果，再运行基线与单变量对照。验收说明JDK、JVM实现、发行版、参数、工作量和采集时刻；若只能复述术语或命令，尚未覆盖“1.5.2 新一代即时编译器”的核心知识。

### 1.5.3 向Native迈进

**正式节点 17/26。** 先界定规范、实现、发行版和版本，再用最小案例把概念连接到可观察行为；保存输入、命令、输出和反例，避免只背结论。本节点必须服务于“从Java体系和JVM演进走到可重复的OpenJDK 12源码构建，建立阅读虚拟机实现的入口”，并写入JVM家族比较表、JDK 12构建清单、构建日志、镜像验证与调试断点。

先预测一个能推翻当前理解的结果，再运行基线与单变量对照。验收说明JDK、JVM实现、发行版、参数、工作量和采集时刻；若只能复述术语或命令，尚未覆盖“1.5.3 向Native迈进”的核心知识。

### 1.5.4 灵活的胖子

**正式节点 18/26。** 先界定规范、实现、发行版和版本，再用最小案例把概念连接到可观察行为；保存输入、命令、输出和反例，避免只背结论。本节点必须服务于“从Java体系和JVM演进走到可重复的OpenJDK 12源码构建，建立阅读虚拟机实现的入口”，并写入JVM家族比较表、JDK 12构建清单、构建日志、镜像验证与调试断点。

先预测一个能推翻当前理解的结果，再运行基线与单变量对照。验收说明JDK、JVM实现、发行版、参数、工作量和采集时刻；若只能复述术语或命令，尚未覆盖“1.5.4 灵活的胖子”的核心知识。

### 1.5.5 语言语法持续增强

**正式节点 19/26。** 先界定规范、实现、发行版和版本，再用最小案例把概念连接到可观察行为；保存输入、命令、输出和反例，避免只背结论。本节点必须服务于“从Java体系和JVM演进走到可重复的OpenJDK 12源码构建，建立阅读虚拟机实现的入口”，并写入JVM家族比较表、JDK 12构建清单、构建日志、镜像验证与调试断点。

先预测一个能推翻当前理解的结果，再运行基线与单变量对照。验收说明JDK、JVM实现、发行版、参数、工作量和采集时刻；若只能复述术语或命令，尚未覆盖“1.5.5 语言语法持续增强”的核心知识。

### 1.6 实战：自己编译JDK

**正式节点 20/26。** 区分前端、解释、JIT和AOT阶段，保存脱糖字节码、编译事件与IR；用预热、基准和去优化反例限制结论。本节点必须服务于“从Java体系和JVM演进走到可重复的OpenJDK 12源码构建，建立阅读虚拟机实现的入口”，并写入JVM家族比较表、JDK 12构建清单、构建日志、镜像验证与调试断点。

先预测一个能推翻当前理解的结果，再运行基线与单变量对照。验收说明JDK、JVM实现、发行版、参数、工作量和采集时刻；若只能复述术语或命令，尚未覆盖“1.6 实战：自己编译JDK”的核心知识。

### 1.6.1 获取源码

**正式节点 21/26。** 把事实放回当时的日期、源码和产品边界，区分规范、实现与预测；历史判断用一手发布记录复核，不能用今天结果改写当时证据。本节点必须服务于“从Java体系和JVM演进走到可重复的OpenJDK 12源码构建，建立阅读虚拟机实现的入口”，并写入JVM家族比较表、JDK 12构建清单、构建日志、镜像验证与调试断点。

先预测一个能推翻当前理解的结果，再运行基线与单变量对照。验收说明JDK、JVM实现、发行版、参数、工作量和采集时刻；若只能复述术语或命令，尚未覆盖“1.6.1 获取源码”的核心知识。

### 1.6.2 系统需求

**正式节点 22/26。** 先界定规范、实现、发行版和版本，再用最小案例把概念连接到可观察行为；保存输入、命令、输出和反例，避免只背结论。本节点必须服务于“从Java体系和JVM演进走到可重复的OpenJDK 12源码构建，建立阅读虚拟机实现的入口”，并写入JVM家族比较表、JDK 12构建清单、构建日志、镜像验证与调试断点。

先预测一个能推翻当前理解的结果，再运行基线与单变量对照。验收说明JDK、JVM实现、发行版、参数、工作量和采集时刻；若只能复述术语或命令，尚未覆盖“1.6.2 系统需求”的核心知识。

### 1.6.3 构建编译环境

**正式节点 23/26。** 区分前端、解释、JIT和AOT阶段，保存脱糖字节码、编译事件与IR；用预热、基准和去优化反例限制结论。本节点必须服务于“从Java体系和JVM演进走到可重复的OpenJDK 12源码构建，建立阅读虚拟机实现的入口”，并写入JVM家族比较表、JDK 12构建清单、构建日志、镜像验证与调试断点。

先预测一个能推翻当前理解的结果，再运行基线与单变量对照。验收说明JDK、JVM实现、发行版、参数、工作量和采集时刻；若只能复述术语或命令，尚未覆盖“1.6.3 构建编译环境”的核心知识。

### 1.6.4 进行编译

**正式节点 24/26。** 区分前端、解释、JIT和AOT阶段，保存脱糖字节码、编译事件与IR；用预热、基准和去优化反例限制结论。本节点必须服务于“从Java体系和JVM演进走到可重复的OpenJDK 12源码构建，建立阅读虚拟机实现的入口”，并写入JVM家族比较表、JDK 12构建清单、构建日志、镜像验证与调试断点。

先预测一个能推翻当前理解的结果，再运行基线与单变量对照。验收说明JDK、JVM实现、发行版、参数、工作量和采集时刻；若只能复述术语或命令，尚未覆盖“1.6.4 进行编译”的核心知识。

### 1.6.5 在IDE工具中进行源码调试

**正式节点 25/26。** 先写工具要回答的问题和探针预算，再保存版本化原始输出；将工具提示视为候选，用第二种证据和故障对照复核。本节点必须服务于“从Java体系和JVM演进走到可重复的OpenJDK 12源码构建，建立阅读虚拟机实现的入口”，并写入JVM家族比较表、JDK 12构建清单、构建日志、镜像验证与调试断点。

先预测一个能推翻当前理解的结果，再运行基线与单变量对照。验收说明JDK、JVM实现、发行版、参数、工作量和采集时刻；若只能复述术语或命令，尚未覆盖“1.6.5 在IDE工具中进行源码调试”的核心知识。

### 1.7 本章小结

**正式节点 26/26。** 先界定规范、实现、发行版和版本，再用最小案例把概念连接到可观察行为；保存输入、命令、输出和反例，避免只背结论。本节点必须服务于“从Java体系和JVM演进走到可重复的OpenJDK 12源码构建，建立阅读虚拟机实现的入口”，并写入JVM家族比较表、JDK 12构建清单、构建日志、镜像验证与调试断点。

先预测一个能推翻当前理解的结果，再运行基线与单变量对照。验收说明JDK、JVM实现、发行版、参数、工作量和采集时刻；若只能复述术语或命令，尚未覆盖“1.7 本章小结”的核心知识。

## 从虚拟机边界开始

学习JVM必须先区分四层：Java语言规范约束源代码语义，Java虚拟机规范约束Class格式与抽象执行，HotSpot等实现选择内部算法，Oracle JDK、OpenJDK构建或其他发行版再决定打包和默认配置。<Term def="由规范定义、不同合规实现都必须保持的可观察承诺">规范合同</Term>不能由一张HotSpot对象头图替代；<Term def="某一虚拟机在特定版本中为满足规范而选择的数据结构和算法">实现细节</Term>也不能被误写成永久规则。

每项结论建立<Term def="记录JDK、JVM实现、供应商、操作系统、参数、容器限制与源码提交的可比较标识">环境指纹</Term>。同样的Java版本号仍可能因构建、收集器、压缩指针、容器感知和启动参数不同而产生不同日志、对象布局与性能。课程以第3版2019年语境还原知识，再单独维护版本账本；现代验证只能标为复核，不能回写作者当时尚未观察到的结果。

从程序到机器行为至少经过源代码前端编译、Class验证和加载、解释执行、热点编译、运行时服务与操作系统调度。<Term def="源码构造、Class表项、运行时状态和机器级结果之间可追溯的对应关系">表示映射</Term>是排错基础。源码中的一行可能对应多条指令，一次方法调用可能因分派和内联看不到直观栈帧，一个对象分配也可能因逃逸分析被消除。必须说明正在观察哪一层。

性能实验遵循固定工作量、充分预热、重复采样和单变量原则。<Term def="程序从解释执行进入分层编译并达到相对稳定代码形态前的阶段">预热</Term>不是可以随意丢弃的数据，而是启动与短任务的一部分；只有稳态问题才可在明确理由下单独报告稳态。均值不能掩盖尾延迟，吞吐提升不能自动抵消内存、CPU、停顿或公平性退化。

故障实验需要<Term def="在受控资源与权限边界内制造OOM、加载失败、竞争或编译条件变化的可恢复环境">隔离实验</Term>。故意溢出、执行字节码、加载任意类和修改编译参数都可能拖垮宿主或突破信任边界，应使用容器或独立进程、限制CPU和内存、禁用不必要网络、设置超时并保留清理审计。能复现错误不等于可以在生产直接运行复现脚本。

## 跨层证据链

原始证据至少包括源码提交、Class校验和、JDK版本输出、完整命令、参数、工作量、开始结束时间、退出码和未过滤日志。派生图表必须保存查询、过滤与单位。线程转储、堆转储和飞行记录可能包含业务数据，应最小授权、加密、限定保留，并在分析后留下销毁记录。

解释证据时先画状态转换。对象经历分配、可达、不可达与回收；类经历加载、验证、准备、解析和初始化；方法经历解释、编译、内联、失效与去优化；线程经历运行、阻塞、等待和终止。故障往往来自两个状态机边界错配，而不是某个孤立参数。把时间线、所有者和资源变化放在同一图上，才能检验因果顺序。

勘误属于版本化来源。作者仓库对Class表、收集器、类加载、编译和JMM等细节持续修正；课程采用修正后的命题，并在需要时保留原印次差异。若样例输出与课程不同，先核对勘误、JDK和参数，再判断实现变化，不得为了匹配书中截图而禁用安全修复或使用不受支持的生产版本。

没有出现预期事件也是证据，但必须确认观测窗口、采样频率和探针能力足以看见它。一次未观察到GC、编译、竞争或类加载失败，不能直接证明事件不存在；先加入已知会触发信号的阳性对照，验证采集链路，再扩大重复次数或缩小假设。若重放仍不稳定，应报告复现概率和未控制变量，而不是选择一条符合预期的运行作为结论。

## 最小可执行切片

先捕获运行环境，而不是从参数猜测：

```java
record VmCase(String javaVersion, String vmName, String vmVersion,
              String flags, String workload, String evidenceHash) {}
var vmCase = captureEnvironmentBeforeExperiment();
```

每个实验把预期、实际与可推翻条件写在执行之前：

```java
var baseline = run(Scenario.BASELINE, fixedWorkload);
var variant = run(Scenario.ONE_VARIABLE_CHANGED, fixedWorkload);
assertEquals(baseline.inputHash(), variant.inputHash());
assertTrue(variant.rawEvidenceRetained());
```

交接合同不依赖特定IDE或图形工具：

```gherkin
Feature: 第1章 走近Java
  Scenario: connect a JVM concept to reproducible evidence
    Given a versioned JDK, VM flags, source, Class file, and fixed workload
    When 固定源码提交和boot JDK，完成一次干净构建并在类加载或GC初始化路径命中源码断点
    Then the observed state transition matches or falsifies the prediction
    And version limits, resource risks, rollback, and raw evidence are recorded
```

## 定量模型与边界

进程内存不是只有堆，至少需要同时观察堆、元空间、线程栈、代码缓存、直接内存和其他本地分配：

$$
M_{process}approx M_{heap}+M_{meta}+N_{threads}M_{stack}+M_{code}+M_{direct}+M_{native}
$$

垃圾收集成本要放回应用总时间和完成工作量，而不是只比较单次最大停顿：

$$
R_{gc}=rac{T_{gc}}{T_{wall}},qquad Throughput=rac{Work_{completed}}{T_{wall}}
$$

热点价值同时取决于单次成本、调用次数和编译形态；内联可减少调用，却可能扩大代码缓存并改变后续优化：

$$
C_{total}(m)=N(m)	imes ar{C}(m),qquad Delta C=C_{before}-C_{after}
$$

并发正确性不是概率。若同步关系形成happens-before边，则前序写入对后序读取可见；性能比较还应报告竞争与尾延迟：

$$
W(a);hb;R(b)Rightarrow visible(a,b),qquad L_{p99}leq Budget_{p99}
$$

公式用于明确变量和反例，不替代实际JDK证据。每个数字注明单位、并发、数据规模、预热、重复次数、置信区间和机器资源；不同环境的绝对值不能直接横比。

## 三步验证实验

<Steps>
  <Step title="1. 建立跨层结构">
    从“从Java体系和JVM演进走到可重复的OpenJDK 12源码构建，建立阅读虚拟机实现的入口”选择一个规范命题和一个HotSpot实现命题，标出源码、Class、运行时和工具证据。先预测二者在版本变化时哪些保持、哪些可能改变。
    <Duj3StructureLab />
  </Step>
  <Step title="2. 重放状态转换">
    固定环境指纹后执行“固定源码提交和boot JDK，完成一次干净构建并在类加载或GC初始化路径命中源码断点”。基线之后一次只改变一个输入、参数或版本，保存未经筛选的原始输出。
    <Duj3ExecutionLab />
  </Step>
  <Step title="3. 通过证据门">
    用JVM家族比较表、JDK 12构建清单、构建日志、镜像验证与调试断点核对规范、实现、版本、失败反例、资源风险和回滚；缺少任一项时结论不得跨环境复用。
    <Duj3EvidenceLab />
  </Step>
</Steps>

<Callout type="trap">**陷阱：实现等同规范。** 对象头位布局、收集器默认值、JIT阈值和类加载器层次都可能随实现与版本改变。先证明来源层次，再决定结论适用范围。</Callout>

<Callout type="trap">**陷阱：参数先于问题。** 加大堆、切换收集器、关闭优化或增加线程可能暂时隐藏症状，却扩大停顿、CPU、内存与一致性风险。没有基线和回滚阈值不得调参。</Callout>

<Callout type="warning">**历史语境。** 原书第3版以JDK 12和JDK 13预览期为边界。已弃用、移除或重做的工具与功能仍应作为书中知识学习，但现代使用必须另行核对目标JDK官方资料。</Callout>

## 本章回顾

本页从第1章 走近Java覆盖到1.7 本章小结，共26个正式节点。最小闭环是先区分规范、实现、发行版和版本，建立环境指纹，再执行“固定源码提交和boot JDK，完成一次干净构建并在类加载或GC初始化路径命中源码断点”，最后用JVM家族比较表、JDK 12构建清单、构建日志、镜像验证与调试断点证明结论可重放、可推翻、可回滚。若仍出现“用今天的发行版默认行为回填2019年的技术语境，或构建成功却不记录源码提交、boot JDK与依赖”，应回到目录节点补做跨层映射和反例，而不是继续叠加参数。

<Exercises>

**问题 1**：如何为第1章 走近Java建立最小可执行证据链？

<Answer>选择一个明确命题，固定JDK、JVM实现、参数、源码和工作量；执行“固定源码提交和boot JDK，完成一次干净构建并在类加载或GC初始化路径命中源码断点”，保存基线、单变量对照、原始输出与可推翻条件，并标注规范与实现边界。</Answer>

**问题 2**：为什么“用今天的发行版默认行为回填2019年的技术语境，或构建成功却不记录源码提交、boot JDK与依赖”会破坏本页结论？

<Answer>它混淆来源层次或改变多个变量，使现象无法归因。应回到JVM家族比较表、JDK 12构建清单、构建日志、镜像验证与调试断点，补齐版本、状态转换、探针效应、失败反例和回滚，再由另一位读者独立重放。</Answer>

**问题 3**：从第3版迁移到目标JDK时怎样控制变量？

<Answer>先在原书时间边界内理解命题，再查询目标JDK一手文档和作者勘误；一次只改变JDK、收集器、参数或工具之一，比较合同、输出、副作用和已移除功能，不把现代结果冒充原书内容。</Answer>

</Exercises>

<Glossary>
  <GlossaryItem term="规范合同">由规范定义、不同合规实现都必须保持的可观察承诺。</GlossaryItem>
  <GlossaryItem term="实现细节">某一虚拟机在特定版本中为满足规范而选择的数据结构和算法。</GlossaryItem>
  <GlossaryItem term="环境指纹">记录JDK、JVM实现、供应商、操作系统、参数、容器限制与源码提交的可比较标识。</GlossaryItem>
  <GlossaryItem term="表示映射">源码构造、Class表项、运行时状态和机器级结果之间可追溯的对应关系。</GlossaryItem>
  <GlossaryItem term="预热">程序从解释执行进入分层编译并达到相对稳定代码形态前的阶段。</GlossaryItem>
  <GlossaryItem term="隔离实验">在受控资源与权限边界内制造OOM、加载失败、竞争或编译条件变化的可恢复环境。</GlossaryItem>
</Glossary>

[← 上一页：第一部分 走近Java](/learn/deep-understanding-jvm/01-duj3-part-1-approaching-java/duj3-part-1-approaching-java) · [下一页：第二部分 自动内存管理 →](/learn/deep-understanding-jvm/03-duj3-part-2-memory-management/duj3-part-2-memory-management)

<Attribution adaptedFrom="周志明《深入理解Java虚拟机》第3版" adaptedUrl="https://github.com/fenixsoft/jvm_book" />
