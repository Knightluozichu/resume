---
title: 第7章 虚拟机类加载机制
description: 第7章 虚拟机类加载机制覆盖17个正式目录节点，以章专属JVM探针、状态交互、故障恢复和版本证据验收。
type: C
section: 深入理解Java虚拟机（第3版 / 2019年JDK 12基线）
draft: false
math: true
sourceUrl: 'https://github.com/fenixsoft/jvm_book'
demo: true
order: 0
qualityVersion: 2
practiceMode: diagnosis
sourceMode: independent-rewrite
---


{/* DUJ3_QUALITY_V2 */}

## 为什么从“加载链接初始化台”开始

第7章 虚拟机类加载机制不能把语言、JVMS、HotSpot和某个JDK发行版混成一层。加载链接初始化台沿“loader identity → load → verify → prepare → resolve → initialize”保存输入和原始输出，让每个结论都能回到规范条款、实现状态和目标版本。

第7章 虚拟机类加载机制固定JDK镜像、VM参数、工作量、采集窗口和资源限制，只改变触发方式或加载器；图中只呈现本章真实运行时对象、状态边与证据出口；高亮表示当前分析路径，不冒充真实吞吐、停顿、对象大小或线程安全证明。

## 来源、勘误与独立重写边界

第7章 虚拟机类加载机制依据作者[第3版样例与持续勘误仓库](https://github.com/fenixsoft/jvm_book)核定代码和已知修正，并用[出版社版式目录](https://18636251.s21i.faiusr.com/61/ABUIABA9GAAg8dqTugYo3O7rmwQ.pdf)限定5个部分、13章、附录A–E和282个正式节点。第7章 虚拟机类加载机制未取得出版正文授权，目录只限定范围，中文解释、命令、图示、实验与答案均为独立教学重写。

第7章 虚拟机类加载机制保留原书2019年的JDK 12 / JDK 13预览期语境；现代复核以[JVMS 25](https://docs.oracle.com/javase/specs/jvms/se25/html/index.html)、[JLS 25线程与锁](https://docs.oracle.com/javase/specs/jls/se25/html/jls-17.html)、[JDK 25工具规范](https://docs.oracle.com/en/java/javase/25/docs/specs/man/index.html)、[OpenJDK 25发布页](https://openjdk.org/projects/jdk/25/)与[OpenJDK源码](https://github.com/openjdk/jdk)为事实依据。第7章 虚拟机类加载机制的JDK 25注记不是原书内容，任何默认值和实现细节都必须重新测量。

## 本章跨层合同与章专属探针

、、、、。

第7章 虚拟机类加载机制的通过不变量是“类身份由二进制名和定义加载器共同决定，初始化顺序符合JVMS”。第7章 虚拟机类加载机制的实验档案必须保存JDK与VM版本、供应商、OS/容器限制、参数、源码或Class哈希、工作量、时间窗、退出码、未过滤日志、探针成本和清理记录。

第7章 虚拟机类加载机制使用以下章专属探针作为起点；读者必须替换PID或样例、触发失败并保存定义加载器、初始化日志与错误类型，不能只复制命令截图：

```bash
java -Xlog:class+load=info,class+init=debug LoaderProbe
```


## 官方目录逐项深读

### 第7章 虚拟机类加载机制

**四级证据 1/17。** 第7章 虚拟机类加载机制中的第7章 虚拟机类加载机制：记录二进制名、定义加载器、代码来源和加载初始化日志，用LinkageError或初始化反例限制结论。第7章 虚拟机类加载机制固定加载器为“platform”，只把触发方式从“主动使用”切到“反射”，以定义加载器、初始化日志与错误类型定位第一处变化并验证“类身份由二进制名和定义加载器共同决定，初始化顺序符合JVMS”。

### 7.1 概述

**四级证据 2/17。** 第7章 虚拟机类加载机制中的7.1 概述：先判定这是规范合同、HotSpot实现还是发行版行为，再用固定环境、原始输出和失败反例建立可重放证据。第7章 虚拟机类加载机制固定加载器为“platform”，只把触发方式从“主动使用”切到“反射”，以定义加载器、初始化日志与错误类型定位第一处变化并验证“类身份由二进制名和定义加载器共同决定，初始化顺序符合JVMS”。

### 7.2 类加载的时机

**四级证据 3/17。** 第7章 虚拟机类加载机制中的7.2 类加载的时机：记录二进制名、定义加载器、代码来源和加载初始化日志，用LinkageError或初始化反例限制结论。第7章 虚拟机类加载机制固定加载器为“platform”，只把触发方式从“主动使用”切到“反射”，以定义加载器、初始化日志与错误类型定位第一处变化并验证“类身份由二进制名和定义加载器共同决定，初始化顺序符合JVMS”。

### 7.3 类加载的过程

**四级证据 4/17。** 第7章 虚拟机类加载机制中的7.3 类加载的过程：记录二进制名、定义加载器、代码来源和加载初始化日志，用LinkageError或初始化反例限制结论。第7章 虚拟机类加载机制固定加载器为“platform”，只把触发方式从“主动使用”切到“反射”，以定义加载器、初始化日志与错误类型定位第一处变化并验证“类身份由二进制名和定义加载器共同决定，初始化顺序符合JVMS”。

### 7.3.1 加载

**四级证据 5/17。** 第7章 虚拟机类加载机制中的7.3.1 加载：记录二进制名、定义加载器、代码来源和加载初始化日志，用LinkageError或初始化反例限制结论。第7章 虚拟机类加载机制固定加载器为“platform”，只把触发方式从“主动使用”切到“反射”，以定义加载器、初始化日志与错误类型定位第一处变化并验证“类身份由二进制名和定义加载器共同决定，初始化顺序符合JVMS”。

### 7.3.2 验证

**四级证据 6/17。** 第7章 虚拟机类加载机制中的7.3.2 验证：先判定这是规范合同、HotSpot实现还是发行版行为，再用固定环境、原始输出和失败反例建立可重放证据。第7章 虚拟机类加载机制固定加载器为“platform”，只把触发方式从“主动使用”切到“反射”，以定义加载器、初始化日志与错误类型定位第一处变化并验证“类身份由二进制名和定义加载器共同决定，初始化顺序符合JVMS”。

### 7.3.3 准备

**四级证据 7/17。** 第7章 虚拟机类加载机制中的7.3.3 准备：先判定这是规范合同、HotSpot实现还是发行版行为，再用固定环境、原始输出和失败反例建立可重放证据。第7章 虚拟机类加载机制固定加载器为“platform”，只把触发方式从“主动使用”切到“反射”，以定义加载器、初始化日志与错误类型定位第一处变化并验证“类身份由二进制名和定义加载器共同决定，初始化顺序符合JVMS”。

### 7.3.4 解析

**四级证据 8/17。** 第7章 虚拟机类加载机制中的7.3.4 解析：先判定这是规范合同、HotSpot实现还是发行版行为，再用固定环境、原始输出和失败反例建立可重放证据。第7章 虚拟机类加载机制固定加载器为“platform”，只把触发方式从“主动使用”切到“反射”，以定义加载器、初始化日志与错误类型定位第一处变化并验证“类身份由二进制名和定义加载器共同决定，初始化顺序符合JVMS”。

### 7.3.5 初始化

**四级证据 9/17。** 第7章 虚拟机类加载机制中的7.3.5 初始化：记录二进制名、定义加载器、代码来源和加载初始化日志，用LinkageError或初始化反例限制结论。第7章 虚拟机类加载机制固定加载器为“platform”，只把触发方式从“主动使用”切到“反射”，以定义加载器、初始化日志与错误类型定位第一处变化并验证“类身份由二进制名和定义加载器共同决定，初始化顺序符合JVMS”。

### 7.4 类加载器

**四级证据 10/17。** 第7章 虚拟机类加载机制中的7.4 类加载器：记录二进制名、定义加载器、代码来源和加载初始化日志，用LinkageError或初始化反例限制结论。第7章 虚拟机类加载机制固定加载器为“platform”，只把触发方式从“主动使用”切到“反射”，以定义加载器、初始化日志与错误类型定位第一处变化并验证“类身份由二进制名和定义加载器共同决定，初始化顺序符合JVMS”。

### 7.4.1 类与类加载器

**四级证据 11/17。** 第7章 虚拟机类加载机制中的7.4.1 类与类加载器：记录二进制名、定义加载器、代码来源和加载初始化日志，用LinkageError或初始化反例限制结论。第7章 虚拟机类加载机制固定加载器为“platform”，只把触发方式从“主动使用”切到“反射”，以定义加载器、初始化日志与错误类型定位第一处变化并验证“类身份由二进制名和定义加载器共同决定，初始化顺序符合JVMS”。

### 7.4.2 双亲委派模型

**四级证据 12/17。** 第7章 虚拟机类加载机制中的7.4.2 双亲委派模型：先判定这是规范合同、HotSpot实现还是发行版行为，再用固定环境、原始输出和失败反例建立可重放证据。第7章 虚拟机类加载机制固定加载器为“platform”，只把触发方式从“主动使用”切到“反射”，以定义加载器、初始化日志与错误类型定位第一处变化并验证“类身份由二进制名和定义加载器共同决定，初始化顺序符合JVMS”。

### 7.4.3 破坏双亲委派模型

**四级证据 13/17。** 第7章 虚拟机类加载机制中的7.4.3 破坏双亲委派模型：先判定这是规范合同、HotSpot实现还是发行版行为，再用固定环境、原始输出和失败反例建立可重放证据。第7章 虚拟机类加载机制固定加载器为“platform”，只把触发方式从“主动使用”切到“反射”，以定义加载器、初始化日志与错误类型定位第一处变化并验证“类身份由二进制名和定义加载器共同决定，初始化顺序符合JVMS”。

### 7.5 Java模块化系统

**四级证据 14/17。** 第7章 虚拟机类加载机制中的7.5 Java模块化系统：先判定这是规范合同、HotSpot实现还是发行版行为，再用固定环境、原始输出和失败反例建立可重放证据。第7章 虚拟机类加载机制固定加载器为“platform”，只把触发方式从“主动使用”切到“反射”，以定义加载器、初始化日志与错误类型定位第一处变化并验证“类身份由二进制名和定义加载器共同决定，初始化顺序符合JVMS”。

### 7.5.1 模块的兼容性

**四级证据 15/17。** 第7章 虚拟机类加载机制中的7.5.1 模块的兼容性：先判定这是规范合同、HotSpot实现还是发行版行为，再用固定环境、原始输出和失败反例建立可重放证据。第7章 虚拟机类加载机制固定加载器为“platform”，只把触发方式从“主动使用”切到“反射”，以定义加载器、初始化日志与错误类型定位第一处变化并验证“类身份由二进制名和定义加载器共同决定，初始化顺序符合JVMS”。

### 7.5.2 模块化下的类加载器

**四级证据 16/17。** 第7章 虚拟机类加载机制中的7.5.2 模块化下的类加载器：记录二进制名、定义加载器、代码来源和加载初始化日志，用LinkageError或初始化反例限制结论。第7章 虚拟机类加载机制固定加载器为“platform”，只把触发方式从“主动使用”切到“反射”，以定义加载器、初始化日志与错误类型定位第一处变化并验证“类身份由二进制名和定义加载器共同决定，初始化顺序符合JVMS”。

### 7.6 本章小结

**四级证据 17/17。** 第7章 虚拟机类加载机制中的7.6 本章小结：先判定这是规范合同、HotSpot实现还是发行版行为，再用固定环境、原始输出和失败反例建立可重放证据。第7章 虚拟机类加载机制固定加载器为“platform”，只把触发方式从“主动使用”切到“反射”，以定义加载器、初始化日志与错误类型定位第一处变化并验证“类身份由二进制名和定义加载器共同决定，初始化顺序符合JVMS”。

## 三个必须主动触发的误区


## 练习、答案与282节点验证


