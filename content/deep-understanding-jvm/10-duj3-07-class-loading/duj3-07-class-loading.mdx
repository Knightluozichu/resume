---
title: 第7章 虚拟机类加载机制
description: 第7章 虚拟机类加载机制覆盖17个正式目录节点，以章专属JVM探针、状态交互、故障恢复和版本证据验收。
type: C
section: 深入理解Java虚拟机（第3版 / 2019年JDK 12基线）
draft: false
math: true
sourceUrl: 'https://github.com/fenixsoft/jvm_book'
demo: true
order: 0
qualityVersion: 2
practiceMode: diagnosis
sourceMode: independent-rewrite
---
import { Duj307ClassLoadingStructureLab, Duj307ClassLoadingExecutionLab, Duj307ClassLoadingEvidenceLab } from "@/components/mdx/deep-understanding-jvm/diagrams/duj3-07-class-loading";
import { Objectives, Callout, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step, Attribution } from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第7章 虚拟机类加载机制全部17个正式目录节点的规范、实现、发行版与版本边界
- 能运行“加载链接初始化台”，一次只改变触发方式或加载器
- 能修改章专属命令或样例，用Class、日志、JFR、转储或并发断言证明状态转换
- 能注入“把类名相同误当运行时类型相同，忽略定义加载器”，清空派生证据后重放并恢复“类身份由二进制名和定义加载器共同决定，初始化顺序符合JVMS”

</Objectives>

{/* DUJ3_QUALITY_V2 */}

## 为什么从“加载链接初始化台”开始

第7章 虚拟机类加载机制不能把语言、JVMS、HotSpot和某个JDK发行版混成一层。加载链接初始化台沿“loader identity → load → verify → prepare → resolve → initialize”保存输入和原始输出，让每个结论都能回到规范条款、实现状态和目标版本。

第7章 虚拟机类加载机制固定JDK镜像、VM参数、工作量、采集窗口和资源限制，只改变触发方式或加载器；交互分数只表达因果方向，不冒充真实吞吐、停顿、对象大小或线程安全证明。

## 来源、勘误与独立重写边界

第7章 虚拟机类加载机制依据作者[第3版样例与持续勘误仓库](https://github.com/fenixsoft/jvm_book)核定代码和已知修正，并用[出版社版式目录](https://18636251.s21i.faiusr.com/61/ABUIABA9GAAg8dqTugYo3O7rmwQ.pdf)限定5个部分、13章、附录A–E和282个正式节点。第7章 虚拟机类加载机制未取得出版正文授权，目录只限定范围，中文解释、命令、图示、实验与答案均为独立教学重写。

第7章 虚拟机类加载机制保留原书2019年的JDK 12 / JDK 13预览期语境；现代复核以[JVMS 25](https://docs.oracle.com/javase/specs/jvms/se25/html/index.html)、[JLS 25线程与锁](https://docs.oracle.com/javase/specs/jls/se25/html/jls-17.html)、[JDK 25工具规范](https://docs.oracle.com/en/java/javase/25/docs/specs/man/index.html)、[OpenJDK 25发布页](https://openjdk.org/projects/jdk/25/)与[OpenJDK源码](https://github.com/openjdk/jdk)为事实依据。第7章 虚拟机类加载机制的JDK 25注记不是原书内容，任何默认值和实现细节都必须重新测量。

## 本章跨层合同与章专属探针

<Term def="loader identity → load → verify → prepare → resolve → initialize是第7章 虚拟机类加载机制连接冻结JDK与输入与可推翻结论的章专属坐标，必须记录版本、输入、单位和探针影响。">loader identity → load → verify → prepare → resolve → initialize</Term>、<Term def="触发方式是第7章 虚拟机类加载机制连接区分规范和实现与可推翻结论的章专属坐标，必须记录版本、输入、单位和探针影响。">触发方式</Term>、<Term def="加载器是第7章 虚拟机类加载机制连接执行章专属探针与可推翻结论的章专属坐标，必须记录版本、输入、单位和探针影响。">加载器</Term>、<Term def="定义加载器、初始化日志与错误类型是第7章 虚拟机类加载机制连接注入失败并恢复与可推翻结论的章专属坐标，必须记录版本、输入、单位和探针影响。">定义加载器、初始化日志与错误类型</Term>、<Term def="加载状态机、初始化触发矩阵、加载器委派图、类身份实验、模块可读性与导出表是第7章 虚拟机类加载机制连接保存原始发布证据与可推翻结论的章专属坐标，必须记录版本、输入、单位和探针影响。">加载状态机、初始化触发矩阵、加载器委派图、类身份实验、模块可读性与导出表</Term>。

第7章 虚拟机类加载机制的通过不变量是“类身份由二进制名和定义加载器共同决定，初始化顺序符合JVMS”。第7章 虚拟机类加载机制的实验档案必须保存JDK与VM版本、供应商、OS/容器限制、参数、源码或Class哈希、工作量、时间窗、退出码、未过滤日志、探针成本和清理记录。

第7章 虚拟机类加载机制使用以下章专属探针作为起点；读者必须替换PID或样例、触发失败并保存定义加载器、初始化日志与错误类型，不能只复制命令截图：

```bash
java -Xlog:class+load=info,class+init=debug LoaderProbe
```

<Callout type="warning" title="危险实验必须隔离">第7章 虚拟机类加载机制涉及OOM、堆转储、调试附加、任意Class、历史工具链或高成本探针时，只能在受限进程、容器或离线虚拟机运行；设置CPU/内存/时间上限并保留敏感数据销毁记录。</Callout>

## 先预测，再操作三层章专属实验

<Stepper>
  <Step title="1. 规范—实现地图">选择正式节点，标出规范、HotSpot、发行版和应用证据。<Duj307ClassLoadingStructureLab /></Step>
  <Step title="2. 单变量探针">固定JDK与工作量，只切换触发方式或加载器。<Duj307ClassLoadingExecutionLab /></Step>
  <Step title="3. 故障、恢复与复位">注入“把类名相同误当运行时类型相同，忽略定义加载器”，从第一处状态偏离修复并同条件重放。<Duj307ClassLoadingEvidenceLab /></Step>
</Stepper>

## 官方目录逐项深读

### 第7章 虚拟机类加载机制

**四级证据 1/17。** 第7章 虚拟机类加载机制中的第7章 虚拟机类加载机制：记录二进制名、定义加载器、代码来源和加载初始化日志，用LinkageError或初始化反例限制结论。第7章 虚拟机类加载机制固定加载器为“platform”，只把触发方式从“主动使用”切到“反射”，以定义加载器、初始化日志与错误类型定位第一处变化并验证“类身份由二进制名和定义加载器共同决定，初始化顺序符合JVMS”。

### 7.1 概述

**四级证据 2/17。** 第7章 虚拟机类加载机制中的7.1 概述：先判定这是规范合同、HotSpot实现还是发行版行为，再用固定环境、原始输出和失败反例建立可重放证据。第7章 虚拟机类加载机制固定加载器为“platform”，只把触发方式从“主动使用”切到“反射”，以定义加载器、初始化日志与错误类型定位第一处变化并验证“类身份由二进制名和定义加载器共同决定，初始化顺序符合JVMS”。

### 7.2 类加载的时机

**四级证据 3/17。** 第7章 虚拟机类加载机制中的7.2 类加载的时机：记录二进制名、定义加载器、代码来源和加载初始化日志，用LinkageError或初始化反例限制结论。第7章 虚拟机类加载机制固定加载器为“platform”，只把触发方式从“主动使用”切到“反射”，以定义加载器、初始化日志与错误类型定位第一处变化并验证“类身份由二进制名和定义加载器共同决定，初始化顺序符合JVMS”。

### 7.3 类加载的过程

**四级证据 4/17。** 第7章 虚拟机类加载机制中的7.3 类加载的过程：记录二进制名、定义加载器、代码来源和加载初始化日志，用LinkageError或初始化反例限制结论。第7章 虚拟机类加载机制固定加载器为“platform”，只把触发方式从“主动使用”切到“反射”，以定义加载器、初始化日志与错误类型定位第一处变化并验证“类身份由二进制名和定义加载器共同决定，初始化顺序符合JVMS”。

### 7.3.1 加载

**四级证据 5/17。** 第7章 虚拟机类加载机制中的7.3.1 加载：记录二进制名、定义加载器、代码来源和加载初始化日志，用LinkageError或初始化反例限制结论。第7章 虚拟机类加载机制固定加载器为“platform”，只把触发方式从“主动使用”切到“反射”，以定义加载器、初始化日志与错误类型定位第一处变化并验证“类身份由二进制名和定义加载器共同决定，初始化顺序符合JVMS”。

### 7.3.2 验证

**四级证据 6/17。** 第7章 虚拟机类加载机制中的7.3.2 验证：先判定这是规范合同、HotSpot实现还是发行版行为，再用固定环境、原始输出和失败反例建立可重放证据。第7章 虚拟机类加载机制固定加载器为“platform”，只把触发方式从“主动使用”切到“反射”，以定义加载器、初始化日志与错误类型定位第一处变化并验证“类身份由二进制名和定义加载器共同决定，初始化顺序符合JVMS”。

### 7.3.3 准备

**四级证据 7/17。** 第7章 虚拟机类加载机制中的7.3.3 准备：先判定这是规范合同、HotSpot实现还是发行版行为，再用固定环境、原始输出和失败反例建立可重放证据。第7章 虚拟机类加载机制固定加载器为“platform”，只把触发方式从“主动使用”切到“反射”，以定义加载器、初始化日志与错误类型定位第一处变化并验证“类身份由二进制名和定义加载器共同决定，初始化顺序符合JVMS”。

### 7.3.4 解析

**四级证据 8/17。** 第7章 虚拟机类加载机制中的7.3.4 解析：先判定这是规范合同、HotSpot实现还是发行版行为，再用固定环境、原始输出和失败反例建立可重放证据。第7章 虚拟机类加载机制固定加载器为“platform”，只把触发方式从“主动使用”切到“反射”，以定义加载器、初始化日志与错误类型定位第一处变化并验证“类身份由二进制名和定义加载器共同决定，初始化顺序符合JVMS”。

### 7.3.5 初始化

**四级证据 9/17。** 第7章 虚拟机类加载机制中的7.3.5 初始化：记录二进制名、定义加载器、代码来源和加载初始化日志，用LinkageError或初始化反例限制结论。第7章 虚拟机类加载机制固定加载器为“platform”，只把触发方式从“主动使用”切到“反射”，以定义加载器、初始化日志与错误类型定位第一处变化并验证“类身份由二进制名和定义加载器共同决定，初始化顺序符合JVMS”。

### 7.4 类加载器

**四级证据 10/17。** 第7章 虚拟机类加载机制中的7.4 类加载器：记录二进制名、定义加载器、代码来源和加载初始化日志，用LinkageError或初始化反例限制结论。第7章 虚拟机类加载机制固定加载器为“platform”，只把触发方式从“主动使用”切到“反射”，以定义加载器、初始化日志与错误类型定位第一处变化并验证“类身份由二进制名和定义加载器共同决定，初始化顺序符合JVMS”。

### 7.4.1 类与类加载器

**四级证据 11/17。** 第7章 虚拟机类加载机制中的7.4.1 类与类加载器：记录二进制名、定义加载器、代码来源和加载初始化日志，用LinkageError或初始化反例限制结论。第7章 虚拟机类加载机制固定加载器为“platform”，只把触发方式从“主动使用”切到“反射”，以定义加载器、初始化日志与错误类型定位第一处变化并验证“类身份由二进制名和定义加载器共同决定，初始化顺序符合JVMS”。

### 7.4.2 双亲委派模型

**四级证据 12/17。** 第7章 虚拟机类加载机制中的7.4.2 双亲委派模型：先判定这是规范合同、HotSpot实现还是发行版行为，再用固定环境、原始输出和失败反例建立可重放证据。第7章 虚拟机类加载机制固定加载器为“platform”，只把触发方式从“主动使用”切到“反射”，以定义加载器、初始化日志与错误类型定位第一处变化并验证“类身份由二进制名和定义加载器共同决定，初始化顺序符合JVMS”。

### 7.4.3 破坏双亲委派模型

**四级证据 13/17。** 第7章 虚拟机类加载机制中的7.4.3 破坏双亲委派模型：先判定这是规范合同、HotSpot实现还是发行版行为，再用固定环境、原始输出和失败反例建立可重放证据。第7章 虚拟机类加载机制固定加载器为“platform”，只把触发方式从“主动使用”切到“反射”，以定义加载器、初始化日志与错误类型定位第一处变化并验证“类身份由二进制名和定义加载器共同决定，初始化顺序符合JVMS”。

### 7.5 Java模块化系统

**四级证据 14/17。** 第7章 虚拟机类加载机制中的7.5 Java模块化系统：先判定这是规范合同、HotSpot实现还是发行版行为，再用固定环境、原始输出和失败反例建立可重放证据。第7章 虚拟机类加载机制固定加载器为“platform”，只把触发方式从“主动使用”切到“反射”，以定义加载器、初始化日志与错误类型定位第一处变化并验证“类身份由二进制名和定义加载器共同决定，初始化顺序符合JVMS”。

### 7.5.1 模块的兼容性

**四级证据 15/17。** 第7章 虚拟机类加载机制中的7.5.1 模块的兼容性：先判定这是规范合同、HotSpot实现还是发行版行为，再用固定环境、原始输出和失败反例建立可重放证据。第7章 虚拟机类加载机制固定加载器为“platform”，只把触发方式从“主动使用”切到“反射”，以定义加载器、初始化日志与错误类型定位第一处变化并验证“类身份由二进制名和定义加载器共同决定，初始化顺序符合JVMS”。

### 7.5.2 模块化下的类加载器

**四级证据 16/17。** 第7章 虚拟机类加载机制中的7.5.2 模块化下的类加载器：记录二进制名、定义加载器、代码来源和加载初始化日志，用LinkageError或初始化反例限制结论。第7章 虚拟机类加载机制固定加载器为“platform”，只把触发方式从“主动使用”切到“反射”，以定义加载器、初始化日志与错误类型定位第一处变化并验证“类身份由二进制名和定义加载器共同决定，初始化顺序符合JVMS”。

### 7.6 本章小结

**四级证据 17/17。** 第7章 虚拟机类加载机制中的7.6 本章小结：先判定这是规范合同、HotSpot实现还是发行版行为，再用固定环境、原始输出和失败反例建立可重放证据。第7章 虚拟机类加载机制固定加载器为“platform”，只把触发方式从“主动使用”切到“反射”，以定义加载器、初始化日志与错误类型定位第一处变化并验证“类身份由二进制名和定义加载器共同决定，初始化顺序符合JVMS”。

## 三个必须主动触发的误区

<Callout type="trap" title="实现图不等于规范">第7章 虚拟机类加载机制中的对象头、收集器默认值、JIT阈值、加载器结构和工具能力都可能随实现或版本变化；只有JVMS/JLS合同可以在对应Java SE版本内作为规范主张。</Callout>

<Callout type="trap" title="章专属失败样本">第7章 虚拟机类加载机制主动触发“把加载和初始化混为一谈、只按类名判断同一类型，或机械打破双亲委派造成重复类与隔离漏洞”，并进一步注入“把类名相同误当运行时类型相同，忽略定义加载器”；若定义加载器、初始化日志与错误类型未出现，先验证采集链路和阳性对照，不得挑选一条符合预期的运行。</Callout>

<Callout type="trap" title="参数不能先于问题">第7章 虚拟机类加载机制只有在SLO、基线、假设、单变量、资源代价和回滚阈值齐全时才允许改变VM参数；编译通过、进程存活或平均值改善都不能替代正确性和尾部证据。</Callout>

## 练习、答案与282节点验证

<Exercises>

**问题1：探针改变。** 如何隔离触发方式对定义加载器、初始化日志与错误类型的因果影响？

<Answer>第7章 虚拟机类加载机制固定镜像、参数、工作量、加载器与采集窗口，只把触发方式从“被动引用”切到“反射”；保存原始输出、退出码、资源和探针成本，再重置并确认基线可重复。</Answer>

**问题2：四级证据。** 怎样证明本页17个目录节点不是标题复述？

<Answer>
1. 第7章 虚拟机类加载机制：说明来源层次，执行适用探针，触发一个边界反例，并用定义加载器、初始化日志与错误类型断言恢复。
2. 7.1 概述：说明来源层次，执行适用探针，触发一个边界反例，并用加载状态机、初始化触发矩阵、加载器委派图、类身份实验、模块可读性与导出表断言恢复。
3. 7.2 类加载的时机：说明来源层次，执行适用探针，触发一个边界反例，并用定义加载器、初始化日志与错误类型断言恢复。
4. 7.3 类加载的过程：说明来源层次，执行适用探针，触发一个边界反例，并用加载状态机、初始化触发矩阵、加载器委派图、类身份实验、模块可读性与导出表断言恢复。
5. 7.3.1 加载：说明来源层次，执行适用探针，触发一个边界反例，并用定义加载器、初始化日志与错误类型断言恢复。
6. 7.3.2 验证：说明来源层次，执行适用探针，触发一个边界反例，并用加载状态机、初始化触发矩阵、加载器委派图、类身份实验、模块可读性与导出表断言恢复。
7. 7.3.3 准备：说明来源层次，执行适用探针，触发一个边界反例，并用定义加载器、初始化日志与错误类型断言恢复。
8. 7.3.4 解析：说明来源层次，执行适用探针，触发一个边界反例，并用加载状态机、初始化触发矩阵、加载器委派图、类身份实验、模块可读性与导出表断言恢复。
9. 7.3.5 初始化：说明来源层次，执行适用探针，触发一个边界反例，并用定义加载器、初始化日志与错误类型断言恢复。
10. 7.4 类加载器：说明来源层次，执行适用探针，触发一个边界反例，并用加载状态机、初始化触发矩阵、加载器委派图、类身份实验、模块可读性与导出表断言恢复。
11. 7.4.1 类与类加载器：说明来源层次，执行适用探针，触发一个边界反例，并用定义加载器、初始化日志与错误类型断言恢复。
12. 7.4.2 双亲委派模型：说明来源层次，执行适用探针，触发一个边界反例，并用加载状态机、初始化触发矩阵、加载器委派图、类身份实验、模块可读性与导出表断言恢复。
13. 7.4.3 破坏双亲委派模型：说明来源层次，执行适用探针，触发一个边界反例，并用定义加载器、初始化日志与错误类型断言恢复。
14. 7.5 Java模块化系统：说明来源层次，执行适用探针，触发一个边界反例，并用加载状态机、初始化触发矩阵、加载器委派图、类身份实验、模块可读性与导出表断言恢复。
15. 7.5.1 模块的兼容性：说明来源层次，执行适用探针，触发一个边界反例，并用定义加载器、初始化日志与错误类型断言恢复。
16. 7.5.2 模块化下的类加载器：说明来源层次，执行适用探针，触发一个边界反例，并用加载状态机、初始化触发矩阵、加载器委派图、类身份实验、模块可读性与导出表断言恢复。
17. 7.6 本章小结：说明来源层次，执行适用探针，触发一个边界反例，并用定义加载器、初始化日志与错误类型断言恢复。
</Answer>

**问题3：恢复闭环。** 如何证明“把类名相同误当运行时类型相同，忽略定义加载器”已经修复？

<Answer>第7章 虚拟机类加载机制沿冻结JDK与输入、区分规范和实现、执行章专属探针、注入失败并恢复、保存原始发布证据定位第一处偏离，只改最小因果条件；删除旧Class、日志、JFR、转储、编译缓存或线程状态等适用派生物，以同输入重跑，直到“类身份由二进制名和定义加载器共同决定，初始化顺序符合JVMS”恢复且加载状态机、初始化触发矩阵、加载器委派图、类身份实验、模块可读性与导出表可由另一位读者复核。</Answer>

</Exercises>

<Glossary>
  <GlossaryItem term="loader identity → load → verify → prepare → resolve → initialize">loader identity → load → verify → prepare → resolve → initialize是第7章 虚拟机类加载机制连接冻结JDK与输入与可推翻结论的章专属坐标，必须记录版本、输入、单位和探针影响。</GlossaryItem>
  <GlossaryItem term="触发方式">触发方式是第7章 虚拟机类加载机制连接区分规范和实现与可推翻结论的章专属坐标，必须记录版本、输入、单位和探针影响。</GlossaryItem>
  <GlossaryItem term="加载器">加载器是第7章 虚拟机类加载机制连接执行章专属探针与可推翻结论的章专属坐标，必须记录版本、输入、单位和探针影响。</GlossaryItem>
  <GlossaryItem term="定义加载器、初始化日志与错误类型">定义加载器、初始化日志与错误类型是第7章 虚拟机类加载机制连接注入失败并恢复与可推翻结论的章专属坐标，必须记录版本、输入、单位和探针影响。</GlossaryItem>
  <GlossaryItem term="加载状态机、初始化触发矩阵、加载器委派图、类身份实验、模块可读性与导出表">加载状态机、初始化触发矩阵、加载器委派图、类身份实验、模块可读性与导出表是第7章 虚拟机类加载机制连接保存原始发布证据与可推翻结论的章专属坐标，必须记录版本、输入、单位和探针影响。</GlossaryItem>
</Glossary>

<Attribution mode="independent-rewrite" sourceBasis="outline-only" workTitle="周志明《深入理解Java虚拟机》第3版" adaptedUrl="https://github.com/fenixsoft/jvm_book" />
