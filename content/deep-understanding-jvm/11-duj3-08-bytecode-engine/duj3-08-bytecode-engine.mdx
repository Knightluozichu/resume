---
title: 第8章 虚拟机字节码执行引擎
description: 第8章 虚拟机字节码执行引擎覆盖22个正式目录节点，以章专属JVM探针、状态交互、故障恢复和版本证据验收。
type: C
section: 深入理解Java虚拟机（第3版 / 2019年JDK 12基线）
draft: false
math: true
sourceUrl: 'https://github.com/fenixsoft/jvm_book'
demo: true
order: 0
qualityVersion: 2
practiceMode: diagnosis
sourceMode: independent-rewrite
---


{/* DUJ3_QUALITY_V2 */}

## 为什么从“栈帧与分派台”开始

第8章 虚拟机字节码执行引擎不能把语言、JVMS、HotSpot和某个JDK发行版混成一层。栈帧与分派台沿“locals/operand stack → invoke opcode → target → return”保存输入和原始输出，让每个结论都能回到规范条款、实现状态和目标版本。

第8章 虚拟机字节码执行引擎固定JDK镜像、VM参数、工作量、采集窗口和资源限制，只改变调用指令或执行形态；图中只呈现本章真实运行时对象、状态边与证据出口；高亮表示当前分析路径，不冒充真实吞吐、停顿、对象大小或线程安全证明。

## 来源、勘误与独立重写边界

第8章 虚拟机字节码执行引擎依据作者[第3版样例与持续勘误仓库](https://github.com/fenixsoft/jvm_book)核定代码和已知修正，并用[出版社版式目录](https://18636251.s21i.faiusr.com/61/ABUIABA9GAAg8dqTugYo3O7rmwQ.pdf)限定5个部分、13章、附录A–E和282个正式节点。第8章 虚拟机字节码执行引擎未取得出版正文授权，目录只限定范围，中文解释、命令、图示、实验与答案均为独立教学重写。

第8章 虚拟机字节码执行引擎保留原书2019年的JDK 12 / JDK 13预览期语境；现代复核以[JVMS 25](https://docs.oracle.com/javase/specs/jvms/se25/html/index.html)、[JLS 25线程与锁](https://docs.oracle.com/javase/specs/jls/se25/html/jls-17.html)、[JDK 25工具规范](https://docs.oracle.com/en/java/javase/25/docs/specs/man/index.html)、[OpenJDK 25发布页](https://openjdk.org/projects/jdk/25/)与[OpenJDK源码](https://github.com/openjdk/jdk)为事实依据。第8章 虚拟机字节码执行引擎的JDK 25注记不是原书内容，任何默认值和实现细节都必须重新测量。

## 本章跨层合同与章专属探针

、、、、。

第8章 虚拟机字节码执行引擎的通过不变量是“每个调用点的描述符、操作数栈、目标选择和返回值可追踪”。第8章 虚拟机字节码执行引擎的实验档案必须保存JDK与VM版本、供应商、OS/容器限制、参数、源码或Class哈希、工作量、时间窗、退出码、未过滤日志、探针成本和清理记录。

第8章 虚拟机字节码执行引擎使用以下章专属探针作为起点；读者必须替换PID或样例、触发失败并保存调用指令、描述符与栈映射帧，不能只复制命令截图：

```bash
javac --release 12 Dispatch.java
javap -v -c Dispatch
```


## 官方目录逐项深读

### 第8章 虚拟机字节码执行引擎

**四级证据 1/22。** 第8章 虚拟机字节码执行引擎中的第8章 虚拟机字节码执行引擎：从Class字节偏移和JVMS结构出发，用javap核对常量池、描述符、栈效应、调用点和验证条件。第8章 虚拟机字节码执行引擎固定执行形态为“编译”，只把调用指令从“invokestatic”切到“invokedynamic”，以调用指令、描述符与栈映射帧定位第一处变化并验证“每个调用点的描述符、操作数栈、目标选择和返回值可追踪”。

### 8.1 概述

**四级证据 2/22。** 第8章 虚拟机字节码执行引擎中的8.1 概述：先判定这是规范合同、HotSpot实现还是发行版行为，再用固定环境、原始输出和失败反例建立可重放证据。第8章 虚拟机字节码执行引擎固定执行形态为“编译”，只把调用指令从“invokestatic”切到“invokedynamic”，以调用指令、描述符与栈映射帧定位第一处变化并验证“每个调用点的描述符、操作数栈、目标选择和返回值可追踪”。

### 8.2 运行时栈帧结构

**四级证据 3/22。** 第8章 虚拟机字节码执行引擎中的8.2 运行时栈帧结构：区分堆、栈、元空间、直接内存与本地分配，限制资源后把异常和NMT差值映射到具体区域。第8章 虚拟机字节码执行引擎固定执行形态为“编译”，只把调用指令从“invokestatic”切到“invokedynamic”，以调用指令、描述符与栈映射帧定位第一处变化并验证“每个调用点的描述符、操作数栈、目标选择和返回值可追踪”。

### 8.2.1 局部变量表

**四级证据 4/22。** 第8章 虚拟机字节码执行引擎中的8.2.1 局部变量表：先判定这是规范合同、HotSpot实现还是发行版行为，再用固定环境、原始输出和失败反例建立可重放证据。第8章 虚拟机字节码执行引擎固定执行形态为“编译”，只把调用指令从“invokestatic”切到“invokedynamic”，以调用指令、描述符与栈映射帧定位第一处变化并验证“每个调用点的描述符、操作数栈、目标选择和返回值可追踪”。

### 8.2.2 操作数栈

**四级证据 5/22。** 第8章 虚拟机字节码执行引擎中的8.2.2 操作数栈：区分堆、栈、元空间、直接内存与本地分配，限制资源后把异常和NMT差值映射到具体区域。第8章 虚拟机字节码执行引擎固定执行形态为“编译”，只把调用指令从“invokestatic”切到“invokedynamic”，以调用指令、描述符与栈映射帧定位第一处变化并验证“每个调用点的描述符、操作数栈、目标选择和返回值可追踪”。

### 8.2.3 动态连接

**四级证据 6/22。** 第8章 虚拟机字节码执行引擎中的8.2.3 动态连接：先判定这是规范合同、HotSpot实现还是发行版行为，再用固定环境、原始输出和失败反例建立可重放证据。第8章 虚拟机字节码执行引擎固定执行形态为“编译”，只把调用指令从“invokestatic”切到“invokedynamic”，以调用指令、描述符与栈映射帧定位第一处变化并验证“每个调用点的描述符、操作数栈、目标选择和返回值可追踪”。

### 8.2.4 方法返回地址

**四级证据 7/22。** 第8章 虚拟机字节码执行引擎中的8.2.4 方法返回地址：先判定这是规范合同、HotSpot实现还是发行版行为，再用固定环境、原始输出和失败反例建立可重放证据。第8章 虚拟机字节码执行引擎固定执行形态为“编译”，只把调用指令从“invokestatic”切到“invokedynamic”，以调用指令、描述符与栈映射帧定位第一处变化并验证“每个调用点的描述符、操作数栈、目标选择和返回值可追踪”。

### 8.2.5 附加信息

**四级证据 8/22。** 第8章 虚拟机字节码执行引擎中的8.2.5 附加信息：先判定这是规范合同、HotSpot实现还是发行版行为，再用固定环境、原始输出和失败反例建立可重放证据。第8章 虚拟机字节码执行引擎固定执行形态为“编译”，只把调用指令从“invokestatic”切到“invokedynamic”，以调用指令、描述符与栈映射帧定位第一处变化并验证“每个调用点的描述符、操作数栈、目标选择和返回值可追踪”。

### 8.3 方法调用

**四级证据 9/22。** 第8章 虚拟机字节码执行引擎中的8.3 方法调用：从Class字节偏移和JVMS结构出发，用javap核对常量池、描述符、栈效应、调用点和验证条件。第8章 虚拟机字节码执行引擎固定执行形态为“编译”，只把调用指令从“invokestatic”切到“invokedynamic”，以调用指令、描述符与栈映射帧定位第一处变化并验证“每个调用点的描述符、操作数栈、目标选择和返回值可追踪”。

### 8.3.1 解析

**四级证据 10/22。** 第8章 虚拟机字节码执行引擎中的8.3.1 解析：先判定这是规范合同、HotSpot实现还是发行版行为，再用固定环境、原始输出和失败反例建立可重放证据。第8章 虚拟机字节码执行引擎固定执行形态为“编译”，只把调用指令从“invokestatic”切到“invokedynamic”，以调用指令、描述符与栈映射帧定位第一处变化并验证“每个调用点的描述符、操作数栈、目标选择和返回值可追踪”。

### 8.3.2 分派

**四级证据 11/22。** 第8章 虚拟机字节码执行引擎中的8.3.2 分派：从Class字节偏移和JVMS结构出发，用javap核对常量池、描述符、栈效应、调用点和验证条件。第8章 虚拟机字节码执行引擎固定执行形态为“编译”，只把调用指令从“invokestatic”切到“invokedynamic”，以调用指令、描述符与栈映射帧定位第一处变化并验证“每个调用点的描述符、操作数栈、目标选择和返回值可追踪”。

### 8.4 动态类型语言支持

**四级证据 12/22。** 第8章 虚拟机字节码执行引擎中的8.4 动态类型语言支持：先判定这是规范合同、HotSpot实现还是发行版行为，再用固定环境、原始输出和失败反例建立可重放证据。第8章 虚拟机字节码执行引擎固定执行形态为“编译”，只把调用指令从“invokestatic”切到“invokedynamic”，以调用指令、描述符与栈映射帧定位第一处变化并验证“每个调用点的描述符、操作数栈、目标选择和返回值可追踪”。

### 8.4.1 动态类型语言

**四级证据 13/22。** 第8章 虚拟机字节码执行引擎中的8.4.1 动态类型语言：先判定这是规范合同、HotSpot实现还是发行版行为，再用固定环境、原始输出和失败反例建立可重放证据。第8章 虚拟机字节码执行引擎固定执行形态为“编译”，只把调用指令从“invokestatic”切到“invokedynamic”，以调用指令、描述符与栈映射帧定位第一处变化并验证“每个调用点的描述符、操作数栈、目标选择和返回值可追踪”。

### 8.4.2 Java与动态类型

**四级证据 14/22。** 第8章 虚拟机字节码执行引擎中的8.4.2 Java与动态类型：先判定这是规范合同、HotSpot实现还是发行版行为，再用固定环境、原始输出和失败反例建立可重放证据。第8章 虚拟机字节码执行引擎固定执行形态为“编译”，只把调用指令从“invokestatic”切到“invokedynamic”，以调用指令、描述符与栈映射帧定位第一处变化并验证“每个调用点的描述符、操作数栈、目标选择和返回值可追踪”。

### 8.4.3 java.lang.invoke包

**四级证据 15/22。** 第8章 虚拟机字节码执行引擎中的8.4.3 java.lang.invoke包：先判定这是规范合同、HotSpot实现还是发行版行为，再用固定环境、原始输出和失败反例建立可重放证据。第8章 虚拟机字节码执行引擎固定执行形态为“编译”，只把调用指令从“invokestatic”切到“invokedynamic”，以调用指令、描述符与栈映射帧定位第一处变化并验证“每个调用点的描述符、操作数栈、目标选择和返回值可追踪”。

### 8.4.4 invokedynamic指令

**四级证据 16/22。** 第8章 虚拟机字节码执行引擎中的8.4.4 invokedynamic指令：从Class字节偏移和JVMS结构出发，用javap核对常量池、描述符、栈效应、调用点和验证条件。第8章 虚拟机字节码执行引擎固定执行形态为“编译”，只把调用指令从“invokestatic”切到“invokedynamic”，以调用指令、描述符与栈映射帧定位第一处变化并验证“每个调用点的描述符、操作数栈、目标选择和返回值可追踪”。

### 8.4.5 实战：掌控方法分派规则

**四级证据 17/22。** 第8章 虚拟机字节码执行引擎中的8.4.5 实战：掌控方法分派规则：从Class字节偏移和JVMS结构出发，用javap核对常量池、描述符、栈效应、调用点和验证条件。第8章 虚拟机字节码执行引擎固定执行形态为“编译”，只把调用指令从“invokestatic”切到“invokedynamic”，以调用指令、描述符与栈映射帧定位第一处变化并验证“每个调用点的描述符、操作数栈、目标选择和返回值可追踪”。

### 8.5 基于栈的字节码解释执行引擎

**四级证据 18/22。** 第8章 虚拟机字节码执行引擎中的8.5 基于栈的字节码解释执行引擎：区分堆、栈、元空间、直接内存与本地分配，限制资源后把异常和NMT差值映射到具体区域。第8章 虚拟机字节码执行引擎固定执行形态为“编译”，只把调用指令从“invokestatic”切到“invokedynamic”，以调用指令、描述符与栈映射帧定位第一处变化并验证“每个调用点的描述符、操作数栈、目标选择和返回值可追踪”。

### 8.5.1 解释执行

**四级证据 19/22。** 第8章 虚拟机字节码执行引擎中的8.5.1 解释执行：先判定这是规范合同、HotSpot实现还是发行版行为，再用固定环境、原始输出和失败反例建立可重放证据。第8章 虚拟机字节码执行引擎固定执行形态为“编译”，只把调用指令从“invokestatic”切到“invokedynamic”，以调用指令、描述符与栈映射帧定位第一处变化并验证“每个调用点的描述符、操作数栈、目标选择和返回值可追踪”。

### 8.5.2 基于栈的指令集与基于寄存器的指令集

**四级证据 20/22。** 第8章 虚拟机字节码执行引擎中的8.5.2 基于栈的指令集与基于寄存器的指令集：区分堆、栈、元空间、直接内存与本地分配，限制资源后把异常和NMT差值映射到具体区域。第8章 虚拟机字节码执行引擎固定执行形态为“编译”，只把调用指令从“invokestatic”切到“invokedynamic”，以调用指令、描述符与栈映射帧定位第一处变化并验证“每个调用点的描述符、操作数栈、目标选择和返回值可追踪”。

### 8.5.3 基于栈的解释器执行过程

**四级证据 21/22。** 第8章 虚拟机字节码执行引擎中的8.5.3 基于栈的解释器执行过程：区分堆、栈、元空间、直接内存与本地分配，限制资源后把异常和NMT差值映射到具体区域。第8章 虚拟机字节码执行引擎固定执行形态为“编译”，只把调用指令从“invokestatic”切到“invokedynamic”，以调用指令、描述符与栈映射帧定位第一处变化并验证“每个调用点的描述符、操作数栈、目标选择和返回值可追踪”。

### 8.6 本章小结

**四级证据 22/22。** 第8章 虚拟机字节码执行引擎中的8.6 本章小结：先判定这是规范合同、HotSpot实现还是发行版行为，再用固定环境、原始输出和失败反例建立可重放证据。第8章 虚拟机字节码执行引擎固定执行形态为“编译”，只把调用指令从“invokestatic”切到“invokedynamic”，以调用指令、描述符与栈映射帧定位第一处变化并验证“每个调用点的描述符、操作数栈、目标选择和返回值可追踪”。

## 三个必须主动触发的误区


## 练习、答案与282节点验证


