---
title: 第11章 后端编译与优化
description: 第11章 后端编译与优化覆盖23个正式目录节点，以章专属JVM探针、状态交互、故障恢复和版本证据验收。
type: C
section: 深入理解Java虚拟机（第3版 / 2019年JDK 12基线）
draft: false
math: true
sourceUrl: 'https://github.com/fenixsoft/jvm_book'
demo: true
order: 0
qualityVersion: 2
practiceMode: diagnosis
sourceMode: independent-rewrite
---


{/* DUJ3_QUALITY_V2 */}

## 为什么从“分层编译与去优化台”开始

第11章 后端编译与优化不能把语言、JVMS、HotSpot和某个JDK发行版混成一层。分层编译与去优化台沿“profile → compile tier → inline/speculate → uncommon trap”保存输入和原始输出，让每个结论都能回到规范条款、实现状态和目标版本。

第11章 后端编译与优化固定JDK镜像、VM参数、工作量、采集窗口和资源限制，只改变执行阶段或优化事件；图中只呈现本章真实运行时对象、状态边与证据出口；高亮表示当前分析路径，不冒充真实吞吐、停顿、对象大小或线程安全证明。

## 来源、勘误与独立重写边界

第11章 后端编译与优化依据作者[第3版样例与持续勘误仓库](https://github.com/fenixsoft/jvm_book)核定代码和已知修正，并用[出版社版式目录](https://18636251.s21i.faiusr.com/61/ABUIABA9GAAg8dqTugYo3O7rmwQ.pdf)限定5个部分、13章、附录A–E和282个正式节点。第11章 后端编译与优化未取得出版正文授权，目录只限定范围，中文解释、命令、图示、实验与答案均为独立教学重写。

第11章 后端编译与优化保留原书2019年的JDK 12 / JDK 13预览期语境；现代复核以[JVMS 25](https://docs.oracle.com/javase/specs/jvms/se25/html/index.html)、[JLS 25线程与锁](https://docs.oracle.com/javase/specs/jls/se25/html/jls-17.html)、[JDK 25工具规范](https://docs.oracle.com/en/java/javase/25/docs/specs/man/index.html)、[OpenJDK 25发布页](https://openjdk.org/projects/jdk/25/)与[OpenJDK源码](https://github.com/openjdk/jdk)为事实依据。第11章 后端编译与优化的JDK 25注记不是原书内容，任何默认值和实现细节都必须重新测量。

## 本章跨层合同与章专属探针

、、、、。

第11章 后端编译与优化的通过不变量是“编译事件对应同一工作量，收益不以代码缓存或尾延迟恶化换取”。第11章 后端编译与优化的实验档案必须保存JDK与VM版本、供应商、OS/容器限制、参数、源码或Class哈希、工作量、时间窗、退出码、未过滤日志、探针成本和清理记录。

第11章 后端编译与优化使用以下章专属探针作为起点；读者必须替换PID或样例、触发失败并保存编译层级、内联决定与去优化，不能只复制命令截图：

```bash
java -XX:+UnlockDiagnosticVMOptions -XX:+PrintCompilation -XX:+PrintInlining JitProbe
```


## 官方目录逐项深读

### 第11章 后端编译与优化

**四级证据 1/23。** 第11章 后端编译与优化中的第11章 后端编译与优化：区分前端、解释、分层编译与去优化，固定预热和工作量后保存Class、编译事件与回退证据。第11章 后端编译与优化固定优化事件为“内联”，只把执行阶段从“冷”切到“稳态”，以编译层级、内联决定与去优化定位第一处变化并验证“编译事件对应同一工作量，收益不以代码缓存或尾延迟恶化换取”。

### 11.1 概述

**四级证据 2/23。** 第11章 后端编译与优化中的11.1 概述：先判定这是规范合同、HotSpot实现还是发行版行为，再用固定环境、原始输出和失败反例建立可重放证据。第11章 后端编译与优化固定优化事件为“内联”，只把执行阶段从“冷”切到“稳态”，以编译层级、内联决定与去优化定位第一处变化并验证“编译事件对应同一工作量，收益不以代码缓存或尾延迟恶化换取”。

### 11.2 即时编译器

**四级证据 3/23。** 第11章 后端编译与优化中的11.2 即时编译器：区分前端、解释、分层编译与去优化，固定预热和工作量后保存Class、编译事件与回退证据。第11章 后端编译与优化固定优化事件为“内联”，只把执行阶段从“冷”切到“稳态”，以编译层级、内联决定与去优化定位第一处变化并验证“编译事件对应同一工作量，收益不以代码缓存或尾延迟恶化换取”。

### 11.2.1 解释器与编译器

**四级证据 4/23。** 第11章 后端编译与优化中的11.2.1 解释器与编译器：区分前端、解释、分层编译与去优化，固定预热和工作量后保存Class、编译事件与回退证据。第11章 后端编译与优化固定优化事件为“内联”，只把执行阶段从“冷”切到“稳态”，以编译层级、内联决定与去优化定位第一处变化并验证“编译事件对应同一工作量，收益不以代码缓存或尾延迟恶化换取”。

### 11.2.2 编译对象与触发条件

**四级证据 5/23。** 第11章 后端编译与优化中的11.2.2 编译对象与触发条件：区分堆、栈、元空间、直接内存与本地分配，限制资源后把异常和NMT差值映射到具体区域。第11章 后端编译与优化固定优化事件为“内联”，只把执行阶段从“冷”切到“稳态”，以编译层级、内联决定与去优化定位第一处变化并验证“编译事件对应同一工作量，收益不以代码缓存或尾延迟恶化换取”。

### 11.2.3 编译过程

**四级证据 6/23。** 第11章 后端编译与优化中的11.2.3 编译过程：区分前端、解释、分层编译与去优化，固定预热和工作量后保存Class、编译事件与回退证据。第11章 后端编译与优化固定优化事件为“内联”，只把执行阶段从“冷”切到“稳态”，以编译层级、内联决定与去优化定位第一处变化并验证“编译事件对应同一工作量，收益不以代码缓存或尾延迟恶化换取”。

### 11.2.4 实战：查看及分析即时编译结果

**四级证据 7/23。** 第11章 后端编译与优化中的11.2.4 实战：查看及分析即时编译结果：区分前端、解释、分层编译与去优化，固定预热和工作量后保存Class、编译事件与回退证据。第11章 后端编译与优化固定优化事件为“内联”，只把执行阶段从“冷”切到“稳态”，以编译层级、内联决定与去优化定位第一处变化并验证“编译事件对应同一工作量，收益不以代码缓存或尾延迟恶化换取”。

### 11.3 提前编译器

**四级证据 8/23。** 第11章 后端编译与优化中的11.3 提前编译器：区分前端、解释、分层编译与去优化，固定预热和工作量后保存Class、编译事件与回退证据。第11章 后端编译与优化固定优化事件为“内联”，只把执行阶段从“冷”切到“稳态”，以编译层级、内联决定与去优化定位第一处变化并验证“编译事件对应同一工作量，收益不以代码缓存或尾延迟恶化换取”。

### 11.3.1 提前编译的优劣得失

**四级证据 9/23。** 第11章 后端编译与优化中的11.3.1 提前编译的优劣得失：区分前端、解释、分层编译与去优化，固定预热和工作量后保存Class、编译事件与回退证据。第11章 后端编译与优化固定优化事件为“内联”，只把执行阶段从“冷”切到“稳态”，以编译层级、内联决定与去优化定位第一处变化并验证“编译事件对应同一工作量，收益不以代码缓存或尾延迟恶化换取”。

### 11.3.2 实战：Jaotc的提前编译

**四级证据 10/23。** 第11章 后端编译与优化中的11.3.2 实战：Jaotc的提前编译：区分前端、解释、分层编译与去优化，固定预热和工作量后保存Class、编译事件与回退证据。第11章 后端编译与优化固定优化事件为“内联”，只把执行阶段从“冷”切到“稳态”，以编译层级、内联决定与去优化定位第一处变化并验证“编译事件对应同一工作量，收益不以代码缓存或尾延迟恶化换取”。

### 11.4 编译器优化技术

**四级证据 11/23。** 第11章 后端编译与优化中的11.4 编译器优化技术：区分前端、解释、分层编译与去优化，固定预热和工作量后保存Class、编译事件与回退证据。第11章 后端编译与优化固定优化事件为“内联”，只把执行阶段从“冷”切到“稳态”，以编译层级、内联决定与去优化定位第一处变化并验证“编译事件对应同一工作量，收益不以代码缓存或尾延迟恶化换取”。

### 11.4.1 优化技术概览

**四级证据 12/23。** 第11章 后端编译与优化中的11.4.1 优化技术概览：区分前端、解释、分层编译与去优化，固定预热和工作量后保存Class、编译事件与回退证据。第11章 后端编译与优化固定优化事件为“内联”，只把执行阶段从“冷”切到“稳态”，以编译层级、内联决定与去优化定位第一处变化并验证“编译事件对应同一工作量，收益不以代码缓存或尾延迟恶化换取”。

### 11.4.2 方法内联

**四级证据 13/23。** 第11章 后端编译与优化中的11.4.2 方法内联：区分前端、解释、分层编译与去优化，固定预热和工作量后保存Class、编译事件与回退证据。第11章 后端编译与优化固定优化事件为“内联”，只把执行阶段从“冷”切到“稳态”，以编译层级、内联决定与去优化定位第一处变化并验证“编译事件对应同一工作量，收益不以代码缓存或尾延迟恶化换取”。

### 11.4.3 逃逸分析

**四级证据 14/23。** 第11章 后端编译与优化中的11.4.3 逃逸分析：区分前端、解释、分层编译与去优化，固定预热和工作量后保存Class、编译事件与回退证据。第11章 后端编译与优化固定优化事件为“内联”，只把执行阶段从“冷”切到“稳态”，以编译层级、内联决定与去优化定位第一处变化并验证“编译事件对应同一工作量，收益不以代码缓存或尾延迟恶化换取”。

### 11.4.4 公共子表达式消除

**四级证据 15/23。** 第11章 后端编译与优化中的11.4.4 公共子表达式消除：先判定这是规范合同、HotSpot实现还是发行版行为，再用固定环境、原始输出和失败反例建立可重放证据。第11章 后端编译与优化固定优化事件为“内联”，只把执行阶段从“冷”切到“稳态”，以编译层级、内联决定与去优化定位第一处变化并验证“编译事件对应同一工作量，收益不以代码缓存或尾延迟恶化换取”。

### 11.4.5 数组边界检查消除

**四级证据 16/23。** 第11章 后端编译与优化中的11.4.5 数组边界检查消除：先判定这是规范合同、HotSpot实现还是发行版行为，再用固定环境、原始输出和失败反例建立可重放证据。第11章 后端编译与优化固定优化事件为“内联”，只把执行阶段从“冷”切到“稳态”，以编译层级、内联决定与去优化定位第一处变化并验证“编译事件对应同一工作量，收益不以代码缓存或尾延迟恶化换取”。

### 11.5 实战：深入理解Graal编译器

**四级证据 17/23。** 第11章 后端编译与优化中的11.5 实战：深入理解Graal编译器：区分前端、解释、分层编译与去优化，固定预热和工作量后保存Class、编译事件与回退证据。第11章 后端编译与优化固定优化事件为“内联”，只把执行阶段从“冷”切到“稳态”，以编译层级、内联决定与去优化定位第一处变化并验证“编译事件对应同一工作量，收益不以代码缓存或尾延迟恶化换取”。

### 11.5.1 历史背景

**四级证据 18/23。** 第11章 后端编译与优化中的11.5.1 历史背景：固定日期、源码和提案状态，区分当时证据、预测与后来结果，不用后见之明改写历史语境。第11章 后端编译与优化固定优化事件为“内联”，只把执行阶段从“冷”切到“稳态”，以编译层级、内联决定与去优化定位第一处变化并验证“编译事件对应同一工作量，收益不以代码缓存或尾延迟恶化换取”。

### 11.5.2 构建编译调试环境

**四级证据 19/23。** 第11章 后端编译与优化中的11.5.2 构建编译调试环境：区分前端、解释、分层编译与去优化，固定预热和工作量后保存Class、编译事件与回退证据。第11章 后端编译与优化固定优化事件为“内联”，只把执行阶段从“冷”切到“稳态”，以编译层级、内联决定与去优化定位第一处变化并验证“编译事件对应同一工作量，收益不以代码缓存或尾延迟恶化换取”。

### 11.5.3 JVMCI编译器接口

**四级证据 20/23。** 第11章 后端编译与优化中的11.5.3 JVMCI编译器接口：区分前端、解释、分层编译与去优化，固定预热和工作量后保存Class、编译事件与回退证据。第11章 后端编译与优化固定优化事件为“内联”，只把执行阶段从“冷”切到“稳态”，以编译层级、内联决定与去优化定位第一处变化并验证“编译事件对应同一工作量，收益不以代码缓存或尾延迟恶化换取”。

### 11.5.4 代码中间表示

**四级证据 21/23。** 第11章 后端编译与优化中的11.5.4 代码中间表示：先判定这是规范合同、HotSpot实现还是发行版行为，再用固定环境、原始输出和失败反例建立可重放证据。第11章 后端编译与优化固定优化事件为“内联”，只把执行阶段从“冷”切到“稳态”，以编译层级、内联决定与去优化定位第一处变化并验证“编译事件对应同一工作量，收益不以代码缓存或尾延迟恶化换取”。

### 11.5.5 代码优化与生成

**四级证据 22/23。** 第11章 后端编译与优化中的11.5.5 代码优化与生成：区分前端、解释、分层编译与去优化，固定预热和工作量后保存Class、编译事件与回退证据。第11章 后端编译与优化固定优化事件为“内联”，只把执行阶段从“冷”切到“稳态”，以编译层级、内联决定与去优化定位第一处变化并验证“编译事件对应同一工作量，收益不以代码缓存或尾延迟恶化换取”。

### 11.6 本章小结

**四级证据 23/23。** 第11章 后端编译与优化中的11.6 本章小结：先判定这是规范合同、HotSpot实现还是发行版行为，再用固定环境、原始输出和失败反例建立可重放证据。第11章 后端编译与优化固定优化事件为“内联”，只把执行阶段从“冷”切到“稳态”，以编译层级、内联决定与去优化定位第一处变化并验证“编译事件对应同一工作量，收益不以代码缓存或尾延迟恶化换取”。

## 三个必须主动触发的误区


## 练习、答案与282节点验证


