---
title: 第12章 Java内存模型与线程
description: 第12章 Java内存模型与线程覆盖19个正式目录节点，以章专属JVM探针、状态交互、故障恢复和版本证据验收。
type: C
section: 深入理解Java虚拟机（第3版 / 2019年JDK 12基线）
draft: false
math: true
sourceUrl: 'https://github.com/fenixsoft/jvm_book'
demo: true
order: 0
qualityVersion: 2
practiceMode: diagnosis
sourceMode: independent-rewrite
---


{/* DUJ3_QUALITY_V2 */}

## 为什么从“happens-before与线程台”开始

第12章 Java内存模型与线程不能把语言、JVMS、HotSpot和某个JDK发行版混成一层。happens-before与线程台沿“actions → synchronization order → visibility/ordering”保存输入和原始输出，让每个结论都能回到规范条款、实现状态和目标版本。

第12章 Java内存模型与线程固定JDK镜像、VM参数、工作量、采集窗口和资源限制，只改变共享方式或线程载体；图中只呈现本章真实运行时对象、状态边与证据出口；高亮表示当前分析路径，不冒充真实吞吐、停顿、对象大小或线程安全证明。

## 来源、勘误与独立重写边界

第12章 Java内存模型与线程依据作者[第3版样例与持续勘误仓库](https://github.com/fenixsoft/jvm_book)核定代码和已知修正，并用[出版社版式目录](https://18636251.s21i.faiusr.com/61/ABUIABA9GAAg8dqTugYo3O7rmwQ.pdf)限定5个部分、13章、附录A–E和282个正式节点。第12章 Java内存模型与线程未取得出版正文授权，目录只限定范围，中文解释、命令、图示、实验与答案均为独立教学重写。

第12章 Java内存模型与线程保留原书2019年的JDK 12 / JDK 13预览期语境；现代复核以[JVMS 25](https://docs.oracle.com/javase/specs/jvms/se25/html/index.html)、[JLS 25线程与锁](https://docs.oracle.com/javase/specs/jls/se25/html/jls-17.html)、[JDK 25工具规范](https://docs.oracle.com/en/java/javase/25/docs/specs/man/index.html)、[OpenJDK 25发布页](https://openjdk.org/projects/jdk/25/)与[OpenJDK源码](https://github.com/openjdk/jdk)为事实依据。第12章 Java内存模型与线程的JDK 25注记不是原书内容，任何默认值和实现细节都必须重新测量。

## 本章跨层合同与章专属探针

、、、、。

第12章 Java内存模型与线程的通过不变量是“所有共享读都由明确happens-before边或安全发布支撑”。第12章 Java内存模型与线程的实验档案必须保存JDK与VM版本、供应商、OS/容器限制、参数、源码或Class哈希、工作量、时间窗、退出码、未过滤日志、探针成本和清理记录。

第12章 Java内存模型与线程使用以下章专属探针作为起点；读者必须替换PID或样例、触发失败并保存反例计数、线程转储与同步边，不能只复制命令截图：

```bash
mvn -q -Dtest=JmmProbeTest test
jcmd PID Thread.print -l
```


## 官方目录逐项深读

### 第12章 Java内存模型与线程

**四级证据 1/19。** 第12章 Java内存模型与线程中的第12章 Java内存模型与线程：区分堆、栈、元空间、直接内存与本地分配，限制资源后把异常和NMT差值映射到具体区域。第12章 Java内存模型与线程固定线程载体为“虚拟”，只把共享方式从“普通字段”切到“锁保护”，以反例计数、线程转储与同步边定位第一处变化并验证“所有共享读都由明确happens-before边或安全发布支撑”。

### 12.1 概述

**四级证据 2/19。** 第12章 Java内存模型与线程中的12.1 概述：先判定这是规范合同、HotSpot实现还是发行版行为，再用固定环境、原始输出和失败反例建立可重放证据。第12章 Java内存模型与线程固定线程载体为“虚拟”，只把共享方式从“普通字段”切到“锁保护”，以反例计数、线程转储与同步边定位第一处变化并验证“所有共享读都由明确happens-before边或安全发布支撑”。

### 12.2 硬件的效率与一致性

**四级证据 3/19。** 第12章 Java内存模型与线程中的12.2 硬件的效率与一致性：先判定这是规范合同、HotSpot实现还是发行版行为，再用固定环境、原始输出和失败反例建立可重放证据。第12章 Java内存模型与线程固定线程载体为“虚拟”，只把共享方式从“普通字段”切到“锁保护”，以反例计数、线程转储与同步边定位第一处变化并验证“所有共享读都由明确happens-before边或安全发布支撑”。

### 12.3 Java内存模型

**四级证据 4/19。** 第12章 Java内存模型与线程中的12.3 Java内存模型：区分堆、栈、元空间、直接内存与本地分配，限制资源后把异常和NMT差值映射到具体区域。第12章 Java内存模型与线程固定线程载体为“虚拟”，只把共享方式从“普通字段”切到“锁保护”，以反例计数、线程转储与同步边定位第一处变化并验证“所有共享读都由明确happens-before边或安全发布支撑”。

### 12.3.1 主内存与工作内存

**四级证据 5/19。** 第12章 Java内存模型与线程中的12.3.1 主内存与工作内存：区分堆、栈、元空间、直接内存与本地分配，限制资源后把异常和NMT差值映射到具体区域。第12章 Java内存模型与线程固定线程载体为“虚拟”，只把共享方式从“普通字段”切到“锁保护”，以反例计数、线程转储与同步边定位第一处变化并验证“所有共享读都由明确happens-before边或安全发布支撑”。

### 12.3.2 内存间交互操作

**四级证据 6/19。** 第12章 Java内存模型与线程中的12.3.2 内存间交互操作：区分堆、栈、元空间、直接内存与本地分配，限制资源后把异常和NMT差值映射到具体区域。第12章 Java内存模型与线程固定线程载体为“虚拟”，只把共享方式从“普通字段”切到“锁保护”，以反例计数、线程转储与同步边定位第一处变化并验证“所有共享读都由明确happens-before边或安全发布支撑”。

### 12.3.3 对于volatile型变量的特殊规则

**四级证据 7/19。** 第12章 Java内存模型与线程中的12.3.3 对于volatile型变量的特殊规则：写出happens-before或临界区不变量，用反复并发测试、线程转储和锁事件证明正确性与竞争代价。第12章 Java内存模型与线程固定线程载体为“虚拟”，只把共享方式从“普通字段”切到“锁保护”，以反例计数、线程转储与同步边定位第一处变化并验证“所有共享读都由明确happens-before边或安全发布支撑”。

### 12.3.4 针对long和double型变量的特殊规则

**四级证据 8/19。** 第12章 Java内存模型与线程中的12.3.4 针对long和double型变量的特殊规则：先判定这是规范合同、HotSpot实现还是发行版行为，再用固定环境、原始输出和失败反例建立可重放证据。第12章 Java内存模型与线程固定线程载体为“虚拟”，只把共享方式从“普通字段”切到“锁保护”，以反例计数、线程转储与同步边定位第一处变化并验证“所有共享读都由明确happens-before边或安全发布支撑”。

### 12.3.5 原子性、可见性与有序性

**四级证据 9/19。** 第12章 Java内存模型与线程中的12.3.5 原子性、可见性与有序性：先判定这是规范合同、HotSpot实现还是发行版行为，再用固定环境、原始输出和失败反例建立可重放证据。第12章 Java内存模型与线程固定线程载体为“虚拟”，只把共享方式从“普通字段”切到“锁保护”，以反例计数、线程转储与同步边定位第一处变化并验证“所有共享读都由明确happens-before边或安全发布支撑”。

### 12.3.6 先行发生原则

**四级证据 10/19。** 第12章 Java内存模型与线程中的12.3.6 先行发生原则：先判定这是规范合同、HotSpot实现还是发行版行为，再用固定环境、原始输出和失败反例建立可重放证据。第12章 Java内存模型与线程固定线程载体为“虚拟”，只把共享方式从“普通字段”切到“锁保护”，以反例计数、线程转储与同步边定位第一处变化并验证“所有共享读都由明确happens-before边或安全发布支撑”。

### 12.4 Java与线程

**四级证据 11/19。** 第12章 Java内存模型与线程中的12.4 Java与线程：写出happens-before或临界区不变量，用反复并发测试、线程转储和锁事件证明正确性与竞争代价。第12章 Java内存模型与线程固定线程载体为“虚拟”，只把共享方式从“普通字段”切到“锁保护”，以反例计数、线程转储与同步边定位第一处变化并验证“所有共享读都由明确happens-before边或安全发布支撑”。

### 12.4.1 线程的实现

**四级证据 12/19。** 第12章 Java内存模型与线程中的12.4.1 线程的实现：写出happens-before或临界区不变量，用反复并发测试、线程转储和锁事件证明正确性与竞争代价。第12章 Java内存模型与线程固定线程载体为“虚拟”，只把共享方式从“普通字段”切到“锁保护”，以反例计数、线程转储与同步边定位第一处变化并验证“所有共享读都由明确happens-before边或安全发布支撑”。

### 12.4.2 Java线程调度

**四级证据 13/19。** 第12章 Java内存模型与线程中的12.4.2 Java线程调度：写出happens-before或临界区不变量，用反复并发测试、线程转储和锁事件证明正确性与竞争代价。第12章 Java内存模型与线程固定线程载体为“虚拟”，只把共享方式从“普通字段”切到“锁保护”，以反例计数、线程转储与同步边定位第一处变化并验证“所有共享读都由明确happens-before边或安全发布支撑”。

### 12.4.3 状态转换

**四级证据 14/19。** 第12章 Java内存模型与线程中的12.4.3 状态转换：先判定这是规范合同、HotSpot实现还是发行版行为，再用固定环境、原始输出和失败反例建立可重放证据。第12章 Java内存模型与线程固定线程载体为“虚拟”，只把共享方式从“普通字段”切到“锁保护”，以反例计数、线程转储与同步边定位第一处变化并验证“所有共享读都由明确happens-before边或安全发布支撑”。

### 12.5 Java与协程

**四级证据 15/19。** 第12章 Java内存模型与线程中的12.5 Java与协程：先判定这是规范合同、HotSpot实现还是发行版行为，再用固定环境、原始输出和失败反例建立可重放证据。第12章 Java内存模型与线程固定线程载体为“虚拟”，只把共享方式从“普通字段”切到“锁保护”，以反例计数、线程转储与同步边定位第一处变化并验证“所有共享读都由明确happens-before边或安全发布支撑”。

### 12.5.1 内核线程的局限

**四级证据 16/19。** 第12章 Java内存模型与线程中的12.5.1 内核线程的局限：写出happens-before或临界区不变量，用反复并发测试、线程转储和锁事件证明正确性与竞争代价。第12章 Java内存模型与线程固定线程载体为“虚拟”，只把共享方式从“普通字段”切到“锁保护”，以反例计数、线程转储与同步边定位第一处变化并验证“所有共享读都由明确happens-before边或安全发布支撑”。

### 12.5.2 协程的复苏

**四级证据 17/19。** 第12章 Java内存模型与线程中的12.5.2 协程的复苏：先判定这是规范合同、HotSpot实现还是发行版行为，再用固定环境、原始输出和失败反例建立可重放证据。第12章 Java内存模型与线程固定线程载体为“虚拟”，只把共享方式从“普通字段”切到“锁保护”，以反例计数、线程转储与同步边定位第一处变化并验证“所有共享读都由明确happens-before边或安全发布支撑”。

### 12.5.3 Java的解决方案

**四级证据 18/19。** 第12章 Java内存模型与线程中的12.5.3 Java的解决方案：先判定这是规范合同、HotSpot实现还是发行版行为，再用固定环境、原始输出和失败反例建立可重放证据。第12章 Java内存模型与线程固定线程载体为“虚拟”，只把共享方式从“普通字段”切到“锁保护”，以反例计数、线程转储与同步边定位第一处变化并验证“所有共享读都由明确happens-before边或安全发布支撑”。

### 12.6 本章小结

**四级证据 19/19。** 第12章 Java内存模型与线程中的12.6 本章小结：先判定这是规范合同、HotSpot实现还是发行版行为，再用固定环境、原始输出和失败反例建立可重放证据。第12章 Java内存模型与线程固定线程载体为“虚拟”，只把共享方式从“普通字段”切到“锁保护”，以反例计数、线程转储与同步边定位第一处变化并验证“所有共享读都由明确happens-before边或安全发布支撑”。

## 三个必须主动触发的误区


## 练习、答案与282节点验证


