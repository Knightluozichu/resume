---
title: "什么是设计模式"
type: A
section: "设计模式入门"
order: 1
description: "什么是设计模式：把反复出现的面向对象设计问题、上下文、力量与后果组织成可检验的模式目录"
demo: true
math: false
sourceUrl: "https://www.informit.com/store/design-patterns-elements-of-reusable-object-oriented-software-9780201633610"
qualityVersion: 2
practiceMode: code
sourceMode: independent-rewrite
draft: false
---


## 直觉：从变化压力出发，而不是从模式名称出发

只背模式名称会把目录当答案，忽略模式成立的条件和承担的代价 因此，什么是设计模式 的第一步不是画类图，而是写清 **评审者面对需求相似但约束不同的两个系统，要判断是否真的适用同一模式** 中哪项变化会扩散、现有结构怎样承担成本，以及什么观察足以拒绝模式。

在实验前先预测：沿 界定上下文 → 识别变化 → 比较结构 → 检验后果 → 记录决定 运行时，哪条依赖最先暴露变化；随后只调整一个参与者或连接，保存代码结果和拒绝原因，最后重置到相同基线。


## 问题合同与术语

**意图。** 把反复出现的面向对象设计问题、上下文、力量与后果组织成可检验的模式目录。**适用边界。** 问题结构重复、变化轴可说明、参与者关系可验证，并存在值得交换的权衡。本页验收不变量是：**任何模式选择都必须同时公开适用条件、被替代方案和可接受后果**。

、、、、、
## 官方目录结构的逐项解释

下面的目录字段采用 GoF 模式目录的标准描述结构来组织独立讲解。每个字段都要连接当前问题、页面专属实验与练习证据；字段名称出现本身不算覆盖。

### 导论

什么是设计模式 对“导论”的范围处理是：导论建立模式、对象组合、继承与可复用设计的阅读坐标，课程只独立解释这些问题，不复现原文。 本页把该节点连接到“评审者面对需求相似但约束不同的两个系统，要判断是否真的适用同一模式”的选择证据，并用 任何模式选择都必须同时公开适用条件、被替代方案和可接受后果 作为拒绝空洞标签的检查线。

### 案例研究：设计一个文档编辑器

什么是设计模式 对“案例研究：设计一个文档编辑器”的范围处理是：文档编辑器案例把抽象、创建、结构和行为选择放进同一系统，提醒读者模式必须落到约束。 本页把该节点连接到“评审者面对需求相似但约束不同的两个系统，要判断是否真的适用同一模式”的选择证据，并用 任何模式选择都必须同时公开适用条件、被替代方案和可接受后果 作为拒绝空洞标签的检查线。

### 创建型模式

什么是设计模式 对“创建型模式”的范围处理是：创建型模式把系统从具体类的构造决定中解耦，关注谁创建、何时创建以及创建哪一族对象。 本页把该节点连接到“评审者面对需求相似但约束不同的两个系统，要判断是否真的适用同一模式”的选择证据，并用 任何模式选择都必须同时公开适用条件、被替代方案和可接受后果 作为拒绝空洞标签的检查线。

### 结构型模式

什么是设计模式 对“结构型模式”的范围处理是：结构型模式通过对象组合或类关系形成更大结构，重点检查接口、身份和所有权边界。 本页把该节点连接到“评审者面对需求相似但约束不同的两个系统，要判断是否真的适用同一模式”的选择证据，并用 任何模式选择都必须同时公开适用条件、被替代方案和可接受后果 作为拒绝空洞标签的检查线。

### 行为型模式

什么是设计模式 对“行为型模式”的范围处理是：行为型模式分配算法、状态和协作责任，重点检查消息路径、变化传播和失败语义。 本页把该节点连接到“评审者面对需求相似但约束不同的两个系统，要判断是否真的适用同一模式”的选择证据，并用 任何模式选择都必须同时公开适用条件、被替代方案和可接受后果 作为拒绝空洞标签的检查线。

### 结论

什么是设计模式 对“结论”的范围处理是：结论回收模式如何进入开发过程，并要求设计决定能由代码和反例重新检验。 本页把该节点连接到“评审者面对需求相似但约束不同的两个系统，要判断是否真的适用同一模式”的选择证据，并用 任何模式选择都必须同时公开适用条件、被替代方案和可接受后果 作为拒绝空洞标签的检查线。

### 附录 A：术语表

什么是设计模式 对“附录 A：术语表”的范围处理是：术语表统一对象、类、接口和模式角色语言，中文解释保留英文关键词以减少歧义。 本页把该节点连接到“评审者面对需求相似但约束不同的两个系统，要判断是否真的适用同一模式”的选择证据，并用 任何模式选择都必须同时公开适用条件、被替代方案和可接受后果 作为拒绝空洞标签的检查线。

### 附录 B：记号指南

什么是设计模式 对“附录 B：记号指南”的范围处理是：记号指南说明官方案例采用的对象建模表达；本站实验改用参与者卡和调用路径呈现。 本页把该节点连接到“评审者面对需求相似但约束不同的两个系统，要判断是否真的适用同一模式”的选择证据，并用 任何模式选择都必须同时公开适用条件、被替代方案和可接受后果 作为拒绝空洞标签的检查线。

### 附录 C：基础类

什么是设计模式 对“附录 C：基础类”的范围处理是：基础类附录支撑原书示例环境；本站 TypeScript 代码只验证结构，不声称移植该基础库。 本页把该节点连接到“评审者面对需求相似但约束不同的两个系统，要判断是否真的适用同一模式”的选择证据，并用 任何模式选择都必须同时公开适用条件、被替代方案和可接受后果 作为拒绝空洞标签的检查线。

### 参考文献

什么是设计模式 对“参考文献”的范围处理是：参考文献是追溯历史语境的入口，不能仅凭书目标题推断某项正文结论。 本页把该节点连接到“评审者面对需求相似但约束不同的两个系统，要判断是否真的适用同一模式”的选择证据，并用 任何模式选择都必须同时公开适用条件、被替代方案和可接受后果 作为拒绝空洞标签的检查线。

### 索引

什么是设计模式 对“索引”的范围处理是：索引帮助从术语回到讨论位置，但命中索引词不等于已经解释、可视化或练习该概念。 本页把该节点连接到“评审者面对需求相似但约束不同的两个系统，要判断是否真的适用同一模式”的选择证据，并用 任何模式选择都必须同时公开适用条件、被替代方案和可接受后果 作为拒绝空洞标签的检查线。

## 常见误区与反例


另一个易错点是把结构相似当作语义相同。创建型管理对象创建，结构型组织对象关系，行为型分配协作与职责；比较时必须同时写出谁触发变化、谁持有状态、失败如何传播，以及替代方案是否更小。

## 术语表


## 练习与答案


## 本章小结

什么是设计模式 把“评审者面对需求相似但约束不同的两个系统，要判断是否真的适用同一模式”收敛为 设计问题、模式目录、方案评审者 的协作合同。学习结果不是能复述定义，而是能修改代码、触发“从模式名称反推需求，或把 SOLID 等后来的课程材料冒充原书目录”这一反例、重置实验，并根据 共享词汇加快沟通，但标签崇拜会让简单设计被模式术语掩盖 接受或拒绝方案。

## 前后导航

- [上一页：全书学习地图](/learn/design-patterns/00-intro/learning-map)
- [下一页：策略模式](/learn/design-patterns/03-behavioral/strategy)

