---
title: "工厂方法模式"
type: A
section: "创建型模式"
order: 8
description: "工厂方法模式：定义创建产品的操作，把具体产品选择延迟给创建者子类或扩展点"
demo: true
math: false
sourceUrl: "https://www.informit.com/store/design-patterns-elements-of-reusable-object-oriented-software-9780201633610"
qualityVersion: 2
practiceMode: code
sourceMode: independent-rewrite
draft: true
---

import {
  Objectives,
  Callout,
  Term,
  Glossary,
  GlossaryItem,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能从“导入流程固定，但 CSV 与 JSON 文档的解析器创建方式不同”识别 工厂方法模式 真正处理的变化轴
- 能解释 ImporterCreator、ParserProduct、CsvImporter 的引用方向与协作责任
- 能修改 TypeScript 最小实现，并验证“新增解析格式不应修改通用导入、错误处理和资源关闭流程”
- 能用“创建逻辑只是参数分支时，独立工厂函数可能比继承层次更直接”作为拒绝或简化方案的信号

</Objectives>

## 直觉：从变化压力出发，而不是从模式名称出发

通用导入算法若直接 new 每种解析器，就不能在不改骨架的情况下扩展格式 因此，工厂方法模式 的第一步不是画类图，而是写清 **导入流程固定，但 CSV 与 JSON 文档的解析器创建方式不同** 中哪项变化会扩散、现有结构怎样承担成本，以及什么观察足以拒绝模式。

在实验前先预测：沿 开始导入 → 调用工厂 → 创建解析器 → 执行解析 → 统一收尾 运行时，哪条依赖最先暴露变化；随后只调整一个参与者或连接，保存代码结果和拒绝原因，最后重置到相同基线。

<Callout type="info" title="来源范围与独立教学重写">
  [InformIT
  官方页面](https://www.informit.com/store/design-patterns-elements-of-reusable-object-oriented-software-9780201633610)确认四位作者、1994
  年第一版、23 个模式、完整目录，并提供获授权的模式样章与示例代码；[Pearson
  书目页](https://www.pearson.com/en-us/subject-catalog/p/design-patterns-elements-of-reusable-object-oriented-software/P200000009480/9780321700698)用于交叉核对版本，[GoF
  作者回顾](https://www.informit.com/articles/article.aspx?p=1327762)用于理解后续评价，[TypeScript
  官方手册](https://www.typescriptlang.org/docs/handbook/2/classes.html)用于核对本站代码语法。项目没有原书完整正文，本页不翻译或复现原书；中文解释、TypeScript
  实现、交互与练习均为独立教学重写。
</Callout>

## 问题合同与术语

**意图。** 定义创建产品的操作，把具体产品选择延迟给创建者子类或扩展点。**适用边界。** 流程骨架稳定而其中一个产品类型需要由扩展者决定，并且继承关系合理。本页验收不变量是：**新增解析格式不应修改通用导入、错误处理和资源关闭流程**。

<Term def={"工厂方法模式在工厂方法模式中用于检查开始导入阶段的责任与证据。"}>工厂方法模式</Term>、<Term def={"ImporterCreator在工厂方法模式中用于检查调用工厂阶段的责任与证据。"}>ImporterCreator</Term>、<Term def={"ParserProduct在工厂方法模式中用于检查创建解析器阶段的责任与证据。"}>ParserProduct</Term>、<Term def={"CsvImporter在工厂方法模式中用于检查执行解析阶段的责任与证据。"}>CsvImporter</Term>、<Term def={"变化轴在工厂方法模式中用于检查统一收尾阶段的责任与证据。"}>变化轴</Term>、<Term def={"拒绝条件在工厂方法模式中用于检查开始导入阶段的责任与证据。"}>拒绝条件</Term>

## 官方目录结构的逐项解释

下面的目录字段采用 GoF 模式目录的标准描述结构来组织独立讲解。每个字段都要连接当前问题、页面专属实验与练习证据；字段名称出现本身不算覆盖。

### 模式名称与分类

“工厂方法模式”归入创建型模式，名称只用于定位 定义创建产品的操作，把具体产品选择延迟给创建者子类或扩展点，不能替代问题证据。 对“导入流程固定，但 CSV 与 JSON 文档的解析器创建方式不同”先保存基线，再改变一个条件；若出现“创建逻辑只是参数分支时，独立工厂函数可能比继承层次更直接”，就拒绝 工厂方法模式 方案并回到更简单结构。

### 意图

“意图”回答模式想改变什么：定义创建产品的操作，把具体产品选择延迟给创建者子类或扩展点。 对“导入流程固定，但 CSV 与 JSON 文档的解析器创建方式不同”先保存基线，再改变一个条件；若出现“创建逻辑只是参数分支时，独立工厂函数可能比继承层次更直接”，就拒绝 工厂方法模式 方案并回到更简单结构。

### 别名

“别名”在本页处理为：没有需要单独记忆的稳定别名；评审时使用目录中的正式名称，避免用近似称呼混淆责任。 对“导入流程固定，但 CSV 与 JSON 文档的解析器创建方式不同”先保存基线，再改变一个条件；若出现“创建逻辑只是参数分支时，独立工厂函数可能比继承层次更直接”，就拒绝 工厂方法模式 方案并回到更简单结构。

### 动机

“动机”从具体压力出发：通用导入算法若直接 new 每种解析器，就不能在不改骨架的情况下扩展格式。 对“导入流程固定，但 CSV 与 JSON 文档的解析器创建方式不同”先保存基线，再改变一个条件；若出现“创建逻辑只是参数分支时，独立工厂函数可能比继承层次更直接”，就拒绝 工厂方法模式 方案并回到更简单结构。

### 适用性

“适用性”要求同时满足：流程骨架稳定而其中一个产品类型需要由扩展者决定，并且继承关系合理。 对“导入流程固定，但 CSV 与 JSON 文档的解析器创建方式不同”先保存基线，再改变一个条件；若出现“创建逻辑只是参数分支时，独立工厂函数可能比继承层次更直接”，就拒绝 工厂方法模式 方案并回到更简单结构。

### 结构

“结构”由 ImporterCreator、ParserProduct、CsvImporter 以及它们之间的引用方向共同确定。 对“导入流程固定，但 CSV 与 JSON 文档的解析器创建方式不同”先保存基线，再改变一个条件；若出现“创建逻辑只是参数分支时，独立工厂函数可能比继承层次更直接”，就拒绝 工厂方法模式 方案并回到更简单结构。

### 参与者

“参与者”逐一说明 ImporterCreator、ParserProduct、CsvImporter 谁保存状态、谁发起调用以及谁承担变化。 对“导入流程固定，但 CSV 与 JSON 文档的解析器创建方式不同”先保存基线，再改变一个条件；若出现“创建逻辑只是参数分支时，独立工厂函数可能比继承层次更直接”，就拒绝 工厂方法模式 方案并回到更简单结构。

### 协作

“协作”的运行协议是：Creator 的业务方法调用 factoryMethod，ConcreteCreator 返回匹配的 Product。 对“导入流程固定，但 CSV 与 JSON 文档的解析器创建方式不同”先保存基线，再改变一个条件；若出现“创建逻辑只是参数分支时，独立工厂函数可能比继承层次更直接”，就拒绝 工厂方法模式 方案并回到更简单结构。

### 后果

“后果”必须连同收益和成本记录：产品创建与流程解耦，但每种产品可能增加创建者子类并强化继承结构。 对“导入流程固定，但 CSV 与 JSON 文档的解析器创建方式不同”先保存基线，再改变一个条件；若出现“创建逻辑只是参数分支时，独立工厂函数可能比继承层次更直接”，就拒绝 工厂方法模式 方案并回到更简单结构。

### 实现

“实现”落地时遵守：保持工厂返回抽象产品，避免调用方立即向下转型到具体类型。 对“导入流程固定，但 CSV 与 JSON 文档的解析器创建方式不同”先保存基线，再改变一个条件；若出现“创建逻辑只是参数分支时，独立工厂函数可能比继承层次更直接”，就拒绝 工厂方法模式 方案并回到更简单结构。

### 示例代码

“示例代码”使用本页 TypeScript 最小切片验证“新增解析格式不应修改通用导入、错误处理和资源关闭流程”，代码是独立教学示例。 对“导入流程固定，但 CSV 与 JSON 文档的解析器创建方式不同”先保存基线，再改变一个条件；若出现“创建逻辑只是参数分支时，独立工厂函数可能比继承层次更直接”，就拒绝 工厂方法模式 方案并回到更简单结构。

### 已知应用

“已知应用”可在这些场景识别：框架回调、文档创建和可扩展解析器常把对象创建留给子类，但名称相似不构成采用证据。 对“导入流程固定，但 CSV 与 JSON 文档的解析器创建方式不同”先保存基线，再改变一个条件；若出现“创建逻辑只是参数分支时，独立工厂函数可能比继承层次更直接”，就拒绝 工厂方法模式 方案并回到更简单结构。

### 相关模式

“相关模式”用于比较边界：抽象工厂创建一组相关产品；模板方法常在算法骨架中调用工厂方法。 对“导入流程固定，但 CSV 与 JSON 文档的解析器创建方式不同”先保存基线，再改变一个条件；若出现“创建逻辑只是参数分支时，独立工厂函数可能比继承层次更直接”，就拒绝 工厂方法模式 方案并回到更简单结构。

## 常见误区与反例

<Callout type="trap" title="工厂方法模式 的拒绝信号">
  创建逻辑只是参数分支时，独立工厂函数可能比继承层次更直接。出现该信号时，删除一层结构并重跑
  开始导入 → 调用工厂 → 创建解析器 → 执行解析 →
  统一收尾；模式名称不能成为保留复杂度的理由。
</Callout>

另一个易错点是把结构相似当作语义相同。抽象工厂创建一组相关产品；模板方法常在算法骨架中调用工厂方法；比较时必须同时写出谁触发变化、谁持有状态、失败如何传播，以及替代方案是否更小。

## 术语表

<Glossary>
  <GlossaryItem term="工厂方法模式">
    工厂方法模式 在“开始导入”阶段使用的可复核术语。
  </GlossaryItem>
  <GlossaryItem term="ImporterCreator">
    工厂方法模式 在“调用工厂”阶段使用的可复核术语。
  </GlossaryItem>
  <GlossaryItem term="ParserProduct">
    工厂方法模式 在“创建解析器”阶段使用的可复核术语。
  </GlossaryItem>
  <GlossaryItem term="CsvImporter">
    工厂方法模式 在“执行解析”阶段使用的可复核术语。
  </GlossaryItem>
  <GlossaryItem term="变化轴">
    工厂方法模式 在“统一收尾”阶段使用的可复核术语。
  </GlossaryItem>
  <GlossaryItem term="拒绝条件">
    工厂方法模式 在“开始导入”阶段使用的可复核术语。
  </GlossaryItem>
</Glossary>

## 练习与答案

<Exercises>

1. **目录字段复核。** 用当前案例逐项说明 模式名称与分类、意图、别名、动机、适用性、结构、参与者、协作、后果、实现、示例代码、已知应用、相关模式；每项至少给出一个对象、一次调用或一条可观察后果。

<Answer>
  工厂方法模式 的复核从“导入流程固定，但 CSV 与 JSON
  文档的解析器创建方式不同”开始。模式名称与分类定位创建型模式；意图是定义创建产品的操作，把具体产品选择延迟给创建者子类或扩展点；别名边界为没有需要单独记忆的稳定别名；评审时使用目录中的正式名称；动机来自通用导入算法若直接
  new
  每种解析器，就不能在不改骨架的情况下扩展格式；适用性要求流程骨架稳定而其中一个产品类型需要由扩展者决定，并且继承关系合理；结构和参与者是ImporterCreator、ParserProduct、CsvImporter；协作规则为Creator
  的业务方法调用 factoryMethod，ConcreteCreator 返回匹配的
  Product；后果是产品创建与流程解耦，但每种产品可能增加创建者子类并强化继承结构；实现遵循保持工厂返回抽象产品，避免调用方立即向下转型到具体类型；示例代码验证新增解析格式不应修改通用导入、错误处理和资源关闭流程；已知应用包括框架回调、文档创建和可扩展解析器常把对象创建留给子类；相关模式比较为抽象工厂创建一组相关产品；模板方法常在算法骨架中调用工厂方法。
</Answer>

2. **代码变更。** 为 ParserProduct 增加一个实现，并证明主流程不需要识别它的具体类；再注入“创建逻辑只是参数分支时，独立工厂函数可能比继承层次更直接”。

<Answer>
  新实现只遵守最小合同并由装配处选择，ImporterCreator 继续沿 开始导入 → 调用工厂
  → 创建解析器 → 执行解析 → 统一收尾
  工作。误用场景必须让实验的因果可追踪下降或让不变量失败；修复后重置并复跑同一输入，不能更换验收条件。
</Answer>

3. **方案判断。** 在什么情况下应拒绝 工厂方法模式，选择直接代码或相邻模式？

<Answer>
  当流程骨架稳定而其中一个产品类型需要由扩展者决定，并且继承关系合理不成立，或简单方案已经让“新增解析格式不应修改通用导入、错误处理和资源关闭流程”可验证时，应拒绝额外层次。抽象工厂创建一组相关产品；模板方法常在算法骨架中调用工厂方法；最终决定同时登记采用收益、产品创建与流程解耦，但每种产品可能增加创建者子类并强化继承结构、删除测试和回退方案。
</Answer>

</Exercises>

## 本章小结

工厂方法模式 把“导入流程固定，但 CSV 与 JSON 文档的解析器创建方式不同”收敛为 ImporterCreator、ParserProduct、CsvImporter 的协作合同。学习结果不是能复述定义，而是能修改代码、触发“创建逻辑只是参数分支时，独立工厂函数可能比继承层次更直接”这一反例、重置实验，并根据 产品创建与流程解耦，但每种产品可能增加创建者子类并强化继承结构 接受或拒绝方案。

## 前后导航

- [上一页：单例模式](/learn/design-patterns/01-creational/singleton)
- [下一页：抽象工厂模式](/learn/design-patterns/01-creational/abstract-factory)

<Attribution
  mode="independent-rewrite"
  sourceBasis="authorized-sample"
  workTitle="Erich Gamma, Richard Helm, Ralph Johnson, John Vlissides, Design Patterns: Elements of Reusable Object-Oriented Software"
  adaptedUrl="https://www.informit.com/store/design-patterns-elements-of-reusable-object-oriented-software-9780201633610"
/>
