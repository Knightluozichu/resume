---
title: 工厂方法模式
type: A
section: 创建型模式
order: 8
description: 定义创建对象的接口，让子类决定实例化哪个类——用子类替代条件分支。
demo: false
math: false
sourceUrl: ""
draft: true
---

import { FactoryMethodDiagram } from "@/components/mdx/design-patterns/diagrams/factory-method-diagram";
import { Objectives, Callout, Attribution, Glossary, GlossaryItem } from "@/components/mdx/mdx-components";

<Objectives>

- 理解工厂方法如何消除简单工厂中的 if-else
- 掌握「定义接口，子类决定实例化」的核心思想
- 能用工厂方法设计日志系统

</Objectives>

## 痛点场景：日志系统

你要做一个日志系统，支持文件日志、数据库日志、控制台日志。

简单工厂：

```typescript
class SimpleLoggerFactory {
  createLogger(type: string): Logger {
    if (type === "file") return new FileLogger();
    else if (type === "db") return new DatabaseLogger();
    else if (type === "console") return new ConsoleLogger();
    throw new Error("Unknown type");
  }
}
```

问题：每加一种日志类型，都要改 `createLogger()` 加一个 else if——**违反开闭原则**。

## 解决方案：子类决定

```typescript
// 产品接口
interface Logger {
  log(message: string): void;
}

// 具体产品
class FileLogger implements Logger {
  log(msg: string) { /* 写文件 */ }
}
class DatabaseLogger implements Logger {
  log(msg: string) { /* 写数据库 */ }
}

// 工厂接口
interface LoggerFactory {
  createLogger(): Logger;
}

// 具体工厂
class FileLoggerFactory implements LoggerFactory {
  createLogger() { return new FileLogger() }
}
class DatabaseLoggerFactory implements LoggerFactory {
  createLogger() { return new DatabaseLogger() }
}
```

新增日志类型？加一个 Logger 实现 + 对应 Factory，不改任何已有代码。

<FactoryMethodDiagram />

## 简单工厂 vs 工厂方法

| 维度 | 简单工厂 | 工厂方法 |
|------|----------|----------|
| 结构 | 一个工厂类 + if-else | 工厂接口 + 多个子类 |
| 扩展 | 改工厂代码（违反 OCP） | 加新子类（符合 OCP） |
| 类数量 | 少 | 多（每个产品一个工厂） |
| 适用 | 类型少且稳定 | 类型多或会扩展 |

<Callout type="tip">
简单工厂不是正式的模式（GoF 23 个里没有它），但它是理解工厂方法的垫脚石。类型少时用简单工厂更简洁，类型多或会扩展时升级为工厂方法。
</Callout>

## 常见误区

### 误区 1：所有对象创建都用工厂

`new Dog()` 就能解决的事情，不需要 `AnimalFactory.createAnimal("dog")`。工厂的价值在于**隔离变化**——当创建逻辑会变化或需要集中管理时才值得用。

### 误区 2：工厂类过多

每个产品配一个工厂类，类数量翻倍。如果产品族有明确的共同创建逻辑，考虑用抽象工厂减少类数。

### 误区 3：工厂方法返回类型太宽

工厂方法返回 `Logger` 接口是对的。但如果返回 `any` 或 `Object`，调用方还要强转，失去了类型安全。

## 小结

- 工厂方法 = 定义创建接口 + 子类决定实例化哪个类
- 核心价值：新增产品不改已有工厂（开闭原则）
- 简单工厂是轻量替代，类型少时够用
- 代价：类数量增多（每产品一工厂）

<Attribution source="GoF《设计模式》+ Head First Design Patterns" license="原创转述" />

<Glossary>

<GlossaryItem term="工厂方法模式">

定义一个用于创建对象的接口，让子类决定实例化哪一个类。工厂方法使一个类的实例化延迟到其子类。

</GlossaryItem>

<GlossaryItem term="简单工厂">

不是 GoF 模式。一个工厂类根据参数返回不同产品。简单但不满足开闭原则——每加一种产品要改工厂代码。

</GlossaryItem>

<GlossaryItem term="产品族">

一组相关或相互依赖的产品。抽象工厂模式用于创建产品族，工厂方法用于创建单个产品。

</GlossaryItem>

</Glossary>
