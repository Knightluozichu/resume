---
title: "享元模式"
type: A
section: "结构型模式"
order: 16
description: "享元模式：共享大量细粒度对象的内在状态，把随上下文变化的外在状态交给调用者"
demo: true
math: false
sourceUrl: "https://www.informit.com/store/design-patterns-elements-of-reusable-object-oriented-software-9780201633610"
qualityVersion: 2
practiceMode: code
sourceMode: independent-rewrite
draft: false
---


## 直觉：从变化压力出发，而不是从模式名称出发

每棵树重复保存相同纹理和模型会耗尽内存，合并全部状态又会串改实例 因此，享元模式 的第一步不是画类图，而是写清 **地图要绘制百万棵树，树种纹理可共享，而坐标和生长状态各不相同** 中哪项变化会扩散、现有结构怎样承担成本，以及什么观察足以拒绝模式。

在实验前先预测：沿 拆分状态 → 查找共享对象 → 复用纹理 → 传入坐标 → 完成绘制 运行时，哪条依赖最先暴露变化；随后只调整一个参与者或连接，保存代码结果和拒绝原因，最后重置到相同基线。


## 问题合同与术语

**意图。** 共享大量细粒度对象的内在状态，把随上下文变化的外在状态交给调用者。**适用边界。** 对象数量巨大、共享部分占比高、外在状态能被调用上下文可靠提供。本页验收不变量是：**改变一棵树的坐标不得修改同树种其他实例的渲染位置**。

、、、、、
## 官方目录结构的逐项解释

下面的目录字段采用 GoF 模式目录的标准描述结构来组织独立讲解。每个字段都要连接当前问题、页面专属实验与练习证据；字段名称出现本身不算覆盖。

### 模式名称与分类

“享元模式”归入结构型模式，名称只用于定位 共享大量细粒度对象的内在状态，把随上下文变化的外在状态交给调用者，不能替代问题证据。 对“地图要绘制百万棵树，树种纹理可共享，而坐标和生长状态各不相同”先保存基线，再改变一个条件；若出现“把可变坐标放进共享对象，会让修改一棵树同时移动整片森林”，就拒绝 享元模式 方案并回到更简单结构。

### 意图

“意图”回答模式想改变什么：共享大量细粒度对象的内在状态，把随上下文变化的外在状态交给调用者。 对“地图要绘制百万棵树，树种纹理可共享，而坐标和生长状态各不相同”先保存基线，再改变一个条件；若出现“把可变坐标放进共享对象，会让修改一棵树同时移动整片森林”，就拒绝 享元模式 方案并回到更简单结构。

### 别名

“别名”在本页处理为：没有需要单独记忆的稳定别名；评审时使用目录中的正式名称，避免用近似称呼混淆责任。 对“地图要绘制百万棵树，树种纹理可共享，而坐标和生长状态各不相同”先保存基线，再改变一个条件；若出现“把可变坐标放进共享对象，会让修改一棵树同时移动整片森林”，就拒绝 享元模式 方案并回到更简单结构。

### 动机

“动机”从具体压力出发：每棵树重复保存相同纹理和模型会耗尽内存，合并全部状态又会串改实例。 对“地图要绘制百万棵树，树种纹理可共享，而坐标和生长状态各不相同”先保存基线，再改变一个条件；若出现“把可变坐标放进共享对象，会让修改一棵树同时移动整片森林”，就拒绝 享元模式 方案并回到更简单结构。

### 适用性

“适用性”要求同时满足：对象数量巨大、共享部分占比高、外在状态能被调用上下文可靠提供。 对“地图要绘制百万棵树，树种纹理可共享，而坐标和生长状态各不相同”先保存基线，再改变一个条件；若出现“把可变坐标放进共享对象，会让修改一棵树同时移动整片森林”，就拒绝 享元模式 方案并回到更简单结构。

### 结构

“结构”由 TreeFlyweight、TreeFactory、MapContext 以及它们之间的引用方向共同确定。 对“地图要绘制百万棵树，树种纹理可共享，而坐标和生长状态各不相同”先保存基线，再改变一个条件；若出现“把可变坐标放进共享对象，会让修改一棵树同时移动整片森林”，就拒绝 享元模式 方案并回到更简单结构。

### 参与者

“参与者”逐一说明 TreeFlyweight、TreeFactory、MapContext 谁保存状态、谁发起调用以及谁承担变化。 对“地图要绘制百万棵树，树种纹理可共享，而坐标和生长状态各不相同”先保存基线，再改变一个条件；若出现“把可变坐标放进共享对象，会让修改一棵树同时移动整片森林”，就拒绝 享元模式 方案并回到更简单结构。

### 协作

“协作”的运行协议是：Factory 按内在键复用 Flyweight，Client 每次操作传入外在状态。 对“地图要绘制百万棵树，树种纹理可共享，而坐标和生长状态各不相同”先保存基线，再改变一个条件；若出现“把可变坐标放进共享对象，会让修改一棵树同时移动整片森林”，就拒绝 享元模式 方案并回到更简单结构。

### 后果

“后果”必须连同收益和成本记录：内存下降，但状态拆分、键设计、线程安全和对象身份判断更困难。 对“地图要绘制百万棵树，树种纹理可共享，而坐标和生长状态各不相同”先保存基线，再改变一个条件；若出现“把可变坐标放进共享对象，会让修改一棵树同时移动整片森林”，就拒绝 享元模式 方案并回到更简单结构。

### 实现

“实现”落地时遵守：让共享状态不可变，测量共享收益，并禁止业务依赖享元对象身份。 对“地图要绘制百万棵树，树种纹理可共享，而坐标和生长状态各不相同”先保存基线，再改变一个条件；若出现“把可变坐标放进共享对象，会让修改一棵树同时移动整片森林”，就拒绝 享元模式 方案并回到更简单结构。

### 示例代码

“示例代码”使用本页 TypeScript 最小切片验证“改变一棵树的坐标不得修改同树种其他实例的渲染位置”，代码是独立教学示例。 对“地图要绘制百万棵树，树种纹理可共享，而坐标和生长状态各不相同”先保存基线，再改变一个条件；若出现“把可变坐标放进共享对象，会让修改一棵树同时移动整片森林”，就拒绝 享元模式 方案并回到更简单结构。

### 已知应用

“已知应用”可在这些场景识别：字形、地图图标、粒子类型和语法符号表常共享不可变表示，但名称相似不构成采用证据。 对“地图要绘制百万棵树，树种纹理可共享，而坐标和生长状态各不相同”先保存基线，再改变一个条件；若出现“把可变坐标放进共享对象，会让修改一棵树同时移动整片森林”，就拒绝 享元模式 方案并回到更简单结构。

### 相关模式

“相关模式”用于比较边界：对象池复用有生命周期的实例；原型复制对象而不是共享内在状态。 对“地图要绘制百万棵树，树种纹理可共享，而坐标和生长状态各不相同”先保存基线，再改变一个条件；若出现“把可变坐标放进共享对象，会让修改一棵树同时移动整片森林”，就拒绝 享元模式 方案并回到更简单结构。

## 常见误区与反例


另一个易错点是把结构相似当作语义相同。对象池复用有生命周期的实例；原型复制对象而不是共享内在状态；比较时必须同时写出谁触发变化、谁持有状态、失败如何传播，以及替代方案是否更小。

## 术语表


## 练习与答案


## 本章小结

享元模式 把“地图要绘制百万棵树，树种纹理可共享，而坐标和生长状态各不相同”收敛为 TreeFlyweight、TreeFactory、MapContext 的协作合同。学习结果不是能复述定义，而是能修改代码、触发“把可变坐标放进共享对象，会让修改一棵树同时移动整片森林”这一反例、重置实验，并根据 内存下降，但状态拆分、键设计、线程安全和对象身份判断更困难 接受或拒绝方案。

## 前后导航

- [上一页：外观模式](/learn/design-patterns/02-structural/facade)
- [下一页：代理模式](/learn/design-patterns/02-structural/proxy)

