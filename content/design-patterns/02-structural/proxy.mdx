---
title: "代理模式"
type: A
section: "结构型模式"
order: 17
description: "代理模式：提供与真实对象相同的替身，以控制访问、延迟创建或跨边界调用"
demo: true
math: false
sourceUrl: "https://www.informit.com/store/design-patterns-elements-of-reusable-object-oriented-software-9780201633610"
qualityVersion: 2
practiceMode: code
sourceMode: independent-rewrite
draft: false
---


## 直觉：从变化压力出发，而不是从模式名称出发

客户端直接创建远端图片会在首屏承担网络延迟，也重复处理缓存和失败 因此，代理模式 的第一步不是画类图，而是写清 **图片查看器要在进入视口时才加载远端大图，并对失败提供可重试状态** 中哪项变化会扩散、现有结构怎样承担成本，以及什么观察足以拒绝模式。

在实验前先预测：沿 请求显示 → 检查缓存 → 延迟加载 → 委托真实对象 → 返回或失败 运行时，哪条依赖最先暴露变化；随后只调整一个参与者或连接，保存代码结果和拒绝原因，最后重置到相同基线。


## 问题合同与术语

**意图。** 提供与真实对象相同的替身，以控制访问、延迟创建或跨边界调用。**适用边界。** 访问真实对象需要控制成本、权限、位置或生命周期，且替身能遵守同一合同。本页验收不变量是：**代理显示成功时必须对应已校验的真实图片版本，而非半加载占位数据**。

、、、、、
## 官方目录结构的逐项解释

下面的目录字段采用 GoF 模式目录的标准描述结构来组织独立讲解。每个字段都要连接当前问题、页面专属实验与练习证据；字段名称出现本身不算覆盖。

### 模式名称与分类

“代理模式”归入结构型模式，名称只用于定位 提供与真实对象相同的替身，以控制访问、延迟创建或跨边界调用，不能替代问题证据。 对“图片查看器要在进入视口时才加载远端大图，并对失败提供可重试状态”先保存基线，再改变一个条件；若出现“代理吞掉网络失败并返回旧数据，会让相同接口产生不可见的一致性差异”，就拒绝 代理模式 方案并回到更简单结构。

### 意图

“意图”回答模式想改变什么：提供与真实对象相同的替身，以控制访问、延迟创建或跨边界调用。 对“图片查看器要在进入视口时才加载远端大图，并对失败提供可重试状态”先保存基线，再改变一个条件；若出现“代理吞掉网络失败并返回旧数据，会让相同接口产生不可见的一致性差异”，就拒绝 代理模式 方案并回到更简单结构。

### 别名

“别名”在本页处理为：没有需要单独记忆的稳定别名；评审时使用目录中的正式名称，避免用近似称呼混淆责任。 对“图片查看器要在进入视口时才加载远端大图，并对失败提供可重试状态”先保存基线，再改变一个条件；若出现“代理吞掉网络失败并返回旧数据，会让相同接口产生不可见的一致性差异”，就拒绝 代理模式 方案并回到更简单结构。

### 动机

“动机”从具体压力出发：客户端直接创建远端图片会在首屏承担网络延迟，也重复处理缓存和失败。 对“图片查看器要在进入视口时才加载远端大图，并对失败提供可重试状态”先保存基线，再改变一个条件；若出现“代理吞掉网络失败并返回旧数据，会让相同接口产生不可见的一致性差异”，就拒绝 代理模式 方案并回到更简单结构。

### 适用性

“适用性”要求同时满足：访问真实对象需要控制成本、权限、位置或生命周期，且替身能遵守同一合同。 对“图片查看器要在进入视口时才加载远端大图，并对失败提供可重试状态”先保存基线，再改变一个条件；若出现“代理吞掉网络失败并返回旧数据，会让相同接口产生不可见的一致性差异”，就拒绝 代理模式 方案并回到更简单结构。

### 结构

“结构”由 ImageSubject、LazyImageProxy、RemoteImage 以及它们之间的引用方向共同确定。 对“图片查看器要在进入视口时才加载远端大图，并对失败提供可重试状态”先保存基线，再改变一个条件；若出现“代理吞掉网络失败并返回旧数据，会让相同接口产生不可见的一致性差异”，就拒绝 代理模式 方案并回到更简单结构。

### 参与者

“参与者”逐一说明 ImageSubject、LazyImageProxy、RemoteImage 谁保存状态、谁发起调用以及谁承担变化。 对“图片查看器要在进入视口时才加载远端大图，并对失败提供可重试状态”先保存基线，再改变一个条件；若出现“代理吞掉网络失败并返回旧数据，会让相同接口产生不可见的一致性差异”，就拒绝 代理模式 方案并回到更简单结构。

### 协作

“协作”的运行协议是：Proxy 接收 Subject 调用，处理访问策略后创建或委托 RealSubject。 对“图片查看器要在进入视口时才加载远端大图，并对失败提供可重试状态”先保存基线，再改变一个条件；若出现“代理吞掉网络失败并返回旧数据，会让相同接口产生不可见的一致性差异”，就拒绝 代理模式 方案并回到更简单结构。

### 后果

“后果”必须连同收益和成本记录：客户端接口稳定，但额外跳转会隐藏延迟、远程失败或权限决策。 对“图片查看器要在进入视口时才加载远端大图，并对失败提供可重试状态”先保存基线，再改变一个条件；若出现“代理吞掉网络失败并返回旧数据，会让相同接口产生不可见的一致性差异”，就拒绝 代理模式 方案并回到更简单结构。

### 实现

“实现”落地时遵守：保持错误与异步语义可见，避免让远程调用伪装成无成本本地属性。 对“图片查看器要在进入视口时才加载远端大图，并对失败提供可重试状态”先保存基线，再改变一个条件；若出现“代理吞掉网络失败并返回旧数据，会让相同接口产生不可见的一致性差异”，就拒绝 代理模式 方案并回到更简单结构。

### 示例代码

“示例代码”使用本页 TypeScript 最小切片验证“代理显示成功时必须对应已校验的真实图片版本，而非半加载占位数据”，代码是独立教学示例。 对“图片查看器要在进入视口时才加载远端大图，并对失败提供可重试状态”先保存基线，再改变一个条件；若出现“代理吞掉网络失败并返回旧数据，会让相同接口产生不可见的一致性差异”，就拒绝 代理模式 方案并回到更简单结构。

### 已知应用

“已知应用”可在这些场景识别：虚拟加载、远程服务、权限检查、缓存与引用计数都可用代理，但名称相似不构成采用证据。 对“图片查看器要在进入视口时才加载远端大图，并对失败提供可重试状态”先保存基线，再改变一个条件；若出现“代理吞掉网络失败并返回旧数据，会让相同接口产生不可见的一致性差异”，就拒绝 代理模式 方案并回到更简单结构。

### 相关模式

“相关模式”用于比较边界：装饰器增加职责；适配器改变接口；外观提供更高层的子系统入口。 对“图片查看器要在进入视口时才加载远端大图，并对失败提供可重试状态”先保存基线，再改变一个条件；若出现“代理吞掉网络失败并返回旧数据，会让相同接口产生不可见的一致性差异”，就拒绝 代理模式 方案并回到更简单结构。

## 常见误区与反例


另一个易错点是把结构相似当作语义相同。装饰器增加职责；适配器改变接口；外观提供更高层的子系统入口；比较时必须同时写出谁触发变化、谁持有状态、失败如何传播，以及替代方案是否更小。

## 术语表


## 练习与答案


## 本章小结

代理模式 把“图片查看器要在进入视口时才加载远端大图，并对失败提供可重试状态”收敛为 ImageSubject、LazyImageProxy、RemoteImage 的协作合同。学习结果不是能复述定义，而是能修改代码、触发“代理吞掉网络失败并返回旧数据，会让相同接口产生不可见的一致性差异”这一反例、重置实验，并根据 客户端接口稳定，但额外跳转会隐藏延迟、远程失败或权限决策 接受或拒绝方案。

## 前后导航

- [上一页：享元模式](/learn/design-patterns/02-structural/flyweight)
- [下一页：责任链模式](/learn/design-patterns/03-behavioral/chain-of-responsibility)

