---
title: "命令模式"
type: A
section: "行为型模式"
order: 5
description: "命令模式：把一次请求封装成对象，以支持排队、记录、撤销或组合操作"
demo: true
math: false
sourceUrl: "https://www.informit.com/store/design-patterns-elements-of-reusable-object-oriented-software-9780201633610"
qualityVersion: 2
practiceMode: code
sourceMode: independent-rewrite
draft: false
---


## 直觉：从变化压力出发，而不是从模式名称出发

按钮直接修改文档会把界面、业务动作和历史管理锁在一起 因此，命令模式 的第一步不是画类图，而是写清 **编辑器既要执行文本变更，又要记录历史并可靠撤销最近一次操作** 中哪项变化会扩散、现有结构怎样承担成本，以及什么观察足以拒绝模式。

在实验前先预测：沿 创建命令 → 记录旧值 → 调用执行 → 写入历史 → 执行撤销 运行时，哪条依赖最先暴露变化；随后只调整一个参与者或连接，保存代码结果和拒绝原因，最后重置到相同基线。


## 问题合同与术语

**意图。** 把一次请求封装成对象，以支持排队、记录、撤销或组合操作。**适用边界。** 请求需要独立生命周期，或必须排队、重试、审计、撤销与组合。本页验收不变量是：**execute 后再 undo 必须恢复编辑器执行前的文档版本**。

、、、、、
## 官方目录结构的逐项解释

下面的目录字段采用 GoF 模式目录的标准描述结构来组织独立讲解。每个字段都要连接当前问题、页面专属实验与练习证据；字段名称出现本身不算覆盖。

### 模式名称与分类

“命令模式”归入行为型模式，名称只用于定位 把一次请求封装成对象，以支持排队、记录、撤销或组合操作，不能替代问题证据。 对“编辑器既要执行文本变更，又要记录历史并可靠撤销最近一次操作”先保存基线，再改变一个条件；若出现“把任意函数都包装成命令，却没有生命周期需求，只会增加样板”，就拒绝 命令模式 方案并回到更简单结构。

### 意图

“意图”回答模式想改变什么：把一次请求封装成对象，以支持排队、记录、撤销或组合操作。 对“编辑器既要执行文本变更，又要记录历史并可靠撤销最近一次操作”先保存基线，再改变一个条件；若出现“把任意函数都包装成命令，却没有生命周期需求，只会增加样板”，就拒绝 命令模式 方案并回到更简单结构。

### 别名

“别名”在本页处理为：没有需要单独记忆的稳定别名；评审时使用目录中的正式名称，避免用近似称呼混淆责任。 对“编辑器既要执行文本变更，又要记录历史并可靠撤销最近一次操作”先保存基线，再改变一个条件；若出现“把任意函数都包装成命令，却没有生命周期需求，只会增加样板”，就拒绝 命令模式 方案并回到更简单结构。

### 动机

“动机”从具体压力出发：按钮直接修改文档会把界面、业务动作和历史管理锁在一起。 对“编辑器既要执行文本变更，又要记录历史并可靠撤销最近一次操作”先保存基线，再改变一个条件；若出现“把任意函数都包装成命令，却没有生命周期需求，只会增加样板”，就拒绝 命令模式 方案并回到更简单结构。

### 适用性

“适用性”要求同时满足：请求需要独立生命周期，或必须排队、重试、审计、撤销与组合。 对“编辑器既要执行文本变更，又要记录历史并可靠撤销最近一次操作”先保存基线，再改变一个条件；若出现“把任意函数都包装成命令，却没有生命周期需求，只会增加样板”，就拒绝 命令模式 方案并回到更简单结构。

### 结构

“结构”由 EditorInvoker、EditCommand、DocumentReceiver 以及它们之间的引用方向共同确定。 对“编辑器既要执行文本变更，又要记录历史并可靠撤销最近一次操作”先保存基线，再改变一个条件；若出现“把任意函数都包装成命令，却没有生命周期需求，只会增加样板”，就拒绝 命令模式 方案并回到更简单结构。

### 参与者

“参与者”逐一说明 EditorInvoker、EditCommand、DocumentReceiver 谁保存状态、谁发起调用以及谁承担变化。 对“编辑器既要执行文本变更，又要记录历史并可靠撤销最近一次操作”先保存基线，再改变一个条件；若出现“把任意函数都包装成命令，却没有生命周期需求，只会增加样板”，就拒绝 命令模式 方案并回到更简单结构。

### 协作

“协作”的运行协议是：Invoker 触发 Command，Command 保存必要参数并委托 Receiver 完成真实动作。 对“编辑器既要执行文本变更，又要记录历史并可靠撤销最近一次操作”先保存基线，再改变一个条件；若出现“把任意函数都包装成命令，却没有生命周期需求，只会增加样板”，就拒绝 命令模式 方案并回到更简单结构。

### 后果

“后果”必须连同收益和成本记录：请求可组合且可追踪，但命令对象增加，撤销数据和幂等边界必须明确。 对“编辑器既要执行文本变更，又要记录历史并可靠撤销最近一次操作”先保存基线，再改变一个条件；若出现“把任意函数都包装成命令，却没有生命周期需求，只会增加样板”，就拒绝 命令模式 方案并回到更简单结构。

### 实现

“实现”落地时遵守：执行前保存最小补偿信息，区分未执行、已执行和已撤销状态。 对“编辑器既要执行文本变更，又要记录历史并可靠撤销最近一次操作”先保存基线，再改变一个条件；若出现“把任意函数都包装成命令，却没有生命周期需求，只会增加样板”，就拒绝 命令模式 方案并回到更简单结构。

### 示例代码

“示例代码”使用本页 TypeScript 最小切片验证“execute 后再 undo 必须恢复编辑器执行前的文档版本”，代码是独立教学示例。 对“编辑器既要执行文本变更，又要记录历史并可靠撤销最近一次操作”先保存基线，再改变一个条件；若出现“把任意函数都包装成命令，却没有生命周期需求，只会增加样板”，就拒绝 命令模式 方案并回到更简单结构。

### 已知应用

“已知应用”可在这些场景识别：菜单动作、作业队列、事务补偿和宏命令都使用请求对象化，但名称相似不构成采用证据。 对“编辑器既要执行文本变更，又要记录历史并可靠撤销最近一次操作”先保存基线，再改变一个条件；若出现“把任意函数都包装成命令，却没有生命周期需求，只会增加样板”，就拒绝 命令模式 方案并回到更简单结构。

### 相关模式

“相关模式”用于比较边界：备忘录可保存撤销快照；责任链让请求经过多个候选处理者。 对“编辑器既要执行文本变更，又要记录历史并可靠撤销最近一次操作”先保存基线，再改变一个条件；若出现“把任意函数都包装成命令，却没有生命周期需求，只会增加样板”，就拒绝 命令模式 方案并回到更简单结构。

## 常见误区与反例


另一个易错点是把结构相似当作语义相同。备忘录可保存撤销快照；责任链让请求经过多个候选处理者；比较时必须同时写出谁触发变化、谁持有状态、失败如何传播，以及替代方案是否更小。

## 术语表


## 练习与答案


## 本章小结

命令模式 把“编辑器既要执行文本变更，又要记录历史并可靠撤销最近一次操作”收敛为 EditorInvoker、EditCommand、DocumentReceiver 的协作合同。学习结果不是能复述定义，而是能修改代码、触发“把任意函数都包装成命令，却没有生命周期需求，只会增加样板”这一反例、重置实验，并根据 请求可组合且可追踪，但命令对象增加，撤销数据和幂等边界必须明确 接受或拒绝方案。

## 前后导航

- [上一页：装饰器模式](/learn/design-patterns/03-behavioral/decorator)
- [下一页：状态模式](/learn/design-patterns/03-behavioral/state)

