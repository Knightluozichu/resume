---
title: "装饰器模式"
type: A
section: "结构型模式"
order: 4
description: "装饰器模式：在保持组件接口的前提下，按对象组合顺序动态叠加职责"
demo: true
math: false
sourceUrl: "https://www.informit.com/store/design-patterns-elements-of-reusable-object-oriented-software-9780201633610"
qualityVersion: 2
practiceMode: code
sourceMode: independent-rewrite
draft: false
---


## 直觉：从变化压力出发，而不是从模式名称出发

为每种压缩与加密组合建立子类会产生组合爆炸，也难以在运行时调整 因此，装饰器模式 的第一步不是画类图，而是写清 **文本输出要按场景组合压缩、加密和审计，排列数量不适合用子类穷举** 中哪项变化会扩散、现有结构怎样承担成本，以及什么观察足以拒绝模式。

在实验前先预测：沿 创建核心 → 包裹压缩 → 包裹加密 → 调用外层 → 沿链返回 运行时，哪条依赖最先暴露变化；随后只调整一个参与者或连接，保存代码结果和拒绝原因，最后重置到相同基线。


## 问题合同与术语

**意图。** 在保持组件接口的前提下，按对象组合顺序动态叠加职责。**适用边界。** 职责可以正交叠加、顺序有清晰语义，并且所有层都遵守同一组件契约。本页验收不变量是：**去掉任意可选层后，核心消息仍符合 MessageComponent 合同**。

、、、、、
## 官方目录结构的逐项解释

下面的目录字段采用 GoF 模式目录的标准描述结构来组织独立讲解。每个字段都要连接当前问题、页面专属实验与练习证据；字段名称出现本身不算覆盖。

### 模式名称与分类

“装饰器模式”归入结构型模式，名称只用于定位 在保持组件接口的前提下，按对象组合顺序动态叠加职责，不能替代问题证据。 对“文本输出要按场景组合压缩、加密和审计，排列数量不适合用子类穷举”先保存基线，再改变一个条件；若出现“包装层偷偷缩窄输入或改变基础语义，会破坏可替换性”，就拒绝 装饰器模式 方案并回到更简单结构。

### 意图

“意图”回答模式想改变什么：在保持组件接口的前提下，按对象组合顺序动态叠加职责。 对“文本输出要按场景组合压缩、加密和审计，排列数量不适合用子类穷举”先保存基线，再改变一个条件；若出现“包装层偷偷缩窄输入或改变基础语义，会破坏可替换性”，就拒绝 装饰器模式 方案并回到更简单结构。

### 别名

“别名”在本页处理为：没有需要单独记忆的稳定别名；评审时使用目录中的正式名称，避免用近似称呼混淆责任。 对“文本输出要按场景组合压缩、加密和审计，排列数量不适合用子类穷举”先保存基线，再改变一个条件；若出现“包装层偷偷缩窄输入或改变基础语义，会破坏可替换性”，就拒绝 装饰器模式 方案并回到更简单结构。

### 动机

“动机”从具体压力出发：为每种压缩与加密组合建立子类会产生组合爆炸，也难以在运行时调整。 对“文本输出要按场景组合压缩、加密和审计，排列数量不适合用子类穷举”先保存基线，再改变一个条件；若出现“包装层偷偷缩窄输入或改变基础语义，会破坏可替换性”，就拒绝 装饰器模式 方案并回到更简单结构。

### 适用性

“适用性”要求同时满足：职责可以正交叠加、顺序有清晰语义，并且所有层都遵守同一组件契约。 对“文本输出要按场景组合压缩、加密和审计，排列数量不适合用子类穷举”先保存基线，再改变一个条件；若出现“包装层偷偷缩窄输入或改变基础语义，会破坏可替换性”，就拒绝 装饰器模式 方案并回到更简单结构。

### 结构

“结构”由 MessageComponent、MessageDecorator、EncryptionLayer 以及它们之间的引用方向共同确定。 对“文本输出要按场景组合压缩、加密和审计，排列数量不适合用子类穷举”先保存基线，再改变一个条件；若出现“包装层偷偷缩窄输入或改变基础语义，会破坏可替换性”，就拒绝 装饰器模式 方案并回到更简单结构。

### 参与者

“参与者”逐一说明 MessageComponent、MessageDecorator、EncryptionLayer 谁保存状态、谁发起调用以及谁承担变化。 对“文本输出要按场景组合压缩、加密和审计，排列数量不适合用子类穷举”先保存基线，再改变一个条件；若出现“包装层偷偷缩窄输入或改变基础语义，会破坏可替换性”，就拒绝 装饰器模式 方案并回到更简单结构。

### 协作

“协作”的运行协议是：Decorator 持有 Component，先后执行附加逻辑并把核心调用交给下一层。 对“文本输出要按场景组合压缩、加密和审计，排列数量不适合用子类穷举”先保存基线，再改变一个条件；若出现“包装层偷偷缩窄输入或改变基础语义，会破坏可替换性”，就拒绝 装饰器模式 方案并回到更简单结构。

### 后果

“后果”必须连同收益和成本记录：组合灵活度提高，但对象身份、层次顺序与堆栈调试会更复杂。 对“文本输出要按场景组合压缩、加密和审计，排列数量不适合用子类穷举”先保存基线，再改变一个条件；若出现“包装层偷偷缩窄输入或改变基础语义，会破坏可替换性”，就拒绝 装饰器模式 方案并回到更简单结构。

### 实现

“实现”落地时遵守：让装饰器完整转发契约，明确调用前后顺序，并为链条提供可观测描述。 对“文本输出要按场景组合压缩、加密和审计，排列数量不适合用子类穷举”先保存基线，再改变一个条件；若出现“包装层偷偷缩窄输入或改变基础语义，会破坏可替换性”，就拒绝 装饰器模式 方案并回到更简单结构。

### 示例代码

“示例代码”使用本页 TypeScript 最小切片验证“去掉任意可选层后，核心消息仍符合 MessageComponent 合同”，代码是独立教学示例。 对“文本输出要按场景组合压缩、加密和审计，排列数量不适合用子类穷举”先保存基线，再改变一个条件；若出现“包装层偷偷缩窄输入或改变基础语义，会破坏可替换性”，就拒绝 装饰器模式 方案并回到更简单结构。

### 已知应用

“已知应用”可在这些场景识别：I/O 流、HTTP 中间件、日志包装和界面边框都是典型应用，但名称相似不构成采用证据。 对“文本输出要按场景组合压缩、加密和审计，排列数量不适合用子类穷举”先保存基线，再改变一个条件；若出现“包装层偷偷缩窄输入或改变基础语义，会破坏可替换性”，就拒绝 装饰器模式 方案并回到更简单结构。

### 相关模式

“相关模式”用于比较边界：代理控制访问，适配器改变接口，组合模式表达整体与部分的树结构。 对“文本输出要按场景组合压缩、加密和审计，排列数量不适合用子类穷举”先保存基线，再改变一个条件；若出现“包装层偷偷缩窄输入或改变基础语义，会破坏可替换性”，就拒绝 装饰器模式 方案并回到更简单结构。

## 常见误区与反例


另一个易错点是把结构相似当作语义相同。代理控制访问，适配器改变接口，组合模式表达整体与部分的树结构；比较时必须同时写出谁触发变化、谁持有状态、失败如何传播，以及替代方案是否更小。

## 术语表


## 练习与答案


## 本章小结

装饰器模式 把“文本输出要按场景组合压缩、加密和审计，排列数量不适合用子类穷举”收敛为 MessageComponent、MessageDecorator、EncryptionLayer 的协作合同。学习结果不是能复述定义，而是能修改代码、触发“包装层偷偷缩窄输入或改变基础语义，会破坏可替换性”这一反例、重置实验，并根据 组合灵活度提高，但对象身份、层次顺序与堆栈调试会更复杂 接受或拒绝方案。

## 前后导航

- [上一页：观察者模式](/learn/design-patterns/03-behavioral/observer)
- [下一页：命令模式](/learn/design-patterns/03-behavioral/command)

