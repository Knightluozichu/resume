---
title: 迭代器模式
type: A
section: 行为型模式
order: 19
description: 统一遍历接口，屏蔽集合的内部结构——数组、链表、树用同一种方式遍历。
demo: false
math: false
sourceUrl: ""
draft: true
---

import { IteratorDiagram } from "@/components/mdx/design-patterns/diagrams/iterator-diagram";
import { Objectives, Callout, Attribution, Glossary, GlossaryItem } from "@/components/mdx/mdx-components";

<Objectives>

- 理解迭代器模式如何统一不同集合的遍历方式
- 掌握迭代器与集合的分离设计
- 能用迭代器配合组合模式遍历树形结构

</Objectives>

## 痛点场景：遍历不同结构

你有数组和链表两种集合，客户端要分别知道它们的内部结构才能遍历：

```typescript
// 数组遍历
for (let i = 0; i < array.length; i++) { process(array[i]) }

// 链表遍历
let node = list.head;
while (node) { process(node.value); node = node.next }

// 树遍历？又是另一套
// 换一种结构就要改客户端代码
```

## 解决方案：统一遍历接口

```typescript
// 迭代器接口
interface Iterator<T> {
  hasNext(): boolean;
  next(): T;
}

// 数组迭代器
class ArrayIterator<T> implements Iterator<T> {
  private index = 0;
  constructor(private data: T[]) {}

  hasNext() { return this.index < this.data.length }
  next() { return this.data[this.index++] }
}

// 链表迭代器
class LinkedListIterator<T> implements Iterator<T> {
  private current: ListNode<T> | null;
  constructor(head: ListNode<T> | null) { this.current = head }

  hasNext() { return this.current !== null }
  next() {
    const val = this.current!.value;
    this.current = this.current!.next;
    return val;
  }
}

// 集合接口
interface Collection<T> {
  createIterator(): Iterator<T>;
}
```

使用：

```typescript
// 客户端不关心是数组还是链表
function printAll<T>(iter: Iterator<T>) {
  while (iter.hasNext()) {
    console.log(iter.next());
  }
}

printAll(array.createIterator());
printAll(list.createIterator());
```

<IteratorDiagram />

## 常见误区

### 误区 1：现代语言已有迭代器，模式已过时

JavaScript 的 `for...of`、Python 的 `for x in`、Java 的增强 for 循环都内置了迭代器模式。但理解迭代器的原理仍有价值——自定义集合时需要实现迭代器接口。

### 误区 2：迭代器中修改集合

遍历过程中修改集合可能导致不可预期行为（跳过元素、重复访问、无限循环）。解决方案：迭代器维护快照，或抛出 `ConcurrentModificationException`。

### 误区 3：只能线性遍历

迭代器不限于线性遍历。可以实现树的前序、中序、后序迭代器，图的广度/深度优先迭代器——只要实现 `hasNext()/next()` 即可。

## 小结

- 迭代器 = 统一遍历接口 + 屏蔽集合内部结构
- 核心价值：客户端用同一套代码遍历数组、链表、树、图
- 现代语言内置迭代器（Symbol.iterator、for...of），但自定义集合仍需实现
- 与组合模式配合：统一遍历树形结构

<Attribution source="GoF《设计模式》+ Head First Design Patterns" license="原创转述" />

<Glossary>

<GlossaryItem term="迭代器模式">

提供一种方法顺序访问一个聚合对象中的各个元素，而又不暴露该对象的内部表示。

</GlossaryItem>

<GlossaryItem term="内部表示">

集合内部存储数据的方式（数组、链表、哈希表等）。迭代器模式让客户端无需知道内部表示即可遍历。

</GlossaryItem>

<GlossaryItem term="并发修改">

在迭代器遍历过程中修改集合内容。可能导致跳过元素、重复访问或无限循环，通常需要特殊处理。

</GlossaryItem>

</Glossary>
