---
title: "迭代器模式"
type: A
section: "行为型模式"
order: 19
description: "迭代器模式：在不暴露聚合内部表示的情况下，提供顺序访问元素的独立游标"
demo: true
math: false
sourceUrl: "https://www.informit.com/store/design-patterns-elements-of-reusable-object-oriented-software-9780201633610"
qualityVersion: 2
practiceMode: code
sourceMode: independent-rewrite
draft: true
---

import {
  Objectives,
  Callout,
  Term,
  Glossary,
  GlossaryItem,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能从“任务集合要支持深度优先与优先级遍历，同时隐藏其树和堆的存储细节”识别 迭代器模式 真正处理的变化轴
- 能解释 TaskAggregate、TaskIterator、PriorityIterator 的引用方向与协作责任
- 能修改 TypeScript 最小实现，并验证“未修改集合时，每个可达任务恰好返回一次且游标互不干扰”
- 能用“遍历中修改集合却没有一致性合同，会出现漏读、重复或越界”作为拒绝或简化方案的信号

</Objectives>

## 直觉：从变化压力出发，而不是从模式名称出发

业务代码若直接依赖数组索引或树节点，存储结构变化会扩散到调用方 因此，迭代器模式 的第一步不是画类图，而是写清 **任务集合要支持深度优先与优先级遍历，同时隐藏其树和堆的存储细节** 中哪项变化会扩散、现有结构怎样承担成本，以及什么观察足以拒绝模式。

在实验前先预测：沿 创建游标 → 检查下一项 → 读取元素 → 推进位置 → 结束遍历 运行时，哪条依赖最先暴露变化；随后只调整一个参与者或连接，保存代码结果和拒绝原因，最后重置到相同基线。

<Callout type="info" title="来源范围与独立教学重写">
  [InformIT
  官方页面](https://www.informit.com/store/design-patterns-elements-of-reusable-object-oriented-software-9780201633610)确认四位作者、1994
  年第一版、23 个模式、完整目录，并提供获授权的模式样章与示例代码；[Pearson
  书目页](https://www.pearson.com/en-us/subject-catalog/p/design-patterns-elements-of-reusable-object-oriented-software/P200000009480/9780321700698)用于交叉核对版本，[GoF
  作者回顾](https://www.informit.com/articles/article.aspx?p=1327762)用于理解后续评价，[TypeScript
  官方手册](https://www.typescriptlang.org/docs/handbook/2/classes.html)用于核对本站代码语法。项目没有原书完整正文，本页不翻译或复现原书；中文解释、TypeScript
  实现、交互与练习均为独立教学重写。
</Callout>

## 问题合同与术语

**意图。** 在不暴露聚合内部表示的情况下，提供顺序访问元素的独立游标。**适用边界。** 聚合需要多种遍历方式、并发遍历或隐藏内部表示。本页验收不变量是：**未修改集合时，每个可达任务恰好返回一次且游标互不干扰**。

<Term def={"迭代器模式在迭代器模式中用于检查创建游标阶段的责任与证据。"}>迭代器模式</Term>、<Term def={"TaskAggregate在迭代器模式中用于检查检查下一项阶段的责任与证据。"}>TaskAggregate</Term>、<Term def={"TaskIterator在迭代器模式中用于检查读取元素阶段的责任与证据。"}>TaskIterator</Term>、<Term def={"PriorityIterator在迭代器模式中用于检查推进位置阶段的责任与证据。"}>PriorityIterator</Term>、<Term def={"变化轴在迭代器模式中用于检查结束遍历阶段的责任与证据。"}>变化轴</Term>、<Term def={"拒绝条件在迭代器模式中用于检查创建游标阶段的责任与证据。"}>拒绝条件</Term>

## 官方目录结构的逐项解释

下面的目录字段采用 GoF 模式目录的标准描述结构来组织独立讲解。每个字段都要连接当前问题、页面专属实验与练习证据；字段名称出现本身不算覆盖。

### 模式名称与分类

“迭代器模式”归入行为型模式，名称只用于定位 在不暴露聚合内部表示的情况下，提供顺序访问元素的独立游标，不能替代问题证据。 对“任务集合要支持深度优先与优先级遍历，同时隐藏其树和堆的存储细节”先保存基线，再改变一个条件；若出现“遍历中修改集合却没有一致性合同，会出现漏读、重复或越界”，就拒绝 迭代器模式 方案并回到更简单结构。

### 意图

“意图”回答模式想改变什么：在不暴露聚合内部表示的情况下，提供顺序访问元素的独立游标。 对“任务集合要支持深度优先与优先级遍历，同时隐藏其树和堆的存储细节”先保存基线，再改变一个条件；若出现“遍历中修改集合却没有一致性合同，会出现漏读、重复或越界”，就拒绝 迭代器模式 方案并回到更简单结构。

### 别名

“别名”在本页处理为：没有需要单独记忆的稳定别名；评审时使用目录中的正式名称，避免用近似称呼混淆责任。 对“任务集合要支持深度优先与优先级遍历，同时隐藏其树和堆的存储细节”先保存基线，再改变一个条件；若出现“遍历中修改集合却没有一致性合同，会出现漏读、重复或越界”，就拒绝 迭代器模式 方案并回到更简单结构。

### 动机

“动机”从具体压力出发：业务代码若直接依赖数组索引或树节点，存储结构变化会扩散到调用方。 对“任务集合要支持深度优先与优先级遍历，同时隐藏其树和堆的存储细节”先保存基线，再改变一个条件；若出现“遍历中修改集合却没有一致性合同，会出现漏读、重复或越界”，就拒绝 迭代器模式 方案并回到更简单结构。

### 适用性

“适用性”要求同时满足：聚合需要多种遍历方式、并发遍历或隐藏内部表示。 对“任务集合要支持深度优先与优先级遍历，同时隐藏其树和堆的存储细节”先保存基线，再改变一个条件；若出现“遍历中修改集合却没有一致性合同，会出现漏读、重复或越界”，就拒绝 迭代器模式 方案并回到更简单结构。

### 结构

“结构”由 TaskAggregate、TaskIterator、PriorityIterator 以及它们之间的引用方向共同确定。 对“任务集合要支持深度优先与优先级遍历，同时隐藏其树和堆的存储细节”先保存基线，再改变一个条件；若出现“遍历中修改集合却没有一致性合同，会出现漏读、重复或越界”，就拒绝 迭代器模式 方案并回到更简单结构。

### 参与者

“参与者”逐一说明 TaskAggregate、TaskIterator、PriorityIterator 谁保存状态、谁发起调用以及谁承担变化。 对“任务集合要支持深度优先与优先级遍历，同时隐藏其树和堆的存储细节”先保存基线，再改变一个条件；若出现“遍历中修改集合却没有一致性合同，会出现漏读、重复或越界”，就拒绝 迭代器模式 方案并回到更简单结构。

### 协作

“协作”的运行协议是：Aggregate 创建 Iterator，Iterator 保存独立位置并按合同返回元素。 对“任务集合要支持深度优先与优先级遍历，同时隐藏其树和堆的存储细节”先保存基线，再改变一个条件；若出现“遍历中修改集合却没有一致性合同，会出现漏读、重复或越界”，就拒绝 迭代器模式 方案并回到更简单结构。

### 后果

“后果”必须连同收益和成本记录：遍历策略可替换，但集合修改、失效检测和快照语义必须定义。 对“任务集合要支持深度优先与优先级遍历，同时隐藏其树和堆的存储细节”先保存基线，再改变一个条件；若出现“遍历中修改集合却没有一致性合同，会出现漏读、重复或越界”，就拒绝 迭代器模式 方案并回到更简单结构。

### 实现

“实现”落地时遵守：明确 fail-fast、快照或弱一致策略，并让结束状态可重复查询。 对“任务集合要支持深度优先与优先级遍历，同时隐藏其树和堆的存储细节”先保存基线，再改变一个条件；若出现“遍历中修改集合却没有一致性合同，会出现漏读、重复或越界”，就拒绝 迭代器模式 方案并回到更简单结构。

### 示例代码

“示例代码”使用本页 TypeScript 最小切片验证“未修改集合时，每个可达任务恰好返回一次且游标互不干扰”，代码是独立教学示例。 对“任务集合要支持深度优先与优先级遍历，同时隐藏其树和堆的存储细节”先保存基线，再改变一个条件；若出现“遍历中修改集合却没有一致性合同，会出现漏读、重复或越界”，就拒绝 迭代器模式 方案并回到更简单结构。

### 已知应用

“已知应用”可在这些场景识别：集合库、语法树、分页结果和文件扫描都使用独立迭代状态，但名称相似不构成采用证据。 对“任务集合要支持深度优先与优先级遍历，同时隐藏其树和堆的存储细节”先保存基线，再改变一个条件；若出现“遍历中修改集合却没有一致性合同，会出现漏读、重复或越界”，就拒绝 迭代器模式 方案并回到更简单结构。

### 相关模式

“相关模式”用于比较边界：组合提供待遍历的树；访问者把多种操作应用到稳定元素结构。 对“任务集合要支持深度优先与优先级遍历，同时隐藏其树和堆的存储细节”先保存基线，再改变一个条件；若出现“遍历中修改集合却没有一致性合同，会出现漏读、重复或越界”，就拒绝 迭代器模式 方案并回到更简单结构。

## 常见误区与反例

<Callout type="trap" title="迭代器模式 的拒绝信号">
  遍历中修改集合却没有一致性合同，会出现漏读、重复或越界。出现该信号时，删除一层结构并重跑
  创建游标 → 检查下一项 → 读取元素 → 推进位置 →
  结束遍历；模式名称不能成为保留复杂度的理由。
</Callout>

另一个易错点是把结构相似当作语义相同。组合提供待遍历的树；访问者把多种操作应用到稳定元素结构；比较时必须同时写出谁触发变化、谁持有状态、失败如何传播，以及替代方案是否更小。

## 术语表

<Glossary>
  <GlossaryItem term="迭代器模式">
    迭代器模式 在“创建游标”阶段使用的可复核术语。
  </GlossaryItem>
  <GlossaryItem term="TaskAggregate">
    迭代器模式 在“检查下一项”阶段使用的可复核术语。
  </GlossaryItem>
  <GlossaryItem term="TaskIterator">
    迭代器模式 在“读取元素”阶段使用的可复核术语。
  </GlossaryItem>
  <GlossaryItem term="PriorityIterator">
    迭代器模式 在“推进位置”阶段使用的可复核术语。
  </GlossaryItem>
  <GlossaryItem term="变化轴">
    迭代器模式 在“结束遍历”阶段使用的可复核术语。
  </GlossaryItem>
  <GlossaryItem term="拒绝条件">
    迭代器模式 在“创建游标”阶段使用的可复核术语。
  </GlossaryItem>
</Glossary>

## 练习与答案

<Exercises>

1. **目录字段复核。** 用当前案例逐项说明 模式名称与分类、意图、别名、动机、适用性、结构、参与者、协作、后果、实现、示例代码、已知应用、相关模式；每项至少给出一个对象、一次调用或一条可观察后果。

<Answer>
  迭代器模式
  的复核从“任务集合要支持深度优先与优先级遍历，同时隐藏其树和堆的存储细节”开始。模式名称与分类定位行为型模式；意图是在不暴露聚合内部表示的情况下，提供顺序访问元素的独立游标；别名边界为没有需要单独记忆的稳定别名；评审时使用目录中的正式名称；动机来自业务代码若直接依赖数组索引或树节点，存储结构变化会扩散到调用方；适用性要求聚合需要多种遍历方式、并发遍历或隐藏内部表示；结构和参与者是TaskAggregate、TaskIterator、PriorityIterator；协作规则为Aggregate
  创建 Iterator，Iterator
  保存独立位置并按合同返回元素；后果是遍历策略可替换，但集合修改、失效检测和快照语义必须定义；实现遵循明确
  fail-fast、快照或弱一致策略，并让结束状态可重复查询；示例代码验证未修改集合时，每个可达任务恰好返回一次且游标互不干扰；已知应用包括集合库、语法树、分页结果和文件扫描都使用独立迭代状态；相关模式比较为组合提供待遍历的树；访问者把多种操作应用到稳定元素结构。
</Answer>

2. **代码变更。** 为 TaskIterator 增加一个实现，并证明主流程不需要识别它的具体类；再注入“遍历中修改集合却没有一致性合同，会出现漏读、重复或越界”。

<Answer>
  新实现只遵守最小合同并由装配处选择，TaskAggregate 继续沿 创建游标 → 检查下一项
  → 读取元素 → 推进位置 → 结束遍历
  工作。误用场景必须让实验的因果可追踪下降或让不变量失败；修复后重置并复跑同一输入，不能更换验收条件。
</Answer>

3. **方案判断。** 在什么情况下应拒绝 迭代器模式，选择直接代码或相邻模式？

<Answer>
  当聚合需要多种遍历方式、并发遍历或隐藏内部表示不成立，或简单方案已经让“未修改集合时，每个可达任务恰好返回一次且游标互不干扰”可验证时，应拒绝额外层次。组合提供待遍历的树；访问者把多种操作应用到稳定元素结构；最终决定同时登记采用收益、遍历策略可替换，但集合修改、失效检测和快照语义必须定义、删除测试和回退方案。
</Answer>

</Exercises>

## 本章小结

迭代器模式 把“任务集合要支持深度优先与优先级遍历，同时隐藏其树和堆的存储细节”收敛为 TaskAggregate、TaskIterator、PriorityIterator 的协作合同。学习结果不是能复述定义，而是能修改代码、触发“遍历中修改集合却没有一致性合同，会出现漏读、重复或越界”这一反例、重置实验，并根据 遍历策略可替换，但集合修改、失效检测和快照语义必须定义 接受或拒绝方案。

## 前后导航

- [上一页：责任链模式](/learn/design-patterns/03-behavioral/chain-of-responsibility)
- [下一页：中介者模式](/learn/design-patterns/03-behavioral/mediator)

<Attribution
  mode="independent-rewrite"
  sourceBasis="authorized-sample"
  workTitle="Erich Gamma, Richard Helm, Ralph Johnson, John Vlissides, Design Patterns: Elements of Reusable Object-Oriented Software"
  adaptedUrl="https://www.informit.com/store/design-patterns-elements-of-reusable-object-oriented-software-9780201633610"
/>
