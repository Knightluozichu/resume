---
title: 模板方法模式
type: A
section: 行为型模式
order: 22
description: 定义算法骨架，将具体步骤延迟到子类——继承式行为型模式。
demo: false
math: false
sourceUrl: ""
draft: true
---

import { TemplateMethodDiagram } from "@/components/mdx/design-patterns/diagrams/template-method-diagram";
import { Objectives, Callout, Attribution, Glossary, GlossaryItem } from "@/components/mdx/mdx-components";

<Objectives>

- 理解模板方法如何用继承定义算法骨架
- 区分模板方法与策略模式（继承 vs 组合）
- 能用钩子方法控制可选步骤

</Objectives>

## 痛点场景：数据处理流程

数据处理的步骤固定：读取→解析→处理→输出。但每步的具体实现不同（CSV 读取 vs JSON 读取）。

不用模式：每个子类从头写整个流程，重复代码多。

## 解决方案：骨架+钩子

```typescript
abstract class DataProcessor {
  // 模板方法：定义骨架，final 不可覆写
  process(): void {
    const raw = this.read();
    const data = this.parse(raw);
    const result = this.transform(data);
    this.output(result);
  }

  // 抽象方法：子类必须实现
  protected abstract read(): string;
  protected abstract parse(raw: string): any[];
  protected abstract transform(data: any[]): any[];

  // 钩子方法：子类可选覆盖，默认空实现
  protected output(result: any[]): void {
    console.log(result);
  }
}

class CSVProcessor extends DataProcessor {
  protected read() { return "name,age\nAlice,30" }
  protected parse(raw: string) { /* CSV 解析 */ return [] }
  protected transform(data: any[]) { /* 过滤 */ return data }
}

class JSONProcessor extends DataProcessor {
  protected read() { return '{"items":[]}' }
  protected parse(raw: string) { /* JSON 解析 */ return [] }
  protected transform(data: any[]) { /* 排序 */ return data }
  protected override output(result: any[]) {
    // 覆写输出：写文件而非打印
    fs.writeFileSync("output.json", JSON.stringify(result));
  }
}
```

使用：

```typescript
new CSVProcessor().process();  // 按骨架执行，每步用 CSV 实现
new JSONProcessor().process(); // 按骨架执行，每步用 JSON 实现
```

<TemplateMethodDiagram />

## 模板方法 vs 策略

| 维度 | 模板方法 | 策略模式 |
|------|----------|----------|
| 机制 | 继承 | 组合 |
| 粒度 | 整个算法骨架 | 单个可替换算法 |
| 灵活性 | 子类受骨架约束 | 运行时可切换 |
| 编译时/运行时 | 编译时确定（子类） | 运行时切换（注入策略） |

<Callout type="tip">
模板方法用继承（is-a），策略用组合（has-a）。现代设计倾向组合优于继承，但骨架固定的场景模板方法更直观。
</Callout>

## 钩子方法

钩子方法是模板方法的精华——在骨架中留出可选的扩展点：

```typescript
abstract class Game {
  play() {
    this.init();
    this.startPlay();
    if (this.enableBonus()) { // 钩子：子类可覆写决定是否执行
      this.playBonus();
    }
    this.end();
  }

  protected enableBonus(): boolean { return false } // 默认不执行
}

class NormalGame extends Game {
  // 不覆写 enableBonus()，不加奖励关
}

class BonusGame extends Game {
  protected override enableBonus(): boolean { return true } // 加奖励关
}
```

## 常见误区

### 误区 1：模板方法太长

如果模板方法有 10+ 步，子类要实现太多方法，复杂度高。考虑拆成多个小模板方法，或改用策略+组合。

### 误区 2：忘了 final

模板方法应该用 `final`（TypeScript 没有此关键字，但约定不覆写）。子类覆写模板方法会破坏骨架完整性。

### 误区 3：抽象方法过多

如果大部分步骤都是抽象的，骨架形同虚设。好的模板方法应该有合理的默认实现，只让真正变化的部分抽象。

## 小结

- 模板方法 = 算法骨架在父类 + 具体步骤延迟到子类
- 核心价值：避免重复代码，统一流程结构
- 钩子方法提供可选扩展点，提升灵活性
- 与策略的区别：模板方法用继承（骨架不变），策略用组合（算法可换）

<Attribution source="GoF《设计模式》+ Head First Design Patterns" license="原创转述" />

<Glossary>

<GlossaryItem term="模板方法模式">

定义一个操作中算法的骨架，而将一些步骤延迟到子类中。模板方法使得子类可以不改变一个算法的结构即可重定义该算法的某些特定步骤。

</GlossaryItem>

<GlossaryItem term="钩子方法">

模板方法中提供的可选扩展点。默认有空实现或返回默认值，子类可以覆写以改变行为。与抽象方法不同，钩子方法不是必须覆写的。

</GlossaryItem>

<GlossaryItem term="骨架">

模板方法中定义的算法步骤顺序。子类只能覆写具体步骤的实现，不能改变步骤的执行顺序。

</GlossaryItem>

</Glossary>
