---
title: "访问者模式"
type: A
section: "行为型模式"
order: 23
description: "访问者模式：把作用于稳定元素结构的新操作集中到访问者，并通过双分派选择实现"
demo: true
math: false
sourceUrl: "https://www.informit.com/store/design-patterns-elements-of-reusable-object-oriented-software-9780201633610"
qualityVersion: 2
practiceMode: code
sourceMode: independent-rewrite
draft: true
---

import {
  Objectives,
  Callout,
  Term,
  Glossary,
  GlossaryItem,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能从“语法树节点类型稳定，但持续新增格式化、类型检查和指标统计操作”识别 访问者模式 真正处理的变化轴
- 能解释 AstElement、AnalysisVisitor、BinaryNode 的引用方向与协作责任
- 能修改 TypeScript 最小实现，并验证“新增一种分析操作不应修改现有 AST 节点类，且所有节点都有明确处理”
- 能用“元素类型频繁变化时，访问者会造成跨所有操作的同步修改”作为拒绝或简化方案的信号

</Objectives>

## 直觉：从变化压力出发，而不是从模式名称出发

每新增一项横切操作都修改所有节点，会让元素类被无关职责填满 因此，访问者模式 的第一步不是画类图，而是写清 **语法树节点类型稳定，但持续新增格式化、类型检查和指标统计操作** 中哪项变化会扩散、现有结构怎样承担成本，以及什么观察足以拒绝模式。

在实验前先预测：沿 遍历元素 → 调用 accept → 回调 visit → 处理具体类型 → 汇总结果 运行时，哪条依赖最先暴露变化；随后只调整一个参与者或连接，保存代码结果和拒绝原因，最后重置到相同基线。

<Callout type="info" title="来源范围与独立教学重写">
  [InformIT
  官方页面](https://www.informit.com/store/design-patterns-elements-of-reusable-object-oriented-software-9780201633610)确认四位作者、1994
  年第一版、23 个模式、完整目录，并提供获授权的模式样章与示例代码；[Pearson
  书目页](https://www.pearson.com/en-us/subject-catalog/p/design-patterns-elements-of-reusable-object-oriented-software/P200000009480/9780321700698)用于交叉核对版本，[GoF
  作者回顾](https://www.informit.com/articles/article.aspx?p=1327762)用于理解后续评价，[TypeScript
  官方手册](https://www.typescriptlang.org/docs/handbook/2/classes.html)用于核对本站代码语法。项目没有原书完整正文，本页不翻译或复现原书；中文解释、TypeScript
  实现、交互与练习均为独立教学重写。
</Callout>

## 问题合同与术语

**意图。** 把作用于稳定元素结构的新操作集中到访问者，并通过双分派选择实现。**适用边界。** 元素类型集合稳定而操作频繁增加，且操作需要依赖具体元素类型。本页验收不变量是：**新增一种分析操作不应修改现有 AST 节点类，且所有节点都有明确处理**。

<Term def={"访问者模式在访问者模式中用于检查遍历元素阶段的责任与证据。"}>访问者模式</Term>、<Term def={"AstElement在访问者模式中用于检查调用 accept阶段的责任与证据。"}>AstElement</Term>、<Term def={"AnalysisVisitor在访问者模式中用于检查回调 visit阶段的责任与证据。"}>AnalysisVisitor</Term>、<Term def={"BinaryNode在访问者模式中用于检查处理具体类型阶段的责任与证据。"}>BinaryNode</Term>、<Term def={"变化轴在访问者模式中用于检查汇总结果阶段的责任与证据。"}>变化轴</Term>、<Term def={"拒绝条件在访问者模式中用于检查遍历元素阶段的责任与证据。"}>拒绝条件</Term>

## 官方目录结构的逐项解释

下面的目录字段采用 GoF 模式目录的标准描述结构来组织独立讲解。每个字段都要连接当前问题、页面专属实验与练习证据；字段名称出现本身不算覆盖。

### 模式名称与分类

“访问者模式”归入行为型模式，名称只用于定位 把作用于稳定元素结构的新操作集中到访问者，并通过双分派选择实现，不能替代问题证据。 对“语法树节点类型稳定，但持续新增格式化、类型检查和指标统计操作”先保存基线，再改变一个条件；若出现“元素类型频繁变化时，访问者会造成跨所有操作的同步修改”，就拒绝 访问者模式 方案并回到更简单结构。

### 意图

“意图”回答模式想改变什么：把作用于稳定元素结构的新操作集中到访问者，并通过双分派选择实现。 对“语法树节点类型稳定，但持续新增格式化、类型检查和指标统计操作”先保存基线，再改变一个条件；若出现“元素类型频繁变化时，访问者会造成跨所有操作的同步修改”，就拒绝 访问者模式 方案并回到更简单结构。

### 别名

“别名”在本页处理为：没有需要单独记忆的稳定别名；评审时使用目录中的正式名称，避免用近似称呼混淆责任。 对“语法树节点类型稳定，但持续新增格式化、类型检查和指标统计操作”先保存基线，再改变一个条件；若出现“元素类型频繁变化时，访问者会造成跨所有操作的同步修改”，就拒绝 访问者模式 方案并回到更简单结构。

### 动机

“动机”从具体压力出发：每新增一项横切操作都修改所有节点，会让元素类被无关职责填满。 对“语法树节点类型稳定，但持续新增格式化、类型检查和指标统计操作”先保存基线，再改变一个条件；若出现“元素类型频繁变化时，访问者会造成跨所有操作的同步修改”，就拒绝 访问者模式 方案并回到更简单结构。

### 适用性

“适用性”要求同时满足：元素类型集合稳定而操作频繁增加，且操作需要依赖具体元素类型。 对“语法树节点类型稳定，但持续新增格式化、类型检查和指标统计操作”先保存基线，再改变一个条件；若出现“元素类型频繁变化时，访问者会造成跨所有操作的同步修改”，就拒绝 访问者模式 方案并回到更简单结构。

### 结构

“结构”由 AstElement、AnalysisVisitor、BinaryNode 以及它们之间的引用方向共同确定。 对“语法树节点类型稳定，但持续新增格式化、类型检查和指标统计操作”先保存基线，再改变一个条件；若出现“元素类型频繁变化时，访问者会造成跨所有操作的同步修改”，就拒绝 访问者模式 方案并回到更简单结构。

### 参与者

“参与者”逐一说明 AstElement、AnalysisVisitor、BinaryNode 谁保存状态、谁发起调用以及谁承担变化。 对“语法树节点类型稳定，但持续新增格式化、类型检查和指标统计操作”先保存基线，再改变一个条件；若出现“元素类型频繁变化时，访问者会造成跨所有操作的同步修改”，就拒绝 访问者模式 方案并回到更简单结构。

### 协作

“协作”的运行协议是：Element.accept 把自身传给 Visitor 的对应 visit 方法，结构负责遍历。 对“语法树节点类型稳定，但持续新增格式化、类型检查和指标统计操作”先保存基线，再改变一个条件；若出现“元素类型频繁变化时，访问者会造成跨所有操作的同步修改”，就拒绝 访问者模式 方案并回到更简单结构。

### 后果

“后果”必须连同收益和成本记录：新增操作容易，但新增元素类型必须修改所有访问者，且可能暴露内部数据。 对“语法树节点类型稳定，但持续新增格式化、类型检查和指标统计操作”先保存基线，再改变一个条件；若出现“元素类型频繁变化时，访问者会造成跨所有操作的同步修改”，就拒绝 访问者模式 方案并回到更简单结构。

### 实现

“实现”落地时遵守：显式覆盖所有元素类型，决定遍历归属，并避免用默认分支吞掉新节点。 对“语法树节点类型稳定，但持续新增格式化、类型检查和指标统计操作”先保存基线，再改变一个条件；若出现“元素类型频繁变化时，访问者会造成跨所有操作的同步修改”，就拒绝 访问者模式 方案并回到更简单结构。

### 示例代码

“示例代码”使用本页 TypeScript 最小切片验证“新增一种分析操作不应修改现有 AST 节点类，且所有节点都有明确处理”，代码是独立教学示例。 对“语法树节点类型稳定，但持续新增格式化、类型检查和指标统计操作”先保存基线，再改变一个条件；若出现“元素类型频繁变化时，访问者会造成跨所有操作的同步修改”，就拒绝 访问者模式 方案并回到更简单结构。

### 已知应用

“已知应用”可在这些场景识别：编译器语法树、文档导出、对象结构审计和报表生成常用访问者，但名称相似不构成采用证据。 对“语法树节点类型稳定，但持续新增格式化、类型检查和指标统计操作”先保存基线，再改变一个条件；若出现“元素类型频繁变化时，访问者会造成跨所有操作的同步修改”，就拒绝 访问者模式 方案并回到更简单结构。

### 相关模式

“相关模式”用于比较边界：迭代器负责遍历顺序；组合提供对象结构；解释器把行为留在表达式节点。 对“语法树节点类型稳定，但持续新增格式化、类型检查和指标统计操作”先保存基线，再改变一个条件；若出现“元素类型频繁变化时，访问者会造成跨所有操作的同步修改”，就拒绝 访问者模式 方案并回到更简单结构。

## 常见误区与反例

<Callout type="trap" title="访问者模式 的拒绝信号">
  元素类型频繁变化时，访问者会造成跨所有操作的同步修改。出现该信号时，删除一层结构并重跑
  遍历元素 → 调用 accept → 回调 visit → 处理具体类型 →
  汇总结果；模式名称不能成为保留复杂度的理由。
</Callout>

另一个易错点是把结构相似当作语义相同。迭代器负责遍历顺序；组合提供对象结构；解释器把行为留在表达式节点；比较时必须同时写出谁触发变化、谁持有状态、失败如何传播，以及替代方案是否更小。

## 术语表

<Glossary>
  <GlossaryItem term="访问者模式">
    访问者模式 在“遍历元素”阶段使用的可复核术语。
  </GlossaryItem>
  <GlossaryItem term="AstElement">
    访问者模式 在“调用 accept”阶段使用的可复核术语。
  </GlossaryItem>
  <GlossaryItem term="AnalysisVisitor">
    访问者模式 在“回调 visit”阶段使用的可复核术语。
  </GlossaryItem>
  <GlossaryItem term="BinaryNode">
    访问者模式 在“处理具体类型”阶段使用的可复核术语。
  </GlossaryItem>
  <GlossaryItem term="变化轴">
    访问者模式 在“汇总结果”阶段使用的可复核术语。
  </GlossaryItem>
  <GlossaryItem term="拒绝条件">
    访问者模式 在“遍历元素”阶段使用的可复核术语。
  </GlossaryItem>
</Glossary>

## 练习与答案

<Exercises>

1. **目录字段复核。** 用当前案例逐项说明 模式名称与分类、意图、别名、动机、适用性、结构、参与者、协作、后果、实现、示例代码、已知应用、相关模式；每项至少给出一个对象、一次调用或一条可观察后果。

<Answer>
  访问者模式
  的复核从“语法树节点类型稳定，但持续新增格式化、类型检查和指标统计操作”开始。模式名称与分类定位行为型模式；意图是把作用于稳定元素结构的新操作集中到访问者，并通过双分派选择实现；别名边界为没有需要单独记忆的稳定别名；评审时使用目录中的正式名称；动机来自每新增一项横切操作都修改所有节点，会让元素类被无关职责填满；适用性要求元素类型集合稳定而操作频繁增加，且操作需要依赖具体元素类型；结构和参与者是AstElement、AnalysisVisitor、BinaryNode；协作规则为Element.accept
  把自身传给 Visitor 的对应 visit
  方法，结构负责遍历；后果是新增操作容易，但新增元素类型必须修改所有访问者，且可能暴露内部数据；实现遵循显式覆盖所有元素类型，决定遍历归属，并避免用默认分支吞掉新节点；示例代码验证新增一种分析操作不应修改现有
  AST
  节点类，且所有节点都有明确处理；已知应用包括编译器语法树、文档导出、对象结构审计和报表生成常用访问者；相关模式比较为迭代器负责遍历顺序；组合提供对象结构；解释器把行为留在表达式节点。
</Answer>

2. **代码变更。** 为 AnalysisVisitor 增加一个实现，并证明主流程不需要识别它的具体类；再注入“元素类型频繁变化时，访问者会造成跨所有操作的同步修改”。

<Answer>
  新实现只遵守最小合同并由装配处选择，AstElement 继续沿 遍历元素 → 调用 accept →
  回调 visit → 处理具体类型 → 汇总结果
  工作。误用场景必须让实验的因果可追踪下降或让不变量失败；修复后重置并复跑同一输入，不能更换验收条件。
</Answer>

3. **方案判断。** 在什么情况下应拒绝 访问者模式，选择直接代码或相邻模式？

<Answer>
  当元素类型集合稳定而操作频繁增加，且操作需要依赖具体元素类型不成立，或简单方案已经让“新增一种分析操作不应修改现有
  AST
  节点类，且所有节点都有明确处理”可验证时，应拒绝额外层次。迭代器负责遍历顺序；组合提供对象结构；解释器把行为留在表达式节点；最终决定同时登记采用收益、新增操作容易，但新增元素类型必须修改所有访问者，且可能暴露内部数据、删除测试和回退方案。
</Answer>

</Exercises>

## 本章小结

访问者模式 把“语法树节点类型稳定，但持续新增格式化、类型检查和指标统计操作”收敛为 AstElement、AnalysisVisitor、BinaryNode 的协作合同。学习结果不是能复述定义，而是能修改代码、触发“元素类型频繁变化时，访问者会造成跨所有操作的同步修改”这一反例、重置实验，并根据 新增操作容易，但新增元素类型必须修改所有访问者，且可能暴露内部数据 接受或拒绝方案。

## 前后导航

- [上一页：模板方法模式](/learn/design-patterns/03-behavioral/template-method)
- [下一页：解释器模式](/learn/design-patterns/03-behavioral/interpreter)

<Attribution
  mode="independent-rewrite"
  sourceBasis="authorized-sample"
  workTitle="Erich Gamma, Richard Helm, Ralph Johnson, John Vlissides, Design Patterns: Elements of Reusable Object-Oriented Software"
  adaptedUrl="https://www.informit.com/store/design-patterns-elements-of-reusable-object-oriented-software-9780201633610"
/>
