---
title: "第2章 一个简单的语法制导翻译器"
type: C
section: "基础 · 编译器全景"
order: 2
description: "第2章 一个简单的语法制导翻译器逐项覆盖41个核心目录层级，以结构化中间产物、运行轨迹与失败对照建立可复查的编译实验。"
demo: true
math: false
sourceUrl: "https://www.pearson.com/en-us/subject-catalog/p/compilers-principles-techniques-and-tools/P200000003472/9780133002140"
draft: false
---

import {
  Dbc02SimpleSyntaxDirectedTranslatorMapLab,
  Dbc02SimpleSyntaxDirectedTranslatorExperimentLab,
  Dbc02SimpleSyntaxDirectedTranslatorEvidenceLab,
} from "@/components/mdx/dragon-book-compilers/diagrams/dbc-02-simple-syntax-directed-translator";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第2章 一个简单的语法制导翻译器在完整编译流水线中的输入、输出、数据结构和责任边界
- 能描述本页41个核心目录层级之间的依赖、算法前提与工程取舍
- 能复现最小三地址代码或伪代码实验，保存正常、边界、失败和优化四类证据
- 能判断“每次推导消费与文法匹配的输入，语义动作按依赖顺序执行，抽象语法舍弃的标点不影响运算符结合性、优先级与生成代码。”是否成立，而不是只看最终程序是否运行

</Objectives>

## 为什么最终输出正确仍然不够

<Term def="上下文无关文法连接“定义文法”阶段的输入、算法前提、结构化结果、成本和第一处失败证据。">
  上下文无关文法
</Term>
、
<Term def="语法制导定义连接“消除左递归”阶段的输入、算法前提、结构化结果、成本和第一处失败证据。">
  语法制导定义
</Term>
、
<Term def="预测分析连接“扫描词法单元”阶段的输入、算法前提、结构化结果、成本和第一处失败证据。">
  预测分析
</Term>
、
<Term def="抽象语法连接“执行语义动作”阶段的输入、算法前提、结构化结果、成本和第一处失败证据。">
  抽象语法
</Term>
、
<Term def="三地址代码连接“构造抽象语法”阶段的输入、算法前提、结构化结果、成本和第一处失败证据。">
  三地址代码
</Term>
共同构成本页坐标。从表达式文法出发，手工实现预测分析器、词法分析器、符号表与三地址代码生成器，形成第一个可以端到端运行的前端。

编译器是一串互相依赖的转换。错误的扫描器可能恰好产生可分析的词法单元，错误的语法树可能被后续语义动作意外修补，错误的数据流事实可能只在冷路径暴露，违反机器约束的调度也可能在当前样本中碰巧执行。因此必须保存阶段输入、输出和不变量，定位第一处偏离，不能把单个样例跑通当作证明。

## 核心模型与实验账本

实验固定源程序、编译器提交、中间表示格式、目标机模型、优化级别和随机种子。前端保存词法单元、推导、语法树、属性值与类型环境；中端保存基本块、控制流图、支配关系、数据流集合和每次变换前后的IR；后端保存模式覆盖、依赖图、发射周期、寄存器映射、溢出代码与最终指令。每份产物都带生成方式、退出状态和摘要值。

样本分成四组。正常样本证明主路径，边界样本覆盖空输入、深嵌套、二义性、不可归约流图、寄存器压力、长延迟、别名和动态调用；失败样本只破坏一个语法、类型、数据流或机器约束；优化样本同时保存变换前后版本，并用解释执行或差分执行核对可观察行为。

诊断记录阶段、源位置或IR位置、实际状态、期望状态、相关前驱和首要根因。某阶段失败后不得继续消费旧缓存伪造下游结果；分析事实被变换失效时，必须显式重算或证明仍然有效。性能结论与正确性结论分开：前者记录静态成本、动态计数、代码大小和运行环境，后者要求语义与全部约束成立。

目录项进入追溯矩阵后，至少关联一个输入、一个算法前提、一份结构化产物、一个失败对照和一个回归断言。跨章接口同时记录生产者和消费者，这样既能发现“提到概念却没有验证”的空洞覆盖，也能在实现变化后只重放真正受影响的实验。

## 六阶段证据链

### 1. 定义文法

围绕上下文无关文法先预测结构化结果，只改变一个条件并运行；进入“消除左递归”前，核对语义、算法前提、成本和首错位置。

### 2. 消除左递归

围绕语法制导定义先预测结构化结果，只改变一个条件并运行；进入“扫描词法单元”前，核对语义、算法前提、成本和首错位置。

### 3. 扫描词法单元

围绕预测分析先预测结构化结果，只改变一个条件并运行；进入“执行语义动作”前，核对语义、算法前提、成本和首错位置。

### 4. 执行语义动作

围绕抽象语法先预测结构化结果，只改变一个条件并运行；进入“构造抽象语法”前，核对语义、算法前提、成本和首错位置。

### 5. 构造抽象语法

围绕三地址代码先预测结构化结果，只改变一个条件并运行；进入“发射三地址代码”前，核对语义、算法前提、成本和首错位置。

### 6. 发射三地址代码

围绕上下文无关文法先预测结构化结果，只改变一个条件并运行；进入“定义文法”前，核对语义、算法前提、成本和首错位置。

## 可运行的最小骨架

```text
expr -> expr + term | term
term -> 0 | 1 | ... | 9
```

```text
expr() { term(); rest(); }
rest() { if (lookahead == '+') { match('+'); term(); emit('+'); rest(); } }
```

```text
x = y + z * 60
t1 = z * 60
x = y + t1
```

<Callout type="trap">
  **版本边界：**
  本页复原的是龙书第二版的算法模型和目录边界。现代LLVM、GCC、JVM或具体ISA可以用来做对照，但不能把其默认实现反写成原书算法的保证。
</Callout>

## 权威目录逐项讲解

### 第2章 一个简单的语法制导翻译器

“第2章 一个简单的语法制导翻译器”在本页不是孤立名词。先在“定义文法”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“消除左递归”检查第一条结构差异。围绕上下文无关文法记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 2.1 引言

“2.1 引言”在本页不是孤立名词。先在“消除左递归”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“扫描词法单元”检查第一条结构差异。围绕语法制导定义记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 2.2 语法定义

“2.2 语法定义”在本页不是孤立名词。先在“扫描词法单元”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“执行语义动作”检查第一条结构差异。围绕预测分析记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 2.2.1 文法定义

“2.2.1 文法定义”在本页不是孤立名词。先在“执行语义动作”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“构造抽象语法”检查第一条结构差异。围绕抽象语法记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 2.2.2 推导

“2.2.2 推导”在本页不是孤立名词。先在“构造抽象语法”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“发射三地址代码”检查第一条结构差异。围绕三地址代码记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 2.2.3 语法分析树

“2.2.3 语法分析树”在本页不是孤立名词。先在“发射三地址代码”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“定义文法”检查第一条结构差异。围绕上下文无关文法记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 2.2.4 二义性

“2.2.4 二义性”在本页不是孤立名词。先在“定义文法”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“消除左递归”检查第一条结构差异。围绕语法制导定义记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 2.2.5 运算符的结合性

“2.2.5 运算符的结合性”在本页不是孤立名词。先在“消除左递归”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“扫描词法单元”检查第一条结构差异。围绕预测分析记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 2.2.6 运算符的优先级

“2.2.6 运算符的优先级”在本页不是孤立名词。先在“扫描词法单元”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“执行语义动作”检查第一条结构差异。围绕抽象语法记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 2.3 语法制导翻译

“2.3 语法制导翻译”在本页不是孤立名词。先在“执行语义动作”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“构造抽象语法”检查第一条结构差异。围绕三地址代码记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 2.3.1 后缀表示

“2.3.1 后缀表示”在本页不是孤立名词。先在“构造抽象语法”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“发射三地址代码”检查第一条结构差异。围绕上下文无关文法记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 2.3.2 综合属性

“2.3.2 综合属性”在本页不是孤立名词。先在“发射三地址代码”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“定义文法”检查第一条结构差异。围绕语法制导定义记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 2.3.3 简单的语法制导定义

“2.3.3 简单的语法制导定义”在本页不是孤立名词。先在“定义文法”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“消除左递归”检查第一条结构差异。围绕预测分析记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 2.3.4 树遍历

“2.3.4 树遍历”在本页不是孤立名词。先在“消除左递归”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“扫描词法单元”检查第一条结构差异。围绕抽象语法记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 2.3.5 翻译方案

“2.3.5 翻译方案”在本页不是孤立名词。先在“扫描词法单元”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“执行语义动作”检查第一条结构差异。围绕三地址代码记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 2.4 语法分析

“2.4 语法分析”在本页不是孤立名词。先在“执行语义动作”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“构造抽象语法”检查第一条结构差异。围绕上下文无关文法记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 2.4.1 自顶向下分析方法

“2.4.1 自顶向下分析方法”在本页不是孤立名词。先在“构造抽象语法”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“发射三地址代码”检查第一条结构差异。围绕语法制导定义记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 2.4.2 预测分析法

“2.4.2 预测分析法”在本页不是孤立名词。先在“发射三地址代码”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“定义文法”检查第一条结构差异。围绕预测分析记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 2.4.3 何时使用空产生式

“2.4.3 何时使用空产生式”在本页不是孤立名词。先在“定义文法”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“消除左递归”检查第一条结构差异。围绕抽象语法记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 2.4.4 设计一个预测语法分析器

“2.4.4 设计一个预测语法分析器”在本页不是孤立名词。先在“消除左递归”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“扫描词法单元”检查第一条结构差异。围绕三地址代码记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 2.4.5 左递归

“2.4.5 左递归”在本页不是孤立名词。先在“扫描词法单元”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“执行语义动作”检查第一条结构差异。围绕上下文无关文法记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 2.5 简单表达式的翻译器

“2.5 简单表达式的翻译器”在本页不是孤立名词。先在“执行语义动作”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“构造抽象语法”检查第一条结构差异。围绕语法制导定义记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 2.5.1 抽象语法和具体语法

“2.5.1 抽象语法和具体语法”在本页不是孤立名词。先在“构造抽象语法”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“发射三地址代码”检查第一条结构差异。围绕预测分析记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 2.5.2 调整翻译方案

“2.5.2 调整翻译方案”在本页不是孤立名词。先在“发射三地址代码”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“定义文法”检查第一条结构差异。围绕抽象语法记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 2.5.3 非终结符号的过程

“2.5.3 非终结符号的过程”在本页不是孤立名词。先在“定义文法”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“消除左递归”检查第一条结构差异。围绕三地址代码记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 2.5.4 简化翻译器

“2.5.4 简化翻译器”在本页不是孤立名词。先在“消除左递归”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“扫描词法单元”检查第一条结构差异。围绕上下文无关文法记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 2.5.5 完整程序

“2.5.5 完整程序”在本页不是孤立名词。先在“扫描词法单元”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“执行语义动作”检查第一条结构差异。围绕语法制导定义记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 2.6 词法分析

“2.6 词法分析”在本页不是孤立名词。先在“执行语义动作”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“构造抽象语法”检查第一条结构差异。围绕预测分析记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 2.6.1 删除空白和注释

“2.6.1 删除空白和注释”在本页不是孤立名词。先在“构造抽象语法”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“发射三地址代码”检查第一条结构差异。围绕抽象语法记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 2.6.2 预读

“2.6.2 预读”在本页不是孤立名词。先在“发射三地址代码”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“定义文法”检查第一条结构差异。围绕三地址代码记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 2.6.3 常量

“2.6.3 常量”在本页不是孤立名词。先在“定义文法”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“消除左递归”检查第一条结构差异。围绕上下文无关文法记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 2.6.4 识别关键字和标识符

“2.6.4 识别关键字和标识符”在本页不是孤立名词。先在“消除左递归”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“扫描词法单元”检查第一条结构差异。围绕语法制导定义记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 2.6.5 词法分析器

“2.6.5 词法分析器”在本页不是孤立名词。先在“扫描词法单元”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“执行语义动作”检查第一条结构差异。围绕预测分析记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 2.7 符号表

“2.7 符号表”在本页不是孤立名词。先在“执行语义动作”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“构造抽象语法”检查第一条结构差异。围绕抽象语法记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 2.7.1 每个作用域一个符号表

“2.7.1 每个作用域一个符号表”在本页不是孤立名词。先在“构造抽象语法”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“发射三地址代码”检查第一条结构差异。围绕三地址代码记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 2.7.2 符号表的使用

“2.7.2 符号表的使用”在本页不是孤立名词。先在“发射三地址代码”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“定义文法”检查第一条结构差异。围绕上下文无关文法记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 2.8 中间代码生成

“2.8 中间代码生成”在本页不是孤立名词。先在“定义文法”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“消除左递归”检查第一条结构差异。围绕语法制导定义记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 2.8.1 两种中间表示

“2.8.1 两种中间表示”在本页不是孤立名词。先在“消除左递归”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“扫描词法单元”检查第一条结构差异。围绕预测分析记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 2.8.2 语法树的构造

“2.8.2 语法树的构造”在本页不是孤立名词。先在“扫描词法单元”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“执行语义动作”检查第一条结构差异。围绕抽象语法记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 2.8.3 静态检查

“2.8.3 静态检查”在本页不是孤立名词。先在“执行语义动作”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“构造抽象语法”检查第一条结构差异。围绕三地址代码记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 2.8.4 三地址代码

“2.8.4 三地址代码”在本页不是孤立名词。先在“构造抽象语法”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“发射三地址代码”检查第一条结构差异。围绕上下文无关文法记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

## 分步视觉验证

先预测当前阶段的树、图、集合、表项或指令，再运行变换；若不同，停在第一处分叉并保留原始证据。

<Stepper>
  <Step title="画出输入、变换与产物">
    标出上下文无关文法、语法制导定义、预测分析、抽象语法、三地址代码的输入、输出、所有权、算法前提和下游消费者。
    <Dbc02SimpleSyntaxDirectedTranslatorMapLab />
  </Step>
  <Step title="运行单变量实验">
    固定源程序与目标模型，只改变一个文法、分析、优化、运行时或机器条件。
    <Dbc02SimpleSyntaxDirectedTranslatorExperimentLab />
  </Step>
  <Step title="核对第一处分叉">
    比较结构化产物、复杂度和行为，清理旧输出后完整重建并重放回归样本。
    <Dbc02SimpleSyntaxDirectedTranslatorEvidenceLab />
  </Step>
</Stepper>

## 常见误区

<Callout type="trap">
  **只看最终结果：**
  没有词法单元、语法树、属性依赖、CFG、数据流集合、调度表和寄存器映射，就无法把错误归因到具体阶段，也无法证明中间变换保持语义。
</Callout>

<Callout type="trap">
  **先变换再补条件：**
  优化、调度、并行化和过程间分析必须先证明前提；任何会令支配、活跃、别名、依赖或调用图失效的修改，都要触发明确重算。
</Callout>

## 四类样本与通过条件

| 样本 | 单一变量               | 必查证据         | 通过条件         |
| ---- | ---------------------- | ---------------- | ---------------- |
| 正常 | 一个最小语言或机器功能 | 全阶段快照       | 与预测一致       |
| 边界 | 深度、宽度、延迟或压力 | 临界分支和成本   | 边界可解释       |
| 失败 | 一个文法、事实或约束   | 首错与下游停止   | 不产出伪结果     |
| 优化 | 一个变换或策略         | 前后IR与差分执行 | 等价且收益可解释 |

## 练习

<Exercises>

**问题 1** 为什么不能只凭最终程序运行成功验收第2章 一个简单的语法制导翻译器？

<Answer>
  最终行为可能隐藏错误词法单元、树结构、属性值、数据流事实、依赖边或机器约束。应保存每层结构化产物，定位第一处分叉，并从固定源程序清理重建。
</Answer>

**问题 2** 如何把“扫描词法单元”变成可回归实验？

<Answer>
  固定输入、实现版本、中间表示和目标模型，先预测上下文无关文法与语法制导定义的中间状态，只改变一个条件，并保留正常、边界、失败和优化四类样本。
</Answer>

**问题 3** 本页最小通过条件是什么？

<Answer>
  每次推导消费与文法匹配的输入，语义动作按依赖顺序执行，抽象语法舍弃的标点不影响运算符结合性、优先级与生成代码。
  修复后必须清理旧产物，完整重建全部下游结果，并让历史样本回到基线。
</Answer>

</Exercises>

## 小结

- 上下文无关文法：连接“定义文法”的算法前提、结构化产物、成本和回归证据。
- 语法制导定义：连接“消除左递归”的算法前提、结构化产物、成本和回归证据。
- 预测分析：连接“扫描词法单元”的算法前提、结构化产物、成本和回归证据。
- 抽象语法：连接“执行语义动作”的算法前提、结构化产物、成本和回归证据。
- 三地址代码：连接“构造抽象语法”的算法前提、结构化产物、成本和回归证据。
- 本页41个核心目录层级均已进入可追溯的编译证据链。

## 术语表

<Glossary>
  <GlossaryItem term="上下文无关文法">
    上下文无关文法用于解释“定义文法”，需要同时说明输入契约、算法前提、结构化产物、复杂度和清理重建方法。
  </GlossaryItem>
  <GlossaryItem term="语法制导定义">
    语法制导定义用于解释“消除左递归”，需要同时说明输入契约、算法前提、结构化产物、复杂度和清理重建方法。
  </GlossaryItem>
  <GlossaryItem term="预测分析">
    预测分析用于解释“扫描词法单元”，需要同时说明输入契约、算法前提、结构化产物、复杂度和清理重建方法。
  </GlossaryItem>
  <GlossaryItem term="抽象语法">
    抽象语法用于解释“执行语义动作”，需要同时说明输入契约、算法前提、结构化产物、复杂度和清理重建方法。
  </GlossaryItem>
  <GlossaryItem term="三地址代码">
    三地址代码用于解释“构造抽象语法”，需要同时说明输入契约、算法前提、结构化产物、复杂度和清理重建方法。
  </GlossaryItem>
</Glossary>

<Attribution
  adaptedFrom="Alfred V. Aho、Monica S. Lam、Ravi Sethi、Jeffrey D. Ullman著，赵建华、郑滔、戴新宇译《编译原理（第2版）》，机械工业出版社，2009，631页，ISBN 9787111251217 · 基于Pearson官方第二版资料、中文版完整目录与高校图书馆元数据的独立教学重构"
  adaptedUrl="https://www.pearson.com/en-us/subject-catalog/p/compilers-principles-techniques-and-tools/P200000003472/9780133002140"
/>
