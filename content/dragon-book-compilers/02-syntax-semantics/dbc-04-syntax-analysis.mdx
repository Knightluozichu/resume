---
title: "第4章 语法分析"
type: C
section: "语法、语义与中间代码"
order: 4
description: "第4章 语法分析逐项覆盖53个核心目录层级，以结构化中间产物、运行轨迹与失败对照建立可复查的编译实验。"
demo: true
math: false
sourceUrl: "https://www.pearson.com/en-us/subject-catalog/p/compilers-principles-techniques-and-tools/P200000003472/9780133002140"
draft: true
---

import {
  Dbc04SyntaxAnalysisMapLab,
  Dbc04SyntaxAnalysisExperimentLab,
  Dbc04SyntaxAnalysisEvidenceLab,
} from "@/components/mdx/dragon-book-compilers/diagrams/dbc-04-syntax-analysis";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第4章 语法分析在完整编译流水线中的输入、输出、数据结构和责任边界
- 能描述本页53个核心目录层级之间的依赖、算法前提与工程取舍
- 能复现最小三地址代码或伪代码实验，保存正常、边界、失败和优化四类证据
- 能判断“合法词法单元流得到预期推导与树结构，分析表中的每个动作都有文法依据，冲突不被静默覆盖，非法输入停在最早可确定位置。”是否成立，而不是只看最终程序是否运行

</Objectives>

## 为什么最终输出正确仍然不够

<Term def="上下文无关文法连接“冻结文法”阶段的输入、算法前提、结构化结果、成本和第一处失败证据。">上下文无关文法</Term>、<Term def="FIRST与FOLLOW连接“消除左递归”阶段的输入、算法前提、结构化结果、成本和第一处失败证据。">FIRST与FOLLOW</Term>、<Term def="LL(1)连接“计算预测集合”阶段的输入、算法前提、结构化结果、成本和第一处失败证据。">LL(1)</Term>、<Term def="LR项目连接“构造LR项目集”阶段的输入、算法前提、结构化结果、成本和第一处失败证据。">LR项目</Term>、<Term def="移进-归约冲突连接“生成分析表”阶段的输入、算法前提、结构化结果、成本和第一处失败证据。">移进-归约冲突</Term>
共同构成本页坐标。从上下文无关文法建立LL与LR两条分析路线，逐步计算FIRST/FOLLOW、项目集族与分析表，并用冲突和错误恢复反例校验文法。

编译器是一串互相依赖的转换。错误的扫描器可能恰好产生可分析的词法单元，错误的语法树可能被后续语义动作意外修补，错误的数据流事实可能只在冷路径暴露，违反机器约束的调度也可能在当前样本中碰巧执行。因此必须保存阶段输入、输出和不变量，定位第一处偏离，不能把单个样例跑通当作证明。

## 核心模型与实验账本

实验固定源程序、编译器提交、中间表示格式、目标机模型、优化级别和随机种子。前端保存词法单元、推导、语法树、属性值与类型环境；中端保存基本块、控制流图、支配关系、数据流集合和每次变换前后的IR；后端保存模式覆盖、依赖图、发射周期、寄存器映射、溢出代码与最终指令。每份产物都带生成方式、退出状态和摘要值。

样本分成四组。正常样本证明主路径，边界样本覆盖空输入、深嵌套、二义性、不可归约流图、寄存器压力、长延迟、别名和动态调用；失败样本只破坏一个语法、类型、数据流或机器约束；优化样本同时保存变换前后版本，并用解释执行或差分执行核对可观察行为。

诊断记录阶段、源位置或IR位置、实际状态、期望状态、相关前驱和首要根因。某阶段失败后不得继续消费旧缓存伪造下游结果；分析事实被变换失效时，必须显式重算或证明仍然有效。性能结论与正确性结论分开：前者记录静态成本、动态计数、代码大小和运行环境，后者要求语义与全部约束成立。

目录项进入追溯矩阵后，至少关联一个输入、一个算法前提、一份结构化产物、一个失败对照和一个回归断言。跨章接口同时记录生产者和消费者，这样既能发现“提到概念却没有验证”的空洞覆盖，也能在实现变化后只重放真正受影响的实验。

## 六阶段证据链

### 1. 冻结文法

围绕上下文无关文法先预测结构化结果，只改变一个条件并运行；进入“消除左递归”前，核对语义、算法前提、成本和首错位置。

### 2. 消除左递归

围绕FIRST与FOLLOW先预测结构化结果，只改变一个条件并运行；进入“计算预测集合”前，核对语义、算法前提、成本和首错位置。

### 3. 计算预测集合

围绕LL(1)先预测结构化结果，只改变一个条件并运行；进入“构造LR项目集”前，核对语义、算法前提、成本和首错位置。

### 4. 构造LR项目集

围绕LR项目先预测结构化结果，只改变一个条件并运行；进入“生成分析表”前，核对语义、算法前提、成本和首错位置。

### 5. 生成分析表

围绕移进-归约冲突先预测结构化结果，只改变一个条件并运行；进入“重放错误恢复”前，核对语义、算法前提、成本和首错位置。

### 6. 重放错误恢复

围绕上下文无关文法先预测结构化结果，只改变一个条件并运行；进入“冻结文法”前，核对语义、算法前提、成本和首错位置。

## 可运行的最小骨架

```text
E -> E + T | T
T -> T * F | F
F -> ( E ) | id
```

```text
ACTION[s, a] = shift t | reduce A -> beta | accept | error
```

```text
stack: 0 E 1 + 6 T 9
input: * id $
action: shift 7
```

<Callout type="trap">
  **版本边界：**
  本页复原的是龙书第二版的算法模型和目录边界。现代LLVM、GCC、JVM或具体ISA可以用来做对照，但不能把其默认实现反写成原书算法的保证。
</Callout>

## 权威目录逐项讲解

### 第4章 语法分析

“第4章 语法分析”在本页不是孤立名词。先在“冻结文法”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“消除左递归”检查第一条结构差异。围绕上下文无关文法记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 4.1 引论

“4.1 引论”在本页不是孤立名词。先在“消除左递归”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“计算预测集合”检查第一条结构差异。围绕FIRST与FOLLOW记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 4.1.1 语法分析器的作用

“4.1.1 语法分析器的作用”在本页不是孤立名词。先在“计算预测集合”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“构造LR项目集”检查第一条结构差异。围绕LL(1)记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 4.1.2 代表性的文法

“4.1.2 代表性的文法”在本页不是孤立名词。先在“构造LR项目集”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“生成分析表”检查第一条结构差异。围绕LR项目记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 4.1.3 语法错误的处理

“4.1.3 语法错误的处理”在本页不是孤立名词。先在“生成分析表”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“重放错误恢复”检查第一条结构差异。围绕移进-归约冲突记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 4.1.4 错误恢复策略

“4.1.4 错误恢复策略”在本页不是孤立名词。先在“重放错误恢复”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“冻结文法”检查第一条结构差异。围绕上下文无关文法记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 4.2 上下文无关文法

“4.2 上下文无关文法”在本页不是孤立名词。先在“冻结文法”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“消除左递归”检查第一条结构差异。围绕FIRST与FOLLOW记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 4.2.1 上下文无关文法的形式定义

“4.2.1 上下文无关文法的形式定义”在本页不是孤立名词。先在“消除左递归”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“计算预测集合”检查第一条结构差异。围绕LL(1)记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 4.2.2 符号表示的约定

“4.2.2 符号表示的约定”在本页不是孤立名词。先在“计算预测集合”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“构造LR项目集”检查第一条结构差异。围绕LR项目记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 4.2.3 推导

“4.2.3 推导”在本页不是孤立名词。先在“构造LR项目集”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“生成分析表”检查第一条结构差异。围绕移进-归约冲突记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 4.2.4 语法分析树和推导

“4.2.4 语法分析树和推导”在本页不是孤立名词。先在“生成分析表”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“重放错误恢复”检查第一条结构差异。围绕上下文无关文法记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 4.2.5 二义性

“4.2.5 二义性”在本页不是孤立名词。先在“重放错误恢复”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“冻结文法”检查第一条结构差异。围绕FIRST与FOLLOW记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 4.2.6 验证文法生成的语言

“4.2.6 验证文法生成的语言”在本页不是孤立名词。先在“冻结文法”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“消除左递归”检查第一条结构差异。围绕LL(1)记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 4.2.7 上下文无关文法与正则表达式

“4.2.7 上下文无关文法与正则表达式”在本页不是孤立名词。先在“消除左递归”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“计算预测集合”检查第一条结构差异。围绕LR项目记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 4.3 文法的书写

“4.3 文法的书写”在本页不是孤立名词。先在“计算预测集合”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“构造LR项目集”检查第一条结构差异。围绕移进-归约冲突记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 4.3.1 词法分析和语法分析

“4.3.1 词法分析和语法分析”在本页不是孤立名词。先在“构造LR项目集”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“生成分析表”检查第一条结构差异。围绕上下文无关文法记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 4.3.2 消除二义性

“4.3.2 消除二义性”在本页不是孤立名词。先在“生成分析表”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“重放错误恢复”检查第一条结构差异。围绕FIRST与FOLLOW记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 4.3.3 消除左递归

“4.3.3 消除左递归”在本页不是孤立名词。先在“重放错误恢复”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“冻结文法”检查第一条结构差异。围绕LL(1)记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 4.3.4 提取左公因子

“4.3.4 提取左公因子”在本页不是孤立名词。先在“冻结文法”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“消除左递归”检查第一条结构差异。围绕LR项目记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 4.3.5 非上下文无关语言构造

“4.3.5 非上下文无关语言构造”在本页不是孤立名词。先在“消除左递归”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“计算预测集合”检查第一条结构差异。围绕移进-归约冲突记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 4.4 自顶向下的语法分析

“4.4 自顶向下的语法分析”在本页不是孤立名词。先在“计算预测集合”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“构造LR项目集”检查第一条结构差异。围绕上下文无关文法记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 4.4.1 递归下降的语法分析

“4.4.1 递归下降的语法分析”在本页不是孤立名词。先在“构造LR项目集”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“生成分析表”检查第一条结构差异。围绕FIRST与FOLLOW记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 4.4.2 FIRST和FOLLOW

“4.4.2 FIRST和FOLLOW”在本页不是孤立名词。先在“生成分析表”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“重放错误恢复”检查第一条结构差异。围绕LL(1)记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 4.4.3 LL(1)文法

“4.4.3 LL(1)文法”在本页不是孤立名词。先在“重放错误恢复”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“冻结文法”检查第一条结构差异。围绕LR项目记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 4.4.4 非递归的预测分析

“4.4.4 非递归的预测分析”在本页不是孤立名词。先在“冻结文法”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“消除左递归”检查第一条结构差异。围绕移进-归约冲突记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 4.4.5 预测分析中的错误恢复

“4.4.5 预测分析中的错误恢复”在本页不是孤立名词。先在“消除左递归”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“计算预测集合”检查第一条结构差异。围绕上下文无关文法记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 4.5 自底向上的语法分析

“4.5 自底向上的语法分析”在本页不是孤立名词。先在“计算预测集合”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“构造LR项目集”检查第一条结构差异。围绕FIRST与FOLLOW记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 4.5.1 归约

“4.5.1 归约”在本页不是孤立名词。先在“构造LR项目集”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“生成分析表”检查第一条结构差异。围绕LL(1)记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 4.5.2 句柄剪枝

“4.5.2 句柄剪枝”在本页不是孤立名词。先在“生成分析表”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“重放错误恢复”检查第一条结构差异。围绕LR项目记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 4.5.3 移进-归约语法分析

“4.5.3 移进-归约语法分析”在本页不是孤立名词。先在“重放错误恢复”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“冻结文法”检查第一条结构差异。围绕移进-归约冲突记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 4.5.4 移进-归约语法分析中的冲突

“4.5.4 移进-归约语法分析中的冲突”在本页不是孤立名词。先在“冻结文法”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“消除左递归”检查第一条结构差异。围绕上下文无关文法记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 4.6 LR语法分析技术介绍：简单LR

“4.6 LR语法分析技术介绍：简单LR”在本页不是孤立名词。先在“消除左递归”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“计算预测集合”检查第一条结构差异。围绕FIRST与FOLLOW记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 4.6.1 为什么使用LR语法分析器

“4.6.1 为什么使用LR语法分析器”在本页不是孤立名词。先在“计算预测集合”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“构造LR项目集”检查第一条结构差异。围绕LL(1)记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 4.6.2 项和LR(0)自动机

“4.6.2 项和LR(0)自动机”在本页不是孤立名词。先在“构造LR项目集”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“生成分析表”检查第一条结构差异。围绕LR项目记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 4.6.3 LR语法分析算法

“4.6.3 LR语法分析算法”在本页不是孤立名词。先在“生成分析表”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“重放错误恢复”检查第一条结构差异。围绕移进-归约冲突记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 4.6.4 构造SLR语法分析表

“4.6.4 构造SLR语法分析表”在本页不是孤立名词。先在“重放错误恢复”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“冻结文法”检查第一条结构差异。围绕上下文无关文法记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 4.6.5 可行前缀

“4.6.5 可行前缀”在本页不是孤立名词。先在“冻结文法”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“消除左递归”检查第一条结构差异。围绕FIRST与FOLLOW记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 4.7 更强大的LR语法分析器

“4.7 更强大的LR语法分析器”在本页不是孤立名词。先在“消除左递归”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“计算预测集合”检查第一条结构差异。围绕LL(1)记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 4.7.1 规范LR(1)项

“4.7.1 规范LR(1)项”在本页不是孤立名词。先在“计算预测集合”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“构造LR项目集”检查第一条结构差异。围绕LR项目记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 4.7.2 构造LR(1)项集

“4.7.2 构造LR(1)项集”在本页不是孤立名词。先在“构造LR项目集”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“生成分析表”检查第一条结构差异。围绕移进-归约冲突记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 4.7.3 规范LR(1)分析表

“4.7.3 规范LR(1)分析表”在本页不是孤立名词。先在“生成分析表”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“重放错误恢复”检查第一条结构差异。围绕上下文无关文法记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 4.7.4 构造LALR分析表

“4.7.4 构造LALR分析表”在本页不是孤立名词。先在“重放错误恢复”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“冻结文法”检查第一条结构差异。围绕FIRST与FOLLOW记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 4.7.5 LALR分析表的高效构造

“4.7.5 LALR分析表的高效构造”在本页不是孤立名词。先在“冻结文法”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“消除左递归”检查第一条结构差异。围绕LL(1)记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 4.7.6 LR分析表的压缩

“4.7.6 LR分析表的压缩”在本页不是孤立名词。先在“消除左递归”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“计算预测集合”检查第一条结构差异。围绕LR项目记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 4.8 使用二义性文法

“4.8 使用二义性文法”在本页不是孤立名词。先在“计算预测集合”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“构造LR项目集”检查第一条结构差异。围绕移进-归约冲突记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 4.8.1 用优先级和结合性解决冲突

“4.8.1 用优先级和结合性解决冲突”在本页不是孤立名词。先在“构造LR项目集”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“生成分析表”检查第一条结构差异。围绕上下文无关文法记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 4.8.2 悬空else二义性

“4.8.2 悬空else二义性”在本页不是孤立名词。先在“生成分析表”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“重放错误恢复”检查第一条结构差异。围绕FIRST与FOLLOW记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 4.8.3 LR语法分析中的错误恢复

“4.8.3 LR语法分析中的错误恢复”在本页不是孤立名词。先在“重放错误恢复”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“冻结文法”检查第一条结构差异。围绕LL(1)记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 4.9 语法分析器生成工具

“4.9 语法分析器生成工具”在本页不是孤立名词。先在“冻结文法”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“消除左递归”检查第一条结构差异。围绕LR项目记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 4.9.1 语法分析器生成工具Yacc

“4.9.1 语法分析器生成工具Yacc”在本页不是孤立名词。先在“消除左递归”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“计算预测集合”检查第一条结构差异。围绕移进-归约冲突记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 4.9.2 使用Yacc处理二义性文法

“4.9.2 使用Yacc处理二义性文法”在本页不是孤立名词。先在“计算预测集合”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“构造LR项目集”检查第一条结构差异。围绕上下文无关文法记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 4.9.3 用Lex创建Yacc词法分析器

“4.9.3 用Lex创建Yacc词法分析器”在本页不是孤立名词。先在“构造LR项目集”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“生成分析表”检查第一条结构差异。围绕FIRST与FOLLOW记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 4.9.4 Yacc中的错误恢复

“4.9.4 Yacc中的错误恢复”在本页不是孤立名词。先在“生成分析表”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“重放错误恢复”检查第一条结构差异。围绕LL(1)记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

## 分步视觉验证

先预测当前阶段的树、图、集合、表项或指令，再运行变换；若不同，停在第一处分叉并保留原始证据。

<Stepper>
  <Step title="画出输入、变换与产物">
    标出上下文无关文法、FIRST与FOLLOW、LL(1)、LR项目、移进-归约冲突的输入、输出、所有权、算法前提和下游消费者。
    <Dbc04SyntaxAnalysisMapLab />
  </Step>
  <Step title="运行单变量实验">
    固定源程序与目标模型，只改变一个文法、分析、优化、运行时或机器条件。
    <Dbc04SyntaxAnalysisExperimentLab />
  </Step>
  <Step title="核对第一处分叉">
    比较结构化产物、复杂度和行为，清理旧输出后完整重建并重放回归样本。
    <Dbc04SyntaxAnalysisEvidenceLab />
  </Step>
</Stepper>

## 常见误区

<Callout type="trap">
  **只看最终结果：**
  没有词法单元、语法树、属性依赖、CFG、数据流集合、调度表和寄存器映射，就无法把错误归因到具体阶段，也无法证明中间变换保持语义。
</Callout>

<Callout type="trap">
  **先变换再补条件：**
  优化、调度、并行化和过程间分析必须先证明前提；任何会令支配、活跃、别名、依赖或调用图失效的修改，都要触发明确重算。
</Callout>

## 四类样本与通过条件

| 样本 | 单一变量               | 必查证据         | 通过条件         |
| ---- | ---------------------- | ---------------- | ---------------- |
| 正常 | 一个最小语言或机器功能 | 全阶段快照       | 与预测一致       |
| 边界 | 深度、宽度、延迟或压力 | 临界分支和成本   | 边界可解释       |
| 失败 | 一个文法、事实或约束   | 首错与下游停止   | 不产出伪结果     |
| 优化 | 一个变换或策略         | 前后IR与差分执行 | 等价且收益可解释 |

## 练习

<Exercises>

**问题 1** 为什么不能只凭最终程序运行成功验收第4章 语法分析？

<Answer>
  最终行为可能隐藏错误词法单元、树结构、属性值、数据流事实、依赖边或机器约束。应保存每层结构化产物，定位第一处分叉，并从固定源程序清理重建。
</Answer>

**问题 2** 如何把“计算预测集合”变成可回归实验？

<Answer>
  固定输入、实现版本、中间表示和目标模型，先预测上下文无关文法与FIRST与FOLLOW的中间状态，只改变一个条件，并保留正常、边界、失败和优化四类样本。
</Answer>

**问题 3** 本页最小通过条件是什么？

<Answer>
  合法词法单元流得到预期推导与树结构，分析表中的每个动作都有文法依据，冲突不被静默覆盖，非法输入停在最早可确定位置。
  修复后必须清理旧产物，完整重建全部下游结果，并让历史样本回到基线。
</Answer>

</Exercises>

## 小结

- 上下文无关文法：连接“冻结文法”的算法前提、结构化产物、成本和回归证据。
- FIRST与FOLLOW：连接“消除左递归”的算法前提、结构化产物、成本和回归证据。
- LL(1)：连接“计算预测集合”的算法前提、结构化产物、成本和回归证据。
- LR项目：连接“构造LR项目集”的算法前提、结构化产物、成本和回归证据。
- 移进-归约冲突：连接“生成分析表”的算法前提、结构化产物、成本和回归证据。
- 本页53个核心目录层级均已进入可追溯的编译证据链。

## 术语表

<Glossary>
  <GlossaryItem term="上下文无关文法">
    上下文无关文法用于解释“冻结文法”，需要同时说明输入契约、算法前提、结构化产物、复杂度和清理重建方法。
  </GlossaryItem>
  <GlossaryItem term="FIRST与FOLLOW">
    FIRST与FOLLOW用于解释“消除左递归”，需要同时说明输入契约、算法前提、结构化产物、复杂度和清理重建方法。
  </GlossaryItem>
  <GlossaryItem term="LL(1)">
    LL(1)用于解释“计算预测集合”，需要同时说明输入契约、算法前提、结构化产物、复杂度和清理重建方法。
  </GlossaryItem>
  <GlossaryItem term="LR项目">
    LR项目用于解释“构造LR项目集”，需要同时说明输入契约、算法前提、结构化产物、复杂度和清理重建方法。
  </GlossaryItem>
  <GlossaryItem term="移进-归约冲突">
    移进-归约冲突用于解释“生成分析表”，需要同时说明输入契约、算法前提、结构化产物、复杂度和清理重建方法。
  </GlossaryItem>
</Glossary>

<Attribution
  adaptedFrom="Alfred V. Aho、Monica S. Lam、Ravi Sethi、Jeffrey D. Ullman著，赵建华、郑滔、戴新宇译《编译原理（第2版）》，机械工业出版社，2009，631页，ISBN 9787111251217 · 基于Pearson官方第二版资料、中文版完整目录与高校图书馆元数据的独立教学重构"
  adaptedUrl="https://www.pearson.com/en-us/subject-catalog/p/compilers-principles-techniques-and-tools/P200000003472/9780133002140"
/>
