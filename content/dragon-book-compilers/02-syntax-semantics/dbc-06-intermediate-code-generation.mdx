---
title: "第6章 中间代码生成"
type: C
section: "语法、语义与中间代码"
order: 6
description: "第6章 中间代码生成逐项覆盖43个核心目录层级，以结构化中间产物、运行轨迹与失败对照建立可复查的编译实验。"
demo: true
math: false
sourceUrl: "https://www.pearson.com/en-us/subject-catalog/p/compilers-principles-techniques-and-tools/P200000003472/9780133002140"
draft: false
---

import {
  Dbc06IntermediateCodeGenerationMapLab,
  Dbc06IntermediateCodeGenerationExperimentLab,
  Dbc06IntermediateCodeGenerationEvidenceLab,
} from "@/components/mdx/dragon-book-compilers/diagrams/dbc-06-intermediate-code-generation";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第6章 中间代码生成在完整编译流水线中的输入、输出、数据结构和责任边界
- 能描述本页43个核心目录层级之间的依赖、算法前提与工程取舍
- 能复现最小三地址代码或伪代码实验，保存正常、边界、失败和优化四类证据
- 能判断“每个临时值和跳转目标均有定义，类型转换显式可查，数组地址遵守布局公式，控制流边与回填列表最终完全闭合。”是否成立，而不是只看最终程序是否运行

</Objectives>

## 为什么最终输出正确仍然不够

<Term def="DAG连接“构造表达式DAG”阶段的输入、算法前提、结构化结果、成本和第一处失败证据。">
  DAG
</Term>
、
<Term def="三地址代码连接“发射三地址码”阶段的输入、算法前提、结构化结果、成本和第一处失败证据。">
  三地址代码
</Term>
、
<Term def="SSA连接“检查并转换类型”阶段的输入、算法前提、结构化结果、成本和第一处失败证据。">
  SSA
</Term>
、
<Term def="类型检查连接“翻译布尔控制流”阶段的输入、算法前提、结构化结果、成本和第一处失败证据。">
  类型检查
</Term>
、
<Term def="回填连接“回填跳转目标”阶段的输入、算法前提、结构化结果、成本和第一处失败证据。">
  回填
</Term>
共同构成本页坐标。把类型、声明、表达式、布尔控制流、回填和过程调用统一翻译为可分析的语法树、DAG、三地址代码与SSA边界。

编译器是一串互相依赖的转换。错误的扫描器可能恰好产生可分析的词法单元，错误的语法树可能被后续语义动作意外修补，错误的数据流事实可能只在冷路径暴露，违反机器约束的调度也可能在当前样本中碰巧执行。因此必须保存阶段输入、输出和不变量，定位第一处偏离，不能把单个样例跑通当作证明。

## 核心模型与实验账本

实验固定源程序、编译器提交、中间表示格式、目标机模型、优化级别和随机种子。前端保存词法单元、推导、语法树、属性值与类型环境；中端保存基本块、控制流图、支配关系、数据流集合和每次变换前后的IR；后端保存模式覆盖、依赖图、发射周期、寄存器映射、溢出代码与最终指令。每份产物都带生成方式、退出状态和摘要值。

样本分成四组。正常样本证明主路径，边界样本覆盖空输入、深嵌套、二义性、不可归约流图、寄存器压力、长延迟、别名和动态调用；失败样本只破坏一个语法、类型、数据流或机器约束；优化样本同时保存变换前后版本，并用解释执行或差分执行核对可观察行为。

诊断记录阶段、源位置或IR位置、实际状态、期望状态、相关前驱和首要根因。某阶段失败后不得继续消费旧缓存伪造下游结果；分析事实被变换失效时，必须显式重算或证明仍然有效。性能结论与正确性结论分开：前者记录静态成本、动态计数、代码大小和运行环境，后者要求语义与全部约束成立。

目录项进入追溯矩阵后，至少关联一个输入、一个算法前提、一份结构化产物、一个失败对照和一个回归断言。跨章接口同时记录生产者和消费者，这样既能发现“提到概念却没有验证”的空洞覆盖，也能在实现变化后只重放真正受影响的实验。

## 六阶段证据链

### 1. 构造表达式DAG

围绕DAG先预测结构化结果，只改变一个条件并运行；进入“发射三地址码”前，核对语义、算法前提、成本和首错位置。

### 2. 发射三地址码

围绕三地址代码先预测结构化结果，只改变一个条件并运行；进入“检查并转换类型”前，核对语义、算法前提、成本和首错位置。

### 3. 检查并转换类型

围绕SSA先预测结构化结果，只改变一个条件并运行；进入“翻译布尔控制流”前，核对语义、算法前提、成本和首错位置。

### 4. 翻译布尔控制流

围绕类型检查先预测结构化结果，只改变一个条件并运行；进入“回填跳转目标”前，核对语义、算法前提、成本和首错位置。

### 5. 回填跳转目标

围绕回填先预测结构化结果，只改变一个条件并运行；进入“核对过程边界”前，核对语义、算法前提、成本和首错位置。

### 6. 核对过程边界

围绕DAG先预测结构化结果，只改变一个条件并运行；进入“构造表达式DAG”前，核对语义、算法前提、成本和首错位置。

## 可运行的最小骨架

```text
x = y op z
x = op y
if x relop y goto L
```

```text
B.truelist = makelist(nextinstr)
B.falselist = makelist(nextinstr + 1)
```

```text
address(A[i]) = base(A) + (i - low) * width
```

<Callout type="trap">
  **版本边界：**
  本页复原的是龙书第二版的算法模型和目录边界。现代LLVM、GCC、JVM或具体ISA可以用来做对照，但不能把其默认实现反写成原书算法的保证。
</Callout>

## 权威目录逐项讲解

### 第6章 中间代码生成

“第6章 中间代码生成”在本页不是孤立名词。先在“构造表达式DAG”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“发射三地址码”检查第一条结构差异。围绕DAG记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 6.1 语法树的变体

“6.1 语法树的变体”在本页不是孤立名词。先在“发射三地址码”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“检查并转换类型”检查第一条结构差异。围绕三地址代码记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 6.1.1 表达式的DAG

“6.1.1 表达式的DAG”在本页不是孤立名词。先在“检查并转换类型”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“翻译布尔控制流”检查第一条结构差异。围绕SSA记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 6.1.2 构造DAG的值编码方法

“6.1.2 构造DAG的值编码方法”在本页不是孤立名词。先在“翻译布尔控制流”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“回填跳转目标”检查第一条结构差异。围绕类型检查记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 6.2 三地址代码

“6.2 三地址代码”在本页不是孤立名词。先在“回填跳转目标”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“核对过程边界”检查第一条结构差异。围绕回填记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 6.2.1 地址和指令

“6.2.1 地址和指令”在本页不是孤立名词。先在“核对过程边界”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“构造表达式DAG”检查第一条结构差异。围绕DAG记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 6.2.2 四元式

“6.2.2 四元式”在本页不是孤立名词。先在“构造表达式DAG”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“发射三地址码”检查第一条结构差异。围绕三地址代码记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 6.2.3 三元式

“6.2.3 三元式”在本页不是孤立名词。先在“发射三地址码”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“检查并转换类型”检查第一条结构差异。围绕SSA记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 6.2.4 静态单赋值形式

“6.2.4 静态单赋值形式”在本页不是孤立名词。先在“检查并转换类型”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“翻译布尔控制流”检查第一条结构差异。围绕类型检查记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 6.3 类型和声明

“6.3 类型和声明”在本页不是孤立名词。先在“翻译布尔控制流”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“回填跳转目标”检查第一条结构差异。围绕回填记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 6.3.1 类型表达式

“6.3.1 类型表达式”在本页不是孤立名词。先在“回填跳转目标”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“核对过程边界”检查第一条结构差异。围绕DAG记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 6.3.2 类型等价

“6.3.2 类型等价”在本页不是孤立名词。先在“核对过程边界”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“构造表达式DAG”检查第一条结构差异。围绕三地址代码记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 6.3.3 声明

“6.3.3 声明”在本页不是孤立名词。先在“构造表达式DAG”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“发射三地址码”检查第一条结构差异。围绕SSA记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 6.3.4 局部名字的存储布局

“6.3.4 局部名字的存储布局”在本页不是孤立名词。先在“发射三地址码”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“检查并转换类型”检查第一条结构差异。围绕类型检查记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 6.3.5 声明的序列

“6.3.5 声明的序列”在本页不是孤立名词。先在“检查并转换类型”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“翻译布尔控制流”检查第一条结构差异。围绕回填记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 6.3.6 记录和类中的字段

“6.3.6 记录和类中的字段”在本页不是孤立名词。先在“翻译布尔控制流”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“回填跳转目标”检查第一条结构差异。围绕DAG记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 6.4 表达式的翻译

“6.4 表达式的翻译”在本页不是孤立名词。先在“回填跳转目标”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“核对过程边界”检查第一条结构差异。围绕三地址代码记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 6.4.1 表达式中的运算

“6.4.1 表达式中的运算”在本页不是孤立名词。先在“核对过程边界”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“构造表达式DAG”检查第一条结构差异。围绕SSA记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 6.4.2 增量翻译

“6.4.2 增量翻译”在本页不是孤立名词。先在“构造表达式DAG”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“发射三地址码”检查第一条结构差异。围绕类型检查记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 6.4.3 数组元素的寻址

“6.4.3 数组元素的寻址”在本页不是孤立名词。先在“发射三地址码”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“检查并转换类型”检查第一条结构差异。围绕回填记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 6.4.4 数组引用的翻译

“6.4.4 数组引用的翻译”在本页不是孤立名词。先在“检查并转换类型”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“翻译布尔控制流”检查第一条结构差异。围绕DAG记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 6.5 类型检查

“6.5 类型检查”在本页不是孤立名词。先在“翻译布尔控制流”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“回填跳转目标”检查第一条结构差异。围绕三地址代码记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 6.5.1 类型检查规则

“6.5.1 类型检查规则”在本页不是孤立名词。先在“回填跳转目标”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“核对过程边界”检查第一条结构差异。围绕SSA记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 6.5.2 类型转换

“6.5.2 类型转换”在本页不是孤立名词。先在“核对过程边界”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“构造表达式DAG”检查第一条结构差异。围绕类型检查记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 6.5.3 函数和运算符的重载

“6.5.3 函数和运算符的重载”在本页不是孤立名词。先在“构造表达式DAG”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“发射三地址码”检查第一条结构差异。围绕回填记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 6.5.4 类型推导和多态函数

“6.5.4 类型推导和多态函数”在本页不是孤立名词。先在“发射三地址码”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“检查并转换类型”检查第一条结构差异。围绕DAG记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 6.5.5 一个合一算法

“6.5.5 一个合一算法”在本页不是孤立名词。先在“检查并转换类型”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“翻译布尔控制流”检查第一条结构差异。围绕三地址代码记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 6.6 控制流

“6.6 控制流”在本页不是孤立名词。先在“翻译布尔控制流”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“回填跳转目标”检查第一条结构差异。围绕SSA记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 6.6.1 布尔表达式

“6.6.1 布尔表达式”在本页不是孤立名词。先在“回填跳转目标”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“核对过程边界”检查第一条结构差异。围绕类型检查记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 6.6.2 短路代码

“6.6.2 短路代码”在本页不是孤立名词。先在“核对过程边界”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“构造表达式DAG”检查第一条结构差异。围绕回填记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 6.6.3 控制流语句

“6.6.3 控制流语句”在本页不是孤立名词。先在“构造表达式DAG”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“发射三地址码”检查第一条结构差异。围绕DAG记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 6.6.4 布尔表达式的控制流翻译

“6.6.4 布尔表达式的控制流翻译”在本页不是孤立名词。先在“发射三地址码”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“检查并转换类型”检查第一条结构差异。围绕三地址代码记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 6.6.5 避免冗余的goto指令

“6.6.5 避免冗余的goto指令”在本页不是孤立名词。先在“检查并转换类型”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“翻译布尔控制流”检查第一条结构差异。围绕SSA记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 6.6.6 布尔值和跳转代码

“6.6.6 布尔值和跳转代码”在本页不是孤立名词。先在“翻译布尔控制流”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“回填跳转目标”检查第一条结构差异。围绕类型检查记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 6.7 回填

“6.7 回填”在本页不是孤立名词。先在“回填跳转目标”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“核对过程边界”检查第一条结构差异。围绕回填记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 6.7.1 使用回填的一趟式代码生成

“6.7.1 使用回填的一趟式代码生成”在本页不是孤立名词。先在“核对过程边界”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“构造表达式DAG”检查第一条结构差异。围绕DAG记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 6.7.2 布尔表达式的回填

“6.7.2 布尔表达式的回填”在本页不是孤立名词。先在“构造表达式DAG”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“发射三地址码”检查第一条结构差异。围绕三地址代码记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 6.7.3 控制转移语句

“6.7.3 控制转移语句”在本页不是孤立名词。先在“发射三地址码”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“检查并转换类型”检查第一条结构差异。围绕SSA记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 6.7.4 break、continue和goto语句

“6.7.4 break、continue和goto语句”在本页不是孤立名词。先在“检查并转换类型”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“翻译布尔控制流”检查第一条结构差异。围绕类型检查记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 6.8 switch语句

“6.8 switch语句”在本页不是孤立名词。先在“翻译布尔控制流”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“回填跳转目标”检查第一条结构差异。围绕回填记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 6.8.1 switch语句的翻译

“6.8.1 switch语句的翻译”在本页不是孤立名词。先在“回填跳转目标”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“核对过程边界”检查第一条结构差异。围绕DAG记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 6.8.2 switch语句的语法制导翻译

“6.8.2 switch语句的语法制导翻译”在本页不是孤立名词。先在“核对过程边界”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“构造表达式DAG”检查第一条结构差异。围绕三地址代码记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 6.9 过程的中间代码

“6.9 过程的中间代码”在本页不是孤立名词。先在“构造表达式DAG”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“发射三地址码”检查第一条结构差异。围绕SSA记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

## 分步视觉验证

先预测当前阶段的树、图、集合、表项或指令，再运行变换；若不同，停在第一处分叉并保留原始证据。

<Stepper>
  <Step title="画出输入、变换与产物">
    标出DAG、三地址代码、SSA、类型检查、回填的输入、输出、所有权、算法前提和下游消费者。
    <Dbc06IntermediateCodeGenerationMapLab />
  </Step>
  <Step title="运行单变量实验">
    固定源程序与目标模型，只改变一个文法、分析、优化、运行时或机器条件。
    <Dbc06IntermediateCodeGenerationExperimentLab />
  </Step>
  <Step title="核对第一处分叉">
    比较结构化产物、复杂度和行为，清理旧输出后完整重建并重放回归样本。
    <Dbc06IntermediateCodeGenerationEvidenceLab />
  </Step>
</Stepper>

## 常见误区

<Callout type="trap">
  **只看最终结果：**
  没有词法单元、语法树、属性依赖、CFG、数据流集合、调度表和寄存器映射，就无法把错误归因到具体阶段，也无法证明中间变换保持语义。
</Callout>

<Callout type="trap">
  **先变换再补条件：**
  优化、调度、并行化和过程间分析必须先证明前提；任何会令支配、活跃、别名、依赖或调用图失效的修改，都要触发明确重算。
</Callout>

## 四类样本与通过条件

| 样本 | 单一变量               | 必查证据         | 通过条件         |
| ---- | ---------------------- | ---------------- | ---------------- |
| 正常 | 一个最小语言或机器功能 | 全阶段快照       | 与预测一致       |
| 边界 | 深度、宽度、延迟或压力 | 临界分支和成本   | 边界可解释       |
| 失败 | 一个文法、事实或约束   | 首错与下游停止   | 不产出伪结果     |
| 优化 | 一个变换或策略         | 前后IR与差分执行 | 等价且收益可解释 |

## 练习

<Exercises>

**问题 1** 为什么不能只凭最终程序运行成功验收第6章 中间代码生成？

<Answer>
  最终行为可能隐藏错误词法单元、树结构、属性值、数据流事实、依赖边或机器约束。应保存每层结构化产物，定位第一处分叉，并从固定源程序清理重建。
</Answer>

**问题 2** 如何把“检查并转换类型”变成可回归实验？

<Answer>
  固定输入、实现版本、中间表示和目标模型，先预测DAG与三地址代码的中间状态，只改变一个条件，并保留正常、边界、失败和优化四类样本。
</Answer>

**问题 3** 本页最小通过条件是什么？

<Answer>
  每个临时值和跳转目标均有定义，类型转换显式可查，数组地址遵守布局公式，控制流边与回填列表最终完全闭合。
  修复后必须清理旧产物，完整重建全部下游结果，并让历史样本回到基线。
</Answer>

</Exercises>

## 小结

- DAG：连接“构造表达式DAG”的算法前提、结构化产物、成本和回归证据。
- 三地址代码：连接“发射三地址码”的算法前提、结构化产物、成本和回归证据。
- SSA：连接“检查并转换类型”的算法前提、结构化产物、成本和回归证据。
- 类型检查：连接“翻译布尔控制流”的算法前提、结构化产物、成本和回归证据。
- 回填：连接“回填跳转目标”的算法前提、结构化产物、成本和回归证据。
- 本页43个核心目录层级均已进入可追溯的编译证据链。

## 术语表

<Glossary>
  <GlossaryItem term="DAG">
    DAG用于解释“构造表达式DAG”，需要同时说明输入契约、算法前提、结构化产物、复杂度和清理重建方法。
  </GlossaryItem>
  <GlossaryItem term="三地址代码">
    三地址代码用于解释“发射三地址码”，需要同时说明输入契约、算法前提、结构化产物、复杂度和清理重建方法。
  </GlossaryItem>
  <GlossaryItem term="SSA">
    SSA用于解释“检查并转换类型”，需要同时说明输入契约、算法前提、结构化产物、复杂度和清理重建方法。
  </GlossaryItem>
  <GlossaryItem term="类型检查">
    类型检查用于解释“翻译布尔控制流”，需要同时说明输入契约、算法前提、结构化产物、复杂度和清理重建方法。
  </GlossaryItem>
  <GlossaryItem term="回填">
    回填用于解释“回填跳转目标”，需要同时说明输入契约、算法前提、结构化产物、复杂度和清理重建方法。
  </GlossaryItem>
</Glossary>

<Attribution
  adaptedFrom="Alfred V. Aho、Monica S. Lam、Ravi Sethi、Jeffrey D. Ullman著，赵建华、郑滔、戴新宇译《编译原理（第2版）》，机械工业出版社，2009，631页，ISBN 9787111251217 · 基于Pearson官方第二版资料、中文版完整目录与高校图书馆元数据的独立教学重构"
  adaptedUrl="https://www.pearson.com/en-us/subject-catalog/p/compilers-principles-techniques-and-tools/P200000003472/9780133002140"
/>
