---
title: "第8章 代码生成"
type: C
section: "运行时与目标代码"
order: 8
description: "第8章 代码生成逐项覆盖56个核心目录层级，以结构化中间产物、运行轨迹与失败对照建立可复查的编译实验。"
demo: true
math: false
sourceUrl: "https://www.pearson.com/en-us/subject-catalog/p/compilers-principles-techniques-and-tools/P200000003472/9780133002140"
draft: true
---

import {
  Dbc08CodeGenerationMapLab,
  Dbc08CodeGenerationExperimentLab,
  Dbc08CodeGenerationEvidenceLab,
} from "@/components/mdx/dragon-book-compilers/diagrams/dbc-08-code-generation";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第8章 代码生成在完整编译流水线中的输入、输出、数据结构和责任边界
- 能描述本页56个核心目录层级之间的依赖、算法前提与工程取舍
- 能复现最小三地址代码或伪代码实验，保存正常、边界、失败和优化四类证据
- 能判断“目标指令满足机器约束并保持IR语义，寄存器与地址描述符同步，基本块边界和后继正确，树覆盖成本与所选指令一致。”是否成立，而不是只看最终程序是否运行

</Objectives>

## 为什么最终输出正确仍然不够

<Term def="基本块连接“冻结目标机”阶段的输入、算法前提、结构化结果、成本和第一处失败证据。">基本块</Term>、<Term def="地址描述符连接“划分基本块”阶段的输入、算法前提、结构化结果、成本和第一处失败证据。">地址描述符</Term>、<Term def="窥孔优化连接“维护描述符”阶段的输入、算法前提、结构化结果、成本和第一处失败证据。">窥孔优化</Term>、<Term def="图着色连接“选择指令”阶段的输入、算法前提、结构化结果、成本和第一处失败证据。">图着色</Term>、<Term def="树覆盖连接“分配寄存器”阶段的输入、算法前提、结构化结果、成本和第一处失败证据。">树覆盖</Term>
共同构成本页坐标。在明确目标机、地址、基本块和成本模型后，依次完成指令选择、局部优化、寄存器分配、树覆盖与动态规划代码生成。

编译器是一串互相依赖的转换。错误的扫描器可能恰好产生可分析的词法单元，错误的语法树可能被后续语义动作意外修补，错误的数据流事实可能只在冷路径暴露，违反机器约束的调度也可能在当前样本中碰巧执行。因此必须保存阶段输入、输出和不变量，定位第一处偏离，不能把单个样例跑通当作证明。

## 核心模型与实验账本

实验固定源程序、编译器提交、中间表示格式、目标机模型、优化级别和随机种子。前端保存词法单元、推导、语法树、属性值与类型环境；中端保存基本块、控制流图、支配关系、数据流集合和每次变换前后的IR；后端保存模式覆盖、依赖图、发射周期、寄存器映射、溢出代码与最终指令。每份产物都带生成方式、退出状态和摘要值。

样本分成四组。正常样本证明主路径，边界样本覆盖空输入、深嵌套、二义性、不可归约流图、寄存器压力、长延迟、别名和动态调用；失败样本只破坏一个语法、类型、数据流或机器约束；优化样本同时保存变换前后版本，并用解释执行或差分执行核对可观察行为。

诊断记录阶段、源位置或IR位置、实际状态、期望状态、相关前驱和首要根因。某阶段失败后不得继续消费旧缓存伪造下游结果；分析事实被变换失效时，必须显式重算或证明仍然有效。性能结论与正确性结论分开：前者记录静态成本、动态计数、代码大小和运行环境，后者要求语义与全部约束成立。

目录项进入追溯矩阵后，至少关联一个输入、一个算法前提、一份结构化产物、一个失败对照和一个回归断言。跨章接口同时记录生产者和消费者，这样既能发现“提到概念却没有验证”的空洞覆盖，也能在实现变化后只重放真正受影响的实验。

## 六阶段证据链

### 1. 冻结目标机

围绕基本块先预测结构化结果，只改变一个条件并运行；进入“划分基本块”前，核对语义、算法前提、成本和首错位置。

### 2. 划分基本块

围绕地址描述符先预测结构化结果，只改变一个条件并运行；进入“维护描述符”前，核对语义、算法前提、成本和首错位置。

### 3. 维护描述符

围绕窥孔优化先预测结构化结果，只改变一个条件并运行；进入“选择指令”前，核对语义、算法前提、成本和首错位置。

### 4. 选择指令

围绕图着色先预测结构化结果，只改变一个条件并运行；进入“分配寄存器”前，核对语义、算法前提、成本和首错位置。

### 5. 分配寄存器

围绕树覆盖先预测结构化结果，只改变一个条件并运行；进入“比较目标执行”前，核对语义、算法前提、成本和首错位置。

### 6. 比较目标执行

围绕基本块先预测结构化结果，只改变一个条件并运行；进入“冻结目标机”前，核对语义、算法前提、成本和首错位置。

## 可运行的最小骨架

```text
LD R1, a
ADD R1, R1, b
ST x, R1
```

```text
registerDescriptor[R1] = { x }
addressDescriptor[x] = { R1, mem[x] }
```

```text
cost[node, rule] = ruleCost + sum(cost[child, selectedRule])
```

<Callout type="trap">
  **版本边界：**
  本页复原的是龙书第二版的算法模型和目录边界。现代LLVM、GCC、JVM或具体ISA可以用来做对照，但不能把其默认实现反写成原书算法的保证。
</Callout>

## 权威目录逐项讲解

### 第8章 代码生成

“第8章 代码生成”在本页不是孤立名词。先在“冻结目标机”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“划分基本块”检查第一条结构差异。围绕基本块记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 8.1 代码生成器设计中的问题

“8.1 代码生成器设计中的问题”在本页不是孤立名词。先在“划分基本块”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“维护描述符”检查第一条结构差异。围绕地址描述符记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 8.1.1 代码生成器的输入

“8.1.1 代码生成器的输入”在本页不是孤立名词。先在“维护描述符”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“选择指令”检查第一条结构差异。围绕窥孔优化记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 8.1.2 目标程序

“8.1.2 目标程序”在本页不是孤立名词。先在“选择指令”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“分配寄存器”检查第一条结构差异。围绕图着色记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 8.1.3 指令选择

“8.1.3 指令选择”在本页不是孤立名词。先在“分配寄存器”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“比较目标执行”检查第一条结构差异。围绕树覆盖记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 8.1.4 寄存器分配

“8.1.4 寄存器分配”在本页不是孤立名词。先在“比较目标执行”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“冻结目标机”检查第一条结构差异。围绕基本块记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 8.1.5 求值顺序

“8.1.5 求值顺序”在本页不是孤立名词。先在“冻结目标机”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“划分基本块”检查第一条结构差异。围绕地址描述符记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 8.2 目标语言

“8.2 目标语言”在本页不是孤立名词。先在“划分基本块”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“维护描述符”检查第一条结构差异。围绕窥孔优化记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 8.2.1 一个简单的目标机模型

“8.2.1 一个简单的目标机模型”在本页不是孤立名词。先在“维护描述符”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“选择指令”检查第一条结构差异。围绕图着色记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 8.2.2 程序和指令的代价

“8.2.2 程序和指令的代价”在本页不是孤立名词。先在“选择指令”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“分配寄存器”检查第一条结构差异。围绕树覆盖记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 8.3 目标代码中的地址

“8.3 目标代码中的地址”在本页不是孤立名词。先在“分配寄存器”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“比较目标执行”检查第一条结构差异。围绕基本块记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 8.3.1 静态分配

“8.3.1 静态分配”在本页不是孤立名词。先在“比较目标执行”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“冻结目标机”检查第一条结构差异。围绕地址描述符记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 8.3.2 栈分配

“8.3.2 栈分配”在本页不是孤立名词。先在“冻结目标机”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“划分基本块”检查第一条结构差异。围绕窥孔优化记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 8.3.3 名字的运行时刻地址

“8.3.3 名字的运行时刻地址”在本页不是孤立名词。先在“划分基本块”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“维护描述符”检查第一条结构差异。围绕图着色记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 8.4 基本块和流图

“8.4 基本块和流图”在本页不是孤立名词。先在“维护描述符”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“选择指令”检查第一条结构差异。围绕树覆盖记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 8.4.1 基本块

“8.4.1 基本块”在本页不是孤立名词。先在“选择指令”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“分配寄存器”检查第一条结构差异。围绕基本块记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 8.4.2 下次使用信息

“8.4.2 下次使用信息”在本页不是孤立名词。先在“分配寄存器”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“比较目标执行”检查第一条结构差异。围绕地址描述符记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 8.4.3 流图

“8.4.3 流图”在本页不是孤立名词。先在“比较目标执行”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“冻结目标机”检查第一条结构差异。围绕窥孔优化记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 8.4.4 流图的表示

“8.4.4 流图的表示”在本页不是孤立名词。先在“冻结目标机”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“划分基本块”检查第一条结构差异。围绕图着色记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 8.4.5 循环

“8.4.5 循环”在本页不是孤立名词。先在“划分基本块”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“维护描述符”检查第一条结构差异。围绕树覆盖记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 8.5 基本块的优化

“8.5 基本块的优化”在本页不是孤立名词。先在“维护描述符”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“选择指令”检查第一条结构差异。围绕基本块记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 8.5.1 基本块的DAG表示

“8.5.1 基本块的DAG表示”在本页不是孤立名词。先在“选择指令”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“分配寄存器”检查第一条结构差异。围绕地址描述符记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 8.5.2 寻找局部公共子表达式

“8.5.2 寻找局部公共子表达式”在本页不是孤立名词。先在“分配寄存器”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“比较目标执行”检查第一条结构差异。围绕窥孔优化记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 8.5.3 消除死代码

“8.5.3 消除死代码”在本页不是孤立名词。先在“比较目标执行”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“冻结目标机”检查第一条结构差异。围绕图着色记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 8.5.4 代数恒等式的使用

“8.5.4 代数恒等式的使用”在本页不是孤立名词。先在“冻结目标机”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“划分基本块”检查第一条结构差异。围绕树覆盖记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 8.5.5 数组引用的表示

“8.5.5 数组引用的表示”在本页不是孤立名词。先在“划分基本块”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“维护描述符”检查第一条结构差异。围绕基本块记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 8.5.6 指针赋值和过程调用

“8.5.6 指针赋值和过程调用”在本页不是孤立名词。先在“维护描述符”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“选择指令”检查第一条结构差异。围绕地址描述符记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 8.5.7 从DAG重新组装基本块

“8.5.7 从DAG重新组装基本块”在本页不是孤立名词。先在“选择指令”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“分配寄存器”检查第一条结构差异。围绕窥孔优化记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 8.6 一个简单的代码生成器

“8.6 一个简单的代码生成器”在本页不是孤立名词。先在“分配寄存器”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“比较目标执行”检查第一条结构差异。围绕图着色记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 8.6.1 寄存器描述符和地址描述符

“8.6.1 寄存器描述符和地址描述符”在本页不是孤立名词。先在“比较目标执行”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“冻结目标机”检查第一条结构差异。围绕树覆盖记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 8.6.2 代码生成算法

“8.6.2 代码生成算法”在本页不是孤立名词。先在“冻结目标机”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“划分基本块”检查第一条结构差异。围绕基本块记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 8.6.3 函数getReg的设计

“8.6.3 函数getReg的设计”在本页不是孤立名词。先在“划分基本块”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“维护描述符”检查第一条结构差异。围绕地址描述符记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 8.7 窥孔优化

“8.7 窥孔优化”在本页不是孤立名词。先在“维护描述符”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“选择指令”检查第一条结构差异。围绕窥孔优化记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 8.7.1 消除冗余的加载和保存指令

“8.7.1 消除冗余的加载和保存指令”在本页不是孤立名词。先在“选择指令”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“分配寄存器”检查第一条结构差异。围绕图着色记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 8.7.2 消除不可达代码

“8.7.2 消除不可达代码”在本页不是孤立名词。先在“分配寄存器”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“比较目标执行”检查第一条结构差异。围绕树覆盖记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 8.7.3 控制流优化

“8.7.3 控制流优化”在本页不是孤立名词。先在“比较目标执行”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“冻结目标机”检查第一条结构差异。围绕基本块记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 8.7.4 代数化简和强度削弱

“8.7.4 代数化简和强度削弱”在本页不是孤立名词。先在“冻结目标机”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“划分基本块”检查第一条结构差异。围绕地址描述符记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 8.7.5 机器特有指令的使用

“8.7.5 机器特有指令的使用”在本页不是孤立名词。先在“划分基本块”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“维护描述符”检查第一条结构差异。围绕窥孔优化记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 8.8 寄存器分配和指派

“8.8 寄存器分配和指派”在本页不是孤立名词。先在“维护描述符”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“选择指令”检查第一条结构差异。围绕图着色记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 8.8.1 全局寄存器分配

“8.8.1 全局寄存器分配”在本页不是孤立名词。先在“选择指令”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“分配寄存器”检查第一条结构差异。围绕树覆盖记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 8.8.2 使用计数

“8.8.2 使用计数”在本页不是孤立名词。先在“分配寄存器”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“比较目标执行”检查第一条结构差异。围绕基本块记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 8.8.3 外层循环的寄存器指派

“8.8.3 外层循环的寄存器指派”在本页不是孤立名词。先在“比较目标执行”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“冻结目标机”检查第一条结构差异。围绕地址描述符记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 8.8.4 通过图着色进行寄存器分配

“8.8.4 通过图着色进行寄存器分配”在本页不是孤立名词。先在“冻结目标机”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“划分基本块”检查第一条结构差异。围绕窥孔优化记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 8.9 通过树重写来选择指令

“8.9 通过树重写来选择指令”在本页不是孤立名词。先在“划分基本块”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“维护描述符”检查第一条结构差异。围绕图着色记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 8.9.1 树翻译方案

“8.9.1 树翻译方案”在本页不是孤立名词。先在“维护描述符”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“选择指令”检查第一条结构差异。围绕树覆盖记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 8.9.2 通过输入树的模式匹配生成代码

“8.9.2 通过输入树的模式匹配生成代码”在本页不是孤立名词。先在“选择指令”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“分配寄存器”检查第一条结构差异。围绕基本块记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 8.9.3 通过语法分析进行模式匹配

“8.9.3 通过语法分析进行模式匹配”在本页不是孤立名词。先在“分配寄存器”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“比较目标执行”检查第一条结构差异。围绕地址描述符记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 8.9.4 语义检查例程

“8.9.4 语义检查例程”在本页不是孤立名词。先在“比较目标执行”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“冻结目标机”检查第一条结构差异。围绕窥孔优化记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 8.9.5 通用树匹配

“8.9.5 通用树匹配”在本页不是孤立名词。先在“冻结目标机”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“划分基本块”检查第一条结构差异。围绕图着色记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 8.10 表达式的最优代码生成

“8.10 表达式的最优代码生成”在本页不是孤立名词。先在“划分基本块”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“维护描述符”检查第一条结构差异。围绕树覆盖记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 8.10.1 Ershov数

“8.10.1 Ershov数”在本页不是孤立名词。先在“维护描述符”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“选择指令”检查第一条结构差异。围绕基本块记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 8.10.2 从带标号的表达式树生成代码

“8.10.2 从带标号的表达式树生成代码”在本页不是孤立名词。先在“选择指令”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“分配寄存器”检查第一条结构差异。围绕地址描述符记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 8.10.3 寄存器数量不足时的表达式求值

“8.10.3 寄存器数量不足时的表达式求值”在本页不是孤立名词。先在“分配寄存器”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“比较目标执行”检查第一条结构差异。围绕窥孔优化记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 8.11 动态规划代码生成

“8.11 动态规划代码生成”在本页不是孤立名词。先在“比较目标执行”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“冻结目标机”检查第一条结构差异。围绕图着色记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 8.11.1 连续求值

“8.11.1 连续求值”在本页不是孤立名词。先在“冻结目标机”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“划分基本块”检查第一条结构差异。围绕树覆盖记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 8.11.2 动态规划算法

“8.11.2 动态规划算法”在本页不是孤立名词。先在“划分基本块”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“维护描述符”检查第一条结构差异。围绕基本块记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

## 分步视觉验证

先预测当前阶段的树、图、集合、表项或指令，再运行变换；若不同，停在第一处分叉并保留原始证据。

<Stepper>
  <Step title="画出输入、变换与产物">
    标出基本块、地址描述符、窥孔优化、图着色、树覆盖的输入、输出、所有权、算法前提和下游消费者。
    <Dbc08CodeGenerationMapLab />
  </Step>
  <Step title="运行单变量实验">
    固定源程序与目标模型，只改变一个文法、分析、优化、运行时或机器条件。
    <Dbc08CodeGenerationExperimentLab />
  </Step>
  <Step title="核对第一处分叉">
    比较结构化产物、复杂度和行为，清理旧输出后完整重建并重放回归样本。
    <Dbc08CodeGenerationEvidenceLab />
  </Step>
</Stepper>

## 常见误区

<Callout type="trap">
  **只看最终结果：**
  没有词法单元、语法树、属性依赖、CFG、数据流集合、调度表和寄存器映射，就无法把错误归因到具体阶段，也无法证明中间变换保持语义。
</Callout>

<Callout type="trap">
  **先变换再补条件：**
  优化、调度、并行化和过程间分析必须先证明前提；任何会令支配、活跃、别名、依赖或调用图失效的修改，都要触发明确重算。
</Callout>

## 四类样本与通过条件

| 样本 | 单一变量               | 必查证据         | 通过条件         |
| ---- | ---------------------- | ---------------- | ---------------- |
| 正常 | 一个最小语言或机器功能 | 全阶段快照       | 与预测一致       |
| 边界 | 深度、宽度、延迟或压力 | 临界分支和成本   | 边界可解释       |
| 失败 | 一个文法、事实或约束   | 首错与下游停止   | 不产出伪结果     |
| 优化 | 一个变换或策略         | 前后IR与差分执行 | 等价且收益可解释 |

## 练习

<Exercises>

**问题 1** 为什么不能只凭最终程序运行成功验收第8章 代码生成？

<Answer>
  最终行为可能隐藏错误词法单元、树结构、属性值、数据流事实、依赖边或机器约束。应保存每层结构化产物，定位第一处分叉，并从固定源程序清理重建。
</Answer>

**问题 2** 如何把“维护描述符”变成可回归实验？

<Answer>
  固定输入、实现版本、中间表示和目标模型，先预测基本块与地址描述符的中间状态，只改变一个条件，并保留正常、边界、失败和优化四类样本。
</Answer>

**问题 3** 本页最小通过条件是什么？

<Answer>
  目标指令满足机器约束并保持IR语义，寄存器与地址描述符同步，基本块边界和后继正确，树覆盖成本与所选指令一致。
  修复后必须清理旧产物，完整重建全部下游结果，并让历史样本回到基线。
</Answer>

</Exercises>

## 小结

- 基本块：连接“冻结目标机”的算法前提、结构化产物、成本和回归证据。
- 地址描述符：连接“划分基本块”的算法前提、结构化产物、成本和回归证据。
- 窥孔优化：连接“维护描述符”的算法前提、结构化产物、成本和回归证据。
- 图着色：连接“选择指令”的算法前提、结构化产物、成本和回归证据。
- 树覆盖：连接“分配寄存器”的算法前提、结构化产物、成本和回归证据。
- 本页56个核心目录层级均已进入可追溯的编译证据链。

## 术语表

<Glossary>
  <GlossaryItem term="基本块">
    基本块用于解释“冻结目标机”，需要同时说明输入契约、算法前提、结构化产物、复杂度和清理重建方法。
  </GlossaryItem>
  <GlossaryItem term="地址描述符">
    地址描述符用于解释“划分基本块”，需要同时说明输入契约、算法前提、结构化产物、复杂度和清理重建方法。
  </GlossaryItem>
  <GlossaryItem term="窥孔优化">
    窥孔优化用于解释“维护描述符”，需要同时说明输入契约、算法前提、结构化产物、复杂度和清理重建方法。
  </GlossaryItem>
  <GlossaryItem term="图着色">
    图着色用于解释“选择指令”，需要同时说明输入契约、算法前提、结构化产物、复杂度和清理重建方法。
  </GlossaryItem>
  <GlossaryItem term="树覆盖">
    树覆盖用于解释“分配寄存器”，需要同时说明输入契约、算法前提、结构化产物、复杂度和清理重建方法。
  </GlossaryItem>
</Glossary>

<Attribution
  adaptedFrom="Alfred V. Aho、Monica S. Lam、Ravi Sethi、Jeffrey D. Ullman著，赵建华、郑滔、戴新宇译《编译原理（第2版）》，机械工业出版社，2009，631页，ISBN 9787111251217 · 基于Pearson官方第二版资料、中文版完整目录与高校图书馆元数据的独立教学重构"
  adaptedUrl="https://www.pearson.com/en-us/subject-catalog/p/compilers-principles-techniques-and-tools/P200000003472/9780133002140"
/>
