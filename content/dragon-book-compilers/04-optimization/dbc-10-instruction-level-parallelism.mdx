---
title: "第10章 指令级并行性"
type: C
section: "优化与指令级并行"
order: 10
description: "第10章 指令级并行性逐项覆盖38个核心目录层级，以结构化中间产物、运行轨迹与失败对照建立可复查的编译实验。"
demo: true
math: false
sourceUrl: "https://www.pearson.com/en-us/subject-catalog/p/compilers-principles-techniques-and-tools/P200000003472/9780133002140"
draft: false
---

import {
  Dbc10InstructionLevelParallelismMapLab,
  Dbc10InstructionLevelParallelismExperimentLab,
  Dbc10InstructionLevelParallelismEvidenceLab,
} from "@/components/mdx/dragon-book-compilers/diagrams/dbc-10-instruction-level-parallelism";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第10章 指令级并行性在完整编译流水线中的输入、输出、数据结构和责任边界
- 能描述本页38个核心目录层级之间的依赖、算法前提与工程取舍
- 能复现最小三地址代码或伪代码实验，保存正常、边界、失败和优化四类证据
- 能判断“任何调度都保持真依赖、反依赖、输出依赖和控制约束，资源占用不冲突，推测执行只跨越已证明安全的边界。”是否成立，而不是只看最终程序是否运行

</Objectives>

## 为什么最终输出正确仍然不够

<Term def="指令级并行连接“冻结机器模型”阶段的输入、算法前提、结构化结果、成本和第一处失败证据。">指令级并行</Term>、<Term def="依赖图连接“建立依赖图”阶段的输入、算法前提、结构化结果、成本和第一处失败证据。">依赖图</Term>、<Term def="列表调度连接“计算优先级”阶段的输入、算法前提、结构化结果、成本和第一处失败证据。">列表调度</Term>、<Term def="全局调度连接“安排发射周期”阶段的输入、算法前提、结构化结果、成本和第一处失败证据。">全局调度</Term>、<Term def="软件流水线连接“检查资源冲突”阶段的输入、算法前提、结构化结果、成本和第一处失败证据。">软件流水线</Term>
共同构成本页坐标。把处理器流水线、数据与控制依赖建成调度约束图，比较基本块调度、全局代码移动和软件流水线对延迟、寄存器压力与推测执行的影响。

编译器是一串互相依赖的转换。错误的扫描器可能恰好产生可分析的词法单元，错误的语法树可能被后续语义动作意外修补，错误的数据流事实可能只在冷路径暴露，违反机器约束的调度也可能在当前样本中碰巧执行。因此必须保存阶段输入、输出和不变量，定位第一处偏离，不能把单个样例跑通当作证明。

## 核心模型与实验账本

实验固定源程序、编译器提交、中间表示格式、目标机模型、优化级别和随机种子。前端保存词法单元、推导、语法树、属性值与类型环境；中端保存基本块、控制流图、支配关系、数据流集合和每次变换前后的IR；后端保存模式覆盖、依赖图、发射周期、寄存器映射、溢出代码与最终指令。每份产物都带生成方式、退出状态和摘要值。

样本分成四组。正常样本证明主路径，边界样本覆盖空输入、深嵌套、二义性、不可归约流图、寄存器压力、长延迟、别名和动态调用；失败样本只破坏一个语法、类型、数据流或机器约束；优化样本同时保存变换前后版本，并用解释执行或差分执行核对可观察行为。

诊断记录阶段、源位置或IR位置、实际状态、期望状态、相关前驱和首要根因。某阶段失败后不得继续消费旧缓存伪造下游结果；分析事实被变换失效时，必须显式重算或证明仍然有效。性能结论与正确性结论分开：前者记录静态成本、动态计数、代码大小和运行环境，后者要求语义与全部约束成立。

目录项进入追溯矩阵后，至少关联一个输入、一个算法前提、一份结构化产物、一个失败对照和一个回归断言。跨章接口同时记录生产者和消费者，这样既能发现“提到概念却没有验证”的空洞覆盖，也能在实现变化后只重放真正受影响的实验。

## 六阶段证据链

### 1. 冻结机器模型

围绕指令级并行先预测结构化结果，只改变一个条件并运行；进入“建立依赖图”前，核对语义、算法前提、成本和首错位置。

### 2. 建立依赖图

围绕依赖图先预测结构化结果，只改变一个条件并运行；进入“计算优先级”前，核对语义、算法前提、成本和首错位置。

### 3. 计算优先级

围绕列表调度先预测结构化结果，只改变一个条件并运行；进入“安排发射周期”前，核对语义、算法前提、成本和首错位置。

### 4. 安排发射周期

围绕全局调度先预测结构化结果，只改变一个条件并运行；进入“检查资源冲突”前，核对语义、算法前提、成本和首错位置。

### 5. 检查资源冲突

围绕软件流水线先预测结构化结果，只改变一个条件并运行；进入“测量吞吐与压力”前，核对语义、算法前提、成本和首错位置。

### 6. 测量吞吐与压力

围绕指令级并行先预测结构化结果，只改变一个条件并运行；进入“冻结机器模型”前，核对语义、算法前提、成本和首错位置。

## 可运行的最小骨架

```text
latency(u, v) <= schedule(v) - schedule(u)
```

```text
ready = priorityTopologicalOrder(DAG)
issue while resourcesAvailable
```

```text
prologue
steady-state: iteration i, i+1, i+2 overlap
epilogue
```

<Callout type="trap">
  **版本边界：**
  本页复原的是龙书第二版的算法模型和目录边界。现代LLVM、GCC、JVM或具体ISA可以用来做对照，但不能把其默认实现反写成原书算法的保证。
</Callout>

## 权威目录逐项讲解

### 第10章 指令级并行性

“第10章 指令级并行性”在本页不是孤立名词。先在“冻结机器模型”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“建立依赖图”检查第一条结构差异。围绕指令级并行记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 10.1 处理器体系结构

“10.1 处理器体系结构”在本页不是孤立名词。先在“建立依赖图”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“计算优先级”检查第一条结构差异。围绕依赖图记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 10.1.1 指令流水线和分支延迟

“10.1.1 指令流水线和分支延迟”在本页不是孤立名词。先在“计算优先级”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“安排发射周期”检查第一条结构差异。围绕列表调度记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 10.1.2 流水线化执行

“10.1.2 流水线化执行”在本页不是孤立名词。先在“安排发射周期”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“检查资源冲突”检查第一条结构差异。围绕全局调度记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 10.1.3 多指令发射

“10.1.3 多指令发射”在本页不是孤立名词。先在“检查资源冲突”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“测量吞吐与压力”检查第一条结构差异。围绕软件流水线记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 10.2 代码调度约束

“10.2 代码调度约束”在本页不是孤立名词。先在“测量吞吐与压力”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“冻结机器模型”检查第一条结构差异。围绕指令级并行记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 10.2.1 数据依赖

“10.2.1 数据依赖”在本页不是孤立名词。先在“冻结机器模型”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“建立依赖图”检查第一条结构差异。围绕依赖图记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 10.2.2 寻找内存访问之间的依赖

“10.2.2 寻找内存访问之间的依赖”在本页不是孤立名词。先在“建立依赖图”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“计算优先级”检查第一条结构差异。围绕列表调度记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 10.2.3 寄存器使用和并行性之间的权衡

“10.2.3 寄存器使用和并行性之间的权衡”在本页不是孤立名词。先在“计算优先级”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“安排发射周期”检查第一条结构差异。围绕全局调度记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 10.2.4 寄存器分配和代码调度的阶段顺序

“10.2.4 寄存器分配和代码调度的阶段顺序”在本页不是孤立名词。先在“安排发射周期”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“检查资源冲突”检查第一条结构差异。围绕软件流水线记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 10.2.5 控制依赖

“10.2.5 控制依赖”在本页不是孤立名词。先在“检查资源冲突”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“测量吞吐与压力”检查第一条结构差异。围绕指令级并行记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 10.2.6 对推测执行的支持

“10.2.6 对推测执行的支持”在本页不是孤立名词。先在“测量吞吐与压力”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“冻结机器模型”检查第一条结构差异。围绕依赖图记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 10.2.7 一个基本机器模型

“10.2.7 一个基本机器模型”在本页不是孤立名词。先在“冻结机器模型”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“建立依赖图”检查第一条结构差异。围绕列表调度记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 10.3 基本块调度

“10.3 基本块调度”在本页不是孤立名词。先在“建立依赖图”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“计算优先级”检查第一条结构差异。围绕全局调度记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 10.3.1 数据依赖图

“10.3.1 数据依赖图”在本页不是孤立名词。先在“计算优先级”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“安排发射周期”检查第一条结构差异。围绕软件流水线记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 10.3.2 基本块的列表调度

“10.3.2 基本块的列表调度”在本页不是孤立名词。先在“安排发射周期”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“检查资源冲突”检查第一条结构差异。围绕指令级并行记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 10.3.3 带优先级的拓扑排序

“10.3.3 带优先级的拓扑排序”在本页不是孤立名词。先在“检查资源冲突”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“测量吞吐与压力”检查第一条结构差异。围绕依赖图记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 10.4 全局代码调度

“10.4 全局代码调度”在本页不是孤立名词。先在“测量吞吐与压力”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“冻结机器模型”检查第一条结构差异。围绕列表调度记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 10.4.1 基本代码移动

“10.4.1 基本代码移动”在本页不是孤立名词。先在“冻结机器模型”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“建立依赖图”检查第一条结构差异。围绕全局调度记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 10.4.2 向上代码移动

“10.4.2 向上代码移动”在本页不是孤立名词。先在“建立依赖图”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“计算优先级”检查第一条结构差异。围绕软件流水线记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 10.4.3 向下代码移动

“10.4.3 向下代码移动”在本页不是孤立名词。先在“计算优先级”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“安排发射周期”检查第一条结构差异。围绕指令级并行记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 10.4.4 更新数据依赖

“10.4.4 更新数据依赖”在本页不是孤立名词。先在“安排发射周期”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“检查资源冲突”检查第一条结构差异。围绕依赖图记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 10.4.5 全局调度算法

“10.4.5 全局调度算法”在本页不是孤立名词。先在“检查资源冲突”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“测量吞吐与压力”检查第一条结构差异。围绕列表调度记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 10.4.6 高级代码移动技术

“10.4.6 高级代码移动技术”在本页不是孤立名词。先在“测量吞吐与压力”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“冻结机器模型”检查第一条结构差异。围绕全局调度记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 10.4.7 和动态调度器的交互

“10.4.7 和动态调度器的交互”在本页不是孤立名词。先在“冻结机器模型”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“建立依赖图”检查第一条结构差异。围绕软件流水线记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 10.5 软件流水线

“10.5 软件流水线”在本页不是孤立名词。先在“建立依赖图”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“计算优先级”检查第一条结构差异。围绕指令级并行记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 10.5.1 引言

“10.5.1 引言”在本页不是孤立名词。先在“计算优先级”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“安排发射周期”检查第一条结构差异。围绕依赖图记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 10.5.2 循环的软件流水线

“10.5.2 循环的软件流水线”在本页不是孤立名词。先在“安排发射周期”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“检查资源冲突”检查第一条结构差异。围绕列表调度记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 10.5.3 寄存器分配和代码生成

“10.5.3 寄存器分配和代码生成”在本页不是孤立名词。先在“检查资源冲突”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“测量吞吐与压力”检查第一条结构差异。围绕全局调度记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 10.5.4 Do-Across循环

“10.5.4 Do-Across循环”在本页不是孤立名词。先在“测量吞吐与压力”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“冻结机器模型”检查第一条结构差异。围绕软件流水线记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 10.5.5 软件流水线的目标和约束

“10.5.5 软件流水线的目标和约束”在本页不是孤立名词。先在“冻结机器模型”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“建立依赖图”检查第一条结构差异。围绕指令级并行记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 10.5.6 一个软件流水线算法

“10.5.6 一个软件流水线算法”在本页不是孤立名词。先在“建立依赖图”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“计算优先级”检查第一条结构差异。围绕依赖图记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 10.5.7 无环数据依赖图的调度

“10.5.7 无环数据依赖图的调度”在本页不是孤立名词。先在“计算优先级”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“安排发射周期”检查第一条结构差异。围绕列表调度记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 10.5.8 有环依赖图的调度

“10.5.8 有环依赖图的调度”在本页不是孤立名词。先在“安排发射周期”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“检查资源冲突”检查第一条结构差异。围绕全局调度记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 10.5.9 流水线算法的改进

“10.5.9 流水线算法的改进”在本页不是孤立名词。先在“检查资源冲突”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“测量吞吐与压力”检查第一条结构差异。围绕软件流水线记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 10.5.10 模变量扩展

“10.5.10 模变量扩展”在本页不是孤立名词。先在“测量吞吐与压力”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“冻结机器模型”检查第一条结构差异。围绕指令级并行记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 10.5.11 条件语句

“10.5.11 条件语句”在本页不是孤立名词。先在“冻结机器模型”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“建立依赖图”检查第一条结构差异。围绕依赖图记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 10.5.12 对软件流水线的硬件支持

“10.5.12 对软件流水线的硬件支持”在本页不是孤立名词。先在“建立依赖图”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“计算优先级”检查第一条结构差异。围绕列表调度记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

## 分步视觉验证

先预测当前阶段的树、图、集合、表项或指令，再运行变换；若不同，停在第一处分叉并保留原始证据。

<Stepper>
  <Step title="画出输入、变换与产物">
    标出指令级并行、依赖图、列表调度、全局调度、软件流水线的输入、输出、所有权、算法前提和下游消费者。
    <Dbc10InstructionLevelParallelismMapLab />
  </Step>
  <Step title="运行单变量实验">
    固定源程序与目标模型，只改变一个文法、分析、优化、运行时或机器条件。
    <Dbc10InstructionLevelParallelismExperimentLab />
  </Step>
  <Step title="核对第一处分叉">
    比较结构化产物、复杂度和行为，清理旧输出后完整重建并重放回归样本。
    <Dbc10InstructionLevelParallelismEvidenceLab />
  </Step>
</Stepper>

## 常见误区

<Callout type="trap">
  **只看最终结果：**
  没有词法单元、语法树、属性依赖、CFG、数据流集合、调度表和寄存器映射，就无法把错误归因到具体阶段，也无法证明中间变换保持语义。
</Callout>

<Callout type="trap">
  **先变换再补条件：**
  优化、调度、并行化和过程间分析必须先证明前提；任何会令支配、活跃、别名、依赖或调用图失效的修改，都要触发明确重算。
</Callout>

## 四类样本与通过条件

| 样本 | 单一变量               | 必查证据         | 通过条件         |
| ---- | ---------------------- | ---------------- | ---------------- |
| 正常 | 一个最小语言或机器功能 | 全阶段快照       | 与预测一致       |
| 边界 | 深度、宽度、延迟或压力 | 临界分支和成本   | 边界可解释       |
| 失败 | 一个文法、事实或约束   | 首错与下游停止   | 不产出伪结果     |
| 优化 | 一个变换或策略         | 前后IR与差分执行 | 等价且收益可解释 |

## 练习

<Exercises>

**问题 1** 为什么不能只凭最终程序运行成功验收第10章 指令级并行性？

<Answer>
  最终行为可能隐藏错误词法单元、树结构、属性值、数据流事实、依赖边或机器约束。应保存每层结构化产物，定位第一处分叉，并从固定源程序清理重建。
</Answer>

**问题 2** 如何把“计算优先级”变成可回归实验？

<Answer>
  固定输入、实现版本、中间表示和目标模型，先预测指令级并行与依赖图的中间状态，只改变一个条件，并保留正常、边界、失败和优化四类样本。
</Answer>

**问题 3** 本页最小通过条件是什么？

<Answer>
  任何调度都保持真依赖、反依赖、输出依赖和控制约束，资源占用不冲突，推测执行只跨越已证明安全的边界。
  修复后必须清理旧产物，完整重建全部下游结果，并让历史样本回到基线。
</Answer>

</Exercises>

## 小结

- 指令级并行：连接“冻结机器模型”的算法前提、结构化产物、成本和回归证据。
- 依赖图：连接“建立依赖图”的算法前提、结构化产物、成本和回归证据。
- 列表调度：连接“计算优先级”的算法前提、结构化产物、成本和回归证据。
- 全局调度：连接“安排发射周期”的算法前提、结构化产物、成本和回归证据。
- 软件流水线：连接“检查资源冲突”的算法前提、结构化产物、成本和回归证据。
- 本页38个核心目录层级均已进入可追溯的编译证据链。

## 术语表

<Glossary>
  <GlossaryItem term="指令级并行">
    指令级并行用于解释“冻结机器模型”，需要同时说明输入契约、算法前提、结构化产物、复杂度和清理重建方法。
  </GlossaryItem>
  <GlossaryItem term="依赖图">
    依赖图用于解释“建立依赖图”，需要同时说明输入契约、算法前提、结构化产物、复杂度和清理重建方法。
  </GlossaryItem>
  <GlossaryItem term="列表调度">
    列表调度用于解释“计算优先级”，需要同时说明输入契约、算法前提、结构化产物、复杂度和清理重建方法。
  </GlossaryItem>
  <GlossaryItem term="全局调度">
    全局调度用于解释“安排发射周期”，需要同时说明输入契约、算法前提、结构化产物、复杂度和清理重建方法。
  </GlossaryItem>
  <GlossaryItem term="软件流水线">
    软件流水线用于解释“检查资源冲突”，需要同时说明输入契约、算法前提、结构化产物、复杂度和清理重建方法。
  </GlossaryItem>
</Glossary>

<Attribution
  adaptedFrom="Alfred V. Aho、Monica S. Lam、Ravi Sethi、Jeffrey D. Ullman著，赵建华、郑滔、戴新宇译《编译原理（第2版）》，机械工业出版社，2009，631页，ISBN 9787111251217 · 基于Pearson官方第二版资料、中文版完整目录与高校图书馆元数据的独立教学重构"
  adaptedUrl="https://www.pearson.com/en-us/subject-catalog/p/compilers-principles-techniques-and-tools/P200000003472/9780133002140"
/>
