---
title: 第10章 指令级并行性
type: C
section: 第10章 指令级并行性
order: 10
description: >-
  把处理器资源、数据/内存/控制依赖、列表调度、全局调度与软件流水线连接到时序约束；用编译流水线、状态轨迹和等价性验证门交付依赖图、周期表、资源占用、寄存器压力与异常反例
demo: true
math: true
sourceUrl: >-
  https://www.pearson.com/en-us/subject-catalog/p/compilers-principles-techniques-and-tools/P200000003472/9780133002140
draft: false
qualityVersion: 2
practiceMode: simulation
sourceMode: independent-rewrite
officialUnitId: dbc-unit-10
---

import {
  Objectives,
  Term,
  Callout,
  Stepper,
  Step,
  Exercises,
  Answer,
  Glossary,
  GlossaryItem,
  Attribution,
} from "@/components/mdx/mdx-components";
import {
  Dbc10InstructionLevelParallelismPipelineContractLab,
  Dbc10InstructionLevelParallelismStateTraceLab,
  Dbc10InstructionLevelParallelismVerificationGateLab,
} from "@/components/mdx/dragon-book-compilers/v2/dbc-10-instruction-level-parallelism";

<Objectives>

- 能说明“第10章 指令级并行性”如何把处理器资源、数据/内存/控制依赖、列表调度、全局调度与软件流水线连接到时序约束，并区分Pearson英文第二版、中文译本、现代工具和本站重写
- 能先预测“一条指令为何能移动到该周期而不破坏依赖、异常和寄存器限制？”会改变哪一个输入、表示、栈/图状态、IR、目标代码或验证结果，再操作三类交互证据
- 能只注入“推测移动可能抛异常的指令越过守卫分支”，定位首个偏离“资源、延迟、数据/内存/控制依赖、寄存器压力和异常语义均满足”的状态，并从同一快照完成恢复

</Objectives>

## 为什么从这个问题开始

“第10章 指令级并行性”围绕“一条指令为何能移动到该周期而不破坏依赖、异常和寄存器限制？”建立贯穿任务：在固定机器模型上调度基本块和循环迭代。先写下哪个输入、表示、栈/图状态、IR、目标代码或验证结果会最先变化，再运行参考、故障和恢复路径；运行后补理由不算预测。只有守住“资源、延迟、数据/内存/控制依赖、寄存器压力和异常语义均满足”并交付依赖图、周期表、资源占用、寄存器压力与异常反例，编译成功、分析收敛、目标码长度或基准加速才构成机制证据。

## 原版书目、556个正式坐标与访问边界

“第10章 指令级并行性”以[Pearson官方书页](https://www.pearson.com/en-us/subject-catalog/p/compilers-principles-techniques-and-tools/P200000003472/9780133002140)核对Alfred V. Aho、Monica S. Lam、Ravi Sethi、Jeffrey D. Ullman著 _Compilers: Principles, Techniques, and Tools, Second Edition_：英文精装ISBN 9780321486813，2006年版；Pearson明确列出12章与两个附录，并说明第10章“指令级并行”、第11章“并行与局部性优化”、第12章“过程间分析”是第二版新增重点。[Pearson官方目录](https://www.pearson.de/media/muster/toc/toc_9781292037233.pdf)继续核对版本与章/附录框架。

“第10章 指令级并行性”再以[中文版完整目录](https://www.tenlong.com.tw/products/9787111251217?list_name=rd)与[高校馆藏书目](https://opac.uibe.edu.cn/opac/book/072a939488f31ce107149cbd219f9386)核对赵建华、郑滔、戴新宇译《编译原理（第2版）》，机械工业出版社，2009年，631页，ISBN 9787111251217。正式分母计入12个章标题、2个附录标题、533个数字编号节/小节和附录A的9个编号节，合计556个核心目录层级；章末总结、练习、参考文献和索引不重复计为知识节点。

原书与译本均受版权保护，“第10章 指令级并行性”不复制、翻译或改写原文、图表、算法伪码和练习，只把官方目录当作范围坐标；中文讲解、状态轨迹、反例、交互、练习与答案均为独立教学重写。[本页独立核对 1](https://llvm.org/docs/CodeGenerator.html)只用于核对现代IR、工具或实验边界，不反向证明原书采用本站表述。

## 原版目录层级与可验证机制

### 第10章 指令级并行性

<Term def="指令级并行性对应正式目录坐标“第10章 指令级并行性”，在“第10章 指令级并行性”中用于在IR、寄存器、内存和目标指令间满足定义—使用、调用约定和代价，并受源程序、表示、规则、目标机、代价与验证边界约束。">
  指令级并行性
</Term>

**正式坐标 1/38。** 原版目录键 `第10章 指令级并行性`。在“第10章 指令级并行性”的第1个正式坐标中，「第10章 指令级并行性」通过在IR、寄存器、内存和目标指令间满足定义—使用、调用约定和代价推进指令调度、依赖与资源；复核者保存基本块DAG、活跃区间、干涉图、指令匹配和差分执行，出现别名、寄存器类、调用约定或目标副作用被忽略就撤回结论。

### 10·1 处理器体系结构

<Term def="处理器体系结构对应正式目录坐标“10.1 处理器体系结构”，在“第10章 指令级并行性”中用于把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表，并受源程序、表示、规则、目标机、代价与验证边界约束。">
  处理器体系结构
</Term>

**正式坐标 2/38。** 原版目录键 `10.1 处理器体系结构`。围绕“一条指令为何能移动到该周期而不破坏依赖、异常和寄存器限制？”，“第10章 指令级并行性”在坐标2把「10·1 处理器体系结构」落实为把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表；只有依赖图、周期表、资源占用、寄存器压力与异常反例可重放且反例排除非法移动跨越依赖、守卫、异常或资源限制，本节点才算掌握。

### 10·1·1 指令流水线和分支延迟

<Term def="指令流水线和分支延迟对应正式目录坐标“10.1.1 指令流水线和分支延迟”，在“第10章 指令级并行性”中用于在IR、寄存器、内存和目标指令间满足定义—使用、调用约定和代价，并受源程序、表示、规则、目标机、代价与验证边界约束。">
  指令流水线和分支延迟
</Term>

**正式坐标 3/38。** 原版目录键 `10.1.1 指令流水线和分支延迟`。“第10章 指令级并行性”的目录节点3「10·1·1 指令流水线和分支延迟」不能停在术语或伪码：它要在IR、寄存器、内存和目标指令间满足定义—使用、调用约定和代价，交付基本块DAG、活跃区间、干涉图、指令匹配和差分执行，并把别名、寄存器类、调用约定或目标副作用被忽略设为单一反事实。

### 10·1·2 流水线化执行

<Term def="流水线化执行对应正式目录坐标“10.1.2 流水线化执行”，在“第10章 指令级并行性”中用于把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表，并受源程序、表示、规则、目标机、代价与验证边界约束。">
  流水线化执行
</Term>

**正式坐标 4/38。** 原版目录键 `10.1.2 流水线化执行`。对“第10章 指令级并行性”而言，「10·1·2 流水线化执行」在第4次检查中改变可观察状态，因为它负责把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表；依赖图、周期表、资源占用、寄存器压力与异常反例必须与“资源、延迟、数据/内存/控制依赖、寄存器压力和异常语义均满足”对齐，不能接受非法移动跨越依赖、守卫、异常或资源限制。

### 10·1·3 多指令发射

<Term def="多指令发射对应正式目录坐标“10.1.3 多指令发射”，在“第10章 指令级并行性”中用于在IR、寄存器、内存和目标指令间满足定义—使用、调用约定和代价，并受源程序、表示、规则、目标机、代价与验证边界约束。">
  多指令发射
</Term>

**正式坐标 5/38。** 原版目录键 `10.1.3 多指令发射`。在“第10章 指令级并行性”的第5个正式坐标中，「10·1·3 多指令发射」通过在IR、寄存器、内存和目标指令间满足定义—使用、调用约定和代价推进指令调度、依赖与资源；复核者保存基本块DAG、活跃区间、干涉图、指令匹配和差分执行，出现别名、寄存器类、调用约定或目标副作用被忽略就撤回结论。

### 10·2 代码调度约束

<Term def="代码调度约束对应正式目录坐标“10.2 代码调度约束”，在“第10章 指令级并行性”中用于把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表，并受源程序、表示、规则、目标机、代价与验证边界约束。">
  代码调度约束
</Term>

**正式坐标 6/38。** 原版目录键 `10.2 代码调度约束`。围绕“一条指令为何能移动到该周期而不破坏依赖、异常和寄存器限制？”，“第10章 指令级并行性”在坐标6把「10·2 代码调度约束」落实为把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表；只有依赖图、周期表、资源占用、寄存器压力与异常反例可重放且反例排除非法移动跨越依赖、守卫、异常或资源限制，本节点才算掌握。

### 10·2·1 数据依赖

<Term def="数据依赖对应正式目录坐标“10.2.1 数据依赖”，在“第10章 指令级并行性”中用于把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表，并受源程序、表示、规则、目标机、代价与验证边界约束。">
  数据依赖
</Term>

**正式坐标 7/38。** 原版目录键 `10.2.1 数据依赖`。“第10章 指令级并行性”的目录节点7「10·2·1 数据依赖」不能停在术语或伪码：它要把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表，交付依赖图、周期表、资源占用、寄存器压力与异常反例，并把非法移动跨越依赖、守卫、异常或资源限制设为单一反事实。

### 10·2·2 寻找内存访问之间的依赖

<Term def="寻找内存访问之间的依赖对应正式目录坐标“10.2.2 寻找内存访问之间的依赖”，在“第10章 指令级并行性”中用于把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表，并受源程序、表示、规则、目标机、代价与验证边界约束。">
  寻找内存访问之间的依赖
</Term>

**正式坐标 8/38。** 原版目录键 `10.2.2 寻找内存访问之间的依赖`。对“第10章 指令级并行性”而言，「10·2·2 寻找内存访问之间的依赖」在第8次检查中改变可观察状态，因为它负责把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表；依赖图、周期表、资源占用、寄存器压力与异常反例必须与“资源、延迟、数据/内存/控制依赖、寄存器压力和异常语义均满足”对齐，不能接受非法移动跨越依赖、守卫、异常或资源限制。

### 10·2·3 寄存器使用和并行性之间的权衡

<Term def="寄存器使用和并行性之间的权衡对应正式目录坐标“10.2.3 寄存器使用和并行性之间的权衡”，在“第10章 指令级并行性”中用于在IR、寄存器、内存和目标指令间满足定义—使用、调用约定和代价，并受源程序、表示、规则、目标机、代价与验证边界约束。">
  寄存器使用和并行性之间的权衡
</Term>

**正式坐标 9/38。** 原版目录键 `10.2.3 寄存器使用和并行性之间的权衡`。在“第10章 指令级并行性”的第9个正式坐标中，「10·2·3 寄存器使用和并行性之间的权衡」通过在IR、寄存器、内存和目标指令间满足定义—使用、调用约定和代价推进指令调度、依赖与资源；复核者保存基本块DAG、活跃区间、干涉图、指令匹配和差分执行，出现别名、寄存器类、调用约定或目标副作用被忽略就撤回结论。

### 10·2·4 寄存器分配和代码调度的阶段顺序

<Term def="寄存器分配和代码调度的阶段顺序对应正式目录坐标“10.2.4 寄存器分配和代码调度的阶段顺序”，在“第10章 指令级并行性”中用于在IR、寄存器、内存和目标指令间满足定义—使用、调用约定和代价，并受源程序、表示、规则、目标机、代价与验证边界约束。">
  寄存器分配和代码调度的阶段顺序
</Term>

**正式坐标 10/38。** 原版目录键 `10.2.4 寄存器分配和代码调度的阶段顺序`。围绕“一条指令为何能移动到该周期而不破坏依赖、异常和寄存器限制？”，“第10章 指令级并行性”在坐标10把「10·2·4 寄存器分配和代码调度的阶段顺序」落实为在IR、寄存器、内存和目标指令间满足定义—使用、调用约定和代价；只有基本块DAG、活跃区间、干涉图、指令匹配和差分执行可重放且反例排除别名、寄存器类、调用约定或目标副作用被忽略，本节点才算掌握。

### 10·2·5 控制依赖

<Term def="控制依赖对应正式目录坐标“10.2.5 控制依赖”，在“第10章 指令级并行性”中用于把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表，并受源程序、表示、规则、目标机、代价与验证边界约束。">
  控制依赖
</Term>

**正式坐标 11/38。** 原版目录键 `10.2.5 控制依赖`。“第10章 指令级并行性”的目录节点11「10·2·5 控制依赖」不能停在术语或伪码：它要把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表，交付依赖图、周期表、资源占用、寄存器压力与异常反例，并把非法移动跨越依赖、守卫、异常或资源限制设为单一反事实。

### 10·2·6 对推测执行的支持

<Term def="对推测执行的支持对应正式目录坐标“10.2.6 对推测执行的支持”，在“第10章 指令级并行性”中用于把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表，并受源程序、表示、规则、目标机、代价与验证边界约束。">
  对推测执行的支持
</Term>

**正式坐标 12/38。** 原版目录键 `10.2.6 对推测执行的支持`。对“第10章 指令级并行性”而言，「10·2·6 对推测执行的支持」在第12次检查中改变可观察状态，因为它负责把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表；依赖图、周期表、资源占用、寄存器压力与异常反例必须与“资源、延迟、数据/内存/控制依赖、寄存器压力和异常语义均满足”对齐，不能接受非法移动跨越依赖、守卫、异常或资源限制。

### 10·2·7 一个基本机器模型

<Term def="一个基本机器模型对应正式目录坐标“10.2.7 一个基本机器模型”，在“第10章 指令级并行性”中用于把“一个基本机器模型”放进指令调度、依赖与资源的输入—状态—变换—验证链，并受源程序、表示、规则、目标机、代价与验证边界约束。">
  一个基本机器模型
</Term>

**正式坐标 13/38。** 原版目录键 `10.2.7 一个基本机器模型`。在“第10章 指令级并行性”的第13个正式坐标中，「10·2·7 一个基本机器模型」通过把“一个基本机器模型”放进指令调度、依赖与资源的输入—状态—变换—验证链推进指令调度、依赖与资源；复核者保存第10章 指令级并行性的输入角色、中间状态、输出、反例与等价性证据，出现只复述“一个基本机器模型”名称而没有可观察状态、单故障和恢复验证就撤回结论。

### 10·3 基本块调度

<Term def="基本块调度对应正式目录坐标“10.3 基本块调度”，在“第10章 指令级并行性”中用于在IR、寄存器、内存和目标指令间满足定义—使用、调用约定和代价，并受源程序、表示、规则、目标机、代价与验证边界约束。">
  基本块调度
</Term>

**正式坐标 14/38。** 原版目录键 `10.3 基本块调度`。围绕“一条指令为何能移动到该周期而不破坏依赖、异常和寄存器限制？”，“第10章 指令级并行性”在坐标14把「10·3 基本块调度」落实为在IR、寄存器、内存和目标指令间满足定义—使用、调用约定和代价；只有基本块DAG、活跃区间、干涉图、指令匹配和差分执行可重放且反例排除别名、寄存器类、调用约定或目标副作用被忽略，本节点才算掌握。

### 10·3·1 数据依赖图

<Term def="数据依赖图对应正式目录坐标“10.3.1 数据依赖图”，在“第10章 指令级并行性”中用于沿依赖图安排属性求值、语义动作和栈位置，并受源程序、表示、规则、目标机、代价与验证边界约束。">
  数据依赖图
</Term>

**正式坐标 15/38。** 原版目录键 `10.3.1 数据依赖图`。“第10章 指令级并行性”的目录节点15「10·3·1 数据依赖图」不能停在术语或伪码：它要沿依赖图安排属性求值、语义动作和栈位置，交付属性依赖图、拓扑序、栈快照、值与副作用日志，并把属性未就绪、循环依赖或副作用错序设为单一反事实。

### 10·3·2 基本块的列表调度

<Term def="基本块的列表调度对应正式目录坐标“10.3.2 基本块的列表调度”，在“第10章 指令级并行性”中用于在IR、寄存器、内存和目标指令间满足定义—使用、调用约定和代价，并受源程序、表示、规则、目标机、代价与验证边界约束。">
  基本块的列表调度
</Term>

**正式坐标 16/38。** 原版目录键 `10.3.2 基本块的列表调度`。对“第10章 指令级并行性”而言，「10·3·2 基本块的列表调度」在第16次检查中改变可观察状态，因为它负责在IR、寄存器、内存和目标指令间满足定义—使用、调用约定和代价；基本块DAG、活跃区间、干涉图、指令匹配和差分执行必须与“资源、延迟、数据/内存/控制依赖、寄存器压力和异常语义均满足”对齐，不能接受别名、寄存器类、调用约定或目标副作用被忽略。

### 10·3·3 带优先级的拓扑排序

<Term def="带优先级的拓扑排序对应正式目录坐标“10.3.3 带优先级的拓扑排序”，在“第10章 指令级并行性”中用于让产生式、推导、分析选择、语义动作和树/IR状态逐步对应，并受源程序、表示、规则、目标机、代价与验证边界约束。">
  带优先级的拓扑排序
</Term>

**正式坐标 17/38。** 原版目录键 `10.3.3 带优先级的拓扑排序`。在“第10章 指令级并行性”的第17个正式坐标中，「10·3·3 带优先级的拓扑排序」通过让产生式、推导、分析选择、语义动作和树/IR状态逐步对应推进指令调度、依赖与资源；复核者保存文法版本、分析栈、输入指针、树、属性和动作日志，出现分析选择或语义动作时点与文法依赖不一致就撤回结论。

### 10·4 全局代码调度

<Term def="全局代码调度对应正式目录坐标“10.4 全局代码调度”，在“第10章 指令级并行性”中用于把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表，并受源程序、表示、规则、目标机、代价与验证边界约束。">
  全局代码调度
</Term>

**正式坐标 18/38。** 原版目录键 `10.4 全局代码调度`。围绕“一条指令为何能移动到该周期而不破坏依赖、异常和寄存器限制？”，“第10章 指令级并行性”在坐标18把「10·4 全局代码调度」落实为把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表；只有依赖图、周期表、资源占用、寄存器压力与异常反例可重放且反例排除非法移动跨越依赖、守卫、异常或资源限制，本节点才算掌握。

### 10·4·1 基本代码移动

<Term def="基本代码移动对应正式目录坐标“10.4.1 基本代码移动”，在“第10章 指令级并行性”中用于把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表，并受源程序、表示、规则、目标机、代价与验证边界约束。">
  基本代码移动
</Term>

**正式坐标 19/38。** 原版目录键 `10.4.1 基本代码移动`。“第10章 指令级并行性”的目录节点19「10·4·1 基本代码移动」不能停在术语或伪码：它要把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表，交付依赖图、周期表、资源占用、寄存器压力与异常反例，并把非法移动跨越依赖、守卫、异常或资源限制设为单一反事实。

### 10·4·2 向上代码移动

<Term def="向上代码移动对应正式目录坐标“10.4.2 向上代码移动”，在“第10章 指令级并行性”中用于把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表，并受源程序、表示、规则、目标机、代价与验证边界约束。">
  向上代码移动
</Term>

**正式坐标 20/38。** 原版目录键 `10.4.2 向上代码移动`。对“第10章 指令级并行性”而言，「10·4·2 向上代码移动」在第20次检查中改变可观察状态，因为它负责把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表；依赖图、周期表、资源占用、寄存器压力与异常反例必须与“资源、延迟、数据/内存/控制依赖、寄存器压力和异常语义均满足”对齐，不能接受非法移动跨越依赖、守卫、异常或资源限制。

### 10·4·3 向下代码移动

<Term def="向下代码移动对应正式目录坐标“10.4.3 向下代码移动”，在“第10章 指令级并行性”中用于把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表，并受源程序、表示、规则、目标机、代价与验证边界约束。">
  向下代码移动
</Term>

**正式坐标 21/38。** 原版目录键 `10.4.3 向下代码移动`。在“第10章 指令级并行性”的第21个正式坐标中，「10·4·3 向下代码移动」通过把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表推进指令调度、依赖与资源；复核者保存依赖图、周期表、资源占用、寄存器压力与异常反例，出现非法移动跨越依赖、守卫、异常或资源限制就撤回结论。

### 10·4·4 更新数据依赖

<Term def="更新数据依赖对应正式目录坐标“10.4.4 更新数据依赖”，在“第10章 指令级并行性”中用于把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表，并受源程序、表示、规则、目标机、代价与验证边界约束。">
  更新数据依赖
</Term>

**正式坐标 22/38。** 原版目录键 `10.4.4 更新数据依赖`。围绕“一条指令为何能移动到该周期而不破坏依赖、异常和寄存器限制？”，“第10章 指令级并行性”在坐标22把「10·4·4 更新数据依赖」落实为把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表；只有依赖图、周期表、资源占用、寄存器压力与异常反例可重放且反例排除非法移动跨越依赖、守卫、异常或资源限制，本节点才算掌握。

### 10·4·5 全局调度算法

<Term def="全局调度算法对应正式目录坐标“10.4.5 全局调度算法”，在“第10章 指令级并行性”中用于把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表，并受源程序、表示、规则、目标机、代价与验证边界约束。">
  全局调度算法
</Term>

**正式坐标 23/38。** 原版目录键 `10.4.5 全局调度算法`。“第10章 指令级并行性”的目录节点23「10·4·5 全局调度算法」不能停在术语或伪码：它要把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表，交付依赖图、周期表、资源占用、寄存器压力与异常反例，并把非法移动跨越依赖、守卫、异常或资源限制设为单一反事实。

### 10·4·6 高级代码移动技术

<Term def="高级代码移动技术对应正式目录坐标“10.4.6 高级代码移动技术”，在“第10章 指令级并行性”中用于把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表，并受源程序、表示、规则、目标机、代价与验证边界约束。">
  高级代码移动技术
</Term>

**正式坐标 24/38。** 原版目录键 `10.4.6 高级代码移动技术`。对“第10章 指令级并行性”而言，「10·4·6 高级代码移动技术」在第24次检查中改变可观察状态，因为它负责把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表；依赖图、周期表、资源占用、寄存器压力与异常反例必须与“资源、延迟、数据/内存/控制依赖、寄存器压力和异常语义均满足”对齐，不能接受非法移动跨越依赖、守卫、异常或资源限制。

### 10·4·7 和动态调度器的交互

<Term def="和动态调度器的交互对应正式目录坐标“10.4.7 和动态调度器的交互”，在“第10章 指令级并行性”中用于把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表，并受源程序、表示、规则、目标机、代价与验证边界约束。">
  和动态调度器的交互
</Term>

**正式坐标 25/38。** 原版目录键 `10.4.7 和动态调度器的交互`。在“第10章 指令级并行性”的第25个正式坐标中，「10·4·7 和动态调度器的交互」通过把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表推进指令调度、依赖与资源；复核者保存依赖图、周期表、资源占用、寄存器压力与异常反例，出现非法移动跨越依赖、守卫、异常或资源限制就撤回结论。

### 10·5 软件流水线

<Term def="软件流水线对应正式目录坐标“10.5 软件流水线”，在“第10章 指令级并行性”中用于把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表，并受源程序、表示、规则、目标机、代价与验证边界约束。">
  软件流水线
</Term>

**正式坐标 26/38。** 原版目录键 `10.5 软件流水线`。围绕“一条指令为何能移动到该周期而不破坏依赖、异常和寄存器限制？”，“第10章 指令级并行性”在坐标26把「10·5 软件流水线」落实为把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表；只有依赖图、周期表、资源占用、寄存器压力与异常反例可重放且反例排除非法移动跨越依赖、守卫、异常或资源限制，本节点才算掌握。

### 10·5·1 引言

<Term def="引言对应正式目录坐标“10.5.1 引言”，在“第10章 指令级并行性”中用于把“引言”放进指令调度、依赖与资源的输入—状态—变换—验证链，并受源程序、表示、规则、目标机、代价与验证边界约束。">
  引言
</Term>

**正式坐标 27/38。** 原版目录键 `10.5.1 引言`。“第10章 指令级并行性”的目录节点27「10·5·1 引言」不能停在术语或伪码：它要把“引言”放进指令调度、依赖与资源的输入—状态—变换—验证链，交付第10章 指令级并行性的输入角色、中间状态、输出、反例与等价性证据，并把只复述“引言”名称而没有可观察状态、单故障和恢复验证设为单一反事实。

### 10·5·2 循环的软件流水线

<Term def="循环的软件流水线对应正式目录坐标“10.5.2 循环的软件流水线”，在“第10章 指令级并行性”中用于求解数据流固定点并证明变换在所有控制流路径上保语义，并受源程序、表示、规则、目标机、代价与验证边界约束。">
  循环的软件流水线
</Term>

**正式坐标 28/38。** 原版目录键 `10.5.2 循环的软件流水线`。对“第10章 指令级并行性”而言，「10·5·2 循环的软件流水线」在第28次检查中改变可观察状态，因为它负责求解数据流固定点并证明变换在所有控制流路径上保语义；格值迭代、交汇/转移、收敛日志、前后CFG和等价性测试必须与“资源、延迟、数据/内存/控制依赖、寄存器压力和异常语义均满足”对齐，不能接受边界值、不可执行路径、别名或单调性假设错误。

### 10·5·3 寄存器分配和代码生成

<Term def="寄存器分配和代码生成对应正式目录坐标“10.5.3 寄存器分配和代码生成”，在“第10章 指令级并行性”中用于声明阶段接口、名字/类型/状态角色和源—目标语义映射，并受源程序、表示、规则、目标机、代价与验证边界约束。">
  寄存器分配和代码生成
</Term>

**正式坐标 29/38。** 原版目录键 `10.5.3 寄存器分配和代码生成`。在“第10章 指令级并行性”的第29个正式坐标中，「10·5·3 寄存器分配和代码生成」通过声明阶段接口、名字/类型/状态角色和源—目标语义映射推进指令调度、依赖与资源；复核者保存阶段快照、符号表、类型、IR谱系、诊断和源位置，出现阶段边界丢失绑定、类型、控制依赖或源位置就撤回结论。

### 10·5·4 Do-Across循环

<Term def="Do-Across循环对应正式目录坐标“10.5.4 Do-Across循环”，在“第10章 指令级并行性”中用于求解数据流固定点并证明变换在所有控制流路径上保语义，并受源程序、表示、规则、目标机、代价与验证边界约束。">
  Do-Across循环
</Term>

**正式坐标 30/38。** 原版目录键 `10.5.4 Do-Across循环`。围绕“一条指令为何能移动到该周期而不破坏依赖、异常和寄存器限制？”，“第10章 指令级并行性”在坐标30把「10·5·4 Do-Across循环」落实为求解数据流固定点并证明变换在所有控制流路径上保语义；只有格值迭代、交汇/转移、收敛日志、前后CFG和等价性测试可重放且反例排除边界值、不可执行路径、别名或单调性假设错误，本节点才算掌握。

### 10·5·5 软件流水线的目标和约束

<Term def="软件流水线的目标和约束对应正式目录坐标“10.5.5 软件流水线的目标和约束”，在“第10章 指令级并行性”中用于把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表，并受源程序、表示、规则、目标机、代价与验证边界约束。">
  软件流水线的目标和约束
</Term>

**正式坐标 31/38。** 原版目录键 `10.5.5 软件流水线的目标和约束`。“第10章 指令级并行性”的目录节点31「10·5·5 软件流水线的目标和约束」不能停在术语或伪码：它要把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表，交付依赖图、周期表、资源占用、寄存器压力与异常反例，并把非法移动跨越依赖、守卫、异常或资源限制设为单一反事实。

### 10·5·6 一个软件流水线算法

<Term def="一个软件流水线算法对应正式目录坐标“10.5.6 一个软件流水线算法”，在“第10章 指令级并行性”中用于把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表，并受源程序、表示、规则、目标机、代价与验证边界约束。">
  一个软件流水线算法
</Term>

**正式坐标 32/38。** 原版目录键 `10.5.6 一个软件流水线算法`。对“第10章 指令级并行性”而言，「10·5·6 一个软件流水线算法」在第32次检查中改变可观察状态，因为它负责把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表；依赖图、周期表、资源占用、寄存器压力与异常反例必须与“资源、延迟、数据/内存/控制依赖、寄存器压力和异常语义均满足”对齐，不能接受非法移动跨越依赖、守卫、异常或资源限制。

### 10·5·7 无环数据依赖图的调度

<Term def="无环数据依赖图的调度对应正式目录坐标“10.5.7 无环数据依赖图的调度”，在“第10章 指令级并行性”中用于沿依赖图安排属性求值、语义动作和栈位置，并受源程序、表示、规则、目标机、代价与验证边界约束。">
  无环数据依赖图的调度
</Term>

**正式坐标 33/38。** 原版目录键 `10.5.7 无环数据依赖图的调度`。在“第10章 指令级并行性”的第33个正式坐标中，「10·5·7 无环数据依赖图的调度」通过沿依赖图安排属性求值、语义动作和栈位置推进指令调度、依赖与资源；复核者保存属性依赖图、拓扑序、栈快照、值与副作用日志，出现属性未就绪、循环依赖或副作用错序就撤回结论。

### 10·5·8 有环依赖图的调度

<Term def="有环依赖图的调度对应正式目录坐标“10.5.8 有环依赖图的调度”，在“第10章 指令级并行性”中用于沿依赖图安排属性求值、语义动作和栈位置，并受源程序、表示、规则、目标机、代价与验证边界约束。">
  有环依赖图的调度
</Term>

**正式坐标 34/38。** 原版目录键 `10.5.8 有环依赖图的调度`。围绕“一条指令为何能移动到该周期而不破坏依赖、异常和寄存器限制？”，“第10章 指令级并行性”在坐标34把「10·5·8 有环依赖图的调度」落实为沿依赖图安排属性求值、语义动作和栈位置；只有属性依赖图、拓扑序、栈快照、值与副作用日志可重放且反例排除属性未就绪、循环依赖或副作用错序，本节点才算掌握。

### 10·5·9 流水线算法的改进

<Term def="流水线算法的改进对应正式目录坐标“10.5.9 流水线算法的改进”，在“第10章 指令级并行性”中用于把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表，并受源程序、表示、规则、目标机、代价与验证边界约束。">
  流水线算法的改进
</Term>

**正式坐标 35/38。** 原版目录键 `10.5.9 流水线算法的改进`。“第10章 指令级并行性”的目录节点35「10·5·9 流水线算法的改进」不能停在术语或伪码：它要把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表，交付依赖图、周期表、资源占用、寄存器压力与异常反例，并把非法移动跨越依赖、守卫、异常或资源限制设为单一反事实。

### 10·5·10 模变量扩展

<Term def="模变量扩展对应正式目录坐标“10.5.10 模变量扩展”，在“第10章 指令级并行性”中用于把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表，并受源程序、表示、规则、目标机、代价与验证边界约束。">
  模变量扩展
</Term>

**正式坐标 36/38。** 原版目录键 `10.5.10 模变量扩展`。对“第10章 指令级并行性”而言，「10·5·10 模变量扩展」在第36次检查中改变可观察状态，因为它负责把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表；依赖图、周期表、资源占用、寄存器压力与异常反例必须与“资源、延迟、数据/内存/控制依赖、寄存器压力和异常语义均满足”对齐，不能接受非法移动跨越依赖、守卫、异常或资源限制。

### 10·5·11 条件语句

<Term def="条件语句对应正式目录坐标“10.5.11 条件语句”，在“第10章 指令级并行性”中用于把“条件语句”放进指令调度、依赖与资源的输入—状态—变换—验证链，并受源程序、表示、规则、目标机、代价与验证边界约束。">
  条件语句
</Term>

**正式坐标 37/38。** 原版目录键 `10.5.11 条件语句`。在“第10章 指令级并行性”的第37个正式坐标中，「10·5·11 条件语句」通过把“条件语句”放进指令调度、依赖与资源的输入—状态—变换—验证链推进指令调度、依赖与资源；复核者保存第10章 指令级并行性的输入角色、中间状态、输出、反例与等价性证据，出现只复述“条件语句”名称而没有可观察状态、单故障和恢复验证就撤回结论。

### 10·5·12 对软件流水线的硬件支持

<Term def="对软件流水线的硬件支持对应正式目录坐标“10.5.12 对软件流水线的硬件支持”，在“第10章 指令级并行性”中用于把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表，并受源程序、表示、规则、目标机、代价与验证边界约束。">
  对软件流水线的硬件支持
</Term>

**正式坐标 38/38。** 原版目录键 `10.5.12 对软件流水线的硬件支持`。围绕“一条指令为何能移动到该周期而不破坏依赖、异常和寄存器限制？”，“第10章 指令级并行性”在坐标38把「10·5·12 对软件流水线的硬件支持」落实为把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表；只有依赖图、周期表、资源占用、寄存器压力与异常反例可重放且反例排除非法移动跨越依赖、守卫、异常或资源限制，本节点才算掌握。

## 先预测，再操作三个章专属实验

<Callout type="info" title="先写出哪个状态会最先变化">
  对“第10章
  指令级并行性”先冻结在固定机器模型上调度基本块和循环迭代的源程序、文法/IR/机器版本、规则、预算和验证口径，再操作三类实验；结果与预测不同就修改假设，不删除失败运行。
</Callout>

<Stepper>
  <Step title="1. 输入、表示与翻译流水线">
    为“第10章 指令级并行性”选择正式目录坐标，在参考流水线与单一故障间切换，逐阶段核对输入、变换、输出和不变量。

    <Dbc10InstructionLevelParallelismPipelineContractLab />

  </Step>
  <Step title="2. 状态、不变量与反事实轨迹">
    保持“第10章 指令级并行性”的在固定机器模型上调度基本块和循环迭代不变，只注入“推测移动可能抛异常的指令越过守卫分支”，逐步定位首个偏离“资源、延迟、数据/内存/控制依赖、寄存器压力和异常语义均满足”的位置。

    <Dbc10InstructionLevelParallelismStateTraceLab />

  </Step>
  <Step title="3. 等价性与交付验证门">
    在“第10章 指令级并行性”的基线、单故障和恢复案例间切换，展开结构、状态、变换和等价性门后再决定是否交付。

    <Dbc10InstructionLevelParallelismVerificationGateLab />

  </Step>
</Stepper>

<Callout type="trap" title="本页单一故障：推测移动可能抛异常的指令越过守卫分支">
  “第10章
  指令级并行性”遇到该故障时必须保持其余源程序、表示、规则、目标机、预算和执行顺序不变；编译成功或样例输出正确不能替代首个状态分岔与恢复证据。
</Callout>

<Callout type="trap" title="分析收敛或基准加速不等于语义正确">
  “第10章
  指令级并行性”中的固定点、冲突消失、目标码变短或基准更快只回答各自合同；它们不能自动证明所有路径、别名、异常、语言特性和目标机上的语义等价。
</Callout>

<Callout type="trap" title="目录可访问不等于允许翻译或复制">
  “第10章
  指令级并行性”可以用Pearson和中文版目录核对范围，但没有获得复制原文、图表、伪码或练习的授权；当前内容必须保持独立表达、工具核对与版本边界。
</Callout>

## 第10章 指令级并行性的可重放协议

| 阶段                             | 允许动作                                                                                                     | 必留证据                                              | 拒绝条件                                                                                    |
| -------------------------------- | ------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------- | ------------------------------------------------------------------------------------------- |
| 第10章 指令级并行性 · 输入与表示 | 冻结指令调度、依赖与资源所需的源程序、文法/IR/机器版本、shape和符号角色                                      | 第10章 指令级并行性的输入合同、版本表与基线快照       | 未满足“第10章 指令级并行性的源位置、名字、类型、控制/数据依赖和可见性没有越界”              |
| 第10章 指令级并行性 · 状态变换   | 执行把处理器资源、数据/内存/控制依赖、列表调度、全局调度与软件流水线连接到时序约束的最小算法并保存每一步状态 | 第10章 指令级并行性的参考轨迹、故障轨迹与首个状态分岔 | 未满足“第10章 指令级并行性每一步可由同一输入、规则、版本和顺序复算”                         |
| 第10章 指令级并行性 · 输出与代价 | 比较变换前后IR/目标状态、诊断、资源或分析精度                                                                | 第10章 指令级并行性的前后差、语义映射、代价和恢复路径 | 未满足“第10章 指令级并行性没有把编译成功、分析收敛或单一基准加速当作完整正确性”             |
| 第10章 指令级并行性 · 独立验证   | 重放预测、单故障、恢复和不适用边界                                                                           | 第10章 指令级并行性的接受、回退或拒绝理由             | 未满足“第10章 指令级并行性满足“资源、延迟、数据/内存/控制依赖、寄存器压力和异常语义均满足”” |

```yaml
unit: "dbc-unit-10"
question: "一条指令为何能移动到该周期而不破坏依赖、异常和寄存器限制？"
scenario: "在固定机器模型上调度基本块和循环迭代"
invariant: "资源、延迟、数据/内存/控制依赖、寄存器压力和异常语义均满足"
fault: "推测移动可能抛异常的指令越过守卫分支"
evidence: "依赖图、周期表、资源占用、寄存器压力与异常反例"
reset: restore_concept_mode_stage_trace_step_case_gates_and_artifact
```

“第10章 指令级并行性”要求从同一源程序、文法/IR/机器版本、规则、预算和执行顺序重放参考、故障与恢复路径。重置后若目录选择、模式、阶段、轨迹步骤、案例、证据门或交付包没有回到基线，本次比较已经混入状态泄漏。

## 本页回顾

掌握“第10章 指令级并行性”不是背术语、表格或伪码，而是围绕“一条指令为何能移动到该周期而不破坏依赖、异常和寄存器限制？”重建输入、表示、状态变换、输出、代价和独立验证，并用“资源、延迟、数据/内存/控制依赖、寄存器压力和异常语义均满足”拒绝“推测移动可能抛异常的指令越过守卫分支”。最终交付为依赖图、周期表、资源占用、寄存器压力与异常反例。

## 练习与答案

<Exercises>

1. **问题 1：编译合同。** “第10章 指令级并行性”为什么必须先冻结源程序、文法/IR/机器版本、规则、预算和验证口径？

<Answer>
  若同时改变这些条件，相同输出可能来自不同语法、表示、代价、目标机或验证路径；先冻结合同，才能把观测连接到单一机制并定位首差。
</Answer>

2. **问题 2：目录逐项覆盖。** 怎样证明“第10章 指令级并行性”的正式目录坐标已经进入机制、交互和练习？

<Answer>
  1. “第10章 指令级并行性”的目录项「第10章
  指令级并行性」：以“在IR、寄存器、内存和目标指令间满足定义—使用、调用约定和代价”解释作用，用“基本块DAG、活跃区间、干涉图、指令匹配和差分执行”提供复核。
  2. “第10章 指令级并行性”的目录项「10·1
  处理器体系结构」：以“把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表”解释作用，用“依赖图、周期表、资源占用、寄存器压力与异常反例”提供复核。
  3. “第10章 指令级并行性”的目录项「10·1·1
  指令流水线和分支延迟」：以“在IR、寄存器、内存和目标指令间满足定义—使用、调用约定和代价”解释作用，用“基本块DAG、活跃区间、干涉图、指令匹配和差分执行”提供复核。
  4. “第10章 指令级并行性”的目录项「10·1·2
  流水线化执行」：以“把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表”解释作用，用“依赖图、周期表、资源占用、寄存器压力与异常反例”提供复核。
  5. “第10章 指令级并行性”的目录项「10·1·3
  多指令发射」：以“在IR、寄存器、内存和目标指令间满足定义—使用、调用约定和代价”解释作用，用“基本块DAG、活跃区间、干涉图、指令匹配和差分执行”提供复核。
  6. “第10章 指令级并行性”的目录项「10·2
  代码调度约束」：以“把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表”解释作用，用“依赖图、周期表、资源占用、寄存器压力与异常反例”提供复核。
  7. “第10章 指令级并行性”的目录项「10·2·1
  数据依赖」：以“把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表”解释作用，用“依赖图、周期表、资源占用、寄存器压力与异常反例”提供复核。
  8. “第10章 指令级并行性”的目录项「10·2·2
  寻找内存访问之间的依赖」：以“把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表”解释作用，用“依赖图、周期表、资源占用、寄存器压力与异常反例”提供复核。
  9. “第10章 指令级并行性”的目录项「10·2·3
  寄存器使用和并行性之间的权衡」：以“在IR、寄存器、内存和目标指令间满足定义—使用、调用约定和代价”解释作用，用“基本块DAG、活跃区间、干涉图、指令匹配和差分执行”提供复核。
  10. “第10章 指令级并行性”的目录项「10·2·4
  寄存器分配和代码调度的阶段顺序」：以“在IR、寄存器、内存和目标指令间满足定义—使用、调用约定和代价”解释作用，用“基本块DAG、活跃区间、干涉图、指令匹配和差分执行”提供复核。
  11. “第10章 指令级并行性”的目录项「10·2·5
  控制依赖」：以“把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表”解释作用，用“依赖图、周期表、资源占用、寄存器压力与异常反例”提供复核。
  12. “第10章 指令级并行性”的目录项「10·2·6
  对推测执行的支持」：以“把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表”解释作用，用“依赖图、周期表、资源占用、寄存器压力与异常反例”提供复核。
  13. “第10章 指令级并行性”的目录项「10·2·7
  一个基本机器模型」：以“把“一个基本机器模型”放进指令调度、依赖与资源的输入—状态—变换—验证链”解释作用，用“第10章
  指令级并行性的输入角色、中间状态、输出、反例与等价性证据”提供复核。 14.
  “第10章 指令级并行性”的目录项「10·3
  基本块调度」：以“在IR、寄存器、内存和目标指令间满足定义—使用、调用约定和代价”解释作用，用“基本块DAG、活跃区间、干涉图、指令匹配和差分执行”提供复核。
  15. “第10章 指令级并行性”的目录项「10·3·1
  数据依赖图」：以“沿依赖图安排属性求值、语义动作和栈位置”解释作用，用“属性依赖图、拓扑序、栈快照、值与副作用日志”提供复核。
  16. “第10章 指令级并行性”的目录项「10·3·2
  基本块的列表调度」：以“在IR、寄存器、内存和目标指令间满足定义—使用、调用约定和代价”解释作用，用“基本块DAG、活跃区间、干涉图、指令匹配和差分执行”提供复核。
  17. “第10章 指令级并行性”的目录项「10·3·3
  带优先级的拓扑排序」：以“让产生式、推导、分析选择、语义动作和树/IR状态逐步对应”解释作用，用“文法版本、分析栈、输入指针、树、属性和动作日志”提供复核。
  18. “第10章 指令级并行性”的目录项「10·4
  全局代码调度」：以“把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表”解释作用，用“依赖图、周期表、资源占用、寄存器压力与异常反例”提供复核。
  19. “第10章 指令级并行性”的目录项「10·4·1
  基本代码移动」：以“把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表”解释作用，用“依赖图、周期表、资源占用、寄存器压力与异常反例”提供复核。
  20. “第10章 指令级并行性”的目录项「10·4·2
  向上代码移动」：以“把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表”解释作用，用“依赖图、周期表、资源占用、寄存器压力与异常反例”提供复核。
  21. “第10章 指令级并行性”的目录项「10·4·3
  向下代码移动」：以“把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表”解释作用，用“依赖图、周期表、资源占用、寄存器压力与异常反例”提供复核。
  22. “第10章 指令级并行性”的目录项「10·4·4
  更新数据依赖」：以“把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表”解释作用，用“依赖图、周期表、资源占用、寄存器压力与异常反例”提供复核。
  23. “第10章 指令级并行性”的目录项「10·4·5
  全局调度算法」：以“把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表”解释作用，用“依赖图、周期表、资源占用、寄存器压力与异常反例”提供复核。
  24. “第10章 指令级并行性”的目录项「10·4·6
  高级代码移动技术」：以“把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表”解释作用，用“依赖图、周期表、资源占用、寄存器压力与异常反例”提供复核。
  25. “第10章 指令级并行性”的目录项「10·4·7
  和动态调度器的交互」：以“把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表”解释作用，用“依赖图、周期表、资源占用、寄存器压力与异常反例”提供复核。
  26. “第10章 指令级并行性”的目录项「10·5
  软件流水线」：以“把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表”解释作用，用“依赖图、周期表、资源占用、寄存器压力与异常反例”提供复核。
  27. “第10章 指令级并行性”的目录项「10·5·1
  引言」：以“把“引言”放进指令调度、依赖与资源的输入—状态—变换—验证链”解释作用，用“第10章
  指令级并行性的输入角色、中间状态、输出、反例与等价性证据”提供复核。 28.
  “第10章 指令级并行性”的目录项「10·5·2
  循环的软件流水线」：以“求解数据流固定点并证明变换在所有控制流路径上保语义”解释作用，用“格值迭代、交汇/转移、收敛日志、前后CFG和等价性测试”提供复核。
  29. “第10章 指令级并行性”的目录项「10·5·3
  寄存器分配和代码生成」：以“声明阶段接口、名字/类型/状态角色和源—目标语义映射”解释作用，用“阶段快照、符号表、类型、IR谱系、诊断和源位置”提供复核。
  30. “第10章 指令级并行性”的目录项「10·5·4
  Do-Across循环」：以“求解数据流固定点并证明变换在所有控制流路径上保语义”解释作用，用“格值迭代、交汇/转移、收敛日志、前后CFG和等价性测试”提供复核。
  31. “第10章 指令级并行性”的目录项「10·5·5
  软件流水线的目标和约束」：以“把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表”解释作用，用“依赖图、周期表、资源占用、寄存器压力与异常反例”提供复核。
  32. “第10章 指令级并行性”的目录项「10·5·6
  一个软件流水线算法」：以“把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表”解释作用，用“依赖图、周期表、资源占用、寄存器压力与异常反例”提供复核。
  33. “第10章 指令级并行性”的目录项「10·5·7
  无环数据依赖图的调度」：以“沿依赖图安排属性求值、语义动作和栈位置”解释作用，用“属性依赖图、拓扑序、栈快照、值与副作用日志”提供复核。
  34. “第10章 指令级并行性”的目录项「10·5·8
  有环依赖图的调度」：以“沿依赖图安排属性求值、语义动作和栈位置”解释作用，用“属性依赖图、拓扑序、栈快照、值与副作用日志”提供复核。
  35. “第10章 指令级并行性”的目录项「10·5·9
  流水线算法的改进」：以“把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表”解释作用，用“依赖图、周期表、资源占用、寄存器压力与异常反例”提供复核。
  36. “第10章 指令级并行性”的目录项「10·5·10
  模变量扩展」：以“把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表”解释作用，用“依赖图、周期表、资源占用、寄存器压力与异常反例”提供复核。
  37. “第10章 指令级并行性”的目录项「10·5·11
  条件语句」：以“把“条件语句”放进指令调度、依赖与资源的输入—状态—变换—验证链”解释作用，用“第10章
  指令级并行性的输入角色、中间状态、输出、反例与等价性证据”提供复核。 38.
  “第10章 指令级并行性”的目录项「10·5·12
  对软件流水线的硬件支持」：以“把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表”解释作用，用“依赖图、周期表、资源占用、寄存器压力与异常反例”提供复核。
</Answer>

3. **问题 3：故障恢复。** 怎样证明“推测移动可能抛异常的指令越过守卫分支”已经被修正？

<Answer>
  为“第10章
  指令级并行性”复用同一源程序、规则、表示、目标机、预算和执行顺序，重放参考路径后只注入该故障；记录首个偏离，撤销故障再运行。只有流水线合同、状态轨迹、等价性门和依赖图、周期表、资源占用、寄存器压力与异常反例重新满足“资源、延迟、数据/内存/控制依赖、寄存器压力和异常语义均满足”，修正才可提交。
</Answer>

</Exercises>

## 名词解释

<Glossary>
  <GlossaryItem term="指令级并行性">
    检索键 dbc-A 对应正式目录坐标「第10章 指令级并行性」；在“第10章
    指令级并行性”中用于在IR、寄存器、内存和目标指令间满足定义—使用、调用约定和代价，需要连接原版范围、状态轨迹、等价性证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="处理器体系结构">
    检索键 dbc-B 对应正式目录坐标「10·1 处理器体系结构」；在“第10章
    指令级并行性”中用于把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表，需要连接原版范围、状态轨迹、等价性证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="指令流水线和分支延迟">
    检索键 dbc-C 对应正式目录坐标「10·1·1 指令流水线和分支延迟」；在“第10章
    指令级并行性”中用于在IR、寄存器、内存和目标指令间满足定义—使用、调用约定和代价，需要连接原版范围、状态轨迹、等价性证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="流水线化执行">
    检索键 dbc-D 对应正式目录坐标「10·1·2 流水线化执行」；在“第10章
    指令级并行性”中用于把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表，需要连接原版范围、状态轨迹、等价性证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="多指令发射">
    检索键 dbc-E 对应正式目录坐标「10·1·3 多指令发射」；在“第10章
    指令级并行性”中用于在IR、寄存器、内存和目标指令间满足定义—使用、调用约定和代价，需要连接原版范围、状态轨迹、等价性证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="代码调度约束">
    检索键 dbc-F 对应正式目录坐标「10·2 代码调度约束」；在“第10章
    指令级并行性”中用于把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表，需要连接原版范围、状态轨迹、等价性证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="数据依赖">
    检索键 dbc-G 对应正式目录坐标「10·2·1 数据依赖」；在“第10章
    指令级并行性”中用于把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表，需要连接原版范围、状态轨迹、等价性证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="寻找内存访问之间的依赖">
    检索键 dbc-H 对应正式目录坐标「10·2·2 寻找内存访问之间的依赖」；在“第10章
    指令级并行性”中用于把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表，需要连接原版范围、状态轨迹、等价性证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="寄存器使用和并行性之间的权衡">
    检索键 dbc-I 对应正式目录坐标「10·2·3
    寄存器使用和并行性之间的权衡」；在“第10章
    指令级并行性”中用于在IR、寄存器、内存和目标指令间满足定义—使用、调用约定和代价，需要连接原版范围、状态轨迹、等价性证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="寄存器分配和代码调度的阶段顺序">
    检索键 dbc-J 对应正式目录坐标「10·2·4
    寄存器分配和代码调度的阶段顺序」；在“第10章
    指令级并行性”中用于在IR、寄存器、内存和目标指令间满足定义—使用、调用约定和代价，需要连接原版范围、状态轨迹、等价性证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="控制依赖">
    检索键 dbc-K 对应正式目录坐标「10·2·5 控制依赖」；在“第10章
    指令级并行性”中用于把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表，需要连接原版范围、状态轨迹、等价性证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="对推测执行的支持">
    检索键 dbc-L 对应正式目录坐标「10·2·6 对推测执行的支持」；在“第10章
    指令级并行性”中用于把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表，需要连接原版范围、状态轨迹、等价性证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="一个基本机器模型">
    检索键 dbc-M 对应正式目录坐标「10·2·7 一个基本机器模型」；在“第10章
    指令级并行性”中用于把“一个基本机器模型”放进指令调度、依赖与资源的输入—状态—变换—验证链，需要连接原版范围、状态轨迹、等价性证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="基本块调度">
    检索键 dbc-N 对应正式目录坐标「10·3 基本块调度」；在“第10章
    指令级并行性”中用于在IR、寄存器、内存和目标指令间满足定义—使用、调用约定和代价，需要连接原版范围、状态轨迹、等价性证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="数据依赖图">
    检索键 dbc-O 对应正式目录坐标「10·3·1 数据依赖图」；在“第10章
    指令级并行性”中用于沿依赖图安排属性求值、语义动作和栈位置，需要连接原版范围、状态轨迹、等价性证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="基本块的列表调度">
    检索键 dbc-P 对应正式目录坐标「10·3·2 基本块的列表调度」；在“第10章
    指令级并行性”中用于在IR、寄存器、内存和目标指令间满足定义—使用、调用约定和代价，需要连接原版范围、状态轨迹、等价性证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="带优先级的拓扑排序">
    检索键 dbc-Q 对应正式目录坐标「10·3·3 带优先级的拓扑排序」；在“第10章
    指令级并行性”中用于让产生式、推导、分析选择、语义动作和树/IR状态逐步对应，需要连接原版范围、状态轨迹、等价性证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="全局代码调度">
    检索键 dbc-R 对应正式目录坐标「10·4 全局代码调度」；在“第10章
    指令级并行性”中用于把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表，需要连接原版范围、状态轨迹、等价性证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="基本代码移动">
    检索键 dbc-S 对应正式目录坐标「10·4·1 基本代码移动」；在“第10章
    指令级并行性”中用于把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表，需要连接原版范围、状态轨迹、等价性证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="向上代码移动">
    检索键 dbc-T 对应正式目录坐标「10·4·2 向上代码移动」；在“第10章
    指令级并行性”中用于把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表，需要连接原版范围、状态轨迹、等价性证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="向下代码移动">
    检索键 dbc-U 对应正式目录坐标「10·4·3 向下代码移动」；在“第10章
    指令级并行性”中用于把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表，需要连接原版范围、状态轨迹、等价性证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="更新数据依赖">
    检索键 dbc-V 对应正式目录坐标「10·4·4 更新数据依赖」；在“第10章
    指令级并行性”中用于把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表，需要连接原版范围、状态轨迹、等价性证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="全局调度算法">
    检索键 dbc-W 对应正式目录坐标「10·4·5 全局调度算法」；在“第10章
    指令级并行性”中用于把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表，需要连接原版范围、状态轨迹、等价性证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="高级代码移动技术">
    检索键 dbc-X 对应正式目录坐标「10·4·6 高级代码移动技术」；在“第10章
    指令级并行性”中用于把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表，需要连接原版范围、状态轨迹、等价性证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="和动态调度器的交互">
    检索键 dbc-Y 对应正式目录坐标「10·4·7 和动态调度器的交互」；在“第10章
    指令级并行性”中用于把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表，需要连接原版范围、状态轨迹、等价性证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="软件流水线">
    检索键 dbc-Z 对应正式目录坐标「10·5 软件流水线」；在“第10章
    指令级并行性”中用于把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表，需要连接原版范围、状态轨迹、等价性证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="引言">
    检索键 dbc-AA 对应正式目录坐标「10·5·1 引言」；在“第10章
    指令级并行性”中用于把“引言”放进指令调度、依赖与资源的输入—状态—变换—验证链，需要连接原版范围、状态轨迹、等价性证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="循环的软件流水线">
    检索键 dbc-AB 对应正式目录坐标「10·5·2 循环的软件流水线」；在“第10章
    指令级并行性”中用于求解数据流固定点并证明变换在所有控制流路径上保语义，需要连接原版范围、状态轨迹、等价性证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="寄存器分配和代码生成">
    检索键 dbc-AC 对应正式目录坐标「10·5·3 寄存器分配和代码生成」；在“第10章
    指令级并行性”中用于声明阶段接口、名字/类型/状态角色和源—目标语义映射，需要连接原版范围、状态轨迹、等价性证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="Do-Across循环">
    检索键 dbc-AD 对应正式目录坐标「10·5·4 Do-Across循环」；在“第10章
    指令级并行性”中用于求解数据流固定点并证明变换在所有控制流路径上保语义，需要连接原版范围、状态轨迹、等价性证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="软件流水线的目标和约束">
    检索键 dbc-AE 对应正式目录坐标「10·5·5 软件流水线的目标和约束」；在“第10章
    指令级并行性”中用于把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表，需要连接原版范围、状态轨迹、等价性证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="一个软件流水线算法">
    检索键 dbc-AF 对应正式目录坐标「10·5·6 一个软件流水线算法」；在“第10章
    指令级并行性”中用于把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表，需要连接原版范围、状态轨迹、等价性证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="无环数据依赖图的调度">
    检索键 dbc-AG 对应正式目录坐标「10·5·7 无环数据依赖图的调度」；在“第10章
    指令级并行性”中用于沿依赖图安排属性求值、语义动作和栈位置，需要连接原版范围、状态轨迹、等价性证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="有环依赖图的调度">
    检索键 dbc-AH 对应正式目录坐标「10·5·8 有环依赖图的调度」；在“第10章
    指令级并行性”中用于沿依赖图安排属性求值、语义动作和栈位置，需要连接原版范围、状态轨迹、等价性证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="流水线算法的改进">
    检索键 dbc-AI 对应正式目录坐标「10·5·9 流水线算法的改进」；在“第10章
    指令级并行性”中用于把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表，需要连接原版范围、状态轨迹、等价性证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="模变量扩展">
    检索键 dbc-AJ 对应正式目录坐标「10·5·10 模变量扩展」；在“第10章
    指令级并行性”中用于把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表，需要连接原版范围、状态轨迹、等价性证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="条件语句">
    检索键 dbc-AK 对应正式目录坐标「10·5·11 条件语句」；在“第10章
    指令级并行性”中用于把“条件语句”放进指令调度、依赖与资源的输入—状态—变换—验证链，需要连接原版范围、状态轨迹、等价性证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="对软件流水线的硬件支持">
    检索键 dbc-AL 对应正式目录坐标「10·5·12 对软件流水线的硬件支持」；在“第10章
    指令级并行性”中用于把资源、延迟、数据/内存/控制依赖和寄存器压力映射到周期表，需要连接原版范围、状态轨迹、等价性证据和不适用边界。
  </GlossaryItem>
</Glossary>

<Attribution
  mode="independent-rewrite"
  sourceBasis="pearson-second-edition-official-toc-plus-chinese-edition-complete-toc"
  workTitle="Aho、Lam、Sethi、Ullman著《Compilers: Principles, Techniques, and Tools, Second Edition》"
  adaptedUrl="https://www.pearson.com/en-us/subject-catalog/p/compilers-principles-techniques-and-tools/P200000003472/9780133002140"
/>
