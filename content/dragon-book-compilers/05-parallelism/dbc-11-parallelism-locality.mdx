---
title: "第11章 并行性和局部性的优化"
type: C
section: "并行化与过程间分析"
order: 11
description: "第11章 并行性和局部性的优化逐项覆盖67个核心目录层级，以结构化中间产物、运行轨迹与失败对照建立可复查的编译实验。"
demo: true
math: false
sourceUrl: "https://www.pearson.com/en-us/subject-catalog/p/compilers-principles-techniques-and-tools/P200000003472/9780133002140"
draft: false
---

import {
  Dbc11ParallelismLocalityMapLab,
  Dbc11ParallelismLocalityExperimentLab,
  Dbc11ParallelismLocalityEvidenceLab,
} from "@/components/mdx/dragon-book-compilers/diagrams/dbc-11-parallelism-locality";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第11章 并行性和局部性的优化在完整编译流水线中的输入、输出、数据结构和责任边界
- 能描述本页67个核心目录层级之间的依赖、算法前提与工程取舍
- 能复现最小三地址代码或伪代码实验，保存正常、边界、失败和优化四类证据
- 能判断“变换后的迭代空间覆盖原迭代且不重复，所有真依赖保持合法方向，生成边界为整数可执行集合，局部性收益不牺牲语义。”是否成立，而不是只看最终程序是否运行

</Objectives>

## 为什么最终输出正确仍然不够

<Term def="迭代空间连接“建立迭代域”阶段的输入、算法前提、结构化结果、成本和第一处失败证据。">
  迭代空间
</Term>
、
<Term def="仿射变换连接“提取仿射访问”阶段的输入、算法前提、结构化结果、成本和第一处失败证据。">
  仿射变换
</Term>
、
<Term def="数组依赖连接“求解依赖约束”阶段的输入、算法前提、结构化结果、成本和第一处失败证据。">
  数组依赖
</Term>
、
<Term def="空间划分连接“选择空间时间变换”阶段的输入、算法前提、结构化结果、成本和第一处失败证据。">
  空间划分
</Term>
、
<Term def="时间划分连接“生成边界代码”阶段的输入、算法前提、结构化结果、成本和第一处失败证据。">
  时间划分
</Term>
共同构成本页坐标。用迭代空间、仿射访问和数组依赖约束统一循环变换，在无同步并行、流水线、同步数量与数据局部性之间求可证明的解。

编译器是一串互相依赖的转换。错误的扫描器可能恰好产生可分析的词法单元，错误的语法树可能被后续语义动作意外修补，错误的数据流事实可能只在冷路径暴露，违反机器约束的调度也可能在当前样本中碰巧执行。因此必须保存阶段输入、输出和不变量，定位第一处偏离，不能把单个样例跑通当作证明。

## 核心模型与实验账本

实验固定源程序、编译器提交、中间表示格式、目标机模型、优化级别和随机种子。前端保存词法单元、推导、语法树、属性值与类型环境；中端保存基本块、控制流图、支配关系、数据流集合和每次变换前后的IR；后端保存模式覆盖、依赖图、发射周期、寄存器映射、溢出代码与最终指令。每份产物都带生成方式、退出状态和摘要值。

样本分成四组。正常样本证明主路径，边界样本覆盖空输入、深嵌套、二义性、不可归约流图、寄存器压力、长延迟、别名和动态调用；失败样本只破坏一个语法、类型、数据流或机器约束；优化样本同时保存变换前后版本，并用解释执行或差分执行核对可观察行为。

诊断记录阶段、源位置或IR位置、实际状态、期望状态、相关前驱和首要根因。某阶段失败后不得继续消费旧缓存伪造下游结果；分析事实被变换失效时，必须显式重算或证明仍然有效。性能结论与正确性结论分开：前者记录静态成本、动态计数、代码大小和运行环境，后者要求语义与全部约束成立。

目录项进入追溯矩阵后，至少关联一个输入、一个算法前提、一份结构化产物、一个失败对照和一个回归断言。跨章接口同时记录生产者和消费者，这样既能发现“提到概念却没有验证”的空洞覆盖，也能在实现变化后只重放真正受影响的实验。

## 六阶段证据链

### 1. 建立迭代域

围绕迭代空间先预测结构化结果，只改变一个条件并运行；进入“提取仿射访问”前，核对语义、算法前提、成本和首错位置。

### 2. 提取仿射访问

围绕仿射变换先预测结构化结果，只改变一个条件并运行；进入“求解依赖约束”前，核对语义、算法前提、成本和首错位置。

### 3. 求解依赖约束

围绕数组依赖先预测结构化结果，只改变一个条件并运行；进入“选择空间时间变换”前，核对语义、算法前提、成本和首错位置。

### 4. 选择空间时间变换

围绕空间划分先预测结构化结果，只改变一个条件并运行；进入“生成边界代码”前，核对语义、算法前提、成本和首错位置。

### 5. 生成边界代码

围绕时间划分先预测结构化结果，只改变一个条件并运行；进入“测量并行与局部性”前，核对语义、算法前提、成本和首错位置。

### 6. 测量并行与局部性

围绕迭代空间先预测结构化结果，只改变一个条件并运行；进入“建立迭代域”前，核对语义、算法前提、成本和首错位置。

## 可运行的最小骨架

```text
D = { i | A i + B n >= c }
```

```text
dependence exists if F(i) = G(j) with i preceding j
```

```text
tile(i, j, Ti, Tj) -> outer tiles + inner points
```

<Callout type="trap">
  **版本边界：**
  本页复原的是龙书第二版的算法模型和目录边界。现代LLVM、GCC、JVM或具体ISA可以用来做对照，但不能把其默认实现反写成原书算法的保证。
</Callout>

## 权威目录逐项讲解

### 第11章 并行性和局部性的优化

“第11章 并行性和局部性的优化”在本页不是孤立名词。先在“建立迭代域”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“提取仿射访问”检查第一条结构差异。围绕迭代空间记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 11.1 基本概念

“11.1 基本概念”在本页不是孤立名词。先在“提取仿射访问”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“求解依赖约束”检查第一条结构差异。围绕仿射变换记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 11.1.1 多处理器

“11.1.1 多处理器”在本页不是孤立名词。先在“求解依赖约束”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“选择空间时间变换”检查第一条结构差异。围绕数组依赖记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 11.1.2 应用中的并行性

“11.1.2 应用中的并行性”在本页不是孤立名词。先在“选择空间时间变换”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“生成边界代码”检查第一条结构差异。围绕空间划分记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 11.1.3 循环层次的并行性

“11.1.3 循环层次的并行性”在本页不是孤立名词。先在“生成边界代码”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“测量并行与局部性”检查第一条结构差异。围绕时间划分记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 11.1.4 数据局部性

“11.1.4 数据局部性”在本页不是孤立名词。先在“测量并行与局部性”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“建立迭代域”检查第一条结构差异。围绕迭代空间记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 11.1.5 仿射变换理论简介

“11.1.5 仿射变换理论简介”在本页不是孤立名词。先在“建立迭代域”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“提取仿射访问”检查第一条结构差异。围绕仿射变换记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 11.2 矩阵乘法：一个深入的例子

“11.2 矩阵乘法：一个深入的例子”在本页不是孤立名词。先在“提取仿射访问”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“求解依赖约束”检查第一条结构差异。围绕数组依赖记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 11.2.1 矩阵乘法算法

“11.2.1 矩阵乘法算法”在本页不是孤立名词。先在“求解依赖约束”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“选择空间时间变换”检查第一条结构差异。围绕空间划分记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 11.2.2 优化

“11.2.2 优化”在本页不是孤立名词。先在“选择空间时间变换”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“生成边界代码”检查第一条结构差异。围绕时间划分记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 11.2.3 缓存干扰

“11.2.3 缓存干扰”在本页不是孤立名词。先在“生成边界代码”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“测量并行与局部性”检查第一条结构差异。围绕迭代空间记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 11.3 迭代空间

“11.3 迭代空间”在本页不是孤立名词。先在“测量并行与局部性”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“建立迭代域”检查第一条结构差异。围绕仿射变换记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 11.3.1 从循环嵌套构造迭代空间

“11.3.1 从循环嵌套构造迭代空间”在本页不是孤立名词。先在“建立迭代域”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“提取仿射访问”检查第一条结构差异。围绕数组依赖记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 11.3.2 循环嵌套的执行顺序

“11.3.2 循环嵌套的执行顺序”在本页不是孤立名词。先在“提取仿射访问”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“求解依赖约束”检查第一条结构差异。围绕空间划分记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 11.3.3 不等式的矩阵表示

“11.3.3 不等式的矩阵表示”在本页不是孤立名词。先在“求解依赖约束”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“选择空间时间变换”检查第一条结构差异。围绕时间划分记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 11.3.4 加入符号常量

“11.3.4 加入符号常量”在本页不是孤立名词。先在“选择空间时间变换”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“生成边界代码”检查第一条结构差异。围绕迭代空间记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 11.3.5 控制执行顺序

“11.3.5 控制执行顺序”在本页不是孤立名词。先在“生成边界代码”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“测量并行与局部性”检查第一条结构差异。围绕仿射变换记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 11.3.6 改变坐标轴

“11.3.6 改变坐标轴”在本页不是孤立名词。先在“测量并行与局部性”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“建立迭代域”检查第一条结构差异。围绕数组依赖记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 11.4 仿射数组下标

“11.4 仿射数组下标”在本页不是孤立名词。先在“建立迭代域”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“提取仿射访问”检查第一条结构差异。围绕空间划分记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 11.4.1 仿射访问

“11.4.1 仿射访问”在本页不是孤立名词。先在“提取仿射访问”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“求解依赖约束”检查第一条结构差异。围绕时间划分记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 11.4.2 实践中的仿射和非仿射访问

“11.4.2 实践中的仿射和非仿射访问”在本页不是孤立名词。先在“求解依赖约束”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“选择空间时间变换”检查第一条结构差异。围绕迭代空间记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 11.5 数据复用

“11.5 数据复用”在本页不是孤立名词。先在“选择空间时间变换”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“生成边界代码”检查第一条结构差异。围绕仿射变换记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 11.5.1 复用的类型

“11.5.1 复用的类型”在本页不是孤立名词。先在“生成边界代码”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“测量并行与局部性”检查第一条结构差异。围绕数组依赖记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 11.5.2 自复用

“11.5.2 自复用”在本页不是孤立名词。先在“测量并行与局部性”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“建立迭代域”检查第一条结构差异。围绕空间划分记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 11.5.3 自空间复用

“11.5.3 自空间复用”在本页不是孤立名词。先在“建立迭代域”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“提取仿射访问”检查第一条结构差异。围绕时间划分记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 11.5.4 组复用

“11.5.4 组复用”在本页不是孤立名词。先在“提取仿射访问”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“求解依赖约束”检查第一条结构差异。围绕迭代空间记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 11.6 数组数据依赖分析

“11.6 数组数据依赖分析”在本页不是孤立名词。先在“求解依赖约束”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“选择空间时间变换”检查第一条结构差异。围绕仿射变换记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 11.6.1 数组访问的数据依赖定义

“11.6.1 数组访问的数据依赖定义”在本页不是孤立名词。先在“选择空间时间变换”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“生成边界代码”检查第一条结构差异。围绕数组依赖记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 11.6.2 整数线性规划

“11.6.2 整数线性规划”在本页不是孤立名词。先在“生成边界代码”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“测量并行与局部性”检查第一条结构差异。围绕空间划分记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 11.6.3 最大公约数测试

“11.6.3 最大公约数测试”在本页不是孤立名词。先在“测量并行与局部性”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“建立迭代域”检查第一条结构差异。围绕时间划分记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 11.6.4 求解整数线性规划的启发式方法

“11.6.4 求解整数线性规划的启发式方法”在本页不是孤立名词。先在“建立迭代域”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“提取仿射访问”检查第一条结构差异。围绕迭代空间记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 11.6.5 通用整数线性规划的求解

“11.6.5 通用整数线性规划的求解”在本页不是孤立名词。先在“提取仿射访问”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“求解依赖约束”检查第一条结构差异。围绕仿射变换记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 11.6.6 本节总结

“11.6.6 本节总结”在本页不是孤立名词。先在“求解依赖约束”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“选择空间时间变换”检查第一条结构差异。围绕数组依赖记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 11.7 寻找无同步的并行性

“11.7 寻找无同步的并行性”在本页不是孤立名词。先在“选择空间时间变换”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“生成边界代码”检查第一条结构差异。围绕空间划分记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 11.7.1 一个入门例子

“11.7.1 一个入门例子”在本页不是孤立名词。先在“生成边界代码”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“测量并行与局部性”检查第一条结构差异。围绕时间划分记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 11.7.2 仿射空间划分

“11.7.2 仿射空间划分”在本页不是孤立名词。先在“测量并行与局部性”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“建立迭代域”检查第一条结构差异。围绕迭代空间记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 11.7.3 空间划分约束

“11.7.3 空间划分约束”在本页不是孤立名词。先在“建立迭代域”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“提取仿射访问”检查第一条结构差异。围绕仿射变换记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 11.7.4 求解空间划分约束

“11.7.4 求解空间划分约束”在本页不是孤立名词。先在“提取仿射访问”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“求解依赖约束”检查第一条结构差异。围绕数组依赖记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 11.7.5 一个简单的代码生成算法

“11.7.5 一个简单的代码生成算法”在本页不是孤立名词。先在“求解依赖约束”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“选择空间时间变换”检查第一条结构差异。围绕空间划分记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 11.7.6 消除空迭代

“11.7.6 消除空迭代”在本页不是孤立名词。先在“选择空间时间变换”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“生成边界代码”检查第一条结构差异。围绕时间划分记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 11.7.7 消除最内层循环中的测试

“11.7.7 消除最内层循环中的测试”在本页不是孤立名词。先在“生成边界代码”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“测量并行与局部性”检查第一条结构差异。围绕迭代空间记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 11.7.8 源代码变换

“11.7.8 源代码变换”在本页不是孤立名词。先在“测量并行与局部性”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“建立迭代域”检查第一条结构差异。围绕仿射变换记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 11.8 并行循环之间的同步

“11.8 并行循环之间的同步”在本页不是孤立名词。先在“建立迭代域”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“提取仿射访问”检查第一条结构差异。围绕数组依赖记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 11.8.1 常数次同步

“11.8.1 常数次同步”在本页不是孤立名词。先在“提取仿射访问”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“求解依赖约束”检查第一条结构差异。围绕空间划分记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 11.8.2 程序依赖图

“11.8.2 程序依赖图”在本页不是孤立名词。先在“求解依赖约束”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“选择空间时间变换”检查第一条结构差异。围绕时间划分记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 11.8.3 层次化时间

“11.8.3 层次化时间”在本页不是孤立名词。先在“选择空间时间变换”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“生成边界代码”检查第一条结构差异。围绕迭代空间记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 11.8.4 并行化算法

“11.8.4 并行化算法”在本页不是孤立名词。先在“生成边界代码”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“测量并行与局部性”检查第一条结构差异。围绕仿射变换记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 11.9 流水线化

“11.9 流水线化”在本页不是孤立名词。先在“测量并行与局部性”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“建立迭代域”检查第一条结构差异。围绕数组依赖记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 11.9.1 什么是流水线化

“11.9.1 什么是流水线化”在本页不是孤立名词。先在“建立迭代域”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“提取仿射访问”检查第一条结构差异。围绕空间划分记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 11.9.2 逐次超松弛：一个例子

“11.9.2 逐次超松弛：一个例子”在本页不是孤立名词。先在“提取仿射访问”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“求解依赖约束”检查第一条结构差异。围绕时间划分记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 11.9.3 完全可置换循环

“11.9.3 完全可置换循环”在本页不是孤立名词。先在“求解依赖约束”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“选择空间时间变换”检查第一条结构差异。围绕迭代空间记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 11.9.4 完全可置换循环的流水线化

“11.9.4 完全可置换循环的流水线化”在本页不是孤立名词。先在“选择空间时间变换”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“生成边界代码”检查第一条结构差异。围绕仿射变换记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 11.9.5 通用理论

“11.9.5 通用理论”在本页不是孤立名词。先在“生成边界代码”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“测量并行与局部性”检查第一条结构差异。围绕数组依赖记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 11.9.6 时间划分约束

“11.9.6 时间划分约束”在本页不是孤立名词。先在“测量并行与局部性”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“建立迭代域”检查第一条结构差异。围绕空间划分记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 11.9.7 使用Farkas引理求解时间划分约束

“11.9.7 使用Farkas引理求解时间划分约束”在本页不是孤立名词。先在“建立迭代域”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“提取仿射访问”检查第一条结构差异。围绕时间划分记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 11.9.8 代码变换

“11.9.8 代码变换”在本页不是孤立名词。先在“提取仿射访问”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“求解依赖约束”检查第一条结构差异。围绕迭代空间记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 11.9.9 使用最少同步的并行性

“11.9.9 使用最少同步的并行性”在本页不是孤立名词。先在“求解依赖约束”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“选择空间时间变换”检查第一条结构差异。围绕仿射变换记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 11.10 局部性优化

“11.10 局部性优化”在本页不是孤立名词。先在“选择空间时间变换”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“生成边界代码”检查第一条结构差异。围绕数组依赖记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 11.10.1 计算数据的时间局部性

“11.10.1 计算数据的时间局部性”在本页不是孤立名词。先在“生成边界代码”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“测量并行与局部性”检查第一条结构差异。围绕空间划分记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 11.10.2 数组收缩

“11.10.2 数组收缩”在本页不是孤立名词。先在“测量并行与局部性”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“建立迭代域”检查第一条结构差异。围绕时间划分记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 11.10.3 划分交错

“11.10.3 划分交错”在本页不是孤立名词。先在“建立迭代域”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“提取仿射访问”检查第一条结构差异。围绕迭代空间记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 11.10.4 综合应用

“11.10.4 综合应用”在本页不是孤立名词。先在“提取仿射访问”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“求解依赖约束”检查第一条结构差异。围绕仿射变换记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 11.11 仿射变换的其他用途

“11.11 仿射变换的其他用途”在本页不是孤立名词。先在“求解依赖约束”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“选择空间时间变换”检查第一条结构差异。围绕数组依赖记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 11.11.1 分布式存储机器

“11.11.1 分布式存储机器”在本页不是孤立名词。先在“选择空间时间变换”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“生成边界代码”检查第一条结构差异。围绕空间划分记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 11.11.2 多指令发射处理器

“11.11.2 多指令发射处理器”在本页不是孤立名词。先在“生成边界代码”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“测量并行与局部性”检查第一条结构差异。围绕时间划分记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 11.11.3 向量和SIMD指令

“11.11.3 向量和SIMD指令”在本页不是孤立名词。先在“测量并行与局部性”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“建立迭代域”检查第一条结构差异。围绕迭代空间记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 11.11.4 预取

“11.11.4 预取”在本页不是孤立名词。先在“建立迭代域”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“提取仿射访问”检查第一条结构差异。围绕仿射变换记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

## 分步视觉验证

先预测当前阶段的树、图、集合、表项或指令，再运行变换；若不同，停在第一处分叉并保留原始证据。

<Stepper>
  <Step title="画出输入、变换与产物">
    标出迭代空间、仿射变换、数组依赖、空间划分、时间划分的输入、输出、所有权、算法前提和下游消费者。
    <Dbc11ParallelismLocalityMapLab />
  </Step>
  <Step title="运行单变量实验">
    固定源程序与目标模型，只改变一个文法、分析、优化、运行时或机器条件。
    <Dbc11ParallelismLocalityExperimentLab />
  </Step>
  <Step title="核对第一处分叉">
    比较结构化产物、复杂度和行为，清理旧输出后完整重建并重放回归样本。
    <Dbc11ParallelismLocalityEvidenceLab />
  </Step>
</Stepper>

## 常见误区

<Callout type="trap">
  **只看最终结果：**
  没有词法单元、语法树、属性依赖、CFG、数据流集合、调度表和寄存器映射，就无法把错误归因到具体阶段，也无法证明中间变换保持语义。
</Callout>

<Callout type="trap">
  **先变换再补条件：**
  优化、调度、并行化和过程间分析必须先证明前提；任何会令支配、活跃、别名、依赖或调用图失效的修改，都要触发明确重算。
</Callout>

## 四类样本与通过条件

| 样本 | 单一变量               | 必查证据         | 通过条件         |
| ---- | ---------------------- | ---------------- | ---------------- |
| 正常 | 一个最小语言或机器功能 | 全阶段快照       | 与预测一致       |
| 边界 | 深度、宽度、延迟或压力 | 临界分支和成本   | 边界可解释       |
| 失败 | 一个文法、事实或约束   | 首错与下游停止   | 不产出伪结果     |
| 优化 | 一个变换或策略         | 前后IR与差分执行 | 等价且收益可解释 |

## 练习

<Exercises>

**问题 1** 为什么不能只凭最终程序运行成功验收第11章 并行性和局部性的优化？

<Answer>
  最终行为可能隐藏错误词法单元、树结构、属性值、数据流事实、依赖边或机器约束。应保存每层结构化产物，定位第一处分叉，并从固定源程序清理重建。
</Answer>

**问题 2** 如何把“求解依赖约束”变成可回归实验？

<Answer>
  固定输入、实现版本、中间表示和目标模型，先预测迭代空间与仿射变换的中间状态，只改变一个条件，并保留正常、边界、失败和优化四类样本。
</Answer>

**问题 3** 本页最小通过条件是什么？

<Answer>
  变换后的迭代空间覆盖原迭代且不重复，所有真依赖保持合法方向，生成边界为整数可执行集合，局部性收益不牺牲语义。
  修复后必须清理旧产物，完整重建全部下游结果，并让历史样本回到基线。
</Answer>

</Exercises>

## 小结

- 迭代空间：连接“建立迭代域”的算法前提、结构化产物、成本和回归证据。
- 仿射变换：连接“提取仿射访问”的算法前提、结构化产物、成本和回归证据。
- 数组依赖：连接“求解依赖约束”的算法前提、结构化产物、成本和回归证据。
- 空间划分：连接“选择空间时间变换”的算法前提、结构化产物、成本和回归证据。
- 时间划分：连接“生成边界代码”的算法前提、结构化产物、成本和回归证据。
- 本页67个核心目录层级均已进入可追溯的编译证据链。

## 术语表

<Glossary>
  <GlossaryItem term="迭代空间">
    迭代空间用于解释“建立迭代域”，需要同时说明输入契约、算法前提、结构化产物、复杂度和清理重建方法。
  </GlossaryItem>
  <GlossaryItem term="仿射变换">
    仿射变换用于解释“提取仿射访问”，需要同时说明输入契约、算法前提、结构化产物、复杂度和清理重建方法。
  </GlossaryItem>
  <GlossaryItem term="数组依赖">
    数组依赖用于解释“求解依赖约束”，需要同时说明输入契约、算法前提、结构化产物、复杂度和清理重建方法。
  </GlossaryItem>
  <GlossaryItem term="空间划分">
    空间划分用于解释“选择空间时间变换”，需要同时说明输入契约、算法前提、结构化产物、复杂度和清理重建方法。
  </GlossaryItem>
  <GlossaryItem term="时间划分">
    时间划分用于解释“生成边界代码”，需要同时说明输入契约、算法前提、结构化产物、复杂度和清理重建方法。
  </GlossaryItem>
</Glossary>

<Attribution
  adaptedFrom="Alfred V. Aho、Monica S. Lam、Ravi Sethi、Jeffrey D. Ullman著，赵建华、郑滔、戴新宇译《编译原理（第2版）》，机械工业出版社，2009，631页，ISBN 9787111251217 · 基于Pearson官方第二版资料、中文版完整目录与高校图书馆元数据的独立教学重构"
  adaptedUrl="https://www.pearson.com/en-us/subject-catalog/p/compilers-principles-techniques-and-tools/P200000003472/9780133002140"
/>
