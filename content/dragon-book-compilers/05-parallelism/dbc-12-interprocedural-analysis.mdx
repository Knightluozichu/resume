---
title: "第12章 过程间分析"
type: C
section: "并行化与过程间分析"
order: 12
description: "第12章 过程间分析逐项覆盖41个核心目录层级，以结构化中间产物、运行轨迹与失败对照建立可复查的编译实验。"
demo: true
math: false
sourceUrl: "https://www.pearson.com/en-us/subject-catalog/p/compilers-principles-techniques-and-tools/P200000003472/9780133002140"
draft: false
---

import {
  Dbc12InterproceduralAnalysisMapLab,
  Dbc12InterproceduralAnalysisExperimentLab,
  Dbc12InterproceduralAnalysisEvidenceLab,
} from "@/components/mdx/dragon-book-compilers/diagrams/dbc-12-interprocedural-analysis";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第12章 过程间分析在完整编译流水线中的输入、输出、数据结构和责任边界
- 能描述本页41个核心目录层级之间的依赖、算法前提与工程取舍
- 能复现最小三地址代码或伪代码实验，保存正常、边界、失败和优化四类证据
- 能判断“调用图与指向关系对实际行为保持保守，Datalog规则单调并到达不动点，上下文区分有明确界限，BDD运算与关系语义一致。”是否成立，而不是只看最终程序是否运行

</Objectives>

## 为什么最终输出正确仍然不够

<Term def="调用图连接“建立调用事实”阶段的输入、算法前提、结构化结果、成本和第一处失败证据。">
  调用图
</Term>
、
<Term def="上下文敏感连接“选择上下文”阶段的输入、算法前提、结构化结果、成本和第一处失败证据。">
  上下文敏感
</Term>
、
<Term def="Datalog连接“写出Datalog规则”阶段的输入、算法前提、结构化结果、成本和第一处失败证据。">
  Datalog
</Term>
、
<Term def="指向分析连接“求关系不动点”阶段的输入、算法前提、结构化结果、成本和第一处失败证据。">
  指向分析
</Term>
、
<Term def="二叉决策图连接“压缩为BDD”阶段的输入、算法前提、结构化结果、成本和第一处失败证据。">
  二叉决策图
</Term>
共同构成本页坐标。从调用图和上下文敏感性进入Datalog、指针分析与BDD，使跨过程别名、虚调用、安全缺陷和动态加载假设成为可查询事实。

编译器是一串互相依赖的转换。错误的扫描器可能恰好产生可分析的词法单元，错误的语法树可能被后续语义动作意外修补，错误的数据流事实可能只在冷路径暴露，违反机器约束的调度也可能在当前样本中碰巧执行。因此必须保存阶段输入、输出和不变量，定位第一处偏离，不能把单个样例跑通当作证明。

## 核心模型与实验账本

实验固定源程序、编译器提交、中间表示格式、目标机模型、优化级别和随机种子。前端保存词法单元、推导、语法树、属性值与类型环境；中端保存基本块、控制流图、支配关系、数据流集合和每次变换前后的IR；后端保存模式覆盖、依赖图、发射周期、寄存器映射、溢出代码与最终指令。每份产物都带生成方式、退出状态和摘要值。

样本分成四组。正常样本证明主路径，边界样本覆盖空输入、深嵌套、二义性、不可归约流图、寄存器压力、长延迟、别名和动态调用；失败样本只破坏一个语法、类型、数据流或机器约束；优化样本同时保存变换前后版本，并用解释执行或差分执行核对可观察行为。

诊断记录阶段、源位置或IR位置、实际状态、期望状态、相关前驱和首要根因。某阶段失败后不得继续消费旧缓存伪造下游结果；分析事实被变换失效时，必须显式重算或证明仍然有效。性能结论与正确性结论分开：前者记录静态成本、动态计数、代码大小和运行环境，后者要求语义与全部约束成立。

目录项进入追溯矩阵后，至少关联一个输入、一个算法前提、一份结构化产物、一个失败对照和一个回归断言。跨章接口同时记录生产者和消费者，这样既能发现“提到概念却没有验证”的空洞覆盖，也能在实现变化后只重放真正受影响的实验。

## 六阶段证据链

### 1. 建立调用事实

围绕调用图先预测结构化结果，只改变一个条件并运行；进入“选择上下文”前，核对语义、算法前提、成本和首错位置。

### 2. 选择上下文

围绕上下文敏感先预测结构化结果，只改变一个条件并运行；进入“写出Datalog规则”前，核对语义、算法前提、成本和首错位置。

### 3. 写出Datalog规则

围绕Datalog先预测结构化结果，只改变一个条件并运行；进入“求关系不动点”前，核对语义、算法前提、成本和首错位置。

### 4. 求关系不动点

围绕指向分析先预测结构化结果，只改变一个条件并运行；进入“压缩为BDD”前，核对语义、算法前提、成本和首错位置。

### 5. 压缩为BDD

围绕二叉决策图先预测结构化结果，只改变一个条件并运行；进入“对照动态轨迹”前，核对语义、算法前提、成本和首错位置。

### 6. 对照动态轨迹

围绕调用图先预测结构化结果，只改变一个条件并运行；进入“建立调用事实”前，核对语义、算法前提、成本和首错位置。

## 可运行的最小骨架

```text
pointsTo(v, h) :- alloc(v, h).
pointsTo(v, h) :- assign(v, w), pointsTo(w, h).
```

```text
callEdge(caller, callee) :- virtualCall(site, recv, sig), pointsTo(recv, type), resolves(type, sig, callee).
```

```text
relation tuples <-> BDD characteristic function
```

<Callout type="trap">
  **版本边界：**
  本页复原的是龙书第二版的算法模型和目录边界。现代LLVM、GCC、JVM或具体ISA可以用来做对照，但不能把其默认实现反写成原书算法的保证。
</Callout>

## 权威目录逐项讲解

### 第12章 过程间分析

“第12章 过程间分析”在本页不是孤立名词。先在“建立调用事实”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“选择上下文”检查第一条结构差异。围绕调用图记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 12.1 基本概念

“12.1 基本概念”在本页不是孤立名词。先在“选择上下文”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“写出Datalog规则”检查第一条结构差异。围绕上下文敏感记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 12.1.1 调用图

“12.1.1 调用图”在本页不是孤立名词。先在“写出Datalog规则”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“求关系不动点”检查第一条结构差异。围绕Datalog记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 12.1.2 上下文敏感性

“12.1.2 上下文敏感性”在本页不是孤立名词。先在“求关系不动点”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“压缩为BDD”检查第一条结构差异。围绕指向分析记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 12.1.3 调用串

“12.1.3 调用串”在本页不是孤立名词。先在“压缩为BDD”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“对照动态轨迹”检查第一条结构差异。围绕二叉决策图记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 12.1.4 基于克隆的上下文相关分析

“12.1.4 基于克隆的上下文相关分析”在本页不是孤立名词。先在“对照动态轨迹”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“建立调用事实”检查第一条结构差异。围绕调用图记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 12.1.5 基于摘要的上下文相关分析

“12.1.5 基于摘要的上下文相关分析”在本页不是孤立名词。先在“建立调用事实”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“选择上下文”检查第一条结构差异。围绕上下文敏感记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 12.2 为什么需要过程间分析

“12.2 为什么需要过程间分析”在本页不是孤立名词。先在“选择上下文”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“写出Datalog规则”检查第一条结构差异。围绕Datalog记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 12.2.1 虚方法调用

“12.2.1 虚方法调用”在本页不是孤立名词。先在“写出Datalog规则”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“求关系不动点”检查第一条结构差异。围绕指向分析记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 12.2.2 指针别名分析

“12.2.2 指针别名分析”在本页不是孤立名词。先在“求关系不动点”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“压缩为BDD”检查第一条结构差异。围绕二叉决策图记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 12.2.3 并行化

“12.2.3 并行化”在本页不是孤立名词。先在“压缩为BDD”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“对照动态轨迹”检查第一条结构差异。围绕调用图记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 12.2.4 软件错误和安全漏洞的检测

“12.2.4 软件错误和安全漏洞的检测”在本页不是孤立名词。先在“对照动态轨迹”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“建立调用事实”检查第一条结构差异。围绕上下文敏感记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 12.2.5 SQL注入

“12.2.5 SQL注入”在本页不是孤立名词。先在“建立调用事实”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“选择上下文”检查第一条结构差异。围绕Datalog记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 12.2.6 缓冲区溢出

“12.2.6 缓冲区溢出”在本页不是孤立名词。先在“选择上下文”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“写出Datalog规则”检查第一条结构差异。围绕指向分析记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 12.3 数据流的逻辑表示

“12.3 数据流的逻辑表示”在本页不是孤立名词。先在“写出Datalog规则”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“求关系不动点”检查第一条结构差异。围绕二叉决策图记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 12.3.1 Datalog简介

“12.3.1 Datalog简介”在本页不是孤立名词。先在“求关系不动点”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“压缩为BDD”检查第一条结构差异。围绕调用图记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 12.3.2 Datalog规则

“12.3.2 Datalog规则”在本页不是孤立名词。先在“压缩为BDD”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“对照动态轨迹”检查第一条结构差异。围绕上下文敏感记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 12.3.3 内涵谓词和外延谓词

“12.3.3 内涵谓词和外延谓词”在本页不是孤立名词。先在“对照动态轨迹”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“建立调用事实”检查第一条结构差异。围绕Datalog记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 12.3.4 Datalog程序的执行

“12.3.4 Datalog程序的执行”在本页不是孤立名词。先在“建立调用事实”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“选择上下文”检查第一条结构差异。围绕指向分析记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 12.3.5 Datalog程序的增量求值

“12.3.5 Datalog程序的增量求值”在本页不是孤立名词。先在“选择上下文”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“写出Datalog规则”检查第一条结构差异。围绕二叉决策图记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 12.3.6 有问题的Datalog规则

“12.3.6 有问题的Datalog规则”在本页不是孤立名词。先在“写出Datalog规则”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“求关系不动点”检查第一条结构差异。围绕调用图记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 12.4 一个简单的指针分析算法

“12.4 一个简单的指针分析算法”在本页不是孤立名词。先在“求关系不动点”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“压缩为BDD”检查第一条结构差异。围绕上下文敏感记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 12.4.1 指针分析为什么困难

“12.4.1 指针分析为什么困难”在本页不是孤立名词。先在“压缩为BDD”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“对照动态轨迹”检查第一条结构差异。围绕Datalog记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 12.4.2 指针和引用的模型

“12.4.2 指针和引用的模型”在本页不是孤立名词。先在“对照动态轨迹”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“建立调用事实”检查第一条结构差异。围绕指向分析记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 12.4.3 流不敏感

“12.4.3 流不敏感”在本页不是孤立名词。先在“建立调用事实”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“选择上下文”检查第一条结构差异。围绕二叉决策图记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 12.4.4 使用Datalog的形式化

“12.4.4 使用Datalog的形式化”在本页不是孤立名词。先在“选择上下文”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“写出Datalog规则”检查第一条结构差异。围绕调用图记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 12.4.5 使用类型信息

“12.4.5 使用类型信息”在本页不是孤立名词。先在“写出Datalog规则”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“求关系不动点”检查第一条结构差异。围绕上下文敏感记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 12.5 上下文无关的过程间分析

“12.5 上下文无关的过程间分析”在本页不是孤立名词。先在“求关系不动点”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“压缩为BDD”检查第一条结构差异。围绕Datalog记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 12.5.1 方法调用的影响

“12.5.1 方法调用的影响”在本页不是孤立名词。先在“压缩为BDD”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“对照动态轨迹”检查第一条结构差异。围绕指向分析记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 12.5.2 使用Datalog发现调用图

“12.5.2 使用Datalog发现调用图”在本页不是孤立名词。先在“对照动态轨迹”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“建立调用事实”检查第一条结构差异。围绕二叉决策图记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 12.5.3 动态加载和反射

“12.5.3 动态加载和反射”在本页不是孤立名词。先在“建立调用事实”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“选择上下文”检查第一条结构差异。围绕调用图记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 12.6 上下文相关的指针分析

“12.6 上下文相关的指针分析”在本页不是孤立名词。先在“选择上下文”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“写出Datalog规则”检查第一条结构差异。围绕上下文敏感记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 12.6.1 上下文和调用串

“12.6.1 上下文和调用串”在本页不是孤立名词。先在“写出Datalog规则”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“求关系不动点”检查第一条结构差异。围绕Datalog记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 12.6.2 向Datalog规则中加入上下文

“12.6.2 向Datalog规则中加入上下文”在本页不是孤立名词。先在“求关系不动点”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“压缩为BDD”检查第一条结构差异。围绕指向分析记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 12.6.3 关于敏感性的进一步观察

“12.6.3 关于敏感性的进一步观察”在本页不是孤立名词。先在“压缩为BDD”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“对照动态轨迹”检查第一条结构差异。围绕二叉决策图记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 12.7 使用BDD实现Datalog

“12.7 使用BDD实现Datalog”在本页不是孤立名词。先在“对照动态轨迹”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“建立调用事实”检查第一条结构差异。围绕调用图记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 12.7.1 二叉决策图

“12.7.1 二叉决策图”在本页不是孤立名词。先在“建立调用事实”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“选择上下文”检查第一条结构差异。围绕上下文敏感记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 12.7.2 BDD上的变换

“12.7.2 BDD上的变换”在本页不是孤立名词。先在“选择上下文”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“写出Datalog规则”检查第一条结构差异。围绕Datalog记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 12.7.3 使用BDD表示关系

“12.7.3 使用BDD表示关系”在本页不是孤立名词。先在“写出Datalog规则”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“求关系不动点”检查第一条结构差异。围绕指向分析记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 12.7.4 将关系运算实现为BDD运算

“12.7.4 将关系运算实现为BDD运算”在本页不是孤立名词。先在“求关系不动点”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“压缩为BDD”检查第一条结构差异。围绕二叉决策图记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 12.7.5 使用BDD进行指向分析

“12.7.5 使用BDD进行指向分析”在本页不是孤立名词。先在“压缩为BDD”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“对照动态轨迹”检查第一条结构差异。围绕调用图记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

## 分步视觉验证

先预测当前阶段的树、图、集合、表项或指令，再运行变换；若不同，停在第一处分叉并保留原始证据。

<Stepper>
  <Step title="画出输入、变换与产物">
    标出调用图、上下文敏感、Datalog、指向分析、二叉决策图的输入、输出、所有权、算法前提和下游消费者。
    <Dbc12InterproceduralAnalysisMapLab />
  </Step>
  <Step title="运行单变量实验">
    固定源程序与目标模型，只改变一个文法、分析、优化、运行时或机器条件。
    <Dbc12InterproceduralAnalysisExperimentLab />
  </Step>
  <Step title="核对第一处分叉">
    比较结构化产物、复杂度和行为，清理旧输出后完整重建并重放回归样本。
    <Dbc12InterproceduralAnalysisEvidenceLab />
  </Step>
</Stepper>

## 常见误区

<Callout type="trap">
  **只看最终结果：**
  没有词法单元、语法树、属性依赖、CFG、数据流集合、调度表和寄存器映射，就无法把错误归因到具体阶段，也无法证明中间变换保持语义。
</Callout>

<Callout type="trap">
  **先变换再补条件：**
  优化、调度、并行化和过程间分析必须先证明前提；任何会令支配、活跃、别名、依赖或调用图失效的修改，都要触发明确重算。
</Callout>

## 四类样本与通过条件

| 样本 | 单一变量               | 必查证据         | 通过条件         |
| ---- | ---------------------- | ---------------- | ---------------- |
| 正常 | 一个最小语言或机器功能 | 全阶段快照       | 与预测一致       |
| 边界 | 深度、宽度、延迟或压力 | 临界分支和成本   | 边界可解释       |
| 失败 | 一个文法、事实或约束   | 首错与下游停止   | 不产出伪结果     |
| 优化 | 一个变换或策略         | 前后IR与差分执行 | 等价且收益可解释 |

## 练习

<Exercises>

**问题 1** 为什么不能只凭最终程序运行成功验收第12章 过程间分析？

<Answer>
  最终行为可能隐藏错误词法单元、树结构、属性值、数据流事实、依赖边或机器约束。应保存每层结构化产物，定位第一处分叉，并从固定源程序清理重建。
</Answer>

**问题 2** 如何把“写出Datalog规则”变成可回归实验？

<Answer>
  固定输入、实现版本、中间表示和目标模型，先预测调用图与上下文敏感的中间状态，只改变一个条件，并保留正常、边界、失败和优化四类样本。
</Answer>

**问题 3** 本页最小通过条件是什么？

<Answer>
  调用图与指向关系对实际行为保持保守，Datalog规则单调并到达不动点，上下文区分有明确界限，BDD运算与关系语义一致。
  修复后必须清理旧产物，完整重建全部下游结果，并让历史样本回到基线。
</Answer>

</Exercises>

## 小结

- 调用图：连接“建立调用事实”的算法前提、结构化产物、成本和回归证据。
- 上下文敏感：连接“选择上下文”的算法前提、结构化产物、成本和回归证据。
- Datalog：连接“写出Datalog规则”的算法前提、结构化产物、成本和回归证据。
- 指向分析：连接“求关系不动点”的算法前提、结构化产物、成本和回归证据。
- 二叉决策图：连接“压缩为BDD”的算法前提、结构化产物、成本和回归证据。
- 本页41个核心目录层级均已进入可追溯的编译证据链。

## 术语表

<Glossary>
  <GlossaryItem term="调用图">
    调用图用于解释“建立调用事实”，需要同时说明输入契约、算法前提、结构化产物、复杂度和清理重建方法。
  </GlossaryItem>
  <GlossaryItem term="上下文敏感">
    上下文敏感用于解释“选择上下文”，需要同时说明输入契约、算法前提、结构化产物、复杂度和清理重建方法。
  </GlossaryItem>
  <GlossaryItem term="Datalog">
    Datalog用于解释“写出Datalog规则”，需要同时说明输入契约、算法前提、结构化产物、复杂度和清理重建方法。
  </GlossaryItem>
  <GlossaryItem term="指向分析">
    指向分析用于解释“求关系不动点”，需要同时说明输入契约、算法前提、结构化产物、复杂度和清理重建方法。
  </GlossaryItem>
  <GlossaryItem term="二叉决策图">
    二叉决策图用于解释“压缩为BDD”，需要同时说明输入契约、算法前提、结构化产物、复杂度和清理重建方法。
  </GlossaryItem>
</Glossary>

<Attribution
  adaptedFrom="Alfred V. Aho、Monica S. Lam、Ravi Sethi、Jeffrey D. Ullman著，赵建华、郑滔、戴新宇译《编译原理（第2版）》，机械工业出版社，2009，631页，ISBN 9787111251217 · 基于Pearson官方第二版资料、中文版完整目录与高校图书馆元数据的独立教学重构"
  adaptedUrl="https://www.pearson.com/en-us/subject-catalog/p/compilers-principles-techniques-and-tools/P200000003472/9780133002140"
/>
