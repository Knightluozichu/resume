---
title: "目标代码生成"
type: C
section: "编译原理"
order: 6
description: "基本块、流图、寄存器分配与窥孔优化实战"
demo: true
math: false
sourceUrl: ""
draft: false
---

import { DbcCodeGenerationDiagram } from "@/components/mdx/dragon-book-compilers/diagrams/dbc-code-generation";
import { Objectives, Callout, Glossary, GlossaryItem, Term, Exercises, Answer } from "@/components/mdx/mdx-components";

<Objectives>

- 理解代码生成的流程：IR → 基本块 → 流图 → 寄存器分配 → 目标码
- 掌握基本块划分与流图构造方法
- 理解活跃变量分析与干扰图的构建
- 掌握图着色寄存器分配法与溢出处理
- 了解窥孔优化的常见技术

</Objectives>

## 代码生成的流程

代码生成（Code Generation）是编译器后端的核心任务，将优化后的中间表示（IR）转换为目标机器代码。流程如下：

```text
IR 指令 → 基本块划分 → 流图构建 → 寄存器分配 → 目标码
```

<DbcCodeGenerationDiagram />

## 基本块与流图

### 基本块（Basic Block）

连续的 IR 指令序列，**只有一个入口和一个出口**。

**首指令**的确定规则：
1. 程序的第一条指令
2. 任何跳转（条件/无条件）的目标指令
3. 紧跟在跳转指令后的指令

从首指令到下一条首指令前为一个基本块。基本块内没有跳转，顺序执行到底。

### 流图（Flow Graph）

以基本块为节点，加边反映控制流：
- B2 紧跟 B1 且 B1 末尾不是无条件跳转 → B1 → B2
- B1 末尾有跳转指向 B2 的首指令 → B1 → B2

流图是后续数据流分析和优化的基础结构。

<Callout type="info">

流图上的**循环识别**通过**回边**和**自然循环**完成：回边是流图中从后代指向祖先的边（通过支配关系判断），自然循环由回边确定入口的循环体。循环识别是循环优化的前提。

</Callout>

## 寄存器分配：图着色法

寄存器分配将无限虚拟寄存器映射到有限的物理寄存器，龙书的经典方法是**图着色法**。

### 三步流程

1. **活跃变量分析**（后向数据流）
   - `OUT[b] = ∪ IN[s]`（后继的 IN 并集）
   - `IN[b] = (OUT[b] - DEF[b]) ∪ USE[b]`
   - 找出在每个程序点哪些变量同时活跃

2. **构建干扰图**
   - 节点 = 变量
   - 若两个变量在某个程序点**同时活跃**，则连边（表示不能共用寄存器）

3. **k 着色**
   - 用 k 种颜色对图着色（k = 可用物理寄存器数）
   - 相邻节点不同色 = 不干扰 = 可共用寄存器
   - 无法着色 → **溢出**（spill）到内存

### 溢出处理

当干扰图无法 k 着色时（色数 > k），选择某些节点溢出：
- 为该变量分配栈空间
- 使用前 `load` 到临时寄存器，使用后 `store` 回内存
- 插入 load/store 后干扰图改变，可能变为可着色

<Callout type="tip">

图着色法的简化启发式：①简化（移除度数 < k 的节点，压栈）②溢出（度数 >= k 的节点标记溢出）③选择（弹栈着色，溢出节点分配内存）。合并（coalescing）可消除不必要的 move 指令。

</Callout>

## 窥孔优化

在目标代码上滑动一个小的「窥孔」窗口，用更高效的等价指令序列替换：

| 技术 | 优化前 | 优化后 |
|------|--------|--------|
| 冗余 load/store | `STORE R0,a; LOAD R0,a` | 删除 LOAD |
| 死代码 | 跳转后的不可达代码 | 删除 |
| 强度削弱 | `MUL R0,R1,1` | `MOV R0,R1` |
| 控制流 | `JMP L1; L1: JMP L2` | `JMP L2` |
| 代数简化 | `ADD R0,R0,0` | 删除 |

<Glossary>

<GlossaryItem term="基本块（Basic Block）">
连续的 IR 指令序列，只有一个入口（首指令）和一个出口（末指令），内无跳转，顺序执行到底。
</GlossaryItem>

<GlossaryItem term="流图（Flow Graph）">
以基本块为节点、控制流转移为边的图，是数据流分析和优化的基础结构。
</GlossaryItem>

<GlossaryItem term="活跃变量分析">
后向数据流分析，找出在每个程序点哪些变量的值在之后会被使用（活跃），用于死代码消除和寄存器分配。
</GlossaryItem>

<GlossaryItem term="干扰图">
寄存器分配的核心数据结构，节点=变量，同时活跃的变量连边（干扰），图的着色=寄存器分配方案。
</GlossaryItem>

<GlossaryItem term="溢出（Spill）">
干扰图无法 k 着色时，将某些变量分配到内存（栈），使用前 load、使用后 store，牺牲速度换取可行性。
</GlossaryItem>

<GlossaryItem term="窥孔优化">
在目标代码上滑动小窗口做局部替换的优化技术，包括冗余 load/store 消除、强度削弱、控制流简化等。
</GlossaryItem>

</Glossary>

<Exercises>

### 问题 1

代码生成阶段的输入和输出是什么？基本块和流图是如何构造的？

<Answer>

输入是优化后的中间表示（IR，如三地址码），输出是目标机器代码。基本块构造：①确定首指令——程序的第一条指令 / 任何跳转目标指令 / 跳转指令的下一条指令 ②从首指令到下一条首指令前为一个基本块。基本块是连续的 IR 序列，只有一个入口（首指令）和一个出口（末指令，通常是跳转）。流图构造：以基本块为节点，加边——如果 B2 紧跟 B1 且 B1 末尾不是无条件跳转，或有跳转从 B1 指向 B2 的首指令，则 B1→B2 有边。流图反映程序的控制流结构。

</Answer>

### 问题 2

寄存器分配的图着色法是如何工作的？什么是溢出？

<Answer>

图着色法三步：①活跃变量分析——后向数据流分析找出在每个程序点哪些变量同时活跃（OUT[b]=∪IN[s], IN[b]=(OUT-DEF)∪USE）②构建干扰图——节点=变量，若两个变量在某个程序点同时活跃则连边（表示不能共用寄存器）③k 着色——用 k 种颜色对图着色（k=可用物理寄存器数），相邻节点不同色。同色=可共用寄存器。溢出（Spill）：当图无法 k 着色时（色数 > k），选择某些节点溢出到内存——为该变量分配栈空间，使用前 load 到临时寄存器，使用后 store 回内存，然后重新分配。溢出后干扰图改变，可能变为可着色。

</Answer>

### 问题 3

代码生成中如何为表达式树选择最优指令序列？

<Answer>

使用树重写 + 动态规划（龙书 Ershov 算法）：①将 IR 表达式树与目标机器指令模式匹配 ②每条指令模式有一个代价（如周期数）③用动态规划求最优覆盖——自底向上计算每个子树的最小代价覆盖 ④选择代价最小的指令组合。关键约束：寄存器数量有限。Ershov 算法给每个节点标注需要的最少寄存器数（Sethi-Ullman 编号），当超过 k 个寄存器时需溢出到栈（插入 store/load）。这样生成的代码在给定寄存器数下是最优的。现代编译器（如 GCC、LLVM）用更复杂的指令选择器（如 BURS——自底向上重写系统）。

</Answer>

### 问题 4

什么是窥孔优化？列举几种常见的窥孔优化技术。

<Answer>

窥孔优化：在目标代码上滑动一个小的「窥孔」窗口（几条连续指令），用更高效的等价指令序列替换。常见技术：①冗余 load/store 消除——`STORE R0, a; LOAD R0, a` → 删除 LOAD（R0 已是 a 的值）②死代码消除——跳转后的不可达代码删除 ③强度削弱——`MUL R0, R1, 1` → `MOV R0, R1`（乘 1 用 move 替代）④控制流优化——`JMP L1; L1: JMP L2` → `JMP L2`（跳转跳转合并）⑤代数简化——`ADD R0, R0, 0` → 删除（加 0 无操作）。窥孔优化简单高效，是代码生成最后阶段的局部清理。

</Answer>

</Exercises>
