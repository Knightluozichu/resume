---
title: "《编译原理（第2版）》全书总复习"
type: C
section: "总复习 · 端到端编译证据"
order: 15
description: "《编译原理（第2版）》全书总复习逐项覆盖14个核心目录层级，以结构化中间产物、运行轨迹与失败对照建立可复查的编译实验。"
demo: true
math: false
sourceUrl: "https://www.pearson.com/en-us/subject-catalog/p/compilers-principles-techniques-and-tools/P200000003472/9780133002140"
draft: false
---

import {
  DbcOfficialFinalReviewMapLab,
  DbcOfficialFinalReviewExperimentLab,
  DbcOfficialFinalReviewEvidenceLab,
} from "@/components/mdx/dragon-book-compilers/diagrams/dbc-official-final-review";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释《编译原理（第2版）》全书总复习在完整编译流水线中的输入、输出、数据结构和责任边界
- 能描述本页14个核心目录层级之间的依赖、算法前提与工程取舍
- 能复现最小三地址代码或伪代码实验，保存正常、边界、失败和优化四类证据
- 能判断“前端和后端产物可追溯，所有分析到达稳定解，优化与调度保持语义，目标行为与源程序一致，失败时不消费过期事实。”是否成立，而不是只看最终程序是否运行

</Objectives>

## 为什么最终输出正确仍然不够

<Term def="前端合同连接“扫描固定样本”阶段的输入、算法前提、结构化结果、成本和第一处失败证据。">前端合同</Term>、<Term def="IR证据连接“保存AST与IR”阶段的输入、算法前提、结构化结果、成本和第一处失败证据。">IR证据</Term>、<Term def="数据流不动点连接“运行分析优化”阶段的输入、算法前提、结构化结果、成本和第一处失败证据。">数据流不动点</Term>、<Term def="目标机约束连接“调度分配指令”阶段的输入、算法前提、结构化结果、成本和第一处失败证据。">目标机约束</Term>、<Term def="端到端回归连接“比较机器行为”阶段的输入、算法前提、结构化结果、成本和第一处失败证据。">端到端回归</Term>
共同构成本页坐标。用同一组源程序贯通扫描、分析、翻译、运行时、优化、调度、并行化和过程间分析，定位第一处语义或约束分叉。

编译器是一串互相依赖的转换。错误的扫描器可能恰好产生可分析的词法单元，错误的语法树可能被后续语义动作意外修补，错误的数据流事实可能只在冷路径暴露，违反机器约束的调度也可能在当前样本中碰巧执行。因此必须保存阶段输入、输出和不变量，定位第一处偏离，不能把单个样例跑通当作证明。

## 核心模型与实验账本

实验固定源程序、编译器提交、中间表示格式、目标机模型、优化级别和随机种子。前端保存词法单元、推导、语法树、属性值与类型环境；中端保存基本块、控制流图、支配关系、数据流集合和每次变换前后的IR；后端保存模式覆盖、依赖图、发射周期、寄存器映射、溢出代码与最终指令。每份产物都带生成方式、退出状态和摘要值。

样本分成四组。正常样本证明主路径，边界样本覆盖空输入、深嵌套、二义性、不可归约流图、寄存器压力、长延迟、别名和动态调用；失败样本只破坏一个语法、类型、数据流或机器约束；优化样本同时保存变换前后版本，并用解释执行或差分执行核对可观察行为。

诊断记录阶段、源位置或IR位置、实际状态、期望状态、相关前驱和首要根因。某阶段失败后不得继续消费旧缓存伪造下游结果；分析事实被变换失效时，必须显式重算或证明仍然有效。性能结论与正确性结论分开：前者记录静态成本、动态计数、代码大小和运行环境，后者要求语义与全部约束成立。

目录项进入追溯矩阵后，至少关联一个输入、一个算法前提、一份结构化产物、一个失败对照和一个回归断言。跨章接口同时记录生产者和消费者，这样既能发现“提到概念却没有验证”的空洞覆盖，也能在实现变化后只重放真正受影响的实验。

## 六阶段证据链

### 1. 扫描固定样本

围绕前端合同先预测结构化结果，只改变一个条件并运行；进入“保存AST与IR”前，核对语义、算法前提、成本和首错位置。

### 2. 保存AST与IR

围绕IR证据先预测结构化结果，只改变一个条件并运行；进入“运行分析优化”前，核对语义、算法前提、成本和首错位置。

### 3. 运行分析优化

围绕数据流不动点先预测结构化结果，只改变一个条件并运行；进入“调度分配指令”前，核对语义、算法前提、成本和首错位置。

### 4. 调度分配指令

围绕目标机约束先预测结构化结果，只改变一个条件并运行；进入“比较机器行为”前，核对语义、算法前提、成本和首错位置。

### 5. 比较机器行为

围绕端到端回归先预测结构化结果，只改变一个条件并运行；进入“归档回归证据”前，核对语义、算法前提、成本和首错位置。

### 6. 归档回归证据

围绕前端合同先预测结构化结果，只改变一个条件并运行；进入“扫描固定样本”前，核对语义、算法前提、成本和首错位置。

## 可运行的最小骨架

```text
source -> tokens -> AST -> TAC -> optimized TAC -> target
```

```text
assert interpret(source) == simulate(target)
```

```text
archive { source, compiler-revision, target-model, every-stage-digest }
```

<Callout type="trap">
  **版本边界：**
  本页复原的是龙书第二版的算法模型和目录边界。现代LLVM、GCC、JVM或具体ISA可以用来做对照，但不能把其默认实现反写成原书算法的保证。
</Callout>

## 权威目录逐项讲解

### 第1章 引论

“第1章 引论”在本页不是孤立名词。先在“扫描固定样本”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“保存AST与IR”检查第一条结构差异。围绕前端合同记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 第2章 一个简单的语法制导翻译器

“第2章 一个简单的语法制导翻译器”在本页不是孤立名词。先在“保存AST与IR”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“运行分析优化”检查第一条结构差异。围绕IR证据记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 第3章 词法分析

“第3章 词法分析”在本页不是孤立名词。先在“运行分析优化”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“调度分配指令”检查第一条结构差异。围绕数据流不动点记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 第4章 语法分析

“第4章 语法分析”在本页不是孤立名词。先在“调度分配指令”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“比较机器行为”检查第一条结构差异。围绕目标机约束记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 第5章 语法制导翻译

“第5章 语法制导翻译”在本页不是孤立名词。先在“比较机器行为”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“归档回归证据”检查第一条结构差异。围绕端到端回归记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 第6章 中间代码生成

“第6章 中间代码生成”在本页不是孤立名词。先在“归档回归证据”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“扫描固定样本”检查第一条结构差异。围绕前端合同记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 第7章 运行时刻环境

“第7章 运行时刻环境”在本页不是孤立名词。先在“扫描固定样本”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“保存AST与IR”检查第一条结构差异。围绕IR证据记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 第8章 代码生成

“第8章 代码生成”在本页不是孤立名词。先在“保存AST与IR”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“运行分析优化”检查第一条结构差异。围绕数据流不动点记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 第9章 机器无关优化

“第9章 机器无关优化”在本页不是孤立名词。先在“运行分析优化”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“调度分配指令”检查第一条结构差异。围绕目标机约束记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 第10章 指令级并行性

“第10章 指令级并行性”在本页不是孤立名词。先在“调度分配指令”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“比较机器行为”检查第一条结构差异。围绕端到端回归记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 第11章 并行性和局部性的优化

“第11章 并行性和局部性的优化”在本页不是孤立名词。先在“比较机器行为”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“归档回归证据”检查第一条结构差异。围绕前端合同记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 第12章 过程间分析

“第12章 过程间分析”在本页不是孤立名词。先在“归档回归证据”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“扫描固定样本”检查第一条结构差异。围绕IR证据记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 附录A 一个完整的前端

“附录A 一个完整的前端”在本页不是孤立名词。先在“扫描固定样本”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“保存AST与IR”检查第一条结构差异。围绕数据流不动点记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

### 附录B 寻找线性无关解

“附录B 寻找线性无关解”在本页不是孤立名词。先在“保存AST与IR”阶段固定输入、文法或机器前提、核心数据结构与预期结果，再进入“运行分析优化”检查第一条结构差异。围绕目标机约束记录产生者、消费者和失效条件，避免后续阶段用偶然正确的输出掩盖上游错误。

验收这一目录项时同时检查语义、完备性、复杂度和失败边界：输出是否保持可观察行为，算法前提是否真的成立，时间与空间成本是否符合规模，以及错误能否停在最早可诊断位置。修复后清理旧产物，从固定输入完整重建并重放正常、边界、失败和优化四组样本。

## 分步视觉验证

先预测当前阶段的树、图、集合、表项或指令，再运行变换；若不同，停在第一处分叉并保留原始证据。

<Stepper>
  <Step title="画出输入、变换与产物">
    标出前端合同、IR证据、数据流不动点、目标机约束、端到端回归的输入、输出、所有权、算法前提和下游消费者。
    <DbcOfficialFinalReviewMapLab />
  </Step>
  <Step title="运行单变量实验">
    固定源程序与目标模型，只改变一个文法、分析、优化、运行时或机器条件。
    <DbcOfficialFinalReviewExperimentLab />
  </Step>
  <Step title="核对第一处分叉">
    比较结构化产物、复杂度和行为，清理旧输出后完整重建并重放回归样本。
    <DbcOfficialFinalReviewEvidenceLab />
  </Step>
</Stepper>

## 常见误区

<Callout type="trap">
  **只看最终结果：**
  没有词法单元、语法树、属性依赖、CFG、数据流集合、调度表和寄存器映射，就无法把错误归因到具体阶段，也无法证明中间变换保持语义。
</Callout>

<Callout type="trap">
  **先变换再补条件：**
  优化、调度、并行化和过程间分析必须先证明前提；任何会令支配、活跃、别名、依赖或调用图失效的修改，都要触发明确重算。
</Callout>

## 四类样本与通过条件

| 样本 | 单一变量               | 必查证据         | 通过条件         |
| ---- | ---------------------- | ---------------- | ---------------- |
| 正常 | 一个最小语言或机器功能 | 全阶段快照       | 与预测一致       |
| 边界 | 深度、宽度、延迟或压力 | 临界分支和成本   | 边界可解释       |
| 失败 | 一个文法、事实或约束   | 首错与下游停止   | 不产出伪结果     |
| 优化 | 一个变换或策略         | 前后IR与差分执行 | 等价且收益可解释 |

## 练习

<Exercises>

**问题 1** 为什么不能只凭最终程序运行成功验收《编译原理（第2版）》全书总复习？

<Answer>
  最终行为可能隐藏错误词法单元、树结构、属性值、数据流事实、依赖边或机器约束。应保存每层结构化产物，定位第一处分叉，并从固定源程序清理重建。
</Answer>

**问题 2** 如何把“运行分析优化”变成可回归实验？

<Answer>
  固定输入、实现版本、中间表示和目标模型，先预测前端合同与IR证据的中间状态，只改变一个条件，并保留正常、边界、失败和优化四类样本。
</Answer>

**问题 3** 本页最小通过条件是什么？

<Answer>
  前端和后端产物可追溯，所有分析到达稳定解，优化与调度保持语义，目标行为与源程序一致，失败时不消费过期事实。
  修复后必须清理旧产物，完整重建全部下游结果，并让历史样本回到基线。
</Answer>

</Exercises>

## 小结

- 前端合同：连接“扫描固定样本”的算法前提、结构化产物、成本和回归证据。
- IR证据：连接“保存AST与IR”的算法前提、结构化产物、成本和回归证据。
- 数据流不动点：连接“运行分析优化”的算法前提、结构化产物、成本和回归证据。
- 目标机约束：连接“调度分配指令”的算法前提、结构化产物、成本和回归证据。
- 端到端回归：连接“比较机器行为”的算法前提、结构化产物、成本和回归证据。
- 本页14个核心目录层级均已进入可追溯的编译证据链。

## 术语表

<Glossary>
  <GlossaryItem term="前端合同">
    前端合同用于解释“扫描固定样本”，需要同时说明输入契约、算法前提、结构化产物、复杂度和清理重建方法。
  </GlossaryItem>
  <GlossaryItem term="IR证据">
    IR证据用于解释“保存AST与IR”，需要同时说明输入契约、算法前提、结构化产物、复杂度和清理重建方法。
  </GlossaryItem>
  <GlossaryItem term="数据流不动点">
    数据流不动点用于解释“运行分析优化”，需要同时说明输入契约、算法前提、结构化产物、复杂度和清理重建方法。
  </GlossaryItem>
  <GlossaryItem term="目标机约束">
    目标机约束用于解释“调度分配指令”，需要同时说明输入契约、算法前提、结构化产物、复杂度和清理重建方法。
  </GlossaryItem>
  <GlossaryItem term="端到端回归">
    端到端回归用于解释“比较机器行为”，需要同时说明输入契约、算法前提、结构化产物、复杂度和清理重建方法。
  </GlossaryItem>
</Glossary>

<Attribution
  adaptedFrom="Alfred V. Aho、Monica S. Lam、Ravi Sethi、Jeffrey D. Ullman著，赵建华、郑滔、戴新宇译《编译原理（第2版）》，机械工业出版社，2009，631页，ISBN 9787111251217 · 基于Pearson官方第二版资料、中文版完整目录与高校图书馆元数据的独立教学重构"
  adaptedUrl="https://www.pearson.com/en-us/subject-catalog/p/compilers-principles-techniques-and-tools/P200000003472/9780133002140"
/>
