---
title: "第8章 Disjoint Sets：动态等价与近常数合并查询"
type: C
section: "排序、并查集与图"
order: 7
description: "按第3版七节完整重构等价关系、动态等价问题、并查集树表示、按秩合并、路径压缩、最坏界与Kruskal类应用。"
demo: true
math: true
sourceUrl: "https://www.informit.com/store/data-structures-and-algorithm-analysis-in-c-plus-plus-9780321441461"
draft: false
---

import { DsaEquivalenceRelationLab, DsaDynamicEquivalenceLab, DsaParentArrayLab, DsaUnionHeuristicLab, DsaPathCompressionLab, DsaInverseAckermannLab, DsaDisjointSetApplicationLab } from "@/components/mdx/dsa-cpp/diagrams/disjoint-set-class";
import { Objectives, Callout, Attribution, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step } from "@/components/mdx/mdx-components";

<Objectives>

- 能解释等价关系为何对应universe的唯一partition
- 能计算naive union、union-by-rank与path compression的tree shape和成本
- 能实现负值root编码、find、unionSets与压缩返回路径
- 能分析rank语义、root precondition和inverse-Ackermann amortized bound
- 能设计Kruskal、动态连通或离线分组中的并查集验证流程

</Objectives>

## 从“只合并，不拆分”的动态分区开始

等价关系（<Term def="同时满足reflexive、symmetric、transitive，并把universe划分为互不相交classes的relation。">等价关系</Term>）描述“属于同一组”。对所有元素 a、b、c：

$$
a\sim a,\qquad
a\sim b\Rightarrow b\sim a,\qquad
a\sim b\land b\sim c\Rightarrow a\sim c
$$

先预测已知0与1等价、1与2等价后，是否还要显式存储0与2的边。Transitivity 已经要求答案为 true；并查集不保存完整 relation matrix，只维护每个 equivalence class 的 representative。

官方七节依次为 **Equivalence Relations、The Dynamic Equivalence Problem、Basic Data Structure、Smart Union Algorithms、Path Compression、Worst Case for Union-by-Rank and Path Compression、An Application**。本章保持从数学 contract 到实现 heuristic，再到严格复杂度和应用的顺序。

<DsaEquivalenceRelationLab />

## 8.1 Equivalence Relations

一个 equivalence relation 唯一诱导 partition：每个 element 恰属于一个 class，不同 classes 不相交。反过来，任何 partition 也定义 relation：两元素等价当且仅当处于同一 block。

元素 x 的 equivalence class 定义为：

$$
[x]=\{y\mid y\sim x\}
$$

这解释了 API 为什么只需 `find(x)` 返回 representative，和 `union(a,b)` 合并两个 classes。Representative 的具体编号没有业务意义；不同 union order 可选择不同 root，但 `find(a)==find(b)` 的 partition semantics 应一致。

## 8.2 The Dynamic Equivalence Problem

动态等价问题（<Term def="从singleton classes开始，在线处理union与same-class find queries且classes只会合并的问题。">动态等价问题</Term>）包含两个 operations：

- `union(a,b)`：把包含 a、b 的两个 classes 合并；
- `find(x)`：返回 x 当前 class 的 name/root，据此判断两元素是否 connected。

<DsaDynamicEquivalenceLab />

它不是完全动态 connectivity：不支持删除 relation 或 split class。若业务需要断边、回滚或历史版本，需要 rollback DSU、offline divide-and-conquer 或 dynamic graph data structure，不能从 parent tree 中随意剪边。

## 8.3 Basic Data Structure

并查集基本数据结构（<Term def="以parent array把每个equivalence class表示为rooted tree，root充当representative。">并查集基本数据结构</Term>）初始化 N 棵 singleton trees。最直白版本令 `parent[i]=i`；第3版 `DisjSets` 用负值表示 root，并把负值幅度用于 rank：

```cpp
explicit DisjSets(int count) : parent(count, -1) {}

int find(int x) const {
    if (parent[x] < 0) return x;
    return find(parent[x]);
}
```

<DsaParentArrayLab />

不带 heuristic 时，`union(root1,root2)` 可简单让一个 root 指向另一个。N-1 次精心安排的 union 能造出 height N-1 chain，使 find O(N)。Array representation 紧凑，但性能完全取决于 tree shape。

官方实现明确假设 `unionSets` 收到两个 distinct roots，并省略 bounds/error checks。现代安全 wrapper 常先调用 find：

```cpp
bool unite(int a, int b) {
    int rootA = find(a);
    int rootB = find(b);
    if (rootA == rootB) return false;
    unionRoots(rootA, rootB);
    return true;
}
```

<Callout type="trap" title="把普通元素直接传给unionRoots">
若函数按root rank解释负值，却收到内部node，可能覆盖其parent、断开subtree或读错rank。API要么只暴露unite(a,b)并内部find，要么把root-only precondition写成不可误用的类型与断言。
</Callout>

## 8.4 Smart Union Algorithms

智能合并算法（<Term def="按size或rank把较浅较小tree挂到较深较大root，以限制height增长的union heuristic。">智能合并算法</Term>）有两种常见策略：

- union-by-size：smaller tree root 指向 larger tree root；
- union-by-rank：lower rank root 指向 higher rank root，rank 相等时任选新 root 并把 rank 加1。

<DsaUnionHeuristicLab />

若 rank 从0开始，rank 为 r 的 root 至少包含：

$$
2^r\ \text{elements}
$$

因为 rank 只在两棵相同 rank trees 合并时增加。因此 rank 至多 floor(log2 N)，单独使用 union-by-rank 就给出 O(log N) find。

第3版用 `s[root]` 的负数编码 rank：更负表示更深；相等时把新 root 再减1。这个值在加入 path compression 后不再等于真实 height，只是 monotonic upper-bound rank，不能在压缩后重新解释成精确树高。

## 8.5 Path Compression

路径压缩（<Term def="find递归返回时把搜索路径上的每个node直接挂到representative root。">路径压缩</Term>）把一次昂贵 find 的路径改短：

```cpp
int find(int x) {
    if (parent[x] < 0) return x;
    return parent[x] = find(parent[x]);
}
```

<DsaPathCompressionLab />

Assignment 必须接收 recursive find 的 root，而不是只改成 grandparent 后错误终止。Compression 不改变 partition，也不要求更新 rank：rank 用于未来 root-to-root union 决策，不是实时 height。

可以用 iterative two-pass 避免深递归：第一遍找 root，第二遍沿原路径改 parent。若输入可能来自不可信 serialized state，还应检测 out-of-range parent 和 cycles，否则 find 可能无限循环。

## 8.6 Worst Case for Union-by-Rank and Path Compression

按秩合并与路径压缩最坏情形（<Term def="分析两种heuristics组合后，一串m次operations只需O(m alpha(N)) amortized时间。">按秩合并与路径压缩最坏情形</Term>）不是简单把两个 O(log N) 相加。对 N 个 elements、m 次 make/find/union operations，经典 bound 为：

$$
O\bigl(m\,\alpha(N)\bigr)
$$

其中 alpha 是 inverse Ackermann function，增长极慢，在任何可实际存储的 N 上都小于很小常数。严格说它不是字面 O(1) worst-case per operation；这是 sequence-level amortized result。

<DsaInverseAckermannLab />

<Callout type="trap" title="把均摊近常数说成每次常数">
第一次find一条长路径仍可访问多个nodes；优势来自压缩让后续operations变快。报告复杂度时要写amortized，并保留operation sequence与heuristic前提。
</Callout>

只用 path compression 而任意 union，或只用 union-by-rank 而不 compression，bound 和常数都不同。测试要分别关闭 heuristics，构造 chain、balanced merges 和重复 find，证明优化真正生效。

## 8.7 An Application

并查集应用（<Term def="在Kruskal、maze generation或offline connectivity中快速判断并合并connected components。">并查集应用</Term>）的代表是 Kruskal minimum spanning tree：

1. 把 edges 按 weight 升序；
2. 若 `find(u)!=find(v)`，接受 edge 并 unite；
3. 若 roots 相同，拒绝 edge，因为它会在当前 forest 中成环；
4. 接受 V-1 条 edges 后结束。

<DsaDisjointSetApplicationLab />

```cpp
for (const Edge& edge : edgesByWeight) {
    if (sets.find(edge.u) == sets.find(edge.v))
        continue;
    tree.push_back(edge);
    sets.unite(edge.u, edge.v);
}
```

Sorting 成本 O(E log E)，DSU 部分约 O(E alpha(V))。若 graph 不连通，输出是 minimum spanning forest；不能在不足 V-1 条 edges 时仍宣称得到 spanning tree。

Randomized maze generation 也可把 cells 初始化为 singleton sets，随机选相邻墙；若两侧 roots 不同就拆墙并 union，直到全连通。DSU 保证不形成 cycle，从而生成 spanning tree maze。

## 统一验证：partition正确性优先于速度

<Stepper>
  <Step title="先验证relation contract">
    <DsaEquivalenceRelationLab />
    对每个element检查reflexive，对pairs检查symmetric，对triples抽查transitive。
  </Step>
  <Step title="每次union后核对partition">
    <DsaDynamicEquivalenceLab />
    与小规模reference partition比较same-set answers，不比较representative编号本身。
  </Step>
  <Step title="检查root与parent编码">
    <DsaParentArrayLab />
    每条parent chain必须终止于合法root，不能越界或成环。
  </Step>
  <Step title="分别启用两个heuristics">
    <DsaUnionHeuristicLab />
    构造worst-order unions，记录max depth与rank变化。
  </Step>
  <Step title="重复find验证压缩收益">
    <DsaPathCompressionLab />
    第一次记录visited nodes，第二次应直接或近直接抵达root。
  </Step>
</Stepper>

## 本章回顾

1. Equivalence relation与partition互相定义，representative编号本身不属于业务语义。
2. Dynamic equivalence只支持classes合并和find，不支持split/delete。
3. Parent array把classes表示成rooted trees；naive union可退化成linear chain。
4. Union-by-size/rank把较小较浅tree挂到较大较深root，限制height。
5. Path compression在find返回时重写整条路径，不改变partition。
6. Rank在压缩后不是精确height。
7. 两种heuristics组合给出O(m alpha(N)) amortized sequence bound。
8. Kruskal用find拒绝成环edge，用union维护forest components。

<Exercises>

**问题 1：为何“同余mod 3”是等价关系，它有哪些classes？**

<Answer>它满足自反、对称和传递。整数按余数分成三个classes：余0、余1、余2；任意整数恰属于一个class，不同classes不相交。</Answer>

**问题 2：union-by-rank中rank为何只在两个相同rank roots合并时增加？**

<Answer>低rank tree挂到高rank tree不会增加最长root-to-leaf bound；相同rank合并时，一棵成为另一棵child，最长bound才增加1。该规则也保证rank r root至少含2^r elements。</Answer>

**问题 3：path compression后为什么不把root rank改成真实height？**

<Answer>Rank是union heuristic维护的单调upper-bound标签，复杂度证明依赖其规则；压缩会降低真实height但无需扫描整棵tree。保持rank不变仍安全，只可能保守，不影响partition或bound。</Answer>

**问题 4：Kruskal看到edge两端find结果相同时为什么拒绝？**

<Answer>两端已经由已接受edges连通，再加入该edge会闭合cycle。拒绝它保持forest；不同roots时加入edge连接两个components且不成环。</Answer>

</Exercises>

## 名词解释

<Glossary>
  <GlossaryItem term="等价关系">满足自反、对称、传递并诱导partition的relation。</GlossaryItem>
  <GlossaryItem term="动态等价问题">在线处理只合并classes与find queries的问题。</GlossaryItem>
  <GlossaryItem term="并查集基本数据结构">用parent array rooted forests表示partition。</GlossaryItem>
  <GlossaryItem term="智能合并算法">按size或rank限制union后tree height的策略。</GlossaryItem>
  <GlossaryItem term="路径压缩">find返回时把访问nodes直接挂到root。</GlossaryItem>
  <GlossaryItem term="按秩合并与路径压缩最坏情形">组合heuristics的inverse-Ackermann均摊分析。</GlossaryItem>
  <GlossaryItem term="并查集应用">Kruskal、maze或dynamic connectivity中的component维护。</GlossaryItem>
</Glossary>

<Attribution
  name="Data Structures and Algorithm Analysis in C++, 3rd Edition · Chapter 8 The Disjoint Set Class"
  href="https://www.informit.com/store/data-structures-and-algorithm-analysis-in-c-plus-plus-9780321441461"
  author="Mark Allen Weiss"
  license="原书与官方示例代码版权归作者及 Pearson/Addison-Wesley 所有；本章依据官方完整目录及作者公开的第3版 DisjSets.h/DisjSets.cpp 示例独立重构教学内容"
/>
