---
title: "Item 18：让接口容易被正确使用，不易被误用"
type: A
section: Chapter 4 · Designs and Declarations
order: 18
description: 对齐 Effective C++ 第三版 Item 18：用强类型、受限值、一致约定和 typed owner 把正确用法变成默认路径，并封闭参数错位、无效状态与跨 DLL 错误释放。
demo: false
math: false
sourceUrl: "https://www.informit.com/store/effective-c-plus-plus-55-specific-ways-to-improve-your-9780321334879"
draft: false
---

import { Objectives } from "@/components/mdx/objectives";
import { Term } from "@/components/mdx/term";
import { Callout } from "@/components/mdx/callout";
import { Attribution } from "@/components/mdx/attribution";
import { Glossary, GlossaryItem } from "@/components/mdx/glossary";
import { Answer, Exercises } from "@/components/mdx/exercises";

<Objectives>

- 能解释同类型参数、无效值、约定不一致和裸资源责任为什么会让接口易被误用
- 能设计 strong types、受限 factory 与一致命名，使错误调用在编译期或系统边界失败
- 能实现跨 DLL typed owner 和配套失败测试，保证创建与释放始终回到同一模块

</Objectives>

## 从调用者的失败开始设计

接口设计不能只问“实现者能否完成工作”，还要问“调用者最自然写出的代码是否正确”。下面的构造器可以编译，却把三种不同概念都压成了 `int`：

```cpp
class Date {
public:
    Date(int month, int day, int year);
};

Date release(30, 2, 2026); // 次序和取值都错，但类型系统沉默
```

<Term def="调用形式看似合理、能够编译，却表达了错误语义或违反领域规则的用法。">interface misuse</Term>

参数名只对阅读声明的人有帮助，调用点只剩三个整数。注释、文档和 code review 可以降低概率，却不能封闭错误路径。

<Term def="调用者为了完成常见正确操作所需记忆、检查和手工配对的总负担。">caller burden</Term>

Item 18 的原始原则是：Make interfaces easy to use correctly and hard to use incorrectly（让接口容易被正确使用，不易被误用）。目标不是让所有错误不可能发生，而是让正确路径阻力最小，让高频误用尽早失败。


## 把不同概念变成不同类型

`month`、`day`、`year` 都能用整数存储，不代表它们应该共享同一个接口类型。

<Term def="为领域概念建立独立类型，使语义不同但底层表示相同的值不能被意外互换。">strong type</Term>

```cpp
class Day {
public:
    explicit Day(unsigned value);
    unsigned value() const noexcept;
private:
    unsigned value_;
};

class Year {
public:
    explicit Year(int value) : value_(value) {}
private:
    int value_;
};

class Date {
public:
    Date(Month month, Day day, Year year);
};
```

现在 `Date{Day{30}, Month::Feb(), Year{2026}}` 因参数类型错位不能编译。strong types（强类型）把“记住参数顺序”改造成 compiler obligation。

<Term def="由编译器根据参数和返回类型强制检查、调用者无需靠记忆维持的契约。">compiler-enforced contract</Term>

强类型应保持轻量：值语义、明确构造、便宜复制，并只暴露领域需要的操作。若类型只是给 `int` 换名字但仍允许任意隐式转换，防线并未建立。

<Callout type="trap">
  **现象**：给参数增加 typedef 后认为已经安全。**原因**：多个 typedef
  仍是同一底层类型，编译器允许互换。**修法**：使用互不转换的 class/struct
  wrapper，并让原始值转换保持 explicit。
</Callout>

## 让无效值难以构造

类型不同只解决参数互换，`Month{37}` 仍可能制造无效状态。接口还应控制合法值的产生方式。

<Term def="对象一旦成功构造，在整个可观察生命周期内都必须保持成立的领域条件。">class invariant</Term>

```cpp
class Month {
public:
    static Month Jan() noexcept { return Month{1}; }
    static Month Feb() noexcept { return Month{2}; }
    static std::optional<Month> fromInt(unsigned value) noexcept;

    unsigned value() const noexcept { return value_; }

private:
    explicit Month(unsigned value) : value_(value) {}
    unsigned value_;
};
```

固定集合可使用 named factory 或 enum class；外部输入走 `fromInt`，失败以 `optional`、`expected` 或项目统一错误类型表达。成功得到 Month 后，内部函数不应反复检查 1 到 12。

<Term def="只有验证成功才发布领域对象，把任意原始输入留在系统边缘的构造入口。">validating factory</Term>


日期还存在“二月没有 30 日”这种跨字段约束。可以让 `Date::create` 集中验证，而不是让每个业务函数猜测对象是否合法。

<Term def="只有在所有组成字段联合满足规则后才允许产生对象的构造策略。">aggregate validation</Term>

## 正确路径必须比逃生口更顺手

如果安全 API 需要五步配置，而危险 API 只需一行，调用者迟早会选危险入口。安全路径应成为默认、简短且可发现的入口。

```cpp
auto request = Request::json(endpoint, payload)
                   .withTimeout(Seconds{2});

client.send(request); // request 已满足发送不变量
```

<Term def="无需额外标志、特殊顺序或调用者知识即可完成常见安全操作的主入口。">safe default path</Term>

高级逃生口可以存在，但应在名称和类型上明确，例如 `unsafeFromUncheckedBytes`，并限制到基础设施层。不要让普通调用和绕过验证共享同名重载。

<Term def="为少数底层场景显式暴露、名称清楚且作用域受控的非默认低层入口。">explicit escape hatch</Term>

## 一致性减少接口猜测

原书强调 consistency（一致性）。用户已经熟悉标准库和项目中的既有习惯，新的接口应复用这些习惯，而不是创造局部方言。

<Term def="同类操作在命名、参数顺序、返回值、错误处理和生命周期规则上遵循相同约定。">interface consistency</Term>

若容器普遍使用 `size()`、`empty()` 和半开区间，新容器就不要改叫 `count()`、`isEmpty()` 并使用闭区间。若项目 factory 统一返回 typed owner，新 factory 就不要返回 raw pointer。差异必须来自语义差异，而不是作者偏好。

```cpp
for (auto it = values.begin(); it != values.end(); ++it) {
    consume(*it);
}

if (!values.empty()) {
    reserve(values.size());
}
```

<Term def="调用者可把已有库和项目经验迁移到新接口，而无需重新学习局部规则。">convention transfer</Term>

一致性不是机械模仿。若操作可能失败，不能为了看起来像 `vector::operator[]` 而隐藏错误；应选择符合真实语义且全项目统一的错误模型。

## 返回类型要携带下一步责任

函数返回值不只传数据，也应表达调用者必须做什么。返回 raw pointer 会同时留下多个问题：能否为空、谁拥有、如何释放、能否跨模块删除。

<Term def="通过返回类型直接编码 ownership、可空性、错误和释放方式的接口设计。">responsibility-bearing return type</Term>

```cpp
std::unique_ptr<Document> openDocument(Path path);
std::optional<UserId> findUser(Name name);
Result<Config, ParseError> parseConfig(TextView text);
```

unique_ptr 表示单一 ownership；optional 表示“无结果不是错误”；Result 区分成功值与失败原因。调用者不需要另查文档判断返回值责任。

<Term def="创建函数在成功返回前已经把资源绑定到正确 owner 和释放策略。">ownership-complete factory</Term>

返回类型还应阻止忽略关键结果。可用 `[[nodiscard]]` 标记错误或事务提交结果，但它只是提醒，不能替代清晰的类型和生命周期模型。

## 防止裸资源协议泄漏到业务层

外部 API 常给出 `acquire/release`、`open/close` 或 handle。包装层应一次完成配对，业务层只看到 owner 与 borrow。

```cpp
struct FileCloser {
    void operator()(std::FILE* file) const noexcept {
        if (file) std::fclose(file);
    }
};

using FileOwner = std::unique_ptr<std::FILE, FileCloser>;

FileOwner openFile(const char* path) {
    FileOwner file{std::fopen(path, "rb")};
    if (!file) throw FileOpenError{path};
    return file;
}
```

<Term def="资源的创建函数与唯一正确释放函数组成的不可拆分协议。">acquire-release pair</Term>

调用者不能把 FileOwner 误传给普通 `delete`，也无需记住 `fclose`。这正是“接口不易被误用”：把协议放进类型，而不是散落在调用点。

## 跨 DLL 的 new/delete 边界

跨 DLL new delete（cross-dll new delete）尤其危险。动态库和应用可能使用不同 CRT、allocator、编译选项或 class-specific delete。对象在库内 `new`，却在应用侧直接 `delete`，可能造成 heap corruption，即使类型声明完全一致。

<Term def="资源必须由创建它的动态模块使用同一 allocator 和 ABI 配套释放的边界规则。">module-local deallocation</Term>

库应同时导出 create/destroy，并把 destroy 固化进 owner：

```cpp
// library API
extern "C" ApiObject* create_api_object();
extern "C" void destroy_api_object(ApiObject*) noexcept;

struct ApiDeleter {
    void operator()(ApiObject* object) const noexcept {
        destroy_api_object(object);
    }
};

using ApiOwner = std::unique_ptr<ApiObject, ApiDeleter>;

ApiOwner makeApiObject() {
    return ApiOwner{create_api_object()};
}
```

<Term def="携带创建模块提供的 destroy 回调，使 owner 析构时回到原模块释放的删除策略。">module-bound deleter</Term>


更稳定的 ABI 可以导出 opaque handle 和 C 函数，而不让 C++ class layout、异常或 STL 类型穿过模块边界。关键不是选哪种语法，而是应用永远没有“直接 delete 库对象”这条自然路径。

<Term def="调用方看不到对象布局，只通过稳定函数表或 C API 操作的跨模块句柄。">opaque handle</Term>

<Callout type="trap">
  **现象**：库返回 raw pointer，并在文档写“必须调用 destroy”。**原因**：最顺手的
  `delete` 仍可编译，错误释放直到线上堆损坏才暴露。**修法**：返回或立即构造带
  module-bound deleter 的 typed owner，业务函数只接受 borrow。
</Callout>

## 错误模型也属于接口一致性

同一层 API 不应有的函数抛异常、有的返回 bool、有的写 thread-local error。混合模型迫使调用者为每个函数记忆特殊规则，最终出现漏检。

<Term def="同一抽象层对可恢复失败、编程错误和不可恢复故障采用稳定一致的表达方式。">error-model consistency</Term>

选择异常、Result 或 status code 要结合边界和性能，但需要统一：

- 构造成功即满足 invariant，失败不发布半成品对象。
- 可恢复失败携带可诊断信息，不以 magic value 混入正常值域。
- destructor、deleter 和 rollback 路径保持 noexcept。
- C ABI 边界拦截异常，转换为该边界约定的错误类型。

<Term def="构造或操作失败时不向调用者暴露只完成部分初始化、责任不明确的对象。">no-partial-publication rule</Term>

## 兼容旧接口时建立迁移通道

改善接口常遇到旧调用点。不要同时长期维护两套等价但语义不同的入口，否则一致性继续恶化。

```cpp
[[deprecated("use Date::create(Month, Day, Year)")]]
Date legacyDate(int month, int day, int year);
```

<Term def="通过适配、弃用诊断、调用点清单和删除期限，把用户从危险入口迁往安全入口。">interface migration path</Term>

迁移可以先让旧函数内部调用新实现，再批量改调用点；CI 把新增 deprecated 使用视为失败；到约定版本删除旧入口。adapter 只负责兼容，不应成为永久双轨设计。

## 用负例证明接口真的难以误用

普通单元测试只证明正确调用能工作，Item 18 还需要验证错误调用无法穿过边界。

<Term def="刻意编写应当无法编译、无法构造或必须返回错误的调用，用来验证误用防线。">misuse-oriented test</Term>

先预测每个错误在哪一层失败，再建立测试矩阵：

- 编译期负例：Day 与 Month 交换、raw int 隐式转换、复制 unique owner。
- 边界值：Month 0/1/12/13，闰年日期，联合字段非法组合。
- ownership ledger：每次 create 都有且仅有一次 destroy，失败路径不发布 handle。
- DLL 集成：真实发布配置创建、移动 owner、借用、销毁，释放回调地址来自库模块。
- API consistency：静态检查命名、返回类型和 deprecated 新增使用。
- fuzz：随机 wire input 只能得到合法领域对象或明确错误，不能产生半有效状态。

<Term def="记录资源创建模块、owner 转移、借用和最终释放模块的可核查事件序列。">module ownership trace</Term>

测试目标不是覆盖实现行数，而是证明不变量从输入边界一直延续到释放边界。

## 小结

- Item 18 要求接口容易被正确使用、不易被误用，设计从调用者的自然路径出发
- strong types 阻止同表示不同语义的参数互换，validating factory 阻止无效值进入核心
- consistency 让标准库和项目经验可迁移，减少每个调用点的特殊记忆
- 返回类型应表达 ownership、可空性和错误，factory 在返回前完成资源绑定
- 跨 DLL 对象由创建模块提供 destroy，typed owner 携带 module-bound deleter
- misuse-oriented test 同时验证编译期负例、边界输入和 create/destroy 配对

<Attribution
  adaptedFrom="Effective C++, Third Edition, Item 18"
  adaptedUrl="https://www.informit.com/store/effective-c-plus-plus-55-specific-ways-to-improve-your-9780321334879"
/>

<Glossary>
  <GlossaryItem term="interface misuse">
    能编译但表达错误语义或违反领域规则的调用。
  </GlossaryItem>
  <GlossaryItem term="caller burden">
    调用者完成正确操作所需承担的记忆和配对负担。
  </GlossaryItem>
  <GlossaryItem term="strong type">
    让不同领域概念不能因底层表示相同而互换的独立类型。
  </GlossaryItem>
  <GlossaryItem term="compiler-enforced contract">
    由类型检查直接执行的接口契约。
  </GlossaryItem>
  <GlossaryItem term="class invariant">
    对象成功构造后始终必须成立的条件。
  </GlossaryItem>
  <GlossaryItem term="validating factory">
    验证外部输入后才发布合法领域对象的入口。
  </GlossaryItem>
  <GlossaryItem term="aggregate validation">
    联合检查多个字段后才构造对象的策略。
  </GlossaryItem>
  <GlossaryItem term="safe default path">
    完成常见安全操作时最直接、最可发现的接口路径。
  </GlossaryItem>
  <GlossaryItem term="explicit escape hatch">
    名称明确、范围受控的低层非默认入口。
  </GlossaryItem>
  <GlossaryItem term="interface consistency">
    同类接口沿用相同命名、参数、错误和生命周期约定。
  </GlossaryItem>
  <GlossaryItem term="convention transfer">
    把既有库和项目经验直接迁移到新接口。
  </GlossaryItem>
  <GlossaryItem term="responsibility-bearing return type">
    编码 ownership、错误或可空性的返回类型。
  </GlossaryItem>
  <GlossaryItem term="ownership-complete factory">
    返回前已完成资源与正确 owner 绑定的创建函数。
  </GlossaryItem>
  <GlossaryItem term="acquire-release pair">
    资源唯一正确的创建与释放函数组合。
  </GlossaryItem>
  <GlossaryItem term="module-local deallocation">
    资源回到创建模块用同一 allocator 释放的规则。
  </GlossaryItem>
  <GlossaryItem term="module-bound deleter">
    调用创建模块 destroy 函数的 owner 删除策略。
  </GlossaryItem>
  <GlossaryItem term="opaque handle">
    隐藏布局、只通过稳定函数操作的跨模块句柄。
  </GlossaryItem>
  <GlossaryItem term="error-model consistency">
    同一抽象层稳定一致的失败表达约定。
  </GlossaryItem>
  <GlossaryItem term="no-partial-publication rule">
    失败时不发布半初始化对象的规则。
  </GlossaryItem>
  <GlossaryItem term="interface migration path">
    从危险旧入口迁往安全新入口的受控通道。
  </GlossaryItem>
  <GlossaryItem term="misuse-oriented test">
    验证错误调用不能越过接口防线的负例测试。
  </GlossaryItem>
  <GlossaryItem term="module ownership trace">
    记录创建、转移、借用和释放模块的事件序列。
  </GlossaryItem>
</Glossary>

<Exercises>
  1. **问题 1：重构日期接口。** 现有 `Date(int, int, int)` 被多处调用，请设计强类型、联合验证和迁移步骤。

<Answer question="怎样让日期接口的参数错位和无效值尽早失败？">
  建立互不隐式转换的 `Month`、`Day`、`Year`；有限 Month 由 named factory 或验证
  factory 产生；`Date::create` 联合检查天数与闰年。旧入口标记 deprecated
  并转调新实现，CI
  禁止新增旧调用，按清单迁移后删除。编译负例验证参数交换失败，边界测试覆盖非法组合。
</Answer>

2. **问题 2：统一三个查询接口。** 一个返回 raw pointer，一个返回 null，一个以 bool 加输出参数报告失败，请设计一致返回模型。

  <Answer question="查询接口怎样同时表达无结果、失败和 ownership？">先区分语义：无结果是正常分支时返回 `optional<Value>`；可恢复失败需原因时返回 `Result<Value, Error>`；创建并转移资源时返回 typed unique owner 或 `Result<Owner, Error>`。同层接口统一命名和错误传播，移除输出参数与 magic null，调用者从返回类型直接知道检查和释放责任。</Answer>

3. **问题 3：修复插件 DLL 工厂。** 插件导出 `ApiObject* create()`，主程序直接 delete，偶发 heap corruption，请设计兼容方案和验证。

  <Answer question="跨 DLL 对象怎样保证在创建模块释放？">插件同时导出 noexcept `destroy(ApiObject*)`；加载层立即把 create 结果放进携带 destroy 函数的 typed owner，业务只接收 reference/pointer borrow，不获得 raw delete 路径。兼容期旧入口由 adapter 接管并告警。真实发布 DLL 集成测试记录 create/destroy 模块、移动 owner、创建失败与卸载顺序，确保每个成功 create 恰有一次同模块 destroy。</Answer>
</Exercises>
