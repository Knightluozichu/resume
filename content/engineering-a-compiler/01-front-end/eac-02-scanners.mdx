---
title: "第2章 词法分析器"
type: C
section: "第一部分 · 编译器前端"
order: 2
description: "第2章 词法分析器逐项覆盖25个正式目录层级，以ILOC、IR快照、分析集合、目标指令与运行轨迹建立可复查的编译器工程实验。"
demo: true
math: false
sourceUrl: "https://shop.elsevier.com/books/engineering-a-compiler/cooper/978-0-12-088478-0"
draft: false
---

import {
  Eac02ScannersMapLab,
  Eac02ScannersExperimentLab,
  Eac02ScannersEvidenceLab,
} from "@/components/mdx/engineering-a-compiler/diagrams/eac-02-scanners";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第2章 词法分析器在源码到目标代码流水线中的输入、输出、数据结构和责任边界
- 能描述本页25个正式目录层级之间的依赖、数据结构与工程取舍
- 能复现最小ILOC或伪代码实验，保存正常、边界、失败和优化四类证据
- 能判断“每个输入字符只被消费一次或进入明确错误状态，接受状态保留token优先级，最小化前后识别语言完全相同。”是否成立，而不是只看最终程序是否运行

</Objectives>

## 为什么最终输出正确仍然不够

<Term def="正则表达式连接定义词素阶段的算法前提、结构化产物、成本与首个失败位置。">正则表达式</Term>、<Term def="NFA连接构造Thompson NFA阶段的算法前提、结构化产物、成本与首个失败位置。">NFA</Term>、<Term def="DFA连接执行子集构造阶段的算法前提、结构化产物、成本与首个失败位置。">DFA</Term>、<Term def="Hopcroft算法连接运行Hopcroft最小化阶段的算法前提、结构化产物、成本与首个失败位置。">Hopcroft算法</Term>、<Term def="最长匹配连接编码扫描器阶段的算法前提、结构化产物、成本与首个失败位置。">最长匹配</Term>
共同构成本页坐标。把正则表达式逐步构造成NFA、DFA和最小识别器，再比较表驱动、直接编码和手写扫描器的正确性与成本。

编译器是一串互相依赖的转换。错误的扫描器可能恰好产生可解析token，错误的语法树可能在语义动作中被修补，错误的数据流事实可能只在冷路径触发，错误的指令调度也可能在当前处理器上没有暴露。因此必须逐层保存输入、输出和不变量，定位最早偏离，而不能把“样例跑通”当作完整证明。

## 可复查的编译工程台账

实验固定源程序、编译器提交、ILOC方言、目标机模型、优化级别与随机种子。前端保存token、推导、AST与类型环境；中端保存基本块、CFG、支配关系、数据流集合、SSA和每个pass的前后IR；后端保存模式覆盖、依赖DAG、寄存器映射、逐出代码和最终指令。每份产物都带生成命令、退出状态与摘要值。

样本分为四组。正常样本证明主路径；边界样本覆盖空输入、深嵌套、临界立即数、不可归约控制流、寄存器压力和长延迟；失败样本只破坏一个语法、类型、数据流或机器约束；优化样本则同时保存未优化与优化版本，并用解释器或差分执行核对可观察行为。

诊断必须包含阶段、源位置或IR位置、实际状态、期望状态、相关前驱和首要根因。某一阶段失败后，不得继续消费旧缓存伪造下游结果。修改算法后先清理生成目录，再重建全部受影响事实；若一个pass令支配、活跃或别名信息失效，就必须显式重算或证明仍然有效。

性能结论与正确性结论分开记录。正确性要求语义等价和机器约束成立；性能要求给出静态成本、动态计数、代码大小、编译时间与硬件环境。一次偶然加速不能证明算法更优，一次慢化也不必然否定变换，关键是解释成本模型与实际瓶颈为何一致或偏离。

正式目录项映射到可追溯矩阵：每项至少关联一个输入、一个算法前提、一份结构化产物、一个失败对照和一个回归断言。跨章接口同时记录生产者与消费者，因而能发现“提到概念却没有验证”的空洞覆盖，也能在实现变化后只重放受影响的实验。

## 六阶段证据链

### 1. 定义词素

围绕正则表达式预测输入和结构化结果，执行后比较第一条差分。进入“构造Thompson NFA”前，确认语义、算法前提与成本记录均完整。

### 2. 构造Thompson NFA

围绕NFA预测输入和结构化结果，执行后比较第一条差分。进入“执行子集构造”前，确认语义、算法前提与成本记录均完整。

### 3. 执行子集构造

围绕DFA预测输入和结构化结果，执行后比较第一条差分。进入“运行Hopcroft最小化”前，确认语义、算法前提与成本记录均完整。

### 4. 运行Hopcroft最小化

围绕Hopcroft算法预测输入和结构化结果，执行后比较第一条差分。进入“编码扫描器”前，确认语义、算法前提与成本记录均完整。

### 5. 编码扫描器

围绕最长匹配预测输入和结构化结果，执行后比较第一条差分。进入“验证最长匹配”前，确认语义、算法前提与成本记录均完整。

### 6. 验证最长匹配

围绕正则表达式预测输入和结构化结果，执行后比较第一条差分。进入“定义词素”前，确认语义、算法前提与成本记录均完整。

## 可运行骨架

```text
identifier = [A-Za-z_][A-Za-z0-9_]*
integer    = [0-9]+
```

```pseudo
D0 = epsilonClosure(start)
for each set D and character c:
  next = epsilonClosure(move(D, c))
```

```text
ifx -> IDENT(ifx)
if( -> IF, LPAREN
12e -> INT(12), IDENT(e) or lexical error by policy
```

<Callout type="trap">
  **版本边界：**
  原书使用ILOC作为教学IR，并以当时的处理器与优化器模型讲解算法。现代LLVM、GCC或具体ISA可以作为对照，但不能把其默认行为反写成原书算法的保证。
</Callout>

## 正式目录逐项讲解

### 第2章 词法分析器

“第2章 词法分析器”不能只作为名词记忆。本节先在“定义词素”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“构造Thompson NFA”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### 2.1 简介

“2.1 简介”不能只作为名词记忆。本节先在“构造Thompson NFA”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“执行子集构造”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### 2.2 识别单词

“2.2 识别单词”不能只作为名词记忆。本节先在“执行子集构造”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“运行Hopcroft最小化”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### 2.2.1 识别器的形式化

“2.2.1 识别器的形式化”不能只作为名词记忆。本节先在“运行Hopcroft最小化”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“编码扫描器”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### 2.2.2 识别更复杂的单词

“2.2.2 识别更复杂的单词”不能只作为名词记忆。本节先在“编码扫描器”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“验证最长匹配”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### 2.3 正则表达式

“2.3 正则表达式”不能只作为名词记忆。本节先在“验证最长匹配”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“定义词素”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### 2.3.1 符号表示法的形式化

“2.3.1 符号表示法的形式化”不能只作为名词记忆。本节先在“定义词素”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“构造Thompson NFA”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### 2.3.2 示例

“2.3.2 示例”不能只作为名词记忆。本节先在“构造Thompson NFA”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“执行子集构造”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### 2.3.3 RE的闭包性质

“2.3.3 RE的闭包性质”不能只作为名词记忆。本节先在“执行子集构造”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“运行Hopcroft最小化”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### 2.4 从正则表达式到词法分析器

“2.4 从正则表达式到词法分析器”不能只作为名词记忆。本节先在“运行Hopcroft最小化”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“编码扫描器”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### 2.4.1 非确定性有限自动机

“2.4.1 非确定性有限自动机”不能只作为名词记忆。本节先在“编码扫描器”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“验证最长匹配”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### 2.4.2 从正则表达式到NFA：Thompson构造法

“2.4.2 从正则表达式到NFA：Thompson构造法”不能只作为名词记忆。本节先在“验证最长匹配”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“定义词素”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### 2.4.3 从NFA到DFA：子集构造法

“2.4.3 从NFA到DFA：子集构造法”不能只作为名词记忆。本节先在“定义词素”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“构造Thompson NFA”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### 2.4.4 从DFA到最小DFA：Hopcroft算法

“2.4.4 从DFA到最小DFA：Hopcroft算法”不能只作为名词记忆。本节先在“构造Thompson NFA”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“执行子集构造”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### 2.4.5 将DFA用做识别器

“2.4.5 将DFA用做识别器”不能只作为名词记忆。本节先在“执行子集构造”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“运行Hopcroft最小化”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### 2.5 实现词法分析器

“2.5 实现词法分析器”不能只作为名词记忆。本节先在“运行Hopcroft最小化”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“编码扫描器”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### 2.5.1 表驱动词法分析器

“2.5.1 表驱动词法分析器”不能只作为名词记忆。本节先在“编码扫描器”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“验证最长匹配”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### 2.5.2 直接编码的词法分析器

“2.5.2 直接编码的词法分析器”不能只作为名词记忆。本节先在“验证最长匹配”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“定义词素”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### 2.5.3 手工编码的词法分析器

“2.5.3 手工编码的词法分析器”不能只作为名词记忆。本节先在“定义词素”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“构造Thompson NFA”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### 2.5.4 处理关键字

“2.5.4 处理关键字”不能只作为名词记忆。本节先在“构造Thompson NFA”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“执行子集构造”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### 2.6 高级主题

“2.6 高级主题”不能只作为名词记忆。本节先在“执行子集构造”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“运行Hopcroft最小化”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### 2.6.1 从DFA到正则表达式

“2.6.1 从DFA到正则表达式”不能只作为名词记忆。本节先在“运行Hopcroft最小化”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“编码扫描器”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### 2.6.2 DFA最小化的另一种方法：Brzozowski算法

“2.6.2 DFA最小化的另一种方法：Brzozowski算法”不能只作为名词记忆。本节先在“编码扫描器”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“验证最长匹配”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### 2.6.3 无闭包的正则表达式

“2.6.3 无闭包的正则表达式”不能只作为名词记忆。本节先在“验证最长匹配”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“定义词素”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### 2.7 小结和展望

“2.7 小结和展望”不能只作为名词记忆。本节先在“定义词素”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“构造Thompson NFA”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

## 分步视觉验证

先预测当前阶段的图、表、集合或指令，再运行变换；若不同，停在第一处分叉并保留原始证据。

<Stepper>
  <Step title="画出编译产物链">
    标出正则表达式、NFA、DFA、Hopcroft算法、最长匹配的输入、输出、所有权和下游消费者。
    <Eac02ScannersMapLab />
  </Step>
  <Step title="运行单变量实验">
    固定ILOC程序与目标模型，只改变一个语法、分析、优化或机器条件。
    <Eac02ScannersExperimentLab />
  </Step>
  <Step title="核对第一处分叉">
    比较结构化产物、成本与行为，清理后完整重建并重放回归样本。
    <Eac02ScannersEvidenceLab />
  </Step>
</Stepper>

## 常见误区

<Callout type="trap">
  **误区一：**
  只保存最终目标代码。没有token、AST、CFG、数据流集合、模式覆盖和寄存器映射，就无法把错误归因到具体阶段。
</Callout>

<Callout type="trap">
  **误区二：**
  先应用变换再补安全条件。优化、调度与分配必须先证明前提，重写后还要重算已经失效的分析结果。
</Callout>

## 四类样本与通过条件

| 样本 | 单一变量               | 必查证据         | 通过条件         |
| ---- | ---------------------- | ---------------- | ---------------- |
| 正常 | 一个最小语言或机器功能 | 全部阶段快照     | 与预测一致       |
| 边界 | 深度、宽度、延迟或压力 | 临界分支与成本   | 边界可解释       |
| 失败 | 一个文法、事实或约束   | 首错与下游停止   | 不产出伪结果     |
| 优化 | 一个pass或策略         | 前后IR与差分执行 | 等价且收益可解释 |

## 练习

<Exercises>

**问题 1** 为什么不能只凭最终程序运行成功验收第2章 词法分析器？

<Answer>
  最终行为可能隐藏错误token、树结构、数据流事实、覆盖规则或寄存器约束。应保存每层结构化产物，定位第一处分叉，并从源程序清理重建。
</Answer>

**问题 2** 如何把“执行子集构造”变成可回归实验？

<Answer>
  固定输入、编译器提交、ILOC方言与目标模型，先预测正则表达式和NFA的中间状态，只改变一个条件，并保留正常、边界、失败和优化四类样本。
</Answer>

**问题 3** 本页最小通过条件是什么？

<Answer>
  每个输入字符只被消费一次或进入明确错误状态，接受状态保留token优先级，最小化前后识别语言完全相同。
  修复后必须清理旧产物，完整重建全部下游结果，并让历史样本回到基线。
</Answer>

</Exercises>

## 小结

- 正则表达式：连接“定义词素”的算法前提、结构化产物、成本与回归证据。
- NFA：连接“构造Thompson NFA”的算法前提、结构化产物、成本与回归证据。
- DFA：连接“执行子集构造”的算法前提、结构化产物、成本与回归证据。
- Hopcroft算法：连接“运行Hopcroft最小化”的算法前提、结构化产物、成本与回归证据。
- 最长匹配：连接“编码扫描器”的算法前提、结构化产物、成本与回归证据。
- 本页25个目录层级均已进入可追溯的编译工程证据链。

## 术语表

<Glossary>
  <GlossaryItem term="正则表达式">
    正则表达式用于解释“定义词素”，需要同时说明输入、算法前提、结构化结果、成本与重建方法。
  </GlossaryItem>
  <GlossaryItem term="NFA">
    NFA用于解释“构造Thompson
    NFA”，需要同时说明输入、算法前提、结构化结果、成本与重建方法。
  </GlossaryItem>
  <GlossaryItem term="DFA">
    DFA用于解释“执行子集构造”，需要同时说明输入、算法前提、结构化结果、成本与重建方法。
  </GlossaryItem>
  <GlossaryItem term="Hopcroft算法">
    Hopcroft算法用于解释“运行Hopcroft最小化”，需要同时说明输入、算法前提、结构化结果、成本与重建方法。
  </GlossaryItem>
  <GlossaryItem term="最长匹配">
    最长匹配用于解释“编码扫描器”，需要同时说明输入、算法前提、结构化结果、成本与重建方法。
  </GlossaryItem>
</Glossary>

<Attribution
  adaptedFrom="Keith D. Cooper、Linda Torczon著，郭旭译《编译器设计（第2版）》，人民邮电出版社，2013，578页，ISBN 9787115301949 · 基于Elsevier官方详细目录与中文版完整目录的独立教学重构"
  adaptedUrl="https://shop.elsevier.com/books/engineering-a-compiler/cooper/978-0-12-088478-0"
/>
