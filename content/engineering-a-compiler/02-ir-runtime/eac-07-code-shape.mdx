---
title: "第7章 代码形式"
type: C
section: "第二部分 · 从源码映射到IR"
order: 7
description: "第7章 代码形式逐项覆盖39个正式目录层级，以ILOC、IR快照、分析集合、目标指令与运行轨迹建立可复查的编译器工程实验。"
demo: true
math: false
sourceUrl: "https://shop.elsevier.com/books/engineering-a-compiler/cooper/978-0-12-088478-0"
draft: false
---


## 为什么最终输出正确仍然不够

、、、、共同构成本页坐标。为表达式、数组、字符串、结构、控制流与调用选择可执行代码形式，同时约束求值顺序、数据布局、边界检查和寄存器压力。

编译器是一串互相依赖的转换。错误的扫描器可能恰好产生可解析token，错误的语法树可能在语义动作中被修补，错误的数据流事实可能只在冷路径触发，错误的指令调度也可能在当前处理器上没有暴露。因此必须逐层保存输入、输出和不变量，定位最早偏离，而不能把“样例跑通”当作完整证明。

## 可复查的编译工程台账

实验固定源程序、编译器提交、ILOC方言、目标机模型、优化级别与随机种子。前端保存token、推导、AST与类型环境；中端保存基本块、CFG、支配关系、数据流集合、SSA和每个pass的前后IR；后端保存模式覆盖、依赖DAG、寄存器映射、逐出代码和最终指令。每份产物都带生成命令、退出状态与摘要值。

样本分为四组。正常样本证明主路径；边界样本覆盖空输入、深嵌套、临界立即数、不可归约控制流、寄存器压力和长延迟；失败样本只破坏一个语法、类型、数据流或机器约束；优化样本则同时保存未优化与优化版本，并用解释器或差分执行核对可观察行为。

诊断必须包含阶段、源位置或IR位置、实际状态、期望状态、相关前驱和首要根因。某一阶段失败后，不得继续消费旧缓存伪造下游结果。修改算法后先清理生成目录，再重建全部受影响事实；若一个pass令支配、活跃或别名信息失效，就必须显式重算或证明仍然有效。

性能结论与正确性结论分开记录。正确性要求语义等价和机器约束成立；性能要求给出静态成本、动态计数、代码大小、编译时间与硬件环境。一次偶然加速不能证明算法更优，一次慢化也不必然否定变换，关键是解释成本模型与实际瓶颈为何一致或偏离。

正式目录项映射到可追溯矩阵：每项至少关联一个输入、一个算法前提、一份结构化产物、一个失败对照和一个回归断言。跨章接口同时记录生产者与消费者，因而能发现“提到概念却没有验证”的空洞覆盖，也能在实现变化后只重放受影响的实验。

## 六阶段证据链

### 1. 确定存储布局

围绕代码形式预测输入和结构化结果，执行后比较第一条差分。进入“降低表达式”前，确认语义、算法前提与成本记录均完整。

### 2. 降低表达式

围绕数组布局预测输入和结构化结果，执行后比较第一条差分。进入“生成地址计算”前，确认语义、算法前提与成本记录均完整。

### 3. 生成地址计算

围绕布尔控制流预测输入和结构化结果，执行后比较第一条差分。进入“形成控制流”前，确认语义、算法前提与成本记录均完整。

### 4. 形成控制流

围绕结构布局预测输入和结构化结果，执行后比较第一条差分。进入“降低过程调用”前，确认语义、算法前提与成本记录均完整。

### 5. 降低过程调用

围绕调用序列预测输入和结构化结果，执行后比较第一条差分。进入“验证代码形状”前，确认语义、算法前提与成本记录均完整。

### 6. 验证代码形状

围绕代码形式预测输入和结构化结果，执行后比较第一条差分。进入“确定存储布局”前，确认语义、算法前提与成本记录均完整。

## 可运行骨架

```iloc
loadAI rarp, 0 => r1
multI rindex, 4 => r2
loadAO r1, r2 => rvalue
```

```pseudo
if condition then goto Ltrue
goto Lfalse
Ltrue: emit(thenPart)
Lfalse: emit(elsePart)
```

```text
struct Pair { char tag; int value; }
offset(tag)=0, padding=3, offset(value)=4, sizeof=8
```


## 正式目录逐项讲解

### 第7章 代码形式

“第7章 代码形式”不能只作为名词记忆。本节先在“确定存储布局”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“降低表达式”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### 7.1 简介

“7.1 简介”不能只作为名词记忆。本节先在“降低表达式”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“生成地址计算”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### 7.2 分配存储位置

“7.2 分配存储位置”不能只作为名词记忆。本节先在“生成地址计算”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“形成控制流”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### 7.2.1 设定运行时数据结构的位置

“7.2.1 设定运行时数据结构的位置”不能只作为名词记忆。本节先在“形成控制流”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“降低过程调用”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### 7.2.2 数据区的布局

“7.2.2 数据区的布局”不能只作为名词记忆。本节先在“降低过程调用”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“验证代码形状”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### 7.2.3 将值保持在寄存器中

“7.2.3 将值保持在寄存器中”不能只作为名词记忆。本节先在“验证代码形状”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“确定存储布局”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### 7.3 算术运算符

“7.3 算术运算符”不能只作为名词记忆。本节先在“确定存储布局”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“降低表达式”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### 7.3.1 减少对寄存器的需求

“7.3.1 减少对寄存器的需求”不能只作为名词记忆。本节先在“降低表达式”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“生成地址计算”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### 7.3.2 访问参数值

“7.3.2 访问参数值”不能只作为名词记忆。本节先在“生成地址计算”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“形成控制流”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### 7.3.3 表达式中的函数调用

“7.3.3 表达式中的函数调用”不能只作为名词记忆。本节先在“形成控制流”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“降低过程调用”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### 7.3.4 其他算术运算符

“7.3.4 其他算术运算符”不能只作为名词记忆。本节先在“降低过程调用”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“验证代码形状”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### 7.3.5 混合类型表达式

“7.3.5 混合类型表达式”不能只作为名词记忆。本节先在“验证代码形状”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“确定存储布局”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### 7.3.6 作为运算符的赋值操作

“7.3.6 作为运算符的赋值操作”不能只作为名词记忆。本节先在“确定存储布局”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“降低表达式”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### 7.4 布尔运算符和关系运算符

“7.4 布尔运算符和关系运算符”不能只作为名词记忆。本节先在“降低表达式”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“生成地址计算”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### 7.4.1 表示

“7.4.1 表示”不能只作为名词记忆。本节先在“生成地址计算”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“形成控制流”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### 7.4.2 对关系操作的硬件支持

“7.4.2 对关系操作的硬件支持”不能只作为名词记忆。本节先在“形成控制流”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“降低过程调用”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### 7.5 数组的存储和访问

“7.5 数组的存储和访问”不能只作为名词记忆。本节先在“降低过程调用”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“验证代码形状”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### 7.5.1 引用向量元素

“7.5.1 引用向量元素”不能只作为名词记忆。本节先在“验证代码形状”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“确定存储布局”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### 7.5.2 数组存储布局

“7.5.2 数组存储布局”不能只作为名词记忆。本节先在“确定存储布局”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“降低表达式”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### 7.5.3 引用数组元素

“7.5.3 引用数组元素”不能只作为名词记忆。本节先在“降低表达式”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“生成地址计算”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### 7.5.4 范围检查

“7.5.4 范围检查”不能只作为名词记忆。本节先在“生成地址计算”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“形成控制流”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### 7.6 字符串

“7.6 字符串”不能只作为名词记忆。本节先在“形成控制流”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“降低过程调用”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### 7.6.1 字符串表示

“7.6.1 字符串表示”不能只作为名词记忆。本节先在“降低过程调用”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“验证代码形状”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### 7.6.2 字符串赋值

“7.6.2 字符串赋值”不能只作为名词记忆。本节先在“验证代码形状”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“确定存储布局”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### 7.6.3 字符串连接

“7.6.3 字符串连接”不能只作为名词记忆。本节先在“确定存储布局”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“降低表达式”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### 7.6.4 字符串长度

“7.6.4 字符串长度”不能只作为名词记忆。本节先在“降低表达式”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“生成地址计算”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### 7.7 结构引用

“7.7 结构引用”不能只作为名词记忆。本节先在“生成地址计算”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“形成控制流”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### 7.7.1 理解结构布局

“7.7.1 理解结构布局”不能只作为名词记忆。本节先在“形成控制流”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“降低过程调用”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### 7.7.2 结构数组

“7.7.2 结构数组”不能只作为名词记忆。本节先在“降低过程调用”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“验证代码形状”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### 7.7.3 联合和运行时标记

“7.7.3 联合和运行时标记”不能只作为名词记忆。本节先在“验证代码形状”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“确定存储布局”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### 7.7.4 指针和匿名值

“7.7.4 指针和匿名值”不能只作为名词记忆。本节先在“确定存储布局”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“降低表达式”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### 7.8 控制流结构

“7.8 控制流结构”不能只作为名词记忆。本节先在“降低表达式”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“生成地址计算”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### 7.8.1 条件执行

“7.8.1 条件执行”不能只作为名词记忆。本节先在“生成地址计算”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“形成控制流”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### 7.8.2 循环和迭代

“7.8.2 循环和迭代”不能只作为名词记忆。本节先在“形成控制流”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“降低过程调用”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### 7.8.3 case语句

“7.8.3 case语句”不能只作为名词记忆。本节先在“降低过程调用”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“验证代码形状”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### 7.9 过程调用

“7.9 过程调用”不能只作为名词记忆。本节先在“验证代码形状”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“确定存储布局”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### 7.9.1 实参求值

“7.9.1 实参求值”不能只作为名词记忆。本节先在“确定存储布局”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“降低表达式”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### 7.9.2 保存和恢复寄存器

“7.9.2 保存和恢复寄存器”不能只作为名词记忆。本节先在“降低表达式”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“生成地址计算”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### 7.10 小结和展望

“7.10 小结和展望”不能只作为名词记忆。本节先在“生成地址计算”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“形成控制流”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

## 分步视觉验证

先预测当前阶段的图、表、集合或指令，再运行变换；若不同，停在第一处分叉并保留原始证据。


## 常见误区


## 四类样本与通过条件

| 样本 | 单一变量               | 必查证据         | 通过条件         |
| ---- | ---------------------- | ---------------- | ---------------- |
| 正常 | 一个最小语言或机器功能 | 全部阶段快照     | 与预测一致       |
| 边界 | 深度、宽度、延迟或压力 | 临界分支与成本   | 边界可解释       |
| 失败 | 一个文法、事实或约束   | 首错与下游停止   | 不产出伪结果     |
| 优化 | 一个pass或策略         | 前后IR与差分执行 | 等价且收益可解释 |

## 练习


## 小结

- 代码形式：连接“确定存储布局”的算法前提、结构化产物、成本与回归证据。
- 数组布局：连接“降低表达式”的算法前提、结构化产物、成本与回归证据。
- 布尔控制流：连接“生成地址计算”的算法前提、结构化产物、成本与回归证据。
- 结构布局：连接“形成控制流”的算法前提、结构化产物、成本与回归证据。
- 调用序列：连接“降低过程调用”的算法前提、结构化产物、成本与回归证据。
- 本页39个目录层级均已进入可追溯的编译工程证据链。

## 术语表


