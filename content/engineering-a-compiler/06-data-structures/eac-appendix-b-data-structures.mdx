---
title: "附录B 数据结构"
type: C
section: "附录B · 编译器数据结构"
order: 15
description: "附录B 数据结构逐项覆盖16个正式目录层级，以ILOC、IR快照、分析集合、目标指令与运行轨迹建立可复查的编译器工程实验。"
demo: true
math: false
sourceUrl: "https://shop.elsevier.com/books/engineering-a-compiler/cooper/978-0-12-088478-0"
draft: false
---


## 为什么最终输出正确仍然不够

、、、、共同构成本页坐标。比较有序表、位向量和稀疏集合，实现图形与线性IR、散列表及嵌套作用域，让编译器核心算法的数据结构成本可测量。

编译器是一串互相依赖的转换。错误的扫描器可能恰好产生可解析token，错误的语法树可能在语义动作中被修补，错误的数据流事实可能只在冷路径触发，错误的指令调度也可能在当前处理器上没有暴露。因此必须逐层保存输入、输出和不变量，定位最早偏离，而不能把“样例跑通”当作完整证明。

## 可复查的编译工程台账

实验固定源程序、编译器提交、ILOC方言、目标机模型、优化级别与随机种子。前端保存token、推导、AST与类型环境；中端保存基本块、CFG、支配关系、数据流集合、SSA和每个pass的前后IR；后端保存模式覆盖、依赖DAG、寄存器映射、逐出代码和最终指令。每份产物都带生成命令、退出状态与摘要值。

样本分为四组。正常样本证明主路径；边界样本覆盖空输入、深嵌套、临界立即数、不可归约控制流、寄存器压力和长延迟；失败样本只破坏一个语法、类型、数据流或机器约束；优化样本则同时保存未优化与优化版本，并用解释器或差分执行核对可观察行为。

诊断必须包含阶段、源位置或IR位置、实际状态、期望状态、相关前驱和首要根因。某一阶段失败后，不得继续消费旧缓存伪造下游结果。修改算法后先清理生成目录，再重建全部受影响事实；若一个pass令支配、活跃或别名信息失效，就必须显式重算或证明仍然有效。

性能结论与正确性结论分开记录。正确性要求语义等价和机器约束成立；性能要求给出静态成本、动态计数、代码大小、编译时间与硬件环境。一次偶然加速不能证明算法更优，一次慢化也不必然否定变换，关键是解释成本模型与实际瓶颈为何一致或偏离。

正式目录项映射到可追溯矩阵：每项至少关联一个输入、一个算法前提、一份结构化产物、一个失败对照和一个回归断言。跨章接口同时记录生产者与消费者，因而能发现“提到概念却没有验证”的空洞覆盖，也能在实现变化后只重放受影响的实验。

## 六阶段证据链

### 1. 选择集合表示

围绕稀疏集合预测输入和结构化结果，执行后比较第一条差分。进入“实现IR节点”前，确认语义、算法前提与成本记录均完整。

### 2. 实现IR节点

围绕位向量预测输入和结构化结果，执行后比较第一条差分。进入“维护图边”前，确认语义、算法前提与成本记录均完整。

### 3. 维护图边

围绕图IR预测输入和结构化结果，执行后比较第一条差分。进入“设计散列表”前，确认语义、算法前提与成本记录均完整。

### 4. 设计散列表

围绕散列表预测输入和结构化结果，执行后比较第一条差分。进入“压入词法作用域”前，确认语义、算法前提与成本记录均完整。

### 5. 压入词法作用域

围绕嵌套作用域预测输入和结构化结果，执行后比较第一条差分。进入“测量时间空间”前，确认语义、算法前提与成本记录均完整。

### 6. 测量时间空间

围绕稀疏集合预测输入和结构化结果，执行后比较第一条差分。进入“选择集合表示”前，确认语义、算法前提与成本记录均完整。

## 可运行骨架

```pseudo
contains(x): dense[sparse[x]] == x
insert(x): sparse[x] = n; dense[n++] = x
```

```text
bitset union: out[i] = left[i] | right[i]
bitset intersect: out[i] = left[i] & right[i]
```

```pseudo
enterScope(): scopes.push(new Table)
lookup(name): search scopes from top to bottom
leaveScope(): scopes.pop()
```


## 正式目录逐项讲解

### 附录B 数据结构

“附录B 数据结构”不能只作为名词记忆。本节先在“选择集合表示”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“实现IR节点”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### B.1 简介

“B.1 简介”不能只作为名词记忆。本节先在“实现IR节点”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“维护图边”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### B.2 集合的表示

“B.2 集合的表示”不能只作为名词记忆。本节先在“维护图边”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“设计散列表”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### B.2.1 将集合表示为有序列表

“B.2.1 将集合表示为有序列表”不能只作为名词记忆。本节先在“设计散列表”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“压入词法作用域”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### B.2.2 将集合表示为位向量

“B.2.2 将集合表示为位向量”不能只作为名词记忆。本节先在“压入词法作用域”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“测量时间空间”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### B.2.3 表示稀疏集合

“B.2.3 表示稀疏集合”不能只作为名词记忆。本节先在“测量时间空间”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“选择集合表示”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### B.3 中间表示的实现

“B.3 中间表示的实现”不能只作为名词记忆。本节先在“选择集合表示”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“实现IR节点”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### B.3.1 图形中间表示

“B.3.1 图形中间表示”不能只作为名词记忆。本节先在“实现IR节点”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“维护图边”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### B.3.2 线性中间形式

“B.3.2 线性中间形式”不能只作为名词记忆。本节先在“维护图边”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“设计散列表”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### B.4 散列表的实现

“B.4 散列表的实现”不能只作为名词记忆。本节先在“设计散列表”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“压入词法作用域”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### B.4.1 选择散列函数

“B.4.1 选择散列函数”不能只作为名词记忆。本节先在“压入词法作用域”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“测量时间空间”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### B.4.2 开放散列

“B.4.2 开放散列”不能只作为名词记忆。本节先在“测量时间空间”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“选择集合表示”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### B.4.3 开放寻址

“B.4.3 开放寻址”不能只作为名词记忆。本节先在“选择集合表示”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“实现IR节点”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### B.4.4 存储符号记录

“B.4.4 存储符号记录”不能只作为名词记忆。本节先在“实现IR节点”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“维护图边”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### B.4.5 添加嵌套词法作用域

“B.4.5 添加嵌套词法作用域”不能只作为名词记忆。本节先在“维护图边”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“设计散列表”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

### B.5 灵活的符号表设计

“B.5 灵活的符号表设计”不能只作为名词记忆。本节先在“设计散列表”阶段固定输入、数据结构、前置事实与成本模型，再预测进入“压入词法作用域”前应出现的IR、集合、图、表项、目标指令或运行时状态。实现时只改变一个条件，记录第一条结构或行为差分，避免最终程序碰巧正确掩盖中间错误。

验收时同时问四个问题：转换是否保持可观察语义，算法前提是否真的成立，时间与空间代价是否符合输入规模，以及失败是否停在最早可诊断边界。把答案连同原始产物、工具版本和最小反例写入台账，修复后清理旧输出并从源程序重建，才能证明本目录项被实际掌握。

## 分步视觉验证

先预测当前阶段的图、表、集合或指令，再运行变换；若不同，停在第一处分叉并保留原始证据。


## 常见误区


## 四类样本与通过条件

| 样本 | 单一变量               | 必查证据         | 通过条件         |
| ---- | ---------------------- | ---------------- | ---------------- |
| 正常 | 一个最小语言或机器功能 | 全部阶段快照     | 与预测一致       |
| 边界 | 深度、宽度、延迟或压力 | 临界分支与成本   | 边界可解释       |
| 失败 | 一个文法、事实或约束   | 首错与下游停止   | 不产出伪结果     |
| 优化 | 一个pass或策略         | 前后IR与差分执行 | 等价且收益可解释 |

## 练习


## 小结

- 稀疏集合：连接“选择集合表示”的算法前提、结构化产物、成本与回归证据。
- 位向量：连接“实现IR节点”的算法前提、结构化产物、成本与回归证据。
- 图IR：连接“维护图边”的算法前提、结构化产物、成本与回归证据。
- 散列表：连接“设计散列表”的算法前提、结构化产物、成本与回归证据。
- 嵌套作用域：连接“压入词法作用域”的算法前提、结构化产物、成本与回归证据。
- 本页16个目录层级均已进入可追溯的编译工程证据链。

## 术语表


