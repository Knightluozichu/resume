---
title: "第2章 探究新语言，快速入门Kotlin编程"
description: "依据郭霖《第一行代码 Android》第3版完整目录独立重构：掌握Kotlin变量、函数、控制流、面向对象、Lambda、集合、空安全与扩展语法，并理解其在Android字节码与Java互操作中的边界"
type: C
section: "第一行代码 Android（第3版 / Android 10 / Kotlin）"
draft: false
math: false
sourceUrl: "https://read.douban.com/ebook/337721450/"
demo: true
order: 0
qualityVersion: 2
practiceMode: diagnosis
sourceMode: independent-rewrite
officialUnitId: fla3-02-kotlin
---

import { Callout } from "@/components/mdx/callout";
import { Objectives } from "@/components/mdx/objectives";
import { Term } from "@/components/mdx/term";
import { Answer, Exercises } from "@/components/mdx/exercises";
import { Glossary, GlossaryItem } from "@/components/mdx/glossary";
import { Attribution } from "@/components/mdx/attribution";
import { Fla302KotlinDecisionLab, Fla302KotlinMechanismMap, Fla302KotlinFailureDiagram } from "@/components/mdx/first-line-android/fla3-02-kotlin";

# 第2章 探究新语言，快速入门Kotlin编程

本页对应第3版正式结构中的 **第2章 探究新语言，快速入门Kotlin编程**。课程不复制原文，而是按出版社电子书目录逐节点独立重构平台机制与工程实践，目标是掌握Kotlin变量、函数、控制流、面向对象、Lambda、集合、空安全与扩展语法，并理解其在Android字节码与Java互操作中的边界。每个示例都必须说明目标API、设备、生命周期、线程、状态来源和失败条件。

<Callout type="info">

  **版本与来源边界。** 本课程锁定郭霖《第一行代码 Android》第3版，人民邮电出版社，2020年4月，692页，ISBN 9787115524836。出版社提供的电子书目录确认16章与136个章/二级正式节点；全书以Android 10.0和Kotlin重写为基线，并在后两章完成天气App与PermissionX。在《第2章 探究新语言，快速入门Kotlin编程》中，书中的kotlin-android-extensions、IntentService、jcenter及部分权限、存储、后台和通知做法具有历史边界，现代目标SDK必须另查官方资料，不能反向改写原书语境。

</Callout>
## 可验收学习目标

<Objectives>
- 能解释本页全部10个正式节点的用户任务、平台合同、生命周期和版本边界。
- 能实现“为同一数据转换分别写命令式和函数式Kotlin实现，加入可空输入、继承和Java调用，比较类型推断与生成行为”，并保存干净构建、设备操作、原始日志和断言。
- 能比较正常创建、配置变更、后台、进程重建、权限拒绝与外部故障下的状态差异。
- 能设计至少一个推翻当前实现的反例，并用Kotlin语义卡、空安全反例、Lambda与集合变换实验、Java互操作字节码记录完成独立交接。
</Objectives>

<Fla302KotlinMechanismMap />

<Fla302KotlinDecisionLab />

<Fla302KotlinFailureDiagram />

首要陷阱是“把Kotlin当成更短的Java，只背语法糖而不理解可空类型、函数类型、对象表达式和互操作平台类型”。先预测再运行；任何“能跑”结论都要经过生命周期、失败输入、安全、资源释放和目标SDK迁移检查。

## 先建立直觉

在《第2章 探究新语言，快速入门Kotlin编程》中，先把<Term def="由系统回调驱动、决定组件何时创建、可见、停止与释放的状态机">生命周期</Term>看成系统与应用之间的时序合同，再把<Term def="Android版本、targetSdk、设备形态和权限政策共同限定的行为适用范围">版本边界</Term>看成这份合同的坐标。API 名称只是入口；真正要追踪的是输入由谁接收、状态由谁保存、任务由谁取消，以及失败后用户看到什么。

《第2章 探究新语言，快速入门Kotlin编程》的直觉检验只改变一个条件：旋转、进程重建、拒绝权限、断网或目标 SDK。若结果随隐藏缓存或旧对象身份漂移，就回到状态所有者和平台合同定位首个分叉，而不是继续堆补丁。

## 最小可执行切片

先把状态与副作用分离，示例只表达可恢复合同：

```kotlin
sealed interface UiState {
    data object Loading : UiState
    data class Ready(val itemId: String) : UiState
    data class Failed(val retryable: Boolean) : UiState
}

fun reduce(state: UiState, event: UserEvent): UiState = nextState(state, event)
```

固定环境并从命令行保留可复现证据：

```bash
./gradlew clean test assembleDebug
adb install -r app/build/outputs/apk/debug/app-debug.apk
adb shell am force-stop example.package
adb shell monkey -p example.package 1
adb logcat -d -v threadtime > run.log
```

验收在操作前写出故障条件：

```gherkin
Given a pinned JDK, SDK, Gradle build, device API, locale, and seed
When the component is recreated after state saving or process death
Then user-visible state is restored from durable facts
And cancelled work cannot update a destroyed owner
And denied permission, offline input, and malformed data remain recoverable
```

在《第2章 探究新语言，快速入门Kotlin编程》中，三段代码分别承担状态模型、环境重放和行为断言。真实工程还需静态检查、单元测试、仪器测试、截图或无障碍检查，以及发布构建验证；不要用日志输出代替断言，也不要让测试依赖本机IDE缓存。

## 线程、状态与资源边界

每个实现列出五张表：入口来自哪里，执行在哪个线程，状态保存在哪里，资源由谁关闭，失败怎样呈现与重试。在《第2章 探究新语言，快速入门Kotlin编程》中，网络重试必须有上限、退避和幂等；数据库升级必须覆盖所有受支持旧版本；文件和媒体句柄在取消与异常路径释放；组件回调不持有超过生命周期的View或Context。

性能以用户任务衡量。在《第2章 探究新语言，快速入门Kotlin编程》中，启动不仅看首帧，还要检查可交互时刻；列表不仅看平均帧率，还要检查滚动卡顿和绑定分配；后台工作不仅看是否完成，还要检查电量、约束与系统调度；网络不仅看成功耗时，还要检查超时、取消和错误恢复。每次比较固定设备温度、构建类型、数据规模与网络条件。

在《第2章 探究新语言，快速入门Kotlin编程》中，版本迁移分两步：先在Android 10/Kotlin语境下还原书中机制，再对目标compileSdk、targetSdk和设备API查询当前官方合同。一次只迁移一个变化，例如存储、权限、通知、后台启动、发布仓库或已弃用库；保存编译警告、运行差异和回滚。新API更现代不代表自动正确，旧API能编译也不代表仍符合平台政策。

<Callout type="trap">

  **核心陷阱。** 把Kotlin当成更短的Java，只背语法糖而不理解可空类型、函数类型、对象表达式和互操作平台类型。修复方法是回到组件所有者与状态源，注入旋转、进程重建、拒权、断网和无效输入，再对照目标SDK合同。

</Callout>
<Callout type="trap">

  在《第2章 探究新语言，快速入门Kotlin编程》中，**历史示例不等于当前最佳实践。** 第3版准确反映Android 10时代；目标SDK变化可能改变后台、存储、权限、通知和发布政策。保留原书机制，再建立迁移层，禁止无说明地混写时代。

</Callout>
<Callout type="warning">

  在《第2章 探究新语言，快速入门Kotlin编程》中，**数据与设备安全。** 真机日志、数据库、媒体、位置和网络响应可能含个人数据；测试使用专用账号与样本，最小授权、清理产物。签名密钥、API密钥和服务端凭据不得写入仓库或客户端日志。

</Callout>
## 本章回顾

- 10个节点覆盖fla3-02-kotlin.mdx
- 最小切片验证为同一数据转换分别写命令式和函数式Ko…
- 故障实验对比正常与故障边界
- 凭证据完成交接

<Exercises>

**问题 1**：第2章 探究新语言，快速入门Kotlin编程覆盖哪些正式节点和主线？
<Answer>
  本页覆盖10个正式节点，从“第2章 探究新语言，快速入门Kotlin编程”到“2.9 小结与点评”。主线是掌握Kotlin变量、函数、控制流、面向对象、Lambda、集合、空安全与扩展语法，并理解其在Android字节码与Java互操作中的边界，交付Kotlin语义卡、空安全反例、Lambda与集合变换实验、Java互操作字节码记录。
</Answer>
**问题 2**：怎样为第2章 探究新语言，快速入门Kotlin编程建立最小可执行实验？
<Answer>
  固定JDK、SDK、Gradle、设备API、输入和账号，执行“为同一数据转换分别写命令式和函数式Kotlin实现，加入可空输入、继承和Java调用，比较类型推断与生成行为”；保存干净构建、操作、原始日志、状态快照和断言。
</Answer>
**问题 3**：为什么“把Kotlin当成更短的Java，只背语法糖而不理解可空类型、函数类型、对象表达式和互操作平台类型”会破坏结论？
<Answer>
  它只覆盖正常路径，遗漏生命周期、状态恢复、线程、资源、安全或版本合同。应回到Kotlin语义卡、空安全反例、Lambda与集合变换实验、Java互操作字节码记录补做故障注入。
</Answer>
**问题 4**：如何设计能推翻本章实现的反例？
<Answer>
  在《第2章 探究新语言，快速入门Kotlin编程》中，保持其他变量不变，分别注入旋转、进程死亡、权限拒绝、断网、无效输入和目标SDK变化；任何状态丢失、崩溃、泄漏或越权都推翻完成结论。
</Answer>
**问题 5**：从Android 10迁移到目标SDK时怎样控制变量？
<Answer>
  在《第2章 探究新语言，快速入门Kotlin编程》中，先按原书还原机制，再查询目标SDK官方合同；一次只迁移存储、权限、后台、通知、依赖或发布之一，比较构建、行为、测试和回滚。
</Answer>
**问题 6**：第2章 探究新语言，快速入门Kotlin编程达到独立交接标准需要什么？
<Answer>
  需要版本与设备指纹、源码提交、输入、构建命令、生命周期轨迹、原始日志、失败测试、安全边界和发布产物，让他人凭Kotlin语义卡、空安全反例、Lambda与集合变换实验、Java互操作字节码记录重放。
</Answer>
</Exercises>

<Glossary>
  <GlossaryItem term="生命周期">
    由系统回调驱动、描述组件从创建到销毁及可见交互阶段的状态机。
  </GlossaryItem>
  <GlossaryItem term="持久状态">
    跨配置变更或进程重建仍能恢复的最小业务事实。
  </GlossaryItem>
  <GlossaryItem term="所有者">
    负责创建、取消并最终释放组件、任务或资源的明确作用域。
  </GlossaryItem>
  <GlossaryItem term="版本边界">
    Android版本、targetSdk、设备形态、权限、网络与厂商实现共同形成的行为适用范围。
  </GlossaryItem>
  <GlossaryItem term="证据链">
    可由测试重放并以日志、状态快照、截图或产物校验支持的结论链。
  </GlossaryItem>
</Glossary>

[← 上一页：第1章 开始启程，你的第一行Android代码](/learn/first-line-android/01-first-code/fla3-01-first-code) · [下一页：第3章 先从看得到的入手，探究Activity →](/learn/first-line-android/03-activity/fla3-03-activity)

## 原版目录概念补充核对

以下条目补齐官方目录中容易被示例主线掩盖的概念。它们不重复罗列目录，而是明确每项概念的机制、适用边界和验收证据。

### 2.1 Kotlin语言简介：机制、边界与证据

第2章 探究新语言，快速入门Kotlin编程中的2.1 Kotlin语言简介要把语言写法连接到类型、可空性、对象状态与生成调用，简洁语法不能替代契约。用可编译/应拒绝案例和边界输入核对推断、空值、集合变化与 Java 互操作。

### 2.2 如何运行Kotlin代码：机制、边界与证据

第2章 探究新语言，快速入门Kotlin编程中的2.2 如何运行Kotlin代码要把语言写法连接到类型、可空性、对象状态与生成调用，简洁语法不能替代契约。用可编译/应拒绝案例和边界输入核对推断、空值、集合变化与 Java 互操作。

### 2.3 编程之本：变量和函数：机制、边界与证据

第2章 探究新语言，快速入门Kotlin编程中的2.3 编程之本：变量和函数要把语言写法连接到类型、可空性、对象状态与生成调用，简洁语法不能替代契约。用可编译/应拒绝案例和边界输入核对推断、空值、集合变化与 Java 互操作。

### 2.4 程序的逻辑控制：机制、边界与证据

第2章 探究新语言，快速入门Kotlin编程中的2.4 程序的逻辑控制要在 Android 10/Kotlin 基线中写清用户任务、平台合同、状态 owner、线程与资源释放，再单列现代 targetSdk 差异。用正常、拒绝和生命周期重建三类样本核对最终行为。

### 2.5 面向对象编程：机制、边界与证据

第2章 探究新语言，快速入门Kotlin编程中的2.5 面向对象编程要在 Android 10/Kotlin 基线中写清用户任务、平台合同、状态 owner、线程与资源释放，再单列现代 targetSdk 差异。用正常、拒绝和生命周期重建三类样本核对最终行为。

### 2.6 Lambda编程：机制、边界与证据

第2章 探究新语言，快速入门Kotlin编程中的2.6 Lambda编程要把语言写法连接到类型、可空性、对象状态与生成调用，简洁语法不能替代契约。用可编译/应拒绝案例和边界输入核对推断、空值、集合变化与 Java 互操作。

### 2.7 空指针检查：机制、边界与证据

第2章 探究新语言，快速入门Kotlin编程中的2.7 空指针检查要把语言写法连接到类型、可空性、对象状态与生成调用，简洁语法不能替代契约。用可编译/应拒绝案例和边界输入核对推断、空值、集合变化与 Java 互操作。

### 2.8 Kotlin中的小魔术：机制、边界与证据

第2章 探究新语言，快速入门Kotlin编程中的2.8 Kotlin中的小魔术要把语言写法连接到类型、可空性、对象状态与生成调用，简洁语法不能替代契约。用可编译/应拒绝案例和边界输入核对推断、空值、集合变化与 Java 互操作。

<Attribution
  adaptedFrom="郭霖《第一行代码 Android》第3版"
  adaptedUrl="https://read.douban.com/ebook/337721450/"
  mode="independent-rewrite"
  sourceBasis="outline-only"
/>
