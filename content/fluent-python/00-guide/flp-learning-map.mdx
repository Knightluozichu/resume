---
title: "《流畅的 Python》第2版全书导览"
type: C
section: "流畅Python 2e · 全书导览"
order: 0
description: "按官方五个Part、24章建立对象协议、函数、类、控制流与元编程学习路线。"
demo: true
math: false
sourceUrl: "https://www.oreilly.com/library/view/fluent-python-2nd/9781492056348/"
draft: false
---


## 从全书问题开始

先预测：读完每章示例是否自动等于掌握《流畅的 Python》第2版？不会。需要把机制放回五个Part形成依赖图，再用代码、反例和复习题验证。负责定义路线，而不是罗列链接。


## 核心结构

五Part学习地图按官方24章建立前后依赖，每章都以模型、边界、证据和复习题闭环。
## 数据与函数

数据结构主线从数据模型、序列、映射、Unicode、数据类走到引用语义，回答对象怎样保存和表现。
```python
def inspect_protocol(value):
    return type(value).__name__, repr(value), bool(value)
```

## 类、协议与类型

函数作为对象主线连接类型提示、闭包装饰器和函数式设计模式，回答行为怎样被传递与组合。

```python
from typing import Protocol
class Runnable(Protocol):
    def run(self) -> object: ...
```

## 控制流与并发

类与协议主线用值对象、序列协议、ABC、继承、泛型和运算符建立可替换的对象协作。
## 元编程与边界

控制流主线从迭代生成器走到上下文、线程进程、Future和asyncio，回答工作怎样暂停、恢复和取消。
```python
def evidence(case, expected):
    actual = case()
    assert actual == expected
    return actual
```

## 分步验收


## 练习策略

每轮只改变一个输入，记录对象身份、类型、返回、异常和资源状态。代码通过后仍要回答为什么由该协议处理、替代方案成本是什么、并发或元编程是否改变求值时机。这样才能把全书导览变成可迁移能力。

## 常见误区


## 本章回顾

五Part学习地图按官方24章建立前后依赖，每章都以模型、边界、证据和复习题闭环。 数据结构主线从数据模型、序列、映射、Unicode、数据类走到引用语义，回答对象怎样保存和表现。 函数作为对象主线连接类型提示、闭包装饰器和函数式设计模式，回答行为怎样被传递与组合。 类与协议主线用值对象、序列协议、ABC、继承、泛型和运算符建立可替换的对象协作。 控制流主线从迭代生成器走到上下文、线程进程、Future和asyncio，回答工作怎样暂停、恢复和取消。 元编程主线依次研究动态属性、描述符和类创建，仅在声明层重复真实存在时采用更强机制。 本页把五Part学习地图、数据结构主线、函数作为对象主线、类与协议主线、控制流主线、元编程主线连接成一条可执行路线。


## 术语表

