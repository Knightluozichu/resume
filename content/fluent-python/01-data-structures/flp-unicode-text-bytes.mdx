---
title: "第4章 Unicode文本与字节"
type: C
section: "流畅Python 2e · Part I 数据结构"
order: 4
description: "按《Fluent Python, 2nd Edition》第4章重建：Unicode文本与字节、编码器、解码器与Unicode错误、BOM与文本文件、Unicode规范化、大小写折叠与排序、str与bytes双模式API。"
demo: true
math: false
sourceUrl: "https://www.oreilly.com/library/view/fluent-python-2nd/9781492056348/"
draft: false
---


## 从一个错误直觉开始

先预测：只记住Unicode文本与字节的语法，是否足以写出可靠实现？不够。真正需要掌握的是对象、协议、状态和失败路径怎样协作。不是孤立技巧，而是把调用方意图映射到可测试行为的模型。


## 核心模型与责任

Unicode文本与字节必须在系统边界分离：str是Unicode码点序列，bytes是八位值序列。程序内部尽量使用str，只在文件、网络和外部协议边缘执行编码与解码。
这一章的阅读方法是先写出输入和不变量，再观察解释器或标准库采取哪条路径，最后用反例确认边界。这样学习得到的是可迁移的判断，而不是只能复述示例。

## 机制一：第一条执行链

编码器、解码器与Unicode错误策略决定数据是否可逆。strict应作为默认，ignore会静默丢数据，replace适合展示而非持久化；先识别实际编码再解码，不能靠反复试错掩盖错误。
```python
text = 'café'
data = text.encode('utf-8')
assert data.decode('utf-8') == text
```

运行后记录对象身份、返回类型和异常位置；再改变一个输入，确认变化来自协议本身，而不是偶然的实现细节。

## 机制二：状态、类型与协作

BOM与文本文件说明编码元数据可能藏在字节前缀或外部约定中。open显式指定encoding和newline，避免开发机默认编码让同一文件在生产环境产生不同结果。

```python
from unicodedata import normalize
a = 'café'
b = 'cafe\u0301'
assert normalize('NFC', a) == normalize('NFC', b)
assert 'Straße'.casefold() == 'STRASSE'.casefold()
```

评审这段代码时，先问调用端依赖什么能力，再问实现是否扩大了共享状态或静默吞掉错误。接口越小，替换和测试通常越直接。

## 机制三：边界条件与成本

Unicode规范化把视觉等价但码点不同的文本转成可比较形式，NFC适合多数存储，casefold比lower更适合无大小写比较。用户可见排序仍需区域规则，不能直接按码点宣称符合语言习惯。
str与bytes双模式API常通过参数类型决定返回类型，但正则、文件路径和操作系统接口各有边界。先设计统一内部表示，再在入口和出口做一次转换，可减少乱码和重复编码。
```python
from pathlib import Path
path = Path('message.txt')
path.write_text('你好', encoding='utf-8')
message = path.read_text(encoding='utf-8')
```

## 分步验证


## 代码实验与诊断

把三段代码保存为同一最小实验，先运行原始版本，再只改变一个维度。至少检查：公开结果是否稳定、失败是否在正确边界暴露、是否产生意外别名或资源泄漏、类型提示与运行时行为是否一致。观察变化后写出因果解释，而不是只记录输出。

## 常见误区


## 本章回顾

官方核心单元核对：Unicode文本与字节；编码器、解码器与Unicode错误；BOM与文本文件；Unicode规范化、大小写折叠与排序；str与bytes双模式API。这些单元共同回答Unicode文本与字节如何从语言机制落到工程边界；下一步应能在没有范例答案时画出执行链、实现最小版本并解释反例。


## 术语表

