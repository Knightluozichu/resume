---
title: "第9章 装饰器与闭包"
type: C
section: "流畅Python 2e · Part II 函数作为对象"
order: 9
description: "按《Fluent Python, 2nd Edition》第9章重建：装饰器与闭包、装饰器执行与注册、变量作用域、闭包与nonlocal、cache、lru_cache与singledispatch、参数化装饰器与类装饰器。"
demo: true
math: false
sourceUrl: "https://www.oreilly.com/library/view/fluent-python-2nd/9781492056348/"
draft: true
---

import { Objectives, Callout, Attribution, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step } from "@/components/mdx/mdx-components";
import { FlpClosuresDecoratorsModelLab, FlpClosuresDecoratorsBoundaryLab, FlpClosuresDecoratorsEvidenceLab } from "@/components/mdx/fluent-python/diagrams/flp-closures-decorators";

<Objectives>
- 能解释装饰器与闭包的核心协议与适用边界
- 能区分装饰器执行与注册和变量作用域、闭包与nonlocal的责任与失败方式
- 能实现并复现cache、lru_cache与singledispatch到参数化装饰器与类装饰器的最小实验
</Objectives>

## 从一个错误直觉开始

先预测：只记住装饰器与闭包的语法，是否足以写出可靠实现？不够。真正需要掌握的是对象、协议、状态和失败路径怎样协作。<Term def="装饰器与闭包都建立在函数一等性上。装饰器在定义后立即接收目标并把名称重新绑定到返回对象；闭包则让内部函数保留定义环境中的自由变量。">装饰器与闭包</Term>不是孤立技巧，而是把调用方意图映射到可测试行为的模型。

<FlpClosuresDecoratorsModelLab />

## 核心模型与责任

装饰器与闭包都建立在函数一等性上。装饰器在定义后立即接收目标并把名称重新绑定到返回对象；闭包则让内部函数保留定义环境中的自由变量。 <Term def="装饰器执行与注册要区分导入阶段和调用阶段。注册装饰器常在导入时把函数加入表并原样返回，包装装饰器则创建新调用链；模块导入副作用必须可预测。">装饰器执行与注册</Term>

这一章的阅读方法是先写出输入和不变量，再观察解释器或标准库采取哪条路径，最后用反例确认边界。这样学习得到的是可迁移的判断，而不是只能复述示例。

## 机制一：第一条执行链

装饰器执行与注册要区分导入阶段和调用阶段。注册装饰器常在导入时把函数加入表并原样返回，包装装饰器则创建新调用链；模块导入副作用必须可预测。 <Term def="变量作用域、闭包与nonlocal解释了为什么读取自由变量可行，而重新绑定需要声明nonlocal。闭包保存的是绑定单元，不只是定义时值，因此可构建有状态平均器。">变量作用域、闭包与nonlocal</Term>

```python
def register(func):
    registry.append(func)
    return func
registry = []
@register
def task(): return 'ok'
```

运行后记录对象身份、返回类型和异常位置；再改变一个输入，确认变化来自协议本身，而不是偶然的实现细节。

## 机制二：状态、类型与协作

变量作用域、闭包与nonlocal解释了为什么读取自由变量可行，而重新绑定需要声明nonlocal。闭包保存的是绑定单元，不只是定义时值，因此可构建有状态平均器。 <Term def="cache、lru_cache与singledispatch分别处理无界记忆、有界淘汰和按第一个参数类型分派。缓存参数必须可哈希，返回值应视为共享；分派注册要避免隐藏业务规则。">cache、lru_cache与singledispatch</Term>

<FlpClosuresDecoratorsBoundaryLab />

```python
def make_averager():
    total = count = 0
    def average(value):
        nonlocal total, count
        total += value; count += 1
        return total / count
    return average
```

评审这段代码时，先问调用端依赖什么能力，再问实现是否扩大了共享状态或静默吞掉错误。接口越小，替换和测试通常越直接。

## 机制三：边界条件与成本

cache、lru_cache与singledispatch分别处理无界记忆、有界淘汰和按第一个参数类型分派。缓存参数必须可哈希，返回值应视为共享；分派注册要避免隐藏业务规则。 <Term def="参数化装饰器多一层函数来接收配置，类装饰器能保存状态。无论形式如何，都应使用wraps保留名称、文档和签名，并明确同步、异步及异常语义。">参数化装饰器与类装饰器</Term>

参数化装饰器多一层函数来接收配置，类装饰器能保存状态。无论形式如何，都应使用wraps保留名称、文档和签名，并明确同步、异步及异常语义。 <Term def="用可复现实验判断装饰器与闭包实现是否保持协议、不变量与失败语义。">装饰器与闭包验证边界</Term>

```python
from functools import lru_cache, wraps
@lru_cache(maxsize=128)
def fib(n): return n if n < 2 else fib(n - 1) + fib(n - 2)
```

<FlpClosuresDecoratorsEvidenceLab />

## 分步验证

<Stepper>
  <Step title="建立模型"><FlpClosuresDecoratorsModelLab /></Step>
  <Step title="施加边界"><FlpClosuresDecoratorsBoundaryLab /></Step>
  <Step title="收集证据">分别运行正常输入、边界输入和失败输入，记录返回值、对象身份、异常类型与资源状态；只有证据支持不变量时才接受实现。</Step>
</Stepper>

## 代码实验与诊断

把三段代码保存为同一最小实验，先运行原始版本，再只改变一个维度。至少检查：公开结果是否稳定、失败是否在正确边界暴露、是否产生意外别名或资源泄漏、类型提示与运行时行为是否一致。观察变化后写出因果解释，而不是只记录输出。

## 常见误区

<Callout type="trap" title="只会装饰器与闭包的写法就等于掌握本章">
语法只能让示例运行；若没有写出输入、所有权、不变量和失败路径，换成真实数据后仍会出现隐蔽错误。应以协议和可复现实验验收。
</Callout>

<Callout type="trap" title="cache、lru_cache与singledispatch在所有场景都更优">
任何抽象都有适用成本。要比较数据规模、可变性、并发、可观察性与团队维护需求，再根据证据选择；本章给的是判断工具，不是无条件规则。
</Callout>

## 本章回顾

官方核心单元核对：装饰器与闭包；装饰器执行与注册；变量作用域、闭包与nonlocal；cache、lru_cache与singledispatch；参数化装饰器与类装饰器。这些单元共同回答装饰器与闭包如何从语言机制落到工程边界；下一步应能在没有范例答案时画出执行链、实现最小版本并解释反例。

<Exercises>
**问题 1：如何解释“装饰器与闭包”？**<Answer>装饰器与闭包都建立在函数一等性上。装饰器在定义后立即接收目标并把名称重新绑定到返回对象；闭包则让内部函数保留定义环境中的自由变量。</Answer>
**问题 2：装饰器执行与注册与变量作用域、闭包与nonlocal应怎样协作？**<Answer>装饰器执行与注册要区分导入阶段和调用阶段。注册装饰器常在导入时把函数加入表并原样返回，包装装饰器则创建新调用链；模块导入副作用必须可预测。 变量作用域、闭包与nonlocal解释了为什么读取自由变量可行，而重新绑定需要声明nonlocal。闭包保存的是绑定单元，不只是定义时值，因此可构建有状态平均器。</Answer>
**问题 3：如何验收参数化装饰器与类装饰器？**<Answer>参数化装饰器多一层函数来接收配置，类装饰器能保存状态。无论形式如何，都应使用wraps保留名称、文档和签名，并明确同步、异步及异常语义。 编写正常、边界和失败三组输入，比较返回类型、对象状态和异常位置，并确认没有依赖未声明的全局状态。</Answer>
</Exercises>

## 术语表
<Glossary>
  <GlossaryItem term="装饰器与闭包" definition="装饰器与闭包都建立在函数一等性上。" />
  <GlossaryItem term="装饰器执行与注册" definition="装饰器执行与注册要区分导入阶段和调用阶段。" />
  <GlossaryItem term="变量作用域、闭包与nonlocal" definition="变量作用域、闭包与nonlocal解释了为什么读取自由变量可行，而重新绑定需要声明nonlocal。" />
  <GlossaryItem term="cache、lru_cache与singledispatch" definition="cache、lru_cache与singledispatch分别处理无界记忆、有界淘汰和按第一个参数类型分派。" />
  <GlossaryItem term="参数化装饰器与类装饰器" definition="参数化装饰器多一层函数来接收配置，类装饰器能保存状态。" />
  <GlossaryItem term="装饰器与闭包验证边界" definition="验证装饰器与闭包协议、不变量和失败路径的边界。" />
</Glossary>

<Attribution adaptedFrom="Fluent Python, 2nd Edition, Chapter 9: 装饰器与闭包" adaptedUrl="https://www.oreilly.com/library/view/fluent-python-2nd/9781492056348/" />
