---
title: "第10章 使用一等函数实现设计模式"
type: C
section: "流畅Python 2e · Part II 函数作为对象"
order: 10
description: "按《Fluent Python, 2nd Edition》第10章重建：使用一等函数实现设计模式、经典Strategy与函数式Strategy、选择与注册策略、装饰器增强Strategy、Command模式。"
demo: true
math: false
sourceUrl: "https://www.oreilly.com/library/view/fluent-python-2nd/9781492056348/"
draft: true
---

import { Objectives, Callout, Attribution, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step } from "@/components/mdx/mdx-components";
import { FlpDesignPatternsModelLab, FlpDesignPatternsBoundaryLab, FlpDesignPatternsEvidenceLab } from "@/components/mdx/fluent-python/diagrams/flp-design-patterns";

<Objectives>
- 能解释使用一等函数实现设计模式的核心协议与适用边界
- 能区分经典Strategy与函数式Strategy和选择与注册策略的责任与失败方式
- 能实现并复现装饰器增强Strategy到Command模式的最小实验
</Objectives>

## 从一个错误直觉开始

先预测：只记住使用一等函数实现设计模式的语法，是否足以写出可靠实现？不够。真正需要掌握的是对象、协议、状态和失败路径怎样协作。<Term def="使用一等函数实现设计模式的目标不是消灭类，而是识别只有一个行为方法、没有共享状态的对象是否可简化为函数。抽象应减少样板并保持替换点清楚。">使用一等函数实现设计模式</Term>不是孤立技巧，而是把调用方意图映射到可测试行为的模型。

<FlpDesignPatternsModelLab />

## 核心模型与责任

使用一等函数实现设计模式的目标不是消灭类，而是识别只有一个行为方法、没有共享状态的对象是否可简化为函数。抽象应减少样板并保持替换点清楚。 <Term def="经典Strategy与函数式Strategy都把促销算法从Order分离。类策略适合携带配置与生命周期，函数策略适合无状态计算；调用方依赖统一签名，而不是具体实现形态。">经典Strategy与函数式Strategy</Term>

这一章的阅读方法是先写出输入和不变量，再观察解释器或标准库采取哪条路径，最后用反例确认边界。这样学习得到的是可迁移的判断，而不是只能复述示例。

## 机制一：第一条执行链

经典Strategy与函数式Strategy都把促销算法从Order分离。类策略适合携带配置与生命周期，函数策略适合无状态计算；调用方依赖统一签名，而不是具体实现形态。 <Term def="选择与注册策略可以用显式列表、命名约定或装饰器。显式表最易审计，自动发现减少维护但扩大导入副作用；生产系统应能列出已注册策略并处理同名冲突。">选择与注册策略</Term>

```python
def fidelity(order): return order.total() * 0.05 if order.customer.points >= 1000 else 0
def bulk_item(order): return sum(item.total() * 0.1 for item in order.cart if item.quantity >= 20)
```

运行后记录对象身份、返回类型和异常位置；再改变一个输入，确认变化来自协议本身，而不是偶然的实现细节。

## 机制二：状态、类型与协作

选择与注册策略可以用显式列表、命名约定或装饰器。显式表最易审计，自动发现减少维护但扩大导入副作用；生产系统应能列出已注册策略并处理同名冲突。 <Term def="装饰器增强Strategy让函数注册与声明靠近，却不应把折扣规则藏进全局状态。注册器最好返回原函数，测试可传入隔离registry，并验证每个策略对边界订单的结果。">装饰器增强Strategy</Term>

<FlpDesignPatternsBoundaryLab />

```python
promos = []
def promotion(func):
    promos.append(func)
    return func
@promotion
def large_order(order): return 7 if len(order.cart) >= 10 else 0
```

评审这段代码时，先问调用端依赖什么能力，再问实现是否扩大了共享状态或静默吞掉错误。接口越小，替换和测试通常越直接。

## 机制三：边界条件与成本

装饰器增强Strategy让函数注册与声明靠近，却不应把折扣规则藏进全局状态。注册器最好返回原函数，测试可传入隔离registry，并验证每个策略对边界订单的结果。 <Term def="Command模式把动作封装为可排队、撤销或记录的对象；若只需execute，可直接保存调用对象。需要撤销状态、审计元数据或复杂生命周期时，显式Command类仍更合适。">Command模式</Term>

Command模式把动作封装为可排队、撤销或记录的对象；若只需execute，可直接保存调用对象。需要撤销状态、审计元数据或复杂生命周期时，显式Command类仍更合适。 <Term def="用可复现实验判断使用一等函数实现设计模式实现是否保持协议、不变量与失败语义。">使用一等函数实现设计模式验证边界</Term>

```python
def best_promo(order):
    return max((promo(order) for promo in promos), default=0)
commands = [lambda: print('save'), lambda: print('notify')]
for command in commands: command()
```

<FlpDesignPatternsEvidenceLab />

## 分步验证

<Stepper>
  <Step title="建立模型"><FlpDesignPatternsModelLab /></Step>
  <Step title="施加边界"><FlpDesignPatternsBoundaryLab /></Step>
  <Step title="收集证据">分别运行正常输入、边界输入和失败输入，记录返回值、对象身份、异常类型与资源状态；只有证据支持不变量时才接受实现。</Step>
</Stepper>

## 代码实验与诊断

把三段代码保存为同一最小实验，先运行原始版本，再只改变一个维度。至少检查：公开结果是否稳定、失败是否在正确边界暴露、是否产生意外别名或资源泄漏、类型提示与运行时行为是否一致。观察变化后写出因果解释，而不是只记录输出。

## 常见误区

<Callout type="trap" title="只会使用一等函数实现设计模式的写法就等于掌握本章">
语法只能让示例运行；若没有写出输入、所有权、不变量和失败路径，换成真实数据后仍会出现隐蔽错误。应以协议和可复现实验验收。
</Callout>

<Callout type="trap" title="装饰器增强Strategy在所有场景都更优">
任何抽象都有适用成本。要比较数据规模、可变性、并发、可观察性与团队维护需求，再根据证据选择；本章给的是判断工具，不是无条件规则。
</Callout>

## 本章回顾

官方核心单元核对：使用一等函数实现设计模式；经典Strategy与函数式Strategy；选择与注册策略；装饰器增强Strategy；Command模式。这些单元共同回答使用一等函数实现设计模式如何从语言机制落到工程边界；下一步应能在没有范例答案时画出执行链、实现最小版本并解释反例。

<Exercises>
**问题 1：如何解释“使用一等函数实现设计模式”？**<Answer>使用一等函数实现设计模式的目标不是消灭类，而是识别只有一个行为方法、没有共享状态的对象是否可简化为函数。抽象应减少样板并保持替换点清楚。</Answer>
**问题 2：经典Strategy与函数式Strategy与选择与注册策略应怎样协作？**<Answer>经典Strategy与函数式Strategy都把促销算法从Order分离。类策略适合携带配置与生命周期，函数策略适合无状态计算；调用方依赖统一签名，而不是具体实现形态。 选择与注册策略可以用显式列表、命名约定或装饰器。显式表最易审计，自动发现减少维护但扩大导入副作用；生产系统应能列出已注册策略并处理同名冲突。</Answer>
**问题 3：如何验收Command模式？**<Answer>Command模式把动作封装为可排队、撤销或记录的对象；若只需execute，可直接保存调用对象。需要撤销状态、审计元数据或复杂生命周期时，显式Command类仍更合适。 编写正常、边界和失败三组输入，比较返回类型、对象状态和异常位置，并确认没有依赖未声明的全局状态。</Answer>
</Exercises>

## 术语表
<Glossary>
  <GlossaryItem term="使用一等函数实现设计模式" definition="使用一等函数实现设计模式的目标不是消灭类，而是识别只有一个行为方法、没有共享状态的对象是否可简化为函数。" />
  <GlossaryItem term="经典Strategy与函数式Strategy" definition="经典Strategy与函数式Strategy都把促销算法从Order分离。" />
  <GlossaryItem term="选择与注册策略" definition="选择与注册策略可以用显式列表、命名约定或装饰器。" />
  <GlossaryItem term="装饰器增强Strategy" definition="装饰器增强Strategy让函数注册与声明靠近，却不应把折扣规则藏进全局状态。" />
  <GlossaryItem term="Command模式" definition="Command模式把动作封装为可排队、撤销或记录的对象；若只需execute，可直接保存调用对象。" />
  <GlossaryItem term="使用一等函数实现设计模式验证边界" definition="验证使用一等函数实现设计模式协议、不变量和失败路径的边界。" />
</Glossary>

<Attribution adaptedFrom="Fluent Python, 2nd Edition, Chapter 10: 使用一等函数实现设计模式" adaptedUrl="https://www.oreilly.com/library/view/fluent-python-2nd/9781492056348/" />
