---
title: "第7章 函数作为一等对象"
type: C
section: "流畅Python 2e · Part II 函数作为对象"
order: 7
description: "按《Fluent Python, 2nd Edition》第7章重建：函数作为一等对象、高阶函数、可调用对象、仅关键字参数与仅位置参数、operator模块与functools.partial。"
demo: true
math: false
sourceUrl: "https://www.oreilly.com/library/view/fluent-python-2nd/9781492056348/"
draft: true
---

import { Objectives, Callout, Attribution, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step } from "@/components/mdx/mdx-components";

<Objectives>
- 能解释函数作为一等对象的核心协议与适用边界
- 能区分高阶函数和可调用对象的责任与失败方式
- 能实现并复现仅关键字参数与仅位置参数到operator模块与functools.partial的最小实验
</Objectives>

## 从一个错误直觉开始

先预测：只记住函数作为一等对象的语法，是否足以写出可靠实现？不够。真正需要掌握的是对象、协议、状态和失败路径怎样协作。<Term def="函数作为一等对象意味着可以赋给变量、放进集合、作为参数传入并作为结果返回。函数名只是引用，调用运算符才执行对象；这让行为可以像数据一样组合和选择。">函数作为一等对象</Term>不是孤立技巧，而是把调用方意图映射到可测试行为的模型。

<FlpFunctionsFirstClassModelLab />

## 核心模型与责任

函数作为一等对象意味着可以赋给变量、放进集合、作为参数传入并作为结果返回。函数名只是引用，调用运算符才执行对象；这让行为可以像数据一样组合和选择。 <Term def="高阶函数接收函数或返回函数，sorted的key、map和reduce都是典型入口。简单映射过滤通常优先推导式，真正需要策略注入或惰性管道时再使用高阶抽象。">高阶函数</Term>

这一章的阅读方法是先写出输入和不变量，再观察解释器或标准库采取哪条路径，最后用反例确认边界。这样学习得到的是可迁移的判断，而不是只能复述示例。

## 机制一：第一条执行链

高阶函数接收函数或返回函数，sorted的key、map和reduce都是典型入口。简单映射过滤通常优先推导式，真正需要策略注入或惰性管道时再使用高阶抽象。 <Term def="可调用对象不局限于函数：实现call的实例、类、绑定方法和生成器函数都可调用。带状态的调用对象适合累计器或配置化策略，但状态和并发可见性需要明确。">可调用对象</Term>

```python
factorial = lambda n: 1 if n < 2 else n * factorial(n - 1)
functions = [len, str.upper, factorial]
assert functions[0]([1, 2]) == 2
```

运行后记录对象身份、返回类型和异常位置；再改变一个输入，确认变化来自协议本身，而不是偶然的实现细节。

## 机制二：状态、类型与协作

可调用对象不局限于函数：实现call的实例、类、绑定方法和生成器函数都可调用。带状态的调用对象适合累计器或配置化策略，但状态和并发可见性需要明确。 <Term def="仅关键字参数与仅位置参数用星号和斜杠表达API调用约束。关键字参数提升可读性，位置专用允许未来修改内部参数名；签名应服务稳定契约，而不是炫技。">仅关键字参数与仅位置参数</Term>

<FlpFunctionsFirstClassBoundaryLab />

```python
class Averager:
    def __init__(self): self.total = 0; self.count = 0
    def __call__(self, value):
        self.total += value; self.count += 1
        return self.total / self.count
```

评审这段代码时，先问调用端依赖什么能力，再问实现是否扩大了共享状态或静默吞掉错误。接口越小，替换和测试通常越直接。

## 机制三：边界条件与成本

仅关键字参数与仅位置参数用星号和斜杠表达API调用约束。关键字参数提升可读性，位置专用允许未来修改内部参数名；签名应服务稳定契约，而不是炫技。 <Term def="operator模块与functools.partial减少微小lambda，把itemgetter、attrgetter和methodcaller用于结构化提取，把partial用于冻结部分参数。包装后仍要保留清楚命名和类型信息。">operator模块与functools.partial</Term>

operator模块与functools.partial减少微小lambda，把itemgetter、attrgetter和methodcaller用于结构化提取，把partial用于冻结部分参数。包装后仍要保留清楚命名和类型信息。 <Term def="用可复现实验判断函数作为一等对象实现是否保持协议、不变量与失败语义。">函数作为一等对象验证边界</Term>

```python
from operator import attrgetter
from functools import partial
normalize = partial(round, ndigits=2)
by_name = attrgetter('name')
```

## 分步验证

<Stepper>
  <Step title="建立模型"><FlpFunctionsFirstClassModelLab /></Step>
  <Step title="施加边界"><FlpFunctionsFirstClassBoundaryLab /></Step>
  <Step title="收集证据">分别运行正常输入、边界输入和失败输入，记录返回值、对象身份、异常类型与资源状态；只有证据支持不变量时才接受实现。</Step>
</Stepper>

## 代码实验与诊断

把三段代码保存为同一最小实验，先运行原始版本，再只改变一个维度。至少检查：公开结果是否稳定、失败是否在正确边界暴露、是否产生意外别名或资源泄漏、类型提示与运行时行为是否一致。观察变化后写出因果解释，而不是只记录输出。

## 常见误区

<Callout type="trap" title="只会函数作为一等对象的写法就等于掌握本章">
语法只能让示例运行；若没有写出输入、所有权、不变量和失败路径，换成真实数据后仍会出现隐蔽错误。应以协议和可复现实验验收。
</Callout>

<Callout type="trap" title="仅关键字参数与仅位置参数在所有场景都更优">
任何抽象都有适用成本。要比较数据规模、可变性、并发、可观察性与团队维护需求，再根据证据选择；本章给的是判断工具，不是无条件规则。
</Callout>

## 本章回顾

官方核心单元核对：函数作为一等对象；高阶函数；可调用对象；仅关键字参数与仅位置参数；operator模块与functools.partial。这些单元共同回答函数作为一等对象如何从语言机制落到工程边界；下一步应能在没有范例答案时画出执行链、实现最小版本并解释反例。

<Exercises>
**问题 1：如何解释“函数作为一等对象”？**<Answer>函数作为一等对象意味着可以赋给变量、放进集合、作为参数传入并作为结果返回。函数名只是引用，调用运算符才执行对象；这让行为可以像数据一样组合和选择。</Answer>
**问题 2：高阶函数与可调用对象应怎样协作？**<Answer>高阶函数接收函数或返回函数，sorted的key、map和reduce都是典型入口。简单映射过滤通常优先推导式，真正需要策略注入或惰性管道时再使用高阶抽象。 可调用对象不局限于函数：实现call的实例、类、绑定方法和生成器函数都可调用。带状态的调用对象适合累计器或配置化策略，但状态和并发可见性需要明确。</Answer>
**问题 3：如何验收operator模块与functools.partial？**<Answer>operator模块与functools.partial减少微小lambda，把itemgetter、attrgetter和methodcaller用于结构化提取，把partial用于冻结部分参数。包装后仍要保留清楚命名和类型信息。 编写正常、边界和失败三组输入，比较返回类型、对象状态和异常位置，并确认没有依赖未声明的全局状态。</Answer>
</Exercises>

## 术语表
<Glossary>
  <GlossaryItem term="函数作为一等对象" definition="函数作为一等对象意味着可以赋给变量、放进集合、作为参数传入并作为结果返回。" />
  <GlossaryItem term="高阶函数" definition="高阶函数接收函数或返回函数，sorted的key、map和reduce都是典型入口。" />
  <GlossaryItem term="可调用对象" definition="可调用对象不局限于函数：实现call的实例、类、绑定方法和生成器函数都可调用。" />
  <GlossaryItem term="仅关键字参数与仅位置参数" definition="仅关键字参数与仅位置参数用星号和斜杠表达API调用约束。" />
  <GlossaryItem term="operator模块与functools.partial" definition="operator模块与functools.partial减少微小lambda，把itemgetter、attrgetter和methodcaller用于结构化提取，把partial用于冻结部分参数。" />
  <GlossaryItem term="函数作为一等对象验证边界" definition="验证函数作为一等对象协议、不变量和失败路径的边界。" />
</Glossary>

<Attribution adaptedFrom="Fluent Python, 2nd Edition, Chapter 7: 函数作为一等对象" adaptedUrl="https://www.oreilly.com/library/view/fluent-python-2nd/9781492056348/" />
