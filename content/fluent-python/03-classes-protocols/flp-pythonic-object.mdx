---
title: "第11章 Python风格对象"
type: C
section: "流畅Python 2e · Part III 类与协议"
order: 11
description: "按《Fluent Python, 2nd Edition》第11章重建：Python风格对象、对象表示与备选构造器、classmethod与staticmethod、格式化显示与可哈希对象、私有属性、slots与类属性。"
demo: true
math: false
sourceUrl: "https://www.oreilly.com/library/view/fluent-python-2nd/9781492056348/"
draft: false
---

import { Objectives, Callout, Attribution, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step } from "@/components/mdx/mdx-components";
import { FlpPythonicObjectModelLab, FlpPythonicObjectBoundaryLab, FlpPythonicObjectEvidenceLab } from "@/components/mdx/fluent-python/diagrams/flp-pythonic-object";

<Objectives>
- 能解释Python风格对象的核心协议与适用边界
- 能区分对象表示与备选构造器和classmethod与staticmethod的责任与失败方式
- 能实现并复现格式化显示与可哈希对象到私有属性、slots与类属性的最小实验
</Objectives>

## 从一个错误直觉开始

先预测：只记住Python风格对象的语法，是否足以写出可靠实现？不够。真正需要掌握的是对象、协议、状态和失败路径怎样协作。<Term def="Python风格对象通过数据模型与语言协作，而不是复制内置类型全部API。先定义不可变性、表示、构造和比较不变量，再实现最小协议，调用端自然获得repr、format和hash等能力。">Python风格对象</Term>不是孤立技巧，而是把调用方意图映射到可测试行为的模型。

<FlpPythonicObjectModelLab />

## 核心模型与责任

Python风格对象通过数据模型与语言协作，而不是复制内置类型全部API。先定义不可变性、表示、构造和比较不变量，再实现最小协议，调用端自然获得repr、format和hash等能力。 <Term def="对象表示与备选构造器把可诊断表示和输入格式分开。repr最好尽量能重建对象，classmethod从字节或文本构造并尊重子类，避免把解析逻辑堆进init。">对象表示与备选构造器</Term>

这一章的阅读方法是先写出输入和不变量，再观察解释器或标准库采取哪条路径，最后用反例确认边界。这样学习得到的是可迁移的判断，而不是只能复述示例。

## 机制一：第一条执行链

对象表示与备选构造器把可诊断表示和输入格式分开。repr最好尽量能重建对象，classmethod从字节或文本构造并尊重子类，避免把解析逻辑堆进init。 <Term def="classmethod与staticmethod都改变描述符绑定方式：classmethod接收类并适合多态构造器，staticmethod只是放在命名空间里的普通函数。若函数不依赖类，模块级函数通常更清楚。">classmethod与staticmethod</Term>

```python
from array import array
class Vector2d:
    typecode = 'd'
    def __iter__(self): return (i for i in (self.x, self.y))
    def __repr__(self): return f'Vector2d({self.x!r}, {self.y!r})'
```

运行后记录对象身份、返回类型和异常位置；再改变一个输入，确认变化来自协议本身，而不是偶然的实现细节。

## 机制二：状态、类型与协作

classmethod与staticmethod都改变描述符绑定方式：classmethod接收类并适合多态构造器，staticmethod只是放在命名空间里的普通函数。若函数不依赖类，模块级函数通常更清楚。 <Term def="格式化显示与可哈希对象涉及format协议、只读属性、相等与hash一致性。可哈希对象必须在参与哈希期间不可变，相等对象必须给出相同hash。">格式化显示与可哈希对象</Term>

<FlpPythonicObjectBoundaryLab />

```python
@classmethod
def frombytes(cls, octets):
    typecode = chr(octets[0])
    values = memoryview(octets[1:]).cast(typecode)
    return cls(*values)
```

评审这段代码时，先问调用端依赖什么能力，再问实现是否扩大了共享状态或静默吞掉错误。接口越小，替换和测试通常越直接。

## 机制三：边界条件与成本

格式化显示与可哈希对象涉及format协议、只读属性、相等与hash一致性。可哈希对象必须在参与哈希期间不可变，相等对象必须给出相同hash。 <Term def="私有属性、slots与类属性是约定和布局工具。双下划线会名称改写而非安全封装，slots减少实例字典但影响弱引用和动态属性；优化前要测量实例规模。">私有属性、slots与类属性</Term>

私有属性、slots与类属性是约定和布局工具。双下划线会名称改写而非安全封装，slots减少实例字典但影响弱引用和动态属性；优化前要测量实例规模。 <Term def="用可复现实验判断Python风格对象实现是否保持协议、不变量与失败语义。">Python风格对象验证边界</Term>

```python
def __hash__(self): return hash((self.x, self.y))
def __format__(self, spec=''):
    components = (format(c, spec) for c in self)
    return f'({", ".join(components)})'
```

<FlpPythonicObjectEvidenceLab />

## 分步验证

<Stepper>
  <Step title="建立模型"><FlpPythonicObjectModelLab /></Step>
  <Step title="施加边界"><FlpPythonicObjectBoundaryLab /></Step>
  <Step title="收集证据">分别运行正常输入、边界输入和失败输入，记录返回值、对象身份、异常类型与资源状态；只有证据支持不变量时才接受实现。</Step>
</Stepper>

## 代码实验与诊断

把三段代码保存为同一最小实验，先运行原始版本，再只改变一个维度。至少检查：公开结果是否稳定、失败是否在正确边界暴露、是否产生意外别名或资源泄漏、类型提示与运行时行为是否一致。观察变化后写出因果解释，而不是只记录输出。

## 常见误区

<Callout type="trap" title="只会Python风格对象的写法就等于掌握本章">
语法只能让示例运行；若没有写出输入、所有权、不变量和失败路径，换成真实数据后仍会出现隐蔽错误。应以协议和可复现实验验收。
</Callout>

<Callout type="trap" title="格式化显示与可哈希对象在所有场景都更优">
任何抽象都有适用成本。要比较数据规模、可变性、并发、可观察性与团队维护需求，再根据证据选择；本章给的是判断工具，不是无条件规则。
</Callout>

## 本章回顾

官方核心单元核对：Python风格对象；对象表示与备选构造器；classmethod与staticmethod；格式化显示与可哈希对象；私有属性、slots与类属性。这些单元共同回答Python风格对象如何从语言机制落到工程边界；下一步应能在没有范例答案时画出执行链、实现最小版本并解释反例。

<Exercises>
**问题 1：如何解释“Python风格对象”？**<Answer>Python风格对象通过数据模型与语言协作，而不是复制内置类型全部API。先定义不可变性、表示、构造和比较不变量，再实现最小协议，调用端自然获得repr、format和hash等能力。</Answer>
**问题 2：对象表示与备选构造器与classmethod与staticmethod应怎样协作？**<Answer>对象表示与备选构造器把可诊断表示和输入格式分开。repr最好尽量能重建对象，classmethod从字节或文本构造并尊重子类，避免把解析逻辑堆进init。 classmethod与staticmethod都改变描述符绑定方式：classmethod接收类并适合多态构造器，staticmethod只是放在命名空间里的普通函数。若函数不依赖类，模块级函数通常更清楚。</Answer>
**问题 3：如何验收私有属性、slots与类属性？**<Answer>私有属性、slots与类属性是约定和布局工具。双下划线会名称改写而非安全封装，slots减少实例字典但影响弱引用和动态属性；优化前要测量实例规模。 编写正常、边界和失败三组输入，比较返回类型、对象状态和异常位置，并确认没有依赖未声明的全局状态。</Answer>
</Exercises>

## 术语表
<Glossary>
  <GlossaryItem term="Python风格对象" definition="Python风格对象通过数据模型与语言协作，而不是复制内置类型全部API。" />
  <GlossaryItem term="对象表示与备选构造器" definition="对象表示与备选构造器把可诊断表示和输入格式分开。" />
  <GlossaryItem term="classmethod与staticmethod" definition="classmethod与staticmethod都改变描述符绑定方式：classmethod接收类并适合多态构造器，staticmethod只是放在命名空间里的普通函数。" />
  <GlossaryItem term="格式化显示与可哈希对象" definition="格式化显示与可哈希对象涉及format协议、只读属性、相等与hash一致性。" />
  <GlossaryItem term="私有属性、slots与类属性" definition="私有属性、slots与类属性是约定和布局工具。" />
  <GlossaryItem term="Python风格对象验证边界" definition="验证Python风格对象协议、不变量和失败路径的边界。" />
</Glossary>

<Attribution adaptedFrom="Fluent Python, 2nd Edition, Chapter 11: Python风格对象" adaptedUrl="https://www.oreilly.com/library/view/fluent-python-2nd/9781492056348/" />
