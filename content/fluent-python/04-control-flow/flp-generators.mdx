---
title: "第17章 迭代器、生成器与经典协程"
type: C
section: "流畅Python 2e · Part IV 控制流"
order: 17
description: "按《Fluent Python, 2nd Edition》第17章重建：迭代器、生成器与经典协程、可迭代对象与迭代器、生成器函数与惰性求值、itertools与yield from、经典协程返回值。"
demo: true
math: false
sourceUrl: "https://www.oreilly.com/library/view/fluent-python-2nd/9781492056348/"
draft: true
---

import { Objectives, Callout, Attribution, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step } from "@/components/mdx/mdx-components";

<Objectives>
- 能解释迭代器、生成器与经典协程的核心协议与适用边界
- 能区分可迭代对象与迭代器和生成器函数与惰性求值的责任与失败方式
- 能实现并复现itertools与yield from到经典协程返回值的最小实验
</Objectives>

## 从一个错误直觉开始

先预测：只记住迭代器、生成器与经典协程的语法，是否足以写出可靠实现？不够。真正需要掌握的是对象、协议、状态和失败路径怎样协作。<Term def="迭代器、生成器与经典协程都把执行拆成暂停和恢复，但协议用途不同。迭代器按拉取方式产出序列，生成器用yield简化状态机，经典协程还可通过send接收值。">迭代器、生成器与经典协程</Term>不是孤立技巧，而是把调用方意图映射到可测试行为的模型。

<FlpGeneratorsModelLab />

## 核心模型与责任

迭代器、生成器与经典协程都把执行拆成暂停和恢复，但协议用途不同。迭代器按拉取方式产出序列，生成器用yield简化状态机，经典协程还可通过send接收值。 <Term def="可迭代对象与迭代器不能混为一谈：iterable能返回新iterator，iterator自身实现next且通常只能消费一次。容器应返回独立迭代器，不要让对象本身在多次循环间共享游标。">可迭代对象与迭代器</Term>

这一章的阅读方法是先写出输入和不变量，再观察解释器或标准库采取哪条路径，最后用反例确认边界。这样学习得到的是可迁移的判断，而不是只能复述示例。

## 机制一：第一条执行链

可迭代对象与迭代器不能混为一谈：iterable能返回新iterator，iterator自身实现next且通常只能消费一次。容器应返回独立迭代器，不要让对象本身在多次循环间共享游标。 <Term def="生成器函数与惰性求值让函数调用先返回生成器对象，直到next才执行函数体。惰性节省内存并支持无限流，但错误可能延迟到消费位置，资源边界也要随迭代关闭。">生成器函数与惰性求值</Term>

```python
class Sentence:
    def __init__(self, text): self.words = text.split()
    def __iter__(self):
        for word in self.words: yield word
```

运行后记录对象身份、返回类型和异常位置；再改变一个输入，确认变化来自协议本身，而不是偶然的实现细节。

## 机制二：状态、类型与协作

生成器函数与惰性求值让函数调用先返回生成器对象，直到next才执行函数体。惰性节省内存并支持无限流，但错误可能延迟到消费位置，资源边界也要随迭代关闭。 <Term def="itertools与yield from提供组合、筛选、分组和子生成器委托。groupby只分组相邻项，tee可能缓存巨大差距；yield from会转发next、send、throw和return值。">itertools与yield from</Term>

<FlpGeneratorsBoundaryLab />

```python
from itertools import chain, islice
def naturals():
    n = 0
    while True:
        yield n
        n += 1
first_ten = list(islice(naturals(), 10))
```

评审这段代码时，先问调用端依赖什么能力，再问实现是否扩大了共享状态或静默吞掉错误。接口越小，替换和测试通常越直接。

## 机制三：边界条件与成本

itertools与yield from提供组合、筛选、分组和子生成器委托。groupby只分组相邻项，tee可能缓存巨大差距；yield from会转发next、send、throw和return值。 <Term def="经典协程返回值藏在StopIteration.value中，yield from可接收它。现代并发优先原生async协程，但理解经典协议有助于掌握生成器委托和异常传播。">经典协程返回值</Term>

经典协程返回值藏在StopIteration.value中，yield from可接收它。现代并发优先原生async协程，但理解经典协议有助于掌握生成器委托和异常传播。 <Term def="用可复现实验判断迭代器、生成器与经典协程实现是否保持协议、不变量与失败语义。">迭代器、生成器与经典协程验证边界</Term>

```python
def subgen():
    yield 1
    return 42
def delegating():
    result = yield from subgen()
    print('returned', result)
```

## 分步验证

<Stepper>
  <Step title="建立模型"><FlpGeneratorsModelLab /></Step>
  <Step title="施加边界"><FlpGeneratorsBoundaryLab /></Step>
  <Step title="收集证据">分别运行正常输入、边界输入和失败输入，记录返回值、对象身份、异常类型与资源状态；只有证据支持不变量时才接受实现。</Step>
</Stepper>

## 代码实验与诊断

把三段代码保存为同一最小实验，先运行原始版本，再只改变一个维度。至少检查：公开结果是否稳定、失败是否在正确边界暴露、是否产生意外别名或资源泄漏、类型提示与运行时行为是否一致。观察变化后写出因果解释，而不是只记录输出。

## 常见误区

<Callout type="trap" title="只会迭代器、生成器与经典协程的写法就等于掌握本章">
语法只能让示例运行；若没有写出输入、所有权、不变量和失败路径，换成真实数据后仍会出现隐蔽错误。应以协议和可复现实验验收。
</Callout>

<Callout type="trap" title="itertools与yield from在所有场景都更优">
任何抽象都有适用成本。要比较数据规模、可变性、并发、可观察性与团队维护需求，再根据证据选择；本章给的是判断工具，不是无条件规则。
</Callout>

## 本章回顾

官方核心单元核对：迭代器、生成器与经典协程；可迭代对象与迭代器；生成器函数与惰性求值；itertools与yield from；经典协程返回值。这些单元共同回答迭代器、生成器与经典协程如何从语言机制落到工程边界；下一步应能在没有范例答案时画出执行链、实现最小版本并解释反例。

<Exercises>
**问题 1：如何解释“迭代器、生成器与经典协程”？**<Answer>迭代器、生成器与经典协程都把执行拆成暂停和恢复，但协议用途不同。迭代器按拉取方式产出序列，生成器用yield简化状态机，经典协程还可通过send接收值。</Answer>
**问题 2：可迭代对象与迭代器与生成器函数与惰性求值应怎样协作？**<Answer>可迭代对象与迭代器不能混为一谈：iterable能返回新iterator，iterator自身实现next且通常只能消费一次。容器应返回独立迭代器，不要让对象本身在多次循环间共享游标。 生成器函数与惰性求值让函数调用先返回生成器对象，直到next才执行函数体。惰性节省内存并支持无限流，但错误可能延迟到消费位置，资源边界也要随迭代关闭。</Answer>
**问题 3：如何验收经典协程返回值？**<Answer>经典协程返回值藏在StopIteration.value中，yield from可接收它。现代并发优先原生async协程，但理解经典协议有助于掌握生成器委托和异常传播。 编写正常、边界和失败三组输入，比较返回类型、对象状态和异常位置，并确认没有依赖未声明的全局状态。</Answer>
</Exercises>

## 术语表
<Glossary>
  <GlossaryItem term="迭代器、生成器与经典协程" definition="迭代器、生成器与经典协程都把执行拆成暂停和恢复，但协议用途不同。" />
  <GlossaryItem term="可迭代对象与迭代器" definition="可迭代对象与迭代器不能混为一谈：iterable能返回新iterator，iterator自身实现next且通常只能消费一次。" />
  <GlossaryItem term="生成器函数与惰性求值" definition="生成器函数与惰性求值让函数调用先返回生成器对象，直到next才执行函数体。" />
  <GlossaryItem term="itertools与yield from" definition="itertools与yield from提供组合、筛选、分组和子生成器委托。" />
  <GlossaryItem term="经典协程返回值" definition="经典协程返回值藏在StopIteration.value中，yield from可接收它。" />
  <GlossaryItem term="迭代器、生成器与经典协程验证边界" definition="验证迭代器、生成器与经典协程协议、不变量和失败路径的边界。" />
</Glossary>

<Attribution adaptedFrom="Fluent Python, 2nd Edition, Chapter 17: 迭代器、生成器与经典协程" adaptedUrl="https://www.oreilly.com/library/view/fluent-python-2nd/9781492056348/" />
