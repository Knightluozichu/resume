---
title: "第18章 with、match与else块"
type: C
section: "流畅Python 2e · Part IV 控制流"
order: 18
description: "按《Fluent Python, 2nd Edition》第18章重建：with、match与else块、上下文管理器与contextlib、lis.py中的模式匹配、OR模式、if之外的else块。"
demo: true
math: false
sourceUrl: "https://www.oreilly.com/library/view/fluent-python-2nd/9781492056348/"
draft: true
---

import { Objectives, Callout, Attribution, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step } from "@/components/mdx/mdx-components";

<Objectives>
- 能解释with、match与else块的核心协议与适用边界
- 能区分上下文管理器与contextlib和lis.py中的模式匹配的责任与失败方式
- 能实现并复现OR模式到if之外的else块的最小实验
</Objectives>

## 从一个错误直觉开始

先预测：只记住with、match与else块的语法，是否足以写出可靠实现？不够。真正需要掌握的是对象、协议、状态和失败路径怎样协作。<Term def="with、match与else块把建立清理、结构分派和成功路径写进控制流语法。它们都减少手工标志位，但前提是理解执行顺序、变量绑定和异常传播。">with、match与else块</Term>不是孤立技巧，而是把调用方意图映射到可测试行为的模型。

<FlpWithMatchElseModelLab />

## 核心模型与责任

with、match与else块把建立清理、结构分派和成功路径写进控制流语法。它们都减少手工标志位，但前提是理解执行顺序、变量绑定和异常传播。 <Term def="上下文管理器与contextlib让enter获取资源、exit总能清理。contextmanager把yield前后代码变成进入退出阶段；是否抑制异常由exit返回值决定，默认应传播未知错误。">上下文管理器与contextlib</Term>

这一章的阅读方法是先写出输入和不变量，再观察解释器或标准库采取哪条路径，最后用反例确认边界。这样学习得到的是可迁移的判断，而不是只能复述示例。

## 机制一：第一条执行链

上下文管理器与contextlib让enter获取资源、exit总能清理。contextmanager把yield前后代码变成进入退出阶段；是否抑制异常由exit返回值决定，默认应传播未知错误。 <Term def="lis.py中的模式匹配展示如何把Scheme表达式按结构分解：原子、quote、if、lambda与函数调用可由不同case表达。模式顺序从具体到宽泛，最后保留明确非法语法分支。">lis.py中的模式匹配</Term>

```python
from contextlib import contextmanager
@contextmanager
def managed(resource):
    try: yield resource
    finally: resource.close()
```

运行后记录对象身份、返回类型和异常位置；再改变一个输入，确认变化来自协议本身，而不是偶然的实现细节。

## 机制二：状态、类型与协作

lis.py中的模式匹配展示如何把Scheme表达式按结构分解：原子、quote、if、lambda与函数调用可由不同case表达。模式顺序从具体到宽泛，最后保留明确非法语法分支。 <Term def="OR模式要求每个分支绑定同一组变量，可合并共享处理逻辑；守卫只在结构匹配后求值。模式不是普通赋值，类模式还依赖match_args或关键字属性。">OR模式</Term>

<FlpWithMatchElseBoundaryLab />

```python
def evaluate(exp):
    match exp:
        case ['quote', value]: return value
        case ['if', test, consequence, alternative]: return evaluate(consequence if evaluate(test) else alternative)
        case [proc, *args]: return apply(evaluate(proc), [evaluate(arg) for arg in args])
```

评审这段代码时，先问调用端依赖什么能力，再问实现是否扩大了共享状态或静默吞掉错误。接口越小，替换和测试通常越直接。

## 机制三：边界条件与成本

OR模式要求每个分支绑定同一组变量，可合并共享处理逻辑；守卫只在结构匹配后求值。模式不是普通赋值，类模式还依赖match_args或关键字属性。 <Term def="if之外的else块出现在for、while和try。循环else只在未break时执行，try else只在主体无异常时执行成功逻辑；把可能抛错的代码限制在try主体可避免捕获过宽。">if之外的else块</Term>

if之外的else块出现在for、while和try。循环else只在未break时执行，try else只在主体无异常时执行成功逻辑；把可能抛错的代码限制在try主体可避免捕获过宽。 <Term def="用可复现实验判断with、match与else块实现是否保持协议、不变量与失败语义。">with、match与else块验证边界</Term>

```python
for item in items:
    if matches(item):
        result = item
        break
else:
    raise LookupError('no match')
```

## 分步验证

<Stepper>
  <Step title="建立模型"><FlpWithMatchElseModelLab /></Step>
  <Step title="施加边界"><FlpWithMatchElseBoundaryLab /></Step>
  <Step title="收集证据">分别运行正常输入、边界输入和失败输入，记录返回值、对象身份、异常类型与资源状态；只有证据支持不变量时才接受实现。</Step>
</Stepper>

## 代码实验与诊断

把三段代码保存为同一最小实验，先运行原始版本，再只改变一个维度。至少检查：公开结果是否稳定、失败是否在正确边界暴露、是否产生意外别名或资源泄漏、类型提示与运行时行为是否一致。观察变化后写出因果解释，而不是只记录输出。

## 常见误区

<Callout type="trap" title="只会with、match与else块的写法就等于掌握本章">
语法只能让示例运行；若没有写出输入、所有权、不变量和失败路径，换成真实数据后仍会出现隐蔽错误。应以协议和可复现实验验收。
</Callout>

<Callout type="trap" title="OR模式在所有场景都更优">
任何抽象都有适用成本。要比较数据规模、可变性、并发、可观察性与团队维护需求，再根据证据选择；本章给的是判断工具，不是无条件规则。
</Callout>

## 本章回顾

官方核心单元核对：with、match与else块；上下文管理器与contextlib；lis.py中的模式匹配；OR模式；if之外的else块。这些单元共同回答with、match与else块如何从语言机制落到工程边界；下一步应能在没有范例答案时画出执行链、实现最小版本并解释反例。

<Exercises>
**问题 1：如何解释“with、match与else块”？**<Answer>with、match与else块把建立清理、结构分派和成功路径写进控制流语法。它们都减少手工标志位，但前提是理解执行顺序、变量绑定和异常传播。</Answer>
**问题 2：上下文管理器与contextlib与lis.py中的模式匹配应怎样协作？**<Answer>上下文管理器与contextlib让enter获取资源、exit总能清理。contextmanager把yield前后代码变成进入退出阶段；是否抑制异常由exit返回值决定，默认应传播未知错误。 lis.py中的模式匹配展示如何把Scheme表达式按结构分解：原子、quote、if、lambda与函数调用可由不同case表达。模式顺序从具体到宽泛，最后保留明确非法语法分支。</Answer>
**问题 3：如何验收if之外的else块？**<Answer>if之外的else块出现在for、while和try。循环else只在未break时执行，try else只在主体无异常时执行成功逻辑；把可能抛错的代码限制在try主体可避免捕获过宽。 编写正常、边界和失败三组输入，比较返回类型、对象状态和异常位置，并确认没有依赖未声明的全局状态。</Answer>
</Exercises>

## 术语表
<Glossary>
  <GlossaryItem term="with、match与else块" definition="with、match与else块把建立清理、结构分派和成功路径写进控制流语法。" />
  <GlossaryItem term="上下文管理器与contextlib" definition="上下文管理器与contextlib让enter获取资源、exit总能清理。" />
  <GlossaryItem term="lis.py中的模式匹配" definition="lis.py中的模式匹配展示如何把Scheme表达式按结构分解：原子、quote、if、lambda与函数调用可由不同case表达。" />
  <GlossaryItem term="OR模式" definition="OR模式要求每个分支绑定同一组变量，可合并共享处理逻辑；守卫只在结构匹配后求值。" />
  <GlossaryItem term="if之外的else块" definition="if之外的else块出现在for、while和try。" />
  <GlossaryItem term="with、match与else块验证边界" definition="验证with、match与else块协议、不变量和失败路径的边界。" />
</Glossary>

<Attribution adaptedFrom="Fluent Python, 2nd Edition, Chapter 18: with、match与else块" adaptedUrl="https://www.oreilly.com/library/view/fluent-python-2nd/9781492056348/" />
