---
title: "第 2 章 脚手架"
type: A
section: "前端工程化 · 历史与脚手架"
order: 2
description: "解释脚手架的一次性发起者角色、本地执行边界、多种实现方式和 Yeoman 集成方案。"
demo: true
math: false
sourceUrl: "https://book.douban.com/subject/27605366/"
draft: true
---

import {
  Feng02ScaffoldingMapLab,
  Feng02ScaffoldingExperimentLab,
  Feng02ScaffoldingEvidenceLab,
} from "@/components/mdx/frontend-engineering/diagrams/feng-02-scaffolding";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第 2 章 脚手架的核心职责，并说出它在原书 7 章体系中的位置
- 能描述每个公开目录条目的源码输入、配置契约、制品、环境与回滚状态
- 能实现最小交付样本，并注入模板、模块、缓存、网络或权限故障定位首个偏离点
- 能比较正常、边界、失败和恢复轨迹，判断 2018 年历史工具与当前替代是否被清楚区分

</Objectives>

## 直觉起点

<Term def="脚手架 是“第 2 章 脚手架”的第 1 个交付坐标，对应“读取项目意图”；必须同时记录源码、制品、环境、失败与回滚证据。">脚手架</Term>、<Term def="模板 是“第 2 章 脚手架”的第 2 个交付坐标，对应“校验环境参数”；必须同时记录源码、制品、环境、失败与回滚证据。">模板</Term>、<Term def="生成器 是“第 2 章 脚手架”的第 3 个交付坐标，对应“选择模板版本”；必须同时记录源码、制品、环境、失败与回滚证据。">生成器</Term>、<Term def="本地执行 是“第 2 章 脚手架”的第 4 个交付坐标，对应“渲染文件事务”；必须同时记录源码、制品、环境、失败与回滚证据。">本地执行</Term>、<Term def="用完即弃 是“第 2 章 脚手架”的第 5 个交付坐标，对应“安装并验证依赖”；必须同时记录源码、制品、环境、失败与回滚证据。">用完即弃</Term>
共同构成本页的观察坐标。脚手架把组织约定转换为可重复项目初始化：收集参数、选择模板、渲染文件、安装依赖并给出验证结果；它发起项目但不应永久接管应用运行时。
工程化不是工具数量，而是把重复操作变成可验证流程，让变更更快获得反馈、失败更早暴露、制品可以追溯、发布可以回滚。

<Feng02ScaffoldingMapLab />

## 六阶段交付链

### 1. 读取项目意图

固定源码提交、依赖锁、工程配置、模板版本和目标环境，先预测输出文件与资源引用。没有输入身份的构建无法复现。

### 2. 校验环境参数

明确源码、生成目录、模块图、静态资源、入口文档与部署环境的所有者。共享环境变更必须有队列或互斥边界。

### 3. 选择模板版本

每个阶段只产生一个可验证输出，构建制品以摘要签发。后续测试与部署消费同一制品，不能按环境重新编译。

### 4. 渲染文件事务

注入单一边界：模板冲突、模块失败、缓存错配、HMR 残留、网络中断或权限拒绝，停止在首个偏离点。

### 5. 安装并验证依赖

区分 2018 年 webpack、Yeoman、Apache 语境与当前工具。现代替代保持原问题身份，并重新验证配置、缓存和回滚行为。

### 6. 移交项目所有权

删除故障并重放同一提交，核对文件树、制品摘要、资源 URL、入口版本与用户探测；旧版本可恢复才完成签发。

## 核心机制深挖

脚手架把组织约定转换为可重复项目初始化：收集参数、选择模板、渲染文件、安装依赖并给出验证结果；它发起项目但不应永久接管应用运行时。 每次变更统一登记六类证据：**源码身份、配置版本、依赖解析、制品摘要、环境状态、回滚条件**。模板不确定会导致项目起点漂移，构建两次会让测试制品与生产制品不同，缓存地址错误会造成跨版本资源，权限与队列缺失会让并发部署互相覆盖。

本页签发不变量是：**同一输入与模板版本生成确定文件树，冲突不会静默覆盖用户文件，失败可回滚，生成后项目不依赖脚手架进程继续运行。**。流程必须回答谁发起、消费什么输入、产生哪个制品、失败由谁观察、共享环境怎样互斥、缓存何时失效，以及旧版本如何恢复。构建成功或页面可打开只是证据的一部分。

## 2018 版与现代前端的版本账本

原书出版于 2018 年，以 Node.js、webpack、Babel、PostCSS、Yeoman、webpack-dev-middleware、Livereload/HMR、Apache、GitFlow 和 WebHook 为主要实例。重构保留 7 章 98 个目录条目，不用 Vite、Module Federation、TypeScript、Playwright、RUM 等后来主题替换原书身份；这些能力只在现代对照中说明如何延续相同工程目标。

版本账本至少记录原书工具、当时用途、当前状态、现代替代、配置差异、制品差异和迁移退出条件。稳定原则是单一制品、内容寻址、资源先于入口、接口契约和可回滚；易变部分是插件 API、开发服务器协议、分支策略、CI 平台与 CDN 配置。

## 完整公开目录逐项讲解

### 第2章 脚手架

脚手架是项目初始化事务：参数校验后选择版本化模板，渲染到临时位置，检查冲突再提交文件。Yeoman 是原书实现载体，现代替代不得改变确定生成与失败回滚目标。

实验从“读取项目意图”开始：固定源码提交、依赖锁、配置和目标环境，先预测文件树与制品引用；每次只改变一个模板、模块、缓存、网络或权限条件，在首个偏离点保存证据。恢复后重放同一提交，不能以重新构建掩盖制品差异。

### 2.1 脚手架的功能和本质

脚手架是项目初始化事务：参数校验后选择版本化模板，渲染到临时位置，检查冲突再提交文件。Yeoman 是原书实现载体，现代替代不得改变确定生成与失败回滚目标。

实验从“校验环境参数”开始：固定源码提交、依赖锁、配置和目标环境，先预测文件树与制品引用；每次只改变一个模板、模块、缓存、网络或权限条件，在首个偏离点保存证据。恢复后重放同一提交，不能以重新构建掩盖制品差异。

### 2.2 脚手架在前端工程中的角色和特征

脚手架是项目初始化事务：参数校验后选择版本化模板，渲染到临时位置，检查冲突再提交文件。Yeoman 是原书实现载体，现代替代不得改变确定生成与失败回滚目标。

实验从“选择模板版本”开始：固定源码提交、依赖锁、配置和目标环境，先预测文件树与制品引用；每次只改变一个模板、模块、缓存、网络或权限条件，在首个偏离点保存证据。恢复后重放同一提交，不能以重新构建掩盖制品差异。

### 2.2.1 用完即弃的发起者角色

脚手架是项目初始化事务：参数校验后选择版本化模板，渲染到临时位置，检查冲突再提交文件。Yeoman 是原书实现载体，现代替代不得改变确定生成与失败回滚目标。

实验从“渲染文件事务”开始：固定源码提交、依赖锁、配置和目标环境，先预测文件树与制品引用；每次只改变一个模板、模块、缓存、网络或权限条件，在首个偏离点保存证据。恢复后重放同一提交，不能以重新构建掩盖制品差异。

### 2.2.2 局限于本地的执行环境

脚手架是项目初始化事务：参数校验后选择版本化模板，渲染到临时位置，检查冲突再提交文件。Yeoman 是原书实现载体，现代替代不得改变确定生成与失败回滚目标。

实验从“安装并验证依赖”开始：固定源码提交、依赖锁、配置和目标环境，先预测文件树与制品引用；每次只改变一个模板、模块、缓存、网络或权限条件，在首个偏离点保存证据。恢复后重放同一提交，不能以重新构建掩盖制品差异。

### 2.2.3 多样性的实现模式

2.2.3 多样性的实现模式 需要放回“读取项目意图、校验环境参数、选择模板版本、渲染文件事务、安装并验证依赖、移交项目所有权”的完整交付链，明确输入、制品、环境、所有者和失败出口。

实验从“移交项目所有权”开始：固定源码提交、依赖锁、配置和目标环境，先预测文件树与制品引用；每次只改变一个模板、模块、缓存、网络或权限条件，在首个偏离点保存证据。恢复后重放同一提交，不能以重新构建掩盖制品差异。

### 2.3 开源脚手架案例剖析

脚手架是项目初始化事务：参数校验后选择版本化模板，渲染到临时位置，检查冲突再提交文件。Yeoman 是原书实现载体，现代替代不得改变确定生成与失败回滚目标。

实验从“读取项目意图”开始：固定源码提交、依赖锁、配置和目标环境，先预测文件树与制品引用；每次只改变一个模板、模块、缓存、网络或权限条件，在首个偏离点保存证据。恢复后重放同一提交，不能以重新构建掩盖制品差异。

### 2.4 集成Yeoman封装脚手架方案

脚手架是项目初始化事务：参数校验后选择版本化模板，渲染到临时位置，检查冲突再提交文件。Yeoman 是原书实现载体，现代替代不得改变确定生成与失败回滚目标。

实验从“校验环境参数”开始：固定源码提交、依赖锁、配置和目标环境，先预测文件树与制品引用；每次只改变一个模板、模块、缓存、网络或权限条件，在首个偏离点保存证据。恢复后重放同一提交，不能以重新构建掩盖制品差异。

### 2.4.1 封装脚手架方案

脚手架是项目初始化事务：参数校验后选择版本化模板，渲染到临时位置，检查冲突再提交文件。Yeoman 是原书实现载体，现代替代不得改变确定生成与失败回滚目标。

实验从“选择模板版本”开始：固定源码提交、依赖锁、配置和目标环境，先预测文件树与制品引用；每次只改变一个模板、模块、缓存、网络或权限条件，在首个偏离点保存证据。恢复后重放同一提交，不能以重新构建掩盖制品差异。

### 2.4.2 集成到工程化体系中

工程化源于协作规模和交付复杂度。Node.js 让前端团队能用 JavaScript 编排工具，前后端分离明确接口与制品责任；衡量应落在反馈时间、失败率、可追溯性和交付频率。

实验从“渲染文件事务”开始：固定源码提交、依赖锁、配置和目标环境，先预测文件树与制品引用；每次只改变一个模板、模块、缓存、网络或权限条件，在首个偏离点保存证据。恢复后重放同一提交，不能以重新构建掩盖制品差异。

### 2.5 总结

2.5 总结 需要放回“读取项目意图、校验环境参数、选择模板版本、渲染文件事务、安装并验证依赖、移交项目所有权”的完整交付链，明确输入、制品、环境、所有者和失败出口。

实验从“安装并验证依赖”开始：固定源码提交、依赖锁、配置和目标环境，先预测文件树与制品引用；每次只改变一个模板、模块、缓存、网络或权限条件，在首个偏离点保存证据。恢复后重放同一提交，不能以重新构建掩盖制品差异。

## 制品与失败矩阵

| 对象       | 正常状态       | 常见边界             | 失败证据       | 恢复条件       |
| ---------- | -------------- | -------------------- | -------------- | -------------- |
| 模板与源码 | 文件树确定     | 冲突、平台差异       | 差异清单       | 不覆盖用户文件 |
| 模块与构建 | 摘要可追溯     | 插件失败、依赖漂移   | 模块图与日志   | 同输入同制品   |
| 本地反馈   | 增量更新准确   | HMR残留、Mock偏差    | 状态与契约     | 与生产差异明确 |
| 部署与缓存 | 资源入口同版本 | 网络、权限、并发发布 | 制品与入口版本 | 可探测并回滚   |

## 分步可视实验

<Stepper>
  <Step title="1. 建立源码与制品地图">
    先预测模板、模块图、制品和入口引用，再启动固定配置样本。
    <Feng02ScaffoldingMapLab />
  </Step>
  <Step title="2. 注入单一交付故障">
    改变模板、模块、缓存、网络或权限条件，在首个状态分叉处保存证据。
    <Feng02ScaffoldingExperimentLab />
  </Step>
  <Step title="3. 使用同一制品恢复">
    删除故障，重放同一提交，确认制品、资源、入口和回滚状态全部收敛。
    <Feng02ScaffoldingEvidenceLab />
  </Step>
</Stepper>

## 最小可运行实验

```js
export async function createProject({ template, target, render }) {
  const staging = target + ".staging";
  await render({ template, target: staging });
  await verifyTree(staging);
  await commitWithoutOverwrite(staging, target);
}
```

```yaml
book: 前端工程化：体系设计与实践
edition: 2018-01
page: feng-02-scaffolding
sample: normal | boundary | failure | recovery
source_commit_fixed: true
artifact_digest_known: true
build_once: true
rollback_ready: true
```

```text
freeze source + lockfile + config + target
predict file tree + module graph + asset URLs
change exactly one delivery condition
stop at the first divergent state
reuse the same artifact during recovery
verify entry, assets, probes, and rollback
```

## 常见误区与故障注入

<Callout type="trap">
  **误区一：安装构建工具就等于完成工程化。**
  工具不自动提供模板确定性、制品同一性、部署互斥、缓存顺序和回滚证据；必须把责任与状态组织成闭环。
</Callout>

<Callout type="trap">
  **误区二：用当代热门工具覆盖原书目录。**
  Vite、模块联邦、TypeScript、现代测试与监控可以作为扩展，但原书正式主线仍是简史、脚手架、构建、本地服务器、部署、工作流和未来。
</Callout>

## 四类样本与验收

| 样本 | 注入方式                   | 必查证据             | 通过条件       |
| ---- | -------------------------- | -------------------- | -------------- |
| 正常 | 固定提交与依赖             | 文件树、摘要、URL    | 结果可重放     |
| 边界 | 大模块、慢网络、并发变更   | 增量范围、缓存、队列 | 时间与资源受控 |
| 失败 | 插件、权限、上传或探测失败 | 首个错误与所有者     | 阻断不污染环境 |
| 恢复 | 同制品重试或回滚           | 入口、资源、用户探测 | 版本重新一致   |

<Feng02ScaffoldingEvidenceLab />

目录证据：第2章 脚手架、2.1 脚手架的功能和本质、2.2 脚手架在前端工程中的角色和特征、2.2.1 用完即弃的发起者角色、2.2.2 局限于本地的执行环境、2.2.3 多样性的实现模式、2.3 开源脚手架案例剖析、2.4 集成Yeoman封装脚手架方案、2.4.1 封装脚手架方案、2.4.2 集成到工程化体系中、2.5 总结。本页按完整公开目录独立教学重构，不复制原书正文；11 个条目全部进入源码、机制、制品、失败和回滚证据链。

## 练习

<Exercises>

**问题 1** 为什么不能只用构建成功验收第 2 章 脚手架？

<Answer>
  构建成功不显示模板漂移、测试与生产制品差异、资源缓存错配、并发部署覆盖和回滚状态。必须注入单一失败并用同一制品恢复。
</Answer>

**问题 2** 如何把本页目录变成可执行证据？

<Answer>
  依次登记“第2章 脚手架、2.1 脚手架的功能和本质、2.2
  脚手架在前端工程中的角色和特征、2.2.1 用完即弃的发起者角色、2.2.2
  局限于本地的执行环境、2.2.3 多样性的实现模式、2.3 开源脚手架案例剖析、2.4
  集成Yeoman封装脚手架方案、2.4.1 封装脚手架方案、2.4.2 集成到工程化体系中、2.5
  总结”，为每项写出源码输入、配置版本、输出制品、环境所有者、失败出口、缓存与回滚条件，再回放正常、边界、失败和恢复样本。
</Answer>

**问题 3** 本页最小通过条件是什么？

<Answer>
  同一输入与模板版本生成确定文件树，冲突不会静默覆盖用户文件，失败可回滚，生成后项目不依赖脚手架进程继续运行。
  同时，源码、制品和部署版本可追溯，最终没有半发布资源、错误入口、未清理本地状态或无法恢复的旧版本。
</Answer>

</Exercises>

## 小结

- 脚手架：对应“读取项目意图”的核心观察量。
- 模板：对应“校验环境参数”的核心观察量。
- 生成器：对应“选择模板版本”的核心观察量。
- 本地执行：对应“渲染文件事务”的核心观察量。
- 用完即弃：对应“安装并验证依赖”的核心观察量。
- 11 个本页公开目录条目已全部映射到交付链与版本账本。
- 构建输出只是证据之一，制品同一性、环境状态与回滚共同决定是否通过。

## 术语表

<Glossary>
  <GlossaryItem term="脚手架">
    脚手架
    对应“读取项目意图”，验收时连接源码身份、配置契约、制品摘要、环境切换、失败传播和回滚状态。
  </GlossaryItem>
  <GlossaryItem term="模板">
    模板
    对应“校验环境参数”，验收时连接源码身份、配置契约、制品摘要、环境切换、失败传播和回滚状态。
  </GlossaryItem>
  <GlossaryItem term="生成器">
    生成器
    对应“选择模板版本”，验收时连接源码身份、配置契约、制品摘要、环境切换、失败传播和回滚状态。
  </GlossaryItem>
  <GlossaryItem term="本地执行">
    本地执行
    对应“渲染文件事务”，验收时连接源码身份、配置契约、制品摘要、环境切换、失败传播和回滚状态。
  </GlossaryItem>
  <GlossaryItem term="用完即弃">
    用完即弃
    对应“安装并验证依赖”，验收时连接源码身份、配置契约、制品摘要、环境切换、失败传播和回滚状态。
  </GlossaryItem>
</Glossary>

<Attribution
  adaptedFrom="周俊鹏 · 前端工程化：体系设计与实践 · 电子工业出版社 · 2018-01 · 完整公开目录"
  adaptedUrl="https://book.douban.com/subject/27605366/"
/>
