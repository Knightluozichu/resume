---
title: "第 3 章 构建"
type: A
section: "前端工程化 · 构建"
order: 3
description: "覆盖配置 API、Babel、CSS/PostCSS、模块化、缓存增量更新、资源定位和 webpack 构建方案。"
demo: true
math: false
sourceUrl: "https://book.douban.com/subject/27605366/"
draft: false
---


## 直觉起点

、、、、共同构成本页的观察坐标。构建把源模块、样式和静态资源转换成可部署制品，同时维持模块图、兼容目标、内容哈希和资源地址的一致性；配置
API 是工程团队的稳定契约。
工程化不是工具数量，而是把重复操作变成可验证流程，让变更更快获得反馈、失败更早暴露、制品可以追溯、发布可以回滚。

## 六阶段交付链

### 1. 读取配置契约

固定源码提交、依赖锁、工程配置、模板版本和目标环境，先预测输出文件与资源引用。没有输入身份的构建无法复现。

### 2. 解析模块依赖

明确源码、生成目录、模块图、静态资源、入口文档与部署环境的所有者。共享环境变更必须有队列或互斥边界。

### 3. 转换脚本样式

每个阶段只产生一个可验证输出，构建制品以摘要签发。后续测试与部署消费同一制品，不能按环境重新编译。

### 4. 切分并命名制品

注入单一边界：模板冲突、模块失败、缓存错配、HMR 残留、网络中断或权限拒绝，停止在首个偏离点。

### 5. 注入资源定位

区分 2018 年 webpack、Yeoman、Apache 语境与当前工具。现代替代保持原问题身份，并重新验证配置、缓存和回滚行为。

### 6. 验证缓存与增量

删除故障并重放同一提交，核对文件树、制品摘要、资源 URL、入口版本与用户探测；旧版本可恢复才完成签发。

## 核心机制深挖

构建把源模块、样式和静态资源转换成可部署制品，同时维持模块图、兼容目标、内容哈希和资源地址的一致性；配置 API 是工程团队的稳定契约。 每次变更统一登记六类证据：**源码身份、配置版本、依赖解析、制品摘要、环境状态、回滚条件**。模板不确定会导致项目起点漂移，构建两次会让测试制品与生产制品不同，缓存地址错误会造成跨版本资源，权限与队列缺失会让并发部署互相覆盖。

本页签发不变量是：**同一源码、锁文件和环境得到可追溯制品，资源 URL 指向同版本内容，增量发布不会让 HTML、脚本和样式跨版本错配。**。流程必须回答谁发起、消费什么输入、产生哪个制品、失败由谁观察、共享环境怎样互斥、缓存何时失效，以及旧版本如何恢复。构建成功或页面可打开只是证据的一部分。

## 2018 版与现代前端的版本账本

原书出版于 2018 年，以 Node.js、webpack、Babel、PostCSS、Yeoman、webpack-dev-middleware、Livereload/HMR、Apache、GitFlow 和 WebHook 为主要实例。重构保留 7 章 98 个目录条目，不用 Vite、Module Federation、TypeScript、Playwright、RUM 等后来主题替换原书身份；这些能力只在现代对照中说明如何延续相同工程目标。

版本账本至少记录原书工具、当时用途、当前状态、现代替代、配置差异、制品差异和迁移退出条件。稳定原则是单一制品、内容寻址、资源先于入口、接口契约和可回滚；易变部分是插件 API、开发服务器协议、分支策略、CI 平台与 CDN 配置。

## 完整公开目录逐项讲解

### 第3章 构建

第3章 构建 需要放回“读取配置契约、解析模块依赖、转换脚本样式、切分并命名制品、注入资源定位、验证缓存与增量”的完整交付链，明确输入、制品、环境、所有者和失败出口。

实验从“读取配置契约”开始：固定源码提交、依赖锁、配置和目标环境，先预测文件树与制品引用；每次只改变一个模板、模块、缓存、网络或权限条件，在首个偏离点保存证据。恢复后重放同一提交，不能以重新构建掩盖制品差异。

### 3.1 构建功能解决的问题

3.1 构建功能解决的问题 需要放回“读取配置契约、解析模块依赖、转换脚本样式、切分并命名制品、注入资源定位、验证缓存与增量”的完整交付链，明确输入、制品、环境、所有者和失败出口。

实验从“解析模块依赖”开始：固定源码提交、依赖锁、配置和目标环境，先预测文件树与制品引用；每次只改变一个模板、模块、缓存、网络或权限条件，在首个偏离点保存证据。恢复后重放同一提交，不能以重新构建掩盖制品差异。

### 3.2 配置API设计原则和编程范式约束

配置 API 面向团队保持稳定，Babel 根据目标环境转换语法并注入所需能力。语法转换、运行时辅助和 polyfill 是不同责任，构建结果必须记录目标浏览器与配置版本。

实验从“转换脚本样式”开始：固定源码提交、依赖锁、配置和目标环境，先预测文件树与制品引用；每次只改变一个模板、模块、缓存、网络或权限条件，在首个偏离点保存证据。恢复后重放同一提交，不能以重新构建掩盖制品差异。

### 3.2.1 配置API设计

配置 API 面向团队保持稳定，Babel 根据目标环境转换语法并注入所需能力。语法转换、运行时辅助和 polyfill 是不同责任，构建结果必须记录目标浏览器与配置版本。

实验从“切分并命名制品”开始：固定源码提交、依赖锁、配置和目标环境，先预测文件树与制品引用；每次只改变一个模板、模块、缓存、网络或权限条件，在首个偏离点保存证据。恢复后重放同一提交，不能以重新构建掩盖制品差异。

### 3.2.2 编程范式约束

配置 API 面向团队保持稳定，Babel 根据目标环境转换语法并注入所需能力。语法转换、运行时辅助和 polyfill 是不同责任，构建结果必须记录目标浏览器与配置版本。

实验从“注入资源定位”开始：固定源码提交、依赖锁、配置和目标环境，先预测文件树与制品引用；每次只改变一个模板、模块、缓存、网络或权限条件，在首个偏离点保存证据。恢复后重放同一提交，不能以重新构建掩盖制品差异。

### 3.3 ECMAScript与Babel

配置 API 面向团队保持稳定，Babel 根据目标环境转换语法并注入所需能力。语法转换、运行时辅助和 polyfill 是不同责任，构建结果必须记录目标浏览器与配置版本。

实验从“验证缓存与增量”开始：固定源码提交、依赖锁、配置和目标环境，先预测文件树与制品引用；每次只改变一个模板、模块、缓存、网络或权限条件，在首个偏离点保存证据。恢复后重放同一提交，不能以重新构建掩盖制品差异。

### 3.3.1 ECMAScript发展史

配置 API 面向团队保持稳定，Babel 根据目标环境转换语法并注入所需能力。语法转换、运行时辅助和 polyfill 是不同责任，构建结果必须记录目标浏览器与配置版本。

实验从“读取配置契约”开始：固定源码提交、依赖锁、配置和目标环境，先预测文件树与制品引用；每次只改变一个模板、模块、缓存、网络或权限条件，在首个偏离点保存证据。恢复后重放同一提交，不能以重新构建掩盖制品差异。

### 3.3.2 ES6的跨时代意义

3.3.2 ES6的跨时代意义 需要放回“读取配置契约、解析模块依赖、转换脚本样式、切分并命名制品、注入资源定位、验证缓存与增量”的完整交付链，明确输入、制品、环境、所有者和失败出口。

实验从“解析模块依赖”开始：固定源码提交、依赖锁、配置和目标环境，先预测文件树与制品引用；每次只改变一个模板、模块、缓存、网络或权限条件，在首个偏离点保存证据。恢复后重放同一提交，不能以重新构建掩盖制品差异。

### 3.3.3 Babel——真正意义的JavaScript编译

配置 API 面向团队保持稳定，Babel 根据目标环境转换语法并注入所需能力。语法转换、运行时辅助和 polyfill 是不同责任，构建结果必须记录目标浏览器与配置版本。

实验从“转换脚本样式”开始：固定源码提交、依赖锁、配置和目标环境，先预测文件树与制品引用；每次只改变一个模板、模块、缓存、网络或权限条件，在首个偏离点保存证据。恢复后重放同一提交，不能以重新构建掩盖制品差异。

### 3.3.4 结合webpack与Babel实现JavaScript构建

配置 API 面向团队保持稳定，Babel 根据目标环境转换语法并注入所需能力。语法转换、运行时辅助和 polyfill 是不同责任，构建结果必须记录目标浏览器与配置版本。

实验从“切分并命名制品”开始：固定源码提交、依赖锁、配置和目标环境，先预测文件树与制品引用；每次只改变一个模板、模块、缓存、网络或权限条件，在首个偏离点保存证据。恢复后重放同一提交，不能以重新构建掩盖制品差异。

### 3.4 CSS预编译与PostCSS

CSS 预编译器扩展作者语法，PostCSS 以插件处理语法树。顺序会影响结果，自动 Sprites 还要保持图片坐标、哈希与引用一致；现代替代仍需验证产物与缓存。

实验从“注入资源定位”开始：固定源码提交、依赖锁、配置和目标环境，先预测文件树与制品引用；每次只改变一个模板、模块、缓存、网络或权限条件，在首个偏离点保存证据。恢复后重放同一提交，不能以重新构建掩盖制品差异。

### 3.4.1 CSS的缺陷

CSS 预编译器扩展作者语法，PostCSS 以插件处理语法树。顺序会影响结果，自动 Sprites 还要保持图片坐标、哈希与引用一致；现代替代仍需验证产物与缓存。

实验从“验证缓存与增量”开始：固定源码提交、依赖锁、配置和目标环境，先预测文件树与制品引用；每次只改变一个模板、模块、缓存、网络或权限条件，在首个偏离点保存证据。恢复后重放同一提交，不能以重新构建掩盖制品差异。

### 3.4.2 CSS预编译器

CSS 预编译器扩展作者语法，PostCSS 以插件处理语法树。顺序会影响结果，自动 Sprites 还要保持图片坐标、哈希与引用一致；现代替代仍需验证产物与缓存。

实验从“读取配置契约”开始：固定源码提交、依赖锁、配置和目标环境，先预测文件树与制品引用；每次只改变一个模板、模块、缓存、网络或权限条件，在首个偏离点保存证据。恢复后重放同一提交，不能以重新构建掩盖制品差异。

### 3.4.3 PostCSS

CSS 预编译器扩展作者语法，PostCSS 以插件处理语法树。顺序会影响结果，自动 Sprites 还要保持图片坐标、哈希与引用一致；现代替代仍需验证产物与缓存。

实验从“解析模块依赖”开始：固定源码提交、依赖锁、配置和目标环境，先预测文件树与制品引用；每次只改变一个模板、模块、缓存、网络或权限条件，在首个偏离点保存证据。恢复后重放同一提交，不能以重新构建掩盖制品差异。

### 3.4.4 webpack结合预编译与PostCSS实现CSS构建

CSS 预编译器扩展作者语法，PostCSS 以插件处理语法树。顺序会影响结果，自动 Sprites 还要保持图片坐标、哈希与引用一致；现代替代仍需验证产物与缓存。

实验从“转换脚本样式”开始：固定源码提交、依赖锁、配置和目标环境，先预测文件树与制品引用；每次只改变一个模板、模块、缓存、网络或权限条件，在首个偏离点保存证据。恢复后重放同一提交，不能以重新构建掩盖制品差异。

### 3.4.5 案例：自动生成CSS Sprites功能实现

CSS 预编译器扩展作者语法，PostCSS 以插件处理语法树。顺序会影响结果，自动 Sprites 还要保持图片坐标、哈希与引用一致；现代替代仍需验证产物与缓存。

实验从“切分并命名制品”开始：固定源码提交、依赖锁、配置和目标环境，先预测文件树与制品引用；每次只改变一个模板、模块、缓存、网络或权限条件，在首个偏离点保存证据。恢复后重放同一提交，不能以重新构建掩盖制品差异。

### 3.5 模块化开发

模块化定义依赖与导出，组件化聚合视图、样式和行为，构建器据模块图切分制品。循环依赖、副作用和动态导入都会影响优化，不能只以打包成功验收。

实验从“注入资源定位”开始：固定源码提交、依赖锁、配置和目标环境，先预测文件树与制品引用；每次只改变一个模板、模块、缓存、网络或权限条件，在首个偏离点保存证据。恢复后重放同一提交，不能以重新构建掩盖制品差异。

### 3.5.1 模块化与组件化

模块化定义依赖与导出，组件化聚合视图、样式和行为，构建器据模块图切分制品。循环依赖、副作用和动态导入都会影响优化，不能只以打包成功验收。

实验从“验证缓存与增量”开始：固定源码提交、依赖锁、配置和目标环境，先预测文件树与制品引用；每次只改变一个模板、模块、缓存、网络或权限条件，在首个偏离点保存证据。恢复后重放同一提交，不能以重新构建掩盖制品差异。

### 3.5.2 模块化与工程化

工程化源于协作规模和交付复杂度。Node.js 让前端团队能用 JavaScript 编排工具，前后端分离明确接口与制品责任；衡量应落在反馈时间、失败率、可追溯性和交付频率。

实验从“读取配置契约”开始：固定源码提交、依赖锁、配置和目标环境，先预测文件树与制品引用；每次只改变一个模板、模块、缓存、网络或权限条件，在首个偏离点保存证据。恢复后重放同一提交，不能以重新构建掩盖制品差异。

### 3.5.3 模块化开发的价值

模块化定义依赖与导出，组件化聚合视图、样式和行为，构建器据模块图切分制品。循环依赖、副作用和动态导入都会影响优化，不能只以打包成功验收。

实验从“解析模块依赖”开始：固定源码提交、依赖锁、配置和目标环境，先预测文件树与制品引用；每次只改变一个模板、模块、缓存、网络或权限条件，在首个偏离点保存证据。恢复后重放同一提交，不能以重新构建掩盖制品差异。

### 3.5.4 前端模块化发展史

模块化定义依赖与导出，组件化聚合视图、样式和行为，构建器据模块图切分制品。循环依赖、副作用和动态导入都会影响优化，不能只以打包成功验收。

实验从“转换脚本样式”开始：固定源码提交、依赖锁、配置和目标环境，先预测文件树与制品引用；每次只改变一个模板、模块、缓存、网络或权限条件，在首个偏离点保存证据。恢复后重放同一提交，不能以重新构建掩盖制品差异。

### 3.5.5 webpack模块化构建

模块化定义依赖与导出，组件化聚合视图、样式和行为，构建器据模块图切分制品。循环依赖、副作用和动态导入都会影响优化，不能只以打包成功验收。

实验从“切分并命名制品”开始：固定源码提交、依赖锁、配置和目标环境，先预测文件树与制品引用；每次只改变一个模板、模块、缓存、网络或权限条件，在首个偏离点保存证据。恢复后重放同一提交，不能以重新构建掩盖制品差异。

### 3.6 增量更新与缓存

内容哈希把资源身份绑定到字节，入口文档注入最终 URL。资源先发布、入口后切换，旧资源在回滚窗口内保留；覆盖同名文件会让 CDN 与浏览器出现跨版本错配。

实验从“注入资源定位”开始：固定源码提交、依赖锁、配置和目标环境，先预测文件树与制品引用；每次只改变一个模板、模块、缓存、网络或权限条件，在首个偏离点保存证据。恢复后重放同一提交，不能以重新构建掩盖制品差异。

### 3.6.1 HTTP缓存策略

内容哈希把资源身份绑定到字节，入口文档注入最终 URL。资源先发布、入口后切换，旧资源在回滚窗口内保留；覆盖同名文件会让 CDN 与浏览器出现跨版本错配。

实验从“验证缓存与增量”开始：固定源码提交、依赖锁、配置和目标环境，先预测文件树与制品引用；每次只改变一个模板、模块、缓存、网络或权限条件，在首个偏离点保存证据。恢复后重放同一提交，不能以重新构建掩盖制品差异。

### 3.6.2 覆盖更新与增量更新

内容哈希把资源身份绑定到字节，入口文档注入最终 URL。资源先发布、入口后切换，旧资源在回滚窗口内保留；覆盖同名文件会让 CDN 与浏览器出现跨版本错配。

实验从“读取配置契约”开始：固定源码提交、依赖锁、配置和目标环境，先预测文件树与制品引用；每次只改变一个模板、模块、缓存、网络或权限条件，在首个偏离点保存证据。恢复后重放同一提交，不能以重新构建掩盖制品差异。

### 3.6.3 按需加载与多模块架构场景下的增量更新

工程化源于协作规模和交付复杂度。Node.js 让前端团队能用 JavaScript 编排工具，前后端分离明确接口与制品责任；衡量应落在反馈时间、失败率、可追溯性和交付频率。

实验从“解析模块依赖”开始：固定源码提交、依赖锁、配置和目标环境，先预测文件树与制品引用；每次只改变一个模板、模块、缓存、网络或权限条件，在首个偏离点保存证据。恢复后重放同一提交，不能以重新构建掩盖制品差异。

### 3.6.4 webpack实现增量更新构建方案

内容哈希把资源身份绑定到字节，入口文档注入最终 URL。资源先发布、入口后切换，旧资源在回滚窗口内保留；覆盖同名文件会让 CDN 与浏览器出现跨版本错配。

实验从“转换脚本样式”开始：固定源码提交、依赖锁、配置和目标环境，先预测文件树与制品引用；每次只改变一个模板、模块、缓存、网络或权限条件，在首个偏离点保存证据。恢复后重放同一提交，不能以重新构建掩盖制品差异。

### 3.7 资源定位

内容哈希把资源身份绑定到字节，入口文档注入最终 URL。资源先发布、入口后切换，旧资源在回滚窗口内保留；覆盖同名文件会让 CDN 与浏览器出现跨版本错配。

实验从“切分并命名制品”开始：固定源码提交、依赖锁、配置和目标环境，先预测文件树与制品引用；每次只改变一个模板、模块、缓存、网络或权限条件，在首个偏离点保存证据。恢复后重放同一提交，不能以重新构建掩盖制品差异。

### 3.7.1 资源定位的历史变迁

工程化源于协作规模和交付复杂度。Node.js 让前端团队能用 JavaScript 编排工具，前后端分离明确接口与制品责任；衡量应落在反馈时间、失败率、可追溯性和交付频率。

实验从“注入资源定位”开始：固定源码提交、依赖锁、配置和目标环境，先预测文件树与制品引用；每次只改变一个模板、模块、缓存、网络或权限条件，在首个偏离点保存证据。恢复后重放同一提交，不能以重新构建掩盖制品差异。

### 3.7.2 常规的资源定位思维

内容哈希把资源身份绑定到字节，入口文档注入最终 URL。资源先发布、入口后切换，旧资源在回滚窗口内保留；覆盖同名文件会让 CDN 与浏览器出现跨版本错配。

实验从“验证缓存与增量”开始：固定源码提交、依赖锁、配置和目标环境，先预测文件树与制品引用；每次只改变一个模板、模块、缓存、网络或权限条件，在首个偏离点保存证据。恢复后重放同一提交，不能以重新构建掩盖制品差异。

### 3.7.3 webpack的逆向注入模式

内容哈希把资源身份绑定到字节，入口文档注入最终 URL。资源先发布、入口后切换，旧资源在回滚窗口内保留；覆盖同名文件会让 CDN 与浏览器出现跨版本错配。

实验从“读取配置契约”开始：固定源码提交、依赖锁、配置和目标环境，先预测文件树与制品引用；每次只改变一个模板、模块、缓存、网络或权限条件，在首个偏离点保存证据。恢复后重放同一提交，不能以重新构建掩盖制品差异。

### 3.8 总结

3.8 总结 需要放回“读取配置契约、解析模块依赖、转换脚本样式、切分并命名制品、注入资源定位、验证缓存与增量”的完整交付链，明确输入、制品、环境、所有者和失败出口。

实验从“解析模块依赖”开始：固定源码提交、依赖锁、配置和目标环境，先预测文件树与制品引用；每次只改变一个模板、模块、缓存、网络或权限条件，在首个偏离点保存证据。恢复后重放同一提交，不能以重新构建掩盖制品差异。

## 制品与失败矩阵

| 对象       | 正常状态       | 常见边界             | 失败证据       | 恢复条件       |
| ---------- | -------------- | -------------------- | -------------- | -------------- |
| 模板与源码 | 文件树确定     | 冲突、平台差异       | 差异清单       | 不覆盖用户文件 |
| 模块与构建 | 摘要可追溯     | 插件失败、依赖漂移   | 模块图与日志   | 同输入同制品   |
| 本地反馈   | 增量更新准确   | HMR残留、Mock偏差    | 状态与契约     | 与生产差异明确 |
| 部署与缓存 | 资源入口同版本 | 网络、权限、并发发布 | 制品与入口版本 | 可探测并回滚   |

## 最小可运行实验

```js
export const buildIdentity = {
  commit: process.env.GIT_SHA,
  lockDigest: process.env.LOCK_DIGEST,
  configDigest: process.env.CONFIG_DIGEST,
  target: "production",
};
```

```yaml
book: 前端工程化：体系设计与实践
edition: 2018-01
page: feng-03-build
sample: normal | boundary | failure | recovery
source_commit_fixed: true
artifact_digest_known: true
build_once: true
rollback_ready: true
```

```text
freeze source + lockfile + config + target
predict file tree + module graph + asset URLs
change exactly one delivery condition
stop at the first divergent state
reuse the same artifact during recovery
verify entry, assets, probes, and rollback
```

## 常见误区与故障注入


## 四类样本与验收

| 样本 | 注入方式                   | 必查证据             | 通过条件       |
| ---- | -------------------------- | -------------------- | -------------- |
| 正常 | 固定提交与依赖             | 文件树、摘要、URL    | 结果可重放     |
| 边界 | 大模块、慢网络、并发变更   | 增量范围、缓存、队列 | 时间与资源受控 |
| 失败 | 插件、权限、上传或探测失败 | 首个错误与所有者     | 阻断不污染环境 |
| 恢复 | 同制品重试或回滚           | 入口、资源、用户探测 | 版本重新一致   |

目录证据：第3章 构建、3.1 构建功能解决的问题、3.2 配置API设计原则和编程范式约束、3.2.1 配置API设计、3.2.2 编程范式约束、3.3 ECMAScript与Babel、3.3.1 ECMAScript发展史、3.3.2 ES6的跨时代意义、3.3.3 Babel——真正意义的JavaScript编译、3.3.4 结合webpack与Babel实现JavaScript构建、3.4 CSS预编译与PostCSS、3.4.1 CSS的缺陷、3.4.2 CSS预编译器、3.4.3 PostCSS、3.4.4 webpack结合预编译与PostCSS实现CSS构建、3.4.5 案例：自动生成CSS Sprites功能实现、3.5 模块化开发、3.5.1 模块化与组件化、3.5.2 模块化与工程化、3.5.3 模块化开发的价值、3.5.4 前端模块化发展史、3.5.5 webpack模块化构建、3.6 增量更新与缓存、3.6.1 HTTP缓存策略、3.6.2 覆盖更新与增量更新、3.6.3 按需加载与多模块架构场景下的增量更新、3.6.4 webpack实现增量更新构建方案、3.7 资源定位、3.7.1 资源定位的历史变迁、3.7.2 常规的资源定位思维、3.7.3 webpack的逆向注入模式、3.8 总结。本页按完整公开目录独立教学重构，不复制原书正文；32 个条目全部进入源码、机制、制品、失败和回滚证据链。

## 练习


## 小结

- 配置 API：对应“读取配置契约”的核心观察量。
- Babel：对应“解析模块依赖”的核心观察量。
- PostCSS：对应“转换脚本样式”的核心观察量。
- 模块图：对应“切分并命名制品”的核心观察量。
- 内容哈希：对应“注入资源定位”的核心观察量。
- 32 个本页公开目录条目已全部映射到交付链与版本账本。
- 构建输出只是证据之一，制品同一性、环境状态与回滚共同决定是否通过。

## 术语表


