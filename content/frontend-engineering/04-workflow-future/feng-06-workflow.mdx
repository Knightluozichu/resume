---
title: "第 6 章 工作流"
type: A
section: "前端工程化 · 工作流与未来"
order: 6
description: "连接本地工作流、测试沙箱、GitFlow、WebHook、自动构建、持续集成与持续交付。"
demo: true
math: false
sourceUrl: "https://book.douban.com/subject/27605366/"
draft: true
---

import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第 6 章 工作流的核心职责，并说出它在原书 7 章体系中的位置
- 能描述每个公开目录条目的源码输入、配置契约、制品、环境与回滚状态
- 能实现最小交付样本，并注入模板、模块、缓存、网络或权限故障定位首个偏离点
- 能比较正常、边界、失败和恢复轨迹，判断 2018 年历史工具与当前替代是否被清楚区分

</Objectives>

## 直觉起点

<Term def="二次构建 是“第 6 章 工作流”的第 1 个交付坐标，对应“提交版本化源码”；必须同时记录源码、制品、环境、失败与回滚证据。">二次构建</Term>、<Term def="测试沙箱 是“第 6 章 工作流”的第 2 个交付坐标，对应“运行本地快速检查”；必须同时记录源码、制品、环境、失败与回滚证据。">测试沙箱</Term>、<Term def="GitFlow 是“第 6 章 工作流”的第 3 个交付坐标，对应“进入测试沙箱”；必须同时记录源码、制品、环境、失败与回滚证据。">GitFlow</Term>、<Term def="WebHook 是“第 6 章 工作流”的第 4 个交付坐标，对应“触发云端构建”；必须同时记录源码、制品、环境、失败与回滚证据。">WebHook</Term>、<Term def="持续交付 是“第 6 章 工作流”的第 5 个交付坐标，对应“签发唯一制品”；必须同时记录源码、制品、环境、失败与回滚证据。">持续交付</Term>
共同构成本页的观察坐标。工作流把个人操作转化为团队状态机：源码只对应一次构建，测试在隔离沙箱运行，版本变更触发
WebHook，持续集成签发可部署制品，持续交付控制推广。
工程化不是工具数量，而是把重复操作变成可验证流程，让变更更快获得反馈、失败更早暴露、制品可以追溯、发布可以回滚。

## 六阶段交付链

### 1. 提交版本化源码

固定源码提交、依赖锁、工程配置、模板版本和目标环境，先预测输出文件与资源引用。没有输入身份的构建无法复现。

### 2. 运行本地快速检查

明确源码、生成目录、模块图、静态资源、入口文档与部署环境的所有者。共享环境变更必须有队列或互斥边界。

### 3. 进入测试沙箱

每个阶段只产生一个可验证输出，构建制品以摘要签发。后续测试与部署消费同一制品，不能按环境重新编译。

### 4. 触发云端构建

注入单一边界：模板冲突、模块失败、缓存错配、HMR 残留、网络中断或权限拒绝，停止在首个偏离点。

### 5. 签发唯一制品

区分 2018 年 webpack、Yeoman、Apache 语境与当前工具。现代替代保持原问题身份，并重新验证配置、缓存和回滚行为。

### 6. 逐环境持续交付

删除故障并重放同一提交，核对文件树、制品摘要、资源 URL、入口版本与用户探测；旧版本可恢复才完成签发。

## 核心机制深挖

工作流把个人操作转化为团队状态机：源码只对应一次构建，测试在隔离沙箱运行，版本变更触发 WebHook，持续集成签发可部署制品，持续交付控制推广。 每次变更统一登记六类证据：**源码身份、配置版本、依赖解析、制品摘要、环境状态、回滚条件**。模板不确定会导致项目起点漂移，构建两次会让测试制品与生产制品不同，缓存地址错误会造成跨版本资源，权限与队列缺失会让并发部署互相覆盖。

本页签发不变量是：**每个提交只对应可追溯制品，环境间推广不重新构建，自动化失败阻断发布且可重试，分支流程不长期隐藏集成风险。**。流程必须回答谁发起、消费什么输入、产生哪个制品、失败由谁观察、共享环境怎样互斥、缓存何时失效，以及旧版本如何恢复。构建成功或页面可打开只是证据的一部分。

## 2018 版与现代前端的版本账本

原书出版于 2018 年，以 Node.js、webpack、Babel、PostCSS、Yeoman、webpack-dev-middleware、Livereload/HMR、Apache、GitFlow 和 WebHook 为主要实例。重构保留 7 章 98 个目录条目，不用 Vite、Module Federation、TypeScript、Playwright、RUM 等后来主题替换原书身份；这些能力只在现代对照中说明如何延续相同工程目标。

版本账本至少记录原书工具、当时用途、当前状态、现代替代、配置差异、制品差异和迁移退出条件。稳定原则是单一制品、内容寻址、资源先于入口、接口契约和可回滚；易变部分是插件 API、开发服务器协议、分支策略、CI 平台与 CDN 配置。

## 完整公开目录逐项讲解

### 第6章 工作流

第6章 工作流 需要放回“提交版本化源码、运行本地快速检查、进入测试沙箱、触发云端构建、签发唯一制品、逐环境持续交付”的完整交付链，明确输入、制品、环境、所有者和失败出口。

实验从“提交版本化源码”开始：固定源码提交、依赖锁、配置和目标环境，先预测文件树与制品引用；每次只改变一个模板、模块、缓存、网络或权限条件，在首个偏离点保存证据。恢复后重放同一提交，不能以重新构建掩盖制品差异。

### 6.1 本地工作流

工作流将提交、检查、构建、测试、签发和推广组织为状态机。环境间重新构建会失去制品同一性；WebHook 必须鉴权、幂等并保留提交与流水线关联。

实验从“运行本地快速检查”开始：固定源码提交、依赖锁、配置和目标环境，先预测文件树与制品引用；每次只改变一个模板、模块、缓存、网络或权限条件，在首个偏离点保存证据。恢复后重放同一提交，不能以重新构建掩盖制品差异。

### 6.1.1 二次构建的隐患

工作流将提交、检查、构建、测试、签发和推广组织为状态机。环境间重新构建会失去制品同一性；WebHook 必须鉴权、幂等并保留提交与流水线关联。

实验从“进入测试沙箱”开始：固定源码提交、依赖锁、配置和目标环境，先预测文件树与制品引用；每次只改变一个模板、模块、缓存、网络或权限条件，在首个偏离点保存证据。恢复后重放同一提交，不能以重新构建掩盖制品差异。

### 6.1.2 代码分离与测试沙箱

工作流将提交、检查、构建、测试、签发和推广组织为状态机。环境间重新构建会失去制品同一性；WebHook 必须鉴权、幂等并保留提交与流水线关联。

实验从“触发云端构建”开始：固定源码提交、依赖锁、配置和目标环境，先预测文件树与制品引用；每次只改变一个模板、模块、缓存、网络或权限条件，在首个偏离点保存证据。恢复后重放同一提交，不能以重新构建掩盖制品差异。

### 6.2 云平台工作流

6.2 云平台工作流 需要放回“提交版本化源码、运行本地快速检查、进入测试沙箱、触发云端构建、签发唯一制品、逐环境持续交付”的完整交付链，明确输入、制品、环境、所有者和失败出口。

实验从“签发唯一制品”开始：固定源码提交、依赖锁、配置和目标环境，先预测文件树与制品引用；每次只改变一个模板、模块、缓存、网络或权限条件，在首个偏离点保存证据。恢复后重放同一提交，不能以重新构建掩盖制品差异。

### 6.2.1 GitFlow与版本管理

工作流将提交、检查、构建、测试、签发和推广组织为状态机。环境间重新构建会失去制品同一性；WebHook 必须鉴权、幂等并保留提交与流水线关联。

实验从“逐环境持续交付”开始：固定源码提交、依赖锁、配置和目标环境，先预测文件树与制品引用；每次只改变一个模板、模块、缓存、网络或权限条件，在首个偏离点保存证据。恢复后重放同一提交，不能以重新构建掩盖制品差异。

### 6.2.2 WebHook与自动构建

工作流将提交、检查、构建、测试、签发和推广组织为状态机。环境间重新构建会失去制品同一性；WebHook 必须鉴权、幂等并保留提交与流水线关联。

实验从“提交版本化源码”开始：固定源码提交、依赖锁、配置和目标环境，先预测文件树与制品引用；每次只改变一个模板、模块、缓存、网络或权限条件，在首个偏离点保存证据。恢复后重放同一提交，不能以重新构建掩盖制品差异。

### 6.3 持续集成与持续交付

工作流将提交、检查、构建、测试、签发和推广组织为状态机。环境间重新构建会失去制品同一性；WebHook 必须鉴权、幂等并保留提交与流水线关联。

实验从“运行本地快速检查”开始：固定源码提交、依赖锁、配置和目标环境，先预测文件树与制品引用；每次只改变一个模板、模块、缓存、网络或权限条件，在首个偏离点保存证据。恢复后重放同一提交，不能以重新构建掩盖制品差异。

### 6.4 总结

6.4 总结 需要放回“提交版本化源码、运行本地快速检查、进入测试沙箱、触发云端构建、签发唯一制品、逐环境持续交付”的完整交付链，明确输入、制品、环境、所有者和失败出口。

实验从“进入测试沙箱”开始：固定源码提交、依赖锁、配置和目标环境，先预测文件树与制品引用；每次只改变一个模板、模块、缓存、网络或权限条件，在首个偏离点保存证据。恢复后重放同一提交，不能以重新构建掩盖制品差异。

## 制品与失败矩阵

| 对象       | 正常状态       | 常见边界             | 失败证据       | 恢复条件       |
| ---------- | -------------- | -------------------- | -------------- | -------------- |
| 模板与源码 | 文件树确定     | 冲突、平台差异       | 差异清单       | 不覆盖用户文件 |
| 模块与构建 | 摘要可追溯     | 插件失败、依赖漂移   | 模块图与日志   | 同输入同制品   |
| 本地反馈   | 增量更新准确   | HMR残留、Mock偏差    | 状态与契约     | 与生产差异明确 |
| 部署与缓存 | 资源入口同版本 | 网络、权限、并发发布 | 制品与入口版本 | 可探测并回滚   |

## 最小可运行实验

```js
export const pipeline = [
  { name: "lint-and-test", input: "source" },
  { name: "build-once", output: "artifact-digest" },
  { name: "integration", input: "artifact-digest" },
  { name: "promote", input: "same-artifact-digest" },
];
```

```yaml
book: 前端工程化：体系设计与实践
edition: 2018-01
page: feng-06-workflow
sample: normal | boundary | failure | recovery
source_commit_fixed: true
artifact_digest_known: true
build_once: true
rollback_ready: true
```

```text
freeze source + lockfile + config + target
predict file tree + module graph + asset URLs
change exactly one delivery condition
stop at the first divergent state
reuse the same artifact during recovery
verify entry, assets, probes, and rollback
```

## 常见误区与故障注入

<Callout type="trap">
  **误区一：安装构建工具就等于完成工程化。**
  工具不自动提供模板确定性、制品同一性、部署互斥、缓存顺序和回滚证据；必须把责任与状态组织成闭环。
</Callout>

<Callout type="trap">
  **误区二：用当代热门工具覆盖原书目录。**
  Vite、模块联邦、TypeScript、现代测试与监控可以作为扩展，但原书正式主线仍是简史、脚手架、构建、本地服务器、部署、工作流和未来。
</Callout>

## 四类样本与验收

| 样本 | 注入方式                   | 必查证据             | 通过条件       |
| ---- | -------------------------- | -------------------- | -------------- |
| 正常 | 固定提交与依赖             | 文件树、摘要、URL    | 结果可重放     |
| 边界 | 大模块、慢网络、并发变更   | 增量范围、缓存、队列 | 时间与资源受控 |
| 失败 | 插件、权限、上传或探测失败 | 首个错误与所有者     | 阻断不污染环境 |
| 恢复 | 同制品重试或回滚           | 入口、资源、用户探测 | 版本重新一致   |

目录证据：第6章 工作流、6.1 本地工作流、6.1.1 二次构建的隐患、6.1.2 代码分离与测试沙箱、6.2 云平台工作流、6.2.1 GitFlow与版本管理、6.2.2 WebHook与自动构建、6.3 持续集成与持续交付、6.4 总结。本页按完整公开目录独立教学重构，不复制原书正文；9 个条目全部进入源码、机制、制品、失败和回滚证据链。

## 练习

<Exercises>

**问题 1** 为什么不能只用构建成功验收第 6 章 工作流？

<Answer>
  构建成功不显示模板漂移、测试与生产制品差异、资源缓存错配、并发部署覆盖和回滚状态。必须注入单一失败并用同一制品恢复。
</Answer>

**问题 2** 如何把本页目录变成可执行证据？

<Answer>
  依次登记“第6章 工作流、6.1 本地工作流、6.1.1 二次构建的隐患、6.1.2
  代码分离与测试沙箱、6.2 云平台工作流、6.2.1 GitFlow与版本管理、6.2.2
  WebHook与自动构建、6.3 持续集成与持续交付、6.4
  总结”，为每项写出源码输入、配置版本、输出制品、环境所有者、失败出口、缓存与回滚条件，再回放正常、边界、失败和恢复样本。
</Answer>

**问题 3** 本页最小通过条件是什么？

<Answer>
  每个提交只对应可追溯制品，环境间推广不重新构建，自动化失败阻断发布且可重试，分支流程不长期隐藏集成风险。
  同时，源码、制品和部署版本可追溯，最终没有半发布资源、错误入口、未清理本地状态或无法恢复的旧版本。
</Answer>

</Exercises>

## 小结

- 二次构建：对应“提交版本化源码”的核心观察量。
- 测试沙箱：对应“运行本地快速检查”的核心观察量。
- GitFlow：对应“进入测试沙箱”的核心观察量。
- WebHook：对应“触发云端构建”的核心观察量。
- 持续交付：对应“签发唯一制品”的核心观察量。
- 9 个本页公开目录条目已全部映射到交付链与版本账本。
- 构建输出只是证据之一，制品同一性、环境状态与回滚共同决定是否通过。

## 术语表

<Glossary>
  <GlossaryItem term="二次构建">
    二次构建
    对应“提交版本化源码”，验收时连接源码身份、配置契约、制品摘要、环境切换、失败传播和回滚状态。
  </GlossaryItem>
  <GlossaryItem term="测试沙箱">
    测试沙箱
    对应“运行本地快速检查”，验收时连接源码身份、配置契约、制品摘要、环境切换、失败传播和回滚状态。
  </GlossaryItem>
  <GlossaryItem term="GitFlow">
    GitFlow
    对应“进入测试沙箱”，验收时连接源码身份、配置契约、制品摘要、环境切换、失败传播和回滚状态。
  </GlossaryItem>
  <GlossaryItem term="WebHook">
    WebHook
    对应“触发云端构建”，验收时连接源码身份、配置契约、制品摘要、环境切换、失败传播和回滚状态。
  </GlossaryItem>
  <GlossaryItem term="持续交付">
    持续交付
    对应“签发唯一制品”，验收时连接源码身份、配置契约、制品摘要、环境切换、失败传播和回滚状态。
  </GlossaryItem>
</Glossary>

<Attribution
  adaptedFrom="周俊鹏 · 前端工程化：体系设计与实践 · 电子工业出版社 · 2018-01 · 完整公开目录"
  adaptedUrl="https://book.douban.com/subject/27605366/"
/>
