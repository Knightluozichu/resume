---
title: "第13章 多线程"
type: B
section: "游戏引擎原理与实践·卷2：高级技术（人邮2021）"
order: 14
description: "本章从同步原语走向异步加载、并行更新、渲染命令、纹理流送、着色器编译和资源热更新。 逐项覆盖41个权威目录坐标，并用姿态、渲染、并发与性能证据复核。"
demo: true
math: true
sourceUrl: "https://www.dedao.cn/ebook/detail?id=kQX7yD4MVoN52PDAnlRdzK6qvg8XEwbVeMwZJjBb7rO4ypxGa9LeQm1kYng9YzK5"
draft: false
---

import {
  Gep2Chapter13MultithreadingMapLab,
  Gep2Chapter13MultithreadingExperimentLab,
  Gep2Chapter13MultithreadingEvidenceLab,
} from "@/components/mdx/game-engine-practice-vol2/diagrams/gep2-chapter-13-multithreading";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第13章 多线程在15个正式单元、14章与247个目录节点中的位置
- 能绘制“建立任务依赖 → 隔离线程所有权 → 分发更新命令 → 同步渲染资源 → 回放并发证据”机制链并标出CPU、GPU、资产和线程边界
- 能计算蒙皮误差、混合权重、帧预算和并行上限，并说明统计口径
- 能设计适用于第13章 多线程的正常、压力与故障实验，让他人独立复现

</Objectives>

## 为什么高级引擎技术必须以状态和证据开始

本章从同步原语走向异步加载、并行更新、渲染命令、纹理流送、着色器编译和资源热更新。 本课程不复制原书正文、插图或配套代码，而把目录中的VSSystem机制重构为状态合同、可运行实验、故障样本与验收工件。本页逐项覆盖41个目录坐标：第13章 多线程、13.1 操作系统中的PV操作、13.2 Windows系统中关于线程的API、1．关键区、2．信号量、3．互斥量、4．事件、13.3 异步加载、13.4 多线程更新、13.4.1 游戏中代码多线程化的常规方法、13.4.2 一种渲染用的多线程更新架构、13.4.3 基于Unreal Engine Command实现多线程命令分发、13.4.4 多线程更新框架、13.4.5 封装组件到多线程中、13.5 多线程渲染、13.5.1 多线程渲染框架、13.5.2 真正要处理的问题、1．不是线程安全的代码是否可以在多线程下执行、2．如何处理锁缓存、3．如果主线程删除资源，而渲染线程还在用这个资源，该怎么办？、4．设备丢失问题、13.5.3 封装渲染命令、13.5.4 小结、13.6 纹理流式加载*、13.6.1 线程架构、13.6.2 最大Mip层级计算、13.7 着色器缓存编译*、13.7.1 材质树编译、13.7.2 自定义文件格式、13.7.3 检测着色器改变、13.7.4 多进程编译着色器、13.8 编辑器资源热更新、练习、示例、示例13.1、示例13.2、示例13.3、示例13.4、示例13.5、示例13.6、示例13.7。

卷2的技术不能拆成互不相干的技巧。骨骼姿态决定动态包围盒与渲染输入，材质图决定着色器变体与管线状态，阴影和后期决定目标资源生命周期，多线程又改变资源何时可见、何时可释放。一个局部模块若不声明身份、所有权、阶段和失败语义，就会在更高层表现为姿态跳变、错误画面、缓存爆炸、帧尾等待或偶发崩溃。

## 版次、目录与改编合同

本路径采用程东哲《游戏引擎原理与实践（卷2）：高级技术》，人民邮电出版社，2021年6月，ISBN 9787115560407。人民邮电出版社授权电子书目录列出9个书前节点、14章、各级小节、练习和示例，共247个正式目录节点。课程按“书前资料 + 14章”组成15个正式单元，另设学习地图与全书复核，共17页。

书中VSSystem、Windows线程API与出版时的图形接口属于2021年前后的工程语境。课程保留其架构问题、目录位置和可迁移机制，但当前项目必须用目标平台SDK与官方文档校正接口和能力；历史类名不是今天实现的唯一答案。

## 五个贯穿本卷的工程术语

<Term def="骨架在某一采样时刻所有骨骼的局部或模型空间变换集合。">骨骼姿态</Term>
、
<Term def="把材质图、平台能力和渲染通道映射到唯一编译产物的规范标识。">
  着色器变体键
</Term>
、<Term def="限定资源读写先后和可复用时刻的CPU/GPU同步标记。">资源栅栏</Term>、
<Term def="跨线程发布后在本帧内不再改变的世界与渲染输入集合。">帧快照</Term>、
<Term def="包含构建、设备、资产、输入、时间线、指标与最终状态的复现实验记录。">
  证据包
</Term>
。

骨骼姿态是动画与渲染的交界；着色器变体键约束材质编译规模；资源栅栏保护飞行中的GPU对象；帧快照隔离游戏与渲染线程；证据包则让这些机制能被第三方复查。任何一个术语如果只有类名而没有状态与不变量，都还不是工程合同。

## 四个可复算模型

线性蒙皮把绑定空间顶点变换到当前姿态：

$$
p^{prime} = sum_{i=1}^{k} w_i M_i B_i^{-1} p, qquad sum_i w_i = 1
$$

两姿态混合的平移与旋转分别采用线性插值和球面插值：

$$
t = (1-alpha)t_0 + alpha t_1, qquad q = operatorname{slerp}(q_0,q_1,alpha)
$$

考虑CPU/GPU重叠与帧尾等待的预算是：

$$
T_{frame}=max(T_{update},T_{render})+T_{wait}
$$

并行优化受串行比例约束：

$$
S(N)=rac{1}{(1-P)+P/N+T_{sync}}
$$

蒙皮应报告末端位置、法线与轮廓误差；混合应检查权重和、旋转最短弧与事件连续性；帧时间应给出P50、P95、P99及异常帧；并行实验必须把同步、队列、缓存和取消成本算入，不能只报告平均任务时间。

## 分步可视化：从机制图到故障证据

<Stepper>
  <Step title="先画依赖和所有权">
    <Gep2Chapter13MultithreadingMapLab />
  </Step>
  <Step title="再改变规模与预算">
    <Gep2Chapter13MultithreadingExperimentLab />
  </Step>
  <Step title="最后注入故障并保存证据">
    <Gep2Chapter13MultithreadingEvidenceLab />
  </Step>
</Stepper>

### 第13章 多线程

**目录节点 1/41。** 本章从同步原语走向异步加载、并行更新、渲染命令、纹理流送、着色器编译和资源热更新。

落实“第13章 多线程”时，沿“建立任务依赖 → 隔离线程所有权 → 分发更新命令 → 同步渲染资源 → 回放并发证据”标出输入、输出、所有者和线程，并把任务依赖变成可检查状态。正常路径至少记录资产或资源ID、帧号、阶段、耗时和最终输出；边界路径还要覆盖空输入、规模上限、版本不匹配和取消。

本节点的验收不是得到一张正确截图，而是能解释第一个状态变化、第一处预算超限和第一条失效引用。保存正常、压力与故障三组轨迹，并同时报告画质、姿态误差、CPU/GPU时间、峰值内存和生产工具影响，才能判断优化是否只是转移成本。

### 13.1 操作系统中的PV操作

**目录节点 2/41。** P/V操作描述计数信号量的等待与释放；正确性来自不变量和等待图，不来自调度时序猜测。

落实“13.1 操作系统中的PV操作”时，沿“建立任务依赖 → 隔离线程所有权 → 分发更新命令 → 同步渲染资源 → 回放并发证据”标出输入、输出、所有者和线程，并把线程所有权变成可检查状态。正常路径至少记录资产或资源ID、帧号、阶段、耗时和最终输出；边界路径还要覆盖空输入、规模上限、版本不匹配和取消。

本节点的验收不是得到一张正确截图，而是能解释第一个状态变化、第一处预算超限和第一条失效引用。保存正常、压力与故障三组轨迹，并同时报告画质、姿态误差、CPU/GPU时间、峰值内存和生产工具影响，才能判断优化是否只是转移成本。

### 13.2 Windows系统中关于线程的API

**目录节点 3/41。** “13.2 Windows系统中关于线程的API”位于“建立任务依赖 → 隔离线程所有权 → 分发更新命令 → 同步渲染资源 → 回放并发证据”链路中；实现时必须明确数据格式、所有权、更新阶段、预算与失败回收。

落实“13.2 Windows系统中关于线程的API”时，沿“建立任务依赖 → 隔离线程所有权 → 分发更新命令 → 同步渲染资源 → 回放并发证据”标出输入、输出、所有者和线程，并把命令队列变成可检查状态。正常路径至少记录资产或资源ID、帧号、阶段、耗时和最终输出；边界路径还要覆盖空输入、规模上限、版本不匹配和取消。

本节点的验收不是得到一张正确截图，而是能解释第一个状态变化、第一处预算超限和第一条失效引用。保存正常、压力与故障三组轨迹，并同时报告画质、姿态误差、CPU/GPU时间、峰值内存和生产工具影响，才能判断优化是否只是转移成本。

### 1．关键区

**目录节点 4/41。** 关键区适合同进程短临界区，持锁期间禁止阻塞I/O或执行不可控回调。

落实“1．关键区”时，沿“建立任务依赖 → 隔离线程所有权 → 分发更新命令 → 同步渲染资源 → 回放并发证据”标出输入、输出、所有者和线程，并把资源栅栏变成可检查状态。正常路径至少记录资产或资源ID、帧号、阶段、耗时和最终输出；边界路径还要覆盖空输入、规模上限、版本不匹配和取消。

本节点的验收不是得到一张正确截图，而是能解释第一个状态变化、第一处预算超限和第一条失效引用。保存正常、压力与故障三组轨迹，并同时报告画质、姿态误差、CPU/GPU时间、峰值内存和生产工具影响，才能判断优化是否只是转移成本。

### 2．信号量

**目录节点 5/41。** 信号量表达有限许可数量，释放与获取必须成对记录，超额释放同样是状态错误。

落实“2．信号量”时，沿“建立任务依赖 → 隔离线程所有权 → 分发更新命令 → 同步渲染资源 → 回放并发证据”标出输入、输出、所有者和线程，并把并发回放变成可检查状态。正常路径至少记录资产或资源ID、帧号、阶段、耗时和最终输出；边界路径还要覆盖空输入、规模上限、版本不匹配和取消。

本节点的验收不是得到一张正确截图，而是能解释第一个状态变化、第一处预算超限和第一条失效引用。保存正常、压力与故障三组轨迹，并同时报告画质、姿态误差、CPU/GPU时间、峰值内存和生产工具影响，才能判断优化是否只是转移成本。

### 3．互斥量

**目录节点 6/41。** 互斥量保护单一所有者临界区；跨进程能力和递归语义不应被滥用为任务依赖。

落实“3．互斥量”时，沿“建立任务依赖 → 隔离线程所有权 → 分发更新命令 → 同步渲染资源 → 回放并发证据”标出输入、输出、所有者和线程，并把任务依赖变成可检查状态。正常路径至少记录资产或资源ID、帧号、阶段、耗时和最终输出；边界路径还要覆盖空输入、规模上限、版本不匹配和取消。

本节点的验收不是得到一张正确截图，而是能解释第一个状态变化、第一处预算超限和第一条失效引用。保存正常、压力与故障三组轨迹，并同时报告画质、姿态误差、CPU/GPU时间、峰值内存和生产工具影响，才能判断优化是否只是转移成本。

### 4．事件

**目录节点 7/41。** 事件表达一次或持续条件通知，手动/自动复位差异必须写进状态机并覆盖丢失唤醒测试。

落实“4．事件”时，沿“建立任务依赖 → 隔离线程所有权 → 分发更新命令 → 同步渲染资源 → 回放并发证据”标出输入、输出、所有者和线程，并把线程所有权变成可检查状态。正常路径至少记录资产或资源ID、帧号、阶段、耗时和最终输出；边界路径还要覆盖空输入、规模上限、版本不匹配和取消。

本节点的验收不是得到一张正确截图，而是能解释第一个状态变化、第一处预算超限和第一条失效引用。保存正常、压力与故障三组轨迹，并同时报告画质、姿态误差、CPU/GPU时间、峰值内存和生产工具影响，才能判断优化是否只是转移成本。

### 13.3 异步加载

**目录节点 8/41。** 异步加载拆成读取、解压、反序列化和GPU上传阶段，每段都携带取消、优先级与资源身份。

落实“13.3 异步加载”时，沿“建立任务依赖 → 隔离线程所有权 → 分发更新命令 → 同步渲染资源 → 回放并发证据”标出输入、输出、所有者和线程，并把命令队列变成可检查状态。正常路径至少记录资产或资源ID、帧号、阶段、耗时和最终输出；边界路径还要覆盖空输入、规模上限、版本不匹配和取消。

本节点的验收不是得到一张正确截图，而是能解释第一个状态变化、第一处预算超限和第一条失效引用。保存正常、压力与故障三组轨迹，并同时报告画质、姿态误差、CPU/GPU时间、峰值内存和生产工具影响，才能判断优化是否只是转移成本。

### 13.4 多线程更新

**目录节点 9/41。** 并行更新先建立组件读写集与任务依赖，阶段结束用屏障提交一致快照，禁止跨任务隐式共享可变状态。

落实“13.4 多线程更新”时，沿“建立任务依赖 → 隔离线程所有权 → 分发更新命令 → 同步渲染资源 → 回放并发证据”标出输入、输出、所有者和线程，并把资源栅栏变成可检查状态。正常路径至少记录资产或资源ID、帧号、阶段、耗时和最终输出；边界路径还要覆盖空输入、规模上限、版本不匹配和取消。

本节点的验收不是得到一张正确截图，而是能解释第一个状态变化、第一处预算超限和第一条失效引用。保存正常、压力与故障三组轨迹，并同时报告画质、姿态误差、CPU/GPU时间、峰值内存和生产工具影响，才能判断优化是否只是转移成本。

### 13.4.1 游戏中代码多线程化的常规方法

**目录节点 10/41。** “13.4.1 游戏中代码多线程化的常规方法”位于“建立任务依赖 → 隔离线程所有权 → 分发更新命令 → 同步渲染资源 → 回放并发证据”链路中；实现时必须明确数据格式、所有权、更新阶段、预算与失败回收。

落实“13.4.1 游戏中代码多线程化的常规方法”时，沿“建立任务依赖 → 隔离线程所有权 → 分发更新命令 → 同步渲染资源 → 回放并发证据”标出输入、输出、所有者和线程，并把并发回放变成可检查状态。正常路径至少记录资产或资源ID、帧号、阶段、耗时和最终输出；边界路径还要覆盖空输入、规模上限、版本不匹配和取消。

本节点的验收不是得到一张正确截图，而是能解释第一个状态变化、第一处预算超限和第一条失效引用。保存正常、压力与故障三组轨迹，并同时报告画质、姿态误差、CPU/GPU时间、峰值内存和生产工具影响，才能判断优化是否只是转移成本。

### 13.4.2 一种渲染用的多线程更新架构

**目录节点 11/41。** 并行更新先建立组件读写集与任务依赖，阶段结束用屏障提交一致快照，禁止跨任务隐式共享可变状态。

落实“13.4.2 一种渲染用的多线程更新架构”时，沿“建立任务依赖 → 隔离线程所有权 → 分发更新命令 → 同步渲染资源 → 回放并发证据”标出输入、输出、所有者和线程，并把任务依赖变成可检查状态。正常路径至少记录资产或资源ID、帧号、阶段、耗时和最终输出；边界路径还要覆盖空输入、规模上限、版本不匹配和取消。

本节点的验收不是得到一张正确截图，而是能解释第一个状态变化、第一处预算超限和第一条失效引用。保存正常、压力与故障三组轨迹，并同时报告画质、姿态误差、CPU/GPU时间、峰值内存和生产工具影响，才能判断优化是否只是转移成本。

### 13.4.3 基于Unreal Engine Command实现多线程命令分发

**目录节点 12/41。** 命令对象复制执行所需参数并进入单向队列；资源引用至少存活到消费栅栏完成。

落实“13.4.3 基于Unreal Engine Command实现多线程命令分发”时，沿“建立任务依赖 → 隔离线程所有权 → 分发更新命令 → 同步渲染资源 → 回放并发证据”标出输入、输出、所有者和线程，并把线程所有权变成可检查状态。正常路径至少记录资产或资源ID、帧号、阶段、耗时和最终输出；边界路径还要覆盖空输入、规模上限、版本不匹配和取消。

本节点的验收不是得到一张正确截图，而是能解释第一个状态变化、第一处预算超限和第一条失效引用。保存正常、压力与故障三组轨迹，并同时报告画质、姿态误差、CPU/GPU时间、峰值内存和生产工具影响，才能判断优化是否只是转移成本。

### 13.4.4 多线程更新框架

**目录节点 13/41。** 并行更新先建立组件读写集与任务依赖，阶段结束用屏障提交一致快照，禁止跨任务隐式共享可变状态。

落实“13.4.4 多线程更新框架”时，沿“建立任务依赖 → 隔离线程所有权 → 分发更新命令 → 同步渲染资源 → 回放并发证据”标出输入、输出、所有者和线程，并把命令队列变成可检查状态。正常路径至少记录资产或资源ID、帧号、阶段、耗时和最终输出；边界路径还要覆盖空输入、规模上限、版本不匹配和取消。

本节点的验收不是得到一张正确截图，而是能解释第一个状态变化、第一处预算超限和第一条失效引用。保存正常、压力与故障三组轨迹，并同时报告画质、姿态误差、CPU/GPU时间、峰值内存和生产工具影响，才能判断优化是否只是转移成本。

### 13.4.5 封装组件到多线程中

**目录节点 14/41。** “13.4.5 封装组件到多线程中”位于“建立任务依赖 → 隔离线程所有权 → 分发更新命令 → 同步渲染资源 → 回放并发证据”链路中；实现时必须明确数据格式、所有权、更新阶段、预算与失败回收。

落实“13.4.5 封装组件到多线程中”时，沿“建立任务依赖 → 隔离线程所有权 → 分发更新命令 → 同步渲染资源 → 回放并发证据”标出输入、输出、所有者和线程，并把资源栅栏变成可检查状态。正常路径至少记录资产或资源ID、帧号、阶段、耗时和最终输出；边界路径还要覆盖空输入、规模上限、版本不匹配和取消。

本节点的验收不是得到一张正确截图，而是能解释第一个状态变化、第一处预算超限和第一条失效引用。保存正常、压力与故障三组轨迹，并同时报告画质、姿态误差、CPU/GPU时间、峰值内存和生产工具影响，才能判断优化是否只是转移成本。

### 13.5 多线程渲染

**目录节点 15/41。** 渲染线程负责构建或提交命令，游戏线程只发布不可变帧快照，二者用帧槽和栅栏限流。

落实“13.5 多线程渲染”时，沿“建立任务依赖 → 隔离线程所有权 → 分发更新命令 → 同步渲染资源 → 回放并发证据”标出输入、输出、所有者和线程，并把并发回放变成可检查状态。正常路径至少记录资产或资源ID、帧号、阶段、耗时和最终输出；边界路径还要覆盖空输入、规模上限、版本不匹配和取消。

本节点的验收不是得到一张正确截图，而是能解释第一个状态变化、第一处预算超限和第一条失效引用。保存正常、压力与故障三组轨迹，并同时报告画质、姿态误差、CPU/GPU时间、峰值内存和生产工具影响，才能判断优化是否只是转移成本。

### 13.5.1 多线程渲染框架

**目录节点 16/41。** 渲染线程负责构建或提交命令，游戏线程只发布不可变帧快照，二者用帧槽和栅栏限流。

落实“13.5.1 多线程渲染框架”时，沿“建立任务依赖 → 隔离线程所有权 → 分发更新命令 → 同步渲染资源 → 回放并发证据”标出输入、输出、所有者和线程，并把任务依赖变成可检查状态。正常路径至少记录资产或资源ID、帧号、阶段、耗时和最终输出；边界路径还要覆盖空输入、规模上限、版本不匹配和取消。

本节点的验收不是得到一张正确截图，而是能解释第一个状态变化、第一处预算超限和第一条失效引用。保存正常、压力与故障三组轨迹，并同时报告画质、姿态误差、CPU/GPU时间、峰值内存和生产工具影响，才能判断优化是否只是转移成本。

### 13.5.2 真正要处理的问题

**目录节点 17/41。** “13.5.2 真正要处理的问题”位于“建立任务依赖 → 隔离线程所有权 → 分发更新命令 → 同步渲染资源 → 回放并发证据”链路中；实现时必须明确数据格式、所有权、更新阶段、预算与失败回收。

落实“13.5.2 真正要处理的问题”时，沿“建立任务依赖 → 隔离线程所有权 → 分发更新命令 → 同步渲染资源 → 回放并发证据”标出输入、输出、所有者和线程，并把线程所有权变成可检查状态。正常路径至少记录资产或资源ID、帧号、阶段、耗时和最终输出；边界路径还要覆盖空输入、规模上限、版本不匹配和取消。

本节点的验收不是得到一张正确截图，而是能解释第一个状态变化、第一处预算超限和第一条失效引用。保存正常、压力与故障三组轨迹，并同时报告画质、姿态误差、CPU/GPU时间、峰值内存和生产工具影响，才能判断优化是否只是转移成本。

### 1．不是线程安全的代码是否可以在多线程下执行

**目录节点 18/41。** 先区分线程封闭、只读共享和同步共享；不能并发的代码可通过所有权转移在专属线程执行。

落实“1．不是线程安全的代码是否可以在多线程下执行”时，沿“建立任务依赖 → 隔离线程所有权 → 分发更新命令 → 同步渲染资源 → 回放并发证据”标出输入、输出、所有者和线程，并把命令队列变成可检查状态。正常路径至少记录资产或资源ID、帧号、阶段、耗时和最终输出；边界路径还要覆盖空输入、规模上限、版本不匹配和取消。

本节点的验收不是得到一张正确截图，而是能解释第一个状态变化、第一处预算超限和第一条失效引用。保存正常、压力与故障三组轨迹，并同时报告画质、姿态误差、CPU/GPU时间、峰值内存和生产工具影响，才能判断优化是否只是转移成本。

### 2．如何处理锁缓存

**目录节点 19/41。** 锁缓存必须与资源版本和帧槽绑定，命中路径也要验证所有权，避免缓存旧锁或旧对象地址。

落实“2．如何处理锁缓存”时，沿“建立任务依赖 → 隔离线程所有权 → 分发更新命令 → 同步渲染资源 → 回放并发证据”标出输入、输出、所有者和线程，并把资源栅栏变成可检查状态。正常路径至少记录资产或资源ID、帧号、阶段、耗时和最终输出；边界路径还要覆盖空输入、规模上限、版本不匹配和取消。

本节点的验收不是得到一张正确截图，而是能解释第一个状态变化、第一处预算超限和第一条失效引用。保存正常、压力与故障三组轨迹，并同时报告画质、姿态误差、CPU/GPU时间、峰值内存和生产工具影响，才能判断优化是否只是转移成本。

### 3．如果主线程删除资源，而渲染线程还在用这个资源，该怎么办？

**目录节点 20/41。** CPU删除只撤销逻辑所有权，GPU资源进入延迟回收队列，待最后使用栅栏完成后真正释放。

落实“3．如果主线程删除资源，而渲染线程还在用这个资源，该怎么办？”时，沿“建立任务依赖 → 隔离线程所有权 → 分发更新命令 → 同步渲染资源 → 回放并发证据”标出输入、输出、所有者和线程，并把并发回放变成可检查状态。正常路径至少记录资产或资源ID、帧号、阶段、耗时和最终输出；边界路径还要覆盖空输入、规模上限、版本不匹配和取消。

本节点的验收不是得到一张正确截图，而是能解释第一个状态变化、第一处预算超限和第一条失效引用。保存正常、压力与故障三组轨迹，并同时报告画质、姿态误差、CPU/GPU时间、峰值内存和生产工具影响，才能判断优化是否只是转移成本。

### 4．设备丢失问题

**目录节点 21/41。** 设备丢失把渲染资源转为不可用状态，重建设备后由可重建描述和源数据恢复，不能继续提交旧句柄。

落实“4．设备丢失问题”时，沿“建立任务依赖 → 隔离线程所有权 → 分发更新命令 → 同步渲染资源 → 回放并发证据”标出输入、输出、所有者和线程，并把任务依赖变成可检查状态。正常路径至少记录资产或资源ID、帧号、阶段、耗时和最终输出；边界路径还要覆盖空输入、规模上限、版本不匹配和取消。

本节点的验收不是得到一张正确截图，而是能解释第一个状态变化、第一处预算超限和第一条失效引用。保存正常、压力与故障三组轨迹，并同时报告画质、姿态误差、CPU/GPU时间、峰值内存和生产工具影响，才能判断优化是否只是转移成本。

### 13.5.3 封装渲染命令

**目录节点 22/41。** 命令对象复制执行所需参数并进入单向队列；资源引用至少存活到消费栅栏完成。

落实“13.5.3 封装渲染命令”时，沿“建立任务依赖 → 隔离线程所有权 → 分发更新命令 → 同步渲染资源 → 回放并发证据”标出输入、输出、所有者和线程，并把线程所有权变成可检查状态。正常路径至少记录资产或资源ID、帧号、阶段、耗时和最终输出；边界路径还要覆盖空输入、规模上限、版本不匹配和取消。

本节点的验收不是得到一张正确截图，而是能解释第一个状态变化、第一处预算超限和第一条失效引用。保存正常、压力与故障三组轨迹，并同时报告画质、姿态误差、CPU/GPU时间、峰值内存和生产工具影响，才能判断优化是否只是转移成本。

### 13.5.4 小结

**目录节点 23/41。** “13.5.4 小结”位于“建立任务依赖 → 隔离线程所有权 → 分发更新命令 → 同步渲染资源 → 回放并发证据”链路中；实现时必须明确数据格式、所有权、更新阶段、预算与失败回收。

落实“13.5.4 小结”时，沿“建立任务依赖 → 隔离线程所有权 → 分发更新命令 → 同步渲染资源 → 回放并发证据”标出输入、输出、所有者和线程，并把命令队列变成可检查状态。正常路径至少记录资产或资源ID、帧号、阶段、耗时和最终输出；边界路径还要覆盖空输入、规模上限、版本不匹配和取消。

本节点的验收不是得到一张正确截图，而是能解释第一个状态变化、第一处预算超限和第一条失效引用。保存正常、压力与故障三组轨迹，并同时报告画质、姿态误差、CPU/GPU时间、峰值内存和生产工具影响，才能判断优化是否只是转移成本。

### 13.6 纹理流式加载\*

**目录节点 24/41。** 纹理流送根据可见贡献、驻留预算和I/O吞吐选择Mip请求，并以代际阻止过期请求覆盖新状态。

落实“13.6 纹理流式加载\*”时，沿“建立任务依赖 → 隔离线程所有权 → 分发更新命令 → 同步渲染资源 → 回放并发证据”标出输入、输出、所有者和线程，并把资源栅栏变成可检查状态。正常路径至少记录资产或资源ID、帧号、阶段、耗时和最终输出；边界路径还要覆盖空输入、规模上限、版本不匹配和取消。

本节点的验收不是得到一张正确截图，而是能解释第一个状态变化、第一处预算超限和第一条失效引用。保存正常、压力与故障三组轨迹，并同时报告画质、姿态误差、CPU/GPU时间、峰值内存和生产工具影响，才能判断优化是否只是转移成本。

### 13.6.1 线程架构

**目录节点 25/41。** “13.6.1 线程架构”位于“建立任务依赖 → 隔离线程所有权 → 分发更新命令 → 同步渲染资源 → 回放并发证据”链路中；实现时必须明确数据格式、所有权、更新阶段、预算与失败回收。

落实“13.6.1 线程架构”时，沿“建立任务依赖 → 隔离线程所有权 → 分发更新命令 → 同步渲染资源 → 回放并发证据”标出输入、输出、所有者和线程，并把并发回放变成可检查状态。正常路径至少记录资产或资源ID、帧号、阶段、耗时和最终输出；边界路径还要覆盖空输入、规模上限、版本不匹配和取消。

本节点的验收不是得到一张正确截图，而是能解释第一个状态变化、第一处预算超限和第一条失效引用。保存正常、压力与故障三组轨迹，并同时报告画质、姿态误差、CPU/GPU时间、峰值内存和生产工具影响，才能判断优化是否只是转移成本。

### 13.6.2 最大Mip层级计算

**目录节点 26/41。** 最大Mip由投影尺寸、纹理密度、偏置和预算共同决定，升降级使用迟滞避免反复加载。

落实“13.6.2 最大Mip层级计算”时，沿“建立任务依赖 → 隔离线程所有权 → 分发更新命令 → 同步渲染资源 → 回放并发证据”标出输入、输出、所有者和线程，并把任务依赖变成可检查状态。正常路径至少记录资产或资源ID、帧号、阶段、耗时和最终输出；边界路径还要覆盖空输入、规模上限、版本不匹配和取消。

本节点的验收不是得到一张正确截图，而是能解释第一个状态变化、第一处预算超限和第一条失效引用。保存正常、压力与故障三组轨迹，并同时报告画质、姿态误差、CPU/GPU时间、峰值内存和生产工具影响，才能判断优化是否只是转移成本。

### 13.7 着色器缓存编译\*

**目录节点 27/41。** 缓存将规范键映射到编译产物和反射签名；命中仍需校验源哈希、编译器和目标平台版本。

落实“13.7 着色器缓存编译\*”时，沿“建立任务依赖 → 隔离线程所有权 → 分发更新命令 → 同步渲染资源 → 回放并发证据”标出输入、输出、所有者和线程，并把线程所有权变成可检查状态。正常路径至少记录资产或资源ID、帧号、阶段、耗时和最终输出；边界路径还要覆盖空输入、规模上限、版本不匹配和取消。

本节点的验收不是得到一张正确截图，而是能解释第一个状态变化、第一处预算超限和第一条失效引用。保存正常、压力与故障三组轨迹，并同时报告画质、姿态误差、CPU/GPU时间、峰值内存和生产工具影响，才能判断优化是否只是转移成本。

### 13.7.1 材质树编译

**目录节点 28/41。** 节点材质是带类型端口的有向无环图，编译器先验证连接，再做常量折叠、公共子表达式消除和代码生成。

落实“13.7.1 材质树编译”时，沿“建立任务依赖 → 隔离线程所有权 → 分发更新命令 → 同步渲染资源 → 回放并发证据”标出输入、输出、所有者和线程，并把命令队列变成可检查状态。正常路径至少记录资产或资源ID、帧号、阶段、耗时和最终输出；边界路径还要覆盖空输入、规模上限、版本不匹配和取消。

本节点的验收不是得到一张正确截图，而是能解释第一个状态变化、第一处预算超限和第一条失效引用。保存正常、压力与故障三组轨迹，并同时报告画质、姿态误差、CPU/GPU时间、峰值内存和生产工具影响，才能判断优化是否只是转移成本。

### 13.7.2 自定义文件格式

**目录节点 29/41。** 文本材质格式需要模式版本、包含依赖和错误定位，并在导入阶段转成统一中间表示。

落实“13.7.2 自定义文件格式”时，沿“建立任务依赖 → 隔离线程所有权 → 分发更新命令 → 同步渲染资源 → 回放并发证据”标出输入、输出、所有者和线程，并把资源栅栏变成可检查状态。正常路径至少记录资产或资源ID、帧号、阶段、耗时和最终输出；边界路径还要覆盖空输入、规模上限、版本不匹配和取消。

本节点的验收不是得到一张正确截图，而是能解释第一个状态变化、第一处预算超限和第一条失效引用。保存正常、压力与故障三组轨迹，并同时报告画质、姿态误差、CPU/GPU时间、峰值内存和生产工具影响，才能判断优化是否只是转移成本。

### 13.7.3 检测着色器改变

**目录节点 30/41。** 依赖图从源文件、包含、编译选项追踪到缓存键，变化时只失效受影响产物。

落实“13.7.3 检测着色器改变”时，沿“建立任务依赖 → 隔离线程所有权 → 分发更新命令 → 同步渲染资源 → 回放并发证据”标出输入、输出、所有者和线程，并把并发回放变成可检查状态。正常路径至少记录资产或资源ID、帧号、阶段、耗时和最终输出；边界路径还要覆盖空输入、规模上限、版本不匹配和取消。

本节点的验收不是得到一张正确截图，而是能解释第一个状态变化、第一处预算超限和第一条失效引用。保存正常、压力与故障三组轨迹，并同时报告画质、姿态误差、CPU/GPU时间、峰值内存和生产工具影响，才能判断优化是否只是转移成本。

### 13.7.4 多进程编译着色器

**目录节点 31/41。** 多进程编译隔离编译器崩溃并并行变体，调度器需去重输入、限制内存并原子发布缓存。

落实“13.7.4 多进程编译着色器”时，沿“建立任务依赖 → 隔离线程所有权 → 分发更新命令 → 同步渲染资源 → 回放并发证据”标出输入、输出、所有者和线程，并把任务依赖变成可检查状态。正常路径至少记录资产或资源ID、帧号、阶段、耗时和最终输出；边界路径还要覆盖空输入、规模上限、版本不匹配和取消。

本节点的验收不是得到一张正确截图，而是能解释第一个状态变化、第一处预算超限和第一条失效引用。保存正常、压力与故障三组轨迹，并同时报告画质、姿态误差、CPU/GPU时间、峰值内存和生产工具影响，才能判断优化是否只是转移成本。

### 13.8 编辑器资源热更新

**目录节点 32/41。** 热更新先构建新资源并验证，再在帧边界原子交换代理；旧资源延迟到所有读者离开后回收。

落实“13.8 编辑器资源热更新”时，沿“建立任务依赖 → 隔离线程所有权 → 分发更新命令 → 同步渲染资源 → 回放并发证据”标出输入、输出、所有者和线程，并把线程所有权变成可检查状态。正常路径至少记录资产或资源ID、帧号、阶段、耗时和最终输出；边界路径还要覆盖空输入、规模上限、版本不匹配和取消。

本节点的验收不是得到一张正确截图，而是能解释第一个状态变化、第一处预算超限和第一条失效引用。保存正常、压力与故障三组轨迹，并同时报告画质、姿态误差、CPU/GPU时间、峰值内存和生产工具影响，才能判断优化是否只是转移成本。

### 练习

**目录节点 33/41。** 练习应从正常、边界、压力和故障四组输入验证本章不变量，并提交可复算指标与反例解释。

落实“练习”时，沿“建立任务依赖 → 隔离线程所有权 → 分发更新命令 → 同步渲染资源 → 回放并发证据”标出输入、输出、所有者和线程，并把命令队列变成可检查状态。正常路径至少记录资产或资源ID、帧号、阶段、耗时和最终输出；边界路径还要覆盖空输入、规模上限、版本不匹配和取消。

本节点的验收不是得到一张正确截图，而是能解释第一个状态变化、第一处预算超限和第一条失效引用。保存正常、压力与故障三组轨迹，并同时报告画质、姿态误差、CPU/GPU时间、峰值内存和生产工具影响，才能判断优化是否只是转移成本。

### 示例

**目录节点 34/41。** 示例要固定资产、构建、目标设备和输入，先写预测，再保存状态快照、时间线与失败回收结果。

落实“示例”时，沿“建立任务依赖 → 隔离线程所有权 → 分发更新命令 → 同步渲染资源 → 回放并发证据”标出输入、输出、所有者和线程，并把资源栅栏变成可检查状态。正常路径至少记录资产或资源ID、帧号、阶段、耗时和最终输出；边界路径还要覆盖空输入、规模上限、版本不匹配和取消。

本节点的验收不是得到一张正确截图，而是能解释第一个状态变化、第一处预算超限和第一条失效引用。保存正常、压力与故障三组轨迹，并同时报告画质、姿态误差、CPU/GPU时间、峰值内存和生产工具影响，才能判断优化是否只是转移成本。

### 示例13.1

**目录节点 35/41。** 示例要固定资产、构建、目标设备和输入，先写预测，再保存状态快照、时间线与失败回收结果。

落实“示例13.1”时，沿“建立任务依赖 → 隔离线程所有权 → 分发更新命令 → 同步渲染资源 → 回放并发证据”标出输入、输出、所有者和线程，并把并发回放变成可检查状态。正常路径至少记录资产或资源ID、帧号、阶段、耗时和最终输出；边界路径还要覆盖空输入、规模上限、版本不匹配和取消。

本节点的验收不是得到一张正确截图，而是能解释第一个状态变化、第一处预算超限和第一条失效引用。保存正常、压力与故障三组轨迹，并同时报告画质、姿态误差、CPU/GPU时间、峰值内存和生产工具影响，才能判断优化是否只是转移成本。

### 示例13.2

**目录节点 36/41。** 示例要固定资产、构建、目标设备和输入，先写预测，再保存状态快照、时间线与失败回收结果。

落实“示例13.2”时，沿“建立任务依赖 → 隔离线程所有权 → 分发更新命令 → 同步渲染资源 → 回放并发证据”标出输入、输出、所有者和线程，并把任务依赖变成可检查状态。正常路径至少记录资产或资源ID、帧号、阶段、耗时和最终输出；边界路径还要覆盖空输入、规模上限、版本不匹配和取消。

本节点的验收不是得到一张正确截图，而是能解释第一个状态变化、第一处预算超限和第一条失效引用。保存正常、压力与故障三组轨迹，并同时报告画质、姿态误差、CPU/GPU时间、峰值内存和生产工具影响，才能判断优化是否只是转移成本。

### 示例13.3

**目录节点 37/41。** 示例要固定资产、构建、目标设备和输入，先写预测，再保存状态快照、时间线与失败回收结果。

落实“示例13.3”时，沿“建立任务依赖 → 隔离线程所有权 → 分发更新命令 → 同步渲染资源 → 回放并发证据”标出输入、输出、所有者和线程，并把线程所有权变成可检查状态。正常路径至少记录资产或资源ID、帧号、阶段、耗时和最终输出；边界路径还要覆盖空输入、规模上限、版本不匹配和取消。

本节点的验收不是得到一张正确截图，而是能解释第一个状态变化、第一处预算超限和第一条失效引用。保存正常、压力与故障三组轨迹，并同时报告画质、姿态误差、CPU/GPU时间、峰值内存和生产工具影响，才能判断优化是否只是转移成本。

### 示例13.4

**目录节点 38/41。** 示例要固定资产、构建、目标设备和输入，先写预测，再保存状态快照、时间线与失败回收结果。

落实“示例13.4”时，沿“建立任务依赖 → 隔离线程所有权 → 分发更新命令 → 同步渲染资源 → 回放并发证据”标出输入、输出、所有者和线程，并把命令队列变成可检查状态。正常路径至少记录资产或资源ID、帧号、阶段、耗时和最终输出；边界路径还要覆盖空输入、规模上限、版本不匹配和取消。

本节点的验收不是得到一张正确截图，而是能解释第一个状态变化、第一处预算超限和第一条失效引用。保存正常、压力与故障三组轨迹，并同时报告画质、姿态误差、CPU/GPU时间、峰值内存和生产工具影响，才能判断优化是否只是转移成本。

### 示例13.5

**目录节点 39/41。** 示例要固定资产、构建、目标设备和输入，先写预测，再保存状态快照、时间线与失败回收结果。

落实“示例13.5”时，沿“建立任务依赖 → 隔离线程所有权 → 分发更新命令 → 同步渲染资源 → 回放并发证据”标出输入、输出、所有者和线程，并把资源栅栏变成可检查状态。正常路径至少记录资产或资源ID、帧号、阶段、耗时和最终输出；边界路径还要覆盖空输入、规模上限、版本不匹配和取消。

本节点的验收不是得到一张正确截图，而是能解释第一个状态变化、第一处预算超限和第一条失效引用。保存正常、压力与故障三组轨迹，并同时报告画质、姿态误差、CPU/GPU时间、峰值内存和生产工具影响，才能判断优化是否只是转移成本。

### 示例13.6

**目录节点 40/41。** 示例要固定资产、构建、目标设备和输入，先写预测，再保存状态快照、时间线与失败回收结果。

落实“示例13.6”时，沿“建立任务依赖 → 隔离线程所有权 → 分发更新命令 → 同步渲染资源 → 回放并发证据”标出输入、输出、所有者和线程，并把并发回放变成可检查状态。正常路径至少记录资产或资源ID、帧号、阶段、耗时和最终输出；边界路径还要覆盖空输入、规模上限、版本不匹配和取消。

本节点的验收不是得到一张正确截图，而是能解释第一个状态变化、第一处预算超限和第一条失效引用。保存正常、压力与故障三组轨迹，并同时报告画质、姿态误差、CPU/GPU时间、峰值内存和生产工具影响，才能判断优化是否只是转移成本。

### 示例13.7

**目录节点 41/41。** 示例要固定资产、构建、目标设备和输入，先写预测，再保存状态快照、时间线与失败回收结果。

落实“示例13.7”时，沿“建立任务依赖 → 隔离线程所有权 → 分发更新命令 → 同步渲染资源 → 回放并发证据”标出输入、输出、所有者和线程，并把任务依赖变成可检查状态。正常路径至少记录资产或资源ID、帧号、阶段、耗时和最终输出；边界路径还要覆盖空输入、规模上限、版本不匹配和取消。

本节点的验收不是得到一张正确截图，而是能解释第一个状态变化、第一处预算超限和第一条失效引用。保存正常、压力与故障三组轨迹，并同时报告画质、姿态误差、CPU/GPU时间、峰值内存和生产工具影响，才能判断优化是否只是转移成本。

## 可迁移实现骨架

先让每个阶段消费不可变输入、产生带版本输出，并把资源回收推迟到最后读者完成。下列结构不绑定某个图形API，字段却足以关联动画、渲染和并发轨迹。

```cpp
struct FrameEvidence {
  uint64_t frame_id;
  uint64_t asset_generation;
  uint64_t resource_fence;
  double cpu_ms;
  double gpu_ms;
  double pose_error;
  const char* final_state;
};
```

每帧流程先冻结输入，再并行计算，最后在明确边界提交。任何中途失败都返回带身份的状态，而不是留下半更新对象。

```text
freeze gameplay state
  -> sample and blend pose
  -> build immutable render snapshot
  -> compile or fetch material pipeline
  -> submit commands with resource fence
  -> collect CPU/GPU evidence and retire resources
```

证据包采用机器可读字段，便于同一输入跨版本比较并定位第一处偏差。

```json
{
  "scenario": "normal|stress|fault",
  "assetGeneration": 42,
  "frameRange": [1200, 1320],
  "budgets": { "cpuMs": 8.0, "gpuMs": 8.0, "memoryMiB": 512 },
  "result": { "state": "accepted", "firstViolation": null }
}
```

## 本单元的验收工件

第一件工件是依赖图：把“建立任务依赖 → 隔离线程所有权 → 分发更新命令 → 同步渲染资源 → 回放并发证据”画成有向图，逐边标出数据、所有者、线程、栅栏与失败返回。第二件工件是目标机轨迹：固定构建、资产和输入，保存正常帧、尾延迟帧与故障帧中的姿态、资源、命令和GPU事件。第三件工件是反例报告：主动破坏任务依赖、线程所有权、命令队列、资源栅栏、并发回放中的一个不变量，定位第一个失效点，并证明修复后没有把成本转移到画质、内存、编辑器或关闭阶段。

<Callout type="trap" title="把历史类型名当成架构原则">
  VSSystem类名和旧平台接口只是实现样本。迁移时保留数据合同、生命周期、预算与证据字段，不复制宏、继承树或后端调用顺序。
</Callout>

<Callout type="trap" title="只验证预热后的正常帧">
  动画、材质和资源系统常在冷启动、切换、热更新、取消或关闭时失效。测试必须包含资源迟到、变体缺失、设备重建和并发回收。
</Callout>

<Callout type="trap" title="帧率提升但质量或工具回退">
  压缩、近似、异步与缓存可能损伤姿态、阴影、颜色、撤销或调试能力。验收应同时报告输出质量、性能分位数和内容生产体验。
</Callout>

## 先预测再运行

运行交互前先预测：角色与材质数量翻倍时CPU、GPU还是内存先越界；工作线程增加后同步项为何可能反而扩大；故障注入后哪条资源引用最先失效。若观察与预测不一致，优先检查跨帧流水、缓存预热、统计窗口、栅栏归属和异步取消。

## 本章回顾

第13章 多线程是“建立任务依赖 → 隔离线程所有权 → 分发更新命令 → 同步渲染资源 → 回放并发证据”中的完整工程合同。掌握本页意味着能逐项定位41个权威目录节点，解释任务依赖、线程所有权、命令队列、资源栅栏、并发回放的适用边界，复算姿态、变体、帧和并行指标，并用正常、压力与故障轨迹让结论可迁移。

<Exercises>

**问题 1：目录与状态。** 重画第13章 多线程的机制链，并标出资产、资源、线程与栅栏边界。

<Answer>
  从建立任务依赖到回放并发证据逐项写输入、输出、所有者和失败回收，再用任务依赖、线程所有权、命令队列检查隐式共享。无法说明代际、资源状态或关闭顺序的边都未完成。
</Answer>

**问题 2：预算实验。** 怎样验证任务依赖在冷启动和稳定运行中都满足目标？

<Answer>
  固定平台、构建、资产和输入，分别采集冷启动、预热、压力和故障样本；报告P50/P95/P99、峰值内存、姿态或图像误差、缓存命中和最终状态，并把异常帧关联到资产与资源ID。
</Answer>

**问题 3：迁移复核。** 怎样证明第13章 多线程不是只会复述VSSystem示例？

<Answer>
  在另一引擎或图形后端中重建同一输入、状态、生命周期、预算和证据字段，先预测线程所有权的失效点，再注入等价故障。独立复核者能从轨迹得到相同边界才算迁移成功。
</Answer>

</Exercises>

## 术语复核

<Glossary>
  <GlossaryItem term="骨骼姿态">
    骨架在某一采样时刻所有骨骼的局部或模型空间变换集合。
  </GlossaryItem>
  <GlossaryItem term="着色器变体键">
    把材质图、平台能力和渲染通道映射到唯一编译产物的规范标识。
  </GlossaryItem>
  <GlossaryItem term="资源栅栏">
    限定资源读写先后和可复用时刻的CPU/GPU同步标记。
  </GlossaryItem>
  <GlossaryItem term="帧快照">
    跨线程发布后在本帧内不再改变的世界与渲染输入集合。
  </GlossaryItem>
  <GlossaryItem term="证据包">
    包含构建、设备、资产、输入、时间线、指标与最终状态的复现实验记录。
  </GlossaryItem>
</Glossary>

<Attribution
  adaptedFrom="游戏引擎原理与实践（卷2）：高级技术"
  adaptedUrl="https://www.dedao.cn/ebook/detail?id=kQX7yD4MVoN52PDAnlRdzK6qvg8XEwbVeMwZJjBb7rO4ypxGa9LeQm1kYng9YzK5"
/>

## 阅读导航

- [上一页：第12章 阴影](/learn/game-engine-practice-vol2/13-chapter-12-shadows/gep2-chapter-12-shadows)
- [下一页：第14章 动态缓冲区与性能分析器](/learn/game-engine-practice-vol2/15-chapter-14-dynamic-buffers-profiler/gep2-chapter-14-dynamic-buffers-profiler)
