---
title: "《3D数学基础》全书导览"
type: C
section: "3D数学基础 · 全书导览"
order: 0
description: "按第二版14章与几何测试附录建立从坐标到曲线、从公式到引擎证据的学习路线。"
demo: true
math: true
sourceUrl: "https://gamemath.com/book/"
draft: true
---

import { Objectives, Callout, Attribution, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step } from "@/components/mdx/mdx-components";
import { Gm3dOfficialLearningMapConceptLab, Gm3dOfficialLearningMapTransformLab, Gm3dOfficialLearningMapEvidenceLab } from "@/components/mdx/game-math-3d/diagrams/gm3d-official-learning-map";

<Objectives>
- 能解释权威目录与空间主线在三维程序中的职责和约定
- 能推导方位主线到模拟主线的关键关系并检查前提
- 能实现证据闭环的最小实验，用反例和残差完成验收
</Objectives>

## 先做预测

先预测：改变坐标手性、组合顺序、时间步或退化输入时，数值和画面最先在哪里分叉。按第二版14章与几何测试附录建立从坐标到曲线、从公式到引擎证据的学习路线。 本页用<Term def="第二版由14章、附录A几何测试和附录B练习答案组成。">权威目录</Term>、<Term def="点和向量通过基、矩阵与层级在物体、世界、相机和裁剪空间流动。">空间主线</Term>、<Term def="欧拉角、矩阵、轴角和四元数服务于不同编辑、计算和插值任务。">方位主线</Term>、<Term def="导数、积分、动量与冲量把轨迹和碰撞变成可验收状态更新。">模拟主线</Term>和<Term def="每章同时交付推导、代码、交互图、反例和可重放测试。">证据闭环</Term>把原书直觉变成可运行证据。

<Gm3dOfficialLearningMapConceptLab />

## 原书章节定位

本页一一对应第二版“《3D数学基础》全书导览”。作者官网给出完整在线目录和正文；站内不重新编排原书章节，而是在同一章内补充公式推导、交互图、工程实现、失败模式与复现实验。

### 原书小节覆盖

1. 第1-3章：坐标、向量与空间
2. 第4-6章：矩阵、线性变换与投影
3. 第7-9章：极坐标、方位与几何图元
4. 第10-12章：图形、运动学与动力学
5. 第13-14章：曲线与后续项目
6. 附录A：几何测试
7. 附录B：练习答案

### 五个核心抓手

1. **权威目录**：第二版由14章、附录A几何测试和附录B练习答案组成。
2. **空间主线**：点和向量通过基、矩阵与层级在物体、世界、相机和裁剪空间流动。
3. **方位主线**：欧拉角、矩阵、轴角和四元数服务于不同编辑、计算和插值任务。
4. **模拟主线**：导数、积分、动量与冲量把轨迹和碰撞变成可验收状态更新。
5. **证据闭环**：每章同时交付推导、代码、交互图、反例和可重放测试。

## 从空间与约定开始

任何三维公式都先写对象类型、来源空间、目标空间、单位和约定。第二版由14章、附录A几何测试和附录B练习答案组成。 点和向量通过基、矩阵与层级在物体、世界、相机和裁剪空间流动。 若只保存三个或四个浮点数而丢失语义，错误通常会延迟到渲染、碰撞或插值阶段才显现。

$$
\mathcal C=(\text{object type},\text{source space},\text{target space},\text{units},\text{convention})
$$

工程接口应让非法组合尽早失败：点与方向不能随意相加，局部法线不能直接与世界光向量点积，度数不能静默传入弧度API。先画出原点和基向量，再检查结果，比只盯最终像素更可解释。

## 可核查推导

$$
\text{coordinates}\rightarrow\text{vectors}\rightarrow\text{matrices}\rightarrow\text{orientation}\rightarrow\text{geometry}\rightarrow\text{simulation}
$$

前一阶段输出就是后一阶段输入：坐标给出分量，向量表达位移，矩阵变换基，方位控制旋转，几何图元参与渲染与碰撞，微积分把状态推进到下一帧。 推导时每一步标明使用了定义、正交性、单位化、线性、连续性还是近似；若条件被非均匀缩放、退化图元或有限时间步破坏，就必须切换算法或返回显式状态。

```python
official_units=list(range(1,15))+["A"]
assert len(official_units)==15

site_pages=1+len(official_units)+1
assert site_pages==17
```

手算、代码和交互图应在一个非对称样例上得到一致结果。单位矩阵、零角度和原点只能做冒烟测试，因为它们经常让转置、符号和顺序错误互相抵消。

## 几何机制与数据流

**权威目录**：第二版由14章、附录A几何测试和附录B练习答案组成。

**空间主线**：点和向量通过基、矩阵与层级在物体、世界、相机和裁剪空间流动。

**方位主线**：欧拉角、矩阵、轴角和四元数服务于不同编辑、计算和插值任务。

数据流必须能逐段记录：输入对象先按约定进入数学表示，经变换或求解得到中间状态，再由渲染、物理或工具消费。每个中间状态保留空间标签和残差，才能区分源数据错误、公式错误与下游显示错误。

<Gm3dOfficialLearningMapTransformLab />

## 数值误差与边界

**模拟主线**：导数、积分、动量与冲量把轨迹和碰撞变成可验收状态更新。

**证据闭环**：每章同时交付推导、代码、交互图、反例和可重放测试。

边界集至少覆盖零长度、平行、共面、极小尺度、负尺度、角度跨界、时间步变化和浮点接近阈值。相对误差与绝对误差要按量纲选用，不能让一个全局epsilon同时承担位置、角度和点积判断。

$$
e_{abs}=\|\hat{\mathbf y}-\mathbf y\|,\qquad e_{rel}=\frac{e_{abs}}{\max(\|\mathbf y\|,s_{min})}
$$

```python
experiment={"normal":"non-symmetric input","boundary":"degenerate or near threshold","counterexample":"按主题跳读却没有固定全书的手性、向量侧和矩阵组合约定。","replay":"fixed inputs and conventions"}
assert set(experiment)=={"normal","boundary","counterexample","replay"}
```

## 工程案例

学习时维护一个共享3D沙盒，每完成一章就增加一个可视层和一个断言：从坐标轴、向量与矩阵开始，逐步加入相机、图元、光照、刚体和曲线。 报告同时保存输入、空间约定、参数、关键中间值、残差与调试图层。若重放后结论改变，先比较约定和数据版本，再比较算法实现。

<Gm3dOfficialLearningMapEvidenceLab />

```python
evidence=[("权威目录","definition"),("方位主线","derivation"),("证据闭环","replay")]
assert len({name for name,_ in evidence})==3
```

## 分步实验

<Stepper>
  <Step title="固定约定"><Gm3dOfficialLearningMapConceptLab /></Step>
  <Step title="改变参数"><Gm3dOfficialLearningMapTransformLab /></Step>
  <Step title="保存证据"><Gm3dOfficialLearningMapEvidenceLab /></Step>
</Stepper>

1. 选一个非对称、可手算的正常样例，记录所有空间、单位和正方向。
2. 一次只改变一个参数，比较公式、代码与交互图的中间状态。
3. 加入退化或近阈值输入，确认返回状态与容差策略一致。
4. 固定输入与时间步重放，保存残差、失败原因和未覆盖范围。

## 验收证书

$$
\operatorname{accept}=\text{conventions fixed}\land\text{invariants hold}\land\text{error within budget}\land\text{replay succeeds}
$$

验收证书拒绝“看起来差不多”。它必须回答用了哪套手性和向量约定、哪个不变量被检查、误差预算是多少、退化输入如何返回，以及另一台机器怎样从输入重建结果。

## 常见误区

<Callout type="trap" title="约定或边界被隐藏">按主题跳读却没有固定全书的手性、向量侧和矩阵组合约定。 修复时先让空间、单位、参数范围与退化状态显式化。</Callout>

<Callout type="trap" title="只验收最终画面">把附录A当公式清单，未测试平行、切触、共面和退化输入。 应同时检查中间数值、不变量、调试图和可重放反例。</Callout>

## 本章回顾

本章覆盖权威目录、空间主线、方位主线、模拟主线、证据闭环。真正掌握的标准是：能从定义推到公式，能把公式落到代码，能通过可交互图解释参数变化，并能在反例下给出稳定且可复现的失败结果。

<Exercises>
**问题1：权威目录与空间主线怎样协作？**
<Answer>第二版由14章、附录A几何测试和附录B练习答案组成。 点和向量通过基、矩阵与层级在物体、世界、相机和裁剪空间流动。 回答时必须补充对象、空间和约定。</Answer>

**问题2：如何推导并检查方位主线？**
<Answer>前一阶段输出就是后一阶段输入：坐标给出分量，向量表达位移，矩阵变换基，方位控制旋转，几何图元参与渲染与碰撞，微积分把状态推进到下一帧。 再用一个非对称小样例对照手算与代码。</Answer>

**问题3：怎样验收证据闭环？**
<Answer>每章同时交付推导、代码、交互图、反例和可重放测试。 使用正常、边界和反例输入，保存残差与重放证据。</Answer>
</Exercises>

## 术语表

<Glossary>
  <GlossaryItem term="权威目录" definition="第二版由14章、附录A几何测试和附录B练习答案组成。" />
  <GlossaryItem term="空间主线" definition="点和向量通过基、矩阵与层级在物体、世界、相机和裁剪空间流动。" />
  <GlossaryItem term="方位主线" definition="欧拉角、矩阵、轴角和四元数服务于不同编辑、计算和插值任务。" />
  <GlossaryItem term="模拟主线" definition="导数、积分、动量与冲量把轨迹和碰撞变成可验收状态更新。" />
  <GlossaryItem term="证据闭环" definition="每章同时交付推导、代码、交互图、反例和可重放测试。" />
  <GlossaryItem term="可重放证据" definition="绑定输入、约定、参数、中间状态、残差和失败原因的验收记录。" />
</Glossary>

<Attribution adaptedFrom="Fletcher Dunn and Ian Parberry · 3D Math Primer for Graphics and Game Development, 2nd Edition · 《3D数学基础》全书导览" adaptedUrl="https://gamemath.com/book/" />
