---
title: "《3D数学基础》全书总复习"
type: C
section: "3D数学基础 · 总复习"
order: 15
description: "用坐标变换、相机、碰撞、刚体和曲线相机的综合场景验收14章与附录A。"
demo: true
math: true
sourceUrl: "https://gamemath.com/book/"
draft: false
---


## 先做预测

先预测：改变坐标手性、组合顺序、时间步或退化输入时，数值和画面最先在哪里分叉。用坐标变换、相机、碰撞、刚体和曲线相机的综合场景验收14章与附录A。 本页用、、、和把原书直觉变成可运行证据。


## 原书章节定位

本页一一对应第二版“《3D数学基础》全书总复习”。作者官网给出完整在线目录和正文；站内不重新编排原书章节，而是在同一章内补充公式推导、交互图、工程实现、失败模式与复现实验。

### 原书小节覆盖

1. 坐标与向量诊断
2. 矩阵组合与投影
3. 方位表示转换
4. 几何图元与相交
5. 渲染空间与光照
6. 运动学与动力学
7. 参数曲线与连续性
8. 附录A边界测试

### 五个核心抓手

1. **约定清单**：手性、向上轴、角度单位、向量侧、矩阵布局和深度范围必须显式记录。
2. **空间追踪**：每个点、方向和法线都标记来源空间与目标空间。
3. **不变量**：正交性、单位长度、重心权重和、动量与端点条件用于自动验收。
4. **误差预算**：浮点、离散积分和近似模型的允许误差在运行前定义。
5. **重放证据**：保存输入、时间步、随机种子、状态快照和图形调试层。

## 从空间与约定开始

任何三维公式都先写对象类型、来源空间、目标空间、单位和约定。手性、向上轴、角度单位、向量侧、矩阵布局和深度范围必须显式记录。 每个点、方向和法线都标记来源空间与目标空间。 若只保存三个或四个浮点数而丢失语义，错误通常会延迟到渲染、碰撞或插值阶段才显现。

$$
\mathcal C=(\text{object type},\text{source space},\text{target space},\text{units},\text{convention})
$$

工程接口应让非法组合尽早失败：点与方向不能随意相加，局部法线不能直接与世界光向量点积，度数不能静默传入弧度API。先画出原点和基向量，再检查结果，比只盯最终像素更可解释。

## 可核查推导

$$
\operatorname{accept}=\bigwedge_{u\in\text{15 units}}(\text{derive}_u\land\text{implement}_u\land\text{counterexample}_u\land\text{replay}_u)
$$

综合验收不是把章节分数相加，而是沿真实数据流检查接口：局部点经矩阵进入世界和相机，投影成像；速度和冲量更新姿态；几何测试与曲线使用同一约定。任一接口失配都会在下游放大。 推导时每一步标明使用了定义、正交性、单位化、线性、连续性还是近似；若条件被非均匀缩放、退化图元或有限时间步破坏，就必须切换算法或返回显式状态。

```python
gates={"14_chapters":True,"appendix_a":True,"68_questions":True,"replay":True}
assert all(gates.values())

residuals=[0.0,1e-8,3e-7]
assert max(residuals)<1e-6
```

手算、代码和交互图应在一个非对称样例上得到一致结果。单位矩阵、零角度和原点只能做冒烟测试，因为它们经常让转置、符号和顺序错误互相抵消。

## 几何机制与数据流

**约定清单**：手性、向上轴、角度单位、向量侧、矩阵布局和深度范围必须显式记录。

**空间追踪**：每个点、方向和法线都标记来源空间与目标空间。

**不变量**：正交性、单位长度、重心权重和、动量与端点条件用于自动验收。

数据流必须能逐段记录：输入对象先按约定进入数学表示，经变换或求解得到中间状态，再由渲染、物理或工具消费。每个中间状态保留空间标签和残差，才能区分源数据错误、公式错误与下游显示错误。


## 数值误差与边界

**误差预算**：浮点、离散积分和近似模型的允许误差在运行前定义。

**重放证据**：保存输入、时间步、随机种子、状态快照和图形调试层。

边界集至少覆盖零长度、平行、共面、极小尺度、负尺度、角度跨界、时间步变化和浮点接近阈值。相对误差与绝对误差要按量纲选用，不能让一个全局epsilon同时承担位置、角度和点积判断。

$$
e_{abs}=\|\hat{\mathbf y}-\mathbf y\|,\qquad e_{rel}=\frac{e_{abs}}{\max(\|\mathbf y\|,s_{min})}
$$

```python
experiment={"normal":"non-symmetric input","boundary":"degenerate or near threshold","counterexample":"只分别测试模块，不测试矩阵、几何、物理和渲染之间的约定接口。","replay":"fixed inputs and conventions"}
assert set(experiment)=={"normal","boundary","counterexample","replay"}
```

## 工程案例

最终项目用轨道相机观察一组刚体沿曲线进入碰撞区：显示物体、世界和相机基轴，记录裁剪坐标、接触参数、冲量、角速度和曲线弧长误差。 报告同时保存输入、空间约定、参数、关键中间值、残差与调试图层。若重放后结论改变，先比较约定和数据版本，再比较算法实现。

```python
evidence=[("约定清单","definition"),("不变量","derivation"),("重放证据","replay")]
assert len({name for name,_ in evidence})==3
```

1. 选一个非对称、可手算的正常样例，记录所有空间、单位和正方向。
2. 一次只改变一个参数，比较公式、代码与交互图的中间状态。
3. 加入退化或近阈值输入，确认返回状态与容差策略一致。
4. 固定输入与时间步重放，保存残差、失败原因和未覆盖范围。

## 验收证书

$$
\operatorname{accept}=\text{conventions fixed}\land\text{invariants hold}\land\text{error within budget}\land\text{replay succeeds}
$$

验收证书拒绝“看起来差不多”。它必须回答用了哪套手性和向量约定、哪个不变量被检查、误差预算是多少、退化输入如何返回，以及另一台机器怎样从输入重建结果。

## 常见误区


## 本章回顾

本章覆盖约定清单、空间追踪、不变量、误差预算、重放证据。真正掌握的标准是：能从定义推到公式，能把公式落到代码，能通过可交互图解释参数变化，并能在反例下给出稳定且可复现的失败结果。


## 术语表


