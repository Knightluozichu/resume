---
title: 3. Flyweight
type: C
section: 游戏编程模式（作者2014 / 人邮2016）
order: 6
description: 3. Flyweight：把大量实例的共享固有状态与逐实例外在状态分开，通过可复位因果实验和反例证据验收。
demo: true
math: false
sourceUrl: 'https://gameprogrammingpatterns.com/flyweight.html'
draft: false
qualityVersion: 2
practiceMode: code
sourceMode: independent-rewrite
---

# 3. Flyweight


{/* GPP_QUALITY_V2 */}

## 为什么“3. Flyweight”从问题证据开始

每棵树重复保存相同网格和纹理，使实例规模直接放大内存。本页不把模式当成必选答案，而是先保存无模式基线，再验证候选机制是否真正改变可观察结果。通过条件是“修改一个实例的外在状态不会污染其他实例或共享资源”，而不是类或接口数量增加。

## 来源、版本与独立重写边界

本页用[作者完整在线正文](https://gameprogrammingpatterns.com/flyweight.html)核对正式标题、设计分叉和时代语境，并以[作者源码仓库](https://github.com/munificent/game-programming-patterns)交叉检查结构。[仓库许可证](https://raw.githubusercontent.com/munificent/game-programming-patterns/master/LICENSE)明确正文、HTML与样式为 CC BY-NC-ND 4.0，示例程序等其他文件为 MIT；因此下列中文解释、图示、交互和代码均为独立教学重写，不翻译、拼接或改写受 ND 限制的原文表达。

## 本章机制与术语

、、、。四个术语共同约束“把大量实例的共享固有状态与逐实例外在状态分开”，任何结论都必须回到唯一资源数、实例字节数、引用身份与渲染结果。

## 官方结构逐项深读

### 3. Flyweight

享元把树模型等共享固有状态与位置、颜色等外在状态分离，实例只保存轻量索引。

3. Flyweight 的因果解释从“拆分状态”开始：先标出输入、所有者和可见结果，再说明享元对象持有共享重资源，实例只保留位置、颜色和共享引用。若没有模式的基线也能更简单地守住“修改一个实例的外在状态不会污染其他实例或共享资源”，这一节点应得出“不采用”的结论。

3. Flyweight 的验证只改变一个条件，记录唯一资源数、实例字节数、引用身份与渲染结果；随后注入“可变颜色被错误放入共享享元”，定位首个分叉并执行重置。这样该节点同时具备出现、解释、专属实验和练习验证四级证据。

### Forest for the Trees

享元把树模型等共享固有状态与位置、颜色等外在状态分离，实例只保存轻量索引。

Forest for the Trees 的因果解释从“建立享元”开始：先标出输入、所有者和可见结果，再说明享元对象持有共享重资源，实例只保留位置、颜色和共享引用。若没有模式的基线也能更简单地守住“修改一个实例的外在状态不会污染其他实例或共享资源”，这一节点应得出“不采用”的结论。

Forest for the Trees 的验证只改变一个条件，记录唯一资源数、实例字节数、引用身份与渲染结果；随后注入“可变颜色被错误放入共享享元”，定位首个分叉并执行重置。这样该节点同时具备出现、解释、专属实验和练习验证四级证据。

### A Thousand Instances

享元把树模型等共享固有状态与位置、颜色等外在状态分离，实例只保存轻量索引。

A Thousand Instances 的因果解释从“创建实例”开始：先标出输入、所有者和可见结果，再说明享元对象持有共享重资源，实例只保留位置、颜色和共享引用。若没有模式的基线也能更简单地守住“修改一个实例的外在状态不会污染其他实例或共享资源”，这一节点应得出“不采用”的结论。

A Thousand Instances 的验证只改变一个条件，记录唯一资源数、实例字节数、引用身份与渲染结果；随后注入“可变颜色被错误放入共享享元”，定位首个分叉并执行重置。这样该节点同时具备出现、解释、专属实验和练习验证四级证据。

### The Flyweight Pattern

享元把树模型等共享固有状态与位置、颜色等外在状态分离，实例只保存轻量索引。

The Flyweight Pattern 的因果解释从“批量渲染”开始：先标出输入、所有者和可见结果，再说明享元对象持有共享重资源，实例只保留位置、颜色和共享引用。若没有模式的基线也能更简单地守住“修改一个实例的外在状态不会污染其他实例或共享资源”，这一节点应得出“不采用”的结论。

The Flyweight Pattern 的验证只改变一个条件，记录唯一资源数、实例字节数、引用身份与渲染结果；随后注入“可变颜色被错误放入共享享元”，定位首个分叉并执行重置。这样该节点同时具备出现、解释、专属实验和练习验证四级证据。

### A Place To Put Down Roots

享元把树模型等共享固有状态与位置、颜色等外在状态分离，实例只保存轻量索引。

A Place To Put Down Roots 的因果解释从“检查污染”开始：先标出输入、所有者和可见结果，再说明享元对象持有共享重资源，实例只保留位置、颜色和共享引用。若没有模式的基线也能更简单地守住“修改一个实例的外在状态不会污染其他实例或共享资源”，这一节点应得出“不采用”的结论。

A Place To Put Down Roots 的验证只改变一个条件，记录唯一资源数、实例字节数、引用身份与渲染结果；随后注入“可变颜色被错误放入共享享元”，定位首个分叉并执行重置。这样该节点同时具备出现、解释、专属实验和练习验证四级证据。

### What About Performance?

共享减少内存不必然更快；需同时测间接访问、批处理、缓存行和并发更新。

What About Performance? 的因果解释从“拆分状态”开始：先标出输入、所有者和可见结果，再说明享元对象持有共享重资源，实例只保留位置、颜色和共享引用。若没有模式的基线也能更简单地守住“修改一个实例的外在状态不会污染其他实例或共享资源”，这一节点应得出“不采用”的结论。

What About Performance? 的验证只改变一个条件，记录唯一资源数、实例字节数、引用身份与渲染结果；随后注入“可变颜色被错误放入共享享元”，定位首个分叉并执行重置。这样该节点同时具备出现、解释、专属实验和练习验证四级证据。

### See Also

关联模式用于比较组合与替代，不应因为两个模式常一起出现就默认同时引入。

See Also 的因果解释从“建立享元”开始：先标出输入、所有者和可见结果，再说明享元对象持有共享重资源，实例只保留位置、颜色和共享引用。若没有模式的基线也能更简单地守住“修改一个实例的外在状态不会污染其他实例或共享资源”，这一节点应得出“不采用”的结论。

See Also 的验证只改变一个条件，记录唯一资源数、实例字节数、引用身份与渲染结果；随后注入“可变颜色被错误放入共享享元”，定位首个分叉并执行重置。这样该节点同时具备出现、解释、专属实验和练习验证四级证据。

## 可迁移实现或计算骨架

```cpp
struct PatternProbe {
  const char* chapter = "3. Flyweight";
  const char* invariant = "修改一个实例的外在状态不会污染其他实例或共享资源";
  int generation = 0;
  bool reset_verified = false;
};

// 同一输入分别运行基线与候选实现，再比较原始证据。
```

该骨架只保存实验合同；真实项目还要把平台、构建、场景、输入和统计窗口固定下来，并保留基线实现以便回退。


## 本章练习与节点验证矩阵


## 术语复核与本章回顾


掌握“3. Flyweight”意味着能从“每棵树重复保存相同网格和纹理，使实例规模直接放大内存”出发，解释享元对象持有共享重资源，实例只保留位置、颜色和共享引用，再用唯一资源数、实例字节数、引用身份与渲染结果推翻或保留实现。若“修改一个实例的外在状态不会污染其他实例或共享资源”不能稳定复现，本章仍未通过。

## 阅读导航

[← 上一页：2. Command](/learn/game-programming-patterns/05-chapter-02-command/gpp-chapter-02-command) · [下一页：4. Observer →](/learn/game-programming-patterns/07-chapter-04-observer/gpp-chapter-04-observer)

