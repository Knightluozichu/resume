---
title: 5. Prototype
type: C
section: 游戏编程模式（作者2014 / 人邮2016）
order: 8
description: 5. Prototype：用可复制对象或数据模板定义新种类，并区分深浅复制边界，通过可复位因果实验和反例证据验收。
demo: true
math: false
sourceUrl: 'https://gameprogrammingpatterns.com/prototype.html'
draft: true
qualityVersion: 2
practiceMode: code
sourceMode: independent-rewrite
---
import {
  GppChapter05PrototypeMapLab,
  GppChapter05PrototypeExperimentLab,
  GppChapter05PrototypeEvidenceLab,
} from "@/components/mdx/game-programming-patterns/diagrams/gpp-chapter-05-prototype";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

# 5. Prototype

<Objectives>

- 能从“每个怪物种类都固化成类会让内容扩展依赖程序重新编译”重建本页的问题基线，不用模式名称替代需求。
- 能解释“原型保存共享配置，生成过程复制可变状态并重建所有权关系”中每个参与者、状态和时序边界。
- 能操作本章专属实验，比较种类子类与数据原型在三档负载下的因果差异。
- 能注入“浅复制让两个实例共享可变背包”，用对象图、资源引用、克隆差异与数据装载日志决定保留、替换或移除方案。

</Objectives>

{/* GPP_QUALITY_V2 */}

## 为什么“5. Prototype”从问题证据开始

每个怪物种类都固化成类会让内容扩展依赖程序重新编译。本页不把模式当成必选答案，而是先保存无模式基线，再验证候选机制是否真正改变可观察结果。通过条件是“克隆实例可独立变化且不会反向修改原型”，而不是类或接口数量增加。

## 来源、版本与独立重写边界

本页用[作者完整在线正文](https://gameprogrammingpatterns.com/prototype.html)核对正式标题、设计分叉和时代语境，并以[作者源码仓库](https://github.com/munificent/game-programming-patterns)交叉检查结构。[仓库许可证](https://raw.githubusercontent.com/munificent/game-programming-patterns/master/LICENSE)明确正文、HTML与样式为 CC BY-NC-ND 4.0，示例程序等其他文件为 MIT；因此下列中文解释、图示、交互和代码均为独立教学重写，不翻译、拼接或改写受 ND 限制的原文表达。

## 本章机制与术语

<Term def="原型对象在“5. Prototype”中用于描述输入到可见结果之间的责任边界。">原型对象</Term>、<Term def="克隆语义在“5. Prototype”中用于检查候选机制是否真正减少本章的主要成本。">克隆语义</Term>、<Term def="深浅复制在“5. Prototype”中用于记录对象、数据或消息在生命周期中的明确状态。">深浅复制</Term>、<Term def="数据建模在“5. Prototype”中用于决定保留、替换或移除实现的可复核证据。">数据建模</Term>。四个术语共同约束“用可复制对象或数据模板定义新种类，并区分深浅复制边界”，任何结论都必须回到对象图、资源引用、克隆差异与数据装载日志。

## 先预测，再操作三层专属实验

操作前先预测：从种类子类切到数据原型后，哪项类型改动先变化；负载扩大四倍时哪条边界先失效；注入“浅复制让两个实例共享可变背包”后能否恢复初始状态。

<Stepper>
  <Step title="1. 节点与因果链">
    选择正式节点和机制阶段，核对输入、所有者、变化与验证证据。

    <GppChapter05PrototypeMapLab />

  </Step>
  <Step title="2. 基线与候选取舍">
    一次只改变实现、负载或故障，比较可复算结果，避免用总风险分数代替因果。

    <GppChapter05PrototypeExperimentLab />

  </Step>
  <Step title="3. 反例与复位">
    在正常、压力和反例路径间切换，验证不变量；最后重置并确认初始状态一致。

    <GppChapter05PrototypeEvidenceLab />

  </Step>
</Stepper>

## 官方结构逐项深读

### 5. Prototype

原型用现有对象或数据模板创建新实例，并比较克隆、生成函数、第一类类型和原型语言范式。

5. Prototype 的因果解释从“定义原型”开始：先标出输入、所有者和可见结果，再说明原型保存共享配置，生成过程复制可变状态并重建所有权关系。若没有模式的基线也能更简单地守住“克隆实例可独立变化且不会反向修改原型”，这一节点应得出“不采用”的结论。

5. Prototype 的验证只改变一个条件，记录对象图、资源引用、克隆差异与数据装载日志；随后注入“浅复制让两个实例共享可变背包”，定位首个分叉并执行重置。这样该节点同时具备出现、解释、专属实验和练习验证四级证据。

### The Prototype Design Pattern

克隆、生成函数和第一类类型是创建实例的不同机制，应按身份、深复制和数据驱动需求选择。

The Prototype Design Pattern 的因果解释从“校验数据”开始：先标出输入、所有者和可见结果，再说明原型保存共享配置，生成过程复制可变状态并重建所有权关系。若没有模式的基线也能更简单地守住“克隆实例可独立变化且不会反向修改原型”，这一节点应得出“不采用”的结论。

The Prototype Design Pattern 的验证只改变一个条件，记录对象图、资源引用、克隆差异与数据装载日志；随后注入“浅复制让两个实例共享可变背包”，定位首个分叉并执行重置。这样该节点同时具备出现、解释、专属实验和练习验证四级证据。

### How well does it work?

How well does it work? 是设计分叉题；回答必须说明选择怎样改变原型保存共享配置，生成过程复制可变状态并重建所有权关系，以及哪条反例会推翻选择。

How well does it work? 的因果解释从“复制实例”开始：先标出输入、所有者和可见结果，再说明原型保存共享配置，生成过程复制可变状态并重建所有权关系。若没有模式的基线也能更简单地守住“克隆实例可独立变化且不会反向修改原型”，这一节点应得出“不采用”的结论。

How well does it work? 的验证只改变一个条件，记录对象图、资源引用、克隆差异与数据装载日志；随后注入“浅复制让两个实例共享可变背包”，定位首个分叉并执行重置。这样该节点同时具备出现、解释、专属实验和练习验证四级证据。

### Spawn functions

克隆、生成函数和第一类类型是创建实例的不同机制，应按身份、深复制和数据驱动需求选择。

Spawn functions 的因果解释从“重建所有权”开始：先标出输入、所有者和可见结果，再说明原型保存共享配置，生成过程复制可变状态并重建所有权关系。若没有模式的基线也能更简单地守住“克隆实例可独立变化且不会反向修改原型”，这一节点应得出“不采用”的结论。

Spawn functions 的验证只改变一个条件，记录对象图、资源引用、克隆差异与数据装载日志；随后注入“浅复制让两个实例共享可变背包”，定位首个分叉并执行重置。这样该节点同时具备出现、解释、专属实验和练习验证四级证据。

### Templates

克隆、生成函数和第一类类型是创建实例的不同机制，应按身份、深复制和数据驱动需求选择。

Templates 的因果解释从“修改隔离”开始：先标出输入、所有者和可见结果，再说明原型保存共享配置，生成过程复制可变状态并重建所有权关系。若没有模式的基线也能更简单地守住“克隆实例可独立变化且不会反向修改原型”，这一节点应得出“不采用”的结论。

Templates 的验证只改变一个条件，记录对象图、资源引用、克隆差异与数据装载日志；随后注入“浅复制让两个实例共享可变背包”，定位首个分叉并执行重置。这样该节点同时具备出现、解释、专属实验和练习验证四级证据。

### First-class types

克隆、生成函数和第一类类型是创建实例的不同机制，应按身份、深复制和数据驱动需求选择。

First-class types 的因果解释从“定义原型”开始：先标出输入、所有者和可见结果，再说明原型保存共享配置，生成过程复制可变状态并重建所有权关系。若没有模式的基线也能更简单地守住“克隆实例可独立变化且不会反向修改原型”，这一节点应得出“不采用”的结论。

First-class types 的验证只改变一个条件，记录对象图、资源引用、克隆差异与数据装载日志；随后注入“浅复制让两个实例共享可变背包”，定位首个分叉并执行重置。这样该节点同时具备出现、解释、专属实验和练习验证四级证据。

### The Prototype Language Paradigm

原型委托适合运行时数据继承，但需定义查找链、循环保护、覆盖和序列化语义。

The Prototype Language Paradigm 的因果解释从“校验数据”开始：先标出输入、所有者和可见结果，再说明原型保存共享配置，生成过程复制可变状态并重建所有权关系。若没有模式的基线也能更简单地守住“克隆实例可独立变化且不会反向修改原型”，这一节点应得出“不采用”的结论。

The Prototype Language Paradigm 的验证只改变一个条件，记录对象图、资源引用、克隆差异与数据装载日志；随后注入“浅复制让两个实例共享可变背包”，定位首个分叉并执行重置。这样该节点同时具备出现、解释、专属实验和练习验证四级证据。

### Self

原型委托适合运行时数据继承，但需定义查找链、循环保护、覆盖和序列化语义。

Self 的因果解释从“复制实例”开始：先标出输入、所有者和可见结果，再说明原型保存共享配置，生成过程复制可变状态并重建所有权关系。若没有模式的基线也能更简单地守住“克隆实例可独立变化且不会反向修改原型”，这一节点应得出“不采用”的结论。

Self 的验证只改变一个条件，记录对象图、资源引用、克隆差异与数据装载日志；随后注入“浅复制让两个实例共享可变背包”，定位首个分叉并执行重置。这样该节点同时具备出现、解释、专属实验和练习验证四级证据。

### How did it go?

How did it go? 是设计分叉题；回答必须说明选择怎样改变原型保存共享配置，生成过程复制可变状态并重建所有权关系，以及哪条反例会推翻选择。

How did it go? 的因果解释从“重建所有权”开始：先标出输入、所有者和可见结果，再说明原型保存共享配置，生成过程复制可变状态并重建所有权关系。若没有模式的基线也能更简单地守住“克隆实例可独立变化且不会反向修改原型”，这一节点应得出“不采用”的结论。

How did it go? 的验证只改变一个条件，记录对象图、资源引用、克隆差异与数据装载日志；随后注入“浅复制让两个实例共享可变背包”，定位首个分叉并执行重置。这样该节点同时具备出现、解释、专属实验和练习验证四级证据。

### What about JavaScript?

原型委托适合运行时数据继承，但需定义查找链、循环保护、覆盖和序列化语义。

What about JavaScript? 的因果解释从“修改隔离”开始：先标出输入、所有者和可见结果，再说明原型保存共享配置，生成过程复制可变状态并重建所有权关系。若没有模式的基线也能更简单地守住“克隆实例可独立变化且不会反向修改原型”，这一节点应得出“不采用”的结论。

What about JavaScript? 的验证只改变一个条件，记录对象图、资源引用、克隆差异与数据装载日志；随后注入“浅复制让两个实例共享可变背包”，定位首个分叉并执行重置。这样该节点同时具备出现、解释、专属实验和练习验证四级证据。

### Prototypes for Data Modeling

原型委托适合运行时数据继承，但需定义查找链、循环保护、覆盖和序列化语义。

Prototypes for Data Modeling 的因果解释从“定义原型”开始：先标出输入、所有者和可见结果，再说明原型保存共享配置，生成过程复制可变状态并重建所有权关系。若没有模式的基线也能更简单地守住“克隆实例可独立变化且不会反向修改原型”，这一节点应得出“不采用”的结论。

Prototypes for Data Modeling 的验证只改变一个条件，记录对象图、资源引用、克隆差异与数据装载日志；随后注入“浅复制让两个实例共享可变背包”，定位首个分叉并执行重置。这样该节点同时具备出现、解释、专属实验和练习验证四级证据。

## 可迁移实现或计算骨架

```cpp
struct PatternProbe {
  const char* chapter = "5. Prototype";
  const char* invariant = "克隆实例可独立变化且不会反向修改原型";
  int generation = 0;
  bool reset_verified = false;
};

// 同一输入分别运行基线与候选实现，再比较原始证据。
```

该骨架只保存实验合同；真实项目还要把平台、构建、场景、输入和统计窗口固定下来，并保留基线实现以便回退。

<Callout type="trap" title="先选模式再找问题">
  5. Prototype若没有“每个怪物种类都固化成类会让内容扩展依赖程序重新编译”的可重放样本，就不应引入候选模式。
</Callout>

<Callout type="trap" title="只看类图不看时序">
  5. Prototype必须解释定义原型、校验数据、复制实例、重建所有权、修改隔离之间谁先发生；最终结果相同不代表中间状态安全。
</Callout>

<Callout type="trap" title="只测顺利路径">
  故意触发“浅复制让两个实例共享可变背包”；若对象图、资源引用、克隆差异与数据装载日志无法定位首个分叉，实验和实现都不能通过。
</Callout>

## 本章练习与节点验证矩阵

<Exercises>

**问题 1：基线与选择。** 怎样证明数据原型解决的是本页真实问题，而不是增加装饰性抽象？

<Answer>
  用同一构建、场景和输入分别运行种类子类与数据原型，比较对象图、资源引用、克隆差异与数据装载日志；候选方案只有在守住“克隆实例可独立变化且不会反向修改原型”且主要代价下降时才保留。
</Answer>

**问题 2：逐节点验证。** 本页正式节点怎样从标题升级为可复查证据？

<Answer>
  - **5. Prototype**：执行“定义原型”，注入“浅复制让两个实例共享可变背包”，以对象图、资源引用、克隆差异与数据装载日志判定是否守住不变量。
  - **The Prototype Design Pattern**：执行“校验数据”，注入“浅复制让两个实例共享可变背包”，以对象图、资源引用、克隆差异与数据装载日志判定是否守住不变量。
  - **How well does it work?**：执行“复制实例”，注入“浅复制让两个实例共享可变背包”，以对象图、资源引用、克隆差异与数据装载日志判定是否守住不变量。
  - **Spawn functions**：执行“重建所有权”，注入“浅复制让两个实例共享可变背包”，以对象图、资源引用、克隆差异与数据装载日志判定是否守住不变量。
  - **Templates**：执行“修改隔离”，注入“浅复制让两个实例共享可变背包”，以对象图、资源引用、克隆差异与数据装载日志判定是否守住不变量。
  - **First-class types**：执行“定义原型”，注入“浅复制让两个实例共享可变背包”，以对象图、资源引用、克隆差异与数据装载日志判定是否守住不变量。
  - **The Prototype Language Paradigm**：执行“校验数据”，注入“浅复制让两个实例共享可变背包”，以对象图、资源引用、克隆差异与数据装载日志判定是否守住不变量。
  - **Self**：执行“复制实例”，注入“浅复制让两个实例共享可变背包”，以对象图、资源引用、克隆差异与数据装载日志判定是否守住不变量。
  - **How did it go?**：执行“重建所有权”，注入“浅复制让两个实例共享可变背包”，以对象图、资源引用、克隆差异与数据装载日志判定是否守住不变量。
  - **What about JavaScript?**：执行“修改隔离”，注入“浅复制让两个实例共享可变背包”，以对象图、资源引用、克隆差异与数据装载日志判定是否守住不变量。
  - **Prototypes for Data Modeling**：执行“定义原型”，注入“浅复制让两个实例共享可变背包”，以对象图、资源引用、克隆差异与数据装载日志判定是否守住不变量。
</Answer>

**问题 3：反例与复位。** 注入“浅复制让两个实例共享可变背包”后怎样关闭实验闭环？

<Answer>
  先定位第一个偏离“克隆实例可独立变化且不会反向修改原型”的阶段，只修复该原因；随后重放正常、压力和故障路径，并点击重置确认状态、代际和计数全部回到同一初值。
</Answer>

</Exercises>

## 术语复核与本章回顾

<Glossary>
  <GlossaryItem term="原型对象">
    原型对象在“5. Prototype”中用于描述输入到可见结果之间的责任边界。
  </GlossaryItem>
  <GlossaryItem term="克隆语义">
    克隆语义在“5. Prototype”中用于检查候选机制是否真正减少本章的主要成本。
  </GlossaryItem>
  <GlossaryItem term="深浅复制">
    深浅复制在“5. Prototype”中用于记录对象、数据或消息在生命周期中的明确状态。
  </GlossaryItem>
  <GlossaryItem term="数据建模">
    数据建模在“5. Prototype”中用于决定保留、替换或移除实现的可复核证据。
  </GlossaryItem>
</Glossary>

掌握“5. Prototype”意味着能从“每个怪物种类都固化成类会让内容扩展依赖程序重新编译”出发，解释原型保存共享配置，生成过程复制可变状态并重建所有权关系，再用对象图、资源引用、克隆差异与数据装载日志推翻或保留实现。若“克隆实例可独立变化且不会反向修改原型”不能稳定复现，本章仍未通过。

## 阅读导航

[← 上一页：4. Observer](/learn/game-programming-patterns/07-chapter-04-observer/gpp-chapter-04-observer) · [下一页：6. Singleton →](/learn/game-programming-patterns/09-chapter-06-singleton/gpp-chapter-06-singleton)

<Attribution
  mode="independent-rewrite"
  sourceBasis="full-text"
  workTitle="Robert Nystrom, Game Programming Patterns (2014)"
  adaptedUrl="https://gameprogrammingpatterns.com/prototype.html"
/>
