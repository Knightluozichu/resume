---
title: 9. Game Loop
type: C
section: 游戏编程模式（作者2014 / 人邮2016）
order: 13
description: 9. Game Loop：把游戏时间推进与输入到达、处理器速度和渲染频率解耦，通过可复位因果实验和反例证据验收。
demo: true
math: false
sourceUrl: 'https://gameprogrammingpatterns.com/game-loop.html'
draft: true
qualityVersion: 2
practiceMode: simulation
sourceMode: independent-rewrite
---
import {
  GppChapter09GameLoopMapLab,
  GppChapter09GameLoopExperimentLab,
  GppChapter09GameLoopEvidenceLab,
} from "@/components/mdx/game-programming-patterns/diagrams/gpp-chapter-09-game-loop";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

# 9. Game Loop

<Objectives>

- 能从“每次循环固定移动量会让更快处理器上的游戏运行得更快”重建本页的问题基线，不用模式名称替代需求。
- 能解释“循环非阻塞处理输入，按时间步更新状态并渲染，可用累积器协调频率”中每个参与者、状态和时序边界。
- 能操作本章专属实验，比较每帧固定移动与时间步循环在三档负载下的因果差异。
- 能注入“长帧让累积器无限追赶形成死亡螺旋”，用真实时间、模拟时间、更新次数、插值比例与帧分位数决定保留、替换或移除方案。

</Objectives>

{/* GPP_QUALITY_V2 */}

## 为什么“9. Game Loop”从问题证据开始

每次循环固定移动量会让更快处理器上的游戏运行得更快。本页不把模式当成必选答案，而是先保存无模式基线，再验证候选机制是否真正改变可观察结果。通过条件是“相同真实时长内的模拟推进量与机器速度无关”，而不是类或接口数量增加。

## 来源、版本与独立重写边界

本页用[作者完整在线正文](https://gameprogrammingpatterns.com/game-loop.html)核对正式标题、设计分叉和时代语境，并以[作者源码仓库](https://github.com/munificent/game-programming-patterns)交叉检查结构。[仓库许可证](https://raw.githubusercontent.com/munificent/game-programming-patterns/master/LICENSE)明确正文、HTML与样式为 CC BY-NC-ND 4.0，示例程序等其他文件为 MIT；因此下列中文解释、图示、交互和代码均为独立教学重写，不翻译、拼接或改写受 ND 限制的原文表达。

## 本章机制与术语

<Term def="游戏循环在“9. Game Loop”中用于描述输入到可见结果之间的责任边界。">游戏循环</Term>、<Term def="固定时间步在“9. Game Loop”中用于检查候选机制是否真正减少本章的主要成本。">固定时间步</Term>、<Term def="累积器在“9. Game Loop”中用于记录对象、数据或消息在生命周期中的明确状态。">累积器</Term>、<Term def="死亡螺旋在“9. Game Loop”中用于决定保留、替换或移除实现的可复核证据。">死亡螺旋</Term>。四个术语共同约束“把游戏时间推进与输入到达、处理器速度和渲染频率解耦”，任何结论都必须回到真实时间、模拟时间、更新次数、插值比例与帧分位数。

## 先预测，再操作三层专属实验

操作前先预测：从每帧固定移动切到时间步循环后，哪项ms偏差先变化；负载扩大四倍时哪条边界先失效；注入“长帧让累积器无限追赶形成死亡螺旋”后能否恢复初始状态。

<Stepper>
  <Step title="1. 节点与因果链">
    选择正式节点和机制阶段，核对输入、所有者、变化与验证证据。

    <GppChapter09GameLoopMapLab />

  </Step>
  <Step title="2. 基线与候选取舍">
    一次只改变实现、负载或故障，比较可复算结果，避免用总风险分数代替因果。

    <GppChapter09GameLoopExperimentLab />

  </Step>
  <Step title="3. 反例与复位">
    在正常、压力和反例路径间切换，验证不变量；最后重置并确认初始状态一致。

    <GppChapter09GameLoopEvidenceLab />

  </Step>
</Stepper>

## 官方结构逐项深读

### 9. Game Loop

游戏循环把平台事件、模拟步长、渲染、功耗与追赶策略组织成稳定的实时调度器。

9. Game Loop 的因果解释从“采集时间”开始：先标出输入、所有者和可见结果，再说明循环非阻塞处理输入，按时间步更新状态并渲染，可用累积器协调频率。若没有模式的基线也能更简单地守住“相同真实时长内的模拟推进量与机器速度无关”，这一节点应得出“不采用”的结论。

9. Game Loop 的验证只改变一个条件，记录真实时间、模拟时间、更新次数、插值比例与帧分位数；随后注入“长帧让累积器无限追赶形成死亡螺旋”，定位首个分叉并执行重置。这样该节点同时具备出现、解释、专属实验和练习验证四级证据。

### Intent

本模式意图是解决游戏循环把平台事件、模拟步长、渲染、功耗与追赶策略组织成稳定的实时调度器。中的核心问题，必须先用失败示例证明问题真实存在。

Intent 的因果解释从“处理输入”开始：先标出输入、所有者和可见结果，再说明循环非阻塞处理输入，按时间步更新状态并渲染，可用累积器协调频率。若没有模式的基线也能更简单地守住“相同真实时长内的模拟推进量与机器速度无关”，这一节点应得出“不采用”的结论。

Intent 的验证只改变一个条件，记录真实时间、模拟时间、更新次数、插值比例与帧分位数；随后注入“长帧让累积器无限追赶形成死亡螺旋”，定位首个分叉并执行重置。这样该节点同时具备出现、解释、专属实验和练习验证四级证据。

### Motivation

动机示例把平台事件、固定步长、时间累积放入具体游戏场景，作为应用前后的比较基线。

Motivation 的因果解释从“累积步长”开始：先标出输入、所有者和可见结果，再说明循环非阻塞处理输入，按时间步更新状态并渲染，可用累积器协调频率。若没有模式的基线也能更简单地守住“相同真实时长内的模拟推进量与机器速度无关”，这一节点应得出“不采用”的结论。

Motivation 的验证只改变一个条件，记录真实时间、模拟时间、更新次数、插值比例与帧分位数；随后注入“长帧让累积器无限追赶形成死亡螺旋”，定位首个分叉并执行重置。这样该节点同时具备出现、解释、专属实验和练习验证四级证据。

### Interview with a CPU

循环所有权受平台约束；睡眠、垂直同步和后台节流要与输入延迟及功耗共同测量。

Interview with a CPU 的因果解释从“更新模拟”开始：先标出输入、所有者和可见结果，再说明循环非阻塞处理输入，按时间步更新状态并渲染，可用累积器协调频率。若没有模式的基线也能更简单地守住“相同真实时长内的模拟推进量与机器速度无关”，这一节点应得出“不采用”的结论。

Interview with a CPU 的验证只改变一个条件，记录真实时间、模拟时间、更新次数、插值比例与帧分位数；随后注入“长帧让累积器无限追赶形成死亡螺旋”，定位首个分叉并执行重置。这样该节点同时具备出现、解释、专属实验和练习验证四级证据。

### Event loops

固定模拟步长提供确定性，累积器与插值连接真实时间；追赶需设上限避免螺旋失控。

Event loops 的因果解释从“渲染插值”开始：先标出输入、所有者和可见结果，再说明循环非阻塞处理输入，按时间步更新状态并渲染，可用累积器协调频率。若没有模式的基线也能更简单地守住“相同真实时长内的模拟推进量与机器速度无关”，这一节点应得出“不采用”的结论。

Event loops 的验证只改变一个条件，记录真实时间、模拟时间、更新次数、插值比例与帧分位数；随后注入“长帧让累积器无限追赶形成死亡螺旋”，定位首个分叉并执行重置。这样该节点同时具备出现、解释、专属实验和练习验证四级证据。

### A world out of time

固定模拟步长提供确定性，累积器与插值连接真实时间；追赶需设上限避免螺旋失控。

A world out of time 的因果解释从“采集时间”开始：先标出输入、所有者和可见结果，再说明循环非阻塞处理输入，按时间步更新状态并渲染，可用累积器协调频率。若没有模式的基线也能更简单地守住“相同真实时长内的模拟推进量与机器速度无关”，这一节点应得出“不采用”的结论。

A world out of time 的验证只改变一个条件，记录真实时间、模拟时间、更新次数、插值比例与帧分位数；随后注入“长帧让累积器无限追赶形成死亡螺旋”，定位首个分叉并执行重置。这样该节点同时具备出现、解释、专属实验和练习验证四级证据。

### Seconds per second

固定模拟步长提供确定性，累积器与插值连接真实时间；追赶需设上限避免螺旋失控。

Seconds per second 的因果解释从“处理输入”开始：先标出输入、所有者和可见结果，再说明循环非阻塞处理输入，按时间步更新状态并渲染，可用累积器协调频率。若没有模式的基线也能更简单地守住“相同真实时长内的模拟推进量与机器速度无关”，这一节点应得出“不采用”的结论。

Seconds per second 的验证只改变一个条件，记录真实时间、模拟时间、更新次数、插值比例与帧分位数；随后注入“长帧让累积器无限追赶形成死亡螺旋”，定位首个分叉并执行重置。这样该节点同时具备出现、解释、专属实验和练习验证四级证据。

### The Pattern

模式结构把“采集平台事件 → 累积真实时间 → 执行固定模拟步 → 插值并渲染 → 节流与记录长尾”中的参与者、消息、状态与时序压缩为最小合同。

The Pattern 的因果解释从“累积步长”开始：先标出输入、所有者和可见结果，再说明循环非阻塞处理输入，按时间步更新状态并渲染，可用累积器协调频率。若没有模式的基线也能更简单地守住“相同真实时长内的模拟推进量与机器速度无关”，这一节点应得出“不采用”的结论。

The Pattern 的验证只改变一个条件，记录真实时间、模拟时间、更新次数、插值比例与帧分位数；随后注入“长帧让累积器无限追赶形成死亡螺旋”，定位首个分叉并执行重置。这样该节点同时具备出现、解释、专属实验和练习验证四级证据。

### When to Use It

只有问题频率、变化压力或性能证据超过模式引入成本时才使用，并写下不适用条件。

When to Use It 的因果解释从“更新模拟”开始：先标出输入、所有者和可见结果，再说明循环非阻塞处理输入，按时间步更新状态并渲染，可用累积器协调频率。若没有模式的基线也能更简单地守住“相同真实时长内的模拟推进量与机器速度无关”，这一节点应得出“不采用”的结论。

When to Use It 的验证只改变一个条件，记录真实时间、模拟时间、更新次数、插值比例与帧分位数；随后注入“长帧让累积器无限追赶形成死亡螺旋”，定位首个分叉并执行重置。这样该节点同时具备出现、解释、专属实验和练习验证四级证据。

### Keep in Mind

代价清单必须覆盖间接层、内存、生命周期、调试、线程和团队理解成本。

Keep in Mind 的因果解释从“渲染插值”开始：先标出输入、所有者和可见结果，再说明循环非阻塞处理输入，按时间步更新状态并渲染，可用累积器协调频率。若没有模式的基线也能更简单地守住“相同真实时长内的模拟推进量与机器速度无关”，这一节点应得出“不采用”的结论。

Keep in Mind 的验证只改变一个条件，记录真实时间、模拟时间、更新次数、插值比例与帧分位数；随后注入“长帧让累积器无限追赶形成死亡螺旋”，定位首个分叉并执行重置。这样该节点同时具备出现、解释、专属实验和练习验证四级证据。

### You may need to coordinate with the platform’s event loop

循环所有权受平台约束；睡眠、垂直同步和后台节流要与输入延迟及功耗共同测量。

You may need to coordinate with the platform’s event loop 的因果解释从“采集时间”开始：先标出输入、所有者和可见结果，再说明循环非阻塞处理输入，按时间步更新状态并渲染，可用累积器协调频率。若没有模式的基线也能更简单地守住“相同真实时长内的模拟推进量与机器速度无关”，这一节点应得出“不采用”的结论。

You may need to coordinate with the platform’s event loop 的验证只改变一个条件，记录真实时间、模拟时间、更新次数、插值比例与帧分位数；随后注入“长帧让累积器无限追赶形成死亡螺旋”，定位首个分叉并执行重置。这样该节点同时具备出现、解释、专属实验和练习验证四级证据。

### Sample Code

先理解本书是问题驱动的模式目录，示例C++用于表达意图而不是可直接复制的现代生产代码。

Sample Code 的因果解释从“处理输入”开始：先标出输入、所有者和可见结果，再说明循环非阻塞处理输入，按时间步更新状态并渲染，可用累积器协调频率。若没有模式的基线也能更简单地守住“相同真实时长内的模拟推进量与机器速度无关”，这一节点应得出“不采用”的结论。

Sample Code 的验证只改变一个条件，记录真实时间、模拟时间、更新次数、插值比例与帧分位数；随后注入“长帧让累积器无限追赶形成死亡螺旋”，定位首个分叉并执行重置。这样该节点同时具备出现、解释、专属实验和练习验证四级证据。

### Run, run as fast as you can

Run, run as fast as you can 聚焦运行代价，固定场景后用真实时间、模拟时间、更新次数、插值比例与帧分位数定位首个超限点，不能以模式名称推断快慢。

Run, run as fast as you can 的因果解释从“累积步长”开始：先标出输入、所有者和可见结果，再说明循环非阻塞处理输入，按时间步更新状态并渲染，可用累积器协调频率。若没有模式的基线也能更简单地守住“相同真实时长内的模拟推进量与机器速度无关”，这一节点应得出“不采用”的结论。

Run, run as fast as you can 的验证只改变一个条件，记录真实时间、模拟时间、更新次数、插值比例与帧分位数；随后注入“长帧让累积器无限追赶形成死亡螺旋”，定位首个分叉并执行重置。这样该节点同时具备出现、解释、专属实验和练习验证四级证据。

### Take a little nap

循环所有权受平台约束；睡眠、垂直同步和后台节流要与输入延迟及功耗共同测量。

Take a little nap 的因果解释从“更新模拟”开始：先标出输入、所有者和可见结果，再说明循环非阻塞处理输入，按时间步更新状态并渲染，可用累积器协调频率。若没有模式的基线也能更简单地守住“相同真实时长内的模拟推进量与机器速度无关”，这一节点应得出“不采用”的结论。

Take a little nap 的验证只改变一个条件，记录真实时间、模拟时间、更新次数、插值比例与帧分位数；随后注入“长帧让累积器无限追赶形成死亡螺旋”，定位首个分叉并执行重置。这样该节点同时具备出现、解释、专属实验和练习验证四级证据。

### One small step, one giant step

固定模拟步长提供确定性，累积器与插值连接真实时间；追赶需设上限避免螺旋失控。

One small step, one giant step 的因果解释从“渲染插值”开始：先标出输入、所有者和可见结果，再说明循环非阻塞处理输入，按时间步更新状态并渲染，可用累积器协调频率。若没有模式的基线也能更简单地守住“相同真实时长内的模拟推进量与机器速度无关”，这一节点应得出“不采用”的结论。

One small step, one giant step 的验证只改变一个条件，记录真实时间、模拟时间、更新次数、插值比例与帧分位数；随后注入“长帧让累积器无限追赶形成死亡螺旋”，定位首个分叉并执行重置。这样该节点同时具备出现、解释、专属实验和练习验证四级证据。

### Play catch up

固定模拟步长提供确定性，累积器与插值连接真实时间；追赶需设上限避免螺旋失控。

Play catch up 的因果解释从“采集时间”开始：先标出输入、所有者和可见结果，再说明循环非阻塞处理输入，按时间步更新状态并渲染，可用累积器协调频率。若没有模式的基线也能更简单地守住“相同真实时长内的模拟推进量与机器速度无关”，这一节点应得出“不采用”的结论。

Play catch up 的验证只改变一个条件，记录真实时间、模拟时间、更新次数、插值比例与帧分位数；随后注入“长帧让累积器无限追赶形成死亡螺旋”，定位首个分叉并执行重置。这样该节点同时具备出现、解释、专属实验和练习验证四级证据。

### Stuck in the middle

固定模拟步长提供确定性，累积器与插值连接真实时间；追赶需设上限避免螺旋失控。

Stuck in the middle 的因果解释从“处理输入”开始：先标出输入、所有者和可见结果，再说明循环非阻塞处理输入，按时间步更新状态并渲染，可用累积器协调频率。若没有模式的基线也能更简单地守住“相同真实时长内的模拟推进量与机器速度无关”，这一节点应得出“不采用”的结论。

Stuck in the middle 的验证只改变一个条件，记录真实时间、模拟时间、更新次数、插值比例与帧分位数；随后注入“长帧让累积器无限追赶形成死亡螺旋”，定位首个分叉并执行重置。这样该节点同时具备出现、解释、专属实验和练习验证四级证据。

### Design Decisions

设计决策应列出互斥选项、选择依据、不可逆成本和触发重新评估的指标。

Design Decisions 的因果解释从“累积步长”开始：先标出输入、所有者和可见结果，再说明循环非阻塞处理输入，按时间步更新状态并渲染，可用累积器协调频率。若没有模式的基线也能更简单地守住“相同真实时长内的模拟推进量与机器速度无关”，这一节点应得出“不采用”的结论。

Design Decisions 的验证只改变一个条件，记录真实时间、模拟时间、更新次数、插值比例与帧分位数；随后注入“长帧让累积器无限追赶形成死亡螺旋”，定位首个分叉并执行重置。这样该节点同时具备出现、解释、专属实验和练习验证四级证据。

### Do you own the game loop, or does the platform?

循环所有权受平台约束；睡眠、垂直同步和后台节流要与输入延迟及功耗共同测量。

Do you own the game loop, or does the platform? 的因果解释从“更新模拟”开始：先标出输入、所有者和可见结果，再说明循环非阻塞处理输入，按时间步更新状态并渲染，可用累积器协调频率。若没有模式的基线也能更简单地守住“相同真实时长内的模拟推进量与机器速度无关”，这一节点应得出“不采用”的结论。

Do you own the game loop, or does the platform? 的验证只改变一个条件，记录真实时间、模拟时间、更新次数、插值比例与帧分位数；随后注入“长帧让累积器无限追赶形成死亡螺旋”，定位首个分叉并执行重置。这样该节点同时具备出现、解释、专属实验和练习验证四级证据。

### How do you manage power consumption?

循环所有权受平台约束；睡眠、垂直同步和后台节流要与输入延迟及功耗共同测量。

How do you manage power consumption? 的因果解释从“渲染插值”开始：先标出输入、所有者和可见结果，再说明循环非阻塞处理输入，按时间步更新状态并渲染，可用累积器协调频率。若没有模式的基线也能更简单地守住“相同真实时长内的模拟推进量与机器速度无关”，这一节点应得出“不采用”的结论。

How do you manage power consumption? 的验证只改变一个条件，记录真实时间、模拟时间、更新次数、插值比例与帧分位数；随后注入“长帧让累积器无限追赶形成死亡螺旋”，定位首个分叉并执行重置。这样该节点同时具备出现、解释、专属实验和练习验证四级证据。

### How do you control gameplay speed?

循环所有权受平台约束；睡眠、垂直同步和后台节流要与输入延迟及功耗共同测量。

How do you control gameplay speed? 的因果解释从“采集时间”开始：先标出输入、所有者和可见结果，再说明循环非阻塞处理输入，按时间步更新状态并渲染，可用累积器协调频率。若没有模式的基线也能更简单地守住“相同真实时长内的模拟推进量与机器速度无关”，这一节点应得出“不采用”的结论。

How do you control gameplay speed? 的验证只改变一个条件，记录真实时间、模拟时间、更新次数、插值比例与帧分位数；随后注入“长帧让累积器无限追赶形成死亡螺旋”，定位首个分叉并执行重置。这样该节点同时具备出现、解释、专属实验和练习验证四级证据。

### See Also

关联模式用于比较组合与替代，不应因为两个模式常一起出现就默认同时引入。

See Also 的因果解释从“处理输入”开始：先标出输入、所有者和可见结果，再说明循环非阻塞处理输入，按时间步更新状态并渲染，可用累积器协调频率。若没有模式的基线也能更简单地守住“相同真实时长内的模拟推进量与机器速度无关”，这一节点应得出“不采用”的结论。

See Also 的验证只改变一个条件，记录真实时间、模拟时间、更新次数、插值比例与帧分位数；随后注入“长帧让累积器无限追赶形成死亡螺旋”，定位首个分叉并执行重置。这样该节点同时具备出现、解释、专属实验和练习验证四级证据。

## 可迁移实现或计算骨架

```text
initial state -> 采集时间 -> 处理输入 -> 累积步长 -> 更新模拟 -> 渲染插值
fault injection -> 长帧让累积器无限追赶形成死亡螺旋
pass condition -> 相同真实时长内的模拟推进量与机器速度无关
reset -> initial state
```

该骨架只保存实验合同；真实项目还要把平台、构建、场景、输入和统计窗口固定下来，并保留基线实现以便回退。

<Callout type="trap" title="先选模式再找问题">
  9. Game Loop若没有“每次循环固定移动量会让更快处理器上的游戏运行得更快”的可重放样本，就不应引入候选模式。
</Callout>

<Callout type="trap" title="只看类图不看时序">
  9. Game Loop必须解释采集时间、处理输入、累积步长、更新模拟、渲染插值之间谁先发生；最终结果相同不代表中间状态安全。
</Callout>

<Callout type="trap" title="只测顺利路径">
  故意触发“长帧让累积器无限追赶形成死亡螺旋”；若真实时间、模拟时间、更新次数、插值比例与帧分位数无法定位首个分叉，实验和实现都不能通过。
</Callout>

## 本章练习与节点验证矩阵

<Exercises>

**问题 1：基线与选择。** 怎样证明时间步循环解决的是本页真实问题，而不是增加装饰性抽象？

<Answer>
  用同一构建、场景和输入分别运行每帧固定移动与时间步循环，比较真实时间、模拟时间、更新次数、插值比例与帧分位数；候选方案只有在守住“相同真实时长内的模拟推进量与机器速度无关”且主要代价下降时才保留。
</Answer>

**问题 2：逐节点验证。** 本页正式节点怎样从标题升级为可复查证据？

<Answer>
  - **9. Game Loop**：执行“采集时间”，注入“长帧让累积器无限追赶形成死亡螺旋”，以真实时间、模拟时间、更新次数、插值比例与帧分位数判定是否守住不变量。
  - **Intent**：执行“处理输入”，注入“长帧让累积器无限追赶形成死亡螺旋”，以真实时间、模拟时间、更新次数、插值比例与帧分位数判定是否守住不变量。
  - **Motivation**：执行“累积步长”，注入“长帧让累积器无限追赶形成死亡螺旋”，以真实时间、模拟时间、更新次数、插值比例与帧分位数判定是否守住不变量。
  - **Interview with a CPU**：执行“更新模拟”，注入“长帧让累积器无限追赶形成死亡螺旋”，以真实时间、模拟时间、更新次数、插值比例与帧分位数判定是否守住不变量。
  - **Event loops**：执行“渲染插值”，注入“长帧让累积器无限追赶形成死亡螺旋”，以真实时间、模拟时间、更新次数、插值比例与帧分位数判定是否守住不变量。
  - **A world out of time**：执行“采集时间”，注入“长帧让累积器无限追赶形成死亡螺旋”，以真实时间、模拟时间、更新次数、插值比例与帧分位数判定是否守住不变量。
  - **Seconds per second**：执行“处理输入”，注入“长帧让累积器无限追赶形成死亡螺旋”，以真实时间、模拟时间、更新次数、插值比例与帧分位数判定是否守住不变量。
  - **The Pattern**：执行“累积步长”，注入“长帧让累积器无限追赶形成死亡螺旋”，以真实时间、模拟时间、更新次数、插值比例与帧分位数判定是否守住不变量。
  - **When to Use It**：执行“更新模拟”，注入“长帧让累积器无限追赶形成死亡螺旋”，以真实时间、模拟时间、更新次数、插值比例与帧分位数判定是否守住不变量。
  - **Keep in Mind**：执行“渲染插值”，注入“长帧让累积器无限追赶形成死亡螺旋”，以真实时间、模拟时间、更新次数、插值比例与帧分位数判定是否守住不变量。
  - **You may need to coordinate with the platform’s event loop**：执行“采集时间”，注入“长帧让累积器无限追赶形成死亡螺旋”，以真实时间、模拟时间、更新次数、插值比例与帧分位数判定是否守住不变量。
  - **Sample Code**：执行“处理输入”，注入“长帧让累积器无限追赶形成死亡螺旋”，以真实时间、模拟时间、更新次数、插值比例与帧分位数判定是否守住不变量。
  - **Run, run as fast as you can**：执行“累积步长”，注入“长帧让累积器无限追赶形成死亡螺旋”，以真实时间、模拟时间、更新次数、插值比例与帧分位数判定是否守住不变量。
  - **Take a little nap**：执行“更新模拟”，注入“长帧让累积器无限追赶形成死亡螺旋”，以真实时间、模拟时间、更新次数、插值比例与帧分位数判定是否守住不变量。
  - **One small step, one giant step**：执行“渲染插值”，注入“长帧让累积器无限追赶形成死亡螺旋”，以真实时间、模拟时间、更新次数、插值比例与帧分位数判定是否守住不变量。
  - **Play catch up**：执行“采集时间”，注入“长帧让累积器无限追赶形成死亡螺旋”，以真实时间、模拟时间、更新次数、插值比例与帧分位数判定是否守住不变量。
  - **Stuck in the middle**：执行“处理输入”，注入“长帧让累积器无限追赶形成死亡螺旋”，以真实时间、模拟时间、更新次数、插值比例与帧分位数判定是否守住不变量。
  - **Design Decisions**：执行“累积步长”，注入“长帧让累积器无限追赶形成死亡螺旋”，以真实时间、模拟时间、更新次数、插值比例与帧分位数判定是否守住不变量。
  - **Do you own the game loop, or does the platform?**：执行“更新模拟”，注入“长帧让累积器无限追赶形成死亡螺旋”，以真实时间、模拟时间、更新次数、插值比例与帧分位数判定是否守住不变量。
  - **How do you manage power consumption?**：执行“渲染插值”，注入“长帧让累积器无限追赶形成死亡螺旋”，以真实时间、模拟时间、更新次数、插值比例与帧分位数判定是否守住不变量。
  - **How do you control gameplay speed?**：执行“采集时间”，注入“长帧让累积器无限追赶形成死亡螺旋”，以真实时间、模拟时间、更新次数、插值比例与帧分位数判定是否守住不变量。
  - **See Also**：执行“处理输入”，注入“长帧让累积器无限追赶形成死亡螺旋”，以真实时间、模拟时间、更新次数、插值比例与帧分位数判定是否守住不变量。
</Answer>

**问题 3：反例与复位。** 注入“长帧让累积器无限追赶形成死亡螺旋”后怎样关闭实验闭环？

<Answer>
  先定位第一个偏离“相同真实时长内的模拟推进量与机器速度无关”的阶段，只修复该原因；随后重放正常、压力和故障路径，并点击重置确认状态、代际和计数全部回到同一初值。
</Answer>

</Exercises>

## 术语复核与本章回顾

<Glossary>
  <GlossaryItem term="游戏循环">
    游戏循环在“9. Game Loop”中用于描述输入到可见结果之间的责任边界。
  </GlossaryItem>
  <GlossaryItem term="固定时间步">
    固定时间步在“9. Game Loop”中用于检查候选机制是否真正减少本章的主要成本。
  </GlossaryItem>
  <GlossaryItem term="累积器">
    累积器在“9. Game Loop”中用于记录对象、数据或消息在生命周期中的明确状态。
  </GlossaryItem>
  <GlossaryItem term="死亡螺旋">
    死亡螺旋在“9. Game Loop”中用于决定保留、替换或移除实现的可复核证据。
  </GlossaryItem>
</Glossary>

掌握“9. Game Loop”意味着能从“每次循环固定移动量会让更快处理器上的游戏运行得更快”出发，解释循环非阻塞处理输入，按时间步更新状态并渲染，可用累积器协调频率，再用真实时间、模拟时间、更新次数、插值比例与帧分位数推翻或保留实现。若“相同真实时长内的模拟推进量与机器速度无关”不能稳定复现，本章仍未通过。

## 阅读导航

[← 上一页：8. Double Buffer](/learn/game-programming-patterns/12-chapter-08-double-buffer/gpp-chapter-08-double-buffer) · [下一页：10. Update Method →](/learn/game-programming-patterns/14-chapter-10-update-method/gpp-chapter-10-update-method)

<Attribution
  mode="independent-rewrite"
  sourceBasis="full-text"
  workTitle="Robert Nystrom, Game Programming Patterns (2014)"
  adaptedUrl="https://gameprogrammingpatterns.com/game-loop.html"
/>
