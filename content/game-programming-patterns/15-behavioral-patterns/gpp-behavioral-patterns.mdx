---
title: IV. Behavioral Patterns
type: C
section: 游戏编程模式（作者2014 / 人邮2016）
order: 15
description: IV. Behavioral Patterns：按内容规模与信任边界选择类型对象、子类沙箱或字节码，通过可复位因果实验和反例证据验收。
demo: true
math: false
sourceUrl: 'https://gameprogrammingpatterns.com/behavioral-patterns.html'
draft: false
qualityVersion: 2
practiceMode: design
sourceMode: independent-rewrite
---
import {
  GppBehavioralPatternsMapLab,
  GppBehavioralPatternsExperimentLab,
  GppBehavioralPatternsEvidenceLab,
} from "@/components/mdx/game-programming-patterns/diagrams/gpp-behavioral-patterns";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

# IV. Behavioral Patterns

<Objectives>

- 能从“大量角色与技能行为若都硬编码，迭代速度和安全性会同时下降”重建本页的问题基线，不用模式名称替代需求。
- 能解释“从数据分类、受限原语到虚拟机逐级增加表达力和工具成本”中每个参与者、状态和时序边界。
- 能操作本章专属实验，比较全部硬编码与分级行为模型在三档负载下的因果差异。
- 能注入“为少量固定行为提前建设完整虚拟机”，用内容改动时间、可表达操作、沙箱边界与工具维护量决定保留、替换或移除方案。

</Objectives>

{/* GPP_QUALITY_V2 */}

## 为什么“IV. Behavioral Patterns”从问题证据开始

大量角色与技能行为若都硬编码，迭代速度和安全性会同时下降。本页不把模式当成必选答案，而是先保存无模式基线，再验证候选机制是否真正改变可观察结果。通过条件是“内容作者的表达能力不越过运行时安全边界”，而不是类或接口数量增加。

## 来源、版本与独立重写边界

本页用[作者完整在线正文](https://gameprogrammingpatterns.com/behavioral-patterns.html)核对正式标题、设计分叉和时代语境，并以[作者源码仓库](https://github.com/munificent/game-programming-patterns)交叉检查结构。[仓库许可证](https://raw.githubusercontent.com/munificent/game-programming-patterns/master/LICENSE)明确正文、HTML与样式为 CC BY-NC-ND 4.0，示例程序等其他文件为 MIT；因此下列中文解释、图示、交互和代码均为独立教学重写，不翻译、拼接或改写受 ND 限制的原文表达。

## 本章机制与术语

<Term def="行为规模在“IV. Behavioral Patterns”中用于描述输入到可见结果之间的责任边界。">行为规模</Term>、<Term def="表达层在“IV. Behavioral Patterns”中用于检查候选机制是否真正减少本章的主要成本。">表达层</Term>、<Term def="信任边界在“IV. Behavioral Patterns”中用于记录对象、数据或消息在生命周期中的明确状态。">信任边界</Term>、<Term def="工具成本在“IV. Behavioral Patterns”中用于决定保留、替换或移除实现的可复核证据。">工具成本</Term>。四个术语共同约束“按内容规模与信任边界选择类型对象、子类沙箱或字节码”，任何结论都必须回到内容改动时间、可表达操作、沙箱边界与工具维护量。

## 先预测，再操作三层专属实验

操作前先预测：从全部硬编码切到分级行为模型后，哪项迭代分钟先变化；负载扩大四倍时哪条边界先失效；注入“为少量固定行为提前建设完整虚拟机”后能否恢复初始状态。

<Stepper>
  <Step title="1. 节点与因果链">
    选择正式节点和机制阶段，核对输入、所有者、变化与验证证据。

    <GppBehavioralPatternsMapLab />

  </Step>
  <Step title="2. 基线与候选取舍">
    一次只改变实现、负载或故障，比较可复算结果，避免用总风险分数代替因果。

    <GppBehavioralPatternsExperimentLab />

  </Step>
  <Step title="3. 反例与复位">
    在正常、压力和反例路径间切换，验证不变量；最后重置并确认初始状态一致。

    <GppBehavioralPatternsEvidenceLab />

  </Step>
</Stepper>

## 官方结构逐项深读

### IV. Behavioral Patterns

行为模式让设计者组合、扩展和数据驱动游戏行为而不让类层次或硬编码分支失控。

IV. Behavioral Patterns 的因果解释从“统计行为”开始：先标出输入、所有者和可见结果，再说明从数据分类、受限原语到虚拟机逐级增加表达力和工具成本。若没有模式的基线也能更简单地守住“内容作者的表达能力不越过运行时安全边界”，这一节点应得出“不采用”的结论。

IV. Behavioral Patterns 的验证只改变一个条件，记录内容改动时间、可表达操作、沙箱边界与工具维护量；随后注入“为少量固定行为提前建设完整虚拟机”，定位首个分叉并执行重置。这样该节点同时具备出现、解释、专属实验和练习验证四级证据。

### The Patterns

分部导言建立模式之间的选择坐标，模式名称不是结论，问题、约束与反例才是选择依据。

The Patterns 的因果解释从“评估信任”开始：先标出输入、所有者和可见结果，再说明从数据分类、受限原语到虚拟机逐级增加表达力和工具成本。若没有模式的基线也能更简单地守住“内容作者的表达能力不越过运行时安全边界”，这一节点应得出“不采用”的结论。

The Patterns 的验证只改变一个条件，记录内容改动时间、可表达操作、沙箱边界与工具维护量；随后注入“为少量固定行为提前建设完整虚拟机”，定位首个分叉并执行重置。这样该节点同时具备出现、解释、专属实验和练习验证四级证据。

## 可迁移实现或计算骨架

```text
initial state -> 统计行为 -> 评估信任 -> 选择表达层 -> 限制能力 -> 测量迭代
fault injection -> 为少量固定行为提前建设完整虚拟机
pass condition -> 内容作者的表达能力不越过运行时安全边界
reset -> initial state
```

该骨架只保存实验合同；真实项目还要把平台、构建、场景、输入和统计窗口固定下来，并保留基线实现以便回退。

<Callout type="trap" title="先选模式再找问题">
  IV. Behavioral Patterns若没有“大量角色与技能行为若都硬编码，迭代速度和安全性会同时下降”的可重放样本，就不应引入候选模式。
</Callout>

<Callout type="trap" title="只看类图不看时序">
  IV. Behavioral Patterns必须解释统计行为、评估信任、选择表达层、限制能力、测量迭代之间谁先发生；最终结果相同不代表中间状态安全。
</Callout>

<Callout type="trap" title="只测顺利路径">
  故意触发“为少量固定行为提前建设完整虚拟机”；若内容改动时间、可表达操作、沙箱边界与工具维护量无法定位首个分叉，实验和实现都不能通过。
</Callout>

## 本章练习与节点验证矩阵

<Exercises>

**问题 1：基线与选择。** 怎样证明分级行为模型解决的是本页真实问题，而不是增加装饰性抽象？

<Answer>
  用同一构建、场景和输入分别运行全部硬编码与分级行为模型，比较内容改动时间、可表达操作、沙箱边界与工具维护量；候选方案只有在守住“内容作者的表达能力不越过运行时安全边界”且主要代价下降时才保留。
</Answer>

**问题 2：逐节点验证。** 本页正式节点怎样从标题升级为可复查证据？

<Answer>
  - **IV. Behavioral Patterns**：执行“统计行为”，注入“为少量固定行为提前建设完整虚拟机”，以内容改动时间、可表达操作、沙箱边界与工具维护量判定是否守住不变量。
  - **The Patterns**：执行“评估信任”，注入“为少量固定行为提前建设完整虚拟机”，以内容改动时间、可表达操作、沙箱边界与工具维护量判定是否守住不变量。
</Answer>

**问题 3：反例与复位。** 注入“为少量固定行为提前建设完整虚拟机”后怎样关闭实验闭环？

<Answer>
  先定位第一个偏离“内容作者的表达能力不越过运行时安全边界”的阶段，只修复该原因；随后重放正常、压力和故障路径，并点击重置确认状态、代际和计数全部回到同一初值。
</Answer>

</Exercises>

## 术语复核与本章回顾

<Glossary>
  <GlossaryItem term="行为规模">
    行为规模在“IV. Behavioral Patterns”中用于描述输入到可见结果之间的责任边界。
  </GlossaryItem>
  <GlossaryItem term="表达层">
    表达层在“IV. Behavioral Patterns”中用于检查候选机制是否真正减少本章的主要成本。
  </GlossaryItem>
  <GlossaryItem term="信任边界">
    信任边界在“IV. Behavioral Patterns”中用于记录对象、数据或消息在生命周期中的明确状态。
  </GlossaryItem>
  <GlossaryItem term="工具成本">
    工具成本在“IV. Behavioral Patterns”中用于决定保留、替换或移除实现的可复核证据。
  </GlossaryItem>
</Glossary>

掌握“IV. Behavioral Patterns”意味着能从“大量角色与技能行为若都硬编码，迭代速度和安全性会同时下降”出发，解释从数据分类、受限原语到虚拟机逐级增加表达力和工具成本，再用内容改动时间、可表达操作、沙箱边界与工具维护量推翻或保留实现。若“内容作者的表达能力不越过运行时安全边界”不能稳定复现，本章仍未通过。

## 阅读导航

[← 上一页：10. Update Method](/learn/game-programming-patterns/14-chapter-10-update-method/gpp-chapter-10-update-method) · [下一页：11. Bytecode →](/learn/game-programming-patterns/16-chapter-11-bytecode/gpp-chapter-11-bytecode)

<Attribution
  mode="independent-rewrite"
  sourceBasis="full-text"
  workTitle="Robert Nystrom, Game Programming Patterns (2014)"
  adaptedUrl="https://gameprogrammingpatterns.com/behavioral-patterns.html"
/>
