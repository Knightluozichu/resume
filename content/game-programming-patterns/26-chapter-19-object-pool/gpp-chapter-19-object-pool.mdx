---
title: 19. Object Pool
type: C
section: 游戏编程模式（作者2014 / 人邮2016）
order: 26
description: 19. Object Pool：预分配固定槽位并复用短命对象，控制碎片和运行时分配，通过可复位因果实验和反例证据验收。
demo: true
math: false
sourceUrl: 'https://gameprogrammingpatterns.com/object-pool.html'
draft: false
qualityVersion: 2
practiceMode: simulation
sourceMode: independent-rewrite
---
import { GppPatternLab } from "@/components/mdx/game-programming-patterns/v2/gpp-chapter-19-object-pool";

# 19. Object Pool


{/* GPP_QUALITY_V2 */}

## 为什么“19. Object Pool”从问题证据开始

粒子等对象频繁分配释放会产生碎片、停顿和不可预测失败。本页不把模式当成必选答案，而是先保存无模式基线，再验证候选机制是否真正改变可观察结果。通过条件是“任一槽位只属于一个活跃对象且复用后没有残留状态”，而不是类或接口数量增加。

## 来源、版本与独立重写边界

本页用[作者完整在线正文](https://gameprogrammingpatterns.com/object-pool.html)核对正式标题、设计分叉和时代语境，并以[作者源码仓库](https://github.com/munificent/game-programming-patterns)交叉检查结构。[仓库许可证](https://raw.githubusercontent.com/munificent/game-programming-patterns/master/LICENSE)明确正文、HTML与样式为 CC BY-NC-ND 4.0，示例程序等其他文件为 MIT；因此下列中文解释、图示、交互和代码均为独立教学重写，不翻译、拼接或改写受 ND 限制的原文表达。

## 本章机制与术语

、、、。四个术语共同约束“预分配固定槽位并复用短命对象，控制碎片和运行时分配”，任何结论都必须回到槽位代际、活跃计数、获取失败、清理字段与分配轨迹。

<GppPatternLab chapter="19" />

## 官方结构逐项深读

### 19. Object Pool

对象池预留同类对象槽位，用固定容量和显式重置换取可预测分配、碎片控制与峰值预算。

19. Object Pool 的因果解释从“预分配”开始：先标出输入、所有者和可见结果，再说明池用空闲链或标志跟踪槽位，获取时完整初始化，释放时清理所有状态。若没有模式的基线也能更简单地守住“任一槽位只属于一个活跃对象且复用后没有残留状态”，这一节点应得出“不采用”的结论。

19. Object Pool 的验证只改变一个条件，记录槽位代际、活跃计数、获取失败、清理字段与分配轨迹；随后注入“复用粒子保留上一代速度或回调”，定位首个分叉并执行重置。这样该节点同时具备出现、解释、专属实验和练习验证四级证据。

### Intent

本模式意图是解决对象池预留同类对象槽位，用固定容量和显式重置换取可预测分配、碎片控制与峰值预算。中的核心问题，必须先用失败示例证明问题真实存在。

Intent 的因果解释从“取得槽位”开始：先标出输入、所有者和可见结果，再说明池用空闲链或标志跟踪槽位，获取时完整初始化，释放时清理所有状态。若没有模式的基线也能更简单地守住“任一槽位只属于一个活跃对象且复用后没有残留状态”，这一节点应得出“不采用”的结论。

Intent 的验证只改变一个条件，记录槽位代际、活跃计数、获取失败、清理字段与分配轨迹；随后注入“复用粒子保留上一代速度或回调”，定位首个分叉并执行重置。这样该节点同时具备出现、解释、专属实验和练习验证四级证据。

### Motivation

动机示例把容量预算、空闲槽位、对象重置放入具体游戏场景，作为应用前后的比较基线。

Motivation 的因果解释从“完整初始化”开始：先标出输入、所有者和可见结果，再说明池用空闲链或标志跟踪槽位，获取时完整初始化，释放时清理所有状态。若没有模式的基线也能更简单地守住“任一槽位只属于一个活跃对象且复用后没有残留状态”，这一节点应得出“不采用”的结论。

Motivation 的验证只改变一个条件，记录槽位代际、活跃计数、获取失败、清理字段与分配轨迹；随后注入“复用粒子保留上一代速度或回调”，定位首个分叉并执行重置。这样该节点同时具备出现、解释、专属实验和练习验证四级证据。

### The curse of fragmentation

对象池用固定峰值换取分配确定性；溢出、重置、变长对象和闲置内存策略必须明确。

The curse of fragmentation 的因果解释从“使用对象”开始：先标出输入、所有者和可见结果，再说明池用空闲链或标志跟踪槽位，获取时完整初始化，释放时清理所有状态。若没有模式的基线也能更简单地守住“任一槽位只属于一个活跃对象且复用后没有残留状态”，这一节点应得出“不采用”的结论。

The curse of fragmentation 的验证只改变一个条件，记录槽位代际、活跃计数、获取失败、清理字段与分配轨迹；随后注入“复用粒子保留上一代速度或回调”，定位首个分叉并执行重置。这样该节点同时具备出现、解释、专属实验和练习验证四级证据。

### The best of both worlds

对象池用固定峰值换取分配确定性；溢出、重置、变长对象和闲置内存策略必须明确。

The best of both worlds 的因果解释从“清理归还”开始：先标出输入、所有者和可见结果，再说明池用空闲链或标志跟踪槽位，获取时完整初始化，释放时清理所有状态。若没有模式的基线也能更简单地守住“任一槽位只属于一个活跃对象且复用后没有残留状态”，这一节点应得出“不采用”的结论。

The best of both worlds 的验证只改变一个条件，记录槽位代际、活跃计数、获取失败、清理字段与分配轨迹；随后注入“复用粒子保留上一代速度或回调”，定位首个分叉并执行重置。这样该节点同时具备出现、解释、专属实验和练习验证四级证据。

### The Pattern

模式结构把“预算池容量 → 获取空闲槽位 → 完整初始化 → 使用并记录代际 → 重置归还池”中的参与者、消息、状态与时序压缩为最小合同。

The Pattern 的因果解释从“预分配”开始：先标出输入、所有者和可见结果，再说明池用空闲链或标志跟踪槽位，获取时完整初始化，释放时清理所有状态。若没有模式的基线也能更简单地守住“任一槽位只属于一个活跃对象且复用后没有残留状态”，这一节点应得出“不采用”的结论。

The Pattern 的验证只改变一个条件，记录槽位代际、活跃计数、获取失败、清理字段与分配轨迹；随后注入“复用粒子保留上一代速度或回调”，定位首个分叉并执行重置。这样该节点同时具备出现、解释、专属实验和练习验证四级证据。

### When to Use It

只有问题频率、变化压力或性能证据超过模式引入成本时才使用，并写下不适用条件。

When to Use It 的因果解释从“取得槽位”开始：先标出输入、所有者和可见结果，再说明池用空闲链或标志跟踪槽位，获取时完整初始化，释放时清理所有状态。若没有模式的基线也能更简单地守住“任一槽位只属于一个活跃对象且复用后没有残留状态”，这一节点应得出“不采用”的结论。

When to Use It 的验证只改变一个条件，记录槽位代际、活跃计数、获取失败、清理字段与分配轨迹；随后注入“复用粒子保留上一代速度或回调”，定位首个分叉并执行重置。这样该节点同时具备出现、解释、专属实验和练习验证四级证据。

### Keep in Mind

代价清单必须覆盖间接层、内存、生命周期、调试、线程和团队理解成本。

Keep in Mind 的因果解释从“完整初始化”开始：先标出输入、所有者和可见结果，再说明池用空闲链或标志跟踪槽位，获取时完整初始化，释放时清理所有状态。若没有模式的基线也能更简单地守住“任一槽位只属于一个活跃对象且复用后没有残留状态”，这一节点应得出“不采用”的结论。

Keep in Mind 的验证只改变一个条件，记录槽位代际、活跃计数、获取失败、清理字段与分配轨迹；随后注入“复用粒子保留上一代速度或回调”，定位首个分叉并执行重置。这样该节点同时具备出现、解释、专属实验和练习验证四级证据。

### The pool may waste memory on unneeded objects

对象池用固定峰值换取分配确定性；溢出、重置、变长对象和闲置内存策略必须明确。

The pool may waste memory on unneeded objects 的因果解释从“使用对象”开始：先标出输入、所有者和可见结果，再说明池用空闲链或标志跟踪槽位，获取时完整初始化，释放时清理所有状态。若没有模式的基线也能更简单地守住“任一槽位只属于一个活跃对象且复用后没有残留状态”，这一节点应得出“不采用”的结论。

The pool may waste memory on unneeded objects 的验证只改变一个条件，记录槽位代际、活跃计数、获取失败、清理字段与分配轨迹；随后注入“复用粒子保留上一代速度或回调”，定位首个分叉并执行重置。这样该节点同时具备出现、解释、专属实验和练习验证四级证据。

### Only a fixed number of objects can be active at any one time

对象池用固定峰值换取分配确定性；溢出、重置、变长对象和闲置内存策略必须明确。

Only a fixed number of objects can be active at any one time 的因果解释从“清理归还”开始：先标出输入、所有者和可见结果，再说明池用空闲链或标志跟踪槽位，获取时完整初始化，释放时清理所有状态。若没有模式的基线也能更简单地守住“任一槽位只属于一个活跃对象且复用后没有残留状态”，这一节点应得出“不采用”的结论。

Only a fixed number of objects can be active at any one time 的验证只改变一个条件，记录槽位代际、活跃计数、获取失败、清理字段与分配轨迹；随后注入“复用粒子保留上一代速度或回调”，定位首个分叉并执行重置。这样该节点同时具备出现、解释、专属实验和练习验证四级证据。

### Memory size for each object is fixed

Memory size for each object is fixed 聚焦运行代价，固定场景后用槽位代际、活跃计数、获取失败、清理字段与分配轨迹定位首个超限点，不能以模式名称推断快慢。

Memory size for each object is fixed 的因果解释从“预分配”开始：先标出输入、所有者和可见结果，再说明池用空闲链或标志跟踪槽位，获取时完整初始化，释放时清理所有状态。若没有模式的基线也能更简单地守住“任一槽位只属于一个活跃对象且复用后没有残留状态”，这一节点应得出“不采用”的结论。

Memory size for each object is fixed 的验证只改变一个条件，记录槽位代际、活跃计数、获取失败、清理字段与分配轨迹；随后注入“复用粒子保留上一代速度或回调”，定位首个分叉并执行重置。这样该节点同时具备出现、解释、专属实验和练习验证四级证据。

### Reused objects aren’t automatically cleared

对象池用固定峰值换取分配确定性；溢出、重置、变长对象和闲置内存策略必须明确。

Reused objects aren’t automatically cleared 的因果解释从“取得槽位”开始：先标出输入、所有者和可见结果，再说明池用空闲链或标志跟踪槽位，获取时完整初始化，释放时清理所有状态。若没有模式的基线也能更简单地守住“任一槽位只属于一个活跃对象且复用后没有残留状态”，这一节点应得出“不采用”的结论。

Reused objects aren’t automatically cleared 的验证只改变一个条件，记录槽位代际、活跃计数、获取失败、清理字段与分配轨迹；随后注入“复用粒子保留上一代速度或回调”，定位首个分叉并执行重置。这样该节点同时具备出现、解释、专属实验和练习验证四级证据。

### Unused objects will remain in memory

对象池用固定峰值换取分配确定性；溢出、重置、变长对象和闲置内存策略必须明确。

Unused objects will remain in memory 的因果解释从“完整初始化”开始：先标出输入、所有者和可见结果，再说明池用空闲链或标志跟踪槽位，获取时完整初始化，释放时清理所有状态。若没有模式的基线也能更简单地守住“任一槽位只属于一个活跃对象且复用后没有残留状态”，这一节点应得出“不采用”的结论。

Unused objects will remain in memory 的验证只改变一个条件，记录槽位代际、活跃计数、获取失败、清理字段与分配轨迹；随后注入“复用粒子保留上一代速度或回调”，定位首个分叉并执行重置。这样该节点同时具备出现、解释、专属实验和练习验证四级证据。

### Sample Code

先理解本书是问题驱动的模式目录，示例C++用于表达意图而不是可直接复制的现代生产代码。

Sample Code 的因果解释从“使用对象”开始：先标出输入、所有者和可见结果，再说明池用空闲链或标志跟踪槽位，获取时完整初始化，释放时清理所有状态。若没有模式的基线也能更简单地守住“任一槽位只属于一个活跃对象且复用后没有残留状态”，这一节点应得出“不采用”的结论。

Sample Code 的验证只改变一个条件，记录槽位代际、活跃计数、获取失败、清理字段与分配轨迹；随后注入“复用粒子保留上一代速度或回调”，定位首个分叉并执行重置。这样该节点同时具备出现、解释、专属实验和练习验证四级证据。

### A free list

空闲链可复用对象自身存储；代际句柄阻止悬空引用，归还与再次获取都要完整重置。

A free list 的因果解释从“清理归还”开始：先标出输入、所有者和可见结果，再说明池用空闲链或标志跟踪槽位，获取时完整初始化，释放时清理所有状态。若没有模式的基线也能更简单地守住“任一槽位只属于一个活跃对象且复用后没有残留状态”，这一节点应得出“不采用”的结论。

A free list 的验证只改变一个条件，记录槽位代际、活跃计数、获取失败、清理字段与分配轨迹；随后注入“复用粒子保留上一代速度或回调”，定位首个分叉并执行重置。这样该节点同时具备出现、解释、专属实验和练习验证四级证据。

### Design Decisions

设计决策应列出互斥选项、选择依据、不可逆成本和触发重新评估的指标。

Design Decisions 的因果解释从“预分配”开始：先标出输入、所有者和可见结果，再说明池用空闲链或标志跟踪槽位，获取时完整初始化，释放时清理所有状态。若没有模式的基线也能更简单地守住“任一槽位只属于一个活跃对象且复用后没有残留状态”，这一节点应得出“不采用”的结论。

Design Decisions 的验证只改变一个条件，记录槽位代际、活跃计数、获取失败、清理字段与分配轨迹；随后注入“复用粒子保留上一代速度或回调”，定位首个分叉并执行重置。这样该节点同时具备出现、解释、专属实验和练习验证四级证据。

### Are objects coupled to the pool?

空闲链可复用对象自身存储；代际句柄阻止悬空引用，归还与再次获取都要完整重置。

Are objects coupled to the pool? 的因果解释从“取得槽位”开始：先标出输入、所有者和可见结果，再说明池用空闲链或标志跟踪槽位，获取时完整初始化，释放时清理所有状态。若没有模式的基线也能更简单地守住“任一槽位只属于一个活跃对象且复用后没有残留状态”，这一节点应得出“不采用”的结论。

Are objects coupled to the pool? 的验证只改变一个条件，记录槽位代际、活跃计数、获取失败、清理字段与分配轨迹；随后注入“复用粒子保留上一代速度或回调”，定位首个分叉并执行重置。这样该节点同时具备出现、解释、专属实验和练习验证四级证据。

### What is responsible for initializing the reused objects?

空闲链可复用对象自身存储；代际句柄阻止悬空引用，归还与再次获取都要完整重置。

What is responsible for initializing the reused objects? 的因果解释从“完整初始化”开始：先标出输入、所有者和可见结果，再说明池用空闲链或标志跟踪槽位，获取时完整初始化，释放时清理所有状态。若没有模式的基线也能更简单地守住“任一槽位只属于一个活跃对象且复用后没有残留状态”，这一节点应得出“不采用”的结论。

What is responsible for initializing the reused objects? 的验证只改变一个条件，记录槽位代际、活跃计数、获取失败、清理字段与分配轨迹；随后注入“复用粒子保留上一代速度或回调”，定位首个分叉并执行重置。这样该节点同时具备出现、解释、专属实验和练习验证四级证据。

### See Also

关联模式用于比较组合与替代，不应因为两个模式常一起出现就默认同时引入。

See Also 的因果解释从“使用对象”开始：先标出输入、所有者和可见结果，再说明池用空闲链或标志跟踪槽位，获取时完整初始化，释放时清理所有状态。若没有模式的基线也能更简单地守住“任一槽位只属于一个活跃对象且复用后没有残留状态”，这一节点应得出“不采用”的结论。

See Also 的验证只改变一个条件，记录槽位代际、活跃计数、获取失败、清理字段与分配轨迹；随后注入“复用粒子保留上一代速度或回调”，定位首个分叉并执行重置。这样该节点同时具备出现、解释、专属实验和练习验证四级证据。

## 可迁移实现或计算骨架

```text
initial state -> 预分配 -> 取得槽位 -> 完整初始化 -> 使用对象 -> 清理归还
fault injection -> 复用粒子保留上一代速度或回调
pass condition -> 任一槽位只属于一个活跃对象且复用后没有残留状态
reset -> initial state
```

该骨架只保存实验合同；真实项目还要把平台、构建、场景、输入和统计窗口固定下来，并保留基线实现以便回退。


## 本章练习与节点验证矩阵


## 术语复核与本章回顾


掌握“19. Object Pool”意味着能从“粒子等对象频繁分配释放会产生碎片、停顿和不可预测失败”出发，解释池用空闲链或标志跟踪槽位，获取时完整初始化，释放时清理所有状态，再用槽位代际、活跃计数、获取失败、清理字段与分配轨迹推翻或保留实现。若“任一槽位只属于一个活跃对象且复用后没有残留状态”不能稳定复现，本章仍未通过。

## 阅读导航

[← 上一页：18. Dirty Flag](/learn/game-programming-patterns/25-chapter-18-dirty-flag/gpp-chapter-18-dirty-flag) · [下一页：20. Spatial Partition →](/learn/game-programming-patterns/27-chapter-20-spatial-partition/gpp-chapter-20-spatial-partition)

