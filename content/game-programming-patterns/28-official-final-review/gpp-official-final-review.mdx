---
title: "《游戏编程模式》全书总复习"
type: C
section: "游戏编程模式（作者2014 / 人邮2016）"
order: 28
description: "总复习以变化传播、帧时序、行为扩展、依赖显式度和目标机性能五条证据链验收模式应用。 逐项覆盖21个作者源码标题，并用变化、时序、依赖和目标机证据复核。"
demo: true
math: false
sourceUrl: "https://gameprogrammingpatterns.com/contents.html"
draft: false
---

import {
  GppOfficialFinalReviewMapLab,
  GppOfficialFinalReviewExperimentLab,
  GppOfficialFinalReviewEvidenceLab,
} from "@/components/mdx/game-programming-patterns/diagrams/gpp-official-final-review";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释《游戏编程模式》全书总复习在27个正式单元、20章与351个权威节点中的位置
- 能绘制“重放需求变化 → 检查帧级时序 → 注入行为扩展 → 审计隐藏依赖 → 签收性能与移除条件”机制链并标出依赖、所有权与帧时序
- 能比较模式前后的变化传播、帧预算、队列延迟与缓存成本
- 能设计基线、适用、反例和移除实验，让第三方独立复核模式选择

</Objectives>

## 为什么模式选择必须从问题证据开始

总复习以变化传播、帧时序、行为扩展、依赖显式度和目标机性能五条证据链验收模式应用。 本课程不复制原书正文、插图或示例代码，而把作者公开目录和源码标题重构为问题合同、交互决策、反例与验收工件。本页逐项覆盖21个权威坐标：《游戏编程模式》全书总复习、1. Architecture, Performance, and Games、2. Command、3. Flyweight、4. Observer、5. Prototype、6. Singleton、7. State、8. Double Buffer、9. Game Loop、10. Update Method、11. Bytecode、12. Subclass Sandbox、13. Type Object、14. Component、15. Event Queue、16. Service Locator、17. Data Locality、18. Dirty Flag、19. Object Pool、20. Spatial Partition。

设计模式不是类图模板。命令解决动作对象化，状态解决行为随状态变化，事件队列解决时间解耦，数据局部性解决访问布局；它们的参与者、时序和代价完全不同。只有先展示没有模式时的具体失败，再定义预期变化和目标机预算，才能判断引入的间接层是否真正减少了复杂度。

## 版次、目录与改编合同

本路径采用Robert Nystrom《Game Programming Patterns》，Genever Benning，2014，ISBN 9780990582908；GPP翻译组译《游戏编程模式》，人民邮电出版社，2016年9月，ISBN 9787115426888。作者官网与开源书稿完整列出致谢、6个分部导言、20章及章内各级标题，共351个正式节点；2014纸书样章确认ISBN、章节与页码，馆藏记录确认中译本版次。课程按27个正式单元逐项覆盖，另设学习地图与全书复核，共29页。

原书示例使用为表达模式而简化的C++，作者明确它不是现代C++风格指南。课程保留模式意图和设计空间，但实现必须使用当前项目的所有权、错误、线程与测试约定；任何模式都允许在证据不足时不采用或删除。

## 五个贯穿全书的术语

<Term def="促使代码结构发生变化的独立需求维度，例如输入设备、行为种类或存储布局。">
  变化轴
</Term>
、<Term def="一个改动需要理解或修改的模块、接口和测试数量。">变化传播</Term>、
<Term def="对象、消息或缓冲区在一帧内被读取、写入和发布的明确顺序。">
  帧时序合同
</Term>
、
<Term def="模式新增的类型、间接调用、内存、同步、工具和认知负担。">
  模式成本
</Term>
、
<Term def="记录问题基线、候选方案、取舍、指标、反例和重新评估条件的工件。">
  模式决策记录
</Term>
。

变化轴决定边界；变化传播衡量架构是否有效；帧时序合同防止抽象破坏实时正确性；模式成本阻止为了“优雅”过度设计；模式决策记录则保留为什么采用以及何时删除的证据。

## 四个可复算指标

一次需求变化的传播率可写为：

$
R_{change}=\frac{N_{modules\ touched}+N_{contracts\ changed}}{N_{requested\ changes}}
$

实时帧预算保留更新、渲染重叠与等待：

$
T_{frame}=\max(T_{update},T_{render})+T_{queue}+T_{sync}
$

事件队列端到端延迟为入队、等待和处理之和：

$
L_{event}=T_{enqueue}+T_{wait}+T_{handle}
$

连续热数据访问的缓存行下界近似为：

$
N_{lines}=\left\lceil\frac{N_{items}S_{hot}}{S_{line}}\right\rceil
$

传播率要和改动风险一起解释；帧预算报告P50/P95/P99；队列延迟必须带事件来源和批次；缓存模型还需用目标机硬件计数器验证。公式用于统一比较口径，不证明某个模式天然更优。

## 分步可视化：从依赖图到反例

<Stepper>
  <Step title="先画参与者和变化轴">
    <GppOfficialFinalReviewMapLab />
  </Step>
  <Step title="再改变对象、事件与线程规模">
    <GppOfficialFinalReviewExperimentLab />
  </Step>
  <Step title="最后注入生命周期和时序故障">
    <GppOfficialFinalReviewEvidenceLab />
  </Step>
</Stepper>

### 《游戏编程模式》全书总复习

**目录节点 1/21。** 总复习以变化传播、帧时序、行为扩展、依赖显式度和目标机性能五条证据链验收模式应用。

落实“《游戏编程模式》全书总复习”时，沿“重放需求变化 → 检查帧级时序 → 注入行为扩展 → 审计隐藏依赖 → 签收性能与移除条件”标出谁创建、谁调用、谁拥有、何时可见和如何失败，并把变化传播转成可测试状态。至少保存不使用模式的基线、最小模式实现和一个反例；如果新需求仍需跨越边界修改多处，说明模式没有切中变化轴。

本节点的验收同时观察正确性、变化传播、帧时序、分配、缓存、队列和调试体验。模式若只减少代码行却隐藏依赖，或只提升微基准却增加关卡制作成本，都必须在决策记录中列为代价，而不是包装成“最佳实践”。

### 1. Architecture, Performance, and Games

**目录节点 2/21。** “1. Architecture, Performance, and Games”位于“重放需求变化 → 检查帧级时序 → 注入行为扩展 → 审计隐藏依赖 → 签收性能与移除条件”中；实现前要写出问题证据、参与者、时序、所有权、成本与移除条件。

落实“1. Architecture, Performance, and Games”时，沿“重放需求变化 → 检查帧级时序 → 注入行为扩展 → 审计隐藏依赖 → 签收性能与移除条件”标出谁创建、谁调用、谁拥有、何时可见和如何失败，并把帧级时序转成可测试状态。至少保存不使用模式的基线、最小模式实现和一个反例；如果新需求仍需跨越边界修改多处，说明模式没有切中变化轴。

本节点的验收同时观察正确性、变化传播、帧时序、分配、缓存、队列和调试体验。模式若只减少代码行却隐藏依赖，或只提升微基准却增加关卡制作成本，都必须在决策记录中列为代价，而不是包装成“最佳实践”。

### 2. Command

**目录节点 3/21。** “2. Command”位于“重放需求变化 → 检查帧级时序 → 注入行为扩展 → 审计隐藏依赖 → 签收性能与移除条件”中；实现前要写出问题证据、参与者、时序、所有权、成本与移除条件。

落实“2. Command”时，沿“重放需求变化 → 检查帧级时序 → 注入行为扩展 → 审计隐藏依赖 → 签收性能与移除条件”标出谁创建、谁调用、谁拥有、何时可见和如何失败，并把行为扩展转成可测试状态。至少保存不使用模式的基线、最小模式实现和一个反例；如果新需求仍需跨越边界修改多处，说明模式没有切中变化轴。

本节点的验收同时观察正确性、变化传播、帧时序、分配、缓存、队列和调试体验。模式若只减少代码行却隐藏依赖，或只提升微基准却增加关卡制作成本，都必须在决策记录中列为代价，而不是包装成“最佳实践”。

### 3. Flyweight

**目录节点 4/21。** 享元把树模型等共享固有状态与位置、颜色等外在状态分离，实例只保存轻量索引。

落实“3. Flyweight”时，沿“重放需求变化 → 检查帧级时序 → 注入行为扩展 → 审计隐藏依赖 → 签收性能与移除条件”标出谁创建、谁调用、谁拥有、何时可见和如何失败，并把隐藏依赖转成可测试状态。至少保存不使用模式的基线、最小模式实现和一个反例；如果新需求仍需跨越边界修改多处，说明模式没有切中变化轴。

本节点的验收同时观察正确性、变化传播、帧时序、分配、缓存、队列和调试体验。模式若只减少代码行却隐藏依赖，或只提升微基准却增加关卡制作成本，都必须在决策记录中列为代价，而不是包装成“最佳实践”。

### 4. Observer

**目录节点 5/21。** “4. Observer”位于“重放需求变化 → 检查帧级时序 → 注入行为扩展 → 审计隐藏依赖 → 签收性能与移除条件”中；实现前要写出问题证据、参与者、时序、所有权、成本与移除条件。

落实“4. Observer”时，沿“重放需求变化 → 检查帧级时序 → 注入行为扩展 → 审计隐藏依赖 → 签收性能与移除条件”标出谁创建、谁调用、谁拥有、何时可见和如何失败，并把移除条件转成可测试状态。至少保存不使用模式的基线、最小模式实现和一个反例；如果新需求仍需跨越边界修改多处，说明模式没有切中变化轴。

本节点的验收同时观察正确性、变化传播、帧时序、分配、缓存、队列和调试体验。模式若只减少代码行却隐藏依赖，或只提升微基准却增加关卡制作成本，都必须在决策记录中列为代价，而不是包装成“最佳实践”。

### 5. Prototype

**目录节点 6/21。** “5. Prototype”位于“重放需求变化 → 检查帧级时序 → 注入行为扩展 → 审计隐藏依赖 → 签收性能与移除条件”中；实现前要写出问题证据、参与者、时序、所有权、成本与移除条件。

落实“5. Prototype”时，沿“重放需求变化 → 检查帧级时序 → 注入行为扩展 → 审计隐藏依赖 → 签收性能与移除条件”标出谁创建、谁调用、谁拥有、何时可见和如何失败，并把变化传播转成可测试状态。至少保存不使用模式的基线、最小模式实现和一个反例；如果新需求仍需跨越边界修改多处，说明模式没有切中变化轴。

本节点的验收同时观察正确性、变化传播、帧时序、分配、缓存、队列和调试体验。模式若只减少代码行却隐藏依赖，或只提升微基准却增加关卡制作成本，都必须在决策记录中列为代价，而不是包装成“最佳实践”。

### 6. Singleton

**目录节点 7/21。** “6. Singleton”位于“重放需求变化 → 检查帧级时序 → 注入行为扩展 → 审计隐藏依赖 → 签收性能与移除条件”中；实现前要写出问题证据、参与者、时序、所有权、成本与移除条件。

落实“6. Singleton”时，沿“重放需求变化 → 检查帧级时序 → 注入行为扩展 → 审计隐藏依赖 → 签收性能与移除条件”标出谁创建、谁调用、谁拥有、何时可见和如何失败，并把帧级时序转成可测试状态。至少保存不使用模式的基线、最小模式实现和一个反例；如果新需求仍需跨越边界修改多处，说明模式没有切中变化轴。

本节点的验收同时观察正确性、变化传播、帧时序、分配、缓存、队列和调试体验。模式若只减少代码行却隐藏依赖，或只提升微基准却增加关卡制作成本，都必须在决策记录中列为代价，而不是包装成“最佳实践”。

### 7. State

**目录节点 8/21。** “7. State”位于“重放需求变化 → 检查帧级时序 → 注入行为扩展 → 审计隐藏依赖 → 签收性能与移除条件”中；实现前要写出问题证据、参与者、时序、所有权、成本与移除条件。

落实“7. State”时，沿“重放需求变化 → 检查帧级时序 → 注入行为扩展 → 审计隐藏依赖 → 签收性能与移除条件”标出谁创建、谁调用、谁拥有、何时可见和如何失败，并把行为扩展转成可测试状态。至少保存不使用模式的基线、最小模式实现和一个反例；如果新需求仍需跨越边界修改多处，说明模式没有切中变化轴。

本节点的验收同时观察正确性、变化传播、帧时序、分配、缓存、队列和调试体验。模式若只减少代码行却隐藏依赖，或只提升微基准却增加关卡制作成本，都必须在决策记录中列为代价，而不是包装成“最佳实践”。

### 8. Double Buffer

**目录节点 9/21。** “8. Double Buffer”位于“重放需求变化 → 检查帧级时序 → 注入行为扩展 → 审计隐藏依赖 → 签收性能与移除条件”中；实现前要写出问题证据、参与者、时序、所有权、成本与移除条件。

落实“8. Double Buffer”时，沿“重放需求变化 → 检查帧级时序 → 注入行为扩展 → 审计隐藏依赖 → 签收性能与移除条件”标出谁创建、谁调用、谁拥有、何时可见和如何失败，并把隐藏依赖转成可测试状态。至少保存不使用模式的基线、最小模式实现和一个反例；如果新需求仍需跨越边界修改多处，说明模式没有切中变化轴。

本节点的验收同时观察正确性、变化传播、帧时序、分配、缓存、队列和调试体验。模式若只减少代码行却隐藏依赖，或只提升微基准却增加关卡制作成本，都必须在决策记录中列为代价，而不是包装成“最佳实践”。

### 9. Game Loop

**目录节点 10/21。** “9. Game Loop”位于“重放需求变化 → 检查帧级时序 → 注入行为扩展 → 审计隐藏依赖 → 签收性能与移除条件”中；实现前要写出问题证据、参与者、时序、所有权、成本与移除条件。

落实“9. Game Loop”时，沿“重放需求变化 → 检查帧级时序 → 注入行为扩展 → 审计隐藏依赖 → 签收性能与移除条件”标出谁创建、谁调用、谁拥有、何时可见和如何失败，并把移除条件转成可测试状态。至少保存不使用模式的基线、最小模式实现和一个反例；如果新需求仍需跨越边界修改多处，说明模式没有切中变化轴。

本节点的验收同时观察正确性、变化传播、帧时序、分配、缓存、队列和调试体验。模式若只减少代码行却隐藏依赖，或只提升微基准却增加关卡制作成本，都必须在决策记录中列为代价，而不是包装成“最佳实践”。

### 10. Update Method

**目录节点 11/21。** “10. Update Method”位于“重放需求变化 → 检查帧级时序 → 注入行为扩展 → 审计隐藏依赖 → 签收性能与移除条件”中；实现前要写出问题证据、参与者、时序、所有权、成本与移除条件。

落实“10. Update Method”时，沿“重放需求变化 → 检查帧级时序 → 注入行为扩展 → 审计隐藏依赖 → 签收性能与移除条件”标出谁创建、谁调用、谁拥有、何时可见和如何失败，并把变化传播转成可测试状态。至少保存不使用模式的基线、最小模式实现和一个反例；如果新需求仍需跨越边界修改多处，说明模式没有切中变化轴。

本节点的验收同时观察正确性、变化传播、帧时序、分配、缓存、队列和调试体验。模式若只减少代码行却隐藏依赖，或只提升微基准却增加关卡制作成本，都必须在决策记录中列为代价，而不是包装成“最佳实践”。

### 11. Bytecode

**目录节点 12/21。** “11. Bytecode”位于“重放需求变化 → 检查帧级时序 → 注入行为扩展 → 审计隐藏依赖 → 签收性能与移除条件”中；实现前要写出问题证据、参与者、时序、所有权、成本与移除条件。

落实“11. Bytecode”时，沿“重放需求变化 → 检查帧级时序 → 注入行为扩展 → 审计隐藏依赖 → 签收性能与移除条件”标出谁创建、谁调用、谁拥有、何时可见和如何失败，并把帧级时序转成可测试状态。至少保存不使用模式的基线、最小模式实现和一个反例；如果新需求仍需跨越边界修改多处，说明模式没有切中变化轴。

本节点的验收同时观察正确性、变化传播、帧时序、分配、缓存、队列和调试体验。模式若只减少代码行却隐藏依赖，或只提升微基准却增加关卡制作成本，都必须在决策记录中列为代价，而不是包装成“最佳实践”。

### 12. Subclass Sandbox

**目录节点 13/21。** “12. Subclass Sandbox”位于“重放需求变化 → 检查帧级时序 → 注入行为扩展 → 审计隐藏依赖 → 签收性能与移除条件”中；实现前要写出问题证据、参与者、时序、所有权、成本与移除条件。

落实“12. Subclass Sandbox”时，沿“重放需求变化 → 检查帧级时序 → 注入行为扩展 → 审计隐藏依赖 → 签收性能与移除条件”标出谁创建、谁调用、谁拥有、何时可见和如何失败，并把行为扩展转成可测试状态。至少保存不使用模式的基线、最小模式实现和一个反例；如果新需求仍需跨越边界修改多处，说明模式没有切中变化轴。

本节点的验收同时观察正确性、变化传播、帧时序、分配、缓存、队列和调试体验。模式若只减少代码行却隐藏依赖，或只提升微基准却增加关卡制作成本，都必须在决策记录中列为代价，而不是包装成“最佳实践”。

### 13. Type Object

**目录节点 14/21。** “13. Type Object”位于“重放需求变化 → 检查帧级时序 → 注入行为扩展 → 审计隐藏依赖 → 签收性能与移除条件”中；实现前要写出问题证据、参与者、时序、所有权、成本与移除条件。

落实“13. Type Object”时，沿“重放需求变化 → 检查帧级时序 → 注入行为扩展 → 审计隐藏依赖 → 签收性能与移除条件”标出谁创建、谁调用、谁拥有、何时可见和如何失败，并把隐藏依赖转成可测试状态。至少保存不使用模式的基线、最小模式实现和一个反例；如果新需求仍需跨越边界修改多处，说明模式没有切中变化轴。

本节点的验收同时观察正确性、变化传播、帧时序、分配、缓存、队列和调试体验。模式若只减少代码行却隐藏依赖，或只提升微基准却增加关卡制作成本，都必须在决策记录中列为代价，而不是包装成“最佳实践”。

### 14. Component

**目录节点 15/21。** “14. Component”位于“重放需求变化 → 检查帧级时序 → 注入行为扩展 → 审计隐藏依赖 → 签收性能与移除条件”中；实现前要写出问题证据、参与者、时序、所有权、成本与移除条件。

落实“14. Component”时，沿“重放需求变化 → 检查帧级时序 → 注入行为扩展 → 审计隐藏依赖 → 签收性能与移除条件”标出谁创建、谁调用、谁拥有、何时可见和如何失败，并把移除条件转成可测试状态。至少保存不使用模式的基线、最小模式实现和一个反例；如果新需求仍需跨越边界修改多处，说明模式没有切中变化轴。

本节点的验收同时观察正确性、变化传播、帧时序、分配、缓存、队列和调试体验。模式若只减少代码行却隐藏依赖，或只提升微基准却增加关卡制作成本，都必须在决策记录中列为代价，而不是包装成“最佳实践”。

### 15. Event Queue

**目录节点 16/21。** “15. Event Queue”位于“重放需求变化 → 检查帧级时序 → 注入行为扩展 → 审计隐藏依赖 → 签收性能与移除条件”中；实现前要写出问题证据、参与者、时序、所有权、成本与移除条件。

落实“15. Event Queue”时，沿“重放需求变化 → 检查帧级时序 → 注入行为扩展 → 审计隐藏依赖 → 签收性能与移除条件”标出谁创建、谁调用、谁拥有、何时可见和如何失败，并把变化传播转成可测试状态。至少保存不使用模式的基线、最小模式实现和一个反例；如果新需求仍需跨越边界修改多处，说明模式没有切中变化轴。

本节点的验收同时观察正确性、变化传播、帧时序、分配、缓存、队列和调试体验。模式若只减少代码行却隐藏依赖，或只提升微基准却增加关卡制作成本，都必须在决策记录中列为代价，而不是包装成“最佳实践”。

### 16. Service Locator

**目录节点 17/21。** “16. Service Locator”位于“重放需求变化 → 检查帧级时序 → 注入行为扩展 → 审计隐藏依赖 → 签收性能与移除条件”中；实现前要写出问题证据、参与者、时序、所有权、成本与移除条件。

落实“16. Service Locator”时，沿“重放需求变化 → 检查帧级时序 → 注入行为扩展 → 审计隐藏依赖 → 签收性能与移除条件”标出谁创建、谁调用、谁拥有、何时可见和如何失败，并把帧级时序转成可测试状态。至少保存不使用模式的基线、最小模式实现和一个反例；如果新需求仍需跨越边界修改多处，说明模式没有切中变化轴。

本节点的验收同时观察正确性、变化传播、帧时序、分配、缓存、队列和调试体验。模式若只减少代码行却隐藏依赖，或只提升微基准却增加关卡制作成本，都必须在决策记录中列为代价，而不是包装成“最佳实践”。

### 17. Data Locality

**目录节点 18/21。** “17. Data Locality”位于“重放需求变化 → 检查帧级时序 → 注入行为扩展 → 审计隐藏依赖 → 签收性能与移除条件”中；实现前要写出问题证据、参与者、时序、所有权、成本与移除条件。

落实“17. Data Locality”时，沿“重放需求变化 → 检查帧级时序 → 注入行为扩展 → 审计隐藏依赖 → 签收性能与移除条件”标出谁创建、谁调用、谁拥有、何时可见和如何失败，并把行为扩展转成可测试状态。至少保存不使用模式的基线、最小模式实现和一个反例；如果新需求仍需跨越边界修改多处，说明模式没有切中变化轴。

本节点的验收同时观察正确性、变化传播、帧时序、分配、缓存、队列和调试体验。模式若只减少代码行却隐藏依赖，或只提升微基准却增加关卡制作成本，都必须在决策记录中列为代价，而不是包装成“最佳实践”。

### 18. Dirty Flag

**目录节点 19/21。** “18. Dirty Flag”位于“重放需求变化 → 检查帧级时序 → 注入行为扩展 → 审计隐藏依赖 → 签收性能与移除条件”中；实现前要写出问题证据、参与者、时序、所有权、成本与移除条件。

落实“18. Dirty Flag”时，沿“重放需求变化 → 检查帧级时序 → 注入行为扩展 → 审计隐藏依赖 → 签收性能与移除条件”标出谁创建、谁调用、谁拥有、何时可见和如何失败，并把隐藏依赖转成可测试状态。至少保存不使用模式的基线、最小模式实现和一个反例；如果新需求仍需跨越边界修改多处，说明模式没有切中变化轴。

本节点的验收同时观察正确性、变化传播、帧时序、分配、缓存、队列和调试体验。模式若只减少代码行却隐藏依赖，或只提升微基准却增加关卡制作成本，都必须在决策记录中列为代价，而不是包装成“最佳实践”。

### 19. Object Pool

**目录节点 20/21。** “19. Object Pool”位于“重放需求变化 → 检查帧级时序 → 注入行为扩展 → 审计隐藏依赖 → 签收性能与移除条件”中；实现前要写出问题证据、参与者、时序、所有权、成本与移除条件。

落实“19. Object Pool”时，沿“重放需求变化 → 检查帧级时序 → 注入行为扩展 → 审计隐藏依赖 → 签收性能与移除条件”标出谁创建、谁调用、谁拥有、何时可见和如何失败，并把移除条件转成可测试状态。至少保存不使用模式的基线、最小模式实现和一个反例；如果新需求仍需跨越边界修改多处，说明模式没有切中变化轴。

本节点的验收同时观察正确性、变化传播、帧时序、分配、缓存、队列和调试体验。模式若只减少代码行却隐藏依赖，或只提升微基准却增加关卡制作成本，都必须在决策记录中列为代价，而不是包装成“最佳实践”。

### 20. Spatial Partition

**目录节点 21/21。** “20. Spatial Partition”位于“重放需求变化 → 检查帧级时序 → 注入行为扩展 → 审计隐藏依赖 → 签收性能与移除条件”中；实现前要写出问题证据、参与者、时序、所有权、成本与移除条件。

落实“20. Spatial Partition”时，沿“重放需求变化 → 检查帧级时序 → 注入行为扩展 → 审计隐藏依赖 → 签收性能与移除条件”标出谁创建、谁调用、谁拥有、何时可见和如何失败，并把变化传播转成可测试状态。至少保存不使用模式的基线、最小模式实现和一个反例；如果新需求仍需跨越边界修改多处，说明模式没有切中变化轴。

本节点的验收同时观察正确性、变化传播、帧时序、分配、缓存、队列和调试体验。模式若只减少代码行却隐藏依赖，或只提升微基准却增加关卡制作成本，都必须在决策记录中列为代价，而不是包装成“最佳实践”。

## 可迁移实现骨架

模式实现先声明决策，而不是从基类开始。下面的接口让基线、模式和替代方案使用同一场景证据。

```cpp
struct PatternDecision {
  const char* problem;
  const char* change_axis;
  const char* owner;
  const char* timing_contract;
  double frame_budget_ms;
  const char* removal_trigger;
};
```

实现流程把问题与方案分开，确保可以证明模式是否减少了真实成本。

```text
capture baseline change and runtime trace
  -> implement the smallest explicit contract
  -> inject a second requirement and a lifecycle fault
  -> compare propagation, latency, allocation and debugging
  -> keep, simplify, replace or remove the pattern
```

决策记录采用机器可读字段，便于版本间复查。

```json
{
  "pattern": "candidate",
  "baselineModulesTouched": 7,
  "patternModulesTouched": 2,
  "p95FrameMs": 15.8,
  "hiddenDependencies": 0,
  "decision": "keep with review trigger"
}
```

## 本单元的验收工件

第一件工件是变化图：把“重放需求变化 → 检查帧级时序 → 注入行为扩展 → 审计隐藏依赖 → 签收性能与移除条件”逐边标出调用、数据、所有者、帧阶段和失败返回。第二件是差分改动：对同一新增需求分别修改基线与模式版本，统计理解范围、改动模块、合同变化和测试数量。第三件是目标机轨迹：固定场景与输入，测量帧分位数、分配、队列延迟和缓存事件。第四件是反例：故意选择一个不适用场景，证明模式何时增加复杂度，并写下移除触发器。

<Callout type="trap" title="先选模式再寻找问题">
  模式名称不能成为需求。先保存没有模式时的具体失败、变化压力或性能热点，再比较最小解决方案。
</Callout>

<Callout type="trap" title="只看类图不看帧时序">
  游戏对象在一帧内的读写顺序会决定正确性。同步观察、异步队列、双缓冲和更新方法不能互换。
</Callout>

<Callout type="trap" title="减少耦合却增加隐藏依赖">
  全局单例、总线和定位器可能让调用更短，却让依赖、生命周期和测试替换更难。必须用依赖图复核。
</Callout>

## 先预测再运行

运行交互前先预测：需求增加时哪些模块会被修改；对象和事件翻倍时帧尾还是队列先超限；观察者提前销毁、对象池未重置或脏标记漏设时哪个不变量先失效。若结果与预测不同，先检查隐藏依赖、帧阶段、所有权和统计窗口。

## 本章回顾

《游戏编程模式》全书总复习是“重放需求变化 → 检查帧级时序 → 注入行为扩展 → 审计隐藏依赖 → 签收性能与移除条件”中的模式选择合同。掌握本页意味着能逐项定位21个权威节点，解释变化传播、帧级时序、行为扩展、隐藏依赖、移除条件的适用边界，并用变化、时序、性能和反例证据决定保留、替换或移除。

<Exercises>

**问题 1：问题与边界。** 怎样证明《游戏编程模式》全书总复习要解决的问题真实存在？

<Answer>
  保存一次代表性需求改动或运行热点，沿“重放需求变化 → 检查帧级时序 →
  注入行为扩展 → 审计隐藏依赖 →
  签收性能与移除条件”记录理解范围、修改模块、依赖、帧时序和失败结果；没有基线就不引入模式。
</Answer>

**问题 2：代价实验。** 怎样验证变化传播没有把成本转移到性能或调试？

<Answer>
  固定平台、构建、场景和输入，比较模式前后的变化传播、P50/P95/P99、分配、队列、缓存和故障定位时间，同时保存内容生产者的工作流影响。
</Answer>

**问题 3：迁移与删除。** 怎样证明《游戏编程模式》全书总复习不是只会复述原书C++？

<Answer>
  更换语言或引擎但保持问题、参与者、时序、所有权、指标和反例；实现最小等价合同，并在移除触发器出现时能安全简化。
</Answer>

</Exercises>

## 术语复核

<Glossary>
  <GlossaryItem term="变化轴">
    促使代码结构发生变化的独立需求维度，例如输入设备、行为种类或存储布局。
  </GlossaryItem>
  <GlossaryItem term="变化传播">
    一个改动需要理解或修改的模块、接口和测试数量。
  </GlossaryItem>
  <GlossaryItem term="帧时序合同">
    对象、消息或缓冲区在一帧内被读取、写入和发布的明确顺序。
  </GlossaryItem>
  <GlossaryItem term="模式成本">
    模式新增的类型、间接调用、内存、同步、工具和认知负担。
  </GlossaryItem>
  <GlossaryItem term="模式决策记录">
    记录问题基线、候选方案、取舍、指标、反例和重新评估条件的工件。
  </GlossaryItem>
</Glossary>

<Attribution
  adaptedFrom="Game Programming Patterns / 游戏编程模式"
  adaptedUrl="https://gameprogrammingpatterns.com/contents.html"
/>

## 阅读导航

- [上一页：20. Spatial Partition](/learn/game-programming-patterns/27-chapter-20-spatial-partition/gpp-chapter-20-spatial-partition)
