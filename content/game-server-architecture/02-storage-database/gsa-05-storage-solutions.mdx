---
title: "第5章 存储方案"
type: C
section: "游戏服务器架构与优化 · 存储与数据库"
order: 5
description: "比较高并发锁策略、Memcached与大文件缓存、二进制对象、大规模计算和区块链，按访问模式建立存储组合。"
demo: true
math: false
sourceUrl: "https://ebooks.cmpbook.com/detail?id=21197"
draft: false
---


## 直觉：先找唯一事实

比较高并发锁策略、Memcached与大文件缓存、二进制对象、大规模计算和区块链，按访问模式建立存储组合。 高并发存储的优化顺序是缩短临界区、减少往返和复制、限定缓存一致性，再决定是否需要分布式或不可变账本。 先预测：如果只保留最终成功截图，本页最可能隐藏哪一种重复执行、状态分叉或失主资源？

## 权威目录定位

本页依据机械工业出版社工程科技数字图书馆和得到电子书公开目录锁定范围。编号和名称用于一对一追踪，正文是独立教学重构，不复制原书段落；现代安全、可观察性与故障演练明确属于教学补充：

- 5.1 高并发服务器的存储方案
- 5.2 高速缓存
- 5.3 二进制存储方案
- 5.4 大规模计算
- 5.5 区块链技术

本页共追踪 **5** 个公开目录条目。全书正式单元为：第1章 Python网络编程模块、第2章 通信加密、第3章 服务器实作、第4章 基础内容存储、第5章 存储方案、第6章 游戏服务器初探、第7章 游戏服务器的交互、第8章 游戏大厅、第9章 实时交互服务器、第10章 天梯和经济系统、第11章 服务器承载量和客户端优化方案、第12章 分布式服务器、附录A 不同语言之间的区别。出版社简介称全书13章，但公开目录把语言选型单元标作“附录A”；学习页面保留目录原标号，并在来源边界说明差异。

## 核心概念与所有权

、、、、。
这些概念必须同时回答四个问题：谁创建输入，谁拥有可变状态，哪个代际拒绝旧结果，失败后谁回滚或补偿。

|   # | 概念       | 工程含义                                           |
| --: | ---------- | -------------------------------------------------- |
|   1 | 缓存命中   | 请求直接从更快副本获得有效结果而无需访问后端。     |
|   2 | 缓存穿透   | 大量不存在或不可缓存请求持续击穿到事实源。         |
|   3 | 写放大     | 一次逻辑写入在索引、复制或编码层产生多次物理写。   |
|   4 | 分片       | 按稳定键把数据与负载划分到多个独立节点。           |
|   5 | 不可变账本 | 通过追加记录和校验链限制历史被静默修改的数据结构。 |

## 目录主线与因果关系

### 1. 先测热点再加缓存

命中率、对象大小、重建成本和失效语义共同决定收益。Memcached适合可丢的派生数据，经济余额不能只存在缓存。

先预测：如果省略这一层，正常样本也许仍能成功，但边界或故障样本会在哪个状态首先偏离？

### 2. 二进制与大文件分离

图片、影音和大型对象应采用对象存储或内容寻址，元数据留在可查询数据库，避免大字段放大锁和备份。

先预测：如果省略这一层，正常样本也许仍能成功，但边界或故障样本会在哪个状态首先偏离？

### 3. 区块链不是默认数据库

只有多方互不信任且需要可验证历史时才评估不可变账本；单一运营方的高频战斗状态通常更需要简单事务和审计日志。

先预测：如果省略这一层，正常样本也许仍能成功，但边界或故障样本会在哪个状态首先偏离？

## 六阶段证据链

本页可操作链条是：测量访问 → 缩短锁域 → 部署缓存 → 分片二进制 → 批量计算 → 一致性签发。正常样本证明能力，边界样本证明预算，失败样本证明拒绝，恢复样本证明没有留下第二份事实。


## 最小实现骨架

下面是用于复现实验的独立骨架，不是原书代码复制；生产实现还需补齐平台差异、权限、安全和资源清理。

```python
def load_profile(player_id):
    key = f'profile:{player_id}:v2'
    if value := cache.get(key):
        return decode(value)
    value = database.read(player_id)
    cache.set(key, encode(value), ttl=jittered_ttl())
    return value
```

证据记录必须把决策与原始指标放在一起：

```yaml
book: 游戏服务器架构与优化
page: gsa-05-storage-solutions
catalog_entries: 5
sample: normal | boundary | failure | recovery
state_owner: explicit
first_divergence: null
recovery_action: replay_same_input
```

故障脚本只改变一个变量，并保留恢复阶段：

```text
baseline -> inject(把缓存当成唯一数据源，淘汰或重启后玩家资产永久消失。)
observe -> stop_at_first_divergence
recover -> replay_same_version_and_input
assert -> no_duplicate_state && no_orphan_resource
```

## 常见误区与故障注入


## 验收矩阵

| 样本 | 输入变化                     | 预期状态         | 通过条件               |
| ---- | ---------------------------- | ---------------- | ---------------------- |
| 正常 | 合法版本、身份、顺序和容量   | 六阶段顺序完成   | 状态摘要与所有权一致   |
| 边界 | 临界时序、容量、重复或切换   | 达到预算但不越界 | 无无限队列和第二份事实 |
| 失败 | 畸形、旧版本、断线或依赖失效 | 在首偏离点停止   | 明确拒绝、补偿或回滚   |
| 恢复 | 修复后重放同一输入           | 收敛到正常轨迹   | 无幽灵状态和资源泄漏   |

本页签发不变量是：**缓存可丢可重建；锁域和租约有界；分片键稳定；经济事实始终可审计并从持久源恢复。** 任何单次演示、截图或平均指标都不能替代版本、状态摘要、首偏离点和恢复证据。

## 练习


## 小结

- **缓存命中**：请求直接从更快副本获得有效结果而无需访问后端。
- **缓存穿透**：大量不存在或不可缓存请求持续击穿到事实源。
- **写放大**：一次逻辑写入在索引、复制或编码层产生多次物理写。
- **分片**：按稳定键把数据与负载划分到多个独立节点。
- **不可变账本**：通过追加记录和校验链限制历史被静默修改的数据结构。
- 六阶段机制链已经映射到正常、边界、失败和恢复证据。
- 公开目录已逐项追踪，现代补充不会冒充原书分节。

## 术语表


## 来源与改编边界

