---
title: 第2章 多线程
type: C
section: 网络游戏服务器端编程 · 网络与并发
order: 2
description: 覆盖进程线程、Windows多线程、事件、临界区、互斥、信号量、互锁、定时器与死锁；用请求路径、单故障轨迹和运行发布门完成独立复核。
demo: true
math: false
sourceUrl: "https://book.douban.com/subject/2240257/"
draft: false
qualityVersion: 2
practiceMode: simulation
sourceMode: independent-rewrite
officialUnitId: gsp-unit-02
---

import {
  Objectives,
  Term,
  Callout,
  Stepper,
  Step,
  Exercises,
  Answer,
  Glossary,
  GlossaryItem,
  Attribution,
} from "@/components/mdx/mdx-components";
import {
  Gsp02MultithreadingRequestPath,
  Gsp02MultithreadingFailureTraceLab,
  Gsp02MultithreadingOperationalGateLab,
} from "@/components/mdx/game-server-programming/v2/gsp-02-multithreading";

<Objectives>

- 能说明“第2章 多线程”如何覆盖进程线程、Windows多线程、事件、临界区、互斥、信号量、互锁、定时器与死锁，并明确2007原书、公开目录披露级别与现行技术资料的时间边界
- 能先预测“怎样给共享状态指定唯一所有者和同步原语，并用等待图证明没有数据竞争、丢唤醒或死锁？”的连接或状态轨迹，再沿接入、队列、所有者、事务、输出与回收逐阶段核对
- 能注入“网络线程持有会话锁等待数据库事件，而数据库线程持有队列锁回调会话，形成锁顺序环”，用“共享对象、读写者、锁顺序、等待条件、唤醒语义和停止协议显式，线程退出后资源可回收”决定接受、降级或拒绝服务器发布

</Objectives>

## 为什么从这个服务器任务开始

多线程页不以线程数量衡量性能；先明确状态所有权，再选择能表达等待和容量的同步原语。 “第2章 多线程”使用的贯穿任务是：为会话表、发送队列和定时任务分配线程职责，分别用事件、临界区、信号量与互锁操作验证。 操作前先预测哪个连接、队列、状态或信任节点会变化，运行后再补理由不算预测。

本页围绕“怎样给共享状态指定唯一所有者和同步原语，并用等待图证明没有数据竞争、丢唤醒或死锁？”建立正常、故障与恢复路径。只有“第2章 多线程”保持“共享对象、读写者、锁顺序、等待条件、唤醒语义和停止协议显式，线程退出后资源可回收”并交付线程清单、共享对象、临界区、事件、互斥、信号量、互锁变量、定时器、等待图、停止信号和回收日志。，功能成功才构成服务器证据。

## 书目、57个公开坐标与披露边界

“第2章 多线程”以[书目信息](https://book.douban.com/subject/2240257/)核对编著单位、电子工业出版社、2007年8月、ISBN 9787121043185和299页；[公开详细目录](https://www.jb51.net/books/334673.html)核对第1至第3章、第4章至4.1.6以及第5至第8章章名，[Google Books](https://books.google.com/books?vid=ISBN9787121043185)交叉核对ISBN与约300页记录。完整公开分母为57个目录坐标。

“第2章 多线程”只依据公开目录限定范围，不逐段改写原文；解释、状态模型、交互、练习与答案均为独立教学重写。第5至第8章公开资料只披露章名，因此本页的现代工程任务是独立教学展开，不登记成原书权威小节。

“第2章 多线程”另以[技术核对 1](https://learn.microsoft.com/en-us/windows/win32/sync/synchronization-object-security-and-access-rights)、[技术核对 2](https://learn.microsoft.com/en-us/windows/win32/fileio/i-o-completion-ports)、[技术核对 3](https://cheatsheetseries.owasp.org/cheatsheets/Logging_Cheat_Sheet.html)核对现行技术事实。2007年的Windows线程、Winsock和IOCP保留为历史技术轨；现行RFC、Microsoft、PostgreSQL、OWASP、Open Match与TUF资料只验证稳定机制、安全和迁移边界，不能反向证明原书包含现代实现。

## 公开目录坐标与服务器机制

### 线程基础

<Term def="线程基础对应公开目录坐标“线程基础”，在“第2章 多线程”中用于以线程所有权和同步原语保护共享状态，并受原书年份、披露级别、平台、状态、安全和运维边界约束。">
  线程基础
</Term>

**公开坐标 1/16。** 在“第2章 多线程”的坐标1中，线程基础用于以线程所有权和同步原语保护共享状态；先锁定输入和所有者，再用读写者、锁顺序、等待图、容量与停止日志复核，出现锁顺序环或丢唤醒时不得发布。

### 进程

<Term def="进程对应公开目录坐标“进程”，在“第2章 多线程”中用于以线程所有权和同步原语保护共享状态，并受原书年份、披露级别、平台、状态、安全和运维边界约束。">
  进程
</Term>

**公开坐标 2/16。** 进程进入“第2章 多线程”后要回答第2张状态卡：它怎样以线程所有权和同步原语保护共享状态、改变哪个对象、由哪些读写者、锁顺序、等待图、容量与停止日志证明，并如何排除锁顺序环或丢唤醒。

### 线程

<Term def="线程对应公开目录坐标“线程”，在“第2章 多线程”中用于以线程所有权和同步原语保护共享状态，并受原书年份、披露级别、平台、状态、安全和运维边界约束。">
  线程
</Term>

**公开坐标 3/16。** 围绕“怎样给共享状态指定唯一所有者和同步原语，并用等待图证明没有数据竞争、丢唤醒或死锁？”，条目3把线程解释为以线程所有权和同步原语保护共享状态；复核者先读取读写者、锁顺序、等待图、容量与停止日志再判断服务，不能接受锁顺序环或丢唤醒。

### 线程的应用

<Term def="线程的应用对应公开目录坐标“线程的应用”，在“第2章 多线程”中用于以线程所有权和同步原语保护共享状态，并受原书年份、披露级别、平台、状态、安全和运维边界约束。">
  线程的应用
</Term>

**公开坐标 4/16。** 对“第2章 多线程”而言，线程的应用的最小合同是以线程所有权和同步原语保护共享状态，第4次检查保存读写者、锁顺序、等待图、容量与停止日志；若产生锁顺序环或丢唤醒，就回到上游。

### Windows下的多线程处理

<Term def="Windows下的多线程处理对应公开目录坐标“Windows下的多线程处理”，在“第2章 多线程”中用于以线程所有权和同步原语保护共享状态，并受原书年份、披露级别、平台、状态、安全和运维边界约束。">
  Windows下的多线程处理
</Term>

**公开坐标 5/16。** 第5个公开坐标Windows下的多线程处理服务于覆盖进程线程、Windows多线程、事件、临界区、互斥、信号量、互锁、定时器与死锁，需要以读写者、锁顺序、等待图、容量与停止日志呈现以线程所有权和同步原语保护共享状态；锁顺序环或丢唤醒会破坏“共享对象、读写者、锁顺序、等待条件、唤醒语义和停止协议显式，线程退出后资源可回收”。

### 随机矩形

<Term def="随机矩形对应公开目录坐标“随机矩形”，在“第2章 多线程”中用于以线程所有权和同步原语保护共享状态，并受原书年份、披露级别、平台、状态、安全和运维边界约束。">
  随机矩形
</Term>

**公开坐标 6/16。** 学习者在“第2章 多线程”中讨论随机矩形前预测以线程所有权和同步原语保护共享状态会改变哪项连接或状态，再读取读写者、锁顺序、等待图、容量与停止日志；观察到锁顺序环或丢唤醒时保留失败轨迹。

### 多工模拟程序

<Term def="多工模拟程序对应公开目录坐标“多工模拟程序”，在“第2章 多线程”中用于以线程所有权和同步原语保护共享状态，并受原书年份、披露级别、平台、状态、安全和运维边界约束。">
  多工模拟程序
</Term>

**公开坐标 7/16。** 在“第2章 多线程”的坐标7中，多工模拟程序用于以线程所有权和同步原语保护共享状态；先锁定输入和所有者，再用读写者、锁顺序、等待图、容量与停止日志复核，出现锁顺序环或丢唤醒时不得发布。

### 线程间的通信与同步

<Term def="线程间的通信与同步对应公开目录坐标“线程间的通信与同步”，在“第2章 多线程”中用于以线程所有权和同步原语保护共享状态，并受原书年份、披露级别、平台、状态、安全和运维边界约束。">
  线程间的通信与同步
</Term>

**公开坐标 8/16。** 线程间的通信与同步进入“第2章 多线程”后要回答第8张状态卡：它怎样以线程所有权和同步原语保护共享状态、改变哪个对象、由哪些读写者、锁顺序、等待图、容量与停止日志证明，并如何排除锁顺序环或丢唤醒。

### 全局变量

<Term def="全局变量对应公开目录坐标“全局变量”，在“第2章 多线程”中用于以线程所有权和同步原语保护共享状态，并受原书年份、披露级别、平台、状态、安全和运维边界约束。">
  全局变量
</Term>

**公开坐标 9/16。** 围绕“怎样给共享状态指定唯一所有者和同步原语，并用等待图证明没有数据竞争、丢唤醒或死锁？”，条目9把全局变量解释为以线程所有权和同步原语保护共享状态；复核者先读取读写者、锁顺序、等待图、容量与停止日志再判断服务，不能接受锁顺序环或丢唤醒。

### 事件

<Term def="事件对应公开目录坐标“事件”，在“第2章 多线程”中用于以线程所有权和同步原语保护共享状态，并受原书年份、披露级别、平台、状态、安全和运维边界约束。">
  事件
</Term>

**公开坐标 10/16。** 对“第2章 多线程”而言，事件的最小合同是以线程所有权和同步原语保护共享状态，第10次检查保存读写者、锁顺序、等待图、容量与停止日志；若产生锁顺序环或丢唤醒，就回到上游。

### 临界区

<Term def="临界区对应公开目录坐标“临界区”，在“第2章 多线程”中用于以线程所有权和同步原语保护共享状态，并受原书年份、披露级别、平台、状态、安全和运维边界约束。">
  临界区
</Term>

**公开坐标 11/16。** 第11个公开坐标临界区服务于覆盖进程线程、Windows多线程、事件、临界区、互斥、信号量、互锁、定时器与死锁，需要以读写者、锁顺序、等待图、容量与停止日志呈现以线程所有权和同步原语保护共享状态；锁顺序环或丢唤醒会破坏“共享对象、读写者、锁顺序、等待条件、唤醒语义和停止协议显式，线程退出后资源可回收”。

### 互斥

<Term def="互斥对应公开目录坐标“互斥”，在“第2章 多线程”中用于以线程所有权和同步原语保护共享状态，并受原书年份、披露级别、平台、状态、安全和运维边界约束。">
  互斥
</Term>

**公开坐标 12/16。** 学习者在“第2章 多线程”中讨论互斥前预测以线程所有权和同步原语保护共享状态会改变哪项连接或状态，再读取读写者、锁顺序、等待图、容量与停止日志；观察到锁顺序环或丢唤醒时保留失败轨迹。

### 信号量

<Term def="信号量对应公开目录坐标“信号量”，在“第2章 多线程”中用于以线程所有权和同步原语保护共享状态，并受原书年份、披露级别、平台、状态、安全和运维边界约束。">
  信号量
</Term>

**公开坐标 13/16。** 在“第2章 多线程”的坐标13中，信号量用于以线程所有权和同步原语保护共享状态；先锁定输入和所有者，再用读写者、锁顺序、等待图、容量与停止日志复核，出现锁顺序环或丢唤醒时不得发布。

### 互锁访问

<Term def="互锁访问对应公开目录坐标“互锁访问”，在“第2章 多线程”中用于以线程所有权和同步原语保护共享状态，并受原书年份、披露级别、平台、状态、安全和运维边界约束。">
  互锁访问
</Term>

**公开坐标 14/16。** 互锁访问进入“第2章 多线程”后要回答第14张状态卡：它怎样以线程所有权和同步原语保护共享状态、改变哪个对象、由哪些读写者、锁顺序、等待图、容量与停止日志证明，并如何排除锁顺序环或丢唤醒。

### 可等待定时器

<Term def="可等待定时器对应公开目录坐标“可等待定时器”，在“第2章 多线程”中用于以线程所有权和同步原语保护共享状态，并受原书年份、披露级别、平台、状态、安全和运维边界约束。">
  可等待定时器
</Term>

**公开坐标 15/16。** 围绕“怎样给共享状态指定唯一所有者和同步原语，并用等待图证明没有数据竞争、丢唤醒或死锁？”，条目15把可等待定时器解释为以线程所有权和同步原语保护共享状态；复核者先读取读写者、锁顺序、等待图、容量与停止日志再判断服务，不能接受锁顺序环或丢唤醒。

### 线程死锁

<Term def="线程死锁对应公开目录坐标“线程死锁”，在“第2章 多线程”中用于以线程所有权和同步原语保护共享状态，并受原书年份、披露级别、平台、状态、安全和运维边界约束。">
  线程死锁
</Term>

**公开坐标 16/16。** 对“第2章 多线程”而言，线程死锁的最小合同是以线程所有权和同步原语保护共享状态，第16次检查保存读写者、锁顺序、等待图、容量与停止日志；若产生锁顺序环或丢唤醒，就回到上游。

## 先预测，再操作三个服务器实验

<Callout type="info" title="先写出哪个节点会先变化">
  对“第2章
  多线程”先选择版本化输入、关联ID、容量、初始状态和预期输出，再操作请求路径、故障轨迹和运行发布门；结果与预测不一致时应修改系统假设，不删除失败运行。
</Callout>

<Stepper>
  <Step title="1. 请求与状态路径">
    沿“网络线程、会话表、任务队列、工作线程、停止与回收”逐节点查看输入、动作、输出与所有者，证明“第2章 多线程”没有跨层偷写状态。

    <Gsp02MultithreadingRequestPath />

  </Step>
  <Step title="2. 正常与单故障轨迹">
    保持“为会话表、发送队列和定时任务分配线程职责，分别用事件、临界区、信号量与互锁操作验证。”不变，切换正常和故障模式，定位“网络线程持有会话锁等待数据库事件，而数据库线程持有队列锁回调会话，形成锁顺序环”最先破坏边界、容量、状态或信任的位置。

    <Gsp02MultithreadingFailureTraceLab />

  </Step>
  <Step title="3. 运行与安全发布门">
    分别切换边界所有权、容量背压、安全权限、恢复观测，展开线程清单、共享对象、临界区、事件、互斥、信号量、互锁变量、定时器、等待图、停止信号和回收日志。后决定是否发布。

    <Gsp02MultithreadingOperationalGateLab />

  </Step>
</Stepper>

<Callout
  type="trap"
  title="本页单一故障：网络线程持有会话锁等待数据库事件，而数据库线程持有队列锁回调会话，形成锁顺序环"
>
  “第2章
  多线程”遇到该故障时应保持版本、输入、关联ID、容量和初始状态不变，沿接入到运维方向寻找最早偏离；最终响应偶尔成功不能证明“共享对象、读写者、锁顺序、等待条件、唤醒语义和停止协议显式，线程退出后资源可回收”。
</Callout>

<Callout type="trap" title="功能演示不等于容量与恢复">
  为会话表、发送队列和定时任务分配线程职责，分别用事件、临界区、信号量与互锁操作验证。
  在单客户端和空队列下成功，只证明理想路径；“第2章
  多线程”仍需边界输入、队列饱和、超时取消、重复请求、冷启动与恢复证据。
</Callout>

<Callout type="trap" title="现代架构不能倒填2007原书">
  “第2章
  多线程”引用现行资料是为了核对稳定机制与迁移；后四章只公开章名，现代事务、匹配、权限和TUF任务不能宣称为原书小节。
</Callout>

## 第2章 多线程的可重放服务协议

| 阶段                     | 服务动作                                                               | 必留证据                           | 拒绝条件                                                                       |
| ------------------------ | ---------------------------------------------------------------------- | ---------------------------------- | ------------------------------------------------------------------------------ |
| 声明线程和共享对象所有权 | 在“第2章 多线程”执行声明线程和共享对象所有权，只允许声明所有者改变状态 | 版本、输入、关联ID、容量和初始状态 | 身份或边界不可追溯                                                             |
| 选择同步原语与锁顺序     | 在“第2章 多线程”执行选择同步原语与锁顺序，只允许声明所有者改变状态     | 连接、队列、线程、状态与提交轨迹   | 网络线程持有会话锁等待数据库事件，而数据库线程持有队列锁回调会话，形成锁顺序环 |
| 注入竞争死锁和停止       | 在“第2章 多线程”执行注入竞争死锁和停止，只允许声明所有者改变状态       | 权限、审计、恢复、迁移与回退记录   | 无法重放或恢复基线                                                             |

```yaml
unit: "gsp-unit-02"
question: "怎样给共享状态指定唯一所有者和同步原语，并用等待图证明没有数据竞争、丢唤醒或死锁？"
scenario: "为会话表、发送队列和定时任务分配线程职责，分别用事件、临界区、信号量与互锁操作验证。"
nodes: ["网络线程", "会话表", "任务队列", "工作线程", "停止与回收"]
stages:
  ["声明线程和共享对象所有权", "选择同步原语与锁顺序", "注入竞争死锁和停止"]
invariant: "共享对象、读写者、锁顺序、等待条件、唤醒语义和停止协议显式，线程退出后资源可回收"
fault: "网络线程持有会话锁等待数据库事件，而数据库线程持有队列锁回调会话，形成锁顺序环"
evidence: "线程清单、共享对象、临界区、事件、互斥、信号量、互锁变量、定时器、等待图、停止信号和回收日志。"
reset: restore_node_trace_mode_step_gates_and_artifact
```

该协议要求“第2章 多线程”在相同版本、输入、关联ID、容量和初始状态下重放。重置后若节点、轨迹模式、步骤、发布门或证据显示没有回到基线，交互状态已经污染比较，不能作为服务器证据。

## 本页回顾

掌握“第2章 多线程”不是记住API调用顺序，而是能围绕“怎样给共享状态指定唯一所有者和同步原语，并用等待图证明没有数据竞争、丢唤醒或死锁？”重建服务器状态，并用“共享对象、读写者、锁顺序、等待条件、唤醒语义和停止协议显式，线程退出后资源可回收”拒绝“网络线程持有会话锁等待数据库事件，而数据库线程持有队列锁回调会话，形成锁顺序环”。最终交付为线程清单、共享对象、临界区、事件、互斥、信号量、互锁变量、定时器、等待图、停止信号和回收日志。

## 练习与答案

<Exercises>

1. **问题 1：服务合同。** “第2章 多线程”为什么必须先声明版本、输入、关联ID、容量、初始状态和所有者？

<Answer>
  为会话表、发送队列和定时任务分配线程职责，分别用事件、临界区、信号量与互锁操作验证。
  若这些条件不固定，相同请求可能进入不同协议、队列、事务、权限或更新版本；“第2章
  多线程”先声明合同，才能把结果连接到可验证状态。
</Answer>

2. **问题 2：目录逐项覆盖。** 怎样证明公开目录坐标已经进入机制、交互和练习？

<Answer>
  1. “第2章
  多线程”的线程基础：以“以线程所有权和同步原语保护共享状态”解释系统作用，用“读写者、锁顺序、等待图、容量与停止日志”提供复核。
  2. “第2章
  多线程”的进程：以“以线程所有权和同步原语保护共享状态”解释系统作用，用“读写者、锁顺序、等待图、容量与停止日志”提供复核。
  3. “第2章
  多线程”的线程：以“以线程所有权和同步原语保护共享状态”解释系统作用，用“读写者、锁顺序、等待图、容量与停止日志”提供复核。
  4. “第2章
  多线程”的线程的应用：以“以线程所有权和同步原语保护共享状态”解释系统作用，用“读写者、锁顺序、等待图、容量与停止日志”提供复核。
  5. “第2章
  多线程”的Windows下的多线程处理：以“以线程所有权和同步原语保护共享状态”解释系统作用，用“读写者、锁顺序、等待图、容量与停止日志”提供复核。
  6. “第2章
  多线程”的随机矩形：以“以线程所有权和同步原语保护共享状态”解释系统作用，用“读写者、锁顺序、等待图、容量与停止日志”提供复核。
  7. “第2章
  多线程”的多工模拟程序：以“以线程所有权和同步原语保护共享状态”解释系统作用，用“读写者、锁顺序、等待图、容量与停止日志”提供复核。
  8. “第2章
  多线程”的线程间的通信与同步：以“以线程所有权和同步原语保护共享状态”解释系统作用，用“读写者、锁顺序、等待图、容量与停止日志”提供复核。
  9. “第2章
  多线程”的全局变量：以“以线程所有权和同步原语保护共享状态”解释系统作用，用“读写者、锁顺序、等待图、容量与停止日志”提供复核。
  10. “第2章
  多线程”的事件：以“以线程所有权和同步原语保护共享状态”解释系统作用，用“读写者、锁顺序、等待图、容量与停止日志”提供复核。
  11. “第2章
  多线程”的临界区：以“以线程所有权和同步原语保护共享状态”解释系统作用，用“读写者、锁顺序、等待图、容量与停止日志”提供复核。
  12. “第2章
  多线程”的互斥：以“以线程所有权和同步原语保护共享状态”解释系统作用，用“读写者、锁顺序、等待图、容量与停止日志”提供复核。
  13. “第2章
  多线程”的信号量：以“以线程所有权和同步原语保护共享状态”解释系统作用，用“读写者、锁顺序、等待图、容量与停止日志”提供复核。
  14. “第2章
  多线程”的互锁访问：以“以线程所有权和同步原语保护共享状态”解释系统作用，用“读写者、锁顺序、等待图、容量与停止日志”提供复核。
  15. “第2章
  多线程”的可等待定时器：以“以线程所有权和同步原语保护共享状态”解释系统作用，用“读写者、锁顺序、等待图、容量与停止日志”提供复核。
  16. “第2章
  多线程”的线程死锁：以“以线程所有权和同步原语保护共享状态”解释系统作用，用“读写者、锁顺序、等待图、容量与停止日志”提供复核。
</Answer>

3. **问题 3：故障恢复。** 怎样证明“网络线程持有会话锁等待数据库事件，而数据库线程持有队列锁回调会话，形成锁顺序环”已经被修正？

<Answer>
  为“第2章
  多线程”复用同一版本、输入、关联ID、容量和初始状态，重放正常路径后只注入“网络线程持有会话锁等待数据库事件，而数据库线程持有队列锁回调会话，形成锁顺序环”；记录最早偏离点，撤销故障并再次运行。只有请求路径、故障轨迹、发布门和线程清单、共享对象、临界区、事件、互斥、信号量、互锁变量、定时器、等待图、停止信号和回收日志。重新满足“共享对象、读写者、锁顺序、等待条件、唤醒语义和停止协议显式，线程退出后资源可回收”，修正才可提交。
</Answer>

</Exercises>

## 名词解释

<Glossary>
  <GlossaryItem term="线程基础">
    对应“线程基础”；在“第2章
    多线程”中用于以线程所有权和同步原语保护共享状态，需要连接原书年份、披露边界、输入、所有者、状态与恢复。
  </GlossaryItem>
  <GlossaryItem term="进程">
    对应“进程”；在“第2章
    多线程”中用于以线程所有权和同步原语保护共享状态，需要连接原书年份、披露边界、输入、所有者、状态与恢复。
  </GlossaryItem>
  <GlossaryItem term="线程">
    对应“线程”；在“第2章
    多线程”中用于以线程所有权和同步原语保护共享状态，需要连接原书年份、披露边界、输入、所有者、状态与恢复。
  </GlossaryItem>
  <GlossaryItem term="线程的应用">
    对应“线程的应用”；在“第2章
    多线程”中用于以线程所有权和同步原语保护共享状态，需要连接原书年份、披露边界、输入、所有者、状态与恢复。
  </GlossaryItem>
  <GlossaryItem term="Windows下的多线程处理">
    对应“Windows下的多线程处理”；在“第2章
    多线程”中用于以线程所有权和同步原语保护共享状态，需要连接原书年份、披露边界、输入、所有者、状态与恢复。
  </GlossaryItem>
  <GlossaryItem term="随机矩形">
    对应“随机矩形”；在“第2章
    多线程”中用于以线程所有权和同步原语保护共享状态，需要连接原书年份、披露边界、输入、所有者、状态与恢复。
  </GlossaryItem>
  <GlossaryItem term="多工模拟程序">
    对应“多工模拟程序”；在“第2章
    多线程”中用于以线程所有权和同步原语保护共享状态，需要连接原书年份、披露边界、输入、所有者、状态与恢复。
  </GlossaryItem>
  <GlossaryItem term="线程间的通信与同步">
    对应“线程间的通信与同步”；在“第2章
    多线程”中用于以线程所有权和同步原语保护共享状态，需要连接原书年份、披露边界、输入、所有者、状态与恢复。
  </GlossaryItem>
  <GlossaryItem term="全局变量">
    对应“全局变量”；在“第2章
    多线程”中用于以线程所有权和同步原语保护共享状态，需要连接原书年份、披露边界、输入、所有者、状态与恢复。
  </GlossaryItem>
  <GlossaryItem term="事件">
    对应“事件”；在“第2章
    多线程”中用于以线程所有权和同步原语保护共享状态，需要连接原书年份、披露边界、输入、所有者、状态与恢复。
  </GlossaryItem>
  <GlossaryItem term="临界区">
    对应“临界区”；在“第2章
    多线程”中用于以线程所有权和同步原语保护共享状态，需要连接原书年份、披露边界、输入、所有者、状态与恢复。
  </GlossaryItem>
  <GlossaryItem term="互斥">
    对应“互斥”；在“第2章
    多线程”中用于以线程所有权和同步原语保护共享状态，需要连接原书年份、披露边界、输入、所有者、状态与恢复。
  </GlossaryItem>
  <GlossaryItem term="信号量">
    对应“信号量”；在“第2章
    多线程”中用于以线程所有权和同步原语保护共享状态，需要连接原书年份、披露边界、输入、所有者、状态与恢复。
  </GlossaryItem>
  <GlossaryItem term="互锁访问">
    对应“互锁访问”；在“第2章
    多线程”中用于以线程所有权和同步原语保护共享状态，需要连接原书年份、披露边界、输入、所有者、状态与恢复。
  </GlossaryItem>
  <GlossaryItem term="可等待定时器">
    对应“可等待定时器”；在“第2章
    多线程”中用于以线程所有权和同步原语保护共享状态，需要连接原书年份、披露边界、输入、所有者、状态与恢复。
  </GlossaryItem>
  <GlossaryItem term="线程死锁">
    对应“线程死锁”；在“第2章
    多线程”中用于以线程所有权和同步原语保护共享状态，需要连接原书年份、披露边界、输入、所有者、状态与恢复。
  </GlossaryItem>
</Glossary>

<Attribution
  mode="independent-rewrite"
  sourceBasis="outline-only"
  workTitle="信息产业部软件与集成电路促进中心、北京汇众益智科技有限公司《网络游戏服务器端编程》"
  adaptedUrl="https://book.douban.com/subject/2240257/"
/>
