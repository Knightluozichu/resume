---
title: "第5章 网络游戏数据库技术"
type: C
section: "网络游戏服务器端编程 · 数据与配套系统"
order: 5
description: "围绕账号与角色状态建立模式、事务、并发控制、幂等写入、缓存边界、备份恢复和可核验的数据持久化链。"
demo: true
math: false
sourceUrl: "https://book.douban.com/subject/2240257/"
draft: false
---


## 从第5章的核心问题开始

数据库的职责不是替内存世界状态完成每一帧计算，而是让关键结果在崩溃、重试和并发修改后仍可恢复。设计要从业务不变量和事务边界开始，再决定表、索引、批量写回和缓存；先选数据库产品再拼业务通常会留下重复发奖和部分提交。


## 官方目录命中与来源边界

本页明确覆盖：网络游戏数据库技术。这些名称来自 2007 年版公开书目。公开目录没有披露的细分内容会标为教学展开；现代 API、安全要求和部署方法用于帮助今天的读者验证原机制，不代表原书在 2007 年已经使用这些实现。

| # | 机制或证据边界 | 可观察解释 |
| ---: | --- | --- |
| 1 | 目录披露边界 | 公开书目只披露本章章名，没有可核验分节。下面的模式、事务、幂等、恢复和缓存是围绕章名的教学展开，不登记为原书小节。 |
| 2 | 业务不变量进入模式 | 账号唯一、角色归属、货币非负、物品唯一和版本递增要由约束、事务或原子条件表达，不能只靠调用者记住。 |
| 3 | 事务按业务原子性切分 | 一次交易的扣款、发货和审计应同成同败；跨系统操作通过幂等键和可恢复事件协调，不能把网络调用包进长事务。 |
| 4 | 并发更新要检测冲突 | 乐观版本适合低冲突角色保存，悲观锁适合短小高冲突段。覆盖式保存必须带版本条件，否则后写会吞掉先写。 |
| 5 | 恢复必须演练 | 备份只有在恢复演练通过后才是证据。要记录恢复点目标、恢复时间目标、校验和以及重放到一致状态的步骤。 |

## 核心概念与因果关系

、、、、。这些概念不能只停留在定义上：每个概念都要落到输入、内部状态、输出和失败证据。读者应能指出哪个模块拥有状态、哪个边界拒绝非法输入、哪个指标证明系统没有把成本推给下游。

### 目录披露边界

公开书目只披露本章章名，没有可核验分节。下面的模式、事务、幂等、恢复和缓存是围绕章名的教学展开，不登记为原书小节。

验证时固定软件版本、配置和输入规模，先保存正常轨迹，再只改变一个边界条件。最终结果相同并不代表过程正确；必须比较阶段状态、队列水位、错误分类和恢复动作。

### 业务不变量进入模式

账号唯一、角色归属、货币非负、物品唯一和版本递增要由约束、事务或原子条件表达，不能只靠调用者记住。

验证时固定软件版本、配置和输入规模，先保存正常轨迹，再只改变一个边界条件。最终结果相同并不代表过程正确；必须比较阶段状态、队列水位、错误分类和恢复动作。

### 事务按业务原子性切分

一次交易的扣款、发货和审计应同成同败；跨系统操作通过幂等键和可恢复事件协调，不能把网络调用包进长事务。

验证时固定软件版本、配置和输入规模，先保存正常轨迹，再只改变一个边界条件。最终结果相同并不代表过程正确；必须比较阶段状态、队列水位、错误分类和恢复动作。

### 并发更新要检测冲突

乐观版本适合低冲突角色保存，悲观锁适合短小高冲突段。覆盖式保存必须带版本条件，否则后写会吞掉先写。

验证时固定软件版本、配置和输入规模，先保存正常轨迹，再只改变一个边界条件。最终结果相同并不代表过程正确；必须比较阶段状态、队列水位、错误分类和恢复动作。

### 恢复必须演练

备份只有在恢复演练通过后才是证据。要记录恢复点目标、恢复时间目标、校验和以及重放到一致状态的步骤。

验证时固定软件版本、配置和输入规模，先保存正常轨迹，再只改变一个边界条件。最终结果相同并不代表过程正确；必须比较阶段状态、队列水位、错误分类和恢复动作。

## 六阶段证据链


## 可复现实验

并发提交两次相同购买请求和两次不同角色保存。先预测数据库最终行数与余额，再在提交前、扣款后和事件发布前注入崩溃；重启后检查幂等键、版本号、审计记录和恢复点是否共同保持不变量。

### 最小实现

```sql
CREATE TABLE purchase_request (
  request_id UUID PRIMARY KEY,
  player_id BIGINT NOT NULL,
  item_id BIGINT NOT NULL,
  created_at TIMESTAMP NOT NULL
);
```

### 边界处理

```sql
BEGIN;
INSERT INTO purchase_request(request_id, player_id, item_id, created_at)
VALUES (:request, :player, :item, CURRENT_TIMESTAMP)
ON CONFLICT (request_id) DO NOTHING;

UPDATE wallet SET gold = gold - :price, version = version + 1
WHERE player_id = :player AND gold >= :price AND version = :expected;
COMMIT;
```

### 证据契约

```sql
type SaveEvidence = {
  requestId: string;
  expectedVersion: number;
  rowsChanged: 0 | 1;
  ledgerBalanced: boolean;
  recoveryCheckpoint: string;
};
```

实现代码只是实验的一部分。每次运行还要记录构建版本、配置摘要、样本类型、输入规模、首偏离节点、恢复动作和最终决策。若无法用相同输入重放，结论不能进入交付清单。

## 两个必须主动制造的失败


## 验收矩阵

| 维度 | 正常样本 | 边界样本 | 失败样本 | 通过条件 |
| --- | --- | --- | --- | --- |
| 输入 | 合法且固定 | 容量、顺序或时序临界值 | 畸形、重复或中断 | 分类清晰且可重放 |
| 状态 | 按六阶段推进 | 达到预算但不越界 | 在首偏离节点停止 | 无隐式跨层写入 |
| 输出 | 结果和证据一致 | 退化可解释 | 拒绝、补偿或回滚 | 不变量恢复 |
| 交接 | 他人按记录复验 | 干净环境复验 | 删除隐含依赖 | 结论仍成立 |

本页签发不变量是：**同一业务请求至多产生一次可见结果；事务失败不留下半成品；并发写入不会静默覆盖；从最近有效备份和日志可以恢复到校验一致状态。** 任何演示、截图或单次成功都不能替代它。

## 练习


## 小结

- **目录披露边界**：公开书目只披露本章章名，没有可核验分节。下面的模式、事务、幂等、恢复和缓存是围绕章名的教学展开，不登记为原书小节。
- **业务不变量进入模式**：账号唯一、角色归属、货币非负、物品唯一和版本递增要由约束、事务或原子条件表达，不能只靠调用者记住。
- **事务按业务原子性切分**：一次交易的扣款、发货和审计应同成同败；跨系统操作通过幂等键和可恢复事件协调，不能把网络调用包进长事务。
- **并发更新要检测冲突**：乐观版本适合低冲突角色保存，悲观锁适合短小高冲突段。覆盖式保存必须带版本条件，否则后写会吞掉先写。
- 已用正常、边界和失败样本验证“同一业务请求至多产生一次可见结果；事务失败不留下半成品；并发写入不会静默覆盖；从最近有效备份和日志可以恢复到校验一致状态。”。
- 已把公开目录、历史语境和现代教学展开分开记录。

## 术语表


## 来源与改编边界

