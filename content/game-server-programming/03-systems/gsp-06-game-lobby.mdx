---
title: "第6章 游戏大厅的设计与实现"
type: C
section: "网络游戏服务器端编程 · 数据与配套系统"
order: 6
description: "把认证会话、在线状态、房间列表、匹配、入房令牌与断线重连组织成有代际和幂等边界的大厅状态机。"
demo: true
math: false
sourceUrl: "https://book.douban.com/subject/2240257/"
draft: false
---


## 从第6章的核心问题开始

大厅位于账号服务、游戏房间与客户端之间，它不是一个页面列表，而是玩家从认证到进入对局的状态协调器。可靠实现必须让会话身份、房间版本、席位预留和进入游戏形成可回放状态机，并在超时、重复点击和房间崩溃时收敛。


## 官方目录命中与来源边界

本页明确覆盖：游戏大厅的设计与实现。这些名称来自 2007 年版公开书目。公开目录没有披露的细分内容会标为教学展开；现代 API、安全要求和部署方法用于帮助今天的读者验证原机制，不代表原书在 2007 年已经使用这些实现。

| # | 机制或证据边界 | 可观察解释 |
| ---: | --- | --- |
| 1 | 目录披露边界 | 公开书目只披露本章章名。会话、房间、匹配、令牌和重连是用于掌握大厅核心职责的教学展开，不冒充原书小节。 |
| 2 | 认证与在线状态分离 | 身份令牌证明玩家是谁，在线会话说明当前连接和代际。旧连接断开不能把新连接的在线状态清除。 |
| 3 | 房间列表是版本化投影 | 列表可以缓存和分页，但创建、满员、关闭等变化要带版本；客户端发现版本缺口时重新获取快照，而不是应用不完整增量。 |
| 4 | 入房要预留再确认 | 匹配结果先创建有期限的席位预留，游戏服接受一次性进入令牌后确认。超时释放，重复确认返回同一结果。 |
| 5 | 断线重连按会话恢复 | 重连不是重新匹配。大厅先查找仍有效的对局归属，再签发新连接代际和短期进入凭据。 |

## 核心概念与因果关系

、、、、。这些概念不能只停留在定义上：每个概念都要落到输入、内部状态、输出和失败证据。读者应能指出哪个模块拥有状态、哪个边界拒绝非法输入、哪个指标证明系统没有把成本推给下游。

### 目录披露边界

公开书目只披露本章章名。会话、房间、匹配、令牌和重连是用于掌握大厅核心职责的教学展开，不冒充原书小节。

验证时固定软件版本、配置和输入规模，先保存正常轨迹，再只改变一个边界条件。最终结果相同并不代表过程正确；必须比较阶段状态、队列水位、错误分类和恢复动作。

### 认证与在线状态分离

身份令牌证明玩家是谁，在线会话说明当前连接和代际。旧连接断开不能把新连接的在线状态清除。

验证时固定软件版本、配置和输入规模，先保存正常轨迹，再只改变一个边界条件。最终结果相同并不代表过程正确；必须比较阶段状态、队列水位、错误分类和恢复动作。

### 房间列表是版本化投影

列表可以缓存和分页，但创建、满员、关闭等变化要带版本；客户端发现版本缺口时重新获取快照，而不是应用不完整增量。

验证时固定软件版本、配置和输入规模，先保存正常轨迹，再只改变一个边界条件。最终结果相同并不代表过程正确；必须比较阶段状态、队列水位、错误分类和恢复动作。

### 入房要预留再确认

匹配结果先创建有期限的席位预留，游戏服接受一次性进入令牌后确认。超时释放，重复确认返回同一结果。

验证时固定软件版本、配置和输入规模，先保存正常轨迹，再只改变一个边界条件。最终结果相同并不代表过程正确；必须比较阶段状态、队列水位、错误分类和恢复动作。

### 断线重连按会话恢复

重连不是重新匹配。大厅先查找仍有效的对局归属，再签发新连接代际和短期进入凭据。

验证时固定软件版本、配置和输入规模，先保存正常轨迹，再只改变一个边界条件。最终结果相同并不代表过程正确；必须比较阶段状态、队列水位、错误分类和恢复动作。

## 六阶段证据链


## 可复现实验

让同一玩家快速双击匹配，并在席位预留后断开网络。先预测大厅会产生几个席位，再延迟游戏服确认、重放旧令牌并模拟房间进程退出；检查玩家最终只能处于空闲、已预留或对局中一个状态。

### 最小实现

```ts
type LobbyState =
  | { kind: "idle"; generation: number }
  | { kind: "reserved"; generation: number; roomId: string; expiresAt: number }
  | { kind: "in_game"; generation: number; roomId: string };
```

### 边界处理

```ts
function disconnect(player: PlayerSession, generation: number) {
  if (player.generation !== generation) return;
  player.connection = null;
  player.reconnectDeadline = now() + 30_000;
}
```

### 证据契约

```ts
function consumeJoinToken(token: JoinToken, room: Room) {
  token.verifySignature();
  token.assertNotExpired();
  if (!room.consumeOnce(token.id, token.seat)) return "duplicate";
  return room.attach(token.playerId, token.seat);
}
```

实现代码只是实验的一部分。每次运行还要记录构建版本、配置摘要、样本类型、输入规模、首偏离节点、恢复动作和最终决策。若无法用相同输入重放，结论不能进入交付清单。

## 两个必须主动制造的失败


## 验收矩阵

| 维度 | 正常样本 | 边界样本 | 失败样本 | 通过条件 |
| --- | --- | --- | --- | --- |
| 输入 | 合法且固定 | 容量、顺序或时序临界值 | 畸形、重复或中断 | 分类清晰且可重放 |
| 状态 | 按六阶段推进 | 达到预算但不越界 | 在首偏离节点停止 | 无隐式跨层写入 |
| 输出 | 结果和证据一致 | 退化可解释 | 拒绝、补偿或回滚 | 不变量恢复 |
| 交接 | 他人按记录复验 | 干净环境复验 | 删除隐含依赖 | 结论仍成立 |

本页签发不变量是：**同一玩家同一时刻最多拥有一个有效大厅会话和一个对局归属；一次性令牌只被消费一次；超时席位必然释放；旧代际断开不会清除新会话。** 任何演示、截图或单次成功都不能替代它。

## 练习


## 小结

- **目录披露边界**：公开书目只披露本章章名。会话、房间、匹配、令牌和重连是用于掌握大厅核心职责的教学展开，不冒充原书小节。
- **认证与在线状态分离**：身份令牌证明玩家是谁，在线会话说明当前连接和代际。旧连接断开不能把新连接的在线状态清除。
- **房间列表是版本化投影**：列表可以缓存和分页，但创建、满员、关闭等变化要带版本；客户端发现版本缺口时重新获取快照，而不是应用不完整增量。
- **入房要预留再确认**：匹配结果先创建有期限的席位预留，游戏服接受一次性进入令牌后确认。超时释放，重复确认返回同一结果。
- 已用正常、边界和失败样本验证“同一玩家同一时刻最多拥有一个有效大厅会话和一个对局归属；一次性令牌只被消费一次；超时席位必然释放；旧代际断开不会清除新会话。”。
- 已把公开目录、历史语境和现代教学展开分开记录。

## 术语表


## 来源与改编边界

