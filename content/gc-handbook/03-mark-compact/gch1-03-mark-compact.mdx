---
title: "第3章 标记-整理垃圾回收"
description: "依据Jones、Hosking与Moss《垃圾回收算法手册》2012版、2016中文译本完整目录独立重构：比较双指针、Lisp 2、线索整理和单遍算法如何计算转发地址、更新引用并移动对象"
type: C
section: "垃圾回收算法手册：自动内存管理的艺术（2016中文版）"
draft: false
math: true
sourceUrl: "https://gchandbook.org/editions.html"
demo: true
order: 0
---

import { Callout } from "@/components/mdx/callout";
import { Objectives } from "@/components/mdx/objectives";
import { Term } from "@/components/mdx/term";
import { Answer, Exercises } from "@/components/mdx/exercises";
import { Glossary, GlossaryItem } from "@/components/mdx/glossary";
import { Attribution } from "@/components/mdx/attribution";

# 第3章 标记-整理垃圾回收

本页对应第一版正式结构中的 **第3章 标记-整理垃圾回收**。课程不复制原文，而是按目录逐节点独立重构算法、证明与工程接口，目标是比较双指针、Lisp 2、线索整理和单遍算法如何计算转发地址、更新引用并移动对象。每项结论必须回到对象图、状态转换、伪代码和可重复测量，不能用某个JVM参数名称替代算法理解。

<Callout type="info">**版本与来源边界。** 本课程锁定Richard Jones、Antony Hosking、Eliot Moss合著的《The Garbage Collection Handbook: The Art of Automatic Memory Management》第一版（英文原版2012；2016修订电子版511页）及机械工业出版社2016年中文译本《垃圾回收算法手册：自动内存管理的艺术》（王雅光、薛迪译，437页，ISBN 9787111528821）。作者官网明确中文本译自2012版；目录按第一版19章、术语表、参考文献和索引核对。2023年第2版新增内容只可作后续补充，不冒充中文版原始结构。</Callout>

## 可验收学习目标

<Objectives>
- 能解释本页全部11个正式节点的对象图、状态转换、正确性条件和性能代价。
- 能实现或手工重放“构造含固定对象、内部指针与不同存活率的堆，重放四类整理算法并验证每条引用都指向新地址”，保存输入、原始输出和停止条件。
- 能比较安全性、完整性、及时性、吞吐、尾延迟、空间与实现复杂度，不用单指标下结论。
- 能设计至少一个会推翻当前选择的反例，并用四类整理算法通路图、地址更新表、对象次序与局部性比较、移动约束清单完成独立交接。
</Objectives>

首要陷阱是“把整理等同于把对象挤到一端，未证明转发地址、引用修复、对象顺序和不可移动对象的处理”。先预测，再执行；任何算法选择都必须说明对象是否移动、何时与变异器并发、需要多少备用空间、怎样处理根与跨区引用，以及失败时如何退化或回滚。

## 官方目录逐节点复刻

### 第3章 标记-整理垃圾回收

**正式节点 1/11。** 从根集合、对象图、分配状态和回收阶段四层解释“第3章 标记-整理垃圾回收”。先写出安全性或进展不变式，再标注实现所需的元数据、读写屏障和空间余量；最后用固定工作负载记录原始证据。该节点服务于“比较双指针、Lisp 2、线索整理和单遍算法如何计算转发地址、更新引用并移动对象”，不能只保留术语定义。

验证时先预测改变存活率、分配率、对象大小、线程数或堆余量后会发生什么，再只改变一个变量。若结果与预测相反，应检查根枚举、对象移动、缓存与同步、并发交错和测量窗口，而不是删掉反例。

### 3.1 双指针整理

**正式节点 2/11。** 从根集合、对象图、分配状态和回收阶段四层解释“3.1 双指针整理”。先写出安全性或进展不变式，再标注实现所需的元数据、读写屏障和空间余量；最后用固定工作负载记录原始证据。该节点服务于“比较双指针、Lisp 2、线索整理和单遍算法如何计算转发地址、更新引用并移动对象”，不能只保留术语定义。

验证时先预测改变存活率、分配率、对象大小、线程数或堆余量后会发生什么，再只改变一个变量。若结果与预测相反，应检查根枚举、对象移动、缓存与同步、并发交错和测量窗口，而不是删掉反例。

### 3.2 Lisp 2算法

**正式节点 3/11。** 从根集合、对象图、分配状态和回收阶段四层解释“3.2 Lisp 2算法”。先写出安全性或进展不变式，再标注实现所需的元数据、读写屏障和空间余量；最后用固定工作负载记录原始证据。该节点服务于“比较双指针、Lisp 2、线索整理和单遍算法如何计算转发地址、更新引用并移动对象”，不能只保留术语定义。

验证时先预测改变存活率、分配率、对象大小、线程数或堆余量后会发生什么，再只改变一个变量。若结果与预测相反，应检查根枚举、对象移动、缓存与同步、并发交错和测量窗口，而不是删掉反例。

### 3.3 线索整理

**正式节点 4/11。** 从根集合、对象图、分配状态和回收阶段四层解释“3.3 线索整理”。先写出安全性或进展不变式，再标注实现所需的元数据、读写屏障和空间余量；最后用固定工作负载记录原始证据。该节点服务于“比较双指针、Lisp 2、线索整理和单遍算法如何计算转发地址、更新引用并移动对象”，不能只保留术语定义。

验证时先预测改变存活率、分配率、对象大小、线程数或堆余量后会发生什么，再只改变一个变量。若结果与预测相反，应检查根枚举、对象移动、缓存与同步、并发交错和测量窗口，而不是删掉反例。

### 3.4 单遍算法

**正式节点 5/11。** 从根集合、对象图、分配状态和回收阶段四层解释“3.4 单遍算法”。先写出安全性或进展不变式，再标注实现所需的元数据、读写屏障和空间余量；最后用固定工作负载记录原始证据。该节点服务于“比较双指针、Lisp 2、线索整理和单遍算法如何计算转发地址、更新引用并移动对象”，不能只保留术语定义。

验证时先预测改变存活率、分配率、对象大小、线程数或堆余量后会发生什么，再只改变一个变量。若结果与预测相反，应检查根枚举、对象移动、缓存与同步、并发交错和测量窗口，而不是删掉反例。

### 3.5 需要考虑的问题

**正式节点 6/11。** 从根集合、对象图、分配状态和回收阶段四层解释“3.5 需要考虑的问题”。先写出安全性或进展不变式，再标注实现所需的元数据、读写屏障和空间余量；最后用固定工作负载记录原始证据。该节点服务于“比较双指针、Lisp 2、线索整理和单遍算法如何计算转发地址、更新引用并移动对象”，不能只保留术语定义。

验证时先预测改变存活率、分配率、对象大小、线程数或堆余量后会发生什么，再只改变一个变量。若结果与预测相反，应检查根枚举、对象移动、缓存与同步、并发交错和测量窗口，而不是删掉反例。

### 整理是否必要？

**正式节点 7/11。** 从根集合、对象图、分配状态和回收阶段四层解释“整理是否必要？”。先写出安全性或进展不变式，再标注实现所需的元数据、读写屏障和空间余量；最后用固定工作负载记录原始证据。该节点服务于“比较双指针、Lisp 2、线索整理和单遍算法如何计算转发地址、更新引用并移动对象”，不能只保留术语定义。

验证时先预测改变存活率、分配率、对象大小、线程数或堆余量后会发生什么，再只改变一个变量。若结果与预测相反，应检查根枚举、对象移动、缓存与同步、并发交错和测量窗口，而不是删掉反例。

### 整理的吞吐成本

**正式节点 8/11。** 从根集合、对象图、分配状态和回收阶段四层解释“整理的吞吐成本”。先写出安全性或进展不变式，再标注实现所需的元数据、读写屏障和空间余量；最后用固定工作负载记录原始证据。该节点服务于“比较双指针、Lisp 2、线索整理和单遍算法如何计算转发地址、更新引用并移动对象”，不能只保留术语定义。

验证时先预测改变存活率、分配率、对象大小、线程数或堆余量后会发生什么，再只改变一个变量。若结果与预测相反，应检查根枚举、对象移动、缓存与同步、并发交错和测量窗口，而不是删掉反例。

### 长寿命数据

**正式节点 9/11。** 从根集合、对象图、分配状态和回收阶段四层解释“长寿命数据”。先写出安全性或进展不变式，再标注实现所需的元数据、读写屏障和空间余量；最后用固定工作负载记录原始证据。该节点服务于“比较双指针、Lisp 2、线索整理和单遍算法如何计算转发地址、更新引用并移动对象”，不能只保留术语定义。

验证时先预测改变存活率、分配率、对象大小、线程数或堆余量后会发生什么，再只改变一个变量。若结果与预测相反，应检查根枚举、对象移动、缓存与同步、并发交错和测量窗口，而不是删掉反例。

### 局部性

**正式节点 10/11。** 从根集合、对象图、分配状态和回收阶段四层解释“局部性”。先写出安全性或进展不变式，再标注实现所需的元数据、读写屏障和空间余量；最后用固定工作负载记录原始证据。该节点服务于“比较双指针、Lisp 2、线索整理和单遍算法如何计算转发地址、更新引用并移动对象”，不能只保留术语定义。

验证时先预测改变存活率、分配率、对象大小、线程数或堆余量后会发生什么，再只改变一个变量。若结果与预测相反，应检查根枚举、对象移动、缓存与同步、并发交错和测量窗口，而不是删掉反例。

### 标记-整理算法的局限

**正式节点 11/11。** 从根集合、对象图、分配状态和回收阶段四层解释“标记-整理算法的局限”。先写出安全性或进展不变式，再标注实现所需的元数据、读写屏障和空间余量；最后用固定工作负载记录原始证据。该节点服务于“比较双指针、Lisp 2、线索整理和单遍算法如何计算转发地址、更新引用并移动对象”，不能只保留术语定义。

验证时先预测改变存活率、分配率、对象大小、线程数或堆余量后会发生什么，再只改变一个变量。若结果与预测相反，应检查根枚举、对象移动、缓存与同步、并发交错和测量窗口，而不是删掉反例。

## 从对象图与不变式开始

整理必须同时解决三个问题：为每个存活对象确定无重叠目标地址，把所有根和堆内引用改写为目标地址，最后按不会覆盖尚未读取数据的顺序移动对象。双指针法空间开销小但会打乱顺序；Lisp 2通常通过计算地址、改写引用、搬移对象三趟保持顺序；线索法借用引用字段形成回链，适合缺少额外转发表的环境。移动换来连续空闲区和指针碰撞分配，却要求运行时能精确找到引用并处理锁、哈希、原生句柄等地址敏感状态。

所有垃圾回收问题先抽象为<Term def="以对象为顶点、引用为有向边并显式标出根的运行时状态模型">对象图</Term>。<Term def="运行时承诺可由栈、寄存器、全局、句柄或原生接口直接访问的入口集合">根集合</Term>是遍历入口；从根可达只是保守的存活近似，并不等于对象未来必被使用。<Term def="执行应用逻辑、分配对象并持续改变对象图的线程">变异器</Term>与回收器共享同一堆状态。安全性要求不能回收仍可能被访问的对象，完整性描述是否能找出所有垃圾，及时性描述识别与回收之间的延迟。回收器、分配器、编译器和语言语义共同决定这些合同，任何一层缺失都可能让局部正确的算法在系统中失效。

停止世界、并行和并发是三个正交维度。停止世界描述变异器是否暂停，并行描述多个回收线程是否同时完成同一阶段，并发描述回收器是否与变异器重叠运行。并发降低某些暂停，却增加屏障、同步、浮动垃圾和CPU竞争；并行缩短阶段墙钟时间，却可能增加总CPU和内存带宽。必须报告阶段时间线，而不是把“并行并发”当成一个模糊标签。

对象移动决定运行时接口的复杂度。不移动方案保留地址稳定性但可能碎片化；复制与整理产生连续空间和快速分配，却必须更新根、堆内引用、句柄、内部指针和原生代码持有的地址。并发移动还需读屏障、间接层或多版本协议，保证变异器无论在搬移前、中、后读取都看到合法对象身份与字段值。<Term def="回收完成前用于吸收继续分配、对象复制和元数据增长的保留容量">空间余量</Term>必须按最坏工作量而不是平均值估算；并发结构还要指出<Term def="一个并发操作可被视为瞬间生效、并与其他操作形成合法顺序的时刻">线性化点</Term>。

## 最小算法切片

先把算法写成可检查的状态机，而不是产品参数清单：

```text
roots -> discover(object)
while worklist is not empty:
  object = remove(worklist)
  for each reference in object:
    if reference has not been discovered:
      mark or forward reference
      add reference to worklist
reclaim, sweep, compact, or publish the completed space
```

实现记录至少包含对象标识、地址或句柄、颜色/计数/年龄、所属分区、扫描状态和转发状态。每次状态变化注明唯一所有者和原子边界；若元数据会溢出、工作表会耗尽或目的空间会不足，失败路径必须在实验前定义。只给出正常路径的伪代码不能证明回收器可用。

实验输入与环境采用可重放合同：

```yaml
runtime: exact-build-and-commit
heap: fixed-min-max-and-region-layout
workload: seeded-object-graph-and-allocation-trace
controls: one-variable-per-run
evidence: raw-events-percentiles-space-cpu-errors
stop: timeout-or-explicit-safety-limit
```

验收把预期和可推翻条件写在运行前：

```gherkin
Given identical roots, object graph, heap limit, threads, and seed
When one collector policy or workload variable changes
Then safety and progress invariants still hold
And target gains do not hide CPU, space, latency, or failure regressions
```

对并发算法，枚举变异器可能执行的读、写、分配、发布、批量复制、原生调用和线程退出。屏障完整性要逐项核对，阶段切换前冲刷线程本地日志；终止检测不仅检查全局队列为空，还要证明没有本地或飞行中的工作。对实时算法，还要把每步最大成本和不可中断区间写进预算。

## 定量模型与边界

回收占比与有效吞吐必须共用同一墙钟窗口：

$$
R_{gc}=T_{gc}/T_{wall}, qquad Throughput=Work_{mutator}/T_{wall}
$$

追踪与复制工作至少受根、扫描边和存活字节控制：

$$
W_{trace} \approx N_{roots}+N_{edges scanned}, qquad W_{copy} \approx Bytes_{live}
$$

空间安全要求回收完成前仍有足够余量吸收分配和疏散：

$$
Reserve ge AllocationRate 	imes T_{cycle}+Bytes_{evacuated}+Metadata
$$

实时与低延迟比较还应明确一个观测窗内留给变异器的最低份额：

$$
U_{mutator}=T_{mutator}/T_{wall}=1-R_{gc}
$$

公式用于明确变量，不替代测量。每个数字注明单位、堆上限、对象分布、线程数、预热、重复次数、分位数和置信范围。平均暂停、最大暂停和服务端请求尾延迟回答不同问题；一次运行的最小值不能作为容量承诺。

<Callout type="trap">**核心陷阱。** 把整理等同于把对象挤到一端，未证明转发地址、引用修复、对象顺序和不可移动对象的处理。修复方法是回到对象图和时间线，一次只改变一个算法或参数，并同时记录目标指标与CPU、驻留集、分配失败、回收频率和错误率。</Callout>

<Callout type="trap">**实现名称不等于算法证明。** 产品可能组合多个算法并在版本间改变屏障、阶段与默认值。先证明根、对象移动、并发交错和空间余量，再把证据映射到具体实现；只有参数名或日志截图不能通过验收。</Callout>

<Callout type="warning">**安全实验。** 堆耗尽、并发竞态、对象移动和原生引用实验应在受限进程或容器内执行，设置内存、CPU和超时，禁止对生产数据直接做破坏性重放。堆转储和日志可能包含业务数据，应最小授权并记录销毁。</Callout>

## 本章回顾

本页从“第3章 标记-整理垃圾回收”覆盖到“标记-整理算法的局限”，共11个正式节点。闭环是先建立对象图与不变式，再执行“构造含固定对象、内部指针与不同存活率的堆，重放四类整理算法并验证每条引用都指向新地址”，最后用四类整理算法通路图、地址更新表、对象次序与局部性比较、移动约束清单证明结论可重放、可推翻、可回滚。若仍出现“把整理等同于把对象挤到一端，未证明转发地址、引用修复、对象顺序和不可移动对象的处理”，应补做失败交错和空间余量实验，而不是继续叠加参数。

<Exercises>

**问题 1**：第3章 标记-整理垃圾回收覆盖哪些正式节点，核心证明主线是什么？

<Answer>本页覆盖11个正式节点，从“第3章 标记-整理垃圾回收”到“标记-整理算法的局限”。核心主线是比较双指针、Lisp 2、线索整理和单遍算法如何计算转发地址、更新引用并移动对象，交付四类整理算法通路图、地址更新表、对象次序与局部性比较、移动约束清单。</Answer>

**问题 2**：怎样为第3章 标记-整理垃圾回收建立最小可执行实验？

<Answer>固定对象图、根、分配序列、堆上限、线程和测量窗口，执行“构造含固定对象、内部指针与不同存活率的堆，重放四类整理算法并验证每条引用都指向新地址”；保存基线、单变量对照、原始证据与停止条件。</Answer>

**问题 3**：为什么“把整理等同于把对象挤到一端，未证明转发地址、引用修复、对象顺序和不可移动对象的处理”会破坏结论？

<Answer>它遗漏正确性合同、系统接口或负面成本。必须回到四类整理算法通路图、地址更新表、对象次序与局部性比较、移动约束清单，补齐对象移动、并发交错、空间余量、吞吐CPU内存与失败回滚。</Answer>

**问题 4**：如何为第3章 标记-整理垃圾回收设计反证？

<Answer>保持输入与环境不变，只提高存活率、分配率、共享度或线程数中的一个；一旦安全性、尾延迟、空间、CPU或进展保证越界，即推翻原选择。</Answer>

**问题 5**：把该章算法迁移到另一语言或运行时时，哪些合同必须重建？

<Answer>重新核对根枚举、对象布局、引用精度、移动能力、屏障插入、弱引用与终结语义、原生接口、内存模型和日志口径，不能复制产品参数。</Answer>

**问题 6**：达到独立交接标准需要哪些证据？

<Answer>需要版本化源码或伪代码、对象图、输入生成器、完整配置、原始测量、失败反例、资源风险和回滚，让他人仅凭四类整理算法通路图、地址更新表、对象次序与局部性比较、移动约束清单重放并挑战结论。</Answer>

</Exercises>

<Glossary>
  <GlossaryItem term="对象图">以对象为顶点、引用为有向边并显式标出根的运行时状态模型。</GlossaryItem>
  <GlossaryItem term="根集合">运行时承诺可由栈、寄存器、全局、句柄或原生接口直接访问的入口集合。</GlossaryItem>
  <GlossaryItem term="变异器">执行应用逻辑并改变对象图的程序线程。</GlossaryItem>
  <GlossaryItem term="空间余量">回收完成前承接继续分配、复制与元数据所需的保留空间。</GlossaryItem>
  <GlossaryItem term="线性化点">一个并发操作可被视为瞬间生效、并与其他操作形成合法顺序的时刻。</GlossaryItem>
</Glossary>

[← 上一页：第2章 标记-清扫垃圾回收](/learn/gc-handbook/02-mark-sweep/gch1-02-mark-sweep) · [下一页：第4章 复制式垃圾回收 →](/learn/gc-handbook/04-copying/gch1-04-copying)

<Attribution adaptedFrom="Jones、Hosking、Moss《The Garbage Collection Handbook》第一版及2016中文译本" adaptedUrl="https://gchandbook.org/editions.html" />
