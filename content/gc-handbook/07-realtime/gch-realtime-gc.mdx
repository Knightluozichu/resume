---
title: "实时GC"
type: A
section: "JVM与Java生态"
order: 7
description: "深入理解实时GC的工作量模型、Slack调度与Metronome时间片策略，以及如何保证GC停顿不超过预设截止时间"
demo: true
math: false
sourceUrl: ""
draft: true
---

import { GchRealtimeGcDiagram } from "@/components/mdx/gc-handbook/diagrams/gch-realtime-gc";
import { Objectives, Callout, Glossary, GlossaryItem, Term, Exercises, Answer } from "@/components/mdx/mdx-components";

<Objectives>
- 理解实时GC的核心目标——保证GC停顿不超过预设截止时间
- 掌握工作量模型——分配率×时间=必须完成的GC工作量
- 理解Slack调度——如何动态调整GC工作时机
- 掌握Metronome时间片调度——固定周期内分配mutator/GC时间比
</Objectives>

## 直觉引入

实时GC像「红绿灯智能环卫」。普通环卫车想扫多久扫多久，堵车（STW）没商量。实时环卫车有严格的时间预算——红灯（mutator运行）和绿灯（GC运行）交替，每次绿灯不超过1毫秒。关键是环卫车得算清楚「这一片区产生多少垃圾（工作量）」，在绿灯时间内做完该做的，如果做不完就提前预警（Slack为负），绝不能超时。Metronome就是那个「定时器」——固定周期切换红绿灯，保证绿灯时间永远在预算内。

<GchRealtimeGcDiagram />

## 实时GC的核心挑战

```text
普通GC的问题：
  停顿时间 = f(堆大小, 存活对象数量, 碎片程度)
  堆越大 → 停顿越长
  无法预测，无法保证上限

实时GC的目标：
  停顿时间 ≤ 预算（如1ms, 10ms）
  不管堆多大，停顿都有上限
  适用于：实时系统、交易系统、游戏引擎、嵌入式

核心矛盾：
  GC工作总量 = 固定的（与堆大小和存活量成正比）
  但必须拆成小片在截止时间内完成
  → 需要精确的工作量模型 + 调度策略
```

<Callout type="insight">
**实时GC的本质**：将一个大GC拆成无数个小GC片段，每个片段在时间预算内完成。关键不是让GC变快，而是让每段GC的停顿可控。这要求GC工作可拆分、可中断、可恢复，且能精确预估每段的工作量。
</Callout>

## 工作量模型

```text
核心公式：

  工作量 W = 分配率 × 时间间隔
  → 每个周期内mutator分配的对象量 = GC需要回收的对象量

  Slack = 时间预算 - 必需GC时间
  → Slack > 0：安全，有余量
  → Slack = 0：临界，刚好完成
  → Slack < 0：溢出！无法在截止时间内完成GC工作

示例：
  分配率 = 100MB/s
  GC周期 = 10ms
  → 每周期工作量 W = 100MB/s × 0.01s = 1MB
  → GC必须在10ms内处理完1MB垃圾

  如果处理1MB需要3ms：
    Slack = 10ms - 3ms = 7ms → 安全

  如果分配率飙升到500MB/s：
    W = 5MB → 处理需要15ms
    Slack = 10ms - 15ms = -5ms → 溢出！
    → 需要增大GC频率或减小分配率
```

## Metronome 时间片调度

```text
Metronome策略（IBM经典实现）：

  固定周期 T（如1ms）：
    ┌──T──┬──T──┬──T──┬──T──┐
    │m|GC │m|GC │m|GC │m|GC │
    └─────┴─────┴─────┴─────┘
    m = mutator运行时间
    GC = GC工作时间
    T = m + GC

  时间分配（如 T=1ms）：
    60% mutator（0.6ms）
    40% GC（0.4ms）

  保证：
    每个时间片内GC停顿 ≤ 0.4ms
    mutator不会被暂停超过0.4ms

  特点：
    优点：停顿可预测，严格保证截止时间
    缺点：即使不需要GC也分配40%给GC（浪费）
    缺点：分配率低时GC空闲，分配率高时可能不够
```

## Slack-based 动态调度

```text
Slack-based策略（动态调整）：

  不固定GC时间比例，根据Slack动态调整：

  ┌─────┬───────┬─────┬───────────┬─────┐
  │m|GC │mmmmmm │m|GC │mmmmmmmmmm │m|GC │
  └─────┴───────┴─────┴───────────┴─────┘

  Slack大（分配率低）→ GC少做，mutator多运行
  Slack小（分配率高）→ GC多做，mutator少运行
  Slack为负 → 紧急模式，增加GC频率

  特点：
    优点：自适应分配率变化，不浪费GC时间片
    优点：分配率低时吞吐更好
    缺点：停顿时间不如Metronome可预测
    缺点：实现复杂，需精确的Slack计算
```

<Callout type="trap">
**实时GC不等于低延迟GC**：实时GC的目标是「可预测的停顿上限」（hard real-time保证截止时间），而非「最低平均停顿」。ZGC的平均停顿可能比某些实时GC更短，但ZGC不保证硬性截止时间。实时GC牺牲了部分吞吐和内存利用率来换取可预测性。对于「大多数时候快但偶尔可以慢」的场景，ZGC/Shenandoah更合适；对于「任何时候都不能超过X毫秒」的场景，才需要实时GC。
</Callout>

## 实时GC的算法要求

```text
可拆分性：
  GC工作必须能拆成小片段
  标记：可按Region/页拆分
  清除/复制：可按对象/块拆分
  压缩：表格法支持分块并行

可中断性：
  GC片段必须能在任意点中断并恢复
  需要保存中间状态（标记栈、扫描位置等）

可预估性：
  每段GC的工作量必须可预估
  才能计算Slack并做出调度决策

读屏障 vs 写屏障：
  实时GC通常用读屏障（Baker式）
  读屏障在每次读引用时检查对象是否已转发
  保证mutator不会访问到未就位的对象
```

<Exercises>
**问题 1**：实时GC的工作量模型是什么？Slack为正和为负分别意味着什么？

<Answer>
工作量模型：W = 分配率 × 时间间隔。mutator在每个GC周期内分配的对象量等于GC需要回收的工作量。例如分配率100MB/s、GC周期10ms，则每周期工作量W=1MB，GC必须在10ms内处理完1MB垃圾。Slack = 时间预算 - 必需GC时间。Slack为正意味着GC在时间预算内有余量，可以安全完成工作，甚至可以少做一些GC工作让mutator多运行（Slack-based调度的做法）。Slack为零意味着GC刚好能在截止时间内完成，处于临界状态。Slack为负意味着必需的GC时间超过了时间预算，GC无法在截止时间内完成所有工作，这是溢出状态——GC工作堆积，可能导致后续停顿超过截止时间或触发降级（如Full GC）。Slack为负的应对策略：①增大GC频率（缩短周期，减少每周期工作量）；②减小mutator分配率（限流或背压）；③增大时间预算（但会增加单次停顿）；④增加GC线程（并行处理更多工作）。
</Answer>

**问题 2**：Metronome调度和Slack-based调度各自的工作原理和优缺点是什么？

<Answer>
Metronome调度：固定周期T（如1ms），每周期内固定比例分给mutator和GC（如60%/40%）。优点：①停顿严格可预测——每周期GC停顿不超过预算（如0.4ms）；②实现相对简单——固定调度不需要复杂计算；③适合hard real-time——严格保证截止时间。缺点：①浪费——分配率低时GC仍占用40%时间片但无事可做；②不够灵活——分配率突增时固定40%可能不够，导致Slack为负；③吞吐损失——即使不需要GC也固定分配时间。Slack-based调度：根据Slack值动态调整mutator和GC的时间分配。Slack大时GC少做（mutator多运行），Slack小时GC多做。优点：①自适应——根据实际分配率调整GC工作量，不浪费；②吞吐更好——分配率低时mutator获得更多时间；③灵活——分配率突增时自动增加GC频率。缺点：①停顿不如Metronome可预测——动态调度导致每周期停顿可能不同；②实现复杂——需精确计算Slack，需处理调度决策的边界条件；③不适合hard real-time——可能偶尔超过截止时间。
</Answer>

**问题 3**：实时GC对算法有什么特殊要求？为什么标记-清除比标记-压缩更适合实时GC？

<Answer>
实时GC对算法的特殊要求：①可拆分性——GC工作必须能拆成小片段在时间预算内完成。标记阶段可按Region/页拆分，每段标记一部分对象；清除阶段可按块拆分，每段清除一部分堆。②可中断性——GC片段必须能在任意点中断并恢复，需要保存中间状态（标记栈位置、扫描进度等）。③可预估性——每段GC的工作量必须可预估，才能计算Slack并做调度决策。标记-清除比标记-压缩更适合实时GC的原因：①标记-清除不移动对象——移动对象需要原子性（不能在中途停），而标记和清除都可以在任意点中断恢复；②标记-清除的清除阶段只需将未标记对象加入自由链表，每个对象的处理是独立的，天然可拆分；③标记-压缩的三趟流程（计算地址→更新引用→移动对象）有依赖关系——必须先算完所有地址才能更新引用，更新完引用才能移动，难以在中间中断恢复；④标记-清除的工作量更容易预估——标记阶段遍历存活对象（数量可统计），清除阶段遍历堆（大小已知），而压缩的移动开销与存活对象大小和引用密度相关，预估更难。
</Answer>

**问题 4**：实时GC与ZGC/Shenandoah等低延迟GC有什么本质区别？各自适合什么场景？

<Answer>
本质区别：实时GC追求「可预测的停顿上限」（hard real-time，保证截止时间），而ZGC/Shenandoah追求「最低平均停顿」（soft real-time，尽量短但不保证上限）。具体区别：①调度模型——实时GC有严格的工作量模型和Slack计算，主动控制GC频率和停顿；ZGC/Shenandoah通过并发化减少STW，不做主动调度。②屏障开销——实时GC通常用读屏障（Baker式），每次读引用都检查转发状态，开销大但保证mutator不访问未就位对象；ZGC也用读屏障但配合染色指针优化，Shenandoah用Brooks转发指针。③GC频率——实时GC以固定或动态周期运行，频率高但每次短；ZGC/Shenandoah在堆使用率达到阈值时触发，频率较低。④吞吐——实时GC因屏障开销和固定GC时间片，吞吐损失较大（10%~30%）；ZGC/Shenandoah的吞吐损失较小（5%~15%）。适合场景：实时GC适合「任何时候都不能超过X毫秒」的hard real-time系统（航空、医疗、工业控制、高频交易）；ZGC/Shenandoah适合「大多数时候快但偶尔可以慢」的soft real-time系统（Web服务、微服务、大数据处理）。
</Answer>
</Exercises>

<Glossary>
<GlossaryItem term="工作量模型">W = 分配率 × 时间间隔。每个GC周期内mutator分配的对象量等于GC需要回收的工作量。是实时GC调度的基础。</GlossaryItem>
<GlossaryItem term="Slack">时间预算 - 必需GC时间。正值=安全有余量，零=临界，负值=溢出无法完成。Slack-based调度据此动态调整GC频率。</GlossaryItem>
<GlossaryItem term="Metronome">IBM的实时GC调度策略。固定周期内固定比例分配mutator/GC时间（如60%/40%）。停顿严格可预测但可能浪费。</GlossaryItem>
<GlossaryItem term="可拆分性">实时GC的算法要求。GC工作必须能拆成小片段在时间预算内完成，且每段可独立中断和恢复。</GlossaryItem>
<GlossaryItem term="读屏障（Baker式）">每次读引用时检查对象是否已转发。实时GC用它保证mutator不访问未就位对象。开销大但保证正确性。</GlossaryItem>
</Glossary>
