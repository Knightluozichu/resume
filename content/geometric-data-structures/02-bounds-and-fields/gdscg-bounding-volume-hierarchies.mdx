---
title: "第4章 包围体层次"
type: C
section: "几何数据结构 · 包围与距离"
order: 1
description: "围绕BVH构建准则、形变更新、碰撞检测和随机碰撞检测建立可量化的剪枝模型。"
demo: true
math: true
sourceUrl: "https://www.routledge.com/Geometric-Data-Structures-for-Computer-Graphics/Langetepe-Zachmann/p/book/9780367453862"
draft: true
---

import { Objectives, Callout, Attribution, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step } from "@/components/mdx/mdx-components";
import { GdscgBoundingVolumeHierarchiesObjectLab, GdscgBoundingVolumeHierarchiesQueryLab, GdscgBoundingVolumeHierarchiesEvidenceLab } from "@/components/mdx/geometric-data-structures/diagrams/gdscg-bounding-volume-hierarchies";

<Objectives>
- 能解释包围体层次、AABB与k-DOP的对象和不变量
- 能推导本页复杂度或几何谓词，并用交互图追踪构建、查询与更新
- 能实现最小实验，以精确基线、边界输入和反例验收重拟合
</Objectives>

## 先做预测

先预测结果，再观察结构变化并解释偏差。逐渐把两个物体靠近并增加局部形变，预测访问节点对、叶测试和重拟合后重叠率。比较紧包围体与廉价包围体，判断何时测试成本抵消剪枝收益。 本页用<Term def="叶包围图元、内部节点包围全部后代的对象层次。">包围体层次</Term>、<Term def="边与坐标轴平行、可用逐轴区间重叠快速测试的包围盒。">AABB</Term>、<Term def="由固定方向上的最小最大投影组成的离散方向多面体。">k-DOP</Term>、<Term def="从两棵BVH根对开始，仅展开包围体重叠的节点对。">同步遍历</Term>、<Term def="形变后从叶到根更新包围体而暂不改变树拓扑。">重拟合</Term>建立从对象、表示、查询到证书的完整链路。

<GdscgBoundingVolumeHierarchiesObjectLab />

## 第一版权威目录定位

本页一一对应Elmar Langetepe与Gabriel Zachmann在A K Peters出版的2006年第一版《Geometric Data Structures for Computer Graphics》第4章 包围体层次。出版社页面确认第一版、两位作者、362页和正式10章目录；作者所在研究组同时给出原书信息、ISBN 9781568812359、目录以及第7章材料。

### 原书小节覆盖

1. 4.1 Construction of BV Hierarchies
2. 4.1.1 Construction Criteria
3. 4.1.2 Criterion for Collision Detection
4. 4.1.3 Construction Algorithm
5. 4.2 Updating for Morphing Objects
6. 4.3 Collision Detection
7. 4.3.1 Stochastic Collision Detection

### 核心对象

1. **包围体层次**：叶包围图元、内部节点包围全部后代的对象层次。
2. **AABB**：边与坐标轴平行、可用逐轴区间重叠快速测试的包围盒。
3. **k-DOP**：由固定方向上的最小最大投影组成的离散方向多面体。
4. **同步遍历**：从两棵BVH根对开始，仅展开包围体重叠的节点对。
5. **重拟合**：形变后从叶到根更新包围体而暂不改变树拓扑。

## 查询与表示契约

先固定数据域、维数、闭开边界、查询输出和数值语义。树高或节点数不是独立目标；只有候选完备、结果无重复、复杂度含输出规模且数值分支一致时，结构才算正确。

$$
B(v)\supseteq\bigcup_{u\in children(v)}B(u)
$$

$$
overlap(A,B)=\bigwedge_i(a_i^{max}\ge b_i^{min}\land b_i^{max}\ge a_i^{min})
$$

$$
C=C_T+P_LC_L+P_RC_R
$$

$$
\rho=\frac{\operatorname{vol}(B_L\cap B_R)}{\operatorname{vol}(B_L\cup B_R)}
$$

BVH正确性来自包含不变量：若父包围体不相交，其所有后代都不相交。同步遍历每次展开更大或非叶节点；构建准则近似最小化预期访问成本，既依赖子体表面积或体积，也依赖图元数量和查询分布。形变重拟合保持正确但可能降低树质量。 每个剪枝判断都应能回答两个问题：被排除的集合是什么，以及哪个不变量证明它不含答案。

## 可核查构建与查询

叶节点保存图元索引和精确测试入口，内部节点保存孩子区间。构建器按质心或代价分裂，遍历器使用显式栈并记录节点对。更新后计算重叠率和包围体膨胀率，超过阈值时局部旋转或重建。随机碰撞模式必须把漏检概率单独报告。


```python
def query_with_trace(root, request):
    stack, hits, trace = [root], [], []
    while stack:
        node = stack.pop()
        trace.append(node.id)
        if node.disjoint(request):
            continue
        if node.is_leaf:
            hits.extend(node.exact_matches(request))
        else:
            stack.extend(node.children)
    return sorted(set(hits)), trace
```

通用骨架必须由本章的具体不变量实例化：区域不相交、包围体不重叠、空邻域、证书未失效或静态块版本有效。不能把候选生成误写成最终答案，叶节点仍需执行本章定义的精确测试。

<GdscgBoundingVolumeHierarchiesQueryLab />

## 复杂度与工程取舍

复杂度报告分离构建、查询、更新、内存和输出规模。平均值之外记录P95与最坏样例；同时保存访问节点、候选对象和精确谓词次数，这样才能判断瓶颈来自树质量、数据分布还是数值回退。

```python
metrics = {
    "build_ms": 0.0,
    "visited_nodes": 0,
    "candidates": 0,
    "exact_tests": 0,
    "update_ms": 0.0,
}
assert set(metrics) == {"build_ms", "visited_nodes", "candidates", "exact_tests", "update_ms"}
```

只在结果集合与基线一致后比较性能。构建更慢可能换来大量查询收益，更新更便宜也可能逐渐恶化结构；因此工作负载中的查询次数、更新比例和生命周期必须写入报告。

## 边界、退化与反例

无限平面不能直接放入有限AABB；零面积三角形和NaN会污染所有父盒；两个盒只接触边界是否算碰撞必须与窄相一致。形变后只更新叶不更新祖先会产生假阴性。

```python
audit = {
    "normal": "representative geometry",
    "boundary": "closed-open convention",
    "degenerate": "duplicate or co-located input",
    "baseline": "brute-force exact result",
}
assert all(audit.values())
```

<GdscgBoundingVolumeHierarchiesEvidenceLab />

## 图形学应用

两个高面数模型做碰撞检测，保存每一层候选节点对、最终三角形对和精确交点。性能报告分离构建、重拟合、遍历与精确测试，并比较重新构建后的访问数量。 应用层还要记录从世界坐标到结构坐标的变换，避免把坐标系错误误判为数据结构错误。

## 分步实验

<Stepper>
  <Step title="固定对象与边界"><GdscgBoundingVolumeHierarchiesObjectLab /></Step>
  <Step title="执行构建与查询"><GdscgBoundingVolumeHierarchiesQueryLab /></Step>
  <Step title="保存反例与证书"><GdscgBoundingVolumeHierarchiesEvidenceLab /></Step>
</Stepper>

1. 用五到十个对象手算结构，标出节点覆盖、邻接或证书。
2. 执行一个正常查询和一个恰落在边界的查询，逐节点记录剪枝理由。
3. 删除一个一般位置或静态假设，构造首个失败样例。
4. 与穷举答案逐项比较，再扩大规模测构建、查询、更新和内存。

## 验收证书

验收包包含原始输入、结构参数、节点或图邻接、查询日志、候选集合、精确结果、复杂度计数和失败反例。用预期遍历成本评估候选分裂，并把子包围体重叠率作为重建信号。 任何无法重放的截图或孤立耗时都不能替代证书。

## 常见误区

<Callout type="trap" title="正确性前提被省略">只追求最小根包围盒，却把高度重叠的子集合分到两个孩子，遍历几乎同时访问两侧。</Callout>

<Callout type="trap" title="只看平均耗时">用预期遍历成本评估候选分裂，并把子包围体重叠率作为重建信号。 同时保留最坏输入、内存和更新尾延迟。</Callout>

## 本章回顾

本页覆盖包围体层次、AABB、k-DOP、同步遍历、重拟合。掌握标准是能从查询契约选择表示，推导剪枝或邻近条件，实现构建与更新，并用精确基线、退化输入和复杂度计数形成可重放证书。

<Exercises>
**问题1：包围体层次与AABB如何协作？**
<Answer>叶包围图元、内部节点包围全部后代的对象层次。 边与坐标轴平行、可用逐轴区间重叠快速测试的包围盒。 回答时必须写出对象域和边界约定。</Answer>

**问题2：怎样核查k-DOP的查询结果？**
<Answer>保存访问轨迹与候选集合，再与穷举或精确谓词给出的结果逐项比较；复杂度必须包含输出规模。</Answer>

**问题3：如何验收重拟合？**
<Answer>形变后从叶到根更新包围体而暂不改变树拓扑。 同时保存正常例、边界例、退化例和最坏工作负载。</Answer>
</Exercises>

## 术语表

<Glossary>
  <GlossaryItem term="包围体层次" definition="叶包围图元、内部节点包围全部后代的对象层次。" />
  <GlossaryItem term="AABB" definition="边与坐标轴平行、可用逐轴区间重叠快速测试的包围盒。" />
  <GlossaryItem term="k-DOP" definition="由固定方向上的最小最大投影组成的离散方向多面体。" />
  <GlossaryItem term="同步遍历" definition="从两棵BVH根对开始，仅展开包围体重叠的节点对。" />
  <GlossaryItem term="重拟合" definition="形变后从叶到根更新包围体而暂不改变树拓扑。" />
  <GlossaryItem term="几何证书" definition="足以重放构建、查询、更新和数值判断的一组输入、轨迹与不变量。" />
</Glossary>

<Attribution adaptedFrom="Elmar Langetepe, Gabriel Zachmann · Geometric Data Structures for Computer Graphics, First Edition · 第4章 包围体层次" adaptedUrl="https://www.routledge.com/Geometric-Data-Structures-for-Computer-Graphics/Langetepe-Zachmann/p/book/9780367453862" />
