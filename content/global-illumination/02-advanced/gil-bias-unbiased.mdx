---
title: "光传输求解策略与误差"
type: B
section: "计算机图形学与渲染"
order: 6
description: "复刻第 4 章：radiance 与 importance 的伴随方程、GRDF、路径表述、算法分类及 bias/variance/consistency 权衡"
demo: true
math: true
sourceUrl: "https://books.google.com/books?id=KodRAAAAMAAJ"
draft: true
---

import { GilBiasUnbiasedDiagram, GilPathStrategyDiagram, GilEstimatorTradeoffDiagram } from "@/components/mdx/global-illumination/diagrams/gil-bias-unbiased";
import { GilStepFlowDiagram } from "@/components/mdx/global-illumination/diagrams/gil-step-flow";
import { Objectives, Callout, Attribution, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step } from "@/components/mdx/mdx-components";

<Objectives>

- 能解释 radiance equation 与 importance adjoint equation 的对偶关系
- 能描述 GRDF 如何把反射、透射与局部散射写成统一传输核
- 能推导 path formulation 中顶点、几何、散射和测量因子的乘积
- 能比较 camera/light/bidirectional/finite-element 光传输策略的误差与成本

</Objectives>

## 直觉：同一方程为什么需要很多算法

先预测：一个小光源经镜面反射后照到 diffuse 墙面，再进入相机的 `L S D E` 路径，为什么普通 camera path tracing 很难，而 light tracing 或 bidirectional connection 更容易？答案不在渲染方程是否正确，而在算法从哪一端生成随机决策，以及其 PDF 是否覆盖高贡献路径。

第 4 章 Strategies for Computing Light Transport 位于概率工具和具体算法之间。它不只是讨论“有偏还是无偏”，而是建立统一分类语言：radiance 从 emitter 传播，importance 从 measurement 传播；local scattering 由广义核表示；最终测量可以写成方向/表面积分，也可以展开成 path space 上不同长度路径的积分。算法只是选择其中哪些变量、从哪一端、以何种分布采样或离散。

<GilBiasUnbiasedDiagram />

## 正向辐亮度方程

把表面位置与方向组成 phase-space 状态，用 $L_e$ 表示 emission，$\mathcal K$ 表示“一次传播 + 一次散射”的线性算子，radiance equation 可写为：

$$
L=L_e+\mathcal K L,
\qquad L=(I-\mathcal K)^{-1}L_e.
$$

若能量守恒且系统稳定，逆算子可展开为 $L=\sum_{k=0}^{\infty}\mathcal K^kL_e$。第 $k$ 项对应包含 $k$ 次散射的 transport。Path tracing 是对这些项的随机估计；radiosity 是选择表面基函数后对算子的有限维近似；二者不是不同物理。

<Term def="把一次局部入射状态映射为出射状态的广义散射核，可统一表达反射、透射和方向变化，并与传播算子组合">广义反射分布函数</Term>（GRDF, generalized reflectance distribution function）用于抽象 local scattering。普通 BRDF 只处理同侧反射方向；广义形式可把 reflection/transmission 以及 delta event 纳入统一状态空间。实现时仍必须明确连续 solid-angle density 与离散 delta probability，不能把所有事件都当普通浮点 PDF。

## 重要度伴随方程

<Term def="从传感器响应向场景反向传播的敏感度，表示某个位置方向状态对最终测量的潜在贡献">重要度函数</Term>（importance function）从 sensor emission $W_e$ 出发，通过伴随算子 $\mathcal K^*$ 传播：

$$
W=W_e+\mathcal K^*W.
$$

这就是 <Term def="由正向传输算子的内积关系定义、从测量端反向传播 importance 的积分方程">伴随方程</Term>（adjoint equation）。伴随不是简单把 ray 方向取反；它要求在选定内积和 measure 下满足 $\langle W,\mathcal K L\rangle=\langle\mathcal K^*W,L\rangle$。对 reciprocal scattering，正反局部核常有对称关系，但 sensor response、emitter distribution 与非互易材料仍需分别处理。

测量可以从任一端计算：

$$
M=\langle W_e,L\rangle=\langle W,L_e\rangle.
$$

Camera tracing 从 $W_e$ 支持集开始，沿 importance 方向寻找 emission；light tracing 从 $L_e$ 支持集开始，沿 radiance 方向寻找 sensor；bidirectional 方法同时生成两端 subpath 并连接。这个对偶解释了不同策略为何对不同 path family 有完全不同的方差。

## 从积分方程到路径表述

<Term def="把最终测量展开为所有有限长度光路上的积分，并把每条路径贡献分解为发射、散射、几何、可见性和测量因子">路径表述</Term>（path formulation）令路径 $\bar x=(x_0,\ldots,x_k)$ 从 emitter 到 sensor。简化的路径贡献可写为：

$$
f(\bar x)=L_e(x_0\!\to x_1)
\left[\prod_{i=1}^{k-1} f_s(x_{i-1}\!\to x_i\!\to x_{i+1})G(x_i,x_{i+1})V(x_i,x_{i+1})\right]
W_e(x_{k-1}\!\to x_k).
$$

$G$ 包含两端 cosine 与 inverse-square distance，$V$ 是 visibility，$f_s$ 是 GRDF/BSDF。完整测量是对所有 $k$ 的路径流形积分。表面面积与方向两种参数化之间要用 geometry Jacobian 转换；否则连接策略的 PDF 无法在同一 measure 下比较。

路径表示使“困难路径”可精确定义：策略是否能生成它？联合 PDF 多大？delta constraint 是否使某种连接概率为零？哪些顶点可重采样、缓存或 density estimate？这比按“实时/离线”或“新/旧”分类更有解释力。

## 光传输策略矩阵

<Term def="规定从相机、光源或两端生成哪些传输状态，怎样连接、存储、重建并对贡献加权的数值求解方案">光传输策略</Term>（light transport strategy）至少包含 endpoint、state representation、sampling/solver、estimator 和 reconstruction 五项。

<GilPathStrategyDiagram />

**Camera path tracing** 天然只生成到达 sensor 的路径，不浪费在不可见区域，但从 diffuse 顶点随机命中小 emitter 或经 specular chain 找 caustic 的概率低。**Light tracing** 能从小光源高效分发能量，却很难随机命中 pinhole sensor，通常要显式连接 sensor。**Bidirectional tracing** 连接两个 subpath，覆盖更多 path family，但连接数、PDF bookkeeping 与 delta compatibility 更复杂。

**Finite-element/radiosity** 选择空间 basis 并求解系数，适合 diffuse transport 和重用；**density estimation** 在空间邻域聚合 photon，改变了重建域；**caching/interpolation** 在平滑假设成立处复用 indirect illumination。后续章节的算法都能放回这张矩阵，而不只是罗列名字。

## 偏差、方差、一致性与工作量

<GilEstimatorTradeoffDiagram />

<Term def="估计量期望与目标值之差；它描述系统误差，但不等同于有限样本的总误差">估计偏差</Term>为 $b_N=\mathbb E[\widehat I_N]-I$。Mean squared error 分解为：

$$
\operatorname{MSE}(\widehat I_N)=\operatorname{Var}(\widehat I_N)+b_N^2.
$$

Unbiased 只表示 $b_N=0$。Consistent 表示当预算与相关参数按规定增长时，$\widehat I_N\to I$；有偏估计器也可一致。Photon density estimation 需要 photon 数增加同时 kernel radius 收缩；固定半径只增加光子会收敛到模糊解。Irradiance cache 是否一致则取决于 record spacing/error threshold 是否随预算收紧，不能一概断言“永远不一致”。

无偏也不自动保证低 MSE。普通 path tracing 对 caustic 可能有极高 variance；适当的有偏 density estimate 在固定时间更准确。另一方面，denoiser 或 clamp 引入的 bias 可能抹去小而真实的高频能量。策略报告应给出 bias proxy、sample variance、work-normalized error、memory 和 temporal stability。

## 算法分类的执行口径

对一个具体 integrator，逐项回答：

1. **Transport direction**：radiance、importance，还是双向；
2. **State domain**：surface point/direction、path vertex、finite element、photon density 或 cache record；
3. **Generation**：random walk、deterministic quadrature、matrix solve、mutation 或 connection；
4. **Estimator**：sample contribution 与联合 PDF 如何配对，是否 MIS；
5. **Reconstruction**：像素平均、kernel density、basis expansion 或 interpolation；
6. **Limits**：delta path、参与介质、动态场景、memory 与 preprocessing 如何处理。

这套分类会直接暴露重复计数。例如 camera subpath 与 light subpath 的多个连接策略都能生成同一 full path 时，每个 estimator 都要知道所有竞争策略的 path-space density，并用合法权重使权重和为 1。

## 最小路径贡献记录

```cpp
struct PathVertex {
    Point p;
    Normal n;
    Spectrum throughput;
    float pdfForwardArea;
    float pdfReverseArea;
    EventType event; // diffuse, glossy, delta, medium, endpoint
};

Contribution connect(const PathVertex& light, const PathVertex& camera) {
    if (!compatible(light, camera) || !visible(light.p, camera.p)) return {};
    auto value = light.throughput * scatter(light, camera)
               * geometry(light, camera) * scatter(camera, light)
               * camera.throughput;
    return {value, pathSpaceMisWeight(light, camera)};
}
```

Forward/reverse area PDF 是连接和 MIS 的证据，不是可选调试字段。若某顶点是 delta event，普通面积 density 可能不存在，`compatible` 必须阻止不合法连接。

## 三步选择策略

<Stepper>
<Step title="第一步：列出目标路径族">

用 endpoint 与散射类型描述主要贡献，如 `E D D S`、`E S D E`，并标出 small emitter、delta chain、visibility discontinuity。

<GilStepFlowDiagram caption="先说明需要求解哪类路径，而不是先选流行算法。" stage={1} stages={["Classify：endpoint → event chain → dominant path family"]} />

</Step>
<Step title="第二步：推导生成概率与连接">

确定从 radiance 端、importance 端或两端生成；把方向 PDF 转为共同 path-space/area measure，检查 support 和重复策略。

<GilStepFlowDiagram caption="策略能力由能否生成路径及其密度决定。" stage={2} stages={["Classify：endpoint → event chain → dominant path family", "Derive：generation → Jacobian → path density → MIS"]} />

</Step>
<Step title="第三步：比较工作量归一误差">

固定时间/内存，分别测 bias、variance、MSE 与 failure signature，并检查预算增长时参数是否保证 consistency。

<GilStepFlowDiagram caption="以误差和资源曲线验收，而不是只看单张结果图。" stage={3} stages={["Classify：endpoint → event chain → dominant path family", "Derive：generation → Jacobian → path density → MIS", "Validate：bias → variance → consistency → work"]} />

</Step>
</Stepper>

## 常见误区

<Callout type="trap">
**现象 →** 把“ray 反向追踪”直接称为 importance transport。**原因 →** 忽略 adjoint 是相对内积、measure 和 scattering operator 定义的。**修法 →** 写出 $\langle W,KL\rangle=\langle K^*W,L\rangle$ 并检查 reciprocal/非互易条件。
</Callout>

<Callout type="trap">
**现象 →** 宣称“无偏一定质量更高”。**原因 →** 只看 bias，没有按同等工作量比较 variance 与 MSE。**修法 →** 报告 work-normalized MSE 和 failure signature，并说明有限预算目标。
</Callout>

<Callout type="trap">
**现象 →** 将 solid-angle PDF、area PDF 和离散 light PMF 直接相加做 MIS。**原因 →** 不同策略未转换到同一路径测度。**修法 →** 显式推导 Jacobian，delta/continuous strategy 分开处理。
</Callout>

## 练习

<Exercises>

**问题 1：为什么 camera tracing 与 light tracing 可以视作伴随策略？**

<Answer>
前者从 sensor importance 的支持集出发，经伴随传输寻找 emission；后者从 source radiance 的支持集出发，经正向传输寻找 sensor。测量内积满足 $\langle W_e,L\rangle=\langle W,L_e\rangle$，两者估计同一量但从不同端生成随机路径。
</Answer>

**问题 2：路径贡献中的 $G(x_i,x_{i+1})$ 通常包含什么？**

<Answer>
包含两端法线余弦、点间距离平方倒数，以及从方向测度转为表面面积测度的几何关系；visibility 常单独写成 $V$。具体公式取决于 path measure，必须与策略 PDF 的 Jacobian 一致。
</Answer>

**问题 3：有偏方法什么时候仍是一致估计？**

<Answer>
当预算增长时，不仅样本数增加，造成 bias 的离散/重建参数也按合适速率收紧，使 bias 与 variance 都趋零。例如 photon 数增加同时 kernel radius 收缩且邻域样本仍增长。固定半径的 density estimate 不满足该条件。
</Answer>

**问题 4：怎样为 `E-S-D-E` 路径选择策略？**

<Answer>
普通 camera random walk 从 diffuse 顶点随机找到 specular chain 与小光源概率低。可从 emitter 构造 light subpath，再与 camera diffuse vertex 连接，或用 bidirectional strategy；若 specular constraint 使普通连接无效，还需专门 mutation/manifold 类策略。选择依据是 path support 与联合 PDF，而不是算法标签。
</Answer>

</Exercises>

## 小结

第 4 章把后续算法放进同一坐标系：radiance equation 正向传播能量，importance adjoint equation 反向传播测量敏感度，GRDF 统一局部散射，path formulation 把测量展开成各长度路径积分。光传输策略由生成端、状态域、连接、估计与重建共同定义，并必须以 bias、variance、consistency 和 work 四维验收。

<Glossary>
<GlossaryItem term="广义反射分布函数">统一表达局部反射、透射与方向变化的散射核。</GlossaryItem>
<GlossaryItem term="重要度函数">从传感器反向传播、衡量状态对最终测量价值的函数。</GlossaryItem>
<GlossaryItem term="伴随方程">由传输算子内积关系定义的重要度反向传播方程。</GlossaryItem>
<GlossaryItem term="路径表述">把测量写成所有光路积分及顶点因子乘积的形式。</GlossaryItem>
<GlossaryItem term="光传输策略">规定路径生成、连接、存储、重建和加权的数值方案。</GlossaryItem>
<GlossaryItem term="估计偏差">估计量期望与目标值之差。</GlossaryItem>
</Glossary>

<Attribution adaptedFrom="Advanced Global Illumination, Second Edition, Chapter 4: Strategies for Computing Light Transport" adaptedUrl="https://books.google.com/books?id=KodRAAAAMAAJ" />
