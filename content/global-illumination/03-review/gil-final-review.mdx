---
title: "Advanced Global Illumination 2e 总复习"
type: B
section: "计算机图形学与渲染"
order: 9
description: "按第二版 9 章与 3 附录复查物理、概率、路径/辐射度/混合求解、真实感边界、实现与理论证据"
demo: true
math: true
sourceUrl: "https://books.google.com/books?id=KodRAAAAMAAJ"
draft: false
---


## 直觉：总复习不是再列一次方法名

先预测：一张图出现墙角漏光，最先应该增加 SPP、缩小 photon radius、细化 radiosity mesh，还是失效 irradiance cache？没有上下文无法回答。必须先知道贡献由哪个 estimator 生成、存储在哪个 domain、以什么 kernel/validity 重建，再选择实验。

全书主线不是“直接光 → 间接光 → 实时 GI”，而是：定义 realistic image synthesis 与 measurement；建立 radiometry/hemisphere/transport physics；用 probability 与 Monte Carlo 构造数值工具；在 radiance/importance/path formulation 下分类策略；实现 stochastic path tracing、stochastic radiosity 与 hybrid algorithms；最后扩展到 media、subsurface、perception、PRT、speed 和 unresolved issues。


## 十二单元逐项验收

### 1-2 与 B：框架、物理、半球测度

能否解释 scene acquisition → global illumination framework → visual display？能否区分 flux、irradiance、radiance 与 importance？能否从 solid angle $d\omega=\sin\theta d\theta d\phi$ 推出 hemisphere area $2\pi$ 和 $\int\cos\theta d\omega=\pi$？

要求公式量纲、坐标/测度和 display state 都可追溯。只会写 $L_o=L_e+\int fL_i\cos d\omega$，但不知道每项 unit 与 PDF measure，不算通过。

### 3：Monte Carlo 方法

能否从 random variable、PDF/CDF、expectation 和 variance 推导 estimator？能否实现 inversion/rejection sampling、disk/sphere/hemisphere transform，并验证 normalization/histogram？能否区分 importance sampling、stratification、quasi-Monte Carlo 与 Russian roulette 解决的误差？

$$
\widehat I_N=\frac1N\sum_{i=1}^{N}\frac{f(X_i)}{p(X_i)},
\qquad \operatorname{SE}=\frac{\sigma}{\sqrt N}.
$$

### 4：光传输策略

能否写出 radiance equation $L=L_e+KL$ 与 importance adjoint equation $W=W_e+K^*W$？能否解释 measurement $\langle W_e,L\rangle=\langle W,L_e\rangle$？能否将 full path contribution 分解为 endpoint、GRDF/BSDF、geometry、visibility 和 measurement，并把不同 strategy PDF 转到共同 path measure？

是方法选择的最小单位。材质名字或“室内/室外”只提供线索，不能代替 path support 分析。

### 5 与 A：随机路径与类库

能否从 camera ray setup 开始，分别处理 eligible emission、direct/environment/indirect illumination、throughput、PDF 与 roulette？能否用 PathNode 保存 event、beta、forward/reverse density，并让 LightSampler 的 sample/evaluate/PDF 一致？能否说明 light tracing 与 camera tracing 的 endpoint 对偶？

$$
\beta_{i+1}=\beta_i\frac{f_i|n_i\cdot\omega_{i+1}|}{p_i(\omega_{i+1})},
\qquad C_i=\beta_iL_i.
$$

### 6 与 C：随机辐射度和理论

能否推导 $(I-RF)B=E$、form-factor reciprocity $A_iF_{ij}=A_jF_{ji}$？能否用 weighted gather/shoot/random walk 替换完整矩阵，并分析 stochastic relaxation 的 conditional expectation、variance、spectral stability 与 residual？能否分开 basis refinement bias、cluster bias 和 stochastic variance？

必须包含 linear HDR/energy，不是只保存 tone-mapped screenshot。

### 7：Hybrid algorithms

能否说明 final gathering、multipass、BDPT、MLT、irradiance cache、photon mapping、instant radiosity 与 lightcuts 各自复用什么状态？能否用 path-class table 证明没有 duplicate/gap？能否解释 MLT mixing、cache validity、photon kernel 与 VPL hierarchy bound？

$$
\operatorname{MSE}(\widehat I)=\operatorname{Var}(\widehat I)
+\bigl(\mathbb E[\widehat I]-I\bigr)^2.
$$

是混合系统最危险的静默错误。每个 component 看起来都合理，组合却可能过亮。

### 8-9：真实感、速度与结论

能否写出 participating-media RTE 与 BSSRDF 的额外位置维度？能否分离 scene-linear HDR、exposure、tone mapping 和 display encoding？能否说明 PRT 的 basis、transport matrix、fixed dimensions、compression 与 invalidation？能否列出 acquisition、modeling、transport、speed、perception/display、validation 的 unresolved issues？

$$
\mathbf L_o=\mathbf T\mathbf l,
\qquad \text{runtime fast} \Longleftrightarrow
\text{geometry/material transfer fixed or cheaply updated}.
$$

防止只报 frame time，却隐藏预计算和 stale transport。

## 统一方法选择矩阵


选择方法的顺序应是：先识别 dominant path family 与 scene dynamics；再确定 state representation 与 estimator support；然后比较 work-normalized error；最后才考虑工程实现。常见组合示例：

1. **Diffuse static architecture**：hierarchical/stochastic radiosity 或 PRT/cache，重点验收 basis/visibility 与 invalidation；
2. **General dynamic scene**：camera path + NEE/MIS，必要时 BDPT，重点验收 path PDF 与 variance；
3. **Strong caustic**：caustic photon density、BDPT/MLT，重点验收 kernel bias 或 chain mixing；
4. **Participating medium**：volume path sampling，重点验收 transmittance/free-flight/phase PDFs；
5. **Interactive fixed geometry lighting**：PRT/VPL/lightcuts，重点验收 representation frequency、bounds 和 update scope。

方法不是互斥标签。组合时必须用 path partition、MIS 或 proposal/control-variate relation说明责任，不能凭经验把输出相加。

## 从症状回到根因


让最终图像可解释。最低集合包括：

- direct/emission/indirect/caustic/medium component AOV；
- path event sequence 与 per-vertex forward/reverse PDF；
- throughput magnitude、roulette survival 和 MIS weight sum；
- photon/cache/VPL neighbor count、radius、validity/bound；
- linear energy totals、sample mean/variance、runtime/memory；
- tone-map 前后 luminance 与 clipped pixels。

调试实验一次只改变一个维度。亮度随 sampler 改变，先查 PDF/measure；噪声随 samples 不按预期下降，查 heavy tail/correlation；漏光随 radius/cache threshold 改变，查 reconstruction boundary；动态拖影查 invalidation/history；只有显示图错而 HDR 对，查 exposure/tone/color encoding。

## 一套完整验收场景

最小 suite 不追求艺术性，而要隔离 path family：

1. Constant environment + Lambertian plane：PDF/cosine/rotation；
2. Two-patch enclosure：form factor/reciprocity/matrix energy；
3. Cornell box：diffuse multi-bounce/color transfer；
4. Small area light + glossy/delta object：NEE/MIS/specular support；
5. Glass caustic：photon/BDPT/MLT difficult paths；
6. Thin wall/doorway：cache/kernel/cluster boundary；
7. Homogeneous fog：transmittance/single/multiple scattering；
8. Subsurface slab：entry-exit distance/scale；
9. Rotating HDR environment on static object：PRT basis/runtime；
10. HDR chart/display sweep：exposure/tone/color encoding。

每个场景固定 unit scale、spectral/RGB convention、seed/budget 和 reference；否则不同算法比较的不是同一目标。

## 可执行的全书审计接口

```cpp
struct TransportEvidence {
    PathClass path;
    Spectrum contribution;
    double jointPdf;
    double misWeight;
    Error biasProxy, variance;
    Cost trace, query, memory;
    VersionedState cacheOrPrecompute;
};

AuditResult validate(const Scene& scene,
                     const Integrator& candidate,
                     const Reference& linearHdr,
                     const DisplayTransform& fixedDisplay);
```

审计结果既要指出 mean error，也要指出覆盖路径、失败位置和资源交换。一个 90+ 分页面只是教学门槛；全书通过要求 12/12 单元、10/10 本地页和全量 MDX/type/lint 一起成立。

## 三步完成最终复查


## 常见误区


## 练习


## 小结

第二版的完整能力不是记住几种 GI 名称，而是从真实感图像合成链出发，以 radiometry、hemisphere measure、rendering/measurement/adjoint equations 定义问题；用 Monte Carlo、path formulation、finite element、density 和 hybrid state 求解；再把 media、subsurface、PRT、display/perception 与 unresolved issues 纳入同一证据体系。最终验收必须同时证明 12 单元覆盖、方法选择逻辑、失败诊断、linear reference 和资源生命周期。


