---
title: "第7章 并发模式"
type: C
section: "Go实战 · 并发"
order: 7
description: "用Runner控制超时和中断，用Pooling复用有限资源，用Work构建固定goroutine工作池。"
demo: true
math: false
sourceUrl: "https://livebook.manning.com/book/go-in-action/chapter-7"
draft: false
---


## 从并发机制到可复用生命周期开始
先预测：worker阻塞在外部I/O时，仅关闭任务channel能否退出？不能，还需要取消、timeout或关闭底层资源。
并发模式不是更多channel语法，而是对停止和资源责任的封装。。

## Runner：控制程序寿命
Runner顺序执行任务，同时select完成、timeout与interrupt。必须停止接收新工作并等待已启动任务。

```go
select { case <-done: case <-timeout: case <-interrupt: }
```


## Pooling：借出有限资源
Buffered channel保存可用资源，Acquire借出，Release归还。防止池枯竭。

```go
resource := <-pool
defer func(){ pool <- resource }()
```

## 损坏资源与关闭
使用失败的连接不应归还；Pool关闭后拒绝新Acquire并关闭空闲资源。要求并发close幂等。


## Work：固定Goroutine工作池
固定worker从队列消费，限制并发度并Wait。要处理worker panic和生产者提前退出。

```go
for i := 0; i < workers; i++ { go run(jobs, &wg) }
close(jobs); wg.Wait()
```


## 验收实验：在每个边界注入停止
Runner分别注入完成、timeout和interrupt；Pool测试创建失败、使用损坏、重复Release、Close后Acquire；Work测试空队列、队列满、worker panic和生产者提前结束。每次记录谁拥有资源、谁发送停止、谁关闭channel、谁等待完成，确保没有goroutine或句柄遗留。

## 模式选择
。三者可组合，但关闭顺序必须从入口到worker再到资源池。


## 本章回顾
本章覆盖并发模式、Runner、程序生命周期超时与中断、Pooling、可复用资源池、Work和goroutine工作池。三种模式最终都必须回答正常、失败和关闭路径由谁收束，不能只展示启动代码。
## 术语表
