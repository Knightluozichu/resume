---
title: "纹理、压缩、资产与数据表示：8篇复现"
type: B
section: "GPU Pro 1-7 · 几何与管线"
order: 3
description: "以出版社目录和现代GPU证据复现8篇GPU Pro核心技术。"
demo: true
math: true
sourceUrl: "https://www.routledge.com/GPU-Pro-Advanced-Rendering-Techniques-1st-Edition/Engel/p/book/9780429108426"
draft: false
---


## 先预测，再建立证据

先预测：如果只把原篇API名替换成现代接口，哪些数学关系、数据顺序、精度和性能假设可能已经悄悄改变？把virtual texturing、双通道颜色、framebuffer/JPEG/BC6H压缩和glTF放在编码、驻留、传输、解码、消费链中验收。 本页用、、、、固定范围与验收语言。

## 官方范围与唯一归属

来源：[GPU Pro 1 publisher TOC](https://www.routledge.com/GPU-Pro-Advanced-Rendering-Techniques-1st-Edition/Engel/p/book/9780429108426) · [GPU Pro 4 publisher TOC](https://www.routledge.com/GPU-Pro-4-Advanced-Rendering-Techniques/Engel/p/book/9780429165177) · [GPU Pro 5 publisher TOC](https://www.routledge.com/GPU-Pro-5-Advanced-Rendering-Techniques/Engel/p/book/9781482208634) · [GPU Pro 7 publisher TOC](https://www.routledge.com/GPU-Pro-7-Advanced-Rendering-Techniques-1st-Edition/Engel/p/book/9780429160035)。以下原篇只在本主题拥有一个主归属；其他页面可以引用，但不得重复计入覆盖。

### GPU Pro 1 · 2篇

- **GPO-V1-10 · Virtual Texture Mapping**：还原反馈、页表、物理缓存、预取与缺页降级，测页命中和外存流量。
- **GPO-V1-38 · Accelerating Virtual Texturing Using CUDA**：还原反馈、页表、物理缓存、预取与缺页降级，测页命中和外存流量。

### GPU Pro 4 · 2篇

- **GPO-V4-14 · Practical Frame Buffer Compression**：利用空间或格式相关性压缩中间缓冲，并验证随机访问、误差和实时开销。
- **GPO-V4-21 · Real-Time JPEG Compression using DirectCompute**：把颜色变换、块DCT、量化和熵前阶段映射到DirectCompute，使用标准解码结果验收。

### GPU Pro 5 · 2篇

- **GPO-V5-02 · Reducing Texture Memory Usage by 2-Channel Color Encoding**：按颜色空间把信息压到双通道，比较解码误差、相关性和纹理带宽。
- **GPO-V5-22 · glTF: Designing an Open-Standard Runtime Asset Format**：把场景、mesh、材质、动画和扩展组织为开放运行时资产合同，验证往返与兼容。

### GPU Pro 7 · 2篇

- **GPO-V7-07 · Adaptive Virtual Textures**：还原反馈、页表、物理缓存、预取与缺页降级，测页命中和外存流量。
- **GPO-V7-13 · Real-Time BC6H Compression on GPU**：在GPU搜索HDR块端点与索引，比较峰值、块边界、编码时间和硬件解码。

## 核心模型与不变量

把virtual texturing、双通道颜色、framebuffer/JPEG/BC6H压缩和glTF放在编码、驻留、传输、解码、消费链中验收。 先从输入、输出、坐标或数据域、迭代顺序和误差判据写模型，再讨论API迁移。任何优化都必须能指出它保持、近似或放弃了哪条关系。

$$
compressionRatio = sourceBytes / encodedBytes
$$

$$
error = metric(reference, decoded)
$$

$$
workingSet = visiblePages + safetyMargin
$$

$$
value = bandwidthSaved - encodeDecodeCost
$$

公式中的量必须给出范围、单位、空间、精度和生产者。画面相似不代表算法等价；数值一致也不代表带宽、时序和设备边界相同。

## 最小现代探针

```hlsl
encoded = EncodeBlock(sourceTile, qualityMetric);
```

探针只暴露一个变量或阶段。先用常量、解析输入和小规模CPU参考确认正确，再逐步恢复纹理、变体、并行和时间复用。


## 历史载体到现代路径

```text
original: volume + article + API/hardware assumptions
invariant: equation + data dependency + boundary
reference: CPU, analytic, offline, or original trend
modern: shader/compute path + explicit translation
proof: intermediate buffers + image/numeric diff + GPU capture
```

迁移报告必须区分完全等价、受控近似和不可直接迁移。旧式fragment pass、DirectX 9、OpenGL ES、CUDA或OpenCL接口可以变化，但读写重叠、同步、采样边界和精度不能被省略。

## 性能、兼容与失败边界

固定设备、驱动、分辨率、场景、帧阶段和统计窗口，分别报告CPU提交、GPU时间、外存流量、显存峰值和画质误差。对移动、桌面和多GPU路径使用各自真实设备，不用名称推断快慢。

```json
{"source":"official article id","correctness":"reference diff","compatibility":"api and device matrix","performance":"end-to-end metrics","failure":"minimal replay"}
```

1. 固定卷号、篇名、来源、原始平台和输入输出。
2. 写出核心不变量、CPU或离线参考以及现代GPU路径。
3. 构造正常、边界、退化和超容量输入，保存中间证据。
4. 在目标设备重放，记录图像或数值误差和端到端成本。

## 常见误区


## 练习


## 本章回顾

纹理、压缩、资产与数据表示页承载8篇唯一原文；每篇都回到问题、不变量、现代实现、边界和证据。


