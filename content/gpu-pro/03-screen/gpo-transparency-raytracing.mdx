---
title: "透明、体素、光追与稀疏结构：13篇复现"
type: B
section: "GPU Pro 1-7 · 屏幕与稀疏表示"
order: 9
description: "以出版社目录和现代GPU证据复现13篇GPU Pro核心技术。"
demo: true
math: true
sourceUrl: "https://www.routledge.com/GPU-Pro-Advanced-Rendering-Techniques-1st-Edition/Engel/p/book/9780429108426"
draft: true
---

import { Objectives, Callout, Attribution, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step } from "@/components/mdx/mdx-components";
import { GpoTransparencyRaytracingMapLab, GpoTransparencyRaytracingTradeoffLab, GpoTransparencyRaytracingEvidenceLab } from "@/components/mdx/gpu-pro/diagrams/gpo-transparency-raytracing";

<Objectives>
- 能解释Per-Pixel List、Voxelization与Acceleration Structure的生产者到消费者关系
- 能绘制原篇方案、历史GPU载体和现代实现之间的等价或近似边界
- 能判断稀疏证书是否包含正确性、兼容、性能和失败重放证据
</Objectives>

## 先预测，再建立证据

先预测：如果只把原篇API名替换成现代接口，哪些数学关系、数据顺序、精度和性能假设可能已经悄悄改变？A-buffer、fragment list、voxel GI、LBVH、octree、ray tracer和多GPU路径都围绕不规则数据如何在GPU上构建、遍历和有界失败。 本页用<Term def="为每个像素保存多个片元，排序或合成以实现OIT。">Per-Pixel List</Term>、<Term def="把表面或实体离散到规则或稀疏体素结构。">Voxelization</Term>、<Term def="用BVH、octree、grid或bin减少光线与几何测试。">Acceleration Structure</Term>、<Term def="用编码跳转或rope避免每条光线维护显式栈。">Stackless Traversal</Term>、<Term def="检查容量、溢出、遍历完备性、顺序、误差和内存。">稀疏证书</Term>固定范围与验收语言。

<GpoTransparencyRaytracingMapLab />

## 官方范围与唯一归属

来源：[GPU Pro 1 publisher TOC](https://www.routledge.com/GPU-Pro-Advanced-Rendering-Techniques-1st-Edition/Engel/p/book/9780429108426) · [GPU Pro 2 publisher TOC](https://www.routledge.com/product/isbn/9781568817187) · [GPU Pro 3 publisher TOC](https://www.routledge.com/GPU-PRO-3-Advanced-Rendering-Techniques/Engel/p/book/9781439887820) · [GPU Pro 4 publisher TOC](https://www.routledge.com/GPU-Pro-4-Advanced-Rendering-Techniques/Engel/p/book/9780429165177) · [GPU Pro 5 publisher TOC](https://www.routledge.com/GPU-Pro-5-Advanced-Rendering-Techniques/Engel/p/book/9781482208634) · [GPU Pro 6 publisher TOC](https://www.routledge.com/GPU-Pro-6-Advanced-Rendering-Techniques/Engel/p/book/9781482264616) · [GPU Pro 7 publisher TOC](https://www.routledge.com/GPU-Pro-7-Advanced-Rendering-Techniques-1st-Edition/Engel/p/book/9780429160035)。以下原篇只在本主题拥有一个主归属；其他页面可以引用，但不得重复计入覆盖。

### GPU Pro 1 · 1篇

- **GPO-V1-40 · Spatial Binning on the GPU**：构建无指针层级或分桶结构，测构建成本、遍历完备性和最坏占用。

### GPU Pro 2 · 1篇

- **GPO-V2-26 · Order-Independent Transparency using Per-Pixel Linked Lists**：构建逐像素片元链并排序/合成，验证节点池容量、溢出、MSAA和顺序。

### GPU Pro 3 · 2篇

- **GPO-V3-20 · Practical Binary Surface and Solid Voxelization with Direct3D 11**：把表面或实体保守写入体素，检查孔洞、重叠写、分辨率和属性归约。
- **GPO-V3-21 · Interactive Ray Tracing Using the Compute Shader in DirectX 11**：从光线生成进入加速结构与命中着色，比较相干/发散负载和参考交点。

### GPU Pro 4 · 2篇

- **GPO-V4-09 · Progressive Screen-Space Multi-Channel Surface Voxelization**：把表面或实体保守写入体素，检查孔洞、重叠写、分辨率和属性归约。
- **GPO-V4-20 · Bit-Trail Traversal for Stackless LBVH on DirectCompute**：构建无指针层级或分桶结构，测构建成本、遍历完备性和最坏占用。

### GPU Pro 5 · 3篇

- **GPO-V5-01 · Per-Pixel Lists for Single Pass A-Buffer**：构建逐像素片元链并排序/合成，验证节点池容量、溢出、MSAA和顺序。
- **GPO-V5-12 · Screen Space Deformable Meshes via CSG with Per-Pixel Linked Lists**：构建逐像素片元链并排序/合成，验证节点池容量、溢出、MSAA和顺序。
- **GPO-V5-25 · Object-Order Ray Tracing for Fully Dynamic Scenes**：从光线生成进入加速结构与命中着色，比较相干/发散负载和参考交点。

### GPU Pro 6 · 2篇

- **GPO-V6-23 · Rendering Vector Displacement Mapped Surfaces in a GPU Ray Tracer**：从光线生成进入加速结构与命中着色，比较相干/发散负载和参考交点。
- **GPO-V6-25 · Blockwise Linear Binary Grids for Fast Ray Casting Operations**：构建无指针层级或分桶结构，测构建成本、遍历完备性和最坏占用。

### GPU Pro 7 · 2篇

- **GPO-V7-15 · Semi-Static Load Balancing for Low Latency Ray Tracing on Heterogeneous Multiple GPUs**：从光线生成进入加速结构与命中着色，比较相干/发散负载和参考交点。
- **GPO-V7-16 · Octree Mapping from a Depth Camera**：从深度相机融合稀疏octree，验证坐标标定、空闲空间、更新冲突和容量。

## 核心模型与不变量

A-buffer、fragment list、voxel GI、LBVH、octree、ray tracer和多GPU路径都围绕不规则数据如何在GPU上构建、遍历和有界失败。 先从输入、输出、坐标或数据域、迭代顺序和误差判据写模型，再讨论API迁移。任何优化都必须能指出它保持、近似或放弃了哪条关系。

$$
memory = pixels * averageFragments * nodeBytes
$$

$$
rayCost = visitedNodes + primitiveTests
$$

$$
coverage = occupiedVoxels / allocatedVoxels
$$

$$
speedup = serialTime / parallelTime
$$

公式中的量必须给出范围、单位、空间、精度和生产者。画面相似不代表算法等价；数值一致也不代表带宽、时序和设备边界相同。

## 最小现代探针

```hlsl
hit = TraverseAccelerationStructure(ray, nodes, primitives);
```

探针只暴露一个变量或阶段。先用常量、解析输入和小规模CPU参考确认正确，再逐步恢复纹理、变体、并行和时间复用。

<GpoTransparencyRaytracingTradeoffLab />

## 历史载体到现代路径

```text
original: volume + article + API/hardware assumptions
invariant: equation + data dependency + boundary
reference: CPU, analytic, offline, or original trend
modern: shader/compute path + explicit translation
proof: intermediate buffers + image/numeric diff + GPU capture
```

迁移报告必须区分完全等价、受控近似和不可直接迁移。旧式fragment pass、DirectX 9、OpenGL ES、CUDA或OpenCL接口可以变化，但读写重叠、同步、采样边界和精度不能被省略。

## 性能、兼容与失败边界

固定设备、驱动、分辨率、场景、帧阶段和统计窗口，分别报告CPU提交、GPU时间、外存流量、显存峰值和画质误差。对移动、桌面和多GPU路径使用各自真实设备，不用名称推断快慢。

```json
{"source":"official article id","correctness":"reference diff","compatibility":"api and device matrix","performance":"end-to-end metrics","failure":"minimal replay"}
```

<GpoTransparencyRaytracingEvidenceLab />

## 三步复现实验

<Stepper>
  <Step title="还原原篇问题与假设"><GpoTransparencyRaytracingMapLab /></Step>
  <Step title="建立参考与现代双路径"><GpoTransparencyRaytracingTradeoffLab /></Step>
  <Step title="捕获边界、失败和成本"><GpoTransparencyRaytracingEvidenceLab /></Step>
</Stepper>

1. 固定卷号、篇名、来源、原始平台和输入输出。
2. 写出核心不变量、CPU或离线参考以及现代GPU路径。
3. 构造正常、边界、退化和超容量输入，保存中间证据。
4. 在目标设备重放，记录图像或数值误差和端到端成本。

## 常见误区

<Callout type="trap" title="范围或身份错误">按平均片元数分配OIT内存，爆炸或毛发场景发生静默溢出。</Callout>

<Callout type="trap" title="证据或边界缺失">只测相干主光线，忽略次级光线发散和稀疏结构最坏路径。</Callout>

## 练习

<Exercises>
**问题1：如何从Per-Pixel List建立最小可核查实验？**
<Answer>固定官方篇章身份，列出输入、输出和一条核心关系；先用解析或CPU参考验证单阶段，再加入GPU并保存中间缓冲和误差。</Answer>

**问题2：怎样证明现代实现没有偷换Voxelization？**
<Answer>对相同输入比较历史模型与现代路径的数据顺序、精度、边界和中间量；差异必须标记为等价、近似或不支持，并给出容差。</Answer>

**问题3：稀疏证书至少包含什么？**
<Answer>包含来源与版本、参考结果、实现配置、正确性差异、兼容矩阵、GPU捕获、端到端指标以及最小失败配置和修复重放。</Answer>
</Exercises>

## 本章回顾

透明、体素、光追与稀疏结构页承载13篇唯一原文；每篇都回到问题、不变量、现代实现、边界和证据。

<Glossary>
  <GlossaryItem term="Per-Pixel List" definition="为每个像素保存多个片元，排序或合成以实现OIT。" />
  <GlossaryItem term="Voxelization" definition="把表面或实体离散到规则或稀疏体素结构。" />
  <GlossaryItem term="Acceleration Structure" definition="用BVH、octree、grid或bin减少光线与几何测试。" />
  <GlossaryItem term="Stackless Traversal" definition="用编码跳转或rope避免每条光线维护显式栈。" />
  <GlossaryItem term="稀疏证书" definition="检查容量、溢出、遍历完备性、顺序、误差和内存。" />
</Glossary>

<Attribution adaptedFrom="GPU Pro 1-7 official A K Peters/CRC Press tables of contents" adaptedUrl="https://www.routledge.com/GPU-Pro-Advanced-Rendering-Techniques-1st-Edition/Engel/p/book/9780429108426" />
