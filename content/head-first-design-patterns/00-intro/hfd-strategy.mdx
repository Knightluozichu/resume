---
title: 策略模式
type: B
section: 模式入门
order: 1
description: 把行为抽成接口，用组合注入代替继承——运行时可替换的算法封装。
demo: true
math: false
sourceUrl: "https://www.oreilly.com/library/view/head-first-design/9781492077992/"
draft: false
---

import { HfdStrategyDiagram } from "@/components/mdx/head-first-design-patterns/diagrams/hfd-strategy";
import {
  OfficialHeadFirstDesignPatternsHfdStrategyMapLab,
  OfficialHeadFirstDesignPatternsHfdStrategyExperimentLab,
  OfficialHeadFirstDesignPatternsHfdStrategyEvidenceLab,
} from "@/components/mdx/head-first-design-patterns/diagrams/hfd-strategy";
import {
  Objectives,
  Callout,
  Attribution,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
} from "@/components/mdx/mdx-components";

{/* OFFICIAL_COURSE_ENHANCEMENT */}

<Objectives>

- 能解释策略模式的输入、核心结构、状态变化和输出合同
- 能绘制并修改策略模式的最小模型，比较至少一种替代方案
- 能复现正常、边界和单故障样本，定位第一条证据分叉
- 能根据成本与风险判断方案适用范围，并完成清理重建

</Objectives>

## 为什么只记结论不足以掌握策略模式

学习“策略模式”时，第一步不是记住结论，而是冻结输入、上下文、版本和成功标准。只有这些条件明确，第1章 设计模式入门的含义才不会随着样例变化。正文已有的概念说明要与结构图、运行轨迹和失败样本互相印证，不能只凭最终输出看似正确就宣布完成。

结构分析从封装变化开始：列出参与者、职责、连接方向、生命周期和所有权，再沿正常路径追踪数据或控制流。每一条边都要说明为什么存在、谁创建、谁消费、谁负责清理；如果边界被跨越，必须能在证据中找到第一处异常。

机制验证要把组合优于继承写成可以执行的条件。正常样本证明主路径，恰好边界样本验证等号和空值，单故障样本只破坏一个假设。三类样本使用同一份观察指标，避免因为测试口径变化而把偶然结果误认成规律。

成本分析同时记录时间、空间、延迟、耦合、可维护性和不可逆操作。策略接口不是一句“可能失败”，而是可复现输入、预期停点、实际轨迹、错误分类与清理步骤。任何自动重试都要有次数、预算和幂等边界。

方案比较不能只列优点。需要给出直接实现、当前方案和至少一个替代方案，逐项比较复杂度、扩展点、故障隔离和团队认知成本。当问题规模很小或变化轴稳定时，更简单的实现往往更好；模式与框架必须由真实变化压力证明。

实现阶段把大结论拆成可检查的中间产物：配置快照、结构清单、状态转移、输入输出样本、日志摘要和测试结果。每个产物带来源与生成命令，下一阶段只消费已通过门禁的版本，避免旧缓存或隐式默认值污染结论。

解释结果时必须区分相关性与因果性、接口承诺与实现细节、设计意图与运行事实。对“运行时替换”的判断要由独立证据支持，并明确适用范围；一旦输入分布、版本、硬件或组织边界改变，就重新运行最小实验。

复盘从首个分叉开始，而不是从最后一个报错倒推。先比较冻结输入，再比较第一份结构化中间产物，随后检查状态、约束和副作用。这样可以把复杂系统的排错范围收缩到一个阶段，避免在多个层次同时修改造成新的不确定性。

迁移到真实项目时，先选择一个最小但有代表性的切片，保存改造前基线，再逐步引入“策略模式”中的机制。每一步只改变一个变量并保留回滚点；性能、正确性、安全性和可理解性至少各有一项可量化指标。

最终验收要求读者能脱离页面重新画出结构、口述关键链路、实现最小版本、构造一个反例并解释失败位置。若只能复述名词而不能预测中间状态，说明知识仍停留在识记层，需要回到图示和实验重新验证。

本页用<Term def="第1章 设计模式入门连接策略模式的机制、边界与可复查证据。">第1章 设计模式入门</Term>、

<Term def="封装变化连接策略模式的机制、边界与可复查证据。">封装变化</Term>、<Term def="组合优于继承连接策略模式的机制、边界与可复查证据。">组合优于继承</Term>、<Term def="策略接口连接策略模式的机制、边界与可复查证据。">策略接口</Term>、<Term def="运行时替换连接策略模式的机制、边界与可复查证据。">运行时替换</Term>
建立统一坐标。先预测这些概念在结构图和运行轨迹中的位置，再操作实验控件；如果结果与预测不一致，停止在首个分叉，不要用后续补丁掩盖早期错误。

## 权威目录与核心概念逐项对照

- 第1章 设计模式入门
- 封装变化
- 组合优于继承
- 策略接口
- 运行时替换
- 共享模式词汇

## 三视图实验：结构、变量与证据

<Stepper>
  <Step title="第一步：冻结结构并先预测">
    先预测参与者、连接方向与关键状态，再点击结构节点核对。
    <OfficialHeadFirstDesignPatternsHfdStrategyMapLab />
  </Step>
  <Step title="第二步：只改变一个变量">
    保持其他条件不变，移动覆盖率或风险滑块，观察门禁变化。
    <OfficialHeadFirstDesignPatternsHfdStrategyExperimentLab />
  </Step>
  <Step title="第三步：注入单故障并保存证据">
    注入一个边界故障，记录第一处偏离、拒绝原因和恢复条件。
    <OfficialHeadFirstDesignPatternsHfdStrategyEvidenceLab />
  </Step>
</Stepper>

<Callout type="trap">
  <strong>误区：</strong>
  看到最终结果正确就认为机制正确。中间状态可能已经偏离，只是被后续默认值或重试偶然修补。应保存首个结构化产物并逐阶段比较。
</Callout>

<Callout type="trap">
  <strong>误区：</strong>
  为了使用模式或框架而制造抽象。若变化轴、失败边界和替代方案没有写清，新增层次只会扩大认知成本和故障面。
</Callout>

## 可复现的最小实现

先把决策记录写成机器可读结构：

```json
{
  "unit": "策略模式",
  "inputFrozen": true,
  "scenario": "normal | boundary | single-fault",
  "firstDivergence": null,
  "cleanupRequired": true
}
```

再用同一条执行链处理三类样本：

```ts
type Evidence = { stage: string; expected: string; actual: string };

function verify(sample: unknown, expected: readonly Evidence[]) {
  const trace = runFromCleanState(sample);
  return expected.find((item, index) => trace[index]?.actual !== item.expected);
}
```

最后保存回归门禁，禁止失败样本静默通过：

```text
normal      -> complete, invariant preserved
boundary    -> complete or explicit rejection
singleFault -> stop at first divergence, no stale output
```

用统一评分解释实验结果：

$$
Q = C_{correct} + C_{trace} + C_{recover} - R_{residual}
$$

$$
C_{coverage} = \frac{N_{verified}}{N_{official}}
$$

$$
R_{residual} = P(failure) \times I(impact)
$$

$$
Accept = (C_{coverage} \ge 0.90) \land (R_{residual} \le R_{budget})
$$

## 本章回顾

- 第1章 设计模式入门必须绑定冻结输入和明确成功标准。
- 封装变化必须能画成结构并沿边追踪责任。
- 组合优于继承要由正常、边界和单故障样本共同验证。
- 策略接口必须保存第一处偏离和清理重建步骤。
- 运行时替换决定方案是否可以进入下一阶段。

<Exercises>

**问题 1**：为“策略模式”写出最小输入合同，并指出一个不能省略的边界条件。

<Answer>
  输入合同至少包含样本身份、版本、配置、成功标准和清理方式；边界条件必须能改变结构或状态，而不是只换一段描述。
</Answer>

**问题 2**：构造一个只破坏“组合优于继承”的单故障样本，第一条预期证据是什么？

<Answer>
  先冻结正常轨迹，只改变该条件；第一条证据应是最早依赖它的结构化中间产物，而不是最终错误消息。
</Answer>

**问题 3**：什么情况下不应采用本页方案？

<Answer>
  当变化轴稳定、直接实现更清晰、额外层次无法降低风险或提供独立测试边界时，应选择更简单方案并记录重新评估触发条件。
</Answer>

</Exercises>

## 术语表

<Glossary>
  <GlossaryItem term="第1章 设计模式入门">
    第1章
    设计模式入门用于描述“策略模式”中的输入、结构、约束或证据边界，必须能在实验记录中定位。
  </GlossaryItem>
  <GlossaryItem term="封装变化">
    封装变化用于描述“策略模式”中的输入、结构、约束或证据边界，必须能在实验记录中定位。
  </GlossaryItem>
  <GlossaryItem term="组合优于继承">
    组合优于继承用于描述“策略模式”中的输入、结构、约束或证据边界，必须能在实验记录中定位。
  </GlossaryItem>
  <GlossaryItem term="策略接口">
    策略接口用于描述“策略模式”中的输入、结构、约束或证据边界，必须能在实验记录中定位。
  </GlossaryItem>
  <GlossaryItem term="运行时替换">
    运行时替换用于描述“策略模式”中的输入、结构、约束或证据边界，必须能在实验记录中定位。
  </GlossaryItem>
</Glossary>

<Attribution
  adaptedFrom="Eric Freeman、Elisabeth Robson著《Head First Design Patterns, 2nd Edition》，O'Reilly Media，2020，672页，ISBN 9781492077992 · 独立教学重构"
  adaptedUrl="https://www.oreilly.com/library/view/head-first-design/9781492077992/"
/>

<Objectives>

- 能用自己的话解释策略模式是什么、为什么需要它
- 能看懂并独立修改策略模式的 Java 代码
- 能回答：策略模式和直接用 if-else 各自适合什么场景？

</Objectives>

## 为什么需要策略模式

想象你在开发一个鸭子游戏。最初只有绿头鸭和红头鸭，它们都能飞、能叫。你写了一个 Duck 父类，把 `fly()` 和 `quack()` 放在里面，子类继承即可。一切看起来很美好。

直到产品加了橡皮鸭——它不会飞。你只好在 RubberDuck 里覆盖 `fly()` 成空方法。然后加了诱饵鸭——它既不会飞也不会叫。再然后加了火箭鸭——它会火箭飞行。你发现每加一种鸭子，就要改父类或覆盖方法，飞行和叫声的行为在继承体系里越搅越乱。

问题的根源是：**行为被硬编码在继承体系中**。飞行方式和鸭子类型绑死了。改一种行为要波及所有鸭子，新增行为要修改一堆子类。

## 概念讲解

策略模式的思路是：**把行为抽成接口，鸭子用组合持有行为引用**。

<Term def="声明所有具体策略共有的方法，是客户端面向的抽象">Strategy 接口</Term>
定义了行为的标准接口（如
`fly()`）。每种具体行为是一个独立的实现类——`FlyWithWings`、`FlyNoWay`、`FlyRocketPowered`。

<Term def="持有 Strategy 引用，把行为调用委托给当前策略对象，不关心具体实现">Context（上下文）</Term>
就是 Duck。它持有 `FlyBehavior` 和 `QuackBehavior` 两个接口引用，`performFly()`
委托给 `flyBehavior.fly()`。Duck 不关心具体怎么飞，只管调接口。

<HfdStrategyDiagram />

关键转变：从「继承获得行为」到「组合注入行为」。Duck 不再继承飞行行为，而是持有一个飞行行为对象。setter 方法让行为可以在运行时替换——一只鸭子可以从「不会飞」变成「火箭飞行」。

## 交互Demo

<Stepper>
  <Step title="第一步：问题——继承方案">
    Duck 父类写了
    `fly()`，所有子类继承。橡皮鸭不会飞，只能覆盖成空方法。火箭鸭要覆盖成火箭飞行。每加一种行为变体，就要修改子类——违反开闭原则。
  </Step>
  <Step title="第二步：抽出接口">
    把飞行行为抽成 `FlyBehavior` 接口，声明 `fly()` 方法。把叫声行为抽成
    `QuackBehavior` 接口，声明 `quack()` 方法。行为不再是 Duck
    的一部分，而是独立的类。
  </Step>
  <Step title="第三步：具体策略实现">
    为每种行为创建实现类：`FlyWithWings`（用翅膀飞）、`FlyNoWay`（不会飞）、`FlyRocketPowered`（火箭飞行）。每个类封装一种行为变体，互不影响。
  </Step>
  <Step title="第四步：组合注入">
    Duck 持有 `FlyBehavior` 和 `QuackBehavior` 引用。构造时传入具体策略，或用
    setter 运行时替换。`performFly()` 委托给 `flyBehavior.fly()`——Duck
    只管调接口，不关心具体怎么飞。
  </Step>
  <Step title="第五步：运行时切换">
    调用 `setFlyBehavior(new FlyRocketPowered())`
    即可让一只原本不会飞的鸭子获得火箭飞行能力。行为在运行时动态切换，无需改
    Duck 代码。
  </Step>
</Stepper>

## 代码对照

```java
// 1. 策略接口
public interface FlyBehavior {
    void fly();
}

// 2. 具体策略
public class FlyWithWings implements FlyBehavior {
    public void fly() { System.out.println("用翅膀飞"); }
}
public class FlyRocketPowered implements FlyBehavior {
    public void fly() { System.out.println("火箭推进飞行"); }
}

// 3. Context——用组合持有策略
public abstract class Duck {
    protected FlyBehavior flyBehavior;

    public void setFlyBehavior(FlyBehavior fb) {
        this.flyBehavior = fb;
    }
    public void performFly() { flyBehavior.fly(); }
}

// 4. 使用——运行时切换行为
Duck model = new ModelDuck();
model.performFly();  // 最初不会飞
model.setFlyBehavior(new FlyRocketPowered());
model.performFly();  // 火箭飞行！
```

Duck 把 `fly()` 委托给 `flyBehavior` 对象。新增飞行方式只需加实现类，Duck 一行不改——符合「多用组合少用继承」。

## 常见误区

<Callout type="trap">
  <strong>现象 →</strong> 只有两个分支且永不变化，却硬套策略模式，代码多了 5
  个类。 <strong>原因 →</strong> 过度设计。策略模式解决的是「算法变体多 +
  需运行时切换 + 需隔离变化」，不是「消灭所有 if-else」。{" "}
  <strong>修法 →</strong> 分支少且稳定时用 if-else 更直白。当分支超过 3
  个且会持续增加时，再重构到策略模式。
</Callout>

## 小结与练习

- 策略模式把行为封装成接口，Context 用组合持有策略引用，运行时可替换
- 三要素：Strategy 接口、ConcreteStrategy 具体策略、Context 上下文
- 核心原则：多用组合少用继承——行为用组合注入，不用继承硬编码
- 适用场景：算法变体多、需运行时切换、需隔离算法变化

<Exercises>
  <Answer question="策略模式比继承方案好在哪里？用「多用组合少用继承」原则解释。">
    继承把行为写死在父类，所有子类共享，改一个行为波及所有子类，且无法运行时切换。策略模式用组合代替继承——Duck
    持有 FlyBehavior 引用，行为是独立对象，可运行时用 setter
    替换，新增行为只需加实现类不改
    Duck。组合比继承灵活：可多维度组合（飞行+叫声各自独立）、可运行时切换、可复用（多个鸭子共享同一策略对象）。
  </Answer>
  <Answer question="什么时候不该用策略模式？">
    分支少（2-3 个）且稳定不变化时，if-else
    更直白易读。策略模式增加了类数量和理解成本，简单问题用简单方案。判断信号：只有两个永不变的分支；策略类里只是包了一层壳但没有独立逻辑；为「可能将来会变」但实际从不变化的需求提前引入——这些都是过度设计。
  </Answer>
</Exercises>

## 名词解释

<Glossary>
  <GlossaryItem term="Strategy 接口">
    声明所有具体策略共有的方法（如
    fly()），是客户端面向的抽象。客户端不依赖具体策略类，只依赖接口。
  </GlossaryItem>
  <GlossaryItem term="ConcreteStrategy">
    实现 Strategy
    接口的具体算法类，每个类封装一种行为变体。新增行为只需新增实现类。
  </GlossaryItem>
  <GlossaryItem term="Context">
    策略模式的上下文对象，持有 Strategy 引用，把行为调用委托给当前策略。Context
    只和接口对话，不关心具体实现。
  </GlossaryItem>
  <GlossaryItem term="组合优于继承">
    设计原则：行为通过组合（持有引用）注入对象，而非通过继承硬编码在父类。组合可在运行时切换，继承在编译期固定。
  </GlossaryItem>
</Glossary>

<Attribution
  adaptedFrom="Head First Design Patterns (Eric Freeman & Elisabeth Robson)"
  adaptedUrl="https://www.oreilly.com/library/view/head-first-design/0596007124/"
/>
