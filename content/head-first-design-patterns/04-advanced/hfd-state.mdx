---
title: 状态模式
type: C
section: 状态驱动行为
order: 10
description: 把状态相关行为分散到状态对象，用显式转移替代不断膨胀的条件分支。
demo: true
math: false
sourceUrl: "https://www.oreilly.com/library/view/head-first-design/9781492077992/"
draft: false
---

import { HfdStateDiagram } from "@/components/mdx/head-first-design-patterns/diagrams/hfd-state";
import {
  Objectives,
  Callout,
  Attribution,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能用自己的话解释状态模式是什么、为什么需要它
- 能看懂并独立修改状态模式的 Java 代码
- 能回答：状态模式和策略模式的本质区别是什么？什么时候该用状态模式？

</Objectives>

## 为什么需要状态模式

你在开发一个口香糖机。它的工作流程是：没投币时只能投币；投了币可以转把手或退币；转了把手就发糖；糖卖光了就拒绝服务。最初你用一堆 `if-else` 或 `switch` 根据当前状态判断该做什么：

```java
public void insertQuarter() {
    if (state == HAS_QUARTER) {
        System.out.println("已经投过硬币了");
    } else if (state == NO_QUARTER) {
        state = HAS_QUARTER;
        System.out.println("投币成功");
    } else if (state == SOLD_OUT) { ... }
    else if (state == SOLD) { ... }
}
```

每个动作方法（投币、退币、转把手、发糖）都要写一遍四状态的判断。加一个新状态（比如「中奖状态」），要改所有方法里的分支——容易漏、容易错。

问题的根源是：**状态判断散落在各个方法里**。状态越多，条件分支越膨胀，行为越难维护。

## 概念讲解

状态模式的思路是：**把每种状态的行为封装到独立的状态对象里，Context 持有当前状态引用，把行为委托出去**。

<Term def="持有当前 State 引用，把行为委托给当前状态对象，状态转移时切换引用">Context（上下文）</Term>
是 GumballMachine。它不再用 if-else 判断状态，而是把 `insertQuarter()` 委托给
`state.insertQuarter()`。状态转移就是把 `state` 引用换成另一个状态对象。

<Term def="定义所有状态共有的行为接口，每个具体状态实现自己在该状态下的行为">State（状态接口）</Term>
声明 `insertQuarter()`、`ejectQuarter()`、`turnCrank()`、`dispense()`
等方法。每个具体状态只实现「自己这个状态下」该做什么——NoQuarterState 的
`insertQuarter()` 是接受投币，HasQuarterState 的 `insertQuarter()` 是拒绝重复投币。

<Term def="状态转移通过切换 Context 的 state 引用实现，显式且集中">状态转移</Term>
不再散落在条件分支里，而是集中在状态对象内部：NoQuarterState 收到投币后，调
`context.setState(hasQuarterState)` 完成转移。转移逻辑和状态行为写在一起，清晰可控。

<HfdStateDiagram />

关键转变：从「Context 根据状态判断行为」到「状态对象自己决定行为」。Context 变简单了（只委托），每个状态对象内聚了「这个状态下该做什么 + 该转移到哪」。加新状态只需新增一个状态类，Context 和其他状态零改动。

## 代码对照

```java
// 1. State 接口
public interface State {
    void insertQuarter();
    void ejectQuarter();
    void turnCrank();
    void dispense();
}

// 2. Context——持有状态引用，委托行为
public class GumballMachine {
    State noQuarterState = new NoQuarterState(this);
    State hasQuarterState = new HasQuarterState(this);
    State soldState = new SoldState(this);
    State state = noQuarterState;  // 当前状态

    public void insertQuarter() { state.insertQuarter(); }  // 委托
    public void turnCrank() { state.turnCrank(); }
    public void setState(State s) { this.state = s; }       // 状态转移
}

// 3. 具体状态——行为 + 转移写在一起
public class NoQuarterState implements State {
    GumballMachine machine;
    public NoQuarterState(GumballMachine m) { machine = m; }

    public void insertQuarter() {
        System.out.println("投币成功");
        machine.setState(machine.getHasQuarterState());  // 转移
    }
    public void turnCrank() {
        System.out.println("还没投币呢");  // 此状态下不合法
    }
    // ...
}
```

Context 只负责委托和切换状态引用，每个状态对象内聚自己的行为——条件分支消失了。

## 常见误区

<Callout type="trap">
  <strong>现象 →</strong> 状态对象里又出现大量 if-else 判断其他状态，状态之间互相调用乱成一团。{" "}
  <strong>原因 →</strong> 状态转移职责没放对位置——要么转移逻辑散落在 Context，要么状态对象越权操作别的状态。{" "}
  <strong>修法 →</strong> 转移逻辑集中在当前状态对象内部（它最清楚自己该往哪转），Context 只提供
  setState()。状态对象之间不直接互相调用，只通过 Context 切换引用。
</Callout>

## 小结与练习

- 状态模式把状态相关行为分散到独立状态对象，Context 持有当前状态引用并委托行为
- 状态转移 = 切换 Context 的 state 引用，显式且集中
- 加新状态只需新增状态类，Context 和其他状态零改动——替代膨胀的 if-else
- 适用场景：对象行为依赖内部状态，且状态多、转移复杂、会持续增加

<Exercises>
  <Answer question="状态模式和策略模式结构几乎一样（Context 持有接口引用并委托），本质区别是什么？">
    意图和转移机制不同。状态模式：对象行为随内部状态自动切换，状态转移由状态对象或 Context
    主动完成（投币后自动切到 HasQuarter），客户端通常不感知状态切换——状态是「对象自身演化的」。策略模式：算法由客户端主动选择和注入，运行时可任意切换，算法之间没有转移关系——策略是「外部挑选的」。判断：行为随对象内部状态自动演化用状态模式；算法由外部选择替换用策略模式。口香糖机的状态会自己流转，所以是状态模式；鸭子的飞行行为由外部设定，所以是策略模式。
  </Answer>
  <Answer question="为什么状态模式能消除 if-else 膨胀？代价是什么？">
    膨胀的根源是「每个动作方法都要判断所有状态」。状态模式把这个矩阵转置：每个状态对象只实现「该状态下的所有动作」，判断从「方法×状态」的二维分支变成「一个状态一个类」的一维组织。加状态从「改所有方法」变成「加一个类」。代价：1.类数量增多（N
    个状态 = N 个类）；2.状态对象通常需要了解 Context（持有引用）有一定耦合；3.状态转移逻辑分散在各状态对象里，想看全局转移图要读多个类。状态少且稳定时 if-else 更直白；状态多、转移复杂、会持续增加时状态模式才划算。
  </Answer>
</Exercises>

<Glossary>
  <GlossaryItem term="Context（上下文）">
    持有当前 State 引用，把行为委托给当前状态对象。状态转移时切换引用，自身不做状态判断。
  </GlossaryItem>
  <GlossaryItem term="State（状态接口）">
    定义所有状态共有的行为接口。每个具体状态实现「该状态下」的行为，不合法的操作给出恰当响应。
  </GlossaryItem>
  <GlossaryItem term="状态转移">
    通过切换 Context 的 state 引用实现。转移逻辑集中在当前状态对象内部，显式且可控。
  </GlossaryItem>
  <GlossaryItem term="状态模式 vs 策略模式">
    结构相似但意图不同：状态随对象内部自动演化（状态模式），算法由外部主动选择（策略模式）。
  </GlossaryItem>
</Glossary>

<Attribution
  adaptedFrom="Eric Freeman、Elisabeth Robson著《Head First Design Patterns, 2nd Edition》，O'Reilly Media，2020，672页，ISBN 9781492077992 · 独立教学重构"
  adaptedUrl="https://www.oreilly.com/library/view/head-first-design/9781492077992/"
/>
