---
title: 模板方法
type: B
section: 模式总结
order: 8
description: 父类定义算法骨架，子类实现具体步骤——好莱坞原则「别调用我们，我们会调用你」。
demo: true
math: false
sourceUrl: "https://www.oreilly.com/library/view/head-first-design/9781492077992/"
draft: false
---

import { HfdTemplateMethodDiagram } from "@/components/mdx/head-first-design-patterns/diagrams/hfd-template-method";
import {
  OfficialHeadFirstDesignPatternsHfdTemplateMethodMapLab,
  OfficialHeadFirstDesignPatternsHfdTemplateMethodExperimentLab,
  OfficialHeadFirstDesignPatternsHfdTemplateMethodEvidenceLab,
} from "@/components/mdx/head-first-design-patterns/diagrams/hfd-template-method";
import {
  Objectives,
  Callout,
  Attribution,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
} from "@/components/mdx/mdx-components";

{/* OFFICIAL_COURSE_ENHANCEMENT */}

<Objectives>

- 能解释模板方法的输入、核心结构、状态变化和输出合同
- 能绘制并修改模板方法的最小模型，比较至少一种替代方案
- 能复现正常、边界和单故障样本，定位第一条证据分叉
- 能根据成本与风险判断方案适用范围，并完成清理重建

</Objectives>

## 为什么只记结论不足以掌握模板方法

学习“模板方法”时，第一步不是记住结论，而是冻结输入、上下文、版本和成功标准。只有这些条件明确，第8章 模板方法模式的含义才不会随着样例变化。正文已有的概念说明要与结构图、运行轨迹和失败样本互相印证，不能只凭最终输出看似正确就宣布完成。

结构分析从算法骨架开始：列出参与者、职责、连接方向、生命周期和所有权，再沿正常路径追踪数据或控制流。每一条边都要说明为什么存在、谁创建、谁消费、谁负责清理；如果边界被跨越，必须能在证据中找到第一处异常。

机制验证要把原语操作写成可以执行的条件。正常样本证明主路径，恰好边界样本验证等号和空值，单故障样本只破坏一个假设。三类样本使用同一份观察指标，避免因为测试口径变化而把偶然结果误认成规律。

成本分析同时记录时间、空间、延迟、耦合、可维护性和不可逆操作。钩子不是一句“可能失败”，而是可复现输入、预期停点、实际轨迹、错误分类与清理步骤。任何自动重试都要有次数、预算和幂等边界。

方案比较不能只列优点。需要给出直接实现、当前方案和至少一个替代方案，逐项比较复杂度、扩展点、故障隔离和团队认知成本。当问题规模很小或变化轴稳定时，更简单的实现往往更好；模式与框架必须由真实变化压力证明。

实现阶段把大结论拆成可检查的中间产物：配置快照、结构清单、状态转移、输入输出样本、日志摘要和测试结果。每个产物带来源与生成命令，下一阶段只消费已通过门禁的版本，避免旧缓存或隐式默认值污染结论。

解释结果时必须区分相关性与因果性、接口承诺与实现细节、设计意图与运行事实。对“好莱坞原则”的判断要由独立证据支持，并明确适用范围；一旦输入分布、版本、硬件或组织边界改变，就重新运行最小实验。

复盘从首个分叉开始，而不是从最后一个报错倒推。先比较冻结输入，再比较第一份结构化中间产物，随后检查状态、约束和副作用。这样可以把复杂系统的排错范围收缩到一个阶段，避免在多个层次同时修改造成新的不确定性。

迁移到真实项目时，先选择一个最小但有代表性的切片，保存改造前基线，再逐步引入“模板方法”中的机制。每一步只改变一个变量并保留回滚点；性能、正确性、安全性和可理解性至少各有一项可量化指标。

最终验收要求读者能脱离页面重新画出结构、口述关键链路、实现最小版本、构造一个反例并解释失败位置。若只能复述名词而不能预测中间状态，说明知识仍停留在识记层，需要回到图示和实验重新验证。

本页用<Term def="第8章 模板方法模式连接模板方法的机制、边界与可复查证据。">第8章 模板方法模式</Term>、

<Term def="算法骨架连接模板方法的机制、边界与可复查证据。">算法骨架</Term>、<Term def="原语操作连接模板方法的机制、边界与可复查证据。">原语操作</Term>、<Term def="钩子连接模板方法的机制、边界与可复查证据。">钩子</Term>、<Term def="好莱坞原则连接模板方法的机制、边界与可复查证据。">好莱坞原则</Term>
建立统一坐标。先预测这些概念在结构图和运行轨迹中的位置，再操作实验控件；如果结果与预测不一致，停止在首个分叉，不要用后续补丁掩盖早期错误。

## 权威目录与核心概念逐项对照

- 第8章 模板方法模式
- 算法骨架
- 原语操作
- 钩子
- 好莱坞原则
- 框架控制反转

## 三视图实验：结构、变量与证据

<Stepper>
  <Step title="第一步：冻结结构并先预测">
    先预测参与者、连接方向与关键状态，再点击结构节点核对。
    <OfficialHeadFirstDesignPatternsHfdTemplateMethodMapLab />
  </Step>
  <Step title="第二步：只改变一个变量">
    保持其他条件不变，移动覆盖率或风险滑块，观察门禁变化。
    <OfficialHeadFirstDesignPatternsHfdTemplateMethodExperimentLab />
  </Step>
  <Step title="第三步：注入单故障并保存证据">
    注入一个边界故障，记录第一处偏离、拒绝原因和恢复条件。
    <OfficialHeadFirstDesignPatternsHfdTemplateMethodEvidenceLab />
  </Step>
</Stepper>

<Callout type="trap">
  <strong>误区：</strong>
  看到最终结果正确就认为机制正确。中间状态可能已经偏离，只是被后续默认值或重试偶然修补。应保存首个结构化产物并逐阶段比较。
</Callout>

<Callout type="trap">
  <strong>误区：</strong>
  为了使用模式或框架而制造抽象。若变化轴、失败边界和替代方案没有写清，新增层次只会扩大认知成本和故障面。
</Callout>

## 可复现的最小实现

先把决策记录写成机器可读结构：

```json
{
  "unit": "模板方法",
  "inputFrozen": true,
  "scenario": "normal | boundary | single-fault",
  "firstDivergence": null,
  "cleanupRequired": true
}
```

再用同一条执行链处理三类样本：

```ts
type Evidence = { stage: string; expected: string; actual: string };

function verify(sample: unknown, expected: readonly Evidence[]) {
  const trace = runFromCleanState(sample);
  return expected.find((item, index) => trace[index]?.actual !== item.expected);
}
```

最后保存回归门禁，禁止失败样本静默通过：

```text
normal      -> complete, invariant preserved
boundary    -> complete or explicit rejection
singleFault -> stop at first divergence, no stale output
```

用统一评分解释实验结果：

$$
Q = C_{correct} + C_{trace} + C_{recover} - R_{residual}
$$

$$
C_{coverage} = \frac{N_{verified}}{N_{official}}
$$

$$
R_{residual} = P(failure) \times I(impact)
$$

$$
Accept = (C_{coverage} \ge 0.90) \land (R_{residual} \le R_{budget})
$$

## 本章回顾

- 第8章 模板方法模式必须绑定冻结输入和明确成功标准。
- 算法骨架必须能画成结构并沿边追踪责任。
- 原语操作要由正常、边界和单故障样本共同验证。
- 钩子必须保存第一处偏离和清理重建步骤。
- 好莱坞原则决定方案是否可以进入下一阶段。

<Exercises>

**问题 1**：为“模板方法”写出最小输入合同，并指出一个不能省略的边界条件。

<Answer>
  输入合同至少包含样本身份、版本、配置、成功标准和清理方式；边界条件必须能改变结构或状态，而不是只换一段描述。
</Answer>

**问题 2**：构造一个只破坏“原语操作”的单故障样本，第一条预期证据是什么？

<Answer>
  先冻结正常轨迹，只改变该条件；第一条证据应是最早依赖它的结构化中间产物，而不是最终错误消息。
</Answer>

**问题 3**：什么情况下不应采用本页方案？

<Answer>
  当变化轴稳定、直接实现更清晰、额外层次无法降低风险或提供独立测试边界时，应选择更简单方案并记录重新评估触发条件。
</Answer>

</Exercises>

## 术语表

<Glossary>
  <GlossaryItem term="第8章 模板方法模式">
    第8章
    模板方法模式用于描述“模板方法”中的输入、结构、约束或证据边界，必须能在实验记录中定位。
  </GlossaryItem>
  <GlossaryItem term="算法骨架">
    算法骨架用于描述“模板方法”中的输入、结构、约束或证据边界，必须能在实验记录中定位。
  </GlossaryItem>
  <GlossaryItem term="原语操作">
    原语操作用于描述“模板方法”中的输入、结构、约束或证据边界，必须能在实验记录中定位。
  </GlossaryItem>
  <GlossaryItem term="钩子">
    钩子用于描述“模板方法”中的输入、结构、约束或证据边界，必须能在实验记录中定位。
  </GlossaryItem>
  <GlossaryItem term="好莱坞原则">
    好莱坞原则用于描述“模板方法”中的输入、结构、约束或证据边界，必须能在实验记录中定位。
  </GlossaryItem>
</Glossary>

<Attribution
  adaptedFrom="Eric Freeman、Elisabeth Robson著《Head First Design Patterns, 2nd Edition》，O'Reilly Media，2020，672页，ISBN 9781492077992 · 独立教学重构"
  adaptedUrl="https://www.oreilly.com/library/view/head-first-design/9781492077992/"
/>

<Objectives>

- 能用自己的话解释模板方法模式是什么、为什么需要它
- 能看懂并独立修改模板方法的 Java 代码
- 能回答：模板方法和策略模式的本质区别是什么？钩子方法和抽象方法有什么不同？

</Objectives>

## 为什么需要模板方法模式

你在开发一个咖啡因饮料系统。咖啡和茶的冲泡流程几乎一样：烧水 → 冲泡 → 倒杯 → 加调料。区别只是冲泡方式（咖啡用咖啡粉冲泡，茶用茶包浸泡）和调料不同（咖啡加糖奶，茶加柠檬）。

如果分别写 Coffee 和 Tea 类，两个类的 `prepareRecipe()` 方法 90% 代码重复——烧水、倒杯的代码一样，只是中间几步不同。如果修改流程（比如加一步「预热杯子」），要同时改两个类。

问题的根源是：**算法骨架重复，但个别步骤变化**。继承可以复用代码，但普通继承没有「强制流程一致」的机制——子类可能乱改步骤顺序。

## 概念讲解

模板方法模式的思路是：**父类定义算法骨架（final 不可覆盖），把可变步骤声明为抽象方法交子类实现**。

<Term def="定义算法骨架的具体方法，声明为 final 防止子类覆盖，内部调用其他步骤方法">模板方法</Term>
是 `prepareRecipe()`。它定义步骤顺序：`boilWater()` → `brew()` → `pourInCup()` →
`addCondiments()`。这个顺序由父类控制，子类不能改——保证流程一致性。

<Term def="声明在父类中的 abstract 方法，子类必须实现，是算法的可变步骤">抽象方法</Term>
是 `brew()` 和
`addCondiments()`。咖啡和茶各自实现——咖啡用咖啡粉冲泡，茶用茶包浸泡。抽象方法是「必填填空」。

<Term def="父类中有默认实现的具体方法，子类可选覆盖，用于影响流程">钩子方法</Term>
是 `customerWantsCondiments()`，默认返回 true。子类可以覆盖返回 false
跳过加调料步骤。钩子是「可选开关」。

<HfdTemplateMethodDiagram />

关键原则：<Term def="高层组件控制流程主动调用低层组件，低层组件不主动调用高层——别调用我们，我们会调用你">好莱坞原则</Term>。父类控制流程（什么时候调什么方法），子类只提供实现（填空）。控制权在父类，细节在子类——控制反转。

## 交互Demo

<Stepper>
  <Step title="第一步：分析重复流程">
    咖啡和茶的冲泡流程几乎一样：烧水 → 冲泡 → 倒杯 →
    加调料。烧水和倒杯完全相同，冲泡和加调料各自不同。两个类的 prepareRecipe()
    90% 代码重复。
  </Step>
  <Step title="第二步：定义抽象父类">
    CaffeineBeverage 定义 prepareRecipe()
    模板方法（final），固定步骤顺序：boilWater() → brew() → pourInCup() →
    addCondiments()。其中 boilWater 和 pourInCup
    是具体方法（父类实现，子类共用），brew 和 addCondiments
    是抽象方法（子类实现）。
  </Step>
  <Step title="第三步：子类填空">
    Coffee 实现 brew()（冲咖啡粉）和 addCondiments()（加糖奶）。Tea 实现
    brew()（茶包浸泡）和
    addCondiments()（加柠檬）。子类只填空，不改流程——prepareRecipe() 是 final
    不能覆盖。
  </Step>
  <Step title="第四步：钩子控制流程">
    父类定义钩子 customerWantsCondiments() 默认返回 true。BlackCoffee 覆盖返回
    false——不加调料。模板方法中 `if (customerWantsCondiments())
    addCondiments()`——子类通过钩子影响流程，但不改骨架。
  </Step>
  <Step title="第五步：好莱坞原则达成">
    父类主动调用子类方法（brew/addCondiments），子类不主动调用父类。控制权在父类——「别调用我们，我们会调用你」。子类只管实现细节，不管流程编排。
  </Step>
</Stepper>

## 代码对照

```java
// 抽象父类——定义骨架
public abstract class CaffeineBeverage {
    // 模板方法——final 防止子类覆盖
    final void prepareRecipe() {
        boilWater();
        brew();
        pourInCup();
        if (customerWantsCondiments()) {  // 钩子
            addCondiments();
        }
    }

    // 抽象方法——子类必须实现
    abstract void brew();
    abstract void addCondiments();

    // 具体方法——子类共用
    void boilWater() { System.out.println("烧水"); }
    void pourInCup() { System.out.println("倒杯"); }

    // 钩子方法——默认实现，子类可选覆盖
    boolean customerWantsCondiments() { return true; }
}

// 子类——只填空，不改骨架
public class Coffee extends CaffeineBeverage {
    void brew() { System.out.println("冲咖啡粉"); }
    void addCondiments() { System.out.println("加糖和奶"); }
}

public class Tea extends CaffeineBeverage {
    void brew() { System.out.println("茶包浸泡"); }
    void addCondiments() { System.out.println("加柠檬"); }
}
```

父类控制流程，子类只实现 brew 和 addCondiments——骨架一致，细节可变。

## 常见误区

<Callout type="trap">
  <strong>现象 →</strong> 模板方法不用 final，子类覆盖了 prepareRecipe()
  改了步骤顺序，流程一致性被破坏。 <strong>原因 →</strong> 没有用 final
  保护模板方法，子类「越权」改骨架。 <strong>修法 →</strong> 模板方法必须声明为
  final，明确告诉子类「骨架不可改，只能填空」。如果子类确实需要改流程，考虑用策略模式替换整个算法，而非覆盖模板方法。
</Callout>

## 小结与练习

- 模板方法在父类定义算法骨架（final），把可变步骤声明为抽象方法交子类实现
- 模板方法控制流程（高层），抽象方法提供实现（低层）——控制权反转
- 钩子方法有默认实现，子类可选覆盖影响流程；抽象方法子类必须实现
- 好莱坞原则：父类主动调用子类方法，子类不主动调用父类——别调用我们，我们会调用你

<Exercises>
  <Answer question="模板方法和策略模式都能封装算法变化，本质区别是什么？">
    变化方式不同：模板方法是「整体不变部分变化」——骨架固定个别步骤可变，用继承实现，子类覆盖步骤。策略模式是「整体变化」——整个算法替换，用组合实现，运行时切换策略对象。控制权不同：模板方法父类控制流程（好莱坞原则），子类只填空；策略模式
    Context
    把整个算法委托给策略，策略内部自己决定流程。粒度不同：模板方法细粒度控制到每个步骤；策略模式粗粒度整个算法替换。运行时行为不同：模板方法编译期确定（子类类型决定），策略模式运行时可切换。选择：骨架固定只有步骤不同用模板方法；整个算法需替换用策略模式。
  </Answer>
  <Answer question="钩子方法和抽象方法有什么区别？什么时候用钩子？">
    抽象方法是 abstract
    方法，父类无实现，子类必须实现——是算法的必要步骤，必填填空。钩子方法是具体方法，父类有默认实现（通常空方法或返回默认值），子类可选覆盖——是算法的可选扩展点，可选开关。用钩子的场景：某个步骤是可选的（如加不加调料），或子类需要有机会影响流程但不强制。钩子让子类在不改变骨架的前提下影响流程——条件分支的决策权交给子类。如果用抽象方法强制实现就没有可选的灵活性。
  </Answer>
</Exercises>

## 名词解释

<Glossary>
  <GlossaryItem term="模板方法">
    定义算法骨架的具体方法，通常声明为 final
    防止子类覆盖。内部按固定顺序调用其他步骤方法（抽象的或具体的）。
  </GlossaryItem>
  <GlossaryItem term="钩子方法">
    父类中有默认实现的具体方法，子类可选覆盖。用于让子类有机会影响模板方法的流程，但不强制实现。
  </GlossaryItem>
  <GlossaryItem term="好莱坞原则">
    「别调用我们，我们会调用你」——高层组件控制流程主动调用低层组件，低层组件不主动调用高层。模板方法是此原则的体现。
  </GlossaryItem>
  <GlossaryItem term="抽象方法">
    父类中声明的 abstract
    方法，无实现，子类必须实现。是算法骨架中必须由子类提供的可变步骤。
  </GlossaryItem>
</Glossary>

<Attribution
  adaptedFrom="Head First Design Patterns (Eric Freeman & Elisabeth Robson)"
  adaptedUrl="https://www.oreilly.com/library/view/head-first-design/0596007124/"
/>
