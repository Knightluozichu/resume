---
title: "第4章 概率计算：把握机会"
type: C
section: "深入浅出统计学 · 概率与分布"
order: 4
description: "按O'Reilly权威目录重建，覆盖互斥事件、条件概率、概率树、全概率公式、贝叶斯定理。"
demo: true
math: true
sourceUrl: "https://www.oreilly.com/library/view/head-first-statistics/9780596527587/"
draft: true
---

import { Objectives, Callout, Attribution, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step } from "@/components/mdx/mdx-components";
import { HfsCalculatingProbabilitiesDataLab, HfsCalculatingProbabilitiesModelLab, HfsCalculatingProbabilitiesEvidenceLab } from "@/components/mdx/head-first-statistics/diagrams/hfs-calculating-probabilities";

<Objectives>
- 能解释互斥事件与条件概率的统计对象和适用边界
- 能推导概率树与全概率公式的关键关系
- 能实现并验证围绕贝叶斯定理的可复现实验
</Objectives>

## 先做预测

先预测样本量翻倍、极端值出现或随机机制改变时，图形、估计与结论会怎样变化。本章用轮盘、Venn图和概率树连接补集、并集、交集、条件概率、全概率公式与贝叶斯定理。重点是先写事件和生成机制，再决定加法还是乘法，不能把条件方向凭直觉互换。 本章以<Term def="两个事件不能同时发生，交集为空时并集概率可直接相加。">互斥事件</Term>、<Term def="已知B发生后在B对应的子空间重新归一化A的概率。">条件概率</Term>、<Term def="按时间或条件顺序展开分支，路径概率相乘、互斥路径概率相加。">概率树</Term>、<Term def="按互斥完备条件分区汇总目标事件概率。">全概率公式</Term>、<Term def="用似然和先验更新后验，证据概率负责归一化。">贝叶斯定理</Term>建立从数据到证据的链条。

<HfsCalculatingProbabilitiesDataLab />

## 原书章节定位

本页一一对应O'Reilly原书“第4章 概率计算：把握机会”。本章用轮盘、Venn图和概率树连接补集、并集、交集、条件概率、全概率公式与贝叶斯定理。重点是先写事件和生成机制，再决定加法还是乘法，不能把条件方向凭直觉互换。 原书的故事负责建立直觉；本页进一步要求写出总体、样本、参数和统计量，并用公式、代码与反例验收直觉。

### 核心单元

1. **互斥事件**：两个事件不能同时发生，交集为空时并集概率可直接相加。
2. **条件概率**：已知B发生后在B对应的子空间重新归一化A的概率。
3. **概率树**：按时间或条件顺序展开分支，路径概率相乘、互斥路径概率相加。
4. **全概率公式**：按互斥完备条件分区汇总目标事件概率。
5. **贝叶斯定理**：用似然和先验更新后验，证据概率负责归一化。

## 从问题开始

先写五个可反驳问题：互斥事件描述谁，条件概率如何改变表示，概率树依赖什么随机机制，全概率公式在哪些边界失效，贝叶斯定理留下什么验收证据。统计计算只有在总体、观察单位、时间窗和缺失规则明确后才有意义。两个事件不能同时发生，交集为空时并集概率可直接相加。 已知B发生后在B对应的子空间重新归一化A的概率。 两者若来自不同总体，任何后续公式都失去解释。

$$
\mathcal D=(\text{population},\text{unit},\text{window},\text{measurement})
$$

数据契约D把样本的来源固定下来。接着用概率树表达结构，并由全概率公式标记前提。贝叶斯定理必须能由另一位读者在同一数据版本上重算。不能先看结果再改变分组、尾部方向或排除条件。

## 反例与敏感性

正常样例应覆盖原书中心场景，边界样例至少包括空组、单样本、极端值、概率接近零或一、强相关与分布偏斜。再以“把互斥与独立混为一谈；正概率互斥事件反而不独立。”构造反例，记录第一个失效的假设。告警系统的后验可信度取决于故障基率、召回和误报率。即使召回很高，低基率也会让多数告警为假。看板应展示分子分母的真实计数，而不只展示一个“准确率”。

$$
\varepsilon=\operatorname{estimate}-\operatorname{target}
$$

误差可能来自抽样波动、选择偏差、测量误差、模型偏差和浮点实现。增加样本通常只降低抽样波动，不能修复抽样框缺失或错误模型。敏感性分析要分别改变分组、异常值规则和模型假设，观察结论方向与区间是否稳定。

## 验收证书

证书包括原始计数、互斥事件定义、条件概率变换、概率树计算、全概率公式边界和贝叶斯定理结论。每个百分比都能回到分子分母，每个图都标明尺度和样本量，每个检验都给效应与区间，每个随机过程都记录种子。

$$
\operatorname{accept}=\text{assumptions valid}\land\text{error within budget}\land\text{result reproducible}
$$

预算在看数据前定义。未通过时要区分数据不足、模型不适用、效果不足和实现错误；“未显著”不能笼统翻译为“没有差异”。

## 可核查推导

$$
P(A\mid B)=\frac{P(A\cap B)}{P(B)},\qquad P(A\mid B)=\frac{P(B\mid A)P(A)}{P(B)}
$$

P(A交B)既可写成P(A给定B)P(B)，也可写成P(B给定A)P(A)。令两式相等并除以P(B)得到贝叶斯公式。全概率公式把P(B)按A及其补集展开，避免遗漏归一化来源。 每一步都要注明使用定义、独立性、分布假设还是渐近近似；若只在大样本下成立，就不能把小样本结果包装成精确结论。

```python
def bayes(prior, sensitivity, false_positive):
    evidence = sensitivity * prior + false_positive * (1-prior)
    return sensitivity * prior / evidence

assert round(bayes(0.01, 0.99, 0.05), 3) == 0.167
```

程序只检查有限实例。解析公式、模拟频率和独立实现应三路对照；若三者不一致，先检查参数化、端点、自由度和随机种子。

## 机制与数据流

**互斥事件**：两个事件不能同时发生，交集为空时并集概率可直接相加。

**条件概率**：已知B发生后在B对应的子空间重新归一化A的概率。

**概率树**：按时间或条件顺序展开分支，路径概率相乘、互斥路径概率相加。

数据流写成“总体 → 抽样或观察 → 统计量 → 抽样分布 → 决策”。任何聚合都保留分母、缺失数和分组规则，避免统计量脱离生成过程。

<HfsCalculatingProbabilitiesModelLab />

## 边界与失败模式

**全概率公式**：按互斥完备条件分区汇总目标事件概率。

**贝叶斯定理**：用似然和先验更新后验，证据概率负责归一化。

边界测试包括零样本、单样本、不平衡类别、概率极端、离群点、强偏态、相关观测和数值退化。失败应返回明确状态，不能用NaN或一张空图悄悄结束。

```python
experiment={"claim":"互斥事件到贝叶斯定理","normal":"假设满足","boundary":"退化或偏斜输入","counterexample":"把互斥与独立混为一谈；正概率互斥事件反而不独立。"}
assert len(experiment)==4
```

## 工程案例

告警系统的后验可信度取决于故障基率、召回和误报率。即使召回很高，低基率也会让多数告警为假。看板应展示分子分母的真实计数，而不只展示一个“准确率”。 报告必须同时给原始计数、效应、误差或区间、模型诊断和未覆盖范围。图表与结论都绑定数据版本，才能在新数据到来后判断变化来自现实还是代码。

<HfsCalculatingProbabilitiesEvidenceLab />

```python
evidence=[("互斥事件","definition"),("概率树","derivation"),("贝叶斯定理","reproduction")]
assert len({name for name,_ in evidence})==3
```

## 分步实验

<Stepper>
  <Step title="审计数据"><HfsCalculatingProbabilitiesDataLab /></Step>
  <Step title="破坏假设"><HfsCalculatingProbabilitiesModelLab /></Step>
  <Step title="保存证据"><HfsCalculatingProbabilitiesEvidenceLab /></Step>
</Stepper>

1. 保存最小手算样例和原始分子分母。
2. 加入空组、极端值、偏态和相关输入，解释哪条假设失效。
3. 用固定种子模拟并与解析结果比较误差。
4. 重建环境重跑，保存数据摘要、参数、代码版本和结论。

## 常见误区

<Callout type="trap" title="统计对象错位">把互斥与独立混为一谈；正概率互斥事件反而不独立。 修复时先回到总体、单位和生成机制。</Callout>

<Callout type="trap" title="单一数字替代证据">将P(A给定B)当成P(B给定A)，漏掉先验基率。 应补充图形、区间、诊断与反例。</Callout>

## 本章回顾

本章覆盖互斥事件、条件概率、概率树、全概率公式、贝叶斯定理。掌握标准是能从原始计数重建公式，说明抽样或分布假设，用代码复查并在假设破坏时给出可解释失败。

<Exercises>
**问题 1：互斥事件与条件概率如何协作？**
<Answer>两个事件不能同时发生，交集为空时并集概率可直接相加。 已知B发生后在B对应的子空间重新归一化A的概率。 两者必须对应同一总体与观察单位。</Answer>

**问题 2：如何从概率树推到全概率公式？**
<Answer>P(A交B)既可写成P(A给定B)P(B)，也可写成P(B给定A)P(A)。令两式相等并除以P(B)得到贝叶斯公式。全概率公式把P(B)按A及其补集展开，避免遗漏归一化来源。 推导需逐步标注假设并用小样例复算。</Answer>

**问题 3：怎样验收贝叶斯定理？**
<Answer>用似然和先验更新后验，证据概率负责归一化。 使用正常、空、极端、偏态和相关输入，保存效应、误差与反例。</Answer>
</Exercises>

## 术语表

<Glossary>
  <GlossaryItem term="互斥事件" definition="两个事件不能同时发生，交集为空时并集概率可直接相加。" />
  <GlossaryItem term="条件概率" definition="已知B发生后在B对应的子空间重新归一化A的概率。" />
  <GlossaryItem term="概率树" definition="按时间或条件顺序展开分支，路径概率相乘、互斥路径概率相加。" />
  <GlossaryItem term="全概率公式" definition="按互斥完备条件分区汇总目标事件概率。" />
  <GlossaryItem term="贝叶斯定理" definition="用似然和先验更新后验，证据概率负责归一化。" />
  <GlossaryItem term="统计证据" definition="把数据来源、模型、效应、不确定性和反例一起交付。" />
</Glossary>

<Attribution adaptedFrom="Dawn Griffiths · Head First Statistics · 第4章 概率计算：把握机会" adaptedUrl="https://www.oreilly.com/library/view/head-first-statistics/9780596527587/" />
