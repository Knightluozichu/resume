---
title: "第5章 离散概率分布：管理期望"
type: C
section: "深入浅出统计学 · 概率与分布"
order: 5
description: "按O'Reilly权威目录重建，覆盖随机变量、概率质量函数、期望、方差、线性变换。"
demo: true
math: true
sourceUrl: "https://www.oreilly.com/library/view/head-first-statistics/9780596527587/"
draft: true
---

import { Objectives, Callout, Attribution, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step } from "@/components/mdx/mdx-components";
import { HfsDiscreteProbabilityDistributionsDataLab, HfsDiscreteProbabilityDistributionsModelLab, HfsDiscreteProbabilityDistributionsEvidenceLab } from "@/components/mdx/head-first-statistics/diagrams/hfs-discrete-probability-distributions";

<Objectives>
- 能解释随机变量与概率质量函数的统计对象和适用边界
- 能推导期望与方差的关键关系
- 能实现并验证围绕线性变换的可复现实验
</Objectives>

## 先做预测

先预测样本量翻倍、极端值出现或随机机制改变时，图形、估计与结论会怎样变化。离散概率分布列出随机变量每个取值及其概率。期望给长期平均，方差描述结果分散；线性变换和独立观测的规则允许不枚举完整联合分布就计算组合变量。 本章以<Term def="把样本空间结果映射为数值，变量的分布由试验机制诱导。">随机变量</Term>、<Term def="为每个离散取值分配非负概率且总和为1。">概率质量函数</Term>、<Term def="概率加权平均，具有线性性，即使变量不独立也可相加。">期望</Term>、<Term def="围绕期望的平方偏差；独立变量方差相加。">方差</Term>、<Term def="Y等于aX加b时，期望变为aE[X]加b，方差变为a平方Var(X)。">线性变换</Term>建立从数据到证据的链条。

<HfsDiscreteProbabilityDistributionsDataLab />

## 原书章节定位

本页一一对应O'Reilly原书“第5章 离散概率分布：管理期望”。离散概率分布列出随机变量每个取值及其概率。期望给长期平均，方差描述结果分散；线性变换和独立观测的规则允许不枚举完整联合分布就计算组合变量。 原书的故事负责建立直觉；本页进一步要求写出总体、样本、参数和统计量，并用公式、代码与反例验收直觉。

### 核心单元

1. **随机变量**：把样本空间结果映射为数值，变量的分布由试验机制诱导。
2. **概率质量函数**：为每个离散取值分配非负概率且总和为1。
3. **期望**：概率加权平均，具有线性性，即使变量不独立也可相加。
4. **方差**：围绕期望的平方偏差；独立变量方差相加。
5. **线性变换**：Y等于aX加b时，期望变为aE[X]加b，方差变为a平方Var(X)。

## 从问题开始

先写五个可反驳问题：随机变量描述谁，概率质量函数如何改变表示，期望依赖什么随机机制，方差在哪些边界失效，线性变换留下什么验收证据。统计计算只有在总体、观察单位、时间窗和缺失规则明确后才有意义。把样本空间结果映射为数值，变量的分布由试验机制诱导。 为每个离散取值分配非负概率且总和为1。 两者若来自不同总体，任何后续公式都失去解释。

$$
\mathcal D=(\text{population},\text{unit},\text{window},\text{measurement})
$$

数据契约D把样本的来源固定下来。接着用期望表达结构，并由方差标记前提。线性变换必须能由另一位读者在同一数据版本上重算。不能先看结果再改变分组、尾部方向或排除条件。

## 反例与敏感性

正常样例应覆盖原书中心场景，边界样例至少包括空组、单样本、极端值、概率接近零或一、强相关与分布偏斜。再以“把期望解释为一次试验最可能出现的结果；期望甚至可能不是可取值。”构造反例，记录第一个失效的假设。促销收益不能只看期望为正，还要看方差和最坏损失。两个游戏平均收益相同，尾部风险可能不同。模拟应与解析期望交叉验证，并记录独立性、赔付上限和样本误差。

$$
\varepsilon=\operatorname{estimate}-\operatorname{target}
$$

误差可能来自抽样波动、选择偏差、测量误差、模型偏差和浮点实现。增加样本通常只降低抽样波动，不能修复抽样框缺失或错误模型。敏感性分析要分别改变分组、异常值规则和模型假设，观察结论方向与区间是否稳定。

## 验收证书

证书包括原始计数、随机变量定义、概率质量函数变换、期望计算、方差边界和线性变换结论。每个百分比都能回到分子分母，每个图都标明尺度和样本量，每个检验都给效应与区间，每个随机过程都记录种子。

$$
\operatorname{accept}=\text{assumptions valid}\land\text{error within budget}\land\text{result reproducible}
$$

预算在看数据前定义。未通过时要区分数据不足、模型不适用、效果不足和实现错误；“未显著”不能笼统翻译为“没有差异”。

## 可核查推导

$$
E[X]=\sum_x xP(X=x),\qquad Var(X)=E[(X-E[X])^2]
$$

将Y=aX+b代入期望定义，常数可提出求和并利用概率总和为1，得到aE[X]+b。Y减E[Y]等于a(X-E[X])，平方后提出a平方，所以平移不改变方差，缩放按平方改变方差。 每一步都要注明使用定义、独立性、分布假设还是渐近近似；若只在大样本下成立，就不能把小样本结果包装成精确结论。

```python
def moments(outcomes):
    mean = sum(value * probability for value, probability in outcomes)
    variance = sum((value-mean)**2 * probability for value, probability in outcomes)
    return mean, variance

assert moments([(0, .5), (2, .5)]) == (1.0, 1.0)
```

程序只检查有限实例。解析公式、模拟频率和独立实现应三路对照；若三者不一致，先检查参数化、端点、自由度和随机种子。

## 机制与数据流

**随机变量**：把样本空间结果映射为数值，变量的分布由试验机制诱导。

**概率质量函数**：为每个离散取值分配非负概率且总和为1。

**期望**：概率加权平均，具有线性性，即使变量不独立也可相加。

数据流写成“总体 → 抽样或观察 → 统计量 → 抽样分布 → 决策”。任何聚合都保留分母、缺失数和分组规则，避免统计量脱离生成过程。

<HfsDiscreteProbabilityDistributionsModelLab />

## 边界与失败模式

**方差**：围绕期望的平方偏差；独立变量方差相加。

**线性变换**：Y等于aX加b时，期望变为aE[X]加b，方差变为a平方Var(X)。

边界测试包括零样本、单样本、不平衡类别、概率极端、离群点、强偏态、相关观测和数值退化。失败应返回明确状态，不能用NaN或一张空图悄悄结束。

```python
experiment={"claim":"随机变量到线性变换","normal":"假设满足","boundary":"退化或偏斜输入","counterexample":"把期望解释为一次试验最可能出现的结果；期望甚至可能不是可取值。"}
assert len(experiment)==4
```

## 工程案例

促销收益不能只看期望为正，还要看方差和最坏损失。两个游戏平均收益相同，尾部风险可能不同。模拟应与解析期望交叉验证，并记录独立性、赔付上限和样本误差。 报告必须同时给原始计数、效应、误差或区间、模型诊断和未覆盖范围。图表与结论都绑定数据版本，才能在新数据到来后判断变化来自现实还是代码。

<HfsDiscreteProbabilityDistributionsEvidenceLab />

```python
evidence=[("随机变量","definition"),("期望","derivation"),("线性变换","reproduction")]
assert len({name for name,_ in evidence})==3
```

## 分步实验

<Stepper>
  <Step title="审计数据"><HfsDiscreteProbabilityDistributionsDataLab /></Step>
  <Step title="破坏假设"><HfsDiscreteProbabilityDistributionsModelLab /></Step>
  <Step title="保存证据"><HfsDiscreteProbabilityDistributionsEvidenceLab /></Step>
</Stepper>

1. 保存最小手算样例和原始分子分母。
2. 加入空组、极端值、偏态和相关输入，解释哪条假设失效。
3. 用固定种子模拟并与解析结果比较误差。
4. 重建环境重跑，保存数据摘要、参数、代码版本和结论。

## 常见误区

<Callout type="trap" title="统计对象错位">把期望解释为一次试验最可能出现的结果；期望甚至可能不是可取值。 修复时先回到总体、单位和生成机制。</Callout>

<Callout type="trap" title="单一数字替代证据">相加相关变量时直接相加方差，遗漏协方差项。 应补充图形、区间、诊断与反例。</Callout>

## 本章回顾

本章覆盖随机变量、概率质量函数、期望、方差、线性变换。掌握标准是能从原始计数重建公式，说明抽样或分布假设，用代码复查并在假设破坏时给出可解释失败。

<Exercises>
**问题 1：随机变量与概率质量函数如何协作？**
<Answer>把样本空间结果映射为数值，变量的分布由试验机制诱导。 为每个离散取值分配非负概率且总和为1。 两者必须对应同一总体与观察单位。</Answer>

**问题 2：如何从期望推到方差？**
<Answer>将Y=aX+b代入期望定义，常数可提出求和并利用概率总和为1，得到aE[X]+b。Y减E[Y]等于a(X-E[X])，平方后提出a平方，所以平移不改变方差，缩放按平方改变方差。 推导需逐步标注假设并用小样例复算。</Answer>

**问题 3：怎样验收线性变换？**
<Answer>Y等于aX加b时，期望变为aE[X]加b，方差变为a平方Var(X)。 使用正常、空、极端、偏态和相关输入，保存效应、误差与反例。</Answer>
</Exercises>

## 术语表

<Glossary>
  <GlossaryItem term="随机变量" definition="把样本空间结果映射为数值，变量的分布由试验机制诱导。" />
  <GlossaryItem term="概率质量函数" definition="为每个离散取值分配非负概率且总和为1。" />
  <GlossaryItem term="期望" definition="概率加权平均，具有线性性，即使变量不独立也可相加。" />
  <GlossaryItem term="方差" definition="围绕期望的平方偏差；独立变量方差相加。" />
  <GlossaryItem term="线性变换" definition="Y等于aX加b时，期望变为aE[X]加b，方差变为a平方Var(X)。" />
  <GlossaryItem term="统计证据" definition="把数据来源、模型、效应、不确定性和反例一起交付。" />
</Glossary>

<Attribution adaptedFrom="Dawn Griffiths · Head First Statistics · 第5章 离散概率分布：管理期望" adaptedUrl="https://www.oreilly.com/library/view/head-first-statistics/9780596527587/" />
