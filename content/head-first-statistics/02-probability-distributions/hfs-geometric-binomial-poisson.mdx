---
title: "第7章 几何、二项与泊松分布：离散模型"
type: C
section: "深入浅出统计学 · 概率与分布"
order: 7
description: "按O'Reilly权威目录重建，覆盖几何分布、二项分布、泊松分布、参数假设、分布可加性。"
demo: true
math: true
sourceUrl: "https://www.oreilly.com/library/view/head-first-statistics/9780596527587/"
draft: true
---

import { Objectives, Callout, Attribution, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step } from "@/components/mdx/mdx-components";

<Objectives>
- 能解释几何分布与二项分布的统计对象和适用边界
- 能推导泊松分布与参数假设的关键关系
- 能实现并验证围绕分布可加性的可复现实验
</Objectives>

## 先做预测

先预测样本量翻倍、极端值出现或随机机制改变时，图形、估计与结论会怎样变化。三种离散分布对应三种问题：几何分布等到首次成功，二项分布数固定试验中的成功次数，泊松分布数固定区间内的稀疏事件。模型选择由生成过程决定。 本章以<Term def="独立伯努利试验直到首次成功的试验次数，期望为1除以p。">几何分布</Term>、<Term def="固定n次独立同概率试验中的成功次数。">二项分布</Term>、<Term def="固定区间内独立稀疏到达次数，均值与方差都为lambda。">泊松分布</Term>、<Term def="独立、固定概率或稳定到达率是公式成立的核心。">参数假设</Term>、<Term def="独立泊松变量相加仍为泊松，参数相加。">分布可加性</Term>建立从数据到证据的链条。

<HfsGeometricBinomialPoissonDataLab />

## 原书章节定位

本页一一对应O'Reilly原书“第7章 几何、二项与泊松分布：离散模型”。三种离散分布对应三种问题：几何分布等到首次成功，二项分布数固定试验中的成功次数，泊松分布数固定区间内的稀疏事件。模型选择由生成过程决定。 原书的故事负责建立直觉；本页进一步要求写出总体、样本、参数和统计量，并用公式、代码与反例验收直觉。

### 核心单元

1. **几何分布**：独立伯努利试验直到首次成功的试验次数，期望为1除以p。
2. **二项分布**：固定n次独立同概率试验中的成功次数。
3. **泊松分布**：固定区间内独立稀疏到达次数，均值与方差都为lambda。
4. **参数假设**：独立、固定概率或稳定到达率是公式成立的核心。
5. **分布可加性**：独立泊松变量相加仍为泊松，参数相加。

## 从问题开始

先写五个可反驳问题：几何分布描述谁，二项分布如何改变表示，泊松分布依赖什么随机机制，参数假设在哪些边界失效，分布可加性留下什么验收证据。统计计算只有在总体、观察单位、时间窗和缺失规则明确后才有意义。独立伯努利试验直到首次成功的试验次数，期望为1除以p。 固定n次独立同概率试验中的成功次数。 两者若来自不同总体，任何后续公式都失去解释。

$$
\mathcal D=(\text{population},\text{unit},\text{window},\text{measurement})
$$

数据契约D把样本的来源固定下来。接着用泊松分布表达结构，并由参数假设标记前提。分布可加性必须能由另一位读者在同一数据版本上重算。不能先看结果再改变分组、尾部方向或排除条件。

## 反例与敏感性

正常样例应覆盖原书中心场景，边界样例至少包括空组、单样本、极端值、概率接近零或一、强相关与分布偏斜。再以“数据是计数就自动套泊松，没有检查稳定率与独立增量。”构造反例，记录第一个失效的假设。每分钟请求到达数可近似泊松，但发布或故障会产生突发和相关。应按时间段检验均值方差关系与过度离散，再决定使用泊松、负二项或经验模型。

$$
\varepsilon=\operatorname{estimate}-\operatorname{target}
$$

误差可能来自抽样波动、选择偏差、测量误差、模型偏差和浮点实现。增加样本通常只降低抽样波动，不能修复抽样框缺失或错误模型。敏感性分析要分别改变分组、异常值规则和模型假设，观察结论方向与区间是否稳定。

## 验收证书

证书包括原始计数、几何分布定义、二项分布变换、泊松分布计算、参数假设边界和分布可加性结论。每个百分比都能回到分子分母，每个图都标明尺度和样本量，每个检验都给效应与区间，每个随机过程都记录种子。

$$
\operatorname{accept}=\text{assumptions valid}\land\text{error within budget}\land\text{result reproducible}
$$

预算在看数据前定义。未通过时要区分数据不足、模型不适用、效果不足和实现错误；“未显著”不能笼统翻译为“没有差异”。

## 可核查推导

$$
P_{bin}(X=k)={n\choose k}p^k(1-p)^{n-k},\qquad P_{pois}(X=k)=e^{-\lambda}\frac{\lambda^k}{k!}
$$

二项分布恰有k次成功时，每个指定位置模式概率为p的k次方乘失败概率的n减k次方，共有n选k种互斥模式。泊松分布可由n大、p小且np固定的二项极限得到。 每一步都要注明使用定义、独立性、分布假设还是渐近近似；若只在大样本下成立，就不能把小样本结果包装成精确结论。

```python
from math import comb, exp, factorial

def binomial(n,k,p): return comb(n,k)*p**k*(1-p)**(n-k)
def poisson(k,rate): return exp(-rate)*rate**k/factorial(k)

assert abs(sum(binomial(5,k,.3) for k in range(6))-1) < 1e-12
```

程序只检查有限实例。解析公式、模拟频率和独立实现应三路对照；若三者不一致，先检查参数化、端点、自由度和随机种子。

## 机制与数据流

**几何分布**：独立伯努利试验直到首次成功的试验次数，期望为1除以p。

**二项分布**：固定n次独立同概率试验中的成功次数。

**泊松分布**：固定区间内独立稀疏到达次数，均值与方差都为lambda。

数据流写成“总体 → 抽样或观察 → 统计量 → 抽样分布 → 决策”。任何聚合都保留分母、缺失数和分组规则，避免统计量脱离生成过程。

<HfsGeometricBinomialPoissonModelLab />

## 边界与失败模式

**参数假设**：独立、固定概率或稳定到达率是公式成立的核心。

**分布可加性**：独立泊松变量相加仍为泊松，参数相加。

边界测试包括零样本、单样本、不平衡类别、概率极端、离群点、强偏态、相关观测和数值退化。失败应返回明确状态，不能用NaN或一张空图悄悄结束。

```python
experiment={"claim":"几何分布到分布可加性","normal":"假设满足","boundary":"退化或偏斜输入","counterexample":"数据是计数就自动套泊松，没有检查稳定率与独立增量。"}
assert len(experiment)==4
```

## 工程案例

每分钟请求到达数可近似泊松，但发布或故障会产生突发和相关。应按时间段检验均值方差关系与过度离散，再决定使用泊松、负二项或经验模型。 报告必须同时给原始计数、效应、误差或区间、模型诊断和未覆盖范围。图表与结论都绑定数据版本，才能在新数据到来后判断变化来自现实还是代码。

```python
evidence=[("几何分布","definition"),("泊松分布","derivation"),("分布可加性","reproduction")]
assert len({name for name,_ in evidence})==3
```

1. 保存最小手算样例和原始分子分母。
2. 加入空组、极端值、偏态和相关输入，解释哪条假设失效。
3. 用固定种子模拟并与解析结果比较误差。
4. 重建环境重跑，保存数据摘要、参数、代码版本和结论。

## 常见误区

<Callout type="trap" title="统计对象错位">数据是计数就自动套泊松，没有检查稳定率与独立增量。 修复时先回到总体、单位和生成机制。</Callout>

<Callout type="trap" title="单一数字替代证据">把几何分布的取值从0开始还是1开始混用，造成期望和概率偏一。 应补充图形、区间、诊断与反例。</Callout>

## 本章回顾

本章覆盖几何分布、二项分布、泊松分布、参数假设、分布可加性。掌握标准是能从原始计数重建公式，说明抽样或分布假设，用代码复查并在假设破坏时给出可解释失败。

<Exercises>
**问题 1：几何分布与二项分布如何协作？**
<Answer>独立伯努利试验直到首次成功的试验次数，期望为1除以p。 固定n次独立同概率试验中的成功次数。 两者必须对应同一总体与观察单位。</Answer>

**问题 2：如何从泊松分布推到参数假设？**
<Answer>二项分布恰有k次成功时，每个指定位置模式概率为p的k次方乘失败概率的n减k次方，共有n选k种互斥模式。泊松分布可由n大、p小且np固定的二项极限得到。 推导需逐步标注假设并用小样例复算。</Answer>

**问题 3：怎样验收分布可加性？**
<Answer>独立泊松变量相加仍为泊松，参数相加。 使用正常、空、极端、偏态和相关输入，保存效应、误差与反例。</Answer>
</Exercises>

## 术语表

<Glossary>
  <GlossaryItem term="几何分布" definition="独立伯努利试验直到首次成功的试验次数，期望为1除以p。" />
  <GlossaryItem term="二项分布" definition="固定n次独立同概率试验中的成功次数。" />
  <GlossaryItem term="泊松分布" definition="固定区间内独立稀疏到达次数，均值与方差都为lambda。" />
  <GlossaryItem term="参数假设" definition="独立、固定概率或稳定到达率是公式成立的核心。" />
  <GlossaryItem term="分布可加性" definition="独立泊松变量相加仍为泊松，参数相加。" />
  <GlossaryItem term="统计证据" definition="把数据来源、模型、效应、不确定性和反例一起交付。" />
</Glossary>

<Attribution adaptedFrom="Dawn Griffiths · Head First Statistics · 第7章 几何、二项与泊松分布：离散模型" adaptedUrl="https://www.oreilly.com/library/view/head-first-statistics/9780596527587/" />
