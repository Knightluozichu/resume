---
title: "第13章 假设检验：查看证据"
type: C
section: "深入浅出统计学 · 抽样与推断"
order: 13
description: "按O'Reilly权威目录重建，覆盖原假设、备择假设、显著性水平、p值、功效。"
demo: true
math: true
sourceUrl: "https://www.oreilly.com/library/view/head-first-statistics/9780596527587/"
draft: false
---

import { Objectives, Callout, Attribution, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step } from "@/components/mdx/mdx-components";
import { HfsHypothesisTestsDataLab, HfsHypothesisTestsModelLab, HfsHypothesisTestsEvidenceLab } from "@/components/mdx/head-first-statistics/diagrams/hfs-hypothesis-tests";

<Objectives>
- 能解释原假设与备择假设的统计对象和适用边界
- 能推导显著性水平与p值的关键关系
- 能实现并验证围绕功效的可复现实验
</Objectives>

## 先做预测

先预测样本量翻倍、极端值出现或随机机制改变时，图形、估计与结论会怎样变化。六步检验流程从原假设、备择假设、统计量、临界区和p值到决策。第一类错误、第二类错误和功效揭示阈值取舍。检验结果必须与效应大小和实际后果一起解释。 本章以<Term def="作为基准被检验的参数声明，统计量分布在其成立时可计算。">原假设</Term>、<Term def="希望检测的偏离方向，可为单侧或双侧并应预先指定。">备择假设</Term>、<Term def="长期第一类错误率上限，决定拒绝区域。">显著性水平</Term>、<Term def="原假设下观察到当前或更极端统计量的概率。">p值</Term>、<Term def="备择真实时正确拒绝原假设的概率，受效应、样本量和噪声影响。">功效</Term>建立从数据到证据的链条。

<HfsHypothesisTestsDataLab />

## 原书章节定位

本页一一对应O'Reilly原书“第13章 假设检验：查看证据”。六步检验流程从原假设、备择假设、统计量、临界区和p值到决策。第一类错误、第二类错误和功效揭示阈值取舍。检验结果必须与效应大小和实际后果一起解释。 原书的故事负责建立直觉；本页进一步要求写出总体、样本、参数和统计量，并用公式、代码与反例验收直觉。

### 核心单元

1. **原假设**：作为基准被检验的参数声明，统计量分布在其成立时可计算。
2. **备择假设**：希望检测的偏离方向，可为单侧或双侧并应预先指定。
3. **显著性水平**：长期第一类错误率上限，决定拒绝区域。
4. **p值**：原假设下观察到当前或更极端统计量的概率。
5. **功效**：备择真实时正确拒绝原假设的概率，受效应、样本量和噪声影响。

## 从问题开始

先写五个可反驳问题：原假设描述谁，备择假设如何改变表示，显著性水平依赖什么随机机制，p值在哪些边界失效，功效留下什么验收证据。统计计算只有在总体、观察单位、时间窗和缺失规则明确后才有意义。作为基准被检验的参数声明，统计量分布在其成立时可计算。 希望检测的偏离方向，可为单侧或双侧并应预先指定。 两者若来自不同总体，任何后续公式都失去解释。

$$
\mathcal D=(\text{population},\text{unit},\text{window},\text{measurement})
$$

数据契约D把样本的来源固定下来。接着用显著性水平表达结构，并由p值标记前提。功效必须能由另一位读者在同一数据版本上重算。不能先看结果再改变分组、尾部方向或排除条件。

## 反例与敏感性

正常样例应覆盖原书中心场景，边界样例至少包括空组、单样本、极端值、概率接近零或一、强相关与分布偏斜。再以“把未拒绝原假设写成证明原假设为真；可能只是功效不足。”构造反例，记录第一个失效的假设。药物或产品实验必须预注册主指标、方向、样本量和停止规则。反复窥视数据会提高误报率。报告应包含效应、区间、p值、功效和不良后果，避免“显著即重要”。

$$
\varepsilon=\operatorname{estimate}-\operatorname{target}
$$

误差可能来自抽样波动、选择偏差、测量误差、模型偏差和浮点实现。增加样本通常只降低抽样波动，不能修复抽样框缺失或错误模型。敏感性分析要分别改变分组、异常值规则和模型假设，观察结论方向与区间是否稳定。

## 验收证书

证书包括原始计数、原假设定义、备择假设变换、显著性水平计算、p值边界和功效结论。每个百分比都能回到分子分母，每个图都标明尺度和样本量，每个检验都给效应与区间，每个随机过程都记录种子。

$$
\operatorname{accept}=\text{assumptions valid}\land\text{error within budget}\land\text{result reproducible}
$$

预算在看数据前定义。未通过时要区分数据不足、模型不适用、效果不足和实现错误；“未显著”不能笼统翻译为“没有差异”。

## 可核查推导

$$
p=P(T\text{ at least as extreme as }T_{obs}\mid H_0)
$$

先在H0下得到统计量分布，再根据备择方向定义“更极端”。p值是该尾部面积。若p不大于alpha则拒绝H0；这个规则控制重复实验中的第一类错误，并不直接给H0为真的概率。 每一步都要注明使用定义、独立性、分布假设还是渐近近似；若只在大样本下成立，就不能把小样本结果包装成精确结论。

```python
def decision(p_value, alpha=.05):
    return "reject" if p_value <= alpha else "do-not-reject"

assert decision(.03)=="reject"
assert decision(.20)=="do-not-reject"
```

程序只检查有限实例。解析公式、模拟频率和独立实现应三路对照；若三者不一致，先检查参数化、端点、自由度和随机种子。

## 机制与数据流

**原假设**：作为基准被检验的参数声明，统计量分布在其成立时可计算。

**备择假设**：希望检测的偏离方向，可为单侧或双侧并应预先指定。

**显著性水平**：长期第一类错误率上限，决定拒绝区域。

数据流写成“总体 → 抽样或观察 → 统计量 → 抽样分布 → 决策”。任何聚合都保留分母、缺失数和分组规则，避免统计量脱离生成过程。

<HfsHypothesisTestsModelLab />

## 边界与失败模式

**p值**：原假设下观察到当前或更极端统计量的概率。

**功效**：备择真实时正确拒绝原假设的概率，受效应、样本量和噪声影响。

边界测试包括零样本、单样本、不平衡类别、概率极端、离群点、强偏态、相关观测和数值退化。失败应返回明确状态，不能用NaN或一张空图悄悄结束。

```python
experiment={"claim":"原假设到功效","normal":"假设满足","boundary":"退化或偏斜输入","counterexample":"把未拒绝原假设写成证明原假设为真；可能只是功效不足。"}
assert len(experiment)==4
```

## 工程案例

药物或产品实验必须预注册主指标、方向、样本量和停止规则。反复窥视数据会提高误报率。报告应包含效应、区间、p值、功效和不良后果，避免“显著即重要”。 报告必须同时给原始计数、效应、误差或区间、模型诊断和未覆盖范围。图表与结论都绑定数据版本，才能在新数据到来后判断变化来自现实还是代码。

<HfsHypothesisTestsEvidenceLab />

```python
evidence=[("原假设","definition"),("显著性水平","derivation"),("功效","reproduction")]
assert len({name for name,_ in evidence})==3
```

## 分步实验

<Stepper>
  <Step title="审计数据"><HfsHypothesisTestsDataLab /></Step>
  <Step title="破坏假设"><HfsHypothesisTestsModelLab /></Step>
  <Step title="保存证据"><HfsHypothesisTestsEvidenceLab /></Step>
</Stepper>

1. 保存最小手算样例和原始分子分母。
2. 加入空组、极端值、偏态和相关输入，解释哪条假设失效。
3. 用固定种子模拟并与解析结果比较误差。
4. 重建环境重跑，保存数据摘要、参数、代码版本和结论。

## 常见误区

<Callout type="trap" title="统计对象错位">把未拒绝原假设写成证明原假设为真；可能只是功效不足。 修复时先回到总体、单位和生成机制。</Callout>

<Callout type="trap" title="单一数字替代证据">实验后才选择单侧方向或删指标，破坏预先错误率控制。 应补充图形、区间、诊断与反例。</Callout>

## 本章回顾

本章覆盖原假设、备择假设、显著性水平、p值、功效。掌握标准是能从原始计数重建公式，说明抽样或分布假设，用代码复查并在假设破坏时给出可解释失败。

<Exercises>
**问题 1：原假设与备择假设如何协作？**
<Answer>作为基准被检验的参数声明，统计量分布在其成立时可计算。 希望检测的偏离方向，可为单侧或双侧并应预先指定。 两者必须对应同一总体与观察单位。</Answer>

**问题 2：如何从显著性水平推到p值？**
<Answer>先在H0下得到统计量分布，再根据备择方向定义“更极端”。p值是该尾部面积。若p不大于alpha则拒绝H0；这个规则控制重复实验中的第一类错误，并不直接给H0为真的概率。 推导需逐步标注假设并用小样例复算。</Answer>

**问题 3：怎样验收功效？**
<Answer>备择真实时正确拒绝原假设的概率，受效应、样本量和噪声影响。 使用正常、空、极端、偏态和相关输入，保存效应、误差与反例。</Answer>
</Exercises>

## 术语表

<Glossary>
  <GlossaryItem term="原假设" definition="作为基准被检验的参数声明，统计量分布在其成立时可计算。" />
  <GlossaryItem term="备择假设" definition="希望检测的偏离方向，可为单侧或双侧并应预先指定。" />
  <GlossaryItem term="显著性水平" definition="长期第一类错误率上限，决定拒绝区域。" />
  <GlossaryItem term="p值" definition="原假设下观察到当前或更极端统计量的概率。" />
  <GlossaryItem term="功效" definition="备择真实时正确拒绝原假设的概率，受效应、样本量和噪声影响。" />
  <GlossaryItem term="统计证据" definition="把数据来源、模型、效应、不确定性和反例一起交付。" />
</Glossary>

<Attribution adaptedFrom="Dawn Griffiths · Head First Statistics · 第13章 假设检验：查看证据" adaptedUrl="https://www.oreilly.com/library/view/head-first-statistics/9780596527587/" />
