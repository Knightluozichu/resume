---
title: "《深入浅出统计学》15章总复习"
type: C
section: "深入浅出统计学 · 总复习"
order: 16
description: "按O'Reilly权威目录重建，覆盖数据审计、模型选择、抽样设计、误差控制、可复现报告。"
demo: true
math: true
sourceUrl: "https://www.oreilly.com/library/view/head-first-statistics/9780596527587/"
draft: true
---

import { Objectives, Callout, Attribution, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step } from "@/components/mdx/mdx-components";

<Objectives>
- 能解释数据审计与模型选择的统计对象和适用边界
- 能推导抽样设计与误差控制的关键关系
- 能实现并验证围绕可复现报告的可复现实验
</Objectives>

## 先做预测

先预测样本量翻倍、极端值出现或随机机制改变时，图形、估计与结论会怎样变化。总复习要求从问题类型选择图表、分布、抽样与推断方法，并把效应、误差和失败边界放在同一份报告中。15章的知识必须形成从数据生成到决策的闭环。 本章以<Term def="核对单位、缺失、分组和图形形态。">数据审计</Term>、<Term def="从生成机制而不是曲线外观选择分布。">模型选择</Term>、<Term def="让样本选择过程支持目标总体推断。">抽样设计</Term>、<Term def="同时报告区间、第一二类错误和功效。">误差控制</Term>、<Term def="保存数据版本、代码、种子和未覆盖范围。">可复现报告</Term>建立从数据到证据的链条。

<HfsOfficialFinalReviewDataLab />

## 原书章节定位

本页一一对应O'Reilly原书“《深入浅出统计学》15章总复习”。总复习要求从问题类型选择图表、分布、抽样与推断方法，并把效应、误差和失败边界放在同一份报告中。15章的知识必须形成从数据生成到决策的闭环。 原书的故事负责建立直觉；本页进一步要求写出总体、样本、参数和统计量，并用公式、代码与反例验收直觉。

### 核心单元

1. **数据审计**：核对单位、缺失、分组和图形形态。
2. **模型选择**：从生成机制而不是曲线外观选择分布。
3. **抽样设计**：让样本选择过程支持目标总体推断。
4. **误差控制**：同时报告区间、第一二类错误和功效。
5. **可复现报告**：保存数据版本、代码、种子和未覆盖范围。

## 从问题开始

先写五个可反驳问题：数据审计描述谁，模型选择如何改变表示，抽样设计依赖什么随机机制，误差控制在哪些边界失效，可复现报告留下什么验收证据。统计计算只有在总体、观察单位、时间窗和缺失规则明确后才有意义。核对单位、缺失、分组和图形形态。 从生成机制而不是曲线外观选择分布。 两者若来自不同总体，任何后续公式都失去解释。

$$
\mathcal D=(\text{population},\text{unit},\text{window},\text{measurement})
$$

数据契约D把样本的来源固定下来。接着用抽样设计表达结构，并由误差控制标记前提。可复现报告必须能由另一位读者在同一数据版本上重算。不能先看结果再改变分组、尾部方向或排除条件。

## 反例与敏感性

正常样例应覆盖原书中心场景，边界样例至少包括空组、单样本、极端值、概率接近零或一、强相关与分布偏斜。再以“把统计显著当业务显著或因果证明。”构造反例，记录第一个失效的假设。综合验收用一次A/B实验：检查分流与缺失，画连续指标分布和分类列联表，估计均值差与区间，预设检验和功效，再用回归检查协变量。结果附效应、残差和敏感性分析。

$$
\varepsilon=\operatorname{estimate}-\operatorname{target}
$$

误差可能来自抽样波动、选择偏差、测量误差、模型偏差和浮点实现。增加样本通常只降低抽样波动，不能修复抽样框缺失或错误模型。敏感性分析要分别改变分组、异常值规则和模型假设，观察结论方向与区间是否稳定。

## 验收证书

证书包括原始计数、数据审计定义、模型选择变换、抽样设计计算、误差控制边界和可复现报告结论。每个百分比都能回到分子分母，每个图都标明尺度和样本量，每个检验都给效应与区间，每个随机过程都记录种子。

$$
\operatorname{accept}=\text{assumptions valid}\land\text{error within budget}\land\text{result reproducible}
$$

预算在看数据前定义。未通过时要区分数据不足、模型不适用、效果不足和实现错误；“未显著”不能笼统翻译为“没有差异”。

## 可核查推导

$$
\text{claim}+\text{uncertainty}+\text{assumptions}+\text{counterexample}=\text{statistical evidence}
$$

完整推断先定义总体和统计单位，再探索分布；随后写估计目标和抽样分布，最后才计算区间或检验。回归与卡方也必须回到数据生成机制，显著结果不能越过关联与因果的边界。 每一步都要注明使用定义、独立性、分布假设还是渐近近似；若只在大样本下成立，就不能把小样本结果包装成精确结论。

```python
report={"data_audit":True,"effect":0.03,"interval":(-0.01,0.07),"power":0.8,"reproducible":True}
assert report["data_audit"] and report["reproducible"]
```

程序只检查有限实例。解析公式、模拟频率和独立实现应三路对照；若三者不一致，先检查参数化、端点、自由度和随机种子。

## 机制与数据流

**数据审计**：核对单位、缺失、分组和图形形态。

**模型选择**：从生成机制而不是曲线外观选择分布。

**抽样设计**：让样本选择过程支持目标总体推断。

数据流写成“总体 → 抽样或观察 → 统计量 → 抽样分布 → 决策”。任何聚合都保留分母、缺失数和分组规则，避免统计量脱离生成过程。

<HfsOfficialFinalReviewModelLab />

## 边界与失败模式

**误差控制**：同时报告区间、第一二类错误和功效。

**可复现报告**：保存数据版本、代码、种子和未覆盖范围。

边界测试包括零样本、单样本、不平衡类别、概率极端、离群点、强偏态、相关观测和数值退化。失败应返回明确状态，不能用NaN或一张空图悄悄结束。

```python
experiment={"claim":"数据审计到可复现报告","normal":"假设满足","boundary":"退化或偏斜输入","counterexample":"把统计显著当业务显著或因果证明。"}
assert len(experiment)==4
```

## 工程案例

综合验收用一次A/B实验：检查分流与缺失，画连续指标分布和分类列联表，估计均值差与区间，预设检验和功效，再用回归检查协变量。结果附效应、残差和敏感性分析。 报告必须同时给原始计数、效应、误差或区间、模型诊断和未覆盖范围。图表与结论都绑定数据版本，才能在新数据到来后判断变化来自现实还是代码。

```python
evidence=[("数据审计","definition"),("抽样设计","derivation"),("可复现报告","reproduction")]
assert len({name for name,_ in evidence})==3
```

1. 保存最小手算样例和原始分子分母。
2. 加入空组、极端值、偏态和相关输入，解释哪条假设失效。
3. 用固定种子模拟并与解析结果比较误差。
4. 重建环境重跑，保存数据摘要、参数、代码版本和结论。

## 常见误区

<Callout type="trap" title="统计对象错位">把统计显著当业务显著或因果证明。 修复时先回到总体、单位和生成机制。</Callout>

<Callout type="trap" title="单一数字替代证据">只发布结论截图，不保留数据版本、排除规则和代码。 应补充图形、区间、诊断与反例。</Callout>

## 本章回顾

本章覆盖数据审计、模型选择、抽样设计、误差控制、可复现报告。掌握标准是能从原始计数重建公式，说明抽样或分布假设，用代码复查并在假设破坏时给出可解释失败。

<Exercises>
**问题 1：数据审计与模型选择如何协作？**
<Answer>核对单位、缺失、分组和图形形态。 从生成机制而不是曲线外观选择分布。 两者必须对应同一总体与观察单位。</Answer>

**问题 2：如何从抽样设计推到误差控制？**
<Answer>完整推断先定义总体和统计单位，再探索分布；随后写估计目标和抽样分布，最后才计算区间或检验。回归与卡方也必须回到数据生成机制，显著结果不能越过关联与因果的边界。 推导需逐步标注假设并用小样例复算。</Answer>

**问题 3：怎样验收可复现报告？**
<Answer>保存数据版本、代码、种子和未覆盖范围。 使用正常、空、极端、偏态和相关输入，保存效应、误差与反例。</Answer>
</Exercises>

## 术语表

<Glossary>
  <GlossaryItem term="数据审计" definition="核对单位、缺失、分组和图形形态。" />
  <GlossaryItem term="模型选择" definition="从生成机制而不是曲线外观选择分布。" />
  <GlossaryItem term="抽样设计" definition="让样本选择过程支持目标总体推断。" />
  <GlossaryItem term="误差控制" definition="同时报告区间、第一二类错误和功效。" />
  <GlossaryItem term="可复现报告" definition="保存数据版本、代码、种子和未覆盖范围。" />
  <GlossaryItem term="统计证据" definition="把数据来源、模型、效应、不确定性和反例一起交付。" />
</Glossary>

<Attribution adaptedFrom="Dawn Griffiths · Head First Statistics · 《深入浅出统计学》15章总复习" adaptedUrl="https://www.oreilly.com/library/view/head-first-statistics/9780596527587/" />
