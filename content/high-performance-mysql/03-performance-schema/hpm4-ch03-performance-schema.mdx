---
title: "第3章 Performance Schema"
description: "依据第4版完整目录覆盖24个节点：用插桩、消费者表和sys视图把线程、语句、等待、锁、内存与错误连接起来"
type: C
section: "高性能MySQL（第4版 / 2022）"
draft: true
math: false
sourceUrl: "https://www.oreilly.com/library/view/gao-xing-neng-mysql-di-4ban/9787121442575/"
demo: true
order: 0
---

import {
  Hpm4Ch03PerformanceSchemaSloLab,
  Hpm4Ch03PerformanceSchemaCapacityLab,
  Hpm4Ch03PerformanceSchemaEvidenceLab,
} from "@/components/mdx/high-performance-mysql/diagrams/hpm4-ch03-performance-schema";

# 第3章 Performance Schema

本课程对应Silvia Botros、Jeremy Tinley著，宁海元、周振兴、张新铭译《高性能MySQL（第4版）》，电子工业出版社2022年9月出版，纸书344页，ISBN 9787121442575。原版High Performance MySQL: Proven Strategies for Operating at Scale由O'Reilly于2021年11月发布，386页，ISBN 9781492080503。

官方中英文目录确认第4版为13章与附录A、B。课程按15个正式单元逐项重构，不沿用第3版的14章结构，也不复制原文；另设学习地图与总复习，共17页。本页覆盖24个正式节点。

<Objectives>

- 能解释“第3章 Performance Schema”的24个目录节点，并连接到“用插桩、消费者表和sys视图把线程、语句、等待、锁、内存与错误连接起来”主线。
- 能复现Performance Schema配置清单、热点语句报告、元数据锁图和开销对照，保存MySQL版本、Schema、数据量、配置、负载、预期与实际结果。
- 能比较基线、压力与故障条件，证明“观测开销受控，摘要维度与时间窗口明确，重置和采样不会被误读为业务归零”。
- 能设计反例推翻“一次性开启全部插桩后直接比较历史数据，忽略内存、采样、摘要重置与观测开销”，并给出上线门、回退与独立验收条件。

</Objectives>

## 从用户SLO和一次故障开始

先预测：在30天窗口内，目标请求成功率、P95与P99延迟、允许错误分钟和恢复时间各是多少？再描述本章机制如何影响这些结果。不要先看图表再给故事；先固定服务目标、MySQL版本、数据规模、并发、缓存状态和故障点，实验后才允许用证据修正模型。

本页主问题是“用插桩、消费者表和sys视图把线程、语句、等待、锁、内存与错误连接起来”。最终交付不是一组建议，而是Performance Schema配置清单、热点语句报告、元数据锁图和开销对照，由另一位工程师证明：**观测开销受控，摘要维度与时间窗口明确，重置和采样不会被误读为业务归零**。

## 核心词汇与运行边界

<Term def="插桩是“用插桩、消费者表和sys视图把线程、语句、等待、锁、内存与错误连接起来”中的第1个核心概念；必须由版本、负载、机制、反例和指标定义">插桩</Term>、<Term def="消费者表是“用插桩、消费者表和sys视图把线程、语句、等待、锁、内存与错误连接起来”中的第2个核心概念；必须由版本、负载、机制、反例和指标定义">消费者表</Term>、<Term def="等待事件是“用插桩、消费者表和sys视图把线程、语句、等待、锁、内存与错误连接起来”中的第3个核心概念；必须由版本、负载、机制、反例和指标定义">等待事件</Term>、<Term def="sys Schema是“用插桩、消费者表和sys视图把线程、语句、等待、锁、内存与错误连接起来”中的第4个核心概念；必须由版本、负载、机制、反例和指标定义">sys Schema</Term>、<Term def="摘要表是“用插桩、消费者表和sys视图把线程、语句、等待、锁、内存与错误连接起来”中的第5个核心概念；必须由版本、负载、机制、反例和指标定义">摘要表</Term>
构成本页词汇。每个词都要回答它改变哪个用户指标、由哪个MySQL或基础设施模块实现、在哪个压力或故障边界失效，以及用哪条独立证据发现失效。

<Hpm4Ch03PerformanceSchemaSloLab />

## 第4版目录逐节点映射

### 3.1 Performance Schema介绍

**节点合同 1/24 · 定义用户影响。** 先写“3.1 Performance Schema介绍”影响的用户请求、MySQL状态和资源，再把它接入“用插桩、消费者表和sys视图把线程、语句、等待、锁、内存与错误连接起来”。上游是“本章服务目标”，下游是“3.1.1 插桩元件”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“观测开销受控，摘要维度与时间窗口明确，重置和采样不会被误读为业务归零”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 3.1.1 插桩元件

**节点合同 2/24 · 固定实验输入。** 先写“3.1.1 插桩元件”影响的用户请求、MySQL状态和资源，再把它接入“用插桩、消费者表和sys视图把线程、语句、等待、锁、内存与错误连接起来”。上游是“3.1 Performance Schema介绍”，下游是“3.1.2 消费者表的组织”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“观测开销受控，摘要维度与时间窗口明确，重置和采样不会被误读为业务归零”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 3.1.2 消费者表的组织

**节点合同 3/24 · 定位责任层。** 先写“3.1.2 消费者表的组织”影响的用户请求、MySQL状态和资源，再把它接入“用插桩、消费者表和sys视图把线程、语句、等待、锁、内存与错误连接起来”。上游是“3.1.1 插桩元件”，下游是“3.1.3 资源消耗”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“观测开销受控，摘要维度与时间窗口明确，重置和采样不会被误读为业务归零”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 3.1.3 资源消耗

**节点合同 4/24 · 推演正常路径。** 先写“3.1.3 资源消耗”影响的用户请求、MySQL状态和资源，再把它接入“用插桩、消费者表和sys视图把线程、语句、等待、锁、内存与错误连接起来”。上游是“3.1.2 消费者表的组织”，下游是“3.1.4 局限性”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“观测开销受控，摘要维度与时间窗口明确，重置和采样不会被误读为业务归零”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 3.1.4 局限性

**节点合同 5/24 · 注入失败条件。** 先写“3.1.4 局限性”影响的用户请求、MySQL状态和资源，再把它接入“用插桩、消费者表和sys视图把线程、语句、等待、锁、内存与错误连接起来”。上游是“3.1.3 资源消耗”，下游是“3.1.5 sys Schema”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“观测开销受控，摘要维度与时间窗口明确，重置和采样不会被误读为业务归零”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 3.1.5 sys Schema

**节点合同 6/24 · 收集独立证据。** 先写“3.1.5 sys Schema”影响的用户请求、MySQL状态和资源，再把它接入“用插桩、消费者表和sys视图把线程、语句、等待、锁、内存与错误连接起来”。上游是“3.1.4 局限性”，下游是“3.1.6 理解线程”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“观测开销受控，摘要维度与时间窗口明确，重置和采样不会被误读为业务归零”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 3.1.6 理解线程

**节点合同 7/24 · 定义用户影响。** 先写“3.1.6 理解线程”影响的用户请求、MySQL状态和资源，再把它接入“用插桩、消费者表和sys视图把线程、语句、等待、锁、内存与错误连接起来”。上游是“3.1.5 sys Schema”，下游是“3.2 配置”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“观测开销受控，摘要维度与时间窗口明确，重置和采样不会被误读为业务归零”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 3.2 配置

**节点合同 8/24 · 固定实验输入。** 先写“3.2 配置”影响的用户请求、MySQL状态和资源，再把它接入“用插桩、消费者表和sys视图把线程、语句、等待、锁、内存与错误连接起来”。上游是“3.1.6 理解线程”，下游是“3.2.1 启用或禁用Performance Schema”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“观测开销受控，摘要维度与时间窗口明确，重置和采样不会被误读为业务归零”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 3.2.1 启用或禁用Performance Schema

**节点合同 9/24 · 定位责任层。** 先写“3.2.1 启用或禁用Performance Schema”影响的用户请求、MySQL状态和资源，再把它接入“用插桩、消费者表和sys视图把线程、语句、等待、锁、内存与错误连接起来”。上游是“3.2 配置”，下游是“3.2.2 启用或禁用插桩”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“观测开销受控，摘要维度与时间窗口明确，重置和采样不会被误读为业务归零”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 3.2.2 启用或禁用插桩

**节点合同 10/24 · 推演正常路径。** 先写“3.2.2 启用或禁用插桩”影响的用户请求、MySQL状态和资源，再把它接入“用插桩、消费者表和sys视图把线程、语句、等待、锁、内存与错误连接起来”。上游是“3.2.1 启用或禁用Performance Schema”，下游是“3.2.3 启用或禁用消费者表”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“观测开销受控，摘要维度与时间窗口明确，重置和采样不会被误读为业务归零”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 3.2.3 启用或禁用消费者表

**节点合同 11/24 · 注入失败条件。** 先写“3.2.3 启用或禁用消费者表”影响的用户请求、MySQL状态和资源，再把它接入“用插桩、消费者表和sys视图把线程、语句、等待、锁、内存与错误连接起来”。上游是“3.2.2 启用或禁用插桩”，下游是“3.2.4 优化特定对象的监控”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“观测开销受控，摘要维度与时间窗口明确，重置和采样不会被误读为业务归零”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 3.2.4 优化特定对象的监控

**节点合同 12/24 · 收集独立证据。** 先写“3.2.4 优化特定对象的监控”影响的用户请求、MySQL状态和资源，再把它接入“用插桩、消费者表和sys视图把线程、语句、等待、锁、内存与错误连接起来”。上游是“3.2.3 启用或禁用消费者表”，下游是“3.2.5 优化线程监控”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“观测开销受控，摘要维度与时间窗口明确，重置和采样不会被误读为业务归零”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 3.2.5 优化线程监控

**节点合同 13/24 · 定义用户影响。** 先写“3.2.5 优化线程监控”影响的用户请求、MySQL状态和资源，再把它接入“用插桩、消费者表和sys视图把线程、语句、等待、锁、内存与错误连接起来”。上游是“3.2.4 优化特定对象的监控”，下游是“3.2.6 调整Performance Schema内存”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“观测开销受控，摘要维度与时间窗口明确，重置和采样不会被误读为业务归零”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 3.2.6 调整Performance Schema内存

**节点合同 14/24 · 固定实验输入。** 先写“3.2.6 调整Performance Schema内存”影响的用户请求、MySQL状态和资源，再把它接入“用插桩、消费者表和sys视图把线程、语句、等待、锁、内存与错误连接起来”。上游是“3.2.5 优化线程监控”，下游是“3.2.7 默认值”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“观测开销受控，摘要维度与时间窗口明确，重置和采样不会被误读为业务归零”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 3.2.7 默认值

**节点合同 15/24 · 定位责任层。** 先写“3.2.7 默认值”影响的用户请求、MySQL状态和资源，再把它接入“用插桩、消费者表和sys视图把线程、语句、等待、锁、内存与错误连接起来”。上游是“3.2.6 调整Performance Schema内存”，下游是“3.3 使用Performance Schema”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“观测开销受控，摘要维度与时间窗口明确，重置和采样不会被误读为业务归零”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 3.3 使用Performance Schema

**节点合同 16/24 · 推演正常路径。** 先写“3.3 使用Performance Schema”影响的用户请求、MySQL状态和资源，再把它接入“用插桩、消费者表和sys视图把线程、语句、等待、锁、内存与错误连接起来”。上游是“3.2.7 默认值”，下游是“3.3.1 检查SQL语句”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“观测开销受控，摘要维度与时间窗口明确，重置和采样不会被误读为业务归零”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 3.3.1 检查SQL语句

**节点合同 17/24 · 注入失败条件。** 先写“3.3.1 检查SQL语句”影响的用户请求、MySQL状态和资源，再把它接入“用插桩、消费者表和sys视图把线程、语句、等待、锁、内存与错误连接起来”。上游是“3.3 使用Performance Schema”，下游是“3.3.2 检查读写性能”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“观测开销受控，摘要维度与时间窗口明确，重置和采样不会被误读为业务归零”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 3.3.2 检查读写性能

**节点合同 18/24 · 收集独立证据。** 先写“3.3.2 检查读写性能”影响的用户请求、MySQL状态和资源，再把它接入“用插桩、消费者表和sys视图把线程、语句、等待、锁、内存与错误连接起来”。上游是“3.3.1 检查SQL语句”，下游是“3.3.3 检查元数据锁”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“观测开销受控，摘要维度与时间窗口明确，重置和采样不会被误读为业务归零”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 3.3.3 检查元数据锁

**节点合同 19/24 · 定义用户影响。** 先写“3.3.3 检查元数据锁”影响的用户请求、MySQL状态和资源，再把它接入“用插桩、消费者表和sys视图把线程、语句、等待、锁、内存与错误连接起来”。上游是“3.3.2 检查读写性能”，下游是“3.3.4 检查内存使用”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“观测开销受控，摘要维度与时间窗口明确，重置和采样不会被误读为业务归零”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 3.3.4 检查内存使用

**节点合同 20/24 · 固定实验输入。** 先写“3.3.4 检查内存使用”影响的用户请求、MySQL状态和资源，再把它接入“用插桩、消费者表和sys视图把线程、语句、等待、锁、内存与错误连接起来”。上游是“3.3.3 检查元数据锁”，下游是“3.3.5 检查变量”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“观测开销受控，摘要维度与时间窗口明确，重置和采样不会被误读为业务归零”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 3.3.5 检查变量

**节点合同 21/24 · 定位责任层。** 先写“3.3.5 检查变量”影响的用户请求、MySQL状态和资源，再把它接入“用插桩、消费者表和sys视图把线程、语句、等待、锁、内存与错误连接起来”。上游是“3.3.4 检查内存使用”，下游是“3.3.6 检查最常见错误”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“观测开销受控，摘要维度与时间窗口明确，重置和采样不会被误读为业务归零”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 3.3.6 检查最常见错误

**节点合同 22/24 · 推演正常路径。** 先写“3.3.6 检查最常见错误”影响的用户请求、MySQL状态和资源，再把它接入“用插桩、消费者表和sys视图把线程、语句、等待、锁、内存与错误连接起来”。上游是“3.3.5 检查变量”，下游是“3.3.7 检查Performance Schema自身”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“观测开销受控，摘要维度与时间窗口明确，重置和采样不会被误读为业务归零”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 3.3.7 检查Performance Schema自身

**节点合同 23/24 · 注入失败条件。** 先写“3.3.7 检查Performance Schema自身”影响的用户请求、MySQL状态和资源，再把它接入“用插桩、消费者表和sys视图把线程、语句、等待、锁、内存与错误连接起来”。上游是“3.3.6 检查最常见错误”，下游是“3.4 小结”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“观测开销受控，摘要维度与时间窗口明确，重置和采样不会被误读为业务归零”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 3.4 小结

**节点合同 24/24 · 收集独立证据。** 先写“3.4 小结”影响的用户请求、MySQL状态和资源，再把它接入“用插桩、消费者表和sys视图把线程、语句、等待、锁、内存与错误连接起来”。上游是“3.3.7 检查Performance Schema自身”，下游是“本章生产交付物”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“观测开销受控，摘要维度与时间窗口明确，重置和采样不会被误读为业务归零”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

## 机制推演：从请求到资源与恢复

Performance Schema在服务端插桩点记录事件，再由current、history和summary等消费者表保存不同粒度。sys Schema把复杂表连接成诊断视图；使用时要先选问题，再启用最小插桩和消费者，最后记录重置时刻与开销。

生产推演始终拆成六层：用户请求与SLO、应用连接和事务、MySQL服务层、InnoDB页锁版本与日志、操作系统及硬件、复制备份和路由。先确定等待发生在哪一层，再选择指标和干预；参数、索引、分片与扩容属于不同层，不能用一个方案覆盖所有症状。

性能证据同时包含工作量和等待。工作量包括每秒请求、实际扫描行、返回字节、日志字节和脏页；等待包括CPU调度、I/O、锁、元数据锁、复制回放与队列。只有延迟下降且结果、持久性和错误预算没有恶化，优化才成立。

可靠性证据必须覆盖最坏时间点。对事务在提交前后崩溃，对副本在接收与应用之间中断，对备份在基线与binlog之间恢复，对分片在路由切换时重试。记录最后确认状态、数据损失上界、恢复步骤、恢复耗时和业务对账，避免把进程重新启动当作服务恢复。

## 最小可重放实验

```sql
-- 固定MySQL 8.0版本、会话与样例规模，再采集输入基线
SELECT VERSION() AS mysql_version, @@transaction_isolation AS isolation;
SELECT COUNT(*) AS orders_n, COUNT(DISTINCT customer_id) AS customers_n
FROM orders;
```

运行前写出预计行数、访问路径、锁或版本、日志变化和尾延迟。运行后保存EXPLAIN ANALYZE、相关Performance Schema摘要与业务对账；不要让待测查询自己证明结果正确。

```text
book=高性能MySQL（第4版 / 2022）
unit=hpm4-ch03-performance-schema
mysql=8.0.x | exact patch required
dataset=orders-lab-v1 | rows and distribution fixed
workload=qps, concurrency, read-write ratio, cache state
prediction=result, p95, p99, waits, failure endpoint
artifact=Performance Schema配置清单、热点语句报告、元数据锁图和开销对照
```

```sql
-- 用独立摘要核对语句负载与等待，不只看单次客户端耗时
SELECT DIGEST_TEXT, COUNT_STAR, SUM_TIMER_WAIT, SUM_ROWS_EXAMINED
FROM performance_schema.events_statements_summary_by_digest
ORDER BY SUM_TIMER_WAIT DESC LIMIT 10;
```

## 三步交互实验

<Stepper>
  <Step title="1. 固定SLO与章节节点">
    选择目录节点，先计算错误预算并声明用户可观察结果。
    <Hpm4Ch03PerformanceSchemaSloLab />
  </Step>
  <Step title="2. 改变容量或故障条件">
    保持Schema和查询不变，调节数据、并发与缓存命中，先预测再观察。
    <Hpm4Ch03PerformanceSchemaCapacityLab />
  </Step>
  <Step title="3. 通过生产证据门">
    六类证据齐备后才能宣布优化、扩容、切换或恢复完成。
    <Hpm4Ch03PerformanceSchemaEvidenceLab />
  </Step>
</Stepper>

<Callout type="trap">
  **优化陷阱。**
  一次性开启全部插桩后直接比较历史数据，忽略内存、采样、摘要重置与观测开销。先恢复SLO、版本、工作负载和结果合同，再改变一项设计；不要用单次快结果掩盖尾延迟、数据风险或观测偏差。
</Callout>

<Callout type="trap">
  **因果陷阱。**
  指标同时变化不等于存在因果关系。缓存预热、流量分布、后台任务、复制延迟和云平台噪声都可能改变结果；必须保留对照并一次改变一个变量。
</Callout>

<Callout type="warning">
  DDL、参数、复制切换、恢复、分片和升级实验只在隔离环境执行。先验证备份和回退，规定最大写入、锁等待、错误预算消耗与停止条件。
</Callout>

## 证据解释与独立交接

服务证据固定为什么优化。保存SLI公式、SLO窗口、流量分母、延迟分位数和错误预算燃烧；若用户指标没有改善，底层计数器变漂亮也不能证明价值。缺失采样和维护窗口必须显式处理。

数据库证据解释发生了什么。保存Schema与统计、SQL和参数、计划估计与实际行、锁和版本、redo与binlog位置、状态变量及Performance Schema摘要。重置摘要或重启实例会改变基线，必须记录时间点。

资源证据定位限制。保存CPU利用率与运行队列、内存与交换、磁盘延迟和队列、网络、云盘限额及后台刷新。平均值不能替代分位数和饱和时段，客户端总延迟也不能自动归因于数据库执行。

故障证据证明可恢复。保存故障注入点、最后确认事务、晋升或恢复步骤、RPO/RTO实测、数据对账与旧源隔离。备份任务成功、副本在线或Pod重建都不是业务恢复的充分条件。

## 本章回顾

重新完成“用插桩、消费者表和sys视图把线程、语句、等待、锁、内存与错误连接起来”：先写SLO和结果合同，按24个目录节点建立因果模型，固定负载，采集基线，只改变一个变量，注入失败并独立对账。最终交付Performance Schema配置清单、热点语句报告、元数据锁图和开销对照，证明“观测开销受控，摘要维度与时间窗口明确，重置和采样不会被误读为业务归零”。

## 复习与生产验收

<Exercises>
**问题 1**：为什么“第3章 Performance Schema”必须覆盖24个正式节点？
<Answer>这些节点共同组成“用插桩、消费者表和sys视图把线程、语句、等待、锁、内存与错误连接起来”的服务目标、机制、负载、故障和证据链；缺项会使Performance Schema配置清单、热点语句报告、元数据锁图和开销对照无法复现。</Answer>

**问题 2**：本页最小运行不变量是什么？

<Answer>
  观测开销受控，摘要维度与时间窗口明确，重置和采样不会被误读为业务归零。必须由固定版本和负载、正常轨迹、压力数据、故障反例与业务对账共同证明。
</Answer>

**问题 3**：怎样构造最小反例？

<Answer>
  把“一次性开启全部插桩后直接比较历史数据，忽略内存、采样、摘要重置与观测开销”写成待证假设，保持Schema与查询不变，只改变缓存、并发、数据分布或故障时点之一，再比较用户与数据库证据。
</Answer>

**问题 4**：为什么平均延迟不足以验收优化？

<Answer>
  平均值会隐藏排队、锁、I/O抖动和少数极慢请求；至少需要P95/P99、吞吐、错误率、实际扫描与等待，并确认结果语义不变。
</Answer>

**问题 5**：可靠性变更怎样验证？

<Answer>
  在最坏确认边界注入故障，记录最后确认事务、恢复或切换过程、RPO/RTO、数据一致性、旧源隔离与回退结果。
</Answer>

**问题 6**：独立交接至少包含什么？

<Answer>需要SLO、版本与配置、Schema与数据分布、负载脚本、Performance Schema配置清单、热点语句报告、元数据锁图和开销对照、计划与等待、故障演练、业务对账和回退条件。</Answer>
</Exercises>

<Glossary>
  <GlossaryItem term="插桩">
    插桩以“观测开销受控，摘要维度与时间窗口明确，重置和采样不会被误读为业务归零”为运行边界，并由正常、压力与故障证据共同验证。
  </GlossaryItem>
  <GlossaryItem term="消费者表">
    消费者表以“观测开销受控，摘要维度与时间窗口明确，重置和采样不会被误读为业务归零”为运行边界，并由正常、压力与故障证据共同验证。
  </GlossaryItem>
  <GlossaryItem term="等待事件">
    等待事件以“观测开销受控，摘要维度与时间窗口明确，重置和采样不会被误读为业务归零”为运行边界，并由正常、压力与故障证据共同验证。
  </GlossaryItem>
  <GlossaryItem term="sys Schema">
    sys
    Schema以“观测开销受控，摘要维度与时间窗口明确，重置和采样不会被误读为业务归零”为运行边界，并由正常、压力与故障证据共同验证。
  </GlossaryItem>
  <GlossaryItem term="摘要表">
    摘要表以“观测开销受控，摘要维度与时间窗口明确，重置和采样不会被误读为业务归零”为运行边界，并由正常、压力与故障证据共同验证。
  </GlossaryItem>
</Glossary>

[← 上一页：第2章 可靠性工程世界中的监控](/learn/high-performance-mysql/02-reliability-monitoring/hpm4-ch02-reliability-monitoring) · [下一页：第4章 操作系统和硬件优化 →](/learn/high-performance-mysql/04-os-hardware/hpm4-ch04-os-hardware)

<Attribution
  adaptedFrom="《高性能MySQL（第4版）》"
  adaptedUrl="https://www.oreilly.com/library/view/gao-xing-neng-mysql-di-4ban/9787121442575/"
/>
