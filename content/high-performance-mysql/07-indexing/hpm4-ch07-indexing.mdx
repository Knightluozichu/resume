---
title: "第7章 创建高性能索引"
description: "依据第4版完整目录覆盖18个节点：从访问模式、选择性、列顺序、聚簇布局和覆盖能力设计可维护索引"
type: C
section: "高性能MySQL（第4版 / 2022）"
draft: false
math: false
sourceUrl: "https://www.oreilly.com/library/view/gao-xing-neng-mysql-di-4ban/9787121442575/"
demo: true
order: 0
---

import { Objectives } from "@/components/mdx/objectives";
import { Term } from "@/components/mdx/term";
import { Callout } from "@/components/mdx/callout";
import { Answer, Exercises } from "@/components/mdx/exercises";
import { Glossary, GlossaryItem } from "@/components/mdx/glossary";
import { Attribution } from "@/components/mdx/attribution";

# 第7章 创建高性能索引

本课程对应Silvia Botros、Jeremy Tinley著，宁海元、周振兴、张新铭译《高性能MySQL（第4版）》，电子工业出版社2022年9月出版，纸书344页，ISBN 9787121442575。原版High Performance MySQL: Proven Strategies for Operating at Scale由O'Reilly于2021年11月发布，386页，ISBN 9781492080503。

官方中英文目录确认第4版为13章与附录A、B。课程按15个正式单元逐项重构，不沿用第3版的14章结构，也不复制原文；另设学习地图与总复习，共17页。本页覆盖18个正式节点。

<Objectives>

- 能解释“第7章 创建高性能索引”的18个目录节点，并连接到“从访问模式、选择性、列顺序、聚簇布局和覆盖能力设计可维护索引”主线。
- 能复现候选索引矩阵、EXPLAIN ANALYZE对照、页密度与写入成本报告，保存MySQL版本、Schema、数据量、配置、负载、预期与实际结果。
- 能比较基线、压力与故障条件，证明“目标查询扫描行与回表次数受控，新增索引收益覆盖写放大、空间和维护成本”。
- 能设计反例推翻“看到慢查询就逐列建索引，或把key字段非空误当成所有谓词都由索引完成”，并给出上线门、回退与独立验收条件。

</Objectives>

## 从用户SLO和一次故障开始

先预测：在30天窗口内，目标请求成功率、P95与P99延迟、允许错误分钟和恢复时间各是多少？再描述本章机制如何影响这些结果。不要先看图表再给故事；先固定服务目标、MySQL版本、数据规模、并发、缓存状态和故障点，实验后才允许用证据修正模型。

本页主问题是“从访问模式、选择性、列顺序、聚簇布局和覆盖能力设计可维护索引”。最终交付不是一组建议，而是候选索引矩阵、EXPLAIN ANALYZE对照、页密度与写入成本报告，由另一位工程师证明：**目标查询扫描行与回表次数受控，新增索引收益覆盖写放大、空间和维护成本**。

## 核心词汇与运行边界

<Term def="B+树是“从访问模式、选择性、列顺序、聚簇布局和覆盖能力设计可维护索引”中的第1个核心概念；必须由版本、负载、机制、反例和指标定义">B+树</Term>、<Term def="选择性是“从访问模式、选择性、列顺序、聚簇布局和覆盖能力设计可维护索引”中的第2个核心概念；必须由版本、负载、机制、反例和指标定义">选择性</Term>、<Term def="聚簇索引是“从访问模式、选择性、列顺序、聚簇布局和覆盖能力设计可维护索引”中的第3个核心概念；必须由版本、负载、机制、反例和指标定义">聚簇索引</Term>、<Term def="覆盖索引是“从访问模式、选择性、列顺序、聚簇布局和覆盖能力设计可维护索引”中的第4个核心概念；必须由版本、负载、机制、反例和指标定义">覆盖索引</Term>、<Term def="索引统计是“从访问模式、选择性、列顺序、聚簇布局和覆盖能力设计可维护索引”中的第5个核心概念；必须由版本、负载、机制、反例和指标定义">索引统计</Term>
构成本页词汇。每个词都要回答它改变哪个用户指标、由哪个MySQL或基础设施模块实现、在哪个压力或故障边界失效，以及用哪条独立证据发现失效。


## 第4版目录逐节点映射

### 7.1 索引基础

**节点合同 1/18 · 定义用户影响。** 先写“7.1 索引基础”影响的用户请求、MySQL状态和资源，再把它接入“从访问模式、选择性、列顺序、聚簇布局和覆盖能力设计可维护索引”。上游是“本章服务目标”，下游是“7.1.1 索引类型”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“目标查询扫描行与回表次数受控，新增索引收益覆盖写放大、空间和维护成本”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 7.1.1 索引类型

**节点合同 2/18 · 固定实验输入。** 先写“7.1.1 索引类型”影响的用户请求、MySQL状态和资源，再把它接入“从访问模式、选择性、列顺序、聚簇布局和覆盖能力设计可维护索引”。上游是“7.1 索引基础”，下游是“7.1.2 使用索引的优点”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“目标查询扫描行与回表次数受控，新增索引收益覆盖写放大、空间和维护成本”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 7.1.2 使用索引的优点

**节点合同 3/18 · 定位责任层。** 先写“7.1.2 使用索引的优点”影响的用户请求、MySQL状态和资源，再把它接入“从访问模式、选择性、列顺序、聚簇布局和覆盖能力设计可维护索引”。上游是“7.1.1 索引类型”，下游是“7.2 高性能索引策略”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“目标查询扫描行与回表次数受控，新增索引收益覆盖写放大、空间和维护成本”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 7.2 高性能索引策略

**节点合同 4/18 · 推演正常路径。** 先写“7.2 高性能索引策略”影响的用户请求、MySQL状态和资源，再把它接入“从访问模式、选择性、列顺序、聚簇布局和覆盖能力设计可维护索引”。上游是“7.1.2 使用索引的优点”，下游是“7.2.1 前缀索引与选择性”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“目标查询扫描行与回表次数受控，新增索引收益覆盖写放大、空间和维护成本”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 7.2.1 前缀索引与选择性

**节点合同 5/18 · 注入失败条件。** 先写“7.2.1 前缀索引与选择性”影响的用户请求、MySQL状态和资源，再把它接入“从访问模式、选择性、列顺序、聚簇布局和覆盖能力设计可维护索引”。上游是“7.2 高性能索引策略”，下游是“7.2.2 多列索引”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“目标查询扫描行与回表次数受控，新增索引收益覆盖写放大、空间和维护成本”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 7.2.2 多列索引

**节点合同 6/18 · 收集独立证据。** 先写“7.2.2 多列索引”影响的用户请求、MySQL状态和资源，再把它接入“从访问模式、选择性、列顺序、聚簇布局和覆盖能力设计可维护索引”。上游是“7.2.1 前缀索引与选择性”，下游是“7.2.3 选择合适的索引列顺序”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“目标查询扫描行与回表次数受控，新增索引收益覆盖写放大、空间和维护成本”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 7.2.3 选择合适的索引列顺序

**节点合同 7/18 · 定义用户影响。** 先写“7.2.3 选择合适的索引列顺序”影响的用户请求、MySQL状态和资源，再把它接入“从访问模式、选择性、列顺序、聚簇布局和覆盖能力设计可维护索引”。上游是“7.2.2 多列索引”，下游是“7.2.4 聚簇索引”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“目标查询扫描行与回表次数受控，新增索引收益覆盖写放大、空间和维护成本”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 7.2.4 聚簇索引

**节点合同 8/18 · 固定实验输入。** 先写“7.2.4 聚簇索引”影响的用户请求、MySQL状态和资源，再把它接入“从访问模式、选择性、列顺序、聚簇布局和覆盖能力设计可维护索引”。上游是“7.2.3 选择合适的索引列顺序”，下游是“7.2.5 覆盖索引”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“目标查询扫描行与回表次数受控，新增索引收益覆盖写放大、空间和维护成本”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 7.2.5 覆盖索引

**节点合同 9/18 · 定位责任层。** 先写“7.2.5 覆盖索引”影响的用户请求、MySQL状态和资源，再把它接入“从访问模式、选择性、列顺序、聚簇布局和覆盖能力设计可维护索引”。上游是“7.2.4 聚簇索引”，下游是“7.2.6 使用索引扫描排序”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“目标查询扫描行与回表次数受控，新增索引收益覆盖写放大、空间和维护成本”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 7.2.6 使用索引扫描排序

**节点合同 10/18 · 推演正常路径。** 先写“7.2.6 使用索引扫描排序”影响的用户请求、MySQL状态和资源，再把它接入“从访问模式、选择性、列顺序、聚簇布局和覆盖能力设计可维护索引”。上游是“7.2.5 覆盖索引”，下游是“7.2.7 冗余和重复索引”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“目标查询扫描行与回表次数受控，新增索引收益覆盖写放大、空间和维护成本”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 7.2.7 冗余和重复索引

**节点合同 11/18 · 注入失败条件。** 先写“7.2.7 冗余和重复索引”影响的用户请求、MySQL状态和资源，再把它接入“从访问模式、选择性、列顺序、聚簇布局和覆盖能力设计可维护索引”。上游是“7.2.6 使用索引扫描排序”，下游是“7.2.8 未使用索引”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“目标查询扫描行与回表次数受控，新增索引收益覆盖写放大、空间和维护成本”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 7.2.8 未使用索引

**节点合同 12/18 · 收集独立证据。** 先写“7.2.8 未使用索引”影响的用户请求、MySQL状态和资源，再把它接入“从访问模式、选择性、列顺序、聚簇布局和覆盖能力设计可维护索引”。上游是“7.2.7 冗余和重复索引”，下游是“7.3 维护索引和表”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“目标查询扫描行与回表次数受控，新增索引收益覆盖写放大、空间和维护成本”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 7.3 维护索引和表

**节点合同 13/18 · 定义用户影响。** 先写“7.3 维护索引和表”影响的用户请求、MySQL状态和资源，再把它接入“从访问模式、选择性、列顺序、聚簇布局和覆盖能力设计可维护索引”。上游是“7.2.8 未使用索引”，下游是“7.3.1 找到并修复损坏的表”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“目标查询扫描行与回表次数受控，新增索引收益覆盖写放大、空间和维护成本”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 7.3.1 找到并修复损坏的表

**节点合同 14/18 · 固定实验输入。** 先写“7.3.1 找到并修复损坏的表”影响的用户请求、MySQL状态和资源，再把它接入“从访问模式、选择性、列顺序、聚簇布局和覆盖能力设计可维护索引”。上游是“7.3 维护索引和表”，下游是“7.3.2 更新索引统计信息”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“目标查询扫描行与回表次数受控，新增索引收益覆盖写放大、空间和维护成本”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 7.3.2 更新索引统计信息

**节点合同 15/18 · 定位责任层。** 先写“7.3.2 更新索引统计信息”影响的用户请求、MySQL状态和资源，再把它接入“从访问模式、选择性、列顺序、聚簇布局和覆盖能力设计可维护索引”。上游是“7.3.1 找到并修复损坏的表”，下游是“7.3.3 减少索引与数据碎片”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“目标查询扫描行与回表次数受控，新增索引收益覆盖写放大、空间和维护成本”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 7.3.3 减少索引与数据碎片

**节点合同 16/18 · 推演正常路径。** 先写“7.3.3 减少索引与数据碎片”影响的用户请求、MySQL状态和资源，再把它接入“从访问模式、选择性、列顺序、聚簇布局和覆盖能力设计可维护索引”。上游是“7.3.2 更新索引统计信息”，下游是“7.4 小结”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“目标查询扫描行与回表次数受控，新增索引收益覆盖写放大、空间和维护成本”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 7.4 小结

**节点合同 17/18 · 注入失败条件。** 先写“7.4 小结”影响的用户请求、MySQL状态和资源，再把它接入“从访问模式、选择性、列顺序、聚簇布局和覆盖能力设计可维护索引”。上游是“7.3.3 减少索引与数据碎片”，下游是“7.5 索引设计验收”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“目标查询扫描行与回表次数受控，新增索引收益覆盖写放大、空间和维护成本”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 7.5 索引设计验收

**节点合同 18/18 · 收集独立证据。** 先写“7.5 索引设计验收”影响的用户请求、MySQL状态和资源，再把它接入“从访问模式、选择性、列顺序、聚簇布局和覆盖能力设计可维护索引”。上游是“7.4 小结”，下游是“本章生产交付物”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“目标查询扫描行与回表次数受控，新增索引收益覆盖写放大、空间和维护成本”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

## 机制推演：从请求到资源与恢复

InnoDB主键叶子保存整行，二级索引叶子保存主键，因此主键宽度影响所有索引。多列索引按左侧列有序，列顺序应结合等值、范围、排序与选择性；覆盖索引避免回表，但每个索引都会增加写入和维护。

生产推演始终拆成六层：用户请求与SLO、应用连接和事务、MySQL服务层、InnoDB页锁版本与日志、操作系统及硬件、复制备份和路由。先确定等待发生在哪一层，再选择指标和干预；参数、索引、分片与扩容属于不同层，不能用一个方案覆盖所有症状。

性能证据同时包含工作量和等待。工作量包括每秒请求、实际扫描行、返回字节、日志字节和脏页；等待包括CPU调度、I/O、锁、元数据锁、复制回放与队列。只有延迟下降且结果、持久性和错误预算没有恶化，优化才成立。

可靠性证据必须覆盖最坏时间点。对事务在提交前后崩溃，对副本在接收与应用之间中断，对备份在基线与binlog之间恢复，对分片在路由切换时重试。记录最后确认状态、数据损失上界、恢复步骤、恢复耗时和业务对账，避免把进程重新启动当作服务恢复。

<Callout type="trap">
  **优化陷阱。**
  看到慢查询就逐列建索引，或把key字段非空误当成所有谓词都由索引完成。先恢复SLO、版本、工作负载和结果合同，再改变一项设计；不要用单次快结果掩盖尾延迟、数据风险或观测偏差。
</Callout>

<Callout type="trap">
  **因果陷阱。**
  指标同时变化不等于存在因果关系。缓存预热、流量分布、后台任务、复制延迟和云平台噪声都可能改变结果；必须保留对照并一次改变一个变量。
</Callout>

<Callout type="warning">
  DDL、参数、复制切换、恢复、分片和升级实验只在隔离环境执行。先验证备份和回退，规定最大写入、锁等待、错误预算消耗与停止条件。
</Callout>

## 证据解释与独立交接

服务证据固定为什么优化。保存SLI公式、SLO窗口、流量分母、延迟分位数和错误预算燃烧；若用户指标没有改善，底层计数器变漂亮也不能证明价值。缺失采样和维护窗口必须显式处理。

数据库证据解释发生了什么。保存Schema与统计、SQL和参数、计划估计与实际行、锁和版本、redo与binlog位置、状态变量及Performance Schema摘要。重置摘要或重启实例会改变基线，必须记录时间点。

资源证据定位限制。保存CPU利用率与运行队列、内存与交换、磁盘延迟和队列、网络、云盘限额及后台刷新。平均值不能替代分位数和饱和时段，客户端总延迟也不能自动归因于数据库执行。

故障证据证明可恢复。保存故障注入点、最后确认事务、晋升或恢复步骤、RPO/RTO实测、数据对账与旧源隔离。备份任务成功、副本在线或Pod重建都不是业务恢复的充分条件。

## 本章回顾

重新完成“从访问模式、选择性、列顺序、聚簇布局和覆盖能力设计可维护索引”：先写SLO和结果合同，按18个目录节点建立因果模型，固定负载，采集基线，只改变一个变量，注入失败并独立对账。最终交付候选索引矩阵、EXPLAIN ANALYZE对照、页密度与写入成本报告，证明“目标查询扫描行与回表次数受控，新增索引收益覆盖写放大、空间和维护成本”。

## 小结

- 从访问模式与选择性设计索引
- 列顺序决定索引能服务哪些查询
- 聚簇布局影响主键查找与范围扫
- 覆盖索引避免回表读
- 索引要可维护，不是越多越好

## 复习与生产验收

<Exercises>
**问题 1**：为什么“第7章 创建高性能索引”必须覆盖18个正式节点？

<Answer>这些节点共同组成“从访问模式、选择性、列顺序、聚簇布局和覆盖能力设计可维护索引”的服务目标、机制、负载、故障和证据链；缺项会使候选索引矩阵、EXPLAIN ANALYZE对照、页密度与写入成本报告无法复现。</Answer>

**问题 2**：本页最小运行不变量是什么？

<Answer>
  目标查询扫描行与回表次数受控，新增索引收益覆盖写放大、空间和维护成本。必须由固定版本和负载、正常轨迹、压力数据、故障反例与业务对账共同证明。
</Answer>

**问题 3**：怎样构造最小反例？

<Answer>
  把“看到慢查询就逐列建索引，或把key字段非空误当成所有谓词都由索引完成”写成待证假设，保持Schema与查询不变，只改变缓存、并发、数据分布或故障时点之一，再比较用户与数据库证据。
</Answer>

**问题 4**：为什么平均延迟不足以验收优化？

<Answer>
  平均值会隐藏排队、锁、I/O抖动和少数极慢请求；至少需要P95/P99、吞吐、错误率、实际扫描与等待，并确认结果语义不变。
</Answer>

**问题 5**：可靠性变更怎样验证？

<Answer>
  在最坏确认边界注入故障，记录最后确认事务、恢复或切换过程、RPO/RTO、数据一致性、旧源隔离与回退结果。
</Answer>

**问题 6**：独立交接至少包含什么？

<Answer>需要SLO、版本与配置、Schema与数据分布、负载脚本、候选索引矩阵、EXPLAIN ANALYZE对照、页密度与写入成本报告、计划与等待、故障演练、业务对账和回退条件。</Answer>
</Exercises>

<Glossary>
  <GlossaryItem term="B+树">
    B+树以“目标查询扫描行与回表次数受控，新增索引收益覆盖写放大、空间和维护成本”为运行边界，并由正常、压力与故障证据共同验证。
  </GlossaryItem>
  <GlossaryItem term="选择性">
    选择性以“目标查询扫描行与回表次数受控，新增索引收益覆盖写放大、空间和维护成本”为运行边界，并由正常、压力与故障证据共同验证。
  </GlossaryItem>
  <GlossaryItem term="聚簇索引">
    聚簇索引以“目标查询扫描行与回表次数受控，新增索引收益覆盖写放大、空间和维护成本”为运行边界，并由正常、压力与故障证据共同验证。
  </GlossaryItem>
  <GlossaryItem term="覆盖索引">
    覆盖索引以“目标查询扫描行与回表次数受控，新增索引收益覆盖写放大、空间和维护成本”为运行边界，并由正常、压力与故障证据共同验证。
  </GlossaryItem>
  <GlossaryItem term="索引统计">
    索引统计以“目标查询扫描行与回表次数受控，新增索引收益覆盖写放大、空间和维护成本”为运行边界，并由正常、压力与故障证据共同验证。
  </GlossaryItem>
</Glossary>

[← 上一页：第6章 Schema设计与管理](/learn/high-performance-mysql/06-schema-design/hpm4-ch06-schema-design) · [下一页：第8章 查询性能优化 →](/learn/high-performance-mysql/08-query-optimization/hpm4-ch08-query-optimization)

<Attribution
  adaptedFrom="《高性能MySQL（第4版）》"
  adaptedUrl="https://www.oreilly.com/library/view/gao-xing-neng-mysql-di-4ban/9787121442575/"
/>
