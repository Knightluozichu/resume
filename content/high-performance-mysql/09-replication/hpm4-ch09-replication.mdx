---
title: "第9章 复制"
description: "依据第4版完整目录覆盖33个节点：用binlog、GTID、并行回放、半同步与拓扑管理副本进度和故障切换"
type: C
section: "高性能MySQL（第4版 / 2022）"
draft: true
math: false
sourceUrl: "https://www.oreilly.com/library/view/gao-xing-neng-mysql-di-4ban/9787121442575/"
demo: true
order: 0
---

# 第9章 复制

本课程对应Silvia Botros、Jeremy Tinley著，宁海元、周振兴、张新铭译《高性能MySQL（第4版）》，电子工业出版社2022年9月出版，纸书344页，ISBN 9787121442575。原版High Performance MySQL: Proven Strategies for Operating at Scale由O'Reilly于2021年11月发布，386页，ISBN 9781492080503。

官方中英文目录确认第4版为13章与附录A、B。课程按15个正式单元逐项重构，不沿用第3版的14章结构，也不复制原文；另设学习地图与总复习，共17页。本页覆盖33个正式节点。

<Objectives>

- 能解释“第9章 复制”的33个目录节点，并连接到“用binlog、GTID、并行回放、半同步与拓扑管理副本进度和故障切换”主线。
- 能复现GTID集合、延迟分解、切换runbook、一致性校验和故障演练，保存MySQL版本、Schema、数据量、配置、负载、预期与实际结果。
- 能比较基线、压力与故障条件，证明“每个已确认事务在目标拓扑有唯一身份，晋升不会丢失已承诺写入或产生双主分叉”。
- 能设计反例推翻“只看Seconds_Behind_Source判定健康，或在旧源未隔离时直接晋升副本”，并给出上线门、回退与独立验收条件。

</Objectives>

## 从用户SLO和一次故障开始

先预测：在30天窗口内，目标请求成功率、P95与P99延迟、允许错误分钟和恢复时间各是多少？再描述本章机制如何影响这些结果。不要先看图表再给故事；先固定服务目标、MySQL版本、数据规模、并发、缓存状态和故障点，实验后才允许用证据修正模型。

本页主问题是“用binlog、GTID、并行回放、半同步与拓扑管理副本进度和故障切换”。最终交付不是一组建议，而是GTID集合、延迟分解、切换runbook、一致性校验和故障演练，由另一位工程师证明：**每个已确认事务在目标拓扑有唯一身份，晋升不会丢失已承诺写入或产生双主分叉**。

## 核心词汇与运行边界

<Term def="binlog是“用binlog、GTID、并行回放、半同步与拓扑管理副本进度和故障切换”中的第1个核心概念；必须由版本、负载、机制、反例和指标定义">binlog</Term>、<Term def="GTID是“用binlog、GTID、并行回放、半同步与拓扑管理副本进度和故障切换”中的第2个核心概念；必须由版本、负载、机制、反例和指标定义">GTID</Term>、<Term def="复制延迟是“用binlog、GTID、并行回放、半同步与拓扑管理副本进度和故障切换”中的第3个核心概念；必须由版本、负载、机制、反例和指标定义">复制延迟</Term>、<Term def="半同步复制是“用binlog、GTID、并行回放、半同步与拓扑管理副本进度和故障切换”中的第4个核心概念；必须由版本、负载、机制、反例和指标定义">半同步复制</Term>、<Term def="故障切换是“用binlog、GTID、并行回放、半同步与拓扑管理副本进度和故障切换”中的第5个核心概念；必须由版本、负载、机制、反例和指标定义">故障切换</Term>
构成本页词汇。每个词都要回答它改变哪个用户指标、由哪个MySQL或基础设施模块实现、在哪个压力或故障边界失效，以及用哪条独立证据发现失效。

<Hpm4Ch09ReplicationSloLab />

## 第4版目录逐节点映射

### 9.1 复制概述

**节点合同 1/33 · 定义用户影响。** 先写“9.1 复制概述”影响的用户请求、MySQL状态和资源，再把它接入“用binlog、GTID、并行回放、半同步与拓扑管理副本进度和故障切换”。上游是“本章服务目标”，下游是“9.2 复制如何工作”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“每个已确认事务在目标拓扑有唯一身份，晋升不会丢失已承诺写入或产生双主分叉”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 9.2 复制如何工作

**节点合同 2/33 · 固定实验输入。** 先写“9.2 复制如何工作”影响的用户请求、MySQL状态和资源，再把它接入“用binlog、GTID、并行回放、半同步与拓扑管理副本进度和故障切换”。上游是“9.1 复制概述”，下游是“9.3 复制原理”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“每个已确认事务在目标拓扑有唯一身份，晋升不会丢失已承诺写入或产生双主分叉”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 9.3 复制原理

**节点合同 3/33 · 定位责任层。** 先写“9.3 复制原理”影响的用户请求、MySQL状态和资源，再把它接入“用binlog、GTID、并行回放、半同步与拓扑管理副本进度和故障切换”。上游是“9.2 复制如何工作”，下游是“9.3.1 选择复制格式”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“每个已确认事务在目标拓扑有唯一身份，晋升不会丢失已承诺写入或产生双主分叉”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 9.3.1 选择复制格式

**节点合同 4/33 · 推演正常路径。** 先写“9.3.1 选择复制格式”影响的用户请求、MySQL状态和资源，再把它接入“用binlog、GTID、并行回放、半同步与拓扑管理副本进度和故障切换”。上游是“9.3 复制原理”，下游是“9.3.2 全局事务标识符”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“每个已确认事务在目标拓扑有唯一身份，晋升不会丢失已承诺写入或产生双主分叉”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 9.3.2 全局事务标识符

**节点合同 5/33 · 注入失败条件。** 先写“9.3.2 全局事务标识符”影响的用户请求、MySQL状态和资源，再把它接入“用binlog、GTID、并行回放、半同步与拓扑管理副本进度和故障切换”。上游是“9.3.1 选择复制格式”，下游是“9.3.3 崩溃后的复制安全”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“每个已确认事务在目标拓扑有唯一身份，晋升不会丢失已承诺写入或产生双主分叉”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 9.3.3 崩溃后的复制安全

**节点合同 6/33 · 收集独立证据。** 先写“9.3.3 崩溃后的复制安全”影响的用户请求、MySQL状态和资源，再把它接入“用binlog、GTID、并行回放、半同步与拓扑管理副本进度和故障切换”。上游是“9.3.2 全局事务标识符”，下游是“9.3.4 延迟复制”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“每个已确认事务在目标拓扑有唯一身份，晋升不会丢失已承诺写入或产生双主分叉”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 9.3.4 延迟复制

**节点合同 7/33 · 定义用户影响。** 先写“9.3.4 延迟复制”影响的用户请求、MySQL状态和资源，再把它接入“用binlog、GTID、并行回放、半同步与拓扑管理副本进度和故障切换”。上游是“9.3.3 崩溃后的复制安全”，下游是“9.3.5 多线程复制”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“每个已确认事务在目标拓扑有唯一身份，晋升不会丢失已承诺写入或产生双主分叉”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 9.3.5 多线程复制

**节点合同 8/33 · 固定实验输入。** 先写“9.3.5 多线程复制”影响的用户请求、MySQL状态和资源，再把它接入“用binlog、GTID、并行回放、半同步与拓扑管理副本进度和故障切换”。上游是“9.3.4 延迟复制”，下游是“9.3.6 半同步复制”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“每个已确认事务在目标拓扑有唯一身份，晋升不会丢失已承诺写入或产生双主分叉”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 9.3.6 半同步复制

**节点合同 9/33 · 定位责任层。** 先写“9.3.6 半同步复制”影响的用户请求、MySQL状态和资源，再把它接入“用binlog、GTID、并行回放、半同步与拓扑管理副本进度和故障切换”。上游是“9.3.5 多线程复制”，下游是“9.3.7 复制过滤器”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“每个已确认事务在目标拓扑有唯一身份，晋升不会丢失已承诺写入或产生双主分叉”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 9.3.7 复制过滤器

**节点合同 10/33 · 推演正常路径。** 先写“9.3.7 复制过滤器”影响的用户请求、MySQL状态和资源，再把它接入“用binlog、GTID、并行回放、半同步与拓扑管理副本进度和故障切换”。上游是“9.3.6 半同步复制”，下游是“9.4 复制切换”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“每个已确认事务在目标拓扑有唯一身份，晋升不会丢失已承诺写入或产生双主分叉”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 9.4 复制切换

**节点合同 11/33 · 注入失败条件。** 先写“9.4 复制切换”影响的用户请求、MySQL状态和资源，再把它接入“用binlog、GTID、并行回放、半同步与拓扑管理副本进度和故障切换”。上游是“9.3.7 复制过滤器”，下游是“9.4.1 计划内切换”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“每个已确认事务在目标拓扑有唯一身份，晋升不会丢失已承诺写入或产生双主分叉”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 9.4.1 计划内切换

**节点合同 12/33 · 收集独立证据。** 先写“9.4.1 计划内切换”影响的用户请求、MySQL状态和资源，再把它接入“用binlog、GTID、并行回放、半同步与拓扑管理副本进度和故障切换”。上游是“9.4 复制切换”，下游是“9.4.2 计划外切换”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“每个已确认事务在目标拓扑有唯一身份，晋升不会丢失已承诺写入或产生双主分叉”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 9.4.2 计划外切换

**节点合同 13/33 · 定义用户影响。** 先写“9.4.2 计划外切换”影响的用户请求、MySQL状态和资源，再把它接入“用binlog、GTID、并行回放、半同步与拓扑管理副本进度和故障切换”。上游是“9.4.1 计划内切换”，下游是“9.4.3 切换权衡”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“每个已确认事务在目标拓扑有唯一身份，晋升不会丢失已承诺写入或产生双主分叉”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 9.4.3 切换权衡

**节点合同 14/33 · 固定实验输入。** 先写“9.4.3 切换权衡”影响的用户请求、MySQL状态和资源，再把它接入“用binlog、GTID、并行回放、半同步与拓扑管理副本进度和故障切换”。上游是“9.4.2 计划外切换”，下游是“9.5 复制拓扑”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“每个已确认事务在目标拓扑有唯一身份，晋升不会丢失已承诺写入或产生双主分叉”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 9.5 复制拓扑

**节点合同 15/33 · 定位责任层。** 先写“9.5 复制拓扑”影响的用户请求、MySQL状态和资源，再把它接入“用binlog、GTID、并行回放、半同步与拓扑管理副本进度和故障切换”。上游是“9.4.3 切换权衡”，下游是“9.5.1 主动/被动模式”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“每个已确认事务在目标拓扑有唯一身份，晋升不会丢失已承诺写入或产生双主分叉”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 9.5.1 主动/被动模式

**节点合同 16/33 · 推演正常路径。** 先写“9.5.1 主动/被动模式”影响的用户请求、MySQL状态和资源，再把它接入“用binlog、GTID、并行回放、半同步与拓扑管理副本进度和故障切换”。上游是“9.5 复制拓扑”，下游是“9.5.2 主动/只读池模式”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“每个已确认事务在目标拓扑有唯一身份，晋升不会丢失已承诺写入或产生双主分叉”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 9.5.2 主动/只读池模式

**节点合同 17/33 · 注入失败条件。** 先写“9.5.2 主动/只读池模式”影响的用户请求、MySQL状态和资源，再把它接入“用binlog、GTID、并行回放、半同步与拓扑管理副本进度和故障切换”。上游是“9.5.1 主动/被动模式”，下游是“9.5.3 不推荐的拓扑”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“每个已确认事务在目标拓扑有唯一身份，晋升不会丢失已承诺写入或产生双主分叉”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 9.5.3 不推荐的拓扑

**节点合同 18/33 · 收集独立证据。** 先写“9.5.3 不推荐的拓扑”影响的用户请求、MySQL状态和资源，再把它接入“用binlog、GTID、并行回放、半同步与拓扑管理副本进度和故障切换”。上游是“9.5.2 主动/只读池模式”，下游是“9.6 复制管理和维护”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“每个已确认事务在目标拓扑有唯一身份，晋升不会丢失已承诺写入或产生双主分叉”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 9.6 复制管理和维护

**节点合同 19/33 · 定义用户影响。** 先写“9.6 复制管理和维护”影响的用户请求、MySQL状态和资源，再把它接入“用binlog、GTID、并行回放、半同步与拓扑管理副本进度和故障切换”。上游是“9.5.3 不推荐的拓扑”，下游是“9.6.1 复制监控”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“每个已确认事务在目标拓扑有唯一身份，晋升不会丢失已承诺写入或产生双主分叉”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 9.6.1 复制监控

**节点合同 20/33 · 固定实验输入。** 先写“9.6.1 复制监控”影响的用户请求、MySQL状态和资源，再把它接入“用binlog、GTID、并行回放、半同步与拓扑管理副本进度和故障切换”。上游是“9.6 复制管理和维护”，下游是“9.6.2 观测复制延迟”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“每个已确认事务在目标拓扑有唯一身份，晋升不会丢失已承诺写入或产生双主分叉”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 9.6.2 观测复制延迟

**节点合同 21/33 · 定位责任层。** 先写“9.6.2 观测复制延迟”影响的用户请求、MySQL状态和资源，再把它接入“用binlog、GTID、并行回放、半同步与拓扑管理副本进度和故障切换”。上游是“9.6.1 复制监控”，下游是“9.6.3 确定副本与源一致”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“每个已确认事务在目标拓扑有唯一身份，晋升不会丢失已承诺写入或产生双主分叉”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 9.6.3 确定副本与源一致

**节点合同 22/33 · 推演正常路径。** 先写“9.6.3 确定副本与源一致”影响的用户请求、MySQL状态和资源，再把它接入“用binlog、GTID、并行回放、半同步与拓扑管理副本进度和故障切换”。上游是“9.6.2 观测复制延迟”，下游是“9.7 复制问题与解决”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“每个已确认事务在目标拓扑有唯一身份，晋升不会丢失已承诺写入或产生双主分叉”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 9.7 复制问题与解决

**节点合同 23/33 · 注入失败条件。** 先写“9.7 复制问题与解决”影响的用户请求、MySQL状态和资源，再把它接入“用binlog、GTID、并行回放、半同步与拓扑管理副本进度和故障切换”。上游是“9.6.3 确定副本与源一致”，下游是“9.7.1 源端binlog损坏”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“每个已确认事务在目标拓扑有唯一身份，晋升不会丢失已承诺写入或产生双主分叉”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 9.7.1 源端binlog损坏

**节点合同 24/33 · 收集独立证据。** 先写“9.7.1 源端binlog损坏”影响的用户请求、MySQL状态和资源，再把它接入“用binlog、GTID、并行回放、半同步与拓扑管理副本进度和故障切换”。上游是“9.7 复制问题与解决”，下游是“9.7.2 非唯一server_id”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“每个已确认事务在目标拓扑有唯一身份，晋升不会丢失已承诺写入或产生双主分叉”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 9.7.2 非唯一server_id

**节点合同 25/33 · 定义用户影响。** 先写“9.7.2 非唯一server_id”影响的用户请求、MySQL状态和资源，再把它接入“用binlog、GTID、并行回放、半同步与拓扑管理副本进度和故障切换”。上游是“9.7.1 源端binlog损坏”，下游是“9.7.3 未配置server_id”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“每个已确认事务在目标拓扑有唯一身份，晋升不会丢失已承诺写入或产生双主分叉”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 9.7.3 未配置server_id

**节点合同 26/33 · 固定实验输入。** 先写“9.7.3 未配置server_id”影响的用户请求、MySQL状态和资源，再把它接入“用binlog、GTID、并行回放、半同步与拓扑管理副本进度和故障切换”。上游是“9.7.2 非唯一server_id”，下游是“9.7.4 临时表丢失”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“每个已确认事务在目标拓扑有唯一身份，晋升不会丢失已承诺写入或产生双主分叉”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 9.7.4 临时表丢失

**节点合同 27/33 · 定位责任层。** 先写“9.7.4 临时表丢失”影响的用户请求、MySQL状态和资源，再把它接入“用binlog、GTID、并行回放、半同步与拓扑管理副本进度和故障切换”。上游是“9.7.3 未配置server_id”，下游是“9.7.5 未复制所有变更”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“每个已确认事务在目标拓扑有唯一身份，晋升不会丢失已承诺写入或产生双主分叉”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 9.7.5 未复制所有变更

**节点合同 28/33 · 推演正常路径。** 先写“9.7.5 未复制所有变更”影响的用户请求、MySQL状态和资源，再把它接入“用binlog、GTID、并行回放、半同步与拓扑管理副本进度和故障切换”。上游是“9.7.4 临时表丢失”，下游是“9.7.6 复制延迟过大”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“每个已确认事务在目标拓扑有唯一身份，晋升不会丢失已承诺写入或产生双主分叉”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 9.7.6 复制延迟过大

**节点合同 29/33 · 注入失败条件。** 先写“9.7.6 复制延迟过大”影响的用户请求、MySQL状态和资源，再把它接入“用binlog、GTID、并行回放、半同步与拓扑管理副本进度和故障切换”。上游是“9.7.5 未复制所有变更”，下游是“9.7.7 源服务器超大数据包”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“每个已确认事务在目标拓扑有唯一身份，晋升不会丢失已承诺写入或产生双主分叉”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 9.7.7 源服务器超大数据包

**节点合同 30/33 · 收集独立证据。** 先写“9.7.7 源服务器超大数据包”影响的用户请求、MySQL状态和资源，再把它接入“用binlog、GTID、并行回放、半同步与拓扑管理副本进度和故障切换”。上游是“9.7.6 复制延迟过大”，下游是“9.7.8 磁盘空间耗尽”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“每个已确认事务在目标拓扑有唯一身份，晋升不会丢失已承诺写入或产生双主分叉”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 9.7.8 磁盘空间耗尽

**节点合同 31/33 · 定义用户影响。** 先写“9.7.8 磁盘空间耗尽”影响的用户请求、MySQL状态和资源，再把它接入“用binlog、GTID、并行回放、半同步与拓扑管理副本进度和故障切换”。上游是“9.7.7 源服务器超大数据包”，下游是“9.8 复制限制”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“每个已确认事务在目标拓扑有唯一身份，晋升不会丢失已承诺写入或产生双主分叉”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 9.8 复制限制

**节点合同 32/33 · 固定实验输入。** 先写“9.8 复制限制”影响的用户请求、MySQL状态和资源，再把它接入“用binlog、GTID、并行回放、半同步与拓扑管理副本进度和故障切换”。上游是“9.7.8 磁盘空间耗尽”，下游是“9.9 小结”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“每个已确认事务在目标拓扑有唯一身份，晋升不会丢失已承诺写入或产生双主分叉”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 9.9 小结

**节点合同 33/33 · 定位责任层。** 先写“9.9 小结”影响的用户请求、MySQL状态和资源，再把它接入“用binlog、GTID、并行回放、半同步与拓扑管理副本进度和故障切换”。上游是“9.8 复制限制”，下游是“本章生产交付物”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“每个已确认事务在目标拓扑有唯一身份，晋升不会丢失已承诺写入或产生双主分叉”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

## 机制推演：从请求到资源与恢复

源端提交产生binlog事件，副本接收并应用；GTID让事务身份跨拓扑可比较。复制延迟包含接收、排队和回放，半同步只改变确认点而非自动消除数据丢失；安全切换必须冻结或隔离旧源并比较事务集合。

生产推演始终拆成六层：用户请求与SLO、应用连接和事务、MySQL服务层、InnoDB页锁版本与日志、操作系统及硬件、复制备份和路由。先确定等待发生在哪一层，再选择指标和干预；参数、索引、分片与扩容属于不同层，不能用一个方案覆盖所有症状。

性能证据同时包含工作量和等待。工作量包括每秒请求、实际扫描行、返回字节、日志字节和脏页；等待包括CPU调度、I/O、锁、元数据锁、复制回放与队列。只有延迟下降且结果、持久性和错误预算没有恶化，优化才成立。

可靠性证据必须覆盖最坏时间点。对事务在提交前后崩溃，对副本在接收与应用之间中断，对备份在基线与binlog之间恢复，对分片在路由切换时重试。记录最后确认状态、数据损失上界、恢复步骤、恢复耗时和业务对账，避免把进程重新启动当作服务恢复。

<Callout type="trap">
  **优化陷阱。**
  只看Seconds_Behind_Source判定健康，或在旧源未隔离时直接晋升副本。先恢复SLO、版本、工作负载和结果合同，再改变一项设计；不要用单次快结果掩盖尾延迟、数据风险或观测偏差。
</Callout>

<Callout type="trap">
  **因果陷阱。**
  指标同时变化不等于存在因果关系。缓存预热、流量分布、后台任务、复制延迟和云平台噪声都可能改变结果；必须保留对照并一次改变一个变量。
</Callout>

<Callout type="warning">
  DDL、参数、复制切换、恢复、分片和升级实验只在隔离环境执行。先验证备份和回退，规定最大写入、锁等待、错误预算消耗与停止条件。
</Callout>

## 证据解释与独立交接

服务证据固定为什么优化。保存SLI公式、SLO窗口、流量分母、延迟分位数和错误预算燃烧；若用户指标没有改善，底层计数器变漂亮也不能证明价值。缺失采样和维护窗口必须显式处理。

数据库证据解释发生了什么。保存Schema与统计、SQL和参数、计划估计与实际行、锁和版本、redo与binlog位置、状态变量及Performance Schema摘要。重置摘要或重启实例会改变基线，必须记录时间点。

资源证据定位限制。保存CPU利用率与运行队列、内存与交换、磁盘延迟和队列、网络、云盘限额及后台刷新。平均值不能替代分位数和饱和时段，客户端总延迟也不能自动归因于数据库执行。

故障证据证明可恢复。保存故障注入点、最后确认事务、晋升或恢复步骤、RPO/RTO实测、数据对账与旧源隔离。备份任务成功、副本在线或Pod重建都不是业务恢复的充分条件。

## 本章回顾

重新完成“用binlog、GTID、并行回放、半同步与拓扑管理副本进度和故障切换”：先写SLO和结果合同，按33个目录节点建立因果模型，固定负载，采集基线，只改变一个变量，注入失败并独立对账。最终交付GTID集合、延迟分解、切换runbook、一致性校验和故障演练，证明“每个已确认事务在目标拓扑有唯一身份，晋升不会丢失已承诺写入或产生双主分叉”。

## 复习与生产验收

<Exercises>
**问题 1**：为什么“第9章 复制”必须覆盖33个正式节点？
<Answer>这些节点共同组成“用binlog、GTID、并行回放、半同步与拓扑管理副本进度和故障切换”的服务目标、机制、负载、故障和证据链；缺项会使GTID集合、延迟分解、切换runbook、一致性校验和故障演练无法复现。</Answer>

**问题 2**：本页最小运行不变量是什么？

<Answer>
  每个已确认事务在目标拓扑有唯一身份，晋升不会丢失已承诺写入或产生双主分叉。必须由固定版本和负载、正常轨迹、压力数据、故障反例与业务对账共同证明。
</Answer>

**问题 3**：怎样构造最小反例？

<Answer>
  把“只看Seconds_Behind_Source判定健康，或在旧源未隔离时直接晋升副本”写成待证假设，保持Schema与查询不变，只改变缓存、并发、数据分布或故障时点之一，再比较用户与数据库证据。
</Answer>

**问题 4**：为什么平均延迟不足以验收优化？

<Answer>
  平均值会隐藏排队、锁、I/O抖动和少数极慢请求；至少需要P95/P99、吞吐、错误率、实际扫描与等待，并确认结果语义不变。
</Answer>

**问题 5**：可靠性变更怎样验证？

<Answer>
  在最坏确认边界注入故障，记录最后确认事务、恢复或切换过程、RPO/RTO、数据一致性、旧源隔离与回退结果。
</Answer>

**问题 6**：独立交接至少包含什么？

<Answer>需要SLO、版本与配置、Schema与数据分布、负载脚本、GTID集合、延迟分解、切换runbook、一致性校验和故障演练、计划与等待、故障演练、业务对账和回退条件。</Answer>
</Exercises>

<Glossary>
  <GlossaryItem term="binlog">
    binlog以“每个已确认事务在目标拓扑有唯一身份，晋升不会丢失已承诺写入或产生双主分叉”为运行边界，并由正常、压力与故障证据共同验证。
  </GlossaryItem>
  <GlossaryItem term="GTID">
    GTID以“每个已确认事务在目标拓扑有唯一身份，晋升不会丢失已承诺写入或产生双主分叉”为运行边界，并由正常、压力与故障证据共同验证。
  </GlossaryItem>
  <GlossaryItem term="复制延迟">
    复制延迟以“每个已确认事务在目标拓扑有唯一身份，晋升不会丢失已承诺写入或产生双主分叉”为运行边界，并由正常、压力与故障证据共同验证。
  </GlossaryItem>
  <GlossaryItem term="半同步复制">
    半同步复制以“每个已确认事务在目标拓扑有唯一身份，晋升不会丢失已承诺写入或产生双主分叉”为运行边界，并由正常、压力与故障证据共同验证。
  </GlossaryItem>
  <GlossaryItem term="故障切换">
    故障切换以“每个已确认事务在目标拓扑有唯一身份，晋升不会丢失已承诺写入或产生双主分叉”为运行边界，并由正常、压力与故障证据共同验证。
  </GlossaryItem>
</Glossary>

[← 上一页：第8章 查询性能优化](/learn/high-performance-mysql/08-query-optimization/hpm4-ch08-query-optimization) · [下一页：第10章 备份与恢复 →](/learn/high-performance-mysql/10-backup-recovery/hpm4-ch10-backup-recovery)

<Attribution
  adaptedFrom="《高性能MySQL（第4版）》"
  adaptedUrl="https://www.oreilly.com/library/view/gao-xing-neng-mysql-di-4ban/9787121442575/"
/>
