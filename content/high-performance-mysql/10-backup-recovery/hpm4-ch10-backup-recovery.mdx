---
title: "第10章 备份与恢复"
description: "依据第4版完整目录覆盖25个节点：从RPO、RTO与故障模型选择逻辑、物理、快照、增量和binlog恢复链"
type: C
section: "高性能MySQL（第4版 / 2022）"
draft: true
math: false
sourceUrl: "https://www.oreilly.com/library/view/gao-xing-neng-mysql-di-4ban/9787121442575/"
demo: true
order: 0
---

# 第10章 备份与恢复

本课程对应Silvia Botros、Jeremy Tinley著，宁海元、周振兴、张新铭译《高性能MySQL（第4版）》，电子工业出版社2022年9月出版，纸书344页，ISBN 9787121442575。原版High Performance MySQL: Proven Strategies for Operating at Scale由O'Reilly于2021年11月发布，386页，ISBN 9781492080503。

官方中英文目录确认第4版为13章与附录A、B。课程按15个正式单元逐项重构，不沿用第3版的14章结构，也不复制原文；另设学习地图与总复习，共17页。本页覆盖25个正式节点。

<Objectives>

- 能解释“第10章 备份与恢复”的25个目录节点，并连接到“从RPO、RTO与故障模型选择逻辑、物理、快照、增量和binlog恢复链”主线。
- 能复现备份清单、校验哈希、时间点恢复演练、RPO/RTO实测和删除事故复盘，保存MySQL版本、Schema、数据量、配置、负载、预期与实际结果。
- 能比较基线、压力与故障条件，证明“备份可验证、可解密、可定位时间点并在目标RTO内恢复到独立环境”。
- 能设计反例推翻“把复制副本当备份，或只记录备份任务成功却从不恢复和核对业务数据”，并给出上线门、回退与独立验收条件。

</Objectives>

## 从用户SLO和一次故障开始

先预测：在30天窗口内，目标请求成功率、P95与P99延迟、允许错误分钟和恢复时间各是多少？再描述本章机制如何影响这些结果。不要先看图表再给故事；先固定服务目标、MySQL版本、数据规模、并发、缓存状态和故障点，实验后才允许用证据修正模型。

本页主问题是“从RPO、RTO与故障模型选择逻辑、物理、快照、增量和binlog恢复链”。最终交付不是一组建议，而是备份清单、校验哈希、时间点恢复演练、RPO/RTO实测和删除事故复盘，由另一位工程师证明：**备份可验证、可解密、可定位时间点并在目标RTO内恢复到独立环境**。

## 核心词汇与运行边界

<Term def="RPO是“从RPO、RTO与故障模型选择逻辑、物理、快照、增量和binlog恢复链”中的第1个核心概念；必须由版本、负载、机制、反例和指标定义">RPO</Term>、<Term def="RTO是“从RPO、RTO与故障模型选择逻辑、物理、快照、增量和binlog恢复链”中的第2个核心概念；必须由版本、负载、机制、反例和指标定义">RTO</Term>、<Term def="逻辑备份是“从RPO、RTO与故障模型选择逻辑、物理、快照、增量和binlog恢复链”中的第3个核心概念；必须由版本、负载、机制、反例和指标定义">逻辑备份</Term>、<Term def="物理备份是“从RPO、RTO与故障模型选择逻辑、物理、快照、增量和binlog恢复链”中的第4个核心概念；必须由版本、负载、机制、反例和指标定义">物理备份</Term>、<Term def="时间点恢复是“从RPO、RTO与故障模型选择逻辑、物理、快照、增量和binlog恢复链”中的第5个核心概念；必须由版本、负载、机制、反例和指标定义">时间点恢复</Term>
构成本页词汇。每个词都要回答它改变哪个用户指标、由哪个MySQL或基础设施模块实现、在哪个压力或故障边界失效，以及用哪条独立证据发现失效。

<Hpm4Ch10BackupRecoverySloLab />

## 第4版目录逐节点映射

### 10.1 为什么要备份

**节点合同 1/25 · 定义用户影响。** 先写“10.1 为什么要备份”影响的用户请求、MySQL状态和资源，再把它接入“从RPO、RTO与故障模型选择逻辑、物理、快照、增量和binlog恢复链”。上游是“本章服务目标”，下游是“10.2 定义恢复需求”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“备份可验证、可解密、可定位时间点并在目标RTO内恢复到独立环境”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 10.2 定义恢复需求

**节点合同 2/25 · 固定实验输入。** 先写“10.2 定义恢复需求”影响的用户请求、MySQL状态和资源，再把它接入“从RPO、RTO与故障模型选择逻辑、物理、快照、增量和binlog恢复链”。上游是“10.1 为什么要备份”，下游是“10.3 设计MySQL备份方案”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“备份可验证、可解密、可定位时间点并在目标RTO内恢复到独立环境”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 10.3 设计MySQL备份方案

**节点合同 3/25 · 定位责任层。** 先写“10.3 设计MySQL备份方案”影响的用户请求、MySQL状态和资源，再把它接入“从RPO、RTO与故障模型选择逻辑、物理、快照、增量和binlog恢复链”。上游是“10.2 定义恢复需求”，下游是“10.3.1 在线或离线备份”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“备份可验证、可解密、可定位时间点并在目标RTO内恢复到独立环境”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 10.3.1 在线或离线备份

**节点合同 4/25 · 推演正常路径。** 先写“10.3.1 在线或离线备份”影响的用户请求、MySQL状态和资源，再把它接入“从RPO、RTO与故障模型选择逻辑、物理、快照、增量和binlog恢复链”。上游是“10.3 设计MySQL备份方案”，下游是“10.3.2 逻辑或裸文件备份”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“备份可验证、可解密、可定位时间点并在目标RTO内恢复到独立环境”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 10.3.2 逻辑或裸文件备份

**节点合同 5/25 · 注入失败条件。** 先写“10.3.2 逻辑或裸文件备份”影响的用户请求、MySQL状态和资源，再把它接入“从RPO、RTO与故障模型选择逻辑、物理、快照、增量和binlog恢复链”。上游是“10.3.1 在线或离线备份”，下游是“10.3.3 备份什么”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“备份可验证、可解密、可定位时间点并在目标RTO内恢复到独立环境”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 10.3.3 备份什么

**节点合同 6/25 · 收集独立证据。** 先写“10.3.3 备份什么”影响的用户请求、MySQL状态和资源，再把它接入“从RPO、RTO与故障模型选择逻辑、物理、快照、增量和binlog恢复链”。上游是“10.3.2 逻辑或裸文件备份”，下游是“10.3.4 增量与差异备份”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“备份可验证、可解密、可定位时间点并在目标RTO内恢复到独立环境”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 10.3.4 增量与差异备份

**节点合同 7/25 · 定义用户影响。** 先写“10.3.4 增量与差异备份”影响的用户请求、MySQL状态和资源，再把它接入“从RPO、RTO与故障模型选择逻辑、物理、快照、增量和binlog恢复链”。上游是“10.3.3 备份什么”，下游是“10.3.5 复制不是备份”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“备份可验证、可解密、可定位时间点并在目标RTO内恢复到独立环境”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 10.3.5 复制不是备份

**节点合同 8/25 · 固定实验输入。** 先写“10.3.5 复制不是备份”影响的用户请求、MySQL状态和资源，再把它接入“从RPO、RTO与故障模型选择逻辑、物理、快照、增量和binlog恢复链”。上游是“10.3.4 增量与差异备份”，下游是“10.3.6 管理和备份binlog”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“备份可验证、可解密、可定位时间点并在目标RTO内恢复到独立环境”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 10.3.6 管理和备份binlog

**节点合同 9/25 · 定位责任层。** 先写“10.3.6 管理和备份binlog”影响的用户请求、MySQL状态和资源，再把它接入“从RPO、RTO与故障模型选择逻辑、物理、快照、增量和binlog恢复链”。上游是“10.3.5 复制不是备份”，下游是“10.4 备份与恢复工具”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“备份可验证、可解密、可定位时间点并在目标RTO内恢复到独立环境”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 10.4 备份与恢复工具

**节点合同 10/25 · 推演正常路径。** 先写“10.4 备份与恢复工具”影响的用户请求、MySQL状态和资源，再把它接入“从RPO、RTO与故障模型选择逻辑、物理、快照、增量和binlog恢复链”。上游是“10.3.6 管理和备份binlog”，下游是“10.4.1 MySQL Enterprise Backup”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“备份可验证、可解密、可定位时间点并在目标RTO内恢复到独立环境”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 10.4.1 MySQL Enterprise Backup

**节点合同 11/25 · 注入失败条件。** 先写“10.4.1 MySQL Enterprise Backup”影响的用户请求、MySQL状态和资源，再把它接入“从RPO、RTO与故障模型选择逻辑、物理、快照、增量和binlog恢复链”。上游是“10.4 备份与恢复工具”，下游是“10.4.2 Percona XtraBackup”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“备份可验证、可解密、可定位时间点并在目标RTO内恢复到独立环境”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 10.4.2 Percona XtraBackup

**节点合同 12/25 · 收集独立证据。** 先写“10.4.2 Percona XtraBackup”影响的用户请求、MySQL状态和资源，再把它接入“从RPO、RTO与故障模型选择逻辑、物理、快照、增量和binlog恢复链”。上游是“10.4.1 MySQL Enterprise Backup”，下游是“10.4.3 mydumper”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“备份可验证、可解密、可定位时间点并在目标RTO内恢复到独立环境”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 10.4.3 mydumper

**节点合同 13/25 · 定义用户影响。** 先写“10.4.3 mydumper”影响的用户请求、MySQL状态和资源，再把它接入“从RPO、RTO与故障模型选择逻辑、物理、快照、增量和binlog恢复链”。上游是“10.4.2 Percona XtraBackup”，下游是“10.4.4 mysqldump”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“备份可验证、可解密、可定位时间点并在目标RTO内恢复到独立环境”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 10.4.4 mysqldump

**节点合同 14/25 · 固定实验输入。** 先写“10.4.4 mysqldump”影响的用户请求、MySQL状态和资源，再把它接入“从RPO、RTO与故障模型选择逻辑、物理、快照、增量和binlog恢复链”。上游是“10.4.3 mydumper”，下游是“10.5 备份数据”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“备份可验证、可解密、可定位时间点并在目标RTO内恢复到独立环境”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 10.5 备份数据

**节点合同 15/25 · 定位责任层。** 先写“10.5 备份数据”影响的用户请求、MySQL状态和资源，再把它接入“从RPO、RTO与故障模型选择逻辑、物理、快照、增量和binlog恢复链”。上游是“10.4.4 mysqldump”，下游是“10.5.1 逻辑SQL备份”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“备份可验证、可解密、可定位时间点并在目标RTO内恢复到独立环境”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 10.5.1 逻辑SQL备份

**节点合同 16/25 · 推演正常路径。** 先写“10.5.1 逻辑SQL备份”影响的用户请求、MySQL状态和资源，再把它接入“从RPO、RTO与故障模型选择逻辑、物理、快照、增量和binlog恢复链”。上游是“10.5 备份数据”，下游是“10.5.2 文件系统快照”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“备份可验证、可解密、可定位时间点并在目标RTO内恢复到独立环境”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 10.5.2 文件系统快照

**节点合同 17/25 · 注入失败条件。** 先写“10.5.2 文件系统快照”影响的用户请求、MySQL状态和资源，再把它接入“从RPO、RTO与故障模型选择逻辑、物理、快照、增量和binlog恢复链”。上游是“10.5.1 逻辑SQL备份”，下游是“10.5.3 使用Percona XtraBackup”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“备份可验证、可解密、可定位时间点并在目标RTO内恢复到独立环境”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 10.5.3 使用Percona XtraBackup

**节点合同 18/25 · 收集独立证据。** 先写“10.5.3 使用Percona XtraBackup”影响的用户请求、MySQL状态和资源，再把它接入“从RPO、RTO与故障模型选择逻辑、物理、快照、增量和binlog恢复链”。上游是“10.5.2 文件系统快照”，下游是“10.6 从备份恢复”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“备份可验证、可解密、可定位时间点并在目标RTO内恢复到独立环境”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 10.6 从备份恢复

**节点合同 19/25 · 定义用户影响。** 先写“10.6 从备份恢复”影响的用户请求、MySQL状态和资源，再把它接入“从RPO、RTO与故障模型选择逻辑、物理、快照、增量和binlog恢复链”。上游是“10.5.3 使用Percona XtraBackup”，下游是“10.6.1 恢复逻辑备份”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“备份可验证、可解密、可定位时间点并在目标RTO内恢复到独立环境”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 10.6.1 恢复逻辑备份

**节点合同 20/25 · 固定实验输入。** 先写“10.6.1 恢复逻辑备份”影响的用户请求、MySQL状态和资源，再把它接入“从RPO、RTO与故障模型选择逻辑、物理、快照、增量和binlog恢复链”。上游是“10.6 从备份恢复”，下游是“10.6.2 从快照恢复裸文件”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“备份可验证、可解密、可定位时间点并在目标RTO内恢复到独立环境”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 10.6.2 从快照恢复裸文件

**节点合同 21/25 · 定位责任层。** 先写“10.6.2 从快照恢复裸文件”影响的用户请求、MySQL状态和资源，再把它接入“从RPO、RTO与故障模型选择逻辑、物理、快照、增量和binlog恢复链”。上游是“10.6.1 恢复逻辑备份”，下游是“10.6.3 使用Percona XtraBackup恢复”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“备份可验证、可解密、可定位时间点并在目标RTO内恢复到独立环境”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 10.6.3 使用Percona XtraBackup恢复

**节点合同 22/25 · 推演正常路径。** 先写“10.6.3 使用Percona XtraBackup恢复”影响的用户请求、MySQL状态和资源，再把它接入“从RPO、RTO与故障模型选择逻辑、物理、快照、增量和binlog恢复链”。上游是“10.6.2 从快照恢复裸文件”，下游是“10.6.4 裸文件恢复后启动MySQL”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“备份可验证、可解密、可定位时间点并在目标RTO内恢复到独立环境”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 10.6.4 裸文件恢复后启动MySQL

**节点合同 23/25 · 注入失败条件。** 先写“10.6.4 裸文件恢复后启动MySQL”影响的用户请求、MySQL状态和资源，再把它接入“从RPO、RTO与故障模型选择逻辑、物理、快照、增量和binlog恢复链”。上游是“10.6.3 使用Percona XtraBackup恢复”，下游是“10.7 小结”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“备份可验证、可解密、可定位时间点并在目标RTO内恢复到独立环境”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 10.7 小结

**节点合同 24/25 · 收集独立证据。** 先写“10.7 小结”影响的用户请求、MySQL状态和资源，再把它接入“从RPO、RTO与故障模型选择逻辑、物理、快照、增量和binlog恢复链”。上游是“10.6.4 裸文件恢复后启动MySQL”，下游是“10.8 恢复演练”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“备份可验证、可解密、可定位时间点并在目标RTO内恢复到独立环境”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### 10.8 恢复演练

**节点合同 25/25 · 定义用户影响。** 先写“10.8 恢复演练”影响的用户请求、MySQL状态和资源，再把它接入“从RPO、RTO与故障模型选择逻辑、物理、快照、增量和binlog恢复链”。上游是“10.7 小结”，下游是“本章生产交付物”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“备份可验证、可解密、可定位时间点并在目标RTO内恢复到独立环境”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

## 机制推演：从请求到资源与恢复

备份方案从故障和恢复目标出发。全量备份提供基线，增量与binlog缩短数据损失窗口，逻辑备份可移植但恢复慢，物理备份快却依赖版本与文件一致性；唯一有效的验证是隔离恢复和业务对账。

生产推演始终拆成六层：用户请求与SLO、应用连接和事务、MySQL服务层、InnoDB页锁版本与日志、操作系统及硬件、复制备份和路由。先确定等待发生在哪一层，再选择指标和干预；参数、索引、分片与扩容属于不同层，不能用一个方案覆盖所有症状。

性能证据同时包含工作量和等待。工作量包括每秒请求、实际扫描行、返回字节、日志字节和脏页；等待包括CPU调度、I/O、锁、元数据锁、复制回放与队列。只有延迟下降且结果、持久性和错误预算没有恶化，优化才成立。

可靠性证据必须覆盖最坏时间点。对事务在提交前后崩溃，对副本在接收与应用之间中断，对备份在基线与binlog之间恢复，对分片在路由切换时重试。记录最后确认状态、数据损失上界、恢复步骤、恢复耗时和业务对账，避免把进程重新启动当作服务恢复。

<Callout type="trap">
  **优化陷阱。**
  把复制副本当备份，或只记录备份任务成功却从不恢复和核对业务数据。先恢复SLO、版本、工作负载和结果合同，再改变一项设计；不要用单次快结果掩盖尾延迟、数据风险或观测偏差。
</Callout>

<Callout type="trap">
  **因果陷阱。**
  指标同时变化不等于存在因果关系。缓存预热、流量分布、后台任务、复制延迟和云平台噪声都可能改变结果；必须保留对照并一次改变一个变量。
</Callout>

<Callout type="warning">
  DDL、参数、复制切换、恢复、分片和升级实验只在隔离环境执行。先验证备份和回退，规定最大写入、锁等待、错误预算消耗与停止条件。
</Callout>

## 证据解释与独立交接

服务证据固定为什么优化。保存SLI公式、SLO窗口、流量分母、延迟分位数和错误预算燃烧；若用户指标没有改善，底层计数器变漂亮也不能证明价值。缺失采样和维护窗口必须显式处理。

数据库证据解释发生了什么。保存Schema与统计、SQL和参数、计划估计与实际行、锁和版本、redo与binlog位置、状态变量及Performance Schema摘要。重置摘要或重启实例会改变基线，必须记录时间点。

资源证据定位限制。保存CPU利用率与运行队列、内存与交换、磁盘延迟和队列、网络、云盘限额及后台刷新。平均值不能替代分位数和饱和时段，客户端总延迟也不能自动归因于数据库执行。

故障证据证明可恢复。保存故障注入点、最后确认事务、晋升或恢复步骤、RPO/RTO实测、数据对账与旧源隔离。备份任务成功、副本在线或Pod重建都不是业务恢复的充分条件。

## 本章回顾

重新完成“从RPO、RTO与故障模型选择逻辑、物理、快照、增量和binlog恢复链”：先写SLO和结果合同，按25个目录节点建立因果模型，固定负载，采集基线，只改变一个变量，注入失败并独立对账。最终交付备份清单、校验哈希、时间点恢复演练、RPO/RTO实测和删除事故复盘，证明“备份可验证、可解密、可定位时间点并在目标RTO内恢复到独立环境”。

## 复习与生产验收

<Exercises>
**问题 1**：为什么“第10章 备份与恢复”必须覆盖25个正式节点？
<Answer>这些节点共同组成“从RPO、RTO与故障模型选择逻辑、物理、快照、增量和binlog恢复链”的服务目标、机制、负载、故障和证据链；缺项会使备份清单、校验哈希、时间点恢复演练、RPO/RTO实测和删除事故复盘无法复现。</Answer>

**问题 2**：本页最小运行不变量是什么？

<Answer>
  备份可验证、可解密、可定位时间点并在目标RTO内恢复到独立环境。必须由固定版本和负载、正常轨迹、压力数据、故障反例与业务对账共同证明。
</Answer>

**问题 3**：怎样构造最小反例？

<Answer>
  把“把复制副本当备份，或只记录备份任务成功却从不恢复和核对业务数据”写成待证假设，保持Schema与查询不变，只改变缓存、并发、数据分布或故障时点之一，再比较用户与数据库证据。
</Answer>

**问题 4**：为什么平均延迟不足以验收优化？

<Answer>
  平均值会隐藏排队、锁、I/O抖动和少数极慢请求；至少需要P95/P99、吞吐、错误率、实际扫描与等待，并确认结果语义不变。
</Answer>

**问题 5**：可靠性变更怎样验证？

<Answer>
  在最坏确认边界注入故障，记录最后确认事务、恢复或切换过程、RPO/RTO、数据一致性、旧源隔离与回退结果。
</Answer>

**问题 6**：独立交接至少包含什么？

<Answer>需要SLO、版本与配置、Schema与数据分布、负载脚本、备份清单、校验哈希、时间点恢复演练、RPO/RTO实测和删除事故复盘、计划与等待、故障演练、业务对账和回退条件。</Answer>
</Exercises>

<Glossary>
  <GlossaryItem term="RPO">
    RPO以“备份可验证、可解密、可定位时间点并在目标RTO内恢复到独立环境”为运行边界，并由正常、压力与故障证据共同验证。
  </GlossaryItem>
  <GlossaryItem term="RTO">
    RTO以“备份可验证、可解密、可定位时间点并在目标RTO内恢复到独立环境”为运行边界，并由正常、压力与故障证据共同验证。
  </GlossaryItem>
  <GlossaryItem term="逻辑备份">
    逻辑备份以“备份可验证、可解密、可定位时间点并在目标RTO内恢复到独立环境”为运行边界，并由正常、压力与故障证据共同验证。
  </GlossaryItem>
  <GlossaryItem term="物理备份">
    物理备份以“备份可验证、可解密、可定位时间点并在目标RTO内恢复到独立环境”为运行边界，并由正常、压力与故障证据共同验证。
  </GlossaryItem>
  <GlossaryItem term="时间点恢复">
    时间点恢复以“备份可验证、可解密、可定位时间点并在目标RTO内恢复到独立环境”为运行边界，并由正常、压力与故障证据共同验证。
  </GlossaryItem>
</Glossary>

[← 上一页：第9章 复制](/learn/high-performance-mysql/09-replication/hpm4-ch09-replication) · [下一页：第11章 扩展MySQL →](/learn/high-performance-mysql/11-scaling/hpm4-ch11-scaling)

<Attribution
  adaptedFrom="《高性能MySQL（第4版）》"
  adaptedUrl="https://www.oreilly.com/library/view/gao-xing-neng-mysql-di-4ban/9787121442575/"
/>
