---
title: "附录A 升级MySQL"
description: "依据第4版完整目录覆盖9个节点：用发布说明、真实流量对照、灰度副本和自动化runbook降低版本升级风险"
type: C
section: "高性能MySQL（第4版 / 2022）"
draft: false
math: false
sourceUrl: "https://www.oreilly.com/library/view/gao-xing-neng-mysql-di-4ban/9787121442575/"
demo: true
order: 0
---

import { Objectives } from "@/components/mdx/objectives";
import { Term } from "@/components/mdx/term";
import { Callout } from "@/components/mdx/callout";
import { Answer, Exercises } from "@/components/mdx/exercises";
import { Glossary, GlossaryItem } from "@/components/mdx/glossary";
import { Attribution } from "@/components/mdx/attribution";

# 附录A 升级MySQL

本课程对应Silvia Botros、Jeremy Tinley著，宁海元、周振兴、张新铭译《高性能MySQL（第4版）》，电子工业出版社2022年9月出版，纸书344页，ISBN 9787121442575。原版High Performance MySQL: Proven Strategies for Operating at Scale由O'Reilly于2021年11月发布，386页，ISBN 9781492080503。

官方中英文目录确认第4版为13章与附录A、B。课程按15个正式单元逐项重构，不沿用第3版的14章结构，也不复制原文；另设学习地图与总复习，共17页。本页覆盖9个正式节点。

<Objectives>

- 能解释“附录A 升级MySQL”的9个目录节点，并连接到“用发布说明、真实流量对照、灰度副本和自动化runbook降低版本升级风险”主线。
- 能复现兼容性矩阵、pt-upgrade对照、灰度结果和自动升级runbook，保存MySQL版本、Schema、数据量、配置、负载、预期与实际结果。
- 能比较基线、压力与故障条件，证明“新旧版本结果与错误差异已知，升级目标不承载写流量，失败可由升级前备份恢复”。
- 能设计反例推翻“把小版本升级视为无风险原地替换，忽略sql_mode、变量弃用和不可降级的数据格式”，并给出上线门、回退与独立验收条件。

</Objectives>

## 从用户SLO和一次故障开始

先预测：在30天窗口内，目标请求成功率、P95与P99延迟、允许错误分钟和恢复时间各是多少？再描述本章机制如何影响这些结果。不要先看图表再给故事；先固定服务目标、MySQL版本、数据规模、并发、缓存状态和故障点，实验后才允许用证据修正模型。

本页主问题是“用发布说明、真实流量对照、灰度副本和自动化runbook降低版本升级风险”。最终交付不是一组建议，而是兼容性矩阵、pt-upgrade对照、灰度结果和自动升级runbook，由另一位工程师证明：**新旧版本结果与错误差异已知，升级目标不承载写流量，失败可由升级前备份恢复**。

## 核心词汇与运行边界

<Term def="升级生命周期是“用发布说明、真实流量对照、灰度副本和自动化runbook降低版本升级风险”中的第1个核心概念；必须由版本、负载、机制、反例和指标定义">升级生命周期</Term>、<Term def="发布说明是“用发布说明、真实流量对照、灰度副本和自动化runbook降低版本升级风险”中的第2个核心概念；必须由版本、负载、机制、反例和指标定义">发布说明</Term>、<Term def="生产镜像是“用发布说明、真实流量对照、灰度副本和自动化runbook降低版本升级风险”中的第3个核心概念；必须由版本、负载、机制、反例和指标定义">生产镜像</Term>、<Term def="灰度副本是“用发布说明、真实流量对照、灰度副本和自动化runbook降低版本升级风险”中的第4个核心概念；必须由版本、负载、机制、反例和指标定义">灰度副本</Term>、<Term def="回退是“用发布说明、真实流量对照、灰度副本和自动化runbook降低版本升级风险”中的第5个核心概念；必须由版本、负载、机制、反例和指标定义">回退</Term>
构成本页词汇。每个词都要回答它改变哪个用户指标、由哪个MySQL或基础设施模块实现、在哪个压力或故障边界失效，以及用哪条独立证据发现失效。


## 第4版目录逐节点映射

### A.1 为什么升级

**节点合同 1/9 · 定义用户影响。** 先写“A.1 为什么升级”影响的用户请求、MySQL状态和资源，再把它接入“用发布说明、真实流量对照、灰度副本和自动化runbook降低版本升级风险”。上游是“本章服务目标”，下游是“A.2 升级生命周期”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“新旧版本结果与错误差异已知，升级目标不承载写流量，失败可由升级前备份恢复”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### A.2 升级生命周期

**节点合同 2/9 · 固定实验输入。** 先写“A.2 升级生命周期”影响的用户请求、MySQL状态和资源，再把它接入“用发布说明、真实流量对照、灰度副本和自动化runbook降低版本升级风险”。上游是“A.1 为什么升级”，下游是“A.3 测试升级”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“新旧版本结果与错误差异已知，升级目标不承载写流量，失败可由升级前备份恢复”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### A.3 测试升级

**节点合同 3/9 · 定位责任层。** 先写“A.3 测试升级”影响的用户请求、MySQL状态和资源，再把它接入“用发布说明、真实流量对照、灰度副本和自动化runbook降低版本升级风险”。上游是“A.2 升级生命周期”，下游是“A.3.1 开发环境测试”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“新旧版本结果与错误差异已知，升级目标不承载写流量，失败可由升级前备份恢复”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### A.3.1 开发环境测试

**节点合同 4/9 · 推演正常路径。** 先写“A.3.1 开发环境测试”影响的用户请求、MySQL状态和资源，再把它接入“用发布说明、真实流量对照、灰度副本和自动化runbook降低版本升级风险”。上游是“A.3 测试升级”，下游是“A.3.2 生产镜像”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“新旧版本结果与错误差异已知，升级目标不承载写流量，失败可由升级前备份恢复”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### A.3.2 生产镜像

**节点合同 5/9 · 注入失败条件。** 先写“A.3.2 生产镜像”影响的用户请求、MySQL状态和资源，再把它接入“用发布说明、真实流量对照、灰度副本和自动化runbook降低版本升级风险”。上游是“A.3.1 开发环境测试”，下游是“A.3.3 副本灰度”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“新旧版本结果与错误差异已知，升级目标不承载写流量，失败可由升级前备份恢复”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### A.3.3 副本灰度

**节点合同 6/9 · 收集独立证据。** 先写“A.3.3 副本灰度”影响的用户请求、MySQL状态和资源，再把它接入“用发布说明、真实流量对照、灰度副本和自动化runbook降低版本升级风险”。上游是“A.3.2 生产镜像”，下游是“A.3.4 升级工具”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“新旧版本结果与错误差异已知，升级目标不承载写流量，失败可由升级前备份恢复”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### A.3.4 升级工具

**节点合同 7/9 · 定义用户影响。** 先写“A.3.4 升级工具”影响的用户请求、MySQL状态和资源，再把它接入“用发布说明、真实流量对照、灰度副本和自动化runbook降低版本升级风险”。上游是“A.3.3 副本灰度”，下游是“A.4 大规模升级”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“新旧版本结果与错误差异已知，升级目标不承载写流量，失败可由升级前备份恢复”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### A.4 大规模升级

**节点合同 8/9 · 固定实验输入。** 先写“A.4 大规模升级”影响的用户请求、MySQL状态和资源，再把它接入“用发布说明、真实流量对照、灰度副本和自动化runbook降低版本升级风险”。上游是“A.3.4 升级工具”，下游是“A.5 小结”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“新旧版本结果与错误差异已知，升级目标不承载写流量，失败可由升级前备份恢复”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

### A.5 小结

**节点合同 9/9 · 定位责任层。** 先写“A.5 小结”影响的用户请求、MySQL状态和资源，再把它接入“用发布说明、真实流量对照、灰度副本和自动化runbook降低版本升级风险”。上游是“A.4 大规模升级”，下游是“本章生产交付物”；若无法解释这条因果关系，术语记忆不能算完成。

在固定MySQL 8.0小型业务库中先预测成功率、实际扫描行、锁或版本、日志位置、资源等待和恢复终点，再只改变一个变量。用“新旧版本结果与错误差异已知，升级目标不承载写流量，失败可由升级前备份恢复”验收正常与失败路径，并保存执行计划、Performance Schema摘要、状态变量或业务对账中真正支持结论的部分。

## 机制推演：从请求到资源与恢复

升级先阅读发布与升级说明，再用开发、生产镜像、真实副本和对照工具逐层增加真实性。MySQL 8.0不能简单降级，因此回退通常依赖升级前备份与拓扑切换；大规模执行还要防止选错可写目标。

生产推演始终拆成六层：用户请求与SLO、应用连接和事务、MySQL服务层、InnoDB页锁版本与日志、操作系统及硬件、复制备份和路由。先确定等待发生在哪一层，再选择指标和干预；参数、索引、分片与扩容属于不同层，不能用一个方案覆盖所有症状。

性能证据同时包含工作量和等待。工作量包括每秒请求、实际扫描行、返回字节、日志字节和脏页；等待包括CPU调度、I/O、锁、元数据锁、复制回放与队列。只有延迟下降且结果、持久性和错误预算没有恶化，优化才成立。

可靠性证据必须覆盖最坏时间点。对事务在提交前后崩溃，对副本在接收与应用之间中断，对备份在基线与binlog之间恢复，对分片在路由切换时重试。记录最后确认状态、数据损失上界、恢复步骤、恢复耗时和业务对账，避免把进程重新启动当作服务恢复。

<Callout type="trap">
  **优化陷阱。**
  把小版本升级视为无风险原地替换，忽略sql_mode、变量弃用和不可降级的数据格式。先恢复SLO、版本、工作负载和结果合同，再改变一项设计；不要用单次快结果掩盖尾延迟、数据风险或观测偏差。
</Callout>

<Callout type="trap">
  **因果陷阱。**
  指标同时变化不等于存在因果关系。缓存预热、流量分布、后台任务、复制延迟和云平台噪声都可能改变结果；必须保留对照并一次改变一个变量。
</Callout>

<Callout type="warning">
  DDL、参数、复制切换、恢复、分片和升级实验只在隔离环境执行。先验证备份和回退，规定最大写入、锁等待、错误预算消耗与停止条件。
</Callout>

## 证据解释与独立交接

服务证据固定为什么优化。保存SLI公式、SLO窗口、流量分母、延迟分位数和错误预算燃烧；若用户指标没有改善，底层计数器变漂亮也不能证明价值。缺失采样和维护窗口必须显式处理。

数据库证据解释发生了什么。保存Schema与统计、SQL和参数、计划估计与实际行、锁和版本、redo与binlog位置、状态变量及Performance Schema摘要。重置摘要或重启实例会改变基线，必须记录时间点。

资源证据定位限制。保存CPU利用率与运行队列、内存与交换、磁盘延迟和队列、网络、云盘限额及后台刷新。平均值不能替代分位数和饱和时段，客户端总延迟也不能自动归因于数据库执行。

故障证据证明可恢复。保存故障注入点、最后确认事务、晋升或恢复步骤、RPO/RTO实测、数据对账与旧源隔离。备份任务成功、副本在线或Pod重建都不是业务恢复的充分条件。

## 本章回顾

重新完成“用发布说明、真实流量对照、灰度副本和自动化runbook降低版本升级风险”：先写SLO和结果合同，按9个目录节点建立因果模型，固定负载，采集基线，只改变一个变量，注入失败并独立对账。最终交付兼容性矩阵、pt-upgrade对照、灰度结果和自动升级runbook，证明“新旧版本结果与错误差异已知，升级目标不承载写流量，失败可由升级前备份恢复”。

## 复习与生产验收

<Exercises>
**问题 1**：为什么“附录A 升级MySQL”必须覆盖9个正式节点？
<Answer>这些节点共同组成“用发布说明、真实流量对照、灰度副本和自动化runbook降低版本升级风险”的服务目标、机制、负载、故障和证据链；缺项会使兼容性矩阵、pt-upgrade对照、灰度结果和自动升级runbook无法复现。</Answer>

**问题 2**：本页最小运行不变量是什么？

<Answer>
  新旧版本结果与错误差异已知，升级目标不承载写流量，失败可由升级前备份恢复。必须由固定版本和负载、正常轨迹、压力数据、故障反例与业务对账共同证明。
</Answer>

**问题 3**：怎样构造最小反例？

<Answer>
  把“把小版本升级视为无风险原地替换，忽略sql_mode、变量弃用和不可降级的数据格式”写成待证假设，保持Schema与查询不变，只改变缓存、并发、数据分布或故障时点之一，再比较用户与数据库证据。
</Answer>

**问题 4**：为什么平均延迟不足以验收优化？

<Answer>
  平均值会隐藏排队、锁、I/O抖动和少数极慢请求；至少需要P95/P99、吞吐、错误率、实际扫描与等待，并确认结果语义不变。
</Answer>

**问题 5**：可靠性变更怎样验证？

<Answer>
  在最坏确认边界注入故障，记录最后确认事务、恢复或切换过程、RPO/RTO、数据一致性、旧源隔离与回退结果。
</Answer>

**问题 6**：独立交接至少包含什么？

<Answer>需要SLO、版本与配置、Schema与数据分布、负载脚本、兼容性矩阵、pt-upgrade对照、灰度结果和自动升级runbook、计划与等待、故障演练、业务对账和回退条件。</Answer>
</Exercises>

<Glossary>
  <GlossaryItem term="升级生命周期">
    升级生命周期以“新旧版本结果与错误差异已知，升级目标不承载写流量，失败可由升级前备份恢复”为运行边界，并由正常、压力与故障证据共同验证。
  </GlossaryItem>
  <GlossaryItem term="发布说明">
    发布说明以“新旧版本结果与错误差异已知，升级目标不承载写流量，失败可由升级前备份恢复”为运行边界，并由正常、压力与故障证据共同验证。
  </GlossaryItem>
  <GlossaryItem term="生产镜像">
    生产镜像以“新旧版本结果与错误差异已知，升级目标不承载写流量，失败可由升级前备份恢复”为运行边界，并由正常、压力与故障证据共同验证。
  </GlossaryItem>
  <GlossaryItem term="灰度副本">
    灰度副本以“新旧版本结果与错误差异已知，升级目标不承载写流量，失败可由升级前备份恢复”为运行边界，并由正常、压力与故障证据共同验证。
  </GlossaryItem>
  <GlossaryItem term="回退">
    回退以“新旧版本结果与错误差异已知，升级目标不承载写流量，失败可由升级前备份恢复”为运行边界，并由正常、压力与故障证据共同验证。
  </GlossaryItem>
</Glossary>

[← 上一页：第13章 MySQL的合规性](/learn/high-performance-mysql/13-compliance/hpm4-ch13-compliance) · [下一页：附录B Kubernetes上的MySQL →](/learn/high-performance-mysql/15-appendix-b/hpm4-appendix-b-kubernetes)

<Attribution
  adaptedFrom="《高性能MySQL（第4版）》"
  adaptedUrl="https://www.oreilly.com/library/view/gao-xing-neng-mysql-di-4ban/9787121442575/"
/>
