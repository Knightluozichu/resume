---
title: "第 3 章 计算机进行小数运算时出错的原因"
type: A
section: "程序怎样运行 · CPU与数据"
order: 3
description: "从二进制小数、浮点规格化、EXCESS 指数与十六进制观察解释舍入误差及规避方法。"
demo: true
math: false
sourceUrl: "https://www.zhihu.com/pub/book/119564974/contents"
draft: false
---

import {
  Hpw03FloatingPointMapLab,
  Hpw03FloatingPointExperimentLab,
  Hpw03FloatingPointEvidenceLab,
} from "@/components/mdx/how-programs-work/diagrams/hpw-03-floating-point";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第 3 章 计算机进行小数运算时出错的原因的核心机制，并说出它在原书12章与附录体系中的位置
- 能描述转换二进制小数、规格化尾数、编码偏置指数、按规则舍入、累计误差界、选择定点或容差六个阶段的源码输入、状态和接口
- 能实现最小实验，注入边界或失败条件并定位首个偏离点
- 能比较正常、边界、失败和恢复轨迹，判断本页不变量是否保持

</Objectives>

## 直觉起点

<Term def="浮点数 是“第 3 章 计算机进行小数运算时出错的原因”的第 1 个运行坐标，对应“转换二进制小数”；必须记录源码、机器状态、接口、失败与恢复证据。">浮点数</Term>、<Term def="尾数 是“第 3 章 计算机进行小数运算时出错的原因”的第 2 个运行坐标，对应“规格化尾数”；必须记录源码、机器状态、接口、失败与恢复证据。">尾数</Term>、<Term def="EXCESS 是“第 3 章 计算机进行小数运算时出错的原因”的第 3 个运行坐标，对应“编码偏置指数”；必须记录源码、机器状态、接口、失败与恢复证据。">EXCESS</Term>、<Term def="舍入误差 是“第 3 章 计算机进行小数运算时出错的原因”的第 4 个运行坐标，对应“按规则舍入”；必须记录源码、机器状态、接口、失败与恢复证据。">舍入误差</Term>、<Term def="十六进制 是“第 3 章 计算机进行小数运算时出错的原因”的第 5 个运行坐标，对应“累计误差界”；必须记录源码、机器状态、接口、失败与恢复证据。">十六进制</Term>
共同构成本页的观察坐标。有限二进制位无法精确表示多数十进制小数；浮点数用符号、尾数和偏置指数近似，舍入误差会在累加和比较中传播，因此业务必须选择误差模型。

程序并不是从源码直接跳到屏幕结果。中间经过编码、编译、装载、指令执行、内存访问、系统调用和设备状态。学习时先预测下一状态，再运行最小样本；只看最后输出会掩盖错误符号、越界、栈不平衡或驱动超时。

<Hpw03FloatingPointMapLab />

## 六阶段运行链

### 1. 转换二进制小数

先声明字长再解释位模式。移位、补码和逻辑掩码都可能丢弃高位；必须分别观察数值、进位和溢出，不能无限精度心算代替机器结果。

实验从“转换二进制小数”开始：固定源码、工具链、输入和环境，先预测寄存器、字节、页面、符号或设备状态；每次只改变一个字长、地址、容量、依赖或时序条件，在首个偏离点保存证据。删除故障后重放相同输入，不能只看最后界面。

### 2. 规格化尾数

规格化尾数 需要放回“转换二进制小数、规格化尾数、编码偏置指数、按规则舍入、累计误差界、选择定点或容差”的运行链，明确输入、机器状态、接口、失败和恢复。

实验从“规格化尾数”开始：固定源码、工具链、输入和环境，先预测寄存器、字节、页面、符号或设备状态；每次只改变一个字长、地址、容量、依赖或时序条件，在首个偏离点保存证据。删除故障后重放相同输入，不能只看最后界面。

### 3. 编码偏置指数

编码偏置指数 需要放回“转换二进制小数、规格化尾数、编码偏置指数、按规则舍入、累计误差界、选择定点或容差”的运行链，明确输入、机器状态、接口、失败和恢复。

实验从“编码偏置指数”开始：固定源码、工具链、输入和环境，先预测寄存器、字节、页面、符号或设备状态；每次只改变一个字长、地址、容量、依赖或时序条件，在首个偏离点保存证据。删除故障后重放相同输入，不能只看最后界面。

### 4. 按规则舍入

按规则舍入 需要放回“转换二进制小数、规格化尾数、编码偏置指数、按规则舍入、累计误差界、选择定点或容差”的运行链，明确输入、机器状态、接口、失败和恢复。

实验从“按规则舍入”开始：固定源码、工具链、输入和环境，先预测寄存器、字节、页面、符号或设备状态；每次只改变一个字长、地址、容量、依赖或时序条件，在首个偏离点保存证据。删除故障后重放相同输入，不能只看最后界面。

### 5. 累计误差界

累计误差界 需要放回“转换二进制小数、规格化尾数、编码偏置指数、按规则舍入、累计误差界、选择定点或容差”的运行链，明确输入、机器状态、接口、失败和恢复。

实验从“累计误差界”开始：固定源码、工具链、输入和环境，先预测寄存器、字节、页面、符号或设备状态；每次只改变一个字长、地址、容量、依赖或时序条件，在首个偏离点保存证据。删除故障后重放相同输入，不能只看最后界面。

### 6. 选择定点或容差

选择定点或容差 需要放回“转换二进制小数、规格化尾数、编码偏置指数、按规则舍入、累计误差界、选择定点或容差”的运行链，明确输入、机器状态、接口、失败和恢复。

实验从“选择定点或容差”开始：固定源码、工具链、输入和环境，先预测寄存器、字节、页面、符号或设备状态；每次只改变一个字长、地址、容量、依赖或时序条件，在首个偏离点保存证据。删除故障后重放相同输入，不能只看最后界面。

## 核心机制深挖

有限二进制位无法精确表示多数十进制小数；浮点数用符号、尾数和偏置指数近似，舍入误差会在累加和比较中传播，因此业务必须选择误差模型。 每次实验统一登记六类证据：**源码与输入、制品字节、机器状态、接口所有者、首个失败、恢复结果**。CPU页看寄存器和程序计数器，存储页看地址与页面，构建页看符号和映射，硬件页看寄存器、中断与DMA；观察对象不同，因果账本一致。

本页签发不变量是：**计算结果附带表示范围与误差界；比较使用容差或整数尺度，金融等精确十进制场景不直接累加二进制浮点。**。正常样本证明主路径，边界样本暴露字长和容量，失败样本确认责任层，恢复样本证明同一输入能回到受控结果。任何无法定位到首个状态分叉的成功都不是完整证据。

## 2015版语境与现代对照

原书基于32位x86、Windows、FreeBSD Port、Java虚拟机、DLL、BIOS、IN/OUT、DMA和C语言讲解。重构保留12章、1个附录、5个专栏及共118个公开目录条目；64位ABI、现代UEFI、容器或当代编译器只作为对照，不能替换原书问题身份。

现代系统扩大了地址和并行规模，却仍要解析符号、建立地址空间、遵守调用约定、跨越系统调用并协调设备。对照时必须写明原书行为、现代变化和仍然成立的不变量，避免把版本差异误判为原理消失。

## 完整公开目录逐项讲解

### 第3章 计算机进行小数运算时出错的原因

把十进制小数展开为二进制分数，记录有限尾数的舍入位置。累加实验同时计算理论值、机器值和误差界，再决定容差、定点数或十进制类型。

实验从“转换二进制小数”开始：固定源码、工具链、输入和环境，先预测寄存器、字节、页面、符号或设备状态；每次只改变一个字长、地址、容量、依赖或时序条件，在首个偏离点保存证据。删除故障后重放相同输入，不能只看最后界面。

### 3.1 将0.1累加100次也得不到10

把十进制小数展开为二进制分数，记录有限尾数的舍入位置。累加实验同时计算理论值、机器值和误差界，再决定容差、定点数或十进制类型。

实验从“规格化尾数”开始：固定源码、工具链、输入和环境，先预测寄存器、字节、页面、符号或设备状态；每次只改变一个字长、地址、容量、依赖或时序条件，在首个偏离点保存证据。删除故障后重放相同输入，不能只看最后界面。

### 3.2 用二进制数表示小数

先声明字长再解释位模式。移位、补码和逻辑掩码都可能丢弃高位；必须分别观察数值、进位和溢出，不能无限精度心算代替机器结果。

实验从“编码偏置指数”开始：固定源码、工具链、输入和环境，先预测寄存器、字节、页面、符号或设备状态；每次只改变一个字长、地址、容量、依赖或时序条件，在首个偏离点保存证据。删除故障后重放相同输入，不能只看最后界面。

### 3.3 计算机运算出错的原因

3.3 计算机运算出错的原因 需要放回“转换二进制小数、规格化尾数、编码偏置指数、按规则舍入、累计误差界、选择定点或容差”的运行链，明确输入、机器状态、接口、失败和恢复。

实验从“按规则舍入”开始：固定源码、工具链、输入和环境，先预测寄存器、字节、页面、符号或设备状态；每次只改变一个字长、地址、容量、依赖或时序条件，在首个偏离点保存证据。删除故障后重放相同输入，不能只看最后界面。

### 3.4 什么是浮点数

把十进制小数展开为二进制分数，记录有限尾数的舍入位置。累加实验同时计算理论值、机器值和误差界，再决定容差、定点数或十进制类型。

实验从“累计误差界”开始：固定源码、工具链、输入和环境，先预测寄存器、字节、页面、符号或设备状态；每次只改变一个字长、地址、容量、依赖或时序条件，在首个偏离点保存证据。删除故障后重放相同输入，不能只看最后界面。

### 3.5 正则表达式和EXCESS系统

把十进制小数展开为二进制分数，记录有限尾数的舍入位置。累加实验同时计算理论值、机器值和误差界，再决定容差、定点数或十进制类型。

实验从“选择定点或容差”开始：固定源码、工具链、输入和环境，先预测寄存器、字节、页面、符号或设备状态；每次只改变一个字长、地址、容量、依赖或时序条件，在首个偏离点保存证据。删除故障后重放相同输入，不能只看最后界面。

### 3.6 在实际的程序中进行确认

3.6 在实际的程序中进行确认 需要放回“转换二进制小数、规格化尾数、编码偏置指数、按规则舍入、累计误差界、选择定点或容差”的运行链，明确输入、机器状态、接口、失败和恢复。

实验从“转换二进制小数”开始：固定源码、工具链、输入和环境，先预测寄存器、字节、页面、符号或设备状态；每次只改变一个字长、地址、容量、依赖或时序条件，在首个偏离点保存证据。删除故障后重放相同输入，不能只看最后界面。

### 3.7 如何避免计算机计算出错

把十进制小数展开为二进制分数，记录有限尾数的舍入位置。累加实验同时计算理论值、机器值和误差界，再决定容差、定点数或十进制类型。

实验从“规格化尾数”开始：固定源码、工具链、输入和环境，先预测寄存器、字节、页面、符号或设备状态；每次只改变一个字长、地址、容量、依赖或时序条件，在首个偏离点保存证据。删除故障后重放相同输入，不能只看最后界面。

### 3.8 二进制数和十六进制数

先声明字长再解释位模式。移位、补码和逻辑掩码都可能丢弃高位；必须分别观察数值、进位和溢出，不能无限精度心算代替机器结果。

实验从“编码偏置指数”开始：固定源码、工具链、输入和环境，先预测寄存器、字节、页面、符号或设备状态；每次只改变一个字长、地址、容量、依赖或时序条件，在首个偏离点保存证据。删除故障后重放相同输入，不能只看最后界面。

## 状态与失败矩阵

| 对象         | 正常状态           | 常见边界             | 失败证据         | 恢复条件           |
| ------------ | ------------------ | -------------------- | ---------------- | ------------------ |
| 字节与寄存器 | 位宽和含义明确     | 溢出、舍入、错地址   | 位模式和指令位置 | 重置到已知状态     |
| 内存与存储   | 映射和生命周期有效 | 缺页、容量、悬空引用 | 页面与对象轨迹   | 重建映射或对象     |
| 构建与装载   | 符号和ABI兼容      | 缺库、重定位失败     | 制品和加载日志   | 使用同一可追溯制品 |
| 系统与硬件   | 权限和设备协议正确 | 超时、中断、DMA竞态  | 调用与寄存器状态 | 清理并重放请求     |

## 分步可视实验

<Stepper>
  <Step title="1. 建立运行地图">
    先预测源码到机器状态的变化，再启动固定样本。
    <Hpw03FloatingPointMapLab />
  </Step>
  <Step title="2. 注入单一边界">
    只改变字长、地址、容量、依赖或时序，在首个分叉处保存证据。
    <Hpw03FloatingPointExperimentLab />
  </Step>
  <Step title="3. 重放并恢复">
    删除故障，以相同输入重放，确认制品、状态和输出重新收敛。
    <Hpw03FloatingPointEvidenceLab />
  </Step>
</Stepper>

## 最小可运行实验

```text
let total = 0;
for (let index = 0; index < 100; index += 1) total += 0.1;
const error = total - 10;
const closeEnough = Math.abs(error) < 1e-12;
```

```yaml
book: 程序是怎样跑起来的
edition: 2015-04
page: hpw-03-floating-point
sample: normal | boundary | failure | recovery
predict_before_run: true
capture_first_divergence: true
replay_same_input: true
```

```text
freeze source, toolchain, input, and environment
predict bytes, registers, memory, symbols, or device state
change exactly one condition
stop at the first divergent boundary
remove the fault and replay the same input
verify artifact, state, output, and cleanup
```

## 常见误区与故障注入

<Callout type="trap">
  **误区一：源码正确就代表程序运行正确。**
  编译、链接、装载、ABI、内存和设备任一层都可能失败，必须追踪完整执行链。
</Callout>

<Callout type="trap">
  **误区二：用今天的平台术语覆盖原书目录。**
  现代工具可以做对照，但二进制、浮点误差、压缩、运行环境、汇编和硬件控制主线不能被删掉。
</Callout>

## 四类样本与验收

| 样本 | 注入方式                 | 必查证据             | 通过条件     |
| ---- | ------------------------ | -------------------- | ------------ |
| 正常 | 固定源码、工具链和输入   | 全阶段状态与输出     | 与预测一致   |
| 边界 | 最大字长、空输入、低内存 | 溢出、终止与资源状态 | 不越界不泄漏 |
| 失败 | 缺库、错地址、设备超时   | 首个错误与责任层     | 失败被隔离   |
| 恢复 | 删除故障后重放           | 制品、状态和输出     | 回到受控结果 |

<Hpw03FloatingPointEvidenceLab />

目录证据：第3章 计算机进行小数运算时出错的原因、3.1 将0.1累加100次也得不到10、3.2 用二进制数表示小数、3.3 计算机运算出错的原因、3.4 什么是浮点数、3.5 正则表达式和EXCESS系统、3.6 在实际的程序中进行确认、3.7 如何避免计算机计算出错、3.8 二进制数和十六进制数。本页按完整公开目录独立教学重构，不复制原书正文；9个条目全部进入源码、字节、状态、接口、失败和恢复证据链。

## 练习

<Exercises>

**问题 1** 为什么不能只看源码和最终输出验收第 3 章 计算机进行小数运算时出错的原因？

<Answer>
  源码与输出之间还有编译、链接、装载、指令、内存、系统调用和设备状态。必须先预测，注入单一失败，保存首个偏离点并重放相同输入。
</Answer>

**问题 2** 如何把本页目录变成可执行证据？

<Answer>
  依次登记“第3章 计算机进行小数运算时出错的原因、3.1
  将0.1累加100次也得不到10、3.2 用二进制数表示小数、3.3
  计算机运算出错的原因、3.4 什么是浮点数、3.5 正则表达式和EXCESS系统、3.6
  在实际的程序中进行确认、3.7 如何避免计算机计算出错、3.8
  二进制数和十六进制数”，为每项写出源码输入、制品字节、机器状态、接口所有者、边界条件、失败出口和恢复结果，再回放四类样本。
</Answer>

**问题 3** 本页最小通过条件是什么？

<Answer>
  计算结果附带表示范围与误差界；比较使用容差或整数尺度，金融等精确十进制场景不直接累加二进制浮点。
  同时，异常不会污染后续样本，恢复轨迹重新到达与正常样本一致的结果。
</Answer>

</Exercises>

## 小结

- 浮点数：对应“转换二进制小数”的核心观察量。
- 尾数：对应“规格化尾数”的核心观察量。
- EXCESS：对应“编码偏置指数”的核心观察量。
- 舍入误差：对应“按规则舍入”的核心观察量。
- 十六进制：对应“累计误差界”的核心观察量。
- 9个本页公开目录条目已全部映射到运行链和实验账本。
- 最终输出只是证据之一，制品、机器状态、边界失败与恢复共同决定是否通过。

## 术语表

<Glossary>
  <GlossaryItem term="浮点数">
    浮点数
    对应“转换二进制小数”，验收时连接源码、字节、运行状态、接口边界、失败传播和恢复结果。
  </GlossaryItem>
  <GlossaryItem term="尾数">
    尾数
    对应“规格化尾数”，验收时连接源码、字节、运行状态、接口边界、失败传播和恢复结果。
  </GlossaryItem>
  <GlossaryItem term="EXCESS">
    EXCESS
    对应“编码偏置指数”，验收时连接源码、字节、运行状态、接口边界、失败传播和恢复结果。
  </GlossaryItem>
  <GlossaryItem term="舍入误差">
    舍入误差
    对应“按规则舍入”，验收时连接源码、字节、运行状态、接口边界、失败传播和恢复结果。
  </GlossaryItem>
  <GlossaryItem term="十六进制">
    十六进制
    对应“累计误差界”，验收时连接源码、字节、运行状态、接口边界、失败传播和恢复结果。
  </GlossaryItem>
</Glossary>

<Attribution
  adaptedFrom="矢泽久雄 · 程序是怎样跑起来的 · 人民邮电出版社 · 2015-04 · 完整公开目录"
  adaptedUrl="https://read.douban.com/ebook/329676432/"
/>
