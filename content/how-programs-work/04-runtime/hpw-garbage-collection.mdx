---
title: "垃圾回收"
type: A
section: "系统与操作系统"
order: 8
description: "理解手动与自动内存管理、GC 标记-清除与分代回收算法——运行时如何自动回收堆内存"
demo: true
math: false
sourceUrl: ""
draft: false
---

import { HpwGarbageCollectionDiagram } from "@/components/mdx/how-programs-work/diagrams/hpw-garbage-collection";
import { Objectives, Callout, Glossary, GlossaryItem, Term, Exercises, Answer } from "@/components/mdx/mdx-components";

<Objectives>
- 理解手动内存管理的痛点与垃圾回收要解决的核心问题
- 掌握 GC 如何判定对象存活（可达性分析）与回收无用对象
- 理解标记-清除、复制、分代回收三种经典算法的权衡
- 建立「GC 用暂停换安全，用分代换吞吐」这一运行时设计心智
</Objectives>

## 直觉引入

把堆想象成一个仓库，malloc 是往里搬箱子，free 是搬走箱子。手动管理（C/C++）是你自己记账：每搬进一个箱子记一笔，用完亲自搬走并销账——忘搬就是仓库越塞越满（内存泄漏），搬错了或搬两次就出事（悬空指针/双释放）。垃圾回收（Java/Python/Go）是雇了个仓库管理员（GC）：你只管搬进和用，管理员定期巡视，把「没有任何人还能碰到的箱子」自动清掉。你不用记账了，但管理员巡视时仓库要暂停营业（STW 暂停），这就是 GC 的代价——用偶尔的停顿换来内存安全。

<HpwGarbageCollectionDiagram />

## 手动 vs 自动

<Term def="垃圾回收（GC）是运行时系统自动识别并回收堆上不再被引用的对象的机制，免去程序员手动 free 的负担。">垃圾回收</Term>要解决的核心问题是手动内存管理的两类 bug：①**内存泄漏**——申请了不释放；②**悬空指针/双释放**——释放了还在用、或释放两次。GC 让程序员只管分配（new），由运行时定期找出「再也不会被访问到的对象」并自动回收其内存。代价是：①运行时开销（GC 算法本身要花 CPU）；②停顿（标记/整理堆时可能要暂停所有应用线程，即 Stop-The-World）；③不确定性（你不知道 GC 何时发生、回收多少，对实时性要求高的场景是隐患）。权衡是：GC 用一点性能和停顿，换来「不泄漏、不悬空」的内存安全，对大多数应用值得。

## 可达性分析

GC 怎么判断一个对象「该回收」？不能看「还有没有变量名指向它」（运行时没有变量名），而要看「还能不能被程序访问到」。方法是从一组**GC Roots**（根对象）出发，沿引用链遍历——能从根到达的对象是「存活的」，到达不了的就是「垃圾」。GC Roots 包括：①栈上的局部变量；②全局/静态变量；③当前正在执行的寄存器里的引用。一个对象即使还被某个局部变量指向，但如果那个变量所在的函数早已返回（栈帧弹出），它就不再是 root，自然不可达——这就是 GC 能自动回收「函数内 new 了但没返回出去」的对象的原因。注意：循环引用（A 指向 B、B 指向 A）的两个对象，如果都不可从 root 到达，依然会被回收——这是可达性分析相比「引用计数」的优势。

## 经典算法

三种经典 GC 算法：①**标记-清除（Mark-Sweep）**——先从 root 出发标记所有可达对象，再遍历堆清除未标记的。简单但产生碎片（空闲内存不连续）。②**复制（Copying）**——把堆分两半，存活对象从 From 半区复制到 To 半区并紧凑排列，然后整块清空 From 半区。无碎片、分配快，但浪费一半空间，适合存活率低的新生代。③**标记-整理（Mark-Compact）**——标记后把存活对象向一端移动紧凑排列。无碎片且不浪费空间，但移动对象要更新所有引用，开销大，适合存活率高的老年代。

## 分代回收

<Term def="分代回收基于「多数对象朝生夕死」的统计规律，把堆分为新生代和老年代，对不同代用不同算法以提升整体效率。">分代回收</Term>是现代 GC（JVM/Go/.NET）的核心优化。观察发现：绝大多数对象分配后很快就变成垃圾（如临时变量、中间字符串），只有少数长期存活。于是把堆分两代：**新生代**用复制算法（存活率低，复制开销小，且能整块清空），对象熬过几次 GC 就**晋升**到老年代；**老年代**用标记-清除或标记-整理（存活率高，不适合复制）。这样大部分 GC 只扫描小的新生代（Minor GC，快），偶尔才扫描整个堆（Full GC，慢）。分代回收用「对不同的对象用不同的策略」大幅提升了 GC 效率。

## 常见误区

<Callout type="trap">
**误区：有 GC 就不会内存泄漏了。** GC 回收的是「不可达」的对象，但如果你把对象放进一个长生命周期容器（全局 List、静态 Map、缓存）却忘记移除，这个对象一直「可达」，GC 永远不会回收它——这就是 GC 语言里的内存泄漏。典型场景：监听器注册了没注销、缓存只增不减、集合当临时变量用却忘了清。GC 语言的泄漏不像 C 那样直接 free 漏掉，而是「逻辑上不再需要但引用还没断」。同样，GC 也不能消除 OOM——如果存活对象总量超过堆大小（如缓存无限增长、大对象未释放引用），GC 回收再多也救不了。理解「泄漏 = 该断的引用没断」，才能在 GC 语言里也写出不漏内存的代码。
</Callout>

<Exercises>
**问题 1**：GC 如何判断对象该回收？为什么循环引用的两个对象能被回收？

<Answer>
GC 用可达性分析：从 GC Roots（栈上的局部变量、全局/静态变量、寄存器中的引用）出发沿引用链遍历，能到达的是存活对象，到达不了的就是垃圾。循环引用（A→B、B→A）的两个对象，如果都不在 root 的引用链上（即没有任何外部引用指向 A 或 B），那从 root 出发根本到不了它们，二者都是不可达的，会被一起回收。这是可达性分析相比引用计数的优势——引用计数会因为互相引用导致计数永不为 0 而泄漏，可达性分析只看从根能否到达。
</Answer>

**问题 2**：标记-清除、复制、标记-整理三种算法各有什么优劣？分代回收如何组合它们？

<Answer>
①标记-清除：先标记可达对象再清除未标记的，简单但产生碎片。②复制：存活对象从 From 复制到 To 半区紧凑排列再整块清空 From，无碎片分配快但浪费一半空间，适合存活率低的新生代。③标记-整理：标记后把存活对象向一端移动紧凑排列，无碎片不浪费空间但移动开销大，适合存活率高的老年代。分代回收组合：新生代存活率低用复制算法（Minor GC 快），熬过几次 GC 的对象晋升到老年代；老年代存活率高用标记-清除或标记-整理。大部分 GC 只扫新生代（快），偶尔才 Full GC 扫整堆，整体效率大幅提升。
</Answer>
</Exercises>

<Glossary>
<GlossaryItem term="垃圾回收">运行时系统自动识别并回收堆上不再被引用对象的机制。用可达性分析判断存活，免去手动 free。代价是运行时开销、STW 停顿和回收时机不确定。</GlossaryItem>
<GlossaryItem term="可达性分析">从 GC Roots（栈变量/全局变量/寄存器引用）出发沿引用链遍历，能到达的是存活对象，到达不了的是垃圾。循环引用但不可达的对象会被回收，优于引用计数。</GlossaryItem>
<GlossaryItem term="分代回收">基于「多数对象朝生夕死」把堆分新生代（复制算法）和老年代（标记-清除/整理），新生代 GC 快、老年代偶尔扫。大幅提升整体 GC 效率，是现代 GC 核心。</GlossaryItem>
</Glossary>
