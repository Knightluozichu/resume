---
title: "《程序是怎样跑起来的》全书总复习"
type: A
section: "程序怎样运行 · 复盘"
order: 14
description: "从一次C函数调用追踪源代码、编译链接、装载、CPU、内存、系统调用、设备中断和最终输出。"
demo: true
math: false
sourceUrl: "https://www.zhihu.com/pub/book/119564974/contents"
draft: false
---


## 直觉起点

、、、、共同构成本页的观察坐标。总复习把源代码经编译与链接变成本地代码，加载器建立地址空间，CPU按寄存器和栈执行，系统调用进入内核，设备通过中断或DMA返回数据，最终结果再被程序解释。

程序并不是从源码直接跳到屏幕结果。中间经过编码、编译、装载、指令执行、内存访问、系统调用和设备状态。学习时先预测下一状态，再运行最小样本；只看最后输出会掩盖错误符号、越界、栈不平衡或驱动超时。

## 六阶段运行链

### 1. 冻结源码输入

冻结源码输入 需要放回“冻结源码输入、生成链接制品、装载地址空间、执行指令栈帧、进入内核设备、核对输出恢复”的运行链，明确输入、机器状态、接口、失败和恢复。

实验从“冻结源码输入”开始：固定源码、工具链、输入和环境，先预测寄存器、字节、页面、符号或设备状态；每次只改变一个字长、地址、容量、依赖或时序条件，在首个偏离点保存证据。删除故障后重放相同输入，不能只看最后界面。

### 2. 生成链接制品

保存预处理、目标文件、符号表、重定位、依赖库和装载映射。编译成功只是中间状态；链接和加载失败必须定位到具体符号、ABI或段权限。

实验从“生成链接制品”开始：固定源码、工具链、输入和环境，先预测寄存器、字节、页面、符号或设备状态；每次只改变一个字长、地址、容量、依赖或时序条件，在首个偏离点保存证据。删除故障后重放相同输入，不能只看最后界面。

### 3. 装载地址空间

逐指令记录程序计数器、寄存器、标志与相关内存。分支只由条件码改变下一地址，函数调用必须保存返回地址和调用者要求保留的状态。

实验从“装载地址空间”开始：固定源码、工具链、输入和环境，先预测寄存器、字节、页面、符号或设备状态；每次只改变一个字长、地址、容量、依赖或时序条件，在首个偏离点保存证据。删除故障后重放相同输入，不能只看最后界面。

### 4. 执行指令栈帧

地址只在对象生命周期和边界内有效。每次插入、删除、入栈或出队后检查头尾、容量与可达性，避免最终输出正常却留下悬空引用。

实验从“执行指令栈帧”开始：固定源码、工具链、输入和环境，先预测寄存器、字节、页面、符号或设备状态；每次只改变一个字长、地址、容量、依赖或时序条件，在首个偏离点保存证据。删除故障后重放相同输入，不能只看最后界面。

### 5. 进入内核设备

进入内核设备 需要放回“冻结源码输入、生成链接制品、装载地址空间、执行指令栈帧、进入内核设备、核对输出恢复”的运行链，明确输入、机器状态、接口、失败和恢复。

实验从“进入内核设备”开始：固定源码、工具链、输入和环境，先预测寄存器、字节、页面、符号或设备状态；每次只改变一个字长、地址、容量、依赖或时序条件，在首个偏离点保存证据。删除故障后重放相同输入，不能只看最后界面。

### 6. 核对输出恢复

核对输出恢复 需要放回“冻结源码输入、生成链接制品、装载地址空间、执行指令栈帧、进入内核设备、核对输出恢复”的运行链，明确输入、机器状态、接口、失败和恢复。

实验从“核对输出恢复”开始：固定源码、工具链、输入和环境，先预测寄存器、字节、页面、符号或设备状态；每次只改变一个字长、地址、容量、依赖或时序条件，在首个偏离点保存证据。删除故障后重放相同输入，不能只看最后界面。

## 核心机制深挖

总复习把源代码经编译与链接变成本地代码，加载器建立地址空间，CPU按寄存器和栈执行，系统调用进入内核，设备通过中断或DMA返回数据，最终结果再被程序解释。 每次实验统一登记六类证据：**源码与输入、制品字节、机器状态、接口所有者、首个失败、恢复结果**。CPU页看寄存器和程序计数器，存储页看地址与页面，构建页看符号和映射，硬件页看寄存器、中断与DMA；观察对象不同，因果账本一致。

本页签发不变量是：**同一源码、工具链与环境得到可追溯制品；正常、边界、失败和恢复样本都能关联到首个偏离的指令、内存、接口或设备状态。**。正常样本证明主路径，边界样本暴露字长和容量，失败样本确认责任层，恢复样本证明同一输入能回到受控结果。任何无法定位到首个状态分叉的成功都不是完整证据。

## 2015版语境与现代对照

原书基于32位x86、Windows、FreeBSD Port、Java虚拟机、DLL、BIOS、IN/OUT、DMA和C语言讲解。重构保留12章、1个附录、5个专栏及共118个公开目录条目；64位ABI、现代UEFI、容器或当代编译器只作为对照，不能替换原书问题身份。

现代系统扩大了地址和并行规模，却仍要解析符号、建立地址空间、遵守调用约定、跨越系统调用并协调设备。对照时必须写明原书行为、现代变化和仍然成立的不变量，避免把版本差异误判为原理消失。

## 完整公开目录逐项讲解

### 第1章 对程序员来说CPU是什么

逐指令记录程序计数器、寄存器、标志与相关内存。分支只由条件码改变下一地址，函数调用必须保存返回地址和调用者要求保留的状态。

实验从“冻结源码输入”开始：固定源码、工具链、输入和环境，先预测寄存器、字节、页面、符号或设备状态；每次只改变一个字长、地址、容量、依赖或时序条件，在首个偏离点保存证据。删除故障后重放相同输入，不能只看最后界面。

### 第2章 数据是用二进制数表示的

先声明字长再解释位模式。移位、补码和逻辑掩码都可能丢弃高位；必须分别观察数值、进位和溢出，不能无限精度心算代替机器结果。

实验从“生成链接制品”开始：固定源码、工具链、输入和环境，先预测寄存器、字节、页面、符号或设备状态；每次只改变一个字长、地址、容量、依赖或时序条件，在首个偏离点保存证据。删除故障后重放相同输入，不能只看最后界面。

### 第3章 计算机进行小数运算时出错的原因

把十进制小数展开为二进制分数，记录有限尾数的舍入位置。累加实验同时计算理论值、机器值和误差界，再决定容差、定点数或十进制类型。

实验从“装载地址空间”开始：固定源码、工具链、输入和环境，先预测寄存器、字节、页面、符号或设备状态；每次只改变一个字长、地址、容量、依赖或时序条件，在首个偏离点保存证据。删除故障后重放相同输入，不能只看最后界面。

### 第4章 熟练使用有棱有角的内存

地址只在对象生命周期和边界内有效。每次插入、删除、入栈或出队后检查头尾、容量与可达性，避免最终输出正常却留下悬空引用。

实验从“执行指令栈帧”开始：固定源码、工具链、输入和环境，先预测寄存器、字节、页面、符号或设备状态；每次只改变一个字长、地址、容量、依赖或时序条件，在首个偏离点保存证据。删除故障后重放相同输入，不能只看最后界面。

### 第5章 内存和磁盘的亲密关系

地址只在对象生命周期和边界内有效。每次插入、删除、入栈或出队后检查头尾、容量与可达性，避免最终输出正常却留下悬空引用。

实验从“进入内核设备”开始：固定源码、工具链、输入和环境，先预测寄存器、字节、页面、符号或设备状态；每次只改变一个字长、地址、容量、依赖或时序条件，在首个偏离点保存证据。删除故障后重放相同输入，不能只看最后界面。

### 第6章 亲自尝试压缩数据

先统计样本分布，再生成游程或前缀码。压缩率必须包含码表与头部；可逆实验用逐字节摘要验证，随机数据还要检查最坏膨胀。

实验从“核对输出恢复”开始：固定源码、工具链、输入和环境，先预测寄存器、字节、页面、符号或设备状态；每次只改变一个字长、地址、容量、依赖或时序条件，在首个偏离点保存证据。删除故障后重放相同输入，不能只看最后界面。

### 第7章 程序是在何种环境中运行的

第7章 程序是在何种环境中运行的 需要放回“冻结源码输入、生成链接制品、装载地址空间、执行指令栈帧、进入内核设备、核对输出恢复”的运行链，明确输入、机器状态、接口、失败和恢复。

实验从“冻结源码输入”开始：固定源码、工具链、输入和环境，先预测寄存器、字节、页面、符号或设备状态；每次只改变一个字长、地址、容量、依赖或时序条件，在首个偏离点保存证据。删除故障后重放相同输入，不能只看最后界面。

### 第8章 从源文件到可执行文件

保存预处理、目标文件、符号表、重定位、依赖库和装载映射。编译成功只是中间状态；链接和加载失败必须定位到具体符号、ABI或段权限。

实验从“生成链接制品”开始：固定源码、工具链、输入和环境，先预测寄存器、字节、页面、符号或设备状态；每次只改变一个字长、地址、容量、依赖或时序条件，在首个偏离点保存证据。删除故障后重放相同输入，不能只看最后界面。

### 第9章 操作系统和应用的关系

把依赖分为指令集、操作系统API、语言运行时和设备。迁移时逐层替换或虚拟化，并保存启动链、版本和失败出口，不能只以能启动判定兼容。

实验从“装载地址空间”开始：固定源码、工具链、输入和环境，先预测寄存器、字节、页面、符号或设备状态；每次只改变一个字长、地址、容量、依赖或时序条件，在首个偏离点保存证据。删除故障后重放相同输入，不能只看最后界面。

### 第10章 通过汇编语言了解程序的实际构成

把高级语句和编译器输出逐段对齐，跟踪mov数据流、push/pop栈平衡、变量地址与跳转目标。伪指令只影响布局，不应被当成CPU指令。

实验从“执行指令栈帧”开始：固定源码、工具链、输入和环境，先预测寄存器、字节、页面、符号或设备状态；每次只改变一个字长、地址、容量、依赖或时序条件，在首个偏离点保存证据。删除故障后重放相同输入，不能只看最后界面。

### 第11章 硬件控制方法

设备寄存器、状态位和时序构成硬件协议。注入忙、超时、中断丢失和DMA完成边界，确认处理程序恢复现场且缓冲区不会提前释放。

实验从“进入内核设备”开始：固定源码、工具链、输入和环境，先预测寄存器、字节、页面、符号或设备状态；每次只改变一个字长、地址、容量、依赖或时序条件，在首个偏离点保存证据。删除故障后重放相同输入，不能只看最后界面。

### 第12章 让计算机“思考”

所谓思考被实现为规则、状态、随机选择和历史记忆。固定随机种子并保存决策轨迹，区分程序能执行的步骤与它没有获得的语义和目标。

实验从“核对输出恢复”开始：固定源码、工具链、输入和环境，先预测寄存器、字节、页面、符号或设备状态；每次只改变一个字长、地址、容量、依赖或时序条件，在首个偏离点保存证据。删除故障后重放相同输入，不能只看最后界面。

### 附录 让我们开始C语言之旅

开启编译告警，用最小程序观察类型宽度、参数传递、局部与全局存储期以及数组边界。每个语言结构都回连到前文的汇编和内存状态。

实验从“冻结源码输入”开始：固定源码、工具链、输入和环境，先预测寄存器、字节、页面、符号或设备状态；每次只改变一个字长、地址、容量、依赖或时序条件，在首个偏离点保存证据。删除故障后重放相同输入，不能只看最后界面。

### 结语

结语 需要放回“冻结源码输入、生成链接制品、装载地址空间、执行指令栈帧、进入内核设备、核对输出恢复”的运行链，明确输入、机器状态、接口、失败和恢复。

实验从“生成链接制品”开始：固定源码、工具链、输入和环境，先预测寄存器、字节、页面、符号或设备状态；每次只改变一个字长、地址、容量、依赖或时序条件，在首个偏离点保存证据。删除故障后重放相同输入，不能只看最后界面。

### 致谢

致谢 需要放回“冻结源码输入、生成链接制品、装载地址空间、执行指令栈帧、进入内核设备、核对输出恢复”的运行链，明确输入、机器状态、接口、失败和恢复。

实验从“装载地址空间”开始：固定源码、工具链、输入和环境，先预测寄存器、字节、页面、符号或设备状态；每次只改变一个字长、地址、容量、依赖或时序条件，在首个偏离点保存证据。删除故障后重放相同输入，不能只看最后界面。

## 状态与失败矩阵

| 对象         | 正常状态           | 常见边界             | 失败证据         | 恢复条件           |
| ------------ | ------------------ | -------------------- | ---------------- | ------------------ |
| 字节与寄存器 | 位宽和含义明确     | 溢出、舍入、错地址   | 位模式和指令位置 | 重置到已知状态     |
| 内存与存储   | 映射和生命周期有效 | 缺页、容量、悬空引用 | 页面与对象轨迹   | 重建映射或对象     |
| 构建与装载   | 符号和ABI兼容      | 缺库、重定位失败     | 制品和加载日志   | 使用同一可追溯制品 |
| 系统与硬件   | 权限和设备协议正确 | 超时、中断、DMA竞态  | 调用与寄存器状态 | 清理并重放请求     |

## 最小可运行实验

```text
const trace = [];
for (const stage of stages) {
  const expected = predict(stage, state);
  state = run(stage, state);
  trace.push({ stage, expected, actual: snapshot(state) });
}
```

```yaml
book: 程序是怎样跑起来的
edition: 2015-04
page: hpw-official-final-review
sample: normal | boundary | failure | recovery
predict_before_run: true
capture_first_divergence: true
replay_same_input: true
```

```text
freeze source, toolchain, input, and environment
predict bytes, registers, memory, symbols, or device state
change exactly one condition
stop at the first divergent boundary
remove the fault and replay the same input
verify artifact, state, output, and cleanup
```

## 常见误区与故障注入


## 四类样本与验收

| 样本 | 注入方式                 | 必查证据             | 通过条件     |
| ---- | ------------------------ | -------------------- | ------------ |
| 正常 | 固定源码、工具链和输入   | 全阶段状态与输出     | 与预测一致   |
| 边界 | 最大字长、空输入、低内存 | 溢出、终止与资源状态 | 不越界不泄漏 |
| 失败 | 缺库、错地址、设备超时   | 首个错误与责任层     | 失败被隔离   |
| 恢复 | 删除故障后重放           | 制品、状态和输出     | 回到受控结果 |

目录证据：第1章 对程序员来说CPU是什么、第2章 数据是用二进制数表示的、第3章 计算机进行小数运算时出错的原因、第4章 熟练使用有棱有角的内存、第5章 内存和磁盘的亲密关系、第6章 亲自尝试压缩数据、第7章 程序是在何种环境中运行的、第8章 从源文件到可执行文件、第9章 操作系统和应用的关系、第10章 通过汇编语言了解程序的实际构成、第11章 硬件控制方法、第12章 让计算机“思考”、附录 让我们开始C语言之旅、结语、致谢。本页按完整公开目录独立教学重构，不复制原书正文；15个条目全部进入源码、字节、状态、接口、失败和恢复证据链。

## 练习


## 小结

- 源代码：对应“冻结源码输入”的核心观察量。
- 可执行文件：对应“生成链接制品”的核心观察量。
- 地址空间：对应“装载地址空间”的核心观察量。
- 系统调用：对应“执行指令栈帧”的核心观察量。
- 执行轨迹：对应“进入内核设备”的核心观察量。
- 15个本页公开目录条目已全部映射到运行链和实验账本。
- 最终输出只是证据之一，制品、机器状态、边界失败与恢复共同决定是否通过。

## 术语表


