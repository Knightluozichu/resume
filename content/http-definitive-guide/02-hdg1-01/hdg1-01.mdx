---
title: "第1章 http 概述"
description: "建立HTTP资源、事务、报文、连接、版本和Web结构组件的全景模型"
type: A
section: "HTTP权威指南（2002年英文首版 / 2012年中译本）"
order: 2
draft: false
math: false
sourceUrl: "https://www.oreilly.com/library/view/http-the-definitive/1565925092/"
demo: true
---

import {
  Hdg101MessageLab,
  Hdg101DecisionLab,
  Hdg101EvidenceLab,
} from "@/components/mdx/http-definitive-guide/diagrams/hdg1-01";

# 第1章 http 概述

本课程锁定David Gourley、Brian Totty、Marjorie Sayer、Sailu Reddy、Anshu Aggarwal著，陈涓、赵振平译《HTTP权威指南》，人民邮电出版社，2012年，ISBN 9787115281487；英文原版HTTP: The Definitive Guide，O'Reilly Media，2002年9月，656页，ISBN 1565925092。O'Reilly官方页面确认英文首版的5个正文部分、21章和8个附录；中文版完整印刷目录另含“第六部分 附录”和索引。忠实度分母共586个部分、章、编号节/小节、附录与索引节点。

<Objectives>

- 能解释“第1章 http 概述”的全部正式节点及其HTTP事务位置。
- 能绘制请求、响应、TCP连接、中间实体和资源状态。
- 能设计单变量实验，验证“每次事务都能定位资源、方法、状态、报文方向、TCP端点和中间组件”。
- 能写出含首版边界、原始报文、故障、恢复和复核人的证据包。

</Objectives>

## 从一条可证伪的HTTP事务开始

先预测：只把HTTP理解为浏览器发GET会遗漏多对象页面、代理、缓存、网关、隧道和用户代理。把预测写成“URL/资源、连接、请求报文、中间实体、响应报文、身份与编码”六行，再运行客户端、代理或服务器。结果不同先定位首个字节或状态偏差。

本页主问题是：建立HTTP资源、事务、报文、连接、版本和Web结构组件的全景模型。每条消息要注明发送者、接收者、HTTP版本、连接复用、逐跳/端到端首部、主体边界与历史协议状态。

验收不变量是：每次事务都能定位资源、方法、状态、报文方向、TCP端点和中间组件。最终页面正常、状态码200或TLS通道建立都只是局部事实，不能单独证明资源身份、缓存变体、代理转发与证书身份全部正确。

## 核心词汇与首版边界

<Term def="在客户端、服务器和中间实体之间交换资源表述的应用层协议">HTTP</Term>、
<Term def="可由URI标识并通过表示形式传输的网络对象">资源</Term>、
<Term def="一条请求和对应响应组成的HTTP交互">事务</Term>、
<Term def="标识或定位资源的统一资源标识符">URI</Term>、
<Term def="代表用户生成HTTP请求并处理响应的客户端程序">用户代理</Term>

这些词汇固定在2002年英文首版和2012年中译本语境。HTTP/2、HTTP/3、OAuth、JWT、SameSite、HSTS和现代CDN行为可以独立比较，但不得替换HTTP-NG、Digest、WebDAV、WPAD等原书节点。

<Hdg101MessageLab />

## 核心机制深读

### 先从HTTP画出事务

建立HTTP资源、事务、报文、连接、版本和Web结构组件的全景模型。先固定资源身份和请求目标，再标出客户端、中间实体与源服务器，任何优化或安全结论都不能跳过原始报文。

动手试：先手写预期请求与响应，再抓取正常事务和一个单变量故障事务。标出首个不同字节或状态，不用最终页面猜根因。

### 区分HTTP与资源

HTTP和资源可能在同一事务中协作，但责任、状态位置和失败语义不同。对比正常请求、单个首部变化和单个连接故障，定位首个偏差。

动手试：先手写预期请求与响应，再抓取正常事务和一个单变量故障事务。标出首个不同字节或状态，不用最终页面猜根因。

### 让事务经过真实中间实体

代理、缓存、网关或隧道会读取、添加、删除或转发不同首部。逐跳记录Via、Connection列出的字段、缓存决策和认证边界。

动手试：先手写预期请求与响应，再抓取正常事务和一个单变量故障事务。标出首个不同字节或状态，不用最终页面猜根因。

### 验证URI的历史语境

原书写于2002年，以HTTP/1.0、HTTP/1.1、SSL/TLS早期版本和当时Web基础设施为背景。现代实现可做对照，但不能冒充原书节点。

动手试：先手写预期请求与响应，再抓取正常事务和一个单变量故障事务。标出首个不同字节或状态，不用最终页面猜根因。

### 用用户代理关闭证据门

每次事务都能定位资源、方法、状态、报文方向、TCP端点和中间组件。证据至少包含原始请求/响应、TCP时间线、中间状态、失败注入、恢复动作和第三方复核。

动手试：先手写预期请求与响应，再抓取正常事务和一个单变量故障事务。标出首个不同字节或状态，不用最终页面猜根因。

## 原书目录逐节点重构

### 第1章 http 概述

把“http 概述”放回“建立HTTP资源、事务、报文、连接、版本和Web结构组件的全景模型”的HTTP事务，明确客户端、服务器、中间实体、请求输入、响应输出和历史协议限制。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“http 概述”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

### 1.1 http——因特网的多媒体信使

把“http——因特网的多媒体信使”放回“建立HTTP资源、事务、报文、连接、版本和Web结构组件的全景模型”的HTTP事务，明确客户端、服务器、中间实体、请求输入、响应输出和历史协议限制。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“http——因特网的多媒体信使”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

### 1.2 web 客户端和服务器

沿接收、解析、映射、访问控制、响应构建、发送与日志七步定位责任，并保存资源映射和响应元数据。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“web 客户端和服务器”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

### 1.3 资源

分解方案、权限、路径、参数、查询和片段，明确规范化与转义发生在哪一端，以及最终请求目标如何形成。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“资源”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 1.3.1 媒体类型

把“媒体类型”放回“建立HTTP资源、事务、报文、连接、版本和Web结构组件的全景模型”的HTTP事务，明确客户端、服务器、中间实体、请求输入、响应输出和历史协议限制。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“媒体类型”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 1.3.2 uri

分解方案、权限、路径、参数、查询和片段，明确规范化与转义发生在哪一端，以及最终请求目标如何形成。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“uri”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 1.3.3 url

分解方案、权限、路径、参数、查询和片段，明确规范化与转义发生在哪一端，以及最终请求目标如何形成。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“url”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 1.3.4 urn

分解方案、权限、路径、参数、查询和片段，明确规范化与转义发生在哪一端，以及最终请求目标如何形成。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“urn”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

### 1.4 事务

把“事务”放回“建立HTTP资源、事务、报文、连接、版本和Web结构组件的全景模型”的HTTP事务，明确客户端、服务器、中间实体、请求输入、响应输出和历史协议限制。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“事务”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 1.4.1 方法

写出原始请求与响应行、首部顺序无关性、主体边界和方法/状态语义，并保留线路字节作为证据。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“方法”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 1.4.2 状态码

写出原始请求与响应行、首部顺序无关性、主体边界和方法/状态语义，并保留线路字节作为证据。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“状态码”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 1.4.3 web 页面中可以包含多个对象

把“web 页面中可以包含多个对象”放回“建立HTTP资源、事务、报文、连接、版本和Web结构组件的全景模型”的HTTP事务，明确客户端、服务器、中间实体、请求输入、响应输出和历史协议限制。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“web 页面中可以包含多个对象”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

### 1.5 报文

写出原始请求与响应行、首部顺序无关性、主体边界和方法/状态语义，并保留线路字节作为证据。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“报文”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

### 1.6 连接

关联四元组、握手、连接复用、消息边界、幂等重试和双向关闭；用时间线区分HTTP等待与TCP等待。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“连接”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 1.6.1 tcp/ip

关联四元组、握手、连接复用、消息边界、幂等重试和双向关闭；用时间线区分HTTP等待与TCP等待。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“tcp/ip”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 1.6.2 连接、ip 地址及端口号

关联四元组、握手、连接复用、消息边界、幂等重试和双向关闭；用时间线区分HTTP等待与TCP等待。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“连接、ip 地址及端口号”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 1.6.3 使用telnet 实例

把“使用telnet 实例”放回“建立HTTP资源、事务、报文、连接、版本和Web结构组件的全景模型”的HTTP事务，明确客户端、服务器、中间实体、请求输入、响应输出和历史协议限制。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“使用telnet 实例”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

### 1.7 协议版本

把“协议版本”放回“建立HTTP资源、事务、报文、连接、版本和Web结构组件的全景模型”的HTTP事务，明确客户端、服务器、中间实体、请求输入、响应输出和历史协议限制。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“协议版本”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

### 1.8 web 的结构组件

把“web 的结构组件”放回“建立HTTP资源、事务、报文、连接、版本和Web结构组件的全景模型”的HTTP事务，明确客户端、服务器、中间实体、请求输入、响应输出和历史协议限制。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“web 的结构组件”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 1.8.1 代理

区分客户端与服务器侧报文形式、逐跳/端到端首部、Via链和认证边界，验证每个代理跳的改写。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“代理”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 1.8.2 缓存

计算当前年龄与新鲜寿命，核对缓存键、Vary、验证器、条件请求和再验证结果，不以是否联系源站猜命中。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“缓存”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 1.8.3 网关

标注两侧协议、字节边界、目标端点、认证与安全责任；验证开放隧道、逐跳首部和半关闭风险。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“网关”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 1.8.4 隧道

标注两侧协议、字节边界、目标端点、认证与安全责任；验证开放隧道、逐跳首部和半关闭风险。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“隧道”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 1.8.5 agent 代理

区分客户端与服务器侧报文形式、逐跳/端到端首部、Via链和认证边界，验证每个代理跳的改写。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“agent 代理”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

### 1.9 起始部分的结束语

把“起始部分的结束语”放回“建立HTTP资源、事务、报文、连接、版本和Web结构组件的全景模型”的HTTP事务，明确客户端、服务器、中间实体、请求输入、响应输出和历史协议限制。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“起始部分的结束语”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

### 1.10 更多信息

把“更多信息”放回“建立HTTP资源、事务、报文、连接、版本和Web结构组件的全景模型”的HTTP事务，明确客户端、服务器、中间实体、请求输入、响应输出和历史协议限制。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“更多信息”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 1.10.1 http 协议信息

把“http 协议信息”放回“建立HTTP资源、事务、报文、连接、版本和Web结构组件的全景模型”的HTTP事务，明确客户端、服务器、中间实体、请求输入、响应输出和历史协议限制。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“http 协议信息”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 1.10.2 历史透视

把“历史透视”放回“建立HTTP资源、事务、报文、连接、版本和Web结构组件的全景模型”的HTTP事务，明确客户端、服务器、中间实体、请求输入、响应输出和历史协议限制。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“历史透视”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 1.10.3 其他万维网信息

把“其他万维网信息”放回“建立HTTP资源、事务、报文、连接、版本和Web结构组件的全景模型”的HTTP事务，明确客户端、服务器、中间实体、请求输入、响应输出和历史协议限制。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“其他万维网信息”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

## 本页完整节点清单

本页承担29个印刷目录正式节点；较短的部分与附录页另列教学支撑，但不扩大586节点分母：

- 第1章 http 概述
- 1.1 http——因特网的多媒体信使
- 1.2 web 客户端和服务器
- 1.3 资源
- 1.3.1 媒体类型
- 1.3.2 uri
- 1.3.3 url
- 1.3.4 urn
- 1.4 事务
- 1.4.1 方法
- 1.4.2 状态码
- 1.4.3 web 页面中可以包含多个对象
- 1.5 报文
- 1.6 连接
- 1.6.1 tcp/ip
- 1.6.2 连接、ip 地址及端口号
- 1.6.3 使用telnet 实例
- 1.7 协议版本
- 1.8 web 的结构组件
- 1.8.1 代理
- 1.8.2 缓存
- 1.8.3 网关
- 1.8.4 隧道
- 1.8.5 agent 代理
- 1.9 起始部分的结束语
- 1.10 更多信息
- 1.10.1 http 协议信息
- 1.10.2 历史透视
- 1.10.3 其他万维网信息

<Stepper>
  <Step title="复原原始HTTP事务">
    固定URL、资源、连接与版本，写出请求行、首部、主体、响应和所有中间实体。
    <Hdg101MessageLab />
  </Step>
  <Step title="只改变一个协议变量">
    改变一个首部、连接、缓存年龄、验证器、认证或编码条件，先写预测再执行。
    <Hdg101DecisionLab />
  </Step>
  <Step title="关闭独立证据门">
    核对首版节点、原始字节、连接时间线、中间状态、安全边界和恢复结果。
    <Hdg101EvidenceLab />
  </Step>
</Stepper>

## 可复现实验记录

```text
GET /resource HTTP/1.1\nHost: example.test\nConnection: keep-alive\nAccept: */*
```

```text
HTTP/1.1 200 OK\nDate: Tue, 01 Jan 2002 00:00:00 GMT\nContent-Type: text/plain\nContent-Length: 5\n\nhello
```

```text
evidence = request_bytes + response_bytes + tcp_timeline + intermediary_state + recovery
```

动手试：保存一次正常请求、一次缓存或连接边界和一次单变量故障。记录客户端、代理、缓存、网关与源服务器看到的原始报文，以及统一时间戳和恢复动作。

<Callout type="trap" title="把端到端首部当成逐跳首部">
  Connection及其列出的字段只能作用于当前连接，代理必须移除；误转发会破坏持久连接、缓存和下游解释。
</Callout>

<Callout type="trap" title="用现代HTTP覆盖原书">
  本书的HTTP-NG、Cookie版本1、Digest、FrontPage、WebDAV、WPAD等内容必须按历史状态讲解。现代替代技术不能改写原书目录。
</Callout>

## 独立证据门

<Hdg101EvidenceLab />

最小证据包包含：首版节点、URL、原始请求/响应、连接时间线、代理与缓存决策、身份和编码、单变量故障、恢复、偏差、责任人与复核人。

## 练习

<Exercises>

**问题 1：为什么“第1章 http 概述”必须固定2002年首版？**

<Answer>
  固定版次才能让5个正文部分、21章、8附录、索引和586个节点与HTTP/1.0、HTTP/1.1时代协议语境一一对应；现代标准只能独立比较。
</Answer>

**问题 2：怎样构造“只把HTTP理解为浏览器发GET会遗漏多对象页面、代理、缓存、网关、隧道和用户代理”的最小反例？**

<Answer>
  固定资源、请求与其他中间状态，只改变一个连接、首部、缓存、认证、编码或重定向变量；比较原始字节、首个偏差和恢复结果。
</Answer>

**问题 3：何时可以认为本页完成独立交接？**

<Answer>
  另一位学习者无需口头补充即可定位全部29个正式节点，复现正常事务和单变量故障，并依据“每次事务都能定位资源、方法、状态、报文方向、TCP端点和中间组件”得到相同结论。
</Answer>

</Exercises>

## 本章回顾

“第1章 http 概述”的核心是建立HTTP资源、事务、报文、连接、版本和Web结构组件的全景模型。真正掌握不是记首部名，而是能用原始报文、连接和中间状态证明资源如何被定位、传输、缓存、保护与交付。

<Glossary>
  <GlossaryItem term="HTTP">
    在客户端、服务器和中间实体之间交换资源表述的应用层协议。掌握标准是能在原始HTTP报文、中间状态或历史规范中定位，并构造最小失败反例。
  </GlossaryItem>
  <GlossaryItem term="资源">
    可由URI标识并通过表示形式传输的网络对象。掌握标准是能在原始HTTP报文、中间状态或历史规范中定位，并构造最小失败反例。
  </GlossaryItem>
  <GlossaryItem term="事务">
    一条请求和对应响应组成的HTTP交互。掌握标准是能在原始HTTP报文、中间状态或历史规范中定位，并构造最小失败反例。
  </GlossaryItem>
  <GlossaryItem term="URI">
    标识或定位资源的统一资源标识符。掌握标准是能在原始HTTP报文、中间状态或历史规范中定位，并构造最小失败反例。
  </GlossaryItem>
  <GlossaryItem term="用户代理">
    代表用户生成HTTP请求并处理响应的客户端程序。掌握标准是能在原始HTTP报文、中间状态或历史规范中定位，并构造最小失败反例。
  </GlossaryItem>
</Glossary>

[← 上一页：第一部分 http：web 的基础](/learn/http-definitive-guide/01-hdg1-part-1/hdg1-part-1) · [下一页：第2章 url 与资源 →](/learn/http-definitive-guide/03-hdg1-02/hdg1-02)

<Attribution
  adaptedFrom="Gourley等《HTTP: The Definitive Guide》2002年首版"
  adaptedUrl="https://www.oreilly.com/library/view/http-the-definitive/1565925092/"
/>
