---
title: "第4章 连接管理"
description: "解释TCP性能、串行与并行连接、Keep-Alive、持久连接、管线化和正确关闭"
type: A
section: "HTTP权威指南（2002年英文首版 / 2012年中译本）"
order: 5
draft: false
math: false
sourceUrl: "https://www.oreilly.com/library/view/http-the-definitive/1565925092/"
demo: true
---

import {
  Hdg104MessageLab,
  Hdg104DecisionLab,
  Hdg104EvidenceLab,
} from "@/components/mdx/http-definitive-guide/diagrams/hdg1-04";

# 第4章 连接管理

本课程锁定David Gourley、Brian Totty、Marjorie Sayer、Sailu Reddy、Anshu Aggarwal著，陈涓、赵振平译《HTTP权威指南》，人民邮电出版社，2012年，ISBN 9787115281487；英文原版HTTP: The Definitive Guide，O'Reilly Media，2002年9月，656页，ISBN 1565925092。O'Reilly官方页面确认英文首版的5个正文部分、21章和8个附录；中文版完整印刷目录另含“第六部分 附录”和索引。忠实度分母共586个部分、章、编号节/小节、附录与索引节点。

<Objectives>

- 能解释“第4章 连接管理”的全部正式节点及其HTTP事务位置。
- 能绘制请求、响应、TCP连接、中间实体和资源状态。
- 能设计单变量实验，验证“每个响应边界、连接复用范围、重试条件和关闭方向都可由报文与TCP状态证明”。
- 能写出含首版边界、原始报文、故障、恢复和复核人的证据包。

</Objectives>

## 从一条可证伪的HTTP事务开始

先预测：盲目增加并行连接或重试非幂等事务会造成端口耗尽、重复副作用和截尾响应。把预测写成“URL/资源、连接、请求报文、中间实体、响应报文、身份与编码”六行，再运行客户端、代理或服务器。结果不同先定位首个字节或状态偏差。

本页主问题是：解释TCP性能、串行与并行连接、Keep-Alive、持久连接、管线化和正确关闭。每条消息要注明发送者、接收者、HTTP版本、连接复用、逐跳/端到端首部、主体边界与历史协议状态。

验收不变量是：每个响应边界、连接复用范围、重试条件和关闭方向都可由报文与TCP状态证明。最终页面正常、状态码200或TLS通道建立都只是局部事实，不能单独证明资源身份、缓存变体、代理转发与证书身份全部正确。

## 核心词汇与首版边界

<Term def="多个HTTP事务复用同一底层连接的机制">持久连接</Term>、
<Term def="HTTP/1.0扩展中协商连接复用的历史机制">Keep-Alive</Term>、
<Term def="在等待先前响应时继续发送后续请求的连接使用方式">管线化</Term>、
<Term def="同一请求执行一次或多次产生相同预期效果的性质">幂等性</Term>、
<Term def="主动关闭TCP一方暂时保留连接标识以处理迟到报文的状态">TIME_WAIT</Term>

这些词汇固定在2002年英文首版和2012年中译本语境。HTTP/2、HTTP/3、OAuth、JWT、SameSite、HSTS和现代CDN行为可以独立比较，但不得替换HTTP-NG、Digest、WebDAV、WPAD等原书节点。

<Hdg104MessageLab />

## 核心机制深读

### 先从持久连接画出事务

解释TCP性能、串行与并行连接、Keep-Alive、持久连接、管线化和正确关闭。先固定资源身份和请求目标，再标出客户端、中间实体与源服务器，任何优化或安全结论都不能跳过原始报文。

动手试：先手写预期请求与响应，再抓取正常事务和一个单变量故障事务。标出首个不同字节或状态，不用最终页面猜根因。

### 区分持久连接与Keep-Alive

持久连接和Keep-Alive可能在同一事务中协作，但责任、状态位置和失败语义不同。对比正常请求、单个首部变化和单个连接故障，定位首个偏差。

动手试：先手写预期请求与响应，再抓取正常事务和一个单变量故障事务。标出首个不同字节或状态，不用最终页面猜根因。

### 让管线化经过真实中间实体

代理、缓存、网关或隧道会读取、添加、删除或转发不同首部。逐跳记录Via、Connection列出的字段、缓存决策和认证边界。

动手试：先手写预期请求与响应，再抓取正常事务和一个单变量故障事务。标出首个不同字节或状态，不用最终页面猜根因。

### 验证幂等性的历史语境

原书写于2002年，以HTTP/1.0、HTTP/1.1、SSL/TLS早期版本和当时Web基础设施为背景。现代实现可做对照，但不能冒充原书节点。

动手试：先手写预期请求与响应，再抓取正常事务和一个单变量故障事务。标出首个不同字节或状态，不用最终页面猜根因。

### 用TIME_WAIT关闭证据门

每个响应边界、连接复用范围、重试条件和关闭方向都可由报文与TCP状态证明。证据至少包含原始请求/响应、TCP时间线、中间状态、失败注入、恢复动作和第三方复核。

动手试：先手写预期请求与响应，再抓取正常事务和一个单变量故障事务。标出首个不同字节或状态，不用最终页面猜根因。

## 原书目录逐节点重构

### 第4章 连接管理

关联四元组、握手、连接复用、消息边界、幂等重试和双向关闭；用时间线区分HTTP等待与TCP等待。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“连接管理”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

### 4.1 tcp 连接

关联四元组、握手、连接复用、消息边界、幂等重试和双向关闭；用时间线区分HTTP等待与TCP等待。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“tcp 连接”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 4.1.1 tcp 的可靠数据管道

关联四元组、握手、连接复用、消息边界、幂等重试和双向关闭；用时间线区分HTTP等待与TCP等待。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“tcp 的可靠数据管道”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 4.1.2 tcp 流是分段的、由ip 分组传送

关联四元组、握手、连接复用、消息边界、幂等重试和双向关闭；用时间线区分HTTP等待与TCP等待。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“tcp 流是分段的、由ip 分组传送”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 4.1.3 保持tcp 连接的正确运行

关联四元组、握手、连接复用、消息边界、幂等重试和双向关闭；用时间线区分HTTP等待与TCP等待。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“保持tcp 连接的正确运行”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 4.1.4 用tcp 套接字编程

关联四元组、握手、连接复用、消息边界、幂等重试和双向关闭；用时间线区分HTTP等待与TCP等待。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“用tcp 套接字编程”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

### 4.2 对tcp 性能的考虑

关联四元组、握手、连接复用、消息边界、幂等重试和双向关闭；用时间线区分HTTP等待与TCP等待。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“对tcp 性能的考虑”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 4.2.1 http 事务的时延

把“http 事务的时延”放回“解释TCP性能、串行与并行连接、Keep-Alive、持久连接、管线化和正确关闭”的HTTP事务，明确客户端、服务器、中间实体、请求输入、响应输出和历史协议限制。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“http 事务的时延”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 4.2.2 性能聚焦区域

把“性能聚焦区域”放回“解释TCP性能、串行与并行连接、Keep-Alive、持久连接、管线化和正确关闭”的HTTP事务，明确客户端、服务器、中间实体、请求输入、响应输出和历史协议限制。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“性能聚焦区域”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 4.2.3 tcp 连接的握手时延

关联四元组、握手、连接复用、消息边界、幂等重试和双向关闭；用时间线区分HTTP等待与TCP等待。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“tcp 连接的握手时延”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 4.2.4 延迟确认

把“延迟确认”放回“解释TCP性能、串行与并行连接、Keep-Alive、持久连接、管线化和正确关闭”的HTTP事务，明确客户端、服务器、中间实体、请求输入、响应输出和历史协议限制。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“延迟确认”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 4.2.5 tcp 慢启动

关联四元组、握手、连接复用、消息边界、幂等重试和双向关闭；用时间线区分HTTP等待与TCP等待。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“tcp 慢启动”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 4.2.6 nagle 算法与tcp_nodelay

关联四元组、握手、连接复用、消息边界、幂等重试和双向关闭；用时间线区分HTTP等待与TCP等待。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“nagle 算法与tcp_nodelay”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 4.2.7 time_wait 累积与端口耗尽

关联四元组、握手、连接复用、消息边界、幂等重试和双向关闭；用时间线区分HTTP等待与TCP等待。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“time_wait 累积与端口耗尽”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

### 4.3 http 连接的处理

关联四元组、握手、连接复用、消息边界、幂等重试和双向关闭；用时间线区分HTTP等待与TCP等待。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“http 连接的处理”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 4.3.1 常被误解的connection 首部

写出原始请求与响应行、首部顺序无关性、主体边界和方法/状态语义，并保留线路字节作为证据。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“常被误解的connection 首部”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 4.3.2 串行事务处理时延

把“串行事务处理时延”放回“解释TCP性能、串行与并行连接、Keep-Alive、持久连接、管线化和正确关闭”的HTTP事务，明确客户端、服务器、中间实体、请求输入、响应输出和历史协议限制。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“串行事务处理时延”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

### 4.4 并行连接

关联四元组、握手、连接复用、消息边界、幂等重试和双向关闭；用时间线区分HTTP等待与TCP等待。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“并行连接”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 4.4.1 并行连接可能会提高页面的加载速度

关联四元组、握手、连接复用、消息边界、幂等重试和双向关闭；用时间线区分HTTP等待与TCP等待。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“并行连接可能会提高页面的加载速度”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 4.4.2 并行连接不一定更快

关联四元组、握手、连接复用、消息边界、幂等重试和双向关闭；用时间线区分HTTP等待与TCP等待。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“并行连接不一定更快”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 4.4.3 并行连接可能让人“感觉”更快一些

关联四元组、握手、连接复用、消息边界、幂等重试和双向关闭；用时间线区分HTTP等待与TCP等待。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“并行连接可能让人“感觉”更快一些”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

### 4.5 持久连接

关联四元组、握手、连接复用、消息边界、幂等重试和双向关闭；用时间线区分HTTP等待与TCP等待。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“持久连接”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 4.5.1 持久以及并行连接

关联四元组、握手、连接复用、消息边界、幂等重试和双向关闭；用时间线区分HTTP等待与TCP等待。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“持久以及并行连接”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 4.5.2 http/1.0+ keep-alive 连接

关联四元组、握手、连接复用、消息边界、幂等重试和双向关闭；用时间线区分HTTP等待与TCP等待。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“http/1.0+ keep-alive 连接”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 4.5.3 keep-alive 操作

关联四元组、握手、连接复用、消息边界、幂等重试和双向关闭；用时间线区分HTTP等待与TCP等待。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“keep-alive 操作”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 4.5.4 keep-alive 选项

关联四元组、握手、连接复用、消息边界、幂等重试和双向关闭；用时间线区分HTTP等待与TCP等待。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“keep-alive 选项”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 4.5.5 keep-alive 连接的限制和规则

关联四元组、握手、连接复用、消息边界、幂等重试和双向关闭；用时间线区分HTTP等待与TCP等待。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“keep-alive 连接的限制和规则”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 4.5.6 keep-alive 和哑代理

关联四元组、握手、连接复用、消息边界、幂等重试和双向关闭；用时间线区分HTTP等待与TCP等待。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“keep-alive 和哑代理”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 4.5.7 插入proxy-connection

标注两侧协议、字节边界、目标端点、认证与安全责任；验证开放隧道、逐跳首部和半关闭风险。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“插入proxy-connection”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 4.5.8 http/1.1 持久连接

关联四元组、握手、连接复用、消息边界、幂等重试和双向关闭；用时间线区分HTTP等待与TCP等待。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“http/1.1 持久连接”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 4.5.9 持久连接的限制和规则

关联四元组、握手、连接复用、消息边界、幂等重试和双向关闭；用时间线区分HTTP等待与TCP等待。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“持久连接的限制和规则”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

### 4.6 管道化连接

关联四元组、握手、连接复用、消息边界、幂等重试和双向关闭；用时间线区分HTTP等待与TCP等待。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“管道化连接”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

### 4.7 关闭连接的奥秘

关联四元组、握手、连接复用、消息边界、幂等重试和双向关闭；用时间线区分HTTP等待与TCP等待。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“关闭连接的奥秘”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 4.7.1 “ 任意”解除连接

关联四元组、握手、连接复用、消息边界、幂等重试和双向关闭；用时间线区分HTTP等待与TCP等待。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与““ 任意”解除连接”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 4.7.2 content-length 及截尾操作

把“content-length 及截尾操作”放回“解释TCP性能、串行与并行连接、Keep-Alive、持久连接、管线化和正确关闭”的HTTP事务，明确客户端、服务器、中间实体、请求输入、响应输出和历史协议限制。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“content-length 及截尾操作”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 4.7.3 连接关闭容限、重试以及幂等性

关联四元组、握手、连接复用、消息边界、幂等重试和双向关闭；用时间线区分HTTP等待与TCP等待。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“连接关闭容限、重试以及幂等性”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 4.7.4 正常关闭连接

关联四元组、握手、连接复用、消息边界、幂等重试和双向关闭；用时间线区分HTTP等待与TCP等待。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“正常关闭连接”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

### 4.8 更多信息

把“更多信息”放回“解释TCP性能、串行与并行连接、Keep-Alive、持久连接、管线化和正确关闭”的HTTP事务，明确客户端、服务器、中间实体、请求输入、响应输出和历史协议限制。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“更多信息”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 4.8.1 http 连接

关联四元组、握手、连接复用、消息边界、幂等重试和双向关闭；用时间线区分HTTP等待与TCP等待。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“http 连接”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 4.8.2 http 性能问题

把“http 性能问题”放回“解释TCP性能、串行与并行连接、Keep-Alive、持久连接、管线化和正确关闭”的HTTP事务，明确客户端、服务器、中间实体、请求输入、响应输出和历史协议限制。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“http 性能问题”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 4.8.3 tcp/ip

关联四元组、握手、连接复用、消息边界、幂等重试和双向关闭；用时间线区分HTTP等待与TCP等待。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“tcp/ip”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

## 本页完整节点清单

本页承担41个印刷目录正式节点；较短的部分与附录页另列教学支撑，但不扩大586节点分母：

- 第4章 连接管理
- 4.1 tcp 连接
- 4.1.1 tcp 的可靠数据管道
- 4.1.2 tcp 流是分段的、由ip 分组传送
- 4.1.3 保持tcp 连接的正确运行
- 4.1.4 用tcp 套接字编程
- 4.2 对tcp 性能的考虑
- 4.2.1 http 事务的时延
- 4.2.2 性能聚焦区域
- 4.2.3 tcp 连接的握手时延
- 4.2.4 延迟确认
- 4.2.5 tcp 慢启动
- 4.2.6 nagle 算法与tcp_nodelay
- 4.2.7 time_wait 累积与端口耗尽
- 4.3 http 连接的处理
- 4.3.1 常被误解的connection 首部
- 4.3.2 串行事务处理时延
- 4.4 并行连接
- 4.4.1 并行连接可能会提高页面的加载速度
- 4.4.2 并行连接不一定更快
- 4.4.3 并行连接可能让人“感觉”更快一些
- 4.5 持久连接
- 4.5.1 持久以及并行连接
- 4.5.2 http/1.0+ keep-alive 连接
- 4.5.3 keep-alive 操作
- 4.5.4 keep-alive 选项
- 4.5.5 keep-alive 连接的限制和规则
- 4.5.6 keep-alive 和哑代理
- 4.5.7 插入proxy-connection
- 4.5.8 http/1.1 持久连接
- 4.5.9 持久连接的限制和规则
- 4.6 管道化连接
- 4.7 关闭连接的奥秘
- 4.7.1 “ 任意”解除连接
- 4.7.2 content-length 及截尾操作
- 4.7.3 连接关闭容限、重试以及幂等性
- 4.7.4 正常关闭连接
- 4.8 更多信息
- 4.8.1 http 连接
- 4.8.2 http 性能问题
- 4.8.3 tcp/ip

<Stepper>
  <Step title="复原原始HTTP事务">
    固定URL、资源、连接与版本，写出请求行、首部、主体、响应和所有中间实体。
    <Hdg104MessageLab />
  </Step>
  <Step title="只改变一个协议变量">
    改变一个首部、连接、缓存年龄、验证器、认证或编码条件，先写预测再执行。
    <Hdg104DecisionLab />
  </Step>
  <Step title="关闭独立证据门">
    核对首版节点、原始字节、连接时间线、中间状态、安全边界和恢复结果。
    <Hdg104EvidenceLab />
  </Step>
</Stepper>

## 可复现实验记录

```text
GET /resource HTTP/1.1\nHost: example.test\nConnection: keep-alive\nAccept: */*
```

```text
HTTP/1.1 200 OK\nDate: Tue, 01 Jan 2002 00:00:00 GMT\nContent-Type: text/plain\nContent-Length: 5\n\nhello
```

```text
evidence = request_bytes + response_bytes + tcp_timeline + intermediary_state + recovery
```

动手试：保存一次正常请求、一次缓存或连接边界和一次单变量故障。记录客户端、代理、缓存、网关与源服务器看到的原始报文，以及统一时间戳和恢复动作。

<Callout type="trap" title="把端到端首部当成逐跳首部">
  Connection及其列出的字段只能作用于当前连接，代理必须移除；误转发会破坏持久连接、缓存和下游解释。
</Callout>

<Callout type="trap" title="用现代HTTP覆盖原书">
  本书的HTTP-NG、Cookie版本1、Digest、FrontPage、WebDAV、WPAD等内容必须按历史状态讲解。现代替代技术不能改写原书目录。
</Callout>

## 独立证据门

<Hdg104EvidenceLab />

最小证据包包含：首版节点、URL、原始请求/响应、连接时间线、代理与缓存决策、身份和编码、单变量故障、恢复、偏差、责任人与复核人。

## 练习

<Exercises>

**问题 1：为什么“第4章 连接管理”必须固定2002年首版？**

<Answer>
  固定版次才能让5个正文部分、21章、8附录、索引和586个节点与HTTP/1.0、HTTP/1.1时代协议语境一一对应；现代标准只能独立比较。
</Answer>

**问题 2：怎样构造“盲目增加并行连接或重试非幂等事务会造成端口耗尽、重复副作用和截尾响应”的最小反例？**

<Answer>
  固定资源、请求与其他中间状态，只改变一个连接、首部、缓存、认证、编码或重定向变量；比较原始字节、首个偏差和恢复结果。
</Answer>

**问题 3：何时可以认为本页完成独立交接？**

<Answer>
  另一位学习者无需口头补充即可定位全部41个正式节点，复现正常事务和单变量故障，并依据“每个响应边界、连接复用范围、重试条件和关闭方向都可由报文与TCP状态证明”得到相同结论。
</Answer>

</Exercises>

## 本章回顾

“第4章 连接管理”的核心是解释TCP性能、串行与并行连接、Keep-Alive、持久连接、管线化和正确关闭。真正掌握不是记首部名，而是能用原始报文、连接和中间状态证明资源如何被定位、传输、缓存、保护与交付。

<Glossary>
  <GlossaryItem term="持久连接">
    多个HTTP事务复用同一底层连接的机制。掌握标准是能在原始HTTP报文、中间状态或历史规范中定位，并构造最小失败反例。
  </GlossaryItem>
  <GlossaryItem term="Keep-Alive">
    HTTP/1.0扩展中协商连接复用的历史机制。掌握标准是能在原始HTTP报文、中间状态或历史规范中定位，并构造最小失败反例。
  </GlossaryItem>
  <GlossaryItem term="管线化">
    在等待先前响应时继续发送后续请求的连接使用方式。掌握标准是能在原始HTTP报文、中间状态或历史规范中定位，并构造最小失败反例。
  </GlossaryItem>
  <GlossaryItem term="幂等性">
    同一请求执行一次或多次产生相同预期效果的性质。掌握标准是能在原始HTTP报文、中间状态或历史规范中定位，并构造最小失败反例。
  </GlossaryItem>
  <GlossaryItem term="TIME_WAIT">
    主动关闭TCP一方暂时保留连接标识以处理迟到报文的状态。掌握标准是能在原始HTTP报文、中间状态或历史规范中定位，并构造最小失败反例。
  </GlossaryItem>
</Glossary>

[← 上一页：第3章 http 报文](/learn/http-definitive-guide/04-hdg1-03/hdg1-03) · [下一页：第二部分 http 结构 →](/learn/http-definitive-guide/06-hdg1-part-2/hdg1-part-2)

<Attribution
  adaptedFrom="Gourley等《HTTP: The Definitive Guide》2002年首版"
  adaptedUrl="https://www.oreilly.com/library/view/http-the-definitive/1565925092/"
/>
