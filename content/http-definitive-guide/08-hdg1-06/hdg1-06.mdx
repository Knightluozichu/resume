---
title: "第6章 代理"
description: "理解代理部署、层次、流量获取、客户端配置、URI差异、Via追踪、认证与互操作"
type: A
section: "HTTP权威指南（2002年英文首版 / 2012年中译本）"
order: 8
draft: false
math: false
sourceUrl: "https://www.oreilly.com/library/view/http-the-definitive/1565925092/"
demo: true
---

import {
  Hdg106MessageLab,
  Hdg106DecisionLab,
  Hdg106EvidenceLab,
} from "@/components/mdx/http-definitive-guide/diagrams/hdg1-06";

# 第6章 代理

本课程锁定David Gourley、Brian Totty、Marjorie Sayer、Sailu Reddy、Anshu Aggarwal著，陈涓、赵振平译《HTTP权威指南》，人民邮电出版社，2012年，ISBN 9787115281487；英文原版HTTP: The Definitive Guide，O'Reilly Media，2002年9月，656页，ISBN 1565925092。O'Reilly官方页面确认英文首版的5个正文部分、21章和8个附录；中文版完整印刷目录另含“第六部分 附录”和索引。忠实度分母共586个部分、章、编号节/小节、附录与索引节点。

<Objectives>

- 能解释“第6章 代理”的全部正式节点及其HTTP事务位置。
- 能绘制请求、响应、TCP连接、中间实体和资源状态。
- 能设计单变量实验，验证“每个代理跳都能解释请求目标形式、逐跳首部、Via链、认证边界和转发结果”。
- 能写出含首版边界、原始报文、故障、恢复和复核人的证据包。

</Objectives>

## 从一条可证伪的HTTP事务开始

先预测：代理若盲转Connection列出的逐跳首部或错误改写URI，会破坏持久连接与虚拟主机。把预测写成“URL/资源、连接、请求报文、中间实体、响应报文、身份与编码”六行，再运行客户端、代理或服务器。结果不同先定位首个字节或状态偏差。

本页主问题是：理解代理部署、层次、流量获取、客户端配置、URI差异、Via追踪、认证与互操作。每条消息要注明发送者、接收者、HTTP版本、连接复用、逐跳/端到端首部、主体边界与历史协议状态。

验收不变量是：每个代理跳都能解释请求目标形式、逐跳首部、Via链、认证边界和转发结果。最终页面正常、状态码200或TLS通道建立都只是局部事实，不能单独证明资源身份、缓存变体、代理转发与证书身份全部正确。

## 核心词汇与首版边界

<Term def="代表客户端向上游发送HTTP请求的中间实体">代理</Term>、
<Term def="记录报文经过协议中间节点及版本信息的首部">Via</Term>、
<Term def="让服务器回显接收请求以诊断中间处理的方法">TRACE</Term>、
<Term def="由客户端执行并为URL选择代理的自动配置脚本">PAC</Term>、
<Term def="在客户端未显式配置时通过网络路径截获流量的代理">拦截代理</Term>

这些词汇固定在2002年英文首版和2012年中译本语境。HTTP/2、HTTP/3、OAuth、JWT、SameSite、HSTS和现代CDN行为可以独立比较，但不得替换HTTP-NG、Digest、WebDAV、WPAD等原书节点。

<Hdg106MessageLab />

## 核心机制深读

### 先从代理画出事务

理解代理部署、层次、流量获取、客户端配置、URI差异、Via追踪、认证与互操作。先固定资源身份和请求目标，再标出客户端、中间实体与源服务器，任何优化或安全结论都不能跳过原始报文。

动手试：先手写预期请求与响应，再抓取正常事务和一个单变量故障事务。标出首个不同字节或状态，不用最终页面猜根因。

### 区分代理与Via

代理和Via可能在同一事务中协作，但责任、状态位置和失败语义不同。对比正常请求、单个首部变化和单个连接故障，定位首个偏差。

动手试：先手写预期请求与响应，再抓取正常事务和一个单变量故障事务。标出首个不同字节或状态，不用最终页面猜根因。

### 让TRACE经过真实中间实体

代理、缓存、网关或隧道会读取、添加、删除或转发不同首部。逐跳记录Via、Connection列出的字段、缓存决策和认证边界。

动手试：先手写预期请求与响应，再抓取正常事务和一个单变量故障事务。标出首个不同字节或状态，不用最终页面猜根因。

### 验证PAC的历史语境

原书写于2002年，以HTTP/1.0、HTTP/1.1、SSL/TLS早期版本和当时Web基础设施为背景。现代实现可做对照，但不能冒充原书节点。

动手试：先手写预期请求与响应，再抓取正常事务和一个单变量故障事务。标出首个不同字节或状态，不用最终页面猜根因。

### 用拦截代理关闭证据门

每个代理跳都能解释请求目标形式、逐跳首部、Via链、认证边界和转发结果。证据至少包含原始请求/响应、TCP时间线、中间状态、失败注入、恢复动作和第三方复核。

动手试：先手写预期请求与响应，再抓取正常事务和一个单变量故障事务。标出首个不同字节或状态，不用最终页面猜根因。

## 原书目录逐节点重构

### 第6章 代理

区分客户端与服务器侧报文形式、逐跳/端到端首部、Via链和认证边界，验证每个代理跳的改写。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“代理”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

### 6.1 web 的中间实体

写出原始请求与响应行、首部顺序无关性、主体边界和方法/状态语义，并保留线路字节作为证据。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“web 的中间实体”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 6.1.1 私有和共享代理

区分客户端与服务器侧报文形式、逐跳/端到端首部、Via链和认证边界，验证每个代理跳的改写。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“私有和共享代理”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 6.1.2 代理与网关的对比

区分客户端与服务器侧报文形式、逐跳/端到端首部、Via链和认证边界，验证每个代理跳的改写。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“代理与网关的对比”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

### 6.2 为什么使用代理

区分客户端与服务器侧报文形式、逐跳/端到端首部、Via链和认证边界，验证每个代理跳的改写。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“为什么使用代理”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

### 6.3 代理会去往何处

区分客户端与服务器侧报文形式、逐跳/端到端首部、Via链和认证边界，验证每个代理跳的改写。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“代理会去往何处”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 6.3.1 代理服务器的部署

沿接收、解析、映射、访问控制、响应构建、发送与日志七步定位责任，并保存资源映射和响应元数据。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“代理服务器的部署”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 6.3.2 代理的层次结构

区分客户端与服务器侧报文形式、逐跳/端到端首部、Via链和认证边界，验证每个代理跳的改写。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“代理的层次结构”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 6.3.3 代理是如何获取流量的

区分客户端与服务器侧报文形式、逐跳/端到端首部、Via链和认证边界，验证每个代理跳的改写。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“代理是如何获取流量的”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

### 6.4 客户端的代理设置

区分客户端与服务器侧报文形式、逐跳/端到端首部、Via链和认证边界，验证每个代理跳的改写。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“客户端的代理设置”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 6.4.1 客户端的代理配置：手工配置

区分客户端与服务器侧报文形式、逐跳/端到端首部、Via链和认证边界，验证每个代理跳的改写。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“客户端的代理配置：手工配置”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 6.4.2 客户端代理配置：pac 文件

区分客户端与服务器侧报文形式、逐跳/端到端首部、Via链和认证边界，验证每个代理跳的改写。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“客户端代理配置：pac 文件”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 6.4.3 客户端代理配置：wpad

区分客户端与服务器侧报文形式、逐跳/端到端首部、Via链和认证边界，验证每个代理跳的改写。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“客户端代理配置：wpad”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

### 6.5 与代理请求有关的一些棘手问题

区分客户端与服务器侧报文形式、逐跳/端到端首部、Via链和认证边界，验证每个代理跳的改写。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“与代理请求有关的一些棘手问题”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 6.5.1 代理uri 与服务器uri 的不同

分解方案、权限、路径、参数、查询和片段，明确规范化与转义发生在哪一端，以及最终请求目标如何形成。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“代理uri 与服务器uri 的不同”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 6.5.2 与虚拟主机一样的问题

写出Host或目标身份、选择算法、健康状态、缓存/锁状态、回退路径与最终后端，保存完整跳转链。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“与虚拟主机一样的问题”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 6.5.3 拦截代理会收到部分uri

分解方案、权限、路径、参数、查询和片段，明确规范化与转义发生在哪一端，以及最终请求目标如何形成。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“拦截代理会收到部分uri”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 6.5.4 代理既可以处理代理请求，也可以处理服务器请求

沿接收、解析、映射、访问控制、响应构建、发送与日志七步定位责任，并保存资源映射和响应元数据。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“代理既可以处理代理请求，也可以处理服务器请求”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 6.5.5 转发过程中对uri 的修改

分解方案、权限、路径、参数、查询和片段，明确规范化与转义发生在哪一端，以及最终请求目标如何形成。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“转发过程中对uri 的修改”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 6.5.6 uri 的客户端自动扩展和主机名解析

分解方案、权限、路径、参数、查询和片段，明确规范化与转义发生在哪一端，以及最终请求目标如何形成。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“uri 的客户端自动扩展和主机名解析”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 6.5.7 没有代理时uri 的解析

分解方案、权限、路径、参数、查询和片段，明确规范化与转义发生在哪一端，以及最终请求目标如何形成。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“没有代理时uri 的解析”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 6.5.8 有显式代理时uri 的解析

分解方案、权限、路径、参数、查询和片段，明确规范化与转义发生在哪一端，以及最终请求目标如何形成。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“有显式代理时uri 的解析”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 6.5.9 有拦截代理时uri 的解析

分解方案、权限、路径、参数、查询和片段，明确规范化与转义发生在哪一端，以及最终请求目标如何形成。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“有拦截代理时uri 的解析”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

### 6.6 追踪报文

写出原始请求与响应行、首部顺序无关性、主体边界和方法/状态语义，并保留线路字节作为证据。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“追踪报文”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 6.6.1 via 首部

写出原始请求与响应行、首部顺序无关性、主体边界和方法/状态语义，并保留线路字节作为证据。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“via 首部”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 6.6.2 trace 方法

写出原始请求与响应行、首部顺序无关性、主体边界和方法/状态语义，并保留线路字节作为证据。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“trace 方法”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

### 6.7 代理认证

区分客户端与服务器侧报文形式、逐跳/端到端首部、Via链和认证边界，验证每个代理跳的改写。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“代理认证”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

### 6.8 代理的互操作性

区分客户端与服务器侧报文形式、逐跳/端到端首部、Via链和认证边界，验证每个代理跳的改写。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“代理的互操作性”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 6.8.1 处理代理不支持的首部和方法

写出原始请求与响应行、首部顺序无关性、主体边界和方法/状态语义，并保留线路字节作为证据。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“处理代理不支持的首部和方法”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 6.8.2 options：发现对可选特性的支持

把“options：发现对可选特性的支持”放回“理解代理部署、层次、流量获取、客户端配置、URI差异、Via追踪、认证与互操作”的HTTP事务，明确客户端、服务器、中间实体、请求输入、响应输出和历史协议限制。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“options：发现对可选特性的支持”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 6.8.3 allow 首部

写出原始请求与响应行、首部顺序无关性、主体边界和方法/状态语义，并保留线路字节作为证据。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“allow 首部”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

### 6.9 更多信息

把“更多信息”放回“理解代理部署、层次、流量获取、客户端配置、URI差异、Via追踪、认证与互操作”的HTTP事务，明确客户端、服务器、中间实体、请求输入、响应输出和历史协议限制。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“更多信息”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

## 本页完整节点清单

本页承担32个印刷目录正式节点；较短的部分与附录页另列教学支撑，但不扩大586节点分母：

- 第6章 代理
- 6.1 web 的中间实体
- 6.1.1 私有和共享代理
- 6.1.2 代理与网关的对比
- 6.2 为什么使用代理
- 6.3 代理会去往何处
- 6.3.1 代理服务器的部署
- 6.3.2 代理的层次结构
- 6.3.3 代理是如何获取流量的
- 6.4 客户端的代理设置
- 6.4.1 客户端的代理配置：手工配置
- 6.4.2 客户端代理配置：pac 文件
- 6.4.3 客户端代理配置：wpad
- 6.5 与代理请求有关的一些棘手问题
- 6.5.1 代理uri 与服务器uri 的不同
- 6.5.2 与虚拟主机一样的问题
- 6.5.3 拦截代理会收到部分uri
- 6.5.4 代理既可以处理代理请求，也可以处理服务器请求
- 6.5.5 转发过程中对uri 的修改
- 6.5.6 uri 的客户端自动扩展和主机名解析
- 6.5.7 没有代理时uri 的解析
- 6.5.8 有显式代理时uri 的解析
- 6.5.9 有拦截代理时uri 的解析
- 6.6 追踪报文
- 6.6.1 via 首部
- 6.6.2 trace 方法
- 6.7 代理认证
- 6.8 代理的互操作性
- 6.8.1 处理代理不支持的首部和方法
- 6.8.2 options：发现对可选特性的支持
- 6.8.3 allow 首部
- 6.9 更多信息

<Stepper>
  <Step title="复原原始HTTP事务">
    固定URL、资源、连接与版本，写出请求行、首部、主体、响应和所有中间实体。
    <Hdg106MessageLab />
  </Step>
  <Step title="只改变一个协议变量">
    改变一个首部、连接、缓存年龄、验证器、认证或编码条件，先写预测再执行。
    <Hdg106DecisionLab />
  </Step>
  <Step title="关闭独立证据门">
    核对首版节点、原始字节、连接时间线、中间状态、安全边界和恢复结果。
    <Hdg106EvidenceLab />
  </Step>
</Stepper>

## 可复现实验记录

```text
GET /resource HTTP/1.1\nHost: example.test\nConnection: keep-alive\nAccept: */*
```

```text
HTTP/1.1 200 OK\nDate: Tue, 01 Jan 2002 00:00:00 GMT\nContent-Type: text/plain\nContent-Length: 5\n\nhello
```

```text
evidence = request_bytes + response_bytes + tcp_timeline + intermediary_state + recovery
```

动手试：保存一次正常请求、一次缓存或连接边界和一次单变量故障。记录客户端、代理、缓存、网关与源服务器看到的原始报文，以及统一时间戳和恢复动作。

<Callout type="trap" title="把端到端首部当成逐跳首部">
  Connection及其列出的字段只能作用于当前连接，代理必须移除；误转发会破坏持久连接、缓存和下游解释。
</Callout>

<Callout type="trap" title="用现代HTTP覆盖原书">
  本书的HTTP-NG、Cookie版本1、Digest、FrontPage、WebDAV、WPAD等内容必须按历史状态讲解。现代替代技术不能改写原书目录。
</Callout>

## 独立证据门

<Hdg106EvidenceLab />

最小证据包包含：首版节点、URL、原始请求/响应、连接时间线、代理与缓存决策、身份和编码、单变量故障、恢复、偏差、责任人与复核人。

## 练习

<Exercises>

**问题 1：为什么“第6章 代理”必须固定2002年首版？**

<Answer>
  固定版次才能让5个正文部分、21章、8附录、索引和586个节点与HTTP/1.0、HTTP/1.1时代协议语境一一对应；现代标准只能独立比较。
</Answer>

**问题 2：怎样构造“代理若盲转Connection列出的逐跳首部或错误改写URI，会破坏持久连接与虚拟主机”的最小反例？**

<Answer>
  固定资源、请求与其他中间状态，只改变一个连接、首部、缓存、认证、编码或重定向变量；比较原始字节、首个偏差和恢复结果。
</Answer>

**问题 3：何时可以认为本页完成独立交接？**

<Answer>
  另一位学习者无需口头补充即可定位全部32个正式节点，复现正常事务和单变量故障，并依据“每个代理跳都能解释请求目标形式、逐跳首部、Via链、认证边界和转发结果”得到相同结论。
</Answer>

</Exercises>

## 本章回顾

“第6章 代理”的核心是理解代理部署、层次、流量获取、客户端配置、URI差异、Via追踪、认证与互操作。真正掌握不是记首部名，而是能用原始报文、连接和中间状态证明资源如何被定位、传输、缓存、保护与交付。

<Glossary>
  <GlossaryItem term="代理">
    代表客户端向上游发送HTTP请求的中间实体。掌握标准是能在原始HTTP报文、中间状态或历史规范中定位，并构造最小失败反例。
  </GlossaryItem>
  <GlossaryItem term="Via">
    记录报文经过协议中间节点及版本信息的首部。掌握标准是能在原始HTTP报文、中间状态或历史规范中定位，并构造最小失败反例。
  </GlossaryItem>
  <GlossaryItem term="TRACE">
    让服务器回显接收请求以诊断中间处理的方法。掌握标准是能在原始HTTP报文、中间状态或历史规范中定位，并构造最小失败反例。
  </GlossaryItem>
  <GlossaryItem term="PAC">
    由客户端执行并为URL选择代理的自动配置脚本。掌握标准是能在原始HTTP报文、中间状态或历史规范中定位，并构造最小失败反例。
  </GlossaryItem>
  <GlossaryItem term="拦截代理">
    在客户端未显式配置时通过网络路径截获流量的代理。掌握标准是能在原始HTTP报文、中间状态或历史规范中定位，并构造最小失败反例。
  </GlossaryItem>
</Glossary>

[← 上一页：第5章 web 服务器](/learn/http-definitive-guide/07-hdg1-05/hdg1-05) · [下一页：第7章 缓存 →](/learn/http-definitive-guide/09-hdg1-07/hdg1-07)

<Attribution
  adaptedFrom="Gourley等《HTTP: The Definitive Guide》2002年首版"
  adaptedUrl="https://www.oreilly.com/library/view/http-the-definitive/1565925092/"
/>
