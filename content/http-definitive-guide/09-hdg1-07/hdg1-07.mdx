---
title: "第7章 缓存"
description: "建立缓存命中、拓扑、处理步骤、新鲜度、再验证、缓存控制、年龄算法和广告计量"
type: A
section: "HTTP权威指南（2002年英文首版 / 2012年中译本）"
order: 9
draft: false
math: false
sourceUrl: "https://www.oreilly.com/library/view/http-the-definitive/1565925092/"
demo: true
---

import {
  Hdg107MessageLab,
  Hdg107DecisionLab,
  Hdg107EvidenceLab,
} from "@/components/mdx/http-definitive-guide/diagrams/hdg1-07";

# 第7章 缓存

本课程锁定David Gourley、Brian Totty、Marjorie Sayer、Sailu Reddy、Anshu Aggarwal著，陈涓、赵振平译《HTTP权威指南》，人民邮电出版社，2012年，ISBN 9787115281487；英文原版HTTP: The Definitive Guide，O'Reilly Media，2002年9月，656页，ISBN 1565925092。O'Reilly官方页面确认英文首版的5个正文部分、21章和8个附录；中文版完整印刷目录另含“第六部分 附录”和索引。忠实度分母共586个部分、章、编号节/小节、附录与索引节点。

<Objectives>

- 能解释“第7章 缓存”的全部正式节点及其HTTP事务位置。
- 能绘制请求、响应、TCP连接、中间实体和资源状态。
- 能设计单变量实验，验证“缓存响应能证明副本键、当前年龄、新鲜寿命、验证器、Vary维度和再验证结果”。
- 能写出含首版边界、原始报文、故障、恢复和复核人的证据包。

</Objectives>

## 从一条可证伪的HTTP事务开始

先预测：把no-cache当作绝不存储或忽略Age/Vary会返回错误变体和陈旧内容。把预测写成“URL/资源、连接、请求报文、中间实体、响应报文、身份与编码”六行，再运行客户端、代理或服务器。结果不同先定位首个字节或状态偏差。

本页主问题是：建立缓存命中、拓扑、处理步骤、新鲜度、再验证、缓存控制、年龄算法和广告计量。每条消息要注明发送者、接收者、HTTP版本、连接复用、逐跳/端到端首部、主体边界与历史协议状态。

验收不变量是：缓存响应能证明副本键、当前年龄、新鲜寿命、验证器、Vary维度和再验证结果。最终页面正常、状态码200或TLS通道建立都只是局部事实，不能单独证明资源身份、缓存变体、代理转发与证书身份全部正确。

## 核心词汇与首版边界

<Term def="请求由已有缓存副本满足而无需完整获取源响应的结果">缓存命中</Term>、
<Term def="缓存副本无需联系源服务器即可复用的有效状态">新鲜度</Term>、
<Term def="用于条件请求判断资源实例是否变化的ETag或日期标识">验证器</Term>、
<Term def="响应自源生成以来按规范计算的累计时间">当前年龄</Term>、
<Term def="缓存向源服务器用条件请求确认副本可否继续使用的过程">再验证</Term>

这些词汇固定在2002年英文首版和2012年中译本语境。HTTP/2、HTTP/3、OAuth、JWT、SameSite、HSTS和现代CDN行为可以独立比较，但不得替换HTTP-NG、Digest、WebDAV、WPAD等原书节点。

<Hdg107MessageLab />

## 核心机制深读

### 先从缓存命中画出事务

建立缓存命中、拓扑、处理步骤、新鲜度、再验证、缓存控制、年龄算法和广告计量。先固定资源身份和请求目标，再标出客户端、中间实体与源服务器，任何优化或安全结论都不能跳过原始报文。

动手试：先手写预期请求与响应，再抓取正常事务和一个单变量故障事务。标出首个不同字节或状态，不用最终页面猜根因。

### 区分缓存命中与新鲜度

缓存命中和新鲜度可能在同一事务中协作，但责任、状态位置和失败语义不同。对比正常请求、单个首部变化和单个连接故障，定位首个偏差。

动手试：先手写预期请求与响应，再抓取正常事务和一个单变量故障事务。标出首个不同字节或状态，不用最终页面猜根因。

### 让验证器经过真实中间实体

代理、缓存、网关或隧道会读取、添加、删除或转发不同首部。逐跳记录Via、Connection列出的字段、缓存决策和认证边界。

动手试：先手写预期请求与响应，再抓取正常事务和一个单变量故障事务。标出首个不同字节或状态，不用最终页面猜根因。

### 验证当前年龄的历史语境

原书写于2002年，以HTTP/1.0、HTTP/1.1、SSL/TLS早期版本和当时Web基础设施为背景。现代实现可做对照，但不能冒充原书节点。

动手试：先手写预期请求与响应，再抓取正常事务和一个单变量故障事务。标出首个不同字节或状态，不用最终页面猜根因。

### 用再验证关闭证据门

缓存响应能证明副本键、当前年龄、新鲜寿命、验证器、Vary维度和再验证结果。证据至少包含原始请求/响应、TCP时间线、中间状态、失败注入、恢复动作和第三方复核。

动手试：先手写预期请求与响应，再抓取正常事务和一个单变量故障事务。标出首个不同字节或状态，不用最终页面猜根因。

## 原书目录逐节点重构

### 第7章 缓存

计算当前年龄与新鲜寿命，核对缓存键、Vary、验证器、条件请求和再验证结果，不以是否联系源站猜命中。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“缓存”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

### 7.1 冗余的数据传输

把“冗余的数据传输”放回“建立缓存命中、拓扑、处理步骤、新鲜度、再验证、缓存控制、年龄算法和广告计量”的HTTP事务，明确客户端、服务器、中间实体、请求输入、响应输出和历史协议限制。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“冗余的数据传输”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

### 7.2 带宽瓶颈

把“带宽瓶颈”放回“建立缓存命中、拓扑、处理步骤、新鲜度、再验证、缓存控制、年龄算法和广告计量”的HTTP事务，明确客户端、服务器、中间实体、请求输入、响应输出和历史协议限制。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“带宽瓶颈”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

### 7.3 瞬间拥塞

把“瞬间拥塞”放回“建立缓存命中、拓扑、处理步骤、新鲜度、再验证、缓存控制、年龄算法和广告计量”的HTTP事务，明确客户端、服务器、中间实体、请求输入、响应输出和历史协议限制。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“瞬间拥塞”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

### 7.4 距离时延

把“距离时延”放回“建立缓存命中、拓扑、处理步骤、新鲜度、再验证、缓存控制、年龄算法和广告计量”的HTTP事务，明确客户端、服务器、中间实体、请求输入、响应输出和历史协议限制。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“距离时延”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

### 7.5 命中和未命中的

把“命中和未命中的”放回“建立缓存命中、拓扑、处理步骤、新鲜度、再验证、缓存控制、年龄算法和广告计量”的HTTP事务，明确客户端、服务器、中间实体、请求输入、响应输出和历史协议限制。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“命中和未命中的”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 7.5.1 再验证

计算当前年龄与新鲜寿命，核对缓存键、Vary、验证器、条件请求和再验证结果，不以是否联系源站猜命中。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“再验证”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 7.5.2 命中率

把“命中率”放回“建立缓存命中、拓扑、处理步骤、新鲜度、再验证、缓存控制、年龄算法和广告计量”的HTTP事务，明确客户端、服务器、中间实体、请求输入、响应输出和历史协议限制。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“命中率”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 7.5.3 字节命中率

把“字节命中率”放回“建立缓存命中、拓扑、处理步骤、新鲜度、再验证、缓存控制、年龄算法和广告计量”的HTTP事务，明确客户端、服务器、中间实体、请求输入、响应输出和历史协议限制。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“字节命中率”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 7.5.4 区分命中和未命中的情况

把“区分命中和未命中的情况”放回“建立缓存命中、拓扑、处理步骤、新鲜度、再验证、缓存控制、年龄算法和广告计量”的HTTP事务，明确客户端、服务器、中间实体、请求输入、响应输出和历史协议限制。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“区分命中和未命中的情况”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

### 7.6 缓存的拓扑结构

计算当前年龄与新鲜寿命，核对缓存键、Vary、验证器、条件请求和再验证结果，不以是否联系源站猜命中。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“缓存的拓扑结构”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 7.6.1 私有缓存

计算当前年龄与新鲜寿命，核对缓存键、Vary、验证器、条件请求和再验证结果，不以是否联系源站猜命中。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“私有缓存”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 7.6.2 公有代理缓存

区分客户端与服务器侧报文形式、逐跳/端到端首部、Via链和认证边界，验证每个代理跳的改写。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“公有代理缓存”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 7.6.3 代理缓存的层次结构

区分客户端与服务器侧报文形式、逐跳/端到端首部、Via链和认证边界，验证每个代理跳的改写。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“代理缓存的层次结构”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 7.6.4 网状缓存、内容路由以及对等缓存

计算当前年龄与新鲜寿命，核对缓存键、Vary、验证器、条件请求和再验证结果，不以是否联系源站猜命中。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“网状缓存、内容路由以及对等缓存”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

### 7.7 缓存的处理步骤

计算当前年龄与新鲜寿命，核对缓存键、Vary、验证器、条件请求和再验证结果，不以是否联系源站猜命中。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“缓存的处理步骤”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 7.7.1 第一步——接收

把“第一步——接收”放回“建立缓存命中、拓扑、处理步骤、新鲜度、再验证、缓存控制、年龄算法和广告计量”的HTTP事务，明确客户端、服务器、中间实体、请求输入、响应输出和历史协议限制。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“第一步——接收”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 7.7.2 第二步——解析

把“第二步——解析”放回“建立缓存命中、拓扑、处理步骤、新鲜度、再验证、缓存控制、年龄算法和广告计量”的HTTP事务，明确客户端、服务器、中间实体、请求输入、响应输出和历史协议限制。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“第二步——解析”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 7.7.3 第三步——查找

把“第三步——查找”放回“建立缓存命中、拓扑、处理步骤、新鲜度、再验证、缓存控制、年龄算法和广告计量”的HTTP事务，明确客户端、服务器、中间实体、请求输入、响应输出和历史协议限制。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“第三步——查找”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 7.7.4 第四步——新鲜度检测

计算当前年龄与新鲜寿命，核对缓存键、Vary、验证器、条件请求和再验证结果，不以是否联系源站猜命中。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“第四步——新鲜度检测”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 7.7.5 第五步——创建响应

把“第五步——创建响应”放回“建立缓存命中、拓扑、处理步骤、新鲜度、再验证、缓存控制、年龄算法和广告计量”的HTTP事务，明确客户端、服务器、中间实体、请求输入、响应输出和历史协议限制。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“第五步——创建响应”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 7.7.6 第六步——发送

把“第六步——发送”放回“建立缓存命中、拓扑、处理步骤、新鲜度、再验证、缓存控制、年龄算法和广告计量”的HTTP事务，明确客户端、服务器、中间实体、请求输入、响应输出和历史协议限制。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“第六步——发送”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 7.7.7 第七步——日志

沿接收、解析、映射、访问控制、响应构建、发送与日志七步定位责任，并保存资源映射和响应元数据。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“第七步——日志”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 7.7.8 缓存处理流程图

计算当前年龄与新鲜寿命，核对缓存键、Vary、验证器、条件请求和再验证结果，不以是否联系源站猜命中。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“缓存处理流程图”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

### 7.8 保持副本的新鲜

计算当前年龄与新鲜寿命，核对缓存键、Vary、验证器、条件请求和再验证结果，不以是否联系源站猜命中。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“保持副本的新鲜”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 7.8.1 文档过期

把“文档过期”放回“建立缓存命中、拓扑、处理步骤、新鲜度、再验证、缓存控制、年龄算法和广告计量”的HTTP事务，明确客户端、服务器、中间实体、请求输入、响应输出和历史协议限制。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“文档过期”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 7.8.2 过期日期和使用期

把“过期日期和使用期”放回“建立缓存命中、拓扑、处理步骤、新鲜度、再验证、缓存控制、年龄算法和广告计量”的HTTP事务，明确客户端、服务器、中间实体、请求输入、响应输出和历史协议限制。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“过期日期和使用期”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 7.8.3 服务器再验证

沿接收、解析、映射、访问控制、响应构建、发送与日志七步定位责任，并保存资源映射和响应元数据。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“服务器再验证”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 7.8.4 用条件方法进行再验证

写出原始请求与响应行、首部顺序无关性、主体边界和方法/状态语义，并保留线路字节作为证据。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“用条件方法进行再验证”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 7.8.5 if-modified-since:date 再验证

计算当前年龄与新鲜寿命，核对缓存键、Vary、验证器、条件请求和再验证结果，不以是否联系源站猜命中。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“if-modified-since:date 再验证”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 7.8.6 if-none-match：实体标签再验证

写出原始请求与响应行、首部顺序无关性、主体边界和方法/状态语义，并保留线路字节作为证据。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“if-none-match：实体标签再验证”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 7.8.7 强弱验证器

计算当前年龄与新鲜寿命，核对缓存键、Vary、验证器、条件请求和再验证结果，不以是否联系源站猜命中。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“强弱验证器”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 7.8.8 什么时候应该使用实体标签和最近修改日期

写出原始请求与响应行、首部顺序无关性、主体边界和方法/状态语义，并保留线路字节作为证据。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“什么时候应该使用实体标签和最近修改日期”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

### 7.9 控制缓存的能力

计算当前年龄与新鲜寿命，核对缓存键、Vary、验证器、条件请求和再验证结果，不以是否联系源站猜命中。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“控制缓存的能力”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 7.9.1 no-store 与no-cache 响应首部

写出原始请求与响应行、首部顺序无关性、主体边界和方法/状态语义，并保留线路字节作为证据。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“no-store 与no-cache 响应首部”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 7.9.2 max-age 响应首部

写出原始请求与响应行、首部顺序无关性、主体边界和方法/状态语义，并保留线路字节作为证据。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“max-age 响应首部”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 7.9.3 expires 响应首部

写出原始请求与响应行、首部顺序无关性、主体边界和方法/状态语义，并保留线路字节作为证据。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“expires 响应首部”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 7.9.4 must-revalidate 响应首部

写出原始请求与响应行、首部顺序无关性、主体边界和方法/状态语义，并保留线路字节作为证据。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“must-revalidate 响应首部”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 7.9.5 试探性过期

把“试探性过期”放回“建立缓存命中、拓扑、处理步骤、新鲜度、再验证、缓存控制、年龄算法和广告计量”的HTTP事务，明确客户端、服务器、中间实体、请求输入、响应输出和历史协议限制。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“试探性过期”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 7.9.6 客户端的新鲜度限制

计算当前年龄与新鲜寿命，核对缓存键、Vary、验证器、条件请求和再验证结果，不以是否联系源站猜命中。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“客户端的新鲜度限制”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 7.9.7 注意事项

把“注意事项”放回“建立缓存命中、拓扑、处理步骤、新鲜度、再验证、缓存控制、年龄算法和广告计量”的HTTP事务，明确客户端、服务器、中间实体、请求输入、响应输出和历史协议限制。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“注意事项”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

### 7.10 设置缓存控制

计算当前年龄与新鲜寿命，核对缓存键、Vary、验证器、条件请求和再验证结果，不以是否联系源站猜命中。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“设置缓存控制”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 7.10.1 控制apache 的http 首部

写出原始请求与响应行、首部顺序无关性、主体边界和方法/状态语义，并保留线路字节作为证据。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“控制apache 的http 首部”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 7.10.2 通过http-equiv 控制html 缓存

计算当前年龄与新鲜寿命，核对缓存键、Vary、验证器、条件请求和再验证结果，不以是否联系源站猜命中。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“通过http-equiv 控制html 缓存”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

### 7.11 详细算法

把“详细算法”放回“建立缓存命中、拓扑、处理步骤、新鲜度、再验证、缓存控制、年龄算法和广告计量”的HTTP事务，明确客户端、服务器、中间实体、请求输入、响应输出和历史协议限制。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“详细算法”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 7.11.1 使用期和新鲜生存期

计算当前年龄与新鲜寿命，核对缓存键、Vary、验证器、条件请求和再验证结果，不以是否联系源站猜命中。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“使用期和新鲜生存期”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 7.11.2 使用期的计算

把“使用期的计算”放回“建立缓存命中、拓扑、处理步骤、新鲜度、再验证、缓存控制、年龄算法和广告计量”的HTTP事务，明确客户端、服务器、中间实体、请求输入、响应输出和历史协议限制。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“使用期的计算”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 7.11.3 完整的使用期计算算法

把“完整的使用期计算算法”放回“建立缓存命中、拓扑、处理步骤、新鲜度、再验证、缓存控制、年龄算法和广告计量”的HTTP事务，明确客户端、服务器、中间实体、请求输入、响应输出和历史协议限制。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“完整的使用期计算算法”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 7.11.4 新鲜生存期计算

计算当前年龄与新鲜寿命，核对缓存键、Vary、验证器、条件请求和再验证结果，不以是否联系源站猜命中。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“新鲜生存期计算”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 7.11.5 完整的服务器——新鲜度算法

沿接收、解析、映射、访问控制、响应构建、发送与日志七步定位责任，并保存资源映射和响应元数据。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“完整的服务器——新鲜度算法”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

### 7.12 缓存和广告

计算当前年龄与新鲜寿命，核对缓存键、Vary、验证器、条件请求和再验证结果，不以是否联系源站猜命中。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“缓存和广告”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 7.12.1 发布广告者的两难处境

计算当前年龄与新鲜寿命，核对缓存键、Vary、验证器、条件请求和再验证结果，不以是否联系源站猜命中。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“发布广告者的两难处境”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 7.12.2 发布者的响应

写出Host或目标身份、选择算法、健康状态、缓存/锁状态、回退路径与最终后端，保存完整跳转链。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“发布者的响应”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 7.12.3 日志迁移

沿接收、解析、映射、访问控制、响应构建、发送与日志七步定位责任，并保存资源映射和响应元数据。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“日志迁移”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 7.12.4 命中计数和使用限制

把“命中计数和使用限制”放回“建立缓存命中、拓扑、处理步骤、新鲜度、再验证、缓存控制、年龄算法和广告计量”的HTTP事务，明确客户端、服务器、中间实体、请求输入、响应输出和历史协议限制。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“命中计数和使用限制”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

### 7.13 更多信息

把“更多信息”放回“建立缓存命中、拓扑、处理步骤、新鲜度、再验证、缓存控制、年龄算法和广告计量”的HTTP事务，明确客户端、服务器、中间实体、请求输入、响应输出和历史协议限制。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“更多信息”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

## 本页完整节点清单

本页承担56个印刷目录正式节点；较短的部分与附录页另列教学支撑，但不扩大586节点分母：

- 第7章 缓存
- 7.1 冗余的数据传输
- 7.2 带宽瓶颈
- 7.3 瞬间拥塞
- 7.4 距离时延
- 7.5 命中和未命中的
- 7.5.1 再验证
- 7.5.2 命中率
- 7.5.3 字节命中率
- 7.5.4 区分命中和未命中的情况
- 7.6 缓存的拓扑结构
- 7.6.1 私有缓存
- 7.6.2 公有代理缓存
- 7.6.3 代理缓存的层次结构
- 7.6.4 网状缓存、内容路由以及对等缓存
- 7.7 缓存的处理步骤
- 7.7.1 第一步——接收
- 7.7.2 第二步——解析
- 7.7.3 第三步——查找
- 7.7.4 第四步——新鲜度检测
- 7.7.5 第五步——创建响应
- 7.7.6 第六步——发送
- 7.7.7 第七步——日志
- 7.7.8 缓存处理流程图
- 7.8 保持副本的新鲜
- 7.8.1 文档过期
- 7.8.2 过期日期和使用期
- 7.8.3 服务器再验证
- 7.8.4 用条件方法进行再验证
- 7.8.5 if-modified-since:date 再验证
- 7.8.6 if-none-match：实体标签再验证
- 7.8.7 强弱验证器
- 7.8.8 什么时候应该使用实体标签和最近修改日期
- 7.9 控制缓存的能力
- 7.9.1 no-store 与no-cache 响应首部
- 7.9.2 max-age 响应首部
- 7.9.3 expires 响应首部
- 7.9.4 must-revalidate 响应首部
- 7.9.5 试探性过期
- 7.9.6 客户端的新鲜度限制
- 7.9.7 注意事项
- 7.10 设置缓存控制
- 7.10.1 控制apache 的http 首部
- 7.10.2 通过http-equiv 控制html 缓存
- 7.11 详细算法
- 7.11.1 使用期和新鲜生存期
- 7.11.2 使用期的计算
- 7.11.3 完整的使用期计算算法
- 7.11.4 新鲜生存期计算
- 7.11.5 完整的服务器——新鲜度算法
- 7.12 缓存和广告
- 7.12.1 发布广告者的两难处境
- 7.12.2 发布者的响应
- 7.12.3 日志迁移
- 7.12.4 命中计数和使用限制
- 7.13 更多信息

<Stepper>
  <Step title="复原原始HTTP事务">
    固定URL、资源、连接与版本，写出请求行、首部、主体、响应和所有中间实体。
    <Hdg107MessageLab />
  </Step>
  <Step title="只改变一个协议变量">
    改变一个首部、连接、缓存年龄、验证器、认证或编码条件，先写预测再执行。
    <Hdg107DecisionLab />
  </Step>
  <Step title="关闭独立证据门">
    核对首版节点、原始字节、连接时间线、中间状态、安全边界和恢复结果。
    <Hdg107EvidenceLab />
  </Step>
</Stepper>

## 可复现实验记录

```text
GET /resource HTTP/1.1\nHost: example.test\nConnection: keep-alive\nAccept: */*
```

```text
HTTP/1.1 200 OK\nDate: Tue, 01 Jan 2002 00:00:00 GMT\nContent-Type: text/plain\nContent-Length: 5\n\nhello
```

```text
evidence = request_bytes + response_bytes + tcp_timeline + intermediary_state + recovery
```

动手试：保存一次正常请求、一次缓存或连接边界和一次单变量故障。记录客户端、代理、缓存、网关与源服务器看到的原始报文，以及统一时间戳和恢复动作。

<Callout type="trap" title="把端到端首部当成逐跳首部">
  Connection及其列出的字段只能作用于当前连接，代理必须移除；误转发会破坏持久连接、缓存和下游解释。
</Callout>

<Callout type="trap" title="用现代HTTP覆盖原书">
  本书的HTTP-NG、Cookie版本1、Digest、FrontPage、WebDAV、WPAD等内容必须按历史状态讲解。现代替代技术不能改写原书目录。
</Callout>

## 独立证据门

<Hdg107EvidenceLab />

最小证据包包含：首版节点、URL、原始请求/响应、连接时间线、代理与缓存决策、身份和编码、单变量故障、恢复、偏差、责任人与复核人。

## 练习

<Exercises>

**问题 1：为什么“第7章 缓存”必须固定2002年首版？**

<Answer>
  固定版次才能让5个正文部分、21章、8附录、索引和586个节点与HTTP/1.0、HTTP/1.1时代协议语境一一对应；现代标准只能独立比较。
</Answer>

**问题 2：怎样构造“把no-cache当作绝不存储或忽略Age/Vary会返回错误变体和陈旧内容”的最小反例？**

<Answer>
  固定资源、请求与其他中间状态，只改变一个连接、首部、缓存、认证、编码或重定向变量；比较原始字节、首个偏差和恢复结果。
</Answer>

**问题 3：何时可以认为本页完成独立交接？**

<Answer>
  另一位学习者无需口头补充即可定位全部56个正式节点，复现正常事务和单变量故障，并依据“缓存响应能证明副本键、当前年龄、新鲜寿命、验证器、Vary维度和再验证结果”得到相同结论。
</Answer>

</Exercises>

## 本章回顾

“第7章 缓存”的核心是建立缓存命中、拓扑、处理步骤、新鲜度、再验证、缓存控制、年龄算法和广告计量。真正掌握不是记首部名，而是能用原始报文、连接和中间状态证明资源如何被定位、传输、缓存、保护与交付。

<Glossary>
  <GlossaryItem term="缓存命中">
    请求由已有缓存副本满足而无需完整获取源响应的结果。掌握标准是能在原始HTTP报文、中间状态或历史规范中定位，并构造最小失败反例。
  </GlossaryItem>
  <GlossaryItem term="新鲜度">
    缓存副本无需联系源服务器即可复用的有效状态。掌握标准是能在原始HTTP报文、中间状态或历史规范中定位，并构造最小失败反例。
  </GlossaryItem>
  <GlossaryItem term="验证器">
    用于条件请求判断资源实例是否变化的ETag或日期标识。掌握标准是能在原始HTTP报文、中间状态或历史规范中定位，并构造最小失败反例。
  </GlossaryItem>
  <GlossaryItem term="当前年龄">
    响应自源生成以来按规范计算的累计时间。掌握标准是能在原始HTTP报文、中间状态或历史规范中定位，并构造最小失败反例。
  </GlossaryItem>
  <GlossaryItem term="再验证">
    缓存向源服务器用条件请求确认副本可否继续使用的过程。掌握标准是能在原始HTTP报文、中间状态或历史规范中定位，并构造最小失败反例。
  </GlossaryItem>
</Glossary>

[← 上一页：第6章 代理](/learn/http-definitive-guide/08-hdg1-06/hdg1-06) · [下一页：第8章 集成点：网关、隧道及中继 →](/learn/http-definitive-guide/10-hdg1-08/hdg1-08)

<Attribution
  adaptedFrom="Gourley等《HTTP: The Definitive Guide》2002年首版"
  adaptedUrl="https://www.oreilly.com/library/view/http-the-definitive/1565925092/"
/>
