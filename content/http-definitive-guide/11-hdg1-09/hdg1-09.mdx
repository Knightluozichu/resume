---
title: "第9章 web 机器人"
description: "构建爬虫根集、链接规范化、环路与重复避免、机器人HTTP、robots.txt和搜索索引"
type: A
section: "HTTP权威指南（2002年英文首版 / 2012年中译本）"
order: 11
draft: false
math: false
sourceUrl: "https://www.oreilly.com/library/view/http-the-definitive/1565925092/"
demo: true
---

import {
  Hdg109MessageLab,
  Hdg109DecisionLab,
  Hdg109EvidenceLab,
} from "@/components/mdx/http-definitive-guide/diagrams/hdg1-09";

# 第9章 web 机器人

本课程锁定David Gourley、Brian Totty、Marjorie Sayer、Sailu Reddy、Anshu Aggarwal著，陈涓、赵振平译《HTTP权威指南》，人民邮电出版社，2012年，ISBN 9787115281487；英文原版HTTP: The Definitive Guide，O'Reilly Media，2002年9月，656页，ISBN 1565925092。O'Reilly官方页面确认英文首版的5个正文部分、21章和8个附录；中文版完整印刷目录另含“第六部分 附录”和索引。忠实度分母共586个部分、章、编号节/小节、附录与索引节点。

<Objectives>

- 能解释“第9章 web 机器人”的全部正式节点及其HTTP事务位置。
- 能绘制请求、响应、TCP连接、中间实体和资源状态。
- 能设计单变量实验，验证“每次抓取都有规范URL、访问策略、条件请求、响应处理、去重状态和礼貌限速”。
- 能写出含首版边界、原始报文、故障、恢复和复核人的证据包。

</Objectives>

## 从一条可证伪的HTTP事务开始

先预测：不做规范化和环路检测会无限抓取别名、动态空间并给站点造成过量负载。把预测写成“URL/资源、连接、请求报文、中间实体、响应报文、身份与编码”六行，再运行客户端、代理或服务器。结果不同先定位首个字节或状态偏差。

本页主问题是：构建爬虫根集、链接规范化、环路与重复避免、机器人HTTP、robots.txt和搜索索引。每条消息要注明发送者、接收者、HTTP版本、连接复用、逐跳/端到端首部、主体边界与历史协议状态。

验收不变量是：每次抓取都有规范URL、访问策略、条件请求、响应处理、去重状态和礼貌限速。最终页面正常、状态码200或TLS通道建立都只是局部事实，不能单独证明资源身份、缓存变体、代理转发与证书身份全部正确。

## 核心词汇与首版边界

<Term def="自动获取页面并从中发现后续链接的Web机器人">爬虫</Term>、
<Term def="爬行开始时提供的初始URL集合">根集</Term>、
<Term def="把等价URL转换为稳定比较形式的过程">URL规范化</Term>、
<Term def="站点发布给机器人读取的访问排除规则文件">robots.txt</Term>、
<Term def="把词项映射到文档以支持搜索查询的数据结构">全文索引</Term>

这些词汇固定在2002年英文首版和2012年中译本语境。HTTP/2、HTTP/3、OAuth、JWT、SameSite、HSTS和现代CDN行为可以独立比较，但不得替换HTTP-NG、Digest、WebDAV、WPAD等原书节点。

<Hdg109MessageLab />

## 核心机制深读

### 先从爬虫画出事务

构建爬虫根集、链接规范化、环路与重复避免、机器人HTTP、robots.txt和搜索索引。先固定资源身份和请求目标，再标出客户端、中间实体与源服务器，任何优化或安全结论都不能跳过原始报文。

动手试：先手写预期请求与响应，再抓取正常事务和一个单变量故障事务。标出首个不同字节或状态，不用最终页面猜根因。

### 区分爬虫与根集

爬虫和根集可能在同一事务中协作，但责任、状态位置和失败语义不同。对比正常请求、单个首部变化和单个连接故障，定位首个偏差。

动手试：先手写预期请求与响应，再抓取正常事务和一个单变量故障事务。标出首个不同字节或状态，不用最终页面猜根因。

### 让URL规范化经过真实中间实体

代理、缓存、网关或隧道会读取、添加、删除或转发不同首部。逐跳记录Via、Connection列出的字段、缓存决策和认证边界。

动手试：先手写预期请求与响应，再抓取正常事务和一个单变量故障事务。标出首个不同字节或状态，不用最终页面猜根因。

### 验证robots.txt的历史语境

原书写于2002年，以HTTP/1.0、HTTP/1.1、SSL/TLS早期版本和当时Web基础设施为背景。现代实现可做对照，但不能冒充原书节点。

动手试：先手写预期请求与响应，再抓取正常事务和一个单变量故障事务。标出首个不同字节或状态，不用最终页面猜根因。

### 用全文索引关闭证据门

每次抓取都有规范URL、访问策略、条件请求、响应处理、去重状态和礼貌限速。证据至少包含原始请求/响应、TCP时间线、中间状态、失败注入、恢复动作和第三方复核。

动手试：先手写预期请求与响应，再抓取正常事务和一个单变量故障事务。标出首个不同字节或状态，不用最终页面猜根因。

## 原书目录逐节点重构

### 第9章 web 机器人

保存规范URL、根集来源、去重状态、robots规则、条件抓取、速率和响应处理，避免动态空间环路。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“web 机器人”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

### 9.1 爬虫及爬行方式

保存规范URL、根集来源、去重状态、robots规则、条件抓取、速率和响应处理，避免动态空间环路。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“爬虫及爬行方式”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 9.1.1 从哪儿开始：根集

把“从哪儿开始：根集”放回“构建爬虫根集、链接规范化、环路与重复避免、机器人HTTP、robots.txt和搜索索引”的HTTP事务，明确客户端、服务器、中间实体、请求输入、响应输出和历史协议限制。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“从哪儿开始：根集”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 9.1.2 链接的提取以及相对链接的标准化

把“链接的提取以及相对链接的标准化”放回“构建爬虫根集、链接规范化、环路与重复避免、机器人HTTP、robots.txt和搜索索引”的HTTP事务，明确客户端、服务器、中间实体、请求输入、响应输出和历史协议限制。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“链接的提取以及相对链接的标准化”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 9.1.3 避免环路的出现

把“避免环路的出现”放回“构建爬虫根集、链接规范化、环路与重复避免、机器人HTTP、robots.txt和搜索索引”的HTTP事务，明确客户端、服务器、中间实体、请求输入、响应输出和历史协议限制。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“避免环路的出现”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 9.1.4 循环与复制

把“循环与复制”放回“构建爬虫根集、链接规范化、环路与重复避免、机器人HTTP、robots.txt和搜索索引”的HTTP事务，明确客户端、服务器、中间实体、请求输入、响应输出和历史协议限制。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“循环与复制”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 9.1.5 面包屑留下的痕迹

把“面包屑留下的痕迹”放回“构建爬虫根集、链接规范化、环路与重复避免、机器人HTTP、robots.txt和搜索索引”的HTTP事务，明确客户端、服务器、中间实体、请求输入、响应输出和历史协议限制。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“面包屑留下的痕迹”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 9.1.6 别名与机器人环路

保存规范URL、根集来源、去重状态、robots规则、条件抓取、速率和响应处理，避免动态空间环路。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“别名与机器人环路”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 9.1.7 规范化url

分解方案、权限、路径、参数、查询和片段，明确规范化与转义发生在哪一端，以及最终请求目标如何形成。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“规范化url”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 9.1.8 文件系统连接环路

关联四元组、握手、连接复用、消息边界、幂等重试和双向关闭；用时间线区分HTTP等待与TCP等待。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“文件系统连接环路”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 9.1.9 动态虚拟web 空间

把“动态虚拟web 空间”放回“构建爬虫根集、链接规范化、环路与重复避免、机器人HTTP、robots.txt和搜索索引”的HTTP事务，明确客户端、服务器、中间实体、请求输入、响应输出和历史协议限制。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“动态虚拟web 空间”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 9.1.10 避免循环和重复

把“避免循环和重复”放回“构建爬虫根集、链接规范化、环路与重复避免、机器人HTTP、robots.txt和搜索索引”的HTTP事务，明确客户端、服务器、中间实体、请求输入、响应输出和历史协议限制。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“避免循环和重复”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

### 9.2 机器人的http

保存规范URL、根集来源、去重状态、robots规则、条件抓取、速率和响应处理，避免动态空间环路。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“机器人的http”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 9.2.1 识别请求首部

写出原始请求与响应行、首部顺序无关性、主体边界和方法/状态语义，并保留线路字节作为证据。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“识别请求首部”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 9.2.2 虚拟主机

写出Host或目标身份、选择算法、健康状态、缓存/锁状态、回退路径与最终后端，保存完整跳转链。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“虚拟主机”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 9.2.3 条件请求

把“条件请求”放回“构建爬虫根集、链接规范化、环路与重复避免、机器人HTTP、robots.txt和搜索索引”的HTTP事务，明确客户端、服务器、中间实体、请求输入、响应输出和历史协议限制。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“条件请求”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 9.2.4 对响应的处理

把“对响应的处理”放回“构建爬虫根集、链接规范化、环路与重复避免、机器人HTTP、robots.txt和搜索索引”的HTTP事务，明确客户端、服务器、中间实体、请求输入、响应输出和历史协议限制。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“对响应的处理”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 9.2.5 user-agent 导向

计算当前年龄与新鲜寿命，核对缓存键、Vary、验证器、条件请求和再验证结果，不以是否联系源站猜命中。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“user-agent 导向”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

### 9.3 行为不当的机器人

保存规范URL、根集来源、去重状态、robots规则、条件抓取、速率和响应处理，避免动态空间环路。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“行为不当的机器人”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

### 9.4 拒绝机器人访问

保存规范URL、根集来源、去重状态、robots规则、条件抓取、速率和响应处理，避免动态空间环路。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“拒绝机器人访问”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 9.4.1 拒绝机器人访问标准

保存规范URL、根集来源、去重状态、robots规则、条件抓取、速率和响应处理，避免动态空间环路。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“拒绝机器人访问标准”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 9.4.2 web 站点和robots.txt 文件

保存规范URL、根集来源、去重状态、robots规则、条件抓取、速率和响应处理，避免动态空间环路。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“web 站点和robots.txt 文件”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 9.4.3 robots.txt 文件的格式

保存规范URL、根集来源、去重状态、robots规则、条件抓取、速率和响应处理，避免动态空间环路。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“robots.txt 文件的格式”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 9.4.4 其他有关robots.txt 的知识

保存规范URL、根集来源、去重状态、robots规则、条件抓取、速率和响应处理，避免动态空间环路。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“其他有关robots.txt 的知识”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 9.4.5 缓存和robots.txt 的过期

计算当前年龄与新鲜寿命，核对缓存键、Vary、验证器、条件请求和再验证结果，不以是否联系源站猜命中。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“缓存和robots.txt 的过期”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 9.4.6 拒绝机器人访问的perl 代码

保存规范URL、根集来源、去重状态、robots规则、条件抓取、速率和响应处理，避免动态空间环路。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“拒绝机器人访问的perl 代码”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 9.4.7 html 的robot-control 元标签

把“html 的robot-control 元标签”放回“构建爬虫根集、链接规范化、环路与重复避免、机器人HTTP、robots.txt和搜索索引”的HTTP事务，明确客户端、服务器、中间实体、请求输入、响应输出和历史协议限制。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“html 的robot-control 元标签”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

### 9.5 机器人的规范

保存规范URL、根集来源、去重状态、robots规则、条件抓取、速率和响应处理，避免动态空间环路。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“机器人的规范”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

### 9.6 搜索引擎

保存规范URL、根集来源、去重状态、robots规则、条件抓取、速率和响应处理，避免动态空间环路。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“搜索引擎”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 9.6.1 大格局

把“大格局”放回“构建爬虫根集、链接规范化、环路与重复避免、机器人HTTP、robots.txt和搜索索引”的HTTP事务，明确客户端、服务器、中间实体、请求输入、响应输出和历史协议限制。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“大格局”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 9.6.2 现代搜索引擎结构

保存规范URL、根集来源、去重状态、robots规则、条件抓取、速率和响应处理，避免动态空间环路。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“现代搜索引擎结构”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 9.6.3 全文索引

保存规范URL、根集来源、去重状态、robots规则、条件抓取、速率和响应处理，避免动态空间环路。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“全文索引”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 9.6.4 发布查询请求

写出Host或目标身份、选择算法、健康状态、缓存/锁状态、回退路径与最终后端，保存完整跳转链。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“发布查询请求”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 9.6.5 对结果进行排序，并提供查询结果

把“对结果进行排序，并提供查询结果”放回“构建爬虫根集、链接规范化、环路与重复避免、机器人HTTP、robots.txt和搜索索引”的HTTP事务，明确客户端、服务器、中间实体、请求输入、响应输出和历史协议限制。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“对结果进行排序，并提供查询结果”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 9.6.6 欺诈

把“欺诈”放回“构建爬虫根集、链接规范化、环路与重复避免、机器人HTTP、robots.txt和搜索索引”的HTTP事务，明确客户端、服务器、中间实体、请求输入、响应输出和历史协议限制。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“欺诈”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

### 9.7 更多信息

把“更多信息”放回“构建爬虫根集、链接规范化、环路与重复避免、机器人HTTP、robots.txt和搜索索引”的HTTP事务，明确客户端、服务器、中间实体、请求输入、响应输出和历史协议限制。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“更多信息”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

## 本页完整节点清单

本页承担36个印刷目录正式节点；较短的部分与附录页另列教学支撑，但不扩大586节点分母：

- 第9章 web 机器人
- 9.1 爬虫及爬行方式
- 9.1.1 从哪儿开始：根集
- 9.1.2 链接的提取以及相对链接的标准化
- 9.1.3 避免环路的出现
- 9.1.4 循环与复制
- 9.1.5 面包屑留下的痕迹
- 9.1.6 别名与机器人环路
- 9.1.7 规范化url
- 9.1.8 文件系统连接环路
- 9.1.9 动态虚拟web 空间
- 9.1.10 避免循环和重复
- 9.2 机器人的http
- 9.2.1 识别请求首部
- 9.2.2 虚拟主机
- 9.2.3 条件请求
- 9.2.4 对响应的处理
- 9.2.5 user-agent 导向
- 9.3 行为不当的机器人
- 9.4 拒绝机器人访问
- 9.4.1 拒绝机器人访问标准
- 9.4.2 web 站点和robots.txt 文件
- 9.4.3 robots.txt 文件的格式
- 9.4.4 其他有关robots.txt 的知识
- 9.4.5 缓存和robots.txt 的过期
- 9.4.6 拒绝机器人访问的perl 代码
- 9.4.7 html 的robot-control 元标签
- 9.5 机器人的规范
- 9.6 搜索引擎
- 9.6.1 大格局
- 9.6.2 现代搜索引擎结构
- 9.6.3 全文索引
- 9.6.4 发布查询请求
- 9.6.5 对结果进行排序，并提供查询结果
- 9.6.6 欺诈
- 9.7 更多信息

<Stepper>
  <Step title="复原原始HTTP事务">
    固定URL、资源、连接与版本，写出请求行、首部、主体、响应和所有中间实体。
    <Hdg109MessageLab />
  </Step>
  <Step title="只改变一个协议变量">
    改变一个首部、连接、缓存年龄、验证器、认证或编码条件，先写预测再执行。
    <Hdg109DecisionLab />
  </Step>
  <Step title="关闭独立证据门">
    核对首版节点、原始字节、连接时间线、中间状态、安全边界和恢复结果。
    <Hdg109EvidenceLab />
  </Step>
</Stepper>

## 可复现实验记录

```text
GET /resource HTTP/1.1\nHost: example.test\nConnection: keep-alive\nAccept: */*
```

```text
HTTP/1.1 200 OK\nDate: Tue, 01 Jan 2002 00:00:00 GMT\nContent-Type: text/plain\nContent-Length: 5\n\nhello
```

```text
evidence = request_bytes + response_bytes + tcp_timeline + intermediary_state + recovery
```

动手试：保存一次正常请求、一次缓存或连接边界和一次单变量故障。记录客户端、代理、缓存、网关与源服务器看到的原始报文，以及统一时间戳和恢复动作。

<Callout type="trap" title="把端到端首部当成逐跳首部">
  Connection及其列出的字段只能作用于当前连接，代理必须移除；误转发会破坏持久连接、缓存和下游解释。
</Callout>

<Callout type="trap" title="用现代HTTP覆盖原书">
  本书的HTTP-NG、Cookie版本1、Digest、FrontPage、WebDAV、WPAD等内容必须按历史状态讲解。现代替代技术不能改写原书目录。
</Callout>

## 独立证据门

<Hdg109EvidenceLab />

最小证据包包含：首版节点、URL、原始请求/响应、连接时间线、代理与缓存决策、身份和编码、单变量故障、恢复、偏差、责任人与复核人。

## 练习

<Exercises>

**问题 1：为什么“第9章 web 机器人”必须固定2002年首版？**

<Answer>
  固定版次才能让5个正文部分、21章、8附录、索引和586个节点与HTTP/1.0、HTTP/1.1时代协议语境一一对应；现代标准只能独立比较。
</Answer>

**问题 2：怎样构造“不做规范化和环路检测会无限抓取别名、动态空间并给站点造成过量负载”的最小反例？**

<Answer>
  固定资源、请求与其他中间状态，只改变一个连接、首部、缓存、认证、编码或重定向变量；比较原始字节、首个偏差和恢复结果。
</Answer>

**问题 3：何时可以认为本页完成独立交接？**

<Answer>
  另一位学习者无需口头补充即可定位全部36个正式节点，复现正常事务和单变量故障，并依据“每次抓取都有规范URL、访问策略、条件请求、响应处理、去重状态和礼貌限速”得到相同结论。
</Answer>

</Exercises>

## 本章回顾

“第9章 web 机器人”的核心是构建爬虫根集、链接规范化、环路与重复避免、机器人HTTP、robots.txt和搜索索引。真正掌握不是记首部名，而是能用原始报文、连接和中间状态证明资源如何被定位、传输、缓存、保护与交付。

<Glossary>
  <GlossaryItem term="爬虫">
    自动获取页面并从中发现后续链接的Web机器人。掌握标准是能在原始HTTP报文、中间状态或历史规范中定位，并构造最小失败反例。
  </GlossaryItem>
  <GlossaryItem term="根集">
    爬行开始时提供的初始URL集合。掌握标准是能在原始HTTP报文、中间状态或历史规范中定位，并构造最小失败反例。
  </GlossaryItem>
  <GlossaryItem term="URL规范化">
    把等价URL转换为稳定比较形式的过程。掌握标准是能在原始HTTP报文、中间状态或历史规范中定位，并构造最小失败反例。
  </GlossaryItem>
  <GlossaryItem term="robots.txt">
    站点发布给机器人读取的访问排除规则文件。掌握标准是能在原始HTTP报文、中间状态或历史规范中定位，并构造最小失败反例。
  </GlossaryItem>
  <GlossaryItem term="全文索引">
    把词项映射到文档以支持搜索查询的数据结构。掌握标准是能在原始HTTP报文、中间状态或历史规范中定位，并构造最小失败反例。
  </GlossaryItem>
</Glossary>

[← 上一页：第8章 集成点：网关、隧道及中继](/learn/http-definitive-guide/10-hdg1-08/hdg1-08) · [下一页：第10章 http-ng →](/learn/http-definitive-guide/12-hdg1-10/hdg1-10)

<Attribution
  adaptedFrom="Gourley等《HTTP: The Definitive Guide》2002年首版"
  adaptedUrl="https://www.oreilly.com/library/view/http-the-definitive/1565925092/"
/>
