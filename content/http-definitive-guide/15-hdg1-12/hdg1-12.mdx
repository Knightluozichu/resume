---
title: "第12章 基本认证机制"
description: "掌握HTTP质询响应框架、安全域、Basic与代理认证及其缺陷"
type: A
section: "HTTP权威指南（2002年英文首版 / 2012年中译本）"
order: 15
draft: false
math: false
sourceUrl: "https://www.oreilly.com/library/view/http-the-definitive/1565925092/"
demo: true
qualityVersion: 2
practiceMode: simulation
sourceMode: independent-rewrite
---

import { Objectives } from "@/components/mdx/objectives";
import { Term } from "@/components/mdx/term";
import { Step, Stepper } from "@/components/mdx/stepper";
import { Callout } from "@/components/mdx/callout";
import { Answer, Exercises } from "@/components/mdx/exercises";
import { Glossary, GlossaryItem } from "@/components/mdx/glossary";
import { Attribution } from "@/components/mdx/attribution";

# 第12章 基本认证机制

“第12章 基本认证机制”锁定David Gourley、Brian Totty、Marjorie Sayer、Sailu Reddy、Anshu Aggarwal著，陈涓、赵振平译《HTTP权威指南》，人民邮电出版社，2012年，ISBN 9787115281487；英文原版HTTP: The Definitive Guide，O'Reilly Media，2002年9月，656页，ISBN 1565925092。O'Reilly官方页面确认英文首版的5个正文部分、21章和8个附录；中文版完整印刷目录另含“第六部分 附录”和索引。忠实度分母共586个部分、章、编号节/小节、附录与索引节点。

“第12章 基本认证机制”未取得未获授权的完整中文正文；课程以 [O’Reilly 官方在线版](https://www.oreilly.com/library/view/http-the-definitive/1565925092/) 与 [章级导览](https://www.oreilly.com/library/view/http-the-definitive/1565925092/pr02s02.html) 界定首版范围，中文解释、报文、实验和练习均为独立教学重写。现代语义仅以 [RFC 9110](https://www.rfc-editor.org/rfc/rfc9110.html)、[RFC 9111](https://www.rfc-editor.org/rfc/rfc9111.html) 和 [RFC 9112](https://www.rfc-editor.org/rfc/rfc9112.html) 核对差异。

<Objectives>

- 能解释“第12章 基本认证机制”的全部正式节点及其HTTP事务位置。
- 能绘制请求、响应、TCP连接、中间实体和资源状态。
- 能设计单变量实验，验证“401/407质询、认证方案、realm、凭据编码和保护范围能形成完整握手”。
- 能写出含首版边界、原始报文、故障、恢复和复核人的证据包。

</Objectives>

## 从一条可证伪的HTTP事务开始

先预测：Base64不是加密，Basic凭据若无安全传输可被窃听和重放。把预测写成“URL/资源、连接、请求报文、中间实体、响应报文、身份与编码”六行，再运行客户端、代理或服务器。结果不同先定位首个字节或状态偏差。

本页主问题是：掌握HTTP质询响应框架、安全域、Basic与代理认证及其缺陷。每条消息要注明发送者、接收者、HTTP版本、连接复用、逐跳/端到端首部、主体边界与历史协议状态。

验收不变量是：401/407质询、认证方案、realm、凭据编码和保护范围能形成完整握手。最终页面正常、状态码200或TLS通道建立都只是局部事实，不能单独证明资源身份、缓存变体、代理转发与证书身份全部正确。

## 核心词汇与首版边界

<Term def="服务器用WWW-Authenticate要求客户端提供指定认证方案凭据的响应">质询</Term>、<Term def="认证方案中标识一组受保护资源的安全域">realm</Term>、<Term def="把用户名和密码组合后以Base64编码发送的HTTP认证方案">Basic认证</Term>、<Term def="把二进制转换为受限ASCII字符的可逆编码而非加密">Base64</Term>、<Term def="代理以407和Proxy-Authenticate执行的认证流程">代理认证</Term>

这些词汇固定在2002年英文首版和2012年中译本语境。在“第12章 基本认证机制”中，HTTP/2、HTTP/3、OAuth、JWT、SameSite、HSTS和现代CDN行为只作独立比较，不得替换HTTP-NG、Digest、WebDAV、WPAD等首版节点。


## 核心机制深读

### 先从质询画出事务

掌握HTTP质询响应框架、安全域、Basic与代理认证及其缺陷。先固定资源身份和请求目标，再标出客户端、中间实体与源服务器，任何优化或安全结论都不能跳过原始报文。

动手试：先手写预期请求与响应，再抓取正常事务和一个单变量故障事务。标出首个不同字节或状态，不用最终页面猜根因。

### 区分质询与realm

质询和realm可能在同一事务中协作，但责任、状态位置和失败语义不同。对比正常请求、单个首部变化和单个连接故障，定位首个偏差。

动手试：先手写预期请求与响应，再抓取正常事务和一个单变量故障事务。标出首个不同字节或状态，不用最终页面猜根因。

### 让Basic认证经过真实中间实体

代理、缓存、网关或隧道会读取、添加、删除或转发不同首部。逐跳记录Via、Connection列出的字段、缓存决策和认证边界。

动手试：先手写预期请求与响应，再抓取正常事务和一个单变量故障事务。标出首个不同字节或状态，不用最终页面猜根因。

### 验证Base64的历史语境

原书写于2002年，以HTTP/1.0、HTTP/1.1、SSL/TLS早期版本和当时Web基础设施为背景。现代实现可做对照，但不能冒充原书节点。

动手试：先手写预期请求与响应，再抓取正常事务和一个单变量故障事务。标出首个不同字节或状态，不用最终页面猜根因。

### 用代理认证关闭证据门

401/407质询、认证方案、realm、凭据编码和保护范围能形成完整握手。证据至少包含原始请求/响应、TCP时间线、中间状态、失败注入、恢复动作和第三方复核。

动手试：先手写预期请求与响应，再抓取正常事务和一个单变量故障事务。标出首个不同字节或状态，不用最终页面猜根因。

## 首版机制逐项深读

### 第12章 基本认证机制

验证“第12章 基本认证机制”分别执行正确凭据、错误凭据和重放事务；摘要输入、时间状态与失败代码都可复算，结论才可交接。

### 12.1 认证

“12.1 认证”的边界包括口令存储、中间人、词典攻击和代理认证；单次登录成功不能证明凭据未在线路或日志中泄露。

#### 12.1.1 http 的质询/ 响应认证框架

“12.1.1 http 的质询/ 响应认证框架”属于挑战—响应认证：服务器给出保护域与挑战参数，客户端生成凭据，验证端检查身份、时效和重放条件；Base64 本身不保密。

#### 12.1.2 认证协议与首部

“12.1.2 认证协议与首部”必须在线路语义中判断：请求方法声明意图，状态码表达处理结果，首部携带元数据，主体边界由明确的报文规则确定。

#### 12.1.3 安全域

“12.1.3 安全域”保护传输中的字节，不自动解决端点权限、应用日志或存储泄露；代理隧道还需约束目标和端口。

### 12.2 基本认证

验证“12.2 基本认证”分别执行正确凭据、错误凭据和重放事务；摘要输入、时间状态与失败代码都可复算，结论才可交接。

#### 12.2.1 基本认证实例

验证“12.2.1 基本认证实例”分别执行正确凭据、错误凭据和重放事务；摘要输入、时间状态与失败代码都可复算，结论才可交接。

#### 12.2.2 base-64 用户名/ 密码编码

“12.2.2 base-64 用户名/ 密码编码”只是把“用户名:密码”的字节转换为可打印字符，任何能看到报文的人都可还原；基本认证必须依赖 TLS 保护传输。

#### 12.2.3 代理认证

“12.2.3 代理认证”的实验固定客户端与源站，只插入或移除一个中间节点，再比较两侧原始报文、认证边界和错误归属。

### 12.3 基本认证的安全缺陷

核对“12.3 基本认证的安全缺陷”要保存 401 或 407 挑战、保护域、nonce、请求目标和 Authorization，区分身份验证、通道保密与资源授权。

### 12.4 更多信息

“12.4 更多信息”是原书的延伸资料入口，不增加新的协议结论；使用时必须记录资料版本、适用的 HTTP 年代和与本章结论的对应关系。
<Stepper>
  <Step title="复原原始HTTP事务">
    固定URL、资源、连接与版本，写出请求行、首部、主体、响应和所有中间实体。
  </Step>
  <Step title="只改变一个协议变量">
    改变一个首部、连接、缓存年龄、验证器、认证或编码条件，先写预测再执行。
  </Step>
  <Step title="关闭独立证据门">
    核对首版节点、原始字节、连接时间线、中间状态、安全边界和恢复结果。
    </Step>
</Stepper>

## 可复现实验记录

```text
GET /resource HTTP/1.1
Host: example.test
Connection: keep-alive
Accept: */*
```

```text
HTTP/1.1 200 OK
Date: Tue, 01 Jan 2002 00:00:00 GMT
Content-Type: text/plain
Content-Length: 5

hello
```

```text
evidence = request_bytes + response_bytes + tcp_timeline + intermediary_state + recovery
```

动手试：保存一次正常请求、一次缓存或连接边界和一次单变量故障。记录客户端、代理、缓存、网关与源服务器看到的原始报文，以及统一时间戳和恢复动作。

<Callout type="trap" title="把端到端首部当成逐跳首部">
  Connection及其列出的字段只能作用于当前连接，代理必须移除；误转发会破坏持久连接、缓存和下游解释。
</Callout>

<Callout type="trap" title="用现代HTTP覆盖原书">
  本书的HTTP-NG、Cookie版本1、Digest、FrontPage、WebDAV、WPAD等内容必须按历史状态讲解。现代替代技术不能改写原书目录。
</Callout>

## 独立证据门

最小证据包包含：首版节点、URL、原始请求/响应、连接时间线、代理与缓存决策、身份和编码、单变量故障、恢复、偏差、责任人与复核人。

## 练习

<Exercises>

**问题 1：为什么“第12章 基本认证机制”必须固定2002年首版？**

<Answer>
  固定版次才能让5个正文部分、21章、8附录、索引和586个节点与HTTP/1.0、HTTP/1.1时代协议语境一一对应；现代标准只能独立比较。
</Answer>

**问题 2：怎样构造“Base64不是加密，Basic凭据若无安全传输可被窃听和重放”的最小反例？**

<Answer>
  固定资源、请求与其他中间状态，只改变一个连接、首部、缓存、认证、编码或重定向变量；比较原始字节、首个偏差和恢复结果。
</Answer>

**问题 3：何时可以认为本页完成独立交接？**

<Answer>
  另一位学习者无需口头补充即可定位全部11个正式节点，复现正常事务和单变量故障，并依据“401/407质询、认证方案、realm、凭据编码和保护范围能形成完整握手”得到相同结论。
</Answer>

</Exercises>

## 本章回顾

“第12章 基本认证机制”的核心是掌握HTTP质询响应框架、安全域、Basic与代理认证及其缺陷。真正掌握不是记首部名，而是能用原始报文、连接和中间状态证明资源如何被定位、传输、缓存、保护与交付。

<Glossary>
  <GlossaryItem term="质询">
    服务器用WWW-Authenticate要求客户端提供指定认证方案凭据的响应。掌握标准是能在原始HTTP报文、中间状态或历史规范中定位，并构造最小失败反例。
  </GlossaryItem>
  <GlossaryItem term="realm">
    认证方案中标识一组受保护资源的安全域。掌握标准是能在原始HTTP报文、中间状态或历史规范中定位，并构造最小失败反例。
  </GlossaryItem>
  <GlossaryItem term="Basic认证">
    把用户名和密码组合后以Base64编码发送的HTTP认证方案。掌握标准是能在原始HTTP报文、中间状态或历史规范中定位，并构造最小失败反例。
  </GlossaryItem>
  <GlossaryItem term="Base64">
    把二进制转换为受限ASCII字符的可逆编码而非加密。掌握标准是能在原始HTTP报文、中间状态或历史规范中定位，并构造最小失败反例。
  </GlossaryItem>
  <GlossaryItem term="代理认证">
    代理以407和Proxy-Authenticate执行的认证流程。掌握标准是能在原始HTTP报文、中间状态或历史规范中定位，并构造最小失败反例。
  </GlossaryItem>
</Glossary>

[← 上一页：第11章 客户端识别与cookie 机制](/learn/http-definitive-guide/14-hdg1-11/hdg1-11) · [下一页：第13章 摘要认证 →](/learn/http-definitive-guide/16-hdg1-13/hdg1-13)

<Attribution
  mode="independent-rewrite"
  sourceBasis="authorized-sample"
  workTitle="David Gourley, Brian Totty, Marjorie Sayer, Sailu Reddy, Anshu Aggarwal, HTTP: The Definitive Guide"
  adaptedUrl="https://www.oreilly.com/library/view/http-the-definitive/1565925092/"
/>
