---
title: "第14章 安全http"
description: "从密码、密钥、签名、证书进入SSL/TLS握手、证书验证、HTTPS客户端和代理隧道"
type: A
section: "HTTP权威指南（2002年英文首版 / 2012年中译本）"
order: 17
draft: false
math: false
sourceUrl: "https://www.oreilly.com/library/view/http-the-definitive/1565925092/"
demo: true
---

import {
  Hdg114MessageLab,
  Hdg114DecisionLab,
  Hdg114EvidenceLab,
} from "@/components/mdx/http-definitive-guide/diagrams/hdg1-14";

# 第14章 安全http

本课程锁定David Gourley、Brian Totty、Marjorie Sayer、Sailu Reddy、Anshu Aggarwal著，陈涓、赵振平译《HTTP权威指南》，人民邮电出版社，2012年，ISBN 9787115281487；英文原版HTTP: The Definitive Guide，O'Reilly Media，2002年9月，656页，ISBN 1565925092。O'Reilly官方页面确认英文首版的5个正文部分、21章和8个附录；中文版完整印刷目录另含“第六部分 附录”和索引。忠实度分母共586个部分、章、编号节/小节、附录与索引节点。

<Objectives>

- 能解释“第14章 安全http”的全部正式节点及其HTTP事务位置。
- 能绘制请求、响应、TCP连接、中间实体和资源状态。
- 能设计单变量实验，验证“HTTPS连接能证明目标名称、证书链、有效期、密钥协商、加密方向和HTTP字节边界”。
- 能写出含首版边界、原始报文、故障、恢复和复核人的证据包。

</Objectives>

## 从一条可证伪的HTTP事务开始

先预测：只看到加密锁标志而不验证主机名与证书链仍可能遭遇中间人。把预测写成“URL/资源、连接、请求报文、中间实体、响应报文、身份与编码”六行，再运行客户端、代理或服务器。结果不同先定位首个字节或状态偏差。

本页主问题是：从密码、密钥、签名、证书进入SSL/TLS握手、证书验证、HTTPS客户端和代理隧道。每条消息要注明发送者、接收者、HTTP版本、连接复用、逐跳/端到端首部、主体边界与历史协议状态。

验收不变量是：HTTPS连接能证明目标名称、证书链、有效期、密钥协商、加密方向和HTTP字节边界。最终页面正常、状态码200或TLS通道建立都只是局部事实，不能单独证明资源身份、缓存变体、代理转发与证书身份全部正确。

## 核心词汇与首版边界

<Term def="在SSL/TLS安全传输之上交换HTTP的方案">HTTPS</Term>、
<Term def="由签发者签名并把身份与公钥绑定的数据结构">数字证书</Term>、
<Term def="加密与解密共享同一秘密的密码密钥">对称密钥</Term>、
<Term def="非对称密码中可公开用于加密或验证签名的密钥">公开密钥</Term>、
<Term def="协商版本、算法、身份和会话密钥的安全连接建立过程">SSL握手</Term>

这些词汇固定在2002年英文首版和2012年中译本语境。HTTP/2、HTTP/3、OAuth、JWT、SameSite、HSTS和现代CDN行为可以独立比较，但不得替换HTTP-NG、Digest、WebDAV、WPAD等原书节点。

<Hdg114MessageLab />

## 核心机制深读

### 先从HTTPS画出事务

从密码、密钥、签名、证书进入SSL/TLS握手、证书验证、HTTPS客户端和代理隧道。先固定资源身份和请求目标，再标出客户端、中间实体与源服务器，任何优化或安全结论都不能跳过原始报文。

动手试：先手写预期请求与响应，再抓取正常事务和一个单变量故障事务。标出首个不同字节或状态，不用最终页面猜根因。

### 区分HTTPS与数字证书

HTTPS和数字证书可能在同一事务中协作，但责任、状态位置和失败语义不同。对比正常请求、单个首部变化和单个连接故障，定位首个偏差。

动手试：先手写预期请求与响应，再抓取正常事务和一个单变量故障事务。标出首个不同字节或状态，不用最终页面猜根因。

### 让对称密钥经过真实中间实体

代理、缓存、网关或隧道会读取、添加、删除或转发不同首部。逐跳记录Via、Connection列出的字段、缓存决策和认证边界。

动手试：先手写预期请求与响应，再抓取正常事务和一个单变量故障事务。标出首个不同字节或状态，不用最终页面猜根因。

### 验证公开密钥的历史语境

原书写于2002年，以HTTP/1.0、HTTP/1.1、SSL/TLS早期版本和当时Web基础设施为背景。现代实现可做对照，但不能冒充原书节点。

动手试：先手写预期请求与响应，再抓取正常事务和一个单变量故障事务。标出首个不同字节或状态，不用最终页面猜根因。

### 用SSL握手关闭证据门

HTTPS连接能证明目标名称、证书链、有效期、密钥协商、加密方向和HTTP字节边界。证据至少包含原始请求/响应、TCP时间线、中间状态、失败注入、恢复动作和第三方复核。

动手试：先手写预期请求与响应，再抓取正常事务和一个单变量故障事务。标出首个不同字节或状态，不用最终页面猜根因。

## 原书目录逐节点重构

### 第14章 安全http

核对目标名称、证书链、有效期、密钥协商、算法和代理隧道，区分加密成功与身份验证成功。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“安全http”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

### 14.1 保护http 的安全

核对目标名称、证书链、有效期、密钥协商、算法和代理隧道，区分加密成功与身份验证成功。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“保护http 的安全”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

### 14.2 数字加密

核对目标名称、证书链、有效期、密钥协商、算法和代理隧道，区分加密成功与身份验证成功。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“数字加密”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 14.2.1 密码编制的机制与技巧

把“密码编制的机制与技巧”放回“从密码、密钥、签名、证书进入SSL/TLS握手、证书验证、HTTPS客户端和代理隧道”的HTTP事务，明确客户端、服务器、中间实体、请求输入、响应输出和历史协议限制。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“密码编制的机制与技巧”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 14.2.2 密码

把“密码”放回“从密码、密钥、签名、证书进入SSL/TLS握手、证书验证、HTTPS客户端和代理隧道”的HTTP事务，明确客户端、服务器、中间实体、请求输入、响应输出和历史协议限制。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“密码”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 14.2.3 密码机

把“密码机”放回“从密码、密钥、签名、证书进入SSL/TLS握手、证书验证、HTTPS客户端和代理隧道”的HTTP事务，明确客户端、服务器、中间实体、请求输入、响应输出和历史协议限制。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“密码机”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 14.2.4 使用了密钥的密码

核对目标名称、证书链、有效期、密钥协商、算法和代理隧道，区分加密成功与身份验证成功。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“使用了密钥的密码”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 14.2.5 数字密码

把“数字密码”放回“从密码、密钥、签名、证书进入SSL/TLS握手、证书验证、HTTPS客户端和代理隧道”的HTTP事务，明确客户端、服务器、中间实体、请求输入、响应输出和历史协议限制。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“数字密码”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

### 14.3 对称密钥加密技术

核对目标名称、证书链、有效期、密钥协商、算法和代理隧道，区分加密成功与身份验证成功。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“对称密钥加密技术”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 14.3.1 密钥长度与枚举攻击

核对目标名称、证书链、有效期、密钥协商、算法和代理隧道，区分加密成功与身份验证成功。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“密钥长度与枚举攻击”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 14.3.2 建立共享密钥

核对目标名称、证书链、有效期、密钥协商、算法和代理隧道，区分加密成功与身份验证成功。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“建立共享密钥”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

### 14.4 公开密钥加密技术

核对目标名称、证书链、有效期、密钥协商、算法和代理隧道，区分加密成功与身份验证成功。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“公开密钥加密技术”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 14.4.1 rsa

把“rsa”放回“从密码、密钥、签名、证书进入SSL/TLS握手、证书验证、HTTPS客户端和代理隧道”的HTTP事务，明确客户端、服务器、中间实体、请求输入、响应输出和历史协议限制。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“rsa”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 14.4.2 混合加密系统和会话密钥

核对目标名称、证书链、有效期、密钥协商、算法和代理隧道，区分加密成功与身份验证成功。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“混合加密系统和会话密钥”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

### 14.5 数字签名

核对目标名称、证书链、有效期、密钥协商、算法和代理隧道，区分加密成功与身份验证成功。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“数字签名”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

### 14.6 数字证书

核对目标名称、证书链、有效期、密钥协商、算法和代理隧道，区分加密成功与身份验证成功。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“数字证书”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 14.6.1 证书的主要内容

核对目标名称、证书链、有效期、密钥协商、算法和代理隧道，区分加密成功与身份验证成功。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“证书的主要内容”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 14.6.2 x.509 v3 证书

核对目标名称、证书链、有效期、密钥协商、算法和代理隧道，区分加密成功与身份验证成功。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“x.509 v3 证书”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 14.6.3 用证书对服务器进行认证

沿接收、解析、映射、访问控制、响应构建、发送与日志七步定位责任，并保存资源映射和响应元数据。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“用证书对服务器进行认证”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

### 14.7 https——细节介绍

核对目标名称、证书链、有效期、密钥协商、算法和代理隧道，区分加密成功与身份验证成功。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“https——细节介绍”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 14.7.1 https 概述

核对目标名称、证书链、有效期、密钥协商、算法和代理隧道，区分加密成功与身份验证成功。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“https 概述”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 14.7.2 https 方案

分解方案、权限、路径、参数、查询和片段，明确规范化与转义发生在哪一端，以及最终请求目标如何形成。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“https 方案”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 14.7.3 建立安全传输

核对目标名称、证书链、有效期、密钥协商、算法和代理隧道，区分加密成功与身份验证成功。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“建立安全传输”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 14.7.4 ssl 握手

核对目标名称、证书链、有效期、密钥协商、算法和代理隧道，区分加密成功与身份验证成功。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“ssl 握手”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 14.7.5 服务器证书

沿接收、解析、映射、访问控制、响应构建、发送与日志七步定位责任，并保存资源映射和响应元数据。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“服务器证书”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 14.7.6 站点证书的有效性

核对目标名称、证书链、有效期、密钥协商、算法和代理隧道，区分加密成功与身份验证成功。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“站点证书的有效性”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 14.7.7 虚拟主机与证书

核对目标名称、证书链、有效期、密钥协商、算法和代理隧道，区分加密成功与身份验证成功。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“虚拟主机与证书”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

### 14.8 https 客户端实例

核对目标名称、证书链、有效期、密钥协商、算法和代理隧道，区分加密成功与身份验证成功。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“https 客户端实例”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 14.8.1 openssl

核对目标名称、证书链、有效期、密钥协商、算法和代理隧道，区分加密成功与身份验证成功。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“openssl”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 14.8.2 简单的https 客户端

核对目标名称、证书链、有效期、密钥协商、算法和代理隧道，区分加密成功与身份验证成功。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“简单的https 客户端”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 14.8.3 执行openssl 客户端

核对目标名称、证书链、有效期、密钥协商、算法和代理隧道，区分加密成功与身份验证成功。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“执行openssl 客户端”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

### 14.9 通过代理以隧道形式传输安全流量

区分客户端与服务器侧报文形式、逐跳/端到端首部、Via链和认证边界，验证每个代理跳的改写。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“通过代理以隧道形式传输安全流量”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

### 14.10 更多信息

把“更多信息”放回“从密码、密钥、签名、证书进入SSL/TLS握手、证书验证、HTTPS客户端和代理隧道”的HTTP事务，明确客户端、服务器、中间实体、请求输入、响应输出和历史协议限制。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“更多信息”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 14.10.1 http 安全性

核对目标名称、证书链、有效期、密钥协商、算法和代理隧道，区分加密成功与身份验证成功。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“http 安全性”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 14.10.2 ssl 与tls

核对目标名称、证书链、有效期、密钥协商、算法和代理隧道，区分加密成功与身份验证成功。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“ssl 与tls”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 14.10.3 公开密钥基础设施

核对目标名称、证书链、有效期、密钥协商、算法和代理隧道，区分加密成功与身份验证成功。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“公开密钥基础设施”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

#### 14.10.4 数字密码

把“数字密码”放回“从密码、密钥、签名、证书进入SSL/TLS握手、证书验证、HTTPS客户端和代理隧道”的HTTP事务，明确客户端、服务器、中间实体、请求输入、响应输出和历史协议限制。 验收时先预测原始报文与状态变化，只改变一个连接、首部、缓存、认证、编码或路由变量；保存首个偏差、影响范围和恢复结果。

**事务核查：** 固定请求目标与源资源，只改变一个与“数字密码”相关的连接、首部、中间实体、身份或编码变量；保存请求、响应、状态、首个偏差和恢复。

## 本页完整节点清单

本页承担37个印刷目录正式节点；较短的部分与附录页另列教学支撑，但不扩大586节点分母：

- 第14章 安全http
- 14.1 保护http 的安全
- 14.2 数字加密
- 14.2.1 密码编制的机制与技巧
- 14.2.2 密码
- 14.2.3 密码机
- 14.2.4 使用了密钥的密码
- 14.2.5 数字密码
- 14.3 对称密钥加密技术
- 14.3.1 密钥长度与枚举攻击
- 14.3.2 建立共享密钥
- 14.4 公开密钥加密技术
- 14.4.1 rsa
- 14.4.2 混合加密系统和会话密钥
- 14.5 数字签名
- 14.6 数字证书
- 14.6.1 证书的主要内容
- 14.6.2 x.509 v3 证书
- 14.6.3 用证书对服务器进行认证
- 14.7 https——细节介绍
- 14.7.1 https 概述
- 14.7.2 https 方案
- 14.7.3 建立安全传输
- 14.7.4 ssl 握手
- 14.7.5 服务器证书
- 14.7.6 站点证书的有效性
- 14.7.7 虚拟主机与证书
- 14.8 https 客户端实例
- 14.8.1 openssl
- 14.8.2 简单的https 客户端
- 14.8.3 执行openssl 客户端
- 14.9 通过代理以隧道形式传输安全流量
- 14.10 更多信息
- 14.10.1 http 安全性
- 14.10.2 ssl 与tls
- 14.10.3 公开密钥基础设施
- 14.10.4 数字密码

<Stepper>
  <Step title="复原原始HTTP事务">
    固定URL、资源、连接与版本，写出请求行、首部、主体、响应和所有中间实体。
    <Hdg114MessageLab />
  </Step>
  <Step title="只改变一个协议变量">
    改变一个首部、连接、缓存年龄、验证器、认证或编码条件，先写预测再执行。
    <Hdg114DecisionLab />
  </Step>
  <Step title="关闭独立证据门">
    核对首版节点、原始字节、连接时间线、中间状态、安全边界和恢复结果。
    <Hdg114EvidenceLab />
  </Step>
</Stepper>

## 可复现实验记录

```text
GET /resource HTTP/1.1\nHost: example.test\nConnection: keep-alive\nAccept: */*
```

```text
HTTP/1.1 200 OK\nDate: Tue, 01 Jan 2002 00:00:00 GMT\nContent-Type: text/plain\nContent-Length: 5\n\nhello
```

```text
evidence = request_bytes + response_bytes + tcp_timeline + intermediary_state + recovery
```

动手试：保存一次正常请求、一次缓存或连接边界和一次单变量故障。记录客户端、代理、缓存、网关与源服务器看到的原始报文，以及统一时间戳和恢复动作。

<Callout type="trap" title="把端到端首部当成逐跳首部">
  Connection及其列出的字段只能作用于当前连接，代理必须移除；误转发会破坏持久连接、缓存和下游解释。
</Callout>

<Callout type="trap" title="用现代HTTP覆盖原书">
  本书的HTTP-NG、Cookie版本1、Digest、FrontPage、WebDAV、WPAD等内容必须按历史状态讲解。现代替代技术不能改写原书目录。
</Callout>

## 独立证据门

<Hdg114EvidenceLab />

最小证据包包含：首版节点、URL、原始请求/响应、连接时间线、代理与缓存决策、身份和编码、单变量故障、恢复、偏差、责任人与复核人。

## 练习

<Exercises>

**问题 1：为什么“第14章 安全http”必须固定2002年首版？**

<Answer>
  固定版次才能让5个正文部分、21章、8附录、索引和586个节点与HTTP/1.0、HTTP/1.1时代协议语境一一对应；现代标准只能独立比较。
</Answer>

**问题 2：怎样构造“只看到加密锁标志而不验证主机名与证书链仍可能遭遇中间人”的最小反例？**

<Answer>
  固定资源、请求与其他中间状态，只改变一个连接、首部、缓存、认证、编码或重定向变量；比较原始字节、首个偏差和恢复结果。
</Answer>

**问题 3：何时可以认为本页完成独立交接？**

<Answer>
  另一位学习者无需口头补充即可定位全部37个正式节点，复现正常事务和单变量故障，并依据“HTTPS连接能证明目标名称、证书链、有效期、密钥协商、加密方向和HTTP字节边界”得到相同结论。
</Answer>

</Exercises>

## 本章回顾

“第14章 安全http”的核心是从密码、密钥、签名、证书进入SSL/TLS握手、证书验证、HTTPS客户端和代理隧道。真正掌握不是记首部名，而是能用原始报文、连接和中间状态证明资源如何被定位、传输、缓存、保护与交付。

<Glossary>
  <GlossaryItem term="HTTPS">
    在SSL/TLS安全传输之上交换HTTP的方案。掌握标准是能在原始HTTP报文、中间状态或历史规范中定位，并构造最小失败反例。
  </GlossaryItem>
  <GlossaryItem term="数字证书">
    由签发者签名并把身份与公钥绑定的数据结构。掌握标准是能在原始HTTP报文、中间状态或历史规范中定位，并构造最小失败反例。
  </GlossaryItem>
  <GlossaryItem term="对称密钥">
    加密与解密共享同一秘密的密码密钥。掌握标准是能在原始HTTP报文、中间状态或历史规范中定位，并构造最小失败反例。
  </GlossaryItem>
  <GlossaryItem term="公开密钥">
    非对称密码中可公开用于加密或验证签名的密钥。掌握标准是能在原始HTTP报文、中间状态或历史规范中定位，并构造最小失败反例。
  </GlossaryItem>
  <GlossaryItem term="SSL握手">
    协商版本、算法、身份和会话密钥的安全连接建立过程。掌握标准是能在原始HTTP报文、中间状态或历史规范中定位，并构造最小失败反例。
  </GlossaryItem>
</Glossary>

[← 上一页：第13章 摘要认证](/learn/http-definitive-guide/16-hdg1-13/hdg1-13) · [下一页：第四部分 实体、编码和国际化 →](/learn/http-definitive-guide/18-hdg1-part-4/hdg1-part-4)

<Attribution
  adaptedFrom="Gourley等《HTTP: The Definitive Guide》2002年首版"
  adaptedUrl="https://www.oreilly.com/library/view/http-the-definitive/1565925092/"
/>
