---
title: "第2章 规则系统及其变体"
type: C
section: "范式与表示"
order: 2
description: "第2章 规则系统及其变体覆盖5个权威目录主题，以交互图解、可复现实验和失败对照重建原书核心知识。"
demo: true
math: false
sourceUrl: "https://www.shoeisha.co.jp/book/detail/9784798145600"
draft: false
---

import {
  Iai02RuleSystemsVariantsMapLab,
  Iai02RuleSystemsVariantsExperimentLab,
  Iai02RuleSystemsVariantsEvidenceLab,
} from "@/components/mdx/illustrated-ai/diagrams/iai-02-rule-systems-variants";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第2章 规则系统及其变体的任务、表示、算法假设和输出证据
- 能比较第2章 规则系统及其变体与01 规则系统的适用边界
- 能复现正常、边界和失败三组实验并定位第一处分叉
- 能判断“规则触发必须满足前提，知识版本与推理结论可追溯，冲突消解稳定；推荐结果必须区分知识规则、用户证据和相似度假设。”是否成立

</Objectives>

## 为什么先固定任务合同

<Term def="规则系统及其变体连接“编码事实”的输入、机制、可观察结果与第一处失败证据。">规则系统及其变体</Term>、<Term def="规则系统连接“匹配规则”的输入、机制、可观察结果与第一处失败证据。">规则系统</Term>、<Term def="知识库连接“消解冲突”的输入、机制、可观察结果与第一处失败证据。">知识库</Term>、<Term def="专家系统连接“执行动作”的输入、机制、可观察结果与第一处失败证据。">专家系统</Term>、<Term def="推荐引擎连接“记录解释”的输入、机制、可观察结果与第一处失败证据。">推荐引擎</Term>、<Term def="编码事实连接“回放反例”的输入、机制、可观察结果与第一处失败证据。">编码事实</Term>
共同构成本页坐标。把事实、产生式规则、冲突消解和解释链组成可追踪的符号推理系统，并比较专家系统与基于相似度或行为数据的推荐机制。

人工智能系统容易出现“结果看起来合理，机制却不可复查”的问题。同一准确率可能来自不同数据切分，同一推荐可能由规则、相似度或反馈偏差造成，同一训练曲线也可能掩盖标签泄漏。学习本章时先固定任务、输入、输出、资源和评价口径，再谈算法名称；否则无法知道提升来自哪里，也无法在失败时定位责任。

本页采用原书2016年技术边界解释核心机制。现代大模型、向量数据库、边缘加速器和云平台只作为延伸对照，不会被反写成原书结论。每个目录主题都进入追溯矩阵，关联一份输入、一个算法前提、一项结构化产物、一个失败样本和一个回归断言。

## 核心模型：从输入到证据

先选择一个可重复的小样本，例如商品推荐、图像分类、语音片段、文本检索或状态智能体。冻结原始数据摘要、标签方案、特征处理、随机种子和运行环境；再为每个阶段记录结构，而不仅是最终分数。规则系统保存触发链，状态模型保存转移轨迹，统计模型保存分布与残差，神经网络保存张量形状和训练曲线，分布系统保存分片、同步和失败重试。

```text
task contract -> representation -> algorithm -> structured evidence -> decision
```

```python
baseline = run(sample, mode="baseline", seed=17)
candidate = run(sample, mode="candidate", seed=17)
assert first_divergence(baseline.trace, candidate.trace) == predicted_stage
```

```text
evidence = {input_version, assumption, intermediate_state, metric, failure_boundary}
```

分数只回答“在这一评价口径下表现如何”，结构证据回答“为什么得到这个结果”。两者必须配对。对概率或学习算法还要报告方差、校准和分布外样本；对搜索或优化算法报告最优性条件、停止标准和资源；对物联网或机器人报告延迟、能耗、降级和现实动作风险。

## 权威目录逐项讲解

### 第2章 规则系统及其变体

规则系统把知识写成条件与结论，推理机执行前向链或后向链。关键不在 if-else 数量，而在事实工作集、规则优先级、终止条件和解释轨迹。

把“第2章 规则系统及其变体”放入本章实验时，先在“编码事实”记录输入对象、假设、参数和预期结构，再进入“匹配规则”只改变一个条件。若指标变化但结构证据没有按预测变化，先检查数据切分、状态版本、随机性和评价口径，不能把偶然分数当作机制成立。

验收这一主题需要同时回答四个问题：它表示什么信息，它依赖哪些前提，它产生什么可观察结果，它在什么边界失效。正常样本证明主路径，边界样本暴露容量或分布限制，失败样本只破坏一个条件，修复后必须从原始输入完整重放。

### 01 规则系统

知识库需要模式、实体、关系、约束和来源元数据。知识获取瓶颈来自专家经验难以形式化、规则间隐含依赖以及现实变化造成的版本漂移。

把“01 规则系统”放入本章实验时，先在“匹配规则”记录输入对象、假设、参数和预期结构，再进入“消解冲突”只改变一个条件。若指标变化但结构证据没有按预测变化，先检查数据切分、状态版本、随机性和评价口径，不能把偶然分数当作机制成立。

验收这一主题需要同时回答四个问题：它表示什么信息，它依赖哪些前提，它产生什么可观察结果，它在什么边界失效。正常样本证明主路径，边界样本暴露容量或分布限制，失败样本只破坏一个条件，修复后必须从原始输入完整重放。

### 02 知识库

专家系统将领域知识与推理机分离，适合边界清晰且需要解释的诊断任务；验证时要覆盖冲突规则、缺失事实、未知状态和错误建议的升级路径。

把“02 知识库”放入本章实验时，先在“消解冲突”记录输入对象、假设、参数和预期结构，再进入“执行动作”只改变一个条件。若指标变化但结构证据没有按预测变化，先检查数据切分、状态版本、随机性和评价口径，不能把偶然分数当作机制成立。

验收这一主题需要同时回答四个问题：它表示什么信息，它依赖哪些前提，它产生什么可观察结果，它在什么边界失效。正常样本证明主路径，边界样本暴露容量或分布限制，失败样本只破坏一个条件，修复后必须从原始输入完整重放。

### 03 专家系统

推荐引擎可由内容规则、协同过滤或混合模型构成。相似用户不等于相同意图，必须处理冷启动、反馈回路、流行度偏差与探索需求。

把“03 专家系统”放入本章实验时，先在“执行动作”记录输入对象、假设、参数和预期结构，再进入“记录解释”只改变一个条件。若指标变化但结构证据没有按预测变化，先检查数据切分、状态版本、随机性和评价口径，不能把偶然分数当作机制成立。

验收这一主题需要同时回答四个问题：它表示什么信息，它依赖哪些前提，它产生什么可观察结果，它在什么边界失效。正常样本证明主路径，边界样本暴露容量或分布限制，失败样本只破坏一个条件，修复后必须从原始输入完整重放。

### 04 推荐引擎

规则系统把知识写成条件与结论，推理机执行前向链或后向链。关键不在 if-else 数量，而在事实工作集、规则优先级、终止条件和解释轨迹。

把“04 推荐引擎”放入本章实验时，先在“记录解释”记录输入对象、假设、参数和预期结构，再进入“回放反例”只改变一个条件。若指标变化但结构证据没有按预测变化，先检查数据切分、状态版本、随机性和评价口径，不能把偶然分数当作机制成立。

验收这一主题需要同时回答四个问题：它表示什么信息，它依赖哪些前提，它产生什么可观察结果，它在什么边界失效。正常样本证明主路径，边界样本暴露容量或分布限制，失败样本只破坏一个条件，修复后必须从原始输入完整重放。

## 三步交互实验

先预测每一步的结构变化，再切换基线、边界、失败和优化模式。滑动样本量只用于观察趋势；若结论依赖一次随机运行，应增加种子和置信区间，而不是挑选最好结果。

<Stepper>
  <Step title="建立表示地图">
    在运行前画出输入、状态、权重或关系，指出每个第2章
    规则系统及其变体主题的生产者、消费者和更新条件。
    <Iai02RuleSystemsVariantsMapLab />
  </Step>
  <Step title="执行单变量实验">
    只改变一个规则、状态、权重、样本或节点配置，先预测第一条分叉，再运行并保存完整轨迹。
    <Iai02RuleSystemsVariantsExperimentLab />
  </Step>
  <Step title="对照结构证据">
    将指标与中间状态并排核对；首错确定后停止下游推断，修复并从原始输入重放。
    <Iai02RuleSystemsVariantsEvidenceLab />
  </Step>
</Stepper>

## 常见失败与边界

<Callout type="trap">
  **把相关性当成机制：**
  指标上升、相似度接近或神经元激活都不能单独证明因果解释。必须用控制变量、反例、消融和独立数据检验“消解冲突”是否真是变化来源。
</Callout>

<Callout type="trap">
  **让版本漂移掩盖首错：**
  数据、规则、特征、依赖或硬件变化后沿用旧缓存，会制造不可复现结论。保存摘要值，清理派生产物，并从“编码事实”开始完整重建。
</Callout>

## 本章回顾

把事实、产生式规则、冲突消解和解释链组成可追踪的符号推理系统，并比较专家系统与基于相似度或行为数据的推荐机制。 本章的验收不是记住术语，而是能够从固定输入预测中间状态，说明假设何时成立，在边界和失败样本上找到第一处分叉，并证明修复没有破坏正常路径。最终结论必须满足：规则触发必须满足前提，知识版本与推理结论可追溯，冲突消解稳定；推荐结果必须区分知识规则、用户证据和相似度假设。

<Glossary>

<GlossaryItem term="规则系统及其变体">
  “规则系统及其变体”在本页用于编码事实：必须同时保存输入版本、结构化结果、评价口径和失效条件，才能与相邻章节形成可回放证据链。
</GlossaryItem>

<GlossaryItem term="规则系统">
  “规则系统”在本页用于匹配规则：必须同时保存输入版本、结构化结果、评价口径和失效条件，才能与相邻章节形成可回放证据链。
</GlossaryItem>

<GlossaryItem term="知识库">
  “知识库”在本页用于消解冲突：必须同时保存输入版本、结构化结果、评价口径和失效条件，才能与相邻章节形成可回放证据链。
</GlossaryItem>

<GlossaryItem term="专家系统">
  “专家系统”在本页用于执行动作：必须同时保存输入版本、结构化结果、评价口径和失效条件，才能与相邻章节形成可回放证据链。
</GlossaryItem>

<GlossaryItem term="推荐引擎">
  “推荐引擎”在本页用于记录解释：必须同时保存输入版本、结构化结果、评价口径和失效条件，才能与相邻章节形成可回放证据链。
</GlossaryItem>

<GlossaryItem term="编码事实">
  “编码事实”在本页用于回放反例：必须同时保存输入版本、结构化结果、评价口径和失效条件，才能与相邻章节形成可回放证据链。
</GlossaryItem>

</Glossary>

<Exercises>

1. 如何为第2章 规则系统及其变体建立最小可复现实验，并区分输入证据、机制证据和结果指标？

<Answer>
固定样本、环境、版本和随机种子，先写出“编码事实 -> 匹配规则 -> 消解冲突 -> 执行动作 -> 记录解释 -> 回放反例”的预期结构。运行后保存每阶段中间状态与摘要值，指标只用于评价结果；第一条与预测不同的结构才是定位起点。修复后清理缓存，从原始输入重放正常、边界和失败样本。
</Answer>

2. 为什么“第2章 规则系统及其变体”不能只用一次最终得分验收？

<Answer>
  最终得分会混合数据切分、表示、参数、随机性和评价口径，无法证明具体机制。应同时保存规则触发、状态转移、权重变化、概率分布、特征表示或分布式执行轨迹，并用简单基线、消融和反例确定提升来源。
</Answer>

3. 设计一个只破坏“规则触发必须满足前提，知识版本与推理结论可追溯，冲突消解稳定；推荐结果必须区分知识规则、用户证据和相似度假设。”中单一条件的失败样本，应该在何处停止？

<Answer>
  先选择一个可观察条件，例如错误规则前提、非法状态转移、标签泄漏、概率未归一、失效同步或断网。只修改该条件，预测它最先影响“编码事实”到“匹配规则”之间的哪份结构；实际运行在第一处分叉立即停止，不允许下游阶段用默认值掩盖根因。
</Answer>

</Exercises>

<Attribution
  adaptedFrom="多田智史著、石井一夫监修、张弥译《图解人工智能》，人民邮电出版社，2021，ISBN 9787115558510；原书《あたらしい人工知能の教科書》，翔泳社，2016，352页，ISBN 9784798145600 · 基于翔泳社官方14章目录、中文版完整编号主题目录与中文版书目信息的独立教学重构"
  adaptedUrl="https://www.shoeisha.co.jp/book/detail/9784798145600"
/>
