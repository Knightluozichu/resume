---
title: "第3章 自动机和人工生命程序"
type: C
section: "范式与表示"
order: 3
description: "第3章 自动机和人工生命程序覆盖5个权威目录主题，以交互图解、可复现实验和失败对照重建原书核心知识。"
demo: true
math: false
sourceUrl: "https://www.shoeisha.co.jp/book/detail/9784798145600"
draft: true
---

import {
  Iai03AutomataArtificialLifeMapLab,
  Iai03AutomataArtificialLifeExperimentLab,
  Iai03AutomataArtificialLifeEvidenceLab,
} from "@/components/mdx/illustrated-ai/diagrams/iai-03-automata-artificial-life";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第3章 自动机和人工生命程序的任务、表示、算法假设和输出证据
- 能比较第3章 自动机和人工生命程序与01 人工生命模型的适用边界
- 能复现正常、边界和失败三组实验并定位第一处分叉
- 能判断“状态集合、转移条件与观察量完备，有限自动机对同一输入序列可重放；概率模型的每行转移和为一，马尔可夫假设不被隐藏历史破坏。”是否成立

</Objectives>

## 为什么先固定任务合同

<Term def="自动机和人工生命程序连接“定义状态”的输入、机制、可观察结果与第一处失败证据。">自动机和人工生命程序</Term>、<Term def="人工生命模型连接“编码局部规则”的输入、机制、可观察结果与第一处失败证据。">人工生命模型</Term>、<Term def="有限自动机连接“注入输入”的输入、机制、可观察结果与第一处失败证据。">有限自动机</Term>、<Term def="马尔可夫模型连接“执行转移”的输入、机制、可观察结果与第一处失败证据。">马尔可夫模型</Term>、<Term def="状态驱动智能体连接“汇总轨迹”的输入、机制、可观察结果与第一处失败证据。">状态驱动智能体</Term>、<Term def="定义状态连接“检验涌现”的输入、机制、可观察结果与第一处失败证据。">定义状态</Term>
共同构成本页坐标。从局部规则涌现、有限状态转移和马尔可夫性三个尺度描述人工生命与状态驱动智能体，明确确定性状态和概率状态的边界。

人工智能系统容易出现“结果看起来合理，机制却不可复查”的问题。同一准确率可能来自不同数据切分，同一推荐可能由规则、相似度或反馈偏差造成，同一训练曲线也可能掩盖标签泄漏。学习本章时先固定任务、输入、输出、资源和评价口径，再谈算法名称；否则无法知道提升来自哪里，也无法在失败时定位责任。

本页采用原书2016年技术边界解释核心机制。现代大模型、向量数据库、边缘加速器和云平台只作为延伸对照，不会被反写成原书结论。每个目录主题都进入追溯矩阵，关联一份输入、一个算法前提、一项结构化产物、一个失败样本和一个回归断言。

## 核心模型：从输入到证据

先选择一个可重复的小样本，例如商品推荐、图像分类、语音片段、文本检索或状态智能体。冻结原始数据摘要、标签方案、特征处理、随机种子和运行环境；再为每个阶段记录结构，而不仅是最终分数。规则系统保存触发链，状态模型保存转移轨迹，统计模型保存分布与残差，神经网络保存张量形状和训练曲线，分布系统保存分片、同步和失败重试。

```text
task contract -> representation -> algorithm -> structured evidence -> decision
```

```python
baseline = run(sample, mode="baseline", seed=17)
candidate = run(sample, mode="candidate", seed=17)
assert first_divergence(baseline.trace, candidate.trace) == predicted_stage
```

```text
evidence = {input_version, assumption, intermediate_state, metric, failure_boundary}
```

分数只回答“在这一评价口径下表现如何”，结构证据回答“为什么得到这个结果”。两者必须配对。对概率或学习算法还要报告方差、校准和分布外样本；对搜索或优化算法报告最优性条件、停止标准和资源；对物联网或机器人报告延迟、能耗、降级和现实动作风险。

## 权威目录逐项讲解

### 第3章 自动机和人工生命程序

人工生命模型用简单个体、局部感知和迭代规则研究群体行为。涌现不是神秘标签，应通过初始条件、邻域、更新顺序和宏观指标之间的因果实验来说明。

把“第3章 自动机和人工生命程序”放入本章实验时，先在“定义状态”记录输入对象、假设、参数和预期结构，再进入“编码局部规则”只改变一个条件。若指标变化但结构证据没有按预测变化，先检查数据切分、状态版本、随机性和评价口径，不能把偶然分数当作机制成立。

验收这一主题需要同时回答四个问题：它表示什么信息，它依赖哪些前提，它产生什么可观察结果，它在什么边界失效。正常样本证明主路径，边界样本暴露容量或分布限制，失败样本只破坏一个条件，修复后必须从原始输入完整重放。

### 01 人工生命模型

有限自动机由状态、字母表、转移函数、初态和接受条件组成。它适合协议、角色行为与模式匹配，但状态数量会随上下文组合迅速膨胀。

把“01 人工生命模型”放入本章实验时，先在“编码局部规则”记录输入对象、假设、参数和预期结构，再进入“注入输入”只改变一个条件。若指标变化但结构证据没有按预测变化，先检查数据切分、状态版本、随机性和评价口径，不能把偶然分数当作机制成立。

验收这一主题需要同时回答四个问题：它表示什么信息，它依赖哪些前提，它产生什么可观察结果，它在什么边界失效。正常样本证明主路径，边界样本暴露容量或分布限制，失败样本只破坏一个条件，修复后必须从原始输入完整重放。

### 02 有限自动机

马尔可夫模型用状态转移概率表示不确定序列。只依赖当前状态是建模假设；若遗漏关键历史，应扩展状态，而不是继续把偏差归因于随机噪声。

把“02 有限自动机”放入本章实验时，先在“注入输入”记录输入对象、假设、参数和预期结构，再进入“执行转移”只改变一个条件。若指标变化但结构证据没有按预测变化，先检查数据切分、状态版本、随机性和评价口径，不能把偶然分数当作机制成立。

验收这一主题需要同时回答四个问题：它表示什么信息，它依赖哪些前提，它产生什么可观察结果，它在什么边界失效。正常样本证明主路径，边界样本暴露容量或分布限制，失败样本只破坏一个条件，修复后必须从原始输入完整重放。

### 03 马尔可夫模型

状态驱动智能体把感知映射为状态，再由状态选择动作。工程验收应保存完整轨迹，检查不可达状态、死锁、振荡与并发事件顺序。

把“03 马尔可夫模型”放入本章实验时，先在“执行转移”记录输入对象、假设、参数和预期结构，再进入“汇总轨迹”只改变一个条件。若指标变化但结构证据没有按预测变化，先检查数据切分、状态版本、随机性和评价口径，不能把偶然分数当作机制成立。

验收这一主题需要同时回答四个问题：它表示什么信息，它依赖哪些前提，它产生什么可观察结果，它在什么边界失效。正常样本证明主路径，边界样本暴露容量或分布限制，失败样本只破坏一个条件，修复后必须从原始输入完整重放。

### 04 状态驱动智能体

人工生命模型用简单个体、局部感知和迭代规则研究群体行为。涌现不是神秘标签，应通过初始条件、邻域、更新顺序和宏观指标之间的因果实验来说明。

把“04 状态驱动智能体”放入本章实验时，先在“汇总轨迹”记录输入对象、假设、参数和预期结构，再进入“检验涌现”只改变一个条件。若指标变化但结构证据没有按预测变化，先检查数据切分、状态版本、随机性和评价口径，不能把偶然分数当作机制成立。

验收这一主题需要同时回答四个问题：它表示什么信息，它依赖哪些前提，它产生什么可观察结果，它在什么边界失效。正常样本证明主路径，边界样本暴露容量或分布限制，失败样本只破坏一个条件，修复后必须从原始输入完整重放。

## 三步交互实验

先预测每一步的结构变化，再切换基线、边界、失败和优化模式。滑动样本量只用于观察趋势；若结论依赖一次随机运行，应增加种子和置信区间，而不是挑选最好结果。

<Stepper>
  <Step title="建立表示地图">
    在运行前画出输入、状态、权重或关系，指出每个第3章
    自动机和人工生命程序主题的生产者、消费者和更新条件。
    <Iai03AutomataArtificialLifeMapLab />
  </Step>
  <Step title="执行单变量实验">
    只改变一个规则、状态、权重、样本或节点配置，先预测第一条分叉，再运行并保存完整轨迹。
    <Iai03AutomataArtificialLifeExperimentLab />
  </Step>
  <Step title="对照结构证据">
    将指标与中间状态并排核对；首错确定后停止下游推断，修复并从原始输入重放。
    <Iai03AutomataArtificialLifeEvidenceLab />
  </Step>
</Stepper>

## 常见失败与边界

<Callout type="trap">
  **把相关性当成机制：**
  指标上升、相似度接近或神经元激活都不能单独证明因果解释。必须用控制变量、反例、消融和独立数据检验“注入输入”是否真是变化来源。
</Callout>

<Callout type="trap">
  **让版本漂移掩盖首错：**
  数据、规则、特征、依赖或硬件变化后沿用旧缓存，会制造不可复现结论。保存摘要值，清理派生产物，并从“定义状态”开始完整重建。
</Callout>

## 本章回顾

从局部规则涌现、有限状态转移和马尔可夫性三个尺度描述人工生命与状态驱动智能体，明确确定性状态和概率状态的边界。 本章的验收不是记住术语，而是能够从固定输入预测中间状态，说明假设何时成立，在边界和失败样本上找到第一处分叉，并证明修复没有破坏正常路径。最终结论必须满足：状态集合、转移条件与观察量完备，有限自动机对同一输入序列可重放；概率模型的每行转移和为一，马尔可夫假设不被隐藏历史破坏。

<Glossary>

<GlossaryItem term="自动机和人工生命程序">
  “自动机和人工生命程序”在本页用于定义状态：必须同时保存输入版本、结构化结果、评价口径和失效条件，才能与相邻章节形成可回放证据链。
</GlossaryItem>

<GlossaryItem term="人工生命模型">
  “人工生命模型”在本页用于编码局部规则：必须同时保存输入版本、结构化结果、评价口径和失效条件，才能与相邻章节形成可回放证据链。
</GlossaryItem>

<GlossaryItem term="有限自动机">
  “有限自动机”在本页用于注入输入：必须同时保存输入版本、结构化结果、评价口径和失效条件，才能与相邻章节形成可回放证据链。
</GlossaryItem>

<GlossaryItem term="马尔可夫模型">
  “马尔可夫模型”在本页用于执行转移：必须同时保存输入版本、结构化结果、评价口径和失效条件，才能与相邻章节形成可回放证据链。
</GlossaryItem>

<GlossaryItem term="状态驱动智能体">
  “状态驱动智能体”在本页用于汇总轨迹：必须同时保存输入版本、结构化结果、评价口径和失效条件，才能与相邻章节形成可回放证据链。
</GlossaryItem>

<GlossaryItem term="定义状态">
  “定义状态”在本页用于检验涌现：必须同时保存输入版本、结构化结果、评价口径和失效条件，才能与相邻章节形成可回放证据链。
</GlossaryItem>

</Glossary>

<Exercises>

1. 如何为第3章 自动机和人工生命程序建立最小可复现实验，并区分输入证据、机制证据和结果指标？

<Answer>
固定样本、环境、版本和随机种子，先写出“定义状态 -> 编码局部规则 -> 注入输入 -> 执行转移 -> 汇总轨迹 -> 检验涌现”的预期结构。运行后保存每阶段中间状态与摘要值，指标只用于评价结果；第一条与预测不同的结构才是定位起点。修复后清理缓存，从原始输入重放正常、边界和失败样本。
</Answer>

2. 为什么“第3章 自动机和人工生命程序”不能只用一次最终得分验收？

<Answer>
  最终得分会混合数据切分、表示、参数、随机性和评价口径，无法证明具体机制。应同时保存规则触发、状态转移、权重变化、概率分布、特征表示或分布式执行轨迹，并用简单基线、消融和反例确定提升来源。
</Answer>

3. 设计一个只破坏“状态集合、转移条件与观察量完备，有限自动机对同一输入序列可重放；概率模型的每行转移和为一，马尔可夫假设不被隐藏历史破坏。”中单一条件的失败样本，应该在何处停止？

<Answer>
  先选择一个可观察条件，例如错误规则前提、非法状态转移、标签泄漏、概率未归一、失效同步或断网。只修改该条件，预测它最先影响“定义状态”到“编码局部规则”之间的哪份结构；实际运行在第一处分叉立即停止，不允许下游阶段用默认值掩盖根因。
</Answer>

</Exercises>

<Attribution
  adaptedFrom="多田智史著、石井一夫监修、张弥译《图解人工智能》，人民邮电出版社，2021，ISBN 9787115558510；原书《あたらしい人工知能の教科書》，翔泳社，2016，352页，ISBN 9784798145600 · 基于翔泳社官方14章目录、中文版完整编号主题目录与中文版书目信息的独立教学重构"
  adaptedUrl="https://www.shoeisha.co.jp/book/detail/9784798145600"
/>
