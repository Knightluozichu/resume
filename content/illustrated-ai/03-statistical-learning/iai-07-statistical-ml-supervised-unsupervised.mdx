---
title: "第7章 统计机器学习（无监督学习和有监督学习）"
type: C
section: "统计学习"
order: 7
description: "第7章 统计机器学习（无监督学习和有监督学习）覆盖3个权威目录主题，以交互图解、可复现实验和失败对照重建原书核心知识。"
demo: true
math: false
sourceUrl: "https://www.shoeisha.co.jp/book/detail/9784798145600"
draft: false
---

import {
  Iai07StatisticalMlSupervisedUnsupervisedMapLab,
  Iai07StatisticalMlSupervisedUnsupervisedExperimentLab,
  Iai07StatisticalMlSupervisedUnsupervisedEvidenceLab,
} from "@/components/mdx/illustrated-ai/diagrams/iai-07-statistical-ml-supervised-unsupervised";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第7章 统计机器学习（无监督学习和有监督学习）的任务、表示、算法假设和输出证据
- 能比较第7章 统计机器学习（无监督学习和有监督学习）与01 无监督学习的适用边界
- 能复现正常、边界和失败三组实验并定位第一处分叉
- 能判断“数据切分在训练前固定，特征处理不泄漏测试信息；聚类结论说明距离与簇假设，监督模型报告基线、误差分布和类别不平衡影响。”是否成立

</Objectives>

## 为什么先固定任务合同

<Term def="统计机器学习（无监督学习和有监督学习）连接“审计样本”的输入、机制、可观察结果与第一处失败证据。">统计机器学习（无监督学习和有监督学习）</Term>、<Term def="无监督学习连接“划分数据”的输入、机制、可观察结果与第一处失败证据。">无监督学习</Term>、<Term def="有监督学习连接“建立基线”的输入、机制、可观察结果与第一处失败证据。">有监督学习</Term>、<Term def="审计样本连接“拟合模型”的输入、机制、可观察结果与第一处失败证据。">审计样本</Term>、<Term def="划分数据连接“评估泛化”的输入、机制、可观察结果与第一处失败证据。">划分数据</Term>、<Term def="建立基线连接“分析错误”的输入、机制、可观察结果与第一处失败证据。">建立基线</Term>
共同构成本页坐标。以标签是否存在区分学习信号：无监督学习寻找结构与表示，有监督学习估计输入到目标的映射，并用独立数据衡量泛化。

人工智能系统容易出现“结果看起来合理，机制却不可复查”的问题。同一准确率可能来自不同数据切分，同一推荐可能由规则、相似度或反馈偏差造成，同一训练曲线也可能掩盖标签泄漏。学习本章时先固定任务、输入、输出、资源和评价口径，再谈算法名称；否则无法知道提升来自哪里，也无法在失败时定位责任。

本页采用原书2016年技术边界解释核心机制。现代大模型、向量数据库、边缘加速器和云平台只作为延伸对照，不会被反写成原书结论。每个目录主题都进入追溯矩阵，关联一份输入、一个算法前提、一项结构化产物、一个失败样本和一个回归断言。

监督学习和无监督学习需要不同的验收协议。监督任务先按实体、时间或来源切分数据，避免同一对象的近重复样本跨越训练集与测试集；再建立多数类、均值预测或简单线性模型等基线，并按类别、场景和置信区间报告误差。若只随机切分高度相关的数据，测试分数会被泄漏抬高。无监督任务没有唯一标签答案，因此不能只展示一张漂亮的二维图。聚类应检查不同初始化、抽样和距离度量下的稳定性，降维应检查邻域或方差保留，密度模型应检查留出样本的似然与异常排序，并说明这些结构能否改善后续任务。

两类学习也共享同一数据责任。缺失值处理、标准化、词表建立和特征选择都必须在训练子集上拟合，再应用到验证与测试子集；否则预处理已经偷看未来。模型选择期间只使用训练与验证数据，测试集保留到最终一次验收。上线后再把真实输入按时间窗口与训练分布比较，分别监控特征漂移、标签漂移、校准变化和业务代价。发现退化时先定位数据合同或评价口径是否改变，再决定重训、调整阈值或更换算法。

## 核心模型：从输入到证据

先选择一个可重复的小样本，例如商品推荐、图像分类、语音片段、文本检索或状态智能体。冻结原始数据摘要、标签方案、特征处理、随机种子和运行环境；再为每个阶段记录结构，而不仅是最终分数。规则系统保存触发链，状态模型保存转移轨迹，统计模型保存分布与残差，神经网络保存张量形状和训练曲线，分布系统保存分片、同步和失败重试。

```text
task contract -> representation -> algorithm -> structured evidence -> decision
```

```python
baseline = run(sample, mode="baseline", seed=17)
candidate = run(sample, mode="candidate", seed=17)
assert first_divergence(baseline.trace, candidate.trace) == predicted_stage
```

```text
evidence = {input_version, assumption, intermediate_state, metric, failure_boundary}
```

分数只回答“在这一评价口径下表现如何”，结构证据回答“为什么得到这个结果”。两者必须配对。对概率或学习算法还要报告方差、校准和分布外样本；对搜索或优化算法报告最优性条件、停止标准和资源；对物联网或机器人报告延迟、能耗、降级和现实动作风险。

## 权威目录逐项讲解

### 第7章 统计机器学习（无监督学习和有监督学习）

无监督学习包括聚类、降维和密度估计。没有标签不代表没有目标，距离、邻域、簇形状与维数约束都构成归纳偏置，结果要用稳定性和外部用途验证。

把“第7章 统计机器学习（无监督学习和有监督学习）”放入本章实验时，先在“审计样本”记录输入对象、假设、参数和预期结构，再进入“划分数据”只改变一个条件。若指标变化但结构证据没有按预测变化，先检查数据切分、状态版本、随机性和评价口径，不能把偶然分数当作机制成立。

验收这一主题需要同时回答四个问题：它表示什么信息，它依赖哪些前提，它产生什么可观察结果，它在什么边界失效。正常样本证明主路径，边界样本暴露容量或分布限制，失败样本只破坏一个条件，修复后必须从原始输入完整重放。

### 01 无监督学习

有监督学习以配对输入和标签训练分类或回归模型。标签噪声、抽样偏差、数据泄漏和分布漂移往往比更换算法更能决定上线表现。

把“01 无监督学习”放入本章实验时，先在“划分数据”记录输入对象、假设、参数和预期结构，再进入“建立基线”只改变一个条件。若指标变化但结构证据没有按预测变化，先检查数据切分、状态版本、随机性和评价口径，不能把偶然分数当作机制成立。

验收这一主题需要同时回答四个问题：它表示什么信息，它依赖哪些前提，它产生什么可观察结果，它在什么边界失效。正常样本证明主路径，边界样本暴露容量或分布限制，失败样本只破坏一个条件，修复后必须从原始输入完整重放。

### 02 有监督学习

无监督学习包括聚类、降维和密度估计。没有标签不代表没有目标，距离、邻域、簇形状与维数约束都构成归纳偏置，结果要用稳定性和外部用途验证。

把“02 有监督学习”放入本章实验时，先在“建立基线”记录输入对象、假设、参数和预期结构，再进入“拟合模型”只改变一个条件。若指标变化但结构证据没有按预测变化，先检查数据切分、状态版本、随机性和评价口径，不能把偶然分数当作机制成立。

验收这一主题需要同时回答四个问题：它表示什么信息，它依赖哪些前提，它产生什么可观察结果，它在什么边界失效。正常样本证明主路径，边界样本暴露容量或分布限制，失败样本只破坏一个条件，修复后必须从原始输入完整重放。

## 三步交互实验

先预测每一步的结构变化，再切换基线、边界、失败和优化模式。滑动样本量只用于观察趋势；若结论依赖一次随机运行，应增加种子和置信区间，而不是挑选最好结果。

<Stepper>
  <Step title="建立表示地图">
    在运行前画出输入、状态、权重或关系，指出每个第7章
    统计机器学习（无监督学习和有监督学习）主题的生产者、消费者和更新条件。
    <Iai07StatisticalMlSupervisedUnsupervisedMapLab />
  </Step>
  <Step title="执行单变量实验">
    只改变一个规则、状态、权重、样本或节点配置，先预测第一条分叉，再运行并保存完整轨迹。
    <Iai07StatisticalMlSupervisedUnsupervisedExperimentLab />
  </Step>
  <Step title="对照结构证据">
    将指标与中间状态并排核对；首错确定后停止下游推断，修复并从原始输入重放。
    <Iai07StatisticalMlSupervisedUnsupervisedEvidenceLab />
  </Step>
</Stepper>

## 常见失败与边界

<Callout type="trap">
  **把相关性当成机制：**
  指标上升、相似度接近或神经元激活都不能单独证明因果解释。必须用控制变量、反例、消融和独立数据检验“建立基线”是否真是变化来源。
</Callout>

<Callout type="trap">
  **让版本漂移掩盖首错：**
  数据、规则、特征、依赖或硬件变化后沿用旧缓存，会制造不可复现结论。保存摘要值，清理派生产物，并从“审计样本”开始完整重建。
</Callout>

## 本章回顾

以标签是否存在区分学习信号：无监督学习寻找结构与表示，有监督学习估计输入到目标的映射，并用独立数据衡量泛化。 本章的验收不是记住术语，而是能够从固定输入预测中间状态，说明假设何时成立，在边界和失败样本上找到第一处分叉，并证明修复没有破坏正常路径。最终结论必须满足：数据切分在训练前固定，特征处理不泄漏测试信息；聚类结论说明距离与簇假设，监督模型报告基线、误差分布和类别不平衡影响。

<Glossary>

<GlossaryItem term="统计机器学习（无监督学习和有监督学习）">
  “统计机器学习（无监督学习和有监督学习）”在本页用于审计样本：必须同时保存输入版本、结构化结果、评价口径和失效条件，才能与相邻章节形成可回放证据链。
</GlossaryItem>

<GlossaryItem term="无监督学习">
  “无监督学习”在本页用于划分数据：必须同时保存输入版本、结构化结果、评价口径和失效条件，才能与相邻章节形成可回放证据链。
</GlossaryItem>

<GlossaryItem term="有监督学习">
  “有监督学习”在本页用于建立基线：必须同时保存输入版本、结构化结果、评价口径和失效条件，才能与相邻章节形成可回放证据链。
</GlossaryItem>

<GlossaryItem term="审计样本">
  “审计样本”在本页用于拟合模型：必须同时保存输入版本、结构化结果、评价口径和失效条件，才能与相邻章节形成可回放证据链。
</GlossaryItem>

<GlossaryItem term="划分数据">
  “划分数据”在本页用于评估泛化：必须同时保存输入版本、结构化结果、评价口径和失效条件，才能与相邻章节形成可回放证据链。
</GlossaryItem>

<GlossaryItem term="建立基线">
  “建立基线”在本页用于分析错误：必须同时保存输入版本、结构化结果、评价口径和失效条件，才能与相邻章节形成可回放证据链。
</GlossaryItem>

</Glossary>

<Exercises>

1. 如何为第7章 统计机器学习（无监督学习和有监督学习）建立最小可复现实验，并区分输入证据、机制证据和结果指标？

<Answer>
固定样本、环境、版本和随机种子，先写出“审计样本 -> 划分数据 -> 建立基线 -> 拟合模型 -> 评估泛化 -> 分析错误”的预期结构。运行后保存每阶段中间状态与摘要值，指标只用于评价结果；第一条与预测不同的结构才是定位起点。修复后清理缓存，从原始输入重放正常、边界和失败样本。
</Answer>

2. 为什么“第7章 统计机器学习（无监督学习和有监督学习）”不能只用一次最终得分验收？

<Answer>
  最终得分会混合数据切分、表示、参数、随机性和评价口径，无法证明具体机制。应同时保存规则触发、状态转移、权重变化、概率分布、特征表示或分布式执行轨迹，并用简单基线、消融和反例确定提升来源。
</Answer>

3. 设计一个只破坏“数据切分在训练前固定，特征处理不泄漏测试信息；聚类结论说明距离与簇假设，监督模型报告基线、误差分布和类别不平衡影响。”中单一条件的失败样本，应该在何处停止？

<Answer>
  先选择一个可观察条件，例如错误规则前提、非法状态转移、标签泄漏、概率未归一、失效同步或断网。只修改该条件，预测它最先影响“审计样本”到“划分数据”之间的哪份结构；实际运行在第一处分叉立即停止，不允许下游阶段用默认值掩盖根因。
</Answer>

</Exercises>

<Attribution
  adaptedFrom="多田智史著、石井一夫监修、张弥译《图解人工智能》，人民邮电出版社，2021，ISBN 9787115558510；原书《あたらしい人工知能の教科書》，翔泳社，2016，352页，ISBN 9784798145600 · 基于翔泳社官方14章目录、中文版完整编号主题目录与中文版书目信息的独立教学重构"
  adaptedUrl="https://www.shoeisha.co.jp/book/detail/9784798145600"
/>
