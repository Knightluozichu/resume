---
title: "强化学习"
type: A
section: "人工智能与机器学习"
order: 7
description: "MDP、Q-learning、策略梯度与深度强化学习"
demo: true
math: false
sourceUrl: ""
draft: true
---

import { IaiReinforcementLearningDiagram } from "@/components/mdx/illustrated-ai/diagrams/iai-reinforcement-learning";
import { Objectives, Callout, Glossary, GlossaryItem, Term, Exercises, Answer } from "@/components/mdx/mdx-components";

<Objectives>

- 理解强化学习交互循环：智能体观察状态 → 选择动作 → 获得奖励 → 状态转移
- 掌握马尔可夫决策过程（MDP）五元组与贝尔曼方程
- 理解价值法（Q-learning/DQN）与策略法（REINFORCE/PPO）的区别
- 了解探索与利用的权衡及 AlphaGo 的里程碑意义

</Objectives>

## 强化学习的本质

强化学习（Reinforcement Learning, RL）研究智能体（Agent）在与环境（Environment）的交互中，通过试错和奖励信号学习最优策略。与监督学习的区别：

| 维度 | 监督学习 | 强化学习 |
|------|---------|---------|
| 信号 | 标准答案（标签） | 奖励信号（标量） |
| 数据 | 静态数据集 | 动态交互序列 |
| 反馈 | 即时 | 延迟（序列决策） |
| 目标 | 最小化预测误差 | 最大化长期累积回报 |

<IaiReinforcementLearningDiagram />

## Agent-Environment 交互循环

```text
         动作 a_t
Agent ──────────────→ Environment
  ↑                      │
  │   状态 s_{t+1}        │
  │   奖励 r_t            │
  └──────────────────────┘
```

智能体观察状态 `s_t`，根据策略 `pi(a|s)` 选择动作 `a_t`，环境执行动作后返回新状态 `s_{t+1}` 和奖励 `r_t`。目标是找到策略使累积折扣回报最大化：

```text
G_t = r_t + gamma * r_{t+1} + gamma^2 * r_{t+2} + ...
```

其中 `gamma`（0~1）是折扣因子，权衡即时与长期回报。

## 马尔可夫决策过程（MDP）

强化学习的数学基础是 MDP，由五元组定义：

| 要素 | 含义 |
|------|------|
| S | 状态空间（State space） |
| A | 动作空间（Action space） |
| P(s'|s,a) | 状态转移概率 |
| R(s,a) | 奖励函数 |
| gamma | 折扣因子（0~1） |

**马尔可夫性质**：未来只依赖当前状态，与历史无关。`P(s_{t+1} | s_t, a_t, s_{t-1}, ...) = P(s_{t+1} | s_t, a_t)`。

### 贝尔曼方程

价值函数满足递归关系——当前状态的价值 = 即时奖励 + 折扣后的后续价值：

```text
V(s) = max_a [ R(s,a) + gamma * sum_{s'} P(s'|s,a) * V(s') ]
```

## 两大方法族

### 价值法（Value-based）

学习**价值函数** Q(s,a)，表示在状态 s 执行动作 a 后的期望累积回报，然后贪心地选择 Q 值最大的动作。

- **Q-learning**：经典表格法，用 TD（时序差分）更新
  ```text
  Q(s,a) ← Q(s,a) + alpha * [r + gamma * max_a' Q(s',a') - Q(s,a)]
  ```
- **DQN**（Deep Q-Network）：用深度网络逼近 Q 函数，处理高维状态空间
- **Double DQN**：缓解 Q 值过高估计问题

### 策略法（Policy-based）

直接学习策略函数 `pi(a|s)`，输出动作的概率分布。

- **REINFORCE**：策略梯度的基础算法
- **PPO**（Proximal Policy Optimization）：限制策略更新幅度，稳定训练
- **Actor-Critic**：结合价值法（Critic 评估）和策略法（Actor 执行）

<Callout type="info">

**价值法 vs 策略法**：价值法适合离散动作空间（如棋类），策略法适合连续动作空间（如机器人控制）。Actor-Critic 结合两者优势，是当前主流。

</Callout>

## 探索与利用

智能体面临根本性权衡：

- **利用（Exploitation）**：选择已知最优动作，最大化当前回报
- **探索（Exploration）**：尝试新动作，发现可能更优的策略

常见策略：

- **epsilon-greedy**：以概率 epsilon 随机探索，1-epsilon 贪心利用
- **UCB**（Upper Confidence Bound）：优先选择不确定性高的动作
- **Softmax**：按 Q 值的 softmax 概率选择动作

## 里程碑：AlphaGo

AlphaGo（2016）是强化学习的标志性成果，击败围棋世界冠军李世石。其核心是：

1. **监督学习初始化**：用人类棋谱训练策略网络
2. **强化学习提升**：自我对弈，用策略梯度优化
3. **价值网络**：评估棋局胜率
4. **MCTS**：蒙特卡洛树搜索结合神经网络

<Callout type="tip">

**AlphaGo 的启示**：AlphaGo 证明了「学习」可以超越「人类经验」——通过自我对弈，AI 发现了人类从未发现的最优策略。这为后续 AlphaZero（无人类知识，纯自对弈）和 ChatGPT 的 RLHF（人类反馈强化学习）奠定了基础。

</Callout>

<Glossary>

<GlossaryItem term="马尔可夫决策过程（MDP）">
强化学习的数学基础，由五元组 (S, A, P, R, gamma) 定义——状态空间、动作空间、转移概率、奖励函数、折扣因子。马尔可夫性质指未来只依赖当前状态。
</GlossaryItem>

<GlossaryItem term="贝尔曼方程">
价值函数的递归关系：当前状态价值 = 即时奖励 + 折扣后的后续状态价值。V(s) = max_a [R(s,a) + gamma * E[V(s')]]，是 RL 算法推导的基础。
</GlossaryItem>

<GlossaryItem term="Q-learning">
经典表格法 TD 控制算法，通过时序差分更新 Q(s,a)：Q ← Q + alpha * [r + gamma * max Q(s',a') - Q]。DQN 用深度网络逼近 Q 函数处理高维状态。
</GlossaryItem>

<GlossaryItem term="策略梯度">
直接学习策略 pi(a|s) 的方法，通过梯度上升最大化期望回报。REINFORCE 是基础算法，PPO 限制更新幅度稳定训练，Actor-Critic 结合价值法。
</GlossaryItem>

<GlossaryItem term="探索与利用">
智能体在已知最优动作（利用）和尝试新动作（探索）之间的权衡。epsilon-greedy 以概率 epsilon 随机探索，UCB 优先选不确定性高的动作。
</GlossaryItem>

<GlossaryItem term="DQN（Deep Q-Network）">
用深度神经网络逼近 Q 函数的算法，处理高维状态空间（如像素输入）。引入经验回放和目标网络稳定训练，是深度强化学习的奠基工作。
</GlossaryItem>

</Glossary>

<Exercises>

### 问题 1

强化学习的交互循环是什么？MDP 五元组分别是什么？马尔可夫性质的含义是什么？

<Answer>

强化学习交互循环：智能体（Agent）观察当前状态 s_t，根据策略 pi(a|s) 选择动作 a_t，环境（Environment）执行动作后返回新状态 s_{t+1} 和奖励 r_t，智能体根据奖励调整策略，目标是最大化累积折扣回报 G_t = r_t + gamma*r_{t+1} + gamma^2*r_{t+2} + ...。MDP 五元组：①S 状态空间——所有可能状态的集合；②A 动作空间——智能体可执行的所有动作；③P(s'|s,a) 状态转移概率——在状态 s 执行动作 a 后转移到 s' 的概率；④R(s,a) 奖励函数——在状态 s 执行动作 a 获得的即时奖励；⑤gamma 折扣因子（0~1）——权衡即时奖励与长期回报，越接近 1 越重视长期。马尔可夫性质：未来只依赖当前状态，与历史无关，即 P(s_{t+1}|s_t, a_t, s_{t-1}, ...) = P(s_{t+1}|s_t, a_t)。这意味着当前状态包含了做决策所需的全部历史信息，无需记住完整历史，大幅简化了问题。

</Answer>

### 问题 2

价值法和策略法的区别是什么？Q-learning 和 DQN 如何工作？

<Answer>

价值法 vs 策略法：①价值法（Value-based）学习价值函数 Q(s,a)（表示在状态 s 执行动作 a 后的期望累积回报），然后贪心地选择 Q 值最大的动作。适合离散动作空间（如棋类），代表算法 Q-learning、DQN。②策略法（Policy-based）直接学习策略函数 pi(a|s) 输出动作的概率分布，无需通过价值函数间接推导。适合连续动作空间（如机器人控制），代表算法 REINFORCE、PPO。③Actor-Critic 结合两者——Actor 执行策略、Critic 评估价值。Q-learning 工作原理：经典表格法，对每个 (s,a) 对维护一个 Q 值表，用 TD（时序差分）更新——Q(s,a) ← Q(s,a) + alpha * [r + gamma * max_a' Q(s',a') - Q(s,a)]，其中 alpha 是学习率，方括号内是 TD 误差（当前估计与 r + gamma*下一步最优估计的差）。DQN（Deep Q-Network）用深度神经网络逼近 Q 函数，处理高维状态空间（如 Atari 游戏的像素输入）。两个关键创新：①经验回放——将交互数据存入缓冲区，随机采样训练，打破数据相关性；②目标网络——用延迟更新的网络计算目标 Q 值，稳定训练。Double DQN 进一步缓解 Q 值过高估计问题。

</Answer>

### 问题 3

探索与利用的权衡是什么？常见策略有哪些？

<Answer>

探索与利用是强化学习的根本性权衡：利用（Exploitation）是选择已知 Q 值最高的动作，最大化当前期望回报，但可能错过更优的未知动作；探索（Exploration）是尝试非最优动作，可能发现更好的策略，但短期回报可能降低。如果只利用不探索，智能体可能陷入局部最优；如果只探索不利用，智能体无法积累回报。常见策略：①epsilon-greedy——以概率 epsilon 随机选择动作（探索），以 1-epsilon 选择 Q 值最高的动作（利用）。epsilon 通常随时间衰减（早期多探索，后期多利用）。简单有效，最常用。②UCB（Upper Confidence Bound）——优先选择不确定性高（被尝试次数少）的动作，公式 UCB = Q(s,a) + c * sqrt(ln(N) / n(s,a))，其中 N 是总尝试次数，n(s,a) 是该动作被选次数。平衡了 Q 值和不确定性。③Softmax——按 Q 值的 softmax 概率选择动作，Q 值越高被选概率越大但非确定性，温度参数控制探索程度。选择依据：简单场景用 epsilon-greedy，理论保证用 UCB，连续空间用 Softmax。

</Answer>

### 问题 4

AlphaGo 的核心技术是什么？它对强化学习领域有什么意义？

<Answer>

AlphaGo（2016）的核心技术组合：①监督学习初始化——用人类专业棋谱（约 3000 万步）训练策略网络，学习人类专家的落子习惯，作为初始策略。②强化学习提升——策略网络通过自我对弈（与历史版本对弈）用策略梯度优化，超越人类水平，发现人类未知的策略。③价值网络——评估棋局胜率（当前局面下黑/白获胜的概率），用于 MCTS 的叶节点评估，替代耗时的随机 rollout。④MCTS（蒙特卡洛树搜索）——结合策略网络（缩小搜索范围）和价值网络（评估叶节点）进行搜索，平衡探索与利用。AlphaGo 的意义：①证明了「学习」可以超越「人类经验」——通过自我对弈，AI 发现了人类从未发现的最优策略（如著名的第 37 手）。②为 AlphaZero 奠定基础——AlphaZero 完全不使用人类棋谱，纯从零自对弈学习，在围棋、国际象棋、将棋中均超越人类。③为 RLHF 奠定基础——ChatGPT 的 RLHF（人类反馈强化学习）借鉴了 AlphaGo 用价值网络对齐人类判断的思路。④推动了深度强化学习从游戏到现实的迁移——后续应用于机器人控制、自动驾驶、芯片设计等领域。

</Answer>

</Exercises>
