---
title: "第3章 卷积神经网络"
type: B
section: "第3章 · 卷积神经网络"
order: 3
description: "第3章 卷积神经网络覆盖8个权威目录层级，以可检查张量、图解训练和失败对照重建原书核心。"
demo: true
math: true
sourceUrl: "https://www.kspub.co.jp/book/detail/1538252.html"
draft: false
---

import {
  Idl03ConvolutionalNeuralNetworksMapLab,
  Idl03ConvolutionalNeuralNetworksExperimentLab,
  Idl03ConvolutionalNeuralNetworksEvidenceLab,
} from "@/components/mdx/illustrated-dl/diagrams/idl-03-convolutional-neural-networks";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第3章 卷积神经网络在原书8章路线中的输入、状态、训练信号与评价边界
- 能计算前向、损失、梯度、能量、采样或正则中的关键量
- 能复现三个互动视图并定位第一条张量、统计量或泛化轨迹分叉
- 能判断“每层空间尺寸、通道数、填充、步幅和参数共享必须可计算；前向与反向索引约定不一致时立即停止。”是否成立

</Objectives>

## 为什么沿原书证据链学习

<Term def="卷积神经网络连接“冻结样本与环境”的模型状态、训练信号与验证证据。">卷积神经网络</Term>、<Term def="卷积神经网络的结构连接“计算前向状态”的模型状态、训练信号与验证证据。">卷积神经网络的结构</Term>、<Term def="卷积层连接“计算目标与梯度”的模型状态、训练信号与验证证据。">卷积层</Term>、<Term def="池化层连接“更新参数或状态”的模型状态、训练信号与验证证据。">池化层</Term>、<Term def="全连接层连接“独立验证”的模型状态、训练信号与验证证据。">全连接层</Term>、<Term def="输出层连接“定位首个分叉”的模型状态、训练信号与验证证据。">输出层</Term>
共同构成本页坐标。沿输入、卷积、池化、全连接与输出层追踪空间张量，解释局部连接、权重共享与反向传播如何共同学习图像表示。

山下隆义从可训练神经网络出发，依次讨论卷积结构、受限玻尔兹曼机、自编码器、泛化方法、工具和应用。这个顺序连接了判别式前向与反向传播、概率生成模型的能量与采样、受约束重构表示以及运行环境。重建时保留2016年原书边界，后来的架构只作为明确标注的补充，不替换原章。

实验统一冻结样本标识、数据折分、预处理统计、参数初始化、随机种子、浮点精度和依赖版本。训练阶段保存每层张量、目标函数分解、梯度或采样统计、更新前后参数；模型选择阶段保存每个候选的训练与验证轨迹；测试集直到选择完成后才打开。这样可区分模型机制、优化误差、采样偏差和数据抽样误差。

阅读每个主题都先预测第一处可观察变化，再运行最小样本。若预期变化发生在前向张量，就不应等到最终准确率才调试；若机制只改变正则或采样，则保持基础网络和数据不变。任何修复都从原始输入重放，不能继续使用已被错误状态污染的缓存、梯度或检查点。

## 数学骨架与可核查量

一层神经网络先计算仿射变换和非线性：

$$
z^{(\ell)}=W^{(\ell)}h^{(\ell-1)}+b^{(\ell)},\qquad h^{(\ell)}=\phi\!\left(z^{(\ell)}\right).
$$

训练目标在冻结样本上汇总任务损失与可选约束：

$$
\mathcal{J}(\theta)=\frac{1}{n}\sum_{i=1}^{n}\ell\!\left(y_i,f_\theta(x_i)\right)+\lambda\,\Omega(\theta).
$$

参数更新必须使用与该目标一致的梯度或近似统计量：

$$
\theta_{t+1}=\theta_t-\eta_t\,\widehat{\nabla_\theta\mathcal{J}}(\theta_t).
$$

本页的专属可核查关系为：

$$
y_{i,j,k}=\phi\!\left(b_k+\sum_c\sum_{u,v}K_{u,v,c,k}x_{i+u,j+v,c}\right).
$$

输出位置由局部输入窗口和跨通道卷积核共同决定，同一核在所有空间位置共享。 实现至少检查形状、有限值、归一化或守恒关系、局部数值容差和更新方向。

## 机制辨析

1. 卷积网络把图像网格结构写入模型，局部感受野提取邻域模式，堆叠层逐步扩大有效感受野。
2. 卷积层在不同空间位置复用同一组核参数，显著减少参数，并使同一模式平移后产生对应响应。
3. 池化层汇总局部响应并降低空间分辨率，可增加小位移稳定性，但过早池化会丢失定位信息。
4. 全连接层把高层特征组合成任务表示，展平顺序和输入维度必须与前一层输出完全一致。
5. 输出层依任务使用线性、sigmoid或softmax，并与回归、二分类或多分类损失配对。
6. 训练卷积网络仍使用反向传播，但卷积核梯度需累加所有共享位置贡献，池化梯度按前向选择路径返回。
7. 小结应从输入尺寸手算各层形状、参数量和感受野，并用有限差分检查一个卷积核元素梯度。

卷积实验先建立形状账本。对每层记录输入高度、宽度、通道、核尺寸、步幅、填充、输出尺寸和参数量，再用小型单通道样本手算卷积和池化。常见首错包括把相关运算误称翻转卷积、通道维累加遗漏、边界填充不一致以及最大池化并列值的梯度规则不同。验证阶段把卷积网络与参数量相近的全连接基线比较，固定数据增强和训练预算；否则无法判断收益来自空间归纳偏置还是更多计算。对平移样本还应检查特征图是否按预期移动，区分卷积等变性和池化后的近似不变性。

同一最终分数可能来自不同机制，因此必须保留结构化中间量。前向实现检查张量尺寸与范围，反向实现检查数值梯度，能量模型检查概率归一与链统计，自编码器检查约束是否阻止复制，泛化方法检查训练和推理模式，工具复现检查跨框架张量容差。首错不通过时，下游结论全部作废。

## 权威目录逐项讲解

### 第3章 卷积神经网络

卷积网络把图像网格结构写入模型，局部感受野提取邻域模式，堆叠层逐步扩大有效感受野。

验证“第3章 卷积神经网络”时，在“冻结样本与环境”冻结输入、张量形状与随机状态，再进入“计算前向状态”只改变一个条件。保存前向状态、目标分解、梯度或采样统计、更新前后参数和独立验证风险；若最终分数改善但首个中间量没有按机制变化，先排查数据泄漏、索引约定和数值误差。

通过标准包含定义、方程、最小算法和反例四层：能说明该主题解决什么问题，手算或枚举关键量，复现一个确定性最小实验，并构造破坏前提的失败样本。修复后清空派生缓存，从冻结原始样本完整重放。

### 3.1 卷积神经网络的结构

卷积层在不同空间位置复用同一组核参数，显著减少参数，并使同一模式平移后产生对应响应。

验证“3.1 卷积神经网络的结构”时，在“计算前向状态”冻结输入、张量形状与随机状态，再进入“计算目标与梯度”只改变一个条件。保存前向状态、目标分解、梯度或采样统计、更新前后参数和独立验证风险；若最终分数改善但首个中间量没有按机制变化，先排查数据泄漏、索引约定和数值误差。

通过标准包含定义、方程、最小算法和反例四层：能说明该主题解决什么问题，手算或枚举关键量，复现一个确定性最小实验，并构造破坏前提的失败样本。修复后清空派生缓存，从冻结原始样本完整重放。

### 3.2 卷积层

池化层汇总局部响应并降低空间分辨率，可增加小位移稳定性，但过早池化会丢失定位信息。

验证“3.2 卷积层”时，在“计算目标与梯度”冻结输入、张量形状与随机状态，再进入“更新参数或状态”只改变一个条件。保存前向状态、目标分解、梯度或采样统计、更新前后参数和独立验证风险；若最终分数改善但首个中间量没有按机制变化，先排查数据泄漏、索引约定和数值误差。

通过标准包含定义、方程、最小算法和反例四层：能说明该主题解决什么问题，手算或枚举关键量，复现一个确定性最小实验，并构造破坏前提的失败样本。修复后清空派生缓存，从冻结原始样本完整重放。

### 3.3 池化层

全连接层把高层特征组合成任务表示，展平顺序和输入维度必须与前一层输出完全一致。

验证“3.3 池化层”时，在“更新参数或状态”冻结输入、张量形状与随机状态，再进入“独立验证”只改变一个条件。保存前向状态、目标分解、梯度或采样统计、更新前后参数和独立验证风险；若最终分数改善但首个中间量没有按机制变化，先排查数据泄漏、索引约定和数值误差。

通过标准包含定义、方程、最小算法和反例四层：能说明该主题解决什么问题，手算或枚举关键量，复现一个确定性最小实验，并构造破坏前提的失败样本。修复后清空派生缓存，从冻结原始样本完整重放。

### 3.4 全连接层

输出层依任务使用线性、sigmoid或softmax，并与回归、二分类或多分类损失配对。

验证“3.4 全连接层”时，在“独立验证”冻结输入、张量形状与随机状态，再进入“定位首个分叉”只改变一个条件。保存前向状态、目标分解、梯度或采样统计、更新前后参数和独立验证风险；若最终分数改善但首个中间量没有按机制变化，先排查数据泄漏、索引约定和数值误差。

通过标准包含定义、方程、最小算法和反例四层：能说明该主题解决什么问题，手算或枚举关键量，复现一个确定性最小实验，并构造破坏前提的失败样本。修复后清空派生缓存，从冻结原始样本完整重放。

### 3.5 输出层

训练卷积网络仍使用反向传播，但卷积核梯度需累加所有共享位置贡献，池化梯度按前向选择路径返回。

验证“3.5 输出层”时，在“定位首个分叉”冻结输入、张量形状与随机状态，再进入“冻结样本与环境”只改变一个条件。保存前向状态、目标分解、梯度或采样统计、更新前后参数和独立验证风险；若最终分数改善但首个中间量没有按机制变化，先排查数据泄漏、索引约定和数值误差。

通过标准包含定义、方程、最小算法和反例四层：能说明该主题解决什么问题，手算或枚举关键量，复现一个确定性最小实验，并构造破坏前提的失败样本。修复后清空派生缓存，从冻结原始样本完整重放。

### 3.6 神经网络的训练方法

小结应从输入尺寸手算各层形状、参数量和感受野，并用有限差分检查一个卷积核元素梯度。

验证“3.6 神经网络的训练方法”时，在“冻结样本与环境”冻结输入、张量形状与随机状态，再进入“计算前向状态”只改变一个条件。保存前向状态、目标分解、梯度或采样统计、更新前后参数和独立验证风险；若最终分数改善但首个中间量没有按机制变化，先排查数据泄漏、索引约定和数值误差。

通过标准包含定义、方程、最小算法和反例四层：能说明该主题解决什么问题，手算或枚举关键量，复现一个确定性最小实验，并构造破坏前提的失败样本。修复后清空派生缓存，从冻结原始样本完整重放。

### 3.7 小结

卷积网络把图像网格结构写入模型，局部感受野提取邻域模式，堆叠层逐步扩大有效感受野。

验证“3.7 小结”时，在“计算前向状态”冻结输入、张量形状与随机状态，再进入“计算目标与梯度”只改变一个条件。保存前向状态、目标分解、梯度或采样统计、更新前后参数和独立验证风险；若最终分数改善但首个中间量没有按机制变化，先排查数据泄漏、索引约定和数值误差。

通过标准包含定义、方程、最小算法和反例四层：能说明该主题解决什么问题，手算或枚举关键量，复现一个确定性最小实验，并构造破坏前提的失败样本。修复后清空派生缓存，从冻结原始样本完整重放。

## 三步交互实验

先预测哪一层张量、哪项统计量或哪段验证轨迹会变化，再切换基线、边界、失败与优化样本。样本量滑块用于观察估计方差和计算成本，不用于挑选单次最好结果。

<Stepper>
  <Step title="画出原书模型地图">
    标出样本、层或状态、目标、更新、验证与失败边界的生产者和消费者。
    <Idl03ConvolutionalNeuralNetworksMapLab />
  </Step>
  <Step title="运行单变量实验">
    只改变一个层宽、核、学习率、链长、扰动率、失活率或环境约定，并预测第一处分叉。
    <Idl03ConvolutionalNeuralNetworksExperimentLab />
  </Step>
  <Step title="核对独立证据">
    并排比较中间状态、目标、梯度或采样统计、验证风险与资源边界，定位首错后从原始样本重放。
    <Idl03ConvolutionalNeuralNetworksEvidenceLab />
  </Step>
</Stepper>

## 常见失败与边界

<Callout type="trap">
  **只看训练准确率：**
  深层模型可以记忆训练样本，能量模型可以产生表面合理重构，工具也可以显示训练完成。必须以独立验证、简单基线、跨种子区间和失败样本检验“独立验证”。
</Callout>

<Callout type="trap">
  **把现代知识回写进原书：**
  本书权威结构止于2016年原版8章。RNN、Transformer、GAN、扩散模型等后续主题可作为现代补充链接，但不得冒充原书章节或替换RBM、DBN和历史工具内容。
</Callout>

<Callout type="tip">
  **最小复现顺序：**
  先手算或枚举极小样本，再核对程序中间量，最后扩大数据。任何非有限值、形状偏差、概率不归一、数值梯度失败或测试集泄漏都应立即停止。
</Callout>

## 本章回顾

沿输入、卷积、池化、全连接与输出层追踪空间张量，解释局部连接、权重共享与反向传播如何共同学习图像表示。 验收时必须从冻结样本预测中间状态、训练信号和更新结果，解释结构或数据条件为何改变解，并在失败样本中找到第一处违背假设的位置。最终结论要求：每层空间尺寸、通道数、填充、步幅和参数共享必须可计算；前向与反向索引约定不一致时立即停止。

<Glossary>

<GlossaryItem term="卷积神经网络">
  “卷积神经网络”用于冻结样本与环境：记录数据版本、张量形状、数学假设、中间状态、数值容差和失效边界，不能只保留最终准确率。
</GlossaryItem>

<GlossaryItem term="卷积神经网络的结构">
  “卷积神经网络的结构”用于计算前向状态：记录数据版本、张量形状、数学假设、中间状态、数值容差和失效边界，不能只保留最终准确率。
</GlossaryItem>

<GlossaryItem term="卷积层">
  “卷积层”用于计算目标与梯度：记录数据版本、张量形状、数学假设、中间状态、数值容差和失效边界，不能只保留最终准确率。
</GlossaryItem>

<GlossaryItem term="池化层">
  “池化层”用于更新参数或状态：记录数据版本、张量形状、数学假设、中间状态、数值容差和失效边界，不能只保留最终准确率。
</GlossaryItem>

<GlossaryItem term="全连接层">
  “全连接层”用于独立验证：记录数据版本、张量形状、数学假设、中间状态、数值容差和失效边界，不能只保留最终准确率。
</GlossaryItem>

<GlossaryItem term="输出层">
  “输出层”用于定位首个分叉：记录数据版本、张量形状、数学假设、中间状态、数值容差和失效边界，不能只保留最终准确率。
</GlossaryItem>

</Glossary>

<Exercises>

1. 如何为第3章 卷积神经网络建立不泄漏测试集的最小复现实验？

<Answer>
先冻结样本标识并划分训练、验证、测试三部分，所有预处理、结构选择、学习率、链长、扰动与失活参数只在训练和验证折确定。按“冻结样本与环境 -> 计算前向状态 -> 计算目标与梯度 -> 更新参数或状态 -> 独立验证 -> 定位首个分叉”保存张量形状、目标值、更新状态和验证风险，选择结束后测试集只使用一次。
</Answer>

2. 如何判断复杂深度模型相对简单基线的提升来自机制而非数值、预算或抽样偶然？

<Answer>
  保持数据折分、初始化、训练轮数与资源预算可比，只替换一个结构、目标、采样或正则条件；先预测中间量变化，再比较多折或多种子的验证分布。若首个结构化变化不符合方程，先修复实现，不采信最终分数。
</Answer>

3. 为“每层空间尺寸、通道数、填充、步幅和参数共享必须可计算；前向与反向索引约定不一致时立即停止。”设计一个失败样本，并说明应在何处停止。

<Answer>
  可构造线性不可分输入、错误卷积边界、未混合采样链、恒等复制、自身统计泄漏、训练推理模式错配或历史API语义漂移。预测它最先破坏张量、概率、梯度、重构约束或验证合同的位置，运行到首个不一致立即停止，下游结果全部作废。
</Answer>

</Exercises>

<Attribution
  adaptedFrom="山下隆义著、张弥译《图解深度学习》，人民邮电出版社，2018，214页，ISBN 9787115480248；原书《イラストで学ぶ ディープラーニング》，讲谈社，2016，215页，ISBN 9784061538252 · 基于讲谈社官方8章51个编号小节完整目录与中文版书目信息的独立教学重构"
  adaptedUrl="https://www.kspub.co.jp/book/detail/1538252.html"
/>
