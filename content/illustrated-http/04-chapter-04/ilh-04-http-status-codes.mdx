---
title: "第4章 返回结果的HTTP状态码"
description: "按2XX成功、3XX重定向、4XX客户端错误和5XX服务器错误解释原书列出的14个代表状态码"
type: C
section: "图解HTTP（2014年首版 / HTTP/1.1时代）"
order: 4
draft: false
math: false
sourceUrl: "https://www.ituring.com.cn/book/1229"
demo: true
---

import { Objectives } from "@/components/mdx/objectives";
import { Term } from "@/components/mdx/term";
import { Step, Stepper } from "@/components/mdx/stepper";
import { Callout } from "@/components/mdx/callout";
import { Answer, Exercises } from "@/components/mdx/exercises";
import { Glossary, GlossaryItem } from "@/components/mdx/glossary";
import { Attribution } from "@/components/mdx/attribution";

# 第4章 返回结果的HTTP状态码

本课程对应[日]上野宣《图解HTTP》，于均良译，人民邮电出版社/图灵教育，2014年4月首版，308页，双色印刷，ISBN 9787115351531；原书《HTTPの教科書》，ISBN 9784798126258。图灵官方资料标明出版日期为2014年4月22日、172张图解；正式目录为11章、202个节/小节节点。本页严格按该首版章次组织，不用后续规范替换原书语境。

<Objectives>

- 能解释“第4章 返回结果的HTTP状态码”全部正式节点，并把概念定位到真实请求、响应或应用状态。
- 能绘制客户端、中介、服务器之间的消息方向、主体边界和关键状态转移。
- 能设计单变量实验，验证“只看状态码和相关首部就能判断请求是否完成、是否需要新请求、责任在客户端还是服务器，以及主体是否存在”。
- 能写出并提交包含原始报文、失败反例、恢复条件和版次边界的独立证据包。

</Objectives>

## 从一条可证伪的HTTP交换开始

先预测：只按百位背分类而忽略304无响应主体、401认证挑战、206范围和重定向方法变化，会让客户端做出错误后续动作。把预测写成“请求输入、线上消息、接收方状态、响应输出、最终副作用”五列，再运行实验。若结果与预测不同，先修正模型，不要只截取一个状态码证明自己。

本章的主问题是：按2XX成功、3XX重定向、4XX客户端错误和5XX服务器错误解释原书列出的14个代表状态码。原书以图解建立直觉，本课程把图解升级为可操作轨迹：每个箭头必须注明方向、协议对象和完成点；每个安全结论必须附一条攻击或误配置反例。

验收不变量是：只看状态码和相关首部就能判断请求是否完成、是否需要新请求、责任在客户端还是服务器，以及主体是否存在。浏览器页面“看起来正常”只能说明某个终点出现，不能单独证明DNS、连接、TLS、缓存、认证或授权中的哪一段正确。

## 核心词汇与首版边界

<Term def="服务器对请求处理结果给出的三位数字分类标记">状态码</Term>、<Term def="2XX类，表示请求被正常接收、理解和处理，但主体与语义仍因具体代码而异">成功</Term>、<Term def="3XX类，要求客户端结合Location或缓存验证器采取附加动作">重定向</Term>、<Term def="4XX类，表示请求语法、认证、权限或目标资源存在问题">客户端错误</Term>、<Term def="5XX类，表示服务器在有效请求处理过程中失败或暂时不可用">服务器错误</Term>

这些词汇按2014年首版语义使用。SPDY、HTTP/2.0、X-XSS-Protection、P3P等保留出版时状态；HTTP/2最终规范、HTTP/3、JWT、OAuth、SameSite等后续技术只能作为另行标注的现代补充，不改变本书目录分母。


## 核心机制深读

### 状态码必须连同方法和首部读

200可能承载GET结果，也可能报告POST处理结果；204明确没有主体；206必须带范围信息。状态码只给处理类别，Location、WWW-Authenticate、Retry-After和表示首部才决定下一步。

动手试：先写下预期请求、响应与状态变化，再捕获一次正常交换和一次只改一个变量的失败交换。比较状态码、首部、主体、缓存或会话状态，确认结论来自证据而非浏览器表象。

### 重定向的关键差异

301表示永久迁移，302表示临时迁移；303明确要求改用GET读取另一个资源；307临时迁移但禁止擅自改变原方法；304不是普通重定向，而是条件GET/HEAD验证成功后继续使用本地缓存。

动手试：先写下预期请求、响应与状态变化，再捕获一次正常交换和一次只改一个变量的失败交换。比较状态码、首部、主体、缓存或会话状态，确认结论来自证据而非浏览器表象。

### 401与403不是同义词

401表示尚未提供可接受的认证凭据，响应应带WWW-Authenticate挑战；403表示服务器理解请求但拒绝授权，即使重新认证也未必允许。404则表示未找到目标，也可用于隐藏拒绝访问资源的存在。

动手试：先写下预期请求、响应与状态变化，再捕获一次正常交换和一次只改一个变量的失败交换。比较状态码、首部、主体、缓存或会话状态，确认结论来自证据而非浏览器表象。

### 500与503的运维含义

500表示处理请求时发生未预期内部错误；503表示因过载或维护暂时不能处理，适合配合Retry-After。重试前必须判断方法是否幂等，并设置上限和退避，不能看到5XX就无限重放POST。

动手试：先写下预期请求、响应与状态变化，再捕获一次正常交换和一次只改一个变量的失败交换。比较状态码、首部、主体、缓存或会话状态，确认结论来自证据而非浏览器表象。

## 原书目录逐节点重构

### 4.1 状态码告知从服务器端返回的请求结果

“状态码告知从服务器端返回的请求结果”位于本章主链“按2XX成功、3XX重定向、4XX客户端错误和5XX服务器错误解释原书列出的14个代表状态码”中；需要说明它接收什么输入、改变哪份协议或应用状态、产生什么可观察输出。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“状态码告知从服务器端返回的请求结果”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

### 4.2 2XX成功

这是“2XX成功”这一组语义的分类入口；学习时按方向、作用域、前置条件和失败结果比较组内成员。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“2XX成功”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 4.2.1 200 OK

请求被正常处理；响应是否带主体取决于方法，GET通常返回资源表示。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“200 OK”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 4.2.2 204 No Content

请求成功但响应不得含报文主体，客户端保留当前文档视图。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“204 No Content”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 4.2.3 206 Partial Content

范围请求成功，只返回Content-Range声明的部分表示。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“206 Partial Content”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

### 4.3 3XX重定向

这是“3XX重定向”这一组语义的分类入口；学习时按方向、作用域、前置条件和失败结果比较组内成员。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“3XX重定向”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 4.3.1 301 Moved Permanently

资源已永久迁移，Location给出新URI，客户端和缓存可更新引用。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“301 Moved Permanently”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 4.3.2 302 Found

资源临时位于另一URI；历史客户端可能把POST后续请求改为GET。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“302 Found”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 4.3.3 303 See Other

要求客户端用GET访问Location，适合提交后转向结果资源。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“303 See Other”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 4.3.4 304 Not Modified

条件GET或HEAD的验证器匹配，复用已有缓存表示且不返回普通消息主体。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“304 Not Modified”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 4.3.5 307 Temporary Redirect

临时重定向并保持原请求方法与主体，不得擅自改成GET。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“307 Temporary Redirect”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

### 4.4 4XX客户端错误

这是“4XX客户端错误”这一组语义的分类入口；学习时按方向、作用域、前置条件和失败结果比较组内成员。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“4XX客户端错误”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 4.4.1 400 Bad Request

服务器无法按HTTP语法或请求结构理解消息。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“400 Bad Request”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 4.4.2 401 Unauthorized

缺少或未通过认证，响应以WWW-Authenticate给出挑战。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“401 Unauthorized”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 4.4.3 403 Forbidden

服务器理解请求但拒绝执行，重新认证也未必改变授权结果。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“403 Forbidden”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 4.4.4 404 Not Found

服务器未找到目标资源，也可用于不披露被拒绝资源的存在。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“404 Not Found”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

### 4.5 5XX服务器错误

这是“5XX服务器错误”这一组语义的分类入口；学习时按方向、作用域、前置条件和失败结果比较组内成员。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“5XX服务器错误”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 4.5.1 500 Internal Server Error

服务器处理有效请求时发生未预期内部故障。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“500 Internal Server Error”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 4.5.2 503 Service Unavailable

服务器因过载或维护暂时不可用，可用Retry-After说明等待时间。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“503 Service Unavailable”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

## 本页完整节点清单

本页承担19个目录或复习节点，正文、图解、实验和题目必须能反向定位每一项：

- 4.1 状态码告知从服务器端返回的请求结果
- 4.2 2XX成功
- 4.2.1 200 OK
- 4.2.2 204 No Content
- 4.2.3 206 Partial Content
- 4.3 3XX重定向
- 4.3.1 301 Moved Permanently
- 4.3.2 302 Found
- 4.3.3 303 See Other
- 4.3.4 304 Not Modified
- 4.3.5 307 Temporary Redirect
- 4.4 4XX客户端错误
- 4.4.1 400 Bad Request
- 4.4.2 401 Unauthorized
- 4.4.3 403 Forbidden
- 4.4.4 404 Not Found
- 4.5 5XX服务器错误
- 4.5.1 500 Internal Server Error
- 4.5.2 503 Service Unavailable

<Stepper>
  <Step title="复原正常协议轨迹">
    固定URI、客户端、网络和服务端状态，标出请求行、首部、主体、中介与最终响应。
  </Step>
  <Step title="只改变一个变量">
    只改方法、条件首部、表示大小、连接状态、Cookie或输入上下文中的一个，先写预测再执行。
    </Step>
  <Step title="关闭独立证据门">
    对照正常与失败交换，核对线上字节、协议状态、业务副作用、恢复步骤和首版边界。
    </Step>
</Stepper>

## 可复现实验

```text
HTTP/1.1 204 No Content
Date: Tue, 16 Jul 2026 08:00:00 GMT

(no message body)
```

```text
HTTP/1.1 303 See Other
Location: /orders/42

next request: GET /orders/42
```

```text
HTTP/1.1 401 Unauthorized
WWW-Authenticate: Basic realm="members"
```

动手试：使用浏览器开发者工具、curl或抓包工具保存同一资源的正常、条件命中、条件失配和无效输入四次交换。记录方法、目标、状态码、关键首部、主体长度、连接复用、缓存/Session状态与最终副作用。

<Callout type="trap" title="把现代规范倒灌进首版">
  原书写作于HTTP/2仍在制定的时期。后来的协议、认证和浏览器安全机制可以比较，但必须明确标为后续变化，不能替换正式节点。
</Callout>

<Callout type="trap" title="把收到响应当成业务正确">
  200只说明服务器选择了成功响应；它不能证明缓存隔离、对象授权、会话轮换、内容类型或输出转义正确。必须继续核对状态与副作用。
</Callout>

## 独立证据门

最小证据包包含：首版目录节点、请求与响应原文、连接/中介方向、主体边界、缓存或会话状态、一个单变量失败、最终副作用、已知限制、恢复步骤、责任人与复核人。

## 练习

<Exercises>

**问题 1：为什么“第4章 返回结果的HTTP状态码”必须固定2014年首版语境？**

<Answer>
  因为本书记录的是HTTP/1.1主流、SPDY与HTTP/2仍在演进的技术快照。固定版次才能让目录、术语、图解和实验结论一一对应，避免以后来的规范覆盖作者原本要解释的问题。
</Answer>

**问题 2：怎样构造“只按百位背分类而忽略304无响应主体、401认证挑战、206范围和重定向方法变化，会让客户端做出错误后续动作”的最小反例？**

<Answer>
  固定URI、客户端、网络、服务器数据和操作，只改变一个方法、首部、主体、连接、Cookie或输入上下文；保存完整请求响应，比较状态、缓存/会话和最终副作用。
</Answer>

**问题 3：何时可以认为本页完成独立交接？**

<Answer>
  另一位读者无需口头补充即可定位全部19个节点，复现正常与失败交换，并依据“只看状态码和相关首部就能判断请求是否完成、是否需要新请求、责任在客户端还是服务器，以及主体是否存在”得到相同结论。
</Answer>

</Exercises>

## 本章回顾

“第4章 返回结果的HTTP状态码”的核心是按2XX成功、3XX重定向、4XX客户端错误和5XX服务器错误解释原书列出的14个代表状态码。真正掌握不止是认出术语，而是能让一条请求在消息、连接、中介、表示、身份和攻击边界之间闭环，并用失败实验说明边界一旦被破坏会发生什么。

<Glossary>
  <GlossaryItem term="状态码">
    服务器对请求处理结果给出的三位数字分类标记。掌握标准是能在原始请求或响应中定位它，并构造一条最小反例。
  </GlossaryItem>
  <GlossaryItem term="成功">
    2XX类，表示请求被正常接收、理解和处理，但主体与语义仍因具体代码而异。掌握标准是能在原始请求或响应中定位它，并构造一条最小反例。
  </GlossaryItem>
  <GlossaryItem term="重定向">
    3XX类，要求客户端结合Location或缓存验证器采取附加动作。掌握标准是能在原始请求或响应中定位它，并构造一条最小反例。
  </GlossaryItem>
  <GlossaryItem term="客户端错误">
    4XX类，表示请求语法、认证、权限或目标资源存在问题。掌握标准是能在原始请求或响应中定位它，并构造一条最小反例。
  </GlossaryItem>
  <GlossaryItem term="服务器错误">
    5XX类，表示服务器在有效请求处理过程中失败或暂时不可用。掌握标准是能在原始请求或响应中定位它，并构造一条最小反例。
  </GlossaryItem>
</Glossary>

[← 上一页：第3章 HTTP报文内的HTTP信息](/learn/illustrated-http/03-chapter-03/ilh-03-http-message-information) · [下一页：第5章 与HTTP协作的Web服务器 →](/learn/illustrated-http/05-chapter-05/ilh-05-web-servers-cooperation)

<Attribution
  adaptedFrom="上野宣《图解HTTP》2014年4月首版"
  adaptedUrl="https://www.ituring.com.cn/book/1229"
/>
