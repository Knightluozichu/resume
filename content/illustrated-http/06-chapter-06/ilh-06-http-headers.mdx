---
title: "第6章 HTTP首部"
description: "系统掌握HTTP/1.1通用、请求、响应、实体、Cookie及原书列出的扩展首部字段"
type: C
section: "图解HTTP（2014年首版 / HTTP/1.1时代）"
order: 6
draft: false
math: false
sourceUrl: "https://www.ituring.com.cn/book/1229"
demo: true
---

import {
  Ilh06HttpHeadersFlowLab,
  Ilh06HttpHeadersExperimentLab,
  Ilh06HttpHeadersEvidenceLab,
} from "@/components/mdx/illustrated-http/diagrams/ilh-06-http-headers";

# 第6章 HTTP首部

本课程对应[日]上野宣《图解HTTP》，于均良译，人民邮电出版社/图灵教育，2014年4月首版，308页，双色印刷，ISBN 9787115351531；原书《HTTPの教科書》，ISBN 9784798126258。图灵官方资料标明出版日期为2014年4月22日、172张图解；正式目录为11章、202个节/小节节点。本页严格按该首版章次组织，不用后续规范替换原书语境。

<Objectives>

- 能解释“第6章 HTTP首部”全部正式节点，并把概念定位到真实请求、响应或应用状态。
- 能绘制客户端、中介、服务器之间的消息方向、主体边界和关键状态转移。
- 能设计单变量实验，验证“面对原始报文能按端到端/逐跳和通用/请求/响应/实体分类解释每个字段，并用条件请求与缓存键验证组合语义”。
- 能写出并提交包含原始报文、失败反例、恢复条件和版次边界的独立证据包。

</Objectives>

## 从一条可证伪的HTTP交换开始

先预测：孤立背字段名、不区分请求方向与逐跳边界，或把同名现代语义倒灌，会造成缓存泄漏、条件更新覆盖和代理转发错误。把预测写成“请求输入、线上消息、接收方状态、响应输出、最终副作用”五列，再运行实验。若结果与预测不同，先修正模型，不要只截取一个状态码证明自己。

本章的主问题是：系统掌握HTTP/1.1通用、请求、响应、实体、Cookie及原书列出的扩展首部字段。原书以图解建立直觉，本课程把图解升级为可操作轨迹：每个箭头必须注明方向、协议对象和完成点；每个安全结论必须附一条攻击或误配置反例。

验收不变量是：面对原始报文能按端到端/逐跳和通用/请求/响应/实体分类解释每个字段，并用条件请求与缓存键验证组合语义。浏览器页面“看起来正常”只能说明某个终点出现，不能单独证明DNS、连接、TLS、缓存、认证或授权中的哪一段正确。

## 核心词汇与首版边界

<Term def="必须转发到最终接收者并由端点解释的首部">端到端首部</Term>、<Term def="只在相邻两节点间有效，不应由代理继续转发的首部">逐跳首部</Term>、<Term def="用If-Match、If-None-Match等验证器约束方法执行的请求">条件请求</Term>、<Term def="描述报文所携带表示的媒体类型、长度、编码、语言和验证时间等元数据">实体首部</Term>、<Term def="服务器下发Set-Cookie、客户端回送Cookie形成的状态携带机制">Cookie首部</Term>

这些词汇按2014年首版语义使用。SPDY、HTTP/2.0、X-XSS-Protection、P3P等保留出版时状态；HTTP/2最终规范、HTTP/3、JWT、OAuth、SameSite等后续技术只能作为另行标注的现代补充，不改变本书目录分母。

<Ilh06HttpHeadersFlowLab />

## 核心机制深读

### 先按方向和作用域分类

通用首部可出现在请求或响应；请求首部补充客户端、目标与条件；响应首部补充服务器结果；实体首部描述表示。Connection、Transfer-Encoding等逐跳字段只能作用于一段连接，代理转发前必须处理。

动手试：先写下预期请求、响应与状态变化，再捕获一次正常交换和一次只改一个变量的失败交换。比较状态码、首部、主体、缓存或会话状态，确认结论来自证据而非浏览器表象。

### 缓存控制是一组约束

Cache-Control在请求和响应中使用不同指令组合控制可缓存性、新鲜度、重验证与共享范围。Age表示共享缓存中估算的驻留时间，Expires给绝对失效时间，ETag与Last-Modified提供验证器，Vary扩展缓存键。

动手试：先写下预期请求、响应与状态变化，再捕获一次正常交换和一次只改一个变量的失败交换。比较状态码、首部、主体、缓存或会话状态，确认结论来自证据而非浏览器表象。

### 条件请求防止浪费与覆盖

If-None-Match用于GET/HEAD重验证可得到304，也可用星号防止意外覆盖已有资源；If-Match用于仅在实体标签匹配时更新；If-Range验证成功才发送范围，否则返回完整表示。失败通常是412而不是盲目执行。

动手试：先写下预期请求、响应与状态变化，再捕获一次正常交换和一次只改一个变量的失败交换。比较状态码、首部、主体、缓存或会话状态，确认结论来自证据而非浏览器表象。

### 首部是协议状态机的输入

Content-Length决定定长主体，Transfer-Encoding决定逐跳编码，Content-Encoding说明表示编码，Content-Type说明媒体类型。若边界字段冲突，接收方不能凭猜测拼接，否则会出现请求走私等解析差异风险。

动手试：先写下预期请求、响应与状态变化，再捕获一次正常交换和一次只改一个变量的失败交换。比较状态码、首部、主体、缓存或会话状态，确认结论来自证据而非浏览器表象。

### 历史扩展保留时代说明

X-Frame-Options用于限制页面被框架嵌入，X-XSS-Protection控制当时浏览器的XSS过滤器，DNT表达不跟踪偏好，P3P描述隐私策略。课程按2014年首版解释其历史角色，不宣称它们是今天充分的安全方案。

动手试：先写下预期请求、响应与状态变化，再捕获一次正常交换和一次只改一个变量的失败交换。比较状态码、首部、主体、缓存或会话状态，确认结论来自证据而非浏览器表象。

## 原书目录逐节点重构

### 6.1 HTTP报文首部

这是“HTTP报文首部”这一组语义的分类入口；学习时按方向、作用域、前置条件和失败结果比较组内成员。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“HTTP报文首部”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

### 6.2 HTTP首部字段

这是“HTTP首部字段”这一组语义的分类入口；学习时按方向、作用域、前置条件和失败结果比较组内成员。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“HTTP首部字段”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 6.2.1 HTTP首部字段传递重要信息

“HTTP首部字段传递重要信息”位于本章主链“系统掌握HTTP/1.1通用、请求、响应、实体、Cookie及原书列出的扩展首部字段”中；需要说明它接收什么输入、改变哪份协议或应用状态、产生什么可观察输出。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“HTTP首部字段传递重要信息”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 6.2.2 HTTP首部字段结构

“HTTP首部字段结构”位于本章主链“系统掌握HTTP/1.1通用、请求、响应、实体、Cookie及原书列出的扩展首部字段”中；需要说明它接收什么输入、改变哪份协议或应用状态、产生什么可观察输出。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“HTTP首部字段结构”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 6.2.3 4种HTTP首部字段类型

“4种HTTP首部字段类型”位于本章主链“系统掌握HTTP/1.1通用、请求、响应、实体、Cookie及原书列出的扩展首部字段”中；需要说明它接收什么输入、改变哪份协议或应用状态、产生什么可观察输出。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“4种HTTP首部字段类型”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 6.2.4 HTTP/1.1首部字段一览

“HTTP/1.1首部字段一览”位于本章主链“系统掌握HTTP/1.1通用、请求、响应、实体、Cookie及原书列出的扩展首部字段”中；需要说明它接收什么输入、改变哪份协议或应用状态、产生什么可观察输出。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“HTTP/1.1首部字段一览”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 6.2.5 非HTTP/1.1首部字段

这是“非HTTP/1.1首部字段”这一组语义的分类入口；学习时按方向、作用域、前置条件和失败结果比较组内成员。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“非HTTP/1.1首部字段”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 6.2.6 End-to-end首部和Hop-by-hop首部

这是“End-to-end首部和Hop-by-hop首部”这一组语义的分类入口；学习时按方向、作用域、前置条件和失败结果比较组内成员。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“End-to-end首部和Hop-by-hop首部”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 6.3 HTTP/1.1通用首部字段

这是“HTTP/1.1通用首部字段”这一组语义的分类入口；学习时按方向、作用域、前置条件和失败结果比较组内成员。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“HTTP/1.1通用首部字段”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 6.3.1 Cache-Control

以指令组合控制缓存可存储性、新鲜度、重验证和共享范围。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“Cache-Control”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 6.3.2 Connection

声明当前连接选项并列出逐跳字段；close表示本次交换后关闭连接。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“Connection”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 6.3.3 Date

记录报文产生日期时间，用于缓存年龄与时序判断。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“Date”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 6.3.4 Pragma

兼容HTTP/1.0的通用字段，常见Pragma: no-cache不等同完整Cache-Control语义。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“Pragma”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 6.3.5 Trailer

预告分块主体结束后会出现哪些尾部字段。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“Trailer”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 6.3.6 Transfer-Encoding

声明逐跳传输编码，chunked以块边界传输未知总长度主体。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“Transfer-Encoding”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 6.3.7 Upgrade

提出切换到另一协议，需Connection: Upgrade并由101响应接受。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“Upgrade”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 6.3.8 Via

记录请求或响应经过的代理/网关及协议版本，支持追踪和环路检测。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“Via”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 6.3.9 Warning

携带与缓存或表示转换相关的警告信息。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“Warning”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

### 6.4 请求首部字段

这是“请求首部字段”这一组语义的分类入口；学习时按方向、作用域、前置条件和失败结果比较组内成员。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“请求首部字段”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 6.4.1 Accept

列出客户端可处理的媒体类型及质量偏好。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“Accept”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 6.4.2 Accept-Charset

列出可接受字符集及质量偏好。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“Accept-Charset”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 6.4.3 Accept-Encoding

列出可接受内容编码，如gzip。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“Accept-Encoding”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 6.4.4 Accept-Language

列出偏好的自然语言标签。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“Accept-Language”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 6.4.5 Authorization

携带向源服务器证明身份的认证凭据。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“Authorization”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 6.4.6 Expect

声明服务器需满足的预期，典型值100-continue用于发送大主体前确认。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“Expect”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 6.4.7 From

给出控制用户代理的用户邮箱，实际使用很少且涉及隐私。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“From”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 6.4.8 Host

指定目标主机与可选端口，是HTTP/1.1虚拟主机路由的必要字段。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“Host”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 6.4.9 If-Match

仅当当前实体标签与给定强验证器匹配时执行方法，否则返回412。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“If-Match”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 6.4.10 If-Modified-Since

仅在资源晚于给定时间修改时返回表示，否则GET/HEAD可返回304。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“If-Modified-Since”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 6.4.11 If-None-Match

仅当当前实体标签均不匹配时执行；GET/HEAD匹配返回304，其他方法通常返回412。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“If-None-Match”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 6.4.12 If-Range

验证器匹配才返回指定范围，否则忽略Range并返回完整表示。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“If-Range”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 6.4.13 If-Unmodified-Since

仅当资源自给定时间后未修改时执行，避免覆盖新版本。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“If-Unmodified-Since”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 6.4.14 Max-Forwards

限制TRACE或OPTIONS还能经过的中介跳数，每跳递减。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“Max-Forwards”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 6.4.15 Proxy-Authorization

携带给代理服务器的认证凭据，不是给源服务器。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“Proxy-Authorization”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 6.4.16 Range

请求表示的一个或多个字节范围，用于断点续传和部分读取。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“Range”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 6.4.17 Referer

给出发起请求页面的URI；字段名保留了历史拼写错误，并可能受隐私策略裁剪。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“Referer”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 6.4.18 TE

声明可接受的传输编码或trailers能力，只作用于当前连接。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“TE”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 6.4.19 User-Agent

标识发起请求的客户端产品及版本信息。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“User-Agent”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

### 6.5 响应首部字段

这是“响应首部字段”这一组语义的分类入口；学习时按方向、作用域、前置条件和失败结果比较组内成员。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“响应首部字段”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 6.5.1 Accept-Ranges

声明服务器是否支持范围请求及范围单位，常见bytes或none。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“Accept-Ranges”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 6.5.2 Age

估算响应在共享缓存中自产生或重验证后经过的秒数。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“Age”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 6.5.3 ETag

表示当前资源版本的实体标签，用于缓存验证和并发条件。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“ETag”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 6.5.4 Location

给出重定向目标或新建资源的URI。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“Location”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 6.5.5 Proxy-Authenticate

由代理发出认证挑战，配合407响应。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“Proxy-Authenticate”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 6.5.6 Retry-After

以日期或秒数告知客户端何时可重试，常见于503或重定向。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“Retry-After”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 6.5.7 Server

标识源服务器软件信息；过度披露会增加指纹信息。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“Server”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 6.5.8 Vary

声明哪些请求首部参与表示选择，缓存必须把它们纳入缓存键。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“Vary”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 6.5.9 WWW-Authenticate

由源服务器发出认证方案、realm和参数挑战。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“WWW-Authenticate”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

### 6.6 实体首部字段

这是“实体首部字段”这一组语义的分类入口；学习时按方向、作用域、前置条件和失败结果比较组内成员。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“实体首部字段”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 6.6.1 Allow

列出目标资源支持的方法，常见于OPTIONS或405响应。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“Allow”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 6.6.2 Content-Encoding

声明表示内容应用的编码，如gzip，接收方解码后才得到原实体。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“Content-Encoding”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 6.6.3 Content-Language

描述表示面向的自然语言。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“Content-Language”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 6.6.4 Content-Length

给出报文主体的八位字节长度；与分块传输边界不能冲突使用。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“Content-Length”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 6.6.5 Content-Location

给出报文实体对应的具体资源URI。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“Content-Location”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 6.6.6 Content-MD5

以Base64形式携带实体MD5值用于偶然损坏检查；其密码学安全性已不足。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“Content-MD5”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 6.6.7 Content-Range

声明部分表示的单位、起止位置和完整长度。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“Content-Range”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 6.6.8 Content-Type

声明实体媒体类型及可选字符集，决定接收方如何解释主体。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“Content-Type”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 6.6.9 Expires

给出响应转为陈旧的绝对日期，受时钟影响并可被Cache-Control覆盖。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“Expires”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 6.6.10 Last-Modified

记录源服务器认定的资源最后修改时间，可作弱时间验证器。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“Last-Modified”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

### 6.7 为Cookie服务的首部字段

这是“为Cookie服务的首部字段”这一组语义的分类入口；学习时按方向、作用域、前置条件和失败结果比较组内成员。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“为Cookie服务的首部字段”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 6.7.1 Set-Cookie

服务器下发Cookie及Expires、Path、Domain、Secure、HttpOnly等属性。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“Set-Cookie”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 6.7.2 Cookie

客户端把与目标域和路径匹配的Cookie名称值对回送给服务器。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“Cookie”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

### 6.8 其他首部字段

这是“其他首部字段”这一组语义的分类入口；学习时按方向、作用域、前置条件和失败结果比较组内成员。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“其他首部字段”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 6.8.1 X-Frame-Options

以DENY或SAMEORIGIN限制页面被frame嵌入，降低点击劫持风险。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“X-Frame-Options”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 6.8.2 X-XSS-Protection

控制当时浏览器内建XSS过滤器的历史字段，不能替代输出编码。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“X-XSS-Protection”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 6.8.3 DNT

表达用户不希望被跟踪的偏好，但执行取决于服务方约定。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“DNT”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 6.8.4 P3P

以机器可读隐私策略描述站点数据实践的历史规范。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“P3P”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

## 本页完整节点清单

本页承担67个目录或复习节点，正文、图解、实验和题目必须能反向定位每一项：

- 6.1 HTTP报文首部
- 6.2 HTTP首部字段
- 6.2.1 HTTP首部字段传递重要信息
- 6.2.2 HTTP首部字段结构
- 6.2.3 4种HTTP首部字段类型
- 6.2.4 HTTP/1.1首部字段一览
- 6.2.5 非HTTP/1.1首部字段
- 6.2.6 End-to-end首部和Hop-by-hop首部
- 6.3 HTTP/1.1通用首部字段
- 6.3.1 Cache-Control
- 6.3.2 Connection
- 6.3.3 Date
- 6.3.4 Pragma
- 6.3.5 Trailer
- 6.3.6 Transfer-Encoding
- 6.3.7 Upgrade
- 6.3.8 Via
- 6.3.9 Warning
- 6.4 请求首部字段
- 6.4.1 Accept
- 6.4.2 Accept-Charset
- 6.4.3 Accept-Encoding
- 6.4.4 Accept-Language
- 6.4.5 Authorization
- 6.4.6 Expect
- 6.4.7 From
- 6.4.8 Host
- 6.4.9 If-Match
- 6.4.10 If-Modified-Since
- 6.4.11 If-None-Match
- 6.4.12 If-Range
- 6.4.13 If-Unmodified-Since
- 6.4.14 Max-Forwards
- 6.4.15 Proxy-Authorization
- 6.4.16 Range
- 6.4.17 Referer
- 6.4.18 TE
- 6.4.19 User-Agent
- 6.5 响应首部字段
- 6.5.1 Accept-Ranges
- 6.5.2 Age
- 6.5.3 ETag
- 6.5.4 Location
- 6.5.5 Proxy-Authenticate
- 6.5.6 Retry-After
- 6.5.7 Server
- 6.5.8 Vary
- 6.5.9 WWW-Authenticate
- 6.6 实体首部字段
- 6.6.1 Allow
- 6.6.2 Content-Encoding
- 6.6.3 Content-Language
- 6.6.4 Content-Length
- 6.6.5 Content-Location
- 6.6.6 Content-MD5
- 6.6.7 Content-Range
- 6.6.8 Content-Type
- 6.6.9 Expires
- 6.6.10 Last-Modified
- 6.7 为Cookie服务的首部字段
- 6.7.1 Set-Cookie
- 6.7.2 Cookie
- 6.8 其他首部字段
- 6.8.1 X-Frame-Options
- 6.8.2 X-XSS-Protection
- 6.8.3 DNT
- 6.8.4 P3P

<Stepper>
  <Step title="复原正常协议轨迹">
    固定URI、客户端、网络和服务端状态，标出请求行、首部、主体、中介与最终响应。
    <Ilh06HttpHeadersFlowLab />
  </Step>
  <Step title="只改变一个变量">
    只改方法、条件首部、表示大小、连接状态、Cookie或输入上下文中的一个，先写预测再执行。
    <Ilh06HttpHeadersExperimentLab />
  </Step>
  <Step title="关闭独立证据门">
    对照正常与失败交换，核对线上字节、协议状态、业务副作用、恢复步骤和首版边界。
    <Ilh06HttpHeadersEvidenceLab />
  </Step>
</Stepper>

## 可复现实验

```text
GET /manual HTTP/1.1
Host: www.example.test
Accept: text/html
Accept-Encoding: gzip
If-None-Match: "v7"
```

```text
HTTP/1.1 200 OK
Date: Tue, 16 Jul 2026 08:00:00 GMT
Content-Type: text/html; charset=utf-8
Content-Encoding: gzip
Content-Length: 842
ETag: "v8"
Vary: Accept-Encoding
```

```text
PUT /item/42 HTTP/1.1
If-Match: "v8"
Content-Type: application/json

{"name":"new"}
```

动手试：使用浏览器开发者工具、curl或抓包工具保存同一资源的正常、条件命中、条件失配和无效输入四次交换。记录方法、目标、状态码、关键首部、主体长度、连接复用、缓存/Session状态与最终副作用。

<Callout type="trap" title="把现代规范倒灌进首版">
  原书写作于HTTP/2仍在制定的时期。后来的协议、认证和浏览器安全机制可以比较，但必须明确标为后续变化，不能替换正式节点。
</Callout>

<Callout type="trap" title="把收到响应当成业务正确">
  200只说明服务器选择了成功响应；它不能证明缓存隔离、对象授权、会话轮换、内容类型或输出转义正确。必须继续核对状态与副作用。
</Callout>

## 独立证据门

<Ilh06HttpHeadersEvidenceLab />

最小证据包包含：首版目录节点、请求与响应原文、连接/中介方向、主体边界、缓存或会话状态、一个单变量失败、最终副作用、已知限制、恢复步骤、责任人与复核人。

## 练习

<Exercises>

**问题 1：为什么“第6章 HTTP首部”必须固定2014年首版语境？**

<Answer>
  因为本书记录的是HTTP/1.1主流、SPDY与HTTP/2仍在演进的技术快照。固定版次才能让目录、术语、图解和实验结论一一对应，避免以后来的规范覆盖作者原本要解释的问题。
</Answer>

**问题 2：怎样构造“孤立背字段名、不区分请求方向与逐跳边界，或把同名现代语义倒灌，会造成缓存泄漏、条件更新覆盖和代理转发错误”的最小反例？**

<Answer>
  固定URI、客户端、网络、服务器数据和操作，只改变一个方法、首部、主体、连接、Cookie或输入上下文；保存完整请求响应，比较状态、缓存/会话和最终副作用。
</Answer>

**问题 3：何时可以认为本页完成独立交接？**

<Answer>
  另一位读者无需口头补充即可定位全部67个节点，复现正常与失败交换，并依据“面对原始报文能按端到端/逐跳和通用/请求/响应/实体分类解释每个字段，并用条件请求与缓存键验证组合语义”得到相同结论。
</Answer>

</Exercises>

## 本章回顾

“第6章 HTTP首部”的核心是系统掌握HTTP/1.1通用、请求、响应、实体、Cookie及原书列出的扩展首部字段。真正掌握不止是认出术语，而是能让一条请求在消息、连接、中介、表示、身份和攻击边界之间闭环，并用失败实验说明边界一旦被破坏会发生什么。

<Glossary>
  <GlossaryItem term="端到端首部">
    必须转发到最终接收者并由端点解释的首部。掌握标准是能在原始请求或响应中定位它，并构造一条最小反例。
  </GlossaryItem>
  <GlossaryItem term="逐跳首部">
    只在相邻两节点间有效，不应由代理继续转发的首部。掌握标准是能在原始请求或响应中定位它，并构造一条最小反例。
  </GlossaryItem>
  <GlossaryItem term="条件请求">
    用If-Match、If-None-Match等验证器约束方法执行的请求。掌握标准是能在原始请求或响应中定位它，并构造一条最小反例。
  </GlossaryItem>
  <GlossaryItem term="实体首部">
    描述报文所携带表示的媒体类型、长度、编码、语言和验证时间等元数据。掌握标准是能在原始请求或响应中定位它，并构造一条最小反例。
  </GlossaryItem>
  <GlossaryItem term="Cookie首部">
    服务器下发Set-Cookie、客户端回送Cookie形成的状态携带机制。掌握标准是能在原始请求或响应中定位它，并构造一条最小反例。
  </GlossaryItem>
</Glossary>

[← 上一页：第5章 与HTTP协作的Web服务器](/learn/illustrated-http/05-chapter-05/ilh-05-web-servers-cooperation) · [下一页：第7章 确保Web安全的HTTPS →](/learn/illustrated-http/07-chapter-07/ilh-07-https-security)

<Attribution
  adaptedFrom="上野宣《图解HTTP》2014年4月首版"
  adaptedUrl="https://www.ituring.com.cn/book/1229"
/>
