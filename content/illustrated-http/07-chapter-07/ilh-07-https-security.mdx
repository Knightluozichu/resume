---
title: "第7章 确保Web安全的HTTPS"
description: "从HTTP明文、身份不验证和完整性不可证三个缺点，推导SSL/TLS、混合密码与证书链的HTTPS通信机制"
type: C
section: "图解HTTP（2014年首版 / HTTP/1.1时代）"
order: 7
draft: false
math: false
sourceUrl: "https://www.ituring.com.cn/book/1229"
demo: true
---

import { Objectives } from "@/components/mdx/objectives";
import { Term } from "@/components/mdx/term";
import { Step, Stepper } from "@/components/mdx/stepper";
import { Callout } from "@/components/mdx/callout";
import { Answer, Exercises } from "@/components/mdx/exercises";
import { Glossary, GlossaryItem } from "@/components/mdx/glossary";
import { Attribution } from "@/components/mdx/attribution";

# 第7章 确保Web安全的HTTPS

本课程对应[日]上野宣《图解HTTP》，于均良译，人民邮电出版社/图灵教育，2014年4月首版，308页，双色印刷，ISBN 9787115351531；原书《HTTPの教科書》，ISBN 9784798126258。图灵官方资料标明出版日期为2014年4月22日、172张图解；正式目录为11章、202个节/小节节点。本页严格按该首版章次组织，不用后续规范替换原书语境。

<Objectives>

- 能解释“第7章 确保Web安全的HTTPS”全部正式节点，并把概念定位到真实请求、响应或应用状态。
- 能绘制客户端、中介、服务器之间的消息方向、主体边界和关键状态转移。
- 能设计单变量实验，验证“能把一次HTTPS连接拆成证书验证、密钥协商、对称加密记录与完整性检查，并指出每一步阻断哪类攻击”。
- 能写出并提交包含原始报文、失败反例、恢复条件和版次边界的独立证据包。

</Objectives>

## 从一条可证伪的HTTP交换开始

先预测：把HTTPS简化成用公钥加密全部网页，或只看到锁图标就跳过主机名、有效期和信任链验证，会留下中间人路径。把预测写成“请求输入、线上消息、接收方状态、响应输出、最终副作用”五列，再运行实验。若结果与预测不同，先修正模型，不要只截取一个状态码证明自己。

本章的主问题是：从HTTP明文、身份不验证和完整性不可证三个缺点，推导SSL/TLS、混合密码与证书链的HTTPS通信机制。原书以图解建立直觉，本课程把图解升级为可操作轨迹：每个箭头必须注明方向、协议对象和完成点；每个安全结论必须附一条攻击或误配置反例。

验收不变量是：能把一次HTTPS连接拆成证书验证、密钥协商、对称加密记录与完整性检查，并指出每一步阻断哪类攻击。浏览器页面“看起来正常”只能说明某个终点出现，不能单独证明DNS、连接、TLS、缓存、认证或授权中的哪一段正确。

## 核心词汇与首版边界

<Term def="攻击者在传输路径读取明文通信内容的威胁">窃听</Term>、<Term def="通信对端身份未验证时，攻击者冒充服务器或客户端">伪装</Term>、<Term def="接收方能判断报文从发送后是否被篡改的性质">完整性</Term>、<Term def="使用公开密钥加密或验证、私有密钥解密或签名的非对称密码机制">公开密钥加密</Term>、<Term def="由可信证书机构签名，把主体身份与公开密钥绑定的凭据">数字证书</Term>

这些词汇按2014年首版语义使用。SPDY、HTTP/2.0、X-XSS-Protection、P3P等保留出版时状态；HTTP/2最终规范、HTTP/3、JWT、OAuth、SameSite等后续技术只能作为另行标注的现代补充，不改变本书目录分母。


## 核心机制深读

### 三个缺点对应三类证据

明文可被路径观察者读取；协议本身不确认对端身份；单靠收到相同长度的报文不能证明未被改写。加密提供机密性，证书与签名建立认证，消息认证机制检测篡改，三者不能互相替代。

动手试：先写下预期请求、响应与状态变化，再捕获一次正常交换和一次只改一个变量的失败交换。比较状态码、首部、主体、缓存或会话状态，确认结论来自证据而非浏览器表象。

### HTTPS仍然传HTTP

应用先构造普通HTTP消息，再交给SSL/TLS记录层保护，底下仍由TCP传输。TLS握手发生在HTTP请求前；握手失败时，应用层不应继续发送敏感HTTP数据。

动手试：先写下预期请求、响应与状态变化，再捕获一次正常交换和一次只改一个变量的失败交换。比较状态码、首部、主体、缓存或会话状态，确认结论来自证据而非浏览器表象。

### 混合密码解决效率和分发

共享密钥密码适合高吞吐数据但难以安全分发密钥；公开密钥密码便于协商和认证但计算昂贵。HTTPS在握手中使用非对称机制建立共享秘密，随后用对称会话密钥保护大量记录。

动手试：先写下预期请求、响应与状态变化，再捕获一次正常交换和一次只改一个变量的失败交换。比较状态码、首部、主体、缓存或会话状态，确认结论来自证据而非浏览器表象。

### 证书链不是一张图片

客户端验证签发链是否到达受信根、签名是否有效、证书是否在有效期、主机名是否匹配，并考虑吊销信息。任一步失败都不能用忽略警告代替安全结论。

动手试：先写下预期请求、响应与状态变化，再捕获一次正常交换和一次只改一个变量的失败交换。比较状态码、首部、主体、缓存或会话状态，确认结论来自证据而非浏览器表象。

## 原书目录逐节点重构

### 7.1 HTTP的缺点

明文、对端身份未验证和报文完整性不可证是HTTP本身的三项安全缺口。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“HTTP的缺点”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 7.1.1 通信使用明文可能会被窃听

“通信使用明文可能会被窃听”位于本章主链“从HTTP明文、身份不验证和完整性不可证三个缺点，推导SSL/TLS、混合密码与证书链的HTTPS通信机制”中；需要说明它接收什么输入、改变哪份协议或应用状态、产生什么可观察输出。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“通信使用明文可能会被窃听”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 7.1.2 不验证通信方的身份就可能遭遇伪装

“不验证通信方的身份就可能遭遇伪装”位于本章主链“从HTTP明文、身份不验证和完整性不可证三个缺点，推导SSL/TLS、混合密码与证书链的HTTPS通信机制”中；需要说明它接收什么输入、改变哪份协议或应用状态、产生什么可观察输出。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“不验证通信方的身份就可能遭遇伪装”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 7.1.3 无法证明报文完整性，可能已遭篡改

“无法证明报文完整性，可能已遭篡改”位于本章主链“从HTTP明文、身份不验证和完整性不可证三个缺点，推导SSL/TLS、混合密码与证书链的HTTPS通信机制”中；需要说明它接收什么输入、改变哪份协议或应用状态、产生什么可观察输出。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“无法证明报文完整性，可能已遭篡改”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

### 7.2 HTTP+加密+认证+完整性保护=HTTPS

“HTTP+加密+认证+完整性保护=HTTPS”位于本章主链“从HTTP明文、身份不验证和完整性不可证三个缺点，推导SSL/TLS、混合密码与证书链的HTTPS通信机制”中；需要说明它接收什么输入、改变哪份协议或应用状态、产生什么可观察输出。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“HTTP+加密+认证+完整性保护=HTTPS”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 7.2.1 HTTP加上加密处理和认证以及完整性保护后即是HTTPS

“HTTP加上加密处理和认证以及完整性保护后即是HTTPS”位于本章主链“从HTTP明文、身份不验证和完整性不可证三个缺点，推导SSL/TLS、混合密码与证书链的HTTPS通信机制”中；需要说明它接收什么输入、改变哪份协议或应用状态、产生什么可观察输出。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“HTTP加上加密处理和认证以及完整性保护后即是HTTPS”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 7.2.2 HTTPS是身披SSL外壳的HTTP

“HTTPS是身披SSL外壳的HTTP”位于本章主链“从HTTP明文、身份不验证和完整性不可证三个缺点，推导SSL/TLS、混合密码与证书链的HTTPS通信机制”中；需要说明它接收什么输入、改变哪份协议或应用状态、产生什么可观察输出。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“HTTPS是身披SSL外壳的HTTP”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 7.2.3 相互交换密钥的公开密钥加密技术

非对称机制解决共享秘密分发和身份认证，对称机制承担高效数据加密。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“相互交换密钥的公开密钥加密技术”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 7.2.4 证明公开密钥正确性的证书

CA签名把域名身份与公钥绑定，客户端沿信任链验证。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“证明公开密钥正确性的证书”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 7.2.5 HTTPS的安全通信机制

先握手协商与认证，再用会话密钥保护HTTP记录并校验完整性。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“HTTPS的安全通信机制”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

## 本页完整节点清单

本页承担10个目录或复习节点，正文、图解、实验和题目必须能反向定位每一项：

- 7.1 HTTP的缺点
- 7.1.1 通信使用明文可能会被窃听
- 7.1.2 不验证通信方的身份就可能遭遇伪装
- 7.1.3 无法证明报文完整性，可能已遭篡改
- 7.2 HTTP+加密+认证+完整性保护=HTTPS
- 7.2.1 HTTP加上加密处理和认证以及完整性保护后即是HTTPS
- 7.2.2 HTTPS是身披SSL外壳的HTTP
- 7.2.3 相互交换密钥的公开密钥加密技术
- 7.2.4 证明公开密钥正确性的证书
- 7.2.5 HTTPS的安全通信机制

<Stepper>
  <Step title="复原正常协议轨迹">
    固定URI、客户端、网络和服务端状态，标出请求行、首部、主体、中介与最终响应。
  </Step>
  <Step title="只改变一个变量">
    只改方法、条件首部、表示大小、连接状态、Cookie或输入上下文中的一个，先写预测再执行。
    </Step>
  <Step title="关闭独立证据门">
    对照正常与失败交换，核对线上字节、协议状态、业务副作用、恢复步骤和首版边界。
    </Step>
</Stepper>

## 可复现实验

```text
ClientHello -> ServerHello + Certificate
certificate validation -> key agreement
Finished <-> Finished
encrypted HTTP request <-> encrypted HTTP response
```

```text
server certificate: subject=www.example.test
SAN=DNS:www.example.test
issuer=Example Intermediate CA
validity=notBefore..notAfter
```

```text
HTTP plaintext -> TLS record protection -> TCP byte stream
TCP byte stream -> TLS verify/decrypt -> HTTP plaintext
```

动手试：使用浏览器开发者工具、curl或抓包工具保存同一资源的正常、条件命中、条件失配和无效输入四次交换。记录方法、目标、状态码、关键首部、主体长度、连接复用、缓存/Session状态与最终副作用。

<Callout type="trap" title="把现代规范倒灌进首版">
  原书写作于HTTP/2仍在制定的时期。后来的协议、认证和浏览器安全机制可以比较，但必须明确标为后续变化，不能替换正式节点。
</Callout>

<Callout type="trap" title="把收到响应当成业务正确">
  200只说明服务器选择了成功响应；它不能证明缓存隔离、对象授权、会话轮换、内容类型或输出转义正确。必须继续核对状态与副作用。
</Callout>

## 独立证据门

最小证据包包含：首版目录节点、请求与响应原文、连接/中介方向、主体边界、缓存或会话状态、一个单变量失败、最终副作用、已知限制、恢复步骤、责任人与复核人。

## 练习

<Exercises>

**问题 1：为什么“第7章 确保Web安全的HTTPS”必须固定2014年首版语境？**

<Answer>
  因为本书记录的是HTTP/1.1主流、SPDY与HTTP/2仍在演进的技术快照。固定版次才能让目录、术语、图解和实验结论一一对应，避免以后来的规范覆盖作者原本要解释的问题。
</Answer>

**问题 2：怎样构造“把HTTPS简化成用公钥加密全部网页，或只看到锁图标就跳过主机名、有效期和信任链验证，会留下中间人路径”的最小反例？**

<Answer>
  固定URI、客户端、网络、服务器数据和操作，只改变一个方法、首部、主体、连接、Cookie或输入上下文；保存完整请求响应，比较状态、缓存/会话和最终副作用。
</Answer>

**问题 3：何时可以认为本页完成独立交接？**

<Answer>
  另一位读者无需口头补充即可定位全部10个节点，复现正常与失败交换，并依据“能把一次HTTPS连接拆成证书验证、密钥协商、对称加密记录与完整性检查，并指出每一步阻断哪类攻击”得到相同结论。
</Answer>

</Exercises>

## 本章回顾

“第7章 确保Web安全的HTTPS”的核心是从HTTP明文、身份不验证和完整性不可证三个缺点，推导SSL/TLS、混合密码与证书链的HTTPS通信机制。真正掌握不止是认出术语，而是能让一条请求在消息、连接、中介、表示、身份和攻击边界之间闭环，并用失败实验说明边界一旦被破坏会发生什么。

<Glossary>
  <GlossaryItem term="窃听">
    攻击者在传输路径读取明文通信内容的威胁。掌握标准是能在原始请求或响应中定位它，并构造一条最小反例。
  </GlossaryItem>
  <GlossaryItem term="伪装">
    通信对端身份未验证时，攻击者冒充服务器或客户端。掌握标准是能在原始请求或响应中定位它，并构造一条最小反例。
  </GlossaryItem>
  <GlossaryItem term="完整性">
    接收方能判断报文从发送后是否被篡改的性质。掌握标准是能在原始请求或响应中定位它，并构造一条最小反例。
  </GlossaryItem>
  <GlossaryItem term="公开密钥加密">
    使用公开密钥加密或验证、私有密钥解密或签名的非对称密码机制。掌握标准是能在原始请求或响应中定位它，并构造一条最小反例。
  </GlossaryItem>
  <GlossaryItem term="数字证书">
    由可信证书机构签名，把主体身份与公开密钥绑定的凭据。掌握标准是能在原始请求或响应中定位它，并构造一条最小反例。
  </GlossaryItem>
</Glossary>

[← 上一页：第6章 HTTP首部](/learn/illustrated-http/06-chapter-06/ilh-06-http-headers) · [下一页：第8章 确认访问用户身份的认证 →](/learn/illustrated-http/08-chapter-08/ilh-08-user-authentication)

<Attribution
  adaptedFrom="上野宣《图解HTTP》2014年4月首版"
  adaptedUrl="https://www.ituring.com.cn/book/1229"
/>
