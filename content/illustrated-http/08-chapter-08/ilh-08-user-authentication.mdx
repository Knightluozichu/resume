---
title: "第8章 确认访问用户身份的认证"
description: "比较BASIC、DIGEST、SSL客户端认证与表单认证，并解释Session和Cookie如何保持认证状态"
type: C
section: "图解HTTP（2014年首版 / HTTP/1.1时代）"
order: 8
draft: false
math: false
sourceUrl: "https://www.ituring.com.cn/book/1229"
demo: true
---

import { Objectives } from "@/components/mdx/objectives";
import { Term } from "@/components/mdx/term";
import { Step, Stepper } from "@/components/mdx/stepper";
import { Callout } from "@/components/mdx/callout";
import { Answer, Exercises } from "@/components/mdx/exercises";
import { Glossary, GlossaryItem } from "@/components/mdx/glossary";
import { Attribution } from "@/components/mdx/attribution";

# 第8章 确认访问用户身份的认证

本课程对应[日]上野宣《图解HTTP》，于均良译，人民邮电出版社/图灵教育，2014年4月首版，308页，双色印刷，ISBN 9787115351531；原书《HTTPの教科書》，ISBN 9784798126258。图灵官方资料标明出版日期为2014年4月22日、172张图解；正式目录为11章、202个节/小节节点。本页严格按该首版章次组织，不用后续规范替换原书语境。

<Objectives>

- 能解释“第8章 确认访问用户身份的认证”全部正式节点，并把概念定位到真实请求、响应或应用状态。
- 能绘制客户端、中介、服务器之间的消息方向、主体边界和关键状态转移。
- 能设计单变量实验，验证“对任一认证流程能指出凭据形式、挑战与响应、验证位置、会话标识生命周期和退出失效点”。
- 能写出并提交包含原始报文、失败反例、恢复条件和版次边界的独立证据包。

</Objectives>

## 从一条可证伪的HTTP交换开始

先预测：把Base64当加密、把DIGEST当现代安全方案，或只依赖客户端Cookie中的用户身份，会让凭据重放和会话劫持直接成立。把预测写成“请求输入、线上消息、接收方状态、响应输出、最终副作用”五列，再运行实验。若结果与预测不同，先修正模型，不要只截取一个状态码证明自己。

本章的主问题是：比较BASIC、DIGEST、SSL客户端认证与表单认证，并解释Session和Cookie如何保持认证状态。原书以图解建立直觉，本课程把图解升级为可操作轨迹：每个箭头必须注明方向、协议对象和完成点；每个安全结论必须附一条攻击或误配置反例。

验收不变量是：对任一认证流程能指出凭据形式、挑战与响应、验证位置、会话标识生命周期和退出失效点。浏览器页面“看起来正常”只能说明某个终点出现，不能单独证明DNS、连接、TLS、缓存、认证或授权中的哪一段正确。

## 核心词汇与首版边界

<Term def="通过凭据确认访问者声称身份的过程">认证</Term>、<Term def="把用户名和密码组合后Base64编码放入Authorization的HTTP挑战响应方案">BASIC认证</Term>、<Term def="以nonce和摘要计算避免直接发送明文密码的挑战响应方案">DIGEST认证</Term>、<Term def="客户端用证书和私钥向服务器证明身份的TLS双向认证机制">SSL客户端认证</Term>、<Term def="服务器保存的会话状态，通常由难预测的Cookie标识关联">Session</Term>

这些词汇按2014年首版语义使用。SPDY、HTTP/2.0、X-XSS-Protection、P3P等保留出版时状态；HTTP/2最终规范、HTTP/3、JWT、OAuth、SameSite等后续技术只能作为另行标注的现代补充，不改变本书目录分母。


## 核心机制深读

### 认证对象不只是密码

可用于核对身份的信息包括密码、动态令牌、证书、IC卡和生物特征。可靠流程要同时考虑凭据如何登记、传输、验证、更新、吊销与审计，而不是只看登录页面。

动手试：先写下预期请求、响应与状态变化，再捕获一次正常交换和一次只改一个变量的失败交换。比较状态码、首部、主体、缓存或会话状态，确认结论来自证据而非浏览器表象。

### BASIC与DIGEST的真实边界

BASIC仅做Base64编码，必须依赖HTTPS保护；DIGEST把密码、realm、nonce、方法和URI等输入摘要，避免原密码直接上网，但算法与部署限制使其不能替代HTTPS。两者都由401和WWW-Authenticate发起挑战。

动手试：先写下预期请求、响应与状态变化，再捕获一次正常交换和一次只改一个变量的失败交换。比较状态码、首部、主体、缓存或会话状态，确认结论来自证据而非浏览器表象。

### 客户端证书的成本

服务器请求客户端证书，客户端证明持有相应私钥，服务器验证链和用途。它能形成强设备或人员凭据，也带来证书签发、安装、私钥保护、吊销和跨设备使用成本，常与密码形成双因素。

动手试：先写下预期请求、响应与状态变化，再捕获一次正常交换和一次只改一个变量的失败交换。比较状态码、首部、主体、缓存或会话状态，确认结论来自证据而非浏览器表象。

### 表单认证依赖会话纪律

应用验证表单凭据后应重新生成Session ID，通过Set-Cookie发给客户端，并设置适当作用域、Secure与HttpOnly。退出、超时、权限变化要在服务端失效会话；只删除浏览器Cookie不足以使被盗标识失效。

动手试：先写下预期请求、响应与状态变化，再捕获一次正常交换和一次只改一个变量的失败交换。比较状态码、首部、主体、缓存或会话状态，确认结论来自证据而非浏览器表象。

## 原书目录逐节点重构

### 8.1 何为认证

认证核对访问者是谁，授权另行判断已认证身份能做什么。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“何为认证”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

### 8.2 BASIC认证

“BASIC认证”位于本章主链“比较BASIC、DIGEST、SSL客户端认证与表单认证，并解释Session和Cookie如何保持认证状态”中；需要说明它接收什么输入、改变哪份协议或应用状态、产生什么可观察输出。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“BASIC认证”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

### 8.3 DIGEST认证

“DIGEST认证”位于本章主链“比较BASIC、DIGEST、SSL客户端认证与表单认证，并解释Session和Cookie如何保持认证状态”中；需要说明它接收什么输入、改变哪份协议或应用状态、产生什么可观察输出。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“DIGEST认证”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

### 8.4 SSL客户端认证

“SSL客户端认证”位于本章主链“比较BASIC、DIGEST、SSL客户端认证与表单认证，并解释Session和Cookie如何保持认证状态”中；需要说明它接收什么输入、改变哪份协议或应用状态、产生什么可观察输出。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“SSL客户端认证”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 8.4.1 SSL客户端认证的认证步骤

“SSL客户端认证的认证步骤”位于本章主链“比较BASIC、DIGEST、SSL客户端认证与表单认证，并解释Session和Cookie如何保持认证状态”中；需要说明它接收什么输入、改变哪份协议或应用状态、产生什么可观察输出。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“SSL客户端认证的认证步骤”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 8.4.2 SSL客户端认证采用双因素认证

“SSL客户端认证采用双因素认证”位于本章主链“比较BASIC、DIGEST、SSL客户端认证与表单认证，并解释Session和Cookie如何保持认证状态”中；需要说明它接收什么输入、改变哪份协议或应用状态、产生什么可观察输出。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“SSL客户端认证采用双因素认证”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 8.4.3 SSL客户端认证必要的费用

“SSL客户端认证必要的费用”位于本章主链“比较BASIC、DIGEST、SSL客户端认证与表单认证，并解释Session和Cookie如何保持认证状态”中；需要说明它接收什么输入、改变哪份协议或应用状态、产生什么可观察输出。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“SSL客户端认证必要的费用”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

### 8.5 基于表单认证

应用自定义登录表单和验证逻辑，成功后通常建立服务端Session。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“基于表单认证”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 8.5.1 认证多半为基于表单认证

“认证多半为基于表单认证”位于本章主链“比较BASIC、DIGEST、SSL客户端认证与表单认证，并解释Session和Cookie如何保持认证状态”中；需要说明它接收什么输入、改变哪份协议或应用状态、产生什么可观察输出。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“认证多半为基于表单认证”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 8.5.2 Session管理及Cookie应用

随机Session ID由Cookie携带，服务端保存真实会话状态并负责轮换、超时和注销。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“Session管理及Cookie应用”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

## 本页完整节点清单

本页承担10个目录或复习节点，正文、图解、实验和题目必须能反向定位每一项：

- 8.1 何为认证
- 8.2 BASIC认证
- 8.3 DIGEST认证
- 8.4 SSL客户端认证
- 8.4.1 SSL客户端认证的认证步骤
- 8.4.2 SSL客户端认证采用双因素认证
- 8.4.3 SSL客户端认证必要的费用
- 8.5 基于表单认证
- 8.5.1 认证多半为基于表单认证
- 8.5.2 Session管理及Cookie应用

<Stepper>
  <Step title="复原正常协议轨迹">
    固定URI、客户端、网络和服务端状态，标出请求行、首部、主体、中介与最终响应。
  </Step>
  <Step title="只改变一个变量">
    只改方法、条件首部、表示大小、连接状态、Cookie或输入上下文中的一个，先写预测再执行。
    </Step>
  <Step title="关闭独立证据门">
    对照正常与失败交换，核对线上字节、协议状态、业务副作用、恢复步骤和首版边界。
    </Step>
</Stepper>

## 可复现实验

```text
HTTP/1.1 401 Unauthorized
WWW-Authenticate: Basic realm="members"

GET /private HTTP/1.1
Authorization: Basic YWxpY2U6cGFzcw==
```

```text
challenge: realm + nonce + qop
client: digest(username, password, method, uri, nonce)
server: recompute and compare
```

```text
POST /login -> verify credentials -> rotate session id
Set-Cookie: sid=random; Path=/; Secure; HttpOnly
Cookie: sid=random -> lookup server-side session
```

动手试：使用浏览器开发者工具、curl或抓包工具保存同一资源的正常、条件命中、条件失配和无效输入四次交换。记录方法、目标、状态码、关键首部、主体长度、连接复用、缓存/Session状态与最终副作用。

<Callout type="trap" title="把现代规范倒灌进首版">
  原书写作于HTTP/2仍在制定的时期。后来的协议、认证和浏览器安全机制可以比较，但必须明确标为后续变化，不能替换正式节点。
</Callout>

<Callout type="trap" title="把收到响应当成业务正确">
  200只说明服务器选择了成功响应；它不能证明缓存隔离、对象授权、会话轮换、内容类型或输出转义正确。必须继续核对状态与副作用。
</Callout>

## 独立证据门

最小证据包包含：首版目录节点、请求与响应原文、连接/中介方向、主体边界、缓存或会话状态、一个单变量失败、最终副作用、已知限制、恢复步骤、责任人与复核人。

## 练习

<Exercises>

**问题 1：为什么“第8章 确认访问用户身份的认证”必须固定2014年首版语境？**

<Answer>
  因为本书记录的是HTTP/1.1主流、SPDY与HTTP/2仍在演进的技术快照。固定版次才能让目录、术语、图解和实验结论一一对应，避免以后来的规范覆盖作者原本要解释的问题。
</Answer>

**问题 2：怎样构造“把Base64当加密、把DIGEST当现代安全方案，或只依赖客户端Cookie中的用户身份，会让凭据重放和会话劫持直接成立”的最小反例？**

<Answer>
  固定URI、客户端、网络、服务器数据和操作，只改变一个方法、首部、主体、连接、Cookie或输入上下文；保存完整请求响应，比较状态、缓存/会话和最终副作用。
</Answer>

**问题 3：何时可以认为本页完成独立交接？**

<Answer>
  另一位读者无需口头补充即可定位全部10个节点，复现正常与失败交换，并依据“对任一认证流程能指出凭据形式、挑战与响应、验证位置、会话标识生命周期和退出失效点”得到相同结论。
</Answer>

</Exercises>

## 本章回顾

“第8章 确认访问用户身份的认证”的核心是比较BASIC、DIGEST、SSL客户端认证与表单认证，并解释Session和Cookie如何保持认证状态。真正掌握不止是认出术语，而是能让一条请求在消息、连接、中介、表示、身份和攻击边界之间闭环，并用失败实验说明边界一旦被破坏会发生什么。

<Glossary>
  <GlossaryItem term="认证">
    通过凭据确认访问者声称身份的过程。掌握标准是能在原始请求或响应中定位它，并构造一条最小反例。
  </GlossaryItem>
  <GlossaryItem term="BASIC认证">
    把用户名和密码组合后Base64编码放入Authorization的HTTP挑战响应方案。掌握标准是能在原始请求或响应中定位它，并构造一条最小反例。
  </GlossaryItem>
  <GlossaryItem term="DIGEST认证">
    以nonce和摘要计算避免直接发送明文密码的挑战响应方案。掌握标准是能在原始请求或响应中定位它，并构造一条最小反例。
  </GlossaryItem>
  <GlossaryItem term="SSL客户端认证">
    客户端用证书和私钥向服务器证明身份的TLS双向认证机制。掌握标准是能在原始请求或响应中定位它，并构造一条最小反例。
  </GlossaryItem>
  <GlossaryItem term="Session">
    服务器保存的会话状态，通常由难预测的Cookie标识关联。掌握标准是能在原始请求或响应中定位它，并构造一条最小反例。
  </GlossaryItem>
</Glossary>

[← 上一页：第7章 确保Web安全的HTTPS](/learn/illustrated-http/07-chapter-07/ilh-07-https-security) · [下一页：第9章 基于HTTP的功能追加协议 →](/learn/illustrated-http/09-chapter-09/ilh-09-http-extensions)

<Attribution
  adaptedFrom="上野宣《图解HTTP》2014年4月首版"
  adaptedUrl="https://www.ituring.com.cn/book/1229"
/>
