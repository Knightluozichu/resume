---
title: "第9章 基于HTTP的功能追加协议"
description: "在2013至2014年的技术快照下理解SPDY、WebSocket、当时仍受期待的HTTP/2.0和WebDAV扩展"
type: C
section: "图解HTTP（2014年首版 / HTTP/1.1时代）"
order: 9
draft: true
math: false
sourceUrl: "https://www.ituring.com.cn/book/1229"
demo: true
---

# 第9章 基于HTTP的功能追加协议

本课程对应[日]上野宣《图解HTTP》，于均良译，人民邮电出版社/图灵教育，2014年4月首版，308页，双色印刷，ISBN 9787115351531；原书《HTTPの教科書》，ISBN 9784798126258。图灵官方资料标明出版日期为2014年4月22日、172张图解；正式目录为11章、202个节/小节节点。本页严格按该首版章次组织，不用后续规范替换原书语境。

<Objectives>

- 能解释“第9章 基于HTTP的功能追加协议”全部正式节点，并把概念定位到真实请求、响应或应用状态。
- 能绘制客户端、中介、服务器之间的消息方向、主体边界和关键状态转移。
- 能设计单变量实验，验证“能区分HTTP承载、协议升级和HTTP方法扩展，并明确哪些结论属于首版出版时的提案或实现状态”。
- 能写出并提交包含原始报文、失败反例、恢复条件和版次边界的独立证据包。

</Objectives>

## 从一条可证伪的HTTP交换开始

先预测：用今天HTTP/2或HTTP/3的最终规范重写本章，会抹掉SPDY为何出现以及原书对HTTP瓶颈的历史判断。把预测写成“请求输入、线上消息、接收方状态、响应输出、最终副作用”五列，再运行实验。若结果与预测不同，先修正模型，不要只截取一个状态码证明自己。

本章的主问题是：在2013至2014年的技术快照下理解SPDY、WebSocket、当时仍受期待的HTTP/2.0和WebDAV扩展。原书以图解建立直觉，本课程把图解升级为可操作轨迹：每个箭头必须注明方向、协议对象和完成点；每个安全结论必须附一条攻击或误配置反例。

验收不变量是：能区分HTTP承载、协议升级和HTTP方法扩展，并明确哪些结论属于首版出版时的提案或实现状态。浏览器页面“看起来正常”只能说明某个终点出现，不能单独证明DNS、连接、TLS、缓存、认证或授权中的哪一段正确。

## 核心词汇与首版边界

<Term def="Google提出、运行在SSL之上的实验性协议，用多路复用、压缩和服务器推送缓解HTTP/1.x瓶颈">SPDY</Term>、<Term def="经HTTP Upgrade握手后切换为持久全双工消息通信的独立协议">WebSocket</Term>、<Term def="原书出版时仍在制定、吸收SPDY等经验的新一代HTTP目标">HTTP/2.0</Term>、<Term def="在HTTP/1.1上增加集合、属性、锁和远程文件管理方法的扩展">WebDAV</Term>、<Term def="客户端提出切换协议、服务器以101接受的HTTP/1.1升级机制">Upgrade</Term>

这些词汇按2014年首版语义使用。SPDY、HTTP/2.0、X-XSS-Protection、P3P等保留出版时状态；HTTP/2最终规范、HTTP/3、JWT、OAuth、SameSite等后续技术只能作为另行标注的现代补充，不改变本书目录分母。

<Ilh09HttpExtensionsFlowLab />

## 核心机制深读

### HTTP/1.x瓶颈的时代背景

页面资源数量增加后，单连接按序响应、并发连接限制、重复且冗长的首部和客户端轮询放大延迟。优化图片合并、内联和域名分片只是绕过协议限制，也会增加缓存与维护复杂度。

动手试：先写下预期请求、响应与状态变化，再捕获一次正常交换和一次只改一个变量的失败交换。比较状态码、首部、主体、缓存或会话状态，确认结论来自证据而非浏览器表象。

### SPDY是实验答案而非终点

SPDY在一条连接上多路复用流、压缩首部、分配优先级并支持服务器推送，但仍依赖现有网络与服务器生态。原书问题是它能否消除全部Web瓶颈，答案必须包括应用、服务端处理和TCP仍可能成为限制。

动手试：先写下预期请求、响应与状态变化，再捕获一次正常交换和一次只改一个变量的失败交换。比较状态码、首部、主体、缓存或会话状态，确认结论来自证据而非浏览器表象。

### WebSocket先握手再换协议

客户端以Upgrade和Connection请求切换，服务器返回101并用Sec-WebSocket-Accept证明握手。此后传输WebSocket帧，双方都可主动发送，不再为每条消息套完整HTTP请求响应。

动手试：先写下预期请求、响应与状态变化，再捕获一次正常交换和一次只改一个变量的失败交换。比较状态码、首部、主体、缓存或会话状态，确认结论来自证据而非浏览器表象。

### WebDAV扩展资源管理

WebDAV增加PROPFIND、PROPPATCH、MKCOL、COPY、MOVE、LOCK、UNLOCK等方法及207 Multi-Status等状态，使服务器端集合、属性和锁可由HTTP管理，也扩大了权限和互操作复杂度。

动手试：先写下预期请求、响应与状态变化，再捕获一次正常交换和一次只改一个变量的失败交换。比较状态码、首部、主体、缓存或会话状态，确认结论来自证据而非浏览器表象。

## 原书目录逐节点重构

### 9.1 基于HTTP的协议

“基于HTTP的协议”位于本章主链“在2013至2014年的技术快照下理解SPDY、WebSocket、当时仍受期待的HTTP/2.0和WebDAV扩展”中；需要说明它接收什么输入、改变哪份协议或应用状态、产生什么可观察输出。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“基于HTTP的协议”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

### 9.2 消除HTTP瓶颈的SPDY

“消除HTTP瓶颈的SPDY”位于本章主链“在2013至2014年的技术快照下理解SPDY、WebSocket、当时仍受期待的HTTP/2.0和WebDAV扩展”中；需要说明它接收什么输入、改变哪份协议或应用状态、产生什么可观察输出。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“消除HTTP瓶颈的SPDY”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 9.2.1 HTTP的瓶颈

连接数、按序响应、重复首部和轮询在高资源页面中放大延迟与带宽。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“HTTP的瓶颈”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 9.2.2 SPDY的设计与功能

多路复用、首部压缩、优先级和服务器推送尝试减少HTTP/1.x绕行优化。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“SPDY的设计与功能”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 9.2.3 SPDY消除Web瓶颈了么

“SPDY消除Web瓶颈了么”位于本章主链“在2013至2014年的技术快照下理解SPDY、WebSocket、当时仍受期待的HTTP/2.0和WebDAV扩展”中；需要说明它接收什么输入、改变哪份协议或应用状态、产生什么可观察输出。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“SPDY消除Web瓶颈了么”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

### 9.3 使用浏览器进行全双工通信的WebSocket

“使用浏览器进行全双工通信的WebSocket”位于本章主链“在2013至2014年的技术快照下理解SPDY、WebSocket、当时仍受期待的HTTP/2.0和WebDAV扩展”中；需要说明它接收什么输入、改变哪份协议或应用状态、产生什么可观察输出。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“使用浏览器进行全双工通信的WebSocket”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 9.3.1 WebSocket的设计与功能

HTTP Upgrade后形成低开销全双工通道，双方都能主动发消息。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“WebSocket的设计与功能”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 9.3.2 WebSocket协议

握手校验后以带操作码、长度和掩码规则的帧交换消息。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“WebSocket协议”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 9.4 期盼已久的HTTP/2.0

“期盼已久的HTTP/2.0”位于本章主链“在2013至2014年的技术快照下理解SPDY、WebSocket、当时仍受期待的HTTP/2.0和WebDAV扩展”中；需要说明它接收什么输入、改变哪份协议或应用状态、产生什么可观察输出。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“期盼已久的HTTP/2.0”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

### 9.5 Web服务器管理文件的WebDAV

“Web服务器管理文件的WebDAV”位于本章主链“在2013至2014年的技术快照下理解SPDY、WebSocket、当时仍受期待的HTTP/2.0和WebDAV扩展”中；需要说明它接收什么输入、改变哪份协议或应用状态、产生什么可观察输出。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“Web服务器管理文件的WebDAV”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 9.5.1 扩展HTTP/1.1的WebDAV

WebDAV保持HTTP资源模型并增加集合、属性、锁与远程文件操作。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“扩展HTTP/1.1的WebDAV”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 9.5.2 WebDAV内新增的方法及状态码

PROPFIND、MKCOL、LOCK等方法配合207等状态表达批量资源结果。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“WebDAV内新增的方法及状态码”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

## 本页完整节点清单

本页承担12个目录或复习节点，正文、图解、实验和题目必须能反向定位每一项：

- 9.1 基于HTTP的协议
- 9.2 消除HTTP瓶颈的SPDY
- 9.2.1 HTTP的瓶颈
- 9.2.2 SPDY的设计与功能
- 9.2.3 SPDY消除Web瓶颈了么
- 9.3 使用浏览器进行全双工通信的WebSocket
- 9.3.1 WebSocket的设计与功能
- 9.3.2 WebSocket协议
- 9.4 期盼已久的HTTP/2.0
- 9.5 Web服务器管理文件的WebDAV
- 9.5.1 扩展HTTP/1.1的WebDAV
- 9.5.2 WebDAV内新增的方法及状态码

<Stepper>
  <Step title="复原正常协议轨迹">
    固定URI、客户端、网络和服务端状态，标出请求行、首部、主体、中介与最终响应。
    <Ilh09HttpExtensionsFlowLab />
  </Step>
  <Step title="只改变一个变量">
    只改方法、条件首部、表示大小、连接状态、Cookie或输入上下文中的一个，先写预测再执行。
    </Step>
  <Step title="关闭独立证据门">
    对照正常与失败交换，核对线上字节、协议状态、业务副作用、恢复步骤和首版边界。
    </Step>
</Stepper>

## 可复现实验

```text
GET /chat HTTP/1.1
Host: server.example.test
Upgrade: websocket
Connection: Upgrade
Sec-WebSocket-Key: random-base64

HTTP/1.1 101 Switching Protocols
```

```text
HTTP polling: request -> wait -> response -> repeat
WebSocket: HTTP upgrade -> frame <-> frame <-> frame
```

```text
PROPFIND /docs/ HTTP/1.1
Depth: 1

HTTP/1.1 207 Multi-Status
```

动手试：使用浏览器开发者工具、curl或抓包工具保存同一资源的正常、条件命中、条件失配和无效输入四次交换。记录方法、目标、状态码、关键首部、主体长度、连接复用、缓存/Session状态与最终副作用。

<Callout type="trap" title="把现代规范倒灌进首版">
  原书写作于HTTP/2仍在制定的时期。后来的协议、认证和浏览器安全机制可以比较，但必须明确标为后续变化，不能替换正式节点。
</Callout>

<Callout type="trap" title="把收到响应当成业务正确">
  200只说明服务器选择了成功响应；它不能证明缓存隔离、对象授权、会话轮换、内容类型或输出转义正确。必须继续核对状态与副作用。
</Callout>

## 独立证据门

最小证据包包含：首版目录节点、请求与响应原文、连接/中介方向、主体边界、缓存或会话状态、一个单变量失败、最终副作用、已知限制、恢复步骤、责任人与复核人。

## 练习

<Exercises>

**问题 1：为什么“第9章 基于HTTP的功能追加协议”必须固定2014年首版语境？**

<Answer>
  因为本书记录的是HTTP/1.1主流、SPDY与HTTP/2仍在演进的技术快照。固定版次才能让目录、术语、图解和实验结论一一对应，避免以后来的规范覆盖作者原本要解释的问题。
</Answer>

**问题 2：怎样构造“用今天HTTP/2或HTTP/3的最终规范重写本章，会抹掉SPDY为何出现以及原书对HTTP瓶颈的历史判断”的最小反例？**

<Answer>
  固定URI、客户端、网络、服务器数据和操作，只改变一个方法、首部、主体、连接、Cookie或输入上下文；保存完整请求响应，比较状态、缓存/会话和最终副作用。
</Answer>

**问题 3：何时可以认为本页完成独立交接？**

<Answer>
  另一位读者无需口头补充即可定位全部12个节点，复现正常与失败交换，并依据“能区分HTTP承载、协议升级和HTTP方法扩展，并明确哪些结论属于首版出版时的提案或实现状态”得到相同结论。
</Answer>

</Exercises>

## 本章回顾

“第9章 基于HTTP的功能追加协议”的核心是在2013至2014年的技术快照下理解SPDY、WebSocket、当时仍受期待的HTTP/2.0和WebDAV扩展。真正掌握不止是认出术语，而是能让一条请求在消息、连接、中介、表示、身份和攻击边界之间闭环，并用失败实验说明边界一旦被破坏会发生什么。

<Glossary>
  <GlossaryItem term="SPDY">
    Google提出、运行在SSL之上的实验性协议，用多路复用、压缩和服务器推送缓解HTTP/1.x瓶颈。掌握标准是能在原始请求或响应中定位它，并构造一条最小反例。
  </GlossaryItem>
  <GlossaryItem term="WebSocket">
    经HTTP
    Upgrade握手后切换为持久全双工消息通信的独立协议。掌握标准是能在原始请求或响应中定位它，并构造一条最小反例。
  </GlossaryItem>
  <GlossaryItem term="HTTP/2.0">
    原书出版时仍在制定、吸收SPDY等经验的新一代HTTP目标。掌握标准是能在原始请求或响应中定位它，并构造一条最小反例。
  </GlossaryItem>
  <GlossaryItem term="WebDAV">
    在HTTP/1.1上增加集合、属性、锁和远程文件管理方法的扩展。掌握标准是能在原始请求或响应中定位它，并构造一条最小反例。
  </GlossaryItem>
  <GlossaryItem term="Upgrade">
    客户端提出切换协议、服务器以101接受的HTTP/1.1升级机制。掌握标准是能在原始请求或响应中定位它，并构造一条最小反例。
  </GlossaryItem>
</Glossary>

[← 上一页：第8章 确认访问用户身份的认证](/learn/illustrated-http/08-chapter-08/ilh-08-user-authentication) · [下一页：第10章 构建Web内容的技术 →](/learn/illustrated-http/10-chapter-10/ilh-10-web-content-technologies)

<Attribution
  adaptedFrom="上野宣《图解HTTP》2014年4月首版"
  adaptedUrl="https://www.ituring.com.cn/book/1229"
/>
