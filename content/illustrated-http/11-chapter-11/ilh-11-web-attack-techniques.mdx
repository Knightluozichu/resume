---
title: "第11章 Web的攻击技术"
description: "按输入输出、设计配置、会话管理和其他风险四类还原XSS、注入、遍历、强制浏览、CSRF、DoS等攻击机制"
type: C
section: "图解HTTP（2014年首版 / HTTP/1.1时代）"
order: 11
draft: false
math: false
sourceUrl: "https://www.ituring.com.cn/book/1229"
demo: true
---

import {
  Ilh11WebAttackTechniquesFlowLab,
  Ilh11WebAttackTechniquesExperimentLab,
  Ilh11WebAttackTechniquesEvidenceLab,
} from "@/components/mdx/illustrated-http/diagrams/ilh-11-web-attack-techniques";

# 第11章 Web的攻击技术

本课程对应[日]上野宣《图解HTTP》，于均良译，人民邮电出版社/图灵教育，2014年4月首版，308页，双色印刷，ISBN 9787115351531；原书《HTTPの教科書》，ISBN 9784798126258。图灵官方资料标明出版日期为2014年4月22日、172张图解；正式目录为11章、202个节/小节节点。本页严格按该首版章次组织，不用后续规范替换原书语境。

<Objectives>

- 能解释“第11章 Web的攻击技术”全部正式节点，并把概念定位到真实请求、响应或应用状态。
- 能绘制客户端、中介、服务器之间的消息方向、主体边界和关键状态转移。
- 能设计单变量实验，验证“每个漏洞都能写出不可信输入、解释器或信任边界、可观察后果、单变量复现和对应防线，而不是只背攻击名称”。
- 能写出并提交包含原始报文、失败反例、恢复条件和版次边界的独立证据包。

</Objectives>

## 从一条可证伪的HTTP交换开始

先预测：只部署HTTPS或只过滤尖括号就宣称Web安全，会遗漏SQL/OS/首部解释器、授权检查、会话生命周期和资源耗尽。把预测写成“请求输入、线上消息、接收方状态、响应输出、最终副作用”五列，再运行实验。若结果与预测不同，先修正模型，不要只截取一个状态码证明自己。

本章的主问题是：按输入输出、设计配置、会话管理和其他风险四类还原XSS、注入、遍历、强制浏览、CSRF、DoS等攻击机制。原书以图解建立直觉，本课程把图解升级为可操作轨迹：每个箭头必须注明方向、协议对象和完成点；每个安全结论必须附一条攻击或误配置反例。

验收不变量是：每个漏洞都能写出不可信输入、解释器或信任边界、可观察后果、单变量复现和对应防线，而不是只背攻击名称。浏览器页面“看起来正常”只能说明某个终点出现，不能单独证明DNS、连接、TLS、缓存、认证或授权中的哪一段正确。

## 核心词汇与首版边界

<Term def="不可信数据未经上下文正确转义进入页面，使攻击者脚本在受害者源下执行">跨站脚本</Term>、<Term def="输入改变SQL语法结构，使数据库执行开发者未预期的查询或命令">SQL注入</Term>、<Term def="利用路径规范化缺陷越出允许目录读取或写入其他文件">目录遍历</Term>、<Term def="攻击者取得有效Session ID并冒充受害者访问应用">会话劫持</Term>、<Term def="诱导已登录浏览器携带自动凭据向目标站点发出非本人意愿请求">CSRF</Term>

这些词汇按2014年首版语义使用。SPDY、HTTP/2.0、X-XSS-Protection、P3P等保留出版时状态；HTTP/2最终规范、HTTP/3、JWT、OAuth、SameSite等后续技术只能作为另行标注的现代补充，不改变本书目录分母。

<Ilh11WebAttackTechniquesFlowLab />

## 核心机制深读

### 请求永远可以被客户端改写

隐藏字段、禁用按钮、JavaScript校验和Referer都不能成为授权依据。攻击者可以直接构造方法、URI、首部、Cookie和主体；服务端必须重新做认证、对象级授权、类型范围校验与业务不变量检查。

动手试：先写下预期请求、响应与状态变化，再捕获一次正常交换和一次只改一个变量的失败交换。比较状态码、首部、主体、缓存或会话状态，确认结论来自证据而非浏览器表象。

### 转义必须匹配输出上下文

HTML文本、HTML属性、URL、JavaScript字符串、SQL、Shell、HTTP首部和邮件首部是不同解释器。优先用参数化API和安全模板；在最终输出点按上下文编码，不能用一条替换规则覆盖所有语法。

动手试：先写下预期请求、响应与状态变化，再捕获一次正常交换和一次只改一个变量的失败交换。比较状态码、首部、主体、缓存或会话状态，确认结论来自证据而非浏览器表象。

### 设计与配置漏洞绕过正常入口

强制浏览直接访问未在导航显示的资源，错误消息泄露堆栈或查询，开放重定向把可信域变成跳板。修复依赖每次请求的服务端授权、最小化错误披露和目标地址白名单。

动手试：先写下预期请求、响应与状态变化，再捕获一次正常交换和一次只改一个变量的失败交换。比较状态码、首部、主体、缓存或会话状态，确认结论来自证据而非浏览器表象。

### 会话攻击针对身份连续性

劫持窃取既有Session ID，固定攻击迫使受害者登录到攻击者已知ID，CSRF利用浏览器自动携带Cookie。登录时轮换标识、全程HTTPS、Cookie安全属性、服务端失效和不可预测CSRF令牌共同形成防线。

动手试：先写下预期请求、响应与状态变化，再捕获一次正常交换和一次只改一个变量的失败交换。比较状态码、首部、主体、缓存或会话状态，确认结论来自证据而非浏览器表象。

### 其他漏洞需要不同资源边界

密码破解要限速、锁定与强凭据策略；点击劫持要限制框架嵌入；DoS要配额、超时、隔离和容量保护；后门程序必须通过供应链、完整性、最小权限与审计发现，不能只靠输入过滤。

动手试：先写下预期请求、响应与状态变化，再捕获一次正常交换和一次只改一个变量的失败交换。比较状态码、首部、主体、缓存或会话状态，确认结论来自证据而非浏览器表象。

## 原书目录逐节点重构

### 11.1 针对Web的攻击技术

“针对Web的攻击技术”位于本章主链“按输入输出、设计配置、会话管理和其他风险四类还原XSS、注入、遍历、强制浏览、CSRF、DoS等攻击机制”中；需要说明它接收什么输入、改变哪份协议或应用状态、产生什么可观察输出。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“针对Web的攻击技术”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 11.1.1 HTTP不具备必要的安全功能

HTTP传输语义不自动提供应用输入校验、对象授权、会话保护或资源配额。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“HTTP不具备必要的安全功能”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 11.1.2 在客户端即可篡改请求

任何客户端侧限制都可绕过，服务端必须把全部请求字段视为不可信。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“在客户端即可篡改请求”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 11.1.3 针对Web应用的攻击模式

攻击可主动发送构造请求，也可诱导受害者浏览器在其身份下被动发起请求。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“针对Web应用的攻击模式”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

### 11.2 因输出值转义不完全引发的安全漏洞

“因输出值转义不完全引发的安全漏洞”位于本章主链“按输入输出、设计配置、会话管理和其他风险四类还原XSS、注入、遍历、强制浏览、CSRF、DoS等攻击机制”中；需要说明它接收什么输入、改变哪份协议或应用状态、产生什么可观察输出。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“因输出值转义不完全引发的安全漏洞”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 11.2.1 跨站脚本攻击

不可信值进入HTML、属性、URL或脚本上下文且未正确编码，攻击代码便在受害者会话的同源权限下执行。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“跨站脚本攻击”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 11.2.2 SQL注入攻击

拼接SQL使输入成为查询语法；参数化查询把数据与命令结构分离。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“SQL注入攻击”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 11.2.3 OS命令注入攻击

输入被Shell解释为命令或参数；应避免Shell拼接并使用受控参数API。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“OS命令注入攻击”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 11.2.4 HTTP首部注入攻击

未拒绝换行的输入可截断或新增响应首部，甚至分割响应。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“HTTP首部注入攻击”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 11.2.5 邮件首部注入攻击

表单输入中的换行进入邮件首部，可篡改收件人或新增字段。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“邮件首部注入攻击”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 11.2.6 目录遍历攻击

../、编码变体或符号链接使规范化路径逃出允许根目录。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“目录遍历攻击”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 11.2.7 远程文件包含漏洞

应用把攻击者控制的远程位置当作代码或模板包含并执行。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“远程文件包含漏洞”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

### 11.3 因设置或设计上的缺陷引发的安全漏洞

“因设置或设计上的缺陷引发的安全漏洞”位于本章主链“按输入输出、设计配置、会话管理和其他风险四类还原XSS、注入、遍历、强制浏览、CSRF、DoS等攻击机制”中；需要说明它接收什么输入、改变哪份协议或应用状态、产生什么可观察输出。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“因设置或设计上的缺陷引发的安全漏洞”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 11.3.1 强制浏览

攻击者跳过界面直接请求可猜测资源，暴露缺失的服务端授权。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“强制浏览”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 11.3.2 不正确的错误消息处理

错误页泄露堆栈、SQL、路径或账户存在性，帮助攻击者缩小搜索空间。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“不正确的错误消息处理”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 11.3.3 开放重定向

未经白名单校验的跳转目标让可信域把用户导向攻击站点。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“开放重定向”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

### 11.4 因会话管理疏忽引发的安全漏洞

“因会话管理疏忽引发的安全漏洞”位于本章主链“按输入输出、设计配置、会话管理和其他风险四类还原XSS、注入、遍历、强制浏览、CSRF、DoS等攻击机制”中；需要说明它接收什么输入、改变哪份协议或应用状态、产生什么可观察输出。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“因会话管理疏忽引发的安全漏洞”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 11.4.1 会话劫持

窃取有效Session ID后重放，服务器误把攻击者识别为受害者。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“会话劫持”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 11.4.2 会话固定攻击

攻击者预先设定已知Session ID，再诱导受害者用同一标识登录。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“会话固定攻击”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 11.4.3 跨站点请求伪造

第三方页面诱导浏览器向目标站发送会自动携带Cookie的状态变更请求。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“跨站点请求伪造”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

### 11.5 其他安全漏洞

“其他安全漏洞”位于本章主链“按输入输出、设计配置、会话管理和其他风险四类还原XSS、注入、遍历、强制浏览、CSRF、DoS等攻击机制”中；需要说明它接收什么输入、改变哪份协议或应用状态、产生什么可观察输出。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“其他安全漏洞”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 11.5.1 密码破解

通过穷举、字典、撞库或针对哈希离线尝试获得凭据。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“密码破解”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 11.5.2 点击劫持

透明或伪装框架诱导用户点击被覆盖的目标站控件。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“点击劫持”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 11.5.3 DoS攻击

用大量请求、昂贵输入或连接占用耗尽计算、内存、线程或带宽。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“DoS攻击”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

#### 11.5.4 后门程序

绕过正常认证或控制流程的隐藏入口，可能来自恶意植入或维护遗留。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“后门程序”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

## 本页完整节点清单

本页承担25个目录或复习节点，正文、图解、实验和题目必须能反向定位每一项：

- 11.1 针对Web的攻击技术
- 11.1.1 HTTP不具备必要的安全功能
- 11.1.2 在客户端即可篡改请求
- 11.1.3 针对Web应用的攻击模式
- 11.2 因输出值转义不完全引发的安全漏洞
- 11.2.1 跨站脚本攻击
- 11.2.2 SQL注入攻击
- 11.2.3 OS命令注入攻击
- 11.2.4 HTTP首部注入攻击
- 11.2.5 邮件首部注入攻击
- 11.2.6 目录遍历攻击
- 11.2.7 远程文件包含漏洞
- 11.3 因设置或设计上的缺陷引发的安全漏洞
- 11.3.1 强制浏览
- 11.3.2 不正确的错误消息处理
- 11.3.3 开放重定向
- 11.4 因会话管理疏忽引发的安全漏洞
- 11.4.1 会话劫持
- 11.4.2 会话固定攻击
- 11.4.3 跨站点请求伪造
- 11.5 其他安全漏洞
- 11.5.1 密码破解
- 11.5.2 点击劫持
- 11.5.3 DoS攻击
- 11.5.4 后门程序

<Stepper>
  <Step title="复原正常协议轨迹">
    固定URI、客户端、网络和服务端状态，标出请求行、首部、主体、中介与最终响应。
    <Ilh11WebAttackTechniquesFlowLab />
  </Step>
  <Step title="只改变一个变量">
    只改方法、条件首部、表示大小、连接状态、Cookie或输入上下文中的一个，先写预测再执行。
    <Ilh11WebAttackTechniquesExperimentLab />
  </Step>
  <Step title="关闭独立证据门">
    对照正常与失败交换，核对线上字节、协议状态、业务副作用、恢复步骤和首版边界。
    <Ilh11WebAttackTechniquesEvidenceLab />
  </Step>
</Stepper>

## 可复现实验

```text
untrusted input -> parser/interpreter -> unintended command
              ^ validate, parameterize, context-encode
```

```text
browser with session cookie --forged state-changing request--> target
defense: unpredictable request token + origin checks + safe cookie policy
```

```text
resource request -> authenticate -> object authorization -> validate transition -> execute -> audit
```

动手试：使用浏览器开发者工具、curl或抓包工具保存同一资源的正常、条件命中、条件失配和无效输入四次交换。记录方法、目标、状态码、关键首部、主体长度、连接复用、缓存/Session状态与最终副作用。

<Callout type="trap" title="把现代规范倒灌进首版">
  原书写作于HTTP/2仍在制定的时期。后来的协议、认证和浏览器安全机制可以比较，但必须明确标为后续变化，不能替换正式节点。
</Callout>

<Callout type="trap" title="把收到响应当成业务正确">
  200只说明服务器选择了成功响应；它不能证明缓存隔离、对象授权、会话轮换、内容类型或输出转义正确。必须继续核对状态与副作用。
</Callout>

## 独立证据门

<Ilh11WebAttackTechniquesEvidenceLab />

最小证据包包含：首版目录节点、请求与响应原文、连接/中介方向、主体边界、缓存或会话状态、一个单变量失败、最终副作用、已知限制、恢复步骤、责任人与复核人。

## 练习

<Exercises>

**问题 1：为什么“第11章 Web的攻击技术”必须固定2014年首版语境？**

<Answer>
  因为本书记录的是HTTP/1.1主流、SPDY与HTTP/2仍在演进的技术快照。固定版次才能让目录、术语、图解和实验结论一一对应，避免以后来的规范覆盖作者原本要解释的问题。
</Answer>

**问题 2：怎样构造“只部署HTTPS或只过滤尖括号就宣称Web安全，会遗漏SQL/OS/首部解释器、授权检查、会话生命周期和资源耗尽”的最小反例？**

<Answer>
  固定URI、客户端、网络、服务器数据和操作，只改变一个方法、首部、主体、连接、Cookie或输入上下文；保存完整请求响应，比较状态、缓存/会话和最终副作用。
</Answer>

**问题 3：何时可以认为本页完成独立交接？**

<Answer>
  另一位读者无需口头补充即可定位全部25个节点，复现正常与失败交换，并依据“每个漏洞都能写出不可信输入、解释器或信任边界、可观察后果、单变量复现和对应防线，而不是只背攻击名称”得到相同结论。
</Answer>

</Exercises>

## 本章回顾

“第11章 Web的攻击技术”的核心是按输入输出、设计配置、会话管理和其他风险四类还原XSS、注入、遍历、强制浏览、CSRF、DoS等攻击机制。真正掌握不止是认出术语，而是能让一条请求在消息、连接、中介、表示、身份和攻击边界之间闭环，并用失败实验说明边界一旦被破坏会发生什么。

<Glossary>
  <GlossaryItem term="跨站脚本">
    不可信数据未经上下文正确转义进入页面，使攻击者脚本在受害者源下执行。掌握标准是能在原始请求或响应中定位它，并构造一条最小反例。
  </GlossaryItem>
  <GlossaryItem term="SQL注入">
    输入改变SQL语法结构，使数据库执行开发者未预期的查询或命令。掌握标准是能在原始请求或响应中定位它，并构造一条最小反例。
  </GlossaryItem>
  <GlossaryItem term="目录遍历">
    利用路径规范化缺陷越出允许目录读取或写入其他文件。掌握标准是能在原始请求或响应中定位它，并构造一条最小反例。
  </GlossaryItem>
  <GlossaryItem term="会话劫持">
    攻击者取得有效Session
    ID并冒充受害者访问应用。掌握标准是能在原始请求或响应中定位它，并构造一条最小反例。
  </GlossaryItem>
  <GlossaryItem term="CSRF">
    诱导已登录浏览器携带自动凭据向目标站点发出非本人意愿请求。掌握标准是能在原始请求或响应中定位它，并构造一条最小反例。
  </GlossaryItem>
</Glossary>

[← 上一页：第10章 构建Web内容的技术](/learn/illustrated-http/10-chapter-10/ilh-10-web-content-technologies) · [下一页：2014年首版总复习与协议审计 →](/learn/illustrated-http/12-official-final-review/ilh-official-final-review)

<Attribution
  adaptedFrom="上野宣《图解HTTP》2014年4月首版"
  adaptedUrl="https://www.ituring.com.cn/book/1229"
/>
