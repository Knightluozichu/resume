---
title: "2014年首版总复习与协议审计"
description: "把11章、202个正式目录节点压缩成可独立复核的一条HTTP请求生命周期与四道证据门"
type: C
section: "图解HTTP（2014年首版 / HTTP/1.1时代）"
order: 12
draft: false
math: false
sourceUrl: "https://www.ituring.com.cn/book/1229"
demo: true
---

import { Objectives } from "@/components/mdx/objectives";
import { Term } from "@/components/mdx/term";
import { Step, Stepper } from "@/components/mdx/stepper";
import { Callout } from "@/components/mdx/callout";
import { Answer, Exercises } from "@/components/mdx/exercises";
import { Glossary, GlossaryItem } from "@/components/mdx/glossary";
import { Attribution } from "@/components/mdx/attribution";

# 2014年首版总复习与协议审计

本课程对应[日]上野宣《图解HTTP》，于均良译，人民邮电出版社/图灵教育，2014年4月首版，308页，双色印刷，ISBN 9787115351531；原书《HTTPの教科書》，ISBN 9784798126258。图灵官方资料标明出版日期为2014年4月22日、172张图解；正式目录为11章、202个节/小节节点。本页把全部节点收束为一次端到端协议审计。

<Objectives>

- 能解释“2014年首版总复习与协议审计”全部正式节点，并把概念定位到真实请求、响应或应用状态。
- 能绘制客户端、中介、服务器之间的消息方向、主体边界和关键状态转移。
- 能设计单变量实验，验证“读者能从URI输入追踪到响应呈现或攻击阻断，逐段说明协议、中介、表示、身份和安全状态”。
- 能写出并提交包含原始报文、失败反例、恢复条件和版次边界的独立证据包。

</Objectives>

## 从一条可证伪的HTTP交换开始

先预测：按术语列表复习会失去跨章因果；真正掌握必须让同一个请求同时通过报文、缓存、认证和攻击面审计。把预测写成“请求输入、线上消息、接收方状态、响应输出、最终副作用”五列，再运行实验。若结果与预测不同，先修正模型，不要只截取一个状态码证明自己。

本章的主问题是：把11章、202个正式目录节点压缩成可独立复核的一条HTTP请求生命周期与四道证据门。原书以图解建立直觉，本课程把图解升级为可操作轨迹：每个箭头必须注明方向、协议对象和完成点；每个安全结论必须附一条攻击或误配置反例。

验收不变量是：读者能从URI输入追踪到响应呈现或攻击阻断，逐段说明协议、中介、表示、身份和安全状态。浏览器页面“看起来正常”只能说明某个终点出现，不能单独证明DNS、连接、TLS、缓存、认证或授权中的哪一段正确。

## 核心词汇与首版边界

<Term def="从URI、DNS、TCP、请求、响应到浏览器处理的有序证据链">协议轨迹</Term>、<Term def="起始行、首部、空行、主体以及长度或分块终止条件">报文边界</Term>、<Term def="新鲜度、实体标签、修改时间和Vary共同决定副本是否可复用">缓存验证</Term>、<Term def="TLS对端认证、应用认证、Session和对象授权各自独立的检查点">身份边界</Term>、<Term def="不可信输入跨越解析器、权限、会话或资源边界时形成的可利用路径">攻击面</Term>

这些词汇按2014年首版语义使用。SPDY、HTTP/2.0、X-XSS-Protection、P3P等保留出版时状态；HTTP/2最终规范、HTTP/3、JWT、OAuth、SameSite等后续技术只能作为另行标注的现代补充，不改变本书目录分母。


## 核心机制深读

### 第一道门：传输轨迹

给定URI，写出DNS结果、TCP连接、TLS状态（若有）、请求行、Host、关键首部和最终状态码。缺少任一层就无法判断超时、连接失败与应用错误分别发生在哪里。

动手试：先写下预期请求、响应与状态变化，再捕获一次正常交换和一次只改一个变量的失败交换。比较状态码、首部、主体、缓存或会话状态，确认结论来自证据而非浏览器表象。

### 第二道门：表示与缓存

记录Content-Type、Content-Encoding、Content-Length或分块、ETag、Last-Modified、Cache-Control、Age和Vary。用一次冷请求、一次新鲜命中和一次过期重验证证明副本生命周期。

动手试：先写下预期请求、响应与状态变化，再捕获一次正常交换和一次只改一个变量的失败交换。比较状态码、首部、主体、缓存或会话状态，确认结论来自证据而非浏览器表象。

### 第三道门：身份与权限

分别回答TLS验证了谁、应用凭据验证了谁、Session ID如何生成和轮换、目标对象由谁授权。认证成功不等于有权访问任意对象，HTTPS也不替代应用授权。

动手试：先写下预期请求、响应与状态变化，再捕获一次正常交换和一次只改一个变量的失败交换。比较状态码、首部、主体、缓存或会话状态，确认结论来自证据而非浏览器表象。

### 第四道门：失败与攻击

修改一个方法、条件首部、Cookie、对象ID或输入上下文，预测400/401/403/404/412/500等结果与状态不变量，再观察服务端是否拒绝且没有副作用。

动手试：先写下预期请求、响应与状态变化，再捕获一次正常交换和一次只改一个变量的失败交换。比较状态码、首部、主体、缓存或会话状态，确认结论来自证据而非浏览器表象。

### 首版边界复核

SPDY、当时的HTTP/2.0期待、X-XSS-Protection和P3P保留历史语义；后来的HTTP/2定稿、HTTP/3、JWT、SameSite等可另设现代补充，但不计入本书正式目录覆盖。

动手试：先写下预期请求、响应与状态变化，再捕获一次正常交换和一次只改一个变量的失败交换。比较状态码、首部、主体、缓存或会话状态，确认结论来自证据而非浏览器表象。

## 原书目录逐节点重构

### 复习1 第1章：Web与网络基础

“第1章：Web与网络基础”位于本章主链“把11章、202个正式目录节点压缩成可独立复核的一条HTTP请求生命周期与四道证据门”中；需要说明它接收什么输入、改变哪份协议或应用状态、产生什么可观察输出。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“第1章：Web与网络基础”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

### 复习2 第2章：简单HTTP协议

“第2章：简单HTTP协议”位于本章主链“把11章、202个正式目录节点压缩成可独立复核的一条HTTP请求生命周期与四道证据门”中；需要说明它接收什么输入、改变哪份协议或应用状态、产生什么可观察输出。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“第2章：简单HTTP协议”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

### 复习3 第3章：HTTP报文信息

“第3章：HTTP报文信息”位于本章主链“把11章、202个正式目录节点压缩成可独立复核的一条HTTP请求生命周期与四道证据门”中；需要说明它接收什么输入、改变哪份协议或应用状态、产生什么可观察输出。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“第3章：HTTP报文信息”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

### 复习4 第4章：HTTP状态码

“第4章：HTTP状态码”位于本章主链“把11章、202个正式目录节点压缩成可独立复核的一条HTTP请求生命周期与四道证据门”中；需要说明它接收什么输入、改变哪份协议或应用状态、产生什么可观察输出。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“第4章：HTTP状态码”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

### 复习5 第5章：协作Web服务器

“第5章：协作Web服务器”位于本章主链“把11章、202个正式目录节点压缩成可独立复核的一条HTTP请求生命周期与四道证据门”中；需要说明它接收什么输入、改变哪份协议或应用状态、产生什么可观察输出。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“第5章：协作Web服务器”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

### 复习6 第6章：HTTP首部

这是“第6章：HTTP首部”这一组语义的分类入口；学习时按方向、作用域、前置条件和失败结果比较组内成员。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“第6章：HTTP首部”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

### 复习7 第7章：HTTPS

“第7章：HTTPS”位于本章主链“把11章、202个正式目录节点压缩成可独立复核的一条HTTP请求生命周期与四道证据门”中；需要说明它接收什么输入、改变哪份协议或应用状态、产生什么可观察输出。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“第7章：HTTPS”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

### 复习8 第8章：认证

“第8章：认证”位于本章主链“把11章、202个正式目录节点压缩成可独立复核的一条HTTP请求生命周期与四道证据门”中；需要说明它接收什么输入、改变哪份协议或应用状态、产生什么可观察输出。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“第8章：认证”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

### 复习9 第9章：HTTP扩展

“第9章：HTTP扩展”位于本章主链“把11章、202个正式目录节点压缩成可独立复核的一条HTTP请求生命周期与四道证据门”中；需要说明它接收什么输入、改变哪份协议或应用状态、产生什么可观察输出。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“第9章：HTTP扩展”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

### 复习10 第10章：Web内容技术

“第10章：Web内容技术”位于本章主链“把11章、202个正式目录节点压缩成可独立复核的一条HTTP请求生命周期与四道证据门”中；需要说明它接收什么输入、改变哪份协议或应用状态、产生什么可观察输出。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“第10章：Web内容技术”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

### 复习11 第11章：Web攻击技术

“第11章：Web攻击技术”位于本章主链“把11章、202个正式目录节点压缩成可独立复核的一条HTTP请求生命周期与四道证据门”中；需要说明它接收什么输入、改变哪份协议或应用状态、产生什么可观察输出。

**核查动作：** 在固定请求和服务端状态下，只改变一个与“第11章：Web攻击技术”相关的变量，保存线上的字节、响应结果与状态前后值；若无法区分请求已发送和业务已完成，本节点不通过。

## 本页完整节点清单

本页承担11个目录或复习节点，正文、图解、实验和题目必须能反向定位每一项：

- 复习1 第1章：Web与网络基础
- 复习2 第2章：简单HTTP协议
- 复习3 第3章：HTTP报文信息
- 复习4 第4章：HTTP状态码
- 复习5 第5章：协作Web服务器
- 复习6 第6章：HTTP首部
- 复习7 第7章：HTTPS
- 复习8 第8章：认证
- 复习9 第9章：HTTP扩展
- 复习10 第10章：Web内容技术
- 复习11 第11章：Web攻击技术

<Stepper>
  <Step title="复原正常协议轨迹">
    固定URI、客户端、网络和服务端状态，标出请求行、首部、主体、中介与最终响应。
  </Step>
  <Step title="只改变一个变量">
    只改方法、条件首部、表示大小、连接状态、Cookie或输入上下文中的一个，先写预测再执行。
    </Step>
  <Step title="关闭独立证据门">
    对照正常与失败交换，核对线上字节、协议状态、业务副作用、恢复步骤和首版边界。
    </Step>
</Stepper>

## 可复现实验

```text
URI -> DNS -> TCP -> TLS? -> HTTP request -> intermediaries -> application -> HTTP response -> representation
```

```text
cold: 200 + representation + validators
fresh: local/shared cache hit
stale: conditional request -> 304 reuse | 200 replace
```

```text
transport identity != application identity != object authorization
all three checks must pass before state change
```

动手试：使用浏览器开发者工具、curl或抓包工具保存同一资源的正常、条件命中、条件失配和无效输入四次交换。记录方法、目标、状态码、关键首部、主体长度、连接复用、缓存/Session状态与最终副作用。

<Callout type="trap" title="把现代规范倒灌进首版">
  原书写作于HTTP/2仍在制定的时期。后来的协议、认证和浏览器安全机制可以比较，但必须明确标为后续变化，不能替换正式节点。
</Callout>

<Callout type="trap" title="把收到响应当成业务正确">
  200只说明服务器选择了成功响应；它不能证明缓存隔离、对象授权、会话轮换、内容类型或输出转义正确。必须继续核对状态与副作用。
</Callout>

## 独立证据门

最小证据包包含：首版目录节点、请求与响应原文、连接/中介方向、主体边界、缓存或会话状态、一个单变量失败、最终副作用、已知限制、恢复步骤、责任人与复核人。

## 练习

<Exercises>

**问题 1：为什么“2014年首版总复习与协议审计”必须固定2014年首版语境？**

<Answer>
  因为本书记录的是HTTP/1.1主流、SPDY与HTTP/2仍在演进的技术快照。固定版次才能让目录、术语、图解和实验结论一一对应，避免以后来的规范覆盖作者原本要解释的问题。
</Answer>

**问题 2：怎样构造“按术语列表复习会失去跨章因果；真正掌握必须让同一个请求同时通过报文、缓存、认证和攻击面审计”的最小反例？**

<Answer>
  固定URI、客户端、网络、服务器数据和操作，只改变一个方法、首部、主体、连接、Cookie或输入上下文；保存完整请求响应，比较状态、缓存/会话和最终副作用。
</Answer>

**问题 3：何时可以认为本页完成独立交接？**

<Answer>
  另一位读者无需口头补充即可定位全部11个节点，复现正常与失败交换，并依据“读者能从URI输入追踪到响应呈现或攻击阻断，逐段说明协议、中介、表示、身份和安全状态”得到相同结论。
</Answer>

</Exercises>

## 本章回顾

“2014年首版总复习与协议审计”的核心是把11章、202个正式目录节点压缩成可独立复核的一条HTTP请求生命周期与四道证据门。真正掌握不止是认出术语，而是能让一条请求在消息、连接、中介、表示、身份和攻击边界之间闭环，并用失败实验说明边界一旦被破坏会发生什么。

<Glossary>
  <GlossaryItem term="协议轨迹">
    从URI、DNS、TCP、请求、响应到浏览器处理的有序证据链。掌握标准是能在原始请求或响应中定位它，并构造一条最小反例。
  </GlossaryItem>
  <GlossaryItem term="报文边界">
    起始行、首部、空行、主体以及长度或分块终止条件。掌握标准是能在原始请求或响应中定位它，并构造一条最小反例。
  </GlossaryItem>
  <GlossaryItem term="缓存验证">
    新鲜度、实体标签、修改时间和Vary共同决定副本是否可复用。掌握标准是能在原始请求或响应中定位它，并构造一条最小反例。
  </GlossaryItem>
  <GlossaryItem term="身份边界">
    TLS对端认证、应用认证、Session和对象授权各自独立的检查点。掌握标准是能在原始请求或响应中定位它，并构造一条最小反例。
  </GlossaryItem>
  <GlossaryItem term="攻击面">
    不可信输入跨越解析器、权限、会话或资源边界时形成的可利用路径。掌握标准是能在原始请求或响应中定位它，并构造一条最小反例。
  </GlossaryItem>
</Glossary>

[← 上一页：第11章 Web的攻击技术](/learn/illustrated-http/11-chapter-11/ilh-11-web-attack-techniques)

<Attribution
  adaptedFrom="上野宣《图解HTTP》2014年4月首版"
  adaptedUrl="https://www.ituring.com.cn/book/1229"
/>
