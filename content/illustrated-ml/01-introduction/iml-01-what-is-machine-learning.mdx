---
title: "第1章 什么是机器学习"
type: B
section: "第一部分 · 绪论"
order: 1
description: "第1章 什么是机器学习覆盖5个权威目录层级，以最小二乘主线、图解推导和可复现实验重建原书核心。"
demo: true
math: true
sourceUrl: "https://www.kspub.co.jp/book/detail/1538214.html"
draft: false
---

import {
  Iml01WhatIsMachineLearningMapLab,
  Iml01WhatIsMachineLearningExperimentLab,
  Iml01WhatIsMachineLearningEvidenceLab,
} from "@/components/mdx/illustrated-ml/diagrams/iml-01-what-is-machine-learning";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第1章 什么是机器学习的模型、损失、约束和数据假设
- 能推导经验风险、正则项与核展开之间的关系
- 能复现 MATLAB 风格最小实验并定位第一处数值或泛化分叉
- 能判断“任务、输入、输出、反馈与评价损失必须在训练前固定，测试样本不参与模型选择，学习结果只对声明的数据分布与风险成立。”是否成立

</Objectives>

## 为什么从最小二乘开始

<Term def="绪论连接“冻结训练样本”的模型、损失、求解结果和泛化证据。">绪论</Term>
、
<Term def="什么是机器学习连接“构造特征与核”的模型、损失、求解结果和泛化证据。">
  什么是机器学习
</Term>
、
<Term def="学习的种类连接“写出经验风险”的模型、损失、求解结果和泛化证据。">
  学习的种类
</Term>
、
<Term def="机器学习任务的例子连接“求解参数”的模型、损失、求解结果和泛化证据。">
  机器学习任务的例子
</Term>
、
<Term def="机器学习的方法连接“选择模型”的模型、损失、求解结果和泛化证据。">
  机器学习的方法
</Term>
、
<Term def="冻结训练样本连接“验证泛化”的模型、损失、求解结果和泛化证据。">
  冻结训练样本
</Term>
共同构成本页坐标。从监督、无监督与新兴学习设定区分可用反馈，把回归、分类、异常检测和降维任务统一为从样本选择预测函数的问题。

杉山将以最小二乘法作为统一入口，因为设计矩阵、平方损失和线性方程把模型表示、求解与泛化问题同时暴露出来。加入约束得到正则化与稀疏学习，替换损失得到鲁棒学习和分类，加入核得到非线性模型，加入图结构得到半监督与降维，加入密度比则能处理异常与分布移位。统一形式不意味着任务相同，每次扩展都必须重新声明训练信号与评价风险。

实验固定样本、特征构造、随机种子、数据折分与数值容差。训练阶段保存设计矩阵、核矩阵、损失分解、正则强度、梯度或线性系统残差；模型选择阶段保存每个候选在训练折与验证折的结果；测试集直到最终选择完成后才打开。这样可以区分模型机制、优化误差和数据抽样误差。

## 数学骨架与可核查量

经验风险把样本损失汇总为训练目标：

$$
\widehat{f}=\arg\min_{f\in\mathcal{F}}\frac{1}{n}\sum_{i=1}^{n}\ell\!\left(y_i,f(x_i)\right).
$$

约束或惩罚限制模型容量：

$$
\widehat{\theta}_{\lambda}=\arg\min_{\theta}\left\|\Phi\theta-y\right\|_2^2+\lambda\,\Omega(\theta).
$$

核展开把非线性预测写成训练样本的相似度组合：

$$
f(x)=\sum_{j=1}^{n}\alpha_j k(x,x_j),\qquad K_{ij}=k(x_i,x_j).
$$

模型选择只估计未见样本风险，不证明训练目标为真：

$$
\widehat{R}_{\mathrm{val}}(\lambda)=\frac{1}{m}\sum_{r=1}^{m}\ell\!\left(y_r^{\mathrm{val}},f_{\lambda}(x_r^{\mathrm{val}})\right).
$$

```matlab
Phi = basis(x_train);
theta = solve(Phi, y_train, lambda);
assert(first_divergence(trace(theta), expected_trace) == predicted_stage);
```

## 机制辨析

1. 学习种类由反馈结构决定：监督学习给出目标，无监督学习寻找结构，半监督、迁移和多任务学习则改变可利用样本之间的关系。
2. 机器学习任务需要写成可验证输入输出合同。回归预测连续量，分类预测类别，异常检测给出偏离程度，降维保存任务相关结构。
3. 机器学习方法由模型族、损失、优化和模型选择组成。训练误差只衡量已见样本，真正目标是未来样本上的期望风险。

同一公式可以有多个等价表达，但实现应避免显式矩阵求逆。正规方程、对偶问题、核技巧或图拉普拉斯都要通过维度、对称性、半正定性、KKT 条件或残差检查。训练误差下降不是停止分析的理由；条件数过大、权重爆炸或验证误差反弹都说明解不可靠。

## 权威目录逐项讲解

### 第I部分 绪论

学习种类由反馈结构决定：监督学习给出目标，无监督学习寻找结构，半监督、迁移和多任务学习则改变可利用样本之间的关系。

验证“第I部分 绪论”时，在“冻结训练样本”固定输入和预测量，再进入“构造特征与核”只改变一个条件。保存设计矩阵或核矩阵的维度、目标函数分解、求解停止状态与留出误差；若最终分数改善但中间结构没有按机制变化，先排查泄漏、数值误差和随机性。

这一主题的通过标准包含定义、推导、算法和边界四层：能解释它优化什么，写出关键量之间的关系，复现最小 MATLAB 风格实验，并构造一个破坏前提的反例。修复后清空派生产物，从原始样本重新执行整条证据链。

### 第1章 什么是机器学习

机器学习任务需要写成可验证输入输出合同。回归预测连续量，分类预测类别，异常检测给出偏离程度，降维保存任务相关结构。

验证“第1章 什么是机器学习”时，在“构造特征与核”固定输入和预测量，再进入“写出经验风险”只改变一个条件。保存设计矩阵或核矩阵的维度、目标函数分解、求解停止状态与留出误差；若最终分数改善但中间结构没有按机制变化，先排查泄漏、数值误差和随机性。

这一主题的通过标准包含定义、推导、算法和边界四层：能解释它优化什么，写出关键量之间的关系，复现最小 MATLAB 风格实验，并构造一个破坏前提的反例。修复后清空派生产物，从原始样本重新执行整条证据链。

### 1.1 学习的种类

机器学习方法由模型族、损失、优化和模型选择组成。训练误差只衡量已见样本，真正目标是未来样本上的期望风险。

验证“1.1 学习的种类”时，在“写出经验风险”固定输入和预测量，再进入“求解参数”只改变一个条件。保存设计矩阵或核矩阵的维度、目标函数分解、求解停止状态与留出误差；若最终分数改善但中间结构没有按机制变化，先排查泄漏、数值误差和随机性。

这一主题的通过标准包含定义、推导、算法和边界四层：能解释它优化什么，写出关键量之间的关系，复现最小 MATLAB 风格实验，并构造一个破坏前提的反例。修复后清空派生产物，从原始样本重新执行整条证据链。

### 1.2 机器学习任务的例子

学习种类由反馈结构决定：监督学习给出目标，无监督学习寻找结构，半监督、迁移和多任务学习则改变可利用样本之间的关系。

验证“1.2 机器学习任务的例子”时，在“求解参数”固定输入和预测量，再进入“选择模型”只改变一个条件。保存设计矩阵或核矩阵的维度、目标函数分解、求解停止状态与留出误差；若最终分数改善但中间结构没有按机制变化，先排查泄漏、数值误差和随机性。

这一主题的通过标准包含定义、推导、算法和边界四层：能解释它优化什么，写出关键量之间的关系，复现最小 MATLAB 风格实验，并构造一个破坏前提的反例。修复后清空派生产物，从原始样本重新执行整条证据链。

### 1.3 机器学习的方法

机器学习任务需要写成可验证输入输出合同。回归预测连续量，分类预测类别，异常检测给出偏离程度，降维保存任务相关结构。

验证“1.3 机器学习的方法”时，在“选择模型”固定输入和预测量，再进入“验证泛化”只改变一个条件。保存设计矩阵或核矩阵的维度、目标函数分解、求解停止状态与留出误差；若最终分数改善但中间结构没有按机制变化，先排查泄漏、数值误差和随机性。

这一主题的通过标准包含定义、推导、算法和边界四层：能解释它优化什么，写出关键量之间的关系，复现最小 MATLAB 风格实验，并构造一个破坏前提的反例。修复后清空派生产物，从原始样本重新执行整条证据链。

## 三步交互实验

先预测哪一份矩阵、参数或损失会变化，再切换基线、边界、失败与优化样本。滑动样本量时同时观察估计方差和计算成本，不挑选单次最好结果。

<Stepper>
  <Step title="画出模型地图">
    标出样本、特征、参数、损失、约束与评价数据的生产者和消费者。
    <Iml01WhatIsMachineLearningMapLab />
  </Step>
  <Step title="运行单变量实验">
    只改变一个正则强度、核宽、损失阈值、邻接图或分布权重，先预测第一处分叉。
    <Iml01WhatIsMachineLearningExperimentLab />
  </Step>
  <Step title="核对泛化证据">
    并排比较参数、训练损失、验证损失与测试边界；定位首错后从原始样本重放。
    <Iml01WhatIsMachineLearningEvidenceLab />
  </Step>
</Stepper>

## 常见失败与边界

<Callout type="trap">
  **把训练最优当成预测最优：**
  更小训练损失可能来自更大容量、异常点追随或数据泄漏。必须用独立验证、简单基线和重采样区间检验“选择模型”。
</Callout>

<Callout type="trap">
  **显式求逆掩盖病态：** 直接计算逆矩阵会放大舍入误差。优先使用
  QR、SVD、Cholesky 或迭代求解，并保存条件数、残差和停止原因。
</Callout>

## 本章回顾

从监督、无监督与新兴学习设定区分可用反馈，把回归、分类、异常检测和降维任务统一为从样本选择预测函数的问题。 验收时必须从固定样本预测矩阵、损失和参数，解释约束或数据关系为何改变解，并在失败样本中找到第一处违背假设的位置。最终结论要求：任务、输入、输出、反馈与评价损失必须在训练前固定，测试样本不参与模型选择，学习结果只对声明的数据分布与风险成立。

<Glossary>

<GlossaryItem term="绪论">
  “绪论”用于冻结训练样本：它必须关联数据版本、数学假设、结构化中间量、求解状态和失效边界，不能只保留一个最终分数。
</GlossaryItem>

<GlossaryItem term="什么是机器学习">
  “什么是机器学习”用于构造特征与核：它必须关联数据版本、数学假设、结构化中间量、求解状态和失效边界，不能只保留一个最终分数。
</GlossaryItem>

<GlossaryItem term="学习的种类">
  “学习的种类”用于写出经验风险：它必须关联数据版本、数学假设、结构化中间量、求解状态和失效边界，不能只保留一个最终分数。
</GlossaryItem>

<GlossaryItem term="机器学习任务的例子">
  “机器学习任务的例子”用于求解参数：它必须关联数据版本、数学假设、结构化中间量、求解状态和失效边界，不能只保留一个最终分数。
</GlossaryItem>

<GlossaryItem term="机器学习的方法">
  “机器学习的方法”用于选择模型：它必须关联数据版本、数学假设、结构化中间量、求解状态和失效边界，不能只保留一个最终分数。
</GlossaryItem>

<GlossaryItem term="冻结训练样本">
  “冻结训练样本”用于验证泛化：它必须关联数据版本、数学假设、结构化中间量、求解状态和失效边界，不能只保留一个最终分数。
</GlossaryItem>

</Glossary>

<Exercises>

1. 如何为第1章 什么是机器学习建立不泄漏测试集的最小复现实验？

<Answer>
先固定样本标识并划分训练、验证、测试三部分，所有标准化、核宽、邻接图和特征选择只在训练折拟合。按“冻结训练样本 -> 构造特征与核 -> 写出经验风险 -> 求解参数 -> 选择模型 -> 验证泛化”保存矩阵维度、目标值、求解残差和验证误差，模型选择完成后测试集只使用一次。
</Answer>

2. 如何判断复杂模型相对最小二乘基线的提升来自机制而非数值或抽样偶然？

<Answer>
  保持数据折分与随机种子不变，只替换目标中的一个损失、约束、核或样本关系；先预测参数和中间量的变化，再比较多折或多种子的验证分布。若结构变化不符合推导，先修复求解和数据管线，不采信最终分数。
</Answer>

3. 为“任务、输入、输出、反馈与评价损失必须在训练前固定，测试样本不参与模型选择，学习结果只对声明的数据分布与风险成立。”设计一个失败样本，并说明应在何处停止。

<Answer>
  可选择重复列制造病态、污染极端标签、使用非半正定核、让近邻图跨越类别、引入测试预处理或制造分布无重叠。预测它最先破坏设计矩阵、KKT
  条件、权重方差或验证合同的位置，运行到首个不一致立即停止，下游结果全部作废。
</Answer>

</Exercises>

<Attribution
  adaptedFrom="杉山将著、许永伟译《图解机器学习》，人民邮电出版社，2015，226页，ISBN 9787115388025；原书《イラストで学ぶ 機械学習》，讲谈社，2013，232页，ISBN 9784061538214 · 基于讲谈社官方6部分20章目录、中文版62个编号小节完整目录与中文版书目信息的独立教学重构"
  adaptedUrl="https://www.kspub.co.jp/book/detail/1538214.html"
/>
