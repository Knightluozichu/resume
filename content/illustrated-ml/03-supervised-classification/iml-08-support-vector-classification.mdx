---
title: "第8章 支持向量机分类"
type: B
section: "第三部分 · 有监督分类"
order: 8
description: "第8章 支持向量机分类覆盖7个权威目录层级，以最小二乘主线、图解推导和可复现实验重建原书核心。"
demo: true
math: true
sourceUrl: "https://www.kspub.co.jp/book/detail/1538214.html"
draft: false
---

import {
  Iml08SupportVectorClassificationMapLab,
  Iml08SupportVectorClassificationExperimentLab,
  Iml08SupportVectorClassificationEvidenceLab,
} from "@/components/mdx/illustrated-ml/diagrams/iml-08-support-vector-classification";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第8章 支持向量机分类的模型、损失、约束和数据假设
- 能推导经验风险、正则项与核展开之间的关系
- 能复现 MATLAB 风格最小实验并定位第一处数值或泛化分叉
- 能判断“间隔与特征尺度一致，对偶变量满足约束和互补条件；核矩阵半正定，软间隔与核参数只由训练折选择。”是否成立

</Objectives>

## 为什么从最小二乘开始

<Term def="支持向量机分类连接“冻结训练样本”的模型、损失、求解结果和泛化证据。">支持向量机分类</Term>、<Term def="间隔最大化分类连接“构造特征与核”的模型、损失、求解结果和泛化证据。">间隔最大化分类</Term>、<Term def="支持向量机分类器的求解方法连接“写出经验风险”的模型、损失、求解结果和泛化证据。">支持向量机分类器的求解方法</Term>、<Term def="稀疏性连接“求解参数”的模型、损失、求解结果和泛化证据。">稀疏性</Term>、<Term def="使用核映射的非线性模型连接“选择模型”的模型、损失、求解结果和泛化证据。">使用核映射的非线性模型</Term>、<Term def="使用Hinge损失最小化学习来解释连接“验证泛化”的模型、损失、求解结果和泛化证据。">使用Hinge损失最小化学习来解释</Term>
共同构成本页坐标。从最大间隔原始问题进入对偶求解与支持向量稀疏性，用核映射构造非线性边界，并比较
Hinge 与 Ramp 损失的鲁棒性。

杉山将以最小二乘法作为统一入口，因为设计矩阵、平方损失和线性方程把模型表示、求解与泛化问题同时暴露出来。加入约束得到正则化与稀疏学习，替换损失得到鲁棒学习和分类，加入核得到非线性模型，加入图结构得到半监督与降维，加入密度比则能处理异常与分布移位。统一形式不意味着任务相同，每次扩展都必须重新声明训练信号与评价风险。

实验固定样本、特征构造、随机种子、数据折分与数值容差。训练阶段保存设计矩阵、核矩阵、损失分解、正则强度、梯度或线性系统残差；模型选择阶段保存每个候选在训练折与验证折的结果；测试集直到最终选择完成后才打开。这样可以区分模型机制、优化误差和数据抽样误差。

## 数学骨架与可核查量

经验风险把样本损失汇总为训练目标：

$$
\widehat{f}=\arg\min_{f\in\mathcal{F}}\frac{1}{n}\sum_{i=1}^{n}\ell\!\left(y_i,f(x_i)\right).
$$

约束或惩罚限制模型容量：

$$
\widehat{\theta}_{\lambda}=\arg\min_{\theta}\left\|\Phi\theta-y\right\|_2^2+\lambda\,\Omega(\theta).
$$

核展开把非线性预测写成训练样本的相似度组合：

$$
f(x)=\sum_{j=1}^{n}\alpha_j k(x,x_j),\qquad K_{ij}=k(x_i,x_j).
$$

模型选择只估计未见样本风险，不证明训练目标为真：

$$
\widehat{R}_{\mathrm{val}}(\lambda)=\frac{1}{m}\sum_{r=1}^{m}\ell\!\left(y_r^{\mathrm{val}},f_{\lambda}(x_r^{\mathrm{val}})\right).
$$

```matlab
Phi = basis(x_train);
theta = solve(Phi, y_train, lambda);
assert(first_divergence(trace(theta), expected_trace) == predicted_stage);
```

## 机制辨析

1. 最大间隔分类在正确分隔样本的同时最小化权重范数，软间隔用松弛变量允许噪声与重叠。
2. 二次规划或序列最小优化求解对偶问题，KKT 残差和对偶间隙用于判断收敛。
3. 只有支持向量具有非零对偶系数，因此预测依赖训练样本子集；稀疏度随正则和核宽变化。
4. 核映射通过内积计算非线性模型，RBF、Polynomial 等核编码不同相似性，带宽过小容易记忆样本。
5. Hinge 损失是 0/1 损失的凸上界，间隔内样本产生梯度，间隔外正确样本不再影响解。
6. Ramp 损失截断极端负间隔以降低异常点影响，但目标非凸，需要多初始化与污染实验。

同一公式可以有多个等价表达，但实现应避免显式矩阵求逆。正规方程、对偶问题、核技巧或图拉普拉斯都要通过维度、对称性、半正定性、KKT 条件或残差检查。训练误差下降不是停止分析的理由；条件数过大、权重爆炸或验证误差反弹都说明解不可靠。

## 权威目录逐项讲解

### 第8章 支持向量机分类

最大间隔分类在正确分隔样本的同时最小化权重范数，软间隔用松弛变量允许噪声与重叠。

验证“第8章 支持向量机分类”时，在“冻结训练样本”固定输入和预测量，再进入“构造特征与核”只改变一个条件。保存设计矩阵或核矩阵的维度、目标函数分解、求解停止状态与留出误差；若最终分数改善但中间结构没有按机制变化，先排查泄漏、数值误差和随机性。

这一主题的通过标准包含定义、推导、算法和边界四层：能解释它优化什么，写出关键量之间的关系，复现最小 MATLAB 风格实验，并构造一个破坏前提的反例。修复后清空派生产物，从原始样本重新执行整条证据链。

### 8.1 间隔最大化分类

二次规划或序列最小优化求解对偶问题，KKT 残差和对偶间隙用于判断收敛。

验证“8.1 间隔最大化分类”时，在“构造特征与核”固定输入和预测量，再进入“写出经验风险”只改变一个条件。保存设计矩阵或核矩阵的维度、目标函数分解、求解停止状态与留出误差；若最终分数改善但中间结构没有按机制变化，先排查泄漏、数值误差和随机性。

这一主题的通过标准包含定义、推导、算法和边界四层：能解释它优化什么，写出关键量之间的关系，复现最小 MATLAB 风格实验，并构造一个破坏前提的反例。修复后清空派生产物，从原始样本重新执行整条证据链。

### 8.2 支持向量机分类器的求解方法

只有支持向量具有非零对偶系数，因此预测依赖训练样本子集；稀疏度随正则和核宽变化。

验证“8.2 支持向量机分类器的求解方法”时，在“写出经验风险”固定输入和预测量，再进入“求解参数”只改变一个条件。保存设计矩阵或核矩阵的维度、目标函数分解、求解停止状态与留出误差；若最终分数改善但中间结构没有按机制变化，先排查泄漏、数值误差和随机性。

这一主题的通过标准包含定义、推导、算法和边界四层：能解释它优化什么，写出关键量之间的关系，复现最小 MATLAB 风格实验，并构造一个破坏前提的反例。修复后清空派生产物，从原始样本重新执行整条证据链。

### 8.3 稀疏性

核映射通过内积计算非线性模型，RBF、Polynomial 等核编码不同相似性，带宽过小容易记忆样本。

验证“8.3 稀疏性”时，在“求解参数”固定输入和预测量，再进入“选择模型”只改变一个条件。保存设计矩阵或核矩阵的维度、目标函数分解、求解停止状态与留出误差；若最终分数改善但中间结构没有按机制变化，先排查泄漏、数值误差和随机性。

这一主题的通过标准包含定义、推导、算法和边界四层：能解释它优化什么，写出关键量之间的关系，复现最小 MATLAB 风格实验，并构造一个破坏前提的反例。修复后清空派生产物，从原始样本重新执行整条证据链。

### 8.4 使用核映射的非线性模型

Hinge 损失是 0/1 损失的凸上界，间隔内样本产生梯度，间隔外正确样本不再影响解。

验证“8.4 使用核映射的非线性模型”时，在“选择模型”固定输入和预测量，再进入“验证泛化”只改变一个条件。保存设计矩阵或核矩阵的维度、目标函数分解、求解停止状态与留出误差；若最终分数改善但中间结构没有按机制变化，先排查泄漏、数值误差和随机性。

这一主题的通过标准包含定义、推导、算法和边界四层：能解释它优化什么，写出关键量之间的关系，复现最小 MATLAB 风格实验，并构造一个破坏前提的反例。修复后清空派生产物，从原始样本重新执行整条证据链。

### 8.5 使用Hinge损失最小化学习来解释

Ramp 损失截断极端负间隔以降低异常点影响，但目标非凸，需要多初始化与污染实验。

验证“8.5 使用Hinge损失最小化学习来解释”时，在“验证泛化”固定输入和预测量，再进入“冻结训练样本”只改变一个条件。保存设计矩阵或核矩阵的维度、目标函数分解、求解停止状态与留出误差；若最终分数改善但中间结构没有按机制变化，先排查泄漏、数值误差和随机性。

这一主题的通过标准包含定义、推导、算法和边界四层：能解释它优化什么，写出关键量之间的关系，复现最小 MATLAB 风格实验，并构造一个破坏前提的反例。修复后清空派生产物，从原始样本重新执行整条证据链。

### 8.6 使用Ramp损失的鲁棒学习

最大间隔分类在正确分隔样本的同时最小化权重范数，软间隔用松弛变量允许噪声与重叠。

验证“8.6 使用Ramp损失的鲁棒学习”时，在“冻结训练样本”固定输入和预测量，再进入“构造特征与核”只改变一个条件。保存设计矩阵或核矩阵的维度、目标函数分解、求解停止状态与留出误差；若最终分数改善但中间结构没有按机制变化，先排查泄漏、数值误差和随机性。

这一主题的通过标准包含定义、推导、算法和边界四层：能解释它优化什么，写出关键量之间的关系，复现最小 MATLAB 风格实验，并构造一个破坏前提的反例。修复后清空派生产物，从原始样本重新执行整条证据链。

## 三步交互实验

先预测哪一份矩阵、参数或损失会变化，再切换基线、边界、失败与优化样本。滑动样本量时同时观察估计方差和计算成本，不挑选单次最好结果。

<Stepper>
  <Step title="画出模型地图">
    标出样本、特征、参数、损失、约束与评价数据的生产者和消费者。
    <Iml08SupportVectorClassificationMapLab />
  </Step>
  <Step title="运行单变量实验">
    只改变一个正则强度、核宽、损失阈值、邻接图或分布权重，先预测第一处分叉。
    <Iml08SupportVectorClassificationExperimentLab />
  </Step>
  <Step title="核对泛化证据">
    并排比较参数、训练损失、验证损失与测试边界；定位首错后从原始样本重放。
    <Iml08SupportVectorClassificationEvidenceLab />
  </Step>
</Stepper>

## 常见失败与边界

<Callout type="trap">
  **把训练最优当成预测最优：**
  更小训练损失可能来自更大容量、异常点追随或数据泄漏。必须用独立验证、简单基线和重采样区间检验“选择模型”。
</Callout>

<Callout type="trap">
  **显式求逆掩盖病态：** 直接计算逆矩阵会放大舍入误差。优先使用
  QR、SVD、Cholesky 或迭代求解，并保存条件数、残差和停止原因。
</Callout>

## 本章回顾

从最大间隔原始问题进入对偶求解与支持向量稀疏性，用核映射构造非线性边界，并比较 Hinge 与 Ramp 损失的鲁棒性。 验收时必须从固定样本预测矩阵、损失和参数，解释约束或数据关系为何改变解，并在失败样本中找到第一处违背假设的位置。最终结论要求：间隔与特征尺度一致，对偶变量满足约束和互补条件；核矩阵半正定，软间隔与核参数只由训练折选择。

<Glossary>

<GlossaryItem term="支持向量机分类">
  “支持向量机分类”用于冻结训练样本：它必须关联数据版本、数学假设、结构化中间量、求解状态和失效边界，不能只保留一个最终分数。
</GlossaryItem>

<GlossaryItem term="间隔最大化分类">
  “间隔最大化分类”用于构造特征与核：它必须关联数据版本、数学假设、结构化中间量、求解状态和失效边界，不能只保留一个最终分数。
</GlossaryItem>

<GlossaryItem term="支持向量机分类器的求解方法">
  “支持向量机分类器的求解方法”用于写出经验风险：它必须关联数据版本、数学假设、结构化中间量、求解状态和失效边界，不能只保留一个最终分数。
</GlossaryItem>

<GlossaryItem term="稀疏性">
  “稀疏性”用于求解参数：它必须关联数据版本、数学假设、结构化中间量、求解状态和失效边界，不能只保留一个最终分数。
</GlossaryItem>

<GlossaryItem term="使用核映射的非线性模型">
  “使用核映射的非线性模型”用于选择模型：它必须关联数据版本、数学假设、结构化中间量、求解状态和失效边界，不能只保留一个最终分数。
</GlossaryItem>

<GlossaryItem term="使用Hinge损失最小化学习来解释">
  “使用Hinge损失最小化学习来解释”用于验证泛化：它必须关联数据版本、数学假设、结构化中间量、求解状态和失效边界，不能只保留一个最终分数。
</GlossaryItem>

</Glossary>

<Exercises>

1. 如何为第8章 支持向量机分类建立不泄漏测试集的最小复现实验？

<Answer>
先固定样本标识并划分训练、验证、测试三部分，所有标准化、核宽、邻接图和特征选择只在训练折拟合。按“冻结训练样本 -> 构造特征与核 -> 写出经验风险 -> 求解参数 -> 选择模型 -> 验证泛化”保存矩阵维度、目标值、求解残差和验证误差，模型选择完成后测试集只使用一次。
</Answer>

2. 如何判断复杂模型相对最小二乘基线的提升来自机制而非数值或抽样偶然？

<Answer>
  保持数据折分与随机种子不变，只替换目标中的一个损失、约束、核或样本关系；先预测参数和中间量的变化，再比较多折或多种子的验证分布。若结构变化不符合推导，先修复求解和数据管线，不采信最终分数。
</Answer>

3. 为“间隔与特征尺度一致，对偶变量满足约束和互补条件；核矩阵半正定，软间隔与核参数只由训练折选择。”设计一个失败样本，并说明应在何处停止。

<Answer>
  可选择重复列制造病态、污染极端标签、使用非半正定核、让近邻图跨越类别、引入测试预处理或制造分布无重叠。预测它最先破坏设计矩阵、KKT
  条件、权重方差或验证合同的位置，运行到首个不一致立即停止，下游结果全部作废。
</Answer>

</Exercises>

<Attribution
  adaptedFrom="杉山将著、许永伟译《图解机器学习》，人民邮电出版社，2015，226页，ISBN 9787115388025；原书《イラストで学ぶ 機械学習》，讲谈社，2013，232页，ISBN 9784061538214 · 基于讲谈社官方6部分20章目录、中文版62个编号小节完整目录与中文版书目信息的独立教学重构"
  adaptedUrl="https://www.kspub.co.jp/book/detail/1538214.html"
/>
