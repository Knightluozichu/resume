---
title: "第2章：新能源汽车电机"
type: B
section: "图解新能源汽车原理与构造（2023版）"
order: 4
description: "本章比较直流、交流异步、永磁同步、开关磁阻和轮毂电机的定转子结构、磁场形成、转矩产生、控制接口与冷却路径。电机选型是效率、转矩密度、成本和控制复杂度的折中。"
demo: true
math: true
sourceUrl: "https://e.dangdang.com/products/1901394961.html#catalogModule"
draft: false
---


## 为什么从可验证能量路径开始

本页按授权电子版完整目录重建 **第2章 新能源汽车电机**，覆盖该正式单元的 15 个目录条目。本章比较直流、交流异步、永磁同步、开关磁阻和轮毂电机的定转子结构、磁场形成、转矩产生、控制接口与冷却路径。电机选型是效率、转矩密度、成本和控制复杂度的折中。

原书以彩色简图、剖面图、分解图和典型车型说明结构。本项目不复制原图与正文，而是把每个目录条目独立转写成可操作的工程教学：先辨认能源或介质，再定位容器、转换器、执行器、控制器与安全装置，最后用能量流、信号流和故障流复核。

车型图解最容易出现三类误读：把同品牌不同年款混在一起，把外观相似的部件视为同规格，把界面或宣传参数当成实车拓扑。每次实验都固定2023版目录、车型、市场配置、前状态和测点；正常样本复原主路径，边界样本选择峰值或阈值，单故障样本只改变一个传感器、阀门、接触器、压力或温度条件。

转矩、转速、电流、磁链与热量必须闭合；连续工况不能用峰值功率替代，冷却能力限制可持续输出。 涉及高压、压力容器、旋转件、氢气、CNG或LPG时，本页只做模型和隔离台架推演；实车操作必须由具备资质的人员按厂商维修资料、断电验电、泄压和防火要求执行。

通过标准是：能在统一工况下比较五类电机，指出磁场来源、转差或同步关系、控制变量和热路径。 独立复核者应能从相同车型资料和初始条件重建能量、信号与故障轨迹；只保存最终读数而没有连接状态、测点、单位和首个异常，视为失败。

## 版次、对象与验收合同

、、、、共同构成本页复核坐标。本章比较直流、交流异步、永磁同步、开关磁阻和轮毂电机的定转子结构、磁场形成、转矩产生、控制接口与冷却路径。电机选型是效率、转矩密度、成本和控制复杂度的折中。


## 核心约束与推导

$$
P_{electric}=U I, \quad E=\int_{t_0}^{t_1} P(t)\,dt
$$

$$
F_{traction}=\frac{T_{motor} i_g \eta_g}{r_w}
$$

$$
\eta_{system}=\frac{E_{wheel}}{E_{source}}, \quad E_{loss}=E_{source}-E_{wheel}
$$

$$
gate=source \times modelYear \times topology \times boundary \times failureEvidence
$$

电功率式用于高压和电驱链，牵引力式把电机、传动比、效率与车轮半径连接起来，能量效率式要求把热与转换损失显式列出。证据门禁采用乘法语义，车型年款、拓扑或故障证据缺失时，漂亮的结构图也不能发布。

## 三段可运行骨架

```typescript
type VehicleEvidence = {
  edition: "2023";
  model: string;
  before: Record<string, number>;
  input: Record<string, number>;
  after?: Record<string, number>;
  error?: string;
};

export function evidenceGate(run: VehicleEvidence) {
  return {
    page: "ine23-02-motors",
    accepted: Boolean(run.after || run.error),
    run,
  };
}
```

```python
def power_balance(source_kw, wheel_kw, thermal_kw, auxiliary_kw):
    residual = source_kw - wheel_kw - thermal_kw - auxiliary_kw
    return {"residual_kw": residual, "valid": abs(residual) <= 0.1}
```

```python
def apply_component(state, command, verify):
    if not verify(state, command):
        return {"accepted": False, "state": state, "reason": "interlock"}
    next_state = dict(state)
    next_state.update(command["changes"])
    return {"accepted": True, "state": next_state, "reason": None}
```

第一段固定版次、车型和测点；第二段用功率残差发现漏记的损失或支路；第三段证明联锁拒绝时保持原状态。实车诊断必须使用厂商工具和安全规程，教学代码不直接控制车辆。

## 正式目录逐项讲解

### 第2章 新能源汽车电机

“第2章 新能源汽车电机”是本单元正式目录坐标 1/15。这是机械与电驱转换坐标。标出能源入口、磁场或齿轮耦合、转矩出口、控制量、损耗和热路径；峰值、持续值与再生方向必须分开。

复核时先标注车型年款、能源或介质、部件接口、控制输入和安全边界，再记录正常、阈值和单故障的测点。转矩、转速、电流、磁链与热量必须闭合；连续工况不能用峰值功率替代，冷却能力限制可持续输出。 本条目通过后还要说明它与上级总成和下一条目的连接，不能把多个独立部件折叠成一句“三电系统”。

### 2.1 直流电机

“2.1 直流电机”是本单元正式目录坐标 2/15。这是机械与电驱转换坐标。标出能源入口、磁场或齿轮耦合、转矩出口、控制量、损耗和热路径；峰值、持续值与再生方向必须分开。

复核时先标注车型年款、能源或介质、部件接口、控制输入和安全边界，再记录正常、阈值和单故障的测点。转矩、转速、电流、磁链与热量必须闭合；连续工况不能用峰值功率替代，冷却能力限制可持续输出。 本条目通过后还要说明它与上级总成和下一条目的连接，不能把多个独立部件折叠成一句“三电系统”。

### 2.1.1 直流电机结构

“2.1.1 直流电机结构”是本单元正式目录坐标 3/15。这是机械与电驱转换坐标。标出能源入口、磁场或齿轮耦合、转矩出口、控制量、损耗和热路径；峰值、持续值与再生方向必须分开。

复核时先标注车型年款、能源或介质、部件接口、控制输入和安全边界，再记录正常、阈值和单故障的测点。转矩、转速、电流、磁链与热量必须闭合；连续工况不能用峰值功率替代，冷却能力限制可持续输出。 本条目通过后还要说明它与上级总成和下一条目的连接，不能把多个独立部件折叠成一句“三电系统”。

### 2.1.2 直流电机工作原理

“2.1.2 直流电机工作原理”是本单元正式目录坐标 4/15。这是机械与电驱转换坐标。标出能源入口、磁场或齿轮耦合、转矩出口、控制量、损耗和热路径；峰值、持续值与再生方向必须分开。

复核时先标注车型年款、能源或介质、部件接口、控制输入和安全边界，再记录正常、阈值和单故障的测点。转矩、转速、电流、磁链与热量必须闭合；连续工况不能用峰值功率替代，冷却能力限制可持续输出。 本条目通过后还要说明它与上级总成和下一条目的连接，不能把多个独立部件折叠成一句“三电系统”。

### 2.2 交流异步电机

“2.2 交流异步电机”是本单元正式目录坐标 5/15。这是机械与电驱转换坐标。标出能源入口、磁场或齿轮耦合、转矩出口、控制量、损耗和热路径；峰值、持续值与再生方向必须分开。

复核时先标注车型年款、能源或介质、部件接口、控制输入和安全边界，再记录正常、阈值和单故障的测点。转矩、转速、电流、磁链与热量必须闭合；连续工况不能用峰值功率替代，冷却能力限制可持续输出。 本条目通过后还要说明它与上级总成和下一条目的连接，不能把多个独立部件折叠成一句“三电系统”。

### 2.2.1 交流异步电机结构

“2.2.1 交流异步电机结构”是本单元正式目录坐标 6/15。这是机械与电驱转换坐标。标出能源入口、磁场或齿轮耦合、转矩出口、控制量、损耗和热路径；峰值、持续值与再生方向必须分开。

复核时先标注车型年款、能源或介质、部件接口、控制输入和安全边界，再记录正常、阈值和单故障的测点。转矩、转速、电流、磁链与热量必须闭合；连续工况不能用峰值功率替代，冷却能力限制可持续输出。 本条目通过后还要说明它与上级总成和下一条目的连接，不能把多个独立部件折叠成一句“三电系统”。

### 2.2.2 交流异步电机工作原理

“2.2.2 交流异步电机工作原理”是本单元正式目录坐标 7/15。这是机械与电驱转换坐标。标出能源入口、磁场或齿轮耦合、转矩出口、控制量、损耗和热路径；峰值、持续值与再生方向必须分开。

复核时先标注车型年款、能源或介质、部件接口、控制输入和安全边界，再记录正常、阈值和单故障的测点。转矩、转速、电流、磁链与热量必须闭合；连续工况不能用峰值功率替代，冷却能力限制可持续输出。 本条目通过后还要说明它与上级总成和下一条目的连接，不能把多个独立部件折叠成一句“三电系统”。

### 2.3 永磁同步电机

“2.3 永磁同步电机”是本单元正式目录坐标 8/15。这是机械与电驱转换坐标。标出能源入口、磁场或齿轮耦合、转矩出口、控制量、损耗和热路径；峰值、持续值与再生方向必须分开。

复核时先标注车型年款、能源或介质、部件接口、控制输入和安全边界，再记录正常、阈值和单故障的测点。转矩、转速、电流、磁链与热量必须闭合；连续工况不能用峰值功率替代，冷却能力限制可持续输出。 本条目通过后还要说明它与上级总成和下一条目的连接，不能把多个独立部件折叠成一句“三电系统”。

### 2.3.1 永磁同步电机结构

“2.3.1 永磁同步电机结构”是本单元正式目录坐标 9/15。这是机械与电驱转换坐标。标出能源入口、磁场或齿轮耦合、转矩出口、控制量、损耗和热路径；峰值、持续值与再生方向必须分开。

复核时先标注车型年款、能源或介质、部件接口、控制输入和安全边界，再记录正常、阈值和单故障的测点。转矩、转速、电流、磁链与热量必须闭合；连续工况不能用峰值功率替代，冷却能力限制可持续输出。 本条目通过后还要说明它与上级总成和下一条目的连接，不能把多个独立部件折叠成一句“三电系统”。

### 2.3.2 永磁同步电机工作原理

“2.3.2 永磁同步电机工作原理”是本单元正式目录坐标 10/15。这是机械与电驱转换坐标。标出能源入口、磁场或齿轮耦合、转矩出口、控制量、损耗和热路径；峰值、持续值与再生方向必须分开。

复核时先标注车型年款、能源或介质、部件接口、控制输入和安全边界，再记录正常、阈值和单故障的测点。转矩、转速、电流、磁链与热量必须闭合；连续工况不能用峰值功率替代，冷却能力限制可持续输出。 本条目通过后还要说明它与上级总成和下一条目的连接，不能把多个独立部件折叠成一句“三电系统”。

### 2.4 开关磁阻电机

“2.4 开关磁阻电机”是本单元正式目录坐标 11/15。这是机械与电驱转换坐标。标出能源入口、磁场或齿轮耦合、转矩出口、控制量、损耗和热路径；峰值、持续值与再生方向必须分开。

复核时先标注车型年款、能源或介质、部件接口、控制输入和安全边界，再记录正常、阈值和单故障的测点。转矩、转速、电流、磁链与热量必须闭合；连续工况不能用峰值功率替代，冷却能力限制可持续输出。 本条目通过后还要说明它与上级总成和下一条目的连接，不能把多个独立部件折叠成一句“三电系统”。

### 2.4.1 开关磁阻电机结构

“2.4.1 开关磁阻电机结构”是本单元正式目录坐标 12/15。这是机械与电驱转换坐标。标出能源入口、磁场或齿轮耦合、转矩出口、控制量、损耗和热路径；峰值、持续值与再生方向必须分开。

复核时先标注车型年款、能源或介质、部件接口、控制输入和安全边界，再记录正常、阈值和单故障的测点。转矩、转速、电流、磁链与热量必须闭合；连续工况不能用峰值功率替代，冷却能力限制可持续输出。 本条目通过后还要说明它与上级总成和下一条目的连接，不能把多个独立部件折叠成一句“三电系统”。

### 2.4.2 开关磁阻电机工作原理

“2.4.2 开关磁阻电机工作原理”是本单元正式目录坐标 13/15。这是机械与电驱转换坐标。标出能源入口、磁场或齿轮耦合、转矩出口、控制量、损耗和热路径；峰值、持续值与再生方向必须分开。

复核时先标注车型年款、能源或介质、部件接口、控制输入和安全边界，再记录正常、阈值和单故障的测点。转矩、转速、电流、磁链与热量必须闭合；连续工况不能用峰值功率替代，冷却能力限制可持续输出。 本条目通过后还要说明它与上级总成和下一条目的连接，不能把多个独立部件折叠成一句“三电系统”。

### 2.5 轮毂电机

“2.5 轮毂电机”是本单元正式目录坐标 14/15。这是机械与电驱转换坐标。标出能源入口、磁场或齿轮耦合、转矩出口、控制量、损耗和热路径；峰值、持续值与再生方向必须分开。

复核时先标注车型年款、能源或介质、部件接口、控制输入和安全边界，再记录正常、阈值和单故障的测点。转矩、转速、电流、磁链与热量必须闭合；连续工况不能用峰值功率替代，冷却能力限制可持续输出。 本条目通过后还要说明它与上级总成和下一条目的连接，不能把多个独立部件折叠成一句“三电系统”。

### 2.6 电机冷却系统

“2.6 电机冷却系统”是本单元正式目录坐标 15/15。这是机械与电驱转换坐标。标出能源入口、磁场或齿轮耦合、转矩出口、控制量、损耗和热路径；峰值、持续值与再生方向必须分开。

复核时先标注车型年款、能源或介质、部件接口、控制输入和安全边界，再记录正常、阈值和单故障的测点。转矩、转速、电流、磁链与热量必须闭合；连续工况不能用峰值功率替代，冷却能力限制可持续输出。 本条目通过后还要说明它与上级总成和下一条目的连接，不能把多个独立部件折叠成一句“三电系统”。

## 正常、边界与故障矩阵

| 样本   | 只改变的变量                         | 预期结果                   | 必存证据                     |
| ------ | ------------------------------------ | -------------------------- | ---------------------------- |
| 正常   | 合法能源、温度、压力、电压与联锁     | 能量沿预期路径到达执行器   | 拓扑、测点、输入输出与损失   |
| 边界   | 峰值、持续值、恰好阈值或温度边缘     | 限功率、切换模式或明确拒绝 | 阈值、控制分支、热或压力状态 |
| 单故障 | 传感器、阀门、接触器、冷却或连接一项 | 进入定义的安全状态         | 首差、故障码、原状态与回退点 |

先预测能量流、部件状态与测点，再操作控件。若同时改变负载与温度，就无法判断限功率来自哪一项，必须重置基线。

## 常见失败与回退


## 本章回顾

本章比较直流、交流异步、永磁同步、开关磁阻和轮毂电机的定转子结构、磁场形成、转矩产生、控制接口与冷却路径。电机选型是效率、转矩密度、成本和控制复杂度的折中。 掌握标准不是记住零件名称，而是能把总成图变成可验证拓扑。转矩、转速、电流、磁链与热量必须闭合；连续工况不能用峰值功率替代，冷却能力限制可持续输出。 每次复核结束都要保存不含隐私的资料版本、车型、测点、单位、状态轨迹和拒绝原因。


## 前后导航

- [上一页：第1章：新能源汽车分类](/learn/illustrated-nev/03-01-classification/ine23-01-classification)
- [下一页：第3章：新能源汽车电池](/learn/illustrated-nev/05-03-batteries/ine23-03-batteries)

