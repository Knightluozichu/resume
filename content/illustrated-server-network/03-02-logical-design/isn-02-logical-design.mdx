---
title: "第2章 逻辑设计"
description: "贯通以太网成帧、MAC交换、VLAN、ARP、IPv4、路由、NAT、DHCP、ICMP并形成可汇总的VLAN与地址设计"
type: C
section: "图解服务器端网络架构（2015年首版）"
order: 3
draft: true
math: false
sourceUrl: "https://www.ituring.com.cn/book/1494"
demo: true
---

import {
  Isn02LogicalDesignPlanLab,
  Isn02LogicalDesignFaultLab,
  Isn02LogicalDesignEvidenceLab,
} from "@/components/mdx/illustrated-server-network/diagrams/isn-02-logical-design";

# 第2章 逻辑设计

本课程对应[日]宫田宽士《图解服务器端网络架构》，曾薇薇译、乌尼日其其格审校，人民邮电出版社/图灵教育，2015年4月第1版第1次印刷，376页、478千字、16开，ISBN 9787115388179；原书ISBN 9784797373516。图灵官方资料标明467张图表，官方试读PDF包含CIP、版次页和完整目录。首版正式结构为第0章加第1至5章，共171个目录节点；课程不会用2024年第2版、云原生、Service Mesh或现代API网关替换原书内容。

<Objectives>

- 能解释“第2章 逻辑设计”全部正式节点，并把每项技术落到一份可施工设计表。
- 能绘制正反向通信流、责任层、状态表和故障替代路径。
- 能设计单变量故障实验，验证“每个广播域、IP网段、默认网关、路由与NAT方向都有唯一责任，正反向通信流均能由表项和报文证据解释”。
- 能写出并提交含需求追溯、容量、告警、恢复和首版边界的独立证据包。

</Objectives>

## 从一条可证伪的通信流开始

先预测：把VLAN、IP网段和安全区随意混用，或只验证去程不验证回程，会产生广播泄漏、地址冲突、非对称路由和NAT故障。把预测写成“需求、正常路径、状态/表项、单故障路径、告警与恢复”五列，再接线、配置或测试。若结果与预测不同，先修正设计模型，不要把现场补丁当作架构成立。

本章的主问题是：贯通以太网成帧、MAC交换、VLAN、ARP、IPv4、路由、NAT、DHCP、ICMP并形成可汇总的VLAN与地址设计。每个箭头要注明源/目的、接口、VLAN、地址、协议端口和设备责任；每个冗余结论要说明丢失哪个故障域、剩余容量和状态如何接管。

验收不变量是：每个广播域、IP网段、默认网关、路由与NAT方向都有唯一责任，正反向通信流均能由表项和报文证据解释。单次ping成功、设备状态为up或拓扑图看似对称，都不能单独证明业务双向通信、容量、故障切换和恢复成立。

## 核心词汇与首版边界

<Term def="在L2交换网络中划分广播域的逻辑标识与端口成员关系">VLAN</Term>、<Term def="在同一IPv4链路上把下一跳IP地址解析为MAC地址的协议">ARP</Term>、<Term def="按目的前缀选择下一跳与出接口的有序规则集合">路由表</Term>、<Term def="在系统边界改写报文地址或端口并维护对应关系的机制">NAT</Term>、<Term def="通过发现、提供、请求、确认流程自动分配地址与网络参数的协议">DHCP</Term>

这些词汇固定在2015年首版语境。第2版新增或更新的技术、云上托管网络、容器网络和Service Mesh可以另行比较，但不改变本页目录分母。

<Isn02LogicalDesignPlanLab />

## 核心机制深读

### L2先学习源地址再转发目的地址

交换机从入站帧学习源MAC与端口映射，再按目的MAC查表；未知单播与广播会在VLAN内泛洪。VLAN通过端口成员和802.1Q标签隔离广播域，Trunk必须在两端一致允许所需VLAN。

动手试：先写预期拓扑、通信流、接口/状态表变化和告警，再执行一次正常验证与一次单故障验证。若结果不同，修正设计规则而不是只改现场配置。

### ARP只解析当前链路的下一跳

主机判断目标是否同网段；同网段ARP目标主机，异网段ARP默认网关。请求广播、应答通常单播。抓包要核对发送者/目标IP和MAC，并警惕免费ARP与代理ARP改变缓存。

动手试：先写预期拓扑、通信流、接口/状态表变化和告警，再执行一次正常验证与一次单故障验证。若结果不同，修正设计规则而不是只改现场配置。

### 路由由最长前缀匹配决定

IPv4地址与掩码划分网络和主机部分。路由表记录目的前缀、下一跳、出接口和度量；转发逐跳重写L2首部而保持端到端IP目标，直到NAT边界明确改写。

动手试：先写预期拓扑、通信流、接口/状态表变化和告警，再执行一次正常验证与一次单故障验证。若结果不同，修正设计规则而不是只改现场配置。

### DHCP和ICMP是可运营性的基础

DHCP的Discover、Offer、Request、ACK分配地址、掩码、网关、DNS和租期，跨网段需Relay转发。ICMP类型和代码报告不可达、超时等网络事实；ping只是其中一种探测，成功也不证明应用端口可用。

动手试：先写预期拓扑、通信流、接口/状态表变化和告警，再执行一次正常验证与一次单故障验证。若结果不同，修正设计规则而不是只改现场配置。

### 逻辑设计要为增长与汇总留空间

先按业务、环境、安全与广播规模列VLAN，再统一VLAN ID和IP起始规则。相邻分配可形成汇总前缀，减少路由数量；入站DNAT和出站SNAT必须分别写出原始/转换后的源与目的及回程状态。

动手试：先写预期拓扑、通信流、接口/状态表变化和告警，再执行一次正常验证与一次单故障验证。若结果不同，修正设计规则而不是只改现场配置。

## 原书目录逐节点重构

### 2.1 数据链路层的技术

“数据链路层的技术”服务于“贯通以太网成帧、MAC交换、VLAN、ARP、IPv4、路由、NAT、DHCP、ICMP并形成可汇总的VLAN与地址设计”；设计时要写明输入需求、责任层、配置对象、容量或状态变化、失败表现和复核证据。

**设计核查：** 固定需求和正常通信流，只改变一个与“数据链路层的技术”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 2.1.1 数据链路层是物理层的帮手

“数据链路层是物理层的帮手”服务于“贯通以太网成帧、MAC交换、VLAN、ARP、IPv4、路由、NAT、DHCP、ICMP并形成可汇总的VLAN与地址设计”；设计时要写明输入需求、责任层、配置对象、容量或状态变化、失败表现和复核证据。

**设计核查：** 固定需求和正常通信流，只改变一个与“数据链路层是物理层的帮手”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

### 专栏：用以太网标准进行成帧处理

以太网帧用目的/源MAC、类型和FCS封装网络层数据并保护相邻链路传输。

**设计核查：** 固定需求和正常通信流，只改变一个与“用以太网标准进行成帧处理”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 2.1.2 数据链路层的关键在于L2交换机的运作

“数据链路层的关键在于L2交换机的运作”服务于“贯通以太网成帧、MAC交换、VLAN、ARP、IPv4、路由、NAT、DHCP、ICMP并形成可汇总的VLAN与地址设计”；设计时要写明输入需求、责任层、配置对象、容量或状态变化、失败表现和复核证据。

**设计核查：** 固定需求和正常通信流，只改变一个与“数据链路层的关键在于L2交换机的运作”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 2.1.2.1 交换MAC地址

交换机学习源MAC与端口，再按目的MAC转发，未知目的在VLAN内泛洪。

**设计核查：** 固定需求和正常通信流，只改变一个与“交换MAC地址”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 2.1.2.2 通过VLAN将广播域分隔开

VLAN划分广播域；端口成员、Trunk允许列表与三层网关必须一致设计。

**设计核查：** 固定需求和正常通信流，只改变一个与“通过VLAN将广播域分隔开”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 2.1.3 ARP将逻辑和物理关联到一起

ARP在当前链路把下一跳IPv4地址解析为MAC，抓包应核对请求、应答和缓存变化。

**设计核查：** 固定需求和正常通信流，只改变一个与“ARP将逻辑和物理关联到一起”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 2.1.3.1 ARP通过IP地址查询MAC地址

ARP在当前链路把下一跳IPv4地址解析为MAC，抓包应核对请求、应答和缓存变化。

**设计核查：** 固定需求和正常通信流，只改变一个与“ARP通过IP地址查询MAC地址”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 2.1.3.2 抓取ARP包，观察它的写法

ARP在当前链路把下一跳IPv4地址解析为MAC，抓包应核对请求、应答和缓存变化。

**设计核查：** 固定需求和正常通信流，只改变一个与“抓取ARP包，观察它的写法”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 2.1.3.3 有几个特殊的ARP

ARP在当前链路把下一跳IPv4地址解析为MAC，抓包应核对请求、应答和缓存变化。

**设计核查：** 固定需求和正常通信流，只改变一个与“有几个特殊的ARP”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

### 2.2 网络层的技术

“网络层的技术”服务于“贯通以太网成帧、MAC交换、VLAN、ARP、IPv4、路由、NAT、DHCP、ICMP并形成可汇总的VLAN与地址设计”；设计时要写明输入需求、责任层、配置对象、容量或状态变化、失败表现和复核证据。

**设计核查：** 固定需求和正常通信流，只改变一个与“网络层的技术”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 2.2.1 网络是由网络层拼接起来的

“网络是由网络层拼接起来的”服务于“贯通以太网成帧、MAC交换、VLAN、ARP、IPv4、路由、NAT、DHCP、ICMP并形成可汇总的VLAN与地址设计”；设计时要写明输入需求、责任层、配置对象、容量或状态变化、失败表现和复核证据。

**设计核查：** 固定需求和正常通信流，只改变一个与“网络是由网络层拼接起来的”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 2.2.1.1 添加IP报头，进行分组化处理

IPv4首部承载端到端地址、TTL和上层协议，掩码划分网络与主机部分。

**设计核查：** 固定需求和正常通信流，只改变一个与“添加IP报头，进行分组化处理”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 2.2.1.2 IP地址由32位构成

IPv4首部承载端到端地址、TTL和上层协议，掩码划分网络与主机部分。

**设计核查：** 固定需求和正常通信流，只改变一个与“IP地址由32位构成”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 2.2.2 将网段连接起来

“将网段连接起来”服务于“贯通以太网成帧、MAC交换、VLAN、ARP、IPv4、路由、NAT、DHCP、ICMP并形成可汇总的VLAN与地址设计”；设计时要写明输入需求、责任层、配置对象、容量或状态变化、失败表现和复核证据。

**设计核查：** 固定需求和正常通信流，只改变一个与“将网段连接起来”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 2.2.2.1 利用IP地址进行路由选择

按最长前缀匹配选择下一跳，使用可汇总地址减少条目并明确正反向路径。

**设计核查：** 固定需求和正常通信流，只改变一个与“利用IP地址进行路由选择”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 2.2.2.2 建立路由表

按最长前缀匹配选择下一跳，使用可汇总地址减少条目并明确正反向路径。

**设计核查：** 固定需求和正常通信流，只改变一个与“建立路由表”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 2.2.2.3 整理路由表

按最长前缀匹配选择下一跳，使用可汇总地址减少条目并明确正反向路径。

**设计核查：** 固定需求和正常通信流，只改变一个与“整理路由表”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 2.2.3 转换IP地址

“转换IP地址”服务于“贯通以太网成帧、MAC交换、VLAN、ARP、IPv4、路由、NAT、DHCP、ICMP并形成可汇总的VLAN与地址设计”；设计时要写明输入需求、责任层、配置对象、容量或状态变化、失败表现和复核证据。

**设计核查：** 固定需求和正常通信流，只改变一个与“转换IP地址”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 2.2.3.1 转换IP地址

“转换IP地址”服务于“贯通以太网成帧、MAC交换、VLAN、ARP、IPv4、路由、NAT、DHCP、ICMP并形成可汇总的VLAN与地址设计”；设计时要写明输入需求、责任层、配置对象、容量或状态变化、失败表现和复核证据。

**设计核查：** 固定需求和正常通信流，只改变一个与“转换IP地址”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 2.2.3.2 私网IP地址

私网地址不在公网路由，需要在系统边界配合NAT或代理访问外部。

**设计核查：** 固定需求和正常通信流，只改变一个与“私网IP地址”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 2.2.4 自动设置IP地址的DHCP

DHCP以Discover、Offer、Request、ACK分配地址与参数，跨网段由Relay中继。

**设计核查：** 固定需求和正常通信流，只改变一个与“自动设置IP地址的DHCP”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 2.2.4.1 DHCP的消息部分中包含着诸多的信息

DHCP以Discover、Offer、Request、ACK分配地址与参数，跨网段由Relay中继。

**设计核查：** 固定需求和正常通信流，只改变一个与“DHCP的消息部分中包含着诸多的信息”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 2.2.4.2 DHCP的原理非常简单

DHCP以Discover、Offer、Request、ACK分配地址与参数，跨网段由Relay中继。

**设计核查：** 固定需求和正常通信流，只改变一个与“DHCP的原理非常简单”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 2.2.4.3 对DHCP报文作中继处理

DHCP以Discover、Offer、Request、ACK分配地址与参数，跨网段由Relay中继。

**设计核查：** 固定需求和正常通信流，只改变一个与“对DHCP报文作中继处理”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 2.2.5 用于故障排除的ICMP

ICMP类型和代码报告网络错误；ping只验证一种回显路径，不证明应用服务完整。

**设计核查：** 固定需求和正常通信流，只改变一个与“用于故障排除的ICMP”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 2.2.5.1 ICMP的关键在于类型和代码

ICMP类型和代码报告网络错误；ping只验证一种回显路径，不证明应用服务完整。

**设计核查：** 固定需求和正常通信流，只改变一个与“ICMP的关键在于类型和代码”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 2.2.5.2 常见的类型和代码有四种组合

“常见的类型和代码有四种组合”服务于“贯通以太网成帧、MAC交换、VLAN、ARP、IPv4、路由、NAT、DHCP、ICMP并形成可汇总的VLAN与地址设计”；设计时要写明输入需求、责任层、配置对象、容量或状态变化、失败表现和复核证据。

**设计核查：** 固定需求和正常通信流，只改变一个与“常见的类型和代码有四种组合”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 2.2.5.3 出现问题时先尝试用ping去排除故障

ICMP类型和代码报告网络错误；ping只验证一种回显路径，不证明应用服务完整。

**设计核查：** 固定需求和正常通信流，只改变一个与“出现问题时先尝试用ping去排除故障”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

### 2.3 逻辑设计

“逻辑设计”服务于“贯通以太网成帧、MAC交换、VLAN、ARP、IPv4、路由、NAT、DHCP、ICMP并形成可汇总的VLAN与地址设计”；设计时要写明输入需求、责任层、配置对象、容量或状态变化、失败表现和复核证据。

**设计核查：** 固定需求和正常通信流，只改变一个与“逻辑设计”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 2.3.1 整理出所需的VLAN

VLAN划分广播域；端口成员、Trunk允许列表与三层网关必须一致设计。

**设计核查：** 固定需求和正常通信流，只改变一个与“整理出所需的VLAN”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 2.3.1.1 实际所需的VLAN会因为诸多因素而变化

VLAN划分广播域；端口成员、Trunk允许列表与三层网关必须一致设计。

**设计核查：** 固定需求和正常通信流，只改变一个与“实际所需的VLAN会因为诸多因素而变化”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 2.3.1.2 规定VLAN的ID

VLAN划分广播域；端口成员、Trunk允许列表与三层网关必须一致设计。

**设计核查：** 固定需求和正常通信流，只改变一个与“规定VLAN的ID”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 2.3.2 在考虑数量增减的基础上分配IP地址

“在考虑数量增减的基础上分配IP地址”服务于“贯通以太网成帧、MAC交换、VLAN、ARP、IPv4、路由、NAT、DHCP、ICMP并形成可汇总的VLAN与地址设计”；设计时要写明输入需求、责任层、配置对象、容量或状态变化、失败表现和复核证据。

**设计核查：** 固定需求和正常通信流，只改变一个与“在考虑数量增减的基础上分配IP地址”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 2.3.2.1 IP地址的估算数量应高于当前所需数量

“IP地址的估算数量应高于当前所需数量”服务于“贯通以太网成帧、MAC交换、VLAN、ARP、IPv4、路由、NAT、DHCP、ICMP并形成可汇总的VLAN与地址设计”；设计时要写明输入需求、责任层、配置对象、容量或状态变化、失败表现和复核证据。

**设计核查：** 固定需求和正常通信流，只改变一个与“IP地址的估算数量应高于当前所需数量”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 2.3.2.2 按顺序排列网段，使之更容易汇总

“按顺序排列网段，使之更容易汇总”服务于“贯通以太网成帧、MAC交换、VLAN、ARP、IPv4、路由、NAT、DHCP、ICMP并形成可汇总的VLAN与地址设计”；设计时要写明输入需求、责任层、配置对象、容量或状态变化、失败表现和复核证据。

**设计核查：** 固定需求和正常通信流，只改变一个与“按顺序排列网段，使之更容易汇总”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 2.3.2.3 必须统一规定从何处开始分配IP地址

“必须统一规定从何处开始分配IP地址”服务于“贯通以太网成帧、MAC交换、VLAN、ARP、IPv4、路由、NAT、DHCP、ICMP并形成可汇总的VLAN与地址设计”；设计时要写明输入需求、责任层、配置对象、容量或状态变化、失败表现和复核证据。

**设计核查：** 固定需求和正常通信流，只改变一个与“必须统一规定从何处开始分配IP地址”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 2.3.3 路由选择以简为上

按最长前缀匹配选择下一跳，使用可汇总地址减少条目并明确正反向路径。

**设计核查：** 固定需求和正常通信流，只改变一个与“路由选择以简为上”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 2.3.3.1 考虑在路由选择中使用哪些协议

按最长前缀匹配选择下一跳，使用可汇总地址减少条目并明确正反向路径。

**设计核查：** 固定需求和正常通信流，只改变一个与“考虑在路由选择中使用哪些协议”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 2.3.3.2 考虑采用哪种路由选择方法

按最长前缀匹配选择下一跳，使用可汇总地址减少条目并明确正反向路径。

**设计核查：** 固定需求和正常通信流，只改变一个与“考虑采用哪种路由选择方法”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 2.3.3.3 将路径汇总以减少路径数量

按最长前缀匹配选择下一跳，使用可汇总地址减少条目并明确正反向路径。

**设计核查：** 固定需求和正常通信流，只改变一个与“将路径汇总以减少路径数量”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 2.3.4 NAT要按入站和出站分别考虑

NAT在边界改写地址或端口并维护状态，入站与出站必须分别画原始和转换后五元组。

**设计核查：** 固定需求和正常通信流，只改变一个与“NAT要按入站和出站分别考虑”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 2.3.4.1 NAT是在系统边界进行的

NAT在边界改写地址或端口并维护状态，入站与出站必须分别画原始和转换后五元组。

**设计核查：** 固定需求和正常通信流，只改变一个与“NAT是在系统边界进行的”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 2.3.4.2 通过入站通信转换地址

“通过入站通信转换地址”服务于“贯通以太网成帧、MAC交换、VLAN、ARP、IPv4、路由、NAT、DHCP、ICMP并形成可汇总的VLAN与地址设计”；设计时要写明输入需求、责任层、配置对象、容量或状态变化、失败表现和复核证据。

**设计核查：** 固定需求和正常通信流，只改变一个与“通过入站通信转换地址”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 2.3.4.3 通过出站通信转换地址

“通过出站通信转换地址”服务于“贯通以太网成帧、MAC交换、VLAN、ARP、IPv4、路由、NAT、DHCP、ICMP并形成可汇总的VLAN与地址设计”；设计时要写明输入需求、责任层、配置对象、容量或状态变化、失败表现和复核证据。

**设计核查：** 固定需求和正常通信流，只改变一个与“通过出站通信转换地址”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

## 本页完整节点清单

本页承担45个目录或复习节点，正文、图解、实验和题目必须能反向定位每一项：

- 2.1 数据链路层的技术
- 2.1.1 数据链路层是物理层的帮手
- 专栏：用以太网标准进行成帧处理
- 2.1.2 数据链路层的关键在于L2交换机的运作
- 2.1.2.1 交换MAC地址
- 2.1.2.2 通过VLAN将广播域分隔开
- 2.1.3 ARP将逻辑和物理关联到一起
- 2.1.3.1 ARP通过IP地址查询MAC地址
- 2.1.3.2 抓取ARP包，观察它的写法
- 2.1.3.3 有几个特殊的ARP
- 2.2 网络层的技术
- 2.2.1 网络是由网络层拼接起来的
- 2.2.1.1 添加IP报头，进行分组化处理
- 2.2.1.2 IP地址由32位构成
- 2.2.2 将网段连接起来
- 2.2.2.1 利用IP地址进行路由选择
- 2.2.2.2 建立路由表
- 2.2.2.3 整理路由表
- 2.2.3 转换IP地址
- 2.2.3.1 转换IP地址
- 2.2.3.2 私网IP地址
- 2.2.4 自动设置IP地址的DHCP
- 2.2.4.1 DHCP的消息部分中包含着诸多的信息
- 2.2.4.2 DHCP的原理非常简单
- 2.2.4.3 对DHCP报文作中继处理
- 2.2.5 用于故障排除的ICMP
- 2.2.5.1 ICMP的关键在于类型和代码
- 2.2.5.2 常见的类型和代码有四种组合
- 2.2.5.3 出现问题时先尝试用ping去排除故障
- 2.3 逻辑设计
- 2.3.1 整理出所需的VLAN
- 2.3.1.1 实际所需的VLAN会因为诸多因素而变化
- 2.3.1.2 规定VLAN的ID
- 2.3.2 在考虑数量增减的基础上分配IP地址
- 2.3.2.1 IP地址的估算数量应高于当前所需数量
- 2.3.2.2 按顺序排列网段，使之更容易汇总
- 2.3.2.3 必须统一规定从何处开始分配IP地址
- 2.3.3 路由选择以简为上
- 2.3.3.1 考虑在路由选择中使用哪些协议
- 2.3.3.2 考虑采用哪种路由选择方法
- 2.3.3.3 将路径汇总以减少路径数量
- 2.3.4 NAT要按入站和出站分别考虑
- 2.3.4.1 NAT是在系统边界进行的
- 2.3.4.2 通过入站通信转换地址
- 2.3.4.3 通过出站通信转换地址

<Stepper>
  <Step title="复原正常设计与通信流">
    固定首版技术边界和业务需求，标出拓扑、接口、VLAN、地址、路由、策略、状态及管理证据。
    <Isn02LogicalDesignPlanLab />
  </Step>
  <Step title="注入一个故障域">
    只断开一条链路、一台设备、一个表项、一个状态同步或一个管理依赖，先写预测再执行。
    <Isn02LogicalDesignFaultLab />
  </Step>
  <Step title="关闭独立证据门">
    对照正常与失败路径，核对容量、正反向流量、告警、恢复、偏差和回退步骤。
    <Isn02LogicalDesignEvidenceLab />
  </Step>
</Stepper>

## 可复现实验

```text
Ethernet: dst MAC | src MAC | type | payload | FCS
IPv4: src IP | dst IP | TTL | protocol | payload
```

```text
destination prefix | next hop | interface | source | metric | owner
10.20.0.0/16      10.0.0.2    uplink1     static   10
```

```text
DHCPDISCOVER -> DHCPOFFER -> DHCPREQUEST -> DHCPACK
client broadcast -> relay -> server -> relay -> client
```

动手试：为同一业务流保存正常、容量边界、单链路故障、单设备故障和恢复五轮证据。记录接口计数、MAC/ARP/路由/NAT/连接状态、应用结果、NTP时间、SNMP告警、Syslog事件和实际恢复时间。

<Callout type="trap" title="把设备双份当成高可用">
  两台设备若共用电源、上行、控制面或错误配置，仍属于同一故障域。必须断开真实依赖并验证剩余路径和容量。
</Callout>

<Callout type="trap" title="把现代架构倒灌进首版">
  原书围绕场内服务器网络的稳定基础设计。第2版、云原生和微服务网络可独立扩展，但不能替换首版的物理、逻辑、安全、高可用与管理节点。
</Callout>

## 独立证据门

<Isn02LogicalDesignEvidenceLab />

最小证据包包含：首版节点、需求编号、物理/逻辑拓扑、端口/VLAN/IP/路由/NAT/策略表、正反向通信流、容量与单故障、监控日志、恢复步骤、偏差、责任人与复核人。

## 练习

<Exercises>

**问题 1：为什么“第2章 逻辑设计”必须固定2015年首版？**

<Answer>
  固定版次才能让目录、设备能力、协议语境和设计方法一一对应。2024年第2版及云原生网络属于后续变化，只能单独比较，不能覆盖原书正式节点。
</Answer>

**问题 2：怎样构造“把VLAN、IP网段和安全区随意混用，或只验证去程不验证回程，会产生广播泄漏、地址冲突、非对称路由和NAT故障”的最小反例？**

<Answer>
  固定需求、业务流和其他设备，只改变一条链路、一台设备、一个表项或一个管理依赖；比较正反向通信、状态、告警、容量和恢复时间。
</Answer>

**问题 3：何时可以认为本页完成独立交接？**

<Answer>
  另一位工程师无需口头补充即可定位全部45个节点，按表施工，复现正常与单故障路径，并依据“每个广播域、IP网段、默认网关、路由与NAT方向都有唯一责任，正反向通信流均能由表项和报文证据解释”得到相同结论。
</Answer>

</Exercises>

## 本章回顾

“第2章 逻辑设计”的核心是贯通以太网成帧、MAC交换、VLAN、ARP、IPv4、路由、NAT、DHCP、ICMP并形成可汇总的VLAN与地址设计。真正掌握不是背设备名，而是把需求、责任层、通信流、状态、容量、故障、监控和恢复连成可施工且可证伪的闭环。

<Glossary>
  <GlossaryItem term="VLAN">
    在L2交换网络中划分广播域的逻辑标识与端口成员关系。掌握标准是能在拓扑、表项或管理证据中定位，并构造一条最小失败反例。
  </GlossaryItem>
  <GlossaryItem term="ARP">
    在同一IPv4链路上把下一跳IP地址解析为MAC地址的协议。掌握标准是能在拓扑、表项或管理证据中定位，并构造一条最小失败反例。
  </GlossaryItem>
  <GlossaryItem term="路由表">
    按目的前缀选择下一跳与出接口的有序规则集合。掌握标准是能在拓扑、表项或管理证据中定位，并构造一条最小失败反例。
  </GlossaryItem>
  <GlossaryItem term="NAT">
    在系统边界改写报文地址或端口并维护对应关系的机制。掌握标准是能在拓扑、表项或管理证据中定位，并构造一条最小失败反例。
  </GlossaryItem>
  <GlossaryItem term="DHCP">
    通过发现、提供、请求、确认流程自动分配地址与网络参数的协议。掌握标准是能在拓扑、表项或管理证据中定位，并构造一条最小失败反例。
  </GlossaryItem>
</Glossary>

[← 上一页：第1章 物理设计](/learn/illustrated-server-network/02-01-physical-design/isn-01-physical-design) · [下一页：第3章 数据安全设计和负载均衡设计 →](/learn/illustrated-server-network/04-03-security-load-balancing/isn-03-security-load-balancing)

<Attribution
  adaptedFrom="宫田宽士《图解服务器端网络架构》2015年首版"
  adaptedUrl="https://www.ituring.com.cn/book/1494"
/>
