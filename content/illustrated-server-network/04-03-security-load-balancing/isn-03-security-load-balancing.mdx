---
title: "第3章 数据安全设计和负载均衡设计"
description: "从TCP/UDP与端口进入防火墙、目的NAT和健康检查，再用HTTP、SSL、FTP、DNS理解应用通信并完成纵深防御与负载均衡设计"
type: C
section: "图解服务器端网络架构（2015年首版）"
order: 4
draft: false
math: false
sourceUrl: "https://www.ituring.com.cn/book/1494"
demo: true
---

import { Objectives } from "@/components/mdx/objectives";
import { Term } from "@/components/mdx/term";
import { Step, Stepper } from "@/components/mdx/stepper";
import { Callout } from "@/components/mdx/callout";
import { Answer, Exercises } from "@/components/mdx/exercises";
import { Glossary, GlossaryItem } from "@/components/mdx/glossary";
import { Attribution } from "@/components/mdx/attribution";

# 第3章 数据安全设计和负载均衡设计

本课程对应[日]宫田宽士《图解服务器端网络架构》，曾薇薇译、乌尼日其其格审校，人民邮电出版社/图灵教育，2015年4月第1版第1次印刷，376页、478千字、16开，ISBN 9787115388179；原书ISBN 9784797373516。图灵官方资料标明467张图表，官方试读PDF包含CIP、版次页和完整目录。首版正式结构为第0章加第1至5章，共171个目录节点；课程不会用2024年第2版、云原生、Service Mesh或现代API网关替换原书内容。

<Objectives>

- 能解释“第3章 数据安全设计和负载均衡设计”全部正式节点，并把每项技术落到一份可施工设计表。
- 能绘制正反向通信流、责任层、状态表和故障替代路径。
- 能设计单变量故障实验，验证“每条允许流和负载均衡虚拟服务都写明五元组、连接状态、地址转换、应用协议、健康判据、返回路径与最小权限”。
- 能写出并提交含需求追溯、容量、告警、恢复和首版边界的独立证据包。

</Objectives>

## 从一条可证伪的通信流开始

先预测：只开一个宽泛端口或只看负载均衡VIP可达，会遗漏动态端口、SSL处理、DNS TCP/UDP差异、健康检查与回程NAT状态。把预测写成“需求、正常路径、状态/表项、单故障路径、告警与恢复”五列，再接线、配置或测试。若结果与预测不同，先修正设计模型，不要把现场补丁当作架构成立。

本章的主问题是：从TCP/UDP与端口进入防火墙、目的NAT和健康检查，再用HTTP、SSL、FTP、DNS理解应用通信并完成纵深防御与负载均衡设计。每个箭头要注明源/目的、接口、VLAN、地址、协议端口和设备责任；每个冗余结论要说明丢失哪个故障域、剩余容量和状态如何接管。

验收不变量是：每条允许流和负载均衡虚拟服务都写明五元组、连接状态、地址转换、应用协议、健康判据、返回路径与最小权限。单次ping成功、设备状态为up或拓扑图看似对称，都不能单独证明业务双向通信、容量、故障切换和恢复成立。

## 核心词汇与首版边界

<Term def="由源IP、目的IP、源端口、目的端口和传输协议标识的通信流">五元组</Term>、<Term def="跟踪连接建立与方向，只允许属于合法会话的后续报文">状态检测</Term>、<Term def="把客户端访问的虚拟目的地址转换为真实服务器地址的负载均衡基础">目的NAT</Term>、<Term def="以TCP、HTTP或应用事务持续判断后端是否可接收新流量">健康检查</Term>、<Term def="在多个独立边界实施最小通信、服务最小化和检测，避免单点控制失效">纵深防御</Term>

这些词汇固定在2015年首版语境。第2版新增或更新的技术、云上托管网络、容器网络和Service Mesh可以另行比较，但不改变本页目录分母。


## 核心机制深读

### 传输层把进程与连接状态带入设计

端口区分同一主机上的服务进程，TCP用握手、序列、确认、窗口和结束维护可靠连接，UDP不维护连接。MTU限制IP包，MSS限制TCP数据部分；错误的MTU/MSS会造成分片或黑洞。

动手试：先写预期拓扑、通信流、接口/状态表变化和告警，再执行一次正常验证与一次单故障验证。若结果不同，修正设计规则而不是只改现场配置。

### 防火墙规则从通信矩阵产生

包过滤逐包匹配地址、端口和协议；状态检测还确认报文属于合法连接。规则应从源区到目的区列最小允许流、方向、理由和责任人，并在末尾拒绝未声明通信。

动手试：先写预期拓扑、通信流、接口/状态表变化和告警，再执行一次正常验证与一次单故障验证。若结果不同，修正设计规则而不是只改现场配置。

### 负载均衡是地址转换加服务状态

客户端访问VIP，负载均衡器选择后端并做目的NAT，必要时也改写源地址保证回程。健康检查必须验证业务所需层次；只连通TCP端口可能无法发现应用已经返回错误。

动手试：先写预期拓扑、通信流、接口/状态表变化和告警，再执行一次正常验证与一次单故障验证。若结果不同，修正设计规则而不是只改现场配置。

### 应用协议会改变端口与状态设计

HTTP/1.1持久连接影响并发状态；SSL增加握手、证书和加密负载；FTP控制连接会协商主动或被动数据端口；DNS通常UDP查询但区域传输使用TCP。防火墙和LB不能只背默认端口。

动手试：先写预期拓扑、通信流、接口/状态表变化和告警，再执行一次正常验证与一次单故障验证。若结果不同，修正设计规则而不是只改现场配置。

### 安全与均衡设计都要最小化

先整理真正必要的业务、管理和监控流，再分层过滤并关闭默认服务。负载均衡只启用有明确需求的会话保持、SSL卸载、内容切换等功能，记录容量代价和故障降级行为。

动手试：先写预期拓扑、通信流、接口/状态表变化和告警，再执行一次正常验证与一次单故障验证。若结果不同，修正设计规则而不是只改现场配置。

## 原书目录逐节点重构

### 3.1 传输层的技术

“传输层的技术”服务于“从TCP/UDP与端口进入防火墙、目的NAT和健康检查，再用HTTP、SSL、FTP、DNS理解应用通信并完成纵深防御与负载均衡设计”；设计时要写明输入需求、责任层、配置对象、容量或状态变化、失败表现和复核证据。

**设计核查：** 固定需求和正常通信流，只改变一个与“传输层的技术”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 3.1.1 通过端口号划分服务器进程

“通过端口号划分服务器进程”服务于“从TCP/UDP与端口进入防火墙、目的NAT和健康检查，再用HTTP、SSL、FTP、DNS理解应用通信并完成纵深防御与负载均衡设计”；设计时要写明输入需求、责任层、配置对象、容量或状态变化、失败表现和复核证据。

**设计核查：** 固定需求和正常通信流，只改变一个与“通过端口号划分服务器进程”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 3.1.1.1 传输层使用TCP和UDP两种协议

TCP维护可靠有序连接，UDP传递独立数据报；按应用实时性、可靠性与状态成本选择。

**设计核查：** 固定需求和正常通信流，只改变一个与“传输层使用TCP和UDP两种协议”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 3.1.1.2 TCP的工作原理比较复杂

握手、序列号、确认、窗口、重传和结束共同维护连接状态。

**设计核查：** 固定需求和正常通信流，只改变一个与“TCP的工作原理比较复杂”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 3.1.1.3 MTU和MSS的差异在于对象层不同

MTU限制IP包总长，MSS限制TCP数据部分；路径不一致会导致分片或黑洞。

**设计核查：** 固定需求和正常通信流，只改变一个与“MTU和MSS的差异在于对象层不同”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 3.1.2 用防火墙守卫系统

“用防火墙守卫系统”服务于“从TCP/UDP与端口进入防火墙、目的NAT和健康检查，再用HTTP、SSL、FTP、DNS理解应用通信并完成纵深防御与负载均衡设计”；设计时要写明输入需求、责任层、配置对象、容量或状态变化、失败表现和复核证据。

**设计核查：** 固定需求和正常通信流，只改变一个与“用防火墙守卫系统”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 3.1.2.1 基于连接进行控制

状态检测关联双向会话，包过滤仅按当前报文字段匹配；规则仍需最小权限。

**设计核查：** 固定需求和正常通信流，只改变一个与“基于连接进行控制”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 3.1.2.2 状态检测和包过滤之间的区别

状态检测关联双向会话，包过滤仅按当前报文字段匹配；规则仍需最小权限。

**设计核查：** 固定需求和正常通信流，只改变一个与“状态检测和包过滤之间的区别”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 3.1.2.3 防火墙在不断进步

“防火墙在不断进步”服务于“从TCP/UDP与端口进入防火墙、目的NAT和健康检查，再用HTTP、SSL、FTP、DNS理解应用通信并完成纵深防御与负载均衡设计”；设计时要写明输入需求、责任层、配置对象、容量或状态变化、失败表现和复核证据。

**设计核查：** 固定需求和正常通信流，只改变一个与“防火墙在不断进步”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 3.1.3 通过负载均衡器分散服务器的负荷

“通过负载均衡器分散服务器的负荷”服务于“从TCP/UDP与端口进入防火墙、目的NAT和健康检查，再用HTTP、SSL、FTP、DNS理解应用通信并完成纵深防御与负载均衡设计”；设计时要写明输入需求、责任层、配置对象、容量或状态变化、失败表现和复核证据。

**设计核查：** 固定需求和正常通信流，只改变一个与“通过负载均衡器分散服务器的负荷”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 3.1.3.1 目的NAT是服务器负载均衡技术的基础

NAT在边界改写地址或端口并维护状态，入站与出站必须分别画原始和转换后五元组。

**设计核查：** 固定需求和正常通信流，只改变一个与“目的NAT是服务器负载均衡技术的基础”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 3.1.3.2 通过健康检查监控服务器的状态

健康检查应验证真正业务就绪条件，并设置失败/恢复阈值防止抖动。

**设计核查：** 固定需求和正常通信流，只改变一个与“通过健康检查监控服务器的状态”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 3.1.3.3 熟练掌握可选功能

“熟练掌握可选功能”服务于“从TCP/UDP与端口进入防火墙、目的NAT和健康检查，再用HTTP、SSL、FTP、DNS理解应用通信并完成纵深防御与负载均衡设计”；设计时要写明输入需求、责任层、配置对象、容量或状态变化、失败表现和复核证据。

**设计核查：** 固定需求和正常通信流，只改变一个与“熟练掌握可选功能”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

### 3.2 从会话层到应用层的技术

“从会话层到应用层的技术”服务于“从TCP/UDP与端口进入防火墙、目的NAT和健康检查，再用HTTP、SSL、FTP、DNS理解应用通信并完成纵深防御与负载均衡设计”；设计时要写明输入需求、责任层、配置对象、容量或状态变化、失败表现和复核证据。

**设计核查：** 固定需求和正常通信流，只改变一个与“从会话层到应用层的技术”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 3.2.1 HTTP支撑着互联网

“HTTP支撑着互联网”服务于“从TCP/UDP与端口进入防火墙、目的NAT和健康检查，再用HTTP、SSL、FTP、DNS理解应用通信并完成纵深防御与负载均衡设计”；设计时要写明输入需求、责任层、配置对象、容量或状态变化、失败表现和复核证据。

**设计核查：** 固定需求和正常通信流，只改变一个与“HTTP支撑着互联网”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 3.2.1.1 HTTP/1.0和HTTP/1.1的TCP连接用法大相径庭

HTTP/1.0常每次新建连接，HTTP/1.1默认持久连接，改变连接容量估算。

**设计核查：** 固定需求和正常通信流，只改变一个与“HTTP/1.0和HTTP/1.1的TCP连接用法大相径庭”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 3.2.1.2 HTTP因请求和响应而得以成立

客户端请求与服务器响应形成方向明确的应用层交换。

**设计核查：** 固定需求和正常通信流，只改变一个与“HTTP因请求和响应而得以成立”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 3.2.2 用SSL保护数据

SSL用证书、非对称协商、对称加密和摘要保护机密性、身份与完整性。

**设计核查：** 固定需求和正常通信流，只改变一个与“用SSL保护数据”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 3.2.2.1 防止窃听、篡改和冒充

SSL用证书、非对称协商、对称加密和摘要保护机密性、身份与完整性。

**设计核查：** 固定需求和正常通信流，只改变一个与“防止窃听、篡改和冒充”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 3.2.2.2 通过SSL可以给各种各样的应用程序协议加密

SSL用证书、非对称协商、对称加密和摘要保护机密性、身份与完整性。

**设计核查：** 固定需求和正常通信流，只改变一个与“通过SSL可以给各种各样的应用程序协议加密”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 3.2.2.3 SSL使用混合加密方式进行加密

SSL用证书、非对称协商、对称加密和摘要保护机密性、身份与完整性。

**设计核查：** 固定需求和正常通信流，只改变一个与“SSL使用混合加密方式进行加密”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 3.2.2.4 消息摘要是消息的概要

SSL用证书、非对称协商、对称加密和摘要保护机密性、身份与完整性。

**设计核查：** 固定需求和正常通信流，只改变一个与“消息摘要是消息的概要”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 3.2.2.5 SSL中执行着大量的处理

SSL用证书、非对称协商、对称加密和摘要保护机密性、身份与完整性。

**设计核查：** 固定需求和正常通信流，只改变一个与“SSL中执行着大量的处理”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 3.2.2.6 用客户端证书对客户端进行认证

SSL用证书、非对称协商、对称加密和摘要保护机密性、身份与完整性。

**设计核查：** 固定需求和正常通信流，只改变一个与“用客户端证书对客户端进行认证”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 3.2.3 用FTP传输文件

FTP控制与数据连接分离，主动/被动模式使用不同方向和动态端口。

**设计核查：** 固定需求和正常通信流，只改变一个与“用FTP传输文件”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 3.2.3.1 主动模式使用特定的端口

“主动模式使用特定的端口”服务于“从TCP/UDP与端口进入防火墙、目的NAT和健康检查，再用HTTP、SSL、FTP、DNS理解应用通信并完成纵深防御与负载均衡设计”；设计时要写明输入需求、责任层、配置对象、容量或状态变化、失败表现和复核证据。

**设计核查：** 固定需求和正常通信流，只改变一个与“主动模式使用特定的端口”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 3.2.3.2 被动模式改变使用的端口

“被动模式改变使用的端口”服务于“从TCP/UDP与端口进入防火墙、目的NAT和健康检查，再用HTTP、SSL、FTP、DNS理解应用通信并完成纵深防御与负载均衡设计”；设计时要写明输入需求、责任层、配置对象、容量或状态变化、失败表现和复核证据。

**设计核查：** 固定需求和正常通信流，只改变一个与“被动模式改变使用的端口”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 3.2.3.3 FTP就应该当作FTP去处理

FTP控制与数据连接分离，主动/被动模式使用不同方向和动态端口。

**设计核查：** 固定需求和正常通信流，只改变一个与“FTP就应该当作FTP去处理”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 3.2.4 用DNS解析名称

DNS常用UDP查询，区域传输等场景使用TCP；安全策略需同时考虑。

**设计核查：** 固定需求和正常通信流，只改变一个与“用DNS解析名称”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 3.2.4.1 用UDP进行名称解析

“用UDP进行名称解析”服务于“从TCP/UDP与端口进入防火墙、目的NAT和健康检查，再用HTTP、SSL、FTP、DNS理解应用通信并完成纵深防御与负载均衡设计”；设计时要写明输入需求、责任层、配置对象、容量或状态变化、失败表现和复核证据。

**设计核查：** 固定需求和正常通信流，只改变一个与“用UDP进行名称解析”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 3.2.4.2 用TCP进行区域传输

“用TCP进行区域传输”服务于“从TCP/UDP与端口进入防火墙、目的NAT和健康检查，再用HTTP、SSL、FTP、DNS理解应用通信并完成纵深防御与负载均衡设计”；设计时要写明输入需求、责任层、配置对象、容量或状态变化、失败表现和复核证据。

**设计核查：** 固定需求和正常通信流，只改变一个与“用TCP进行区域传输”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

### 3.3 数据安全设计与负载均衡设计

“数据安全设计与负载均衡设计”服务于“从TCP/UDP与端口进入防火墙、目的NAT和健康检查，再用HTTP、SSL、FTP、DNS理解应用通信并完成纵深防御与负载均衡设计”；设计时要写明输入需求、责任层、配置对象、容量或状态变化、失败表现和复核证据。

**设计核查：** 固定需求和正常通信流，只改变一个与“数据安全设计与负载均衡设计”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 3.3.1 数据安全设计

“数据安全设计”服务于“从TCP/UDP与端口进入防火墙、目的NAT和健康检查，再用HTTP、SSL、FTP、DNS理解应用通信并完成纵深防御与负载均衡设计”；设计时要写明输入需求、责任层、配置对象、容量或状态变化、失败表现和复核证据。

**设计核查：** 固定需求和正常通信流，只改变一个与“数据安全设计”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 3.3.1.1 整理出真正需要的通信

从通信矩阵生成最小允许规则，在多层独立控制并关闭无需求服务。

**设计核查：** 固定需求和正常通信流，只改变一个与“整理出真正需要的通信”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 3.3.1.2 通过多级防御提高安全系数

从通信矩阵生成最小允许规则，在多层独立控制并关闭无需求服务。

**设计核查：** 固定需求和正常通信流，只改变一个与“通过多级防御提高安全系数”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 3.3.1.3 默认启动的服务应控制在最小范围内

从通信矩阵生成最小允许规则，在多层独立控制并关闭无需求服务。

**设计核查：** 固定需求和正常通信流，只改变一个与“默认启动的服务应控制在最小范围内”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 3.3.2 负载均衡设计

“负载均衡设计”服务于“从TCP/UDP与端口进入防火墙、目的NAT和健康检查，再用HTTP、SSL、FTP、DNS理解应用通信并完成纵深防御与负载均衡设计”；设计时要写明输入需求、责任层、配置对象、容量或状态变化、失败表现和复核证据。

**设计核查：** 固定需求和正常通信流，只改变一个与“负载均衡设计”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 3.3.2.1 要高效地均衡负载

“要高效地均衡负载”服务于“从TCP/UDP与端口进入防火墙、目的NAT和健康检查，再用HTTP、SSL、FTP、DNS理解应用通信并完成纵深防御与负载均衡设计”；设计时要写明输入需求、责任层、配置对象、容量或状态变化、失败表现和复核证据。

**设计核查：** 固定需求和正常通信流，只改变一个与“要高效地均衡负载”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 3.3.2.2 启用哪些可选功能

“启用哪些可选功能”服务于“从TCP/UDP与端口进入防火墙、目的NAT和健康检查，再用HTTP、SSL、FTP、DNS理解应用通信并完成纵深防御与负载均衡设计”；设计时要写明输入需求、责任层、配置对象、容量或状态变化、失败表现和复核证据。

**设计核查：** 固定需求和正常通信流，只改变一个与“启用哪些可选功能”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

## 本页完整节点清单

本页承担39个目录或复习节点，正文、图解、实验和题目必须能反向定位每一项：

- 3.1 传输层的技术
- 3.1.1 通过端口号划分服务器进程
- 3.1.1.1 传输层使用TCP和UDP两种协议
- 3.1.1.2 TCP的工作原理比较复杂
- 3.1.1.3 MTU和MSS的差异在于对象层不同
- 3.1.2 用防火墙守卫系统
- 3.1.2.1 基于连接进行控制
- 3.1.2.2 状态检测和包过滤之间的区别
- 3.1.2.3 防火墙在不断进步
- 3.1.3 通过负载均衡器分散服务器的负荷
- 3.1.3.1 目的NAT是服务器负载均衡技术的基础
- 3.1.3.2 通过健康检查监控服务器的状态
- 3.1.3.3 熟练掌握可选功能
- 3.2 从会话层到应用层的技术
- 3.2.1 HTTP支撑着互联网
- 3.2.1.1 HTTP/1.0和HTTP/1.1的TCP连接用法大相径庭
- 3.2.1.2 HTTP因请求和响应而得以成立
- 3.2.2 用SSL保护数据
- 3.2.2.1 防止窃听、篡改和冒充
- 3.2.2.2 通过SSL可以给各种各样的应用程序协议加密
- 3.2.2.3 SSL使用混合加密方式进行加密
- 3.2.2.4 消息摘要是消息的概要
- 3.2.2.5 SSL中执行着大量的处理
- 3.2.2.6 用客户端证书对客户端进行认证
- 3.2.3 用FTP传输文件
- 3.2.3.1 主动模式使用特定的端口
- 3.2.3.2 被动模式改变使用的端口
- 3.2.3.3 FTP就应该当作FTP去处理
- 3.2.4 用DNS解析名称
- 3.2.4.1 用UDP进行名称解析
- 3.2.4.2 用TCP进行区域传输
- 3.3 数据安全设计与负载均衡设计
- 3.3.1 数据安全设计
- 3.3.1.1 整理出真正需要的通信
- 3.3.1.2 通过多级防御提高安全系数
- 3.3.1.3 默认启动的服务应控制在最小范围内
- 3.3.2 负载均衡设计
- 3.3.2.1 要高效地均衡负载
- 3.3.2.2 启用哪些可选功能

<Stepper>
  <Step title="复原正常设计与通信流">
    固定首版技术边界和业务需求，标出拓扑、接口、VLAN、地址、路由、策略、状态及管理证据。
  </Step>
  <Step title="注入一个故障域">
    只断开一条链路、一台设备、一个表项、一个状态同步或一个管理依赖，先写预测再执行。
  </Step>
  <Step title="关闭独立证据门">
    对照正常与失败路径，核对容量、正反向流量、告警、恢复、偏差和回退步骤。
    </Step>
</Stepper>

## 可复现实验

```text
src zone/IP:port -> firewall state -> VIP:port -> DNAT -> real server:port -> return state
```

```text
health: TCP connect != HTTP 200 != application transaction
choose the shallowest check that still proves service readiness
```

```text
FTP active: server:20 -> client negotiated port
FTP passive: client -> server negotiated high port
DNS query: UDP/53; zone transfer: TCP/53
```

动手试：为同一业务流保存正常、容量边界、单链路故障、单设备故障和恢复五轮证据。记录接口计数、MAC/ARP/路由/NAT/连接状态、应用结果、NTP时间、SNMP告警、Syslog事件和实际恢复时间。

<Callout type="trap" title="把设备双份当成高可用">
  两台设备若共用电源、上行、控制面或错误配置，仍属于同一故障域。必须断开真实依赖并验证剩余路径和容量。
</Callout>

<Callout type="trap" title="把现代架构倒灌进首版">
  原书围绕场内服务器网络的稳定基础设计。第2版、云原生和微服务网络可独立扩展，但不能替换首版的物理、逻辑、安全、高可用与管理节点。
</Callout>

## 独立证据门

最小证据包包含：首版节点、需求编号、物理/逻辑拓扑、端口/VLAN/IP/路由/NAT/策略表、正反向通信流、容量与单故障、监控日志、恢复步骤、偏差、责任人与复核人。

## 练习

<Exercises>

**问题 1：为什么“第3章 数据安全设计和负载均衡设计”必须固定2015年首版？**

<Answer>
  固定版次才能让目录、设备能力、协议语境和设计方法一一对应。2024年第2版及云原生网络属于后续变化，只能单独比较，不能覆盖原书正式节点。
</Answer>

**问题 2：怎样构造“只开一个宽泛端口或只看负载均衡VIP可达，会遗漏动态端口、SSL处理、DNS TCP/UDP差异、健康检查与回程NAT状态”的最小反例？**

<Answer>
  固定需求、业务流和其他设备，只改变一条链路、一台设备、一个表项或一个管理依赖；比较正反向通信、状态、告警、容量和恢复时间。
</Answer>

**问题 3：何时可以认为本页完成独立交接？**

<Answer>
  另一位工程师无需口头补充即可定位全部39个节点，按表施工，复现正常与单故障路径，并依据“每条允许流和负载均衡虚拟服务都写明五元组、连接状态、地址转换、应用协议、健康判据、返回路径与最小权限”得到相同结论。
</Answer>

</Exercises>

## 本章回顾

“第3章 数据安全设计和负载均衡设计”的核心是从TCP/UDP与端口进入防火墙、目的NAT和健康检查，再用HTTP、SSL、FTP、DNS理解应用通信并完成纵深防御与负载均衡设计。真正掌握不是背设备名，而是把需求、责任层、通信流、状态、容量、故障、监控和恢复连成可施工且可证伪的闭环。

<Glossary>
  <GlossaryItem term="五元组">
    由源IP、目的IP、源端口、目的端口和传输协议标识的通信流。掌握标准是能在拓扑、表项或管理证据中定位，并构造一条最小失败反例。
  </GlossaryItem>
  <GlossaryItem term="状态检测">
    跟踪连接建立与方向，只允许属于合法会话的后续报文。掌握标准是能在拓扑、表项或管理证据中定位，并构造一条最小失败反例。
  </GlossaryItem>
  <GlossaryItem term="目的NAT">
    把客户端访问的虚拟目的地址转换为真实服务器地址的负载均衡基础。掌握标准是能在拓扑、表项或管理证据中定位，并构造一条最小失败反例。
  </GlossaryItem>
  <GlossaryItem term="健康检查">
    以TCP、HTTP或应用事务持续判断后端是否可接收新流量。掌握标准是能在拓扑、表项或管理证据中定位，并构造一条最小失败反例。
  </GlossaryItem>
  <GlossaryItem term="纵深防御">
    在多个独立边界实施最小通信、服务最小化和检测，避免单点控制失效。掌握标准是能在拓扑、表项或管理证据中定位，并构造一条最小失败反例。
  </GlossaryItem>
</Glossary>

[← 上一页：第2章 逻辑设计](/learn/illustrated-server-network/03-02-logical-design/isn-02-logical-design) · [下一页：第4章 高可用性设计 →](/learn/illustrated-server-network/05-04-high-availability/isn-04-high-availability)

<Attribution
  adaptedFrom="宫田宽士《图解服务器端网络架构》2015年首版"
  adaptedUrl="https://www.ituring.com.cn/book/1494"
/>
