---
title: "第4章 高可用性设计"
description: "按物理、数据链路、网络、传输到应用层配置链路聚合、网卡绑定、设备虚拟化、STP、FHRP及防火墙/负载均衡冗余并理清通信流"
type: C
section: "图解服务器端网络架构（2015年首版）"
order: 5
draft: true
math: false
sourceUrl: "https://www.ituring.com.cn/book/1494"
demo: true
---

import {
  Isn04HighAvailabilityPlanLab,
  Isn04HighAvailabilityFaultLab,
  Isn04HighAvailabilityEvidenceLab,
} from "@/components/mdx/illustrated-server-network/diagrams/isn-04-high-availability";

# 第4章 高可用性设计

本课程对应[日]宫田宽士《图解服务器端网络架构》，曾薇薇译、乌尼日其其格审校，人民邮电出版社/图灵教育，2015年4月第1版第1次印刷，376页、478千字、16开，ISBN 9787115388179；原书ISBN 9784797373516。图灵官方资料标明467张图表，官方试读PDF包含CIP、版次页和完整目录。首版正式结构为第0章加第1至5章，共171个目录节点；课程不会用2024年第2版、云原生、Service Mesh或现代API网关替换原书内容。

<Objectives>

- 能解释“第4章 高可用性设计”全部正式节点，并把每项技术落到一份可施工设计表。
- 能绘制正反向通信流、责任层、状态表和故障替代路径。
- 能设计单变量故障实验，验证“任一链路或单台设备故障后，剩余路径无环、状态一致、容量足够，正反向流量按设计恢复且管理面能观察到切换”。
- 能写出并提交含需求追溯、容量、告警、恢复和首版边界的独立证据包。

</Objectives>

## 从一条可证伪的通信流开始

先预测：设备成双不等于高可用；若忽略STP阻塞、FHRP网关、状态同步、回程路径和故障后容量，切换会形成环路、黑洞或连接中断。把预测写成“需求、正常路径、状态/表项、单故障路径、告警与恢复”五列，再接线、配置或测试。若结果与预测不同，先修正设计模型，不要把现场补丁当作架构成立。

本章的主问题是：按物理、数据链路、网络、传输到应用层配置链路聚合、网卡绑定、设备虚拟化、STP、FHRP及防火墙/负载均衡冗余并理清通信流。每个箭头要注明源/目的、接口、VLAN、地址、协议端口和设备责任；每个冗余结论要说明丢失哪个故障域、剩余容量和状态如何接管。

验收不变量是：任一链路或单台设备故障后，剩余路径无环、状态一致、容量足够，正反向流量按设计恢复且管理面能观察到切换。单次ping成功、设备状态为up或拓扑图看似对称，都不能单独证明业务双向通信、容量、故障切换和恢复成立。

## 核心词汇与首版边界

<Term def="把多条物理链路组合成一条逻辑链路以增加容量和冗余">链路聚合</Term>、<Term def="通过选根网桥和阻塞冗余端口消除二层桥接环路的协议族">STP</Term>、<Term def="交换机之间交换生成树信息并保护拓扑的控制报文">BPDU</Term>、<Term def="让多台三层设备共同提供一个虚拟默认网关的第一跳冗余协议">FHRP</Term>、<Term def="防火墙或负载均衡器之间复制连接与会话状态以降低切换中断">状态同步</Term>

这些词汇固定在2015年首版语境。第2版新增或更新的技术、云上托管网络、容器网络和Service Mesh可以另行比较，但不改变本页目录分母。

<Isn04HighAvailabilityPlanLab />

## 核心机制深读

### 物理冗余必须跨故障域

链路聚合提高并行带宽并容忍成员故障，服务器网卡绑定形成逻辑接口，设备堆叠或虚拟化把多机作为逻辑节点。若两条链路仍共用模块、电源或管道，它们不是独立冗余。

动手试：先写预期拓扑、通信流、接口/状态表变化和告警，再执行一次正常验证与一次单故障验证。若结果不同，修正设计规则而不是只改现场配置。

### 二层冗余先消除环路

STP选出根网桥，计算根端口和指定端口，阻塞其他路径；RSTP/MSTP改进收敛与实例控制。BPDU Guard等可选功能必须按端口角色使用，错误过滤BPDU会让环路失去控制。

动手试：先写预期拓扑、通信流、接口/状态表变化和告警，再执行一次正常验证与一次单故障验证。若结果不同，修正设计规则而不是只改现场配置。

### 三层冗余要同时处理网关和上行

FHRP让终端使用虚拟IP/MAC作为默认网关，设备间选活动节点；动态或静态路由确保活动设备仍有上行路径。网关切换成功但路由缺失，流量仍然无法到达。

动手试：先写预期拓扑、通信流、接口/状态表变化和告警，再执行一次正常验证与一次单故障验证。若结果不同，修正设计规则而不是只改现场配置。

### 有状态设备需要复制上下文

防火墙和负载均衡器不仅持有IP，还持有连接、NAT、持久化和健康状态。高可用对需要心跳、配置一致、状态同步和故障仲裁；切换测试必须观察既有连接与新连接分别如何处理。

动手试：先写预期拓扑、通信流、接口/状态表变化和告警，再执行一次正常验证与一次单故障验证。若结果不同，修正设计规则而不是只改现场配置。

### 串联与并联都要画正反向通信流

串联路径清晰但每层都可能成为单点，并联更灵活却容易出现非对称。逐跳标出活动/备用接口、VLAN、网关、NAT、状态所有者和故障后的选择，并验证单故障下剩余容量。

动手试：先写预期拓扑、通信流、接口/状态表变化和告警，再执行一次正常验证与一次单故障验证。若结果不同，修正设计规则而不是只改现场配置。

## 原书目录逐节点重构

### 4.1 冗余技术

“冗余技术”服务于“按物理、数据链路、网络、传输到应用层配置链路聚合、网卡绑定、设备虚拟化、STP、FHRP及防火墙/负载均衡冗余并理清通信流”；设计时要写明输入需求、责任层、配置对象、容量或状态变化、失败表现和复核证据。

**设计核查：** 固定需求和正常通信流，只改变一个与“冗余技术”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 4.1.1 物理层的冗余技术

“物理层的冗余技术”服务于“按物理、数据链路、网络、传输到应用层配置链路聚合、网卡绑定、设备虚拟化、STP、FHRP及防火墙/负载均衡冗余并理清通信流”；设计时要写明输入需求、责任层、配置对象、容量或状态变化、失败表现和复核证据。

**设计核查：** 固定需求和正常通信流，只改变一个与“物理层的冗余技术”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 4.1.1.1 将多条物理链路集结成一条逻辑链路

链路聚合把成员作为一条逻辑邻接，提高带宽并容忍部分成员故障。

**设计核查：** 固定需求和正常通信流，只改变一个与“将多条物理链路集结成一条逻辑链路”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 4.1.1.2 将多个物理网卡集结成一个逻辑网卡

网卡绑定把多个物理接口呈现为一个逻辑接口，但需明确交换机配合和故障模式。

**设计核查：** 固定需求和正常通信流，只改变一个与“将多个物理网卡集结成一个逻辑网卡”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 4.1.1.3 将多台物理设备集结成一台逻辑设备

堆叠或虚拟化简化控制与跨设备聚合，但仍要识别共享控制面的故障域。

**设计核查：** 固定需求和正常通信流，只改变一个与“将多台物理设备集结成一台逻辑设备”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 4.1.1.4 当上行链路中断时，让下行链路也随之中断

链路状态联动避免下游继续把流量送入已失去上行的设备。

**设计核查：** 固定需求和正常通信流，只改变一个与“当上行链路中断时，让下行链路也随之中断”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 4.1.2 数据链路层的冗余技术

“数据链路层的冗余技术”服务于“按物理、数据链路、网络、传输到应用层配置链路聚合、网卡绑定、设备虚拟化、STP、FHRP及防火墙/负载均衡冗余并理清通信流”；设计时要写明输入需求、责任层、配置对象、容量或状态变化、失败表现和复核证据。

**设计核查：** 固定需求和正常通信流，只改变一个与“数据链路层的冗余技术”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 4.1.2.1 STP的关键在于根网桥和阻塞端口

生成树选根并阻塞冗余端口防环，拓扑变化后重新收敛。

**设计核查：** 固定需求和正常通信流，只改变一个与“STP的关键在于根网桥和阻塞端口”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 4.1.2.2 STP有三种

生成树选根并阻塞冗余端口防环，拓扑变化后重新收敛。

**设计核查：** 固定需求和正常通信流，只改变一个与“STP有三种”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 4.1.2.3 同时启用多项可选功能

“同时启用多项可选功能”服务于“按物理、数据链路、网络、传输到应用层配置链路聚合、网卡绑定、设备虚拟化、STP、FHRP及防火墙/负载均衡冗余并理清通信流”；设计时要写明输入需求、责任层、配置对象、容量或状态变化、失败表现和复核证据。

**设计核查：** 固定需求和正常通信流，只改变一个与“同时启用多项可选功能”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 4.1.2.4 利用BPDU切断桥接环路

BPDU传递生成树控制信息；边缘保护与过滤必须按端口角色谨慎启用。

**设计核查：** 固定需求和正常通信流，只改变一个与“利用BPDU切断桥接环路”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 4.1.3 网络层的冗余技术

“网络层的冗余技术”服务于“按物理、数据链路、网络、传输到应用层配置链路聚合、网卡绑定、设备虚拟化、STP、FHRP及防火墙/负载均衡冗余并理清通信流”；设计时要写明输入需求、责任层、配置对象、容量或状态变化、失败表现和复核证据。

**设计核查：** 固定需求和正常通信流，只改变一个与“网络层的冗余技术”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 4.1.3.1 FHRP

多台三层设备共享虚拟网关，选活动节点并在故障时转移虚拟身份。

**设计核查：** 固定需求和正常通信流，只改变一个与“FHRP”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 4.1.3.2 利用路由协议确保通往上层设备的路径

“利用路由协议确保通往上层设备的路径”服务于“按物理、数据链路、网络、传输到应用层配置链路聚合、网卡绑定、设备虚拟化、STP、FHRP及防火墙/负载均衡冗余并理清通信流”；设计时要写明输入需求、责任层、配置对象、容量或状态变化、失败表现和复核证据。

**设计核查：** 固定需求和正常通信流，只改变一个与“利用路由协议确保通往上层设备的路径”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 4.1.4 从传输层到应用层的冗余技术

“从传输层到应用层的冗余技术”服务于“按物理、数据链路、网络、传输到应用层配置链路聚合、网卡绑定、设备虚拟化、STP、FHRP及防火墙/负载均衡冗余并理清通信流”；设计时要写明输入需求、责任层、配置对象、容量或状态变化、失败表现和复核证据。

**设计核查：** 固定需求和正常通信流，只改变一个与“从传输层到应用层的冗余技术”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 4.1.4.1 防火墙的冗余技术

有状态设备需心跳、配置一致、连接/NAT状态同步和明确仲裁。

**设计核查：** 固定需求和正常通信流，只改变一个与“防火墙的冗余技术”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 4.1.4.2 负载均衡器的冗余技术

有状态设备需心跳、配置一致、连接/NAT状态同步和明确仲裁。

**设计核查：** 固定需求和正常通信流，只改变一个与“负载均衡器的冗余技术”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

### 4.2 高可用性设计

“高可用性设计”服务于“按物理、数据链路、网络、传输到应用层配置链路聚合、网卡绑定、设备虚拟化、STP、FHRP及防火墙/负载均衡冗余并理清通信流”；设计时要写明输入需求、责任层、配置对象、容量或状态变化、失败表现和复核证据。

**设计核查：** 固定需求和正常通信流，只改变一个与“高可用性设计”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 4.2.1 高可用性设计

“高可用性设计”服务于“按物理、数据链路、网络、传输到应用层配置链路聚合、网卡绑定、设备虚拟化、STP、FHRP及防火墙/负载均衡冗余并理清通信流”；设计时要写明输入需求、责任层、配置对象、容量或状态变化、失败表现和复核证据。

**设计核查：** 固定需求和正常通信流，只改变一个与“高可用性设计”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 4.2.1.1 串联式结构

流量依次穿过设备，路径和策略清晰，但中间设备承担全部流量并需逐层冗余。

**设计核查：** 固定需求和正常通信流，只改变一个与“串联式结构”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 4.2.1.2 单路并联式结构

设备并列或旁路接入，扩展更灵活，但必须明确选路、返回路径和故障旁路。

**设计核查：** 固定需求和正常通信流，只改变一个与“单路并联式结构”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 4.2.2 理清通信流

“理清通信流”服务于“按物理、数据链路、网络、传输到应用层配置链路聚合、网卡绑定、设备虚拟化、STP、FHRP及防火墙/负载均衡冗余并理清通信流”；设计时要写明输入需求、责任层、配置对象、容量或状态变化、失败表现和复核证据。

**设计核查：** 固定需求和正常通信流，只改变一个与“理清通信流”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 4.2.2.1 串联式结构

流量依次穿过设备，路径和策略清晰，但中间设备承担全部流量并需逐层冗余。

**设计核查：** 固定需求和正常通信流，只改变一个与“串联式结构”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

#### 4.2.2.2 单路并联式结构

设备并列或旁路接入，扩展更灵活，但必须明确选路、返回路径和故障旁路。

**设计核查：** 固定需求和正常通信流，只改变一个与“单路并联式结构”相关的链路、设备、表项或管理变量；保存接口/协议状态、正反向流量、告警和恢复结果。

## 本页完整节点清单

本页承担24个目录或复习节点，正文、图解、实验和题目必须能反向定位每一项：

- 4.1 冗余技术
- 4.1.1 物理层的冗余技术
- 4.1.1.1 将多条物理链路集结成一条逻辑链路
- 4.1.1.2 将多个物理网卡集结成一个逻辑网卡
- 4.1.1.3 将多台物理设备集结成一台逻辑设备
- 4.1.1.4 当上行链路中断时，让下行链路也随之中断
- 4.1.2 数据链路层的冗余技术
- 4.1.2.1 STP的关键在于根网桥和阻塞端口
- 4.1.2.2 STP有三种
- 4.1.2.3 同时启用多项可选功能
- 4.1.2.4 利用BPDU切断桥接环路
- 4.1.3 网络层的冗余技术
- 4.1.3.1 FHRP
- 4.1.3.2 利用路由协议确保通往上层设备的路径
- 4.1.4 从传输层到应用层的冗余技术
- 4.1.4.1 防火墙的冗余技术
- 4.1.4.2 负载均衡器的冗余技术
- 4.2 高可用性设计
- 4.2.1 高可用性设计
- 4.2.1.1 串联式结构
- 4.2.1.2 单路并联式结构
- 4.2.2 理清通信流
- 4.2.2.1 串联式结构
- 4.2.2.2 单路并联式结构

<Stepper>
  <Step title="复原正常设计与通信流">
    固定首版技术边界和业务需求，标出拓扑、接口、VLAN、地址、路由、策略、状态及管理证据。
    <Isn04HighAvailabilityPlanLab />
  </Step>
  <Step title="注入一个故障域">
    只断开一条链路、一台设备、一个表项、一个状态同步或一个管理依赖，先写预测再执行。
    <Isn04HighAvailabilityFaultLab />
  </Step>
  <Step title="关闭独立证据门">
    对照正常与失败路径，核对容量、正反向流量、告警、恢复、偏差和回退步骤。
    <Isn04HighAvailabilityEvidenceLab />
  </Step>
</Stepper>

## 可复现实验

```text
LACP members -> logical port-channel -> one forwarding adjacency
member failure -> remaining members carry traffic within capacity
```

```text
root bridge -> root/designated ports forwarding
redundant port blocking -> topology change -> reconverge without loop
```

```text
client -> virtual gateway -> active firewall/LB -> server
fail active -> virtual identity + state -> standby -> verify forward and return path
```

动手试：为同一业务流保存正常、容量边界、单链路故障、单设备故障和恢复五轮证据。记录接口计数、MAC/ARP/路由/NAT/连接状态、应用结果、NTP时间、SNMP告警、Syslog事件和实际恢复时间。

<Callout type="trap" title="把设备双份当成高可用">
  两台设备若共用电源、上行、控制面或错误配置，仍属于同一故障域。必须断开真实依赖并验证剩余路径和容量。
</Callout>

<Callout type="trap" title="把现代架构倒灌进首版">
  原书围绕场内服务器网络的稳定基础设计。第2版、云原生和微服务网络可独立扩展，但不能替换首版的物理、逻辑、安全、高可用与管理节点。
</Callout>

## 独立证据门

<Isn04HighAvailabilityEvidenceLab />

最小证据包包含：首版节点、需求编号、物理/逻辑拓扑、端口/VLAN/IP/路由/NAT/策略表、正反向通信流、容量与单故障、监控日志、恢复步骤、偏差、责任人与复核人。

## 练习

<Exercises>

**问题 1：为什么“第4章 高可用性设计”必须固定2015年首版？**

<Answer>
  固定版次才能让目录、设备能力、协议语境和设计方法一一对应。2024年第2版及云原生网络属于后续变化，只能单独比较，不能覆盖原书正式节点。
</Answer>

**问题 2：怎样构造“设备成双不等于高可用；若忽略STP阻塞、FHRP网关、状态同步、回程路径和故障后容量，切换会形成环路、黑洞或连接中断”的最小反例？**

<Answer>
  固定需求、业务流和其他设备，只改变一条链路、一台设备、一个表项或一个管理依赖；比较正反向通信、状态、告警、容量和恢复时间。
</Answer>

**问题 3：何时可以认为本页完成独立交接？**

<Answer>
  另一位工程师无需口头补充即可定位全部24个节点，按表施工，复现正常与单故障路径，并依据“任一链路或单台设备故障后，剩余路径无环、状态一致、容量足够，正反向流量按设计恢复且管理面能观察到切换”得到相同结论。
</Answer>

</Exercises>

## 本章回顾

“第4章 高可用性设计”的核心是按物理、数据链路、网络、传输到应用层配置链路聚合、网卡绑定、设备虚拟化、STP、FHRP及防火墙/负载均衡冗余并理清通信流。真正掌握不是背设备名，而是把需求、责任层、通信流、状态、容量、故障、监控和恢复连成可施工且可证伪的闭环。

<Glossary>
  <GlossaryItem term="链路聚合">
    把多条物理链路组合成一条逻辑链路以增加容量和冗余。掌握标准是能在拓扑、表项或管理证据中定位，并构造一条最小失败反例。
  </GlossaryItem>
  <GlossaryItem term="STP">
    通过选根网桥和阻塞冗余端口消除二层桥接环路的协议族。掌握标准是能在拓扑、表项或管理证据中定位，并构造一条最小失败反例。
  </GlossaryItem>
  <GlossaryItem term="BPDU">
    交换机之间交换生成树信息并保护拓扑的控制报文。掌握标准是能在拓扑、表项或管理证据中定位，并构造一条最小失败反例。
  </GlossaryItem>
  <GlossaryItem term="FHRP">
    让多台三层设备共同提供一个虚拟默认网关的第一跳冗余协议。掌握标准是能在拓扑、表项或管理证据中定位，并构造一条最小失败反例。
  </GlossaryItem>
  <GlossaryItem term="状态同步">
    防火墙或负载均衡器之间复制连接与会话状态以降低切换中断。掌握标准是能在拓扑、表项或管理证据中定位，并构造一条最小失败反例。
  </GlossaryItem>
</Glossary>

[← 上一页：第3章 数据安全设计和负载均衡设计](/learn/illustrated-server-network/04-03-security-load-balancing/isn-03-security-load-balancing) · [下一页：第5章 管理设计 →](/learn/illustrated-server-network/06-05-management-design/isn-05-management-design)

<Attribution
  adaptedFrom="宫田宽士《图解服务器端网络架构》2015年首版"
  adaptedUrl="https://www.ituring.com.cn/book/1494"
/>
