---
title: "Chapter 10 · Elementary Data Structures"
type: C
section: "CLRS4 · 数据结构"
order: 10
description: "按CLRS第四版第10章重建数组与矩阵地址、栈和环形队列、双向哨兵链表，以及二叉树和左孩子右兄弟有根树表示。"
demo: true
math: true
sourceUrl: "https://mitp-content-server.mit.edu/books/content/sectbyfn/books_pres_0/11599/4e_toc.pdf"
draft: false
---

import { CLRS4ArrayAddressLab, CLRS4MatrixLayoutLab, CLRS4StackLab, CLRS4QueueRingLab, CLRS4BoundaryStateLab, CLRS4LinkedListLab, CLRS4SentinelListLab, CLRS4ListOperationLab, CLRS4RootedTreeLab, CLRS4LeftChildSiblingLab } from "@/components/mdx/introduction-to-algorithms/diagrams/10-elementary-data-structures";
import { Objectives, Callout, Attribution, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step } from "@/components/mdx/mdx-components";

<Objectives>

- 能解释数组、矩阵、栈、队列、链表和有根树的表示不变式与访问边界
- 能计算数组/矩阵地址、环形队列索引和各基本操作的时间空间成本
- 能实现数组栈、环形队列、双向哨兵链表与左孩子右兄弟树遍历
- 能分析“已有节点handle”和“按键搜索”为什么让链表操作复杂度不同
- 能设计包含空满状态、所有权、哨兵、指针互逆和树形无环性的结构证书

</Objectives>

## 从“同一组值怎样支持不同操作”开始

先预测：10个元素放进数组、链表或树中，存储的值可以相同，但访问成本会一样吗？不会。数据结构把值与一组结构关系组合起来，表示方式决定哪些操作是常数时间、哪些需要遍历，以及哪些状态必须额外记录。

Elementary Data Structures先建立最基础的表示不变式。后续散列表、搜索树和图算法都会把这些结构当成组件。

## 10.1 Simple array-based data structures: arrays, matrices, stacks, queues

### Arrays and matrices

<Term def="把固定宽度元素连续存放，并通过基址加索引乘元素宽度在常数时间定位任意位置的序列表示。">数组</Term>的关键不是“可以装很多值”，而是地址可计算。零基数组A中：

$$
\operatorname{address}(A[i])
=\operatorname{base}(A)+i\cdot w,
$$

其中$w$为元素宽度。只要索引已知，访问为$\Theta(1)$；在无序数组中按值搜索仍为$\Theta(n)$。

<CLRS4ArrayAddressLab />

越界索引计算仍能得到某个地址，却不属于数组对象。低层语言中这会触发未定义行为或内存破坏；高层容器可能抛错。复杂度分析假设contract合法，生产接口必须单独验证边界。

矩阵可把二维索引线性化。$m\times n$矩阵按row-major存储时：

$$
\operatorname{offset}(i,j)=i\cdot n+j.
$$

<CLRS4MatrixLayoutLab />

column-major则为$j\cdot m+i$。两者都能常数定位，但遍历顺序决定缓存局部性：row-major应让列索引在内层连续变化。子矩阵view还需保存stride，不能假设逻辑列数等于物理行跨度。

### Stacks

<Term def="只允许在同一端push和pop，按后进先出顺序访问元素的抽象数据类型。">栈</Term>用数组前缀和`top`表示。`PUSH`写入下一个空位并增大top，`POP`先检查非空再缩小top并返回原顶部。

<CLRS4StackLab />

```cpp
template<class T>
class Stack {
    std::vector<T> data_;
public:
    bool empty() const { return data_.empty(); }
    void push(T value) { data_.push_back(std::move(value)); }
    T pop() {
        if (empty()) throw std::underflow_error("empty stack");
        T value = std::move(data_.back());
        data_.pop_back();
        return value;
    }
};
```

固定容量数组需要overflow政策；动态数组push在扩容时可能$\Theta(n)$，但按几何增长可摊还$\Theta(1)$，这将在摊还分析章节严格证明。pop空栈是underflow，返回哨兵值会与合法数据冲突，通常应使用错误、optional或前置条件。

### Queues

<Term def="从tail端enqueue、从head端dequeue，按先进先出顺序访问元素的抽象数据类型。">队列</Term>若每次dequeue都左移数组，成本为$\Theta(n)$。环形数组用模运算让head与tail在容量范围内循环：

$$
\operatorname{next}(i)=(i+1)\bmod capacity.
$$

<CLRS4QueueRingLab />

只存head和tail时，二者相等可能表示空，也可能表示满。常见方案是额外存`size`，或永远保留一个空槽；前者用满容量，后者状态判定更简单但可用容量少1。

<CLRS4BoundaryStateLab />

```cpp
void enqueue(int value) {
    if (size == capacity) throw overflow_error{};
    buffer[tail] = value;
    tail = (tail + 1) % capacity;
    ++size;
}

int dequeue() {
    if (size == 0) throw underflow_error{};
    int value = buffer[head];
    head = (head + 1) % capacity;
    --size;
    return value;
}
```

队列不变式是逻辑第$t$个元素位于`buffer[(head+t) mod capacity]`，其中$0\le t\lt size$。每次操作只修改常数个位置和索引，因此$\Theta(1)$；并发队列还需原子顺序与可见性协议，不包含在这个顺序模型中。

<Callout type="trap" title="head等于tail就一定为空">
若允许占满全部槽且不记录size，head等于tail也可能表示满。修法是额外记录元素数、保留一个空槽，或增加独立full标志，并把政策写进不变式。
</Callout>

## 10.2 Linked lists

### 指针表达逻辑相邻

<Term def="每个节点保存key、next和prev，使逻辑相邻节点可双向常数导航的链式序列。">双向链表</Term>不要求节点物理连续。头节点prev为空、尾节点next为空；内部节点$x$满足：

$$
x.next.prev=x,\qquad x.prev.next=x.
$$

<CLRS4LinkedListLab />

按键搜索从head沿next最坏访问所有节点，$\Theta(n)$。若调用者已经持有节点$x$，在其前后插入或删除$x$只改常数个指针，$\Theta(1)$。因此“链表删除是O(1)”隐含目标handle已知；若先按值定位，总操作仍是$\Theta(n)$。

<CLRS4ListOperationLab />

```cpp
void erase(Node* x) {
    x->prev->next = x->next;
    x->next->prev = x->prev;
    delete x;
}
```

这段代码假设边界也有有效prev/next。普通nil终点版本需要分别处理head和tail；循环<Term def="不承载业务值、永久连接在链表边界并让空表和非空表都满足同一指针公式的特殊节点。">哨兵</Term>使`sentinel.next`指向首节点、`sentinel.prev`指向尾节点，空表时二者都指回自己。

<CLRS4SentinelListLab />

哨兵删除公式无需分支，但必须禁止删除sentinel本身。节点所有权也要明确：裸指针版由谁释放、迭代器删除后何时失效、节点是否能属于多个链表。垃圾回收语言省去显式free，却不能自动修复断链或多表别名。

sentinel的生命周期必须覆盖整条链表，移动容器时也要重新建立首尾节点对新sentinel的引用。调试检查可以从sentinel沿next走一圈，验证节点数与size一致、每条next/prev互逆且最终恰好回到sentinel；再反向走一圈可捕获只更新单向指针的半断链。哨兵减少操作分支，但不替代这些结构完整性检查。

单链表只存next，空间更小、前向遍历简单；已知前驱时删除为常数，但只有目标节点时通常无法找到前驱。尾插是否常数还取决于是否保存tail指针。

<Callout type="trap" title="链表任何删除都是O(1)">
按键删除包含线性搜索；单链表若只有目标节点也可能缺少前驱。修法是区分“已有双向节点handle”的更新成本与“寻找目标”的成本，并声明表示字段。
</Callout>

## 10.3 Representing rooted trees

### Parent and child pointers

有根树给每个非根节点唯一parent。只存parent数组可以常数向上走，却要扫描所有节点才能列出孩子；为每个节点保存children列表可快速向下遍历，但增加容器与维护成本。

<CLRS4RootedTreeLab />

二叉树节点保存`left`和`right`，可再存parent支持向上导航。表示不变式包括：根parent为空；每条孩子边反向parent指回当前节点；除根外每个节点恰有一个父；从任意节点沿parent最终到根，不能形成环。

递归遍历会使用与树高相同的栈空间。退化链高度$n$时可能栈溢出，可改显式栈；BFS则用队列按层访问。选择遍历结构正是本章stack和queue的组合应用。

### Arbitrary-degree rooted trees

每个节点保存动态children数组很直观，但不同节点分配不同大小。<Term def="任意度有根树的二指针编码：left-child指向第一个孩子，right-sibling指向同一父节点的下一个兄弟。">左孩子右兄弟表示</Term>让每节点固定两个指针：

- `left-child`指向最左孩子，没有孩子则为空。
- `right-sibling`指向下一个兄弟，最后兄弟为空。

<CLRS4LeftChildSiblingLab />

列出一个节点的全部孩子：先到left-child，再沿right-sibling链走，成本与孩子数量成正比。查找第$k$个孩子不是常数；插入最左孩子可常数完成，按给定兄弟位置插入也可常数完成。

这种编码在形状上像二叉树，但right-sibling边不是“右子树”语义。遍历原树时，访问节点后递归其第一个孩子，再沿兄弟链处理同父节点其余孩子；把二进制树遍历公式机械套用会误读层级。

<Stepper>
<Step title="先写表示不变式">
固定索引、head/tail、nil或sentinel政策，以及树的父子互逆和无环条件。

<CLRS4BoundaryStateLab />
</Step>
<Step title="再拆分定位和更新">
分别计算“找到对象”与“已有handle后改结构”的成本，避免把搜索时间藏在常数更新中。

<CLRS4ListOperationLab />
</Step>
<Step title="最后验证边界状态">
覆盖空、单元素、满容量、首尾节点、根与叶，并检查每次更新后所有双向关系仍成立。

<CLRS4LeftChildSiblingLab />
</Step>
</Stepper>

## 小结

Chapter 10 Elementary Data Structures从表示决定操作成本。数组和矩阵用地址公式实现随机访问；栈在同一端后进先出；环形队列用head、tail、size和模运算实现常数enqueue/dequeue。每种数组结构都需要显式越界、underflow和overflow政策。

双向链表以next/prev表达逻辑相邻，已有handle时插入删除为常数，按键搜索仍为线性；循环哨兵消除首尾分支。有根树可用parent、二叉孩子指针或左孩子右兄弟编码，选择会改变向上、向下和按序访问成本。正确实现依赖状态不变式、所有权和边界测试，而不只是操作名称。

<Exercises>

**问题 1**：row-major的$m\times n$矩阵中，零基元素$(i,j)$地址怎样计算？

<Answer>
线性offset为$i\cdot n+j$，地址为base加$(i\cdot n+j)w$，其中$w$是元素宽度。合法条件是$0\le i\lt m$且$0\le j\lt n$。
</Answer>

**问题 2**：容量8且保留一个空槽的环形队列最多存多少元素，怎样判断满？

<Answer>
最多存7个。空状态为`head==tail`；若`next(tail)==head`则再enqueue会让head与tail相等而失去区分，所以把该状态定义为满并拒绝写入。
</Answer>

**问题 3**：为何双向哨兵链表能用同一公式删除首、尾和中间节点？

<Answer>
首节点prev与尾节点next不再是nil，而是sentinel。任意业务节点$x$都有有效前驱后继，所以都可执行`x.prev.next=x.next`和`x.next.prev=x.prev`；空表则由sentinel自环表示。
</Answer>

**问题 4**：左孩子右兄弟表示中怎样枚举节点$x$的全部孩子，成本是多少？

<Answer>
从`x.left-child`开始，访问当前节点后反复沿`right-sibling`直到nil。若$x$有$d$个孩子，恰访问$d$个节点，时间$\Theta(d)$、迭代辅助空间$O(1)$。
</Answer>

</Exercises>

<Glossary>
<GlossaryItem term="数组">把固定宽度元素连续存放，并通过基址加索引乘元素宽度在常数时间定位任意位置的序列表示。</GlossaryItem>
<GlossaryItem term="栈">只允许在同一端push和pop，按后进先出顺序访问元素的抽象数据类型。</GlossaryItem>
<GlossaryItem term="队列">从tail端enqueue、从head端dequeue，按先进先出顺序访问元素的抽象数据类型。</GlossaryItem>
<GlossaryItem term="双向链表">每个节点保存key、next和prev，使逻辑相邻节点可双向常数导航的链式序列。</GlossaryItem>
<GlossaryItem term="哨兵">不承载业务值、永久连接在链表边界并让空表和非空表都满足同一指针公式的特殊节点。</GlossaryItem>
<GlossaryItem term="左孩子右兄弟表示">任意度有根树的二指针编码：left-child指向第一个孩子，right-sibling指向同一父节点的下一个兄弟。</GlossaryItem>
</Glossary>

<Attribution adaptedFrom="Introduction to Algorithms, Fourth Edition, Chapter 10 · Elementary Data Structures" adaptedUrl="https://mitp-content-server.mit.edu/books/content/sectbyfn/books_pres_0/11599/4e_toc.pdf" />
