---
title: 《Java核心技术（第14版·全两卷）》权威学习地图
type: C
section: Java核心技术（第14版·全两卷 / Java 25）
order: 0
description: >-
  把两卷25章编排成从语言语义、标准库到边界系统的可验证学习路径，并要求每次跨章迁移都保留工具链与失败证据。 覆盖25个正式节点，并通过Java
  25代码、故障注入和复位证据验收。
demo: true
math: true
sourceUrl: 'https://www.informit.com/store/core-java-volume-i-fundamentals-9780135558577'
draft: true
qualityVersion: 2
practiceMode: design
sourceMode: independent-rewrite
---
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释把两卷25章编排成从语言语义、标准库到边界系统的可验证学习路径，并要求每次跨章迁移都保留工具链与失败证据。
- 能沿冻结工具链、语言与对象、库与并发、外部边界、综合交接画出本章语义、状态、资源与失败传播链
- 能操作两卷学习路径编排器，一次只改变起点知识层或证据强度
- 能触发“直接进入FFM或并发，却没有建立类型、生命周期和关闭合同”，根据每条学习路径都能回到一个可编译探针、一个反例和一次复位完成修复、复位与同输入重放

</Objectives>

{/* JCT_QUALITY_V2 */}

## 为什么从“两卷学习路径编排器”开始

《Java核心技术（第14版·全两卷）》权威学习地图要解决的不是 API 名称记忆，而是把两卷25章编排成从语言语义、标准库到边界系统的可验证学习路径，并要求每次跨章迁移都保留工具链与失败证据。 直觉上，编译成功只排除了部分静态错误；运行一次成功也没有覆盖空值、极值、取消、并发、资源关闭、区域、协议与版本边界。

先预测：把起点知识层从“对象与类型”切换为“系统边界”，路径闭环度与跨层跳跃风险会如何变化？随后只改这一项运行探针；结果不符时修改机制假设，不移动输入边界。

## 来源、版次与独立重写边界

InformIT/Oracle Press 的[卷 I 产品页](https://www.informit.com/store/core-java-volume-i-fundamentals-9780135558577)与[卷 II 产品页](https://www.informit.com/store/core-java-volume-ii-advanced-features-9780135558690)确认 Cay S. Horstmann《Core Java》第14版全两卷于2025年10月出版、面向 Java 25，并给出25章、214个正式章/节标题。未取得本章原书正文；产品页只限定标题和小节范围，目录核对不被表述为正文忠实。

本页技术事实由[Java SE 25 API](https://docs.oracle.com/en/java/javase/25/docs/api/index.html)、[Java SE 25 语言规范](https://docs.oracle.com/javase/specs/jls/se25/html/index.html)、[Java SE 25 JVM 规范](https://docs.oracle.com/javase/specs/jvms/se25/html/index.html)与[OpenJDK 25 项目页](https://openjdk.org/projects/jdk/25/)中的适用部分独立复核。中文解释、代码、图示、交互、参数、练习与答案均为独立教学重写；Oracle 文档许可证不被误报为原书授权。

## 本章机制与六个检查点

<Term def="Java 25是《Java核心技术（第14版·全两卷）》权威学习地图首先要固定的语言、API或运行时坐标。">Java 25</Term>、<Term def="JLS语义连接冻结工具链与语言与对象，改变它必须留下编译或运行证据。">JLS语义</Term>、<Term def="JVM运行标识《Java核心技术（第14版·全两卷）》权威学习地图中的状态、资源或协议责任，不能依赖默认环境猜测。">JVM运行</Term>、<Term def="模块边界用于观察路径闭环度，同时要报告跨层跳跃风险。">模块边界</Term>、<Term def="证据链限定起点知识层与证据强度的适用范围。">证据链</Term>、<Term def="迁移顺序进入《Java核心技术（第14版·全两卷）》权威学习地图的回归证据；故障修复后必须以相同输入重放。">迁移顺序</Term>。

《Java核心技术（第14版·全两卷）》权威学习地图以“每条学习路径都能回到一个可编译探针、一个反例和一次复位”为不变量。起点知识层与证据强度是离散输入，路径闭环度是主结果，跨层跳跃风险必须单独报告；这些交互分数是课程中的透明反馈，不是原书或JDK性能基准。

## 官方目录逐项深读

### Volume I, Chapter 1: An Introduction to Java

**四级证据 1/25。** Volume I, Chapter 1: An Introduction to Java：Volume I, Chapter 1: An Introduction to Java必须放进冻结工具链—语言与对象—库与并发—外部边界—综合交接这条本章机制链，而不能从目录词直接推断实现保证。本节点以“Java 25”为观察焦点；运行`java --version && javac --version`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 Volume I, Chapter 1: An Introduction to Java 时，先预测它在 冻结工具链 阶段会怎样改变路径闭环度；只切换起点知识层或证据强度中的一项。若故障“直接进入FFM或并发，却没有建立类型、生命周期和关闭合同”没有产生预期首错，就撤回当前解释并检查替代原因。

### Volume I, Chapter 2: The Java Programming Environment

**四级证据 2/25。** Volume I, Chapter 2: The Java Programming Environment：Volume I, Chapter 2: The Java Programming Environment必须放进冻结工具链—语言与对象—库与并发—外部边界—综合交接这条本章机制链，而不能从目录词直接推断实现保证。本节点以“JLS语义”为观察焦点；运行`java --version && javac --version`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 Volume I, Chapter 2: The Java Programming Environment 时，先预测它在 语言与对象 阶段会怎样改变路径闭环度；只切换起点知识层或证据强度中的一项。若故障“直接进入FFM或并发，却没有建立类型、生命周期和关闭合同”没有产生预期首错，就撤回当前解释并检查替代原因。

### Volume I, Chapter 3: Fundamental Programming Structures in Java

**四级证据 3/25。** Volume I, Chapter 3: Fundamental Programming Structures in Java：Volume I, Chapter 3: Fundamental Programming Structures in Java必须放进冻结工具链—语言与对象—库与并发—外部边界—综合交接这条本章机制链，而不能从目录词直接推断实现保证。本节点以“JVM运行”为观察焦点；运行`java --version && javac --version`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 Volume I, Chapter 3: Fundamental Programming Structures in Java 时，先预测它在 库与并发 阶段会怎样改变路径闭环度；只切换起点知识层或证据强度中的一项。若故障“直接进入FFM或并发，却没有建立类型、生命周期和关闭合同”没有产生预期首错，就撤回当前解释并检查替代原因。

### Volume I, Chapter 4: Objects and Classes

**四级证据 4/25。** Volume I, Chapter 4: Objects and Classes：Volume I, Chapter 4: Objects and Classes必须放进冻结工具链—语言与对象—库与并发—外部边界—综合交接这条本章机制链，而不能从目录词直接推断实现保证。本节点以“模块边界”为观察焦点；运行`java --version && javac --version`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 Volume I, Chapter 4: Objects and Classes 时，先预测它在 外部边界 阶段会怎样改变路径闭环度；只切换起点知识层或证据强度中的一项。若故障“直接进入FFM或并发，却没有建立类型、生命周期和关闭合同”没有产生预期首错，就撤回当前解释并检查替代原因。

### Volume I, Chapter 5: Inheritance

**四级证据 5/25。** Volume I, Chapter 5: Inheritance：Volume I, Chapter 5: Inheritance必须放进冻结工具链—语言与对象—库与并发—外部边界—综合交接这条本章机制链，而不能从目录词直接推断实现保证。本节点以“证据链”为观察焦点；运行`java --version && javac --version`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 Volume I, Chapter 5: Inheritance 时，先预测它在 综合交接 阶段会怎样改变路径闭环度；只切换起点知识层或证据强度中的一项。若故障“直接进入FFM或并发，却没有建立类型、生命周期和关闭合同”没有产生预期首错，就撤回当前解释并检查替代原因。

### Volume I, Chapter 6: Interfaces, Lambda Expressions, and Inner Classes

**四级证据 6/25。** Volume I, Chapter 6: Interfaces, Lambda Expressions, and Inner Classes：Lambda 由目标函数式接口确定参数与返回类型，只能捕获 final 或有效 final 的局部变量。本节点以“迁移顺序”为观察焦点；运行`java --version && javac --version`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 Volume I, Chapter 6: Interfaces, Lambda Expressions, and Inner Classes 时，先预测它在 冻结工具链 阶段会怎样改变路径闭环度；只切换起点知识层或证据强度中的一项。若故障“直接进入FFM或并发，却没有建立类型、生命周期和关闭合同”没有产生预期首错，就撤回当前解释并检查替代原因。

### Volume I, Chapter 7: Exceptions, Assertions, and Logging

**四级证据 7/25。** Volume I, Chapter 7: Exceptions, Assertions, and Logging：assert 用于内部不变量且可在运行时关闭，不能承担公开参数校验或安全检查。本节点以“Java 25”为观察焦点；运行`java --version && javac --version`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 Volume I, Chapter 7: Exceptions, Assertions, and Logging 时，先预测它在 语言与对象 阶段会怎样改变路径闭环度；只切换起点知识层或证据强度中的一项。若故障“直接进入FFM或并发，却没有建立类型、生命周期和关闭合同”没有产生预期首错，就撤回当前解释并检查替代原因。

### Volume I, Chapter 8: Generic Programming

**四级证据 8/25。** Volume I, Chapter 8: Generic Programming：Volume I, Chapter 8: Generic Programming必须放进冻结工具链—语言与对象—库与并发—外部边界—综合交接这条本章机制链，而不能从目录词直接推断实现保证。本节点以“JLS语义”为观察焦点；运行`java --version && javac --version`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 Volume I, Chapter 8: Generic Programming 时，先预测它在 库与并发 阶段会怎样改变路径闭环度；只切换起点知识层或证据强度中的一项。若故障“直接进入FFM或并发，却没有建立类型、生命周期和关闭合同”没有产生预期首错，就撤回当前解释并检查替代原因。

### Volume I, Chapter 9: Collections

**四级证据 9/25。** Volume I, Chapter 9: Collections：Volume I, Chapter 9: Collections必须放进冻结工具链—语言与对象—库与并发—外部边界—综合交接这条本章机制链，而不能从目录词直接推断实现保证。本节点以“JVM运行”为观察焦点；运行`java --version && javac --version`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 Volume I, Chapter 9: Collections 时，先预测它在 外部边界 阶段会怎样改变路径闭环度；只切换起点知识层或证据强度中的一项。若故障“直接进入FFM或并发，却没有建立类型、生命周期和关闭合同”没有产生预期首错，就撤回当前解释并检查替代原因。

### Volume I, Chapter 10: Concurrency

**四级证据 10/25。** Volume I, Chapter 10: Concurrency：Volume I, Chapter 10: Concurrency必须放进冻结工具链—语言与对象—库与并发—外部边界—综合交接这条本章机制链，而不能从目录词直接推断实现保证。本节点以“模块边界”为观察焦点；运行`java --version && javac --version`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 Volume I, Chapter 10: Concurrency 时，先预测它在 综合交接 阶段会怎样改变路径闭环度；只切换起点知识层或证据强度中的一项。若故障“直接进入FFM或并发，却没有建立类型、生命周期和关闭合同”没有产生预期首错，就撤回当前解释并检查替代原因。

### Volume I, Chapter 11: Annotations

**四级证据 11/25。** Volume I, Chapter 11: Annotations：Volume I, Chapter 11: Annotations必须放进冻结工具链—语言与对象—库与并发—外部边界—综合交接这条本章机制链，而不能从目录词直接推断实现保证。本节点以“证据链”为观察焦点；运行`java --version && javac --version`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 Volume I, Chapter 11: Annotations 时，先预测它在 冻结工具链 阶段会怎样改变路径闭环度；只切换起点知识层或证据强度中的一项。若故障“直接进入FFM或并发，却没有建立类型、生命周期和关闭合同”没有产生预期首错，就撤回当前解释并检查替代原因。

### Volume I, Chapter 12: The Java Platform Module System

**四级证据 12/25。** Volume I, Chapter 12: The Java Platform Module System：Volume I, Chapter 12: The Java Platform Module System必须放进冻结工具链—语言与对象—库与并发—外部边界—综合交接这条本章机制链，而不能从目录词直接推断实现保证。本节点以“迁移顺序”为观察焦点；运行`java --version && javac --version`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 Volume I, Chapter 12: The Java Platform Module System 时，先预测它在 语言与对象 阶段会怎样改变路径闭环度；只切换起点知识层或证据强度中的一项。若故障“直接进入FFM或并发，却没有建立类型、生命周期和关闭合同”没有产生预期首错，就撤回当前解释并检查替代原因。

### Volume II, Chapter 1: Streams

**四级证据 13/25。** Volume II, Chapter 1: Streams：Volume II, Chapter 1: Streams必须放进冻结工具链—语言与对象—库与并发—外部边界—综合交接这条本章机制链，而不能从目录词直接推断实现保证。本节点以“Java 25”为观察焦点；运行`java --version && javac --version`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 Volume II, Chapter 1: Streams 时，先预测它在 库与并发 阶段会怎样改变路径闭环度；只切换起点知识层或证据强度中的一项。若故障“直接进入FFM或并发，却没有建立类型、生命周期和关闭合同”没有产生预期首错，就撤回当前解释并检查替代原因。

### Volume II, Chapter 2: Input and Output

**四级证据 14/25。** Volume II, Chapter 2: Input and Output：控制台输入输出仍受编码、Locale 与异常影响，测试必须固定输入文本和预期字节或字符。本节点以“JLS语义”为观察焦点；运行`java --version && javac --version`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 Volume II, Chapter 2: Input and Output 时，先预测它在 外部边界 阶段会怎样改变路径闭环度；只切换起点知识层或证据强度中的一项。若故障“直接进入FFM或并发，却没有建立类型、生命周期和关闭合同”没有产生预期首错，就撤回当前解释并检查替代原因。

### Volume II, Chapter 3: XML

**四级证据 15/25。** Volume II, Chapter 3: XML：Volume II, Chapter 3: XML必须放进冻结工具链—语言与对象—库与并发—外部边界—综合交接这条本章机制链，而不能从目录词直接推断实现保证。本节点以“JVM运行”为观察焦点；运行`java --version && javac --version`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 Volume II, Chapter 3: XML 时，先预测它在 综合交接 阶段会怎样改变路径闭环度；只切换起点知识层或证据强度中的一项。若故障“直接进入FFM或并发，却没有建立类型、生命周期和关闭合同”没有产生预期首错，就撤回当前解释并检查替代原因。

### Volume II, Chapter 4: Networking

**四级证据 16/25。** Volume II, Chapter 4: Networking：Volume II, Chapter 4: Networking必须放进冻结工具链—语言与对象—库与并发—外部边界—综合交接这条本章机制链，而不能从目录词直接推断实现保证。本节点以“模块边界”为观察焦点；运行`java --version && javac --version`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 Volume II, Chapter 4: Networking 时，先预测它在 冻结工具链 阶段会怎样改变路径闭环度；只切换起点知识层或证据强度中的一项。若故障“直接进入FFM或并发，却没有建立类型、生命周期和关闭合同”没有产生预期首错，就撤回当前解释并检查替代原因。

### Volume II, Chapter 5: Database Programming

**四级证据 17/25。** Volume II, Chapter 5: Database Programming：Volume II, Chapter 5: Database Programming必须放进冻结工具链—语言与对象—库与并发—外部边界—综合交接这条本章机制链，而不能从目录词直接推断实现保证。本节点以“证据链”为观察焦点；运行`java --version && javac --version`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 Volume II, Chapter 5: Database Programming 时，先预测它在 语言与对象 阶段会怎样改变路径闭环度；只切换起点知识层或证据强度中的一项。若故障“直接进入FFM或并发，却没有建立类型、生命周期和关闭合同”没有产生预期首错，就撤回当前解释并检查替代原因。

### Volume II, Chapter 6: The Date and Time API

**四级证据 18/25。** Volume II, Chapter 6: The Date and Time API：Volume II, Chapter 6: The Date and Time API必须放进冻结工具链—语言与对象—库与并发—外部边界—综合交接这条本章机制链，而不能从目录词直接推断实现保证。本节点以“迁移顺序”为观察焦点；运行`java --version && javac --version`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 Volume II, Chapter 6: The Date and Time API 时，先预测它在 库与并发 阶段会怎样改变路径闭环度；只切换起点知识层或证据强度中的一项。若故障“直接进入FFM或并发，却没有建立类型、生命周期和关闭合同”没有产生预期首错，就撤回当前解释并检查替代原因。

### Volume II, Chapter 7: Internationalization

**四级证据 19/25。** Volume II, Chapter 7: Internationalization：Volume II, Chapter 7: Internationalization必须放进冻结工具链—语言与对象—库与并发—外部边界—综合交接这条本章机制链，而不能从目录词直接推断实现保证。本节点以“Java 25”为观察焦点；运行`java --version && javac --version`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 Volume II, Chapter 7: Internationalization 时，先预测它在 外部边界 阶段会怎样改变路径闭环度；只切换起点知识层或证据强度中的一项。若故障“直接进入FFM或并发，却没有建立类型、生命周期和关闭合同”没有产生预期首错，就撤回当前解释并检查替代原因。

### Volume II, Chapter 8: Compiling and Scripting

**四级证据 20/25。** Volume II, Chapter 8: Compiling and Scripting：脚本API只定义引擎发现与绑定接口，具体语言引擎并非Java SE 25默认保证，执行仍需隔离。本节点以“JLS语义”为观察焦点；运行`java --version && javac --version`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 Volume II, Chapter 8: Compiling and Scripting 时，先预测它在 综合交接 阶段会怎样改变路径闭环度；只切换起点知识层或证据强度中的一项。若故障“直接进入FFM或并发，却没有建立类型、生命周期和关闭合同”没有产生预期首错，就撤回当前解释并检查替代原因。

### Volume II, Chapter 9: Security

**四级证据 21/25。** Volume II, Chapter 9: Security：Volume II, Chapter 9: Security必须放进冻结工具链—语言与对象—库与并发—外部边界—综合交接这条本章机制链，而不能从目录词直接推断实现保证。本节点以“JVM运行”为观察焦点；运行`java --version && javac --version`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 Volume II, Chapter 9: Security 时，先预测它在 冻结工具链 阶段会怎样改变路径闭环度；只切换起点知识层或证据强度中的一项。若故障“直接进入FFM或并发，却没有建立类型、生命周期和关闭合同”没有产生预期首错，就撤回当前解释并检查替代原因。

### Volume II, Chapter 10: Graphical User Interface Programming

**四级证据 22/25。** Volume II, Chapter 10: Graphical User Interface Programming：Volume II, Chapter 10: Graphical User Interface Programming必须放进冻结工具链—语言与对象—库与并发—外部边界—综合交接这条本章机制链，而不能从目录词直接推断实现保证。本节点以“模块边界”为观察焦点；运行`java --version && javac --version`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 Volume II, Chapter 10: Graphical User Interface Programming 时，先预测它在 语言与对象 阶段会怎样改变路径闭环度；只切换起点知识层或证据强度中的一项。若故障“直接进入FFM或并发，却没有建立类型、生命周期和关闭合同”没有产生预期首错，就撤回当前解释并检查替代原因。

### Volume II, Chapter 11: User Interface Components with Swing

**四级证据 23/25。** Volume II, Chapter 11: User Interface Components with Swing：Volume II, Chapter 11: User Interface Components with Swing必须放进冻结工具链—语言与对象—库与并发—外部边界—综合交接这条本章机制链，而不能从目录词直接推断实现保证。本节点以“证据链”为观察焦点；运行`java --version && javac --version`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 Volume II, Chapter 11: User Interface Components with Swing 时，先预测它在 库与并发 阶段会怎样改变路径闭环度；只切换起点知识层或证据强度中的一项。若故障“直接进入FFM或并发，却没有建立类型、生命周期和关闭合同”没有产生预期首错，就撤回当前解释并检查替代原因。

### Volume II, Chapter 12: Advanced Swing and Graphics

**四级证据 24/25。** Volume II, Chapter 12: Advanced Swing and Graphics：Volume II, Chapter 12: Advanced Swing and Graphics必须放进冻结工具链—语言与对象—库与并发—外部边界—综合交接这条本章机制链，而不能从目录词直接推断实现保证。本节点以“迁移顺序”为观察焦点；运行`java --version && javac --version`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 Volume II, Chapter 12: Advanced Swing and Graphics 时，先预测它在 外部边界 阶段会怎样改变路径闭环度；只切换起点知识层或证据强度中的一项。若故障“直接进入FFM或并发，却没有建立类型、生命周期和关闭合同”没有产生预期首错，就撤回当前解释并检查替代原因。

### Volume II, Chapter 13: The Foreign Functions and Memory API

**四级证据 25/25。** Volume II, Chapter 13: The Foreign Functions and Memory API：Volume II, Chapter 13: The Foreign Functions and Memory API必须放进冻结工具链—语言与对象—库与并发—外部边界—综合交接这条本章机制链，而不能从目录词直接推断实现保证。本节点以“Java 25”为观察焦点；运行`java --version && javac --version`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 Volume II, Chapter 13: The Foreign Functions and Memory API 时，先预测它在 综合交接 阶段会怎样改变路径闭环度；只切换起点知识层或证据强度中的一项。若故障“直接进入FFM或并发，却没有建立类型、生命周期和关闭合同”没有产生预期首错，就撤回当前解释并检查替代原因。

## Java 25 最小可执行切片

```java
java --version
javac --version
java --list-modules | head
```

该切片围绕“java --version && javac --version”保留本章最小机制，不承担完整生产实现。运行时固定 Java 25 发行版、操作系统、编译选项、模块或类路径和输入；保存标准输出、标准错误、退出码、资源状态与实际耗时。

```yaml
unit: jct-14e-official-learning-map
axis_a: 起点知识层
axis_b: 证据强度
fault: 直接进入FFM或并发，却没有建立类型、生命周期和关闭合同
invariant: 每条学习路径都能回到一个可编译探针、一个反例和一次复位
scenarios: [baseline, fault, recovery, reset]
```

## 三个必须主动触发的误区

<Callout type="trap" title="编译成功不等于合同成立">
  《Java核心技术（第14版·全两卷）》权威学习地图必须覆盖运行时输入、状态与资源边界；编译器没有承诺每条学习路径都能回到一个可编译探针、一个反例和一次复位会在所有外部条件下自动成立。
</Callout>

<Callout type="trap" title="本章核心故障">
  直接进入FFM或并发，却没有建立类型、生命周期和关闭合同。先定位冻结工具链→语言与对象→库与并发→外部边界→综合交接中的第一个分叉，再修改最小原因。
</Callout>

<Callout type="trap" title="修复后没有同输入重放">
  只展示新结果无法证明修复；必须恢复原始Java版本、输入与资源坐标，让原反例转成稳定回归，再执行重置。
</Callout>

## 练习、答案与目录节点验证

<Exercises>

**问题 1：单变量实验。** 怎样验证起点知识层而不让证据强度混入结论？

<Answer>
  固定Java 25、输入、证据强度与观察窗口，只把起点知识层从“对象与类型”切到“系统边界”；保存路径闭环度、跨层跳跃风险与首个诊断差异。
</Answer>

**问题 2：逐节点四级证据。** 本页正式目录怎样从“出现”升级为解释、可视与练习验证？

<Answer>
1. Volume I, Chapter 1: An Introduction to Java：选择“Java 25”作为观察点，运行`java --version && javac --version`或同义最小切片，再以故障、恢复和复位补齐四级证据。
2. Volume I, Chapter 2: The Java Programming Environment：选择“JLS语义”作为观察点，运行`java --version && javac --version`或同义最小切片，再以故障、恢复和复位补齐四级证据。
3. Volume I, Chapter 3: Fundamental Programming Structures in Java：选择“JVM运行”作为观察点，运行`java --version && javac --version`或同义最小切片，再以故障、恢复和复位补齐四级证据。
4. Volume I, Chapter 4: Objects and Classes：选择“模块边界”作为观察点，运行`java --version && javac --version`或同义最小切片，再以故障、恢复和复位补齐四级证据。
5. Volume I, Chapter 5: Inheritance：选择“证据链”作为观察点，运行`java --version && javac --version`或同义最小切片，再以故障、恢复和复位补齐四级证据。
6. Volume I, Chapter 6: Interfaces, Lambda Expressions, and Inner Classes：选择“迁移顺序”作为观察点，运行`java --version && javac --version`或同义最小切片，再以故障、恢复和复位补齐四级证据。
7. Volume I, Chapter 7: Exceptions, Assertions, and Logging：选择“Java 25”作为观察点，运行`java --version && javac --version`或同义最小切片，再以故障、恢复和复位补齐四级证据。
8. Volume I, Chapter 8: Generic Programming：选择“JLS语义”作为观察点，运行`java --version && javac --version`或同义最小切片，再以故障、恢复和复位补齐四级证据。
9. Volume I, Chapter 9: Collections：选择“JVM运行”作为观察点，运行`java --version && javac --version`或同义最小切片，再以故障、恢复和复位补齐四级证据。
10. Volume I, Chapter 10: Concurrency：选择“模块边界”作为观察点，运行`java --version && javac --version`或同义最小切片，再以故障、恢复和复位补齐四级证据。
11. Volume I, Chapter 11: Annotations：选择“证据链”作为观察点，运行`java --version && javac --version`或同义最小切片，再以故障、恢复和复位补齐四级证据。
12. Volume I, Chapter 12: The Java Platform Module System：选择“迁移顺序”作为观察点，运行`java --version && javac --version`或同义最小切片，再以故障、恢复和复位补齐四级证据。
13. Volume II, Chapter 1: Streams：选择“Java 25”作为观察点，运行`java --version && javac --version`或同义最小切片，再以故障、恢复和复位补齐四级证据。
14. Volume II, Chapter 2: Input and Output：选择“JLS语义”作为观察点，运行`java --version && javac --version`或同义最小切片，再以故障、恢复和复位补齐四级证据。
15. Volume II, Chapter 3: XML：选择“JVM运行”作为观察点，运行`java --version && javac --version`或同义最小切片，再以故障、恢复和复位补齐四级证据。
16. Volume II, Chapter 4: Networking：选择“模块边界”作为观察点，运行`java --version && javac --version`或同义最小切片，再以故障、恢复和复位补齐四级证据。
17. Volume II, Chapter 5: Database Programming：选择“证据链”作为观察点，运行`java --version && javac --version`或同义最小切片，再以故障、恢复和复位补齐四级证据。
18. Volume II, Chapter 6: The Date and Time API：选择“迁移顺序”作为观察点，运行`java --version && javac --version`或同义最小切片，再以故障、恢复和复位补齐四级证据。
19. Volume II, Chapter 7: Internationalization：选择“Java 25”作为观察点，运行`java --version && javac --version`或同义最小切片，再以故障、恢复和复位补齐四级证据。
20. Volume II, Chapter 8: Compiling and Scripting：选择“JLS语义”作为观察点，运行`java --version && javac --version`或同义最小切片，再以故障、恢复和复位补齐四级证据。
21. Volume II, Chapter 9: Security：选择“JVM运行”作为观察点，运行`java --version && javac --version`或同义最小切片，再以故障、恢复和复位补齐四级证据。
22. Volume II, Chapter 10: Graphical User Interface Programming：选择“模块边界”作为观察点，运行`java --version && javac --version`或同义最小切片，再以故障、恢复和复位补齐四级证据。
23. Volume II, Chapter 11: User Interface Components with Swing：选择“证据链”作为观察点，运行`java --version && javac --version`或同义最小切片，再以故障、恢复和复位补齐四级证据。
24. Volume II, Chapter 12: Advanced Swing and Graphics：选择“迁移顺序”作为观察点，运行`java --version && javac --version`或同义最小切片，再以故障、恢复和复位补齐四级证据。
25. Volume II, Chapter 13: The Foreign Functions and Memory API：选择“Java 25”作为观察点，运行`java --version && javac --version`或同义最小切片，再以故障、恢复和复位补齐四级证据。
</Answer>

**问题 3：故障闭环。** 遇到“直接进入FFM或并发，却没有建立类型、生命周期和关闭合同”时怎样判定修复真正成立？

<Answer>
  在冻结工具链、语言与对象、库与并发、外部边界、综合交接中定位首错，只修复该因果前提；随后重放正常、故障、恢复和复位四条轨迹。只有“每条学习路径都能回到一个可编译探针、一个反例和一次复位”再次成立，且计数、选择和资源都回到相同初值，本章才通过。
</Answer>

</Exercises>

## 术语复核与本章回顾

<Glossary>
  <GlossaryItem term="Java 25">Java 25是《Java核心技术（第14版·全两卷）》权威学习地图首先要固定的语言、API或运行时坐标。</GlossaryItem>
  <GlossaryItem term="JLS语义">JLS语义连接冻结工具链与语言与对象，改变它必须留下编译或运行证据。</GlossaryItem>
  <GlossaryItem term="JVM运行">JVM运行标识《Java核心技术（第14版·全两卷）》权威学习地图中的状态、资源或协议责任，不能依赖默认环境猜测。</GlossaryItem>
  <GlossaryItem term="模块边界">模块边界用于观察路径闭环度，同时要报告跨层跳跃风险。</GlossaryItem>
  <GlossaryItem term="证据链">证据链限定起点知识层与证据强度的适用范围。</GlossaryItem>
  <GlossaryItem term="迁移顺序">迁移顺序进入《Java核心技术（第14版·全两卷）》权威学习地图的回归证据；故障修复后必须以相同输入重放。</GlossaryItem>
</Glossary>

完成《Java核心技术（第14版·全两卷）》权威学习地图意味着能解释把两卷25章编排成从语言语义、标准库到边界系统的可验证学习路径，并要求每次跨章迁移都保留工具链与失败证据。，能运行并改动章专属切片，能主动制造“直接进入FFM或并发，却没有建立类型、生命周期和关闭合同”，还能凭可重放证据完成恢复与交接。只会复述目录或展示一次成功输出，均未满足本章门禁。

<Attribution
  mode="independent-rewrite"
  sourceBasis="outline-only"
  workTitle="Cay S. Horstmann, Core Java, Fourteenth Edition, Volumes I and II"
  adaptedUrl="https://www.informit.com/store/core-java-volume-i-fundamentals-9780135558577"
/>
