---
title: 卷I 第3章 Java 基本程序结构
type: C
section: Java核心技术（第14版·全两卷 / Java 25）
order: 3
description: >-
  用 Java 25 的静态类型、表达式求值、字符串、控制流、大数与数组边界写出第一组可编译程序合同。 覆盖11个正式节点，并通过Java
  25代码、故障注入和复位证据验收。
demo: true
math: true
sourceUrl: 'https://www.informit.com/store/core-java-volume-i-fundamentals-9780135558577'
draft: false
qualityVersion: 2
practiceMode: code
sourceMode: independent-rewrite
---


{/* JCT_QUALITY_V2 */}

## 为什么从“表达式与控制流单步台”开始

卷I 第3章 Java 基本程序结构要解决的不是 API 名称记忆，而是用 Java 25 的静态类型、表达式求值、字符串、控制流、大数与数组边界写出第一组可编译程序合同。 直觉上，编译成功只排除了部分静态错误；运行一次成功也没有覆盖空值、极值、取消、并发、资源关闭、区域、协议与版本边界。

先预测：把输入边界从“零与空”切换为“极值与溢出”，结果正确率与隐式转换风险会如何变化？随后只改这一项运行探针；结果不符时修改机制假设，不移动输入边界。

## 来源、版次与独立重写边界

InformIT/Oracle Press 的[卷 I 产品页](https://www.informit.com/store/core-java-volume-i-fundamentals-9780135558577)与[卷 II 产品页](https://www.informit.com/store/core-java-volume-ii-advanced-features-9780135558690)确认 Cay S. Horstmann《Core Java》第14版全两卷于2025年10月出版、面向 Java 25，并给出25章、214个正式章/节标题。本页所在卷的产品页同时提供本章合法试读；试读仅用于核对公开样章范围，仍不复制原书叙事。

本页技术事实由[Java SE 25 API](https://docs.oracle.com/en/java/javase/25/docs/api/index.html)、[Java SE 25 语言规范](https://docs.oracle.com/javase/specs/jls/se25/html/index.html)、[Java SE 25 JVM 规范](https://docs.oracle.com/javase/specs/jvms/se25/html/index.html)与[OpenJDK 25 项目页](https://openjdk.org/projects/jdk/25/)中的适用部分独立复核。中文解释、代码、图示、交互、参数、练习与答案均为独立教学重写；Oracle 文档许可证不被误报为原书授权。

## 本章机制与六个检查点

、、、、、。

卷I 第3章 Java 基本程序结构以“每条控制路径都产生明确类型的结果或明确失败”为不变量。输入边界与数值表示是离散输入，结果正确率是主结果，隐式转换风险必须单独报告；这些交互分数是课程中的透明反馈，不是原书或JDK性能基准。

## 官方目录逐项深读

### Chapter 3: Fundamental Programming Structures in Java

**四级证据 1/11。** Chapter 3: Fundamental Programming Structures in Java：Chapter 3: Fundamental Programming Structures in Java必须放进词法与类型—表达式求值—控制路径—输入输出—边界断言这条本章机制链，而不能从目录词直接推断实现保证。本节点以“基本类型”为观察焦点；运行`Math.addExact(Integer.MAX_VALUE, 1)`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 Chapter 3: Fundamental Programming Structures in Java 时，先预测它在 词法与类型 阶段会怎样改变结果正确率；只切换输入边界或数值表示中的一项。若故障“整数溢出、浮点金额比较或数组越界被一次正常输出掩盖”没有产生预期首错，就撤回当前解释并检查替代原因。

### 3.1 A Simple Java Program

**四级证据 2/11。** 3.1 A Simple Java Program：最小程序用编译单元、类型声明与入口方法连接源码和运行结果，诊断应保留完整命令。本节点以“变量作用域”为观察焦点；运行`Math.addExact(Integer.MAX_VALUE, 1)`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 3.1 A Simple Java Program 时，先预测它在 表达式求值 阶段会怎样改变结果正确率；只切换输入边界或数值表示中的一项。若故障“整数溢出、浮点金额比较或数组越界被一次正常输出掩盖”没有产生预期首错，就撤回当前解释并检查替代原因。

### 3.2 Comments

**四级证据 3/11。** 3.2 Comments：注释不参与普通运行语义，文档注释会进入 API 工件；关键不变量仍应由类型与测试执行。本节点以“运算转换”为观察焦点；运行`Math.addExact(Integer.MAX_VALUE, 1)`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 3.2 Comments 时，先预测它在 控制路径 阶段会怎样改变结果正确率；只切换输入边界或数值表示中的一项。若故障“整数溢出、浮点金额比较或数组越界被一次正常输出掩盖”没有产生预期首错，就撤回当前解释并检查替代原因。

### 3.3 Data Types

**四级证据 4/11。** 3.3 Data Types：基本类型和值域由 JLS 规定，引用类型保存对象引用；数值提升、溢出与 null 必须分别验证。本节点以“String不可变”为观察焦点；运行`Math.addExact(Integer.MAX_VALUE, 1)`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 3.3 Data Types 时，先预测它在 输入输出 阶段会怎样改变结果正确率；只切换输入边界或数值表示中的一项。若故障“整数溢出、浮点金额比较或数组越界被一次正常输出掩盖”没有产生预期首错，就撤回当前解释并检查替代原因。

### 3.4 Variables and Constants

**四级证据 5/11。** 3.4 Variables and Constants：变量有声明类型、作用域与初始化规则，final 限制重新赋值但不会递归冻结对象内部状态。本节点以“控制流”为观察焦点；运行`Math.addExact(Integer.MAX_VALUE, 1)`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 3.4 Variables and Constants 时，先预测它在 边界断言 阶段会怎样改变结果正确率；只切换输入边界或数值表示中的一项。若故障“整数溢出、浮点金额比较或数组越界被一次正常输出掩盖”没有产生预期首错，就撤回当前解释并检查替代原因。

### 3.5 Operators

**四级证据 6/11。** 3.5 Operators：运算符遵循提升、求值顺序和短路规则；边界探针要覆盖溢出、除零、NaN 与副作用次序。本节点以“数组边界”为观察焦点；运行`Math.addExact(Integer.MAX_VALUE, 1)`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 3.5 Operators 时，先预测它在 词法与类型 阶段会怎样改变结果正确率；只切换输入边界或数值表示中的一项。若故障“整数溢出、浮点金额比较或数组越界被一次正常输出掩盖”没有产生预期首错，就撤回当前解释并检查替代原因。

### 3.6 Strings

**四级证据 7/11。** 3.6 Strings：String 不可变且可能共享池化实例，内容比较使用 equals，频繁拼接的成本要以构建方式与规模衡量。本节点以“基本类型”为观察焦点；运行`Math.addExact(Integer.MAX_VALUE, 1)`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 3.6 Strings 时，先预测它在 表达式求值 阶段会怎样改变结果正确率；只切换输入边界或数值表示中的一项。若故障“整数溢出、浮点金额比较或数组越界被一次正常输出掩盖”没有产生预期首错，就撤回当前解释并检查替代原因。

### 3.7 Input and Output

**四级证据 8/11。** 3.7 Input and Output：控制台输入输出仍受编码、Locale 与异常影响，测试必须固定输入文本和预期字节或字符。本节点以“变量作用域”为观察焦点；运行`Math.addExact(Integer.MAX_VALUE, 1)`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 3.7 Input and Output 时，先预测它在 控制路径 阶段会怎样改变结果正确率；只切换输入边界或数值表示中的一项。若故障“整数溢出、浮点金额比较或数组越界被一次正常输出掩盖”没有产生预期首错，就撤回当前解释并检查替代原因。

### 3.8 Control Flow

**四级证据 9/11。** 3.8 Control Flow：分支和循环要覆盖所有可达路径；switch 表达式的穷尽性与模式作用域应由编译诊断验证。本节点以“运算转换”为观察焦点；运行`Math.addExact(Integer.MAX_VALUE, 1)`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 3.8 Control Flow 时，先预测它在 输入输出 阶段会怎样改变结果正确率；只切换输入边界或数值表示中的一项。若故障“整数溢出、浮点金额比较或数组越界被一次正常输出掩盖”没有产生预期首错，就撤回当前解释并检查替代原因。

### 3.9 Big Numbers

**四级证据 10/11。** 3.9 Big Numbers：BigInteger 提供任意精度整数，BigDecimal 需要显式尺度与舍入策略，二者都不是运算符重载值类型。本节点以“String不可变”为观察焦点；运行`Math.addExact(Integer.MAX_VALUE, 1)`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 3.9 Big Numbers 时，先预测它在 边界断言 阶段会怎样改变结果正确率；只切换输入边界或数值表示中的一项。若故障“整数溢出、浮点金额比较或数组越界被一次正常输出掩盖”没有产生预期首错，就撤回当前解释并检查替代原因。

### 3.10 Arrays

**四级证据 11/11。** 3.10 Arrays：数组长度固定、协变且运行时保留元素类型，因此既有越界检查也可能产生 ArrayStoreException。本节点以“控制流”为观察焦点；运行`Math.addExact(Integer.MAX_VALUE, 1)`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 3.10 Arrays 时，先预测它在 词法与类型 阶段会怎样改变结果正确率；只切换输入边界或数值表示中的一项。若故障“整数溢出、浮点金额比较或数组越界被一次正常输出掩盖”没有产生预期首错，就撤回当前解释并检查替代原因。

## Java 25 最小可执行切片

```java
var values = new int[] { 2, 3, 5 };
var total = java.util.Arrays.stream(values).sum();
System.out.println(total);
```

该切片围绕“Math.addExact(Integer.MAX_VALUE, 1)”保留本章最小机制，不承担完整生产实现。运行时固定 Java 25 发行版、操作系统、编译选项、模块或类路径和输入；保存标准输出、标准错误、退出码、资源状态与实际耗时。

```yaml
unit: jct-14e-v1-03-fundamental-structures
axis_a: 输入边界
axis_b: 数值表示
fault: 整数溢出、浮点金额比较或数组越界被一次正常输出掩盖
invariant: 每条控制路径都产生明确类型的结果或明确失败
scenarios: [baseline, fault, recovery, reset]
```

## 三个必须主动触发的误区


## 练习、答案与目录节点验证


## 术语复核与本章回顾


完成卷I 第3章 Java 基本程序结构意味着能解释用 Java 25 的静态类型、表达式求值、字符串、控制流、大数与数组边界写出第一组可编译程序合同。，能运行并改动章专属切片，能主动制造“整数溢出、浮点金额比较或数组越界被一次正常输出掩盖”，还能凭可重放证据完成恢复与交接。只会复述目录或展示一次成功输出，均未满足本章门禁。

