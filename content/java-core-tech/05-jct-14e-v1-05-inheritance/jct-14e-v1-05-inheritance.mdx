---
title: 卷I 第5章 继承
type: C
section: Java核心技术（第14版·全两卷 / Java 25）
order: 5
description: 用子类型替换、动态分派、sealed 层级、模式匹配与反射边界判断继承设计是否成立。 覆盖12个正式节点，并通过Java 25代码、故障注入和复位证据验收。
demo: true
math: true
sourceUrl: 'https://www.informit.com/store/core-java-volume-i-fundamentals-9780135558577'
draft: false
qualityVersion: 2
practiceMode: code
sourceMode: independent-rewrite
---


{/* JCT_QUALITY_V2 */}

## 为什么从“子类型与分派决策台”开始

卷I 第5章 继承要解决的不是 API 名称记忆，而是用子类型替换、动态分派、sealed 层级、模式匹配与反射边界判断继承设计是否成立。 直觉上，编译成功只排除了部分静态错误；运行一次成功也没有覆盖空值、极值、取消、并发、资源关闭、区域、协议与版本边界。

先预测：把层级开放度从“sealed枚举”切换为“开放继承”，替换一致度与脆弱基类风险会如何变化？随后只改这一项运行探针；结果不符时修改机制假设，不移动输入边界。

## 来源、版次与独立重写边界

InformIT/Oracle Press 的[卷 I 产品页](https://www.informit.com/store/core-java-volume-i-fundamentals-9780135558577)与[卷 II 产品页](https://www.informit.com/store/core-java-volume-ii-advanced-features-9780135558690)确认 Cay S. Horstmann《Core Java》第14版全两卷于2025年10月出版、面向 Java 25，并给出25章、214个正式章/节标题。未取得本章原书正文；产品页只限定标题和小节范围，目录核对不被表述为正文忠实。

本页技术事实由[Java SE 25 API](https://docs.oracle.com/en/java/javase/25/docs/api/index.html)、[Java SE 25 语言规范](https://docs.oracle.com/javase/specs/jls/se25/html/index.html)、[Java SE 25 JVM 规范](https://docs.oracle.com/javase/specs/jvms/se25/html/index.html)与[OpenJDK 25 项目页](https://openjdk.org/projects/jdk/25/)中的适用部分独立复核。中文解释、代码、图示、交互、参数、练习与答案均为独立教学重写；Oracle 文档许可证不被误报为原书授权。

## 本章机制与六个检查点

、、、、、。

卷I 第5章 继承以“任何父类型可接受位置都保持父合同与穷尽处理”为不变量。层级开放度与分派方式是离散输入，替换一致度是主结果，脆弱基类风险必须单独报告；这些交互分数是课程中的透明反馈，不是原书或JDK性能基准。

## 官方目录逐项深读

### Chapter 5: Inheritance

**四级证据 1/12。** Chapter 5: Inheritance：Chapter 5: Inheritance必须放进声明层级—建立子类型—动态分派—模式覆盖—反射审计这条本章机制链，而不能从目录词直接推断实现保证。本节点以“is-a关系”为观察焦点；运行`sealed-switch-probe`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 Chapter 5: Inheritance 时，先预测它在 声明层级 阶段会怎样改变替换一致度；只切换层级开放度或分派方式中的一项。若故障“子类破坏父类不变量，或反射绕开访问边界后仍宣称层级安全”没有产生预期首错，就撤回当前解释并检查替代原因。

### 5.1 Classes, Superclasses, and Subclasses

**四级证据 2/12。** 5.1 Classes, Superclasses, and Subclasses：子类继承可见成员并参与动态分派，但不能收紧父类型前置条件或破坏后置条件。本节点以“Object合同”为观察焦点；运行`sealed-switch-probe`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 5.1 Classes, Superclasses, and Subclasses 时，先预测它在 建立子类型 阶段会怎样改变替换一致度；只切换层级开放度或分派方式中的一项。若故障“子类破坏父类不变量，或反射绕开访问边界后仍宣称层级安全”没有产生预期首错，就撤回当前解释并检查替代原因。

### 5.2 Object: The Cosmic Superclass

**四级证据 3/12。** 5.2 Object: The Cosmic Superclass：Object 定义身份相关的 equals、hashCode、toString 与监视器入口；重写相等性必须同步维护哈希合同。本节点以“自动装箱”为观察焦点；运行`sealed-switch-probe`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 5.2 Object: The Cosmic Superclass 时，先预测它在 动态分派 阶段会怎样改变替换一致度；只切换层级开放度或分派方式中的一项。若故障“子类破坏父类不变量，或反射绕开访问边界后仍宣称层级安全”没有产生预期首错，就撤回当前解释并检查替代原因。

### 5.3 Generic Array Lists

**四级证据 4/12。** 5.3 Generic Array Lists：ArrayList 的泛型参数提供编译期元素约束，扩容与索引访问仍受具体实现和运行规模影响。本节点以“sealed”为观察焦点；运行`sealed-switch-probe`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 5.3 Generic Array Lists 时，先预测它在 模式覆盖 阶段会怎样改变替换一致度；只切换层级开放度或分派方式中的一项。若故障“子类破坏父类不变量，或反射绕开访问边界后仍宣称层级安全”没有产生预期首错，就撤回当前解释并检查替代原因。

### 5.4 Object Wrappers and Autoboxing

**四级证据 5/12。** 5.4 Object Wrappers and Autoboxing：装箱把基本值转换为包装对象，缓存身份、null 拆箱与额外分配都使 == 比较和性能推断易错。本节点以“模式匹配”为观察焦点；运行`sealed-switch-probe`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 5.4 Object Wrappers and Autoboxing 时，先预测它在 反射审计 阶段会怎样改变替换一致度；只切换层级开放度或分派方式中的一项。若故障“子类破坏父类不变量，或反射绕开访问边界后仍宣称层级安全”没有产生预期首错，就撤回当前解释并检查替代原因。

### 5.5 Methods with a Variable Number of Arguments

**四级证据 6/12。** 5.5 Methods with a Variable Number of Arguments：可变参数在调用边界表现为数组，重载选择、空参数与堆污染需要显式测试。本节点以“反射访问”为观察焦点；运行`sealed-switch-probe`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 5.5 Methods with a Variable Number of Arguments 时，先预测它在 声明层级 阶段会怎样改变替换一致度；只切换层级开放度或分派方式中的一项。若故障“子类破坏父类不变量，或反射绕开访问边界后仍宣称层级安全”没有产生预期首错，就撤回当前解释并检查替代原因。

### 5.6 Abstract Classes

**四级证据 7/12。** 5.6 Abstract Classes：抽象类可以共享状态和部分实现，但仍要为所有具体子类维护共同不变量。本节点以“is-a关系”为观察焦点；运行`sealed-switch-probe`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 5.6 Abstract Classes 时，先预测它在 建立子类型 阶段会怎样改变替换一致度；只切换层级开放度或分派方式中的一项。若故障“子类破坏父类不变量，或反射绕开访问边界后仍宣称层级安全”没有产生预期首错，就撤回当前解释并检查替代原因。

### 5.7 Enumeration Classes

**四级证据 8/12。** 5.7 Enumeration Classes：enum 实例集合由声明限定，可携带状态与行为；持久化时不要依赖 ordinal 作为稳定协议值。本节点以“Object合同”为观察焦点；运行`sealed-switch-probe`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 5.7 Enumeration Classes 时，先预测它在 动态分派 阶段会怎样改变替换一致度；只切换层级开放度或分派方式中的一项。若故障“子类破坏父类不变量，或反射绕开访问边界后仍宣称层级安全”没有产生预期首错，就撤回当前解释并检查替代原因。

### 5.8 Sealed Classes

**四级证据 9/12。** 5.8 Sealed Classes：sealed 层级显式列出允许的直接子类型，使模式分支可以在编译期检查穷尽性。本节点以“自动装箱”为观察焦点；运行`sealed-switch-probe`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 5.8 Sealed Classes 时，先预测它在 模式覆盖 阶段会怎样改变替换一致度；只切换层级开放度或分派方式中的一项。若故障“子类破坏父类不变量，或反射绕开访问边界后仍宣称层级安全”没有产生预期首错，就撤回当前解释并检查替代原因。

### 5.9 Pattern Matching

**四级证据 10/12。** 5.9 Pattern Matching：模式匹配把类型测试、转换和变量作用域绑定，守卫与 null 路径仍需单独覆盖。本节点以“sealed”为观察焦点；运行`sealed-switch-probe`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 5.9 Pattern Matching 时，先预测它在 反射审计 阶段会怎样改变替换一致度；只切换层级开放度或分派方式中的一项。若故障“子类破坏父类不变量，或反射绕开访问边界后仍宣称层级安全”没有产生预期首错，就撤回当前解释并检查替代原因。

### 5.10 Reflection

**四级证据 11/12。** 5.10 Reflection：反射检查运行时类型和成员，会受到模块开放、访问权限与擦除影响，并非绕开合同的万能入口。本节点以“模式匹配”为观察焦点；运行`sealed-switch-probe`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 5.10 Reflection 时，先预测它在 声明层级 阶段会怎样改变替换一致度；只切换层级开放度或分派方式中的一项。若故障“子类破坏父类不变量，或反射绕开访问边界后仍宣称层级安全”没有产生预期首错，就撤回当前解释并检查替代原因。

### 5.11 Design Hints for Inheritance

**四级证据 12/12。** 5.11 Design Hints for Inheritance：Design Hints for Inheritance必须放进声明层级—建立子类型—动态分派—模式覆盖—反射审计这条本章机制链，而不能从目录词直接推断实现保证。本节点以“反射访问”为观察焦点；运行`sealed-switch-probe`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 5.11 Design Hints for Inheritance 时，先预测它在 建立子类型 阶段会怎样改变替换一致度；只切换层级开放度或分派方式中的一项。若故障“子类破坏父类不变量，或反射绕开访问边界后仍宣称层级安全”没有产生预期首错，就撤回当前解释并检查替代原因。

## Java 25 最小可执行切片

```java
sealed interface Shape permits Circle {}
record Circle(double r) implements Shape {}
double area(Shape s) { return switch (s) { case Circle(var r) -> Math.PI*r*r; }; }
```

该切片围绕“sealed-switch-probe”保留本章最小机制，不承担完整生产实现。运行时固定 Java 25 发行版、操作系统、编译选项、模块或类路径和输入；保存标准输出、标准错误、退出码、资源状态与实际耗时。

```yaml
unit: jct-14e-v1-05-inheritance
axis_a: 层级开放度
axis_b: 分派方式
fault: 子类破坏父类不变量，或反射绕开访问边界后仍宣称层级安全
invariant: 任何父类型可接受位置都保持父合同与穷尽处理
scenarios: [baseline, fault, recovery, reset]
```

## 三个必须主动触发的误区


## 练习、答案与目录节点验证


## 术语复核与本章回顾


完成卷I 第5章 继承意味着能解释用子类型替换、动态分派、sealed 层级、模式匹配与反射边界判断继承设计是否成立。，能运行并改动章专属切片，能主动制造“子类破坏父类不变量，或反射绕开访问边界后仍宣称层级安全”，还能凭可重放证据完成恢复与交接。只会复述目录或展示一次成功输出，均未满足本章门禁。

