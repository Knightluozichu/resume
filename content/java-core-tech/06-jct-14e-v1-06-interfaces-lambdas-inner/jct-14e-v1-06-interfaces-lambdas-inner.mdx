---
title: 卷I 第6章 接口、Lambda 与内部类
type: C
section: Java核心技术（第14版·全两卷 / Java 25）
order: 6
description: 比较接口合同、Lambda 目标类型、捕获语义、内部类外部实例、服务发现与动态代理。 覆盖6个正式节点，并通过Java 25代码、故障注入和复位证据验收。
demo: true
math: true
sourceUrl: 'https://www.informit.com/store/core-java-volume-i-fundamentals-9780135558577'
draft: true
qualityVersion: 2
practiceMode: code
sourceMode: independent-rewrite
---
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释比较接口合同、Lambda 目标类型、捕获语义、内部类外部实例、服务发现与动态代理。
- 能沿声明行为、确定目标类型、捕获上下文、装配服务、代理调用画出本章语义、状态、资源与失败传播链
- 能操作行为对象与捕获语义台，一次只改变行为表示或上下文耦合
- 能触发“Lambda 捕获可变上下文或代理遗漏 Object 方法语义，导致行为与身份不可预测”，根据目标类型、捕获值和调用边界在执行前可说明完成修复、复位与同输入重放

</Objectives>

{/* JCT_QUALITY_V2 */}

## 为什么从“行为对象与捕获语义台”开始

卷I 第6章 接口、Lambda 与内部类要解决的不是 API 名称记忆，而是比较接口合同、Lambda 目标类型、捕获语义、内部类外部实例、服务发现与动态代理。 直觉上，编译成功只排除了部分静态错误；运行一次成功也没有覆盖空值、极值、取消、并发、资源关闭、区域、协议与版本边界。

先预测：把行为表示从“Lambda”切换为“服务实现”，行为替换度与隐藏捕获风险会如何变化？随后只改这一项运行探针；结果不符时修改机制假设，不移动输入边界。

## 来源、版次与独立重写边界

InformIT/Oracle Press 的[卷 I 产品页](https://www.informit.com/store/core-java-volume-i-fundamentals-9780135558577)与[卷 II 产品页](https://www.informit.com/store/core-java-volume-ii-advanced-features-9780135558690)确认 Cay S. Horstmann《Core Java》第14版全两卷于2025年10月出版、面向 Java 25，并给出25章、214个正式章/节标题。未取得本章原书正文；产品页只限定标题和小节范围，目录核对不被表述为正文忠实。

本页技术事实由[Java SE 25 API](https://docs.oracle.com/en/java/javase/25/docs/api/index.html)、[Java SE 25 语言规范](https://docs.oracle.com/javase/specs/jls/se25/html/index.html)、[Java SE 25 JVM 规范](https://docs.oracle.com/javase/specs/jvms/se25/html/index.html)与[OpenJDK 25 项目页](https://openjdk.org/projects/jdk/25/)中的适用部分独立复核。中文解释、代码、图示、交互、参数、练习与答案均为独立教学重写；Oracle 文档许可证不被误报为原书授权。

## 本章机制与六个检查点

<Term def="默认方法是卷I 第6章 接口、Lambda 与内部类首先要固定的语言、API或运行时坐标。">默认方法</Term>、<Term def="函数式接口连接声明行为与确定目标类型，改变它必须留下编译或运行证据。">函数式接口</Term>、<Term def="有效final标识卷I 第6章 接口、Lambda 与内部类中的状态、资源或协议责任，不能依赖默认环境猜测。">有效final</Term>、<Term def="内部类用于观察行为替换度，同时要报告隐藏捕获风险。">内部类</Term>、<Term def="ServiceLoader限定行为表示与上下文耦合的适用范围。">ServiceLoader</Term>、<Term def="Proxy进入卷I 第6章 接口、Lambda 与内部类的回归证据；故障修复后必须以相同输入重放。">Proxy</Term>。

卷I 第6章 接口、Lambda 与内部类以“目标类型、捕获值和调用边界在执行前可说明”为不变量。行为表示与上下文耦合是离散输入，行为替换度是主结果，隐藏捕获风险必须单独报告；这些交互分数是课程中的透明反馈，不是原书或JDK性能基准。

## 官方目录逐项深读

### Chapter 6: Interfaces, Lambda Expressions, and Inner Classes

**四级证据 1/6。** Chapter 6: Interfaces, Lambda Expressions, and Inner Classes：Lambda 由目标函数式接口确定参数与返回类型，只能捕获 final 或有效 final 的局部变量。本节点以“默认方法”为观察焦点；运行`lambda.getClass().isSynthetic()`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 Chapter 6: Interfaces, Lambda Expressions, and Inner Classes 时，先预测它在 声明行为 阶段会怎样改变行为替换度；只切换行为表示或上下文耦合中的一项。若故障“Lambda 捕获可变上下文或代理遗漏 Object 方法语义，导致行为与身份不可预测”没有产生预期首错，就撤回当前解释并检查替代原因。

### 6.1 Interfaces

**四级证据 2/6。** 6.1 Interfaces：接口声明行为合同并支持多实现；默认方法解决演进问题时仍要处理继承冲突。本节点以“函数式接口”为观察焦点；运行`lambda.getClass().isSynthetic()`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 6.1 Interfaces 时，先预测它在 确定目标类型 阶段会怎样改变行为替换度；只切换行为表示或上下文耦合中的一项。若故障“Lambda 捕获可变上下文或代理遗漏 Object 方法语义，导致行为与身份不可预测”没有产生预期首错，就撤回当前解释并检查替代原因。

### 6.2 Lambda Expressions

**四级证据 3/6。** 6.2 Lambda Expressions：Lambda 由目标函数式接口确定参数与返回类型，只能捕获 final 或有效 final 的局部变量。本节点以“有效final”为观察焦点；运行`lambda.getClass().isSynthetic()`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 6.2 Lambda Expressions 时，先预测它在 捕获上下文 阶段会怎样改变行为替换度；只切换行为表示或上下文耦合中的一项。若故障“Lambda 捕获可变上下文或代理遗漏 Object 方法语义，导致行为与身份不可预测”没有产生预期首错，就撤回当前解释并检查替代原因。

### 6.3 Inner Classes

**四级证据 4/6。** 6.3 Inner Classes：非静态内部类持有外部实例引用，生命周期和序列化边界因此可能比源码表面更大。本节点以“内部类”为观察焦点；运行`lambda.getClass().isSynthetic()`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 6.3 Inner Classes 时，先预测它在 装配服务 阶段会怎样改变行为替换度；只切换行为表示或上下文耦合中的一项。若故障“Lambda 捕获可变上下文或代理遗漏 Object 方法语义，导致行为与身份不可预测”没有产生预期首错，就撤回当前解释并检查替代原因。

### 6.4 Service Loaders

**四级证据 5/6。** 6.4 Service Loaders：ServiceLoader 通过模块或类路径发现提供者，提供者身份、失败隔离和顺序不能依赖偶然扫描结果。本节点以“ServiceLoader”为观察焦点；运行`lambda.getClass().isSynthetic()`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 6.4 Service Loaders 时，先预测它在 代理调用 阶段会怎样改变行为替换度；只切换行为表示或上下文耦合中的一项。若故障“Lambda 捕获可变上下文或代理遗漏 Object 方法语义，导致行为与身份不可预测”没有产生预期首错，就撤回当前解释并检查替代原因。

### 6.5 Proxies

**四级证据 6/6。** 6.5 Proxies：动态代理把接口调用交给 InvocationHandler，异常解包、默认方法和 Object 方法都需设计。本节点以“Proxy”为观察焦点；运行`lambda.getClass().isSynthetic()`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 6.5 Proxies 时，先预测它在 声明行为 阶段会怎样改变行为替换度；只切换行为表示或上下文耦合中的一项。若故障“Lambda 捕获可变上下文或代理遗漏 Object 方法语义，导致行为与身份不可预测”没有产生预期首错，就撤回当前解释并检查替代原因。

## Java 25 最小可执行切片

```java
java.util.function.IntUnaryOperator twice = x -> x * 2;
System.out.println(twice.applyAsInt(21));
```

该切片围绕“lambda.getClass().isSynthetic()”保留本章最小机制，不承担完整生产实现。运行时固定 Java 25 发行版、操作系统、编译选项、模块或类路径和输入；保存标准输出、标准错误、退出码、资源状态与实际耗时。

```yaml
unit: jct-14e-v1-06-interfaces-lambdas-inner
axis_a: 行为表示
axis_b: 上下文耦合
fault: Lambda 捕获可变上下文或代理遗漏 Object 方法语义，导致行为与身份不可预测
invariant: 目标类型、捕获值和调用边界在执行前可说明
scenarios: [baseline, fault, recovery, reset]
```

## 三个必须主动触发的误区

<Callout type="trap" title="编译成功不等于合同成立">
  卷I 第6章 接口、Lambda 与内部类必须覆盖运行时输入、状态与资源边界；编译器没有承诺目标类型、捕获值和调用边界在执行前可说明会在所有外部条件下自动成立。
</Callout>

<Callout type="trap" title="本章核心故障">
  Lambda 捕获可变上下文或代理遗漏 Object 方法语义，导致行为与身份不可预测。先定位声明行为→确定目标类型→捕获上下文→装配服务→代理调用中的第一个分叉，再修改最小原因。
</Callout>

<Callout type="trap" title="修复后没有同输入重放">
  只展示新结果无法证明修复；必须恢复原始Java版本、输入与资源坐标，让原反例转成稳定回归，再执行重置。
</Callout>

## 练习、答案与目录节点验证

<Exercises>

**问题 1：单变量实验。** 怎样验证行为表示而不让上下文耦合混入结论？

<Answer>
  固定Java 25、输入、上下文耦合与观察窗口，只把行为表示从“Lambda”切到“服务实现”；保存行为替换度、隐藏捕获风险与首个诊断差异。
</Answer>

**问题 2：逐节点四级证据。** 本页正式目录怎样从“出现”升级为解释、可视与练习验证？

<Answer>
1. Chapter 6: Interfaces, Lambda Expressions, and Inner Classes：选择“默认方法”作为观察点，运行`lambda.getClass().isSynthetic()`或同义最小切片，再以故障、恢复和复位补齐四级证据。
2. 6.1 Interfaces：选择“函数式接口”作为观察点，运行`lambda.getClass().isSynthetic()`或同义最小切片，再以故障、恢复和复位补齐四级证据。
3. 6.2 Lambda Expressions：选择“有效final”作为观察点，运行`lambda.getClass().isSynthetic()`或同义最小切片，再以故障、恢复和复位补齐四级证据。
4. 6.3 Inner Classes：选择“内部类”作为观察点，运行`lambda.getClass().isSynthetic()`或同义最小切片，再以故障、恢复和复位补齐四级证据。
5. 6.4 Service Loaders：选择“ServiceLoader”作为观察点，运行`lambda.getClass().isSynthetic()`或同义最小切片，再以故障、恢复和复位补齐四级证据。
6. 6.5 Proxies：选择“Proxy”作为观察点，运行`lambda.getClass().isSynthetic()`或同义最小切片，再以故障、恢复和复位补齐四级证据。
</Answer>

**问题 3：故障闭环。** 遇到“Lambda 捕获可变上下文或代理遗漏 Object 方法语义，导致行为与身份不可预测”时怎样判定修复真正成立？

<Answer>
  在声明行为、确定目标类型、捕获上下文、装配服务、代理调用中定位首错，只修复该因果前提；随后重放正常、故障、恢复和复位四条轨迹。只有“目标类型、捕获值和调用边界在执行前可说明”再次成立，且计数、选择和资源都回到相同初值，本章才通过。
</Answer>

</Exercises>

## 术语复核与本章回顾

<Glossary>
  <GlossaryItem term="默认方法">默认方法是卷I 第6章 接口、Lambda 与内部类首先要固定的语言、API或运行时坐标。</GlossaryItem>
  <GlossaryItem term="函数式接口">函数式接口连接声明行为与确定目标类型，改变它必须留下编译或运行证据。</GlossaryItem>
  <GlossaryItem term="有效final">有效final标识卷I 第6章 接口、Lambda 与内部类中的状态、资源或协议责任，不能依赖默认环境猜测。</GlossaryItem>
  <GlossaryItem term="内部类">内部类用于观察行为替换度，同时要报告隐藏捕获风险。</GlossaryItem>
  <GlossaryItem term="ServiceLoader">ServiceLoader限定行为表示与上下文耦合的适用范围。</GlossaryItem>
  <GlossaryItem term="Proxy">Proxy进入卷I 第6章 接口、Lambda 与内部类的回归证据；故障修复后必须以相同输入重放。</GlossaryItem>
</Glossary>

完成卷I 第6章 接口、Lambda 与内部类意味着能解释比较接口合同、Lambda 目标类型、捕获语义、内部类外部实例、服务发现与动态代理。，能运行并改动章专属切片，能主动制造“Lambda 捕获可变上下文或代理遗漏 Object 方法语义，导致行为与身份不可预测”，还能凭可重放证据完成恢复与交接。只会复述目录或展示一次成功输出，均未满足本章门禁。

<Attribution
  mode="independent-rewrite"
  sourceBasis="outline-only"
  workTitle="Cay S. Horstmann, Core Java, Fourteenth Edition, Volumes I and II"
  adaptedUrl="https://www.informit.com/store/core-java-volume-i-fundamentals-9780135558577"
/>
