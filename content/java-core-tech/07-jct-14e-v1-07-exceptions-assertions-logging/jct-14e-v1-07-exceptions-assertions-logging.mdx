---
title: 卷I 第7章 异常、断言与日志
type: C
section: Java核心技术（第14版·全两卷 / Java 25）
order: 7
description: 把可恢复异常、程序缺陷断言和可运营日志分开，保存失败传播、资源关闭与诊断上下文。 覆盖7个正式节点，并通过Java 25代码、故障注入和复位证据验收。
demo: true
math: true
sourceUrl: 'https://www.informit.com/store/core-java-volume-i-fundamentals-9780135558577'
draft: false
qualityVersion: 2
practiceMode: diagnosis
sourceMode: independent-rewrite
---
import {
  Jct14eV107ExceptionsAssertionsLoggingMapLab,
  Jct14eV107ExceptionsAssertionsLoggingExperimentLab,
  Jct14eV107ExceptionsAssertionsLoggingEvidenceLab,
} from "@/components/mdx/java-core-tech/diagrams/jct-14e-v1-07-exceptions-assertions-logging";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释把可恢复异常、程序缺陷断言和可运营日志分开，保存失败传播、资源关闭与诊断上下文。
- 能沿识别失败、传播异常、选择处理器、关闭资源、记录诊断画出本章语义、状态、资源与失败传播链
- 能操作异常传播与资源关闭台，一次只改变处理位置或诊断上下文
- 能触发“catch Exception 后继续运行，或关闭异常覆盖原始异常而丢失根因”，根据失败要么被明确恢复，要么携带根因到达责任边界完成修复、复位与同输入重放

</Objectives>

{/* JCT_QUALITY_V2 */}

## 为什么从“异常传播与资源关闭台”开始

卷I 第7章 异常、断言与日志要解决的不是 API 名称记忆，而是把可恢复异常、程序缺陷断言和可运营日志分开，保存失败传播、资源关闭与诊断上下文。 直觉上，编译成功只排除了部分静态错误；运行一次成功也没有覆盖空值、极值、取消、并发、资源关闭、区域、协议与版本边界。

先预测：把处理位置从“边界转换”切换为“顶层决议”，根因定位率与失败遮蔽率会如何变化？随后只改这一项运行探针；结果不符时修改机制假设，不移动输入边界。

## 来源、版次与独立重写边界

InformIT/Oracle Press 的[卷 I 产品页](https://www.informit.com/store/core-java-volume-i-fundamentals-9780135558577)与[卷 II 产品页](https://www.informit.com/store/core-java-volume-ii-advanced-features-9780135558690)确认 Cay S. Horstmann《Core Java》第14版全两卷于2025年10月出版、面向 Java 25，并给出25章、214个正式章/节标题。未取得本章原书正文；产品页只限定标题和小节范围，目录核对不被表述为正文忠实。

本页技术事实由[Java SE 25 API](https://docs.oracle.com/en/java/javase/25/docs/api/index.html)、[Java SE 25 语言规范](https://docs.oracle.com/javase/specs/jls/se25/html/index.html)、[Java SE 25 JVM 规范](https://docs.oracle.com/javase/specs/jvms/se25/html/index.html)与[OpenJDK 25 项目页](https://openjdk.org/projects/jdk/25/)中的适用部分独立复核。中文解释、代码、图示、交互、参数、练习与答案均为独立教学重写；Oracle 文档许可证不被误报为原书授权。

## 本章机制与六个检查点

<Term def="checked异常是卷I 第7章 异常、断言与日志首先要固定的语言、API或运行时坐标。">checked异常</Term>、<Term def="try-with-resources连接识别失败与传播异常，改变它必须留下编译或运行证据。">try-with-resources</Term>、<Term def="suppressed标识卷I 第7章 异常、断言与日志中的状态、资源或协议责任，不能依赖默认环境猜测。">suppressed</Term>、<Term def="assert用于观察根因定位率，同时要报告失败遮蔽率。">assert</Term>、<Term def="Logger限定处理位置与诊断上下文的适用范围。">Logger</Term>、<Term def="根因链进入卷I 第7章 异常、断言与日志的回归证据；故障修复后必须以相同输入重放。">根因链</Term>。

卷I 第7章 异常、断言与日志以“失败要么被明确恢复，要么携带根因到达责任边界”为不变量。处理位置与诊断上下文是离散输入，根因定位率是主结果，失败遮蔽率必须单独报告；这些交互分数是课程中的透明反馈，不是原书或JDK性能基准。

## 先预测，再操作三层章专属实验

<Stepper>
  <Step title="1. 正式节点与语义运行链">
    选择任一正式节点，核对它在识别失败、传播异常、选择处理器、关闭资源、记录诊断中的位置及负责证据。

    <Jct14eV107ExceptionsAssertionsLoggingMapLab />
  </Step>
  <Step title="2. 单变量代码或配置实验">
    只切换处理位置或诊断上下文，比较结果与风险，并运行本章最小探针。

    <Jct14eV107ExceptionsAssertionsLoggingExperimentLab />
  </Step>
  <Step title="3. 故障、恢复与复位">
    注入“catch Exception 后继续运行，或关闭异常覆盖原始异常而丢失根因”，保存首错；修复后以相同输入重放，最后点击重置核对初值。

    <Jct14eV107ExceptionsAssertionsLoggingEvidenceLab />
  </Step>
</Stepper>

## 官方目录逐项深读

### Chapter 7: Exceptions, Assertions, and Logging

**四级证据 1/7。** Chapter 7: Exceptions, Assertions, and Logging：assert 用于内部不变量且可在运行时关闭，不能承担公开参数校验或安全检查。本节点以“checked异常”为观察焦点；运行`exception.getSuppressed().length`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 Chapter 7: Exceptions, Assertions, and Logging 时，先预测它在 识别失败 阶段会怎样改变根因定位率；只切换处理位置或诊断上下文中的一项。若故障“catch Exception 后继续运行，或关闭异常覆盖原始异常而丢失根因”没有产生预期首错，就撤回当前解释并检查替代原因。

### 7.1 Dealing with Errors

**四级证据 2/7。** 7.1 Dealing with Errors：异常类型表达失败类别，处理器只有能恢复或添加责任边界上下文时才应捕获。本节点以“try-with-resources”为观察焦点；运行`exception.getSuppressed().length`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 7.1 Dealing with Errors 时，先预测它在 传播异常 阶段会怎样改变根因定位率；只切换处理位置或诊断上下文中的一项。若故障“catch Exception 后继续运行，或关闭异常覆盖原始异常而丢失根因”没有产生预期首错，就撤回当前解释并检查替代原因。

### 7.2 Catching Exceptions

**四级证据 3/7。** 7.2 Catching Exceptions：异常类型表达失败类别，处理器只有能恢复或添加责任边界上下文时才应捕获。本节点以“suppressed”为观察焦点；运行`exception.getSuppressed().length`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 7.2 Catching Exceptions 时，先预测它在 选择处理器 阶段会怎样改变根因定位率；只切换处理位置或诊断上下文中的一项。若故障“catch Exception 后继续运行，或关闭异常覆盖原始异常而丢失根因”没有产生预期首错，就撤回当前解释并检查替代原因。

### 7.3 Tips for Using Exceptions

**四级证据 4/7。** 7.3 Tips for Using Exceptions：异常不能替代普通控制流；保留 cause、输入坐标和资源清理比笼统包装更重要。本节点以“assert”为观察焦点；运行`exception.getSuppressed().length`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 7.3 Tips for Using Exceptions 时，先预测它在 关闭资源 阶段会怎样改变根因定位率；只切换处理位置或诊断上下文中的一项。若故障“catch Exception 后继续运行，或关闭异常覆盖原始异常而丢失根因”没有产生预期首错，就撤回当前解释并检查替代原因。

### 7.4 Using Assertions

**四级证据 5/7。** 7.4 Using Assertions：assert 用于内部不变量且可在运行时关闭，不能承担公开参数校验或安全检查。本节点以“Logger”为观察焦点；运行`exception.getSuppressed().length`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 7.4 Using Assertions 时，先预测它在 记录诊断 阶段会怎样改变根因定位率；只切换处理位置或诊断上下文中的一项。若故障“catch Exception 后继续运行，或关闭异常覆盖原始异常而丢失根因”没有产生预期首错，就撤回当前解释并检查替代原因。

### 7.5 Logging

**四级证据 6/7。** 7.5 Logging：日志要结构化记录事件、上下文与因果链，同时避免泄露密钥、口令和受保护数据。本节点以“根因链”为观察焦点；运行`exception.getSuppressed().length`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 7.5 Logging 时，先预测它在 识别失败 阶段会怎样改变根因定位率；只切换处理位置或诊断上下文中的一项。若故障“catch Exception 后继续运行，或关闭异常覆盖原始异常而丢失根因”没有产生预期首错，就撤回当前解释并检查替代原因。

### 7.6 Debugging Tips

**四级证据 7/7。** 7.6 Debugging Tips：调试从可重放输入和首个分叉开始，观察器自身对时序与并发的扰动也应记录。本节点以“checked异常”为观察焦点；运行`exception.getSuppressed().length`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 7.6 Debugging Tips 时，先预测它在 传播异常 阶段会怎样改变根因定位率；只切换处理位置或诊断上下文中的一项。若故障“catch Exception 后继续运行，或关闭异常覆盖原始异常而丢失根因”没有产生预期首错，就撤回当前解释并检查替代原因。

## Java 25 最小可执行切片

```java
try (var in = java.nio.file.Files.newInputStream(path)) {
  return in.read();
} catch (java.io.IOException ex) {
  throw new IllegalStateException("read " + path, ex);
}
```

该切片围绕“exception.getSuppressed().length”保留本章最小机制，不承担完整生产实现。运行时固定 Java 25 发行版、操作系统、编译选项、模块或类路径和输入；保存标准输出、标准错误、退出码、资源状态与实际耗时。

```yaml
unit: jct-14e-v1-07-exceptions-assertions-logging
axis_a: 处理位置
axis_b: 诊断上下文
fault: catch Exception 后继续运行，或关闭异常覆盖原始异常而丢失根因
invariant: 失败要么被明确恢复，要么携带根因到达责任边界
scenarios: [baseline, fault, recovery, reset]
```

## 三个必须主动触发的误区

<Callout type="trap" title="编译成功不等于合同成立">
  卷I 第7章 异常、断言与日志必须覆盖运行时输入、状态与资源边界；编译器没有承诺失败要么被明确恢复，要么携带根因到达责任边界会在所有外部条件下自动成立。
</Callout>

<Callout type="trap" title="本章核心故障">
  catch Exception 后继续运行，或关闭异常覆盖原始异常而丢失根因。先定位识别失败→传播异常→选择处理器→关闭资源→记录诊断中的第一个分叉，再修改最小原因。
</Callout>

<Callout type="trap" title="修复后没有同输入重放">
  只展示新结果无法证明修复；必须恢复原始Java版本、输入与资源坐标，让原反例转成稳定回归，再执行重置。
</Callout>

## 练习、答案与目录节点验证

<Exercises>

**问题 1：单变量实验。** 怎样验证处理位置而不让诊断上下文混入结论？

<Answer>
  固定Java 25、输入、诊断上下文与观察窗口，只把处理位置从“边界转换”切到“顶层决议”；保存根因定位率、失败遮蔽率与首个诊断差异。
</Answer>

**问题 2：逐节点四级证据。** 本页正式目录怎样从“出现”升级为解释、可视与练习验证？

<Answer>
1. Chapter 7: Exceptions, Assertions, and Logging：选择“checked异常”作为观察点，运行`exception.getSuppressed().length`或同义最小切片，再以故障、恢复和复位补齐四级证据。
2. 7.1 Dealing with Errors：选择“try-with-resources”作为观察点，运行`exception.getSuppressed().length`或同义最小切片，再以故障、恢复和复位补齐四级证据。
3. 7.2 Catching Exceptions：选择“suppressed”作为观察点，运行`exception.getSuppressed().length`或同义最小切片，再以故障、恢复和复位补齐四级证据。
4. 7.3 Tips for Using Exceptions：选择“assert”作为观察点，运行`exception.getSuppressed().length`或同义最小切片，再以故障、恢复和复位补齐四级证据。
5. 7.4 Using Assertions：选择“Logger”作为观察点，运行`exception.getSuppressed().length`或同义最小切片，再以故障、恢复和复位补齐四级证据。
6. 7.5 Logging：选择“根因链”作为观察点，运行`exception.getSuppressed().length`或同义最小切片，再以故障、恢复和复位补齐四级证据。
7. 7.6 Debugging Tips：选择“checked异常”作为观察点，运行`exception.getSuppressed().length`或同义最小切片，再以故障、恢复和复位补齐四级证据。
</Answer>

**问题 3：故障闭环。** 遇到“catch Exception 后继续运行，或关闭异常覆盖原始异常而丢失根因”时怎样判定修复真正成立？

<Answer>
  在识别失败、传播异常、选择处理器、关闭资源、记录诊断中定位首错，只修复该因果前提；随后重放正常、故障、恢复和复位四条轨迹。只有“失败要么被明确恢复，要么携带根因到达责任边界”再次成立，且计数、选择和资源都回到相同初值，本章才通过。
</Answer>

</Exercises>

## 术语复核与本章回顾

<Glossary>
  <GlossaryItem term="checked异常">checked异常是卷I 第7章 异常、断言与日志首先要固定的语言、API或运行时坐标。</GlossaryItem>
  <GlossaryItem term="try-with-resources">try-with-resources连接识别失败与传播异常，改变它必须留下编译或运行证据。</GlossaryItem>
  <GlossaryItem term="suppressed">suppressed标识卷I 第7章 异常、断言与日志中的状态、资源或协议责任，不能依赖默认环境猜测。</GlossaryItem>
  <GlossaryItem term="assert">assert用于观察根因定位率，同时要报告失败遮蔽率。</GlossaryItem>
  <GlossaryItem term="Logger">Logger限定处理位置与诊断上下文的适用范围。</GlossaryItem>
  <GlossaryItem term="根因链">根因链进入卷I 第7章 异常、断言与日志的回归证据；故障修复后必须以相同输入重放。</GlossaryItem>
</Glossary>

完成卷I 第7章 异常、断言与日志意味着能解释把可恢复异常、程序缺陷断言和可运营日志分开，保存失败传播、资源关闭与诊断上下文。，能运行并改动章专属切片，能主动制造“catch Exception 后继续运行，或关闭异常覆盖原始异常而丢失根因”，还能凭可重放证据完成恢复与交接。只会复述目录或展示一次成功输出，均未满足本章门禁。

<Attribution
  mode="independent-rewrite"
  sourceBasis="outline-only"
  workTitle="Cay S. Horstmann, Core Java, Fourteenth Edition, Volumes I and II"
  adaptedUrl="https://www.informit.com/store/core-java-volume-i-fundamentals-9780135558577"
/>
