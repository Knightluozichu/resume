---
title: 卷I 第9章 集合
type: C
section: Java核心技术（第14版·全两卷 / Java 25）
order: 9
description: 按顺序、唯一性、键值索引、并发与操作复杂度选择集合，并验证视图、迭代器和遗留类型语义。 覆盖8个正式节点，并通过Java 25代码、故障注入和复位证据验收。
demo: true
math: true
sourceUrl: 'https://www.informit.com/store/core-java-volume-i-fundamentals-9780135558577'
draft: false
qualityVersion: 2
practiceMode: code
sourceMode: independent-rewrite
---


{/* JCT_QUALITY_V2 */}

## 为什么从“集合结构选择器”开始

卷I 第9章 集合要解决的不是 API 名称记忆，而是按顺序、唯一性、键值索引、并发与操作复杂度选择集合，并验证视图、迭代器和遗留类型语义。 直觉上，编译成功只排除了部分静态错误；运行一次成功也没有覆盖空值、极值、取消、并发、资源关闭、区域、协议与版本边界。

先预测：把主要访问模式从“成员查询”切换为“键值聚合”，操作匹配度与语义错配成本会如何变化？随后只改这一项运行探针；结果不符时修改机制假设，不移动输入边界。

## 来源、版次与独立重写边界

InformIT/Oracle Press 的[卷 I 产品页](https://www.informit.com/store/core-java-volume-i-fundamentals-9780135558577)与[卷 II 产品页](https://www.informit.com/store/core-java-volume-ii-advanced-features-9780135558690)确认 Cay S. Horstmann《Core Java》第14版全两卷于2025年10月出版、面向 Java 25，并给出25章、214个正式章/节标题。未取得本章原书正文；产品页只限定标题和小节范围，目录核对不被表述为正文忠实。

本页技术事实由[Java SE 25 API](https://docs.oracle.com/en/java/javase/25/docs/api/index.html)、[Java SE 25 语言规范](https://docs.oracle.com/javase/specs/jls/se25/html/index.html)、[Java SE 25 JVM 规范](https://docs.oracle.com/javase/specs/jvms/se25/html/index.html)与[OpenJDK 25 项目页](https://openjdk.org/projects/jdk/25/)中的适用部分独立复核。中文解释、代码、图示、交互、参数、练习与答案均为独立教学重写；Oracle 文档许可证不被误报为原书授权。

## 本章机制与六个检查点

、、、、、。

卷I 第9章 集合以“实现选择必须守住接口语义与所需复杂度”为不变量。主要访问模式与数据语义是离散输入，操作匹配度是主结果，语义错配成本必须单独报告；这些交互分数是课程中的透明反馈，不是原书或JDK性能基准。

## 官方目录逐项深读

### Chapter 9: Collections

**四级证据 1/8。** Chapter 9: Collections：Chapter 9: Collections必须放进声明操作—选择接口—选择实现—执行算法—验证视图这条本章机制链，而不能从目录词直接推断实现保证。本节点以“List”为观察焦点；运行`map.merge(key, 1, Integer::sum)`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 Chapter 9: Collections 时，先预测它在 声明操作 阶段会怎样改变操作匹配度；只切换主要访问模式或数据语义中的一项。若故障“在遍历期间结构性修改集合，或把受支持的视图误当成独立副本”没有产生预期首错，就撤回当前解释并检查替代原因。

### 9.1 The Java Collections Framework

**四级证据 2/8。** 9.1 The Java Collections Framework：集合框架以接口分离序列、集合、队列和映射语义，再由实现权衡顺序、内存和复杂度。本节点以“Set”为观察焦点；运行`map.merge(key, 1, Integer::sum)`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 9.1 The Java Collections Framework 时，先预测它在 选择接口 阶段会怎样改变操作匹配度；只切换主要访问模式或数据语义中的一项。若故障“在遍历期间结构性修改集合，或把受支持的视图误当成独立副本”没有产生预期首错，就撤回当前解释并检查替代原因。

### 9.2 Interfaces in the Collections Framework

**四级证据 3/8。** 9.2 Interfaces in the Collections Framework：集合框架以接口分离序列、集合、队列和映射语义，再由实现权衡顺序、内存和复杂度。本节点以“Map”为观察焦点；运行`map.merge(key, 1, Integer::sum)`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 9.2 Interfaces in the Collections Framework 时，先预测它在 选择实现 阶段会怎样改变操作匹配度；只切换主要访问模式或数据语义中的一项。若故障“在遍历期间结构性修改集合，或把受支持的视图误当成独立副本”没有产生预期首错，就撤回当前解释并检查替代原因。

### 9.3 Concrete Collections

**四级证据 4/8。** 9.3 Concrete Collections：具体集合应按主要操作、顺序、重复、null 与并发需求选择，不能仅凭熟悉度默认 HashMap。本节点以“迭代器”为观察焦点；运行`map.merge(key, 1, Integer::sum)`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 9.3 Concrete Collections 时，先预测它在 执行算法 阶段会怎样改变操作匹配度；只切换主要访问模式或数据语义中的一项。若故障“在遍历期间结构性修改集合，或把受支持的视图误当成独立副本”没有产生预期首错，就撤回当前解释并检查替代原因。

### 9.4 Maps

**四级证据 5/8。** 9.4 Maps：Map 维护键到值的关联；键的 equals/hashCode 稳定性直接决定查找与更新能否正确。本节点以“视图”为观察焦点；运行`map.merge(key, 1, Integer::sum)`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 9.4 Maps 时，先预测它在 验证视图 阶段会怎样改变操作匹配度；只切换主要访问模式或数据语义中的一项。若故障“在遍历期间结构性修改集合，或把受支持的视图误当成独立副本”没有产生预期首错，就撤回当前解释并检查替代原因。

### 9.5 Copies and Views

**四级证据 6/8。** 9.5 Copies and Views：副本拥有独立结构，视图把操作映射到原集合；修改传播和不支持操作需要反例验证。本节点以“复杂度”为观察焦点；运行`map.merge(key, 1, Integer::sum)`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 9.5 Copies and Views 时，先预测它在 声明操作 阶段会怎样改变操作匹配度；只切换主要访问模式或数据语义中的一项。若故障“在遍历期间结构性修改集合，或把受支持的视图误当成独立副本”没有产生预期首错，就撤回当前解释并检查替代原因。

### 9.6 Algorithms

**四级证据 7/8。** 9.6 Algorithms：集合算法的正确性依赖排序、比较器一致性和可修改性，复杂度也受底层结构影响。本节点以“List”为观察焦点；运行`map.merge(key, 1, Integer::sum)`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 9.6 Algorithms 时，先预测它在 选择接口 阶段会怎样改变操作匹配度；只切换主要访问模式或数据语义中的一项。若故障“在遍历期间结构性修改集合，或把受支持的视图误当成独立副本”没有产生预期首错，就撤回当前解释并检查替代原因。

### 9.7 Legacy Collections

**四级证据 8/8。** 9.7 Legacy Collections：遗留集合保留兼容价值但常携带旧同步或类型模型，迁移要保存行为而不是机械替名。本节点以“Set”为观察焦点；运行`map.merge(key, 1, Integer::sum)`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 9.7 Legacy Collections 时，先预测它在 选择实现 阶段会怎样改变操作匹配度；只切换主要访问模式或数据语义中的一项。若故障“在遍历期间结构性修改集合，或把受支持的视图误当成独立副本”没有产生预期首错，就撤回当前解释并检查替代原因。

## Java 25 最小可执行切片

```java
var counts = new java.util.HashMap<String,Integer>();
words.forEach(w -> counts.merge(w, 1, Integer::sum));
```

该切片围绕“map.merge(key, 1, Integer::sum)”保留本章最小机制，不承担完整生产实现。运行时固定 Java 25 发行版、操作系统、编译选项、模块或类路径和输入；保存标准输出、标准错误、退出码、资源状态与实际耗时。

```yaml
unit: jct-14e-v1-09-collections
axis_a: 主要访问模式
axis_b: 数据语义
fault: 在遍历期间结构性修改集合，或把受支持的视图误当成独立副本
invariant: 实现选择必须守住接口语义与所需复杂度
scenarios: [baseline, fault, recovery, reset]
```

## 三个必须主动触发的误区


## 练习、答案与目录节点验证


## 术语复核与本章回顾


完成卷I 第9章 集合意味着能解释按顺序、唯一性、键值索引、并发与操作复杂度选择集合，并验证视图、迭代器和遗留类型语义。，能运行并改动章专属切片，能主动制造“在遍历期间结构性修改集合，或把受支持的视图误当成独立副本”，还能凭可重放证据完成恢复与交接。只会复述目录或展示一次成功输出，均未满足本章门禁。

