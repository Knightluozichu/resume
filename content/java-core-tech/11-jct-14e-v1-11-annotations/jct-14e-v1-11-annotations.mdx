---
title: 卷I 第11章 注解
type: C
section: Java核心技术（第14版·全两卷 / Java 25）
order: 11
description: >-
  从 Target、Retention 与元素合同出发，区分编译期处理、运行时反射和字节码变换。 覆盖7个正式节点，并通过Java
  25代码、故障注入和复位证据验收。
demo: true
math: true
sourceUrl: 'https://www.informit.com/store/core-java-volume-i-fundamentals-9780135558577'
draft: false
qualityVersion: 2
practiceMode: code
sourceMode: independent-rewrite
---


{/* JCT_QUALITY_V2 */}

## 为什么从“注解生命周期处理台”开始

卷I 第11章 注解要解决的不是 API 名称记忆，而是从 Target、Retention 与元素合同出发，区分编译期处理、运行时反射和字节码变换。 直觉上，编译成功只排除了部分静态错误；运行一次成功也没有覆盖空值、极值、取消、并发、资源关闭、区域、协议与版本边界。

先预测：把处理阶段从“CLASS”切换为“RUNTIME”，元数据可用率与阶段错配风险会如何变化？随后只改这一项运行探针；结果不符时修改机制假设，不移动输入边界。

## 来源、版次与独立重写边界

InformIT/Oracle Press 的[卷 I 产品页](https://www.informit.com/store/core-java-volume-i-fundamentals-9780135558577)与[卷 II 产品页](https://www.informit.com/store/core-java-volume-ii-advanced-features-9780135558690)确认 Cay S. Horstmann《Core Java》第14版全两卷于2025年10月出版、面向 Java 25，并给出25章、214个正式章/节标题。未取得本章原书正文；产品页只限定标题和小节范围，目录核对不被表述为正文忠实。

本页技术事实由[Java SE 25 API](https://docs.oracle.com/en/java/javase/25/docs/api/index.html)、[Java SE 25 语言规范](https://docs.oracle.com/javase/specs/jls/se25/html/index.html)、[Java SE 25 JVM 规范](https://docs.oracle.com/javase/specs/jvms/se25/html/index.html)与[OpenJDK 25 项目页](https://openjdk.org/projects/jdk/25/)中的适用部分独立复核。中文解释、代码、图示、交互、参数、练习与答案均为独立教学重写；Oracle 文档许可证不被误报为原书授权。

## 本章机制与六个检查点

、、、、、。

卷I 第11章 注解以“使用方只在声明的目标与保留阶段读取注解”为不变量。处理阶段与目标范围是离散输入，元数据可用率是主结果，阶段错配风险必须单独报告；这些交互分数是课程中的透明反馈，不是原书或JDK性能基准。

## 官方目录逐项深读

### Chapter 11: Annotations

**四级证据 1/7。** Chapter 11: Annotations：Chapter 11: Annotations必须放进定义注解—选择目标—保留元数据—处理模型—检查产物这条本章机制链，而不能从目录词直接推断实现保证。本节点以“Target”为观察焦点；运行`element.getAnnotation(Audited.class)`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 Chapter 11: Annotations 时，先预测它在 定义注解 阶段会怎样改变元数据可用率；只切换处理阶段或目标范围中的一项。若故障“选择SOURCE保留却在运行时反射读取，或处理器生成不稳定源码造成重复构建差异”没有产生预期首错，就撤回当前解释并检查替代原因。

### 11.1 Using Annotations

**四级证据 2/7。** 11.1 Using Annotations：注解实例由接口元素、默认值与元注解定义，使用位置必须符合 Target。本节点以“Retention”为观察焦点；运行`element.getAnnotation(Audited.class)`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 11.1 Using Annotations 时，先预测它在 选择目标 阶段会怎样改变元数据可用率；只切换处理阶段或目标范围中的一项。若故障“选择SOURCE保留却在运行时反射读取，或处理器生成不稳定源码造成重复构建差异”没有产生预期首错，就撤回当前解释并检查替代原因。

### 11.2 Defining Annotations

**四级证据 3/7。** 11.2 Defining Annotations：注解实例由接口元素、默认值与元注解定义，使用位置必须符合 Target。本节点以“重复注解”为观察焦点；运行`element.getAnnotation(Audited.class)`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 11.2 Defining Annotations 时，先预测它在 保留元数据 阶段会怎样改变元数据可用率；只切换处理阶段或目标范围中的一项。若故障“选择SOURCE保留却在运行时反射读取，或处理器生成不稳定源码造成重复构建差异”没有产生预期首错，就撤回当前解释并检查替代原因。

### 11.3 Annotations in the Java API

**四级证据 4/7。** 11.3 Annotations in the Java API：标准注解的语义由各使用方解释，例如 Override 由编译器检查，Deprecated 传递迁移信号。本节点以“反射处理”为观察焦点；运行`element.getAnnotation(Audited.class)`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 11.3 Annotations in the Java API 时，先预测它在 处理模型 阶段会怎样改变元数据可用率；只切换处理阶段或目标范围中的一项。若故障“选择SOURCE保留却在运行时反射读取，或处理器生成不稳定源码造成重复构建差异”没有产生预期首错，就撤回当前解释并检查替代原因。

### 11.4 Processing Annotations at Runtime

**四级证据 5/7。** 11.4 Processing Annotations at Runtime：运行时注解需要 RUNTIME 保留策略并接受模块访问边界，反射扫描成本也要量化。本节点以“Processor”为观察焦点；运行`element.getAnnotation(Audited.class)`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 11.4 Processing Annotations at Runtime 时，先预测它在 检查产物 阶段会怎样改变元数据可用率；只切换处理阶段或目标范围中的一项。若故障“选择SOURCE保留却在运行时反射读取，或处理器生成不稳定源码造成重复构建差异”没有产生预期首错，就撤回当前解释并检查替代原因。

### 11.5 Source-Level Annotation Processing

**四级证据 6/7。** 11.5 Source-Level Annotation Processing：源级处理器按轮次读取语言模型并生成文件，输出应确定且不能修改既有用户源码。本节点以“字节码”为观察焦点；运行`element.getAnnotation(Audited.class)`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 11.5 Source-Level Annotation Processing 时，先预测它在 定义注解 阶段会怎样改变元数据可用率；只切换处理阶段或目标范围中的一项。若故障“选择SOURCE保留却在运行时反射读取，或处理器生成不稳定源码造成重复构建差异”没有产生预期首错，就撤回当前解释并检查替代原因。

### 11.6 Bytecode Engineering

**四级证据 7/7。** 11.6 Bytecode Engineering：字节码变换必须符合 class 文件与验证规则，并记录输入输出哈希、代理版本和回退路径。本节点以“Target”为观察焦点；运行`element.getAnnotation(Audited.class)`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 11.6 Bytecode Engineering 时，先预测它在 选择目标 阶段会怎样改变元数据可用率；只切换处理阶段或目标范围中的一项。若故障“选择SOURCE保留却在运行时反射读取，或处理器生成不稳定源码造成重复构建差异”没有产生预期首错，就撤回当前解释并检查替代原因。

## Java 25 最小可执行切片

```java
@java.lang.annotation.Retention(java.lang.annotation.RetentionPolicy.RUNTIME)
@interface Audited { String value(); }
```

该切片围绕“element.getAnnotation(Audited.class)”保留本章最小机制，不承担完整生产实现。运行时固定 Java 25 发行版、操作系统、编译选项、模块或类路径和输入；保存标准输出、标准错误、退出码、资源状态与实际耗时。

```yaml
unit: jct-14e-v1-11-annotations
axis_a: 处理阶段
axis_b: 目标范围
fault: 选择SOURCE保留却在运行时反射读取，或处理器生成不稳定源码造成重复构建差异
invariant: 使用方只在声明的目标与保留阶段读取注解
scenarios: [baseline, fault, recovery, reset]
```

## 三个必须主动触发的误区


## 练习、答案与目录节点验证


## 术语复核与本章回顾


完成卷I 第11章 注解意味着能解释从 Target、Retention 与元素合同出发，区分编译期处理、运行时反射和字节码变换。，能运行并改动章专属切片，能主动制造“选择SOURCE保留却在运行时反射读取，或处理器生成不稳定源码造成重复构建差异”，还能凭可重放证据完成恢复与交接。只会复述目录或展示一次成功输出，均未满足本章门禁。

