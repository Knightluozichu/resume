---
title: 卷I 第12章 Java 平台模块系统
type: C
section: Java核心技术（第14版·全两卷 / Java 25）
order: 12
description: >-
  用 requires、exports、opens、服务装配与迁移工具建立可读性、可访问性和反射边界。 覆盖16个正式节点，并通过Java
  25代码、故障注入和复位证据验收。
demo: true
math: true
sourceUrl: 'https://www.informit.com/store/core-java-volume-i-fundamentals-9780135558577'
draft: false
qualityVersion: 2
practiceMode: design
sourceMode: independent-rewrite
---


{/* JCT_QUALITY_V2 */}

## 为什么从“模块可读性图验证台”开始

卷I 第12章 Java 平台模块系统要解决的不是 API 名称记忆，而是用 requires、exports、opens、服务装配与迁移工具建立可读性、可访问性和反射边界。 直觉上，编译成功只排除了部分静态错误；运行一次成功也没有覆盖空值、极值、取消、并发、资源关闭、区域、协议与版本边界。

先预测：把依赖声明从“自动模块”切换为“显式模块”，边界可解释度与封装穿透风险会如何变化？随后只改这一项运行探针；结果不符时修改机制假设，不移动输入边界。

## 来源、版次与独立重写边界

InformIT/Oracle Press 的[卷 I 产品页](https://www.informit.com/store/core-java-volume-i-fundamentals-9780135558577)与[卷 II 产品页](https://www.informit.com/store/core-java-volume-ii-advanced-features-9780135558690)确认 Cay S. Horstmann《Core Java》第14版全两卷于2025年10月出版、面向 Java 25，并给出25章、214个正式章/节标题。未取得本章原书正文；产品页只限定标题和小节范围，目录核对不被表述为正文忠实。

本页技术事实由[Java SE 25 API](https://docs.oracle.com/en/java/javase/25/docs/api/index.html)、[Java SE 25 语言规范](https://docs.oracle.com/javase/specs/jls/se25/html/index.html)、[Java SE 25 JVM 规范](https://docs.oracle.com/javase/specs/jvms/se25/html/index.html)与[OpenJDK 25 项目页](https://openjdk.org/projects/jdk/25/)中的适用部分独立复核。中文解释、代码、图示、交互、参数、练习与答案均为独立教学重写；Oracle 文档许可证不被误报为原书授权。

## 本章机制与六个检查点

、、、、、。

卷I 第12章 Java 平台模块系统以“编译期可读性与运行时反射权限分别最小化声明”为不变量。依赖声明与包暴露是离散输入，边界可解释度是主结果，封装穿透风险必须单独报告；这些交互分数是课程中的透明反馈，不是原书或JDK性能基准。

## 官方目录逐项深读

### Chapter 12: The Java Platform Module System

**四级证据 1/16。** Chapter 12: The Java Platform Module System：Chapter 12: The Java Platform Module System必须放进命名模块—声明依赖—导出API—开放反射—链接运行时这条本章机制链，而不能从目录词直接推断实现保证。本节点以“module-info”为观察焦点；运行`jdeps --print-module-deps app.jar`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 Chapter 12: The Java Platform Module System 时，先预测它在 命名模块 阶段会怎样改变边界可解释度；只切换依赖声明或包暴露中的一项。若故障“为修复反射错误直接open整个模块，或依赖自动模块名却没有迁移记录”没有产生预期首错，就撤回当前解释并检查替代原因。

### 12.1 The Module Concept

**四级证据 2/16。** 12.1 The Module Concept：命名模块把依赖和包暴露写进描述符，稳定名称是可读性图和发布身份的一部分。本节点以“requires”为观察焦点；运行`jdeps --print-module-deps app.jar`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 12.1 The Module Concept 时，先预测它在 声明依赖 阶段会怎样改变边界可解释度；只切换依赖声明或包暴露中的一项。若故障“为修复反射错误直接open整个模块，或依赖自动模块名却没有迁移记录”没有产生预期首错，就撤回当前解释并检查替代原因。

### 12.2 Naming Modules

**四级证据 3/16。** 12.2 Naming Modules：命名模块把依赖和包暴露写进描述符，稳定名称是可读性图和发布身份的一部分。本节点以“exports”为观察焦点；运行`jdeps --print-module-deps app.jar`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 12.2 Naming Modules 时，先预测它在 导出API 阶段会怎样改变边界可解释度；只切换依赖声明或包暴露中的一项。若故障“为修复反射错误直接open整个模块，或依赖自动模块名却没有迁移记录”没有产生预期首错，就撤回当前解释并检查替代原因。

### 12.3 The Modular "Hello, World!" Program

**四级证据 4/16。** 12.3 The Modular "Hello, World!" Program：最小模块程序同时验证 module-info、包路径、编译输出与 -m 启动坐标。本节点以“opens”为观察焦点；运行`jdeps --print-module-deps app.jar`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 12.3 The Modular "Hello, World!" Program 时，先预测它在 开放反射 阶段会怎样改变边界可解释度；只切换依赖声明或包暴露中的一项。若故障“为修复反射错误直接open整个模块，或依赖自动模块名却没有迁移记录”没有产生预期首错，就撤回当前解释并检查替代原因。

### 12.4 Requiring Modules

**四级证据 5/16。** 12.4 Requiring Modules：requires 建立读取边，只有被读模块导出的包才成为普通编译访问候选。本节点以“ServiceLoader”为观察焦点；运行`jdeps --print-module-deps app.jar`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 12.4 Requiring Modules 时，先预测它在 链接运行时 阶段会怎样改变边界可解释度；只切换依赖声明或包暴露中的一项。若故障“为修复反射错误直接open整个模块，或依赖自动模块名却没有迁移记录”没有产生预期首错，就撤回当前解释并检查替代原因。

### 12.5 Exporting Packages

**四级证据 6/16。** 12.5 Exporting Packages：包组织名称与包级访问；它不是独立部署或强封装边界，跨模块可见性还受 JPMS 控制。本节点以“jdeps”为观察焦点；运行`jdeps --print-module-deps app.jar`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 12.5 Exporting Packages 时，先预测它在 命名模块 阶段会怎样改变边界可解释度；只切换依赖声明或包暴露中的一项。若故障“为修复反射错误直接open整个模块，或依赖自动模块名却没有迁移记录”没有产生预期首错，就撤回当前解释并检查替代原因。

### 12.6 Modular JARs

**四级证据 7/16。** 12.6 Modular JARs：模块化JAR携带描述符；模块路径、主类和版本元数据应在发布前由工具检查。本节点以“module-info”为观察焦点；运行`jdeps --print-module-deps app.jar`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 12.6 Modular JARs 时，先预测它在 声明依赖 阶段会怎样改变边界可解释度；只切换依赖声明或包暴露中的一项。若故障“为修复反射错误直接open整个模块，或依赖自动模块名却没有迁移记录”没有产生预期首错，就撤回当前解释并检查替代原因。

### 12.7 Modules and Reflective Access

**四级证据 8/16。** 12.7 Modules and Reflective Access：opens 控制深反射访问，可限定目标模块；为便利全开放会扩大封装和安全影响面。本节点以“requires”为观察焦点；运行`jdeps --print-module-deps app.jar`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 12.7 Modules and Reflective Access 时，先预测它在 导出API 阶段会怎样改变边界可解释度；只切换依赖声明或包暴露中的一项。若故障“为修复反射错误直接open整个模块，或依赖自动模块名却没有迁移记录”没有产生预期首错，就撤回当前解释并检查替代原因。

### 12.8 Automatic Modules

**四级证据 9/16。** 12.8 Automatic Modules：自动模块帮助迁移但名称和依赖推断可能不稳定，应作为过渡坐标而非最终合同。本节点以“exports”为观察焦点；运行`jdeps --print-module-deps app.jar`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 12.8 Automatic Modules 时，先预测它在 开放反射 阶段会怎样改变边界可解释度；只切换依赖声明或包暴露中的一项。若故障“为修复反射错误直接open整个模块，或依赖自动模块名却没有迁移记录”没有产生预期首错，就撤回当前解释并检查替代原因。

### 12.9 The Unnamed Module

**四级证据 10/16。** 12.9 The Unnamed Module：类路径代码属于未命名模块，可读所有命名模块，但不能被命名模块直接 requires。本节点以“opens”为观察焦点；运行`jdeps --print-module-deps app.jar`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 12.9 The Unnamed Module 时，先预测它在 链接运行时 阶段会怎样改变边界可解释度；只切换依赖声明或包暴露中的一项。若故障“为修复反射错误直接open整个模块，或依赖自动模块名却没有迁移记录”没有产生预期首错，就撤回当前解释并检查替代原因。

### 12.10 Command-Line Flags for Migration

**四级证据 11/16。** 12.10 Command-Line Flags for Migration：命令行链把编译器选项、类路径或模块路径与实际输出显式化，是复现 IDE 行为的基线。本节点以“ServiceLoader”为观察焦点；运行`jdeps --print-module-deps app.jar`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 12.10 Command-Line Flags for Migration 时，先预测它在 命名模块 阶段会怎样改变边界可解释度；只切换依赖声明或包暴露中的一项。若故障“为修复反射错误直接open整个模块，或依赖自动模块名却没有迁移记录”没有产生预期首错，就撤回当前解释并检查替代原因。

### 12.11 Transitive and Static Requirements

**四级证据 12/16。** 12.11 Transitive and Static Requirements：requires transitive 向使用者传播读取，requires static 表达编译期必需而运行期可选。本节点以“jdeps”为观察焦点；运行`jdeps --print-module-deps app.jar`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 12.11 Transitive and Static Requirements 时，先预测它在 声明依赖 阶段会怎样改变边界可解释度；只切换依赖声明或包暴露中的一项。若故障“为修复反射错误直接open整个模块，或依赖自动模块名却没有迁移记录”没有产生预期首错，就撤回当前解释并检查替代原因。

### 12.12 Importing Modules

**四级证据 13/16。** 12.12 Importing Modules：单模块导入影响源代码名称解析，不会自动改变运行时模块读取与包导出。本节点以“module-info”为观察焦点；运行`jdeps --print-module-deps app.jar`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 12.12 Importing Modules 时，先预测它在 导出API 阶段会怎样改变边界可解释度；只切换依赖声明或包暴露中的一项。若故障“为修复反射错误直接open整个模块，或依赖自动模块名却没有迁移记录”没有产生预期首错，就撤回当前解释并检查替代原因。

### 12.13 Qualified Exporting and Opening

**四级证据 14/16。** 12.13 Qualified Exporting and Opening：opens 控制深反射访问，可限定目标模块；为便利全开放会扩大封装和安全影响面。本节点以“requires”为观察焦点；运行`jdeps --print-module-deps app.jar`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 12.13 Qualified Exporting and Opening 时，先预测它在 开放反射 阶段会怎样改变边界可解释度；只切换依赖声明或包暴露中的一项。若故障“为修复反射错误直接open整个模块，或依赖自动模块名却没有迁移记录”没有产生预期首错，就撤回当前解释并检查替代原因。

### 12.14 Service Loading

**四级证据 15/16。** 12.14 Service Loading：uses 与 provides 声明服务消费和提供，使模块无需读取具体实现即可装配。本节点以“exports”为观察焦点；运行`jdeps --print-module-deps app.jar`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 12.14 Service Loading 时，先预测它在 链接运行时 阶段会怎样改变边界可解释度；只切换依赖声明或包暴露中的一项。若故障“为修复反射错误直接open整个模块，或依赖自动模块名却没有迁移记录”没有产生预期首错，就撤回当前解释并检查替代原因。

### 12.15 Tools for Working with Modules

**四级证据 16/16。** 12.15 Tools for Working with Modules：jdeps、jar、jlink 等工具分别检查依赖、工件和运行镜像，输出应纳入发布证据。本节点以“opens”为观察焦点；运行`jdeps --print-module-deps app.jar`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 12.15 Tools for Working with Modules 时，先预测它在 命名模块 阶段会怎样改变边界可解释度；只切换依赖声明或包暴露中的一项。若故障“为修复反射错误直接open整个模块，或依赖自动模块名却没有迁移记录”没有产生预期首错，就撤回当前解释并检查替代原因。

## Java 25 最小可执行切片

```java
module app.main {
  requires app.api;
  uses app.api.Plugin;
}
```

该切片围绕“jdeps --print-module-deps app.jar”保留本章最小机制，不承担完整生产实现。运行时固定 Java 25 发行版、操作系统、编译选项、模块或类路径和输入；保存标准输出、标准错误、退出码、资源状态与实际耗时。

```yaml
unit: jct-14e-v1-12-modules
axis_a: 依赖声明
axis_b: 包暴露
fault: 为修复反射错误直接open整个模块，或依赖自动模块名却没有迁移记录
invariant: 编译期可读性与运行时反射权限分别最小化声明
scenarios: [baseline, fault, recovery, reset]
```

## 三个必须主动触发的误区


## 练习、答案与目录节点验证


## 术语复核与本章回顾


完成卷I 第12章 Java 平台模块系统意味着能解释用 requires、exports、opens、服务装配与迁移工具建立可读性、可访问性和反射边界。，能运行并改动章专属切片，能主动制造“为修复反射错误直接open整个模块，或依赖自动模块名却没有迁移记录”，还能凭可重放证据完成恢复与交接。只会复述目录或展示一次成功输出，均未满足本章门禁。

