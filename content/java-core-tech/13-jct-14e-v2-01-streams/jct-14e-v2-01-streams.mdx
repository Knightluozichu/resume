---
title: 卷II 第1章 Stream
type: C
section: Java核心技术（第14版·全两卷 / Java 25）
order: 13
description: >-
  把惰性 Stream 管线拆成来源、中间操作、终止操作与归约合同，验证 Optional、Collector、Gatherer 和并行安全。
  覆盖14个正式节点，并通过Java 25代码、故障注入和复位证据验收。
demo: true
math: true
sourceUrl: >-
  https://www.informit.com/store/core-java-volume-ii-advanced-features-9780135558690
draft: false
qualityVersion: 2
practiceMode: calculation
sourceMode: independent-rewrite
---


{/* JCT_QUALITY_V2 */}

## 为什么从“Stream 管线求值追踪台”开始

卷II 第1章 Stream要解决的不是 API 名称记忆，而是把惰性 Stream 管线拆成来源、中间操作、终止操作与归约合同，验证 Optional、Collector、Gatherer 和并行安全。 直觉上，编译成功只排除了部分静态错误；运行一次成功也没有覆盖空值、极值、取消、并发、资源关闭、区域、协议与版本边界。

先预测：把执行方式从“顺序Stream”切换为“并行Stream”，管线等价率与副作用竞态风险会如何变化？随后只改这一项运行探针；结果不符时修改机制假设，不移动输入边界。

## 来源、版次与独立重写边界

InformIT/Oracle Press 的[卷 I 产品页](https://www.informit.com/store/core-java-volume-i-fundamentals-9780135558577)与[卷 II 产品页](https://www.informit.com/store/core-java-volume-ii-advanced-features-9780135558690)确认 Cay S. Horstmann《Core Java》第14版全两卷于2025年10月出版、面向 Java 25，并给出25章、214个正式章/节标题。本页所在卷的产品页同时提供本章合法试读；试读仅用于核对公开样章范围，仍不复制原书叙事。

本页技术事实由[Java SE 25 API](https://docs.oracle.com/en/java/javase/25/docs/api/index.html)、[Java SE 25 语言规范](https://docs.oracle.com/javase/specs/jls/se25/html/index.html)、[Java SE 25 JVM 规范](https://docs.oracle.com/javase/specs/jvms/se25/html/index.html)与[OpenJDK 25 项目页](https://openjdk.org/projects/jdk/25/)中的适用部分独立复核。中文解释、代码、图示、交互、参数、练习与答案均为独立教学重写；Oracle 文档许可证不被误报为原书授权。

## 本章机制与六个检查点

、、、、、。

卷II 第1章 Stream以“同一输入在声明的顺序合同下得到同一结果”为不变量。执行方式与归约合同是离散输入，管线等价率是主结果，副作用竞态风险必须单独报告；这些交互分数是课程中的透明反馈，不是原书或JDK性能基准。

## 官方目录逐项深读

### Chapter 1: Streams

**四级证据 1/14。** Chapter 1: Streams：Chapter 1: Streams必须放进创建来源—组合变换—触发终止—归约收集—验证并行这条本章机制链，而不能从目录词直接推断实现保证。本节点以“惰性求值”为观察焦点；运行`stream.peek(trace::add).toList()`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 Chapter 1: Streams 时，先预测它在 创建来源 阶段会怎样改变管线等价率；只切换执行方式或归约合同中的一项。若故障“在并行管线写共享可变容器，或对非结合运算使用reduce”没有产生预期首错，就撤回当前解释并检查替代原因。

### 1.1 From Iterating to Stream Operations

**四级证据 2/14。** 1.1 From Iterating to Stream Operations：Stream 把外部迭代改为声明式管线，惰性中间操作直到终止操作才真正消费来源。本节点以“filter/map”为观察焦点；运行`stream.peek(trace::add).toList()`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 1.1 From Iterating to Stream Operations 时，先预测它在 组合变换 阶段会怎样改变管线等价率；只切换执行方式或归约合同中的一项。若故障“在并行管线写共享可变容器，或对非结合运算使用reduce”没有产生预期首错，就撤回当前解释并检查替代原因。

### 1.2 Stream Creation

**四级证据 3/14。** 1.2 Stream Creation：流来源可能一次性、无限或有资源所有权；创建时要说明关闭和重复消费能力。本节点以“Optional”为观察焦点；运行`stream.peek(trace::add).toList()`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 1.2 Stream Creation 时，先预测它在 触发终止 阶段会怎样改变管线等价率；只切换执行方式或归约合同中的一项。若故障“在并行管线写共享可变容器，或对非结合运算使用reduce”没有产生预期首错，就撤回当前解释并检查替代原因。

### 1.3 The filter, map, and flatMap Methods

**四级证据 4/14。** 1.3 The filter, map, and flatMap Methods：filter 保留元素，map 一对一变换，flatMap 展平嵌套来源；函数应避免隐藏共享副作用。本节点以“Collector”为观察焦点；运行`stream.peek(trace::add).toList()`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 1.3 The filter, map, and flatMap Methods 时，先预测它在 归约收集 阶段会怎样改变管线等价率；只切换执行方式或归约合同中的一项。若故障“在并行管线写共享可变容器，或对非结合运算使用reduce”没有产生预期首错，就撤回当前解释并检查替代原因。

### 1.4 Extracting Substreams and Combining Streams

**四级证据 5/14。** 1.4 Extracting Substreams and Combining Streams：limit、skip、take/drop 与 concat 改变消费边界，短路行为要用计数轨迹验证。本节点以“Gatherer”为观察焦点；运行`stream.peek(trace::add).toList()`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 1.4 Extracting Substreams and Combining Streams 时，先预测它在 验证并行 阶段会怎样改变管线等价率；只切换执行方式或归约合同中的一项。若故障“在并行管线写共享可变容器，或对非结合运算使用reduce”没有产生预期首错，就撤回当前解释并检查替代原因。

### 1.5 Other Stream Transformations

**四级证据 6/14。** 1.5 Other Stream Transformations：distinct、sorted、peek 等操作可能有状态或顺序要求，不能仅凭链式语法推断成本。本节点以“并行归约”为观察焦点；运行`stream.peek(trace::add).toList()`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 1.5 Other Stream Transformations 时，先预测它在 创建来源 阶段会怎样改变管线等价率；只切换执行方式或归约合同中的一项。若故障“在并行管线写共享可变容器，或对非结合运算使用reduce”没有产生预期首错，就撤回当前解释并检查替代原因。

### 1.6 Simple Reductions

**四级证据 7/14。** 1.6 Simple Reductions：归约需要身份值、累加器与组合器满足合同，尤其要验证结合律和并行等价。本节点以“惰性求值”为观察焦点；运行`stream.peek(trace::add).toList()`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 1.6 Simple Reductions 时，先预测它在 组合变换 阶段会怎样改变管线等价率；只切换执行方式或归约合同中的一项。若故障“在并行管线写共享可变容器，或对非结合运算使用reduce”没有产生预期首错，就撤回当前解释并检查替代原因。

### 1.7 The Optional Type

**四级证据 8/14。** 1.7 The Optional Type：Optional 表达可能无结果的返回值，不能用 get 或把 Optional 字段化来逃避缺失分支。本节点以“filter/map”为观察焦点；运行`stream.peek(trace::add).toList()`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 1.7 The Optional Type 时，先预测它在 触发终止 阶段会怎样改变管线等价率；只切换执行方式或归约合同中的一项。若故障“在并行管线写共享可变容器，或对非结合运算使用reduce”没有产生预期首错，就撤回当前解释并检查替代原因。

### 1.8 Collecting Results

**四级证据 9/14。** 1.8 Collecting Results：Collector 明确 supplier、accumulator、combiner 与 finisher，并用特征声明并发和顺序假设。本节点以“Optional”为观察焦点；运行`stream.peek(trace::add).toList()`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 1.8 Collecting Results 时，先预测它在 归约收集 阶段会怎样改变管线等价率；只切换执行方式或归约合同中的一项。若故障“在并行管线写共享可变容器，或对非结合运算使用reduce”没有产生预期首错，就撤回当前解释并检查替代原因。

### 1.9 Collectors

**四级证据 10/14。** 1.9 Collectors：Collector 明确 supplier、accumulator、combiner 与 finisher，并用特征声明并发和顺序假设。本节点以“Collector”为观察焦点；运行`stream.peek(trace::add).toList()`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 1.9 Collectors 时，先预测它在 验证并行 阶段会怎样改变管线等价率；只切换执行方式或归约合同中的一项。若故障“在并行管线写共享可变容器，或对非结合运算使用reduce”没有产生预期首错，就撤回当前解释并检查替代原因。

### 1.10 Reduction Operations

**四级证据 11/14。** 1.10 Reduction Operations：归约需要身份值、累加器与组合器满足合同，尤其要验证结合律和并行等价。本节点以“Gatherer”为观察焦点；运行`stream.peek(trace::add).toList()`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 1.10 Reduction Operations 时，先预测它在 创建来源 阶段会怎样改变管线等价率；只切换执行方式或归约合同中的一项。若故障“在并行管线写共享可变容器，或对非结合运算使用reduce”没有产生预期首错，就撤回当前解释并检查替代原因。

### 1.11 Gatherers

**四级证据 12/14。** 1.11 Gatherers：Gatherer 提供可组合的自定义中间操作，初始化、集成、合并和完成阶段决定状态与并行边界。本节点以“并行归约”为观察焦点；运行`stream.peek(trace::add).toList()`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 1.11 Gatherers 时，先预测它在 组合变换 阶段会怎样改变管线等价率；只切换执行方式或归约合同中的一项。若故障“在并行管线写共享可变容器，或对非结合运算使用reduce”没有产生预期首错，就撤回当前解释并检查替代原因。

### 1.12 Primitive Type Streams

**四级证据 13/14。** 1.12 Primitive Type Streams：IntStream 等避免装箱并提供数值归约，转换回对象流时要注意语义和分配变化。本节点以“惰性求值”为观察焦点；运行`stream.peek(trace::add).toList()`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 1.12 Primitive Type Streams 时，先预测它在 触发终止 阶段会怎样改变管线等价率；只切换执行方式或归约合同中的一项。若故障“在并行管线写共享可变容器，或对非结合运算使用reduce”没有产生预期首错，就撤回当前解释并检查替代原因。

### 1.13 Parallel Streams

**四级证据 14/14。** 1.13 Parallel Streams：并行流使用拆分和公共执行资源；只有无干扰函数、合适规模与结合归约才可能安全获益。本节点以“filter/map”为观察焦点；运行`stream.peek(trace::add).toList()`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 1.13 Parallel Streams 时，先预测它在 归约收集 阶段会怎样改变管线等价率；只切换执行方式或归约合同中的一项。若故障“在并行管线写共享可变容器，或对非结合运算使用reduce”没有产生预期首错，就撤回当前解释并检查替代原因。

## Java 25 最小可执行切片

```java
var result = words.stream()
    .filter(w -> !w.isBlank())
    .map(String::toLowerCase)
    .toList();
```

该切片围绕“stream.peek(trace::add).toList()”保留本章最小机制，不承担完整生产实现。运行时固定 Java 25 发行版、操作系统、编译选项、模块或类路径和输入；保存标准输出、标准错误、退出码、资源状态与实际耗时。

```yaml
unit: jct-14e-v2-01-streams
axis_a: 执行方式
axis_b: 归约合同
fault: 在并行管线写共享可变容器，或对非结合运算使用reduce
invariant: 同一输入在声明的顺序合同下得到同一结果
scenarios: [baseline, fault, recovery, reset]
```

## 三个必须主动触发的误区


## 练习、答案与目录节点验证


## 术语复核与本章回顾


完成卷II 第1章 Stream意味着能解释把惰性 Stream 管线拆成来源、中间操作、终止操作与归约合同，验证 Optional、Collector、Gatherer 和并行安全。，能运行并改动章专属切片，能主动制造“在并行管线写共享可变容器，或对非结合运算使用reduce”，还能凭可重放证据完成恢复与交接。只会复述目录或展示一次成功输出，均未满足本章门禁。

