---
title: 卷II 第11章 Swing 用户界面组件
type: C
section: Java核心技术（第14版·全两卷 / Java 25）
order: 23
description: >-
  用 MVC、布局约束、输入与选择模型、菜单和对话框构建可缩放、可键盘操作的 Swing 界面。 覆盖9个正式节点，并通过Java
  25代码、故障注入和复位证据验收。
demo: true
math: true
sourceUrl: >-
  https://www.informit.com/store/core-java-volume-ii-advanced-features-9780135558690
draft: false
qualityVersion: 2
practiceMode: design
sourceMode: independent-rewrite
---


{/* JCT_QUALITY_V2 */}

## 为什么从“Swing 组件模型与布局台”开始

卷II 第11章 Swing 用户界面组件要解决的不是 API 名称记忆，而是用 MVC、布局约束、输入与选择模型、菜单和对话框构建可缩放、可键盘操作的 Swing 界面。 直觉上，编译成功只排除了部分静态错误；运行一次成功也没有覆盖空值、极值、取消、并发、资源关闭、区域、协议与版本边界。

先预测：把布局策略从“标准Layout”切换为“自定义约束”，布局适应率与状态耦合风险会如何变化？随后只改这一项运行探针；结果不符时修改机制假设，不移动输入边界。

## 来源、版次与独立重写边界

InformIT/Oracle Press 的[卷 I 产品页](https://www.informit.com/store/core-java-volume-i-fundamentals-9780135558577)与[卷 II 产品页](https://www.informit.com/store/core-java-volume-ii-advanced-features-9780135558690)确认 Cay S. Horstmann《Core Java》第14版全两卷于2025年10月出版、面向 Java 25，并给出25章、214个正式章/节标题。未取得本章原书正文；产品页只限定标题和小节范围，目录核对不被表述为正文忠实。

本页技术事实由[Java SE 25 API](https://docs.oracle.com/en/java/javase/25/docs/api/index.html)、[Java SE 25 语言规范](https://docs.oracle.com/javase/specs/jls/se25/html/index.html)、[Java SE 25 JVM 规范](https://docs.oracle.com/javase/specs/jvms/se25/html/index.html)与[OpenJDK 25 项目页](https://openjdk.org/projects/jdk/25/)中的适用部分独立复核。中文解释、代码、图示、交互、参数、练习与答案均为独立教学重写；Oracle 文档许可证不被误报为原书授权。

## 本章机制与六个检查点

、、、、、。

卷II 第11章 Swing 用户界面组件以“内容变化与窗口缩放不破坏操作顺序和模型值”为不变量。布局策略与状态位置是离散输入，布局适应率是主结果，状态耦合风险必须单独报告；这些交互分数是课程中的透明反馈，不是原书或JDK性能基准。

## 官方目录逐项深读

### Chapter 11: User Interface Components with Swing

**四级证据 1/9。** Chapter 11: User Interface Components with Swing：Chapter 11: User Interface Components with Swing必须放进定义模型—选择布局—绑定输入—响应选择—弹出对话这条本章机制链，而不能从目录词直接推断实现保证。本节点以“MVC”为观察焦点；运行`component.getPreferredSize()`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 Chapter 11: User Interface Components with Swing 时，先预测它在 定义模型 阶段会怎样改变布局适应率；只切换布局策略或状态位置中的一项。若故障“使用绝对坐标，在字体缩放或翻译变长后裁切控件，并把视图当唯一业务状态”没有产生预期首错，就撤回当前解释并检查替代原因。

### 11.1 Swing and the Model-View-Controller Design Pattern

**四级证据 2/9。** 11.1 Swing and the Model-View-Controller Design Pattern：Swing组件常把模型、UI委托和控制事件拆开，业务状态不应只存在于可视控件。本节点以“LayoutManager”为观察焦点；运行`component.getPreferredSize()`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 11.1 Swing and the Model-View-Controller Design Pattern 时，先预测它在 选择布局 阶段会怎样改变布局适应率；只切换布局策略或状态位置中的一项。若故障“使用绝对坐标，在字体缩放或翻译变长后裁切控件，并把视图当唯一业务状态”没有产生预期首错，就撤回当前解释并检查替代原因。

### 11.2 Introduction to Layout Management

**四级证据 3/9。** 11.2 Introduction to Layout Management：布局管理器根据首选、最小尺寸和容器约束计算位置，比绝对坐标更能适应字体与Locale。本节点以“Document”为观察焦点；运行`component.getPreferredSize()`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 11.2 Introduction to Layout Management 时，先预测它在 绑定输入 阶段会怎样改变布局适应率；只切换布局策略或状态位置中的一项。若故障“使用绝对坐标，在字体缩放或翻译变长后裁切控件，并把视图当唯一业务状态”没有产生预期首错，就撤回当前解释并检查替代原因。

### 11.3 Text Input

**四级证据 4/9。** 11.3 Text Input：文本组件的Document承载内容模型，验证、文档事件和线程边界应围绕模型设计。本节点以“ButtonModel”为观察焦点；运行`component.getPreferredSize()`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 11.3 Text Input 时，先预测它在 响应选择 阶段会怎样改变布局适应率；只切换布局策略或状态位置中的一项。若故障“使用绝对坐标，在字体缩放或翻译变长后裁切控件，并把视图当唯一业务状态”没有产生预期首错，就撤回当前解释并检查替代原因。

### 11.4 Choice Components

**四级证据 5/9。** 11.4 Choice Components：选择组件把可选项模型与当前选择分离，键盘、空选择和动态更新都要验证。本节点以“Action”为观察焦点；运行`component.getPreferredSize()`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 11.4 Choice Components 时，先预测它在 弹出对话 阶段会怎样改变布局适应率；只切换布局策略或状态位置中的一项。若故障“使用绝对坐标，在字体缩放或翻译变长后裁切控件，并把视图当唯一业务状态”没有产生预期首错，就撤回当前解释并检查替代原因。

### 11.5 Menus

**四级证据 6/9。** 11.5 Menus：Action 可让菜单、按钮和快捷键共享命令状态，启用条件应来自模型而非复制布尔值。本节点以“Dialog”为观察焦点；运行`component.getPreferredSize()`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 11.5 Menus 时，先预测它在 定义模型 阶段会怎样改变布局适应率；只切换布局策略或状态位置中的一项。若故障“使用绝对坐标，在字体缩放或翻译变长后裁切控件，并把视图当唯一业务状态”没有产生预期首错，就撤回当前解释并检查替代原因。

### 11.6 The Grid Bag Layout

**四级证据 7/9。** 11.6 The Grid Bag Layout：GridBagLayout 用网格位置、权重、填充和边距表达伸缩约束，错误权重会制造大片空白或裁切。本节点以“MVC”为观察焦点；运行`component.getPreferredSize()`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 11.6 The Grid Bag Layout 时，先预测它在 选择布局 阶段会怎样改变布局适应率；只切换布局策略或状态位置中的一项。若故障“使用绝对坐标，在字体缩放或翻译变长后裁切控件，并把视图当唯一业务状态”没有产生预期首错，就撤回当前解释并检查替代原因。

### 11.7 Custom Layout Managers

**四级证据 8/9。** 11.7 Custom Layout Managers：自定义布局要实现尺寸计算和容器布局，并覆盖缩放、方向和不可见组件。本节点以“LayoutManager”为观察焦点；运行`component.getPreferredSize()`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 11.7 Custom Layout Managers 时，先预测它在 绑定输入 阶段会怎样改变布局适应率；只切换布局策略或状态位置中的一项。若故障“使用绝对坐标，在字体缩放或翻译变长后裁切控件，并把视图当唯一业务状态”没有产生预期首错，就撤回当前解释并检查替代原因。

### 11.8 Dialog Boxes

**四级证据 9/9。** 11.8 Dialog Boxes：对话框有模态、所有者和结果生命周期；取消与关闭应产生明确业务结果。本节点以“Document”为观察焦点；运行`component.getPreferredSize()`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 11.8 Dialog Boxes 时，先预测它在 响应选择 阶段会怎样改变布局适应率；只切换布局策略或状态位置中的一项。若故障“使用绝对坐标，在字体缩放或翻译变长后裁切控件，并把视图当唯一业务状态”没有产生预期首错，就撤回当前解释并检查替代原因。

## Java 25 最小可执行切片

```java
panel.setLayout(new java.awt.GridBagLayout());
var constraints = new java.awt.GridBagConstraints();
constraints.fill = java.awt.GridBagConstraints.HORIZONTAL;
```

该切片围绕“component.getPreferredSize()”保留本章最小机制，不承担完整生产实现。运行时固定 Java 25 发行版、操作系统、编译选项、模块或类路径和输入；保存标准输出、标准错误、退出码、资源状态与实际耗时。

```yaml
unit: jct-14e-v2-11-swing-components
axis_a: 布局策略
axis_b: 状态位置
fault: 使用绝对坐标，在字体缩放或翻译变长后裁切控件，并把视图当唯一业务状态
invariant: 内容变化与窗口缩放不破坏操作顺序和模型值
scenarios: [baseline, fault, recovery, reset]
```

## 三个必须主动触发的误区


## 练习、答案与目录节点验证


## 术语复核与本章回顾


完成卷II 第11章 Swing 用户界面组件意味着能解释用 MVC、布局约束、输入与选择模型、菜单和对话框构建可缩放、可键盘操作的 Swing 界面。，能运行并改动章专属切片，能主动制造“使用绝对坐标，在字体缩放或翻译变长后裁切控件，并把视图当唯一业务状态”，还能凭可重放证据完成恢复与交接。只会复述目录或展示一次成功输出，均未满足本章门禁。

