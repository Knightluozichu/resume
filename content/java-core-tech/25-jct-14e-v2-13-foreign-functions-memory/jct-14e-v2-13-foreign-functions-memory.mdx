---
title: 卷II 第13章 外部函数与内存 API
type: C
section: Java核心技术（第14版·全两卷 / Java 25）
order: 25
description: >-
  比较 JNI 与 FFM，显式管理 Linker、Arena、MemorySegment、MemoryLayout、下调句柄和回调生命周期。
  覆盖9个正式节点，并通过Java 25代码、故障注入和复位证据验收。
demo: true
math: true
sourceUrl: >-
  https://www.informit.com/store/core-java-volume-ii-advanced-features-9780135558690
draft: false
qualityVersion: 2
practiceMode: diagnosis
sourceMode: independent-rewrite
---


{/* JCT_QUALITY_V2 */}

## 为什么从“FFM ABI 与生命周期台”开始

卷II 第13章 外部函数与内存 API要解决的不是 API 名称记忆，而是比较 JNI 与 FFM，显式管理 Linker、Arena、MemorySegment、MemoryLayout、下调句柄和回调生命周期。 直觉上，编译成功只排除了部分静态错误；运行一次成功也没有覆盖空值、极值、取消、并发、资源关闭、区域、协议与版本边界。

先预测：把互操作方式从“FFM downcall”切换为“FFM upcall”，ABI匹配率与越界与悬垂风险会如何变化？随后只改这一项运行探针；结果不符时修改机制假设，不移动输入边界。

## 来源、版次与独立重写边界

InformIT/Oracle Press 的[卷 I 产品页](https://www.informit.com/store/core-java-volume-i-fundamentals-9780135558577)与[卷 II 产品页](https://www.informit.com/store/core-java-volume-ii-advanced-features-9780135558690)确认 Cay S. Horstmann《Core Java》第14版全两卷于2025年10月出版、面向 Java 25，并给出25章、214个正式章/节标题。未取得本章原书正文；产品页只限定标题和小节范围，目录核对不被表述为正文忠实。

本页技术事实由[Java SE 25 API](https://docs.oracle.com/en/java/javase/25/docs/api/index.html)、[Java SE 25 语言规范](https://docs.oracle.com/javase/specs/jls/se25/html/index.html)、[Java SE 25 JVM 规范](https://docs.oracle.com/javase/specs/jvms/se25/html/index.html)与[OpenJDK 25 项目页](https://openjdk.org/projects/jdk/25/)中的适用部分独立复核。中文解释、代码、图示、交互、参数、练习与答案均为独立教学重写；Oracle 文档许可证不被误报为原书授权。

## 本章机制与六个检查点

、、、、、。

卷II 第13章 外部函数与内存 API以“每个外部地址的布局、线程可达性和寿命覆盖完整调用窗口”为不变量。互操作方式与Arena寿命是离散输入，ABI匹配率是主结果，越界与悬垂风险必须单独报告；这些交互分数是课程中的透明反馈，不是原书或JDK性能基准。

## 官方目录逐项深读

### Chapter 13: The Foreign Functions and Memory API

**四级证据 1/9。** Chapter 13: The Foreign Functions and Memory API：Chapter 13: The Foreign Functions and Memory API必须放进确认ABI—建立布局—分配Arena—调用外部函数—关闭与回调这条本章机制链，而不能从目录词直接推断实现保证。本节点以“JNI”为观察焦点；运行`segment.scope().isAlive()`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 Chapter 13: The Foreign Functions and Memory API 时，先预测它在 确认ABI 阶段会怎样改变ABI匹配率；只切换互操作方式或Arena寿命中的一项。若故障“MemorySegment逃逸已关闭Arena，或按错误字节序和布局调用本地函数”没有产生预期首错，就撤回当前解释并检查替代原因。

### 13.1 Using JNI to Call C Code from a Java Program

**四级证据 2/9。** 13.1 Using JNI to Call C Code from a Java Program：JNI 通过本地桥接调用C代码，签名、线程附着、引用和异常处理均有额外脆弱边界。本节点以“Linker”为观察焦点；运行`segment.scope().isAlive()`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 13.1 Using JNI to Call C Code from a Java Program 时，先预测它在 建立布局 阶段会怎样改变ABI匹配率；只切换互操作方式或Arena寿命中的一项。若故障“MemorySegment逃逸已关闭Arena，或按错误字节序和布局调用本地函数”没有产生预期首错，就撤回当前解释并检查替代原因。

### 13.2 Using FFM to Call a Foreign Function

**四级证据 3/9。** 13.2 Using FFM to Call a Foreign Function：FFM 用Linker、函数描述符和MethodHandle表达外部调用，使ABI合同在Java侧可检查。本节点以“Arena”为观察焦点；运行`segment.scope().isAlive()`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 13.2 Using FFM to Call a Foreign Function 时，先预测它在 分配Arena 阶段会怎样改变ABI匹配率；只切换互操作方式或Arena寿命中的一项。若故障“MemorySegment逃逸已关闭Arena，或按错误字节序和布局调用本地函数”没有产生预期首错，就撤回当前解释并检查替代原因。

### 13.3 Arenas

**四级证据 4/9。** 13.3 Arenas：Arena 管理一组内存段寿命；confined、shared与global策略改变关闭和线程访问规则。本节点以“MemorySegment”为观察焦点；运行`segment.scope().isAlive()`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 13.3 Arenas 时，先预测它在 调用外部函数 阶段会怎样改变ABI匹配率；只切换互操作方式或Arena寿命中的一项。若故障“MemorySegment逃逸已关闭Arena，或按错误字节序和布局调用本地函数”没有产生预期首错，就撤回当前解释并检查替代原因。

### 13.4 Memory Segments

**四级证据 5/9。** 13.4 Memory Segments：MemorySegment 提供有界内存视图并关联存活作用域，关闭后访问会在Java边界失败。本节点以“MemoryLayout”为观察焦点；运行`segment.scope().isAlive()`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 13.4 Memory Segments 时，先预测它在 关闭与回调 阶段会怎样改变ABI匹配率；只切换互操作方式或Arena寿命中的一项。若故障“MemorySegment逃逸已关闭Arena，或按错误字节序和布局调用本地函数”没有产生预期首错，就撤回当前解释并检查替代原因。

### 13.5 Memory Layout

**四级证据 6/9。** 13.5 Memory Layout：MemoryLayout 描述字段大小、对齐、顺序与路径，必须与目标ABI真实布局一致。本节点以“upcall”为观察焦点；运行`segment.scope().isAlive()`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 13.5 Memory Layout 时，先预测它在 确认ABI 阶段会怎样改变ABI匹配率；只切换互操作方式或Arena寿命中的一项。若故障“MemorySegment逃逸已关闭Arena，或按错误字节序和布局调用本地函数”没有产生预期首错，就撤回当前解释并检查替代原因。

### 13.6 Looking Up and Invoking Foreign Functions

**四级证据 7/9。** 13.6 Looking Up and Invoking Foreign Functions：SymbolLookup 找地址，Linker 根据函数描述符创建下调句柄；库身份和调用约定要冻结。本节点以“JNI”为观察焦点；运行`segment.scope().isAlive()`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 13.6 Looking Up and Invoking Foreign Functions 时，先预测它在 建立布局 阶段会怎样改变ABI匹配率；只切换互操作方式或Arena寿命中的一项。若故障“MemorySegment逃逸已关闭Arena，或按错误字节序和布局调用本地函数”没有产生预期首错，就撤回当前解释并检查替代原因。

### 13.7 Callbacks

**四级证据 8/9。** 13.7 Callbacks：upcall stub 让本地代码回调Java，目标方法句柄、线程与Arena寿命必须覆盖回调窗口。本节点以“Linker”为观察焦点；运行`segment.scope().isAlive()`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 13.7 Callbacks 时，先预测它在 分配Arena 阶段会怎样改变ABI匹配率；只切换互操作方式或Arena寿命中的一项。若故障“MemorySegment逃逸已关闭Arena，或按错误字节序和布局调用本地函数”没有产生预期首错，就撤回当前解释并检查替代原因。

### 13.8 Advanced Topics

**四级证据 9/9。** 13.8 Advanced Topics：高级互操作要补充可变参数、结构返回、平台ABI和错误码等边界，并在目标平台逐项验证。本节点以“Arena”为观察焦点；运行`segment.scope().isAlive()`或等价最小探针后，保存输入、诊断、实际结果和不适用边界。

验证 13.8 Advanced Topics 时，先预测它在 调用外部函数 阶段会怎样改变ABI匹配率；只切换互操作方式或Arena寿命中的一项。若故障“MemorySegment逃逸已关闭Arena，或按错误字节序和布局调用本地函数”没有产生预期首错，就撤回当前解释并检查替代原因。

## Java 25 最小可执行切片

```java
try (var arena = java.lang.foreign.Arena.ofConfined()) {
  var segment = arena.allocate(java.lang.foreign.ValueLayout.JAVA_INT);
  segment.set(java.lang.foreign.ValueLayout.JAVA_INT, 0, 42);
}
```

该切片围绕“segment.scope().isAlive()”保留本章最小机制，不承担完整生产实现。运行时固定 Java 25 发行版、操作系统、编译选项、模块或类路径和输入；保存标准输出、标准错误、退出码、资源状态与实际耗时。

```yaml
unit: jct-14e-v2-13-foreign-functions-memory
axis_a: 互操作方式
axis_b: Arena寿命
fault: MemorySegment逃逸已关闭Arena，或按错误字节序和布局调用本地函数
invariant: 每个外部地址的布局、线程可达性和寿命覆盖完整调用窗口
scenarios: [baseline, fault, recovery, reset]
```

## 三个必须主动触发的误区


## 练习、答案与目录节点验证


## 术语复核与本章回顾


完成卷II 第13章 外部函数与内存 API意味着能解释比较 JNI 与 FFM，显式管理 Linker、Arena、MemorySegment、MemoryLayout、下调句柄和回调生命周期。，能运行并改动章专属切片，能主动制造“MemorySegment逃逸已关闭Arena，或按错误字节序和布局调用本地函数”，还能凭可重放证据完成恢复与交接。只会复述目录或展示一次成功输出，均未满足本章门禁。

