---
title: "第 7 章 数组"
type: A
section: "JavaScript 权威指南 · 数据与代码组织"
order: 7
description: "数组是带长度语义的有序对象；稀疏性、迭代方式、原地修改与返回新数组的差异，决定数据管道是否稳定。"
demo: true
math: false
sourceUrl: "https://www.oreilly.com/library/view/javascript-the-definitive/9781491952016/ch07.html"
draft: false
---

import {
  Jdg07ArraysMapLab,
  Jdg07ArraysExperimentLab,
  Jdg07ArraysEvidenceLab,
} from "@/components/mdx/javascript-definitive-guide/diagrams/jdg-07-arrays";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第 7 章 数组的核心主张，并把它放回第 7 版 17 章体系
- 能按出版社目录逐项说明 25 个标题的输入、状态、输出与边界
- 能运行本章最小样例，定位值、控制流、对象、任务或宿主环境的首个偏离点
- 能完成正常、边界、失败和恢复四类样本，证明没有旧任务或资源残留

</Objectives>

## 本章定位

<Term def="数组是带长度语义的有序对象；稀疏性、迭代方式、原地修改与返回新数组的差异，决定数据管道是否稳定。">数组索引</Term> 是本章的入口观察量。数组是带长度语义的有序对象；稀疏性、迭代方式、原地修改与返回新数组的差异，决定数据管道是否稳定。 这意味着阅读不能停在 API 名称或最终打印值，而要回答：输入何时进入、值或对象怎样变化、控制权在哪里转移、错误如何传播、宿主资源由谁清理。

<Jdg07ArraysMapLab />

## 六阶段机制链

### 1. 创建稠密或稀疏数组

固定源码、运行环境和输入，在动作发生前记录可见绑定、值类型、对象身份、待处理任务和已打开资源。若起点不稳定，后续比较没有意义。

### 2. 按索引读写元素

只执行本阶段最小动作，记录它读取了什么、写入了什么，以及控制权交给语言运行时还是浏览器、Node 或工具链。

### 3. 维护 length 与空槽

检查抽象规则是否被真正满足。不要用最终输出反推过程；对象引用、隐式转换、短路与异步调度都可能产生表面相同的结果。

### 4. 选择迭代策略

在边界值、重复输入、迟到结果和错误宿主状态下重放。保存第一个与正常轨迹不同的位置，而不是只保存最后一条异常。

### 5. 执行变换、搜索或排序

让失败沿规定通道传播，并核对调用者是否能区分可重试、应停止和必须降级的情况。静默吞错不能作为恢复策略。

### 6. 检查原数组是否被修改

修复单一故障后重放完全相同的输入。只有绑定、值、任务、句柄和生成产物都回到可预测状态，本章才可以签发。

## 出版社目录逐项讲解

### Chapter 7: Arrays

**Chapter 7: Arrays** 对应本章“创建稠密或稀疏数组”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 7.1 Creating Arrays

**7.1 Creating Arrays** 对应本章“按索引读写元素”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 7.1.1 Array Literals

**7.1.1 Array Literals** 对应本章“维护 length 与空槽”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 7.1.2 The Spread Operator

**7.1.2 The Spread Operator** 对应本章“选择迭代策略”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 7.1.3 The Array() Constructor

**7.1.3 The Array() Constructor** 对应本章“执行变换、搜索或排序”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 7.1.4 Array.of()

**7.1.4 Array.of()** 对应本章“检查原数组是否被修改”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 7.1.5 Array.from()

**7.1.5 Array.from()** 对应本章“创建稠密或稀疏数组”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 7.2 Reading and Writing Array Elements

**7.2 Reading and Writing Array Elements** 对应本章“按索引读写元素”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 7.3 Sparse Arrays

**7.3 Sparse Arrays** 对应本章“维护 length 与空槽”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 7.4 Array Length

**7.4 Array Length** 对应本章“选择迭代策略”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 7.5 Adding and Deleting Array Elements

**7.5 Adding and Deleting Array Elements** 对应本章“执行变换、搜索或排序”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 7.6 Iterating Arrays

**7.6 Iterating Arrays** 对应本章“检查原数组是否被修改”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 7.7 Multidimensional Arrays

**7.7 Multidimensional Arrays** 对应本章“创建稠密或稀疏数组”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 7.8 Array Methods

**7.8 Array Methods** 对应本章“按索引读写元素”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 7.8.1 Array Iterator Methods

**7.8.1 Array Iterator Methods** 对应本章“维护 length 与空槽”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 7.8.2 Flattening arrays with flat() and flatMap()

**7.8.2 Flattening arrays with flat() and flatMap()** 对应本章“选择迭代策略”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 7.8.3 Adding arrays with concat()

**7.8.3 Adding arrays with concat()** 对应本章“执行变换、搜索或排序”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 7.8.4 Stacks and Queues with push(), pop(), shift(), and unshift()

**7.8.4 Stacks and Queues with push(), pop(), shift(), and unshift()** 对应本章“检查原数组是否被修改”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 7.8.5 Subarrays with slice(), splice(), fill(), and copyWithin()

**7.8.5 Subarrays with slice(), splice(), fill(), and copyWithin()** 对应本章“创建稠密或稀疏数组”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 7.8.6 Array Searching and Sorting Methods

**7.8.6 Array Searching and Sorting Methods** 对应本章“按索引读写元素”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 7.8.7 Array to String Conversions

**7.8.7 Array to String Conversions** 对应本章“维护 length 与空槽”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 7.8.8 Static Array Functions

**7.8.8 Static Array Functions** 对应本章“选择迭代策略”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 7.9 Array-Like Objects

**7.9 Array-Like Objects** 对应本章“执行变换、搜索或排序”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 7.10 Strings as Arrays

**7.10 Strings as Arrays** 对应本章“检查原数组是否被修改”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 7.11 Summary

**7.11 Summary** 对应本章“创建稠密或稀疏数组”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

## 机制实验

<Stepper>
  <Step title="1. 创建稠密或稀疏数组与按索引读写元素">
    固定输入和初始状态，逐行记录前两段机制证据。
    <Jdg07ArraysMapLab />
  </Step>
  <Step title="2. 维护 length 与空槽与选择迭代策略">
    注入一个边界或失败条件，在首个无法满足契约的位置停止。
    <Jdg07ArraysExperimentLab />
  </Step>
  <Step title="3. 执行变换、搜索或排序与检查原数组是否被修改">
    清除故障后重放相同输入，核对所有状态和资源已经收敛。
    <Jdg07ArraysEvidenceLab />
  </Step>
</Stepper>

## 最小可运行实验

```javascript
const source = [3, 1, 2];
const result = source.filter((n) => n > 1).map((n) => n * 10).toSorted();
console.log(source, result);
```

```yaml
book: JavaScript 权威指南（原书第 7 版）
page: jdg-07-arrays
official_entries: 25
sample: normal | boundary | failure | recovery
first_divergence: null
values_explained: true
ordering_explained: true
cleanup_complete: true
```

```text
freeze source + runtime + input
predict values + references + control flow + tasks
inject one controlled fault
stop at first divergence
recover and replay the same sample
assert stable state and no stale work
```

## 常见误区与故障注入

<Callout type="trap">
  **误区一：记住“第 7 章 数组”的结论，却不展开从“创建稠密或稀疏数组”到“检查原数组是否被修改”的过程。** 相同输出可能来自不同引用、隐式转换、迟到任务或宿主缓存，必须保存中间证据。
</Callout>

<Callout type="trap">
  **误区二：把 2020 年第 7 版的历史工具与 API 当成永远不变的当前推荐。** 目录忠实度和现代工程状态是两条证据；前者锁定原书，后者应通过当前规范和工具文档单独校验。
</Callout>

## 四类样本与验收

| 样本 | 注入方式 | 必查证据 | 通过条件 |
|---|---|---|---|
| 正常 | 规范输入与单次执行 | 值、对象、调用点、任务、资源 | 六阶段按预期完成 |
| 边界 | 空值、极值、重复输入或慢 I/O | 类型边界与停止条件 | 不发生隐式越界 |
| 失败 | 错误参数、拒绝、断流或工具失败 | 首个偏离点 | 明确停止、传播或降级 |
| 恢复 | 修复后重放完全相同输入 | 清理后的状态与任务数 | 无旧值、旧任务和旧资源 |

<Jdg07ArraysEvidenceLab />

本页签发不变量是：**数组是带长度语义的有序对象；稀疏性、迭代方式、原地修改与返回新数组的差异，决定数据管道是否稳定。** 单次输出、截图或一次测试通过不能替代值语义、控制流、对象身份、异步顺序和宿主资源的完整轨迹。

## 练习

<Exercises>

**问题 1** 为什么不能只以最终输出正确作为第 7 章 数组的验收？

<Answer>
最终输出会隐藏隐式转换、引用别名、短路路径、异步调度和宿主资源状态。必须比较输入、执行步骤、首个偏离点与恢复后的清理结果。
</Answer>

**问题 2** 如何把本页出版社目录变成可执行学习证据？

<Answer>
按公开顺序登记“Chapter 7: Arrays、7.1 Creating Arrays、7.1.1 Array Literals、7.1.2 The Spread Operator、7.1.3 The Array() Constructor、7.1.4 Array.of()、7.1.5 Array.from()、7.2 Reading and Writing Array Elements、7.3 Sparse Arrays、7.4 Array Length、7.5 Adding and Deleting Array Elements、7.6 Iterating Arrays、7.7 Multidimensional Arrays、7.8 Array Methods、7.8.1 Array Iterator Methods、7.8.2 Flattening arrays with flat() and flatMap()、7.8.3 Adding arrays with concat()、7.8.4 Stacks and Queues with push(), pop(), shift(), and unshift()、7.8.5 Subarrays with slice(), splice(), fill(), and copyWithin()、7.8.6 Array Searching and Sorting Methods、7.8.7 Array to String Conversions、7.8.8 Static Array Functions、7.9 Array-Like Objects、7.10 Strings as Arrays、7.11 Summary”，为每项写出输入、状态、输出、失败与恢复条件，再在同一环境重放正常样本和反例。
</Answer>

**问题 3** 本页的最小通过条件是什么？

<Answer>
数组是带长度语义的有序对象；稀疏性、迭代方式、原地修改与返回新数组的差异，决定数据管道是否稳定。 同时，六阶段必须能被同一组输入重放，所有旧值、旧任务、旧句柄和旧产物在恢复后都已清除。
</Answer>

</Exercises>

## 小结

- **数组索引**：对应“创建稠密或稀疏数组”阶段的核心观察量。
- **稀疏数组**：对应“按索引读写元素”阶段的核心观察量。
- **迭代器方法**：对应“维护 length 与空槽”阶段的核心观察量。
- **类数组对象**：对应“选择迭代策略”阶段的核心观察量。
- **原地修改**：对应“执行变换、搜索或排序”阶段的核心观察量。
- 25 个出版社目录条目已经全部映射到本章证据链。
- 正常、边界、失败和恢复样本共同决定是否通过，最终输出不是唯一证据。

## 术语表

<Glossary>
  <GlossaryItem term="数组索引">
    数组索引是本章第 1 个核心观察量，位于“创建稠密或稀疏数组”阶段。判断它是否正确，必须同时记录输入、状态、输出、失败路径与清理责任。
  </GlossaryItem>
  <GlossaryItem term="稀疏数组">
    稀疏数组是本章第 2 个核心观察量，位于“按索引读写元素”阶段。判断它是否正确，必须同时记录输入、状态、输出、失败路径与清理责任。
  </GlossaryItem>
  <GlossaryItem term="迭代器方法">
    迭代器方法是本章第 3 个核心观察量，位于“维护 length 与空槽”阶段。判断它是否正确，必须同时记录输入、状态、输出、失败路径与清理责任。
  </GlossaryItem>
  <GlossaryItem term="类数组对象">
    类数组对象是本章第 4 个核心观察量，位于“选择迭代策略”阶段。判断它是否正确，必须同时记录输入、状态、输出、失败路径与清理责任。
  </GlossaryItem>
  <GlossaryItem term="原地修改">
    原地修改是本章第 5 个核心观察量，位于“执行变换、搜索或排序”阶段。判断它是否正确，必须同时记录输入、状态、输出、失败路径与清理责任。
  </GlossaryItem>
</Glossary>

## 版本与改编边界

本单元以 David Flanagan 的英文第 7 版和李松峰译中文第 7 版为身份基线。目录来自 O’Reilly 出版社公开页面，正文是独立教学重构，不复制原书段落；工具版本、浏览器能力和 Node API 的现状需另行按当前官方文档核验。

<Attribution
  adaptedFrom="David Flanagan 著，JavaScript: The Definitive Guide, Seventh Edition；李松峰译，《JavaScript 权威指南（原书第 7 版）》，机械工业出版社，2021，ISBN 9787111677222。本页按出版社公开目录独立教学重构。"
  adaptedUrl="https://www.oreilly.com/library/view/javascript-the-definitive/9781491952016/ch07.html"
/>
