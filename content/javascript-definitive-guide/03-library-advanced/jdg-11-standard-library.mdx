---
title: "第 11 章 JavaScript 标准库"
type: A
section: "JavaScript 权威指南 · 标准库与高级机制"
order: 11
description: "标准库为集合、二进制数据、正则、日期、错误、JSON、国际化、控制台、URL 和定时器提供可组合能力；选型要同时检查数据模型、编码和环境边界。"
demo: true
math: false
sourceUrl: "https://www.oreilly.com/library/view/javascript-the-definitive/9781491952016/ch11.html"
draft: false
---

import {
  Jdg11StandardLibraryMapLab,
  Jdg11StandardLibraryExperimentLab,
  Jdg11StandardLibraryEvidenceLab,
} from "@/components/mdx/javascript-definitive-guide/diagrams/jdg-11-standard-library";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第 11 章 JavaScript 标准库的核心主张，并把它放回第 7 版 17 章体系
- 能按出版社目录逐项说明 32 个标题的输入、状态、输出与边界
- 能运行本章最小样例，定位值、控制流、对象、任务或宿主环境的首个偏离点
- 能完成正常、边界、失败和恢复四类样本，证明没有旧任务或资源残留

</Objectives>

## 本章定位

<Term def="标准库为集合、二进制数据、正则、日期、错误、JSON、国际化、控制台、URL 和定时器提供可组合能力；选型要同时检查数据模型、编码和环境边界。">Map与Set</Term> 是本章的入口观察量。标准库为集合、二进制数据、正则、日期、错误、JSON、国际化、控制台、URL 和定时器提供可组合能力；选型要同时检查数据模型、编码和环境边界。 这意味着阅读不能停在 API 名称或最终打印值，而要回答：输入何时进入、值或对象怎样变化、控制权在哪里转移、错误如何传播、宿主资源由谁清理。

<Jdg11StandardLibraryMapLab />

## 六阶段机制链

### 1. 选择标准库类型

固定源码、运行环境和输入，在动作发生前记录可见绑定、值类型、对象身份、待处理任务和已打开资源。若起点不稳定，后续比较没有意义。

### 2. 构造规范化输入

只执行本阶段最小动作，记录它读取了什么、写入了什么，以及控制权交给语言运行时还是浏览器、Node 或工具链。

### 3. 执行查询或变换

检查抽象规则是否被真正满足。不要用最终输出反推过程；对象引用、隐式转换、短路与异步调度都可能产生表面相同的结果。

### 4. 处理编码、时区与区域设置

在边界值、重复输入、迟到结果和错误宿主状态下重放。保存第一个与正常轨迹不同的位置，而不是只保存最后一条异常。

### 5. 捕获错误与边界

让失败沿规定通道传播，并核对调用者是否能区分可重试、应停止和必须降级的情况。静默吞错不能作为恢复策略。

### 6. 序列化并交付结果

修复单一故障后重放完全相同的输入。只有绑定、值、任务、句柄和生成产物都回到可预测状态，本章才可以签发。

## 出版社目录逐项讲解

### Chapter 11: The JavaScript Standard Library

**Chapter 11: The JavaScript Standard Library** 对应本章“选择标准库类型”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 11.1 Sets and Maps

**11.1 Sets and Maps** 对应本章“构造规范化输入”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 11.1.1 The Set Class

**11.1.1 The Set Class** 对应本章“执行查询或变换”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 11.1.2 The Map Class

**11.1.2 The Map Class** 对应本章“处理编码、时区与区域设置”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 11.1.3 WeakMap and WeakSet

**11.1.3 WeakMap and WeakSet** 对应本章“捕获错误与边界”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 11.2 Typed Arrays and Binary Data

**11.2 Typed Arrays and Binary Data** 对应本章“序列化并交付结果”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 11.2.1 Typed Array Types

**11.2.1 Typed Array Types** 对应本章“选择标准库类型”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 11.2.2 Creating Typed Arrays

**11.2.2 Creating Typed Arrays** 对应本章“构造规范化输入”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 11.2.3 Using Typed Arrays

**11.2.3 Using Typed Arrays** 对应本章“执行查询或变换”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 11.2.4 Typed Array Methods and Properties

**11.2.4 Typed Array Methods and Properties** 对应本章“处理编码、时区与区域设置”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 11.2.5 DataView and Endianness

**11.2.5 DataView and Endianness** 对应本章“捕获错误与边界”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 11.3 Pattern Matching with Regular Expressions

**11.3 Pattern Matching with Regular Expressions** 对应本章“序列化并交付结果”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 11.3.1 Defining Regular Expressions

**11.3.1 Defining Regular Expressions** 对应本章“选择标准库类型”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 11.3.2 String Methods for Pattern Matching

**11.3.2 String Methods for Pattern Matching** 对应本章“构造规范化输入”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 11.3.3 The RegExp Class

**11.3.3 The RegExp Class** 对应本章“执行查询或变换”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 11.4 Dates and Times

**11.4 Dates and Times** 对应本章“处理编码、时区与区域设置”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 11.4.1 Timestamps

**11.4.1 Timestamps** 对应本章“捕获错误与边界”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 11.4.2 Date Arithmetic

**11.4.2 Date Arithmetic** 对应本章“序列化并交付结果”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 11.4.3 Formatting and Parsing Date Strings

**11.4.3 Formatting and Parsing Date Strings** 对应本章“选择标准库类型”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 11.5 Error Classes

**11.5 Error Classes** 对应本章“构造规范化输入”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 11.6 JSON Serialization and Parsing

**11.6 JSON Serialization and Parsing** 对应本章“执行查询或变换”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 11.6.1 JSON Customizations

**11.6.1 JSON Customizations** 对应本章“处理编码、时区与区域设置”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 11.7 The Internationalization API

**11.7 The Internationalization API** 对应本章“捕获错误与边界”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 11.7.1 Formatting Numbers

**11.7.1 Formatting Numbers** 对应本章“序列化并交付结果”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 11.7.2 Formatting Dates and Times

**11.7.2 Formatting Dates and Times** 对应本章“选择标准库类型”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 11.7.3 Comparing Strings

**11.7.3 Comparing Strings** 对应本章“构造规范化输入”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 11.8 The Console API

**11.8 The Console API** 对应本章“执行查询或变换”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 11.8.1 Formatted Output with Console

**11.8.1 Formatted Output with Console** 对应本章“处理编码、时区与区域设置”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 11.9 URL APIs

**11.9 URL APIs** 对应本章“捕获错误与边界”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 11.9.1 Legacy URL Functions

**11.9.1 Legacy URL Functions** 对应本章“序列化并交付结果”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 11.10 Timers

**11.10 Timers** 对应本章“选择标准库类型”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 11.11 Summary

**11.11 Summary** 对应本章“构造规范化输入”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

## 机制实验

<Stepper>
  <Step title="1. 选择标准库类型与构造规范化输入">
    固定输入和初始状态，逐行记录前两段机制证据。
    <Jdg11StandardLibraryMapLab />
  </Step>
  <Step title="2. 执行查询或变换与处理编码、时区与区域设置">
    注入一个边界或失败条件，在首个无法满足契约的位置停止。
    <Jdg11StandardLibraryExperimentLab />
  </Step>
  <Step title="3. 捕获错误与边界与序列化并交付结果">
    清除故障后重放相同输入，核对所有状态和资源已经收敛。
    <Jdg11StandardLibraryEvidenceLab />
  </Step>
</Stepper>

## 最小可运行实验

```javascript
const prices = new Map([["book", 68.5]]);
const format = new Intl.NumberFormat("zh-CN", { style: "currency", currency: "CNY" });
const bytes = new Uint8Array([82, 77, 83]);
console.log(format.format(prices.get("book")), bytes);
```

```yaml
book: JavaScript 权威指南（原书第 7 版）
page: jdg-11-standard-library
official_entries: 32
sample: normal | boundary | failure | recovery
first_divergence: null
values_explained: true
ordering_explained: true
cleanup_complete: true
```

```text
freeze source + runtime + input
predict values + references + control flow + tasks
inject one controlled fault
stop at first divergence
recover and replay the same sample
assert stable state and no stale work
```

## 常见误区与故障注入

<Callout type="trap">
  **误区一：记住“第 11 章 JavaScript 标准库”的结论，却不展开从“选择标准库类型”到“序列化并交付结果”的过程。** 相同输出可能来自不同引用、隐式转换、迟到任务或宿主缓存，必须保存中间证据。
</Callout>

<Callout type="trap">
  **误区二：把 2020 年第 7 版的历史工具与 API 当成永远不变的当前推荐。** 目录忠实度和现代工程状态是两条证据；前者锁定原书，后者应通过当前规范和工具文档单独校验。
</Callout>

## 四类样本与验收

| 样本 | 注入方式 | 必查证据 | 通过条件 |
|---|---|---|---|
| 正常 | 规范输入与单次执行 | 值、对象、调用点、任务、资源 | 六阶段按预期完成 |
| 边界 | 空值、极值、重复输入或慢 I/O | 类型边界与停止条件 | 不发生隐式越界 |
| 失败 | 错误参数、拒绝、断流或工具失败 | 首个偏离点 | 明确停止、传播或降级 |
| 恢复 | 修复后重放完全相同输入 | 清理后的状态与任务数 | 无旧值、旧任务和旧资源 |

<Jdg11StandardLibraryEvidenceLab />

本页签发不变量是：**标准库为集合、二进制数据、正则、日期、错误、JSON、国际化、控制台、URL 和定时器提供可组合能力；选型要同时检查数据模型、编码和环境边界。** 单次输出、截图或一次测试通过不能替代值语义、控制流、对象身份、异步顺序和宿主资源的完整轨迹。

## 练习

<Exercises>

**问题 1** 为什么不能只以最终输出正确作为第 11 章 JavaScript 标准库的验收？

<Answer>
最终输出会隐藏隐式转换、引用别名、短路路径、异步调度和宿主资源状态。必须比较输入、执行步骤、首个偏离点与恢复后的清理结果。
</Answer>

**问题 2** 如何把本页出版社目录变成可执行学习证据？

<Answer>
按公开顺序登记“Chapter 11: The JavaScript Standard Library、11.1 Sets and Maps、11.1.1 The Set Class、11.1.2 The Map Class、11.1.3 WeakMap and WeakSet、11.2 Typed Arrays and Binary Data、11.2.1 Typed Array Types、11.2.2 Creating Typed Arrays、11.2.3 Using Typed Arrays、11.2.4 Typed Array Methods and Properties、11.2.5 DataView and Endianness、11.3 Pattern Matching with Regular Expressions、11.3.1 Defining Regular Expressions、11.3.2 String Methods for Pattern Matching、11.3.3 The RegExp Class、11.4 Dates and Times、11.4.1 Timestamps、11.4.2 Date Arithmetic、11.4.3 Formatting and Parsing Date Strings、11.5 Error Classes、11.6 JSON Serialization and Parsing、11.6.1 JSON Customizations、11.7 The Internationalization API、11.7.1 Formatting Numbers、11.7.2 Formatting Dates and Times、11.7.3 Comparing Strings、11.8 The Console API、11.8.1 Formatted Output with Console、11.9 URL APIs、11.9.1 Legacy URL Functions、11.10 Timers、11.11 Summary”，为每项写出输入、状态、输出、失败与恢复条件，再在同一环境重放正常样本和反例。
</Answer>

**问题 3** 本页的最小通过条件是什么？

<Answer>
标准库为集合、二进制数据、正则、日期、错误、JSON、国际化、控制台、URL 和定时器提供可组合能力；选型要同时检查数据模型、编码和环境边界。 同时，六阶段必须能被同一组输入重放，所有旧值、旧任务、旧句柄和旧产物在恢复后都已清除。
</Answer>

</Exercises>

## 小结

- **Map与Set**：对应“选择标准库类型”阶段的核心观察量。
- **TypedArray**：对应“构造规范化输入”阶段的核心观察量。
- **RegExp**：对应“执行查询或变换”阶段的核心观察量。
- **Intl**：对应“处理编码、时区与区域设置”阶段的核心观察量。
- **URL**：对应“捕获错误与边界”阶段的核心观察量。
- 32 个出版社目录条目已经全部映射到本章证据链。
- 正常、边界、失败和恢复样本共同决定是否通过，最终输出不是唯一证据。

## 术语表

<Glossary>
  <GlossaryItem term="Map与Set">
    Map与Set是本章第 1 个核心观察量，位于“选择标准库类型”阶段。判断它是否正确，必须同时记录输入、状态、输出、失败路径与清理责任。
  </GlossaryItem>
  <GlossaryItem term="TypedArray">
    TypedArray是本章第 2 个核心观察量，位于“构造规范化输入”阶段。判断它是否正确，必须同时记录输入、状态、输出、失败路径与清理责任。
  </GlossaryItem>
  <GlossaryItem term="RegExp">
    RegExp是本章第 3 个核心观察量，位于“执行查询或变换”阶段。判断它是否正确，必须同时记录输入、状态、输出、失败路径与清理责任。
  </GlossaryItem>
  <GlossaryItem term="Intl">
    Intl是本章第 4 个核心观察量，位于“处理编码、时区与区域设置”阶段。判断它是否正确，必须同时记录输入、状态、输出、失败路径与清理责任。
  </GlossaryItem>
  <GlossaryItem term="URL">
    URL是本章第 5 个核心观察量，位于“捕获错误与边界”阶段。判断它是否正确，必须同时记录输入、状态、输出、失败路径与清理责任。
  </GlossaryItem>
</Glossary>

## 版本与改编边界

本单元以 David Flanagan 的英文第 7 版和李松峰译中文第 7 版为身份基线。目录来自 O’Reilly 出版社公开页面，正文是独立教学重构，不复制原书段落；工具版本、浏览器能力和 Node API 的现状需另行按当前官方文档核验。

<Attribution
  adaptedFrom="David Flanagan 著，JavaScript: The Definitive Guide, Seventh Edition；李松峰译，《JavaScript 权威指南（原书第 7 版）》，机械工业出版社，2021，ISBN 9787111677222。本页按出版社公开目录独立教学重构。"
  adaptedUrl="https://www.oreilly.com/library/view/javascript-the-definitive/9781491952016/ch11.html"
/>
