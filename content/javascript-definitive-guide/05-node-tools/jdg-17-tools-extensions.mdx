---
title: "第 17 章 JavaScript 工具与扩展"
type: A
section: "JavaScript 权威指南 · Node 与工程工具"
order: 17
description: "工程工具把源代码送入 lint、格式化、测试、包管理、打包、转译、JSX 和类型检查流水线；每一步都要保留可复现输入和失败证据。"
demo: true
math: false
sourceUrl: "https://www.oreilly.com/library/view/javascript-the-definitive/9781491952016/ch17.html"
draft: false
---


## 本章定位

 是本章的入口观察量。工程工具把源代码送入 lint、格式化、测试、包管理、打包、转译、JSX 和类型检查流水线；每一步都要保留可复现输入和失败证据。 这意味着阅读不能停在 API 名称或最终打印值，而要回答：输入何时进入、值或对象怎样变化、控制权在哪里转移、错误如何传播、宿主资源由谁清理。

## 六阶段机制链

### 1. 解析项目与依赖

固定源码、运行环境和输入，在动作发生前记录可见绑定、值类型、对象身份、待处理任务和已打开资源。若起点不稳定，后续比较没有意义。

### 2. 执行 lint 和格式化

只执行本阶段最小动作，记录它读取了什么、写入了什么，以及控制权交给语言运行时还是浏览器、Node 或工具链。

### 3. 运行单元测试

检查抽象规则是否被真正满足。不要用最终输出反推过程；对象引用、隐式转换、短路与异步调度都可能产生表面相同的结果。

### 4. 解析包并构建模块图

在边界值、重复输入、迟到结果和错误宿主状态下重放。保存第一个与正常轨迹不同的位置，而不是只保存最后一条异常。

### 5. 转译扩展语法与类型

让失败沿规定通道传播，并核对调用者是否能区分可重试、应停止和必须降级的情况。静默吞错不能作为恢复策略。

### 6. 生成可追溯产物

修复单一故障后重放完全相同的输入。只有绑定、值、任务、句柄和生成产物都回到可预测状态，本章才可以签发。

## 出版社目录逐项讲解

### Chapter 17: JavaScript Tools and Extensions

**Chapter 17: JavaScript Tools and Extensions** 对应本章“解析项目与依赖”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 17.1 Linting with ESLint

**17.1 Linting with ESLint** 对应本章“执行 lint 和格式化”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 17.2 JavaScript Formatting with Prettier

**17.2 JavaScript Formatting with Prettier** 对应本章“运行单元测试”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 17.3 Unit Testing with Jest

**17.3 Unit Testing with Jest** 对应本章“解析包并构建模块图”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 17.4 Package Management with npm

**17.4 Package Management with npm** 对应本章“转译扩展语法与类型”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 17.5 Code Bundling

**17.5 Code Bundling** 对应本章“生成可追溯产物”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 17.6 Transpilation with Babel

**17.6 Transpilation with Babel** 对应本章“解析项目与依赖”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 17.7 JSX: Markup Expressions in JavaScript

**17.7 JSX: Markup Expressions in JavaScript** 对应本章“执行 lint 和格式化”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 17.8 Type Checking with Flow

**17.8 Type Checking with Flow** 对应本章“运行单元测试”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 17.8.1 Installing and Running Flow

**17.8.1 Installing and Running Flow** 对应本章“解析包并构建模块图”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 17.8.2 Using Type Annotations

**17.8.2 Using Type Annotations** 对应本章“转译扩展语法与类型”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 17.8.3 Class Types

**17.8.3 Class Types** 对应本章“生成可追溯产物”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 17.8.4 Object Types

**17.8.4 Object Types** 对应本章“解析项目与依赖”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 17.8.5 Type Aliases

**17.8.5 Type Aliases** 对应本章“执行 lint 和格式化”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 17.8.6 Array Types

**17.8.6 Array Types** 对应本章“运行单元测试”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 17.8.7 Other Parameterized Types

**17.8.7 Other Parameterized Types** 对应本章“解析包并构建模块图”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 17.8.8 Read-Only Types

**17.8.8 Read-Only Types** 对应本章“转译扩展语法与类型”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 17.8.9 Function Types

**17.8.9 Function Types** 对应本章“生成可追溯产物”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 17.8.10 Union Types

**17.8.10 Union Types** 对应本章“解析项目与依赖”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 17.8.11 Enumerated Types and Discriminated Unions

**17.8.11 Enumerated Types and Discriminated Unions** 对应本章“执行 lint 和格式化”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

### 17.9 Summary

**17.9 Summary** 对应本章“运行单元测试”观察点。先写出输入和值或资源的初始状态，再逐步记录执行后的类型、绑定、调用点、任务顺序或宿主结果。若边界样本与正常样本得到相同最终输出，也必须比较中间轨迹；只有首个偏离点、异常传播与恢复动作都能解释，才算掌握该目录项。

## 最小可运行实验

```javascript
export function total(values) {
  return values.reduce((sum, value) => sum + value, 0);
}

if (total([1, 2, 3]) !== 6) {
  throw new Error("total regression");
}
```

```yaml
book: JavaScript 权威指南（原书第 7 版）
page: jdg-17-tools-extensions
official_entries: 21
sample: normal | boundary | failure | recovery
first_divergence: null
values_explained: true
ordering_explained: true
cleanup_complete: true
```

```text
freeze source + runtime + input
predict values + references + control flow + tasks
inject one controlled fault
stop at first divergence
recover and replay the same sample
assert stable state and no stale work
```

## 常见误区与故障注入


## 四类样本与验收

| 样本 | 注入方式 | 必查证据 | 通过条件 |
|---|---|---|---|
| 正常 | 规范输入与单次执行 | 值、对象、调用点、任务、资源 | 六阶段按预期完成 |
| 边界 | 空值、极值、重复输入或慢 I/O | 类型边界与停止条件 | 不发生隐式越界 |
| 失败 | 错误参数、拒绝、断流或工具失败 | 首个偏离点 | 明确停止、传播或降级 |
| 恢复 | 修复后重放完全相同输入 | 清理后的状态与任务数 | 无旧值、旧任务和旧资源 |

本页签发不变量是：**工程工具把源代码送入 lint、格式化、测试、包管理、打包、转译、JSX 和类型检查流水线；每一步都要保留可复现输入和失败证据。** 单次输出、截图或一次测试通过不能替代值语义、控制流、对象身份、异步顺序和宿主资源的完整轨迹。

## 练习


## 小结

- **ESLint**：对应“解析项目与依赖”阶段的核心观察量。
- **Prettier**：对应“执行 lint 和格式化”阶段的核心观察量。
- **Jest**：对应“运行单元测试”阶段的核心观察量。
- **Bundler**：对应“解析包并构建模块图”阶段的核心观察量。
- **Transpiler**：对应“转译扩展语法与类型”阶段的核心观察量。
- 21 个出版社目录条目已经全部映射到本章证据链。
- 正常、边界、失败和恢复样本共同决定是否通过，最终输出不是唯一证据。

## 术语表


## 版本与改编边界

本单元以 David Flanagan 的英文第 7 版和李松峰译中文第 7 版为身份基线。目录来自 O’Reilly 出版社公开页面，正文是独立教学重构，不复制原书段落；工具版本、浏览器能力和 Node API 的现状需另行按当前官方文档核验。

