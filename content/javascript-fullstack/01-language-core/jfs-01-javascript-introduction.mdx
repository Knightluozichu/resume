---
title: "第 1 章 JavaScript 简介"
type: A
section: "JavaScript 全栈开发 · 起点"
order: 1
description: "JavaScript 由 ECMAScript 语言、宿主对象与运行时服务共同构成；先分清语言能力和浏览器、Node.js 提供的接口，才能解释同一段源码为什么会在不同环境中表现不同。"
demo: true
math: false
sourceUrl: "https://www.dedao.cn/ebook/detail?id=OAdXprx6N41dm9BQkayr8z7OqLGoE3lvgb0YMlVAnxRZXK2Dg5pbevPJjjnQv2eb"
draft: false
---

import {
  Jfs01JavascriptIntroductionMapLab,
  Jfs01JavascriptIntroductionExperimentLab,
  Jfs01JavascriptIntroductionEvidenceLab,
} from "@/components/mdx/javascript-fullstack/diagrams/jfs-01-javascript-introduction";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第 1 章 JavaScript 简介的核心主张，并把它放回原书 14 章三部分体系
- 能按公开目录逐项描述 6 个标题的输入、状态、输出与边界
- 能实现本页最小样例，定位语言、浏览器或 Node.js 宿主中的首个偏离点
- 能比较正常、边界、失败和恢复四类样本，证明没有旧任务或资源残留

</Objectives>

## 直觉起点

<Term def="ECMAScript 是“第 1 章 JavaScript 简介”的第 1 个核心观察量；它位于“追溯语言与标准”阶段，必须同时说明输入、状态、输出、失败路径与清理责任。">ECMAScript</Term>、<Term def="宿主环境 是“第 1 章 JavaScript 简介”的第 2 个核心观察量；它位于“拆分语言和宿主能力”阶段，必须同时说明输入、状态、输出、失败路径与清理责任。">宿主环境</Term>、<Term def="JavaScript 引擎 是“第 1 章 JavaScript 简介”的第 3 个核心观察量；它位于“辨认动态与函数特性”阶段，必须同时说明输入、状态、输出、失败路径与清理责任。">JavaScript 引擎</Term>、<Term def="全栈开发 是“第 1 章 JavaScript 简介”的第 4 个核心观察量；它位于“映射浏览器与服务器场景”阶段，必须同时说明输入、状态、输出、失败路径与清理责任。">全栈开发</Term>、<Term def="运行环境 是“第 1 章 JavaScript 简介”的第 5 个核心观察量；它位于“搭建双运行环境”阶段，必须同时说明输入、状态、输出、失败路径与清理责任。">运行环境</Term>
共同构成本页的观察坐标。JavaScript 由 ECMAScript
语言、宿主对象与运行时服务共同构成；先分清语言能力和浏览器、Node.js
提供的接口，才能解释同一段源码为什么会在不同环境中表现不同。
这意味着学习不能停在 API
名称和最终截图，而要回答：值在何处产生，对象由谁持有，控制权何时转交宿主，错误怎样返回，监听器、定时器、请求、响应和数据库连接由谁清理。

<Jfs01JavascriptIntroductionMapLab />

## 六阶段机制链

### 1. 追溯语言与标准

固定源码、运行环境、输入与初始状态，先预测值、对象身份、调用点、任务队列和宿主资源。不要从最终输出反推过程；一旦出现分歧，保存首个偏离点再继续。

### 2. 拆分语言和宿主能力

只执行当前最小动作，记录值、对象身份、调用点、任务队列和宿主资源。不要从最终输出反推过程；一旦出现分歧，保存首个偏离点再继续。

### 3. 辨认动态与函数特性

只执行当前最小动作，记录值、对象身份、调用点、任务队列和宿主资源。不要从最终输出反推过程；一旦出现分歧，保存首个偏离点再继续。

### 4. 映射浏览器与服务器场景

只执行当前最小动作，记录值、对象身份、调用点、任务队列和宿主资源。不要从最终输出反推过程；一旦出现分歧，保存首个偏离点再继续。

### 5. 搭建双运行环境

只执行当前最小动作，记录值、对象身份、调用点、任务队列和宿主资源。不要从最终输出反推过程；一旦出现分歧，保存首个偏离点再继续。

### 6. 核对同源程序的差异

只执行当前最小动作，记录值、对象身份、调用点、任务队列和宿主资源。恢复后必须用完全相同的输入重放，并确认所有状态可预测、所有资源有明确归属。

## 边界记录模板

每次实验都建立一行六列证据：**输入快照、当前阶段、语言状态、宿主资源、首个偏离点、恢复结果**。语言核心章节要展开变量绑定、对象身份和调用栈；浏览器章节要增加节点连接状态、事件监听数量、网络请求与页面地址；Node.js 章节要增加请求流、响应是否结束、文件或数据库连接和事务状态。执行“追溯语言与标准”前先写预测，经过“辨认动态与函数特性”后只比较一项受控变化，到“核对同源程序的差异”时重放同一输入。若最终值相同但对象、任务、监听器、流或连接数量不同，仍判定失败；若异常被捕获但部分状态已经提交，也不能签发。这个模板把 API 记忆转换成可复查的因果链，并让正常、边界、失败和恢复样本可以逐列对照。

## 公开目录逐项讲解

### 第 1 章 JavaScript 简介

**第 1 章 JavaScript 简介** 的核心是：建立语言标准、执行引擎和宿主接口三层模型，并把它作为全书判断边界的坐标系。先预测正常输入经过“追溯语言与标准”时会读取和修改哪些值，再运行最小样例，保存“ECMAScript”的实际状态。边界样本必须覆盖空值、重复输入、非法格式或迟到结果；失败样本只注入一个变量，并停在首个偏离点。最后清除故障、重放同一输入，确认输出、任务、监听器、连接和持久化状态都已收敛。

### 1.1 JavaScript 的前世今生

**1.1 JavaScript 的前世今生** 的核心是：从网页脚本、ECMAScript 标准化到 Node.js，理解语言核心为何能跨越客户端和服务器。先预测正常输入经过“拆分语言和宿主能力”时会读取和修改哪些值，再运行最小样例，保存“宿主环境”的实际状态。边界样本必须覆盖空值、重复输入、非法格式或迟到结果；失败样本只注入一个变量，并停在首个偏离点。最后清除故障、重放同一输入，确认输出、任务、监听器、连接和持久化状态都已收敛。

### 1.2 JavaScript 的组成与特性

**1.2 JavaScript 的组成与特性** 的核心是：区分 ECMAScript、DOM、BOM 与 Node API，并掌握动态类型、原型和一等函数的组合方式。先预测正常输入经过“辨认动态与函数特性”时会读取和修改哪些值，再运行最小样例，保存“JavaScript 引擎”的实际状态。边界样本必须覆盖空值、重复输入、非法格式或迟到结果；失败样本只注入一个变量，并停在首个偏离点。最后清除故障、重放同一输入，确认输出、任务、监听器、连接和持久化状态都已收敛。

### 1.3 JavaScript 的适用领域

**1.3 JavaScript 的适用领域** 的核心是：比较浏览器交互、服务器 I/O、工具脚本等场景的输入、资源与失败边界。先预测正常输入经过“映射浏览器与服务器场景”时会读取和修改哪些值，再运行最小样例，保存“全栈开发”的实际状态。边界样本必须覆盖空值、重复输入、非法格式或迟到结果；失败样本只注入一个变量，并停在首个偏离点。最后清除故障、重放同一输入，确认输出、任务、监听器、连接和持久化状态都已收敛。

### 1.4 运行环境的搭建

**1.4 运行环境的搭建** 的核心是：分别用浏览器开发者工具和 Node.js 执行同一最小程序，记录全局对象和模块边界。先预测正常输入经过“搭建双运行环境”时会读取和修改哪些值，再运行最小样例，保存“运行环境”的实际状态。边界样本必须覆盖空值、重复输入、非法格式或迟到结果；失败样本只注入一个变量，并停在首个偏离点。最后清除故障、重放同一输入，确认输出、任务、监听器、连接和持久化状态都已收敛。

### 本章小结

**本章小结** 的核心是：用语言核心加宿主能力的模型复盘 JavaScript 全栈路线，避免把框架名称误当成语言本身。先预测正常输入经过“核对同源程序的差异”时会读取和修改哪些值，再运行最小样例，保存“ECMAScript”的实际状态。边界样本必须覆盖空值、重复输入、非法格式或迟到结果；失败样本只注入一个变量，并停在首个偏离点。最后清除故障、重放同一输入，确认输出、任务、监听器、连接和持久化状态都已收敛。

## 机制实验

<Stepper>
  <Step title="1. 追溯语言与标准与拆分语言和宿主能力">
    先预测前两阶段的值、对象和控制权，再运行正常样本并保存基线。
    <Jfs01JavascriptIntroductionMapLab />
  </Step>
  <Step title="2. 辨认动态与函数特性与映射浏览器与服务器场景">
    只注入一个边界或失败条件，在首个无法满足契约的位置停止。
    <Jfs01JavascriptIntroductionExperimentLab />
  </Step>
  <Step title="3. 搭建双运行环境与核对同源程序的差异">
    清除故障后重放同一输入，核对任务、监听器、流、连接和生成状态全部收敛。
    <Jfs01JavascriptIntroductionEvidenceLab />
  </Step>
</Stepper>

## 最小可运行实验

```javascript
const facts = {
  language: typeof Promise === "function",
  host: typeof document === "object" ? "browser" : "node",
  timer: typeof setTimeout === "function",
};
console.table(facts);
```

```yaml
book: JavaScript 全栈开发
page: jfs-01-javascript-introduction
public_toc_entries: 6
sample: normal | boundary | failure | recovery
first_divergence: null
language_state_explained: true
host_resources_explained: true
cleanup_complete: true
```

```text
freeze source + runtime + input
predict language values + host effects
inject exactly one controlled fault
stop at the first divergence
recover and replay the same input
assert stable state and no stale resources
```

## 常见误区与故障注入

<Callout type="trap">
  **误区一：记住“第 1 章 JavaScript
  简介”的结论，却不展开从“追溯语言与标准”到“核对同源程序的差异”的过程。**
  相同输出可能来自不同对象别名、任务顺序、DOM 状态、HTTP
  状态或数据库提交结果，必须保存中间证据。
</Callout>

<Callout type="trap">
  **误区二：把框架名称当成全栈知识本身。** 原作者明确说明本书专注 JavaScript
  及宿主环境接口，不以 React、Express 等快速变化的框架替代语言、浏览器和 Node.js
  底层机制；框架现状应另行查当前官方文档。
</Callout>

## 四类样本与验收

| 样本 | 注入方式                         | 必查证据                         | 通过条件                   |
| ---- | -------------------------------- | -------------------------------- | -------------------------- |
| 正常 | 合法输入与单次执行               | 值、对象、调用点、任务、宿主资源 | 六阶段按预期完成           |
| 边界 | 空值、极值、重复输入、慢 I/O     | 类型与协议边界、停止条件         | 不发生隐式越界或重复提交   |
| 失败 | 错误参数、拒绝、断流、数据库失败 | 首个偏离点与错误通道             | 明确停止、传播或回滚       |
| 恢复 | 清除故障后重放相同输入           | 清理后的状态与资源数             | 无旧任务、监听器、流和连接 |

<Jfs01JavascriptIntroductionEvidenceLab />

本页签发不变量是：**JavaScript 由 ECMAScript 语言、宿主对象与运行时服务共同构成；先分清语言能力和浏览器、Node.js 提供的接口，才能解释同一段源码为什么会在不同环境中表现不同。** 单次输出、截图或一次请求成功不能替代值语义、对象身份、控制流、任务顺序和宿主资源的完整轨迹。

## 练习

<Exercises>

**问题 1** 为什么不能只以最终输出正确作为第 1 章 JavaScript 简介的验收？

<Answer>
  最终输出会隐藏对象别名、短路路径、异步顺序、DOM 连接状态、HTTP
  分支和持久化副作用。必须比较输入、执行步骤、首个偏离点与恢复后的清理结果。
</Answer>

**问题 2** 如何把本页公开目录变成可执行学习证据？

<Answer>
  按公开顺序登记“第 1 章 JavaScript 简介、1.1 JavaScript 的前世今生、1.2
  JavaScript 的组成与特性、1.3 JavaScript 的适用领域、1.4
  运行环境的搭建、本章小结”，为每项写出输入、状态、输出、失败与恢复条件，再在同一环境重放正常样本和反例。
</Answer>

**问题 3** 本页的最小通过条件是什么？

<Answer>
  JavaScript 由 ECMAScript
  语言、宿主对象与运行时服务共同构成；先分清语言能力和浏览器、Node.js
  提供的接口，才能解释同一段源码为什么会在不同环境中表现不同。
  同时，六阶段必须能被同一组输入重放，旧值、旧任务、旧监听器、旧流、旧连接和未提交状态在恢复后全部清除。
</Answer>

</Exercises>

## 小结

- **ECMAScript**：对应“追溯语言与标准”阶段的核心观察量。
- **宿主环境**：对应“拆分语言和宿主能力”阶段的核心观察量。
- **JavaScript 引擎**：对应“辨认动态与函数特性”阶段的核心观察量。
- **全栈开发**：对应“映射浏览器与服务器场景”阶段的核心观察量。
- **运行环境**：对应“搭建双运行环境”阶段的核心观察量。
- 6 个公开目录条目已经全部映射到本页证据链。
- 正常、边界、失败和恢复样本共同决定是否通过，最终输出不是唯一证据。

## 术语表

<Glossary>
  <GlossaryItem term="ECMAScript">
    ECMAScript 是本页第 1
    个核心观察量，位于“追溯语言与标准”阶段。判断它是否正确，必须同时记录输入、状态、输出、失败路径与清理责任。
  </GlossaryItem>
  <GlossaryItem term="宿主环境">
    宿主环境 是本页第 2
    个核心观察量，位于“拆分语言和宿主能力”阶段。判断它是否正确，必须同时记录输入、状态、输出、失败路径与清理责任。
  </GlossaryItem>
  <GlossaryItem term="JavaScript 引擎">
    JavaScript 引擎 是本页第 3
    个核心观察量，位于“辨认动态与函数特性”阶段。判断它是否正确，必须同时记录输入、状态、输出、失败路径与清理责任。
  </GlossaryItem>
  <GlossaryItem term="全栈开发">
    全栈开发 是本页第 4
    个核心观察量，位于“映射浏览器与服务器场景”阶段。判断它是否正确，必须同时记录输入、状态、输出、失败路径与清理责任。
  </GlossaryItem>
  <GlossaryItem term="运行环境">
    运行环境 是本页第 5
    个核心观察量，位于“搭建双运行环境”阶段。判断它是否正确，必须同时记录输入、状态、输出、失败路径与清理责任。
  </GlossaryItem>
</Glossary>

## 版本与改编边界

本单元以凌杰著《JavaScript 全栈开发》为身份基线。公开目录来自人民邮电出版社出版电子版页面，创作原则由作者出版说明交叉确认；正文是独立教学重构，不复制原书段落。浏览器、Node.js 和数据库 API 的当前状态需另行按各自官方文档核验。

<Attribution
  adaptedFrom="凌杰著，《JavaScript 全栈开发》，人民邮电出版社，2021 年 6 月，376 页，ISBN 9787115553669。本页按出版社公开目录与作者出版说明独立教学重构。"
  adaptedUrl="https://www.dedao.cn/ebook/detail?id=OAdXprx6N41dm9BQkayr8z7OqLGoE3lvgb0YMlVAnxRZXK2Dg5pbevPJjjnQv2eb"
/>
