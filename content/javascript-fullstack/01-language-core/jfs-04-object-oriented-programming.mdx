---
title: "第 4 章 面向对象编程"
type: A
section: "JavaScript 全栈开发 · 语言核心"
order: 4
description: "JavaScript 的面向对象建立在对象与原型委托之上；类语法提供更清晰的构造接口，但属性所有权、原型链和方法接收者仍是运行时真相。"
demo: true
math: false
sourceUrl: "https://www.dedao.cn/ebook/detail?id=OAdXprx6N41dm9BQkayr8z7OqLGoE3lvgb0YMlVAnxRZXK2Dg5pbevPJjjnQv2eb"
draft: false
---


## 直觉起点

、、、、共同构成本页的观察坐标。JavaScript
的面向对象建立在对象与原型委托之上；类语法提供更清晰的构造接口，但属性所有权、原型链和方法接收者仍是运行时真相。
这意味着学习不能停在 API
名称和最终截图，而要回答：值在何处产生，对象由谁持有，控制权何时转交宿主，错误怎样返回，监听器、定时器、请求、响应和数据库连接由谁清理。

## 六阶段机制链

### 1. 提取对象职责

固定源码、运行环境、输入与初始状态，先预测值、对象身份、调用点、任务队列和宿主资源。不要从最终输出反推过程；一旦出现分歧，保存首个偏离点再继续。

### 2. 定义构造不变量

只执行当前最小动作，记录值、对象身份、调用点、任务队列和宿主资源。不要从最终输出反推过程；一旦出现分歧，保存首个偏离点再继续。

### 3. 创建实例状态

只执行当前最小动作，记录值、对象身份、调用点、任务队列和宿主资源。不要从最终输出反推过程；一旦出现分歧，保存首个偏离点再继续。

### 4. 沿原型共享方法

只执行当前最小动作，记录值、对象身份、调用点、任务队列和宿主资源。不要从最终输出反推过程；一旦出现分歧，保存首个偏离点再继续。

### 5. 组合或继承行为

只执行当前最小动作，记录值、对象身份、调用点、任务队列和宿主资源。不要从最终输出反推过程；一旦出现分歧，保存首个偏离点再继续。

### 6. 检查所有权与替换原则

只执行当前最小动作，记录值、对象身份、调用点、任务队列和宿主资源。恢复后必须用完全相同的输入重放，并确认所有状态可预测、所有资源有明确归属。

## 边界记录模板

每次实验都建立一行六列证据：**输入快照、当前阶段、语言状态、宿主资源、首个偏离点、恢复结果**。语言核心章节要展开变量绑定、对象身份和调用栈；浏览器章节要增加节点连接状态、事件监听数量、网络请求与页面地址；Node.js 章节要增加请求流、响应是否结束、文件或数据库连接和事务状态。执行“提取对象职责”前先写预测，经过“创建实例状态”后只比较一项受控变化，到“检查所有权与替换原则”时重放同一输入。若最终值相同但对象、任务、监听器、流或连接数量不同，仍判定失败；若异常被捕获但部分状态已经提交，也不能签发。这个模板把 API 记忆转换成可复查的因果链，并让正常、边界、失败和恢复样本可以逐列对照。

## 公开目录逐项讲解

### 第 4 章 面向对象编程

**第 4 章 面向对象编程** 的核心是：用职责、状态不变量和方法委托解释对象协作，而不是只记 class 语法。先预测正常输入经过“提取对象职责”时会读取和修改哪些值，再运行最小样例，保存“对象职责”的实际状态。边界样本必须覆盖空值、重复输入、非法格式或迟到结果；失败样本只注入一个变量，并停在首个偏离点。最后清除故障、重放同一输入，确认输出、任务、监听器、连接和持久化状态都已收敛。

### 4.1 何谓面向对象

**4.1 何谓面向对象** 的核心是：从消息协作、封装和多态理解对象模型，判断何时组合比继承更直接。先预测正常输入经过“定义构造不变量”时会读取和修改哪些值，再运行最小样例，保存“构造函数”的实际状态。边界样本必须覆盖空值、重复输入、非法格式或迟到结果；失败样本只注入一个变量，并停在首个偏离点。最后清除故障、重放同一输入，确认输出、任务、监听器、连接和持久化状态都已收敛。

### 4.2 深度探索对象

**4.2 深度探索对象** 的核心是：检查自有属性、访问器、描述符、this 接收者与对象可扩展性对行为的影响。先预测正常输入经过“创建实例状态”时会读取和修改哪些值，再运行最小样例，保存“原型链”的实际状态。边界样本必须覆盖空值、重复输入、非法格式或迟到结果；失败样本只注入一个变量，并停在首个偏离点。最后清除故障、重放同一输入，确认输出、任务、监听器、连接和持久化状态都已收敛。

### 4.3 原型继承机制

**4.3 原型继承机制** 的核心是：沿原型链推演读取、遮蔽和方法共享，并把 class/extends 还原为委托关系。先预测正常输入经过“沿原型共享方法”时会读取和修改哪些值，再运行最小样例，保存“继承”的实际状态。边界样本必须覆盖空值、重复输入、非法格式或迟到结果；失败样本只注入一个变量，并停在首个偏离点。最后清除故障、重放同一输入，确认输出、任务、监听器、连接和持久化状态都已收敛。

### 4.4 综合练习

**4.4 综合练习** 的核心是：构造账户对象族，注入非法金额、方法脱离接收者和属性遮蔽，验证不变量。先预测正常输入经过“组合或继承行为”时会读取和修改哪些值，再运行最小样例，保存“方法委托”的实际状态。边界样本必须覆盖空值、重复输入、非法格式或迟到结果；失败样本只注入一个变量，并停在首个偏离点。最后清除故障、重放同一输入，确认输出、任务、监听器、连接和持久化状态都已收敛。

### 本章小结

**本章小结** 的核心是：用实例状态、原型方法、自有属性和接收者四张表复盘面向对象机制。先预测正常输入经过“检查所有权与替换原则”时会读取和修改哪些值，再运行最小样例，保存“对象职责”的实际状态。边界样本必须覆盖空值、重复输入、非法格式或迟到结果；失败样本只注入一个变量，并停在首个偏离点。最后清除故障、重放同一输入，确认输出、任务、监听器、连接和持久化状态都已收敛。

## 最小可运行实验

```javascript
const accountMethods = {
  deposit(amount) {
    if (amount <= 0) throw new RangeError("amount");
    this.balance += amount;
  },
};
const account = Object.assign(Object.create(accountMethods), { balance: 10 });
account.deposit(5);
console.log(account.balance, accountMethods.isPrototypeOf(account));
```

```yaml
book: JavaScript 全栈开发
page: jfs-04-object-oriented-programming
public_toc_entries: 6
sample: normal | boundary | failure | recovery
first_divergence: null
language_state_explained: true
host_resources_explained: true
cleanup_complete: true
```

```text
freeze source + runtime + input
predict language values + host effects
inject exactly one controlled fault
stop at the first divergence
recover and replay the same input
assert stable state and no stale resources
```

## 常见误区与故障注入


## 四类样本与验收

| 样本 | 注入方式                         | 必查证据                         | 通过条件                   |
| ---- | -------------------------------- | -------------------------------- | -------------------------- |
| 正常 | 合法输入与单次执行               | 值、对象、调用点、任务、宿主资源 | 六阶段按预期完成           |
| 边界 | 空值、极值、重复输入、慢 I/O     | 类型与协议边界、停止条件         | 不发生隐式越界或重复提交   |
| 失败 | 错误参数、拒绝、断流、数据库失败 | 首个偏离点与错误通道             | 明确停止、传播或回滚       |
| 恢复 | 清除故障后重放相同输入           | 清理后的状态与资源数             | 无旧任务、监听器、流和连接 |

本页签发不变量是：**JavaScript 的面向对象建立在对象与原型委托之上；类语法提供更清晰的构造接口，但属性所有权、原型链和方法接收者仍是运行时真相。** 单次输出、截图或一次请求成功不能替代值语义、对象身份、控制流、任务顺序和宿主资源的完整轨迹。

## 练习


## 小结

- **对象职责**：对应“提取对象职责”阶段的核心观察量。
- **构造函数**：对应“定义构造不变量”阶段的核心观察量。
- **原型链**：对应“创建实例状态”阶段的核心观察量。
- **继承**：对应“沿原型共享方法”阶段的核心观察量。
- **方法委托**：对应“组合或继承行为”阶段的核心观察量。
- 6 个公开目录条目已经全部映射到本页证据链。
- 正常、边界、失败和恢复样本共同决定是否通过，最终输出不是唯一证据。

## 术语表


## 版本与改编边界

本单元以凌杰著《JavaScript 全栈开发》为身份基线。公开目录来自人民邮电出版社出版电子版页面，创作原则由作者出版说明交叉确认；正文是独立教学重构，不复制原书段落。浏览器、Node.js 和数据库 API 的当前状态需另行按各自官方文档核验。

