---
title: "第 6 章 前端编程概述"
type: A
section: "JavaScript 全栈开发 · 浏览器端"
order: 6
description: "浏览器负责取得资源、解析文档、构建可视树并响应用户输入；前端 JavaScript 在安全沙箱中协调 DOM、事件、网络与存储，而不是替代 HTML 和 CSS。"
demo: true
math: false
sourceUrl: "https://www.dedao.cn/ebook/detail?id=OAdXprx6N41dm9BQkayr8z7OqLGoE3lvgb0YMlVAnxRZXK2Dg5pbevPJjjnQv2eb"
draft: true
---

import {
  Jfs06FrontendOverviewMapLab,
  Jfs06FrontendOverviewExperimentLab,
  Jfs06FrontendOverviewEvidenceLab,
} from "@/components/mdx/javascript-fullstack/diagrams/jfs-06-frontend-overview";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第 6 章 前端编程概述的核心主张，并把它放回原书 14 章三部分体系
- 能按公开目录逐项描述 7 个标题的输入、状态、输出与边界
- 能实现本页最小样例，定位语言、浏览器或 Node.js 宿主中的首个偏离点
- 能比较正常、边界、失败和恢复四类样本，证明没有旧任务或资源残留

</Objectives>

## 直觉起点

<Term def="浏览器上下文 是“第 6 章 前端编程概述”的第 1 个核心观察量；它位于“请求页面资源”阶段，必须同时说明输入、状态、输出、失败路径与清理责任。">浏览器上下文</Term>、<Term def="文档对象 是“第 6 章 前端编程概述”的第 2 个核心观察量；它位于“解析 HTML 与 CSS”阶段，必须同时说明输入、状态、输出、失败路径与清理责任。">文档对象</Term>、<Term def="前端职责 是“第 6 章 前端编程概述”的第 3 个核心观察量；它位于“创建文档和窗口对象”阶段，必须同时说明输入、状态、输出、失败路径与清理责任。">前端职责</Term>、<Term def="渐进增强 是“第 6 章 前端编程概述”的第 4 个核心观察量；它位于“加载并执行脚本”阶段，必须同时说明输入、状态、输出、失败路径与清理责任。">渐进增强</Term>、<Term def="同源边界 是“第 6 章 前端编程概述”的第 5 个核心观察量；它位于“响应输入和网络”阶段，必须同时说明输入、状态、输出、失败路径与清理责任。">同源边界</Term>
共同构成本页的观察坐标。浏览器负责取得资源、解析文档、构建可视树并响应用户输入；前端
JavaScript 在安全沙箱中协调 DOM、事件、网络与存储，而不是替代 HTML 和 CSS。
这意味着学习不能停在 API
名称和最终截图，而要回答：值在何处产生，对象由谁持有，控制权何时转交宿主，错误怎样返回，监听器、定时器、请求、响应和数据库连接由谁清理。

<Jfs06FrontendOverviewMapLab />

## 六阶段机制链

### 1. 请求页面资源

固定源码、运行环境、输入与初始状态，先预测值、对象身份、调用点、任务队列和宿主资源。不要从最终输出反推过程；一旦出现分歧，保存首个偏离点再继续。

### 2. 解析 HTML 与 CSS

只执行当前最小动作，记录值、对象身份、调用点、任务队列和宿主资源。不要从最终输出反推过程；一旦出现分歧，保存首个偏离点再继续。

### 3. 创建文档和窗口对象

只执行当前最小动作，记录值、对象身份、调用点、任务队列和宿主资源。不要从最终输出反推过程；一旦出现分歧，保存首个偏离点再继续。

### 4. 加载并执行脚本

只执行当前最小动作，记录值、对象身份、调用点、任务队列和宿主资源。不要从最终输出反推过程；一旦出现分歧，保存首个偏离点再继续。

### 5. 响应输入和网络

只执行当前最小动作，记录值、对象身份、调用点、任务队列和宿主资源。不要从最终输出反推过程；一旦出现分歧，保存首个偏离点再继续。

### 6. 更新界面并释放监听

只执行当前最小动作，记录值、对象身份、调用点、任务队列和宿主资源。恢复后必须用完全相同的输入重放，并确认所有状态可预测、所有资源有明确归属。

## 边界记录模板

每次实验都建立一行六列证据：**输入快照、当前阶段、语言状态、宿主资源、首个偏离点、恢复结果**。语言核心章节要展开变量绑定、对象身份和调用栈；浏览器章节要增加节点连接状态、事件监听数量、网络请求与页面地址；Node.js 章节要增加请求流、响应是否结束、文件或数据库连接和事务状态。执行“请求页面资源”前先写预测，经过“创建文档和窗口对象”后只比较一项受控变化，到“更新界面并释放监听”时重放同一输入。若最终值相同但对象、任务、监听器、流或连接数量不同，仍判定失败；若异常被捕获但部分状态已经提交，也不能签发。这个模板把 API 记忆转换成可复查的因果链，并让正常、边界、失败和恢复样本可以逐列对照。

## 公开目录逐项讲解

### 第 6 章 前端编程概述

**第 6 章 前端编程概述** 的核心是：建立浏览器从资源到交互的执行模型，为 DOM、事件和 AJAX 三条后续主线定位。先预测正常输入经过“请求页面资源”时会读取和修改哪些值，再运行最小样例，保存“浏览器上下文”的实际状态。边界样本必须覆盖空值、重复输入、非法格式或迟到结果；失败样本只注入一个变量，并停在首个偏离点。最后清除故障、重放同一输入，确认输出、任务、监听器、连接和持久化状态都已收敛。

### 6.1 浏览器扮演的角色

**6.1 浏览器扮演的角色** 的核心是：跟踪导航、解析、布局、绘制和事件循环，区分浏览器进程与页面脚本职责。先预测正常输入经过“解析 HTML 与 CSS”时会读取和修改哪些值，再运行最小样例，保存“文档对象”的实际状态。边界样本必须覆盖空值、重复输入、非法格式或迟到结果；失败样本只注入一个变量，并停在首个偏离点。最后清除故障、重放同一输入，确认输出、任务、监听器、连接和持久化状态都已收敛。

### 6.2 明确前端开发任务

**6.2 明确前端开发任务** 的核心是：把语义结构交给 HTML、视觉布局交给 CSS、状态交互交给 JavaScript。先预测正常输入经过“创建文档和窗口对象”时会读取和修改哪些值，再运行最小样例，保存“前端职责”的实际状态。边界样本必须覆盖空值、重复输入、非法格式或迟到结果；失败样本只注入一个变量，并停在首个偏离点。最后清除故障、重放同一输入，确认输出、任务、监听器、连接和持久化状态都已收敛。

### 6.3 前端编程中的 JavaScript

**6.3 前端编程中的 JavaScript** 的核心是：理解脚本装载时机、主线程预算、同源策略与用户输入驱动的执行特点。先预测正常输入经过“加载并执行脚本”时会读取和修改哪些值，再运行最小样例，保存“渐进增强”的实际状态。边界样本必须覆盖空值、重复输入、非法格式或迟到结果；失败样本只注入一个变量，并停在首个偏离点。最后清除故障、重放同一输入，确认输出、任务、监听器、连接和持久化状态都已收敛。

### 6.4 前端编程对象

**6.4 前端编程对象** 的核心是：辨认 Window、Document、Element、Event、Location、History 等宿主对象的所有权。先预测正常输入经过“响应输入和网络”时会读取和修改哪些值，再运行最小样例，保存“同源边界”的实际状态。边界样本必须覆盖空值、重复输入、非法格式或迟到结果；失败样本只注入一个变量，并停在首个偏离点。最后清除故障、重放同一输入，确认输出、任务、监听器、连接和持久化状态都已收敛。

### 6.5 综合练习

**6.5 综合练习** 的核心是：编写可重复初始化和销毁的页面增强函数，验证缺失节点与重复挂载不会泄漏监听器。先预测正常输入经过“更新界面并释放监听”时会读取和修改哪些值，再运行最小样例，保存“浏览器上下文”的实际状态。边界样本必须覆盖空值、重复输入、非法格式或迟到结果；失败样本只注入一个变量，并停在首个偏离点。最后清除故障、重放同一输入，确认输出、任务、监听器、连接和持久化状态都已收敛。

### 本章小结

**本章小结** 的核心是：用加载、解析、执行、交互、更新和清理六阶段复盘浏览器端职责。先预测正常输入经过“请求页面资源”时会读取和修改哪些值，再运行最小样例，保存“文档对象”的实际状态。边界样本必须覆盖空值、重复输入、非法格式或迟到结果；失败样本只注入一个变量，并停在首个偏离点。最后清除故障、重放同一输入，确认输出、任务、监听器、连接和持久化状态都已收敛。

## 机制实验

<Stepper>
  <Step title="1. 请求页面资源与解析 HTML 与 CSS">
    先预测前两阶段的值、对象和控制权，再运行正常样本并保存基线。
    <Jfs06FrontendOverviewMapLab />
  </Step>
  <Step title="2. 创建文档和窗口对象与加载并执行脚本">
    只注入一个边界或失败条件，在首个无法满足契约的位置停止。
    <Jfs06FrontendOverviewExperimentLab />
  </Step>
  <Step title="3. 响应输入和网络与更新界面并释放监听">
    清除故障后重放同一输入，核对任务、监听器、流、连接和生成状态全部收敛。
    <Jfs06FrontendOverviewEvidenceLab />
  </Step>
</Stepper>

## 最小可运行实验

```javascript
function boot(documentRef) {
  const status = documentRef.querySelector("[data-status]");
  if (!status) return () => {};
  status.textContent = "ready";
  const onClick = () => status.toggleAttribute("data-active");
  status.addEventListener("click", onClick);
  return () => status.removeEventListener("click", onClick);
}
```

```yaml
book: JavaScript 全栈开发
page: jfs-06-frontend-overview
public_toc_entries: 7
sample: normal | boundary | failure | recovery
first_divergence: null
language_state_explained: true
host_resources_explained: true
cleanup_complete: true
```

```text
freeze source + runtime + input
predict language values + host effects
inject exactly one controlled fault
stop at the first divergence
recover and replay the same input
assert stable state and no stale resources
```

## 常见误区与故障注入

<Callout type="trap">
  **误区一：记住“第 6 章
  前端编程概述”的结论，却不展开从“请求页面资源”到“更新界面并释放监听”的过程。**
  相同输出可能来自不同对象别名、任务顺序、DOM 状态、HTTP
  状态或数据库提交结果，必须保存中间证据。
</Callout>

<Callout type="trap">
  **误区二：把框架名称当成全栈知识本身。** 原作者明确说明本书专注 JavaScript
  及宿主环境接口，不以 React、Express 等快速变化的框架替代语言、浏览器和 Node.js
  底层机制；框架现状应另行查当前官方文档。
</Callout>

## 四类样本与验收

| 样本 | 注入方式                         | 必查证据                         | 通过条件                   |
| ---- | -------------------------------- | -------------------------------- | -------------------------- |
| 正常 | 合法输入与单次执行               | 值、对象、调用点、任务、宿主资源 | 六阶段按预期完成           |
| 边界 | 空值、极值、重复输入、慢 I/O     | 类型与协议边界、停止条件         | 不发生隐式越界或重复提交   |
| 失败 | 错误参数、拒绝、断流、数据库失败 | 首个偏离点与错误通道             | 明确停止、传播或回滚       |
| 恢复 | 清除故障后重放相同输入           | 清理后的状态与资源数             | 无旧任务、监听器、流和连接 |

<Jfs06FrontendOverviewEvidenceLab />

本页签发不变量是：**浏览器负责取得资源、解析文档、构建可视树并响应用户输入；前端 JavaScript 在安全沙箱中协调 DOM、事件、网络与存储，而不是替代 HTML 和 CSS。** 单次输出、截图或一次请求成功不能替代值语义、对象身份、控制流、任务顺序和宿主资源的完整轨迹。

## 练习

<Exercises>

**问题 1** 为什么不能只以最终输出正确作为第 6 章 前端编程概述的验收？

<Answer>
  最终输出会隐藏对象别名、短路路径、异步顺序、DOM 连接状态、HTTP
  分支和持久化副作用。必须比较输入、执行步骤、首个偏离点与恢复后的清理结果。
</Answer>

**问题 2** 如何把本页公开目录变成可执行学习证据？

<Answer>
  按公开顺序登记“第 6 章 前端编程概述、6.1 浏览器扮演的角色、6.2
  明确前端开发任务、6.3 前端编程中的 JavaScript、6.4 前端编程对象、6.5
  综合练习、本章小结”，为每项写出输入、状态、输出、失败与恢复条件，再在同一环境重放正常样本和反例。
</Answer>

**问题 3** 本页的最小通过条件是什么？

<Answer>
  浏览器负责取得资源、解析文档、构建可视树并响应用户输入；前端 JavaScript
  在安全沙箱中协调 DOM、事件、网络与存储，而不是替代 HTML 和 CSS。
  同时，六阶段必须能被同一组输入重放，旧值、旧任务、旧监听器、旧流、旧连接和未提交状态在恢复后全部清除。
</Answer>

</Exercises>

## 小结

- **浏览器上下文**：对应“请求页面资源”阶段的核心观察量。
- **文档对象**：对应“解析 HTML 与 CSS”阶段的核心观察量。
- **前端职责**：对应“创建文档和窗口对象”阶段的核心观察量。
- **渐进增强**：对应“加载并执行脚本”阶段的核心观察量。
- **同源边界**：对应“响应输入和网络”阶段的核心观察量。
- 7 个公开目录条目已经全部映射到本页证据链。
- 正常、边界、失败和恢复样本共同决定是否通过，最终输出不是唯一证据。

## 术语表

<Glossary>
  <GlossaryItem term="浏览器上下文">
    浏览器上下文 是本页第 1
    个核心观察量，位于“请求页面资源”阶段。判断它是否正确，必须同时记录输入、状态、输出、失败路径与清理责任。
  </GlossaryItem>
  <GlossaryItem term="文档对象">
    文档对象 是本页第 2 个核心观察量，位于“解析 HTML 与
    CSS”阶段。判断它是否正确，必须同时记录输入、状态、输出、失败路径与清理责任。
  </GlossaryItem>
  <GlossaryItem term="前端职责">
    前端职责 是本页第 3
    个核心观察量，位于“创建文档和窗口对象”阶段。判断它是否正确，必须同时记录输入、状态、输出、失败路径与清理责任。
  </GlossaryItem>
  <GlossaryItem term="渐进增强">
    渐进增强 是本页第 4
    个核心观察量，位于“加载并执行脚本”阶段。判断它是否正确，必须同时记录输入、状态、输出、失败路径与清理责任。
  </GlossaryItem>
  <GlossaryItem term="同源边界">
    同源边界 是本页第 5
    个核心观察量，位于“响应输入和网络”阶段。判断它是否正确，必须同时记录输入、状态、输出、失败路径与清理责任。
  </GlossaryItem>
</Glossary>

## 版本与改编边界

本单元以凌杰著《JavaScript 全栈开发》为身份基线。公开目录来自人民邮电出版社出版电子版页面，创作原则由作者出版说明交叉确认；正文是独立教学重构，不复制原书段落。浏览器、Node.js 和数据库 API 的当前状态需另行按各自官方文档核验。

<Attribution
  adaptedFrom="凌杰著，《JavaScript 全栈开发》，人民邮电出版社，2021 年 6 月，376 页，ISBN 9787115553669。本页按出版社公开目录与作者出版说明独立教学重构。"
  adaptedUrl="https://www.dedao.cn/ebook/detail?id=OAdXprx6N41dm9BQkayr8z7OqLGoE3lvgb0YMlVAnxRZXK2Dg5pbevPJjjnQv2eb"
/>
