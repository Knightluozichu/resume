---
title: "第 10 章 AJAX 编程方法"
type: A
section: "JavaScript 全栈开发 · 浏览器端"
order: 10
description: "AJAX 在不重新导航整页的前提下发起 HTTP 请求并局部更新界面；可靠封装必须统一地址、方法、头、正文、超时、取消、状态码和解析错误。"
demo: true
math: false
sourceUrl: "https://www.dedao.cn/ebook/detail?id=OAdXprx6N41dm9BQkayr8z7OqLGoE3lvgb0YMlVAnxRZXK2Dg5pbevPJjjnQv2eb"
draft: false
---


## 直觉起点

、、、、共同构成本页的观察坐标。AJAX 在不重新导航整页的前提下发起 HTTP
请求并局部更新界面；可靠封装必须统一地址、方法、头、正文、超时、取消、状态码和解析错误。
这意味着学习不能停在 API
名称和最终截图，而要回答：值在何处产生，对象由谁持有，控制权何时转交宿主，错误怎样返回，监听器、定时器、请求、响应和数据库连接由谁清理。

## 六阶段机制链

### 1. 构造请求契约

固定源码、运行环境、输入与初始状态，先预测值、对象身份、调用点、任务队列和宿主资源。不要从最终输出反推过程；一旦出现分歧，保存首个偏离点再继续。

### 2. 发送异步 HTTP

只执行当前最小动作，记录值、对象身份、调用点、任务队列和宿主资源。不要从最终输出反推过程；一旦出现分歧，保存首个偏离点再继续。

### 3. 等待响应头和正文

只执行当前最小动作，记录值、对象身份、调用点、任务队列和宿主资源。不要从最终输出反推过程；一旦出现分歧，保存首个偏离点再继续。

### 4. 按状态码分类

只执行当前最小动作，记录值、对象身份、调用点、任务队列和宿主资源。不要从最终输出反推过程；一旦出现分歧，保存首个偏离点再继续。

### 5. 解析并提交界面

只执行当前最小动作，记录值、对象身份、调用点、任务队列和宿主资源。不要从最终输出反推过程；一旦出现分歧，保存首个偏离点再继续。

### 6. 取消迟到请求

只执行当前最小动作，记录值、对象身份、调用点、任务队列和宿主资源。恢复后必须用完全相同的输入重放，并确认所有状态可预测、所有资源有明确归属。

## 边界记录模板

每次实验都建立一行六列证据：**输入快照、当前阶段、语言状态、宿主资源、首个偏离点、恢复结果**。语言核心章节要展开变量绑定、对象身份和调用栈；浏览器章节要增加节点连接状态、事件监听数量、网络请求与页面地址；Node.js 章节要增加请求流、响应是否结束、文件或数据库连接和事务状态。执行“构造请求契约”前先写预测，经过“等待响应头和正文”后只比较一项受控变化，到“取消迟到请求”时重放同一输入。若最终值相同但对象、任务、监听器、流或连接数量不同，仍判定失败；若异常被捕获但部分状态已经提交，也不能签发。这个模板把 API 记忆转换成可复查的因果链，并让正常、边界、失败和恢复样本可以逐列对照。

## 公开目录逐项讲解

### 第 10 章 AJAX 编程方法

**第 10 章 AJAX 编程方法** 的核心是：把网络通信组织为可取消、可分类、可恢复的数据流，而不是一次成功回调。先预测正常输入经过“构造请求契约”时会读取和修改哪些值，再运行最小样例，保存“AJAX”的实际状态。边界样本必须覆盖空值、重复输入、非法格式或迟到结果；失败样本只注入一个变量，并停在首个偏离点。最后清除故障、重放同一输入，确认输出、任务、监听器、连接和持久化状态都已收敛。

### 10.1 AJAX 编程基础

**10.1 AJAX 编程基础** 的核心是：从 XMLHttpRequest 的状态变化理解异步 HTTP，并对照 fetch 的 Promise 与流式响应模型。先预测正常输入经过“发送异步 HTTP”时会读取和修改哪些值，再运行最小样例，保存“XMLHttpRequest”的实际状态。边界样本必须覆盖空值、重复输入、非法格式或迟到结果；失败样本只注入一个变量，并停在首个偏离点。最后清除故障、重放同一输入，确认输出、任务、监听器、连接和持久化状态都已收敛。

### 10.2 不要重复发明轮子

**10.2 不要重复发明轮子** 的核心是：抽取稳定的请求适配层而非复制样板，统一错误、解析、取消和调用方契约。先预测正常输入经过“等待响应头和正文”时会读取和修改哪些值，再运行最小样例，保存“Fetch API”的实际状态。边界样本必须覆盖空值、重复输入、非法格式或迟到结果；失败样本只注入一个变量，并停在首个偏离点。最后清除故障、重放同一输入，确认输出、任务、监听器、连接和持久化状态都已收敛。

### 10.3 综合练习

**10.3 综合练习** 的核心是：实现 JSON 请求函数，注入 404、500、非法 JSON、超时和用户取消，验证界面不被迟到结果覆盖。先预测正常输入经过“按状态码分类”时会读取和修改哪些值，再运行最小样例，保存“请求取消”的实际状态。边界样本必须覆盖空值、重复输入、非法格式或迟到结果；失败样本只注入一个变量，并停在首个偏离点。最后清除故障、重放同一输入，确认输出、任务、监听器、连接和持久化状态都已收敛。

### 本章小结

**本章小结** 的核心是：用请求契约、网络状态、响应分类、解析结果和取消证据复盘 AJAX。先预测正常输入经过“解析并提交界面”时会读取和修改哪些值，再运行最小样例，保存“响应解析”的实际状态。边界样本必须覆盖空值、重复输入、非法格式或迟到结果；失败样本只注入一个变量，并停在首个偏离点。最后清除故障、重放同一输入，确认输出、任务、监听器、连接和持久化状态都已收敛。

## 最小可运行实验

```javascript
async function requestJson(url, { signal, ...init } = {}) {
  const response = await fetch(url, { ...init, signal });
  const text = await response.text();
  if (!response.ok) throw new Error("HTTP " + response.status + ": " + text);
  try {
    return text ? JSON.parse(text) : null;
  } catch (cause) {
    throw new Error("invalid JSON", { cause });
  }
}
```

```yaml
book: JavaScript 全栈开发
page: jfs-10-ajax-programming
public_toc_entries: 5
sample: normal | boundary | failure | recovery
first_divergence: null
language_state_explained: true
host_resources_explained: true
cleanup_complete: true
```

```text
freeze source + runtime + input
predict language values + host effects
inject exactly one controlled fault
stop at the first divergence
recover and replay the same input
assert stable state and no stale resources
```

## 常见误区与故障注入


## 四类样本与验收

| 样本 | 注入方式                         | 必查证据                         | 通过条件                   |
| ---- | -------------------------------- | -------------------------------- | -------------------------- |
| 正常 | 合法输入与单次执行               | 值、对象、调用点、任务、宿主资源 | 六阶段按预期完成           |
| 边界 | 空值、极值、重复输入、慢 I/O     | 类型与协议边界、停止条件         | 不发生隐式越界或重复提交   |
| 失败 | 错误参数、拒绝、断流、数据库失败 | 首个偏离点与错误通道             | 明确停止、传播或回滚       |
| 恢复 | 清除故障后重放相同输入           | 清理后的状态与资源数             | 无旧任务、监听器、流和连接 |

本页签发不变量是：**AJAX 在不重新导航整页的前提下发起 HTTP 请求并局部更新界面；可靠封装必须统一地址、方法、头、正文、超时、取消、状态码和解析错误。** 单次输出、截图或一次请求成功不能替代值语义、对象身份、控制流、任务顺序和宿主资源的完整轨迹。

## 练习


## 小结

- **AJAX**：对应“构造请求契约”阶段的核心观察量。
- **XMLHttpRequest**：对应“发送异步 HTTP”阶段的核心观察量。
- **Fetch API**：对应“等待响应头和正文”阶段的核心观察量。
- **请求取消**：对应“按状态码分类”阶段的核心观察量。
- **响应解析**：对应“解析并提交界面”阶段的核心观察量。
- 5 个公开目录条目已经全部映射到本页证据链。
- 正常、边界、失败和恢复样本共同决定是否通过，最终输出不是唯一证据。

## 术语表


## 版本与改编边界

本单元以凌杰著《JavaScript 全栈开发》为身份基线。公开目录来自人民邮电出版社出版电子版页面，创作原则由作者出版说明交叉确认；正文是独立教学重构，不复制原书段落。浏览器、Node.js 和数据库 API 的当前状态需另行按各自官方文档核验。

