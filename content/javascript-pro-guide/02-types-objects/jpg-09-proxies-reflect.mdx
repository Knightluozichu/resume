---
title: "第 9 章 代理与反射"
type: A
section: "JavaScript高级程序设计（第4版） · 类型与对象"
order: 9
description: "Proxy可以拦截对象内部操作，Reflect提供对应默认语义；捕获器必须保持规范不变式并避免递归。"
demo: true
math: false
sourceUrl: "https://uat.store.wiley.com/en-us/professional-javascript-for-web-developers-4th-edition-p-9781119366577"
draft: false
---


## 直觉：先画执行与所有权

Proxy可以拦截对象内部操作，Reflect提供对应默认语义；捕获器必须保持规范不变式并避免递归。 真正的难点不是让一次示例得到预期输出，而是让绑定、对象、任务、权限和资源在异常、取消、重复与版本切换后仍可解释。先预测：若只保存最终截图，最可能隐藏哪一种陈旧状态？

## 权威目录定位

本页依据 Wiley 第4版官方31页目录样章与中文版公开完整目录锁定范围。目录名称用于一对一追踪，正文为独立教学重构；新浏览器差异、安全与可观察性补充会明确披露：

- 9.1 代理基础
- 9.1.1 创建空代理
- 9.1.2 定义捕获器
- 9.1.3 捕获器参数和反射API
- 9.1.4 捕获器不变式
- 9.1.5 可撤销代理
- 9.1.6 实用反射API
- 9.1.7 代理另一个代理
- 9.1.8 代理的问题与不足
- 9.2 代理捕获器与反射方法
- 9.2.1 get()
- 9.2.2 set()
- 9.2.3 has()
- 9.2.4 defineProperty()
- 9.2.5 getOwnPropertyDescriptor()
- 9.2.6 deleteProperty()
- 9.2.7 ownKeys()
- 9.2.8 getPrototypeOf()
- 9.2.9 setPrototypeOf()
- 9.2.10 isExtensible()
- 9.2.11 preventExtensions()
- 9.2.12 apply()
- 9.2.13 construct()
- 9.3 代理模式
- 9.3.1 跟踪属性访问
- 9.3.2 隐藏属性
- 9.3.3 属性验证
- 9.3.4 函数与构造函数参数验证
- 9.3.5 数据绑定与可观察对象
- 9.4 小结

本页逐项覆盖 **30** 个公开目录条目，并纳入正常、边界、失败和恢复验收。

## 核心概念与所有权

、、、、。
每个概念都要回答：谁创建输入，状态保存在哪里，哪个任务或版本可修改它，结束后由谁清理。

|   # | 概念         | 工程含义                                                         |
| --: | ------------ | ---------------------------------------------------------------- |
|   1 | Proxy        | 以目标对象和处理器创建、可拦截对象内部操作的代理对象。           |
|   2 | 捕获器       | 处理器上对应get、set、ownKeys等内部操作的拦截方法。              |
|   3 | Reflect      | 提供与代理捕获器一一对应、返回更规则结果的反射方法集合。         |
|   4 | 捕获器不变式 | 代理结果必须与目标不可配置属性、可扩展状态等事实保持一致的约束。 |
|   5 | 可撤销代理   | 可通过revoke永久切断代理与目标访问能力的代理组合。               |

## 机制拆解

### 1. Reflect是正确默认路径

捕获器参数与Reflect同名方法对应，先调用Reflect保持目标默认语义，再叠加日志、验证或绑定。直接用target[prop]容易改变receiver和访问器this，从而破坏继承场景。

先预测：若省略这一层，哪个绑定、任务、权限或资源会先偏离？

### 2. 不变式限制虚构能力

不可配置且不可写属性不能被get捕获器报告为另一个值，目标不可扩展时ownKeys不能遗漏或新增键。代理不是任意伪造对象；违反不变式会由引擎抛出TypeError。

先预测：若省略这一层，哪个绑定、任务、权限或资源会先偏离？

### 3. 代理模式要控制身份与递归

代理不等于目标对象，严格相等和私有字段访问可能不同。在捕获器内操作代理自身会递归；元数据应放到外部WeakMap，并测试嵌套代理、撤销和错误堆栈。

先预测：若省略这一层，哪个绑定、任务、权限或资源会先偏离？

## 六阶段证据链

可操作链条是：确定拦截操作 → 创建目标代理 → 委托Reflect默认行为 → 加入验证跟踪 → 检查规范不变式 → 撤销与异常签发。正常样本证明能力，边界样本证明预算，失败样本证明拒绝，恢复样本证明任务、状态和资源重新收敛。


## 最小可运行实验

以下代码用于独立观察本页机制；生产实现还需加入输入验证、兼容策略、权限、取消与清理。

```js
const target = { count: 0 };
const proxy = new Proxy(target, {
  set(obj, key, value, receiver) {
    if (!Number.isFinite(value)) return false;
    return Reflect.set(obj, key, value, receiver);
  },
});
proxy.count = 2;
console.assert(target.count === 2);
```

证据记录把目录、运行时与状态摘要放在一起：

```yaml
book: JavaScript高级程序设计（第4版）
page: jpg-09-proxies-reflect
official_entries: 30
sample: normal | boundary | failure | recovery
browser_version: fixed
input_fixture: fixed
first_divergence: null
cleanup_complete: true
```

故障脚本一次只改变一个变量：

```text
baseline -> inject(exception | cancel | duplicate | denied | stale-cache)
observe -> stop_at_first_divergence
recover -> replay_same_runtime_and_input
assert -> one_state_owner && stable_output && no_stale_task && no_resource_leak
```

## 常见误区与故障注入


## 验收矩阵

| 样本 | 输入与环境变化                   | 预期状态       | 通过条件                     |
| ---- | -------------------------------- | -------------- | ---------------------------- |
| 正常 | 合法输入、权限、顺序与版本       | 六阶段完成     | 输出和状态摘要一致           |
| 边界 | 空值、极限容量、重复事件或慢任务 | 达到预算不越界 | 主线程与内存有界             |
| 失败 | 异常、取消、拒权、断网或旧产物   | 首偏离点停止   | 明确传播、回滚或降级         |
| 恢复 | 修复后重放同一输入               | 收敛到正常轨迹 | 无陈旧任务、监听器和资源泄漏 |

本页签发不变量是：**捕获器通过Reflect保留默认语义；所有规范不变式成立；代理身份、撤销和递归路径有测试。** 单次成功、平均耗时或最终截图不能替代运行时版本、执行轨迹、首偏离点与清理证据。

## 练习


## 小结

- **Proxy**：以目标对象和处理器创建、可拦截对象内部操作的代理对象。
- **捕获器**：处理器上对应get、set、ownKeys等内部操作的拦截方法。
- **Reflect**：提供与代理捕获器一一对应、返回更规则结果的反射方法集合。
- **捕获器不变式**：代理结果必须与目标不可配置属性、可扩展状态等事实保持一致的约束。
- **可撤销代理**：可通过revoke永久切断代理与目标访问能力的代理组合。
- 六阶段机制链已经映射到正常、边界、失败与恢复证据。
- 官方目录逐项追踪，现代补充不会冒充原书正式分节。

## 术语表


## 来源与改编边界

