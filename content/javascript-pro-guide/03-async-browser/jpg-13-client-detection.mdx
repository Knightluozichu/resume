---
title: "第 13 章 客户端检测"
type: A
section: "JavaScript高级程序设计（第4版） · 异步与浏览器"
order: 13
description: "客户端检测应先检测完成任务所需的能力，再在无法替代时有限使用用户代理与软硬件信息。"
demo: true
math: false
sourceUrl: "https://uat.store.wiley.com/en-us/professional-javascript-for-web-developers-4th-edition-p-9781119366577"
draft: false
---

import {
  Jpg13ClientDetectionMapLab,
  Jpg13ClientDetectionExperimentLab,
  Jpg13ClientDetectionEvidenceLab,
} from "@/components/mdx/javascript-pro-guide/diagrams/jpg-13-client-detection";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第 13 章 客户端检测的核心机制，并把它放回28章与4个附录的主线
- 能比较正常、边界、失败和恢复样本，指出首个执行或状态偏离点
- 能实现最小JavaScript实验，记录浏览器版本、输入、任务顺序与资源所有权
- 能设计证据回答“核心功能不依赖品牌判断；增强由直接能力和权限决定；检测结果不参与信任与安全裁决。”是否成立

</Objectives>

## 直觉：先画执行与所有权

客户端检测应先检测完成任务所需的能力，再在无法替代时有限使用用户代理与软硬件信息。 真正的难点不是让一次示例得到预期输出，而是让绑定、对象、任务、权限和资源在异常、取消、重复与版本切换后仍可解释。先预测：若只保存最终截图，最可能隐藏哪一种陈旧状态？

<Jpg13ClientDetectionMapLab />

## 权威目录定位

本页依据 Wiley 第4版官方31页目录样章与中文版公开完整目录锁定范围。目录名称用于一对一追踪，正文为独立教学重构；新浏览器差异、安全与可观察性补充会明确披露：

- 13.1 能力检测
- 13.1.1 安全能力检测
- 13.1.2 基于能力检测进行浏览器分析
- 13.2 用户代理检测
- 13.2.1 用户代理的历史
- 13.2.2 浏览器分析
- 13.3 软件与硬件检测
- 13.3.1 识别浏览器与操作系统
- 13.3.2 浏览器元数据
- 13.3.3 硬件
- 13.4 小结

本页逐项覆盖 **11** 个公开目录条目，并纳入正常、边界、失败和恢复验收。

## 核心概念与所有权

<Term def="直接判断目标对象、方法和所需语义是否存在并可用的策略。">能力检测</Term>、<Term def="解析浏览器自报身份字符串以推断产品与版本的策略。">用户代理检测</Term>、<Term def="由一个已检测特性推断另一个不相关特性存在的脆弱做法。">特性推断</Term>、<Term def="先提供普遍可用核心能力，再为支持环境增加增强体验的设计。">渐进增强</Term>、<Term def="确认API不仅存在，而且当前上下文和用户授权允许调用的过程。">权限检测</Term>。
每个概念都要回答：谁创建输入，状态保存在哪里，哪个任务或版本可修改它，结束后由谁清理。

|   # | 概念         | 工程含义                                                  |
| --: | ------------ | --------------------------------------------------------- |
|   1 | 能力检测     | 直接判断目标对象、方法和所需语义是否存在并可用的策略。    |
|   2 | 用户代理检测 | 解析浏览器自报身份字符串以推断产品与版本的策略。          |
|   3 | 特性推断     | 由一个已检测特性推断另一个不相关特性存在的脆弱做法。      |
|   4 | 渐进增强     | 先提供普遍可用核心能力，再为支持环境增加增强体验的设计。  |
|   5 | 权限检测     | 确认API不仅存在，而且当前上下文和用户授权允许调用的过程。 |

## 机制拆解

### 1. 检测任务而非浏览器

判断fetch存在比判断Chrome版本更接近需求，但仅存在仍不足以证明所需选项可用。对复杂能力可执行无副作用探针，或依赖经过维护的兼容层。

先预测：若省略这一层，哪个绑定、任务、权限或资源会先偏离？

### 2. UA只用于没有能力接口的差异

某些布局错误或特定引擎漏洞无法直接检测时，UA可作为最后手段，但规则必须窄、可过期且有遥测。客户端提示也会受隐私预算和服务端协商约束。

先预测：若省略这一层，哪个绑定、任务、权限或资源会先偏离？

### 3. 硬件信息不是容量承诺

CPU核心数、内存和网络类型可能被粗化，也不代表当前负载。性能策略应结合运行测量、自适应预算和用户偏好，而不是按设备型号永久分档。

先预测：若省略这一层，哪个绑定、任务、权限或资源会先偏离？

## 六阶段证据链

可操作链条是：定义目标任务 → 检测直接能力 → 验证方法语义 → 查询权限上下文 → 选择增强或降级 → 记录真实失败。正常样本证明能力，边界样本证明预算，失败样本证明拒绝，恢复样本证明任务、状态和资源重新收敛。

<Stepper>
  <Step title="1. 定义目标任务与检测直接能力">
    固定浏览器版本、输入、初始DOM与时钟，运行正常和边界样本。
    <Jpg13ClientDetectionMapLab />
  </Step>
  <Step title="2. 验证方法语义与查询权限上下文">
    注入异常、取消、重复事件、权限拒绝或旧缓存，要求在首偏离点停止。
    <Jpg13ClientDetectionExperimentLab />
  </Step>
  <Step title="3. 选择增强或降级与记录真实失败">
    清除故障后重放相同输入，核对输出、任务队列、DOM摘要、监听器与资源数量。
    <Jpg13ClientDetectionEvidenceLab />
  </Step>
</Stepper>

## 最小可运行实验

以下代码用于独立观察本页机制；生产实现还需加入输入验证、兼容策略、权限、取消与清理。

```js
const canShare = typeof navigator.share === "function";
if (canShare)
  await navigator.share({ title: document.title, url: location.href });
else await navigator.clipboard.writeText(location.href);
```

证据记录把目录、运行时与状态摘要放在一起：

```yaml
book: JavaScript高级程序设计（第4版）
page: jpg-13-client-detection
official_entries: 11
sample: normal | boundary | failure | recovery
browser_version: fixed
input_fixture: fixed
first_divergence: null
cleanup_complete: true
```

故障脚本一次只改变一个变量：

```text
baseline -> inject(exception | cancel | duplicate | denied | stale-cache)
observe -> stop_at_first_divergence
recover -> replay_same_runtime_and_input
assert -> one_state_owner && stable_output && no_stale_task && no_resource_leak
```

## 常见误区与故障注入

<Callout type="trap">
  **误区一：由支持一个API推断另一个同年代API也支持。**
  必须保存输入、任务顺序、状态摘要与首次拒绝原因，而不是只看最终页面。
</Callout>

<Callout type="trap">
  **误区二：把UA解析结果当安全或授权依据，客户端伪装即可绕过。**
  必须加入边界、失败与恢复样本，证明错误没有跨入下一阶段。
</Callout>

## 验收矩阵

| 样本 | 输入与环境变化                   | 预期状态       | 通过条件                     |
| ---- | -------------------------------- | -------------- | ---------------------------- |
| 正常 | 合法输入、权限、顺序与版本       | 六阶段完成     | 输出和状态摘要一致           |
| 边界 | 空值、极限容量、重复事件或慢任务 | 达到预算不越界 | 主线程与内存有界             |
| 失败 | 异常、取消、拒权、断网或旧产物   | 首偏离点停止   | 明确传播、回滚或降级         |
| 恢复 | 修复后重放同一输入               | 收敛到正常轨迹 | 无陈旧任务、监听器和资源泄漏 |

<Jpg13ClientDetectionEvidenceLab />

本页签发不变量是：**核心功能不依赖品牌判断；增强由直接能力和权限决定；检测结果不参与信任与安全裁决。** 单次成功、平均耗时或最终截图不能替代运行时版本、执行轨迹、首偏离点与清理证据。

## 练习

<Exercises>

**问题 1** 为什么不能只以最终页面结果正确作为验收？

<Answer>
  最终结果会隐藏重复副作用、旧任务覆盖、错误吞没和资源泄漏。必须比较输入、执行顺序、状态所有者、首偏离点与清理结果。
</Answer>

**问题 2** 如何把本页公开目录变成可执行学习证据？

<Answer>
  按公开顺序建立追踪表，为每组条目写出输入、状态、输出、失败与恢复条件。本页重点包括：13.1
  能力检测、13.1.1 安全能力检测、13.1.2 基于能力检测进行浏览器分析、13.2
  用户代理检测、13.2.1 用户代理的历史、13.2.2 浏览器分析、13.3
  软件与硬件检测、13.3.1 识别浏览器与操作系统；其余条目已在上方逐项列出。
</Answer>

**问题 3** 本页的最小通过条件是什么？

<Answer>
  核心功能不依赖品牌判断；增强由直接能力和权限决定；检测结果不参与信任与安全裁决。
  并且相同浏览器版本、输入与初始状态可以重放正常、边界、失败和恢复轨迹。
</Answer>

</Exercises>

## 小结

- **能力检测**：直接判断目标对象、方法和所需语义是否存在并可用的策略。
- **用户代理检测**：解析浏览器自报身份字符串以推断产品与版本的策略。
- **特性推断**：由一个已检测特性推断另一个不相关特性存在的脆弱做法。
- **渐进增强**：先提供普遍可用核心能力，再为支持环境增加增强体验的设计。
- **权限检测**：确认API不仅存在，而且当前上下文和用户授权允许调用的过程。
- 六阶段机制链已经映射到正常、边界、失败与恢复证据。
- 官方目录逐项追踪，现代补充不会冒充原书正式分节。

## 术语表

<Glossary>
  <GlossaryItem term="能力检测">
    直接判断目标对象、方法和所需语义是否存在并可用的策略。
  </GlossaryItem>
  <GlossaryItem term="用户代理检测">
    解析浏览器自报身份字符串以推断产品与版本的策略。
  </GlossaryItem>
  <GlossaryItem term="特性推断">
    由一个已检测特性推断另一个不相关特性存在的脆弱做法。
  </GlossaryItem>
  <GlossaryItem term="渐进增强">
    先提供普遍可用核心能力，再为支持环境增加增强体验的设计。
  </GlossaryItem>
  <GlossaryItem term="权限检测">
    确认API不仅存在，而且当前上下文和用户授权允许调用的过程。
  </GlossaryItem>
</Glossary>

## 来源与改编边界

<Attribution
  adaptedFrom="Matt Frisbie 著，李松峰译，人民邮电出版社，2020年9月，866页，ISBN 9787115545381；英文原版 Professional JavaScript for Web Developers, 4th Edition，Wiley，2019年10月，1200页，印刷版 ISBN 9781119366447、电子版 ISBN 9781119366577。Wiley官方31页目录样章与中文版公开目录确认28章、4个附录和757个二三级目录条目。正文为独立教学重构。"
  adaptedUrl="https://uat.store.wiley.com/en-us/professional-javascript-for-web-developers-4th-edition-p-9781119366577"
/>
