---
title: "第 22 章 处理XML"
type: A
section: "JavaScript高级程序设计（第4版） · 数据、网络与存储"
order: 22
description: "浏览器XML处理由DOMParser、XPath与XSLT等独立能力组成，解析错误、命名空间和不可信实体必须显式处理。"
demo: true
math: false
sourceUrl: "https://uat.store.wiley.com/en-us/professional-javascript-for-web-developers-4th-edition-p-9781119366577"
draft: false
---


## 直觉：先画执行与所有权

浏览器XML处理由DOMParser、XPath与XSLT等独立能力组成，解析错误、命名空间和不可信实体必须显式处理。 真正的难点不是让一次示例得到预期输出，而是让绑定、对象、任务、权限和资源在异常、取消、重复与版本切换后仍可解释。先预测：若只保存最终截图，最可能隐藏哪一种陈旧状态？

## 权威目录定位

本页依据 Wiley 第4版官方31页目录样章与中文版公开完整目录锁定范围。目录名称用于一对一追踪，正文为独立教学重构；新浏览器差异、安全与可观察性补充会明确披露：

- 22.1 浏览器对XML DOM的支持
- 22.1.1 DOM Level 2 Core
- 22.1.2 DOMParser类型
- 22.1.3 XMLSerializer类型
- 22.2 浏览器对XPath的支持
- 22.2.1 DOM Level 3 XPath
- 22.2.2 单个节点结果
- 22.2.3 简单类型结果
- 22.2.4 默认类型结果
- 22.2.5 命名空间支持
- 22.3 浏览器对XSLT的支持
- 22.3.1 XSLTProcessor类型
- 22.3.2 使用参数
- 22.3.3 重置处理器
- 22.4 小结

本页逐项覆盖 **15** 个公开目录条目，并纳入正常、边界、失败和恢复验收。

## 核心概念与所有权

、、、、。
每个概念都要回答：谁创建输入，状态保存在哪里，哪个任务或版本可修改它，结束后由谁清理。

|   # | 概念      | 工程含义                                            |
| --: | --------- | --------------------------------------------------- |
|   1 | XML DOM   | 按XML规则解析并以DOM节点树表示文档的模型。          |
|   2 | DOMParser | 把字符串解析为指定MIME类型文档的浏览器接口。        |
|   3 | XPath     | 用路径表达式在XML节点树中选择节点或计算值的语言。   |
|   4 | XSLT      | 以样式表规则把XML树转换为另一文档或文本的转换语言。 |
|   5 | 命名空间  | 以URI而非前缀标识XML元素和属性所属词汇的机制。      |

## 机制拆解

### 1. 解析成功要检查错误节点

DOMParser对XML语法错误通常返回包含parsererror的文档而不是抛异常。调用方要检查根元素和错误节点，并限制输入大小；HTML与XML解析模式对大小写、闭合和错误恢复完全不同。

先预测：若省略这一层，哪个绑定、任务、权限或资源会先偏离？

### 2. XPath按命名空间URI匹配

文档默认命名空间不会自动应用到XPath无前缀名称。提供前缀解析器，把查询前缀映射到URI；不能依赖源文档选用的具体前缀字符串。

先预测：若省略这一层，哪个绑定、任务、权限或资源会先偏离？

### 3. 转换输出仍是不可信内容

XSLT生成HTML后直接插入页面可能带来脚本或URL风险。优先转换为受控数据模型，若必须呈现则使用成熟清洗策略；外部实体和网络加载也应在服务端安全边界处理。

先预测：若省略这一层，哪个绑定、任务、权限或资源会先偏离？

## 六阶段证据链

可操作链条是：确认输入MIME → 解析XML文档 → 检测parsererror → 绑定命名空间 → 执行XPath或XSLT → 序列化并验证输出。正常样本证明能力，边界样本证明预算，失败样本证明拒绝，恢复样本证明任务、状态和资源重新收敛。


## 最小可运行实验

以下代码用于独立观察本页机制；生产实现还需加入输入验证、兼容策略、权限、取消与清理。

```js
const documentXml = new DOMParser().parseFromString(
  '<root><item id="1"/></root>',
  "application/xml",
);
if (documentXml.querySelector("parsererror"))
  throw new SyntaxError("invalid XML");
console.assert(documentXml.documentElement.localName === "root");
```

证据记录把目录、运行时与状态摘要放在一起：

```yaml
book: JavaScript高级程序设计（第4版）
page: jpg-22-working-with-xml
official_entries: 15
sample: normal | boundary | failure | recovery
browser_version: fixed
input_fixture: fixed
first_divergence: null
cleanup_complete: true
```

故障脚本一次只改变一个变量：

```text
baseline -> inject(exception | cancel | duplicate | denied | stale-cache)
observe -> stop_at_first_divergence
recover -> replay_same_runtime_and_input
assert -> one_state_owner && stable_output && no_stale_task && no_resource_leak
```

## 常见误区与故障注入


## 验收矩阵

| 样本 | 输入与环境变化                   | 预期状态       | 通过条件                     |
| ---- | -------------------------------- | -------------- | ---------------------------- |
| 正常 | 合法输入、权限、顺序与版本       | 六阶段完成     | 输出和状态摘要一致           |
| 边界 | 空值、极限容量、重复事件或慢任务 | 达到预算不越界 | 主线程与内存有界             |
| 失败 | 异常、取消、拒权、断网或旧产物   | 首偏离点停止   | 明确传播、回滚或降级         |
| 恢复 | 修复后重放同一输入               | 收敛到正常轨迹 | 无陈旧任务、监听器和资源泄漏 |

本页签发不变量是：**XML语法错误被明确拒绝；查询按URI处理命名空间；转换结果在进入HTML前经过可信边界。** 单次成功、平均耗时或最终截图不能替代运行时版本、执行轨迹、首偏离点与清理证据。

## 练习


## 小结

- **XML DOM**：按XML规则解析并以DOM节点树表示文档的模型。
- **DOMParser**：把字符串解析为指定MIME类型文档的浏览器接口。
- **XPath**：用路径表达式在XML节点树中选择节点或计算值的语言。
- **XSLT**：以样式表规则把XML树转换为另一文档或文本的转换语言。
- **命名空间**：以URI而非前缀标识XML元素和属性所属词汇的机制。
- 六阶段机制链已经映射到正常、边界、失败与恢复证据。
- 官方目录逐项追踪，现代补充不会冒充原书正式分节。

## 术语表


## 来源与改编边界

