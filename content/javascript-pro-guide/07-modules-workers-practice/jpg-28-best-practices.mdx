---
title: "第 28 章 最佳实践"
type: A
section: "JavaScript高级程序设计（第4版） · 模块、Worker与工程"
order: 28
description: "最佳实践要把可维护性、运行性能和可回滚部署变成可测量约束，而不是风格清单。"
demo: true
math: false
sourceUrl: "https://uat.store.wiley.com/en-us/professional-javascript-for-web-developers-4th-edition-p-9781119366577"
draft: true
---

import {
  Jpg28BestPracticesMapLab,
  Jpg28BestPracticesExperimentLab,
  Jpg28BestPracticesEvidenceLab,
} from "@/components/mdx/javascript-pro-guide/diagrams/jpg-28-best-practices";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第 28 章 最佳实践的核心机制，并把它放回28章与4个附录的主线
- 能比较正常、边界、失败和恢复样本，指出首个执行或状态偏离点
- 能实现最小JavaScript实验，记录浏览器版本、输入、任务顺序与资源所有权
- 能设计证据回答“代码边界可测试；优化由用户指标证明且不越预算；任一发布版本都是完整可恢复资源集合。”是否成立

</Objectives>

## 直觉：先画执行与所有权

最佳实践要把可维护性、运行性能和可回滚部署变成可测量约束，而不是风格清单。 真正的难点不是让一次示例得到预期输出，而是让绑定、对象、任务、权限和资源在异常、取消、重复与版本切换后仍可解释。先预测：若只保存最终截图，最可能隐藏哪一种陈旧状态？

<Jpg28BestPracticesMapLab />

## 权威目录定位

本页依据 Wiley 第4版官方31页目录样章与中文版公开完整目录锁定范围。目录名称用于一对一追踪，正文为独立教学重构；新浏览器差异、安全与可观察性补充会明确披露：

- 28.1 可维护性
- 28.1.1 什么是可维护的代码
- 28.1.2 编码规范
- 28.1.3 松散耦合
- 28.1.4 编码惯例
- 28.2 性能
- 28.2.1 作用域意识
- 28.2.2 选择正确的方法
- 28.2.3 语句最少化
- 28.2.4 优化DOM交互
- 28.3 部署
- 28.3.1 构建流程
- 28.3.2 验证
- 28.3.3 压缩
- 28.4 小结

本页逐项覆盖 **15** 个公开目录条目，并纳入正常、边界、失败和恢复验收。

## 核心概念与所有权

<Term def="代码在需求变化中仍易理解、测试、修改和审查的质量。">可维护性</Term>、<Term def="模块通过窄契约协作、内部变化不要求无关模块同步修改的结构。">松耦合</Term>、<Term def="对脚本体积、主线程耗时、内存和用户指标设定的可执行上限。">性能预算</Term>、<Term def="源码经过转换、打包、压缩与指纹命名后可部署的文件集合。">构建产物</Term>、<Term def="新版本失败时能恢复上一组完整兼容资源与配置的发布能力。">可回滚部署</Term>。
每个概念都要回答：谁创建输入，状态保存在哪里，哪个任务或版本可修改它，结束后由谁清理。

|   # | 概念       | 工程含义                                                   |
| --: | ---------- | ---------------------------------------------------------- |
|   1 | 可维护性   | 代码在需求变化中仍易理解、测试、修改和审查的质量。         |
|   2 | 松耦合     | 模块通过窄契约协作、内部变化不要求无关模块同步修改的结构。 |
|   3 | 性能预算   | 对脚本体积、主线程耗时、内存和用户指标设定的可执行上限。   |
|   4 | 构建产物   | 源码经过转换、打包、压缩与指纹命名后可部署的文件集合。     |
|   5 | 可回滚部署 | 新版本失败时能恢复上一组完整兼容资源与配置的发布能力。     |

## 机制拆解

### 1. 可维护性来自边界

命名、一致风格和注释有帮助，但更重要的是模块单一责任、显式输入输出、少用全局状态和可独立测试。事件、DOM和网络依赖通过适配边界进入核心逻辑。

先预测：若省略这一层，哪个绑定、任务、权限或资源会先偏离？

### 2. 先测量再优化

算法复杂度、DOM批处理、事件委托和惰性加载各有适用条件。以真实设备上的LCP、INP、长任务、内存和错误率建立基线，优化后比较分布并保留可读性成本。

先预测：若省略这一层，哪个绑定、任务、权限或资源会先偏离？

### 3. 部署是版本原子性问题

压缩与树摇前先保证语义测试，文件用内容哈希和长期缓存，HTML短缓存并只引用同一发布清单。灰度监控错误和性能，回滚恢复整套资源、服务工作者和配置。

先预测：若省略这一层，哪个绑定、任务、权限或资源会先偏离？

## 六阶段证据链

可操作链条是：建立代码契约 → 自动化静态与单元检查 → 测量用户性能 → 生成不可变产物 → 灰度发布观察 → 按证据回滚。正常样本证明能力，边界样本证明预算，失败样本证明拒绝，恢复样本证明任务、状态和资源重新收敛。

<Stepper>
  <Step title="1. 建立代码契约与自动化静态与单元检查">
    固定浏览器版本、输入、初始DOM与时钟，运行正常和边界样本。
    <Jpg28BestPracticesMapLab />
  </Step>
  <Step title="2. 测量用户性能与生成不可变产物">
    注入异常、取消、重复事件、权限拒绝或旧缓存，要求在首偏离点停止。
    <Jpg28BestPracticesExperimentLab />
  </Step>
  <Step title="3. 灰度发布观察与按证据回滚">
    清除故障后重放相同输入，核对输出、任务队列、DOM摘要、监听器与资源数量。
    <Jpg28BestPracticesEvidenceLab />
  </Step>
</Stepper>

## 最小可运行实验

以下代码用于独立观察本页机制；生产实现还需加入输入验证、兼容策略、权限、取消与清理。

```js
performance.mark("task:start");
runTask();
performance.mark("task:end");
performance.measure("task", "task:start", "task:end");
console.table(performance.getEntriesByName("task"));
```

证据记录把目录、运行时与状态摘要放在一起：

```yaml
book: JavaScript高级程序设计（第4版）
page: jpg-28-best-practices
official_entries: 15
sample: normal | boundary | failure | recovery
browser_version: fixed
input_fixture: fixed
first_divergence: null
cleanup_complete: true
```

故障脚本一次只改变一个变量：

```text
baseline -> inject(exception | cancel | duplicate | denied | stale-cache)
observe -> stop_at_first_divergence
recover -> replay_same_runtime_and_input
assert -> one_state_owner && stable_output && no_stale_task && no_resource_leak
```

## 常见误区与故障注入

<Callout type="trap">
  **误区一：为微小基准优化可读性，却没有真实用户指标改善。**
  必须保存输入、任务顺序、状态摘要与首次拒绝原因，而不是只看最终页面。
</Callout>

<Callout type="trap">
  **误区二：只回滚入口HTML，旧入口与新分块或Service Worker版本不兼容。**
  必须加入边界、失败与恢复样本，证明错误没有跨入下一阶段。
</Callout>

## 验收矩阵

| 样本 | 输入与环境变化                   | 预期状态       | 通过条件                     |
| ---- | -------------------------------- | -------------- | ---------------------------- |
| 正常 | 合法输入、权限、顺序与版本       | 六阶段完成     | 输出和状态摘要一致           |
| 边界 | 空值、极限容量、重复事件或慢任务 | 达到预算不越界 | 主线程与内存有界             |
| 失败 | 异常、取消、拒权、断网或旧产物   | 首偏离点停止   | 明确传播、回滚或降级         |
| 恢复 | 修复后重放同一输入               | 收敛到正常轨迹 | 无陈旧任务、监听器和资源泄漏 |

<Jpg28BestPracticesEvidenceLab />

本页签发不变量是：**代码边界可测试；优化由用户指标证明且不越预算；任一发布版本都是完整可恢复资源集合。** 单次成功、平均耗时或最终截图不能替代运行时版本、执行轨迹、首偏离点与清理证据。

## 练习

<Exercises>

**问题 1** 为什么不能只以最终页面结果正确作为验收？

<Answer>
  最终结果会隐藏重复副作用、旧任务覆盖、错误吞没和资源泄漏。必须比较输入、执行顺序、状态所有者、首偏离点与清理结果。
</Answer>

**问题 2** 如何把本页公开目录变成可执行学习证据？

<Answer>
  按公开顺序建立追踪表，为每组条目写出输入、状态、输出、失败与恢复条件。本页重点包括：28.1
  可维护性、28.1.1 什么是可维护的代码、28.1.2 编码规范、28.1.3 松散耦合、28.1.4
  编码惯例、28.2 性能、28.2.1 作用域意识、28.2.2
  选择正确的方法；其余条目已在上方逐项列出。
</Answer>

**问题 3** 本页的最小通过条件是什么？

<Answer>
  代码边界可测试；优化由用户指标证明且不越预算；任一发布版本都是完整可恢复资源集合。
  并且相同浏览器版本、输入与初始状态可以重放正常、边界、失败和恢复轨迹。
</Answer>

</Exercises>

## 小结

- **可维护性**：代码在需求变化中仍易理解、测试、修改和审查的质量。
- **松耦合**：模块通过窄契约协作、内部变化不要求无关模块同步修改的结构。
- **性能预算**：对脚本体积、主线程耗时、内存和用户指标设定的可执行上限。
- **构建产物**：源码经过转换、打包、压缩与指纹命名后可部署的文件集合。
- **可回滚部署**：新版本失败时能恢复上一组完整兼容资源与配置的发布能力。
- 六阶段机制链已经映射到正常、边界、失败与恢复证据。
- 官方目录逐项追踪，现代补充不会冒充原书正式分节。

## 术语表

<Glossary>
  <GlossaryItem term="可维护性">
    代码在需求变化中仍易理解、测试、修改和审查的质量。
  </GlossaryItem>
  <GlossaryItem term="松耦合">
    模块通过窄契约协作、内部变化不要求无关模块同步修改的结构。
  </GlossaryItem>
  <GlossaryItem term="性能预算">
    对脚本体积、主线程耗时、内存和用户指标设定的可执行上限。
  </GlossaryItem>
  <GlossaryItem term="构建产物">
    源码经过转换、打包、压缩与指纹命名后可部署的文件集合。
  </GlossaryItem>
  <GlossaryItem term="可回滚部署">
    新版本失败时能恢复上一组完整兼容资源与配置的发布能力。
  </GlossaryItem>
</Glossary>

## 来源与改编边界

<Attribution
  adaptedFrom="Matt Frisbie 著，李松峰译，人民邮电出版社，2020年9月，866页，ISBN 9787115545381；英文原版 Professional JavaScript for Web Developers, 4th Edition，Wiley，2019年10月，1200页，印刷版 ISBN 9781119366447、电子版 ISBN 9781119366577。Wiley官方31页目录样章与中文版公开目录确认28章、4个附录和757个二三级目录条目。正文为独立教学重构。"
  adaptedUrl="https://uat.store.wiley.com/en-us/professional-javascript-for-web-developers-4th-edition-p-9781119366577"
/>
