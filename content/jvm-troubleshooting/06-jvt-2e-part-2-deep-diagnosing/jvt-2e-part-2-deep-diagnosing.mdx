---
title: Part 2 深入诊断应用执行
description: 在普通调试不足时升级到采样、剖析、锁监测和线程转储，并始终保留工具开销与时间窗口。 覆盖1个正式节点，并用基线、故障、恢复和复位证据验收。
type: C
section: Troubleshooting Java（第2版 / 2025年10月版）
draft: false
math: true
sourceUrl: 'https://www.manning.com/books/troubleshooting-java-second-edition'
demo: true
order: 0
qualityVersion: 2
practiceMode: design
sourceMode: independent-rewrite
---
import {
  Jvt2ePart2DeepDiagnosingInvestigationLab,
  Jvt2ePart2DeepDiagnosingTimelineLab,
  Jvt2ePart2DeepDiagnosingEvidenceLab,
} from "@/components/mdx/jvm-troubleshooting/diagrams/jvt-2e-part-2-deep-diagnosing";
import { Objectives, Callout, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step, Attribution } from "@/components/mdx/mdx-components";

<Objectives>

- 能解释在普通调试不足时升级到采样、剖析、锁监测和线程转储，并始终保留工具开销与时间窗口。
- 能沿确认症状、选择采样、定位热点、抓取线程、交叉验证建立本章可证伪的调查链
- 能操作深度诊断升级门，只改变工具深度或窗口代表性中的一项
- 能注入“采集窗口没有覆盖故障，却根据最热方法给出优化结论”，用深度证据必须覆盖症状窗口并可与稳定基线比较判断修复、恢复和交接是否通过

</Objectives>

{/* JVT_QUALITY_V2 */}

## 为什么从“深度诊断升级门”开始

Part 2 深入诊断应用执行要解决的是在普通调试不足时升级到采样、剖析、锁监测和线程转储，并始终保留工具开销与时间窗口。 直觉上，最后出现的异常、最热的方法和最大的对象都只是信号；根因必须解释正常与故障时间线的第一个分叉，并能让原反例在最小修复后稳定转为通过。

先预测：把工具深度从“采样剖析”切换到“事件与转储”，热点解释度和误采样风险将怎样变化？运行前写下能推翻假设的观察，结果不符时修改假设而不是删除失败样本。

## 来源、版次与独立重写边界

Manning[官方产品页](https://www.manning.com/books/troubleshooting-java-second-edition)确认 Laurențiu Spilcă《Troubleshooting Java》第2版于2025年10月出版、360页、ISBN 9781633435575，并公开4个Part、13章、附录A–F的完整目录；课程映射24个正式单元、138个目录节点，另设学习地图与总复习。本页未取得原书正文，完整目录只用于限定单元和小节范围，不宣称正文忠实。

本页机制以[Oracle JDK 25故障排查指南](https://docs.oracle.com/en/java/javase/25/troubleshoot/index.html)、[jcmd手册](https://docs.oracle.com/en/java/javase/25/docs/specs/man/jcmd.html)、[JFR手册](https://docs.oracle.com/en/java/javase/25/docs/specs/man/jfr.html)、[VisualVM官方文档](https://visualvm.github.io/documentation.html)和[OpenTelemetry Java文档](https://opentelemetry.io/docs/languages/java/)中的适用部分独立核验。中文解释、命令组织、交互模型、练习与答案均为独立教学重写；工具文档许可证不被误报为原书授权。

## 本章术语与调查合同

<Term def="CPU样本是Part 2 深入诊断应用执行调查前需要冻结的症状、环境或时间坐标。">CPU样本</Term>、<Term def="分配样本连接确认症状与选择采样，改变时必须重新建立基线。">分配样本</Term>、<Term def="SQL调用是Part 2 深入诊断应用执行中的原始信号，不等同于已经证明的根因。">SQL调用</Term>、<Term def="锁等待用于定位热点解释度，同时要报告误采样风险。">锁等待</Term>、<Term def="线程转储限制工具深度和窗口代表性的解释范围。">线程转储</Term>、<Term def="探针成本进入修复回归与交接包，使未参与者可以独立重放。">探针成本</Term>。

Part 2 深入诊断应用执行必须守住“深度证据必须覆盖症状窗口并可与稳定基线比较”。工具深度与窗口代表性是离散调查坐标，热点解释度和误采样风险分开报告；交互中的数值只表达透明因果方向，不是假装采集了真实生产指标。

## 先预测，再操作三层章专属实验

<Stepper>
  <Step title="1. 目录节点与可证伪假设">
    选择正式节点，指出它位于确认症状、选择采样、定位热点、抓取线程、交叉验证的哪一步，以及哪项原始信号能推翻当前假设。

    <Jvt2ePart2DeepDiagnosingInvestigationLab />
  </Step>
  <Step title="2. 单变量时间线对照">
    一次只切换工具深度或窗口代表性，运行本章命令并比较基线与故障窗口。

    <Jvt2ePart2DeepDiagnosingTimelineLab />
  </Step>
  <Step title="3. 故障、恢复与复位">
    注入“采集窗口没有覆盖故障，却根据最热方法给出优化结论”，保存首个分叉；修复后使用同一负载重放，最后点击重置核对初值。

    <Jvt2ePart2DeepDiagnosingEvidenceLab />
  </Step>
</Stepper>

## 官方目录逐项深读

### Part 2 Deep diagnosing an app’s execution

**四级证据 1/1。** Part 2 Deep diagnosing an app’s execution：分部页负责声明相邻章节共用的升级条件、证据接口和不可跨越的前置门禁。本节点以“CPU样本”为焦点，运行`jcmd <pid> JFR.start duration=60s filename=incident.jfr`或等价探针，保存原始输出、时间窗、扰动预算和适用边界。

验证 Part 2 Deep diagnosing an app’s execution 时，先预测它在 确认症状 阶段怎样改变热点解释度；随后只调整工具深度或窗口代表性。若“采集窗口没有覆盖故障，却根据最热方法给出优化结论”没有制造预期首错，就撤回当前假设并检查替代解释。

## 最小调查切片

```bash
jcmd <pid> JFR.start name=incident settings=profile duration=60s filename=incident.jfr
```

Part 2 深入诊断应用执行运行该切片时要固定主机、容器、进程、JDK、应用commit、配置、负载与时间窗；所有日志、JFR、线程转储和堆转储都写入受控目录并登记大小、哈希、权限和删除期限。命令失败或超出扰动预算时立即停止，不把半份工件包装成完整证据。

```yaml
unit: jvt-2e-part-2-deep-diagnosing
command: "jcmd <pid> JFR.start duration=60s filename=incident.jfr"
baseline: frozen
fault: "采集窗口没有覆盖故障，却根据最热方法给出优化结论"
invariant: "深度证据必须覆盖症状窗口并可与稳定基线比较"
replay: same_input_same_window
```

## 三个必须主动触发的误区

<Callout type="trap" title="信号不等于根因">
  Part 2 深入诊断应用执行中的热点解释度只能缩小假设；必须用源码、竞争假设和反例解释为什么该信号出现。
</Callout>

<Callout type="trap" title="调查动作改变现场">
  采集窗口没有覆盖故障，却根据最热方法给出优化结论。保存工具配置和扰动预算，必要时退回低侵入采样，并在未附加工具的运行中确认首错。
</Callout>

<Callout type="trap" title="修复后只看告警消失">
  Part 2 深入诊断应用执行的修复要重放原负载、原故障和正常基线；资源、线程、数据与外部副作用都恢复，才满足深度证据必须覆盖症状窗口并可与稳定基线比较。
</Callout>

## 练习、答案与节点验证

<Exercises>

**问题 1：假设与单变量。** 怎样验证工具深度而不让窗口代表性混入因果结论？

<Answer>
  固定应用commit、JDK、负载、时间窗和窗口代表性，只把工具深度从“采样剖析”切到“事件与转储”；保存热点解释度、误采样风险和首个分叉。
</Answer>

**问题 2：逐节点四级证据。** 正式目录节点怎样从出现升级为解释、交互和练习验证？

<Answer>
1. Part 2 Deep diagnosing an app’s execution：用CPU样本定位观察点，运行`jcmd <pid> JFR.start duration=60s filename=incident.jfr`或同义探针，补齐基线、故障、恢复和复位证据。
</Answer>

**问题 3：恢复闭环。** 注入“采集窗口没有覆盖故障，却根据最热方法给出优化结论”后如何证明根因和修复？

<Answer>
  沿确认症状、选择采样、定位热点、抓取线程、交叉验证定位首错，只改变最小因果前提；随后用相同输入重放正常、故障、恢复和复位四条轨迹。只有深度证据必须覆盖症状窗口并可与稳定基线比较再次成立，调查才通过。
</Answer>

</Exercises>

## 术语复核与本章回顾

<Glossary>
  <GlossaryItem term="CPU样本">CPU样本是Part 2 深入诊断应用执行调查前需要冻结的症状、环境或时间坐标。</GlossaryItem>
  <GlossaryItem term="分配样本">分配样本连接确认症状与选择采样，改变时必须重新建立基线。</GlossaryItem>
  <GlossaryItem term="SQL调用">SQL调用是Part 2 深入诊断应用执行中的原始信号，不等同于已经证明的根因。</GlossaryItem>
  <GlossaryItem term="锁等待">锁等待用于定位热点解释度，同时要报告误采样风险。</GlossaryItem>
  <GlossaryItem term="线程转储">线程转储限制工具深度和窗口代表性的解释范围。</GlossaryItem>
  <GlossaryItem term="探针成本">探针成本进入修复回归与交接包，使未参与者可以独立重放。</GlossaryItem>
</Glossary>

完成Part 2 深入诊断应用执行意味着能说明在普通调试不足时升级到采样、剖析、锁监测和线程转储，并始终保留工具开销与时间窗口。，能运行本章调查切片，能主动制造“采集窗口没有覆盖故障，却根据最热方法给出优化结论”，还能用原始信号和反例证明修复而非只展示工具截图。

<Attribution
  mode="independent-rewrite"
  sourceBasis="outline-only"
  workTitle="Laurențiu Spilcă, Troubleshooting Java, Second Edition"
  adaptedUrl="https://www.manning.com/books/troubleshooting-java-second-edition"
/>
