---
title: "第7章 调查多线程架构中的锁"
description: "依据Laurențiu Spilcă《Troubleshooting Java》第2版官方完整目录独立重构：区分锁拥有者、进入等待、条件等待和正常空闲，沿资源依赖解释阻塞而不把所有WAITING视作故障"
type: C
section: "Troubleshooting Java（第2版 / 2025年10月版）"
draft: false
math: true
sourceUrl: "https://www.manning.com/books/troubleshooting-java-second-edition"
demo: true
order: 0
---

import { Jvt2InvestigationLab, Jvt2TimelineLab, Jvt2EvidenceLab } from "@/components/mdx/jvm-troubleshooting/diagrams/jvt-2e-07-thread-locks";

# 第7章 调查多线程架构中的锁

本页对应官方结构中的 **7 Investigating locks in multithreaded architectures**。目标不是复制原文，而是独立还原其调查方法与核心知识，并让每个结论都能由另一位读者重放、质疑和推翻。当前任务是：区分锁拥有者、进入等待、条件等待和正常空闲，沿资源依赖解释阻塞而不把所有WAITING视作故障。

<Callout type="info">**版本与来源边界。** 本课程锁定Laurențiu Spilcă《Troubleshooting Java, Second Edition》，Manning，2025年10月，360页，ISBN 9781633435575。官方产品页确认书目信息，Manning官方预览目录确认4个Part、13章与附录A–F。课程文本、示例和图示为独立教学重构，不冒充原书原文；工具与JVM行为需以运行时版本和一手文档再次核对。</Callout>

## 可验收学习目标

<Objectives>
- 能解释本页全部5个正式目录节点在故障调查中的责任、前置关系与证据边界。
- 能实现一个包含基线、故障注入、原始信号和停止条件的最小可重复实验。
- 能比较代码阅读、调试、日志、剖析、转储与跨服务证据，按扰动和风险选择工具。
- 能设计反例、回滚与交接档案，使未参与调查的人能够重放并挑战结论。
</Objectives>

预期能力是“能从线程状态和锁关系识别竞争热点、等待原因与最小修复边界”。本页交付 **线程状态序列、锁拥有关系图、等待原因分类、竞争前后吞吐对照**；首要陷阱是“只截取单份快照、忽略线程池队列与外部I/O，或盲目扩大锁范围造成吞吐下降”。

## 官方目录逐节点复刻

### 7 Investigating locks in multithreaded architectures

**正式节点 1/5。** 连续采集线程状态、栈和锁拥有关系，构建等待图并区分正常等待、竞争、死锁、活锁与饥饿；修复后在相同压力和停止条件下重放。本节点服务于“区分锁拥有者、进入等待、条件等待和正常空闲，沿资源依赖解释阻塞而不把所有WAITING视作故障”，验收物写入线程状态序列、锁拥有关系图、等待原因分类、竞争前后吞吐对照。

先预测最可能出现的反例，再执行一次基线与一次故障对照。结论必须注明适用工作负载、JDK与工具版本、采集时刻和已排除假设；如果只能复述命令或工具截图，就尚未覆盖“7 Investigating locks in multithreaded architectures”的核心责任。

### 7.1 Monitoring threads for locks

**正式节点 2/5。** 连续采集线程状态、栈和锁拥有关系，构建等待图并区分正常等待、竞争、死锁、活锁与饥饿；修复后在相同压力和停止条件下重放。本节点服务于“区分锁拥有者、进入等待、条件等待和正常空闲，沿资源依赖解释阻塞而不把所有WAITING视作故障”，验收物写入线程状态序列、锁拥有关系图、等待原因分类、竞争前后吞吐对照。

先预测最可能出现的反例，再执行一次基线与一次故障对照。结论必须注明适用工作负载、JDK与工具版本、采集时刻和已排除假设；如果只能复述命令或工具截图，就尚未覆盖“7.1 Monitoring threads for locks”的核心责任。

### 7.2 Analyzing thread locks

**正式节点 3/5。** 连续采集线程状态、栈和锁拥有关系，构建等待图并区分正常等待、竞争、死锁、活锁与饥饿；修复后在相同压力和停止条件下重放。本节点服务于“区分锁拥有者、进入等待、条件等待和正常空闲，沿资源依赖解释阻塞而不把所有WAITING视作故障”，验收物写入线程状态序列、锁拥有关系图、等待原因分类、竞争前后吞吐对照。

先预测最可能出现的反例，再执行一次基线与一次故障对照。结论必须注明适用工作负载、JDK与工具版本、采集时刻和已排除假设；如果只能复述命令或工具截图，就尚未覆盖“7.2 Analyzing thread locks”的核心责任。

### 7.3 Analyzing waiting threads

**正式节点 4/5。** 连续采集线程状态、栈和锁拥有关系，构建等待图并区分正常等待、竞争、死锁、活锁与饥饿；修复后在相同压力和停止条件下重放。本节点服务于“区分锁拥有者、进入等待、条件等待和正常空闲，沿资源依赖解释阻塞而不把所有WAITING视作故障”，验收物写入线程状态序列、锁拥有关系图、等待原因分类、竞争前后吞吐对照。

先预测最可能出现的反例，再执行一次基线与一次故障对照。结论必须注明适用工作负载、JDK与工具版本、采集时刻和已排除假设；如果只能复述命令或工具截图，就尚未覆盖“7.3 Analyzing waiting threads”的核心责任。

### Summary

**正式节点 5/5。** 把该节点作为能力检查点：说明输入、适用范围、失败边界、最小实验和交付证据，并能指出它与前后正式单元的依赖关系。本节点服务于“区分锁拥有者、进入等待、条件等待和正常空闲，沿资源依赖解释阻塞而不把所有WAITING视作故障”，验收物写入线程状态序列、锁拥有关系图、等待原因分类、竞争前后吞吐对照。

先预测最可能出现的反例，再执行一次基线与一次故障对照。结论必须注明适用工作负载、JDK与工具版本、采集时刻和已排除假设；如果只能复述命令或工具截图，就尚未覆盖“Summary”的核心责任。

## 从故障现象开始

可靠排障不是从工具菜单开始，而是从可观察差异开始。先写下期望、实际、首次出现时间、影响范围、可复现输入和最近变化，再把事实与解释分开。<Term def="对一次故障的症状、时间线、环境、假设、原始证据和决策进行不可变记录的集合">调查档案</Term>保存未经加工的输入；<Term def="在相同工作量和环境下、故障尚未出现时的可比较运行记录">基线</Term>提供对照。没有基线，CPU、堆、暂停或延迟的单个数字几乎没有诊断意义。

每个候选原因都写成可证伪假设：若原因成立，应观察到什么；若不成立，哪项证据会排除它；采集这项证据会不会暂停线程、改变调度、耗尽磁盘或暴露业务数据。<Term def="观测工具改变被观测程序时序、资源消耗或状态的程度">探针效应</Term>必须进入结论。断点会暂停，插桩会增加调用成本，转储可能触发停顿，详细日志会增加I/O；因此一次只增加一个探针，并保留无探针对照。

调查顺序遵循低成本、低扰动、高判别力。配置、版本、输入和变更记录通常先于调试器；结构化日志与指标先于高开销剖析；线程或堆转储只在能回答明确问题时采集。所谓<Term def="能够让至少两个候选原因产生不同预期结果的观察或实验">判别性证据</Term>，不是“收集得更多”，而是能缩小假设空间。工具的热点、警告和AI解释都是线索，不能替代因果实验。

本页使用统一的证据状态机：事实已保全、假设已提出、判别实验已设计、基线已执行、故障已重放、反例已检查、修复已验证、恢复已演练。<Term def="预先声明何时停止采集、接受或拒绝假设、升级风险控制的条件">停止条件</Term>防止无限调查。任何无法说明输入、时间窗、版本和工作量的截图，都只能作为导航提示，不能作为最终证据。

AI辅助必须置于信任边界之外。不要上传令牌、客户数据、完整堆转储、私有源码或可识别日志；先脱敏并最小化上下文。要求模型给出互斥假设、所需证据与可推翻条件，然后回到源码、运行结果或一手文档逐项验证。AI擅长扩展候选空间，不拥有现场事实，也不能代替授权、变更评审与生产操作责任。

## 证据链与因果时间线

时间线至少记录单调时钟持续时间、墙上时钟、线程或服务、业务键、关联ID、输入摘要、状态变化和外部副作用。跨服务时钟可能偏移，所以不能只按时间戳推断先后；应结合span父子关系、消息偏移、数据库提交与审计序号。对于超时，明确调用方究竟知道什么：请求未返回不等于操作未发生，自动重试前必须有业务幂等边界。

性能结论按工作量归一化。将端到端耗时拆成排队、应用计算、同步等待、运行时暂停和序列化成本；CPU高可能表示有效工作，也可能是重试或自旋，CPU低也可能因为线程在锁、网络或数据库上等待。内存同样要区分分配率、当前占用、Full GC后存活集与进程外本地内存。一个指标必须同时注明单位、聚合窗口和标签基数。

根因定义为能解释症状、时间线和反例，并在移除或控制后使故障按预测消失的最小必要条件。相关变化不自动成为根因；“重启恢复”只证明状态被清空。修复验收必须在相同输入、负载、环境和观察窗口重放，检查副作用、资源释放及恢复路径。最后把预防控制绑定到测试、监控、容量、发布或操作规程的明确所有者。

证据保全还要做到可定位与可撤销：原始文件记录校验和、采集命令、操作者、权限和时区，派生图表记录查询与过滤条件。结论复核时从原始数据重新生成图表，并由未参与首次调查的人抽查一项被排除的假设。若原始证据已经过期、缺失或无法证明来源，就降低结论置信度并重新采集，不能用事后整理的摘要补写现场事实。

## 最小可复现实验

以下代码展示调查记录的最小边界；实际项目应把敏感值替换为不可逆摘要，并把时钟、工作量和版本一并保存。

```java
record Evidence(String hypothesis, String workload, String signal,
                String expected, String actual, boolean falsified) {}

var plan = synchronized (ledgerLock) { applyInvariant(command); }
```

实验运行器把基线和故障场景分开，禁止在结果出现后偷偷修改成功标准：

```java
var baseline = run(caseFile, Scenario.BASELINE, fixedWorkload);
var failure = run(caseFile, Scenario.CONTROLLED_FAILURE, fixedWorkload);
assertEquals(baseline.environmentFingerprint(), failure.environmentFingerprint());
assertTrue(failure.rawSignalsRetained());
```

交接时只描述可观察合同，不绑定某个剖析器界面：

```gherkin
Feature: 7 Investigating locks in multithreaded architectures
  Scenario: falsify a troubleshooting hypothesis
    Given a versioned environment, fixed workload, baseline, and stop condition
    When 用两个负载级别重复采集线程状态，确认同一锁等待是否持续增长且与延迟相关
    Then raw evidence distinguishes the competing hypotheses
    And remediation, rollback, and recovery can be replayed independently
```

## 定量判断与误差边界

端到端耗时先按可观察阶段相加，避免把所有延迟归咎于采样排名第一的方法：

$$
L_{e2e}=L_{queue}+L_{application}+L_{io}+L_{runtime}+L_{serialize}
$$

资源热点应同时考虑每次成本与执行次数。一次很慢但极少发生的方法，可能不如每次很快却被意外调用百万次的方法重要：

$$
C_{total}(m)=N(m)	imes ar{C}(m)
$$

内存泄漏判断关注相同工作量下Full GC后的存活集趋势，而不是某一时刻堆占用：

$$
g=rac{Live_{after}(t_2)-Live_{after}(t_1)}{Work(t_2)-Work(t_1)}
$$

分布式重试预算必须受端到端截止时间和尝试次数约束；每层独立重试会乘法放大请求：

$$
A_{max}=prod_{i=1}^{n} r_i,qquad T_{attempt}leq T_{deadline}-T_{elapsed}
$$

这些公式是建立问题的工具，不是自动诊断器。每次计算都要写明采样窗口、预热、并发、数据规模、JDK、机器资源和误差来源，并用至少三次重复运行观察分布而非只报平均值。

## 三步调查实验

<Steps>
  <Step title="1. 建立假设地图">
    从“区分锁拥有者、进入等待、条件等待和正常空闲，沿资源依赖解释阻塞而不把所有WAITING视作故障”提取两个互斥候选原因，为每个原因写出支持信号、反例、采集开销和停止条件。先预测哪项证据最有判别力。
    <Jvt2InvestigationLab />
  </Step>
  <Step title="2. 重放时间线">
    固定版本、工作量和输入，执行基线后再执行“用两个负载级别重复采集线程状态，确认同一锁等待是否持续增长且与延迟相关”。一次只改变一个变量，保存原始输出而非只有截图。
    <Jvt2TimelineLab />
  </Step>
  <Step title="3. 通过证据门">
    用线程状态序列、锁拥有关系图、等待原因分类、竞争前后吞吐对照检查结论能否解释成功、边界、故障与恢复四条路径；缺少反例或回滚时不得关闭调查。
    <Jvt2EvidenceLab />
  </Step>
</Steps>

<Callout type="trap">**陷阱：工具结论替代问题定义。** “热点第一”“锁很多”“堆很大”都只是界面排序。若未固定工作量、时间窗和版本，也未写出竞争假设，就不能从排名跳到根因。</Callout>

<Callout type="trap">**陷阱：修复后不重放。** 参数修改、加锁、加缓存或重启可能让症状暂时消失，却引入吞吐、饥饿、陈旧数据或容量风险。必须用原始触发条件和恢复条件复测。</Callout>

<Callout type="warning">**安全边界。** 日志、线程转储、堆转储和追踪可能包含密钥、个人数据与业务载荷。采集前最小授权，传输与静态加密，限制保留和导出，完成后留下销毁审计。</Callout>

## 本章回顾

本页的最小闭环是：由症状建立基线，把“区分锁拥有者、进入等待、条件等待和正常空闲，沿资源依赖解释阻塞而不把所有WAITING视作故障”拆成竞争假设，以最低扰动证据运行“用两个负载级别重复采集线程状态，确认同一锁等待是否持续增长且与延迟相关”，再用线程状态序列、锁拥有关系图、等待原因分类、竞争前后吞吐对照证明结论、修复和恢复均可重放。真正的掌握标准是能从线程状态和锁关系识别竞争热点、等待原因与最小修复边界；若仍落入“只截取单份快照、忽略线程池队列与外部I/O，或盲目扩大锁范围造成吞吐下降”，应返回官方节点和反例实验，而不是继续叠加工具。

<Exercises>

**问题 1**：如何为第7章 调查多线程架构中的锁建立一个可推翻的最小调查？

<Answer>先固定版本、输入、工作量、期望和实际，提出两个互斥假设；执行“用两个负载级别重复采集线程状态，确认同一锁等待是否持续增长且与延迟相关”，保存基线与故障的原始证据，并预先声明支持、证伪和停止条件。</Answer>

**问题 2**：为什么“只截取单份快照、忽略线程池队列与外部I/O，或盲目扩大锁范围造成吞吐下降”会破坏结论？

<Answer>因为它跳过了可比基线、判别性证据或风险边界。应回到线程状态序列、锁拥有关系图、等待原因分类、竞争前后吞吐对照，检查事实与解释是否分开、探针效应是否记录、反例能否执行，以及修复后是否在相同条件重放。</Answer>

**问题 3**：本页达到可交接标准至少需要哪些证据？

<Answer>至少需要版本化环境、固定工作量、症状时间线、竞争假设、原始信号、故障对照、可推翻条件、修复前后结果、回滚阈值和恢复演练；另一位读者应能仅凭档案重复调查。</Answer>

</Exercises>

<Glossary>
  <GlossaryItem term="调查档案">对症状、时间线、环境、假设、原始证据和决策进行不可变记录的集合。</GlossaryItem>
  <GlossaryItem term="基线">在相同工作量和环境下、故障尚未出现时的可比较运行记录。</GlossaryItem>
  <GlossaryItem term="探针效应">观测工具改变被观测程序时序、资源消耗或状态的程度。</GlossaryItem>
  <GlossaryItem term="判别性证据">能够让至少两个候选原因产生不同预期结果的观察或实验。</GlossaryItem>
  <GlossaryItem term="停止条件">预先声明何时停止采集、接受或拒绝假设、升级风险控制的条件。</GlossaryItem>
</Glossary>

[← 上一页：第6章 用剖析发现隐藏问题](/learn/jvm-troubleshooting/08-jvt-2e-06-hidden-profiling/jvt-2e-06-hidden-profiling) · [下一页：第8章 用线程转储调查死锁 →](/learn/jvm-troubleshooting/10-jvt-2e-08-deadlocks-thread-dumps/jvt-2e-08-deadlocks-thread-dumps)

<Attribution adaptedFrom="Laurențiu Spilcă, Troubleshooting Java, Second Edition" adaptedUrl="https://www.manning.com/books/troubleshooting-java-second-edition" />
