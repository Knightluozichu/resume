---
title: 第8章 用线程转储调查死锁
description: 正确生成、阅读并用工具交叉检查线程转储，依据锁循环而不是线程数量诊断死锁。 覆盖8个正式节点，并用基线、故障、恢复和复位证据验收。
type: C
section: Troubleshooting Java（第2版 / 2025年10月版）
draft: false
math: true
sourceUrl: 'https://www.manning.com/books/troubleshooting-java-second-edition'
demo: true
order: 0
qualityVersion: 2
practiceMode: simulation
sourceMode: independent-rewrite
---


{/* JVT_QUALITY_V2 */}

## 为什么从“线程转储锁环重建台”开始

第8章 用线程转储调查死锁要解决的是正确生成、阅读并用工具交叉检查线程转储，依据锁循环而不是线程数量诊断死锁。 直觉上，最后出现的异常、最热的方法和最大的对象都只是信号；根因必须解释正常与故障时间线的第一个分叉，并能让原反例在最小修复后稳定转为通过。

先预测：把转储来源从“jcmd”切换到“容器信号”，锁环还原度和死锁误报率将怎样变化？运行前写下能推翻假设的观察，结果不符时修改假设而不是删除失败样本。

## 来源、版次与独立重写边界

Manning[官方产品页](https://www.manning.com/books/troubleshooting-java-second-edition)确认 Laurențiu Spilcă《Troubleshooting Java》第2版于2025年10月出版、360页、ISBN 9781633435575，并公开4个Part、13章、附录A–F的完整目录；课程映射24个正式单元、138个目录节点，另设学习地图与总复习。本页未取得原书正文，完整目录只用于限定单元和小节范围，不宣称正文忠实。

本页机制以[Oracle JDK 25故障排查指南](https://docs.oracle.com/en/java/javase/25/troubleshoot/index.html)、[jcmd手册](https://docs.oracle.com/en/java/javase/25/docs/specs/man/jcmd.html)、[JFR手册](https://docs.oracle.com/en/java/javase/25/docs/specs/man/jfr.html)、[VisualVM官方文档](https://visualvm.github.io/documentation.html)和[OpenTelemetry Java文档](https://opentelemetry.io/docs/languages/java/)中的适用部分独立核验。中文解释、命令组织、交互模型、练习与答案均为独立教学重写；工具文档许可证不被误报为原书授权。

## 本章术语与调查合同

、、、、、。

第8章 用线程转储调查死锁必须守住“死锁结论包含至少一个闭合等待环及各线程持有/请求关系”。转储来源与分析方法是离散调查坐标，锁环还原度和死锁误报率分开报告；交互中的数值只表达透明因果方向，不是假装采集了真实生产指标。

## 官方目录逐项深读

### 8 Investigating deadlocks with thread dumps

**四级证据 1/8。** 8 Investigating deadlocks with thread dumps：死锁由闭合等待环使参与者永久无法推进，线程转储可重建锁环。本节点以“jcmd Thread.print”为焦点，运行`jcmd <pid> Thread.print -l`或等价探针，保存原始输出、时间窗、扰动预算和适用边界。

验证 8 Investigating deadlocks with thread dumps 时，先预测它在 选择时刻 阶段怎样改变锁环还原度；随后只调整转储来源或分析方法。若“只凭大量BLOCKED线程宣称死锁，没有形成线程—锁—线程的闭合等待环”没有制造预期首错，就撤回当前假设并检查替代解释。

### 8.1 Getting a thread dump

**四级证据 2/8。** 8.1 Getting a thread dump：线程转储是全进程线程栈和锁状态快照，文件名应包含主机、PID和时间。本节点以“plain text”为焦点，运行`jcmd <pid> Thread.print -l`或等价探针，保存原始输出、时间窗、扰动预算和适用边界。

验证 8.1 Getting a thread dump 时，先预测它在 生成转储 阶段怎样改变锁环还原度；随后只调整转储来源或分析方法。若“只凭大量BLOCKED线程宣称死锁，没有形成线程—锁—线程的闭合等待环”没有制造预期首错，就撤回当前假设并检查替代解释。

### 8.1.1 Getting a thread dump using a profiler

**四级证据 3/8。** 8.1.1 Getting a thread dump using a profiler：启用剖析前要记录附加方式、事件配置、持续时间、输出位置和性能预算。本节点以“locked”为焦点，运行`jcmd <pid> Thread.print -l`或等价探针，保存原始输出、时间窗、扰动预算和适用边界。

验证 8.1.1 Getting a thread dump using a profiler 时，先预测它在 解析栈帧 阶段怎样改变锁环还原度；随后只调整转储来源或分析方法。若“只凭大量BLOCKED线程宣称死锁，没有形成线程—锁—线程的闭合等待环”没有制造预期首错，就撤回当前假设并检查替代解释。

### 8.1.2 Generating a thread dump from the command line

**四级证据 4/8。** 8.1.2 Generating a thread dump from the command line：jcmd PID Thread.print -l 可生成带锁信息的转储，命令身份和退出状态要记录。本节点以“waiting to lock”为焦点，运行`jcmd <pid> Thread.print -l`或等价探针，保存原始输出、时间窗、扰动预算和适用边界。

验证 8.1.2 Generating a thread dump from the command line 时，先预测它在 构建锁环 阶段怎样改变锁环还原度；随后只调整转储来源或分析方法。若“只凭大量BLOCKED线程宣称死锁，没有形成线程—锁—线程的闭合等待环”没有制造预期首错，就撤回当前假设并检查替代解释。

### 8.2 Reading thread dumps

**四级证据 5/8。** 8.2 Reading thread dumps：阅读从线程名、状态、栈顶阻塞点、锁拥有与等待关系开始，而不是按文件长度判断。本节点以“deadlock cycle”为焦点，运行`jcmd <pid> Thread.print -l`或等价探针，保存原始输出、时间窗、扰动预算和适用边界。

验证 8.2 Reading thread dumps 时，先预测它在 验证修复 阶段怎样改变锁环还原度；随后只调整转储来源或分析方法。若“只凭大量BLOCKED线程宣称死锁，没有形成线程—锁—线程的闭合等待环”没有制造预期首错，就撤回当前假设并检查替代解释。

### 8.2.1 Reading plain-text thread dumps

**四级证据 6/8。** 8.2.1 Reading plain-text thread dumps：纯文本中的locked与waiting to lock能重建等待图，连续快照可区分暂态与持续环。本节点以“连续转储”为焦点，运行`jcmd <pid> Thread.print -l`或等价探针，保存原始输出、时间窗、扰动预算和适用边界。

验证 8.2.1 Reading plain-text thread dumps 时，先预测它在 选择时刻 阶段怎样改变锁环还原度；随后只调整转储来源或分析方法。若“只凭大量BLOCKED线程宣称死锁，没有形成线程—锁—线程的闭合等待环”没有制造预期首错，就撤回当前假设并检查替代解释。

### 8.2.2 Using tools to better grasp thread dumps

**四级证据 7/8。** 8.2.2 Using tools to better grasp thread dumps：分析工具可以聚类相同栈和标出死锁，但结论要回查原始锁标识和栈帧。本节点以“jcmd Thread.print”为焦点，运行`jcmd <pid> Thread.print -l`或等价探针，保存原始输出、时间窗、扰动预算和适用边界。

验证 8.2.2 Using tools to better grasp thread dumps 时，先预测它在 生成转储 阶段怎样改变锁环还原度；随后只调整转储来源或分析方法。若“只凭大量BLOCKED线程宣称死锁，没有形成线程—锁—线程的闭合等待环”没有制造预期首错，就撤回当前假设并检查替代解释。

### Summary

**四级证据 8/8。** Summary：小结应回到本章的不变量、首错信号、反例与恢复条件，而不是重复工具名称。本节点以“plain text”为焦点，运行`jcmd <pid> Thread.print -l`或等价探针，保存原始输出、时间窗、扰动预算和适用边界。

验证 Summary 时，先预测它在 解析栈帧 阶段怎样改变锁环还原度；随后只调整转储来源或分析方法。若“只凭大量BLOCKED线程宣称死锁，没有形成线程—锁—线程的闭合等待环”没有制造预期首错，就撤回当前假设并检查替代解释。

## 最小调查切片

```bash
jcmd <pid> Thread.print -l > deadlock.txt
rg 'Found one Java-level deadlock|waiting to lock|locked' deadlock.txt
```

第8章 用线程转储调查死锁运行该切片时要固定主机、容器、进程、JDK、应用commit、配置、负载与时间窗；所有日志、JFR、线程转储和堆转储都写入受控目录并登记大小、哈希、权限和删除期限。命令失败或超出扰动预算时立即停止，不把半份工件包装成完整证据。

```yaml
unit: jvt-2e-08-deadlocks-thread-dumps
command: "jcmd <pid> Thread.print -l"
baseline: frozen
fault: "只凭大量BLOCKED线程宣称死锁，没有形成线程—锁—线程的闭合等待环"
invariant: "死锁结论包含至少一个闭合等待环及各线程持有/请求关系"
replay: same_input_same_window
```

## 三个必须主动触发的误区


## 练习、答案与节点验证


## 术语复核与本章回顾


完成第8章 用线程转储调查死锁意味着能说明正确生成、阅读并用工具交叉检查线程转储，依据锁循环而不是线程数量诊断死锁。，能运行本章调查切片，能主动制造“只凭大量BLOCKED线程宣称死锁，没有形成线程—锁—线程的闭合等待环”，还能用原始信号和反例证明修复而非只展示工具截图。

