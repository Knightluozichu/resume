---
title: "第4章 Kafka消费者——从Kafka读取数据"
description: "依据O'Reilly与中文版第2版完整目录：沿消费者群组、订阅、轮询、分区分配、再均衡、偏移量提交、反序列化和退出协议建立可证明消费语义"
type: C
section: "Kafka权威指南（第2版 / 2022）"
draft: true
math: false
sourceUrl: "https://www.oreilly.com/library/view/kafkaquan-wei-zhi-nan-di-2ban/9787115601421/"
demo: true
order: 0
---

# 第4章 Kafka消费者——从Kafka读取数据

本课程对应Gwen Shapira、Todd Palino、Rajini Sivaram、Krit Petty著《Kafka权威指南》第2版，薛命灯译，人民邮电出版社2022年11月出版，318页，ISBN 9787115601421；英文第2版由O'Reilly于2021年出版。课程严格使用第2版14章与附录A、B的目录边界，不把第1版旧目录、后来Kafka版本的实现或厂商功能冒充原书内容。

本页对应一个正式单元，逐项覆盖39个目录节点。主问题是“沿消费者群组、订阅、轮询、分区分配、再均衡、偏移量提交、反序列化和退出协议建立可证明消费语义”，最终交付为群组分配图、轮询与提交时序、再均衡故障实验、偏移量对账与退出检查表，由另一位工程师更换消息键、负载和故障点重放。

<Objectives>

- 能解释“第4章 Kafka消费者——从Kafka读取数据”的39个目录节点，并绘制它们之间的数据面、控制面与持久化边界。
- 能比较正常、压力、再均衡、broker故障和跨网络故障轨迹，判断确认、偏移量、ISR和业务结果是否一致。
- 能设计并复现群组分配图、轮询与提交时序、再均衡故障实验、偏移量对账与退出检查表，用分位延迟、吞吐、滞后、日志与消息ID对账支持结论。
- 能分析“开启自动提交后假定消息已经处理完成，或在慢处理期间忽略max.poll.interval.ms导致分区被回收”为何失败，并写出停止、恢复、回退与责任交接条件。

</Objectives>

## 从一条记录的生命周期开始

先预测：带键、值、标头和时间戳的记录进入生产者后落到哪个分区，何时组成批次，哪个broker确认，副本何时进入或离开ISR，消费者何时处理并提交下一偏移量。随后再预测broker重启、网络延迟、再均衡或事务中止时，最后可见记录和业务结果应是什么。观察结果之前必须留下预测。

Kafka的高吞吐来自顺序追加、批处理、压缩、页缓存和分区并行；可靠性来自复制、确认、幂等或事务与消费提交的组合。任何调参都可能同时改变延迟、吞吐、资源和交付语义，本页不接受只报告单一峰值。

## 核心词汇与版本边界

<Term def="消费者群组是“沿消费者群组、订阅、轮询、分区分配、再均衡、偏移量提交、反序列化和退出协议建立可证明消费语义”中的第1个核心概念，定义必须同时给出状态、边界、失败语义和可观测证据">消费者群组</Term>、<Term def="再均衡是“沿消费者群组、订阅、轮询、分区分配、再均衡、偏移量提交、反序列化和退出协议建立可证明消费语义”中的第2个核心概念，定义必须同时给出状态、边界、失败语义和可观测证据">再均衡</Term>、<Term def="轮询是“沿消费者群组、订阅、轮询、分区分配、再均衡、偏移量提交、反序列化和退出协议建立可证明消费语义”中的第3个核心概念，定义必须同时给出状态、边界、失败语义和可观测证据">轮询</Term>、<Term def="偏移量提交是“沿消费者群组、订阅、轮询、分区分配、再均衡、偏移量提交、反序列化和退出协议建立可证明消费语义”中的第4个核心概念，定义必须同时给出状态、边界、失败语义和可观测证据">偏移量提交</Term>、<Term def="反序列化器是“沿消费者群组、订阅、轮询、分区分配、再均衡、偏移量提交、反序列化和退出协议建立可证明消费语义”中的第5个核心概念，定义必须同时给出状态、边界、失败语义和可观测证据">反序列化器</Term>
构成本页词汇。每个词都要回答：它属于客户端还是broker，作用于主题、分区、副本或消费者位置中的哪一层，何时创建与推进，超时、重试、故障或版本变化后留下什么证据。

<Kfk04KafkaConsumersTopologyLab />

## 原书目录逐节点重构

### Kafka消费者相关概念

**目录节点 1/39。** “Kafka消费者相关概念”服务于“沿消费者群组、订阅、轮询、分区分配、再均衡、偏移量提交、反序列化和退出协议建立可证明消费语义”。先说明它接收的状态、配置或记录，再画出跨客户端、broker、分区、副本、磁盘或外部系统的边界；结论必须能够从配置、指标、日志、偏移量和消息样本中复核。

机制轨迹从“本章输入与契约”进入“Kafka消费者相关概念”，再到“消费者和消费者群组”。逐步记录请求所属线程或进程、读取与写入的状态、确认发生的位置、超时与重试分支，以及失败后由谁接管。尤其区分“请求已发出”“broker已确认”“消费者已处理”和“业务结果已对账”，这些不是同一个完成点。

实验固定主题、分区键、消息ID和负载，只改变acks、批次、消费者数量、故障时点或网络条件之一。比较吞吐、P95/P99、消费者滞后、ISR、磁盘和最终消息集合，用“每个分区在一个群组内同一时刻只有一个有效消费者，处理完成点与提交偏移量关系明确，再均衡和崩溃后可界定重复或丢失窗口”判定；若证据冲突，回到本节点缩小故障窗口，不用平均值掩盖尾部与丢失。

#### 消费者和消费者群组

**目录节点 2/39。** “消费者和消费者群组”服务于“沿消费者群组、订阅、轮询、分区分配、再均衡、偏移量提交、反序列化和退出协议建立可证明消费语义”。先说明它接收的状态、配置或记录，再画出跨客户端、broker、分区、副本、磁盘或外部系统的边界；结论必须能够从配置、指标、日志、偏移量和消息样本中复核。

机制轨迹从“Kafka消费者相关概念”进入“消费者和消费者群组”，再到“消费者群组和分区再均衡”。逐步记录请求所属线程或进程、读取与写入的状态、确认发生的位置、超时与重试分支，以及失败后由谁接管。尤其区分“请求已发出”“broker已确认”“消费者已处理”和“业务结果已对账”，这些不是同一个完成点。

实验固定主题、分区键、消息ID和负载，只改变acks、批次、消费者数量、故障时点或网络条件之一。比较吞吐、P95/P99、消费者滞后、ISR、磁盘和最终消息集合，用“每个分区在一个群组内同一时刻只有一个有效消费者，处理完成点与提交偏移量关系明确，再均衡和崩溃后可界定重复或丢失窗口”判定；若证据冲突，回到本节点缩小故障窗口，不用平均值掩盖尾部与丢失。

#### 消费者群组和分区再均衡

**目录节点 3/39。** “消费者群组和分区再均衡”服务于“沿消费者群组、订阅、轮询、分区分配、再均衡、偏移量提交、反序列化和退出协议建立可证明消费语义”。先说明它接收的状态、配置或记录，再画出跨客户端、broker、分区、副本、磁盘或外部系统的边界；结论必须能够从配置、指标、日志、偏移量和消息样本中复核。

机制轨迹从“消费者和消费者群组”进入“消费者群组和分区再均衡”，再到“群组固定成员”。逐步记录请求所属线程或进程、读取与写入的状态、确认发生的位置、超时与重试分支，以及失败后由谁接管。尤其区分“请求已发出”“broker已确认”“消费者已处理”和“业务结果已对账”，这些不是同一个完成点。

实验固定主题、分区键、消息ID和负载，只改变acks、批次、消费者数量、故障时点或网络条件之一。比较吞吐、P95/P99、消费者滞后、ISR、磁盘和最终消息集合，用“每个分区在一个群组内同一时刻只有一个有效消费者，处理完成点与提交偏移量关系明确，再均衡和崩溃后可界定重复或丢失窗口”判定；若证据冲突，回到本节点缩小故障窗口，不用平均值掩盖尾部与丢失。

#### 群组固定成员

**目录节点 4/39。** “群组固定成员”服务于“沿消费者群组、订阅、轮询、分区分配、再均衡、偏移量提交、反序列化和退出协议建立可证明消费语义”。先说明它接收的状态、配置或记录，再画出跨客户端、broker、分区、副本、磁盘或外部系统的边界；结论必须能够从配置、指标、日志、偏移量和消息样本中复核。

机制轨迹从“消费者群组和分区再均衡”进入“群组固定成员”，再到“创建Kafka消费者”。逐步记录请求所属线程或进程、读取与写入的状态、确认发生的位置、超时与重试分支，以及失败后由谁接管。尤其区分“请求已发出”“broker已确认”“消费者已处理”和“业务结果已对账”，这些不是同一个完成点。

实验固定主题、分区键、消息ID和负载，只改变acks、批次、消费者数量、故障时点或网络条件之一。比较吞吐、P95/P99、消费者滞后、ISR、磁盘和最终消息集合，用“每个分区在一个群组内同一时刻只有一个有效消费者，处理完成点与提交偏移量关系明确，再均衡和崩溃后可界定重复或丢失窗口”判定；若证据冲突，回到本节点缩小故障窗口，不用平均值掩盖尾部与丢失。

### 创建Kafka消费者

**目录节点 5/39。** “创建Kafka消费者”服务于“沿消费者群组、订阅、轮询、分区分配、再均衡、偏移量提交、反序列化和退出协议建立可证明消费语义”。先说明它接收的状态、配置或记录，再画出跨客户端、broker、分区、副本、磁盘或外部系统的边界；结论必须能够从配置、指标、日志、偏移量和消息样本中复核。

机制轨迹从“群组固定成员”进入“创建Kafka消费者”，再到“订阅主题”。逐步记录请求所属线程或进程、读取与写入的状态、确认发生的位置、超时与重试分支，以及失败后由谁接管。尤其区分“请求已发出”“broker已确认”“消费者已处理”和“业务结果已对账”，这些不是同一个完成点。

实验固定主题、分区键、消息ID和负载，只改变acks、批次、消费者数量、故障时点或网络条件之一。比较吞吐、P95/P99、消费者滞后、ISR、磁盘和最终消息集合，用“每个分区在一个群组内同一时刻只有一个有效消费者，处理完成点与提交偏移量关系明确，再均衡和崩溃后可界定重复或丢失窗口”判定；若证据冲突，回到本节点缩小故障窗口，不用平均值掩盖尾部与丢失。

### 订阅主题

**目录节点 6/39。** “订阅主题”服务于“沿消费者群组、订阅、轮询、分区分配、再均衡、偏移量提交、反序列化和退出协议建立可证明消费语义”。先说明它接收的状态、配置或记录，再画出跨客户端、broker、分区、副本、磁盘或外部系统的边界；结论必须能够从配置、指标、日志、偏移量和消息样本中复核。

机制轨迹从“创建Kafka消费者”进入“订阅主题”，再到“轮询”。逐步记录请求所属线程或进程、读取与写入的状态、确认发生的位置、超时与重试分支，以及失败后由谁接管。尤其区分“请求已发出”“broker已确认”“消费者已处理”和“业务结果已对账”，这些不是同一个完成点。

实验固定主题、分区键、消息ID和负载，只改变acks、批次、消费者数量、故障时点或网络条件之一。比较吞吐、P95/P99、消费者滞后、ISR、磁盘和最终消息集合，用“每个分区在一个群组内同一时刻只有一个有效消费者，处理完成点与提交偏移量关系明确，再均衡和崩溃后可界定重复或丢失窗口”判定；若证据冲突，回到本节点缩小故障窗口，不用平均值掩盖尾部与丢失。

### 轮询

**目录节点 7/39。** “轮询”服务于“沿消费者群组、订阅、轮询、分区分配、再均衡、偏移量提交、反序列化和退出协议建立可证明消费语义”。先说明它接收的状态、配置或记录，再画出跨客户端、broker、分区、副本、磁盘或外部系统的边界；结论必须能够从配置、指标、日志、偏移量和消息样本中复核。

机制轨迹从“订阅主题”进入“轮询”，再到“配置消费者”。逐步记录请求所属线程或进程、读取与写入的状态、确认发生的位置、超时与重试分支，以及失败后由谁接管。尤其区分“请求已发出”“broker已确认”“消费者已处理”和“业务结果已对账”，这些不是同一个完成点。

实验固定主题、分区键、消息ID和负载，只改变acks、批次、消费者数量、故障时点或网络条件之一。比较吞吐、P95/P99、消费者滞后、ISR、磁盘和最终消息集合，用“每个分区在一个群组内同一时刻只有一个有效消费者，处理完成点与提交偏移量关系明确，再均衡和崩溃后可界定重复或丢失窗口”判定；若证据冲突，回到本节点缩小故障窗口，不用平均值掩盖尾部与丢失。

### 配置消费者

**目录节点 8/39。** “配置消费者”服务于“沿消费者群组、订阅、轮询、分区分配、再均衡、偏移量提交、反序列化和退出协议建立可证明消费语义”。先说明它接收的状态、配置或记录，再画出跨客户端、broker、分区、副本、磁盘或外部系统的边界；结论必须能够从配置、指标、日志、偏移量和消息样本中复核。

机制轨迹从“轮询”进入“配置消费者”，再到“fetch.min.bytes”。逐步记录请求所属线程或进程、读取与写入的状态、确认发生的位置、超时与重试分支，以及失败后由谁接管。尤其区分“请求已发出”“broker已确认”“消费者已处理”和“业务结果已对账”，这些不是同一个完成点。

实验固定主题、分区键、消息ID和负载，只改变acks、批次、消费者数量、故障时点或网络条件之一。比较吞吐、P95/P99、消费者滞后、ISR、磁盘和最终消息集合，用“每个分区在一个群组内同一时刻只有一个有效消费者，处理完成点与提交偏移量关系明确，再均衡和崩溃后可界定重复或丢失窗口”判定；若证据冲突，回到本节点缩小故障窗口，不用平均值掩盖尾部与丢失。

#### fetch.min.bytes

**目录节点 9/39。** “fetch.min.bytes”服务于“沿消费者群组、订阅、轮询、分区分配、再均衡、偏移量提交、反序列化和退出协议建立可证明消费语义”。先说明它接收的状态、配置或记录，再画出跨客户端、broker、分区、副本、磁盘或外部系统的边界；结论必须能够从配置、指标、日志、偏移量和消息样本中复核。

机制轨迹从“配置消费者”进入“fetch.min.bytes”，再到“fetch.max.wait.ms”。逐步记录请求所属线程或进程、读取与写入的状态、确认发生的位置、超时与重试分支，以及失败后由谁接管。尤其区分“请求已发出”“broker已确认”“消费者已处理”和“业务结果已对账”，这些不是同一个完成点。

实验固定主题、分区键、消息ID和负载，只改变acks、批次、消费者数量、故障时点或网络条件之一。比较吞吐、P95/P99、消费者滞后、ISR、磁盘和最终消息集合，用“每个分区在一个群组内同一时刻只有一个有效消费者，处理完成点与提交偏移量关系明确，再均衡和崩溃后可界定重复或丢失窗口”判定；若证据冲突，回到本节点缩小故障窗口，不用平均值掩盖尾部与丢失。

#### fetch.max.wait.ms

**目录节点 10/39。** “fetch.max.wait.ms”服务于“沿消费者群组、订阅、轮询、分区分配、再均衡、偏移量提交、反序列化和退出协议建立可证明消费语义”。先说明它接收的状态、配置或记录，再画出跨客户端、broker、分区、副本、磁盘或外部系统的边界；结论必须能够从配置、指标、日志、偏移量和消息样本中复核。

机制轨迹从“fetch.min.bytes”进入“fetch.max.wait.ms”，再到“fetch.max.bytes”。逐步记录请求所属线程或进程、读取与写入的状态、确认发生的位置、超时与重试分支，以及失败后由谁接管。尤其区分“请求已发出”“broker已确认”“消费者已处理”和“业务结果已对账”，这些不是同一个完成点。

实验固定主题、分区键、消息ID和负载，只改变acks、批次、消费者数量、故障时点或网络条件之一。比较吞吐、P95/P99、消费者滞后、ISR、磁盘和最终消息集合，用“每个分区在一个群组内同一时刻只有一个有效消费者，处理完成点与提交偏移量关系明确，再均衡和崩溃后可界定重复或丢失窗口”判定；若证据冲突，回到本节点缩小故障窗口，不用平均值掩盖尾部与丢失。

#### fetch.max.bytes

**目录节点 11/39。** “fetch.max.bytes”服务于“沿消费者群组、订阅、轮询、分区分配、再均衡、偏移量提交、反序列化和退出协议建立可证明消费语义”。先说明它接收的状态、配置或记录，再画出跨客户端、broker、分区、副本、磁盘或外部系统的边界；结论必须能够从配置、指标、日志、偏移量和消息样本中复核。

机制轨迹从“fetch.max.wait.ms”进入“fetch.max.bytes”，再到“max.poll.records”。逐步记录请求所属线程或进程、读取与写入的状态、确认发生的位置、超时与重试分支，以及失败后由谁接管。尤其区分“请求已发出”“broker已确认”“消费者已处理”和“业务结果已对账”，这些不是同一个完成点。

实验固定主题、分区键、消息ID和负载，只改变acks、批次、消费者数量、故障时点或网络条件之一。比较吞吐、P95/P99、消费者滞后、ISR、磁盘和最终消息集合，用“每个分区在一个群组内同一时刻只有一个有效消费者，处理完成点与提交偏移量关系明确，再均衡和崩溃后可界定重复或丢失窗口”判定；若证据冲突，回到本节点缩小故障窗口，不用平均值掩盖尾部与丢失。

#### max.poll.records

**目录节点 12/39。** “max.poll.records”服务于“沿消费者群组、订阅、轮询、分区分配、再均衡、偏移量提交、反序列化和退出协议建立可证明消费语义”。先说明它接收的状态、配置或记录，再画出跨客户端、broker、分区、副本、磁盘或外部系统的边界；结论必须能够从配置、指标、日志、偏移量和消息样本中复核。

机制轨迹从“fetch.max.bytes”进入“max.poll.records”，再到“max.partition.fetch.bytes”。逐步记录请求所属线程或进程、读取与写入的状态、确认发生的位置、超时与重试分支，以及失败后由谁接管。尤其区分“请求已发出”“broker已确认”“消费者已处理”和“业务结果已对账”，这些不是同一个完成点。

实验固定主题、分区键、消息ID和负载，只改变acks、批次、消费者数量、故障时点或网络条件之一。比较吞吐、P95/P99、消费者滞后、ISR、磁盘和最终消息集合，用“每个分区在一个群组内同一时刻只有一个有效消费者，处理完成点与提交偏移量关系明确，再均衡和崩溃后可界定重复或丢失窗口”判定；若证据冲突，回到本节点缩小故障窗口，不用平均值掩盖尾部与丢失。

#### max.partition.fetch.bytes

**目录节点 13/39。** “max.partition.fetch.bytes”服务于“沿消费者群组、订阅、轮询、分区分配、再均衡、偏移量提交、反序列化和退出协议建立可证明消费语义”。先说明它接收的状态、配置或记录，再画出跨客户端、broker、分区、副本、磁盘或外部系统的边界；结论必须能够从配置、指标、日志、偏移量和消息样本中复核。

机制轨迹从“max.poll.records”进入“max.partition.fetch.bytes”，再到“session.timeout.ms和heartbeat.interval.ms”。逐步记录请求所属线程或进程、读取与写入的状态、确认发生的位置、超时与重试分支，以及失败后由谁接管。尤其区分“请求已发出”“broker已确认”“消费者已处理”和“业务结果已对账”，这些不是同一个完成点。

实验固定主题、分区键、消息ID和负载，只改变acks、批次、消费者数量、故障时点或网络条件之一。比较吞吐、P95/P99、消费者滞后、ISR、磁盘和最终消息集合，用“每个分区在一个群组内同一时刻只有一个有效消费者，处理完成点与提交偏移量关系明确，再均衡和崩溃后可界定重复或丢失窗口”判定；若证据冲突，回到本节点缩小故障窗口，不用平均值掩盖尾部与丢失。

#### session.timeout.ms和heartbeat.interval.ms

**目录节点 14/39。** “session.timeout.ms和heartbeat.interval.ms”服务于“沿消费者群组、订阅、轮询、分区分配、再均衡、偏移量提交、反序列化和退出协议建立可证明消费语义”。先说明它接收的状态、配置或记录，再画出跨客户端、broker、分区、副本、磁盘或外部系统的边界；结论必须能够从配置、指标、日志、偏移量和消息样本中复核。

机制轨迹从“max.partition.fetch.bytes”进入“session.timeout.ms和heartbeat.interval.ms”，再到“max.poll.interval.ms”。逐步记录请求所属线程或进程、读取与写入的状态、确认发生的位置、超时与重试分支，以及失败后由谁接管。尤其区分“请求已发出”“broker已确认”“消费者已处理”和“业务结果已对账”，这些不是同一个完成点。

实验固定主题、分区键、消息ID和负载，只改变acks、批次、消费者数量、故障时点或网络条件之一。比较吞吐、P95/P99、消费者滞后、ISR、磁盘和最终消息集合，用“每个分区在一个群组内同一时刻只有一个有效消费者，处理完成点与提交偏移量关系明确，再均衡和崩溃后可界定重复或丢失窗口”判定；若证据冲突，回到本节点缩小故障窗口，不用平均值掩盖尾部与丢失。

#### max.poll.interval.ms

**目录节点 15/39。** “max.poll.interval.ms”服务于“沿消费者群组、订阅、轮询、分区分配、再均衡、偏移量提交、反序列化和退出协议建立可证明消费语义”。先说明它接收的状态、配置或记录，再画出跨客户端、broker、分区、副本、磁盘或外部系统的边界；结论必须能够从配置、指标、日志、偏移量和消息样本中复核。

机制轨迹从“session.timeout.ms和heartbeat.interval.ms”进入“max.poll.interval.ms”，再到“default.api.timeout.ms”。逐步记录请求所属线程或进程、读取与写入的状态、确认发生的位置、超时与重试分支，以及失败后由谁接管。尤其区分“请求已发出”“broker已确认”“消费者已处理”和“业务结果已对账”，这些不是同一个完成点。

实验固定主题、分区键、消息ID和负载，只改变acks、批次、消费者数量、故障时点或网络条件之一。比较吞吐、P95/P99、消费者滞后、ISR、磁盘和最终消息集合，用“每个分区在一个群组内同一时刻只有一个有效消费者，处理完成点与提交偏移量关系明确，再均衡和崩溃后可界定重复或丢失窗口”判定；若证据冲突，回到本节点缩小故障窗口，不用平均值掩盖尾部与丢失。

#### default.api.timeout.ms

**目录节点 16/39。** “default.api.timeout.ms”服务于“沿消费者群组、订阅、轮询、分区分配、再均衡、偏移量提交、反序列化和退出协议建立可证明消费语义”。先说明它接收的状态、配置或记录，再画出跨客户端、broker、分区、副本、磁盘或外部系统的边界；结论必须能够从配置、指标、日志、偏移量和消息样本中复核。

机制轨迹从“max.poll.interval.ms”进入“default.api.timeout.ms”，再到“request.timeout.ms”。逐步记录请求所属线程或进程、读取与写入的状态、确认发生的位置、超时与重试分支，以及失败后由谁接管。尤其区分“请求已发出”“broker已确认”“消费者已处理”和“业务结果已对账”，这些不是同一个完成点。

实验固定主题、分区键、消息ID和负载，只改变acks、批次、消费者数量、故障时点或网络条件之一。比较吞吐、P95/P99、消费者滞后、ISR、磁盘和最终消息集合，用“每个分区在一个群组内同一时刻只有一个有效消费者，处理完成点与提交偏移量关系明确，再均衡和崩溃后可界定重复或丢失窗口”判定；若证据冲突，回到本节点缩小故障窗口，不用平均值掩盖尾部与丢失。

#### request.timeout.ms

**目录节点 17/39。** “request.timeout.ms”服务于“沿消费者群组、订阅、轮询、分区分配、再均衡、偏移量提交、反序列化和退出协议建立可证明消费语义”。先说明它接收的状态、配置或记录，再画出跨客户端、broker、分区、副本、磁盘或外部系统的边界；结论必须能够从配置、指标、日志、偏移量和消息样本中复核。

机制轨迹从“default.api.timeout.ms”进入“request.timeout.ms”，再到“auto.offset.reset”。逐步记录请求所属线程或进程、读取与写入的状态、确认发生的位置、超时与重试分支，以及失败后由谁接管。尤其区分“请求已发出”“broker已确认”“消费者已处理”和“业务结果已对账”，这些不是同一个完成点。

实验固定主题、分区键、消息ID和负载，只改变acks、批次、消费者数量、故障时点或网络条件之一。比较吞吐、P95/P99、消费者滞后、ISR、磁盘和最终消息集合，用“每个分区在一个群组内同一时刻只有一个有效消费者，处理完成点与提交偏移量关系明确，再均衡和崩溃后可界定重复或丢失窗口”判定；若证据冲突，回到本节点缩小故障窗口，不用平均值掩盖尾部与丢失。

#### auto.offset.reset

**目录节点 18/39。** “auto.offset.reset”服务于“沿消费者群组、订阅、轮询、分区分配、再均衡、偏移量提交、反序列化和退出协议建立可证明消费语义”。先说明它接收的状态、配置或记录，再画出跨客户端、broker、分区、副本、磁盘或外部系统的边界；结论必须能够从配置、指标、日志、偏移量和消息样本中复核。

机制轨迹从“request.timeout.ms”进入“auto.offset.reset”，再到“enable.auto.commit”。逐步记录请求所属线程或进程、读取与写入的状态、确认发生的位置、超时与重试分支，以及失败后由谁接管。尤其区分“请求已发出”“broker已确认”“消费者已处理”和“业务结果已对账”，这些不是同一个完成点。

实验固定主题、分区键、消息ID和负载，只改变acks、批次、消费者数量、故障时点或网络条件之一。比较吞吐、P95/P99、消费者滞后、ISR、磁盘和最终消息集合，用“每个分区在一个群组内同一时刻只有一个有效消费者，处理完成点与提交偏移量关系明确，再均衡和崩溃后可界定重复或丢失窗口”判定；若证据冲突，回到本节点缩小故障窗口，不用平均值掩盖尾部与丢失。

#### enable.auto.commit

**目录节点 19/39。** “enable.auto.commit”服务于“沿消费者群组、订阅、轮询、分区分配、再均衡、偏移量提交、反序列化和退出协议建立可证明消费语义”。先说明它接收的状态、配置或记录，再画出跨客户端、broker、分区、副本、磁盘或外部系统的边界；结论必须能够从配置、指标、日志、偏移量和消息样本中复核。

机制轨迹从“auto.offset.reset”进入“enable.auto.commit”，再到“partition.assignment.strategy”。逐步记录请求所属线程或进程、读取与写入的状态、确认发生的位置、超时与重试分支，以及失败后由谁接管。尤其区分“请求已发出”“broker已确认”“消费者已处理”和“业务结果已对账”，这些不是同一个完成点。

实验固定主题、分区键、消息ID和负载，只改变acks、批次、消费者数量、故障时点或网络条件之一。比较吞吐、P95/P99、消费者滞后、ISR、磁盘和最终消息集合，用“每个分区在一个群组内同一时刻只有一个有效消费者，处理完成点与提交偏移量关系明确，再均衡和崩溃后可界定重复或丢失窗口”判定；若证据冲突，回到本节点缩小故障窗口，不用平均值掩盖尾部与丢失。

#### partition.assignment.strategy

**目录节点 20/39。** “partition.assignment.strategy”服务于“沿消费者群组、订阅、轮询、分区分配、再均衡、偏移量提交、反序列化和退出协议建立可证明消费语义”。先说明它接收的状态、配置或记录，再画出跨客户端、broker、分区、副本、磁盘或外部系统的边界；结论必须能够从配置、指标、日志、偏移量和消息样本中复核。

机制轨迹从“enable.auto.commit”进入“partition.assignment.strategy”，再到“client.id”。逐步记录请求所属线程或进程、读取与写入的状态、确认发生的位置、超时与重试分支，以及失败后由谁接管。尤其区分“请求已发出”“broker已确认”“消费者已处理”和“业务结果已对账”，这些不是同一个完成点。

实验固定主题、分区键、消息ID和负载，只改变acks、批次、消费者数量、故障时点或网络条件之一。比较吞吐、P95/P99、消费者滞后、ISR、磁盘和最终消息集合，用“每个分区在一个群组内同一时刻只有一个有效消费者，处理完成点与提交偏移量关系明确，再均衡和崩溃后可界定重复或丢失窗口”判定；若证据冲突，回到本节点缩小故障窗口，不用平均值掩盖尾部与丢失。

#### client.id

**目录节点 21/39。** “client.id”服务于“沿消费者群组、订阅、轮询、分区分配、再均衡、偏移量提交、反序列化和退出协议建立可证明消费语义”。先说明它接收的状态、配置或记录，再画出跨客户端、broker、分区、副本、磁盘或外部系统的边界；结论必须能够从配置、指标、日志、偏移量和消息样本中复核。

机制轨迹从“partition.assignment.strategy”进入“client.id”，再到“client.rack”。逐步记录请求所属线程或进程、读取与写入的状态、确认发生的位置、超时与重试分支，以及失败后由谁接管。尤其区分“请求已发出”“broker已确认”“消费者已处理”和“业务结果已对账”，这些不是同一个完成点。

实验固定主题、分区键、消息ID和负载，只改变acks、批次、消费者数量、故障时点或网络条件之一。比较吞吐、P95/P99、消费者滞后、ISR、磁盘和最终消息集合，用“每个分区在一个群组内同一时刻只有一个有效消费者，处理完成点与提交偏移量关系明确，再均衡和崩溃后可界定重复或丢失窗口”判定；若证据冲突，回到本节点缩小故障窗口，不用平均值掩盖尾部与丢失。

#### client.rack

**目录节点 22/39。** “client.rack”服务于“沿消费者群组、订阅、轮询、分区分配、再均衡、偏移量提交、反序列化和退出协议建立可证明消费语义”。先说明它接收的状态、配置或记录，再画出跨客户端、broker、分区、副本、磁盘或外部系统的边界；结论必须能够从配置、指标、日志、偏移量和消息样本中复核。

机制轨迹从“client.id”进入“client.rack”，再到“group.instance.id”。逐步记录请求所属线程或进程、读取与写入的状态、确认发生的位置、超时与重试分支，以及失败后由谁接管。尤其区分“请求已发出”“broker已确认”“消费者已处理”和“业务结果已对账”，这些不是同一个完成点。

实验固定主题、分区键、消息ID和负载，只改变acks、批次、消费者数量、故障时点或网络条件之一。比较吞吐、P95/P99、消费者滞后、ISR、磁盘和最终消息集合，用“每个分区在一个群组内同一时刻只有一个有效消费者，处理完成点与提交偏移量关系明确，再均衡和崩溃后可界定重复或丢失窗口”判定；若证据冲突，回到本节点缩小故障窗口，不用平均值掩盖尾部与丢失。

#### group.instance.id

**目录节点 23/39。** “group.instance.id”服务于“沿消费者群组、订阅、轮询、分区分配、再均衡、偏移量提交、反序列化和退出协议建立可证明消费语义”。先说明它接收的状态、配置或记录，再画出跨客户端、broker、分区、副本、磁盘或外部系统的边界；结论必须能够从配置、指标、日志、偏移量和消息样本中复核。

机制轨迹从“client.rack”进入“group.instance.id”，再到“receive.buffer.bytes和send.buffer.bytes”。逐步记录请求所属线程或进程、读取与写入的状态、确认发生的位置、超时与重试分支，以及失败后由谁接管。尤其区分“请求已发出”“broker已确认”“消费者已处理”和“业务结果已对账”，这些不是同一个完成点。

实验固定主题、分区键、消息ID和负载，只改变acks、批次、消费者数量、故障时点或网络条件之一。比较吞吐、P95/P99、消费者滞后、ISR、磁盘和最终消息集合，用“每个分区在一个群组内同一时刻只有一个有效消费者，处理完成点与提交偏移量关系明确，再均衡和崩溃后可界定重复或丢失窗口”判定；若证据冲突，回到本节点缩小故障窗口，不用平均值掩盖尾部与丢失。

#### receive.buffer.bytes和send.buffer.bytes

**目录节点 24/39。** “receive.buffer.bytes和send.buffer.bytes”服务于“沿消费者群组、订阅、轮询、分区分配、再均衡、偏移量提交、反序列化和退出协议建立可证明消费语义”。先说明它接收的状态、配置或记录，再画出跨客户端、broker、分区、副本、磁盘或外部系统的边界；结论必须能够从配置、指标、日志、偏移量和消息样本中复核。

机制轨迹从“group.instance.id”进入“receive.buffer.bytes和send.buffer.bytes”，再到“offsets.retention.minutes”。逐步记录请求所属线程或进程、读取与写入的状态、确认发生的位置、超时与重试分支，以及失败后由谁接管。尤其区分“请求已发出”“broker已确认”“消费者已处理”和“业务结果已对账”，这些不是同一个完成点。

实验固定主题、分区键、消息ID和负载，只改变acks、批次、消费者数量、故障时点或网络条件之一。比较吞吐、P95/P99、消费者滞后、ISR、磁盘和最终消息集合，用“每个分区在一个群组内同一时刻只有一个有效消费者，处理完成点与提交偏移量关系明确，再均衡和崩溃后可界定重复或丢失窗口”判定；若证据冲突，回到本节点缩小故障窗口，不用平均值掩盖尾部与丢失。

#### offsets.retention.minutes

**目录节点 25/39。** “offsets.retention.minutes”服务于“沿消费者群组、订阅、轮询、分区分配、再均衡、偏移量提交、反序列化和退出协议建立可证明消费语义”。先说明它接收的状态、配置或记录，再画出跨客户端、broker、分区、副本、磁盘或外部系统的边界；结论必须能够从配置、指标、日志、偏移量和消息样本中复核。

机制轨迹从“receive.buffer.bytes和send.buffer.bytes”进入“offsets.retention.minutes”，再到“提交和偏移量”。逐步记录请求所属线程或进程、读取与写入的状态、确认发生的位置、超时与重试分支，以及失败后由谁接管。尤其区分“请求已发出”“broker已确认”“消费者已处理”和“业务结果已对账”，这些不是同一个完成点。

实验固定主题、分区键、消息ID和负载，只改变acks、批次、消费者数量、故障时点或网络条件之一。比较吞吐、P95/P99、消费者滞后、ISR、磁盘和最终消息集合，用“每个分区在一个群组内同一时刻只有一个有效消费者，处理完成点与提交偏移量关系明确，再均衡和崩溃后可界定重复或丢失窗口”判定；若证据冲突，回到本节点缩小故障窗口，不用平均值掩盖尾部与丢失。

### 提交和偏移量

**目录节点 26/39。** “提交和偏移量”服务于“沿消费者群组、订阅、轮询、分区分配、再均衡、偏移量提交、反序列化和退出协议建立可证明消费语义”。先说明它接收的状态、配置或记录，再画出跨客户端、broker、分区、副本、磁盘或外部系统的边界；结论必须能够从配置、指标、日志、偏移量和消息样本中复核。

机制轨迹从“offsets.retention.minutes”进入“提交和偏移量”，再到“自动提交”。逐步记录请求所属线程或进程、读取与写入的状态、确认发生的位置、超时与重试分支，以及失败后由谁接管。尤其区分“请求已发出”“broker已确认”“消费者已处理”和“业务结果已对账”，这些不是同一个完成点。

实验固定主题、分区键、消息ID和负载，只改变acks、批次、消费者数量、故障时点或网络条件之一。比较吞吐、P95/P99、消费者滞后、ISR、磁盘和最终消息集合，用“每个分区在一个群组内同一时刻只有一个有效消费者，处理完成点与提交偏移量关系明确，再均衡和崩溃后可界定重复或丢失窗口”判定；若证据冲突，回到本节点缩小故障窗口，不用平均值掩盖尾部与丢失。

#### 自动提交

**目录节点 27/39。** “自动提交”服务于“沿消费者群组、订阅、轮询、分区分配、再均衡、偏移量提交、反序列化和退出协议建立可证明消费语义”。先说明它接收的状态、配置或记录，再画出跨客户端、broker、分区、副本、磁盘或外部系统的边界；结论必须能够从配置、指标、日志、偏移量和消息样本中复核。

机制轨迹从“提交和偏移量”进入“自动提交”，再到“提交当前偏移量”。逐步记录请求所属线程或进程、读取与写入的状态、确认发生的位置、超时与重试分支，以及失败后由谁接管。尤其区分“请求已发出”“broker已确认”“消费者已处理”和“业务结果已对账”，这些不是同一个完成点。

实验固定主题、分区键、消息ID和负载，只改变acks、批次、消费者数量、故障时点或网络条件之一。比较吞吐、P95/P99、消费者滞后、ISR、磁盘和最终消息集合，用“每个分区在一个群组内同一时刻只有一个有效消费者，处理完成点与提交偏移量关系明确，再均衡和崩溃后可界定重复或丢失窗口”判定；若证据冲突，回到本节点缩小故障窗口，不用平均值掩盖尾部与丢失。

#### 提交当前偏移量

**目录节点 28/39。** “提交当前偏移量”服务于“沿消费者群组、订阅、轮询、分区分配、再均衡、偏移量提交、反序列化和退出协议建立可证明消费语义”。先说明它接收的状态、配置或记录，再画出跨客户端、broker、分区、副本、磁盘或外部系统的边界；结论必须能够从配置、指标、日志、偏移量和消息样本中复核。

机制轨迹从“自动提交”进入“提交当前偏移量”，再到“异步提交”。逐步记录请求所属线程或进程、读取与写入的状态、确认发生的位置、超时与重试分支，以及失败后由谁接管。尤其区分“请求已发出”“broker已确认”“消费者已处理”和“业务结果已对账”，这些不是同一个完成点。

实验固定主题、分区键、消息ID和负载，只改变acks、批次、消费者数量、故障时点或网络条件之一。比较吞吐、P95/P99、消费者滞后、ISR、磁盘和最终消息集合，用“每个分区在一个群组内同一时刻只有一个有效消费者，处理完成点与提交偏移量关系明确，再均衡和崩溃后可界定重复或丢失窗口”判定；若证据冲突，回到本节点缩小故障窗口，不用平均值掩盖尾部与丢失。

#### 异步提交

**目录节点 29/39。** “异步提交”服务于“沿消费者群组、订阅、轮询、分区分配、再均衡、偏移量提交、反序列化和退出协议建立可证明消费语义”。先说明它接收的状态、配置或记录，再画出跨客户端、broker、分区、副本、磁盘或外部系统的边界；结论必须能够从配置、指标、日志、偏移量和消息样本中复核。

机制轨迹从“提交当前偏移量”进入“异步提交”，再到“同步和异步组合提交”。逐步记录请求所属线程或进程、读取与写入的状态、确认发生的位置、超时与重试分支，以及失败后由谁接管。尤其区分“请求已发出”“broker已确认”“消费者已处理”和“业务结果已对账”，这些不是同一个完成点。

实验固定主题、分区键、消息ID和负载，只改变acks、批次、消费者数量、故障时点或网络条件之一。比较吞吐、P95/P99、消费者滞后、ISR、磁盘和最终消息集合，用“每个分区在一个群组内同一时刻只有一个有效消费者，处理完成点与提交偏移量关系明确，再均衡和崩溃后可界定重复或丢失窗口”判定；若证据冲突，回到本节点缩小故障窗口，不用平均值掩盖尾部与丢失。

#### 同步和异步组合提交

**目录节点 30/39。** “同步和异步组合提交”服务于“沿消费者群组、订阅、轮询、分区分配、再均衡、偏移量提交、反序列化和退出协议建立可证明消费语义”。先说明它接收的状态、配置或记录，再画出跨客户端、broker、分区、副本、磁盘或外部系统的边界；结论必须能够从配置、指标、日志、偏移量和消息样本中复核。

机制轨迹从“异步提交”进入“同步和异步组合提交”，再到“提交特定的偏移量”。逐步记录请求所属线程或进程、读取与写入的状态、确认发生的位置、超时与重试分支，以及失败后由谁接管。尤其区分“请求已发出”“broker已确认”“消费者已处理”和“业务结果已对账”，这些不是同一个完成点。

实验固定主题、分区键、消息ID和负载，只改变acks、批次、消费者数量、故障时点或网络条件之一。比较吞吐、P95/P99、消费者滞后、ISR、磁盘和最终消息集合，用“每个分区在一个群组内同一时刻只有一个有效消费者，处理完成点与提交偏移量关系明确，再均衡和崩溃后可界定重复或丢失窗口”判定；若证据冲突，回到本节点缩小故障窗口，不用平均值掩盖尾部与丢失。

#### 提交特定的偏移量

**目录节点 31/39。** “提交特定的偏移量”服务于“沿消费者群组、订阅、轮询、分区分配、再均衡、偏移量提交、反序列化和退出协议建立可证明消费语义”。先说明它接收的状态、配置或记录，再画出跨客户端、broker、分区、副本、磁盘或外部系统的边界；结论必须能够从配置、指标、日志、偏移量和消息样本中复核。

机制轨迹从“同步和异步组合提交”进入“提交特定的偏移量”，再到“再均衡监听器”。逐步记录请求所属线程或进程、读取与写入的状态、确认发生的位置、超时与重试分支，以及失败后由谁接管。尤其区分“请求已发出”“broker已确认”“消费者已处理”和“业务结果已对账”，这些不是同一个完成点。

实验固定主题、分区键、消息ID和负载，只改变acks、批次、消费者数量、故障时点或网络条件之一。比较吞吐、P95/P99、消费者滞后、ISR、磁盘和最终消息集合，用“每个分区在一个群组内同一时刻只有一个有效消费者，处理完成点与提交偏移量关系明确，再均衡和崩溃后可界定重复或丢失窗口”判定；若证据冲突，回到本节点缩小故障窗口，不用平均值掩盖尾部与丢失。

### 再均衡监听器

**目录节点 32/39。** “再均衡监听器”服务于“沿消费者群组、订阅、轮询、分区分配、再均衡、偏移量提交、反序列化和退出协议建立可证明消费语义”。先说明它接收的状态、配置或记录，再画出跨客户端、broker、分区、副本、磁盘或外部系统的边界；结论必须能够从配置、指标、日志、偏移量和消息样本中复核。

机制轨迹从“提交特定的偏移量”进入“再均衡监听器”，再到“从特定偏移量位置读取记录”。逐步记录请求所属线程或进程、读取与写入的状态、确认发生的位置、超时与重试分支，以及失败后由谁接管。尤其区分“请求已发出”“broker已确认”“消费者已处理”和“业务结果已对账”，这些不是同一个完成点。

实验固定主题、分区键、消息ID和负载，只改变acks、批次、消费者数量、故障时点或网络条件之一。比较吞吐、P95/P99、消费者滞后、ISR、磁盘和最终消息集合，用“每个分区在一个群组内同一时刻只有一个有效消费者，处理完成点与提交偏移量关系明确，再均衡和崩溃后可界定重复或丢失窗口”判定；若证据冲突，回到本节点缩小故障窗口，不用平均值掩盖尾部与丢失。

### 从特定偏移量位置读取记录

**目录节点 33/39。** “从特定偏移量位置读取记录”服务于“沿消费者群组、订阅、轮询、分区分配、再均衡、偏移量提交、反序列化和退出协议建立可证明消费语义”。先说明它接收的状态、配置或记录，再画出跨客户端、broker、分区、副本、磁盘或外部系统的边界；结论必须能够从配置、指标、日志、偏移量和消息样本中复核。

机制轨迹从“再均衡监听器”进入“从特定偏移量位置读取记录”，再到“如何退出”。逐步记录请求所属线程或进程、读取与写入的状态、确认发生的位置、超时与重试分支，以及失败后由谁接管。尤其区分“请求已发出”“broker已确认”“消费者已处理”和“业务结果已对账”，这些不是同一个完成点。

实验固定主题、分区键、消息ID和负载，只改变acks、批次、消费者数量、故障时点或网络条件之一。比较吞吐、P95/P99、消费者滞后、ISR、磁盘和最终消息集合，用“每个分区在一个群组内同一时刻只有一个有效消费者，处理完成点与提交偏移量关系明确，再均衡和崩溃后可界定重复或丢失窗口”判定；若证据冲突，回到本节点缩小故障窗口，不用平均值掩盖尾部与丢失。

### 如何退出

**目录节点 34/39。** “如何退出”服务于“沿消费者群组、订阅、轮询、分区分配、再均衡、偏移量提交、反序列化和退出协议建立可证明消费语义”。先说明它接收的状态、配置或记录，再画出跨客户端、broker、分区、副本、磁盘或外部系统的边界；结论必须能够从配置、指标、日志、偏移量和消息样本中复核。

机制轨迹从“从特定偏移量位置读取记录”进入“如何退出”，再到“反序列化器”。逐步记录请求所属线程或进程、读取与写入的状态、确认发生的位置、超时与重试分支，以及失败后由谁接管。尤其区分“请求已发出”“broker已确认”“消费者已处理”和“业务结果已对账”，这些不是同一个完成点。

实验固定主题、分区键、消息ID和负载，只改变acks、批次、消费者数量、故障时点或网络条件之一。比较吞吐、P95/P99、消费者滞后、ISR、磁盘和最终消息集合，用“每个分区在一个群组内同一时刻只有一个有效消费者，处理完成点与提交偏移量关系明确，再均衡和崩溃后可界定重复或丢失窗口”判定；若证据冲突，回到本节点缩小故障窗口，不用平均值掩盖尾部与丢失。

### 反序列化器

**目录节点 35/39。** “反序列化器”服务于“沿消费者群组、订阅、轮询、分区分配、再均衡、偏移量提交、反序列化和退出协议建立可证明消费语义”。先说明它接收的状态、配置或记录，再画出跨客户端、broker、分区、副本、磁盘或外部系统的边界；结论必须能够从配置、指标、日志、偏移量和消息样本中复核。

机制轨迹从“如何退出”进入“反序列化器”，再到“自定义反序列化器”。逐步记录请求所属线程或进程、读取与写入的状态、确认发生的位置、超时与重试分支，以及失败后由谁接管。尤其区分“请求已发出”“broker已确认”“消费者已处理”和“业务结果已对账”，这些不是同一个完成点。

实验固定主题、分区键、消息ID和负载，只改变acks、批次、消费者数量、故障时点或网络条件之一。比较吞吐、P95/P99、消费者滞后、ISR、磁盘和最终消息集合，用“每个分区在一个群组内同一时刻只有一个有效消费者，处理完成点与提交偏移量关系明确，再均衡和崩溃后可界定重复或丢失窗口”判定；若证据冲突，回到本节点缩小故障窗口，不用平均值掩盖尾部与丢失。

#### 自定义反序列化器

**目录节点 36/39。** “自定义反序列化器”服务于“沿消费者群组、订阅、轮询、分区分配、再均衡、偏移量提交、反序列化和退出协议建立可证明消费语义”。先说明它接收的状态、配置或记录，再画出跨客户端、broker、分区、副本、磁盘或外部系统的边界；结论必须能够从配置、指标、日志、偏移量和消息样本中复核。

机制轨迹从“反序列化器”进入“自定义反序列化器”，再到“在消费者里使用Avro反序列器”。逐步记录请求所属线程或进程、读取与写入的状态、确认发生的位置、超时与重试分支，以及失败后由谁接管。尤其区分“请求已发出”“broker已确认”“消费者已处理”和“业务结果已对账”，这些不是同一个完成点。

实验固定主题、分区键、消息ID和负载，只改变acks、批次、消费者数量、故障时点或网络条件之一。比较吞吐、P95/P99、消费者滞后、ISR、磁盘和最终消息集合，用“每个分区在一个群组内同一时刻只有一个有效消费者，处理完成点与提交偏移量关系明确，再均衡和崩溃后可界定重复或丢失窗口”判定；若证据冲突，回到本节点缩小故障窗口，不用平均值掩盖尾部与丢失。

#### 在消费者里使用Avro反序列器

**目录节点 37/39。** “在消费者里使用Avro反序列器”服务于“沿消费者群组、订阅、轮询、分区分配、再均衡、偏移量提交、反序列化和退出协议建立可证明消费语义”。先说明它接收的状态、配置或记录，再画出跨客户端、broker、分区、副本、磁盘或外部系统的边界；结论必须能够从配置、指标、日志、偏移量和消息样本中复核。

机制轨迹从“自定义反序列化器”进入“在消费者里使用Avro反序列器”，再到“独立的消费者：为什么以及怎样使用不属于任何群组的消费者”。逐步记录请求所属线程或进程、读取与写入的状态、确认发生的位置、超时与重试分支，以及失败后由谁接管。尤其区分“请求已发出”“broker已确认”“消费者已处理”和“业务结果已对账”，这些不是同一个完成点。

实验固定主题、分区键、消息ID和负载，只改变acks、批次、消费者数量、故障时点或网络条件之一。比较吞吐、P95/P99、消费者滞后、ISR、磁盘和最终消息集合，用“每个分区在一个群组内同一时刻只有一个有效消费者，处理完成点与提交偏移量关系明确，再均衡和崩溃后可界定重复或丢失窗口”判定；若证据冲突，回到本节点缩小故障窗口，不用平均值掩盖尾部与丢失。

### 独立的消费者：为什么以及怎样使用不属于任何群组的消费者

**目录节点 38/39。** “独立的消费者：为什么以及怎样使用不属于任何群组的消费者”服务于“沿消费者群组、订阅、轮询、分区分配、再均衡、偏移量提交、反序列化和退出协议建立可证明消费语义”。先说明它接收的状态、配置或记录，再画出跨客户端、broker、分区、副本、磁盘或外部系统的边界；结论必须能够从配置、指标、日志、偏移量和消息样本中复核。

机制轨迹从“在消费者里使用Avro反序列器”进入“独立的消费者：为什么以及怎样使用不属于任何群组的消费者”，再到“小结”。逐步记录请求所属线程或进程、读取与写入的状态、确认发生的位置、超时与重试分支，以及失败后由谁接管。尤其区分“请求已发出”“broker已确认”“消费者已处理”和“业务结果已对账”，这些不是同一个完成点。

实验固定主题、分区键、消息ID和负载，只改变acks、批次、消费者数量、故障时点或网络条件之一。比较吞吐、P95/P99、消费者滞后、ISR、磁盘和最终消息集合，用“每个分区在一个群组内同一时刻只有一个有效消费者，处理完成点与提交偏移量关系明确，再均衡和崩溃后可界定重复或丢失窗口”判定；若证据冲突，回到本节点缩小故障窗口，不用平均值掩盖尾部与丢失。

### 小结

**目录节点 39/39。** “小结”服务于“沿消费者群组、订阅、轮询、分区分配、再均衡、偏移量提交、反序列化和退出协议建立可证明消费语义”。先说明它接收的状态、配置或记录，再画出跨客户端、broker、分区、副本、磁盘或外部系统的边界；结论必须能够从配置、指标、日志、偏移量和消息样本中复核。

机制轨迹从“独立的消费者：为什么以及怎样使用不属于任何群组的消费者”进入“小结”，再到“本章输出与验收”。逐步记录请求所属线程或进程、读取与写入的状态、确认发生的位置、超时与重试分支，以及失败后由谁接管。尤其区分“请求已发出”“broker已确认”“消费者已处理”和“业务结果已对账”，这些不是同一个完成点。

实验固定主题、分区键、消息ID和负载，只改变acks、批次、消费者数量、故障时点或网络条件之一。比较吞吐、P95/P99、消费者滞后、ISR、磁盘和最终消息集合，用“每个分区在一个群组内同一时刻只有一个有效消费者，处理完成点与提交偏移量关系明确，再均衡和崩溃后可界定重复或丢失窗口”判定；若证据冲突，回到本节点缩小故障窗口，不用平均值掩盖尾部与丢失。

## 机制模型：数据面、控制面与存储

数据面从生产者序列化开始，经分区器、记录累加器与网络请求进入分区首领；追随者拉取后，高水位限定消费者可见范围。消费端通过poll取得记录，处理完成点和提交偏移量必须分别标记。记录键决定分区时，调整分区数可能改变后续映射，因此顺序承诺必须写到契约中。

控制面管理集群成员、控制器、分区首领、ISR、配置、配额和消费者群组。控制器或群组协调器切换期间会存在旧纪元、重试与最终一致窗口；客户端不能把一次元数据响应当作永久真相。状态机图必须标出纪元或generation，防止旧成员继续提交。

存储面以分区日志片段、偏移索引和时间索引为核心。删除策略按保留窗口移除旧片段，压实策略保留每个键的最新值并处理删除标记；它们不等于数据库行更新。磁盘、页缓存、批次压缩和副本获取共同决定吞吐与恢复时间。

<Callout type="trap">
  **语义陷阱。**
  开启自动提交后假定消息已经处理完成，或在慢处理期间忽略max.poll.interval.ms导致分区被回收。先写清确认点、处理点、提交点和最终业务不变量，再选择配置。
</Callout>

<Callout type="trap">
  **测量陷阱。**
  平均吞吐、进程在线、请求无异常或消费者滞后为零只能证明局部状态；必须保留P99、ISR变化、重试次数、分区偏移和消息ID集合，才能证明每个分区在一个群组内同一时刻只有一个有效消费者，处理完成点与提交偏移量关系明确，再均衡和崩溃后可界定重复或丢失窗口。
</Callout>

<Callout type="warning">
  broker宕机、网络隔离、磁盘耗尽、ACL变更、偏移量重置、事务中止和跨集群切换只在隔离环境演练。预先规定数据损失、重复、延迟、磁盘和恢复时间上界。
</Callout>

## 独立证据与生产交接

目录证据保存O'Reilly第2版目录、本页节点与页面标题映射；环境证据保存Kafka与Java版本、broker和客户端配置、主题分区副本状态；运行证据保存消息生成规则、分区键、负载、分位延迟、吞吐、滞后、ISR与磁盘；故障证据保存注入时点、最后确认偏移量、恢复动作和最终消息集合。

性能判断同时给出生产与消费侧背压。增大批次和压缩通常提高吞吐，却会增加等待、CPU或单批失败影响；增加分区提高并行度，也增加文件句柄、控制面与再均衡成本。所有对比固定消息大小、键分布、副本与确认语义。

恢复判断从业务对账结束。broker重新加入、ISR恢复、消费者继续poll、MirrorMaker继续复制或Streams任务重启都只是阶段；还需核对每个消息ID的出现次数、顺序约束、状态存储和外部结果，并确认旧成员或旧站点不再错误写入。

## 本章回顾

重新完成“沿消费者群组、订阅、轮询、分区分配、再均衡、偏移量提交、反序列化和退出协议建立可证明消费语义”：先固定第2版目录与版本，再从拓扑和状态机推导配置，通过基线、压力与故障实验测量，最后交付群组分配图、轮询与提交时序、再均衡故障实验、偏移量对账与退出检查表。只有“每个分区在一个群组内同一时刻只有一个有效消费者，处理完成点与提交偏移量关系明确，再均衡和崩溃后可界定重复或丢失窗口”及反例都能被独立重放，本页才算完成。

## 复习与运行验收

<Exercises>
**问题 1**：为什么“第4章 Kafka消费者——从Kafka读取数据”必须覆盖39个目录节点？
<Answer>这些节点共同组成“沿消费者群组、订阅、轮询、分区分配、再均衡、偏移量提交、反序列化和退出协议建立可证明消费语义”的结构与证据链；漏项会使群组分配图、轮询与提交时序、再均衡故障实验、偏移量对账与退出检查表无法独立复现。</Answer>

**问题 2**：本页最小不变量是什么？

<Answer>
  每个分区在一个群组内同一时刻只有一个有效消费者，处理完成点与提交偏移量关系明确，再均衡和崩溃后可界定重复或丢失窗口。需要配置、拓扑、偏移量、指标、故障轨迹和业务对账共同证明。
</Answer>

**问题 3**：怎样构造最小反例？

<Answer>
  固定消息、键、主题与副本，只改变acks、最少ISR、批次、消费者处理速度或单一故障点，比较确认、可见偏移量和最终消息集合。
</Answer>

**问题 4**：为什么不能用新版本功能替代第2版目录？

<Answer>
  课程分母由2021/2022第2版固定；后来版本可作差异说明，但不能覆盖原书节点、当时配置语义和本页验收。
</Answer>

**问题 5**：如何验证性能结论？

<Answer>
  固定消息大小、键分布、分区、副本与交付语义，重复测吞吐、P50/P95/P99、滞后、ISR、CPU、网络和磁盘，并对账结果。
</Answer>

**问题 6**：独立交接需要什么？

<Answer>需要完整目录映射、版本配置、拓扑与时序、群组分配图、轮询与提交时序、再均衡故障实验、偏移量对账与退出检查表、负载、故障、对账、停止、恢复、回退和责任人。</Answer>
</Exercises>

<Glossary>
  <GlossaryItem term="消费者群组">
    消费者群组在本页指向Kafka第2版中的具体状态、协议或操作边界；适用范围由“每个分区在一个群组内同一时刻只有一个有效消费者，处理完成点与提交偏移量关系明确，再均衡和崩溃后可界定重复或丢失窗口”及故障反例共同限定。
  </GlossaryItem>
  <GlossaryItem term="再均衡">
    再均衡在本页指向Kafka第2版中的具体状态、协议或操作边界；适用范围由“每个分区在一个群组内同一时刻只有一个有效消费者，处理完成点与提交偏移量关系明确，再均衡和崩溃后可界定重复或丢失窗口”及故障反例共同限定。
  </GlossaryItem>
  <GlossaryItem term="轮询">
    轮询在本页指向Kafka第2版中的具体状态、协议或操作边界；适用范围由“每个分区在一个群组内同一时刻只有一个有效消费者，处理完成点与提交偏移量关系明确，再均衡和崩溃后可界定重复或丢失窗口”及故障反例共同限定。
  </GlossaryItem>
  <GlossaryItem term="偏移量提交">
    偏移量提交在本页指向Kafka第2版中的具体状态、协议或操作边界；适用范围由“每个分区在一个群组内同一时刻只有一个有效消费者，处理完成点与提交偏移量关系明确，再均衡和崩溃后可界定重复或丢失窗口”及故障反例共同限定。
  </GlossaryItem>
  <GlossaryItem term="反序列化器">
    反序列化器在本页指向Kafka第2版中的具体状态、协议或操作边界；适用范围由“每个分区在一个群组内同一时刻只有一个有效消费者，处理完成点与提交偏移量关系明确，再均衡和崩溃后可界定重复或丢失窗口”及故障反例共同限定。
  </GlossaryItem>
</Glossary>

[← 上一页：第3章 Kafka生产者——向Kafka写入数据](/learn/kafka-definitive-guide/03-kafka-producers/kfk-03-kafka-producers) · [下一页：第5章 编程式管理Kafka →](/learn/kafka-definitive-guide/05-programmatic-administration/kfk-05-programmatic-administration)

<Attribution
  adaptedFrom="Gwen Shapira等《Kafka权威指南》第2版"
  adaptedUrl="https://www.oreilly.com/library/view/kafkaquan-wei-zhi-nan-di-2ban/9787115601421/"
/>
