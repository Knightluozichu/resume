---
title: "第14章 流式处理"
description: "依据O'Reilly与中文版第2版完整目录：用拓扑、时间、状态、流表二元性、窗口和处理保证解释设计模式，并完成Kafka Streams拓扑的构建、优化、测试、扩展与故障恢复"
type: C
section: "Kafka权威指南（第2版 / 2022）"
draft: false
math: false
sourceUrl: "https://www.oreilly.com/library/view/kafkaquan-wei-zhi-nan-di-2ban/9787115601421/"
demo: true
order: 0
---

import {
  Kfk14StreamProcessingTopologyLab,
  Kfk14StreamProcessingReliabilityLab,
  Kfk14StreamProcessingEvidenceLab,
} from "@/components/mdx/kafka-definitive-guide/diagrams/kfk-14-stream-processing";

# 第14章 流式处理

本课程对应Gwen Shapira、Todd Palino、Rajini Sivaram、Krit Petty著《Kafka权威指南》第2版，薛命灯译，人民邮电出版社2022年11月出版，318页，ISBN 9787115601421；英文第2版由O'Reilly于2021年出版。课程严格使用第2版14章与附录A、B的目录边界，不把第1版旧目录、后来Kafka版本的实现或厂商功能冒充原书内容。

本页对应一个正式单元，逐项覆盖31个目录节点。主问题是“用拓扑、时间、状态、流表二元性、窗口和处理保证解释设计模式，并完成Kafka Streams拓扑的构建、优化、测试、扩展与故障恢复”，最终交付为流处理拓扑、窗口与乱序样本、状态恢复演练、TopologyTestDriver测试和框架选型矩阵，由另一位工程师更换消息键、负载和故障点重放。

<Objectives>

- 能解释“第14章 流式处理”的31个目录节点，并绘制它们之间的数据面、控制面与持久化边界。
- 能比较正常、压力、再均衡、broker故障和跨网络故障轨迹，判断确认、偏移量、ISR和业务结果是否一致。
- 能设计并复现流处理拓扑、窗口与乱序样本、状态恢复演练、TopologyTestDriver测试和框架选型矩阵，用分位延迟、吞吐、滞后、日志与消息ID对账支持结论。
- 能分析“把逐条消费称为流处理，忽略时间语义、状态、窗口、重分区、乱序和处理保证”为何失败，并写出停止、恢复、回退与责任交接条件。

</Objectives>

## 从一条记录的生命周期开始

先预测：带键、值、标头和时间戳的记录进入生产者后落到哪个分区，何时组成批次，哪个broker确认，副本何时进入或离开ISR，消费者何时处理并提交下一偏移量。随后再预测broker重启、网络延迟、再均衡或事务中止时，最后可见记录和业务结果应是什么。观察结果之前必须留下预测。

Kafka的高吞吐来自顺序追加、批处理、压缩、页缓存和分区并行；可靠性来自复制、确认、幂等或事务与消费提交的组合。任何调参都可能同时改变延迟、吞吐、资源和交付语义，本页不接受只报告单一峰值。

## 核心词汇与版本边界

<Term def="处理拓扑是“用拓扑、时间、状态、流表二元性、窗口和处理保证解释设计模式，并完成Kafka Streams拓扑的构建、优化、测试、扩展与故障恢复”中的第1个核心概念，定义必须同时给出状态、边界、失败语义和可观测证据">处理拓扑</Term>、<Term def="事件时间是“用拓扑、时间、状态、流表二元性、窗口和处理保证解释设计模式，并完成Kafka Streams拓扑的构建、优化、测试、扩展与故障恢复”中的第2个核心概念，定义必须同时给出状态、边界、失败语义和可观测证据">事件时间</Term>、<Term def="状态存储是“用拓扑、时间、状态、流表二元性、窗口和处理保证解释设计模式，并完成Kafka Streams拓扑的构建、优化、测试、扩展与故障恢复”中的第3个核心概念，定义必须同时给出状态、边界、失败语义和可观测证据">状态存储</Term>、<Term def="流与表是“用拓扑、时间、状态、流表二元性、窗口和处理保证解释设计模式，并完成Kafka Streams拓扑的构建、优化、测试、扩展与故障恢复”中的第4个核心概念，定义必须同时给出状态、边界、失败语义和可观测证据">流与表</Term>、<Term def="时间窗口是“用拓扑、时间、状态、流表二元性、窗口和处理保证解释设计模式，并完成Kafka Streams拓扑的构建、优化、测试、扩展与故障恢复”中的第5个核心概念，定义必须同时给出状态、边界、失败语义和可观测证据">时间窗口</Term>
构成本页词汇。每个词都要回答：它属于客户端还是broker，作用于主题、分区、副本或消费者位置中的哪一层，何时创建与推进，超时、重试、故障或版本变化后留下什么证据。

<Kfk14StreamProcessingTopologyLab />

## 原书目录逐节点重构

### 什么是流式处理

**目录节点 1/31。** “什么是流式处理”服务于“用拓扑、时间、状态、流表二元性、窗口和处理保证解释设计模式，并完成Kafka Streams拓扑的构建、优化、测试、扩展与故障恢复”。先说明它接收的状态、配置或记录，再画出跨客户端、broker、分区、副本、磁盘或外部系统的边界；结论必须能够从配置、指标、日志、偏移量和消息样本中复核。

机制轨迹从“本章输入与契约”进入“什么是流式处理”，再到“流式处理相关概念”。逐步记录请求所属线程或进程、读取与写入的状态、确认发生的位置、超时与重试分支，以及失败后由谁接管。尤其区分“请求已发出”“broker已确认”“消费者已处理”和“业务结果已对账”，这些不是同一个完成点。

实验固定主题、分区键、消息ID和负载，只改变acks、批次、消费者数量、故障时点或网络条件之一。比较吞吐、P95/P99、消费者滞后、ISR、磁盘和最终消息集合，用“事件时间与处理时间不混淆，状态和变更日志可恢复，连接与窗口边界明确，乱序、重放和故障不会产生未说明结果”判定；若证据冲突，回到本节点缩小故障窗口，不用平均值掩盖尾部与丢失。

### 流式处理相关概念

**目录节点 2/31。** “流式处理相关概念”服务于“用拓扑、时间、状态、流表二元性、窗口和处理保证解释设计模式，并完成Kafka Streams拓扑的构建、优化、测试、扩展与故障恢复”。先说明它接收的状态、配置或记录，再画出跨客户端、broker、分区、副本、磁盘或外部系统的边界；结论必须能够从配置、指标、日志、偏移量和消息样本中复核。

机制轨迹从“什么是流式处理”进入“流式处理相关概念”，再到“拓扑”。逐步记录请求所属线程或进程、读取与写入的状态、确认发生的位置、超时与重试分支，以及失败后由谁接管。尤其区分“请求已发出”“broker已确认”“消费者已处理”和“业务结果已对账”，这些不是同一个完成点。

实验固定主题、分区键、消息ID和负载，只改变acks、批次、消费者数量、故障时点或网络条件之一。比较吞吐、P95/P99、消费者滞后、ISR、磁盘和最终消息集合，用“事件时间与处理时间不混淆，状态和变更日志可恢复，连接与窗口边界明确，乱序、重放和故障不会产生未说明结果”判定；若证据冲突，回到本节点缩小故障窗口，不用平均值掩盖尾部与丢失。

#### 拓扑

**目录节点 3/31。** “拓扑”服务于“用拓扑、时间、状态、流表二元性、窗口和处理保证解释设计模式，并完成Kafka Streams拓扑的构建、优化、测试、扩展与故障恢复”。先说明它接收的状态、配置或记录，再画出跨客户端、broker、分区、副本、磁盘或外部系统的边界；结论必须能够从配置、指标、日志、偏移量和消息样本中复核。

机制轨迹从“流式处理相关概念”进入“拓扑”，再到“时间”。逐步记录请求所属线程或进程、读取与写入的状态、确认发生的位置、超时与重试分支，以及失败后由谁接管。尤其区分“请求已发出”“broker已确认”“消费者已处理”和“业务结果已对账”，这些不是同一个完成点。

实验固定主题、分区键、消息ID和负载，只改变acks、批次、消费者数量、故障时点或网络条件之一。比较吞吐、P95/P99、消费者滞后、ISR、磁盘和最终消息集合，用“事件时间与处理时间不混淆，状态和变更日志可恢复，连接与窗口边界明确，乱序、重放和故障不会产生未说明结果”判定；若证据冲突，回到本节点缩小故障窗口，不用平均值掩盖尾部与丢失。

#### 时间

**目录节点 4/31。** “时间”服务于“用拓扑、时间、状态、流表二元性、窗口和处理保证解释设计模式，并完成Kafka Streams拓扑的构建、优化、测试、扩展与故障恢复”。先说明它接收的状态、配置或记录，再画出跨客户端、broker、分区、副本、磁盘或外部系统的边界；结论必须能够从配置、指标、日志、偏移量和消息样本中复核。

机制轨迹从“拓扑”进入“时间”，再到“状态”。逐步记录请求所属线程或进程、读取与写入的状态、确认发生的位置、超时与重试分支，以及失败后由谁接管。尤其区分“请求已发出”“broker已确认”“消费者已处理”和“业务结果已对账”，这些不是同一个完成点。

实验固定主题、分区键、消息ID和负载，只改变acks、批次、消费者数量、故障时点或网络条件之一。比较吞吐、P95/P99、消费者滞后、ISR、磁盘和最终消息集合，用“事件时间与处理时间不混淆，状态和变更日志可恢复，连接与窗口边界明确，乱序、重放和故障不会产生未说明结果”判定；若证据冲突，回到本节点缩小故障窗口，不用平均值掩盖尾部与丢失。

#### 状态

**目录节点 5/31。** “状态”服务于“用拓扑、时间、状态、流表二元性、窗口和处理保证解释设计模式，并完成Kafka Streams拓扑的构建、优化、测试、扩展与故障恢复”。先说明它接收的状态、配置或记录，再画出跨客户端、broker、分区、副本、磁盘或外部系统的边界；结论必须能够从配置、指标、日志、偏移量和消息样本中复核。

机制轨迹从“时间”进入“状态”，再到“流和表”。逐步记录请求所属线程或进程、读取与写入的状态、确认发生的位置、超时与重试分支，以及失败后由谁接管。尤其区分“请求已发出”“broker已确认”“消费者已处理”和“业务结果已对账”，这些不是同一个完成点。

实验固定主题、分区键、消息ID和负载，只改变acks、批次、消费者数量、故障时点或网络条件之一。比较吞吐、P95/P99、消费者滞后、ISR、磁盘和最终消息集合，用“事件时间与处理时间不混淆，状态和变更日志可恢复，连接与窗口边界明确，乱序、重放和故障不会产生未说明结果”判定；若证据冲突，回到本节点缩小故障窗口，不用平均值掩盖尾部与丢失。

#### 流和表

**目录节点 6/31。** “流和表”服务于“用拓扑、时间、状态、流表二元性、窗口和处理保证解释设计模式，并完成Kafka Streams拓扑的构建、优化、测试、扩展与故障恢复”。先说明它接收的状态、配置或记录，再画出跨客户端、broker、分区、副本、磁盘或外部系统的边界；结论必须能够从配置、指标、日志、偏移量和消息样本中复核。

机制轨迹从“状态”进入“流和表”，再到“时间窗口”。逐步记录请求所属线程或进程、读取与写入的状态、确认发生的位置、超时与重试分支，以及失败后由谁接管。尤其区分“请求已发出”“broker已确认”“消费者已处理”和“业务结果已对账”，这些不是同一个完成点。

实验固定主题、分区键、消息ID和负载，只改变acks、批次、消费者数量、故障时点或网络条件之一。比较吞吐、P95/P99、消费者滞后、ISR、磁盘和最终消息集合，用“事件时间与处理时间不混淆，状态和变更日志可恢复，连接与窗口边界明确，乱序、重放和故障不会产生未说明结果”判定；若证据冲突，回到本节点缩小故障窗口，不用平均值掩盖尾部与丢失。

#### 时间窗口

**目录节点 7/31。** “时间窗口”服务于“用拓扑、时间、状态、流表二元性、窗口和处理保证解释设计模式，并完成Kafka Streams拓扑的构建、优化、测试、扩展与故障恢复”。先说明它接收的状态、配置或记录，再画出跨客户端、broker、分区、副本、磁盘或外部系统的边界；结论必须能够从配置、指标、日志、偏移量和消息样本中复核。

机制轨迹从“流和表”进入“时间窗口”，再到“处理保证”。逐步记录请求所属线程或进程、读取与写入的状态、确认发生的位置、超时与重试分支，以及失败后由谁接管。尤其区分“请求已发出”“broker已确认”“消费者已处理”和“业务结果已对账”，这些不是同一个完成点。

实验固定主题、分区键、消息ID和负载，只改变acks、批次、消费者数量、故障时点或网络条件之一。比较吞吐、P95/P99、消费者滞后、ISR、磁盘和最终消息集合，用“事件时间与处理时间不混淆，状态和变更日志可恢复，连接与窗口边界明确，乱序、重放和故障不会产生未说明结果”判定；若证据冲突，回到本节点缩小故障窗口，不用平均值掩盖尾部与丢失。

#### 处理保证

**目录节点 8/31。** “处理保证”服务于“用拓扑、时间、状态、流表二元性、窗口和处理保证解释设计模式，并完成Kafka Streams拓扑的构建、优化、测试、扩展与故障恢复”。先说明它接收的状态、配置或记录，再画出跨客户端、broker、分区、副本、磁盘或外部系统的边界；结论必须能够从配置、指标、日志、偏移量和消息样本中复核。

机制轨迹从“时间窗口”进入“处理保证”，再到“流式处理设计模式”。逐步记录请求所属线程或进程、读取与写入的状态、确认发生的位置、超时与重试分支，以及失败后由谁接管。尤其区分“请求已发出”“broker已确认”“消费者已处理”和“业务结果已对账”，这些不是同一个完成点。

实验固定主题、分区键、消息ID和负载，只改变acks、批次、消费者数量、故障时点或网络条件之一。比较吞吐、P95/P99、消费者滞后、ISR、磁盘和最终消息集合，用“事件时间与处理时间不混淆，状态和变更日志可恢复，连接与窗口边界明确，乱序、重放和故障不会产生未说明结果”判定；若证据冲突，回到本节点缩小故障窗口，不用平均值掩盖尾部与丢失。

### 流式处理设计模式

**目录节点 9/31。** “流式处理设计模式”服务于“用拓扑、时间、状态、流表二元性、窗口和处理保证解释设计模式，并完成Kafka Streams拓扑的构建、优化、测试、扩展与故障恢复”。先说明它接收的状态、配置或记录，再画出跨客户端、broker、分区、副本、磁盘或外部系统的边界；结论必须能够从配置、指标、日志、偏移量和消息样本中复核。

机制轨迹从“处理保证”进入“流式处理设计模式”，再到“单事件处理”。逐步记录请求所属线程或进程、读取与写入的状态、确认发生的位置、超时与重试分支，以及失败后由谁接管。尤其区分“请求已发出”“broker已确认”“消费者已处理”和“业务结果已对账”，这些不是同一个完成点。

实验固定主题、分区键、消息ID和负载，只改变acks、批次、消费者数量、故障时点或网络条件之一。比较吞吐、P95/P99、消费者滞后、ISR、磁盘和最终消息集合，用“事件时间与处理时间不混淆，状态和变更日志可恢复，连接与窗口边界明确，乱序、重放和故障不会产生未说明结果”判定；若证据冲突，回到本节点缩小故障窗口，不用平均值掩盖尾部与丢失。

#### 单事件处理

**目录节点 10/31。** “单事件处理”服务于“用拓扑、时间、状态、流表二元性、窗口和处理保证解释设计模式，并完成Kafka Streams拓扑的构建、优化、测试、扩展与故障恢复”。先说明它接收的状态、配置或记录，再画出跨客户端、broker、分区、副本、磁盘或外部系统的边界；结论必须能够从配置、指标、日志、偏移量和消息样本中复核。

机制轨迹从“流式处理设计模式”进入“单事件处理”，再到“使用本地状态”。逐步记录请求所属线程或进程、读取与写入的状态、确认发生的位置、超时与重试分支，以及失败后由谁接管。尤其区分“请求已发出”“broker已确认”“消费者已处理”和“业务结果已对账”，这些不是同一个完成点。

实验固定主题、分区键、消息ID和负载，只改变acks、批次、消费者数量、故障时点或网络条件之一。比较吞吐、P95/P99、消费者滞后、ISR、磁盘和最终消息集合，用“事件时间与处理时间不混淆，状态和变更日志可恢复，连接与窗口边界明确，乱序、重放和故障不会产生未说明结果”判定；若证据冲突，回到本节点缩小故障窗口，不用平均值掩盖尾部与丢失。

#### 使用本地状态

**目录节点 11/31。** “使用本地状态”服务于“用拓扑、时间、状态、流表二元性、窗口和处理保证解释设计模式，并完成Kafka Streams拓扑的构建、优化、测试、扩展与故障恢复”。先说明它接收的状态、配置或记录，再画出跨客户端、broker、分区、副本、磁盘或外部系统的边界；结论必须能够从配置、指标、日志、偏移量和消息样本中复核。

机制轨迹从“单事件处理”进入“使用本地状态”，再到“多阶段处理和重分区”。逐步记录请求所属线程或进程、读取与写入的状态、确认发生的位置、超时与重试分支，以及失败后由谁接管。尤其区分“请求已发出”“broker已确认”“消费者已处理”和“业务结果已对账”，这些不是同一个完成点。

实验固定主题、分区键、消息ID和负载，只改变acks、批次、消费者数量、故障时点或网络条件之一。比较吞吐、P95/P99、消费者滞后、ISR、磁盘和最终消息集合，用“事件时间与处理时间不混淆，状态和变更日志可恢复，连接与窗口边界明确，乱序、重放和故障不会产生未说明结果”判定；若证据冲突，回到本节点缩小故障窗口，不用平均值掩盖尾部与丢失。

#### 多阶段处理和重分区

**目录节点 12/31。** “多阶段处理和重分区”服务于“用拓扑、时间、状态、流表二元性、窗口和处理保证解释设计模式，并完成Kafka Streams拓扑的构建、优化、测试、扩展与故障恢复”。先说明它接收的状态、配置或记录，再画出跨客户端、broker、分区、副本、磁盘或外部系统的边界；结论必须能够从配置、指标、日志、偏移量和消息样本中复核。

机制轨迹从“使用本地状态”进入“多阶段处理和重分区”，再到“使用外部查找：流和表的连接”。逐步记录请求所属线程或进程、读取与写入的状态、确认发生的位置、超时与重试分支，以及失败后由谁接管。尤其区分“请求已发出”“broker已确认”“消费者已处理”和“业务结果已对账”，这些不是同一个完成点。

实验固定主题、分区键、消息ID和负载，只改变acks、批次、消费者数量、故障时点或网络条件之一。比较吞吐、P95/P99、消费者滞后、ISR、磁盘和最终消息集合，用“事件时间与处理时间不混淆，状态和变更日志可恢复，连接与窗口边界明确，乱序、重放和故障不会产生未说明结果”判定；若证据冲突，回到本节点缩小故障窗口，不用平均值掩盖尾部与丢失。

#### 使用外部查找：流和表的连接

**目录节点 13/31。** “使用外部查找：流和表的连接”服务于“用拓扑、时间、状态、流表二元性、窗口和处理保证解释设计模式，并完成Kafka Streams拓扑的构建、优化、测试、扩展与故障恢复”。先说明它接收的状态、配置或记录，再画出跨客户端、broker、分区、副本、磁盘或外部系统的边界；结论必须能够从配置、指标、日志、偏移量和消息样本中复核。

机制轨迹从“多阶段处理和重分区”进入“使用外部查找：流和表的连接”，再到“表与表的连接”。逐步记录请求所属线程或进程、读取与写入的状态、确认发生的位置、超时与重试分支，以及失败后由谁接管。尤其区分“请求已发出”“broker已确认”“消费者已处理”和“业务结果已对账”，这些不是同一个完成点。

实验固定主题、分区键、消息ID和负载，只改变acks、批次、消费者数量、故障时点或网络条件之一。比较吞吐、P95/P99、消费者滞后、ISR、磁盘和最终消息集合，用“事件时间与处理时间不混淆，状态和变更日志可恢复，连接与窗口边界明确，乱序、重放和故障不会产生未说明结果”判定；若证据冲突，回到本节点缩小故障窗口，不用平均值掩盖尾部与丢失。

#### 表与表的连接

**目录节点 14/31。** “表与表的连接”服务于“用拓扑、时间、状态、流表二元性、窗口和处理保证解释设计模式，并完成Kafka Streams拓扑的构建、优化、测试、扩展与故障恢复”。先说明它接收的状态、配置或记录，再画出跨客户端、broker、分区、副本、磁盘或外部系统的边界；结论必须能够从配置、指标、日志、偏移量和消息样本中复核。

机制轨迹从“使用外部查找：流和表的连接”进入“表与表的连接”，再到“流与流的连接”。逐步记录请求所属线程或进程、读取与写入的状态、确认发生的位置、超时与重试分支，以及失败后由谁接管。尤其区分“请求已发出”“broker已确认”“消费者已处理”和“业务结果已对账”，这些不是同一个完成点。

实验固定主题、分区键、消息ID和负载，只改变acks、批次、消费者数量、故障时点或网络条件之一。比较吞吐、P95/P99、消费者滞后、ISR、磁盘和最终消息集合，用“事件时间与处理时间不混淆，状态和变更日志可恢复，连接与窗口边界明确，乱序、重放和故障不会产生未说明结果”判定；若证据冲突，回到本节点缩小故障窗口，不用平均值掩盖尾部与丢失。

#### 流与流的连接

**目录节点 15/31。** “流与流的连接”服务于“用拓扑、时间、状态、流表二元性、窗口和处理保证解释设计模式，并完成Kafka Streams拓扑的构建、优化、测试、扩展与故障恢复”。先说明它接收的状态、配置或记录，再画出跨客户端、broker、分区、副本、磁盘或外部系统的边界；结论必须能够从配置、指标、日志、偏移量和消息样本中复核。

机制轨迹从“表与表的连接”进入“流与流的连接”，再到“乱序事件”。逐步记录请求所属线程或进程、读取与写入的状态、确认发生的位置、超时与重试分支，以及失败后由谁接管。尤其区分“请求已发出”“broker已确认”“消费者已处理”和“业务结果已对账”，这些不是同一个完成点。

实验固定主题、分区键、消息ID和负载，只改变acks、批次、消费者数量、故障时点或网络条件之一。比较吞吐、P95/P99、消费者滞后、ISR、磁盘和最终消息集合，用“事件时间与处理时间不混淆，状态和变更日志可恢复，连接与窗口边界明确，乱序、重放和故障不会产生未说明结果”判定；若证据冲突，回到本节点缩小故障窗口，不用平均值掩盖尾部与丢失。

#### 乱序事件

**目录节点 16/31。** “乱序事件”服务于“用拓扑、时间、状态、流表二元性、窗口和处理保证解释设计模式，并完成Kafka Streams拓扑的构建、优化、测试、扩展与故障恢复”。先说明它接收的状态、配置或记录，再画出跨客户端、broker、分区、副本、磁盘或外部系统的边界；结论必须能够从配置、指标、日志、偏移量和消息样本中复核。

机制轨迹从“流与流的连接”进入“乱序事件”，再到“重新处理”。逐步记录请求所属线程或进程、读取与写入的状态、确认发生的位置、超时与重试分支，以及失败后由谁接管。尤其区分“请求已发出”“broker已确认”“消费者已处理”和“业务结果已对账”，这些不是同一个完成点。

实验固定主题、分区键、消息ID和负载，只改变acks、批次、消费者数量、故障时点或网络条件之一。比较吞吐、P95/P99、消费者滞后、ISR、磁盘和最终消息集合，用“事件时间与处理时间不混淆，状态和变更日志可恢复，连接与窗口边界明确，乱序、重放和故障不会产生未说明结果”判定；若证据冲突，回到本节点缩小故障窗口，不用平均值掩盖尾部与丢失。

#### 重新处理

**目录节点 17/31。** “重新处理”服务于“用拓扑、时间、状态、流表二元性、窗口和处理保证解释设计模式，并完成Kafka Streams拓扑的构建、优化、测试、扩展与故障恢复”。先说明它接收的状态、配置或记录，再画出跨客户端、broker、分区、副本、磁盘或外部系统的边界；结论必须能够从配置、指标、日志、偏移量和消息样本中复核。

机制轨迹从“乱序事件”进入“重新处理”，再到“交互式查询”。逐步记录请求所属线程或进程、读取与写入的状态、确认发生的位置、超时与重试分支，以及失败后由谁接管。尤其区分“请求已发出”“broker已确认”“消费者已处理”和“业务结果已对账”，这些不是同一个完成点。

实验固定主题、分区键、消息ID和负载，只改变acks、批次、消费者数量、故障时点或网络条件之一。比较吞吐、P95/P99、消费者滞后、ISR、磁盘和最终消息集合，用“事件时间与处理时间不混淆，状态和变更日志可恢复，连接与窗口边界明确，乱序、重放和故障不会产生未说明结果”判定；若证据冲突，回到本节点缩小故障窗口，不用平均值掩盖尾部与丢失。

#### 交互式查询

**目录节点 18/31。** “交互式查询”服务于“用拓扑、时间、状态、流表二元性、窗口和处理保证解释设计模式，并完成Kafka Streams拓扑的构建、优化、测试、扩展与故障恢复”。先说明它接收的状态、配置或记录，再画出跨客户端、broker、分区、副本、磁盘或外部系统的边界；结论必须能够从配置、指标、日志、偏移量和消息样本中复核。

机制轨迹从“重新处理”进入“交互式查询”，再到“Streams示例”。逐步记录请求所属线程或进程、读取与写入的状态、确认发生的位置、超时与重试分支，以及失败后由谁接管。尤其区分“请求已发出”“broker已确认”“消费者已处理”和“业务结果已对账”，这些不是同一个完成点。

实验固定主题、分区键、消息ID和负载，只改变acks、批次、消费者数量、故障时点或网络条件之一。比较吞吐、P95/P99、消费者滞后、ISR、磁盘和最终消息集合，用“事件时间与处理时间不混淆，状态和变更日志可恢复，连接与窗口边界明确，乱序、重放和故障不会产生未说明结果”判定；若证据冲突，回到本节点缩小故障窗口，不用平均值掩盖尾部与丢失。

### Streams示例

**目录节点 19/31。** “Streams示例”服务于“用拓扑、时间、状态、流表二元性、窗口和处理保证解释设计模式，并完成Kafka Streams拓扑的构建、优化、测试、扩展与故障恢复”。先说明它接收的状态、配置或记录，再画出跨客户端、broker、分区、副本、磁盘或外部系统的边界；结论必须能够从配置、指标、日志、偏移量和消息样本中复核。

机制轨迹从“交互式查询”进入“Streams示例”，再到“字数统计”。逐步记录请求所属线程或进程、读取与写入的状态、确认发生的位置、超时与重试分支，以及失败后由谁接管。尤其区分“请求已发出”“broker已确认”“消费者已处理”和“业务结果已对账”，这些不是同一个完成点。

实验固定主题、分区键、消息ID和负载，只改变acks、批次、消费者数量、故障时点或网络条件之一。比较吞吐、P95/P99、消费者滞后、ISR、磁盘和最终消息集合，用“事件时间与处理时间不混淆，状态和变更日志可恢复，连接与窗口边界明确，乱序、重放和故障不会产生未说明结果”判定；若证据冲突，回到本节点缩小故障窗口，不用平均值掩盖尾部与丢失。

#### 字数统计

**目录节点 20/31。** “字数统计”服务于“用拓扑、时间、状态、流表二元性、窗口和处理保证解释设计模式，并完成Kafka Streams拓扑的构建、优化、测试、扩展与故障恢复”。先说明它接收的状态、配置或记录，再画出跨客户端、broker、分区、副本、磁盘或外部系统的边界；结论必须能够从配置、指标、日志、偏移量和消息样本中复核。

机制轨迹从“Streams示例”进入“字数统计”，再到“股票市场统计”。逐步记录请求所属线程或进程、读取与写入的状态、确认发生的位置、超时与重试分支，以及失败后由谁接管。尤其区分“请求已发出”“broker已确认”“消费者已处理”和“业务结果已对账”，这些不是同一个完成点。

实验固定主题、分区键、消息ID和负载，只改变acks、批次、消费者数量、故障时点或网络条件之一。比较吞吐、P95/P99、消费者滞后、ISR、磁盘和最终消息集合，用“事件时间与处理时间不混淆，状态和变更日志可恢复，连接与窗口边界明确，乱序、重放和故障不会产生未说明结果”判定；若证据冲突，回到本节点缩小故障窗口，不用平均值掩盖尾部与丢失。

#### 股票市场统计

**目录节点 21/31。** “股票市场统计”服务于“用拓扑、时间、状态、流表二元性、窗口和处理保证解释设计模式，并完成Kafka Streams拓扑的构建、优化、测试、扩展与故障恢复”。先说明它接收的状态、配置或记录，再画出跨客户端、broker、分区、副本、磁盘或外部系统的边界；结论必须能够从配置、指标、日志、偏移量和消息样本中复核。

机制轨迹从“字数统计”进入“股票市场统计”，再到“填充点击事件流”。逐步记录请求所属线程或进程、读取与写入的状态、确认发生的位置、超时与重试分支，以及失败后由谁接管。尤其区分“请求已发出”“broker已确认”“消费者已处理”和“业务结果已对账”，这些不是同一个完成点。

实验固定主题、分区键、消息ID和负载，只改变acks、批次、消费者数量、故障时点或网络条件之一。比较吞吐、P95/P99、消费者滞后、ISR、磁盘和最终消息集合，用“事件时间与处理时间不混淆，状态和变更日志可恢复，连接与窗口边界明确，乱序、重放和故障不会产生未说明结果”判定；若证据冲突，回到本节点缩小故障窗口，不用平均值掩盖尾部与丢失。

#### 填充点击事件流

**目录节点 22/31。** “填充点击事件流”服务于“用拓扑、时间、状态、流表二元性、窗口和处理保证解释设计模式，并完成Kafka Streams拓扑的构建、优化、测试、扩展与故障恢复”。先说明它接收的状态、配置或记录，再画出跨客户端、broker、分区、副本、磁盘或外部系统的边界；结论必须能够从配置、指标、日志、偏移量和消息样本中复核。

机制轨迹从“股票市场统计”进入“填充点击事件流”，再到“Streams架构概览”。逐步记录请求所属线程或进程、读取与写入的状态、确认发生的位置、超时与重试分支，以及失败后由谁接管。尤其区分“请求已发出”“broker已确认”“消费者已处理”和“业务结果已对账”，这些不是同一个完成点。

实验固定主题、分区键、消息ID和负载，只改变acks、批次、消费者数量、故障时点或网络条件之一。比较吞吐、P95/P99、消费者滞后、ISR、磁盘和最终消息集合，用“事件时间与处理时间不混淆，状态和变更日志可恢复，连接与窗口边界明确，乱序、重放和故障不会产生未说明结果”判定；若证据冲突，回到本节点缩小故障窗口，不用平均值掩盖尾部与丢失。

### Streams架构概览

**目录节点 23/31。** “Streams架构概览”服务于“用拓扑、时间、状态、流表二元性、窗口和处理保证解释设计模式，并完成Kafka Streams拓扑的构建、优化、测试、扩展与故障恢复”。先说明它接收的状态、配置或记录，再画出跨客户端、broker、分区、副本、磁盘或外部系统的边界；结论必须能够从配置、指标、日志、偏移量和消息样本中复核。

机制轨迹从“填充点击事件流”进入“Streams架构概览”，再到“构建拓扑”。逐步记录请求所属线程或进程、读取与写入的状态、确认发生的位置、超时与重试分支，以及失败后由谁接管。尤其区分“请求已发出”“broker已确认”“消费者已处理”和“业务结果已对账”，这些不是同一个完成点。

实验固定主题、分区键、消息ID和负载，只改变acks、批次、消费者数量、故障时点或网络条件之一。比较吞吐、P95/P99、消费者滞后、ISR、磁盘和最终消息集合，用“事件时间与处理时间不混淆，状态和变更日志可恢复，连接与窗口边界明确，乱序、重放和故障不会产生未说明结果”判定；若证据冲突，回到本节点缩小故障窗口，不用平均值掩盖尾部与丢失。

#### 构建拓扑

**目录节点 24/31。** “构建拓扑”服务于“用拓扑、时间、状态、流表二元性、窗口和处理保证解释设计模式，并完成Kafka Streams拓扑的构建、优化、测试、扩展与故障恢复”。先说明它接收的状态、配置或记录，再画出跨客户端、broker、分区、副本、磁盘或外部系统的边界；结论必须能够从配置、指标、日志、偏移量和消息样本中复核。

机制轨迹从“Streams架构概览”进入“构建拓扑”，再到“优化拓扑”。逐步记录请求所属线程或进程、读取与写入的状态、确认发生的位置、超时与重试分支，以及失败后由谁接管。尤其区分“请求已发出”“broker已确认”“消费者已处理”和“业务结果已对账”，这些不是同一个完成点。

实验固定主题、分区键、消息ID和负载，只改变acks、批次、消费者数量、故障时点或网络条件之一。比较吞吐、P95/P99、消费者滞后、ISR、磁盘和最终消息集合，用“事件时间与处理时间不混淆，状态和变更日志可恢复，连接与窗口边界明确，乱序、重放和故障不会产生未说明结果”判定；若证据冲突，回到本节点缩小故障窗口，不用平均值掩盖尾部与丢失。

#### 优化拓扑

**目录节点 25/31。** “优化拓扑”服务于“用拓扑、时间、状态、流表二元性、窗口和处理保证解释设计模式，并完成Kafka Streams拓扑的构建、优化、测试、扩展与故障恢复”。先说明它接收的状态、配置或记录，再画出跨客户端、broker、分区、副本、磁盘或外部系统的边界；结论必须能够从配置、指标、日志、偏移量和消息样本中复核。

机制轨迹从“构建拓扑”进入“优化拓扑”，再到“测试拓扑”。逐步记录请求所属线程或进程、读取与写入的状态、确认发生的位置、超时与重试分支，以及失败后由谁接管。尤其区分“请求已发出”“broker已确认”“消费者已处理”和“业务结果已对账”，这些不是同一个完成点。

实验固定主题、分区键、消息ID和负载，只改变acks、批次、消费者数量、故障时点或网络条件之一。比较吞吐、P95/P99、消费者滞后、ISR、磁盘和最终消息集合，用“事件时间与处理时间不混淆，状态和变更日志可恢复，连接与窗口边界明确，乱序、重放和故障不会产生未说明结果”判定；若证据冲突，回到本节点缩小故障窗口，不用平均值掩盖尾部与丢失。

#### 测试拓扑

**目录节点 26/31。** “测试拓扑”服务于“用拓扑、时间、状态、流表二元性、窗口和处理保证解释设计模式，并完成Kafka Streams拓扑的构建、优化、测试、扩展与故障恢复”。先说明它接收的状态、配置或记录，再画出跨客户端、broker、分区、副本、磁盘或外部系统的边界；结论必须能够从配置、指标、日志、偏移量和消息样本中复核。

机制轨迹从“优化拓扑”进入“测试拓扑”，再到“扩展拓扑”。逐步记录请求所属线程或进程、读取与写入的状态、确认发生的位置、超时与重试分支，以及失败后由谁接管。尤其区分“请求已发出”“broker已确认”“消费者已处理”和“业务结果已对账”，这些不是同一个完成点。

实验固定主题、分区键、消息ID和负载，只改变acks、批次、消费者数量、故障时点或网络条件之一。比较吞吐、P95/P99、消费者滞后、ISR、磁盘和最终消息集合，用“事件时间与处理时间不混淆，状态和变更日志可恢复，连接与窗口边界明确，乱序、重放和故障不会产生未说明结果”判定；若证据冲突，回到本节点缩小故障窗口，不用平均值掩盖尾部与丢失。

#### 扩展拓扑

**目录节点 27/31。** “扩展拓扑”服务于“用拓扑、时间、状态、流表二元性、窗口和处理保证解释设计模式，并完成Kafka Streams拓扑的构建、优化、测试、扩展与故障恢复”。先说明它接收的状态、配置或记录，再画出跨客户端、broker、分区、副本、磁盘或外部系统的边界；结论必须能够从配置、指标、日志、偏移量和消息样本中复核。

机制轨迹从“测试拓扑”进入“扩展拓扑”，再到“在故障中存活下来”。逐步记录请求所属线程或进程、读取与写入的状态、确认发生的位置、超时与重试分支，以及失败后由谁接管。尤其区分“请求已发出”“broker已确认”“消费者已处理”和“业务结果已对账”，这些不是同一个完成点。

实验固定主题、分区键、消息ID和负载，只改变acks、批次、消费者数量、故障时点或网络条件之一。比较吞吐、P95/P99、消费者滞后、ISR、磁盘和最终消息集合，用“事件时间与处理时间不混淆，状态和变更日志可恢复，连接与窗口边界明确，乱序、重放和故障不会产生未说明结果”判定；若证据冲突，回到本节点缩小故障窗口，不用平均值掩盖尾部与丢失。

#### 在故障中存活下来

**目录节点 28/31。** “在故障中存活下来”服务于“用拓扑、时间、状态、流表二元性、窗口和处理保证解释设计模式，并完成Kafka Streams拓扑的构建、优化、测试、扩展与故障恢复”。先说明它接收的状态、配置或记录，再画出跨客户端、broker、分区、副本、磁盘或外部系统的边界；结论必须能够从配置、指标、日志、偏移量和消息样本中复核。

机制轨迹从“扩展拓扑”进入“在故障中存活下来”，再到“流式处理应用场景”。逐步记录请求所属线程或进程、读取与写入的状态、确认发生的位置、超时与重试分支，以及失败后由谁接管。尤其区分“请求已发出”“broker已确认”“消费者已处理”和“业务结果已对账”，这些不是同一个完成点。

实验固定主题、分区键、消息ID和负载，只改变acks、批次、消费者数量、故障时点或网络条件之一。比较吞吐、P95/P99、消费者滞后、ISR、磁盘和最终消息集合，用“事件时间与处理时间不混淆，状态和变更日志可恢复，连接与窗口边界明确，乱序、重放和故障不会产生未说明结果”判定；若证据冲突，回到本节点缩小故障窗口，不用平均值掩盖尾部与丢失。

### 流式处理应用场景

**目录节点 29/31。** “流式处理应用场景”服务于“用拓扑、时间、状态、流表二元性、窗口和处理保证解释设计模式，并完成Kafka Streams拓扑的构建、优化、测试、扩展与故障恢复”。先说明它接收的状态、配置或记录，再画出跨客户端、broker、分区、副本、磁盘或外部系统的边界；结论必须能够从配置、指标、日志、偏移量和消息样本中复核。

机制轨迹从“在故障中存活下来”进入“流式处理应用场景”，再到“如何选择流式处理框架”。逐步记录请求所属线程或进程、读取与写入的状态、确认发生的位置、超时与重试分支，以及失败后由谁接管。尤其区分“请求已发出”“broker已确认”“消费者已处理”和“业务结果已对账”，这些不是同一个完成点。

实验固定主题、分区键、消息ID和负载，只改变acks、批次、消费者数量、故障时点或网络条件之一。比较吞吐、P95/P99、消费者滞后、ISR、磁盘和最终消息集合，用“事件时间与处理时间不混淆，状态和变更日志可恢复，连接与窗口边界明确，乱序、重放和故障不会产生未说明结果”判定；若证据冲突，回到本节点缩小故障窗口，不用平均值掩盖尾部与丢失。

### 如何选择流式处理框架

**目录节点 30/31。** “如何选择流式处理框架”服务于“用拓扑、时间、状态、流表二元性、窗口和处理保证解释设计模式，并完成Kafka Streams拓扑的构建、优化、测试、扩展与故障恢复”。先说明它接收的状态、配置或记录，再画出跨客户端、broker、分区、副本、磁盘或外部系统的边界；结论必须能够从配置、指标、日志、偏移量和消息样本中复核。

机制轨迹从“流式处理应用场景”进入“如何选择流式处理框架”，再到“小结”。逐步记录请求所属线程或进程、读取与写入的状态、确认发生的位置、超时与重试分支，以及失败后由谁接管。尤其区分“请求已发出”“broker已确认”“消费者已处理”和“业务结果已对账”，这些不是同一个完成点。

实验固定主题、分区键、消息ID和负载，只改变acks、批次、消费者数量、故障时点或网络条件之一。比较吞吐、P95/P99、消费者滞后、ISR、磁盘和最终消息集合，用“事件时间与处理时间不混淆，状态和变更日志可恢复，连接与窗口边界明确，乱序、重放和故障不会产生未说明结果”判定；若证据冲突，回到本节点缩小故障窗口，不用平均值掩盖尾部与丢失。

### 小结

**目录节点 31/31。** “小结”服务于“用拓扑、时间、状态、流表二元性、窗口和处理保证解释设计模式，并完成Kafka Streams拓扑的构建、优化、测试、扩展与故障恢复”。先说明它接收的状态、配置或记录，再画出跨客户端、broker、分区、副本、磁盘或外部系统的边界；结论必须能够从配置、指标、日志、偏移量和消息样本中复核。

机制轨迹从“如何选择流式处理框架”进入“小结”，再到“本章输出与验收”。逐步记录请求所属线程或进程、读取与写入的状态、确认发生的位置、超时与重试分支，以及失败后由谁接管。尤其区分“请求已发出”“broker已确认”“消费者已处理”和“业务结果已对账”，这些不是同一个完成点。

实验固定主题、分区键、消息ID和负载，只改变acks、批次、消费者数量、故障时点或网络条件之一。比较吞吐、P95/P99、消费者滞后、ISR、磁盘和最终消息集合，用“事件时间与处理时间不混淆，状态和变更日志可恢复，连接与窗口边界明确，乱序、重放和故障不会产生未说明结果”判定；若证据冲突，回到本节点缩小故障窗口，不用平均值掩盖尾部与丢失。

## 机制模型：数据面、控制面与存储

数据面从生产者序列化开始，经分区器、记录累加器与网络请求进入分区首领；追随者拉取后，高水位限定消费者可见范围。消费端通过poll取得记录，处理完成点和提交偏移量必须分别标记。记录键决定分区时，调整分区数可能改变后续映射，因此顺序承诺必须写到契约中。

控制面管理集群成员、控制器、分区首领、ISR、配置、配额和消费者群组。控制器或群组协调器切换期间会存在旧纪元、重试与最终一致窗口；客户端不能把一次元数据响应当作永久真相。状态机图必须标出纪元或generation，防止旧成员继续提交。

存储面以分区日志片段、偏移索引和时间索引为核心。删除策略按保留窗口移除旧片段，压实策略保留每个键的最新值并处理删除标记；它们不等于数据库行更新。磁盘、页缓存、批次压缩和副本获取共同决定吞吐与恢复时间。

## 最小可重放实验

```text
book=Kafka权威指南（第2版 / 2022）
unit=kfk-14-stream-processing
edition=Chinese 2nd edition 2022 / English 2nd edition 2021
focus=用拓扑、时间、状态、流表二元性、窗口和处理保证解释设计模式，并完成Kafka Streams拓扑的构建、优化、测试、扩展与故障恢复
artifact=流处理拓扑、窗口与乱序样本、状态恢复演练、TopologyTestDriver测试和框架选型矩阵
```

```java
// 伪代码：生产完成、业务处理和偏移提交是三个独立边界。
producer.send(record, (metadata, error) -> recordDelivery(metadata, error));
for (ConsumerRecord<String, Event> event : consumer.poll(timeout)) {
  applyIdempotently(event.key(), event.value());
  reconcileBusinessResult(event.topic(), event.partition(), event.offset());
}
commitOnlyProcessedOffsets();
```

```text
evidence = broker/client/version configuration
         + key/partition/offset/message-id trace
         + throughput/p50/p95/p99/lag/isr/disk metrics
         + retry/rebalance/broker/network fault injection
         + source-output reconciliation and rollback record
```

## 三步交互实验

<Stepper>
  <Step title="1. 建立主题、分区与副本拓扑">
    选择正式目录节点，调节分区与副本，观察键到分区、首领和追随者的映射。
    <Kfk14StreamProcessingTopologyLab />
  </Step>
  <Step title="2. 改变吞吐、确认与故障条件">
    调节批次、消息率、acks、最少ISR和消费者处理速度，比较吞吐、尾延迟、滞后与风险窗口。
    <Kfk14StreamProcessingReliabilityLab />
  </Step>
  <Step title="3. 通过端到端证据门">
    目录、配置、轨迹、指标、故障和消息对账六类材料齐全后才接受结论。
    <Kfk14StreamProcessingEvidenceLab />
  </Step>
</Stepper>

<Callout type="trap">
  **语义陷阱。**
  把逐条消费称为流处理，忽略时间语义、状态、窗口、重分区、乱序和处理保证。先写清确认点、处理点、提交点和最终业务不变量，再选择配置。
</Callout>

<Callout type="trap">
  **测量陷阱。**
  平均吞吐、进程在线、请求无异常或消费者滞后为零只能证明局部状态；必须保留P99、ISR变化、重试次数、分区偏移和消息ID集合，才能证明事件时间与处理时间不混淆，状态和变更日志可恢复，连接与窗口边界明确，乱序、重放和故障不会产生未说明结果。
</Callout>

<Callout type="warning">
  broker宕机、网络隔离、磁盘耗尽、ACL变更、偏移量重置、事务中止和跨集群切换只在隔离环境演练。预先规定数据损失、重复、延迟、磁盘和恢复时间上界。
</Callout>

## 独立证据与生产交接

目录证据保存O'Reilly第2版目录、本页节点与页面标题映射；环境证据保存Kafka与Java版本、broker和客户端配置、主题分区副本状态；运行证据保存消息生成规则、分区键、负载、分位延迟、吞吐、滞后、ISR与磁盘；故障证据保存注入时点、最后确认偏移量、恢复动作和最终消息集合。

性能判断同时给出生产与消费侧背压。增大批次和压缩通常提高吞吐，却会增加等待、CPU或单批失败影响；增加分区提高并行度，也增加文件句柄、控制面与再均衡成本。所有对比固定消息大小、键分布、副本与确认语义。

恢复判断从业务对账结束。broker重新加入、ISR恢复、消费者继续poll、MirrorMaker继续复制或Streams任务重启都只是阶段；还需核对每个消息ID的出现次数、顺序约束、状态存储和外部结果，并确认旧成员或旧站点不再错误写入。

## 本章回顾

重新完成“用拓扑、时间、状态、流表二元性、窗口和处理保证解释设计模式，并完成Kafka Streams拓扑的构建、优化、测试、扩展与故障恢复”：先固定第2版目录与版本，再从拓扑和状态机推导配置，通过基线、压力与故障实验测量，最后交付流处理拓扑、窗口与乱序样本、状态恢复演练、TopologyTestDriver测试和框架选型矩阵。只有“事件时间与处理时间不混淆，状态和变更日志可恢复，连接与窗口边界明确，乱序、重放和故障不会产生未说明结果”及反例都能被独立重放，本页才算完成。

## 复习与运行验收

<Exercises>
**问题 1**：为什么“第14章 流式处理”必须覆盖31个目录节点？
<Answer>这些节点共同组成“用拓扑、时间、状态、流表二元性、窗口和处理保证解释设计模式，并完成Kafka Streams拓扑的构建、优化、测试、扩展与故障恢复”的结构与证据链；漏项会使流处理拓扑、窗口与乱序样本、状态恢复演练、TopologyTestDriver测试和框架选型矩阵无法独立复现。</Answer>

**问题 2**：本页最小不变量是什么？

<Answer>
  事件时间与处理时间不混淆，状态和变更日志可恢复，连接与窗口边界明确，乱序、重放和故障不会产生未说明结果。需要配置、拓扑、偏移量、指标、故障轨迹和业务对账共同证明。
</Answer>

**问题 3**：怎样构造最小反例？

<Answer>
  固定消息、键、主题与副本，只改变acks、最少ISR、批次、消费者处理速度或单一故障点，比较确认、可见偏移量和最终消息集合。
</Answer>

**问题 4**：为什么不能用新版本功能替代第2版目录？

<Answer>
  课程分母由2021/2022第2版固定；后来版本可作差异说明，但不能覆盖原书节点、当时配置语义和本页验收。
</Answer>

**问题 5**：如何验证性能结论？

<Answer>
  固定消息大小、键分布、分区、副本与交付语义，重复测吞吐、P50/P95/P99、滞后、ISR、CPU、网络和磁盘，并对账结果。
</Answer>

**问题 6**：独立交接需要什么？

<Answer>需要完整目录映射、版本配置、拓扑与时序、流处理拓扑、窗口与乱序样本、状态恢复演练、TopologyTestDriver测试和框架选型矩阵、负载、故障、对账、停止、恢复、回退和责任人。</Answer>
</Exercises>

<Glossary>
  <GlossaryItem term="处理拓扑">
    处理拓扑在本页指向Kafka第2版中的具体状态、协议或操作边界；适用范围由“事件时间与处理时间不混淆，状态和变更日志可恢复，连接与窗口边界明确，乱序、重放和故障不会产生未说明结果”及故障反例共同限定。
  </GlossaryItem>
  <GlossaryItem term="事件时间">
    事件时间在本页指向Kafka第2版中的具体状态、协议或操作边界；适用范围由“事件时间与处理时间不混淆，状态和变更日志可恢复，连接与窗口边界明确，乱序、重放和故障不会产生未说明结果”及故障反例共同限定。
  </GlossaryItem>
  <GlossaryItem term="状态存储">
    状态存储在本页指向Kafka第2版中的具体状态、协议或操作边界；适用范围由“事件时间与处理时间不混淆，状态和变更日志可恢复，连接与窗口边界明确，乱序、重放和故障不会产生未说明结果”及故障反例共同限定。
  </GlossaryItem>
  <GlossaryItem term="流与表">
    流与表在本页指向Kafka第2版中的具体状态、协议或操作边界；适用范围由“事件时间与处理时间不混淆，状态和变更日志可恢复，连接与窗口边界明确，乱序、重放和故障不会产生未说明结果”及故障反例共同限定。
  </GlossaryItem>
  <GlossaryItem term="时间窗口">
    时间窗口在本页指向Kafka第2版中的具体状态、协议或操作边界；适用范围由“事件时间与处理时间不混淆，状态和变更日志可恢复，连接与窗口边界明确，乱序、重放和故障不会产生未说明结果”及故障反例共同限定。
  </GlossaryItem>
</Glossary>

[← 上一页：第13章 监控Kafka](/learn/kafka-definitive-guide/13-monitoring-kafka/kfk-13-monitoring-kafka) · [下一页：附录A 在其他操作系统中安装Kafka →](/learn/kafka-definitive-guide/15-appendix-a-installation/kfk-appendix-a-installation)

<Attribution
  adaptedFrom="Gwen Shapira等《Kafka权威指南》第2版"
  adaptedUrl="https://www.oreilly.com/library/view/kafkaquan-wei-zhi-nan-di-2ban/9787115601421/"
/>
