---
title: "第12章 Kong网关安全与集群高可用"
description: "依据孔庆雍《Kong网关：入门、实战与进阶》完整目录：组合网络限制、Admin API回路与Nginx配置，理解Kong集群缓存，并用HAProxy构建和故障验证高可用"
type: C
section: "Kong网关：入门、实战与进阶（Kong 2.0.5 / 2021）"
order: 12
draft: false
math: false
sourceUrl: "https://www.tenlong.com.tw/products/9787111689478"
demo: true
---

import { Objectives } from "@/components/mdx/objectives";
import { Term } from "@/components/mdx/term";
import { Callout } from "@/components/mdx/callout";
import { Answer, Exercises } from "@/components/mdx/exercises";
import { Glossary, GlossaryItem } from "@/components/mdx/glossary";
import { Attribution } from "@/components/mdx/attribution";

# 第12章 Kong网关安全与集群高可用

本课程对应孔庆雍著《Kong网关：入门、实战与进阶》，机械工业出版社2021年9月出版，472页，ISBN 9787111689478。权威目录为4篇、16章、附录A至D。作者配套源码与第1章示例固定kong:2.0.5，因此课程以Kong 2.0.5为行为边界。

全书正式分母为16章与4个附录，共20个正式单元、280个唯一章/附录/节节点；另设学习地图和总复习，共22页。后来的Kong Gateway路由表达式、新插件、Konnect、现代控制面或AI Gateway只能作为版本差异，不能替代原书内容。本页逐项覆盖12个目录节点，交付威胁边界、管理回路测试、缓存传播轨迹、HAProxy拓扑、节点故障注入和恢复对账。

<Objectives>

- 能解释“第12章 Kong网关安全与集群高可用”的12个目录节点，并绘制客户端、Kong、插件、上游与外部依赖的责任边界。
- 能比较基线、压力、配置变化、节点故障和恢复轨迹，判断路由、插件、缓存、重试与业务完成点。
- 能设计并复现威胁边界、管理回路测试、缓存传播轨迹、HAProxy拓扑、节点故障注入和恢复对账，用配置、请求、日志、指标与最终状态支持结论。
- 能分析“把前置HAProxy健康检查通过等同网关正确，把缓存传播延迟当故障，或将Admin API直接暴露给公网”为何失败，并写出停止、恢复、回退与独立交接条件。

</Objectives>

## 从一条经过网关的请求开始

先预测：请求携带Host、path、method、headers和协议进入Kong后会命中哪个Route，关联哪个Service，哪些插件按优先级进入rewrite、access、header_filter、body_filter与log阶段，Upstream怎样选择Target，最终响应和日志包含什么。观察前必须写下预测，再用真实轨迹纠正模型。

Kong 2.0.5的正确性不是“端口能通”。控制面接受配置、worker取得配置、路由匹配、插件执行、上游完成、客户端收到响应和业务状态落定是不同检查点。超时与重试还可能让一次客户端请求触发多次上游操作，非幂等操作必须单独验证。

## 核心词汇与版本边界

<Term def="Admin API回路是第12章 Kong网关安全与集群高可用的第1个核心概念，必须同时说明Kong 2.0.5对象、执行阶段、故障边界和观测证据">Admin API回路</Term>、<Term def="集群缓存是第12章 Kong网关安全与集群高可用的第2个核心概念，必须同时说明Kong 2.0.5对象、执行阶段、故障边界和观测证据">集群缓存</Term>、<Term def="HAProxy是第12章 Kong网关安全与集群高可用的第3个核心概念，必须同时说明Kong 2.0.5对象、执行阶段、故障边界和观测证据">HAProxy</Term>、<Term def="健康探测是第12章 Kong网关安全与集群高可用的第4个核心概念，必须同时说明Kong 2.0.5对象、执行阶段、故障边界和观测证据">健康探测</Term>、<Term def="故障转移是第12章 Kong网关安全与集群高可用的第5个核心概念，必须同时说明Kong 2.0.5对象、执行阶段、故障边界和观测证据">故障转移</Term>

这些词构成本页词汇。每个词都要回答：由谁创建，保存在哪里，在哪个请求或配置阶段生效，失败后留下什么，怎样观测，以及Kong 2.0.5之后哪些变化不属于本书。


## 原书目录逐节点重构

### 12.1 Kong网关安全配置

**目录节点 1/12。** Kong网关安全配置服务于“组合网络限制、Admin API回路与Nginx配置，理解Kong集群缓存，并用HAProxy构建和故障验证高可用”。先识别它在Kong 2.0.5中属于客户端、Nginx/OpenResty、Route、Service、Plugin、Upstream、Target、数据库、控制面还是外部平台，再写清输入、状态变化、输出与所有者。结论必须能从配置、命令输出、Admin API响应、代理轨迹、日志和业务结果复核。

机制轨迹从“本章输入与版本契约”进入“Kong网关安全配置”，随后到“网络层访问限制”。沿请求接入、路由匹配、插件阶段、上游选择、响应过滤和日志阶段记录时间线；配置加载、缓存传播、进程重载、节点故障与重试必须各有分支。任何“已启动”“返回2xx”或“界面可见”都只是中间状态。

实验固定Kong 2.0.5、OpenResty、数据库模式、请求集合、Route和Service，只改变路径或Host、插件、Target权重、失败节点、并发或部署状态之一。保存吞吐、P50/P95/P99、错误率、上游分布、worker和数据库指标，并用“管理面不可从业务入口越权访问，节点配置和缓存最终可解释，任一网关节点故障时代理流量在目标上界内恢复”验收。

#### 12.1.1 网络层访问限制

**目录节点 2/12。** 网络层访问限制服务于“组合网络限制、Admin API回路与Nginx配置，理解Kong集群缓存，并用HAProxy构建和故障验证高可用”。先识别它在Kong 2.0.5中属于客户端、Nginx/OpenResty、Route、Service、Plugin、Upstream、Target、数据库、控制面还是外部平台，再写清输入、状态变化、输出与所有者。结论必须能从配置、命令输出、Admin API响应、代理轨迹、日志和业务结果复核。

机制轨迹从“Kong网关安全配置”进入“网络层访问限制”，随后到“Kong API回路”。沿请求接入、路由匹配、插件阶段、上游选择、响应过滤和日志阶段记录时间线；配置加载、缓存传播、进程重载、节点故障与重试必须各有分支。任何“已启动”“返回2xx”或“界面可见”都只是中间状态。

实验固定Kong 2.0.5、OpenResty、数据库模式、请求集合、Route和Service，只改变路径或Host、插件、Target权重、失败节点、并发或部署状态之一。保存吞吐、P50/P95/P99、错误率、上游分布、worker和数据库指标，并用“管理面不可从业务入口越权访问，节点配置和缓存最终可解释，任一网关节点故障时代理流量在目标上界内恢复”验收。

#### 12.1.2 Kong API回路

**目录节点 3/12。** Kong API回路服务于“组合网络限制、Admin API回路与Nginx配置，理解Kong集群缓存，并用HAProxy构建和故障验证高可用”。先识别它在Kong 2.0.5中属于客户端、Nginx/OpenResty、Route、Service、Plugin、Upstream、Target、数据库、控制面还是外部平台，再写清输入、状态变化、输出与所有者。结论必须能从配置、命令输出、Admin API响应、代理轨迹、日志和业务结果复核。

机制轨迹从“网络层访问限制”进入“Kong API回路”，随后到“自定义Nginx配置”。沿请求接入、路由匹配、插件阶段、上游选择、响应过滤和日志阶段记录时间线；配置加载、缓存传播、进程重载、节点故障与重试必须各有分支。任何“已启动”“返回2xx”或“界面可见”都只是中间状态。

实验固定Kong 2.0.5、OpenResty、数据库模式、请求集合、Route和Service，只改变路径或Host、插件、Target权重、失败节点、并发或部署状态之一。保存吞吐、P50/P95/P99、错误率、上游分布、worker和数据库指标，并用“管理面不可从业务入口越权访问，节点配置和缓存最终可解释，任一网关节点故障时代理流量在目标上界内恢复”验收。

#### 12.1.3 自定义Nginx配置

**目录节点 4/12。** 自定义Nginx配置服务于“组合网络限制、Admin API回路与Nginx配置，理解Kong集群缓存，并用HAProxy构建和故障验证高可用”。先识别它在Kong 2.0.5中属于客户端、Nginx/OpenResty、Route、Service、Plugin、Upstream、Target、数据库、控制面还是外部平台，再写清输入、状态变化、输出与所有者。结论必须能从配置、命令输出、Admin API响应、代理轨迹、日志和业务结果复核。

机制轨迹从“Kong API回路”进入“自定义Nginx配置”，随后到“Kong集群”。沿请求接入、路由匹配、插件阶段、上游选择、响应过滤和日志阶段记录时间线；配置加载、缓存传播、进程重载、节点故障与重试必须各有分支。任何“已启动”“返回2xx”或“界面可见”都只是中间状态。

实验固定Kong 2.0.5、OpenResty、数据库模式、请求集合、Route和Service，只改变路径或Host、插件、Target权重、失败节点、并发或部署状态之一。保存吞吐、P50/P95/P99、错误率、上游分布、worker和数据库指标，并用“管理面不可从业务入口越权访问，节点配置和缓存最终可解释，任一网关节点故障时代理流量在目标上界内恢复”验收。

### 12.2 Kong集群

**目录节点 5/12。** Kong集群服务于“组合网络限制、Admin API回路与Nginx配置，理解Kong集群缓存，并用HAProxy构建和故障验证高可用”。先识别它在Kong 2.0.5中属于客户端、Nginx/OpenResty、Route、Service、Plugin、Upstream、Target、数据库、控制面还是外部平台，再写清输入、状态变化、输出与所有者。结论必须能从配置、命令输出、Admin API响应、代理轨迹、日志和业务结果复核。

机制轨迹从“自定义Nginx配置”进入“Kong集群”，随后到“Kong集群简介”。沿请求接入、路由匹配、插件阶段、上游选择、响应过滤和日志阶段记录时间线；配置加载、缓存传播、进程重载、节点故障与重试必须各有分支。任何“已启动”“返回2xx”或“界面可见”都只是中间状态。

实验固定Kong 2.0.5、OpenResty、数据库模式、请求集合、Route和Service，只改变路径或Host、插件、Target权重、失败节点、并发或部署状态之一。保存吞吐、P50/P95/P99、错误率、上游分布、worker和数据库指标，并用“管理面不可从业务入口越权访问，节点配置和缓存最终可解释，任一网关节点故障时代理流量在目标上界内恢复”验收。

#### 12.2.1 Kong集群简介

**目录节点 6/12。** Kong集群简介服务于“组合网络限制、Admin API回路与Nginx配置，理解Kong集群缓存，并用HAProxy构建和故障验证高可用”。先识别它在Kong 2.0.5中属于客户端、Nginx/OpenResty、Route、Service、Plugin、Upstream、Target、数据库、控制面还是外部平台，再写清输入、状态变化、输出与所有者。结论必须能从配置、命令输出、Admin API响应、代理轨迹、日志和业务结果复核。

机制轨迹从“Kong集群”进入“Kong集群简介”，随后到“Kong集群缓存”。沿请求接入、路由匹配、插件阶段、上游选择、响应过滤和日志阶段记录时间线；配置加载、缓存传播、进程重载、节点故障与重试必须各有分支。任何“已启动”“返回2xx”或“界面可见”都只是中间状态。

实验固定Kong 2.0.5、OpenResty、数据库模式、请求集合、Route和Service，只改变路径或Host、插件、Target权重、失败节点、并发或部署状态之一。保存吞吐、P50/P95/P99、错误率、上游分布、worker和数据库指标，并用“管理面不可从业务入口越权访问，节点配置和缓存最终可解释，任一网关节点故障时代理流量在目标上界内恢复”验收。

#### 12.2.2 Kong集群缓存

**目录节点 7/12。** Kong集群缓存服务于“组合网络限制、Admin API回路与Nginx配置，理解Kong集群缓存，并用HAProxy构建和故障验证高可用”。先识别它在Kong 2.0.5中属于客户端、Nginx/OpenResty、Route、Service、Plugin、Upstream、Target、数据库、控制面还是外部平台，再写清输入、状态变化、输出与所有者。结论必须能从配置、命令输出、Admin API响应、代理轨迹、日志和业务结果复核。

机制轨迹从“Kong集群简介”进入“Kong集群缓存”，随后到“Kong网关高可用”。沿请求接入、路由匹配、插件阶段、上游选择、响应过滤和日志阶段记录时间线；配置加载、缓存传播、进程重载、节点故障与重试必须各有分支。任何“已启动”“返回2xx”或“界面可见”都只是中间状态。

实验固定Kong 2.0.5、OpenResty、数据库模式、请求集合、Route和Service，只改变路径或Host、插件、Target权重、失败节点、并发或部署状态之一。保存吞吐、P50/P95/P99、错误率、上游分布、worker和数据库指标，并用“管理面不可从业务入口越权访问，节点配置和缓存最终可解释，任一网关节点故障时代理流量在目标上界内恢复”验收。

### 12.3 Kong网关高可用

**目录节点 8/12。** Kong网关高可用服务于“组合网络限制、Admin API回路与Nginx配置，理解Kong集群缓存，并用HAProxy构建和故障验证高可用”。先识别它在Kong 2.0.5中属于客户端、Nginx/OpenResty、Route、Service、Plugin、Upstream、Target、数据库、控制面还是外部平台，再写清输入、状态变化、输出与所有者。结论必须能从配置、命令输出、Admin API响应、代理轨迹、日志和业务结果复核。

机制轨迹从“Kong集群缓存”进入“Kong网关高可用”，随后到“架构设计”。沿请求接入、路由匹配、插件阶段、上游选择、响应过滤和日志阶段记录时间线；配置加载、缓存传播、进程重载、节点故障与重试必须各有分支。任何“已启动”“返回2xx”或“界面可见”都只是中间状态。

实验固定Kong 2.0.5、OpenResty、数据库模式、请求集合、Route和Service，只改变路径或Host、插件、Target权重、失败节点、并发或部署状态之一。保存吞吐、P50/P95/P99、错误率、上游分布、worker和数据库指标，并用“管理面不可从业务入口越权访问，节点配置和缓存最终可解释，任一网关节点故障时代理流量在目标上界内恢复”验收。

#### 12.3.1 架构设计

**目录节点 9/12。** 架构设计服务于“组合网络限制、Admin API回路与Nginx配置，理解Kong集群缓存，并用HAProxy构建和故障验证高可用”。先识别它在Kong 2.0.5中属于客户端、Nginx/OpenResty、Route、Service、Plugin、Upstream、Target、数据库、控制面还是外部平台，再写清输入、状态变化、输出与所有者。结论必须能从配置、命令输出、Admin API响应、代理轨迹、日志和业务结果复核。

机制轨迹从“Kong网关高可用”进入“架构设计”，随后到“引入HAProxy层”。沿请求接入、路由匹配、插件阶段、上游选择、响应过滤和日志阶段记录时间线；配置加载、缓存传播、进程重载、节点故障与重试必须各有分支。任何“已启动”“返回2xx”或“界面可见”都只是中间状态。

实验固定Kong 2.0.5、OpenResty、数据库模式、请求集合、Route和Service，只改变路径或Host、插件、Target权重、失败节点、并发或部署状态之一。保存吞吐、P50/P95/P99、错误率、上游分布、worker和数据库指标，并用“管理面不可从业务入口越权访问，节点配置和缓存最终可解释，任一网关节点故障时代理流量在目标上界内恢复”验收。

#### 12.3.2 引入HAProxy层

**目录节点 10/12。** 引入HAProxy层服务于“组合网络限制、Admin API回路与Nginx配置，理解Kong集群缓存，并用HAProxy构建和故障验证高可用”。先识别它在Kong 2.0.5中属于客户端、Nginx/OpenResty、Route、Service、Plugin、Upstream、Target、数据库、控制面还是外部平台，再写清输入、状态变化、输出与所有者。结论必须能从配置、命令输出、Admin API响应、代理轨迹、日志和业务结果复核。

机制轨迹从“架构设计”进入“引入HAProxy层”，随后到“高可用方案测试”。沿请求接入、路由匹配、插件阶段、上游选择、响应过滤和日志阶段记录时间线；配置加载、缓存传播、进程重载、节点故障与重试必须各有分支。任何“已启动”“返回2xx”或“界面可见”都只是中间状态。

实验固定Kong 2.0.5、OpenResty、数据库模式、请求集合、Route和Service，只改变路径或Host、插件、Target权重、失败节点、并发或部署状态之一。保存吞吐、P50/P95/P99、错误率、上游分布、worker和数据库指标，并用“管理面不可从业务入口越权访问，节点配置和缓存最终可解释，任一网关节点故障时代理流量在目标上界内恢复”验收。

#### 12.3.3 高可用方案测试

**目录节点 11/12。** 高可用方案测试服务于“组合网络限制、Admin API回路与Nginx配置，理解Kong集群缓存，并用HAProxy构建和故障验证高可用”。先识别它在Kong 2.0.5中属于客户端、Nginx/OpenResty、Route、Service、Plugin、Upstream、Target、数据库、控制面还是外部平台，再写清输入、状态变化、输出与所有者。结论必须能从配置、命令输出、Admin API响应、代理轨迹、日志和业务结果复核。

机制轨迹从“引入HAProxy层”进入“高可用方案测试”，随后到“本章小结”。沿请求接入、路由匹配、插件阶段、上游选择、响应过滤和日志阶段记录时间线；配置加载、缓存传播、进程重载、节点故障与重试必须各有分支。任何“已启动”“返回2xx”或“界面可见”都只是中间状态。

实验固定Kong 2.0.5、OpenResty、数据库模式、请求集合、Route和Service，只改变路径或Host、插件、Target权重、失败节点、并发或部署状态之一。保存吞吐、P50/P95/P99、错误率、上游分布、worker和数据库指标，并用“管理面不可从业务入口越权访问，节点配置和缓存最终可解释，任一网关节点故障时代理流量在目标上界内恢复”验收。

### 12.4 本章小结

**目录节点 12/12。** 本章小结服务于“组合网络限制、Admin API回路与Nginx配置，理解Kong集群缓存，并用HAProxy构建和故障验证高可用”。先识别它在Kong 2.0.5中属于客户端、Nginx/OpenResty、Route、Service、Plugin、Upstream、Target、数据库、控制面还是外部平台，再写清输入、状态变化、输出与所有者。结论必须能从配置、命令输出、Admin API响应、代理轨迹、日志和业务结果复核。

机制轨迹从“高可用方案测试”进入“本章小结”，随后到“本章证据门与回退”。沿请求接入、路由匹配、插件阶段、上游选择、响应过滤和日志阶段记录时间线；配置加载、缓存传播、进程重载、节点故障与重试必须各有分支。任何“已启动”“返回2xx”或“界面可见”都只是中间状态。

实验固定Kong 2.0.5、OpenResty、数据库模式、请求集合、Route和Service，只改变路径或Host、插件、Target权重、失败节点、并发或部署状态之一。保存吞吐、P50/P95/P99、错误率、上游分布、worker和数据库指标，并用“管理面不可从业务入口越权访问，节点配置和缓存最终可解释，任一网关节点故障时代理流量在目标上界内恢复”验收。

<Callout type="trap" title="语义陷阱">
  把前置HAProxy健康检查通过等同网关正确，把缓存传播延迟当故障，或将Admin
  API直接暴露给公网。先写清请求匹配、配置传播、插件阶段、上游副作用与最终业务不变量，再选择命令或插件。
</Callout>

<Callout type="trap" title="版本陷阱">
  原书及源码以Kong
  2.0.5为基线，Cassandra支持、传统集群、路由、插件PDK、Kuma和Kubernetes接口均有时代语境；新版结果可作迁移差异，不能证明本书行为。
</Callout>

<Callout type="warning" title="隔离实验">
  数据库迁移、配置覆盖、节点停止、网络隔离、Target摘除、证书和权限变更只在隔离环境演练。先保存配置、数据库和请求样本，规定停止条件与恢复上界。
</Callout>

## 独立证据与生产交接

目录证据保存16章4附录与280个唯一节点映射；环境证据保存Kong 2.0.5、OpenResty、LuaJIT、数据库、插件和外部平台版本；配置证据保存来源优先级、声明文件、Admin API请求响应和生效时间；请求证据保存关联ID、Route、Service、Plugin、Upstream、Target与最终业务结果。

性能判断固定请求集合、并发模型、连接复用、插件链、上游容量、数据库模式和日志策略。关闭认证、日志或健康检查换来的吞吐不是等价优化。平均值会隐藏worker停顿、缓存失效、数据库抖动、上游重试与节点切换，必须保留P95/P99和错误分类。

恢复判断从业务状态结束。进程重启、Pod Ready、HAProxy重新放行、配置重新可见或监控恢复都只是阶段；还要核对非幂等请求、上游副作用、Consumer身份、配置版本和旧节点是否停止提供错误结果。

## 本章回顾

重新完成“组合网络限制、Admin API回路与Nginx配置，理解Kong集群缓存，并用HAProxy构建和故障验证高可用”：固定Kong 2.0.5和权威目录，从请求与配置状态机推导边界，通过基线、压力与故障实验测量，最后交付威胁边界、管理回路测试、缓存传播轨迹、HAProxy拓扑、节点故障注入和恢复对账。只有“管理面不可从业务入口越权访问，节点配置和缓存最终可解释，任一网关节点故障时代理流量在目标上界内恢复”与反例都能由另一位读者独立重放，本页才算完成。

## 复习与运行验收

<Exercises>
**问题 1**：为什么“第12章 Kong网关安全与集群高可用”必须覆盖12个目录节点？
<Answer>这些节点共同组成“组合网络限制、Admin API回路与Nginx配置，理解Kong集群缓存，并用HAProxy构建和故障验证高可用”的请求、配置、运行与证据链；漏项会使威胁边界、管理回路测试、缓存传播轨迹、HAProxy拓扑、节点故障注入和恢复对账无法独立复现。</Answer>

**问题 2**：本页最小不变量是什么？

<Answer>
  管理面不可从业务入口越权访问，节点配置和缓存最终可解释，任一网关节点故障时代理流量在目标上界内恢复。需要版本、配置、请求轨迹、指标、故障与业务对账共同证明。
</Answer>

**问题 3**：怎样构造最小反例？

<Answer>
  固定Kong
  2.0.5、请求集合、Route、Service和上游，只改变一个匹配条件、插件、Target权重、失败节点或数据库状态，再比较决策轨迹与最终业务结果。
</Answer>

**问题 4**：为什么不能直接用新版Kong替代本页实验？

<Answer>
  原书与源码以Kong
  2.0.5为基线；路由、插件、部署、数据库和云原生接口后来均有变化，新版只能作为差异材料。
</Answer>

**问题 5**：如何验证性能结论？

<Answer>
  固定请求分布、连接、插件链、上游与可靠语义，重复测吞吐、P50/P95/P99、错误率、worker、数据库和上游分布，并独立对账。
</Answer>

**问题 6**：独立交接需要什么？

<Answer>需要完整目录映射、版本环境、配置快照、请求轨迹、威胁边界、管理回路测试、缓存传播轨迹、HAProxy拓扑、节点故障注入和恢复对账、负载、故障、对账、停止、恢复、回退和责任人。</Answer>
</Exercises>

<Glossary>
  <GlossaryItem term="Admin API回路">
    Admin API回路在本页对应Kong
    2.0.5中的具体对象、配置或状态；适用范围由“管理面不可从业务入口越权访问，节点配置和缓存最终可解释，任一网关节点故障时代理流量在目标上界内恢复”和故障反例共同限定。
  </GlossaryItem>
  <GlossaryItem term="集群缓存">
    集群缓存在本页对应Kong
    2.0.5中的具体对象、配置或状态；适用范围由“管理面不可从业务入口越权访问，节点配置和缓存最终可解释，任一网关节点故障时代理流量在目标上界内恢复”和故障反例共同限定。
  </GlossaryItem>
  <GlossaryItem term="HAProxy">
    HAProxy在本页对应Kong
    2.0.5中的具体对象、配置或状态；适用范围由“管理面不可从业务入口越权访问，节点配置和缓存最终可解释，任一网关节点故障时代理流量在目标上界内恢复”和故障反例共同限定。
  </GlossaryItem>
  <GlossaryItem term="健康探测">
    健康探测在本页对应Kong
    2.0.5中的具体对象、配置或状态；适用范围由“管理面不可从业务入口越权访问，节点配置和缓存最终可解释，任一网关节点故障时代理流量在目标上界内恢复”和故障反例共同限定。
  </GlossaryItem>
  <GlossaryItem term="故障转移">
    故障转移在本页对应Kong
    2.0.5中的具体对象、配置或状态；适用范围由“管理面不可从业务入口越权访问，节点配置和缓存最终可解释，任一网关节点故障时代理流量在目标上界内恢复”和故障反例共同限定。
  </GlossaryItem>
</Glossary>

[← 上一页：第11章 Kong网关运维](/learn/kong-gateway/11-11-operations/kga-11-operations) · [下一页：第13章 Kong网关结合微服务架构 →](/learn/kong-gateway/13-13-microservices-devops/kga-13-microservices-devops)

<Attribution
  adaptedFrom="孔庆雍《Kong网关：入门、实战与进阶》"
  adaptedUrl="https://www.tenlong.com.tw/products/9787111689478"
/>
