---
title: 《Kotlin编程权威指南》权威学习地图
description: >-
  《Kotlin编程权威指南》权威学习地图：沿Kotlin
  1.2原书顺序串联语言基础、值与集合、对象模型、函数式编程、Java互操作、Android和协程入门，通过全节点状态链、历史迁移对照和故障恢复完成验收。
type: C
section: Kotlin权威指南 · 学习地图
draft: true
math: false
sourceUrl: 'https://www.oreilly.com/library/view/kotlin-programming-the/9780135165188/'
demo: true
order: 1
qualityVersion: 2
practiceMode: design
sourceMode: independent-rewrite
---
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能说明沿Kotlin 1.2原书顺序串联语言基础、值与集合、对象模型、函数式编程、Java互操作、Android和协程入门，并画出输入、类型或对象边界与可见结果。
- 能运行本页保留的 Kotlin 切片，解释它在 Kotlin 1.2/JVM 语境中的编译或执行路径。
- 能操作覆盖、合同和恢复三层实验，让全部 27 个正式节点拥有可见教学因果。
- 能触发把23章压成几个主题页，或混入第二版与现代Android内容，凭27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表完成诊断、修复、复位与同输入重放。

</Objectives>

{/* KOTLIN_DEFINITIVE_GUIDE_QUALITY_V2 */}

## 从本章问题开始

《Kotlin编程权威指南》权威学习地图要解决的不是语法记忆，而是沿Kotlin 1.2原书顺序串联语言基础、值与集合、对象模型、函数式编程、Java互操作、Android和协程入门。先写出输入和期望结果，再运行最小切片；如果无法解释失败落在编译期、边界层还是运行期，程序即使输出一次正确答案也未通过。

本页不把 val 等同深度不可变，也不把简洁语法等同动态类型。Kotlin 的类型、可空性、求值规则和对象模型共同压缩非法状态；本章不变量是沿Kotlin 1.2原书顺序串联语言基础、值与集合、对象模型、函数式编程、Java互操作、Android和协程入门的输入、类型边界、求值结果和失败路径都能由独立读者重放。

## 来源范围与年代边界

O’Reilly 的[第1版书目与完整目录](https://www.oreilly.com/library/view/kotlin-programming-the/9780135165188/)只用于核定本页范围，[中文版出版记录](https://e.dangdang.com/touch/products/1901154363.html)用于交叉核对译本信息；未取得的原书正文不作忠实性宣称。《Kotlin编程权威指南》权威学习地图涉及的语言机制由[JetBrains/Android 一手文档](https://kotlinlang.org/docs/basic-syntax.html)独立核验，以下中文讲解、代码组织、图示、实验和练习均为独立教学重写。

第1版的教学坐标是2018年前后的 Kotlin 1.2、JVM、IntelliJ 与当时 Android/早期协程接口。[Kotlin 1.2 发布说明](https://kotlinlang.org/docs/whatsnew12.html)用于锁定历史能力；当前文档只提供迁移对照，Flow、Compose、MockK、现代 Gradle Kotlin DSL 和第二版新增内容不倒灌为原书内容。

## 本章机制与六个检查点

<Term def="职责边界在本页限定《Kotlin编程权威指南》权威学习地图允许进入系统的值与前置条件。">职责边界</Term>、<Term def="设计约束在本页标记沿Kotlin 1.2原书顺序串联语言基础、值与集合、对象模型、函数式编程、Java互操作、Android和协程入门中状态由谁创建、修改和释放。">设计约束</Term>、<Term def="不变量在本页说明Kotlin 1.2实现与当前迁移之间必须保持的语义。">不变量</Term>、<Term def="替换规则在本页保存能推翻把23章压成几个主题页，或混入第二版与现代Android内容的原始观察。">替换规则</Term>、<Term def="权衡记录在本页把错误停在记录决策而非继续扩散。">权衡记录</Term>、<Term def="验收产物在本页让另一位读者凭27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表得到相同结论。">验收产物</Term>。这些检查点共同回答沿Kotlin 1.2原书顺序串联语言基础、值与集合、对象模型、函数式编程、Java互操作、Android和协程入门，最终必须落到27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表，不能只停在API名称或目录词。

## 正式目录逐项深读

### Introducing Kotlin

Introducing Kotlin服务于沿Kotlin 1.2原书顺序串联语言基础、值与集合、对象模型、函数式编程、Java互操作、Android和协程入门。把这个目录坐标翻译成一个可执行或可判定合同，明确输入、状态变化、可见结果和反例，并以27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表完成独立复核。

在 《Kotlin编程权威指南》权威学习地图 的 界定责任 阶段，Introducing Kotlin先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入比较方案，使用同一份输入把正常路径与把23章压成几个主题页，或混入第二版与现代Android内容产生的反例并排保存。

对 Introducing Kotlin 的四级验收分别是：正文出现该坐标；解释它如何参与沿Kotlin 1.2原书顺序串联语言基础、值与集合、对象模型、函数式编程、Java互操作、Android和协程入门；在章专属实验中选择该节点并观察反馈；最后凭27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表回答练习、复位并重放。

### 1. Your First Kotlin Application

1. Your First Kotlin Application服务于沿Kotlin 1.2原书顺序串联语言基础、值与集合、对象模型、函数式编程、Java互操作、Android和协程入门。把这个目录坐标翻译成一个可执行或可判定合同，明确输入、状态变化、可见结果和反例，并以27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表完成独立复核。

在 《Kotlin编程权威指南》权威学习地图 的 比较方案 阶段，1. Your First Kotlin Application先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入声明不变量，使用同一份输入把正常路径与把23章压成几个主题页，或混入第二版与现代Android内容产生的反例并排保存。

对 1. Your First Kotlin Application 的四级验收分别是：正文出现该坐标；解释它如何参与沿Kotlin 1.2原书顺序串联语言基础、值与集合、对象模型、函数式编程、Java互操作、Android和协程入门；在章专属实验中选择该节点并观察反馈；最后凭27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表回答练习、复位并重放。

### 2. Variables, Constants, and Types

2. Variables, Constants, and Types服务于沿Kotlin 1.2原书顺序串联语言基础、值与集合、对象模型、函数式编程、Java互操作、Android和协程入门。画出类型关系、状态拥有者、初始化时刻和允许替换，再用编译失败或替身实现挑战边界；验收时检查27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表是否支持对象不变量。

在 《Kotlin编程权威指南》权威学习地图 的 声明不变量 阶段，2. Variables, Constants, and Types先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入验证替换，使用同一份输入把正常路径与把23章压成几个主题页，或混入第二版与现代Android内容产生的反例并排保存。

对 2. Variables, Constants, and Types 的四级验收分别是：正文出现该坐标；解释它如何参与沿Kotlin 1.2原书顺序串联语言基础、值与集合、对象模型、函数式编程、Java互操作、Android和协程入门；在章专属实验中选择该节点并观察反馈；最后凭27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表回答练习、复位并重放。

### 3. Conditionals

3. Conditionals服务于沿Kotlin 1.2原书顺序串联语言基础、值与集合、对象模型、函数式编程、Java互操作、Android和协程入门。把这个目录坐标翻译成一个可执行或可判定合同，明确输入、状态变化、可见结果和反例，并以27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表完成独立复核。

在 《Kotlin编程权威指南》权威学习地图 的 验证替换 阶段，3. Conditionals先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入记录决策，使用同一份输入把正常路径与把23章压成几个主题页，或混入第二版与现代Android内容产生的反例并排保存。

对 3. Conditionals 的四级验收分别是：正文出现该坐标；解释它如何参与沿Kotlin 1.2原书顺序串联语言基础、值与集合、对象模型、函数式编程、Java互操作、Android和协程入门；在章专属实验中选择该节点并观察反馈；最后凭27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表回答练习、复位并重放。

### 4. Functions

4. Functions服务于沿Kotlin 1.2原书顺序串联语言基础、值与集合、对象模型、函数式编程、Java互操作、Android和协程入门。把接收者、参数、返回值、捕获状态和副作用逐项展开，再判断简写是否保持相同求值顺序；27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表必须能区分语法缩短与合同改变。

在 《Kotlin编程权威指南》权威学习地图 的 记录决策 阶段，4. Functions先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入界定责任，使用同一份输入把正常路径与把23章压成几个主题页，或混入第二版与现代Android内容产生的反例并排保存。

对 4. Functions 的四级验收分别是：正文出现该坐标；解释它如何参与沿Kotlin 1.2原书顺序串联语言基础、值与集合、对象模型、函数式编程、Java互操作、Android和协程入门；在章专属实验中选择该节点并观察反馈；最后凭27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表回答练习、复位并重放。

### 5. Anonymous Functions and the Function Type

5. Anonymous Functions and the Function Type服务于沿Kotlin 1.2原书顺序串联语言基础、值与集合、对象模型、函数式编程、Java互操作、Android和协程入门。把接收者、参数、返回值、捕获状态和副作用逐项展开，再判断简写是否保持相同求值顺序；27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表必须能区分语法缩短与合同改变。

在 《Kotlin编程权威指南》权威学习地图 的 界定责任 阶段，5. Anonymous Functions and the Function Type先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入比较方案，使用同一份输入把正常路径与把23章压成几个主题页，或混入第二版与现代Android内容产生的反例并排保存。

对 5. Anonymous Functions and the Function Type 的四级验收分别是：正文出现该坐标；解释它如何参与沿Kotlin 1.2原书顺序串联语言基础、值与集合、对象模型、函数式编程、Java互操作、Android和协程入门；在章专属实验中选择该节点并观察反馈；最后凭27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表回答练习、复位并重放。

### 6. Null Safety and Exceptions

6. Null Safety and Exceptions服务于沿Kotlin 1.2原书顺序串联语言基础、值与集合、对象模型、函数式编程、Java互操作、Android和协程入门。这里要把缺失值、异常来源和调用者责任分别放进类型或失败合同，禁止用强制解包、空捕获或虚假默认值擦除风险；用27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表定位最早失效处。

在 《Kotlin编程权威指南》权威学习地图 的 比较方案 阶段，6. Null Safety and Exceptions先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入声明不变量，使用同一份输入把正常路径与把23章压成几个主题页，或混入第二版与现代Android内容产生的反例并排保存。

对 6. Null Safety and Exceptions 的四级验收分别是：正文出现该坐标；解释它如何参与沿Kotlin 1.2原书顺序串联语言基础、值与集合、对象模型、函数式编程、Java互操作、Android和协程入门；在章专属实验中选择该节点并观察反馈；最后凭27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表回答练习、复位并重放。

### 7. Strings

7. Strings服务于沿Kotlin 1.2原书顺序串联语言基础、值与集合、对象模型、函数式编程、Java互操作、Android和协程入门。固定集合或数值样本，记录索引、类型、转换和求值次数，仅改变一个边界条件；由27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表判断只读、不可变、精度或惰性结论是否成立。

在 《Kotlin编程权威指南》权威学习地图 的 声明不变量 阶段，7. Strings先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入验证替换，使用同一份输入把正常路径与把23章压成几个主题页，或混入第二版与现代Android内容产生的反例并排保存。

对 7. Strings 的四级验收分别是：正文出现该坐标；解释它如何参与沿Kotlin 1.2原书顺序串联语言基础、值与集合、对象模型、函数式编程、Java互操作、Android和协程入门；在章专属实验中选择该节点并观察反馈；最后凭27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表回答练习、复位并重放。

### 8. Numbers

8. Numbers服务于沿Kotlin 1.2原书顺序串联语言基础、值与集合、对象模型、函数式编程、Java互操作、Android和协程入门。固定集合或数值样本，记录索引、类型、转换和求值次数，仅改变一个边界条件；由27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表判断只读、不可变、精度或惰性结论是否成立。

在 《Kotlin编程权威指南》权威学习地图 的 验证替换 阶段，8. Numbers先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入记录决策，使用同一份输入把正常路径与把23章压成几个主题页，或混入第二版与现代Android内容产生的反例并排保存。

对 8. Numbers 的四级验收分别是：正文出现该坐标；解释它如何参与沿Kotlin 1.2原书顺序串联语言基础、值与集合、对象模型、函数式编程、Java互操作、Android和协程入门；在章专属实验中选择该节点并观察反馈；最后凭27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表回答练习、复位并重放。

### 9. Standard Functions

9. Standard Functions服务于沿Kotlin 1.2原书顺序串联语言基础、值与集合、对象模型、函数式编程、Java互操作、Android和协程入门。把接收者、参数、返回值、捕获状态和副作用逐项展开，再判断简写是否保持相同求值顺序；27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表必须能区分语法缩短与合同改变。

在 《Kotlin编程权威指南》权威学习地图 的 记录决策 阶段，9. Standard Functions先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入界定责任，使用同一份输入把正常路径与把23章压成几个主题页，或混入第二版与现代Android内容产生的反例并排保存。

对 9. Standard Functions 的四级验收分别是：正文出现该坐标；解释它如何参与沿Kotlin 1.2原书顺序串联语言基础、值与集合、对象模型、函数式编程、Java互操作、Android和协程入门；在章专属实验中选择该节点并观察反馈；最后凭27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表回答练习、复位并重放。

### 10. Lists and Sets

10. Lists and Sets服务于沿Kotlin 1.2原书顺序串联语言基础、值与集合、对象模型、函数式编程、Java互操作、Android和协程入门。固定集合或数值样本，记录索引、类型、转换和求值次数，仅改变一个边界条件；由27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表判断只读、不可变、精度或惰性结论是否成立。

在 《Kotlin编程权威指南》权威学习地图 的 界定责任 阶段，10. Lists and Sets先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入比较方案，使用同一份输入把正常路径与把23章压成几个主题页，或混入第二版与现代Android内容产生的反例并排保存。

对 10. Lists and Sets 的四级验收分别是：正文出现该坐标；解释它如何参与沿Kotlin 1.2原书顺序串联语言基础、值与集合、对象模型、函数式编程、Java互操作、Android和协程入门；在章专属实验中选择该节点并观察反馈；最后凭27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表回答练习、复位并重放。

### 11. Maps

11. Maps服务于沿Kotlin 1.2原书顺序串联语言基础、值与集合、对象模型、函数式编程、Java互操作、Android和协程入门。固定集合或数值样本，记录索引、类型、转换和求值次数，仅改变一个边界条件；由27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表判断只读、不可变、精度或惰性结论是否成立。

在 《Kotlin编程权威指南》权威学习地图 的 比较方案 阶段，11. Maps先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入声明不变量，使用同一份输入把正常路径与把23章压成几个主题页，或混入第二版与现代Android内容产生的反例并排保存。

对 11. Maps 的四级验收分别是：正文出现该坐标；解释它如何参与沿Kotlin 1.2原书顺序串联语言基础、值与集合、对象模型、函数式编程、Java互操作、Android和协程入门；在章专属实验中选择该节点并观察反馈；最后凭27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表回答练习、复位并重放。

### 12. Defining Classes

12. Defining Classes服务于沿Kotlin 1.2原书顺序串联语言基础、值与集合、对象模型、函数式编程、Java互操作、Android和协程入门。画出类型关系、状态拥有者、初始化时刻和允许替换，再用编译失败或替身实现挑战边界；验收时检查27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表是否支持对象不变量。

在 《Kotlin编程权威指南》权威学习地图 的 声明不变量 阶段，12. Defining Classes先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入验证替换，使用同一份输入把正常路径与把23章压成几个主题页，或混入第二版与现代Android内容产生的反例并排保存。

对 12. Defining Classes 的四级验收分别是：正文出现该坐标；解释它如何参与沿Kotlin 1.2原书顺序串联语言基础、值与集合、对象模型、函数式编程、Java互操作、Android和协程入门；在章专属实验中选择该节点并观察反馈；最后凭27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表回答练习、复位并重放。

### 13. Initialization

13. Initialization服务于沿Kotlin 1.2原书顺序串联语言基础、值与集合、对象模型、函数式编程、Java互操作、Android和协程入门。把这个目录坐标翻译成一个可执行或可判定合同，明确输入、状态变化、可见结果和反例，并以27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表完成独立复核。

在 《Kotlin编程权威指南》权威学习地图 的 验证替换 阶段，13. Initialization先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入记录决策，使用同一份输入把正常路径与把23章压成几个主题页，或混入第二版与现代Android内容产生的反例并排保存。

对 13. Initialization 的四级验收分别是：正文出现该坐标；解释它如何参与沿Kotlin 1.2原书顺序串联语言基础、值与集合、对象模型、函数式编程、Java互操作、Android和协程入门；在章专属实验中选择该节点并观察反馈；最后凭27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表回答练习、复位并重放。

### 14. Inheritance

14. Inheritance服务于沿Kotlin 1.2原书顺序串联语言基础、值与集合、对象模型、函数式编程、Java互操作、Android和协程入门。画出类型关系、状态拥有者、初始化时刻和允许替换，再用编译失败或替身实现挑战边界；验收时检查27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表是否支持对象不变量。

在 《Kotlin编程权威指南》权威学习地图 的 记录决策 阶段，14. Inheritance先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入界定责任，使用同一份输入把正常路径与把23章压成几个主题页，或混入第二版与现代Android内容产生的反例并排保存。

对 14. Inheritance 的四级验收分别是：正文出现该坐标；解释它如何参与沿Kotlin 1.2原书顺序串联语言基础、值与集合、对象模型、函数式编程、Java互操作、Android和协程入门；在章专属实验中选择该节点并观察反馈；最后凭27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表回答练习、复位并重放。

### 15. Objects

15. Objects服务于沿Kotlin 1.2原书顺序串联语言基础、值与集合、对象模型、函数式编程、Java互操作、Android和协程入门。画出类型关系、状态拥有者、初始化时刻和允许替换，再用编译失败或替身实现挑战边界；验收时检查27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表是否支持对象不变量。

在 《Kotlin编程权威指南》权威学习地图 的 界定责任 阶段，15. Objects先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入比较方案，使用同一份输入把正常路径与把23章压成几个主题页，或混入第二版与现代Android内容产生的反例并排保存。

对 15. Objects 的四级验收分别是：正文出现该坐标；解释它如何参与沿Kotlin 1.2原书顺序串联语言基础、值与集合、对象模型、函数式编程、Java互操作、Android和协程入门；在章专属实验中选择该节点并观察反馈；最后凭27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表回答练习、复位并重放。

### 16. Interfaces and Abstract Classes

16. Interfaces and Abstract Classes服务于沿Kotlin 1.2原书顺序串联语言基础、值与集合、对象模型、函数式编程、Java互操作、Android和协程入门。画出类型关系、状态拥有者、初始化时刻和允许替换，再用编译失败或替身实现挑战边界；验收时检查27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表是否支持对象不变量。

在 《Kotlin编程权威指南》权威学习地图 的 比较方案 阶段，16. Interfaces and Abstract Classes先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入声明不变量，使用同一份输入把正常路径与把23章压成几个主题页，或混入第二版与现代Android内容产生的反例并排保存。

对 16. Interfaces and Abstract Classes 的四级验收分别是：正文出现该坐标；解释它如何参与沿Kotlin 1.2原书顺序串联语言基础、值与集合、对象模型、函数式编程、Java互操作、Android和协程入门；在章专属实验中选择该节点并观察反馈；最后凭27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表回答练习、复位并重放。

### 17. Generics

17. Generics服务于沿Kotlin 1.2原书顺序串联语言基础、值与集合、对象模型、函数式编程、Java互操作、Android和协程入门。画出类型关系、状态拥有者、初始化时刻和允许替换，再用编译失败或替身实现挑战边界；验收时检查27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表是否支持对象不变量。

在 《Kotlin编程权威指南》权威学习地图 的 声明不变量 阶段，17. Generics先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入验证替换，使用同一份输入把正常路径与把23章压成几个主题页，或混入第二版与现代Android内容产生的反例并排保存。

对 17. Generics 的四级验收分别是：正文出现该坐标；解释它如何参与沿Kotlin 1.2原书顺序串联语言基础、值与集合、对象模型、函数式编程、Java互操作、Android和协程入门；在章专属实验中选择该节点并观察反馈；最后凭27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表回答练习、复位并重放。

### 18. Extensions

18. Extensions服务于沿Kotlin 1.2原书顺序串联语言基础、值与集合、对象模型、函数式编程、Java互操作、Android和协程入门。把这个目录坐标翻译成一个可执行或可判定合同，明确输入、状态变化、可见结果和反例，并以27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表完成独立复核。

在 《Kotlin编程权威指南》权威学习地图 的 验证替换 阶段，18. Extensions先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入记录决策，使用同一份输入把正常路径与把23章压成几个主题页，或混入第二版与现代Android内容产生的反例并排保存。

对 18. Extensions 的四级验收分别是：正文出现该坐标；解释它如何参与沿Kotlin 1.2原书顺序串联语言基础、值与集合、对象模型、函数式编程、Java互操作、Android和协程入门；在章专属实验中选择该节点并观察反馈；最后凭27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表回答练习、复位并重放。

### 19. Functional Programming Basics

19. Functional Programming Basics服务于沿Kotlin 1.2原书顺序串联语言基础、值与集合、对象模型、函数式编程、Java互操作、Android和协程入门。把接收者、参数、返回值、捕获状态和副作用逐项展开，再判断简写是否保持相同求值顺序；27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表必须能区分语法缩短与合同改变。

在 《Kotlin编程权威指南》权威学习地图 的 记录决策 阶段，19. Functional Programming Basics先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入界定责任，使用同一份输入把正常路径与把23章压成几个主题页，或混入第二版与现代Android内容产生的反例并排保存。

对 19. Functional Programming Basics 的四级验收分别是：正文出现该坐标；解释它如何参与沿Kotlin 1.2原书顺序串联语言基础、值与集合、对象模型、函数式编程、Java互操作、Android和协程入门；在章专属实验中选择该节点并观察反馈；最后凭27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表回答练习、复位并重放。

### 20. Java Interoperability

20. Java Interoperability服务于沿Kotlin 1.2原书顺序串联语言基础、值与集合、对象模型、函数式编程、Java互操作、Android和协程入门。把这个目录坐标翻译成一个可执行或可判定合同，明确输入、状态变化、可见结果和反例，并以27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表完成独立复核。

在 《Kotlin编程权威指南》权威学习地图 的 界定责任 阶段，20. Java Interoperability先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入比较方案，使用同一份输入把正常路径与把23章压成几个主题页，或混入第二版与现代Android内容产生的反例并排保存。

对 20. Java Interoperability 的四级验收分别是：正文出现该坐标；解释它如何参与沿Kotlin 1.2原书顺序串联语言基础、值与集合、对象模型、函数式编程、Java互操作、Android和协程入门；在章专属实验中选择该节点并观察反馈；最后凭27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表回答练习、复位并重放。

### 21. Building Your First Android Application with Kotlin

21. Building Your First Android Application with Kotlin服务于沿Kotlin 1.2原书顺序串联语言基础、值与集合、对象模型、函数式编程、Java互操作、Android和协程入门。把接收者、参数、返回值、捕获状态和副作用逐项展开，再判断简写是否保持相同求值顺序；27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表必须能区分语法缩短与合同改变。

在 《Kotlin编程权威指南》权威学习地图 的 比较方案 阶段，21. Building Your First Android Application with Kotlin先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入声明不变量，使用同一份输入把正常路径与把23章压成几个主题页，或混入第二版与现代Android内容产生的反例并排保存。

对 21. Building Your First Android Application with Kotlin 的四级验收分别是：正文出现该坐标；解释它如何参与沿Kotlin 1.2原书顺序串联语言基础、值与集合、对象模型、函数式编程、Java互操作、Android和协程入门；在章专属实验中选择该节点并观察反馈；最后凭27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表回答练习、复位并重放。

### 22. Introduction to Coroutines

22. Introduction to Coroutines服务于沿Kotlin 1.2原书顺序串联语言基础、值与集合、对象模型、函数式编程、Java互操作、Android和协程入门。将生命周期、线程或任务所有者和取消出口放在同一时间线上，区分2018年的接口身份与当前迁移方案；用27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表验证状态恢复和失败隔离。

在 《Kotlin编程权威指南》权威学习地图 的 声明不变量 阶段，22. Introduction to Coroutines先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入验证替换，使用同一份输入把正常路径与把23章压成几个主题页，或混入第二版与现代Android内容产生的反例并排保存。

对 22. Introduction to Coroutines 的四级验收分别是：正文出现该坐标；解释它如何参与沿Kotlin 1.2原书顺序串联语言基础、值与集合、对象模型、函数式编程、Java互操作、Android和协程入门；在章专属实验中选择该节点并观察反馈；最后凭27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表回答练习、复位并重放。

### 23. Afterword

23. Afterword服务于沿Kotlin 1.2原书顺序串联语言基础、值与集合、对象模型、函数式编程、Java互操作、Android和协程入门。把这个目录坐标翻译成一个可执行或可判定合同，明确输入、状态变化、可见结果和反例，并以27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表完成独立复核。

在 《Kotlin编程权威指南》权威学习地图 的 验证替换 阶段，23. Afterword先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入记录决策，使用同一份输入把正常路径与把23章压成几个主题页，或混入第二版与现代Android内容产生的反例并排保存。

对 23. Afterword 的四级验收分别是：正文出现该坐标；解释它如何参与沿Kotlin 1.2原书顺序串联语言基础、值与集合、对象模型、函数式编程、Java互操作、Android和协程入门；在章专属实验中选择该节点并观察反馈；最后凭27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表回答练习、复位并重放。

### A. More Challenges

A. More Challenges服务于沿Kotlin 1.2原书顺序串联语言基础、值与集合、对象模型、函数式编程、Java互操作、Android和协程入门。先把题目改写成输入、输出、约束和失败样例，再运行正常、边界与非法三组断言；挑战完成的证据是27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表，不是一次示例输出。

在 《Kotlin编程权威指南》权威学习地图 的 记录决策 阶段，A. More Challenges先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入界定责任，使用同一份输入把正常路径与把23章压成几个主题页，或混入第二版与现代Android内容产生的反例并排保存。

对 A. More Challenges 的四级验收分别是：正文出现该坐标；解释它如何参与沿Kotlin 1.2原书顺序串联语言基础、值与集合、对象模型、函数式编程、Java互操作、Android和协程入门；在章专属实验中选择该节点并观察反馈；最后凭27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表回答练习、复位并重放。

### Glossary

Glossary服务于沿Kotlin 1.2原书顺序串联语言基础、值与集合、对象模型、函数式编程、Java互操作、Android和协程入门。把这个目录坐标翻译成一个可执行或可判定合同，明确输入、状态变化、可见结果和反例，并以27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表完成独立复核。

在 《Kotlin编程权威指南》权威学习地图 的 界定责任 阶段，Glossary先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入比较方案，使用同一份输入把正常路径与把23章压成几个主题页，或混入第二版与现代Android内容产生的反例并排保存。

对 Glossary 的四级验收分别是：正文出现该坐标；解释它如何参与沿Kotlin 1.2原书顺序串联语言基础、值与集合、对象模型、函数式编程、Java互操作、Android和协程入门；在章专属实验中选择该节点并观察反馈；最后凭27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表回答练习、复位并重放。

### Index

Index服务于沿Kotlin 1.2原书顺序串联语言基础、值与集合、对象模型、函数式编程、Java互操作、Android和协程入门。把这个目录坐标翻译成一个可执行或可判定合同，明确输入、状态变化、可见结果和反例，并以27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表完成独立复核。

在 《Kotlin编程权威指南》权威学习地图 的 比较方案 阶段，Index先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入声明不变量，使用同一份输入把正常路径与把23章压成几个主题页，或混入第二版与现代Android内容产生的反例并排保存。

对 Index 的四级验收分别是：正文出现该坐标；解释它如何参与沿Kotlin 1.2原书顺序串联语言基础、值与集合、对象模型、函数式编程、Java互操作、Android和协程入门；在章专属实验中选择该节点并观察反馈；最后凭27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表回答练习、复位并重放。

## 最小可执行切片

```kotlin
data class Coverage(val unit: String, val nodes: Int, val passed: Boolean)

val ledger = listOf(
    Coverage("language foundations", 4, true),
    Coverage("object model", 7, false),
)
check(ledger.map { it.nodes }.sum() > 0)
```

运行切片时固定 Kotlin/JDK 或 Android 工具链版本、输入数据和命令；保存标准输出、编译诊断及断言。迁移实验必须另建记录，不能静默改写2018年的历史坐标。

<Callout type="trap" title="本章核心陷阱">
  把23章压成几个主题页，或混入第二版与现代Android内容。先缩小到最小反例，明确是类型合同、对象所有权、求值顺序还是平台边界失效，再用同输入验证修复。
</Callout>

<Callout type="trap" title="把当前惯例倒灌进第一版">
  《Kotlin编程权威指南》权威学习地图保留 Kotlin 1.2/JVM 的历史身份；当前 Kotlin 或 Android 方案只作单独迁移坐标，不能据此改写第一版目录和接口身份。
</Callout>

<Callout type="trap" title="只保留成功输出">
  27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表必须同时包含正常、边界和失败路径；如果故障后不能恢复并复位，本页实践闭环仍未完成。
</Callout>

## 练习、答案与四级证据

<Exercises>

**问题 1：历史与迁移。** 怎样既保留 Kotlin 1.2 的教学身份，又使用当前官方资料核验机制？

<Answer>
  先以[Kotlin 1.2发布说明](https://kotlinlang.org/docs/whatsnew12.html)和目录锁定历史范围，再用[当前技术文档](https://kotlinlang.org/docs/basic-syntax.html)建立单独迁移记录。比较相同输入、结果和不变量；只改变版本坐标，不把当前API伪装成第一版内容。
</Answer>

**问题 2：逐节点验证。** 本页 27 个正式节点怎样从出现升级到可复查证据？

<Answer>
  - **Introducing Kotlin**：在界定责任阶段保存输入与结果，触发把23章压成几个主题页，或混入第二版与现代Android内容后恢复初态，并由27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表确认语义未漂移。
  - **1. Your First Kotlin Application**：在比较方案阶段保存输入与结果，触发把23章压成几个主题页，或混入第二版与现代Android内容后恢复初态，并由27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表确认语义未漂移。
  - **2. Variables, Constants, and Types**：在声明不变量阶段保存输入与结果，触发把23章压成几个主题页，或混入第二版与现代Android内容后恢复初态，并由27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表确认语义未漂移。
  - **3. Conditionals**：在验证替换阶段保存输入与结果，触发把23章压成几个主题页，或混入第二版与现代Android内容后恢复初态，并由27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表确认语义未漂移。
  - **4. Functions**：在记录决策阶段保存输入与结果，触发把23章压成几个主题页，或混入第二版与现代Android内容后恢复初态，并由27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表确认语义未漂移。
  - **5. Anonymous Functions and the Function Type**：在界定责任阶段保存输入与结果，触发把23章压成几个主题页，或混入第二版与现代Android内容后恢复初态，并由27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表确认语义未漂移。
  - **6. Null Safety and Exceptions**：在比较方案阶段保存输入与结果，触发把23章压成几个主题页，或混入第二版与现代Android内容后恢复初态，并由27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表确认语义未漂移。
  - **7. Strings**：在声明不变量阶段保存输入与结果，触发把23章压成几个主题页，或混入第二版与现代Android内容后恢复初态，并由27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表确认语义未漂移。
  - **8. Numbers**：在验证替换阶段保存输入与结果，触发把23章压成几个主题页，或混入第二版与现代Android内容后恢复初态，并由27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表确认语义未漂移。
  - **9. Standard Functions**：在记录决策阶段保存输入与结果，触发把23章压成几个主题页，或混入第二版与现代Android内容后恢复初态，并由27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表确认语义未漂移。
  - **10. Lists and Sets**：在界定责任阶段保存输入与结果，触发把23章压成几个主题页，或混入第二版与现代Android内容后恢复初态，并由27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表确认语义未漂移。
  - **11. Maps**：在比较方案阶段保存输入与结果，触发把23章压成几个主题页，或混入第二版与现代Android内容后恢复初态，并由27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表确认语义未漂移。
  - **12. Defining Classes**：在声明不变量阶段保存输入与结果，触发把23章压成几个主题页，或混入第二版与现代Android内容后恢复初态，并由27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表确认语义未漂移。
  - **13. Initialization**：在验证替换阶段保存输入与结果，触发把23章压成几个主题页，或混入第二版与现代Android内容后恢复初态，并由27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表确认语义未漂移。
  - **14. Inheritance**：在记录决策阶段保存输入与结果，触发把23章压成几个主题页，或混入第二版与现代Android内容后恢复初态，并由27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表确认语义未漂移。
  - **15. Objects**：在界定责任阶段保存输入与结果，触发把23章压成几个主题页，或混入第二版与现代Android内容后恢复初态，并由27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表确认语义未漂移。
  - **16. Interfaces and Abstract Classes**：在比较方案阶段保存输入与结果，触发把23章压成几个主题页，或混入第二版与现代Android内容后恢复初态，并由27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表确认语义未漂移。
  - **17. Generics**：在声明不变量阶段保存输入与结果，触发把23章压成几个主题页，或混入第二版与现代Android内容后恢复初态，并由27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表确认语义未漂移。
  - **18. Extensions**：在验证替换阶段保存输入与结果，触发把23章压成几个主题页，或混入第二版与现代Android内容后恢复初态，并由27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表确认语义未漂移。
  - **19. Functional Programming Basics**：在记录决策阶段保存输入与结果，触发把23章压成几个主题页，或混入第二版与现代Android内容后恢复初态，并由27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表确认语义未漂移。
  - **20. Java Interoperability**：在界定责任阶段保存输入与结果，触发把23章压成几个主题页，或混入第二版与现代Android内容后恢复初态，并由27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表确认语义未漂移。
  - **21. Building Your First Android Application with Kotlin**：在比较方案阶段保存输入与结果，触发把23章压成几个主题页，或混入第二版与现代Android内容后恢复初态，并由27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表确认语义未漂移。
  - **22. Introduction to Coroutines**：在声明不变量阶段保存输入与结果，触发把23章压成几个主题页，或混入第二版与现代Android内容后恢复初态，并由27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表确认语义未漂移。
  - **23. Afterword**：在验证替换阶段保存输入与结果，触发把23章压成几个主题页，或混入第二版与现代Android内容后恢复初态，并由27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表确认语义未漂移。
  - **A. More Challenges**：在记录决策阶段保存输入与结果，触发把23章压成几个主题页，或混入第二版与现代Android内容后恢复初态，并由27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表确认语义未漂移。
  - **Glossary**：在界定责任阶段保存输入与结果，触发把23章压成几个主题页，或混入第二版与现代Android内容后恢复初态，并由27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表确认语义未漂移。
  - **Index**：在比较方案阶段保存输入与结果，触发把23章压成几个主题页，或混入第二版与现代Android内容后恢复初态，并由27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表确认语义未漂移。
</Answer>

**问题 3：反例闭环。** 遇到把23章压成几个主题页，或混入第二版与现代Android内容时，如何判断修复真正成立？

<Answer>
  保存首个失败诊断，恢复显式输入域、类型或对象边界和求值顺序，再以相同工具链和输入重跑正常、边界、非法三组样例。只有27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表回到基线且沿Kotlin 1.2原书顺序串联语言基础、值与集合、对象模型、函数式编程、Java互操作、Android和协程入门的输入、类型边界、求值结果和失败路径都能由独立读者重放重新成立，才能通过。
</Answer>

</Exercises>

## 术语复核与本章回顾

<Glossary>
  <GlossaryItem term="职责边界">
    职责边界在本页限定《Kotlin编程权威指南》权威学习地图允许进入系统的值与前置条件。
  </GlossaryItem>
  <GlossaryItem term="设计约束">
    设计约束在本页标记沿Kotlin 1.2原书顺序串联语言基础、值与集合、对象模型、函数式编程、Java互操作、Android和协程入门中状态由谁创建、修改和释放。
  </GlossaryItem>
  <GlossaryItem term="不变量">
    不变量在本页说明Kotlin 1.2实现与当前迁移之间必须保持的语义。
  </GlossaryItem>
  <GlossaryItem term="替换规则">
    替换规则在本页保存能推翻把23章压成几个主题页，或混入第二版与现代Android内容的原始观察。
  </GlossaryItem>
  <GlossaryItem term="权衡记录">
    权衡记录在本页把错误停在记录决策而非继续扩散。
  </GlossaryItem>
  <GlossaryItem term="验收产物">
    验收产物在本页让另一位读者凭27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表得到相同结论。
  </GlossaryItem>
</Glossary>

完成《Kotlin编程权威指南》权威学习地图意味着：能解释沿Kotlin 1.2原书顺序串联语言基础、值与集合、对象模型、函数式编程、Java互操作、Android和协程入门，能运行并改动章专属切片，能主动制造把23章压成几个主题页，或混入第二版与现代Android内容，还能凭27单元覆盖矩阵、版本边界、依赖路径、实验账本和整书验收表恢复并交接，而不是只识别术语。

## 阅读导航

[下一页：Kotlin导论 →](/learn/kotlin-definitive-guide/01-introduction/kdg1-introducing-kotlin)

<Attribution
  mode="independent-rewrite"
  sourceBasis="outline-only"
  workTitle="Josh Skeen、David Greenhalgh《Kotlin Programming: The Big Nerd Ranch Guide》第1版（Addison-Wesley，2018）"
  adaptedUrl="https://www.oreilly.com/library/view/kotlin-programming-the/9780135165188/"
/>
