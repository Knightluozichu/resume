---
title: 1. Your First Kotlin Application
description: >-
  1. Your First Kotlin
  Application：从IntelliJ项目、Kotlin文件、main入口和REPL贯通源码、字节码与JVM执行，通过全节点状态链、历史迁移对照和故障恢复完成验收。
type: C
section: Kotlin权威指南 · 语言基础
draft: false
math: false
sourceUrl: 'https://www.oreilly.com/library/view/kotlin-programming-the/9780135165188/'
demo: true
order: 1
qualityVersion: 2
practiceMode: code
sourceMode: independent-rewrite
---
import { KdgModelLab, KdgFailureLab, KdgEvidenceLab } from "@/components/mdx/kotlin-definitive-guide/diagrams/kdg1-01-first-application";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能说明从IntelliJ项目、Kotlin文件、main入口和REPL贯通源码、字节码与JVM执行，并画出输入、类型或对象边界与可见结果。
- 能运行本页保留的 Kotlin 切片，解释它在 Kotlin 1.2/JVM 语境中的编译或执行路径。
- 能操作覆盖、合同和恢复三层实验，让全部 10 个正式节点拥有可见教学因果。
- 能触发只依赖IDE绿色运行按钮而不知道编译产物与目标JVM，凭IDE与JDK指纹、源码、编译命令、字节码检查和REPL记录完成诊断、修复、复位与同输入重放。

</Objectives>

{/* KOTLIN_DEFINITIVE_GUIDE_QUALITY_V2 */}

## 从本章问题开始

1. Your First Kotlin Application要解决的不是语法记忆，而是从IntelliJ项目、Kotlin文件、main入口和REPL贯通源码、字节码与JVM执行。先写出输入和期望结果，再运行最小切片；如果无法解释失败落在编译期、边界层还是运行期，程序即使输出一次正确答案也未通过。

本页不把 val 等同深度不可变，也不把简洁语法等同动态类型。Kotlin 的类型、可空性、求值规则和对象模型共同压缩非法状态；本章不变量是从IntelliJ项目、Kotlin文件、main入口和REPL贯通源码、字节码与JVM执行的输入、类型边界、求值结果和失败路径都能由独立读者重放。

## 来源范围与年代边界

O’Reilly 的[第1版书目与完整目录](https://www.oreilly.com/library/view/kotlin-programming-the/9780135165188/)只用于核定本页范围，[中文版出版记录](https://e.dangdang.com/touch/products/1901154363.html)用于交叉核对译本信息；未取得的原书正文不作忠实性宣称。1. Your First Kotlin Application涉及的语言机制由[JetBrains/Android 一手文档](https://kotlinlang.org/docs/basic-syntax.html)独立核验，以下中文讲解、代码组织、图示、实验和练习均为独立教学重写。

第1版的教学坐标是2018年前后的 Kotlin 1.2、JVM、IntelliJ 与当时 Android/早期协程接口。[Kotlin 1.2 发布说明](https://kotlinlang.org/docs/whatsnew12.html)用于锁定历史能力；当前文档只提供迁移对照，Flow、Compose、MockK、现代 Gradle Kotlin DSL 和第二版新增内容不倒灌为原书内容。

## 本章机制与六个检查点

<Term def="静态合同在本页限定1. Your First Kotlin Application允许进入系统的值与前置条件。">静态合同</Term>、<Term def="输入域在本页标记从IntelliJ项目、Kotlin文件、main入口和REPL贯通源码、字节码与JVM执行中状态由谁创建、修改和释放。">输入域</Term>、<Term def="求值顺序在本页说明Kotlin 1.2实现与当前迁移之间必须保持的语义。">求值顺序</Term>、<Term def="可观察结果在本页保存能推翻只依赖IDE绿色运行按钮而不知道编译产物与目标JVM的原始观察。">可观察结果</Term>、<Term def="失败出口在本页把错误停在断言回归而非继续扩散。">失败出口</Term>、<Term def="回归断言在本页让另一位读者凭IDE与JDK指纹、源码、编译命令、字节码检查和REPL记录得到相同结论。">回归断言</Term>。这些检查点共同回答从IntelliJ项目、Kotlin文件、main入口和REPL贯通源码、字节码与JVM执行，最终必须落到IDE与JDK指纹、源码、编译命令、字节码检查和REPL记录，不能只停在API名称或目录词。

## 先预测，再操作三层实验

先预测：选择一个正式节点后哪一阶段会先改变；切换历史基线与当前迁移时哪项语义必须不变；触发只依赖IDE绿色运行按钮而不知道编译产物与目标JVM后，应由哪份证据定位并恢复。

<Stepper>
  <Step title="1. 全节点覆盖与状态链">
    选择任一正式节点和机制阶段，检查输入、状态拥有者、可见结果与专属证据；节点列表不截断。

    <KdgModelLab />

  </Step>
  <Step title="2. 历史合同与当前迁移">
    在正常、边界和故障输入间切换，只改变保护合同或技术坐标，观察结果为何变化。

    <KdgFailureLab />

  </Step>
  <Step title="3. 故障、恢复与重放">
    比较正常轨迹、首错轨迹和恢复轨迹；最后点击重置，确认选择项与可见输出都回到初始值。

    <KdgEvidenceLab />

  </Step>
</Stepper>

## 正式目录逐项深读

### 1. Your First Kotlin Application

1. Your First Kotlin Application服务于从IntelliJ项目、Kotlin文件、main入口和REPL贯通源码、字节码与JVM执行。把这个目录坐标翻译成一个可执行或可判定合同，明确输入、状态变化、可见结果和反例，并以IDE与JDK指纹、源码、编译命令、字节码检查和REPL记录完成独立复核。

在 1. Your First Kotlin Application 的 声明合同 阶段，1. Your First Kotlin Application先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入建立输入，使用同一份输入把正常路径与只依赖IDE绿色运行按钮而不知道编译产物与目标JVM产生的反例并排保存。

对 1. Your First Kotlin Application 的四级验收分别是：正文出现该坐标；解释它如何参与从IntelliJ项目、Kotlin文件、main入口和REPL贯通源码、字节码与JVM执行；在章专属实验中选择该节点并观察反馈；最后凭IDE与JDK指纹、源码、编译命令、字节码检查和REPL记录回答练习、复位并重放。

### Installing IntelliJ IDEA

Installing IntelliJ IDEA服务于从IntelliJ项目、Kotlin文件、main入口和REPL贯通源码、字节码与JVM执行。保存工具链版本、源码、编译命令、产物和退出状态，使IDE按钮之外仍能解释源码如何进入JVM；IDE与JDK指纹、源码、编译命令、字节码检查和REPL记录承担复现责任。

在 1. Your First Kotlin Application 的 建立输入 阶段，Installing IntelliJ IDEA先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入执行转换，使用同一份输入把正常路径与只依赖IDE绿色运行按钮而不知道编译产物与目标JVM产生的反例并排保存。

对 Installing IntelliJ IDEA 的四级验收分别是：正文出现该坐标；解释它如何参与从IntelliJ项目、Kotlin文件、main入口和REPL贯通源码、字节码与JVM执行；在章专属实验中选择该节点并观察反馈；最后凭IDE与JDK指纹、源码、编译命令、字节码检查和REPL记录回答练习、复位并重放。

### Your First Kotlin Project

Your First Kotlin Project服务于从IntelliJ项目、Kotlin文件、main入口和REPL贯通源码、字节码与JVM执行。保存工具链版本、源码、编译命令、产物和退出状态，使IDE按钮之外仍能解释源码如何进入JVM；IDE与JDK指纹、源码、编译命令、字节码检查和REPL记录承担复现责任。

在 1. Your First Kotlin Application 的 执行转换 阶段，Your First Kotlin Project先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入观察产物，使用同一份输入把正常路径与只依赖IDE绿色运行按钮而不知道编译产物与目标JVM产生的反例并排保存。

对 Your First Kotlin Project 的四级验收分别是：正文出现该坐标；解释它如何参与从IntelliJ项目、Kotlin文件、main入口和REPL贯通源码、字节码与JVM执行；在章专属实验中选择该节点并观察反馈；最后凭IDE与JDK指纹、源码、编译命令、字节码检查和REPL记录回答练习、复位并重放。

### Creating your first Kotlin file

Creating your first Kotlin file服务于从IntelliJ项目、Kotlin文件、main入口和REPL贯通源码、字节码与JVM执行。保存工具链版本、源码、编译命令、产物和退出状态，使IDE按钮之外仍能解释源码如何进入JVM；IDE与JDK指纹、源码、编译命令、字节码检查和REPL记录承担复现责任。

在 1. Your First Kotlin Application 的 观察产物 阶段，Creating your first Kotlin file先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入断言回归，使用同一份输入把正常路径与只依赖IDE绿色运行按钮而不知道编译产物与目标JVM产生的反例并排保存。

对 Creating your first Kotlin file 的四级验收分别是：正文出现该坐标；解释它如何参与从IntelliJ项目、Kotlin文件、main入口和REPL贯通源码、字节码与JVM执行；在章专属实验中选择该节点并观察反馈；最后凭IDE与JDK指纹、源码、编译命令、字节码检查和REPL记录回答练习、复位并重放。

### Running your Kotlin file

Running your Kotlin file服务于从IntelliJ项目、Kotlin文件、main入口和REPL贯通源码、字节码与JVM执行。把接收者、参数、返回值、捕获状态和副作用逐项展开，再判断简写是否保持相同求值顺序；IDE与JDK指纹、源码、编译命令、字节码检查和REPL记录必须能区分语法缩短与合同改变。

在 1. Your First Kotlin Application 的 断言回归 阶段，Running your Kotlin file先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入声明合同，使用同一份输入把正常路径与只依赖IDE绿色运行按钮而不知道编译产物与目标JVM产生的反例并排保存。

对 Running your Kotlin file 的四级验收分别是：正文出现该坐标；解释它如何参与从IntelliJ项目、Kotlin文件、main入口和REPL贯通源码、字节码与JVM执行；在章专属实验中选择该节点并观察反馈；最后凭IDE与JDK指纹、源码、编译命令、字节码检查和REPL记录回答练习、复位并重放。

### Compilation and execution of Kotlin/JVM code

Compilation and execution of Kotlin/JVM code服务于从IntelliJ项目、Kotlin文件、main入口和REPL贯通源码、字节码与JVM执行。保存工具链版本、源码、编译命令、产物和退出状态，使IDE按钮之外仍能解释源码如何进入JVM；IDE与JDK指纹、源码、编译命令、字节码检查和REPL记录承担复现责任。

在 1. Your First Kotlin Application 的 声明合同 阶段，Compilation and execution of Kotlin/JVM code先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入建立输入，使用同一份输入把正常路径与只依赖IDE绿色运行按钮而不知道编译产物与目标JVM产生的反例并排保存。

对 Compilation and execution of Kotlin/JVM code 的四级验收分别是：正文出现该坐标；解释它如何参与从IntelliJ项目、Kotlin文件、main入口和REPL贯通源码、字节码与JVM执行；在章专属实验中选择该节点并观察反馈；最后凭IDE与JDK指纹、源码、编译命令、字节码检查和REPL记录回答练习、复位并重放。

### The Kotlin REPL

The Kotlin REPL服务于从IntelliJ项目、Kotlin文件、main入口和REPL贯通源码、字节码与JVM执行。保存工具链版本、源码、编译命令、产物和退出状态，使IDE按钮之外仍能解释源码如何进入JVM；IDE与JDK指纹、源码、编译命令、字节码检查和REPL记录承担复现责任。

在 1. Your First Kotlin Application 的 建立输入 阶段，The Kotlin REPL先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入执行转换，使用同一份输入把正常路径与只依赖IDE绿色运行按钮而不知道编译产物与目标JVM产生的反例并排保存。

对 The Kotlin REPL 的四级验收分别是：正文出现该坐标；解释它如何参与从IntelliJ项目、Kotlin文件、main入口和REPL贯通源码、字节码与JVM执行；在章专属实验中选择该节点并观察反馈；最后凭IDE与JDK指纹、源码、编译命令、字节码检查和REPL记录回答练习、复位并重放。

### For the More Curious: Why Use IntelliJ?

For the More Curious: Why Use IntelliJ?服务于从IntelliJ项目、Kotlin文件、main入口和REPL贯通源码、字节码与JVM执行。把这个目录坐标翻译成一个可执行或可判定合同，明确输入、状态变化、可见结果和反例，并以IDE与JDK指纹、源码、编译命令、字节码检查和REPL记录完成独立复核。

在 1. Your First Kotlin Application 的 执行转换 阶段，For the More Curious: Why Use IntelliJ?先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入观察产物，使用同一份输入把正常路径与只依赖IDE绿色运行按钮而不知道编译产物与目标JVM产生的反例并排保存。

对 For the More Curious: Why Use IntelliJ? 的四级验收分别是：正文出现该坐标；解释它如何参与从IntelliJ项目、Kotlin文件、main入口和REPL贯通源码、字节码与JVM执行；在章专属实验中选择该节点并观察反馈；最后凭IDE与JDK指纹、源码、编译命令、字节码检查和REPL记录回答练习、复位并重放。

### For the More Curious: Targeting the JVM

For the More Curious: Targeting the JVM服务于从IntelliJ项目、Kotlin文件、main入口和REPL贯通源码、字节码与JVM执行。保存工具链版本、源码、编译命令、产物和退出状态，使IDE按钮之外仍能解释源码如何进入JVM；IDE与JDK指纹、源码、编译命令、字节码检查和REPL记录承担复现责任。

在 1. Your First Kotlin Application 的 观察产物 阶段，For the More Curious: Targeting the JVM先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入断言回归，使用同一份输入把正常路径与只依赖IDE绿色运行按钮而不知道编译产物与目标JVM产生的反例并排保存。

对 For the More Curious: Targeting the JVM 的四级验收分别是：正文出现该坐标；解释它如何参与从IntelliJ项目、Kotlin文件、main入口和REPL贯通源码、字节码与JVM执行；在章专属实验中选择该节点并观察反馈；最后凭IDE与JDK指纹、源码、编译命令、字节码检查和REPL记录回答练习、复位并重放。

### Challenge: REPL Arithmetic

Challenge: REPL Arithmetic服务于从IntelliJ项目、Kotlin文件、main入口和REPL贯通源码、字节码与JVM执行。先把题目改写成输入、输出、约束和失败样例，再运行正常、边界与非法三组断言；挑战完成的证据是IDE与JDK指纹、源码、编译命令、字节码检查和REPL记录，不是一次示例输出。

在 1. Your First Kotlin Application 的 断言回归 阶段，Challenge: REPL Arithmetic先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入声明合同，使用同一份输入把正常路径与只依赖IDE绿色运行按钮而不知道编译产物与目标JVM产生的反例并排保存。

对 Challenge: REPL Arithmetic 的四级验收分别是：正文出现该坐标；解释它如何参与从IntelliJ项目、Kotlin文件、main入口和REPL贯通源码、字节码与JVM执行；在章专属实验中选择该节点并观察反馈；最后凭IDE与JDK指纹、源码、编译命令、字节码检查和REPL记录回答练习、复位并重放。

## 最小可执行切片

```kotlin
fun main(args: Array<String>) {
    val answer = 6 * 7
    println("Kotlin/JVM answer: " + answer)
}

// kotlinc Main.kt -include-runtime -d app.jar
```

运行切片时固定 Kotlin/JDK 或 Android 工具链版本、输入数据和命令；保存标准输出、编译诊断及断言。迁移实验必须另建记录，不能静默改写2018年的历史坐标。

<Callout type="trap" title="本章核心陷阱">
  只依赖IDE绿色运行按钮而不知道编译产物与目标JVM。先缩小到最小反例，明确是类型合同、对象所有权、求值顺序还是平台边界失效，再用同输入验证修复。
</Callout>

<Callout type="trap" title="把当前惯例倒灌进第一版">
  1. Your First Kotlin Application保留 Kotlin 1.2/JVM 的历史身份；当前 Kotlin 或 Android 方案只作单独迁移坐标，不能据此改写第一版目录和接口身份。
</Callout>

<Callout type="trap" title="只保留成功输出">
  IDE与JDK指纹、源码、编译命令、字节码检查和REPL记录必须同时包含正常、边界和失败路径；如果故障后不能恢复并复位，本页实践闭环仍未完成。
</Callout>

## 练习、答案与四级证据

<Exercises>

**问题 1：历史与迁移。** 怎样既保留 Kotlin 1.2 的教学身份，又使用当前官方资料核验机制？

<Answer>
  先以[Kotlin 1.2发布说明](https://kotlinlang.org/docs/whatsnew12.html)和目录锁定历史范围，再用[当前技术文档](https://kotlinlang.org/docs/basic-syntax.html)建立单独迁移记录。比较相同输入、结果和不变量；只改变版本坐标，不把当前API伪装成第一版内容。
</Answer>

**问题 2：逐节点验证。** 本页 10 个正式节点怎样从出现升级到可复查证据？

<Answer>
  - **1. Your First Kotlin Application**：在声明合同阶段保存输入与结果，触发只依赖IDE绿色运行按钮而不知道编译产物与目标JVM后恢复初态，并由IDE与JDK指纹、源码、编译命令、字节码检查和REPL记录确认语义未漂移。
  - **Installing IntelliJ IDEA**：在建立输入阶段保存输入与结果，触发只依赖IDE绿色运行按钮而不知道编译产物与目标JVM后恢复初态，并由IDE与JDK指纹、源码、编译命令、字节码检查和REPL记录确认语义未漂移。
  - **Your First Kotlin Project**：在执行转换阶段保存输入与结果，触发只依赖IDE绿色运行按钮而不知道编译产物与目标JVM后恢复初态，并由IDE与JDK指纹、源码、编译命令、字节码检查和REPL记录确认语义未漂移。
  - **Creating your first Kotlin file**：在观察产物阶段保存输入与结果，触发只依赖IDE绿色运行按钮而不知道编译产物与目标JVM后恢复初态，并由IDE与JDK指纹、源码、编译命令、字节码检查和REPL记录确认语义未漂移。
  - **Running your Kotlin file**：在断言回归阶段保存输入与结果，触发只依赖IDE绿色运行按钮而不知道编译产物与目标JVM后恢复初态，并由IDE与JDK指纹、源码、编译命令、字节码检查和REPL记录确认语义未漂移。
  - **Compilation and execution of Kotlin/JVM code**：在声明合同阶段保存输入与结果，触发只依赖IDE绿色运行按钮而不知道编译产物与目标JVM后恢复初态，并由IDE与JDK指纹、源码、编译命令、字节码检查和REPL记录确认语义未漂移。
  - **The Kotlin REPL**：在建立输入阶段保存输入与结果，触发只依赖IDE绿色运行按钮而不知道编译产物与目标JVM后恢复初态，并由IDE与JDK指纹、源码、编译命令、字节码检查和REPL记录确认语义未漂移。
  - **For the More Curious: Why Use IntelliJ?**：在执行转换阶段保存输入与结果，触发只依赖IDE绿色运行按钮而不知道编译产物与目标JVM后恢复初态，并由IDE与JDK指纹、源码、编译命令、字节码检查和REPL记录确认语义未漂移。
  - **For the More Curious: Targeting the JVM**：在观察产物阶段保存输入与结果，触发只依赖IDE绿色运行按钮而不知道编译产物与目标JVM后恢复初态，并由IDE与JDK指纹、源码、编译命令、字节码检查和REPL记录确认语义未漂移。
  - **Challenge: REPL Arithmetic**：在断言回归阶段保存输入与结果，触发只依赖IDE绿色运行按钮而不知道编译产物与目标JVM后恢复初态，并由IDE与JDK指纹、源码、编译命令、字节码检查和REPL记录确认语义未漂移。
</Answer>

**问题 3：反例闭环。** 遇到只依赖IDE绿色运行按钮而不知道编译产物与目标JVM时，如何判断修复真正成立？

<Answer>
  保存首个失败诊断，恢复显式输入域、类型或对象边界和求值顺序，再以相同工具链和输入重跑正常、边界、非法三组样例。只有IDE与JDK指纹、源码、编译命令、字节码检查和REPL记录回到基线且从IntelliJ项目、Kotlin文件、main入口和REPL贯通源码、字节码与JVM执行的输入、类型边界、求值结果和失败路径都能由独立读者重放重新成立，才能通过。
</Answer>

</Exercises>

## 术语复核与本章回顾

<Glossary>
  <GlossaryItem term="静态合同">
    静态合同在本页限定1. Your First Kotlin Application允许进入系统的值与前置条件。
  </GlossaryItem>
  <GlossaryItem term="输入域">
    输入域在本页标记从IntelliJ项目、Kotlin文件、main入口和REPL贯通源码、字节码与JVM执行中状态由谁创建、修改和释放。
  </GlossaryItem>
  <GlossaryItem term="求值顺序">
    求值顺序在本页说明Kotlin 1.2实现与当前迁移之间必须保持的语义。
  </GlossaryItem>
  <GlossaryItem term="可观察结果">
    可观察结果在本页保存能推翻只依赖IDE绿色运行按钮而不知道编译产物与目标JVM的原始观察。
  </GlossaryItem>
  <GlossaryItem term="失败出口">
    失败出口在本页把错误停在断言回归而非继续扩散。
  </GlossaryItem>
  <GlossaryItem term="回归断言">
    回归断言在本页让另一位读者凭IDE与JDK指纹、源码、编译命令、字节码检查和REPL记录得到相同结论。
  </GlossaryItem>
</Glossary>

完成1. Your First Kotlin Application意味着：能解释从IntelliJ项目、Kotlin文件、main入口和REPL贯通源码、字节码与JVM执行，能运行并改动章专属切片，能主动制造只依赖IDE绿色运行按钮而不知道编译产物与目标JVM，还能凭IDE与JDK指纹、源码、编译命令、字节码检查和REPL记录恢复并交接，而不是只识别术语。

## 阅读导航

[← 上一页：Kotlin导论](/learn/kotlin-definitive-guide/01-introduction/kdg1-introducing-kotlin) · [下一页：2. Variables, Constants, and Types →](/learn/kotlin-definitive-guide/02-language-foundations/kdg1-02-variables-types)

<Attribution
  mode="independent-rewrite"
  sourceBasis="outline-only"
  workTitle="Josh Skeen、David Greenhalgh《Kotlin Programming: The Big Nerd Ranch Guide》第1版（Addison-Wesley，2018）"
  adaptedUrl="https://www.oreilly.com/library/view/kotlin-programming-the/9780135165188/"
/>
