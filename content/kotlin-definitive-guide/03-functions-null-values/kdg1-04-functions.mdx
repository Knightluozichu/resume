---
title: 4. Functions
description: >-
  4.
  Functions：以签名、参数、返回类型、作用域和可见性设计可组合函数，并用默认值与命名参数表达调用意图，通过全节点状态链、历史迁移对照和故障恢复完成验收。
type: C
section: Kotlin权威指南 · 函数与空安全
draft: false
math: false
sourceUrl: 'https://www.oreilly.com/library/view/kotlin-programming-the/9780135165188/'
demo: true
order: 1
qualityVersion: 2
practiceMode: code
sourceMode: independent-rewrite
---
import { KdgModelLab, KdgFailureLab, KdgEvidenceLab } from "@/components/mdx/kotlin-definitive-guide/diagrams/kdg1-04-functions";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能说明以签名、参数、返回类型、作用域和可见性设计可组合函数，并用默认值与命名参数表达调用意图，并画出输入、类型或对象边界与可见结果。
- 能运行本页保留的 Kotlin 切片，解释它在 Kotlin 1.2/JVM 语境中的编译或执行路径。
- 能操作覆盖、合同和恢复三层实验，让全部 24 个正式节点拥有可见教学因果。
- 能触发用默认参数隐藏必填业务事实，或让Unit函数承担不可见副作用，凭函数合同、调用样例、边界测试、作用域图和重构前后对照完成诊断、修复、复位与同输入重放。

</Objectives>

{/* KOTLIN_DEFINITIVE_GUIDE_QUALITY_V2 */}

## 从本章问题开始

4. Functions要解决的不是语法记忆，而是以签名、参数、返回类型、作用域和可见性设计可组合函数，并用默认值与命名参数表达调用意图。先写出输入和期望结果，再运行最小切片；如果无法解释失败落在编译期、边界层还是运行期，程序即使输出一次正确答案也未通过。

本页不把 val 等同深度不可变，也不把简洁语法等同动态类型。Kotlin 的类型、可空性、求值规则和对象模型共同压缩非法状态；本章不变量是以签名、参数、返回类型、作用域和可见性设计可组合函数，并用默认值与命名参数表达调用意图的输入、类型边界、求值结果和失败路径都能由独立读者重放。

## 来源范围与年代边界

O’Reilly 的[第1版书目与完整目录](https://www.oreilly.com/library/view/kotlin-programming-the/9780135165188/)只用于核定本页范围，[中文版出版记录](https://e.dangdang.com/touch/products/1901154363.html)用于交叉核对译本信息；未取得的原书正文不作忠实性宣称。4. Functions涉及的语言机制由[JetBrains/Android 一手文档](https://kotlinlang.org/docs/basic-syntax.html)独立核验，以下中文讲解、代码组织、图示、实验和练习均为独立教学重写。

第1版的教学坐标是2018年前后的 Kotlin 1.2、JVM、IntelliJ 与当时 Android/早期协程接口。[Kotlin 1.2 发布说明](https://kotlinlang.org/docs/whatsnew12.html)用于锁定历史能力；当前文档只提供迁移对照，Flow、Compose、MockK、现代 Gradle Kotlin DSL 和第二版新增内容不倒灌为原书内容。

## 本章机制与六个检查点

<Term def="静态合同在本页限定4. Functions允许进入系统的值与前置条件。">静态合同</Term>、<Term def="输入域在本页标记以签名、参数、返回类型、作用域和可见性设计可组合函数，并用默认值与命名参数表达调用意图中状态由谁创建、修改和释放。">输入域</Term>、<Term def="求值顺序在本页说明Kotlin 1.2实现与当前迁移之间必须保持的语义。">求值顺序</Term>、<Term def="可观察结果在本页保存能推翻用默认参数隐藏必填业务事实，或让Unit函数承担不可见副作用的原始观察。">可观察结果</Term>、<Term def="失败出口在本页把错误停在断言回归而非继续扩散。">失败出口</Term>、<Term def="回归断言在本页让另一位读者凭函数合同、调用样例、边界测试、作用域图和重构前后对照得到相同结论。">回归断言</Term>。这些检查点共同回答以签名、参数、返回类型、作用域和可见性设计可组合函数，并用默认值与命名参数表达调用意图，最终必须落到函数合同、调用样例、边界测试、作用域图和重构前后对照，不能只停在API名称或目录词。

## 先预测，再操作三层实验

先预测：选择一个正式节点后哪一阶段会先改变；切换历史基线与当前迁移时哪项语义必须不变；触发用默认参数隐藏必填业务事实，或让Unit函数承担不可见副作用后，应由哪份证据定位并恢复。

<Stepper>
  <Step title="1. 全节点覆盖与状态链">
    选择任一正式节点和机制阶段，检查输入、状态拥有者、可见结果与专属证据；节点列表不截断。

    <KdgModelLab />

  </Step>
  <Step title="2. 历史合同与当前迁移">
    在正常、边界和故障输入间切换，只改变保护合同或技术坐标，观察结果为何变化。

    <KdgFailureLab />

  </Step>
  <Step title="3. 故障、恢复与重放">
    比较正常轨迹、首错轨迹和恢复轨迹；最后点击重置，确认选择项与可见输出都回到初始值。

    <KdgEvidenceLab />

  </Step>
</Stepper>

## 正式目录逐项深读

### 4. Functions

4. Functions服务于以签名、参数、返回类型、作用域和可见性设计可组合函数，并用默认值与命名参数表达调用意图。把接收者、参数、返回值、捕获状态和副作用逐项展开，再判断简写是否保持相同求值顺序；函数合同、调用样例、边界测试、作用域图和重构前后对照必须能区分语法缩短与合同改变。

在 4. Functions 的 声明合同 阶段，4. Functions先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入建立输入，使用同一份输入把正常路径与用默认参数隐藏必填业务事实，或让Unit函数承担不可见副作用产生的反例并排保存。

对 4. Functions 的四级验收分别是：正文出现该坐标；解释它如何参与以签名、参数、返回类型、作用域和可见性设计可组合函数，并用默认值与命名参数表达调用意图；在章专属实验中选择该节点并观察反馈；最后凭函数合同、调用样例、边界测试、作用域图和重构前后对照回答练习、复位并重放。

### Extracting Code to Functions

Extracting Code to Functions服务于以签名、参数、返回类型、作用域和可见性设计可组合函数，并用默认值与命名参数表达调用意图。把接收者、参数、返回值、捕获状态和副作用逐项展开，再判断简写是否保持相同求值顺序；函数合同、调用样例、边界测试、作用域图和重构前后对照必须能区分语法缩短与合同改变。

在 4. Functions 的 建立输入 阶段，Extracting Code to Functions先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入执行转换，使用同一份输入把正常路径与用默认参数隐藏必填业务事实，或让Unit函数承担不可见副作用产生的反例并排保存。

对 Extracting Code to Functions 的四级验收分别是：正文出现该坐标；解释它如何参与以签名、参数、返回类型、作用域和可见性设计可组合函数，并用默认值与命名参数表达调用意图；在章专属实验中选择该节点并观察反馈；最后凭函数合同、调用样例、边界测试、作用域图和重构前后对照回答练习、复位并重放。

### Anatomy of a Function

Anatomy of a Function服务于以签名、参数、返回类型、作用域和可见性设计可组合函数，并用默认值与命名参数表达调用意图。把接收者、参数、返回值、捕获状态和副作用逐项展开，再判断简写是否保持相同求值顺序；函数合同、调用样例、边界测试、作用域图和重构前后对照必须能区分语法缩短与合同改变。

在 4. Functions 的 执行转换 阶段，Anatomy of a Function先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入观察产物，使用同一份输入把正常路径与用默认参数隐藏必填业务事实，或让Unit函数承担不可见副作用产生的反例并排保存。

对 Anatomy of a Function 的四级验收分别是：正文出现该坐标；解释它如何参与以签名、参数、返回类型、作用域和可见性设计可组合函数，并用默认值与命名参数表达调用意图；在章专属实验中选择该节点并观察反馈；最后凭函数合同、调用样例、边界测试、作用域图和重构前后对照回答练习、复位并重放。

### Function header

Function header服务于以签名、参数、返回类型、作用域和可见性设计可组合函数，并用默认值与命名参数表达调用意图。把接收者、参数、返回值、捕获状态和副作用逐项展开，再判断简写是否保持相同求值顺序；函数合同、调用样例、边界测试、作用域图和重构前后对照必须能区分语法缩短与合同改变。

在 4. Functions 的 观察产物 阶段，Function header先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入断言回归，使用同一份输入把正常路径与用默认参数隐藏必填业务事实，或让Unit函数承担不可见副作用产生的反例并排保存。

对 Function header 的四级验收分别是：正文出现该坐标；解释它如何参与以签名、参数、返回类型、作用域和可见性设计可组合函数，并用默认值与命名参数表达调用意图；在章专属实验中选择该节点并观察反馈；最后凭函数合同、调用样例、边界测试、作用域图和重构前后对照回答练习、复位并重放。

### Visibility modifier

Visibility modifier服务于以签名、参数、返回类型、作用域和可见性设计可组合函数，并用默认值与命名参数表达调用意图。把这个目录坐标翻译成一个可执行或可判定合同，明确输入、状态变化、可见结果和反例，并以函数合同、调用样例、边界测试、作用域图和重构前后对照完成独立复核。

在 4. Functions 的 断言回归 阶段，Visibility modifier先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入声明合同，使用同一份输入把正常路径与用默认参数隐藏必填业务事实，或让Unit函数承担不可见副作用产生的反例并排保存。

对 Visibility modifier 的四级验收分别是：正文出现该坐标；解释它如何参与以签名、参数、返回类型、作用域和可见性设计可组合函数，并用默认值与命名参数表达调用意图；在章专属实验中选择该节点并观察反馈；最后凭函数合同、调用样例、边界测试、作用域图和重构前后对照回答练习、复位并重放。

### Function name declaration

Function name declaration服务于以签名、参数、返回类型、作用域和可见性设计可组合函数，并用默认值与命名参数表达调用意图。把接收者、参数、返回值、捕获状态和副作用逐项展开，再判断简写是否保持相同求值顺序；函数合同、调用样例、边界测试、作用域图和重构前后对照必须能区分语法缩短与合同改变。

在 4. Functions 的 声明合同 阶段，Function name declaration先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入建立输入，使用同一份输入把正常路径与用默认参数隐藏必填业务事实，或让Unit函数承担不可见副作用产生的反例并排保存。

对 Function name declaration 的四级验收分别是：正文出现该坐标；解释它如何参与以签名、参数、返回类型、作用域和可见性设计可组合函数，并用默认值与命名参数表达调用意图；在章专属实验中选择该节点并观察反馈；最后凭函数合同、调用样例、边界测试、作用域图和重构前后对照回答练习、复位并重放。

### Function parameters

Function parameters服务于以签名、参数、返回类型、作用域和可见性设计可组合函数，并用默认值与命名参数表达调用意图。把接收者、参数、返回值、捕获状态和副作用逐项展开，再判断简写是否保持相同求值顺序；函数合同、调用样例、边界测试、作用域图和重构前后对照必须能区分语法缩短与合同改变。

在 4. Functions 的 建立输入 阶段，Function parameters先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入执行转换，使用同一份输入把正常路径与用默认参数隐藏必填业务事实，或让Unit函数承担不可见副作用产生的反例并排保存。

对 Function parameters 的四级验收分别是：正文出现该坐标；解释它如何参与以签名、参数、返回类型、作用域和可见性设计可组合函数，并用默认值与命名参数表达调用意图；在章专属实验中选择该节点并观察反馈；最后凭函数合同、调用样例、边界测试、作用域图和重构前后对照回答练习、复位并重放。

### Function return type

Function return type服务于以签名、参数、返回类型、作用域和可见性设计可组合函数，并用默认值与命名参数表达调用意图。把接收者、参数、返回值、捕获状态和副作用逐项展开，再判断简写是否保持相同求值顺序；函数合同、调用样例、边界测试、作用域图和重构前后对照必须能区分语法缩短与合同改变。

在 4. Functions 的 执行转换 阶段，Function return type先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入观察产物，使用同一份输入把正常路径与用默认参数隐藏必填业务事实，或让Unit函数承担不可见副作用产生的反例并排保存。

对 Function return type 的四级验收分别是：正文出现该坐标；解释它如何参与以签名、参数、返回类型、作用域和可见性设计可组合函数，并用默认值与命名参数表达调用意图；在章专属实验中选择该节点并观察反馈；最后凭函数合同、调用样例、边界测试、作用域图和重构前后对照回答练习、复位并重放。

### Function body

Function body服务于以签名、参数、返回类型、作用域和可见性设计可组合函数，并用默认值与命名参数表达调用意图。把接收者、参数、返回值、捕获状态和副作用逐项展开，再判断简写是否保持相同求值顺序；函数合同、调用样例、边界测试、作用域图和重构前后对照必须能区分语法缩短与合同改变。

在 4. Functions 的 观察产物 阶段，Function body先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入断言回归，使用同一份输入把正常路径与用默认参数隐藏必填业务事实，或让Unit函数承担不可见副作用产生的反例并排保存。

对 Function body 的四级验收分别是：正文出现该坐标；解释它如何参与以签名、参数、返回类型、作用域和可见性设计可组合函数，并用默认值与命名参数表达调用意图；在章专属实验中选择该节点并观察反馈；最后凭函数合同、调用样例、边界测试、作用域图和重构前后对照回答练习、复位并重放。

### Function scope

Function scope服务于以签名、参数、返回类型、作用域和可见性设计可组合函数，并用默认值与命名参数表达调用意图。把接收者、参数、返回值、捕获状态和副作用逐项展开，再判断简写是否保持相同求值顺序；函数合同、调用样例、边界测试、作用域图和重构前后对照必须能区分语法缩短与合同改变。

在 4. Functions 的 断言回归 阶段，Function scope先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入声明合同，使用同一份输入把正常路径与用默认参数隐藏必填业务事实，或让Unit函数承担不可见副作用产生的反例并排保存。

对 Function scope 的四级验收分别是：正文出现该坐标；解释它如何参与以签名、参数、返回类型、作用域和可见性设计可组合函数，并用默认值与命名参数表达调用意图；在章专属实验中选择该节点并观察反馈；最后凭函数合同、调用样例、边界测试、作用域图和重构前后对照回答练习、复位并重放。

### Calling a Function

Calling a Function服务于以签名、参数、返回类型、作用域和可见性设计可组合函数，并用默认值与命名参数表达调用意图。把接收者、参数、返回值、捕获状态和副作用逐项展开，再判断简写是否保持相同求值顺序；函数合同、调用样例、边界测试、作用域图和重构前后对照必须能区分语法缩短与合同改变。

在 4. Functions 的 声明合同 阶段，Calling a Function先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入建立输入，使用同一份输入把正常路径与用默认参数隐藏必填业务事实，或让Unit函数承担不可见副作用产生的反例并排保存。

对 Calling a Function 的四级验收分别是：正文出现该坐标；解释它如何参与以签名、参数、返回类型、作用域和可见性设计可组合函数，并用默认值与命名参数表达调用意图；在章专属实验中选择该节点并观察反馈；最后凭函数合同、调用样例、边界测试、作用域图和重构前后对照回答练习、复位并重放。

### Refactoring to Functions

Refactoring to Functions服务于以签名、参数、返回类型、作用域和可见性设计可组合函数，并用默认值与命名参数表达调用意图。把接收者、参数、返回值、捕获状态和副作用逐项展开，再判断简写是否保持相同求值顺序；函数合同、调用样例、边界测试、作用域图和重构前后对照必须能区分语法缩短与合同改变。

在 4. Functions 的 建立输入 阶段，Refactoring to Functions先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入执行转换，使用同一份输入把正常路径与用默认参数隐藏必填业务事实，或让Unit函数承担不可见副作用产生的反例并排保存。

对 Refactoring to Functions 的四级验收分别是：正文出现该坐标；解释它如何参与以签名、参数、返回类型、作用域和可见性设计可组合函数，并用默认值与命名参数表达调用意图；在章专属实验中选择该节点并观察反馈；最后凭函数合同、调用样例、边界测试、作用域图和重构前后对照回答练习、复位并重放。

### Writing Your Own Functions

Writing Your Own Functions服务于以签名、参数、返回类型、作用域和可见性设计可组合函数，并用默认值与命名参数表达调用意图。把接收者、参数、返回值、捕获状态和副作用逐项展开，再判断简写是否保持相同求值顺序；函数合同、调用样例、边界测试、作用域图和重构前后对照必须能区分语法缩短与合同改变。

在 4. Functions 的 执行转换 阶段，Writing Your Own Functions先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入观察产物，使用同一份输入把正常路径与用默认参数隐藏必填业务事实，或让Unit函数承担不可见副作用产生的反例并排保存。

对 Writing Your Own Functions 的四级验收分别是：正文出现该坐标；解释它如何参与以签名、参数、返回类型、作用域和可见性设计可组合函数，并用默认值与命名参数表达调用意图；在章专属实验中选择该节点并观察反馈；最后凭函数合同、调用样例、边界测试、作用域图和重构前后对照回答练习、复位并重放。

### Default Arguments

Default Arguments服务于以签名、参数、返回类型、作用域和可见性设计可组合函数，并用默认值与命名参数表达调用意图。把这个目录坐标翻译成一个可执行或可判定合同，明确输入、状态变化、可见结果和反例，并以函数合同、调用样例、边界测试、作用域图和重构前后对照完成独立复核。

在 4. Functions 的 观察产物 阶段，Default Arguments先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入断言回归，使用同一份输入把正常路径与用默认参数隐藏必填业务事实，或让Unit函数承担不可见副作用产生的反例并排保存。

对 Default Arguments 的四级验收分别是：正文出现该坐标；解释它如何参与以签名、参数、返回类型、作用域和可见性设计可组合函数，并用默认值与命名参数表达调用意图；在章专属实验中选择该节点并观察反馈；最后凭函数合同、调用样例、边界测试、作用域图和重构前后对照回答练习、复位并重放。

### Single-Expression Functions

Single-Expression Functions服务于以签名、参数、返回类型、作用域和可见性设计可组合函数，并用默认值与命名参数表达调用意图。把接收者、参数、返回值、捕获状态和副作用逐项展开，再判断简写是否保持相同求值顺序；函数合同、调用样例、边界测试、作用域图和重构前后对照必须能区分语法缩短与合同改变。

在 4. Functions 的 断言回归 阶段，Single-Expression Functions先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入声明合同，使用同一份输入把正常路径与用默认参数隐藏必填业务事实，或让Unit函数承担不可见副作用产生的反例并排保存。

对 Single-Expression Functions 的四级验收分别是：正文出现该坐标；解释它如何参与以签名、参数、返回类型、作用域和可见性设计可组合函数，并用默认值与命名参数表达调用意图；在章专属实验中选择该节点并观察反馈；最后凭函数合同、调用样例、边界测试、作用域图和重构前后对照回答练习、复位并重放。

### Unit Functions

Unit Functions服务于以签名、参数、返回类型、作用域和可见性设计可组合函数，并用默认值与命名参数表达调用意图。把接收者、参数、返回值、捕获状态和副作用逐项展开，再判断简写是否保持相同求值顺序；函数合同、调用样例、边界测试、作用域图和重构前后对照必须能区分语法缩短与合同改变。

在 4. Functions 的 声明合同 阶段，Unit Functions先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入建立输入，使用同一份输入把正常路径与用默认参数隐藏必填业务事实，或让Unit函数承担不可见副作用产生的反例并排保存。

对 Unit Functions 的四级验收分别是：正文出现该坐标；解释它如何参与以签名、参数、返回类型、作用域和可见性设计可组合函数，并用默认值与命名参数表达调用意图；在章专属实验中选择该节点并观察反馈；最后凭函数合同、调用样例、边界测试、作用域图和重构前后对照回答练习、复位并重放。

### Named Function Arguments

Named Function Arguments服务于以签名、参数、返回类型、作用域和可见性设计可组合函数，并用默认值与命名参数表达调用意图。把接收者、参数、返回值、捕获状态和副作用逐项展开，再判断简写是否保持相同求值顺序；函数合同、调用样例、边界测试、作用域图和重构前后对照必须能区分语法缩短与合同改变。

在 4. Functions 的 建立输入 阶段，Named Function Arguments先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入执行转换，使用同一份输入把正常路径与用默认参数隐藏必填业务事实，或让Unit函数承担不可见副作用产生的反例并排保存。

对 Named Function Arguments 的四级验收分别是：正文出现该坐标；解释它如何参与以签名、参数、返回类型、作用域和可见性设计可组合函数，并用默认值与命名参数表达调用意图；在章专属实验中选择该节点并观察反馈；最后凭函数合同、调用样例、边界测试、作用域图和重构前后对照回答练习、复位并重放。

### For the More Curious: The Nothing Type

For the More Curious: The Nothing Type服务于以签名、参数、返回类型、作用域和可见性设计可组合函数，并用默认值与命名参数表达调用意图。画出类型关系、状态拥有者、初始化时刻和允许替换，再用编译失败或替身实现挑战边界；验收时检查函数合同、调用样例、边界测试、作用域图和重构前后对照是否支持对象不变量。

在 4. Functions 的 执行转换 阶段，For the More Curious: The Nothing Type先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入观察产物，使用同一份输入把正常路径与用默认参数隐藏必填业务事实，或让Unit函数承担不可见副作用产生的反例并排保存。

对 For the More Curious: The Nothing Type 的四级验收分别是：正文出现该坐标；解释它如何参与以签名、参数、返回类型、作用域和可见性设计可组合函数，并用默认值与命名参数表达调用意图；在章专属实验中选择该节点并观察反馈；最后凭函数合同、调用样例、边界测试、作用域图和重构前后对照回答练习、复位并重放。

### For the More Curious: File-Level Functions in Java

For the More Curious: File-Level Functions in Java服务于以签名、参数、返回类型、作用域和可见性设计可组合函数，并用默认值与命名参数表达调用意图。把接收者、参数、返回值、捕获状态和副作用逐项展开，再判断简写是否保持相同求值顺序；函数合同、调用样例、边界测试、作用域图和重构前后对照必须能区分语法缩短与合同改变。

在 4. Functions 的 观察产物 阶段，For the More Curious: File-Level Functions in Java先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入断言回归，使用同一份输入把正常路径与用默认参数隐藏必填业务事实，或让Unit函数承担不可见副作用产生的反例并排保存。

对 For the More Curious: File-Level Functions in Java 的四级验收分别是：正文出现该坐标；解释它如何参与以签名、参数、返回类型、作用域和可见性设计可组合函数，并用默认值与命名参数表达调用意图；在章专属实验中选择该节点并观察反馈；最后凭函数合同、调用样例、边界测试、作用域图和重构前后对照回答练习、复位并重放。

### For the More Curious: Function Overloading

For the More Curious: Function Overloading服务于以签名、参数、返回类型、作用域和可见性设计可组合函数，并用默认值与命名参数表达调用意图。把接收者、参数、返回值、捕获状态和副作用逐项展开，再判断简写是否保持相同求值顺序；函数合同、调用样例、边界测试、作用域图和重构前后对照必须能区分语法缩短与合同改变。

在 4. Functions 的 断言回归 阶段，For the More Curious: Function Overloading先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入声明合同，使用同一份输入把正常路径与用默认参数隐藏必填业务事实，或让Unit函数承担不可见副作用产生的反例并排保存。

对 For the More Curious: Function Overloading 的四级验收分别是：正文出现该坐标；解释它如何参与以签名、参数、返回类型、作用域和可见性设计可组合函数，并用默认值与命名参数表达调用意图；在章专属实验中选择该节点并观察反馈；最后凭函数合同、调用样例、边界测试、作用域图和重构前后对照回答练习、复位并重放。

### For the More Curious: Function Names in Backticks

For the More Curious: Function Names in Backticks服务于以签名、参数、返回类型、作用域和可见性设计可组合函数，并用默认值与命名参数表达调用意图。把接收者、参数、返回值、捕获状态和副作用逐项展开，再判断简写是否保持相同求值顺序；函数合同、调用样例、边界测试、作用域图和重构前后对照必须能区分语法缩短与合同改变。

在 4. Functions 的 声明合同 阶段，For the More Curious: Function Names in Backticks先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入建立输入，使用同一份输入把正常路径与用默认参数隐藏必填业务事实，或让Unit函数承担不可见副作用产生的反例并排保存。

对 For the More Curious: Function Names in Backticks 的四级验收分别是：正文出现该坐标；解释它如何参与以签名、参数、返回类型、作用域和可见性设计可组合函数，并用默认值与命名参数表达调用意图；在章专属实验中选择该节点并观察反馈；最后凭函数合同、调用样例、边界测试、作用域图和重构前后对照回答练习、复位并重放。

### Challenge: Single-Expression Functions

Challenge: Single-Expression Functions服务于以签名、参数、返回类型、作用域和可见性设计可组合函数，并用默认值与命名参数表达调用意图。先把题目改写成输入、输出、约束和失败样例，再运行正常、边界与非法三组断言；挑战完成的证据是函数合同、调用样例、边界测试、作用域图和重构前后对照，不是一次示例输出。

在 4. Functions 的 建立输入 阶段，Challenge: Single-Expression Functions先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入执行转换，使用同一份输入把正常路径与用默认参数隐藏必填业务事实，或让Unit函数承担不可见副作用产生的反例并排保存。

对 Challenge: Single-Expression Functions 的四级验收分别是：正文出现该坐标；解释它如何参与以签名、参数、返回类型、作用域和可见性设计可组合函数，并用默认值与命名参数表达调用意图；在章专属实验中选择该节点并观察反馈；最后凭函数合同、调用样例、边界测试、作用域图和重构前后对照回答练习、复位并重放。

### Challenge: Fireball Inebriation Level

Challenge: Fireball Inebriation Level服务于以签名、参数、返回类型、作用域和可见性设计可组合函数，并用默认值与命名参数表达调用意图。先把题目改写成输入、输出、约束和失败样例，再运行正常、边界与非法三组断言；挑战完成的证据是函数合同、调用样例、边界测试、作用域图和重构前后对照，不是一次示例输出。

在 4. Functions 的 执行转换 阶段，Challenge: Fireball Inebriation Level先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入观察产物，使用同一份输入把正常路径与用默认参数隐藏必填业务事实，或让Unit函数承担不可见副作用产生的反例并排保存。

对 Challenge: Fireball Inebriation Level 的四级验收分别是：正文出现该坐标；解释它如何参与以签名、参数、返回类型、作用域和可见性设计可组合函数，并用默认值与命名参数表达调用意图；在章专属实验中选择该节点并观察反馈；最后凭函数合同、调用样例、边界测试、作用域图和重构前后对照回答练习、复位并重放。

### Challenge: Inebriation Status

Challenge: Inebriation Status服务于以签名、参数、返回类型、作用域和可见性设计可组合函数，并用默认值与命名参数表达调用意图。先把题目改写成输入、输出、约束和失败样例，再运行正常、边界与非法三组断言；挑战完成的证据是函数合同、调用样例、边界测试、作用域图和重构前后对照，不是一次示例输出。

在 4. Functions 的 观察产物 阶段，Challenge: Inebriation Status先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入断言回归，使用同一份输入把正常路径与用默认参数隐藏必填业务事实，或让Unit函数承担不可见副作用产生的反例并排保存。

对 Challenge: Inebriation Status 的四级验收分别是：正文出现该坐标；解释它如何参与以签名、参数、返回类型、作用域和可见性设计可组合函数，并用默认值与命名参数表达调用意图；在章专属实验中选择该节点并观察反馈；最后凭函数合同、调用样例、边界测试、作用域图和重构前后对照回答练习、复位并重放。

## 最小可执行切片

```kotlin
private fun inebriationStatus(level: Int, max: Int = 50): String =
    when {
        level <= 0 -> "sober"
        level < max -> "tipsy"
        else -> "passed out"
    }

val status = inebriationStatus(level = 12)
```

运行切片时固定 Kotlin/JDK 或 Android 工具链版本、输入数据和命令；保存标准输出、编译诊断及断言。迁移实验必须另建记录，不能静默改写2018年的历史坐标。

<Callout type="trap" title="本章核心陷阱">
  用默认参数隐藏必填业务事实，或让Unit函数承担不可见副作用。先缩小到最小反例，明确是类型合同、对象所有权、求值顺序还是平台边界失效，再用同输入验证修复。
</Callout>

<Callout type="trap" title="把当前惯例倒灌进第一版">
  4. Functions保留 Kotlin 1.2/JVM 的历史身份；当前 Kotlin 或 Android 方案只作单独迁移坐标，不能据此改写第一版目录和接口身份。
</Callout>

<Callout type="trap" title="只保留成功输出">
  函数合同、调用样例、边界测试、作用域图和重构前后对照必须同时包含正常、边界和失败路径；如果故障后不能恢复并复位，本页实践闭环仍未完成。
</Callout>

## 练习、答案与四级证据

<Exercises>

**问题 1：历史与迁移。** 怎样既保留 Kotlin 1.2 的教学身份，又使用当前官方资料核验机制？

<Answer>
  先以[Kotlin 1.2发布说明](https://kotlinlang.org/docs/whatsnew12.html)和目录锁定历史范围，再用[当前技术文档](https://kotlinlang.org/docs/basic-syntax.html)建立单独迁移记录。比较相同输入、结果和不变量；只改变版本坐标，不把当前API伪装成第一版内容。
</Answer>

**问题 2：逐节点验证。** 本页 24 个正式节点怎样从出现升级到可复查证据？

<Answer>
  - **4. Functions**：在声明合同阶段保存输入与结果，触发用默认参数隐藏必填业务事实，或让Unit函数承担不可见副作用后恢复初态，并由函数合同、调用样例、边界测试、作用域图和重构前后对照确认语义未漂移。
  - **Extracting Code to Functions**：在建立输入阶段保存输入与结果，触发用默认参数隐藏必填业务事实，或让Unit函数承担不可见副作用后恢复初态，并由函数合同、调用样例、边界测试、作用域图和重构前后对照确认语义未漂移。
  - **Anatomy of a Function**：在执行转换阶段保存输入与结果，触发用默认参数隐藏必填业务事实，或让Unit函数承担不可见副作用后恢复初态，并由函数合同、调用样例、边界测试、作用域图和重构前后对照确认语义未漂移。
  - **Function header**：在观察产物阶段保存输入与结果，触发用默认参数隐藏必填业务事实，或让Unit函数承担不可见副作用后恢复初态，并由函数合同、调用样例、边界测试、作用域图和重构前后对照确认语义未漂移。
  - **Visibility modifier**：在断言回归阶段保存输入与结果，触发用默认参数隐藏必填业务事实，或让Unit函数承担不可见副作用后恢复初态，并由函数合同、调用样例、边界测试、作用域图和重构前后对照确认语义未漂移。
  - **Function name declaration**：在声明合同阶段保存输入与结果，触发用默认参数隐藏必填业务事实，或让Unit函数承担不可见副作用后恢复初态，并由函数合同、调用样例、边界测试、作用域图和重构前后对照确认语义未漂移。
  - **Function parameters**：在建立输入阶段保存输入与结果，触发用默认参数隐藏必填业务事实，或让Unit函数承担不可见副作用后恢复初态，并由函数合同、调用样例、边界测试、作用域图和重构前后对照确认语义未漂移。
  - **Function return type**：在执行转换阶段保存输入与结果，触发用默认参数隐藏必填业务事实，或让Unit函数承担不可见副作用后恢复初态，并由函数合同、调用样例、边界测试、作用域图和重构前后对照确认语义未漂移。
  - **Function body**：在观察产物阶段保存输入与结果，触发用默认参数隐藏必填业务事实，或让Unit函数承担不可见副作用后恢复初态，并由函数合同、调用样例、边界测试、作用域图和重构前后对照确认语义未漂移。
  - **Function scope**：在断言回归阶段保存输入与结果，触发用默认参数隐藏必填业务事实，或让Unit函数承担不可见副作用后恢复初态，并由函数合同、调用样例、边界测试、作用域图和重构前后对照确认语义未漂移。
  - **Calling a Function**：在声明合同阶段保存输入与结果，触发用默认参数隐藏必填业务事实，或让Unit函数承担不可见副作用后恢复初态，并由函数合同、调用样例、边界测试、作用域图和重构前后对照确认语义未漂移。
  - **Refactoring to Functions**：在建立输入阶段保存输入与结果，触发用默认参数隐藏必填业务事实，或让Unit函数承担不可见副作用后恢复初态，并由函数合同、调用样例、边界测试、作用域图和重构前后对照确认语义未漂移。
  - **Writing Your Own Functions**：在执行转换阶段保存输入与结果，触发用默认参数隐藏必填业务事实，或让Unit函数承担不可见副作用后恢复初态，并由函数合同、调用样例、边界测试、作用域图和重构前后对照确认语义未漂移。
  - **Default Arguments**：在观察产物阶段保存输入与结果，触发用默认参数隐藏必填业务事实，或让Unit函数承担不可见副作用后恢复初态，并由函数合同、调用样例、边界测试、作用域图和重构前后对照确认语义未漂移。
  - **Single-Expression Functions**：在断言回归阶段保存输入与结果，触发用默认参数隐藏必填业务事实，或让Unit函数承担不可见副作用后恢复初态，并由函数合同、调用样例、边界测试、作用域图和重构前后对照确认语义未漂移。
  - **Unit Functions**：在声明合同阶段保存输入与结果，触发用默认参数隐藏必填业务事实，或让Unit函数承担不可见副作用后恢复初态，并由函数合同、调用样例、边界测试、作用域图和重构前后对照确认语义未漂移。
  - **Named Function Arguments**：在建立输入阶段保存输入与结果，触发用默认参数隐藏必填业务事实，或让Unit函数承担不可见副作用后恢复初态，并由函数合同、调用样例、边界测试、作用域图和重构前后对照确认语义未漂移。
  - **For the More Curious: The Nothing Type**：在执行转换阶段保存输入与结果，触发用默认参数隐藏必填业务事实，或让Unit函数承担不可见副作用后恢复初态，并由函数合同、调用样例、边界测试、作用域图和重构前后对照确认语义未漂移。
  - **For the More Curious: File-Level Functions in Java**：在观察产物阶段保存输入与结果，触发用默认参数隐藏必填业务事实，或让Unit函数承担不可见副作用后恢复初态，并由函数合同、调用样例、边界测试、作用域图和重构前后对照确认语义未漂移。
  - **For the More Curious: Function Overloading**：在断言回归阶段保存输入与结果，触发用默认参数隐藏必填业务事实，或让Unit函数承担不可见副作用后恢复初态，并由函数合同、调用样例、边界测试、作用域图和重构前后对照确认语义未漂移。
  - **For the More Curious: Function Names in Backticks**：在声明合同阶段保存输入与结果，触发用默认参数隐藏必填业务事实，或让Unit函数承担不可见副作用后恢复初态，并由函数合同、调用样例、边界测试、作用域图和重构前后对照确认语义未漂移。
  - **Challenge: Single-Expression Functions**：在建立输入阶段保存输入与结果，触发用默认参数隐藏必填业务事实，或让Unit函数承担不可见副作用后恢复初态，并由函数合同、调用样例、边界测试、作用域图和重构前后对照确认语义未漂移。
  - **Challenge: Fireball Inebriation Level**：在执行转换阶段保存输入与结果，触发用默认参数隐藏必填业务事实，或让Unit函数承担不可见副作用后恢复初态，并由函数合同、调用样例、边界测试、作用域图和重构前后对照确认语义未漂移。
  - **Challenge: Inebriation Status**：在观察产物阶段保存输入与结果，触发用默认参数隐藏必填业务事实，或让Unit函数承担不可见副作用后恢复初态，并由函数合同、调用样例、边界测试、作用域图和重构前后对照确认语义未漂移。
</Answer>

**问题 3：反例闭环。** 遇到用默认参数隐藏必填业务事实，或让Unit函数承担不可见副作用时，如何判断修复真正成立？

<Answer>
  保存首个失败诊断，恢复显式输入域、类型或对象边界和求值顺序，再以相同工具链和输入重跑正常、边界、非法三组样例。只有函数合同、调用样例、边界测试、作用域图和重构前后对照回到基线且以签名、参数、返回类型、作用域和可见性设计可组合函数，并用默认值与命名参数表达调用意图的输入、类型边界、求值结果和失败路径都能由独立读者重放重新成立，才能通过。
</Answer>

</Exercises>

## 术语复核与本章回顾

<Glossary>
  <GlossaryItem term="静态合同">
    静态合同在本页限定4. Functions允许进入系统的值与前置条件。
  </GlossaryItem>
  <GlossaryItem term="输入域">
    输入域在本页标记以签名、参数、返回类型、作用域和可见性设计可组合函数，并用默认值与命名参数表达调用意图中状态由谁创建、修改和释放。
  </GlossaryItem>
  <GlossaryItem term="求值顺序">
    求值顺序在本页说明Kotlin 1.2实现与当前迁移之间必须保持的语义。
  </GlossaryItem>
  <GlossaryItem term="可观察结果">
    可观察结果在本页保存能推翻用默认参数隐藏必填业务事实，或让Unit函数承担不可见副作用的原始观察。
  </GlossaryItem>
  <GlossaryItem term="失败出口">
    失败出口在本页把错误停在断言回归而非继续扩散。
  </GlossaryItem>
  <GlossaryItem term="回归断言">
    回归断言在本页让另一位读者凭函数合同、调用样例、边界测试、作用域图和重构前后对照得到相同结论。
  </GlossaryItem>
</Glossary>

完成4. Functions意味着：能解释以签名、参数、返回类型、作用域和可见性设计可组合函数，并用默认值与命名参数表达调用意图，能运行并改动章专属切片，能主动制造用默认参数隐藏必填业务事实，或让Unit函数承担不可见副作用，还能凭函数合同、调用样例、边界测试、作用域图和重构前后对照恢复并交接，而不是只识别术语。

## 阅读导航

[← 上一页：3. Conditionals](/learn/kotlin-definitive-guide/02-language-foundations/kdg1-03-conditionals) · [下一页：5. Anonymous Functions and the Function Type →](/learn/kotlin-definitive-guide/03-functions-null-values/kdg1-05-anonymous-functions)

<Attribution
  mode="independent-rewrite"
  sourceBasis="outline-only"
  workTitle="Josh Skeen、David Greenhalgh《Kotlin Programming: The Big Nerd Ranch Guide》第1版（Addison-Wesley，2018）"
  adaptedUrl="https://www.oreilly.com/library/view/kotlin-programming-the/9780135165188/"
/>
