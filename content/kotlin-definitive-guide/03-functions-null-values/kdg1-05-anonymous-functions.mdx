---
title: 5. Anonymous Functions and the Function Type
description: >-
  5. Anonymous Functions and the Function
  Type：把函数当值传递，理解函数类型、闭包、内联、引用以及高阶函数的输入输出合同，通过全节点状态链、历史迁移对照和故障恢复完成验收。
type: C
section: Kotlin权威指南 · 函数与空安全
draft: true
math: false
sourceUrl: 'https://www.oreilly.com/library/view/kotlin-programming-the/9780135165188/'
demo: true
order: 2
qualityVersion: 2
practiceMode: code
sourceMode: independent-rewrite
---
import { KdgModelLab, KdgFailureLab, KdgEvidenceLab } from "@/components/mdx/kotlin-definitive-guide/diagrams/kdg1-05-anonymous-functions";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能说明把函数当值传递，理解函数类型、闭包、内联、引用以及高阶函数的输入输出合同，并画出输入、类型或对象边界与可见结果。
- 能运行本页保留的 Kotlin 切片，解释它在 Kotlin 1.2/JVM 语境中的编译或执行路径。
- 能操作覆盖、合同和恢复三层实验，让全部 15 个正式节点拥有可见教学因果。
- 能触发闭包意外捕获可变状态，导致调用顺序影响结果，凭函数类型标注、捕获变量实验、内联对照、引用调用和多参数测试完成诊断、修复、复位与同输入重放。

</Objectives>

{/* KOTLIN_DEFINITIVE_GUIDE_QUALITY_V2 */}

## 从本章问题开始

5. Anonymous Functions and the Function Type要解决的不是语法记忆，而是把函数当值传递，理解函数类型、闭包、内联、引用以及高阶函数的输入输出合同。先写出输入和期望结果，再运行最小切片；如果无法解释失败落在编译期、边界层还是运行期，程序即使输出一次正确答案也未通过。

本页不把 val 等同深度不可变，也不把简洁语法等同动态类型。Kotlin 的类型、可空性、求值规则和对象模型共同压缩非法状态；本章不变量是把函数当值传递，理解函数类型、闭包、内联、引用以及高阶函数的输入输出合同的输入、类型边界、求值结果和失败路径都能由独立读者重放。

## 来源范围与年代边界

O’Reilly 的[第1版书目与完整目录](https://www.oreilly.com/library/view/kotlin-programming-the/9780135165188/)只用于核定本页范围，[中文版出版记录](https://e.dangdang.com/touch/products/1901154363.html)用于交叉核对译本信息；未取得的原书正文不作忠实性宣称。5. Anonymous Functions and the Function Type涉及的语言机制由[JetBrains/Android 一手文档](https://kotlinlang.org/docs/basic-syntax.html)独立核验，以下中文讲解、代码组织、图示、实验和练习均为独立教学重写。

第1版的教学坐标是2018年前后的 Kotlin 1.2、JVM、IntelliJ 与当时 Android/早期协程接口。[Kotlin 1.2 发布说明](https://kotlinlang.org/docs/whatsnew12.html)用于锁定历史能力；当前文档只提供迁移对照，Flow、Compose、MockK、现代 Gradle Kotlin DSL 和第二版新增内容不倒灌为原书内容。

## 本章机制与六个检查点

<Term def="静态合同在本页限定5. Anonymous Functions and the Function Type允许进入系统的值与前置条件。">静态合同</Term>、<Term def="输入域在本页标记把函数当值传递，理解函数类型、闭包、内联、引用以及高阶函数的输入输出合同中状态由谁创建、修改和释放。">输入域</Term>、<Term def="求值顺序在本页说明Kotlin 1.2实现与当前迁移之间必须保持的语义。">求值顺序</Term>、<Term def="可观察结果在本页保存能推翻闭包意外捕获可变状态，导致调用顺序影响结果的原始观察。">可观察结果</Term>、<Term def="失败出口在本页把错误停在断言回归而非继续扩散。">失败出口</Term>、<Term def="回归断言在本页让另一位读者凭函数类型标注、捕获变量实验、内联对照、引用调用和多参数测试得到相同结论。">回归断言</Term>。这些检查点共同回答把函数当值传递，理解函数类型、闭包、内联、引用以及高阶函数的输入输出合同，最终必须落到函数类型标注、捕获变量实验、内联对照、引用调用和多参数测试，不能只停在API名称或目录词。

## 先预测，再操作三层实验

先预测：选择一个正式节点后哪一阶段会先改变；切换历史基线与当前迁移时哪项语义必须不变；触发闭包意外捕获可变状态，导致调用顺序影响结果后，应由哪份证据定位并恢复。

<Stepper>
  <Step title="1. 全节点覆盖与状态链">
    选择任一正式节点和机制阶段，检查输入、状态拥有者、可见结果与专属证据；节点列表不截断。

    <KdgModelLab />

  </Step>
  <Step title="2. 历史合同与当前迁移">
    在正常、边界和故障输入间切换，只改变保护合同或技术坐标，观察结果为何变化。

    <KdgFailureLab />

  </Step>
  <Step title="3. 故障、恢复与重放">
    比较正常轨迹、首错轨迹和恢复轨迹；最后点击重置，确认选择项与可见输出都回到初始值。

    <KdgEvidenceLab />

  </Step>
</Stepper>

## 正式目录逐项深读

### 5. Anonymous Functions and the Function Type

5. Anonymous Functions and the Function Type服务于把函数当值传递，理解函数类型、闭包、内联、引用以及高阶函数的输入输出合同。把接收者、参数、返回值、捕获状态和副作用逐项展开，再判断简写是否保持相同求值顺序；函数类型标注、捕获变量实验、内联对照、引用调用和多参数测试必须能区分语法缩短与合同改变。

在 5. Anonymous Functions and the Function Type 的 声明合同 阶段，5. Anonymous Functions and the Function Type先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入建立输入，使用同一份输入把正常路径与闭包意外捕获可变状态，导致调用顺序影响结果产生的反例并排保存。

对 5. Anonymous Functions and the Function Type 的四级验收分别是：正文出现该坐标；解释它如何参与把函数当值传递，理解函数类型、闭包、内联、引用以及高阶函数的输入输出合同；在章专属实验中选择该节点并观察反馈；最后凭函数类型标注、捕获变量实验、内联对照、引用调用和多参数测试回答练习、复位并重放。

### Anonymous Functions

Anonymous Functions服务于把函数当值传递，理解函数类型、闭包、内联、引用以及高阶函数的输入输出合同。把接收者、参数、返回值、捕获状态和副作用逐项展开，再判断简写是否保持相同求值顺序；函数类型标注、捕获变量实验、内联对照、引用调用和多参数测试必须能区分语法缩短与合同改变。

在 5. Anonymous Functions and the Function Type 的 建立输入 阶段，Anonymous Functions先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入执行转换，使用同一份输入把正常路径与闭包意外捕获可变状态，导致调用顺序影响结果产生的反例并排保存。

对 Anonymous Functions 的四级验收分别是：正文出现该坐标；解释它如何参与把函数当值传递，理解函数类型、闭包、内联、引用以及高阶函数的输入输出合同；在章专属实验中选择该节点并观察反馈；最后凭函数类型标注、捕获变量实验、内联对照、引用调用和多参数测试回答练习、复位并重放。

### The function type

The function type服务于把函数当值传递，理解函数类型、闭包、内联、引用以及高阶函数的输入输出合同。把接收者、参数、返回值、捕获状态和副作用逐项展开，再判断简写是否保持相同求值顺序；函数类型标注、捕获变量实验、内联对照、引用调用和多参数测试必须能区分语法缩短与合同改变。

在 5. Anonymous Functions and the Function Type 的 执行转换 阶段，The function type先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入观察产物，使用同一份输入把正常路径与闭包意外捕获可变状态，导致调用顺序影响结果产生的反例并排保存。

对 The function type 的四级验收分别是：正文出现该坐标；解释它如何参与把函数当值传递，理解函数类型、闭包、内联、引用以及高阶函数的输入输出合同；在章专属实验中选择该节点并观察反馈；最后凭函数类型标注、捕获变量实验、内联对照、引用调用和多参数测试回答练习、复位并重放。

### Implicit returns

Implicit returns服务于把函数当值传递，理解函数类型、闭包、内联、引用以及高阶函数的输入输出合同。把这个目录坐标翻译成一个可执行或可判定合同，明确输入、状态变化、可见结果和反例，并以函数类型标注、捕获变量实验、内联对照、引用调用和多参数测试完成独立复核。

在 5. Anonymous Functions and the Function Type 的 观察产物 阶段，Implicit returns先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入断言回归，使用同一份输入把正常路径与闭包意外捕获可变状态，导致调用顺序影响结果产生的反例并排保存。

对 Implicit returns 的四级验收分别是：正文出现该坐标；解释它如何参与把函数当值传递，理解函数类型、闭包、内联、引用以及高阶函数的输入输出合同；在章专属实验中选择该节点并观察反馈；最后凭函数类型标注、捕获变量实验、内联对照、引用调用和多参数测试回答练习、复位并重放。

### Function arguments

Function arguments服务于把函数当值传递，理解函数类型、闭包、内联、引用以及高阶函数的输入输出合同。把接收者、参数、返回值、捕获状态和副作用逐项展开，再判断简写是否保持相同求值顺序；函数类型标注、捕获变量实验、内联对照、引用调用和多参数测试必须能区分语法缩短与合同改变。

在 5. Anonymous Functions and the Function Type 的 断言回归 阶段，Function arguments先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入声明合同，使用同一份输入把正常路径与闭包意外捕获可变状态，导致调用顺序影响结果产生的反例并排保存。

对 Function arguments 的四级验收分别是：正文出现该坐标；解释它如何参与把函数当值传递，理解函数类型、闭包、内联、引用以及高阶函数的输入输出合同；在章专属实验中选择该节点并观察反馈；最后凭函数类型标注、捕获变量实验、内联对照、引用调用和多参数测试回答练习、复位并重放。

### The it keyword

The it keyword服务于把函数当值传递，理解函数类型、闭包、内联、引用以及高阶函数的输入输出合同。把这个目录坐标翻译成一个可执行或可判定合同，明确输入、状态变化、可见结果和反例，并以函数类型标注、捕获变量实验、内联对照、引用调用和多参数测试完成独立复核。

在 5. Anonymous Functions and the Function Type 的 声明合同 阶段，The it keyword先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入建立输入，使用同一份输入把正常路径与闭包意外捕获可变状态，导致调用顺序影响结果产生的反例并排保存。

对 The it keyword 的四级验收分别是：正文出现该坐标；解释它如何参与把函数当值传递，理解函数类型、闭包、内联、引用以及高阶函数的输入输出合同；在章专属实验中选择该节点并观察反馈；最后凭函数类型标注、捕获变量实验、内联对照、引用调用和多参数测试回答练习、复位并重放。

### Accepting multiple arguments

Accepting multiple arguments服务于把函数当值传递，理解函数类型、闭包、内联、引用以及高阶函数的输入输出合同。把这个目录坐标翻译成一个可执行或可判定合同，明确输入、状态变化、可见结果和反例，并以函数类型标注、捕获变量实验、内联对照、引用调用和多参数测试完成独立复核。

在 5. Anonymous Functions and the Function Type 的 建立输入 阶段，Accepting multiple arguments先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入执行转换，使用同一份输入把正常路径与闭包意外捕获可变状态，导致调用顺序影响结果产生的反例并排保存。

对 Accepting multiple arguments 的四级验收分别是：正文出现该坐标；解释它如何参与把函数当值传递，理解函数类型、闭包、内联、引用以及高阶函数的输入输出合同；在章专属实验中选择该节点并观察反馈；最后凭函数类型标注、捕获变量实验、内联对照、引用调用和多参数测试回答练习、复位并重放。

### Type Inference Support

Type Inference Support服务于把函数当值传递，理解函数类型、闭包、内联、引用以及高阶函数的输入输出合同。画出类型关系、状态拥有者、初始化时刻和允许替换，再用编译失败或替身实现挑战边界；验收时检查函数类型标注、捕获变量实验、内联对照、引用调用和多参数测试是否支持对象不变量。

在 5. Anonymous Functions and the Function Type 的 执行转换 阶段，Type Inference Support先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入观察产物，使用同一份输入把正常路径与闭包意外捕获可变状态，导致调用顺序影响结果产生的反例并排保存。

对 Type Inference Support 的四级验收分别是：正文出现该坐标；解释它如何参与把函数当值传递，理解函数类型、闭包、内联、引用以及高阶函数的输入输出合同；在章专属实验中选择该节点并观察反馈；最后凭函数类型标注、捕获变量实验、内联对照、引用调用和多参数测试回答练习、复位并重放。

### Defining a Function That Accepts a Function

Defining a Function That Accepts a Function服务于把函数当值传递，理解函数类型、闭包、内联、引用以及高阶函数的输入输出合同。把接收者、参数、返回值、捕获状态和副作用逐项展开，再判断简写是否保持相同求值顺序；函数类型标注、捕获变量实验、内联对照、引用调用和多参数测试必须能区分语法缩短与合同改变。

在 5. Anonymous Functions and the Function Type 的 观察产物 阶段，Defining a Function That Accepts a Function先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入断言回归，使用同一份输入把正常路径与闭包意外捕获可变状态，导致调用顺序影响结果产生的反例并排保存。

对 Defining a Function That Accepts a Function 的四级验收分别是：正文出现该坐标；解释它如何参与把函数当值传递，理解函数类型、闭包、内联、引用以及高阶函数的输入输出合同；在章专属实验中选择该节点并观察反馈；最后凭函数类型标注、捕获变量实验、内联对照、引用调用和多参数测试回答练习、复位并重放。

### Shorthand syntax

Shorthand syntax服务于把函数当值传递，理解函数类型、闭包、内联、引用以及高阶函数的输入输出合同。把这个目录坐标翻译成一个可执行或可判定合同，明确输入、状态变化、可见结果和反例，并以函数类型标注、捕获变量实验、内联对照、引用调用和多参数测试完成独立复核。

在 5. Anonymous Functions and the Function Type 的 断言回归 阶段，Shorthand syntax先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入声明合同，使用同一份输入把正常路径与闭包意外捕获可变状态，导致调用顺序影响结果产生的反例并排保存。

对 Shorthand syntax 的四级验收分别是：正文出现该坐标；解释它如何参与把函数当值传递，理解函数类型、闭包、内联、引用以及高阶函数的输入输出合同；在章专属实验中选择该节点并观察反馈；最后凭函数类型标注、捕获变量实验、内联对照、引用调用和多参数测试回答练习、复位并重放。

### Function Inlining

Function Inlining服务于把函数当值传递，理解函数类型、闭包、内联、引用以及高阶函数的输入输出合同。把接收者、参数、返回值、捕获状态和副作用逐项展开，再判断简写是否保持相同求值顺序；函数类型标注、捕获变量实验、内联对照、引用调用和多参数测试必须能区分语法缩短与合同改变。

在 5. Anonymous Functions and the Function Type 的 声明合同 阶段，Function Inlining先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入建立输入，使用同一份输入把正常路径与闭包意外捕获可变状态，导致调用顺序影响结果产生的反例并排保存。

对 Function Inlining 的四级验收分别是：正文出现该坐标；解释它如何参与把函数当值传递，理解函数类型、闭包、内联、引用以及高阶函数的输入输出合同；在章专属实验中选择该节点并观察反馈；最后凭函数类型标注、捕获变量实验、内联对照、引用调用和多参数测试回答练习、复位并重放。

### Function References

Function References服务于把函数当值传递，理解函数类型、闭包、内联、引用以及高阶函数的输入输出合同。把接收者、参数、返回值、捕获状态和副作用逐项展开，再判断简写是否保持相同求值顺序；函数类型标注、捕获变量实验、内联对照、引用调用和多参数测试必须能区分语法缩短与合同改变。

在 5. Anonymous Functions and the Function Type 的 建立输入 阶段，Function References先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入执行转换，使用同一份输入把正常路径与闭包意外捕获可变状态，导致调用顺序影响结果产生的反例并排保存。

对 Function References 的四级验收分别是：正文出现该坐标；解释它如何参与把函数当值传递，理解函数类型、闭包、内联、引用以及高阶函数的输入输出合同；在章专属实验中选择该节点并观察反馈；最后凭函数类型标注、捕获变量实验、内联对照、引用调用和多参数测试回答练习、复位并重放。

### Function Type as Return Type

Function Type as Return Type服务于把函数当值传递，理解函数类型、闭包、内联、引用以及高阶函数的输入输出合同。把接收者、参数、返回值、捕获状态和副作用逐项展开，再判断简写是否保持相同求值顺序；函数类型标注、捕获变量实验、内联对照、引用调用和多参数测试必须能区分语法缩短与合同改变。

在 5. Anonymous Functions and the Function Type 的 执行转换 阶段，Function Type as Return Type先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入观察产物，使用同一份输入把正常路径与闭包意外捕获可变状态，导致调用顺序影响结果产生的反例并排保存。

对 Function Type as Return Type 的四级验收分别是：正文出现该坐标；解释它如何参与把函数当值传递，理解函数类型、闭包、内联、引用以及高阶函数的输入输出合同；在章专属实验中选择该节点并观察反馈；最后凭函数类型标注、捕获变量实验、内联对照、引用调用和多参数测试回答练习、复位并重放。

### For the More Curious: Kotlin’s Lambdas Are Closures

For the More Curious: Kotlin’s Lambdas Are Closures服务于把函数当值传递，理解函数类型、闭包、内联、引用以及高阶函数的输入输出合同。把接收者、参数、返回值、捕获状态和副作用逐项展开，再判断简写是否保持相同求值顺序；函数类型标注、捕获变量实验、内联对照、引用调用和多参数测试必须能区分语法缩短与合同改变。

在 5. Anonymous Functions and the Function Type 的 观察产物 阶段，For the More Curious: Kotlin’s Lambdas Are Closures先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入断言回归，使用同一份输入把正常路径与闭包意外捕获可变状态，导致调用顺序影响结果产生的反例并排保存。

对 For the More Curious: Kotlin’s Lambdas Are Closures 的四级验收分别是：正文出现该坐标；解释它如何参与把函数当值传递，理解函数类型、闭包、内联、引用以及高阶函数的输入输出合同；在章专属实验中选择该节点并观察反馈；最后凭函数类型标注、捕获变量实验、内联对照、引用调用和多参数测试回答练习、复位并重放。

### For the More Curious: Lambdas vs Anonymous Inner Classes

For the More Curious: Lambdas vs Anonymous Inner Classes服务于把函数当值传递，理解函数类型、闭包、内联、引用以及高阶函数的输入输出合同。把接收者、参数、返回值、捕获状态和副作用逐项展开，再判断简写是否保持相同求值顺序；函数类型标注、捕获变量实验、内联对照、引用调用和多参数测试必须能区分语法缩短与合同改变。

在 5. Anonymous Functions and the Function Type 的 断言回归 阶段，For the More Curious: Lambdas vs Anonymous Inner Classes先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入声明合同，使用同一份输入把正常路径与闭包意外捕获可变状态，导致调用顺序影响结果产生的反例并排保存。

对 For the More Curious: Lambdas vs Anonymous Inner Classes 的四级验收分别是：正文出现该坐标；解释它如何参与把函数当值传递，理解函数类型、闭包、内联、引用以及高阶函数的输入输出合同；在章专属实验中选择该节点并观察反馈；最后凭函数类型标注、捕获变量实验、内联对照、引用调用和多参数测试回答练习、复位并重放。

## 最小可执行切片

```kotlin
fun configureGreeting(transform: (String) -> String): (String) -> String =
    { name -> transform("Welcome, " + name) }

val loud = configureGreeting(String::toUpperCase)
check(loud("Madrigal") == "WELCOME, MADRIGAL")
```

运行切片时固定 Kotlin/JDK 或 Android 工具链版本、输入数据和命令；保存标准输出、编译诊断及断言。迁移实验必须另建记录，不能静默改写2018年的历史坐标。

<Callout type="trap" title="本章核心陷阱">
  闭包意外捕获可变状态，导致调用顺序影响结果。先缩小到最小反例，明确是类型合同、对象所有权、求值顺序还是平台边界失效，再用同输入验证修复。
</Callout>

<Callout type="trap" title="把当前惯例倒灌进第一版">
  5. Anonymous Functions and the Function Type保留 Kotlin 1.2/JVM 的历史身份；当前 Kotlin 或 Android 方案只作单独迁移坐标，不能据此改写第一版目录和接口身份。
</Callout>

<Callout type="trap" title="只保留成功输出">
  函数类型标注、捕获变量实验、内联对照、引用调用和多参数测试必须同时包含正常、边界和失败路径；如果故障后不能恢复并复位，本页实践闭环仍未完成。
</Callout>

## 练习、答案与四级证据

<Exercises>

**问题 1：历史与迁移。** 怎样既保留 Kotlin 1.2 的教学身份，又使用当前官方资料核验机制？

<Answer>
  先以[Kotlin 1.2发布说明](https://kotlinlang.org/docs/whatsnew12.html)和目录锁定历史范围，再用[当前技术文档](https://kotlinlang.org/docs/basic-syntax.html)建立单独迁移记录。比较相同输入、结果和不变量；只改变版本坐标，不把当前API伪装成第一版内容。
</Answer>

**问题 2：逐节点验证。** 本页 15 个正式节点怎样从出现升级到可复查证据？

<Answer>
  - **5. Anonymous Functions and the Function Type**：在声明合同阶段保存输入与结果，触发闭包意外捕获可变状态，导致调用顺序影响结果后恢复初态，并由函数类型标注、捕获变量实验、内联对照、引用调用和多参数测试确认语义未漂移。
  - **Anonymous Functions**：在建立输入阶段保存输入与结果，触发闭包意外捕获可变状态，导致调用顺序影响结果后恢复初态，并由函数类型标注、捕获变量实验、内联对照、引用调用和多参数测试确认语义未漂移。
  - **The function type**：在执行转换阶段保存输入与结果，触发闭包意外捕获可变状态，导致调用顺序影响结果后恢复初态，并由函数类型标注、捕获变量实验、内联对照、引用调用和多参数测试确认语义未漂移。
  - **Implicit returns**：在观察产物阶段保存输入与结果，触发闭包意外捕获可变状态，导致调用顺序影响结果后恢复初态，并由函数类型标注、捕获变量实验、内联对照、引用调用和多参数测试确认语义未漂移。
  - **Function arguments**：在断言回归阶段保存输入与结果，触发闭包意外捕获可变状态，导致调用顺序影响结果后恢复初态，并由函数类型标注、捕获变量实验、内联对照、引用调用和多参数测试确认语义未漂移。
  - **The it keyword**：在声明合同阶段保存输入与结果，触发闭包意外捕获可变状态，导致调用顺序影响结果后恢复初态，并由函数类型标注、捕获变量实验、内联对照、引用调用和多参数测试确认语义未漂移。
  - **Accepting multiple arguments**：在建立输入阶段保存输入与结果，触发闭包意外捕获可变状态，导致调用顺序影响结果后恢复初态，并由函数类型标注、捕获变量实验、内联对照、引用调用和多参数测试确认语义未漂移。
  - **Type Inference Support**：在执行转换阶段保存输入与结果，触发闭包意外捕获可变状态，导致调用顺序影响结果后恢复初态，并由函数类型标注、捕获变量实验、内联对照、引用调用和多参数测试确认语义未漂移。
  - **Defining a Function That Accepts a Function**：在观察产物阶段保存输入与结果，触发闭包意外捕获可变状态，导致调用顺序影响结果后恢复初态，并由函数类型标注、捕获变量实验、内联对照、引用调用和多参数测试确认语义未漂移。
  - **Shorthand syntax**：在断言回归阶段保存输入与结果，触发闭包意外捕获可变状态，导致调用顺序影响结果后恢复初态，并由函数类型标注、捕获变量实验、内联对照、引用调用和多参数测试确认语义未漂移。
  - **Function Inlining**：在声明合同阶段保存输入与结果，触发闭包意外捕获可变状态，导致调用顺序影响结果后恢复初态，并由函数类型标注、捕获变量实验、内联对照、引用调用和多参数测试确认语义未漂移。
  - **Function References**：在建立输入阶段保存输入与结果，触发闭包意外捕获可变状态，导致调用顺序影响结果后恢复初态，并由函数类型标注、捕获变量实验、内联对照、引用调用和多参数测试确认语义未漂移。
  - **Function Type as Return Type**：在执行转换阶段保存输入与结果，触发闭包意外捕获可变状态，导致调用顺序影响结果后恢复初态，并由函数类型标注、捕获变量实验、内联对照、引用调用和多参数测试确认语义未漂移。
  - **For the More Curious: Kotlin’s Lambdas Are Closures**：在观察产物阶段保存输入与结果，触发闭包意外捕获可变状态，导致调用顺序影响结果后恢复初态，并由函数类型标注、捕获变量实验、内联对照、引用调用和多参数测试确认语义未漂移。
  - **For the More Curious: Lambdas vs Anonymous Inner Classes**：在断言回归阶段保存输入与结果，触发闭包意外捕获可变状态，导致调用顺序影响结果后恢复初态，并由函数类型标注、捕获变量实验、内联对照、引用调用和多参数测试确认语义未漂移。
</Answer>

**问题 3：反例闭环。** 遇到闭包意外捕获可变状态，导致调用顺序影响结果时，如何判断修复真正成立？

<Answer>
  保存首个失败诊断，恢复显式输入域、类型或对象边界和求值顺序，再以相同工具链和输入重跑正常、边界、非法三组样例。只有函数类型标注、捕获变量实验、内联对照、引用调用和多参数测试回到基线且把函数当值传递，理解函数类型、闭包、内联、引用以及高阶函数的输入输出合同的输入、类型边界、求值结果和失败路径都能由独立读者重放重新成立，才能通过。
</Answer>

</Exercises>

## 术语复核与本章回顾

<Glossary>
  <GlossaryItem term="静态合同">
    静态合同在本页限定5. Anonymous Functions and the Function Type允许进入系统的值与前置条件。
  </GlossaryItem>
  <GlossaryItem term="输入域">
    输入域在本页标记把函数当值传递，理解函数类型、闭包、内联、引用以及高阶函数的输入输出合同中状态由谁创建、修改和释放。
  </GlossaryItem>
  <GlossaryItem term="求值顺序">
    求值顺序在本页说明Kotlin 1.2实现与当前迁移之间必须保持的语义。
  </GlossaryItem>
  <GlossaryItem term="可观察结果">
    可观察结果在本页保存能推翻闭包意外捕获可变状态，导致调用顺序影响结果的原始观察。
  </GlossaryItem>
  <GlossaryItem term="失败出口">
    失败出口在本页把错误停在断言回归而非继续扩散。
  </GlossaryItem>
  <GlossaryItem term="回归断言">
    回归断言在本页让另一位读者凭函数类型标注、捕获变量实验、内联对照、引用调用和多参数测试得到相同结论。
  </GlossaryItem>
</Glossary>

完成5. Anonymous Functions and the Function Type意味着：能解释把函数当值传递，理解函数类型、闭包、内联、引用以及高阶函数的输入输出合同，能运行并改动章专属切片，能主动制造闭包意外捕获可变状态，导致调用顺序影响结果，还能凭函数类型标注、捕获变量实验、内联对照、引用调用和多参数测试恢复并交接，而不是只识别术语。

## 阅读导航

[← 上一页：4. Functions](/learn/kotlin-definitive-guide/03-functions-null-values/kdg1-04-functions) · [下一页：6. Null Safety and Exceptions →](/learn/kotlin-definitive-guide/03-functions-null-values/kdg1-06-null-safety-exceptions)

<Attribution
  mode="independent-rewrite"
  sourceBasis="outline-only"
  workTitle="Josh Skeen、David Greenhalgh《Kotlin Programming: The Big Nerd Ranch Guide》第1版（Addison-Wesley，2018）"
  adaptedUrl="https://www.oreilly.com/library/view/kotlin-programming-the/9780135165188/"
/>
