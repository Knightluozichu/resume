---
title: 6. Null Safety and Exceptions
description: >-
  6. Null Safety and
  Exceptions：用可空类型、安全调用、let、Elvis、先决条件与异常把缺失值和失败路径显式化，通过全节点状态链、历史迁移对照和故障恢复完成验收。
type: C
section: Kotlin权威指南 · 函数与空安全
draft: false
math: false
sourceUrl: 'https://www.oreilly.com/library/view/kotlin-programming-the/9780135165188/'
demo: true
order: 3
qualityVersion: 2
practiceMode: diagnosis
sourceMode: independent-rewrite
---


{/* KOTLIN_DEFINITIVE_GUIDE_QUALITY_V2 */}

## 从本章问题开始

6. Null Safety and Exceptions要解决的不是语法记忆，而是用可空类型、安全调用、let、Elvis、先决条件与异常把缺失值和失败路径显式化。先写出输入和期望结果，再运行最小切片；如果无法解释失败落在编译期、边界层还是运行期，程序即使输出一次正确答案也未通过。

本页不把 val 等同深度不可变，也不把简洁语法等同动态类型。Kotlin 的类型、可空性、求值规则和对象模型共同压缩非法状态；本章不变量是用可空类型、安全调用、let、Elvis、先决条件与异常把缺失值和失败路径显式化的输入、类型边界、求值结果和失败路径都能由独立读者重放。

## 来源范围与年代边界

O’Reilly 的[第1版书目与完整目录](https://www.oreilly.com/library/view/kotlin-programming-the/9780135165188/)只用于核定本页范围，[中文版出版记录](https://e.dangdang.com/touch/products/1901154363.html)用于交叉核对译本信息；未取得的原书正文不作忠实性宣称。6. Null Safety and Exceptions涉及的语言机制由[JetBrains/Android 一手文档](https://kotlinlang.org/docs/null-safety.html)独立核验，以下中文讲解、代码组织、图示、实验和练习均为独立教学重写。

第1版的教学坐标是2018年前后的 Kotlin 1.2、JVM、IntelliJ 与当时 Android/早期协程接口。[Kotlin 1.2 发布说明](https://kotlinlang.org/docs/whatsnew12.html)用于锁定历史能力；当前文档只提供迁移对照，Flow、Compose、MockK、现代 Gradle Kotlin DSL 和第二版新增内容不倒灌为原书内容。

## 本章机制与六个检查点

、、、、、。这些检查点共同回答用可空类型、安全调用、let、Elvis、先决条件与异常把缺失值和失败路径显式化，最终必须落到可空数据流图、三种处理策略、异常分类、失败测试和平台类型边界，不能只停在API名称或目录词。

## 正式目录逐项深读

### 6. Null Safety and Exceptions

6. Null Safety and Exceptions服务于用可空类型、安全调用、let、Elvis、先决条件与异常把缺失值和失败路径显式化。这里要把缺失值、异常来源和调用者责任分别放进类型或失败合同，禁止用强制解包、空捕获或虚假默认值擦除风险；用可空数据流图、三种处理策略、异常分类、失败测试和平台类型边界定位最早失效处。

在 6. Null Safety and Exceptions 的 建立基线 阶段，6. Null Safety and Exceptions先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入注入反例，使用同一份输入把正常路径与用双感叹号绕过类型系统，或吞掉异常后伪装成功产生的反例并排保存。

对 6. Null Safety and Exceptions 的四级验收分别是：正文出现该坐标；解释它如何参与用可空类型、安全调用、let、Elvis、先决条件与异常把缺失值和失败路径显式化；在章专属实验中选择该节点并观察反馈；最后凭可空数据流图、三种处理策略、异常分类、失败测试和平台类型边界回答练习、复位并重放。

### Nullability

Nullability服务于用可空类型、安全调用、let、Elvis、先决条件与异常把缺失值和失败路径显式化。这里要把缺失值、异常来源和调用者责任分别放进类型或失败合同，禁止用强制解包、空捕获或虚假默认值擦除风险；用可空数据流图、三种处理策略、异常分类、失败测试和平台类型边界定位最早失效处。

在 6. Null Safety and Exceptions 的 注入反例 阶段，Nullability先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入定位边界，使用同一份输入把正常路径与用双感叹号绕过类型系统，或吞掉异常后伪装成功产生的反例并排保存。

对 Nullability 的四级验收分别是：正文出现该坐标；解释它如何参与用可空类型、安全调用、let、Elvis、先决条件与异常把缺失值和失败路径显式化；在章专属实验中选择该节点并观察反馈；最后凭可空数据流图、三种处理策略、异常分类、失败测试和平台类型边界回答练习、复位并重放。

### Kotlin’s Explicit Null Type

Kotlin’s Explicit Null Type服务于用可空类型、安全调用、let、Elvis、先决条件与异常把缺失值和失败路径显式化。这里要把缺失值、异常来源和调用者责任分别放进类型或失败合同，禁止用强制解包、空捕获或虚假默认值擦除风险；用可空数据流图、三种处理策略、异常分类、失败测试和平台类型边界定位最早失效处。

在 6. Null Safety and Exceptions 的 定位边界 阶段，Kotlin’s Explicit Null Type先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入修复合同，使用同一份输入把正常路径与用双感叹号绕过类型系统，或吞掉异常后伪装成功产生的反例并排保存。

对 Kotlin’s Explicit Null Type 的四级验收分别是：正文出现该坐标；解释它如何参与用可空类型、安全调用、let、Elvis、先决条件与异常把缺失值和失败路径显式化；在章专属实验中选择该节点并观察反馈；最后凭可空数据流图、三种处理策略、异常分类、失败测试和平台类型边界回答练习、复位并重放。

### Compile Time vs Runtime

Compile Time vs Runtime服务于用可空类型、安全调用、let、Elvis、先决条件与异常把缺失值和失败路径显式化。把接收者、参数、返回值、捕获状态和副作用逐项展开，再判断简写是否保持相同求值顺序；可空数据流图、三种处理策略、异常分类、失败测试和平台类型边界必须能区分语法缩短与合同改变。

在 6. Null Safety and Exceptions 的 修复合同 阶段，Compile Time vs Runtime先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入同输入复验，使用同一份输入把正常路径与用双感叹号绕过类型系统，或吞掉异常后伪装成功产生的反例并排保存。

对 Compile Time vs Runtime 的四级验收分别是：正文出现该坐标；解释它如何参与用可空类型、安全调用、let、Elvis、先决条件与异常把缺失值和失败路径显式化；在章专属实验中选择该节点并观察反馈；最后凭可空数据流图、三种处理策略、异常分类、失败测试和平台类型边界回答练习、复位并重放。

### Null Safety

Null Safety服务于用可空类型、安全调用、let、Elvis、先决条件与异常把缺失值和失败路径显式化。这里要把缺失值、异常来源和调用者责任分别放进类型或失败合同，禁止用强制解包、空捕获或虚假默认值擦除风险；用可空数据流图、三种处理策略、异常分类、失败测试和平台类型边界定位最早失效处。

在 6. Null Safety and Exceptions 的 同输入复验 阶段，Null Safety先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入建立基线，使用同一份输入把正常路径与用双感叹号绕过类型系统，或吞掉异常后伪装成功产生的反例并排保存。

对 Null Safety 的四级验收分别是：正文出现该坐标；解释它如何参与用可空类型、安全调用、let、Elvis、先决条件与异常把缺失值和失败路径显式化；在章专属实验中选择该节点并观察反馈；最后凭可空数据流图、三种处理策略、异常分类、失败测试和平台类型边界回答练习、复位并重放。

### Option one: the safe call operator

Option one: the safe call operator服务于用可空类型、安全调用、let、Elvis、先决条件与异常把缺失值和失败路径显式化。把这个目录坐标翻译成一个可执行或可判定合同，明确输入、状态变化、可见结果和反例，并以可空数据流图、三种处理策略、异常分类、失败测试和平台类型边界完成独立复核。

在 6. Null Safety and Exceptions 的 建立基线 阶段，Option one: the safe call operator先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入注入反例，使用同一份输入把正常路径与用双感叹号绕过类型系统，或吞掉异常后伪装成功产生的反例并排保存。

对 Option one: the safe call operator 的四级验收分别是：正文出现该坐标；解释它如何参与用可空类型、安全调用、let、Elvis、先决条件与异常把缺失值和失败路径显式化；在章专属实验中选择该节点并观察反馈；最后凭可空数据流图、三种处理策略、异常分类、失败测试和平台类型边界回答练习、复位并重放。

### Using safe calls with let

Using safe calls with let服务于用可空类型、安全调用、let、Elvis、先决条件与异常把缺失值和失败路径显式化。把接收者、参数、返回值、捕获状态和副作用逐项展开，再判断简写是否保持相同求值顺序；可空数据流图、三种处理策略、异常分类、失败测试和平台类型边界必须能区分语法缩短与合同改变。

在 6. Null Safety and Exceptions 的 注入反例 阶段，Using safe calls with let先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入定位边界，使用同一份输入把正常路径与用双感叹号绕过类型系统，或吞掉异常后伪装成功产生的反例并排保存。

对 Using safe calls with let 的四级验收分别是：正文出现该坐标；解释它如何参与用可空类型、安全调用、let、Elvis、先决条件与异常把缺失值和失败路径显式化；在章专属实验中选择该节点并观察反馈；最后凭可空数据流图、三种处理策略、异常分类、失败测试和平台类型边界回答练习、复位并重放。

### Option two: the double-bang operator

Option two: the double-bang operator服务于用可空类型、安全调用、let、Elvis、先决条件与异常把缺失值和失败路径显式化。把这个目录坐标翻译成一个可执行或可判定合同，明确输入、状态变化、可见结果和反例，并以可空数据流图、三种处理策略、异常分类、失败测试和平台类型边界完成独立复核。

在 6. Null Safety and Exceptions 的 定位边界 阶段，Option two: the double-bang operator先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入修复合同，使用同一份输入把正常路径与用双感叹号绕过类型系统，或吞掉异常后伪装成功产生的反例并排保存。

对 Option two: the double-bang operator 的四级验收分别是：正文出现该坐标；解释它如何参与用可空类型、安全调用、let、Elvis、先决条件与异常把缺失值和失败路径显式化；在章专属实验中选择该节点并观察反馈；最后凭可空数据流图、三种处理策略、异常分类、失败测试和平台类型边界回答练习、复位并重放。

### Option three: checking whether a value is null with if

Option three: checking whether a value is null with if服务于用可空类型、安全调用、let、Elvis、先决条件与异常把缺失值和失败路径显式化。这里要把缺失值、异常来源和调用者责任分别放进类型或失败合同，禁止用强制解包、空捕获或虚假默认值擦除风险；用可空数据流图、三种处理策略、异常分类、失败测试和平台类型边界定位最早失效处。

在 6. Null Safety and Exceptions 的 修复合同 阶段，Option three: checking whether a value is null with if先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入同输入复验，使用同一份输入把正常路径与用双感叹号绕过类型系统，或吞掉异常后伪装成功产生的反例并排保存。

对 Option three: checking whether a value is null with if 的四级验收分别是：正文出现该坐标；解释它如何参与用可空类型、安全调用、let、Elvis、先决条件与异常把缺失值和失败路径显式化；在章专属实验中选择该节点并观察反馈；最后凭可空数据流图、三种处理策略、异常分类、失败测试和平台类型边界回答练习、复位并重放。

### The null coalescing operator

The null coalescing operator服务于用可空类型、安全调用、let、Elvis、先决条件与异常把缺失值和失败路径显式化。这里要把缺失值、异常来源和调用者责任分别放进类型或失败合同，禁止用强制解包、空捕获或虚假默认值擦除风险；用可空数据流图、三种处理策略、异常分类、失败测试和平台类型边界定位最早失效处。

在 6. Null Safety and Exceptions 的 同输入复验 阶段，The null coalescing operator先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入建立基线，使用同一份输入把正常路径与用双感叹号绕过类型系统，或吞掉异常后伪装成功产生的反例并排保存。

对 The null coalescing operator 的四级验收分别是：正文出现该坐标；解释它如何参与用可空类型、安全调用、let、Elvis、先决条件与异常把缺失值和失败路径显式化；在章专属实验中选择该节点并观察反馈；最后凭可空数据流图、三种处理策略、异常分类、失败测试和平台类型边界回答练习、复位并重放。

### Exceptions

Exceptions服务于用可空类型、安全调用、let、Elvis、先决条件与异常把缺失值和失败路径显式化。这里要把缺失值、异常来源和调用者责任分别放进类型或失败合同，禁止用强制解包、空捕获或虚假默认值擦除风险；用可空数据流图、三种处理策略、异常分类、失败测试和平台类型边界定位最早失效处。

在 6. Null Safety and Exceptions 的 建立基线 阶段，Exceptions先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入注入反例，使用同一份输入把正常路径与用双感叹号绕过类型系统，或吞掉异常后伪装成功产生的反例并排保存。

对 Exceptions 的四级验收分别是：正文出现该坐标；解释它如何参与用可空类型、安全调用、let、Elvis、先决条件与异常把缺失值和失败路径显式化；在章专属实验中选择该节点并观察反馈；最后凭可空数据流图、三种处理策略、异常分类、失败测试和平台类型边界回答练习、复位并重放。

### Throwing an exception

Throwing an exception服务于用可空类型、安全调用、let、Elvis、先决条件与异常把缺失值和失败路径显式化。这里要把缺失值、异常来源和调用者责任分别放进类型或失败合同，禁止用强制解包、空捕获或虚假默认值擦除风险；用可空数据流图、三种处理策略、异常分类、失败测试和平台类型边界定位最早失效处。

在 6. Null Safety and Exceptions 的 注入反例 阶段，Throwing an exception先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入定位边界，使用同一份输入把正常路径与用双感叹号绕过类型系统，或吞掉异常后伪装成功产生的反例并排保存。

对 Throwing an exception 的四级验收分别是：正文出现该坐标；解释它如何参与用可空类型、安全调用、let、Elvis、先决条件与异常把缺失值和失败路径显式化；在章专属实验中选择该节点并观察反馈；最后凭可空数据流图、三种处理策略、异常分类、失败测试和平台类型边界回答练习、复位并重放。

### Custom exceptions

Custom exceptions服务于用可空类型、安全调用、let、Elvis、先决条件与异常把缺失值和失败路径显式化。这里要把缺失值、异常来源和调用者责任分别放进类型或失败合同，禁止用强制解包、空捕获或虚假默认值擦除风险；用可空数据流图、三种处理策略、异常分类、失败测试和平台类型边界定位最早失效处。

在 6. Null Safety and Exceptions 的 定位边界 阶段，Custom exceptions先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入修复合同，使用同一份输入把正常路径与用双感叹号绕过类型系统，或吞掉异常后伪装成功产生的反例并排保存。

对 Custom exceptions 的四级验收分别是：正文出现该坐标；解释它如何参与用可空类型、安全调用、let、Elvis、先决条件与异常把缺失值和失败路径显式化；在章专属实验中选择该节点并观察反馈；最后凭可空数据流图、三种处理策略、异常分类、失败测试和平台类型边界回答练习、复位并重放。

### Handling exceptions

Handling exceptions服务于用可空类型、安全调用、let、Elvis、先决条件与异常把缺失值和失败路径显式化。这里要把缺失值、异常来源和调用者责任分别放进类型或失败合同，禁止用强制解包、空捕获或虚假默认值擦除风险；用可空数据流图、三种处理策略、异常分类、失败测试和平台类型边界定位最早失效处。

在 6. Null Safety and Exceptions 的 修复合同 阶段，Handling exceptions先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入同输入复验，使用同一份输入把正常路径与用双感叹号绕过类型系统，或吞掉异常后伪装成功产生的反例并排保存。

对 Handling exceptions 的四级验收分别是：正文出现该坐标；解释它如何参与用可空类型、安全调用、let、Elvis、先决条件与异常把缺失值和失败路径显式化；在章专属实验中选择该节点并观察反馈；最后凭可空数据流图、三种处理策略、异常分类、失败测试和平台类型边界回答练习、复位并重放。

### Preconditions

Preconditions服务于用可空类型、安全调用、let、Elvis、先决条件与异常把缺失值和失败路径显式化。这里要把缺失值、异常来源和调用者责任分别放进类型或失败合同，禁止用强制解包、空捕获或虚假默认值擦除风险；用可空数据流图、三种处理策略、异常分类、失败测试和平台类型边界定位最早失效处。

在 6. Null Safety and Exceptions 的 同输入复验 阶段，Preconditions先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入建立基线，使用同一份输入把正常路径与用双感叹号绕过类型系统，或吞掉异常后伪装成功产生的反例并排保存。

对 Preconditions 的四级验收分别是：正文出现该坐标；解释它如何参与用可空类型、安全调用、let、Elvis、先决条件与异常把缺失值和失败路径显式化；在章专属实验中选择该节点并观察反馈；最后凭可空数据流图、三种处理策略、异常分类、失败测试和平台类型边界回答练习、复位并重放。

### Null: What Is It Good For?

Null: What Is It Good For?服务于用可空类型、安全调用、let、Elvis、先决条件与异常把缺失值和失败路径显式化。这里要把缺失值、异常来源和调用者责任分别放进类型或失败合同，禁止用强制解包、空捕获或虚假默认值擦除风险；用可空数据流图、三种处理策略、异常分类、失败测试和平台类型边界定位最早失效处。

在 6. Null Safety and Exceptions 的 建立基线 阶段，Null: What Is It Good For?先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入注入反例，使用同一份输入把正常路径与用双感叹号绕过类型系统，或吞掉异常后伪装成功产生的反例并排保存。

对 Null: What Is It Good For? 的四级验收分别是：正文出现该坐标；解释它如何参与用可空类型、安全调用、let、Elvis、先决条件与异常把缺失值和失败路径显式化；在章专属实验中选择该节点并观察反馈；最后凭可空数据流图、三种处理策略、异常分类、失败测试和平台类型边界回答练习、复位并重放。

### For the More Curious: Checked vs Unchecked Exceptions

For the More Curious: Checked vs Unchecked Exceptions服务于用可空类型、安全调用、let、Elvis、先决条件与异常把缺失值和失败路径显式化。这里要把缺失值、异常来源和调用者责任分别放进类型或失败合同，禁止用强制解包、空捕获或虚假默认值擦除风险；用可空数据流图、三种处理策略、异常分类、失败测试和平台类型边界定位最早失效处。

在 6. Null Safety and Exceptions 的 注入反例 阶段，For the More Curious: Checked vs Unchecked Exceptions先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入定位边界，使用同一份输入把正常路径与用双感叹号绕过类型系统，或吞掉异常后伪装成功产生的反例并排保存。

对 For the More Curious: Checked vs Unchecked Exceptions 的四级验收分别是：正文出现该坐标；解释它如何参与用可空类型、安全调用、let、Elvis、先决条件与异常把缺失值和失败路径显式化；在章专属实验中选择该节点并观察反馈；最后凭可空数据流图、三种处理策略、异常分类、失败测试和平台类型边界回答练习、复位并重放。

### For the More Curious: How Is Nullability Enforced?

For the More Curious: How Is Nullability Enforced?服务于用可空类型、安全调用、let、Elvis、先决条件与异常把缺失值和失败路径显式化。这里要把缺失值、异常来源和调用者责任分别放进类型或失败合同，禁止用强制解包、空捕获或虚假默认值擦除风险；用可空数据流图、三种处理策略、异常分类、失败测试和平台类型边界定位最早失效处。

在 6. Null Safety and Exceptions 的 定位边界 阶段，For the More Curious: How Is Nullability Enforced?先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入修复合同，使用同一份输入把正常路径与用双感叹号绕过类型系统，或吞掉异常后伪装成功产生的反例并排保存。

对 For the More Curious: How Is Nullability Enforced? 的四级验收分别是：正文出现该坐标；解释它如何参与用可空类型、安全调用、let、Elvis、先决条件与异常把缺失值和失败路径显式化；在章专属实验中选择该节点并观察反馈；最后凭可空数据流图、三种处理策略、异常分类、失败测试和平台类型边界回答练习、复位并重放。

## 最小可执行切片

```kotlin
fun displayName(raw: String?): String {
    require(raw == null || raw.length <= 40)
    return raw
        ?.trim()
        ?.takeIf { it.isNotEmpty() }
        ?: "Anonymous"
}
```

运行切片时固定 Kotlin/JDK 或 Android 工具链版本、输入数据和命令；保存标准输出、编译诊断及断言。迁移实验必须另建记录，不能静默改写2018年的历史坐标。


## 练习、答案与四级证据


## 术语复核与本章回顾


完成6. Null Safety and Exceptions意味着：能解释用可空类型、安全调用、let、Elvis、先决条件与异常把缺失值和失败路径显式化，能运行并改动章专属切片，能主动制造用双感叹号绕过类型系统，或吞掉异常后伪装成功，还能凭可空数据流图、三种处理策略、异常分类、失败测试和平台类型边界恢复并交接，而不是只识别术语。

## 阅读导航

[← 上一页：5. Anonymous Functions and the Function Type](/learn/kotlin-definitive-guide/03-functions-null-values/kdg1-05-anonymous-functions) · [下一页：7. Strings →](/learn/kotlin-definitive-guide/04-values-collections/kdg1-07-strings)

