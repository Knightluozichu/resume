---
title: "类与对象"
type: C
section: "Android开发"
order: 3
description: "实战Kotlin面向对象编程——类定义、继承、接口、数据类、密封类、单例与属性委托"
demo: true
math: false
sourceUrl: ""
draft: true
---

import { KdgClassesObjectsDiagram } from "@/components/mdx/kotlin-definitive-guide/diagrams/kdg-classes-objects";
import { Objectives, Callout, Glossary, GlossaryItem, Term, Exercises, Answer } from "@/components/mdx/mdx-components";

<Objectives>
- 掌握Kotlin类定义、主构造函数、属性声明与init初始化块
- 理解`open`/`override`继承机制，类默认final的设计理念
- 熟练使用`data class`数据类和`sealed class`密封类
- 掌握`object`单例、`companion object`伴生对象与`by`属性委托
- 能用面向对象建模真实业务场景（如UI状态、数据实体）
</Objectives>

## 类定义与主构造函数

Kotlin的类定义比Java简洁得多——主构造函数直接在类头声明：

```kotlin
// 主构造函数 + val/var 自动生成属性
class Person(val name: String, var age: Int)

// 等价于Java的：
// class Person {
//     private String name; private int age;
//     public Person(String name, int age) { ... }
//     public String getName() { ... }
//     public void setAge(int age) { ... }
// }

val p = Person("Alice", 30)
println(p.name)   // val 只读属性
p.age = 31        // var 可写属性
```

<Callout type="insight">
**核心理念**：Kotlin主构造函数参数加`val`/`var`直接成为属性，一行代码完成Java需要十几行的构造函数+字段+getter/setter。这是Kotlin消除样板代码的典型设计。
</Callout>

<KdgClassesObjectsDiagram />

## 继承与接口

Kotlin类默认是`final`的（不可继承），需要用`open`显式标记可继承：

```kotlin
// 父类需要open标记
open class Animal(val name: String) {
    open fun speak() = "$name makes a sound"
}

// 子类用 : 继承，调用父类构造用 ()
class Dog(name: String) : Animal(name) {
    override fun speak() = "$name barks"  // override隐含open
}

// 接口——可包含默认实现
interface Greeter {
    fun greet(): String  // 抽象方法
    fun sayHi() = "Hi"   // 默认实现
}

class FriendlyDog(name: String) : Animal(name), Greeter {
    override fun greet() = "${speak()} and says ${sayHi()}"
}
```

<Callout type="trap">
**误区**：像Java一样随意设计继承体系。Kotlin默认final是故意的——继承是「打开封装」的操作，应该有意识地进行。优先用组合而非继承，需要继承时才`open`。这符合Effective Java「为继承设计并文档化，否则禁止继承」的建议。
</Callout>

## 数据类与密封类

```kotlin
// data class —— 一行生成 equals/hashCode/copy/toString/componentN
data class User(val name: String, val id: Int)

val u1 = User("Alice", 1)
val u2 = u1.copy(name = "Bob")  // copy并修改部分字段
val (name, id) = u1             // 解构声明

// sealed class —— 有限子类，when分支可穷尽
sealed class UiState {
    data class Success(val data: List<String>) : UiState()
    data class Error(val message: String) : UiState()
    object Loading : UiState()
}

fun render(state: UiState) = when (state) {
    is UiState.Success -> showList(state.data)  // 智能转换
    is UiState.Error -> showError(state.message)
    UiState.Loading -> showSpinner()
    // 无需else——编译器知道所有分支已覆盖
}
```

## 单例、伴生对象与委托

```kotlin
// object —— 线程安全的单例
object Database {
    fun connect() = "connected"
}
Database.connect()  // 直接通过类名调用

// companion object —— 类的静态成员
class Foo {
    companion object {
        fun create() = Foo()
        const val TAG = "Foo"
    }
}
Foo.create()  // 像静态方法一样调用

// 属性委托 by
import kotlin.properties.Delegates

class Config {
    // 懒加载——首次访问时初始化
    val cache: Map<String, String> by lazy { loadFromDisk() }

    // 可观察——值变化时回调
    var count: Int by Delegates.observable(0) { _, old, new ->
        println("count: $old -> $new")
    }

    // Map委托——用Map存储属性
    val props by mapOf("name" to "value")
}
```

## 实战：用OOP建模UI状态机

```kotlin
// 用sealed class + data class 建模UI状态
sealed class LoginState {
    object Idle : LoginState()
    object Loading : LoginState()
    data class Success(val token: String) : LoginState()
    data class Failure(val error: String) : LoginState()
}

class LoginViewModel(private val repo: UserRepository) {
    fun login(email: String, pass: String): LoginState {
        if (email.isBlank()) return LoginState.Failure("邮箱不能为空")
        return try {
            val token = repo.auth(email, pass)
            LoginState.Success(token)
        } catch (e: Exception) {
            LoginState.Failure(e.message ?: "未知错误")
        }
    }
}

// View层渲染——when编译器保证覆盖所有状态
fun render(state: LoginState) = when (state) {
    LoginState.Idle -> showForm()
    LoginState.Loading -> showProgress()
    is LoginState.Success -> navigate(state.token)
    is LoginState.Failure -> showError(state.error)
}
```

<Exercises>
**问题 1**：Kotlin的`data class`自动生成了哪些方法？与Java的POJO有什么区别？

<Answer>
`data class`自动生成：①equals()——基于主构造函数属性比较相等性。②hashCode()——基于主构造函数属性计算哈希。③toString()——格式为`ClassName(prop1=value1, prop2=value2)`。④copy()——创建副本，可修改部分属性：`u.copy(name="Bob")`。⑤componentN()——解构声明支持：`val (name, id) = user`。与Java POJO的区别：①Java POJO需要手写或用Lombok生成这些方法，Kotlin一行`data class User(val name: String, val id: Int)`搞定。②data class的主构造函数必须至少有一个参数，且参数必须是val/var属性。③data class默认是final的（不可继承）。④data class不能是abstract/open/sealed/inner。⑤data class的equals/hashCode只基于主构造函数属性，不包括body中声明的属性。data class是Kotlin消除样板代码最典型的特性，特别适合建模数据实体、DTO、领域值对象。
</Answer>

**问题 2**：Kotlin的`sealed class`（密封类）有什么作用？它如何与`when`配合？

<Answer>
密封类（sealed class）限定子类必须在同一文件（Kotlin 1.5+同一模块）内声明，编译器因此知道所有可能的子类。作用：①有限类型建模——当值的取值是有限集合时（如UI状态Loading/Success/Error、操作结果Ok/Fail、扑克牌花色），用sealed class表达。②when穷尽性检查——when表达式处理sealed class时，编译器验证所有子类分支都已覆盖，无需else分支。新增子类时，编译器在所有when表达式处报错提醒你处理新分支。③类型安全的状态机——sealed class + data class子类 + when智能转换构成类型安全的状态处理模式。示例：`sealed class Result`，子类`data class Success<T>(val data: T): Result()`、`data class Error(val msg: String): Result()`、`object Loading: Result()`。when处理时`is Success`分支自动智能转换state为Success类型，可直接访问state.data。对比Java的enum：enum实例是单例不能携带不同类型的数据，sealed class子类可以是data class携带各自的数据。对比抽象类：抽象类不限制子类位置，编译器无法保证穷尽。
</Answer>

**问题 3**：Kotlin为什么默认类是`final`的？`object`和`companion object`有什么区别？

<Answer>
Kotlin类默认final的设计理念：继承打破了封装——子类可以覆盖父类行为，父类作者无法预知子类如何修改。Effective Java建议「为继承设计并文档化，否则禁止继承」。Kotlin将默认值设为安全的选择（不可继承），需要继承时显式open，这是「安全优先」的设计哲学。`object`与`companion object`的区别：①object——声明一个单例对象，线程安全，直接通过对象名访问成员：`object Database { fun connect() = ... }`，调用`Database.connect()`。用于全局单例、工具类替代。②companion object——类内部的伴生对象，为类提供静态成员：`class Foo { companion object { fun create() = ... } }`，调用`Foo.create()`。companion object是真实对象，可实现接口、有名字、可作为参数传递。companion object的成员通过类名访问，模拟Java的static，但更强大——它是真实对象。object是顶层单例，companion object是类关联的单例。
</Answer>

**问题 4**：Kotlin的属性委托（`by`）能做什么？举例说明其应用场景。

<Answer>
属性委托（by）将属性的get/set逻辑委托给第三方对象，实现属性逻辑的复用。语法：`val/var <属性名>: <类型> by <委托对象>`。委托对象需实现`getValue`/`setValue`操作符函数。标准库提供的委托：①lazy——懒加载，首次访问时执行初始化Lambda并缓存结果：`val cache: Map<String,String> by lazy { loadFromDisk() }`。线程安全（默认LazyThreadSafetyMode.SYNCHRONIZED）。②Delegates.observable——可观察属性，值变化时回调：`var count by Delegates.observable(0) { _, old, new -> log("$old -> $new") }`。③Delegates.vetoable——可否决属性修改，变化前检查。④Map委托——用Map存储属性：`val name by map`（map的key需匹配属性名）。自定义委托：实现`ReadOnlyProperty`/`ReadWriteProperty`接口，或直接提供`getValue`/`setValue`操作符。应用场景：懒加载昂贵资源、属性变化监听/数据绑定、从Map/SharedPreferences加载配置、权限校验属性、缓存属性。属性委托将横切关注点（懒加载、监听、校验）从业务逻辑中分离。
</Answer>
</Exercises>

<Glossary>
<GlossaryItem term="主构造函数">类头中声明的构造函数，参数加val/var自动成为属性。一行代码完成Java的字段+构造+getter/setter。</GlossaryItem>
<GlossaryItem term="open/override">Kotlin类默认final不可继承，需open标记才可继承。override隐含open，可用final override禁止再覆盖。</GlossaryItem>
<GlossaryItem term="data class">数据类，一行声明自动生成equals/hashCode/copy/toString/componentN。适合建模数据实体。</GlossaryItem>
<GlossaryItem term="sealed class">密封类，限定子类在同一文件/模块。编译器知道所有子类，when可穷尽检查，新增子类时编译器报错提醒。</GlossaryItem>
<GlossaryItem term="object单例">用object声明的线程安全单例对象，直接通过对象名访问成员，替代Java的单例模式和静态工具类。</GlossaryItem>
<GlossaryItem term="属性委托by">将属性get/set委托给第三方对象，实现懒加载lazy、可观察observable、Map映射等复用逻辑。</GlossaryItem>
</Glossary>
