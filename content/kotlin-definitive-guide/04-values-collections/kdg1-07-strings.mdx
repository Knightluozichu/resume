---
title: 7. Strings
description: 7. Strings：掌握子串、split、不可变转换、比较、Unicode与字符遍历的边界，通过全节点状态链、历史迁移对照和故障恢复完成验收。
type: C
section: Kotlin权威指南 · 值与集合
draft: false
math: false
sourceUrl: 'https://www.oreilly.com/library/view/kotlin-programming-the/9780135165188/'
demo: true
order: 1
qualityVersion: 2
practiceMode: code
sourceMode: independent-rewrite
---


{/* KOTLIN_DEFINITIVE_GUIDE_QUALITY_V2 */}

## 从本章问题开始

7. Strings要解决的不是语法记忆，而是掌握子串、split、不可变转换、比较、Unicode与字符遍历的边界。先写出输入和期望结果，再运行最小切片；如果无法解释失败落在编译期、边界层还是运行期，程序即使输出一次正确答案也未通过。

本页不把 val 等同深度不可变，也不把简洁语法等同动态类型。Kotlin 的类型、可空性、求值规则和对象模型共同压缩非法状态；本章不变量是掌握子串、split、不可变转换、比较、Unicode与字符遍历的边界的输入、类型边界、求值结果和失败路径都能由独立读者重放。

## 来源范围与年代边界

O’Reilly 的[第1版书目与完整目录](https://www.oreilly.com/library/view/kotlin-programming-the/9780135165188/)只用于核定本页范围，[中文版出版记录](https://e.dangdang.com/touch/products/1901154363.html)用于交叉核对译本信息；未取得的原书正文不作忠实性宣称。7. Strings涉及的语言机制由[JetBrains/Android 一手文档](https://kotlinlang.org/docs/basic-syntax.html)独立核验，以下中文讲解、代码组织、图示、实验和练习均为独立教学重写。

第1版的教学坐标是2018年前后的 Kotlin 1.2、JVM、IntelliJ 与当时 Android/早期协程接口。[Kotlin 1.2 发布说明](https://kotlinlang.org/docs/whatsnew12.html)用于锁定历史能力；当前文档只提供迁移对照，Flow、Compose、MockK、现代 Gradle Kotlin DSL 和第二版新增内容不倒灌为原书内容。

## 本章机制与六个检查点

、、、、、。这些检查点共同回答掌握子串、split、不可变转换、比较、Unicode与字符遍历的边界，最终必须落到输入样本、Unicode码点说明、转换管线、比较断言和DragonSpeak挑战，不能只停在API名称或目录词。

## 正式目录逐项深读

### 7. Strings

7. Strings服务于掌握子串、split、不可变转换、比较、Unicode与字符遍历的边界。固定集合或数值样本，记录索引、类型、转换和求值次数，仅改变一个边界条件；由输入样本、Unicode码点说明、转换管线、比较断言和DragonSpeak挑战判断只读、不可变、精度或惰性结论是否成立。

在 7. Strings 的 声明合同 阶段，7. Strings先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入建立输入，使用同一份输入把正常路径与按UTF-16索引误判用户可见字符，或忽略区域与大小写规则产生的反例并排保存。

对 7. Strings 的四级验收分别是：正文出现该坐标；解释它如何参与掌握子串、split、不可变转换、比较、Unicode与字符遍历的边界；在章专属实验中选择该节点并观察反馈；最后凭输入样本、Unicode码点说明、转换管线、比较断言和DragonSpeak挑战回答练习、复位并重放。

### Extracting Substrings

Extracting Substrings服务于掌握子串、split、不可变转换、比较、Unicode与字符遍历的边界。固定集合或数值样本，记录索引、类型、转换和求值次数，仅改变一个边界条件；由输入样本、Unicode码点说明、转换管线、比较断言和DragonSpeak挑战判断只读、不可变、精度或惰性结论是否成立。

在 7. Strings 的 建立输入 阶段，Extracting Substrings先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入执行转换，使用同一份输入把正常路径与按UTF-16索引误判用户可见字符，或忽略区域与大小写规则产生的反例并排保存。

对 Extracting Substrings 的四级验收分别是：正文出现该坐标；解释它如何参与掌握子串、split、不可变转换、比较、Unicode与字符遍历的边界；在章专属实验中选择该节点并观察反馈；最后凭输入样本、Unicode码点说明、转换管线、比较断言和DragonSpeak挑战回答练习、复位并重放。

### substring

substring服务于掌握子串、split、不可变转换、比较、Unicode与字符遍历的边界。固定集合或数值样本，记录索引、类型、转换和求值次数，仅改变一个边界条件；由输入样本、Unicode码点说明、转换管线、比较断言和DragonSpeak挑战判断只读、不可变、精度或惰性结论是否成立。

在 7. Strings 的 执行转换 阶段，substring先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入观察产物，使用同一份输入把正常路径与按UTF-16索引误判用户可见字符，或忽略区域与大小写规则产生的反例并排保存。

对 substring 的四级验收分别是：正文出现该坐标；解释它如何参与掌握子串、split、不可变转换、比较、Unicode与字符遍历的边界；在章专属实验中选择该节点并观察反馈；最后凭输入样本、Unicode码点说明、转换管线、比较断言和DragonSpeak挑战回答练习、复位并重放。

### split

split服务于掌握子串、split、不可变转换、比较、Unicode与字符遍历的边界。把这个目录坐标翻译成一个可执行或可判定合同，明确输入、状态变化、可见结果和反例，并以输入样本、Unicode码点说明、转换管线、比较断言和DragonSpeak挑战完成独立复核。

在 7. Strings 的 观察产物 阶段，split先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入断言回归，使用同一份输入把正常路径与按UTF-16索引误判用户可见字符，或忽略区域与大小写规则产生的反例并排保存。

对 split 的四级验收分别是：正文出现该坐标；解释它如何参与掌握子串、split、不可变转换、比较、Unicode与字符遍历的边界；在章专属实验中选择该节点并观察反馈；最后凭输入样本、Unicode码点说明、转换管线、比较断言和DragonSpeak挑战回答练习、复位并重放。

### String Manipulation

String Manipulation服务于掌握子串、split、不可变转换、比较、Unicode与字符遍历的边界。固定集合或数值样本，记录索引、类型、转换和求值次数，仅改变一个边界条件；由输入样本、Unicode码点说明、转换管线、比较断言和DragonSpeak挑战判断只读、不可变、精度或惰性结论是否成立。

在 7. Strings 的 断言回归 阶段，String Manipulation先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入声明合同，使用同一份输入把正常路径与按UTF-16索引误判用户可见字符，或忽略区域与大小写规则产生的反例并排保存。

对 String Manipulation 的四级验收分别是：正文出现该坐标；解释它如何参与掌握子串、split、不可变转换、比较、Unicode与字符遍历的边界；在章专属实验中选择该节点并观察反馈；最后凭输入样本、Unicode码点说明、转换管线、比较断言和DragonSpeak挑战回答练习、复位并重放。

### Strings are immutable

Strings are immutable服务于掌握子串、split、不可变转换、比较、Unicode与字符遍历的边界。固定集合或数值样本，记录索引、类型、转换和求值次数，仅改变一个边界条件；由输入样本、Unicode码点说明、转换管线、比较断言和DragonSpeak挑战判断只读、不可变、精度或惰性结论是否成立。

在 7. Strings 的 声明合同 阶段，Strings are immutable先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入建立输入，使用同一份输入把正常路径与按UTF-16索引误判用户可见字符，或忽略区域与大小写规则产生的反例并排保存。

对 Strings are immutable 的四级验收分别是：正文出现该坐标；解释它如何参与掌握子串、split、不可变转换、比较、Unicode与字符遍历的边界；在章专属实验中选择该节点并观察反馈；最后凭输入样本、Unicode码点说明、转换管线、比较断言和DragonSpeak挑战回答练习、复位并重放。

### String Comparison

String Comparison服务于掌握子串、split、不可变转换、比较、Unicode与字符遍历的边界。固定集合或数值样本，记录索引、类型、转换和求值次数，仅改变一个边界条件；由输入样本、Unicode码点说明、转换管线、比较断言和DragonSpeak挑战判断只读、不可变、精度或惰性结论是否成立。

在 7. Strings 的 建立输入 阶段，String Comparison先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入执行转换，使用同一份输入把正常路径与按UTF-16索引误判用户可见字符，或忽略区域与大小写规则产生的反例并排保存。

对 String Comparison 的四级验收分别是：正文出现该坐标；解释它如何参与掌握子串、split、不可变转换、比较、Unicode与字符遍历的边界；在章专属实验中选择该节点并观察反馈；最后凭输入样本、Unicode码点说明、转换管线、比较断言和DragonSpeak挑战回答练习、复位并重放。

### For the More Curious: Unicode

For the More Curious: Unicode服务于掌握子串、split、不可变转换、比较、Unicode与字符遍历的边界。固定集合或数值样本，记录索引、类型、转换和求值次数，仅改变一个边界条件；由输入样本、Unicode码点说明、转换管线、比较断言和DragonSpeak挑战判断只读、不可变、精度或惰性结论是否成立。

在 7. Strings 的 执行转换 阶段，For the More Curious: Unicode先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入观察产物，使用同一份输入把正常路径与按UTF-16索引误判用户可见字符，或忽略区域与大小写规则产生的反例并排保存。

对 For the More Curious: Unicode 的四级验收分别是：正文出现该坐标；解释它如何参与掌握子串、split、不可变转换、比较、Unicode与字符遍历的边界；在章专属实验中选择该节点并观察反馈；最后凭输入样本、Unicode码点说明、转换管线、比较断言和DragonSpeak挑战回答练习、复位并重放。

### For the More Curious: Traversing a String’s Characters

For the More Curious: Traversing a String’s Characters服务于掌握子串、split、不可变转换、比较、Unicode与字符遍历的边界。固定集合或数值样本，记录索引、类型、转换和求值次数，仅改变一个边界条件；由输入样本、Unicode码点说明、转换管线、比较断言和DragonSpeak挑战判断只读、不可变、精度或惰性结论是否成立。

在 7. Strings 的 观察产物 阶段，For the More Curious: Traversing a String’s Characters先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入断言回归，使用同一份输入把正常路径与按UTF-16索引误判用户可见字符，或忽略区域与大小写规则产生的反例并排保存。

对 For the More Curious: Traversing a String’s Characters 的四级验收分别是：正文出现该坐标；解释它如何参与掌握子串、split、不可变转换、比较、Unicode与字符遍历的边界；在章专属实验中选择该节点并观察反馈；最后凭输入样本、Unicode码点说明、转换管线、比较断言和DragonSpeak挑战回答练习、复位并重放。

### Challenge: Improving DragonSpeak

Challenge: Improving DragonSpeak服务于掌握子串、split、不可变转换、比较、Unicode与字符遍历的边界。先把题目改写成输入、输出、约束和失败样例，再运行正常、边界与非法三组断言；挑战完成的证据是输入样本、Unicode码点说明、转换管线、比较断言和DragonSpeak挑战，不是一次示例输出。

在 7. Strings 的 断言回归 阶段，Challenge: Improving DragonSpeak先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入声明合同，使用同一份输入把正常路径与按UTF-16索引误判用户可见字符，或忽略区域与大小写规则产生的反例并排保存。

对 Challenge: Improving DragonSpeak 的四级验收分别是：正文出现该坐标；解释它如何参与掌握子串、split、不可变转换、比较、Unicode与字符遍历的边界；在章专属实验中选择该节点并观察反馈；最后凭输入样本、Unicode码点说明、转换管线、比较断言和DragonSpeak挑战回答练习、复位并重放。

## 最小可执行切片

```kotlin
fun toDragonSpeak(phrase: String): String = phrase
    .split(" ")
    .joinToString(" ") { word ->
        word.replace("th", "ss", ignoreCase = true)
    }

check(toDragonSpeak("the hearth").isNotBlank())
```

运行切片时固定 Kotlin/JDK 或 Android 工具链版本、输入数据和命令；保存标准输出、编译诊断及断言。迁移实验必须另建记录，不能静默改写2018年的历史坐标。


## 练习、答案与四级证据


## 术语复核与本章回顾


完成7. Strings意味着：能解释掌握子串、split、不可变转换、比较、Unicode与字符遍历的边界，能运行并改动章专属切片，能主动制造按UTF-16索引误判用户可见字符，或忽略区域与大小写规则，还能凭输入样本、Unicode码点说明、转换管线、比较断言和DragonSpeak挑战恢复并交接，而不是只识别术语。

## 阅读导航

[← 上一页：6. Null Safety and Exceptions](/learn/kotlin-definitive-guide/03-functions-null-values/kdg1-06-null-safety-exceptions) · [下一页：8. Numbers →](/learn/kotlin-definitive-guide/04-values-collections/kdg1-08-numbers)

