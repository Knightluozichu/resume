---
title: 10. Lists and Sets
description: >-
  10. Lists and
  Sets：区分只读接口与不可变对象，掌握索引、遍历、文件读取、解构、集合转换、while与break，通过全节点状态链、历史迁移对照和故障恢复完成验收。
type: C
section: Kotlin权威指南 · 值与集合
draft: true
math: false
sourceUrl: 'https://www.oreilly.com/library/view/kotlin-programming-the/9780135165188/'
demo: true
order: 4
qualityVersion: 2
practiceMode: simulation
sourceMode: independent-rewrite
---
import { KdgModelLab, KdgFailureLab, KdgEvidenceLab } from "@/components/mdx/kotlin-definitive-guide/diagrams/kdg1-10-lists-sets";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能说明区分只读接口与不可变对象，掌握索引、遍历、文件读取、解构、集合转换、while与break，并画出输入、类型或对象边界与可见结果。
- 能运行本页保留的 Kotlin 切片，解释它在 Kotlin 1.2/JVM 语境中的编译或执行路径。
- 能操作覆盖、合同和恢复三层实验，让全部 19 个正式节点拥有可见教学因果。
- 能触发把只读引用当作深度不可变，或在遍历期间修改共享集合，凭集合所有权图、越界实验、去重结果、文件样本和菜单格式断言完成诊断、修复、复位与同输入重放。

</Objectives>

{/* KOTLIN_DEFINITIVE_GUIDE_QUALITY_V2 */}

## 从本章问题开始

10. Lists and Sets要解决的不是语法记忆，而是区分只读接口与不可变对象，掌握索引、遍历、文件读取、解构、集合转换、while与break。先写出输入和期望结果，再运行最小切片；如果无法解释失败落在编译期、边界层还是运行期，程序即使输出一次正确答案也未通过。

本页不把 val 等同深度不可变，也不把简洁语法等同动态类型。Kotlin 的类型、可空性、求值规则和对象模型共同压缩非法状态；本章不变量是区分只读接口与不可变对象，掌握索引、遍历、文件读取、解构、集合转换、while与break的输入、类型边界、求值结果和失败路径都能由独立读者重放。

## 来源范围与年代边界

O’Reilly 的[第1版书目与完整目录](https://www.oreilly.com/library/view/kotlin-programming-the/9780135165188/)只用于核定本页范围，[中文版出版记录](https://e.dangdang.com/touch/products/1901154363.html)用于交叉核对译本信息；未取得的原书正文不作忠实性宣称。10. Lists and Sets涉及的语言机制由[JetBrains/Android 一手文档](https://kotlinlang.org/docs/basic-syntax.html)独立核验，以下中文讲解、代码组织、图示、实验和练习均为独立教学重写。

第1版的教学坐标是2018年前后的 Kotlin 1.2、JVM、IntelliJ 与当时 Android/早期协程接口。[Kotlin 1.2 发布说明](https://kotlinlang.org/docs/whatsnew12.html)用于锁定历史能力；当前文档只提供迁移对照，Flow、Compose、MockK、现代 Gradle Kotlin DSL 和第二版新增内容不倒灌为原书内容。

## 本章机制与六个检查点

<Term def="初始状态在本页限定10. Lists and Sets允许进入系统的值与前置条件。">初始状态</Term>、<Term def="单一变量在本页标记区分只读接口与不可变对象，掌握索引、遍历、文件读取、解构、集合转换、while与break中状态由谁创建、修改和释放。">单一变量</Term>、<Term def="状态迁移在本页说明Kotlin 1.2实现与当前迁移之间必须保持的语义。">状态迁移</Term>、<Term def="可见反馈在本页保存能推翻把只读引用当作深度不可变，或在遍历期间修改共享集合的原始观察。">可见反馈</Term>、<Term def="故障注入在本页把错误停在复位重放而非继续扩散。">故障注入</Term>、<Term def="复位重放在本页让另一位读者凭集合所有权图、越界实验、去重结果、文件样本和菜单格式断言得到相同结论。">复位重放</Term>。这些检查点共同回答区分只读接口与不可变对象，掌握索引、遍历、文件读取、解构、集合转换、while与break，最终必须落到集合所有权图、越界实验、去重结果、文件样本和菜单格式断言，不能只停在API名称或目录词。

## 先预测，再操作三层实验

先预测：选择一个正式节点后哪一阶段会先改变；切换历史基线与当前迁移时哪项语义必须不变；触发把只读引用当作深度不可变，或在遍历期间修改共享集合后，应由哪份证据定位并恢复。

<Stepper>
  <Step title="1. 全节点覆盖与状态链">
    选择任一正式节点和机制阶段，检查输入、状态拥有者、可见结果与专属证据；节点列表不截断。

    <KdgModelLab />

  </Step>
  <Step title="2. 历史合同与当前迁移">
    在正常、边界和故障输入间切换，只改变保护合同或技术坐标，观察结果为何变化。

    <KdgFailureLab />

  </Step>
  <Step title="3. 故障、恢复与重放">
    比较正常轨迹、首错轨迹和恢复轨迹；最后点击重置，确认选择项与可见输出都回到初始值。

    <KdgEvidenceLab />

  </Step>
</Stepper>

## 正式目录逐项深读

### 10. Lists and Sets

10. Lists and Sets服务于区分只读接口与不可变对象，掌握索引、遍历、文件读取、解构、集合转换、while与break。固定集合或数值样本，记录索引、类型、转换和求值次数，仅改变一个边界条件；由集合所有权图、越界实验、去重结果、文件样本和菜单格式断言判断只读、不可变、精度或惰性结论是否成立。

在 10. Lists and Sets 的 固定初态 阶段，10. Lists and Sets先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入施加动作，使用同一份输入把正常路径与把只读引用当作深度不可变，或在遍历期间修改共享集合产生的反例并排保存。

对 10. Lists and Sets 的四级验收分别是：正文出现该坐标；解释它如何参与区分只读接口与不可变对象，掌握索引、遍历、文件读取、解构、集合转换、while与break；在章专属实验中选择该节点并观察反馈；最后凭集合所有权图、越界实验、去重结果、文件样本和菜单格式断言回答练习、复位并重放。

### Lists

Lists服务于区分只读接口与不可变对象，掌握索引、遍历、文件读取、解构、集合转换、while与break。固定集合或数值样本，记录索引、类型、转换和求值次数，仅改变一个边界条件；由集合所有权图、越界实验、去重结果、文件样本和菜单格式断言判断只读、不可变、精度或惰性结论是否成立。

在 10. Lists and Sets 的 施加动作 阶段，Lists先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入推进状态，使用同一份输入把正常路径与把只读引用当作深度不可变，或在遍历期间修改共享集合产生的反例并排保存。

对 Lists 的四级验收分别是：正文出现该坐标；解释它如何参与区分只读接口与不可变对象，掌握索引、遍历、文件读取、解构、集合转换、while与break；在章专属实验中选择该节点并观察反馈；最后凭集合所有权图、越界实验、去重结果、文件样本和菜单格式断言回答练习、复位并重放。

### Accessing a list’s elements

Accessing a list’s elements服务于区分只读接口与不可变对象，掌握索引、遍历、文件读取、解构、集合转换、while与break。固定集合或数值样本，记录索引、类型、转换和求值次数，仅改变一个边界条件；由集合所有权图、越界实验、去重结果、文件样本和菜单格式断言判断只读、不可变、精度或惰性结论是否成立。

在 10. Lists and Sets 的 推进状态 阶段，Accessing a list’s elements先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入观察差异，使用同一份输入把正常路径与把只读引用当作深度不可变，或在遍历期间修改共享集合产生的反例并排保存。

对 Accessing a list’s elements 的四级验收分别是：正文出现该坐标；解释它如何参与区分只读接口与不可变对象，掌握索引、遍历、文件读取、解构、集合转换、while与break；在章专属实验中选择该节点并观察反馈；最后凭集合所有权图、越界实验、去重结果、文件样本和菜单格式断言回答练习、复位并重放。

### Index boundaries and safe index access

Index boundaries and safe index access服务于区分只读接口与不可变对象，掌握索引、遍历、文件读取、解构、集合转换、while与break。把这个目录坐标翻译成一个可执行或可判定合同，明确输入、状态变化、可见结果和反例，并以集合所有权图、越界实验、去重结果、文件样本和菜单格式断言完成独立复核。

在 10. Lists and Sets 的 观察差异 阶段，Index boundaries and safe index access先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入复位重放，使用同一份输入把正常路径与把只读引用当作深度不可变，或在遍历期间修改共享集合产生的反例并排保存。

对 Index boundaries and safe index access 的四级验收分别是：正文出现该坐标；解释它如何参与区分只读接口与不可变对象，掌握索引、遍历、文件读取、解构、集合转换、while与break；在章专属实验中选择该节点并观察反馈；最后凭集合所有权图、越界实验、去重结果、文件样本和菜单格式断言回答练习、复位并重放。

### Checking the contents of a list

Checking the contents of a list服务于区分只读接口与不可变对象，掌握索引、遍历、文件读取、解构、集合转换、while与break。固定集合或数值样本，记录索引、类型、转换和求值次数，仅改变一个边界条件；由集合所有权图、越界实验、去重结果、文件样本和菜单格式断言判断只读、不可变、精度或惰性结论是否成立。

在 10. Lists and Sets 的 复位重放 阶段，Checking the contents of a list先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入固定初态，使用同一份输入把正常路径与把只读引用当作深度不可变，或在遍历期间修改共享集合产生的反例并排保存。

对 Checking the contents of a list 的四级验收分别是：正文出现该坐标；解释它如何参与区分只读接口与不可变对象，掌握索引、遍历、文件读取、解构、集合转换、while与break；在章专属实验中选择该节点并观察反馈；最后凭集合所有权图、越界实验、去重结果、文件样本和菜单格式断言回答练习、复位并重放。

### Changing a list’s contents

Changing a list’s contents服务于区分只读接口与不可变对象，掌握索引、遍历、文件读取、解构、集合转换、while与break。固定集合或数值样本，记录索引、类型、转换和求值次数，仅改变一个边界条件；由集合所有权图、越界实验、去重结果、文件样本和菜单格式断言判断只读、不可变、精度或惰性结论是否成立。

在 10. Lists and Sets 的 固定初态 阶段，Changing a list’s contents先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入施加动作，使用同一份输入把正常路径与把只读引用当作深度不可变，或在遍历期间修改共享集合产生的反例并排保存。

对 Changing a list’s contents 的四级验收分别是：正文出现该坐标；解释它如何参与区分只读接口与不可变对象，掌握索引、遍历、文件读取、解构、集合转换、while与break；在章专属实验中选择该节点并观察反馈；最后凭集合所有权图、越界实验、去重结果、文件样本和菜单格式断言回答练习、复位并重放。

### Iteration

Iteration服务于区分只读接口与不可变对象，掌握索引、遍历、文件读取、解构、集合转换、while与break。把这个目录坐标翻译成一个可执行或可判定合同，明确输入、状态变化、可见结果和反例，并以集合所有权图、越界实验、去重结果、文件样本和菜单格式断言完成独立复核。

在 10. Lists and Sets 的 施加动作 阶段，Iteration先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入推进状态，使用同一份输入把正常路径与把只读引用当作深度不可变，或在遍历期间修改共享集合产生的反例并排保存。

对 Iteration 的四级验收分别是：正文出现该坐标；解释它如何参与区分只读接口与不可变对象，掌握索引、遍历、文件读取、解构、集合转换、while与break；在章专属实验中选择该节点并观察反馈；最后凭集合所有权图、越界实验、去重结果、文件样本和菜单格式断言回答练习、复位并重放。

### Reading a File into a List

Reading a File into a List服务于区分只读接口与不可变对象，掌握索引、遍历、文件读取、解构、集合转换、while与break。固定集合或数值样本，记录索引、类型、转换和求值次数，仅改变一个边界条件；由集合所有权图、越界实验、去重结果、文件样本和菜单格式断言判断只读、不可变、精度或惰性结论是否成立。

在 10. Lists and Sets 的 推进状态 阶段，Reading a File into a List先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入观察差异，使用同一份输入把正常路径与把只读引用当作深度不可变，或在遍历期间修改共享集合产生的反例并排保存。

对 Reading a File into a List 的四级验收分别是：正文出现该坐标；解释它如何参与区分只读接口与不可变对象，掌握索引、遍历、文件读取、解构、集合转换、while与break；在章专属实验中选择该节点并观察反馈；最后凭集合所有权图、越界实验、去重结果、文件样本和菜单格式断言回答练习、复位并重放。

### Destructuring

Destructuring服务于区分只读接口与不可变对象，掌握索引、遍历、文件读取、解构、集合转换、while与break。把这个目录坐标翻译成一个可执行或可判定合同，明确输入、状态变化、可见结果和反例，并以集合所有权图、越界实验、去重结果、文件样本和菜单格式断言完成独立复核。

在 10. Lists and Sets 的 观察差异 阶段，Destructuring先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入复位重放，使用同一份输入把正常路径与把只读引用当作深度不可变，或在遍历期间修改共享集合产生的反例并排保存。

对 Destructuring 的四级验收分别是：正文出现该坐标；解释它如何参与区分只读接口与不可变对象，掌握索引、遍历、文件读取、解构、集合转换、while与break；在章专属实验中选择该节点并观察反馈；最后凭集合所有权图、越界实验、去重结果、文件样本和菜单格式断言回答练习、复位并重放。

### Sets

Sets服务于区分只读接口与不可变对象，掌握索引、遍历、文件读取、解构、集合转换、while与break。固定集合或数值样本，记录索引、类型、转换和求值次数，仅改变一个边界条件；由集合所有权图、越界实验、去重结果、文件样本和菜单格式断言判断只读、不可变、精度或惰性结论是否成立。

在 10. Lists and Sets 的 复位重放 阶段，Sets先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入固定初态，使用同一份输入把正常路径与把只读引用当作深度不可变，或在遍历期间修改共享集合产生的反例并排保存。

对 Sets 的四级验收分别是：正文出现该坐标；解释它如何参与区分只读接口与不可变对象，掌握索引、遍历、文件读取、解构、集合转换、while与break；在章专属实验中选择该节点并观察反馈；最后凭集合所有权图、越界实验、去重结果、文件样本和菜单格式断言回答练习、复位并重放。

### Creating a set

Creating a set服务于区分只读接口与不可变对象，掌握索引、遍历、文件读取、解构、集合转换、while与break。固定集合或数值样本，记录索引、类型、转换和求值次数，仅改变一个边界条件；由集合所有权图、越界实验、去重结果、文件样本和菜单格式断言判断只读、不可变、精度或惰性结论是否成立。

在 10. Lists and Sets 的 固定初态 阶段，Creating a set先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入施加动作，使用同一份输入把正常路径与把只读引用当作深度不可变，或在遍历期间修改共享集合产生的反例并排保存。

对 Creating a set 的四级验收分别是：正文出现该坐标；解释它如何参与区分只读接口与不可变对象，掌握索引、遍历、文件读取、解构、集合转换、while与break；在章专属实验中选择该节点并观察反馈；最后凭集合所有权图、越界实验、去重结果、文件样本和菜单格式断言回答练习、复位并重放。

### Adding elements to a set

Adding elements to a set服务于区分只读接口与不可变对象，掌握索引、遍历、文件读取、解构、集合转换、while与break。固定集合或数值样本，记录索引、类型、转换和求值次数，仅改变一个边界条件；由集合所有权图、越界实验、去重结果、文件样本和菜单格式断言判断只读、不可变、精度或惰性结论是否成立。

在 10. Lists and Sets 的 施加动作 阶段，Adding elements to a set先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入推进状态，使用同一份输入把正常路径与把只读引用当作深度不可变，或在遍历期间修改共享集合产生的反例并排保存。

对 Adding elements to a set 的四级验收分别是：正文出现该坐标；解释它如何参与区分只读接口与不可变对象，掌握索引、遍历、文件读取、解构、集合转换、while与break；在章专属实验中选择该节点并观察反馈；最后凭集合所有权图、越界实验、去重结果、文件样本和菜单格式断言回答练习、复位并重放。

### while Loops

while Loops服务于区分只读接口与不可变对象，掌握索引、遍历、文件读取、解构、集合转换、while与break。把这个目录坐标翻译成一个可执行或可判定合同，明确输入、状态变化、可见结果和反例，并以集合所有权图、越界实验、去重结果、文件样本和菜单格式断言完成独立复核。

在 10. Lists and Sets 的 推进状态 阶段，while Loops先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入观察差异，使用同一份输入把正常路径与把只读引用当作深度不可变，或在遍历期间修改共享集合产生的反例并排保存。

对 while Loops 的四级验收分别是：正文出现该坐标；解释它如何参与区分只读接口与不可变对象，掌握索引、遍历、文件读取、解构、集合转换、while与break；在章专属实验中选择该节点并观察反馈；最后凭集合所有权图、越界实验、去重结果、文件样本和菜单格式断言回答练习、复位并重放。

### The break Expression

The break Expression服务于区分只读接口与不可变对象，掌握索引、遍历、文件读取、解构、集合转换、while与break。把这个目录坐标翻译成一个可执行或可判定合同，明确输入、状态变化、可见结果和反例，并以集合所有权图、越界实验、去重结果、文件样本和菜单格式断言完成独立复核。

在 10. Lists and Sets 的 观察差异 阶段，The break Expression先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入复位重放，使用同一份输入把正常路径与把只读引用当作深度不可变，或在遍历期间修改共享集合产生的反例并排保存。

对 The break Expression 的四级验收分别是：正文出现该坐标；解释它如何参与区分只读接口与不可变对象，掌握索引、遍历、文件读取、解构、集合转换、while与break；在章专属实验中选择该节点并观察反馈；最后凭集合所有权图、越界实验、去重结果、文件样本和菜单格式断言回答练习、复位并重放。

### Collection Conversion

Collection Conversion服务于区分只读接口与不可变对象，掌握索引、遍历、文件读取、解构、集合转换、while与break。把这个目录坐标翻译成一个可执行或可判定合同，明确输入、状态变化、可见结果和反例，并以集合所有权图、越界实验、去重结果、文件样本和菜单格式断言完成独立复核。

在 10. Lists and Sets 的 复位重放 阶段，Collection Conversion先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入固定初态，使用同一份输入把正常路径与把只读引用当作深度不可变，或在遍历期间修改共享集合产生的反例并排保存。

对 Collection Conversion 的四级验收分别是：正文出现该坐标；解释它如何参与区分只读接口与不可变对象，掌握索引、遍历、文件读取、解构、集合转换、while与break；在章专属实验中选择该节点并观察反馈；最后凭集合所有权图、越界实验、去重结果、文件样本和菜单格式断言回答练习、复位并重放。

### For the More Curious: Array Types

For the More Curious: Array Types服务于区分只读接口与不可变对象，掌握索引、遍历、文件读取、解构、集合转换、while与break。画出类型关系、状态拥有者、初始化时刻和允许替换，再用编译失败或替身实现挑战边界；验收时检查集合所有权图、越界实验、去重结果、文件样本和菜单格式断言是否支持对象不变量。

在 10. Lists and Sets 的 固定初态 阶段，For the More Curious: Array Types先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入施加动作，使用同一份输入把正常路径与把只读引用当作深度不可变，或在遍历期间修改共享集合产生的反例并排保存。

对 For the More Curious: Array Types 的四级验收分别是：正文出现该坐标；解释它如何参与区分只读接口与不可变对象，掌握索引、遍历、文件读取、解构、集合转换、while与break；在章专属实验中选择该节点并观察反馈；最后凭集合所有权图、越界实验、去重结果、文件样本和菜单格式断言回答练习、复位并重放。

### For the More Curious: Read-Only vs Immutable

For the More Curious: Read-Only vs Immutable服务于区分只读接口与不可变对象，掌握索引、遍历、文件读取、解构、集合转换、while与break。把这个目录坐标翻译成一个可执行或可判定合同，明确输入、状态变化、可见结果和反例，并以集合所有权图、越界实验、去重结果、文件样本和菜单格式断言完成独立复核。

在 10. Lists and Sets 的 施加动作 阶段，For the More Curious: Read-Only vs Immutable先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入推进状态，使用同一份输入把正常路径与把只读引用当作深度不可变，或在遍历期间修改共享集合产生的反例并排保存。

对 For the More Curious: Read-Only vs Immutable 的四级验收分别是：正文出现该坐标；解释它如何参与区分只读接口与不可变对象，掌握索引、遍历、文件读取、解构、集合转换、while与break；在章专属实验中选择该节点并观察反馈；最后凭集合所有权图、越界实验、去重结果、文件样本和菜单格式断言回答练习、复位并重放。

### Challenge: Formatted Tavern Menu

Challenge: Formatted Tavern Menu服务于区分只读接口与不可变对象，掌握索引、遍历、文件读取、解构、集合转换、while与break。先把题目改写成输入、输出、约束和失败样例，再运行正常、边界与非法三组断言；挑战完成的证据是集合所有权图、越界实验、去重结果、文件样本和菜单格式断言，不是一次示例输出。

在 10. Lists and Sets 的 推进状态 阶段，Challenge: Formatted Tavern Menu先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入观察差异，使用同一份输入把正常路径与把只读引用当作深度不可变，或在遍历期间修改共享集合产生的反例并排保存。

对 Challenge: Formatted Tavern Menu 的四级验收分别是：正文出现该坐标；解释它如何参与区分只读接口与不可变对象，掌握索引、遍历、文件读取、解构、集合转换、while与break；在章专属实验中选择该节点并观察反馈；最后凭集合所有权图、越界实验、去重结果、文件样本和菜单格式断言回答练习、复位并重放。

### Challenge: Advanced Formatted Tavern Menu

Challenge: Advanced Formatted Tavern Menu服务于区分只读接口与不可变对象，掌握索引、遍历、文件读取、解构、集合转换、while与break。先把题目改写成输入、输出、约束和失败样例，再运行正常、边界与非法三组断言；挑战完成的证据是集合所有权图、越界实验、去重结果、文件样本和菜单格式断言，不是一次示例输出。

在 10. Lists and Sets 的 观察差异 阶段，Challenge: Advanced Formatted Tavern Menu先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入复位重放，使用同一份输入把正常路径与把只读引用当作深度不可变，或在遍历期间修改共享集合产生的反例并排保存。

对 Challenge: Advanced Formatted Tavern Menu 的四级验收分别是：正文出现该坐标；解释它如何参与区分只读接口与不可变对象，掌握索引、遍历、文件读取、解构、集合转换、while与break；在章专属实验中选择该节点并观察反馈；最后凭集合所有权图、越界实验、去重结果、文件样本和菜单格式断言回答练习、复位并重放。

## 最小可执行切片

```kotlin
val menu = mutableListOf("mead", "wine", "mead")
val unique = menu.toSet()

for ((index, item) in unique.sorted().withIndex()) {
    println("" + (index + 1) + ". " + item)
}
check(unique.size == 2)
```

运行切片时固定 Kotlin/JDK 或 Android 工具链版本、输入数据和命令；保存标准输出、编译诊断及断言。迁移实验必须另建记录，不能静默改写2018年的历史坐标。

<Callout type="trap" title="本章核心陷阱">
  把只读引用当作深度不可变，或在遍历期间修改共享集合。先缩小到最小反例，明确是类型合同、对象所有权、求值顺序还是平台边界失效，再用同输入验证修复。
</Callout>

<Callout type="trap" title="把当前惯例倒灌进第一版">
  10. Lists and Sets保留 Kotlin 1.2/JVM 的历史身份；当前 Kotlin 或 Android 方案只作单独迁移坐标，不能据此改写第一版目录和接口身份。
</Callout>

<Callout type="trap" title="只保留成功输出">
  集合所有权图、越界实验、去重结果、文件样本和菜单格式断言必须同时包含正常、边界和失败路径；如果故障后不能恢复并复位，本页实践闭环仍未完成。
</Callout>

## 练习、答案与四级证据

<Exercises>

**问题 1：历史与迁移。** 怎样既保留 Kotlin 1.2 的教学身份，又使用当前官方资料核验机制？

<Answer>
  先以[Kotlin 1.2发布说明](https://kotlinlang.org/docs/whatsnew12.html)和目录锁定历史范围，再用[当前技术文档](https://kotlinlang.org/docs/basic-syntax.html)建立单独迁移记录。比较相同输入、结果和不变量；只改变版本坐标，不把当前API伪装成第一版内容。
</Answer>

**问题 2：逐节点验证。** 本页 19 个正式节点怎样从出现升级到可复查证据？

<Answer>
  - **10. Lists and Sets**：在固定初态阶段保存输入与结果，触发把只读引用当作深度不可变，或在遍历期间修改共享集合后恢复初态，并由集合所有权图、越界实验、去重结果、文件样本和菜单格式断言确认语义未漂移。
  - **Lists**：在施加动作阶段保存输入与结果，触发把只读引用当作深度不可变，或在遍历期间修改共享集合后恢复初态，并由集合所有权图、越界实验、去重结果、文件样本和菜单格式断言确认语义未漂移。
  - **Accessing a list’s elements**：在推进状态阶段保存输入与结果，触发把只读引用当作深度不可变，或在遍历期间修改共享集合后恢复初态，并由集合所有权图、越界实验、去重结果、文件样本和菜单格式断言确认语义未漂移。
  - **Index boundaries and safe index access**：在观察差异阶段保存输入与结果，触发把只读引用当作深度不可变，或在遍历期间修改共享集合后恢复初态，并由集合所有权图、越界实验、去重结果、文件样本和菜单格式断言确认语义未漂移。
  - **Checking the contents of a list**：在复位重放阶段保存输入与结果，触发把只读引用当作深度不可变，或在遍历期间修改共享集合后恢复初态，并由集合所有权图、越界实验、去重结果、文件样本和菜单格式断言确认语义未漂移。
  - **Changing a list’s contents**：在固定初态阶段保存输入与结果，触发把只读引用当作深度不可变，或在遍历期间修改共享集合后恢复初态，并由集合所有权图、越界实验、去重结果、文件样本和菜单格式断言确认语义未漂移。
  - **Iteration**：在施加动作阶段保存输入与结果，触发把只读引用当作深度不可变，或在遍历期间修改共享集合后恢复初态，并由集合所有权图、越界实验、去重结果、文件样本和菜单格式断言确认语义未漂移。
  - **Reading a File into a List**：在推进状态阶段保存输入与结果，触发把只读引用当作深度不可变，或在遍历期间修改共享集合后恢复初态，并由集合所有权图、越界实验、去重结果、文件样本和菜单格式断言确认语义未漂移。
  - **Destructuring**：在观察差异阶段保存输入与结果，触发把只读引用当作深度不可变，或在遍历期间修改共享集合后恢复初态，并由集合所有权图、越界实验、去重结果、文件样本和菜单格式断言确认语义未漂移。
  - **Sets**：在复位重放阶段保存输入与结果，触发把只读引用当作深度不可变，或在遍历期间修改共享集合后恢复初态，并由集合所有权图、越界实验、去重结果、文件样本和菜单格式断言确认语义未漂移。
  - **Creating a set**：在固定初态阶段保存输入与结果，触发把只读引用当作深度不可变，或在遍历期间修改共享集合后恢复初态，并由集合所有权图、越界实验、去重结果、文件样本和菜单格式断言确认语义未漂移。
  - **Adding elements to a set**：在施加动作阶段保存输入与结果，触发把只读引用当作深度不可变，或在遍历期间修改共享集合后恢复初态，并由集合所有权图、越界实验、去重结果、文件样本和菜单格式断言确认语义未漂移。
  - **while Loops**：在推进状态阶段保存输入与结果，触发把只读引用当作深度不可变，或在遍历期间修改共享集合后恢复初态，并由集合所有权图、越界实验、去重结果、文件样本和菜单格式断言确认语义未漂移。
  - **The break Expression**：在观察差异阶段保存输入与结果，触发把只读引用当作深度不可变，或在遍历期间修改共享集合后恢复初态，并由集合所有权图、越界实验、去重结果、文件样本和菜单格式断言确认语义未漂移。
  - **Collection Conversion**：在复位重放阶段保存输入与结果，触发把只读引用当作深度不可变，或在遍历期间修改共享集合后恢复初态，并由集合所有权图、越界实验、去重结果、文件样本和菜单格式断言确认语义未漂移。
  - **For the More Curious: Array Types**：在固定初态阶段保存输入与结果，触发把只读引用当作深度不可变，或在遍历期间修改共享集合后恢复初态，并由集合所有权图、越界实验、去重结果、文件样本和菜单格式断言确认语义未漂移。
  - **For the More Curious: Read-Only vs Immutable**：在施加动作阶段保存输入与结果，触发把只读引用当作深度不可变，或在遍历期间修改共享集合后恢复初态，并由集合所有权图、越界实验、去重结果、文件样本和菜单格式断言确认语义未漂移。
  - **Challenge: Formatted Tavern Menu**：在推进状态阶段保存输入与结果，触发把只读引用当作深度不可变，或在遍历期间修改共享集合后恢复初态，并由集合所有权图、越界实验、去重结果、文件样本和菜单格式断言确认语义未漂移。
  - **Challenge: Advanced Formatted Tavern Menu**：在观察差异阶段保存输入与结果，触发把只读引用当作深度不可变，或在遍历期间修改共享集合后恢复初态，并由集合所有权图、越界实验、去重结果、文件样本和菜单格式断言确认语义未漂移。
</Answer>

**问题 3：反例闭环。** 遇到把只读引用当作深度不可变，或在遍历期间修改共享集合时，如何判断修复真正成立？

<Answer>
  保存首个失败诊断，恢复显式输入域、类型或对象边界和求值顺序，再以相同工具链和输入重跑正常、边界、非法三组样例。只有集合所有权图、越界实验、去重结果、文件样本和菜单格式断言回到基线且区分只读接口与不可变对象，掌握索引、遍历、文件读取、解构、集合转换、while与break的输入、类型边界、求值结果和失败路径都能由独立读者重放重新成立，才能通过。
</Answer>

</Exercises>

## 术语复核与本章回顾

<Glossary>
  <GlossaryItem term="初始状态">
    初始状态在本页限定10. Lists and Sets允许进入系统的值与前置条件。
  </GlossaryItem>
  <GlossaryItem term="单一变量">
    单一变量在本页标记区分只读接口与不可变对象，掌握索引、遍历、文件读取、解构、集合转换、while与break中状态由谁创建、修改和释放。
  </GlossaryItem>
  <GlossaryItem term="状态迁移">
    状态迁移在本页说明Kotlin 1.2实现与当前迁移之间必须保持的语义。
  </GlossaryItem>
  <GlossaryItem term="可见反馈">
    可见反馈在本页保存能推翻把只读引用当作深度不可变，或在遍历期间修改共享集合的原始观察。
  </GlossaryItem>
  <GlossaryItem term="故障注入">
    故障注入在本页把错误停在复位重放而非继续扩散。
  </GlossaryItem>
  <GlossaryItem term="复位重放">
    复位重放在本页让另一位读者凭集合所有权图、越界实验、去重结果、文件样本和菜单格式断言得到相同结论。
  </GlossaryItem>
</Glossary>

完成10. Lists and Sets意味着：能解释区分只读接口与不可变对象，掌握索引、遍历、文件读取、解构、集合转换、while与break，能运行并改动章专属切片，能主动制造把只读引用当作深度不可变，或在遍历期间修改共享集合，还能凭集合所有权图、越界实验、去重结果、文件样本和菜单格式断言恢复并交接，而不是只识别术语。

## 阅读导航

[← 上一页：9. Standard Functions](/learn/kotlin-definitive-guide/04-values-collections/kdg1-09-standard-functions) · [下一页：11. Maps →](/learn/kotlin-definitive-guide/04-values-collections/kdg1-11-maps)

<Attribution
  mode="independent-rewrite"
  sourceBasis="outline-only"
  workTitle="Josh Skeen、David Greenhalgh《Kotlin Programming: The Big Nerd Ranch Guide》第1版（Addison-Wesley，2018）"
  adaptedUrl="https://www.oreilly.com/library/view/kotlin-programming-the/9780135165188/"
/>
