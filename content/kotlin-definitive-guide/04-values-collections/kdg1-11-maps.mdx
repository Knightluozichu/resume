---
title: 11. Maps
description: 11. Maps：用键值合同创建、读取、添加和修改Map，并区分缺键与可空值，通过全节点状态链、历史迁移对照和故障恢复完成验收。
type: C
section: Kotlin权威指南 · 值与集合
draft: false
math: false
sourceUrl: 'https://www.oreilly.com/library/view/kotlin-programming-the/9780135165188/'
demo: true
order: 5
qualityVersion: 2
practiceMode: simulation
sourceMode: independent-rewrite
---


{/* KOTLIN_DEFINITIVE_GUIDE_QUALITY_V2 */}

## 从本章问题开始

11. Maps要解决的不是语法记忆，而是用键值合同创建、读取、添加和修改Map，并区分缺键与可空值。先写出输入和期望结果，再运行最小切片；如果无法解释失败落在编译期、边界层还是运行期，程序即使输出一次正确答案也未通过。

本页不把 val 等同深度不可变，也不把简洁语法等同动态类型。Kotlin 的类型、可空性、求值规则和对象模型共同压缩非法状态；本章不变量是用键值合同创建、读取、添加和修改Map，并区分缺键与可空值的输入、类型边界、求值结果和失败路径都能由独立读者重放。

## 来源范围与年代边界

O’Reilly 的[第1版书目与完整目录](https://www.oreilly.com/library/view/kotlin-programming-the/9780135165188/)只用于核定本页范围，[中文版出版记录](https://e.dangdang.com/touch/products/1901154363.html)用于交叉核对译本信息；未取得的原书正文不作忠实性宣称。11. Maps涉及的语言机制由[JetBrains/Android 一手文档](https://kotlinlang.org/docs/basic-syntax.html)独立核验，以下中文讲解、代码组织、图示、实验和练习均为独立教学重写。

第1版的教学坐标是2018年前后的 Kotlin 1.2、JVM、IntelliJ 与当时 Android/早期协程接口。[Kotlin 1.2 发布说明](https://kotlinlang.org/docs/whatsnew12.html)用于锁定历史能力；当前文档只提供迁移对照，Flow、Compose、MockK、现代 Gradle Kotlin DSL 和第二版新增内容不倒灌为原书内容。

## 本章机制与六个检查点

、、、、、。这些检查点共同回答用键值合同创建、读取、添加和修改Map，并区分缺键与可空值，最终必须落到键空间说明、缺键样例、更新前后快照、守卫规则与断言，不能只停在API名称或目录词。

## 正式目录逐项深读

### 11. Maps

11. Maps服务于用键值合同创建、读取、添加和修改Map，并区分缺键与可空值。固定集合或数值样本，记录索引、类型、转换和求值次数，仅改变一个边界条件；由键空间说明、缺键样例、更新前后快照、守卫规则与断言判断只读、不可变、精度或惰性结论是否成立。

在 11. Maps 的 固定初态 阶段，11. Maps先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入施加动作，使用同一份输入把正常路径与用get返回null同时表示缺键和实际空值，掩盖数据状态产生的反例并排保存。

对 11. Maps 的四级验收分别是：正文出现该坐标；解释它如何参与用键值合同创建、读取、添加和修改Map，并区分缺键与可空值；在章专属实验中选择该节点并观察反馈；最后凭键空间说明、缺键样例、更新前后快照、守卫规则与断言回答练习、复位并重放。

### Creating a Map

Creating a Map服务于用键值合同创建、读取、添加和修改Map，并区分缺键与可空值。固定集合或数值样本，记录索引、类型、转换和求值次数，仅改变一个边界条件；由键空间说明、缺键样例、更新前后快照、守卫规则与断言判断只读、不可变、精度或惰性结论是否成立。

在 11. Maps 的 施加动作 阶段，Creating a Map先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入推进状态，使用同一份输入把正常路径与用get返回null同时表示缺键和实际空值，掩盖数据状态产生的反例并排保存。

对 Creating a Map 的四级验收分别是：正文出现该坐标；解释它如何参与用键值合同创建、读取、添加和修改Map，并区分缺键与可空值；在章专属实验中选择该节点并观察反馈；最后凭键空间说明、缺键样例、更新前后快照、守卫规则与断言回答练习、复位并重放。

### Accessing Map Values

Accessing Map Values服务于用键值合同创建、读取、添加和修改Map，并区分缺键与可空值。固定集合或数值样本，记录索引、类型、转换和求值次数，仅改变一个边界条件；由键空间说明、缺键样例、更新前后快照、守卫规则与断言判断只读、不可变、精度或惰性结论是否成立。

在 11. Maps 的 推进状态 阶段，Accessing Map Values先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入观察差异，使用同一份输入把正常路径与用get返回null同时表示缺键和实际空值，掩盖数据状态产生的反例并排保存。

对 Accessing Map Values 的四级验收分别是：正文出现该坐标；解释它如何参与用键值合同创建、读取、添加和修改Map，并区分缺键与可空值；在章专属实验中选择该节点并观察反馈；最后凭键空间说明、缺键样例、更新前后快照、守卫规则与断言回答练习、复位并重放。

### Adding Entries to a Map

Adding Entries to a Map服务于用键值合同创建、读取、添加和修改Map，并区分缺键与可空值。固定集合或数值样本，记录索引、类型、转换和求值次数，仅改变一个边界条件；由键空间说明、缺键样例、更新前后快照、守卫规则与断言判断只读、不可变、精度或惰性结论是否成立。

在 11. Maps 的 观察差异 阶段，Adding Entries to a Map先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入复位重放，使用同一份输入把正常路径与用get返回null同时表示缺键和实际空值，掩盖数据状态产生的反例并排保存。

对 Adding Entries to a Map 的四级验收分别是：正文出现该坐标；解释它如何参与用键值合同创建、读取、添加和修改Map，并区分缺键与可空值；在章专属实验中选择该节点并观察反馈；最后凭键空间说明、缺键样例、更新前后快照、守卫规则与断言回答练习、复位并重放。

### Modifying Map Values

Modifying Map Values服务于用键值合同创建、读取、添加和修改Map，并区分缺键与可空值。固定集合或数值样本，记录索引、类型、转换和求值次数，仅改变一个边界条件；由键空间说明、缺键样例、更新前后快照、守卫规则与断言判断只读、不可变、精度或惰性结论是否成立。

在 11. Maps 的 复位重放 阶段，Modifying Map Values先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入固定初态，使用同一份输入把正常路径与用get返回null同时表示缺键和实际空值，掩盖数据状态产生的反例并排保存。

对 Modifying Map Values 的四级验收分别是：正文出现该坐标；解释它如何参与用键值合同创建、读取、添加和修改Map，并区分缺键与可空值；在章专属实验中选择该节点并观察反馈；最后凭键空间说明、缺键样例、更新前后快照、守卫规则与断言回答练习、复位并重放。

### Challenge: Tavern Bouncer

Challenge: Tavern Bouncer服务于用键值合同创建、读取、添加和修改Map，并区分缺键与可空值。先把题目改写成输入、输出、约束和失败样例，再运行正常、边界与非法三组断言；挑战完成的证据是键空间说明、缺键样例、更新前后快照、守卫规则与断言，不是一次示例输出。

在 11. Maps 的 固定初态 阶段，Challenge: Tavern Bouncer先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入施加动作，使用同一份输入把正常路径与用get返回null同时表示缺键和实际空值，掩盖数据状态产生的反例并排保存。

对 Challenge: Tavern Bouncer 的四级验收分别是：正文出现该坐标；解释它如何参与用键值合同创建、读取、添加和修改Map，并区分缺键与可空值；在章专属实验中选择该节点并观察反馈；最后凭键空间说明、缺键样例、更新前后快照、守卫规则与断言回答练习、复位并重放。

## 最小可执行切片

```kotlin
val patronGold = mutableMapOf("Madrigal" to 10)

fun charge(name: String, price: Int): Boolean {
    val balance = patronGold[name] ?: return false
    if (balance < price) return false
    patronGold[name] = balance - price
    return true
}
```

运行切片时固定 Kotlin/JDK 或 Android 工具链版本、输入数据和命令；保存标准输出、编译诊断及断言。迁移实验必须另建记录，不能静默改写2018年的历史坐标。


## 练习、答案与四级证据


## 术语复核与本章回顾


完成11. Maps意味着：能解释用键值合同创建、读取、添加和修改Map，并区分缺键与可空值，能运行并改动章专属切片，能主动制造用get返回null同时表示缺键和实际空值，掩盖数据状态，还能凭键空间说明、缺键样例、更新前后快照、守卫规则与断言恢复并交接，而不是只识别术语。

## 阅读导航

[← 上一页：10. Lists and Sets](/learn/kotlin-definitive-guide/04-values-collections/kdg1-10-lists-sets) · [下一页：12. Defining Classes →](/learn/kotlin-definitive-guide/05-object-model/kdg1-12-defining-classes)

