---
title: 13. Initialization
description: >-
  13.
  Initialization：比较主构造、次构造、初始化块、属性顺序、lateinit与lazy，保证对象一经发布即有效，通过全节点状态链、历史迁移对照和故障恢复完成验收。
type: C
section: Kotlin权威指南 · 对象模型
draft: false
math: false
sourceUrl: 'https://www.oreilly.com/library/view/kotlin-programming-the/9780135165188/'
demo: true
order: 2
qualityVersion: 2
practiceMode: diagnosis
sourceMode: independent-rewrite
---


{/* KOTLIN_DEFINITIVE_GUIDE_QUALITY_V2 */}

## 从本章问题开始

13. Initialization要解决的不是语法记忆，而是比较主构造、次构造、初始化块、属性顺序、lateinit与lazy，保证对象一经发布即有效。先写出输入和期望结果，再运行最小切片；如果无法解释失败落在编译期、边界层还是运行期，程序即使输出一次正确答案也未通过。

本页不把 val 等同深度不可变，也不把简洁语法等同动态类型。Kotlin 的类型、可空性、求值规则和对象模型共同压缩非法状态；本章不变量是比较主构造、次构造、初始化块、属性顺序、lateinit与lazy，保证对象一经发布即有效的输入、类型边界、求值结果和失败路径都能由独立读者重放。

## 来源范围与年代边界

O’Reilly 的[第1版书目与完整目录](https://www.oreilly.com/library/view/kotlin-programming-the/9780135165188/)只用于核定本页范围，[中文版出版记录](https://e.dangdang.com/touch/products/1901154363.html)用于交叉核对译本信息；未取得的原书正文不作忠实性宣称。13. Initialization涉及的语言机制由[JetBrains/Android 一手文档](https://kotlinlang.org/docs/basic-syntax.html)独立核验，以下中文讲解、代码组织、图示、实验和练习均为独立教学重写。

第1版的教学坐标是2018年前后的 Kotlin 1.2、JVM、IntelliJ 与当时 Android/早期协程接口。[Kotlin 1.2 发布说明](https://kotlinlang.org/docs/whatsnew12.html)用于锁定历史能力；当前文档只提供迁移对照，Flow、Compose、MockK、现代 Gradle Kotlin DSL 和第二版新增内容不倒灌为原书内容。

## 本章机制与六个检查点

、、、、、。这些检查点共同回答比较主构造、次构造、初始化块、属性顺序、lateinit与lazy，保证对象一经发布即有效，最终必须落到初始化时间线、构造路径表、失败样例、lazy调用计数和圣剑挑战，不能只停在API名称或目录词。

## 正式目录逐项深读

### 13. Initialization

13. Initialization服务于比较主构造、次构造、初始化块、属性顺序、lateinit与lazy，保证对象一经发布即有效。把这个目录坐标翻译成一个可执行或可判定合同，明确输入、状态变化、可见结果和反例，并以初始化时间线、构造路径表、失败样例、lazy调用计数和圣剑挑战完成独立复核。

在 13. Initialization 的 建立基线 阶段，13. Initialization先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入注入反例，使用同一份输入把正常路径与在初始化顺序中读取尚未建立的属性，或滥用lateinit推迟必填依赖产生的反例并排保存。

对 13. Initialization 的四级验收分别是：正文出现该坐标；解释它如何参与比较主构造、次构造、初始化块、属性顺序、lateinit与lazy，保证对象一经发布即有效；在章专属实验中选择该节点并观察反馈；最后凭初始化时间线、构造路径表、失败样例、lazy调用计数和圣剑挑战回答练习、复位并重放。

### Constructors

Constructors服务于比较主构造、次构造、初始化块、属性顺序、lateinit与lazy，保证对象一经发布即有效。画出类型关系、状态拥有者、初始化时刻和允许替换，再用编译失败或替身实现挑战边界；验收时检查初始化时间线、构造路径表、失败样例、lazy调用计数和圣剑挑战是否支持对象不变量。

在 13. Initialization 的 注入反例 阶段，Constructors先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入定位边界，使用同一份输入把正常路径与在初始化顺序中读取尚未建立的属性，或滥用lateinit推迟必填依赖产生的反例并排保存。

对 Constructors 的四级验收分别是：正文出现该坐标；解释它如何参与比较主构造、次构造、初始化块、属性顺序、lateinit与lazy，保证对象一经发布即有效；在章专属实验中选择该节点并观察反馈；最后凭初始化时间线、构造路径表、失败样例、lazy调用计数和圣剑挑战回答练习、复位并重放。

### Primary constructors

Primary constructors服务于比较主构造、次构造、初始化块、属性顺序、lateinit与lazy，保证对象一经发布即有效。画出类型关系、状态拥有者、初始化时刻和允许替换，再用编译失败或替身实现挑战边界；验收时检查初始化时间线、构造路径表、失败样例、lazy调用计数和圣剑挑战是否支持对象不变量。

在 13. Initialization 的 定位边界 阶段，Primary constructors先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入修复合同，使用同一份输入把正常路径与在初始化顺序中读取尚未建立的属性，或滥用lateinit推迟必填依赖产生的反例并排保存。

对 Primary constructors 的四级验收分别是：正文出现该坐标；解释它如何参与比较主构造、次构造、初始化块、属性顺序、lateinit与lazy，保证对象一经发布即有效；在章专属实验中选择该节点并观察反馈；最后凭初始化时间线、构造路径表、失败样例、lazy调用计数和圣剑挑战回答练习、复位并重放。

### Defining properties in a primary constructor

Defining properties in a primary constructor服务于比较主构造、次构造、初始化块、属性顺序、lateinit与lazy，保证对象一经发布即有效。画出类型关系、状态拥有者、初始化时刻和允许替换，再用编译失败或替身实现挑战边界；验收时检查初始化时间线、构造路径表、失败样例、lazy调用计数和圣剑挑战是否支持对象不变量。

在 13. Initialization 的 修复合同 阶段，Defining properties in a primary constructor先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入同输入复验，使用同一份输入把正常路径与在初始化顺序中读取尚未建立的属性，或滥用lateinit推迟必填依赖产生的反例并排保存。

对 Defining properties in a primary constructor 的四级验收分别是：正文出现该坐标；解释它如何参与比较主构造、次构造、初始化块、属性顺序、lateinit与lazy，保证对象一经发布即有效；在章专属实验中选择该节点并观察反馈；最后凭初始化时间线、构造路径表、失败样例、lazy调用计数和圣剑挑战回答练习、复位并重放。

### Secondary constructors

Secondary constructors服务于比较主构造、次构造、初始化块、属性顺序、lateinit与lazy，保证对象一经发布即有效。画出类型关系、状态拥有者、初始化时刻和允许替换，再用编译失败或替身实现挑战边界；验收时检查初始化时间线、构造路径表、失败样例、lazy调用计数和圣剑挑战是否支持对象不变量。

在 13. Initialization 的 同输入复验 阶段，Secondary constructors先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入建立基线，使用同一份输入把正常路径与在初始化顺序中读取尚未建立的属性，或滥用lateinit推迟必填依赖产生的反例并排保存。

对 Secondary constructors 的四级验收分别是：正文出现该坐标；解释它如何参与比较主构造、次构造、初始化块、属性顺序、lateinit与lazy，保证对象一经发布即有效；在章专属实验中选择该节点并观察反馈；最后凭初始化时间线、构造路径表、失败样例、lazy调用计数和圣剑挑战回答练习、复位并重放。

### Default arguments

Default arguments服务于比较主构造、次构造、初始化块、属性顺序、lateinit与lazy，保证对象一经发布即有效。把这个目录坐标翻译成一个可执行或可判定合同，明确输入、状态变化、可见结果和反例，并以初始化时间线、构造路径表、失败样例、lazy调用计数和圣剑挑战完成独立复核。

在 13. Initialization 的 建立基线 阶段，Default arguments先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入注入反例，使用同一份输入把正常路径与在初始化顺序中读取尚未建立的属性，或滥用lateinit推迟必填依赖产生的反例并排保存。

对 Default arguments 的四级验收分别是：正文出现该坐标；解释它如何参与比较主构造、次构造、初始化块、属性顺序、lateinit与lazy，保证对象一经发布即有效；在章专属实验中选择该节点并观察反馈；最后凭初始化时间线、构造路径表、失败样例、lazy调用计数和圣剑挑战回答练习、复位并重放。

### Named arguments

Named arguments服务于比较主构造、次构造、初始化块、属性顺序、lateinit与lazy，保证对象一经发布即有效。把这个目录坐标翻译成一个可执行或可判定合同，明确输入、状态变化、可见结果和反例，并以初始化时间线、构造路径表、失败样例、lazy调用计数和圣剑挑战完成独立复核。

在 13. Initialization 的 注入反例 阶段，Named arguments先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入定位边界，使用同一份输入把正常路径与在初始化顺序中读取尚未建立的属性，或滥用lateinit推迟必填依赖产生的反例并排保存。

对 Named arguments 的四级验收分别是：正文出现该坐标；解释它如何参与比较主构造、次构造、初始化块、属性顺序、lateinit与lazy，保证对象一经发布即有效；在章专属实验中选择该节点并观察反馈；最后凭初始化时间线、构造路径表、失败样例、lazy调用计数和圣剑挑战回答练习、复位并重放。

### Initializer Blocks

Initializer Blocks服务于比较主构造、次构造、初始化块、属性顺序、lateinit与lazy，保证对象一经发布即有效。把这个目录坐标翻译成一个可执行或可判定合同，明确输入、状态变化、可见结果和反例，并以初始化时间线、构造路径表、失败样例、lazy调用计数和圣剑挑战完成独立复核。

在 13. Initialization 的 定位边界 阶段，Initializer Blocks先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入修复合同，使用同一份输入把正常路径与在初始化顺序中读取尚未建立的属性，或滥用lateinit推迟必填依赖产生的反例并排保存。

对 Initializer Blocks 的四级验收分别是：正文出现该坐标；解释它如何参与比较主构造、次构造、初始化块、属性顺序、lateinit与lazy，保证对象一经发布即有效；在章专属实验中选择该节点并观察反馈；最后凭初始化时间线、构造路径表、失败样例、lazy调用计数和圣剑挑战回答练习、复位并重放。

### Property Initialization

Property Initialization服务于比较主构造、次构造、初始化块、属性顺序、lateinit与lazy，保证对象一经发布即有效。画出类型关系、状态拥有者、初始化时刻和允许替换，再用编译失败或替身实现挑战边界；验收时检查初始化时间线、构造路径表、失败样例、lazy调用计数和圣剑挑战是否支持对象不变量。

在 13. Initialization 的 修复合同 阶段，Property Initialization先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入同输入复验，使用同一份输入把正常路径与在初始化顺序中读取尚未建立的属性，或滥用lateinit推迟必填依赖产生的反例并排保存。

对 Property Initialization 的四级验收分别是：正文出现该坐标；解释它如何参与比较主构造、次构造、初始化块、属性顺序、lateinit与lazy，保证对象一经发布即有效；在章专属实验中选择该节点并观察反馈；最后凭初始化时间线、构造路径表、失败样例、lazy调用计数和圣剑挑战回答练习、复位并重放。

### Initialization Order

Initialization Order服务于比较主构造、次构造、初始化块、属性顺序、lateinit与lazy，保证对象一经发布即有效。把这个目录坐标翻译成一个可执行或可判定合同，明确输入、状态变化、可见结果和反例，并以初始化时间线、构造路径表、失败样例、lazy调用计数和圣剑挑战完成独立复核。

在 13. Initialization 的 同输入复验 阶段，Initialization Order先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入建立基线，使用同一份输入把正常路径与在初始化顺序中读取尚未建立的属性，或滥用lateinit推迟必填依赖产生的反例并排保存。

对 Initialization Order 的四级验收分别是：正文出现该坐标；解释它如何参与比较主构造、次构造、初始化块、属性顺序、lateinit与lazy，保证对象一经发布即有效；在章专属实验中选择该节点并观察反馈；最后凭初始化时间线、构造路径表、失败样例、lazy调用计数和圣剑挑战回答练习、复位并重放。

### Delaying Initialization

Delaying Initialization服务于比较主构造、次构造、初始化块、属性顺序、lateinit与lazy，保证对象一经发布即有效。把这个目录坐标翻译成一个可执行或可判定合同，明确输入、状态变化、可见结果和反例，并以初始化时间线、构造路径表、失败样例、lazy调用计数和圣剑挑战完成独立复核。

在 13. Initialization 的 建立基线 阶段，Delaying Initialization先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入注入反例，使用同一份输入把正常路径与在初始化顺序中读取尚未建立的属性，或滥用lateinit推迟必填依赖产生的反例并排保存。

对 Delaying Initialization 的四级验收分别是：正文出现该坐标；解释它如何参与比较主构造、次构造、初始化块、属性顺序、lateinit与lazy，保证对象一经发布即有效；在章专属实验中选择该节点并观察反馈；最后凭初始化时间线、构造路径表、失败样例、lazy调用计数和圣剑挑战回答练习、复位并重放。

### Late initialization

Late initialization服务于比较主构造、次构造、初始化块、属性顺序、lateinit与lazy，保证对象一经发布即有效。把这个目录坐标翻译成一个可执行或可判定合同，明确输入、状态变化、可见结果和反例，并以初始化时间线、构造路径表、失败样例、lazy调用计数和圣剑挑战完成独立复核。

在 13. Initialization 的 注入反例 阶段，Late initialization先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入定位边界，使用同一份输入把正常路径与在初始化顺序中读取尚未建立的属性，或滥用lateinit推迟必填依赖产生的反例并排保存。

对 Late initialization 的四级验收分别是：正文出现该坐标；解释它如何参与比较主构造、次构造、初始化块、属性顺序、lateinit与lazy，保证对象一经发布即有效；在章专属实验中选择该节点并观察反馈；最后凭初始化时间线、构造路径表、失败样例、lazy调用计数和圣剑挑战回答练习、复位并重放。

### Lazy initialization

Lazy initialization服务于比较主构造、次构造、初始化块、属性顺序、lateinit与lazy，保证对象一经发布即有效。把这个目录坐标翻译成一个可执行或可判定合同，明确输入、状态变化、可见结果和反例，并以初始化时间线、构造路径表、失败样例、lazy调用计数和圣剑挑战完成独立复核。

在 13. Initialization 的 定位边界 阶段，Lazy initialization先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入修复合同，使用同一份输入把正常路径与在初始化顺序中读取尚未建立的属性，或滥用lateinit推迟必填依赖产生的反例并排保存。

对 Lazy initialization 的四级验收分别是：正文出现该坐标；解释它如何参与比较主构造、次构造、初始化块、属性顺序、lateinit与lazy，保证对象一经发布即有效；在章专属实验中选择该节点并观察反馈；最后凭初始化时间线、构造路径表、失败样例、lazy调用计数和圣剑挑战回答练习、复位并重放。

### For the More Curious: Initialization Gotchas

For the More Curious: Initialization Gotchas服务于比较主构造、次构造、初始化块、属性顺序、lateinit与lazy，保证对象一经发布即有效。把这个目录坐标翻译成一个可执行或可判定合同，明确输入、状态变化、可见结果和反例，并以初始化时间线、构造路径表、失败样例、lazy调用计数和圣剑挑战完成独立复核。

在 13. Initialization 的 修复合同 阶段，For the More Curious: Initialization Gotchas先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入同输入复验，使用同一份输入把正常路径与在初始化顺序中读取尚未建立的属性，或滥用lateinit推迟必填依赖产生的反例并排保存。

对 For the More Curious: Initialization Gotchas 的四级验收分别是：正文出现该坐标；解释它如何参与比较主构造、次构造、初始化块、属性顺序、lateinit与lazy，保证对象一经发布即有效；在章专属实验中选择该节点并观察反馈；最后凭初始化时间线、构造路径表、失败样例、lazy调用计数和圣剑挑战回答练习、复位并重放。

### Challenge: The Riddle of Excalibur

Challenge: The Riddle of Excalibur服务于比较主构造、次构造、初始化块、属性顺序、lateinit与lazy，保证对象一经发布即有效。先把题目改写成输入、输出、约束和失败样例，再运行正常、边界与非法三组断言；挑战完成的证据是初始化时间线、构造路径表、失败样例、lazy调用计数和圣剑挑战，不是一次示例输出。

在 13. Initialization 的 同输入复验 阶段，Challenge: The Riddle of Excalibur先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入建立基线，使用同一份输入把正常路径与在初始化顺序中读取尚未建立的属性，或滥用lateinit推迟必填依赖产生的反例并排保存。

对 Challenge: The Riddle of Excalibur 的四级验收分别是：正文出现该坐标；解释它如何参与比较主构造、次构造、初始化块、属性顺序、lateinit与lazy，保证对象一经发布即有效；在章专属实验中选择该节点并观察反馈；最后凭初始化时间线、构造路径表、失败样例、lazy调用计数和圣剑挑战回答练习、复位并重放。

## 最小可执行切片

```kotlin
class Room(val name: String, danger: Int = 1) {
    val dangerLevel: Int
    val description: String by lazy { "Room: " + name }

    init {
        require(name.isNotBlank())
        dangerLevel = danger.coerceIn(0, 100)
    }
}
```

运行切片时固定 Kotlin/JDK 或 Android 工具链版本、输入数据和命令；保存标准输出、编译诊断及断言。迁移实验必须另建记录，不能静默改写2018年的历史坐标。


## 练习、答案与四级证据


## 术语复核与本章回顾


完成13. Initialization意味着：能解释比较主构造、次构造、初始化块、属性顺序、lateinit与lazy，保证对象一经发布即有效，能运行并改动章专属切片，能主动制造在初始化顺序中读取尚未建立的属性，或滥用lateinit推迟必填依赖，还能凭初始化时间线、构造路径表、失败样例、lazy调用计数和圣剑挑战恢复并交接，而不是只识别术语。

## 阅读导航

[← 上一页：12. Defining Classes](/learn/kotlin-definitive-guide/05-object-model/kdg1-12-defining-classes) · [下一页：14. Inheritance →](/learn/kotlin-definitive-guide/05-object-model/kdg1-14-inheritance)

