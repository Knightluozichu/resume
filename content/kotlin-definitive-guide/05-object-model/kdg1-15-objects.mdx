---
title: 15. Objects
description: 15. Objects：比较对象声明、对象表达式、伴生对象、嵌套类、数据类、枚举、解构与运算符重载，通过全节点状态链、历史迁移对照和故障恢复完成验收。
type: C
section: Kotlin权威指南 · 对象模型
draft: false
math: false
sourceUrl: 'https://www.oreilly.com/library/view/kotlin-programming-the/9780135165188/'
demo: true
order: 4
qualityVersion: 2
practiceMode: design
sourceMode: independent-rewrite
---


{/* KOTLIN_DEFINITIVE_GUIDE_QUALITY_V2 */}

## 从本章问题开始

15. Objects要解决的不是语法记忆，而是比较对象声明、对象表达式、伴生对象、嵌套类、数据类、枚举、解构与运算符重载。先写出输入和期望结果，再运行最小切片；如果无法解释失败落在编译期、边界层还是运行期，程序即使输出一次正确答案也未通过。

本页不把 val 等同深度不可变，也不把简洁语法等同动态类型。Kotlin 的类型、可空性、求值规则和对象模型共同压缩非法状态；本章不变量是比较对象声明、对象表达式、伴生对象、嵌套类、数据类、枚举、解构与运算符重载的输入、类型边界、求值结果和失败路径都能由独立读者重放。

## 来源范围与年代边界

O’Reilly 的[第1版书目与完整目录](https://www.oreilly.com/library/view/kotlin-programming-the/9780135165188/)只用于核定本页范围，[中文版出版记录](https://e.dangdang.com/touch/products/1901154363.html)用于交叉核对译本信息；未取得的原书正文不作忠实性宣称。15. Objects涉及的语言机制由[JetBrains/Android 一手文档](https://kotlinlang.org/docs/basic-syntax.html)独立核验，以下中文讲解、代码组织、图示、实验和练习均为独立教学重写。

第1版的教学坐标是2018年前后的 Kotlin 1.2、JVM、IntelliJ 与当时 Android/早期协程接口。[Kotlin 1.2 发布说明](https://kotlinlang.org/docs/whatsnew12.html)用于锁定历史能力；当前文档只提供迁移对照，Flow、Compose、MockK、现代 Gradle Kotlin DSL 和第二版新增内容不倒灌为原书内容。

## 本章机制与六个检查点

、、、、、。这些检查点共同回答比较对象声明、对象表达式、伴生对象、嵌套类、数据类、枚举、解构与运算符重载，最终必须落到实例数量图、生成方法检查、equals合同、copy实验、命令模型和世界地图，不能只停在API名称或目录词。

## 正式目录逐项深读

### 15. Objects

15. Objects服务于比较对象声明、对象表达式、伴生对象、嵌套类、数据类、枚举、解构与运算符重载。画出类型关系、状态拥有者、初始化时刻和允许替换，再用编译失败或替身实现挑战边界；验收时检查实例数量图、生成方法检查、equals合同、copy实验、命令模型和世界地图是否支持对象不变量。

在 15. Objects 的 界定责任 阶段，15. Objects先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入比较方案，使用同一份输入把正常路径与把全局对象变成隐式可变状态，或重载运算符却违背读者直觉产生的反例并排保存。

对 15. Objects 的四级验收分别是：正文出现该坐标；解释它如何参与比较对象声明、对象表达式、伴生对象、嵌套类、数据类、枚举、解构与运算符重载；在章专属实验中选择该节点并观察反馈；最后凭实例数量图、生成方法检查、equals合同、copy实验、命令模型和世界地图回答练习、复位并重放。

### The object Keyword

The object Keyword服务于比较对象声明、对象表达式、伴生对象、嵌套类、数据类、枚举、解构与运算符重载。画出类型关系、状态拥有者、初始化时刻和允许替换，再用编译失败或替身实现挑战边界；验收时检查实例数量图、生成方法检查、equals合同、copy实验、命令模型和世界地图是否支持对象不变量。

在 15. Objects 的 比较方案 阶段，The object Keyword先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入声明不变量，使用同一份输入把正常路径与把全局对象变成隐式可变状态，或重载运算符却违背读者直觉产生的反例并排保存。

对 The object Keyword 的四级验收分别是：正文出现该坐标；解释它如何参与比较对象声明、对象表达式、伴生对象、嵌套类、数据类、枚举、解构与运算符重载；在章专属实验中选择该节点并观察反馈；最后凭实例数量图、生成方法检查、equals合同、copy实验、命令模型和世界地图回答练习、复位并重放。

### Object declarations

Object declarations服务于比较对象声明、对象表达式、伴生对象、嵌套类、数据类、枚举、解构与运算符重载。画出类型关系、状态拥有者、初始化时刻和允许替换，再用编译失败或替身实现挑战边界；验收时检查实例数量图、生成方法检查、equals合同、copy实验、命令模型和世界地图是否支持对象不变量。

在 15. Objects 的 声明不变量 阶段，Object declarations先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入验证替换，使用同一份输入把正常路径与把全局对象变成隐式可变状态，或重载运算符却违背读者直觉产生的反例并排保存。

对 Object declarations 的四级验收分别是：正文出现该坐标；解释它如何参与比较对象声明、对象表达式、伴生对象、嵌套类、数据类、枚举、解构与运算符重载；在章专属实验中选择该节点并观察反馈；最后凭实例数量图、生成方法检查、equals合同、copy实验、命令模型和世界地图回答练习、复位并重放。

### Object expressions

Object expressions服务于比较对象声明、对象表达式、伴生对象、嵌套类、数据类、枚举、解构与运算符重载。画出类型关系、状态拥有者、初始化时刻和允许替换，再用编译失败或替身实现挑战边界；验收时检查实例数量图、生成方法检查、equals合同、copy实验、命令模型和世界地图是否支持对象不变量。

在 15. Objects 的 验证替换 阶段，Object expressions先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入记录决策，使用同一份输入把正常路径与把全局对象变成隐式可变状态，或重载运算符却违背读者直觉产生的反例并排保存。

对 Object expressions 的四级验收分别是：正文出现该坐标；解释它如何参与比较对象声明、对象表达式、伴生对象、嵌套类、数据类、枚举、解构与运算符重载；在章专属实验中选择该节点并观察反馈；最后凭实例数量图、生成方法检查、equals合同、copy实验、命令模型和世界地图回答练习、复位并重放。

### Companion objects

Companion objects服务于比较对象声明、对象表达式、伴生对象、嵌套类、数据类、枚举、解构与运算符重载。画出类型关系、状态拥有者、初始化时刻和允许替换，再用编译失败或替身实现挑战边界；验收时检查实例数量图、生成方法检查、equals合同、copy实验、命令模型和世界地图是否支持对象不变量。

在 15. Objects 的 记录决策 阶段，Companion objects先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入界定责任，使用同一份输入把正常路径与把全局对象变成隐式可变状态，或重载运算符却违背读者直觉产生的反例并排保存。

对 Companion objects 的四级验收分别是：正文出现该坐标；解释它如何参与比较对象声明、对象表达式、伴生对象、嵌套类、数据类、枚举、解构与运算符重载；在章专属实验中选择该节点并观察反馈；最后凭实例数量图、生成方法检查、equals合同、copy实验、命令模型和世界地图回答练习、复位并重放。

### Nested Classes

Nested Classes服务于比较对象声明、对象表达式、伴生对象、嵌套类、数据类、枚举、解构与运算符重载。画出类型关系、状态拥有者、初始化时刻和允许替换，再用编译失败或替身实现挑战边界；验收时检查实例数量图、生成方法检查、equals合同、copy实验、命令模型和世界地图是否支持对象不变量。

在 15. Objects 的 界定责任 阶段，Nested Classes先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入比较方案，使用同一份输入把正常路径与把全局对象变成隐式可变状态，或重载运算符却违背读者直觉产生的反例并排保存。

对 Nested Classes 的四级验收分别是：正文出现该坐标；解释它如何参与比较对象声明、对象表达式、伴生对象、嵌套类、数据类、枚举、解构与运算符重载；在章专属实验中选择该节点并观察反馈；最后凭实例数量图、生成方法检查、equals合同、copy实验、命令模型和世界地图回答练习、复位并重放。

### Data Classes

Data Classes服务于比较对象声明、对象表达式、伴生对象、嵌套类、数据类、枚举、解构与运算符重载。画出类型关系、状态拥有者、初始化时刻和允许替换，再用编译失败或替身实现挑战边界；验收时检查实例数量图、生成方法检查、equals合同、copy实验、命令模型和世界地图是否支持对象不变量。

在 15. Objects 的 比较方案 阶段，Data Classes先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入声明不变量，使用同一份输入把正常路径与把全局对象变成隐式可变状态，或重载运算符却违背读者直觉产生的反例并排保存。

对 Data Classes 的四级验收分别是：正文出现该坐标；解释它如何参与比较对象声明、对象表达式、伴生对象、嵌套类、数据类、枚举、解构与运算符重载；在章专属实验中选择该节点并观察反馈；最后凭实例数量图、生成方法检查、equals合同、copy实验、命令模型和世界地图回答练习、复位并重放。

### toString

toString服务于比较对象声明、对象表达式、伴生对象、嵌套类、数据类、枚举、解构与运算符重载。固定集合或数值样本，记录索引、类型、转换和求值次数，仅改变一个边界条件；由实例数量图、生成方法检查、equals合同、copy实验、命令模型和世界地图判断只读、不可变、精度或惰性结论是否成立。

在 15. Objects 的 声明不变量 阶段，toString先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入验证替换，使用同一份输入把正常路径与把全局对象变成隐式可变状态，或重载运算符却违背读者直觉产生的反例并排保存。

对 toString 的四级验收分别是：正文出现该坐标；解释它如何参与比较对象声明、对象表达式、伴生对象、嵌套类、数据类、枚举、解构与运算符重载；在章专属实验中选择该节点并观察反馈；最后凭实例数量图、生成方法检查、equals合同、copy实验、命令模型和世界地图回答练习、复位并重放。

### equals

equals服务于比较对象声明、对象表达式、伴生对象、嵌套类、数据类、枚举、解构与运算符重载。把这个目录坐标翻译成一个可执行或可判定合同，明确输入、状态变化、可见结果和反例，并以实例数量图、生成方法检查、equals合同、copy实验、命令模型和世界地图完成独立复核。

在 15. Objects 的 验证替换 阶段，equals先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入记录决策，使用同一份输入把正常路径与把全局对象变成隐式可变状态，或重载运算符却违背读者直觉产生的反例并排保存。

对 equals 的四级验收分别是：正文出现该坐标；解释它如何参与比较对象声明、对象表达式、伴生对象、嵌套类、数据类、枚举、解构与运算符重载；在章专属实验中选择该节点并观察反馈；最后凭实例数量图、生成方法检查、equals合同、copy实验、命令模型和世界地图回答练习、复位并重放。

### copy

copy服务于比较对象声明、对象表达式、伴生对象、嵌套类、数据类、枚举、解构与运算符重载。把这个目录坐标翻译成一个可执行或可判定合同，明确输入、状态变化、可见结果和反例，并以实例数量图、生成方法检查、equals合同、copy实验、命令模型和世界地图完成独立复核。

在 15. Objects 的 记录决策 阶段，copy先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入界定责任，使用同一份输入把正常路径与把全局对象变成隐式可变状态，或重载运算符却违背读者直觉产生的反例并排保存。

对 copy 的四级验收分别是：正文出现该坐标；解释它如何参与比较对象声明、对象表达式、伴生对象、嵌套类、数据类、枚举、解构与运算符重载；在章专属实验中选择该节点并观察反馈；最后凭实例数量图、生成方法检查、equals合同、copy实验、命令模型和世界地图回答练习、复位并重放。

### Destructuring declarations

Destructuring declarations服务于比较对象声明、对象表达式、伴生对象、嵌套类、数据类、枚举、解构与运算符重载。把这个目录坐标翻译成一个可执行或可判定合同，明确输入、状态变化、可见结果和反例，并以实例数量图、生成方法检查、equals合同、copy实验、命令模型和世界地图完成独立复核。

在 15. Objects 的 界定责任 阶段，Destructuring declarations先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入比较方案，使用同一份输入把正常路径与把全局对象变成隐式可变状态，或重载运算符却违背读者直觉产生的反例并排保存。

对 Destructuring declarations 的四级验收分别是：正文出现该坐标；解释它如何参与比较对象声明、对象表达式、伴生对象、嵌套类、数据类、枚举、解构与运算符重载；在章专属实验中选择该节点并观察反馈；最后凭实例数量图、生成方法检查、equals合同、copy实验、命令模型和世界地图回答练习、复位并重放。

### Enumerated Classes

Enumerated Classes服务于比较对象声明、对象表达式、伴生对象、嵌套类、数据类、枚举、解构与运算符重载。画出类型关系、状态拥有者、初始化时刻和允许替换，再用编译失败或替身实现挑战边界；验收时检查实例数量图、生成方法检查、equals合同、copy实验、命令模型和世界地图是否支持对象不变量。

在 15. Objects 的 比较方案 阶段，Enumerated Classes先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入声明不变量，使用同一份输入把正常路径与把全局对象变成隐式可变状态，或重载运算符却违背读者直觉产生的反例并排保存。

对 Enumerated Classes 的四级验收分别是：正文出现该坐标；解释它如何参与比较对象声明、对象表达式、伴生对象、嵌套类、数据类、枚举、解构与运算符重载；在章专属实验中选择该节点并观察反馈；最后凭实例数量图、生成方法检查、equals合同、copy实验、命令模型和世界地图回答练习、复位并重放。

### Operator Overloading

Operator Overloading服务于比较对象声明、对象表达式、伴生对象、嵌套类、数据类、枚举、解构与运算符重载。把这个目录坐标翻译成一个可执行或可判定合同，明确输入、状态变化、可见结果和反例，并以实例数量图、生成方法检查、equals合同、copy实验、命令模型和世界地图完成独立复核。

在 15. Objects 的 声明不变量 阶段，Operator Overloading先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入验证替换，使用同一份输入把正常路径与把全局对象变成隐式可变状态，或重载运算符却违背读者直觉产生的反例并排保存。

对 Operator Overloading 的四级验收分别是：正文出现该坐标；解释它如何参与比较对象声明、对象表达式、伴生对象、嵌套类、数据类、枚举、解构与运算符重载；在章专属实验中选择该节点并观察反馈；最后凭实例数量图、生成方法检查、equals合同、copy实验、命令模型和世界地图回答练习、复位并重放。

### Exploring the World of NyetHack

Exploring the World of NyetHack服务于比较对象声明、对象表达式、伴生对象、嵌套类、数据类、枚举、解构与运算符重载。把这个目录坐标翻译成一个可执行或可判定合同，明确输入、状态变化、可见结果和反例，并以实例数量图、生成方法检查、equals合同、copy实验、命令模型和世界地图完成独立复核。

在 15. Objects 的 验证替换 阶段，Exploring the World of NyetHack先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入记录决策，使用同一份输入把正常路径与把全局对象变成隐式可变状态，或重载运算符却违背读者直觉产生的反例并排保存。

对 Exploring the World of NyetHack 的四级验收分别是：正文出现该坐标；解释它如何参与比较对象声明、对象表达式、伴生对象、嵌套类、数据类、枚举、解构与运算符重载；在章专属实验中选择该节点并观察反馈；最后凭实例数量图、生成方法检查、equals合同、copy实验、命令模型和世界地图回答练习、复位并重放。

### For the More Curious: Defining Structural Comparison

For the More Curious: Defining Structural Comparison服务于比较对象声明、对象表达式、伴生对象、嵌套类、数据类、枚举、解构与运算符重载。把这个目录坐标翻译成一个可执行或可判定合同，明确输入、状态变化、可见结果和反例，并以实例数量图、生成方法检查、equals合同、copy实验、命令模型和世界地图完成独立复核。

在 15. Objects 的 记录决策 阶段，For the More Curious: Defining Structural Comparison先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入界定责任，使用同一份输入把正常路径与把全局对象变成隐式可变状态，或重载运算符却违背读者直觉产生的反例并排保存。

对 For the More Curious: Defining Structural Comparison 的四级验收分别是：正文出现该坐标；解释它如何参与比较对象声明、对象表达式、伴生对象、嵌套类、数据类、枚举、解构与运算符重载；在章专属实验中选择该节点并观察反馈；最后凭实例数量图、生成方法检查、equals合同、copy实验、命令模型和世界地图回答练习、复位并重放。

### For the More Curious: Algebraic Data Types

For the More Curious: Algebraic Data Types服务于比较对象声明、对象表达式、伴生对象、嵌套类、数据类、枚举、解构与运算符重载。画出类型关系、状态拥有者、初始化时刻和允许替换，再用编译失败或替身实现挑战边界；验收时检查实例数量图、生成方法检查、equals合同、copy实验、命令模型和世界地图是否支持对象不变量。

在 15. Objects 的 界定责任 阶段，For the More Curious: Algebraic Data Types先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入比较方案，使用同一份输入把正常路径与把全局对象变成隐式可变状态，或重载运算符却违背读者直觉产生的反例并排保存。

对 For the More Curious: Algebraic Data Types 的四级验收分别是：正文出现该坐标；解释它如何参与比较对象声明、对象表达式、伴生对象、嵌套类、数据类、枚举、解构与运算符重载；在章专属实验中选择该节点并观察反馈；最后凭实例数量图、生成方法检查、equals合同、copy实验、命令模型和世界地图回答练习、复位并重放。

### Challenge: “Quit” Command

Challenge: “Quit” Command服务于比较对象声明、对象表达式、伴生对象、嵌套类、数据类、枚举、解构与运算符重载。先把题目改写成输入、输出、约束和失败样例，再运行正常、边界与非法三组断言；挑战完成的证据是实例数量图、生成方法检查、equals合同、copy实验、命令模型和世界地图，不是一次示例输出。

在 15. Objects 的 比较方案 阶段，Challenge: “Quit” Command先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入声明不变量，使用同一份输入把正常路径与把全局对象变成隐式可变状态，或重载运算符却违背读者直觉产生的反例并排保存。

对 Challenge: “Quit” Command 的四级验收分别是：正文出现该坐标；解释它如何参与比较对象声明、对象表达式、伴生对象、嵌套类、数据类、枚举、解构与运算符重载；在章专属实验中选择该节点并观察反馈；最后凭实例数量图、生成方法检查、equals合同、copy实验、命令模型和世界地图回答练习、复位并重放。

### Challenge: Implementing a World Map

Challenge: Implementing a World Map服务于比较对象声明、对象表达式、伴生对象、嵌套类、数据类、枚举、解构与运算符重载。先把题目改写成输入、输出、约束和失败样例，再运行正常、边界与非法三组断言；挑战完成的证据是实例数量图、生成方法检查、equals合同、copy实验、命令模型和世界地图，不是一次示例输出。

在 15. Objects 的 声明不变量 阶段，Challenge: Implementing a World Map先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入验证替换，使用同一份输入把正常路径与把全局对象变成隐式可变状态，或重载运算符却违背读者直觉产生的反例并排保存。

对 Challenge: Implementing a World Map 的四级验收分别是：正文出现该坐标；解释它如何参与比较对象声明、对象表达式、伴生对象、嵌套类、数据类、枚举、解构与运算符重载；在章专属实验中选择该节点并观察反馈；最后凭实例数量图、生成方法检查、equals合同、copy实验、命令模型和世界地图回答练习、复位并重放。

### Challenge: Ring the Bell

Challenge: Ring the Bell服务于比较对象声明、对象表达式、伴生对象、嵌套类、数据类、枚举、解构与运算符重载。先把题目改写成输入、输出、约束和失败样例，再运行正常、边界与非法三组断言；挑战完成的证据是实例数量图、生成方法检查、equals合同、copy实验、命令模型和世界地图，不是一次示例输出。

在 15. Objects 的 验证替换 阶段，Challenge: Ring the Bell先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入记录决策，使用同一份输入把正常路径与把全局对象变成隐式可变状态，或重载运算符却违背读者直觉产生的反例并排保存。

对 Challenge: Ring the Bell 的四级验收分别是：正文出现该坐标；解释它如何参与比较对象声明、对象表达式、伴生对象、嵌套类、数据类、枚举、解构与运算符重载；在章专属实验中选择该节点并观察反馈；最后凭实例数量图、生成方法检查、equals合同、copy实验、命令模型和世界地图回答练习、复位并重放。

## 最小可执行切片

```kotlin
enum class Direction { NORTH, SOUTH, EAST, WEST }

data class Coordinate(val x: Int, val y: Int) {
    operator fun plus(other: Coordinate) =
        Coordinate(x + other.x, y + other.y)
}

object GameClock { var turn: Int = 0 }
```

运行切片时固定 Kotlin/JDK 或 Android 工具链版本、输入数据和命令；保存标准输出、编译诊断及断言。迁移实验必须另建记录，不能静默改写2018年的历史坐标。


## 练习、答案与四级证据


## 术语复核与本章回顾


完成15. Objects意味着：能解释比较对象声明、对象表达式、伴生对象、嵌套类、数据类、枚举、解构与运算符重载，能运行并改动章专属切片，能主动制造把全局对象变成隐式可变状态，或重载运算符却违背读者直觉，还能凭实例数量图、生成方法检查、equals合同、copy实验、命令模型和世界地图恢复并交接，而不是只识别术语。

## 阅读导航

[← 上一页：14. Inheritance](/learn/kotlin-definitive-guide/05-object-model/kdg1-14-inheritance) · [下一页：16. Interfaces and Abstract Classes →](/learn/kotlin-definitive-guide/05-object-model/kdg1-16-interfaces-abstract-classes)

