---
title: 17. Generics
description: >-
  17.
  Generics：通过泛型类型、函数、多个参数、约束、vararg、get、in/out与reified表达类型安全复用，通过全节点状态链、历史迁移对照和故障恢复完成验收。
type: C
section: Kotlin权威指南 · 对象模型
draft: true
math: false
sourceUrl: 'https://www.oreilly.com/library/view/kotlin-programming-the/9780135165188/'
demo: true
order: 6
qualityVersion: 2
practiceMode: diagnosis
sourceMode: independent-rewrite
---
import { KdgModelLab, KdgFailureLab, KdgEvidenceLab } from "@/components/mdx/kotlin-definitive-guide/diagrams/kdg1-17-generics";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能说明通过泛型类型、函数、多个参数、约束、vararg、get、in/out与reified表达类型安全复用，并画出输入、类型或对象边界与可见结果。
- 能运行本页保留的 Kotlin 切片，解释它在 Kotlin 1.2/JVM 语境中的编译或执行路径。
- 能操作覆盖、合同和恢复三层实验，让全部 8 个正式节点拥有可见教学因果。
- 能触发忽略方差方向，令可写容器暴露不安全的类型替换，凭类型参数角色表、约束失败、生产者消费者实验、运行时类型检查和调用样例完成诊断、修复、复位与同输入重放。

</Objectives>

{/* KOTLIN_DEFINITIVE_GUIDE_QUALITY_V2 */}

## 从本章问题开始

17. Generics要解决的不是语法记忆，而是通过泛型类型、函数、多个参数、约束、vararg、get、in/out与reified表达类型安全复用。先写出输入和期望结果，再运行最小切片；如果无法解释失败落在编译期、边界层还是运行期，程序即使输出一次正确答案也未通过。

本页不把 val 等同深度不可变，也不把简洁语法等同动态类型。Kotlin 的类型、可空性、求值规则和对象模型共同压缩非法状态；本章不变量是通过泛型类型、函数、多个参数、约束、vararg、get、in/out与reified表达类型安全复用的输入、类型边界、求值结果和失败路径都能由独立读者重放。

## 来源范围与年代边界

O’Reilly 的[第1版书目与完整目录](https://www.oreilly.com/library/view/kotlin-programming-the/9780135165188/)只用于核定本页范围，[中文版出版记录](https://e.dangdang.com/touch/products/1901154363.html)用于交叉核对译本信息；未取得的原书正文不作忠实性宣称。17. Generics涉及的语言机制由[JetBrains/Android 一手文档](https://kotlinlang.org/docs/basic-syntax.html)独立核验，以下中文讲解、代码组织、图示、实验和练习均为独立教学重写。

第1版的教学坐标是2018年前后的 Kotlin 1.2、JVM、IntelliJ 与当时 Android/早期协程接口。[Kotlin 1.2 发布说明](https://kotlinlang.org/docs/whatsnew12.html)用于锁定历史能力；当前文档只提供迁移对照，Flow、Compose、MockK、现代 Gradle Kotlin DSL 和第二版新增内容不倒灌为原书内容。

## 本章机制与六个检查点

<Term def="正常基线在本页限定17. Generics允许进入系统的值与前置条件。">正常基线</Term>、<Term def="反例输入在本页标记通过泛型类型、函数、多个参数、约束、vararg、get、in/out与reified表达类型安全复用中状态由谁创建、修改和释放。">反例输入</Term>、<Term def="首个分叉在本页说明Kotlin 1.2实现与当前迁移之间必须保持的语义。">首个分叉</Term>、<Term def="边界隔离在本页保存能推翻忽略方差方向，令可写容器暴露不安全的类型替换的原始观察。">边界隔离</Term>、<Term def="修复策略在本页把错误停在同输入复验而非继续扩散。">修复策略</Term>、<Term def="恢复证据在本页让另一位读者凭类型参数角色表、约束失败、生产者消费者实验、运行时类型检查和调用样例得到相同结论。">恢复证据</Term>。这些检查点共同回答通过泛型类型、函数、多个参数、约束、vararg、get、in/out与reified表达类型安全复用，最终必须落到类型参数角色表、约束失败、生产者消费者实验、运行时类型检查和调用样例，不能只停在API名称或目录词。

## 先预测，再操作三层实验

先预测：选择一个正式节点后哪一阶段会先改变；切换历史基线与当前迁移时哪项语义必须不变；触发忽略方差方向，令可写容器暴露不安全的类型替换后，应由哪份证据定位并恢复。

<Stepper>
  <Step title="1. 全节点覆盖与状态链">
    选择任一正式节点和机制阶段，检查输入、状态拥有者、可见结果与专属证据；节点列表不截断。

    <KdgModelLab />

  </Step>
  <Step title="2. 历史合同与当前迁移">
    在正常、边界和故障输入间切换，只改变保护合同或技术坐标，观察结果为何变化。

    <KdgFailureLab />

  </Step>
  <Step title="3. 故障、恢复与重放">
    比较正常轨迹、首错轨迹和恢复轨迹；最后点击重置，确认选择项与可见输出都回到初始值。

    <KdgEvidenceLab />

  </Step>
</Stepper>

## 正式目录逐项深读

### 17. Generics

17. Generics服务于通过泛型类型、函数、多个参数、约束、vararg、get、in/out与reified表达类型安全复用。画出类型关系、状态拥有者、初始化时刻和允许替换，再用编译失败或替身实现挑战边界；验收时检查类型参数角色表、约束失败、生产者消费者实验、运行时类型检查和调用样例是否支持对象不变量。

在 17. Generics 的 建立基线 阶段，17. Generics先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入注入反例，使用同一份输入把正常路径与忽略方差方向，令可写容器暴露不安全的类型替换产生的反例并排保存。

对 17. Generics 的四级验收分别是：正文出现该坐标；解释它如何参与通过泛型类型、函数、多个参数、约束、vararg、get、in/out与reified表达类型安全复用；在章专属实验中选择该节点并观察反馈；最后凭类型参数角色表、约束失败、生产者消费者实验、运行时类型检查和调用样例回答练习、复位并重放。

### Defining Generic Types

Defining Generic Types服务于通过泛型类型、函数、多个参数、约束、vararg、get、in/out与reified表达类型安全复用。画出类型关系、状态拥有者、初始化时刻和允许替换，再用编译失败或替身实现挑战边界；验收时检查类型参数角色表、约束失败、生产者消费者实验、运行时类型检查和调用样例是否支持对象不变量。

在 17. Generics 的 注入反例 阶段，Defining Generic Types先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入定位边界，使用同一份输入把正常路径与忽略方差方向，令可写容器暴露不安全的类型替换产生的反例并排保存。

对 Defining Generic Types 的四级验收分别是：正文出现该坐标；解释它如何参与通过泛型类型、函数、多个参数、约束、vararg、get、in/out与reified表达类型安全复用；在章专属实验中选择该节点并观察反馈；最后凭类型参数角色表、约束失败、生产者消费者实验、运行时类型检查和调用样例回答练习、复位并重放。

### Generic Functions

Generic Functions服务于通过泛型类型、函数、多个参数、约束、vararg、get、in/out与reified表达类型安全复用。把接收者、参数、返回值、捕获状态和副作用逐项展开，再判断简写是否保持相同求值顺序；类型参数角色表、约束失败、生产者消费者实验、运行时类型检查和调用样例必须能区分语法缩短与合同改变。

在 17. Generics 的 定位边界 阶段，Generic Functions先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入修复合同，使用同一份输入把正常路径与忽略方差方向，令可写容器暴露不安全的类型替换产生的反例并排保存。

对 Generic Functions 的四级验收分别是：正文出现该坐标；解释它如何参与通过泛型类型、函数、多个参数、约束、vararg、get、in/out与reified表达类型安全复用；在章专属实验中选择该节点并观察反馈；最后凭类型参数角色表、约束失败、生产者消费者实验、运行时类型检查和调用样例回答练习、复位并重放。

### Multiple Generic Type Parameters

Multiple Generic Type Parameters服务于通过泛型类型、函数、多个参数、约束、vararg、get、in/out与reified表达类型安全复用。画出类型关系、状态拥有者、初始化时刻和允许替换，再用编译失败或替身实现挑战边界；验收时检查类型参数角色表、约束失败、生产者消费者实验、运行时类型检查和调用样例是否支持对象不变量。

在 17. Generics 的 修复合同 阶段，Multiple Generic Type Parameters先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入同输入复验，使用同一份输入把正常路径与忽略方差方向，令可写容器暴露不安全的类型替换产生的反例并排保存。

对 Multiple Generic Type Parameters 的四级验收分别是：正文出现该坐标；解释它如何参与通过泛型类型、函数、多个参数、约束、vararg、get、in/out与reified表达类型安全复用；在章专属实验中选择该节点并观察反馈；最后凭类型参数角色表、约束失败、生产者消费者实验、运行时类型检查和调用样例回答练习、复位并重放。

### Generic Constraints

Generic Constraints服务于通过泛型类型、函数、多个参数、约束、vararg、get、in/out与reified表达类型安全复用。画出类型关系、状态拥有者、初始化时刻和允许替换，再用编译失败或替身实现挑战边界；验收时检查类型参数角色表、约束失败、生产者消费者实验、运行时类型检查和调用样例是否支持对象不变量。

在 17. Generics 的 同输入复验 阶段，Generic Constraints先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入建立基线，使用同一份输入把正常路径与忽略方差方向，令可写容器暴露不安全的类型替换产生的反例并排保存。

对 Generic Constraints 的四级验收分别是：正文出现该坐标；解释它如何参与通过泛型类型、函数、多个参数、约束、vararg、get、in/out与reified表达类型安全复用；在章专属实验中选择该节点并观察反馈；最后凭类型参数角色表、约束失败、生产者消费者实验、运行时类型检查和调用样例回答练习、复位并重放。

### vararg and get

vararg and get服务于通过泛型类型、函数、多个参数、约束、vararg、get、in/out与reified表达类型安全复用。把这个目录坐标翻译成一个可执行或可判定合同，明确输入、状态变化、可见结果和反例，并以类型参数角色表、约束失败、生产者消费者实验、运行时类型检查和调用样例完成独立复核。

在 17. Generics 的 建立基线 阶段，vararg and get先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入注入反例，使用同一份输入把正常路径与忽略方差方向，令可写容器暴露不安全的类型替换产生的反例并排保存。

对 vararg and get 的四级验收分别是：正文出现该坐标；解释它如何参与通过泛型类型、函数、多个参数、约束、vararg、get、in/out与reified表达类型安全复用；在章专属实验中选择该节点并观察反馈；最后凭类型参数角色表、约束失败、生产者消费者实验、运行时类型检查和调用样例回答练习、复位并重放。

### in and out

in and out服务于通过泛型类型、函数、多个参数、约束、vararg、get、in/out与reified表达类型安全复用。画出类型关系、状态拥有者、初始化时刻和允许替换，再用编译失败或替身实现挑战边界；验收时检查类型参数角色表、约束失败、生产者消费者实验、运行时类型检查和调用样例是否支持对象不变量。

在 17. Generics 的 注入反例 阶段，in and out先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入定位边界，使用同一份输入把正常路径与忽略方差方向，令可写容器暴露不安全的类型替换产生的反例并排保存。

对 in and out 的四级验收分别是：正文出现该坐标；解释它如何参与通过泛型类型、函数、多个参数、约束、vararg、get、in/out与reified表达类型安全复用；在章专属实验中选择该节点并观察反馈；最后凭类型参数角色表、约束失败、生产者消费者实验、运行时类型检查和调用样例回答练习、复位并重放。

### For the More Curious: The reified Keyword

For the More Curious: The reified Keyword服务于通过泛型类型、函数、多个参数、约束、vararg、get、in/out与reified表达类型安全复用。画出类型关系、状态拥有者、初始化时刻和允许替换，再用编译失败或替身实现挑战边界；验收时检查类型参数角色表、约束失败、生产者消费者实验、运行时类型检查和调用样例是否支持对象不变量。

在 17. Generics 的 定位边界 阶段，For the More Curious: The reified Keyword先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入修复合同，使用同一份输入把正常路径与忽略方差方向，令可写容器暴露不安全的类型替换产生的反例并排保存。

对 For the More Curious: The reified Keyword 的四级验收分别是：正文出现该坐标；解释它如何参与通过泛型类型、函数、多个参数、约束、vararg、get、in/out与reified表达类型安全复用；在章专属实验中选择该节点并观察反馈；最后凭类型参数角色表、约束失败、生产者消费者实验、运行时类型检查和调用样例回答练习、复位并重放。

## 最小可执行切片

```kotlin
class LootBox<out T>(private val value: T) {
    fun open(): T = value
}

fun <T : Comparable<T>> maxOfTwo(a: T, b: T): T =
    if (a >= b) a else b

check(maxOfTwo(4, 7) == 7)
```

运行切片时固定 Kotlin/JDK 或 Android 工具链版本、输入数据和命令；保存标准输出、编译诊断及断言。迁移实验必须另建记录，不能静默改写2018年的历史坐标。

<Callout type="trap" title="本章核心陷阱">
  忽略方差方向，令可写容器暴露不安全的类型替换。先缩小到最小反例，明确是类型合同、对象所有权、求值顺序还是平台边界失效，再用同输入验证修复。
</Callout>

<Callout type="trap" title="把当前惯例倒灌进第一版">
  17. Generics保留 Kotlin 1.2/JVM 的历史身份；当前 Kotlin 或 Android 方案只作单独迁移坐标，不能据此改写第一版目录和接口身份。
</Callout>

<Callout type="trap" title="只保留成功输出">
  类型参数角色表、约束失败、生产者消费者实验、运行时类型检查和调用样例必须同时包含正常、边界和失败路径；如果故障后不能恢复并复位，本页实践闭环仍未完成。
</Callout>

## 练习、答案与四级证据

<Exercises>

**问题 1：历史与迁移。** 怎样既保留 Kotlin 1.2 的教学身份，又使用当前官方资料核验机制？

<Answer>
  先以[Kotlin 1.2发布说明](https://kotlinlang.org/docs/whatsnew12.html)和目录锁定历史范围，再用[当前技术文档](https://kotlinlang.org/docs/basic-syntax.html)建立单独迁移记录。比较相同输入、结果和不变量；只改变版本坐标，不把当前API伪装成第一版内容。
</Answer>

**问题 2：逐节点验证。** 本页 8 个正式节点怎样从出现升级到可复查证据？

<Answer>
  - **17. Generics**：在建立基线阶段保存输入与结果，触发忽略方差方向，令可写容器暴露不安全的类型替换后恢复初态，并由类型参数角色表、约束失败、生产者消费者实验、运行时类型检查和调用样例确认语义未漂移。
  - **Defining Generic Types**：在注入反例阶段保存输入与结果，触发忽略方差方向，令可写容器暴露不安全的类型替换后恢复初态，并由类型参数角色表、约束失败、生产者消费者实验、运行时类型检查和调用样例确认语义未漂移。
  - **Generic Functions**：在定位边界阶段保存输入与结果，触发忽略方差方向，令可写容器暴露不安全的类型替换后恢复初态，并由类型参数角色表、约束失败、生产者消费者实验、运行时类型检查和调用样例确认语义未漂移。
  - **Multiple Generic Type Parameters**：在修复合同阶段保存输入与结果，触发忽略方差方向，令可写容器暴露不安全的类型替换后恢复初态，并由类型参数角色表、约束失败、生产者消费者实验、运行时类型检查和调用样例确认语义未漂移。
  - **Generic Constraints**：在同输入复验阶段保存输入与结果，触发忽略方差方向，令可写容器暴露不安全的类型替换后恢复初态，并由类型参数角色表、约束失败、生产者消费者实验、运行时类型检查和调用样例确认语义未漂移。
  - **vararg and get**：在建立基线阶段保存输入与结果，触发忽略方差方向，令可写容器暴露不安全的类型替换后恢复初态，并由类型参数角色表、约束失败、生产者消费者实验、运行时类型检查和调用样例确认语义未漂移。
  - **in and out**：在注入反例阶段保存输入与结果，触发忽略方差方向，令可写容器暴露不安全的类型替换后恢复初态，并由类型参数角色表、约束失败、生产者消费者实验、运行时类型检查和调用样例确认语义未漂移。
  - **For the More Curious: The reified Keyword**：在定位边界阶段保存输入与结果，触发忽略方差方向，令可写容器暴露不安全的类型替换后恢复初态，并由类型参数角色表、约束失败、生产者消费者实验、运行时类型检查和调用样例确认语义未漂移。
</Answer>

**问题 3：反例闭环。** 遇到忽略方差方向，令可写容器暴露不安全的类型替换时，如何判断修复真正成立？

<Answer>
  保存首个失败诊断，恢复显式输入域、类型或对象边界和求值顺序，再以相同工具链和输入重跑正常、边界、非法三组样例。只有类型参数角色表、约束失败、生产者消费者实验、运行时类型检查和调用样例回到基线且通过泛型类型、函数、多个参数、约束、vararg、get、in/out与reified表达类型安全复用的输入、类型边界、求值结果和失败路径都能由独立读者重放重新成立，才能通过。
</Answer>

</Exercises>

## 术语复核与本章回顾

<Glossary>
  <GlossaryItem term="正常基线">
    正常基线在本页限定17. Generics允许进入系统的值与前置条件。
  </GlossaryItem>
  <GlossaryItem term="反例输入">
    反例输入在本页标记通过泛型类型、函数、多个参数、约束、vararg、get、in/out与reified表达类型安全复用中状态由谁创建、修改和释放。
  </GlossaryItem>
  <GlossaryItem term="首个分叉">
    首个分叉在本页说明Kotlin 1.2实现与当前迁移之间必须保持的语义。
  </GlossaryItem>
  <GlossaryItem term="边界隔离">
    边界隔离在本页保存能推翻忽略方差方向，令可写容器暴露不安全的类型替换的原始观察。
  </GlossaryItem>
  <GlossaryItem term="修复策略">
    修复策略在本页把错误停在同输入复验而非继续扩散。
  </GlossaryItem>
  <GlossaryItem term="恢复证据">
    恢复证据在本页让另一位读者凭类型参数角色表、约束失败、生产者消费者实验、运行时类型检查和调用样例得到相同结论。
  </GlossaryItem>
</Glossary>

完成17. Generics意味着：能解释通过泛型类型、函数、多个参数、约束、vararg、get、in/out与reified表达类型安全复用，能运行并改动章专属切片，能主动制造忽略方差方向，令可写容器暴露不安全的类型替换，还能凭类型参数角色表、约束失败、生产者消费者实验、运行时类型检查和调用样例恢复并交接，而不是只识别术语。

## 阅读导航

[← 上一页：16. Interfaces and Abstract Classes](/learn/kotlin-definitive-guide/05-object-model/kdg1-16-interfaces-abstract-classes) · [下一页：18. Extensions →](/learn/kotlin-definitive-guide/05-object-model/kdg1-18-extensions)

<Attribution
  mode="independent-rewrite"
  sourceBasis="outline-only"
  workTitle="Josh Skeen、David Greenhalgh《Kotlin Programming: The Big Nerd Ranch Guide》第1版（Addison-Wesley，2018）"
  adaptedUrl="https://www.oreilly.com/library/view/kotlin-programming-the/9780135165188/"
/>
