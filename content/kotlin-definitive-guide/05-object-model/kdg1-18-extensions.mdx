---
title: 18. Extensions
description: >-
  18.
  Extensions：掌握扩展函数与属性的静态分派、泛型与可空接收者、文件组织、重命名和带接收者函数字面量，通过全节点状态链、历史迁移对照和故障恢复完成验收。
type: C
section: Kotlin权威指南 · 对象模型
draft: true
math: false
sourceUrl: 'https://www.oreilly.com/library/view/kotlin-programming-the/9780135165188/'
demo: true
order: 7
qualityVersion: 2
practiceMode: code
sourceMode: independent-rewrite
---
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能说明掌握扩展函数与属性的静态分派、泛型与可空接收者、文件组织、重命名和带接收者函数字面量，并画出输入、类型或对象边界与可见结果。
- 能运行本页保留的 Kotlin 切片，解释它在 Kotlin 1.2/JVM 语境中的编译或执行路径。
- 能操作覆盖、合同和恢复三层实验，让全部 14 个正式节点拥有可见教学因果。
- 能触发认为扩展真正修改了类或可以覆盖成员的动态分派，凭静态分派实验、可空扩展、导入别名、标准库对照和两个挑战实现完成诊断、修复、复位与同输入重放。

</Objectives>

{/* KOTLIN_DEFINITIVE_GUIDE_QUALITY_V2 */}

## 从本章问题开始

18. Extensions要解决的不是语法记忆，而是掌握扩展函数与属性的静态分派、泛型与可空接收者、文件组织、重命名和带接收者函数字面量。先写出输入和期望结果，再运行最小切片；如果无法解释失败落在编译期、边界层还是运行期，程序即使输出一次正确答案也未通过。

本页不把 val 等同深度不可变，也不把简洁语法等同动态类型。Kotlin 的类型、可空性、求值规则和对象模型共同压缩非法状态；本章不变量是掌握扩展函数与属性的静态分派、泛型与可空接收者、文件组织、重命名和带接收者函数字面量的输入、类型边界、求值结果和失败路径都能由独立读者重放。

## 来源范围与年代边界

O’Reilly 的[第1版书目与完整目录](https://www.oreilly.com/library/view/kotlin-programming-the/9780135165188/)只用于核定本页范围，[中文版出版记录](https://e.dangdang.com/touch/products/1901154363.html)用于交叉核对译本信息；未取得的原书正文不作忠实性宣称。18. Extensions涉及的语言机制由[JetBrains/Android 一手文档](https://kotlinlang.org/docs/basic-syntax.html)独立核验，以下中文讲解、代码组织、图示、实验和练习均为独立教学重写。

第1版的教学坐标是2018年前后的 Kotlin 1.2、JVM、IntelliJ 与当时 Android/早期协程接口。[Kotlin 1.2 发布说明](https://kotlinlang.org/docs/whatsnew12.html)用于锁定历史能力；当前文档只提供迁移对照，Flow、Compose、MockK、现代 Gradle Kotlin DSL 和第二版新增内容不倒灌为原书内容。

## 本章机制与六个检查点

<Term def="静态合同在本页限定18. Extensions允许进入系统的值与前置条件。">静态合同</Term>、<Term def="输入域在本页标记掌握扩展函数与属性的静态分派、泛型与可空接收者、文件组织、重命名和带接收者函数字面量中状态由谁创建、修改和释放。">输入域</Term>、<Term def="求值顺序在本页说明Kotlin 1.2实现与当前迁移之间必须保持的语义。">求值顺序</Term>、<Term def="可观察结果在本页保存能推翻认为扩展真正修改了类或可以覆盖成员的动态分派的原始观察。">可观察结果</Term>、<Term def="失败出口在本页把错误停在断言回归而非继续扩散。">失败出口</Term>、<Term def="回归断言在本页让另一位读者凭静态分派实验、可空扩展、导入别名、标准库对照和两个挑战实现得到相同结论。">回归断言</Term>。这些检查点共同回答掌握扩展函数与属性的静态分派、泛型与可空接收者、文件组织、重命名和带接收者函数字面量，最终必须落到静态分派实验、可空扩展、导入别名、标准库对照和两个挑战实现，不能只停在API名称或目录词。

## 正式目录逐项深读

### 18. Extensions

18. Extensions服务于掌握扩展函数与属性的静态分派、泛型与可空接收者、文件组织、重命名和带接收者函数字面量。把这个目录坐标翻译成一个可执行或可判定合同，明确输入、状态变化、可见结果和反例，并以静态分派实验、可空扩展、导入别名、标准库对照和两个挑战实现完成独立复核。

在 18. Extensions 的 声明合同 阶段，18. Extensions先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入建立输入，使用同一份输入把正常路径与认为扩展真正修改了类或可以覆盖成员的动态分派产生的反例并排保存。

对 18. Extensions 的四级验收分别是：正文出现该坐标；解释它如何参与掌握扩展函数与属性的静态分派、泛型与可空接收者、文件组织、重命名和带接收者函数字面量；在章专属实验中选择该节点并观察反馈；最后凭静态分派实验、可空扩展、导入别名、标准库对照和两个挑战实现回答练习、复位并重放。

### Defining Extension Functions

Defining Extension Functions服务于掌握扩展函数与属性的静态分派、泛型与可空接收者、文件组织、重命名和带接收者函数字面量。把接收者、参数、返回值、捕获状态和副作用逐项展开，再判断简写是否保持相同求值顺序；静态分派实验、可空扩展、导入别名、标准库对照和两个挑战实现必须能区分语法缩短与合同改变。

在 18. Extensions 的 建立输入 阶段，Defining Extension Functions先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入执行转换，使用同一份输入把正常路径与认为扩展真正修改了类或可以覆盖成员的动态分派产生的反例并排保存。

对 Defining Extension Functions 的四级验收分别是：正文出现该坐标；解释它如何参与掌握扩展函数与属性的静态分派、泛型与可空接收者、文件组织、重命名和带接收者函数字面量；在章专属实验中选择该节点并观察反馈；最后凭静态分派实验、可空扩展、导入别名、标准库对照和两个挑战实现回答练习、复位并重放。

### Defining an extension on a superclass

Defining an extension on a superclass服务于掌握扩展函数与属性的静态分派、泛型与可空接收者、文件组织、重命名和带接收者函数字面量。画出类型关系、状态拥有者、初始化时刻和允许替换，再用编译失败或替身实现挑战边界；验收时检查静态分派实验、可空扩展、导入别名、标准库对照和两个挑战实现是否支持对象不变量。

在 18. Extensions 的 执行转换 阶段，Defining an extension on a superclass先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入观察产物，使用同一份输入把正常路径与认为扩展真正修改了类或可以覆盖成员的动态分派产生的反例并排保存。

对 Defining an extension on a superclass 的四级验收分别是：正文出现该坐标；解释它如何参与掌握扩展函数与属性的静态分派、泛型与可空接收者、文件组织、重命名和带接收者函数字面量；在章专属实验中选择该节点并观察反馈；最后凭静态分派实验、可空扩展、导入别名、标准库对照和两个挑战实现回答练习、复位并重放。

### Generic Extension Functions

Generic Extension Functions服务于掌握扩展函数与属性的静态分派、泛型与可空接收者、文件组织、重命名和带接收者函数字面量。把接收者、参数、返回值、捕获状态和副作用逐项展开，再判断简写是否保持相同求值顺序；静态分派实验、可空扩展、导入别名、标准库对照和两个挑战实现必须能区分语法缩短与合同改变。

在 18. Extensions 的 观察产物 阶段，Generic Extension Functions先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入断言回归，使用同一份输入把正常路径与认为扩展真正修改了类或可以覆盖成员的动态分派产生的反例并排保存。

对 Generic Extension Functions 的四级验收分别是：正文出现该坐标；解释它如何参与掌握扩展函数与属性的静态分派、泛型与可空接收者、文件组织、重命名和带接收者函数字面量；在章专属实验中选择该节点并观察反馈；最后凭静态分派实验、可空扩展、导入别名、标准库对照和两个挑战实现回答练习、复位并重放。

### Extension Properties

Extension Properties服务于掌握扩展函数与属性的静态分派、泛型与可空接收者、文件组织、重命名和带接收者函数字面量。画出类型关系、状态拥有者、初始化时刻和允许替换，再用编译失败或替身实现挑战边界；验收时检查静态分派实验、可空扩展、导入别名、标准库对照和两个挑战实现是否支持对象不变量。

在 18. Extensions 的 断言回归 阶段，Extension Properties先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入声明合同，使用同一份输入把正常路径与认为扩展真正修改了类或可以覆盖成员的动态分派产生的反例并排保存。

对 Extension Properties 的四级验收分别是：正文出现该坐标；解释它如何参与掌握扩展函数与属性的静态分派、泛型与可空接收者、文件组织、重命名和带接收者函数字面量；在章专属实验中选择该节点并观察反馈；最后凭静态分派实验、可空扩展、导入别名、标准库对照和两个挑战实现回答练习、复位并重放。

### Extensions on Nullable Types

Extensions on Nullable Types服务于掌握扩展函数与属性的静态分派、泛型与可空接收者、文件组织、重命名和带接收者函数字面量。这里要把缺失值、异常来源和调用者责任分别放进类型或失败合同，禁止用强制解包、空捕获或虚假默认值擦除风险；用静态分派实验、可空扩展、导入别名、标准库对照和两个挑战实现定位最早失效处。

在 18. Extensions 的 声明合同 阶段，Extensions on Nullable Types先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入建立输入，使用同一份输入把正常路径与认为扩展真正修改了类或可以覆盖成员的动态分派产生的反例并排保存。

对 Extensions on Nullable Types 的四级验收分别是：正文出现该坐标；解释它如何参与掌握扩展函数与属性的静态分派、泛型与可空接收者、文件组织、重命名和带接收者函数字面量；在章专属实验中选择该节点并观察反馈；最后凭静态分派实验、可空扩展、导入别名、标准库对照和两个挑战实现回答练习、复位并重放。

### Extensions, Under the Hood

Extensions, Under the Hood服务于掌握扩展函数与属性的静态分派、泛型与可空接收者、文件组织、重命名和带接收者函数字面量。把这个目录坐标翻译成一个可执行或可判定合同，明确输入、状态变化、可见结果和反例，并以静态分派实验、可空扩展、导入别名、标准库对照和两个挑战实现完成独立复核。

在 18. Extensions 的 建立输入 阶段，Extensions, Under the Hood先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入执行转换，使用同一份输入把正常路径与认为扩展真正修改了类或可以覆盖成员的动态分派产生的反例并排保存。

对 Extensions, Under the Hood 的四级验收分别是：正文出现该坐标；解释它如何参与掌握扩展函数与属性的静态分派、泛型与可空接收者、文件组织、重命名和带接收者函数字面量；在章专属实验中选择该节点并观察反馈；最后凭静态分派实验、可空扩展、导入别名、标准库对照和两个挑战实现回答练习、复位并重放。

### Extracting to Extensions

Extracting to Extensions服务于掌握扩展函数与属性的静态分派、泛型与可空接收者、文件组织、重命名和带接收者函数字面量。把这个目录坐标翻译成一个可执行或可判定合同，明确输入、状态变化、可见结果和反例，并以静态分派实验、可空扩展、导入别名、标准库对照和两个挑战实现完成独立复核。

在 18. Extensions 的 执行转换 阶段，Extracting to Extensions先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入观察产物，使用同一份输入把正常路径与认为扩展真正修改了类或可以覆盖成员的动态分派产生的反例并排保存。

对 Extracting to Extensions 的四级验收分别是：正文出现该坐标；解释它如何参与掌握扩展函数与属性的静态分派、泛型与可空接收者、文件组织、重命名和带接收者函数字面量；在章专属实验中选择该节点并观察反馈；最后凭静态分派实验、可空扩展、导入别名、标准库对照和两个挑战实现回答练习、复位并重放。

### Defining an Extensions File

Defining an Extensions File服务于掌握扩展函数与属性的静态分派、泛型与可空接收者、文件组织、重命名和带接收者函数字面量。保存工具链版本、源码、编译命令、产物和退出状态，使IDE按钮之外仍能解释源码如何进入JVM；静态分派实验、可空扩展、导入别名、标准库对照和两个挑战实现承担复现责任。

在 18. Extensions 的 观察产物 阶段，Defining an Extensions File先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入断言回归，使用同一份输入把正常路径与认为扩展真正修改了类或可以覆盖成员的动态分派产生的反例并排保存。

对 Defining an Extensions File 的四级验收分别是：正文出现该坐标；解释它如何参与掌握扩展函数与属性的静态分派、泛型与可空接收者、文件组织、重命名和带接收者函数字面量；在章专属实验中选择该节点并观察反馈；最后凭静态分派实验、可空扩展、导入别名、标准库对照和两个挑战实现回答练习、复位并重放。

### Renaming an Extension

Renaming an Extension服务于掌握扩展函数与属性的静态分派、泛型与可空接收者、文件组织、重命名和带接收者函数字面量。把这个目录坐标翻译成一个可执行或可判定合同，明确输入、状态变化、可见结果和反例，并以静态分派实验、可空扩展、导入别名、标准库对照和两个挑战实现完成独立复核。

在 18. Extensions 的 断言回归 阶段，Renaming an Extension先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入声明合同，使用同一份输入把正常路径与认为扩展真正修改了类或可以覆盖成员的动态分派产生的反例并排保存。

对 Renaming an Extension 的四级验收分别是：正文出现该坐标；解释它如何参与掌握扩展函数与属性的静态分派、泛型与可空接收者、文件组织、重命名和带接收者函数字面量；在章专属实验中选择该节点并观察反馈；最后凭静态分派实验、可空扩展、导入别名、标准库对照和两个挑战实现回答练习、复位并重放。

### Extensions in the Kotlin Standard Library

Extensions in the Kotlin Standard Library服务于掌握扩展函数与属性的静态分派、泛型与可空接收者、文件组织、重命名和带接收者函数字面量。把这个目录坐标翻译成一个可执行或可判定合同，明确输入、状态变化、可见结果和反例，并以静态分派实验、可空扩展、导入别名、标准库对照和两个挑战实现完成独立复核。

在 18. Extensions 的 声明合同 阶段，Extensions in the Kotlin Standard Library先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入建立输入，使用同一份输入把正常路径与认为扩展真正修改了类或可以覆盖成员的动态分派产生的反例并排保存。

对 Extensions in the Kotlin Standard Library 的四级验收分别是：正文出现该坐标；解释它如何参与掌握扩展函数与属性的静态分派、泛型与可空接收者、文件组织、重命名和带接收者函数字面量；在章专属实验中选择该节点并观察反馈；最后凭静态分派实验、可空扩展、导入别名、标准库对照和两个挑战实现回答练习、复位并重放。

### For the More Curious: Function Literals with Receivers

For the More Curious: Function Literals with Receivers服务于掌握扩展函数与属性的静态分派、泛型与可空接收者、文件组织、重命名和带接收者函数字面量。把接收者、参数、返回值、捕获状态和副作用逐项展开，再判断简写是否保持相同求值顺序；静态分派实验、可空扩展、导入别名、标准库对照和两个挑战实现必须能区分语法缩短与合同改变。

在 18. Extensions 的 建立输入 阶段，For the More Curious: Function Literals with Receivers先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入执行转换，使用同一份输入把正常路径与认为扩展真正修改了类或可以覆盖成员的动态分派产生的反例并排保存。

对 For the More Curious: Function Literals with Receivers 的四级验收分别是：正文出现该坐标；解释它如何参与掌握扩展函数与属性的静态分派、泛型与可空接收者、文件组织、重命名和带接收者函数字面量；在章专属实验中选择该节点并观察反馈；最后凭静态分派实验、可空扩展、导入别名、标准库对照和两个挑战实现回答练习、复位并重放。

### Challenge: toDragonSpeak Extension

Challenge: toDragonSpeak Extension服务于掌握扩展函数与属性的静态分派、泛型与可空接收者、文件组织、重命名和带接收者函数字面量。先把题目改写成输入、输出、约束和失败样例，再运行正常、边界与非法三组断言；挑战完成的证据是静态分派实验、可空扩展、导入别名、标准库对照和两个挑战实现，不是一次示例输出。

在 18. Extensions 的 执行转换 阶段，Challenge: toDragonSpeak Extension先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入观察产物，使用同一份输入把正常路径与认为扩展真正修改了类或可以覆盖成员的动态分派产生的反例并排保存。

对 Challenge: toDragonSpeak Extension 的四级验收分别是：正文出现该坐标；解释它如何参与掌握扩展函数与属性的静态分派、泛型与可空接收者、文件组织、重命名和带接收者函数字面量；在章专属实验中选择该节点并观察反馈；最后凭静态分派实验、可空扩展、导入别名、标准库对照和两个挑战实现回答练习、复位并重放。

### Challenge: Frame Extension

Challenge: Frame Extension服务于掌握扩展函数与属性的静态分派、泛型与可空接收者、文件组织、重命名和带接收者函数字面量。先把题目改写成输入、输出、约束和失败样例，再运行正常、边界与非法三组断言；挑战完成的证据是静态分派实验、可空扩展、导入别名、标准库对照和两个挑战实现，不是一次示例输出。

在 18. Extensions 的 观察产物 阶段，Challenge: Frame Extension先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入断言回归，使用同一份输入把正常路径与认为扩展真正修改了类或可以覆盖成员的动态分派产生的反例并排保存。

对 Challenge: Frame Extension 的四级验收分别是：正文出现该坐标；解释它如何参与掌握扩展函数与属性的静态分派、泛型与可空接收者、文件组织、重命名和带接收者函数字面量；在章专属实验中选择该节点并观察反馈；最后凭静态分派实验、可空扩展、导入别名、标准库对照和两个挑战实现回答练习、复位并重放。

## 最小可执行切片

```kotlin
fun String.toDragonSpeak(): String =
    replace("th", "ss", ignoreCase = true)

val String?.orUnknown: String
    get() = this ?: "unknown"

check("the".toDragonSpeak() == "sse")
```

运行切片时固定 Kotlin/JDK 或 Android 工具链版本、输入数据和命令；保存标准输出、编译诊断及断言。迁移实验必须另建记录，不能静默改写2018年的历史坐标。

<Callout type="trap" title="本章核心陷阱">
  认为扩展真正修改了类或可以覆盖成员的动态分派。先缩小到最小反例，明确是类型合同、对象所有权、求值顺序还是平台边界失效，再用同输入验证修复。
</Callout>

<Callout type="trap" title="把当前惯例倒灌进第一版">
  18. Extensions保留 Kotlin 1.2/JVM 的历史身份；当前 Kotlin 或 Android 方案只作单独迁移坐标，不能据此改写第一版目录和接口身份。
</Callout>

<Callout type="trap" title="只保留成功输出">
  静态分派实验、可空扩展、导入别名、标准库对照和两个挑战实现必须同时包含正常、边界和失败路径；如果故障后不能恢复并复位，本页实践闭环仍未完成。
</Callout>

## 练习、答案与四级证据

<Exercises>

**问题 1：历史与迁移。** 怎样既保留 Kotlin 1.2 的教学身份，又使用当前官方资料核验机制？

<Answer>
  先以[Kotlin 1.2发布说明](https://kotlinlang.org/docs/whatsnew12.html)和目录锁定历史范围，再用[当前技术文档](https://kotlinlang.org/docs/basic-syntax.html)建立单独迁移记录。比较相同输入、结果和不变量；只改变版本坐标，不把当前API伪装成第一版内容。
</Answer>

**问题 2：逐节点验证。** 本页 14 个正式节点怎样从出现升级到可复查证据？

<Answer>
  - **18. Extensions**：在声明合同阶段保存输入与结果，触发认为扩展真正修改了类或可以覆盖成员的动态分派后恢复初态，并由静态分派实验、可空扩展、导入别名、标准库对照和两个挑战实现确认语义未漂移。
  - **Defining Extension Functions**：在建立输入阶段保存输入与结果，触发认为扩展真正修改了类或可以覆盖成员的动态分派后恢复初态，并由静态分派实验、可空扩展、导入别名、标准库对照和两个挑战实现确认语义未漂移。
  - **Defining an extension on a superclass**：在执行转换阶段保存输入与结果，触发认为扩展真正修改了类或可以覆盖成员的动态分派后恢复初态，并由静态分派实验、可空扩展、导入别名、标准库对照和两个挑战实现确认语义未漂移。
  - **Generic Extension Functions**：在观察产物阶段保存输入与结果，触发认为扩展真正修改了类或可以覆盖成员的动态分派后恢复初态，并由静态分派实验、可空扩展、导入别名、标准库对照和两个挑战实现确认语义未漂移。
  - **Extension Properties**：在断言回归阶段保存输入与结果，触发认为扩展真正修改了类或可以覆盖成员的动态分派后恢复初态，并由静态分派实验、可空扩展、导入别名、标准库对照和两个挑战实现确认语义未漂移。
  - **Extensions on Nullable Types**：在声明合同阶段保存输入与结果，触发认为扩展真正修改了类或可以覆盖成员的动态分派后恢复初态，并由静态分派实验、可空扩展、导入别名、标准库对照和两个挑战实现确认语义未漂移。
  - **Extensions, Under the Hood**：在建立输入阶段保存输入与结果，触发认为扩展真正修改了类或可以覆盖成员的动态分派后恢复初态，并由静态分派实验、可空扩展、导入别名、标准库对照和两个挑战实现确认语义未漂移。
  - **Extracting to Extensions**：在执行转换阶段保存输入与结果，触发认为扩展真正修改了类或可以覆盖成员的动态分派后恢复初态，并由静态分派实验、可空扩展、导入别名、标准库对照和两个挑战实现确认语义未漂移。
  - **Defining an Extensions File**：在观察产物阶段保存输入与结果，触发认为扩展真正修改了类或可以覆盖成员的动态分派后恢复初态，并由静态分派实验、可空扩展、导入别名、标准库对照和两个挑战实现确认语义未漂移。
  - **Renaming an Extension**：在断言回归阶段保存输入与结果，触发认为扩展真正修改了类或可以覆盖成员的动态分派后恢复初态，并由静态分派实验、可空扩展、导入别名、标准库对照和两个挑战实现确认语义未漂移。
  - **Extensions in the Kotlin Standard Library**：在声明合同阶段保存输入与结果，触发认为扩展真正修改了类或可以覆盖成员的动态分派后恢复初态，并由静态分派实验、可空扩展、导入别名、标准库对照和两个挑战实现确认语义未漂移。
  - **For the More Curious: Function Literals with Receivers**：在建立输入阶段保存输入与结果，触发认为扩展真正修改了类或可以覆盖成员的动态分派后恢复初态，并由静态分派实验、可空扩展、导入别名、标准库对照和两个挑战实现确认语义未漂移。
  - **Challenge: toDragonSpeak Extension**：在执行转换阶段保存输入与结果，触发认为扩展真正修改了类或可以覆盖成员的动态分派后恢复初态，并由静态分派实验、可空扩展、导入别名、标准库对照和两个挑战实现确认语义未漂移。
  - **Challenge: Frame Extension**：在观察产物阶段保存输入与结果，触发认为扩展真正修改了类或可以覆盖成员的动态分派后恢复初态，并由静态分派实验、可空扩展、导入别名、标准库对照和两个挑战实现确认语义未漂移。
</Answer>

**问题 3：反例闭环。** 遇到认为扩展真正修改了类或可以覆盖成员的动态分派时，如何判断修复真正成立？

<Answer>
  保存首个失败诊断，恢复显式输入域、类型或对象边界和求值顺序，再以相同工具链和输入重跑正常、边界、非法三组样例。只有静态分派实验、可空扩展、导入别名、标准库对照和两个挑战实现回到基线且掌握扩展函数与属性的静态分派、泛型与可空接收者、文件组织、重命名和带接收者函数字面量的输入、类型边界、求值结果和失败路径都能由独立读者重放重新成立，才能通过。
</Answer>

</Exercises>

## 术语复核与本章回顾

<Glossary>
  <GlossaryItem term="静态合同">
    静态合同在本页限定18. Extensions允许进入系统的值与前置条件。
  </GlossaryItem>
  <GlossaryItem term="输入域">
    输入域在本页标记掌握扩展函数与属性的静态分派、泛型与可空接收者、文件组织、重命名和带接收者函数字面量中状态由谁创建、修改和释放。
  </GlossaryItem>
  <GlossaryItem term="求值顺序">
    求值顺序在本页说明Kotlin 1.2实现与当前迁移之间必须保持的语义。
  </GlossaryItem>
  <GlossaryItem term="可观察结果">
    可观察结果在本页保存能推翻认为扩展真正修改了类或可以覆盖成员的动态分派的原始观察。
  </GlossaryItem>
  <GlossaryItem term="失败出口">
    失败出口在本页把错误停在断言回归而非继续扩散。
  </GlossaryItem>
  <GlossaryItem term="回归断言">
    回归断言在本页让另一位读者凭静态分派实验、可空扩展、导入别名、标准库对照和两个挑战实现得到相同结论。
  </GlossaryItem>
</Glossary>

完成18. Extensions意味着：能解释掌握扩展函数与属性的静态分派、泛型与可空接收者、文件组织、重命名和带接收者函数字面量，能运行并改动章专属切片，能主动制造认为扩展真正修改了类或可以覆盖成员的动态分派，还能凭静态分派实验、可空扩展、导入别名、标准库对照和两个挑战实现恢复并交接，而不是只识别术语。

## 阅读导航

[← 上一页：17. Generics](/learn/kotlin-definitive-guide/05-object-model/kdg1-17-generics) · [下一页：19. Functional Programming Basics →](/learn/kotlin-definitive-guide/06-integration/kdg1-19-functional-programming)

<Attribution
  mode="independent-rewrite"
  sourceBasis="outline-only"
  workTitle="Josh Skeen、David Greenhalgh《Kotlin Programming: The Big Nerd Ranch Guide》第1版（Addison-Wesley，2018）"
  adaptedUrl="https://www.oreilly.com/library/view/kotlin-programming-the/9780135165188/"
/>
