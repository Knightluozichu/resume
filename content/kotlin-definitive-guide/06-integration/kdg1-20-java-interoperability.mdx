---
title: 20. Java Interoperability
description: >-
  20. Java
  Interoperability：控制Java类、平台类型、类型映射、访问器、顶层声明、异常与函数类型的互操作边界，通过全节点状态链、历史迁移对照和故障恢复完成验收。
type: C
section: Kotlin权威指南 · 互操作与协程
draft: false
math: false
sourceUrl: 'https://www.oreilly.com/library/view/kotlin-programming-the/9780135165188/'
demo: true
order: 2
qualityVersion: 2
practiceMode: diagnosis
sourceMode: independent-rewrite
---


{/* KOTLIN_DEFINITIVE_GUIDE_QUALITY_V2 */}

## 从本章问题开始

20. Java Interoperability要解决的不是语法记忆，而是控制Java类、平台类型、类型映射、访问器、顶层声明、异常与函数类型的互操作边界。先写出输入和期望结果，再运行最小切片；如果无法解释失败落在编译期、边界层还是运行期，程序即使输出一次正确答案也未通过。

本页不把 val 等同深度不可变，也不把简洁语法等同动态类型。Kotlin 的类型、可空性、求值规则和对象模型共同压缩非法状态；本章不变量是控制Java类、平台类型、类型映射、访问器、顶层声明、异常与函数类型的互操作边界的输入、类型边界、求值结果和失败路径都能由独立读者重放。

## 来源范围与年代边界

O’Reilly 的[第1版书目与完整目录](https://www.oreilly.com/library/view/kotlin-programming-the/9780135165188/)只用于核定本页范围，[中文版出版记录](https://e.dangdang.com/touch/products/1901154363.html)用于交叉核对译本信息；未取得的原书正文不作忠实性宣称。20. Java Interoperability涉及的语言机制由[JetBrains/Android 一手文档](https://kotlinlang.org/docs/java-interop.html)独立核验，以下中文讲解、代码组织、图示、实验和练习均为独立教学重写。

第1版的教学坐标是2018年前后的 Kotlin 1.2、JVM、IntelliJ 与当时 Android/早期协程接口。[Kotlin 1.2 发布说明](https://kotlinlang.org/docs/whatsnew12.html)用于锁定历史能力；当前文档只提供迁移对照，Flow、Compose、MockK、现代 Gradle Kotlin DSL 和第二版新增内容不倒灌为原书内容。

## 本章机制与六个检查点

、、、、、。这些检查点共同回答控制Java类、平台类型、类型映射、访问器、顶层声明、异常与函数类型的互操作边界，最终必须落到Java/Kotlin签名对照、平台类型隔离、异常合同、生成字节码和跨语言测试，不能只停在API名称或目录词。

## 正式目录逐项深读

### 20. Java Interoperability

20. Java Interoperability服务于控制Java类、平台类型、类型映射、访问器、顶层声明、异常与函数类型的互操作边界。把这个目录坐标翻译成一个可执行或可判定合同，明确输入、状态变化、可见结果和反例，并以Java/Kotlin签名对照、平台类型隔离、异常合同、生成字节码和跨语言测试完成独立复核。

在 20. Java Interoperability 的 建立基线 阶段，20. Java Interoperability先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入注入反例，使用同一份输入把正常路径与让平台类型扩散到业务层，令空值风险失去编译期保护产生的反例并排保存。

对 20. Java Interoperability 的四级验收分别是：正文出现该坐标；解释它如何参与控制Java类、平台类型、类型映射、访问器、顶层声明、异常与函数类型的互操作边界；在章专属实验中选择该节点并观察反馈；最后凭Java/Kotlin签名对照、平台类型隔离、异常合同、生成字节码和跨语言测试回答练习、复位并重放。

### Interoperating with a Java Class

Interoperating with a Java Class服务于控制Java类、平台类型、类型映射、访问器、顶层声明、异常与函数类型的互操作边界。把接收者、参数、返回值、捕获状态和副作用逐项展开，再判断简写是否保持相同求值顺序；Java/Kotlin签名对照、平台类型隔离、异常合同、生成字节码和跨语言测试必须能区分语法缩短与合同改变。

在 20. Java Interoperability 的 注入反例 阶段，Interoperating with a Java Class先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入定位边界，使用同一份输入把正常路径与让平台类型扩散到业务层，令空值风险失去编译期保护产生的反例并排保存。

对 Interoperating with a Java Class 的四级验收分别是：正文出现该坐标；解释它如何参与控制Java类、平台类型、类型映射、访问器、顶层声明、异常与函数类型的互操作边界；在章专属实验中选择该节点并观察反馈；最后凭Java/Kotlin签名对照、平台类型隔离、异常合同、生成字节码和跨语言测试回答练习、复位并重放。

### Interoperability and Nullity

Interoperability and Nullity服务于控制Java类、平台类型、类型映射、访问器、顶层声明、异常与函数类型的互操作边界。这里要把缺失值、异常来源和调用者责任分别放进类型或失败合同，禁止用强制解包、空捕获或虚假默认值擦除风险；用Java/Kotlin签名对照、平台类型隔离、异常合同、生成字节码和跨语言测试定位最早失效处。

在 20. Java Interoperability 的 定位边界 阶段，Interoperability and Nullity先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入修复合同，使用同一份输入把正常路径与让平台类型扩散到业务层，令空值风险失去编译期保护产生的反例并排保存。

对 Interoperability and Nullity 的四级验收分别是：正文出现该坐标；解释它如何参与控制Java类、平台类型、类型映射、访问器、顶层声明、异常与函数类型的互操作边界；在章专属实验中选择该节点并观察反馈；最后凭Java/Kotlin签名对照、平台类型隔离、异常合同、生成字节码和跨语言测试回答练习、复位并重放。

### Type Mapping

Type Mapping服务于控制Java类、平台类型、类型映射、访问器、顶层声明、异常与函数类型的互操作边界。画出类型关系、状态拥有者、初始化时刻和允许替换，再用编译失败或替身实现挑战边界；验收时检查Java/Kotlin签名对照、平台类型隔离、异常合同、生成字节码和跨语言测试是否支持对象不变量。

在 20. Java Interoperability 的 修复合同 阶段，Type Mapping先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入同输入复验，使用同一份输入把正常路径与让平台类型扩散到业务层，令空值风险失去编译期保护产生的反例并排保存。

对 Type Mapping 的四级验收分别是：正文出现该坐标；解释它如何参与控制Java类、平台类型、类型映射、访问器、顶层声明、异常与函数类型的互操作边界；在章专属实验中选择该节点并观察反馈；最后凭Java/Kotlin签名对照、平台类型隔离、异常合同、生成字节码和跨语言测试回答练习、复位并重放。

### Getters, Setters, and Interoperability

Getters, Setters, and Interoperability服务于控制Java类、平台类型、类型映射、访问器、顶层声明、异常与函数类型的互操作边界。固定集合或数值样本，记录索引、类型、转换和求值次数，仅改变一个边界条件；由Java/Kotlin签名对照、平台类型隔离、异常合同、生成字节码和跨语言测试判断只读、不可变、精度或惰性结论是否成立。

在 20. Java Interoperability 的 同输入复验 阶段，Getters, Setters, and Interoperability先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入建立基线，使用同一份输入把正常路径与让平台类型扩散到业务层，令空值风险失去编译期保护产生的反例并排保存。

对 Getters, Setters, and Interoperability 的四级验收分别是：正文出现该坐标；解释它如何参与控制Java类、平台类型、类型映射、访问器、顶层声明、异常与函数类型的互操作边界；在章专属实验中选择该节点并观察反馈；最后凭Java/Kotlin签名对照、平台类型隔离、异常合同、生成字节码和跨语言测试回答练习、复位并重放。

### Beyond Classes

Beyond Classes服务于控制Java类、平台类型、类型映射、访问器、顶层声明、异常与函数类型的互操作边界。画出类型关系、状态拥有者、初始化时刻和允许替换，再用编译失败或替身实现挑战边界；验收时检查Java/Kotlin签名对照、平台类型隔离、异常合同、生成字节码和跨语言测试是否支持对象不变量。

在 20. Java Interoperability 的 建立基线 阶段，Beyond Classes先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入注入反例，使用同一份输入把正常路径与让平台类型扩散到业务层，令空值风险失去编译期保护产生的反例并排保存。

对 Beyond Classes 的四级验收分别是：正文出现该坐标；解释它如何参与控制Java类、平台类型、类型映射、访问器、顶层声明、异常与函数类型的互操作边界；在章专属实验中选择该节点并观察反馈；最后凭Java/Kotlin签名对照、平台类型隔离、异常合同、生成字节码和跨语言测试回答练习、复位并重放。

### Exceptions and Interoperability

Exceptions and Interoperability服务于控制Java类、平台类型、类型映射、访问器、顶层声明、异常与函数类型的互操作边界。这里要把缺失值、异常来源和调用者责任分别放进类型或失败合同，禁止用强制解包、空捕获或虚假默认值擦除风险；用Java/Kotlin签名对照、平台类型隔离、异常合同、生成字节码和跨语言测试定位最早失效处。

在 20. Java Interoperability 的 注入反例 阶段，Exceptions and Interoperability先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入定位边界，使用同一份输入把正常路径与让平台类型扩散到业务层，令空值风险失去编译期保护产生的反例并排保存。

对 Exceptions and Interoperability 的四级验收分别是：正文出现该坐标；解释它如何参与控制Java类、平台类型、类型映射、访问器、顶层声明、异常与函数类型的互操作边界；在章专属实验中选择该节点并观察反馈；最后凭Java/Kotlin签名对照、平台类型隔离、异常合同、生成字节码和跨语言测试回答练习、复位并重放。

### Function Types in Java

Function Types in Java服务于控制Java类、平台类型、类型映射、访问器、顶层声明、异常与函数类型的互操作边界。把接收者、参数、返回值、捕获状态和副作用逐项展开，再判断简写是否保持相同求值顺序；Java/Kotlin签名对照、平台类型隔离、异常合同、生成字节码和跨语言测试必须能区分语法缩短与合同改变。

在 20. Java Interoperability 的 定位边界 阶段，Function Types in Java先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入修复合同，使用同一份输入把正常路径与让平台类型扩散到业务层，令空值风险失去编译期保护产生的反例并排保存。

对 Function Types in Java 的四级验收分别是：正文出现该坐标；解释它如何参与控制Java类、平台类型、类型映射、访问器、顶层声明、异常与函数类型的互操作边界；在章专属实验中选择该节点并观察反馈；最后凭Java/Kotlin签名对照、平台类型隔离、异常合同、生成字节码和跨语言测试回答练习、复位并重放。

## 最小可执行切片

```kotlin
fun safeJavaName(source: JavaPlayer): String {
    val platformName = source.name
    return platformName?.trim() ?: "unknown"
}

@Throws(IllegalArgumentException::class)
fun parseLevel(raw: String): Int = raw.toInt()
```

运行切片时固定 Kotlin/JDK 或 Android 工具链版本、输入数据和命令；保存标准输出、编译诊断及断言。迁移实验必须另建记录，不能静默改写2018年的历史坐标。


## 练习、答案与四级证据


## 术语复核与本章回顾


完成20. Java Interoperability意味着：能解释控制Java类、平台类型、类型映射、访问器、顶层声明、异常与函数类型的互操作边界，能运行并改动章专属切片，能主动制造让平台类型扩散到业务层，令空值风险失去编译期保护，还能凭Java/Kotlin签名对照、平台类型隔离、异常合同、生成字节码和跨语言测试恢复并交接，而不是只识别术语。

## 阅读导航

[← 上一页：19. Functional Programming Basics](/learn/kotlin-definitive-guide/06-integration/kdg1-19-functional-programming) · [下一页：21. Building Your First Android Application with Kotlin →](/learn/kotlin-definitive-guide/06-integration/kdg1-21-first-android-application)

