---
title: 22. Introduction to Coroutines
description: >-
  22. Introduction to Coroutines：在Kotlin
  1.2早期协程语境下理解主线程、async、launch、await与挂起函数，并隔离网络失败，通过全节点状态链、历史迁移对照和故障恢复完成验收。
type: C
section: Kotlin权威指南 · 互操作与协程
draft: true
math: false
sourceUrl: 'https://www.oreilly.com/library/view/kotlin-programming-the/9780135165188/'
demo: true
order: 4
qualityVersion: 2
practiceMode: simulation
sourceMode: independent-rewrite
---
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能说明在Kotlin 1.2早期协程语境下理解主线程、async、launch、await与挂起函数，并隔离网络失败，并画出输入、类型或对象边界与可见结果。
- 能运行本页保留的 Kotlin 切片，解释它在 Kotlin 1.2/JVM 语境中的编译或执行路径。
- 能操作覆盖、合同和恢复三层实验，让全部 10 个正式节点拥有可见教学因果。
- 能触发把协程等同后台线程，或启动无所有者的任务后更新已销毁界面，凭调度时间线、主线程断言、取消记录、异常路径、动态数据测试和版本迁移说明完成诊断、修复、复位与同输入重放。

</Objectives>

{/* KOTLIN_DEFINITIVE_GUIDE_QUALITY_V2 */}

## 从本章问题开始

22. Introduction to Coroutines要解决的不是语法记忆，而是在Kotlin 1.2早期协程语境下理解主线程、async、launch、await与挂起函数，并隔离网络失败。先写出输入和期望结果，再运行最小切片；如果无法解释失败落在编译期、边界层还是运行期，程序即使输出一次正确答案也未通过。

本页不把 val 等同深度不可变，也不把简洁语法等同动态类型。Kotlin 的类型、可空性、求值规则和对象模型共同压缩非法状态；本章不变量是在Kotlin 1.2早期协程语境下理解主线程、async、launch、await与挂起函数，并隔离网络失败的输入、类型边界、求值结果和失败路径都能由独立读者重放。

## 来源范围与年代边界

O’Reilly 的[第1版书目与完整目录](https://www.oreilly.com/library/view/kotlin-programming-the/9780135165188/)只用于核定本页范围，[中文版出版记录](https://e.dangdang.com/touch/products/1901154363.html)用于交叉核对译本信息；未取得的原书正文不作忠实性宣称。22. Introduction to Coroutines涉及的语言机制由[JetBrains/Android 一手文档](https://kotlinlang.org/docs/coroutines-guide.html)独立核验，以下中文讲解、代码组织、图示、实验和练习均为独立教学重写。

第1版的教学坐标是2018年前后的 Kotlin 1.2、JVM、IntelliJ 与当时 Android/早期协程接口。[Kotlin 1.2 发布说明](https://kotlinlang.org/docs/whatsnew12.html)用于锁定历史能力；当前文档只提供迁移对照，Flow、Compose、MockK、现代 Gradle Kotlin DSL 和第二版新增内容不倒灌为原书内容。

## 本章机制与六个检查点

<Term def="初始状态在本页限定22. Introduction to Coroutines允许进入系统的值与前置条件。">初始状态</Term>、<Term def="单一变量在本页标记在Kotlin 1.2早期协程语境下理解主线程、async、launch、await与挂起函数，并隔离网络失败中状态由谁创建、修改和释放。">单一变量</Term>、<Term def="状态迁移在本页说明Kotlin 1.2实现与当前迁移之间必须保持的语义。">状态迁移</Term>、<Term def="可见反馈在本页保存能推翻把协程等同后台线程，或启动无所有者的任务后更新已销毁界面的原始观察。">可见反馈</Term>、<Term def="故障注入在本页把错误停在复位重放而非继续扩散。">故障注入</Term>、<Term def="复位重放在本页让另一位读者凭调度时间线、主线程断言、取消记录、异常路径、动态数据测试和版本迁移说明得到相同结论。">复位重放</Term>。这些检查点共同回答在Kotlin 1.2早期协程语境下理解主线程、async、launch、await与挂起函数，并隔离网络失败，最终必须落到调度时间线、主线程断言、取消记录、异常路径、动态数据测试和版本迁移说明，不能只停在API名称或目录词。

## 正式目录逐项深读

### 22. Introduction to Coroutines

22. Introduction to Coroutines服务于在Kotlin 1.2早期协程语境下理解主线程、async、launch、await与挂起函数，并隔离网络失败。将生命周期、线程或任务所有者和取消出口放在同一时间线上，区分2018年的接口身份与当前迁移方案；用调度时间线、主线程断言、取消记录、异常路径、动态数据测试和版本迁移说明验证状态恢复和失败隔离。

在 22. Introduction to Coroutines 的 固定初态 阶段，22. Introduction to Coroutines先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入施加动作，使用同一份输入把正常路径与把协程等同后台线程，或启动无所有者的任务后更新已销毁界面产生的反例并排保存。

对 22. Introduction to Coroutines 的四级验收分别是：正文出现该坐标；解释它如何参与在Kotlin 1.2早期协程语境下理解主线程、async、launch、await与挂起函数，并隔离网络失败；在章专属实验中选择该节点并观察反馈；最后凭调度时间线、主线程断言、取消记录、异常路径、动态数据测试和版本迁移说明回答练习、复位并重放。

### Parsing Character Data

Parsing Character Data服务于在Kotlin 1.2早期协程语境下理解主线程、async、launch、await与挂起函数，并隔离网络失败。把这个目录坐标翻译成一个可执行或可判定合同，明确输入、状态变化、可见结果和反例，并以调度时间线、主线程断言、取消记录、异常路径、动态数据测试和版本迁移说明完成独立复核。

在 22. Introduction to Coroutines 的 施加动作 阶段，Parsing Character Data先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入推进状态，使用同一份输入把正常路径与把协程等同后台线程，或启动无所有者的任务后更新已销毁界面产生的反例并排保存。

对 Parsing Character Data 的四级验收分别是：正文出现该坐标；解释它如何参与在Kotlin 1.2早期协程语境下理解主线程、async、launch、await与挂起函数，并隔离网络失败；在章专属实验中选择该节点并观察反馈；最后凭调度时间线、主线程断言、取消记录、异常路径、动态数据测试和版本迁移说明回答练习、复位并重放。

### Fetching Live Data

Fetching Live Data服务于在Kotlin 1.2早期协程语境下理解主线程、async、launch、await与挂起函数，并隔离网络失败。将生命周期、线程或任务所有者和取消出口放在同一时间线上，区分2018年的接口身份与当前迁移方案；用调度时间线、主线程断言、取消记录、异常路径、动态数据测试和版本迁移说明验证状态恢复和失败隔离。

在 22. Introduction to Coroutines 的 推进状态 阶段，Fetching Live Data先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入观察差异，使用同一份输入把正常路径与把协程等同后台线程，或启动无所有者的任务后更新已销毁界面产生的反例并排保存。

对 Fetching Live Data 的四级验收分别是：正文出现该坐标；解释它如何参与在Kotlin 1.2早期协程语境下理解主线程、async、launch、await与挂起函数，并隔离网络失败；在章专属实验中选择该节点并观察反馈；最后凭调度时间线、主线程断言、取消记录、异常路径、动态数据测试和版本迁移说明回答练习、复位并重放。

### The Android Main Thread

The Android Main Thread服务于在Kotlin 1.2早期协程语境下理解主线程、async、launch、await与挂起函数，并隔离网络失败。将生命周期、线程或任务所有者和取消出口放在同一时间线上，区分2018年的接口身份与当前迁移方案；用调度时间线、主线程断言、取消记录、异常路径、动态数据测试和版本迁移说明验证状态恢复和失败隔离。

在 22. Introduction to Coroutines 的 观察差异 阶段，The Android Main Thread先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入复位重放，使用同一份输入把正常路径与把协程等同后台线程，或启动无所有者的任务后更新已销毁界面产生的反例并排保存。

对 The Android Main Thread 的四级验收分别是：正文出现该坐标；解释它如何参与在Kotlin 1.2早期协程语境下理解主线程、async、launch、await与挂起函数，并隔离网络失败；在章专属实验中选择该节点并观察反馈；最后凭调度时间线、主线程断言、取消记录、异常路径、动态数据测试和版本迁移说明回答练习、复位并重放。

### Enabling Coroutines

Enabling Coroutines服务于在Kotlin 1.2早期协程语境下理解主线程、async、launch、await与挂起函数，并隔离网络失败。将生命周期、线程或任务所有者和取消出口放在同一时间线上，区分2018年的接口身份与当前迁移方案；用调度时间线、主线程断言、取消记录、异常路径、动态数据测试和版本迁移说明验证状态恢复和失败隔离。

在 22. Introduction to Coroutines 的 复位重放 阶段，Enabling Coroutines先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入固定初态，使用同一份输入把正常路径与把协程等同后台线程，或启动无所有者的任务后更新已销毁界面产生的反例并排保存。

对 Enabling Coroutines 的四级验收分别是：正文出现该坐标；解释它如何参与在Kotlin 1.2早期协程语境下理解主线程、async、launch、await与挂起函数，并隔离网络失败；在章专属实验中选择该节点并观察反馈；最后凭调度时间线、主线程断言、取消记录、异常路径、动态数据测试和版本迁移说明回答练习、复位并重放。

### Specifying a Coroutine with async

Specifying a Coroutine with async服务于在Kotlin 1.2早期协程语境下理解主线程、async、launch、await与挂起函数，并隔离网络失败。把接收者、参数、返回值、捕获状态和副作用逐项展开，再判断简写是否保持相同求值顺序；调度时间线、主线程断言、取消记录、异常路径、动态数据测试和版本迁移说明必须能区分语法缩短与合同改变。

在 22. Introduction to Coroutines 的 固定初态 阶段，Specifying a Coroutine with async先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入施加动作，使用同一份输入把正常路径与把协程等同后台线程，或启动无所有者的任务后更新已销毁界面产生的反例并排保存。

对 Specifying a Coroutine with async 的四级验收分别是：正文出现该坐标；解释它如何参与在Kotlin 1.2早期协程语境下理解主线程、async、launch、await与挂起函数，并隔离网络失败；在章专属实验中选择该节点并观察反馈；最后凭调度时间线、主线程断言、取消记录、异常路径、动态数据测试和版本迁移说明回答练习、复位并重放。

### launch vs async/await

launch vs async/await服务于在Kotlin 1.2早期协程语境下理解主线程、async、launch、await与挂起函数，并隔离网络失败。将生命周期、线程或任务所有者和取消出口放在同一时间线上，区分2018年的接口身份与当前迁移方案；用调度时间线、主线程断言、取消记录、异常路径、动态数据测试和版本迁移说明验证状态恢复和失败隔离。

在 22. Introduction to Coroutines 的 施加动作 阶段，launch vs async/await先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入推进状态，使用同一份输入把正常路径与把协程等同后台线程，或启动无所有者的任务后更新已销毁界面产生的反例并排保存。

对 launch vs async/await 的四级验收分别是：正文出现该坐标；解释它如何参与在Kotlin 1.2早期协程语境下理解主线程、async、launch、await与挂起函数，并隔离网络失败；在章专属实验中选择该节点并观察反馈；最后凭调度时间线、主线程断言、取消记录、异常路径、动态数据测试和版本迁移说明回答练习、复位并重放。

### Suspending Functions

Suspending Functions服务于在Kotlin 1.2早期协程语境下理解主线程、async、launch、await与挂起函数，并隔离网络失败。把接收者、参数、返回值、捕获状态和副作用逐项展开，再判断简写是否保持相同求值顺序；调度时间线、主线程断言、取消记录、异常路径、动态数据测试和版本迁移说明必须能区分语法缩短与合同改变。

在 22. Introduction to Coroutines 的 推进状态 阶段，Suspending Functions先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入观察差异，使用同一份输入把正常路径与把协程等同后台线程，或启动无所有者的任务后更新已销毁界面产生的反例并排保存。

对 Suspending Functions 的四级验收分别是：正文出现该坐标；解释它如何参与在Kotlin 1.2早期协程语境下理解主线程、async、launch、await与挂起函数，并隔离网络失败；在章专属实验中选择该节点并观察反馈；最后凭调度时间线、主线程断言、取消记录、异常路径、动态数据测试和版本迁移说明回答练习、复位并重放。

### Challenge: Live Data

Challenge: Live Data服务于在Kotlin 1.2早期协程语境下理解主线程、async、launch、await与挂起函数，并隔离网络失败。先把题目改写成输入、输出、约束和失败样例，再运行正常、边界与非法三组断言；挑战完成的证据是调度时间线、主线程断言、取消记录、异常路径、动态数据测试和版本迁移说明，不是一次示例输出。

在 22. Introduction to Coroutines 的 观察差异 阶段，Challenge: Live Data先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入复位重放，使用同一份输入把正常路径与把协程等同后台线程，或启动无所有者的任务后更新已销毁界面产生的反例并排保存。

对 Challenge: Live Data 的四级验收分别是：正文出现该坐标；解释它如何参与在Kotlin 1.2早期协程语境下理解主线程、async、launch、await与挂起函数，并隔离网络失败；在章专属实验中选择该节点并观察反馈；最后凭调度时间线、主线程断言、取消记录、异常路径、动态数据测试和版本迁移说明回答练习、复位并重放。

### Challenge: Minimum Strength

Challenge: Minimum Strength服务于在Kotlin 1.2早期协程语境下理解主线程、async、launch、await与挂起函数，并隔离网络失败。先把题目改写成输入、输出、约束和失败样例，再运行正常、边界与非法三组断言；挑战完成的证据是调度时间线、主线程断言、取消记录、异常路径、动态数据测试和版本迁移说明，不是一次示例输出。

在 22. Introduction to Coroutines 的 复位重放 阶段，Challenge: Minimum Strength先声明可接受输入和状态拥有者，再说明为何得到当前结果。随后进入固定初态，使用同一份输入把正常路径与把协程等同后台线程，或启动无所有者的任务后更新已销毁界面产生的反例并排保存。

对 Challenge: Minimum Strength 的四级验收分别是：正文出现该坐标；解释它如何参与在Kotlin 1.2早期协程语境下理解主线程、async、launch、await与挂起函数，并隔离网络失败；在章专属实验中选择该节点并观察反馈；最后凭调度时间线、主线程断言、取消记录、异常路径、动态数据测试和版本迁移说明回答练习、复位并重放。

## 最小可执行切片

```kotlin
suspend fun loadCharacter(api: CharacterApi): Character {
    val raw = api.fetch()
    return parseCharacter(raw)
}

// Kotlin 1.2-era coroutine APIs were experimental.
// Modern structured concurrency belongs in a migration layer.
```

运行切片时固定 Kotlin/JDK 或 Android 工具链版本、输入数据和命令；保存标准输出、编译诊断及断言。迁移实验必须另建记录，不能静默改写2018年的历史坐标。

<Callout type="trap" title="本章核心陷阱">
  把协程等同后台线程，或启动无所有者的任务后更新已销毁界面。先缩小到最小反例，明确是类型合同、对象所有权、求值顺序还是平台边界失效，再用同输入验证修复。
</Callout>

<Callout type="trap" title="把当前惯例倒灌进第一版">
  22. Introduction to Coroutines保留 Kotlin 1.2/JVM 的历史身份；当前 Kotlin 或 Android 方案只作单独迁移坐标，不能据此改写第一版目录和接口身份。
</Callout>

<Callout type="trap" title="只保留成功输出">
  调度时间线、主线程断言、取消记录、异常路径、动态数据测试和版本迁移说明必须同时包含正常、边界和失败路径；如果故障后不能恢复并复位，本页实践闭环仍未完成。
</Callout>

## 练习、答案与四级证据

<Exercises>

**问题 1：历史与迁移。** 怎样既保留 Kotlin 1.2 的教学身份，又使用当前官方资料核验机制？

<Answer>
  先以[Kotlin 1.2发布说明](https://kotlinlang.org/docs/whatsnew12.html)和目录锁定历史范围，再用[当前技术文档](https://kotlinlang.org/docs/coroutines-guide.html)建立单独迁移记录。比较相同输入、结果和不变量；只改变版本坐标，不把当前API伪装成第一版内容。
</Answer>

**问题 2：逐节点验证。** 本页 10 个正式节点怎样从出现升级到可复查证据？

<Answer>
  - **22. Introduction to Coroutines**：在固定初态阶段保存输入与结果，触发把协程等同后台线程，或启动无所有者的任务后更新已销毁界面后恢复初态，并由调度时间线、主线程断言、取消记录、异常路径、动态数据测试和版本迁移说明确认语义未漂移。
  - **Parsing Character Data**：在施加动作阶段保存输入与结果，触发把协程等同后台线程，或启动无所有者的任务后更新已销毁界面后恢复初态，并由调度时间线、主线程断言、取消记录、异常路径、动态数据测试和版本迁移说明确认语义未漂移。
  - **Fetching Live Data**：在推进状态阶段保存输入与结果，触发把协程等同后台线程，或启动无所有者的任务后更新已销毁界面后恢复初态，并由调度时间线、主线程断言、取消记录、异常路径、动态数据测试和版本迁移说明确认语义未漂移。
  - **The Android Main Thread**：在观察差异阶段保存输入与结果，触发把协程等同后台线程，或启动无所有者的任务后更新已销毁界面后恢复初态，并由调度时间线、主线程断言、取消记录、异常路径、动态数据测试和版本迁移说明确认语义未漂移。
  - **Enabling Coroutines**：在复位重放阶段保存输入与结果，触发把协程等同后台线程，或启动无所有者的任务后更新已销毁界面后恢复初态，并由调度时间线、主线程断言、取消记录、异常路径、动态数据测试和版本迁移说明确认语义未漂移。
  - **Specifying a Coroutine with async**：在固定初态阶段保存输入与结果，触发把协程等同后台线程，或启动无所有者的任务后更新已销毁界面后恢复初态，并由调度时间线、主线程断言、取消记录、异常路径、动态数据测试和版本迁移说明确认语义未漂移。
  - **launch vs async/await**：在施加动作阶段保存输入与结果，触发把协程等同后台线程，或启动无所有者的任务后更新已销毁界面后恢复初态，并由调度时间线、主线程断言、取消记录、异常路径、动态数据测试和版本迁移说明确认语义未漂移。
  - **Suspending Functions**：在推进状态阶段保存输入与结果，触发把协程等同后台线程，或启动无所有者的任务后更新已销毁界面后恢复初态，并由调度时间线、主线程断言、取消记录、异常路径、动态数据测试和版本迁移说明确认语义未漂移。
  - **Challenge: Live Data**：在观察差异阶段保存输入与结果，触发把协程等同后台线程，或启动无所有者的任务后更新已销毁界面后恢复初态，并由调度时间线、主线程断言、取消记录、异常路径、动态数据测试和版本迁移说明确认语义未漂移。
  - **Challenge: Minimum Strength**：在复位重放阶段保存输入与结果，触发把协程等同后台线程，或启动无所有者的任务后更新已销毁界面后恢复初态，并由调度时间线、主线程断言、取消记录、异常路径、动态数据测试和版本迁移说明确认语义未漂移。
</Answer>

**问题 3：反例闭环。** 遇到把协程等同后台线程，或启动无所有者的任务后更新已销毁界面时，如何判断修复真正成立？

<Answer>
  保存首个失败诊断，恢复显式输入域、类型或对象边界和求值顺序，再以相同工具链和输入重跑正常、边界、非法三组样例。只有调度时间线、主线程断言、取消记录、异常路径、动态数据测试和版本迁移说明回到基线且在Kotlin 1.2早期协程语境下理解主线程、async、launch、await与挂起函数，并隔离网络失败的输入、类型边界、求值结果和失败路径都能由独立读者重放重新成立，才能通过。
</Answer>

</Exercises>

## 术语复核与本章回顾

<Glossary>
  <GlossaryItem term="初始状态">
    初始状态在本页限定22. Introduction to Coroutines允许进入系统的值与前置条件。
  </GlossaryItem>
  <GlossaryItem term="单一变量">
    单一变量在本页标记在Kotlin 1.2早期协程语境下理解主线程、async、launch、await与挂起函数，并隔离网络失败中状态由谁创建、修改和释放。
  </GlossaryItem>
  <GlossaryItem term="状态迁移">
    状态迁移在本页说明Kotlin 1.2实现与当前迁移之间必须保持的语义。
  </GlossaryItem>
  <GlossaryItem term="可见反馈">
    可见反馈在本页保存能推翻把协程等同后台线程，或启动无所有者的任务后更新已销毁界面的原始观察。
  </GlossaryItem>
  <GlossaryItem term="故障注入">
    故障注入在本页把错误停在复位重放而非继续扩散。
  </GlossaryItem>
  <GlossaryItem term="复位重放">
    复位重放在本页让另一位读者凭调度时间线、主线程断言、取消记录、异常路径、动态数据测试和版本迁移说明得到相同结论。
  </GlossaryItem>
</Glossary>

完成22. Introduction to Coroutines意味着：能解释在Kotlin 1.2早期协程语境下理解主线程、async、launch、await与挂起函数，并隔离网络失败，能运行并改动章专属切片，能主动制造把协程等同后台线程，或启动无所有者的任务后更新已销毁界面，还能凭调度时间线、主线程断言、取消记录、异常路径、动态数据测试和版本迁移说明恢复并交接，而不是只识别术语。

## 阅读导航

[← 上一页：21. Building Your First Android Application with Kotlin](/learn/kotlin-definitive-guide/06-integration/kdg1-21-first-android-application) · [下一页：23. Afterword →](/learn/kotlin-definitive-guide/07-reference/kdg1-23-afterword)

<Attribution
  mode="independent-rewrite"
  sourceBasis="outline-only"
  workTitle="Josh Skeen、David Greenhalgh《Kotlin Programming: The Big Nerd Ranch Guide》第1版（Addison-Wesley，2018）"
  adaptedUrl="https://www.oreilly.com/library/view/kotlin-programming-the/9780135165188/"
/>
