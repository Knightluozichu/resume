---
title: "第3章 pod ：运行于 Kubernetes 中的容器"
description: "依据电子工业出版社第1版完整目录：掌握Pod共享边界、YAML描述、日志端口转发、标签选择器、命名空间和删除语义"
type: C
section: "Kubernetes in Action中文版（第1版 / Kubernetes 1.8 / 2018）"
order: 3
draft: false
math: false
sourceUrl: "https://www.phei.com.cn/module/goods/wssd_content.jsp?bookid=53240"
demo: true
---

import { Objectives } from "@/components/mdx/objectives";
import { Term } from "@/components/mdx/term";
import { Callout } from "@/components/mdx/callout";
import { Answer, Exercises } from "@/components/mdx/exercises";
import { Glossary, GlossaryItem } from "@/components/mdx/glossary";
import { Attribution } from "@/components/mdx/attribution";

# 第3章 pod ：运行于 Kubernetes 中的容器

本课程对应Marko Lukša著《Kubernetes in Action中文版》第1版，七牛容器云团队译，电子工业出版社2018年12月出版，592页，ISBN 9787121349959；英文原版由Manning于2017年12月出版。作者第1版源码第8章明确下载Kubernetes 1.8.0客户端，课程据此固定行为边界。

全书正式分母为18章与附录A至D，共22个正式单元、404个唯一章/附录/节节点；另设学习地图和总复习，共24页。第2版、现代apps/v1写法、EndpointSlice、Gateway API、Pod Security Admission或后来的多集群方案只能作为差异材料，不能替代第1版。本页逐项覆盖37个目录节点，最终交付Pod边界图、YAML清单、日志与请求轨迹、标签选择实验、命名空间和删除演练。

<Objectives>

- 能解释“第3章 pod ：运行于 Kubernetes 中的容器”的37个目录节点，并绘制API对象、控制器、调度、节点执行、网络与存储边界。
- 能比较基线、压力、对象变化、Pod或节点故障和恢复轨迹，判断期望状态、实际状态与业务完成点。
- 能设计并复现Pod边界图、YAML清单、日志与请求轨迹、标签选择实验、命名空间和删除演练，用清单、事件、日志、指标与最终状态支持结论。
- 能分析“把Pod当永久主机或安全租户边界，直接修改临时实例而不保留声明”为何失败，并写出停止、恢复、回退与独立交接条件。

</Objectives>

## 从一次声明式变更开始

先预测：kubectl提交对象后，API服务器完成哪些认证、授权、准入和持久化步骤，哪个控制器watch到变化，调度器如何选择节点，kubelet怎样创建容器，Service、卷与探针何时就绪。观察前先写预测，再按resourceVersion、UID、ownerReference和事件时间线修正模型。

Kubernetes 1.8的完成不是命令返回created。对象进入etcd、控制器观察、创建下级对象、Pod被调度、镜像启动、探针通过、Endpoint接入流量和业务结果稳定是不同检查点。控制循环允许中间状态，验收必须明确等待条件和超时后的诊断路径。

## 核心词汇与版本边界

<Term def="Pod是第3章 pod ：运行于 Kubernetes 中的容器的第1个核心概念，必须同时说明Kubernetes 1.8对象、控制循环、故障边界和观测证据">Pod</Term>、<Term def="标签是第3章 pod ：运行于 Kubernetes 中的容器的第2个核心概念，必须同时说明Kubernetes 1.8对象、控制循环、故障边界和观测证据">标签</Term>、<Term def="标签选择器是第3章 pod ：运行于 Kubernetes 中的容器的第3个核心概念，必须同时说明Kubernetes 1.8对象、控制循环、故障边界和观测证据">标签选择器</Term>、<Term def="注解是第3章 pod ：运行于 Kubernetes 中的容器的第4个核心概念，必须同时说明Kubernetes 1.8对象、控制循环、故障边界和观测证据">注解</Term>、<Term def="命名空间是第3章 pod ：运行于 Kubernetes 中的容器的第5个核心概念，必须同时说明Kubernetes 1.8对象、控制循环、故障边界和观测证据">命名空间</Term>

这些词构成本页词汇。每个词都要回答：对象由谁声明，谁拥有，谁调谐，期望与实际状态保存在哪里，失败后会重试还是终止，怎样观测，以及Kubernetes 1.8之后哪些变化不属于本书。


## 原书目录逐节点重构

### 3.1 介绍 pod

**目录节点 1/37。** “介绍 pod”服务于“掌握Pod共享边界、YAML描述、日志端口转发、标签选择器、命名空间和删除语义”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“本章输入与版本契约”进入“介绍 pod”，再到“为何需要 pod”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“同一Pod容器共享网络与卷但保持进程隔离，标签和命名空间选择只影响预期对象”验收。

#### 3.1.1 为何需要 pod

**目录节点 2/37。** “为何需要 pod”服务于“掌握Pod共享边界、YAML描述、日志端口转发、标签选择器、命名空间和删除语义”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“介绍 pod”进入“为何需要 pod”，再到“了解 pod”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“同一Pod容器共享网络与卷但保持进程隔离，标签和命名空间选择只影响预期对象”验收。

#### 3.1.2 了解 pod

**目录节点 3/37。** “了解 pod”服务于“掌握Pod共享边界、YAML描述、日志端口转发、标签选择器、命名空间和删除语义”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“为何需要 pod”进入“了解 pod”，再到“通过 pod 合理管理容器”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“同一Pod容器共享网络与卷但保持进程隔离，标签和命名空间选择只影响预期对象”验收。

#### 3.1.3 通过 pod 合理管理容器

**目录节点 4/37。** “通过 pod 合理管理容器”服务于“掌握Pod共享边界、YAML描述、日志端口转发、标签选择器、命名空间和删除语义”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“了解 pod”进入“通过 pod 合理管理容器”，再到“以 YAML 或 JSON 描述文件创建 pod”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“同一Pod容器共享网络与卷但保持进程隔离，标签和命名空间选择只影响预期对象”验收。

### 3.2 以 YAML 或 JSON 描述文件创建 pod

**目录节点 5/37。** “以 YAML 或 JSON 描述文件创建 pod”服务于“掌握Pod共享边界、YAML描述、日志端口转发、标签选择器、命名空间和删除语义”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“通过 pod 合理管理容器”进入“以 YAML 或 JSON 描述文件创建 pod”，再到“检查现有 pod 的 YAML 描述文件”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“同一Pod容器共享网络与卷但保持进程隔离，标签和命名空间选择只影响预期对象”验收。

#### 3.2.1 检查现有 pod 的 YAML 描述文件

**目录节点 6/37。** “检查现有 pod 的 YAML 描述文件”服务于“掌握Pod共享边界、YAML描述、日志端口转发、标签选择器、命名空间和删除语义”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“以 YAML 或 JSON 描述文件创建 pod”进入“检查现有 pod 的 YAML 描述文件”，再到“为 pod 创建一个简单的 YAML 描述文件”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“同一Pod容器共享网络与卷但保持进程隔离，标签和命名空间选择只影响预期对象”验收。

#### 3.2.2 为 pod 创建一个简单的 YAML 描述文件

**目录节点 7/37。** “为 pod 创建一个简单的 YAML 描述文件”服务于“掌握Pod共享边界、YAML描述、日志端口转发、标签选择器、命名空间和删除语义”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“检查现有 pod 的 YAML 描述文件”进入“为 pod 创建一个简单的 YAML 描述文件”，再到“使用 kubectl create 来创建 pod”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“同一Pod容器共享网络与卷但保持进程隔离，标签和命名空间选择只影响预期对象”验收。

#### 3.2.3 使用 kubectl create 来创建 pod

**目录节点 8/37。** “使用 kubectl create 来创建 pod”服务于“掌握Pod共享边界、YAML描述、日志端口转发、标签选择器、命名空间和删除语义”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“为 pod 创建一个简单的 YAML 描述文件”进入“使用 kubectl create 来创建 pod”，再到“查看应用程序日志”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“同一Pod容器共享网络与卷但保持进程隔离，标签和命名空间选择只影响预期对象”验收。

#### 3.2.4 查看应用程序日志

**目录节点 9/37。** “查看应用程序日志”服务于“掌握Pod共享边界、YAML描述、日志端口转发、标签选择器、命名空间和删除语义”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“使用 kubectl create 来创建 pod”进入“查看应用程序日志”，再到“向 pod 发送请求”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“同一Pod容器共享网络与卷但保持进程隔离，标签和命名空间选择只影响预期对象”验收。

#### 3.2.5 向 pod 发送请求

**目录节点 10/37。** “向 pod 发送请求”服务于“掌握Pod共享边界、YAML描述、日志端口转发、标签选择器、命名空间和删除语义”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“查看应用程序日志”进入“向 pod 发送请求”，再到“使用标签组织 pod”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“同一Pod容器共享网络与卷但保持进程隔离，标签和命名空间选择只影响预期对象”验收。

### 3.3 使用标签组织 pod

**目录节点 11/37。** “使用标签组织 pod”服务于“掌握Pod共享边界、YAML描述、日志端口转发、标签选择器、命名空间和删除语义”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“向 pod 发送请求”进入“使用标签组织 pod”，再到“介绍标签”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“同一Pod容器共享网络与卷但保持进程隔离，标签和命名空间选择只影响预期对象”验收。

#### 3.3.1 介绍标签

**目录节点 12/37。** “介绍标签”服务于“掌握Pod共享边界、YAML描述、日志端口转发、标签选择器、命名空间和删除语义”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“使用标签组织 pod”进入“介绍标签”，再到“创建 pod 时指定标签”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“同一Pod容器共享网络与卷但保持进程隔离，标签和命名空间选择只影响预期对象”验收。

#### 3.3.2 创建 pod 时指定标签

**目录节点 13/37。** “创建 pod 时指定标签”服务于“掌握Pod共享边界、YAML描述、日志端口转发、标签选择器、命名空间和删除语义”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“介绍标签”进入“创建 pod 时指定标签”，再到“修改现有 pod 的标签”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“同一Pod容器共享网络与卷但保持进程隔离，标签和命名空间选择只影响预期对象”验收。

#### 3.3.3 修改现有 pod 的标签

**目录节点 14/37。** “修改现有 pod 的标签”服务于“掌握Pod共享边界、YAML描述、日志端口转发、标签选择器、命名空间和删除语义”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“创建 pod 时指定标签”进入“修改现有 pod 的标签”，再到“通过标签选择器列出 pod 子集”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“同一Pod容器共享网络与卷但保持进程隔离，标签和命名空间选择只影响预期对象”验收。

### 3.4 通过标签选择器列出 pod 子集

**目录节点 15/37。** “通过标签选择器列出 pod 子集”服务于“掌握Pod共享边界、YAML描述、日志端口转发、标签选择器、命名空间和删除语义”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“修改现有 pod 的标签”进入“通过标签选择器列出 pod 子集”，再到“使用标签选择器列出 pod”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“同一Pod容器共享网络与卷但保持进程隔离，标签和命名空间选择只影响预期对象”验收。

#### 3.4.1 使用标签选择器列出 pod

**目录节点 16/37。** “使用标签选择器列出 pod”服务于“掌握Pod共享边界、YAML描述、日志端口转发、标签选择器、命名空间和删除语义”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“通过标签选择器列出 pod 子集”进入“使用标签选择器列出 pod”，再到“在标签选择器中使用多个条件”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“同一Pod容器共享网络与卷但保持进程隔离，标签和命名空间选择只影响预期对象”验收。

#### 3.4.2 在标签选择器中使用多个条件

**目录节点 17/37。** “在标签选择器中使用多个条件”服务于“掌握Pod共享边界、YAML描述、日志端口转发、标签选择器、命名空间和删除语义”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“使用标签选择器列出 pod”进入“在标签选择器中使用多个条件”，再到“使用标签和选择器来约束 pod 调度”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“同一Pod容器共享网络与卷但保持进程隔离，标签和命名空间选择只影响预期对象”验收。

### 3.5 使用标签和选择器来约束 pod 调度

**目录节点 18/37。** “使用标签和选择器来约束 pod 调度”服务于“掌握Pod共享边界、YAML描述、日志端口转发、标签选择器、命名空间和删除语义”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“在标签选择器中使用多个条件”进入“使用标签和选择器来约束 pod 调度”，再到“使用标签分类工作节点”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“同一Pod容器共享网络与卷但保持进程隔离，标签和命名空间选择只影响预期对象”验收。

#### 3.5.1 使用标签分类工作节点

**目录节点 19/37。** “使用标签分类工作节点”服务于“掌握Pod共享边界、YAML描述、日志端口转发、标签选择器、命名空间和删除语义”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“使用标签和选择器来约束 pod 调度”进入“使用标签分类工作节点”，再到“将 pod 调度到特定节点”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“同一Pod容器共享网络与卷但保持进程隔离，标签和命名空间选择只影响预期对象”验收。

#### 3.5.2 将 pod 调度到特定节点

**目录节点 20/37。** “将 pod 调度到特定节点”服务于“掌握Pod共享边界、YAML描述、日志端口转发、标签选择器、命名空间和删除语义”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“使用标签分类工作节点”进入“将 pod 调度到特定节点”，再到“调度到一个特定节点”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“同一Pod容器共享网络与卷但保持进程隔离，标签和命名空间选择只影响预期对象”验收。

#### 3.5.3 调度到一个特定节点

**目录节点 21/37。** “调度到一个特定节点”服务于“掌握Pod共享边界、YAML描述、日志端口转发、标签选择器、命名空间和删除语义”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“将 pod 调度到特定节点”进入“调度到一个特定节点”，再到“注解 pod”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“同一Pod容器共享网络与卷但保持进程隔离，标签和命名空间选择只影响预期对象”验收。

### 3.6 注解 pod

**目录节点 22/37。** “注解 pod”服务于“掌握Pod共享边界、YAML描述、日志端口转发、标签选择器、命名空间和删除语义”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“调度到一个特定节点”进入“注解 pod”，再到“查找对象的注解”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“同一Pod容器共享网络与卷但保持进程隔离，标签和命名空间选择只影响预期对象”验收。

#### 3.6.1 查找对象的注解

**目录节点 23/37。** “查找对象的注解”服务于“掌握Pod共享边界、YAML描述、日志端口转发、标签选择器、命名空间和删除语义”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“注解 pod”进入“查找对象的注解”，再到“添加和修改注解”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“同一Pod容器共享网络与卷但保持进程隔离，标签和命名空间选择只影响预期对象”验收。

#### 3.6.2 添加和修改注解

**目录节点 24/37。** “添加和修改注解”服务于“掌握Pod共享边界、YAML描述、日志端口转发、标签选择器、命名空间和删除语义”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“查找对象的注解”进入“添加和修改注解”，再到“使用命名空间对资源进行分组”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“同一Pod容器共享网络与卷但保持进程隔离，标签和命名空间选择只影响预期对象”验收。

### 3.7 使用命名空间对资源进行分组

**目录节点 25/37。** “使用命名空间对资源进行分组”服务于“掌握Pod共享边界、YAML描述、日志端口转发、标签选择器、命名空间和删除语义”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“添加和修改注解”进入“使用命名空间对资源进行分组”，再到“了解对命名空间的需求”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“同一Pod容器共享网络与卷但保持进程隔离，标签和命名空间选择只影响预期对象”验收。

#### 3.7.1 了解对命名空间的需求

**目录节点 26/37。** “了解对命名空间的需求”服务于“掌握Pod共享边界、YAML描述、日志端口转发、标签选择器、命名空间和删除语义”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“使用命名空间对资源进行分组”进入“了解对命名空间的需求”，再到“发现其他命名空间及其 pod”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“同一Pod容器共享网络与卷但保持进程隔离，标签和命名空间选择只影响预期对象”验收。

#### 3.7.2 发现其他命名空间及其 pod

**目录节点 27/37。** “发现其他命名空间及其 pod”服务于“掌握Pod共享边界、YAML描述、日志端口转发、标签选择器、命名空间和删除语义”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“了解对命名空间的需求”进入“发现其他命名空间及其 pod”，再到“创建一个命名空间”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“同一Pod容器共享网络与卷但保持进程隔离，标签和命名空间选择只影响预期对象”验收。

#### 3.7.3 创建一个命名空间

**目录节点 28/37。** “创建一个命名空间”服务于“掌握Pod共享边界、YAML描述、日志端口转发、标签选择器、命名空间和删除语义”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“发现其他命名空间及其 pod”进入“创建一个命名空间”，再到“管理其他命名空间中的对象”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“同一Pod容器共享网络与卷但保持进程隔离，标签和命名空间选择只影响预期对象”验收。

#### 3.7.4 管理其他命名空间中的对象

**目录节点 29/37。** “管理其他命名空间中的对象”服务于“掌握Pod共享边界、YAML描述、日志端口转发、标签选择器、命名空间和删除语义”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“创建一个命名空间”进入“管理其他命名空间中的对象”，再到“命名空间提供的隔离”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“同一Pod容器共享网络与卷但保持进程隔离，标签和命名空间选择只影响预期对象”验收。

#### 3.7.5 命名空间提供的隔离

**目录节点 30/37。** “命名空间提供的隔离”服务于“掌握Pod共享边界、YAML描述、日志端口转发、标签选择器、命名空间和删除语义”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“管理其他命名空间中的对象”进入“命名空间提供的隔离”，再到“停止和移除 pod”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“同一Pod容器共享网络与卷但保持进程隔离，标签和命名空间选择只影响预期对象”验收。

### 3.8 停止和移除 pod

**目录节点 31/37。** “停止和移除 pod”服务于“掌握Pod共享边界、YAML描述、日志端口转发、标签选择器、命名空间和删除语义”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“命名空间提供的隔离”进入“停止和移除 pod”，再到“按名称删除 pod”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“同一Pod容器共享网络与卷但保持进程隔离，标签和命名空间选择只影响预期对象”验收。

#### 3.8.1 按名称删除 pod

**目录节点 32/37。** “按名称删除 pod”服务于“掌握Pod共享边界、YAML描述、日志端口转发、标签选择器、命名空间和删除语义”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“停止和移除 pod”进入“按名称删除 pod”，再到“使用标签选择器删除 pod”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“同一Pod容器共享网络与卷但保持进程隔离，标签和命名空间选择只影响预期对象”验收。

#### 3.8.2 使用标签选择器删除 pod

**目录节点 33/37。** “使用标签选择器删除 pod”服务于“掌握Pod共享边界、YAML描述、日志端口转发、标签选择器、命名空间和删除语义”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“按名称删除 pod”进入“使用标签选择器删除 pod”，再到“通过删除整个命名空间来删除 pod”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“同一Pod容器共享网络与卷但保持进程隔离，标签和命名空间选择只影响预期对象”验收。

#### 3.8.3 通过删除整个命名空间来删除 pod

**目录节点 34/37。** “通过删除整个命名空间来删除 pod”服务于“掌握Pod共享边界、YAML描述、日志端口转发、标签选择器、命名空间和删除语义”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“使用标签选择器删除 pod”进入“通过删除整个命名空间来删除 pod”，再到“删除命名空间中的所有 pod，但保留命名空间”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“同一Pod容器共享网络与卷但保持进程隔离，标签和命名空间选择只影响预期对象”验收。

#### 3.8.4 删除命名空间中的所有 pod，但保留命名空间

**目录节点 35/37。** “删除命名空间中的所有 pod，但保留命名空间”服务于“掌握Pod共享边界、YAML描述、日志端口转发、标签选择器、命名空间和删除语义”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“通过删除整个命名空间来删除 pod”进入“删除命名空间中的所有 pod，但保留命名空间”，再到“删除命名空间中的（几乎）所有资源”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“同一Pod容器共享网络与卷但保持进程隔离，标签和命名空间选择只影响预期对象”验收。

#### 3.8.5 删除命名空间中的（几乎）所有资源

**目录节点 36/37。** “删除命名空间中的（几乎）所有资源”服务于“掌握Pod共享边界、YAML描述、日志端口转发、标签选择器、命名空间和删除语义”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“删除命名空间中的所有 pod，但保留命名空间”进入“删除命名空间中的（几乎）所有资源”，再到“本章小结”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“同一Pod容器共享网络与卷但保持进程隔离，标签和命名空间选择只影响预期对象”验收。

### 3.9 本章小结

**目录节点 37/37。** “本章小结”服务于“掌握Pod共享边界、YAML描述、日志端口转发、标签选择器、命名空间和删除语义”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“删除命名空间中的（几乎）所有资源”进入“本章小结”，再到“本章证据门与回退”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“同一Pod容器共享网络与卷但保持进程隔离，标签和命名空间选择只影响预期对象”验收。

<Callout type="trap" title="语义陷阱">
  把Pod当永久主机或安全租户边界，直接修改临时实例而不保留声明。先写清对象所有权、控制器完成点、节点执行与最终业务不变量，再选择命令或清单。
</Callout>

<Callout type="trap" title="版本陷阱">
  原书以Kubernetes
  1.8.0为基线，ReplicationController、extensions/v1beta1、rkt、Heapster、Service
  Catalog和Federation均有时代语境；现代结果可作迁移差异，不能证明本书行为。
</Callout>

<Callout type="warning" title="隔离实验">
  删除命名空间、驱逐Pod、停止节点、网络隔离、卷回收、证书与RBAC变更只在隔离集群演练。先备份清单、etcd或业务样本并规定停止上界。
</Callout>

## 独立证据与生产交接

目录证据保存18章4附录与404个唯一节点映射；环境证据保存客户端、API服务器、etcd、容器运行时、网络与存储插件版本；对象证据保存清单、UID、resourceVersion、ownerReference、spec和status；运行证据保存事件、控制器、调度、kubelet、探针、流量和最终业务结果。

验收记录按一次变更一个编号：记录上下文、命名空间、操作者、开始结束时间、对象前后状态、请求样本、预期与实际结果、日志和指标窗口。失败时保留原始事件、Pending或终止原因、影响范围、停止条件和回退验证；成功时也须由另一位读者按同一材料重放。

性能判断固定镜像、请求分布、副本、资源、探针、Service、节点和存储语义。扩大limit或关闭探针换来的吞吐不是等价优化。平均值会隐藏调度等待、镜像拉取、CPU节流、OOM、控制器重试、Endpoint切换和卷挂载，必须保留P95/P99与状态时间线。

恢复判断从业务状态结束。Pod Running、节点Ready、Deployment Available、PVC Bound或API可访问都只是阶段；还要核对客户端请求、数据集合、身份权限、对象版本和旧实例是否停止提供错误结果。

## 本章回顾

重新完成“掌握Pod共享边界、YAML描述、日志端口转发、标签选择器、命名空间和删除语义”：固定Kubernetes 1.8和第1版目录，从声明式对象与控制循环推导边界，通过基线、压力与故障实验测量，最后交付Pod边界图、YAML清单、日志与请求轨迹、标签选择实验、命名空间和删除演练。只有“同一Pod容器共享网络与卷但保持进程隔离，标签和命名空间选择只影响预期对象”与反例都能独立重放，本页才算完成。

## 复习与运行验收

<Exercises>
**问题 1**：为什么“第3章 pod ：运行于 Kubernetes 中的容器”必须覆盖37个目录节点？
<Answer>这些节点共同组成“掌握Pod共享边界、YAML描述、日志端口转发、标签选择器、命名空间和删除语义”的对象、控制循环、运行与证据链；漏项会使Pod边界图、YAML清单、日志与请求轨迹、标签选择实验、命名空间和删除演练无法独立复现。</Answer>

**问题 2**：本页最小不变量是什么？

<Answer>
  同一Pod容器共享网络与卷但保持进程隔离，标签和命名空间选择只影响预期对象。需要版本、对象、控制器轨迹、指标、故障与业务对账共同证明。
</Answer>

**问题 3**：怎样构造最小反例？

<Answer>
  固定Kubernetes
  1.8、集群、命名空间、镜像和清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一，再比较状态轨迹与最终业务结果。
</Answer>

**问题 4**：为什么不能直接用现代Kubernetes替代本页实验？

<Answer>
  原书源码以Kubernetes
  1.8.0为基线；API版本、运行时、网络、安全、伸缩和多集群机制后来均有变化，新版只能作为差异材料。
</Answer>

**问题 5**：如何验证性能和恢复结论？

<Answer>
  固定镜像、请求分布、副本、资源、探针、网络和存储语义，重复测期望/当前/就绪、重启、Pending、吞吐、P95/P99和错误，并独立对账。
</Answer>

**问题 6**：独立交接需要什么？

<Answer>需要完整目录映射、版本环境、对象快照、控制循环轨迹、Pod边界图、YAML清单、日志与请求轨迹、标签选择实验、命名空间和删除演练、负载、故障、对账、停止、恢复、回退和责任人。</Answer>
</Exercises>

<Glossary>
  <GlossaryItem term="Pod">
    Pod在本页对应Kubernetes
    1.8中的对象、字段或状态；适用范围由“同一Pod容器共享网络与卷但保持进程隔离，标签和命名空间选择只影响预期对象”和故障反例共同限定。
  </GlossaryItem>
  <GlossaryItem term="标签">
    标签在本页对应Kubernetes
    1.8中的对象、字段或状态；适用范围由“同一Pod容器共享网络与卷但保持进程隔离，标签和命名空间选择只影响预期对象”和故障反例共同限定。
  </GlossaryItem>
  <GlossaryItem term="标签选择器">
    标签选择器在本页对应Kubernetes
    1.8中的对象、字段或状态；适用范围由“同一Pod容器共享网络与卷但保持进程隔离，标签和命名空间选择只影响预期对象”和故障反例共同限定。
  </GlossaryItem>
  <GlossaryItem term="注解">
    注解在本页对应Kubernetes
    1.8中的对象、字段或状态；适用范围由“同一Pod容器共享网络与卷但保持进程隔离，标签和命名空间选择只影响预期对象”和故障反例共同限定。
  </GlossaryItem>
  <GlossaryItem term="命名空间">
    命名空间在本页对应Kubernetes
    1.8中的对象、字段或状态；适用范围由“同一Pod容器共享网络与卷但保持进程隔离，标签和命名空间选择只影响预期对象”和故障反例共同限定。
  </GlossaryItem>
</Glossary>

[← 上一页：第2章 开始使用 Kubernetes 和 Docker](/learn/kubernetes-in-action/02-02-docker-first-app/k8s-02-docker-first-app) · [下一页：第4章 副本机制和其他控制器 ：部署托管的 pod →](/learn/kubernetes-in-action/04-04-replication-controllers/k8s-04-replication-controllers)

<Attribution
  adaptedFrom="Marko Lukša《Kubernetes in Action》第1版"
  adaptedUrl="https://www.phei.com.cn/module/goods/wssd_content.jsp?bookid=53240"
/>
