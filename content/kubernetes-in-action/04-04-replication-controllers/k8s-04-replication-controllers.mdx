---
title: "第4章 副本机制和其他控制器 ：部署托管的 pod"
description: "依据电子工业出版社第1版完整目录：用存活探针、ReplicationController、ReplicaSet、DaemonSet、Job和CronJob管理Pod"
type: C
section: "Kubernetes in Action中文版（第1版 / Kubernetes 1.8 / 2018）"
order: 4
draft: false
math: false
sourceUrl: "https://www.phei.com.cn/module/goods/wssd_content.jsp?bookid=53240"
demo: true
---

import {
  K8s04ReplicationControllersObjectLab,
  K8s04ReplicationControllersReconcileLab,
  K8s04ReplicationControllersEvidenceLab,
} from "@/components/mdx/kubernetes-in-action/diagrams/k8s-04-replication-controllers";

# 第4章 副本机制和其他控制器 ：部署托管的 pod

本课程对应Marko Lukša著《Kubernetes in Action中文版》第1版，七牛容器云团队译，电子工业出版社2018年12月出版，592页，ISBN 9787121349959；英文原版由Manning于2017年12月出版。作者第1版源码第8章明确下载Kubernetes 1.8.0客户端，课程据此固定行为边界。

全书正式分母为18章与附录A至D，共22个正式单元、404个唯一章/附录/节节点；另设学习地图和总复习，共24页。第2版、现代apps/v1写法、EndpointSlice、Gateway API、Pod Security Admission或后来的多集群方案只能作为差异材料，不能替代第1版。本页逐项覆盖33个目录节点，最终交付探针实验、RC与RS对照、DaemonSet节点覆盖、Job完成证据和CronJob调度记录。

<Objectives>

- 能解释“第4章 副本机制和其他控制器 ：部署托管的 pod”的33个目录节点，并绘制API对象、控制器、调度、节点执行、网络与存储边界。
- 能比较基线、压力、对象变化、Pod或节点故障和恢复轨迹，判断期望状态、实际状态与业务完成点。
- 能设计并复现探针实验、RC与RS对照、DaemonSet节点覆盖、Job完成证据和CronJob调度记录，用清单、事件、日志、指标与最终状态支持结论。
- 能分析“把控制器拥有的Pod当独立资源手工修复，或用错误探针制造持续重启”为何失败，并写出停止、恢复、回退与独立交接条件。

</Objectives>

## 从一次声明式变更开始

先预测：kubectl提交对象后，API服务器完成哪些认证、授权、准入和持久化步骤，哪个控制器watch到变化，调度器如何选择节点，kubelet怎样创建容器，Service、卷与探针何时就绪。观察前先写预测，再按resourceVersion、UID、ownerReference和事件时间线修正模型。

Kubernetes 1.8的完成不是命令返回created。对象进入etcd、控制器观察、创建下级对象、Pod被调度、镜像启动、探针通过、Endpoint接入流量和业务结果稳定是不同检查点。控制循环允许中间状态，验收必须明确等待条件和超时后的诊断路径。

## 核心词汇与版本边界

<Term def="存活探针是第4章 副本机制和其他控制器 ：部署托管的 pod的第1个核心概念，必须同时说明Kubernetes 1.8对象、控制循环、故障边界和观测证据">存活探针</Term>、<Term def="ReplicationController是第4章 副本机制和其他控制器 ：部署托管的 pod的第2个核心概念，必须同时说明Kubernetes 1.8对象、控制循环、故障边界和观测证据">ReplicationController</Term>、<Term def="ReplicaSet是第4章 副本机制和其他控制器 ：部署托管的 pod的第3个核心概念，必须同时说明Kubernetes 1.8对象、控制循环、故障边界和观测证据">ReplicaSet</Term>、<Term def="DaemonSet是第4章 副本机制和其他控制器 ：部署托管的 pod的第4个核心概念，必须同时说明Kubernetes 1.8对象、控制循环、故障边界和观测证据">DaemonSet</Term>、<Term def="Job是第4章 副本机制和其他控制器 ：部署托管的 pod的第5个核心概念，必须同时说明Kubernetes 1.8对象、控制循环、故障边界和观测证据">Job</Term>

这些词构成本页词汇。每个词都要回答：对象由谁声明，谁拥有，谁调谐，期望与实际状态保存在哪里，失败后会重试还是终止，怎样观测，以及Kubernetes 1.8之后哪些变化不属于本书。

<K8s04ReplicationControllersObjectLab />

## 原书目录逐节点重构

### 4.1 保持 pod 健康

**目录节点 1/33。** “保持 pod 健康”服务于“用存活探针、ReplicationController、ReplicaSet、DaemonSet、Job和CronJob管理Pod”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“本章输入与版本契约”进入“保持 pod 健康”，再到“介绍存活探针”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“控制器选择器、模板和期望副本明确，删除、漂移、探针失败和任务完成后状态按控制循环收敛”验收。

#### 4.1.1 介绍存活探针

**目录节点 2/33。** “介绍存活探针”服务于“用存活探针、ReplicationController、ReplicaSet、DaemonSet、Job和CronJob管理Pod”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“保持 pod 健康”进入“介绍存活探针”，再到“创建基于 HTTP 的存活探针”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“控制器选择器、模板和期望副本明确，删除、漂移、探针失败和任务完成后状态按控制循环收敛”验收。

#### 4.1.2 创建基于 HTTP 的存活探针

**目录节点 3/33。** “创建基于 HTTP 的存活探针”服务于“用存活探针、ReplicationController、ReplicaSet、DaemonSet、Job和CronJob管理Pod”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“介绍存活探针”进入“创建基于 HTTP 的存活探针”，再到“使用存活探针”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“控制器选择器、模板和期望副本明确，删除、漂移、探针失败和任务完成后状态按控制循环收敛”验收。

#### 4.1.3 使用存活探针

**目录节点 4/33。** “使用存活探针”服务于“用存活探针、ReplicationController、ReplicaSet、DaemonSet、Job和CronJob管理Pod”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“创建基于 HTTP 的存活探针”进入“使用存活探针”，再到“配置存活探针的附加属性”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“控制器选择器、模板和期望副本明确，删除、漂移、探针失败和任务完成后状态按控制循环收敛”验收。

#### 4.1.4 配置存活探针的附加属性

**目录节点 5/33。** “配置存活探针的附加属性”服务于“用存活探针、ReplicationController、ReplicaSet、DaemonSet、Job和CronJob管理Pod”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“使用存活探针”进入“配置存活探针的附加属性”，再到“创建有效的存活探针”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“控制器选择器、模板和期望副本明确，删除、漂移、探针失败和任务完成后状态按控制循环收敛”验收。

#### 4.1.5 创建有效的存活探针

**目录节点 6/33。** “创建有效的存活探针”服务于“用存活探针、ReplicationController、ReplicaSet、DaemonSet、Job和CronJob管理Pod”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“配置存活探针的附加属性”进入“创建有效的存活探针”，再到“了解 ReplicationController”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“控制器选择器、模板和期望副本明确，删除、漂移、探针失败和任务完成后状态按控制循环收敛”验收。

### 4.2 了解 ReplicationController

**目录节点 7/33。** “了解 ReplicationController”服务于“用存活探针、ReplicationController、ReplicaSet、DaemonSet、Job和CronJob管理Pod”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“创建有效的存活探针”进入“了解 ReplicationController”，再到“ReplicationController 的操作”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“控制器选择器、模板和期望副本明确，删除、漂移、探针失败和任务完成后状态按控制循环收敛”验收。

#### 4.2.1 ReplicationController 的操作

**目录节点 8/33。** “ReplicationController 的操作”服务于“用存活探针、ReplicationController、ReplicaSet、DaemonSet、Job和CronJob管理Pod”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“了解 ReplicationController”进入“ReplicationController 的操作”，再到“创建一个 ReplicationController”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“控制器选择器、模板和期望副本明确，删除、漂移、探针失败和任务完成后状态按控制循环收敛”验收。

#### 4.2.2 创建一个 ReplicationController

**目录节点 9/33。** “创建一个 ReplicationController”服务于“用存活探针、ReplicationController、ReplicaSet、DaemonSet、Job和CronJob管理Pod”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“ReplicationController 的操作”进入“创建一个 ReplicationController”，再到“使用 ReplicationController”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“控制器选择器、模板和期望副本明确，删除、漂移、探针失败和任务完成后状态按控制循环收敛”验收。

#### 4.2.3 使用 ReplicationController

**目录节点 10/33。** “使用 ReplicationController”服务于“用存活探针、ReplicationController、ReplicaSet、DaemonSet、Job和CronJob管理Pod”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“创建一个 ReplicationController”进入“使用 ReplicationController”，再到“将 pod 移入或移出 ReplicationController 的作用域”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“控制器选择器、模板和期望副本明确，删除、漂移、探针失败和任务完成后状态按控制循环收敛”验收。

#### 4.2.4 将 pod 移入或移出 ReplicationController 的作用域

**目录节点 11/33。** “将 pod 移入或移出 ReplicationController 的作用域”服务于“用存活探针、ReplicationController、ReplicaSet、DaemonSet、Job和CronJob管理Pod”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“使用 ReplicationController”进入“将 pod 移入或移出 ReplicationController 的作用域”，再到“修改 pod 模板”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“控制器选择器、模板和期望副本明确，删除、漂移、探针失败和任务完成后状态按控制循环收敛”验收。

#### 4.2.5 修改 pod 模板

**目录节点 12/33。** “修改 pod 模板”服务于“用存活探针、ReplicationController、ReplicaSet、DaemonSet、Job和CronJob管理Pod”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“将 pod 移入或移出 ReplicationController 的作用域”进入“修改 pod 模板”，再到“水平缩放 pod”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“控制器选择器、模板和期望副本明确，删除、漂移、探针失败和任务完成后状态按控制循环收敛”验收。

#### 4.2.6 水平缩放 pod

**目录节点 13/33。** “水平缩放 pod”服务于“用存活探针、ReplicationController、ReplicaSet、DaemonSet、Job和CronJob管理Pod”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“修改 pod 模板”进入“水平缩放 pod”，再到“删除一个 ReplicationController”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“控制器选择器、模板和期望副本明确，删除、漂移、探针失败和任务完成后状态按控制循环收敛”验收。

#### 4.2.7 删除一个 ReplicationController

**目录节点 14/33。** “删除一个 ReplicationController”服务于“用存活探针、ReplicationController、ReplicaSet、DaemonSet、Job和CronJob管理Pod”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“水平缩放 pod”进入“删除一个 ReplicationController”，再到“使用 ReplicaSet 而不是 ReplicationController”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“控制器选择器、模板和期望副本明确，删除、漂移、探针失败和任务完成后状态按控制循环收敛”验收。

### 4.3 使用 ReplicaSet 而不是 ReplicationController

**目录节点 15/33。** “使用 ReplicaSet 而不是 ReplicationController”服务于“用存活探针、ReplicationController、ReplicaSet、DaemonSet、Job和CronJob管理Pod”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“删除一个 ReplicationController”进入“使用 ReplicaSet 而不是 ReplicationController”，再到“比较 ReplicaSet 和 ReplicationController”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“控制器选择器、模板和期望副本明确，删除、漂移、探针失败和任务完成后状态按控制循环收敛”验收。

#### 4.3.1 比较 ReplicaSet 和 ReplicationController

**目录节点 16/33。** “比较 ReplicaSet 和 ReplicationController”服务于“用存活探针、ReplicationController、ReplicaSet、DaemonSet、Job和CronJob管理Pod”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“使用 ReplicaSet 而不是 ReplicationController”进入“比较 ReplicaSet 和 ReplicationController”，再到“定义 ReplicaSet”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“控制器选择器、模板和期望副本明确，删除、漂移、探针失败和任务完成后状态按控制循环收敛”验收。

#### 4.3.2 定义 ReplicaSet

**目录节点 17/33。** “定义 ReplicaSet”服务于“用存活探针、ReplicationController、ReplicaSet、DaemonSet、Job和CronJob管理Pod”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“比较 ReplicaSet 和 ReplicationController”进入“定义 ReplicaSet”，再到“创建和检查 ReplicaSet”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“控制器选择器、模板和期望副本明确，删除、漂移、探针失败和任务完成后状态按控制循环收敛”验收。

#### 4.3.3 创建和检查 ReplicaSet

**目录节点 18/33。** “创建和检查 ReplicaSet”服务于“用存活探针、ReplicationController、ReplicaSet、DaemonSet、Job和CronJob管理Pod”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“定义 ReplicaSet”进入“创建和检查 ReplicaSet”，再到“使用 ReplicaSet 的更富表达力的标签选择器”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“控制器选择器、模板和期望副本明确，删除、漂移、探针失败和任务完成后状态按控制循环收敛”验收。

#### 4.3.4 使用 ReplicaSet 的更富表达力的标签选择器

**目录节点 19/33。** “使用 ReplicaSet 的更富表达力的标签选择器”服务于“用存活探针、ReplicationController、ReplicaSet、DaemonSet、Job和CronJob管理Pod”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“创建和检查 ReplicaSet”进入“使用 ReplicaSet 的更富表达力的标签选择器”，再到“ReplicaSet 小结”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“控制器选择器、模板和期望副本明确，删除、漂移、探针失败和任务完成后状态按控制循环收敛”验收。

#### 4.3.5 ReplicaSet 小结

**目录节点 20/33。** “ReplicaSet 小结”服务于“用存活探针、ReplicationController、ReplicaSet、DaemonSet、Job和CronJob管理Pod”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“使用 ReplicaSet 的更富表达力的标签选择器”进入“ReplicaSet 小结”，再到“使用 DaemonSet 在每个节点上运行一个 pod”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“控制器选择器、模板和期望副本明确，删除、漂移、探针失败和任务完成后状态按控制循环收敛”验收。

### 4.4 使用 DaemonSet 在每个节点上运行一个 pod

**目录节点 21/33。** “使用 DaemonSet 在每个节点上运行一个 pod”服务于“用存活探针、ReplicationController、ReplicaSet、DaemonSet、Job和CronJob管理Pod”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“ReplicaSet 小结”进入“使用 DaemonSet 在每个节点上运行一个 pod”，再到“使用 DaemonSet 在每个节点上运行一个 pod”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“控制器选择器、模板和期望副本明确，删除、漂移、探针失败和任务完成后状态按控制循环收敛”验收。

#### 4.4.1 使用 DaemonSet 在每个节点上运行一个 pod

**目录节点 22/33。** “使用 DaemonSet 在每个节点上运行一个 pod”服务于“用存活探针、ReplicationController、ReplicaSet、DaemonSet、Job和CronJob管理Pod”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“使用 DaemonSet 在每个节点上运行一个 pod”进入“使用 DaemonSet 在每个节点上运行一个 pod”，再到“使用 DaemonSet 只在特定的节点上运行 pod”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“控制器选择器、模板和期望副本明确，删除、漂移、探针失败和任务完成后状态按控制循环收敛”验收。

#### 4.4.2 使用 DaemonSet 只在特定的节点上运行 pod

**目录节点 23/33。** “使用 DaemonSet 只在特定的节点上运行 pod”服务于“用存活探针、ReplicationController、ReplicaSet、DaemonSet、Job和CronJob管理Pod”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“使用 DaemonSet 在每个节点上运行一个 pod”进入“使用 DaemonSet 只在特定的节点上运行 pod”，再到“运行执行单个任务的 pod”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“控制器选择器、模板和期望副本明确，删除、漂移、探针失败和任务完成后状态按控制循环收敛”验收。

### 4.5 运行执行单个任务的 pod

**目录节点 24/33。** “运行执行单个任务的 pod”服务于“用存活探针、ReplicationController、ReplicaSet、DaemonSet、Job和CronJob管理Pod”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“使用 DaemonSet 只在特定的节点上运行 pod”进入“运行执行单个任务的 pod”，再到“介绍 Job 资源”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“控制器选择器、模板和期望副本明确，删除、漂移、探针失败和任务完成后状态按控制循环收敛”验收。

#### 4.5.1 介绍 Job 资源

**目录节点 25/33。** “介绍 Job 资源”服务于“用存活探针、ReplicationController、ReplicaSet、DaemonSet、Job和CronJob管理Pod”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“运行执行单个任务的 pod”进入“介绍 Job 资源”，再到“定义 Job 资源”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“控制器选择器、模板和期望副本明确，删除、漂移、探针失败和任务完成后状态按控制循环收敛”验收。

#### 4.5.2 定义 Job 资源

**目录节点 26/33。** “定义 Job 资源”服务于“用存活探针、ReplicationController、ReplicaSet、DaemonSet、Job和CronJob管理Pod”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“介绍 Job 资源”进入“定义 Job 资源”，再到“看 Job 运行一个 pod”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“控制器选择器、模板和期望副本明确，删除、漂移、探针失败和任务完成后状态按控制循环收敛”验收。

#### 4.5.3 看 Job 运行一个 pod

**目录节点 27/33。** “看 Job 运行一个 pod”服务于“用存活探针、ReplicationController、ReplicaSet、DaemonSet、Job和CronJob管理Pod”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“定义 Job 资源”进入“看 Job 运行一个 pod”，再到“在 Job 中运行多个 pod 实例”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“控制器选择器、模板和期望副本明确，删除、漂移、探针失败和任务完成后状态按控制循环收敛”验收。

#### 4.5.4 在 Job 中运行多个 pod 实例

**目录节点 28/33。** “在 Job 中运行多个 pod 实例”服务于“用存活探针、ReplicationController、ReplicaSet、DaemonSet、Job和CronJob管理Pod”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“看 Job 运行一个 pod”进入“在 Job 中运行多个 pod 实例”，再到“限制 Job pod 完成任务的时间”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“控制器选择器、模板和期望副本明确，删除、漂移、探针失败和任务完成后状态按控制循环收敛”验收。

#### 4.5.5 限制 Job pod 完成任务的时间

**目录节点 29/33。** “限制 Job pod 完成任务的时间”服务于“用存活探针、ReplicationController、ReplicaSet、DaemonSet、Job和CronJob管理Pod”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“在 Job 中运行多个 pod 实例”进入“限制 Job pod 完成任务的时间”，再到“安排 Job 定期运行或在将来运行一次”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“控制器选择器、模板和期望副本明确，删除、漂移、探针失败和任务完成后状态按控制循环收敛”验收。

### 4.6 安排 Job 定期运行或在将来运行一次

**目录节点 30/33。** “安排 Job 定期运行或在将来运行一次”服务于“用存活探针、ReplicationController、ReplicaSet、DaemonSet、Job和CronJob管理Pod”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“限制 Job pod 完成任务的时间”进入“安排 Job 定期运行或在将来运行一次”，再到“创建一个 CronJob”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“控制器选择器、模板和期望副本明确，删除、漂移、探针失败和任务完成后状态按控制循环收敛”验收。

#### 4.6.1 创建一个 CronJob

**目录节点 31/33。** “创建一个 CronJob”服务于“用存活探针、ReplicationController、ReplicaSet、DaemonSet、Job和CronJob管理Pod”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“安排 Job 定期运行或在将来运行一次”进入“创建一个 CronJob”，再到“了解计划任务的运行方式”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“控制器选择器、模板和期望副本明确，删除、漂移、探针失败和任务完成后状态按控制循环收敛”验收。

#### 4.6.2 了解计划任务的运行方式

**目录节点 32/33。** “了解计划任务的运行方式”服务于“用存活探针、ReplicationController、ReplicaSet、DaemonSet、Job和CronJob管理Pod”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“创建一个 CronJob”进入“了解计划任务的运行方式”，再到“本章小结”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“控制器选择器、模板和期望副本明确，删除、漂移、探针失败和任务完成后状态按控制循环收敛”验收。

### 4.7 本章小结

**目录节点 33/33。** “本章小结”服务于“用存活探针、ReplicationController、ReplicaSet、DaemonSet、Job和CronJob管理Pod”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“了解计划任务的运行方式”进入“本章小结”，再到“本章证据门与回退”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“控制器选择器、模板和期望副本明确，删除、漂移、探针失败和任务完成后状态按控制循环收敛”验收。

## 可复现实验与对象切片

```bash
# 固定Kubernetes 1.8客户端、上下文、命名空间和对象版本。
kubectl version --short
kubectl config current-context
kubectl get --raw /version
kubectl get events --sort-by=.metadata.creationTimestamp
```

```yaml
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: book-lab
spec:
  replicas: 3
  template:
    metadata:
      labels: { app: book-lab }
    spec:
      containers:
        - name: app
          image: example/book-lab@sha256:fixed
          resources:
            requests: { cpu: 100m, memory: 64Mi }
```

```text
evidence = client/server/runtime/api-version and kubeconfig context
         + manifest/uid/resourceVersion/ownerReference/status snapshot
         + api/watch/controller/scheduler/kubelet/event timeline
         + desired/current/ready/restarts/pending/p95/p99 metrics
         + pod/node/network/storage/control-plane fault and business reconciliation
```

## 三步交互实验

<Stepper>
  <Step title="1. 选择目录节点并预测对象链">
    改变期望副本、就绪副本与对象阶段，先预测API、控制器、调度与kubelet路径，再观察。
    <K8s04ReplicationControllersObjectLab />
  </Step>
  <Step title="2. 改变资源、探针与故障条件">
    调整负载、requests、limits、节点与探针故障，比较Pending、重启、可用副本和尾延迟。
    <K8s04ReplicationControllersReconcileLab />
  </Step>
  <Step title="3. 通过端到端证据门">
    目录、版本、对象、控制循环、指标、故障、业务对账与回退齐全后才接受结论。
    <K8s04ReplicationControllersEvidenceLab />
  </Step>
</Stepper>

<Callout type="trap" title="语义陷阱">
  把控制器拥有的Pod当独立资源手工修复，或用错误探针制造持续重启。先写清对象所有权、控制器完成点、节点执行与最终业务不变量，再选择命令或清单。
</Callout>

<Callout type="trap" title="版本陷阱">
  原书以Kubernetes
  1.8.0为基线，ReplicationController、extensions/v1beta1、rkt、Heapster、Service
  Catalog和Federation均有时代语境；现代结果可作迁移差异，不能证明本书行为。
</Callout>

<Callout type="warning" title="隔离实验">
  删除命名空间、驱逐Pod、停止节点、网络隔离、卷回收、证书与RBAC变更只在隔离集群演练。先备份清单、etcd或业务样本并规定停止上界。
</Callout>

## 独立证据与生产交接

目录证据保存18章4附录与404个唯一节点映射；环境证据保存客户端、API服务器、etcd、容器运行时、网络与存储插件版本；对象证据保存清单、UID、resourceVersion、ownerReference、spec和status；运行证据保存事件、控制器、调度、kubelet、探针、流量和最终业务结果。

验收记录按一次变更一个编号：记录上下文、命名空间、操作者、开始结束时间、对象前后状态、请求样本、预期与实际结果、日志和指标窗口。失败时保留原始事件、Pending或终止原因、影响范围、停止条件和回退验证；成功时也须由另一位读者按同一材料重放。

性能判断固定镜像、请求分布、副本、资源、探针、Service、节点和存储语义。扩大limit或关闭探针换来的吞吐不是等价优化。平均值会隐藏调度等待、镜像拉取、CPU节流、OOM、控制器重试、Endpoint切换和卷挂载，必须保留P95/P99与状态时间线。

恢复判断从业务状态结束。Pod Running、节点Ready、Deployment Available、PVC Bound或API可访问都只是阶段；还要核对客户端请求、数据集合、身份权限、对象版本和旧实例是否停止提供错误结果。

## 本章回顾

重新完成“用存活探针、ReplicationController、ReplicaSet、DaemonSet、Job和CronJob管理Pod”：固定Kubernetes 1.8和第1版目录，从声明式对象与控制循环推导边界，通过基线、压力与故障实验测量，最后交付探针实验、RC与RS对照、DaemonSet节点覆盖、Job完成证据和CronJob调度记录。只有“控制器选择器、模板和期望副本明确，删除、漂移、探针失败和任务完成后状态按控制循环收敛”与反例都能独立重放，本页才算完成。

## 复习与运行验收

<Exercises>
**问题 1**：为什么“第4章 副本机制和其他控制器 ：部署托管的 pod”必须覆盖33个目录节点？
<Answer>这些节点共同组成“用存活探针、ReplicationController、ReplicaSet、DaemonSet、Job和CronJob管理Pod”的对象、控制循环、运行与证据链；漏项会使探针实验、RC与RS对照、DaemonSet节点覆盖、Job完成证据和CronJob调度记录无法独立复现。</Answer>

**问题 2**：本页最小不变量是什么？

<Answer>
  控制器选择器、模板和期望副本明确，删除、漂移、探针失败和任务完成后状态按控制循环收敛。需要版本、对象、控制器轨迹、指标、故障与业务对账共同证明。
</Answer>

**问题 3**：怎样构造最小反例？

<Answer>
  固定Kubernetes
  1.8、集群、命名空间、镜像和清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一，再比较状态轨迹与最终业务结果。
</Answer>

**问题 4**：为什么不能直接用现代Kubernetes替代本页实验？

<Answer>
  原书源码以Kubernetes
  1.8.0为基线；API版本、运行时、网络、安全、伸缩和多集群机制后来均有变化，新版只能作为差异材料。
</Answer>

**问题 5**：如何验证性能和恢复结论？

<Answer>
  固定镜像、请求分布、副本、资源、探针、网络和存储语义，重复测期望/当前/就绪、重启、Pending、吞吐、P95/P99和错误，并独立对账。
</Answer>

**问题 6**：独立交接需要什么？

<Answer>需要完整目录映射、版本环境、对象快照、控制循环轨迹、探针实验、RC与RS对照、DaemonSet节点覆盖、Job完成证据和CronJob调度记录、负载、故障、对账、停止、恢复、回退和责任人。</Answer>
</Exercises>

<Glossary>
  <GlossaryItem term="存活探针">
    存活探针在本页对应Kubernetes
    1.8中的对象、字段或状态；适用范围由“控制器选择器、模板和期望副本明确，删除、漂移、探针失败和任务完成后状态按控制循环收敛”和故障反例共同限定。
  </GlossaryItem>
  <GlossaryItem term="ReplicationController">
    ReplicationController在本页对应Kubernetes
    1.8中的对象、字段或状态；适用范围由“控制器选择器、模板和期望副本明确，删除、漂移、探针失败和任务完成后状态按控制循环收敛”和故障反例共同限定。
  </GlossaryItem>
  <GlossaryItem term="ReplicaSet">
    ReplicaSet在本页对应Kubernetes
    1.8中的对象、字段或状态；适用范围由“控制器选择器、模板和期望副本明确，删除、漂移、探针失败和任务完成后状态按控制循环收敛”和故障反例共同限定。
  </GlossaryItem>
  <GlossaryItem term="DaemonSet">
    DaemonSet在本页对应Kubernetes
    1.8中的对象、字段或状态；适用范围由“控制器选择器、模板和期望副本明确，删除、漂移、探针失败和任务完成后状态按控制循环收敛”和故障反例共同限定。
  </GlossaryItem>
  <GlossaryItem term="Job">
    Job在本页对应Kubernetes
    1.8中的对象、字段或状态；适用范围由“控制器选择器、模板和期望副本明确，删除、漂移、探针失败和任务完成后状态按控制循环收敛”和故障反例共同限定。
  </GlossaryItem>
</Glossary>

[← 上一页：第3章 pod ：运行于 Kubernetes 中的容器](/learn/kubernetes-in-action/03-03-pods/k8s-03-pods) · [下一页：第5章 服务 ：让客户端发现 pod 并与之通信 →](/learn/kubernetes-in-action/05-05-services/k8s-05-services)

<Attribution
  adaptedFrom="Marko Lukša《Kubernetes in Action》第1版"
  adaptedUrl="https://www.phei.com.cn/module/goods/wssd_content.jsp?bookid=53240"
/>
