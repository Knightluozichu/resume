---
title: "第11章 了解 Kubernetes 机理"
description: "依据电子工业出版社第1版完整目录：追踪API服务器、etcd、调度器、控制器、kubelet、kube-proxy、插件和高可用协作"
type: C
section: "Kubernetes in Action中文版（第1版 / Kubernetes 1.8 / 2018）"
order: 11
draft: true
math: false
sourceUrl: "https://www.phei.com.cn/module/goods/wssd_content.jsp?bookid=53240"
demo: true
---

# 第11章 了解 Kubernetes 机理

本课程对应Marko Lukša著《Kubernetes in Action中文版》第1版，七牛容器云团队译，电子工业出版社2018年12月出版，592页，ISBN 9787121349959；英文原版由Manning于2017年12月出版。作者第1版源码第8章明确下载Kubernetes 1.8.0客户端，课程据此固定行为边界。

全书正式分母为18章与附录A至D，共22个正式单元、404个唯一章/附录/节节点；另设学习地图和总复习，共24页。第2版、现代apps/v1写法、EndpointSlice、Gateway API、Pod Security Admission或后来的多集群方案只能作为差异材料，不能替代第1版。本页逐项覆盖27个目录节点，最终交付控制面拓扑、etcd对象、watch事件链、调度与kubelet轨迹、iptables和高可用实验。

<Objectives>

- 能解释“第11章 了解 Kubernetes 机理”的27个目录节点，并绘制API对象、控制器、调度、节点执行、网络与存储边界。
- 能比较基线、压力、对象变化、Pod或节点故障和恢复轨迹，判断期望状态、实际状态与业务完成点。
- 能设计并复现控制面拓扑、etcd对象、watch事件链、调度与kubelet轨迹、iptables和高可用实验，用清单、事件、日志、指标与最终状态支持结论。
- 能分析“把所有组件称作主节点黑盒，无法区分持久化、决策、执行和网络转发故障”为何失败，并写出停止、恢复、回退与独立交接条件。

</Objectives>

## 从一次声明式变更开始

先预测：kubectl提交对象后，API服务器完成哪些认证、授权、准入和持久化步骤，哪个控制器watch到变化，调度器如何选择节点，kubelet怎样创建容器，Service、卷与探针何时就绪。观察前先写预测，再按resourceVersion、UID、ownerReference和事件时间线修正模型。

Kubernetes 1.8的完成不是命令返回created。对象进入etcd、控制器观察、创建下级对象、Pod被调度、镜像启动、探针通过、Endpoint接入流量和业务结果稳定是不同检查点。控制循环允许中间状态，验收必须明确等待条件和超时后的诊断路径。

## 核心词汇与版本边界

<Term def="etcd是第11章 了解 Kubernetes 机理的第1个核心概念，必须同时说明Kubernetes 1.8对象、控制循环、故障边界和观测证据">etcd</Term>、<Term def="watch是第11章 了解 Kubernetes 机理的第2个核心概念，必须同时说明Kubernetes 1.8对象、控制循环、故障边界和观测证据">watch</Term>、<Term def="调度器是第11章 了解 Kubernetes 机理的第3个核心概念，必须同时说明Kubernetes 1.8对象、控制循环、故障边界和观测证据">调度器</Term>、<Term def="控制器管理器是第11章 了解 Kubernetes 机理的第4个核心概念，必须同时说明Kubernetes 1.8对象、控制循环、故障边界和观测证据">控制器管理器</Term>、<Term def="kubelet是第11章 了解 Kubernetes 机理的第5个核心概念，必须同时说明Kubernetes 1.8对象、控制循环、故障边界和观测证据">kubelet</Term>

这些词构成本页词汇。每个词都要回答：对象由谁声明，谁拥有，谁调谐，期望与实际状态保存在哪里，失败后会重试还是终止，怎样观测，以及Kubernetes 1.8之后哪些变化不属于本书。

<K8s11InternalsObjectLab />

## 原书目录逐节点重构

### 11.1 了解架构

**目录节点 1/27。** “了解架构”服务于“追踪API服务器、etcd、调度器、控制器、kubelet、kube-proxy、插件和高可用协作”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“本章输入与版本契约”进入“了解架构”，再到“Kubernetes 组件的分布式特性”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“一次声明从持久化、watch、控制循环、调度到节点执行的事件链完整，组件失败边界可定位”验收。

#### 11.1.1 Kubernetes 组件的分布式特性

**目录节点 2/27。** “Kubernetes 组件的分布式特性”服务于“追踪API服务器、etcd、调度器、控制器、kubelet、kube-proxy、插件和高可用协作”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“了解架构”进入“Kubernetes 组件的分布式特性”，再到“Kubernetes 如何使用 etcd”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“一次声明从持久化、watch、控制循环、调度到节点执行的事件链完整，组件失败边界可定位”验收。

#### 11.1.2 Kubernetes 如何使用 etcd

**目录节点 3/27。** “Kubernetes 如何使用 etcd”服务于“追踪API服务器、etcd、调度器、控制器、kubelet、kube-proxy、插件和高可用协作”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“Kubernetes 组件的分布式特性”进入“Kubernetes 如何使用 etcd”，再到“API 服务器做了什么”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“一次声明从持久化、watch、控制循环、调度到节点执行的事件链完整，组件失败边界可定位”验收。

#### 11.1.3 API 服务器做了什么

**目录节点 4/27。** “API 服务器做了什么”服务于“追踪API服务器、etcd、调度器、控制器、kubelet、kube-proxy、插件和高可用协作”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“Kubernetes 如何使用 etcd”进入“API 服务器做了什么”，再到“API 服务器如何通知客户端资源变更”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“一次声明从持久化、watch、控制循环、调度到节点执行的事件链完整，组件失败边界可定位”验收。

#### 11.1.4 API 服务器如何通知客户端资源变更

**目录节点 5/27。** “API 服务器如何通知客户端资源变更”服务于“追踪API服务器、etcd、调度器、控制器、kubelet、kube-proxy、插件和高可用协作”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“API 服务器做了什么”进入“API 服务器如何通知客户端资源变更”，再到“了解调度器”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“一次声明从持久化、watch、控制循环、调度到节点执行的事件链完整，组件失败边界可定位”验收。

#### 11.1.5 了解调度器

**目录节点 6/27。** “了解调度器”服务于“追踪API服务器、etcd、调度器、控制器、kubelet、kube-proxy、插件和高可用协作”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“API 服务器如何通知客户端资源变更”进入“了解调度器”，再到“介绍控制器管理器中运行的控制器”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“一次声明从持久化、watch、控制循环、调度到节点执行的事件链完整，组件失败边界可定位”验收。

#### 11.1.6 介绍控制器管理器中运行的控制器

**目录节点 7/27。** “介绍控制器管理器中运行的控制器”服务于“追踪API服务器、etcd、调度器、控制器、kubelet、kube-proxy、插件和高可用协作”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“了解调度器”进入“介绍控制器管理器中运行的控制器”，再到“Kubelet 做了什么”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“一次声明从持久化、watch、控制循环、调度到节点执行的事件链完整，组件失败边界可定位”验收。

#### 11.1.7 Kubelet 做了什么

**目录节点 8/27。** “Kubelet 做了什么”服务于“追踪API服务器、etcd、调度器、控制器、kubelet、kube-proxy、插件和高可用协作”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“介绍控制器管理器中运行的控制器”进入“Kubelet 做了什么”，再到“Kubernetes Service Proxy 的作用”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“一次声明从持久化、watch、控制循环、调度到节点执行的事件链完整，组件失败边界可定位”验收。

#### 11.1.8 Kubernetes Service Proxy 的作用

**目录节点 9/27。** “Kubernetes Service Proxy 的作用”服务于“追踪API服务器、etcd、调度器、控制器、kubelet、kube-proxy、插件和高可用协作”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“Kubelet 做了什么”进入“Kubernetes Service Proxy 的作用”，再到“介绍 Kubernetes 插件”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“一次声明从持久化、watch、控制循环、调度到节点执行的事件链完整，组件失败边界可定位”验收。

#### 11.1.9 介绍 Kubernetes 插件

**目录节点 10/27。** “介绍 Kubernetes 插件”服务于“追踪API服务器、etcd、调度器、控制器、kubelet、kube-proxy、插件和高可用协作”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“Kubernetes Service Proxy 的作用”进入“介绍 Kubernetes 插件”，再到“总结概览”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“一次声明从持久化、watch、控制循环、调度到节点执行的事件链完整，组件失败边界可定位”验收。

#### 11.1.10 总结概览

**目录节点 11/27。** “总结概览”服务于“追踪API服务器、etcd、调度器、控制器、kubelet、kube-proxy、插件和高可用协作”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“介绍 Kubernetes 插件”进入“总结概览”，再到“控制器如何协作”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“一次声明从持久化、watch、控制循环、调度到节点执行的事件链完整，组件失败边界可定位”验收。

### 11.2 控制器如何协作

**目录节点 12/27。** “控制器如何协作”服务于“追踪API服务器、etcd、调度器、控制器、kubelet、kube-proxy、插件和高可用协作”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“总结概览”进入“控制器如何协作”，再到“了解涉及哪些组件”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“一次声明从持久化、watch、控制循环、调度到节点执行的事件链完整，组件失败边界可定位”验收。

#### 11.2.1 了解涉及哪些组件

**目录节点 13/27。** “了解涉及哪些组件”服务于“追踪API服务器、etcd、调度器、控制器、kubelet、kube-proxy、插件和高可用协作”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“控制器如何协作”进入“了解涉及哪些组件”，再到“事件链”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“一次声明从持久化、watch、控制循环、调度到节点执行的事件链完整，组件失败边界可定位”验收。

#### 11.2.2 事件链

**目录节点 14/27。** “事件链”服务于“追踪API服务器、etcd、调度器、控制器、kubelet、kube-proxy、插件和高可用协作”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“了解涉及哪些组件”进入“事件链”，再到“观察集群事件”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“一次声明从持久化、watch、控制循环、调度到节点执行的事件链完整，组件失败边界可定位”验收。

#### 11.2.3 观察集群事件

**目录节点 15/27。** “观察集群事件”服务于“追踪API服务器、etcd、调度器、控制器、kubelet、kube-proxy、插件和高可用协作”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“事件链”进入“观察集群事件”，再到“了解运行中的 pod 是什么”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“一次声明从持久化、watch、控制循环、调度到节点执行的事件链完整，组件失败边界可定位”验收。

### 11.3 了解运行中的 pod 是什么

**目录节点 16/27。** “了解运行中的 pod 是什么”服务于“追踪API服务器、etcd、调度器、控制器、kubelet、kube-proxy、插件和高可用协作”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“观察集群事件”进入“了解运行中的 pod 是什么”，再到“跨 pod 网络”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“一次声明从持久化、watch、控制循环、调度到节点执行的事件链完整，组件失败边界可定位”验收。

### 11.4 跨 pod 网络

**目录节点 17/27。** “跨 pod 网络”服务于“追踪API服务器、etcd、调度器、控制器、kubelet、kube-proxy、插件和高可用协作”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“了解运行中的 pod 是什么”进入“跨 pod 网络”，再到“网络应该是什么样的”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“一次声明从持久化、watch、控制循环、调度到节点执行的事件链完整，组件失败边界可定位”验收。

#### 11.4.1 网络应该是什么样的

**目录节点 18/27。** “网络应该是什么样的”服务于“追踪API服务器、etcd、调度器、控制器、kubelet、kube-proxy、插件和高可用协作”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“跨 pod 网络”进入“网络应该是什么样的”，再到“深入了解网络工作原理”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“一次声明从持久化、watch、控制循环、调度到节点执行的事件链完整，组件失败边界可定位”验收。

#### 11.4.2 深入了解网络工作原理

**目录节点 19/27。** “深入了解网络工作原理”服务于“追踪API服务器、etcd、调度器、控制器、kubelet、kube-proxy、插件和高可用协作”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“网络应该是什么样的”进入“深入了解网络工作原理”，再到“引入容器网络接口”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“一次声明从持久化、watch、控制循环、调度到节点执行的事件链完整，组件失败边界可定位”验收。

#### 11.4.3 引入容器网络接口

**目录节点 20/27。** “引入容器网络接口”服务于“追踪API服务器、etcd、调度器、控制器、kubelet、kube-proxy、插件和高可用协作”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“深入了解网络工作原理”进入“引入容器网络接口”，再到“服务是如何实现的”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“一次声明从持久化、watch、控制循环、调度到节点执行的事件链完整，组件失败边界可定位”验收。

### 11.5 服务是如何实现的

**目录节点 21/27。** “服务是如何实现的”服务于“追踪API服务器、etcd、调度器、控制器、kubelet、kube-proxy、插件和高可用协作”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“引入容器网络接口”进入“服务是如何实现的”，再到“引入 kube-proxy”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“一次声明从持久化、watch、控制循环、调度到节点执行的事件链完整，组件失败边界可定位”验收。

#### 11.5.1 引入 kube-proxy

**目录节点 22/27。** “引入 kube-proxy”服务于“追踪API服务器、etcd、调度器、控制器、kubelet、kube-proxy、插件和高可用协作”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“服务是如何实现的”进入“引入 kube-proxy”，再到“kube-proxy 如何使用 iptables”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“一次声明从持久化、watch、控制循环、调度到节点执行的事件链完整，组件失败边界可定位”验收。

#### 11.5.2 kube-proxy 如何使用 iptables

**目录节点 23/27。** “kube-proxy 如何使用 iptables”服务于“追踪API服务器、etcd、调度器、控制器、kubelet、kube-proxy、插件和高可用协作”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“引入 kube-proxy”进入“kube-proxy 如何使用 iptables”，再到“运行高可用集群”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“一次声明从持久化、watch、控制循环、调度到节点执行的事件链完整，组件失败边界可定位”验收。

### 11.6 运行高可用集群

**目录节点 24/27。** “运行高可用集群”服务于“追踪API服务器、etcd、调度器、控制器、kubelet、kube-proxy、插件和高可用协作”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“kube-proxy 如何使用 iptables”进入“运行高可用集群”，再到“让你的应用变得高可用”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“一次声明从持久化、watch、控制循环、调度到节点执行的事件链完整，组件失败边界可定位”验收。

#### 11.6.1 让你的应用变得高可用

**目录节点 25/27。** “让你的应用变得高可用”服务于“追踪API服务器、etcd、调度器、控制器、kubelet、kube-proxy、插件和高可用协作”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“运行高可用集群”进入“让你的应用变得高可用”，再到“让 Kubernetes 控制平面变得高可用”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“一次声明从持久化、watch、控制循环、调度到节点执行的事件链完整，组件失败边界可定位”验收。

#### 11.6.2 让 Kubernetes 控制平面变得高可用

**目录节点 26/27。** “让 Kubernetes 控制平面变得高可用”服务于“追踪API服务器、etcd、调度器、控制器、kubelet、kube-proxy、插件和高可用协作”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“让你的应用变得高可用”进入“让 Kubernetes 控制平面变得高可用”，再到“本章小结”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“一次声明从持久化、watch、控制循环、调度到节点执行的事件链完整，组件失败边界可定位”验收。

### 11.7 本章小结

**目录节点 27/27。** “本章小结”服务于“追踪API服务器、etcd、调度器、控制器、kubelet、kube-proxy、插件和高可用协作”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“让 Kubernetes 控制平面变得高可用”进入“本章小结”，再到“本章证据门与回退”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“一次声明从持久化、watch、控制循环、调度到节点执行的事件链完整，组件失败边界可定位”验收。

<Callout type="trap" title="语义陷阱">
  把所有组件称作主节点黑盒，无法区分持久化、决策、执行和网络转发故障。先写清对象所有权、控制器完成点、节点执行与最终业务不变量，再选择命令或清单。
</Callout>

<Callout type="trap" title="版本陷阱">
  原书以Kubernetes
  1.8.0为基线，ReplicationController、extensions/v1beta1、rkt、Heapster、Service
  Catalog和Federation均有时代语境；现代结果可作迁移差异，不能证明本书行为。
</Callout>

<Callout type="warning" title="隔离实验">
  删除命名空间、驱逐Pod、停止节点、网络隔离、卷回收、证书与RBAC变更只在隔离集群演练。先备份清单、etcd或业务样本并规定停止上界。
</Callout>

## 独立证据与生产交接

目录证据保存18章4附录与404个唯一节点映射；环境证据保存客户端、API服务器、etcd、容器运行时、网络与存储插件版本；对象证据保存清单、UID、resourceVersion、ownerReference、spec和status；运行证据保存事件、控制器、调度、kubelet、探针、流量和最终业务结果。

验收记录按一次变更一个编号：记录上下文、命名空间、操作者、开始结束时间、对象前后状态、请求样本、预期与实际结果、日志和指标窗口。失败时保留原始事件、Pending或终止原因、影响范围、停止条件和回退验证；成功时也须由另一位读者按同一材料重放。

性能判断固定镜像、请求分布、副本、资源、探针、Service、节点和存储语义。扩大limit或关闭探针换来的吞吐不是等价优化。平均值会隐藏调度等待、镜像拉取、CPU节流、OOM、控制器重试、Endpoint切换和卷挂载，必须保留P95/P99与状态时间线。

恢复判断从业务状态结束。Pod Running、节点Ready、Deployment Available、PVC Bound或API可访问都只是阶段；还要核对客户端请求、数据集合、身份权限、对象版本和旧实例是否停止提供错误结果。

## 本章回顾

重新完成“追踪API服务器、etcd、调度器、控制器、kubelet、kube-proxy、插件和高可用协作”：固定Kubernetes 1.8和第1版目录，从声明式对象与控制循环推导边界，通过基线、压力与故障实验测量，最后交付控制面拓扑、etcd对象、watch事件链、调度与kubelet轨迹、iptables和高可用实验。只有“一次声明从持久化、watch、控制循环、调度到节点执行的事件链完整，组件失败边界可定位”与反例都能独立重放，本页才算完成。

## 复习与运行验收

<Exercises>
**问题 1**：为什么“第11章 了解 Kubernetes 机理”必须覆盖27个目录节点？
<Answer>这些节点共同组成“追踪API服务器、etcd、调度器、控制器、kubelet、kube-proxy、插件和高可用协作”的对象、控制循环、运行与证据链；漏项会使控制面拓扑、etcd对象、watch事件链、调度与kubelet轨迹、iptables和高可用实验无法独立复现。</Answer>

**问题 2**：本页最小不变量是什么？

<Answer>
  一次声明从持久化、watch、控制循环、调度到节点执行的事件链完整，组件失败边界可定位。需要版本、对象、控制器轨迹、指标、故障与业务对账共同证明。
</Answer>

**问题 3**：怎样构造最小反例？

<Answer>
  固定Kubernetes
  1.8、集群、命名空间、镜像和清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一，再比较状态轨迹与最终业务结果。
</Answer>

**问题 4**：为什么不能直接用现代Kubernetes替代本页实验？

<Answer>
  原书源码以Kubernetes
  1.8.0为基线；API版本、运行时、网络、安全、伸缩和多集群机制后来均有变化，新版只能作为差异材料。
</Answer>

**问题 5**：如何验证性能和恢复结论？

<Answer>
  固定镜像、请求分布、副本、资源、探针、网络和存储语义，重复测期望/当前/就绪、重启、Pending、吞吐、P95/P99和错误，并独立对账。
</Answer>

**问题 6**：独立交接需要什么？

<Answer>需要完整目录映射、版本环境、对象快照、控制循环轨迹、控制面拓扑、etcd对象、watch事件链、调度与kubelet轨迹、iptables和高可用实验、负载、故障、对账、停止、恢复、回退和责任人。</Answer>
</Exercises>

<Glossary>
  <GlossaryItem term="etcd">
    etcd在本页对应Kubernetes
    1.8中的对象、字段或状态；适用范围由“一次声明从持久化、watch、控制循环、调度到节点执行的事件链完整，组件失败边界可定位”和故障反例共同限定。
  </GlossaryItem>
  <GlossaryItem term="watch">
    watch在本页对应Kubernetes
    1.8中的对象、字段或状态；适用范围由“一次声明从持久化、watch、控制循环、调度到节点执行的事件链完整，组件失败边界可定位”和故障反例共同限定。
  </GlossaryItem>
  <GlossaryItem term="调度器">
    调度器在本页对应Kubernetes
    1.8中的对象、字段或状态；适用范围由“一次声明从持久化、watch、控制循环、调度到节点执行的事件链完整，组件失败边界可定位”和故障反例共同限定。
  </GlossaryItem>
  <GlossaryItem term="控制器管理器">
    控制器管理器在本页对应Kubernetes
    1.8中的对象、字段或状态；适用范围由“一次声明从持久化、watch、控制循环、调度到节点执行的事件链完整，组件失败边界可定位”和故障反例共同限定。
  </GlossaryItem>
  <GlossaryItem term="kubelet">
    kubelet在本页对应Kubernetes
    1.8中的对象、字段或状态；适用范围由“一次声明从持久化、watch、控制循环、调度到节点执行的事件链完整，组件失败边界可定位”和故障反例共同限定。
  </GlossaryItem>
</Glossary>

[← 上一页：第10章 StatefulSet ：部署有状态的多副本应用](/learn/kubernetes-in-action/10-10-statefulsets/k8s-10-statefulsets) · [下一页：第12章 Kubernetes API 服务器的安全防护 →](/learn/kubernetes-in-action/12-12-api-security/k8s-12-api-security)

<Attribution
  adaptedFrom="Marko Lukša《Kubernetes in Action》第1版"
  adaptedUrl="https://www.phei.com.cn/module/goods/wssd_content.jsp?bookid=53240"
/>
