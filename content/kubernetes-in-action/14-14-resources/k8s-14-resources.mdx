---
title: "第14章 计算资源管理"
description: "依据电子工业出版社第1版完整目录：用requests、limits、QoS、LimitRange、ResourceQuota和监控管理计算资源"
type: C
section: "Kubernetes in Action中文版（第1版 / Kubernetes 1.8 / 2018）"
order: 14
draft: false
math: false
sourceUrl: "https://www.phei.com.cn/module/goods/wssd_content.jsp?bookid=53240"
demo: true
---

import { Objectives } from "@/components/mdx/objectives";
import { Term } from "@/components/mdx/term";
import { Callout } from "@/components/mdx/callout";
import { Answer, Exercises } from "@/components/mdx/exercises";
import { Glossary, GlossaryItem } from "@/components/mdx/glossary";
import { Attribution } from "@/components/mdx/attribution";

# 第14章 计算资源管理

本课程对应Marko Lukša著《Kubernetes in Action中文版》第1版，七牛容器云团队译，电子工业出版社2018年12月出版，592页，ISBN 9787121349959；英文原版由Manning于2017年12月出版。作者第1版源码第8章明确下载Kubernetes 1.8.0客户端，课程据此固定行为边界。

全书正式分母为18章与附录A至D，共22个正式单元、404个唯一章/附录/节节点；另设学习地图和总复习，共24页。第2版、现代apps/v1写法、EndpointSlice、Gateway API、Pod Security Admission或后来的多集群方案只能作为差异材料，不能替代第1版。本页逐项覆盖26个目录节点，最终交付资源模型、调度实验、limits反例、QoS驱逐、LimitRange、Quota与历史监控。

<Objectives>

- 能解释“第14章 计算资源管理”的26个目录节点，并绘制API对象、控制器、调度、节点执行、网络与存储边界。
- 能比较基线、压力、对象变化、Pod或节点故障和恢复轨迹，判断期望状态、实际状态与业务完成点。
- 能设计并复现资源模型、调度实验、limits反例、QoS驱逐、LimitRange、Quota与历史监控，用清单、事件、日志、指标与最终状态支持结论。
- 能分析“把CPU使用率当request，或用limit代替容量规划，导致调度过密、节流和OOM”为何失败，并写出停止、恢复、回退与独立交接条件。

</Objectives>

## 从一次声明式变更开始

先预测：kubectl提交对象后，API服务器完成哪些认证、授权、准入和持久化步骤，哪个控制器watch到变化，调度器如何选择节点，kubelet怎样创建容器，Service、卷与探针何时就绪。观察前先写预测，再按resourceVersion、UID、ownerReference和事件时间线修正模型。

Kubernetes 1.8的完成不是命令返回created。对象进入etcd、控制器观察、创建下级对象、Pod被调度、镜像启动、探针通过、Endpoint接入流量和业务结果稳定是不同检查点。控制循环允许中间状态，验收必须明确等待条件和超时后的诊断路径。

## 核心词汇与版本边界

<Term def="requests是第14章 计算资源管理的第1个核心概念，必须同时说明Kubernetes 1.8对象、控制循环、故障边界和观测证据">requests</Term>、<Term def="limits是第14章 计算资源管理的第2个核心概念，必须同时说明Kubernetes 1.8对象、控制循环、故障边界和观测证据">limits</Term>、<Term def="QoS是第14章 计算资源管理的第3个核心概念，必须同时说明Kubernetes 1.8对象、控制循环、故障边界和观测证据">QoS</Term>、<Term def="LimitRange是第14章 计算资源管理的第4个核心概念，必须同时说明Kubernetes 1.8对象、控制循环、故障边界和观测证据">LimitRange</Term>、<Term def="ResourceQuota是第14章 计算资源管理的第5个核心概念，必须同时说明Kubernetes 1.8对象、控制循环、故障边界和观测证据">ResourceQuota</Term>

这些词构成本页词汇。每个词都要回答：对象由谁声明，谁拥有，谁调谐，期望与实际状态保存在哪里，失败后会重试还是终止，怎样观测，以及Kubernetes 1.8之后哪些变化不属于本书。


## 原书目录逐节点重构

### 14.1 为 pod 中的容器申请资源

**目录节点 1/26。** “为 pod 中的容器申请资源”服务于“用requests、limits、QoS、LimitRange、ResourceQuota和监控管理计算资源”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“本章输入与版本契约”进入“为 pod 中的容器申请资源”，再到“创建包含资源 requests 的 pod”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“调度按requests决策，运行时按limits约束，OOM和CPU节流可解释，命名空间配额不能被绕过”验收。

#### 14.1.1 创建包含资源 requests 的 pod

**目录节点 2/26。** “创建包含资源 requests 的 pod”服务于“用requests、limits、QoS、LimitRange、ResourceQuota和监控管理计算资源”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“为 pod 中的容器申请资源”进入“创建包含资源 requests 的 pod”，再到“资源 requests 如何影响调度”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“调度按requests决策，运行时按limits约束，OOM和CPU节流可解释，命名空间配额不能被绕过”验收。

#### 14.1.2 资源 requests 如何影响调度

**目录节点 3/26。** “资源 requests 如何影响调度”服务于“用requests、limits、QoS、LimitRange、ResourceQuota和监控管理计算资源”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“创建包含资源 requests 的 pod”进入“资源 requests 如何影响调度”，再到“CPU requests 如何影响 CPU 时间分配”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“调度按requests决策，运行时按limits约束，OOM和CPU节流可解释，命名空间配额不能被绕过”验收。

#### 14.1.3 CPU requests 如何影响 CPU 时间分配

**目录节点 4/26。** “CPU requests 如何影响 CPU 时间分配”服务于“用requests、limits、QoS、LimitRange、ResourceQuota和监控管理计算资源”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“资源 requests 如何影响调度”进入“CPU requests 如何影响 CPU 时间分配”，再到“定义和申请自定义资源”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“调度按requests决策，运行时按limits约束，OOM和CPU节流可解释，命名空间配额不能被绕过”验收。

#### 14.1.4 定义和申请自定义资源

**目录节点 5/26。** “定义和申请自定义资源”服务于“用requests、limits、QoS、LimitRange、ResourceQuota和监控管理计算资源”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“CPU requests 如何影响 CPU 时间分配”进入“定义和申请自定义资源”，再到“限制容器的可用资源”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“调度按requests决策，运行时按limits约束，OOM和CPU节流可解释，命名空间配额不能被绕过”验收。

### 14.2 限制容器的可用资源

**目录节点 6/26。** “限制容器的可用资源”服务于“用requests、limits、QoS、LimitRange、ResourceQuota和监控管理计算资源”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“定义和申请自定义资源”进入“限制容器的可用资源”，再到“设置容器可使用资源量的硬限制”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“调度按requests决策，运行时按limits约束，OOM和CPU节流可解释，命名空间配额不能被绕过”验收。

#### 14.2.1 设置容器可使用资源量的硬限制

**目录节点 7/26。** “设置容器可使用资源量的硬限制”服务于“用requests、limits、QoS、LimitRange、ResourceQuota和监控管理计算资源”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“限制容器的可用资源”进入“设置容器可使用资源量的硬限制”，再到“超过 limits”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“调度按requests决策，运行时按limits约束，OOM和CPU节流可解释，命名空间配额不能被绕过”验收。

#### 14.2.2 超过 limits

**目录节点 8/26。** “超过 limits”服务于“用requests、limits、QoS、LimitRange、ResourceQuota和监控管理计算资源”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“设置容器可使用资源量的硬限制”进入“超过 limits”，再到“容器中的应用如何看待 limits”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“调度按requests决策，运行时按limits约束，OOM和CPU节流可解释，命名空间配额不能被绕过”验收。

#### 14.2.3 容器中的应用如何看待 limits

**目录节点 9/26。** “容器中的应用如何看待 limits”服务于“用requests、limits、QoS、LimitRange、ResourceQuota和监控管理计算资源”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“超过 limits”进入“容器中的应用如何看待 limits”，再到“了解 pod QoS 等级”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“调度按requests决策，运行时按limits约束，OOM和CPU节流可解释，命名空间配额不能被绕过”验收。

### 14.3 了解 pod QoS 等级

**目录节点 10/26。** “了解 pod QoS 等级”服务于“用requests、limits、QoS、LimitRange、ResourceQuota和监控管理计算资源”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“容器中的应用如何看待 limits”进入“了解 pod QoS 等级”，再到“定义 pod 的 QoS 等级”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“调度按requests决策，运行时按limits约束，OOM和CPU节流可解释，命名空间配额不能被绕过”验收。

#### 14.3.1 定义 pod 的 QoS 等级

**目录节点 11/26。** “定义 pod 的 QoS 等级”服务于“用requests、limits、QoS、LimitRange、ResourceQuota和监控管理计算资源”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“了解 pod QoS 等级”进入“定义 pod 的 QoS 等级”，再到“内存不足时哪个进程会被杀死”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“调度按requests决策，运行时按limits约束，OOM和CPU节流可解释，命名空间配额不能被绕过”验收。

#### 14.3.2 内存不足时哪个进程会被杀死

**目录节点 12/26。** “内存不足时哪个进程会被杀死”服务于“用requests、limits、QoS、LimitRange、ResourceQuota和监控管理计算资源”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“定义 pod 的 QoS 等级”进入“内存不足时哪个进程会被杀死”，再到“为命名空间中的 pod 设置默认的 requests 和 limits”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“调度按requests决策，运行时按limits约束，OOM和CPU节流可解释，命名空间配额不能被绕过”验收。

### 14.4 为命名空间中的 pod 设置默认的 requests 和 limits

**目录节点 13/26。** “为命名空间中的 pod 设置默认的 requests 和 limits”服务于“用requests、limits、QoS、LimitRange、ResourceQuota和监控管理计算资源”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“内存不足时哪个进程会被杀死”进入“为命名空间中的 pod 设置默认的 requests 和 limits”，再到“LimitRange 资源简介”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“调度按requests决策，运行时按limits约束，OOM和CPU节流可解释，命名空间配额不能被绕过”验收。

#### 14.4.1 LimitRange 资源简介

**目录节点 14/26。** “LimitRange 资源简介”服务于“用requests、limits、QoS、LimitRange、ResourceQuota和监控管理计算资源”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“为命名空间中的 pod 设置默认的 requests 和 limits”进入“LimitRange 资源简介”，再到“LimitRange 对象的创建”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“调度按requests决策，运行时按limits约束，OOM和CPU节流可解释，命名空间配额不能被绕过”验收。

#### 14.4.2 LimitRange 对象的创建

**目录节点 15/26。** “LimitRange 对象的创建”服务于“用requests、limits、QoS、LimitRange、ResourceQuota和监控管理计算资源”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“LimitRange 资源简介”进入“LimitRange 对象的创建”，再到“强制进行限制”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“调度按requests决策，运行时按limits约束，OOM和CPU节流可解释，命名空间配额不能被绕过”验收。

#### 14.4.3 强制进行限制

**目录节点 16/26。** “强制进行限制”服务于“用requests、limits、QoS、LimitRange、ResourceQuota和监控管理计算资源”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“LimitRange 对象的创建”进入“强制进行限制”，再到“应用资源 requests 和 limits 的默认值”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“调度按requests决策，运行时按limits约束，OOM和CPU节流可解释，命名空间配额不能被绕过”验收。

#### 14.4.4 应用资源 requests 和 limits 的默认值

**目录节点 17/26。** “应用资源 requests 和 limits 的默认值”服务于“用requests、limits、QoS、LimitRange、ResourceQuota和监控管理计算资源”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“强制进行限制”进入“应用资源 requests 和 limits 的默认值”，再到“限制命名空间中的可用资源总量”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“调度按requests决策，运行时按limits约束，OOM和CPU节流可解释，命名空间配额不能被绕过”验收。

### 14.5 限制命名空间中的可用资源总量

**目录节点 18/26。** “限制命名空间中的可用资源总量”服务于“用requests、limits、QoS、LimitRange、ResourceQuota和监控管理计算资源”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“应用资源 requests 和 limits 的默认值”进入“限制命名空间中的可用资源总量”，再到“ResourceQuota 资源介绍”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“调度按requests决策，运行时按limits约束，OOM和CPU节流可解释，命名空间配额不能被绕过”验收。

#### 14.5.1 ResourceQuota 资源介绍

**目录节点 19/26。** “ResourceQuota 资源介绍”服务于“用requests、limits、QoS、LimitRange、ResourceQuota和监控管理计算资源”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“限制命名空间中的可用资源总量”进入“ResourceQuota 资源介绍”，再到“为持久化存储指定配额”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“调度按requests决策，运行时按limits约束，OOM和CPU节流可解释，命名空间配额不能被绕过”验收。

#### 14.5.2 为持久化存储指定配额

**目录节点 20/26。** “为持久化存储指定配额”服务于“用requests、limits、QoS、LimitRange、ResourceQuota和监控管理计算资源”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“ResourceQuota 资源介绍”进入“为持久化存储指定配额”，再到“限制可创建对象的个数”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“调度按requests决策，运行时按limits约束，OOM和CPU节流可解释，命名空间配额不能被绕过”验收。

#### 14.5.3 限制可创建对象的个数

**目录节点 21/26。** “限制可创建对象的个数”服务于“用requests、limits、QoS、LimitRange、ResourceQuota和监控管理计算资源”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“为持久化存储指定配额”进入“限制可创建对象的个数”，再到“为特定的 pod 状态或者 QoS 等级指定配额”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“调度按requests决策，运行时按limits约束，OOM和CPU节流可解释，命名空间配额不能被绕过”验收。

#### 14.5.4 为特定的 pod 状态或者 QoS 等级指定配额

**目录节点 22/26。** “为特定的 pod 状态或者 QoS 等级指定配额”服务于“用requests、limits、QoS、LimitRange、ResourceQuota和监控管理计算资源”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“限制可创建对象的个数”进入“为特定的 pod 状态或者 QoS 等级指定配额”，再到“监控 pod 的资源使用量”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“调度按requests决策，运行时按limits约束，OOM和CPU节流可解释，命名空间配额不能被绕过”验收。

### 14.6 监控 pod 的资源使用量

**目录节点 23/26。** “监控 pod 的资源使用量”服务于“用requests、limits、QoS、LimitRange、ResourceQuota和监控管理计算资源”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“为特定的 pod 状态或者 QoS 等级指定配额”进入“监控 pod 的资源使用量”，再到“收集、获取实际资源使用情况”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“调度按requests决策，运行时按limits约束，OOM和CPU节流可解释，命名空间配额不能被绕过”验收。

#### 14.6.1 收集、获取实际资源使用情况

**目录节点 24/26。** “收集、获取实际资源使用情况”服务于“用requests、limits、QoS、LimitRange、ResourceQuota和监控管理计算资源”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“监控 pod 的资源使用量”进入“收集、获取实际资源使用情况”，再到“保存并分析历史资源的使用统计信息”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“调度按requests决策，运行时按limits约束，OOM和CPU节流可解释，命名空间配额不能被绕过”验收。

#### 14.6.2 保存并分析历史资源的使用统计信息

**目录节点 25/26。** “保存并分析历史资源的使用统计信息”服务于“用requests、limits、QoS、LimitRange、ResourceQuota和监控管理计算资源”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“收集、获取实际资源使用情况”进入“保存并分析历史资源的使用统计信息”，再到“本章小结”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“调度按requests决策，运行时按limits约束，OOM和CPU节流可解释，命名空间配额不能被绕过”验收。

### 14.7 本章小结

**目录节点 26/26。** “本章小结”服务于“用requests、limits、QoS、LimitRange、ResourceQuota和监控管理计算资源”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“保存并分析历史资源的使用统计信息”进入“本章小结”，再到“本章证据门与回退”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“调度按requests决策，运行时按limits约束，OOM和CPU节流可解释，命名空间配额不能被绕过”验收。

<Callout type="trap" title="语义陷阱">
  把CPU使用率当request，或用limit代替容量规划，导致调度过密、节流和OOM。先写清对象所有权、控制器完成点、节点执行与最终业务不变量，再选择命令或清单。
</Callout>

<Callout type="trap" title="版本陷阱">
  原书以Kubernetes
  1.8.0为基线，ReplicationController、extensions/v1beta1、rkt、Heapster、Service
  Catalog和Federation均有时代语境；现代结果可作迁移差异，不能证明本书行为。
</Callout>

<Callout type="warning" title="隔离实验">
  删除命名空间、驱逐Pod、停止节点、网络隔离、卷回收、证书与RBAC变更只在隔离集群演练。先备份清单、etcd或业务样本并规定停止上界。
</Callout>

## 独立证据与生产交接

目录证据保存18章4附录与404个唯一节点映射；环境证据保存客户端、API服务器、etcd、容器运行时、网络与存储插件版本；对象证据保存清单、UID、resourceVersion、ownerReference、spec和status；运行证据保存事件、控制器、调度、kubelet、探针、流量和最终业务结果。

验收记录按一次变更一个编号：记录上下文、命名空间、操作者、开始结束时间、对象前后状态、请求样本、预期与实际结果、日志和指标窗口。失败时保留原始事件、Pending或终止原因、影响范围、停止条件和回退验证；成功时也须由另一位读者按同一材料重放。

性能判断固定镜像、请求分布、副本、资源、探针、Service、节点和存储语义。扩大limit或关闭探针换来的吞吐不是等价优化。平均值会隐藏调度等待、镜像拉取、CPU节流、OOM、控制器重试、Endpoint切换和卷挂载，必须保留P95/P99与状态时间线。

恢复判断从业务状态结束。Pod Running、节点Ready、Deployment Available、PVC Bound或API可访问都只是阶段；还要核对客户端请求、数据集合、身份权限、对象版本和旧实例是否停止提供错误结果。

## 本章回顾

重新完成“用requests、limits、QoS、LimitRange、ResourceQuota和监控管理计算资源”：固定Kubernetes 1.8和第1版目录，从声明式对象与控制循环推导边界，通过基线、压力与故障实验测量，最后交付资源模型、调度实验、limits反例、QoS驱逐、LimitRange、Quota与历史监控。只有“调度按requests决策，运行时按limits约束，OOM和CPU节流可解释，命名空间配额不能被绕过”与反例都能独立重放，本页才算完成。

## 复习与运行验收

<Exercises>
**问题 1**：为什么“第14章 计算资源管理”必须覆盖26个目录节点？
<Answer>这些节点共同组成“用requests、limits、QoS、LimitRange、ResourceQuota和监控管理计算资源”的对象、控制循环、运行与证据链；漏项会使资源模型、调度实验、limits反例、QoS驱逐、LimitRange、Quota与历史监控无法独立复现。</Answer>

**问题 2**：本页最小不变量是什么？

<Answer>
  调度按requests决策，运行时按limits约束，OOM和CPU节流可解释，命名空间配额不能被绕过。需要版本、对象、控制器轨迹、指标、故障与业务对账共同证明。
</Answer>

**问题 3**：怎样构造最小反例？

<Answer>
  固定Kubernetes
  1.8、集群、命名空间、镜像和清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一，再比较状态轨迹与最终业务结果。
</Answer>

**问题 4**：为什么不能直接用现代Kubernetes替代本页实验？

<Answer>
  原书源码以Kubernetes
  1.8.0为基线；API版本、运行时、网络、安全、伸缩和多集群机制后来均有变化，新版只能作为差异材料。
</Answer>

**问题 5**：如何验证性能和恢复结论？

<Answer>
  固定镜像、请求分布、副本、资源、探针、网络和存储语义，重复测期望/当前/就绪、重启、Pending、吞吐、P95/P99和错误，并独立对账。
</Answer>

**问题 6**：独立交接需要什么？

<Answer>需要完整目录映射、版本环境、对象快照、控制循环轨迹、资源模型、调度实验、limits反例、QoS驱逐、LimitRange、Quota与历史监控、负载、故障、对账、停止、恢复、回退和责任人。</Answer>
</Exercises>

<Glossary>
  <GlossaryItem term="requests">
    requests在本页对应Kubernetes
    1.8中的对象、字段或状态；适用范围由“调度按requests决策，运行时按limits约束，OOM和CPU节流可解释，命名空间配额不能被绕过”和故障反例共同限定。
  </GlossaryItem>
  <GlossaryItem term="limits">
    limits在本页对应Kubernetes
    1.8中的对象、字段或状态；适用范围由“调度按requests决策，运行时按limits约束，OOM和CPU节流可解释，命名空间配额不能被绕过”和故障反例共同限定。
  </GlossaryItem>
  <GlossaryItem term="QoS">
    QoS在本页对应Kubernetes
    1.8中的对象、字段或状态；适用范围由“调度按requests决策，运行时按limits约束，OOM和CPU节流可解释，命名空间配额不能被绕过”和故障反例共同限定。
  </GlossaryItem>
  <GlossaryItem term="LimitRange">
    LimitRange在本页对应Kubernetes
    1.8中的对象、字段或状态；适用范围由“调度按requests决策，运行时按limits约束，OOM和CPU节流可解释，命名空间配额不能被绕过”和故障反例共同限定。
  </GlossaryItem>
  <GlossaryItem term="ResourceQuota">
    ResourceQuota在本页对应Kubernetes
    1.8中的对象、字段或状态；适用范围由“调度按requests决策，运行时按limits约束，OOM和CPU节流可解释，命名空间配额不能被绕过”和故障反例共同限定。
  </GlossaryItem>
</Glossary>

[← 上一页：第13章 保障集群内节点和网络安全](/learn/kubernetes-in-action/13-13-node-network-security/k8s-13-node-network-security) · [下一页：第15章 自动横向伸缩 pod 与集群节点 →](/learn/kubernetes-in-action/15-15-autoscaling/k8s-15-autoscaling)

<Attribution
  adaptedFrom="Marko Lukša《Kubernetes in Action》第1版"
  adaptedUrl="https://www.phei.com.cn/module/goods/wssd_content.jsp?bookid=53240"
/>
