---
title: "第17章 开发应用的最佳实践"
description: "依据电子工业出版社第1版完整目录：整合资源清单、Pod生命周期、优雅启停、镜像标签、日志、开发测试与CI/CD"
type: C
section: "Kubernetes in Action中文版（第1版 / Kubernetes 1.8 / 2018）"
order: 17
draft: false
math: false
sourceUrl: "https://www.phei.com.cn/module/goods/wssd_content.jsp?bookid=53240"
demo: true
---

import {
  K8s17BestPracticesObjectLab,
  K8s17BestPracticesReconcileLab,
  K8s17BestPracticesEvidenceLab,
} from "@/components/mdx/kubernetes-in-action/diagrams/k8s-17-best-practices";

# 第17章 开发应用的最佳实践

本课程对应Marko Lukša著《Kubernetes in Action中文版》第1版，七牛容器云团队译，电子工业出版社2018年12月出版，592页，ISBN 9787121349959；英文原版由Manning于2017年12月出版。作者第1版源码第8章明确下载Kubernetes 1.8.0客户端，课程据此固定行为边界。

全书正式分母为18章与附录A至D，共22个正式单元、404个唯一章/附录/节节点；另设学习地图和总复习，共24页。第2版、现代apps/v1写法、EndpointSlice、Gateway API、Pod Security Admission或后来的多集群方案只能作为差异材料，不能替代第1版。本页逐项覆盖24个目录节点，最终交付资源包、生命周期钩子、优雅终止、镜像策略、日志契约、开发与持续交付演练。

<Objectives>

- 能解释“第17章 开发应用的最佳实践”的24个目录节点，并绘制API对象、控制器、调度、节点执行、网络与存储边界。
- 能比较基线、压力、对象变化、Pod或节点故障和恢复轨迹，判断期望状态、实际状态与业务完成点。
- 能设计并复现资源包、生命周期钩子、优雅终止、镜像策略、日志契约、开发与持续交付演练，用清单、事件、日志、指标与最终状态支持结论。
- 能分析“依赖固定Pod顺序、可写容器状态或latest标签，使滚动发布和故障重调度不可预测”为何失败，并写出停止、恢复、回退与独立交接条件。

</Objectives>

## 从一次声明式变更开始

先预测：kubectl提交对象后，API服务器完成哪些认证、授权、准入和持久化步骤，哪个控制器watch到变化，调度器如何选择节点，kubelet怎样创建容器，Service、卷与探针何时就绪。观察前先写预测，再按resourceVersion、UID、ownerReference和事件时间线修正模型。

Kubernetes 1.8的完成不是命令返回created。对象进入etcd、控制器观察、创建下级对象、Pod被调度、镜像启动、探针通过、Endpoint接入流量和业务结果稳定是不同检查点。控制循环允许中间状态，验收必须明确等待条件和超时后的诊断路径。

## 核心词汇与版本边界

<Term def="生命周期钩子是第17章 开发应用的最佳实践的第1个核心概念，必须同时说明Kubernetes 1.8对象、控制循环、故障边界和观测证据">生命周期钩子</Term>、<Term def="优雅终止是第17章 开发应用的最佳实践的第2个核心概念，必须同时说明Kubernetes 1.8对象、控制循环、故障边界和观测证据">优雅终止</Term>、<Term def="ImagePullPolicy是第17章 开发应用的最佳实践的第3个核心概念，必须同时说明Kubernetes 1.8对象、控制循环、故障边界和观测证据">ImagePullPolicy</Term>、<Term def="日志契约是第17章 开发应用的最佳实践的第4个核心概念，必须同时说明Kubernetes 1.8对象、控制循环、故障边界和观测证据">日志契约</Term>、<Term def="持续交付是第17章 开发应用的最佳实践的第5个核心概念，必须同时说明Kubernetes 1.8对象、控制循环、故障边界和观测证据">持续交付</Term>

这些词构成本页词汇。每个词都要回答：对象由谁声明，谁拥有，谁调谐，期望与实际状态保存在哪里，失败后会重试还是终止，怎样观测，以及Kubernetes 1.8之后哪些变化不属于本书。

<K8s17BestPracticesObjectLab />

## 原书目录逐节点重构

### 17.1 集中一切资源

**目录节点 1/24。** “集中一切资源”服务于“整合资源清单、Pod生命周期、优雅启停、镜像标签、日志、开发测试与CI/CD”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“本章输入与版本契约”进入“集中一切资源”，再到“了解 pod 的生命周期”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“Pod随时可重建，启动和终止窗口不丢客户端请求，制品与清单按版本联合发布和回退”验收。

### 17.2 了解 pod 的生命周期

**目录节点 2/24。** “了解 pod 的生命周期”服务于“整合资源清单、Pod生命周期、优雅启停、镜像标签、日志、开发测试与CI/CD”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“集中一切资源”进入“了解 pod 的生命周期”，再到“应用必须预料到会被杀死或者重新调度”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“Pod随时可重建，启动和终止窗口不丢客户端请求，制品与清单按版本联合发布和回退”验收。

#### 17.2.1 应用必须预料到会被杀死或者重新调度

**目录节点 3/24。** “应用必须预料到会被杀死或者重新调度”服务于“整合资源清单、Pod生命周期、优雅启停、镜像标签、日志、开发测试与CI/CD”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“了解 pod 的生命周期”进入“应用必须预料到会被杀死或者重新调度”，再到“重新调度死亡的或者部分死亡的 pod”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“Pod随时可重建，启动和终止窗口不丢客户端请求，制品与清单按版本联合发布和回退”验收。

#### 17.2.2 重新调度死亡的或者部分死亡的 pod

**目录节点 4/24。** “重新调度死亡的或者部分死亡的 pod”服务于“整合资源清单、Pod生命周期、优雅启停、镜像标签、日志、开发测试与CI/CD”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“应用必须预料到会被杀死或者重新调度”进入“重新调度死亡的或者部分死亡的 pod”，再到“以固定顺序启动 pod”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“Pod随时可重建，启动和终止窗口不丢客户端请求，制品与清单按版本联合发布和回退”验收。

#### 17.2.3 以固定顺序启动 pod

**目录节点 5/24。** “以固定顺序启动 pod”服务于“整合资源清单、Pod生命周期、优雅启停、镜像标签、日志、开发测试与CI/CD”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“重新调度死亡的或者部分死亡的 pod”进入“以固定顺序启动 pod”，再到“增加生命周期钩子”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“Pod随时可重建，启动和终止窗口不丢客户端请求，制品与清单按版本联合发布和回退”验收。

#### 17.2.4 增加生命周期钩子

**目录节点 6/24。** “增加生命周期钩子”服务于“整合资源清单、Pod生命周期、优雅启停、镜像标签、日志、开发测试与CI/CD”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“以固定顺序启动 pod”进入“增加生命周期钩子”，再到“了解 pod 的关闭”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“Pod随时可重建，启动和终止窗口不丢客户端请求，制品与清单按版本联合发布和回退”验收。

#### 17.2.5 了解 pod 的关闭

**目录节点 7/24。** “了解 pod 的关闭”服务于“整合资源清单、Pod生命周期、优雅启停、镜像标签、日志、开发测试与CI/CD”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“增加生命周期钩子”进入“了解 pod 的关闭”，再到“确保所有的客户端请求都得到了妥善处理”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“Pod随时可重建，启动和终止窗口不丢客户端请求，制品与清单按版本联合发布和回退”验收。

### 17.3 确保所有的客户端请求都得到了妥善处理

**目录节点 8/24。** “确保所有的客户端请求都得到了妥善处理”服务于“整合资源清单、Pod生命周期、优雅启停、镜像标签、日志、开发测试与CI/CD”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“了解 pod 的关闭”进入“确保所有的客户端请求都得到了妥善处理”，再到“在 pod 启动时避免客户端连接断开”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“Pod随时可重建，启动和终止窗口不丢客户端请求，制品与清单按版本联合发布和回退”验收。

#### 17.3.1 在 pod 启动时避免客户端连接断开

**目录节点 9/24。** “在 pod 启动时避免客户端连接断开”服务于“整合资源清单、Pod生命周期、优雅启停、镜像标签、日志、开发测试与CI/CD”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“确保所有的客户端请求都得到了妥善处理”进入“在 pod 启动时避免客户端连接断开”，再到“在 pod 关闭时避免客户端连接断开”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“Pod随时可重建，启动和终止窗口不丢客户端请求，制品与清单按版本联合发布和回退”验收。

#### 17.3.2 在 pod 关闭时避免客户端连接断开

**目录节点 10/24。** “在 pod 关闭时避免客户端连接断开”服务于“整合资源清单、Pod生命周期、优雅启停、镜像标签、日志、开发测试与CI/CD”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“在 pod 启动时避免客户端连接断开”进入“在 pod 关闭时避免客户端连接断开”，再到“让应用在 Kubernetes 中方便运行和管理”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“Pod随时可重建，启动和终止窗口不丢客户端请求，制品与清单按版本联合发布和回退”验收。

### 17.4 让应用在 Kubernetes 中方便运行和管理

**目录节点 11/24。** “让应用在 Kubernetes 中方便运行和管理”服务于“整合资源清单、Pod生命周期、优雅启停、镜像标签、日志、开发测试与CI/CD”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“在 pod 关闭时避免客户端连接断开”进入“让应用在 Kubernetes 中方便运行和管理”，再到“构建可管理的容器镜像”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“Pod随时可重建，启动和终止窗口不丢客户端请求，制品与清单按版本联合发布和回退”验收。

#### 17.4.1 构建可管理的容器镜像

**目录节点 12/24。** “构建可管理的容器镜像”服务于“整合资源清单、Pod生命周期、优雅启停、镜像标签、日志、开发测试与CI/CD”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“让应用在 Kubernetes 中方便运行和管理”进入“构建可管理的容器镜像”，再到“合理地给镜像打标签，正确地使用 ImagePullPolicy”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“Pod随时可重建，启动和终止窗口不丢客户端请求，制品与清单按版本联合发布和回退”验收。

#### 17.4.2 合理地给镜像打标签，正确地使用 ImagePullPolicy

**目录节点 13/24。** “合理地给镜像打标签，正确地使用 ImagePullPolicy”服务于“整合资源清单、Pod生命周期、优雅启停、镜像标签、日志、开发测试与CI/CD”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“构建可管理的容器镜像”进入“合理地给镜像打标签，正确地使用 ImagePullPolicy”，再到“使用多维度而不是单维度的标签”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“Pod随时可重建，启动和终止窗口不丢客户端请求，制品与清单按版本联合发布和回退”验收。

#### 17.4.3 使用多维度而不是单维度的标签

**目录节点 14/24。** “使用多维度而不是单维度的标签”服务于“整合资源清单、Pod生命周期、优雅启停、镜像标签、日志、开发测试与CI/CD”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“合理地给镜像打标签，正确地使用 ImagePullPolicy”进入“使用多维度而不是单维度的标签”，再到“通过注解描述每个资源”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“Pod随时可重建，启动和终止窗口不丢客户端请求，制品与清单按版本联合发布和回退”验收。

#### 17.4.4 通过注解描述每个资源

**目录节点 15/24。** “通过注解描述每个资源”服务于“整合资源清单、Pod生命周期、优雅启停、镜像标签、日志、开发测试与CI/CD”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“使用多维度而不是单维度的标签”进入“通过注解描述每个资源”，再到“给进程终止提供更多的信息”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“Pod随时可重建，启动和终止窗口不丢客户端请求，制品与清单按版本联合发布和回退”验收。

#### 17.4.5 给进程终止提供更多的信息

**目录节点 16/24。** “给进程终止提供更多的信息”服务于“整合资源清单、Pod生命周期、优雅启停、镜像标签、日志、开发测试与CI/CD”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“通过注解描述每个资源”进入“给进程终止提供更多的信息”，再到“处理应用日志”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“Pod随时可重建，启动和终止窗口不丢客户端请求，制品与清单按版本联合发布和回退”验收。

#### 17.4.6 处理应用日志

**目录节点 17/24。** “处理应用日志”服务于“整合资源清单、Pod生命周期、优雅启停、镜像标签、日志、开发测试与CI/CD”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“给进程终止提供更多的信息”进入“处理应用日志”，再到“开发和测试的最佳实践”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“Pod随时可重建，启动和终止窗口不丢客户端请求，制品与清单按版本联合发布和回退”验收。

### 17.5 开发和测试的最佳实践

**目录节点 18/24。** “开发和测试的最佳实践”服务于“整合资源清单、Pod生命周期、优雅启停、镜像标签、日志、开发测试与CI/CD”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“处理应用日志”进入“开发和测试的最佳实践”，再到“开发过程中在 Kubernetes 之外运行应用”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“Pod随时可重建，启动和终止窗口不丢客户端请求，制品与清单按版本联合发布和回退”验收。

#### 17.5.1 开发过程中在 Kubernetes 之外运行应用

**目录节点 19/24。** “开发过程中在 Kubernetes 之外运行应用”服务于“整合资源清单、Pod生命周期、优雅启停、镜像标签、日志、开发测试与CI/CD”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“开发和测试的最佳实践”进入“开发过程中在 Kubernetes 之外运行应用”，再到“在开发过程中使用 Minikube”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“Pod随时可重建，启动和终止窗口不丢客户端请求，制品与清单按版本联合发布和回退”验收。

#### 17.5.2 在开发过程中使用 Minikube

**目录节点 20/24。** “在开发过程中使用 Minikube”服务于“整合资源清单、Pod生命周期、优雅启停、镜像标签、日志、开发测试与CI/CD”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“开发过程中在 Kubernetes 之外运行应用”进入“在开发过程中使用 Minikube”，再到“发布版本和自动部署资源清单”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“Pod随时可重建，启动和终止窗口不丢客户端请求，制品与清单按版本联合发布和回退”验收。

#### 17.5.3 发布版本和自动部署资源清单

**目录节点 21/24。** “发布版本和自动部署资源清单”服务于“整合资源清单、Pod生命周期、优雅启停、镜像标签、日志、开发测试与CI/CD”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“在开发过程中使用 Minikube”进入“发布版本和自动部署资源清单”，再到“使用 Ksonnet 作为编写 YAML/JSON manifest 文件的额外选择”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“Pod随时可重建，启动和终止窗口不丢客户端请求，制品与清单按版本联合发布和回退”验收。

#### 17.5.4 使用 Ksonnet 作为编写 YAML/JSON manifest 文件的额外选择

**目录节点 22/24。** “使用 Ksonnet 作为编写 YAML/JSON manifest 文件的额外选择”服务于“整合资源清单、Pod生命周期、优雅启停、镜像标签、日志、开发测试与CI/CD”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“发布版本和自动部署资源清单”进入“使用 Ksonnet 作为编写 YAML/JSON manifest 文件的额外选择”，再到“利用持续集成和持续交付”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“Pod随时可重建，启动和终止窗口不丢客户端请求，制品与清单按版本联合发布和回退”验收。

#### 17.5.5 利用持续集成和持续交付

**目录节点 23/24。** “利用持续集成和持续交付”服务于“整合资源清单、Pod生命周期、优雅启停、镜像标签、日志、开发测试与CI/CD”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“使用 Ksonnet 作为编写 YAML/JSON manifest 文件的额外选择”进入“利用持续集成和持续交付”，再到“本章小结”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“Pod随时可重建，启动和终止窗口不丢客户端请求，制品与清单按版本联合发布和回退”验收。

### 17.6 本章小结

**目录节点 24/24。** “本章小结”服务于“整合资源清单、Pod生命周期、优雅启停、镜像标签、日志、开发测试与CI/CD”。先识别它在Kubernetes 1.8中对应API对象、期望状态、实际状态、所有者引用和执行组件，再标出创建、观察、调谐、删除与垃圾回收边界。结论必须从清单、API响应、事件、控制器日志、节点状态和应用结果复核。

状态轨迹从“利用持续集成和持续交付”进入“本章小结”，再到“本章证据门与回退”。沿kubectl请求、API认证授权、etcd持久化、watch、控制器调谐、调度、kubelet执行、网络与存储准备记录resourceVersion和时间线。Pod重建、节点故障、网络分区或控制面重启必须有独立分支，不能只画正常箭头。

实验固定Kubernetes 1.8、集群拓扑、镜像摘要、命名空间和对象清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一。比较期望/当前/就绪副本、Pending原因、重启、事件、吞吐、P95/P99和最终业务集合，用“Pod随时可重建，启动和终止窗口不丢客户端请求，制品与清单按版本联合发布和回退”验收。

## 可复现实验与对象切片

```bash
# 固定Kubernetes 1.8客户端、上下文、命名空间和对象版本。
kubectl version --short
kubectl config current-context
kubectl get --raw /version
kubectl get events --sort-by=.metadata.creationTimestamp
```

```yaml
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: book-lab
spec:
  replicas: 3
  template:
    metadata:
      labels: { app: book-lab }
    spec:
      containers:
        - name: app
          image: example/book-lab@sha256:fixed
          resources:
            requests: { cpu: 100m, memory: 64Mi }
```

```text
evidence = client/server/runtime/api-version and kubeconfig context
         + manifest/uid/resourceVersion/ownerReference/status snapshot
         + api/watch/controller/scheduler/kubelet/event timeline
         + desired/current/ready/restarts/pending/p95/p99 metrics
         + pod/node/network/storage/control-plane fault and business reconciliation
```

## 三步交互实验

<Stepper>
  <Step title="1. 选择目录节点并预测对象链">
    改变期望副本、就绪副本与对象阶段，先预测API、控制器、调度与kubelet路径，再观察。
    <K8s17BestPracticesObjectLab />
  </Step>
  <Step title="2. 改变资源、探针与故障条件">
    调整负载、requests、limits、节点与探针故障，比较Pending、重启、可用副本和尾延迟。
    <K8s17BestPracticesReconcileLab />
  </Step>
  <Step title="3. 通过端到端证据门">
    目录、版本、对象、控制循环、指标、故障、业务对账与回退齐全后才接受结论。
    <K8s17BestPracticesEvidenceLab />
  </Step>
</Stepper>

<Callout type="trap" title="语义陷阱">
  依赖固定Pod顺序、可写容器状态或latest标签，使滚动发布和故障重调度不可预测。先写清对象所有权、控制器完成点、节点执行与最终业务不变量，再选择命令或清单。
</Callout>

<Callout type="trap" title="版本陷阱">
  原书以Kubernetes
  1.8.0为基线，ReplicationController、extensions/v1beta1、rkt、Heapster、Service
  Catalog和Federation均有时代语境；现代结果可作迁移差异，不能证明本书行为。
</Callout>

<Callout type="warning" title="隔离实验">
  删除命名空间、驱逐Pod、停止节点、网络隔离、卷回收、证书与RBAC变更只在隔离集群演练。先备份清单、etcd或业务样本并规定停止上界。
</Callout>

## 独立证据与生产交接

目录证据保存18章4附录与404个唯一节点映射；环境证据保存客户端、API服务器、etcd、容器运行时、网络与存储插件版本；对象证据保存清单、UID、resourceVersion、ownerReference、spec和status；运行证据保存事件、控制器、调度、kubelet、探针、流量和最终业务结果。

验收记录按一次变更一个编号：记录上下文、命名空间、操作者、开始结束时间、对象前后状态、请求样本、预期与实际结果、日志和指标窗口。失败时保留原始事件、Pending或终止原因、影响范围、停止条件和回退验证；成功时也须由另一位读者按同一材料重放。

性能判断固定镜像、请求分布、副本、资源、探针、Service、节点和存储语义。扩大limit或关闭探针换来的吞吐不是等价优化。平均值会隐藏调度等待、镜像拉取、CPU节流、OOM、控制器重试、Endpoint切换和卷挂载，必须保留P95/P99与状态时间线。

恢复判断从业务状态结束。Pod Running、节点Ready、Deployment Available、PVC Bound或API可访问都只是阶段；还要核对客户端请求、数据集合、身份权限、对象版本和旧实例是否停止提供错误结果。

## 本章回顾

重新完成“整合资源清单、Pod生命周期、优雅启停、镜像标签、日志、开发测试与CI/CD”：固定Kubernetes 1.8和第1版目录，从声明式对象与控制循环推导边界，通过基线、压力与故障实验测量，最后交付资源包、生命周期钩子、优雅终止、镜像策略、日志契约、开发与持续交付演练。只有“Pod随时可重建，启动和终止窗口不丢客户端请求，制品与清单按版本联合发布和回退”与反例都能独立重放，本页才算完成。

## 复习与运行验收

<Exercises>
**问题 1**：为什么“第17章 开发应用的最佳实践”必须覆盖24个目录节点？
<Answer>这些节点共同组成“整合资源清单、Pod生命周期、优雅启停、镜像标签、日志、开发测试与CI/CD”的对象、控制循环、运行与证据链；漏项会使资源包、生命周期钩子、优雅终止、镜像策略、日志契约、开发与持续交付演练无法独立复现。</Answer>

**问题 2**：本页最小不变量是什么？

<Answer>
  Pod随时可重建，启动和终止窗口不丢客户端请求，制品与清单按版本联合发布和回退。需要版本、对象、控制器轨迹、指标、故障与业务对账共同证明。
</Answer>

**问题 3**：怎样构造最小反例？

<Answer>
  固定Kubernetes
  1.8、集群、命名空间、镜像和清单，只改变副本、探针、资源、标签选择、节点、网络或存储条件之一，再比较状态轨迹与最终业务结果。
</Answer>

**问题 4**：为什么不能直接用现代Kubernetes替代本页实验？

<Answer>
  原书源码以Kubernetes
  1.8.0为基线；API版本、运行时、网络、安全、伸缩和多集群机制后来均有变化，新版只能作为差异材料。
</Answer>

**问题 5**：如何验证性能和恢复结论？

<Answer>
  固定镜像、请求分布、副本、资源、探针、网络和存储语义，重复测期望/当前/就绪、重启、Pending、吞吐、P95/P99和错误，并独立对账。
</Answer>

**问题 6**：独立交接需要什么？

<Answer>需要完整目录映射、版本环境、对象快照、控制循环轨迹、资源包、生命周期钩子、优雅终止、镜像策略、日志契约、开发与持续交付演练、负载、故障、对账、停止、恢复、回退和责任人。</Answer>
</Exercises>

<Glossary>
  <GlossaryItem term="生命周期钩子">
    生命周期钩子在本页对应Kubernetes
    1.8中的对象、字段或状态；适用范围由“Pod随时可重建，启动和终止窗口不丢客户端请求，制品与清单按版本联合发布和回退”和故障反例共同限定。
  </GlossaryItem>
  <GlossaryItem term="优雅终止">
    优雅终止在本页对应Kubernetes
    1.8中的对象、字段或状态；适用范围由“Pod随时可重建，启动和终止窗口不丢客户端请求，制品与清单按版本联合发布和回退”和故障反例共同限定。
  </GlossaryItem>
  <GlossaryItem term="ImagePullPolicy">
    ImagePullPolicy在本页对应Kubernetes
    1.8中的对象、字段或状态；适用范围由“Pod随时可重建，启动和终止窗口不丢客户端请求，制品与清单按版本联合发布和回退”和故障反例共同限定。
  </GlossaryItem>
  <GlossaryItem term="日志契约">
    日志契约在本页对应Kubernetes
    1.8中的对象、字段或状态；适用范围由“Pod随时可重建，启动和终止窗口不丢客户端请求，制品与清单按版本联合发布和回退”和故障反例共同限定。
  </GlossaryItem>
  <GlossaryItem term="持续交付">
    持续交付在本页对应Kubernetes
    1.8中的对象、字段或状态；适用范围由“Pod随时可重建，启动和终止窗口不丢客户端请求，制品与清单按版本联合发布和回退”和故障反例共同限定。
  </GlossaryItem>
</Glossary>

[← 上一页：第16章 高级调度](/learn/kubernetes-in-action/16-16-scheduling/k8s-16-scheduling) · [下一页：第18章 Kubernetes 应用扩展 →](/learn/kubernetes-in-action/18-18-extension/k8s-18-extension)

<Attribution
  adaptedFrom="Marko Lukša《Kubernetes in Action》第1版"
  adaptedUrl="https://www.phei.com.cn/module/goods/wssd_content.jsp?bookid=53240"
/>
