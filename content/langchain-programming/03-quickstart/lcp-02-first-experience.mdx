---
title: "第2章 LangChain初体验"
type: C
section: "第2章 LangChain初体验"
order: 3
description: "第2章 LangChain初体验按首版正式目录重建，以Runnable、trace和故障注入复现LangChain数据流。"
demo: true
math: true
sourceUrl: "https://aitutor.liduos.com/"
draft: false
---

import { Lcp02FirstExperienceGraphLab, Lcp02FirstExperienceRunLab, Lcp02FirstExperienceFaultLab } from "@/components/mdx/langchain-programming/diagrams/lcp-02-first-experience";
import { Objectives, Callout, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step, Attribution } from "@/components/mdx/mdx-components";

<Objectives>

- 能解释依赖锁定、语言模型、提示模板的输入、输出、状态和首版版本语境
- 能推导并计算四个核心表达式，把节点、检索、记忆或trace映射到实现状态
- 能实现并运行三种交互实验，定位并发、上下文、依赖或工具故障的首个分叉
- 能设计正常与单一故障样本，独立证明“能在固定依赖与假模型上运行invoke、stream和服务适配，保存trace，并证明密钥、输入与输出不会越过边界。”

</Objectives>

## 为什么先冻结依赖、数据流与trace

<Term def="依赖锁定是第2章 LangChain初体验的第1个可执行证据坐标。">依赖锁定</Term>、<Term def="语言模型是第2章 LangChain初体验的第2个可执行证据坐标。">语言模型</Term>、<Term def="提示模板是第2章 LangChain初体验的第3个可执行证据坐标。">提示模板</Term>、<Term def="输出解析是第2章 LangChain初体验的第4个可执行证据坐标。">输出解析</Term>、<Term def="LCEL是第2章 LangChain初体验的第5个可执行证据坐标。">LCEL</Term>、<Term def="安全实践是第2章 LangChain初体验的第6个可执行证据坐标。">安全实践</Term>共同构成本页坐标。从可复现环境开始，把模型、提示、解析器组合成最小LCEL链，再接入观测与服务边界，最后用密钥和输入安全关闭快速入门。

LangChain是快速演进的框架，本书首版记录的是2023至2024年前后的模块和接口。学习时必须分开原书的架构脉络、首版代码形状和后来版本的迁移方式。类名废弃不会让Runnable、schema、检索、工具权限、记忆隔离和事件追踪失效；实现应以端口与契约测试隔离具体版本。

每次运行保存Python版本、依赖锁摘要、代码提交、环境配置名、输入、节点图、模型与工具版本、事件trace、原始错误、输出校验和回退动作。只保存最终回答无法区分模板、模型、检索、记忆、工具或回调中的首个错误，也无法证明升级前后语义相同。

<Callout type="info">**验收命题：** 能在固定依赖与假模型上运行invoke、stream和服务适配，保存trace，并证明密钥、输入与输出不会越过边界。</Callout>

## 数学与执行骨架

$$
output=parse(model(render(input)))
$$

$$
environment=python+packages+config
$$

$$
latency=render+model+parse
$$

$$
safe=secret\land input\land output
$$

公式用于固定比较对象：组合必须说明函数方向，检索必须说明分母，代理必须有预算，事件必须有父子关系。框架生成的图或trace只是观测结果，仍需用输入输出断言和故障样本证明业务语义。

## 三段可运行骨架

```python
def freeze_run(code_version, dependency_lock, config, inputs):
    return {"code": code_version, "lock": dependency_lock, "config": config, "inputs": inputs}
```

```python
def record(trace, node, expected, actual):
    trace.append({"node": node, "expected": expected, "actual": actual, "ok": expected == actual})
    return trace
```

```python
def first_divergence(trace):
    return next((item for item in trace if not item["ok"]), None)
```

代码通过普通Python表达核心合同，可在没有外部模型、LangSmith或LangServe时运行。接入真实LangChain版本时，由薄适配器映射到相应Runnable、模型、检索器或回调API；契约测试不随类名变化，从而可以精确定位迁移差异。

## 固定样例与手算

先用三个节点构造管道：模板节点接收字典并输出消息，模型假对象接收消息并输出固定结构，解析节点验证必需字段。分别调用单次、两项批量和逐块流式接口，聚合后的业务结果应一致；若事件数量或顺序不同，差异必须在trace中可解释。

再取查询向量q=(1,0)、文档向量d1=(0.8,0.6)、d2=(0,1)，余弦相似度分别为0.8和0。检索top-1应返回d1，但这只证明向量邻近，不证明d1支持答案。生成前仍需核对文档ID、嵌入版本、文本范围和引用；空召回必须拒答。

代理实验注册read与write两个工具，普通权限只允许read。模型若选择write，schema可以正确但权限门禁仍应拒绝；工具超时不得被改写成“执行成功”。记忆实验再写入两名用户的同名事实，读取必须按租户隔离，并在过期时间后删除。

最后注入三个故障：依赖版本改变输出字段、检索器返回失效文档ID、回调处理器抛出异常。系统应分别在解析、引用校验和观察端口报告首个分叉；回调失败不篡改已完成的业务结果，解析与引用失败则阻断发布。

## 独立对照实验

基线在干净环境中运行，冻结依赖锁、假模型、文档集、工具注册表和输入集合。记录每个节点的schema、开始结束事件、耗时、输出摘要与错误类型。相同配置重复三次，确定性节点必须一致；随机模型按结构、事实、安全和费用断言比较，不要求逐字相同。

第二轮每次只改一个因素：升级LangChain但保持适配器外合同，切换模型但保持节点图，修改切块但保持文档与查询，注入工具错误但保持模型选择。首个分叉若早于预期，先修环境或适配器；若晚于预期，说明门禁没有在正确边界生效。

独立复核进程只读取保存的配置和受控样本，不共享会话内存。它重建环境、重放失败案例、相邻边界和正常基线，检查日志脱敏、事件父子关系、幂等与回退次数。三个集合都通过后才关闭问题，不能用一次新的成功运行覆盖原失败证据。

## 版本迁移演练

迁移前先导出旧版本的节点清单、输入输出schema、配置字段、事件类型与冻结样本结果，再在新版本建立逐项映射。若某个类被拆分或移动，只修改适配器；若默认参数、异常类型或流式事件语义变化，则把它记录为行为差异，并为旧新路径各保留一条可运行契约测试。没有映射和测试的接口不得批量替换。

演练使用四类样本：普通成功、结构化输出损坏、模型或工具超时、外部文本注入。旧新版本都必须在相同边界作出等价决定，允许最终措辞不同，但不能改变schema、权限、引用和回退语义。迁移报告写清依赖锁、差异、临时兼容层、删除条件与负责人；回滚需能恢复代码和锁文件，而不依赖已经污染的进程状态。

复核者还要从空目录按说明重建环境，证明锁文件、配置模板、假模型和样本数据足以启动测试。若必须依赖本机缓存、未声明环境变量或在线账户，复现记录应明确判定为不完整。最终产物包含一条最小成功trace和一条预期失败trace，让后来读者能区分框架升级问题与业务逻辑问题，并能重复验证结论。

## 正式目录逐项讲解

### 第2章 LangChain初体验

“LangChain初体验”先画每个节点的输入输出，再组合渲染、模型、工具与解析。场景链只复用合同，不绕过权限和校验；数据库与API查询使用只读白名单，摘要和问答保留来源与覆盖范围。

针对“第2章 LangChain初体验”，先预测只改变依赖版本、配置、输入、模型或工具结果时哪一个节点最先分叉，再运行正常与单故障路径。保存事件层级和原始错误，不用最终回答覆盖中间失败。

### 2.1 开发环境准备

“开发环境准备”要求保存Python版本、依赖锁、平台和环境变量名，并用干净环境重建。源码安装记录提交摘要；二进制与可选库分别验证。导入成功只证明环境可加载，仍需运行最小合同测试。

针对“2.1 开发环境准备”，先预测只改变依赖版本、配置、输入、模型或工具结果时哪一个节点最先分叉，再运行正常与单故障路径。保存事件层级和原始错误，不用最终回答覆盖中间失败。

### 2.1.1 管理工具安装

“管理工具安装”把每步状态、观察、动作、工具参数、权限、结果和剩余预算写入trace。工具注册包含schema、超时、幂等与风险等级；越权、重复动作、解析失败或步数耗尽必须显式停止。

针对“2.1.1 管理工具安装”，先预测只改变依赖版本、配置、输入、模型或工具结果时哪一个节点最先分叉，再运行正常与单故障路径。保存事件层级和原始错误，不用最终回答覆盖中间失败。

### 2.1.2 源码安装

“源码安装”要求保存Python版本、依赖锁、平台和环境变量名，并用干净环境重建。源码安装记录提交摘要；二进制与可选库分别验证。导入成功只证明环境可加载，仍需运行最小合同测试。

针对“2.1.2 源码安装”，先预测只改变依赖版本、配置、输入、模型或工具结果时哪一个节点最先分叉，再运行正常与单故障路径。保存事件层级和原始错误，不用最终回答覆盖中间失败。

### 2.1.3 其他库安装

“其他库安装”要求保存Python版本、依赖锁、平台和环境变量名，并用干净环境重建。源码安装记录提交摘要；二进制与可选库分别验证。导入成功只证明环境可加载，仍需运行最小合同测试。

针对“2.1.3 其他库安装”，先预测只改变依赖版本、配置、输入、模型或工具结果时哪一个节点最先分叉，再运行正常与单故障路径。保存事件层级和原始错误，不用最终回答覆盖中间失败。

### 2.2 快速开始

“快速开始”按定义、输入、输出、状态、错误与版本六项落地。先预测只改变一个依赖、配置或外部结果时首个分叉，再运行正常和故障路径；从可复现环境开始，把模型、提示、解析器组合成最小LCEL链，再接入观测与服务边界，最后用密钥和输入安全关闭快速入门。

针对“2.2 快速开始”，先预测只改变依赖版本、配置、输入、模型或工具结果时哪一个节点最先分叉，再运行正常与单故障路径。保存事件层级和原始错误，不用最终回答覆盖中间失败。

### 2.2.1 语言模型

“语言模型”按定义、输入、输出、状态、错误与版本六项落地。先预测只改变一个依赖、配置或外部结果时首个分叉，再运行正常和故障路径；从可复现环境开始，把模型、提示、解析器组合成最小LCEL链，再接入观测与服务边界，最后用密钥和输入安全关闭快速入门。

针对“2.2.1 语言模型”，先预测只改变依赖版本、配置、输入、模型或工具结果时哪一个节点最先分叉，再运行正常与单故障路径。保存事件层级和原始错误，不用最终回答覆盖中间失败。

### 2.2.2 提示模板

“提示模板”声明变量schema、示例来源和输出schema。用户与检索文本只进入数据槽；解析失败返回稳定错误，不静默退回原文。冻结任务集分别比较模板、示例选择和采样变化，避免一次好答案替代回归。

针对“2.2.2 提示模板”，先预测只改变依赖版本、配置、输入、模型或工具结果时哪一个节点最先分叉，再运行正常与单故障路径。保存事件层级和原始错误，不用最终回答覆盖中间失败。

### 2.2.3 输出解析器

“输出解析器”声明变量schema、示例来源和输出schema。用户与检索文本只进入数据槽；解析失败返回稳定错误，不静默退回原文。冻结任务集分别比较模板、示例选择和采样变化，避免一次好答案替代回归。

针对“2.2.3 输出解析器”，先预测只改变依赖版本、配置、输入、模型或工具结果时哪一个节点最先分叉，再运行正常与单故障路径。保存事件层级和原始错误，不用最终回答覆盖中间失败。

### 2.2.4 使用LCEL进行组合

“使用LCEL进行组合”落到Runnable的输入schema、输出schema、配置与事件流。对同一输入比较invoke、stream聚合和batch单项结果，记录并行分支、顺序、取消与部分失败；组合语法简洁不能隐藏真实数据流。

针对“2.2.4 使用LCEL进行组合”，先预测只改变依赖版本、配置、输入、模型或工具结果时哪一个节点最先分叉，再运行正常与单故障路径。保存事件层级和原始错误，不用最终回答覆盖中间失败。

### 2.2.5 使用LangSmith进行观测

“使用LangSmith进行观测”视为观测或服务适配层，而不是自动质量保证。保存版本、配置、trace采样和访问控制；服务入口验证认证、限额与输入，观测数据脱敏并设保留期限，断开平台后核心链仍可本地测试。

针对“2.2.5 使用LangSmith进行观测”，先预测只改变依赖版本、配置、输入、模型或工具结果时哪一个节点最先分叉，再运行正常与单故障路径。保存事件层级和原始错误，不用最终回答覆盖中间失败。

### 2.2.6 使用LangServe提供服务

“使用LangServe提供服务”视为观测或服务适配层，而不是自动质量保证。保存版本、配置、trace采样和访问控制；服务入口验证认证、限额与输入，观测数据脱敏并设保留期限，断开平台后核心链仍可本地测试。

针对“2.2.6 使用LangServe提供服务”，先预测只改变依赖版本、配置、输入、模型或工具结果时哪一个节点最先分叉，再运行正常与单故障路径。保存事件层级和原始错误，不用最终回答覆盖中间失败。

### 2.3 最佳安全实践

“最佳安全实践”从密钥、外部输入、模型输出、工具权限和日志五处设门禁。密钥只由服务端注入；外部文本不提升指令优先级；输出在执行前过schema与权限；日志不保存秘密和完整个人数据。

针对“2.3 最佳安全实践”，先预测只改变依赖版本、配置、输入、模型或工具结果时哪一个节点最先分叉，再运行正常与单故障路径。保存事件层级和原始错误，不用最终回答覆盖中间失败。

## 六类工程证据

### 版本与环境合同

保存解释器、依赖锁、代码摘要和配置名称。首版接口与当前接口通过适配器和兼容矩阵连接；未经锁定的环境不进入行为归因。

### Runnable与schema合同

每个节点声明输入输出和错误；invoke、stream与batch在业务语义上可对照。并行、分支和fallback保存实际路由，避免组合语法隐藏执行。

### 检索与来源合同

文档使用稳定ID，切块与嵌入带版本，召回和生成分开评分。回答逐项绑定来源，无召回或证据冲突时显式拒答或转人工。

### 工具与权限合同

工具含schema、权限、超时、幂等和风险等级。代理每步保存选择、参数、原始结果和预算，高风险动作等待外部批准。

### 记忆与隔离合同

记忆区分短期消息、摘要、实体和持久状态，写入有来源、租户和过期时间。读取内容仍是不可信数据，删除可验证。

### 回调与发布合同

trace以稳定ID连接节点、模型和工具事件，日志先脱敏。发布同时满足质量、安全、延迟和成本基线，失败回退到已验证依赖锁。

## 三步交互实验

<Stepper>
  <Step title="追踪Runnable图">推动节点阶段，检查输入、Runnable、工具或模型、解析与trace的顺序。<Lcp02FirstExperienceGraphLab /></Step>
  <Step title="改变执行参数">调节并发与上下文预算，先预测节点时序和预算余量，再观察变化。<Lcp02FirstExperienceRunLab /></Step>
  <Step title="注入节点故障">注入依赖、工具或输出故障，要求诊断在最终发布之前出现。<Lcp02FirstExperienceFaultLab /></Step>
</Stepper>

## 常见失败与回退

<Callout type="trap">**本章高风险失败：** 复制最新版文档代码到首版环境，未锁依赖就排查逻辑，或把LangServe暴露的链视为自动安全。</Callout>

<Callout type="trap">**框架抽象不是责任转移：** 链能运行不代表schema、来源、权限、记忆隔离和trace通过；中间合同失败必须阻断相应动作。</Callout>

<Callout type="tip">**回退顺序：** 环境锁 → 输入schema → 节点路由 → 模型或检索 → 输出解析 → 工具权限 → 记忆写入 → 回调。首个差异未解释前不比较整段回答。</Callout>

## 本章回顾

从可复现环境开始，把模型、提示、解析器组合成最小LCEL链，再接入观测与服务边界，最后用密钥和输入安全关闭快速入门。 掌握标准不是记住快速变化的API，而是能重建节点、合同、trace、失败和回退。通过标准为：能在固定依赖与假模型上运行invoke、stream和服务适配，保存trace，并证明密钥、输入与输出不会越过边界。

<Glossary>

<GlossaryItem term="依赖锁定">
  “依赖锁定”是第2章 LangChain初体验的第1个执行坐标。定义包含输入、输出、状态、版本、错误和最小反例，并指出框架抽象下面的真实责任。
</GlossaryItem>

<GlossaryItem term="语言模型">
  “语言模型”是第2章 LangChain初体验的第2个执行坐标。定义包含输入、输出、状态、版本、错误和最小反例，并指出框架抽象下面的真实责任。
</GlossaryItem>

<GlossaryItem term="提示模板">
  “提示模板”是第2章 LangChain初体验的第3个执行坐标。定义包含输入、输出、状态、版本、错误和最小反例，并指出框架抽象下面的真实责任。
</GlossaryItem>

<GlossaryItem term="输出解析">
  “输出解析”是第2章 LangChain初体验的第4个执行坐标。定义包含输入、输出、状态、版本、错误和最小反例，并指出框架抽象下面的真实责任。
</GlossaryItem>

<GlossaryItem term="LCEL">
  “LCEL”是第2章 LangChain初体验的第5个执行坐标。定义包含输入、输出、状态、版本、错误和最小反例，并指出框架抽象下面的真实责任。
</GlossaryItem>

<GlossaryItem term="安全实践">
  “安全实践”是第2章 LangChain初体验的第6个执行坐标。定义包含输入、输出、状态、版本、错误和最小反例，并指出框架抽象下面的真实责任。
</GlossaryItem>

</Glossary>

<Exercises>

1. 如何为第2章 LangChain初体验建立最小可重放基线？

<Answer>固定依赖锁、三个Runnable假节点和一组输入，保存schema、事件、输出与错误，再运行单次、批量和故障路径。</Answer>

2. 如何证明“能在固定依赖与假模型上运行invoke、stream和服务适配，保存trace，并证明密钥、输入与输出不会越过边界。”不是一次偶然成功？

<Answer>在同一冻结集合上并排运行正常路径与只破坏一个依赖、检索、工具、记忆或回调的路径，要求首个分叉符合预期。</Answer>

3. 修复第2章 LangChain初体验后怎样回归？

<Answer>先重建环境，再重放节点与trace，随后检查来源、权限和状态，最后评价业务输出；全部中间证据一致后才发布。</Answer>

</Exercises>

## 前后导航

- [第1章 LangChain简介](/learn/langchain-programming/02-introduction/lcp-01-introduction)
- [第3章 模型输入与输出](/learn/langchain-programming/04-model-io/lcp-03-model-io)

<Attribution adaptedFrom="李多多（@莫尔索）《LangChain编程：从入门到实践》，人民邮电出版社，2024年4月，192页，ISBN 9787115639424 · 基于146个正式目录层级的独立教学重构" adaptedUrl="https://aitutor.liduos.com/" />
