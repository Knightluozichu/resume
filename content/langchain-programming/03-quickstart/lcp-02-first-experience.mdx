---
title: 第2章 LangChain初体验
type: C
section: 第2章 LangChain初体验
order: 3
description: >-
  冻结Python、包锁、密钥边界并重放模型—提示—解析—观测—服务链；并列原书2024与当前v1，用版本合同、执行trace和发布门交付环境锁、输入输出schema、trace、服务合同与密钥泄露检查
demo: true
math: false
sourceUrl: "https://aitutor.liduos.com/"
draft: false
qualityVersion: 2
practiceMode: simulation
sourceMode: independent-rewrite
officialUnitId: lcp-02
---

import {
  Objectives,
  Term,
  Callout,
  Stepper,
  Step,
  Exercises,
  Answer,
  Glossary,
  GlossaryItem,
  Attribution,
} from "@/components/mdx/mdx-components";
import {
  Lcp02FirstExperienceVersionContractLab,
  Lcp02FirstExperienceRunnableTraceLab,
  Lcp02FirstExperienceReleaseGateLab,
} from "@/components/mdx/langchain-programming/v2/lcp-02-first-experience";

<Objectives>

- 解释环境、快速开始、LCEL、LangSmith、LangServe与安全的输入schema、执行状态、事件与输出，而不只背API名称
- 并列原书2024轨道与当前LangChain v1轨道，定位“使用未锁定最新版依赖和真实密钥，成功一次后隐藏trace与失败输出”的首个分岔
- 交付环境锁、输入输出schema、trace、服务合同与密钥泄露检查，说明评估、回退、安全与未测试边界

</Objectives>

## 为什么从这个问题开始

第2章 LangChain初体验围绕“快速开始怎样从一次成功调用升级为可复现、可观测且安全的最小服务？”建立贯穿任务：在锁定的2024原书轨道和当前LangChain v1轨道上重放环境、快速开始、LCEL、LangSmith、LangServe与安全。先预测依赖、schema、事件、状态、工具/检索调用或评估中的首个变化，再运行原书参考、v1参考、迁移故障与回退；只有守住“第2章 LangChain初体验的包版本、输入输出schema、执行事件、评估结果与回退路径始终一致”并交付环境锁、输入输出schema、trace、服务合同与密钥泄露检查，代码运行才构成可发布证据。

## 原书、伴学材料与当前v1边界

第2章 LangChain初体验以[作者李多多（莫尔索）的开放学习站](https://aitutor.liduos.com/)核对作者身份、实践取向和相关公开伴学材料；站点明确称其为个人学习笔记并宣传纸书，但它不是人民邮电出版社纸书的逐页全文，因此本课程把来源级别记为“作者授权伴学样本”，不虚构完整书稿访问权。

第2章 LangChain初体验以[首版公开书目与完整目录](https://m.bookschina.com/9299314.htm)核对《LangChain编程：从入门到实践》、人民邮电出版社、2024年4月、192页、ISBN 9787115639424，以及前言、10章和全部节/小节。正式分母是1个前言、10个章标题和135个节/小节，共146个层级；课程用学习地图、前言、10章和总复习共13页承载。

第2章 LangChain初体验以[LangChain当前官方总览](https://docs.langchain.com/oss/python/langchain/overview)与[v1迁移指南](https://docs.langchain.com/oss/python/migrate/langchain-v1)建立第二条事实轨道：当前v1精简核心命名空间、把大量旧Chain与检索接口移到langchain-classic，并以create_agent、中间件、LangGraph和LangSmith组织主路径。本站不复制纸书或伴学站正文、代码和图片，所有中文解释、双轨迁移、反例、交互、练习与答案均为独立重写。

### 本页独立事实来源

- [LangChain当前官方总览](https://docs.langchain.com/oss/python/langchain/overview)：在第2章 LangChain初体验中，核对当前v1的agent harness、模型接口、LangGraph与LangSmith定位。
- [LangChain v1官方迁移指南](https://docs.langchain.com/oss/python/migrate/langchain-v1)：在第2章 LangChain初体验中，核对精简命名空间、langchain-classic、create_agent和破坏性变化。
- [LangSmith官方可观测性文档](https://docs.langchain.com/langsmith/observability)：在第2章 LangChain初体验中，核对trace、调试和评估的当前证据入口。
- [LangChain官方安全公告](https://github.com/langchain-ai/langchain/security/advisories)：在第2章 LangChain初体验中，核对依赖与模板等安全风险不能只靠书中静态建议。

## 146个正式坐标中的本页覆盖

### 第2章 LangChain初体验

**坐标 1/13：第2章 LangChain初体验。** 第2章 LangChain初体验把第2章 LangChain初体验映射到版本、输入schema、执行状态、输出证据和失败边界，再用单一迁移故障定位首个不兼容点。 对第2章 LangChain初体验的验收必须同时保存依赖锁、输入输出schema、参考trace、单一迁移故障、独立评估和回退结果；仅有导入成功、无异常或最终答案流畅，都不能证明当前v1兼容。

### 2.1 开发环境准备

**坐标 2/13：2.1 开发环境准备。** 第2章 LangChain初体验冻结Python与包锁，隔离密钥，保存输入输出schema和trace；一次本地成功不能替代服务合同、安全检查与回退。 对第2章 LangChain初体验的验收必须同时保存依赖锁、输入输出schema、参考trace、单一迁移故障、独立评估和回退结果；仅有导入成功、无异常或最终答案流畅，都不能证明当前v1兼容。

### 2.1.1 管理工具安装

**坐标 3/13：2.1.1 管理工具安装。** 第2章 LangChain初体验冻结Python与包锁，隔离密钥，保存输入输出schema和trace；一次本地成功不能替代服务合同、安全检查与回退。 对第2章 LangChain初体验的验收必须同时保存依赖锁、输入输出schema、参考trace、单一迁移故障、独立评估和回退结果；仅有导入成功、无异常或最终答案流畅，都不能证明当前v1兼容。

### 2.1.2 源码安装

**坐标 4/13：2.1.2 源码安装。** 第2章 LangChain初体验冻结Python与包锁，隔离密钥，保存输入输出schema和trace；一次本地成功不能替代服务合同、安全检查与回退。 对第2章 LangChain初体验的验收必须同时保存依赖锁、输入输出schema、参考trace、单一迁移故障、独立评估和回退结果；仅有导入成功、无异常或最终答案流畅，都不能证明当前v1兼容。

### 2.1.3 其他库安装

**坐标 5/13：2.1.3 其他库安装。** 第2章 LangChain初体验冻结Python与包锁，隔离密钥，保存输入输出schema和trace；一次本地成功不能替代服务合同、安全检查与回退。 对第2章 LangChain初体验的验收必须同时保存依赖锁、输入输出schema、参考trace、单一迁移故障、独立评估和回退结果；仅有导入成功、无异常或最终答案流畅，都不能证明当前v1兼容。

### 2.2 快速开始

**坐标 6/13：2.2 快速开始。** 第2章 LangChain初体验把2.2 快速开始映射到版本、输入schema、执行状态、输出证据和失败边界，再用单一迁移故障定位首个不兼容点。 对第2章 LangChain初体验的验收必须同时保存依赖锁、输入输出schema、参考trace、单一迁移故障、独立评估和回退结果；仅有导入成功、无异常或最终答案流畅，都不能证明当前v1兼容。

### 2.2.1 语言模型

**坐标 7/13：2.2.1 语言模型。** 第2章 LangChain初体验用消息角色、模板变量、模型配置和解析schema组成类型链，并分别测试正常输出、拒答、截断与格式错误。 对第2章 LangChain初体验的验收必须同时保存依赖锁、输入输出schema、参考trace、单一迁移故障、独立评估和回退结果；仅有导入成功、无异常或最终答案流畅，都不能证明当前v1兼容。

### 2.2.2 提示模板

**坐标 8/13：2.2.2 提示模板。** 第2章 LangChain初体验用消息角色、模板变量、模型配置和解析schema组成类型链，并分别测试正常输出、拒答、截断与格式错误。 对第2章 LangChain初体验的验收必须同时保存依赖锁、输入输出schema、参考trace、单一迁移故障、独立评估和回退结果；仅有导入成功、无异常或最终答案流畅，都不能证明当前v1兼容。

### 2.2.3 输出解析器

**坐标 9/13：2.2.3 输出解析器。** 第2章 LangChain初体验用消息角色、模板变量、模型配置和解析schema组成类型链，并分别测试正常输出、拒答、截断与格式错误。 对第2章 LangChain初体验的验收必须同时保存依赖锁、输入输出schema、参考trace、单一迁移故障、独立评估和回退结果；仅有导入成功、无异常或最终答案流畅，都不能证明当前v1兼容。

### 2.2.4 使用LCEL进行组合

**坐标 10/13：2.2.4 使用LCEL进行组合。** 第2章 LangChain初体验把2.2.4 使用LCEL进行组合映射到版本、输入schema、执行状态、输出证据和失败边界，再用单一迁移故障定位首个不兼容点。 对第2章 LangChain初体验的验收必须同时保存依赖锁、输入输出schema、参考trace、单一迁移故障、独立评估和回退结果；仅有导入成功、无异常或最终答案流畅，都不能证明当前v1兼容。

### 2.2.5 使用LangSmith进行观测

**坐标 11/13：2.2.5 使用LangSmith进行观测。** 第2章 LangChain初体验冻结Python与包锁，隔离密钥，保存输入输出schema和trace；一次本地成功不能替代服务合同、安全检查与回退。 对第2章 LangChain初体验的验收必须同时保存依赖锁、输入输出schema、参考trace、单一迁移故障、独立评估和回退结果；仅有导入成功、无异常或最终答案流畅，都不能证明当前v1兼容。

### 2.2.6 使用LangServe提供服务

**坐标 12/13：2.2.6 使用LangServe提供服务。** 第2章 LangChain初体验冻结Python与包锁，隔离密钥，保存输入输出schema和trace；一次本地成功不能替代服务合同、安全检查与回退。 对第2章 LangChain初体验的验收必须同时保存依赖锁、输入输出schema、参考trace、单一迁移故障、独立评估和回退结果；仅有导入成功、无异常或最终答案流畅，都不能证明当前v1兼容。

### 2.3 最佳安全实践

**坐标 13/13：2.3 最佳安全实践。** 第2章 LangChain初体验冻结Python与包锁，隔离密钥，保存输入输出schema和trace；一次本地成功不能替代服务合同、安全检查与回退。 对第2章 LangChain初体验的验收必须同时保存依赖锁、输入输出schema、参考trace、单一迁移故障、独立评估和回退结果；仅有导入成功、无异常或最终答案流畅，都不能证明当前v1兼容。

## 先预测，再操作双轨实验

先预测：在第2章 LangChain初体验只注入“使用未锁定最新版依赖和真实密钥，成功一次后隐藏trace与失败输出”时，依赖解析、输入schema、执行事件、工具/检索状态、输出解析或独立评估中的哪一项最先变化？请先写下可观测信号，再切换原书/v1轨道和参考/故障trace。

<Stepper>
  <Step title="版本合同：比较原书与v1">
    <Lcp02FirstExperienceVersionContractLab />
  </Step>
  <Step title="执行轨迹：重放参考与故障">
    <Lcp02FirstExperienceRunnableTraceLab />
  </Step>
  <Step title="发布证据门：检查评估与回退">
    <Lcp02FirstExperienceReleaseGateLab />
  </Step>
</Stepper>

## 最小可复现与迁移协议

1. 为第2章 LangChain初体验分别冻结原书2024与当前v1的Python、包锁、集成包、模型/工具版本、输入schema、随机性和评估集，禁止跨轨拼接。
2. 在两条轨道运行参考流程，保存环境锁、输入输出schema、trace、服务合同与密钥泄露检查；若只能看到最终输出，就无法判断差异来自框架、provider还是数据。
3. 保持模型、数据和评估不变，只注入“使用未锁定最新版依赖和真实密钥，成功一次后隐藏trace与失败输出”，定位依赖、schema、状态、事件或输出中的首个分岔。
4. 撤销故障，从同一快照重放；只有trace、评估和回退都恢复，才允许批准迁移或发布。

<Callout type="trap" title="第2章 LangChain初体验误区一：旧API等于错误知识">
  原书2024接口是历史教学轨道，仍可解释稳定机制；第2章
  LangChain初体验应标注版本并建立迁移关系，而不是删除历史或假装它仍是当前默认。
</Callout>

<Callout type="trap" title="第2章 LangChain初体验误区二：改导入路径就完成迁移">
  v1改变命名空间、agent构建、状态、中间件和内容块等语义；第2章
  LangChain初体验必须重跑schema、stream、工具错误、trace和评估，不能只让代码通过import。
</Callout>

<Callout type="trap" title="第2章 LangChain初体验误区三：演示成功即可上线">
  LangChain应用还依赖模型、检索、外部工具、权限和数据；第2章
  LangChain初体验必须覆盖故障、重复事件、敏感数据、回退与持续评估。
</Callout>

## 小结

- 环境、LCEL、LangSmith与安全纳入证据链
- 原书轨道解释2024语义，v1文档重建接口
- schema和trace揭示行为并定位不兼容点
- 独立评估与回退机制决定能否发布
- 交付环境锁、IO schema、服务合同与密钥检查

## 练习与答案

<Exercises>

**问题 1：第2章 LangChain初体验**

为第2章 LangChain初体验的第2章 LangChain初体验写一个原书2024输入和一个当前v1输入，指出最小迁移故障、首个可观测分岔、独立评估与回退条件。

<Answer>
  先分别锁定两条版本轨道，把第2章
  LangChain初体验映射到明确schema、执行状态和输出事件；参考运行保存环境锁、输入输出schema、trace、服务合同与密钥泄露检查，故障运行只注入“使用未锁定最新版依赖和真实密钥，成功一次后隐藏trace与失败输出”。若首个分岔可定位、当前v1行为满足独立评估且回退恢复参考状态，才能批准局部迁移；未测试provider、模型、数据源和部署仍在第2章
  LangChain初体验边界外。
</Answer>

**问题 2：2.1 开发环境准备**

为第2章 LangChain初体验的2·1 开发环境准备写一个原书2024输入和一个当前v1输入，指出最小迁移故障、首个可观测分岔、独立评估与回退条件。

<Answer>
  先分别锁定两条版本轨道，把2·1
  开发环境准备映射到明确schema、执行状态和输出事件；参考运行保存环境锁、输入输出schema、trace、服务合同与密钥泄露检查，故障运行只注入“使用未锁定最新版依赖和真实密钥，成功一次后隐藏trace与失败输出”。若首个分岔可定位、当前v1行为满足独立评估且回退恢复参考状态，才能批准局部迁移；未测试provider、模型、数据源和部署仍在第2章
  LangChain初体验边界外。
</Answer>

**问题 3：2.1.1 管理工具安装**

为第2章 LangChain初体验的2·1·1 管理工具安装写一个原书2024输入和一个当前v1输入，指出最小迁移故障、首个可观测分岔、独立评估与回退条件。

<Answer>
  先分别锁定两条版本轨道，把2·1·1
  管理工具安装映射到明确schema、执行状态和输出事件；参考运行保存环境锁、输入输出schema、trace、服务合同与密钥泄露检查，故障运行只注入“使用未锁定最新版依赖和真实密钥，成功一次后隐藏trace与失败输出”。若首个分岔可定位、当前v1行为满足独立评估且回退恢复参考状态，才能批准局部迁移；未测试provider、模型、数据源和部署仍在第2章
  LangChain初体验边界外。
</Answer>

**问题 4：2.1.2 源码安装**

为第2章 LangChain初体验的2·1·2 源码安装写一个原书2024输入和一个当前v1输入，指出最小迁移故障、首个可观测分岔、独立评估与回退条件。

<Answer>
  先分别锁定两条版本轨道，把2·1·2
  源码安装映射到明确schema、执行状态和输出事件；参考运行保存环境锁、输入输出schema、trace、服务合同与密钥泄露检查，故障运行只注入“使用未锁定最新版依赖和真实密钥，成功一次后隐藏trace与失败输出”。若首个分岔可定位、当前v1行为满足独立评估且回退恢复参考状态，才能批准局部迁移；未测试provider、模型、数据源和部署仍在第2章
  LangChain初体验边界外。
</Answer>

**问题 5：2.1.3 其他库安装**

为第2章 LangChain初体验的2·1·3 其他库安装写一个原书2024输入和一个当前v1输入，指出最小迁移故障、首个可观测分岔、独立评估与回退条件。

<Answer>
  先分别锁定两条版本轨道，把2·1·3
  其他库安装映射到明确schema、执行状态和输出事件；参考运行保存环境锁、输入输出schema、trace、服务合同与密钥泄露检查，故障运行只注入“使用未锁定最新版依赖和真实密钥，成功一次后隐藏trace与失败输出”。若首个分岔可定位、当前v1行为满足独立评估且回退恢复参考状态，才能批准局部迁移；未测试provider、模型、数据源和部署仍在第2章
  LangChain初体验边界外。
</Answer>

**问题 6：2.2 快速开始**

为第2章 LangChain初体验的2·2 快速开始写一个原书2024输入和一个当前v1输入，指出最小迁移故障、首个可观测分岔、独立评估与回退条件。

<Answer>
  先分别锁定两条版本轨道，把2·2
  快速开始映射到明确schema、执行状态和输出事件；参考运行保存环境锁、输入输出schema、trace、服务合同与密钥泄露检查，故障运行只注入“使用未锁定最新版依赖和真实密钥，成功一次后隐藏trace与失败输出”。若首个分岔可定位、当前v1行为满足独立评估且回退恢复参考状态，才能批准局部迁移；未测试provider、模型、数据源和部署仍在第2章
  LangChain初体验边界外。
</Answer>

**问题 7：2.2.1 语言模型**

为第2章 LangChain初体验的2·2·1 语言模型写一个原书2024输入和一个当前v1输入，指出最小迁移故障、首个可观测分岔、独立评估与回退条件。

<Answer>
  先分别锁定两条版本轨道，把2·2·1
  语言模型映射到明确schema、执行状态和输出事件；参考运行保存环境锁、输入输出schema、trace、服务合同与密钥泄露检查，故障运行只注入“使用未锁定最新版依赖和真实密钥，成功一次后隐藏trace与失败输出”。若首个分岔可定位、当前v1行为满足独立评估且回退恢复参考状态，才能批准局部迁移；未测试provider、模型、数据源和部署仍在第2章
  LangChain初体验边界外。
</Answer>

**问题 8：2.2.2 提示模板**

为第2章 LangChain初体验的2·2·2 提示模板写一个原书2024输入和一个当前v1输入，指出最小迁移故障、首个可观测分岔、独立评估与回退条件。

<Answer>
  先分别锁定两条版本轨道，把2·2·2
  提示模板映射到明确schema、执行状态和输出事件；参考运行保存环境锁、输入输出schema、trace、服务合同与密钥泄露检查，故障运行只注入“使用未锁定最新版依赖和真实密钥，成功一次后隐藏trace与失败输出”。若首个分岔可定位、当前v1行为满足独立评估且回退恢复参考状态，才能批准局部迁移；未测试provider、模型、数据源和部署仍在第2章
  LangChain初体验边界外。
</Answer>

**问题 9：2.2.3 输出解析器**

为第2章 LangChain初体验的2·2·3 输出解析器写一个原书2024输入和一个当前v1输入，指出最小迁移故障、首个可观测分岔、独立评估与回退条件。

<Answer>
  先分别锁定两条版本轨道，把2·2·3
  输出解析器映射到明确schema、执行状态和输出事件；参考运行保存环境锁、输入输出schema、trace、服务合同与密钥泄露检查，故障运行只注入“使用未锁定最新版依赖和真实密钥，成功一次后隐藏trace与失败输出”。若首个分岔可定位、当前v1行为满足独立评估且回退恢复参考状态，才能批准局部迁移；未测试provider、模型、数据源和部署仍在第2章
  LangChain初体验边界外。
</Answer>

**问题 10：2.2.4 使用LCEL进行组合**

为第2章 LangChain初体验的2·2·4 使用LCEL进行组合写一个原书2024输入和一个当前v1输入，指出最小迁移故障、首个可观测分岔、独立评估与回退条件。

<Answer>
  先分别锁定两条版本轨道，把2·2·4
  使用LCEL进行组合映射到明确schema、执行状态和输出事件；参考运行保存环境锁、输入输出schema、trace、服务合同与密钥泄露检查，故障运行只注入“使用未锁定最新版依赖和真实密钥，成功一次后隐藏trace与失败输出”。若首个分岔可定位、当前v1行为满足独立评估且回退恢复参考状态，才能批准局部迁移；未测试provider、模型、数据源和部署仍在第2章
  LangChain初体验边界外。
</Answer>

**问题 11：2.2.5 使用LangSmith进行观测**

为第2章 LangChain初体验的2·2·5 使用LangSmith进行观测写一个原书2024输入和一个当前v1输入，指出最小迁移故障、首个可观测分岔、独立评估与回退条件。

<Answer>
  先分别锁定两条版本轨道，把2·2·5
  使用LangSmith进行观测映射到明确schema、执行状态和输出事件；参考运行保存环境锁、输入输出schema、trace、服务合同与密钥泄露检查，故障运行只注入“使用未锁定最新版依赖和真实密钥，成功一次后隐藏trace与失败输出”。若首个分岔可定位、当前v1行为满足独立评估且回退恢复参考状态，才能批准局部迁移；未测试provider、模型、数据源和部署仍在第2章
  LangChain初体验边界外。
</Answer>

**问题 12：2.2.6 使用LangServe提供服务**

为第2章 LangChain初体验的2·2·6 使用LangServe提供服务写一个原书2024输入和一个当前v1输入，指出最小迁移故障、首个可观测分岔、独立评估与回退条件。

<Answer>
  先分别锁定两条版本轨道，把2·2·6
  使用LangServe提供服务映射到明确schema、执行状态和输出事件；参考运行保存环境锁、输入输出schema、trace、服务合同与密钥泄露检查，故障运行只注入“使用未锁定最新版依赖和真实密钥，成功一次后隐藏trace与失败输出”。若首个分岔可定位、当前v1行为满足独立评估且回退恢复参考状态，才能批准局部迁移；未测试provider、模型、数据源和部署仍在第2章
  LangChain初体验边界外。
</Answer>

**问题 13：2.3 最佳安全实践**

为第2章 LangChain初体验的2·3 最佳安全实践写一个原书2024输入和一个当前v1输入，指出最小迁移故障、首个可观测分岔、独立评估与回退条件。

<Answer>
  先分别锁定两条版本轨道，把2·3
  最佳安全实践映射到明确schema、执行状态和输出事件；参考运行保存环境锁、输入输出schema、trace、服务合同与密钥泄露检查，故障运行只注入“使用未锁定最新版依赖和真实密钥，成功一次后隐藏trace与失败输出”。若首个分岔可定位、当前v1行为满足独立评估且回退恢复参考状态，才能批准局部迁移；未测试provider、模型、数据源和部署仍在第2章
  LangChain初体验边界外。
</Answer>

**问题 14：为什么不能混合两条轨道**

解释第2章 LangChain初体验为什么不能在同一示例中无标记地混用原书v0接口、langchain-classic与当前v1接口。

<Answer>
  三者的命名空间、状态、事件、agent和内容语义可能不同。无标记混用会让读者无法复现依赖，也无法判断成功来自兼容层还是当前接口。正确做法是分开锁文件和运行入口，逐项写迁移映射，再用同一输入与评估比较行为。
</Answer>

**问题 15：发布证据**

第2章 LangChain初体验除了“代码能运行”还需要哪些证据才能上线？

<Answer>
  第2章
  LangChain初体验至少需要依赖与许可证清单、输入输出schema、完整trace、失败和重试路径、检索或工具调用证据、安全与敏感数据检查、独立质量评估、延迟成本预算、监控告警以及可执行回退快照。任一项未知都应显式列入第2章
  LangChain初体验的发布边界。
</Answer>

</Exercises>

## 术语与运行证据

在第2章 LangChain初体验中，<Term def="第2章 LangChain初体验把原书2024与当前v1分开的依赖和接口语义">版本轨道</Term>、<Term def="第2章 LangChain初体验对消息、文档、工具参数或事件负载的类型合同">输入schema</Term>、<Term def="第2章 LangChain初体验把模型、检索、工具、状态和错误关联起来的事件树">执行trace</Term>、<Term def="第2章 LangChain初体验只改变一个版本或接口假设以定位不兼容点">迁移故障</Term>、<Term def="第2章 LangChain初体验不复用演示样本的质量、安全、延迟和成本检查">独立评估</Term>、<Term def="第2章 LangChain初体验能够恢复依赖、状态和服务行为的可部署版本">回退快照</Term>共同约束发布。术语必须落到锁文件、schema、trace、评估和回退；把旧类名替换成新类名而不重跑行为合同，不叫迁移。

<Glossary>
  <GlossaryItem term="版本轨道">
    第2章 LangChain初体验把原书2024与当前v1分开的依赖和接口语义。
  </GlossaryItem>
  <GlossaryItem term="输入schema">
    第2章 LangChain初体验对消息、文档、工具参数或事件负载的类型合同。
  </GlossaryItem>
  <GlossaryItem term="执行trace">
    第2章 LangChain初体验把模型、检索、工具、状态和错误关联起来的事件树。
  </GlossaryItem>
  <GlossaryItem term="迁移故障">
    第2章 LangChain初体验只改变一个版本或接口假设以定位不兼容点。
  </GlossaryItem>
  <GlossaryItem term="独立评估">
    第2章 LangChain初体验不复用演示样本的质量、安全、延迟和成本检查。
  </GlossaryItem>
  <GlossaryItem term="回退快照">
    第2章 LangChain初体验能够恢复依赖、状态和服务行为的可部署版本。
  </GlossaryItem>
</Glossary>

<Attribution
  mode="independent-rewrite"
  sourceBasis="author-authorized-companion-sample-plus-public-toc-and-current-upstream-docs"
  workTitle="李多多（莫尔索）《LangChain编程：从入门到实践》"
  adaptedUrl="https://aitutor.liduos.com/"
/>
