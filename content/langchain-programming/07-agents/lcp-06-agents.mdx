---
title: "第6章 智能代理设计"
type: C
section: "第6章 智能代理设计"
order: 7
description: "第6章 智能代理设计按首版正式目录重建，以Runnable、trace和故障注入复现LangChain数据流。"
demo: true
math: true
sourceUrl: "https://aitutor.liduos.com/"
draft: false
---


## 为什么先冻结依赖、数据流与trace

、、、、、共同构成本页坐标。从LLM驱动决策到LangChain代理类型、自定义工具与多模态代理，强调每一步观察、选择、权限和预算可审计。

LangChain是快速演进的框架，本书首版记录的是2023至2024年前后的模块和接口。学习时必须分开原书的架构脉络、首版代码形状和后来版本的迁移方式。类名废弃不会让Runnable、schema、检索、工具权限、记忆隔离和事件追踪失效；实现应以端口与契约测试隔离具体版本。

每次运行保存Python版本、依赖锁摘要、代码提交、环境配置名、输入、节点图、模型与工具版本、事件trace、原始错误、输出校验和回退动作。只保存最终回答无法区分模板、模型、检索、记忆、工具或回调中的首个错误，也无法证明升级前后语义相同。


## 数学与执行骨架

$$
a_t=policy(state_t,tools)
$$

$$
state_{t+1}=reduce(state_t,observation_t)
$$

$$
budget_{t+1}=budget_t-cost(a_t)
$$

$$
execute=allow(permission\land schema)
$$

公式用于固定比较对象：组合必须说明函数方向，检索必须说明分母，代理必须有预算，事件必须有父子关系。框架生成的图或trace只是观测结果，仍需用输入输出断言和故障样本证明业务语义。

## 三段可运行骨架

```python
def register_tool(name, schema, permissions, invoke):
    return {"name": name, "schema": schema, "permissions": permissions, "invoke": invoke}
```

```python
def execute(tool, arguments, permission):
    tool["schema"](arguments)
    if permission not in tool["permissions"]: raise PermissionError(tool["name"])
    return tool["invoke"](arguments)
```

```python
def agent_loop(state, choose, tools, max_steps=5):
    trace = []
    for _ in range(max_steps):
        action = choose(state, tools); trace.append(action)
        if action["name"] == "finish": return {"state": state, "trace": trace}
    raise RuntimeError("step budget exhausted")
```

代码通过普通Python表达核心合同，可在没有外部模型、LangSmith或LangServe时运行。接入真实LangChain版本时，由薄适配器映射到相应Runnable、模型、检索器或回调API；契约测试不随类名变化，从而可以精确定位迁移差异。

## 固定样例与手算

先用三个节点构造管道：模板节点接收字典并输出消息，模型假对象接收消息并输出固定结构，解析节点验证必需字段。分别调用单次、两项批量和逐块流式接口，聚合后的业务结果应一致；若事件数量或顺序不同，差异必须在trace中可解释。

再取查询向量q=(1,0)、文档向量d1=(0.8,0.6)、d2=(0,1)，余弦相似度分别为0.8和0。检索top-1应返回d1，但这只证明向量邻近，不证明d1支持答案。生成前仍需核对文档ID、嵌入版本、文本范围和引用；空召回必须拒答。

代理实验注册read与write两个工具，普通权限只允许read。模型若选择write，schema可以正确但权限门禁仍应拒绝；工具超时不得被改写成“执行成功”。记忆实验再写入两名用户的同名事实，读取必须按租户隔离，并在过期时间后删除。

最后注入三个故障：依赖版本改变输出字段、检索器返回失效文档ID、回调处理器抛出异常。系统应分别在解析、引用校验和观察端口报告首个分叉；回调失败不篡改已完成的业务结果，解析与引用失败则阻断发布。

## 常见失败与回退


## 本章回顾

从LLM驱动决策到LangChain代理类型、自定义工具与多模态代理，强调每一步观察、选择、权限和预算可审计。 掌握标准不是记住快速变化的API，而是能重建节点、合同、trace、失败和回退。通过标准为：能注册带schema与权限的工具，运行有界代理循环，并在越权、超时、重复动作或预算耗尽时安全停止。


## 前后导航

- [第5章 RAG](/learn/langchain-programming/06-rag/lcp-05-rag)
- [第7章 记忆组件](/learn/langchain-programming/08-memory/lcp-07-memory)

