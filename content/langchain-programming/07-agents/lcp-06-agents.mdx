---
title: 第6章 智能代理设计
type: C
section: 第6章 智能代理设计
order: 7
description: >-
  把模型、工具、提示、状态、循环、停止条件和人工介入组成agent合同；并列原书2024与当前v1，用版本合同、执行trace和发布门交付工具schema、状态轨迹、权限矩阵、停止原因与恢复检查点
demo: true
math: false
sourceUrl: "https://aitutor.liduos.com/"
draft: false
qualityVersion: 2
practiceMode: simulation
sourceMode: independent-rewrite
officialUnitId: lcp-06
---

import {
  Objectives,
  Term,
  Callout,
  Stepper,
  Step,
  Exercises,
  Answer,
  Glossary,
  GlossaryItem,
  Attribution,
} from "@/components/mdx/mdx-components";
import {
  Lcp06AgentsVersionContractLab,
  Lcp06AgentsRunnableTraceLab,
  Lcp06AgentsReleaseGateLab,
} from "@/components/mdx/langchain-programming/v2/lcp-06-agents";

<Objectives>

- 解释代理概念、代理类型、自定义工具与多模态代理的输入schema、执行状态、事件与输出，而不只背API名称
- 并列原书2024轨道与当前LangChain v1轨道，定位“让模型自由选择高权限工具，并把成功最终答案当成每次工具调用都正确”的首个分岔
- 交付工具schema、状态轨迹、权限矩阵、停止原因与恢复检查点，说明评估、回退、安全与未测试边界

</Objectives>

## 为什么从这个问题开始

第6章 智能代理设计围绕“智能代理怎样在多模态工具调用中保持参数验证、权限最小化和可恢复状态？”建立贯穿任务：在锁定的2024原书轨道和当前LangChain v1轨道上重放代理概念、代理类型、自定义工具与多模态代理。先预测依赖、schema、事件、状态、工具/检索调用或评估中的首个变化，再运行原书参考、v1参考、迁移故障与回退；只有守住“第6章 智能代理设计的包版本、输入输出schema、执行事件、评估结果与回退路径始终一致”并交付工具schema、状态轨迹、权限矩阵、停止原因与恢复检查点，代码运行才构成可发布证据。

## 原书、伴学材料与当前v1边界

第6章 智能代理设计以[作者李多多（莫尔索）的开放学习站](https://aitutor.liduos.com/)核对作者身份、实践取向和相关公开伴学材料；站点明确称其为个人学习笔记并宣传纸书，但它不是人民邮电出版社纸书的逐页全文，因此本课程把来源级别记为“作者授权伴学样本”，不虚构完整书稿访问权。

第6章 智能代理设计以[首版公开书目与完整目录](https://m.bookschina.com/9299314.htm)核对《LangChain编程：从入门到实践》、人民邮电出版社、2024年4月、192页、ISBN 9787115639424，以及前言、10章和全部节/小节。正式分母是1个前言、10个章标题和135个节/小节，共146个层级；课程用学习地图、前言、10章和总复习共13页承载。

第6章 智能代理设计以[LangChain当前官方总览](https://docs.langchain.com/oss/python/langchain/overview)与[v1迁移指南](https://docs.langchain.com/oss/python/migrate/langchain-v1)建立第二条事实轨道：当前v1精简核心命名空间、把大量旧Chain与检索接口移到langchain-classic，并以create_agent、中间件、LangGraph和LangSmith组织主路径。本站不复制纸书或伴学站正文、代码和图片，所有中文解释、双轨迁移、反例、交互、练习与答案均为独立重写。

### 本页独立事实来源

- [LangChain当前官方总览](https://docs.langchain.com/oss/python/langchain/overview)：在第6章 智能代理设计中，核对当前v1的agent harness、模型接口、LangGraph与LangSmith定位。
- [LangChain v1官方迁移指南](https://docs.langchain.com/oss/python/migrate/langchain-v1)：在第6章 智能代理设计中，核对精简命名空间、langchain-classic、create_agent和破坏性变化。
- [LangGraph官方代码库](https://github.com/langchain-ai/langgraph)：在第6章 智能代理设计中，核对当前有状态、可恢复编排层的职责。
- [LangChain官方安全公告](https://github.com/langchain-ai/langchain/security/advisories)：在第6章 智能代理设计中，核对依赖与模板等安全风险不能只靠书中静态建议。

## 146个正式坐标中的本页覆盖

### 第6章 智能代理设计

**坐标 1/8：第6章 智能代理设计。** 第6章 智能代理设计把模型、工具schema、状态、循环、停止原因和权限写成agent合同，并用工具错误与人工介入路径测试恢复。 对第6章 智能代理设计的验收必须同时保存依赖锁、输入输出schema、参考trace、单一迁移故障、独立评估和回退结果；仅有导入成功、无异常或最终答案流畅，都不能证明当前v1兼容。

### 6.1 智能代理的概念

**坐标 2/8：6.1 智能代理的概念。** 第6章 智能代理设计把模型、工具schema、状态、循环、停止原因和权限写成agent合同，并用工具错误与人工介入路径测试恢复。 对第6章 智能代理设计的验收必须同时保存依赖锁、输入输出schema、参考trace、单一迁移故障、独立评估和回退结果；仅有导入成功、无异常或最终答案流畅，都不能证明当前v1兼容。

### 6.2 LangChain中的代理

**坐标 3/8：6.2 LangChain中的代理。** 第6章 智能代理设计把模型、工具schema、状态、循环、停止原因和权限写成agent合同，并用工具错误与人工介入路径测试恢复。 对第6章 智能代理设计的验收必须同时保存依赖锁、输入输出schema、参考trace、单一迁移故障、独立评估和回退结果；仅有导入成功、无异常或最终答案流畅，都不能证明当前v1兼容。

### 6.2.1 LLM驱动的智能代理

**坐标 4/8：6.2.1 LLM驱动的智能代理。** 第6章 智能代理设计把模型、工具schema、状态、循环、停止原因和权限写成agent合同，并用工具错误与人工介入路径测试恢复。 对第6章 智能代理设计的验收必须同时保存依赖锁、输入输出schema、参考trace、单一迁移故障、独立评估和回退结果；仅有导入成功、无异常或最终答案流畅，都不能证明当前v1兼容。

### 6.2.2 LangChain中的代理

**坐标 5/8：6.2.2 LangChain中的代理。** 第6章 智能代理设计把模型、工具schema、状态、循环、停止原因和权限写成agent合同，并用工具错误与人工介入路径测试恢复。 对第6章 智能代理设计的验收必须同时保存依赖锁、输入输出schema、参考trace、单一迁移故障、独立评估和回退结果；仅有导入成功、无异常或最终答案流畅，都不能证明当前v1兼容。

### 6.2.3 代理的类型

**坐标 6/8：6.2.3 代理的类型。** 第6章 智能代理设计把模型、工具schema、状态、循环、停止原因和权限写成agent合同，并用工具错误与人工介入路径测试恢复。 对第6章 智能代理设计的验收必须同时保存依赖锁、输入输出schema、参考trace、单一迁移故障、独立评估和回退结果；仅有导入成功、无异常或最终答案流畅，都不能证明当前v1兼容。

### 6.2.4 自定义代理工具

**坐标 7/8：6.2.4 自定义代理工具。** 第6章 智能代理设计把模型、工具schema、状态、循环、停止原因和权限写成agent合同，并用工具错误与人工介入路径测试恢复。 对第6章 智能代理设计的验收必须同时保存依赖锁、输入输出schema、参考trace、单一迁移故障、独立评估和回退结果；仅有导入成功、无异常或最终答案流畅，都不能证明当前v1兼容。

### 6.3 设计并实现一个多模态代理

**坐标 8/8：6.3 设计并实现一个多模态代理。** 第6章 智能代理设计把模型、工具schema、状态、循环、停止原因和权限写成agent合同，并用工具错误与人工介入路径测试恢复。 对第6章 智能代理设计的验收必须同时保存依赖锁、输入输出schema、参考trace、单一迁移故障、独立评估和回退结果；仅有导入成功、无异常或最终答案流畅，都不能证明当前v1兼容。

## 先预测，再操作双轨实验

先预测：在第6章 智能代理设计只注入“让模型自由选择高权限工具，并把成功最终答案当成每次工具调用都正确”时，依赖解析、输入schema、执行事件、工具/检索状态、输出解析或独立评估中的哪一项最先变化？请先写下可观测信号，再切换原书/v1轨道和参考/故障trace。

<Stepper>
  <Step title="版本合同：比较原书与v1">
    <Lcp06AgentsVersionContractLab />
  </Step>
  <Step title="执行轨迹：重放参考与故障">
    <Lcp06AgentsRunnableTraceLab />
  </Step>
  <Step title="发布证据门：检查评估与回退">
    <Lcp06AgentsReleaseGateLab />
  </Step>
</Stepper>

## 最小可复现与迁移协议

1. 为第6章 智能代理设计分别冻结原书2024与当前v1的Python、包锁、集成包、模型/工具版本、输入schema、随机性和评估集，禁止跨轨拼接。
2. 在两条轨道运行参考流程，保存工具schema、状态轨迹、权限矩阵、停止原因与恢复检查点；若只能看到最终输出，就无法判断差异来自框架、provider还是数据。
3. 保持模型、数据和评估不变，只注入“让模型自由选择高权限工具，并把成功最终答案当成每次工具调用都正确”，定位依赖、schema、状态、事件或输出中的首个分岔。
4. 撤销故障，从同一快照重放；只有trace、评估和回退都恢复，才允许批准迁移或发布。

<Callout type="trap" title="第6章 智能代理设计误区一：旧API等于错误知识">
  原书2024接口是历史教学轨道，仍可解释稳定机制；第6章
  智能代理设计应标注版本并建立迁移关系，而不是删除历史或假装它仍是当前默认。
</Callout>

<Callout type="trap" title="第6章 智能代理设计误区二：改导入路径就完成迁移">
  v1改变命名空间、agent构建、状态、中间件和内容块等语义；第6章
  智能代理设计必须重跑schema、stream、工具错误、trace和评估，不能只让代码通过import。
</Callout>

<Callout type="trap" title="第6章 智能代理设计误区三：演示成功即可上线">
  LangChain应用还依赖模型、检索、外部工具、权限和数据；第6章
  智能代理设计必须覆盖故障、重复事件、敏感数据、回退与持续评估。
</Callout>

## 小结

- 代理概念、类型与自定义工具纳入证据链
- 原书轨道解释2024语义，v1文档重建接口
- schema和trace揭示行为并定位不兼容点
- 独立评估与回退机制决定能否发布
- 交付工具schema、权限矩阵与恢复检查点

## 练习与答案

<Exercises>

**问题 1：第6章 智能代理设计**

为第6章 智能代理设计的第6章 智能代理设计写一个原书2024输入和一个当前v1输入，指出最小迁移故障、首个可观测分岔、独立评估与回退条件。

<Answer>
  先分别锁定两条版本轨道，把第6章
  智能代理设计映射到明确schema、执行状态和输出事件；参考运行保存工具schema、状态轨迹、权限矩阵、停止原因与恢复检查点，故障运行只注入“让模型自由选择高权限工具，并把成功最终答案当成每次工具调用都正确”。若首个分岔可定位、当前v1行为满足独立评估且回退恢复参考状态，才能批准局部迁移；未测试provider、模型、数据源和部署仍在第6章
  智能代理设计边界外。
</Answer>

**问题 2：6.1 智能代理的概念**

为第6章 智能代理设计的6·1 智能代理的概念写一个原书2024输入和一个当前v1输入，指出最小迁移故障、首个可观测分岔、独立评估与回退条件。

<Answer>
  先分别锁定两条版本轨道，把6·1
  智能代理的概念映射到明确schema、执行状态和输出事件；参考运行保存工具schema、状态轨迹、权限矩阵、停止原因与恢复检查点，故障运行只注入“让模型自由选择高权限工具，并把成功最终答案当成每次工具调用都正确”。若首个分岔可定位、当前v1行为满足独立评估且回退恢复参考状态，才能批准局部迁移；未测试provider、模型、数据源和部署仍在第6章
  智能代理设计边界外。
</Answer>

**问题 3：6.2 LangChain中的代理**

为第6章 智能代理设计的6·2 LangChain中的代理写一个原书2024输入和一个当前v1输入，指出最小迁移故障、首个可观测分岔、独立评估与回退条件。

<Answer>
  先分别锁定两条版本轨道，把6·2
  LangChain中的代理映射到明确schema、执行状态和输出事件；参考运行保存工具schema、状态轨迹、权限矩阵、停止原因与恢复检查点，故障运行只注入“让模型自由选择高权限工具，并把成功最终答案当成每次工具调用都正确”。若首个分岔可定位、当前v1行为满足独立评估且回退恢复参考状态，才能批准局部迁移；未测试provider、模型、数据源和部署仍在第6章
  智能代理设计边界外。
</Answer>

**问题 4：6.2.1 LLM驱动的智能代理**

为第6章 智能代理设计的6·2·1 LLM驱动的智能代理写一个原书2024输入和一个当前v1输入，指出最小迁移故障、首个可观测分岔、独立评估与回退条件。

<Answer>
  先分别锁定两条版本轨道，把6·2·1
  LLM驱动的智能代理映射到明确schema、执行状态和输出事件；参考运行保存工具schema、状态轨迹、权限矩阵、停止原因与恢复检查点，故障运行只注入“让模型自由选择高权限工具，并把成功最终答案当成每次工具调用都正确”。若首个分岔可定位、当前v1行为满足独立评估且回退恢复参考状态，才能批准局部迁移；未测试provider、模型、数据源和部署仍在第6章
  智能代理设计边界外。
</Answer>

**问题 5：6.2.2 LangChain中的代理**

为第6章 智能代理设计的6·2·2 LangChain中的代理写一个原书2024输入和一个当前v1输入，指出最小迁移故障、首个可观测分岔、独立评估与回退条件。

<Answer>
  先分别锁定两条版本轨道，把6·2·2
  LangChain中的代理映射到明确schema、执行状态和输出事件；参考运行保存工具schema、状态轨迹、权限矩阵、停止原因与恢复检查点，故障运行只注入“让模型自由选择高权限工具，并把成功最终答案当成每次工具调用都正确”。若首个分岔可定位、当前v1行为满足独立评估且回退恢复参考状态，才能批准局部迁移；未测试provider、模型、数据源和部署仍在第6章
  智能代理设计边界外。
</Answer>

**问题 6：6.2.3 代理的类型**

为第6章 智能代理设计的6·2·3 代理的类型写一个原书2024输入和一个当前v1输入，指出最小迁移故障、首个可观测分岔、独立评估与回退条件。

<Answer>
  先分别锁定两条版本轨道，把6·2·3
  代理的类型映射到明确schema、执行状态和输出事件；参考运行保存工具schema、状态轨迹、权限矩阵、停止原因与恢复检查点，故障运行只注入“让模型自由选择高权限工具，并把成功最终答案当成每次工具调用都正确”。若首个分岔可定位、当前v1行为满足独立评估且回退恢复参考状态，才能批准局部迁移；未测试provider、模型、数据源和部署仍在第6章
  智能代理设计边界外。
</Answer>

**问题 7：6.2.4 自定义代理工具**

为第6章 智能代理设计的6·2·4 自定义代理工具写一个原书2024输入和一个当前v1输入，指出最小迁移故障、首个可观测分岔、独立评估与回退条件。

<Answer>
  先分别锁定两条版本轨道，把6·2·4
  自定义代理工具映射到明确schema、执行状态和输出事件；参考运行保存工具schema、状态轨迹、权限矩阵、停止原因与恢复检查点，故障运行只注入“让模型自由选择高权限工具，并把成功最终答案当成每次工具调用都正确”。若首个分岔可定位、当前v1行为满足独立评估且回退恢复参考状态，才能批准局部迁移；未测试provider、模型、数据源和部署仍在第6章
  智能代理设计边界外。
</Answer>

**问题 8：6.3 设计并实现一个多模态代理**

为第6章 智能代理设计的6·3 设计并实现一个多模态代理写一个原书2024输入和一个当前v1输入，指出最小迁移故障、首个可观测分岔、独立评估与回退条件。

<Answer>
  先分别锁定两条版本轨道，把6·3
  设计并实现一个多模态代理映射到明确schema、执行状态和输出事件；参考运行保存工具schema、状态轨迹、权限矩阵、停止原因与恢复检查点，故障运行只注入“让模型自由选择高权限工具，并把成功最终答案当成每次工具调用都正确”。若首个分岔可定位、当前v1行为满足独立评估且回退恢复参考状态，才能批准局部迁移；未测试provider、模型、数据源和部署仍在第6章
  智能代理设计边界外。
</Answer>

**问题 9：为什么不能混合两条轨道**

解释第6章 智能代理设计为什么不能在同一示例中无标记地混用原书v0接口、langchain-classic与当前v1接口。

<Answer>
  三者的命名空间、状态、事件、agent和内容语义可能不同。无标记混用会让读者无法复现依赖，也无法判断成功来自兼容层还是当前接口。正确做法是分开锁文件和运行入口，逐项写迁移映射，再用同一输入与评估比较行为。
</Answer>

**问题 10：发布证据**

第6章 智能代理设计除了“代码能运行”还需要哪些证据才能上线？

<Answer>
  第6章
  智能代理设计至少需要依赖与许可证清单、输入输出schema、完整trace、失败和重试路径、检索或工具调用证据、安全与敏感数据检查、独立质量评估、延迟成本预算、监控告警以及可执行回退快照。任一项未知都应显式列入第6章
  智能代理设计的发布边界。
</Answer>

</Exercises>

## 术语与运行证据

在第6章 智能代理设计中，<Term def="第6章 智能代理设计把原书2024与当前v1分开的依赖和接口语义">版本轨道</Term>、<Term def="第6章 智能代理设计对消息、文档、工具参数或事件负载的类型合同">输入schema</Term>、<Term def="第6章 智能代理设计把模型、检索、工具、状态和错误关联起来的事件树">执行trace</Term>、<Term def="第6章 智能代理设计只改变一个版本或接口假设以定位不兼容点">迁移故障</Term>、<Term def="第6章 智能代理设计不复用演示样本的质量、安全、延迟和成本检查">独立评估</Term>、<Term def="第6章 智能代理设计能够恢复依赖、状态和服务行为的可部署版本">回退快照</Term>共同约束发布。术语必须落到锁文件、schema、trace、评估和回退；把旧类名替换成新类名而不重跑行为合同，不叫迁移。

<Glossary>
  <GlossaryItem term="版本轨道">
    第6章 智能代理设计把原书2024与当前v1分开的依赖和接口语义。
  </GlossaryItem>
  <GlossaryItem term="输入schema">
    第6章 智能代理设计对消息、文档、工具参数或事件负载的类型合同。
  </GlossaryItem>
  <GlossaryItem term="执行trace">
    第6章 智能代理设计把模型、检索、工具、状态和错误关联起来的事件树。
  </GlossaryItem>
  <GlossaryItem term="迁移故障">
    第6章 智能代理设计只改变一个版本或接口假设以定位不兼容点。
  </GlossaryItem>
  <GlossaryItem term="独立评估">
    第6章 智能代理设计不复用演示样本的质量、安全、延迟和成本检查。
  </GlossaryItem>
  <GlossaryItem term="回退快照">
    第6章 智能代理设计能够恢复依赖、状态和服务行为的可部署版本。
  </GlossaryItem>
</Glossary>

<Attribution
  mode="independent-rewrite"
  sourceBasis="author-authorized-companion-sample-plus-public-toc-and-current-upstream-docs"
  workTitle="李多多（莫尔索）《LangChain编程：从入门到实践》"
  adaptedUrl="https://aitutor.liduos.com/"
/>
