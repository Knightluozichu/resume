---
title: "第7章 记忆组件"
type: C
section: "第7章 记忆组件"
order: 8
description: "第7章 记忆组件按首版正式目录重建，以Runnable、trace和故障注入复现LangChain数据流。"
demo: true
math: true
sourceUrl: "https://aitutor.liduos.com/"
draft: true
---

import { Lcp07MemoryGraphLab, Lcp07MemoryRunLab, Lcp07MemoryFaultLab } from "@/components/mdx/langchain-programming/diagrams/lcp-07-memory";
import { Objectives, Callout, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step, Attribution } from "@/components/mdx/mdx-components";

<Objectives>

- 能解释短期记忆、窗口记忆、实体记忆的输入、输出、状态和首版版本语境
- 能推导并计算四个核心表达式，把节点、检索、记忆或trace映射到实现状态
- 能实现并运行三种交互实验，定位并发、上下文、依赖或工具故障的首个分叉
- 能设计正常与单一故障样本，独立证明“能在同一对话上比较不同记忆策略的保真、词元与污染风险，实现隔离、过期、删除和组合策略。”

</Objectives>

## 为什么先冻结依赖、数据流与trace

<Term def="短期记忆是第7章 记忆组件的第1个可执行证据坐标。">短期记忆</Term>、<Term def="窗口记忆是第7章 记忆组件的第2个可执行证据坐标。">窗口记忆</Term>、<Term def="实体记忆是第7章 记忆组件的第3个可执行证据坐标。">实体记忆</Term>、<Term def="知识图谱是第7章 记忆组件的第4个可执行证据坐标。">知识图谱</Term>、<Term def="摘要记忆是第7章 记忆组件的第5个可执行证据坐标。">摘要记忆</Term>、<Term def="向量记忆是第7章 记忆组件的第6个可执行证据坐标。">向量记忆</Term>共同构成本页坐标。比较缓冲、窗口、实体、知识图谱、向量与摘要记忆，说明写入、读取、压缩、持久化和接入代理的状态合同。

LangChain是快速演进的框架，本书首版记录的是2023至2024年前后的模块和接口。学习时必须分开原书的架构脉络、首版代码形状和后来版本的迁移方式。类名废弃不会让Runnable、schema、检索、工具权限、记忆隔离和事件追踪失效；实现应以端口与契约测试隔离具体版本。

每次运行保存Python版本、依赖锁摘要、代码提交、环境配置名、输入、节点图、模型与工具版本、事件trace、原始错误、输出校验和回退动作。只保存最终回答无法区分模板、模型、检索、记忆、工具或回调中的首个错误，也无法证明升级前后语义相同。

<Callout type="info">**验收命题：** 能在同一对话上比较不同记忆策略的保真、词元与污染风险，实现隔离、过期、删除和组合策略。</Callout>

## 数学与执行骨架

$$
memory_{t+1}=update(memory_t,event_t)
$$

$$
window=events_{t-k+1:t}
$$

$$
summary'=compress(summary,event)
$$

$$
retrieve=top_k(sim(query,item))
$$

公式用于固定比较对象：组合必须说明函数方向，检索必须说明分母，代理必须有预算，事件必须有父子关系。框架生成的图或trace只是观测结果，仍需用输入输出断言和故障样本证明业务语义。

## 三段可运行骨架

```python
def window_memory(events, size):
    return events[-size:]
```

```python
def write_memory(store, tenant, item, expires_at):
    store.setdefault(tenant, []).append({"item": item, "expires_at": expires_at})
```

```python
def read_memory(store, tenant, now):
    return [row["item"] for row in store.get(tenant, []) if row["expires_at"] > now]
```

代码通过普通Python表达核心合同，可在没有外部模型、LangSmith或LangServe时运行。接入真实LangChain版本时，由薄适配器映射到相应Runnable、模型、检索器或回调API；契约测试不随类名变化，从而可以精确定位迁移差异。

## 固定样例与手算

先用三个节点构造管道：模板节点接收字典并输出消息，模型假对象接收消息并输出固定结构，解析节点验证必需字段。分别调用单次、两项批量和逐块流式接口，聚合后的业务结果应一致；若事件数量或顺序不同，差异必须在trace中可解释。

再取查询向量q=(1,0)、文档向量d1=(0.8,0.6)、d2=(0,1)，余弦相似度分别为0.8和0。检索top-1应返回d1，但这只证明向量邻近，不证明d1支持答案。生成前仍需核对文档ID、嵌入版本、文本范围和引用；空召回必须拒答。

代理实验注册read与write两个工具，普通权限只允许read。模型若选择write，schema可以正确但权限门禁仍应拒绝；工具超时不得被改写成“执行成功”。记忆实验再写入两名用户的同名事实，读取必须按租户隔离，并在过期时间后删除。

最后注入三个故障：依赖版本改变输出字段、检索器返回失效文档ID、回调处理器抛出异常。系统应分别在解析、引用校验和观察端口报告首个分叉；回调失败不篡改已完成的业务结果，解析与引用失败则阻断发布。

## 常见失败与回退

<Callout type="trap">**本章高风险失败：** 把对话历史当可信事实或永久用户画像，混淆不同用户状态，或摘要后丢失关键约束却不留来源。</Callout>

<Callout type="trap">**框架抽象不是责任转移：** 链能运行不代表schema、来源、权限、记忆隔离和trace通过；中间合同失败必须阻断相应动作。</Callout>

<Callout type="tip">**回退顺序：** 环境锁 → 输入schema → 节点路由 → 模型或检索 → 输出解析 → 工具权限 → 记忆写入 → 回调。首个差异未解释前不比较整段回答。</Callout>

## 本章回顾

比较缓冲、窗口、实体、知识图谱、向量与摘要记忆，说明写入、读取、压缩、持久化和接入代理的状态合同。 掌握标准不是记住快速变化的API，而是能重建节点、合同、trace、失败和回退。通过标准为：能在同一对话上比较不同记忆策略的保真、词元与污染风险，实现隔离、过期、删除和组合策略。

<Glossary>

<GlossaryItem term="短期记忆">
  “短期记忆”是第7章 记忆组件的第1个执行坐标。定义包含输入、输出、状态、版本、错误和最小反例，并指出框架抽象下面的真实责任。
</GlossaryItem>

<GlossaryItem term="窗口记忆">
  “窗口记忆”是第7章 记忆组件的第2个执行坐标。定义包含输入、输出、状态、版本、错误和最小反例，并指出框架抽象下面的真实责任。
</GlossaryItem>

<GlossaryItem term="实体记忆">
  “实体记忆”是第7章 记忆组件的第3个执行坐标。定义包含输入、输出、状态、版本、错误和最小反例，并指出框架抽象下面的真实责任。
</GlossaryItem>

<GlossaryItem term="知识图谱">
  “知识图谱”是第7章 记忆组件的第4个执行坐标。定义包含输入、输出、状态、版本、错误和最小反例，并指出框架抽象下面的真实责任。
</GlossaryItem>

<GlossaryItem term="摘要记忆">
  “摘要记忆”是第7章 记忆组件的第5个执行坐标。定义包含输入、输出、状态、版本、错误和最小反例，并指出框架抽象下面的真实责任。
</GlossaryItem>

<GlossaryItem term="向量记忆">
  “向量记忆”是第7章 记忆组件的第6个执行坐标。定义包含输入、输出、状态、版本、错误和最小反例，并指出框架抽象下面的真实责任。
</GlossaryItem>

</Glossary>

<Exercises>

1. 如何为第7章 记忆组件建立最小可重放基线？

<Answer>固定依赖锁、三个Runnable假节点和一组输入，保存schema、事件、输出与错误，再运行单次、批量和故障路径。</Answer>

2. 如何证明“能在同一对话上比较不同记忆策略的保真、词元与污染风险，实现隔离、过期、删除和组合策略。”不是一次偶然成功？

<Answer>在同一冻结集合上并排运行正常路径与只破坏一个依赖、检索、工具、记忆或回调的路径，要求首个分叉符合预期。</Answer>

3. 修复第7章 记忆组件后怎样回归？

<Answer>先重建环境，再重放节点与trace，随后检查来源、权限和状态，最后评价业务输出；全部中间证据一致后才发布。</Answer>

</Exercises>

## 前后导航

- [第6章 智能代理设计](/learn/langchain-programming/07-agents/lcp-06-agents)
- [第8章 回调机制](/learn/langchain-programming/09-callbacks/lcp-08-callbacks)

<Attribution adaptedFrom="李多多（@莫尔索）《LangChain编程：从入门到实践》，人民邮电出版社，2024年4月，192页，ISBN 9787115639424 · 基于146个正式目录层级的独立教学重构" adaptedUrl="https://aitutor.liduos.com/" />
