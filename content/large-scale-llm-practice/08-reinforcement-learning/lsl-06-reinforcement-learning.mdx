---
title: "第6章 强化学习"
type: C
section: "第6章 强化学习"
order: 8
description: "从强化学习与监督学习的差异进入RLHF流程，依次重建偏好数据、奖励模型、策略梯度、广义优势估计、PPO和MOSS-RLHF实践。"
demo: true
math: true
sourceUrl: "https://intro-llm.github.io/"
draft: true
---

import {
  Lsl06ReinforcementLearningPipelineLab,
  Lsl06ReinforcementLearningTrainingLab,
  Lsl06ReinforcementLearningEvidenceLab,
} from "@/components/mdx/large-scale-llm-practice/diagrams/lsl-06-reinforcement-learning";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释RLHF、奖励模型、策略梯度的输入、输出、假设、指标和版本边界
- 能推导并手算条件概率、注意力、训练更新与证据门禁四个核心表达式
- 能实现三种交互实验，定位数据、张量、训练、反馈、服务或评测的首个分叉
- 能设计正常、边界与单故障样本，独立证明“能从成对偏好计算奖励损失与优势，解释PPO裁剪和KL约束，并检测奖励黑客、标注偏差与策略退化。”

</Objectives>

## 为什么先冻结版本、对象与验收命题

<Term def="RLHF是第6章 强化学习的第1个可执行坐标，必须关联输入、输出、指标和失败边界。">RLHF</Term>、<Term def="奖励模型是第6章 强化学习的第2个可执行坐标，必须关联输入、输出、指标和失败边界。">奖励模型</Term>、<Term def="策略梯度是第6章 强化学习的第3个可执行坐标，必须关联输入、输出、指标和失败边界。">策略梯度</Term>、<Term def="广义优势估计是第6章 强化学习的第4个可执行坐标，必须关联输入、输出、指标和失败边界。">广义优势估计</Term>、<Term def="PPO是第6章 强化学习的第5个可执行坐标，必须关联输入、输出、指标和失败边界。">PPO</Term>、<Term def="奖励黑客是第6章 强化学习的第6个可执行坐标，必须关联输入、输出、指标和失败边界。">奖励黑客</Term>
共同构成本页坐标。从强化学习与监督学习的差异进入RLHF流程，依次重建偏好数据、奖励模型、策略梯度、广义优势估计、PPO和MOSS-RLHF实践。
本页不是模型或工具名清单，而是从问题、数学、数据、实现、实验到失败回退的闭环；每项结论都要求找到可观察的中间对象。

本书严格采用2024年第一版：前言、数学符号、8章、参考文献和索引。2025年第二版扩展为12章且ISBN不同，不并入本页目录分母。模型库、框架接口和在线服务会继续变化，因此固定原理、原书历史坐标、本站复现实验与后续工程选择之间的边界。

每次实验保存来源版本、数据哈希与切分、词元器、模型配置、代码提交、依赖锁、种子、硬件、预算、阶段产物和逐样本评测。只有最终文本、训练损失或排行榜无法区分数据泄漏、mask错位、预算差异、奖励偏移和评测污染，也不能证明另一台机器可重放。

<Callout type="info">
  **验收命题：**
  能从成对偏好计算奖励损失与优势，解释PPO裁剪和KL约束，并检测奖励黑客、标注偏差与策略退化。
</Callout>

## 数学与资源骨架

$$
p(x)=prod_{t=1}^{T}p(x_tmid x_{1:t-1})
$$

$$
A(Q,K,V)=operatorname{softmax}left(rac{QK^T}{sqrt{d_k}}+M
ight)V
$$

$$
	heta_{k+1}=	heta_k-eta,widehat{
abla}_{	heta}mathcal{L}(	heta_k)
$$

$$
E=source	imes reproducibility	imes boundary
$$

第一式固定自回归概率及条件上下文；比较困惑度时还要固定分词器或换成相同字符、字节、完整序列单位。第二式固定注意力形状、缩放与mask；被遮蔽位置的权重必须为零。第三式要求学习率、有效批量、梯度累积和数据顺序进入训练合同。第四式把来源、可复现性和边界同时作为强结论的必要条件。

公式不仅用于说明概念，还用于检查实现。先写每个张量的批量、序列、头和通道维度，逐元素验证mask，再记录前向值、损失、梯度范数和更新后参数。若使用混合精度、并行通信、低秩适配或PPO，继续记录缩放、分片、裁剪和KL项，不能把框架默认值当成数学定义。

## 三段可运行骨架

```python
def freeze_run(source, data_hash, tokenizer, model, seed, config):
    return {"source": source, "data": data_hash, "tokenizer": tokenizer,
            "model": model, "seed": seed, "config": config}
```

```python
def compare_controlled(baseline, candidate, changed):
    assert len(changed) == 1, "一次只改变一个可归因因素"
    return {key: candidate[key] - baseline[key] for key in baseline}
```

```python
def evidence_gate(records):
    required = {"source", "inputs", "config", "outputs", "limits"}
    missing = [i for i, record in enumerate(records)
               if not required.issubset(record)]
    return {"pass": not missing, "missing_records": missing}
```

第一段产生不可变运行清单，第二段强制单变量对照，第三段在结论前检查证据。接入HuggingFace、DeepSpeed、MOSS、LangChain或vLLM时，只由薄适配器提供真实执行对象；清单字段、对照顺序和失败协议保持稳定，使库升级导致的默认值变化成为可见差异。

## 固定样例与手算

取三个目标词元的条件概率0.8、0.5和0.25，平均负对数似然约为0.998，困惑度约为2.71。若另一分词器把相同文本切为六个词元，词元级困惑度不能直接横比；应报告完整序列对数似然、字符或字节单位，并附切分结果和未知字符。

注意力例取二维查询q=(1,0)，键k1=(1,0)、k2=(0,1)，值v1=(2,0)、v2=(0,2)。缩放得分为1/根号2和0，softmax权重约0.67与0.33，输出约(1.34,0.66)。加入因果mask后，被遮蔽位置权重严格为0；否则损失下降可能来自未来信息泄漏。

训练例令标量参数为2、梯度为0.3、学习率为0.1，更新后参数应为1.97。若梯度累积四次却未除以累积步数，等效更新扩大四倍；若数据并行又做求和归约，世界规模也会改变有效梯度。资源账本必须同时记录全局批量、词元数、峰值显存、通信字节和每秒有效词元。

## 实验设计与故障注入

基线冻结数据、分词、初始化、预算和评测，只改变一个因素：过滤规则、mask、位置方法、并行拓扑、LoRA秩、奖励符号、检索器或解码参数。运行至少一个正常样本、一个边界样本和一个单故障样本；随机阶段用多种子报告均值、方差和逐样本结果。

故障轮次依次注入近重复跨集合泄漏、标签mask错一位、有效批量不等、断点缺失优化器状态、奖励方向翻转、检索证据为空、工具参数越权和群体切片遗漏。系统应在数据、张量、资源、反馈、服务或评测门禁处报告首个分叉，而不是等待用户看到错误答案。

修复后先重放原失败，再运行相邻边界和正常基线。确定性阶段逐项一致，随机阶段落入预先声明区间；若只提升平均质量却恶化尾延迟、安全切片或成本预算，候选版本不能晋级。线上反馈进入新的隔离数据版本，不能直接污染测试集或奖励协议。

## 正式目录逐项讲解

### 第6章 强化学习

“章 强化学习”沿状态、动作、偏好、奖励、优势、策略比率和KL约束追踪。保存标注协议、奖励分布与逐样本轨迹，分别测试正常偏好、并列偏好和奖励方向翻转；奖励升高不等于帮助性、安全性与真实性同时改善。

针对“章 强化学习”，先预测只改变数据、mask、参数、并行拓扑、反馈或评测协议时哪个中间对象最先分叉，再运行正常与单故障路径。保存来源、配置、阶段产物和判断依据，不用最终平均分覆盖前序失败。

### 6.1 基于人类反馈的强化学习

“基于人类反馈的强化学习”沿状态、动作、偏好、奖励、优势、策略比率和KL约束追踪。保存标注协议、奖励分布与逐样本轨迹，分别测试正常偏好、并列偏好和奖励方向翻转；奖励升高不等于帮助性、安全性与真实性同时改善。

针对“基于人类反馈的强化学习”，先预测只改变数据、mask、参数、并行拓扑、反馈或评测协议时哪个中间对象最先分叉，再运行正常与单故障路径。保存来源、配置、阶段产物和判断依据，不用最终平均分覆盖前序失败。

### 6.1.1 强化学习概述

“强化学习概述”沿状态、动作、偏好、奖励、优势、策略比率和KL约束追踪。保存标注协议、奖励分布与逐样本轨迹，分别测试正常偏好、并列偏好和奖励方向翻转；奖励升高不等于帮助性、安全性与真实性同时改善。

针对“强化学习概述”，先预测只改变数据、mask、参数、并行拓扑、反馈或评测协议时哪个中间对象最先分叉，再运行正常与单故障路径。保存来源、配置、阶段产物和判断依据，不用最终平均分覆盖前序失败。

### 6.1.2 强化学习与有监督学习的区别

“强化学习与有监督学习的区别”沿状态、动作、偏好、奖励、优势、策略比率和KL约束追踪。保存标注协议、奖励分布与逐样本轨迹，分别测试正常偏好、并列偏好和奖励方向翻转；奖励升高不等于帮助性、安全性与真实性同时改善。

针对“强化学习与有监督学习的区别”，先预测只改变数据、mask、参数、并行拓扑、反馈或评测协议时哪个中间对象最先分叉，再运行正常与单故障路径。保存来源、配置、阶段产物和判断依据，不用最终平均分覆盖前序失败。

### 6.1.3 基于人类反馈的强化学习流程

“基于人类反馈的强化学习流程”沿状态、动作、偏好、奖励、优势、策略比率和KL约束追踪。保存标注协议、奖励分布与逐样本轨迹，分别测试正常偏好、并列偏好和奖励方向翻转；奖励升高不等于帮助性、安全性与真实性同时改善。

针对“基于人类反馈的强化学习流程”，先预测只改变数据、mask、参数、并行拓扑、反馈或评测协议时哪个中间对象最先分叉，再运行正常与单故障路径。保存来源、配置、阶段产物和判断依据，不用最终平均分覆盖前序失败。

### 6.2 奖励模型

“奖励模型”沿状态、动作、偏好、奖励、优势、策略比率和KL约束追踪。保存标注协议、奖励分布与逐样本轨迹，分别测试正常偏好、并列偏好和奖励方向翻转；奖励升高不等于帮助性、安全性与真实性同时改善。

针对“奖励模型”，先预测只改变数据、mask、参数、并行拓扑、反馈或评测协议时哪个中间对象最先分叉，再运行正常与单故障路径。保存来源、配置、阶段产物和判断依据，不用最终平均分覆盖前序失败。

### 6.2.1 数据收集

“数据收集”按来源、许可、快照哈希、清洗规则、去重簇、隐私动作、切分和保留率建立数据谱系。对照实验必须在同一模型和训练预算下只改变一个数据因素，并同时报告质量、多样性、污染和群体覆盖，不能只用最终损失替代数据证据。

针对“数据收集”，先预测只改变数据、mask、参数、并行拓扑、反馈或评测协议时哪个中间对象最先分叉，再运行正常与单故障路径。保存来源、配置、阶段产物和判断依据，不用最终平均分覆盖前序失败。

### 6.2.2 模型训练

“模型训练”先定义输入、输出、假设、指标与最小反例，再放回大模型构建流程。学习证据包含一次手算、一次冻结对照和一个单故障样本，保存中间产物与判断理由，使结论能被独立重放而不是依赖模型名或最终平均分。

针对“模型训练”，先预测只改变数据、mask、参数、并行拓扑、反馈或评测协议时哪个中间对象最先分叉，再运行正常与单故障路径。保存来源、配置、阶段产物和判断依据，不用最终平均分覆盖前序失败。

### 6.2.3 开源数据

“开源数据”按来源、许可、快照哈希、清洗规则、去重簇、隐私动作、切分和保留率建立数据谱系。对照实验必须在同一模型和训练预算下只改变一个数据因素，并同时报告质量、多样性、污染和群体覆盖，不能只用最终损失替代数据证据。

针对“开源数据”，先预测只改变数据、mask、参数、并行拓扑、反馈或评测协议时哪个中间对象最先分叉，再运行正常与单故障路径。保存来源、配置、阶段产物和判断依据，不用最终平均分覆盖前序失败。

### 6.3 近端策略优化

“近端策略优化”沿状态、动作、偏好、奖励、优势、策略比率和KL约束追踪。保存标注协议、奖励分布与逐样本轨迹，分别测试正常偏好、并列偏好和奖励方向翻转；奖励升高不等于帮助性、安全性与真实性同时改善。

针对“近端策略优化”，先预测只改变数据、mask、参数、并行拓扑、反馈或评测协议时哪个中间对象最先分叉，再运行正常与单故障路径。保存来源、配置、阶段产物和判断依据，不用最终平均分覆盖前序失败。

### 6.3.1 策略梯度

“策略梯度”沿状态、动作、偏好、奖励、优势、策略比率和KL约束追踪。保存标注协议、奖励分布与逐样本轨迹，分别测试正常偏好、并列偏好和奖励方向翻转；奖励升高不等于帮助性、安全性与真实性同时改善。

针对“策略梯度”，先预测只改变数据、mask、参数、并行拓扑、反馈或评测协议时哪个中间对象最先分叉，再运行正常与单故障路径。保存来源、配置、阶段产物和判断依据，不用最终平均分覆盖前序失败。

### 6.3.2 广义优势估计

“广义优势估计”沿状态、动作、偏好、奖励、优势、策略比率和KL约束追踪。保存标注协议、奖励分布与逐样本轨迹，分别测试正常偏好、并列偏好和奖励方向翻转；奖励升高不等于帮助性、安全性与真实性同时改善。

针对“广义优势估计”，先预测只改变数据、mask、参数、并行拓扑、反馈或评测协议时哪个中间对象最先分叉，再运行正常与单故障路径。保存来源、配置、阶段产物和判断依据，不用最终平均分覆盖前序失败。

### 6.3.3 近端策略优化算法

“近端策略优化算法”沿状态、动作、偏好、奖励、优势、策略比率和KL约束追踪。保存标注协议、奖励分布与逐样本轨迹，分别测试正常偏好、并列偏好和奖励方向翻转；奖励升高不等于帮助性、安全性与真实性同时改善。

针对“近端策略优化算法”，先预测只改变数据、mask、参数、并行拓扑、反馈或评测协议时哪个中间对象最先分叉，再运行正常与单故障路径。保存来源、配置、阶段产物和判断依据，不用最终平均分覆盖前序失败。

### 6.4 MOSS-RLHF实践

“MOSS-RLHF实践”先定义输入、输出、假设、指标与最小反例，再放回大模型构建流程。学习证据包含一次手算、一次冻结对照和一个单故障样本，保存中间产物与判断理由，使结论能被独立重放而不是依赖模型名或最终平均分。

针对“MOSS-RLHF实践”，先预测只改变数据、mask、参数、并行拓扑、反馈或评测协议时哪个中间对象最先分叉，再运行正常与单故障路径。保存来源、配置、阶段产物和判断依据，不用最终平均分覆盖前序失败。

### 6.4.1 奖励模型训练

“奖励模型训练”沿状态、动作、偏好、奖励、优势、策略比率和KL约束追踪。保存标注协议、奖励分布与逐样本轨迹，分别测试正常偏好、并列偏好和奖励方向翻转；奖励升高不等于帮助性、安全性与真实性同时改善。

针对“奖励模型训练”，先预测只改变数据、mask、参数、并行拓扑、反馈或评测协议时哪个中间对象最先分叉，再运行正常与单故障路径。保存来源、配置、阶段产物和判断依据，不用最终平均分覆盖前序失败。

### 6.4.2 PPO微调

“PPO微调”沿状态、动作、偏好、奖励、优势、策略比率和KL约束追踪。保存标注协议、奖励分布与逐样本轨迹，分别测试正常偏好、并列偏好和奖励方向翻转；奖励升高不等于帮助性、安全性与真实性同时改善。

针对“PPO微调”，先预测只改变数据、mask、参数、并行拓扑、反馈或评测协议时哪个中间对象最先分叉，再运行正常与单故障路径。保存来源、配置、阶段产物和判断依据，不用最终平均分覆盖前序失败。

### 6.5 实践思考

“实践思考”先定义输入、输出、假设、指标与最小反例，再放回大模型构建流程。学习证据包含一次手算、一次冻结对照和一个单故障样本，保存中间产物与判断理由，使结论能被独立重放而不是依赖模型名或最终平均分。

针对“实践思考”，先预测只改变数据、mask、参数、并行拓扑、反馈或评测协议时哪个中间对象最先分叉，再运行正常与单故障路径。保存来源、配置、阶段产物和判断依据，不用最终平均分覆盖前序失败。

## 六类构建证据

### 来源与版本合同

保存第一版官方PDF、出版信息、原始论文、代码提交、模型卡和数据卡。区分原书陈述、原始研究结果、本站复现和工程推断；无法取得训练语料或完整配置时写明不可完整复现，不用近似实验冒充原结果。

### 数据与词元合同

记录许可、来源、时间、哈希、语言、质量过滤、去重簇、隐私动作、切分和词表。派生样本按来源组切分，避免模板或近重复跨集合；任何删除都保留原因码和聚合统计，使模型差异能追到数据动作。

### 模型与目标合同

记录层数、隐藏维、头数、位置方法、归一化、激活、参数共享、mask、目标函数和冻结参数。模型名称相同不保证配置、权重、分词或上下文语义相同；小张量单测先于大规模训练。

### 训练与资源合同

保存有效词元、全局批量、学习率日程、优化器、数值格式、并行拓扑、吞吐、显存、通信和断点。比较在相同预算下进行或明确预算差异，恢复训练要验证数据游标、随机状态、优化器和调度器连续。

### 反馈、应用与安全合同

偏好标注记录协议、一致性和分歧；检索与代理记录证据片段、工具权限、状态和回退。提示注入、敏感信息、越权工具、奖励黑客、幻觉和过度拒答分别测试，聚合分数不能替代失败样本。

### 评测与发布合同

冻结任务、样本、提示、解码、判定和聚合规则，结果可下钻到输入、输出与理由。质量、安全、性能、成本和复现门禁全部通过才发布；发布单元包含模型、词元器、适配器、索引、提示、服务镜像和评测清单，并能整体回滚。

## 三步交互实验

<Stepper>
  <Step title="追踪构建流水线">
    拖动阶段，检查来源、数据、词元、模型、训练和评测的依赖；先预测当前阶段改变后哪个对象最先分叉。
    <Lsl06ReinforcementLearningPipelineLab />
  </Step>
  <Step title="改变训练条件">
    调节词元预算与证据阈值，观察受控曲线、资源变化和边界；一次只改变一个因素。
    <Lsl06ReinforcementLearningTrainingLab />
  </Step>
  <Step title="注入证据故障">
    注入泄漏、mask、恢复、反馈或评测故障，要求门禁在最终结论前捕获并给出回退点。
    <Lsl06ReinforcementLearningEvidenceLab />
  </Step>
</Stepper>

## 常见失败与回退

<Callout type="trap">
  **本页高风险失败：**
  把模型名、框架默认值或最终平均分当作完整证据，未保存数据、张量、预算和逐样本产物，导致收益无法归因也无法重放。
</Callout>

<Callout type="trap">
  **版本混入：** 第一版只有8章；若将第二版12章目录、454页或ISBN
  9787121500572并入，会让目录分母和章节语义失真。
</Callout>

<Callout type="tip">
  **回退顺序：** 来源版本 → 数据切分 → 词元与mask → 模型与目标 → 训练预算 →
  反馈或工具 → 评测切片 → 发布边界。首个差异未解释前不比较最终结论。
</Callout>

## 本章回顾

从强化学习与监督学习的差异进入RLHF流程，依次重建偏好数据、奖励模型、策略梯度、广义优势估计、PPO和MOSS-RLHF实践。 掌握标准不是记住术语或接口，而是能重建输入、假设、数学、数据、实现、对照、指标、限制与失败证据。通过标准为：能从成对偏好计算奖励损失与优势，解释PPO裁剪和KL约束，并检测奖励黑客、标注偏差与策略退化。

<Glossary>

<GlossaryItem term="RLHF">
  “RLHF”是第6章
  强化学习的第1个执行坐标；定义同时记录输入、输出、假设、指标、版本、失败与最小反例，并能从冻结产物独立复核。
</GlossaryItem>

<GlossaryItem term="奖励模型">
  “奖励模型”是第6章
  强化学习的第2个执行坐标；定义同时记录输入、输出、假设、指标、版本、失败与最小反例，并能从冻结产物独立复核。
</GlossaryItem>

<GlossaryItem term="策略梯度">
  “策略梯度”是第6章
  强化学习的第3个执行坐标；定义同时记录输入、输出、假设、指标、版本、失败与最小反例，并能从冻结产物独立复核。
</GlossaryItem>

<GlossaryItem term="广义优势估计">
  “广义优势估计”是第6章
  强化学习的第4个执行坐标；定义同时记录输入、输出、假设、指标、版本、失败与最小反例，并能从冻结产物独立复核。
</GlossaryItem>

<GlossaryItem term="PPO">
  “PPO”是第6章
  强化学习的第5个执行坐标；定义同时记录输入、输出、假设、指标、版本、失败与最小反例，并能从冻结产物独立复核。
</GlossaryItem>

<GlossaryItem term="奖励黑客">
  “奖励黑客”是第6章
  强化学习的第6个执行坐标；定义同时记录输入、输出、假设、指标、版本、失败与最小反例，并能从冻结产物独立复核。
</GlossaryItem>

</Glossary>

<Exercises>

1. 如何为第6章 强化学习建立最小可重放基线？

<Answer>
  固定第一版来源、数据哈希、切分、词元器、模型、预算、种子和评测，保存全部阶段输入输出，再运行正常、边界与单故障样本。
</Answer>

2. 如何证明“能从成对偏好计算奖励损失与优势，解释PPO裁剪和KL约束，并检测奖励黑客、标注偏差与策略退化。”不是一次偶然成功？

<Answer>
  在同一冻结集合上执行多种子或多顺序对照，并由独立进程从空目录重放；确定性阶段逐项一致，随机阶段满足预设区间和安全门禁。
</Answer>

3. 修复第6章 强化学习后怎样回归？

<Answer>
  先重放原失败，再运行相邻边界和正常基线，按来源、数据、张量、预算、阶段产物与逐样本评测定位首个差异，全部门禁通过后才发布。
</Answer>

</Exercises>

## 前后导航

- [第5章 有监督微调](/learn/large-scale-llm-practice/07-supervised-finetuning/lsl-05-supervised-finetuning)
- [第7章 大语言模型应用](/learn/large-scale-llm-practice/09-applications/lsl-07-llm-applications)

<Attribution
  adaptedFrom="张奇、桂韬、郑锐、黄萱菁《大规模语言模型：从理论到实践》，电子工业出版社/博文视点，2024年1月，320页，ISBN 9787121467059 · 基于第一版127个正式目录层级的独立教学重构"
  adaptedUrl="https://intro-llm.github.io/"
/>
