---
title: 你好，三角形
type: C
section: 入门
order: 2
description: 跑通从顶点数据到屏幕像素的最短链路——亲手把第一个三角形画出来，并能改出自己想要的形状与颜色。
demo: true
math: true
sourceUrl: https://learnopengl-cn.github.io/01%20Getting%20started/04%20Hello%20Triangle/
draft: true
---

{/* C 实战型样例章（HEL-22 起；HEL-30 集成 M4 真组件）：M3 内容系统端到端样例，九段式齐全，每知识点配一个 viz。主 Demo = editable ShaderDemo（改 GLSL/uniform 实时渲染）；§5 管线流程用 PipelineViz；§4 坐标变换预告用 MathViz；另有 CodeTabs / Figure / Callout / Stepper（仅 §10 光栅化词条内嵌）。内容为样例级，draft:false 由总监按 chapter-spec.md review 后定（硬规则 7）。 */}

{/* 1. 学习目标（spec §一）：C 型必含「能改出某效果」一条；末条为可当场自测的检验问题。 */}

<Objectives>

- 能用自己的话解释**顶点缓冲对象**与**顶点数组对象**各自负责什么
- 能看懂创建三角形的代码，并改出一个不同形状或不同颜色的三角形
- 能回答：一段顶点数据从内存到屏幕像素，中间经过了哪几个阶段？

</Objectives>

{/* 2. 直觉引入（spec §二）：3 段内、零术语（术语最早第 3 节）、回答「解决什么问题/没有它会怎样」、类比固定用工厂流水线。 */}

## 为什么画一个三角形这么费劲

把一个三角形画到屏幕上，听起来像「给三个点连线填色」那么简单。但显卡不是这么工作的——它更像一条**工厂流水线**：原料从一头进去，经过一道道工位加工，成品从另一头出来。

你要做的不是「画」，而是**备料 + 排产**：先把三个角的坐标当原料送进车间，再告诉流水线每道工位怎么处理这批原料，最后按下开工按钮。

没有这套流程会怎样？显卡收到一堆数字却不知道它们是坐标、该连成什么、用什么颜色填——于是什么都不会出现。这一章就是把这条流水线一道道走通。

{/* 3. 概念讲解（spec §三）：C 型压缩「是什么」让篇幅给第六节；一节一概念（顶点/属性 → VBO → VAO+属性指针）；术语首现 <Term def> 高亮且进章末 Glossary；每概念配图或引用 Demo；顺序 是什么 → 为什么 → 在管线中位置。新核心概念：顶点/属性、VBO、VAO、属性指针、输入装配、NDC（NDC 与视口在 §4 用 Term，全章 ≤ 适度）。 */}

## 第一个角色：顶点，与它携带的「属性」

流水线的原料是 <Term def="渲染里最小的数据单元：一个点，至少携带位置，还可附带颜色、法线、纹理坐标等。三角形由 3 个顶点定义。">顶点（Vertex）</Term>——一个数据点。三角形由三个顶点定义。但顶点不只是「一个坐标」：它身上可以挂多种信息，每一种叫一个 <Term def="顶点身上的一项数据，如位置、颜色、法线、纹理坐标。一个顶点可同时带多个属性，着色器按编号（location）读取它们。">顶点属性（attribute）</Term>。

本章最简单的情形：每个顶点只带一个属性——**位置**，用 3 个 `float`（x、y、z）表示。三个顶点连起来就是 9 个 `float`，在内存里挨着排成一条直线：

```text
顶点0          顶点1          顶点2
x0 y0 z0   |   x1 y1 z1   |   x2 y2 z2     ← 内存里就是连续的 9 个 float
```

显卡拿到的其实只是这 9 个裸数字，它并不天然知道「每 3 个是一个顶点的位置」——这一点稍后由 VAO 来交代。

为什么坐标只能写在某个范围里？因为顶点位置用的是 <Term def="Normalized Device Coordinates，标准化设备坐标：x/y/z 都落在 [-1,1] 的标准空间，正中间是 0，框外的会被裁掉。">标准化设备坐标（NDC）</Term>——一个 $x/y/z$ 都落在 $[-1, 1]$ 的标准空间，正中间是原点。不管屏幕多大，先用这套统一坐标写顶点，落在框外的部分会被裁掉。本章三个顶点 `(0, 0.5)`、`(-0.5, -0.5)`、`(0.5, -0.5)` 就是这套坐标里的一个三角形。

## 第二个角色：VBO，把原料一次性搬进显存

顶点数据现在还在内存（CPU 那边）。但绘制发生在显卡（GPU）那边，每帧都从内存现搬数据既慢又浪费带宽。于是先把它搬进 <Term def="Vertex Buffer Object，显存里一块专门存顶点数据的缓冲区；把顶点一次性搬进去，绘制时显卡就近取料，不必每帧从内存重传。">顶点缓冲对象（VBO）</Term>——显存里一块专门的「料仓」。

它解决的问题：把顶点数据**一次性**从内存搬进显存，之后每帧绘制时显卡就近取料。没有它，每帧都要把这堆数字重新上传一遍，慢且浪费带宽。VBO 处在流水线最前端，是顶点数据进显卡的第一站。

## 第三个角色：VAO 与属性指针，告诉显卡怎么读这堆字节

数据进了显存，可显卡看到的还是一串裸字节，它不知道「每个顶点占几个字节、位置从第几字节开始读、一次读几个 `float`」。补上这份说明的，是 <Term def="一个告诉显卡怎么从 VBO 里切出顶点的设置：第几号属性、每次读几个值、什么类型、相邻两个顶点间隔多少字节（步长）、从第几字节起读（偏移）。">属性指针</Term>，它通过 `glVertexAttribPointer` / `gl.vertexAttribPointer` 配置：

- **location**：这是第几号属性（位置属性用 0 号，着色器里 `layout(location = 0)` 对应）
- **size**：每次读几个值（位置是 3 个 `float`，所以填 3）
- **stride（步长）**：相邻两个顶点的起点间隔多少字节（每顶点 3 个 `float` = 12 字节）
- **offset（偏移）**：这个属性从顶点的第几字节开始读（位置在最前，填 0）

而这套「怎么读」的配置本身，得有个东西记下来——这就是 <Term def="Vertex Array Object，记录「顶点数据该怎么解读」的说明书对象：哪些属性开了、各自的步长/偏移/类型。绑回它，显卡就知道怎么读 VBO。">顶点数组对象（VAO）</Term>。VAO 不存顶点数据（数据在 VBO 里），它只存「解读方式」。绑定 VAO 之后再调 `vertexAttribPointer`，配置就被这张说明书捕获；之后绘制时只要绑回 VAO，显卡就知道怎么解读那堆数字。它处在流水线最前端的 <Term def="渲染管线第一道工位，把顶点数据按 VAO 说明书取出、组织好交给后续阶段。">输入装配</Term> 环节。

一句话记住三者关系：**VBO 是料（一串字节），属性指针是「怎么切这串字节」的规则，VAO 是把这套规则记下来的说明书。** 下面这张图把这条链路和「属性指针怎么切字节」画在一起：

<VertexPipelineDiagram />

{/* 4. 数学推导（spec §四，math:true）：C 型数学为「选」；此处放一个轻量公式 NDC 到屏幕坐标的视口变换；公式后跟一句人话翻译，不跳步。 */}

## 一点点坐标数学

上一节说过，顶点坐标用的是 **NDC**，范围 $[-1, 1]$。屏幕像素却是从左上角 $(0,0)$ 到右下角 $(w, h)$。流水线靠 <Term def="把 [-1,1] 的 NDC 坐标按视口大小映射成屏幕像素坐标的那一步变换。">视口变换</Term> 把前者映射到后者：

$$
x_{\text{screen}} = \frac{x_{\text{ndc}} + 1}{2} \cdot w,
\qquad
y_{\text{screen}} = \frac{1 - y_{\text{ndc}}}{2} \cdot h
$$

这个式子在说：把 $[-1, 1]$ 先平移、缩放到 $[0, 1]$，再乘以屏幕宽/高。$y$ 用 $1 - y_{\text{ndc}}$ 是因为屏幕的 $y$ 轴朝下，而 NDC 的 $y$ 轴朝上，需要翻转。

这一步由显卡按你设的视口自动完成，代码里通常只调一次 `glViewport` / `gl.viewport`，无需手算——理解它能帮你想明白「为什么图形上下没颠倒」。

本章只用到上面这个轻量映射。但顶点在进 NDC 之前，往往还要被一连串**矩阵变换**「掰」到位（平移、旋转、缩放、投影）——那是后续「变换 / 坐标系统」章的主角。这里先给一个坐标变换预告：下面这个可视化里，拖动输入向量、调 2×2 矩阵的四个数，看变换后的网格和基向量 î / ĵ 怎么扭曲——先建立「矩阵 = 把整个空间掰一下」的手感。

<MathViz
  mode="basis"
  initialMatrix={{ a: 1, b: 0.5, c: 0, d: 1 }}
  caption="坐标变换预告（非本章必学）：拖动紫色输入向量 v，或调下方 a/b/c/d 四个数改变矩阵 M。绿色是 M·v，红/黄是变换后的基向量 î/ĵ——矩阵把单位网格「掰」成什么样，向量就被带到哪。预设里可一键试旋转/缩放/切变。"
/>

{/* 5. 交互 Demo（spec §五）：主 Demo C 型为 ShaderDemo（M4 真组件，editable）；Demo 前必有猜一猜引导；管线流程改用专业的 PipelineViz 辅 Demo——同一份三角形数据在五阶段里逐步演变形态（点→三角形→像素格→上色→帧缓冲），比通用 Stepper 换图换字更直观。 */}

## 流水线的五道工位

渲染管线把顶点变成像素，中间这五道工位是固定流程。其中第三道 <Term def="把矢量图元（如三角形）切成一个个屏幕像素格子的过程，决定它盖住了哪些像素。">光栅化</Term> 会把矢量三角形切成一个个屏幕像素——每个被覆盖的像素产生一个 <Term def="光栅化后产生的、对应一个像素的待着色数据点，片段着色器会逐个处理它。">片段</Term>，交给后面的工位上色。

下面这个分步可视化把同一份三角形数据走一遍：从三个孤立顶点，到连成三角形、铺到像素网格、逐片段上色、最后写入帧缓冲。可暂停、可单步、可拖进度：

<PipelineViz interval={2000} />

走通流程后，下面是本章主 Demo——一个**真·三角形**：顶点着色器吐出本章那三个 NDC 顶点 `(0, 0.5)`、`(-0.5, -0.5)`、`(0.5, -0.5)`，片段着色器只给三角形覆盖到的片段上色，背景留空（透明）。先别看代码——

> 猜一猜：右侧片段着色器现在输出的是「随位置渐变 + 基色」。如果把 `FragColor` 那行整段改成固定红色 `vec4(1.0, 0.0, 0.0, 1.0)`，画面会变成什么样？三角形的形状会变吗？动手改一下再看答案。

<ShaderDemo
  editable
  caption="一个真三角形：顶点着色器用 gl_VertexID 吐出本章那三个 NDC 顶点，片段着色器只给被三角形覆盖的片段算色。拖「脉动速度」、选「基色」、移动鼠标看实时变化；右侧可直接改 GLSL，停手后自动重渲染——改 FragColor 改颜色，改顶点着色器里的坐标改形状。"
  vert={`#version 300 es
// 本章三角形的三个 NDC 顶点，按 gl_VertexID 选出（无需 VBO，引擎画 3 个顶点）。
const vec2 verts[3] = vec2[3](
  vec2( 0.0,  0.5),
  vec2(-0.5, -0.5),
  vec2( 0.5, -0.5)
);
void main() {
  gl_Position = vec4(verts[gl_VertexID], 0.0, 1.0);
}`}
  frag={`#version 300 es
precision highp float;
uniform vec2 uResolution;
uniform vec2 uMouse;
uniform float uTime;
uniform vec3 uColorA;   // 控件：基色（颜色选择器，rgb 0..1）
uniform float uSpeed;   // 控件：脉动速度（滑块）
out vec4 FragColor;
void main() {
  // 把片段的屏幕坐标归一化到 0..1（uv 即「这个片段在画布的哪个位置」）
  vec2 uv = gl_FragCoord.xy / uResolution;
  // 蓝随时间脉动（速度可调）、随鼠标偏移
  float b = 0.5 + 0.5 * sin(uTime * uSpeed + uMouse.x * 6.2831);
  // 位置渐变叠加作者选的基色
  vec3 col = mix(uColorA, vec3(uv.x, uv.y, b), 0.6);
  FragColor = vec4(col, 1.0);
}`}
  controls={[
    { name: "uColorA", label: "基色", type: "color", default: [0.49, 0.36, 1] },
    {
      name: "uSpeed",
      label: "脉动速度",
      type: "float",
      min: 0,
      max: 4,
      step: 0.1,
      default: 1,
    },
  ]}
/>

{/* 6. 代码对照（spec §六，C 型重心，覆盖 ≥80% 关键代码）：五步覆盖 着色器源码 → 编译链接 → VBO → VAO+属性指针 → 绘制；单块 ≤30 行；两 Tab 同一逻辑段；代码块之间有解说；API 差异首现用 Callout 标注。 */}

## 代码逐段拆解

画一个三角形的完整代码就五步：**写两段着色器 → 编译链接成程序 → 把顶点搬进 VBO → 用 VAO 记录怎么读 → 下达绘制命令**。下面逐步对照，C++/OpenGL 与 WebGL2/TS 互为镜像。

### 第一步：写两段着色器源码

显卡画图靠两段小程序：<Term def="渲染管线里第一段可编程小程序，对每个顶点跑一次，主要把顶点位置摆到 NDC 里（输出 gl_Position）。">顶点着色器</Term> 处理每个顶点的位置，<Term def="光栅化后对每个片段跑一次的小程序，算出这个片段最终是什么颜色（输出到 FragColor）。">片段着色器</Term> 算出每个片段的颜色。本章最简版：顶点着色器把传进来的位置原样输出，片段着色器输出固定的红色。

<CodeTabs>
  <Tab label="C++ / OpenGL 3.3">

```cpp
const char *vertexSrc = R"(#version 330 core
layout (location = 0) in vec3 aPos;
void main() {
  gl_Position = vec4(aPos, 1.0);
})";
const char *fragmentSrc = R"(#version 330 core
out vec4 FragColor;
void main() {
  FragColor = vec4(1.0, 0.0, 0.0, 1.0); // 红
})";
```

  </Tab>
  <Tab label="TypeScript / WebGL2">

```ts
const vertexSrc = `#version 300 es
layout (location = 0) in vec3 aPos;
void main() {
  gl_Position = vec4(aPos, 1.0);
}`;
const fragmentSrc = `#version 300 es
precision highp float;
out vec4 FragColor;
void main() {
  FragColor = vec4(1.0, 0.0, 0.0, 1.0); // 红
}`;
```

  </Tab>
</CodeTabs>

两段逻辑完全相同，只有版本声明这一行不同——这是第一个要记住的差异点：

<Callout type="warn">
  API 差异：着色器版本声明不同——桌面 OpenGL 写 `#version 330 core`，WebGL2 写
  `#version 300 es`，且 WebGL2 的**片段着色器必须**多写一行 `precision highp
  float;` 声明浮点精度，否则编译失败。`layout (location = 0)` 把顶点着色器的
  `aPos` 绑到 0 号属性——和后面属性指针填的 location 0 对上。
</Callout>

### 第二步：编译、链接成一个着色器程序

源码只是字符串，要先编译成显卡能跑的东西，再把两段链接成一个 <Term def="把编译好的顶点着色器和片段着色器链接到一起、可被显卡执行的整体；绘制前用 useProgram 启用它。">着色器程序</Term>。编译/链接失败时一定要查日志，否则后面只会得到一片黑。

<CodeTabs>
  <Tab label="C++ / OpenGL 3.3">

```cpp
unsigned int vs = glCreateShader(GL_VERTEX_SHADER);
glShaderSource(vs, 1, &vertexSrc, NULL);
glCompileShader(vs);
unsigned int fs = glCreateShader(GL_FRAGMENT_SHADER);
glShaderSource(fs, 1, &fragmentSrc, NULL);
glCompileShader(fs);
unsigned int program = glCreateProgram();
glAttachShader(program, vs);
glAttachShader(program, fs);
glLinkProgram(program); // 失败用 glGetProgramInfoLog 查日志
```

  </Tab>
  <Tab label="TypeScript / WebGL2">

```ts
function compile(type: number, src: string) {
  const s = gl.createShader(type)!;
  gl.shaderSource(s, src);
  gl.compileShader(s);
  if (!gl.getShaderParameter(s, gl.COMPILE_STATUS))
    throw new Error(gl.getShaderInfoLog(s) ?? "编译失败");
  return s;
}
const program = gl.createProgram()!;
gl.attachShader(program, compile(gl.VERTEX_SHADER, vertexSrc));
gl.attachShader(program, compile(gl.FRAGMENT_SHADER, fragmentSrc));
gl.linkProgram(program); // 失败用 gl.getProgramInfoLog 查日志
```

  </Tab>
</CodeTabs>

`createShader → shaderSource → compileShader` 编译每一段，`createProgram → attachShader → linkProgram` 把两段焊成一个程序。绘制前再用 `glUseProgram(program)` / `gl.useProgram(program)` 把它启用。接着才轮到准备数据。

### 第三步：把顶点数据搬进 VBO

三角形的三个顶点先写成一个扁平数组，再创建一块缓冲、绑定、上传到显存：

<CodeTabs>
  <Tab label="C++ / OpenGL 3.3">

```cpp
float vertices[] = {
  -0.5f, -0.5f, 0.0f,
   0.5f, -0.5f, 0.0f,
   0.0f,  0.5f, 0.0f,
};
unsigned int VBO;
glGenBuffers(1, &VBO);
glBindBuffer(GL_ARRAY_BUFFER, VBO);
glBufferData(GL_ARRAY_BUFFER, sizeof(vertices), vertices, GL_STATIC_DRAW);
```

  </Tab>
  <Tab label="TypeScript / WebGL2">

```ts
const vertices = new Float32Array([
  -0.5, -0.5, 0.0, 0.5, -0.5, 0.0, 0.0, 0.5, 0.0,
]);
const vbo = gl.createBuffer();
gl.bindBuffer(gl.ARRAY_BUFFER, vbo);
gl.bufferData(gl.ARRAY_BUFFER, vertices, gl.STATIC_DRAW);
```

  </Tab>
</CodeTabs>

两边逻辑完全对应，只是申请缓冲的 API 命名风格不同：

<Callout type="warn">
  API 差异：C++ 用 `glGenBuffers(1, &VBO)` 先生成
  id（一个整数句柄）再绑定；WebGL2 用 `gl.createBuffer()` 直接返回一个
  `WebGLBuffer` 对象。创建 VAO 也是同理： `glGenVertexArrays` vs
  `gl.createVertexArray()`。
</Callout>

### 第四步：用 VAO 记录属性布局（属性指针）

数据进了显存，还要告诉显卡「每个顶点 3 个 `float`、从第 0 字节开始」。绑定 VAO 后调用 `vertexAttribPointer`，这段配置就被这张「说明书」记下来：

<CodeTabs>
  <Tab label="C++ / OpenGL 3.3">

```cpp
unsigned int VAO;
glGenVertexArrays(1, &VAO);
glBindVertexArray(VAO);
glVertexAttribPointer(
  0, 3, GL_FLOAT, GL_FALSE, 3 * sizeof(float), (void*)0);
glEnableVertexAttribArray(0);
```

  </Tab>
  <Tab label="TypeScript / WebGL2">

```ts
const vao = gl.createVertexArray();
gl.bindVertexArray(vao);
gl.vertexAttribPointer(0, 3, gl.FLOAT, false, 3 * 4, 0);
gl.enableVertexAttribArray(0);
```

  </Tab>
</CodeTabs>

对照 §3 那把尺子看这几个参数：第一个 `0` 是 location（对上着色器里 `layout(location = 0)`）；`3` 是每次读 3 个值；`3 * sizeof(float)` / `3 * 4` 是步长（每顶点 12 字节）；最后的 `0` 是偏移（位置从第 0 字节起读）。

<Callout type="tip">
  我们直接用 `layout(location = 0)` 写死了属性编号，所以填 0
  即可。如果着色器没写 `layout`，就得先用 `glGetAttribLocation(program, "aPos")`
  / `gl.getAttribLocation(program, "aPos")` 按变量名问出编号，再把它填进
  `vertexAttribPointer` 的第一个参数。
</Callout>

### 第五步：下达绘制命令

万事俱备——启用程序、绑回 VAO、发一条 <Term def="按 VAO 说明书从 VBO 取顶点、走完整条管线画出图元的绘制命令；glDrawArrays 从第 0 个顶点起画 3 个顶点、组成一个三角形。">绘制调用</Term>，三角形就出现了：

<CodeTabs>
  <Tab label="C++ / OpenGL 3.3">

```cpp
glUseProgram(program);     // 启用着色器程序
glBindVertexArray(VAO);    // 绑回说明书
glDrawArrays(GL_TRIANGLES, 0, 3); // 从第 0 个顶点起画 3 个 → 一个三角形
```

  </Tab>
  <Tab label="TypeScript / WebGL2">

```ts
gl.useProgram(program); // 启用着色器程序
gl.bindVertexArray(vao); // 绑回说明书
gl.drawArrays(gl.TRIANGLES, 0, 3); // 从第 0 个顶点起画 3 个 → 一个三角形
```

  </Tab>
</CodeTabs>

`drawArrays(TRIANGLES, 0, 3)` 的意思是「从第 0 个顶点开始，取 3 个顶点，每 3 个组成一个三角形」。这一条命令一发，§5 那条「点 → 三角形 → 像素 → 上色」的流水线就被完整跑了一遍。完整可运行源码将在章末提供（M5）。

{/* 7. 常见误区（spec §七）：C 型至少 3 个；固定格式 现象 到 原因 到 修法；每个 150 字内。 */}

## 容易踩的坑

<Callout type="trap">
  **现象**：编译运行后屏幕全黑，什么都没有。**原因**：忘了绑定 VAO 就调
  `vertexAttribPointer`，属性配置没被任何对象记录。**修法**：先
  `glBindVertexArray(VAO)`，再配置属性，最后绘制时绑回同一个 VAO。
</Callout>

<Callout type="trap">
  **现象**：三角形顶点位置乱跳或只画出一部分。**原因**：`vertexAttribPointer`
  的步长（stride）或偏移算错，显卡按错误字节数读取数据。**修法**：步长 =
  单个顶点 字节数（3 个 float = 12 字节），位置属性偏移 = 0，逐项核对。
</Callout>

<Callout type="trap">
  **现象**：WebGL2 下报错 `No precision specified`
  或片段不显示。**原因**：WebGL2 的片段着色器必须显式声明浮点精度。**修法**：在
  `#version 300 es` 之后加一行 `precision highp float;`（或
  `mediump`）。**亲手看翻车**：到 §5 那个可编辑 Demo 里，把 `precision highp
  float;`
  那行删掉，停手后编辑器会标红报错、画面停更——这就是这个坑的真实样子；改回来即恢复。
</Callout>

<Callout type="trap">
  **现象**：编译报错或三角形不出现，控制台只有一句模糊的失败。**原因**：着色器编译/链接失败了却没查日志，比如拼错了内置变量名（`gl_Position`
  写成 `gl_position`）。**修法**：编译后用 `getShaderInfoLog`、链接后用
  `getProgramInfoLog` 把日志打出来——错误行和原因都在里面，别盲猜。
</Callout>

{/* 8. 小结与练习（spec §八）：小结 5 条内与学习目标一一呼应；练习含一道改 Demo 代码题，答案折叠；C 型加一道独立实现题。 */}

## 小结

- 显卡像工厂流水线：你负责备料（顶点）与排产（配置），不直接「画」
- **VBO** 是显存料仓，**VAO** 是料仓说明书，两者配合才能正确取数
- 渲染管线五道工位：顶点 → 图元装配 → 光栅化 → 片段 → 测试混合
- 改**片段着色器**就能改出不同颜色/渐变；改顶点数据就能改形状
- 顶点坐标用 NDC（$[-1,1]$），由视口变换映射到屏幕像素

<Exercises>

**练习 1（改 Demo 代码型）** 在上面可编辑 Demo 里做两件事：①把片段着色器 `FragColor` 那行改成固定红色 `vec4(1.0, 0.0, 0.0, 1.0)`，看三角形变成纯红；②再把顶点着色器里 `vec2( 0.0, 0.5)` 这个顶点改成 `vec2( 0.0, 0.9)`，看形状怎么变。解释为什么改 frag 只换颜色、改 vert 才换形状。

<Answer>

①把片段着色器输出改为固定色：

```glsl
FragColor = vec4(1.0, 0.0, 0.0, 1.0); // 整块三角形变纯红
```

片段着色器只决定「每个被覆盖的片段是什么颜色」，不决定覆盖了哪些片段——所以改它只换颜色，三角形形状纹丝不动。

②把顶点着色器里的顶点 `vec2(0.0, 0.5)` 抬高到 `vec2(0.0, 0.9)`，顶点位置变了，三角形就被拉高变尖。形状由**顶点**决定，所以要改形状得改顶点着色器（或顶点数据）。这正是「改 frag 改颜色、改顶点改形状」的由来。

</Answer>

**练习 2（独立实现题）** 不看上面的代码，自己从零写出「创建 VBO + VAO + 配置位置属性」三步，画出一个**矩形**（目标效果：屏幕中央一个正方形）。

<Answer>

矩形用两个三角形拼成。提供 4 个顶点 + 一个索引缓冲（EBO / `ELEMENT_ARRAY_BUFFER`），用 `glDrawElements` 按索引绘制，避免重复顶点：

```ts
const vertices = new Float32Array([
  -0.5, 0.5, 0, 0.5, 0.5, 0, 0.5, -0.5, 0, -0.5, -0.5, 0,
]);
const indices = new Uint16Array([0, 1, 2, 0, 2, 3]);
// VBO + VAO 同正文；额外：把 indices 传入 ELEMENT_ARRAY_BUFFER，
// 绘制改用 gl.drawElements(gl.TRIANGLES, 6, gl.UNSIGNED_SHORT, 0)
```

</Answer>

**练习 3（问答型）** 为什么要先绑定 VAO 再配置顶点属性，而不是反过来？

<Answer>

`vertexAttribPointer` / `enableVertexAttribArray` 的状态会被「当前绑定的 VAO」记录。如果还没绑 VAO 就配置，这些状态无处归属（WebGL2 下甚至直接报错）。先绑 VAO，配置才会被它捕获，之后绘制只需绑回这张说明书。

</Answer>

</Exercises>

{/* 10. 名词解释（spec §十）：与正文 <Term> 高亮一一对应（14 个，按首现顺序）；释义面向零基础；难懂词「光栅化」内嵌 Stepper 动画掰碎。位置固定章末、出处声明之前。 */}

<Glossary>

<GlossaryItem term="顶点（Vertex）">
  渲染里最小的数据单元——一个点。它至少带「位置」，还可以再挂颜色、法线、纹理坐标等。三角形就是由
  3 个顶点定义的。详见本章「第一个角色」。
</GlossaryItem>

<GlossaryItem term="顶点属性（attribute）">
  挂在一个顶点身上的某一项数据，比如位置、颜色。一个顶点可以同时带好几个属性，着色器按编号（location）去读它们。本章每个顶点只带一个属性：位置。
</GlossaryItem>

<GlossaryItem term="顶点缓冲对象（VBO）">
  显存里一块专门存顶点数据的「料仓」。先把三角形三个角的坐标一次性搬进去，显卡画的时候就近取，不用每帧从内存重新搬——快、省。详见本章「第二个角色」。
</GlossaryItem>

<GlossaryItem term="属性指针">
  一份「怎么从 VBO
  这串字节里切出一个个顶点」的说明：第几号属性、每次读几个值、相邻顶点隔多少字节（步长）、从第几字节起读（偏移）。靠
  `vertexAttribPointer` 配置。详见本章「第三个角色」。
</GlossaryItem>

<GlossaryItem term="顶点数组对象（VAO）">
  料仓的「说明书」。它不存数据，只记「这堆数字里每个顶点占几个字节、坐标从第几字节开始读」（也就是上面的属性指针）。绑回这张说明书，显卡才知道怎么解读那堆数字。详见本章「第三个角色」。
</GlossaryItem>

<GlossaryItem term="输入装配">
  渲染管线的第一道工位：按 VAO
  说明书把顶点数据取出、组织好，再交给后面的工位。可以理解成流水线的「上料口」。
</GlossaryItem>

<GlossaryItem term="标准化设备坐标（NDC）">
  一套「标准坐标尺」：不管屏幕多大，坐标都用 [-1, 1]
  这个固定范围来表示，正中间是
  0。落在这个范围外的东西会被裁掉。这样写顶点坐标时就不用关心屏幕到底多少像素。
</GlossaryItem>

<GlossaryItem term="视口变换">
  把上面那套 [-1, 1]
  的标准坐标，按屏幕实际宽高换算成「第几个像素」的那一步。比如把
  x=0（正中）算成屏幕宽度的一半处。显卡按你设的视口自动做，无需手算。
</GlossaryItem>

<GlossaryItem term="光栅化">

把一个矢量图形（如三角形）切成一个个屏幕像素格子的过程。三角形本是「三个角 + 三条边」的数学描述，屏幕却只能点亮一个个方块像素——光栅化就是决定「哪些方块在三角形里、该被点亮」。下面这步分解可单步、可拖进度：

<Stepper>
  <Step title="① 拿到三角形">
    输入是三个顶点连成的矢量三角形，此刻它还没有任何「像素」概念，只是三个坐标。
  </Step>
  <Step title="② 铺上像素网格">
    屏幕是一张方格纸（每格 = 一个像素）。把三角形盖在这张网格上。
  </Step>
  <Step title="③ 判定覆盖">
    逐格判断「这个格子的中心点落在三角形内部吗？」落在内部的格子被选中。
  </Step>
  <Step title="④ 生成片段">
    每个被选中的格子产出一个「片段」，交给片段着色器去决定它最终是什么颜色。
  </Step>
</Stepper>

</GlossaryItem>

<GlossaryItem term="片段">
  光栅化后产生的、对应一个像素的「待上色数据点」。它还不是最终像素——要等片段着色器算出颜色、再通过深度/混合测试后，才真正写进屏幕。可以理解成「准像素」。
</GlossaryItem>

<GlossaryItem term="顶点着色器">
  管线里第一段你能写的小程序，对每个顶点跑一次，主要任务是把顶点位置摆到正确的地方（输出
  `gl_Position`）。本章里它只是把传进来的坐标原样输出。详见本章「第一步」。
</GlossaryItem>

<GlossaryItem term="片段着色器">
  光栅化后对每个片段跑一次的小程序，决定这个片段最终是什么颜色（输出
  `FragColor`）。改它就能改出不同颜色或渐变——这正是本章主 Demo 在玩的东西。
</GlossaryItem>

<GlossaryItem term="着色器程序">
  把编译好的顶点着色器和片段着色器链接到一起、能被显卡执行的整体。绘制前用
  `useProgram` 启用它，显卡画图时才知道该跑哪两段着色器。详见本章「第二步」。
</GlossaryItem>

<GlossaryItem term="绘制调用">
  按 VAO 说明书从 VBO 取顶点、走完整条管线画出图形的那条命令。本章用
  `drawArrays(TRIANGLES, 0, 3)`：从第 0 个顶点起取 3
  个、组成一个三角形。详见本章「第五步」。
</GlossaryItem>

</Glossary>

{/* 9. 出处声明（spec §九）：Attribution 由 page.tsx 闭包注入本章 frontmatter.sourceUrl，写空标签即可（硬规则 4）。 */}

<Attribution />
