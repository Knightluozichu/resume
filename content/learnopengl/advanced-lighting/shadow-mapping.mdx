---
title: 阴影映射
type: C
section: 高级光照
order: 3
description: 能讲清阴影映射的两遍流程——先从光源视角渲一张深度图记下每个方向最近的遮挡距离，再回相机比深度判断每个片元是否被挡；能在 demo 里拖 depth bias 亲手造出又修掉 peter panning 阴影脱离，并能解释 shadow acne 自遮挡条纹的成因和 bias 修法，还能说出阴影锯齿和阴影图分辨率、PCF 软阴影之间的关系。
demo: true
math: false
sourceUrl: https://learnopengl-cn.github.io/05%20Advanced%20Lighting/03%20Shadows/01%20Shadow%20Mapping/
draft: false
---

{/* C 实战型章（HEL-82，高级光照篇第 3 篇）：原书「阴影映射 Shadow Mapping」。重心是代码对照(spec C 列 §6 重，覆盖两遍法关键代码)：①第一遍——光空间矩阵 lightProjection×lightView + 渲深度到深度纹理 FBO；②第二遍片段着色器——FragPos 变光空间 / 透视除法 / 转 [0,1] / 采样 shadow map / 深度比较 + bias + PCF 3×3。承接同篇 blinn-phong / gamma-correction（已学世界空间光照、点乘、片段着色器），沿用「光与材质 / 相机与摄影」类比库（光源也是一台「相机」）。C 型段式：概念讲解(§3 必，是什么压一段篇幅让给 §6) + 数学 math:false 整节省略(§4，光空间矩阵在 §6 代码对照里讲) + 交互 Demo(§5 必，主 R3F 内建 shadow map 调参 + Stepper 两遍法每步带图) + 代码对照(§6 重) + 误区 ≥3(§7) + 小结含改 Demo 题 + C 型独立实现题(§8)。主 viz = ShadowMappingDemo（three.js 内建 shadow map，控件：光源角度/阴影图分辨率/depth bias/PCF 开关 + 重置，亲手拖出 peter panning/锯齿/软硬——acne 因 three 内建偏移太稳盲调演示不出，归图解/误区）；图解 = ShadowMapStepDiagram（Stepper 两遍法四步配图）+ ShadowAcneDiagram（acne 成因 + bias 修法）。draft:true，老板验收后才发布（硬规则 7）。 */}

{/* 知识点清单（动笔前先摊开，每条标 viz；难啃的用图掰碎；全部配 viz，无纯文字一笔带过；Term 首现高亮 + 章末 Glossary 一一对应）：1) 阴影成因（光被遮挡处变暗）= 直觉引入 + 概念；2) shadow map 核心思想（先从光源视角把场景渲成一张深度图，记下每个方向最近遮挡距离）= 概念 + <ShadowMapStepDiagram step=1/2> + 主 ShadowMappingDemo；3) 光空间变换（用光的 view×proj 把片元变到光裁剪空间、取 [0,1] 深度）= 概念 + §6 代码对照 + ShadowMapStepDiagram step=3；4) 深度比较判阴影（current > 深度图存的 closest ⇒ 被挡 ⇒ 在阴影）= <ShadowMapStepDiagram step=4> + §6 代码对照；5) shadow acne 自遮挡条纹 + depth bias 偏移修法 = 误区 + <ShadowAcneDiagram mode=acne/bias>（图解讲透；three 内建偏移太稳，demo 盲调演示不出 acne）；6) PCF 百分比邻近滤波（采样周围多点取平均、软化锯齿边）= 概念 + demo PCF 开关 + §6 代码对照 PCF 3×3；7) peter panning（bias 过大致阴影脱离悬浮）= 误区 + demo bias 拖大；8) 阴影图分辨率 / 光锥（分辨率低→阶梯锯齿；超出光锥范围处理）= demo 分辨率切换 + 误区。Term 共 6 个（阴影映射 / shadow map / 光空间 / 深度比较 / shadow acne / PCF），控制在 ≤6 核心、不超 spec §3「全章新概念 ≤5」太多。 */}

{/* 1. 学习目标（spec §一）：C 型必含「能改出 ___ 效果」一条（能在 demo 拖 bias 调出/修掉 peter panning——three 内建偏移太稳，acne 演示不出，归图解/误区）；2-3 条可检验动词；末条为可当场自测的检验问题。 */}

<Objectives>

- 能解释**阴影映射的两遍流程**：第一遍从光源视角渲一张**深度图**记下每个方向最近的遮挡距离，第二遍从相机渲场景、把每个片元变到**光空间**比深度——说得清「比什么、比出什么算在阴影里」
- 能在下面的 demo 里把 **depth bias** 拖到**最大**造出 **peter panning**（影子与物体脚下脱节）再拖回修掉；并能解释 **shadow acne** 自遮挡条纹的成因和 depth bias 的修法（见图解）；还能说出阴影边缘**锯齿**和**阴影图分辨率 / PCF** 的关系
- 能回答：一个片元在光空间算出的深度，比深度图里存的「最近遮挡距离」**大**，这说明它被挡住了还是没被挡住？所以它在不在阴影里？

</Objectives>

{/* 2. 直觉引入（spec §二）：≤3 段、每段 ≤120 字、零术语（专业名词最早第 3 节）、回答「解决什么问题 / 没有它会怎样」、类比固定用「光与材质 / 相机与摄影」库（光源 = 一台相机）。 */}

## 没有影子，东西像飘在空中

前两篇你把高光调平滑、把颜色校正对了。可只要场景里没有影子，再精致的光照也透着一股假：一个球放在地面上，没有影子托着，它看起来不像「放」在地上，倒像悬浮在半空，分不清离地多高、到底贴没贴着。影子是我们判断「东西在哪、离得多远」的本能线索，少了它，整个画面就「飘」。

为什么会少？因为前面算光照时，每块表面只顾自己「正不正对着光」，从不管**光在到达它之前有没有被别的东西挡住**。可现实里光是走直线的：一堵墙挡在前面，墙后那片地就晒不到太阳——那片晒不到的地方，就是影子。

这一章要解决的就是这件事：**怎么让程序知道「这块地被前面的墙挡住了、照不到光」，从而把它画暗**。窍门是借一台「相机」站到光源的位置去看一眼——凡是它看不见的角落，就是被挡住的影子区。没有它，你的物体永远飘在空中；有了它，东西终于稳稳落了地。

{/* 3. 概念讲解（spec §三，C 型「是什么」压缩一段、篇幅让给 §6）：一节一概念；术语首现 <Term def> 高亮且进章末 Glossary；每概念配图或引用 Demo；顺序 是什么 → 为什么 → 在渲染流程位置。新核心概念 ≤5~6。 */}

## 核心思想：让光源也当一回「相机」

判断「一块表面有没有被挡住」，最聪明的办法是**换到光的视角去看**。想象把一台相机搬到光源的位置、朝场景拍一张——这台「光相机」能看见的表面，就是直接被照到的；被它挡在后面、拍不到的，就在影子里。这就是 <Term def="一种实时阴影技术：先从光源视角把场景渲染一遍，记下「从光看过去，每个方向上最近的遮挡物有多远」存成一张深度图；再从相机正常渲染时，把每个片元变换到光的视角下，比较它的深度和深度图里存的最近遮挡距离——如果它比最近遮挡还远，说明前面有东西先挡住了光，它就在阴影里。是绝大多数实时引擎做阴影的基础办法。">阴影映射</Term>（shadow mapping）的全部直觉：**凡是光相机看不见的地方，就是影子**。

可「看得见/看不见」怎么用数字表示？办法是只记**距离**。从光源视角渲染时，我们不记颜色，只记「沿这个方向望过去，**最近**的那个遮挡物离光多远」，把每个方向的这个最近距离存成一整张图——这张图就叫 <Term def="从光源视角渲染场景得到的一张深度纹理：它的每个像素（texel）记录的不是颜色，而是「沿光的这个方向望过去，离光源最近的遮挡物的深度（距离）」。第二遍渲染时拿它来对比，判断相机看到的每个片元是否被更近的东西挡住。也叫深度图。">shadow map</Term>（深度图）。它的每个像素不存颜色、只存一个距离值：这个方向上，最近的遮挡有多远。

整套阴影映射就是**两遍渲染**：第一遍从光源视角渲出这张深度图，第二遍从相机正常渲场景、顺便拿深度图判每个片元在不在影子里。下面这个分步图把两遍法掰开，你跟着走一遍就懂了（下面交互 Demo 还能亲手玩）：

{/* 5(图解). 交互 Demo（spec §五）：C 型 Stepper 两遍法——每步带 <ShadowMapStepDiagram step>（满足「每步必带图示」，纯文字步=打回）。①第一遍渲深度图→②存下→③第二遍算光空间深度→④比深度判阴影。 */}

<Stepper>
  <Step title="① 第一遍：从光源视角，把每方向最近遮挡距离渲进深度图">
    <ShadowMapStepDiagram step={1} />
    站到光源的位置朝场景看，沿光的每条方向射线，记下「这条方向上离光
    <strong>最近</strong>的遮挡物有多远」，写进一张深度图（shadow map）。这一遍
    <strong>不画颜色</strong>，只存距离：越近的遮挡存的深度越小。
  </Step>
  <Step title="② 存下这张深度图（shadow map）">
    <ShadowMapStepDiagram step={2} />
    第一遍结束，深度图存好备用。它记的是「从光看过去，
    <strong>每个方向最近的遮挡距离</strong>
    」——后面第二遍要靠它，对每个片元回答一句「你这个方向上，是不是被更近的东西先挡住了？」
  </Step>
  <Step title="③ 第二遍：从相机渲，把每个片元变到光空间、取它的当前深度">
    <ShadowMapStepDiagram step={3} />
    改从相机视角正常渲场景。对画面上每一个片元，用光源的
    <strong>观察矩阵 × 投影矩阵</strong>
    把它的世界坐标变换到光的视角下，取出它在光眼里的深度——叫它
    <strong>当前深度</strong>（current depth）。
  </Step>
  <Step title="④ 比深度判阴影：current > 深度图存的最近深度 ⇒ 在阴影">
    <ShadowMapStepDiagram step={4} />
    拿这个片元的当前深度，和深度图里同方向存的<strong>最近遮挡距离</strong>
    比：如果当前深度<strong>更大</strong>
    （它比最近遮挡还远），说明前面有别的东西先挡住了光 ⇒ 它
    <span style={{ color: "var(--danger)" }}>在阴影</span>里；如果两者基本
    <strong>相等</strong>，说明它自己就是最近的、
    <span style={{ color: "var(--success)" }}>直接受光</span>。
  </Step>
</Stepper>

## 第二遍要做的关键动作：变到光空间 + 比深度

第二遍里，每个片元要做两件关键事。第一件是变换：把片元的世界坐标，用**光源的观察矩阵和投影矩阵**乘一遍，变到 <Term def="把片元位置用光源的「观察矩阵 × 投影矩阵」变换后所处的坐标空间，也叫光的裁剪空间。在这个空间里做透视除法、再从 [-1,1] 映射到 [0,1]，就能得到片元在光眼里的深度（z），以及它对应深度图上的取样坐标（xy）。第一遍渲深度图用的就是同一套光源矩阵，所以两遍的坐标系对得上。">光空间</Term>——也就是「光眼里」的坐标。这一步和第一遍渲深度图用的是**同一套光源矩阵**，所以两遍的坐标系严丝合缝对得上：变换后片元的 xy 正好对应它落在深度图上的哪个像素，z 正好是它在光眼里的深度。

第二件是 <Term def="阴影映射第二遍的核心判定：把当前片元在光空间算出的深度（current depth），和深度图里同方向存的最近遮挡距离（closest depth）做大小比较。current > closest 说明前面有更近的东西先挡住了光、该片元在阴影里；current ≤ closest 说明它自己就是最近的、直接受光。比较结果通常用 0/1 表示「在阴影 / 受光」，乘到光照上即可。">深度比较</Term>：拿片元算出的当前深度，去深度图对应像素取出存着的「最近遮挡距离」，两个一比。当前深度更大 ⇒ 它比最近遮挡还靠后 ⇒ 被挡 ⇒ 在阴影；当前深度不大于 ⇒ 它自己就是最近的 ⇒ 受光。这个判定每个片元做一次，结果（在阴影 0 / 受光 1）乘到光照上，影子就出来了。

听起来天衣无缝，但真写出来，你会立刻撞上两个经典翻车——一个让本该干净的表面爬满条纹，一个让影子飘起来。下面 Demo 里你能亲手把它们调出来再修掉，这正是本章最该上手的部分。

{/* 4. 数学推导（spec §四）：math:false，整节省略，不写。光空间矩阵（lightProjection×lightView）+ 透视除法 + [0,1] 映射 已在 §3 概念节点到、§6 代码对照里逐行讲，符合任务卡「math:false 整节省略，光空间矩阵在代码对照里讲」。 */}

{/* 5(主 viz). 交互 Demo（spec §五，C 型主 viz = ShadowMappingDemo，three.js 内建 shadow map）：控件 光源角度/阴影图分辨率/depth bias/PCF 开关 + 重置（≤5）。猜一猜引导（bias 拖到最大出 peter panning——three 内建偏移太稳、盲调演示不出 acne，acne 交给图解/误区）。caption 诚实说明用 three 内建 shadow map、与两遍法同原理。 */}

## 动手：拖 bias 造出又修掉 peter panning

> 猜一猜：下面的场景已经有了干净的阴影。把 **depth bias** 滑块拖到**最大**，你觉得物体的影子会怎样？是变得更黑，还是和物体**脚下脱了节、整片往后缩**、物体像重新飘起来？先动手拖一拖，再看下面的解释。

这个 demo 用的是 three.js 的**内建 shadow map**——它的底层做的正是本章讲的两遍法（先从光源视角渲一张深度图、再回相机比深度判阴影），只是 three 把它封装好了。我们把本章要教的参数全做成了实时控件，让你亲手拖出每个概念：**光源角度**绕场景转光、看阴影方向怎么变；**阴影图分辨率**切 256↔2048、看阴影边缘的锯齿；**depth bias** 往大拖、造出又修掉 **peter panning** 阴影脱离；**PCF 软阴影**开关、看硬边变软边。

<ShadowMappingDemo />

谜底：把 **bias 拖到最大**，你会看到物体的影子**和物体脚下脱了节、整片往后缩**，球像重新飘了起来——这就是 **peter panning**。把 bias 往回拖到中间那段，影子重新贴回物体脚下、恢复干净。再试试把**分辨率**从 2048 切到 256：阴影边缘从平滑变成粗粗的**阶梯锯齿**；把 **PCF** 关掉，边缘是生硬的硬边，开开就软化了。每个控件都对着一个本章概念，多拖几次就有手感了。至于 bias **太小**会出的 <Term def="阴影映射里本该干净的受光表面爬满交替亮暗条纹的自遮挡现象（像摩尔纹）。根因：深度图分辨率有限，一个像素覆盖地面斜斜一小片，整片只存取样点一个最近深度，片内更远离光的半边 current > stored 被自己误判「在阴影」。修法是加 depth bias 把存的最近深度往更远推一点点，让本该受光的半边越不过这条线。">shadow acne</Term> 自遮挡条纹，这台 three.js 演示因内建偏移很稳、盲调不易拖出，下一节「容易踩的坑」用图解把它的成因和 bias 修法讲清。

{/* 6. 代码对照（spec §六，C 型重心，覆盖 ≥80% 关键代码）：两遍法 C++/GLSL330 ↔ WebGL2/GLSL300es 同逻辑段；单块 ≤30 行；块间有解说；version/precision/深度纹理格式差异 Callout warn。a. 第一遍光空间矩阵 + 渲深度 FBO；b. 第二遍片段着色器：FragPos 变光空间 / 透视除法 / 转 [0,1] / 采样 shadow map / 深度比较 + bias + PCF 3×3。 */}

## 代码对照：两遍法怎么落到 shader

上面的 demo 是 three 帮你封装好的；这一节把底层「裸写」一遍，看清两遍法每一步的代码。先看**第一遍**的两件事：① 算**光空间矩阵** = 光的投影矩阵 × 光的观察矩阵（方向光用正交投影，`lookAt` 让光「看向」场景中心）；② 绑定一个只挂深度纹理的 FBO，用这个矩阵当 MVP 把场景渲一遍——这一遍只写深度、不写颜色。

<CodeTabs>
  <Tab label="C++ / OpenGL 3.3（第一遍：光空间矩阵 + 渲深度）">

```cpp
// ① 光空间矩阵 = 光的正交投影 × 光的观察矩阵（方向光用正交）
float near = 1.0f, far = 24.0f;
glm::mat4 lightProjection = glm::ortho(-9.0f, 9.0f, -9.0f, 9.0f, near, far);
glm::mat4 lightView = glm::lookAt(lightPos, glm::vec3(0.0f), glm::vec3(0, 1, 0));
glm::mat4 lightSpaceMatrix = lightProjection * lightView;
// ② 绑深度 FBO，用 lightSpaceMatrix 当 MVP 把场景渲一遍（只写深度）
glViewport(0, 0, SHADOW_W, SHADOW_H);
glBindFramebuffer(GL_FRAMEBUFFER, depthMapFBO);
glClear(GL_DEPTH_BUFFER_BIT);
depthShader.setMat4("lightSpaceMatrix", lightSpaceMatrix);
renderScene(depthShader);                 // 只渲深度，得到 shadow map
glBindFramebuffer(GL_FRAMEBUFFER, 0);     // 绑回默认帧缓冲
```

  </Tab>
  <Tab label="TypeScript / WebGL2（第一遍：光空间矩阵 + 渲深度）">

```ts
// ① 光空间矩阵 = 光的正交投影 × 光的观察矩阵（方向光用正交）
const near = 1.0,
  far = 24.0;
const lightProjection = mat4.ortho(-9, 9, -9, 9, near, far);
const lightView = mat4.lookAt(lightPos, [0, 0, 0], [0, 1, 0]);
const lightSpaceMatrix = mat4.multiply(lightProjection, lightView);
// ② 绑深度 FBO，用 lightSpaceMatrix 当 MVP 把场景渲一遍（只写深度）
gl.viewport(0, 0, SHADOW_W, SHADOW_H);
gl.bindFramebuffer(gl.FRAMEBUFFER, depthMapFBO);
gl.clear(gl.DEPTH_BUFFER_BIT);
gl.uniformMatrix4fv(uLightSpaceMatrix, false, lightSpaceMatrix);
renderScene(depthProgram); // 只渲深度，得到 shadow map
gl.bindFramebuffer(gl.FRAMEBUFFER, null); // 绑回默认帧缓冲
```

  </Tab>
</CodeTabs>

第一遍的深度 FBO 只挂一张**深度纹理**当附件（没有颜色附件），渲完它就成了 shadow map。建纹理这步两端有个要注意的格式差异：

<Callout type="warn">
  深度纹理的内部格式两端写法不同：桌面 OpenGL 用 `GL_DEPTH_COMPONENT`，WebGL2
  要写成 `gl.DEPTH_COMPONENT24` 这类带位数的**确定尺寸格式**（WebGL2
  对深度纹理格式更严）。另外只渲深度时，桌面要 `glDrawBuffer(GL_NONE)` /
  `glReadBuffer(GL_NONE)` 显式说明「没有颜色附件」；WebGL2 里只挂
  `DEPTH_ATTACHMENT`、不挂颜色附件即可。其余逻辑一致。
</Callout>

第一遍渲深度用的顶点着色器只有一行关键计算：用 `lightSpaceMatrix` 把模型顶点变到光的裁剪空间，剩下交给硬件写深度。两端 GLSL 几乎一样，只差版本声明：

<CodeTabs>
  <Tab label="C++ / OpenGL 3.3（第一遍 · 顶点着色器）">

```glsl
#version 330 core
layout (location = 0) in vec3 aPos;
uniform mat4 lightSpaceMatrix;
uniform mat4 model;
void main() {
  // 用光空间矩阵把顶点变到光的裁剪空间，硬件自动写深度
  gl_Position = lightSpaceMatrix * model * vec4(aPos, 1.0);
}
```

  </Tab>
  <Tab label="TypeScript / WebGL2（第一遍 · 顶点着色器）">

```glsl
#version 300 es
layout (location = 0) in vec3 aPos;
uniform mat4 lightSpaceMatrix;
uniform mat4 model;
void main() {
  // 用光空间矩阵把顶点变到光的裁剪空间，硬件自动写深度
  gl_Position = lightSpaceMatrix * model * vec4(aPos, 1.0);
}
```

  </Tab>
</CodeTabs>

第二遍是相机正常渲场景。顶点着色器除了照常算屏幕位置，**额外**把世界坐标乘一遍 `lightSpaceMatrix`、传给片段着色器当 `FragPosLightSpace`（就是这个片元在光空间的位置）。真正的判定全在片段着色器的 `ShadowCalculation` 里：透视除法 → 映射到 `[0,1]` → 采样深度图取 `closestDepth` → 和 `currentDepth` 比较，并加上 **bias** 防自遮挡：

<CodeTabs>
  <Tab label="C++ / OpenGL 3.3（第二遍 · 阴影判定 + bias）">

```glsl
float ShadowCalculation(vec4 fragPosLightSpace, vec3 N, vec3 L) {
  // 透视除法 → [-1,1]，再映射到 [0,1]（对齐深度图取样坐标）
  vec3 proj = fragPosLightSpace.xyz / fragPosLightSpace.w;
  proj = proj * 0.5 + 0.5;
  float currentDepth = proj.z;            // 片元在光眼里的深度
  // 斜面偏移：表面越斜 bias 越大，防 shadow acne
  float bias = max(0.005 * (1.0 - dot(N, L)), 0.0005);
  // 取出深度图里这方向存的「最近遮挡距离」
  float closestDepth = texture(shadowMap, proj.xy).r;
  // 当前深度比最近遮挡还远（+bias）⇒ 被挡 ⇒ 在阴影（1.0）
  return currentDepth - bias > closestDepth ? 1.0 : 0.0;
}
```

  </Tab>
  <Tab label="TypeScript / WebGL2（第二遍 · 阴影判定 + bias）">

```glsl
float ShadowCalculation(vec4 fragPosLightSpace, vec3 N, vec3 L) {
  // 透视除法 → [-1,1]，再映射到 [0,1]（对齐深度图取样坐标）
  vec3 proj = fragPosLightSpace.xyz / fragPosLightSpace.w;
  proj = proj * 0.5 + 0.5;
  float currentDepth = proj.z;            // 片元在光眼里的深度
  // 斜面偏移：表面越斜 bias 越大，防 shadow acne
  float bias = max(0.005 * (1.0 - dot(N, L)), 0.0005);
  // 取出深度图里这方向存的「最近遮挡距离」
  float closestDepth = texture(shadowMap, proj.xy).r;
  // 当前深度比最近遮挡还远（+bias）⇒ 被挡 ⇒ 在阴影（1.0）
  return currentDepth - bias > closestDepth ? 1.0 : 0.0;
}
```

  </Tab>
</CodeTabs>

上面只采样了深度图**一个**像素，阴影边缘会是生硬的硬边、还带阶梯锯齿。<Term def="Percentage-Closer Filtering，百分比邻近滤波：阴影判定时不只采样深度图上一个像素，而是采样它周围一圈（如 3×3 共 9 个）像素各做一次深度比较，把「在阴影」的比例平均成一个 0~1 的值。于是阴影边缘不再是非黑即白的硬边，而是从受光到全影的一段灰色过渡——边缘被软化、锯齿被抹平。">PCF</Term>（百分比邻近滤波）的修法是：不只看一个像素，而是采样周围 **3×3 共 9 个**像素各比一次深度，把「在阴影」的比例**平均**——边缘就从非黑即白变成一段灰色过渡，软化、抹平锯齿。把上面最后一行换成一个 3×3 循环：

<CodeTabs>
  <Tab label="C++ / OpenGL 3.3（PCF 3×3 软化边缘）">

```glsl
// 用 PCF 替换「单像素比较」：采样 3×3 邻域，平均「在阴影」的比例
float shadow = 0.0;
vec2 texelSize = 1.0 / vec2(textureSize(shadowMap, 0));
for (int x = -1; x <= 1; ++x) {
  for (int y = -1; y <= 1; ++y) {
    // 偏移一个像素采样邻居，各自做一次深度比较
    float pcfDepth = texture(shadowMap, proj.xy + vec2(x, y) * texelSize).r;
    shadow += currentDepth - bias > pcfDepth ? 1.0 : 0.0;
  }
}
shadow /= 9.0;                             // 9 次比较取平均 → 软边
```

  </Tab>
  <Tab label="TypeScript / WebGL2（PCF 3×3 软化边缘）">

```glsl
// 用 PCF 替换「单像素比较」：采样 3×3 邻域，平均「在阴影」的比例
float shadow = 0.0;
vec2 texelSize = 1.0 / vec2(textureSize(shadowMap, 0));
for (int x = -1; x <= 1; ++x) {
  for (int y = -1; y <= 1; ++y) {
    // 偏移一个像素采样邻居，各自做一次深度比较
    float pcfDepth = texture(shadowMap, proj.xy + vec2(x, y) * texelSize).r;
    shadow += currentDepth - bias > pcfDepth ? 1.0 : 0.0;
  }
}
shadow /= 9.0;                             // 9 次比较取平均 → 软边
```

  </Tab>
</CodeTabs>

最后把 `shadow`（0 受光 ~ 1 全影）乘进光照：阴影里只留环境光、漫反射和镜面被 `(1 - shadow)` 压掉，比如 `(ambient + (1.0 - shadow) * (diffuse + specular)) * color`。这几段计算两端 GLSL 一字不差，差异只有版本声明：

<Callout type="warn">
  这几段着色器逻辑桌面和 WebGL2 完全一致，跨端区别还是老几样：桌面写 `#version
  330 core`、WebGL2 写 `#version 300 es`，且 WebGL2 片段着色器开头必须加
  `precision highp float;`、采样器也建议 `precision highp
  sampler2D;`。`texture()` / `textureSize()` 两端同名。深度纹理采样回来的 `.r`
  就是 `[0,1]` 的深度。
</Callout>

{/* 7. 常见误区（spec §七，C 型 ≥3）：固定格式 现象 → 原因 → 修法；每个 ≤150 字；至少 1 个能在 §5 Demo 亲手触发。必含：①shadow acne 没加 bias 自遮挡条纹（成因/修法靠 ShadowAcneDiagram 图解——three 内建偏移太稳，demo 盲调演示不出 acne）；②peter panning bias 过大阴影脱离（demo 可触发）；③光锥外/越界采样致远处全黑或全亮（clamp/border 或范围判断）。可选第四：忘了透视除法 / 光空间矩阵算错。 */}

## 容易踩的坑

第一个坑你在 demo 里已经亲手见过了，这里说清它为什么发生、怎么修——配图把成因和修法画在一起：

<ShadowAcneDiagram mode="acne" />

<ShadowAcneDiagram mode="bias" />

<Callout type="trap">
  **现象（shadow acne 自遮挡条纹）**：本该干净的受光表面爬满一道道**交替亮暗的条纹**（像摩尔纹）。**原因**：深度图分辨率有限，一个像素覆盖地面**斜斜一小片**，整片只存取样点那**一个**最近深度；片内更远离光的半边
  `current > stored`，被自己误判「在阴影」。**修法**：加 **depth bias**——把存的深度往**更远**推一点点（斜面越斜推得越多，如
  `max(0.005*(1-N·L), 0.0005)`），让本该受光的半边越不过这条线。
</Callout>

修法虽简单，但**矫枉过正**会引出第二个坑——这也是 demo 里把 bias 拖太大时你看到的「影子飘起来」：

<Callout type="trap">
  **现象（peter panning
  阴影脱离）**：物体的影子和物体**脚下脱了节、整片往后缩**，物体看着重新「飘」了起来。**原因**：上一个坑的
  **bias
  加过头**——把存的深度推得太远，连物体**贴地接触处那一圈本该有的影子**也被一起判成了受光，影子于是缺了根、与物体分离。**修法**：bias
  取**够用就好**的小值（demo
  里中间那段）；想两全可用**正面剔除**渲深度图（只渲背面写深度），从根上同时压住
  acne 和 panning。
</Callout>

<Callout type="trap">
  **现象（光锥外全黑或全亮）**：场景中**远处**或**光照不到的边角**整片**死黑**或整片**异常发亮**，且阴影只在中间一小块正常。**原因**：片元变到光空间后 `proj.xy`
  **超出了 [0,1]**（落在光的正交视锥之外），采样深度图时**越界**取到了边缘重复值或 `> 1` 的
  `proj.z`，判定全乱。**修法**：深度图采样用 `CLAMP_TO_EDGE` + 把边界设成**白色（深度
  1.0）**（`CLAMP_TO_BORDER` borderColor=1），并对 `proj.z > 1.0` 的片元直接判**受光**（`shadow = 0`）；同时让光的正交视锥够大、罩住整个场景。
</Callout>

<Callout type="trap">
  **现象（影子位置全错或满屏乱影）**：影子完全对不上物体，或整屏闪烁错位。**原因**：光空间坐标算错了——**忘了透视除法**（`fragPosLightSpace.xyz
  / .w`），或忘了把 `[-1,1]` **映射到 `[0,1]`**（`* 0.5 +
  0.5`），导致取样坐标和深度全错位。**修法**：第二遍判定**必做**这两步：先 `proj
  = fragPosLightSpace.xyz / fragPosLightSpace.w`，再 `proj = proj * 0.5 +
  0.5`，之后才用 `proj.xy` 采样、`proj.z` 比较。
</Callout>

{/* 8. 小结与练习（spec §八）：小结 ≤5 条且与学习目标一一呼应；练习含 1 道改 Demo 题 + C 型 1 道独立实现题（给目标效果）；答案折叠。 */}

## 小结

- **阴影映射 = 两遍法**：第一遍从光源视角渲一张**深度图**（shadow map），只记「每个方向最近的遮挡距离」；第二遍从相机渲场景顺便判阴影
- 第二遍每个片元做两件事：用**光空间矩阵**把它变到光眼里（含透视除法 + 映射到 `[0,1]`），再做**深度比较**——`current > 深度图存的 closest` ⇒ 被挡 ⇒ 在阴影
- **shadow acne** 自遮挡条纹是深度图分辨率有限 + 自遮挡所致（成因与 bias 修法见图解），加 **depth bias**（把存的深度往更远推一点）修掉；推**过头**则出 **peter panning** 阴影脱离——这一头在 demo 里把 bias 拖到最大就能亲手看到，bias 取够用就好
- 单像素采样的阴影边缘是硬边 + 阶梯锯齿；**PCF** 采样 3×3 邻域取平均把边缘**软化、抹平锯齿**；**阴影图分辨率**越高锯齿越细
- 片元落在**光锥外**（`proj.xy` 越界 / `proj.z > 1`）要 clamp 到边界白色并直接判受光，否则远处会整片全黑或全亮

<Exercises>

**练习 1（改 Demo 代码型）** 在上面的 demo 里，**复现 peter panning** 然后**修掉它**：先把影子和物体脚下拖到脱节、缩回去（物体像重新飘起来），再只动一个控件把它修回贴地。说出你怎么调，并解释为什么那样会出 peter panning、又为什么那一步能修掉。

<Answer>

- **造出 peter panning**：把 **depth bias** 拖到**最大**。bias 加过头，相当于把深度图里存的最近深度往「更远」推得太多，连物体**贴地接触处那一圈本该有的影子**也被一起判成了受光 ⇒ 影子缺了根、与物体脚下分离、整片往后缩，物体看着重新「飘」了起来。
- **修掉它**：只把 **depth bias** 往回拖到**中间**那段。把存的最近深度只往更远推一点点（够用就好），贴地接触处的影子重新被判在阴影里、影子贴回物体脚下。
- 这也说明 bias 是把双刃剑：太大出 peter panning（影子脱离），太小则理论上会出 shadow acne（自遮挡条纹，成因见上方图解与误区）——「够用就好」的中间值才两全。

</Answer>

**练习 2（独立实现题 · C 型）** 不依赖 three 这类封装，自己用裸 WebGL2 实现一个**方向光的硬阴影**（目标效果：一个立方体在地面上投下一块清晰的影子，无 acne、无 peter panning，边缘是硬边即可）。列出你需要的关键步骤（不必写全部代码，但要点要全）。

<Answer>

关键步骤（对应正文两遍法）：

1. **建深度 FBO**：创建一张深度纹理（WebGL2 用 `gl.DEPTH_COMPONENT24`），只挂到 FBO 的 `DEPTH_ATTACHMENT`、不挂颜色附件；采样参数设 `CLAMP_TO_EDGE`。
2. **第一遍渲深度图**：算 `lightSpaceMatrix = lightProjection(正交) × lightView(lookAt 光源→场景中心)`；绑深度 FBO、设 viewport 为阴影图尺寸、清深度，用只输出 `gl_Position = lightSpaceMatrix * model * pos` 的简易着色器把场景渲一遍。
3. **第二遍渲场景**：绑回默认帧缓冲，相机正常渲；顶点着色器额外算 `FragPosLightSpace = lightSpaceMatrix * worldPos` 传给片段着色器。
4. **片段着色器判阴影**：透视除法 `proj = FragPosLightSpace.xyz / .w` → `proj = proj*0.5+0.5` → `closest = texture(shadowMap, proj.xy).r` → `currentDepth = proj.z` → 加斜面 bias `max(0.005*(1-N·L), 0.0005)` → `shadow = currentDepth - bias > closest ? 1 : 0`；对 `proj.z > 1.0` 直接判受光。
5. **合成**：最终色 `(ambient + (1 - shadow) * (diffuse + specular)) * objColor`。
   （想要软边再把第 4 步换成 PCF 3×3 循环取平均。）

</Answer>

</Exercises>

{/* 10. 名词解释（spec §十）：与正文 <Term> 高亮一一对应（6 个，按首现顺序）；释义零基础大白话。位置章末、出处声明之前。 */}

<Glossary>

<GlossaryItem term="阴影映射">
  一种做实时阴影的办法：先把一台「相机」搬到**光源**的位置朝场景拍一遍，记下「从光看过去，每个方向最近的遮挡物有多远」存成一张图；再从真正的相机渲染时，对每个片元换到光的视角下比一比——它比最近遮挡还远，就说明前面有东西先挡住了光，它就在影子里。一句话：**凡是光相机看不见的地方就是影子**。详见本章「核心思想」一节。
</GlossaryItem>

<GlossaryItem term="shadow map">
  从光源视角渲染场景得到的一张**深度图**（深度纹理）。它的每个像素不存颜色，只存一个**距离**：沿光的这个方向望过去，离光源**最近**的遮挡物有多远。第二遍渲染时拿它和片元的当前深度对比，判断片元是否被更近的东西挡住。详见本章「核心思想」一节。
</GlossaryItem>

<GlossaryItem term="光空间">
  把片元位置用**光源的观察矩阵 ×
  投影矩阵**变换后所处的坐标空间（也叫光的裁剪空间）。在这里做一次**透视除法**、再从
  `[-1,1]` 映射到
  `[0,1]`，就能得到片元在「光眼里」的深度（z）和它对应深度图上的取样坐标（xy）。因为它和第一遍渲深度图用的是**同一套光源矩阵**，两遍的坐标系对得上。详见本章「关键动作」一节。
</GlossaryItem>

<GlossaryItem term="深度比较">
  阴影映射第二遍的核心判定：拿片元在光空间算出的**当前深度**，和深度图里同方向存的**最近遮挡距离**比大小。当前深度**更大**（比最近遮挡还远）⇒
  前面有更近的东西先挡住了光 ⇒ 它**在阴影**里；当前深度**不大于** ⇒
  它自己就是最近的 ⇒ **受光**。结果（在阴影 /
  受光）乘到光照上，影子就出来了。详见本章「关键动作」一节。
</GlossaryItem>

<GlossaryItem term="shadow acne">
  阴影映射里本该干净的受光表面爬满**交替亮暗条纹**的自遮挡现象。根因：深度图分辨率有限，一个像素覆盖地面**斜斜一小片**，整片只存取样点**一个**最近深度，片内更远离光的半边
  `current > stored` 被自己误判「在阴影」。修法是加 **depth bias**（把存的最近深度往**更远**推一点点），让本该受光的半边越不过这条线。详见本章「容易踩的坑」一节。
</GlossaryItem>

<GlossaryItem term="PCF">
  Percentage-Closer
  Filtering，百分比邻近滤波。判阴影时不只采样深度图上**一个**像素，而是采样它**周围一圈**（如
  3×3 共 9 个）各做一次深度比较，把「在阴影」的比例**平均**成一个 0~1
  的值。于是阴影边缘不再是非黑即白的硬边，而是一段从受光到全影的**灰色过渡**——边缘被软化、锯齿被抹平。详见本章「代码对照」一节。
</GlossaryItem>

</Glossary>

{/* 9. 出处声明（spec §九）：Attribution 由 page.tsx 闭包注入本章 frontmatter.sourceUrl，写空标签即可（硬规则 4）。 */}

<Attribution />
