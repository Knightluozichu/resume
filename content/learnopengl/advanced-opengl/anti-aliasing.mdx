---
title: 抗锯齿
type: A
section: 高级OpenGL
order: 11
description: 搞懂斜边在像素方格上为什么会变成「阶梯」（锯齿）——采样不足的成因、超采样 SSAA 按更高分辨率渲染再缩小、多重采样 MSAA 每像素多个采样点判覆盖度只在边缘多采、GLFW_SAMPLES 开 MSAA、离屏 MSAA 帧缓冲 blit 解析、WebGL2 的 antialias 开关，以及 MSAA 抗不了的那些锯齿。高级 OpenGL 篇收官章。
demo: true
math: false
sourceUrl: https://learnopengl-cn.github.io/04%20Advanced%20OpenGL/11%20Anti%20Aliasing/
draft: true
---

{/* A 概念型（HEL-77，高级OpenGL 篇第 11 篇 · 收官）：原书「抗锯齿 Anti Aliasing」。重心是概念讲解(§3)：锯齿成因（采样不足、斜边落方格成阶梯）、抗锯齿（让边缘平滑）、超采样 SSAA（按更高分辨率渲染再缩小，质量好但贵）、多重采样 MSAA（每像素多采样点判覆盖度、只边缘多采、片段着色仍每像素一次）、覆盖度 coverage（几个采样点落图元内→边缘混色比例）、GLFW_SAMPLES 开 MSAA、离屏 MSAA 帧缓冲（multisample renderbuffer + blit 解析）、WebGL2 抗锯齿（antialias:true / renderbufferStorageMultisample）、FXAA 后处理简介。数学 math:false（§4 整节省略）。承接帧缓冲（离屏 FBO/blit）与面剔除/深度（同 section 管线测试叙述），沿用相机与摄影/像素网格类比库（屏幕=方格纸，斜边落格上只能整涂/不涂）。主 viz 两个：CompareSlider 关 vs 开抗锯齿（<AliasingDiagram aa> 两态同框）+ Stepper MSAA 一像素判覆盖度（<MsaaSampleDiagram step> 每步图），辅 <SsaaVsMsaaDiagram> 对照。draft:true，老板验收后才发布（硬规则 7）。 */}

{/* 知识点清单（动笔前先摊开，每条标 viz；难啃的用图掰碎；全部配 viz，无纯文字一笔带过；Term 首现高亮 + 章末 Glossary 一一对应）：1) 锯齿 aliasing（斜边落方格上、采样不足成阶梯）= 直觉引入 + 概念 + <AliasingDiagram aa={false}> + <CompareSlider>；2) 抗锯齿 anti-aliasing（让边缘平滑）= 概念 + <CompareSlider>（主 viz 1，AliasingDiagram 关/开）；3) 超采样 SSAA（按更高分辨率渲染再缩小，质量好但贵）= 概念 + <SsaaVsMsaaDiagram>；4) 多重采样 MSAA（每像素多采样点判覆盖、只边缘多采、片段着色每像素一次）= 概念 + <Stepper>（主 viz 2，<MsaaSampleDiagram> 每步图）+ <SsaaVsMsaaDiagram>；5) 覆盖度 coverage（几个采样点落图元内→边缘混色比例）= 概念 + <MsaaSampleDiagram step={2,3}>；6) GLFW_SAMPLES + glEnable(GL_MULTISAMPLE) 开 MSAA = 概念 + 代码对照；7) 离屏 MSAA 帧缓冲（multisample renderbuffer + blit 解析）= 概念 + 代码对照 + 误区；8) WebGL2 抗锯齿（antialias:true / renderbufferStorageMultisample / blitFramebuffer）= 代码对照 + Callout；9) FXAA 后处理简介（基于图像找边再模糊，便宜但糊）= 概念。Term 共 6 个（锯齿 / 抗锯齿 / 超采样 SSAA / 多重采样 MSAA / 覆盖度 / FXAA）。 */}

{/* 1. 学习目标（spec §一）：A 型必含「能解释 ___ 在渲染管线中的位置」；2-3 条可检验动词；末条为可当场自测的检验问题。 */}

<Objectives>

- 能解释**锯齿**是怎么来的（光栅化时每像素只取一个采样点、斜边落在像素方格上只能整涂或整不涂），以及抗锯齿在管线里发生在哪一步（光栅化判覆盖度这一环）
- 能说清 **SSAA**（整张图按更高分辨率渲染再缩小）和 **MSAA**（每像素多个采样点判覆盖度、只在几何边缘多采、片段着色仍每像素一次）各自怎么省、为什么 MSAA 性价比更高
- 能回答：开了 MSAA，物体的几何边缘顺滑了，可贴在它身上的**纹理内部**、还有着色器里 `if`/`discard` 切出来的硬边（比如高光边、镂空草叶边）却还是锯齿——这是为什么？MSAA 到底抗的是哪种锯齿？

</Objectives>

{/* 2. 直觉引入（spec §二）：≤3 段、每段 ≤120 字、零术语（专业名词最早第 3 节）、回答「解决什么问题 / 没有它会怎样」、类比固定用相机与摄影/像素网格库（屏幕=方格纸，斜边落格上只能整涂/不涂）。 */}

## 屏幕是一张方格纸

把屏幕想成一张方格纸，每个格子就是一个像素，要么整格涂一种颜色、要么整格留白，没有「涂半格」这回事。现在让你在这张方格纸上画一条**斜线**——麻烦来了：斜线会斜斜地穿过一排格子，每个格子它只盖住了一角或一半，可你只能决定「这格涂还是不涂」。

于是这条本该笔直顺滑的斜边，画出来就成了一级一级的**阶梯**：盖得多的格子涂上、盖得少的留白，台阶状的边缘看着毛毛糙糙，尤其放大或斜线很缓时特别扎眼。圆和斜的物体边缘全是这种毛刺。这一章要解决的就是：**怎么把这种台阶状的毛糙边缘，变回看起来顺滑的样子**。

办法说穿了很朴素：与其在边缘格子上非黑即白地硬选，不如给它们涂上**半深的过渡色**——这格被斜线盖了一半，就涂一半深的颜色。远看，这些过渡色就把台阶「抹」顺滑了。没有它，你的每一条斜边、每一个圆、每一根细线都带着锯齿毛刺，画面廉价又刺眼。

{/* 3. 概念讲解（spec §三，A 型重心）：一小节一概念；术语首现 <Term def> 高亮且进章末 Glossary；每概念配图或引用 Demo；顺序 是什么 → 为什么 → 在管线中位置。全章新核心概念克制（锯齿 / 抗锯齿 / SSAA / MSAA / 覆盖度 / FXAA）。 */}

## 锯齿：斜边落在方格上的阶梯

先给那个台阶边缘起个正名。它叫 <Term def="光栅化时，由于屏幕由一个个像素方格组成、每个像素只用一个采样点（通常是像素中心）来判断自己有没有被图元盖住，斜边/曲边落在方格上只能「整格涂」或「整格不涂」，于是平滑的边被画成一级一级的硬阶梯状毛刺。本质是「采样点太少、采样不足」造成的走样。">锯齿</Term>（aliasing，也叫走样）：GPU 把三角形变成像素时，对每个像素只取**一个采样点**（通常就是像素中心），用它判断「这个像素到底有没有被三角形盖住」——盖住了就整格涂三角形的色，没盖住就整格不涂。

问题正出在「一个像素只问一个点」上。斜边穿过一个像素时，可能盖住了它一大半面积，但只要像素中心那个采样点没落在三角形里，这格就被判成「没盖住」、整格留白；反之中心被盖住，哪怕只盖了一点点，也整格涂满。这种「非涂即不涂」的二选一，把本该平滑的边逼成了阶梯。下面这张图把「方格纸上的斜边为什么成阶梯」画出来——虚线是它本该有的样子：

<AliasingDiagram aa={false} />

它在管线里的位置也清楚了：锯齿诞生在**光栅化**这一步（三角形被切成像素、逐像素判覆盖的时候）。根子是**采样不足**——一个像素只采一个点，信息不够，自然还原不出平滑的边。抗锯齿要做的，就是在这一步给每个边缘像素多一点信息、或更精细的判断。

## 抗锯齿：给边缘格涂上过渡色

对症的解法就叫 <Term def="一类让图元边缘看起来平滑、消除锯齿阶梯的技术的统称。核心思路是不再对边缘像素「非涂即不涂」地硬选，而是根据它被图元盖住的程度，涂上介于图元色和背景色之间的半深过渡色，远看就把台阶抹顺滑了。常见做法有超采样 SSAA、多重采样 MSAA、后处理 FXAA 等。">抗锯齿</Term>（anti-aliasing，常缩写 AA）：不再对边缘像素「非涂即不涂」地硬选，而是看它**被图元盖住了多少**，盖一半就涂一半深的过渡色，盖三成就涂三成深——让边缘格子用介于图元色和背景色之间的中间色来填。

为什么这招管用？因为人眼在正常观看距离下会自动把这些深浅不一的过渡色「平均」掉，台阶的硬棱被柔化，远看就成了一条顺滑的斜边。同一条斜边，关抗锯齿是硬阶梯、开抗锯齿是带过渡灰的平滑边，差别一看便知。它仍然发生在光栅化附近，只是把「一像素一个点的硬判定」换成了「按覆盖程度的软填色」。马上的 §5 第一个 Demo 就让你左右拖着对比这两者。

剩下的问题只有一个：**边缘像素到底被盖住了多少？** 不同的抗锯齿方法，区别就在「怎么估这个比例、估得多准、花多大代价」。下面三种是最主流的思路。

## 超采样 SSAA：先画大再缩小，简单粗暴但贵

最直接的思路是 <Term id="term-ssaa" def="超采样抗锯齿（Super Sample Anti-Aliasing）：先按远高于屏幕的分辨率（如每边 2 倍 = 4 倍像素）渲染整个场景，每个子像素都完整跑一遍片段着色，再把这张大图缩小（下采样）到屏幕分辨率——缩小时多个子像素被平均成一个像素，边缘自然得到过渡色。质量极好，但因为每个子像素都要着色，开销巨大（4 倍像素就约 4 倍着色量），实际很少直接用。">超采样</Term>（SSAA，Super Sample Anti-Aliasing）：干脆按**远高于屏幕的分辨率**把整个场景渲染一遍（比如每边放大 2 倍，就是 4 倍的像素数），然后再把这张大图**缩小**回屏幕分辨率。

为什么能抗锯齿？因为缩小时，屏幕上一个像素对应着大图里好几个子像素，把这几个子像素的颜色一**平均**，边缘处「一半子像素是图元色、一半是背景色」就自然平均出了过渡色。质量极好——它对一切边缘都生效，因为整张图就是更精细地画了一遍。

代价也极其直白：你多画了好几倍的像素，**每个子像素都得完整跑一遍片段着色**，着色开销跟着翻几倍。所以 SSAA「只拥有过短暂的辉煌」，今天很少直接用——它太贵了。能不能只在真正需要的地方（边缘）多花钱？这就引出了 MSAA。

## 多重采样 MSAA：只在边缘多采，着色还是每像素一次

<Term
  id="term-msaa"
  def="多重采样抗锯齿（Multisample Anti-Aliasing）：对每个像素放多个采样点（如 4 个），但只用这些采样点判断「这个像素被图元盖住了几个采样点」（即覆盖度），片段着色器仍然每个像素只跑一次（用插值到像素中心的数据），其结果按覆盖度与背景混合。深度/模板按子采样点存储。因为多花的只是覆盖度判定、且着色不翻倍，又只有几何边缘的像素覆盖度才不是 0 或满，所以性价比远高于 SSAA，是实时渲染默认的抗锯齿方案。"
>
  多重采样
</Term>
（MSAA，Multisample Anti-Aliasing）是 SSAA
的「精打细算版」。它给每个像素放**多个采样点**（典型是 4
个），但关键省钱点在于：这些采样点**只用来判覆盖**——数一数有几个采样点落在三角形里，从而知道这个像素被盖住了多少；而**片段着色器仍然每个像素只跑一次**（用插值到像素中心的数据算一次颜色），不像
SSAA 每个子像素都着一遍色。

于是边缘像素的最终色 = 这一次算出的图元色，**按覆盖比例和背景色混合**。比如 4 个采样点里有 2 个落在三角形内，覆盖度就是 2/4 = 50%，这个边缘像素就涂「图元色和背景色五五开」的过渡色。深度和模板这些倒是按子采样点分别存（保证遮挡判定也精细），但**最贵的着色没翻倍**。再加上图元**内部**的像素 4 个采样点全被盖住（覆盖度满）、**外部**的全没盖住（覆盖度 0），它们根本不需要混色——只有**几何边缘**那一圈像素的覆盖度才介于中间。所以 MSAA 实际只在边缘多花了点钱，性价比碾压 SSAA，是实时渲染默认的抗锯齿方案。这套「一个像素里数采样点定覆盖度、再混色」的过程，§5 第二个 Demo 单步走一遍就透了。

## 覆盖度：几个采样点落在图元内

上面反复出现的那个比例值得单独点名：<Term def="一个像素里，落在图元（三角形）内部的采样点数量占总采样点的比例。如 4 个采样点中 2 个在三角形内，覆盖度就是 2/4 = 50%。它直接决定了 MSAA 给这个边缘像素混多少图元色：覆盖度 100% 就是纯图元色、0% 就是纯背景、50% 就是两者各半。覆盖度只有在几何边缘的像素上才介于 0 和 100% 之间，图元内部恒为满、外部恒为 0。">覆盖度</Term>（coverage）——一个像素里落在图元内部的采样点占总采样点的比例。它就是 MSAA 决定「这个边缘像素该混多少图元色」的依据：覆盖度 100%（采样点全在图元内）就涂纯图元色，0%（全在外）就纯背景，50% 就图元色和背景各半。

正因为覆盖度只有在**几何边缘**的像素上才是个中间值（图元内部恒满、外部恒为 0），MSAA 的过渡色才只出现在边缘那一圈——这也正是它高效的来源。下面这张图把 SSAA「每个子像素都着色」和 MSAA「只边缘多采样点判覆盖、着色还是每像素一次」的开销差别并排画出来：

<SsaaVsMsaaDiagram />

## FXAA：拿到画面再找边缘抹一抹

还有一类思路完全不在光栅化里折腾采样点，而是等画面**整个渲染完**之后，把它当成一张普通图片来后处理——这就是 <Term def="快速近似抗锯齿（Fast Approximate Anti-Aliasing），一种后处理抗锯齿：在场景正常渲染完成后，把成品图当作普通图像，靠分析相邻像素的颜色/亮度差异找出「看起来像边缘」的地方，再沿边缘做有方向的模糊把锯齿抹掉。优点是极便宜、和几何无关、对纹理边和着色硬边也有效；缺点是基于图像猜测、会让画面整体偏糊、可能误伤细节。">FXAA</Term>（Fast Approximate Anti-Aliasing，快速近似抗锯齿）。它的做法是：在成品图上分析相邻像素的颜色/亮度差异，**猜**出哪里像是边缘，然后沿着边缘做有方向的模糊，把锯齿抹平。

它的优缺点和 MSAA 几乎互补：因为只对最终图像操作、和几何无关，所以**极其便宜**，而且对纹理边、着色器切出的硬边这些 MSAA 管不了的锯齿也照样有效（§7 会讲 MSAA 为什么管不了）。代价是它**基于图像猜测**，会把整个画面弄得**偏糊**、有时还误伤本该锐利的细节。便宜但糊，是它的标签。

{/* 4. 数学推导（spec §四，math:false）：抗锯齿是采样/覆盖度的概念与工程，无需公式推导，整节按规范省略（frontmatter math:false）。 */}

{/* 5(主 viz 1). 交互 Demo（spec §五）：A 型主 viz = <CompareSlider> 关 vs 开抗锯齿，左右两侧用 <AliasingDiagram aa={false|true} bare> 当同构同框示意（同方格网 + 同斜边，只差边缘格整涂 vs 过渡灰）。CompareSlider 前给猜一猜。 */}

## 对比一下：关 vs 开抗锯齿

> 猜一猜：把下面这个滑块从左拖到右，左边是「抗锯齿关」的那条斜边、右边是「抗锯齿开」的。关着的时候斜边边缘会是一级级的**台阶**还是**平滑**的？开了以后，边缘多出来的那些深浅不一的格子是什么？拖动看看，再回想 §3——它们正是「按覆盖程度涂上的过渡色」。

左边抗锯齿**关**着，每个格子只能整涂或整不涂，斜边被画成扎眼的硬阶梯；右边抗锯齿**开**了，边缘那圈格子按被斜线盖住的多少涂上半深的过渡灰，远看台阶就被抹顺滑了。两侧是**同一条斜边、同一张方格纸**，只差边缘格子怎么涂。拖动中间滑块左右对比：

<CompareSlider
  leftLabel="抗锯齿关（硬阶梯 · 锯齿）"
  rightLabel="抗锯齿开（过渡色 · 平滑）"
  initial={42}
  left={<AliasingDiagram aa={false} bare />}
  right={<AliasingDiagram aa={true} bare />}
/>

看出来了吧——开抗锯齿后多出来的，全是**边缘格子上深浅不一的过渡色**。它们一个不少地待在斜边经过的那一圈格子上，按各自被盖住的程度取了图元色和背景色之间的中间值。远看，正是这些过渡色把硬台阶柔化成了顺滑的斜边。这就是一切抗锯齿方法的共同终点；它们的分歧只在「怎么估出每格的覆盖比例」。

{/* 5(主 viz 2). 交互 Demo（spec §五）：A 型主 viz = <Stepper> 把「MSAA 如何在一个像素里判覆盖度」概念步拆开——每步必带 SVG（chapter-spec §五：纯文字步 = 打回），用按 step prop 切状态的 <MsaaSampleDiagram>（仿 DepthTestStepDiagram/WindingCullStepDiagram）。猜一猜引导。 */}

## 单步走一遍：MSAA 怎么在一个像素里判覆盖度

把 MSAA 那套「多采样点 → 覆盖度 → 混色」放大到**一个边缘像素**上，单步走完。

> 猜一猜：放大看一个被斜边压过的像素，里面放了 4 个采样点。如果其中 2 个落在三角形内、2 个落在外，这个像素最后会被涂成**纯三角形色**、**纯背景色**，还是**两者之间的某个中间色**？是几比几的中间色？先想一想，再单步走下面三步。

每一步左边画**放大的这个像素**、右边写**这一步算出了什么**，盯着「4 个采样点里几个落在图元内、最后怎么定出混色比例」，就懂了 MSAA 的全部精髓：

<Stepper>
  <Step title="① 一条斜边压过一个像素格">
    <MsaaSampleDiagram step={1} />
    放大看场景里的<strong>一个像素</strong>。一条三角形的
    <strong>斜边</strong>正好压过它，把这个格子斜着切成两片：斜边
    <strong>一侧是图元内</strong>（三角形内部）、<strong>另一侧是图元外</strong>
    （背景）。光看这一个像素，它<strong>一半在内、一半在外</strong>
    ——该涂什么色还说不准。
  </Step>
  <Step title="② 放 4 个采样点，数几个落在图元内 → 覆盖度">
    <MsaaSampleDiagram step={2} />
    MSAA 不在像素里只取中心<strong>一个</strong>点，而是放
    <strong>4 个采样点</strong>。逐个判定它们落在斜边的哪一侧：这里
    <strong>2 个在图元内</strong>（打勾）、<strong>2 个在图元外</strong>
    。于是这个像素的
    <strong>覆盖度</strong>就是 <code>2/4 = 50%</code>——它被三角形盖住了一半。
    注意：片段着色器<strong>仍只跑一次</strong>
    ，算出一个三角形色备用，采样点只管数覆盖。
  </Step>
  <Step title="③ 按覆盖度 50% 混色 → 边缘柔和">
    <MsaaSampleDiagram step={3} />
    最后按覆盖度 <code>50%</code> 给这个像素定色：取
    <strong>三角形图元色</strong>和<strong>背景色</strong>的
    <strong>中间色</strong>
    （五五开）填进去。于是这个边缘像素不再是非黑即白的硬棱，而是一块
    <strong>半深的过渡色</strong>
    。整条边缘的像素都这么按各自覆盖度混一遍，边缘就
    <strong>柔和</strong>顺滑了——这正是 §5 那个 CompareSlider 右边的效果。
  </Step>
</Stepper>

走完这一遍就抓住了 MSAA 省钱的命门：**多出来的只是「数采样点判覆盖度」这点活，最贵的片段着色还是每像素一次**；而且只有边缘像素的覆盖度才不是 0 或满，内部/外部像素根本不用混色。这就是它比 SSAA「每子像素都着色」便宜得多、却仍能把几何边缘抹顺滑的原因。

{/* 6. 代码对照（spec §六，A 型为「选」，只贴最能说明概念的 1-2 段）：单块 ≤30 行；两 Tab 同一逻辑段；块间有解说；API 差异用 Callout 标注。① 开默认帧缓冲 MSAA：桌面 glfwWindowHint(GLFW_SAMPLES,4)+glEnable(GL_MULTISAMPLE) ↔ WebGL2 getContext("webgl2",{antialias:true})；② 离屏 MSAA：glRenderbufferStorageMultisample + glBlitFramebuffer 解析 ↔ WebGL2 renderbufferStorageMultisample + blitFramebuffer。 */}

## 代码对照：开 MSAA 与离屏解析

先看最省事的一种——给**默认帧缓冲**开 MSAA。桌面 OpenGL 在创建窗口前告诉 GLFW「我要 4 个采样点」，再启用多重采样开关；WebGL2 则在拿上下文时传一个 `antialias: true`，浏览器就自动给默认帧缓冲配好了多重采样。

<CodeTabs>
  <Tab label="C++ / OpenGL 3.3">

```cpp
// 创建窗口之前：要求默认帧缓冲每像素 4 个采样点
glfwWindowHint(GLFW_SAMPLES, 4);
// …创建窗口、初始化 GLAD 之后…
// 启用多重采样（默认是关的）
glEnable(GL_MULTISAMPLE);
// 之后正常渲染，几何边缘就自动被 MSAA 抹顺滑了
```

  </Tab>
  <Tab label="TypeScript / WebGL2">

```ts
// 拿 WebGL2 上下文时开抗锯齿：浏览器给默认帧缓冲配好多重采样
const gl = canvas.getContext("webgl2", { antialias: true });
// WebGL2 没有 GL_MULTISAMPLE 这个开关——默认帧缓冲的 MSAA
// 完全由上面这个 antialias 标志决定，开了就生效，无需再 enable
```

  </Tab>
</CodeTabs>

两端做的是同一件事，只是入口不同：桌面用 `GLFW_SAMPLES` 指定采样点数 + `glEnable(GL_MULTISAMPLE)` 开关；WebGL2 把这些都收进了 `getContext` 的 `antialias` 一个布尔里。注意这里的关键差异：

<Callout type="warn">
  API 差异：桌面 MSAA 是两步——`glfwWindowHint(GLFW_SAMPLES, 4)`
  在建窗口前定采样点数、`glEnable(GL_MULTISAMPLE)` 再启用；**WebGL2
  没有 `GL_MULTISAMPLE` 这个 `enable` 开关**，默认帧缓冲的 MSAA
  只能在创建上下文那一刻用 `getContext("webgl2", { antialias: true })`
  决定，建好就改不了。采样点数也由浏览器/驱动定，你说不准是几个。
</Callout>

可一旦你像「帧缓冲」那章那样把场景渲到**自建的离屏帧缓冲**（FBO）上做后处理，`antialias` 就管不着它了——离屏那条管线得自己配多重采样：用**多重采样 renderbuffer** 当附件渲染，画完再把它**解析（blit）**到一张普通缓冲才能采样/上屏。

<CodeTabs>
  <Tab label="C++ / OpenGL 3.3">

```cpp
// 给离屏 FBO 挂一个多重采样 renderbuffer 作颜色附件（4 个采样点）
glRenderbufferStorageMultisample(GL_RENDERBUFFER, 4, GL_RGB, w, h);
// …把场景渲进这个多重采样 FBO 之后，解析到普通 FBO…
glBindFramebuffer(GL_READ_FRAMEBUFFER, msaaFBO);
glBindFramebuffer(GL_DRAW_FRAMEBUFFER, resolveFBO);
glBlitFramebuffer(0, 0, w, h, 0, 0, w, h, GL_COLOR_BUFFER_BIT, GL_NEAREST);
```

  </Tab>
  <Tab label="TypeScript / WebGL2">

```ts
// 给离屏 FBO 挂一个多重采样 renderbuffer（WebGL2 同名方法）
gl.renderbufferStorageMultisample(gl.RENDERBUFFER, 4, gl.RGBA8, w, h);
// …渲完后把多重采样 FBO 解析到普通 FBO（同样用 blitFramebuffer）…
gl.bindFramebuffer(gl.READ_FRAMEBUFFER, msaaFBO);
gl.bindFramebuffer(gl.DRAW_FRAMEBUFFER, resolveFBO);
gl.blitFramebuffer(0, 0, w, h, 0, 0, w, h, gl.COLOR_BUFFER_BIT, gl.NEAREST);
```

  </Tab>
</CodeTabs>

两端逻辑一字对应：`renderbufferStorageMultisample` 造一个带 4 个采样点的附件、把场景渲进去，再用 `blitFramebuffer` 把它**解析**成普通图像（多个采样点合成一个像素），之后才能把这张普通图当纹理采样、做后处理或上屏。WebGL2 把 GL 全局函数搬成了 `gl.` 上下文方法，名字基本同名。完整可运行源码将在章末提供（高级 OpenGL 篇基建完成后）。

{/* 7. 常见误区（spec §七，A 型 ≥2）：固定格式 现象 → 原因 → 修法；每个 ≤150 字；至少 1 个能让读者亲眼看翻车（联想 §5 CompareSlider/Stepper）。① 以为 MSAA 抗所有锯齿 → 只抗几何边缘，纹理内部/着色器硬边它管不了；② 离屏 FBO 忘用 multisample renderbuffer / 忘 blit 解析 → MSAA 没生效照样锯齿；③ SSAA 盲目调高分辨率倍数 → 帧率暴跌。 */}

## 容易踩的坑

<Callout type="trap">
  **现象**：开了
  MSAA，物体的**外轮廓**确实顺滑了，可贴在它身上的**纹理内部**花纹边、还有用
  `if`/`discard`
  切出来的**硬边**（草叶/铁丝网的镂空边、高光的锐利边缘）却**照样锯齿**。**原因**：MSAA
  只在**几何边缘**判覆盖度、抹的是**三角形轮廓**的锯齿；纹理内部和着色器里
  `discard` 产生的边不是几何边，MSAA 的覆盖度根本不覆盖它们。**修法**：纹理走
  **mipmap / 各向异性过滤**、镂空硬边用 **alpha-to-coverage**、或对全画面加一道
  **FXAA** 这类后处理来兜。
</Callout>

<Callout type="trap">
  **现象**：给默认帧缓冲开了 MSAA
  一切正常，可一旦改成像「帧缓冲」章那样**渲到自建离屏 FBO**
  做后处理，画面就**又全是锯齿**了。**原因**：`antialias` / `GL_MULTISAMPLE`
  只管**默认**帧缓冲；离屏 FBO 是你自己建的，要么忘了给它挂**多重采样
  renderbuffer**、要么渲完忘了用 `blitFramebuffer` **解析**——这条离屏管线的 MSAA
  压根没生效。**修法**：离屏附件用 `renderbufferStorageMultisample` 造、渲完务必
  `blitFramebuffer` 解析成普通缓冲再采样上屏。
</Callout>

<Callout type="trap">
  **现象**：嫌 MSAA 还不够干净，干脆把渲染分辨率往上猛调（2 倍、3 倍、4
  倍…）想用 SSAA
  暴力抹平，结果**帧率断崖式暴跌**、画面卡成幻灯片。**原因**：SSAA
  下**每个子像素都要完整跑一遍片段着色**，分辨率每边乘 2、像素数就乘
  4，着色开销也跟着乘 4，调到很高直接拖垮
  GPU。**修法**：别盲目堆分辨率——几何边缘的锯齿交给
  **MSAA**（只在边缘多采、着色不翻倍），它才是性价比之选；SSAA
  留给离线渲染或截图。
</Callout>

{/* 8. 小结与练习（spec §八）：小结 ≤5 条且与学习目标一一呼应（含一句篇收官）；练习 2-3 题含 1 道「场景选 SSAA/MSAA/FXAA」选型题，答案折叠。 */}

## 小结

- **锯齿**来自**采样不足**：光栅化时一个像素只取一个采样点，斜边落在像素方格上只能整涂或整不涂，平滑边被逼成阶梯
- **抗锯齿**的共同终点是给边缘像素涂**按覆盖程度的过渡色**；分歧只在「怎么估覆盖比例、花多大代价」
- **SSAA** 整图按更高分辨率渲染再缩小，**每个子像素都着色**——质量好但着色翻几倍，太贵
- **MSAA** 每像素多个采样点只判**覆盖度**、片段着色仍**每像素一次**，且只有**几何边缘**覆盖度才是中间值，性价比之选；`GLFW_SAMPLES` / WebGL2 `antialias` 开它，离屏要 multisample renderbuffer + `blitFramebuffer` 解析
- MSAA 只抗**几何边缘**锯齿，纹理内部 / 着色器硬边它管不了（要 mipmap / alpha-to-coverage / **FXAA**）——至此**高级 OpenGL 篇收官**

<Exercises>

**练习 1（场景选型题：SSAA / MSAA / FXAA）** 下面三个场景，各自最该选哪种抗锯齿，为什么？① 一款要在普通显卡上跑满 60fps 的实时 3D 游戏，主要烦恼是物体**外轮廓**锯齿；② 一个着色器里大量用 `discard` 做**镂空草叶**的场景，草叶边缘锯齿严重；③ 一张离线渲染、**不在乎渲染时间**、只求画质拉满的产品效果图。

<Answer>

- **① 实时游戏、烦恼外轮廓** → 选 **MSAA**。外轮廓是几何边缘，正是 MSAA 的主场；它只在边缘多采、片段着色不翻倍，能在 60fps 预算内把轮廓抹顺，性价比最高。
- **② 大量 `discard` 镂空草叶边** → MSAA **管不了**（草叶边是着色器 `discard` 切出来的、不是几何边缘）。该上 **FXAA**（后处理找边模糊，对这种硬边也有效、还便宜），或专门的 **alpha-to-coverage**。
- **③ 离线、不在乎时间、画质拉满** → 选 **SSAA**。它对一切边缘都生效、质量最好，唯一的缺点是贵——而离线渲染恰恰不在乎慢，正好让它发挥。

一句话：实时几何边缘选 MSAA，着色硬边/便宜兜底选 FXAA，离线极致画质选 SSAA。

</Answer>

**练习 2（问答题）** 有人说「MSAA 之所以比 SSAA 便宜，是因为 MSAA 的采样点比 SSAA 少」。这个说法准确吗？MSAA 省钱的真正关键是什么？它和 SSAA 各自在哪里花了/省了开销？

<Answer>

不准确。便宜的关键**不在采样点数量**，而在**片段着色跑几次**。

- **SSAA**：按更高分辨率渲染，**每个子像素都完整跑一遍片段着色器**——最贵的着色开销跟着像素数翻几倍。
- **MSAA**：每像素也有多个采样点，但它们**只用来判覆盖度**（数几个落在图元内），**片段着色器仍然每个像素只跑一次**（用插值到像素中心的数据算一次色），再按覆盖度和背景混合。

所以 MSAA 多花的只是「数采样点」这点轻活，最贵的着色没翻倍；再加上只有几何边缘像素的覆盖度才是中间值、内部/外部根本不混色，它实际只在边缘多花了钱。这才是它便宜的真正原因。

</Answer>

**练习 3（观察 / 排错题）** 回到 §5 那个「关 vs 开抗锯齿」的对比：开抗锯齿后，边缘格子被涂上了深浅不一的过渡色。如果有人把渲染改成「渲到自建离屏 FBO 再上屏」，却发现 MSAA 像没开一样、边缘又是硬阶梯，最可能漏了哪两步？

<Answer>

最可能漏了离屏 MSAA 的**两步配套**：

1. **没给离屏 FBO 挂多重采样附件**——离屏渲染目标得用 `renderbufferStorageMultisample`（带采样点数）建的 renderbuffer，而不是普通 renderbuffer/纹理。默认帧缓冲的 `antialias` 开关管不到自建 FBO。
2. **渲完没做解析（blit）**——多重采样缓冲不能直接当普通纹理采样，必须用 `blitFramebuffer` 把它解析成一张普通缓冲（多个采样点合成一个像素、得到过渡色），再拿这张普通图去采样、后处理、上屏。

漏了第 1 步，根本没多采样；漏了第 2 步，多采样的结果没被合成出来。两步都补上，离屏管线的 MSAA 才会真正生效。

</Answer>

</Exercises>

{/* 10. 名词解释（spec §十）：与正文 <Term> 高亮一一对应（6 个，按首现顺序）；释义零基础大白话。位置章末、出处声明之前。 */}

<Glossary>

<GlossaryItem term="锯齿">
  也叫走样（aliasing）：屏幕由一个个像素方格组成，光栅化时每个像素只用**一个采样点**（通常是像素中心）判断自己有没有被图元盖住，于是斜边/曲边落在方格上只能**整格涂或整格不涂**，平滑的边被画成一级一级的硬**阶梯**毛刺。本质是「采样点太少、采样不足」。详见本章「锯齿」一节。
</GlossaryItem>

<GlossaryItem term="抗锯齿">
  让图元边缘看起来平滑、消除锯齿阶梯的技术统称（缩写
  AA）。核心是不再对边缘像素「非涂即不涂」地硬选，而是按它**被图元盖住的程度**涂上介于图元色和背景色之间的**过渡色**，远看就把台阶抹顺滑了。常见做法有
  SSAA、MSAA、FXAA。详见本章「抗锯齿」一节。
</GlossaryItem>

<GlossaryItem id="term-ssaa" term="超采样 SSAA">
  超采样抗锯齿（Super Sample
  Anti-Aliasing）：先按**远高于屏幕的分辨率**渲染整个场景（每个子像素都完整着色），再把大图**缩小**回屏幕分辨率——缩小时多个子像素被平均成一个，边缘自然得到过渡色。质量极好，但每个子像素都要着色，**开销巨大**，很少直接用。详见本章「超采样
  SSAA」一节。
</GlossaryItem>

<GlossaryItem id="term-msaa" term="多重采样 MSAA">
  多重采样抗锯齿（Multisample Anti-Aliasing）：每个像素放**多个采样点**（如 4
  个），但它们**只判覆盖度**，片段着色器**仍每像素只跑一次**，结果按覆盖度与背景混合（深度/模板按子采样点存）。因为着色不翻倍、又只有几何边缘才需混色，**性价比远高于
  SSAA**，是实时渲染默认方案。详见本章「多重采样 MSAA」一节。
</GlossaryItem>

<GlossaryItem term="覆盖度">
  一个像素里落在图元（三角形）**内部**的采样点占总采样点的比例（如 4 个里 2
  个在内 = 2/4 = 50%）。它直接决定 MSAA 给这个边缘像素混多少图元色：100%
  涂纯图元色、0% 纯背景、50%
  两者各半。只有**几何边缘**的像素覆盖度才是中间值，图元内部恒满、外部恒为
  0。详见本章「覆盖度」一节。
</GlossaryItem>

<GlossaryItem term="FXAA">
  快速近似抗锯齿（Fast Approximate
  Anti-Aliasing），一种**后处理**抗锯齿：场景渲染完后把成品图当普通图像，靠分析相邻像素的颜色/亮度差**找出边缘**，再沿边缘做有方向的模糊抹掉锯齿。优点是**极便宜**、和几何无关、对纹理边/着色硬边也有效；缺点是基于图像猜测，会让画面**偏糊**、可能误伤细节。详见本章「FXAA」一节。
</GlossaryItem>

</Glossary>

{/* 9. 出处声明（spec §九）：Attribution 由 page.tsx 闭包注入本章 frontmatter.sourceUrl，写空标签即可（硬规则 4）。 */}

<Attribution />
