---
title: 你好，窗口
type: A
section: 入门
order: 1
description: 搞懂网页上一帧画面是怎么来的——从 canvas 拿到 WebGL2 这支画笔、开起渲染循环、把整块画布刷成你想要的颜色。
demo: true
math: false
sourceUrl: https://learnopengl-cn.github.io/01%20Getting%20started/03%20Hello%20Window/
draft: false
---

{/* A 概念型首章（HEL-31）：原书「你好窗口」(GLFW 建窗 + GLAD 加载 + 渲染循环 + glClearColor 清屏) 映射到 Web/WebGL2：从 canvas 拿 WebGL2 上下文 + requestAnimationFrame 渲染循环 + gl.clearColor/gl.clear 清屏。重心是概念讲解(§3)，数学省略(math:false)，代码对照只放关键 1-2 段。draft:true 待总监 review。 */}

{/* 知识点清单（动笔前先摊开，每条标 viz 形态；难啃的用动画掰碎；全部知识点均配 viz，无纯文字一笔带过，术语首现 Term 高亮、章末 Glossary 一一对应）：1) canvas 画布元素 = Figure 图示（画家面前空白画布）；2) WebGL2 上下文 gl = 概念讲解 + 引用主 Demo（拿到画笔）；3) 渲染循环 requestAnimationFrame = Stepper 动画（电影一帧帧放，把「一帧怎么来」掰碎可单步）；4) 帧 / 颜色缓冲 color buffer = Stepper 内嵌（草稿纸到显示器的搬运）；5) 清屏 gl.clearColor+gl.clear = 主 ShaderDemo（调色看整块画布被刷成该色）；6) 上下文/画布在渲染管线中的位置 = Figure 管线位置图（A 型必含）。 */}

{/* 1. 学习目标（spec §一）：A 型必含「能解释 ___ 在渲染管线中的位置」；末条为可当场自测的检验问题。 */}

<Objectives>

- 能用自己的话解释**渲染循环**是什么、为什么画面要一帧一帧地反复重画
- 能解释**画布（canvas）与 WebGL2 上下文**在整条渲染管线里处在什么位置——它们是「画到哪、用什么笔」
- 能回答：每一帧开头为什么要先「清屏」？不清会发生什么？

</Objectives>

{/* 2. 直觉引入（spec §二）：≤3 段 × ≤120 字；零术语（术语最早第 3 节）；回答「解决什么问题 / 没有它会怎样」；类比固定用③相机与摄影（电影一帧帧放）+ 画家与画布。 */}

## 屏幕上的画面，其实是「画」出来的

想象一位画家面前摆着一张空白画布。他要展示一段动画，靠的不是魔法，而是**一张接一张地飞快重画**：擦掉旧画、画上新的一张、再擦掉、再画……快到你的眼睛看成了连续的动作。电影院的胶片、电视、游戏画面，背后都是这一套——一秒钟翻过几十张。

网页里要让画面动起来，做的是同一件事：先要有一张「画布」，再要有一支能往画布上涂的「画笔」，然后不停地重画。这一章解决的就是最开头的问题——**画布从哪来、画笔怎么拿、怎么让重画这件事一直转下去**。

没有这套机制会怎样？你连「往哪画、用什么画」都没有，更别说让画面动起来——屏幕只会是一片什么都没发生的空白。所以在画三角形、打光、贴纹理之前，得先把这张画布支起来。

{/* 3. 概念讲解（spec §三，A 型重心）：一小节一概念；术语首现 <Term def> 高亮且进章末 Glossary；每概念配图或引用 Demo；顺序 是什么 → 为什么 → 在管线中位置。全章新概念 5 个（canvas / 上下文 / 渲染循环 / 帧缓冲 / 清屏）≤5。 */}

## 画布：先在网页上钉一块板子

要在网页上画图，第一步是有一块能画的地方。这块地方就是 <Term def="HTML 里一个专门用来画图的方块元素 <canvas>，本身只是一块空白画板，要配合画图接口才能往上画。">画布（canvas）</Term>——HTML 里一个叫 `<canvas>` 的元素。它本身就是一块钉在网页上的空白板子，规定了「能画的范围有多大」，但单凭它自己，板子上什么都不会出现。

为什么需要它？因为浏览器得知道你的画往**哪儿**放、画多大。`<canvas>` 就是这块预留好的地盘。它在整条流程的最前端，是后面一切的落脚点。

下面这张图标出了我们这一章要搭的几个角色，以及它们在整条「从数据到屏幕」的流水线里各自的位置：

{/* HEL-40：手工 SVG 管线位置示意图（取代误用首页跑车海报的占位 Figure）。四角色 + 「渲染循环包住整体、每帧重复」关系，token 色、矢量清晰、与 Stepper 行内 SVG 同款视觉语言。静态图、reduced-motion 无关、不引 three。 */}

<SetupPipelineDiagram />

## 画笔：从画布身上拿到 WebGL2 上下文

光有板子还不能画——你得有支「专门画 3D 图形的笔」。在网页里，这支笔就是 <Term def="从 canvas 拿到的一个对象（通常叫 gl），它是和 GPU 沟通的总开关，所有画图命令都通过它下达。">WebGL2 上下文</Term>：调用 `canvas.getContext('webgl2')`，浏览器就把一个对象交到你手里，习惯上把它命名为 `gl`。

它是什么？可以把它想成「连通显卡（GPU）的总开关」。之后所有命令——清屏、上传数据、画三角形——全都是对这个 `gl` 对象发号施令，由它转交给 GPU 去真正执行。

为什么从 canvas 身上拿？因为这支笔天生就和那块板子绑定：你对 `gl` 下的每条命令，最终都画在它所属的那块 `<canvas>` 上。在管线里，它紧跟在画布之后——板子有了，握住笔，才能开画。

## 渲染循环：让重画这件事一直转下去

拿到笔，还差最关键的一步：**让画面动起来**。这就要靠 <Term def="一段不断重复执行「画一帧」的循环，通常用 requestAnimationFrame 驱动，让浏览器在每次刷新前调你一次。">渲染循环</Term>——一段「画一帧 → 等下一次刷新 → 再画一帧」不停重复的代码。

回到电影的类比：电影是一张张静止的胶片飞快放出来的，每一张叫一 <Term def="一整张完成的画面。屏幕上的动画 = 一帧接一帧飞快地播放，每秒通常几十帧。">帧</Term>。渲染循环就是网页里的「放映机」，它一帧接一帧地驱动你的画笔重画。我们用浏览器提供的 `requestAnimationFrame`：你把「画一帧」的函数交给它，它会在屏幕**下一次刷新之前**回头调你一次；你在函数末尾再登记一次，于是就一直转下去了。

为什么要循环、不能画一次就完？因为画面要变——物体在动、颜色在渐变、鼠标在交互。只有反复重画，每次用最新的状态画，眼睛才会看到「动」。它处在管线的最外层：每一帧，循环都会把后面「清屏 → 绘制」整套流程从头跑一遍。下面把「一帧是怎么来的」单步拆开看：

{/* HEL-39：每步配一张图（统一 viewBox 0 0 120 80，DESIGN token 色），把「一帧怎么来」从纯文字升级成图示+补充说明（chapter-spec §五 概念步必带图示）。静态图、reduced-motion 无关；外层 Stepper 根已 not-prose，内部 SVG/文字不受 prose 列表样式影响。HEL-41：4 张 SVG 抽成 <FrameStageDiagram stage={n} /> 组件——内联 SVG + 紧邻松散文字的 MDX 块/行内歧义致服务端/客户端 <p> 包裹分歧、hydration mismatch；改为组件 + 文字即消除歧义。 */}

<Stepper>
  <Step title="① 放映机喊「该画下一帧了」">
    <FrameStageDiagram stage={1} />
    requestAnimationFrame
    在屏幕即将刷新前，回头调用你登记的那个「画一帧」函数。一秒钟通常喊 60 次。
  </Step>
  <Step title="② 先清屏：擦掉上一帧">
    <FrameStageDiagram stage={2} />
    用一支颜色把整块画布刷一遍，把上一帧留下的旧画面盖掉，得到一张干净的底。
  </Step>
  <Step title="③ 再绘制：画上这一帧的新内容">
    <FrameStageDiagram stage={3} />
    在干净的底上画这一帧该有的东西（本章还没有内容，所以画完清屏就是一整块纯色）。
  </Step>
  <Step title="④ 登记下一帧，回到 ①">
    <FrameStageDiagram stage={4} />
    在函数末尾再调一次
    requestAnimationFrame，把自己重新排进下一次刷新——循环就这样一直转。
  </Step>
</Stepper>

## 清屏：每帧开头先擦干净画板

上面第 ② 步是这一章的主角。<Term def="在画新一帧之前，用一种颜色把整块画布（颜色缓冲）整体刷一遍，盖掉上一帧的内容。">清屏</Term> 干的事就是「擦黑板」：在画新内容之前，先用一种颜色把整块画布抹一遍，把上一帧的痕迹彻底盖掉。

它分两步下命令：先用 `gl.clearColor(r, g, b, a)` 告诉画笔「待会儿要拿哪种颜色来抹」（这只是定好颜料，还没动手）；再用 `gl.clear(...)` 真正动手，把那块存画面的内存——也就是 <Term def="显存里存放「当前这一帧每个像素是什么颜色」的一块内存；屏幕显示的就是它，清屏就是把它整体重置成一种颜色。">颜色缓冲</Term>——整体填成那个颜色。

为什么每帧都要清？因为画布上一帧的内容不会自己消失。不擦干净就接着画，新旧画面会叠在一起糊成残影。在原书里，这一步对应「把窗口涂成青色」——窗口里还没有任何模型，整个画面就是清屏色铺满。下面的 Demo 就让你亲手调这个清屏色。

{/* 4. 数学推导（spec §四）：math:false，整节省略，不写。 */}

{/* 5. 交互 Demo（spec §五）：A 型主 Demo 概念上对应 PipelineViz，但本章核心是「清屏色」这一可调概念，按任务用 ShaderDemo + 一个 color 控件最贴切（读者调色直接看整块画布被刷成该色 = 清屏）。猜一猜引导必有；控件 1 个 ≤5；ShaderDemo 自带重置 + 骨架屏 + WebGL2 兜底。 */}

## 动手：给整块画布选个清屏色

下面这块演示区就是一张 `<canvas>` 画布。它的片段着色器没画任何形状，只是把**你选的清屏色**铺满整块画布——这正是「清屏」的效果：整面被刷成一种颜色。

> 猜一猜：把下面的「清屏颜色」调成你喜欢的颜色，整块画布会怎样？是只变一个角，还是整面一起变？动手拖一下颜色块再看。

<ShaderDemo
  caption="这块演示区是一张 canvas 画布；它什么形状都没画，只把你选的清屏色铺满整块画面——这就是 gl.clearColor + gl.clear 做的事。拖动颜色块换色，整面会一起变。点重置回到原书那抹青色。"
  controls={[
    {
      name: "uClearColor",
      label: "清屏颜色",
      type: "color",
      default: [0.2, 0.3, 0.3],
    },
  ]}
  frag={`#version 300 es
precision highp float;
uniform vec3 uClearColor;   // 控件：清屏颜色（颜色选择器，rgb 0..1）
out vec4 FragColor;
void main() {
  // 不算任何形状：每个像素都直接输出同一种颜色 —— 等价于「整块画布被刷成清屏色」。
  FragColor = vec4(uClearColor, 1.0);
}`}
/>

默认那抹偏暗的青绿，正是原书里 `glClearColor(0.2, 0.3, 0.3, 1.0)` 那杯经典的清屏色。你每换一次颜色，相当于改了 `gl.clearColor` 的参数——整块画布下一帧就被刷成新色。

{/* 6. 代码对照（spec §六，A 型为「选」，只贴最能说明概念的 1-2 段）：单块 ≤30 行；两 Tab 同一逻辑段；块间有解说；API 差异用 Callout 标注。 */}

## 代码对照：原书的 C++ ↔ 我们的 TypeScript

原书用 C++ 配 GLFW 开一个桌面窗口、用 GLAD 加载函数指针，再开渲染循环清屏。我们在网页里不需要「开窗口」——`<canvas>` 已经在页面上了，直接从它身上拿上下文即可。先看「拿画笔」这一段：

<CodeTabs>
  <Tab label="C++ / OpenGL 3.3">

```cpp
// 创建窗口 + 拿到 OpenGL 上下文（GLFW 建窗，GLAD 加载函数指针）
GLFWwindow* window = glfwCreateWindow(800, 600, "LearnOpenGL", NULL, NULL);
glfwMakeContextCurrent(window);
gladLoadGLLoader((GLADloadproc)glfwGetProcAddress);
```

  </Tab>
  <Tab label="TypeScript / WebGL2">

```ts
// 网页里 <canvas> 已在页面上，直接从它身上拿 WebGL2 上下文（这支「画笔」）
const canvas = document.querySelector("canvas")!;
const gl = canvas.getContext("webgl2");
if (!gl) throw new Error("此浏览器不支持 WebGL2");
```

  </Tab>
</CodeTabs>

两边目的相同——拿到一个能对 GPU 下命令的上下文对象；只是桌面端要先自己开窗口、加载函数，而网页端浏览器都替你做好了。这里有个第一次就要留意的差异点：

<Callout type="warn">
  API 差异：桌面 OpenGL 要 GLFW 建窗 + GLAD 加载函数才有上下文；网页里 `<canvas>`
  是现成元素，`canvas.getContext('webgl2')` 直接返回上下文，**但它可能返回
  `null`**（浏览器太旧、被禁用），所以这一行返回值必须判空，不能想当然往下用。
</Callout>

拿到 `gl` 后，渲染循环里的「清屏」两边几乎一模一样——先定色、再清，区别只在循环靠谁驱动：

<CodeTabs>
  <Tab label="C++ / OpenGL 3.3">

```cpp
// 渲染循环：窗口没被关就一直转
while (!glfwWindowShouldClose(window)) {
  glClearColor(0.2f, 0.3f, 0.3f, 1.0f); // 定好清屏色
  glClear(GL_COLOR_BUFFER_BIT);         // 用该色刷满颜色缓冲
  glfwSwapBuffers(window);
  glfwPollEvents();
}
```

  </Tab>
  <Tab label="TypeScript / WebGL2">

```ts
// 渲染循环：requestAnimationFrame 每帧回头调一次 frame
function frame() {
  gl.clearColor(0.2, 0.3, 0.3, 1.0); // 定好清屏色
  gl.clear(gl.COLOR_BUFFER_BIT); // 用该色刷满颜色缓冲
  requestAnimationFrame(frame); // 登记下一帧
}
requestAnimationFrame(frame);
```

  </Tab>
</CodeTabs>

`clearColor` / `clear` 两步在两端完全对应。最大的不同是循环：桌面端用 `while` 自己转、末尾手动交换缓冲；网页端把「画一帧」的函数交给 `requestAnimationFrame`，由浏览器在每次刷新前回调，节奏跟着屏幕走、切到后台标签页还会自动暂停省电。

{/* 7. 常见误区（spec §七，A 型 ≥2）：固定格式 现象 → 原因 → 修法；每个 ≤150 字；至少 1 个可让读者亲眼看翻车（这里指向上面 Demo 改色即「清屏色」实验，残影坑配文字现象）。 */}

## 容易踩的坑

<Callout type="trap">
  **现象**：画面里物体移动后拖出一条条残影，越积越糊。**原因**：每帧绘制前忘了
  `gl.clear`，上一帧的画面没被擦掉，新一帧直接叠在旧的上面。**修法**：在每帧绘制的最
  开头先 `gl.clearColor(...)` 定色、再 `gl.clear(gl.COLOR_BUFFER_BIT)`
  擦干净，然后才画 新内容。
</Callout>

<Callout type="trap">
  **现象**：页面报错「Cannot read properties of null」，或画布一片空白什么都没有。**原因**：
  `canvas.getContext('webgl2')` 返回了 `null`（浏览器太旧或 WebGL2 被禁用），代码却没判空
  就拿它去调命令。**修法**：拿到上下文后先 `if (!gl) {...}` 判空，给出兜底提示，确认
  非空再往下画。
</Callout>

<Callout type="trap">
  **现象**：只调了 `gl.clearColor`
  换了颜色值，画面却纹丝不动。**原因**：`clearColor`
  只是「定好待会儿用哪种颜料」，并没有真正动手刷；不调 `gl.clear`
  就不会有任何变化。 **修法**：记住两步配套——先 `clearColor` 定色，再 `clear`
  执行，缺一不可。
</Callout>

{/* 8. 小结与练习（spec §八）：小结 ≤5 条且与学习目标一一呼应；练习 2-3 题含 1 道改 Demo 题，答案折叠。 */}

## 小结

- 屏幕动画 = 一帧接一帧飞快重画，像电影放胶片
- `<canvas>` 是钉在网页上的画板，规定「往哪画、多大」
- 从 canvas 拿到的 **WebGL2 上下文**（`gl`）是连通 GPU 的画笔，所有命令都对它下
- **渲染循环**用 `requestAnimationFrame` 驱动，每帧重画，让画面动起来
- 每帧开头先**清屏**（`clearColor` 定色 + `clear` 执行），擦掉上一帧，否则会糊成残影

<Exercises>

**练习 1（改 Demo 代码型）** 把上面 Demo 的清屏色控件默认值，从原书的青色 `[0.2, 0.3, 0.3]` 改成你喜欢的颜色（比如纯紫 `[0.49, 0.36, 1.0]`）。说说改的是哪个 OpenGL 函数的参数。

<Answer>

把 `controls` 里那条的 `default` 改成你想要的 rgb（0..1）即可，例如：

```ts
{ name: "uClearColor", label: "清屏颜色", type: "color", default: [0.49, 0.36, 1.0] }
```

改的是 **`gl.clearColor(r, g, b, a)`** 的前三个参数——它定下「清屏时用哪种颜色」。Demo 里这个 rgb 被当成清屏色铺满整块画布，等价于每帧 `gl.clearColor(0.49, 0.36, 1.0, 1.0)` 后再 `gl.clear`。

</Answer>

**练习 2（问答型）** 为什么渲染循环里要「每一帧」都清屏，而不是程序启动时清一次就够了？

<Answer>

因为画布上一帧画的内容不会自己消失。如果只在开头清一次，之后每帧画的新内容都会叠在旧画面上，物体一移动就拖出残影。每帧开头先清一次，等于先把画板擦回干净的底，再画这一帧的新内容，画面才干净、才正确地「动」起来。

</Answer>

**练习 3（问答型）** 用一句话说出 `<canvas>`、WebGL2 上下文、渲染循环三者在「从数据到屏幕」的管线里各自的位置。

<Answer>

`<canvas>` 在最前端，是「画到哪」的那块板子；WebGL2 上下文紧跟其后，是从板子上拿到的「用什么笔」、所有命令都经它下达给 GPU；渲染循环在最外层，每一帧把「清屏 → 绘制」整套流程从头驱动一遍。

</Answer>

</Exercises>

{/* 10. 名词解释（spec §十）：与正文 <Term> 高亮一一对应（5 个：画布 / WebGL2 上下文 / 渲染循环 / 帧 / 清屏 / 颜色缓冲）；释义零基础大白话。位置章末、出处声明之前。 */}

<Glossary>

<GlossaryItem term="画布（canvas）">
  网页上一个叫 `<canvas>` 的方块元素，就是一块空白画板，规定了「能画的范围有多大」。光有它还画不出东西，得再配一支「画笔」（上下文）。详见本章「画布」一节。
</GlossaryItem>

<GlossaryItem term="WebGL2 上下文">
  从画布身上拿到的一个对象（一般命名
  `gl`），是连通显卡（GPU）的「总开关」兼「画笔」。清屏、上传数据、画三角形等所有命令，都是对它下达、再由它转交
  GPU 执行。
</GlossaryItem>

<GlossaryItem term="渲染循环">
  一段「画一帧 → 等屏幕下次刷新 → 再画一帧」不停重复的代码，通常用
  `requestAnimationFrame`
  驱动。它是网页里的「放映机」，让画面一帧帧动起来。详见本章「渲染循环」一节。
</GlossaryItem>

<GlossaryItem term="帧">
  一整张完成的画面。屏幕上的动画，本质是一帧接一帧飞快地播放（一秒通常几十帧），快到眼睛看成了连续的动作，就像电影放胶片。
</GlossaryItem>

<GlossaryItem term="清屏">
  在画新一帧之前，用一种颜色把整块画布整体刷一遍，盖掉上一帧留下的内容。两步配套：先
  `gl.clearColor` 定好用哪种颜色，再 `gl.clear`
  真正动手刷。详见本章「清屏」一节。
</GlossaryItem>

<GlossaryItem term="颜色缓冲">
  显存里存放「当前这一帧每个像素是什么颜色」的一块内存。屏幕显示的就是它的内容；清屏做的事，就是把这块内存整体重置成一种颜色。
</GlossaryItem>

</Glossary>

{/* 9. 出处声明（spec §九）：Attribution 由 page.tsx 闭包注入本章 frontmatter.sourceUrl，写空标签即可（硬规则 4）。 */}

<Attribution />
