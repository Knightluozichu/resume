---
title: 颜色
type: A
section: 光照
order: 1
description: 搞懂「我们看到的物体颜色，其实是光的颜色乘上物体反射的颜色」这条光照的第一性原理——为什么白光下物体显本色，有色光下却会变样。
demo: true
math: false
sourceUrl: https://learnopengl-cn.github.io/02%20Lighting/01%20Colors/
draft: false
---

{/* A 概念型章（HEL-50，M5 光照篇第 1 篇，新建 lighting/ section 目录）：光照篇开篇，核心是建立「物体颜色 = 光的颜色 × 物体反射的颜色（逐分量相乘）」这条认知——人眼看到的是物体「没吸收、反射回来」的那部分光。承接入门篇（已能画三角形、写着色器、调 uniform），本章不算真实光照（漫反射/镜面留给后续 Phong），只把「颜色从哪来」这条第一性原理讲透，为整篇光照打地基。A 型：概念讲解为重心（spec §3 重），math:false 整节省略（§4 省），代码对照选讲 1 段（§6 选），常见误区 ≥2（§7）。主 viz = editable ShaderDemo：把「物体色 × 光色 = 反射所见」做成可拖两个颜色控件的实时画布，左中右三栏直观对照。draft:true，老板验收后才发布（硬规则 7）。 */}

{/* 知识点清单（动笔前先摊开，每条标 viz；难啃的用图掰碎；全部配 viz，无纯文字一笔带过；Term 首现高亮 + 章末 Glossary 一一对应）：1) 物体颜色不是物体「自带」的，而是反射光的结果 = 直觉引入比喻（挑食的反射器）+ 概念讲解；2) 反射 = 物体吸收一部分光、把没吸收的反射回来，人眼接住的就是反射光 = 概念 + 主 ShaderDemo（光色×物色三栏对照）；3) 反射颜色 = 光色 × 物色，逐分量相乘 = 概念（文字说清，math:false 不展开公式）+ 主 ShaderDemo + 代码对照；4) 白光显本色：白光各分量=1，乘任何物色都不变 = 概念 + 主 Demo（光色拉到白）；5) 有色光偏色：光色某分量=0，物色对应分量被「掐灭」= 概念 + 主 Demo（猜一猜：珊瑚色打绿光）+ 误区；6) 这条认知在光照模型中的位置：后续 Phong 把「光色」拆成环境/漫反射/镜面三块算出来，物色不变 = 概念（引出后续章）。Term 共 4 个：物体颜色（反射模型）/ 光源颜色 / 逐分量相乘 / 光照模型。 */}

{/* 1. 学习目标（spec §一）：2-3 条可检验动词；A 型必含「能解释 ___ 在光照模型/管线中的位置」一条；末条为可当场自测的检验问题。 */}

<Objectives>

- 能用自己的话解释「我们看到的物体颜色」到底是什么——它是物体自带的，还是光给的？
- 能解释「光的颜色 × 物体颜色」这条规则在整个光照模型里处在什么位置：它是后续所有光照计算的起点
- 能回答：一个珊瑚红的物体，拿一束纯绿光去照它，看上去会偏成什么颜色？为什么？

</Objectives>

{/* 2. 直觉引入（spec §二）：≤3 段、每段 ≤120 字、零术语（专业名词最早第 3 节）、回答「解决什么问题 / 没有它会怎样」、类比固定用「光与材质」库（光照/PBR 类）。 */}

## 你看到的「红苹果」，红是谁给的？

随手拿起一个红苹果，你会理所当然地觉得「它就是红的」。可把它端进伸手不见五指的暗房，红色去哪了？灯一关，苹果就成了一团黑。可见**颜色不是苹果自己揣在兜里的东西**——没有光，什么颜色都谈不上。

换个想法：苹果更像一个**挑食的反射器**。照过来的光里混着各种颜色，苹果把不合胃口的那些「吃掉」（吸收），只把红色那一份**弹回来**。弹回你眼睛里的，才是你看到的颜色。所以你看到的红，其实是「光送来的红，被苹果留下并反射回来」的红。

这一章要解决的就是这件事：在屏幕上画一个物体时，它最终显什么色，到底由谁说了算？想不清这条，后面整篇光照——影子、高光、明暗——全是空中楼阁；想清了，它就是一切的地基。

{/* 3. 概念讲解（spec §三，A 型重心）：一节一概念；术语首现 <Term def> 高亮且进章末 Glossary；每概念配图或引用第 5 节 Demo；顺序 是什么 → 为什么 → 在光照模型中的位置。新核心概念克制（物体颜色/光源颜色/逐分量相乘/光照模型，≤5）。 */}

## 物体颜色：反射回来的那部分光

先把直觉变精确。在图形学里，一个物体的「颜色」指的不是它发光，而是它的<Term def="物体的颜色不是它自身发出的，而是它「反射」回来的那部分光的颜色。白光照过来，物体吸收掉一部分颜色、反射剩下的，反射回你眼睛的就是你看到的色。所以同一个物体在不同颜色的光下会显出不同的样子。">物体颜色（反射特性）</Term>——它面对一束光时，会**反射哪些颜色、吸收哪些颜色**。我们说「这个物体是珊瑚红」，真正的意思是：照白光时，它主要把红色、加一点绿和蓝反射出来，其余吃掉。

为什么要这么绕？因为这正贴合现实：同一个物体，换一种光照，看上去就变样了。白炽灯下暖、冷白灯下冷、舞台红灯下整个发红——物体本身没变，**反射规则没变，变的是照过来的光**。把「颜色 = 反射」这件事认下来，才解释得了这些日常现象。

## 光源颜色：照过来的那束光是什么色

另一半是光本身。<Term def="照射到物体上的那束光的颜色。比如太阳光/白炽灯近似白光（红绿蓝都强），舞台灯可能是纯红、纯绿等有色光。光源颜色决定了「有哪些颜色的光可供物体反射」。">光源颜色</Term>就是照过来那束光的颜色：太阳光、白炽灯近似**白光**（红、绿、蓝三种成分都很足）；舞台上的红灯、绿灯则是**有色光**（只有某一两种成分强，别的几乎没有）。

关键点在这：**物体只能反射「光里本来就有的」颜色**。光里没有的成分，物体再想反射也无米下锅。一个红苹果在纯绿光下——绿光里压根没有红色成分可供它反射，红就弹不回来了，于是它看上去发暗、发黑。这就埋下了下一节那条核心规则。

## 核心规则：反射所见 = 光色 × 物色

把两半合起来，就得到本章最重要的一句话：

> **你最终看到的颜色 = 光源颜色 × 物体颜色**，而且是**红配红、绿配绿、蓝配蓝**地分开相乘。

这种「对应分量各自相乘」的算法叫<Term def="把两个颜色（各有红/绿/蓝三个数）的对应分量分别相乘：红×红、绿×绿、蓝×蓝，得到一个新颜色。不是把六个数加在一起，而是三组各算各的。在 GLSL 里对两个 vec3 直接写 * 就是这个意思。">逐分量相乘</Term>：颜色都用 `(红, 绿, 蓝)` 三个 0~1 的数表示，相乘时红跟红乘、绿跟绿乘、蓝跟蓝乘，互不串门。直觉上它就是在问每个通道：「这种颜色的光（光色），物体愿意反射回多少比例（物色）？」两个一乘，就是真正弹回你眼里的量。

为什么这条规则在整个<Term def="一套用来算「物体表面最终显什么颜色」的计算流程。本章是它最朴素的起点（只有光色×物色）；后续会逐步加入环境光、漫反射、镜面高光等项，让明暗和光泽更真实，但「光色×物色」这个内核始终在最里层。">光照模型</Term>里是地基？因为后面要学的环境光、漫反射、镜面高光，本质都是在**更精细地算出「这块表面到底被多少光照到、是什么色的光」**——也就是把上面那个「光源颜色」算得更讲究；而「乘上物体颜色得到反射所见」这一步，自始至终都在最里层。先把这条朴素规则攥牢，后面只是往里填料。下面的 Demo 就让你亲手乘一乘。

{/* 4. 数学推导（spec §四）：math:false，整节省略，不写。「逐分量相乘」用第 3 节文字 + 第 5 节三栏 Demo 的直觉代替，不上公式，符合 A 型 + 小白友好。 */}

{/* 5. 交互 Demo（spec §五）：A 型主 Demo 用 editable ShaderDemo（无现成 PipelineViz 适配本主题，按任务卡用 ShaderDemo 做「光色×物色」核心交互）；猜一猜引导；控件 2 个颜色选择器（≤5）；自带重置 + 骨架屏 + WebGL2 兜底。frag 用 `{`...`}` 模板字符串表达式传（blockJS:false 生效）。画布左中右三栏：物体本色 | 光色 | 反射结果，让乘法可见。 */}

## 动手：拖两个颜色，看它们相乘

下面是本章的主角。画布分三栏：左边是**物体本色**（假设白光下它长这样）、中间是**照过来的光色**、右边是两者**逐分量相乘**得到的**反射所见**——也就是你真正会看到的颜色。右侧两个颜色控件分别驱动「物体色」和「光色」这两个 uniform，拖一下，右栏立刻跟着变。

初始就是经典例子：珊瑚红的物体（`(1.0, 0.5, 0.31)`）被白光（`(1.0, 1.0, 1.0)`）照——白光各分量都是 1，乘下来物体原封不动，所以右栏 = 左栏，物体显出本色。

> 猜一猜：把中间的「光色」拖成**纯绿**（红、蓝都拉到 0，只留绿），这个珊瑚红物体的「反射所见」会偏成什么色？是更红、更绿，还是发暗发黑？先在脑子里算一下红配红、绿配绿、蓝配蓝，再拖控件验证。

<ShaderDemo
  editable
  aspect={2.4}
  caption="左：物体本色（白光下的样子）｜中：照过来的光色｜右：两者逐分量相乘的反射所见。拖右侧两个颜色控件即在改 uObjectColor / uLightColor 两个 uniform，右栏实时重算。试试把光色拖成纯绿，看珊瑚红物体怎么变暗；或把光色拉回纯白，物体就显本色。也可直接改 GLSL：把 reflected 那行的 `*` 换成 `+`（相加），看看『加法』为什么不对。"
  frag={`#version 300 es
precision highp float;          // WebGL2 片段着色器必写：声明浮点精度
uniform vec2 uResolution;       // 画布像素尺寸（标准 uniform，自动注入）
uniform vec3 uObjectColor;      // 控件：物体色（白光下的本色，rgb 0..1）
uniform vec3 uLightColor;       // 控件：光源颜色（rgb 0..1）
out vec4 FragColor;             // 内建输出：这个片段的最终颜色
void main() {
  // 把画布横向分成左/中/右三栏（各占 1/3）
  float x = gl_FragCoord.x / uResolution.x;
  // 本章核心：反射所见 = 光色 × 物色（逐分量相乘，红×红 绿×绿 蓝×蓝）
  vec3 reflected = uLightColor * uObjectColor;
  vec3 col;
  if (x < 0.3333)      col = uObjectColor;   // 左：物体本色
  else if (x < 0.6667) col = uLightColor;    // 中：照过来的光色
  else                 col = reflected;       // 右：反射所见 = 你真正看到的
  FragColor = vec4(col, 1.0);
}`}
  controls={[
    {
      name: "uObjectColor",
      label: "物体色",
      type: "color",
      default: [1.0, 0.5, 0.31],
    },
    {
      name: "uLightColor",
      label: "光色",
      type: "color",
      default: [1.0, 1.0, 1.0],
    },
  ]}
/>

谜底：纯绿光是 `(0, 1, 0)`，珊瑚红物体是 `(1.0, 0.5, 0.31)`，逐分量相乘 = `(0×1.0, 1×0.5, 0×0.31)` = `(0, 0.5, 0)`——红、蓝两个通道被绿光「掐灭」（光里没有这两种成分，物体无从反射），只剩半亮的绿。所以珊瑚红物体在绿光下会显成一抹**发暗的绿**。把光色拉回纯白 `(1, 1, 1)`，乘下来又恢复成物体本色——这正是「白光显本色」的由来。

{/* 6. 代码对照（spec §六，A 型选讲 1-2 段，只贴最能说明概念的）：单块 ≤30 行；两 Tab 同一逻辑段（FragColor = vec4(lightColor * objectColor, 1.0)）；块间有解说；API 差异（#version / precision）用 Callout 标注。 */}

## 代码：一行就是全部规则

把上面 Demo 的核心摊开看，本章的全部规则其实就浓缩在片段着色器的**一行乘法**里。物体色和光色各是一个 uniform（你的程序用 `setVec3` / `gl.uniform3f` 传进去，做法和上一篇「着色器」章一样），相乘后输出：

<CodeTabs>
  <Tab label="C++ / OpenGL 3.3">

```cpp
// 片段着色器
#version 330 core
out vec4 FragColor;

uniform vec3 objectColor;   // 物体色（白光下的本色）
uniform vec3 lightColor;    // 光源颜色

void main() {
  // 逐分量相乘：红×红、绿×绿、蓝×蓝 —— 这一行就是本章的核心
  FragColor = vec4(lightColor * objectColor, 1.0);
}
```

  </Tab>
  <Tab label="TypeScript / WebGL2">

```ts
const fragmentSrc = `#version 300 es
precision highp float;       // WebGL2 片段着色器必写：声明浮点精度
out vec4 FragColor;

uniform vec3 objectColor;    // 物体色（白光下的本色）
uniform vec3 lightColor;     // 光源颜色

void main() {
  // 逐分量相乘：红×红、绿×绿、蓝×蓝 —— 这一行就是本章的核心
  FragColor = vec4(lightColor * objectColor, 1.0);
}`;
```

  </Tab>
</CodeTabs>

GLSL 里两个 `vec3` 直接写 `*`，做的就是逐分量相乘——`lightColor * objectColor` 一行，红配红、绿配绿、蓝配蓝全办妥，不用你手写三遍。两端的 GLSL 源码完全一样，唯一差异在那行版本声明和精度声明：

<Callout type="warn">
  API 差异：桌面 OpenGL 写 `#version 330 core`，WebGL2 写 `#version 300
  es`；并且 WebGL2 的**片段着色器必须**多写一行 `precision highp
  float;`，否则编译直接失败，桌面 OpenGL
  不需要这行。这点上一篇「着色器」章已经踩过，这里同样适用。
</Callout>

至于怎么把 `objectColor` / `lightColor` 的值从程序里传进这两个 uniform（`getUniformLocation` 问位置、`uniform3f` 写值、先 `useProgram`），和上一篇「着色器」章一模一样，这里不再重复。本章要你记住的，就是 `main` 里那一行乘法。

{/* 7. 常见误区（spec §七）：A 型 ≥2 个；固定格式 现象 到 原因 到 修法；含 1 个可在 §5 editable Demo 亲手触发的错误模式（把相乘改成相加）。 */}

## 容易踩的坑

<Callout type="trap">
  **现象**：以为「物体是什么色就永远显什么色」，于是纳闷为什么物体在有色光下「变色」了，怀疑代码写错。**原因**：把颜色当成了物体自带的固定属性，忘了你看到的是**反射光**——光色变了，能被反射的成分就变了，物体自然跟着变样。**修法**：记住「看到的
  = 光色 × 物色」；物体在纯绿光下发暗发绿不是 bug，正是这条规则的正确结果。
</Callout>

<Callout type="trap">
  **现象**：把光照写成 `lightColor +
  objectColor`（相加），结果颜色全偏白、越照越亮，怎么调都不对。**原因**：颜色混合不是相加——相加是「两束光叠在一起更亮」（那是叠加光源的事），而「物体反射光」是**相乘**（物体最多反射
  100%，只会变暗不会凭空变亮）。**修法**：物体反射一律用逐分量相乘 `lightColor *
  objectColor`。**亲手看翻车**：到上面那个可编辑 Demo 里，把 `uLightColor *
  uObjectColor` 改成 `uLightColor +
  uObjectColor`，停手后看右栏——颜色立刻泛白发飘，这就是这个坑的真身；改回 `*`
  即恢复。
</Callout>

<Callout type="trap">
  **现象**：物体在「白光」下却不显本色，偏色或过曝。**原因**：把白光的某个分量设成了大于
  1 的值（如 `(2, 2, 2)`），或根本不是 `(1, 1, 1)`——只有**正白光 `(1, 1, 1)`**
  乘下来才恰好保持物体本色。**修法**：要「显本色」就让光色 = `(1, 1,
  1)`；想整体调亮调暗可以整体缩放光色（如 `(0.5, 0.5, 0.5)`
  变暗），但别让单个分量畸高导致偏色。
</Callout>

{/* 8. 小结与练习（spec §八）：小结 ≤5 条且与学习目标一一呼应；练习含 1 道改 Demo 题（含目标效果描述）+ 问答题；答案折叠。 */}

## 小结

- 物体颜色不是物体自带的，而是它**反射回来**的那部分光——没有光就没有颜色
- 光源颜色决定「有哪些颜色的光可供反射」；物体只能反射光里本来就有的成分
- 核心规则：**反射所见 = 光色 × 物色**，红配红、绿配绿、蓝配蓝**逐分量相乘**
- 白光 `(1,1,1)` 乘下来不改变物体，所以显本色；有色光会掐灭某些分量，物体就偏色
- 这条「光色 × 物色」是整个**光照模型**的最里层地基，后续只是把「光色」算得越来越讲究

<Exercises>

**练习 1（问答型）** 一个纯蓝物体 `(0, 0, 1)`，分别在白光 `(1, 1, 1)` 和纯红光 `(1, 0, 0)` 下，看上去各是什么色？为什么？

<Answer>

- 白光下：`(1, 0, 0)`… 不对，逐分量乘是 `(1×0, 1×0, 1×1) = (0, 0, 1)`——还是纯蓝，显本色（白光各分量为 1，乘下来不变）。
- 纯红光下：`(1×0, 0×0, 0×1) = (0, 0, 0)`——纯黑！红光里没有蓝色成分，蓝物体无蓝可反射，于是几乎不反光，看上去是黑的。这就是「物体只能反射光里本来就有的颜色」最极端的体现。

</Answer>

**练习 2（改 Demo 代码型）** 在上面的可编辑 Demo 里，把画布改成只显示**一整块「反射所见」**（去掉左中两栏的物体色/光色对照），整块都是 `光色 × 物色` 的结果。提示：删掉分栏的 `if`，让 `col` 直接等于 `reflected`。

<Answer>

把 `main` 里分栏那几行删掉，改成：

```glsl
void main() {
  vec3 reflected = uLightColor * uObjectColor;  // 光色 × 物色
  FragColor = vec4(reflected, 1.0);             // 整块都显反射所见
}
```

不再按 `gl_FragCoord.x` 分栏，每个片段都输出同一个 `reflected`，于是整块画布是一种纯色——就是这个物体在当前光色下真正会被你看到的颜色。拖两个控件，整块一起变。

</Answer>

**练习 3（问答型）** 后面要学的 Phong 光照里有「环境光、漫反射、镜面高光」三项。按本章的认知，这三项算的到底是「光色」那一半，还是「物色」那一半？

<Answer>

主要是在**更讲究地算「光色」那一半**——也就是「这块表面到底被多大强度、什么方向、什么颜色的光照到」。环境光是「四面八方都有的底光」、漫反射是「正对光源的面更亮」、镜面是「特定角度的高光」，它们叠起来得到一个更真实的「到达这块表面的光」。而「乘上物体颜色得到反射所见」这一步（本章的乘法）始终在最里层不变。这就是为什么本章是整篇光照的地基。

</Answer>

</Exercises>

{/* 10. 名词解释（spec §十）：与正文 <Term> 高亮一一对应（4 个，按首现顺序）；释义零基础大白话。位置章末、出处声明之前。 */}

<Glossary>

<GlossaryItem term="物体颜色（反射特性）">
  物体的颜色不是它自己发出来的，而是它**反射**回来的那部分光的颜色。一束光照过来，物体吸收掉一部分颜色、把没吸收的反射回去，反射进你眼睛的就是你看到的色。所以同一个物体在不同颜色的光下会显得不一样。详见本章「物体颜色」一节。
</GlossaryItem>

<GlossaryItem term="光源颜色">
  照到物体上的那束光本身的颜色。太阳光、白炽灯接近**白光**（红绿蓝成分都很足），舞台红灯/绿灯则是**有色光**（只有某一两种成分）。光源颜色决定了「有哪些颜色的光可供物体反射」——光里没有的颜色，物体再想反射也没有。详见本章「光源颜色」一节。
</GlossaryItem>

<GlossaryItem term="逐分量相乘">
  把两个颜色（各有红、绿、蓝三个数）的对应分量分别相乘：红×红、绿×绿、蓝×蓝，得到一个新颜色——不是把六个数加在一起，而是三组各算各的。在
  GLSL 里对两个 `vec3` 直接写 `*` 就是这个意思。本章「光色 ×
  物色」用的就是它。详见本章「核心规则」一节。
</GlossaryItem>

<GlossaryItem term="光照模型">
  一套用来算「物体表面最终显什么颜色」的计算流程。本章是它最朴素的起点（只有「光色
  ×
  物色」）；后续会逐步加入环境光、漫反射、镜面高光等项，让明暗和光泽更真实，但「光色
  × 物色」这个内核始终在最里层。详见本章「核心规则」一节。
</GlossaryItem>

</Glossary>

{/* 9. 出处声明（spec §九）：Attribution 由 page.tsx 闭包注入本章 frontmatter.sourceUrl，写空标签即可（硬规则 4）。 */}

<Attribution />
