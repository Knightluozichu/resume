---
title: "第2章 有限维向量空间"
type: C
section: "线性代数应该这样学4e · 向量空间"
order: 2
description: "用张成、线性无关、基和维数刻画有限维空间，并掌握基扩充与长度比较。"
demo: true
math: true
sourceUrl: "https://linear.axler.net/LADR4e.pdf"
draft: true
---

import { Objectives, Callout, Attribution, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step } from "@/components/mdx/mdx-components";
import { Lad4FiniteDimensionalVectorSpacesStructureLab, Lad4FiniteDimensionalVectorSpacesProofLab, Lad4FiniteDimensionalVectorSpacesEvidenceLab } from "@/components/mdx/linear-algebra-done-right/diagrams/lad4-finite-dimensional-vector-spaces";

<Objectives>
- 能解释张成与线性无关的定义、对象和假设
- 能推导基到维数的证明主链并指出关键引理
- 能实现基扩充的最小例子，用反例和残差验收结论
</Objectives>

## 先做预测

先预测：如果删去有限维、复数域、正交性或非零条件，哪条结论会最先失败？用张成、线性无关、基和维数刻画有限维空间，并掌握基扩充与长度比较。 本页围绕<Term def="一个向量列表所有线性组合构成的集合，是包含该列表的最小子空间。">张成</Term>、<Term def="只有全零系数才能给出零向量的线性组合，意味着表示没有冗余。">线性无关</Term>、<Term def="既线性无关又张成空间的向量列表，使每个向量拥有唯一坐标。">基</Term>、<Term def="有限维空间任意基的长度相同，这个长度是空间的不变量。">维数</Term>和<Term def="线性无关列表可扩充为基，张成列表可删减为基。">基扩充</Term>建立定义、定理、证明、反例和计算证据。

<Lad4FiniteDimensionalVectorSpacesStructureLab />

## 第四版权威目录定位

本页一一对应Sheldon Axler《Linear Algebra Done Right》第四版“第2章 有限维向量空间”。第四版是Springer 2024开放获取版；站内保留原书9章顺序，并把每章的正式小节压缩为可追踪的学习单元，而不沿用第三版旧目录。

### 原书小节覆盖

1. 2A Linear Combinations and Span
2. Linear Independence
3. 2B Bases
4. Basis Reduction and Extension
5. 2C Dimension
6. Length of Linearly Independent and Spanning Lists

### 核心定义

1. **张成**：一个向量列表所有线性组合构成的集合，是包含该列表的最小子空间。
2. **线性无关**：只有全零系数才能给出零向量的线性组合，意味着表示没有冗余。
3. **基**：既线性无关又张成空间的向量列表，使每个向量拥有唯一坐标。
4. **维数**：有限维空间任意基的长度相同，这个长度是空间的不变量。
5. **基扩充**：线性无关列表可扩充为基，张成列表可删减为基。

## 先声明假设

证明前先写标量域、定义域、陪域、维数以及是否给定内积。许多错误并非推导失误，而是把实数域结论搬到复数域、把有限维结论搬到无限维，或把一般算子当作正规算子。

$$
\mathcal A=(\mathbb F, V, W, \dim V, \text{inner product if any})
$$

张成与线性无关必须放在同一组假设下比较。定义中的“任意”“存在”“唯一”和“非零”都影响证明方向，不能在转述时省略。

## 定理与可核查推导

$$
\operatorname{span}(v_1,\ldots,v_m)=V,\quad (u_1,\ldots,u_n)\ \text{independent}\Longrightarrow n\le m
$$

用线性相关引理逐个把张成列表中的向量替换为独立列表中的向量，同时保持张成不变。每次替换消耗一个原列表位置，因此独立列表长度不能超过任一张成列表长度。 证明验收要求逐步标注定义、已有定理和新构造，不能用待证结论的等价形式循环论证。

$$
\text{assumptions}\Longrightarrow\text{lemma}\Longrightarrow\text{invariant}\Longrightarrow\text{claim}
$$

```python
def pivot_columns(rows):
    a=[list(map(float,row)) for row in rows]; pivots=[]; r=0
    for c in range(len(a[0])):
        p=next((i for i in range(r,len(a)) if abs(a[i][c])>1e-9),None)
        if p is None: continue
        a[r],a[p]=a[p],a[r]; pivots.append(c); r+=1
    return pivots

assert pivot_columns([[1,0,1],[0,1,1]]) == [0,1]
```

代码不是证明，但能暴露维数、顺序、共轭和边界错误。至少使用一个非对角、非对称或不满秩样例；单位矩阵与零算子只能作为冒烟测试。

## 结构图与证明骨架

**张成**：一个向量列表所有线性组合构成的集合，是包含该列表的最小子空间。

**线性无关**：只有全零系数才能给出零向量的线性组合，意味着表示没有冗余。

**基**：既线性无关又张成空间的向量列表，使每个向量拥有唯一坐标。

先从定义构造对象，再找到保持不变的子空间、维数或多项式，最后把局部结论拼成全空间结论。若构造依赖选择基，要继续说明最终结论为何与基无关。

<Lad4FiniteDimensionalVectorSpacesProofLab />

## 反例与边界

**维数**：有限维空间任意基的长度相同，这个长度是空间的不变量。

**基扩充**：线性无关列表可扩充为基，张成列表可删减为基。

边界集至少覆盖零空间、零映射、重复特征值、线性相关列表、实数与复数域切换、缺少内积和接近秩亏。抽象证明用反例检查必要性，数值实现用残差和条件尺度检查稳定性。

$$
r=\|\text{left side}-\text{right side}\|,\qquad r\le\tau_{abs}+\tau_{rel}\|\text{reference}\|
$$

```python
audit={"assumptions":"explicit","normal":"nontrivial example","counterexample":"把张成空间的生成列表当成基，未检查其中是否存在冗余向量。","replay":"fixed basis and tolerance"}
assert set(audit)=={"assumptions","normal","counterexample","replay"}
```

## 工程与学习案例

数据特征列是否冗余可转化为线性无关问题；选基不是寻找唯一的一组向量，而是在保持张成的同时删除冗余，并用秩或重构残差验收。 交付物同时保存定义域和陪域、基、矩阵或算子表示、关键中间子空间、残差、反例以及结论适用范围。

<Lad4FiniteDimensionalVectorSpacesEvidenceLab />

```python
evidence=[("张成","definition"),("基","proof"),("基扩充","counterexample")]
assert len({name for name,_ in evidence})==3
```

## 分步证明实验

<Stepper>
  <Step title="固定定义"><Lad4FiniteDimensionalVectorSpacesStructureLab /></Step>
  <Step title="展开证明"><Lad4FiniteDimensionalVectorSpacesProofLab /></Step>
  <Step title="反例验收"><Lad4FiniteDimensionalVectorSpacesEvidenceLab /></Step>
</Stepper>

1. 原样写出定义中的量词、域、空间和非零条件。
2. 画出子空间、映射或分解关系，并标记要保持的不变量。
3. 用最小非平凡例子逐步重算，再删除一个假设寻找反例。
4. 对数值版本保存基、容差、残差和重构误差，保证可重复。

## 常见误区

<Callout type="trap" title="假设被静默删除">把张成空间的生成列表当成基，未检查其中是否存在冗余向量。 修复时回到定义与定理原句，逐项勾选假设。</Callout>

<Callout type="trap" title="坐标替代结构">认为维数依赖所选坐标系，忽略任意两组基长度相同。 应区分抽象对象、所选基下表示和换基不变量。</Callout>

## 本章回顾

本章覆盖张成、线性无关、基、维数、基扩充。掌握标准不是背结论，而是能声明假设、重建证明骨架、给出删除假设后的反例，并以具体矩阵或代码检查表示与不变量。

<Exercises>
**问题1：张成与线性无关怎样连接？**
<Answer>一个向量列表所有线性组合构成的集合，是包含该列表的最小子空间。 只有全零系数才能给出零向量的线性组合，意味着表示没有冗余。 回答时保留全部量词和空间假设。</Answer>

**问题2：基的证明关键是什么？**
<Answer>用线性相关引理逐个把张成列表中的向量替换为独立列表中的向量，同时保持张成不变。每次替换消耗一个原列表位置，因此独立列表长度不能超过任一张成列表长度。 应明确指出构造、引理与不变量。</Answer>

**问题3：怎样验收基扩充？**
<Answer>线性无关列表可扩充为基，张成列表可删减为基。 使用正常例、边界例、删除假设的反例和数值残差交叉检查。</Answer>
</Exercises>

## 术语表

<Glossary>
  <GlossaryItem term="张成" definition="一个向量列表所有线性组合构成的集合，是包含该列表的最小子空间。" />
  <GlossaryItem term="线性无关" definition="只有全零系数才能给出零向量的线性组合，意味着表示没有冗余。" />
  <GlossaryItem term="基" definition="既线性无关又张成空间的向量列表，使每个向量拥有唯一坐标。" />
  <GlossaryItem term="维数" definition="有限维空间任意基的长度相同，这个长度是空间的不变量。" />
  <GlossaryItem term="基扩充" definition="线性无关列表可扩充为基，张成列表可删减为基。" />
  <GlossaryItem term="证明证书" definition="记录假设、构造、引理、不变量、反例和数值残差的可复查链条。" />
</Glossary>

<Attribution adaptedFrom="Sheldon Axler · Linear Algebra Done Right, Fourth Edition · 第2章 有限维向量空间" adaptedUrl="https://linear.axler.net/LADR4e.pdf" />
