---
title: "第6章 内积空间"
type: C
section: "线性代数应该这样学4e · 谱与内积"
order: 6
description: "用内积、范数、正交基、Gram-Schmidt、正交补、最小化与伪逆恢复几何。"
demo: true
math: true
sourceUrl: "https://linear.axler.net/LADR4e.pdf"
draft: false
---


## 先做预测

先预测：如果删去有限维、复数域、正交性或非零条件，哪条结论会最先失败？用内积、范数、正交基、Gram-Schmidt、正交补、最小化与伪逆恢复几何。 本页围绕、、、和建立定义、定理、证明、反例和计算证据。


## 第四版权威目录定位

本页一一对应Sheldon Axler《Linear Algebra Done Right》第四版“第6章 内积空间”。第四版是Springer 2024开放获取版；站内保留原书9章顺序，并把每章的正式小节压缩为可追踪的学习单元，而不沿用第三版旧目录。

### 原书小节覆盖

1. 6A Inner Products and Norms
2. Cauchy-Schwarz and Triangle Inequalities
3. 6B Orthonormal Lists and Gram-Schmidt
4. Linear Functionals on Inner Product Spaces
5. 6C Orthogonal Complements
6. Minimization Problems
7. Pseudoinverse

### 核心定义

1. **内积**：满足共轭对称、线性和正定的标量函数，用于定义长度与角度。
2. **正交规范基**：基向量两两正交且长度为一，坐标直接由内积取得。
3. **Gram-Schmidt**：按顺序减去已有正交方向上的投影，把独立列表转成正交规范列表。
4. **正交补**：与子空间中每个向量都正交的向量集合，并给出V等于U直和U正交补。
5. **伪逆**：把目标投影到range T后求最小范数原像，统一最小二乘与欠定问题。

## 先声明假设

证明前先写标量域、定义域、陪域、维数以及是否给定内积。许多错误并非推导失误，而是把实数域结论搬到复数域、把有限维结论搬到无限维，或把一般算子当作正规算子。

$$
\mathcal A=(\mathbb F, V, W, \dim V, \text{inner product if any})
$$

内积与正交规范基必须放在同一组假设下比较。定义中的“任意”“存在”“唯一”和“非零”都影响证明方向，不能在转述时省略。

## 定理与可核查推导

$$
P_Uv=\sum_{k=1}^{m}\langle v,e_k\rangle e_k,\qquad \|v-P_Uv\|=\min_{u\in U}\|v-u\|
$$

对任意u属于U，将v-u拆成(v-P_Uv)+(P_Uv-u)。第一项属于U的正交补，第二项属于U，勾股等式说明距离平方至少为第一项长度平方，且u=P_Uv时取到最小值。 证明验收要求逐步标注定义、已有定理和新构造，不能用待证结论的等价形式循环论证。

$$
\text{assumptions}\Longrightarrow\text{lemma}\Longrightarrow\text{invariant}\Longrightarrow\text{claim}
$$

```python
def project(v,e):
    coeff=sum(x*y for x,y in zip(v,e))
    return tuple(coeff*x for x in e)

p=project((3,4),(1,0))
assert p==(3,0)
```

代码不是证明，但能暴露维数、顺序、共轭和边界错误。至少使用一个非对角、非对称或不满秩样例；单位矩阵与零算子只能作为冒烟测试。

## 结构图与证明骨架

**内积**：满足共轭对称、线性和正定的标量函数，用于定义长度与角度。

**正交规范基**：基向量两两正交且长度为一，坐标直接由内积取得。

**Gram-Schmidt**：按顺序减去已有正交方向上的投影，把独立列表转成正交规范列表。

先从定义构造对象，再找到保持不变的子空间、维数或多项式，最后把局部结论拼成全空间结论。若构造依赖选择基，要继续说明最终结论为何与基无关。


## 反例与边界

**正交补**：与子空间中每个向量都正交的向量集合，并给出V等于U直和U正交补。

**伪逆**：把目标投影到range T后求最小范数原像，统一最小二乘与欠定问题。

边界集至少覆盖零空间、零映射、重复特征值、线性相关列表、实数与复数域切换、缺少内积和接近秩亏。抽象证明用反例检查必要性，数值实现用残差和条件尺度检查稳定性。

$$
r=\|\text{left side}-\text{right side}\|,\qquad r\le\tau_{abs}+\tau_{rel}\|\text{reference}\|
$$

```python
audit={"assumptions":"explicit","normal":"nontrivial example","counterexample":"在复内积空间忘记一个槽需要共轭，导致所谓范数不再总是实非负。","replay":"fixed basis and tolerance"}
assert set(audit)=={"assumptions","normal","counterexample","replay"}
```

## 工程与学习案例

最小二乘应同时输出投影、残差和正规方程残差；若列向量近线性相关，不直接求逆正规矩阵，而使用QR或SVD构造伪逆。 交付物同时保存定义域和陪域、基、矩阵或算子表示、关键中间子空间、残差、反例以及结论适用范围。

```python
evidence=[("内积","definition"),("Gram-Schmidt","proof"),("伪逆","counterexample")]
assert len({name for name,_ in evidence})==3
```

1. 原样写出定义中的量词、域、空间和非零条件。
2. 画出子空间、映射或分解关系，并标记要保持的不变量。
3. 用最小非平凡例子逐步重算，再删除一个假设寻找反例。
4. 对数值版本保存基、容差、残差和重构误差，保证可重复。

## 常见误区


## 本章回顾

本章覆盖内积、正交规范基、Gram-Schmidt、正交补、伪逆。掌握标准不是背结论，而是能声明假设、重建证明骨架、给出删除假设后的反例，并以具体矩阵或代码检查表示与不变量。


## 术语表


