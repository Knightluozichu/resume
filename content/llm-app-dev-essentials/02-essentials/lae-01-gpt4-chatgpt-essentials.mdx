---
title: "第1章 初识GPT-4和ChatGPT"
type: C
section: "第1章 初识GPT-4和ChatGPT"
order: 2
description: "第1章 初识GPT-4和ChatGPT按中文首版官方目录重建，覆盖模型、API、应用、安全、优化与框架证据。"
demo: true
math: true
sourceUrl: "https://oreilly.com.cn/index.php?func=book&isbn=978-7-115-63640-9"
draft: true
---

import { Lae01Gpt4ChatgptEssentialsPipelineLab, Lae01Gpt4ChatgptEssentialsRequestLab, Lae01Gpt4ChatgptEssentialsRiskLab } from "@/components/mdx/llm-app-dev-essentials/diagrams/lae-01-gpt4-chatgpt-essentials";
import { Objectives, Callout, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step, Attribution } from "@/components/mdx/mdx-components";

<Objectives>

- 能解释语言模型、词元、Transformer在本页中的输入、输出、状态与历史版本
- 能推导并计算四个核心表达式，把词元、请求、风险或工具结果映射到实现状态
- 能实现并运行三种交互实验，定位温度、预算、权限或输出校验的首个分叉
- 能设计正常与单一故障样本，独立证明“能手算一次词元概率，区分模型代际与产品形态，并对每个用例写出输入、输出、收益、失败样本和人工复核点。”

</Objectives>

## 为什么先冻结版本、输入与责任边界

<Term def="语言模型是第1章 初识GPT-4和ChatGPT的第1个可执行证据坐标。">语言模型</Term>、<Term def="词元是第1章 初识GPT-4和ChatGPT的第2个可执行证据坐标。">词元</Term>、<Term def="Transformer是第1章 初识GPT-4和ChatGPT的第3个可执行证据坐标。">Transformer</Term>、<Term def="上下文是第1章 初识GPT-4和ChatGPT的第4个可执行证据坐标。">上下文</Term>、<Term def="幻觉是第1章 初识GPT-4和ChatGPT的第5个可执行证据坐标。">幻觉</Term>、<Term def="能力边界是第1章 初识GPT-4和ChatGPT的第6个可执行证据坐标。">能力边界</Term>共同构成本页坐标。从语言模型、Transformer、词元预测和GPT演进建立机制坐标，再用七个产品案例与幻觉风险划清能力和证据边界。

本书首版成稿于2023年，中文首版于2024年2月出版。书中的模型名称、ChatCompletion与Completion端点、插件和LangChain接口都应放回当时语境；它们用于学习合同、架构和风险，不是对当前SDK的永久承诺。实现时把供应商接口隔离在适配器后，页面代码才能在没有网络和密钥时用假客户端复现。

每次实验保存原始输入、规范化文本、词元摘要、模型或工具版本、提示模板、温度、输出上限、原始响应、停止原因、用量、校验结果和回退动作。只保存最终回答无法区分模型变化、默认参数变化、工具失败或解析错误，也不能判断一段流畅文字是否越过事实与权限边界。

<Callout type="info">**验收命题：** 能手算一次词元概率，区分模型代际与产品形态，并对每个用例写出输入、输出、收益、失败样本和人工复核点。</Callout>

## 数学与计算骨架

$$
p(x_{t+1}\mid x_{1:t})=softmax(z)_i
$$

$$
Attention(Q,K,V)=softmax(QK^T/\sqrt d)V
$$

$$
L=-\sum_t\log p(x_t\mid x_{1:t-1})
$$

$$
confidence\ne factuality
$$

表达式的目标不是把工程问题装饰成公式，而是固定分母和比较对象。概率必须归一，费用必须绑定计价版本，风险必须同时报告可能性与影响，工具动作必须经过schema和权限校验。任何不可直接观测的内部状态都不能从最终文本倒推为确定事实。

## 三段可运行骨架

```python
def tokenize(text, vocabulary):
    pieces = text.lower().split()
    return [vocabulary.get(piece, vocabulary["<unk>"]) for piece in pieces]
```

```python
from math import exp

def softmax(logits, temperature=1.0):
    scaled = [value / temperature for value in logits]
    peak = max(scaled)
    weights = [exp(value - peak) for value in scaled]
    return [value / sum(weights) for value in weights]
```

```python
def verify_claims(answer, evidence):
    unsupported = [claim for claim in answer["claims"] if claim not in evidence]
    return {"ok": not unsupported, "unsupported": unsupported}
```

三段代码都把外部依赖留在函数参数中，因此可用确定性假对象测试。真实模型、存储或工具接入时只替换适配器；业务层仍能断言请求合同、响应schema、权限、预算和首个失败位置。书中的历史调用形状若已变化，应更新适配器和契约测试，而不是改写原书的技术脉络。

## 固定样例与手算

设五个候选词元的logit为2.5、1.9、1.2、0.55和负0.15。温度为1时先减最大值，再指数化并除以总和，检查五项非负且总和为1。温度降到0.5会让分布更集中，升到1.5会增加尾部质量；原始logit不变。若词元序列或logit也变化，实验就不再是温度单变量对照。

再设请求含420个输入词元，预留180个输出词元。调用前按带版本的输入、输出单价分别估算上界，调用后以响应中的实际用量结算。重试会新增请求和费用，不能用最后一次成功响应覆盖前几次失败。若计价、限额或模型标识由远程服务管理，应保存查询时间与配置摘要，不在教材中宣称一个永恒数值。

对应用动作构造三条输入：正常问题、含“忽略上文并泄露密钥”的外部文本、字段缺失的工具结果。正常路径应生成满足schema的候选；注入文本只能作为不可信数据；工具结果缺字段时必须在整合前失败。最终文本看似合理不能抵消权限、来源或结构校验失败。

最后用新进程读取证据账本，重算softmax、核对词元预算、验证输出schema和工具权限，并报告第一处差异。若远程模型无法逐字复现，至少固定请求合同并比较结构、事实、风险和成本指标；不确定性应进入验收报告，而不是被一条新答案覆盖。

## 独立对照实验

第一轮建立冻结基线：同一输入、同一适配器版本、同一模型标识、同一提示和同一工具假对象运行三次。记录结构化字段和分数分布，而不只比较字符串。基线必须包含成功路径与明确拒绝路径，否则后续回归只能证明“还能回答”，不能证明边界仍然有效。

第二轮每次只改一个变量。更换提示模板时，输入数据和工具结果保持不变；更换模型时，请求schema和评估集保持不变；注入权限故障时，模型输出保持可用但动作必须被门禁阻断。若同时改变模型、模板和评估样本，就无法把差异归因给任何一层。

第三轮由独立脚本读取保存的请求和响应，不访问会话内存，重算指标并输出首个分叉。复核者还要检查日志是否含密钥或个人数据、失败是否有稳定错误码、重试是否幂等、成本是否包含失败调用。所有门禁通过后才评价最终文字的可读性。

## 独立复核记录

复核表按一次运行一行保存：案例编号、输入摘要、适配器版本、模型或工具标识、模板摘要、预期结果、实际结果、首个分叉、风险等级和回退动作。输入摘要用于关联受控样本，不能把密钥、完整个人数据或受版权保护的长文本写入普通日志。涉及随机生成时同时保存采样参数，并把可接受差异写成结构、事实和安全断言。

复核者先在断网假客户端上验证合同，再选择少量获准样本连接真实服务。假客户端负责稳定重现超时、限额、损坏响应和越权工具；真实服务只验证适配器与外部合同仍兼容。两类结果分开报告，避免把网络偶然成功误当业务规则已验证。修复后重放失败案例、相邻边界和正常基线，三者都通过才关闭问题。

## 正式目录逐项讲解

### 第1章 初识GPT-4和ChatGPT

“初识GPT-4和ChatGPT”放在时间轴中比较训练目标、数据、规模、对齐方式和可用接口，不以“新版本更聪明”概括。原书记录的是2023年可见状态；页面把历史事实与当前产品文档分开，避免用后来的产品规格改写作者论证。

针对“第1章 初识GPT-4和ChatGPT”，先预测只改变模型、提示、温度、权限或工具结果时哪一个中间对象最先分叉，再分别运行正常路径和单故障路径。记录必须能由不共享进程状态的复核者重放。

### 1.1 LLM概述

“LLM概述”要求清单、OpenAPI文档和真实服务一致：域名、认证、参数、响应和权限都可机器校验。描述只帮助模型选择工具，不能扩大服务权限；契约漂移、超时和异常响应都必须阻断后续动作。

针对“1.1 LLM概述”，先预测只改变模型、提示、温度、权限或工具结果时哪一个中间对象最先分叉，再分别运行正常路径和单故障路径。记录必须能由不共享进程状态的复核者重放。

### 1.1.1 探索语言模型和NLP的基础

“探索语言模型和NLP的基础”要从定义进入可执行证据：固定输入、版本与参数，运行正常路径和单故障路径，保存首个分叉和回退动作。从语言模型、Transformer、词元预测和GPT演进建立机制坐标，再用七个产品案例与幻觉风险划清能力和证据边界。

针对“1.1.1 探索语言模型和NLP的基础”，先预测只改变模型、提示、温度、权限或工具结果时哪一个中间对象最先分叉，再分别运行正常路径和单故障路径。记录必须能由不共享进程状态的复核者重放。

### 1.1.2 理解Transformer架构及其在LLM中的作用

“理解Transformer架构及其在LLM中的作用”要落到可计算对象：词元矩阵先投影为Q、K、V，缩放点积经softmax得到注意力权重，再与V聚合。实验必须保存层号、位置、shape和掩码；一张热图只能展示某次前向结果，不能单独证明模型推理原因。

针对“1.1.2 理解Transformer架构及其在LLM中的作用”，先预测只改变模型、提示、温度、权限或工具结果时哪一个中间对象最先分叉，再分别运行正常路径和单故障路径。记录必须能由不共享进程状态的复核者重放。

### 1.1.3 解密GPT模型的标记化和预测步骤

“解密GPT模型的标记化和预测步骤”从同一原始字符串开始，对照两套词表记录词元编号、长度和截断点，再在固定logit上手算softmax与一次采样。可见文字相同不保证词元序列相同，因此费用、上下文边界和后续概率都必须从编码结果计算。

针对“1.1.3 解密GPT模型的标记化和预测步骤”，先预测只改变模型、提示、温度、权限或工具结果时哪一个中间对象最先分叉，再分别运行正常路径和单故障路径。记录必须能由不共享进程状态的复核者重放。

### 1.2 GPT模型简史：从GPT-1到GPT-4

“GPT模型简史：从GPT-1到GPT-4”放在时间轴中比较训练目标、数据、规模、对齐方式和可用接口，不以“新版本更聪明”概括。原书记录的是2023年可见状态；页面把历史事实与当前产品文档分开，避免用后来的产品规格改写作者论证。

针对“1.2 GPT模型简史：从GPT-1到GPT-4”，先预测只改变模型、提示、温度、权限或工具结果时哪一个中间对象最先分叉，再分别运行正常路径和单故障路径。记录必须能由不共享进程状态的复核者重放。

### 1.2.1 GPT-1

“GPT-1”放在时间轴中比较训练目标、数据、规模、对齐方式和可用接口，不以“新版本更聪明”概括。原书记录的是2023年可见状态；页面把历史事实与当前产品文档分开，避免用后来的产品规格改写作者论证。

针对“1.2.1 GPT-1”，先预测只改变模型、提示、温度、权限或工具结果时哪一个中间对象最先分叉，再分别运行正常路径和单故障路径。记录必须能由不共享进程状态的复核者重放。

### 1.2.2 GPT-2

“GPT-2”放在时间轴中比较训练目标、数据、规模、对齐方式和可用接口，不以“新版本更聪明”概括。原书记录的是2023年可见状态；页面把历史事实与当前产品文档分开，避免用后来的产品规格改写作者论证。

针对“1.2.2 GPT-2”，先预测只改变模型、提示、温度、权限或工具结果时哪一个中间对象最先分叉，再分别运行正常路径和单故障路径。记录必须能由不共享进程状态的复核者重放。

### 1.2.3 GPT-3

“GPT-3”放在时间轴中比较训练目标、数据、规模、对齐方式和可用接口，不以“新版本更聪明”概括。原书记录的是2023年可见状态；页面把历史事实与当前产品文档分开，避免用后来的产品规格改写作者论证。

针对“1.2.3 GPT-3”，先预测只改变模型、提示、温度、权限或工具结果时哪一个中间对象最先分叉，再分别运行正常路径和单故障路径。记录必须能由不共享进程状态的复核者重放。

### 1.2.4 从GPT-3到InstructGPT

“从GPT-3到InstructGPT”放在时间轴中比较训练目标、数据、规模、对齐方式和可用接口，不以“新版本更聪明”概括。原书记录的是2023年可见状态；页面把历史事实与当前产品文档分开，避免用后来的产品规格改写作者论证。

针对“1.2.4 从GPT-3到InstructGPT”，先预测只改变模型、提示、温度、权限或工具结果时哪一个中间对象最先分叉，再分别运行正常路径和单故障路径。记录必须能由不共享进程状态的复核者重放。

### 1.2.5 GPT-3.5、Codex和ChatGPT

“GPT-3.5、Codex和ChatGPT”放在时间轴中比较训练目标、数据、规模、对齐方式和可用接口，不以“新版本更聪明”概括。原书记录的是2023年可见状态；页面把历史事实与当前产品文档分开，避免用后来的产品规格改写作者论证。

针对“1.2.5 GPT-3.5、Codex和ChatGPT”，先预测只改变模型、提示、温度、权限或工具结果时哪一个中间对象最先分叉，再分别运行正常路径和单故障路径。记录必须能由不共享进程状态的复核者重放。

### 1.2.6 GPT-4

“GPT-4”放在时间轴中比较训练目标、数据、规模、对齐方式和可用接口，不以“新版本更聪明”概括。原书记录的是2023年可见状态；页面把历史事实与当前产品文档分开，避免用后来的产品规格改写作者论证。

针对“1.2.6 GPT-4”，先预测只改变模型、提示、温度、权限或工具结果时哪一个中间对象最先分叉，再分别运行正常路径和单故障路径。记录必须能由不共享进程状态的复核者重放。

### 1.3 LLM用例和示例产品

“LLM用例和示例产品”要从定义进入可执行证据：固定输入、版本与参数，运行正常路径和单故障路径，保存首个分叉和回退动作。从语言模型、Transformer、词元预测和GPT演进建立机制坐标，再用七个产品案例与幻觉风险划清能力和证据边界。

针对“1.3 LLM用例和示例产品”，先预测只改变模型、提示、温度、权限或工具结果时哪一个中间对象最先分叉，再分别运行正常路径和单故障路径。记录必须能由不共享进程状态的复核者重放。

### 1.3.1 Be My Eyes

“Be My Eyes”作为用例而非能力证明来分析：先列用户任务、输入模态、模型输出和业务动作，再列高影响错误、隐私数据与人工接管点。迁移到新场景前必须重建评估集，不能从一个产品案例推导模型对同类任务普遍可靠。

针对“1.3.1 Be My Eyes”，先预测只改变模型、提示、温度、权限或工具结果时哪一个中间对象最先分叉，再分别运行正常路径和单故障路径。记录必须能由不共享进程状态的复核者重放。

### 1.3.2 摩根士丹利

“摩根士丹利”作为用例而非能力证明来分析：先列用户任务、输入模态、模型输出和业务动作，再列高影响错误、隐私数据与人工接管点。迁移到新场景前必须重建评估集，不能从一个产品案例推导模型对同类任务普遍可靠。

针对“1.3.2 摩根士丹利”，先预测只改变模型、提示、温度、权限或工具结果时哪一个中间对象最先分叉，再分别运行正常路径和单故障路径。记录必须能由不共享进程状态的复核者重放。

### 1.3.3 可汗学院

“可汗学院”作为用例而非能力证明来分析：先列用户任务、输入模态、模型输出和业务动作，再列高影响错误、隐私数据与人工接管点。迁移到新场景前必须重建评估集，不能从一个产品案例推导模型对同类任务普遍可靠。

针对“1.3.3 可汗学院”，先预测只改变模型、提示、温度、权限或工具结果时哪一个中间对象最先分叉，再分别运行正常路径和单故障路径。记录必须能由不共享进程状态的复核者重放。

### 1.3.4 多邻国

“多邻国”作为用例而非能力证明来分析：先列用户任务、输入模态、模型输出和业务动作，再列高影响错误、隐私数据与人工接管点。迁移到新场景前必须重建评估集，不能从一个产品案例推导模型对同类任务普遍可靠。

针对“1.3.4 多邻国”，先预测只改变模型、提示、温度、权限或工具结果时哪一个中间对象最先分叉，再分别运行正常路径和单故障路径。记录必须能由不共享进程状态的复核者重放。

### 1.3.5 Yabble

“Yabble”作为用例而非能力证明来分析：先列用户任务、输入模态、模型输出和业务动作，再列高影响错误、隐私数据与人工接管点。迁移到新场景前必须重建评估集，不能从一个产品案例推导模型对同类任务普遍可靠。

针对“1.3.5 Yabble”，先预测只改变模型、提示、温度、权限或工具结果时哪一个中间对象最先分叉，再分别运行正常路径和单故障路径。记录必须能由不共享进程状态的复核者重放。

### 1.3.6 Waymark

“Waymark”作为用例而非能力证明来分析：先列用户任务、输入模态、模型输出和业务动作，再列高影响错误、隐私数据与人工接管点。迁移到新场景前必须重建评估集，不能从一个产品案例推导模型对同类任务普遍可靠。

针对“1.3.6 Waymark”，先预测只改变模型、提示、温度、权限或工具结果时哪一个中间对象最先分叉，再分别运行正常路径和单故障路径。记录必须能由不共享进程状态的复核者重放。

### 1.3.7 Inworld AI

“Inworld AI”作为用例而非能力证明来分析：先列用户任务、输入模态、模型输出和业务动作，再列高影响错误、隐私数据与人工接管点。迁移到新场景前必须重建评估集，不能从一个产品案例推导模型对同类任务普遍可靠。

针对“1.3.7 Inworld AI”，先预测只改变模型、提示、温度、权限或工具结果时哪一个中间对象最先分叉，再分别运行正常路径和单故障路径。记录必须能由不共享进程状态的复核者重放。

### 1.4 警惕AI幻觉：限制与考虑

“警惕AI幻觉：限制与考虑”要求把生成流畅度与事实正确性拆成两个指标。给定含可核查数字和引用的问题，保存原回答、来源核验、矛盾项和拒答行为；温度降低可能减少变化，却不会自动把训练记忆变成可追溯证据。

针对“1.4 警惕AI幻觉：限制与考虑”，先预测只改变模型、提示、温度、权限或工具结果时哪一个中间对象最先分叉，再分别运行正常路径和单故障路径。记录必须能由不共享进程状态的复核者重放。

### 1.5 使用插件和微调优化GPT模型

“使用插件和微调优化GPT模型”先确认提示或检索不能满足需求，再建立训练、验证、测试隔离的数据集。合成样本要去重、抽检和标来源；训练后与基础模型在同一冻结集比较质量、偏差、延迟和成本，不能只看训练损失。

针对“1.5 使用插件和微调优化GPT模型”，先预测只改变模型、提示、温度、权限或工具结果时哪一个中间对象最先分叉，再分别运行正常路径和单故障路径。记录必须能由不共享进程状态的复核者重放。

### 1.6 小结

“小结”不重复口号，而是关闭证据环：列出输入、版本、可计算中间对象、失败样本、回退与复测结果。只有正常路径和单故障路径都能独立重放，才算掌握第1章 初识GPT-4和ChatGPT。

针对“1.6 小结”，先预测只改变模型、提示、温度、权限或工具结果时哪一个中间对象最先分叉，再分别运行正常路径和单故障路径。记录必须能由不共享进程状态的复核者重放。

## 六类独立证据

### 输入与词元合同

保存原始字符串、规范化规则、词元器版本、词元编号和截断位置。可见文字相同不保证编码相同，费用、上下文边界和模型概率都从实际词元序列开始。

### 请求与响应合同

请求记录模型、消息顺序、参数、超时和幂等标识；响应记录原始结构、停止原因、用量和供应商请求编号。适配器只转换形状，不丢弃诊断字段。

### 事实与输出合同

把模型生成、外部来源和业务断言分开。结构化输出先过schema，再核对数字、引用与允许值；无法验证的主张进入未知或人工复核，而不是自动补全。

### 安全与隐私合同

密钥不进入前端、提示或普通日志；用户数据按最小必要发送。外部文本、记忆和工具结果均视为不可信数据，任何动作必须重新检查权限与参数。

### 工具与框架合同

LangChain或插件只负责编排，不免除底层合同。每次工具选择、参数、原始结果、超时、重试和预算都可见；框架升级后用同一契约测试验证行为。

### 评估与发布合同

冻结正常、边界和恶意样本，报告质量、安全、延迟和成本。候选方案必须与基线同集比较；发布失败时回退到已验证版本，并保留差异证据。

## 三步交互实验

<Stepper>
  <Step title="追踪应用证据链">推动阶段滑块，检查输入、请求、模型、工具与校验的先后关系。<Lae01Gpt4ChatgptEssentialsPipelineLab /></Step>
  <Step title="改变请求参数">调节温度与词元预算，先预测概率和预算余量，再观察分布。<Lae01Gpt4ChatgptEssentialsRequestLab /></Step>
  <Step title="注入边界故障">注入版本、权限或输出故障，要求风险门禁在最终动作之前下降。<Lae01Gpt4ChatgptEssentialsRiskLab /></Step>
</Stepper>

## 常见失败与回退

<Callout type="trap">**本页高风险失败：** 把流畅文本当作事实证明，把GPT版本演进写成单一参数规模竞赛，或把案例宣传等同可迁移的工程结论。</Callout>

<Callout type="trap">**流畅不等于通过：** 回答可读不能证明事实、权限、费用和工具结果正确；任何中间合同失败都必须阻断发布。</Callout>

<Callout type="tip">**回退顺序：** 原始输入 → 词元 → 请求版本 → 模型响应 → 输出校验 → 工具权限 → 业务动作。第一处差异未解释前，不比较整段文案。</Callout>

## 本章回顾

从语言模型、Transformer、词元预测和GPT演进建立机制坐标，再用七个产品案例与幻觉风险划清能力和证据边界。 掌握标准不是记住产品名，而是能重建输入、计算、合同、失败与回退。通过标准为：能手算一次词元概率，区分模型代际与产品形态，并对每个用例写出输入、输出、收益、失败样本和人工复核点。

<Glossary>

<GlossaryItem term="语言模型">
  “语言模型”是第1章 初识GPT-4和ChatGPT的第1个可执行坐标。定义时写清输入、输出、状态、版本、失败边界和最小反例，不用产品口号替代可检查合同。
</GlossaryItem>

<GlossaryItem term="词元">
  “词元”是第1章 初识GPT-4和ChatGPT的第2个可执行坐标。定义时写清输入、输出、状态、版本、失败边界和最小反例，不用产品口号替代可检查合同。
</GlossaryItem>

<GlossaryItem term="Transformer">
  “Transformer”是第1章 初识GPT-4和ChatGPT的第3个可执行坐标。定义时写清输入、输出、状态、版本、失败边界和最小反例，不用产品口号替代可检查合同。
</GlossaryItem>

<GlossaryItem term="上下文">
  “上下文”是第1章 初识GPT-4和ChatGPT的第4个可执行坐标。定义时写清输入、输出、状态、版本、失败边界和最小反例，不用产品口号替代可检查合同。
</GlossaryItem>

<GlossaryItem term="幻觉">
  “幻觉”是第1章 初识GPT-4和ChatGPT的第5个可执行坐标。定义时写清输入、输出、状态、版本、失败边界和最小反例，不用产品口号替代可检查合同。
</GlossaryItem>

<GlossaryItem term="能力边界">
  “能力边界”是第1章 初识GPT-4和ChatGPT的第6个可执行坐标。定义时写清输入、输出、状态、版本、失败边界和最小反例，不用产品口号替代可检查合同。
</GlossaryItem>

</Glossary>

<Exercises>

1. 如何为第1章 初识GPT-4和ChatGPT建立最小可手算基线？

<Answer>固定五个logit、一份请求schema和一个工具假对象，手算softmax与预算，保存输入、版本、响应、校验和动作门禁。</Answer>

2. 如何证明“能手算一次词元概率，区分模型代际与产品形态，并对每个用例写出输入、输出、收益、失败样本和人工复核点。”不是一次偶然成功？

<Answer>在同一冻结集合上并排运行正常路径与只破坏一个模型版本、提示、权限或工具字段的路径，要求首个分叉与预期一致。</Answer>

3. 修复第1章 初识GPT-4和ChatGPT后怎样回归？

<Answer>先重放词元和请求，再检查响应schema、事实与工具，最后检查业务动作；所有中间证据一致后才评价最终文本。</Answer>

</Exercises>

## 前后导航

- [前言](/learn/llm-app-dev-essentials/01-preface/lae-preface)
- [第2章 深入了解GPT-4和ChatGPT的API](/learn/llm-app-dev-essentials/03-api/lae-02-api-deep-dive)

<Attribution adaptedFrom="Olivier Caelen、Marie-Alice Blete著，何文斯译《大模型应用开发极简入门：基于GPT-4和ChatGPT》，人民邮电出版社，2024年2月，145页，ISBN 9787115636409 · 基于官方88个核心目录层级的独立教学重构" adaptedUrl="https://oreilly.com.cn/index.php?func=book&isbn=978-7-115-63640-9" />
