---
title: "Chapter 18. Iterators and the Generic for"
type: C
section: "Programming in Lua 4e · Real Programming"
order: 18
description: "掌握closure iterator、generic-for三元协议、stateless iterator、有序table遍历与true iterator，并验证progress、order和lifecycle。"
demo: true
math: false
sourceUrl: "https://www.lua.org/pil/"
draft: false
---

import { Objectives, Callout, Attribution, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step } from "@/components/mdx/mdx-components";
import { PilIteratorClosureLab, PilGenericForProtocolLab, PilIteratorStyleLab } from "@/components/mdx/lua-programming/diagrams/iterators-generic-for";

<Objectives>

- 能解释closure iterator的state、progress、termination与lifecycle，并判断独立实例和共享消费行为
- 能推导generic for的iterator/state/control三元协议，复现first-result nil与false控制值差异
- 能设计stateless、有序和true iterator，比较snapshot、mutation、early stop与callback错误边界

</Objectives>

## 为什么Generic for背后有三个State Values

表面上`for value in values() do`像一个函数，实际generic for在进入时保存iterator function、invariant state和initial control value。每轮调用iterator(state, control)，把首结果更新为next control；首结果nil时结束。Closure iterator可把state藏进upvalues，但protocol本身不要求closure。

先预测：iterator首结果false是否结束；expression list中的factory调用几次；body给loop variable赋值能否改变下一control；同一个closure iterator交给两个loops会独立吗；pairs为何能stateless；按sorted keys遍历时source中途新增key会不会出现；callback-style true iterator如何break。先画owner与control。

<Term def="Generic for进入时保存iterator function、invariant state与initial control，并在每轮用首返回值更新control直到nil。">generic-for iterator triple</Term>把generator syntax还原为普通call protocol。Lua 5.3的核心语义以这三个values理解；更新版语言可能加入close value，不应倒灌到本书baseline。

## Iterators and Closures

Closure iterator factory创建private state并返回next function。每次调用推进position、返回item；结束时首结果nil。<Term def="Iterator每次调用要么严格推进到新状态并返回item，要么以首结果nil永久结束，不能停留在同一可返回状态。">iterator progress invariant</Term>防止malformed input或empty match造成infinite loop。

每次factory call创建独立upvalues；同一个iterator function被多个consumers调用则共享并交错state。Stateful iterator通常不可rewind/reentrant，API应命名为new/make并记录single-consumer。若closure捕获file handle，loop提前break不会自动通知iterator cleanup；owner需with-iterator helper、explicit close或更合适的resource protocol。

```lua
local function range(first, last, step)
  step = step or 1
  assert(step ~= 0)
  local current = first - step
  return function()
    current = current + step
    if (step > 0 and current <= last) or (step < 0 and current >= last) then
      return current
    end
    return nil
  end
end

local values = {}
for value in range(1, 5, 2) do values[#values + 1] = value end
assert(table.concat(values, ",") == "1,3,5")
```

Range这里仅返回iterator，generic for把缺少的state/control视为nil并反复调用closure。Untrusted bounds需限制iteration count与integer overflow；`current=current+step`溢出可能让termination失效。

<PilIteratorClosureLab />

<Stepper>
  <Step title="切换Factory、Yield Value、Finish和Reset/Reuse"><PilIteratorClosureLab /></Step>
  <Step title="画State Timeline">记录每次factory产生的binding、每次call前后position和first result；两个consumers共享时交错标色。</Step>
  <Step title="注入Lifecycle Exit">覆盖normal EOF、break、body error和abandoned iterator，验证captured resource由谁关闭及最后reference何时释放。</Step>
</Stepper>

<Callout type="trap" title="把同一个Iterator Function放进两个for循环会各自从头开始">
Closure的state属于function/upvalues，不属于for loop。两个loops调用同一个closure会交错消费；需要独立遍历就各自调用factory创建新iterator triple。
</Callout>

## The Semantics of the Generic for

Generic form的expression list只求值一次，得到iterator、state和control。概念展开如下：保存三元组；循环调用iterator(state, control)；把results赋给fresh loop locals；首结果赋给hidden control；若首结果nil则break；否则执行body。

<Term def="Iterator首返回值同时是本轮第一loop value和下一轮control，只有nil终止，false、0与empty string都继续。">first-result control</Term>要求data如果可能nil，改用separate non-nil key/control并把data放后续result。Additional results可以nil，不影响termination。

```lua
local function step(state, control)
  local next_control = control + 1
  if next_control > #state then return nil end
  return next_control, state[next_control]
end

local function values(sequence)
  return step, sequence, 0
end

local seen = {}
for index, value in values({ false, "b", "c" }) do
  seen[index] = value
end
assert(seen[1] == false and seen[3] == "c")
```

若step错误地写`if value then return ... end`，false element会提前停止；正确termination检查index/range或`value ~= nil`。Body中修改index local不改变hidden control，因为下一control来自iterator首结果。

<PilGenericForProtocolLab />

<Stepper>
  <Step title="切换Evaluate、Call、Update和Terminate"><PilGenericForProtocolLab /></Step>
  <Step title="手工展开Loop">把一个generic for改写为while三元协议，逐轮记录state、incoming control、results和body locals。</Step>
  <Step title="做First-result Matrix">首结果依次为1、false、0、empty string、nil，后续data含nil；验证只有first nil停止。</Step>
</Stepper>

## Stateless Iterators

Stateless iterator不把iteration state存upvalue，全部由invariant state与control提供，因此同一iterator function可服务多个loops。`next(table, key)`返回下一个key/value，key nil从头开始；`pairs`通常提供next、table、nil。Order unspecified且mutation policy有限。

Ipairs-style iterator用sequence作为state、index作为control：next index加一，读取value，value为nil时结束。<Term def="Stateless iterator的下一步完全由显式invariant state和control决定，function自身不保存每个遍历实例的mutable position。">stateless iterator protocol</Term>使reentrancy更清晰，但underlying table mutation仍可能破坏结果。

Iterator function可复用不等于state thread-safe。两个loops遍历同一mutable table时，修改可见性取决于collection contract。需要snapshot就先copy keys/values；需要live view则限制allowed mutations。

## Traversing Tables in Order

Pairs不承诺order。有序遍历先collect keys到sequence，按total comparator排序，再用closure/stateless iterator依次返回key与current value。<Term def="Ordered table traversal先固定key snapshot与total order，再声明source在迭代期间mutation时使用snapshot key还是snapshot value。">ordered traversal snapshot</Term>区分两种语义：只snapshot keys时删除key会返回nil、新key不出现；snapshot key-value pairs则完全稳定但复制更多。

```lua
local function ordered_pairs(source)
  local keys = {}
  for key in pairs(source) do keys[#keys + 1] = key end
  table.sort(keys)
  local index = 0
  return function()
    index = index + 1
    local key = keys[index]
    if key == nil then return nil end
    return key, source[key]
  end
end

local output = {}
for key, value in ordered_pairs({ z = 1, a = 2, m = 3 }) do
  output[#output + 1] = key .. value
end
assert(table.concat(output, ",") == "a2,m3,z1")
```

Mixed key types不能直接`table.sort(keys)`，必须schema禁止或提供type-ranked comparator。Comparator不能读取迭代中变化的external state。Large tables的O(n) memory/O(n log n) cost需预算；若data源已ordered，使用其native iterator。

## True Iterators

原书把由traversal function控制循环、对每个item调用callback的形式称true iterator。Generator把control交给caller的for body；true iterator持有control：`all_words(function(word) ... end)`。<Term def="True iterator由遍历函数主动调用consumer callback，并显式规定callback result、early stop、error propagation与reentrancy。">callback-driven iterator</Term>可避免每item generator state transfer，但control composition更困难。

Early stop可让callback返回false/tag，或raise专用sentinel再在边界捕获；前者更清晰。Callback error默认传播，traversal必须cleanup resources。Nested traversal与yieldability按runtime测试。若caller需要普通break、多loop clauses和generic algorithms，generator通常更符合Lua习惯。

<PilIteratorStyleLab />

<Stepper>
  <Step title="切换Stateless、Ordered、Generator和True Iterator"><PilIteratorStyleLab /></Step>
  <Step title="比较Control Owner">记录谁推进、谁能break、谁cleanup、callback/error result shape与per-item call cost，按workflow选择style。</Step>
  <Step title="验证Mutation Policy">Ordered keys snapshot期间删除/添加/改value，分别测试key-snapshot与value-snapshot；stateless live iterator限制mutation。</Step>
</Stepper>

<Callout type="trap" title="Sorted Keys Iterator意味着整个遍历是Immutable Snapshot">
若只复制keys，values仍从source实时读取：删除会得到nil，修改会见新值；新增keys不出现。需要完整snapshot就复制key-value pairs并明确memory cost。
</Callout>

## Iterator Errors、Resources与Testing

Iterator error发生在for获取next item阶段，body尚未执行本轮。Protected boundary应覆盖整个loop，resource owner在success、iterator error、body error与break都cleanup。不要让iterator用nil吞I/O error；返回control加result，或raise/typed status保持EOF与failure不同。

Property tests比较iterator输出与reference sequence，并断言调用次数最多n+1、首nil后不再产生item。Fuzz empty/single/false/nil data、mutation与deep inputs。

## 本章回顾：Iterator是Progress与Control Ownership协议

1. Closure iterator隐藏state，每个factory实例独立；同一function多consumer会共享消费。
2. Generic for一次求值iterator/state/control，每轮首result更新control，首nil才终止。
3. Stateless iterator从显式state/control计算下一项，next/pairs与ipairs-style是典型例子。
4. Ordered traversal必须collect/sort keys并声明key snapshot还是完整value snapshot。
5. True iterator主动调用callback，early stop、error、cleanup和reentrancy需专门协议。
6. 所有iterator都要证明strict progress、bounded work与resource lifecycle。

<Exercises>

**问题 1：Iterator首Result为False时Generic for为何继续？**

<Answer>Generic for只用首result是否为nil判断termination，false是合法control/loop value。若实现用truthiness会丢false data；按index/range或`value ~= nil`判断，并测试false、0、empty string。</Answer>

**问题 2：怎样让Ordered Traversal成为完整Snapshot？**

<Answer>Collect时保存records `{key=...,value=...}`，按key total comparator排序，iterator只读records，不再lookup source。这样后续source add/remove/update不影响结果；代价是复制values/references与O(n) memory。</Answer>

**问题 3：File-backed Iterator遇到Break时谁Close？**

<Answer>Lua 5.3 ordinary closure iterator没有break通知。让外层owner打开handle并用protected cleanup包住整个loop，或提供explicit iterator:close/with_lines API；normal EOF、break、body/iterator error都测试close outcome。</Answer>

</Exercises>

## 术语表

<Glossary>
  <GlossaryItem term="generic-for iterator triple" definition="Generic for保存iterator、invariant state与initial control的三元状态。" />
  <GlossaryItem term="iterator progress invariant" definition="每次调用严格推进或首result nil永久结束的迭代约束。" />
  <GlossaryItem term="first-result control" definition="首result既是loop value又更新control且只有nil终止的规则。" />
  <GlossaryItem term="stateless iterator protocol" definition="Next step只由显式state和control决定且function不存实例位置的协议。" />
  <GlossaryItem term="ordered traversal snapshot" definition="先固定sorted keys/values并声明mutation可见性的遍历快照。" />
  <GlossaryItem term="callback-driven iterator" definition="Traversal主动调用consumer并规定early stop、error和cleanup的迭代器。" />
</Glossary>

<Attribution adaptedFrom="Programming in Lua, Fourth Edition, Chapter 18: Iterators and the Generic for" adaptedUrl="https://www.lua.org/pil/" />
