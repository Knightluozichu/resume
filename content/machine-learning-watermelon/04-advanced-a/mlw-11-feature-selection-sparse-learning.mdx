---
title: "第11章 特征选择与稀疏学习"
type: B
section: "第三部分 · 高级主题 I"
order: 11
description: "第11章 特征选择与稀疏学习覆盖8个权威目录层级，以模型机制、数学推导、交互实验和失败对照重建核心知识。"
demo: true
math: true
sourceUrl: "https://www.tup.tsinghua.edu.cn/bookscenter/book_06402703.html"
draft: false
---

import {
  Mlw11FeatureSelectionSparseLearningMapLab,
  Mlw11FeatureSelectionSparseLearningExperimentLab,
  Mlw11FeatureSelectionSparseLearningEvidenceLab,
} from "@/components/mdx/machine-learning-watermelon/diagrams/mlw-11-feature-selection-sparse-learning";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第11章 特征选择与稀疏学习的任务、假设、模型状态与适用边界
- 能计算经验风险、目标函数、更新量和独立评价中的关键数值
- 能复现三个互动视图，并在受控实验中定位第一条中间证据分叉
- 能判断“任何特征评分、子集搜索、字典和正则强度只能在训练折拟合；相关特征下入选集合不稳定时不能把一次非零系数解释为因果。”是否成立

</Objectives>

## 为什么从问题合同开始

<Term def="第11章 特征选择与稀疏学习是本页第1个核查坐标，用于连接定义、计算、验证和失效边界。">第11章 特征选择与稀疏学习</Term>、<Term def="11.1 子集搜索与评价是本页第2个核查坐标，用于连接定义、计算、验证和失效边界。">11.1 子集搜索与评价</Term>、<Term def="11.2 过滤式选择是本页第3个核查坐标，用于连接定义、计算、验证和失效边界。">11.2 过滤式选择</Term>、<Term def="11.3 包裹式选择是本页第4个核查坐标，用于连接定义、计算、验证和失效边界。">11.3 包裹式选择</Term>、<Term def="11.4 嵌入式选择与L1正则化是本页第5个核查坐标，用于连接定义、计算、验证和失效边界。">11.4 嵌入式选择与L1正则化</Term>、<Term def="11.5 稀疏表示与字典学习是本页第6个核查坐标，用于连接定义、计算、验证和失效边界。">11.5 稀疏表示与字典学习</Term>
共同构成本页的学习坐标。区分特征子集选择与稀疏表示，比较过滤、包裹、嵌入方法，并从L1几何进入字典学习和压缩感知的可恢复条件。

机器学习不是先选流行算法再寻找数据，而是先写清任务对象、可观测属性、目标变量或反馈、数据生成过程、允许使用的信息和错误代价。训练集用于估计模型，验证集用于选择结构与超参数，测试集只在所有决定冻结后评价一次。聚类、半监督和强化学习虽然反馈形式不同，也必须给出对应的独立证据，不能把训练目标本身当作泛化结论。

每次实验记录样本标识、折分策略、预处理拟合范围、特征顺序、随机种子、数值精度、依赖版本和停止规则。模型训练保存每步目标分解、关键状态与更新；模型选择保存所有候选而不是只留最好一次；失败样本与成功样本使用同一管线。这样才能区分概念假设错误、数据泄漏、数值错误、优化未收敛和抽样波动。

阅读时先预测：若只改变一个条件，第一处应该变化的是输入表示、模型状态、目标项、参数更新、预测分布还是评价轨迹？随后运行最小样本并在第一条不一致处停止。继续观察下游准确率通常只会增加噪声。任何修复都清空派生产物并从原始数据重放，防止错误状态污染后续结论。

## 数学骨架与可核查量

经验风险把冻结训练样本上的逐例损失聚合：

$$
\widehat R(f)=\frac{1}{m}\sum_{i=1}^{m}\ell\bigl(f(x_i),y_i\bigr).
$$

结构约束或先验通过正则项进入训练目标：

$$
\mathcal J(\theta)=\widehat R(f_\theta)+\lambda\Omega(\theta).
$$

模型选择在验证数据上完成，测试风险不得参与选择：

$$
\hat\lambda=\arg\min_{\lambda\in\Lambda}\widehat R_{\mathrm{valid}}(f_\lambda),\qquad \widehat R_{\mathrm{test}}(f_{\hat\lambda})\text{ once}.
$$

本页专属可核查关系为：

$$
\min_{\boldsymbol w}\frac{1}{2}\lVert X\boldsymbol w-y\rVert_2^2+\lambda\lVert\boldsymbol w\rVert_1.
$$

L1惩罚的尖角几何使解更易落在坐标轴上形成稀疏系数，lambda控制拟合与稀疏之间的权衡。 实现时同时检查输入输出形状、有限值、约束残差、目标变化方向、概率归一或守恒关系，并用可枚举小样本或有限差分建立独立基线。

## 机制辨析

1. 特征选择保留原属性子集，稀疏表示用少量基向量重构样本。两者都追求简洁，但优化变量和语义不同。
2. 子集搜索在指数空间中寻找候选，常用前向、后向或双向贪心。评价函数应与最终用途一致，并计入子集大小。
3. 过滤式方法先于学习器评分，如相关、互信息或Relief，计算快且通用，但可能遗漏只有组合后才有用的特征。
4. 包裹式方法用学习器验证性能评价子集，能捕获模型交互但成本高且更易过拟合搜索过程，需要嵌套验证。
5. 嵌入式方法把选择放入训练，L1正则是代表。特征尺度、共线性和正则路径影响非零集合，系数稀疏不等于因果充分。
6. 字典学习交替优化字典与稀疏编码，尺度歧义需用列范数约束。重构误差、稀疏度和下游用途应同时检查。
7. 压缩感知从少量线性测量恢复稀疏信号，依赖稀疏性和测量矩阵性质。L0问题困难，L1在特定条件下可等价恢复。
8. 阅读材料连接稳定选择、组稀疏和矩阵分解。报告选择频率、正则路径与外部验证，而不是单次特征名单。

本章机制必须与替代解释分开。例如训练风险下降可能来自容量增加、泄漏、更多计算或随机好运，而不一定来自目标机制；概率输出可能已归一却未校准；凸目标可全局求解却仍受错误特征和分布偏移影响。对照实验只改变一个因素，并让简单基线获得相同数据、调参机会和资源预算。

## 权威目录逐项讲解

### 第11章 特征选择与稀疏学习

特征选择保留原属性子集，稀疏表示用少量基向量重构样本。两者都追求简洁，但优化变量和语义不同。

学习“第11章 特征选择与稀疏学习”时先在“冻结数据合同”冻结输入、样本身份、特征形状、随机状态和适用假设，再进入“计算模型状态”只改变一个条件。至少保存目标值、关键中间量、更新前后状态和独立评价；若最终指标改善但机制预期的第一处变化没有发生，应先排查泄漏、索引、尺度、优化容差和随机性，不能用末端分数覆盖首错。

通过标准包含四层：能够准确解释定义与适用前提；能够在极小样本上手算公式或枚举状态；能够用固定种子复现算法一步；能够构造一个破坏前提的反例并指出停止位置。修复后清空派生特征、缓存、伪标签、模型状态和评价结果，从冻结原始样本完整重放。

### 11.1 子集搜索与评价

子集搜索在指数空间中寻找候选，常用前向、后向或双向贪心。评价函数应与最终用途一致，并计入子集大小。

学习“11.1 子集搜索与评价”时先在“计算模型状态”冻结输入、样本身份、特征形状、随机状态和适用假设，再进入“核对目标与约束”只改变一个条件。至少保存目标值、关键中间量、更新前后状态和独立评价；若最终指标改善但机制预期的第一处变化没有发生，应先排查泄漏、索引、尺度、优化容差和随机性，不能用末端分数覆盖首错。

通过标准包含四层：能够准确解释定义与适用前提；能够在极小样本上手算公式或枚举状态；能够用固定种子复现算法一步；能够构造一个破坏前提的反例并指出停止位置。修复后清空派生特征、缓存、伪标签、模型状态和评价结果，从冻结原始样本完整重放。

### 11.2 过滤式选择

过滤式方法先于学习器评分，如相关、互信息或Relief，计算快且通用，但可能遗漏只有组合后才有用的特征。

学习“11.2 过滤式选择”时先在“核对目标与约束”冻结输入、样本身份、特征形状、随机状态和适用假设，再进入“执行学习或推断”只改变一个条件。至少保存目标值、关键中间量、更新前后状态和独立评价；若最终指标改善但机制预期的第一处变化没有发生，应先排查泄漏、索引、尺度、优化容差和随机性，不能用末端分数覆盖首错。

通过标准包含四层：能够准确解释定义与适用前提；能够在极小样本上手算公式或枚举状态；能够用固定种子复现算法一步；能够构造一个破坏前提的反例并指出停止位置。修复后清空派生特征、缓存、伪标签、模型状态和评价结果，从冻结原始样本完整重放。

### 11.3 包裹式选择

包裹式方法用学习器验证性能评价子集，能捕获模型交互但成本高且更易过拟合搜索过程，需要嵌套验证。

学习“11.3 包裹式选择”时先在“执行学习或推断”冻结输入、样本身份、特征形状、随机状态和适用假设，再进入“独立评估泛化”只改变一个条件。至少保存目标值、关键中间量、更新前后状态和独立评价；若最终指标改善但机制预期的第一处变化没有发生，应先排查泄漏、索引、尺度、优化容差和随机性，不能用末端分数覆盖首错。

通过标准包含四层：能够准确解释定义与适用前提；能够在极小样本上手算公式或枚举状态；能够用固定种子复现算法一步；能够构造一个破坏前提的反例并指出停止位置。修复后清空派生特征、缓存、伪标签、模型状态和评价结果，从冻结原始样本完整重放。

### 11.4 嵌入式选择与L1正则化

嵌入式方法把选择放入训练，L1正则是代表。特征尺度、共线性和正则路径影响非零集合，系数稀疏不等于因果充分。

学习“11.4 嵌入式选择与L1正则化”时先在“独立评估泛化”冻结输入、样本身份、特征形状、随机状态和适用假设，再进入“定位首个分叉”只改变一个条件。至少保存目标值、关键中间量、更新前后状态和独立评价；若最终指标改善但机制预期的第一处变化没有发生，应先排查泄漏、索引、尺度、优化容差和随机性，不能用末端分数覆盖首错。

通过标准包含四层：能够准确解释定义与适用前提；能够在极小样本上手算公式或枚举状态；能够用固定种子复现算法一步；能够构造一个破坏前提的反例并指出停止位置。修复后清空派生特征、缓存、伪标签、模型状态和评价结果，从冻结原始样本完整重放。

### 11.5 稀疏表示与字典学习

字典学习交替优化字典与稀疏编码，尺度歧义需用列范数约束。重构误差、稀疏度和下游用途应同时检查。

学习“11.5 稀疏表示与字典学习”时先在“定位首个分叉”冻结输入、样本身份、特征形状、随机状态和适用假设，再进入“冻结数据合同”只改变一个条件。至少保存目标值、关键中间量、更新前后状态和独立评价；若最终指标改善但机制预期的第一处变化没有发生，应先排查泄漏、索引、尺度、优化容差和随机性，不能用末端分数覆盖首错。

通过标准包含四层：能够准确解释定义与适用前提；能够在极小样本上手算公式或枚举状态；能够用固定种子复现算法一步；能够构造一个破坏前提的反例并指出停止位置。修复后清空派生特征、缓存、伪标签、模型状态和评价结果，从冻结原始样本完整重放。

### 11.6 压缩感知

压缩感知从少量线性测量恢复稀疏信号，依赖稀疏性和测量矩阵性质。L0问题困难，L1在特定条件下可等价恢复。

学习“11.6 压缩感知”时先在“冻结数据合同”冻结输入、样本身份、特征形状、随机状态和适用假设，再进入“计算模型状态”只改变一个条件。至少保存目标值、关键中间量、更新前后状态和独立评价；若最终指标改善但机制预期的第一处变化没有发生，应先排查泄漏、索引、尺度、优化容差和随机性，不能用末端分数覆盖首错。

通过标准包含四层：能够准确解释定义与适用前提；能够在极小样本上手算公式或枚举状态；能够用固定种子复现算法一步；能够构造一个破坏前提的反例并指出停止位置。修复后清空派生特征、缓存、伪标签、模型状态和评价结果，从冻结原始样本完整重放。

### 11.7 阅读材料

阅读材料连接稳定选择、组稀疏和矩阵分解。报告选择频率、正则路径与外部验证，而不是单次特征名单。

学习“11.7 阅读材料”时先在“计算模型状态”冻结输入、样本身份、特征形状、随机状态和适用假设，再进入“核对目标与约束”只改变一个条件。至少保存目标值、关键中间量、更新前后状态和独立评价；若最终指标改善但机制预期的第一处变化没有发生，应先排查泄漏、索引、尺度、优化容差和随机性，不能用末端分数覆盖首错。

通过标准包含四层：能够准确解释定义与适用前提；能够在极小样本上手算公式或枚举状态；能够用固定种子复现算法一步；能够构造一个破坏前提的反例并指出停止位置。修复后清空派生特征、缓存、伪标签、模型状态和评价结果，从冻结原始样本完整重放。

## 三步交互实验

先预测数据量、模型复杂度或噪声改变后，训练风险、验证风险和第一处分叉会如何移动。交互控件用于检查趋势和边界，不用于挑选一次最好看的结果。

<Stepper>
  <Step title="画出任务与模型地图">
    标出数据、假设、目标、学习、推断、评价与失败边界的生产者和消费者。
    <Mlw11FeatureSelectionSparseLearningMapLab />
  </Step>
  <Step title="运行单变量实验">
    构造含冗余、相关和纯噪声特征的数据，比较互信息过滤、递归包裹和L1嵌入；在重复折分中记录选择频率与验证风险。
    <Mlw11FeatureSelectionSparseLearningExperimentLab />
  </Step>
  <Step title="核对独立证据">
    并排比较训练与验证风险、复杂度、约束残差和首错位置；条件不满足时从原始样本重放。
    <Mlw11FeatureSelectionSparseLearningEvidenceLab />
  </Step>
</Stepper>

## 常见失败与边界

<Callout type="trap">
  **把最终指标当作机制证明：**
  先用全数据选特征再交叉验证、未标准化就比较L1系数，或把相关特征中一次被选中的变量当因果解释，都会夸大稳定性。
  必须检查机制预期的第一条中间证据，并与简单基线和失败样本对照。
</Callout>

<Callout type="trap">
  **让测试信息进入选择过程：**
  在全数据上拟合预处理、选特征、调阈值或决定停止轮次，都会使测试分数乐观。所有数据依赖决策必须嵌套在训练与验证内部。
</Callout>

<Callout type="tip">
  **最小复现顺序：**
  先手算或枚举极小样本，再核对程序中间量，最后扩大数据。任何形状偏差、非有限值、约束残差、概率不归一、数值梯度失败或测试泄漏都应立即停止。
</Callout>

## 本章回顾

区分特征子集选择与稀疏表示，比较过滤、包裹、嵌入方法，并从L1几何进入字典学习和压缩感知的可恢复条件。 验收时从冻结数据预测中间状态、目标与更新，解释结构或分布条件为何改变答案，并在失败样本中指出第一处违背前提的位置。最终底线是：任何特征评分、子集搜索、字典和正则强度只能在训练折拟合；相关特征下入选集合不稳定时不能把一次非零系数解释为因果。

<Glossary>

<GlossaryItem term="第11章 特征选择与稀疏学习">
  “第11章
  特征选择与稀疏学习”对应“冻结数据合同”：记录输入定义、数学假设、中间状态、数值容差、独立证据和失效边界，不能只保留最终分数。
</GlossaryItem>

<GlossaryItem term="11.1 子集搜索与评价">
  “11.1
  子集搜索与评价”对应“计算模型状态”：记录输入定义、数学假设、中间状态、数值容差、独立证据和失效边界，不能只保留最终分数。
</GlossaryItem>

<GlossaryItem term="11.2 过滤式选择">
  “11.2
  过滤式选择”对应“核对目标与约束”：记录输入定义、数学假设、中间状态、数值容差、独立证据和失效边界，不能只保留最终分数。
</GlossaryItem>

<GlossaryItem term="11.3 包裹式选择">
  “11.3
  包裹式选择”对应“执行学习或推断”：记录输入定义、数学假设、中间状态、数值容差、独立证据和失效边界，不能只保留最终分数。
</GlossaryItem>

<GlossaryItem term="11.4 嵌入式选择与L1正则化">
  “11.4
  嵌入式选择与L1正则化”对应“独立评估泛化”：记录输入定义、数学假设、中间状态、数值容差、独立证据和失效边界，不能只保留最终分数。
</GlossaryItem>

<GlossaryItem term="11.5 稀疏表示与字典学习">
  “11.5
  稀疏表示与字典学习”对应“定位首个分叉”：记录输入定义、数学假设、中间状态、数值容差、独立证据和失效边界，不能只保留最终分数。
</GlossaryItem>

</Glossary>

<Exercises>

1. 如何为第11章 特征选择与稀疏学习建立不泄漏测试信息的最小复现实验？

<Answer>
先冻结样本身份与数据合同，按任务结构划分训练、验证和测试。预处理、特征、模型、超参数、阈值和停止规则只在训练与验证中确定；保存“冻结数据合同 -> 计算模型状态 -> 核对目标与约束 -> 执行学习或推断 -> 独立评估泛化 -> 定位首个分叉”的中间证据，全部选择完成后测试集只使用一次。
</Answer>

2. 如何判断一个复杂方法相对简单基线的提升来自本章机制，而不是预算、泄漏或随机波动？

<Answer>
  保持数据折分、预处理、评价指标和资源预算可比，只替换一个结构或学习条件。先写出根据方程应最先变化的中间量，再比较多折或多种子的验证分布；若首个变化不符合机制，先修复实现，不采信末端分数。
</Answer>

3. 为“任何特征评分、子集搜索、字典和正则强度只能在训练折拟合；相关特征下入选集合不稳定时不能把一次非零系数解释为因果。”设计一个失败样本，并说明应在何处停止。

<Answer>
  先用全数据选特征再交叉验证、未标准化就比较L1系数，或把相关特征中一次被选中的变量当因果解释，都会夸大稳定性。
  运行前先预测它会破坏数据合同、目标、约束、更新还是评价；在首个不一致处停止并保留原始证据，清空下游产物后从冻结输入重放。
</Answer>

</Exercises>

<Attribution
  adaptedFrom="周志华著《机器学习》，清华大学出版社，2016，425页，ISBN 9787302423287 · 基于清华大学出版社官方16章三部分结构与作者官方完整目录PDF的独立教学重构"
  adaptedUrl="https://www.tup.tsinghua.edu.cn/bookscenter/book_06402703.html"
/>
