---
title: "Chapter 3 Bitcoin Core: The Reference Implementation"
type: C
section: "Mastering Bitcoin（第3版）"
order: 4
description: "覆盖Bitcoin Core来源、开发环境、源码构建、节点配置、RPC探索与替代客户端工具链。"
demo: true
math: true
sourceUrl: "https://github.com/bitcoinbook/bitcoinbook/tree/third_edition_print1"
draft: false
---


## 为什么从可验证状态开始

Bitcoin Core是参考实现而非协议的唯一文本。可复现节点要冻结源码标签、依赖、构建选项、网络、数据目录和RPC暴露面，并用交易、区块与节点状态查询证明它运行在预期上下文。

比特币最容易被界面和术语掩盖：钱包余额是UTXO视图，地址是脚本编码，交易哈希只是候选对象标识，矿工出块也不代表全节点必须接受。要判断实现是否正确，必须追踪前状态、被引用输出、规范序列化、脚本与签名、节点策略、区块承诺、累计工作量和后状态。只保存最终余额或区块高度，会漏掉错误网络、冲突交易、脚本失败、重组与索引视图滞后。

本页先手算最小实例，再运行工具。正常样本证明主路径；边界样本取零值、最大金额、恰好费用、最短证明或时间锁边界；单故障样本只改变网络前缀、字节、签名、outpoint、脚本、target或确认中的一项。三组样本共享源码标签、节点、创世状态和观察点，使首个差异可以归因，而不是靠重复广播或刷新页面碰运气。

每次实验生成机器可读证据：官方印刷标签、软件提交、网络与创世哈希、输入原文、解析对象、验证结果、错误、前后UTXO或链状态和确认区块。独立复核者从空目录重放，若字段缺失、节点未同步或结果绑定到不同哈希，门禁明确失败。涉及密钥的示例只使用公开测试向量、regtest或signet，不把教学代码连接真实资产。

官方标题层级还要转成可检查任务：章标题定义端到端目标，节标题定义机制与对象，小节标题定义算法、字段、异常或部署选择。每个标题至少对应一个输入、一个不变量、一个可观察结果和一个失败样本；多个标题可以共享实验环境，但不能因主题相近而合并掉独立语义。这样既保留原书结构，也让读者能从目录逐项证明掌握程度。

## 版次、对象与验收合同

、、、、共同构成本页复核坐标。覆盖Bitcoin
Core来源、开发环境、源码构建、节点配置、RPC探索与替代客户端工具链。

本书锁定Andreas M. Antonopoulos、David A. Harding《Mastering Bitcoin: Programming the Open Blockchain》第3版，O'Reilly Media，2023年，402页，ISBN 9781098150082。官方GitHub标签 third_edition_print1 对应第三版首印，公开前言、14章和附录A至C的完整源码，按二至五级标题解析得到333个正式节点；O'Reilly页面交叉核对作者、2023年、402页与ISBN。学习地图和总复习不计入原书分母。本文按CC BY-SA 4.0来源做独立中文教学重构，不复制原书正文。


## 核心约束与推导

$$
\sum value(inputs)=\sum value(outputs)+fee
$$

$$
H_i=hash(H_{i-1} \Vert merkle(T_i) \Vert metadata_i)
$$

$$
S_{t+1}=apply(S_t,T_t), \quad valid(T_t,S_t)=1
$$

$$
gate=source \times version \times replay \times boundary \times failureEvidence
$$

前两式对应本页的密码、交易、费用、网络、区块或二层核心不变量。状态转移式要求候选对象先通过当前状态验证，再原子地产生后状态；失败保留原状态。证据门禁使用乘法语义，来源、版本、重放、边界或失败证据任一缺失，整条结论都不能发布。

## 三段可运行骨架

以下代码展示比特币验证的两层视角：TypeScript 段把节点重放证据统一为结构，Python 段证明拒绝路径不污染原状态，再校验最小协议对象，作为 Bitcoin Core 构建、配置与 RPC 探索的最小可复现单元。

<CodeTabs>
  <Tab label="TypeScript / 证据门禁">

```typescript
type Evidence = {
  version: string;
  before: string;
  input: string;
  after?: string;
  error?: string;
};

function evidenceGate(run: Evidence) {
  const complete = Boolean(
    run.version && run.before && run.input && (run.after || run.error),
  );
  return { complete, page: "mbt3-03-bitcoin-core", run };
}
```

  </Tab>
  <Tab label="Python / 状态验证">

```python
def apply_candidate(state, candidate, verify):
    if not verify(state, candidate):
        return {"accepted": False, "state": state, "reason": "invalid"}
    next_state = state.copy()
    next_state.update(candidate["changes"])
    return {"accepted": True, "state": next_state, "reason": None}

def verify_value_conservation(inputs, outputs, fee):
    if len(inputs) != len({item["outpoint"] for item in inputs}):
        return False
    return sum(item["value"] for item in inputs) == sum(outputs) + fee
```

  </Tab>
</CodeTabs>

第一段把成功与失败放进同一证据结构；第二段证明拒绝路径不污染原状态；第三段验证本页最小协议对象。生产系统必须使用成熟密码、序列化和节点库，并补充资源限制、并发、持久化、超时、隐私与密钥隔离。

## 正式标题逐项讲解

### Chapter 3 Bitcoin Core: The Reference Implementation

“Chapter 3 Bitcoin Core: The Reference Implementation”位于本页正式标题坐标 1/23。Bitcoin Core实现P2P、共识验证、内存池、钱包和RPC，但协议由所有兼容节点执行的规则约束。版本选择、构建选项和运行网络必须记录，参考实现也可能有勘误。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能从官方标签构建或验证Bitcoin Core，启动隔离节点，使用RPC解码交易和区块，并证明网络、版本和数据目录正确。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### From Bitcoin to Bitcoin Core

“From Bitcoin to Bitcoin Core”位于本页正式标题坐标 2/23。Bitcoin Core实现P2P、共识验证、内存池、钱包和RPC，但协议由所有兼容节点执行的规则约束。版本选择、构建选项和运行网络必须记录，参考实现也可能有勘误。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能从官方标签构建或验证Bitcoin Core，启动隔离节点，使用RPC解码交易和区块，并证明网络、版本和数据目录正确。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Bitcoin Development Environment

“Bitcoin Development Environment”位于本页正式标题坐标 3/23。可复现构建绑定源码标签、依赖、编译器、目标平台和配置。验证不仅看退出码，还要保存提交哈希、构建日志、二进制摘要和运行时版本。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能从官方标签构建或验证Bitcoin Core，启动隔离节点，使用RPC解码交易和区块，并证明网络、版本和数据目录正确。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Compiling Bitcoin Core from the Source Code

“Compiling Bitcoin Core from the Source Code”位于本页正式标题坐标 4/23。Bitcoin Core实现P2P、共识验证、内存池、钱包和RPC，但协议由所有兼容节点执行的规则约束。版本选择、构建选项和运行网络必须记录，参考实现也可能有勘误。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能从官方标签构建或验证Bitcoin Core，启动隔离节点，使用RPC解码交易和区块，并证明网络、版本和数据目录正确。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Selecting a Bitcoin Core Release

“Selecting a Bitcoin Core Release”位于本页正式标题坐标 5/23。Bitcoin Core实现P2P、共识验证、内存池、钱包和RPC，但协议由所有兼容节点执行的规则约束。版本选择、构建选项和运行网络必须记录，参考实现也可能有勘误。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能从官方标签构建或验证Bitcoin Core，启动隔离节点，使用RPC解码交易和区块，并证明网络、版本和数据目录正确。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Configuring the Bitcoin Core Build

“Configuring the Bitcoin Core Build”位于本页正式标题坐标 6/23。Bitcoin Core实现P2P、共识验证、内存池、钱包和RPC，但协议由所有兼容节点执行的规则约束。版本选择、构建选项和运行网络必须记录，参考实现也可能有勘误。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能从官方标签构建或验证Bitcoin Core，启动隔离节点，使用RPC解码交易和区块，并证明网络、版本和数据目录正确。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Building the Bitcoin Core Executables

“Building the Bitcoin Core Executables”位于本页正式标题坐标 7/23。Bitcoin Core实现P2P、共识验证、内存池、钱包和RPC，但协议由所有兼容节点执行的规则约束。版本选择、构建选项和运行网络必须记录，参考实现也可能有勘误。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能从官方标签构建或验证Bitcoin Core，启动隔离节点，使用RPC解码交易和区块，并证明网络、版本和数据目录正确。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Running a Bitcoin Core Node

“Running a Bitcoin Core Node”位于本页正式标题坐标 8/23。Bitcoin Core实现P2P、共识验证、内存池、钱包和RPC，但协议由所有兼容节点执行的规则约束。版本选择、构建选项和运行网络必须记录，参考实现也可能有勘误。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能从官方标签构建或验证Bitcoin Core，启动隔离节点，使用RPC解码交易和区块，并证明网络、版本和数据目录正确。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Configuring the Bitcoin Core Node

“Configuring the Bitcoin Core Node”位于本页正式标题坐标 9/23。Bitcoin Core实现P2P、共识验证、内存池、钱包和RPC，但协议由所有兼容节点执行的规则约束。版本选择、构建选项和运行网络必须记录，参考实现也可能有勘误。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能从官方标签构建或验证Bitcoin Core，启动隔离节点，使用RPC解码交易和区块，并证明网络、版本和数据目录正确。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Bitcoin Core API

“Bitcoin Core API”位于本页正式标题坐标 10/23。Bitcoin Core实现P2P、共识验证、内存池、钱包和RPC，但协议由所有兼容节点执行的规则约束。版本选择、构建选项和运行网络必须记录，参考实现也可能有勘误。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能从官方标签构建或验证Bitcoin Core，启动隔离节点，使用RPC解码交易和区块，并证明网络、版本和数据目录正确。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Getting Information on Bitcoin Core's Status

“Getting Information on Bitcoin Core's Status”位于本页正式标题坐标 11/23。Bitcoin Core实现P2P、共识验证、内存池、钱包和RPC，但协议由所有兼容节点执行的规则约束。版本选择、构建选项和运行网络必须记录，参考实现也可能有勘误。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能从官方标签构建或验证Bitcoin Core，启动隔离节点，使用RPC解码交易和区块，并证明网络、版本和数据目录正确。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Exploring and Decoding Transactions

“Exploring and Decoding Transactions”位于本页正式标题坐标 12/23。RPC调用要记录方法、参数、节点版本、区块上下文、原始结果和错误对象。解码只解释字节，不证明交易有效；查询latest也可能随重组变化，审计使用固定哈希。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能从官方标签构建或验证Bitcoin Core，启动隔离节点，使用RPC解码交易和区块，并证明网络、版本和数据目录正确。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Exploring Blocks

“Exploring Blocks”位于本页正式标题坐标 13/23。RPC调用要记录方法、参数、节点版本、区块上下文、原始结果和错误对象。解码只解释字节，不证明交易有效；查询latest也可能随重组变化，审计使用固定哈希。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能从官方标签构建或验证Bitcoin Core，启动隔离节点，使用RPC解码交易和区块，并证明网络、版本和数据目录正确。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Using Bitcoin Core's Programmatic Interface

“Using Bitcoin Core's Programmatic Interface”位于本页正式标题坐标 14/23。Bitcoin Core实现P2P、共识验证、内存池、钱包和RPC，但协议由所有兼容节点执行的规则约束。版本选择、构建选项和运行网络必须记录，参考实现也可能有勘误。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能从官方标签构建或验证Bitcoin Core，启动隔离节点，使用RPC解码交易和区块，并证明网络、版本和数据目录正确。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Alternative Clients, Libraries, and Toolkits

“Alternative Clients, Libraries, and Toolkits”位于本页正式标题坐标 15/23。Bitcoin Core是参考实现而非协议的唯一文本。可复现节点要冻结源码标签、依赖、构建选项、网络、数据目录和RPC暴露面，并用交易、区块与节点状态查询证明它运行在预期上下文。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能从官方标签构建或验证Bitcoin Core，启动隔离节点，使用RPC解码交易和区块，并证明网络、版本和数据目录正确。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### C/C++

“C/C++”位于本页正式标题坐标 16/23。“C/C++”工具链可以构造、解析或查询比特币对象，但库默认网络、地址格式和序列化行为可能不同。选型依据维护状态、测试向量和依赖锁，而不是语言偏好。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能从官方标签构建或验证Bitcoin Core，启动隔离节点，使用RPC解码交易和区块，并证明网络、版本和数据目录正确。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### JavaScript

“JavaScript”位于本页正式标题坐标 17/23。“JavaScript”工具链可以构造、解析或查询比特币对象，但库默认网络、地址格式和序列化行为可能不同。选型依据维护状态、测试向量和依赖锁，而不是语言偏好。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能从官方标签构建或验证Bitcoin Core，启动隔离节点，使用RPC解码交易和区块，并证明网络、版本和数据目录正确。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Java

“Java”位于本页正式标题坐标 18/23。“Java”工具链可以构造、解析或查询比特币对象，但库默认网络、地址格式和序列化行为可能不同。选型依据维护状态、测试向量和依赖锁，而不是语言偏好。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能从官方标签构建或验证Bitcoin Core，启动隔离节点，使用RPC解码交易和区块，并证明网络、版本和数据目录正确。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Python

“Python”位于本页正式标题坐标 19/23。“Python”工具链可以构造、解析或查询比特币对象，但库默认网络、地址格式和序列化行为可能不同。选型依据维护状态、测试向量和依赖锁，而不是语言偏好。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能从官方标签构建或验证Bitcoin Core，启动隔离节点，使用RPC解码交易和区块，并证明网络、版本和数据目录正确。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Go

“Go”位于本页正式标题坐标 20/23。“Go”工具链可以构造、解析或查询比特币对象，但库默认网络、地址格式和序列化行为可能不同。选型依据维护状态、测试向量和依赖锁，而不是语言偏好。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能从官方标签构建或验证Bitcoin Core，启动隔离节点，使用RPC解码交易和区块，并证明网络、版本和数据目录正确。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Rust

“Rust”位于本页正式标题坐标 21/23。“Rust”工具链可以构造、解析或查询比特币对象，但库默认网络、地址格式和序列化行为可能不同。选型依据维护状态、测试向量和依赖锁，而不是语言偏好。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能从官方标签构建或验证Bitcoin Core，启动隔离节点，使用RPC解码交易和区块，并证明网络、版本和数据目录正确。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Scala

“Scala”位于本页正式标题坐标 22/23。“Scala”工具链可以构造、解析或查询比特币对象，但库默认网络、地址格式和序列化行为可能不同。选型依据维护状态、测试向量和依赖锁，而不是语言偏好。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能从官方标签构建或验证Bitcoin Core，启动隔离节点，使用RPC解码交易和区块，并证明网络、版本和数据目录正确。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### C#

“C#”位于本页正式标题坐标 23/23。“C#”工具链可以构造、解析或查询比特币对象，但库默认网络、地址格式和序列化行为可能不同。选型依据维护状态、测试向量和依赖锁，而不是语言偏好。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能从官方标签构建或验证Bitcoin Core，启动隔离节点，使用RPC解码交易和区块，并证明网络、版本和数据目录正确。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

## 正常、边界与故障矩阵

| 样本   | 只改变的变量                    | 预期结果               | 必存证据                     |
| ------ | ------------------------------- | ---------------------- | ---------------------------- |
| 正常   | 有效UTXO、脚本、费用与网络      | 状态转移并达到确认条件 | 原始对象、前后状态、区块哈希 |
| 边界   | 零值、最大值、恰好阈值或时间锁  | 按明确规则接受或拒绝   | 边界值、判断分支、资源成本   |
| 单故障 | 网络、字节、签名、引用或PoW一项 | 首个无效规则处拒绝     | 错误、首差、原状态与回退点   |

先写出预期字节、栈、金额或哈希，再运行节点和解析器。若钱包、区块浏览器与本地节点给出不同答案，以固定网络上的原始交易、被花费输出、区块头和本地共识验证为坐标；第三方展示不能覆盖本地可验证事实。

## 容易踩的坑

**现象 →** 用 `git clone` 默认分支构建，不锁标签。
**原因 →** 默认分支会随上游变动，导致构建结果不可复现，结论绑定不到固定哈希。
**修法 →** 显式 `git checkout third_edition_print1` 或对应 release 标签，保存提交哈希、构建日志与二进制摘要。你可以亲自试一下：同一标签在两台机器构建，比对二进制摘要是否一致。

**现象 →** 把 RPC 的 `decoderawtransaction` 结果当成交易有效性证明。
**原因 →** 解码只解释字节，不证明交易合法，也不检查被引用输出与共识规则。
**修法 →** 解码后还要用 `testmempoolaccept` 或全节点验证，并记录节点版本与区块上下文。

**现象 →** 在主网数据目录跑教学 RPC，误广播真实交易。
**原因 →** 数据目录混用，测试与主网配置没有物理隔离。
**修法 →** 为教学用隔离的 regtest/signet 数据目录与配置文件，主网节点 RPC 暴露面最小化并加认证。

## 小结

- Bitcoin Core 是参考实现，协议由所有兼容节点约束
- 可复现构建要冻结标签、依赖、编译器与配置
- RPC 解码只解释字节，不证明交易有效
- 查询 latest 会随重组变化，审计用固定哈希
- 替代客户端选型看维护状态、测试向量与依赖锁

<Exercises>

**问题 1**
为什么"Bitcoin Core 是参考实现"不等于"Bitcoin Core 就是协议"？

<Answer>

协议由所有兼容节点执行的共识规则约束，Bitcoin Core 只是其中一种实现；参考实现也可能有勘误或策略选择，版本、构建选项和运行网络必须记录，并以 BIP 和后续修正交叉验证。

</Answer>

**问题 2**
独立实现一个最小 RPC 封装 `callRpc(method, params, nodeVersion, blockContext)`：返回包含方法、参数、节点版本、区块上下文、原始结果与错误的对象。要求成功与失败都返回完整证据。

<Answer>

参考骨架：`return { method, params, nodeVersion, blockContext, result, error }`，失败时 result 为 null 并填充 error。关键点是 RPC 调用必须记录节点版本与区块上下文，解码结果不等于有效性证明，查询 latest 要随重组变化因此审计用固定哈希。

</Answer>

**问题 3**
为什么不同语言的比特币库默认网络、地址格式和序列化行为可能不同？选型时应依据什么？

<Answer>

各库的设计目标与默认配置不同，有的默认主网、有的默认 testnet，地址编码与序列化也可能差异；选型应依据维护状态、测试向量覆盖与依赖锁，而不是语言偏好，并在使用前显式指定网络与脚本类型。

</Answer>

</Exercises>

## 本章回顾

覆盖Bitcoin Core来源、开发环境、源码构建、节点配置、RPC探索与替代客户端工具链。 Bitcoin Core是参考实现而非协议的唯一文本。可复现节点要冻结源码标签、依赖、构建选项、网络、数据目录和RPC暴露面，并用交易、区块与节点状态查询证明它运行在预期上下文。 掌握标准是能从官方标签构建或验证Bitcoin Core，启动隔离节点，使用RPC解码交易和区块，并证明网络、版本和数据目录正确。。独立复核必须从官方第三版标签、固定测试网络和原始协议对象重放正常、边界与单故障样本。


## 前后导航

- [上一页：Chapter 2 How Bitcoin Works](/learn/mastering-bitcoin/03-02-how-bitcoin-works/mbt3-02-how-bitcoin-works)
- [下一页：Chapter 4 Keys and Addresses](/learn/mastering-bitcoin/05-04-keys-addresses/mbt3-04-keys-addresses)

<Attribution
  mode="independent-rewrite"
  sourceBasis="publisher-catalog"
  workTitle="Andreas M. Antonopoulos《Mastering Bitcoin》3rd Edition / O'Reilly / 2023"
  adaptedUrl="https://github.com/bitcoinbook/bitcoinbook/tree/third_edition_print1"
/>

