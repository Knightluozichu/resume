---
title: "Chapter 4 Keys and Addresses"
type: C
section: "Mastering Bitcoin（第3版）"
order: 5
description: "从私钥、公钥和脚本进入P2PK、P2PKH、P2SH、Base58Check、Bech32、Bech32m及高级地址。"
demo: true
math: true
sourceUrl: "https://github.com/bitcoinbook/bitcoinbook/tree/third_edition_print1"
draft: false
---


## 为什么从可验证状态开始

私钥产生授权能力，公钥和脚本定义可验证条件，地址只是特定输出脚本的网络编码提示。安全性依赖高熵密钥、正确曲线运算、校验编码、网络前缀与离线备份，不能把地址当账户。

比特币最容易被界面和术语掩盖：钱包余额是UTXO视图，地址是脚本编码，交易哈希只是候选对象标识，矿工出块也不代表全节点必须接受。要判断实现是否正确，必须追踪前状态、被引用输出、规范序列化、脚本与签名、节点策略、区块承诺、累计工作量和后状态。只保存最终余额或区块高度，会漏掉错误网络、冲突交易、脚本失败、重组与索引视图滞后。

本页先手算最小实例，再运行工具。正常样本证明主路径；边界样本取零值、最大金额、恰好费用、最短证明或时间锁边界；单故障样本只改变网络前缀、字节、签名、outpoint、脚本、target或确认中的一项。三组样本共享源码标签、节点、创世状态和观察点，使首个差异可以归因，而不是靠重复广播或刷新页面碰运气。

每次实验生成机器可读证据：官方印刷标签、软件提交、网络与创世哈希、输入原文、解析对象、验证结果、错误、前后UTXO或链状态和确认区块。独立复核者从空目录重放，若字段缺失、节点未同步或结果绑定到不同哈希，门禁明确失败。涉及密钥的示例只使用公开测试向量、regtest或signet，不把教学代码连接真实资产。

官方标题层级还要转成可检查任务：章标题定义端到端目标，节标题定义机制与对象，小节标题定义算法、字段、异常或部署选择。每个标题至少对应一个输入、一个不变量、一个可观察结果和一个失败样本；多个标题可以共享实验环境，但不能因主题相近而合并掉独立语义。这样既保留原书结构，也让读者能从目录逐项证明掌握程度。

## 版次、对象与验收合同

、、、、共同构成本页复核坐标。从私钥、公钥和脚本进入P2PK、P2PKH、P2SH、Base58Check、Bech32、Bech32m及高级地址。

本书锁定Andreas M. Antonopoulos、David A. Harding《Mastering Bitcoin: Programming the Open Blockchain》第3版，O'Reilly Media，2023年，402页，ISBN 9781098150082。官方GitHub标签 third_edition_print1 对应第三版首印，公开前言、14章和附录A至C的完整源码，按二至五级标题解析得到333个正式节点；O'Reilly页面交叉核对作者、2023年、402页与ISBN。学习地图和总复习不计入原书分母。本文按CC BY-SA 4.0来源做独立中文教学重构，不复制原书正文。


## 核心约束与推导

$$
K=kG, \quad 1 \le k \lt n
$$

$$
address=encode(network \Vert HASH160(K) \Vert checksum)
$$

$$
S_{t+1}=apply(S_t,T_t), \quad valid(T_t,S_t)=1
$$

$$
gate=source \times version \times replay \times boundary \times failureEvidence
$$

前两式对应本页的密码、交易、费用、网络、区块或二层核心不变量。状态转移式要求候选对象先通过当前状态验证，再原子地产生后状态；失败保留原状态。证据门禁使用乘法语义，来源、版本、重放、边界或失败证据任一缺失，整条结论都不能发布。

## 三段可运行骨架

```typescript
type Evidence = {
  version: string;
  before: string;
  input: string;
  after?: string;
  error?: string;
};

function evidenceGate(run: Evidence) {
  const complete = Boolean(
    run.version && run.before && run.input && (run.after || run.error),
  );
  return { complete, page: "mbt3-04-keys-addresses", run };
}
```

```python
def apply_candidate(state, candidate, verify):
    if not verify(state, candidate):
        return {"accepted": False, "state": state, "reason": "invalid"}
    next_state = state.copy()
    next_state.update(candidate["changes"])
    return {"accepted": True, "state": next_state, "reason": None}
```

```python
from hashlib import new, sha256

def hash160(public_key: bytes) -> bytes:
    return new("ripemd160", sha256(public_key).digest()).digest()
```

第一段把成功与失败放进同一证据结构；第二段证明拒绝路径不污染原状态；第三段验证本页最小协议对象。生产系统必须使用成熟密码、序列化和节点库，并补充资源限制、并发、持久化、超时、隐私与密钥隔离。

## 正式标题逐项讲解

### Chapter 4 Keys and Addresses

“Chapter 4 Keys and Addresses”位于本页正式标题坐标 1/21。地址是输出脚本模板的网络编码提示，不是链上账户。解析先验证字符集、校验和和网络，再转换为脚本；复制错误、网络错误或错误脚本类型都可能造成不可逆支付。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能从固定测试私钥推导公钥和不同地址编码，验证校验和与网络，并解释每种地址对应的输出脚本和失败边界。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Public Key Cryptography

“Public Key Cryptography”位于本页正式标题坐标 2/21。公钥由标量乘法K=kG推导，离散对数困难使反推私钥不可行。压缩编码只保存x坐标与奇偶位，验算仍要检查曲线点有效、子群和规范编码。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能从固定测试私钥推导公钥和不同地址编码，验证校验和与网络，并解释每种地址对应的输出脚本和失败边界。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Private Keys

“Private Keys”位于本页正式标题坐标 3/21。私钥是secp256k1阶范围内的高熵整数。安全生成依赖密码学随机源，导入导出要保持网络和压缩标志；任何日志、截图或在线复制都会扩大不可撤销泄漏面。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能从固定测试私钥推导公钥和不同地址编码，验证校验和与网络，并解释每种地址对应的输出脚本和失败边界。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Elliptic Curve Cryptography Explained

“Elliptic Curve Cryptography Explained”位于本页正式标题坐标 4/21。公钥由标量乘法K=kG推导，离散对数困难使反推私钥不可行。压缩编码只保存x坐标与奇偶位，验算仍要检查曲线点有效、子群和规范编码。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能从固定测试私钥推导公钥和不同地址编码，验证校验和与网络，并解释每种地址对应的输出脚本和失败边界。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Public Keys

“Public Keys”位于本页正式标题坐标 5/21。公钥由标量乘法K=kG推导，离散对数困难使反推私钥不可行。压缩编码只保存x坐标与奇偶位，验算仍要检查曲线点有效、子群和规范编码。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能从固定测试私钥推导公钥和不同地址编码，验证校验和与网络，并解释每种地址对应的输出脚本和失败边界。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Output and Input Scripts

“Output and Input Scripts”位于本页正式标题坐标 6/21。输入引用先前交易哈希和输出索引，并提供满足锁定条件的数据。节点还要检查被引用输出存在、尚未花费、金额有效和脚本成功，不能只验证签名。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能从固定测试私钥推导公钥和不同地址编码，验证校验和与网络，并解释每种地址对应的输出脚本和失败边界。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### IP Addresses: The Original Address for Bitcoin (P2PK)

“IP Addresses: The Original Address for Bitcoin (P2PK)”位于本页正式标题坐标 7/21。地址是输出脚本模板的网络编码提示，不是链上账户。解析先验证字符集、校验和和网络，再转换为脚本；复制错误、网络错误或错误脚本类型都可能造成不可逆支付。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能从固定测试私钥推导公钥和不同地址编码，验证校验和与网络，并解释每种地址对应的输出脚本和失败边界。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Legacy Addresses for P2PKH

“Legacy Addresses for P2PKH”位于本页正式标题坐标 8/21。地址是输出脚本模板的网络编码提示，不是链上账户。解析先验证字符集、校验和和网络，再转换为脚本；复制错误、网络错误或错误脚本类型都可能造成不可逆支付。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能从固定测试私钥推导公钥和不同地址编码，验证校验和与网络，并解释每种地址对应的输出脚本和失败边界。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Base58check Encoding

“Base58check Encoding”位于本页正式标题坐标 9/21。Base58Check把版本、载荷和双SHA256校验片段编码为人类可读字符串。它能发现多数抄写错误但不提供认证，解析后仍要验证网络和载荷长度。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能从固定测试私钥推导公钥和不同地址编码，验证校验和与网络，并解释每种地址对应的输出脚本和失败边界。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Compressed Public Keys

“Compressed Public Keys”位于本页正式标题坐标 10/21。私钥产生授权能力，公钥和脚本定义可验证条件，地址只是特定输出脚本的网络编码提示。安全性依赖高熵密钥、正确曲线运算、校验编码、网络前缀与离线备份，不能把地址当账户。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能从固定测试私钥推导公钥和不同地址编码，验证校验和与网络，并解释每种地址对应的输出脚本和失败边界。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Legacy Pay to Script Hash (P2SH)

“Legacy Pay to Script Hash (P2SH)”位于本页正式标题坐标 11/21。Script以栈机组合数据和操作码，输出脚本定义条件，输入或见证提供满足数据。P2SH承诺赎回脚本，多签要同时检查阈值、密钥顺序和历史CHECKMULTISIG额外栈项。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能从固定测试私钥推导公钥和不同地址编码，验证校验和与网络，并解释每种地址对应的输出脚本和失败边界。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Bech32 Addresses

“Bech32 Addresses”位于本页正式标题坐标 12/21。地址是输出脚本模板的网络编码提示，不是链上账户。解析先验证字符集、校验和和网络，再转换为脚本；复制错误、网络错误或错误脚本类型都可能造成不可逆支付。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能从固定测试私钥推导公钥和不同地址编码，验证校验和与网络，并解释每种地址对应的输出脚本和失败边界。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Problems with Bech32 Addresses

“Problems with Bech32 Addresses”位于本页正式标题坐标 13/21。地址是输出脚本模板的网络编码提示，不是链上账户。解析先验证字符集、校验和和网络，再转换为脚本；复制错误、网络错误或错误脚本类型都可能造成不可逆支付。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能从固定测试私钥推导公钥和不同地址编码，验证校验和与网络，并解释每种地址对应的输出脚本和失败边界。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Bech32m

“Bech32m”位于本页正式标题坐标 14/21。Bech32和Bech32m用人类可读前缀、数据部分和多项式校验编码见证程序。见证版本决定应使用哪种常量；混用算法会产生看似合法但不可接受的地址。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能从固定测试私钥推导公钥和不同地址编码，验证校验和与网络，并解释每种地址对应的输出脚本和失败边界。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Private Key Formats

“Private Key Formats”位于本页正式标题坐标 15/21。私钥是secp256k1阶范围内的高熵整数。安全生成依赖密码学随机源，导入导出要保持网络和压缩标志；任何日志、截图或在线复制都会扩大不可撤销泄漏面。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能从固定测试私钥推导公钥和不同地址编码，验证校验和与网络，并解释每种地址对应的输出脚本和失败边界。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Compressed Private Keys

“Compressed Private Keys”位于本页正式标题坐标 16/21。私钥是secp256k1阶范围内的高熵整数。安全生成依赖密码学随机源，导入导出要保持网络和压缩标志；任何日志、截图或在线复制都会扩大不可撤销泄漏面。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能从固定测试私钥推导公钥和不同地址编码，验证校验和与网络，并解释每种地址对应的输出脚本和失败边界。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Advanced Keys and Addresses

“Advanced Keys and Addresses”位于本页正式标题坐标 17/21。地址是输出脚本模板的网络编码提示，不是链上账户。解析先验证字符集、校验和和网络，再转换为脚本；复制错误、网络错误或错误脚本类型都可能造成不可逆支付。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能从固定测试私钥推导公钥和不同地址编码，验证校验和与网络，并解释每种地址对应的输出脚本和失败边界。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Vanity Addresses

“Vanity Addresses”位于本页正式标题坐标 18/21。地址是输出脚本模板的网络编码提示，不是链上账户。解析先验证字符集、校验和和网络，再转换为脚本；复制错误、网络错误或错误脚本类型都可能造成不可逆支付。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能从固定测试私钥推导公钥和不同地址编码，验证校验和与网络，并解释每种地址对应的输出脚本和失败边界。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Generating vanity addresses

“Generating vanity addresses”位于本页正式标题坐标 19/21。地址是输出脚本模板的网络编码提示，不是链上账户。解析先验证字符集、校验和和网络，再转换为脚本；复制错误、网络错误或错误脚本类型都可能造成不可逆支付。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能从固定测试私钥推导公钥和不同地址编码，验证校验和与网络，并解释每种地址对应的输出脚本和失败边界。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Vanity address security and privacy

“Vanity address security and privacy”位于本页正式标题坐标 20/21。地址是输出脚本模板的网络编码提示，不是链上账户。解析先验证字符集、校验和和网络，再转换为脚本；复制错误、网络错误或错误脚本类型都可能造成不可逆支付。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能从固定测试私钥推导公钥和不同地址编码，验证校验和与网络，并解释每种地址对应的输出脚本和失败边界。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Paper Wallets

“Paper Wallets”位于本页正式标题坐标 21/21。钱包管理密钥、派生路径和交易构造，并不存放链上币。恢复需要种子或恢复码、可选口令、派生路径、脚本类型和账户元数据一致；托管钱包则把控制权交给第三方。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能从固定测试私钥推导公钥和不同地址编码，验证校验和与网络，并解释每种地址对应的输出脚本和失败边界。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

## 正常、边界与故障矩阵

| 样本   | 只改变的变量                    | 预期结果               | 必存证据                     |
| ------ | ------------------------------- | ---------------------- | ---------------------------- |
| 正常   | 有效UTXO、脚本、费用与网络      | 状态转移并达到确认条件 | 原始对象、前后状态、区块哈希 |
| 边界   | 零值、最大值、恰好阈值或时间锁  | 按明确规则接受或拒绝   | 边界值、判断分支、资源成本   |
| 单故障 | 网络、字节、签名、引用或PoW一项 | 首个无效规则处拒绝     | 错误、首差、原状态与回退点   |

先写出预期字节、栈、金额或哈希，再运行节点和解析器。若钱包、区块浏览器与本地节点给出不同答案，以固定网络上的原始交易、被花费输出、区块头和本地共识验证为坐标；第三方展示不能覆盖本地可验证事实。

## 常见失败与回退


## 本章回顾

从私钥、公钥和脚本进入P2PK、P2PKH、P2SH、Base58Check、Bech32、Bech32m及高级地址。 私钥产生授权能力，公钥和脚本定义可验证条件，地址只是特定输出脚本的网络编码提示。安全性依赖高熵密钥、正确曲线运算、校验编码、网络前缀与离线备份，不能把地址当账户。 掌握标准是能从固定测试私钥推导公钥和不同地址编码，验证校验和与网络，并解释每种地址对应的输出脚本和失败边界。。独立复核必须从官方第三版标签、固定测试网络和原始协议对象重放正常、边界与单故障样本。


## 前后导航

- [上一页：Chapter 3 Bitcoin Core: The Reference Implementation](/learn/mastering-bitcoin/04-03-bitcoin-core/mbt3-03-bitcoin-core)
- [下一页：Chapter 5 Wallet Recovery](/learn/mastering-bitcoin/06-05-wallet-recovery/mbt3-05-wallet-recovery)

