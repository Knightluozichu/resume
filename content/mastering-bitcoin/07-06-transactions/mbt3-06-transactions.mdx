---
title: "Chapter 6 Transactions"
type: C
section: "Mastering Bitcoin（第3版）"
order: 7
description: "逐字段解释序列化交易、输入、输出、见证、延展性、锁定时间、coinbase、权重与vbytes。"
demo: true
math: true
sourceUrl: "https://github.com/bitcoinbook/bitcoinbook/tree/third_edition_print1"
draft: false
---


## 为什么从可验证状态开始

交易ID和签名都绑定规范序列化。版本、标记、输入输出计数、outpoint、脚本、sequence、见证与锁定时间各有独立语义；解析器必须先检查长度和边界，再解释脚本与价值。

比特币最容易被界面和术语掩盖：钱包余额是UTXO视图，地址是脚本编码，交易哈希只是候选对象标识，矿工出块也不代表全节点必须接受。要判断实现是否正确，必须追踪前状态、被引用输出、规范序列化、脚本与签名、节点策略、区块承诺、累计工作量和后状态。只保存最终余额或区块高度，会漏掉错误网络、冲突交易、脚本失败、重组与索引视图滞后。

本页先手算最小实例，再运行工具。正常样本证明主路径；边界样本取零值、最大金额、恰好费用、最短证明或时间锁边界；单故障样本只改变网络前缀、字节、签名、outpoint、脚本、target或确认中的一项。三组样本共享源码标签、节点、创世状态和观察点，使首个差异可以归因，而不是靠重复广播或刷新页面碰运气。

每次实验生成机器可读证据：官方印刷标签、软件提交、网络与创世哈希、输入原文、解析对象、验证结果、错误、前后UTXO或链状态和确认区块。独立复核者从空目录重放，若字段缺失、节点未同步或结果绑定到不同哈希，门禁明确失败。涉及密钥的示例只使用公开测试向量、regtest或signet，不把教学代码连接真实资产。

官方标题层级还要转成可检查任务：章标题定义端到端目标，节标题定义机制与对象，小节标题定义算法、字段、异常或部署选择。每个标题至少对应一个输入、一个不变量、一个可观察结果和一个失败样本；多个标题可以共享实验环境，但不能因主题相近而合并掉独立语义。这样既保留原书结构，也让读者能从目录逐项证明掌握程度。

## 版次、对象与验收合同

、、、、共同构成本页复核坐标。逐字段解释序列化交易、输入、输出、见证、延展性、锁定时间、coinbase、权重与vbytes。

本书锁定Andreas M. Antonopoulos、David A. Harding《Mastering Bitcoin: Programming the Open Blockchain》第3版，O'Reilly Media，2023年，402页，ISBN 9781098150082。官方GitHub标签 third_edition_print1 对应第三版首印，公开前言、14章和附录A至C的完整源码，按二至五级标题解析得到333个正式节点；O'Reilly页面交叉核对作者、2023年、402页与ISBN。学习地图和总复习不计入原书分母。本文按CC BY-SA 4.0来源做独立中文教学重构，不复制原书正文。


## 核心约束与推导

$$
txid=SHA256(SHA256(legacySerialization(T)))
$$

$$
weight=4 \times baseSize+witnessSize
$$

$$
S_{t+1}=apply(S_t,T_t), \quad valid(T_t,S_t)=1
$$

$$
gate=source \times version \times replay \times boundary \times failureEvidence
$$

前两式对应本页的密码、交易、费用、网络、区块或二层核心不变量。状态转移式要求候选对象先通过当前状态验证，再原子地产生后状态；失败保留原状态。证据门禁使用乘法语义，来源、版本、重放、边界或失败证据任一缺失，整条结论都不能发布。

## 三段可运行骨架

```typescript
type Evidence = {
  version: string;
  before: string;
  input: string;
  after?: string;
  error?: string;
};

function evidenceGate(run: Evidence) {
  const complete = Boolean(
    run.version && run.before && run.input && (run.after || run.error),
  );
  return { complete, page: "mbt3-06-transactions", run };
}
```

```python
def apply_candidate(state, candidate, verify):
    if not verify(state, candidate):
        return {"accepted": False, "state": state, "reason": "invalid"}
    next_state = state.copy()
    next_state.update(candidate["changes"])
    return {"accepted": True, "state": next_state, "reason": None}
```

```python
from hashlib import sha256

def transaction_id(legacy_serialization: bytes) -> str:
    digest = sha256(sha256(legacy_serialization).digest()).digest()
    return digest[::-1].hex()
```

第一段把成功与失败放进同一证据结构；第二段证明拒绝路径不污染原状态；第三段验证本页最小协议对象。生产系统必须使用成熟密码、序列化和节点库，并补充资源限制、并发、持久化、超时、隐私与密钥隔离。

## 正式标题逐项讲解

### Chapter 6 Transactions

“Chapter 6 Transactions”位于本页正式标题坐标 1/27。交易ID和签名都绑定规范序列化。版本、标记、输入输出计数、outpoint、脚本、sequence、见证与锁定时间各有独立语义；解析器必须先检查长度和边界，再解释脚本与价值。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能逐字节解析一笔含见证的测试交易，重算txid与wtxid、权重和费用，并拒绝截断、超长、重复输入或金额越界。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### A Serialized Bitcoin Transaction

“A Serialized Bitcoin Transaction”位于本页正式标题坐标 2/27。交易序列化使用小端整数、CompactSize长度和严格字段顺序。SegWit标记与见证改变网络编码和wtxid，不改变legacy txid覆盖范围；解析必须先做长度边界检查。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能逐字节解析一笔含见证的测试交易，重算txid与wtxid、权重和费用，并拒绝截断、超长、重复输入或金额越界。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Version

“Version”位于本页正式标题坐标 3/27。交易序列化使用小端整数、CompactSize长度和严格字段顺序。SegWit标记与见证改变网络编码和wtxid，不改变legacy txid覆盖范围；解析必须先做长度边界检查。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能逐字节解析一笔含见证的测试交易，重算txid与wtxid、权重和费用，并拒绝截断、超长、重复输入或金额越界。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Extended Marker and Flag

“Extended Marker and Flag”位于本页正式标题坐标 4/27。交易序列化使用小端整数、CompactSize长度和严格字段顺序。SegWit标记与见证改变网络编码和wtxid，不改变legacy txid覆盖范围；解析必须先做长度边界检查。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能逐字节解析一笔含见证的测试交易，重算txid与wtxid、权重和费用，并拒绝截断、超长、重复输入或金额越界。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Inputs

“Inputs”位于本页正式标题坐标 5/27。输入引用先前交易哈希和输出索引，并提供满足锁定条件的数据。节点还要检查被引用输出存在、尚未花费、金额有效和脚本成功，不能只验证签名。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能逐字节解析一笔含见证的测试交易，重算txid与wtxid、权重和费用，并拒绝截断、超长、重复输入或金额越界。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Length of Transaction Input List

“Length of Transaction Input List”位于本页正式标题坐标 6/27。输入引用先前交易哈希和输出索引，并提供满足锁定条件的数据。节点还要检查被引用输出存在、尚未花费、金额有效和脚本成功，不能只验证签名。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能逐字节解析一笔含见证的测试交易，重算txid与wtxid、权重和费用，并拒绝截断、超长、重复输入或金额越界。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Outpoint

“Outpoint”位于本页正式标题坐标 7/27。输入引用先前交易哈希和输出索引，并提供满足锁定条件的数据。节点还要检查被引用输出存在、尚未花费、金额有效和脚本成功，不能只验证签名。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能逐字节解析一笔含见证的测试交易，重算txid与wtxid、权重和费用，并拒绝截断、超长、重复输入或金额越界。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Input Script

“Input Script”位于本页正式标题坐标 8/27。输入引用先前交易哈希和输出索引，并提供满足锁定条件的数据。节点还要检查被引用输出存在、尚未花费、金额有效和脚本成功，不能只验证签名。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能逐字节解析一笔含见证的测试交易，重算txid与wtxid、权重和费用，并拒绝截断、超长、重复输入或金额越界。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Sequence

“Sequence”位于本页正式标题坐标 9/27。sequence字段先后承载替换信号与相对时间锁语义。解释必须结合交易版本、禁用位、单位位和共识规则；不能把一个整数同时当作所有历史含义。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能逐字节解析一笔含见证的测试交易，重算txid与wtxid、权重和费用，并拒绝截断、超长、重复输入或金额越界。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Original sequence-based transaction replacement

“Original sequence-based transaction replacement”位于本页正式标题坐标 10/27。sequence字段先后承载替换信号与相对时间锁语义。解释必须结合交易版本、禁用位、单位位和共识规则；不能把一个整数同时当作所有历史含义。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能逐字节解析一笔含见证的测试交易，重算txid与wtxid、权重和费用，并拒绝截断、超长、重复输入或金额越界。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Opt-in transaction replacement signaling

“Opt-in transaction replacement signaling”位于本页正式标题坐标 11/27。硬分叉放宽规则会让旧节点拒绝新块，软分叉收紧规则可让旧节点表面接受。矿工信号表达准备或协调，真正共识效力来自节点在激活点执行规则。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能逐字节解析一笔含见证的测试交易，重算txid与wtxid、权重和费用，并拒绝截断、超长、重复输入或金额越界。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Sequence as a consensus-enforced relative timelock

“Sequence as a consensus-enforced relative timelock”位于本页正式标题坐标 12/27。sequence字段先后承载替换信号与相对时间锁语义。解释必须结合交易版本、禁用位、单位位和共识规则；不能把一个整数同时当作所有历史含义。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能逐字节解析一笔含见证的测试交易，重算txid与wtxid、权重和费用，并拒绝截断、超长、重复输入或金额越界。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Outputs

“Outputs”位于本页正式标题坐标 13/27。输出由聪金额和锁定脚本组成，创建新的UTXO。支付、找零和数据输出具有不同经济与脚本语义；金额范围、尘埃策略和脚本标准性要分别验证。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能逐字节解析一笔含见证的测试交易，重算txid与wtxid、权重和费用，并拒绝截断、超长、重复输入或金额越界。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Outputs Count

“Outputs Count”位于本页正式标题坐标 14/27。输出由聪金额和锁定脚本组成，创建新的UTXO。支付、找零和数据输出具有不同经济与脚本语义；金额范围、尘埃策略和脚本标准性要分别验证。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能逐字节解析一笔含见证的测试交易，重算txid与wtxid、权重和费用，并拒绝截断、超长、重复输入或金额越界。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Amount

“Amount”位于本页正式标题坐标 15/27。交易ID和签名都绑定规范序列化。版本、标记、输入输出计数、outpoint、脚本、sequence、见证与锁定时间各有独立语义；解析器必须先检查长度和边界，再解释脚本与价值。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能逐字节解析一笔含见证的测试交易，重算txid与wtxid、权重和费用，并拒绝截断、超长、重复输入或金额越界。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Uneconomical outputs and disallowed dust

“Uneconomical outputs and disallowed dust”位于本页正式标题坐标 16/27。输出由聪金额和锁定脚本组成，创建新的UTXO。支付、找零和数据输出具有不同经济与脚本语义；金额范围、尘埃策略和脚本标准性要分别验证。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能逐字节解析一笔含见证的测试交易，重算txid与wtxid、权重和费用，并拒绝截断、超长、重复输入或金额越界。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Output Scripts

“Output Scripts”位于本页正式标题坐标 17/27。输出由聪金额和锁定脚本组成，创建新的UTXO。支付、找零和数据输出具有不同经济与脚本语义；金额范围、尘埃策略和脚本标准性要分别验证。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能逐字节解析一笔含见证的测试交易，重算txid与wtxid、权重和费用，并拒绝截断、超长、重复输入或金额越界。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Witness Structure

“Witness Structure”位于本页正式标题坐标 18/27。交易ID和签名都绑定规范序列化。版本、标记、输入输出计数、outpoint、脚本、sequence、见证与锁定时间各有独立语义；解析器必须先检查长度和边界，再解释脚本与价值。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能逐字节解析一笔含见证的测试交易，重算txid与wtxid、权重和费用，并拒绝截断、超长、重复输入或金额越界。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Circular Dependencies

“Circular Dependencies”位于本页正式标题坐标 19/27。交易ID和签名都绑定规范序列化。版本、标记、输入输出计数、outpoint、脚本、sequence、见证与锁定时间各有独立语义；解析器必须先检查长度和边界，再解释脚本与价值。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能逐字节解析一笔含见证的测试交易，重算txid与wtxid、权重和费用，并拒绝截断、超长、重复输入或金额越界。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Third-Party Transaction Malleability

“Third-Party Transaction Malleability”位于本页正式标题坐标 20/27。交易延展性指不改变花费效果却改变标识或签名承诺。SegWit把见证移出legacy txid并定义新签名摘要，解决第三方延展性，但签名者仍可能按允许范围创建不同交易。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能逐字节解析一笔含见证的测试交易，重算txid与wtxid、权重和费用，并拒绝截断、超长、重复输入或金额越界。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Second-Party Transaction Malleability

“Second-Party Transaction Malleability”位于本页正式标题坐标 21/27。交易延展性指不改变花费效果却改变标识或签名承诺。SegWit把见证移出legacy txid并定义新签名摘要，解决第三方延展性，但签名者仍可能按允许范围创建不同交易。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能逐字节解析一笔含见证的测试交易，重算txid与wtxid、权重和费用，并拒绝截断、超长、重复输入或金额越界。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Segregated Witness

“Segregated Witness”位于本页正式标题坐标 22/27。交易延展性指不改变花费效果却改变标识或签名承诺。SegWit把见证移出legacy txid并定义新签名摘要，解决第三方延展性，但签名者仍可能按允许范围创建不同交易。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能逐字节解析一笔含见证的测试交易，重算txid与wtxid、权重和费用，并拒绝截断、超长、重复输入或金额越界。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Witness Structure Serialization

“Witness Structure Serialization”位于本页正式标题坐标 23/27。交易序列化使用小端整数、CompactSize长度和严格字段顺序。SegWit标记与见证改变网络编码和wtxid，不改变legacy txid覆盖范围；解析必须先做长度边界检查。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能逐字节解析一笔含见证的测试交易，重算txid与wtxid、权重和费用，并拒绝截断、超长、重复输入或金额越界。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Lock Time

“Lock Time”位于本页正式标题坐标 24/27。nLockTime设置交易最早可进入区块的高度或时间，并与输入sequence共同决定是否启用。它不是未来执行计划，交易仍需届时被广播和挖矿。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能逐字节解析一笔含见证的测试交易，重算txid与wtxid、权重和费用，并拒绝截断、超长、重复输入或金额越界。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Coinbase Transactions

“Coinbase Transactions”位于本页正式标题坐标 25/27。coinbase是区块首笔特殊交易，创建补贴并收取该块费用，没有普通outpoint。其输入承载高度和额外数据，输出还受成熟期约束，不能立即花费。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能逐字节解析一笔含见证的测试交易，重算txid与wtxid、权重和费用，并拒绝截断、超长、重复输入或金额越界。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Weight and Vbytes

“Weight and Vbytes”位于本页正式标题坐标 26/27。交易重量按基础字节四倍加见证字节计算，vbytes向上取weight/4。尘埃是节点策略根据未来花费成本判断的低价值输出，不等同共识无效。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能逐字节解析一笔含见证的测试交易，重算txid与wtxid、权重和费用，并拒绝截断、超长、重复输入或金额越界。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Legacy Serialization

“Legacy Serialization”位于本页正式标题坐标 27/27。交易序列化使用小端整数、CompactSize长度和严格字段顺序。SegWit标记与见证改变网络编码和wtxid，不改变legacy txid覆盖范围；解析必须先做长度边界检查。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能逐字节解析一笔含见证的测试交易，重算txid与wtxid、权重和费用，并拒绝截断、超长、重复输入或金额越界。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

## 正常、边界与故障矩阵

| 样本   | 只改变的变量                    | 预期结果               | 必存证据                     |
| ------ | ------------------------------- | ---------------------- | ---------------------------- |
| 正常   | 有效UTXO、脚本、费用与网络      | 状态转移并达到确认条件 | 原始对象、前后状态、区块哈希 |
| 边界   | 零值、最大值、恰好阈值或时间锁  | 按明确规则接受或拒绝   | 边界值、判断分支、资源成本   |
| 单故障 | 网络、字节、签名、引用或PoW一项 | 首个无效规则处拒绝     | 错误、首差、原状态与回退点   |

先写出预期字节、栈、金额或哈希，再运行节点和解析器。若钱包、区块浏览器与本地节点给出不同答案，以固定网络上的原始交易、被花费输出、区块头和本地共识验证为坐标；第三方展示不能覆盖本地可验证事实。

## 常见失败与回退


## 本章回顾

逐字段解释序列化交易、输入、输出、见证、延展性、锁定时间、coinbase、权重与vbytes。 交易ID和签名都绑定规范序列化。版本、标记、输入输出计数、outpoint、脚本、sequence、见证与锁定时间各有独立语义；解析器必须先检查长度和边界，再解释脚本与价值。 掌握标准是能逐字节解析一笔含见证的测试交易，重算txid与wtxid、权重和费用，并拒绝截断、超长、重复输入或金额越界。。独立复核必须从官方第三版标签、固定测试网络和原始协议对象重放正常、边界与单故障样本。


## 前后导航

- [上一页：Chapter 5 Wallet Recovery](/learn/mastering-bitcoin/06-05-wallet-recovery/mbt3-05-wallet-recovery)
- [下一页：Chapter 7 Authorization and Authentication](/learn/mastering-bitcoin/08-07-authorization-authentication/mbt3-07-authorization-authentication)

