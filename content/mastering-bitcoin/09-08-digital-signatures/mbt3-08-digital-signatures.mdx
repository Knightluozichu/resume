---
title: "Chapter 8 Digital Signatures"
type: C
section: "Mastering Bitcoin（第3版）"
order: 9
description: "解释签名创建验证、SIGHASH、Schnorr、无脚本多签与阈值签名、ECDSA、DER和随机数安全。"
demo: true
math: true
sourceUrl: "https://github.com/bitcoinbook/bitcoinbook/tree/third_edition_print1"
draft: false
---


## 为什么从可验证状态开始

数字签名授权确定交易摘要，而摘要范围由SIGHASH规则决定。Schnorr提供线性组合能力，ECDSA依赖严格DER与安全nonce；实现应使用经过审计的库并验证域、曲线、编码和公钥。

比特币最容易被界面和术语掩盖：钱包余额是UTXO视图，地址是脚本编码，交易哈希只是候选对象标识，矿工出块也不代表全节点必须接受。要判断实现是否正确，必须追踪前状态、被引用输出、规范序列化、脚本与签名、节点策略、区块承诺、累计工作量和后状态。只保存最终余额或区块高度，会漏掉错误网络、冲突交易、脚本失败、重组与索引视图滞后。

本页先手算最小实例，再运行工具。正常样本证明主路径；边界样本取零值、最大金额、恰好费用、最短证明或时间锁边界；单故障样本只改变网络前缀、字节、签名、outpoint、脚本、target或确认中的一项。三组样本共享源码标签、节点、创世状态和观察点，使首个差异可以归因，而不是靠重复广播或刷新页面碰运气。

每次实验生成机器可读证据：官方印刷标签、软件提交、网络与创世哈希、输入原文、解析对象、验证结果、错误、前后UTXO或链状态和确认区块。独立复核者从空目录重放，若字段缺失、节点未同步或结果绑定到不同哈希，门禁明确失败。涉及密钥的示例只使用公开测试向量、regtest或signet，不把教学代码连接真实资产。

官方标题层级还要转成可检查任务：章标题定义端到端目标，节标题定义机制与对象，小节标题定义算法、字段、异常或部署选择。每个标题至少对应一个输入、一个不变量、一个可观察结果和一个失败样本；多个标题可以共享实验环境，但不能因主题相近而合并掉独立语义。这样既保留原书结构，也让读者能从目录逐项证明掌握程度。

## 版次、对象与验收合同

、、、、共同构成本页复核坐标。解释签名创建验证、SIGHASH、Schnorr、无脚本多签与阈值签名、ECDSA、DER和随机数安全。

本书锁定Andreas M. Antonopoulos、David A. Harding《Mastering Bitcoin: Programming the Open Blockchain》第3版，O'Reilly Media，2023年，402页，ISBN 9781098150082。官方GitHub标签 third_edition_print1 对应第三版首印，公开前言、14章和附录A至C的完整源码，按二至五级标题解析得到333个正式节点；O'Reilly页面交叉核对作者、2023年、402页与ISBN。学习地图和总复习不计入原书分母。本文按CC BY-SA 4.0来源做独立中文教学重构，不复制原书正文。


## 核心约束与推导

$$
e=H(R \Vert P \Vert m), \quad s=k+ex \pmod n
$$

$$
verify: sG=R+eP
$$

$$
S_{t+1}=apply(S_t,T_t), \quad valid(T_t,S_t)=1
$$

$$
gate=source \times version \times replay \times boundary \times failureEvidence
$$

前两式对应本页的密码、交易、费用、网络、区块或二层核心不变量。状态转移式要求候选对象先通过当前状态验证，再原子地产生后状态；失败保留原状态。证据门禁使用乘法语义，来源、版本、重放、边界或失败证据任一缺失，整条结论都不能发布。

## 三段可运行骨架

```typescript
type Evidence = {
  version: string;
  before: string;
  input: string;
  after?: string;
  error?: string;
};

function evidenceGate(run: Evidence) {
  const complete = Boolean(
    run.version && run.before && run.input && (run.after || run.error),
  );
  return { complete, page: "mbt3-08-digital-signatures", run };
}
```

```python
def apply_candidate(state, candidate, verify):
    if not verify(state, candidate):
        return {"accepted": False, "state": state, "reason": "invalid"}
    next_state = state.copy()
    next_state.update(candidate["changes"])
    return {"accepted": True, "state": next_state, "reason": None}
```

```python
def verify_value_conservation(inputs, outputs, fee):
    if len(inputs) != len({item["outpoint"] for item in inputs}):
        return False
    return sum(item["value"] for item in inputs) == sum(outputs) + fee
```

第一段把成功与失败放进同一证据结构；第二段证明拒绝路径不污染原状态；第三段验证本页最小协议对象。生产系统必须使用成熟密码、序列化和节点库，并补充资源限制、并发、持久化、超时、隐私与密钥隔离。

## 正式标题逐项讲解

### Chapter 8 Digital Signatures

“Chapter 8 Digital Signatures”位于本页正式标题坐标 1/14。签名把私钥授权绑定到交易摘要，SIGHASH决定覆盖字段。Schnorr的线性结构支持聚合协议，ECDSA依赖安全随机nonce；两者都必须使用标准库和规范编码。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能用标准库对测试交易摘要签名与验签，比较Schnorr和ECDSA编码，演示重复nonce泄密，并解释不同SIGHASH覆盖哪些字段。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### How Digital Signatures Work

“How Digital Signatures Work”位于本页正式标题坐标 2/14。签名把私钥授权绑定到交易摘要，SIGHASH决定覆盖字段。Schnorr的线性结构支持聚合协议，ECDSA依赖安全随机nonce；两者都必须使用标准库和规范编码。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能用标准库对测试交易摘要签名与验签，比较Schnorr和ECDSA编码，演示重复nonce泄密，并解释不同SIGHASH覆盖哪些字段。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Creating a Digital Signature

“Creating a Digital Signature”位于本页正式标题坐标 3/14。签名把私钥授权绑定到交易摘要，SIGHASH决定覆盖字段。Schnorr的线性结构支持聚合协议，ECDSA依赖安全随机nonce；两者都必须使用标准库和规范编码。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能用标准库对测试交易摘要签名与验签，比较Schnorr和ECDSA编码，演示重复nonce泄密，并解释不同SIGHASH覆盖哪些字段。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Verifying the Signature

“Verifying the Signature”位于本页正式标题坐标 4/14。签名把私钥授权绑定到交易摘要，SIGHASH决定覆盖字段。Schnorr的线性结构支持聚合协议，ECDSA依赖安全随机nonce；两者都必须使用标准库和规范编码。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能用标准库对测试交易摘要签名与验签，比较Schnorr和ECDSA编码，演示重复nonce泄密，并解释不同SIGHASH覆盖哪些字段。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Signature Hash Types (SIGHASH)

“Signature Hash Types (SIGHASH)”位于本页正式标题坐标 5/14。签名把私钥授权绑定到交易摘要，SIGHASH决定覆盖字段。Schnorr的线性结构支持聚合协议，ECDSA依赖安全随机nonce；两者都必须使用标准库和规范编码。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能用标准库对测试交易摘要签名与验签，比较Schnorr和ECDSA编码，演示重复nonce泄密，并解释不同SIGHASH覆盖哪些字段。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Schnorr Signatures

“Schnorr Signatures”位于本页正式标题坐标 6/14。签名把私钥授权绑定到交易摘要，SIGHASH决定覆盖字段。Schnorr的线性结构支持聚合协议，ECDSA依赖安全随机nonce；两者都必须使用标准库和规范编码。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能用标准库对测试交易摘要签名与验签，比较Schnorr和ECDSA编码，演示重复nonce泄密，并解释不同SIGHASH覆盖哪些字段。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Serialization of Schnorr Signatures

“Serialization of Schnorr Signatures”位于本页正式标题坐标 7/14。交易序列化使用小端整数、CompactSize长度和严格字段顺序。SegWit标记与见证改变网络编码和wtxid，不改变legacy txid覆盖范围；解析必须先做长度边界检查。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能用标准库对测试交易摘要签名与验签，比较Schnorr和ECDSA编码，演示重复nonce泄密，并解释不同SIGHASH覆盖哪些字段。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Schnorr-based Scriptless Multisignatures

“Schnorr-based Scriptless Multisignatures”位于本页正式标题坐标 8/14。Script以栈机组合数据和操作码，输出脚本定义条件，输入或见证提供满足数据。P2SH承诺赎回脚本，多签要同时检查阈值、密钥顺序和历史CHECKMULTISIG额外栈项。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能用标准库对测试交易摘要签名与验签，比较Schnorr和ECDSA编码，演示重复nonce泄密，并解释不同SIGHASH覆盖哪些字段。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Schnorr-based Scriptless Threshold Signatures

“Schnorr-based Scriptless Threshold Signatures”位于本页正式标题坐标 9/14。Script以栈机组合数据和操作码，输出脚本定义条件，输入或见证提供满足数据。P2SH承诺赎回脚本，多签要同时检查阈值、密钥顺序和历史CHECKMULTISIG额外栈项。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能用标准库对测试交易摘要签名与验签，比较Schnorr和ECDSA编码，演示重复nonce泄密，并解释不同SIGHASH覆盖哪些字段。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### ECDSA Signatures

“ECDSA Signatures”位于本页正式标题坐标 10/14。签名把私钥授权绑定到交易摘要，SIGHASH决定覆盖字段。Schnorr的线性结构支持聚合协议，ECDSA依赖安全随机nonce；两者都必须使用标准库和规范编码。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能用标准库对测试交易摘要签名与验签，比较Schnorr和ECDSA编码，演示重复nonce泄密，并解释不同SIGHASH覆盖哪些字段。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### ECDSA Algorithm

“ECDSA Algorithm”位于本页正式标题坐标 11/14。签名把私钥授权绑定到交易摘要，SIGHASH决定覆盖字段。Schnorr的线性结构支持聚合协议，ECDSA依赖安全随机nonce；两者都必须使用标准库和规范编码。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能用标准库对测试交易摘要签名与验签，比较Schnorr和ECDSA编码，演示重复nonce泄密，并解释不同SIGHASH覆盖哪些字段。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Serialization of ECDSA Signatures (DER)

“Serialization of ECDSA Signatures (DER)”位于本页正式标题坐标 12/14。交易序列化使用小端整数、CompactSize长度和严格字段顺序。SegWit标记与见证改变网络编码和wtxid，不改变legacy txid覆盖范围；解析必须先做长度边界检查。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能用标准库对测试交易摘要签名与验签，比较Schnorr和ECDSA编码，演示重复nonce泄密，并解释不同SIGHASH覆盖哪些字段。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### The Importance of Randomness in Signatures

“The Importance of Randomness in Signatures”位于本页正式标题坐标 13/14。签名把私钥授权绑定到交易摘要，SIGHASH决定覆盖字段。Schnorr的线性结构支持聚合协议，ECDSA依赖安全随机nonce；两者都必须使用标准库和规范编码。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能用标准库对测试交易摘要签名与验签，比较Schnorr和ECDSA编码，演示重复nonce泄密，并解释不同SIGHASH覆盖哪些字段。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Segregated Witness's New Signing Algorithm

“Segregated Witness's New Signing Algorithm”位于本页正式标题坐标 14/14。交易延展性指不改变花费效果却改变标识或签名承诺。SegWit把见证移出legacy txid并定义新签名摘要，解决第三方延展性，但签名者仍可能按允许范围创建不同交易。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能用标准库对测试交易摘要签名与验签，比较Schnorr和ECDSA编码，演示重复nonce泄密，并解释不同SIGHASH覆盖哪些字段。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

## 正常、边界与故障矩阵

| 样本   | 只改变的变量                    | 预期结果               | 必存证据                     |
| ------ | ------------------------------- | ---------------------- | ---------------------------- |
| 正常   | 有效UTXO、脚本、费用与网络      | 状态转移并达到确认条件 | 原始对象、前后状态、区块哈希 |
| 边界   | 零值、最大值、恰好阈值或时间锁  | 按明确规则接受或拒绝   | 边界值、判断分支、资源成本   |
| 单故障 | 网络、字节、签名、引用或PoW一项 | 首个无效规则处拒绝     | 错误、首差、原状态与回退点   |

先写出预期字节、栈、金额或哈希，再运行节点和解析器。若钱包、区块浏览器与本地节点给出不同答案，以固定网络上的原始交易、被花费输出、区块头和本地共识验证为坐标；第三方展示不能覆盖本地可验证事实。

## 常见失败与回退


## 本章回顾

解释签名创建验证、SIGHASH、Schnorr、无脚本多签与阈值签名、ECDSA、DER和随机数安全。 数字签名授权确定交易摘要，而摘要范围由SIGHASH规则决定。Schnorr提供线性组合能力，ECDSA依赖严格DER与安全nonce；实现应使用经过审计的库并验证域、曲线、编码和公钥。 掌握标准是能用标准库对测试交易摘要签名与验签，比较Schnorr和ECDSA编码，演示重复nonce泄密，并解释不同SIGHASH覆盖哪些字段。。独立复核必须从官方第三版标签、固定测试网络和原始协议对象重放正常、边界与单故障样本。


## 前后导航

- [上一页：Chapter 7 Authorization and Authentication](/learn/mastering-bitcoin/08-07-authorization-authentication/mbt3-07-authorization-authentication)
- [下一页：Chapter 9 Transaction Fees](/learn/mastering-bitcoin/10-09-transaction-fees/mbt3-09-transaction-fees)

