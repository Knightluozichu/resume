---
title: "Chapter 14 Second-Layer Applications"
type: C
section: "Mastering Bitcoin（第3版）"
order: 15
description: "从协议原语组合出单次封印、P2C、客户端验证、RGB、Taproot Assets、状态通道、HTLC与闪电网络。"
demo: true
math: true
sourceUrl: "https://github.com/bitcoinbook/bitcoinbook/tree/third_edition_print1"
draft: false
---


## 为什么从可验证状态开始

二层协议把高频状态放到链外，用链上承诺、时间锁和争议交易保护退出。扩展吞吐不会免费继承主链全部可用性；参与者仍要管理通道流动性、监控旧状态和验证链外证明。

比特币最容易被界面和术语掩盖：钱包余额是UTXO视图，地址是脚本编码，交易哈希只是候选对象标识，矿工出块也不代表全节点必须接受。要判断实现是否正确，必须追踪前状态、被引用输出、规范序列化、脚本与签名、节点策略、区块承诺、累计工作量和后状态。只保存最终余额或区块高度，会漏掉错误网络、冲突交易、脚本失败、重组与索引视图滞后。

本页先手算最小实例，再运行工具。正常样本证明主路径；边界样本取零值、最大金额、恰好费用、最短证明或时间锁边界；单故障样本只改变网络前缀、字节、签名、outpoint、脚本、target或确认中的一项。三组样本共享源码标签、节点、创世状态和观察点，使首个差异可以归因，而不是靠重复广播或刷新页面碰运气。

每次实验生成机器可读证据：官方印刷标签、软件提交、网络与创世哈希、输入原文、解析对象、验证结果、错误、前后UTXO或链状态和确认区块。独立复核者从空目录重放，若字段缺失、节点未同步或结果绑定到不同哈希，门禁明确失败。涉及密钥的示例只使用公开测试向量、regtest或signet，不把教学代码连接真实资产。

官方标题层级还要转成可检查任务：章标题定义端到端目标，节标题定义机制与对象，小节标题定义算法、字段、异常或部署选择。每个标题至少对应一个输入、一个不变量、一个可观察结果和一个失败样本；多个标题可以共享实验环境，但不能因主题相近而合并掉独立语义。这样既保留原书结构，也让读者能从目录逐项证明掌握程度。

## 版次、对象与验收合同

、、、、共同构成本页复核坐标。从协议原语组合出单次封印、P2C、客户端验证、RGB、Taproot
Assets、状态通道、HTLC与闪电网络。

本书锁定Andreas M. Antonopoulos、David A. Harding《Mastering Bitcoin: Programming the Open Blockchain》第3版，O'Reilly Media，2023年，402页，ISBN 9781098150082。官方GitHub标签 third_edition_print1 对应第三版首印，公开前言、14章和附录A至C的完整源码，按二至五级标题解析得到333个正式节点；O'Reilly页面交叉核对作者、2023年、402页与ISBN。学习地图和总复习不计入原书分母。本文按CC BY-SA 4.0来源做独立中文教学重构，不复制原书正文。


## 核心约束与推导

$$
balance_A(t)+balance_B(t)=capacity
$$

$$
HTLC=hashPreimage \lor timeoutRefund
$$

$$
S_{t+1}=apply(S_t,T_t), \quad valid(T_t,S_t)=1
$$

$$
gate=source \times version \times replay \times boundary \times failureEvidence
$$

前两式对应本页的密码、交易、费用、网络、区块或二层核心不变量。状态转移式要求候选对象先通过当前状态验证，再原子地产生后状态；失败保留原状态。证据门禁使用乘法语义，来源、版本、重放、边界或失败证据任一缺失，整条结论都不能发布。

## 三段可运行骨架

以下代码展示二层证据门禁与状态验证两层视角：TypeScript 把成功与失败统一进同一证据结构，Python 证明拒绝路径不污染原状态并复算 HTLC 结算分支。

<CodeTabs>
  <Tab label="TypeScript / 证据门禁">

```typescript
type Evidence = {
  version: string;
  before: string;
  input: string;
  after?: string;
  error?: string;
};

function evidenceGate(run: Evidence) {
  const complete = Boolean(
    run.version && run.before && run.input && (run.after || run.error),
  );
  return { complete, page: "mbt3-14-second-layer-applications", run };
}
```

  </Tab>
  <Tab label="Python / 状态验证">

```python
def apply_candidate(state, candidate, verify):
    if not verify(state, candidate):
        return {"accepted": False, "state": state, "reason": "invalid"}
    next_state = state.copy()
    next_state.update(candidate["changes"])
    return {"accepted": True, "state": next_state, "reason": None}

def settle_htlc(preimage_ok, before_timeout, sender, receiver, amount):
    if preimage_ok and before_timeout:
        return {sender: -amount, receiver: amount, "path": "success"}
    return {sender: 0, receiver: 0, "path": "refund"}
```

  </Tab>
</CodeTabs>

第一段把成功与失败放进同一证据结构；第二段证明拒绝路径不污染原状态；第三段验证本页最小协议对象。生产系统必须使用成熟密码、序列化和节点库，并补充资源限制、并发、持久化、超时、隐私与密钥隔离。

## 正式标题逐项讲解

### Chapter 14 Second-Layer Applications

“Chapter 14 Second-Layer Applications”位于本页正式标题坐标 1/19。二层协议把高频状态放到链外，用链上承诺、时间锁和争议交易保护退出。扩展吞吐不会免费继承主链全部可用性；参与者仍要管理通道流动性、监控旧状态和验证链外证明。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能手算双向通道余额和HTLC超时路径，解释旧承诺惩罚与路由支付，并列出离线、流动性不足和链上拥堵时的恢复边界。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Building Blocks (Primitives)

“Building Blocks (Primitives)”位于本页正式标题坐标 2/19。可复现构建绑定源码标签、依赖、编译器、目标平台和配置。验证不仅看退出码，还要保存提交哈希、构建日志、二进制摘要和运行时版本。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能手算双向通道余额和HTLC超时路径，解释旧承诺惩罚与路由支付，并列出离线、流动性不足和链上拥堵时的恢复边界。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Applications from Building Blocks

“Applications from Building Blocks”位于本页正式标题坐标 3/19。可复现构建绑定源码标签、依赖、编译器、目标平台和配置。验证不仅看退出码，还要保存提交哈希、构建日志、二进制摘要和运行时版本。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能手算双向通道余额和HTLC超时路径，解释旧承诺惩罚与路由支付，并列出离线、流动性不足和链上拥堵时的恢复边界。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Colored Coins

“Colored Coins”位于本页正式标题坐标 4/19。客户端验证协议把资产状态与链上唯一花费点绑定，参与者只验证自己需要的历史。链上交易提供排序和双花保护，不自动保证发行资产或链外声明真实。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能手算双向通道余额和HTLC超时路径，解释旧承诺惩罚与路由支付，并列出离线、流动性不足和链上拥堵时的恢复边界。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Single-Use Seals

“Single-Use Seals”位于本页正式标题坐标 5/19。客户端验证协议把资产状态与链上唯一花费点绑定，参与者只验证自己需要的历史。链上交易提供排序和双花保护，不自动保证发行资产或链外声明真实。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能手算双向通道余额和HTLC超时路径，解释旧承诺惩罚与路由支付，并列出离线、流动性不足和链上拥堵时的恢复边界。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Pay to Contract (P2C)

“Pay to Contract (P2C)”位于本页正式标题坐标 6/19。Taproot输出承诺聚合密钥和可选脚本树，正常密钥路径只揭示一个Schnorr签名，脚本路径揭示所用叶与Merkle证明。Tapscript定义叶脚本的新验证语义。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能手算双向通道余额和HTLC超时路径，解释旧承诺惩罚与路由支付，并列出离线、流动性不足和链上拥堵时的恢复边界。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Client-Side Validation

“Client-Side Validation”位于本页正式标题坐标 7/19。客户端验证协议把资产状态与链上唯一花费点绑定，参与者只验证自己需要的历史。链上交易提供排序和双花保护，不自动保证发行资产或链外声明真实。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能手算双向通道余额和HTLC超时路径，解释旧承诺惩罚与路由支付，并列出离线、流动性不足和链上拥堵时的恢复边界。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### RGB

“RGB”位于本页正式标题坐标 8/19。客户端验证协议把资产状态与链上唯一花费点绑定，参与者只验证自己需要的历史。链上交易提供排序和双花保护，不自动保证发行资产或链外声明真实。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能手算双向通道余额和HTLC超时路径，解释旧承诺惩罚与路由支付，并列出离线、流动性不足和链上拥堵时的恢复边界。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Taproot Assets

“Taproot Assets”位于本页正式标题坐标 9/19。Taproot输出承诺聚合密钥和可选脚本树，正常密钥路径只揭示一个Schnorr签名，脚本路径揭示所用叶与Merkle证明。Tapscript定义叶脚本的新验证语义。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能手算双向通道余额和HTLC超时路径，解释旧承诺惩罚与路由支付，并列出离线、流动性不足和链上拥堵时的恢复边界。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Payment Channels and State Channels

“Payment Channels and State Channels”位于本页正式标题坐标 10/19。支付通道锁定链上资金并交换更新承诺，只有开启、争议或关闭需要上链。撤销旧状态和HTLC哈希时间条件支持可信退出与路由，仍依赖在线监控和链上容量。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能手算双向通道余额和HTLC超时路径，解释旧承诺惩罚与路由支付，并列出离线、流动性不足和链上拥堵时的恢复边界。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### State Channels—Basic Concepts and Terminology

“State Channels—Basic Concepts and Terminology”位于本页正式标题坐标 11/19。支付通道锁定链上资金并交换更新承诺，只有开启、争议或关闭需要上链。撤销旧状态和HTLC哈希时间条件支持可信退出与路由，仍依赖在线监控和链上容量。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能手算双向通道余额和HTLC超时路径，解释旧承诺惩罚与路由支付，并列出离线、流动性不足和链上拥堵时的恢复边界。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Simple Payment Channel Example

“Simple Payment Channel Example”位于本页正式标题坐标 12/19。支付通道锁定链上资金并交换更新承诺，只有开启、争议或关闭需要上链。撤销旧状态和HTLC哈希时间条件支持可信退出与路由，仍依赖在线监控和链上容量。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能手算双向通道余额和HTLC超时路径，解释旧承诺惩罚与路由支付，并列出离线、流动性不足和链上拥堵时的恢复边界。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Making Trustless Channels

“Making Trustless Channels”位于本页正式标题坐标 13/19。支付通道锁定链上资金并交换更新承诺，只有开启、争议或关闭需要上链。撤销旧状态和HTLC哈希时间条件支持可信退出与路由，仍依赖在线监控和链上容量。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能手算双向通道余额和HTLC超时路径，解释旧承诺惩罚与路由支付，并列出离线、流动性不足和链上拥堵时的恢复边界。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Asymmetric Revocable Commitments

“Asymmetric Revocable Commitments”位于本页正式标题坐标 14/19。支付通道锁定链上资金并交换更新承诺，只有开启、争议或关闭需要上链。撤销旧状态和HTLC哈希时间条件支持可信退出与路由，仍依赖在线监控和链上容量。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能手算双向通道余额和HTLC超时路径，解释旧承诺惩罚与路由支付，并列出离线、流动性不足和链上拥堵时的恢复边界。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Hash Time Lock Contracts (HTLC)

“Hash Time Lock Contracts (HTLC)”位于本页正式标题坐标 15/19。支付通道锁定链上资金并交换更新承诺，只有开启、争议或关闭需要上链。撤销旧状态和HTLC哈希时间条件支持可信退出与路由，仍依赖在线监控和链上容量。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能手算双向通道余额和HTLC超时路径，解释旧承诺惩罚与路由支付，并列出离线、流动性不足和链上拥堵时的恢复边界。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Routed Payment Channels (Lightning Network)

“Routed Payment Channels (Lightning Network)”位于本页正式标题坐标 16/19。支付通道锁定链上资金并交换更新承诺，只有开启、争议或关闭需要上链。撤销旧状态和HTLC哈希时间条件支持可信退出与路由，仍依赖在线监控和链上容量。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能手算双向通道余额和HTLC超时路径，解释旧承诺惩罚与路由支付，并列出离线、流动性不足和链上拥堵时的恢复边界。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Basic Lightning Network Example

“Basic Lightning Network Example”位于本页正式标题坐标 17/19。支付通道锁定链上资金并交换更新承诺，只有开启、争议或关闭需要上链。撤销旧状态和HTLC哈希时间条件支持可信退出与路由，仍依赖在线监控和链上容量。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能手算双向通道余额和HTLC超时路径，解释旧承诺惩罚与路由支付，并列出离线、流动性不足和链上拥堵时的恢复边界。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Lightning Network Transport and Pathfinding

“Lightning Network Transport and Pathfinding”位于本页正式标题坐标 18/19。备份除种子外还要保留派生路径、脚本策略、描述符、账户标签和创建时间。恢复程序通过已知地址、交易或描述符校验，不能盲目扫描无限路径。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能手算双向通道余额和HTLC超时路径，解释旧承诺惩罚与路由支付，并列出离线、流动性不足和链上拥堵时的恢复边界。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

### Lightning Network Benefits

“Lightning Network Benefits”位于本页正式标题坐标 19/19。支付通道锁定链上资金并交换更新承诺，只有开启、争议或关闭需要上链。撤销旧状态和HTLC哈希时间条件支持可信退出与路由，仍依赖在线监控和链上容量。

复核时固定网络、软件、前状态与原始输入，保存解析、验证、传播或执行的中间对象；失败必须给出首个拒绝规则并保持原状态。通过标准是“能手算双向通道余额和HTLC超时路径，解释旧承诺惩罚与路由支付，并列出离线、流动性不足和链上拥堵时的恢复边界。”，结果必须绑定固定对象哈希、网络和官方第三版来源。

## 正常、边界与故障矩阵

| 样本   | 只改变的变量                    | 预期结果               | 必存证据                     |
| ------ | ------------------------------- | ---------------------- | ---------------------------- |
| 正常   | 有效UTXO、脚本、费用与网络      | 状态转移并达到确认条件 | 原始对象、前后状态、区块哈希 |
| 边界   | 零值、最大值、恰好阈值或时间锁  | 按明确规则接受或拒绝   | 边界值、判断分支、资源成本   |
| 单故障 | 网络、字节、签名、引用或PoW一项 | 首个无效规则处拒绝     | 错误、首差、原状态与回退点   |

先写出预期字节、栈、金额或哈希，再运行节点和解析器。若钱包、区块浏览器与本地节点给出不同答案，以固定网络上的原始交易、被花费输出、区块头和本地共识验证为坐标；第三方展示不能覆盖本地可验证事实。

## 容易踩的坑

**现象 →** 以为闪电支付成功就等于链上结算完成。
**原因 →** 混淆通道更新与链上确认，链外状态仍可能被旧承诺挑战。
**修法 →** 区分通道更新、HTLC 路由与链上关闭，关闭前持续监控旧状态。

**现象 →** 通道只开不关，认为对方会一直在线合作关闭。
**原因 →** 对方离线或恶意广播旧承诺时，需在时间窗内链上惩罚。
**修法 →** 保持看塔在线并预留链上费用。你可以亲自试一下：模拟对方广播旧状态，观察惩罚交易能否在 timeout 内上链。

**现象 →** 把 RGB/Taproot Assets 资产当成主链原生余额查看。
**原因 →** 客户端验证资产状态存于链外，链上只提供花费点排序。
**修法 →** 单独保存链外证明与发行声明，链上交易不自动保证资产真实。

## 小结

- 二层把高频状态放链外，用承诺、时间锁与争议交易保护退出
- 扩展吞吐不免费继承主链全部可用性，需管理流动性
- 客户端验证资产状态绑链上花费点，链上不证发行真实
- 支付通道靠撤销旧承诺与 HTLC 支持可信退出与路由
- 通过标准：能手算通道余额、HTLC 超时与旧承诺惩罚

<Exercises>

**问题 1** 双向支付通道为何需要撤销旧承诺与 HTLC？离线、流动性不足与链上拥堵时各有什么恢复边界？

<Answer>

撤销旧承诺让一方广播过期状态时另一方能在时间窗内链上惩罚，否则旧余额可被恶意结算；HTLC 用原像或超时退款支持跨路由支付。对方离线需看塔及时上链惩罚，流动性不足只能重路由或开新通道，链上拥堵时手续费激增可能错过撤销窗口，需预留链上费用。

</Answer>

**问题 2**（独立实现）实现 HTLC 结算模拟器：输入原像是否正确、是否在超时前，输出成功支付或退款两条路径及余额变动证据。

<Answer>

```python
def settle_htlc(preimage_ok, before_timeout, sender, receiver, amount):
    if preimage_ok and before_timeout:
        return {
            sender: -amount,
            receiver: amount,
            "path": "success",
            "evidence": "preimage-revealed-before-timeout",
        }
    return {
        sender: 0,
        receiver: 0,
        "path": "refund",
        "evidence": "no-preimage-or-timeout",
    }

assert settle_htlc(True, True, "A", "B", 5)["path"] == "success"
assert settle_htlc(False, True, "A", "B", 5)["path"] == "refund"
assert settle_htlc(True, False, "A", "B", 5)["path"] == "refund"
```

</Answer>

</Exercises>


## 本章回顾

从协议原语组合出单次封印、P2C、客户端验证、RGB、Taproot Assets、状态通道、HTLC与闪电网络。 二层协议把高频状态放到链外，用链上承诺、时间锁和争议交易保护退出。扩展吞吐不会免费继承主链全部可用性；参与者仍要管理通道流动性、监控旧状态和验证链外证明。 掌握标准是能手算双向通道余额和HTLC超时路径，解释旧承诺惩罚与路由支付，并列出离线、流动性不足和链上拥堵时的恢复边界。。独立复核必须从官方第三版标签、固定测试网络和原始协议对象重放正常、边界与单故障样本。


## 前后导航

- [上一页：Chapter 13 Bitcoin Security](/learn/mastering-bitcoin/14-13-security/mbt3-13-security)
- [下一页：Appendix A The Bitcoin Whitepaper by Satoshi Nakamoto](/learn/mastering-bitcoin/16-appendix-a-whitepaper/mbt3-appendix-a-whitepaper)

<Attribution
  mode="independent-rewrite"
  sourceBasis="publisher-catalog"
  workTitle="Andreas M. Antonopoulos《Mastering Bitcoin》3rd Edition / O'Reilly / 2023"
  adaptedUrl="https://github.com/bitcoinbook/bitcoinbook/tree/third_edition_print1"
/>

