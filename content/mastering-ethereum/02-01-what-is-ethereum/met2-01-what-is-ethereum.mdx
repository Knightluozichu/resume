---
title: "第1章：什么是以太坊"
type: B
section: "Mastering Ethereum（第2版）"
order: 2
description: "以太坊是由全球单例状态、确定性状态转换函数、P2P传播和PoS共识共同维护的通用计算平台。图灵完备带来表达力，也要求Gas、安全边界和独立验证。"
demo: true
math: true
sourceUrl: "https://masteringethereum.xyz/chapter_1.html"
draft: false
---

import {
  Met201WhatIsEthereumFlowLab,
  Met201WhatIsEthereumExperimentLab,
  Met201WhatIsEthereumEvidenceLab,
} from "@/components/mdx/mastering-ethereum/diagrams/met2-01-what-is-ethereum";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第1章：什么是以太坊的对象、状态、信任边界与官方目录坐标
- 能推导状态转换、Gas、状态承诺和证据门禁四组约束
- 能实现并运行三段最小代码，比较正常、边界和单故障样本
- 能设计独立复核并证明“能比较以太坊与比特币的目标和状态模型，并解释通用区块链怎样演化为DApp平台。”

</Objectives>

## 为什么从可验证状态开始

本页按官方第二版在线目录重建 **Chapter 1. What Is Ethereum?**，覆盖该正式单元的 14 个二至五级标题。教学顺序保留原目录的依赖关系，但正文全部独立撰写：每个标题都转成对象、前状态、输入、规则、后状态和失败证据，避免只给术语摘要。

以太坊是由全球单例状态、确定性状态转换函数、P2P传播和PoS共识共同维护的通用计算平台。图灵完备带来表达力，也要求Gas、安全边界和独立验证。 阅读时把“客户端或网页显示了什么”和“协议能验证什么”分开：钱包、浏览器、RPC与索引器是观察面，规范编码、签名、交易、收据、区块、状态根、证明和最终化检查点才是可复核对象。任何结论都要绑定链ID、客户端版本、区块坐标与原始输入。

实验统一在本地开发链或公开测试网完成，私钥只使用一次性测试向量。先写预期，再运行；先保存原始对象，再看格式化界面。正常样本证明主路径，边界样本选择零值、最大值、恰好阈值或时序边缘，单故障样本只改变链ID、nonce、一个字节、一个签名、一个价格或一个证明条件。

相同前状态与有效交易序列必须得到相同后状态根；任何非确定性输入都不能直接进入共识执行。 若执行失败，证据必须说明首个拒绝规则、错误数据、消耗的资源以及状态是否回滚；若执行成功，还要区分交易池接纳、区块包含、执行状态与PoS最终性，不能把交易哈希当成最终结算。

通过标准是：能比较以太坊与比特币的目标和状态模型，并解释通用区块链怎样演化为DApp平台。 独立复核者应能从空目录和相同测试向量重建结果；缺失来源、版本、输入、前状态、后状态或失败证据任一项，结论都不能进入下一页。

## 版次、对象与验收合同

<Term def="世界计算机是第1章：什么是以太坊的第1个复核坐标，必须绑定对象、状态、规则和失败证据。">
  世界计算机
</Term>
、
<Term def="单例状态是第1章：什么是以太坊的第2个复核坐标，必须绑定对象、状态、规则和失败证据。">
  单例状态
</Term>
、
<Term def="状态转换是第1章：什么是以太坊的第3个复核坐标，必须绑定对象、状态、规则和失败证据。">
  状态转换
</Term>
、
<Term def="图灵完备是第1章：什么是以太坊的第4个复核坐标，必须绑定对象、状态、规则和失败证据。">
  图灵完备
</Term>
、
<Term def="Gas计量是第1章：什么是以太坊的第5个复核坐标，必须绑定对象、状态、规则和失败证据。">
  Gas计量
</Term>
共同构成本页的复核坐标。以太坊是由全球单例状态、确定性状态转换函数、P2P传播和PoS共识共同维护的通用计算平台。图灵完备带来表达力，也要求Gas、安全边界和独立验证。

<Callout type="info">
  **验收命题：**
  能比较以太坊与比特币的目标和状态模型，并解释通用区块链怎样演化为DApp平台。
</Callout>

## 核心约束与推导

$$
S_{t+1}=Apply(S_t, T_t), \quad Valid(T_t,S_t)=1
$$

$$
gasUsed(T) \le gasLimit(T), \quad fee=gasUsed \times effectiveGasPrice
$$

$$
stateRoot_{n}=Commit(accounts,storage,code,balances)
$$

$$
gate=source \times version \times replay \times boundary \times failureEvidence
$$

状态转移只接受针对当前前状态有效的输入；Gas约束把资源消耗纳入确定性执行；状态根把大量账户与存储压缩为区块可承诺对象。证据门禁采用乘法语义，任一因子缺失都不能用其他截图或口头解释补偿。

## 三段可运行骨架

```typescript
type EthereumEvidence = {
  chainId: number;
  block: string;
  beforeRoot: string;
  inputHash: string;
  afterRoot?: string;
  error?: string;
};

export function evidenceGate(run: EthereumEvidence) {
  return {
    page: "met2-01-what-is-ethereum",
    accepted: Boolean(run.afterRoot || run.error),
    immutableInput: run.inputHash,
  };
}
```

```solidity
// SPDX-License-Identifier: MIT
pragma solidity ^0.8.26;

contract InvariantProbe {
    uint256 public value;
    event Applied(uint256 beforeValue, uint256 afterValue);

    function apply(uint256 next) external {
        uint256 beforeValue = value;
        require(next >= beforeValue, "non-monotonic");
        value = next;
        emit Applied(beforeValue, next);
    }
}
```

```typescript
function applyCandidate(
  state: bigint,
  next: bigint,
  gasUsed: bigint,
  gasLimit: bigint,
) {
  if (gasUsed > gasLimit)
    return { accepted: false, state, reason: "out-of-gas" };
  if (next < state)
    return { accepted: false, state, reason: "invalid-transition" };
  return { accepted: true, state: next, reason: null };
}
```

第一段把成功与失败放进同一证据合同；第二段给出可观察事件和显式不变量；第三段证明拒绝路径保持原状态。真实系统应使用成熟客户端、ABI、密码与证明库，并补齐并发、重组、超时、持久化、隐私和密钥隔离。

## 正式标题逐项讲解

### Ethereum Compared to Bitcoin

“Ethereum Compared to Bitcoin”是本单元正式标题 1/14，原目录层级 h2。以太坊是由全球单例状态、确定性状态转换函数、P2P传播和PoS共识共同维护的通用计算平台。图灵完备带来表达力，也要求Gas、安全边界和独立验证。 本标题要求把术语落到明确对象和状态转换：给出输入、前置条件、算法或协议规则、可观察输出以及至少一个拒绝样本。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。相同前状态与有效交易序列必须得到相同后状态根；任何非确定性输入都不能直接进入共识执行。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Components of a Blockchain

“Components of a Blockchain”是本单元正式标题 2/14，原目录层级 h2。以太坊是由全球单例状态、确定性状态转换函数、P2P传播和PoS共识共同维护的通用计算平台。图灵完备带来表达力，也要求Gas、安全边界和独立验证。 本标题要求把术语落到明确对象和状态转换：给出输入、前置条件、算法或协议规则、可观察输出以及至少一个拒绝样本。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。相同前状态与有效交易序列必须得到相同后状态根；任何非确定性输入都不能直接进入共识执行。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### The Birth of Ethereum

“The Birth of Ethereum”是本单元正式标题 3/14，原目录层级 h2。以太坊是由全球单例状态、确定性状态转换函数、P2P传播和PoS共识共同维护的通用计算平台。图灵完备带来表达力，也要求Gas、安全边界和独立验证。 本标题要求把术语落到明确对象和状态转换：给出输入、前置条件、算法或协议规则、可观察输出以及至少一个拒绝样本。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。相同前状态与有效交易序列必须得到相同后状态根；任何非确定性输入都不能直接进入共识执行。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Ethereum’s Stages of Development

“Ethereum’s Stages of Development”是本单元正式标题 4/14，原目录层级 h2。以太坊是由全球单例状态、确定性状态转换函数、P2P传播和PoS共识共同维护的通用计算平台。图灵完备带来表达力，也要求Gas、安全边界和独立验证。 本标题要求把术语落到明确对象和状态转换：给出输入、前置条件、算法或协议规则、可观察输出以及至少一个拒绝样本。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。相同前状态与有效交易序列必须得到相同后状态根；任何非确定性输入都不能直接进入共识执行。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Ethereum: A General-Purpose Blockchain

“Ethereum: A General-Purpose Blockchain”是本单元正式标题 5/14，原目录层级 h2。以太坊是由全球单例状态、确定性状态转换函数、P2P传播和PoS共识共同维护的通用计算平台。图灵完备带来表达力，也要求Gas、安全边界和独立验证。 本标题要求把术语落到明确对象和状态转换：给出输入、前置条件、算法或协议规则、可观察输出以及至少一个拒绝样本。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。相同前状态与有效交易序列必须得到相同后状态根；任何非确定性输入都不能直接进入共识执行。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Ethereum’s Components

“Ethereum’s Components”是本单元正式标题 6/14，原目录层级 h2。以太坊是由全球单例状态、确定性状态转换函数、P2P传播和PoS共识共同维护的通用计算平台。图灵完备带来表达力，也要求Gas、安全边界和独立验证。 本标题要求把术语落到明确对象和状态转换：给出输入、前置条件、算法或协议规则、可观察输出以及至少一个拒绝样本。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。相同前状态与有效交易序列必须得到相同后状态根；任何非确定性输入都不能直接进入共识执行。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Ethereum and Turing Completeness

“Ethereum and Turing Completeness”是本单元正式标题 7/14，原目录层级 h2。以太坊是由全球单例状态、确定性状态转换函数、P2P传播和PoS共识共同维护的通用计算平台。图灵完备带来表达力，也要求Gas、安全边界和独立验证。 本标题要求把术语落到明确对象和状态转换：给出输入、前置条件、算法或协议规则、可观察输出以及至少一个拒绝样本。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。相同前状态与有效交易序列必须得到相同后状态根；任何非确定性输入都不能直接进入共识执行。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Turing Completeness as a “Feature”

“Turing Completeness as a “Feature””是本单元正式标题 8/14，原目录层级 h2。以太坊是由全球单例状态、确定性状态转换函数、P2P传播和PoS共识共同维护的通用计算平台。图灵完备带来表达力，也要求Gas、安全边界和独立验证。 本标题要求把术语落到明确对象和状态转换：给出输入、前置条件、算法或协议规则、可观察输出以及至少一个拒绝样本。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。相同前状态与有效交易序列必须得到相同后状态根；任何非确定性输入都不能直接进入共识执行。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Implications of Turing Completeness

“Implications of Turing Completeness”是本单元正式标题 9/14，原目录层级 h2。以太坊是由全球单例状态、确定性状态转换函数、P2P传播和PoS共识共同维护的通用计算平台。图灵完备带来表达力，也要求Gas、安全边界和独立验证。 本标题要求把术语落到明确对象和状态转换：给出输入、前置条件、算法或协议规则、可观察输出以及至少一个拒绝样本。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。相同前状态与有效交易序列必须得到相同后状态根；任何非确定性输入都不能直接进入共识执行。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### From General-Purpose Blockchains to DApps

“From General-Purpose Blockchains to DApps”是本单元正式标题 10/14，原目录层级 h2。这是端到端应用边界。前端、钱包、RPC、合约、索引器和链下存储分别保存不同视图；最终判断回到固定链ID上的合约代码、交易收据、事件和状态。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。相同前状态与有效交易序列必须得到相同后状态根；任何非确定性输入都不能直接进入共识执行。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### The Third Age of the Internet

“The Third Age of the Internet”是本单元正式标题 11/14，原目录层级 h2。以太坊是由全球单例状态、确定性状态转换函数、P2P传播和PoS共识共同维护的通用计算平台。图灵完备带来表达力，也要求Gas、安全边界和独立验证。 本标题要求把术语落到明确对象和状态转换：给出输入、前置条件、算法或协议规则、可观察输出以及至少一个拒绝样本。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。相同前状态与有效交易序列必须得到相同后状态根；任何非确定性输入都不能直接进入共识执行。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Ethereum’s Development Culture

“Ethereum’s Development Culture”是本单元正式标题 12/14，原目录层级 h2。以太坊是由全球单例状态、确定性状态转换函数、P2P传播和PoS共识共同维护的通用计算平台。图灵完备带来表达力，也要求Gas、安全边界和独立验证。 本标题要求把术语落到明确对象和状态转换：给出输入、前置条件、算法或协议规则、可观察输出以及至少一个拒绝样本。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。相同前状态与有效交易序列必须得到相同后状态根；任何非确定性输入都不能直接进入共识执行。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Why Learn Ethereum?

“Why Learn Ethereum?”是本单元正式标题 13/14，原目录层级 h2。以太坊是由全球单例状态、确定性状态转换函数、P2P传播和PoS共识共同维护的通用计算平台。图灵完备带来表达力，也要求Gas、安全边界和独立验证。 本标题要求把术语落到明确对象和状态转换：给出输入、前置条件、算法或协议规则、可观察输出以及至少一个拒绝样本。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。相同前状态与有效交易序列必须得到相同后状态根；任何非确定性输入都不能直接进入共识执行。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Conclusion

“Conclusion”是本单元正式标题 14/14，原目录层级 h2。这里不是重复定义，而是把本章对象收束为可验收合同：相同前状态与有效交易序列必须得到相同后状态根；任何非确定性输入都不能直接进入共识执行。 复习时必须能从结论反向定位输入、规则、输出和失败证据。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。相同前状态与有效交易序列必须得到相同后状态根；任何非确定性输入都不能直接进入共识执行。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

## 正常、边界与故障矩阵

| 样本   | 只改变的变量                            | 预期结果             | 必存证据                   |
| ------ | --------------------------------------- | -------------------- | -------------------------- |
| 正常   | 有效链ID、输入、签名、Gas与前状态       | 接受并得到预期后状态 | 原始交易、收据、前后状态根 |
| 边界   | 零值、最大值、恰好阈值或时序边缘        | 按明确规则接受或拒绝 | 边界值、判断分支、资源成本 |
| 单故障 | 链ID、nonce、签名、价格、证明或数据一项 | 首个无效规则处拒绝   | 错误数据、首差、原状态     |

先预测每个样本的状态根、返回值和错误，再操作滑块；若两个变量同时变化，实验失去归因能力，必须重置。

## 三步交互实验

<Stepper>
  <Step title="追踪状态转换">
    先预测当前阶段的输入、验证依据与后状态，再拖动阶段滑块定位首个分叉。
    <Met201WhatIsEthereumFlowLab />
  </Step>
  <Step title="改变Gas与负载">
    一次只调整Gas上限或负载，观察执行预算、状态承诺和最终性指标怎样变化。
    <Met201WhatIsEthereumExperimentLab />
  </Step>
  <Step title="注入单一故障">
    注入错误链、签名、状态、数据或证明，要求门禁在提交前拒绝并指出回退点。
    <Met201WhatIsEthereumEvidenceLab />
  </Step>
</Stepper>

## 常见失败与回退

<Callout type="trap">
  **把界面成功当成协议成功：**
  交易哈希、RPC返回或前端提示只表示某一观察点。回退到原始交易、收据状态、区块哈希、状态根和最终化检查点逐项核对。
</Callout>

<Callout type="trap">
  **修复时同时改变多个变量：**
  升级客户端、换网络、改账户又改输入会失去首差。恢复固定基线，只改变一个变量，重放原失败、相邻边界和正常样本。
</Callout>

## 本章回顾

以太坊是由全球单例状态、确定性状态转换函数、P2P传播和PoS共识共同维护的通用计算平台。图灵完备带来表达力，也要求Gas、安全边界和独立验证。 核心不是记住工具名称，而是能从官方标题定位对象、规则与证据。相同前状态与有效交易序列必须得到相同后状态根；任何非确定性输入都不能直接进入共识执行。 完成后应删除临时密钥和测试状态，保留不含秘密的版本、输入哈希、收据、状态根、错误与复核结论。

<Glossary>

<GlossaryItem term="世界计算机">
  世界计算机用于界定第1章：什么是以太坊中的输入、验证规则和可观察结果。
</GlossaryItem>

<GlossaryItem term="单例状态">
  单例状态用于界定第1章：什么是以太坊中的输入、验证规则和可观察结果。
</GlossaryItem>

<GlossaryItem term="状态转换">
  状态转换用于界定第1章：什么是以太坊中的输入、验证规则和可观察结果。
</GlossaryItem>

<GlossaryItem term="图灵完备">
  图灵完备用于界定第1章：什么是以太坊中的输入、验证规则和可观察结果。
</GlossaryItem>

<GlossaryItem term="Gas计量">
  Gas计量用于界定第1章：什么是以太坊中的输入、验证规则和可观察结果。
</GlossaryItem>

</Glossary>

<Exercises>

1. 如何为Chapter 1. What Is Ethereum?建立最小可重放基线？

<Answer>
  固定官方第二版页面、链ID、客户端版本、账户、区块坐标、前状态和原始输入，先手算预期，再保存解析、验证、执行、收据与状态证据。
</Answer>

2. 如何证明“能比较以太坊与比特币的目标和状态模型，并解释通用区块链怎样演化为DApp平台。”不是一次偶然成功？

<Answer>
  在同一基线上执行正常、边界和单故障样本，由独立客户端核对原始对象、状态根与拒绝点；三组证据一致才通过。
</Answer>

3. 修复Chapter 1. What Is Ethereum?的一个失败后怎样回归？

<Answer>
  先重放原失败，再运行相邻边界和正常基线；按版本、网络、编码、签名、状态、Gas、证明与最终性定位首差，全部不变量通过后才进入下一页。
</Answer>

</Exercises>

## 前后导航

- [上一页：前言：版本、读者与证据合同](/learn/mastering-ethereum/01-preface/met2-preface)
- [下一页：第2章：以太坊基础操作](/learn/mastering-ethereum/03-02-ethereum-basics/met2-02-ethereum-basics)

<Attribution
  adaptedFrom="Andreas M. Antonopoulos、Gavin Wood、Carlo Parisi、Alessandro Mazza、Niccolò Pozzolini《Mastering Ethereum》第2版，O'Reilly Media，2025年10月，570页，数字版ISBN 9781098168414，印刷版ISBN 9781098168421 · 官方在线版前言与17章共516个二至五级标题 · 基于公开目录的独立中文教学重构，不复制受限正文"
  adaptedUrl="https://masteringethereum.xyz/chapter_1.html"
/>
