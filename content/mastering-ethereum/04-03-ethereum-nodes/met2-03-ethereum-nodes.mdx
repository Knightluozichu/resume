---
title: "第3章：以太坊节点"
type: B
section: "Mastering Ethereum（第2版）"
order: 4
description: "PoS以太坊节点由执行客户端与共识客户端配对，Engine API协调区块载荷，JSON-RPC面向应用暴露受限视图。同步、检查点、客户端多样性和接口隔离共同决定可验证性。"
demo: true
math: true
sourceUrl: "https://masteringethereum.xyz/chapter_3.html"
draft: false
---

import {
  Met203EthereumNodesFlowLab,
  Met203EthereumNodesExperimentLab,
  Met203EthereumNodesEvidenceLab,
} from "@/components/mdx/mastering-ethereum/diagrams/met2-03-ethereum-nodes";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第3章：以太坊节点的对象、状态、信任边界与官方目录坐标
- 能推导状态转换、Gas、状态承诺和证据门禁四组约束
- 能实现并运行三段最小代码，比较正常、边界和单故障样本
- 能设计独立复核并证明“能部署一对客户端，证明网络、同步状态和最终化检查点一致，并对错误JWT或错误链给出拒绝证据。”

</Objectives>

## 为什么从可验证状态开始

本页按官方第二版在线目录重建 **Chapter 3. Ethereum Nodes**，覆盖该正式单元的 24 个二至五级标题。教学顺序保留原目录的依赖关系，但正文全部独立撰写：每个标题都转成对象、前状态、输入、规则、后状态和失败证据，避免只给术语摘要。

PoS以太坊节点由执行客户端与共识客户端配对，Engine API协调区块载荷，JSON-RPC面向应用暴露受限视图。同步、检查点、客户端多样性和接口隔离共同决定可验证性。 阅读时把“客户端或网页显示了什么”和“协议能验证什么”分开：钱包、浏览器、RPC与索引器是观察面，规范编码、签名、交易、收据、区块、状态根、证明和最终化检查点才是可复核对象。任何结论都要绑定链ID、客户端版本、区块坐标与原始输入。

实验统一在本地开发链或公开测试网完成，私钥只使用一次性测试向量。先写预期，再运行；先保存原始对象，再看格式化界面。正常样本证明主路径，边界样本选择零值、最大值、恰好阈值或时序边缘，单故障样本只改变链ID、nonce、一个字节、一个签名、一个价格或一个证明条件。

执行与共识客户端必须连接同一网络并认可同一规范状态；管理接口不得暴露到不受信任网络。 若执行失败，证据必须说明首个拒绝规则、错误数据、消耗的资源以及状态是否回滚；若执行成功，还要区分交易池接纳、区块包含、执行状态与PoS最终性，不能把交易哈希当成最终结算。

通过标准是：能部署一对客户端，证明网络、同步状态和最终化检查点一致，并对错误JWT或错误链给出拒绝证据。 独立复核者应能从空目录和相同测试向量重建结果；缺失来源、版本、输入、前状态、后状态或失败证据任一项，结论都不能进入下一页。

## 版次、对象与验收合同

<Term def="执行客户端是第3章：以太坊节点的第1个复核坐标，必须绑定对象、状态、规则和失败证据。">
  执行客户端
</Term>
、
<Term def="共识客户端是第3章：以太坊节点的第2个复核坐标，必须绑定对象、状态、规则和失败证据。">
  共识客户端
</Term>
、
<Term def="Engine API是第3章：以太坊节点的第3个复核坐标，必须绑定对象、状态、规则和失败证据。">
  Engine API
</Term>
、
<Term def="JSON-RPC是第3章：以太坊节点的第4个复核坐标，必须绑定对象、状态、规则和失败证据。">
  JSON-RPC
</Term>
、
<Term def="检查点同步是第3章：以太坊节点的第5个复核坐标，必须绑定对象、状态、规则和失败证据。">
  检查点同步
</Term>
共同构成本页的复核坐标。PoS以太坊节点由执行客户端与共识客户端配对，Engine
API协调区块载荷，JSON-RPC面向应用暴露受限视图。同步、检查点、客户端多样性和接口隔离共同决定可验证性。

<Callout type="info">
  **验收命题：**
  能部署一对客户端，证明网络、同步状态和最终化检查点一致，并对错误JWT或错误链给出拒绝证据。
</Callout>

## 核心约束与推导

$$
S_{t+1}=Apply(S_t, T_t), \quad Valid(T_t,S_t)=1
$$

$$
gasUsed(T) \le gasLimit(T), \quad fee=gasUsed \times effectiveGasPrice
$$

$$
stateRoot_{n}=Commit(accounts,storage,code,balances)
$$

$$
gate=source \times version \times replay \times boundary \times failureEvidence
$$

状态转移只接受针对当前前状态有效的输入；Gas约束把资源消耗纳入确定性执行；状态根把大量账户与存储压缩为区块可承诺对象。证据门禁采用乘法语义，任一因子缺失都不能用其他截图或口头解释补偿。

## 三段可运行骨架

```typescript
type EthereumEvidence = {
  chainId: number;
  block: string;
  beforeRoot: string;
  inputHash: string;
  afterRoot?: string;
  error?: string;
};

export function evidenceGate(run: EthereumEvidence) {
  return {
    page: "met2-03-ethereum-nodes",
    accepted: Boolean(run.afterRoot || run.error),
    immutableInput: run.inputHash,
  };
}
```

```solidity
// SPDX-License-Identifier: MIT
pragma solidity ^0.8.26;

contract InvariantProbe {
    uint256 public value;
    event Applied(uint256 beforeValue, uint256 afterValue);

    function apply(uint256 next) external {
        uint256 beforeValue = value;
        require(next >= beforeValue, "non-monotonic");
        value = next;
        emit Applied(beforeValue, next);
    }
}
```

```typescript
function applyCandidate(
  state: bigint,
  next: bigint,
  gasUsed: bigint,
  gasLimit: bigint,
) {
  if (gasUsed > gasLimit)
    return { accepted: false, state, reason: "out-of-gas" };
  if (next < state)
    return { accepted: false, state, reason: "invalid-transition" };
  return { accepted: true, state: next, reason: null };
}
```

第一段把成功与失败放进同一证据合同；第二段给出可观察事件和显式不变量；第三段证明拒绝路径保持原状态。真实系统应使用成熟客户端、ABI、密码与证明库，并补齐并发、重组、超时、持久化、隐私和密钥隔离。

## 正式标题逐项讲解

### Ethereum Networks

“Ethereum Networks”是本单元正式标题 1/24，原目录层级 h2。PoS以太坊节点由执行客户端与共识客户端配对，Engine API协调区块载荷，JSON-RPC面向应用暴露受限视图。同步、检查点、客户端多样性和接口隔离共同决定可验证性。 本标题要求把术语落到明确对象和状态转换：给出输入、前置条件、算法或协议规则、可观察输出以及至少一个拒绝样本。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。执行与共识客户端必须连接同一网络并认可同一规范状态；管理接口不得暴露到不受信任网络。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Should I Run a Full Node?

“Should I Run a Full Node?”是本单元正式标题 2/24，原目录层级 h2。这是可复现实作坐标。先固定工具版本、网络、账户与原始输入，写出预期对象，再执行命令或合约；保存配置、交易哈希、收据、日志、状态首差与回退点，不能只截取成功界面。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。执行与共识客户端必须连接同一网络并认可同一规范状态；管理接口不得暴露到不受信任网络。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Full Node Advantages and Disadvantages

“Full Node Advantages and Disadvantages”是本单元正式标题 3/24，原目录层级 h3。PoS以太坊节点由执行客户端与共识客户端配对，Engine API协调区块载荷，JSON-RPC面向应用暴露受限视图。同步、检查点、客户端多样性和接口隔离共同决定可验证性。 本标题要求把术语落到明确对象和状态转换：给出输入、前置条件、算法或协议规则、可观察输出以及至少一个拒绝样本。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。执行与共识客户端必须连接同一网络并认可同一规范状态；管理接口不得暴露到不受信任网络。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Public Testnet Advantages and Disadvantages

“Public Testnet Advantages and Disadvantages”是本单元正式标题 4/24，原目录层级 h3。PoS以太坊节点由执行客户端与共识客户端配对，Engine API协调区块载荷，JSON-RPC面向应用暴露受限视图。同步、检查点、客户端多样性和接口隔离共同决定可验证性。 本标题要求把术语落到明确对象和状态转换：给出输入、前置条件、算法或协议规则、可观察输出以及至少一个拒绝样本。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。执行与共识客户端必须连接同一网络并认可同一规范状态；管理接口不得暴露到不受信任网络。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Local Blockchain Simulation Advantages and Disadvantages

“Local Blockchain Simulation Advantages and Disadvantages”是本单元正式标题 5/24，原目录层级 h3。PoS以太坊节点由执行客户端与共识客户端配对，Engine API协调区块载荷，JSON-RPC面向应用暴露受限视图。同步、检查点、客户端多样性和接口隔离共同决定可验证性。 本标题要求把术语落到明确对象和状态转换：给出输入、前置条件、算法或协议规则、可观察输出以及至少一个拒绝样本。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。执行与共识客户端必须连接同一网络并认可同一规范状态；管理接口不得暴露到不受信任网络。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Running an Ethereum Node

“Running an Ethereum Node”是本单元正式标题 6/24，原目录层级 h2。这是可复现实作坐标。先固定工具版本、网络、账户与原始输入，写出预期对象，再执行命令或合约；保存配置、交易哈希、收据、日志、状态首差与回退点，不能只截取成功界面。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。执行与共识客户端必须连接同一网络并认可同一规范状态；管理接口不得暴露到不受信任网络。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Hardware Requirements for a Full Node

“Hardware Requirements for a Full Node”是本单元正式标题 7/24，原目录层级 h3。PoS以太坊节点由执行客户端与共识客户端配对，Engine API协调区块载荷，JSON-RPC面向应用暴露受限视图。同步、检查点、客户端多样性和接口隔离共同决定可验证性。 本标题要求把术语落到明确对象和状态转换：给出输入、前置条件、算法或协议规则、可观察输出以及至少一个拒绝样本。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。执行与共识客户端必须连接同一网络并认可同一规范状态；管理接口不得暴露到不受信任网络。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Software Requirements for Building and Running a Client

“Software Requirements for Building and Running a Client”是本单元正式标题 8/24，原目录层级 h3。这是可复现实作坐标。先固定工具版本、网络、账户与原始输入，写出预期对象，再执行命令或合约；保存配置、交易哈希、收据、日志、状态首差与回退点，不能只截取成功界面。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。执行与共识客户端必须连接同一网络并认可同一规范状态；管理接口不得暴露到不受信任网络。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Preparation Phase

“Preparation Phase”是本单元正式标题 9/24，原目录层级 h3。PoS以太坊节点由执行客户端与共识客户端配对，Engine API协调区块载荷，JSON-RPC面向应用暴露受限视图。同步、检查点、客户端多样性和接口隔离共同决定可验证性。 本标题要求把术语落到明确对象和状态转换：给出输入、前置条件、算法或协议规则、可观察输出以及至少一个拒绝样本。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。执行与共识客户端必须连接同一网络并认可同一规范状态；管理接口不得暴露到不受信任网络。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Geth-Prysm

“Geth-Prysm”是本单元正式标题 10/24，原目录层级 h3。PoS以太坊节点由执行客户端与共识客户端配对，Engine API协调区块载荷，JSON-RPC面向应用暴露受限视图。同步、检查点、客户端多样性和接口隔离共同决定可验证性。 本标题要求把术语落到明确对象和状态转换：给出输入、前置条件、算法或协议规则、可观察输出以及至少一个拒绝样本。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。执行与共识客户端必须连接同一网络并认可同一规范状态；管理接口不得暴露到不受信任网络。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Geth

“Geth”是本单元正式标题 11/24，原目录层级 h4。PoS以太坊节点由执行客户端与共识客户端配对，Engine API协调区块载荷，JSON-RPC面向应用暴露受限视图。同步、检查点、客户端多样性和接口隔离共同决定可验证性。 本标题要求把术语落到明确对象和状态转换：给出输入、前置条件、算法或协议规则、可观察输出以及至少一个拒绝样本。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。执行与共识客户端必须连接同一网络并认可同一规范状态；管理接口不得暴露到不受信任网络。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Prysm

“Prysm”是本单元正式标题 12/24，原目录层级 h4。PoS以太坊节点由执行客户端与共识客户端配对，Engine API协调区块载荷，JSON-RPC面向应用暴露受限视图。同步、检查点、客户端多样性和接口隔离共同决定可验证性。 本标题要求把术语落到明确对象和状态转换：给出输入、前置条件、算法或协议规则、可观察输出以及至少一个拒绝样本。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。执行与共识客户端必须连接同一网络并认可同一规范状态；管理接口不得暴露到不受信任网络。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Run the node

“Run the node”是本单元正式标题 13/24，原目录层级 h4。这是可复现实作坐标。先固定工具版本、网络、账户与原始输入，写出预期对象，再执行命令或合约；保存配置、交易哈希、收据、日志、状态首差与回退点，不能只截取成功界面。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。执行与共识客户端必须连接同一网络并认可同一规范状态；管理接口不得暴露到不受信任网络。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Reth-Lighthouse

“Reth-Lighthouse”是本单元正式标题 14/24，原目录层级 h3。PoS以太坊节点由执行客户端与共识客户端配对，Engine API协调区块载荷，JSON-RPC面向应用暴露受限视图。同步、检查点、客户端多样性和接口隔离共同决定可验证性。 本标题要求把术语落到明确对象和状态转换：给出输入、前置条件、算法或协议规则、可观察输出以及至少一个拒绝样本。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。执行与共识客户端必须连接同一网络并认可同一规范状态；管理接口不得暴露到不受信任网络。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Reth

“Reth”是本单元正式标题 15/24，原目录层级 h4。PoS以太坊节点由执行客户端与共识客户端配对，Engine API协调区块载荷，JSON-RPC面向应用暴露受限视图。同步、检查点、客户端多样性和接口隔离共同决定可验证性。 本标题要求把术语落到明确对象和状态转换：给出输入、前置条件、算法或协议规则、可观察输出以及至少一个拒绝样本。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。执行与共识客户端必须连接同一网络并认可同一规范状态；管理接口不得暴露到不受信任网络。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Lighthouse

“Lighthouse”是本单元正式标题 16/24，原目录层级 h4。PoS以太坊节点由执行客户端与共识客户端配对，Engine API协调区块载荷，JSON-RPC面向应用暴露受限视图。同步、检查点、客户端多样性和接口隔离共同决定可验证性。 本标题要求把术语落到明确对象和状态转换：给出输入、前置条件、算法或协议规则、可观察输出以及至少一个拒绝样本。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。执行与共识客户端必须连接同一网络并认可同一规范状态；管理接口不得暴露到不受信任网络。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Run the node

“Run the node”是本单元正式标题 17/24，原目录层级 h4。这是可复现实作坐标。先固定工具版本、网络、账户与原始输入，写出预期对象，再执行命令或合约；保存配置、交易哈希、收据、日志、状态首差与回退点，不能只截取成功界面。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。执行与共识客户端必须连接同一网络并认可同一规范状态；管理接口不得暴露到不受信任网络。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### The First Synchronization of Ethereum-Based Blockchains

“The First Synchronization of Ethereum-Based Blockchains”是本单元正式标题 18/24，原目录层级 h2。PoS以太坊节点由执行客户端与共识客户端配对，Engine API协调区块载荷，JSON-RPC面向应用暴露受限视图。同步、检查点、客户端多样性和接口隔离共同决定可验证性。 本标题要求把术语落到明确对象和状态转换：给出输入、前置条件、算法或协议规则、可观察输出以及至少一个拒绝样本。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。执行与共识客户端必须连接同一网络并认可同一规范状态；管理接口不得暴露到不受信任网络。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### The JSON-RPC Interface

“The JSON-RPC Interface”是本单元正式标题 19/24，原目录层级 h2。PoS以太坊节点由执行客户端与共识客户端配对，Engine API协调区块载荷，JSON-RPC面向应用暴露受限视图。同步、检查点、客户端多样性和接口隔离共同决定可验证性。 本标题要求把术语落到明确对象和状态转换：给出输入、前置条件、算法或协议规则、可观察输出以及至少一个拒绝样本。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。执行与共识客户端必须连接同一网络并认可同一规范状态；管理接口不得暴露到不受信任网络。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Remote Ethereum Clients

“Remote Ethereum Clients”是本单元正式标题 20/24，原目录层级 h2。PoS以太坊节点由执行客户端与共识客户端配对，Engine API协调区块载荷，JSON-RPC面向应用暴露受限视图。同步、检查点、客户端多样性和接口隔离共同决定可验证性。 本标题要求把术语落到明确对象和状态转换：给出输入、前置条件、算法或协议规则、可观察输出以及至少一个拒绝样本。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。执行与共识客户端必须连接同一网络并认可同一规范状态；管理接口不得暴露到不受信任网络。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Mobile (Smartphone) Wallets

“Mobile (Smartphone) Wallets”是本单元正式标题 21/24，原目录层级 h3。这是控制权与恢复边界。钱包界面不拥有链上资产，密钥、种子、口令、派生路径、链ID和地址方案共同决定可控制账户；恢复实验必须在空环境复现同一地址集合。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。执行与共识客户端必须连接同一网络并认可同一规范状态；管理接口不得暴露到不受信任网络。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Browser Wallets

“Browser Wallets”是本单元正式标题 22/24，原目录层级 h3。这是控制权与恢复边界。钱包界面不拥有链上资产，密钥、种子、口令、派生路径、链ID和地址方案共同决定可控制账户；恢复实验必须在空环境复现同一地址集合。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。执行与共识客户端必须连接同一网络并认可同一规范状态；管理接口不得暴露到不受信任网络。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Hardware Wallets

“Hardware Wallets”是本单元正式标题 23/24，原目录层级 h3。这是控制权与恢复边界。钱包界面不拥有链上资产，密钥、种子、口令、派生路径、链ID和地址方案共同决定可控制账户；恢复实验必须在空环境复现同一地址集合。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。执行与共识客户端必须连接同一网络并认可同一规范状态；管理接口不得暴露到不受信任网络。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Conclusion

“Conclusion”是本单元正式标题 24/24，原目录层级 h2。这里不是重复定义，而是把本章对象收束为可验收合同：执行与共识客户端必须连接同一网络并认可同一规范状态；管理接口不得暴露到不受信任网络。 复习时必须能从结论反向定位输入、规则、输出和失败证据。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。执行与共识客户端必须连接同一网络并认可同一规范状态；管理接口不得暴露到不受信任网络。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

## 正常、边界与故障矩阵

| 样本   | 只改变的变量                            | 预期结果             | 必存证据                   |
| ------ | --------------------------------------- | -------------------- | -------------------------- |
| 正常   | 有效链ID、输入、签名、Gas与前状态       | 接受并得到预期后状态 | 原始交易、收据、前后状态根 |
| 边界   | 零值、最大值、恰好阈值或时序边缘        | 按明确规则接受或拒绝 | 边界值、判断分支、资源成本 |
| 单故障 | 链ID、nonce、签名、价格、证明或数据一项 | 首个无效规则处拒绝   | 错误数据、首差、原状态     |

先预测每个样本的状态根、返回值和错误，再操作滑块；若两个变量同时变化，实验失去归因能力，必须重置。

## 三步交互实验

<Stepper>
  <Step title="追踪状态转换">
    先预测当前阶段的输入、验证依据与后状态，再拖动阶段滑块定位首个分叉。
    <Met203EthereumNodesFlowLab />
  </Step>
  <Step title="改变Gas与负载">
    一次只调整Gas上限或负载，观察执行预算、状态承诺和最终性指标怎样变化。
    <Met203EthereumNodesExperimentLab />
  </Step>
  <Step title="注入单一故障">
    注入错误链、签名、状态、数据或证明，要求门禁在提交前拒绝并指出回退点。
    <Met203EthereumNodesEvidenceLab />
  </Step>
</Stepper>

## 常见失败与回退

<Callout type="trap">
  **把界面成功当成协议成功：**
  交易哈希、RPC返回或前端提示只表示某一观察点。回退到原始交易、收据状态、区块哈希、状态根和最终化检查点逐项核对。
</Callout>

<Callout type="trap">
  **修复时同时改变多个变量：**
  升级客户端、换网络、改账户又改输入会失去首差。恢复固定基线，只改变一个变量，重放原失败、相邻边界和正常样本。
</Callout>

## 本章回顾

PoS以太坊节点由执行客户端与共识客户端配对，Engine API协调区块载荷，JSON-RPC面向应用暴露受限视图。同步、检查点、客户端多样性和接口隔离共同决定可验证性。 核心不是记住工具名称，而是能从官方标题定位对象、规则与证据。执行与共识客户端必须连接同一网络并认可同一规范状态；管理接口不得暴露到不受信任网络。 完成后应删除临时密钥和测试状态，保留不含秘密的版本、输入哈希、收据、状态根、错误与复核结论。

<Glossary>

<GlossaryItem term="执行客户端">
  执行客户端用于界定第3章：以太坊节点中的输入、验证规则和可观察结果。
</GlossaryItem>

<GlossaryItem term="共识客户端">
  共识客户端用于界定第3章：以太坊节点中的输入、验证规则和可观察结果。
</GlossaryItem>

<GlossaryItem term="Engine API">
  Engine API用于界定第3章：以太坊节点中的输入、验证规则和可观察结果。
</GlossaryItem>

<GlossaryItem term="JSON-RPC">
  JSON-RPC用于界定第3章：以太坊节点中的输入、验证规则和可观察结果。
</GlossaryItem>

<GlossaryItem term="检查点同步">
  检查点同步用于界定第3章：以太坊节点中的输入、验证规则和可观察结果。
</GlossaryItem>

</Glossary>

<Exercises>

1. 如何为Chapter 3. Ethereum Nodes建立最小可重放基线？

<Answer>
  固定官方第二版页面、链ID、客户端版本、账户、区块坐标、前状态和原始输入，先手算预期，再保存解析、验证、执行、收据与状态证据。
</Answer>

2. 如何证明“能部署一对客户端，证明网络、同步状态和最终化检查点一致，并对错误JWT或错误链给出拒绝证据。”不是一次偶然成功？

<Answer>
  在同一基线上执行正常、边界和单故障样本，由独立客户端核对原始对象、状态根与拒绝点；三组证据一致才通过。
</Answer>

3. 修复Chapter 3. Ethereum Nodes的一个失败后怎样回归？

<Answer>
  先重放原失败，再运行相邻边界和正常基线；按版本、网络、编码、签名、状态、Gas、证明与最终性定位首差，全部不变量通过后才进入下一页。
</Answer>

</Exercises>

## 前后导航

- [上一页：第2章：以太坊基础操作](/learn/mastering-ethereum/03-02-ethereum-basics/met2-02-ethereum-basics)
- [下一页：第4章：密码学](/learn/mastering-ethereum/05-04-cryptography/met2-04-cryptography)

<Attribution
  adaptedFrom="Andreas M. Antonopoulos、Gavin Wood、Carlo Parisi、Alessandro Mazza、Niccolò Pozzolini《Mastering Ethereum》第2版，O'Reilly Media，2025年10月，570页，数字版ISBN 9781098168414，印刷版ISBN 9781098168421 · 官方在线版前言与17章共516个二至五级标题 · 基于公开目录的独立中文教学重构，不复制受限正文"
  adaptedUrl="https://masteringethereum.xyz/chapter_3.html"
/>
