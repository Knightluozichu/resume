---
title: "第4章：密码学"
type: B
section: "Mastering Ethereum（第2版）"
order: 5
description: "本章贯通secp256k1私钥与地址、Keccak-256、ECDSA、验证者BLS签名以及数据可用性使用的KZG承诺。每类原语的安全目标、输入域和验证方不同，不能互换。"
demo: true
math: true
sourceUrl: "https://masteringethereum.xyz/chapter_4.html"
draft: true
---

import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第4章：密码学的对象、状态、信任边界与官方目录坐标
- 能推导状态转换、Gas、状态承诺和证据门禁四组约束
- 能实现并运行三段最小代码，比较正常、边界和单故障样本
- 能设计独立复核并证明“能推导测试私钥到地址、验证ERC-55校验和，并区分ECDSA、BLS和KZG各自证明的命题。”

</Objectives>

## 为什么从可验证状态开始

本页按官方第二版在线目录重建 **Chapter 4. Cryptography**，覆盖该正式单元的 38 个二至五级标题。教学顺序保留原目录的依赖关系，但正文全部独立撰写：每个标题都转成对象、前状态、输入、规则、后状态和失败证据，避免只给术语摘要。

本章贯通secp256k1私钥与地址、Keccak-256、ECDSA、验证者BLS签名以及数据可用性使用的KZG承诺。每类原语的安全目标、输入域和验证方不同，不能互换。 阅读时把“客户端或网页显示了什么”和“协议能验证什么”分开：钱包、浏览器、RPC与索引器是观察面，规范编码、签名、交易、收据、区块、状态根、证明和最终化检查点才是可复核对象。任何结论都要绑定链ID、客户端版本、区块坐标与原始输入。

实验统一在本地开发链或公开测试网完成，私钥只使用一次性测试向量。先写预期，再运行；先保存原始对象，再看格式化界面。正常样本证明主路径，边界样本选择零值、最大值、恰好阈值或时序边缘，单故障样本只改变链ID、nonce、一个字节、一个签名、一个价格或一个证明条件。

密钥必须均匀生成，编码必须规范，签名和承诺验证必须绑定正确消息、域分离与公共参数。 若执行失败，证据必须说明首个拒绝规则、错误数据、消耗的资源以及状态是否回滚；若执行成功，还要区分交易池接纳、区块包含、执行状态与PoS最终性，不能把交易哈希当成最终结算。

通过标准是：能推导测试私钥到地址、验证ERC-55校验和，并区分ECDSA、BLS和KZG各自证明的命题。 独立复核者应能从空目录和相同测试向量重建结果；缺失来源、版本、输入、前状态、后状态或失败证据任一项，结论都不能进入下一页。

## 版次、对象与验收合同

<Term def="secp256k1是第4章：密码学的第1个复核坐标，必须绑定对象、状态、规则和失败证据。">secp256k1</Term>、<Term def="Keccak-256是第4章：密码学的第2个复核坐标，必须绑定对象、状态、规则和失败证据。">Keccak-256</Term>、<Term def="ECDSA是第4章：密码学的第3个复核坐标，必须绑定对象、状态、规则和失败证据。">ECDSA</Term>、<Term def="BLS签名是第4章：密码学的第4个复核坐标，必须绑定对象、状态、规则和失败证据。">BLS签名</Term>、<Term def="KZG承诺是第4章：密码学的第5个复核坐标，必须绑定对象、状态、规则和失败证据。">KZG承诺</Term>
共同构成本页的复核坐标。本章贯通secp256k1私钥与地址、Keccak-256、ECDSA、验证者BLS签名以及数据可用性使用的KZG承诺。每类原语的安全目标、输入域和验证方不同，不能互换。

<Callout type="info">
  **验收命题：**
  能推导测试私钥到地址、验证ERC-55校验和，并区分ECDSA、BLS和KZG各自证明的命题。
</Callout>

## 核心约束与推导

$$
S_{t+1}=Apply(S_t, T_t), \quad Valid(T_t,S_t)=1
$$

$$
gasUsed(T) \le gasLimit(T), \quad fee=gasUsed \times effectiveGasPrice
$$

$$
stateRoot_{n}=Commit(accounts,storage,code,balances)
$$

$$
gate=source \times version \times replay \times boundary \times failureEvidence
$$

状态转移只接受针对当前前状态有效的输入；Gas约束把资源消耗纳入确定性执行；状态根把大量账户与存储压缩为区块可承诺对象。证据门禁采用乘法语义，任一因子缺失都不能用其他截图或口头解释补偿。

## 三段可运行骨架

```typescript
type EthereumEvidence = {
  chainId: number;
  block: string;
  beforeRoot: string;
  inputHash: string;
  afterRoot?: string;
  error?: string;
};

export function evidenceGate(run: EthereumEvidence) {
  return {
    page: "met2-04-cryptography",
    accepted: Boolean(run.afterRoot || run.error),
    immutableInput: run.inputHash,
  };
}
```

```solidity
// SPDX-License-Identifier: MIT
pragma solidity ^0.8.26;

contract InvariantProbe {
    uint256 public value;
    event Applied(uint256 beforeValue, uint256 afterValue);

    function apply(uint256 next) external {
        uint256 beforeValue = value;
        require(next >= beforeValue, "non-monotonic");
        value = next;
        emit Applied(beforeValue, next);
    }
}
```

```typescript
function applyCandidate(
  state: bigint,
  next: bigint,
  gasUsed: bigint,
  gasLimit: bigint,
) {
  if (gasUsed > gasLimit)
    return { accepted: false, state, reason: "out-of-gas" };
  if (next < state)
    return { accepted: false, state, reason: "invalid-transition" };
  return { accepted: true, state: next, reason: null };
}
```

第一段把成功与失败放进同一证据合同；第二段给出可观察事件和显式不变量；第三段证明拒绝路径保持原状态。真实系统应使用成熟客户端、ABI、密码与证明库，并补齐并发、重组、超时、持久化、隐私和密钥隔离。

## 正式标题逐项讲解

### Keys and Addresses

“Keys and Addresses”是本单元正式标题 1/38，原目录层级 h2。这是密码与证明边界。必须区分秘密输入、公开输入、编码、域分离、算法输出和验证命题；使用成熟库与公开测试向量，篡改一个字节时验证应在首个不满足的规则处失败。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。密钥必须均匀生成，编码必须规范，签名和承诺验证必须绑定正确消息、域分离与公共参数。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### PKC and Cryptocurrency

“PKC and Cryptocurrency”是本单元正式标题 2/38，原目录层级 h2。这是密码与证明边界。必须区分秘密输入、公开输入、编码、域分离、算法输出和验证命题；使用成熟库与公开测试向量，篡改一个字节时验证应在首个不满足的规则处失败。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。密钥必须均匀生成，编码必须规范，签名和承诺验证必须绑定正确消息、域分离与公共参数。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Private Keys

“Private Keys”是本单元正式标题 3/38，原目录层级 h2。这是密码与证明边界。必须区分秘密输入、公开输入、编码、域分离、算法输出和验证命题；使用成熟库与公开测试向量，篡改一个字节时验证应在首个不满足的规则处失败。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。密钥必须均匀生成，编码必须规范，签名和承诺验证必须绑定正确消息、域分离与公共参数。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Public Keys

“Public Keys”是本单元正式标题 4/38，原目录层级 h2。这是密码与证明边界。必须区分秘密输入、公开输入、编码、域分离、算法输出和验证命题；使用成熟库与公开测试向量，篡改一个字节时验证应在首个不满足的规则处失败。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。密钥必须均匀生成，编码必须规范，签名和承诺验证必须绑定正确消息、域分离与公共参数。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Elliptic Curve Cryptography Explained

“Elliptic Curve Cryptography Explained”是本单元正式标题 5/38，原目录层级 h3。这是密码与证明边界。必须区分秘密输入、公开输入、编码、域分离、算法输出和验证命题；使用成熟库与公开测试向量，篡改一个字节时验证应在首个不满足的规则处失败。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。密钥必须均匀生成，编码必须规范，签名和承诺验证必须绑定正确消息、域分离与公共参数。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Elliptic Curve Arithmetic Operations

“Elliptic Curve Arithmetic Operations”是本单元正式标题 6/38，原目录层级 h3。这是密码与证明边界。必须区分秘密输入、公开输入、编码、域分离、算法输出和验证命题；使用成熟库与公开测试向量，篡改一个字节时验证应在首个不满足的规则处失败。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。密钥必须均匀生成，编码必须规范，签名和承诺验证必须绑定正确消息、域分离与公共参数。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Generating a Public Key

“Generating a Public Key”是本单元正式标题 7/38，原目录层级 h3。这是密码与证明边界。必须区分秘密输入、公开输入、编码、域分离、算法输出和验证命题；使用成熟库与公开测试向量，篡改一个字节时验证应在首个不满足的规则处失败。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。密钥必须均匀生成，编码必须规范，签名和承诺验证必须绑定正确消息、域分离与公共参数。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Elliptic Curve Libraries

“Elliptic Curve Libraries”是本单元正式标题 8/38，原目录层级 h3。这是密码与证明边界。必须区分秘密输入、公开输入、编码、域分离、算法输出和验证命题；使用成熟库与公开测试向量，篡改一个字节时验证应在首个不满足的规则处失败。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。密钥必须均匀生成，编码必须规范，签名和承诺验证必须绑定正确消息、域分离与公共参数。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Cryptographic Hash Functions

“Cryptographic Hash Functions”是本单元正式标题 9/38，原目录层级 h2。这是密码与证明边界。必须区分秘密输入、公开输入、编码、域分离、算法输出和验证命题；使用成熟库与公开测试向量，篡改一个字节时验证应在首个不满足的规则处失败。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。密钥必须均匀生成，编码必须规范，签名和承诺验证必须绑定正确消息、域分离与公共参数。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Ethereum's Cryptographic Hash Function: Keccak-256

“Ethereum's Cryptographic Hash Function: Keccak-256”是本单元正式标题 10/38，原目录层级 h3。这是密码与证明边界。必须区分秘密输入、公开输入、编码、域分离、算法输出和验证命题；使用成熟库与公开测试向量，篡改一个字节时验证应在首个不满足的规则处失败。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。密钥必须均匀生成，编码必须规范，签名和承诺验证必须绑定正确消息、域分离与公共参数。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Which Hash Function Am I Using?

“Which Hash Function Am I Using?”是本单元正式标题 11/38，原目录层级 h3。这是密码与证明边界。必须区分秘密输入、公开输入、编码、域分离、算法输出和验证命题；使用成熟库与公开测试向量，篡改一个字节时验证应在首个不满足的规则处失败。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。密钥必须均匀生成，编码必须规范，签名和承诺验证必须绑定正确消息、域分离与公共参数。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Ethereum Addresses

“Ethereum Addresses”是本单元正式标题 12/38，原目录层级 h2。这是密码与证明边界。必须区分秘密输入、公开输入、编码、域分离、算法输出和验证命题；使用成熟库与公开测试向量，篡改一个字节时验证应在首个不满足的规则处失败。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。密钥必须均匀生成，编码必须规范，签名和承诺验证必须绑定正确消息、域分离与公共参数。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Ethereum Address Formats

“Ethereum Address Formats”是本单元正式标题 13/38，原目录层级 h3。这是密码与证明边界。必须区分秘密输入、公开输入、编码、域分离、算法输出和验证命题；使用成熟库与公开测试向量，篡改一个字节时验证应在首个不满足的规则处失败。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。密钥必须均匀生成，编码必须规范，签名和承诺验证必须绑定正确消息、域分离与公共参数。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Hex Encoding with Checksum in Capitalization (ERC-55)

“Hex Encoding with Checksum in Capitalization (ERC-55)”是本单元正式标题 14/38，原目录层级 h3。这是资产接口与账本边界。接口、事件、余额、总供应、授权和接收回调必须同步满足标准；兼容函数签名不代表经济不变量、元数据或外部集成天然安全。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。密钥必须均匀生成，编码必须规范，签名和承诺验证必须绑定正确消息、域分离与公共参数。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Detecting an Error in an ERC-55 Encoded Address

“Detecting an Error in an ERC-55 Encoded Address”是本单元正式标题 15/38，原目录层级 h3。这是密码与证明边界。必须区分秘密输入、公开输入、编码、域分离、算法输出和验证命题；使用成熟库与公开测试向量，篡改一个字节时验证应在首个不满足的规则处失败。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。密钥必须均匀生成，编码必须规范，签名和承诺验证必须绑定正确消息、域分离与公共参数。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Validators' Cryptography

“Validators' Cryptography”是本单元正式标题 16/38，原目录层级 h2。这是密码与证明边界。必须区分秘密输入、公开输入、编码、域分离、算法输出和验证命题；使用成熟库与公开测试向量，篡改一个字节时验证应在首个不满足的规则处失败。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。密钥必须均匀生成，编码必须规范，签名和承诺验证必须绑定正确消息、域分离与公共参数。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Introduction

“Introduction”是本单元正式标题 17/38，原目录层级 h3。本章贯通secp256k1私钥与地址、Keccak-256、ECDSA、验证者BLS签名以及数据可用性使用的KZG承诺。每类原语的安全目标、输入域和验证方不同，不能互换。 本标题要求把术语落到明确对象和状态转换：给出输入、前置条件、算法或协议规则、可观察输出以及至少一个拒绝样本。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。密钥必须均匀生成，编码必须规范，签名和承诺验证必须绑定正确消息、域分离与公共参数。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Requirements

“Requirements”是本单元正式标题 18/38，原目录层级 h3。本章贯通secp256k1私钥与地址、Keccak-256、ECDSA、验证者BLS签名以及数据可用性使用的KZG承诺。每类原语的安全目标、输入域和验证方不同，不能互换。 本标题要求把术语落到明确对象和状态转换：给出输入、前置条件、算法或协议规则、可观察输出以及至少一个拒绝样本。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。密钥必须均匀生成，编码必须规范，签名和承诺验证必须绑定正确消息、域分离与公共参数。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### BLS Digital Signatures

“BLS Digital Signatures”是本单元正式标题 19/38，原目录层级 h3。这是密码与证明边界。必须区分秘密输入、公开输入、编码、域分离、算法输出和验证命题；使用成熟库与公开测试向量，篡改一个字节时验证应在首个不满足的规则处失败。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。密钥必须均匀生成，编码必须规范，签名和承诺验证必须绑定正确消息、域分离与公共参数。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### How Does It Work?

“How Does It Work?”是本单元正式标题 20/38，原目录层级 h4。本章贯通secp256k1私钥与地址、Keccak-256、ECDSA、验证者BLS签名以及数据可用性使用的KZG承诺。每类原语的安全目标、输入域和验证方不同，不能互换。 本标题要求把术语落到明确对象和状态转换：给出输入、前置条件、算法或协议规则、可观察输出以及至少一个拒绝样本。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。密钥必须均匀生成，编码必须规范，签名和承诺验证必须绑定正确消息、域分离与公共参数。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Inside Elliptic Curve Properties

“Inside Elliptic Curve Properties”是本单元正式标题 21/38，原目录层级 h4。这是密码与证明边界。必须区分秘密输入、公开输入、编码、域分离、算法输出和验证命题；使用成熟库与公开测试向量，篡改一个字节时验证应在首个不满足的规则处失败。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。密钥必须均匀生成，编码必须规范，签名和承诺验证必须绑定正确消息、域分离与公共参数。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### In Summary

“In Summary”是本单元正式标题 22/38，原目录层级 h4。这里不是重复定义，而是把本章对象收束为可验收合同：密钥必须均匀生成，编码必须规范，签名和承诺验证必须绑定正确消息、域分离与公共参数。 复习时必须能从结论反向定位输入、规则、输出和失败证据。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。密钥必须均匀生成，编码必须规范，签名和承诺验证必须绑定正确消息、域分离与公共参数。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### KZG Cryptography

“KZG Cryptography”是本单元正式标题 23/38，原目录层级 h2。这是密码与证明边界。必须区分秘密输入、公开输入、编码、域分离、算法输出和验证命题；使用成熟库与公开测试向量，篡改一个字节时验证应在首个不满足的规则处失败。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。密钥必须均匀生成，编码必须规范，签名和承诺验证必须绑定正确消息、域分离与公共参数。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Introduction

“Introduction”是本单元正式标题 24/38，原目录层级 h3。本章贯通secp256k1私钥与地址、Keccak-256、ECDSA、验证者BLS签名以及数据可用性使用的KZG承诺。每类原语的安全目标、输入域和验证方不同，不能互换。 本标题要求把术语落到明确对象和状态转换：给出输入、前置条件、算法或协议规则、可观察输出以及至少一个拒绝样本。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。密钥必须均匀生成，编码必须规范，签名和承诺验证必须绑定正确消息、域分离与公共参数。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Polynomial Commitment Schemes

“Polynomial Commitment Schemes”是本单元正式标题 25/38，原目录层级 h3。这是密码与证明边界。必须区分秘密输入、公开输入、编码、域分离、算法输出和验证命题；使用成熟库与公开测试向量，篡改一个字节时验证应在首个不满足的规则处失败。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。密钥必须均匀生成，编码必须规范，签名和承诺验证必须绑定正确消息、域分离与公共参数。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Introduction to Polynomials

“Introduction to Polynomials”是本单元正式标题 26/38，原目录层级 h4。本章贯通secp256k1私钥与地址、Keccak-256、ECDSA、验证者BLS签名以及数据可用性使用的KZG承诺。每类原语的安全目标、输入域和验证方不同，不能互换。 本标题要求把术语落到明确对象和状态转换：给出输入、前置条件、算法或协议规则、可观察输出以及至少一个拒绝样本。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。密钥必须均匀生成，编码必须规范，签名和承诺验证必须绑定正确消息、域分离与公共参数。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Using Merkle Trees

“Using Merkle Trees”是本单元正式标题 27/38，原目录层级 h3。本章贯通secp256k1私钥与地址、Keccak-256、ECDSA、验证者BLS签名以及数据可用性使用的KZG承诺。每类原语的安全目标、输入域和验证方不同，不能互换。 本标题要求把术语落到明确对象和状态转换：给出输入、前置条件、算法或协议规则、可观察输出以及至少一个拒绝样本。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。密钥必须均匀生成，编码必须规范，签名和承诺验证必须绑定正确消息、域分离与公共参数。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### KZG Commitment

“KZG Commitment”是本单元正式标题 28/38，原目录层级 h3。这是密码与证明边界。必须区分秘密输入、公开输入、编码、域分离、算法输出和验证命题；使用成熟库与公开测试向量，篡改一个字节时验证应在首个不满足的规则处失败。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。密钥必须均匀生成，编码必须规范，签名和承诺验证必须绑定正确消息、域分离与公共参数。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### The trusted setup

“The trusted setup”是本单元正式标题 29/38，原目录层级 h4。本章贯通secp256k1私钥与地址、Keccak-256、ECDSA、验证者BLS签名以及数据可用性使用的KZG承诺。每类原语的安全目标、输入域和验证方不同，不能互换。 本标题要求把术语落到明确对象和状态转换：给出输入、前置条件、算法或协议规则、可观察输出以及至少一个拒绝样本。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。密钥必须均匀生成，编码必须规范，签名和承诺验证必须绑定正确消息、域分离与公共参数。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### The commitment

“The commitment”是本单元正式标题 30/38，原目录层级 h4。这是密码与证明边界。必须区分秘密输入、公开输入、编码、域分离、算法输出和验证命题；使用成熟库与公开测试向量，篡改一个字节时验证应在首个不满足的规则处失败。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。密钥必须均匀生成，编码必须规范，签名和承诺验证必须绑定正确消息、域分离与公共参数。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Soundness of KZG: Why the Prover Can't Fake the Commitment

“Soundness of KZG: Why the Prover Can't Fake the Commitment”是本单元正式标题 31/38，原目录层级 h4。这是密码与证明边界。必须区分秘密输入、公开输入、编码、域分离、算法输出和验证命题；使用成熟库与公开测试向量，篡改一个字节时验证应在首个不满足的规则处失败。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。密钥必须均匀生成，编码必须规范，签名和承诺验证必须绑定正确消息、域分离与公共参数。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### KZG Proof

“KZG Proof”是本单元正式标题 32/38，原目录层级 h3。这是密码与证明边界。必须区分秘密输入、公开输入、编码、域分离、算法输出和验证命题；使用成熟库与公开测试向量，篡改一个字节时验证应在首个不满足的规则处失败。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。密钥必须均匀生成，编码必须规范，签名和承诺验证必须绑定正确消息、域分离与公共参数。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Correctness

“Correctness”是本单元正式标题 33/38，原目录层级 h4。本章贯通secp256k1私钥与地址、Keccak-256、ECDSA、验证者BLS签名以及数据可用性使用的KZG承诺。每类原语的安全目标、输入域和验证方不同，不能互换。 本标题要求把术语落到明确对象和状态转换：给出输入、前置条件、算法或协议规则、可观察输出以及至少一个拒绝样本。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。密钥必须均匀生成，编码必须规范，签名和承诺验证必须绑定正确消息、域分离与公共参数。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Soundness

“Soundness”是本单元正式标题 34/38，原目录层级 h4。本章贯通secp256k1私钥与地址、Keccak-256、ECDSA、验证者BLS签名以及数据可用性使用的KZG承诺。每类原语的安全目标、输入域和验证方不同，不能互换。 本标题要求把术语落到明确对象和状态转换：给出输入、前置条件、算法或协议规则、可观察输出以及至少一个拒绝样本。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。密钥必须均匀生成，编码必须规范，签名和承诺验证必须绑定正确消息、域分离与公共参数。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### KZG Properties

“KZG Properties”是本单元正式标题 35/38，原目录层级 h3。这是密码与证明边界。必须区分秘密输入、公开输入、编码、域分离、算法输出和验证命题；使用成熟库与公开测试向量，篡改一个字节时验证应在首个不满足的规则处失败。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。密钥必须均匀生成，编码必须规范，签名和承诺验证必须绑定正确消息、域分离与公共参数。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Multiproofs

“Multiproofs”是本单元正式标题 36/38，原目录层级 h3。这是密码与证明边界。必须区分秘密输入、公开输入、编码、域分离、算法输出和验证命题；使用成熟库与公开测试向量，篡改一个字节时验证应在首个不满足的规则处失败。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。密钥必须均匀生成，编码必须规范，签名和承诺验证必须绑定正确消息、域分离与公共参数。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### In Summary

“In Summary”是本单元正式标题 37/38，原目录层级 h3。这里不是重复定义，而是把本章对象收束为可验收合同：密钥必须均匀生成，编码必须规范，签名和承诺验证必须绑定正确消息、域分离与公共参数。 复习时必须能从结论反向定位输入、规则、输出和失败证据。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。密钥必须均匀生成，编码必须规范，签名和承诺验证必须绑定正确消息、域分离与公共参数。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Conclusion

“Conclusion”是本单元正式标题 38/38，原目录层级 h2。这里不是重复定义，而是把本章对象收束为可验收合同：密钥必须均匀生成，编码必须规范，签名和承诺验证必须绑定正确消息、域分离与公共参数。 复习时必须能从结论反向定位输入、规则、输出和失败证据。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。密钥必须均匀生成，编码必须规范，签名和承诺验证必须绑定正确消息、域分离与公共参数。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

## 正常、边界与故障矩阵

| 样本   | 只改变的变量                            | 预期结果             | 必存证据                   |
| ------ | --------------------------------------- | -------------------- | -------------------------- |
| 正常   | 有效链ID、输入、签名、Gas与前状态       | 接受并得到预期后状态 | 原始交易、收据、前后状态根 |
| 边界   | 零值、最大值、恰好阈值或时序边缘        | 按明确规则接受或拒绝 | 边界值、判断分支、资源成本 |
| 单故障 | 链ID、nonce、签名、价格、证明或数据一项 | 首个无效规则处拒绝   | 错误数据、首差、原状态     |

先预测每个样本的状态根、返回值和错误，再操作滑块；若两个变量同时变化，实验失去归因能力，必须重置。

## 常见失败与回退

<Callout type="trap">
  **把界面成功当成协议成功：**
  交易哈希、RPC返回或前端提示只表示某一观察点。回退到原始交易、收据状态、区块哈希、状态根和最终化检查点逐项核对。
</Callout>

<Callout type="trap">
  **修复时同时改变多个变量：**
  升级客户端、换网络、改账户又改输入会失去首差。恢复固定基线，只改变一个变量，重放原失败、相邻边界和正常样本。
</Callout>

## 本章回顾

本章贯通secp256k1私钥与地址、Keccak-256、ECDSA、验证者BLS签名以及数据可用性使用的KZG承诺。每类原语的安全目标、输入域和验证方不同，不能互换。 核心不是记住工具名称，而是能从官方标题定位对象、规则与证据。密钥必须均匀生成，编码必须规范，签名和承诺验证必须绑定正确消息、域分离与公共参数。 完成后应删除临时密钥和测试状态，保留不含秘密的版本、输入哈希、收据、状态根、错误与复核结论。

<Glossary>

<GlossaryItem term="secp256k1">
  secp256k1用于界定第4章：密码学中的输入、验证规则和可观察结果。
</GlossaryItem>

<GlossaryItem term="Keccak-256">
  Keccak-256用于界定第4章：密码学中的输入、验证规则和可观察结果。
</GlossaryItem>

<GlossaryItem term="ECDSA">
  ECDSA用于界定第4章：密码学中的输入、验证规则和可观察结果。
</GlossaryItem>

<GlossaryItem term="BLS签名">
  BLS签名用于界定第4章：密码学中的输入、验证规则和可观察结果。
</GlossaryItem>

<GlossaryItem term="KZG承诺">
  KZG承诺用于界定第4章：密码学中的输入、验证规则和可观察结果。
</GlossaryItem>

</Glossary>

<Exercises>

1. 如何为Chapter 4. Cryptography建立最小可重放基线？

<Answer>
  固定官方第二版页面、链ID、客户端版本、账户、区块坐标、前状态和原始输入，先手算预期，再保存解析、验证、执行、收据与状态证据。
</Answer>

2. 如何证明“能推导测试私钥到地址、验证ERC-55校验和，并区分ECDSA、BLS和KZG各自证明的命题。”不是一次偶然成功？

<Answer>
  在同一基线上执行正常、边界和单故障样本，由独立客户端核对原始对象、状态根与拒绝点；三组证据一致才通过。
</Answer>

3. 修复Chapter 4. Cryptography的一个失败后怎样回归？

<Answer>
  先重放原失败，再运行相邻边界和正常基线；按版本、网络、编码、签名、状态、Gas、证明与最终性定位首差，全部不变量通过后才进入下一页。
</Answer>

</Exercises>

## 前后导航

- [上一页：第3章：以太坊节点](/learn/mastering-ethereum/04-03-ethereum-nodes/met2-03-ethereum-nodes)
- [下一页：第5章：钱包](/learn/mastering-ethereum/06-05-wallets/met2-05-wallets)

<Attribution
  adaptedFrom="Andreas M. Antonopoulos、Gavin Wood、Carlo Parisi、Alessandro Mazza、Niccolò Pozzolini《Mastering Ethereum》第2版，O'Reilly Media，2025年10月，570页，数字版ISBN 9781098168414，印刷版ISBN 9781098168421 · 官方在线版前言与17章共516个二至五级标题 · 基于公开目录的独立中文教学重构，不复制受限正文"
  adaptedUrl="https://masteringethereum.xyz/chapter_4.html"
/>
