---
title: "第6章：交易"
type: B
section: "Mastering Ethereum（第2版）"
order: 7
description: "交易是触发以太坊状态变化的外部输入。类型化信封、链ID、nonce、费用字段、to、value、data、access list和签名共同确定被验证和执行的对象。"
demo: true
math: true
sourceUrl: "https://masteringethereum.xyz/chapter_6.html"
draft: true
---

import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第6章：交易的对象、状态、信任边界与官方目录坐标
- 能推导状态转换、Gas、状态承诺和证据门禁四组约束
- 能实现并运行三段最小代码，比较正常、边界和单故障样本
- 能设计独立复核并证明“能构造、编码、签名和解码一笔EIP-1559测试交易，并定位错误nonce、错误链ID与费用不足的首个拒绝点。”

</Objectives>

## 为什么从可验证状态开始

本页按官方第二版在线目录重建 **Chapter 6. Transactions**，覆盖该正式单元的 43 个二至五级标题。教学顺序保留原目录的依赖关系，但正文全部独立撰写：每个标题都转成对象、前状态、输入、规则、后状态和失败证据，避免只给术语摘要。

交易是触发以太坊状态变化的外部输入。类型化信封、链ID、nonce、费用字段、to、value、data、access list和签名共同确定被验证和执行的对象。 阅读时把“客户端或网页显示了什么”和“协议能验证什么”分开：钱包、浏览器、RPC与索引器是观察面，规范编码、签名、交易、收据、区块、状态根、证明和最终化检查点才是可复核对象。任何结论都要绑定链ID、客户端版本、区块坐标与原始输入。

实验统一在本地开发链或公开测试网完成，私钥只使用一次性测试向量。先写预期，再运行；先保存原始对象，再看格式化界面。正常样本证明主路径，边界样本选择零值、最大值、恰好阈值或时序边缘，单故障样本只改变链ID、nonce、一个字节、一个签名、一个价格或一个证明条件。

签名摘要必须覆盖正确交易类型与链ID；nonce按发送者顺序推进，gasUsed不得超过gasLimit，失败执行仍消耗已使用Gas。 若执行失败，证据必须说明首个拒绝规则、错误数据、消耗的资源以及状态是否回滚；若执行成功，还要区分交易池接纳、区块包含、执行状态与PoS最终性，不能把交易哈希当成最终结算。

通过标准是：能构造、编码、签名和解码一笔EIP-1559测试交易，并定位错误nonce、错误链ID与费用不足的首个拒绝点。 独立复核者应能从空目录和相同测试向量重建结果；缺失来源、版本、输入、前状态、后状态或失败证据任一项，结论都不能进入下一页。

## 版次、对象与验收合同

<Term def="类型化交易是第6章：交易的第1个复核坐标，必须绑定对象、状态、规则和失败证据。">类型化交易</Term>、<Term def="nonce是第6章：交易的第2个复核坐标，必须绑定对象、状态、规则和失败证据。">nonce</Term>、<Term def="EIP-1559是第6章：交易的第3个复核坐标，必须绑定对象、状态、规则和失败证据。">EIP-1559</Term>、<Term def="交易签名是第6章：交易的第4个复核坐标，必须绑定对象、状态、规则和失败证据。">交易签名</Term>、<Term def="交易池是第6章：交易的第5个复核坐标，必须绑定对象、状态、规则和失败证据。">交易池</Term>
共同构成本页的复核坐标。交易是触发以太坊状态变化的外部输入。类型化信封、链ID、nonce、费用字段、to、value、data、access
list和签名共同确定被验证和执行的对象。

<Callout type="info">
  **验收命题：**
  能构造、编码、签名和解码一笔EIP-1559测试交易，并定位错误nonce、错误链ID与费用不足的首个拒绝点。
</Callout>

## 核心约束与推导

$$
S_{t+1}=Apply(S_t, T_t), \quad Valid(T_t,S_t)=1
$$

$$
gasUsed(T) \le gasLimit(T), \quad fee=gasUsed \times effectiveGasPrice
$$

$$
stateRoot_{n}=Commit(accounts,storage,code,balances)
$$

$$
gate=source \times version \times replay \times boundary \times failureEvidence
$$

状态转移只接受针对当前前状态有效的输入；Gas约束把资源消耗纳入确定性执行；状态根把大量账户与存储压缩为区块可承诺对象。证据门禁采用乘法语义，任一因子缺失都不能用其他截图或口头解释补偿。

## 三段可运行骨架

```typescript
type EthereumEvidence = {
  chainId: number;
  block: string;
  beforeRoot: string;
  inputHash: string;
  afterRoot?: string;
  error?: string;
};

export function evidenceGate(run: EthereumEvidence) {
  return {
    page: "met2-06-transactions",
    accepted: Boolean(run.afterRoot || run.error),
    immutableInput: run.inputHash,
  };
}
```

```solidity
// SPDX-License-Identifier: MIT
pragma solidity ^0.8.26;

contract InvariantProbe {
    uint256 public value;
    event Applied(uint256 beforeValue, uint256 afterValue);

    function apply(uint256 next) external {
        uint256 beforeValue = value;
        require(next >= beforeValue, "non-monotonic");
        value = next;
        emit Applied(beforeValue, next);
    }
}
```

```typescript
function applyCandidate(
  state: bigint,
  next: bigint,
  gasUsed: bigint,
  gasLimit: bigint,
) {
  if (gasUsed > gasLimit)
    return { accepted: false, state, reason: "out-of-gas" };
  if (next < state)
    return { accepted: false, state, reason: "invalid-transition" };
  return { accepted: true, state: next, reason: null };
}
```

第一段把成功与失败放进同一证据合同；第二段给出可观察事件和显式不变量；第三段证明拒绝路径保持原状态。真实系统应使用成熟客户端、ABI、密码与证明库，并补齐并发、重组、超时、持久化、隐私和密钥隔离。

## 正式标题逐项讲解

### The Structure of a Transaction

“The Structure of a Transaction”是本单元正式标题 1/43，原目录层级 h2。这是交易状态机坐标。按类型化信封解析链ID、nonce、费用上限、目标、value、data和签名，先做静态有效性检查，再区分交易池接纳、区块包含、执行成功与最终化。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。签名摘要必须覆盖正确交易类型与链ID；nonce按发送者顺序推进，gasUsed不得超过gasLimit，失败执行仍消耗已使用Gas。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Legacy Transactions

“Legacy Transactions”是本单元正式标题 2/43，原目录层级 h3。这是交易状态机坐标。按类型化信封解析链ID、nonce、费用上限、目标、value、data和签名，先做静态有效性检查，再区分交易池接纳、区块包含、执行成功与最终化。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。签名摘要必须覆盖正确交易类型与链ID；nonce按发送者顺序推进，gasUsed不得超过gasLimit，失败执行仍消耗已使用Gas。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### EIP-2930 Transactions

“EIP-2930 Transactions”是本单元正式标题 3/43，原目录层级 h3。这是交易状态机坐标。按类型化信封解析链ID、nonce、费用上限、目标、value、data和签名，先做静态有效性检查，再区分交易池接纳、区块包含、执行成功与最终化。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。签名摘要必须覆盖正确交易类型与链ID；nonce按发送者顺序推进，gasUsed不得超过gasLimit，失败执行仍消耗已使用Gas。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### EIP-1559 Transactions

“EIP-1559 Transactions”是本单元正式标题 4/43，原目录层级 h3。这是交易状态机坐标。按类型化信封解析链ID、nonce、费用上限、目标、value、data和签名，先做静态有效性检查，再区分交易池接纳、区块包含、执行成功与最终化。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。签名摘要必须覆盖正确交易类型与链ID；nonce按发送者顺序推进，gasUsed不得超过gasLimit，失败执行仍消耗已使用Gas。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### EIP-4844 Transactions

“EIP-4844 Transactions”是本单元正式标题 5/43，原目录层级 h3。这是交易状态机坐标。按类型化信封解析链ID、nonce、费用上限、目标、value、data和签名，先做静态有效性检查，再区分交易池接纳、区块包含、执行成功与最终化。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。签名摘要必须覆盖正确交易类型与链ID；nonce按发送者顺序推进，gasUsed不得超过gasLimit，失败执行仍消耗已使用Gas。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### EIP-7702 Transactions

“EIP-7702 Transactions”是本单元正式标题 6/43，原目录层级 h3。这是交易状态机坐标。按类型化信封解析链ID、nonce、费用上限、目标、value、data和签名，先做静态有效性检查，再区分交易池接纳、区块包含、执行成功与最终化。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。签名摘要必须覆盖正确交易类型与链ID；nonce按发送者顺序推进，gasUsed不得超过gasLimit，失败执行仍消耗已使用Gas。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### The Transaction Nonce

“The Transaction Nonce”是本单元正式标题 7/43，原目录层级 h2。这是交易状态机坐标。按类型化信封解析链ID、nonce、费用上限、目标、value、data和签名，先做静态有效性检查，再区分交易池接纳、区块包含、执行成功与最终化。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。签名摘要必须覆盖正确交易类型与链ID；nonce按发送者顺序推进，gasUsed不得超过gasLimit，失败执行仍消耗已使用Gas。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Keeping Track of Nonces

“Keeping Track of Nonces”是本单元正式标题 8/43，原目录层级 h3。这是交易状态机坐标。按类型化信封解析链ID、nonce、费用上限、目标、value、data和签名，先做静态有效性检查，再区分交易池接纳、区块包含、执行成功与最终化。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。签名摘要必须覆盖正确交易类型与链ID；nonce按发送者顺序推进，gasUsed不得超过gasLimit，失败执行仍消耗已使用Gas。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Gaps in Nonces, Duplicate Nonces, and Confirmation

“Gaps in Nonces, Duplicate Nonces, and Confirmation”是本单元正式标题 9/43，原目录层级 h3。这是交易状态机坐标。按类型化信封解析链ID、nonce、费用上限、目标、value、data和签名，先做静态有效性检查，再区分交易池接纳、区块包含、执行成功与最终化。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。签名摘要必须覆盖正确交易类型与链ID；nonce按发送者顺序推进，gasUsed不得超过gasLimit，失败执行仍消耗已使用Gas。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Concurrency, Transaction Origination, and Nonces

“Concurrency, Transaction Origination, and Nonces”是本单元正式标题 10/43，原目录层级 h3。这是交易状态机坐标。按类型化信封解析链ID、nonce、费用上限、目标、value、data和签名，先做静态有效性检查，再区分交易池接纳、区块包含、执行成功与最终化。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。签名摘要必须覆盖正确交易类型与链ID；nonce按发送者顺序推进，gasUsed不得超过gasLimit，失败执行仍消耗已使用Gas。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Transaction Gas

“Transaction Gas”是本单元正式标题 11/43，原目录层级 h2。这是交易状态机坐标。按类型化信封解析链ID、nonce、费用上限、目标、value、data和签名，先做静态有效性检查，再区分交易池接纳、区块包含、执行成功与最终化。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。签名摘要必须覆盖正确交易类型与链ID；nonce按发送者顺序推进，gasUsed不得超过gasLimit，失败执行仍消耗已使用Gas。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### EIP-1559: Base Fee and Priority Fee

“EIP-1559: Base Fee and Priority Fee”是本单元正式标题 12/43，原目录层级 h3。这是交易状态机坐标。按类型化信封解析链ID、nonce、费用上限、目标、value、data和签名，先做静态有效性检查，再区分交易池接纳、区块包含、执行成功与最终化。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。签名摘要必须覆盖正确交易类型与链ID；nonce按发送者顺序推进，gasUsed不得超过gasLimit，失败执行仍消耗已使用Gas。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### How to Know the "Correct" Gas Price

“How to Know the "Correct" Gas Price”是本单元正式标题 13/43，原目录层级 h3。这是交易状态机坐标。按类型化信封解析链ID、nonce、费用上限、目标、value、data和签名，先做静态有效性检查，再区分交易池接纳、区块包含、执行成功与最终化。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。签名摘要必须覆盖正确交易类型与链ID；nonce按发送者顺序推进，gasUsed不得超过gasLimit，失败执行仍消耗已使用Gas。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Transaction Recipient

“Transaction Recipient”是本单元正式标题 14/43，原目录层级 h2。这是交易状态机坐标。按类型化信封解析链ID、nonce、费用上限、目标、value、data和签名，先做静态有效性检查，再区分交易池接纳、区块包含、执行成功与最终化。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。签名摘要必须覆盖正确交易类型与链ID；nonce按发送者顺序推进，gasUsed不得超过gasLimit，失败执行仍消耗已使用Gas。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Transaction Value and Data

“Transaction Value and Data”是本单元正式标题 15/43，原目录层级 h2。这是交易状态机坐标。按类型化信封解析链ID、nonce、费用上限、目标、value、data和签名，先做静态有效性检查，再区分交易池接纳、区块包含、执行成功与最终化。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。签名摘要必须覆盖正确交易类型与链ID；nonce按发送者顺序推进，gasUsed不得超过gasLimit，失败执行仍消耗已使用Gas。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Transmitting Value to EOAs and Contracts

“Transmitting Value to EOAs and Contracts”是本单元正式标题 16/43，原目录层级 h2。这是合约执行边界。源码、编译器、ABI、字节码、调用上下文与持久状态必须一一对应；外部调用视为不可信，失败路径要证明状态原子回滚并记录Gas和错误数据。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。签名摘要必须覆盖正确交易类型与链ID；nonce按发送者顺序推进，gasUsed不得超过gasLimit，失败执行仍消耗已使用Gas。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Transmitting a Data Payload to an EOA or Contract

“Transmitting a Data Payload to an EOA or Contract”是本单元正式标题 17/43，原目录层级 h2。这是合约执行边界。源码、编译器、ABI、字节码、调用上下文与持久状态必须一一对应；外部调用视为不可信，失败路径要证明状态原子回滚并记录Gas和错误数据。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。签名摘要必须覆盖正确交易类型与链ID；nonce按发送者顺序推进，gasUsed不得超过gasLimit，失败执行仍消耗已使用Gas。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Special Transaction: Contract Creation

“Special Transaction: Contract Creation”是本单元正式标题 18/43，原目录层级 h2。这是交易状态机坐标。按类型化信封解析链ID、nonce、费用上限、目标、value、data和签名，先做静态有效性检查，再区分交易池接纳、区块包含、执行成功与最终化。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。签名摘要必须覆盖正确交易类型与链ID；nonce按发送者顺序推进，gasUsed不得超过gasLimit，失败执行仍消耗已使用Gas。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Digital Signatures

“Digital Signatures”是本单元正式标题 19/43，原目录层级 h2。这是密码与证明边界。必须区分秘密输入、公开输入、编码、域分离、算法输出和验证命题；使用成熟库与公开测试向量，篡改一个字节时验证应在首个不满足的规则处失败。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。签名摘要必须覆盖正确交易类型与链ID；nonce按发送者顺序推进，gasUsed不得超过gasLimit，失败执行仍消耗已使用Gas。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### The ECDSA

“The ECDSA”是本单元正式标题 20/43，原目录层级 h3。交易是触发以太坊状态变化的外部输入。类型化信封、链ID、nonce、费用字段、to、value、data、access list和签名共同确定被验证和执行的对象。 本标题要求把术语落到明确对象和状态转换：给出输入、前置条件、算法或协议规则、可观察输出以及至少一个拒绝样本。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。签名摘要必须覆盖正确交易类型与链ID；nonce按发送者顺序推进，gasUsed不得超过gasLimit，失败执行仍消耗已使用Gas。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Definition of a Digital Signature

“Definition of a Digital Signature”是本单元正式标题 21/43，原目录层级 h4。这是密码与证明边界。必须区分秘密输入、公开输入、编码、域分离、算法输出和验证命题；使用成熟库与公开测试向量，篡改一个字节时验证应在首个不满足的规则处失败。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。签名摘要必须覆盖正确交易类型与链ID；nonce按发送者顺序推进，gasUsed不得超过gasLimit，失败执行仍消耗已使用Gas。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### How Digital Signatures Work

“How Digital Signatures Work”是本单元正式标题 22/43，原目录层级 h3。这是密码与证明边界。必须区分秘密输入、公开输入、编码、域分离、算法输出和验证命题；使用成熟库与公开测试向量，篡改一个字节时验证应在首个不满足的规则处失败。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。签名摘要必须覆盖正确交易类型与链ID；nonce按发送者顺序推进，gasUsed不得超过gasLimit，失败执行仍消耗已使用Gas。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Creating a digital signature

“Creating a digital signature”是本单元正式标题 23/43，原目录层级 h4。这是可复现实作坐标。先固定工具版本、网络、账户与原始输入，写出预期对象，再执行命令或合约；保存配置、交易哈希、收据、日志、状态首差与回退点，不能只截取成功界面。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。签名摘要必须覆盖正确交易类型与链ID；nonce按发送者顺序推进，gasUsed不得超过gasLimit，失败执行仍消耗已使用Gas。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Verifying the signature

“Verifying the signature”是本单元正式标题 24/43，原目录层级 h4。这是密码与证明边界。必须区分秘密输入、公开输入、编码、域分离、算法输出和验证命题；使用成熟库与公开测试向量，篡改一个字节时验证应在首个不满足的规则处失败。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。签名摘要必须覆盖正确交易类型与链ID；nonce按发送者顺序推进，gasUsed不得超过gasLimit，失败执行仍消耗已使用Gas。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### ECDSA Math

“ECDSA Math”是本单元正式标题 25/43，原目录层级 h3。交易是触发以太坊状态变化的外部输入。类型化信封、链ID、nonce、费用字段、to、value、data、access list和签名共同确定被验证和执行的对象。 本标题要求把术语落到明确对象和状态转换：给出输入、前置条件、算法或协议规则、可观察输出以及至少一个拒绝样本。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。签名摘要必须覆盖正确交易类型与链ID；nonce按发送者顺序推进，gasUsed不得超过gasLimit，失败执行仍消耗已使用Gas。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Transaction Signing in Practice

“Transaction Signing in Practice”是本单元正式标题 26/43，原目录层级 h2。这是交易状态机坐标。按类型化信封解析链ID、nonce、费用上限、目标、value、data和签名，先做静态有效性检查，再区分交易池接纳、区块包含、执行成功与最终化。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。签名摘要必须覆盖正确交易类型与链ID；nonce按发送者顺序推进，gasUsed不得超过gasLimit，失败执行仍消耗已使用Gas。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Raw Transaction Creation and Signing

“Raw Transaction Creation and Signing”是本单元正式标题 27/43，原目录层级 h3。这是交易状态机坐标。按类型化信封解析链ID、nonce、费用上限、目标、value、data和签名，先做静态有效性检查，再区分交易池接纳、区块包含、执行成功与最终化。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。签名摘要必须覆盖正确交易类型与链ID；nonce按发送者顺序推进，gasUsed不得超过gasLimit，失败执行仍消耗已使用Gas。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Low-Level Manual Construction of an EIP-1559 Raw Transaction

“Low-Level Manual Construction of an EIP-1559 Raw Transaction”是本单元正式标题 28/43，原目录层级 h3。这是交易状态机坐标。按类型化信封解析链ID、nonce、费用上限、目标、value、data和签名，先做静态有效性检查，再区分交易池接纳、区块包含、执行成功与最终化。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。签名摘要必须覆盖正确交易类型与链ID；nonce按发送者顺序推进，gasUsed不得超过gasLimit，失败执行仍消耗已使用Gas。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Deserializing the Transaction

“Deserializing the Transaction”是本单元正式标题 29/43，原目录层级 h2。这是交易状态机坐标。按类型化信封解析链ID、nonce、费用上限、目标、value、data和签名，先做静态有效性检查，再区分交易池接纳、区块包含、执行成功与最终化。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。签名摘要必须覆盖正确交易类型与链ID；nonce按发送者顺序推进，gasUsed不得超过gasLimit，失败执行仍消耗已使用Gas。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Raw Transaction Creation with EIP-155

“Raw Transaction Creation with EIP-155”是本单元正式标题 30/43，原目录层级 h2。这是交易状态机坐标。按类型化信封解析链ID、nonce、费用上限、目标、value、data和签名，先做静态有效性检查，再区分交易池接纳、区块包含、执行成功与最终化。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。签名摘要必须覆盖正确交易类型与链ID；nonce按发送者顺序推进，gasUsed不得超过gasLimit，失败执行仍消耗已使用Gas。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### The Signature Prefix Value (v) and Public Key Recovery

“The Signature Prefix Value (v) and Public Key Recovery”是本单元正式标题 31/43，原目录层级 h2。这是密码与证明边界。必须区分秘密输入、公开输入、编码、域分离、算法输出和验证命题；使用成熟库与公开测试向量，篡改一个字节时验证应在首个不满足的规则处失败。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。签名摘要必须覆盖正确交易类型与链ID；nonce按发送者顺序推进，gasUsed不得超过gasLimit，失败执行仍消耗已使用Gas。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Separating Signing and Transmission (Offline Signing)

“Separating Signing and Transmission (Offline Signing)”是本单元正式标题 32/43，原目录层级 h2。交易是触发以太坊状态变化的外部输入。类型化信封、链ID、nonce、费用字段、to、value、data、access list和签名共同确定被验证和执行的对象。 本标题要求把术语落到明确对象和状态转换：给出输入、前置条件、算法或协议规则、可观察输出以及至少一个拒绝样本。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。签名摘要必须覆盖正确交易类型与链ID；nonce按发送者顺序推进，gasUsed不得超过gasLimit，失败执行仍消耗已使用Gas。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Transaction Life Cycle

“Transaction Life Cycle”是本单元正式标题 33/43，原目录层级 h2。这是交易状态机坐标。按类型化信封解析链ID、nonce、费用上限、目标、value、data和签名，先做静态有效性检查，再区分交易池接纳、区块包含、执行成功与最终化。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。签名摘要必须覆盖正确交易类型与链ID；nonce按发送者顺序推进，gasUsed不得超过gasLimit，失败执行仍消耗已使用Gas。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Creating and Signing the Transaction

“Creating and Signing the Transaction”是本单元正式标题 34/43，原目录层级 h3。这是可复现实作坐标。先固定工具版本、网络、账户与原始输入，写出预期对象，再执行命令或合约；保存配置、交易哈希、收据、日志、状态首差与回退点，不能只截取成功界面。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。签名摘要必须覆盖正确交易类型与链ID；nonce按发送者顺序推进，gasUsed不得超过gasLimit，失败执行仍消耗已使用Gas。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Sending the Transaction to the Network

“Sending the Transaction to the Network”是本单元正式标题 35/43，原目录层级 h3。这是可复现实作坐标。先固定工具版本、网络、账户与原始输入，写出预期对象，再执行命令或合约；保存配置、交易哈希、收据、日志、状态首差与回退点，不能只截取成功界面。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。签名摘要必须覆盖正确交易类型与链ID；nonce按发送者顺序推进，gasUsed不得超过gasLimit，失败执行仍消耗已使用Gas。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Building the Block

“Building the Block”是本单元正式标题 36/43，原目录层级 h3。这是可复现实作坐标。先固定工具版本、网络、账户与原始输入，写出预期对象，再执行命令或合约；保存配置、交易哈希、收据、日志、状态首差与回退点，不能只截取成功界面。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。签名摘要必须覆盖正确交易类型与链ID；nonce按发送者顺序推进，gasUsed不得超过gasLimit，失败执行仍消耗已使用Gas。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Finalizing the Transaction

“Finalizing the Transaction”是本单元正式标题 37/43，原目录层级 h3。这是交易状态机坐标。按类型化信封解析链ID、nonce、费用上限、目标、value、data和签名，先做静态有效性检查，再区分交易池接纳、区块包含、执行成功与最终化。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。签名摘要必须覆盖正确交易类型与链ID；nonce按发送者顺序推进，gasUsed不得超过gasLimit，失败执行仍消耗已使用Gas。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### An Alternative Life Cycle

“An Alternative Life Cycle”是本单元正式标题 38/43，原目录层级 h2。交易是触发以太坊状态变化的外部输入。类型化信封、链ID、nonce、费用字段、to、value、data、access list和签名共同确定被验证和执行的对象。 本标题要求把术语落到明确对象和状态转换：给出输入、前置条件、算法或协议规则、可观察输出以及至少一个拒绝样本。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。签名摘要必须覆盖正确交易类型与链ID；nonce按发送者顺序推进，gasUsed不得超过gasLimit，失败执行仍消耗已使用Gas。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### MEV and Proposer and Builder Separation

“MEV and Proposer and Builder Separation”是本单元正式标题 39/43，原目录层级 h3。这是可复现实作坐标。先固定工具版本、网络、账户与原始输入，写出预期对象，再执行命令或合约；保存配置、交易哈希、收据、日志、状态首差与回退点，不能只截取成功界面。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。签名摘要必须覆盖正确交易类型与链ID；nonce按发送者顺序推进，gasUsed不得超过gasLimit，失败执行仍消耗已使用Gas。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Private Mempools

“Private Mempools”是本单元正式标题 40/43，原目录层级 h3。这是交易状态机坐标。按类型化信封解析链ID、nonce、费用上限、目标、value、data和签名，先做静态有效性检查，再区分交易池接纳、区块包含、执行成功与最终化。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。签名摘要必须覆盖正确交易类型与链ID；nonce按发送者顺序推进，gasUsed不得超过gasLimit，失败执行仍消耗已使用Gas。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### New Transaction Life Cycle

“New Transaction Life Cycle”是本单元正式标题 41/43，原目录层级 h3。这是交易状态机坐标。按类型化信封解析链ID、nonce、费用上限、目标、value、data和签名，先做静态有效性检查，再区分交易池接纳、区块包含、执行成功与最终化。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。签名摘要必须覆盖正确交易类型与链ID；nonce按发送者顺序推进，gasUsed不得超过gasLimit，失败执行仍消耗已使用Gas。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Multiple-Signature Transactions

“Multiple-Signature Transactions”是本单元正式标题 42/43，原目录层级 h2。这是密码与证明边界。必须区分秘密输入、公开输入、编码、域分离、算法输出和验证命题；使用成熟库与公开测试向量，篡改一个字节时验证应在首个不满足的规则处失败。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。签名摘要必须覆盖正确交易类型与链ID；nonce按发送者顺序推进，gasUsed不得超过gasLimit，失败执行仍消耗已使用Gas。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Conclusion

“Conclusion”是本单元正式标题 43/43，原目录层级 h2。这里不是重复定义，而是把本章对象收束为可验收合同：签名摘要必须覆盖正确交易类型与链ID；nonce按发送者顺序推进，gasUsed不得超过gasLimit，失败执行仍消耗已使用Gas。 复习时必须能从结论反向定位输入、规则、输出和失败证据。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。签名摘要必须覆盖正确交易类型与链ID；nonce按发送者顺序推进，gasUsed不得超过gasLimit，失败执行仍消耗已使用Gas。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

## 正常、边界与故障矩阵

| 样本   | 只改变的变量                            | 预期结果             | 必存证据                   |
| ------ | --------------------------------------- | -------------------- | -------------------------- |
| 正常   | 有效链ID、输入、签名、Gas与前状态       | 接受并得到预期后状态 | 原始交易、收据、前后状态根 |
| 边界   | 零值、最大值、恰好阈值或时序边缘        | 按明确规则接受或拒绝 | 边界值、判断分支、资源成本 |
| 单故障 | 链ID、nonce、签名、价格、证明或数据一项 | 首个无效规则处拒绝   | 错误数据、首差、原状态     |

先预测每个样本的状态根、返回值和错误，再操作滑块；若两个变量同时变化，实验失去归因能力，必须重置。

## 常见失败与回退

<Callout type="trap">
  **把界面成功当成协议成功：**
  交易哈希、RPC返回或前端提示只表示某一观察点。回退到原始交易、收据状态、区块哈希、状态根和最终化检查点逐项核对。
</Callout>

<Callout type="trap">
  **修复时同时改变多个变量：**
  升级客户端、换网络、改账户又改输入会失去首差。恢复固定基线，只改变一个变量，重放原失败、相邻边界和正常样本。
</Callout>

## 本章回顾

交易是触发以太坊状态变化的外部输入。类型化信封、链ID、nonce、费用字段、to、value、data、access list和签名共同确定被验证和执行的对象。 核心不是记住工具名称，而是能从官方标题定位对象、规则与证据。签名摘要必须覆盖正确交易类型与链ID；nonce按发送者顺序推进，gasUsed不得超过gasLimit，失败执行仍消耗已使用Gas。 完成后应删除临时密钥和测试状态，保留不含秘密的版本、输入哈希、收据、状态根、错误与复核结论。

<Glossary>

<GlossaryItem term="类型化交易">
  类型化交易用于界定第6章：交易中的输入、验证规则和可观察结果。
</GlossaryItem>

<GlossaryItem term="nonce">
  nonce用于界定第6章：交易中的输入、验证规则和可观察结果。
</GlossaryItem>

<GlossaryItem term="EIP-1559">
  EIP-1559用于界定第6章：交易中的输入、验证规则和可观察结果。
</GlossaryItem>

<GlossaryItem term="交易签名">
  交易签名用于界定第6章：交易中的输入、验证规则和可观察结果。
</GlossaryItem>

<GlossaryItem term="交易池">
  交易池用于界定第6章：交易中的输入、验证规则和可观察结果。
</GlossaryItem>

</Glossary>

<Exercises>

1. 如何为Chapter 6. Transactions建立最小可重放基线？

<Answer>
  固定官方第二版页面、链ID、客户端版本、账户、区块坐标、前状态和原始输入，先手算预期，再保存解析、验证、执行、收据与状态证据。
</Answer>

2. 如何证明“能构造、编码、签名和解码一笔EIP-1559测试交易，并定位错误nonce、错误链ID与费用不足的首个拒绝点。”不是一次偶然成功？

<Answer>
  在同一基线上执行正常、边界和单故障样本，由独立客户端核对原始对象、状态根与拒绝点；三组证据一致才通过。
</Answer>

3. 修复Chapter 6. Transactions的一个失败后怎样回归？

<Answer>
  先重放原失败，再运行相邻边界和正常基线；按版本、网络、编码、签名、状态、Gas、证明与最终性定位首差，全部不变量通过后才进入下一页。
</Answer>

</Exercises>

## 前后导航

- [上一页：第5章：钱包](/learn/mastering-ethereum/06-05-wallets/met2-05-wallets)
- [下一页：第7章：智能合约与Solidity](/learn/mastering-ethereum/08-07-smart-contracts-solidity/met2-07-smart-contracts-solidity)

<Attribution
  adaptedFrom="Andreas M. Antonopoulos、Gavin Wood、Carlo Parisi、Alessandro Mazza、Niccolò Pozzolini《Mastering Ethereum》第2版，O'Reilly Media，2025年10月，570页，数字版ISBN 9781098168414，印刷版ISBN 9781098168421 · 官方在线版前言与17章共516个二至五级标题 · 基于公开目录的独立中文教学重构，不复制受限正文"
  adaptedUrl="https://masteringethereum.xyz/chapter_6.html"
/>
