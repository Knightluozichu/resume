---
title: "第7章：智能合约与Solidity"
type: B
section: "Mastering Ethereum（第2版）"
order: 8
description: "Solidity源码经版本锁定、编译和部署形成初始化代码与运行时代码；ABI把函数选择器和参数编码成calldata。状态、调用语义、事件、错误和外部交互决定合约行为。"
demo: true
math: true
sourceUrl: "https://masteringethereum.xyz/chapter_7.html"
draft: false
---


## 为什么从可验证状态开始

本页按官方第二版在线目录重建 **Chapter 7. Smart Contracts and Solidity**，覆盖该正式单元的 38 个二至五级标题。教学顺序保留原目录的依赖关系，但正文全部独立撰写：每个标题都转成对象、前状态、输入、规则、后状态和失败证据，避免只给术语摘要。

Solidity源码经版本锁定、编译和部署形成初始化代码与运行时代码；ABI把函数选择器和参数编码成calldata。状态、调用语义、事件、错误和外部交互决定合约行为。 阅读时把“客户端或网页显示了什么”和“协议能验证什么”分开：钱包、浏览器、RPC与索引器是观察面，规范编码、签名、交易、收据、区块、状态根、证明和最终化检查点才是可复核对象。任何结论都要绑定链ID、客户端版本、区块坐标与原始输入。

实验统一在本地开发链或公开测试网完成，私钥只使用一次性测试向量。先写预期，再运行；先保存原始对象，再看格式化界面。正常样本证明主路径，边界样本选择零值、最大值、恰好阈值或时序边缘，单故障样本只改变链ID、nonce、一个字节、一个签名、一个价格或一个证明条件。

相同编译器、优化参数与源码应产生可核对字节码；外部调用前后必须维护状态不变量并检查返回结果。 若执行失败，证据必须说明首个拒绝规则、错误数据、消耗的资源以及状态是否回滚；若执行成功，还要区分交易池接纳、区块包含、执行状态与PoS最终性，不能把交易哈希当成最终结算。

通过标准是：能从源码复现ABI与字节码，部署最小合约，手算函数选择器并验证成功与回滚两条收据轨迹。 独立复核者应能从空目录和相同测试向量重建结果；缺失来源、版本、输入、前状态、后状态或失败证据任一项，结论都不能进入下一页。

## 版次、对象与验收合同

、、、、共同构成本页的复核坐标。Solidity源码经版本锁定、编译和部署形成初始化代码与运行时代码；ABI把函数选择器和参数编码成calldata。状态、调用语义、事件、错误和外部交互决定合约行为。


## 核心约束与推导

$$
S_{t+1}=Apply(S_t, T_t), \quad Valid(T_t,S_t)=1
$$

$$
gasUsed(T) \le gasLimit(T), \quad fee=gasUsed \times effectiveGasPrice
$$

$$
stateRoot_{n}=Commit(accounts,storage,code,balances)
$$

$$
gate=source \times version \times replay \times boundary \times failureEvidence
$$

状态转移只接受针对当前前状态有效的输入；Gas约束把资源消耗纳入确定性执行；状态根把大量账户与存储压缩为区块可承诺对象。证据门禁采用乘法语义，任一因子缺失都不能用其他截图或口头解释补偿。

## 三段可运行骨架

```typescript
type EthereumEvidence = {
  chainId: number;
  block: string;
  beforeRoot: string;
  inputHash: string;
  afterRoot?: string;
  error?: string;
};

export function evidenceGate(run: EthereumEvidence) {
  return {
    page: "met2-07-smart-contracts-solidity",
    accepted: Boolean(run.afterRoot || run.error),
    immutableInput: run.inputHash,
  };
}
```

```solidity
// SPDX-License-Identifier: MIT
pragma solidity ^0.8.26;

contract InvariantProbe {
    uint256 public value;
    event Applied(uint256 beforeValue, uint256 afterValue);

    function apply(uint256 next) external {
        uint256 beforeValue = value;
        require(next >= beforeValue, "non-monotonic");
        value = next;
        emit Applied(beforeValue, next);
    }
}
```

```typescript
function applyCandidate(
  state: bigint,
  next: bigint,
  gasUsed: bigint,
  gasLimit: bigint,
) {
  if (gasUsed > gasLimit)
    return { accepted: false, state, reason: "out-of-gas" };
  if (next < state)
    return { accepted: false, state, reason: "invalid-transition" };
  return { accepted: true, state: next, reason: null };
}
```

第一段把成功与失败放进同一证据合同；第二段给出可观察事件和显式不变量；第三段证明拒绝路径保持原状态。真实系统应使用成熟客户端、ABI、密码与证明库，并补齐并发、重组、超时、持久化、隐私和密钥隔离。

## 正式标题逐项讲解

### What Is a Smart Contract?

“What Is a Smart Contract?”是本单元正式标题 1/38，原目录层级 h2。这是合约执行边界。源码、编译器、ABI、字节码、调用上下文与持久状态必须一一对应；外部调用视为不可信，失败路径要证明状态原子回滚并记录Gas和错误数据。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。相同编译器、优化参数与源码应产生可核对字节码；外部调用前后必须维护状态不变量并检查返回结果。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Life Cycle of a Smart Contract

“Life Cycle of a Smart Contract”是本单元正式标题 2/38，原目录层级 h2。这是合约执行边界。源码、编译器、ABI、字节码、调用上下文与持久状态必须一一对应；外部调用视为不可信，失败路径要证明状态原子回滚并记录Gas和错误数据。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。相同编译器、优化参数与源码应产生可核对字节码；外部调用前后必须维护状态不变量并检查返回结果。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Introduction to Ethereum High-Level Languages

“Introduction to Ethereum High-Level Languages”是本单元正式标题 3/38，原目录层级 h2。Solidity源码经版本锁定、编译和部署形成初始化代码与运行时代码；ABI把函数选择器和参数编码成calldata。状态、调用语义、事件、错误和外部交互决定合约行为。 本标题要求把术语落到明确对象和状态转换：给出输入、前置条件、算法或协议规则、可观察输出以及至少一个拒绝样本。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。相同编译器、优化参数与源码应产生可核对字节码；外部调用前后必须维护状态不变量并检查返回结果。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Building a Smart Contract with Solidity

“Building a Smart Contract with Solidity”是本单元正式标题 4/38，原目录层级 h2。这是可复现实作坐标。先固定工具版本、网络、账户与原始输入，写出预期对象，再执行命令或合约；保存配置、交易哈希、收据、日志、状态首差与回退点，不能只截取成功界面。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。相同编译器、优化参数与源码应产生可核对字节码；外部调用前后必须维护状态不变量并检查返回结果。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Selecting a Version of Solidity

“Selecting a Version of Solidity”是本单元正式标题 5/38，原目录层级 h3。这是合约执行边界。源码、编译器、ABI、字节码、调用上下文与持久状态必须一一对应；外部调用视为不可信，失败路径要证明状态原子回滚并记录Gas和错误数据。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。相同编译器、优化参数与源码应产生可核对字节码；外部调用前后必须维护状态不变量并检查返回结果。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Downloading and Installing Solidity

“Downloading and Installing Solidity”是本单元正式标题 6/38，原目录层级 h3。这是可复现实作坐标。先固定工具版本、网络、账户与原始输入，写出预期对象，再执行命令或合约；保存配置、交易哈希、收据、日志、状态首差与回退点，不能只截取成功界面。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。相同编译器、优化参数与源码应产生可核对字节码；外部调用前后必须维护状态不变量并检查返回结果。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Development Environment

“Development Environment”是本单元正式标题 7/38，原目录层级 h3。Solidity源码经版本锁定、编译和部署形成初始化代码与运行时代码；ABI把函数选择器和参数编码成calldata。状态、调用语义、事件、错误和外部交互决定合约行为。 本标题要求把术语落到明确对象和状态转换：给出输入、前置条件、算法或协议规则、可观察输出以及至少一个拒绝样本。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。相同编译器、优化参数与源码应产生可核对字节码；外部调用前后必须维护状态不变量并检查返回结果。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Writing a Simple Solidity Program

“Writing a Simple Solidity Program”是本单元正式标题 8/38，原目录层级 h3。这是合约执行边界。源码、编译器、ABI、字节码、调用上下文与持久状态必须一一对应；外部调用视为不可信，失败路径要证明状态原子回滚并记录Gas和错误数据。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。相同编译器、优化参数与源码应产生可核对字节码；外部调用前后必须维护状态不变量并检查返回结果。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Compiling with the Solidity Compiler (solc)

“Compiling with the Solidity Compiler (solc)”是本单元正式标题 9/38，原目录层级 h3。这是可复现实作坐标。先固定工具版本、网络、账户与原始输入，写出预期对象，再执行命令或合约；保存配置、交易哈希、收据、日志、状态首差与回退点，不能只截取成功界面。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。相同编译器、优化参数与源码应产生可核对字节码；外部调用前后必须维护状态不变量并检查返回结果。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### The Ethereum Contract ABI

“The Ethereum Contract ABI”是本单元正式标题 10/38，原目录层级 h2。这是合约执行边界。源码、编译器、ABI、字节码、调用上下文与持久状态必须一一对应；外部调用视为不可信，失败路径要证明状态原子回滚并记录Gas和错误数据。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。相同编译器、优化参数与源码应产生可核对字节码；外部调用前后必须维护状态不变量并检查返回结果。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Selecting a Solidity Compiler and Language Version

“Selecting a Solidity Compiler and Language Version”是本单元正式标题 11/38，原目录层级 h2。这是可复现实作坐标。先固定工具版本、网络、账户与原始输入，写出预期对象，再执行命令或合约；保存配置、交易哈希、收据、日志、状态首差与回退点，不能只截取成功界面。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。相同编译器、优化参数与源码应产生可核对字节码；外部调用前后必须维护状态不变量并检查返回结果。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Programming with Solidity

“Programming with Solidity”是本单元正式标题 12/38，原目录层级 h2。这是合约执行边界。源码、编译器、ABI、字节码、调用上下文与持久状态必须一一对应；外部调用视为不可信，失败路径要证明状态原子回滚并记录Gas和错误数据。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。相同编译器、优化参数与源码应产生可核对字节码；外部调用前后必须维护状态不变量并检查返回结果。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Data Types

“Data Types”是本单元正式标题 13/38，原目录层级 h3。Solidity源码经版本锁定、编译和部署形成初始化代码与运行时代码；ABI把函数选择器和参数编码成calldata。状态、调用语义、事件、错误和外部交互决定合约行为。 本标题要求把术语落到明确对象和状态转换：给出输入、前置条件、算法或协议规则、可观察输出以及至少一个拒绝样本。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。相同编译器、优化参数与源码应产生可核对字节码；外部调用前后必须维护状态不变量并检查返回结果。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Variables: Definition and Scope

“Variables: Definition and Scope”是本单元正式标题 14/38，原目录层级 h3。这是开放金融不变量。计算资产守恒、价格函数、费用、抵押率与清算阈值，并显式记录预言机、治理、流动性和外部协议假设；收益率必须能追溯风险来源。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。相同编译器、优化参数与源码应产生可核对字节码；外部调用前后必须维护状态不变量并检查返回结果。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Predefined Global Variables and Functions

“Predefined Global Variables and Functions”是本单元正式标题 15/38，原目录层级 h3。这是合约执行边界。源码、编译器、ABI、字节码、调用上下文与持久状态必须一一对应；外部调用视为不可信，失败路径要证明状态原子回滚并记录Gas和错误数据。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。相同编译器、优化参数与源码应产生可核对字节码；外部调用前后必须维护状态不变量并检查返回结果。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Transaction/message call context

“Transaction/message call context”是本单元正式标题 16/38，原目录层级 h4。这是交易状态机坐标。按类型化信封解析链ID、nonce、费用上限、目标、value、data和签名，先做静态有效性检查，再区分交易池接纳、区块包含、执行成功与最终化。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。相同编译器、优化参数与源码应产生可核对字节码；外部调用前后必须维护状态不变量并检查返回结果。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Transaction context

“Transaction context”是本单元正式标题 17/38，原目录层级 h4。这是交易状态机坐标。按类型化信封解析链ID、nonce、费用上限、目标、value、data和签名，先做静态有效性检查，再区分交易池接纳、区块包含、执行成功与最终化。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。相同编译器、优化参数与源码应产生可核对字节码；外部调用前后必须维护状态不变量并检查返回结果。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Block context

“Block context”是本单元正式标题 18/38，原目录层级 h4。Solidity源码经版本锁定、编译和部署形成初始化代码与运行时代码；ABI把函数选择器和参数编码成calldata。状态、调用语义、事件、错误和外部交互决定合约行为。 本标题要求把术语落到明确对象和状态转换：给出输入、前置条件、算法或协议规则、可观察输出以及至少一个拒绝样本。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。相同编译器、优化参数与源码应产生可核对字节码；外部调用前后必须维护状态不变量并检查返回结果。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Address object

“Address object”是本单元正式标题 19/38，原目录层级 h4。这是密码与证明边界。必须区分秘密输入、公开输入、编码、域分离、算法输出和验证命题；使用成熟库与公开测试向量，篡改一个字节时验证应在首个不满足的规则处失败。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。相同编译器、优化参数与源码应产生可核对字节码；外部调用前后必须维护状态不变量并检查返回结果。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Built-in functions

“Built-in functions”是本单元正式标题 20/38，原目录层级 h4。这是合约执行边界。源码、编译器、ABI、字节码、调用上下文与持久状态必须一一对应；外部调用视为不可信，失败路径要证明状态原子回滚并记录Gas和错误数据。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。相同编译器、优化参数与源码应产生可核对字节码；外部调用前后必须维护状态不变量并检查返回结果。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Contract Definition

“Contract Definition”是本单元正式标题 21/38，原目录层级 h3。这是合约执行边界。源码、编译器、ABI、字节码、调用上下文与持久状态必须一一对应；外部调用视为不可信，失败路径要证明状态原子回滚并记录Gas和错误数据。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。相同编译器、优化参数与源码应产生可核对字节码；外部调用前后必须维护状态不变量并检查返回结果。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Functions

“Functions”是本单元正式标题 22/38，原目录层级 h3。这是合约执行边界。源码、编译器、ABI、字节码、调用上下文与持久状态必须一一对应；外部调用视为不可信，失败路径要证明状态原子回滚并记录Gas和错误数据。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。相同编译器、优化参数与源码应产生可核对字节码；外部调用前后必须维护状态不变量并检查返回结果。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Contract Constructor

“Contract Constructor”是本单元正式标题 23/38，原目录层级 h3。这是合约执行边界。源码、编译器、ABI、字节码、调用上下文与持久状态必须一一对应；外部调用视为不可信，失败路径要证明状态原子回滚并记录Gas和错误数据。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。相同编译器、优化参数与源码应产生可核对字节码；外部调用前后必须维护状态不变量并检查返回结果。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Function Modifiers

“Function Modifiers”是本单元正式标题 24/38，原目录层级 h3。这是合约执行边界。源码、编译器、ABI、字节码、调用上下文与持久状态必须一一对应；外部调用视为不可信，失败路径要证明状态原子回滚并记录Gas和错误数据。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。相同编译器、优化参数与源码应产生可核对字节码；外部调用前后必须维护状态不变量并检查返回结果。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Contract Inheritance

“Contract Inheritance”是本单元正式标题 25/38，原目录层级 h3。这是合约执行边界。源码、编译器、ABI、字节码、调用上下文与持久状态必须一一对应；外部调用视为不可信，失败路径要证明状态原子回滚并记录Gas和错误数据。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。相同编译器、优化参数与源码应产生可核对字节码；外部调用前后必须维护状态不变量并检查返回结果。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Multiple Inheritance

“Multiple Inheritance”是本单元正式标题 26/38，原目录层级 h3。Solidity源码经版本锁定、编译和部署形成初始化代码与运行时代码；ABI把函数选择器和参数编码成calldata。状态、调用语义、事件、错误和外部交互决定合约行为。 本标题要求把术语落到明确对象和状态转换：给出输入、前置条件、算法或协议规则、可观察输出以及至少一个拒绝样本。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。相同编译器、优化参数与源码应产生可核对字节码；外部调用前后必须维护状态不变量并检查返回结果。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Error Handling

“Error Handling”是本单元正式标题 27/38，原目录层级 h3。Solidity源码经版本锁定、编译和部署形成初始化代码与运行时代码；ABI把函数选择器和参数编码成calldata。状态、调用语义、事件、错误和外部交互决定合约行为。 本标题要求把术语落到明确对象和状态转换：给出输入、前置条件、算法或协议规则、可观察输出以及至少一个拒绝样本。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。相同编译器、优化参数与源码应产生可核对字节码；外部调用前后必须维护状态不变量并检查返回结果。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Events

“Events”是本单元正式标题 28/38，原目录层级 h3。这是合约执行边界。源码、编译器、ABI、字节码、调用上下文与持久状态必须一一对应；外部调用视为不可信，失败路径要证明状态原子回滚并记录Gas和错误数据。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。相同编译器、优化参数与源码应产生可核对字节码；外部调用前后必须维护状态不变量并检查返回结果。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Adding events

“Adding events”是本单元正式标题 29/38，原目录层级 h4。这是合约执行边界。源码、编译器、ABI、字节码、调用上下文与持久状态必须一一对应；外部调用视为不可信，失败路径要证明状态原子回滚并记录Gas和错误数据。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。相同编译器、优化参数与源码应产生可核对字节码；外部调用前后必须维护状态不变量并检查返回结果。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Catching events

“Catching events”是本单元正式标题 30/38，原目录层级 h4。这是合约执行边界。源码、编译器、ABI、字节码、调用上下文与持久状态必须一一对应；外部调用视为不可信，失败路径要证明状态原子回滚并记录Gas和错误数据。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。相同编译器、优化参数与源码应产生可核对字节码；外部调用前后必须维护状态不变量并检查返回结果。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Calling Other Contracts

“Calling Other Contracts”是本单元正式标题 31/38，原目录层级 h3。这是合约执行边界。源码、编译器、ABI、字节码、调用上下文与持久状态必须一一对应；外部调用视为不可信，失败路径要证明状态原子回滚并记录Gas和错误数据。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。相同编译器、优化参数与源码应产生可核对字节码；外部调用前后必须维护状态不变量并检查返回结果。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Creating a new instance

“Creating a new instance”是本单元正式标题 32/38，原目录层级 h4。这是可复现实作坐标。先固定工具版本、网络、账户与原始输入，写出预期对象，再执行命令或合约；保存配置、交易哈希、收据、日志、状态首差与回退点，不能只截取成功界面。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。相同编译器、优化参数与源码应产生可核对字节码；外部调用前后必须维护状态不变量并检查返回结果。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Addressing an existing instance

“Addressing an existing instance”是本单元正式标题 33/38，原目录层级 h4。这是密码与证明边界。必须区分秘密输入、公开输入、编码、域分离、算法输出和验证命题；使用成熟库与公开测试向量，篡改一个字节时验证应在首个不满足的规则处失败。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。相同编译器、优化参数与源码应产生可核对字节码；外部调用前后必须维护状态不变量并检查返回结果。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Raw call, delegatecall, and staticcall

“Raw call, delegatecall, and staticcall”是本单元正式标题 34/38，原目录层级 h4。这是合约执行边界。源码、编译器、ABI、字节码、调用上下文与持久状态必须一一对应；外部调用视为不可信，失败路径要证明状态原子回滚并记录Gas和错误数据。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。相同编译器、优化参数与源码应产生可核对字节码；外部调用前后必须维护状态不变量并检查返回结果。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Gas Considerations

“Gas Considerations”是本单元正式标题 35/38，原目录层级 h2。这是交易状态机坐标。按类型化信封解析链ID、nonce、费用上限、目标、value、data和签名，先做静态有效性检查，再区分交易池接纳、区块包含、执行成功与最终化。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。相同编译器、优化参数与源码应产生可核对字节码；外部调用前后必须维护状态不变量并检查返回结果。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Best Practices

“Best Practices”是本单元正式标题 36/38，原目录层级 h3。Solidity源码经版本锁定、编译和部署形成初始化代码与运行时代码；ABI把函数选择器和参数编码成calldata。状态、调用语义、事件、错误和外部交互决定合约行为。 本标题要求把术语落到明确对象和状态转换：给出输入、前置条件、算法或协议规则、可观察输出以及至少一个拒绝样本。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。相同编译器、优化参数与源码应产生可核对字节码；外部调用前后必须维护状态不变量并检查返回结果。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Estimating Gas Cost

“Estimating Gas Cost”是本单元正式标题 37/38，原目录层级 h3。这是交易状态机坐标。按类型化信封解析链ID、nonce、费用上限、目标、value、data和签名，先做静态有效性检查，再区分交易池接纳、区块包含、执行成功与最终化。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。相同编译器、优化参数与源码应产生可核对字节码；外部调用前后必须维护状态不变量并检查返回结果。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Conclusion

“Conclusion”是本单元正式标题 38/38，原目录层级 h2。这里不是重复定义，而是把本章对象收束为可验收合同：相同编译器、优化参数与源码应产生可核对字节码；外部调用前后必须维护状态不变量并检查返回结果。 复习时必须能从结论反向定位输入、规则、输出和失败证据。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。相同编译器、优化参数与源码应产生可核对字节码；外部调用前后必须维护状态不变量并检查返回结果。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

## 正常、边界与故障矩阵

| 样本   | 只改变的变量                            | 预期结果             | 必存证据                   |
| ------ | --------------------------------------- | -------------------- | -------------------------- |
| 正常   | 有效链ID、输入、签名、Gas与前状态       | 接受并得到预期后状态 | 原始交易、收据、前后状态根 |
| 边界   | 零值、最大值、恰好阈值或时序边缘        | 按明确规则接受或拒绝 | 边界值、判断分支、资源成本 |
| 单故障 | 链ID、nonce、签名、价格、证明或数据一项 | 首个无效规则处拒绝   | 错误数据、首差、原状态     |

先预测每个样本的状态根、返回值和错误，再操作滑块；若两个变量同时变化，实验失去归因能力，必须重置。

## 常见失败与回退


## 本章回顾

Solidity源码经版本锁定、编译和部署形成初始化代码与运行时代码；ABI把函数选择器和参数编码成calldata。状态、调用语义、事件、错误和外部交互决定合约行为。 核心不是记住工具名称，而是能从官方标题定位对象、规则与证据。相同编译器、优化参数与源码应产生可核对字节码；外部调用前后必须维护状态不变量并检查返回结果。 完成后应删除临时密钥和测试状态，保留不含秘密的版本、输入哈希、收据、状态根、错误与复核结论。


## 前后导航

- [上一页：第6章：交易](/learn/mastering-ethereum/07-06-transactions/met2-06-transactions)
- [下一页：第8章：智能合约与Vyper](/learn/mastering-ethereum/09-08-smart-contracts-vyper/met2-08-smart-contracts-vyper)

