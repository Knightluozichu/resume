---
title: "第10章：代币"
type: B
section: "Mastering Ethereum（第2版）"
order: 11
description: "代币是实现标准接口和账本不变量的合约。ERC-20、ERC-721、ERC-1155及扩展授权方案定义不同资产语义，接口兼容不等于经济安全或元数据可信。"
demo: true
math: true
sourceUrl: "https://masteringethereum.xyz/chapter_10.html"
draft: false
---

import {
  Met210TokensFlowLab,
  Met210TokensExperimentLab,
  Met210TokensEvidenceLab,
} from "@/components/mdx/mastering-ethereum/diagrams/met2-10-tokens";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第10章：代币的对象、状态、信任边界与官方目录坐标
- 能推导状态转换、Gas、状态承诺和证据门禁四组约束
- 能实现并运行三段最小代码，比较正常、边界和单故障样本
- 能设计独立复核并证明“能实现最小标准代币，核对接口、事件和供应不变量，并复现授权竞态或不安全接收边界。”

</Objectives>

## 为什么从可验证状态开始

本页按官方第二版在线目录重建 **Chapter 10. Tokens**，覆盖该正式单元的 33 个二至五级标题。教学顺序保留原目录的依赖关系，但正文全部独立撰写：每个标题都转成对象、前状态、输入、规则、后状态和失败证据，避免只给术语摘要。

代币是实现标准接口和账本不变量的合约。ERC-20、ERC-721、ERC-1155及扩展授权方案定义不同资产语义，接口兼容不等于经济安全或元数据可信。 阅读时把“客户端或网页显示了什么”和“协议能验证什么”分开：钱包、浏览器、RPC与索引器是观察面，规范编码、签名、交易、收据、区块、状态根、证明和最终化检查点才是可复核对象。任何结论都要绑定链ID、客户端版本、区块坐标与原始输入。

实验统一在本地开发链或公开测试网完成，私钥只使用一次性测试向量。先写预期，再运行；先保存原始对象，再看格式化界面。正常样本证明主路径，边界样本选择零值、最大值、恰好阈值或时序边缘，单故障样本只改变链ID、nonce、一个字节、一个签名、一个价格或一个证明条件。

铸造、销毁和转移必须守恒总供应与账户余额；授权更新、回调和元数据必须按标准与威胁模型处理。 若执行失败，证据必须说明首个拒绝规则、错误数据、消耗的资源以及状态是否回滚；若执行成功，还要区分交易池接纳、区块包含、执行状态与PoS最终性，不能把交易哈希当成最终结算。

通过标准是：能实现最小标准代币，核对接口、事件和供应不变量，并复现授权竞态或不安全接收边界。 独立复核者应能从空目录和相同测试向量重建结果；缺失来源、版本、输入、前状态、后状态或失败证据任一项，结论都不能进入下一页。

## 版次、对象与验收合同

<Term def="ERC-20是第10章：代币的第1个复核坐标，必须绑定对象、状态、规则和失败证据。">ERC-20</Term>、<Term def="ERC-721是第10章：代币的第2个复核坐标，必须绑定对象、状态、规则和失败证据。">ERC-721</Term>、<Term def="ERC-1155是第10章：代币的第3个复核坐标，必须绑定对象、状态、规则和失败证据。">ERC-1155</Term>、<Term def="allowance是第10章：代币的第4个复核坐标，必须绑定对象、状态、规则和失败证据。">allowance</Term>、<Term def="代币不变量是第10章：代币的第5个复核坐标，必须绑定对象、状态、规则和失败证据。">代币不变量</Term>
共同构成本页的复核坐标。代币是实现标准接口和账本不变量的合约。ERC-20、ERC-721、ERC-1155及扩展授权方案定义不同资产语义，接口兼容不等于经济安全或元数据可信。

<Callout type="info">
  **验收命题：**
  能实现最小标准代币，核对接口、事件和供应不变量，并复现授权竞态或不安全接收边界。
</Callout>

## 核心约束与推导

$$
S_{t+1}=Apply(S_t, T_t), \quad Valid(T_t,S_t)=1
$$

$$
gasUsed(T) \le gasLimit(T), \quad fee=gasUsed \times effectiveGasPrice
$$

$$
stateRoot_{n}=Commit(accounts,storage,code,balances)
$$

$$
gate=source \times version \times replay \times boundary \times failureEvidence
$$

状态转移只接受针对当前前状态有效的输入；Gas约束把资源消耗纳入确定性执行；状态根把大量账户与存储压缩为区块可承诺对象。证据门禁采用乘法语义，任一因子缺失都不能用其他截图或口头解释补偿。

## 三段可运行骨架

```typescript
type EthereumEvidence = {
  chainId: number;
  block: string;
  beforeRoot: string;
  inputHash: string;
  afterRoot?: string;
  error?: string;
};

export function evidenceGate(run: EthereumEvidence) {
  return {
    page: "met2-10-tokens",
    accepted: Boolean(run.afterRoot || run.error),
    immutableInput: run.inputHash,
  };
}
```

```solidity
// SPDX-License-Identifier: MIT
pragma solidity ^0.8.26;

contract InvariantProbe {
    uint256 public value;
    event Applied(uint256 beforeValue, uint256 afterValue);

    function apply(uint256 next) external {
        uint256 beforeValue = value;
        require(next >= beforeValue, "non-monotonic");
        value = next;
        emit Applied(beforeValue, next);
    }
}
```

```typescript
function applyCandidate(
  state: bigint,
  next: bigint,
  gasUsed: bigint,
  gasLimit: bigint,
) {
  if (gasUsed > gasLimit)
    return { accepted: false, state, reason: "out-of-gas" };
  if (next < state)
    return { accepted: false, state, reason: "invalid-transition" };
  return { accepted: true, state: next, reason: null };
}
```

第一段把成功与失败放进同一证据合同；第二段给出可观察事件和显式不变量；第三段证明拒绝路径保持原状态。真实系统应使用成熟客户端、ABI、密码与证明库，并补齐并发、重组、超时、持久化、隐私和密钥隔离。

## 正式标题逐项讲解

### How Tokens Are Used

“How Tokens Are Used”是本单元正式标题 1/33，原目录层级 h2。这是资产接口与账本边界。接口、事件、余额、总供应、授权和接收回调必须同步满足标准；兼容函数签名不代表经济不变量、元数据或外部集成天然安全。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。铸造、销毁和转移必须守恒总供应与账户余额；授权更新、回调和元数据必须按标准与威胁模型处理。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Tokens and Fungibility

“Tokens and Fungibility”是本单元正式标题 2/33，原目录层级 h2。这是资产接口与账本边界。接口、事件、余额、总供应、授权和接收回调必须同步满足标准；兼容函数签名不代表经济不变量、元数据或外部集成天然安全。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。铸造、销毁和转移必须守恒总供应与账户余额；授权更新、回调和元数据必须按标准与威胁模型处理。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Counterparty Risk

“Counterparty Risk”是本单元正式标题 3/33，原目录层级 h2。这是威胁模型坐标。先写资产、攻击者能力和安全不变量，再构造只改变一个变量的攻击样本；修复要覆盖正常、边界和原失败，且不能依赖调用顺序或前端隐藏。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。铸造、销毁和转移必须守恒总供应与账户余额；授权更新、回调和元数据必须按标准与威胁模型处理。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Tokens and Intrinsicality

“Tokens and Intrinsicality”是本单元正式标题 4/33，原目录层级 h2。这是资产接口与账本边界。接口、事件、余额、总供应、授权和接收回调必须同步满足标准；兼容函数签名不代表经济不变量、元数据或外部集成天然安全。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。铸造、销毁和转移必须守恒总供应与账户余额；授权更新、回调和元数据必须按标准与威胁模型处理。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Utility, Equity, or Cash Grab?

“Utility, Equity, or Cash Grab?”是本单元正式标题 5/33，原目录层级 h2。代币是实现标准接口和账本不变量的合约。ERC-20、ERC-721、ERC-1155及扩展授权方案定义不同资产语义，接口兼容不等于经济安全或元数据可信。 本标题要求把术语落到明确对象和状态转换：给出输入、前置条件、算法或协议规则、可观察输出以及至少一个拒绝样本。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。铸造、销毁和转移必须守恒总供应与账户余额；授权更新、回调和元数据必须按标准与威胁模型处理。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### It’s a Duck!

“It’s a Duck!”是本单元正式标题 6/33，原目录层级 h2。代币是实现标准接口和账本不变量的合约。ERC-20、ERC-721、ERC-1155及扩展授权方案定义不同资产语义，接口兼容不等于经济安全或元数据可信。 本标题要求把术语落到明确对象和状态转换：给出输入、前置条件、算法或协议规则、可观察输出以及至少一个拒绝样本。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。铸造、销毁和转移必须守恒总供应与账户余额；授权更新、回调和元数据必须按标准与威胁模型处理。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Tokens on Ethereum

“Tokens on Ethereum”是本单元正式标题 7/33，原目录层级 h2。这是资产接口与账本边界。接口、事件、余额、总供应、授权和接收回调必须同步满足标准；兼容函数签名不代表经济不变量、元数据或外部集成天然安全。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。铸造、销毁和转移必须守恒总供应与账户余额；授权更新、回调和元数据必须按标准与威胁模型处理。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### The ERC-20 Token Standard

“The ERC-20 Token Standard”是本单元正式标题 8/33，原目录层级 h2。这是资产接口与账本边界。接口、事件、余额、总供应、授权和接收回调必须同步满足标准；兼容函数签名不代表经济不变量、元数据或外部集成天然安全。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。铸造、销毁和转移必须守恒总供应与账户余额；授权更新、回调和元数据必须按标准与威胁模型处理。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### ERC-20 required functions and events

“ERC-20 required functions and events”是本单元正式标题 9/33，原目录层级 h3。这是合约执行边界。源码、编译器、ABI、字节码、调用上下文与持久状态必须一一对应；外部调用视为不可信，失败路径要证明状态原子回滚并记录Gas和错误数据。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。铸造、销毁和转移必须守恒总供应与账户余额；授权更新、回调和元数据必须按标准与威胁模型处理。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### ERC-20 optional functions

“ERC-20 optional functions”是本单元正式标题 10/33，原目录层级 h4。这是合约执行边界。源码、编译器、ABI、字节码、调用上下文与持久状态必须一一对应；外部调用视为不可信，失败路径要证明状态原子回滚并记录Gas和错误数据。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。铸造、销毁和转移必须守恒总供应与账户余额；授权更新、回调和元数据必须按标准与威胁模型处理。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### The ERC-20 interface defined in Solidity

“The ERC-20 interface defined in Solidity”是本单元正式标题 11/33，原目录层级 h4。这是合约执行边界。源码、编译器、ABI、字节码、调用上下文与持久状态必须一一对应；外部调用视为不可信，失败路径要证明状态原子回滚并记录Gas和错误数据。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。铸造、销毁和转移必须守恒总供应与账户余额；授权更新、回调和元数据必须按标准与威胁模型处理。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### ERC-20 data structures

“ERC-20 data structures”是本单元正式标题 12/33，原目录层级 h4。这是资产接口与账本边界。接口、事件、余额、总供应、授权和接收回调必须同步满足标准；兼容函数签名不代表经济不变量、元数据或外部集成天然安全。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。铸造、销毁和转移必须守恒总供应与账户余额；授权更新、回调和元数据必须按标准与威胁模型处理。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### ERC-20 workflows: “Transfer” and “approve and transferFrom”

“ERC-20 workflows: “Transfer” and “approve and transferFrom””是本单元正式标题 13/33，原目录层级 h4。这是资产接口与账本边界。接口、事件、余额、总供应、授权和接收回调必须同步满足标准；兼容函数签名不代表经济不变量、元数据或外部集成天然安全。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。铸造、销毁和转移必须守恒总供应与账户余额；授权更新、回调和元数据必须按标准与威胁模型处理。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### ERC-2612: Gasless transfers with “permit”

“ERC-2612: Gasless transfers with “permit””是本单元正式标题 14/33，原目录层级 h4。这是交易状态机坐标。按类型化信封解析链ID、nonce、费用上限、目标、value、data和签名，先做静态有效性检查，再区分交易池接纳、区块包含、执行成功与最终化。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。铸造、销毁和转移必须守恒总供应与账户余额；授权更新、回调和元数据必须按标准与威胁模型处理。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### ERC-20 implementations

“ERC-20 implementations”是本单元正式标题 15/33，原目录层级 h4。这是可复现实作坐标。先固定工具版本、网络、账户与原始输入，写出预期对象，再执行命令或合约；保存配置、交易哈希、收据、日志、状态首差与回退点，不能只截取成功界面。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。铸造、销毁和转移必须守恒总供应与账户余额；授权更新、回调和元数据必须按标准与威胁模型处理。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Launching Our Own ERC-20 Token

“Launching Our Own ERC-20 Token”是本单元正式标题 16/33，原目录层级 h3。这是资产接口与账本边界。接口、事件、余额、总供应、授权和接收回调必须同步满足标准；兼容函数签名不代表经济不变量、元数据或外部集成天然安全。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。铸造、销毁和转移必须守恒总供应与账户余额；授权更新、回调和元数据必须按标准与威胁模型处理。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Interacting with METoken

“Interacting with METoken”是本单元正式标题 17/33，原目录层级 h4。这是可复现实作坐标。先固定工具版本、网络、账户与原始输入，写出预期对象，再执行命令或合约；保存配置、交易哈希、收据、日志、状态首差与回退点，不能只截取成功界面。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。铸造、销毁和转移必须守恒总供应与账户余额；授权更新、回调和元数据必须按标准与威胁模型处理。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Sending ERC-20 tokens to contract addresses

“Sending ERC-20 tokens to contract addresses”是本单元正式标题 18/33，原目录层级 h4。这是可复现实作坐标。先固定工具版本、网络、账户与原始输入，写出预期对象，再执行命令或合约；保存配置、交易哈希、收据、日志、状态首差与回退点，不能只截取成功界面。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。铸造、销毁和转移必须守恒总供应与账户余额；授权更新、回调和元数据必须按标准与威胁模型处理。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Demonstrating the “approve and transferFrom” workflow

“Demonstrating the “approve and transferFrom” workflow”是本单元正式标题 19/33，原目录层级 h4。代币是实现标准接口和账本不变量的合约。ERC-20、ERC-721、ERC-1155及扩展授权方案定义不同资产语义，接口兼容不等于经济安全或元数据可信。 本标题要求把术语落到明确对象和状态转换：给出输入、前置条件、算法或协议规则、可观察输出以及至少一个拒绝样本。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。铸造、销毁和转移必须守恒总供应与账户余额；授权更新、回调和元数据必须按标准与威胁模型处理。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Issues with ERC-20 Tokens

“Issues with ERC-20 Tokens”是本单元正式标题 20/33，原目录层级 h3。这是资产接口与账本边界。接口、事件、余额、总供应、授权和接收回调必须同步满足标准；兼容函数签名不代表经济不变量、元数据或外部集成天然安全。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。铸造、销毁和转移必须守恒总供应与账户余额；授权更新、回调和元数据必须按标准与威胁模型处理。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### ERC-223: A proposed token contract interface standard

“ERC-223: A proposed token contract interface standard”是本单元正式标题 21/33，原目录层级 h3。这是合约执行边界。源码、编译器、ABI、字节码、调用上下文与持久状态必须一一对应；外部调用视为不可信，失败路径要证明状态原子回滚并记录Gas和错误数据。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。铸造、销毁和转移必须守恒总供应与账户余额；授权更新、回调和元数据必须按标准与威胁模型处理。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### ERC-777: The future that could have been

“ERC-777: The future that could have been”是本单元正式标题 22/33，原目录层级 h3。这是资产接口与账本边界。接口、事件、余额、总供应、授权和接收回调必须同步满足标准；兼容函数签名不代表经济不变量、元数据或外部集成天然安全。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。铸造、销毁和转移必须守恒总供应与账户余额；授权更新、回调和元数据必须按标准与威胁模型处理。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Issues with ERC-777 tokens

“Issues with ERC-777 tokens”是本单元正式标题 23/33，原目录层级 h4。这是资产接口与账本边界。接口、事件、余额、总供应、授权和接收回调必须同步满足标准；兼容函数签名不代表经济不变量、元数据或外部集成天然安全。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。铸造、销毁和转移必须守恒总供应与账户余额；授权更新、回调和元数据必须按标准与威胁模型处理。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### The future that could have been

“The future that could have been”是本单元正式标题 24/33，原目录层级 h4。代币是实现标准接口和账本不变量的合约。ERC-20、ERC-721、ERC-1155及扩展授权方案定义不同资产语义，接口兼容不等于经济安全或元数据可信。 本标题要求把术语落到明确对象和状态转换：给出输入、前置条件、算法或协议规则、可观察输出以及至少一个拒绝样本。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。铸造、销毁和转移必须守恒总供应与账户余额；授权更新、回调和元数据必须按标准与威胁模型处理。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### ERC-721: NFT Standard

“ERC-721: NFT Standard”是本单元正式标题 25/33，原目录层级 h2。这是资产接口与账本边界。接口、事件、余额、总供应、授权和接收回调必须同步满足标准；兼容函数签名不代表经济不变量、元数据或外部集成天然安全。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。铸造、销毁和转移必须守恒总供应与账户余额；授权更新、回调和元数据必须按标准与威胁模型处理。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### ERC-1155: Multitoken Standard

“ERC-1155: Multitoken Standard”是本单元正式标题 26/33，原目录层级 h2。这是资产接口与账本边界。接口、事件、余额、总供应、授权和接收回调必须同步满足标准；兼容函数签名不代表经济不变量、元数据或外部集成天然安全。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。铸造、销毁和转移必须守恒总供应与账户余额；授权更新、回调和元数据必须按标准与威胁模型处理。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Using Token Standards

“Using Token Standards”是本单元正式标题 27/33，原目录层级 h2。这是资产接口与账本边界。接口、事件、余额、总供应、授权和接收回调必须同步满足标准；兼容函数签名不代表经济不变量、元数据或外部集成天然安全。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。铸造、销毁和转移必须守恒总供应与账户余额；授权更新、回调和元数据必须按标准与威胁模型处理。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### What Are Token Standards and What Is Their Purpose?

“What Are Token Standards and What Is Their Purpose?”是本单元正式标题 28/33，原目录层级 h3。这是资产接口与账本边界。接口、事件、余额、总供应、授权和接收回调必须同步满足标准；兼容函数签名不代表经济不变量、元数据或外部集成天然安全。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。铸造、销毁和转移必须守恒总供应与账户余额；授权更新、回调和元数据必须按标准与威胁模型处理。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Should You Use These Standards?

“Should You Use These Standards?”是本单元正式标题 29/33，原目录层级 h3。代币是实现标准接口和账本不变量的合约。ERC-20、ERC-721、ERC-1155及扩展授权方案定义不同资产语义，接口兼容不等于经济安全或元数据可信。 本标题要求把术语落到明确对象和状态转换：给出输入、前置条件、算法或协议规则、可观察输出以及至少一个拒绝样本。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。铸造、销毁和转移必须守恒总供应与账户余额；授权更新、回调和元数据必须按标准与威胁模型处理。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Detecting Standards: EIP-165

“Detecting Standards: EIP-165”是本单元正式标题 30/33，原目录层级 h3。代币是实现标准接口和账本不变量的合约。ERC-20、ERC-721、ERC-1155及扩展授权方案定义不同资产语义，接口兼容不等于经济安全或元数据可信。 本标题要求把术语落到明确对象和状态转换：给出输入、前置条件、算法或协议规则、可观察输出以及至少一个拒绝样本。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。铸造、销毁和转移必须守恒总供应与账户余额；授权更新、回调和元数据必须按标准与威胁模型处理。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Security by Maturity

“Security by Maturity”是本单元正式标题 31/33，原目录层级 h3。这是威胁模型坐标。先写资产、攻击者能力和安全不变量，再构造只改变一个变量的攻击样本；修复要覆盖正常、边界和原失败，且不能依赖调用顺序或前端隐藏。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。铸造、销毁和转移必须守恒总供应与账户余额；授权更新、回调和元数据必须按标准与威胁模型处理。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Extensions to Token Interface Standards

“Extensions to Token Interface Standards”是本单元正式标题 32/33，原目录层级 h2。这是资产接口与账本边界。接口、事件、余额、总供应、授权和接收回调必须同步满足标准；兼容函数签名不代表经济不变量、元数据或外部集成天然安全。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。铸造、销毁和转移必须守恒总供应与账户余额；授权更新、回调和元数据必须按标准与威胁模型处理。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

### Conclusion

“Conclusion”是本单元正式标题 33/33，原目录层级 h2。这里不是重复定义，而是把本章对象收束为可验收合同：铸造、销毁和转移必须守恒总供应与账户余额；授权更新、回调和元数据必须按标准与威胁模型处理。 复习时必须能从结论反向定位输入、规则、输出和失败证据。

复核时固定第二版来源、链ID、客户端、前状态与原始输入，保存编码、解析、签名、执行、收据、状态或证明的中间对象。铸造、销毁和转移必须守恒总供应与账户余额；授权更新、回调和元数据必须按标准与威胁模型处理。 本标题通过后还要能说明它与上级标题及下一标题的依赖，避免把相邻机制合并成一句概述。

## 正常、边界与故障矩阵

| 样本   | 只改变的变量                            | 预期结果             | 必存证据                   |
| ------ | --------------------------------------- | -------------------- | -------------------------- |
| 正常   | 有效链ID、输入、签名、Gas与前状态       | 接受并得到预期后状态 | 原始交易、收据、前后状态根 |
| 边界   | 零值、最大值、恰好阈值或时序边缘        | 按明确规则接受或拒绝 | 边界值、判断分支、资源成本 |
| 单故障 | 链ID、nonce、签名、价格、证明或数据一项 | 首个无效规则处拒绝   | 错误数据、首差、原状态     |

先预测每个样本的状态根、返回值和错误，再操作滑块；若两个变量同时变化，实验失去归因能力，必须重置。

## 三步交互实验

<Stepper>
  <Step title="追踪状态转换">
    先预测当前阶段的输入、验证依据与后状态，再拖动阶段滑块定位首个分叉。
    <Met210TokensFlowLab />
  </Step>
  <Step title="改变Gas与负载">
    一次只调整Gas上限或负载，观察执行预算、状态承诺和最终性指标怎样变化。
    <Met210TokensExperimentLab />
  </Step>
  <Step title="注入单一故障">
    注入错误链、签名、状态、数据或证明，要求门禁在提交前拒绝并指出回退点。
    <Met210TokensEvidenceLab />
  </Step>
</Stepper>

## 常见失败与回退

<Callout type="trap">
  **把界面成功当成协议成功：**
  交易哈希、RPC返回或前端提示只表示某一观察点。回退到原始交易、收据状态、区块哈希、状态根和最终化检查点逐项核对。
</Callout>

<Callout type="trap">
  **修复时同时改变多个变量：**
  升级客户端、换网络、改账户又改输入会失去首差。恢复固定基线，只改变一个变量，重放原失败、相邻边界和正常样本。
</Callout>

## 本章回顾

代币是实现标准接口和账本不变量的合约。ERC-20、ERC-721、ERC-1155及扩展授权方案定义不同资产语义，接口兼容不等于经济安全或元数据可信。 核心不是记住工具名称，而是能从官方标题定位对象、规则与证据。铸造、销毁和转移必须守恒总供应与账户余额；授权更新、回调和元数据必须按标准与威胁模型处理。 完成后应删除临时密钥和测试状态，保留不含秘密的版本、输入哈希、收据、状态根、错误与复核结论。

<Glossary>

<GlossaryItem term="ERC-20">
  ERC-20用于界定第10章：代币中的输入、验证规则和可观察结果。
</GlossaryItem>

<GlossaryItem term="ERC-721">
  ERC-721用于界定第10章：代币中的输入、验证规则和可观察结果。
</GlossaryItem>

<GlossaryItem term="ERC-1155">
  ERC-1155用于界定第10章：代币中的输入、验证规则和可观察结果。
</GlossaryItem>

<GlossaryItem term="allowance">
  allowance用于界定第10章：代币中的输入、验证规则和可观察结果。
</GlossaryItem>

<GlossaryItem term="代币不变量">
  代币不变量用于界定第10章：代币中的输入、验证规则和可观察结果。
</GlossaryItem>

</Glossary>

<Exercises>

1. 如何为Chapter 10. Tokens建立最小可重放基线？

<Answer>
  固定官方第二版页面、链ID、客户端版本、账户、区块坐标、前状态和原始输入，先手算预期，再保存解析、验证、执行、收据与状态证据。
</Answer>

2. 如何证明“能实现最小标准代币，核对接口、事件和供应不变量，并复现授权竞态或不安全接收边界。”不是一次偶然成功？

<Answer>
  在同一基线上执行正常、边界和单故障样本，由独立客户端核对原始对象、状态根与拒绝点；三组证据一致才通过。
</Answer>

3. 修复Chapter 10. Tokens的一个失败后怎样回归？

<Answer>
  先重放原失败，再运行相邻边界和正常基线；按版本、网络、编码、签名、状态、Gas、证明与最终性定位首差，全部不变量通过后才进入下一页。
</Answer>

</Exercises>

## 前后导航

- [上一页：第9章：智能合约安全](/learn/mastering-ethereum/10-09-smart-contract-security/met2-09-smart-contract-security)
- [下一页：第11章：预言机](/learn/mastering-ethereum/12-11-oracles/met2-11-oracles)

<Attribution
  adaptedFrom="Andreas M. Antonopoulos、Gavin Wood、Carlo Parisi、Alessandro Mazza、Niccolò Pozzolini《Mastering Ethereum》第2版，O'Reilly Media，2025年10月，570页，数字版ISBN 9781098168414，印刷版ISBN 9781098168421 · 官方在线版前言与17章共516个二至五级标题 · 基于公开目录的独立中文教学重构，不复制受限正文"
  adaptedUrl="https://masteringethereum.xyz/chapter_10.html"
/>
