---
title: "第15章 Web 上的 Rust 与 WebAssembly"
type: C
section: "精通Rust 2e · 跨平台与调试"
order: 15
description: "按 原书第15章 Rust on the Web with WebAssembly 一比一重建核心知识：WebAssembly 模型、设计目标、模块与线性内存、Rust 到 WebAssembly、浏览器集成。"
demo: true
math: false
sourceUrl: "https://www.packtpub.com/en-us/product/mastering-rust-9781789346572"
draft: true
---

import { Objectives, Callout, Attribution, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step } from "@/components/mdx/mdx-components";
import { MrsWebassemblyModelLab, MrsWebassemblyBoundaryLab, MrsWebassemblyEvidenceLab } from "@/components/mdx/mastering-rust-2e/diagrams/mrs-webassembly";

<Objectives>
- 能解释WebAssembly 模型与设计目标在本章执行链中的责任
- 能区分模块与线性内存和Rust 到 WebAssembly的适用边界与失败语义
- 能实现并复现一个围绕浏览器集成的最小实验
</Objectives>

## 先做预测

先回答：示例能够编译并运行，是否已经证明设计正确？不能。编译成功只说明当前程序满足类型与语法约束，还要验证输入边界、失败路径、资源生命周期、并发或外部系统条件。<Term def="WebAssembly是可移植的低层字节码与验证执行模型，不是浏览器里的完整操作系统；宿主通过导入导出提供DOM、网络和时钟等能力。">WebAssembly 模型</Term>是本章的起点，<Term def="紧凑、快速解码、接近原生性能和语言无关是核心目标；沙箱减少权限面，但应用仍要验证输入、限制资源并更新依赖。">设计目标</Term>则把起点放进可重复的工程过程。

<MrsWebassemblyModelLab />

## 原书章节骨架

Packt 官方目录把本章组织为WebAssembly 模型、设计目标、模块与线性内存、Rust 到 WebAssembly、浏览器集成。这里保留原书问题顺序，再把2019年前后的案例放到当前Rust工程语境中理解：概念不因crate版本变化而失效，但命令、依赖和框架API必须以当前项目锁定版本的官方文档为准。

官方核心单元核对：WebAssembly模型；WebAssembly设计目标；WebAssembly入门；Rust到WebAssembly；浏览器集成。

<Term def="模块导出函数并使用线性内存交换数据，跨边界字符串通常需要指针、长度与编码协议；复制和分配成本要真实测量。">模块与线性内存</Term>不是孤立语法。它承接WebAssembly 模型给出的目标，并通过设计目标进入真实项目。阅读代码时要沿着输入、所有权或状态、动作、结果和证据五个节点追踪，而不是只记函数名。

```rust
#[cfg(target_arch = "wasm32")]
#[unsafe(no_mangle)]
pub extern "C" fn checksum(ptr: *const u8, len: usize) -> u32 {
    let bytes = unsafe { std::slice::from_raw_parts(ptr, len) };
    bytes.iter().map(|value| u32::from(*value)).sum()
}
```

## 机制一：从类型到行为

WebAssembly是可移植的低层字节码与验证执行模型，不是浏览器里的完整操作系统；宿主通过导入导出提供DOM、网络和时钟等能力。 紧凑、快速解码、接近原生性能和语言无关是核心目标；沙箱减少权限面，但应用仍要验证输入、限制资源并更新依赖。 两者协作时，先写出谁拥有数据、谁可以修改、失败由谁处理，再决定具体API。这样可以把编译器诊断还原为契约冲突，而不是把错误信息当成需要逐条消除的噪声。

<Term def="Rust目标、绑定生成和JavaScript胶水共同组成构建链；Rust类型不能自动无损映射到宿主对象，边界应保持小而稳定。">Rust 到 WebAssembly</Term>进一步约束运行行为。它说明“类型正确”与“协议正确”是两层证据：前者由编译器帮助证明，后者还需要测试、时间线和边界输入。

<MrsWebassemblyBoundaryLab />

```rust
use wasm_bindgen::prelude::*;

#[wasm_bindgen]
pub fn greet(name: &str) -> String {
    format!("Hello, {name}")
}
```

## 机制二：失败、资源与边界

模块导出函数并使用线性内存交换数据，跨边界字符串通常需要指针、长度与编码协议；复制和分配成本要真实测量。 Rust目标、绑定生成和JavaScript胶水共同组成构建链；Rust类型不能自动无损映射到宿主对象，边界应保持小而稳定。 失败实验至少覆盖正常、空值或零长度、上限附近、显式错误和资源中断。涉及线程、网络、数据库、GUI或FFI时，还要加入超时、取消、部分完成与关闭路径。

不要把原书示例的依赖版本直接复制到新项目。先固定Rust edition与最小支持版本，再查询维护中的crate文档和迁移说明；保留原书要讲的架构边界，按当前API重写适配层。

```bash
rustup target add wasm32-unknown-unknown
cargo build --target wasm32-unknown-unknown --release
wasm-tools validate target/wasm32-unknown-unknown/release/app.wasm
```

## 机制三：可复现证据

<Term def="事件回调、生命周期和错误需要在宿主与模块间明确归属；释放闭包、处理panic并保留source map，才能让线上问题可诊断。">浏览器集成</Term>负责把本章收束成证据。事件回调、生命周期和错误需要在宿主与模块间明确归属；释放闭包、处理panic并保留source map，才能让线上问题可诊断。 一次可靠验收要保存输入、工具链、依赖锁定、命令、退出状态、关键输出和失败样本。性能结论还要记录release构建、样本数与环境噪声；并发结论则要记录任务边界、超时和关闭结果。

<MrsWebassemblyEvidenceLab />

## 分步实验

<Stepper>
  <Step title="建立模型"><MrsWebassemblyModelLab /></Step>
  <Step title="注入边界"><MrsWebassemblyBoundaryLab /></Step>
  <Step title="保留证据"><MrsWebassemblyEvidenceLab /></Step>
</Stepper>

## 验收清单

1. 在隔离目录运行三段示例，先预测结果和所有权变化，再记录实际编译诊断。
2. 为主路径增加一个正常测试和至少三个边界测试，错误断言检查类别与上下文，而不只匹配整段文本。
3. 清理构建产物后重跑，确认没有依赖本地缓存、环境变量或未声明工具。
4. 若章节涉及外部系统，使用本地测试服务或临时数据库，设置时间预算并验证资源最终释放。

## 常见误区

<Callout type="trap" title="把编译通过当成章节验收">
编译器不能证明业务输入完整、超时合理、协议有背压或外部副作用已回滚。必须补充失败测试与可观察证据。
</Callout>

<Callout type="trap" title="把2019年的crate API当成固定标准">
本书核心结构值得保留，但框架和工具API会变化。新代码应锁定版本、查阅维护中的官方文档，并把迁移集中在适配边界。
</Callout>

## 本章回顾

本章按照官方目录逐项覆盖WebAssembly 模型、设计目标、模块与线性内存、Rust 到 WebAssembly、浏览器集成。核心方法是先用类型表达资源与能力，再用运行期协议约束超时、顺序和外部失败，最后用测试、日志或调试记录证明结果可重复。

<Exercises>
**问题 1：如何解释WebAssembly 模型与设计目标的协作？**
<Answer>WebAssembly是可移植的低层字节码与验证执行模型，不是浏览器里的完整操作系统；宿主通过导入导出提供DOM、网络和时钟等能力。 紧凑、快速解码、接近原生性能和语言无关是核心目标；沙箱减少权限面，但应用仍要验证输入、限制资源并更新依赖。</Answer>

**问题 2：模块与线性内存和Rust 到 WebAssembly的边界是什么？**
<Answer>模块导出函数并使用线性内存交换数据，跨边界字符串通常需要指针、长度与编码协议；复制和分配成本要真实测量。 Rust目标、绑定生成和JavaScript胶水共同组成构建链；Rust类型不能自动无损映射到宿主对象，边界应保持小而稳定。</Answer>

**问题 3：怎样验收浏览器集成？**
<Answer>事件回调、生命周期和错误需要在宿主与模块间明确归属；释放闭包、处理panic并保留source map，才能让线上问题可诊断。 使用正常、边界、错误和资源中断输入，保存工具链、命令、退出状态和可复查输出。</Answer>
</Exercises>

## 术语表

<Glossary>
  <GlossaryItem term="WebAssembly 模型" definition="WebAssembly是可移植的低层字节码与验证执行模型，不是浏览器里的完整操作系统" />
  <GlossaryItem term="设计目标" definition="紧凑、快速解码、接近原生性能和语言无关是核心目标" />
  <GlossaryItem term="模块与线性内存" definition="模块导出函数并使用线性内存交换数据，跨边界字符串通常需要指针、长度与编码协议" />
  <GlossaryItem term="Rust 到 WebAssembly" definition="Rust目标、绑定生成和JavaScript胶水共同组成构建链" />
  <GlossaryItem term="浏览器集成" definition="事件回调、生命周期和错误需要在宿主与模块间明确归属" />
  <GlossaryItem term="章节验收边界" definition="用输入、失败、资源、证据和迁移约束验证章节核心。" />
</Glossary>

<Attribution adaptedFrom="Mastering Rust, Second Edition · 原书第15章 Rust on the Web with WebAssembly" adaptedUrl="https://www.packtpub.com/en-us/product/mastering-rust-9781789346572" />
