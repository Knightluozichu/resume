---
title: "全书总复习"
type: C
section: "精通Rust 2e · 总复习"
order: 18
description: "按 Mastering Rust Integration Review 一比一重建核心知识：静态契约、运行期协议、边界最小化、可复现工程、迁移原则。"
demo: true
math: false
sourceUrl: "https://www.packtpub.com/en-us/product/mastering-rust-9781789346572"
draft: true
---

import { Objectives, Callout, Attribution, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step } from "@/components/mdx/mdx-components";
import { MrsFinalReviewModelLab, MrsFinalReviewBoundaryLab, MrsFinalReviewEvidenceLab } from "@/components/mdx/mastering-rust-2e/diagrams/mrs-final-review";

<Objectives>
- 能解释静态契约与运行期协议在本章执行链中的责任
- 能区分边界最小化和可复现工程的适用边界与失败语义
- 能实现并复现一个围绕迁移原则的最小实验
</Objectives>

## 先做预测

先回答：示例能够编译并运行，是否已经证明设计正确？不能。编译成功只说明当前程序满足类型与语法约束，还要验证输入边界、失败路径、资源生命周期、并发或外部系统条件。<Term def="所有权、trait bound、Result和Send或Sync把资源、能力、失败与线程边界编码进类型，先让非法组合无法通过编译。">静态契约</Term>是本章的起点，<Term def="超时、背压、锁顺序、事务、日志字段和优雅关闭无法只靠类型完成，必须用状态机、测试和运行证据验证。">运行期协议</Term>则把起点放进可重复的工程过程。

<MrsFinalReviewModelLab />

## 原书章节骨架

Packt 官方目录把本章组织为静态契约、运行期协议、边界最小化、可复现工程、迁移原则。这里保留原书问题顺序，再把2019年前后的案例放到当前Rust工程语境中理解：概念不因crate版本变化而失效，但命令、依赖和框架API必须以当前项目锁定版本的官方文档为准。

<Term def="宏、unsafe、FFI、Web请求、数据库和宿主集成都应缩小不可信边界，在安全核心前完成解析与验证。">边界最小化</Term>不是孤立语法。它承接静态契约给出的目标，并通过运行期协议进入真实项目。阅读代码时要沿着输入、所有权或状态、动作、结果和证据五个节点追踪，而不是只记函数名。

```rust
trait Command { type Output; fn run(self) -> Result<Self::Output, AppError>; }

fn execute<C: Command + Send>(command: C) -> Result<C::Output, AppError> {
    command.run()
}
```

## 机制一：从类型到行为

所有权、trait bound、Result和Send或Sync把资源、能力、失败与线程边界编码进类型，先让非法组合无法通过编译。 超时、背压、锁顺序、事务、日志字段和优雅关闭无法只靠类型完成，必须用状态机、测试和运行证据验证。 两者协作时，先写出谁拥有数据、谁可以修改、失败由谁处理，再决定具体API。这样可以把编译器诊断还原为契约冲突，而不是把错误信息当成需要逐条消除的噪声。

<Term def="Cargo清单、锁定策略、测试、文档、基准、调试符号和最小复现共同让同一结论能在干净环境重复。">可复现工程</Term>进一步约束运行行为。它说明“类型正确”与“协议正确”是两层证据：前者由编译器帮助证明，后者还需要测试、时间线和边界输入。

<MrsFinalReviewBoundaryLab />

```text
输入验证 -> 类型化命令 -> 有界执行 -> Result
        -> 结构化日志 -> 持久化事务 -> 回归证据
```

## 机制二：失败、资源与边界

宏、unsafe、FFI、Web请求、数据库和宿主集成都应缩小不可信边界，在安全核心前完成解析与验证。 Cargo清单、锁定策略、测试、文档、基准、调试符号和最小复现共同让同一结论能在干净环境重复。 失败实验至少覆盖正常、空值或零长度、上限附近、显式错误和资源中断。涉及线程、网络、数据库、GUI或FFI时，还要加入超时、取消、部分完成与关闭路径。

不要把原书示例的依赖版本直接复制到新项目。先固定Rust edition与最小支持版本，再查询维护中的crate文档和迁移说明；保留原书要讲的架构边界，按当前API重写适配层。

```bash
cargo fmt --check
cargo clippy --all-targets -- -D warnings
cargo test --all-targets
cargo test --doc
```

## 机制三：可复现证据

<Term def="原书基于Rust 2018时代生态，复习时保留项目模型和安全不变量，具体crate API按当前维护版本与官方迁移文档更新。">迁移原则</Term>负责把本章收束成证据。原书基于Rust 2018时代生态，复习时保留项目模型和安全不变量，具体crate API按当前维护版本与官方迁移文档更新。 一次可靠验收要保存输入、工具链、依赖锁定、命令、退出状态、关键输出和失败样本。性能结论还要记录release构建、样本数与环境噪声；并发结论则要记录任务边界、超时和关闭结果。

<MrsFinalReviewEvidenceLab />

## 分步实验

<Stepper>
  <Step title="建立模型"><MrsFinalReviewModelLab /></Step>
  <Step title="注入边界"><MrsFinalReviewBoundaryLab /></Step>
  <Step title="保留证据"><MrsFinalReviewEvidenceLab /></Step>
</Stepper>

## 验收清单

1. 在隔离目录运行三段示例，先预测结果和所有权变化，再记录实际编译诊断。
2. 为主路径增加一个正常测试和至少三个边界测试，错误断言检查类别与上下文，而不只匹配整段文本。
3. 清理构建产物后重跑，确认没有依赖本地缓存、环境变量或未声明工具。
4. 若章节涉及外部系统，使用本地测试服务或临时数据库，设置时间预算并验证资源最终释放。

## 常见误区

<Callout type="trap" title="把编译通过当成章节验收">
编译器不能证明业务输入完整、超时合理、协议有背压或外部副作用已回滚。必须补充失败测试与可观察证据。
</Callout>

<Callout type="trap" title="把2019年的crate API当成固定标准">
本书核心结构值得保留，但框架和工具API会变化。新代码应锁定版本、查阅维护中的官方文档，并把迁移集中在适配边界。
</Callout>

## 本章回顾

本章按照官方目录逐项覆盖静态契约、运行期协议、边界最小化、可复现工程、迁移原则。核心方法是先用类型表达资源与能力，再用运行期协议约束超时、顺序和外部失败，最后用测试、日志或调试记录证明结果可重复。

<Exercises>
**问题 1：如何解释静态契约与运行期协议的协作？**
<Answer>所有权、trait bound、Result和Send或Sync把资源、能力、失败与线程边界编码进类型，先让非法组合无法通过编译。 超时、背压、锁顺序、事务、日志字段和优雅关闭无法只靠类型完成，必须用状态机、测试和运行证据验证。</Answer>

**问题 2：边界最小化和可复现工程的边界是什么？**
<Answer>宏、unsafe、FFI、Web请求、数据库和宿主集成都应缩小不可信边界，在安全核心前完成解析与验证。 Cargo清单、锁定策略、测试、文档、基准、调试符号和最小复现共同让同一结论能在干净环境重复。</Answer>

**问题 3：怎样验收迁移原则？**
<Answer>原书基于Rust 2018时代生态，复习时保留项目模型和安全不变量，具体crate API按当前维护版本与官方迁移文档更新。 使用正常、边界、错误和资源中断输入，保存工具链、命令、退出状态和可复查输出。</Answer>
</Exercises>

## 术语表

<Glossary>
  <GlossaryItem term="静态契约" definition="所有权、trait bound、Result和Send或Sync把资源、能力、失败与线程边界编码进类型，先让非法组合无法通过编译。" />
  <GlossaryItem term="运行期协议" definition="超时、背压、锁顺序、事务、日志字段和优雅关闭无法只靠类型完成，必须用状态机、测试和运行证据验证。" />
  <GlossaryItem term="边界最小化" definition="宏、unsafe、FFI、Web请求、数据库和宿主集成都应缩小不可信边界，在安全核心前完成解析与验证。" />
  <GlossaryItem term="可复现工程" definition="Cargo清单、锁定策略、测试、文档、基准、调试符号和最小复现共同让同一结论能在干净环境重复。" />
  <GlossaryItem term="迁移原则" definition="原书基于Rust 2018时代生态，复习时保留项目模型和安全不变量，具体crate API按当前维护版本与官方迁移文档更新。" />
  <GlossaryItem term="章节验收边界" definition="用输入、失败、资源、证据和迁移约束验证章节核心。" />
</Glossary>

<Attribution adaptedFrom="Mastering Rust, Second Edition · Mastering Rust Integration Review" adaptedUrl="https://www.packtpub.com/en-us/product/mastering-rust-9781789346572" />
