---
title: "跨卷导读：质数、互质、同余与费马大定理"
type: B
section: "数学女孩系列 · 跨卷导读"
order: 1
description: "以第2卷十章为主线，串联质数分解、勾股数组、互质、反证法、代数结构、模运算、无穷递降和费马大定理，并用可执行算法检验GCD、贝祖等式与模逆元。"
demo: true
math: true
sourceUrl: "https://www.hyuki.com/girl/"
draft: false
---

import { MglNumberTheoryDiagram } from "@/components/mdx/math-girl/diagrams/mgl-number-theory";


## 从第2卷开始：这不是独立的“数论基础章”

先预测：若只读质数、GCD、RSA三个知识点，是否已经覆盖《数学女孩2：费马大定理》？

没有。第2卷用十章搭建一条长路线：

1. 《将无限宇宙尽收掌心》建立无限与证明的视野；
2. 《勾股定理》进入整数边长直角三角形；
3. 《互质》约束勾股数组的共同因子；
4. 《反证法》把“不存在”改写成矛盾；
5. 《分裂的质数》观察质数在更大代数结构中的分解；
6. 《阿贝尔群的眼泪》引入运算封闭、单位元、逆元与交换性；
7. 《以发型为模》把整数投影到剩余类；
8. 《无穷递降法》从最小反例制造更小反例；
9. 《最美的数学公式》连接复数与欧拉公式；
10. 《费马大定理》回看工具、历史与现代证明。

本页是。它集中复习整数工具，却不能把第2卷十章压缩成一个“数论基础”进度。

<MglNumberTheoryDiagram />


## 质数与唯一分解

### 先分清1、质数与合数

不是“不能再分”的含糊说法，而是明确限制在大于1的正整数。可以写成两个更小正整数之积。数字1是，既不是质数也不是合数。

把整数乘法结构变成质因数指数结构。例如：

$$
60=2^2\cdot3\cdot5.
$$

唯一性很重要。若允许1成为质数，同一个数就能随意添上任意多个1，分解不再唯一。

### 质数为什么无穷

假设质数只有有限个：

$$
p_1,p_2,\ldots,p_n.
$$

构造：

$$
N=p_1p_2\cdots p_n+1.
$$

对任意`i`，都有`N`除以`pᵢ`余1，所以列表中的任何质数都不整除`N`。但`N>1`，根据算术基本定理，`N`本身是质数，或至少拥有质因数；这个质因数不在原列表中，产生矛盾。

注意证明不要求`N`一定是质数。例如：

$$
2\cdot3\cdot5\cdot7\cdot11\cdot13+1=30031=59\cdot509.
$$

关键是30031的质因数59和509都不在原有限列表里。的力量来自精确构造，不来自“乘积加一看起来像质数”。


## 整除、互质与欧几里得算法

### 最大公约数是一个不变量

若存在整数`q,r`使：

$$
a=qb+r,\qquad 0\le r<|b|,
$$

那么同时整除`a,b`的整数，也同时整除`b,r`；反向同样成立。因此两对数的公因数集合完全相同：

$$
\gcd(a,b)=\gcd(b,a\bmod b).
$$

这就是的循环不变量。第二参数每轮严格减小且保持非负，所以算法必然停止；停止在`b=0`时返回`|a|`。

以48和18为例：

$$
\begin{aligned}
48&=2\cdot18+12,\\
18&=1\cdot12+6,\\
12&=2\cdot6+0.
\end{aligned}
$$

故`gcd(48,18)=6`。

### 从回代到贝祖等式

可以由辗转相除逆向回代得到。对48与18：

$$
6=18-12=18-(48-2\cdot18)=3\cdot18-48.
$$

因此一组贝祖系数是`x=-1,y=3`。

若`gcd(a,m)=1`，贝祖等式给出：

$$
ax+my=1.
$$

对模`m`取同余：

$$
ax\equiv1\pmod m.
$$

此时`x`是。不是“两个数都是质数”；例如8与15都是合数，但互质。

```ts
export function gcd(a: bigint, b: bigint): bigint {
  a = a < 0n ? -a : a;
  b = b < 0n ? -b : b;

  while (b !== 0n) {
    [a, b] = [b, a % b];
  }

  return a;
}

export function extendedGcd(
  a: bigint,
  b: bigint,
): { gcd: bigint; x: bigint; y: bigint } {
  if (b === 0n) {
    return { gcd: a, x: 1n, y: 0n };
  }

  const next = extendedGcd(b, a % b);
  return {
    gcd: next.gcd,
    x: next.y,
    y: next.x - (a / b) * next.y,
  };
}
```

代码验收不能只看返回的GCD。还要检查：

$$
a\cdot x+b\cdot y=\gcd(a,b).
$$


## 同余与有限结构

### 同余不是“只剩一个余数”

是整数间的等价关系。它把所有整数分成`m`个，并允许加法与乘法在类上良好定义：

$$
\begin{aligned}
a\equiv b\pmod m,\ c\equiv d\pmod m
&\Rightarrow a+c\equiv b+d\pmod m,\\
&\Rightarrow ac\equiv bd\pmod m.
\end{aligned}
$$

第2卷第7章用“发型”为模建立直觉，第6章则让读者看到。可逆剩余类在乘法下形成群，这正是欧拉定理和RSA教学模型使用的结构。

### 快速模幂

直接计算`aᵉ`再取模会产生巨大中间数。每轮将指数折半：

```ts
export function modPow(
  base: bigint,
  exponent: bigint,
  modulus: bigint,
): bigint {
  if (modulus <= 0n || exponent < 0n) {
    throw new RangeError("modulus must be positive and exponent non-negative");
  }

  let result = 1n % modulus;
  base %= modulus;

  while (exponent > 0n) {
    if (exponent % 2n === 1n) result = (result * base) % modulus;
    base = (base * base) % modulus;
    exponent /= 2n;
  }

  return result;
}
```

它只需`O(log e)`轮，并在每轮将中间值限制在模数范围内。

## RSA教学模型与安全边界

### 代数链条

在简化教学模型中，选择不同质数`p,q`，令`n=pq`，并计算欧拉函数：

$$
\varphi(n)=(p-1)(q-1).
$$

选择`e`满足`gcd(e,φ(n))=1`，再求`d`使：

$$
ed\equiv1\pmod{\varphi(n)}.
$$

模幂把消息代表元`m`变成`c≡mᵉ (mod n)`，再用`d`恢复。这里复用了唯一分解、互质、贝祖等式、模逆元和快速模幂。

### 不能越过的结论

必须与真实密码系统分开：

- 分解`n`会暴露`p,q`，进而计算私钥参数；
- 但不能因此声称“破解一般RSA已经证明与整数分解完全等价”；
- 裸RSA是确定性的，不提供现代协议需要的语义安全；
- 真实加密或签名需要标准填充、足够密钥长度、侧信道防护和成熟密码库；
- 不能把课堂代码直接用于密钥、身份或资金安全。


## 回到原书：十章各自不能被替代

数论工具在第2卷中的价值来自叙事和证明链，而不是术语数量：

- 勾股定理章把几何关系转成整数方程；
- 互质章通过约去共同因子得到本原结构；
- 反证法章训练假设、构造与矛盾的方向；
- 分裂质数和阿贝尔群章说明“质数”与“分解”依赖所处代数结构；
- 模运算章把无限整数组织成有限剩余类；
- 无穷递降章把最小性变成证明发动机；
- 欧拉公式章引入复数表示；
- 费马大定理章把古典问题与现代证明连接起来。

因此本导读完成后，应回到对应原章做章节练习。、和都不能只靠RSA示例掌握。

## 本章回顾：从整除关系到可执行证明

1. 本页是第2卷相关概念的跨卷导读，不替代十个原书章节。
2. 质数大于1，数字1是乘法单位元，不是质数。
3. 算术基本定理保证大于1整数的质因数分解存在且唯一。
4. 质数无穷证明构造乘积加一，只需产生列表外质因子。
5. 欧几里得算法保持公因数集合不变，余数严格下降保证终止。
6. 贝祖等式由辗转相除逆向回代得到。
7. 模逆元存在当且仅当对应整数与模数互质。
8. 同余把整数划分成剩余类，加法和乘法可传递到类上。
9. 模除法只有除数可逆时才合法。
10. 平方—乘算法用`O(log e)`轮计算模幂。
11. RSA教学模型复用互质、模逆元和模幂，但不等于生产密码系统。
12. 分解模数可恢复私钥参数，不代表一般RSA破解已证明等价于分解。
13. 第2卷还包含勾股数组、代数结构、无穷递降和复数，必须回到原章学习。


