---
title: "跨卷导读：概率、期望与随机过程"
type: B
section: "数学女孩系列 · 跨卷导读"
order: 6
description: "以第4卷十章为主线，连接概率空间、条件概率、贝叶斯公式、独立与互斥、随机变量、期望线性、方差、随机漫步、马尔可夫链和随机算法保证。"
demo: true
math: true
sourceUrl: "https://www.hyuki.com/girl/"
draft: false
---

import { MglProbabilityDiagram } from "@/components/mdx/math-girl/diagrams/mgl-probability";


## 从第4卷开始：随机不等于含糊

先预测：随机算法既然每次结果可能不同，数学还能给出严格结论吗？

可以。严格性来自先定义、与，再明确随机变量、期望和失败事件。

本页是。第4卷原始路线是：

1. 赌博问题暴露直觉偏差；
2. 线性搜索建立算法逐行计数；
3. 指数规模说明穷举边界；
4. 概率公理定义不确定性；
5. 期望与线性法则汇总随机代价；
6. 渐近阶比较运行时间；
7. 矩阵表示状态变换；
8. 随机漫步与马尔可夫链分析长期行为；
9. 随机3-SAT量化成功率和疏忽概率；
10. 随机快速排序对任意输入给出期望上界。

<MglProbabilityDiagram />

## 概率空间与三条公理

写作`(Ω,F,P)`。概率函数满足：

$$
P(A)\ge0,\qquad P(\Omega)=1,
$$

以及对两两互斥事件`A₁,A₂,...`：

$$
P\left(\bigcup_{i=1}^{\infty}A_i\right)
=\sum_{i=1}^{\infty}P(A_i).
$$

这是。由公理可推出`P(∅)=0`、补事件公式和一般加法公式：

$$
P(A\cup B)=P(A)+P(B)-P(A\cap B).
$$

### 有限计数模型的隐藏前提

只有有限结果**等可能**时，才能写：

$$
P(A)=\frac{|A|}{|\Omega|}.
$$

若硬币偏置、路径每步概率不同或抽样机制不均匀，只数结果个数会错。组合计数负责“有多少结果”，概率模型还要负责“每个结果多重”。

## 互斥与独立不能互换

描述共存可能性。若`A∩B=∅`：

$$
P(A\cup B)=P(A)+P(B).
$$

描述信息关系：

$$
P(A\cap B)=P(A)P(B).
$$

两个正概率互斥事件不可能独立，因为左边为0，右边大于0。独立事件反而可以同时发生。第4卷多次提醒：互斥事件的“或”用加法，独立事件连续发生的“且”用乘法。


## 条件概率与贝叶斯公式

当`P(B)>0`，定义为：

$$
P(A\mid B)=\frac{P(A\cap B)}{P(B)}.
$$

由：

$$
P(A\cap B)=P(B\mid A)P(A)
$$

得到：

$$
P(A\mid B)
=\frac{P(B\mid A)P(A)}{P(B)}.
$$

### 用人数树校正检测例子

这只是概率演算示例，不是医疗建议。假设：

- 先验患病率1%；
- 灵敏度`P(阳性|患病)=99%`；
- 假阳性率`P(阳性|未患病)=1%`。

取10000人作可视化基数：100人患病，其中约99人阳性；9900人未患病，其中约99人假阳性。因此阳性总数约198，后验为：

$$
P(\text{患病}\mid\text{阳性})
=\frac{99}{99+99}
=\frac12.
$$

旧说法“检测准确率99%”不够，因为、和总体准确率不是同一个量。后验还依赖。


## 随机变量、期望与方差

`X:Ω→R`把复杂结果变成可计算数值。离散随机变量的：

$$
\mathbb E[X]
=\sum_x x\Pr(X=x).
$$

期望不必是可能取值。公平骰子的期望是3.5，但一次不会掷出3.5。

### 期望线性不需要独立

若期望存在：

$$
\mathbb E[aX+bY]
=a\mathbb E[X]+b\mathbb E[Y],
$$

无论`X,Y`是否独立。支撑第4卷优惠券问题和随机快排比较次数。

`I_A`满足：

$$
\mathbb E[I_A]=P(A).
$$

总计数写成指示器之和后，期望就变成概率之和。

### 方差与协方差

：

$$
\operatorname{Var}(X)
=\mathbb E[(X-\mathbb E[X])^2]
=\mathbb E[X^2]-\mathbb E[X]^2.
$$

一般有：

$$
\operatorname{Var}(X+Y)
=\operatorname{Var}(X)+\operatorname{Var}(Y)
+2\operatorname{Cov}(X,Y).
$$

不能随意丢掉。独立可推出协方差为0，但期望线性从来不要求独立。


## 随机漫步与马尔可夫链

用路径表示一系列状态。路径概率不是只数路径条数，而是每一步转移概率的乘积；多个互斥路径到达同一终点时再求和。

用转移矩阵`P`表示一步概率：

$$
\mathbf p_{n+1}=P\mathbf p_n.
$$

第4卷二状态随机漫步通过矩阵幂、特征值和特征向量求第`n`年分布与长期极限。马尔可夫性质是模型假设，不能只因用了转移矩阵就自动成立。

## 蒙特卡洛模拟与复现

用样本均值估计期望。圆内随机点比例估计π只是一个例子。可靠实验必须记录：

- 随机数生成器与种子；
- 样本分布和抽样方法；
- 样本数；
- 估计值、标准误或置信区间；
- 独立重复策略。

```ts
type RandomSource = () => number;

export function estimatePi(samples: number, random: RandomSource): number {
  if (!Number.isInteger(samples) || samples <= 0) {
    throw new RangeError("samples must be a positive integer");
  }

  let inside = 0;
  for (let index = 0; index < samples; index += 1) {
    const x = random();
    const y = random();
    if (x * x + y * y <= 1) inside += 1;
  }

  return (4 * inside) / samples;
}
```

注入随机源让测试可以使用固定伪随机序列，也让生产调用显式拥有随机性。不等于样本独立，但它是排查实现错误的基础。

## 输入概率分析与随机算法分析

普通快排的“平均运行步数”先假设输入排列均匀；这是。随机快排则固定任意输入，由算法内部均匀选枢纽；这是。

以随机快排为代表。把不确定性放在正确性上。第4卷随机3-SAT是单侧错误：找到满足分配时可直接验证，失败退出却可能只是漏找。


## 本章回顾：概率结论必须带概率空间

1. 样本空间列出全部结果，事件是结果集合。
2. 概率满足非负、规范和可列可加三条公理。
3. 有利数除以总数只适用于有限等可能模型。
4. 互斥描述不能同时发生，独立描述信息不改变概率。
5. 条件概率把样本空间限制到已知事件。
6. 贝叶斯公式用先验、似然和证据交换条件方向。
7. 灵敏度、特异度、假阳性率与后验不是同一个量。
8. 随机变量是样本空间到数值的函数。
9. 期望线性不要求独立，方差相加必须处理协方差。
10. 指示器期望等于事件概率，适合计数。
11. 随机漫步路径概率由转移概率相乘并对互斥路径求和。
12. 马尔可夫性质是只依赖当前状态的模型假设。
13. 蒙特卡洛实验应记录随机源、种子、样本数与误差证据。
14. 输入概率分析与随机算法分析使用不同概率空间。
15. 本页是跨卷导读，不替代第4卷原章。


