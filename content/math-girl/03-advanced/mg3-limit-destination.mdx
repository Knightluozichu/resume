---
title: "第3卷 第4章 无限接近的目的地"
type: B
section: "数学女孩第3卷 · 哥德尔不完备定理"
order: 4
description: "从0.999...是否等于1的争论区分有限项与极限，以等比部分和推导循环小数，并用误差与门槛预告数列极限的严格定义。"
demo: true
math: true
sourceUrl: "https://www.hyuki.com/girl/goedel/"
draft: true
---

import { Objectives, Callout, Attribution, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step } from "@/components/mdx/mdx-components";
import { Mg3NineSequenceLab, Mg3LimitNotationLab, Mg3GeometricSumLab, Mg3RepresentationLab } from "@/components/mdx/math-girl/diagrams/mg3-limit-destination";

<Objectives>

- 能计算由n个9组成的有限小数数列，证明每个有限项小于1，同时用误差解释其极限等于1
- 能从有限等比和推导通项，严格定义循环小数0.999...并审查常见代数证明所依赖的前提
- 能读写数列极限的记号和量词骨架，区分收敛到有限实数与发散到正无穷，并用误差门槛完成证明

</Objectives>

## 从0.999...是否等于1开始

第3卷第4章从家中的一道争论题开始：

$$
0.999\ldots=1\ ?
$$

先预测：每个写得完的`0.9`、`0.99`、`0.999`都小于1，为什么无限循环小数却能等于1？省略号是在表示“还会再来一项”，还是已经定义了一个数？“永远到不了”和“目的地是什么”究竟是不是同一个问题？

原章依次经过家中、超市、音乐教室和归途。尤里的犹豫不是算术粗心，而是准确抓住了无限过程中最容易混淆的两类对象：

1. 过程中的每个有限阶段；
2. 整个过程所趋近的固定目的地。

把这两个对象分开，矛盾感才会变成极限概念。

## 家中：两份证明与一个疑惑

### 男生的“证明”

第一种常见论证从：

$$
1=1
$$

出发，两边除以3：

$$
\frac13=0.333\ldots,
$$

再乘以3：

$$
1=0.999\ldots.
$$

结论正确，但论证把最需要说明的事藏在`\frac13=0.333\ldots`里：循环小数究竟如何被定义？若尚未说明省略号代表什么，等式只是把问题移动到另一个循环小数。

另一种写法令：

$$
x=0.999\ldots.
$$

于是：

$$
10x=9.999\ldots,
$$

相减得到：

$$
9x=9,\qquad x=1.
$$

这同样可以成为严格证明，但要先知道`x`是收敛无穷级数的值，并证明乘10与相减保留该极限。代数步骤没有错，缺少的是对象定义和操作许可。

<Callout type="trap" title="会移位相减就已经定义了循环小数">
形式运算只有在对象存在且运算合法时才构成证明。先把循环小数定义为有限部分和数列的极限，才能解释为什么整体乘10、相减和取极限不会改变论证含义。
</Callout>

### 尤里的“证明”

尤里从差值观察：

$$
1-0.9=0.1,
$$

$$
1-0.99=0.01,
$$

$$
1-0.999=0.001.
$$

若右侧的小数位一直向后移动，差就会任意小。她把最终差想成`0.000...`，于是判断它等于0。这份直觉已经接近正确的极限证明：关键不是出现一个“最后的全零小数”，而是对任何预先给定的正误差，都能让差比它更小。

### 尤里的“疑惑”

尤里随即从另一面反驳自己。所有有限截断都满足：

$$
0.9\lt1,\qquad
0.99\lt1,\qquad
0.999\lt1.
$$

无论写多少个9，只要数量有限，末尾都还留下一个正差。因此她问：“每一步都没有到1，怎么会突然等于1？”

这个问题里藏着一个并不存在的对象：所有有限项之后的“下一项”或“最后一项”。自然数没有最大值，所以数列也没有完成全部有限项后再出现的那一项。极限不是额外追加的项，而是描述整列数共同趋势的数。

## 我的讲解：给每个有限阶段编号

定义<Term def="按照正整数下标依次排列的一列数，每个下标对应唯一一项。">数列</Term>`(a_n)`：

$$
a_1=0.9,\quad
a_2=0.99,\quad
a_3=0.999,\quad\ldots
$$

第`n`项恰好含有`n`个9。它的精确表达式是：

$$
a_n=1-\frac1{10^n}.
$$

因此对每个有限正整数`n`：

$$
1-a_n=\frac1{10^n}\gt0,
$$

所以：

$$
a_n\lt1.
$$

这证明了尤里的有限阶段观察完全正确。另一方面，随着`n`增大：

$$
|a_n-1|=\frac1{10^n}\longrightarrow0.
$$

所以：

$$
\lim_{n\to\infty}a_n=1.
$$

两条结论可以同时成立：

1. 对每个有限`n`，`a_n\lt1`；
2. 数列`(a_n)`的极限是1。

第一条用全称量词谈每个<Term def="数列中由某个有限下标指定的具体数。">有限项</Term>；第二条谈整列数的<Term def="数列各项最终可以任意接近的固定数值。">极限值</Term>。它们的主语不同，不构成矛盾。

<Mg3NineSequenceLab />

<Stepper>
  <Step title="切换有限项、正差、距离归零与目的地"><Mg3NineSequenceLab /></Step>
  <Step title="制作Nine Table">列出n等于1、2、3、6时的a_n与1-a_n，把“项的大小”和“到1的距离”分成两列。</Step>
  <Step title="执行Subject Check">遇到“它小于1”或“它等于1”时，先圈出主语究竟是有限项a_n、整列数列，还是数列的极限值。</Step>
</Stepper>

<Callout type="trap" title="极限是所有项之后出现的最后一项">
数列只按正整数编号，不存在“无穷号项”。极限可以不等于任何一项；它是整列数的长期行为，不是过程结束后补上的成员。
</Callout>

## 超市：把项与目的地分开命名

在超市的对话中，尤里继续追问：既然没有任何有限项等于1，等于1的究竟是谁？

答案是数列收敛到一个数。`0.999...`中的省略号位于末尾时，表示所有有限部分和所决定的极限；而：

$$
0.\underbrace{99\ldots9}_{n\text{个9}}
$$

表示某个有限项。两种记号看起来只差省略号的位置，语义却不同：

$$
0.\underbrace{99\ldots9}_{n\text{个9}}
=a_n\lt1,
$$

$$
0.999\ldots:
=\lim_{n\to\infty}a_n
=1.
$$

尤里提议给目的地画一个爱心，免得它和路上的项混在一起。数学采用字母`A`承担同样工作：

$$
A=\lim_{n\to\infty}a_n.
$$

`A`是固定数，不随着`n`移动。移动的是项`a_n`，变化的是它们到`A`的距离。

<Callout type="trap" title="既然a_n不断变化，极限A也应跟着变化">
极限值是用来衡量各项距离的固定参照。若把A也写成随n变化的量，就换成了另一个数列，原来的收敛命题也随之改变。
</Callout>

## 音乐教室：字母、下标与极限

米尔嘉在音乐教室把尤里的爱心换成标准记号：

$$
\lim_{n\to\infty}a_n=A.
$$

也可以把趋势并排写成：

$$
n\to\infty
\quad\Longrightarrow\quad
a_n\to A.
$$

这里`n\to\infty`不是说`n`在某刻取到无穷大，而是考察任意大的有限下标。若存在有限实数`A`使各项最终任意靠近它，就说数列<Term def="存在有限实数A，使数列各项到A的距离最终能小于任意给定正误差。">收敛</Term>到`A`；否则在实数意义下<Term def="不存在有限实数极限的数列行为。">发散</Term>。

例如：

$$
b_n=10^n
$$

会写作：

$$
10^n\to+\infty.
$$

这表示对每个给定上界`M`，最终都有`10^n\gt M`。正无穷不是一个有限实数，不能把它当作普通极限值代入距离公式。它描述无界增长方向，而不是数轴终点。

### 把“无限接近”拆成可检查条件

数列极限的严格量词骨架是：

$$
\forall\varepsilon\gt0,\quad
\exists N\in\mathbb N,\quad
\forall n\ge N,\quad
|a_n-A|\lt\varepsilon.
$$

<Term def="预先任意给定的正距离容许量，用来衡量数列项与极限值的接近程度。">误差epsilon</Term>由挑战者先选；证明者再找<Term def="使所有后续项都进入误差范围的有限下标界限。">门槛`N`</Term>。量词顺序不能反过来，因为一个预先固定的`N`无法应付所有可能的小误差。

对`a_n=1-10^{-n}`，给定`\varepsilon\gt0`，选择满足：

$$
10^{-N}\lt\varepsilon
$$

的自然数`N`。当`n\ge N`时：

$$
|a_n-1|
=10^{-n}
\le10^{-N}
\lt\varepsilon.
$$

所以极限确实是1。这里没有使用“最后一项”，只有任意误差和一个有限门槛。

<Mg3LimitNotationLab />

<Stepper>
  <Step title="切换通项、有限极限、无界增长与量词骨架"><Mg3LimitNotationLab /></Step>
  <Step title="制作Epsilon Challenge">任选0.1、0.001和10的负六次方作为误差，分别找N并验证从N起所有项都在1的误差带内。</Step>
  <Step title="执行Quantifier Read">按“任意误差、存在门槛、所有后项”的顺序朗读公式；交换任意两个量词，再构造例子说明新命题为何变弱或错误。</Step>
</Stepper>

## 极限的计算：先求有限和

循环小数的每个有限截断都是<Term def="把一个数列的前n项相加所得的有限和。">部分和</Term>：

$$
s_n=\sum_{k=1}^{n}\frac9{10^k}
=\frac9{10}+\frac9{10^2}
+\cdots+\frac9{10^n}.
$$

先只处理有限项。整体乘10：

$$
10s_n
=9+\frac9{10}+\frac9{10^2}
+\cdots+\frac9{10^{n-1}}.
$$

两式相减，中间项逐一消去：

$$
10s_n-s_n
=9-\frac9{10^n}.
$$

所以：

$$
9s_n=9\left(1-\frac1{10^n}\right),
$$

$$
s_n=1-\frac1{10^n}.
$$

这就是<Term def="相邻项具有固定比值的数列或由其项组成的有限和。">等比数列</Term>有限求和公式在公比`1/10`时的具体推导。现在才取极限：

$$
\lim_{n\to\infty}s_n
=\lim_{n\to\infty}
\left(1-\frac1{10^n}\right)
=1.
$$

于是把<Term def="由有限小数部分和数列的极限所定义的无限循环十进制表示。">循环小数</Term>严格定义为：

$$
0.999\ldots:
=
\lim_{n\to\infty}
\sum_{k=1}^{n}\frac9{10^k}
=1.
$$

这不是“无限多个近似数忽然跳到1”，而是实数1拥有两种十进制表示。一般地，每个有限小数都还有一个以无限多个9结尾的表示，例如：

$$
0.25000\ldots=0.24999\ldots.
$$

十进制表示并非总是唯一，但它们代表的实数可以完全相同。

<Mg3GeometricSumLab />

<Stepper>
  <Step title="切换部分和、乘10平移、相减与循环小数定义"><Mg3GeometricSumLab /></Step>
  <Step title="制作Finite First Proof">只对含n项的s_n做乘10与相减，逐项标出被消去的项，最后才让n趋向无穷。</Step>
  <Step title="验证Decimal Twin">把0.24999...写成1/5加1/20再加从千分之9开始的等比级数，求和后核对它等于0.25。</Step>
</Stepper>

## 凭声音决定音乐：表示为什么重要

音乐可以作为流动的声音被体验，也可以写成乐谱供人反复定位、比较和演奏。原章借这个场景讨论数学表示：自然语言“不断接近”“永远不到”能唤起直觉，却没有自动规定量词、误差和对象身份；公式把这些关系固定下来。

尤里的爱心很有价值，因为它主动分开了路上的项和目的地。标准极限记号进一步完成三件事：

1. `a_n`指明被观察的是哪一列数；
2. `n\to\infty`指明考察越来越大的有限下标；
3. `A`指明固定候选极限，并允许用距离检验。

“不要依赖感觉，要依赖逻辑；不要只依赖自然语言，要依赖公式”并不是要求抛弃直觉。感觉负责提出问题和猜测目的地，公式负责暴露前提并让别人复查。

<Mg3RepresentationLab />

<Stepper>
  <Step title="切换尤里直觉、爱心记号、声音乐谱与严格定义"><Mg3RepresentationLab /></Step>
  <Step title="制作Representation Ladder">把“越来越近”依次改写成距离趋零、误差带、量词公式，并标出每一步消除了哪一种歧义。</Step>
  <Step title="执行Proof License">审查移位相减证明：逐条写出循环小数定义、有限求和、极限存在和线性运算许可，区分直观捷径与完整论证。</Step>
</Stepper>

## 归途：目的地与下一章的门

章末在归途中回到标题。泰朵拉谈自己的未来：目的地并不等于脚下任何一步，但没有每一步，也不会形成朝向目的地的轨迹。这个比喻对应数列，却不能替代定义。

Cauchy系统发展了极限分析，Weierstrass进一步以误差和量词消除“无穷小量”语言中的含混。对数列，严格形式是本章预告的`\varepsilon`与`N`；对函数，下一阶段会把它扩展成`\varepsilon`与`\delta`。本章最重要的学习动作是：当“无限接近”听起来有道理时，继续问距离是什么、谁先给误差、门槛依赖什么、门槛之后是否所有项都满足。

## 本章回顾：路上的项不等于目的地

1. `0.999...=1`的难点不是普通减法，而是循环小数与省略号的定义。
2. 除以3或移位相减的证明结论正确，但严格性依赖收敛级数与合法极限运算。
3. 含n个9的有限小数满足`a_n=1-10^{-n}`。
4. 对每个有限`n`都有`a_n\lt1`，因为正差为`10^{-n}`。
5. 同时`\lim a_n=1`，因为距离`|a_n-1|=10^{-n}`可以任意小。
6. 极限不是最后一项，也不是无穷号项，而是描述整列数长期行为的固定数。
7. `0.999...9`表示有限项，末尾省略的`0.999...`表示部分和数列的极限。
8. 实数收敛要求存在有限实数极限；`10^n\to+\infty`描述无界增长，不把正无穷当成实数。
9. 严格极限按“任意误差、存在门槛、所有后项”的量词顺序陈述。
10. 有限等比和先给出`s_n=1-10^{-n}`，再取极限得到`0.999...=1`。
11. 有限小数可同时拥有终止表示和以无限个9结尾的表示。
12. 直觉帮助预测，公式通过对象、距离和量词使预测可验证。

<Exercises>

**问题 1：设`a_n=0.99...9`且共有n个9。证明每个a_n小于1，但数列极限为1。**

<Answer>有限等比和给出a_n=1-10的负n次方。对每个有限n，10的负n次方为正，所以a_n小于1。给定任意正误差epsilon，取N使10的负N次方小于epsilon；当n不小于N时，|a_n-1|=10的负n次方不大于10的负N次方并小于epsilon，因此极限为1。</Answer>

**问题 2：指出“极限是数列走完所有项后到达的最后一项”错在哪里。**

<Answer>数列以自然数为下标，而自然数没有最大值，所以不存在走完所有有限下标后的最后一项，也没有无穷号项。极限是一个固定数，用来描述各项最终进入任意小误差带的性质；它甚至可以从未成为数列中的某一项。</Answer>

**问题 3：严格推导0.24999...=0.25。**

<Answer>令s_n=0.24再加从小数第三位开始的n个9，即s_n=0.24加上从k=3到n+2的9乘10的负k次方。后半段有限等比和为0.01乘(1-10的负n次方)，所以s_n=0.25-10的负(n+2)次方。令n趋于无穷，余量趋于0，故极限为0.25。</Answer>

**问题 4：写出`10^n\to+\infty`的量词含义，并说明它为何不同于收敛到有限实数。**

<Answer>它表示对任意给定实数M，都存在自然数N，使所有n不小于N时有10的n次方大于M。该命题要求最终超过任意界，而有限极限A要求最终落入A周围任意小的有界误差带；正无穷不是可代入|a_n-A|的实数A。</Answer>

</Exercises>

## 术语表

<Glossary>
  <GlossaryItem term="数列" definition="按照正整数下标依次排列的一列数，每个下标对应唯一一项。" />
  <GlossaryItem term="有限项" definition="数列中由某个有限下标指定的具体数。" />
  <GlossaryItem term="极限值" definition="数列各项最终可以任意接近的固定数值。" />
  <GlossaryItem term="收敛" definition="存在有限实数A，使数列各项到A的距离最终能小于任意给定正误差。" />
  <GlossaryItem term="发散" definition="不存在有限实数极限的数列行为。" />
  <GlossaryItem term="误差epsilon" definition="预先任意给定的正距离容许量，用来衡量数列项与极限值的接近程度。" />
  <GlossaryItem term="门槛N" definition="使所有后续项都进入误差范围的有限下标界限。" />
  <GlossaryItem term="部分和" definition="把一个数列的前n项相加所得的有限和。" />
  <GlossaryItem term="等比数列" definition="相邻项具有固定比值的数列或由其项组成的有限和。" />
  <GlossaryItem term="循环小数" definition="由有限小数部分和数列的极限所定义的无限循环十进制表示。" />
</Glossary>

<Attribution adaptedFrom="数学女孩3：哥德尔不完备定理（中文ISBN 9787115469915），第4章 无限接近的目的地" adaptedUrl="https://www.hyuki.com/girl/goedel/" />
