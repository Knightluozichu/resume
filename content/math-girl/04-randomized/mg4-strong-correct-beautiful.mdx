---
title: "第4卷 第9章 坚强、正直、美丽"
type: B
section: "数学女孩第4卷 · 随机算法"
order: 9
description: "从强正美柔逻辑题进入SAT、3-CNF、P与NP，再完整分析RANDOM-WALK-3-SAT：用汉明距离、单侧错误、二项式定理、钢琴路径和斯特林公式把指数底从2降到1.334。"
demo: true
math: true
sourceUrl: "https://www.hyuki.com/girl/random.html"
draft: false
---

import { Objectives, Callout, Attribution, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step } from "@/components/mdx/mdx-components";
import { Mg4StrongCorrectLogicLab, Mg4SatVocabularyLab, Mg4RandomWalk3SatLab, Mg4SchoningAnalysisLab } from "@/components/mdx/math-girl/diagrams/mg4-strong-correct-beautiful";

<Objectives>

- 能解释变量、字面量、子句、CNF、3-CNF与满足分配，并用真值表证明强正美柔公式不可满足
- 能区分P、NP与NP完全，分析暴力3-SAT的`2ⁿ`规模以及寻找和验证的差异
- 能描述RANDOM-WALK-3-SAT的重启、局部翻转与单侧错误，并用汉明距离证明一步接近概率至少为`1/3`
- 能推导单轮成功率的`(2/3)ⁿ`粗界与`(3/4)ⁿ/√n`精确界，解释指数底从2降到1.5再到1.334

</Objectives>

## 从八个条件开始：为什么“看起来简单”仍可能极难

本章从四个日常形容词开始，却通向计算机科学最著名的未解决问题之一。局部上，一个三字面量子句只需让其中一项为真；整体上，许多子句互相牵制，找到同时满足它们的分配可能需要探索指数规模的空间。

先预测：

1. 四个真假变量只有16种分配，为什么把变量数扩大到34就会出现171亿7986万9184种？
2. 从一个不满足的三字面量子句随机翻转变量，为什么至少有`1/3`概率更接近某个正确分配？
3. 算法返回“可以满足”为什么绝不会错，返回“大概无法满足”却可能错？
4. 只计“笔直走向答案”得到`1.5ⁿ`，把绕路也算进去为什么反而能把上界改进到`1.334ⁿ`？

## 9.1 家

### 雨天的周六

又一个雨天周六，尤里却与上一章相反，兴高采烈地来到房间。原因藏在她带来的信封里，但她先递出一道“强正美柔问题”。

令四个<Term def="只能取真或假两种值、用于表示命题是否成立的变量。">布尔变量</Term>分别表示：

$$
S=\text{坚强},\quad
R=\text{正直},\quad
B=\text{美丽},\quad
G=\text{温柔}.
$$

八个条件是：

$$
\begin{aligned}
P_1&=S\lor R\lor B,\\
P_2&=G\lor R\lor\neg B,\\
P_3&=\neg S\lor G\lor B,\\
P_4&=\neg S\lor\neg G\lor R,\\
P_5&=\neg G\lor\neg R\lor B,\\
P_6&=\neg S\lor\neg R\lor\neg B,\\
P_7&=S\lor\neg G\lor\neg B,\\
P_8&=S\lor G\lor\neg R.
\end{aligned}
$$

问题是：是否存在一组`S、R、B、G`的真假值，使八个条件同时为真？

“我”先尝试逻辑推导。假定坚强，再由不同条件推出温柔、正直、美丽，最后却与`P₆`冲突；改从不坚强出发，也会在另一条件卡住。但试过几条推理路线都失败，仍不能证明不存在答案。

四个变量共有：

$$
2^4=16
$$

种<Term def="为公式中每个变量指定真或假的完整对应关系。">分配方式</Term>。把16行全部列出并逐一检查`P₁`到`P₈`，每一行都恰有一个条件为假。因此：

$$
P_1\land P_2\land\cdots\land P_8
$$

不可满足。更精巧的是，删除任意一个`Pᵢ`，都存在一行满足剩余七项；八个条件中没有冗余。

<Mg4StrongCorrectLogicLab />

<Stepper>
  <Step title="切换四变量、八条件、真值表与极小结构"><Mg4StrongCorrectLogicLab /></Step>
  <Step title="制作Sixteen-Assignment Table">按0000至1111枚举四变量分配，逐列计算P1至P8，圈出每行唯一为假的条件。</Step>
  <Step title="执行Remove-One-Clause Test">依次删除一个Pi，寻找原来只违反该项的分配，验证其余七项立刻可同时满足。</Step>
</Stepper>

<Callout type="trap" title="尝试几个分配都失败，已经足以证明无解">
有限例子只能支持猜想。这里能证明无解，是因为真值表覆盖了四变量的全部16种分配；漏掉一行就不能排除那一行恰好满足所有条件。
</Callout>

题目并非尤里原创，而是转学男生在回信中出的。她抱怨对方回信太慢、内容还是数学题，脸上却藏不住高兴。上一章等待回信的随机摇摆，在这一章以新的数学对话落地。

## 9.2 图书室

### 9.2.1 逻辑题

放学后，泰朵拉听说尤里收到回信，比当事人还高兴。她相信语言可以让心意互通。米尔嘉与理纱随后来到图书室，“我”把强正美柔问题讲给大家听。

米尔嘉没有把它只看成16行小题，而是立刻认出背后的可满足性问题：日常形容词只是变量名字，真正结构是“能否为变量赋值，使整个逻辑公式为真”。

### 9.2.2 可满足性问题

<Term def="给定布尔公式，判断是否存在使整个公式为真的变量分配的问题。">可满足性问题</Term>英文是`Satisfiability Problem`。直接枚举当然总会结束，但“能算完”不等于“高效”。

若公式有`n`个变量，全部分配有：

$$
2^n
$$

种。所谓高效，在这里指运行步数可由`n`的某个固定次幂控制，也就是：

$$
O(n^K)
$$

而不是指数阶`O(2ⁿ)`。变量数从4增到34时：

$$
2^{34}=17\,179\,869\,184.
$$

这正是第3章标题里的171亿7986万9184。有限并没有阻止指数爆炸。

### 9.2.3 3-SAT

`Satisfiability`缩写为<Term def="判断一般布尔公式是否存在满足分配的可满足性问题。">SAT</Term>。先分层建立术语：

1. `x₁、x₂、...`是变量；
2. `¬`是<Term def="把真变假、把假变真的逻辑运算。">非运算</Term>；
3. 变量`x`或否定变量`¬x`叫<Term def="布尔变量或它的否定，是组成子句的基本元素。">字面量</Term>；
4. 用“或”连接若干字面量得到<Term def="由多个字面量用逻辑或连接而成的公式，至少一个字面量为真时整体为真。">子句</Term>；
5. 用“且”连接多个子句得到<Term def="多个子句用逻辑且连接形成的合取范式。">CNF</Term>；
6. 每个子句恰有三个字面量的CNF叫<Term def="每个子句恰好包含三个字面量的合取范式。">3-CNF</Term>。

例如：

$$
(x_1\lor\neg x_2\lor\neg x_3)
\land
(x_2\lor x_3\lor\neg x_4)
$$

是3-CNF。判断一个3-CNF是否存在满足分配的问题叫<Term def="判断给定3-CNF是否存在满足分配的可满足性问题。">3-SAT</Term>。

### 9.2.4 满足

当分配`a`使公式`f`为真，就说`a`<Term def="在给定分配下，变量、字面量、子句或整个公式取真。">满足</Term>`f`。3-CNF整体为真，当且仅当每个子句都为真；只要有一个子句为假，整体就为假。

这一区分非常重要：

- “公式可满足”表示至少存在一个满足分配；
- “这个分配满足公式”表示给定分配验证为真；
- “公式不可满足”表示所有分配都失败。

强正美柔问题就是由8个三字面量子句组成的3-CNF不可满足性问题。

### 9.2.5 分配方式的练习

短公式可以凭观察找出满足分配，但算法必须处理任意长度输入。最直接的<Term def="按顺序枚举所有候选分配并逐一验证的算法。">暴力算法</Term>枚举`2ⁿ`种分配，最坏情况直到最后一项才找到答案，或全部检查后确认无解。

对4个变量只是16种，对34个变量已超过170亿。理纱用“低效”概括问题：指数底数看似只是2，变量每增加1个，工作量却翻倍。

### 9.2.6 NP完全问题

<Term def="能够在输入规模的多项式时间内找到正确解的问题类。">P问题</Term>可以高效求解。<Term def="给定候选解后，能够在多项式时间内验证其正确性的问题类。">NP问题</Term>关注的是高效验证候选，而不是已经知道如何高效找到候选。

`NP`不是`Not Polynomial`，而是`Nondeterministic Polynomial time`。所有P问题都是NP问题，但是否：

$$
P=NP
$$

仍未解决。多数研究者相信`P≠NP`，但未经证明仍只是猜想。

<Term def="属于NP且所有NP问题都能在多项式时间归约到它的问题，是NP中最难的一类。">NP完全问题</Term>具有关键地位：若任何一个NP完全问题被证明属于P，就有`P=NP`。SAT是历史上第一个被证明NP完全的问题。给定一个分配，检查公式是否为真很快；怎样快速找到满足分配，或证明不存在，仍没有已知的一般多项式算法。

<Mg4SatVocabularyLab />

<Stepper>
  <Step title="切换字面量子句、CNF、暴力规模与P-NP"><Mg4SatVocabularyLab /></Step>
  <Step title="制作Formula Construction Ladder">从变量x开始，依次添加否定、逻辑或、逻辑且，标出字面量、子句、CNF与3-CNF的边界。</Step>
  <Step title="执行Find-versus-Verify Audit">给出一组分配并计时验证，再比较在2ⁿ个候选中寻找；明确P与NP定义分别约束哪个动作。</Step>
</Stepper>

<Callout type="trap" title="NP就是不能在多项式时间求解的问题">
NP的定义是候选解可在多项式时间验证，不是“非多项式”。目前不知道P是否等于NP，因此也不能把所有NP问题直接断言为无法高效求解。
</Callout>

## 9.3 回家路上

### 9.3.1 誓言与约定

四人走向车站。泰朵拉谈起亲戚婚礼上的誓言，“无论生病还是健康”让她感动。“我”想起米尔嘉深夜电话里的话：约定是表明决心。数学问题要求明确条件，人生的选择也要求说出自己愿意承担的方向。

### 9.3.2 会议

米尔嘉再次问泰朵拉是否愿意在双仓图书馆会议上报告。泰朵拉仍害怕，但当“我”说也许会有初中生因她的报告第一次遇见算法，她决定接受：有人愿意听，就应该为听众认真整理思想。

理纱在会议秘书处帮忙，提醒宣传单仍印着米尔嘉的名字。米尔嘉当天会在美国，计划更改必须真正传达到执行方。这里把“想传递”推进到“对听众、时间和协作负责”。

## 9.4 图书室

### 9.4.1 求解3-SAT问题的随机算法

第二天，米尔嘉与理纱完成一个<Term def="Schöning提出的局部随机搜索：随机重启，从不满足子句中随机翻转变量求解3-SAT。">RANDOM-WALK-3-SAT</Term>版本。输入为3-CNF公式`f`、变量数`n`与重启轮数`R`：

```text
RANDOM-WALK-3-SAT(f, n, R)
  repeat R rounds
    a <- uniformly random assignment of n variables
    repeat at most 3n steps
      if a satisfies f
        return SAT with assignment a
      c <- any clause of f not satisfied by a
      x <- a uniformly random variable occurring in c
      flip x in a
  return probably UNSAT
```

外层做`R`次随机重启，内层每轮最多走`3n`步。只有重启时从全体`2ⁿ`分配中随机选起点；轮内每一步都受当前不满足子句引导。

### 9.4.2 随机漫步

每个分配是`n`维布尔立方体上的一个点，相邻点只差一个变量。算法先随机落到一个点，再反复翻转一个变量，所以它在`2ⁿ`个分配组成的空间里做随机漫步。

关键不是随便翻转。若当前分配`a`不满足某个三字面量子句`c`，则`c`的三个字面量在`a`下全为假。假设公式确实可满足，并固定某个满足分配`a*`。在`a*`下，`c`至少有一个字面量为真，因此三个相关变量中至少一个在`a`与`a*`之间不一致。

从三个变量中均匀选一个翻转，至少有：

$$
\frac13
$$

概率翻中不一致变量。局部动作可能让别的子句变坏，但相对固定的`a*`，它至少有`1/3`概率靠近一步。

### 9.4.3 向着定量评估前进

算法并不知道`a*`是什么，所以运行时无法观察自己究竟在接近还是远离。但分析者可以固定任意一个满足分配作为参照，并给出概率边界：

$$
\Pr(\text{接近一步})\ge\frac13,
\qquad
\Pr(\text{远离一步})\le\frac23.
$$

“不知道当前方向”不妨碍“知道方向概率”。这正是随机算法分析的转换：不追踪每次具体选择，而约束大量可能运行的总体行为。

### 9.4.4 另一个随机漫步

两个等长真假向量中不同位置的数量叫<Term def="两个等长向量在对应位置不同的分量数，也是把一个分配变成另一个所需最少翻转次数。">汉明距离</Term>。记：

$$
d(a,a^*)=
\#\{j:a_j\ne a_j^*\}.
$$

若距离为0，`a=a*`，公式已满足。一次变量翻转只改变一个位置，所以距离只能：

$$
m\longmapsto m-1
\quad\text{或}\quad
m\longmapsto m+1.
$$

高维分配空间中的随机漫步，由汉明距离投影成一条数轴上的一维随机漫步。靠近概率至少`1/3`，远离概率至多`2/3`。

### 9.4.5 关注循环

把内层`3n`步视为一轮。要估计总运行时间，先估计<Term def="单轮随机重启与局部漫步在规定步数内找到满足分配的概率。">循环的成功概率</Term>。

算法输出“可以满足”时，同时给出可验证分配，因此不会误报；输出“大概无法满足”时，可能只是没有走到答案。这叫<Term def="一个方向的结论始终正确，另一个方向允许以受控概率出错的随机算法。">单侧错误蒙特卡罗算法</Term>。

若单轮成功率至少为`M⁻ⁿ`，独立运行：

$$
R=K M^n
$$

轮后仍全部失败的<Term def="公式可满足但所有随机轮次都未找到满足分配的概率。">疏忽概率</Term>至多：

$$
\begin{aligned}
\left(1-\frac1{M^n}\right)^{K M^n}
&\le
\left(e^{-1/M^n}\right)^{K M^n}\\
&=e^{-K}.
\end{aligned}
$$

这里用到`1+x≤eˣ`。增大常数`K`会指数降低疏忽概率，而指数底仍由`Mⁿ`决定。

<Mg4RandomWalk3SatLab />

<Stepper>
  <Step title="切换算法骨架、局部一步、汉明距离与单侧错误"><Mg4RandomWalk3SatLab /></Step>
  <Step title="制作Assignment Hypercube Walk">用三个变量画8个立方体顶点，标出只差一位的边，从不满足子句选择变量并演示一次局部翻转。</Step>
  <Step title="执行One-Sided Output Check">对“可以满足”和“大概无法满足”分别列出可验证证据、可能错误方向与重复R轮后的错误上界。</Step>
</Stepper>

<Callout type="trap" title="RANDOM-WALK-3-SAT每一步都重新随机选择完整分配">
完整随机抽样只发生在每轮起点。之后算法从当前不满足子句中选择变量，只移动到汉明距离为1的邻居；这种局部引导正是它优于盲目反复抽签的来源。
</Callout>

## 9.5 家

### 9.5.1 幸运的评估

夜里，“我”独自分析。随机起点`a`与固定正确分配`a*`距离为`m`，意味着恰有`m`个变量不同。起点均匀随机，因此：

$$
p(m)
=
\Pr(d(a,a^*)=m)
=
\frac1{2^n}\binom{n}{m}.
$$

先只计算最幸运路线：从距离`m`开始，连续`m`次都靠近。每步靠近概率至少`1/3`，所以：

$$
q(m)\ge\left(\frac13\right)^m.
$$

因为`m=0,1,\ldots,n`的起点事件互斥且覆盖全部起点，单轮成功率至少：

$$
\sum_{m=0}^{n}p(m)q(m)
\ge
\frac1{2^n}
\sum_{m=0}^{n}
\binom{n}{m}
\left(\frac13\right)^m.
$$

### 9.5.2 化简和式

二项式定理：

$$
\sum_{m=0}^{n}\binom{n}{m}x^m
=(1+x)^n
$$

取`x=1/3`：

$$
\begin{aligned}
\Pr(\text{单轮成功})
&\ge
\frac1{2^n}
\left(1+\frac13\right)^n\\
&=
\left(\frac23\right)^n.
\end{aligned}
$$

这里同时使用了两种概率结构：不同起始距离事件彼此<Term def="两个事件不能同时发生，因此并集概率等于各自概率之和。">互斥</Term>，所以对`m`求和；连续翻转选择按随机步骤相乘，后面会用独立性解释。

### 9.5.3 次数的评估

单轮成功率至少`(2/3)ⁿ`，其倒数是`(3/2)ⁿ`。令：

$$
R=K\left(\frac32\right)^n,
$$

疏忽概率至多`e⁻ᴷ`。暴力算法指数部分为`2ⁿ`，幸运粗估把它降为：

$$
1.5^n.
$$

这不是说算法必定在`1.5ⁿ`步内找到答案，而是以可调错误概率换取更小的指数底。

## 9.6 图书室

### 9.6.1 独立与互斥

第二天，“我”把`1.5ⁿ`结果讲给伙伴们。米尔嘉区分两个容易混淆的概念。

事件A、B<Term def="一个事件是否发生不改变另一个事件的概率；交集概率等于概率乘积。">独立</Term>时：

$$
\Pr(A\cap B)=\Pr(A)\Pr(B).
$$

事件A、B互斥时：

$$
\Pr(A\cup B)=\Pr(A)+\Pr(B).
$$

独立用于连续随机选择的概率相乘；互斥用于不同起始距离的概率相加。除非某事件概率为0，否则互斥事件通常不独立。

### 9.6.2 精确的评估

幸运粗估只计算从距离`m`连续靠近`m`次的直线路线，丢掉了“先远离再回来”的成功路径。现在计入远离`i`步的路线。为了补偿这`i`步并消除原有距离`m`，必须靠近`m+i`步，总步数：

$$
m+2i.
$$

限制`0≤i≤m`，总步数最多`3m≤3n`，这也解释内层循环为何取`3n`。

距离不能低于0，路径计数与第8章钢琴问题左右翻转后一一对应。令钢琴一般解中的：

$$
(a,b)=(m+i,i),
$$

合法路径数是：

$$
\frac{m}{m+2i}
\binom{m+2i}{i}.
$$

每条路线包含`i`次远离和`m+i`次靠近。用最坏偏置硬币估计：

$$
P(m,i)
\ge
\frac{m}{m+2i}
\binom{m+2i}{i}
\left(\frac23\right)^i
\left(\frac13\right)^{m+i}.
$$

对`0≤i≤m`求和，令从距离`m`最终到达0的概率下界为`Q(m)`：

$$
Q(m)
\ge
\sum_{i=0}^{m}
\frac{m}{m+2i}
\binom{m+2i}{i}
\left(\frac23\right)^i
\left(\frac13\right)^{m+i}.
$$

利用`i≤m`：

$$
\frac{m}{m+2i}\ge\frac13,
\qquad
\left(\frac23\right)^i\ge\left(\frac23\right)^m,
\qquad
\left(\frac13\right)^{m+i}\ge\left(\frac13\right)^{2m}.
$$

因此：

$$
Q(m)
\ge
\frac13
\left(\frac2{27}\right)^m
\sum_{i=0}^{m}\binom{m+2i}{i}.
$$

和式各项非负，所以至少不小于最后一项：

$$
\sum_{i=0}^{m}\binom{m+2i}{i}
\ge
\binom{3m}{m}.
$$

于是：

$$
Q(m)
\ge
\frac13
\left(\frac2{27}\right)^m
\binom{3m}{m}.
$$

直冲终点只有一条模式；现在钢琴路径把大量绕路成功方式都计入，因此下界会更高。

### 9.6.3 斯特林公式

<Term def="阶乘的渐近公式n!约等于√(2πn)(n/e)ⁿ，并可配合上下界估计组合数。">斯特林公式</Term>是：

$$
n!
\sim
\sqrt{2\pi n}
\left(\frac ne\right)^n.
$$

原章使用配套界：

$$
\sqrt{2\pi n}\left(\frac ne\right)^n
\le n!
\le
\sqrt{2\pi n}\left(\frac ne\right)^n e^{1/(12n)}.
$$

把：

$$
\binom{3m}{m}
=
\frac{(3m)!}{m!(2m)!}
$$

的分子用下界、分母用上界，可整理为：

$$
\binom{3m}{m}
\ge
\frac{C}{\sqrt m}
\left(\frac{27}{4}\right)^m,
$$

其中：

$$
C=\frac{\sqrt3}{2\sqrt\pi}e^{-1/8}
$$

是与`m、n`无关的正常数。代回：

$$
\begin{aligned}
Q(m)
&\ge
\frac13
\left(\frac2{27}\right)^m
\frac{C}{\sqrt m}
\left(\frac{27}{4}\right)^m\\
&=
\frac{C'}{\sqrt m}
\left(\frac12\right)^m,
\end{aligned}
$$

其中`C'=C/3`。

再按随机起点距离求和。把`m=0`单独处理，并用`1/√m≥1/√n`：

$$
\begin{aligned}
\Pr(\text{单轮成功})
&=
\sum_{m=0}^{n}
\frac1{2^n}\binom{n}{m}Q(m)\\
&\ge
\frac{C'}{\sqrt n}
\frac1{2^n}
\sum_{m=0}^{n}
\binom{n}{m}
\left(\frac12\right)^m\\
&=
\frac{C'}{\sqrt n}
\left(\frac34\right)^n.
\end{aligned}
$$

所以重复次数最多需要多项式因子乘：

$$
\frac{\sqrt n}{C'}
\left(\frac43\right)^n.
$$

比较指数部分：

$$
\left(\frac43\right)^n
=(1.333\ldots)^n
<1.334^n.
$$

粗估的`1.5ⁿ`降到`1.334ⁿ`。`√n/C'`并未消失，只是在比较指数阶时不支配增长。

<Mg4SchoningAnalysisLab />

<Stepper>
  <Step title="切换幸运粗估、计入回退、斯特林下界与最终底数"><Mg4SchoningAnalysisLab /></Step>
  <Step title="制作Distance-Path Correspondence">从距离m画含i次远离、m+i次靠近的路径，水平翻转成钢琴路径，并核对长度m+2i与路径数公式。</Step>
  <Step title="执行Exponent-Base Ledger">逐行记录2、3/2、4/3三个底数来自哪一个成功率倒数，另列常数与多项式因子，避免把它们错误删除。</Step>
</Stepper>

<Callout type="trap" title="最终时间复杂度就是精确的1.334ⁿ">
`1.334ⁿ`只概括指数部分。每轮最多3n步，检查公式也有多项式成本，精确评估还含`√n/C'`。结论是指数底被压到4/3，而不是所有其他因子都等于1。
</Callout>

伙伴们画出评估“旅行地图”：固定起点距离，借钢琴问题数路径，转成抛硬币概率，求`Q(m)`，用斯特林公式估计组合数，再对初始距离使用二项式定理。泰朵拉意识到，武器不仅是公式，还包括判断何时求上界、何时求下界的方向感。

离馆前，大家抢在瑞谷老师之前齐声宣布放学，理纱也小声加入；老师仍按自己的节奏重复原句。高强度推导在这个小小恶作剧里收束。

## 9.7 回家路上

### 奥林匹克

泰朵拉追问为什么每轮恰是`3n`步。若起点距离为`m`、途中远离`i≤m`步，总步数为`m+2i≤3m≤3n`，所以精确分析计入的路径都能在一轮内走完。

随机起点、检查满足性、选择子句等步骤仍需时间，但假设它们都可在输入规模的多项式时间内完成；决定指数竞争的是重启轮数。

泰朵拉把降低3-SAT指数底比作奥林匹克纪录。研究者提出算法、证明边界、发表论文，后来者继续改进。米尔嘉所读的是乌韦·舍宁1999年的`A Probabilistic Algorithm for k-SAT and Constraint Satisfaction Problems`；当时3-SAT纪录的指数部分是`1.334ⁿ`。论文的本质是规范、正确地写下值得传达的结果。

## 9.8 家

### 逻辑

周末，尤里戴着回信中得到的新缎带，再次与“我”谈可满足性。她发现不仅可以解问题，还可以把“设计更快算法”本身变成问题，也就是研究问题的问题。

逻辑公式转成概率、不等式、组合数与渐近估计，说明数学主题并不孤立。尤里听说理纱能理解二进制斐波那契手势，又得知双仓图书馆会议有面向中学生的研讨会，立刻意识到自己也能参加。她嘴上担心泰朵拉在台上摔倒，实际已经在期待新的对话。

本章没有解决`P`与`NP`，却给出一种面对难题的可靠姿态：先把对象定义清楚，再设计算法、量化成功概率、控制错误、比较增长阶，并把证明写到别人能够复查。

## 本章回顾：从16行真值表到1.334ⁿ

1. 强正美柔问题用四个布尔变量与八个三字面量条件构成逻辑公式。
2. 四变量共有16种分配，真值表证明每种分配都恰好违反一个条件。
3. 删除任意条件后其余七项可满足，因此原公式是极小不可满足结构。
4. 变量或其否定是字面量，字面量用或连接成子句。
5. 子句用且连接成CNF，每个子句恰有三个字面量时是3-CNF。
6. SAT判断布尔公式能否满足，3-SAT把输入限制为3-CNF。
7. 暴力枚举n变量的全部`2ⁿ`个分配，变量数每加1，候选数翻倍。
8. P问题可在多项式时间求解，NP问题的候选解可在多项式时间验证。
9. SAT是历史上第一个被证明NP完全的问题，P是否等于NP仍未解决。
10. RANDOM-WALK-3-SAT每轮随机选起点，再做最多`3n`次局部变量翻转。
11. 当前不满足子句的三个变量中，相对任意固定正确分配至少一个是错的。
12. 随机翻转使汉明距离减1的概率至少`1/3`，增1的概率至多`2/3`。
13. 汉明距离把高维分配空间投影成一维随机漫步。
14. “可以满足”附有可验证分配，不会误报；“大概无法满足”可能漏解。
15. 增加独立重启次数可把单侧疏忽概率压到`e⁻ᴷ`。
16. 随机起点距离为m的概率是`C(n,m)/2ⁿ`。
17. 只计算连续m次靠近的幸运路线，单轮成功率至少为`(2/3)ⁿ`。
18. 幸运粗估对应重复次数指数部分`(3/2)ⁿ=1.5ⁿ`。
19. 计入i次远离后，成功路径长度为`m+2i`，且最多为`3n`。
20. 钢琴问题给出路径数`m/(m+2i)·C(m+2i,i)`。
21. 对`0≤i≤m`求和得到`Q(m)`，并下界为`(1/3)(2/27)ᵐC(3m,m)`。
22. 斯特林公式给出`C(3m,m)≥C(27/4)ᵐ/√m`。
23. 因此`Q(m)≥C'(1/2)ᵐ/√m`。
24. 对初始距离再次使用二项式定理，单轮成功率至少为`C'(3/4)ⁿ/√n`。
25. 重复次数的指数部分至多`(4/3)ⁿ<1.334ⁿ`。
26. 独立事件用概率乘法，互斥事件用概率加法，两者不能混用。
27. 论文通过可复查定义、算法与证明，让其他研究者继续降低纪录。

<Exercises>

**问题 1：把公式`(x₁∨¬x₂∨x₃)∧(¬x₁∨x₂∨¬x₃)`分类，并判断分配`(真,真,假)`是否满足。**

<Answer>每个括号是含三个字面量的子句，两个子句用且连接，所以是3-CNF。第一子句因`x₁`为真而真；第二子句因`x₂`和`¬x₃`为真而真，因此该分配满足公式。</Answer>

**问题 2：当前分配不满足子句`x₁∨¬x₂∨x₃`。写出三个变量当前真假，并说明为何随机翻转至少有1/3概率接近任意固定满足分配a*。**

<Answer>三个字面量都假，所以当前`x₁=假、x₂=真、x₃=假`。在满足整个公式的`a*`中，该子句至少一个字面量为真，因此三个变量中至少一个与当前分配不同；均匀选一个翻转，命中不同变量的概率至少为1/3。</Answer>

**问题 3：若单轮成功率至少为`(2/3)ⁿ`，运行`R=K(3/2)ⁿ`轮，证明全部失败概率至多`e⁻ᴷ`。**

<Answer>单轮失败率至多`1-(2/3)ⁿ`。独立运行R轮，全部失败率至多`(1-(2/3)ⁿ)^{K(3/2)ⁿ}`。用`1-x≤e^{-x}`，上界为`e^{-K(2/3)ⁿ(3/2)ⁿ}=e^{-K}`。</Answer>

**问题 4：在精确分析中取起点距离`m=2`、远离步数`i=1`。求总步数、靠近步数与合法距离路径数。**

<Answer>总步数为`m+2i=4`，靠近步数为`m+i=3`，远离1步。合法路径数是`m/(m+2i)·C(m+2i,i)=2/4×C(4,1)=2`。</Answer>

</Exercises>

<Glossary>
  <GlossaryItem term="布尔变量">只能取真或假两种值的变量。</GlossaryItem>
  <GlossaryItem term="分配方式">为公式中每个变量指定真假值。</GlossaryItem>
  <GlossaryItem term="可满足性问题">判断布尔公式是否存在满足分配的问题。</GlossaryItem>
  <GlossaryItem term="SAT">一般布尔可满足性问题。</GlossaryItem>
  <GlossaryItem term="非运算">反转布尔真假值的逻辑运算。</GlossaryItem>
  <GlossaryItem term="字面量">变量或变量的否定。</GlossaryItem>
  <GlossaryItem term="子句">由字面量用逻辑或连接而成的公式。</GlossaryItem>
  <GlossaryItem term="CNF">由多个子句用逻辑且连接形成的合取范式。</GlossaryItem>
  <GlossaryItem term="3-CNF">每个子句恰有三个字面量的CNF。</GlossaryItem>
  <GlossaryItem term="3-SAT">判断3-CNF是否存在满足分配的问题。</GlossaryItem>
  <GlossaryItem term="满足">给定分配使相应逻辑对象取真。</GlossaryItem>
  <GlossaryItem term="暴力算法">枚举所有候选分配并逐一验证的算法。</GlossaryItem>
  <GlossaryItem term="P问题">可在多项式时间找到解的问题。</GlossaryItem>
  <GlossaryItem term="NP问题">候选解可在多项式时间验证的问题。</GlossaryItem>
  <GlossaryItem term="NP完全问题">NP中所有NP问题均可归约到的最难问题类。</GlossaryItem>
  <GlossaryItem term="RANDOM-WALK-3-SAT">随机重启并从不满足子句中随机翻转变量的3-SAT算法。</GlossaryItem>
  <GlossaryItem term="汉明距离">两个等长分配在对应位置不同的变量数。</GlossaryItem>
  <GlossaryItem term="循环的成功概率">一轮随机漫步在步数上限内找到满足分配的概率。</GlossaryItem>
  <GlossaryItem term="单侧错误蒙特卡罗算法">一类输出必然正确、另一类输出允许概率错误的随机算法。</GlossaryItem>
  <GlossaryItem term="疏忽概率">可满足但所有随机轮次都没找到满足分配的概率。</GlossaryItem>
  <GlossaryItem term="互斥">事件不能同时发生，并集概率可直接相加。</GlossaryItem>
  <GlossaryItem term="独立">事件互不影响，交集概率等于概率乘积。</GlossaryItem>
  <GlossaryItem term="斯特林公式">用√(2πn)(n/e)ⁿ渐近估计n!的公式。</GlossaryItem>
</Glossary>

<Attribution source="《数学女孩 4：随机算法》第9章（结城浩）">
本章按原版“雨天周六的强正美柔问题与回信—逻辑题、SAT术语、分配练习与NP完全—誓言和会议—RANDOM-WALK-3-SAT、分配空间漫步、汉明距离、循环与单侧错误—幸运评估、二项式化简、1.5ⁿ—独立互斥、钢琴路径、斯特林公式、1.334ⁿ—奥林匹克与周末逻辑”顺序重构，保留从2到1.5再到4/3的完整定量证明。
</Attribution>
