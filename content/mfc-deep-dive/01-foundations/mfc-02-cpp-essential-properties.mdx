---
title: "第2章 C++的重要性质"
type: C
section: "第一篇 · 勿在浮砂筑高台"
order: 3
description: "用封装、继承、this、虚函数、对象生存期、RTTI、动态创建、异常和模板解释MFC宏背后的C++机制。 覆盖17个本页目录坐标。"
demo: true
math: false
sourceUrl: "https://m.dushu.com/book/10076731/"
draft: false
---


## 直觉起点

、、、、共同构成本页坐标。用封装、继承、this、虚函数、对象生存期、RTTI、动态创建、异常和模板解释MFC宏背后的C++机制。

阅读MFC代码要同时保留三层视图：最上层是AppWizard生成类和宏，中间层是MFC类层次、运行时表与回调，底层是Win32窗口、消息、句柄、线程和文件。只会点击向导或背宏，无法解释对象为何诞生、消息为何到达、文件为何能重建动态类型。

## 六阶段机制链

### 1. 定义类契约

用封装、继承、this、虚函数、对象生存期、RTTI、动态创建、异常和模板解释MFC宏背后的C++机制。 本阶段围绕“第2章 C++的重要性质”建立预测：列出参与类、入口函数、宏展开、资源ID、线程或文件状态，再运行最小样例。进入“构造对象”前必须保存首个分叉证据，不能让后续框架回调覆盖真正原因。

### 2. 构造对象

用封装、继承、this、虚函数、对象生存期、RTTI、动态创建、异常和模板解释MFC宏背后的C++机制。 本阶段围绕“类及其成员——谈封装（encapsulation）”建立预测：列出参与类、入口函数、宏展开、资源ID、线程或文件状态，再运行最小样例。进入“绑定this”前必须保存首个分叉证据，不能让后续框架回调覆盖真正原因。

### 3. 绑定this

用封装、继承、this、虚函数、对象生存期、RTTI、动态创建、异常和模板解释MFC宏背后的C++机制。 本阶段围绕“基类与派生类：谈继承（Inheritance）”建立预测：列出参与类、入口函数、宏展开、资源ID、线程或文件状态，再运行最小样例。进入“执行虚分派”前必须保存首个分叉证据，不能让后续框架回调覆盖真正原因。

### 4. 执行虚分派

用封装、继承、this、虚函数、对象生存期、RTTI、动态创建、异常和模板解释MFC宏背后的C++机制。 本阶段围绕“this指针”建立预测：列出参与类、入口函数、宏展开、资源ID、线程或文件状态，再运行最小样例。进入“识别动态类型”前必须保存首个分叉证据，不能让后续框架回调覆盖真正原因。

### 5. 识别动态类型

用封装、继承、this、虚函数、对象生存期、RTTI、动态创建、异常和模板解释MFC宏背后的C++机制。 本阶段围绕“虚拟函数与多态（Polymorphism）”建立预测：列出参与类、入口函数、宏展开、资源ID、线程或文件状态，再运行最小样例。进入“析构并释放”前必须保存首个分叉证据，不能让后续框架回调覆盖真正原因。

### 6. 析构并释放

用封装、继承、this、虚函数、对象生存期、RTTI、动态创建、异常和模板解释MFC宏背后的C++机制。 本阶段围绕“类与对象大解剖”建立预测：列出参与类、入口函数、宏展开、资源ID、线程或文件状态，再运行最小样例。进入“定义类契约”前必须保存首个分叉证据，不能让后续框架回调覆盖真正原因。

## 核心机制深挖

用封装、继承、this、虚函数、对象生存期、RTTI、动态创建、异常和模板解释MFC宏背后的C++机制。 原书绑定Visual C++ 5.0与MFC 4.2。本页保留这个历史技术身份；现代Visual Studio、64位、Unicode、智能指针或新UI框架只可作为明确标注的对照，不能反向改写原书机制。

本页不变量是：**动态类型、对象所有权、虚调用目标和模板实例来源可解释；切片、悬空对象与跨模块实例化不会被隐藏。**。它必须在正常输入、空输入、创建失败、消息未处理、持久化版本不符和中断恢复中都成立。窗口看起来能显示、命令偶尔能执行或文件能写出，都不足以证明对象图、分派路径和资源生存期正确。

最小实验：构造基类指针、栈/堆/全局/静态对象和模板容器，打印构造析构、type_info与虚调用轨迹。 运行前记录工具版本、编译选项、链接方式、资源哈希、输入序列和预期调用路径；运行后保存消息日志、对象关系、文件字节或线程退出状态。

故障实验：注入对象切片、非虚析构、异常中途退出和模板定义缺失，比较编译期与运行期失败。 每次只改变一个条件，停在第一处与预测不同的位置。删除故障后从干净构建或明确逆操作恢复，并以完全相同的输入重放；如果最终画面相同但对象、句柄、线程、Archive或资源状态不同，仍判定失败。

机制证据表至少保留六列：框架表面动作、对应的C++对象或宏、下沉到Win32的入口、可观察状态、首个失败信号、恢复操作。对历史代码还要单列“原书行为”和“现代迁移差异”：前者用于解释MFC 4.2为何这样工作，后者只说明在64位、Unicode或新工具链中怎样验证同一不变量。这样既不会把旧API当成今天的最佳实践，也不会因现代封装更方便而跳过原书真正要揭示的内部机制。

## 最小可重放骨架

```cpp
struct Base { virtual ~Base() = default; virtual const char* name() const = 0; };
struct Derived final : Base { const char* name() const override { return "Derived"; } };
```

```cpp
void inspect(Base& object) {
  std::cout << object.name() << ' ' << typeid(object).name();
}
```

```cpp
template<class T> class Owner {
 public: explicit Owner(T* value) : value_(value) {}
 ~Owner() { delete value_; }
 private: T* value_;
};
```

## 正式目录逐项讲解

### 第2章 C++的重要性质

“第2章 C++的重要性质”在本页用于连接本章术语、输入输出、状态变化和可验证结果。先在“定义类契约”阶段标出对象布局涉及的对象、线程、资源或表项，写下预期入口、状态变化和退出条件；再推进到“构造对象”，用调用栈、消息日志、对象图、序列化字节或界面状态验证推断。这里不能只记住向导操作或宏名字：必须说明它替代了哪段Win32/C++机制、失败首先在哪里可见，以及怎样恢复后用同一输入重放。

### 类及其成员——谈封装（encapsulation）

“类及其成员——谈封装（encapsulation）”在本页用于定位类层次中的责任、所有权和协作关系。先在“构造对象”阶段标出虚函数涉及的对象、线程、资源或表项，写下预期入口、状态变化和退出条件；再推进到“绑定this”，用调用栈、消息日志、对象图、序列化字节或界面状态验证推断。这里不能只记住向导操作或宏名字：必须说明它替代了哪段Win32/C++机制、失败首先在哪里可见，以及怎样恢复后用同一输入重放。

### 基类与派生类：谈继承（Inheritance）

“基类与派生类：谈继承（Inheritance）”在本页用于定位类层次中的责任、所有权和协作关系。先在“绑定this”阶段标出对象生存期涉及的对象、线程、资源或表项，写下预期入口、状态变化和退出条件；再推进到“执行虚分派”，用调用栈、消息日志、对象图、序列化字节或界面状态验证推断。这里不能只记住向导操作或宏名字：必须说明它替代了哪段Win32/C++机制、失败首先在哪里可见，以及怎样恢复后用同一输入重放。

### this指针

“this指针”在本页用于连接本章术语、输入输出、状态变化和可验证结果。先在“执行虚分派”阶段标出RTTI涉及的对象、线程、资源或表项，写下预期入口、状态变化和退出条件；再推进到“识别动态类型”，用调用栈、消息日志、对象图、序列化字节或界面状态验证推断。这里不能只记住向导操作或宏名字：必须说明它替代了哪段Win32/C++机制、失败首先在哪里可见，以及怎样恢复后用同一输入重放。

### 虚拟函数与多态（Polymorphism）

“虚拟函数与多态（Polymorphism）”在本页用于连接本章术语、输入输出、状态变化和可验证结果。先在“识别动态类型”阶段标出模板实例化涉及的对象、线程、资源或表项，写下预期入口、状态变化和退出条件；再推进到“析构并释放”，用调用栈、消息日志、对象图、序列化字节或界面状态验证推断。这里不能只记住向导操作或宏名字：必须说明它替代了哪段Win32/C++机制、失败首先在哪里可见，以及怎样恢复后用同一输入重放。

### 类与对象大解剖

“类与对象大解剖”在本页用于定位类层次中的责任、所有权和协作关系。先在“析构并释放”阶段标出对象布局涉及的对象、线程、资源或表项，写下预期入口、状态变化和退出条件；再推进到“定义类契约”，用调用栈、消息日志、对象图、序列化字节或界面状态验证推断。这里不能只记住向导操作或宏名字：必须说明它替代了哪段Win32/C++机制、失败首先在哪里可见，以及怎样恢复后用同一输入重放。

### Object slicing与虚拟函数

“Object slicing与虚拟函数”在本页用于连接本章术语、输入输出、状态变化和可验证结果。先在“定义类契约”阶段标出虚函数涉及的对象、线程、资源或表项，写下预期入口、状态变化和退出条件；再推进到“构造对象”，用调用栈、消息日志、对象图、序列化字节或界面状态验证推断。这里不能只记住向导操作或宏名字：必须说明它替代了哪段Win32/C++机制、失败首先在哪里可见，以及怎样恢复后用同一输入重放。

### 静态成员（变量与函数）

“静态成员（变量与函数）”在本页用于连接本章术语、输入输出、状态变化和可验证结果。先在“构造对象”阶段标出对象生存期涉及的对象、线程、资源或表项，写下预期入口、状态变化和退出条件；再推进到“绑定this”，用调用栈、消息日志、对象图、序列化字节或界面状态验证推断。这里不能只记住向导操作或宏名字：必须说明它替代了哪段Win32/C++机制、失败首先在哪里可见，以及怎样恢复后用同一输入重放。

### C++程序的生与死：兼谈构造函数与解构函数

“C++程序的生与死：兼谈构造函数与解构函数”在本页用于把抽象机制落到可运行对象图和调用轨迹。先在“绑定this”阶段标出RTTI涉及的对象、线程、资源或表项，写下预期入口、状态变化和退出条件；再推进到“执行虚分派”，用调用栈、消息日志、对象图、序列化字节或界面状态验证推断。这里不能只记住向导操作或宏名字：必须说明它替代了哪段Win32/C++机制、失败首先在哪里可见，以及怎样恢复后用同一输入重放。

### 四种不同的对象生存方式（in stack、in heap、global、local static）

“四种不同的对象生存方式（in stack、in heap、global、local static）”在本页用于定位类层次中的责任、所有权和协作关系。先在“执行虚分派”阶段标出模板实例化涉及的对象、线程、资源或表项，写下预期入口、状态变化和退出条件；再推进到“识别动态类型”，用调用栈、消息日志、对象图、序列化字节或界面状态验证推断。这里不能只记住向导操作或宏名字：必须说明它替代了哪段Win32/C++机制、失败首先在哪里可见，以及怎样恢复后用同一输入重放。

### 执行期类型信息（RTTI）

“执行期类型信息（RTTI）”在本页用于定位类层次中的责任、所有权和协作关系。先在“识别动态类型”阶段标出对象布局涉及的对象、线程、资源或表项，写下预期入口、状态变化和退出条件；再推进到“析构并释放”，用调用栈、消息日志、对象图、序列化字节或界面状态验证推断。这里不能只记住向导操作或宏名字：必须说明它替代了哪段Win32/C++机制、失败首先在哪里可见，以及怎样恢复后用同一输入重放。

### 动态生成（Dynamic Creation）

“动态生成（Dynamic Creation）”在本页用于连接本章术语、输入输出、状态变化和可验证结果。先在“析构并释放”阶段标出虚函数涉及的对象、线程、资源或表项，写下预期入口、状态变化和退出条件；再推进到“定义类契约”，用调用栈、消息日志、对象图、序列化字节或界面状态验证推断。这里不能只记住向导操作或宏名字：必须说明它替代了哪段Win32/C++机制、失败首先在哪里可见，以及怎样恢复后用同一输入重放。

### 异常处理（Exception Handling）

“异常处理（Exception Handling）”在本页用于连接本章术语、输入输出、状态变化和可验证结果。先在“定义类契约”阶段标出对象生存期涉及的对象、线程、资源或表项，写下预期入口、状态变化和退出条件；再推进到“构造对象”，用调用栈、消息日志、对象图、序列化字节或界面状态验证推断。这里不能只记住向导操作或宏名字：必须说明它替代了哪段Win32/C++机制、失败首先在哪里可见，以及怎样恢复后用同一输入重放。

### Template

“Template”在本页用于定位类层次中的责任、所有权和协作关系。先在“构造对象”阶段标出RTTI涉及的对象、线程、资源或表项，写下预期入口、状态变化和退出条件；再推进到“绑定this”，用调用栈、消息日志、对象图、序列化字节或界面状态验证推断。这里不能只记住向导操作或宏名字：必须说明它替代了哪段Win32/C++机制、失败首先在哪里可见，以及怎样恢复后用同一输入重放。

### Template Functions

“Template Functions”在本页用于定位类层次中的责任、所有权和协作关系。先在“绑定this”阶段标出模板实例化涉及的对象、线程、资源或表项，写下预期入口、状态变化和退出条件；再推进到“执行虚分派”，用调用栈、消息日志、对象图、序列化字节或界面状态验证推断。这里不能只记住向导操作或宏名字：必须说明它替代了哪段Win32/C++机制、失败首先在哪里可见，以及怎样恢复后用同一输入重放。

### Template Classes

“Template Classes”在本页用于定位类层次中的责任、所有权和协作关系。先在“执行虚分派”阶段标出对象布局涉及的对象、线程、资源或表项，写下预期入口、状态变化和退出条件；再推进到“识别动态类型”，用调用栈、消息日志、对象图、序列化字节或界面状态验证推断。这里不能只记住向导操作或宏名字：必须说明它替代了哪段Win32/C++机制、失败首先在哪里可见，以及怎样恢复后用同一输入重放。

### Template的编译与链接

“Template的编译与链接”在本页用于定位类层次中的责任、所有权和协作关系。先在“识别动态类型”阶段标出虚函数涉及的对象、线程、资源或表项，写下预期入口、状态变化和退出条件；再推进到“析构并释放”，用调用栈、消息日志、对象图、序列化字节或界面状态验证推断。这里不能只记住向导操作或宏名字：必须说明它替代了哪段Win32/C++机制、失败首先在哪里可见，以及怎样恢复后用同一输入重放。

## 分步视觉验证


## 常见误区与故障注入


## 四类样本与验收

| 样本 | 注入方式                   | 必查证据                   | 通过条件     |
| ---- | -------------------------- | -------------------------- | ------------ |
| 基线 | 固定工程、资源与输入       | 对象图、调用栈、消息序列   | 与预测一致   |
| 边界 | 空文档、无处理器、极限尺寸 | 默认路径、返回值、资源状态 | 不越界不泄漏 |
| 失败 | 缺宏、创建失败、版本不符   | 首个分叉及责任层           | 失败被隔离   |
| 恢复 | 干净重建后同输入重放       | 文件、窗口、线程与句柄     | 回到基线     |

## 练习


## 小结

- 对象布局：连接“定义类契约”的对象、控制流与证据。
- 虚函数：连接“构造对象”的对象、控制流与证据。
- 对象生存期：连接“绑定this”的对象、控制流与证据。
- RTTI：连接“执行虚分派”的对象、控制流与证据。
- 模板实例化：连接“识别动态类型”的对象、控制流与证据。
- 本页17个目录坐标全部进入机制链，不用泛化主题替代原书章节。

## 术语表


