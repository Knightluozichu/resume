---
title: "第7章 简单而完整：MFC骨干程序"
type: C
section: "第三篇 · 浅出MFC程序设计"
order: 8
description: "解剖AppWizard生成的Scribble step0，串起Document Template、主框架、工具栏、状态栏、拖放、消息映射、菜单与View。 覆盖15个本页目录坐标。"
demo: true
math: false
sourceUrl: "https://m.dushu.com/book/10076731/"
draft: false
---

import {
  Mfc07FrameworkSkeletonMapLab,
  Mfc07FrameworkSkeletonExperimentLab,
  Mfc07FrameworkSkeletonEvidenceLab,
} from "@/components/mdx/mfc-deep-dive/diagrams/mfc-07-framework-skeleton";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第7章 简单而完整：MFC骨干程序的核心对象、控制流和它在全书中的位置
- 能描述并沿“生成骨干项目 → 注册Document Template → 创建Frame与View → 装载菜单工具栏 → 连接消息映射 → 验证文档交互”恢复隐藏在宏与向导后的真实机制
- 能实现一个固定版本、可重放、可故障注入的最小MFC实验
- 能比较基线、边界、失败与恢复轨迹，并用不变量而非截图验收

</Objectives>

## 直觉起点

<Term def="Scribble step0连接生成骨干项目阶段的对象责任、调用路径与恢复证据。">
  Scribble step0
</Term>
、
<Term def="Document Template连接注册Document Template阶段的对象责任、调用路径与恢复证据。">
  Document Template
</Term>
、
<Term def="主框架连接创建Frame与View阶段的对象责任、调用路径与恢复证据。">
  主框架
</Term>
、
<Term def="消息映射连接装载菜单工具栏阶段的对象责任、调用路径与恢复证据。">
  消息映射
</Term>
、
<Term def="CEditView连接连接消息映射阶段的对象责任、调用路径与恢复证据。">
  CEditView
</Term>
共同构成本页坐标。解剖AppWizard生成的Scribble step0，串起Document
Template、主框架、工具栏、状态栏、拖放、消息映射、菜单与View。

阅读MFC代码要同时保留三层视图：最上层是AppWizard生成类和宏，中间层是MFC类层次、运行时表与回调，底层是Win32窗口、消息、句柄、线程和文件。只会点击向导或背宏，无法解释对象为何诞生、消息为何到达、文件为何能重建动态类型。

<Mfc07FrameworkSkeletonMapLab />

## 六阶段机制链

### 1. 生成骨干项目

解剖AppWizard生成的Scribble step0，串起Document Template、主框架、工具栏、状态栏、拖放、消息映射、菜单与View。 本阶段围绕“第7章 简单而完整：MFC骨干程序”建立预测：列出参与类、入口函数、宏展开、资源ID、线程或文件状态，再运行最小样例。进入“注册Document Template”前必须保存首个分叉证据，不能让后续框架回调覆盖真正原因。

### 2. 注册Document Template

解剖AppWizard生成的Scribble step0，串起Document Template、主框架、工具栏、状态栏、拖放、消息映射、菜单与View。 本阶段围绕“不二法门：熟记MFC类层次结构”建立预测：列出参与类、入口函数、宏展开、资源ID、线程或文件状态，再运行最小样例。进入“创建Frame与View”前必须保存首个分叉证据，不能让后续框架回调覆盖真正原因。

### 3. 创建Frame与View

解剖AppWizard生成的Scribble step0，串起Document Template、主框架、工具栏、状态栏、拖放、消息映射、菜单与View。 本阶段围绕“MFC程序的UI新风貌”建立预测：列出参与类、入口函数、宏展开、资源ID、线程或文件状态，再运行最小样例。进入“装载菜单工具栏”前必须保存首个分叉证据，不能让后续框架回调覆盖真正原因。

### 4. 装载菜单工具栏

解剖AppWizard生成的Scribble step0，串起Document Template、主框架、工具栏、状态栏、拖放、消息映射、菜单与View。 本阶段围绕“Document/View支撑你的应用程序”建立预测：列出参与类、入口函数、宏展开、资源ID、线程或文件状态，再运行最小样例。进入“连接消息映射”前必须保存首个分叉证据，不能让后续框架回调覆盖真正原因。

### 5. 连接消息映射

解剖AppWizard生成的Scribble step0，串起Document Template、主框架、工具栏、状态栏、拖放、消息映射、菜单与View。 本阶段围绕“利用Visual C++工具完成Scribble step0”建立预测：列出参与类、入口函数、宏展开、资源ID、线程或文件状态，再运行最小样例。进入“验证文档交互”前必须保存首个分叉证据，不能让后续框架回调覆盖真正原因。

### 6. 验证文档交互

解剖AppWizard生成的Scribble step0，串起Document Template、主框架、工具栏、状态栏、拖放、消息映射、菜单与View。 本阶段围绕“骨干程序使用哪些MFC类？”建立预测：列出参与类、入口函数、宏展开、资源ID、线程或文件状态，再运行最小样例。进入“生成骨干项目”前必须保存首个分叉证据，不能让后续框架回调覆盖真正原因。

## 核心机制深挖

解剖AppWizard生成的Scribble step0，串起Document Template、主框架、工具栏、状态栏、拖放、消息映射、菜单与View。 原书绑定Visual C++ 5.0与MFC 4.2。本页保留这个历史技术身份；现代Visual Studio、64位、Unicode、智能指针或新UI框架只可作为明确标注的对照，不能反向改写原书机制。

本页不变量是：**Document、View、Frame和Document Template注册成闭合对象图，资源命令ID与消息处理函数一一对应。**。它必须在正常输入、空输入、创建失败、消息未处理、持久化版本不符和中断恢复中都成立。窗口看起来能显示、命令偶尔能执行或文件能写出，都不足以证明对象图、分派路径和资源生存期正确。

最小实验：对骨干项目做三次最小改动：替换View类、增加命令、启用拖放；每次比较对象创建与路由轨迹。 运行前记录工具版本、编译选项、链接方式、资源哈希、输入序列和预期调用路径；运行后保存消息日志、对象关系、文件字节或线程退出状态。

故障实验：移除DocTemplate、资源ID或消息项，区分窗口能显示但命令失效与文档根本无法创建。 每次只改变一个条件，停在第一处与预测不同的位置。删除故障后从干净构建或明确逆操作恢复，并以完全相同的输入重放；如果最终画面相同但对象、句柄、线程、Archive或资源状态不同，仍判定失败。

机制证据表至少保留六列：框架表面动作、对应的C++对象或宏、下沉到Win32的入口、可观察状态、首个失败信号、恢复操作。对历史代码还要单列“原书行为”和“现代迁移差异”：前者用于解释MFC 4.2为何这样工作，后者只说明在64位、Unicode或新工具链中怎样验证同一不变量。这样既不会把旧API当成今天的最佳实践，也不会因现代封装更方便而跳过原书真正要揭示的内部机制。

## 最小可重放骨架

```cpp
auto* docTemplate = new CSingleDocTemplate(IDR_MAINFRAME,
  RUNTIME_CLASS(CScribbleDoc), RUNTIME_CLASS(CMainFrame), RUNTIME_CLASS(CScribbleView));
AddDocTemplate(docTemplate);
```

```cpp
BEGIN_MESSAGE_MAP(CScribbleView, CView)
  ON_WM_LBUTTONDOWN()
  ON_COMMAND(ID_EDIT_CLEAR, OnEditClear)
END_MESSAGE_MAP()
```

```text
resource command ID -> message map entry -> View handler -> Document mutation -> UpdateAllViews -> OnDraw
```

## 正式目录逐项讲解

### 第7章 简单而完整：MFC骨干程序

“第7章 简单而完整：MFC骨干程序”在本页用于把抽象机制落到可运行对象图和调用轨迹。先在“生成骨干项目”阶段标出Scribble step0涉及的对象、线程、资源或表项，写下预期入口、状态变化和退出条件；再推进到“注册Document Template”，用调用栈、消息日志、对象图、序列化字节或界面状态验证推断。这里不能只记住向导操作或宏名字：必须说明它替代了哪段Win32/C++机制、失败首先在哪里可见，以及怎样恢复后用同一输入重放。

### 不二法门：熟记MFC类层次结构

“不二法门：熟记MFC类层次结构”在本页用于定位类层次中的责任、所有权和协作关系。先在“注册Document Template”阶段标出Document Template涉及的对象、线程、资源或表项，写下预期入口、状态变化和退出条件；再推进到“创建Frame与View”，用调用栈、消息日志、对象图、序列化字节或界面状态验证推断。这里不能只记住向导操作或宏名字：必须说明它替代了哪段Win32/C++机制、失败首先在哪里可见，以及怎样恢复后用同一输入重放。

### MFC程序的UI新风貌

“MFC程序的UI新风貌”在本页用于把抽象机制落到可运行对象图和调用轨迹。先在“创建Frame与View”阶段标出主框架涉及的对象、线程、资源或表项，写下预期入口、状态变化和退出条件；再推进到“装载菜单工具栏”，用调用栈、消息日志、对象图、序列化字节或界面状态验证推断。这里不能只记住向导操作或宏名字：必须说明它替代了哪段Win32/C++机制、失败首先在哪里可见，以及怎样恢复后用同一输入重放。

### Document/View支撑你的应用程序

“Document/View支撑你的应用程序”在本页用于把抽象机制落到可运行对象图和调用轨迹。先在“装载菜单工具栏”阶段标出消息映射涉及的对象、线程、资源或表项，写下预期入口、状态变化和退出条件；再推进到“连接消息映射”，用调用栈、消息日志、对象图、序列化字节或界面状态验证推断。这里不能只记住向导操作或宏名字：必须说明它替代了哪段Win32/C++机制、失败首先在哪里可见，以及怎样恢复后用同一输入重放。

### 利用Visual C++工具完成Scribble step0

“利用Visual C++工具完成Scribble step0”在本页用于把抽象机制落到可运行对象图和调用轨迹。先在“连接消息映射”阶段标出CEditView涉及的对象、线程、资源或表项，写下预期入口、状态变化和退出条件；再推进到“验证文档交互”，用调用栈、消息日志、对象图、序列化字节或界面状态验证推断。这里不能只记住向导操作或宏名字：必须说明它替代了哪段Win32/C++机制、失败首先在哪里可见，以及怎样恢复后用同一输入重放。

### 骨干程序使用哪些MFC类？

“骨干程序使用哪些MFC类？”在本页用于把抽象机制落到可运行对象图和调用轨迹。先在“验证文档交互”阶段标出Scribble step0涉及的对象、线程、资源或表项，写下预期入口、状态变化和退出条件；再推进到“生成骨干项目”，用调用栈、消息日志、对象图、序列化字节或界面状态验证推断。这里不能只记住向导操作或宏名字：必须说明它替代了哪段Win32/C++机制、失败首先在哪里可见，以及怎样恢复后用同一输入重放。

### Document Template的意义

“Document Template的意义”在本页用于定位类层次中的责任、所有权和协作关系。先在“生成骨干项目”阶段标出Document Template涉及的对象、线程、资源或表项，写下预期入口、状态变化和退出条件；再推进到“注册Document Template”，用调用栈、消息日志、对象图、序列化字节或界面状态验证推断。这里不能只记住向导操作或宏名字：必须说明它替代了哪段Win32/C++机制、失败首先在哪里可见，以及怎样恢复后用同一输入重放。

### Scribble的Document/View设计

“Scribble的Document/View设计”在本页用于把抽象机制落到可运行对象图和调用轨迹。先在“注册Document Template”阶段标出主框架涉及的对象、线程、资源或表项，写下预期入口、状态变化和退出条件；再推进到“创建Frame与View”，用调用栈、消息日志、对象图、序列化字节或界面状态验证推断。这里不能只记住向导操作或宏名字：必须说明它替代了哪段Win32/C++机制、失败首先在哪里可见，以及怎样恢复后用同一输入重放。

### 主窗口的诞生

“主窗口的诞生”在本页用于连接本章术语、输入输出、状态变化和可验证结果。先在“创建Frame与View”阶段标出消息映射涉及的对象、线程、资源或表项，写下预期入口、状态变化和退出条件；再推进到“装载菜单工具栏”，用调用栈、消息日志、对象图、序列化字节或界面状态验证推断。这里不能只记住向导操作或宏名字：必须说明它替代了哪段Win32/C++机制、失败首先在哪里可见，以及怎样恢复后用同一输入重放。

### 工具栏和状态栏的诞生（Toolbar&Status bar）

“工具栏和状态栏的诞生（Toolbar&Status bar）”在本页用于连接本章术语、输入输出、状态变化和可验证结果。先在“装载菜单工具栏”阶段标出CEditView涉及的对象、线程、资源或表项，写下预期入口、状态变化和退出条件；再推进到“连接消息映射”，用调用栈、消息日志、对象图、序列化字节或界面状态验证推断。这里不能只记住向导操作或宏名字：必须说明它替代了哪段Win32/C++机制、失败首先在哪里可见，以及怎样恢复后用同一输入重放。

### 鼠标拖放（Drag and Drop）

“鼠标拖放（Drag and Drop）”在本页用于连接本章术语、输入输出、状态变化和可验证结果。先在“连接消息映射”阶段标出Scribble step0涉及的对象、线程、资源或表项，写下预期入口、状态变化和退出条件；再推进到“验证文档交互”，用调用栈、消息日志、对象图、序列化字节或界面状态验证推断。这里不能只记住向导操作或宏名字：必须说明它替代了哪段Win32/C++机制、失败首先在哪里可见，以及怎样恢复后用同一输入重放。

### 消息映射（Message Map）

“消息映射（Message Map）”在本页用于追踪事件从入口到处理器再到退出的分派路径。先在“验证文档交互”阶段标出Document Template涉及的对象、线程、资源或表项，写下预期入口、状态变化和退出条件；再推进到“生成骨干项目”，用调用栈、消息日志、对象图、序列化字节或界面状态验证推断。这里不能只记住向导操作或宏名字：必须说明它替代了哪段Win32/C++机制、失败首先在哪里可见，以及怎样恢复后用同一输入重放。

### 标准菜单File/Edit/View/Window/Help

“标准菜单File/Edit/View/Window/Help”在本页用于定位类层次中的责任、所有权和协作关系。先在“生成骨干项目”阶段标出主框架涉及的对象、线程、资源或表项，写下预期入口、状态变化和退出条件；再推进到“注册Document Template”，用调用栈、消息日志、对象图、序列化字节或界面状态验证推断。这里不能只记住向导操作或宏名字：必须说明它替代了哪段Win32/C++机制、失败首先在哪里可见，以及怎样恢复后用同一输入重放。

### 对话框

“对话框”在本页用于连接资源控件、对象成员、校验与提交边界。先在“注册Document Template”阶段标出消息映射涉及的对象、线程、资源或表项，写下预期入口、状态变化和退出条件；再推进到“创建Frame与View”，用调用栈、消息日志、对象图、序列化字节或界面状态验证推断。这里不能只记住向导操作或宏名字：必须说明它替代了哪段Win32/C++机制、失败首先在哪里可见，以及怎样恢复后用同一输入重放。

### 改用CEditView

“改用CEditView”在本页用于定位类层次中的责任、所有权和协作关系。先在“创建Frame与View”阶段标出CEditView涉及的对象、线程、资源或表项，写下预期入口、状态变化和退出条件；再推进到“装载菜单工具栏”，用调用栈、消息日志、对象图、序列化字节或界面状态验证推断。这里不能只记住向导操作或宏名字：必须说明它替代了哪段Win32/C++机制、失败首先在哪里可见，以及怎样恢复后用同一输入重放。

## 分步视觉验证

<Stepper>
  <Step title="先画对象与控制流">
    标出六阶段涉及的类、窗口、消息表、资源、线程或文件，运行前先预测入口和状态变化。
    <Mfc07FrameworkSkeletonMapLab />
  </Step>
  <Step title="只改变一个故障条件">
    固定工具版本、工程和输入，只切换缺失宏、错误类、未处理消息、Archive不匹配等单一条件。
    <Mfc07FrameworkSkeletonExperimentLab />
  </Step>
  <Step title="用同一输入验收恢复">
    恢复干净构建，重放相同消息和文件输入，比较对象图、路由、输出、资源与退出状态。
    <Mfc07FrameworkSkeletonEvidenceLab />
  </Step>
</Stepper>

## 常见误区与故障注入

<Callout type="trap">
  **误区一：AppWizard生成并能编译，就等于理解MFC。**
  生成代码只是一个样本。必须能沿全局Application对象、Document
  Template、运行时类、消息表和Win32回调解释每个关键动作。
</Callout>

<Callout type="trap">
  **误区二：把现代框架经验直接套回Visual C++ 5.0。** 原书讨论MFC
  4.2、32位Windows和当时的工具链。现代API可帮助迁移，但不能替代对原目录、宏展开和历史行为的忠实解释。
</Callout>

## 四类样本与验收

| 样本 | 注入方式                   | 必查证据                   | 通过条件     |
| ---- | -------------------------- | -------------------------- | ------------ |
| 基线 | 固定工程、资源与输入       | 对象图、调用栈、消息序列   | 与预测一致   |
| 边界 | 空文档、无处理器、极限尺寸 | 默认路径、返回值、资源状态 | 不越界不泄漏 |
| 失败 | 缺宏、创建失败、版本不符   | 首个分叉及责任层           | 失败被隔离   |
| 恢复 | 干净重建后同输入重放       | 文件、窗口、线程与句柄     | 回到基线     |

## 练习

<Exercises>

**问题 1** 为什么不能只看最终窗口验收第7章 简单而完整：MFC骨干程序？

<Answer>
  窗口可能由错误类、错误资源或偶然的默认处理显示出来。必须核对对象创建、消息路由、状态变化、退出路径和恢复后的同输入重放。
</Answer>

**问题 2** 如何把“第7章 简单而完整：MFC骨干程序”变成可执行证据？

<Answer>
  沿“生成骨干项目、注册Document
  Template、创建Frame与View、装载菜单工具栏、连接消息映射、验证文档交互”固定Visual
  C++/MFC版本、工程、资源和输入，先预测对象与调用路径，再只改变一个条件并保存首错；恢复后比较对象图、消息、文件或线程状态。
</Answer>

**问题 3** 本页最小通过条件是什么？

<Answer>
  Document、View、Frame和Document
  Template注册成闭合对象图，资源命令ID与消息处理函数一一对应。
  失败路径不得污染下一次实验，资源清理明确，同一输入能够重新到达基线。
</Answer>

</Exercises>

## 小结

- Scribble step0：连接“生成骨干项目”的对象、控制流与证据。
- Document Template：连接“注册Document Template”的对象、控制流与证据。
- 主框架：连接“创建Frame与View”的对象、控制流与证据。
- 消息映射：连接“装载菜单工具栏”的对象、控制流与证据。
- CEditView：连接“连接消息映射”的对象、控制流与证据。
- 本页15个目录坐标全部进入机制链，不用泛化主题替代原书章节。

## 术语表

<Glossary>
  <GlossaryItem term="Scribble step0">
    Scribble
    step0用于解释“生成骨干项目”：既要指出框架表面接口，也要还原其Win32/C++基础、状态变化、失败信号和恢复方法。
  </GlossaryItem>
  <GlossaryItem term="Document Template">
    Document Template用于解释“注册Document
    Template”：既要指出框架表面接口，也要还原其Win32/C++基础、状态变化、失败信号和恢复方法。
  </GlossaryItem>
  <GlossaryItem term="主框架">
    主框架用于解释“创建Frame与View”：既要指出框架表面接口，也要还原其Win32/C++基础、状态变化、失败信号和恢复方法。
  </GlossaryItem>
  <GlossaryItem term="消息映射">
    消息映射用于解释“装载菜单工具栏”：既要指出框架表面接口，也要还原其Win32/C++基础、状态变化、失败信号和恢复方法。
  </GlossaryItem>
  <GlossaryItem term="CEditView">
    CEditView用于解释“连接消息映射”：既要指出框架表面接口，也要还原其Win32/C++基础、状态变化、失败信号和恢复方法。
  </GlossaryItem>
</Glossary>

<Attribution
  adaptedFrom="侯俊杰（侯捷） · 深入浅出MFC（第二版） · Visual C++ 5.0 / MFC 4.2 · 完整目录独立教学重构"
  adaptedUrl="https://m.dushu.com/book/10076731/"
/>
