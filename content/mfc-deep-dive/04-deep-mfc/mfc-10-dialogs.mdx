---
title: "第10章 MFC与对话盒"
type: C
section: "第四篇 · 深入MFC程序设计"
order: 11
description: "从对话框资源与专用类连接消息处理、DDX/DDV和模态/非模态唤起，建立界面控件与领域状态的同步边界。 覆盖7个本页目录坐标。"
demo: true
math: false
sourceUrl: "https://m.dushu.com/book/10076731/"
draft: true
---

import {
  Mfc10DialogsMapLab,
  Mfc10DialogsExperimentLab,
  Mfc10DialogsEvidenceLab,
} from "@/components/mdx/mfc-deep-dive/diagrams/mfc-10-dialogs";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第10章 MFC与对话盒的核心对象、控制流和它在全书中的位置
- 能描述并沿“设计对话框资源 → 生成专用类 → 绑定控件数据 → 执行校验 → 处理消息 → 提交或取消”恢复隐藏在宏与向导后的真实机制
- 能实现一个固定版本、可重放、可故障注入的最小MFC实验
- 能比较基线、边界、失败与恢复轨迹，并用不变量而非截图验收

</Objectives>

## 直觉起点

<Term def="Dialog资源连接设计对话框资源阶段的对象责任、调用路径与恢复证据。">Dialog资源</Term>、<Term def="ClassWizard连接生成专用类阶段的对象责任、调用路径与恢复证据。">ClassWizard</Term>、<Term def="DDX连接绑定控件数据阶段的对象责任、调用路径与恢复证据。">DDX</Term>、<Term def="DDV连接执行校验阶段的对象责任、调用路径与恢复证据。">DDV</Term>、<Term def="DoModal连接处理消息阶段的对象责任、调用路径与恢复证据。">DoModal</Term>
共同构成本页坐标。从对话框资源与专用类连接消息处理、DDX/DDV和模态/非模态唤起，建立界面控件与领域状态的同步边界。

阅读MFC代码要同时保留三层视图：最上层是AppWizard生成类和宏，中间层是MFC类层次、运行时表与回调，底层是Win32窗口、消息、句柄、线程和文件。只会点击向导或背宏，无法解释对象为何诞生、消息为何到达、文件为何能重建动态类型。

<Mfc10DialogsMapLab />

## 六阶段机制链

### 1. 设计对话框资源

从对话框资源与专用类连接消息处理、DDX/DDV和模态/非模态唤起，建立界面控件与领域状态的同步边界。 本阶段围绕“第10章 MFC与对话盒”建立预测：列出参与类、入口函数、宏展开、资源ID、线程或文件状态，再运行最小样例。进入“生成专用类”前必须保存首个分叉证据，不能让后续框架回调覆盖真正原因。

### 2. 生成专用类

从对话框资源与专用类连接消息处理、DDX/DDV和模态/非模态唤起，建立界面控件与领域状态的同步边界。 本阶段围绕“对话框编辑器”建立预测：列出参与类、入口函数、宏展开、资源ID、线程或文件状态，再运行最小样例。进入“绑定控件数据”前必须保存首个分叉证据，不能让后续框架回调覆盖真正原因。

### 3. 绑定控件数据

从对话框资源与专用类连接消息处理、DDX/DDV和模态/非模态唤起，建立界面控件与领域状态的同步边界。 本阶段围绕“利用ClassWizard连接对话框与其专用类”建立预测：列出参与类、入口函数、宏展开、资源ID、线程或文件状态，再运行最小样例。进入“执行校验”前必须保存首个分叉证据，不能让后续框架回调覆盖真正原因。

### 4. 执行校验

从对话框资源与专用类连接消息处理、DDX/DDV和模态/非模态唤起，建立界面控件与领域状态的同步边界。 本阶段围绕“对话框的消息处理函数”建立预测：列出参与类、入口函数、宏展开、资源ID、线程或文件状态，再运行最小样例。进入“处理消息”前必须保存首个分叉证据，不能让后续框架回调覆盖真正原因。

### 5. 处理消息

从对话框资源与专用类连接消息处理、DDX/DDV和模态/非模态唤起，建立界面控件与领域状态的同步边界。 本阶段围绕“对话框数据交换与校验（DDX&DDV）”建立预测：列出参与类、入口函数、宏展开、资源ID、线程或文件状态，再运行最小样例。进入“提交或取消”前必须保存首个分叉证据，不能让后续框架回调覆盖真正原因。

### 6. 提交或取消

从对话框资源与专用类连接消息处理、DDX/DDV和模态/非模态唤起，建立界面控件与领域状态的同步边界。 本阶段围绕“如何唤起对话框”建立预测：列出参与类、入口函数、宏展开、资源ID、线程或文件状态，再运行最小样例。进入“设计对话框资源”前必须保存首个分叉证据，不能让后续框架回调覆盖真正原因。

## 核心机制深挖

从对话框资源与专用类连接消息处理、DDX/DDV和模态/非模态唤起，建立界面控件与领域状态的同步边界。 原书绑定Visual C++ 5.0与MFC 4.2。本页保留这个历史技术身份；现代Visual Studio、64位、Unicode、智能指针或新UI框架只可作为明确标注的对照，不能反向改写原书机制。

本页不变量是：**资源ID、控件成员、DDX方向和DDV规则一致；取消不会提交状态，非模态对象生存期覆盖窗口生存期。**。它必须在正常输入、空输入、创建失败、消息未处理、持久化版本不符和中断恢复中都成立。窗口看起来能显示、命令偶尔能执行或文件能写出，都不足以证明对象图、分派路径和资源生存期正确。

最小实验：实现一个笔画设置对话框，分别测试有效输入、边界值、DDV拒绝、Cancel和非模态关闭。 运行前记录工具版本、编译选项、链接方式、资源哈希、输入序列和预期调用路径；运行后保存消息日志、对象关系、文件字节或线程退出状态。

故障实验：注入错误控件ID、反向DDX、栈上非模态对象和绕过UpdateData，检查数据与窗口生命周期。 每次只改变一个条件，停在第一处与预测不同的位置。删除故障后从干净构建或明确逆操作恢复，并以完全相同的输入重放；如果最终画面相同但对象、句柄、线程、Archive或资源状态不同，仍判定失败。

机制证据表至少保留六列：框架表面动作、对应的C++对象或宏、下沉到Win32的入口、可观察状态、首个失败信号、恢复操作。对历史代码还要单列“原书行为”和“现代迁移差异”：前者用于解释MFC 4.2为何这样工作，后者只说明在64位、Unicode或新工具链中怎样验证同一不变量。这样既不会把旧API当成今天的最佳实践，也不会因现代封装更方便而跳过原书真正要揭示的内部机制。

## 最小可重放骨架

```cpp
void CStrokeDialog::DoDataExchange(CDataExchange* dx) {
  CDialog::DoDataExchange(dx);
  DDX_Text(dx, IDC_WIDTH, width_);
  DDV_MinMaxInt(dx, width_, 1, 64);
}
```

```cpp
CStrokeDialog dialog;
if (dialog.DoModal() == IDOK) document.setWidth(dialog.width_);
```

```text
resource controls -> dialog members -> UpdateData(TRUE) -> DDV -> domain commit
Cancel -> no commit
```

## 正式目录逐项讲解

### 第10章 MFC与对话盒

“第10章 MFC与对话盒”在本页用于连接本章术语、输入输出、状态变化和可验证结果。先在“设计对话框资源”阶段标出Dialog资源涉及的对象、线程、资源或表项，写下预期入口、状态变化和退出条件；再推进到“生成专用类”，用调用栈、消息日志、对象图、序列化字节或界面状态验证推断。这里不能只记住向导操作或宏名字：必须说明它替代了哪段Win32/C++机制、失败首先在哪里可见，以及怎样恢复后用同一输入重放。

### 对话框编辑器

“对话框编辑器”在本页用于连接资源控件、对象成员、校验与提交边界。先在“生成专用类”阶段标出ClassWizard涉及的对象、线程、资源或表项，写下预期入口、状态变化和退出条件；再推进到“绑定控件数据”，用调用栈、消息日志、对象图、序列化字节或界面状态验证推断。这里不能只记住向导操作或宏名字：必须说明它替代了哪段Win32/C++机制、失败首先在哪里可见，以及怎样恢复后用同一输入重放。

### 利用ClassWizard连接对话框与其专用类

“利用ClassWizard连接对话框与其专用类”在本页用于定位类层次中的责任、所有权和协作关系。先在“绑定控件数据”阶段标出DDX涉及的对象、线程、资源或表项，写下预期入口、状态变化和退出条件；再推进到“执行校验”，用调用栈、消息日志、对象图、序列化字节或界面状态验证推断。这里不能只记住向导操作或宏名字：必须说明它替代了哪段Win32/C++机制、失败首先在哪里可见，以及怎样恢复后用同一输入重放。

### 对话框的消息处理函数

“对话框的消息处理函数”在本页用于追踪事件从入口到处理器再到退出的分派路径。先在“执行校验”阶段标出DDV涉及的对象、线程、资源或表项，写下预期入口、状态变化和退出条件；再推进到“处理消息”，用调用栈、消息日志、对象图、序列化字节或界面状态验证推断。这里不能只记住向导操作或宏名字：必须说明它替代了哪段Win32/C++机制、失败首先在哪里可见，以及怎样恢复后用同一输入重放。

### 对话框数据交换与校验（DDX&DDV）

“对话框数据交换与校验（DDX&DDV）”在本页用于连接资源控件、对象成员、校验与提交边界。先在“处理消息”阶段标出DoModal涉及的对象、线程、资源或表项，写下预期入口、状态变化和退出条件；再推进到“提交或取消”，用调用栈、消息日志、对象图、序列化字节或界面状态验证推断。这里不能只记住向导操作或宏名字：必须说明它替代了哪段Win32/C++机制、失败首先在哪里可见，以及怎样恢复后用同一输入重放。

### 如何唤起对话框

“如何唤起对话框”在本页用于连接资源控件、对象成员、校验与提交边界。先在“提交或取消”阶段标出Dialog资源涉及的对象、线程、资源或表项，写下预期入口、状态变化和退出条件；再推进到“设计对话框资源”，用调用栈、消息日志、对象图、序列化字节或界面状态验证推断。这里不能只记住向导操作或宏名字：必须说明它替代了哪段Win32/C++机制、失败首先在哪里可见，以及怎样恢复后用同一输入重放。

### 本章回顾

“本章回顾”在本页用于收束本章不变量并组织回归样本。先在“设计对话框资源”阶段标出ClassWizard涉及的对象、线程、资源或表项，写下预期入口、状态变化和退出条件；再推进到“生成专用类”，用调用栈、消息日志、对象图、序列化字节或界面状态验证推断。这里不能只记住向导操作或宏名字：必须说明它替代了哪段Win32/C++机制、失败首先在哪里可见，以及怎样恢复后用同一输入重放。

## 分步视觉验证

<Stepper>
  <Step title="先画对象与控制流">
    标出六阶段涉及的类、窗口、消息表、资源、线程或文件，运行前先预测入口和状态变化。
    <Mfc10DialogsMapLab />
  </Step>
  <Step title="只改变一个故障条件">
    固定工具版本、工程和输入，只切换缺失宏、错误类、未处理消息、Archive不匹配等单一条件。
    <Mfc10DialogsExperimentLab />
  </Step>
  <Step title="用同一输入验收恢复">
    恢复干净构建，重放相同消息和文件输入，比较对象图、路由、输出、资源与退出状态。
    <Mfc10DialogsEvidenceLab />
  </Step>
</Stepper>

## 常见误区与故障注入

<Callout type="trap">
  **误区一：AppWizard生成并能编译，就等于理解MFC。**
  生成代码只是一个样本。必须能沿全局Application对象、Document
  Template、运行时类、消息表和Win32回调解释每个关键动作。
</Callout>

<Callout type="trap">
  **误区二：把现代框架经验直接套回Visual C++ 5.0。** 原书讨论MFC
  4.2、32位Windows和当时的工具链。现代API可帮助迁移，但不能替代对原目录、宏展开和历史行为的忠实解释。
</Callout>

## 四类样本与验收

| 样本 | 注入方式                   | 必查证据                   | 通过条件     |
| ---- | -------------------------- | -------------------------- | ------------ |
| 基线 | 固定工程、资源与输入       | 对象图、调用栈、消息序列   | 与预测一致   |
| 边界 | 空文档、无处理器、极限尺寸 | 默认路径、返回值、资源状态 | 不越界不泄漏 |
| 失败 | 缺宏、创建失败、版本不符   | 首个分叉及责任层           | 失败被隔离   |
| 恢复 | 干净重建后同输入重放       | 文件、窗口、线程与句柄     | 回到基线     |

## 练习

<Exercises>

**问题 1** 为什么不能只看最终窗口验收第10章 MFC与对话盒？

<Answer>
  窗口可能由错误类、错误资源或偶然的默认处理显示出来。必须核对对象创建、消息路由、状态变化、退出路径和恢复后的同输入重放。
</Answer>

**问题 2** 如何把“第10章 MFC与对话盒”变成可执行证据？

<Answer>
  沿“设计对话框资源、生成专用类、绑定控件数据、执行校验、处理消息、提交或取消”固定Visual
  C++/MFC版本、工程、资源和输入，先预测对象与调用路径，再只改变一个条件并保存首错；恢复后比较对象图、消息、文件或线程状态。
</Answer>

**问题 3** 本页最小通过条件是什么？

<Answer>
  资源ID、控件成员、DDX方向和DDV规则一致；取消不会提交状态，非模态对象生存期覆盖窗口生存期。
  失败路径不得污染下一次实验，资源清理明确，同一输入能够重新到达基线。
</Answer>

</Exercises>

## 小结

- Dialog资源：连接“设计对话框资源”的对象、控制流与证据。
- ClassWizard：连接“生成专用类”的对象、控制流与证据。
- DDX：连接“绑定控件数据”的对象、控制流与证据。
- DDV：连接“执行校验”的对象、控制流与证据。
- DoModal：连接“处理消息”的对象、控制流与证据。
- 本页7个目录坐标全部进入机制链，不用泛化主题替代原书章节。

## 术语表

<Glossary>
  <GlossaryItem term="Dialog资源">
    Dialog资源用于解释“设计对话框资源”：既要指出框架表面接口，也要还原其Win32/C++基础、状态变化、失败信号和恢复方法。
  </GlossaryItem>
  <GlossaryItem term="ClassWizard">
    ClassWizard用于解释“生成专用类”：既要指出框架表面接口，也要还原其Win32/C++基础、状态变化、失败信号和恢复方法。
  </GlossaryItem>
  <GlossaryItem term="DDX">
    DDX用于解释“绑定控件数据”：既要指出框架表面接口，也要还原其Win32/C++基础、状态变化、失败信号和恢复方法。
  </GlossaryItem>
  <GlossaryItem term="DDV">
    DDV用于解释“执行校验”：既要指出框架表面接口，也要还原其Win32/C++基础、状态变化、失败信号和恢复方法。
  </GlossaryItem>
  <GlossaryItem term="DoModal">
    DoModal用于解释“处理消息”：既要指出框架表面接口，也要还原其Win32/C++基础、状态变化、失败信号和恢复方法。
  </GlossaryItem>
</Glossary>

<Attribution
  adaptedFrom="侯俊杰（侯捷） · 深入浅出MFC（第二版） · Visual C++ 5.0 / MFC 4.2 · 完整目录独立教学重构"
  adaptedUrl="https://m.dushu.com/book/10076731/"
/>
