---
title: "第12章 打印与预览"
type: C
section: "第四篇 · 深入MFC程序设计"
order: 13
description: "从Windows打印后台和MFC默认机制进入Scribble增强、设备页/逻辑页、映射模式、分页、页眉页脚、页码与预览。 覆盖13个本页目录坐标。"
demo: true
math: false
sourceUrl: "https://m.dushu.com/book/10076731/"
draft: false
---

import {
  Mfc12PrintPreviewMapLab,
  Mfc12PrintPreviewExperimentLab,
  Mfc12PrintPreviewEvidenceLab,
} from "@/components/mdx/mfc-deep-dive/diagrams/mfc-12-print-preview";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第12章 打印与预览的核心对象、控制流和它在全书中的位置
- 能描述并沿“准备打印任务 → 查询设备能力 → 设置映射方式 → 计算分页 → 绘制页眉正文 → 预览并结束”恢复隐藏在宏与向导后的真实机制
- 能实现一个固定版本、可重放、可故障注入的最小MFC实验
- 能比较基线、边界、失败与恢复轨迹，并用不变量而非截图验收

</Objectives>

## 直觉起点

<Term def="打印后台连接准备打印任务阶段的对象责任、调用路径与恢复证据。">
  打印后台
</Term>
、
<Term def="CPrintInfo连接查询设备能力阶段的对象责任、调用路径与恢复证据。">
  CPrintInfo
</Term>
、
<Term def="映射方式连接设置映射方式阶段的对象责任、调用路径与恢复证据。">
  映射方式
</Term>
、<Term def="分页连接计算分页阶段的对象责任、调用路径与恢复证据。">分页</Term>、
<Term def="Print Preview连接绘制页眉正文阶段的对象责任、调用路径与恢复证据。">
  Print Preview
</Term>
共同构成本页坐标。从Windows打印后台和MFC默认机制进入Scribble增强、设备页/逻辑页、映射模式、分页、页眉页脚、页码与预览。

阅读MFC代码要同时保留三层视图：最上层是AppWizard生成类和宏，中间层是MFC类层次、运行时表与回调，底层是Win32窗口、消息、句柄、线程和文件。只会点击向导或背宏，无法解释对象为何诞生、消息为何到达、文件为何能重建动态类型。

<Mfc12PrintPreviewMapLab />

## 六阶段机制链

### 1. 准备打印任务

从Windows打印后台和MFC默认机制进入Scribble增强、设备页/逻辑页、映射模式、分页、页眉页脚、页码与预览。 本阶段围绕“第12章 打印与预览”建立预测：列出参与类、入口函数、宏展开、资源ID、线程或文件状态，再运行最小样例。进入“查询设备能力”前必须保存首个分叉证据，不能让后续框架回调覆盖真正原因。

### 2. 查询设备能力

从Windows打印后台和MFC默认机制进入Scribble增强、设备页/逻辑页、映射模式、分页、页眉页脚、页码与预览。 本阶段围绕“概述”建立预测：列出参与类、入口函数、宏展开、资源ID、线程或文件状态，再运行最小样例。进入“设置映射方式”前必须保存首个分叉证据，不能让后续框架回调覆盖真正原因。

### 3. 设置映射方式

从Windows打印后台和MFC默认机制进入Scribble增强、设备页/逻辑页、映射模式、分页、页眉页脚、页码与预览。 本阶段围绕“打印操作的后台原理”建立预测：列出参与类、入口函数、宏展开、资源ID、线程或文件状态，再运行最小样例。进入“计算分页”前必须保存首个分叉证据，不能让后续框架回调覆盖真正原因。

### 4. 计算分页

从Windows打印后台和MFC默认机制进入Scribble增强、设备页/逻辑页、映射模式、分页、页眉页脚、页码与预览。 本阶段围绕“MFC默认的打印机制”建立预测：列出参与类、入口函数、宏展开、资源ID、线程或文件状态，再运行最小样例。进入“绘制页眉正文”前必须保存首个分叉证据，不能让后续框架回调覆盖真正原因。

### 5. 绘制页眉正文

从Windows打印后台和MFC默认机制进入Scribble增强、设备页/逻辑页、映射模式、分页、页眉页脚、页码与预览。 本阶段围绕“Scribble打印机制的增强”建立预测：列出参与类、入口函数、宏展开、资源ID、线程或文件状态，再运行最小样例。进入“预览并结束”前必须保存首个分叉证据，不能让后续框架回调覆盖真正原因。

### 6. 预览并结束

从Windows打印后台和MFC默认机制进入Scribble增强、设备页/逻辑页、映射模式、分页、页眉页脚、页码与预览。 本阶段围绕“打印机的页和文件的页”建立预测：列出参与类、入口函数、宏展开、资源ID、线程或文件状态，再运行最小样例。进入“准备打印任务”前必须保存首个分叉证据，不能让后续框架回调覆盖真正原因。

## 核心机制深挖

从Windows打印后台和MFC默认机制进入Scribble增强、设备页/逻辑页、映射模式、分页、页眉页脚、页码与预览。 原书绑定Visual C++ 5.0与MFC 4.2。本页保留这个历史技术身份；现代Visual Studio、64位、Unicode、智能指针或新UI框架只可作为明确标注的对照，不能反向改写原书机制。

本页不变量是：**屏幕、打印与预览共享文档语义；设备上下文、映射模式、页范围和资源成对建立释放，分页结果可预测。**。它必须在正常输入、空输入、创建失败、消息未处理、持久化版本不符和中断恢复中都成立。窗口看起来能显示、命令偶尔能执行或文件能写出，都不足以证明对象图、分派路径和资源生存期正确。

最小实验：以固定笔画文档在两种纸张和缩放下打印到虚拟设备，比较页数、边距、坐标与预览。 运行前记录工具版本、编译选项、链接方式、资源哈希、输入序列和预期调用路径；运行后保存消息日志、对象关系、文件字节或线程退出状态。

故障实验：注入零尺寸页面、错误映射模式、页码越界和未恢复GDI对象，检查预览与实际打印差异。 每次只改变一个条件，停在第一处与预测不同的位置。删除故障后从干净构建或明确逆操作恢复，并以完全相同的输入重放；如果最终画面相同但对象、句柄、线程、Archive或资源状态不同，仍判定失败。

机制证据表至少保留六列：框架表面动作、对应的C++对象或宏、下沉到Win32的入口、可观察状态、首个失败信号、恢复操作。对历史代码还要单列“原书行为”和“现代迁移差异”：前者用于解释MFC 4.2为何这样工作，后者只说明在64位、Unicode或新工具链中怎样验证同一不变量。这样既不会把旧API当成今天的最佳实践，也不会因现代封装更方便而跳过原书真正要揭示的内部机制。

## 最小可重放骨架

```cpp
BOOL CScribbleView::OnPreparePrinting(CPrintInfo* info) {
  info->SetMaxPage(computePageCount());
  return DoPreparePrinting(info);
}
```

```cpp
void CScribbleView::OnPrint(CDC* dc, CPrintInfo* info) {
  configureMappingMode(*dc); drawHeader(*dc, info->m_nCurPage); drawPage(*dc, info->m_nCurPage);
}
```

```text
device pixels -> physical page -> logical mapping -> document page -> header/footer
preview and printer share pagination
```

## 正式目录逐项讲解

### 第12章 打印与预览

“第12章 打印与预览”在本页用于说明设备上下文、坐标、分页和输出证据。先在“准备打印任务”阶段标出打印后台涉及的对象、线程、资源或表项，写下预期入口、状态变化和退出条件；再推进到“查询设备能力”，用调用栈、消息日志、对象图、序列化字节或界面状态验证推断。这里不能只记住向导操作或宏名字：必须说明它替代了哪段Win32/C++机制、失败首先在哪里可见，以及怎样恢复后用同一输入重放。

### 概述

“概述”在本页用于划定概念边界、前置条件和反例。先在“查询设备能力”阶段标出CPrintInfo涉及的对象、线程、资源或表项，写下预期入口、状态变化和退出条件；再推进到“设置映射方式”，用调用栈、消息日志、对象图、序列化字节或界面状态验证推断。这里不能只记住向导操作或宏名字：必须说明它替代了哪段Win32/C++机制、失败首先在哪里可见，以及怎样恢复后用同一输入重放。

### 打印操作的后台原理

“打印操作的后台原理”在本页用于说明设备上下文、坐标、分页和输出证据。先在“设置映射方式”阶段标出映射方式涉及的对象、线程、资源或表项，写下预期入口、状态变化和退出条件；再推进到“计算分页”，用调用栈、消息日志、对象图、序列化字节或界面状态验证推断。这里不能只记住向导操作或宏名字：必须说明它替代了哪段Win32/C++机制、失败首先在哪里可见，以及怎样恢复后用同一输入重放。

### MFC默认的打印机制

“MFC默认的打印机制”在本页用于说明设备上下文、坐标、分页和输出证据。先在“计算分页”阶段标出分页涉及的对象、线程、资源或表项，写下预期入口、状态变化和退出条件；再推进到“绘制页眉正文”，用调用栈、消息日志、对象图、序列化字节或界面状态验证推断。这里不能只记住向导操作或宏名字：必须说明它替代了哪段Win32/C++机制、失败首先在哪里可见，以及怎样恢复后用同一输入重放。

### Scribble打印机制的增强

“Scribble打印机制的增强”在本页用于把抽象机制落到可运行对象图和调用轨迹。先在“绘制页眉正文”阶段标出Print Preview涉及的对象、线程、资源或表项，写下预期入口、状态变化和退出条件；再推进到“预览并结束”，用调用栈、消息日志、对象图、序列化字节或界面状态验证推断。这里不能只记住向导操作或宏名字：必须说明它替代了哪段Win32/C++机制、失败首先在哪里可见，以及怎样恢复后用同一输入重放。

### 打印机的页和文件的页

“打印机的页和文件的页”在本页用于核对持久化格式、动态类型、读写对称与版本恢复。先在“预览并结束”阶段标出打印后台涉及的对象、线程、资源或表项，写下预期入口、状态变化和退出条件；再推进到“准备打印任务”，用调用栈、消息日志、对象图、序列化字节或界面状态验证推断。这里不能只记住向导操作或宏名字：必须说明它替代了哪段Win32/C++机制、失败首先在哪里可见，以及怎样恢复后用同一输入重放。

### 配置GDI绘图工具

“配置GDI绘图工具”在本页用于说明设备上下文、坐标、分页和输出证据。先在“准备打印任务”阶段标出CPrintInfo涉及的对象、线程、资源或表项，写下预期入口、状态变化和退出条件；再推进到“查询设备能力”，用调用栈、消息日志、对象图、序列化字节或界面状态验证推断。这里不能只记住向导操作或宏名字：必须说明它替代了哪段Win32/C++机制、失败首先在哪里可见，以及怎样恢复后用同一输入重放。

### 尺寸与方向：关于映射方式（坐标系统）

“尺寸与方向：关于映射方式（坐标系统）”在本页用于说明设备上下文、坐标、分页和输出证据。先在“查询设备能力”阶段标出映射方式涉及的对象、线程、资源或表项，写下预期入口、状态变化和退出条件；再推进到“设置映射方式”，用调用栈、消息日志、对象图、序列化字节或界面状态验证推断。这里不能只记住向导操作或宏名字：必须说明它替代了哪段Win32/C++机制、失败首先在哪里可见，以及怎样恢复后用同一输入重放。

### 分页

“分页”在本页用于说明设备上下文、坐标、分页和输出证据。先在“设置映射方式”阶段标出分页涉及的对象、线程、资源或表项，写下预期入口、状态变化和退出条件；再推进到“计算分页”，用调用栈、消息日志、对象图、序列化字节或界面状态验证推断。这里不能只记住向导操作或宏名字：必须说明它替代了哪段Win32/C++机制、失败首先在哪里可见，以及怎样恢复后用同一输入重放。

### 页眉与页脚

“页眉与页脚”在本页用于说明设备上下文、坐标、分页和输出证据。先在“计算分页”阶段标出Print Preview涉及的对象、线程、资源或表项，写下预期入口、状态变化和退出条件；再推进到“绘制页眉正文”，用调用栈、消息日志、对象图、序列化字节或界面状态验证推断。这里不能只记住向导操作或宏名字：必须说明它替代了哪段Win32/C++机制、失败首先在哪里可见，以及怎样恢复后用同一输入重放。

### 动态计算页码

“动态计算页码”在本页用于说明设备上下文、坐标、分页和输出证据。先在“绘制页眉正文”阶段标出打印后台涉及的对象、线程、资源或表项，写下预期入口、状态变化和退出条件；再推进到“预览并结束”，用调用栈、消息日志、对象图、序列化字节或界面状态验证推断。这里不能只记住向导操作或宏名字：必须说明它替代了哪段Win32/C++机制、失败首先在哪里可见，以及怎样恢复后用同一输入重放。

### 打印预览（Print Preview）

“打印预览（Print Preview）”在本页用于说明设备上下文、坐标、分页和输出证据。先在“预览并结束”阶段标出CPrintInfo涉及的对象、线程、资源或表项，写下预期入口、状态变化和退出条件；再推进到“准备打印任务”，用调用栈、消息日志、对象图、序列化字节或界面状态验证推断。这里不能只记住向导操作或宏名字：必须说明它替代了哪段Win32/C++机制、失败首先在哪里可见，以及怎样恢复后用同一输入重放。

### 本章回顾

“本章回顾”在本页用于收束本章不变量并组织回归样本。先在“准备打印任务”阶段标出映射方式涉及的对象、线程、资源或表项，写下预期入口、状态变化和退出条件；再推进到“查询设备能力”，用调用栈、消息日志、对象图、序列化字节或界面状态验证推断。这里不能只记住向导操作或宏名字：必须说明它替代了哪段Win32/C++机制、失败首先在哪里可见，以及怎样恢复后用同一输入重放。

## 分步视觉验证

<Stepper>
  <Step title="先画对象与控制流">
    标出六阶段涉及的类、窗口、消息表、资源、线程或文件，运行前先预测入口和状态变化。
    <Mfc12PrintPreviewMapLab />
  </Step>
  <Step title="只改变一个故障条件">
    固定工具版本、工程和输入，只切换缺失宏、错误类、未处理消息、Archive不匹配等单一条件。
    <Mfc12PrintPreviewExperimentLab />
  </Step>
  <Step title="用同一输入验收恢复">
    恢复干净构建，重放相同消息和文件输入，比较对象图、路由、输出、资源与退出状态。
    <Mfc12PrintPreviewEvidenceLab />
  </Step>
</Stepper>

## 常见误区与故障注入

<Callout type="trap">
  **误区一：AppWizard生成并能编译，就等于理解MFC。**
  生成代码只是一个样本。必须能沿全局Application对象、Document
  Template、运行时类、消息表和Win32回调解释每个关键动作。
</Callout>

<Callout type="trap">
  **误区二：把现代框架经验直接套回Visual C++ 5.0。** 原书讨论MFC
  4.2、32位Windows和当时的工具链。现代API可帮助迁移，但不能替代对原目录、宏展开和历史行为的忠实解释。
</Callout>

## 四类样本与验收

| 样本 | 注入方式                   | 必查证据                   | 通过条件     |
| ---- | -------------------------- | -------------------------- | ------------ |
| 基线 | 固定工程、资源与输入       | 对象图、调用栈、消息序列   | 与预测一致   |
| 边界 | 空文档、无处理器、极限尺寸 | 默认路径、返回值、资源状态 | 不越界不泄漏 |
| 失败 | 缺宏、创建失败、版本不符   | 首个分叉及责任层           | 失败被隔离   |
| 恢复 | 干净重建后同输入重放       | 文件、窗口、线程与句柄     | 回到基线     |

## 练习

<Exercises>

**问题 1** 为什么不能只看最终窗口验收第12章 打印与预览？

<Answer>
  窗口可能由错误类、错误资源或偶然的默认处理显示出来。必须核对对象创建、消息路由、状态变化、退出路径和恢复后的同输入重放。
</Answer>

**问题 2** 如何把“第12章 打印与预览”变成可执行证据？

<Answer>
  沿“准备打印任务、查询设备能力、设置映射方式、计算分页、绘制页眉正文、预览并结束”固定Visual
  C++/MFC版本、工程、资源和输入，先预测对象与调用路径，再只改变一个条件并保存首错；恢复后比较对象图、消息、文件或线程状态。
</Answer>

**问题 3** 本页最小通过条件是什么？

<Answer>
  屏幕、打印与预览共享文档语义；设备上下文、映射模式、页范围和资源成对建立释放，分页结果可预测。
  失败路径不得污染下一次实验，资源清理明确，同一输入能够重新到达基线。
</Answer>

</Exercises>

## 小结

- 打印后台：连接“准备打印任务”的对象、控制流与证据。
- CPrintInfo：连接“查询设备能力”的对象、控制流与证据。
- 映射方式：连接“设置映射方式”的对象、控制流与证据。
- 分页：连接“计算分页”的对象、控制流与证据。
- Print Preview：连接“绘制页眉正文”的对象、控制流与证据。
- 本页13个目录坐标全部进入机制链，不用泛化主题替代原书章节。

## 术语表

<Glossary>
  <GlossaryItem term="打印后台">
    打印后台用于解释“准备打印任务”：既要指出框架表面接口，也要还原其Win32/C++基础、状态变化、失败信号和恢复方法。
  </GlossaryItem>
  <GlossaryItem term="CPrintInfo">
    CPrintInfo用于解释“查询设备能力”：既要指出框架表面接口，也要还原其Win32/C++基础、状态变化、失败信号和恢复方法。
  </GlossaryItem>
  <GlossaryItem term="映射方式">
    映射方式用于解释“设置映射方式”：既要指出框架表面接口，也要还原其Win32/C++基础、状态变化、失败信号和恢复方法。
  </GlossaryItem>
  <GlossaryItem term="分页">
    分页用于解释“计算分页”：既要指出框架表面接口，也要还原其Win32/C++基础、状态变化、失败信号和恢复方法。
  </GlossaryItem>
  <GlossaryItem term="Print Preview">
    Print
    Preview用于解释“绘制页眉正文”：既要指出框架表面接口，也要还原其Win32/C++基础、状态变化、失败信号和恢复方法。
  </GlossaryItem>
</Glossary>

<Attribution
  adaptedFrom="侯俊杰（侯捷） · 深入浅出MFC（第二版） · Visual C++ 5.0 / MFC 4.2 · 完整目录独立教学重构"
  adaptedUrl="https://m.dushu.com/book/10076731/"
/>
