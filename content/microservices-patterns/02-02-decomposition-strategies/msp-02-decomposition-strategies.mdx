---
title: "第2章 服务的拆分策略"
description: "依据Chris Richardson英文初版与喻勇译2019中文版完整目录：从系统操作、业务能力与DDD子域推导服务边界，并用内聚、耦合和API契约校验拆分结果"
type: C
section: "微服务架构设计模式（2019中文版初版 / 2018英文初版）"
order: 2
draft: false
math: false
sourceUrl: "https://livebook.manning.com/book/microservices-patterns/table-of-contents"
demo: true
---

import { Objectives } from "@/components/mdx/objectives";
import { Term } from "@/components/mdx/term";
import { Callout } from "@/components/mdx/callout";
import { Answer, Exercises } from "@/components/mdx/exercises";
import { Glossary, GlossaryItem } from "@/components/mdx/glossary";
import { Attribution } from "@/components/mdx/attribution";

# 第2章 服务的拆分策略

本课程对应Chris Richardson著、喻勇译《微服务架构设计模式》，机械工业出版社2019年出版，ISBN 9787111624127，馆藏著录为前置26页、正文455页。英文原版为Manning 2018年10月初版，ISBN 9781617294549、520页。课程固定该初版的13章与44个模式，不混入当前第二版MEAP重新编排和新增的章节。

全书权威分母为13章、52个二级节、177个三级节，共242个编号目录节点；另设学习地图和总复习，共15页、45个交互视图和90道独立复习题。本章覆盖2个二级节与9个三级节，连同章标题共12个编号节点。

<Objectives>

- 能解释“第2章 服务的拆分策略”的正式目录节点，并把每个模式写成问题、约束、解决方案、结果与相关模式。
- 能绘制FTGO主体、服务、数据所有权、同步/异步边界和业务完成点，区分局部成功与最终业务成功。
- 能比较正常、超时、重复、乱序、部分失败与恢复轨迹，并用状态、遥测和业务对账验证“每个系统操作都有明确入口、责任服务和数据所有者；跨服务协作不绕过API，边界变化不要求无关服务同步发布”。
- 能设计系统操作清单、业务能力地图、子域边界图、服务API契约，写出停止、恢复、回退和独立交接条件。

</Objectives>

## 从一个可证伪的问题开始

先预测：按类、表或技术层拆分会让一个业务变化横跨多个服务并共享数据，团队无法独立交付，网络调用只放大原有耦合。把预测写成可观察反例，再决定是否采用模式。模式不是技术标签，而是在特定约束中解决反复出现问题的决策；结果部分既包含收益，也包含采用后必须处理的新问题。

FTGO是全书贯穿案例。读者要沿同一订单标识追踪消费者、餐厅、厨房、配送、信用和通知状态，明确哪个服务拥有哪份事实、谁可修改、消息何时可重放、查询允许多旧、测试在哪层证明、发布如何停止和回退。验收不变量是：每个系统操作都有明确入口、责任服务和数据所有者；跨服务协作不绕过API，边界变化不要求无关服务同步发布。

## 核心词汇与初版边界

<Term def="软件架构是“第2章 服务的拆分策略”的第1个核心概念，必须同时说明问题、约束、机制、代价、失败反例和可观测证据">软件架构</Term>、<Term def="业务能力是“第2章 服务的拆分策略”的第2个核心概念，必须同时说明问题、约束、机制、代价、失败反例和可观测证据">业务能力</Term>、<Term def="子域是“第2章 服务的拆分策略”的第3个核心概念，必须同时说明问题、约束、机制、代价、失败反例和可观测证据">子域</Term>、<Term def="聚合是“第2章 服务的拆分策略”的第4个核心概念，必须同时说明问题、约束、机制、代价、失败反例和可观测证据">聚合</Term>、<Term def="系统操作是“第2章 服务的拆分策略”的第5个核心概念，必须同时说明问题、约束、机制、代价、失败反例和可观测证据">系统操作</Term>

以上词汇都要回答六个问题：解决什么问题、受什么约束、状态由谁持有、失败如何传播、怎样恢复、用什么证据证明。2018/2019初版之后出现的第二版结构、云产品行为和模式补充只能作为迁移差异，不能改写本页正式目录。

## 原书目录逐节点重构

### 2.1 微服务架构到底是什么

**正式节点 1/2。** 用4+1视图区分逻辑、实现、进程和部署结构；微服务首先约束组件关系和部署单元，而不是指定某个框架、容器或协议。

本节的三级目录如下，均在本页展开而不合并成泛化主题：

- 2.1.1 什么是软件架构以及它为什么重要
- 2.1.2 架构风格概述
- 2.1.3 微服务架构是一种架构风格

把“2.1 微服务架构到底是什么”放回“从系统操作、业务能力与DDD子域推导服务边界，并用内聚、耦合和API契约校验拆分结果”这条责任链：输入来自“本章输入与版本契约”，输出进入“2.2 为应用程序定义微服务架构”。先写主体、拥有的数据、命令或查询、同步与异步边界、可见完成点，再画正常、超时、重复、乱序、部分失败和恢复轨迹。不能用框架名称替代协议语义，也不能用组件健康替代“每个系统操作都有明确入口、责任服务和数据所有者；跨服务协作不绕过API，边界变化不要求无关服务同步发布”。

实验固定FTGO业务样本、初版代码语义、数据、负载和观测窗口，只改变一个架构变量或故障。比较状态转移、P50/P95/P99、错误、队列滞后、重试放大、恢复时间和最终业务集合；若只有HTTP成功、消息已确认、Pod就绪或函数返回，而没有持久状态与业务对账，本节仍未通过。

### 2.2 为应用程序定义微服务架构

**正式节点 2/2。** 先从关键请求生成抽象领域模型和系统操作，再比较业务能力与DDD子域两条拆分路线；最后以单一职责、共同闭包、数据所有权和协作成本校验。

本节的三级目录如下，均在本页展开而不合并成泛化主题：

- 2.2.1 识别系统操作
- 2.2.2 应用按业务能力拆分模式定义服务
- 2.2.3 应用按子域拆分模式定义服务
- 2.2.4 拆分指导原则
- 2.2.5 将应用拆分为服务的障碍
- 2.2.6 定义服务API

把“2.2 为应用程序定义微服务架构”放回“从系统操作、业务能力与DDD子域推导服务边界，并用内聚、耦合和API契约校验拆分结果”这条责任链：输入来自“2.1 微服务架构到底是什么”，输出进入“本章证据门与跨章连接”。先写主体、拥有的数据、命令或查询、同步与异步边界、可见完成点，再画正常、超时、重复、乱序、部分失败和恢复轨迹。不能用框架名称替代协议语义，也不能用组件健康替代“每个系统操作都有明确入口、责任服务和数据所有者；跨服务协作不绕过API，边界变化不要求无关服务同步发布”。

实验固定FTGO业务样本、初版代码语义、数据、负载和观测窗口，只改变一个架构变量或故障。比较状态转移、P50/P95/P99、错误、队列滞后、重试放大、恢复时间和最终业务集合；若只有HTTP成功、消息已确认、Pod就绪或函数返回，而没有持久状态与业务对账，本节仍未通过。

## 模式决策矩阵

| 模式或机制     | 要解决的问题            | 核心机制                               | 最小证据               |
| -------------- | ----------------------- | -------------------------------------- | ---------------------- |
| 按业务能力拆分 | 组织需要稳定的业务责任  | 围绕业务能力形成服务和团队             | 变化是否局限在一个能力 |
| 按子域拆分     | 复杂领域需要模型边界    | 用核心、支撑、通用子域与限界上下文划界 | 术语一致性与模型泄漏   |
| 系统操作       | 需求叙述无法直接给出API | 从命令和查询识别输入、结果与不变量     | 操作到服务的完整映射   |
| 绞杀式校准     | 初始边界必然不完美      | 保留可迁移契约并按证据调整边界         | 跨边界变更和调用趋势   |

选择模式时先写“未采用会怎样”，再写采用后的新成本。例如同步RPC需要超时、断路器和发现；异步消息需要事务性发件箱、顺序、去重和死信；Saga需要补偿和隔离对策；CQRS需要视图水位与重建。相关模式组成因果链，不能独立打勾。

## 机制、状态与不变量

### 责任与数据所有权

服务只能通过API或消息公开能力，数据表、聚合事件流和查询视图都有唯一写入责任。一个业务命令可跨越多个服务，但每一步本地事务、发出的消息、预期回复、补偿和终态都必须可追踪。共享数据库、跨库联表和绕过API的“临时读取”会让边界失真，必须记录退出日期和删除证据。

### 完成点与失败语义

客户端收到响应、网关完成组合、消息代理确认、消费者写库、Saga进入终态、CQRS视图追上水位、Pod通过就绪和业务对账完成是不同检查点。超时表示结果未知，不等于失败；至少一次消息表示可能重复，不等于恰好一次业务副作用。恢复从持久事实继续，而不是从易失内存猜测。

### 可用性与放大效应

串行依赖近似满足 $A_{path}=\prod_i A_i$。每次失败都重试 $r$ 次时，最坏尝试量接近 $N(1+r)$；多层重试会相乘而非相加。该估算只用于发现方向性风险，生产结论还必须固定业务、数据、负载、版本和观测窗口，测量P50/P95/P99、错误、饱和度、队列滞后、恢复时间和最终业务集合。

## 可复现实现与证据格式

```yaml
decision:
  book: microservices-patterns-first-edition
  unit: msp-02-decomposition-strategies
  problem: "从系统操作、业务能力与DDD子域推导服务边界，并用内聚、耦合和API契约校验拆分结果"
  invariant: business-state-reconciled
  observe: [contract-version, state, trace, latency-p99, queue-lag, business]
  stop: [error-budget-exhausted, invariant-broken]
  rollback: verified-baseline
```

```java
// 业务状态与待发消息在同一个本地事务中提交；发布器可重试，消费者按消息ID幂等。
@Transactional
Result handle(Command command) {
  Aggregate aggregate = repository.load(command.aggregateId());
  Events events = aggregate.decide(command);
  repository.save(aggregate, events.expectedVersion());
  outbox.append(events.withCorrelation(command.correlationId()));
  return Result.accepted(aggregate.id(), aggregate.version());
}
```

```text
experiment = fixed(ftgo-case, data, load, version, observation-window)
           + change(one boundary | failure | retry | ordering variable)
           + observe(api + message + state + telemetry + business result)
           + reconcile(expected invariant, durable terminal state)
```

<Callout type="trap" title="模式清单陷阱">
  把Saga、CQRS、API
  Gateway、容器等名称列出来并不等于设计。必须说明前驱问题、约束、状态所有者、结果与后继模式，否则无法判断是否适用。
</Callout>

<Callout type="trap" title="恰好一次陷阱">
  网络重试和至少一次消息都可能重复。代理确认、HTTP成功和消费者返回不能证明业务副作用恰好一次；必须用业务键、幂等记录与最终状态对账。
</Callout>

<Callout type="warning" title="隔离实验">
  故障注入、补偿、消息回放、数据库迁移、切流和权限轮换只在隔离环境演练。先保留基线，规定影响上界、停止条件、恢复步骤和回退验收。
</Callout>

## 独立交接与跨章连接

交接包至少包含系统操作清单、业务能力地图、子域边界图、服务API契约、初版目录映射、服务和数据所有权、API/事件模式、关联标识、失败状态机、测试层级、遥测查询、业务对账、停止与回退。另一位维护者必须能在无口头补充的情况下重放一个成功案例、一个超时未知案例、一个重复案例和一个恢复案例。

本页输出不是终点。它会成为后继模式的输入：边界产生通信，通信产生一致性与查询问题，分布式行为产生测试与可观测需求，运行单元产生部署问题，已有单体产生迁移问题。若后继问题没有负责人和证据门，当前选择尚未完成。

## 本章回顾

重新完成“从系统操作、业务能力与DDD子域推导服务边界，并用内聚、耦合和API契约校验拆分结果”：从初版目录和FTGO问题开始，明确主体、数据与契约，推导模式并记录收益和代价；再通过正常、超时、重复、乱序、部分失败和恢复实验，验证“每个系统操作都有明确入口、责任服务和数据所有者；跨服务协作不绕过API，边界变化不要求无关服务同步发布”。只有目录节点、机制轨迹和最终业务状态均可独立复现，本页才算完成。

## 复习与运行验收

<Exercises>
**问题 1**：本页为什么必须逐项覆盖正式目录？
<Answer>目录节点共同组成“从系统操作、业务能力与DDD子域推导服务边界，并用内聚、耦合和API契约校验拆分结果”的完整因果链；漏项会使系统操作清单、业务能力地图、子域边界图、服务API契约无法独立复现，并可能隐藏采用模式后出现的后继问题。</Answer>

**问题 2**：最小业务不变量是什么？

<Answer>
  每个系统操作都有明确入口、责任服务和数据所有者；跨服务协作不绕过API，边界变化不要求无关服务同步发布。组件状态只能作为中间证据，最终还要核对用户可见结果和持久事实。
</Answer>

**问题 3**：怎样构造最小反例？

<Answer>
  固定FTGO案例、数据、负载、版本与观测窗口，只改变一个边界、故障、重试或顺序变量，比较组件轨迹与最终业务集合。
</Answer>

**问题 4**：为什么不能混入第二版目录？

<Answer>
  本课程对应2018英文初版和2019中文版；第二版重新组织并新增内容，只能作为迁移差异，不能改变242个初版编号节点的分母。
</Answer>

**问题 5**：性能与恢复结论需要哪些证据？

<Answer>
  需要契约版本、日志、追踪、指标、消息偏移、状态快照、P50/P95/P99、错误、饱和度、恢复时间和最终业务对账。
</Answer>

**问题 6**：独立交接需要什么？

<Answer>需要初版目录映射、决策记录、系统操作清单、业务能力地图、子域边界图、服务API契约、故障反例、测试、业务对账、停止、恢复、回退与责任人。</Answer>
</Exercises>

<Glossary>
  <GlossaryItem term="软件架构">
    软件架构在本页不是孤立名词；它必须服务于“每个系统操作都有明确入口、责任服务和数据所有者；跨服务协作不绕过API，边界变化不要求无关服务同步发布”，并能由版本、契约、状态轨迹、故障注入和最终业务对账独立证明。
  </GlossaryItem>
  <GlossaryItem term="业务能力">
    业务能力在本页不是孤立名词；它必须服务于“每个系统操作都有明确入口、责任服务和数据所有者；跨服务协作不绕过API，边界变化不要求无关服务同步发布”，并能由版本、契约、状态轨迹、故障注入和最终业务对账独立证明。
  </GlossaryItem>
  <GlossaryItem term="子域">
    子域在本页不是孤立名词；它必须服务于“每个系统操作都有明确入口、责任服务和数据所有者；跨服务协作不绕过API，边界变化不要求无关服务同步发布”，并能由版本、契约、状态轨迹、故障注入和最终业务对账独立证明。
  </GlossaryItem>
  <GlossaryItem term="聚合">
    聚合在本页不是孤立名词；它必须服务于“每个系统操作都有明确入口、责任服务和数据所有者；跨服务协作不绕过API，边界变化不要求无关服务同步发布”，并能由版本、契约、状态轨迹、故障注入和最终业务对账独立证明。
  </GlossaryItem>
  <GlossaryItem term="系统操作">
    系统操作在本页不是孤立名词；它必须服务于“每个系统操作都有明确入口、责任服务和数据所有者；跨服务协作不绕过API，边界变化不要求无关服务同步发布”，并能由版本、契约、状态轨迹、故障注入和最终业务对账独立证明。
  </GlossaryItem>
</Glossary>

[← 上一页：第1章 逃离单体地狱](/learn/microservices-patterns/01-01-escaping-monolithic-hell/msp-01-escaping-monolithic-hell) · [下一页：第3章 微服务架构中的进程间通信 →](/learn/microservices-patterns/03-03-interprocess-communication/msp-03-interprocess-communication)

<Attribution
  adaptedFrom="Chris Richardson《Microservices Patterns》2018英文初版 / 喻勇译2019中文版"
  adaptedUrl="https://www.manning.com/books/microservices-patterns"
/>
