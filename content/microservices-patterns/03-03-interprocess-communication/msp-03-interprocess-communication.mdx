---
title: "第3章 微服务架构中的进程间通信"
description: "依据Chris Richardson英文初版与喻勇译2019中文版完整目录：比较同步远程调用与异步消息，建立可演化API、部分失败、发现、顺序、去重和事务性消息的完整通信契约"
type: C
section: "微服务架构设计模式（2019中文版初版 / 2018英文初版）"
order: 3
draft: true
math: false
sourceUrl: "https://livebook.manning.com/book/microservices-patterns/table-of-contents"
demo: true
---

import {
  Msp03InterprocessCommunicationPatternLab,
  Msp03InterprocessCommunicationFailureLab,
  Msp03InterprocessCommunicationEvidenceLab,
} from "@/components/mdx/microservices-patterns/diagrams/msp-03-interprocess-communication";

# 第3章 微服务架构中的进程间通信

本课程对应Chris Richardson著、喻勇译《微服务架构设计模式》，机械工业出版社2019年出版，ISBN 9787111624127，馆藏著录为前置26页、正文455页。英文原版为Manning 2018年10月初版，ISBN 9781617294549、520页。课程固定该初版的13章与44个模式，不混入当前第二版MEAP重新编排和新增的章节。

全书权威分母为13章、52个二级节、177个三级节，共242个编号目录节点；另设学习地图和总复习，共15页、45个交互视图和90道独立复习题。本章覆盖4个二级节与18个三级节，连同章标题共23个编号节点。

<Objectives>

- 能解释“第3章 微服务架构中的进程间通信”的正式目录节点，并把每个模式写成问题、约束、解决方案、结果与相关模式。
- 能绘制FTGO主体、服务、数据所有权、同步/异步边界和业务完成点，区分局部成功与最终业务成功。
- 能比较正常、超时、重复、乱序、部分失败与恢复轨迹，并用状态、遥测和业务对账验证“每次跨进程交互都明确交互风格、契约版本、超时、幂等、顺序和交付语义；数据库提交与消息发布之间不存在不可解释的丢失窗口”。
- 能设计交互风格矩阵、API演化契约、消息时序图、发件箱对账表，写出停止、恢复、回退和独立交接条件。

</Objectives>

## 从一个可证伪的问题开始

先预测：把远程调用当本地方法并叠加无界重试，会在慢依赖时放大流量；先写数据库再发消息则会在进程崩溃窗口永久丢事件。把预测写成可观察反例，再决定是否采用模式。模式不是技术标签，而是在特定约束中解决反复出现问题的决策；结果部分既包含收益，也包含采用后必须处理的新问题。

FTGO是全书贯穿案例。读者要沿同一订单标识追踪消费者、餐厅、厨房、配送、信用和通知状态，明确哪个服务拥有哪份事实、谁可修改、消息何时可重放、查询允许多旧、测试在哪层证明、发布如何停止和回退。验收不变量是：每次跨进程交互都明确交互风格、契约版本、超时、幂等、顺序和交付语义；数据库提交与消息发布之间不存在不可解释的丢失窗口。

## 核心词汇与初版边界

<Term def="交互风格是“第3章 微服务架构中的进程间通信”的第1个核心概念，必须同时说明问题、约束、机制、代价、失败反例和可观测证据">交互风格</Term>、<Term def="远程过程调用是“第3章 微服务架构中的进程间通信”的第2个核心概念，必须同时说明问题、约束、机制、代价、失败反例和可观测证据">远程过程调用</Term>、<Term def="异步消息是“第3章 微服务架构中的进程间通信”的第3个核心概念，必须同时说明问题、约束、机制、代价、失败反例和可观测证据">异步消息</Term>、<Term def="事务性发件箱是“第3章 微服务架构中的进程间通信”的第4个核心概念，必须同时说明问题、约束、机制、代价、失败反例和可观测证据">事务性发件箱</Term>、<Term def="幂等消费者是“第3章 微服务架构中的进程间通信”的第5个核心概念，必须同时说明问题、约束、机制、代价、失败反例和可观测证据">幂等消费者</Term>

以上词汇都要回答六个问题：解决什么问题、受什么约束、状态由谁持有、失败如何传播、怎样恢复、用什么证据证明。2018/2019初版之后出现的第二版结构、云产品行为和模式补充只能作为迁移差异，不能改写本页正式目录。

## 原书目录逐节点重构

### 3.1 微服务架构中的进程间通信概述

**正式节点 1/4。** 用一对一/一对多与同步/异步两个维度分类交互；API优先、语义化版本、兼容读写和文本/二进制格式选择共同决定演化能力。

本节的三级目录如下，均在本页展开而不合并成泛化主题：

- 3.1.1 交互风格
- 3.1.2 在微服务架构中定义API
- 3.1.3 API的演化
- 3.1.4 消息的格式

把“3.1 微服务架构中的进程间通信概述”放回“比较同步远程调用与异步消息，建立可演化API、部分失败、发现、顺序、去重和事务性消息的完整通信契约”这条责任链：输入来自“本章输入与版本契约”，输出进入“3.2 基于同步远程过程调用模式的通信”。先写主体、拥有的数据、命令或查询、同步与异步边界、可见完成点，再画正常、超时、重复、乱序、部分失败和恢复轨迹。不能用框架名称替代协议语义，也不能用组件健康替代“每次跨进程交互都明确交互风格、契约版本、超时、幂等、顺序和交付语义；数据库提交与消息发布之间不存在不可解释的丢失窗口”。

实验固定FTGO业务样本、初版代码语义、数据、负载和观测窗口，只改变一个架构变量或故障。比较状态转移、P50/P95/P99、错误、队列滞后、重试放大、恢复时间和最终业务集合；若只有HTTP成功、消息已确认、Pod就绪或函数返回，而没有持久状态与业务对账，本节仍未通过。

### 3.2 基于同步远程过程调用模式的通信

**正式节点 2/4。** REST强调资源和HTTP语义，gRPC强调IDL与高效二进制；两者都必须配置超时、断路器、发现和调用预算，协议不同不消除部分失败。

本节的三级目录如下，均在本页展开而不合并成泛化主题：

- 3.2.1 使用REST
- 3.2.2 使用gRPC
- 3.2.3 使用断路器模式处理部分失败
- 3.2.4 使用服务发现

把“3.2 基于同步远程过程调用模式的通信”放回“比较同步远程调用与异步消息，建立可演化API、部分失败、发现、顺序、去重和事务性消息的完整通信契约”这条责任链：输入来自“3.1 微服务架构中的进程间通信概述”，输出进入“3.3 基于异步消息模式的通信”。先写主体、拥有的数据、命令或查询、同步与异步边界、可见完成点，再画正常、超时、重复、乱序、部分失败和恢复轨迹。不能用框架名称替代协议语义，也不能用组件健康替代“每次跨进程交互都明确交互风格、契约版本、超时、幂等、顺序和交付语义；数据库提交与消息发布之间不存在不可解释的丢失窗口”。

实验固定FTGO业务样本、初版代码语义、数据、负载和观测窗口，只改变一个架构变量或故障。比较状态转移、P50/P95/P99、错误、队列滞后、重试放大、恢复时间和最终业务集合；若只有HTTP成功、消息已确认、Pod就绪或函数返回，而没有持久状态与业务对账，本节仍未通过。

### 3.3 基于异步消息模式的通信

**正式节点 3/4。** 消息通道解耦发送与处理时间，但引入代理、分区、重复和顺序约束；事务性发件箱配合轮询发布或日志拖尾关闭数据库与消息代理之间的双写窗口。

本节的三级目录如下，均在本页展开而不合并成泛化主题：

- 3.3.1 消息机制概述
- 3.3.2 使用消息机制实现交互方式
- 3.3.3 为基于消息的服务API创建规范
- 3.3.4 使用消息代理
- 3.3.5 处理并发接收者和消息顺序
- 3.3.6 处理重复消息
- 3.3.7 事务性消息
- 3.3.8 消息机制的类库和框架

把“3.3 基于异步消息模式的通信”放回“比较同步远程调用与异步消息，建立可演化API、部分失败、发现、顺序、去重和事务性消息的完整通信契约”这条责任链：输入来自“3.2 基于同步远程过程调用模式的通信”，输出进入“3.4 使用异步消息提高可用性”。先写主体、拥有的数据、命令或查询、同步与异步边界、可见完成点，再画正常、超时、重复、乱序、部分失败和恢复轨迹。不能用框架名称替代协议语义，也不能用组件健康替代“每次跨进程交互都明确交互风格、契约版本、超时、幂等、顺序和交付语义；数据库提交与消息发布之间不存在不可解释的丢失窗口”。

实验固定FTGO业务样本、初版代码语义、数据、负载和观测窗口，只改变一个架构变量或故障。比较状态转移、P50/P95/P99、错误、队列滞后、重试放大、恢复时间和最终业务集合；若只有HTTP成功、消息已确认、Pod就绪或函数返回，而没有持久状态与业务对账，本节仍未通过。

### 3.4 使用异步消息提高可用性

**正式节点 4/4。** 串行同步依赖的可用性近似相乘；通过异步交互、数据复制和先响应后处理减少关键路径，但必须公开延迟完成和最终一致语义。

本节的三级目录如下，均在本页展开而不合并成泛化主题：

- 3.4.1 同步通信会降低可用性
- 3.4.2 消除同步交互

把“3.4 使用异步消息提高可用性”放回“比较同步远程调用与异步消息，建立可演化API、部分失败、发现、顺序、去重和事务性消息的完整通信契约”这条责任链：输入来自“3.3 基于异步消息模式的通信”，输出进入“本章证据门与跨章连接”。先写主体、拥有的数据、命令或查询、同步与异步边界、可见完成点，再画正常、超时、重复、乱序、部分失败和恢复轨迹。不能用框架名称替代协议语义，也不能用组件健康替代“每次跨进程交互都明确交互风格、契约版本、超时、幂等、顺序和交付语义；数据库提交与消息发布之间不存在不可解释的丢失窗口”。

实验固定FTGO业务样本、初版代码语义、数据、负载和观测窗口，只改变一个架构变量或故障。比较状态转移、P50/P95/P99、错误、队列滞后、重试放大、恢复时间和最终业务集合；若只有HTTP成功、消息已确认、Pod就绪或函数返回，而没有持久状态与业务对账，本节仍未通过。

## 模式决策矩阵

| 模式或机制   | 要解决的问题                 | 核心机制                         | 最小证据                 |
| ------------ | ---------------------------- | -------------------------------- | ------------------------ |
| 远程过程调用 | 调用方需要立即结果           | REST或gRPC加超时、发现和断路器   | 端到端延迟与超时分类     |
| 异步消息     | 处理可与请求时间解耦         | 命令、事件和回复通过持久通道传递 | 队列滞后、重复和死信     |
| 事务性发件箱 | 数据库与消息代理无法原子双写 | 业务状态和待发消息同事务提交     | 提交记录与已发布消息对账 |
| 幂等消费者   | 至少一次交付产生重复         | 以消息ID或业务键原子去重         | 重复注入后副作用集合不变 |

选择模式时先写“未采用会怎样”，再写采用后的新成本。例如同步RPC需要超时、断路器和发现；异步消息需要事务性发件箱、顺序、去重和死信；Saga需要补偿和隔离对策；CQRS需要视图水位与重建。相关模式组成因果链，不能独立打勾。

## 机制、状态与不变量

### 责任与数据所有权

服务只能通过API或消息公开能力，数据表、聚合事件流和查询视图都有唯一写入责任。一个业务命令可跨越多个服务，但每一步本地事务、发出的消息、预期回复、补偿和终态都必须可追踪。共享数据库、跨库联表和绕过API的“临时读取”会让边界失真，必须记录退出日期和删除证据。

### 完成点与失败语义

客户端收到响应、网关完成组合、消息代理确认、消费者写库、Saga进入终态、CQRS视图追上水位、Pod通过就绪和业务对账完成是不同检查点。超时表示结果未知，不等于失败；至少一次消息表示可能重复，不等于恰好一次业务副作用。恢复从持久事实继续，而不是从易失内存猜测。

### 可用性与放大效应

串行依赖近似满足 $A_{path}=\prod_i A_i$。每次失败都重试 $r$ 次时，最坏尝试量接近 $N(1+r)$；多层重试会相乘而非相加。该估算只用于发现方向性风险，生产结论还必须固定业务、数据、负载、版本和观测窗口，测量P50/P95/P99、错误、饱和度、队列滞后、恢复时间和最终业务集合。

## 可复现实现与证据格式

```yaml
decision:
  book: microservices-patterns-first-edition
  unit: msp-03-interprocess-communication
  problem: "比较同步远程调用与异步消息，建立可演化API、部分失败、发现、顺序、去重和事务性消息的完整通信契约"
  invariant: business-state-reconciled
  observe: [contract-version, state, trace, latency-p99, queue-lag, business]
  stop: [error-budget-exhausted, invariant-broken]
  rollback: verified-baseline
```

```java
// 业务状态与待发消息在同一个本地事务中提交；发布器可重试，消费者按消息ID幂等。
@Transactional
Result handle(Command command) {
  Aggregate aggregate = repository.load(command.aggregateId());
  Events events = aggregate.decide(command);
  repository.save(aggregate, events.expectedVersion());
  outbox.append(events.withCorrelation(command.correlationId()));
  return Result.accepted(aggregate.id(), aggregate.version());
}
```

```text
experiment = fixed(ftgo-case, data, load, version, observation-window)
           + change(one boundary | failure | retry | ordering variable)
           + observe(api + message + state + telemetry + business result)
           + reconcile(expected invariant, durable terminal state)
```

## 三步交互实验

<Stepper>
  <Step title="1. 选择目录节点与模式">
    选择一个正式节点，先写问题和约束，再观察模式机制与最小证据；不要从产品名开始。
    <Msp03InterprocessCommunicationPatternLab />
  </Step>
  <Step title="2. 注入部分失败并预测">
    只改变请求量、超时、重复率、重试预算或幂等开关之一，比较流量放大、未知结果与业务副作用。
    <Msp03InterprocessCommunicationFailureLab />
  </Step>
  <Step title="3. 通过独立证据门">
    目录、版本、契约、状态、反例、遥测、对账、停止与回退齐全后才接受结论。
    <Msp03InterprocessCommunicationEvidenceLab />
  </Step>
</Stepper>

<Callout type="trap" title="模式清单陷阱">
  把Saga、CQRS、API
  Gateway、容器等名称列出来并不等于设计。必须说明前驱问题、约束、状态所有者、结果与后继模式，否则无法判断是否适用。
</Callout>

<Callout type="trap" title="恰好一次陷阱">
  网络重试和至少一次消息都可能重复。代理确认、HTTP成功和消费者返回不能证明业务副作用恰好一次；必须用业务键、幂等记录与最终状态对账。
</Callout>

<Callout type="warning" title="隔离实验">
  故障注入、补偿、消息回放、数据库迁移、切流和权限轮换只在隔离环境演练。先保留基线，规定影响上界、停止条件、恢复步骤和回退验收。
</Callout>

## 独立交接与跨章连接

交接包至少包含交互风格矩阵、API演化契约、消息时序图、发件箱对账表、初版目录映射、服务和数据所有权、API/事件模式、关联标识、失败状态机、测试层级、遥测查询、业务对账、停止与回退。另一位维护者必须能在无口头补充的情况下重放一个成功案例、一个超时未知案例、一个重复案例和一个恢复案例。

本页输出不是终点。它会成为后继模式的输入：边界产生通信，通信产生一致性与查询问题，分布式行为产生测试与可观测需求，运行单元产生部署问题，已有单体产生迁移问题。若后继问题没有负责人和证据门，当前选择尚未完成。

## 本章回顾

重新完成“比较同步远程调用与异步消息，建立可演化API、部分失败、发现、顺序、去重和事务性消息的完整通信契约”：从初版目录和FTGO问题开始，明确主体、数据与契约，推导模式并记录收益和代价；再通过正常、超时、重复、乱序、部分失败和恢复实验，验证“每次跨进程交互都明确交互风格、契约版本、超时、幂等、顺序和交付语义；数据库提交与消息发布之间不存在不可解释的丢失窗口”。只有目录节点、机制轨迹和最终业务状态均可独立复现，本页才算完成。

## 复习与运行验收

<Exercises>
**问题 1**：本页为什么必须逐项覆盖正式目录？
<Answer>目录节点共同组成“比较同步远程调用与异步消息，建立可演化API、部分失败、发现、顺序、去重和事务性消息的完整通信契约”的完整因果链；漏项会使交互风格矩阵、API演化契约、消息时序图、发件箱对账表无法独立复现，并可能隐藏采用模式后出现的后继问题。</Answer>

**问题 2**：最小业务不变量是什么？

<Answer>
  每次跨进程交互都明确交互风格、契约版本、超时、幂等、顺序和交付语义；数据库提交与消息发布之间不存在不可解释的丢失窗口。组件状态只能作为中间证据，最终还要核对用户可见结果和持久事实。
</Answer>

**问题 3**：怎样构造最小反例？

<Answer>
  固定FTGO案例、数据、负载、版本与观测窗口，只改变一个边界、故障、重试或顺序变量，比较组件轨迹与最终业务集合。
</Answer>

**问题 4**：为什么不能混入第二版目录？

<Answer>
  本课程对应2018英文初版和2019中文版；第二版重新组织并新增内容，只能作为迁移差异，不能改变242个初版编号节点的分母。
</Answer>

**问题 5**：性能与恢复结论需要哪些证据？

<Answer>
  需要契约版本、日志、追踪、指标、消息偏移、状态快照、P50/P95/P99、错误、饱和度、恢复时间和最终业务对账。
</Answer>

**问题 6**：独立交接需要什么？

<Answer>需要初版目录映射、决策记录、交互风格矩阵、API演化契约、消息时序图、发件箱对账表、故障反例、测试、业务对账、停止、恢复、回退与责任人。</Answer>
</Exercises>

<Glossary>
  <GlossaryItem term="交互风格">
    交互风格在本页不是孤立名词；它必须服务于“每次跨进程交互都明确交互风格、契约版本、超时、幂等、顺序和交付语义；数据库提交与消息发布之间不存在不可解释的丢失窗口”，并能由版本、契约、状态轨迹、故障注入和最终业务对账独立证明。
  </GlossaryItem>
  <GlossaryItem term="远程过程调用">
    远程过程调用在本页不是孤立名词；它必须服务于“每次跨进程交互都明确交互风格、契约版本、超时、幂等、顺序和交付语义；数据库提交与消息发布之间不存在不可解释的丢失窗口”，并能由版本、契约、状态轨迹、故障注入和最终业务对账独立证明。
  </GlossaryItem>
  <GlossaryItem term="异步消息">
    异步消息在本页不是孤立名词；它必须服务于“每次跨进程交互都明确交互风格、契约版本、超时、幂等、顺序和交付语义；数据库提交与消息发布之间不存在不可解释的丢失窗口”，并能由版本、契约、状态轨迹、故障注入和最终业务对账独立证明。
  </GlossaryItem>
  <GlossaryItem term="事务性发件箱">
    事务性发件箱在本页不是孤立名词；它必须服务于“每次跨进程交互都明确交互风格、契约版本、超时、幂等、顺序和交付语义；数据库提交与消息发布之间不存在不可解释的丢失窗口”，并能由版本、契约、状态轨迹、故障注入和最终业务对账独立证明。
  </GlossaryItem>
  <GlossaryItem term="幂等消费者">
    幂等消费者在本页不是孤立名词；它必须服务于“每次跨进程交互都明确交互风格、契约版本、超时、幂等、顺序和交付语义；数据库提交与消息发布之间不存在不可解释的丢失窗口”，并能由版本、契约、状态轨迹、故障注入和最终业务对账独立证明。
  </GlossaryItem>
</Glossary>

[← 上一页：第2章 服务的拆分策略](/learn/microservices-patterns/02-02-decomposition-strategies/msp-02-decomposition-strategies) · [下一页：第4章 使用Saga管理事务 →](/learn/microservices-patterns/04-04-managing-transactions-with-sagas/msp-04-managing-transactions-with-sagas)

<Attribution
  adaptedFrom="Chris Richardson《Microservices Patterns》2018英文初版 / 喻勇译2019中文版"
  adaptedUrl="https://www.manning.com/books/microservices-patterns"
/>
