---
title: "第7章 在微服务架构中实现查询"
description: "依据Chris Richardson英文初版与喻勇译2019中文版完整目录：在服务各自拥有数据库的前提下比较API组合与CQRS，设计可重建、可追踪延迟和面向查询的数据视图"
type: C
section: "微服务架构设计模式（2019中文版初版 / 2018英文初版）"
order: 7
draft: true
math: false
sourceUrl: "https://livebook.manning.com/book/microservices-patterns/table-of-contents"
demo: true
---

import {
  Msp07ImplementingQueriesPatternLab,
  Msp07ImplementingQueriesFailureLab,
  Msp07ImplementingQueriesEvidenceLab,
} from "@/components/mdx/microservices-patterns/diagrams/msp-07-implementing-queries";

# 第7章 在微服务架构中实现查询

本课程对应Chris Richardson著、喻勇译《微服务架构设计模式》，机械工业出版社2019年出版，ISBN 9787111624127，馆藏著录为前置26页、正文455页。英文原版为Manning 2018年10月初版，ISBN 9781617294549、520页。课程固定该初版的13章与44个模式，不混入当前第二版MEAP重新编排和新增的章节。

全书权威分母为13章、52个二级节、177个三级节，共242个编号目录节点；另设学习地图和总复习，共15页、45个交互视图和90道独立复习题。本章覆盖4个二级节与15个三级节，连同章标题共20个编号节点。

<Objectives>

- 能解释“第7章 在微服务架构中实现查询”的正式目录节点，并把每个模式写成问题、约束、解决方案、结果与相关模式。
- 能绘制FTGO主体、服务、数据所有权、同步/异步边界和业务完成点，区分局部成功与最终业务成功。
- 能比较正常、超时、重复、乱序、部分失败与恢复轨迹，并用状态、遥测和业务对账验证“查询不能绕过服务API直接联表；CQRS视图可从源事件重建并公开新鲜度，API组合在部分失败时返回明确而非伪造完整结果”。
- 能设计查询责任图、组合调用树、CQRS更新流水线、视图重建与水位表，写出停止、恢复、回退和独立交接条件。

</Objectives>

## 从一个可证伪的问题开始

先预测：把共享只读数据库伪装成查询服务会破坏数据所有权；忽略事件缺口和乱序的CQRS视图则会长期返回无法解释的旧数据。把预测写成可观察反例，再决定是否采用模式。模式不是技术标签，而是在特定约束中解决反复出现问题的决策；结果部分既包含收益，也包含采用后必须处理的新问题。

FTGO是全书贯穿案例。读者要沿同一订单标识追踪消费者、餐厅、厨房、配送、信用和通知状态，明确哪个服务拥有哪份事实、谁可修改、消息何时可重放、查询允许多旧、测试在哪层证明、发布如何停止和回退。验收不变量是：查询不能绕过服务API直接联表；CQRS视图可从源事件重建并公开新鲜度，API组合在部分失败时返回明确而非伪造完整结果。

## 核心词汇与初版边界

<Term def="API组合是“第7章 在微服务架构中实现查询”的第1个核心概念，必须同时说明问题、约束、机制、代价、失败反例和可观测证据">API组合</Term>、<Term def="CQRS是“第7章 在微服务架构中实现查询”的第2个核心概念，必须同时说明问题、约束、机制、代价、失败反例和可观测证据">CQRS</Term>、<Term def="物化视图是“第7章 在微服务架构中实现查询”的第3个核心概念，必须同时说明问题、约束、机制、代价、失败反例和可观测证据">物化视图</Term>、<Term def="最终一致性是“第7章 在微服务架构中实现查询”的第4个核心概念，必须同时说明问题、约束、机制、代价、失败反例和可观测证据">最终一致性</Term>、<Term def="查询模型是“第7章 在微服务架构中实现查询”的第5个核心概念，必须同时说明问题、约束、机制、代价、失败反例和可观测证据">查询模型</Term>

以上词汇都要回答六个问题：解决什么问题、受什么约束、状态由谁持有、失败如何传播、怎样恢复、用什么证据证明。2018/2019初版之后出现的第二版结构、云产品行为和模式补充只能作为迁移差异，不能改写本页正式目录。

## 原书目录逐节点重构

### 7.1 使用API组合模式进行查询

**正式节点 1/4。** 组合器调用拥有数据的服务并在内存合并结果，适合简单、低扇出查询；必须预算并行度、超时、缓存和部分响应语义。

本节的三级目录如下，均在本页展开而不合并成泛化主题：

- 7.1.1 findOrder()查询操作
- 7.1.2 API组合模式概述
- 7.1.3 使用API组合实现findOrder()查询
- 7.1.4 API组合的设计问题
- 7.1.5 API组合的好处和弊端

把“7.1 使用API组合模式进行查询”放回“在服务各自拥有数据库的前提下比较API组合与CQRS，设计可重建、可追踪延迟和面向查询的数据视图”这条责任链：输入来自“本章输入与版本契约”，输出进入“7.2 使用CQRS模式”。先写主体、拥有的数据、命令或查询、同步与异步边界、可见完成点，再画正常、超时、重复、乱序、部分失败和恢复轨迹。不能用框架名称替代协议语义，也不能用组件健康替代“查询不能绕过服务API直接联表；CQRS视图可从源事件重建并公开新鲜度，API组合在部分失败时返回明确而非伪造完整结果”。

实验固定FTGO业务样本、初版代码语义、数据、负载和观测窗口，只改变一个架构变量或故障。比较状态转移、P50/P95/P99、错误、队列滞后、重试放大、恢复时间和最终业务集合；若只有HTTP成功、消息已确认、Pod就绪或函数返回，而没有持久状态与业务对账，本节仍未通过。

### 7.2 使用CQRS模式

**正式节点 2/4。** CQRS用事件维护面向查询的副本，消除运行时跨服务连接；代价是额外存储、事件处理、最终一致性和重建运维。

本节的三级目录如下，均在本页展开而不合并成泛化主题：

- 7.2.1 使用CQRS的动机
- 7.2.2 CQRS概述
- 7.2.3 CQRS的好处
- 7.2.4 CQRS的弊端

把“7.2 使用CQRS模式”放回“在服务各自拥有数据库的前提下比较API组合与CQRS，设计可重建、可追踪延迟和面向查询的数据视图”这条责任链：输入来自“7.1 使用API组合模式进行查询”，输出进入“7.3 设计CQRS视图”。先写主体、拥有的数据、命令或查询、同步与异步边界、可见完成点，再画正常、超时、重复、乱序、部分失败和恢复轨迹。不能用框架名称替代协议语义，也不能用组件健康替代“查询不能绕过服务API直接联表；CQRS视图可从源事件重建并公开新鲜度，API组合在部分失败时返回明确而非伪造完整结果”。

实验固定FTGO业务样本、初版代码语义、数据、负载和观测窗口，只改变一个架构变量或故障。比较状态转移、P50/P95/P99、错误、队列滞后、重试放大、恢复时间和最终业务集合；若只有HTTP成功、消息已确认、Pod就绪或函数返回，而没有持久状态与业务对账，本节仍未通过。

### 7.3 设计CQRS视图

**正式节点 3/4。** 存储技术由查询形状、更新率和一致性要求选择；处理器需按事件ID去重、按聚合版本检测缺口并记录消费水位。

本节的三级目录如下，均在本页展开而不合并成泛化主题：

- 7.3.1 选择视图存储库
- 7.3.2 数据访问模块设计
- 7.3.3 添加和更新CQRS视图

把“7.3 设计CQRS视图”放回“在服务各自拥有数据库的前提下比较API组合与CQRS，设计可重建、可追踪延迟和面向查询的数据视图”这条责任链：输入来自“7.2 使用CQRS模式”，输出进入“7.4 实现基于AWS DynamoDB的CQRS视图”。先写主体、拥有的数据、命令或查询、同步与异步边界、可见完成点，再画正常、超时、重复、乱序、部分失败和恢复轨迹。不能用框架名称替代协议语义，也不能用组件健康替代“查询不能绕过服务API直接联表；CQRS视图可从源事件重建并公开新鲜度，API组合在部分失败时返回明确而非伪造完整结果”。

实验固定FTGO业务样本、初版代码语义、数据、负载和观测窗口，只改变一个架构变量或故障。比较状态转移、P50/P95/P99、错误、队列滞后、重试放大、恢复时间和最终业务集合；若只有HTTP成功、消息已确认、Pod就绪或函数返回，而没有持久状态与业务对账，本节仍未通过。

### 7.4 实现基于AWS DynamoDB的CQRS视图

**正式节点 4/4。** 围绕访问模式设计分区键和排序键，事件处理器更新订单历史视图，DAO只暴露受支持查询并提供分页与新鲜度。

本节的三级目录如下，均在本页展开而不合并成泛化主题：

- 7.4.1 OrderHistoryEventHandlers模块
- 7.4.2 DynamoDB中的数据建模和查询设计
- 7.4.3 OrderHistoryDaoDynamoDb类

把“7.4 实现基于AWS DynamoDB的CQRS视图”放回“在服务各自拥有数据库的前提下比较API组合与CQRS，设计可重建、可追踪延迟和面向查询的数据视图”这条责任链：输入来自“7.3 设计CQRS视图”，输出进入“本章证据门与跨章连接”。先写主体、拥有的数据、命令或查询、同步与异步边界、可见完成点，再画正常、超时、重复、乱序、部分失败和恢复轨迹。不能用框架名称替代协议语义，也不能用组件健康替代“查询不能绕过服务API直接联表；CQRS视图可从源事件重建并公开新鲜度，API组合在部分失败时返回明确而非伪造完整结果”。

实验固定FTGO业务样本、初版代码语义、数据、负载和观测窗口，只改变一个架构变量或故障。比较状态转移、P50/P95/P99、错误、队列滞后、重试放大、恢复时间和最终业务集合；若只有HTTP成功、消息已确认、Pod就绪或函数返回，而没有持久状态与业务对账，本节仍未通过。

## 模式决策矩阵

| 模式或机制   | 要解决的问题             | 核心机制                 | 最小证据             |
| ------------ | ------------------------ | ------------------------ | -------------------- |
| API组合      | 查询分散在少量服务       | 并行调用后合并响应       | 扇出延迟与部分失败   |
| CQRS         | 高价值查询跨越多服务数据 | 事件维护专用查询视图     | 消费水位与视图新鲜度 |
| 物化视图重建 | 视图损坏或模式变化       | 从受控偏移全量重放       | 源事件数与投影版本   |
| 缺口检测     | 消息可能乱序或遗漏       | 按聚合版本发现并暂停修复 | 连续版本和隔离队列   |

选择模式时先写“未采用会怎样”，再写采用后的新成本。例如同步RPC需要超时、断路器和发现；异步消息需要事务性发件箱、顺序、去重和死信；Saga需要补偿和隔离对策；CQRS需要视图水位与重建。相关模式组成因果链，不能独立打勾。

## 机制、状态与不变量

### 责任与数据所有权

服务只能通过API或消息公开能力，数据表、聚合事件流和查询视图都有唯一写入责任。一个业务命令可跨越多个服务，但每一步本地事务、发出的消息、预期回复、补偿和终态都必须可追踪。共享数据库、跨库联表和绕过API的“临时读取”会让边界失真，必须记录退出日期和删除证据。

### 完成点与失败语义

客户端收到响应、网关完成组合、消息代理确认、消费者写库、Saga进入终态、CQRS视图追上水位、Pod通过就绪和业务对账完成是不同检查点。超时表示结果未知，不等于失败；至少一次消息表示可能重复，不等于恰好一次业务副作用。恢复从持久事实继续，而不是从易失内存猜测。

### 可用性与放大效应

串行依赖近似满足 $A_{path}=\prod_i A_i$。每次失败都重试 $r$ 次时，最坏尝试量接近 $N(1+r)$；多层重试会相乘而非相加。该估算只用于发现方向性风险，生产结论还必须固定业务、数据、负载、版本和观测窗口，测量P50/P95/P99、错误、饱和度、队列滞后、恢复时间和最终业务集合。

## 可复现实现与证据格式

```yaml
decision:
  book: microservices-patterns-first-edition
  unit: msp-07-implementing-queries
  problem: "在服务各自拥有数据库的前提下比较API组合与CQRS，设计可重建、可追踪延迟和面向查询的数据视图"
  invariant: business-state-reconciled
  observe: [contract-version, state, trace, latency-p99, queue-lag, business]
  stop: [error-budget-exhausted, invariant-broken]
  rollback: verified-baseline
```

```java
// 业务状态与待发消息在同一个本地事务中提交；发布器可重试，消费者按消息ID幂等。
@Transactional
Result handle(Command command) {
  Aggregate aggregate = repository.load(command.aggregateId());
  Events events = aggregate.decide(command);
  repository.save(aggregate, events.expectedVersion());
  outbox.append(events.withCorrelation(command.correlationId()));
  return Result.accepted(aggregate.id(), aggregate.version());
}
```

```text
experiment = fixed(ftgo-case, data, load, version, observation-window)
           + change(one boundary | failure | retry | ordering variable)
           + observe(api + message + state + telemetry + business result)
           + reconcile(expected invariant, durable terminal state)
```

## 三步交互实验

<Stepper>
  <Step title="1. 选择目录节点与模式">
    选择一个正式节点，先写问题和约束，再观察模式机制与最小证据；不要从产品名开始。
    <Msp07ImplementingQueriesPatternLab />
  </Step>
  <Step title="2. 注入部分失败并预测">
    只改变请求量、超时、重复率、重试预算或幂等开关之一，比较流量放大、未知结果与业务副作用。
    <Msp07ImplementingQueriesFailureLab />
  </Step>
  <Step title="3. 通过独立证据门">
    目录、版本、契约、状态、反例、遥测、对账、停止与回退齐全后才接受结论。
    <Msp07ImplementingQueriesEvidenceLab />
  </Step>
</Stepper>

<Callout type="trap" title="模式清单陷阱">
  把Saga、CQRS、API
  Gateway、容器等名称列出来并不等于设计。必须说明前驱问题、约束、状态所有者、结果与后继模式，否则无法判断是否适用。
</Callout>

<Callout type="trap" title="恰好一次陷阱">
  网络重试和至少一次消息都可能重复。代理确认、HTTP成功和消费者返回不能证明业务副作用恰好一次；必须用业务键、幂等记录与最终状态对账。
</Callout>

<Callout type="warning" title="隔离实验">
  故障注入、补偿、消息回放、数据库迁移、切流和权限轮换只在隔离环境演练。先保留基线，规定影响上界、停止条件、恢复步骤和回退验收。
</Callout>

## 独立交接与跨章连接

交接包至少包含查询责任图、组合调用树、CQRS更新流水线、视图重建与水位表、初版目录映射、服务和数据所有权、API/事件模式、关联标识、失败状态机、测试层级、遥测查询、业务对账、停止与回退。另一位维护者必须能在无口头补充的情况下重放一个成功案例、一个超时未知案例、一个重复案例和一个恢复案例。

本页输出不是终点。它会成为后继模式的输入：边界产生通信，通信产生一致性与查询问题，分布式行为产生测试与可观测需求，运行单元产生部署问题，已有单体产生迁移问题。若后继问题没有负责人和证据门，当前选择尚未完成。

## 本章回顾

重新完成“在服务各自拥有数据库的前提下比较API组合与CQRS，设计可重建、可追踪延迟和面向查询的数据视图”：从初版目录和FTGO问题开始，明确主体、数据与契约，推导模式并记录收益和代价；再通过正常、超时、重复、乱序、部分失败和恢复实验，验证“查询不能绕过服务API直接联表；CQRS视图可从源事件重建并公开新鲜度，API组合在部分失败时返回明确而非伪造完整结果”。只有目录节点、机制轨迹和最终业务状态均可独立复现，本页才算完成。

## 复习与运行验收

<Exercises>
**问题 1**：本页为什么必须逐项覆盖正式目录？
<Answer>目录节点共同组成“在服务各自拥有数据库的前提下比较API组合与CQRS，设计可重建、可追踪延迟和面向查询的数据视图”的完整因果链；漏项会使查询责任图、组合调用树、CQRS更新流水线、视图重建与水位表无法独立复现，并可能隐藏采用模式后出现的后继问题。</Answer>

**问题 2**：最小业务不变量是什么？

<Answer>
  查询不能绕过服务API直接联表；CQRS视图可从源事件重建并公开新鲜度，API组合在部分失败时返回明确而非伪造完整结果。组件状态只能作为中间证据，最终还要核对用户可见结果和持久事实。
</Answer>

**问题 3**：怎样构造最小反例？

<Answer>
  固定FTGO案例、数据、负载、版本与观测窗口，只改变一个边界、故障、重试或顺序变量，比较组件轨迹与最终业务集合。
</Answer>

**问题 4**：为什么不能混入第二版目录？

<Answer>
  本课程对应2018英文初版和2019中文版；第二版重新组织并新增内容，只能作为迁移差异，不能改变242个初版编号节点的分母。
</Answer>

**问题 5**：性能与恢复结论需要哪些证据？

<Answer>
  需要契约版本、日志、追踪、指标、消息偏移、状态快照、P50/P95/P99、错误、饱和度、恢复时间和最终业务对账。
</Answer>

**问题 6**：独立交接需要什么？

<Answer>需要初版目录映射、决策记录、查询责任图、组合调用树、CQRS更新流水线、视图重建与水位表、故障反例、测试、业务对账、停止、恢复、回退与责任人。</Answer>
</Exercises>

<Glossary>
  <GlossaryItem term="API组合">
    API组合在本页不是孤立名词；它必须服务于“查询不能绕过服务API直接联表；CQRS视图可从源事件重建并公开新鲜度，API组合在部分失败时返回明确而非伪造完整结果”，并能由版本、契约、状态轨迹、故障注入和最终业务对账独立证明。
  </GlossaryItem>
  <GlossaryItem term="CQRS">
    CQRS在本页不是孤立名词；它必须服务于“查询不能绕过服务API直接联表；CQRS视图可从源事件重建并公开新鲜度，API组合在部分失败时返回明确而非伪造完整结果”，并能由版本、契约、状态轨迹、故障注入和最终业务对账独立证明。
  </GlossaryItem>
  <GlossaryItem term="物化视图">
    物化视图在本页不是孤立名词；它必须服务于“查询不能绕过服务API直接联表；CQRS视图可从源事件重建并公开新鲜度，API组合在部分失败时返回明确而非伪造完整结果”，并能由版本、契约、状态轨迹、故障注入和最终业务对账独立证明。
  </GlossaryItem>
  <GlossaryItem term="最终一致性">
    最终一致性在本页不是孤立名词；它必须服务于“查询不能绕过服务API直接联表；CQRS视图可从源事件重建并公开新鲜度，API组合在部分失败时返回明确而非伪造完整结果”，并能由版本、契约、状态轨迹、故障注入和最终业务对账独立证明。
  </GlossaryItem>
  <GlossaryItem term="查询模型">
    查询模型在本页不是孤立名词；它必须服务于“查询不能绕过服务API直接联表；CQRS视图可从源事件重建并公开新鲜度，API组合在部分失败时返回明确而非伪造完整结果”，并能由版本、契约、状态轨迹、故障注入和最终业务对账独立证明。
  </GlossaryItem>
</Glossary>

[← 上一页：第6章 使用事件溯源开发业务逻辑](/learn/microservices-patterns/06-06-event-sourcing/msp-06-event-sourcing) · [下一页：第8章 外部API模式 →](/learn/microservices-patterns/08-08-external-api-patterns/msp-08-external-api-patterns)

<Attribution
  adaptedFrom="Chris Richardson《Microservices Patterns》2018英文初版 / 喻勇译2019中文版"
  adaptedUrl="https://www.manning.com/books/microservices-patterns"
/>
