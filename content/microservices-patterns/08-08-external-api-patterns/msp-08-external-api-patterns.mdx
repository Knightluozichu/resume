---
title: "第8章 外部API模式"
description: "依据Chris Richardson英文初版与喻勇译2019中文版完整目录：为移动端、浏览器和合作伙伴建立稳定外部边界，比较API Gateway、BFF、响应式组合与GraphQL实现"
type: C
section: "微服务架构设计模式（2019中文版初版 / 2018英文初版）"
order: 8
draft: false
math: false
sourceUrl: "https://livebook.manning.com/book/microservices-patterns/table-of-contents"
demo: true
---

import { Objectives } from "@/components/mdx/objectives";
import { Term } from "@/components/mdx/term";
import { Callout } from "@/components/mdx/callout";
import { Answer, Exercises } from "@/components/mdx/exercises";
import { Glossary, GlossaryItem } from "@/components/mdx/glossary";
import { Attribution } from "@/components/mdx/attribution";

# 第8章 外部API模式

本课程对应Chris Richardson著、喻勇译《微服务架构设计模式》，机械工业出版社2019年出版，ISBN 9787111624127，馆藏著录为前置26页、正文455页。英文原版为Manning 2018年10月初版，ISBN 9781617294549、520页。课程固定该初版的13章与44个模式，不混入当前第二版MEAP重新编排和新增的章节。

全书权威分母为13章、52个二级节、177个三级节，共242个编号目录节点；另设学习地图和总复习，共15页、45个交互视图和90道独立复习题。本章覆盖3个二级节与9个三级节，连同章标题共13个编号节点。

<Objectives>

- 能解释“第8章 外部API模式”的正式目录节点，并把每个模式写成问题、约束、解决方案、结果与相关模式。
- 能绘制FTGO主体、服务、数据所有权、同步/异步边界和业务完成点，区分局部成功与最终业务成功。
- 能比较正常、超时、重复、乱序、部分失败与恢复轨迹，并用状态、遥测和业务对账验证“客户端不感知内部服务拓扑；网关只承担路由、边缘安全、协议适配和有限组合，业务不变量仍由领域服务拥有”。
- 能设计外部API契约、客户端需求矩阵、网关责任表、GraphQL扇出预算，写出停止、恢复、回退和独立交接条件。

</Objectives>

## 从一个可证伪的问题开始

先预测：把所有业务逻辑塞进统一网关会形成新的单体与发布瓶颈；客户端直连服务则把发现、版本和故障处理扩散到每个终端。把预测写成可观察反例，再决定是否采用模式。模式不是技术标签，而是在特定约束中解决反复出现问题的决策；结果部分既包含收益，也包含采用后必须处理的新问题。

FTGO是全书贯穿案例。读者要沿同一订单标识追踪消费者、餐厅、厨房、配送、信用和通知状态，明确哪个服务拥有哪份事实、谁可修改、消息何时可重放、查询允许多旧、测试在哪层证明、发布如何停止和回退。验收不变量是：客户端不感知内部服务拓扑；网关只承担路由、边缘安全、协议适配和有限组合，业务不变量仍由领域服务拥有。

## 核心词汇与初版边界

<Term def="API Gateway是“第8章 外部API模式”的第1个核心概念，必须同时说明问题、约束、机制、代价、失败反例和可观测证据">API Gateway</Term>、<Term def="BFF是“第8章 外部API模式”的第2个核心概念，必须同时说明问题、约束、机制、代价、失败反例和可观测证据">BFF</Term>、<Term def="响应式编程是“第8章 外部API模式”的第3个核心概念，必须同时说明问题、约束、机制、代价、失败反例和可观测证据">响应式编程</Term>、<Term def="GraphQL是“第8章 外部API模式”的第4个核心概念，必须同时说明问题、约束、机制、代价、失败反例和可观测证据">GraphQL</Term>、<Term def="边缘适配是“第8章 外部API模式”的第5个核心概念，必须同时说明问题、约束、机制、代价、失败反例和可观测证据">边缘适配</Term>

以上词汇都要回答六个问题：解决什么问题、受什么约束、状态由谁持有、失败如何传播、怎样恢复、用什么证据证明。2018/2019初版之后出现的第二版结构、云产品行为和模式补充只能作为迁移差异，不能改写本页正式目录。

## 原书目录逐节点重构

### 8.1 外部API的设计难题

**正式节点 1/3。** 不同网络、屏幕和发布节奏需要不同数据形状；外部契约不能等于内部服务列表，也不能迫使移动端完成大量细粒度往返。

本节的三级目录如下，均在本页展开而不合并成泛化主题：

- 8.1.1 FTGO移动客户端的API设计问题
- 8.1.2 其他类型客户端的API设计问题

把“8.1 外部API的设计难题”放回“为移动端、浏览器和合作伙伴建立稳定外部边界，比较API Gateway、BFF、响应式组合与GraphQL实现”这条责任链：输入来自“本章输入与版本契约”，输出进入“8.2 API Gateway模式”。先写主体、拥有的数据、命令或查询、同步与异步边界、可见完成点，再画正常、超时、重复、乱序、部分失败和恢复轨迹。不能用框架名称替代协议语义，也不能用组件健康替代“客户端不感知内部服务拓扑；网关只承担路由、边缘安全、协议适配和有限组合，业务不变量仍由领域服务拥有”。

实验固定FTGO业务样本、初版代码语义、数据、负载和观测窗口，只改变一个架构变量或故障。比较状态转移、P50/P95/P99、错误、队列滞后、重试放大、恢复时间和最终业务集合；若只有HTTP成功、消息已确认、Pod就绪或函数返回，而没有持久状态与业务对账，本节仍未通过。

### 8.2 API Gateway模式

**正式节点 2/3。** 网关作为单一入口执行路由、认证、限流、协议转换和组合；BFF按客户端类型分离责任，避免一个团队和一个发布队列承载所有变化。

本节的三级目录如下，均在本页展开而不合并成泛化主题：

- 8.2.1 API Gateway模式概述
- 8.2.2 API Gateway的好处和弊端
- 8.2.3 Netflix的API Gateway示例
- 8.2.4 API Gateway的设计问题

把“8.2 API Gateway模式”放回“为移动端、浏览器和合作伙伴建立稳定外部边界，比较API Gateway、BFF、响应式组合与GraphQL实现”这条责任链：输入来自“8.1 外部API的设计难题”，输出进入“8.3 实现一个API Gateway”。先写主体、拥有的数据、命令或查询、同步与异步边界、可见完成点，再画正常、超时、重复、乱序、部分失败和恢复轨迹。不能用框架名称替代协议语义，也不能用组件健康替代“客户端不感知内部服务拓扑；网关只承担路由、边缘安全、协议适配和有限组合，业务不变量仍由领域服务拥有”。

实验固定FTGO业务样本、初版代码语义、数据、负载和观测窗口，只改变一个架构变量或故障。比较状态转移、P50/P95/P99、错误、队列滞后、重试放大、恢复时间和最终业务集合；若只有HTTP成功、消息已确认、Pod就绪或函数返回，而没有持久状态与业务对账，本节仍未通过。

### 8.3 实现一个API Gateway

**正式节点 3/3。** 现成产品适合通用边缘策略，自研适合特定组合；响应式编程处理并发I/O，GraphQL必须限制查询复杂度、深度和后端扇出。

本节的三级目录如下，均在本页展开而不合并成泛化主题：

- 8.3.1 使用现成API Gateway产品或服务
- 8.3.2 开发自己的API Gateway
- 8.3.3 使用GraphQL实现API Gateway

把“8.3 实现一个API Gateway”放回“为移动端、浏览器和合作伙伴建立稳定外部边界，比较API Gateway、BFF、响应式组合与GraphQL实现”这条责任链：输入来自“8.2 API Gateway模式”，输出进入“本章证据门与跨章连接”。先写主体、拥有的数据、命令或查询、同步与异步边界、可见完成点，再画正常、超时、重复、乱序、部分失败和恢复轨迹。不能用框架名称替代协议语义，也不能用组件健康替代“客户端不感知内部服务拓扑；网关只承担路由、边缘安全、协议适配和有限组合，业务不变量仍由领域服务拥有”。

实验固定FTGO业务样本、初版代码语义、数据、负载和观测窗口，只改变一个架构变量或故障。比较状态转移、P50/P95/P99、错误、队列滞后、重试放大、恢复时间和最终业务集合；若只有HTTP成功、消息已确认、Pod就绪或函数返回，而没有持久状态与业务对账，本节仍未通过。

## 模式决策矩阵

| 模式或机制  | 要解决的问题               | 核心机制                   | 最小证据                 |
| ----------- | -------------------------- | -------------------------- | ------------------------ |
| API Gateway | 外部客户端不能跟随内部拓扑 | 稳定入口路由和适配         | 契约版本、路由与错误语义 |
| BFF         | 客户端需求差异大           | 每类客户端拥有独立边缘后端 | 客户端变更的独立发布率   |
| 响应式组合  | 网关需并行等待多个服务     | 非阻塞流组合结果与超时     | 事件循环饱和和尾延迟     |
| GraphQL网关 | 客户端需要选择数据形状     | schema解析为受控后端调用   | 深度、复杂度和扇出上界   |

选择模式时先写“未采用会怎样”，再写采用后的新成本。例如同步RPC需要超时、断路器和发现；异步消息需要事务性发件箱、顺序、去重和死信；Saga需要补偿和隔离对策；CQRS需要视图水位与重建。相关模式组成因果链，不能独立打勾。

## 机制、状态与不变量

### 责任与数据所有权

服务只能通过API或消息公开能力，数据表、聚合事件流和查询视图都有唯一写入责任。一个业务命令可跨越多个服务，但每一步本地事务、发出的消息、预期回复、补偿和终态都必须可追踪。共享数据库、跨库联表和绕过API的“临时读取”会让边界失真，必须记录退出日期和删除证据。

### 完成点与失败语义

客户端收到响应、网关完成组合、消息代理确认、消费者写库、Saga进入终态、CQRS视图追上水位、Pod通过就绪和业务对账完成是不同检查点。超时表示结果未知，不等于失败；至少一次消息表示可能重复，不等于恰好一次业务副作用。恢复从持久事实继续，而不是从易失内存猜测。

### 可用性与放大效应

串行依赖近似满足 $A_{path}=\prod_i A_i$。每次失败都重试 $r$ 次时，最坏尝试量接近 $N(1+r)$；多层重试会相乘而非相加。该估算只用于发现方向性风险，生产结论还必须固定业务、数据、负载、版本和观测窗口，测量P50/P95/P99、错误、饱和度、队列滞后、恢复时间和最终业务集合。

## 可复现实现与证据格式

```yaml
decision:
  book: microservices-patterns-first-edition
  unit: msp-08-external-api-patterns
  problem: "为移动端、浏览器和合作伙伴建立稳定外部边界，比较API Gateway、BFF、响应式组合与GraphQL实现"
  invariant: business-state-reconciled
  observe: [contract-version, state, trace, latency-p99, queue-lag, business]
  stop: [error-budget-exhausted, invariant-broken]
  rollback: verified-baseline
```

```java
// 业务状态与待发消息在同一个本地事务中提交；发布器可重试，消费者按消息ID幂等。
@Transactional
Result handle(Command command) {
  Aggregate aggregate = repository.load(command.aggregateId());
  Events events = aggregate.decide(command);
  repository.save(aggregate, events.expectedVersion());
  outbox.append(events.withCorrelation(command.correlationId()));
  return Result.accepted(aggregate.id(), aggregate.version());
}
```

```text
experiment = fixed(ftgo-case, data, load, version, observation-window)
           + change(one boundary | failure | retry | ordering variable)
           + observe(api + message + state + telemetry + business result)
           + reconcile(expected invariant, durable terminal state)
```

<Callout type="trap" title="模式清单陷阱">
  把Saga、CQRS、API
  Gateway、容器等名称列出来并不等于设计。必须说明前驱问题、约束、状态所有者、结果与后继模式，否则无法判断是否适用。
</Callout>

<Callout type="trap" title="恰好一次陷阱">
  网络重试和至少一次消息都可能重复。代理确认、HTTP成功和消费者返回不能证明业务副作用恰好一次；必须用业务键、幂等记录与最终状态对账。
</Callout>

<Callout type="warning" title="隔离实验">
  故障注入、补偿、消息回放、数据库迁移、切流和权限轮换只在隔离环境演练。先保留基线，规定影响上界、停止条件、恢复步骤和回退验收。
</Callout>

## 独立交接与跨章连接

交接包至少包含外部API契约、客户端需求矩阵、网关责任表、GraphQL扇出预算、初版目录映射、服务和数据所有权、API/事件模式、关联标识、失败状态机、测试层级、遥测查询、业务对账、停止与回退。另一位维护者必须能在无口头补充的情况下重放一个成功案例、一个超时未知案例、一个重复案例和一个恢复案例。

本页输出不是终点。它会成为后继模式的输入：边界产生通信，通信产生一致性与查询问题，分布式行为产生测试与可观测需求，运行单元产生部署问题，已有单体产生迁移问题。若后继问题没有负责人和证据门，当前选择尚未完成。

## 本章回顾

重新完成“为移动端、浏览器和合作伙伴建立稳定外部边界，比较API Gateway、BFF、响应式组合与GraphQL实现”：从初版目录和FTGO问题开始，明确主体、数据与契约，推导模式并记录收益和代价；再通过正常、超时、重复、乱序、部分失败和恢复实验，验证“客户端不感知内部服务拓扑；网关只承担路由、边缘安全、协议适配和有限组合，业务不变量仍由领域服务拥有”。只有目录节点、机制轨迹和最终业务状态均可独立复现，本页才算完成。

## 复习与运行验收

<Exercises>
**问题 1**：本页为什么必须逐项覆盖正式目录？
<Answer>目录节点共同组成“为移动端、浏览器和合作伙伴建立稳定外部边界，比较API Gateway、BFF、响应式组合与GraphQL实现”的完整因果链；漏项会使外部API契约、客户端需求矩阵、网关责任表、GraphQL扇出预算无法独立复现，并可能隐藏采用模式后出现的后继问题。</Answer>

**问题 2**：最小业务不变量是什么？

<Answer>
  客户端不感知内部服务拓扑；网关只承担路由、边缘安全、协议适配和有限组合，业务不变量仍由领域服务拥有。组件状态只能作为中间证据，最终还要核对用户可见结果和持久事实。
</Answer>

**问题 3**：怎样构造最小反例？

<Answer>
  固定FTGO案例、数据、负载、版本与观测窗口，只改变一个边界、故障、重试或顺序变量，比较组件轨迹与最终业务集合。
</Answer>

**问题 4**：为什么不能混入第二版目录？

<Answer>
  本课程对应2018英文初版和2019中文版；第二版重新组织并新增内容，只能作为迁移差异，不能改变242个初版编号节点的分母。
</Answer>

**问题 5**：性能与恢复结论需要哪些证据？

<Answer>
  需要契约版本、日志、追踪、指标、消息偏移、状态快照、P50/P95/P99、错误、饱和度、恢复时间和最终业务对账。
</Answer>

**问题 6**：独立交接需要什么？

<Answer>需要初版目录映射、决策记录、外部API契约、客户端需求矩阵、网关责任表、GraphQL扇出预算、故障反例、测试、业务对账、停止、恢复、回退与责任人。</Answer>
</Exercises>

<Glossary>
  <GlossaryItem term="API Gateway">
    API
    Gateway在本页不是孤立名词；它必须服务于“客户端不感知内部服务拓扑；网关只承担路由、边缘安全、协议适配和有限组合，业务不变量仍由领域服务拥有”，并能由版本、契约、状态轨迹、故障注入和最终业务对账独立证明。
  </GlossaryItem>
  <GlossaryItem term="BFF">
    BFF在本页不是孤立名词；它必须服务于“客户端不感知内部服务拓扑；网关只承担路由、边缘安全、协议适配和有限组合，业务不变量仍由领域服务拥有”，并能由版本、契约、状态轨迹、故障注入和最终业务对账独立证明。
  </GlossaryItem>
  <GlossaryItem term="响应式编程">
    响应式编程在本页不是孤立名词；它必须服务于“客户端不感知内部服务拓扑；网关只承担路由、边缘安全、协议适配和有限组合，业务不变量仍由领域服务拥有”，并能由版本、契约、状态轨迹、故障注入和最终业务对账独立证明。
  </GlossaryItem>
  <GlossaryItem term="GraphQL">
    GraphQL在本页不是孤立名词；它必须服务于“客户端不感知内部服务拓扑；网关只承担路由、边缘安全、协议适配和有限组合，业务不变量仍由领域服务拥有”，并能由版本、契约、状态轨迹、故障注入和最终业务对账独立证明。
  </GlossaryItem>
  <GlossaryItem term="边缘适配">
    边缘适配在本页不是孤立名词；它必须服务于“客户端不感知内部服务拓扑；网关只承担路由、边缘安全、协议适配和有限组合，业务不变量仍由领域服务拥有”，并能由版本、契约、状态轨迹、故障注入和最终业务对账独立证明。
  </GlossaryItem>
</Glossary>

[← 上一页：第7章 在微服务架构中实现查询](/learn/microservices-patterns/07-07-implementing-queries/msp-07-implementing-queries) · [下一页：第9章 微服务架构中的测试策略（上） →](/learn/microservices-patterns/09-09-testing-part-1/msp-09-testing-part-1)

<Attribution
  adaptedFrom="Chris Richardson《Microservices Patterns》2018英文初版 / 喻勇译2019中文版"
  adaptedUrl="https://www.manning.com/books/microservices-patterns"
/>
