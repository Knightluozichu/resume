---
title: "第11章 开发面向生产环境的微服务应用"
description: "依据Chris Richardson英文初版与喻勇译2019中文版完整目录：把安全、外部化配置、健康检查、日志、追踪、指标、异常与审计纳入服务契约，并用微服务基底复用横切能力"
type: C
section: "微服务架构设计模式（2019中文版初版 / 2018英文初版）"
order: 11
draft: true
math: false
sourceUrl: "https://livebook.manning.com/book/microservices-patterns/table-of-contents"
demo: true
---

import {
  Msp11ProductionReadyServicesPatternLab,
  Msp11ProductionReadyServicesFailureLab,
  Msp11ProductionReadyServicesEvidenceLab,
} from "@/components/mdx/microservices-patterns/diagrams/msp-11-production-ready-services";

# 第11章 开发面向生产环境的微服务应用

本课程对应Chris Richardson著、喻勇译《微服务架构设计模式》，机械工业出版社2019年出版，ISBN 9787111624127，馆藏著录为前置26页、正文455页。英文原版为Manning 2018年10月初版，ISBN 9781617294549、520页。课程固定该初版的13章与44个模式，不混入当前第二版MEAP重新编排和新增的章节。

全书权威分母为13章、52个二级节、177个三级节，共242个编号目录节点；另设学习地图和总复习，共15页、45个交互视图和90道独立复习题。本章覆盖4个二级节与12个三级节，连同章标题共17个编号节点。

<Objectives>

- 能解释“第11章 开发面向生产环境的微服务应用”的正式目录节点，并把每个模式写成问题、约束、解决方案、结果与相关模式。
- 能绘制FTGO主体、服务、数据所有权、同步/异步边界和业务完成点，区分局部成功与最终业务成功。
- 能比较正常、超时、重复、乱序、部分失败与恢复轨迹，并用状态、遥测和业务对账验证“生产服务的每个请求都可验证主体与权限、追溯配置版本并关联日志/跨度/指标；健康信号表达真实依赖边界且不泄露敏感信息”。
- 能设计威胁与信任边界、配置版本表、遥测关联图、基底能力清单，写出停止、恢复、回退和独立交接条件。

</Objectives>

## 从一个可证伪的问题开始

先预测：复制粘贴安全与可观测代码会在服务间漂移；把存活探针等同业务就绪会让故障实例继续接流量或形成重启循环。把预测写成可观察反例，再决定是否采用模式。模式不是技术标签，而是在特定约束中解决反复出现问题的决策；结果部分既包含收益，也包含采用后必须处理的新问题。

FTGO是全书贯穿案例。读者要沿同一订单标识追踪消费者、餐厅、厨房、配送、信用和通知状态，明确哪个服务拥有哪份事实、谁可修改、消息何时可重放、查询允许多旧、测试在哪层证明、发布如何停止和回退。验收不变量是：生产服务的每个请求都可验证主体与权限、追溯配置版本并关联日志/跨度/指标；健康信号表达真实依赖边界且不泄露敏感信息。

## 核心词汇与初版边界

<Term def="访问令牌是“第11章 开发面向生产环境的微服务应用”的第1个核心概念，必须同时说明问题、约束、机制、代价、失败反例和可观测证据">访问令牌</Term>、<Term def="外部化配置是“第11章 开发面向生产环境的微服务应用”的第2个核心概念，必须同时说明问题、约束、机制、代价、失败反例和可观测证据">外部化配置</Term>、<Term def="健康检查API是“第11章 开发面向生产环境的微服务应用”的第3个核心概念，必须同时说明问题、约束、机制、代价、失败反例和可观测证据">健康检查API</Term>、<Term def="分布式追踪是“第11章 开发面向生产环境的微服务应用”的第4个核心概念，必须同时说明问题、约束、机制、代价、失败反例和可观测证据">分布式追踪</Term>、<Term def="微服务基底是“第11章 开发面向生产环境的微服务应用”的第5个核心概念，必须同时说明问题、约束、机制、代价、失败反例和可观测证据">微服务基底</Term>

以上词汇都要回答六个问题：解决什么问题、受什么约束、状态由谁持有、失败如何传播、怎样恢复、用什么证据证明。2018/2019初版之后出现的第二版结构、云产品行为和模式补充只能作为迁移差异，不能改写本页正式目录。

## 原书目录逐节点重构

### 11.1 开发安全的服务

**正式节点 1/4。** 边缘验证访问令牌后仍需把身份和授权上下文安全传播到服务；服务执行最小权限检查，令牌生命周期、签名密钥和审计可追踪。

本节的三级目录如下，均在本页展开而不合并成泛化主题：

- 11.1.1 传统单体应用中的安全概述
- 11.1.2 在微服务架构中实现安全

把“11.1 开发安全的服务”放回“把安全、外部化配置、健康检查、日志、追踪、指标、异常与审计纳入服务契约，并用微服务基底复用横切能力”这条责任链：输入来自“本章输入与版本契约”，输出进入“11.2 设计可配置的服务”。先写主体、拥有的数据、命令或查询、同步与异步边界、可见完成点，再画正常、超时、重复、乱序、部分失败和恢复轨迹。不能用框架名称替代协议语义，也不能用组件健康替代“生产服务的每个请求都可验证主体与权限、追溯配置版本并关联日志/跨度/指标；健康信号表达真实依赖边界且不泄露敏感信息”。

实验固定FTGO业务样本、初版代码语义、数据、负载和观测窗口，只改变一个架构变量或故障。比较状态转移、P50/P95/P99、错误、队列滞后、重试放大、恢复时间和最终业务集合；若只有HTTP成功、消息已确认、Pod就绪或函数返回，而没有持久状态与业务对账，本节仍未通过。

### 11.2 设计可配置的服务

**正式节点 2/4。** 配置与制品分离并有模式、版本和回滚；推送由部署系统注入，拉取由服务读取配置服务器，两者都需定义启动失败和动态刷新语义。

本节的三级目录如下，均在本页展开而不合并成泛化主题：

- 11.2.1 使用推送式外部化配置
- 11.2.2 使用拉取式外部化配置

把“11.2 设计可配置的服务”放回“把安全、外部化配置、健康检查、日志、追踪、指标、异常与审计纳入服务契约，并用微服务基底复用横切能力”这条责任链：输入来自“11.1 开发安全的服务”，输出进入“11.3 设计可观测的服务”。先写主体、拥有的数据、命令或查询、同步与异步边界、可见完成点，再画正常、超时、重复、乱序、部分失败和恢复轨迹。不能用框架名称替代协议语义，也不能用组件健康替代“生产服务的每个请求都可验证主体与权限、追溯配置版本并关联日志/跨度/指标；健康信号表达真实依赖边界且不泄露敏感信息”。

实验固定FTGO业务样本、初版代码语义、数据、负载和观测窗口，只改变一个架构变量或故障。比较状态转移、P50/P95/P99、错误、队列滞后、重试放大、恢复时间和最终业务集合；若只有HTTP成功、消息已确认、Pod就绪或函数返回，而没有持久状态与业务对账，本节仍未通过。

### 11.3 设计可观测的服务

**正式节点 3/4。** 日志回答离散事件，追踪重建请求路径，指标观察总体趋势，异常平台聚类堆栈，审计记录敏感动作；共同关联而不相互替代。

本节的三级目录如下，均在本页展开而不合并成泛化主题：

- 11.3.1 使用健康检查API模式
- 11.3.2 使用日志聚合模式
- 11.3.3 使用分布式追踪模式
- 11.3.4 使用应用指标模式
- 11.3.5 使用异常追踪模式
- 11.3.6 使用审计日志模式

把“11.3 设计可观测的服务”放回“把安全、外部化配置、健康检查、日志、追踪、指标、异常与审计纳入服务契约，并用微服务基底复用横切能力”这条责任链：输入来自“11.2 设计可配置的服务”，输出进入“11.4 使用微服务基底模式开发服务”。先写主体、拥有的数据、命令或查询、同步与异步边界、可见完成点，再画正常、超时、重复、乱序、部分失败和恢复轨迹。不能用框架名称替代协议语义，也不能用组件健康替代“生产服务的每个请求都可验证主体与权限、追溯配置版本并关联日志/跨度/指标；健康信号表达真实依赖边界且不泄露敏感信息”。

实验固定FTGO业务样本、初版代码语义、数据、负载和观测窗口，只改变一个架构变量或故障。比较状态转移、P50/P95/P99、错误、队列滞后、重试放大、恢复时间和最终业务集合；若只有HTTP成功、消息已确认、Pod就绪或函数返回，而没有持久状态与业务对账，本节仍未通过。

### 11.4 使用微服务基底模式开发服务

**正式节点 4/4。** 基底封装语言内横切能力并提供一致默认值；服务网格可下沉部分通信策略，但领域授权、业务遥测和正确性仍在应用内。

本节的三级目录如下，均在本页展开而不合并成泛化主题：

- 11.4.1 使用微服务基底
- 11.4.2 从微服务基底到服务网格

把“11.4 使用微服务基底模式开发服务”放回“把安全、外部化配置、健康检查、日志、追踪、指标、异常与审计纳入服务契约，并用微服务基底复用横切能力”这条责任链：输入来自“11.3 设计可观测的服务”，输出进入“本章证据门与跨章连接”。先写主体、拥有的数据、命令或查询、同步与异步边界、可见完成点，再画正常、超时、重复、乱序、部分失败和恢复轨迹。不能用框架名称替代协议语义，也不能用组件健康替代“生产服务的每个请求都可验证主体与权限、追溯配置版本并关联日志/跨度/指标；健康信号表达真实依赖边界且不泄露敏感信息”。

实验固定FTGO业务样本、初版代码语义、数据、负载和观测窗口，只改变一个架构变量或故障。比较状态转移、P50/P95/P99、错误、队列滞后、重试放大、恢复时间和最终业务集合；若只有HTTP成功、消息已确认、Pod就绪或函数返回，而没有持久状态与业务对账，本节仍未通过。

## 模式决策矩阵

| 模式或机制  | 要解决的问题               | 核心机制                     | 最小证据             |
| ----------- | -------------------------- | ---------------------------- | -------------------- |
| 外部化配置  | 环境差异不应重建制品       | 版本化配置在运行时注入       | 制品哈希与配置版本   |
| 健康检查API | 平台需要判断实例是否接流量 | 分别暴露存活、就绪与启动状态 | 探针结果和真实依赖   |
| 分布式追踪  | 一次请求跨越多个服务       | 传播trace/span上下文         | 入口到终态的完整跨度 |
| 微服务基底  | 横切能力在服务间重复       | 受治理的库或模板提供默认能力 | 版本采用率和漂移检测 |

选择模式时先写“未采用会怎样”，再写采用后的新成本。例如同步RPC需要超时、断路器和发现；异步消息需要事务性发件箱、顺序、去重和死信；Saga需要补偿和隔离对策；CQRS需要视图水位与重建。相关模式组成因果链，不能独立打勾。

## 机制、状态与不变量

### 责任与数据所有权

服务只能通过API或消息公开能力，数据表、聚合事件流和查询视图都有唯一写入责任。一个业务命令可跨越多个服务，但每一步本地事务、发出的消息、预期回复、补偿和终态都必须可追踪。共享数据库、跨库联表和绕过API的“临时读取”会让边界失真，必须记录退出日期和删除证据。

### 完成点与失败语义

客户端收到响应、网关完成组合、消息代理确认、消费者写库、Saga进入终态、CQRS视图追上水位、Pod通过就绪和业务对账完成是不同检查点。超时表示结果未知，不等于失败；至少一次消息表示可能重复，不等于恰好一次业务副作用。恢复从持久事实继续，而不是从易失内存猜测。

### 可用性与放大效应

串行依赖近似满足 $A_{path}=\prod_i A_i$。每次失败都重试 $r$ 次时，最坏尝试量接近 $N(1+r)$；多层重试会相乘而非相加。该估算只用于发现方向性风险，生产结论还必须固定业务、数据、负载、版本和观测窗口，测量P50/P95/P99、错误、饱和度、队列滞后、恢复时间和最终业务集合。

## 可复现实现与证据格式

```yaml
decision:
  book: microservices-patterns-first-edition
  unit: msp-11-production-ready-services
  problem: "把安全、外部化配置、健康检查、日志、追踪、指标、异常与审计纳入服务契约，并用微服务基底复用横切能力"
  invariant: business-state-reconciled
  observe: [contract-version, state, trace, latency-p99, queue-lag, business]
  stop: [error-budget-exhausted, invariant-broken]
  rollback: verified-baseline
```

```java
// 业务状态与待发消息在同一个本地事务中提交；发布器可重试，消费者按消息ID幂等。
@Transactional
Result handle(Command command) {
  Aggregate aggregate = repository.load(command.aggregateId());
  Events events = aggregate.decide(command);
  repository.save(aggregate, events.expectedVersion());
  outbox.append(events.withCorrelation(command.correlationId()));
  return Result.accepted(aggregate.id(), aggregate.version());
}
```

```text
experiment = fixed(ftgo-case, data, load, version, observation-window)
           + change(one boundary | failure | retry | ordering variable)
           + observe(api + message + state + telemetry + business result)
           + reconcile(expected invariant, durable terminal state)
```

## 三步交互实验

<Stepper>
  <Step title="1. 选择目录节点与模式">
    选择一个正式节点，先写问题和约束，再观察模式机制与最小证据；不要从产品名开始。
    <Msp11ProductionReadyServicesPatternLab />
  </Step>
  <Step title="2. 注入部分失败并预测">
    只改变请求量、超时、重复率、重试预算或幂等开关之一，比较流量放大、未知结果与业务副作用。
    <Msp11ProductionReadyServicesFailureLab />
  </Step>
  <Step title="3. 通过独立证据门">
    目录、版本、契约、状态、反例、遥测、对账、停止与回退齐全后才接受结论。
    <Msp11ProductionReadyServicesEvidenceLab />
  </Step>
</Stepper>

<Callout type="trap" title="模式清单陷阱">
  把Saga、CQRS、API
  Gateway、容器等名称列出来并不等于设计。必须说明前驱问题、约束、状态所有者、结果与后继模式，否则无法判断是否适用。
</Callout>

<Callout type="trap" title="恰好一次陷阱">
  网络重试和至少一次消息都可能重复。代理确认、HTTP成功和消费者返回不能证明业务副作用恰好一次；必须用业务键、幂等记录与最终状态对账。
</Callout>

<Callout type="warning" title="隔离实验">
  故障注入、补偿、消息回放、数据库迁移、切流和权限轮换只在隔离环境演练。先保留基线，规定影响上界、停止条件、恢复步骤和回退验收。
</Callout>

## 独立交接与跨章连接

交接包至少包含威胁与信任边界、配置版本表、遥测关联图、基底能力清单、初版目录映射、服务和数据所有权、API/事件模式、关联标识、失败状态机、测试层级、遥测查询、业务对账、停止与回退。另一位维护者必须能在无口头补充的情况下重放一个成功案例、一个超时未知案例、一个重复案例和一个恢复案例。

本页输出不是终点。它会成为后继模式的输入：边界产生通信，通信产生一致性与查询问题，分布式行为产生测试与可观测需求，运行单元产生部署问题，已有单体产生迁移问题。若后继问题没有负责人和证据门，当前选择尚未完成。

## 本章回顾

重新完成“把安全、外部化配置、健康检查、日志、追踪、指标、异常与审计纳入服务契约，并用微服务基底复用横切能力”：从初版目录和FTGO问题开始，明确主体、数据与契约，推导模式并记录收益和代价；再通过正常、超时、重复、乱序、部分失败和恢复实验，验证“生产服务的每个请求都可验证主体与权限、追溯配置版本并关联日志/跨度/指标；健康信号表达真实依赖边界且不泄露敏感信息”。只有目录节点、机制轨迹和最终业务状态均可独立复现，本页才算完成。

## 复习与运行验收

<Exercises>
**问题 1**：本页为什么必须逐项覆盖正式目录？
<Answer>目录节点共同组成“把安全、外部化配置、健康检查、日志、追踪、指标、异常与审计纳入服务契约，并用微服务基底复用横切能力”的完整因果链；漏项会使威胁与信任边界、配置版本表、遥测关联图、基底能力清单无法独立复现，并可能隐藏采用模式后出现的后继问题。</Answer>

**问题 2**：最小业务不变量是什么？

<Answer>
  生产服务的每个请求都可验证主体与权限、追溯配置版本并关联日志/跨度/指标；健康信号表达真实依赖边界且不泄露敏感信息。组件状态只能作为中间证据，最终还要核对用户可见结果和持久事实。
</Answer>

**问题 3**：怎样构造最小反例？

<Answer>
  固定FTGO案例、数据、负载、版本与观测窗口，只改变一个边界、故障、重试或顺序变量，比较组件轨迹与最终业务集合。
</Answer>

**问题 4**：为什么不能混入第二版目录？

<Answer>
  本课程对应2018英文初版和2019中文版；第二版重新组织并新增内容，只能作为迁移差异，不能改变242个初版编号节点的分母。
</Answer>

**问题 5**：性能与恢复结论需要哪些证据？

<Answer>
  需要契约版本、日志、追踪、指标、消息偏移、状态快照、P50/P95/P99、错误、饱和度、恢复时间和最终业务对账。
</Answer>

**问题 6**：独立交接需要什么？

<Answer>需要初版目录映射、决策记录、威胁与信任边界、配置版本表、遥测关联图、基底能力清单、故障反例、测试、业务对账、停止、恢复、回退与责任人。</Answer>
</Exercises>

<Glossary>
  <GlossaryItem term="访问令牌">
    访问令牌在本页不是孤立名词；它必须服务于“生产服务的每个请求都可验证主体与权限、追溯配置版本并关联日志/跨度/指标；健康信号表达真实依赖边界且不泄露敏感信息”，并能由版本、契约、状态轨迹、故障注入和最终业务对账独立证明。
  </GlossaryItem>
  <GlossaryItem term="外部化配置">
    外部化配置在本页不是孤立名词；它必须服务于“生产服务的每个请求都可验证主体与权限、追溯配置版本并关联日志/跨度/指标；健康信号表达真实依赖边界且不泄露敏感信息”，并能由版本、契约、状态轨迹、故障注入和最终业务对账独立证明。
  </GlossaryItem>
  <GlossaryItem term="健康检查API">
    健康检查API在本页不是孤立名词；它必须服务于“生产服务的每个请求都可验证主体与权限、追溯配置版本并关联日志/跨度/指标；健康信号表达真实依赖边界且不泄露敏感信息”，并能由版本、契约、状态轨迹、故障注入和最终业务对账独立证明。
  </GlossaryItem>
  <GlossaryItem term="分布式追踪">
    分布式追踪在本页不是孤立名词；它必须服务于“生产服务的每个请求都可验证主体与权限、追溯配置版本并关联日志/跨度/指标；健康信号表达真实依赖边界且不泄露敏感信息”，并能由版本、契约、状态轨迹、故障注入和最终业务对账独立证明。
  </GlossaryItem>
  <GlossaryItem term="微服务基底">
    微服务基底在本页不是孤立名词；它必须服务于“生产服务的每个请求都可验证主体与权限、追溯配置版本并关联日志/跨度/指标；健康信号表达真实依赖边界且不泄露敏感信息”，并能由版本、契约、状态轨迹、故障注入和最终业务对账独立证明。
  </GlossaryItem>
</Glossary>

[← 上一页：第10章 微服务架构中的测试策略（下）](/learn/microservices-patterns/10-10-testing-part-2/msp-10-testing-part-2) · [下一页：第12章 部署微服务应用 →](/learn/microservices-patterns/12-12-deploying-microservices/msp-12-deploying-microservices)

<Attribution
  adaptedFrom="Chris Richardson《Microservices Patterns》2018英文初版 / 喻勇译2019中文版"
  adaptedUrl="https://www.manning.com/books/microservices-patterns"
/>
