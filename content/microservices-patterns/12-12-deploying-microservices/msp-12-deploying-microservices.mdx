---
title: "第12章 部署微服务应用"
description: "依据Chris Richardson英文初版与喻勇译2019中文版完整目录：比较语言包、虚拟机、容器和Serverless四类部署模式，并以Kubernetes、零停机发布、服务网格和Lambda落实选择"
type: C
section: "微服务架构设计模式（2019中文版初版 / 2018英文初版）"
order: 12
draft: true
math: false
sourceUrl: "https://livebook.manning.com/book/microservices-patterns/table-of-contents"
demo: true
---

import {
  Msp12DeployingMicroservicesPatternLab,
  Msp12DeployingMicroservicesFailureLab,
  Msp12DeployingMicroservicesEvidenceLab,
} from "@/components/mdx/microservices-patterns/diagrams/msp-12-deploying-microservices";

# 第12章 部署微服务应用

本课程对应Chris Richardson著、喻勇译《微服务架构设计模式》，机械工业出版社2019年出版，ISBN 9787111624127，馆藏著录为前置26页、正文455页。英文原版为Manning 2018年10月初版，ISBN 9781617294549、520页。课程固定该初版的13章与44个模式，不混入当前第二版MEAP重新编排和新增的章节。

全书权威分母为13章、52个二级节、177个三级节，共242个编号目录节点；另设学习地图和总复习，共15页、45个交互视图和90道独立复习题。本章覆盖6个二级节与20个三级节，连同章标题共27个编号节点。

<Objectives>

- 能解释“第12章 部署微服务应用”的正式目录节点，并把每个模式写成问题、约束、解决方案、结果与相关模式。
- 能绘制FTGO主体、服务、数据所有权、同步/异步边界和业务完成点，区分局部成功与最终业务成功。
- 能比较正常、超时、重复、乱序、部分失败与恢复轨迹，并用状态、遥测和业务对账验证“每个服务制品、运行身份、资源、配置和版本可追踪且可独立回退；部署完成与发布接流量分离，失败版本不会继续扩大影响”。
- 能设计部署模式决策表、Kubernetes对象图、发布状态机、Lambda资源预算，写出停止、恢复、回退和独立交接条件。

</Objectives>

## 从一个可证伪的问题开始

先预测：仅看到容器Running或函数上传成功就宣布发布完成，会遗漏就绪、迁移、流量、业务指标和回退验证；共享运行时也可能让服务互相耗尽资源。把预测写成可观察反例，再决定是否采用模式。模式不是技术标签，而是在特定约束中解决反复出现问题的决策；结果部分既包含收益，也包含采用后必须处理的新问题。

FTGO是全书贯穿案例。读者要沿同一订单标识追踪消费者、餐厅、厨房、配送、信用和通知状态，明确哪个服务拥有哪份事实、谁可修改、消息何时可重放、查询允许多旧、测试在哪层证明、发布如何停止和回退。验收不变量是：每个服务制品、运行身份、资源、配置和版本可追踪且可独立回退；部署完成与发布接流量分离，失败版本不会继续扩大影响。

## 核心词汇与初版边界

<Term def="语言特定发布包是“第12章 部署微服务应用”的第1个核心概念，必须同时说明问题、约束、机制、代价、失败反例和可观测证据">语言特定发布包</Term>、<Term def="虚拟机是“第12章 部署微服务应用”的第2个核心概念，必须同时说明问题、约束、机制、代价、失败反例和可观测证据">虚拟机</Term>、<Term def="容器是“第12章 部署微服务应用”的第3个核心概念，必须同时说明问题、约束、机制、代价、失败反例和可观测证据">容器</Term>、<Term def="Kubernetes是“第12章 部署微服务应用”的第4个核心概念，必须同时说明问题、约束、机制、代价、失败反例和可观测证据">Kubernetes</Term>、<Term def="Serverless是“第12章 部署微服务应用”的第5个核心概念，必须同时说明问题、约束、机制、代价、失败反例和可观测证据">Serverless</Term>

以上词汇都要回答六个问题：解决什么问题、受什么约束、状态由谁持有、失败如何传播、怎样恢复、用什么证据证明。2018/2019初版之后出现的第二版结构、云产品行为和模式补充只能作为迁移差异，不能改写本页正式目录。

## 原书目录逐节点重构

### 12.1 部署模式：编程语言特定的发布包格式

**正式节点 1/6。** 多个服务进程共享主机和运行时，启动快、资源高效，但隔离弱且技术栈受限；必须限制资源、端口、版本和故障传播。

本节的三级目录如下，均在本页展开而不合并成泛化主题：

- 12.1.1 服务作为语言特定包的好处
- 12.1.2 服务作为语言特定包的弊端

把“12.1 部署模式：编程语言特定的发布包格式”放回“比较语言包、虚拟机、容器和Serverless四类部署模式，并以Kubernetes、零停机发布、服务网格和Lambda落实选择”这条责任链：输入来自“本章输入与版本契约”，输出进入“12.2 部署模式：将服务部署为虚拟机”。先写主体、拥有的数据、命令或查询、同步与异步边界、可见完成点，再画正常、超时、重复、乱序、部分失败和恢复轨迹。不能用框架名称替代协议语义，也不能用组件健康替代“每个服务制品、运行身份、资源、配置和版本可追踪且可独立回退；部署完成与发布接流量分离，失败版本不会继续扩大影响”。

实验固定FTGO业务样本、初版代码语义、数据、负载和观测窗口，只改变一个架构变量或故障。比较状态转移、P50/P95/P99、错误、队列滞后、重试放大、恢复时间和最终业务集合；若只有HTTP成功、消息已确认、Pod就绪或函数返回，而没有持久状态与业务对账，本节仍未通过。

### 12.2 部署模式：将服务部署为虚拟机

**正式节点 2/6。** 每个服务实例拥有VM隔离和成熟基础设施，但镜像大、启动慢、资源利用率较低，制品到镜像映射必须可重现。

本节的三级目录如下，均在本页展开而不合并成泛化主题：

- 12.2.1 将服务部署为虚拟机的好处
- 12.2.2 将服务部署为虚拟机的弊端

把“12.2 部署模式：将服务部署为虚拟机”放回“比较语言包、虚拟机、容器和Serverless四类部署模式，并以Kubernetes、零停机发布、服务网格和Lambda落实选择”这条责任链：输入来自“12.1 部署模式：编程语言特定的发布包格式”，输出进入“12.3 部署模式：将服务部署为容器”。先写主体、拥有的数据、命令或查询、同步与异步边界、可见完成点，再画正常、超时、重复、乱序、部分失败和恢复轨迹。不能用框架名称替代协议语义，也不能用组件健康替代“每个服务制品、运行身份、资源、配置和版本可追踪且可独立回退；部署完成与发布接流量分离，失败版本不会继续扩大影响”。

实验固定FTGO业务样本、初版代码语义、数据、负载和观测窗口，只改变一个架构变量或故障。比较状态转移、P50/P95/P99、错误、队列滞后、重试放大、恢复时间和最终业务集合；若只有HTTP成功、消息已确认、Pod就绪或函数返回，而没有持久状态与业务对账，本节仍未通过。

### 12.3 部署模式：将服务部署为容器

**正式节点 3/6。** 容器把应用与依赖封装成轻量镜像并提高密度；共享内核、镜像供应链、持久化和编排仍需额外控制。

本节的三级目录如下，均在本页展开而不合并成泛化主题：

- 12.3.1 使用Docker部署服务
- 12.3.2 将服务部署为容器的好处
- 12.3.3 将服务部署为容器的弊端

把“12.3 部署模式：将服务部署为容器”放回“比较语言包、虚拟机、容器和Serverless四类部署模式，并以Kubernetes、零停机发布、服务网格和Lambda落实选择”这条责任链：输入来自“12.2 部署模式：将服务部署为虚拟机”，输出进入“12.4 使用Kubernetes部署FTGO应用程序”。先写主体、拥有的数据、命令或查询、同步与异步边界、可见完成点，再画正常、超时、重复、乱序、部分失败和恢复轨迹。不能用框架名称替代协议语义，也不能用组件健康替代“每个服务制品、运行身份、资源、配置和版本可追踪且可独立回退；部署完成与发布接流量分离，失败版本不会继续扩大影响”。

实验固定FTGO业务样本、初版代码语义、数据、负载和观测窗口，只改变一个架构变量或故障。比较状态转移、P50/P95/P99、错误、队列滞后、重试放大、恢复时间和最终业务集合；若只有HTTP成功、消息已确认、Pod就绪或函数返回，而没有持久状态与业务对账，本节仍未通过。

### 12.4 使用Kubernetes部署FTGO应用程序

**正式节点 4/6。** Deployment维护副本，Service提供稳定发现，Config/Secret注入环境；滚动更新结合就绪门和流量权重，把制品部署与用户发布分开。

本节的三级目录如下，均在本页展开而不合并成泛化主题：

- 12.4.1 Kubernetes概述
- 12.4.2 在Kubernetes上部署Restaurant Service
- 12.4.3 部署API Gateway
- 12.4.4 零停机部署
- 12.4.5 使用服务网格分离部署与发布

把“12.4 使用Kubernetes部署FTGO应用程序”放回“比较语言包、虚拟机、容器和Serverless四类部署模式，并以Kubernetes、零停机发布、服务网格和Lambda落实选择”这条责任链：输入来自“12.3 部署模式：将服务部署为容器”，输出进入“12.5 部署模式：Serverless部署”。先写主体、拥有的数据、命令或查询、同步与异步边界、可见完成点，再画正常、超时、重复、乱序、部分失败和恢复轨迹。不能用框架名称替代协议语义，也不能用组件健康替代“每个服务制品、运行身份、资源、配置和版本可追踪且可独立回退；部署完成与发布接流量分离，失败版本不会继续扩大影响”。

实验固定FTGO业务样本、初版代码语义、数据、负载和观测窗口，只改变一个架构变量或故障。比较状态转移、P50/P95/P99、错误、队列滞后、重试放大、恢复时间和最终业务集合；若只有HTTP成功、消息已确认、Pod就绪或函数返回，而没有持久状态与业务对账，本节仍未通过。

### 12.5 部署模式：Serverless部署

**正式节点 5/6。** 函数按事件触发和执行计费，减少服务器管理；冷启动、运行时限制、状态外置、供应商依赖和本地测试构成适用边界。

本节的三级目录如下，均在本页展开而不合并成泛化主题：

- 12.5.1 使用AWS Lambda进行Serverless部署概述
- 12.5.2 开发Lambda函数
- 12.5.3 调用Lambda函数
- 12.5.4 使用Lambda函数的好处
- 12.5.5 使用Lambda函数的弊端

把“12.5 部署模式：Serverless部署”放回“比较语言包、虚拟机、容器和Serverless四类部署模式，并以Kubernetes、零停机发布、服务网格和Lambda落实选择”这条责任链：输入来自“12.4 使用Kubernetes部署FTGO应用程序”，输出进入“12.6 使用AWS Lambda和AWS Gateway部署RESTful服务”。先写主体、拥有的数据、命令或查询、同步与异步边界、可见完成点，再画正常、超时、重复、乱序、部分失败和恢复轨迹。不能用框架名称替代协议语义，也不能用组件健康替代“每个服务制品、运行身份、资源、配置和版本可追踪且可独立回退；部署完成与发布接流量分离，失败版本不会继续扩大影响”。

实验固定FTGO业务样本、初版代码语义、数据、负载和观测窗口，只改变一个架构变量或故障。比较状态转移、P50/P95/P99、错误、队列滞后、重试放大、恢复时间和最终业务集合；若只有HTTP成功、消息已确认、Pod就绪或函数返回，而没有持久状态与业务对账，本节仍未通过。

### 12.6 使用AWS Lambda和AWS Gateway部署RESTful服务

**正式节点 6/6。** API Gateway将HTTP映射到Lambda，函数访问持久服务并返回契约响应；包哈希、IAM权限、并发上界和回滚别名必须进入发布证据。

本节的三级目录如下，均在本页展开而不合并成泛化主题：

- 12.6.1 Restaurant Service的AWS Lambda版本设计
- 12.6.2 将服务打包为ZIP文件
- 12.6.3 使用Serverless框架部署Lambda函数

把“12.6 使用AWS Lambda和AWS Gateway部署RESTful服务”放回“比较语言包、虚拟机、容器和Serverless四类部署模式，并以Kubernetes、零停机发布、服务网格和Lambda落实选择”这条责任链：输入来自“12.5 部署模式：Serverless部署”，输出进入“本章证据门与跨章连接”。先写主体、拥有的数据、命令或查询、同步与异步边界、可见完成点，再画正常、超时、重复、乱序、部分失败和恢复轨迹。不能用框架名称替代协议语义，也不能用组件健康替代“每个服务制品、运行身份、资源、配置和版本可追踪且可独立回退；部署完成与发布接流量分离，失败版本不会继续扩大影响”。

实验固定FTGO业务样本、初版代码语义、数据、负载和观测窗口，只改变一个架构变量或故障。比较状态转移、P50/P95/P99、错误、队列滞后、重试放大、恢复时间和最终业务集合；若只有HTTP成功、消息已确认、Pod就绪或函数返回，而没有持久状态与业务对账，本节仍未通过。

## 模式决策矩阵

| 模式或机制   | 要解决的问题             | 核心机制               | 最小证据             |
| ------------ | ------------------------ | ---------------------- | -------------------- |
| 语言特定包   | 追求最高密度和快速启动   | 同主机运行多个语言进程 | 资源隔离与版本冲突   |
| 服务即虚拟机 | 需要强隔离和成熟基础设施 | 每实例一个不可变VM镜像 | 启动时间与镜像追溯   |
| 服务即容器   | 需要轻量可移植部署单元   | 镜像加编排器调度       | 镜像签名、就绪与资源 |
| Serverless   | 事件驱动且负载突发       | 托管函数按调用伸缩     | 冷启动、并发和成本   |

选择模式时先写“未采用会怎样”，再写采用后的新成本。例如同步RPC需要超时、断路器和发现；异步消息需要事务性发件箱、顺序、去重和死信；Saga需要补偿和隔离对策；CQRS需要视图水位与重建。相关模式组成因果链，不能独立打勾。

## 机制、状态与不变量

### 责任与数据所有权

服务只能通过API或消息公开能力，数据表、聚合事件流和查询视图都有唯一写入责任。一个业务命令可跨越多个服务，但每一步本地事务、发出的消息、预期回复、补偿和终态都必须可追踪。共享数据库、跨库联表和绕过API的“临时读取”会让边界失真，必须记录退出日期和删除证据。

### 完成点与失败语义

客户端收到响应、网关完成组合、消息代理确认、消费者写库、Saga进入终态、CQRS视图追上水位、Pod通过就绪和业务对账完成是不同检查点。超时表示结果未知，不等于失败；至少一次消息表示可能重复，不等于恰好一次业务副作用。恢复从持久事实继续，而不是从易失内存猜测。

### 可用性与放大效应

串行依赖近似满足 $A_{path}=\prod_i A_i$。每次失败都重试 $r$ 次时，最坏尝试量接近 $N(1+r)$；多层重试会相乘而非相加。该估算只用于发现方向性风险，生产结论还必须固定业务、数据、负载、版本和观测窗口，测量P50/P95/P99、错误、饱和度、队列滞后、恢复时间和最终业务集合。

## 可复现实现与证据格式

```yaml
decision:
  book: microservices-patterns-first-edition
  unit: msp-12-deploying-microservices
  problem: "比较语言包、虚拟机、容器和Serverless四类部署模式，并以Kubernetes、零停机发布、服务网格和Lambda落实选择"
  invariant: business-state-reconciled
  observe: [contract-version, state, trace, latency-p99, queue-lag, business]
  stop: [error-budget-exhausted, invariant-broken]
  rollback: verified-baseline
```

```java
// 业务状态与待发消息在同一个本地事务中提交；发布器可重试，消费者按消息ID幂等。
@Transactional
Result handle(Command command) {
  Aggregate aggregate = repository.load(command.aggregateId());
  Events events = aggregate.decide(command);
  repository.save(aggregate, events.expectedVersion());
  outbox.append(events.withCorrelation(command.correlationId()));
  return Result.accepted(aggregate.id(), aggregate.version());
}
```

```text
experiment = fixed(ftgo-case, data, load, version, observation-window)
           + change(one boundary | failure | retry | ordering variable)
           + observe(api + message + state + telemetry + business result)
           + reconcile(expected invariant, durable terminal state)
```

## 三步交互实验

<Stepper>
  <Step title="1. 选择目录节点与模式">
    选择一个正式节点，先写问题和约束，再观察模式机制与最小证据；不要从产品名开始。
    <Msp12DeployingMicroservicesPatternLab />
  </Step>
  <Step title="2. 注入部分失败并预测">
    只改变请求量、超时、重复率、重试预算或幂等开关之一，比较流量放大、未知结果与业务副作用。
    <Msp12DeployingMicroservicesFailureLab />
  </Step>
  <Step title="3. 通过独立证据门">
    目录、版本、契约、状态、反例、遥测、对账、停止与回退齐全后才接受结论。
    <Msp12DeployingMicroservicesEvidenceLab />
  </Step>
</Stepper>

<Callout type="trap" title="模式清单陷阱">
  把Saga、CQRS、API
  Gateway、容器等名称列出来并不等于设计。必须说明前驱问题、约束、状态所有者、结果与后继模式，否则无法判断是否适用。
</Callout>

<Callout type="trap" title="恰好一次陷阱">
  网络重试和至少一次消息都可能重复。代理确认、HTTP成功和消费者返回不能证明业务副作用恰好一次；必须用业务键、幂等记录与最终状态对账。
</Callout>

<Callout type="warning" title="隔离实验">
  故障注入、补偿、消息回放、数据库迁移、切流和权限轮换只在隔离环境演练。先保留基线，规定影响上界、停止条件、恢复步骤和回退验收。
</Callout>

## 独立交接与跨章连接

交接包至少包含部署模式决策表、Kubernetes对象图、发布状态机、Lambda资源预算、初版目录映射、服务和数据所有权、API/事件模式、关联标识、失败状态机、测试层级、遥测查询、业务对账、停止与回退。另一位维护者必须能在无口头补充的情况下重放一个成功案例、一个超时未知案例、一个重复案例和一个恢复案例。

本页输出不是终点。它会成为后继模式的输入：边界产生通信，通信产生一致性与查询问题，分布式行为产生测试与可观测需求，运行单元产生部署问题，已有单体产生迁移问题。若后继问题没有负责人和证据门，当前选择尚未完成。

## 本章回顾

重新完成“比较语言包、虚拟机、容器和Serverless四类部署模式，并以Kubernetes、零停机发布、服务网格和Lambda落实选择”：从初版目录和FTGO问题开始，明确主体、数据与契约，推导模式并记录收益和代价；再通过正常、超时、重复、乱序、部分失败和恢复实验，验证“每个服务制品、运行身份、资源、配置和版本可追踪且可独立回退；部署完成与发布接流量分离，失败版本不会继续扩大影响”。只有目录节点、机制轨迹和最终业务状态均可独立复现，本页才算完成。

## 复习与运行验收

<Exercises>
**问题 1**：本页为什么必须逐项覆盖正式目录？
<Answer>目录节点共同组成“比较语言包、虚拟机、容器和Serverless四类部署模式，并以Kubernetes、零停机发布、服务网格和Lambda落实选择”的完整因果链；漏项会使部署模式决策表、Kubernetes对象图、发布状态机、Lambda资源预算无法独立复现，并可能隐藏采用模式后出现的后继问题。</Answer>

**问题 2**：最小业务不变量是什么？

<Answer>
  每个服务制品、运行身份、资源、配置和版本可追踪且可独立回退；部署完成与发布接流量分离，失败版本不会继续扩大影响。组件状态只能作为中间证据，最终还要核对用户可见结果和持久事实。
</Answer>

**问题 3**：怎样构造最小反例？

<Answer>
  固定FTGO案例、数据、负载、版本与观测窗口，只改变一个边界、故障、重试或顺序变量，比较组件轨迹与最终业务集合。
</Answer>

**问题 4**：为什么不能混入第二版目录？

<Answer>
  本课程对应2018英文初版和2019中文版；第二版重新组织并新增内容，只能作为迁移差异，不能改变242个初版编号节点的分母。
</Answer>

**问题 5**：性能与恢复结论需要哪些证据？

<Answer>
  需要契约版本、日志、追踪、指标、消息偏移、状态快照、P50/P95/P99、错误、饱和度、恢复时间和最终业务对账。
</Answer>

**问题 6**：独立交接需要什么？

<Answer>需要初版目录映射、决策记录、部署模式决策表、Kubernetes对象图、发布状态机、Lambda资源预算、故障反例、测试、业务对账、停止、恢复、回退与责任人。</Answer>
</Exercises>

<Glossary>
  <GlossaryItem term="语言特定发布包">
    语言特定发布包在本页不是孤立名词；它必须服务于“每个服务制品、运行身份、资源、配置和版本可追踪且可独立回退；部署完成与发布接流量分离，失败版本不会继续扩大影响”，并能由版本、契约、状态轨迹、故障注入和最终业务对账独立证明。
  </GlossaryItem>
  <GlossaryItem term="虚拟机">
    虚拟机在本页不是孤立名词；它必须服务于“每个服务制品、运行身份、资源、配置和版本可追踪且可独立回退；部署完成与发布接流量分离，失败版本不会继续扩大影响”，并能由版本、契约、状态轨迹、故障注入和最终业务对账独立证明。
  </GlossaryItem>
  <GlossaryItem term="容器">
    容器在本页不是孤立名词；它必须服务于“每个服务制品、运行身份、资源、配置和版本可追踪且可独立回退；部署完成与发布接流量分离，失败版本不会继续扩大影响”，并能由版本、契约、状态轨迹、故障注入和最终业务对账独立证明。
  </GlossaryItem>
  <GlossaryItem term="Kubernetes">
    Kubernetes在本页不是孤立名词；它必须服务于“每个服务制品、运行身份、资源、配置和版本可追踪且可独立回退；部署完成与发布接流量分离，失败版本不会继续扩大影响”，并能由版本、契约、状态轨迹、故障注入和最终业务对账独立证明。
  </GlossaryItem>
  <GlossaryItem term="Serverless">
    Serverless在本页不是孤立名词；它必须服务于“每个服务制品、运行身份、资源、配置和版本可追踪且可独立回退；部署完成与发布接流量分离，失败版本不会继续扩大影响”，并能由版本、契约、状态轨迹、故障注入和最终业务对账独立证明。
  </GlossaryItem>
</Glossary>

[← 上一页：第11章 开发面向生产环境的微服务应用](/learn/microservices-patterns/11-11-production-ready-services/msp-11-production-ready-services) · [下一页：第13章 微服务架构的重构策略 →](/learn/microservices-patterns/13-13-refactoring-to-microservices/msp-13-refactoring-to-microservices)

<Attribution
  adaptedFrom="Chris Richardson《Microservices Patterns》2018英文初版 / 喻勇译2019中文版"
  adaptedUrl="https://www.manning.com/books/microservices-patterns"
/>
