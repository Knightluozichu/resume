---
title: "Adaptive Performance：热状态驱动的动态质量"
type: C
section: "渲染基础、测量与热适应"
order: 4
description: "按 Unity 6 官方指南第 25—26 页复现热状态驱动的动态质量闭环。"
demo: true
math: false
draft: false
sourceUrl: "https://unity.com/resources/mobile-xr-web-game-performance-optimization-unity-6"
---


## 问题：怎样证明掌握的是官方单元 5，而不是背下一组技巧？

先预测：若只报告修改后FPS，没有设备、构建、场景、输入、热状态和画面合同，另一位开发者能否证明收益来自这次修改？不能。跨平台优化首先是受控实验，其次才是技巧选择。

Adaptive Performance从设备热与性能状态产生信号，再由可审计Scaler调整帧率或质量。策略需要迟滞、保持时间和恢复条件，避免在阈值附近抖动画质。

## 官方目录边界

本页严格对应Unity Technologies《Optimize your game performance for mobile, XR, and the web in Unity》Unity 6版第25—26页。以下目录项必须进入实现或证据：

- **Adaptive Performance**
- **Thermal Status**
- **Performance Status**
- **Bottleneck detection**
- **Adaptive Framerate**
- **Scaler profiles**

本站重新组织中文讲解、代码和实验，不逐字复制原文。平台API和包能力随Unity与设备版本变化，实际项目要固定版本并保存迁移差异。

## 核心模型与不可变条件

Adaptive Performance从设备热与性能状态产生信号，再由可审计Scaler调整帧率或质量。策略需要迟滞、保持时间和恢复条件，避免在阈值附近抖动画质。

不可变条件是：**每次自适应降级都能说明触发信号、修改项、最短保持时间和恢复门，并保持玩法可用与视觉下限。** 如果测试上下文、视觉或玩法结果发生变化，就不能把两次数字直接相减。先解释变化，再决定是否建立新基线。

## 五个必须落到证据的术语


它必须落到“热性能信号 → 瓶颈分类 → 策略迟滞 → 质量Scaler → 稳态观察 → 恢复签发”的证据链，不能只作为术语记忆。


它必须落到“热性能信号 → 瓶颈分类 → 策略迟滞 → 质量Scaler → 稳态观察 → 恢复签发”的证据链，不能只作为术语记忆。


它必须落到“热性能信号 → 瓶颈分类 → 策略迟滞 → 质量Scaler → 稳态观察 → 恢复签发”的证据链，不能只作为术语记忆。


它必须落到“热性能信号 → 瓶颈分类 → 策略迟滞 → 质量Scaler → 稳态观察 → 恢复签发”的证据链，不能只作为术语记忆。


它必须落到“热性能信号 → 瓶颈分类 → 策略迟滞 → 质量Scaler → 稳态观察 → 恢复签发”的证据链，不能只作为术语记忆。

## 从输入到发布的证据链

主链为：**热性能信号 → 瓶颈分类 → 策略迟滞 → 质量Scaler → 稳态观察 → 恢复签发**。每个节点保存平台、设备、浏览器或运行时、Unity与包版本、构建提交、场景检查点、预算、实际值和首个异常。后一节点只消费前一节点已验证事实。

### 1. 信号来源

热状态反映温度趋势，性能状态反映CPU或GPU压力；两者不能只靠瞬时FPS替代。

复现这一项时，记录输入条件、观测工具、预算阈值和下一动作。输入决定数据是否可比，工具说明在哪个线程、对象、资源或平台阶段观察；阈值来自本项目合同，下一动作必须是一轮可反证的单变量实验。

### 2. 瓶颈分类

CPU受限应减少脚本、物理或提交，GPU受限可调整分辨率、阴影或效果，策略必须对症。

复现这一项时，记录输入条件、观测工具、预算阈值和下一动作。输入决定数据是否可比，工具说明在哪个线程、对象、资源或平台阶段观察；阈值来自本项目合同，下一动作必须是一轮可反证的单变量实验。

### 3. 帧率

降低目标帧率能减少持续功耗，但会改变交互体验；XR还受显示刷新和平台运行时约束。

复现这一项时，记录输入条件、观测工具、预算阈值和下一动作。输入决定数据是否可比，工具说明在哪个线程、对象、资源或平台阶段观察；阈值来自本项目合同，下一动作必须是一轮可反证的单变量实验。

### 4. Scaler

每个Scaler只拥有一个可解释参数，定义上下限和步长，多个Scaler按优先级协同。

复现这一项时，记录输入条件、观测工具、预算阈值和下一动作。输入决定数据是否可比，工具说明在哪个线程、对象、资源或平台阶段观察；阈值来自本项目合同，下一动作必须是一轮可反证的单变量实验。

### 5. 迟滞

进入降级与恢复使用不同阈值，并设置最短保持时间，防止画质和负载来回跳变。

复现这一项时，记录输入条件、观测工具、预算阈值和下一动作。输入决定数据是否可比，工具说明在哪个线程、对象、资源或平台阶段观察；阈值来自本项目合同，下一动作必须是一轮可反证的单变量实验。

### 6. 视觉下限

最低档仍须保持UI可读、玩法提示、舒适度和平台认证要求，不能无限降质换取数字。

复现这一项时，记录输入条件、观测工具、预算阈值和下一动作。输入决定数据是否可比，工具说明在哪个线程、对象、资源或平台阶段观察；阈值来自本项目合同，下一动作必须是一轮可反证的单变量实验。

### 7. 热稳态

在代表时长内记录温度、频率、帧时间和档位变化，证明策略不是延迟而是真正控制热量。

复现这一项时，记录输入条件、观测工具、预算阈值和下一动作。输入决定数据是否可比，工具说明在哪个线程、对象、资源或平台阶段观察；阈值来自本项目合同，下一动作必须是一轮可反证的单变量实验。

## 三步复现与分步观察


## 可执行实现骨架

```csharp
public void ApplyPressure(int level)
{
    if (Time.unscaledTime < nextAllowedChange) return;
    int tier = Mathf.Clamp(level, 0, 2);
    Application.targetFrameRate = tier == 2 ? 30 : 60;
    renderAsset.renderScale = tier == 2 ? 0.75f : 1f;
    nextAllowedChange = Time.unscaledTime + 10f;
}
```

每份采集都保存结构化上下文，报告与原始文件必须通过ID互相引用：

```json
{
  "chapter": "mxrw-05-adaptive-performance",
  "officialPages": "第25—26页",
  "chain": [
    "热性能信号",
    "瓶颈分类",
    "策略迟滞",
    "质量Scaler",
    "稳态观察",
    "恢复签发"
  ],
  "platform": "mobile-web-or-xr",
  "device": "representative-target",
  "build": "commit-unity-packages",
  "scenario": "fixed-checkpoint",
  "normal": true,
  "boundary": true,
  "failureReplay": true
}
```

自动门必须保留数值、结果合同和拒绝原因：

```text
PASS context=platform+device+build+scene+quality+input
PASS chain=热性能信号 -> 瓶颈分类 -> 策略迟滞 -> 质量Scaler -> 稳态观察 -> 恢复签发
PASS invariant=每次自适应降级都能说明触发信号、修改项、最短保持时间和恢复门，并保持玩法可用与视觉下限。
REJECT changed-context | hidden-quality-loss | shifted-cost | missing-failure-replay
```

## 两个必须主动制造的失败样本


## 证据矩阵与签发标准

| 维度       | 正常样本         | 边界样本           | 失败样本         | 签发条件       |
| ---------- | ---------------- | ------------------ | ---------------- | -------------- |
| 平台上下文 | 代表设备固定构建 | 最低规格或长时运行 | 故意改变一项条件 | 差异可解释     |
| 主证据链   | 六节点均记录     | 接近预算输入       | 首个节点注入偏差 | 能定位首因     |
| 性能结果   | 达到预算         | 无长尾越界         | 主动制造回归     | 同输入可复验   |
| 正确性     | 视觉玩法一致     | 生命周期完整       | 降质或转移成本   | 所有门同时通过 |

## 练习：把官方单元 5变成可审计交付


## 小结

- 是否逐项覆盖本页官方目录，而没有用通用建议替代？
- 是否沿“热性能信号 → 瓶颈分类 → 策略迟滞 → 质量Scaler → 稳态观察 → 恢复签发”保存可追溯中间证据？
- 是否在真实移动、浏览器或XR目标中验证，而非只看Editor？
- 是否主动制造失败，并证明优化没有转移成本或破坏结果？

## 术语表


## 来源与改编边界

