---
title: "Shader：过绘、透明、后处理与移动 GPU 指标"
type: C
section: "GPU、光照与 Shader"
order: 2
description: "按 Unity 6 官方指南第 62—66 页复现过绘、透明、后处理与移动 GPU 指标。"
demo: true
math: false
draft: false
sourceUrl: "https://unity.com/resources/mobile-xr-web-game-performance-optimization-unity-6"
---


## 问题：怎样证明掌握的是官方单元 10，而不是背下一组技巧？

先预测：若只报告修改后FPS，没有设备、构建、场景、输入、热状态和画面合同，另一位开发者能否证明收益来自这次修改？不能。跨平台优化首先是受控实验，其次才是技巧选择。

Shader成本由变体、指令、纹理访问、精度、像素覆盖与混合共同决定。Renderer.material还会实例化材质；移动GPU计数器和过绘视图用于把代码变化连接到硬件结果。

## 官方目录边界

本页严格对应Unity Technologies《Optimize your game performance for mobile, XR, and the web in Unity》Unity 6版第62—66页。以下目录项必须进入实现或证据：

- **Keep shaders simple and optimized**
- **Minimize overdraw and alpha blending**
- **Limit post-processing effects**
- **Be careful with Renderer.material**
- **Optimize SkinnedMeshRenderers**
- **Minimize reflection probes**
- **System Metrics Mali**

本站重新组织中文讲解、代码和实验，不逐字复制原文。平台API和包能力随Unity与设备版本变化，实际项目要固定版本并保存迁移差异。

## 核心模型与不可变条件

Shader成本由变体、指令、纹理访问、精度、像素覆盖与混合共同决定。Renderer.material还会实例化材质；移动GPU计数器和过绘视图用于把代码变化连接到硬件结果。

不可变条件是：**Shader优化保持材质语义与视觉合同，变体、Pass、过绘、带宽和GPU毫秒均可追溯，材质实例数量不意外增长。** 如果测试上下文、视觉或玩法结果发生变化，就不能把两次数字直接相减。先解释变化，再决定是否建立新基线。

## 五个必须落到证据的术语


它必须落到“Shader变体 → 指令纹理 → 像素覆盖 → 材质实例 → GPU计数器 → 视觉回归”的证据链，不能只作为术语记忆。


它必须落到“Shader变体 → 指令纹理 → 像素覆盖 → 材质实例 → GPU计数器 → 视觉回归”的证据链，不能只作为术语记忆。


它必须落到“Shader变体 → 指令纹理 → 像素覆盖 → 材质实例 → GPU计数器 → 视觉回归”的证据链，不能只作为术语记忆。


它必须落到“Shader变体 → 指令纹理 → 像素覆盖 → 材质实例 → GPU计数器 → 视觉回归”的证据链，不能只作为术语记忆。


它必须落到“Shader变体 → 指令纹理 → 像素覆盖 → 材质实例 → GPU计数器 → 视觉回归”的证据链，不能只作为术语记忆。

## 从输入到发布的证据链

主链为：**Shader变体 → 指令纹理 → 像素覆盖 → 材质实例 → GPU计数器 → 视觉回归**。每个节点保存平台、设备、浏览器或运行时、Unity与包版本、构建提交、场景检查点、预算、实际值和首个异常。后一节点只消费前一节点已验证事实。

### 1. 简化Shader

移除未用功能、控制关键字和Pass，按目标GPU检查指令、寄存器、纹理采样与精度。

复现这一项时，记录输入条件、观测工具、预算阈值和下一动作。输入决定数据是否可比，工具说明在哪个线程、对象、资源或平台阶段观察；阈值来自本项目合同，下一动作必须是一轮可反证的单变量实验。

### 2. 过绘

透明粒子、UI和全屏效果会重复像素工作；用过绘视图、分辨率实验和GPU捕获确认。

复现这一项时，记录输入条件、观测工具、预算阈值和下一动作。输入决定数据是否可比，工具说明在哪个线程、对象、资源或平台阶段观察；阈值来自本项目合同，下一动作必须是一轮可反证的单变量实验。

### 3. 后处理

移动与XR双眼或高分辨率放大后处理成本，限制效果数量和中间目标格式。

复现这一项时，记录输入条件、观测工具、预算阈值和下一动作。输入决定数据是否可比，工具说明在哪个线程、对象、资源或平台阶段观察；阈值来自本项目合同，下一动作必须是一轮可反证的单变量实验。

### 4. 材质访问

sharedMaterial读取共享资产，material可能实例化；动态属性优先MaterialPropertyBlock并验证Batcher。

复现这一项时，记录输入条件、观测工具、预算阈值和下一动作。输入决定数据是否可比，工具说明在哪个线程、对象、资源或平台阶段观察；阈值来自本项目合同，下一动作必须是一轮可反证的单变量实验。

### 5. SkinnedMesh

减少骨骼、顶点影响与不可见更新，按动画和边界正确性选择Update When Offscreen。

复现这一项时，记录输入条件、观测工具、预算阈值和下一动作。输入决定数据是否可比，工具说明在哪个线程、对象、资源或平台阶段观察；阈值来自本项目合同，下一动作必须是一轮可反证的单变量实验。

### 6. 反射探针

控制数量、分辨率、刷新与影响范围，实时更新必须有明确玩法理由。

复现这一项时，记录输入条件、观测工具、预算阈值和下一动作。输入决定数据是否可比，工具说明在哪个线程、对象、资源或平台阶段观察；阈值来自本项目合同，下一动作必须是一轮可反证的单变量实验。

### 7. 硬件证据

Mali或其他厂商指标解释带宽、算术和片元压力，计数器含义不得跨GPU直接外推。

复现这一项时，记录输入条件、观测工具、预算阈值和下一动作。输入决定数据是否可比，工具说明在哪个线程、对象、资源或平台阶段观察；阈值来自本项目合同，下一动作必须是一轮可反证的单变量实验。

## 三步复现与分步观察


## 可执行实现骨架

```csharp
static readonly int TintId = Shader.PropertyToID("_BaseColor");
readonly MaterialPropertyBlock block = new();

void SetTint(Renderer target, Color color)
{
    target.GetPropertyBlock(block);
    block.SetColor(TintId, color);
    target.SetPropertyBlock(block);
}
```

每份采集都保存结构化上下文，报告与原始文件必须通过ID互相引用：

```json
{
  "chapter": "mxrw-10-shaders",
  "officialPages": "第62—66页",
  "chain": [
    "Shader变体",
    "指令纹理",
    "像素覆盖",
    "材质实例",
    "GPU计数器",
    "视觉回归"
  ],
  "platform": "mobile-web-or-xr",
  "device": "representative-target",
  "build": "commit-unity-packages",
  "scenario": "fixed-checkpoint",
  "normal": true,
  "boundary": true,
  "failureReplay": true
}
```

自动门必须保留数值、结果合同和拒绝原因：

```text
PASS context=platform+device+build+scene+quality+input
PASS chain=Shader变体 -> 指令纹理 -> 像素覆盖 -> 材质实例 -> GPU计数器 -> 视觉回归
PASS invariant=Shader优化保持材质语义与视觉合同，变体、Pass、过绘、带宽和GPU毫秒均可追溯，材质实例数量不意外增长。
REJECT changed-context | hidden-quality-loss | shifted-cost | missing-failure-replay
```

## 两个必须主动制造的失败样本


## 证据矩阵与签发标准

| 维度       | 正常样本         | 边界样本           | 失败样本         | 签发条件       |
| ---------- | ---------------- | ------------------ | ---------------- | -------------- |
| 平台上下文 | 代表设备固定构建 | 最低规格或长时运行 | 故意改变一项条件 | 差异可解释     |
| 主证据链   | 六节点均记录     | 接近预算输入       | 首个节点注入偏差 | 能定位首因     |
| 性能结果   | 达到预算         | 无长尾越界         | 主动制造回归     | 同输入可复验   |
| 正确性     | 视觉玩法一致     | 生命周期完整       | 降质或转移成本   | 所有门同时通过 |

## 练习：把官方单元 10变成可审计交付


## 小结

- 是否逐项覆盖本页官方目录，而没有用通用建议替代？
- 是否沿“Shader变体 → 指令纹理 → 像素覆盖 → 材质实例 → GPU计数器 → 视觉回归”保存可追溯中间证据？
- 是否在真实移动、浏览器或XR目标中验证，而非只看Editor？
- 是否主动制造失败，并证明优化没有转移成本或破坏结果？

## 术语表


## 来源与改编边界

