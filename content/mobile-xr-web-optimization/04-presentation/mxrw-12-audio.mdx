---
title: "音频：声道、压缩、加载类型与采样率"
type: C
section: "UI、音频与动画"
order: 2
description: "按 Unity 6 官方指南第 73—75 页复现声道、压缩、加载类型与采样率。"
demo: true
math: false
draft: false
sourceUrl: "https://unity.com/resources/mobile-xr-web-game-performance-optimization-unity-6"
---


## 问题：怎样证明掌握的是官方单元 12，而不是背下一组技巧？

先预测：若只报告修改后FPS，没有设备、构建、场景、输入、热状态和画面合同，另一位开发者能否证明收益来自这次修改？不能。跨平台优化首先是受控实验，其次才是技巧选择。

音频成本由声道、源文件、平台压缩、Load Type、同时播放和采样率共同决定。短音效、长音乐和空间音频需要不同导入策略，静音不等于卸载。

## 官方目录边界

本页严格对应Unity Technologies《Optimize your game performance for mobile, XR, and the web in Unity》Unity 6版第73—75页。以下目录项必须进入实现或证据：

- **Make sound clips mono when possible**
- **Use original uncompressed WAV files as your source assets**
- **Compress the clip and reduce the compression bitrate**
- **Choose the proper Load Type**
- **Unload muted AudioSources from memory**
- **Use the Sample Rate Setting**

本站重新组织中文讲解、代码和实验，不逐字复制原文。平台API和包能力随Unity与设备版本变化，实际项目要固定版本并保存迁移差异。

## 核心模型与不可变条件

音频成本由声道、源文件、平台压缩、Load Type、同时播放和采样率共同决定。短音效、长音乐和空间音频需要不同导入策略，静音不等于卸载。

不可变条件是：**每类音频都有声道、格式、Load Type、采样率、内存与并发预算，听感和播放时延在目标设备通过。** 如果测试上下文、视觉或玩法结果发生变化，就不能把两次数字直接相减。先解释变化，再决定是否建立新基线。

## 五个必须落到证据的术语


它必须落到“WAV源文件 → 平台导入 → Load Type → 运行播放 → 内存CPU → 听感签发”的证据链，不能只作为术语记忆。


它必须落到“WAV源文件 → 平台导入 → Load Type → 运行播放 → 内存CPU → 听感签发”的证据链，不能只作为术语记忆。


它必须落到“WAV源文件 → 平台导入 → Load Type → 运行播放 → 内存CPU → 听感签发”的证据链，不能只作为术语记忆。


它必须落到“WAV源文件 → 平台导入 → Load Type → 运行播放 → 内存CPU → 听感签发”的证据链，不能只作为术语记忆。


它必须落到“WAV源文件 → 平台导入 → Load Type → 运行播放 → 内存CPU → 听感签发”的证据链，不能只作为术语记忆。

## 从输入到发布的证据链

主链为：**WAV源文件 → 平台导入 → Load Type → 运行播放 → 内存CPU → 听感签发**。每个节点保存平台、设备、浏览器或运行时、Unity与包版本、构建提交、场景检查点、预算、实际值和首个异常。后一节点只消费前一节点已验证事实。

### 1. 声道

非空间或无需立体信息的音效可转Mono，减少磁盘和内存；音乐与环境声先由音频设计确认。

复现这一项时，记录输入条件、观测工具、预算阈值和下一动作。输入决定数据是否可比，工具说明在哪个线程、对象、资源或平台阶段观察；阈值来自本项目合同，下一动作必须是一轮可反证的单变量实验。

### 2. 源文件

保留原始无压缩WAV作为主资产，让Unity按平台一次压缩，避免二次有损编码。

复现这一项时，记录输入条件、观测工具、预算阈值和下一动作。输入决定数据是否可比，工具说明在哪个线程、对象、资源或平台阶段观察；阈值来自本项目合同，下一动作必须是一轮可反证的单变量实验。

### 3. 压缩率

降低比特率能减包体和内存，却增加伪影；在手机扬声器、耳机和XR设备分别试听。

复现这一项时，记录输入条件、观测工具、预算阈值和下一动作。输入决定数据是否可比，工具说明在哪个线程、对象、资源或平台阶段观察；阈值来自本项目合同，下一动作必须是一轮可反证的单变量实验。

### 4. 加载类型

短且频繁音效适合解压，较大音频可压缩驻留，长音乐通常流式；用Profiler验证CPU和内存。

复现这一项时，记录输入条件、观测工具、预算阈值和下一动作。输入决定数据是否可比，工具说明在哪个线程、对象、资源或平台阶段观察；阈值来自本项目合同，下一动作必须是一轮可反证的单变量实验。

### 5. 静音与卸载

AudioSource音量为零仍可能持有Clip，场景或功能结束时按所有权释放不再需要资源。

复现这一项时，记录输入条件、观测工具、预算阈值和下一动作。输入决定数据是否可比，工具说明在哪个线程、对象、资源或平台阶段观察；阈值来自本项目合同，下一动作必须是一轮可反证的单变量实验。

### 6. 采样率

按频谱内容和输出设备选择Preserve、Optimize或Override，过高采样率只增加数据。

复现这一项时，记录输入条件、观测工具、预算阈值和下一动作。输入决定数据是否可比，工具说明在哪个线程、对象、资源或平台阶段观察；阈值来自本项目合同，下一动作必须是一轮可反证的单变量实验。

### 7. 并发

限制同时Voice数量和优先级，峰值战斗场景测解码、混音、内存和可听结果。

复现这一项时，记录输入条件、观测工具、预算阈值和下一动作。输入决定数据是否可比，工具说明在哪个线程、对象、资源或平台阶段观察；阈值来自本项目合同，下一动作必须是一轮可反证的单变量实验。

## 三步复现与分步观察


## 可执行实现骨架

```csharp
public void ReleaseMutedSource(AudioSource source)
{
    source.Stop();
    source.clip = null;
    source.gameObject.SetActive(false);
}

// Release the Addressables handle through the owning audio service.
```

每份采集都保存结构化上下文，报告与原始文件必须通过ID互相引用：

```json
{
  "chapter": "mxrw-12-audio",
  "officialPages": "第73—75页",
  "chain": [
    "WAV源文件",
    "平台导入",
    "Load Type",
    "运行播放",
    "内存CPU",
    "听感签发"
  ],
  "platform": "mobile-web-or-xr",
  "device": "representative-target",
  "build": "commit-unity-packages",
  "scenario": "fixed-checkpoint",
  "normal": true,
  "boundary": true,
  "failureReplay": true
}
```

自动门必须保留数值、结果合同和拒绝原因：

```text
PASS context=platform+device+build+scene+quality+input
PASS chain=WAV源文件 -> 平台导入 -> Load Type -> 运行播放 -> 内存CPU -> 听感签发
PASS invariant=每类音频都有声道、格式、Load Type、采样率、内存与并发预算，听感和播放时延在目标设备通过。
REJECT changed-context | hidden-quality-loss | shifted-cost | missing-failure-replay
```

## 两个必须主动制造的失败样本


## 证据矩阵与签发标准

| 维度       | 正常样本         | 边界样本           | 失败样本         | 签发条件       |
| ---------- | ---------------- | ------------------ | ---------------- | -------------- |
| 平台上下文 | 代表设备固定构建 | 最低规格或长时运行 | 故意改变一项条件 | 差异可解释     |
| 主证据链   | 六节点均记录     | 接近预算输入       | 首个节点注入偏差 | 能定位首因     |
| 性能结果   | 达到预算         | 无长尾越界         | 主动制造回归     | 同输入可复验   |
| 正确性     | 视觉玩法一致     | 生命周期完整       | 降质或转移成本   | 所有门同时通过 |

## 练习：把官方单元 12变成可审计交付


## 小结

- 是否逐项覆盖本页官方目录，而没有用通用建议替代？
- 是否沿“WAV源文件 → 平台导入 → Load Type → 运行播放 → 内存CPU → 听感签发”保存可追溯中间证据？
- 是否在真实移动、浏览器或XR目标中验证，而非只看Editor？
- 是否主动制造失败，并证明优化没有转移成本或破坏结果？

## 术语表


## 来源与改编边界

