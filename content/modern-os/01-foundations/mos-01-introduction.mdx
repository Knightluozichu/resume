---
title: "第 1 章 引论"
type: A
section: "现代操作系统 · 基础与抽象"
order: 1
description: "从扩展机器和资源管理者两种视角定义操作系统，贯通硬件、系统概念、系统调用、单体/分层/微内核/虚拟机结构与 C 运行模型。覆盖57个正式目录坐标。"
demo: true
math: false
sourceUrl: "https://www.cmpedu.com/books/book/5610493.htm"
draft: false
---

import {
  Mos01IntroductionMapLab,
  Mos01IntroductionExperimentLab,
  Mos01IntroductionEvidenceLab,
} from "@/components/mdx/modern-os/diagrams/mos-01-introduction";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第 1 章 引论的核心机制，并说出它在中文原书第4版13章体系中的位置
- 能描述识别硬件资源、建立系统抽象、发起系统调用、进入内核、执行策略机制、返回受控结果六个阶段的输入、内核状态、资源所有者与不变量
- 能实现最小可重放实验，注入边界或失败条件并定位首个偏离点
- 能比较正常、边界、失败和恢复轨迹，判断目录概念是否形成可迁移能力

</Objectives>

## 直觉起点

<Term def="扩展机器是第 1 章 引论的第一个观察坐标，连接请求、内核状态与恢复证据。">
  扩展机器
</Term>
、
<Term def="资源管理者是第 1 章 引论的第二个观察坐标，用于解释资源与责任边界。">
  资源管理者
</Term>
、
<Term def="系统调用是第 1 章 引论的第三个观察坐标，需要用最小实验验收。">
  系统调用
</Term>
、
<Term def="微内核是第 1 章 引论的第四个观察坐标，边界条件会暴露约束。">
  微内核
</Term>
、
<Term def="运行模型是第 1 章 引论的第五个观察坐标，必须定位首个状态分叉。">
  运行模型
</Term>
共同构成本页坐标。从扩展机器和资源管理者两种视角定义操作系统，贯通硬件、系统概念、系统调用、单体/分层/微内核/虚拟机结构与
C 运行模型。
学习时先预测内核下一状态，再运行样本；最终输出正确不能证明调度、地址、设备、持久化与清理都正确。

<Mos01IntroductionMapLab />

## 六阶段资源链

### 1. 识别硬件资源

从扩展机器和资源管理者两种视角定义操作系统，贯通硬件、系统概念、系统调用、单体/分层/微内核/虚拟机结构与 C 运行模型。 对“第1章 引论”先在“识别硬件资源”写出输入与预测，再推进到“建立系统抽象”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 2. 建立系统抽象

从扩展机器和资源管理者两种视角定义操作系统，贯通硬件、系统概念、系统调用、单体/分层/微内核/虚拟机结构与 C 运行模型。 对“1.1 什么是操作系统”先在“建立系统抽象”写出输入与预测，再推进到“发起系统调用”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 3. 发起系统调用

从扩展机器和资源管理者两种视角定义操作系统，贯通硬件、系统概念、系统调用、单体/分层/微内核/虚拟机结构与 C 运行模型。 对“1.1.1 作为扩展机器的操作系统”先在“发起系统调用”写出输入与预测，再推进到“进入内核”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 4. 进入内核

用资源守恒和等待关系建模，区分预防、避免、检测恢复与忽略；没有进度还需继续区分死锁、活锁和饥饿。 对“1.1.2 作为资源管理者的操作系统”先在“进入内核”写出输入与预测，再推进到“执行策略机制”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 5. 执行策略机制

从扩展机器和资源管理者两种视角定义操作系统，贯通硬件、系统概念、系统调用、单体/分层/微内核/虚拟机结构与 C 运行模型。 对“1.2 操作系统的历史”先在“执行策略机制”写出输入与预测，再推进到“返回受控结果”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 6. 返回受控结果

从扩展机器和资源管理者两种视角定义操作系统，贯通硬件、系统概念、系统调用、单体/分层/微内核/虚拟机结构与 C 运行模型。 对“1.2.1 第一代（1945～1955）：真空管和穿孔卡片”先在“返回受控结果”写出输入与预测，再推进到“识别硬件资源”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

## 核心机制深挖

从扩展机器和资源管理者两种视角定义操作系统，贯通硬件、系统概念、系统调用、单体/分层/微内核/虚拟机结构与 C 运行模型。 本页把每个目录概念放回“识别硬件资源 → 建立系统抽象 → 发起系统调用 → 进入内核 → 执行策略机制 → 返回受控结果”的资源状态机。输入可能是系统调用、地址、字节流、中断、资源请求或远程消息；状态可能位于进程表、页表、驱动队列、文件元数据、虚拟机或保护域；输出必须同时说明返回值、资源所有权与下一阶段可依赖的承诺。

本页签发的不变量是：**用户程序只能经受控接口请求特权资源；抽象隐藏设备细节但保留权限、错误和生命周期边界，机制与策略能够分别说明。**。不变量在正常路径与错误路径都应成立。一次成功运行只证明一个样本可用，不能证明边界安全；因此目录中的机制都要进入正常、边界、失败和恢复四条轨迹，并明确谁检测、谁阻塞或重试、谁逆序清理、谁向上报告。

跟踪一个进程从 shell 发起系统调用，经陷入、参数检查、内核对象操作再返回用户态，比较单体与微内核消息路径。 运行前把预期写成状态快照，运行后逐字段比较。并发实验记录同步边，地址实验记录位宽与权限，I/O 实验记录缓冲区和完成事件，持久化实验区分提交与落盘，分布式实验记录丢失、重复和重排。

注入非法系统调用号、坏指针、权限不足和服务进程失联，定位错误在接口、保护边界还是结构通信层。 删除故障后用完全相同的输入重放，确认返回值、内核对象、外部结果和清理状态一起恢复。只修正界面结果却留下线程、页、设备请求、文件引用、虚拟机状态或权限污染，仍然不通过。

原书第4版使用 Linux、Android、Windows 8.1、VMware 和当时的多处理机技术作为实例。现代内核和云平台会改变接口版本与性能数值，但进程状态、地址翻译、I/O 分层、资源死锁、虚拟化隔离和保护域仍可验证。现代对照必须写明差异，不能覆盖2017年中文版本的目录身份。

## 正式目录逐项讲解

### 第1章 引论

从扩展机器和资源管理者两种视角定义操作系统，贯通硬件、系统概念、系统调用、单体/分层/微内核/虚拟机结构与 C 运行模型。 对“第1章 引论”先在“识别硬件资源”写出输入与预测，再推进到“建立系统抽象”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 1.1 什么是操作系统

从扩展机器和资源管理者两种视角定义操作系统，贯通硬件、系统概念、系统调用、单体/分层/微内核/虚拟机结构与 C 运行模型。 对“1.1 什么是操作系统”先在“建立系统抽象”写出输入与预测，再推进到“发起系统调用”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 1.1.1 作为扩展机器的操作系统

从扩展机器和资源管理者两种视角定义操作系统，贯通硬件、系统概念、系统调用、单体/分层/微内核/虚拟机结构与 C 运行模型。 对“1.1.1 作为扩展机器的操作系统”先在“发起系统调用”写出输入与预测，再推进到“进入内核”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 1.1.2 作为资源管理者的操作系统

用资源守恒和等待关系建模，区分预防、避免、检测恢复与忽略；没有进度还需继续区分死锁、活锁和饥饿。 对“1.1.2 作为资源管理者的操作系统”先在“进入内核”写出输入与预测，再推进到“执行策略机制”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 1.2 操作系统的历史

从扩展机器和资源管理者两种视角定义操作系统，贯通硬件、系统概念、系统调用、单体/分层/微内核/虚拟机结构与 C 运行模型。 对“1.2 操作系统的历史”先在“执行策略机制”写出输入与预测，再推进到“返回受控结果”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 1.2.1 第一代（1945～1955）：真空管和穿孔卡片

从扩展机器和资源管理者两种视角定义操作系统，贯通硬件、系统概念、系统调用、单体/分层/微内核/虚拟机结构与 C 运行模型。 对“1.2.1 第一代（1945～1955）：真空管和穿孔卡片”先在“返回受控结果”写出输入与预测，再推进到“识别硬件资源”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 1.2.2 第二代（1955～1965）：晶体管和批处理系统

从扩展机器和资源管理者两种视角定义操作系统，贯通硬件、系统概念、系统调用、单体/分层/微内核/虚拟机结构与 C 运行模型。 对“1.2.2 第二代（1955～1965）：晶体管和批处理系统”先在“识别硬件资源”写出输入与预测，再推进到“建立系统抽象”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 1.2.3 第三代（1965～1980）：集成电路和多道程序设计

把目标、接口、结构、权衡和证据分开：设计结论需要可诊断接口、可替换策略、稳定测量和可追溯研究来源。 对“1.2.3 第三代（1965～1980）：集成电路和多道程序设计”先在“建立系统抽象”写出输入与预测，再推进到“发起系统调用”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 1.2.4 第四代（1980年至今）：个人计算机

从扩展机器和资源管理者两种视角定义操作系统，贯通硬件、系统概念、系统调用、单体/分层/微内核/虚拟机结构与 C 运行模型。 对“1.2.4 第四代（1980年至今）：个人计算机”先在“发起系统调用”写出输入与预测，再推进到“进入内核”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 1.2.5 第五代（1990年至今）：移动计算机

从扩展机器和资源管理者两种视角定义操作系统，贯通硬件、系统概念、系统调用、单体/分层/微内核/虚拟机结构与 C 运行模型。 对“1.2.5 第五代（1990年至今）：移动计算机”先在“进入内核”写出输入与预测，再推进到“执行策略机制”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 1.3 计算机硬件简介

从扩展机器和资源管理者两种视角定义操作系统，贯通硬件、系统概念、系统调用、单体/分层/微内核/虚拟机结构与 C 运行模型。 对“1.3 计算机硬件简介”先在“执行策略机制”写出输入与预测，再推进到“返回受控结果”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 1.3.1 处理器

从扩展机器和资源管理者两种视角定义操作系统，贯通硬件、系统概念、系统调用、单体/分层/微内核/虚拟机结构与 C 运行模型。 对“1.3.1 处理器”先在“返回受控结果”写出输入与预测，再推进到“识别硬件资源”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 1.3.2 存储器

从扩展机器和资源管理者两种视角定义操作系统，贯通硬件、系统概念、系统调用、单体/分层/微内核/虚拟机结构与 C 运行模型。 对“1.3.2 存储器”先在“识别硬件资源”写出输入与预测，再推进到“建立系统抽象”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 1.3.3 磁盘

沿请求、控制器、数据搬运、完成通知和持久化逐层追踪；缓冲区生命周期、元数据一致性和错误传播不能省略。 对“1.3.3 磁盘”先在“建立系统抽象”写出输入与预测，再推进到“发起系统调用”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 1.3.4 I/O设备

沿请求、控制器、数据搬运、完成通知和持久化逐层追踪；缓冲区生命周期、元数据一致性和错误传播不能省略。 对“1.3.4 I/O设备”先在“发起系统调用”写出输入与预测，再推进到“进入内核”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 1.3.5 总线

从扩展机器和资源管理者两种视角定义操作系统，贯通硬件、系统概念、系统调用、单体/分层/微内核/虚拟机结构与 C 运行模型。 对“1.3.5 总线”先在“进入内核”写出输入与预测，再推进到“执行策略机制”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 1.3.6 启动计算机

从扩展机器和资源管理者两种视角定义操作系统，贯通硬件、系统概念、系统调用、单体/分层/微内核/虚拟机结构与 C 运行模型。 对“1.3.6 启动计算机”先在“执行策略机制”写出输入与预测，再推进到“返回受控结果”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 1.4 操作系统大观园

从扩展机器和资源管理者两种视角定义操作系统，贯通硬件、系统概念、系统调用、单体/分层/微内核/虚拟机结构与 C 运行模型。 对“1.4 操作系统大观园”先在“返回受控结果”写出输入与预测，再推进到“识别硬件资源”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 1.4.1 大型机操作系统

从扩展机器和资源管理者两种视角定义操作系统，贯通硬件、系统概念、系统调用、单体/分层/微内核/虚拟机结构与 C 运行模型。 对“1.4.1 大型机操作系统”先在“识别硬件资源”写出输入与预测，再推进到“建立系统抽象”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 1.4.2 服务器操作系统

从扩展机器和资源管理者两种视角定义操作系统，贯通硬件、系统概念、系统调用、单体/分层/微内核/虚拟机结构与 C 运行模型。 对“1.4.2 服务器操作系统”先在“建立系统抽象”写出输入与预测，再推进到“发起系统调用”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 1.4.3 多处理器操作系统

先声明共享或消息模型，再定义一致性、失败和重试语义；缓存同步、远程调用与负载迁移都可能改变性能和所有权。 对“1.4.3 多处理器操作系统”先在“发起系统调用”写出输入与预测，再推进到“进入内核”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 1.4.4 个人计算机操作系统

从扩展机器和资源管理者两种视角定义操作系统，贯通硬件、系统概念、系统调用、单体/分层/微内核/虚拟机结构与 C 运行模型。 对“1.4.4 个人计算机操作系统”先在“进入内核”写出输入与预测，再推进到“执行策略机制”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 1.4.5 掌上计算机操作系统

从扩展机器和资源管理者两种视角定义操作系统，贯通硬件、系统概念、系统调用、单体/分层/微内核/虚拟机结构与 C 运行模型。 对“1.4.5 掌上计算机操作系统”先在“执行策略机制”写出输入与预测，再推进到“返回受控结果”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 1.4.6 嵌入式操作系统

从扩展机器和资源管理者两种视角定义操作系统，贯通硬件、系统概念、系统调用、单体/分层/微内核/虚拟机结构与 C 运行模型。 对“1.4.6 嵌入式操作系统”先在“返回受控结果”写出输入与预测，再推进到“识别硬件资源”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 1.4.7 传感器节点操作系统

从扩展机器和资源管理者两种视角定义操作系统，贯通硬件、系统概念、系统调用、单体/分层/微内核/虚拟机结构与 C 运行模型。 对“1.4.7 传感器节点操作系统”先在“识别硬件资源”写出输入与预测，再推进到“建立系统抽象”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 1.4.8 实时操作系统

从扩展机器和资源管理者两种视角定义操作系统，贯通硬件、系统概念、系统调用、单体/分层/微内核/虚拟机结构与 C 运行模型。 对“1.4.8 实时操作系统”先在“建立系统抽象”写出输入与预测，再推进到“发起系统调用”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 1.4.9 智能卡操作系统

从扩展机器和资源管理者两种视角定义操作系统，贯通硬件、系统概念、系统调用、单体/分层/微内核/虚拟机结构与 C 运行模型。 对“1.4.9 智能卡操作系统”先在“发起系统调用”写出输入与预测，再推进到“进入内核”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 1.5 操作系统概念

从扩展机器和资源管理者两种视角定义操作系统，贯通硬件、系统概念、系统调用、单体/分层/微内核/虚拟机结构与 C 运行模型。 对“1.5 操作系统概念”先在“进入内核”写出输入与预测，再推进到“执行策略机制”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 1.5.1 进程

先标出逻辑流、内核保存状态与共享变量，再用同步边和调度事件解释每次交错；安全、活性、公平和回收分别验收。 对“1.5.1 进程”先在“执行策略机制”写出输入与预测，再推进到“返回受控结果”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 1.5.2 地址空间

把虚拟地址拆为可核查字段，依次检查映射、权限、驻留和置换；地址抽象必须同时说明隔离、共享与缺页恢复。 对“1.5.2 地址空间”先在“返回受控结果”写出输入与预测，再推进到“识别硬件资源”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 1.5.3 文件

沿请求、控制器、数据搬运、完成通知和持久化逐层追踪；缓冲区生命周期、元数据一致性和错误传播不能省略。 对“1.5.3 文件”先在“识别硬件资源”写出输入与预测，再推进到“建立系统抽象”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 1.5.4 输入/输出

从扩展机器和资源管理者两种视角定义操作系统，贯通硬件、系统概念、系统调用、单体/分层/微内核/虚拟机结构与 C 运行模型。 对“1.5.4 输入/输出”先在“建立系统抽象”写出输入与预测，再推进到“发起系统调用”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 1.5.5 保护

从威胁主体、信任边界和保护域开始，分别验证认证、授权、完整性与审计；单一防线不能代替最小权限和纵深防御。 对“1.5.5 保护”先在“发起系统调用”写出输入与预测，再推进到“进入内核”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 1.5.6 shell

从扩展机器和资源管理者两种视角定义操作系统，贯通硬件、系统概念、系统调用、单体/分层/微内核/虚拟机结构与 C 运行模型。 对“1.5.6 shell”先在“进入内核”写出输入与预测，再推进到“执行策略机制”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 1.5.7 个体重复系统发育

从扩展机器和资源管理者两种视角定义操作系统，贯通硬件、系统概念、系统调用、单体/分层/微内核/虚拟机结构与 C 运行模型。 对“1.5.7 个体重复系统发育”先在“执行策略机制”写出输入与预测，再推进到“返回受控结果”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 1.6 系统调用

从扩展机器和资源管理者两种视角定义操作系统，贯通硬件、系统概念、系统调用、单体/分层/微内核/虚拟机结构与 C 运行模型。 对“1.6 系统调用”先在“返回受控结果”写出输入与预测，再推进到“识别硬件资源”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 1.6.1 用于进程管理的系统调用

先标出逻辑流、内核保存状态与共享变量，再用同步边和调度事件解释每次交错；安全、活性、公平和回收分别验收。 对“1.6.1 用于进程管理的系统调用”先在“识别硬件资源”写出输入与预测，再推进到“建立系统抽象”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 1.6.2 用于文件管理的系统调用

沿请求、控制器、数据搬运、完成通知和持久化逐层追踪；缓冲区生命周期、元数据一致性和错误传播不能省略。 对“1.6.2 用于文件管理的系统调用”先在“建立系统抽象”写出输入与预测，再推进到“发起系统调用”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 1.6.3 用于目录管理的系统调用

沿请求、控制器、数据搬运、完成通知和持久化逐层追踪；缓冲区生命周期、元数据一致性和错误传播不能省略。 对“1.6.3 用于目录管理的系统调用”先在“发起系统调用”写出输入与预测，再推进到“进入内核”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 1.6.4 各种系统调用

从扩展机器和资源管理者两种视角定义操作系统，贯通硬件、系统概念、系统调用、单体/分层/微内核/虚拟机结构与 C 运行模型。 对“1.6.4 各种系统调用”先在“进入内核”写出输入与预测，再推进到“执行策略机制”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 1.6.5 Windows Win32 API

把产品中的系统调用、内核对象与权限路径回连通用机制，同时标明平台扩展与版本语境，避免用名称代替因果解释。 对“1.6.5 Windows Win32 API”先在“执行策略机制”写出输入与预测，再推进到“返回受控结果”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 1.7 操作系统结构

从扩展机器和资源管理者两种视角定义操作系统，贯通硬件、系统概念、系统调用、单体/分层/微内核/虚拟机结构与 C 运行模型。 对“1.7 操作系统结构”先在“返回受控结果”写出输入与预测，再推进到“识别硬件资源”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 1.7.1 单体系统

从扩展机器和资源管理者两种视角定义操作系统，贯通硬件、系统概念、系统调用、单体/分层/微内核/虚拟机结构与 C 运行模型。 对“1.7.1 单体系统”先在“识别硬件资源”写出输入与预测，再推进到“建立系统抽象”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 1.7.2 层次式系统

从扩展机器和资源管理者两种视角定义操作系统，贯通硬件、系统概念、系统调用、单体/分层/微内核/虚拟机结构与 C 运行模型。 对“1.7.2 层次式系统”先在“建立系统抽象”写出输入与预测，再推进到“发起系统调用”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 1.7.3 微内核

从扩展机器和资源管理者两种视角定义操作系统，贯通硬件、系统概念、系统调用、单体/分层/微内核/虚拟机结构与 C 运行模型。 对“1.7.3 微内核”先在“发起系统调用”写出输入与预测，再推进到“进入内核”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 1.7.4 客户端–服务器模式

从扩展机器和资源管理者两种视角定义操作系统，贯通硬件、系统概念、系统调用、单体/分层/微内核/虚拟机结构与 C 运行模型。 对“1.7.4 客户端–服务器模式”先在“进入内核”写出输入与预测，再推进到“执行策略机制”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 1.7.5 虚拟机

管理程序必须截获敏感操作并保持等价、隔离和资源记账；处理器、内存、设备和时间状态共同决定检查点能否恢复。 对“1.7.5 虚拟机”先在“执行策略机制”写出输入与预测，再推进到“返回受控结果”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 1.7.6 外核

从扩展机器和资源管理者两种视角定义操作系统，贯通硬件、系统概念、系统调用、单体/分层/微内核/虚拟机结构与 C 运行模型。 对“1.7.6 外核”先在“返回受控结果”写出输入与预测，再推进到“识别硬件资源”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 1.8 依靠C的世界

从扩展机器和资源管理者两种视角定义操作系统，贯通硬件、系统概念、系统调用、单体/分层/微内核/虚拟机结构与 C 运行模型。 对“1.8 依靠C的世界”先在“识别硬件资源”写出输入与预测，再推进到“建立系统抽象”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 1.8.1 C语言

从扩展机器和资源管理者两种视角定义操作系统，贯通硬件、系统概念、系统调用、单体/分层/微内核/虚拟机结构与 C 运行模型。 对“1.8.1 C语言”先在“建立系统抽象”写出输入与预测，再推进到“发起系统调用”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 1.8.2 头文件

沿请求、控制器、数据搬运、完成通知和持久化逐层追踪；缓冲区生命周期、元数据一致性和错误传播不能省略。 对“1.8.2 头文件”先在“发起系统调用”写出输入与预测，再推进到“进入内核”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 1.8.3 大型编程项目

把目标、接口、结构、权衡和证据分开：设计结论需要可诊断接口、可替换策略、稳定测量和可追溯研究来源。 对“1.8.3 大型编程项目”先在“进入内核”写出输入与预测，再推进到“执行策略机制”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 1.8.4 运行模型

从扩展机器和资源管理者两种视角定义操作系统，贯通硬件、系统概念、系统调用、单体/分层/微内核/虚拟机结构与 C 运行模型。 对“1.8.4 运行模型”先在“执行策略机制”写出输入与预测，再推进到“返回受控结果”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 1.9 有关操作系统的研究

从扩展机器和资源管理者两种视角定义操作系统，贯通硬件、系统概念、系统调用、单体/分层/微内核/虚拟机结构与 C 运行模型。 对“1.9 有关操作系统的研究”先在“返回受控结果”写出输入与预测，再推进到“识别硬件资源”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 1.10 本书其他部分概要

从扩展机器和资源管理者两种视角定义操作系统，贯通硬件、系统概念、系统调用、单体/分层/微内核/虚拟机结构与 C 运行模型。 对“1.10 本书其他部分概要”先在“识别硬件资源”写出输入与预测，再推进到“建立系统抽象”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 1.11 公制单位

从扩展机器和资源管理者两种视角定义操作系统，贯通硬件、系统概念、系统调用、单体/分层/微内核/虚拟机结构与 C 运行模型。 对“1.11 公制单位”先在“建立系统抽象”写出输入与预测，再推进到“发起系统调用”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 1.12 小结

本节用“用户程序只能经受控接口请求特权资源；抽象隐藏设备细节但保留权限、错误和生命周期边界，机制与策略能够分别说明。”回查正常、边界、失败和恢复四条轨迹，而不是只重复术语。 对“1.12 小结”先在“发起系统调用”写出输入与预测，再推进到“进入内核”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

## 状态与失败矩阵

| 对象         | 正常状态               | 边界样本             | 失败证据           | 恢复条件           |
| ------------ | ---------------------- | -------------------- | ------------------ | ------------------ |
| 进程与同步   | 状态转换和同步边明确   | 抢占、竞争、低容量   | 调度与等待图       | 唤醒并回收全部流   |
| 地址与存储   | 映射、权限、元数据有效 | 缺页、空间耗尽、断电 | 页表与日志轨迹     | 重建一致映射或事务 |
| 设备与虚拟化 | 请求和完成一一对应     | 超时、迁移、过量提交 | 控制器与客户机状态 | 清理后相同输入重放 |
| 分布式与安全 | 所有权、身份、协议明确 | 节点失联、越权、重放 | 消息与审计记录     | 恢复隔离和最小权限 |

## 分步可视实验

<Stepper>
  <Step title="1. 建立资源预测">
    写出识别硬件资源到建立系统抽象之间的输入、内核对象、所有者和允许的失败出口。
    <Mos01IntroductionMapLab />
  </Step>
  <Step title="2. 注入单一边界">
    只改变一个容量、权限、设备或时序条件，在首个偏离处停止。
    <Mos01IntroductionExperimentLab />
  </Step>
  <Step title="3. 清理并恢复重放">
    删除故障，用同一输入核对状态、结果和资源回收。
    <Mos01IntroductionEvidenceLab />
  </Step>
</Stepper>

先预测：若在“发起系统调用”注入失败，哪个内核状态应最先变化，后续哪个阶段必须停止提交副作用？写出答案后再操作实验控件。

## 可重放实验记录

```text
trace = []
for stage in ["识别硬件资源", "建立系统抽象", "发起系统调用", "进入内核", "执行策略机制", "返回受控结果"]:
  expected = predict(stage, kernel_state)
  actual = execute(stage, kernel_state)
  trace.append({ stage, expected, actual, owner, cleanup })
  stop_at_first_divergence(expected, actual)
```

```yaml
book: 现代操作系统（原书第4版）
edition: 2017-07-19
page: mos-01-introduction
formal_scope: 57条
sample: normal | boundary | failure | recovery
predict_before_run: true
capture_first_divergence: true
replay_same_input: true
```

```text
freeze version, input, workload, and environment
record system calls, kernel state, resources, devices, and synchronization
change exactly one condition
stop at the first divergent boundary
clean up in reverse acquisition order
replay the same input and verify recovery
```

## 常见误区与故障注入

<Callout type="trap">
  **误区一：应用结果正确就代表操作系统资源正确。**
  中间仍可能有竞争、脏映射、丢中断、未提交元数据、泄漏对象或越权；必须检查首个内核状态变化和清理结果。
</Callout>

<Callout type="trap">
  **误区二：只学进程、内存和文件就等于复刻第4版。** 原书还包含完整
  I/O、虚拟化与云、多处理机、安全、Linux/Android、Windows
  8、操作系统设计和研究书目，不能用九个泛化主题代替13章。
</Callout>

## 四类样本与验收

| 样本 | 注入方式                     | 必查证据                 | 通过条件     |
| ---- | ---------------------------- | ------------------------ | ------------ |
| 正常 | 固定版本、负载和输入         | 全阶段状态与输出         | 与预测一致   |
| 边界 | 极值、空输入、低容量         | 终止、权限与资源状态     | 不越界不泄漏 |
| 失败 | 错地址、设备超时、断连或竞争 | 首个错误与责任层         | 失败被隔离   |
| 恢复 | 逆序清理后重放               | 状态、输出、持久化和审计 | 回到受控结果 |

## 练习

<Exercises>

**问题 1** 为什么不能只看最终输出验收第 1 章 引论？

<Answer>
  最终输出会隐藏调度次序、地址映射、设备队列、持久化边界、资源所有权和权限状态。必须先预测，保存首个偏离点，逆序清理后再以同一输入重放。
</Answer>

**问题 2** 如何把“第1章 引论”变成可执行证据？

<Answer>
  依次沿“识别硬件资源、建立系统抽象、发起系统调用、进入内核、执行策略机制、返回受控结果”登记输入、内核状态、责任接口、边界、失败出口和恢复条件，并让正常、边界、失败、恢复四类样本都留下可比较快照。
</Answer>

**问题 3** 本页最小通过条件是什么？

<Answer>
  用户程序只能经受控接口请求特权资源；抽象隐藏设备细节但保留权限、错误和生命周期边界，机制与策略能够分别说明。
  同时，异常路径不污染下一次运行，所有已获得资源都有唯一逆序清理路径，恢复轨迹重新到达正常样本的状态与结果。
</Answer>

</Exercises>

## 小结

- 扩展机器：连接“识别硬件资源”的请求与内核状态。
- 资源管理者：连接“建立系统抽象”的资源所有权。
- 系统调用：连接“发起系统调用”的同步或权限边界。
- 微内核：连接“进入内核”的失败传播。
- 运行模型：连接“执行策略机制”的恢复证据。
- 57个正式目录坐标已进入本页或全书资源链，不以泛化主题替代原书章节。
- 返回值只是证据之一，内核状态、资源、持久化和清理共同决定是否通过。

## 术语表

<Glossary>
  <GlossaryItem term="扩展机器">
    扩展机器对应“识别硬件资源”，验收时连接请求、内核状态、资源边界、失败传播和恢复结果。
  </GlossaryItem>
  <GlossaryItem term="资源管理者">
    资源管理者对应“建立系统抽象”，验收时连接请求、内核状态、资源边界、失败传播和恢复结果。
  </GlossaryItem>
  <GlossaryItem term="系统调用">
    系统调用对应“发起系统调用”，验收时连接请求、内核状态、资源边界、失败传播和恢复结果。
  </GlossaryItem>
  <GlossaryItem term="微内核">
    微内核对应“进入内核”，验收时连接请求、内核状态、资源边界、失败传播和恢复结果。
  </GlossaryItem>
  <GlossaryItem term="运行模型">
    运行模型对应“执行策略机制”，验收时连接请求、内核状态、资源边界、失败传播和恢复结果。
  </GlossaryItem>
</Glossary>

<Attribution
  adaptedFrom="Tanenbaum、Bos · 现代操作系统（原书第4版）· 陈向群、马洪兵等译 · 机械工业出版社 · 2017-07-19 · 出版社完整目录独立教学重构"
  adaptedUrl="https://www.cmpedu.com/books/book/5610493.htm"
/>
