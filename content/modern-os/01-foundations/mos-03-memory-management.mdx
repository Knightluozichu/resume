---
title: "第 3 章 内存管理"
type: A
section: "现代操作系统 · 地址空间"
order: 3
description: "从无抽象和交换进入地址空间、分页与页表、TLB、大内存页表、十类置换算法、分页设计问题、缺页实现和分段。覆盖45个正式目录坐标。"
demo: true
math: false
sourceUrl: "https://www.cmpedu.com/books/book/5610493.htm"
draft: false
---


## 直觉起点

、、、、共同构成本页坐标。从无抽象和交换进入地址空间、分页与页表、TLB、大内存页表、十类置换算法、分页设计问题、缺页实现和分段。
学习时先预测内核下一状态，再运行样本；最终输出正确不能证明调度、地址、设备、持久化与清理都正确。

## 六阶段资源链

### 1. 生成虚拟地址

把虚拟地址拆为可核查字段，依次检查映射、权限、驻留和置换；地址抽象必须同时说明隔离、共享与缺页恢复。 对“第3章 内存管理”先在“生成虚拟地址”写出输入与预测，再推进到“查询地址映射”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 2. 查询地址映射

从无抽象和交换进入地址空间、分页与页表、TLB、大内存页表、十类置换算法、分页设计问题、缺页实现和分段。 对“3.1 无存储器抽象”先在“查询地址映射”写出输入与预测，再推进到“检查页面权限”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 3. 检查页面权限

把虚拟地址拆为可核查字段，依次检查映射、权限、驻留和置换；地址抽象必须同时说明隔离、共享与缺页恢复。 对“3.2 一种存储器抽象：地址空间”先在“检查页面权限”写出输入与预测，再推进到“处理缺页”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 4. 处理缺页

把虚拟地址拆为可核查字段，依次检查映射、权限、驻留和置换；地址抽象必须同时说明隔离、共享与缺页恢复。 对“3.2.1 地址空间的概念”先在“处理缺页”写出输入与预测，再推进到“选择置换页”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 5. 选择置换页

把虚拟地址拆为可核查字段，依次检查映射、权限、驻留和置换；地址抽象必须同时说明隔离、共享与缺页恢复。 对“3.2.2 交换技术”先在“选择置换页”写出输入与预测，再推进到“更新驻留集合”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 6. 更新驻留集合

把虚拟地址拆为可核查字段，依次检查映射、权限、驻留和置换；地址抽象必须同时说明隔离、共享与缺页恢复。 对“3.2.3 空闲内存管理”先在“更新驻留集合”写出输入与预测，再推进到“生成虚拟地址”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

## 核心机制深挖

从无抽象和交换进入地址空间、分页与页表、TLB、大内存页表、十类置换算法、分页设计问题、缺页实现和分段。 本页把每个目录概念放回“生成虚拟地址 → 查询地址映射 → 检查页面权限 → 处理缺页 → 选择置换页 → 更新驻留集合”的资源状态机。输入可能是系统调用、地址、字节流、中断、资源请求或远程消息；状态可能位于进程表、页表、驱动队列、文件元数据、虚拟机或保护域；输出必须同时说明返回值、资源所有权与下一阶段可依赖的承诺。

本页签发的不变量是：**每个可访问虚拟地址映射到合法物理页或触发受控缺页；置换只淘汰允许的页面，工作集与负载控制避免持续抖动。**。不变量在正常路径与错误路径都应成立。一次成功运行只证明一个样本可用，不能证明边界安全；因此目录中的机制都要进入正常、边界、失败和恢复四条轨迹，并明确谁检测、谁阻塞或重试、谁逆序清理、谁向上报告。

逐位翻译地址并模拟 FIFO、Clock、LRU、工作集和 WSClock，对同一引用串比较缺页、写回与驻留集合。 运行前把预期写成状态快照，运行后逐字段比较。并发实验记录同步边，地址实验记录位宽与权限，I/O 实验记录缓冲区和完成事件，持久化实验区分提交与落盘，分布式实验记录丢失、重复和重排。

注入页表权限错误、TLB 陈旧、物理帧不足和全局置换干扰，区分地址无效、保护异常与负载过高。 删除故障后用完全相同的输入重放，确认返回值、内核对象、外部结果和清理状态一起恢复。只修正界面结果却留下线程、页、设备请求、文件引用、虚拟机状态或权限污染，仍然不通过。

原书第4版使用 Linux、Android、Windows 8.1、VMware 和当时的多处理机技术作为实例。现代内核和云平台会改变接口版本与性能数值，但进程状态、地址翻译、I/O 分层、资源死锁、虚拟化隔离和保护域仍可验证。现代对照必须写明差异，不能覆盖2017年中文版本的目录身份。

## 正式目录逐项讲解

### 第3章 内存管理

把虚拟地址拆为可核查字段，依次检查映射、权限、驻留和置换；地址抽象必须同时说明隔离、共享与缺页恢复。 对“第3章 内存管理”先在“生成虚拟地址”写出输入与预测，再推进到“查询地址映射”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 3.1 无存储器抽象

从无抽象和交换进入地址空间、分页与页表、TLB、大内存页表、十类置换算法、分页设计问题、缺页实现和分段。 对“3.1 无存储器抽象”先在“查询地址映射”写出输入与预测，再推进到“检查页面权限”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 3.2 一种存储器抽象：地址空间

把虚拟地址拆为可核查字段，依次检查映射、权限、驻留和置换；地址抽象必须同时说明隔离、共享与缺页恢复。 对“3.2 一种存储器抽象：地址空间”先在“检查页面权限”写出输入与预测，再推进到“处理缺页”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 3.2.1 地址空间的概念

把虚拟地址拆为可核查字段，依次检查映射、权限、驻留和置换；地址抽象必须同时说明隔离、共享与缺页恢复。 对“3.2.1 地址空间的概念”先在“处理缺页”写出输入与预测，再推进到“选择置换页”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 3.2.2 交换技术

把虚拟地址拆为可核查字段，依次检查映射、权限、驻留和置换；地址抽象必须同时说明隔离、共享与缺页恢复。 对“3.2.2 交换技术”先在“选择置换页”写出输入与预测，再推进到“更新驻留集合”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 3.2.3 空闲内存管理

把虚拟地址拆为可核查字段，依次检查映射、权限、驻留和置换；地址抽象必须同时说明隔离、共享与缺页恢复。 对“3.2.3 空闲内存管理”先在“更新驻留集合”写出输入与预测，再推进到“生成虚拟地址”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 3.3 虚拟内存

把虚拟地址拆为可核查字段，依次检查映射、权限、驻留和置换；地址抽象必须同时说明隔离、共享与缺页恢复。 对“3.3 虚拟内存”先在“生成虚拟地址”写出输入与预测，再推进到“查询地址映射”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 3.3.1 分页

把虚拟地址拆为可核查字段，依次检查映射、权限、驻留和置换；地址抽象必须同时说明隔离、共享与缺页恢复。 对“3.3.1 分页”先在“查询地址映射”写出输入与预测，再推进到“检查页面权限”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 3.3.2 页表

把虚拟地址拆为可核查字段，依次检查映射、权限、驻留和置换；地址抽象必须同时说明隔离、共享与缺页恢复。 对“3.3.2 页表”先在“检查页面权限”写出输入与预测，再推进到“处理缺页”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 3.3.3 加速分页过程

把虚拟地址拆为可核查字段，依次检查映射、权限、驻留和置换；地址抽象必须同时说明隔离、共享与缺页恢复。 对“3.3.3 加速分页过程”先在“处理缺页”写出输入与预测，再推进到“选择置换页”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 3.3.4 针对大内存的页表

把虚拟地址拆为可核查字段，依次检查映射、权限、驻留和置换；地址抽象必须同时说明隔离、共享与缺页恢复。 对“3.3.4 针对大内存的页表”先在“选择置换页”写出输入与预测，再推进到“更新驻留集合”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 3.4 页面置换算法

把虚拟地址拆为可核查字段，依次检查映射、权限、驻留和置换；地址抽象必须同时说明隔离、共享与缺页恢复。 对“3.4 页面置换算法”先在“更新驻留集合”写出输入与预测，再推进到“生成虚拟地址”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 3.4.1 最优页面置换算法

把虚拟地址拆为可核查字段，依次检查映射、权限、驻留和置换；地址抽象必须同时说明隔离、共享与缺页恢复。 对“3.4.1 最优页面置换算法”先在“生成虚拟地址”写出输入与预测，再推进到“查询地址映射”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 3.4.2 最近未使用页面置换算法

把虚拟地址拆为可核查字段，依次检查映射、权限、驻留和置换；地址抽象必须同时说明隔离、共享与缺页恢复。 对“3.4.2 最近未使用页面置换算法”先在“查询地址映射”写出输入与预测，再推进到“检查页面权限”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 3.4.3 先进先出页面置换算法

把虚拟地址拆为可核查字段，依次检查映射、权限、驻留和置换；地址抽象必须同时说明隔离、共享与缺页恢复。 对“3.4.3 先进先出页面置换算法”先在“检查页面权限”写出输入与预测，再推进到“处理缺页”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 3.4.4 第二次机会页面置换算法

把虚拟地址拆为可核查字段，依次检查映射、权限、驻留和置换；地址抽象必须同时说明隔离、共享与缺页恢复。 对“3.4.4 第二次机会页面置换算法”先在“处理缺页”写出输入与预测，再推进到“选择置换页”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 3.4.5 时钟页面置换算法

把虚拟地址拆为可核查字段，依次检查映射、权限、驻留和置换；地址抽象必须同时说明隔离、共享与缺页恢复。 对“3.4.5 时钟页面置换算法”先在“选择置换页”写出输入与预测，再推进到“更新驻留集合”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 3.4.6 最近最少使用页面置换算法

把虚拟地址拆为可核查字段，依次检查映射、权限、驻留和置换；地址抽象必须同时说明隔离、共享与缺页恢复。 对“3.4.6 最近最少使用页面置换算法”先在“更新驻留集合”写出输入与预测，再推进到“生成虚拟地址”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 3.4.7 用软件模拟LRU

从无抽象和交换进入地址空间、分页与页表、TLB、大内存页表、十类置换算法、分页设计问题、缺页实现和分段。 对“3.4.7 用软件模拟LRU”先在“生成虚拟地址”写出输入与预测，再推进到“查询地址映射”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 3.4.8 工作集页面置换算法

把虚拟地址拆为可核查字段，依次检查映射、权限、驻留和置换；地址抽象必须同时说明隔离、共享与缺页恢复。 对“3.4.8 工作集页面置换算法”先在“查询地址映射”写出输入与预测，再推进到“检查页面权限”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 3.4.9 工作集时钟页面置换算法

把虚拟地址拆为可核查字段，依次检查映射、权限、驻留和置换；地址抽象必须同时说明隔离、共享与缺页恢复。 对“3.4.9 工作集时钟页面置换算法”先在“检查页面权限”写出输入与预测，再推进到“处理缺页”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 3.4.10 页面置换算法小结

本节用“每个可访问虚拟地址映射到合法物理页或触发受控缺页；置换只淘汰允许的页面，工作集与负载控制避免持续抖动。”回查正常、边界、失败和恢复四条轨迹，而不是只重复术语。 对“3.4.10 页面置换算法小结”先在“处理缺页”写出输入与预测，再推进到“选择置换页”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 3.5 分页系统中的设计问题

把虚拟地址拆为可核查字段，依次检查映射、权限、驻留和置换；地址抽象必须同时说明隔离、共享与缺页恢复。 对“3.5 分页系统中的设计问题”先在“选择置换页”写出输入与预测，再推进到“更新驻留集合”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 3.5.1 局部分配策略与全局分配策略

从无抽象和交换进入地址空间、分页与页表、TLB、大内存页表、十类置换算法、分页设计问题、缺页实现和分段。 对“3.5.1 局部分配策略与全局分配策略”先在“更新驻留集合”写出输入与预测，再推进到“生成虚拟地址”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 3.5.2 负载控制

先声明共享或消息模型，再定义一致性、失败和重试语义；缓存同步、远程调用与负载迁移都可能改变性能和所有权。 对“3.5.2 负载控制”先在“生成虚拟地址”写出输入与预测，再推进到“查询地址映射”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 3.5.3 页面大小

把虚拟地址拆为可核查字段，依次检查映射、权限、驻留和置换；地址抽象必须同时说明隔离、共享与缺页恢复。 对“3.5.3 页面大小”先在“查询地址映射”写出输入与预测，再推进到“检查页面权限”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 3.5.4 分离的指令空间和数据空间

从无抽象和交换进入地址空间、分页与页表、TLB、大内存页表、十类置换算法、分页设计问题、缺页实现和分段。 对“3.5.4 分离的指令空间和数据空间”先在“检查页面权限”写出输入与预测，再推进到“处理缺页”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 3.5.5 共享页面

把虚拟地址拆为可核查字段，依次检查映射、权限、驻留和置换；地址抽象必须同时说明隔离、共享与缺页恢复。 对“3.5.5 共享页面”先在“处理缺页”写出输入与预测，再推进到“选择置换页”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 3.5.6 共享库

从无抽象和交换进入地址空间、分页与页表、TLB、大内存页表、十类置换算法、分页设计问题、缺页实现和分段。 对“3.5.6 共享库”先在“选择置换页”写出输入与预测，再推进到“更新驻留集合”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 3.5.7 内存映射文件

把虚拟地址拆为可核查字段，依次检查映射、权限、驻留和置换；地址抽象必须同时说明隔离、共享与缺页恢复。 对“3.5.7 内存映射文件”先在“更新驻留集合”写出输入与预测，再推进到“生成虚拟地址”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 3.5.8 清除策略

从无抽象和交换进入地址空间、分页与页表、TLB、大内存页表、十类置换算法、分页设计问题、缺页实现和分段。 对“3.5.8 清除策略”先在“生成虚拟地址”写出输入与预测，再推进到“查询地址映射”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 3.5.9 虚拟内存接口

把虚拟地址拆为可核查字段，依次检查映射、权限、驻留和置换；地址抽象必须同时说明隔离、共享与缺页恢复。 对“3.5.9 虚拟内存接口”先在“查询地址映射”写出输入与预测，再推进到“检查页面权限”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 3.6 有关实现的问题

从无抽象和交换进入地址空间、分页与页表、TLB、大内存页表、十类置换算法、分页设计问题、缺页实现和分段。 对“3.6 有关实现的问题”先在“检查页面权限”写出输入与预测，再推进到“处理缺页”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 3.6.1 与分页有关的工作

把虚拟地址拆为可核查字段，依次检查映射、权限、驻留和置换；地址抽象必须同时说明隔离、共享与缺页恢复。 对“3.6.1 与分页有关的工作”先在“处理缺页”写出输入与预测，再推进到“选择置换页”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 3.6.2 缺页中断处理

把虚拟地址拆为可核查字段，依次检查映射、权限、驻留和置换；地址抽象必须同时说明隔离、共享与缺页恢复。 对“3.6.2 缺页中断处理”先在“选择置换页”写出输入与预测，再推进到“更新驻留集合”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 3.6.3 指令备份

从无抽象和交换进入地址空间、分页与页表、TLB、大内存页表、十类置换算法、分页设计问题、缺页实现和分段。 对“3.6.3 指令备份”先在“更新驻留集合”写出输入与预测，再推进到“生成虚拟地址”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 3.6.4 锁定内存中的页面

把虚拟地址拆为可核查字段，依次检查映射、权限、驻留和置换；地址抽象必须同时说明隔离、共享与缺页恢复。 对“3.6.4 锁定内存中的页面”先在“生成虚拟地址”写出输入与预测，再推进到“查询地址映射”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 3.6.5 后备存储

从无抽象和交换进入地址空间、分页与页表、TLB、大内存页表、十类置换算法、分页设计问题、缺页实现和分段。 对“3.6.5 后备存储”先在“查询地址映射”写出输入与预测，再推进到“检查页面权限”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 3.6.6 策略和机制的分离

从无抽象和交换进入地址空间、分页与页表、TLB、大内存页表、十类置换算法、分页设计问题、缺页实现和分段。 对“3.6.6 策略和机制的分离”先在“检查页面权限”写出输入与预测，再推进到“处理缺页”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 3.7 分段

把虚拟地址拆为可核查字段，依次检查映射、权限、驻留和置换；地址抽象必须同时说明隔离、共享与缺页恢复。 对“3.7 分段”先在“处理缺页”写出输入与预测，再推进到“选择置换页”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 3.7.1 纯分段的实现

把虚拟地址拆为可核查字段，依次检查映射、权限、驻留和置换；地址抽象必须同时说明隔离、共享与缺页恢复。 对“3.7.1 纯分段的实现”先在“选择置换页”写出输入与预测，再推进到“更新驻留集合”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 3.7.2 分段和分页结合：MULTICS

把虚拟地址拆为可核查字段，依次检查映射、权限、驻留和置换；地址抽象必须同时说明隔离、共享与缺页恢复。 对“3.7.2 分段和分页结合：MULTICS”先在“更新驻留集合”写出输入与预测，再推进到“生成虚拟地址”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 3.7.3 分段和分页结合：Intel x86

把虚拟地址拆为可核查字段，依次检查映射、权限、驻留和置换；地址抽象必须同时说明隔离、共享与缺页恢复。 对“3.7.3 分段和分页结合：Intel x86”先在“生成虚拟地址”写出输入与预测，再推进到“查询地址映射”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 3.8 有关内存管理的研究

把虚拟地址拆为可核查字段，依次检查映射、权限、驻留和置换；地址抽象必须同时说明隔离、共享与缺页恢复。 对“3.8 有关内存管理的研究”先在“查询地址映射”写出输入与预测，再推进到“检查页面权限”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 3.9 小结

本节用“每个可访问虚拟地址映射到合法物理页或触发受控缺页；置换只淘汰允许的页面，工作集与负载控制避免持续抖动。”回查正常、边界、失败和恢复四条轨迹，而不是只重复术语。 对“3.9 小结”先在“检查页面权限”写出输入与预测，再推进到“处理缺页”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

## 状态与失败矩阵

| 对象         | 正常状态               | 边界样本             | 失败证据           | 恢复条件           |
| ------------ | ---------------------- | -------------------- | ------------------ | ------------------ |
| 进程与同步   | 状态转换和同步边明确   | 抢占、竞争、低容量   | 调度与等待图       | 唤醒并回收全部流   |
| 地址与存储   | 映射、权限、元数据有效 | 缺页、空间耗尽、断电 | 页表与日志轨迹     | 重建一致映射或事务 |
| 设备与虚拟化 | 请求和完成一一对应     | 超时、迁移、过量提交 | 控制器与客户机状态 | 清理后相同输入重放 |
| 分布式与安全 | 所有权、身份、协议明确 | 节点失联、越权、重放 | 消息与审计记录     | 恢复隔离和最小权限 |

先预测：若在“检查页面权限”注入失败，哪个内核状态应最先变化，后续哪个阶段必须停止提交副作用？写出答案后再操作实验控件。

## 可重放实验记录

```text
trace = []
for stage in ["生成虚拟地址", "查询地址映射", "检查页面权限", "处理缺页", "选择置换页", "更新驻留集合"]:
  expected = predict(stage, kernel_state)
  actual = execute(stage, kernel_state)
  trace.append({ stage, expected, actual, owner, cleanup })
  stop_at_first_divergence(expected, actual)
```

```yaml
book: 现代操作系统（原书第4版）
edition: 2017-07-19
page: mos-03-memory-management
formal_scope: 45条
sample: normal | boundary | failure | recovery
predict_before_run: true
capture_first_divergence: true
replay_same_input: true
```

```text
freeze version, input, workload, and environment
record system calls, kernel state, resources, devices, and synchronization
change exactly one condition
stop at the first divergent boundary
clean up in reverse acquisition order
replay the same input and verify recovery
```

## 常见误区与故障注入


## 四类样本与验收

| 样本 | 注入方式                     | 必查证据                 | 通过条件     |
| ---- | ---------------------------- | ------------------------ | ------------ |
| 正常 | 固定版本、负载和输入         | 全阶段状态与输出         | 与预测一致   |
| 边界 | 极值、空输入、低容量         | 终止、权限与资源状态     | 不越界不泄漏 |
| 失败 | 错地址、设备超时、断连或竞争 | 首个错误与责任层         | 失败被隔离   |
| 恢复 | 逆序清理后重放               | 状态、输出、持久化和审计 | 回到受控结果 |

## 练习


## 小结

- 地址空间：连接“生成虚拟地址”的请求与内核状态。
- 分页：连接“查询地址映射”的资源所有权。
- 页表：连接“检查页面权限”的同步或权限边界。
- 页面置换：连接“处理缺页”的失败传播。
- 工作集：连接“选择置换页”的恢复证据。
- 45个正式目录坐标已进入本页或全书资源链，不以泛化主题替代原书章节。
- 返回值只是证据之一，内核状态、资源、持久化和清理共同决定是否通过。

## 术语表


