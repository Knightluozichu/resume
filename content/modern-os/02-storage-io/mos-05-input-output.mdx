---
title: "第 5 章 输入/输出"
type: A
section: "现代操作系统 · 设备与 I/O"
order: 5
description: "贯通 I/O 硬件、程序控制/中断驱动/DMA、软件层次、磁盘、时钟、输入输出界面、瘦客户机与电源管理。覆盖37个正式目录坐标。"
demo: true
math: false
sourceUrl: "https://www.cmpedu.com/books/book/5610493.htm"
draft: false
---


## 直觉起点

、、、、共同构成本页坐标。贯通 I/O
硬件、程序控制/中断驱动/DMA、软件层次、磁盘、时钟、输入输出界面、瘦客户机与电源管理。
学习时先预测内核下一状态，再运行样本；最终输出正确不能证明调度、地址、设备、持久化与清理都正确。

## 六阶段资源链

### 1. 提交 I/O 请求

贯通 I/O 硬件、程序控制/中断驱动/DMA、软件层次、磁盘、时钟、输入输出界面、瘦客户机与电源管理。 对“第5章 输入/输出”先在“提交 I/O 请求”写出输入与预测，再推进到“配置设备控制器”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 2. 配置设备控制器

沿请求、控制器、数据搬运、完成通知和持久化逐层追踪；缓冲区生命周期、元数据一致性和错误传播不能省略。 对“5.1 I/O硬件原理”先在“配置设备控制器”写出输入与预测，再推进到“搬运数据”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 3. 搬运数据

沿请求、控制器、数据搬运、完成通知和持久化逐层追踪；缓冲区生命周期、元数据一致性和错误传播不能省略。 对“5.1.1 I/O设备”先在“搬运数据”写出输入与预测，再推进到“触发中断”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 4. 触发中断

沿请求、控制器、数据搬运、完成通知和持久化逐层追踪；缓冲区生命周期、元数据一致性和错误传播不能省略。 对“5.1.2 设备控制器”先在“触发中断”写出输入与预测，再推进到“完成驱动回调”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 5. 完成驱动回调

把虚拟地址拆为可核查字段，依次检查映射、权限、驻留和置换；地址抽象必须同时说明隔离、共享与缺页恢复。 对“5.1.3 内存映射I/O”先在“完成驱动回调”写出输入与预测，再推进到“返回设备无关结果”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 6. 返回设备无关结果

贯通 I/O 硬件、程序控制/中断驱动/DMA、软件层次、磁盘、时钟、输入输出界面、瘦客户机与电源管理。 对“5.1.4 直接存储器存取”先在“返回设备无关结果”写出输入与预测，再推进到“提交 I/O 请求”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

## 核心机制深挖

贯通 I/O 硬件、程序控制/中断驱动/DMA、软件层次、磁盘、时钟、输入输出界面、瘦客户机与电源管理。 本页把每个目录概念放回“提交 I/O 请求 → 配置设备控制器 → 搬运数据 → 触发中断 → 完成驱动回调 → 返回设备无关结果”的资源状态机。输入可能是系统调用、地址、字节流、中断、资源请求或远程消息；状态可能位于进程表、页表、驱动队列、文件元数据、虚拟机或保护域；输出必须同时说明返回值、资源所有权与下一阶段可依赖的承诺。

本页签发的不变量是：**设备所有权、缓冲区生命周期和完成信号一致；中断处理保持最小工作，DMA 完成前内存不被释放，错误向正确软件层传播。**。不变量在正常路径与错误路径都应成立。一次成功运行只证明一个样本可用，不能证明边界安全；因此目录中的机制都要进入正常、边界、失败和恢复四条轨迹，并明确谁检测、谁阻塞或重试、谁逆序清理、谁向上报告。

对同一块传输比较轮询、中断和 DMA，记录控制寄存器、内存缓冲区与中断时序，再模拟磁盘臂调度和软定时器。 运行前把预期写成状态快照，运行后逐字段比较。并发实验记录同步边，地址实验记录位宽与权限，I/O 实验记录缓冲区和完成事件，持久化实验区分提交与落盘，分布式实验记录丢失、重复和重排。

注入设备忙、超时、丢中断、DMA 越界、坏扇区和电源切换，验证驱动能清理并重试而不重复完成请求。 删除故障后用完全相同的输入重放，确认返回值、内核对象、外部结果和清理状态一起恢复。只修正界面结果却留下线程、页、设备请求、文件引用、虚拟机状态或权限污染，仍然不通过。

原书第4版使用 Linux、Android、Windows 8.1、VMware 和当时的多处理机技术作为实例。现代内核和云平台会改变接口版本与性能数值，但进程状态、地址翻译、I/O 分层、资源死锁、虚拟化隔离和保护域仍可验证。现代对照必须写明差异，不能覆盖2017年中文版本的目录身份。

## 正式目录逐项讲解

### 第5章 输入/输出

贯通 I/O 硬件、程序控制/中断驱动/DMA、软件层次、磁盘、时钟、输入输出界面、瘦客户机与电源管理。 对“第5章 输入/输出”先在“提交 I/O 请求”写出输入与预测，再推进到“配置设备控制器”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 5.1 I/O硬件原理

沿请求、控制器、数据搬运、完成通知和持久化逐层追踪；缓冲区生命周期、元数据一致性和错误传播不能省略。 对“5.1 I/O硬件原理”先在“配置设备控制器”写出输入与预测，再推进到“搬运数据”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 5.1.1 I/O设备

沿请求、控制器、数据搬运、完成通知和持久化逐层追踪；缓冲区生命周期、元数据一致性和错误传播不能省略。 对“5.1.1 I/O设备”先在“搬运数据”写出输入与预测，再推进到“触发中断”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 5.1.2 设备控制器

沿请求、控制器、数据搬运、完成通知和持久化逐层追踪；缓冲区生命周期、元数据一致性和错误传播不能省略。 对“5.1.2 设备控制器”先在“触发中断”写出输入与预测，再推进到“完成驱动回调”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 5.1.3 内存映射I/O

把虚拟地址拆为可核查字段，依次检查映射、权限、驻留和置换；地址抽象必须同时说明隔离、共享与缺页恢复。 对“5.1.3 内存映射I/O”先在“完成驱动回调”写出输入与预测，再推进到“返回设备无关结果”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 5.1.4 直接存储器存取

贯通 I/O 硬件、程序控制/中断驱动/DMA、软件层次、磁盘、时钟、输入输出界面、瘦客户机与电源管理。 对“5.1.4 直接存储器存取”先在“返回设备无关结果”写出输入与预测，再推进到“提交 I/O 请求”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 5.1.5 重温中断

沿请求、控制器、数据搬运、完成通知和持久化逐层追踪；缓冲区生命周期、元数据一致性和错误传播不能省略。 对“5.1.5 重温中断”先在“提交 I/O 请求”写出输入与预测，再推进到“配置设备控制器”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 5.2 I/O软件原理

沿请求、控制器、数据搬运、完成通知和持久化逐层追踪；缓冲区生命周期、元数据一致性和错误传播不能省略。 对“5.2 I/O软件原理”先在“配置设备控制器”写出输入与预测，再推进到“搬运数据”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 5.2.1 I/O软件的目标

沿请求、控制器、数据搬运、完成通知和持久化逐层追踪；缓冲区生命周期、元数据一致性和错误传播不能省略。 对“5.2.1 I/O软件的目标”先在“搬运数据”写出输入与预测，再推进到“触发中断”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 5.2.2 程序控制I/O

沿请求、控制器、数据搬运、完成通知和持久化逐层追踪；缓冲区生命周期、元数据一致性和错误传播不能省略。 对“5.2.2 程序控制I/O”先在“触发中断”写出输入与预测，再推进到“完成驱动回调”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 5.2.3 中断驱动I/O

沿请求、控制器、数据搬运、完成通知和持久化逐层追踪；缓冲区生命周期、元数据一致性和错误传播不能省略。 对“5.2.3 中断驱动I/O”先在“完成驱动回调”写出输入与预测，再推进到“返回设备无关结果”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 5.2.4 使用DMA的I/O

沿请求、控制器、数据搬运、完成通知和持久化逐层追踪；缓冲区生命周期、元数据一致性和错误传播不能省略。 对“5.2.4 使用DMA的I/O”先在“返回设备无关结果”写出输入与预测，再推进到“提交 I/O 请求”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 5.3 I/O软件层次

沿请求、控制器、数据搬运、完成通知和持久化逐层追踪；缓冲区生命周期、元数据一致性和错误传播不能省略。 对“5.3 I/O软件层次”先在“提交 I/O 请求”写出输入与预测，再推进到“配置设备控制器”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 5.3.1 中断处理程序

沿请求、控制器、数据搬运、完成通知和持久化逐层追踪；缓冲区生命周期、元数据一致性和错误传播不能省略。 对“5.3.1 中断处理程序”先在“配置设备控制器”写出输入与预测，再推进到“搬运数据”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 5.3.2 设备驱动程序

沿请求、控制器、数据搬运、完成通知和持久化逐层追踪；缓冲区生命周期、元数据一致性和错误传播不能省略。 对“5.3.2 设备驱动程序”先在“搬运数据”写出输入与预测，再推进到“触发中断”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 5.3.3 与设备无关的I/O软件

沿请求、控制器、数据搬运、完成通知和持久化逐层追踪；缓冲区生命周期、元数据一致性和错误传播不能省略。 对“5.3.3 与设备无关的I/O软件”先在“触发中断”写出输入与预测，再推进到“完成驱动回调”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 5.3.4 用户空间的I/O软件

沿请求、控制器、数据搬运、完成通知和持久化逐层追踪；缓冲区生命周期、元数据一致性和错误传播不能省略。 对“5.3.4 用户空间的I/O软件”先在“完成驱动回调”写出输入与预测，再推进到“返回设备无关结果”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 5.4 盘

沿请求、控制器、数据搬运、完成通知和持久化逐层追踪；缓冲区生命周期、元数据一致性和错误传播不能省略。 对“5.4 盘”先在“返回设备无关结果”写出输入与预测，再推进到“提交 I/O 请求”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 5.4.1 盘的硬件

沿请求、控制器、数据搬运、完成通知和持久化逐层追踪；缓冲区生命周期、元数据一致性和错误传播不能省略。 对“5.4.1 盘的硬件”先在“提交 I/O 请求”写出输入与预测，再推进到“配置设备控制器”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 5.4.2 磁盘格式化

沿请求、控制器、数据搬运、完成通知和持久化逐层追踪；缓冲区生命周期、元数据一致性和错误传播不能省略。 对“5.4.2 磁盘格式化”先在“配置设备控制器”写出输入与预测，再推进到“搬运数据”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 5.4.3 磁盘臂调度算法

先标出逻辑流、内核保存状态与共享变量，再用同步边和调度事件解释每次交错；安全、活性、公平和回收分别验收。 对“5.4.3 磁盘臂调度算法”先在“搬运数据”写出输入与预测，再推进到“触发中断”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 5.4.4 错误处理

贯通 I/O 硬件、程序控制/中断驱动/DMA、软件层次、磁盘、时钟、输入输出界面、瘦客户机与电源管理。 对“5.4.4 错误处理”先在“触发中断”写出输入与预测，再推进到“完成驱动回调”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 5.4.5 稳定存储器

贯通 I/O 硬件、程序控制/中断驱动/DMA、软件层次、磁盘、时钟、输入输出界面、瘦客户机与电源管理。 对“5.4.5 稳定存储器”先在“完成驱动回调”写出输入与预测，再推进到“返回设备无关结果”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 5.5 时钟

沿请求、控制器、数据搬运、完成通知和持久化逐层追踪；缓冲区生命周期、元数据一致性和错误传播不能省略。 对“5.5 时钟”先在“返回设备无关结果”写出输入与预测，再推进到“提交 I/O 请求”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 5.5.1 时钟硬件

沿请求、控制器、数据搬运、完成通知和持久化逐层追踪；缓冲区生命周期、元数据一致性和错误传播不能省略。 对“5.5.1 时钟硬件”先在“提交 I/O 请求”写出输入与预测，再推进到“配置设备控制器”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 5.5.2 时钟软件

沿请求、控制器、数据搬运、完成通知和持久化逐层追踪；缓冲区生命周期、元数据一致性和错误传播不能省略。 对“5.5.2 时钟软件”先在“配置设备控制器”写出输入与预测，再推进到“搬运数据”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 5.5.3 软定时器

贯通 I/O 硬件、程序控制/中断驱动/DMA、软件层次、磁盘、时钟、输入输出界面、瘦客户机与电源管理。 对“5.5.3 软定时器”先在“搬运数据”写出输入与预测，再推进到“触发中断”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 5.6 用户界面：键盘、鼠标和监视器

沿请求、控制器、数据搬运、完成通知和持久化逐层追踪；缓冲区生命周期、元数据一致性和错误传播不能省略。 对“5.6 用户界面：键盘、鼠标和监视器”先在“触发中断”写出输入与预测，再推进到“完成驱动回调”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 5.6.1 输入软件

贯通 I/O 硬件、程序控制/中断驱动/DMA、软件层次、磁盘、时钟、输入输出界面、瘦客户机与电源管理。 对“5.6.1 输入软件”先在“完成驱动回调”写出输入与预测，再推进到“返回设备无关结果”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 5.6.2 输出软件

贯通 I/O 硬件、程序控制/中断驱动/DMA、软件层次、磁盘、时钟、输入输出界面、瘦客户机与电源管理。 对“5.6.2 输出软件”先在“返回设备无关结果”写出输入与预测，再推进到“提交 I/O 请求”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 5.7 瘦客户机

贯通 I/O 硬件、程序控制/中断驱动/DMA、软件层次、磁盘、时钟、输入输出界面、瘦客户机与电源管理。 对“5.7 瘦客户机”先在“提交 I/O 请求”写出输入与预测，再推进到“配置设备控制器”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 5.8 电源管理

贯通 I/O 硬件、程序控制/中断驱动/DMA、软件层次、磁盘、时钟、输入输出界面、瘦客户机与电源管理。 对“5.8 电源管理”先在“配置设备控制器”写出输入与预测，再推进到“搬运数据”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 5.8.1 硬件问题

贯通 I/O 硬件、程序控制/中断驱动/DMA、软件层次、磁盘、时钟、输入输出界面、瘦客户机与电源管理。 对“5.8.1 硬件问题”先在“搬运数据”写出输入与预测，再推进到“触发中断”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 5.8.2 操作系统问题

贯通 I/O 硬件、程序控制/中断驱动/DMA、软件层次、磁盘、时钟、输入输出界面、瘦客户机与电源管理。 对“5.8.2 操作系统问题”先在“触发中断”写出输入与预测，再推进到“完成驱动回调”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 5.8.3 应用程序问题

贯通 I/O 硬件、程序控制/中断驱动/DMA、软件层次、磁盘、时钟、输入输出界面、瘦客户机与电源管理。 对“5.8.3 应用程序问题”先在“完成驱动回调”写出输入与预测，再推进到“返回设备无关结果”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 5.9 有关输入/输出的研究

贯通 I/O 硬件、程序控制/中断驱动/DMA、软件层次、磁盘、时钟、输入输出界面、瘦客户机与电源管理。 对“5.9 有关输入/输出的研究”先在“返回设备无关结果”写出输入与预测，再推进到“提交 I/O 请求”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 5.10 小结

本节用“设备所有权、缓冲区生命周期和完成信号一致；中断处理保持最小工作，DMA 完成前内存不被释放，错误向正确软件层传播。”回查正常、边界、失败和恢复四条轨迹，而不是只重复术语。 对“5.10 小结”先在“提交 I/O 请求”写出输入与预测，再推进到“配置设备控制器”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

## 状态与失败矩阵

| 对象         | 正常状态               | 边界样本             | 失败证据           | 恢复条件           |
| ------------ | ---------------------- | -------------------- | ------------------ | ------------------ |
| 进程与同步   | 状态转换和同步边明确   | 抢占、竞争、低容量   | 调度与等待图       | 唤醒并回收全部流   |
| 地址与存储   | 映射、权限、元数据有效 | 缺页、空间耗尽、断电 | 页表与日志轨迹     | 重建一致映射或事务 |
| 设备与虚拟化 | 请求和完成一一对应     | 超时、迁移、过量提交 | 控制器与客户机状态 | 清理后相同输入重放 |
| 分布式与安全 | 所有权、身份、协议明确 | 节点失联、越权、重放 | 消息与审计记录     | 恢复隔离和最小权限 |

先预测：若在“搬运数据”注入失败，哪个内核状态应最先变化，后续哪个阶段必须停止提交副作用？写出答案后再操作实验控件。

## 可重放实验记录

```text
trace = []
for stage in ["提交 I/O 请求", "配置设备控制器", "搬运数据", "触发中断", "完成驱动回调", "返回设备无关结果"]:
  expected = predict(stage, kernel_state)
  actual = execute(stage, kernel_state)
  trace.append({ stage, expected, actual, owner, cleanup })
  stop_at_first_divergence(expected, actual)
```

```yaml
book: 现代操作系统（原书第4版）
edition: 2017-07-19
page: mos-05-input-output
formal_scope: 37条
sample: normal | boundary | failure | recovery
predict_before_run: true
capture_first_divergence: true
replay_same_input: true
```

```text
freeze version, input, workload, and environment
record system calls, kernel state, resources, devices, and synchronization
change exactly one condition
stop at the first divergent boundary
clean up in reverse acquisition order
replay the same input and verify recovery
```

## 常见误区与故障注入


## 四类样本与验收

| 样本 | 注入方式                     | 必查证据                 | 通过条件     |
| ---- | ---------------------------- | ------------------------ | ------------ |
| 正常 | 固定版本、负载和输入         | 全阶段状态与输出         | 与预测一致   |
| 边界 | 极值、空输入、低容量         | 终止、权限与资源状态     | 不越界不泄漏 |
| 失败 | 错地址、设备超时、断连或竞争 | 首个错误与责任层         | 失败被隔离   |
| 恢复 | 逆序清理后重放               | 状态、输出、持久化和审计 | 回到受控结果 |

## 练习


## 小结

- 设备控制器：连接“提交 I/O 请求”的请求与内核状态。
- DMA：连接“配置设备控制器”的资源所有权。
- 中断：连接“搬运数据”的同步或权限边界。
- 设备驱动：连接“触发中断”的失败传播。
- 磁盘调度：连接“完成驱动回调”的恢复证据。
- 37个正式目录坐标已进入本页或全书资源链，不以泛化主题替代原书章节。
- 返回值只是证据之一，内核状态、资源、持久化和清理共同决定是否通过。

## 术语表


