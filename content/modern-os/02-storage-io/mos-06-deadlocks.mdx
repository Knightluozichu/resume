---
title: "第 6 章 死锁"
type: A
section: "现代操作系统 · 资源与进度"
order: 6
description: "从可抢占/不可抢占资源和四个必要条件进入建模、鸵鸟算法、检测恢复、银行家避免、条件破坏预防以及活锁与饥饿。覆盖29个正式目录坐标。"
demo: true
math: false
sourceUrl: "https://www.cmpedu.com/books/book/5610493.htm"
draft: true
---

import {
  Mos06DeadlocksMapLab,
  Mos06DeadlocksExperimentLab,
  Mos06DeadlocksEvidenceLab,
} from "@/components/mdx/modern-os/diagrams/mos-06-deadlocks";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第 6 章 死锁的核心机制，并说出它在中文原书第4版13章体系中的位置
- 能描述声明资源需求、分配当前资源、构造等待关系、判定安全序列、检测循环、恢复系统进度六个阶段的输入、内核状态、资源所有者与不变量
- 能实现最小可重放实验，注入边界或失败条件并定位首个偏离点
- 能比较正常、边界、失败和恢复轨迹，判断目录概念是否形成可迁移能力

</Objectives>

## 直觉起点

<Term def="资源图是第 6 章 死锁的第一个观察坐标，连接请求、内核状态与恢复证据。">资源图</Term>、<Term def="死锁是第 6 章 死锁的第二个观察坐标，用于解释资源与责任边界。">死锁</Term>、<Term def="安全状态是第 6 章 死锁的第三个观察坐标，需要用最小实验验收。">安全状态</Term>、<Term def="银行家算法是第 6 章 死锁的第四个观察坐标，边界条件会暴露约束。">银行家算法</Term>、<Term def="活锁是第 6 章 死锁的第五个观察坐标，必须定位首个状态分叉。">活锁</Term>
共同构成本页坐标。从可抢占/不可抢占资源和四个必要条件进入建模、鸵鸟算法、检测恢复、银行家避免、条件破坏预防以及活锁与饥饿。
学习时先预测内核下一状态，再运行样本；最终输出正确不能证明调度、地址、设备、持久化与清理都正确。

<Mos06DeadlocksMapLab />

## 六阶段资源链

### 1. 声明资源需求

用资源守恒和等待关系建模，区分预防、避免、检测恢复与忽略；没有进度还需继续区分死锁、活锁和饥饿。 对“第6章 死锁”先在“声明资源需求”写出输入与预测，再推进到“分配当前资源”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 2. 分配当前资源

用资源守恒和等待关系建模，区分预防、避免、检测恢复与忽略；没有进度还需继续区分死锁、活锁和饥饿。 对“6.1 资源”先在“分配当前资源”写出输入与预测，再推进到“构造等待关系”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 3. 构造等待关系

用资源守恒和等待关系建模，区分预防、避免、检测恢复与忽略；没有进度还需继续区分死锁、活锁和饥饿。 对“6.1.1 可抢占资源和不可抢占资源”先在“构造等待关系”写出输入与预测，再推进到“判定安全序列”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 4. 判定安全序列

用资源守恒和等待关系建模，区分预防、避免、检测恢复与忽略；没有进度还需继续区分死锁、活锁和饥饿。 对“6.1.2 资源获取”先在“判定安全序列”写出输入与预测，再推进到“检测循环”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 5. 检测循环

用资源守恒和等待关系建模，区分预防、避免、检测恢复与忽略；没有进度还需继续区分死锁、活锁和饥饿。 对“6.2 死锁简介”先在“检测循环”写出输入与预测，再推进到“恢复系统进度”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 6. 恢复系统进度

用资源守恒和等待关系建模，区分预防、避免、检测恢复与忽略；没有进度还需继续区分死锁、活锁和饥饿。 对“6.2.1 资源死锁的条件”先在“恢复系统进度”写出输入与预测，再推进到“声明资源需求”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

## 核心机制深挖

从可抢占/不可抢占资源和四个必要条件进入建模、鸵鸟算法、检测恢复、银行家避免、条件破坏预防以及活锁与饥饿。 本页把每个目录概念放回“声明资源需求 → 分配当前资源 → 构造等待关系 → 判定安全序列 → 检测循环 → 恢复系统进度”的资源状态机。输入可能是系统调用、地址、字节流、中断、资源请求或远程消息；状态可能位于进程表、页表、驱动队列、文件元数据、虚拟机或保护域；输出必须同时说明返回值、资源所有权与下一阶段可依赖的承诺。

本页签发的不变量是：**资源总量、已分配、可用和最大需求守恒；避免算法只批准仍有安全序列的请求，恢复操作最终打破等待循环。**。不变量在正常路径与错误路径都应成立。一次成功运行只证明一个样本可用，不能证明边界安全；因此目录中的机制都要进入正常、边界、失败和恢复四条轨迹，并明确谁检测、谁阻塞或重试、谁逆序清理、谁向上报告。

绘制资源分配图并运行单类/多类资源检测与银行家算法，比较预防、避免、检测恢复和忽略策略成本。 运行前把预期写成状态快照，运行后逐字段比较。并发实验记录同步边，地址实验记录位宽与权限，I/O 实验记录缓冲区和完成事件，持久化实验区分提交与落盘，分布式实验记录丢失、重复和重排。

构造循环等待、通信死锁、活锁与饥饿，区分没有进度的不同原因并验证恢复不会破坏资源守恒。 删除故障后用完全相同的输入重放，确认返回值、内核对象、外部结果和清理状态一起恢复。只修正界面结果却留下线程、页、设备请求、文件引用、虚拟机状态或权限污染，仍然不通过。

原书第4版使用 Linux、Android、Windows 8.1、VMware 和当时的多处理机技术作为实例。现代内核和云平台会改变接口版本与性能数值，但进程状态、地址翻译、I/O 分层、资源死锁、虚拟化隔离和保护域仍可验证。现代对照必须写明差异，不能覆盖2017年中文版本的目录身份。

## 正式目录逐项讲解

### 第6章 死锁

用资源守恒和等待关系建模，区分预防、避免、检测恢复与忽略；没有进度还需继续区分死锁、活锁和饥饿。 对“第6章 死锁”先在“声明资源需求”写出输入与预测，再推进到“分配当前资源”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 6.1 资源

用资源守恒和等待关系建模，区分预防、避免、检测恢复与忽略；没有进度还需继续区分死锁、活锁和饥饿。 对“6.1 资源”先在“分配当前资源”写出输入与预测，再推进到“构造等待关系”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 6.1.1 可抢占资源和不可抢占资源

用资源守恒和等待关系建模，区分预防、避免、检测恢复与忽略；没有进度还需继续区分死锁、活锁和饥饿。 对“6.1.1 可抢占资源和不可抢占资源”先在“构造等待关系”写出输入与预测，再推进到“判定安全序列”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 6.1.2 资源获取

用资源守恒和等待关系建模，区分预防、避免、检测恢复与忽略；没有进度还需继续区分死锁、活锁和饥饿。 对“6.1.2 资源获取”先在“判定安全序列”写出输入与预测，再推进到“检测循环”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 6.2 死锁简介

用资源守恒和等待关系建模，区分预防、避免、检测恢复与忽略；没有进度还需继续区分死锁、活锁和饥饿。 对“6.2 死锁简介”先在“检测循环”写出输入与预测，再推进到“恢复系统进度”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 6.2.1 资源死锁的条件

用资源守恒和等待关系建模，区分预防、避免、检测恢复与忽略；没有进度还需继续区分死锁、活锁和饥饿。 对“6.2.1 资源死锁的条件”先在“恢复系统进度”写出输入与预测，再推进到“声明资源需求”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 6.2.2 死锁建模

用资源守恒和等待关系建模，区分预防、避免、检测恢复与忽略；没有进度还需继续区分死锁、活锁和饥饿。 对“6.2.2 死锁建模”先在“声明资源需求”写出输入与预测，再推进到“分配当前资源”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 6.3 鸵鸟算法

从可抢占/不可抢占资源和四个必要条件进入建模、鸵鸟算法、检测恢复、银行家避免、条件破坏预防以及活锁与饥饿。 对“6.3 鸵鸟算法”先在“分配当前资源”写出输入与预测，再推进到“构造等待关系”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 6.4 死锁检测和死锁恢复

用资源守恒和等待关系建模，区分预防、避免、检测恢复与忽略；没有进度还需继续区分死锁、活锁和饥饿。 对“6.4 死锁检测和死锁恢复”先在“构造等待关系”写出输入与预测，再推进到“判定安全序列”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 6.4.1 每种类型一个资源的死锁检测

用资源守恒和等待关系建模，区分预防、避免、检测恢复与忽略；没有进度还需继续区分死锁、活锁和饥饿。 对“6.4.1 每种类型一个资源的死锁检测”先在“判定安全序列”写出输入与预测，再推进到“检测循环”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 6.4.2 每种类型多个资源的死锁检测

用资源守恒和等待关系建模，区分预防、避免、检测恢复与忽略；没有进度还需继续区分死锁、活锁和饥饿。 对“6.4.2 每种类型多个资源的死锁检测”先在“检测循环”写出输入与预测，再推进到“恢复系统进度”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 6.4.3 从死锁中恢复

用资源守恒和等待关系建模，区分预防、避免、检测恢复与忽略；没有进度还需继续区分死锁、活锁和饥饿。 对“6.4.3 从死锁中恢复”先在“恢复系统进度”写出输入与预测，再推进到“声明资源需求”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 6.5 死锁避免

用资源守恒和等待关系建模，区分预防、避免、检测恢复与忽略；没有进度还需继续区分死锁、活锁和饥饿。 对“6.5 死锁避免”先在“声明资源需求”写出输入与预测，再推进到“分配当前资源”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 6.5.1 资源轨迹图

用资源守恒和等待关系建模，区分预防、避免、检测恢复与忽略；没有进度还需继续区分死锁、活锁和饥饿。 对“6.5.1 资源轨迹图”先在“分配当前资源”写出输入与预测，再推进到“构造等待关系”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 6.5.2 安全状态和不安全状态

用资源守恒和等待关系建模，区分预防、避免、检测恢复与忽略；没有进度还需继续区分死锁、活锁和饥饿。 对“6.5.2 安全状态和不安全状态”先在“构造等待关系”写出输入与预测，再推进到“判定安全序列”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 6.5.3 单个资源的银行家算法

用资源守恒和等待关系建模，区分预防、避免、检测恢复与忽略；没有进度还需继续区分死锁、活锁和饥饿。 对“6.5.3 单个资源的银行家算法”先在“判定安全序列”写出输入与预测，再推进到“检测循环”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 6.5.4 多个资源的银行家算法

用资源守恒和等待关系建模，区分预防、避免、检测恢复与忽略；没有进度还需继续区分死锁、活锁和饥饿。 对“6.5.4 多个资源的银行家算法”先在“检测循环”写出输入与预测，再推进到“恢复系统进度”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 6.6 死锁预防

用资源守恒和等待关系建模，区分预防、避免、检测恢复与忽略；没有进度还需继续区分死锁、活锁和饥饿。 对“6.6 死锁预防”先在“恢复系统进度”写出输入与预测，再推进到“声明资源需求”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 6.6.1 破坏互斥条件

先标出逻辑流、内核保存状态与共享变量，再用同步边和调度事件解释每次交错；安全、活性、公平和回收分别验收。 对“6.6.1 破坏互斥条件”先在“声明资源需求”写出输入与预测，再推进到“分配当前资源”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 6.6.2 破坏占有并等待条件

从可抢占/不可抢占资源和四个必要条件进入建模、鸵鸟算法、检测恢复、银行家避免、条件破坏预防以及活锁与饥饿。 对“6.6.2 破坏占有并等待条件”先在“分配当前资源”写出输入与预测，再推进到“构造等待关系”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 6.6.3 破坏不可抢占条件

从可抢占/不可抢占资源和四个必要条件进入建模、鸵鸟算法、检测恢复、银行家避免、条件破坏预防以及活锁与饥饿。 对“6.6.3 破坏不可抢占条件”先在“构造等待关系”写出输入与预测，再推进到“判定安全序列”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 6.6.4 破坏环路等待条件

从可抢占/不可抢占资源和四个必要条件进入建模、鸵鸟算法、检测恢复、银行家避免、条件破坏预防以及活锁与饥饿。 对“6.6.4 破坏环路等待条件”先在“判定安全序列”写出输入与预测，再推进到“检测循环”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 6.7 其他问题

从可抢占/不可抢占资源和四个必要条件进入建模、鸵鸟算法、检测恢复、银行家避免、条件破坏预防以及活锁与饥饿。 对“6.7 其他问题”先在“检测循环”写出输入与预测，再推进到“恢复系统进度”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 6.7.1 两阶段加锁

从可抢占/不可抢占资源和四个必要条件进入建模、鸵鸟算法、检测恢复、银行家避免、条件破坏预防以及活锁与饥饿。 对“6.7.1 两阶段加锁”先在“恢复系统进度”写出输入与预测，再推进到“声明资源需求”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 6.7.2 通信死锁

用资源守恒和等待关系建模，区分预防、避免、检测恢复与忽略；没有进度还需继续区分死锁、活锁和饥饿。 对“6.7.2 通信死锁”先在“声明资源需求”写出输入与预测，再推进到“分配当前资源”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 6.7.3 活锁

用资源守恒和等待关系建模，区分预防、避免、检测恢复与忽略；没有进度还需继续区分死锁、活锁和饥饿。 对“6.7.3 活锁”先在“分配当前资源”写出输入与预测，再推进到“构造等待关系”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 6.7.4 饥饿

用资源守恒和等待关系建模，区分预防、避免、检测恢复与忽略；没有进度还需继续区分死锁、活锁和饥饿。 对“6.7.4 饥饿”先在“构造等待关系”写出输入与预测，再推进到“判定安全序列”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 6.8 有关死锁的研究

用资源守恒和等待关系建模，区分预防、避免、检测恢复与忽略；没有进度还需继续区分死锁、活锁和饥饿。 对“6.8 有关死锁的研究”先在“判定安全序列”写出输入与预测，再推进到“检测循环”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 6.9 小结

本节用“资源总量、已分配、可用和最大需求守恒；避免算法只批准仍有安全序列的请求，恢复操作最终打破等待循环。”回查正常、边界、失败和恢复四条轨迹，而不是只重复术语。 对“6.9 小结”先在“检测循环”写出输入与预测，再推进到“恢复系统进度”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

## 状态与失败矩阵

| 对象         | 正常状态               | 边界样本             | 失败证据           | 恢复条件           |
| ------------ | ---------------------- | -------------------- | ------------------ | ------------------ |
| 进程与同步   | 状态转换和同步边明确   | 抢占、竞争、低容量   | 调度与等待图       | 唤醒并回收全部流   |
| 地址与存储   | 映射、权限、元数据有效 | 缺页、空间耗尽、断电 | 页表与日志轨迹     | 重建一致映射或事务 |
| 设备与虚拟化 | 请求和完成一一对应     | 超时、迁移、过量提交 | 控制器与客户机状态 | 清理后相同输入重放 |
| 分布式与安全 | 所有权、身份、协议明确 | 节点失联、越权、重放 | 消息与审计记录     | 恢复隔离和最小权限 |

## 分步可视实验

<Stepper>
  <Step title="1. 建立资源预测">
    写出声明资源需求到分配当前资源之间的输入、内核对象、所有者和允许的失败出口。
    <Mos06DeadlocksMapLab />
  </Step>
  <Step title="2. 注入单一边界">
    只改变一个容量、权限、设备或时序条件，在首个偏离处停止。
    <Mos06DeadlocksExperimentLab />
  </Step>
  <Step title="3. 清理并恢复重放">
    删除故障，用同一输入核对状态、结果和资源回收。
    <Mos06DeadlocksEvidenceLab />
  </Step>
</Stepper>

先预测：若在“构造等待关系”注入失败，哪个内核状态应最先变化，后续哪个阶段必须停止提交副作用？写出答案后再操作实验控件。

## 可重放实验记录

```text
trace = []
for stage in ["声明资源需求", "分配当前资源", "构造等待关系", "判定安全序列", "检测循环", "恢复系统进度"]:
  expected = predict(stage, kernel_state)
  actual = execute(stage, kernel_state)
  trace.append({ stage, expected, actual, owner, cleanup })
  stop_at_first_divergence(expected, actual)
```

```yaml
book: 现代操作系统（原书第4版）
edition: 2017-07-19
page: mos-06-deadlocks
formal_scope: 29条
sample: normal | boundary | failure | recovery
predict_before_run: true
capture_first_divergence: true
replay_same_input: true
```

```text
freeze version, input, workload, and environment
record system calls, kernel state, resources, devices, and synchronization
change exactly one condition
stop at the first divergent boundary
clean up in reverse acquisition order
replay the same input and verify recovery
```

## 常见误区与故障注入

<Callout type="trap">
  **误区一：应用结果正确就代表操作系统资源正确。**
  中间仍可能有竞争、脏映射、丢中断、未提交元数据、泄漏对象或越权；必须检查首个内核状态变化和清理结果。
</Callout>

<Callout type="trap">
  **误区二：只学进程、内存和文件就等于复刻第4版。** 原书还包含完整
  I/O、虚拟化与云、多处理机、安全、Linux/Android、Windows
  8、操作系统设计和研究书目，不能用九个泛化主题代替13章。
</Callout>

## 四类样本与验收

| 样本 | 注入方式                     | 必查证据                 | 通过条件     |
| ---- | ---------------------------- | ------------------------ | ------------ |
| 正常 | 固定版本、负载和输入         | 全阶段状态与输出         | 与预测一致   |
| 边界 | 极值、空输入、低容量         | 终止、权限与资源状态     | 不越界不泄漏 |
| 失败 | 错地址、设备超时、断连或竞争 | 首个错误与责任层         | 失败被隔离   |
| 恢复 | 逆序清理后重放               | 状态、输出、持久化和审计 | 回到受控结果 |

## 练习

<Exercises>

**问题 1** 为什么不能只看最终输出验收第 6 章 死锁？

<Answer>
  最终输出会隐藏调度次序、地址映射、设备队列、持久化边界、资源所有权和权限状态。必须先预测，保存首个偏离点，逆序清理后再以同一输入重放。
</Answer>

**问题 2** 如何把“第6章 死锁”变成可执行证据？

<Answer>
  依次沿“声明资源需求、分配当前资源、构造等待关系、判定安全序列、检测循环、恢复系统进度”登记输入、内核状态、责任接口、边界、失败出口和恢复条件，并让正常、边界、失败、恢复四类样本都留下可比较快照。
</Answer>

**问题 3** 本页最小通过条件是什么？

<Answer>
  资源总量、已分配、可用和最大需求守恒；避免算法只批准仍有安全序列的请求，恢复操作最终打破等待循环。
  同时，异常路径不污染下一次运行，所有已获得资源都有唯一逆序清理路径，恢复轨迹重新到达正常样本的状态与结果。
</Answer>

</Exercises>

## 小结

- 资源图：连接“声明资源需求”的请求与内核状态。
- 死锁：连接“分配当前资源”的资源所有权。
- 安全状态：连接“构造等待关系”的同步或权限边界。
- 银行家算法：连接“判定安全序列”的失败传播。
- 活锁：连接“检测循环”的恢复证据。
- 29个正式目录坐标已进入本页或全书资源链，不以泛化主题替代原书章节。
- 返回值只是证据之一，内核状态、资源、持久化和清理共同决定是否通过。

## 术语表

<Glossary>
  <GlossaryItem term="资源图">
    资源图对应“声明资源需求”，验收时连接请求、内核状态、资源边界、失败传播和恢复结果。
  </GlossaryItem>
  <GlossaryItem term="死锁">
    死锁对应“分配当前资源”，验收时连接请求、内核状态、资源边界、失败传播和恢复结果。
  </GlossaryItem>
  <GlossaryItem term="安全状态">
    安全状态对应“构造等待关系”，验收时连接请求、内核状态、资源边界、失败传播和恢复结果。
  </GlossaryItem>
  <GlossaryItem term="银行家算法">
    银行家算法对应“判定安全序列”，验收时连接请求、内核状态、资源边界、失败传播和恢复结果。
  </GlossaryItem>
  <GlossaryItem term="活锁">
    活锁对应“检测循环”，验收时连接请求、内核状态、资源边界、失败传播和恢复结果。
  </GlossaryItem>
</Glossary>

<Attribution
  adaptedFrom="Tanenbaum、Bos · 现代操作系统（原书第4版）· 陈向群、马洪兵等译 · 机械工业出版社 · 2017-07-19 · 出版社完整目录独立教学重构"
  adaptedUrl="https://www.cmpedu.com/books/book/5610493.htm"
/>
