---
title: "第 7 章 虚拟化和云"
type: A
section: "现代操作系统 · 虚拟化与云"
order: 7
description: "从可虚拟化条件、第一类/第二类虚拟机管理程序和高效技术进入内存、I/O、多核虚拟化、云服务、迁移、检查点与 VMware。覆盖25个正式目录坐标。"
demo: true
math: false
sourceUrl: "https://www.cmpedu.com/books/book/5610493.htm"
draft: false
---

import {
  Mos07VirtualizationCloudMapLab,
  Mos07VirtualizationCloudExperimentLab,
  Mos07VirtualizationCloudEvidenceLab,
} from "@/components/mdx/modern-os/diagrams/mos-07-virtualization-cloud";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第 7 章 虚拟化和云的核心机制，并说出它在中文原书第4版13章体系中的位置
- 能描述截获敏感操作、虚拟处理器状态、映射客户内存、虚拟化设备、保存检查点、迁移并恢复六个阶段的输入、内核状态、资源所有者与不变量
- 能实现最小可重放实验，注入边界或失败条件并定位首个偏离点
- 能比较正常、边界、失败和恢复轨迹，判断目录概念是否形成可迁移能力

</Objectives>

## 直觉起点

<Term def="虚拟机管理程序是第 7 章 虚拟化和云的第一个观察坐标，连接请求、内核状态与恢复证据。">
  虚拟机管理程序
</Term>
、
<Term def="敏感指令是第 7 章 虚拟化和云的第二个观察坐标，用于解释资源与责任边界。">
  敏感指令
</Term>
、
<Term def="内存虚拟化是第 7 章 虚拟化和云的第三个观察坐标，需要用最小实验验收。">
  内存虚拟化
</Term>
、
<Term def="I/O 虚拟化是第 7 章 虚拟化和云的第四个观察坐标，边界条件会暴露约束。">
  I/O 虚拟化
</Term>
、
<Term def="检查点是第 7 章 虚拟化和云的第五个观察坐标，必须定位首个状态分叉。">
  检查点
</Term>
共同构成本页坐标。从可虚拟化条件、第一类/第二类虚拟机管理程序和高效技术进入内存、I/O、多核虚拟化、云服务、迁移、检查点与
VMware。
学习时先预测内核下一状态，再运行样本；最终输出正确不能证明调度、地址、设备、持久化与清理都正确。

<Mos07VirtualizationCloudMapLab />

## 六阶段资源链

### 1. 截获敏感操作

管理程序必须截获敏感操作并保持等价、隔离和资源记账；处理器、内存、设备和时间状态共同决定检查点能否恢复。 对“第7章 虚拟化和云”先在“截获敏感操作”写出输入与预测，再推进到“虚拟处理器状态”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 2. 虚拟处理器状态

从可虚拟化条件、第一类/第二类虚拟机管理程序和高效技术进入内存、I/O、多核虚拟化、云服务、迁移、检查点与 VMware。 对“7.1 历史”先在“虚拟处理器状态”写出输入与预测，再推进到“映射客户内存”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 3. 映射客户内存

管理程序必须截获敏感操作并保持等价、隔离和资源记账；处理器、内存、设备和时间状态共同决定检查点能否恢复。 对“7.2 虚拟化的必要条件”先在“映射客户内存”写出输入与预测，再推进到“虚拟化设备”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 4. 虚拟化设备

管理程序必须截获敏感操作并保持等价、隔离和资源记账；处理器、内存、设备和时间状态共同决定检查点能否恢复。 对“7.3 第一类和第二类虚拟机管理程序”先在“虚拟化设备”写出输入与预测，再推进到“保存检查点”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 5. 保存检查点

管理程序必须截获敏感操作并保持等价、隔离和资源记账；处理器、内存、设备和时间状态共同决定检查点能否恢复。 对“7.4 高效虚拟化技术”先在“保存检查点”写出输入与预测，再推进到“迁移并恢复”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 6. 迁移并恢复

管理程序必须截获敏感操作并保持等价、隔离和资源记账；处理器、内存、设备和时间状态共同决定检查点能否恢复。 对“7.4.1 在不支持虚拟化的平台上实现虚拟化”先在“迁移并恢复”写出输入与预测，再推进到“截获敏感操作”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

## 核心机制深挖

从可虚拟化条件、第一类/第二类虚拟机管理程序和高效技术进入内存、I/O、多核虚拟化、云服务、迁移、检查点与 VMware。 本页把每个目录概念放回“截获敏感操作 → 虚拟处理器状态 → 映射客户内存 → 虚拟化设备 → 保存检查点 → 迁移并恢复”的资源状态机。输入可能是系统调用、地址、字节流、中断、资源请求或远程消息；状态可能位于进程表、页表、驱动队列、文件元数据、虚拟机或保护域；输出必须同时说明返回值、资源所有权与下一阶段可依赖的承诺。

本页签发的不变量是：**客户机看到与独占机器一致的体系结构状态；管理程序保持隔离和资源记账，迁移或恢复后设备、内存与执行状态一致。**。不变量在正常路径与错误路径都应成立。一次成功运行只证明一个样本可用，不能证明边界安全；因此目录中的机制都要进入正常、边界、失败和恢复四条轨迹，并明确谁检测、谁阻塞或重试、谁逆序清理、谁向上报告。

模拟陷入仿真、二进制翻译和硬件辅助虚拟化，逐页执行影子/嵌套映射并制作可恢复检查点。 运行前把预期写成状态快照，运行后逐字段比较。并发实验记录同步边，地址实验记录位宽与权限，I/O 实验记录缓冲区和完成事件，持久化实验区分提交与落盘，分布式实验记录丢失、重复和重排。

注入不可陷入敏感指令、过量提交、脏页速度高于迁移和虚拟设备状态缺失，定位虚拟化透明性被破坏的位置。 删除故障后用完全相同的输入重放，确认返回值、内核对象、外部结果和清理状态一起恢复。只修正界面结果却留下线程、页、设备请求、文件引用、虚拟机状态或权限污染，仍然不通过。

原书第4版使用 Linux、Android、Windows 8.1、VMware 和当时的多处理机技术作为实例。现代内核和云平台会改变接口版本与性能数值，但进程状态、地址翻译、I/O 分层、资源死锁、虚拟化隔离和保护域仍可验证。现代对照必须写明差异，不能覆盖2017年中文版本的目录身份。

## 正式目录逐项讲解

### 第7章 虚拟化和云

管理程序必须截获敏感操作并保持等价、隔离和资源记账；处理器、内存、设备和时间状态共同决定检查点能否恢复。 对“第7章 虚拟化和云”先在“截获敏感操作”写出输入与预测，再推进到“虚拟处理器状态”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 7.1 历史

从可虚拟化条件、第一类/第二类虚拟机管理程序和高效技术进入内存、I/O、多核虚拟化、云服务、迁移、检查点与 VMware。 对“7.1 历史”先在“虚拟处理器状态”写出输入与预测，再推进到“映射客户内存”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 7.2 虚拟化的必要条件

管理程序必须截获敏感操作并保持等价、隔离和资源记账；处理器、内存、设备和时间状态共同决定检查点能否恢复。 对“7.2 虚拟化的必要条件”先在“映射客户内存”写出输入与预测，再推进到“虚拟化设备”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 7.3 第一类和第二类虚拟机管理程序

管理程序必须截获敏感操作并保持等价、隔离和资源记账；处理器、内存、设备和时间状态共同决定检查点能否恢复。 对“7.3 第一类和第二类虚拟机管理程序”先在“虚拟化设备”写出输入与预测，再推进到“保存检查点”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 7.4 高效虚拟化技术

管理程序必须截获敏感操作并保持等价、隔离和资源记账；处理器、内存、设备和时间状态共同决定检查点能否恢复。 对“7.4 高效虚拟化技术”先在“保存检查点”写出输入与预测，再推进到“迁移并恢复”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 7.4.1 在不支持虚拟化的平台上实现虚拟化

管理程序必须截获敏感操作并保持等价、隔离和资源记账；处理器、内存、设备和时间状态共同决定检查点能否恢复。 对“7.4.1 在不支持虚拟化的平台上实现虚拟化”先在“迁移并恢复”写出输入与预测，再推进到“截获敏感操作”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 7.4.2 虚拟化的开销

管理程序必须截获敏感操作并保持等价、隔离和资源记账；处理器、内存、设备和时间状态共同决定检查点能否恢复。 对“7.4.2 虚拟化的开销”先在“截获敏感操作”写出输入与预测，再推进到“虚拟处理器状态”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 7.5 虚拟机管理程序是正确的微内核吗

管理程序必须截获敏感操作并保持等价、隔离和资源记账；处理器、内存、设备和时间状态共同决定检查点能否恢复。 对“7.5 虚拟机管理程序是正确的微内核吗”先在“虚拟处理器状态”写出输入与预测，再推进到“映射客户内存”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 7.6 内存虚拟化

把虚拟地址拆为可核查字段，依次检查映射、权限、驻留和置换；地址抽象必须同时说明隔离、共享与缺页恢复。 对“7.6 内存虚拟化”先在“映射客户内存”写出输入与预测，再推进到“虚拟化设备”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 7.7 I/O虚拟化

沿请求、控制器、数据搬运、完成通知和持久化逐层追踪；缓冲区生命周期、元数据一致性和错误传播不能省略。 对“7.7 I/O虚拟化”先在“虚拟化设备”写出输入与预测，再推进到“保存检查点”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 7.8 虚拟装置

从可虚拟化条件、第一类/第二类虚拟机管理程序和高效技术进入内存、I/O、多核虚拟化、云服务、迁移、检查点与 VMware。 对“7.8 虚拟装置”先在“保存检查点”写出输入与预测，再推进到“迁移并恢复”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 7.9 多核CPU上的虚拟机

管理程序必须截获敏感操作并保持等价、隔离和资源记账；处理器、内存、设备和时间状态共同决定检查点能否恢复。 对“7.9 多核CPU上的虚拟机”先在“迁移并恢复”写出输入与预测，再推进到“截获敏感操作”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 7.10 授权问题

从可虚拟化条件、第一类/第二类虚拟机管理程序和高效技术进入内存、I/O、多核虚拟化、云服务、迁移、检查点与 VMware。 对“7.10 授权问题”先在“截获敏感操作”写出输入与预测，再推进到“虚拟处理器状态”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 7.11 云

管理程序必须截获敏感操作并保持等价、隔离和资源记账；处理器、内存、设备和时间状态共同决定检查点能否恢复。 对“7.11 云”先在“虚拟处理器状态”写出输入与预测，再推进到“映射客户内存”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 7.11.1 云即服务

管理程序必须截获敏感操作并保持等价、隔离和资源记账；处理器、内存、设备和时间状态共同决定检查点能否恢复。 对“7.11.1 云即服务”先在“映射客户内存”写出输入与预测，再推进到“虚拟化设备”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 7.11.2 虚拟机迁移

管理程序必须截获敏感操作并保持等价、隔离和资源记账；处理器、内存、设备和时间状态共同决定检查点能否恢复。 对“7.11.2 虚拟机迁移”先在“虚拟化设备”写出输入与预测，再推进到“保存检查点”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 7.11.3 检查点

管理程序必须截获敏感操作并保持等价、隔离和资源记账；处理器、内存、设备和时间状态共同决定检查点能否恢复。 对“7.11.3 检查点”先在“保存检查点”写出输入与预测，再推进到“迁移并恢复”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 7.12 案例研究：VMware

管理程序必须截获敏感操作并保持等价、隔离和资源记账；处理器、内存、设备和时间状态共同决定检查点能否恢复。 对“7.12 案例研究：VMware”先在“迁移并恢复”写出输入与预测，再推进到“截获敏感操作”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 7.12.1 VMware的早期历史

管理程序必须截获敏感操作并保持等价、隔离和资源记账；处理器、内存、设备和时间状态共同决定检查点能否恢复。 对“7.12.1 VMware的早期历史”先在“截获敏感操作”写出输入与预测，再推进到“虚拟处理器状态”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 7.12.2 VMware Workstation

管理程序必须截获敏感操作并保持等价、隔离和资源记账；处理器、内存、设备和时间状态共同决定检查点能否恢复。 对“7.12.2 VMware Workstation”先在“虚拟处理器状态”写出输入与预测，再推进到“映射客户内存”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 7.12.3 将虚拟化引入x86的挑战

管理程序必须截获敏感操作并保持等价、隔离和资源记账；处理器、内存、设备和时间状态共同决定检查点能否恢复。 对“7.12.3 将虚拟化引入x86的挑战”先在“映射客户内存”写出输入与预测，再推进到“虚拟化设备”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 7.12.4 VMware Workstation解决方案概览

管理程序必须截获敏感操作并保持等价、隔离和资源记账；处理器、内存、设备和时间状态共同决定检查点能否恢复。 对“7.12.4 VMware Workstation解决方案概览”先在“虚拟化设备”写出输入与预测，再推进到“保存检查点”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 7.12.5 VMware Workstation的演变

管理程序必须截获敏感操作并保持等价、隔离和资源记账；处理器、内存、设备和时间状态共同决定检查点能否恢复。 对“7.12.5 VMware Workstation的演变”先在“保存检查点”写出输入与预测，再推进到“迁移并恢复”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 7.12.6 VMware的第一类虚拟机管理程序ESX Server

管理程序必须截获敏感操作并保持等价、隔离和资源记账；处理器、内存、设备和时间状态共同决定检查点能否恢复。 对“7.12.6 VMware的第一类虚拟机管理程序ESX Server”先在“迁移并恢复”写出输入与预测，再推进到“截获敏感操作”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 7.13 有关虚拟化和云的研究

管理程序必须截获敏感操作并保持等价、隔离和资源记账；处理器、内存、设备和时间状态共同决定检查点能否恢复。 对“7.13 有关虚拟化和云的研究”先在“截获敏感操作”写出输入与预测，再推进到“虚拟处理器状态”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

## 状态与失败矩阵

| 对象         | 正常状态               | 边界样本             | 失败证据           | 恢复条件           |
| ------------ | ---------------------- | -------------------- | ------------------ | ------------------ |
| 进程与同步   | 状态转换和同步边明确   | 抢占、竞争、低容量   | 调度与等待图       | 唤醒并回收全部流   |
| 地址与存储   | 映射、权限、元数据有效 | 缺页、空间耗尽、断电 | 页表与日志轨迹     | 重建一致映射或事务 |
| 设备与虚拟化 | 请求和完成一一对应     | 超时、迁移、过量提交 | 控制器与客户机状态 | 清理后相同输入重放 |
| 分布式与安全 | 所有权、身份、协议明确 | 节点失联、越权、重放 | 消息与审计记录     | 恢复隔离和最小权限 |

## 分步可视实验

<Stepper>
  <Step title="1. 建立资源预测">
    写出截获敏感操作到虚拟处理器状态之间的输入、内核对象、所有者和允许的失败出口。
    <Mos07VirtualizationCloudMapLab />
  </Step>
  <Step title="2. 注入单一边界">
    只改变一个容量、权限、设备或时序条件，在首个偏离处停止。
    <Mos07VirtualizationCloudExperimentLab />
  </Step>
  <Step title="3. 清理并恢复重放">
    删除故障，用同一输入核对状态、结果和资源回收。
    <Mos07VirtualizationCloudEvidenceLab />
  </Step>
</Stepper>

先预测：若在“映射客户内存”注入失败，哪个内核状态应最先变化，后续哪个阶段必须停止提交副作用？写出答案后再操作实验控件。

## 可重放实验记录

```text
trace = []
for stage in ["截获敏感操作", "虚拟处理器状态", "映射客户内存", "虚拟化设备", "保存检查点", "迁移并恢复"]:
  expected = predict(stage, kernel_state)
  actual = execute(stage, kernel_state)
  trace.append({ stage, expected, actual, owner, cleanup })
  stop_at_first_divergence(expected, actual)
```

```yaml
book: 现代操作系统（原书第4版）
edition: 2017-07-19
page: mos-07-virtualization-cloud
formal_scope: 25条
sample: normal | boundary | failure | recovery
predict_before_run: true
capture_first_divergence: true
replay_same_input: true
```

```text
freeze version, input, workload, and environment
record system calls, kernel state, resources, devices, and synchronization
change exactly one condition
stop at the first divergent boundary
clean up in reverse acquisition order
replay the same input and verify recovery
```

## 常见误区与故障注入

<Callout type="trap">
  **误区一：应用结果正确就代表操作系统资源正确。**
  中间仍可能有竞争、脏映射、丢中断、未提交元数据、泄漏对象或越权；必须检查首个内核状态变化和清理结果。
</Callout>

<Callout type="trap">
  **误区二：只学进程、内存和文件就等于复刻第4版。** 原书还包含完整
  I/O、虚拟化与云、多处理机、安全、Linux/Android、Windows
  8、操作系统设计和研究书目，不能用九个泛化主题代替13章。
</Callout>

## 四类样本与验收

| 样本 | 注入方式                     | 必查证据                 | 通过条件     |
| ---- | ---------------------------- | ------------------------ | ------------ |
| 正常 | 固定版本、负载和输入         | 全阶段状态与输出         | 与预测一致   |
| 边界 | 极值、空输入、低容量         | 终止、权限与资源状态     | 不越界不泄漏 |
| 失败 | 错地址、设备超时、断连或竞争 | 首个错误与责任层         | 失败被隔离   |
| 恢复 | 逆序清理后重放               | 状态、输出、持久化和审计 | 回到受控结果 |

## 练习

<Exercises>

**问题 1** 为什么不能只看最终输出验收第 7 章 虚拟化和云？

<Answer>
  最终输出会隐藏调度次序、地址映射、设备队列、持久化边界、资源所有权和权限状态。必须先预测，保存首个偏离点，逆序清理后再以同一输入重放。
</Answer>

**问题 2** 如何把“第7章 虚拟化和云”变成可执行证据？

<Answer>
  依次沿“截获敏感操作、虚拟处理器状态、映射客户内存、虚拟化设备、保存检查点、迁移并恢复”登记输入、内核状态、责任接口、边界、失败出口和恢复条件，并让正常、边界、失败、恢复四类样本都留下可比较快照。
</Answer>

**问题 3** 本页最小通过条件是什么？

<Answer>
  客户机看到与独占机器一致的体系结构状态；管理程序保持隔离和资源记账，迁移或恢复后设备、内存与执行状态一致。
  同时，异常路径不污染下一次运行，所有已获得资源都有唯一逆序清理路径，恢复轨迹重新到达正常样本的状态与结果。
</Answer>

</Exercises>

## 小结

- 虚拟机管理程序：连接“截获敏感操作”的请求与内核状态。
- 敏感指令：连接“虚拟处理器状态”的资源所有权。
- 内存虚拟化：连接“映射客户内存”的同步或权限边界。
- I/O 虚拟化：连接“虚拟化设备”的失败传播。
- 检查点：连接“保存检查点”的恢复证据。
- 25个正式目录坐标已进入本页或全书资源链，不以泛化主题替代原书章节。
- 返回值只是证据之一，内核状态、资源、持久化和清理共同决定是否通过。

## 术语表

<Glossary>
  <GlossaryItem term="虚拟机管理程序">
    虚拟机管理程序对应“截获敏感操作”，验收时连接请求、内核状态、资源边界、失败传播和恢复结果。
  </GlossaryItem>
  <GlossaryItem term="敏感指令">
    敏感指令对应“虚拟处理器状态”，验收时连接请求、内核状态、资源边界、失败传播和恢复结果。
  </GlossaryItem>
  <GlossaryItem term="内存虚拟化">
    内存虚拟化对应“映射客户内存”，验收时连接请求、内核状态、资源边界、失败传播和恢复结果。
  </GlossaryItem>
  <GlossaryItem term="I/O 虚拟化">
    I/O
    虚拟化对应“虚拟化设备”，验收时连接请求、内核状态、资源边界、失败传播和恢复结果。
  </GlossaryItem>
  <GlossaryItem term="检查点">
    检查点对应“保存检查点”，验收时连接请求、内核状态、资源边界、失败传播和恢复结果。
  </GlossaryItem>
</Glossary>

<Attribution
  adaptedFrom="Tanenbaum、Bos · 现代操作系统（原书第4版）· 陈向群、马洪兵等译 · 机械工业出版社 · 2017-07-19 · 出版社完整目录独立教学重构"
  adaptedUrl="https://www.cmpedu.com/books/book/5610493.htm"
/>
