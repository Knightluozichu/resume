---
title: "第 10 章 实例研究 1：UNIX、Linux 和 Android"
type: A
section: "现代操作系统 · Linux/Android 实例"
order: 10
description: "把前九章机制落到 UNIX/Linux 历史、内核结构、进程、内存、I/O、文件系统、安全，以及 Android 架构、Dalvik、Binder 和沙箱。覆盖55个正式目录坐标。"
demo: true
math: false
sourceUrl: "https://www.cmpedu.com/books/book/5610493.htm"
draft: true
---

import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第 10 章 实例研究 1：UNIX、Linux 和 Android的核心机制，并说出它在中文原书第4版13章体系中的位置
- 能描述启动 Linux、创建调度任务、管理虚拟内存、执行 I/O 与文件操作、通过 Binder 通信、隔离 Android 应用六个阶段的输入、内核状态、资源所有者与不变量
- 能实现最小可重放实验，注入边界或失败条件并定位首个偏离点
- 能比较正常、边界、失败和恢复轨迹，判断目录概念是否形成可迁移能力

</Objectives>

## 直觉起点

<Term def="UNIX是第 10 章 实例研究 1：UNIX、Linux 和 Android的第一个观察坐标，连接请求、内核状态与恢复证据。">UNIX</Term>、<Term def="Linux 内核是第 10 章 实例研究 1：UNIX、Linux 和 Android的第二个观察坐标，用于解释资源与责任边界。">Linux 内核</Term>、<Term def="NFS是第 10 章 实例研究 1：UNIX、Linux 和 Android的第三个观察坐标，需要用最小实验验收。">NFS</Term>、<Term def="Binder IPC是第 10 章 实例研究 1：UNIX、Linux 和 Android的第四个观察坐标，边界条件会暴露约束。">Binder IPC</Term>、<Term def="应用沙箱是第 10 章 实例研究 1：UNIX、Linux 和 Android的第五个观察坐标，必须定位首个状态分叉。">应用沙箱</Term>
共同构成本页坐标。把前九章机制落到 UNIX/Linux
历史、内核结构、进程、内存、I/O、文件系统、安全，以及 Android
架构、Dalvik、Binder 和沙箱。
学习时先预测内核下一状态，再运行样本；最终输出正确不能证明调度、地址、设备、持久化与清理都正确。

## 六阶段资源链

### 1. 启动 Linux

把产品中的系统调用、内核对象与权限路径回连通用机制，同时标明平台扩展与版本语境，避免用名称代替因果解释。 对“第10章 实例研究1：UNIX、Linux和Android”先在“启动 Linux”写出输入与预测，再推进到“创建调度任务”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 2. 创建调度任务

把产品中的系统调用、内核对象与权限路径回连通用机制，同时标明平台扩展与版本语境，避免用名称代替因果解释。 对“10.1 UNIX与Linux的历史”先在“创建调度任务”写出输入与预测，再推进到“管理虚拟内存”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 3. 管理虚拟内存

把前九章机制落到 UNIX/Linux 历史、内核结构、进程、内存、I/O、文件系统、安全，以及 Android 架构、Dalvik、Binder 和沙箱。 对“10.1.1 UNICS”先在“管理虚拟内存”写出输入与预测，再推进到“执行 I/O 与文件操作”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 4. 执行 I/O 与文件操作

把产品中的系统调用、内核对象与权限路径回连通用机制，同时标明平台扩展与版本语境，避免用名称代替因果解释。 对“10.1.2 PDP-11 UNIX”先在“执行 I/O 与文件操作”写出输入与预测，再推进到“通过 Binder 通信”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 5. 通过 Binder 通信

把产品中的系统调用、内核对象与权限路径回连通用机制，同时标明平台扩展与版本语境，避免用名称代替因果解释。 对“10.1.3 可移植的UNIX”先在“通过 Binder 通信”写出输入与预测，再推进到“隔离 Android 应用”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 6. 隔离 Android 应用

把产品中的系统调用、内核对象与权限路径回连通用机制，同时标明平台扩展与版本语境，避免用名称代替因果解释。 对“10.1.4 Berkeley UNIX”先在“隔离 Android 应用”写出输入与预测，再推进到“启动 Linux”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

## 核心机制深挖

把前九章机制落到 UNIX/Linux 历史、内核结构、进程、内存、I/O、文件系统、安全，以及 Android 架构、Dalvik、Binder 和沙箱。 本页把每个目录概念放回“启动 Linux → 创建调度任务 → 管理虚拟内存 → 执行 I/O 与文件操作 → 通过 Binder 通信 → 隔离 Android 应用”的资源状态机。输入可能是系统调用、地址、字节流、中断、资源请求或远程消息；状态可能位于进程表、页表、驱动队列、文件元数据、虚拟机或保护域；输出必须同时说明返回值、资源所有权与下一阶段可依赖的承诺。

本页签发的不变量是：**实例中的系统调用、内核对象和权限边界都能回连通用机制；Linux 与 Android 的差异有明确扩展层，不以产品名替代理论。**。不变量在正常路径与错误路径都应成立。一次成功运行只证明一个样本可用，不能证明边界安全；因此目录中的机制都要进入正常、边界、失败和恢复四条轨迹，并明确谁检测、谁阻塞或重试、谁逆序清理、谁向上报告。

追踪 Linux 进程启动、调度、缺页、VFS/NFS 与安全检查，再绘制 Android 应用经 Binder 调用系统服务的身份传播。 运行前把预期写成状态快照，运行后逐字段比较。并发实验记录同步边，地址实验记录位宽与权限，I/O 实验记录缓冲区和完成事件，持久化实验区分提交与落盘，分布式实验记录丢失、重复和重排。

注入模块失败、NFS 断连、Binder 服务死亡、意图错误和沙箱越权，定位通用内核机制与平台扩展的责任边界。 删除故障后用完全相同的输入重放，确认返回值、内核对象、外部结果和清理状态一起恢复。只修正界面结果却留下线程、页、设备请求、文件引用、虚拟机状态或权限污染，仍然不通过。

原书第4版使用 Linux、Android、Windows 8.1、VMware 和当时的多处理机技术作为实例。现代内核和云平台会改变接口版本与性能数值，但进程状态、地址翻译、I/O 分层、资源死锁、虚拟化隔离和保护域仍可验证。现代对照必须写明差异，不能覆盖2017年中文版本的目录身份。

## 正式目录逐项讲解

### 第10章 实例研究1：UNIX、Linux和Android

把产品中的系统调用、内核对象与权限路径回连通用机制，同时标明平台扩展与版本语境，避免用名称代替因果解释。 对“第10章 实例研究1：UNIX、Linux和Android”先在“启动 Linux”写出输入与预测，再推进到“创建调度任务”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 10.1 UNIX与Linux的历史

把产品中的系统调用、内核对象与权限路径回连通用机制，同时标明平台扩展与版本语境，避免用名称代替因果解释。 对“10.1 UNIX与Linux的历史”先在“创建调度任务”写出输入与预测，再推进到“管理虚拟内存”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 10.1.1 UNICS

把前九章机制落到 UNIX/Linux 历史、内核结构、进程、内存、I/O、文件系统、安全，以及 Android 架构、Dalvik、Binder 和沙箱。 对“10.1.1 UNICS”先在“管理虚拟内存”写出输入与预测，再推进到“执行 I/O 与文件操作”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 10.1.2 PDP-11 UNIX

把产品中的系统调用、内核对象与权限路径回连通用机制，同时标明平台扩展与版本语境，避免用名称代替因果解释。 对“10.1.2 PDP-11 UNIX”先在“执行 I/O 与文件操作”写出输入与预测，再推进到“通过 Binder 通信”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 10.1.3 可移植的UNIX

把产品中的系统调用、内核对象与权限路径回连通用机制，同时标明平台扩展与版本语境，避免用名称代替因果解释。 对“10.1.3 可移植的UNIX”先在“通过 Binder 通信”写出输入与预测，再推进到“隔离 Android 应用”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 10.1.4 Berkeley UNIX

把产品中的系统调用、内核对象与权限路径回连通用机制，同时标明平台扩展与版本语境，避免用名称代替因果解释。 对“10.1.4 Berkeley UNIX”先在“隔离 Android 应用”写出输入与预测，再推进到“启动 Linux”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 10.1.5 标准UNIX

把产品中的系统调用、内核对象与权限路径回连通用机制，同时标明平台扩展与版本语境，避免用名称代替因果解释。 对“10.1.5 标准UNIX”先在“启动 Linux”写出输入与预测，再推进到“创建调度任务”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 10.1.6 MINIX

把前九章机制落到 UNIX/Linux 历史、内核结构、进程、内存、I/O、文件系统、安全，以及 Android 架构、Dalvik、Binder 和沙箱。 对“10.1.6 MINIX”先在“创建调度任务”写出输入与预测，再推进到“管理虚拟内存”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 10.1.7 Linux

把产品中的系统调用、内核对象与权限路径回连通用机制，同时标明平台扩展与版本语境，避免用名称代替因果解释。 对“10.1.7 Linux”先在“管理虚拟内存”写出输入与预测，再推进到“执行 I/O 与文件操作”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 10.2 Linux简介

把产品中的系统调用、内核对象与权限路径回连通用机制，同时标明平台扩展与版本语境，避免用名称代替因果解释。 对“10.2 Linux简介”先在“执行 I/O 与文件操作”写出输入与预测，再推进到“通过 Binder 通信”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 10.2.1 Linux的设计目标

把产品中的系统调用、内核对象与权限路径回连通用机制，同时标明平台扩展与版本语境，避免用名称代替因果解释。 对“10.2.1 Linux的设计目标”先在“通过 Binder 通信”写出输入与预测，再推进到“隔离 Android 应用”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 10.2.2 到Linux的接口

把产品中的系统调用、内核对象与权限路径回连通用机制，同时标明平台扩展与版本语境，避免用名称代替因果解释。 对“10.2.2 到Linux的接口”先在“隔离 Android 应用”写出输入与预测，再推进到“启动 Linux”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 10.2.3 shell

把前九章机制落到 UNIX/Linux 历史、内核结构、进程、内存、I/O、文件系统、安全，以及 Android 架构、Dalvik、Binder 和沙箱。 对“10.2.3 shell”先在“启动 Linux”写出输入与预测，再推进到“创建调度任务”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 10.2.4 Linux应用程序

把产品中的系统调用、内核对象与权限路径回连通用机制，同时标明平台扩展与版本语境，避免用名称代替因果解释。 对“10.2.4 Linux应用程序”先在“创建调度任务”写出输入与预测，再推进到“管理虚拟内存”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 10.2.5 内核结构

把前九章机制落到 UNIX/Linux 历史、内核结构、进程、内存、I/O、文件系统、安全，以及 Android 架构、Dalvik、Binder 和沙箱。 对“10.2.5 内核结构”先在“管理虚拟内存”写出输入与预测，再推进到“执行 I/O 与文件操作”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 10.3 Linux中的进程

先标出逻辑流、内核保存状态与共享变量，再用同步边和调度事件解释每次交错；安全、活性、公平和回收分别验收。 对“10.3 Linux中的进程”先在“执行 I/O 与文件操作”写出输入与预测，再推进到“通过 Binder 通信”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 10.3.1 基本概念

把前九章机制落到 UNIX/Linux 历史、内核结构、进程、内存、I/O、文件系统、安全，以及 Android 架构、Dalvik、Binder 和沙箱。 对“10.3.1 基本概念”先在“通过 Binder 通信”写出输入与预测，再推进到“隔离 Android 应用”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 10.3.2 Linux中进程管理相关的系统调用

先标出逻辑流、内核保存状态与共享变量，再用同步边和调度事件解释每次交错；安全、活性、公平和回收分别验收。 对“10.3.2 Linux中进程管理相关的系统调用”先在“隔离 Android 应用”写出输入与预测，再推进到“启动 Linux”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 10.3.3 Linux中进程与线程的实现

先标出逻辑流、内核保存状态与共享变量，再用同步边和调度事件解释每次交错；安全、活性、公平和回收分别验收。 对“10.3.3 Linux中进程与线程的实现”先在“启动 Linux”写出输入与预测，再推进到“创建调度任务”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 10.3.4 Linux中的调度

先标出逻辑流、内核保存状态与共享变量，再用同步边和调度事件解释每次交错；安全、活性、公平和回收分别验收。 对“10.3.4 Linux中的调度”先在“创建调度任务”写出输入与预测，再推进到“管理虚拟内存”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 10.3.5 启动Linux系统

把产品中的系统调用、内核对象与权限路径回连通用机制，同时标明平台扩展与版本语境，避免用名称代替因果解释。 对“10.3.5 启动Linux系统”先在“管理虚拟内存”写出输入与预测，再推进到“执行 I/O 与文件操作”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 10.4 Linux中的内存管理

把虚拟地址拆为可核查字段，依次检查映射、权限、驻留和置换；地址抽象必须同时说明隔离、共享与缺页恢复。 对“10.4 Linux中的内存管理”先在“执行 I/O 与文件操作”写出输入与预测，再推进到“通过 Binder 通信”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 10.4.1 基本概念

把前九章机制落到 UNIX/Linux 历史、内核结构、进程、内存、I/O、文件系统、安全，以及 Android 架构、Dalvik、Binder 和沙箱。 对“10.4.1 基本概念”先在“通过 Binder 通信”写出输入与预测，再推进到“隔离 Android 应用”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 10.4.2 Linux中的内存管理系统调用

把虚拟地址拆为可核查字段，依次检查映射、权限、驻留和置换；地址抽象必须同时说明隔离、共享与缺页恢复。 对“10.4.2 Linux中的内存管理系统调用”先在“隔离 Android 应用”写出输入与预测，再推进到“启动 Linux”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 10.4.3 Linux中内存管理的实现

把虚拟地址拆为可核查字段，依次检查映射、权限、驻留和置换；地址抽象必须同时说明隔离、共享与缺页恢复。 对“10.4.3 Linux中内存管理的实现”先在“启动 Linux”写出输入与预测，再推进到“创建调度任务”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 10.4.4 Linux中的分页

把虚拟地址拆为可核查字段，依次检查映射、权限、驻留和置换；地址抽象必须同时说明隔离、共享与缺页恢复。 对“10.4.4 Linux中的分页”先在“创建调度任务”写出输入与预测，再推进到“管理虚拟内存”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 10.5 Linux中的I/O系统

沿请求、控制器、数据搬运、完成通知和持久化逐层追踪；缓冲区生命周期、元数据一致性和错误传播不能省略。 对“10.5 Linux中的I/O系统”先在“管理虚拟内存”写出输入与预测，再推进到“执行 I/O 与文件操作”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 10.5.1 基本概念

把前九章机制落到 UNIX/Linux 历史、内核结构、进程、内存、I/O、文件系统、安全，以及 Android 架构、Dalvik、Binder 和沙箱。 对“10.5.1 基本概念”先在“执行 I/O 与文件操作”写出输入与预测，再推进到“通过 Binder 通信”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 10.5.2 网络

把前九章机制落到 UNIX/Linux 历史、内核结构、进程、内存、I/O、文件系统、安全，以及 Android 架构、Dalvik、Binder 和沙箱。 对“10.5.2 网络”先在“通过 Binder 通信”写出输入与预测，再推进到“隔离 Android 应用”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 10.5.3 Linux中的I/O系统调用

沿请求、控制器、数据搬运、完成通知和持久化逐层追踪；缓冲区生命周期、元数据一致性和错误传播不能省略。 对“10.5.3 Linux中的I/O系统调用”先在“隔离 Android 应用”写出输入与预测，再推进到“启动 Linux”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 10.5.4 I/O在Linux中的实现

沿请求、控制器、数据搬运、完成通知和持久化逐层追踪；缓冲区生命周期、元数据一致性和错误传播不能省略。 对“10.5.4 I/O在Linux中的实现”先在“启动 Linux”写出输入与预测，再推进到“创建调度任务”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 10.5.5 Linux中的模块

把产品中的系统调用、内核对象与权限路径回连通用机制，同时标明平台扩展与版本语境，避免用名称代替因果解释。 对“10.5.5 Linux中的模块”先在“创建调度任务”写出输入与预测，再推进到“管理虚拟内存”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 10.6 Linux文件系统

沿请求、控制器、数据搬运、完成通知和持久化逐层追踪；缓冲区生命周期、元数据一致性和错误传播不能省略。 对“10.6 Linux文件系统”先在“管理虚拟内存”写出输入与预测，再推进到“执行 I/O 与文件操作”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 10.6.1 基本概念

把前九章机制落到 UNIX/Linux 历史、内核结构、进程、内存、I/O、文件系统、安全，以及 Android 架构、Dalvik、Binder 和沙箱。 对“10.6.1 基本概念”先在“执行 I/O 与文件操作”写出输入与预测，再推进到“通过 Binder 通信”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 10.6.2 Linux中的文件系统调用

沿请求、控制器、数据搬运、完成通知和持久化逐层追踪；缓冲区生命周期、元数据一致性和错误传播不能省略。 对“10.6.2 Linux中的文件系统调用”先在“通过 Binder 通信”写出输入与预测，再推进到“隔离 Android 应用”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 10.6.3 Linux文件系统的实现

沿请求、控制器、数据搬运、完成通知和持久化逐层追踪；缓冲区生命周期、元数据一致性和错误传播不能省略。 对“10.6.3 Linux文件系统的实现”先在“隔离 Android 应用”写出输入与预测，再推进到“启动 Linux”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 10.6.4 NFS：网络文件系统

沿请求、控制器、数据搬运、完成通知和持久化逐层追踪；缓冲区生命周期、元数据一致性和错误传播不能省略。 对“10.6.4 NFS：网络文件系统”先在“启动 Linux”写出输入与预测，再推进到“创建调度任务”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 10.7 Linux的安全性

从威胁主体、信任边界和保护域开始，分别验证认证、授权、完整性与审计；单一防线不能代替最小权限和纵深防御。 对“10.7 Linux的安全性”先在“创建调度任务”写出输入与预测，再推进到“管理虚拟内存”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 10.7.1 基本概念

把前九章机制落到 UNIX/Linux 历史、内核结构、进程、内存、I/O、文件系统、安全，以及 Android 架构、Dalvik、Binder 和沙箱。 对“10.7.1 基本概念”先在“管理虚拟内存”写出输入与预测，再推进到“执行 I/O 与文件操作”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 10.7.2 Linux中安全相关的系统调用

从威胁主体、信任边界和保护域开始，分别验证认证、授权、完整性与审计；单一防线不能代替最小权限和纵深防御。 对“10.7.2 Linux中安全相关的系统调用”先在“执行 I/O 与文件操作”写出输入与预测，再推进到“通过 Binder 通信”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 10.7.3 Linux中的安全实现

从威胁主体、信任边界和保护域开始，分别验证认证、授权、完整性与审计；单一防线不能代替最小权限和纵深防御。 对“10.7.3 Linux中的安全实现”先在“通过 Binder 通信”写出输入与预测，再推进到“隔离 Android 应用”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 10.8 Android

把产品中的系统调用、内核对象与权限路径回连通用机制，同时标明平台扩展与版本语境，避免用名称代替因果解释。 对“10.8 Android”先在“隔离 Android 应用”写出输入与预测，再推进到“启动 Linux”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 10.8.1 Android与Google

把产品中的系统调用、内核对象与权限路径回连通用机制，同时标明平台扩展与版本语境，避免用名称代替因果解释。 对“10.8.1 Android与Google”先在“启动 Linux”写出输入与预测，再推进到“创建调度任务”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 10.8.2 Android的历史

把产品中的系统调用、内核对象与权限路径回连通用机制，同时标明平台扩展与版本语境，避免用名称代替因果解释。 对“10.8.2 Android的历史”先在“创建调度任务”写出输入与预测，再推进到“管理虚拟内存”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 10.8.3 设计目标

把目标、接口、结构、权衡和证据分开：设计结论需要可诊断接口、可替换策略、稳定测量和可追溯研究来源。 对“10.8.3 设计目标”先在“管理虚拟内存”写出输入与预测，再推进到“执行 I/O 与文件操作”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 10.8.4 Android体系结构

把产品中的系统调用、内核对象与权限路径回连通用机制，同时标明平台扩展与版本语境，避免用名称代替因果解释。 对“10.8.4 Android体系结构”先在“执行 I/O 与文件操作”写出输入与预测，再推进到“通过 Binder 通信”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 10.8.5 Linux扩展

把产品中的系统调用、内核对象与权限路径回连通用机制，同时标明平台扩展与版本语境，避免用名称代替因果解释。 对“10.8.5 Linux扩展”先在“通过 Binder 通信”写出输入与预测，再推进到“隔离 Android 应用”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 10.8.6 Dalvik

把前九章机制落到 UNIX/Linux 历史、内核结构、进程、内存、I/O、文件系统、安全，以及 Android 架构、Dalvik、Binder 和沙箱。 对“10.8.6 Dalvik”先在“隔离 Android 应用”写出输入与预测，再推进到“启动 Linux”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 10.8.7 Binder IPC

把产品中的系统调用、内核对象与权限路径回连通用机制，同时标明平台扩展与版本语境，避免用名称代替因果解释。 对“10.8.7 Binder IPC”先在“启动 Linux”写出输入与预测，再推进到“创建调度任务”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 10.8.8 Android应用

把产品中的系统调用、内核对象与权限路径回连通用机制，同时标明平台扩展与版本语境，避免用名称代替因果解释。 对“10.8.8 Android应用”先在“创建调度任务”写出输入与预测，再推进到“管理虚拟内存”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 10.8.9 意图

把前九章机制落到 UNIX/Linux 历史、内核结构、进程、内存、I/O、文件系统、安全，以及 Android 架构、Dalvik、Binder 和沙箱。 对“10.8.9 意图”先在“管理虚拟内存”写出输入与预测，再推进到“执行 I/O 与文件操作”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 10.8.10 应用程序沙箱

把前九章机制落到 UNIX/Linux 历史、内核结构、进程、内存、I/O、文件系统、安全，以及 Android 架构、Dalvik、Binder 和沙箱。 对“10.8.10 应用程序沙箱”先在“执行 I/O 与文件操作”写出输入与预测，再推进到“通过 Binder 通信”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 10.8.11 安全性

从威胁主体、信任边界和保护域开始，分别验证认证、授权、完整性与审计；单一防线不能代替最小权限和纵深防御。 对“10.8.11 安全性”先在“通过 Binder 通信”写出输入与预测，再推进到“隔离 Android 应用”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 10.8.12 进程模型

先标出逻辑流、内核保存状态与共享变量，再用同步边和调度事件解释每次交错；安全、活性、公平和回收分别验收。 对“10.8.12 进程模型”先在“隔离 Android 应用”写出输入与预测，再推进到“启动 Linux”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 10.9 小结

本节用“实例中的系统调用、内核对象和权限边界都能回连通用机制；Linux 与 Android 的差异有明确扩展层，不以产品名替代理论。”回查正常、边界、失败和恢复四条轨迹，而不是只重复术语。 对“10.9 小结”先在“启动 Linux”写出输入与预测，再推进到“创建调度任务”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

## 状态与失败矩阵

| 对象         | 正常状态               | 边界样本             | 失败证据           | 恢复条件           |
| ------------ | ---------------------- | -------------------- | ------------------ | ------------------ |
| 进程与同步   | 状态转换和同步边明确   | 抢占、竞争、低容量   | 调度与等待图       | 唤醒并回收全部流   |
| 地址与存储   | 映射、权限、元数据有效 | 缺页、空间耗尽、断电 | 页表与日志轨迹     | 重建一致映射或事务 |
| 设备与虚拟化 | 请求和完成一一对应     | 超时、迁移、过量提交 | 控制器与客户机状态 | 清理后相同输入重放 |
| 分布式与安全 | 所有权、身份、协议明确 | 节点失联、越权、重放 | 消息与审计记录     | 恢复隔离和最小权限 |

先预测：若在“管理虚拟内存”注入失败，哪个内核状态应最先变化，后续哪个阶段必须停止提交副作用？写出答案后再操作实验控件。

## 可重放实验记录

```text
trace = []
for stage in ["启动 Linux", "创建调度任务", "管理虚拟内存", "执行 I/O 与文件操作", "通过 Binder 通信", "隔离 Android 应用"]:
  expected = predict(stage, kernel_state)
  actual = execute(stage, kernel_state)
  trace.append({ stage, expected, actual, owner, cleanup })
  stop_at_first_divergence(expected, actual)
```

```yaml
book: 现代操作系统（原书第4版）
edition: 2017-07-19
page: mos-10-unix-linux-android
formal_scope: 55条
sample: normal | boundary | failure | recovery
predict_before_run: true
capture_first_divergence: true
replay_same_input: true
```

```text
freeze version, input, workload, and environment
record system calls, kernel state, resources, devices, and synchronization
change exactly one condition
stop at the first divergent boundary
clean up in reverse acquisition order
replay the same input and verify recovery
```

## 常见误区与故障注入

<Callout type="trap">
  **误区一：应用结果正确就代表操作系统资源正确。**
  中间仍可能有竞争、脏映射、丢中断、未提交元数据、泄漏对象或越权；必须检查首个内核状态变化和清理结果。
</Callout>

<Callout type="trap">
  **误区二：只学进程、内存和文件就等于复刻第4版。** 原书还包含完整
  I/O、虚拟化与云、多处理机、安全、Linux/Android、Windows
  8、操作系统设计和研究书目，不能用九个泛化主题代替13章。
</Callout>

## 四类样本与验收

| 样本 | 注入方式                     | 必查证据                 | 通过条件     |
| ---- | ---------------------------- | ------------------------ | ------------ |
| 正常 | 固定版本、负载和输入         | 全阶段状态与输出         | 与预测一致   |
| 边界 | 极值、空输入、低容量         | 终止、权限与资源状态     | 不越界不泄漏 |
| 失败 | 错地址、设备超时、断连或竞争 | 首个错误与责任层         | 失败被隔离   |
| 恢复 | 逆序清理后重放               | 状态、输出、持久化和审计 | 回到受控结果 |

## 练习

<Exercises>

**问题 1** 为什么不能只看最终输出验收第 10 章 实例研究 1：UNIX、Linux 和 Android？

<Answer>
  最终输出会隐藏调度次序、地址映射、设备队列、持久化边界、资源所有权和权限状态。必须先预测，保存首个偏离点，逆序清理后再以同一输入重放。
</Answer>

**问题 2** 如何把“第10章 实例研究1：UNIX、Linux和Android”变成可执行证据？

<Answer>
  依次沿“启动 Linux、创建调度任务、管理虚拟内存、执行 I/O 与文件操作、通过
  Binder 通信、隔离 Android
  应用”登记输入、内核状态、责任接口、边界、失败出口和恢复条件，并让正常、边界、失败、恢复四类样本都留下可比较快照。
</Answer>

**问题 3** 本页最小通过条件是什么？

<Answer>
  实例中的系统调用、内核对象和权限边界都能回连通用机制；Linux 与 Android
  的差异有明确扩展层，不以产品名替代理论。
  同时，异常路径不污染下一次运行，所有已获得资源都有唯一逆序清理路径，恢复轨迹重新到达正常样本的状态与结果。
</Answer>

</Exercises>

## 小结

- UNIX：连接“启动 Linux”的请求与内核状态。
- Linux 内核：连接“创建调度任务”的资源所有权。
- NFS：连接“管理虚拟内存”的同步或权限边界。
- Binder IPC：连接“执行 I/O 与文件操作”的失败传播。
- 应用沙箱：连接“通过 Binder 通信”的恢复证据。
- 55个正式目录坐标已进入本页或全书资源链，不以泛化主题替代原书章节。
- 返回值只是证据之一，内核状态、资源、持久化和清理共同决定是否通过。

## 术语表

<Glossary>
  <GlossaryItem term="UNIX">
    UNIX对应“启动
    Linux”，验收时连接请求、内核状态、资源边界、失败传播和恢复结果。
  </GlossaryItem>
  <GlossaryItem term="Linux 内核">
    Linux
    内核对应“创建调度任务”，验收时连接请求、内核状态、资源边界、失败传播和恢复结果。
  </GlossaryItem>
  <GlossaryItem term="NFS">
    NFS对应“管理虚拟内存”，验收时连接请求、内核状态、资源边界、失败传播和恢复结果。
  </GlossaryItem>
  <GlossaryItem term="Binder IPC">
    Binder IPC对应“执行 I/O
    与文件操作”，验收时连接请求、内核状态、资源边界、失败传播和恢复结果。
  </GlossaryItem>
  <GlossaryItem term="应用沙箱">
    应用沙箱对应“通过 Binder
    通信”，验收时连接请求、内核状态、资源边界、失败传播和恢复结果。
  </GlossaryItem>
</Glossary>

<Attribution
  adaptedFrom="Tanenbaum、Bos · 现代操作系统（原书第4版）· 陈向群、马洪兵等译 · 机械工业出版社 · 2017-07-19 · 出版社完整目录独立教学重构"
  adaptedUrl="https://www.cmpedu.com/books/book/5610493.htm"
/>
