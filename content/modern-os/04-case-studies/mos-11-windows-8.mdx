---
title: "第 11 章 实例研究 2：Windows 8"
type: A
section: "现代操作系统 · Windows 8 实例"
order: 11
description: "从 Windows 历史与编程接口进入 NT 结构、对象管理、用户态服务、进程线程、内存、缓存、I/O、NTFS、电源和安全。覆盖39个正式目录坐标。"
demo: true
math: false
sourceUrl: "https://www.cmpedu.com/books/book/5610493.htm"
draft: false
---


## 直觉起点

、、、、共同构成本页坐标。从 Windows 历史与编程接口进入 NT
结构、对象管理、用户态服务、进程线程、内存、缓存、I/O、NTFS、电源和安全。
学习时先预测内核下一状态，再运行样本；最终输出正确不能证明调度、地址、设备、持久化与清理都正确。

## 六阶段资源链

### 1. 启动 Windows

把产品中的系统调用、内核对象与权限路径回连通用机制，同时标明平台扩展与版本语境，避免用名称代替因果解释。 对“第11章 实例研究2：Windows 8”先在“启动 Windows”写出输入与预测，再推进到“进入 NT/Win32 接口”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 2. 进入 NT/Win32 接口

把产品中的系统调用、内核对象与权限路径回连通用机制，同时标明平台扩展与版本语境，避免用名称代替因果解释。 对“11.1 Windows 8.1的历史”先在“进入 NT/Win32 接口”写出输入与预测，再推进到“创建内核对象”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 3. 创建内核对象

从 Windows 历史与编程接口进入 NT 结构、对象管理、用户态服务、进程线程、内存、缓存、I/O、NTFS、电源和安全。 对“11.1.1 20世纪80年代：MS-DOS”先在“创建内核对象”写出输入与预测，再推进到“调度进程线程”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 4. 调度进程线程

把产品中的系统调用、内核对象与权限路径回连通用机制，同时标明平台扩展与版本语境，避免用名称代替因果解释。 对“11.1.2 20世纪90年代：基于MS-DOS的Windows”先在“调度进程线程”写出输入与预测，再推进到“访问缓存与 NTFS”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 5. 访问缓存与 NTFS

把产品中的系统调用、内核对象与权限路径回连通用机制，同时标明平台扩展与版本语境，避免用名称代替因果解释。 对“11.1.3 21世纪00年代：基于NT的Windows”先在“访问缓存与 NTFS”写出输入与预测，再推进到“执行安全检查”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 6. 执行安全检查

把产品中的系统调用、内核对象与权限路径回连通用机制，同时标明平台扩展与版本语境，避免用名称代替因果解释。 对“11.1.4 Windows Vista”先在“执行安全检查”写出输入与预测，再推进到“启动 Windows”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

## 核心机制深挖

从 Windows 历史与编程接口进入 NT 结构、对象管理、用户态服务、进程线程、内存、缓存、I/O、NTFS、电源和安全。 本页把每个目录概念放回“启动 Windows → 进入 NT/Win32 接口 → 创建内核对象 → 调度进程线程 → 访问缓存与 NTFS → 执行安全检查”的资源状态机。输入可能是系统调用、地址、字节流、中断、资源请求或远程消息；状态可能位于进程表、页表、驱动队列、文件元数据、虚拟机或保护域；输出必须同时说明返回值、资源所有权与下一阶段可依赖的承诺。

本页签发的不变量是：**句柄经对象管理器解析到受引用计数和安全描述符保护的对象；I/O、缓存与 NTFS 的完成和持久化边界明确。**。不变量在正常路径与错误路径都应成立。一次成功运行只证明一个样本可用，不能证明边界安全；因此目录中的机制都要进入正常、边界、失败和恢复四条轨迹，并明确谁检测、谁阻塞或重试、谁逆序清理、谁向上报告。

跟踪 Win32 调用进入原生 NT 接口和对象管理器，观察作业/进程/线程、内存节、I/O 请求包与 NTFS 元数据。 运行前把预期写成状态快照，运行后逐字段比较。并发实验记录同步边，地址实验记录位宽与权限，I/O 实验记录缓冲区和完成事件，持久化实验区分提交与落盘，分布式实验记录丢失、重复和重排。

注入无效句柄、对象权限不足、缓存与磁盘不一致、NTFS 日志恢复和安全缓解触发，保留首个 NT 状态码。 删除故障后用完全相同的输入重放，确认返回值、内核对象、外部结果和清理状态一起恢复。只修正界面结果却留下线程、页、设备请求、文件引用、虚拟机状态或权限污染，仍然不通过。

原书第4版使用 Linux、Android、Windows 8.1、VMware 和当时的多处理机技术作为实例。现代内核和云平台会改变接口版本与性能数值，但进程状态、地址翻译、I/O 分层、资源死锁、虚拟化隔离和保护域仍可验证。现代对照必须写明差异，不能覆盖2017年中文版本的目录身份。

## 正式目录逐项讲解

### 第11章 实例研究2：Windows 8

把产品中的系统调用、内核对象与权限路径回连通用机制，同时标明平台扩展与版本语境，避免用名称代替因果解释。 对“第11章 实例研究2：Windows 8”先在“启动 Windows”写出输入与预测，再推进到“进入 NT/Win32 接口”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 11.1 Windows 8.1的历史

把产品中的系统调用、内核对象与权限路径回连通用机制，同时标明平台扩展与版本语境，避免用名称代替因果解释。 对“11.1 Windows 8.1的历史”先在“进入 NT/Win32 接口”写出输入与预测，再推进到“创建内核对象”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 11.1.1 20世纪80年代：MS-DOS

从 Windows 历史与编程接口进入 NT 结构、对象管理、用户态服务、进程线程、内存、缓存、I/O、NTFS、电源和安全。 对“11.1.1 20世纪80年代：MS-DOS”先在“创建内核对象”写出输入与预测，再推进到“调度进程线程”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 11.1.2 20世纪90年代：基于MS-DOS的Windows

把产品中的系统调用、内核对象与权限路径回连通用机制，同时标明平台扩展与版本语境，避免用名称代替因果解释。 对“11.1.2 20世纪90年代：基于MS-DOS的Windows”先在“调度进程线程”写出输入与预测，再推进到“访问缓存与 NTFS”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 11.1.3 21世纪00年代：基于NT的Windows

把产品中的系统调用、内核对象与权限路径回连通用机制，同时标明平台扩展与版本语境，避免用名称代替因果解释。 对“11.1.3 21世纪00年代：基于NT的Windows”先在“访问缓存与 NTFS”写出输入与预测，再推进到“执行安全检查”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 11.1.4 Windows Vista

把产品中的系统调用、内核对象与权限路径回连通用机制，同时标明平台扩展与版本语境，避免用名称代替因果解释。 对“11.1.4 Windows Vista”先在“执行安全检查”写出输入与预测，再推进到“启动 Windows”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 11.1.5 21世纪10年代：现代Windows

把产品中的系统调用、内核对象与权限路径回连通用机制，同时标明平台扩展与版本语境，避免用名称代替因果解释。 对“11.1.5 21世纪10年代：现代Windows”先在“启动 Windows”写出输入与预测，再推进到“进入 NT/Win32 接口”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 11.2 Windows 编程

把产品中的系统调用、内核对象与权限路径回连通用机制，同时标明平台扩展与版本语境，避免用名称代替因果解释。 对“11.2 Windows 编程”先在“进入 NT/Win32 接口”写出输入与预测，再推进到“创建内核对象”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 11.2.1 原生NT应用编程接口

把目标、接口、结构、权衡和证据分开：设计结论需要可诊断接口、可替换策略、稳定测量和可追溯研究来源。 对“11.2.1 原生NT应用编程接口”先在“创建内核对象”写出输入与预测，再推进到“调度进程线程”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 11.2.2 Win32应用编程接口

把产品中的系统调用、内核对象与权限路径回连通用机制，同时标明平台扩展与版本语境，避免用名称代替因果解释。 对“11.2.2 Win32应用编程接口”先在“调度进程线程”写出输入与预测，再推进到“访问缓存与 NTFS”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 11.2.3 Windows注册表

把产品中的系统调用、内核对象与权限路径回连通用机制，同时标明平台扩展与版本语境，避免用名称代替因果解释。 对“11.2.3 Windows注册表”先在“访问缓存与 NTFS”写出输入与预测，再推进到“执行安全检查”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 11.3 系统结构

从 Windows 历史与编程接口进入 NT 结构、对象管理、用户态服务、进程线程、内存、缓存、I/O、NTFS、电源和安全。 对“11.3 系统结构”先在“执行安全检查”写出输入与预测，再推进到“启动 Windows”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 11.3.1 操作系统结构

从 Windows 历史与编程接口进入 NT 结构、对象管理、用户态服务、进程线程、内存、缓存、I/O、NTFS、电源和安全。 对“11.3.1 操作系统结构”先在“启动 Windows”写出输入与预测，再推进到“进入 NT/Win32 接口”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 11.3.2 启动Windows

把产品中的系统调用、内核对象与权限路径回连通用机制，同时标明平台扩展与版本语境，避免用名称代替因果解释。 对“11.3.2 启动Windows”先在“进入 NT/Win32 接口”写出输入与预测，再推进到“创建内核对象”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 11.3.3 对象管理器的实现

从 Windows 历史与编程接口进入 NT 结构、对象管理、用户态服务、进程线程、内存、缓存、I/O、NTFS、电源和安全。 对“11.3.3 对象管理器的实现”先在“创建内核对象”写出输入与预测，再推进到“调度进程线程”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 11.3.4 子系统、DLL和用户态服务

从 Windows 历史与编程接口进入 NT 结构、对象管理、用户态服务、进程线程、内存、缓存、I/O、NTFS、电源和安全。 对“11.3.4 子系统、DLL和用户态服务”先在“调度进程线程”写出输入与预测，再推进到“访问缓存与 NTFS”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 11.4 Windows中的进程和线程

先标出逻辑流、内核保存状态与共享变量，再用同步边和调度事件解释每次交错；安全、活性、公平和回收分别验收。 对“11.4 Windows中的进程和线程”先在“访问缓存与 NTFS”写出输入与预测，再推进到“执行安全检查”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 11.4.1 基本概念

从 Windows 历史与编程接口进入 NT 结构、对象管理、用户态服务、进程线程、内存、缓存、I/O、NTFS、电源和安全。 对“11.4.1 基本概念”先在“执行安全检查”写出输入与预测，再推进到“启动 Windows”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 11.4.2 作业、进程、线程和纤程管理API调用

先标出逻辑流、内核保存状态与共享变量，再用同步边和调度事件解释每次交错；安全、活性、公平和回收分别验收。 对“11.4.2 作业、进程、线程和纤程管理API调用”先在“启动 Windows”写出输入与预测，再推进到“进入 NT/Win32 接口”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 11.4.3 进程和线程的实现

先标出逻辑流、内核保存状态与共享变量，再用同步边和调度事件解释每次交错；安全、活性、公平和回收分别验收。 对“11.4.3 进程和线程的实现”先在“进入 NT/Win32 接口”写出输入与预测，再推进到“创建内核对象”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 11.5 内存管理

把虚拟地址拆为可核查字段，依次检查映射、权限、驻留和置换；地址抽象必须同时说明隔离、共享与缺页恢复。 对“11.5 内存管理”先在“创建内核对象”写出输入与预测，再推进到“调度进程线程”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 11.5.1 基本概念

从 Windows 历史与编程接口进入 NT 结构、对象管理、用户态服务、进程线程、内存、缓存、I/O、NTFS、电源和安全。 对“11.5.1 基本概念”先在“调度进程线程”写出输入与预测，再推进到“访问缓存与 NTFS”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 11.5.2 内存管理系统调用

把虚拟地址拆为可核查字段，依次检查映射、权限、驻留和置换；地址抽象必须同时说明隔离、共享与缺页恢复。 对“11.5.2 内存管理系统调用”先在“访问缓存与 NTFS”写出输入与预测，再推进到“执行安全检查”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 11.5.3 存储管理的实现

从 Windows 历史与编程接口进入 NT 结构、对象管理、用户态服务、进程线程、内存、缓存、I/O、NTFS、电源和安全。 对“11.5.3 存储管理的实现”先在“执行安全检查”写出输入与预测，再推进到“启动 Windows”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 11.6 Windows的高速缓存

把产品中的系统调用、内核对象与权限路径回连通用机制，同时标明平台扩展与版本语境，避免用名称代替因果解释。 对“11.6 Windows的高速缓存”先在“启动 Windows”写出输入与预测，再推进到“进入 NT/Win32 接口”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 11.7 Windows的I/O

沿请求、控制器、数据搬运、完成通知和持久化逐层追踪；缓冲区生命周期、元数据一致性和错误传播不能省略。 对“11.7 Windows的I/O”先在“进入 NT/Win32 接口”写出输入与预测，再推进到“创建内核对象”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 11.7.1 基本概念

从 Windows 历史与编程接口进入 NT 结构、对象管理、用户态服务、进程线程、内存、缓存、I/O、NTFS、电源和安全。 对“11.7.1 基本概念”先在“创建内核对象”写出输入与预测，再推进到“调度进程线程”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 11.7.2 I/O的API调用

沿请求、控制器、数据搬运、完成通知和持久化逐层追踪；缓冲区生命周期、元数据一致性和错误传播不能省略。 对“11.7.2 I/O的API调用”先在“调度进程线程”写出输入与预测，再推进到“访问缓存与 NTFS”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 11.7.3 I/O实现

沿请求、控制器、数据搬运、完成通知和持久化逐层追踪；缓冲区生命周期、元数据一致性和错误传播不能省略。 对“11.7.3 I/O实现”先在“访问缓存与 NTFS”写出输入与预测，再推进到“执行安全检查”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 11.8 Windows NT文件系统

沿请求、控制器、数据搬运、完成通知和持久化逐层追踪；缓冲区生命周期、元数据一致性和错误传播不能省略。 对“11.8 Windows NT文件系统”先在“执行安全检查”写出输入与预测，再推进到“启动 Windows”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 11.8.1 基本概念

从 Windows 历史与编程接口进入 NT 结构、对象管理、用户态服务、进程线程、内存、缓存、I/O、NTFS、电源和安全。 对“11.8.1 基本概念”先在“启动 Windows”写出输入与预测，再推进到“进入 NT/Win32 接口”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 11.8.2 NTFS文件系统的实现

沿请求、控制器、数据搬运、完成通知和持久化逐层追踪；缓冲区生命周期、元数据一致性和错误传播不能省略。 对“11.8.2 NTFS文件系统的实现”先在“进入 NT/Win32 接口”写出输入与预测，再推进到“创建内核对象”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 11.9 Windows电源管理

把产品中的系统调用、内核对象与权限路径回连通用机制，同时标明平台扩展与版本语境，避免用名称代替因果解释。 对“11.9 Windows电源管理”先在“创建内核对象”写出输入与预测，再推进到“调度进程线程”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 11.10 Windows 8中的安全

从威胁主体、信任边界和保护域开始，分别验证认证、授权、完整性与审计；单一防线不能代替最小权限和纵深防御。 对“11.10 Windows 8中的安全”先在“调度进程线程”写出输入与预测，再推进到“访问缓存与 NTFS”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 11.10.1 基本概念

从 Windows 历史与编程接口进入 NT 结构、对象管理、用户态服务、进程线程、内存、缓存、I/O、NTFS、电源和安全。 对“11.10.1 基本概念”先在“访问缓存与 NTFS”写出输入与预测，再推进到“执行安全检查”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 11.10.2 安全相关的API调用

从威胁主体、信任边界和保护域开始，分别验证认证、授权、完整性与审计；单一防线不能代替最小权限和纵深防御。 对“11.10.2 安全相关的API调用”先在“执行安全检查”写出输入与预测，再推进到“启动 Windows”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 11.10.3 安全实现

从威胁主体、信任边界和保护域开始，分别验证认证、授权、完整性与审计；单一防线不能代替最小权限和纵深防御。 对“11.10.3 安全实现”先在“启动 Windows”写出输入与预测，再推进到“进入 NT/Win32 接口”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 11.10.4 安全缓解技术

从威胁主体、信任边界和保护域开始，分别验证认证、授权、完整性与审计；单一防线不能代替最小权限和纵深防御。 对“11.10.4 安全缓解技术”先在“进入 NT/Win32 接口”写出输入与预测，再推进到“创建内核对象”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 11.11 小结

本节用“句柄经对象管理器解析到受引用计数和安全描述符保护的对象；I/O、缓存与 NTFS 的完成和持久化边界明确。”回查正常、边界、失败和恢复四条轨迹，而不是只重复术语。 对“11.11 小结”先在“创建内核对象”写出输入与预测，再推进到“调度进程线程”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

## 状态与失败矩阵

| 对象         | 正常状态               | 边界样本             | 失败证据           | 恢复条件           |
| ------------ | ---------------------- | -------------------- | ------------------ | ------------------ |
| 进程与同步   | 状态转换和同步边明确   | 抢占、竞争、低容量   | 调度与等待图       | 唤醒并回收全部流   |
| 地址与存储   | 映射、权限、元数据有效 | 缺页、空间耗尽、断电 | 页表与日志轨迹     | 重建一致映射或事务 |
| 设备与虚拟化 | 请求和完成一一对应     | 超时、迁移、过量提交 | 控制器与客户机状态 | 清理后相同输入重放 |
| 分布式与安全 | 所有权、身份、协议明确 | 节点失联、越权、重放 | 消息与审计记录     | 恢复隔离和最小权限 |

先预测：若在“创建内核对象”注入失败，哪个内核状态应最先变化，后续哪个阶段必须停止提交副作用？写出答案后再操作实验控件。

## 可重放实验记录

```text
trace = []
for stage in ["启动 Windows", "进入 NT/Win32 接口", "创建内核对象", "调度进程线程", "访问缓存与 NTFS", "执行安全检查"]:
  expected = predict(stage, kernel_state)
  actual = execute(stage, kernel_state)
  trace.append({ stage, expected, actual, owner, cleanup })
  stop_at_first_divergence(expected, actual)
```

```yaml
book: 现代操作系统（原书第4版）
edition: 2017-07-19
page: mos-11-windows-8
formal_scope: 39条
sample: normal | boundary | failure | recovery
predict_before_run: true
capture_first_divergence: true
replay_same_input: true
```

```text
freeze version, input, workload, and environment
record system calls, kernel state, resources, devices, and synchronization
change exactly one condition
stop at the first divergent boundary
clean up in reverse acquisition order
replay the same input and verify recovery
```

## 常见误区与故障注入


## 四类样本与验收

| 样本 | 注入方式                     | 必查证据                 | 通过条件     |
| ---- | ---------------------------- | ------------------------ | ------------ |
| 正常 | 固定版本、负载和输入         | 全阶段状态与输出         | 与预测一致   |
| 边界 | 极值、空输入、低容量         | 终止、权限与资源状态     | 不越界不泄漏 |
| 失败 | 错地址、设备超时、断连或竞争 | 首个错误与责任层         | 失败被隔离   |
| 恢复 | 逆序清理后重放               | 状态、输出、持久化和审计 | 回到受控结果 |

## 练习


## 小结

- NT 内核：连接“启动 Windows”的请求与内核状态。
- 对象管理器：连接“进入 NT/Win32 接口”的资源所有权。
- 作业对象：连接“创建内核对象”的同步或权限边界。
- NTFS：连接“调度进程线程”的失败传播。
- 安全缓解：连接“访问缓存与 NTFS”的恢复证据。
- 39个正式目录坐标已进入本页或全书资源链，不以泛化主题替代原书章节。
- 返回值只是证据之一，内核状态、资源、持久化和清理共同决定是否通过。

## 术语表


