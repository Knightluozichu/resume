---
title: "第 12 章 操作系统设计"
type: A
section: "现代操作系统 · 设计与实现"
order: 12
description: "从设计目标和接口范型进入结构、机制策略分离、正交性、命名、绑定、通信、性能权衡、项目管理与未来趋势。覆盖39个正式目录坐标。"
demo: true
math: false
sourceUrl: "https://www.cmpedu.com/books/book/5610493.htm"
draft: false
---

import {
  Mos12OsDesignMapLab,
  Mos12OsDesignExperimentLab,
  Mos12OsDesignEvidenceLab,
} from "@/components/mdx/modern-os/diagrams/mos-12-os-design";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第 12 章 操作系统设计的核心机制，并说出它在中文原书第4版13章体系中的位置
- 能描述定义设计目标、签发系统接口、选择系统结构、分离机制策略、测量关键性能、管理演进风险六个阶段的输入、内核状态、资源所有者与不变量
- 能实现最小可重放实验，注入边界或失败条件并定位首个偏离点
- 能比较正常、边界、失败和恢复轨迹，判断目录概念是否形成可迁移能力

</Objectives>

## 直觉起点

<Term def="接口设计是第 12 章 操作系统设计的第一个观察坐标，连接请求、内核状态与恢复证据。">接口设计</Term>、<Term def="机制与策略是第 12 章 操作系统设计的第二个观察坐标，用于解释资源与责任边界。">机制与策略</Term>、<Term def="正交性是第 12 章 操作系统设计的第三个观察坐标，需要用最小实验验收。">正交性</Term>、<Term def="局部性是第 12 章 操作系统设计的第四个观察坐标，边界条件会暴露约束。">局部性</Term>、<Term def="项目管理是第 12 章 操作系统设计的第五个观察坐标，必须定位首个状态分叉。">项目管理</Term>
共同构成本页坐标。从设计目标和接口范型进入结构、机制策略分离、正交性、命名、绑定、通信、性能权衡、项目管理与未来趋势。
学习时先预测内核下一状态，再运行样本；最终输出正确不能证明调度、地址、设备、持久化与清理都正确。

<Mos12OsDesignMapLab />

## 六阶段资源链

### 1. 定义设计目标

把目标、接口、结构、权衡和证据分开：设计结论需要可诊断接口、可替换策略、稳定测量和可追溯研究来源。 对“第12章 操作系统设计”先在“定义设计目标”写出输入与预测，再推进到“签发系统接口”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 2. 签发系统接口

把目标、接口、结构、权衡和证据分开：设计结论需要可诊断接口、可替换策略、稳定测量和可追溯研究来源。 对“12.1 设计问题的本质”先在“签发系统接口”写出输入与预测，再推进到“选择系统结构”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 3. 选择系统结构

从设计目标和接口范型进入结构、机制策略分离、正交性、命名、绑定、通信、性能权衡、项目管理与未来趋势。 对“12.1.1 目标”先在“选择系统结构”写出输入与预测，再推进到“分离机制策略”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 4. 分离机制策略

把目标、接口、结构、权衡和证据分开：设计结论需要可诊断接口、可替换策略、稳定测量和可追溯研究来源。 对“12.1.2 设计操作系统为什么困难”先在“分离机制策略”写出输入与预测，再推进到“测量关键性能”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 5. 测量关键性能

把目标、接口、结构、权衡和证据分开：设计结论需要可诊断接口、可替换策略、稳定测量和可追溯研究来源。 对“12.2 接口设计”先在“测量关键性能”写出输入与预测，再推进到“管理演进风险”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 6. 管理演进风险

从设计目标和接口范型进入结构、机制策略分离、正交性、命名、绑定、通信、性能权衡、项目管理与未来趋势。 对“12.2.1 指导原则”先在“管理演进风险”写出输入与预测，再推进到“定义设计目标”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

## 核心机制深挖

从设计目标和接口范型进入结构、机制策略分离、正交性、命名、绑定、通信、性能权衡、项目管理与未来趋势。 本页把每个目录概念放回“定义设计目标 → 签发系统接口 → 选择系统结构 → 分离机制策略 → 测量关键性能 → 管理演进风险”的资源状态机。输入可能是系统调用、地址、字节流、中断、资源请求或远程消息；状态可能位于进程表、页表、驱动队列、文件元数据、虚拟机或保护域；输出必须同时说明返回值、资源所有权与下一阶段可依赖的承诺。

本页签发的不变量是：**接口含义稳定且错误可诊断；机制足以承载多种策略，优化依据常见路径和测量，组织决策不破坏架构边界。**。不变量在正常路径与错误路径都应成立。一次成功运行只证明一个样本可用，不能证明边界安全；因此目录中的机制都要进入正常、边界、失败和恢复四条轨迹，并明确谁检测、谁阻塞或重试、谁逆序清理、谁向上报告。

为一个微内核服务设计系统调用、名称与绑定时机，比较同步/异步通信和静态/动态结构，再用基准定位优化对象。 运行前把预期写成状态快照，运行后逐字段比较。并发实验记录同步边，地址实验记录位宽与权限，I/O 实验记录缓冲区和完成事件，持久化实验区分提交与落盘，分布式实验记录丢失、重复和重排。

注入接口歧义、策略硬编码、循环依赖、过早优化和团队边界错位，判断失败来自技术结构还是项目沟通。 删除故障后用完全相同的输入重放，确认返回值、内核对象、外部结果和清理状态一起恢复。只修正界面结果却留下线程、页、设备请求、文件引用、虚拟机状态或权限污染，仍然不通过。

原书第4版使用 Linux、Android、Windows 8.1、VMware 和当时的多处理机技术作为实例。现代内核和云平台会改变接口版本与性能数值，但进程状态、地址翻译、I/O 分层、资源死锁、虚拟化隔离和保护域仍可验证。现代对照必须写明差异，不能覆盖2017年中文版本的目录身份。

## 正式目录逐项讲解

### 第12章 操作系统设计

把目标、接口、结构、权衡和证据分开：设计结论需要可诊断接口、可替换策略、稳定测量和可追溯研究来源。 对“第12章 操作系统设计”先在“定义设计目标”写出输入与预测，再推进到“签发系统接口”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 12.1 设计问题的本质

把目标、接口、结构、权衡和证据分开：设计结论需要可诊断接口、可替换策略、稳定测量和可追溯研究来源。 对“12.1 设计问题的本质”先在“签发系统接口”写出输入与预测，再推进到“选择系统结构”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 12.1.1 目标

从设计目标和接口范型进入结构、机制策略分离、正交性、命名、绑定、通信、性能权衡、项目管理与未来趋势。 对“12.1.1 目标”先在“选择系统结构”写出输入与预测，再推进到“分离机制策略”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 12.1.2 设计操作系统为什么困难

把目标、接口、结构、权衡和证据分开：设计结论需要可诊断接口、可替换策略、稳定测量和可追溯研究来源。 对“12.1.2 设计操作系统为什么困难”先在“分离机制策略”写出输入与预测，再推进到“测量关键性能”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 12.2 接口设计

把目标、接口、结构、权衡和证据分开：设计结论需要可诊断接口、可替换策略、稳定测量和可追溯研究来源。 对“12.2 接口设计”先在“测量关键性能”写出输入与预测，再推进到“管理演进风险”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 12.2.1 指导原则

从设计目标和接口范型进入结构、机制策略分离、正交性、命名、绑定、通信、性能权衡、项目管理与未来趋势。 对“12.2.1 指导原则”先在“管理演进风险”写出输入与预测，再推进到“定义设计目标”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 12.2.2 范型

从设计目标和接口范型进入结构、机制策略分离、正交性、命名、绑定、通信、性能权衡、项目管理与未来趋势。 对“12.2.2 范型”先在“定义设计目标”写出输入与预测，再推进到“签发系统接口”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 12.2.3 系统调用接口

把目标、接口、结构、权衡和证据分开：设计结论需要可诊断接口、可替换策略、稳定测量和可追溯研究来源。 对“12.2.3 系统调用接口”先在“签发系统接口”写出输入与预测，再推进到“选择系统结构”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 12.3 实现

从设计目标和接口范型进入结构、机制策略分离、正交性、命名、绑定、通信、性能权衡、项目管理与未来趋势。 对“12.3 实现”先在“选择系统结构”写出输入与预测，再推进到“分离机制策略”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 12.3.1 系统结构

从设计目标和接口范型进入结构、机制策略分离、正交性、命名、绑定、通信、性能权衡、项目管理与未来趋势。 对“12.3.1 系统结构”先在“分离机制策略”写出输入与预测，再推进到“测量关键性能”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 12.3.2 机制与策略

把目标、接口、结构、权衡和证据分开：设计结论需要可诊断接口、可替换策略、稳定测量和可追溯研究来源。 对“12.3.2 机制与策略”先在“测量关键性能”写出输入与预测，再推进到“管理演进风险”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 12.3.3 正交性

从设计目标和接口范型进入结构、机制策略分离、正交性、命名、绑定、通信、性能权衡、项目管理与未来趋势。 对“12.3.3 正交性”先在“管理演进风险”写出输入与预测，再推进到“定义设计目标”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 12.3.4 命名

从设计目标和接口范型进入结构、机制策略分离、正交性、命名、绑定、通信、性能权衡、项目管理与未来趋势。 对“12.3.4 命名”先在“定义设计目标”写出输入与预测，再推进到“签发系统接口”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 12.3.5 绑定的时机

从设计目标和接口范型进入结构、机制策略分离、正交性、命名、绑定、通信、性能权衡、项目管理与未来趋势。 对“12.3.5 绑定的时机”先在“签发系统接口”写出输入与预测，再推进到“选择系统结构”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 12.3.6 静态与动态结构

从设计目标和接口范型进入结构、机制策略分离、正交性、命名、绑定、通信、性能权衡、项目管理与未来趋势。 对“12.3.6 静态与动态结构”先在“选择系统结构”写出输入与预测，再推进到“分离机制策略”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 12.3.7 自顶向下与自底向上的实现

从设计目标和接口范型进入结构、机制策略分离、正交性、命名、绑定、通信、性能权衡、项目管理与未来趋势。 对“12.3.7 自顶向下与自底向上的实现”先在“分离机制策略”写出输入与预测，再推进到“测量关键性能”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 12.3.8 同步通信与异步通信

先声明共享或消息模型，再定义一致性、失败和重试语义；缓存同步、远程调用与负载迁移都可能改变性能和所有权。 对“12.3.8 同步通信与异步通信”先在“测量关键性能”写出输入与预测，再推进到“管理演进风险”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 12.3.9 实用技术

从设计目标和接口范型进入结构、机制策略分离、正交性、命名、绑定、通信、性能权衡、项目管理与未来趋势。 对“12.3.9 实用技术”先在“管理演进风险”写出输入与预测，再推进到“定义设计目标”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 12.4 性能

把目标、接口、结构、权衡和证据分开：设计结论需要可诊断接口、可替换策略、稳定测量和可追溯研究来源。 对“12.4 性能”先在“定义设计目标”写出输入与预测，再推进到“签发系统接口”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 12.4.1 操作系统为什么运行缓慢

从设计目标和接口范型进入结构、机制策略分离、正交性、命名、绑定、通信、性能权衡、项目管理与未来趋势。 对“12.4.1 操作系统为什么运行缓慢”先在“签发系统接口”写出输入与预测，再推进到“选择系统结构”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 12.4.2 什么应该优化

从设计目标和接口范型进入结构、机制策略分离、正交性、命名、绑定、通信、性能权衡、项目管理与未来趋势。 对“12.4.2 什么应该优化”先在“选择系统结构”写出输入与预测，再推进到“分离机制策略”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 12.4.3 空间–时间的权衡

从设计目标和接口范型进入结构、机制策略分离、正交性、命名、绑定、通信、性能权衡、项目管理与未来趋势。 对“12.4.3 空间–时间的权衡”先在“分离机制策略”写出输入与预测，再推进到“测量关键性能”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 12.4.4 缓存

从设计目标和接口范型进入结构、机制策略分离、正交性、命名、绑定、通信、性能权衡、项目管理与未来趋势。 对“12.4.4 缓存”先在“测量关键性能”写出输入与预测，再推进到“管理演进风险”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 12.4.5 线索

从设计目标和接口范型进入结构、机制策略分离、正交性、命名、绑定、通信、性能权衡、项目管理与未来趋势。 对“12.4.5 线索”先在“管理演进风险”写出输入与预测，再推进到“定义设计目标”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 12.4.6 利用局部性

从设计目标和接口范型进入结构、机制策略分离、正交性、命名、绑定、通信、性能权衡、项目管理与未来趋势。 对“12.4.6 利用局部性”先在“定义设计目标”写出输入与预测，再推进到“签发系统接口”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 12.4.7 优化常见的情况

从设计目标和接口范型进入结构、机制策略分离、正交性、命名、绑定、通信、性能权衡、项目管理与未来趋势。 对“12.4.7 优化常见的情况”先在“签发系统接口”写出输入与预测，再推进到“选择系统结构”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 12.5 项目管理

把目标、接口、结构、权衡和证据分开：设计结论需要可诊断接口、可替换策略、稳定测量和可追溯研究来源。 对“12.5 项目管理”先在“选择系统结构”写出输入与预测，再推进到“分离机制策略”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 12.5.1 人月神话

从设计目标和接口范型进入结构、机制策略分离、正交性、命名、绑定、通信、性能权衡、项目管理与未来趋势。 对“12.5.1 人月神话”先在“分离机制策略”写出输入与预测，再推进到“测量关键性能”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 12.5.2 团队结构

从设计目标和接口范型进入结构、机制策略分离、正交性、命名、绑定、通信、性能权衡、项目管理与未来趋势。 对“12.5.2 团队结构”先在“测量关键性能”写出输入与预测，再推进到“管理演进风险”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 12.5.3 经验的作用

从设计目标和接口范型进入结构、机制策略分离、正交性、命名、绑定、通信、性能权衡、项目管理与未来趋势。 对“12.5.3 经验的作用”先在“管理演进风险”写出输入与预测，再推进到“定义设计目标”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 12.5.4 没有银弹

从设计目标和接口范型进入结构、机制策略分离、正交性、命名、绑定、通信、性能权衡、项目管理与未来趋势。 对“12.5.4 没有银弹”先在“定义设计目标”写出输入与预测，再推进到“签发系统接口”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 12.6 操作系统设计的趋势

把目标、接口、结构、权衡和证据分开：设计结论需要可诊断接口、可替换策略、稳定测量和可追溯研究来源。 对“12.6 操作系统设计的趋势”先在“签发系统接口”写出输入与预测，再推进到“选择系统结构”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 12.6.1 虚拟化与云

管理程序必须截获敏感操作并保持等价、隔离和资源记账；处理器、内存、设备和时间状态共同决定检查点能否恢复。 对“12.6.1 虚拟化与云”先在“选择系统结构”写出输入与预测，再推进到“分离机制策略”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 12.6.2 众核芯片

从设计目标和接口范型进入结构、机制策略分离、正交性、命名、绑定、通信、性能权衡、项目管理与未来趋势。 对“12.6.2 众核芯片”先在“分离机制策略”写出输入与预测，再推进到“测量关键性能”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 12.6.3 大型地址空间操作系统

把虚拟地址拆为可核查字段，依次检查映射、权限、驻留和置换；地址抽象必须同时说明隔离、共享与缺页恢复。 对“12.6.3 大型地址空间操作系统”先在“测量关键性能”写出输入与预测，再推进到“管理演进风险”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 12.6.4 无缝的数据访问

从设计目标和接口范型进入结构、机制策略分离、正交性、命名、绑定、通信、性能权衡、项目管理与未来趋势。 对“12.6.4 无缝的数据访问”先在“管理演进风险”写出输入与预测，再推进到“定义设计目标”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 12.6.5 电池供电的计算机

从设计目标和接口范型进入结构、机制策略分离、正交性、命名、绑定、通信、性能权衡、项目管理与未来趋势。 对“12.6.5 电池供电的计算机”先在“定义设计目标”写出输入与预测，再推进到“签发系统接口”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 12.6.6 嵌入式系统

从设计目标和接口范型进入结构、机制策略分离、正交性、命名、绑定、通信、性能权衡、项目管理与未来趋势。 对“12.6.6 嵌入式系统”先在“签发系统接口”写出输入与预测，再推进到“选择系统结构”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

### 12.7 小结

本节用“接口含义稳定且错误可诊断；机制足以承载多种策略，优化依据常见路径和测量，组织决策不破坏架构边界。”回查正常、边界、失败和恢复四条轨迹，而不是只重复术语。 对“12.7 小结”先在“选择系统结构”写出输入与预测，再推进到“分离机制策略”并比较内核可见状态。实验固定版本、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层和逆序清理结果。

## 状态与失败矩阵

| 对象         | 正常状态               | 边界样本             | 失败证据           | 恢复条件           |
| ------------ | ---------------------- | -------------------- | ------------------ | ------------------ |
| 进程与同步   | 状态转换和同步边明确   | 抢占、竞争、低容量   | 调度与等待图       | 唤醒并回收全部流   |
| 地址与存储   | 映射、权限、元数据有效 | 缺页、空间耗尽、断电 | 页表与日志轨迹     | 重建一致映射或事务 |
| 设备与虚拟化 | 请求和完成一一对应     | 超时、迁移、过量提交 | 控制器与客户机状态 | 清理后相同输入重放 |
| 分布式与安全 | 所有权、身份、协议明确 | 节点失联、越权、重放 | 消息与审计记录     | 恢复隔离和最小权限 |

## 分步可视实验

<Stepper>
  <Step title="1. 建立资源预测">
    写出定义设计目标到签发系统接口之间的输入、内核对象、所有者和允许的失败出口。
    <Mos12OsDesignMapLab />
  </Step>
  <Step title="2. 注入单一边界">
    只改变一个容量、权限、设备或时序条件，在首个偏离处停止。
    <Mos12OsDesignExperimentLab />
  </Step>
  <Step title="3. 清理并恢复重放">
    删除故障，用同一输入核对状态、结果和资源回收。
    <Mos12OsDesignEvidenceLab />
  </Step>
</Stepper>

先预测：若在“选择系统结构”注入失败，哪个内核状态应最先变化，后续哪个阶段必须停止提交副作用？写出答案后再操作实验控件。

## 可重放实验记录

```text
trace = []
for stage in ["定义设计目标", "签发系统接口", "选择系统结构", "分离机制策略", "测量关键性能", "管理演进风险"]:
  expected = predict(stage, kernel_state)
  actual = execute(stage, kernel_state)
  trace.append({ stage, expected, actual, owner, cleanup })
  stop_at_first_divergence(expected, actual)
```

```yaml
book: 现代操作系统（原书第4版）
edition: 2017-07-19
page: mos-12-os-design
formal_scope: 39条
sample: normal | boundary | failure | recovery
predict_before_run: true
capture_first_divergence: true
replay_same_input: true
```

```text
freeze version, input, workload, and environment
record system calls, kernel state, resources, devices, and synchronization
change exactly one condition
stop at the first divergent boundary
clean up in reverse acquisition order
replay the same input and verify recovery
```

## 常见误区与故障注入

<Callout type="trap">
  **误区一：应用结果正确就代表操作系统资源正确。**
  中间仍可能有竞争、脏映射、丢中断、未提交元数据、泄漏对象或越权；必须检查首个内核状态变化和清理结果。
</Callout>

<Callout type="trap">
  **误区二：只学进程、内存和文件就等于复刻第4版。** 原书还包含完整
  I/O、虚拟化与云、多处理机、安全、Linux/Android、Windows
  8、操作系统设计和研究书目，不能用九个泛化主题代替13章。
</Callout>

## 四类样本与验收

| 样本 | 注入方式                     | 必查证据                 | 通过条件     |
| ---- | ---------------------------- | ------------------------ | ------------ |
| 正常 | 固定版本、负载和输入         | 全阶段状态与输出         | 与预测一致   |
| 边界 | 极值、空输入、低容量         | 终止、权限与资源状态     | 不越界不泄漏 |
| 失败 | 错地址、设备超时、断连或竞争 | 首个错误与责任层         | 失败被隔离   |
| 恢复 | 逆序清理后重放               | 状态、输出、持久化和审计 | 回到受控结果 |

## 练习

<Exercises>

**问题 1** 为什么不能只看最终输出验收第 12 章 操作系统设计？

<Answer>
  最终输出会隐藏调度次序、地址映射、设备队列、持久化边界、资源所有权和权限状态。必须先预测，保存首个偏离点，逆序清理后再以同一输入重放。
</Answer>

**问题 2** 如何把“第12章 操作系统设计”变成可执行证据？

<Answer>
  依次沿“定义设计目标、签发系统接口、选择系统结构、分离机制策略、测量关键性能、管理演进风险”登记输入、内核状态、责任接口、边界、失败出口和恢复条件，并让正常、边界、失败、恢复四类样本都留下可比较快照。
</Answer>

**问题 3** 本页最小通过条件是什么？

<Answer>
  接口含义稳定且错误可诊断；机制足以承载多种策略，优化依据常见路径和测量，组织决策不破坏架构边界。
  同时，异常路径不污染下一次运行，所有已获得资源都有唯一逆序清理路径，恢复轨迹重新到达正常样本的状态与结果。
</Answer>

</Exercises>

## 小结

- 接口设计：连接“定义设计目标”的请求与内核状态。
- 机制与策略：连接“签发系统接口”的资源所有权。
- 正交性：连接“选择系统结构”的同步或权限边界。
- 局部性：连接“分离机制策略”的失败传播。
- 项目管理：连接“测量关键性能”的恢复证据。
- 39个正式目录坐标已进入本页或全书资源链，不以泛化主题替代原书章节。
- 返回值只是证据之一，内核状态、资源、持久化和清理共同决定是否通过。

## 术语表

<Glossary>
  <GlossaryItem term="接口设计">
    接口设计对应“定义设计目标”，验收时连接请求、内核状态、资源边界、失败传播和恢复结果。
  </GlossaryItem>
  <GlossaryItem term="机制与策略">
    机制与策略对应“签发系统接口”，验收时连接请求、内核状态、资源边界、失败传播和恢复结果。
  </GlossaryItem>
  <GlossaryItem term="正交性">
    正交性对应“选择系统结构”，验收时连接请求、内核状态、资源边界、失败传播和恢复结果。
  </GlossaryItem>
  <GlossaryItem term="局部性">
    局部性对应“分离机制策略”，验收时连接请求、内核状态、资源边界、失败传播和恢复结果。
  </GlossaryItem>
  <GlossaryItem term="项目管理">
    项目管理对应“测量关键性能”，验收时连接请求、内核状态、资源边界、失败传播和恢复结果。
  </GlossaryItem>
</Glossary>

<Attribution
  adaptedFrom="Tanenbaum、Bos · 现代操作系统（原书第4版）· 陈向群、马洪兵等译 · 机械工业出版社 · 2017-07-19 · 出版社完整目录独立教学重构"
  adaptedUrl="https://www.cmpedu.com/books/book/5610493.htm"
/>
