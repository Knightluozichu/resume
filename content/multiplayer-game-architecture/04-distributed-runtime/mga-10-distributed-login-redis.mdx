---
title: "第10章 分布式登录与Redis内存数据库"
type: C
section: "多人在线游戏架构 · 分布式运行时"
order: 10
description: "在game与space职责之间引入Redis共享短期状态，以性能剖析验证多进程登录协议而不把Redis误当永久事实源。"
demo: true
math: false
sourceUrl: "https://e.dangdang.com/products/1901359679.html"
draft: false
---


## 直觉：先找唯一事实

在game与space职责之间引入Redis共享短期状态，以性能剖析验证多进程登录协议而不把Redis误当永久事实源。 真正的难点不是把接口调用成功，而是让每次输入只产生一份事实，旧连接、旧实体、旧进程和旧定时器不能越过代际边界。先预测：如果只保留最终成功截图，本章最可能隐藏哪一种重复执行、状态污染或资源泄漏？

## 权威目录定位

本页按完整公开目录追踪以下条目；编号和名称用于锁定原书范围，正文是独立教学重构，不复制原书段落：

- 10.1 game与space的定位
- 10.2 Redis及其第三方库
- 10.3 性能瓶颈分析
- 10.4 多进程登录协议回顾
- 10.5 总结

本章从“10.1 game与space的定位”进入，以“10.5 总结”收束。现代安全、可观察性和故障演练作为教学补充单独标明，不会被登记成原书分节。

## 核心概念与所有权

、、、、。
这些概念要同时回答四个问题：谁创建输入，谁拥有可变状态，什么代际拒绝旧结果，失败后谁回滚或补偿。

|   # | 概念       | 工程含义                                                   |
| --: | ---------- | ---------------------------------------------------------- |
|   1 | game进程   | 承载玩家业务身份和非空间玩法状态的服务进程。               |
|   2 | space进程  | 拥有地图空间实体、移动和区域交互事实的服务进程。           |
|   3 | Redis      | 用于跨进程共享短期状态、原子键操作和过期数据的内存数据库。 |
|   4 | TTL        | Redis键自动失效前的剩余生存时间。                          |
|   5 | 登录幂等键 | 让同一登录意图在重试时只产生一个有效会话的唯一键。         |

## 目录主线与因果关系

### 1. 10.1 game与space的定位

这一段从“10.1 game与space的定位”推进到“10.2 Redis及其第三方库”，覆盖10.1 game与space的定位、10.2 Redis及其第三方库。先确定输入来源和状态所有者，再明确中间版本、输出以及失败补偿。相邻目录条目不是并列清单：前项提供后项的前置条件，越序输入必须在第一个不满足的阶段被拒绝。

验证时固定客户端、服务端、配置和协议版本，保存请求ID、实体或会话代际、队列水位、状态摘要和恢复动作。若相同输入不能重放，最终结果看起来正确也不能证明本段机制成立。

### 2. 10.3 性能瓶颈分析

这一段从“10.3 性能瓶颈分析”推进到“10.4 多进程登录协议回顾”，覆盖10.3 性能瓶颈分析、10.4 多进程登录协议回顾。先确定输入来源和状态所有者，再明确中间版本、输出以及失败补偿。相邻目录条目不是并列清单：前项提供后项的前置条件，越序输入必须在第一个不满足的阶段被拒绝。

验证时固定客户端、服务端、配置和协议版本，保存请求ID、实体或会话代际、队列水位、状态摘要和恢复动作。若相同输入不能重放，最终结果看起来正确也不能证明本段机制成立。

### 3. 10.5 总结

这一段从“10.5 总结”推进到“10.5 总结”，覆盖10.5 总结。先确定输入来源和状态所有者，再明确中间版本、输出以及失败补偿。相邻目录条目不是并列清单：前项提供后项的前置条件，越序输入必须在第一个不满足的阶段被拒绝。

验证时固定客户端、服务端、配置和协议版本，保存请求ID、实体或会话代际、队列水位、状态摘要和恢复动作。若相同输入不能重放，最终结果看起来正确也不能证明本段机制成立。

## 六阶段证据链

本章的可操作链条是：划分game与space → 连接Redis并设预算 → 写入短期登录状态 → 分析共享瓶颈 → 重放多进程协议 → Redis失效后签发。正常样本证明能力，边界样本证明预算，失败样本证明拒绝，恢复样本证明没有留下第二份事实。


## 最小实现骨架

下面的代码是用于复现实验的独立最小骨架，不是原书源码复制；生产实现还要补齐超时、错误分类、指标和资源清理。

```text
SET login:{account} {session_generation} NX EX 30
if result != OK: reject_duplicate_login
bind ticket to session_generation
renew only after authenticated heartbeat
delete with compare-and-delete on logout
```

同时保存阶段轨迹，不能只看最终返回值：

```text
trace_id,stage,request_id,generation,state_version,budget,decision
baseline-01,划分game与space,42,7,1,normal,accept
boundary-01,分析共享瓶颈,43,7,2,limit,accept
failure-01,Redis失效后签发,44,6,2,exceeded,reject
```

每个样本从相同夹具开始，只改变一个故障变量：

```text
for sample in [baseline, boundary, failure, recovery]:
  reset_fixture(seed = 20260716)
  run_same_script(sample)
  assert_single_owner_and_current_generation()
  assert_bounded_queue_and_memory()
  export_first_divergence_and_recovery()
```

## 可复现实验

让两个login进程并发处理同一账号并注入Redis延迟、主从切换和键过期，先预测幂等键结果，再核对game与space会话。 保存编译版本、配置摘要、协议版本、随机种子、请求序号、首偏离节点、恢复动作和最终决策。平均吞吐只能说明趋势，不能证明关键状态没有重复或丢失。

## 两个必须主动制造的失败


## 验收矩阵

| 样本 | 输入变化                     | 预期状态         | 通过条件               |
| ---- | ---------------------------- | ---------------- | ---------------------- |
| 正常 | 合法版本、身份和容量         | 六阶段顺序完成   | 双端摘要和状态版本一致 |
| 边界 | 临界时序、容量、重复或切换   | 达到预算但不越界 | 无无限队列和第二份事实 |
| 失败 | 畸形、旧代际、断线或依赖失效 | 在首偏离点停止   | 明确拒绝、补偿或回滚   |
| 恢复 | 修复后重放同一输入           | 收敛到正常轨迹   | 无幽灵状态和资源泄漏   |

本页签发不变量是：**同一账号只有一个活动登录代际；条件写原子；短期键有TTL；Redis不可用时拒绝或降级而不伪造成功。** 任何单次演示、截图或平均指标都不能替代代际、序号、状态摘要和恢复证据。

## 练习


## 小结

- **game进程**：承载玩家业务身份和非空间玩法状态的服务进程。
- **space进程**：拥有地图空间实体、移动和区域交互事实的服务进程。
- **Redis**：用于跨进程共享短期状态、原子键操作和过期数据的内存数据库。
- **TTL**：Redis键自动失效前的剩余生存时间。
- **登录幂等键**：让同一登录意图在重试时只产生一个有效会话的唯一键。
- 六阶段机制链已经映射到正常、边界、失败和恢复证据。
- 公开目录条目已逐项列出，随书源码与现代补充不会冒充原书分节。

## 术语表


## 来源与改编边界

