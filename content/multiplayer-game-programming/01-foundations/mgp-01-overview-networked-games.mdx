---
title: "第1章 网络游戏概览（Overview of Networked Games）"
type: A
section: "Multiplayer Game Programming · 网络游戏概览"
order: 1
description: "网络游戏架构不是从套接字开始，而是从共享体验、权威归属和可接受延迟的历史约束开始。"
demo: true
math: false
sourceUrl: "https://www.informit.com/store/multiplayer-game-programming-architecting-networked-9780134034331"
draft: false
---

import {
  Mgp01OverviewNetworkedGamesMapLab,
  Mgp01OverviewNetworkedGamesExperimentLab,
  Mgp01OverviewNetworkedGamesEvidenceLab,
} from "@/components/mdx/multiplayer-game-programming/diagrams/mgp-01-overview-networked-games";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释网络游戏概览的核心机制，并把它放回全书13章与附录A的主线
- 能比较正常、边界、失败和恢复样本，指出首个状态或时间偏离点
- 能实现最小C++或协议实验，记录网络身份、版本、所有权与预算
- 能设计证据回答“网络模型由玩法约束推导；每条消息只有一个进入模拟的受控边界；相同输入能复现相同世界摘要。”是否成立

</Objectives>

## 直觉：先找唯一事实

网络游戏架构不是从套接字开始，而是从共享体验、权威归属和可接受延迟的历史约束开始。 真正的难点不是让一次理想样本跑通，而是让消息身份、世界版本、状态权威和时间预算在丢包、乱序、重复与断线后仍可解释。先预测：如果只保存最终成功画面，本页最可能隐藏哪一种分歧？

<Mgp01OverviewNetworkedGamesMapLab />

## 权威目录定位

本页依据 Addison-Wesley / InformIT 官方书目与官方目录样章锁定范围。英文标题用于一对一追踪，中文正文为独立教学重构；现代平台差异、安全强化和可观察性补充会明确披露：

- A Brief History of Multiplayer Games
- Starsiege: Tribes
- Age of Empires
- Summary
- Review Questions
- Additional Readings

本页逐项覆盖6个公开目录条目，并纳入同一套正常、边界、失败与恢复验收。

## 核心概念与所有权

<Term def="多名玩家在同一设备或同一物理空间共享输入与游戏状态的形态。">本地多人</Term>、
<Term def="以文本世界、持久角色和远程多用户交互为核心的早期网络游戏。">MUD</Term>、
<Term def="在低时延、相对可信的本地网络内交换实时游戏状态的多人形态。">局域网游戏</Term>、
<Term def="各节点只交换输入并以相同初态和步序独立推演出一致状态。">确定性锁步</Term>、
<Term def="把网络消息转换为游戏事件并按明确顺序交给模拟系统的边界。">事件管理器</Term>。每个概念都要回答：谁创建输入，谁拥有可变状态，哪个序号或版本拒绝旧结果，断线后由谁恢复。

| # | 概念 | 工程含义 |
| -: | --- | --- |
| 1 | 本地多人 | 多名玩家在同一设备或同一物理空间共享输入与游戏状态的形态。 |
| 2 | MUD | 以文本世界、持久角色和远程多用户交互为核心的早期网络游戏。 |
| 3 | 局域网游戏 | 在低时延、相对可信的本地网络内交换实时游戏状态的多人形态。 |
| 4 | 确定性锁步 | 各节点只交换输入并以相同初态和步序独立推演出一致状态。 |
| 5 | 事件管理器 | 把网络消息转换为游戏事件并按明确顺序交给模拟系统的边界。 |

## 机制拆解

### 1. 历史不是年表，而是约束来源

从同机、本地网络到互联网，带宽、延迟、参与人数与作弊面不断变化。每次架构演进都在回答同一组问题：谁拥有真相、传什么、多久传一次，以及分歧如何恢复。把产品需求写成这些约束，才能避免先选技术再解释需求。

先预测：若省略这一层，哪一个序号、所有权或时间预算会最先失真？

### 2. Tribes：消息进入游戏逻辑的边界

Starsiege: Tribes 的案例强调网络管理器、事件管理器与对象之间的职责分离。接收线程不能直接任意修改世界；消息要先经过解析、验证和排序，再形成游戏可理解的事件。这个边界使重放、拒绝畸形输入和定位首个分歧成为可能。

先预测：若省略这一层，哪一个序号、所有权或时间预算会最先失真？

### 3. Age of Empires：少传数据但提高确定性成本

确定性锁步只交换玩家命令，能把带宽从世界状态规模降到输入规模，却要求所有节点对相同输入产生完全相同结果。随机数种子、浮点差异、命令帧和迟到策略都会成为协议的一部分；任何未受控差异都会累积成世界分叉。

先预测：若省略这一层，哪一个序号、所有权或时间预算会最先失真？

## 六阶段证据链

可操作链条是：确定共享体验 → 识别状态权威 → 比较Tribes模块 → 分析帝国时代锁步 → 选定网络模型 → 用样本签发。正常样本证明能力，边界样本证明预算，失败样本证明拒绝，恢复样本证明状态与资源重新收敛。

<Stepper>
  <Step title="1. 确定共享体验与识别状态权威">
    固定协议版本、网络种子、输入序号与状态所有者，运行正常和边界样本。
    <Mgp01OverviewNetworkedGamesMapLab />
  </Step>
  <Step title="2. 比较Tribes模块与分析帝国时代锁步">
    注入延迟、抖动、丢包、重复或责任节点中断，要求在首偏离点停止。
    <Mgp01OverviewNetworkedGamesExperimentLab />
  </Step>
  <Step title="3. 选定网络模型与用样本签发">
    清除故障后重放相同输入，核对世界摘要、对象数量、消息债务与资源租约。
    <Mgp01OverviewNetworkedGamesEvidenceLab />
  </Step>
</Stepper>

## 最小实现骨架

以下代码是独立复现实验骨架，不复制原书代码。序号比较必须处理回绕，生产实现还需补齐平台错误、认证、容量与资源清理。

```cpp
struct NetworkEvidence {
  std::uint32_t sequence;
  std::uint32_t worldVersion;
  std::uint64_t objectId;
  double sentAtMs;
  double appliedAtMs;
};

bool accepts(const NetworkEvidence& next, const NetworkEvidence& current) {
  return next.objectId == current.objectId &&
         next.worldVersion >= current.worldVersion;
}
```

证据记录把目录、时序与状态摘要放在一起：

```yaml
book: Multiplayer Game Programming
page: mgp-01-overview-networked-games
official_entries: 6
sample: normal | boundary | failure | recovery
protocol_version: fixed
network_seed: fixed
state_owner: explicit
first_divergence: null
```

故障脚本一次只改变一个变量：

```text
baseline -> inject(delay | jitter | loss | duplicate | disconnect)
observe -> stop_at_first_divergence
recover -> replay_same_version_seed_and_input
assert -> one_authority && converged_state && no_orphan_resource
```

## 常见误区与故障注入

<Callout type="trap">
  **误区一：把网络游戏史当作背景材料，跳过每个案例背后的状态权威与带宽取舍。** 必须保存消息序号、世界版本和首次拒绝原因，而不是只看最终画面。
</Callout>

<Callout type="trap">
  **误区二：只看到锁步节省带宽，却没有验证确定性、迟到命令和掉线玩家的处理。** 必须加入边界、失败与恢复样本，证明错误没有跨入下一阶段。
</Callout>

## 验收矩阵

| 样本 | 网络与输入变化 | 预期状态 | 通过条件 |
| --- | --- | --- | --- |
| 正常 | 合法版本、顺序、身份与容量 | 六阶段完成 | 权威摘要与对象身份一致 |
| 边界 | 临界延迟、抖动、回绕或满载 | 达到预算不越界 | 队列有界且无永久饥饿 |
| 失败 | 丢包、重复、畸形、断线或节点失效 | 首偏离点停止 | 明确拒绝、回滚或接管 |
| 恢复 | 清除故障后重放同一输入 | 收敛到正常轨迹 | 无双权威、幽灵对象和资源泄漏 |

<Mgp01OverviewNetworkedGamesEvidenceLab />

本页签发不变量是：**网络模型由玩法约束推导；每条消息只有一个进入模拟的受控边界；相同输入能复现相同世界摘要。** 单次演示、平均延迟或最终截图不能替代序号、版本、首偏离点和恢复证据。

## 练习

<Exercises>

**问题 1** 为什么不能只以最终世界状态正确作为验收？

<Answer>
  最终状态会隐藏重复副作用、旧版本覆盖、瞬时双权威和资源泄漏。必须比较消息序号、对象身份、状态所有者、时间预算与首偏离点。
</Answer>

**问题 2** 如何把本页公开目录变成可执行学习证据？

<Answer>
  先按公开顺序建立目录追踪表，再为每个主题写出输入、状态、输出、失败与恢复条件。本页重点包括：A Brief History of Multiplayer Games、Starsiege: Tribes、Age of Empires、Summary、Review Questions、Additional Readings；其余条目已在上方逐项列出并纳入验收。
</Answer>

**问题 3** 本页的最小通过条件是什么？

<Answer>
  网络模型由玩法约束推导；每条消息只有一个进入模拟的受控边界；相同输入能复现相同世界摘要。 并且相同协议版本、网络种子和输入可重放正常、边界、失败与恢复轨迹。
</Answer>

</Exercises>

## 小结

- **本地多人**：多名玩家在同一设备或同一物理空间共享输入与游戏状态的形态。
- **MUD**：以文本世界、持久角色和远程多用户交互为核心的早期网络游戏。
- **局域网游戏**：在低时延、相对可信的本地网络内交换实时游戏状态的多人形态。
- **确定性锁步**：各节点只交换输入并以相同初态和步序独立推演出一致状态。
- **事件管理器**：把网络消息转换为游戏事件并按明确顺序交给模拟系统的边界。
- 六阶段机制链已经映射到正常、边界、失败和恢复证据。
- 官方目录逐项追踪，现代补充不会冒充原书正式分节。

## 术语表

<Glossary>
  <GlossaryItem term="本地多人">多名玩家在同一设备或同一物理空间共享输入与游戏状态的形态。</GlossaryItem>
  <GlossaryItem term="MUD">以文本世界、持久角色和远程多用户交互为核心的早期网络游戏。</GlossaryItem>
  <GlossaryItem term="局域网游戏">在低时延、相对可信的本地网络内交换实时游戏状态的多人形态。</GlossaryItem>
  <GlossaryItem term="确定性锁步">各节点只交换输入并以相同初态和步序独立推演出一致状态。</GlossaryItem>
  <GlossaryItem term="事件管理器">把网络消息转换为游戏事件并按明确顺序交给模拟系统的边界。</GlossaryItem>
</Glossary>

## 来源与改编边界

<Attribution
  adaptedFrom="Josh Glazer、Sanjay Madhav 著，Addison-Wesley Professional，第一版，2015年10月出版、版权年2016，384页，eBook ISBN 9780134034331，印刷版 ISBN 9780134034300；InformIT官方书目与官方Pearson目录样章确认13章、附录A及112个公开目录条目。正文为独立教学重构。"
  adaptedUrl="https://www.informit.com/store/multiplayer-game-programming-architecting-networked-9780134034331"
/>
