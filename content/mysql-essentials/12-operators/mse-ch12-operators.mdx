---
title: "第12章 运算符"
description: "依据2016年第3版四篇结构覆盖11个节点：掌握算术、比较、逻辑、位和模式运算中的类型与三值逻辑"
type: C
section: "MySQL数据库应用从入门到精通（第3版 / 2016）"
draft: false
math: false
sourceUrl: "https://www.sanmin.com.tw/product/index/005559908"
demo: true
order: 0
---

import { Objectives } from "@/components/mdx/objectives";
import { Term } from "@/components/mdx/term";
import { Callout } from "@/components/mdx/callout";
import { Answer, Exercises } from "@/components/mdx/exercises";
import { Glossary, GlossaryItem } from "@/components/mdx/glossary";
import { Attribution } from "@/components/mdx/attribution";

# 第12章 运算符

本页对应崔洋、贺亚茹《MySQL数据库应用从入门到精通》第3版，中国铁道出版社2016年1月出版，431页，ISBN 9787113211011。课程依据公开书目、零售书目和第2版详细目录进行独立重构，不复制原文；第3版正式结构为四篇20章与五个附录，另设学习地图和总复习，共22页。

本书的版本环境早于MySQL 8.0。正文先恢复第3版知识顺序和案例边界，再把现代差异放进迁移提示；不能把窗口函数、角色、CHECK约束强化等新特性倒写成2016年原版事实。每项操作都在隔离实例完成，生产环境只采用经过备份、回滚和权限审核的变更。

<Objectives>

- 能解释“第12章 运算符”的11个学习节点，并把它们连接到“掌握算术、比较、逻辑、位和模式运算中的类型与三值逻辑”主线。
- 能复现真值表、类型转换样例和边界表达式测试，记录输入、操作、预期、实际、证据和回滚条件。
- 能区分成功、边界和失败数据，验证“表达式结果在NULL、零值、字符串数字和边界值下均符合预期”，而不是只保存一张正常结果截图。
- 能设计反例推翻“用等号比较NULL，或依赖字符串与数字的隐式转换得到偶然正确的结果”这一错误模式，完成可由另一位学习者重放的交接。

</Objectives>

## 从业务问题开始

先问“什么状态绝不允许进入数据库”，再问“哪条SQL可以建立或推翻它”。本页把语法放回业务事实、数据状态与失败恢复之中：如果不能预测影响对象、影响行数和错误结果，就先不要执行命令。

## 学习合同与证据

<Term def="本页用于掌握算术、比较、逻辑、位和模式运算中的类型与三值逻辑的第1个核心概念；必须能用数据、SQL和失败结果说明其边界">三值逻辑</Term>、<Term def="本页用于掌握算术、比较、逻辑、位和模式运算中的类型与三值逻辑的第2个核心概念；必须能用数据、SQL和失败结果说明其边界">安全等值</Term>、<Term def="本页用于掌握算术、比较、逻辑、位和模式运算中的类型与三值逻辑的第3个核心概念；必须能用数据、SQL和失败结果说明其边界">优先级</Term>、<Term def="本页用于掌握算术、比较、逻辑、位和模式运算中的类型与三值逻辑的第4个核心概念；必须能用数据、SQL和失败结果说明其边界">隐式转换</Term>、<Term def="本页用于掌握算术、比较、逻辑、位和模式运算中的类型与三值逻辑的第5个核心概念；必须能用数据、SQL和失败结果说明其边界">模式匹配</Term>
构成本页的最小词汇表。学习顺序固定为：先画数据和状态边界，再写可重放SQL；先预测结果与影响行数，再执行；最后注入空值、重复键、并发、权限不足或服务重启等失败条件。这样才能把“会输入命令”提升为“能证明数据库行为”。

本页主线是“掌握算术、比较、逻辑、位和模式运算中的类型与三值逻辑”。交付物是真值表、类型转换样例和边界表达式测试。最小不变量是：表达式结果在NULL、零值、字符串数字和边界值下均符合预期。所有实验使用专用的bookstore_lab数据库与低权限账户，先记录版本、字符集、隔离级别和SQL模式，避免同一句SQL因环境不同而得到不可比较的结果。


## 目录节点逐项映射

### 12.1 算术运算符

**节点合同 1/11 · 定义边界。** 本节点不是孤立术语。先说明“12.1 算术运算符”处理的对象、输入和输出，再把它接入“掌握算术、比较、逻辑、位和模式运算中的类型与三值逻辑”。执行前写下预期行数、约束、权限或状态，执行后同时保存结果集、影响行数、元数据与错误信息。验收以“表达式结果在NULL、零值、字符串数字和边界值下均符合预期”为准；若只能描述语法而不能给出反例和证据，本节点仍未掌握。

### 12.2 比较运算符

**节点合同 2/11 · 建立结构。** 本节点不是孤立术语。先说明“12.2 比较运算符”处理的对象、输入和输出，再把它接入“掌握算术、比较、逻辑、位和模式运算中的类型与三值逻辑”。执行前写下预期行数、约束、权限或状态，执行后同时保存结果集、影响行数、元数据与错误信息。验收以“表达式结果在NULL、零值、字符串数字和边界值下均符合预期”为准；若只能描述语法而不能给出反例和证据，本节点仍未掌握。

### 12.3 等值与安全等值

**节点合同 3/11 · 执行操作。** 本节点不是孤立术语。先说明“12.3 等值与安全等值”处理的对象、输入和输出，再把它接入“掌握算术、比较、逻辑、位和模式运算中的类型与三值逻辑”。执行前写下预期行数、约束、权限或状态，执行后同时保存结果集、影响行数、元数据与错误信息。验收以“表达式结果在NULL、零值、字符串数字和边界值下均符合预期”为准；若只能描述语法而不能给出反例和证据，本节点仍未掌握。

### 12.4 BETWEEN与IN

**节点合同 4/11 · 观察状态。** 本节点不是孤立术语。先说明“12.4 BETWEEN与IN”处理的对象、输入和输出，再把它接入“掌握算术、比较、逻辑、位和模式运算中的类型与三值逻辑”。执行前写下预期行数、约束、权限或状态，执行后同时保存结果集、影响行数、元数据与错误信息。验收以“表达式结果在NULL、零值、字符串数字和边界值下均符合预期”为准；若只能描述语法而不能给出反例和证据，本节点仍未掌握。

### 12.5 LIKE与REGEXP

**节点合同 5/11 · 验证失败。** 本节点不是孤立术语。先说明“12.5 LIKE与REGEXP”处理的对象、输入和输出，再把它接入“掌握算术、比较、逻辑、位和模式运算中的类型与三值逻辑”。执行前写下预期行数、约束、权限或状态，执行后同时保存结果集、影响行数、元数据与错误信息。验收以“表达式结果在NULL、零值、字符串数字和边界值下均符合预期”为准；若只能描述语法而不能给出反例和证据，本节点仍未掌握。

### 12.6 IS NULL与三值逻辑

**节点合同 6/11 · 保存证据。** 本节点不是孤立术语。先说明“12.6 IS NULL与三值逻辑”处理的对象、输入和输出，再把它接入“掌握算术、比较、逻辑、位和模式运算中的类型与三值逻辑”。执行前写下预期行数、约束、权限或状态，执行后同时保存结果集、影响行数、元数据与错误信息。验收以“表达式结果在NULL、零值、字符串数字和边界值下均符合预期”为准；若只能描述语法而不能给出反例和证据，本节点仍未掌握。

### 12.7 逻辑运算符

**节点合同 7/11 · 定义边界。** 本节点不是孤立术语。先说明“12.7 逻辑运算符”处理的对象、输入和输出，再把它接入“掌握算术、比较、逻辑、位和模式运算中的类型与三值逻辑”。执行前写下预期行数、约束、权限或状态，执行后同时保存结果集、影响行数、元数据与错误信息。验收以“表达式结果在NULL、零值、字符串数字和边界值下均符合预期”为准；若只能描述语法而不能给出反例和证据，本节点仍未掌握。

### 12.8 位运算符

**节点合同 8/11 · 建立结构。** 本节点不是孤立术语。先说明“12.8 位运算符”处理的对象、输入和输出，再把它接入“掌握算术、比较、逻辑、位和模式运算中的类型与三值逻辑”。执行前写下预期行数、约束、权限或状态，执行后同时保存结果集、影响行数、元数据与错误信息。验收以“表达式结果在NULL、零值、字符串数字和边界值下均符合预期”为准；若只能描述语法而不能给出反例和证据，本节点仍未掌握。

### 12.9 运算符优先级

**节点合同 9/11 · 执行操作。** 本节点不是孤立术语。先说明“12.9 运算符优先级”处理的对象、输入和输出，再把它接入“掌握算术、比较、逻辑、位和模式运算中的类型与三值逻辑”。执行前写下预期行数、约束、权限或状态，执行后同时保存结果集、影响行数、元数据与错误信息。验收以“表达式结果在NULL、零值、字符串数字和边界值下均符合预期”为准；若只能描述语法而不能给出反例和证据，本节点仍未掌握。

### 12.10 隐式类型转换

**节点合同 10/11 · 观察状态。** 本节点不是孤立术语。先说明“12.10 隐式类型转换”处理的对象、输入和输出，再把它接入“掌握算术、比较、逻辑、位和模式运算中的类型与三值逻辑”。执行前写下预期行数、约束、权限或状态，执行后同时保存结果集、影响行数、元数据与错误信息。验收以“表达式结果在NULL、零值、字符串数字和边界值下均符合预期”为准；若只能描述语法而不能给出反例和证据，本节点仍未掌握。

### 12.11 本章验证清单

**节点合同 11/11 · 验证失败。** 本节点不是孤立术语。先说明“12.11 本章验证清单”处理的对象、输入和输出，再把它接入“掌握算术、比较、逻辑、位和模式运算中的类型与三值逻辑”。执行前写下预期行数、约束、权限或状态，执行后同时保存结果集、影响行数、元数据与错误信息。验收以“表达式结果在NULL、零值、字符串数字和边界值下均符合预期”为准；若只能描述语法而不能给出反例和证据，本节点仍未掌握。

## 机制推演：从业务事实到数据库状态

第一步是写业务不变量，不是立刻写SQL。把实体标识、值域、唯一性、引用关系、允许为空、状态转移和并发条件列成表，再决定由数据类型、主键、外键、唯一约束、检查、事务还是应用层负责。数据库约束负责拒绝永远不应出现的状态；应用逻辑负责组织流程，但不能靠界面校验替代底层完整性。

第二步是画执行边界。一次请求可能经过客户端、连接池、服务器会话、解析与优化、存储引擎、日志和磁盘。记录当前数据库、账户、事务状态、隔离级别、连接ID、SQL文本、参数、执行计划、影响行数和错误码。若操作涉及多张表，必须明确第一次写入之前和最后一次提交之后分别满足什么条件。

第三步是验证数据语义。至少准备正常值、边界值、NULL、重复值、孤立引用和并发冲突六类样本。查询要验证列集合、行集合、分组口径与顺序；写入要验证影响行数、约束、日志和回滚；管理操作还要验证低权限账户被拒绝。只跑正常样本会隐藏最危险的错误路径。

第四步是建立迁移账本。第3版所处年代常见MySQL 5.x环境，而现代部署可能使用MySQL 8.0。保留原版概念顺序，同时逐项记录默认字符集、认证插件、SQL模式、系统表、角色、窗口函数、数据字典和废弃语法的变化。迁移一次只改变一个变量，结果不一致时先解释差异，再更新脚本。

<Callout type="trap">
  **核心陷阱。**
  用等号比较NULL，或依赖字符串与数字的隐式转换得到偶然正确的结果。修复方法是先恢复对象边界和不变量，再写最小SQL，用失败样本证明约束确实生效。
</Callout>

<Callout type="trap">
  **版本陷阱。**
  第3版讲解环境与现代MySQL不同。不能把8.0行为直接归给原书，也不能照抄过时认证或系统表操作；必须在版本卡中分别验证。
</Callout>

<Callout type="warning">
  数据库实验只使用隔离实例和脱敏数据。删除、授权、日志、参数和恢复操作先做快照，明确恢复点、影响范围和停止条件。
</Callout>

## 证据解释与故障定位

定义证据回答“对象是什么”。保存SHOW CREATE结果、information_schema元数据、账户和服务器变量，证明表、索引、视图、例程、触发器或权限确实按脚本建立。截图不足以比较版本；文本结果必须带时间、版本、实例标识和连接身份。

行为证据回答“执行了什么”。保存参数化SQL、执行计划、结果集、影响行数、警告和错误码。查询结果正确但扫描行数异常，属于性能失败；写入返回成功但业务不变量被破坏，属于完整性失败；低权限账户本应被拒绝却成功，属于安全失败。

恢复证据回答“失败后能否回到一致状态”。事务回滚后重跑对账查询，服务重启后核对持久数据，备份恢复后比较行数、校验值和关键业务总额。恢复时间和数据损失窗口应有实际测量，不能只写“已有备份”。

迁移证据回答“换版本后哪些结论仍成立”。使用相同数据、SQL和失败样本比较历史兼容环境与现代实例，把语法、默认值、执行计划、权限、错误码和性能变化逐项入账。只有能够解释差异并给出回退条件，迁移才算完成。

## 本章回顾

从“掌握算术、比较、逻辑、位和模式运算中的类型与三值逻辑”重新走一遍：写业务不变量，确认对象和所有者，预测SQL结果，执行并保存证据，注入失败，验证回滚，最后记录版本差异。最终必须交付真值表、类型转换样例和边界表达式测试，并由另一位学习者在隔离实例复现“表达式结果在NULL、零值、字符串数字和边界值下均符合预期”。

## 小结

- 算术运算注意类型隐式转换
- 比较运算遇 NULL 得 UNKNOWN
- 逻辑运算遵循三值逻辑
- 位运算适合标志位场景
- LIKE 模式匹配注意转义符

## 复习与独立交接

<Exercises>
**问题 1**：为什么“第12章 运算符”必须覆盖全部11个节点？

<Answer>这些节点共同建立“掌握算术、比较、逻辑、位和模式运算中的类型与三值逻辑”所需的概念、语法、状态、失败与验证链；删去任何关键环节都会让真值表、类型转换样例和边界表达式测试无法独立交接。</Answer>

**问题 2**：本页最小正确性合同是什么？

<Answer>
  表达式结果在NULL、零值、字符串数字和边界值下均符合预期。需要同时提交定义证据、行为证据、失败证据和恢复证据，不能用正常路径截图代替。
</Answer>

**问题 3**：怎样构造能推翻错误理解的反例？

<Answer>
  先让“用等号比较NULL，或依赖字符串与数字的隐式转换得到偶然正确的结果”成为待证假设，再用NULL、重复键、越权、并发或重启中的一个变量触发；若不变量失效就定位到约束、事务、权限或恢复边界。
</Answer>

**问题 4**：为什么执行成功不等于业务正确？

<Answer>
  SQL成功只说明服务器接受了语句。还必须核对命中行集、约束、最终状态、并发可见性、日志和资源释放，才能证明业务事实没有被破坏。
</Answer>

**问题 5**：如何处理第3版与现代MySQL的差异？

<Answer>
  保留原书概念与章节顺序，在独立迁移账本中用同一数据和失败样本比较版本；一次只替换一个环境变量，记录差异、风险与回退。
</Answer>

**问题 6**：独立交接至少包含什么？

<Answer>版本卡、可重放脚本、真值表、类型转换样例和边界表达式测试、预期与实际结果、错误码、失败实验、恢复步骤、迁移差异和明确验收条件。</Answer>
</Exercises>

<Glossary>
  <GlossaryItem term="三值逻辑">
    三值逻辑在本页中必须由数据样本、SQL结果和失败边界共同定义。
  </GlossaryItem>
  <GlossaryItem term="安全等值">
    安全等值在本页中必须由数据样本、SQL结果和失败边界共同定义。
  </GlossaryItem>
  <GlossaryItem term="优先级">
    优先级在本页中必须由数据样本、SQL结果和失败边界共同定义。
  </GlossaryItem>
  <GlossaryItem term="隐式转换">
    隐式转换在本页中必须由数据样本、SQL结果和失败边界共同定义。
  </GlossaryItem>
  <GlossaryItem term="模式匹配">
    模式匹配在本页中必须由数据样本、SQL结果和失败边界共同定义。
  </GlossaryItem>
</Glossary>

[← 上一页：第11章 多表查询](/learn/mysql-essentials/11-multi-table-query/mse-ch11-multi-table-query) · [下一页：第13章 常用函数 →](/learn/mysql-essentials/13-functions/mse-ch13-functions)

<Attribution
  adaptedFrom="《MySQL数据库应用从入门到精通》第3版"
  adaptedUrl="https://www.sanmin.com.tw/product/index/005559908"
/>
