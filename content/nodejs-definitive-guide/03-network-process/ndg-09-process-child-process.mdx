---
title: "第 9 章 进程与子进程"
type: A
section: "Node.js 权威指南 · 网络与进程"
order: 9
description: "区分进程身份、环境、标准流、IPC 与退出码，用受控子进程隔离 CPU 或外部命令，并完成信号驱动的优雅关闭。"
demo: true
math: false
sourceUrl: "https://www.dedao.cn/ebook/detail?id=poVrgXBZpKrJvLmo89Q4bD7O1de2aWAj9n3YgXGPxBMVzqlRNk5j6yEnAa9E576Q"
draft: true
---

import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第 9 章 进程与子进程的核心职责，并说出它在原书 16 章三部分结构中的位置
- 能描述本页每个公开目录条目的输入、Node API、异步边界、资源所有者与结束状态
- 能实现一个最小正常样本，并注入超时、断线、权限、背压或版本变化定位首个偏离点
- 能比较正常、边界、失败和恢复轨迹，判断历史接口与现代替代是否被清楚区分

</Objectives>

## 直觉起点

<Term def="process 是“第 9 章 进程与子进程”的第 1 个观察量，对应“定义进程角色”；需要同时记录输入、资源、错误、关闭和版本证据。">process</Term>、<Term def="child_process 是“第 9 章 进程与子进程”的第 2 个观察量，对应“冻结参数环境”；需要同时记录输入、资源、错误、关闭和版本证据。">child_process</Term>、<Term def="IPC 是“第 9 章 进程与子进程”的第 3 个观察量，对应“创建子进程”；需要同时记录输入、资源、错误、关闭和版本证据。">IPC</Term>、<Term def="signal 是“第 9 章 进程与子进程”的第 4 个观察量，对应“交换消息”；需要同时记录输入、资源、错误、关闭和版本证据。">signal</Term>、<Term def="exit code 是“第 9 章 进程与子进程”的第 5 个观察量，对应“传播失败”；需要同时记录输入、资源、错误、关闭和版本证据。">exit code</Term>
共同构成本页的观察坐标。区分进程身份、环境、标准流、IPC
与退出码，用受控子进程隔离 CPU 或外部命令，并完成信号驱动的优雅关闭。 Node.js
的关键不只是异步
API，而是谁创建资源、谁接收完成通知、错误沿哪条边传播、什么条件下进程可以安全退出。2014
年版接口与现代 Node 的差异会明确标注，不用新 API
冒充原书目录，也不把废弃接口直接推荐给新项目。

## 六阶段执行链

### 1. 定义进程角色

固定 Node 版本、启动参数、环境变量和输入，先预测同步调用栈、异步资源与最终输出。没有版本证据的示例无法区分书中历史行为与当前运行时。

### 2. 冻结参数环境

明确数据是字符串、字节、流、报文、请求、事件还是数据库记录，并标出所有者。创建资源的代码必须同时设计 error、timeout、abort 或 close 路径。

### 3. 创建子进程

只保留最小处理器和一个完成出口。回调、Promise 与事件不得为同一操作重复签发结果；监听器注册顺序和一次性语义要能从代码读出。

### 4. 交换消息

注入一个边界：空输入、长输入、半包、慢消费者、权限拒绝、数据库回滚或客户端断开。停止在首个偏离点，保存错误对象、资源状态与未完成数量。

### 5. 传播失败

检查旧接口在现代 Node 的状态：仍支持、语义变化、弃用或移出核心。现代替代必须保持原章节要解决的问题，同时说明迁移不能自动保证的兼容行为。

### 6. 等待退出

清除故障并重放同一输入，等待文件、套接字、连接池和子进程排空。退出码、最终响应和资源计数全部收敛，才算恢复而不是暂时停止报错。

## 核心机制深挖

区分进程身份、环境、标准流、IPC 与退出码，用受控子进程隔离 CPU 或外部命令，并完成信号驱动的优雅关闭。 所有案例统一记录五类证据：**输入表示、调度边界、资源所有者、错误出口、关闭条件**。输入表示错误会造成编码、分帧或查询歧义；调度边界错误会把同步事件误认为异步回调；所有权不清会留下描述符和连接；错误出口重复会产生二次响应；关闭条件缺失会让进程一直存活或强行丢数据。

本页签发不变量是：**命令参数不经 shell 拼接，标准流有消费方，父子失败与信号能传播，退出码决定任务是否成功。** 代码必须能回答正常路径完成了什么、边界输入限制在哪里、失败由谁观察、取消怎样传播、关闭等待哪些资源，以及 2014 年 API 在当前版本中是否仍适合新代码。只展示成功日志或最终页面，不能证明这些条件。

## 版本兼容账本

原书出版于 2014 年，示例所处的 Node、npm、Express、Socket.IO 与数据库驱动生态都早于当前版本。重构保留原书问题与模块身份，但代码采用安全、可运行的现代形式，并为 domain、punycode、旧 Buffer 构造器和旧框架 API 标记历史状态。读者迁移旧项目时应先固定原版本复现，再逐层替换接口与测试，不能一次升级所有依赖后凭报错猜测语义变化。

兼容账本至少包含：原书接口、当时用途、当前支持状态、推荐替代、行为差异和删除旧兼容层的条件。核心原理例如 TCP 无消息边界、流需要背压、事务需要回滚不会因版本改变；具体函数签名、默认超时、模块入口和错误类型则可能变化。把稳定原理与易变 API 分开，才能既忠实复刻知识体系，又不误导今天的新代码。

## 出版社目录逐项讲解

### 第9章 进程与子进程

区分进程身份、环境、标准流、IPC 与退出码，用受控子进程隔离 CPU 或外部命令，并完成信号驱动的优雅关闭。

实验从“定义进程角色”开始：固定 Node 版本、入口、输入与资源上限，先预测事件、字节或消息怎样流动，再只注入一个超时、断线、权限、背压或版本变化。记录首个偏离点，删除故障后重放同一输入，不能用额外监听器和重复重试掩盖根因。

### 9.1 Node.js中的进程

process 暴露参数、环境、标准流、身份、信号和退出状态。环境变量都是字符串且可能缺失，进程级事件只用于最终记录和关闭，不能恢复未知状态继续服务。

实验从“冻结参数环境”开始：固定 Node 版本、入口、输入与资源上限，先预测事件、字节或消息怎样流动，再只注入一个超时、断线、权限、背压或版本变化。记录首个偏离点，删除故障后重放同一输入，不能用额外监听器和重复重试掩盖根因。

### 9.2 创建多进程应用程序

多进程可利用多核并隔离故障，但状态与内存不共享。任务分配、IPC、健康检查和重启策略必须明确，不能假设增加 worker 就线性提升吞吐。

实验从“创建子进程”开始：固定 Node 版本、入口、输入与资源上限，先预测事件、字节或消息怎样流动，再只注入一个超时、断线、权限、背压或版本变化。记录首个偏离点，删除故障后重放同一输入，不能用额外监听器和重复重试掩盖根因。

### 9.3 在多个子进程中运行Node.js应用程序

fork 专用于 Node 模块并建立 IPC，spawn 适合一般命令。父进程要消费标准流、限制并发、验证参数并等待 exit；子进程退出前的 close 与 exit 事件含义不同。

实验从“交换消息”开始：固定 Node 版本、入口、输入与资源上限，先预测事件、字节或消息怎样流动，再只注入一个超时、断线、权限、背压或版本变化。记录首个偏离点，删除故障后重放同一输入，不能用额外监听器和重复重试掩盖根因。

### 9.4 小结

本章小结不是 API 清单，而是复核“定义进程角色、冻结参数环境、创建子进程、交换消息、传播失败、等待退出”是否闭环：输入从哪里进入、资源由谁持有、错误怎样传播、何时释放，以及现代运行时是否改变了原书接口的支持状态。

实验从“传播失败”开始：固定 Node 版本、入口、输入与资源上限，先预测事件、字节或消息怎样流动，再只注入一个超时、断线、权限、背压或版本变化。记录首个偏离点，删除故障后重放同一输入，不能用额外监听器和重复重试掩盖根因。

## 资源与失败矩阵

| 资源           | 正常完成       | 常见边界         | 失败出口       | 关闭证据     |
| -------------- | -------------- | ---------------- | -------------- | ------------ |
| Buffer 或消息  | 完整编码、解析 | 空、截断、超长   | 校验错误       | 无越界读写   |
| 文件或流       | 读写、管道结束 | 权限、慢目标     | pipeline 拒绝  | 描述符关闭   |
| 套接字或请求   | 响应一次       | 半包、断线、超时 | error 或 abort | close 已观察 |
| 子进程或连接池 | 退出或归还     | stderr、回滚     | 非零码或异常   | 无悬挂资源   |

## 最小可运行实验

```js
import { spawn } from "node:child_process";

const child = spawn(process.execPath, ["worker.js"], {
  stdio: ["ignore", "pipe", "pipe"],
});
child.stdout.setEncoding("utf8");
child.stdout.on("data", (line) => process.stdout.write(line));
child.on("exit", (code) => console.log({ code }));
```

```yaml
book: Node.js 权威指南
page: ndg-09-process-child-process
edition: 2014
sample: normal | boundary | failure | recovery
resource_owner_known: true
single_completion: true
close_observed: true
```

```text
freeze node version + entry + input
predict sync stack + async resources
change exactly one boundary condition
stop at first divergent state
remove fault and replay
wait for every resource to close
```

## 常见误区与故障注入

<Callout type="trap">
  **误区一：把“异步”理解成自动并行、自动安全。**
  回调仍可能阻塞主线程，异步资源仍会耗尽，多个完成出口仍会竞态。必须记录队列、并发上限、背压和资源关闭。
</Callout>

<Callout type="trap">
  **误区二：用最新 Node 文档替换原书身份，或照搬 2014 年 API 给新项目。**
  正确做法是保留 16 章与 101
  个目录条目，分别讲清稳定原理、历史接口、当前状态和现代替代。
</Callout>

## 四类样本与验收

| 样本 | 注入方式               | 必查证据               | 通过条件       |
| ---- | ---------------------- | ---------------------- | -------------- |
| 正常 | 小输入、可用依赖       | 事件、字节、响应、提交 | 只完成一次     |
| 边界 | 空、长、慢、并发       | 上限、背压、顺序       | 内存与延迟受控 |
| 失败 | 权限、断线、超时、回滚 | 首个错误与所有者       | 错误不丢不重复 |
| 恢复 | 删除故障后重放         | close、exit、连接数    | 无悬挂资源     |

目录证据：第9章 进程与子进程、9.1 Node.js中的进程、9.2 创建多进程应用程序、9.3 在多个子进程中运行Node.js应用程序、9.4 小结。本页按公开目录独立重构，不复制原书正文；每个条目都进入输入、机制、故障、版本和关闭证据链。

## 练习

<Exercises>

**问题 1** 为什么不能只用成功输出验收第 9 章 进程与子进程？

<Answer>
  成功输出不显示调度顺序、背压、错误出口、资源所有权和关闭状态。必须注入超时、断线、权限或版本变化，并确认恢复后没有重复完成和悬挂句柄。
</Answer>

**问题 2** 如何把本页公开目录变成可执行证据？

<Answer>
  依次登记“第9章 进程与子进程、9.1 Node.js中的进程、9.2 创建多进程应用程序、9.3
  在多个子进程中运行Node.js应用程序、9.4 小结”，为每项写出输入表示、Node
  API、异步边界、资源所有者、失败出口、关闭条件与当前版本状态，再用同一输入重放四类样本。
</Answer>

**问题 3** 本页最小通过条件是什么？

<Answer>
  命令参数不经 shell
  拼接，标准流有消费方，父子失败与信号能传播，退出码决定任务是否成功。
  同时，正常、边界、失败和恢复轨迹可复现，最终没有未归还连接、未关闭套接字、重复回调或模糊版本依赖。
</Answer>

</Exercises>

## 小结

- process：对应“定义进程角色”的核心观察量。
- child_process：对应“冻结参数环境”的核心观察量。
- IPC：对应“创建子进程”的核心观察量。
- signal：对应“交换消息”的核心观察量。
- exit code：对应“传播失败”的核心观察量。
- 5 个本页目录条目已全部映射到执行链与版本账本。
- 最终输出只是证据之一，资源关闭和错误传播同样决定是否通过。

## 术语表

<Glossary>
  <GlossaryItem term="process">
    process 对应“定义进程角色”，验收时连接 API
    语义、资源所有权、失败传播、关闭顺序与版本状态。
  </GlossaryItem>
  <GlossaryItem term="child_process">
    child_process 对应“冻结参数环境”，验收时连接 API
    语义、资源所有权、失败传播、关闭顺序与版本状态。
  </GlossaryItem>
  <GlossaryItem term="IPC">
    IPC 对应“创建子进程”，验收时连接 API
    语义、资源所有权、失败传播、关闭顺序与版本状态。
  </GlossaryItem>
  <GlossaryItem term="signal">
    signal 对应“交换消息”，验收时连接 API
    语义、资源所有权、失败传播、关闭顺序与版本状态。
  </GlossaryItem>
  <GlossaryItem term="exit code">
    exit code 对应“传播失败”，验收时连接 API
    语义、资源所有权、失败传播、关闭顺序与版本状态。
  </GlossaryItem>
</Glossary>

<Attribution
  adaptedFrom="陆凌牛 · Node.js权威指南 · 机械工业出版社 · 2014 · 完整公开目录"
  adaptedUrl="https://www.dedao.cn/ebook/detail?id=poVrgXBZpKrJvLmo89Q4bD7O1de2aWAj9n3YgXGPxBMVzqlRNk5j6yEnAa9E576Q"
/>
