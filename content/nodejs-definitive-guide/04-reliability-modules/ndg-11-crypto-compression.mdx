---
title: "第 11 章 加密与压缩"
type: A
section: "Node.js 权威指南 · 可靠性与工具模块"
order: 11
description: "区分哈希、认证加密、密钥派生和传输压缩，以算法参数、随机数、认证标签和解压上限建立安全边界。"
demo: true
math: false
sourceUrl: "https://www.dedao.cn/ebook/detail?id=poVrgXBZpKrJvLmo89Q4bD7O1de2aWAj9n3YgXGPxBMVzqlRNk5j6yEnAa9E576Q"
draft: false
---


## 直觉起点

、、、、共同构成本页的观察坐标。区分哈希、认证加密、密钥派生和传输压缩，以算法参数、随机数、认证标签和解压上限建立安全边界。
Node.js 的关键不只是异步
API，而是谁创建资源、谁接收完成通知、错误沿哪条边传播、什么条件下进程可以安全退出。2014
年版接口与现代 Node 的差异会明确标注，不用新 API
冒充原书目录，也不把废弃接口直接推荐给新项目。

## 六阶段执行链

### 1. 定义安全目标

固定 Node 版本、启动参数、环境变量和输入，先预测同步调用栈、异步资源与最终输出。没有版本证据的示例无法区分书中历史行为与当前运行时。

### 2. 选择原语

明确数据是字符串、字节、流、报文、请求、事件还是数据库记录，并标出所有者。创建资源的代码必须同时设计 error、timeout、abort 或 close 路径。

### 3. 生成随机材料

只保留最小处理器和一个完成出口。回调、Promise 与事件不得为同一操作重复签发结果；监听器注册顺序和一次性语义要能从代码读出。

### 4. 流式处理

注入一个边界：空输入、长输入、半包、慢消费者、权限拒绝、数据库回滚或客户端断开。停止在首个偏离点，保存错误对象、资源状态与未完成数量。

### 5. 验证完整性

检查旧接口在现代 Node 的状态：仍支持、语义变化、弃用或移出核心。现代替代必须保持原章节要解决的问题，同时说明迁移不能自动保证的兼容行为。

### 6. 清理敏感数据

清除故障并重放同一输入，等待文件、套接字、连接池和子进程排空。退出码、最终响应和资源计数全部收敛，才算恢复而不是暂时停止报错。

## 核心机制深挖

区分哈希、认证加密、密钥派生和传输压缩，以算法参数、随机数、认证标签和解压上限建立安全边界。 所有案例统一记录五类证据：**输入表示、调度边界、资源所有者、错误出口、关闭条件**。输入表示错误会造成编码、分帧或查询歧义；调度边界错误会把同步事件误认为异步回调；所有权不清会留下描述符和连接；错误出口重复会产生二次响应；关闭条件缺失会让进程一直存活或强行丢数据。

本页签发不变量是：**算法用途、密钥与随机数来源、完整性验证和资源上限清楚；压缩不能在未设上限时处理不可信巨量输入。** 代码必须能回答正常路径完成了什么、边界输入限制在哪里、失败由谁观察、取消怎样传播、关闭等待哪些资源，以及 2014 年 API 在当前版本中是否仍适合新代码。只展示成功日志或最终页面，不能证明这些条件。

## 版本兼容账本

原书出版于 2014 年，示例所处的 Node、npm、Express、Socket.IO 与数据库驱动生态都早于当前版本。重构保留原书问题与模块身份，但代码采用安全、可运行的现代形式，并为 domain、punycode、旧 Buffer 构造器和旧框架 API 标记历史状态。读者迁移旧项目时应先固定原版本复现，再逐层替换接口与测试，不能一次升级所有依赖后凭报错猜测语义变化。

兼容账本至少包含：原书接口、当时用途、当前支持状态、推荐替代、行为差异和删除旧兼容层的条件。核心原理例如 TCP 无消息边界、流需要背压、事务需要回滚不会因版本改变；具体函数签名、默认超时、模块入口和错误类型则可能变化。把稳定原理与易变 API 分开，才能既忠实复刻知识体系，又不误导今天的新代码。

## 出版社目录逐项讲解

### 第11章 加密与压缩

区分哈希、认证加密、密钥派生和传输压缩，以算法参数、随机数、认证标签和解压上限建立安全边界。

实验从“定义安全目标”开始：固定 Node 版本、入口、输入与资源上限，先预测事件、字节或消息怎样流动，再只注入一个超时、断线、权限、背压或版本变化。记录首个偏离点，删除故障后重放同一输入，不能用额外监听器和重复重试掩盖根因。

### 11.1 加密与解密处理

先区分哈希、MAC、加密和签名的安全目标。口令需要专用密钥派生，数据加密优先认证加密并保存 nonce 与标签；自创算法或固定 IV 都不能提供可靠安全性。

实验从“选择原语”开始：固定 Node 版本、入口、输入与资源上限，先预测事件、字节或消息怎样流动，再只注入一个超时、断线、权限、背压或版本变化。记录首个偏离点，删除故障后重放同一输入，不能用额外监听器和重复重试掩盖根因。

### 11.2 压缩与解压缩处理

zlib 流可接入 pipeline 并遵守背压。压缩比取决于数据，解压不可信输入必须限制输出和 CPU，HTTP 内容编码还要正确协商并设置响应头。

实验从“生成随机材料”开始：固定 Node 版本、入口、输入与资源上限，先预测事件、字节或消息怎样流动，再只注入一个超时、断线、权限、背压或版本变化。记录首个偏离点，删除故障后重放同一输入，不能用额外监听器和重复重试掩盖根因。

### 11.3 小结

本章小结不是 API 清单，而是复核“定义安全目标、选择原语、生成随机材料、流式处理、验证完整性、清理敏感数据”是否闭环：输入从哪里进入、资源由谁持有、错误怎样传播、何时释放，以及现代运行时是否改变了原书接口的支持状态。

实验从“流式处理”开始：固定 Node 版本、入口、输入与资源上限，先预测事件、字节或消息怎样流动，再只注入一个超时、断线、权限、背压或版本变化。记录首个偏离点，删除故障后重放同一输入，不能用额外监听器和重复重试掩盖根因。

## 资源与失败矩阵

| 资源           | 正常完成       | 常见边界         | 失败出口       | 关闭证据     |
| -------------- | -------------- | ---------------- | -------------- | ------------ |
| Buffer 或消息  | 完整编码、解析 | 空、截断、超长   | 校验错误       | 无越界读写   |
| 文件或流       | 读写、管道结束 | 权限、慢目标     | pipeline 拒绝  | 描述符关闭   |
| 套接字或请求   | 响应一次       | 半包、断线、超时 | error 或 abort | close 已观察 |
| 子进程或连接池 | 退出或归还     | stderr、回滚     | 非零码或异常   | 无悬挂资源   |

## 最小可运行实验

```js
import { createHash } from "node:crypto";
import { createGzip } from "node:zlib";
import { pipeline } from "node:stream/promises";

const digest = createHash("sha256").update("payload").digest("hex");
console.log(digest);
await pipeline(process.stdin, createGzip(), process.stdout);
```

```yaml
book: Node.js 权威指南
page: ndg-11-crypto-compression
edition: 2014
sample: normal | boundary | failure | recovery
resource_owner_known: true
single_completion: true
close_observed: true
```

```text
freeze node version + entry + input
predict sync stack + async resources
change exactly one boundary condition
stop at first divergent state
remove fault and replay
wait for every resource to close
```

## 常见误区与故障注入


## 四类样本与验收

| 样本 | 注入方式               | 必查证据               | 通过条件       |
| ---- | ---------------------- | ---------------------- | -------------- |
| 正常 | 小输入、可用依赖       | 事件、字节、响应、提交 | 只完成一次     |
| 边界 | 空、长、慢、并发       | 上限、背压、顺序       | 内存与延迟受控 |
| 失败 | 权限、断线、超时、回滚 | 首个错误与所有者       | 错误不丢不重复 |
| 恢复 | 删除故障后重放         | close、exit、连接数    | 无悬挂资源     |

目录证据：第11章 加密与压缩、11.1 加密与解密处理、11.2 压缩与解压缩处理、11.3 小结。本页按公开目录独立重构，不复制原书正文；每个条目都进入输入、机制、故障、版本和关闭证据链。

## 练习


## 小结

- crypto：对应“定义安全目标”的核心观察量。
- AEAD：对应“选择原语”的核心观察量。
- nonce：对应“生成随机材料”的核心观察量。
- authentication tag：对应“流式处理”的核心观察量。
- zlib：对应“验证完整性”的核心观察量。
- 4 个本页目录条目已全部映射到执行链与版本账本。
- 最终输出只是证据之一，资源关闭和错误传播同样决定是否通过。

## 术语表


