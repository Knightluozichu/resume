---
title: "第 13 章 数据库访问"
type: A
section: "Node.js 权威指南 · 数据库与 Web"
order: 13
description: "比较 MongoDB 文档操作与 MySQL 关系事务，统一处理连接池、参数化、索引、超时、提交和回滚。"
demo: true
math: false
sourceUrl: "https://www.dedao.cn/ebook/detail?id=poVrgXBZpKrJvLmo89Q4bD7O1de2aWAj9n3YgXGPxBMVzqlRNk5j6yEnAa9E576Q"
draft: false
---


## 直觉起点

、、、、共同构成本页的观察坐标。比较 MongoDB 文档操作与 MySQL
关系事务，统一处理连接池、参数化、索引、超时、提交和回滚。 Node.js
的关键不只是异步
API，而是谁创建资源、谁接收完成通知、错误沿哪条边传播、什么条件下进程可以安全退出。2014
年版接口与现代 Node 的差异会明确标注，不用新 API
冒充原书目录，也不把废弃接口直接推荐给新项目。

## 六阶段执行链

### 1. 定义数据契约

固定 Node 版本、启动参数、环境变量和输入，先预测同步调用栈、异步资源与最终输出。没有版本证据的示例无法区分书中历史行为与当前运行时。

### 2. 获取连接

明确数据是字符串、字节、流、报文、请求、事件还是数据库记录，并标出所有者。创建资源的代码必须同时设计 error、timeout、abort 或 close 路径。

### 3. 参数化查询

只保留最小处理器和一个完成出口。回调、Promise 与事件不得为同一操作重复签发结果；监听器注册顺序和一次性语义要能从代码读出。

### 4. 检查结果

注入一个边界：空输入、长输入、半包、慢消费者、权限拒绝、数据库回滚或客户端断开。停止在首个偏离点，保存错误对象、资源状态与未完成数量。

### 5. 提交或回滚

检查旧接口在现代 Node 的状态：仍支持、语义变化、弃用或移出核心。现代替代必须保持原章节要解决的问题，同时说明迁移不能自动保证的兼容行为。

### 6. 归还连接

清除故障并重放同一输入，等待文件、套接字、连接池和子进程排空。退出码、最终响应和资源计数全部收敛，才算恢复而不是暂时停止报错。

## 核心机制深挖

比较 MongoDB 文档操作与 MySQL 关系事务，统一处理连接池、参数化、索引、超时、提交和回滚。 所有案例统一记录五类证据：**输入表示、调度边界、资源所有者、错误出口、关闭条件**。输入表示错误会造成编码、分帧或查询歧义；调度边界错误会把同步事件误认为异步回调；所有权不清会留下描述符和连接；错误出口重复会产生二次响应；关闭条件缺失会让进程一直存活或强行丢数据。

本页签发不变量是：**查询参数不拼接、连接必归还、写入边界有事务或幂等策略，超时与重复请求不会悄悄破坏数据。** 代码必须能回答正常路径完成了什么、边界输入限制在哪里、失败由谁观察、取消怎样传播、关闭等待哪些资源，以及 2014 年 API 在当前版本中是否仍适合新代码。只展示成功日志或最终页面，不能证明这些条件。

## 版本兼容账本

原书出版于 2014 年，示例所处的 Node、npm、Express、Socket.IO 与数据库驱动生态都早于当前版本。重构保留原书问题与模块身份，但代码采用安全、可运行的现代形式，并为 domain、punycode、旧 Buffer 构造器和旧框架 API 标记历史状态。读者迁移旧项目时应先固定原版本复现，再逐层替换接口与测试，不能一次升级所有依赖后凭报错猜测语义变化。

兼容账本至少包含：原书接口、当时用途、当前支持状态、推荐替代、行为差异和删除旧兼容层的条件。核心原理例如 TCP 无消息边界、流需要背压、事务需要回滚不会因版本改变；具体函数签名、默认超时、模块入口和错误类型则可能变化。把稳定原理与易变 API 分开，才能既忠实复刻知识体系，又不误导今天的新代码。

## 出版社目录逐项讲解

### 第13章 数据库访问

比较 MongoDB 文档操作与 MySQL 关系事务，统一处理连接池、参数化、索引、超时、提交和回滚。

实验从“定义数据契约”开始：固定 Node 版本、入口、输入与资源上限，先预测事件、字节或消息怎样流动，再只注入一个超时、断线、权限、背压或版本变化。记录首个偏离点，删除故障后重放同一输入，不能用额外监听器和重复重试掩盖根因。

### 13.1 在MongoDB数据库中存取数据

文档写入要定义 schema、唯一键、索引和写关注，ObjectId 与业务 ID 分开。驱动连接池、超时和事务能力依版本而异，重复请求需幂等键。

实验从“获取连接”开始：固定 Node 版本、入口、输入与资源上限，先预测事件、字节或消息怎样流动，再只注入一个超时、断线、权限、背压或版本变化。记录首个偏离点，删除故障后重放同一输入，不能用额外监听器和重复重试掩盖根因。

### 13.2 在MySQL数据库中存取数据

所有值使用参数化查询，连接从池中借出并在 finally 归还。跨多条写入用事务和明确隔离级别，死锁与网络中断需要有界重试而非盲目重复。

实验从“参数化查询”开始：固定 Node 版本、入口、输入与资源上限，先预测事件、字节或消息怎样流动，再只注入一个超时、断线、权限、背压或版本变化。记录首个偏离点，删除故障后重放同一输入，不能用额外监听器和重复重试掩盖根因。

### 13.3 小结

本章小结不是 API 清单，而是复核“定义数据契约、获取连接、参数化查询、检查结果、提交或回滚、归还连接”是否闭环：输入从哪里进入、资源由谁持有、错误怎样传播、何时释放，以及现代运行时是否改变了原书接口的支持状态。

实验从“检查结果”开始：固定 Node 版本、入口、输入与资源上限，先预测事件、字节或消息怎样流动，再只注入一个超时、断线、权限、背压或版本变化。记录首个偏离点，删除故障后重放同一输入，不能用额外监听器和重复重试掩盖根因。

## 资源与失败矩阵

| 资源           | 正常完成       | 常见边界         | 失败出口       | 关闭证据     |
| -------------- | -------------- | ---------------- | -------------- | ------------ |
| Buffer 或消息  | 完整编码、解析 | 空、截断、超长   | 校验错误       | 无越界读写   |
| 文件或流       | 读写、管道结束 | 权限、慢目标     | pipeline 拒绝  | 描述符关闭   |
| 套接字或请求   | 响应一次       | 半包、断线、超时 | error 或 abort | close 已观察 |
| 子进程或连接池 | 退出或归还     | stderr、回滚     | 非零码或异常   | 无悬挂资源   |

## 最小可运行实验

```js
const connection = await pool.getConnection();
try {
  await connection.beginTransaction();
  await connection.execute(
    "UPDATE accounts SET balance = balance - ? WHERE id = ?",
    [amount, from],
  );
  await connection.execute(
    "UPDATE accounts SET balance = balance + ? WHERE id = ?",
    [amount, to],
  );
  await connection.commit();
} catch (error) {
  await connection.rollback();
  throw error;
} finally {
  connection.release();
}
```

```yaml
book: Node.js 权威指南
page: ndg-13-database-access
edition: 2014
sample: normal | boundary | failure | recovery
resource_owner_known: true
single_completion: true
close_observed: true
```

```text
freeze node version + entry + input
predict sync stack + async resources
change exactly one boundary condition
stop at first divergent state
remove fault and replay
wait for every resource to close
```

## 常见误区与故障注入


## 四类样本与验收

| 样本 | 注入方式               | 必查证据               | 通过条件       |
| ---- | ---------------------- | ---------------------- | -------------- |
| 正常 | 小输入、可用依赖       | 事件、字节、响应、提交 | 只完成一次     |
| 边界 | 空、长、慢、并发       | 上限、背压、顺序       | 内存与延迟受控 |
| 失败 | 权限、断线、超时、回滚 | 首个错误与所有者       | 错误不丢不重复 |
| 恢复 | 删除故障后重放         | close、exit、连接数    | 无悬挂资源     |

目录证据：第13章 数据库访问、13.1 在MongoDB数据库中存取数据、13.2 在MySQL数据库中存取数据、13.3 小结。本页按公开目录独立重构，不复制原书正文；每个条目都进入输入、机制、故障、版本和关闭证据链。

## 练习


## 小结

- MongoDB：对应“定义数据契约”的核心观察量。
- MySQL：对应“获取连接”的核心观察量。
- 连接池：对应“参数化查询”的核心观察量。
- 参数化查询：对应“检查结果”的核心观察量。
- 事务：对应“提交或回滚”的核心观察量。
- 4 个本页目录条目已全部映射到执行链与版本账本。
- 最终输出只是证据之一，资源关闭和错误传播同样决定是否通过。

## 术语表


