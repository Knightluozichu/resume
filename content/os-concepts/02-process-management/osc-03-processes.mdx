---
title: "第 3 章 进程"
type: A
section: "第二部分 进程管理"
order: 3
description: "围绕进程概念、状态与 PCB、调度、创建终止、进程间通信、共享内存、消息传递和客户机服务器通信建立执行模型。覆盖28个核心目录坐标。"
demo: true
math: false
sourceUrl: "https://www.cmpedu.com/books/book/5610337.htm"
draft: false
---


## 直觉起点

、、、、共同构成本页坐标。围绕进程概念、状态与
PCB、调度、创建终止、进程间通信、共享内存、消息传递和客户机服务器通信建立执行模型。
先预测下一内核状态再运行样本；最终输出正确不能证明调度、地址、设备、持久化与清理都正确。

## 六阶段资源链

### 1. 创建进程

先标出逻辑流、共享变量与内核保存状态，再用调度事件和 happens-before 边解释交错，分别验证安全、活性、公平与回收。 对“第3章 进程”先在“创建进程”写出输入与预测，再推进到“保存执行上下文”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 2. 保存执行上下文

先标出逻辑流、共享变量与内核保存状态，再用调度事件和 happens-before 边解释交错，分别验证安全、活性、公平与回收。 对“3.1 进程的概念”先在“保存执行上下文”写出输入与预测，再推进到“调度就绪队列”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 3. 调度就绪队列

先标出逻辑流、共享变量与内核保存状态，再用调度事件和 happens-before 边解释交错，分别验证安全、活性、公平与回收。 对“3.1.1 进程概述”先在“调度就绪队列”写出输入与预测，再推进到“执行进程操作”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 4. 执行进程操作

先标出逻辑流、共享变量与内核保存状态，再用调度事件和 happens-before 边解释交错，分别验证安全、活性、公平与回收。 对“3.1.2 进程状态”先在“执行进程操作”写出输入与预测，再推进到“交换进程消息”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 5. 交换进程消息

先标出逻辑流、共享变量与内核保存状态，再用调度事件和 happens-before 边解释交错，分别验证安全、活性、公平与回收。 对“3.1.3 进程控制块”先在“交换进程消息”写出输入与预测，再推进到“终止并回收”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 6. 终止并回收

先标出逻辑流、共享变量与内核保存状态，再用调度事件和 happens-before 边解释交错，分别验证安全、活性、公平与回收。 对“3.1.4 线程”先在“终止并回收”写出输入与预测，再推进到“创建进程”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

## 核心机制深挖

围绕进程概念、状态与 PCB、调度、创建终止、进程间通信、共享内存、消息传递和客户机服务器通信建立执行模型。 每个目录概念都放回“创建进程 → 保存执行上下文 → 调度就绪队列 → 执行进程操作 → 交换进程消息 → 终止并回收”资源状态机。输入可能是系统调用、地址、字节流、中断、资源请求或远程消息；状态可能位于进程表、页表、设备队列、文件元数据、虚拟机或保护域；输出同时说明返回值、所有权与下一阶段可依赖的承诺。

本页不变量是：**进程状态转换合法，父子与资源所有权明确；IPC 定义边界、顺序和错误，终止后内核对象最终被回收。**。正常和错误路径都必须保持它。一次成功只证明一个样本可用，不能证明边界安全；所以每项机制进入正常、边界、失败和恢复四条轨迹，并写清谁检测、谁阻塞或重试、谁逆序清理、谁上报。

用最小工作负载沿“创建进程 → 保存执行上下文 → 调度就绪队列 → 执行进程操作 → 交换进程消息 → 终止并回收”记录系统调用、内核对象和资源状态，再注入单一边界并重放。 运行前把预期写成快照，运行后逐字段比较。并发记录同步边，地址记录位宽权限，I/O 记录缓冲区与完成事件，持久化区分提交与落盘，分布式记录丢失、重复和重排。

注入权限、容量、时序或设备故障，在“创建进程”之后的首个偏离点停止，逆序清理并验证同一输入恢复。 删除故障后以相同输入重放，确认返回值、内核对象、外部结果和清理状态一起恢复。只修正末端输出却留下线程、页、设备请求、文件引用、虚拟状态或权限污染，仍不通过。

第10版使用 Linux、Windows 10、多核、NVM、云和容器作为实例。现代版本会改变实现与性能数值，但进程状态、地址翻译、I/O 分层、持久化、虚拟化隔离和保护域仍可验证。现代对照不能覆盖2018英文原版与2023中文出版版的目录身份。

## 核心目录逐项讲解

### 第3章 进程

先标出逻辑流、共享变量与内核保存状态，再用调度事件和 happens-before 边解释交错，分别验证安全、活性、公平与回收。 对“第3章 进程”先在“创建进程”写出输入与预测，再推进到“保存执行上下文”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 3.1 进程的概念

先标出逻辑流、共享变量与内核保存状态，再用调度事件和 happens-before 边解释交错，分别验证安全、活性、公平与回收。 对“3.1 进程的概念”先在“保存执行上下文”写出输入与预测，再推进到“调度就绪队列”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 3.1.1 进程概述

先标出逻辑流、共享变量与内核保存状态，再用调度事件和 happens-before 边解释交错，分别验证安全、活性、公平与回收。 对“3.1.1 进程概述”先在“调度就绪队列”写出输入与预测，再推进到“执行进程操作”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 3.1.2 进程状态

先标出逻辑流、共享变量与内核保存状态，再用调度事件和 happens-before 边解释交错，分别验证安全、活性、公平与回收。 对“3.1.2 进程状态”先在“执行进程操作”写出输入与预测，再推进到“交换进程消息”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 3.1.3 进程控制块

先标出逻辑流、共享变量与内核保存状态，再用调度事件和 happens-before 边解释交错，分别验证安全、活性、公平与回收。 对“3.1.3 进程控制块”先在“交换进程消息”写出输入与预测，再推进到“终止并回收”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 3.1.4 线程

先标出逻辑流、共享变量与内核保存状态，再用调度事件和 happens-before 边解释交错，分别验证安全、活性、公平与回收。 对“3.1.4 线程”先在“终止并回收”写出输入与预测，再推进到“创建进程”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 3.2 进程调度

先标出逻辑流、共享变量与内核保存状态，再用调度事件和 happens-before 边解释交错，分别验证安全、活性、公平与回收。 对“3.2 进程调度”先在“创建进程”写出输入与预测，再推进到“保存执行上下文”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 3.2.1 调度队列

先标出逻辑流、共享变量与内核保存状态，再用调度事件和 happens-before 边解释交错，分别验证安全、活性、公平与回收。 对“3.2.1 调度队列”先在“保存执行上下文”写出输入与预测，再推进到“调度就绪队列”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 3.2.2 CPU调度

先标出逻辑流、共享变量与内核保存状态，再用调度事件和 happens-before 边解释交错，分别验证安全、活性、公平与回收。 对“3.2.2 CPU调度”先在“调度就绪队列”写出输入与预测，再推进到“执行进程操作”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 3.2.3 上下文切换

围绕进程概念、状态与 PCB、调度、创建终止、进程间通信、共享内存、消息传递和客户机服务器通信建立执行模型。 对“3.2.3 上下文切换”先在“执行进程操作”写出输入与预测，再推进到“交换进程消息”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 3.3 进程操作

先标出逻辑流、共享变量与内核保存状态，再用调度事件和 happens-before 边解释交错，分别验证安全、活性、公平与回收。 对“3.3 进程操作”先在“交换进程消息”写出输入与预测，再推进到“终止并回收”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 3.3.1 进程创建

先标出逻辑流、共享变量与内核保存状态，再用调度事件和 happens-before 边解释交错，分别验证安全、活性、公平与回收。 对“3.3.1 进程创建”先在“终止并回收”写出输入与预测，再推进到“创建进程”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 3.3.2 进程终止

先标出逻辑流、共享变量与内核保存状态，再用调度事件和 happens-before 边解释交错，分别验证安全、活性、公平与回收。 对“3.3.2 进程终止”先在“创建进程”写出输入与预测，再推进到“保存执行上下文”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 3.4 进程间通信

先标出逻辑流、共享变量与内核保存状态，再用调度事件和 happens-before 边解释交错，分别验证安全、活性、公平与回收。 对“3.4 进程间通信”先在“保存执行上下文”写出输入与预测，再推进到“调度就绪队列”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 3.5 共享内存系统的IPC

逐位解释地址，依次检查绑定、映射、权限、驻留和置换；共享、隔离、缺页恢复与释放后的失效都要有证据。 对“3.5 共享内存系统的IPC”先在“调度就绪队列”写出输入与预测，再推进到“执行进程操作”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 3.6 消息传递系统的IPC

围绕进程概念、状态与 PCB、调度、创建终止、进程间通信、共享内存、消息传递和客户机服务器通信建立执行模型。 对“3.6 消息传递系统的IPC”先在“执行进程操作”写出输入与预测，再推进到“交换进程消息”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 3.6.1 命名

定义消息丢失、重复、重排、节点失败和复制一致性语义；名称、身份与资源所有权必须跨节点保持可追溯。 对“3.6.1 命名”先在“交换进程消息”写出输入与预测，再推进到“终止并回收”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 3.6.2 同步

先标出逻辑流、共享变量与内核保存状态，再用调度事件和 happens-before 边解释交错，分别验证安全、活性、公平与回收。 对“3.6.2 同步”先在“终止并回收”写出输入与预测，再推进到“创建进程”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 3.6.3 缓冲

围绕进程概念、状态与 PCB、调度、创建终止、进程间通信、共享内存、消息传递和客户机服务器通信建立执行模型。 对“3.6.3 缓冲”先在“创建进程”写出输入与预测，再推进到“保存执行上下文”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 3.7 IPC系统示例

围绕进程概念、状态与 PCB、调度、创建终止、进程间通信、共享内存、消息传递和客户机服务器通信建立执行模型。 对“3.7 IPC系统示例”先在“保存执行上下文”写出输入与预测，再推进到“调度就绪队列”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 3.7.1 POSIX共享内存

逐位解释地址，依次检查绑定、映射、权限、驻留和置换；共享、隔离、缺页恢复与释放后的失效都要有证据。 对“3.7.1 POSIX共享内存”先在“调度就绪队列”写出输入与预测，再推进到“执行进程操作”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 3.7.2 Mach消息传递

围绕进程概念、状态与 PCB、调度、创建终止、进程间通信、共享内存、消息传递和客户机服务器通信建立执行模型。 对“3.7.2 Mach消息传递”先在“执行进程操作”写出输入与预测，再推进到“交换进程消息”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 3.7.3 Windows

把平台系统调用、内核对象和权限路径回连通用机制，标明版本和实现差异，避免用产品名称替代因果解释。 对“3.7.3 Windows”先在“交换进程消息”写出输入与预测，再推进到“终止并回收”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 3.7.4 管道

围绕进程概念、状态与 PCB、调度、创建终止、进程间通信、共享内存、消息传递和客户机服务器通信建立执行模型。 对“3.7.4 管道”先在“终止并回收”写出输入与预测，再推进到“创建进程”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 3.8 客户机-服务器系统中的通信

把用户请求、保护边界、结构通信与内核状态转移分开记录；接口、机制、策略和诊断责任需要分别说明。 对“3.8 客户机-服务器系统中的通信”先在“创建进程”写出输入与预测，再推进到“保存执行上下文”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 3.8.1 套接字

围绕进程概念、状态与 PCB、调度、创建终止、进程间通信、共享内存、消息传递和客户机服务器通信建立执行模型。 对“3.8.1 套接字”先在“保存执行上下文”写出输入与预测，再推进到“调度就绪队列”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 3.8.2 远程过程调用

定义消息丢失、重复、重排、节点失败和复制一致性语义；名称、身份与资源所有权必须跨节点保持可追溯。 对“3.8.2 远程过程调用”先在“调度就绪队列”写出输入与预测，再推进到“执行进程操作”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 3.9 本章小结

用“进程状态转换合法，父子与资源所有权明确；IPC 定义边界、顺序和错误，终止后内核对象最终被回收。”回查正常、边界、失败和恢复，而不是只重复名词。 对“3.9 本章小结”先在“执行进程操作”写出输入与预测，再推进到“交换进程消息”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

## 状态与失败矩阵

| 对象         | 正常状态               | 边界样本             | 失败证据           | 恢复条件           |
| ------------ | ---------------------- | -------------------- | ------------------ | ------------------ |
| 进程与同步   | 状态转换和同步边明确   | 抢占、竞争、低容量   | 调度与等待图       | 唤醒并回收全部流   |
| 地址与存储   | 映射、权限、元数据有效 | 缺页、空间耗尽、断电 | 页表与日志轨迹     | 重建一致映射或事务 |
| 设备与虚拟化 | 请求和完成一一对应     | 超时、迁移、过量提交 | 控制器与客户机状态 | 清理后相同输入重放 |
| 分布式与安全 | 所有权、身份、协议明确 | 节点失联、越权、重放 | 消息与审计记录     | 恢复隔离和最小权限 |

先预测：若在“调度就绪队列”失败，哪个内核字段应最先变化，后续哪个阶段必须停止提交副作用？写出答案后再操作控件。

## 可重放实验记录

```text
for stage in ["创建进程", "保存执行上下文", "调度就绪队列", "执行进程操作", "交换进程消息", "终止并回收"]:
  expected = predict(stage, kernel_state)
  actual = execute(stage, kernel_state)
  trace({ stage, expected, actual, owner, cleanup })
  stop_at_first_divergence(expected, actual)
```

```yaml
book: 操作系统概念（原书第10版）
edition: 2023-09-25
page: osc-03-processes
formal_scope: 28条
sample: normal | boundary | failure | recovery
predict_before_run: true
replay_same_input: true
```

```text
freeze version, workload, input, and environment
record system calls, kernel state, resources, devices, and synchronization
change exactly one condition and stop at the first divergence
clean up in reverse acquisition order
replay the same input and verify recovery
```

## 常见误区与故障注入


## 四类样本与验收

| 样本 | 注入方式                     | 必查证据                 | 通过条件     |
| ---- | ---------------------------- | ------------------------ | ------------ |
| 正常 | 固定版本、负载和输入         | 全阶段状态与输出         | 与预测一致   |
| 边界 | 极值、空输入、低容量         | 终止、权限与资源         | 不越界不泄漏 |
| 失败 | 错地址、设备超时、断连或竞争 | 首个错误与责任层         | 失败被隔离   |
| 恢复 | 逆序清理后重放               | 状态、输出、持久化和审计 | 回到受控结果 |

## 练习


## 小结

- 进程：连接“创建进程”的请求与内核状态。
- PCB：连接“保存执行上下文”的资源所有权。
- 进程调度：连接“调度就绪队列”的同步或权限边界。
- IPC：连接“执行进程操作”的失败传播。
- 客户机服务器：连接“交换进程消息”的恢复证据。
- 28个核心目录坐标已进入本页或全书资源链，不以泛化主题替代原书章节。

## 术语表


