---
title: "第 8 章 死锁"
type: A
section: "第三部分 进程同步"
order: 8
description: "从系统模型和四个必要条件进入资源图、预防、避免、银行家算法、检测与恢复，并讨论现实系统中的策略选择。覆盖24个核心目录坐标。"
demo: true
math: false
sourceUrl: "https://www.cmpedu.com/books/book/5610337.htm"
draft: false
---

import {
  Osc08DeadlocksMapLab,
  Osc08DeadlocksExperimentLab,
  Osc08DeadlocksEvidenceLab,
} from "@/components/mdx/os-concepts/diagrams/osc-08-deadlocks";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第 8 章 死锁的核心机制，并说出它在中文原书第10版9部分、21章体系中的位置
- 能描述声明资源需求、分配资源实例、构造等待关系、判定安全序列、检测等待循环、抢占终止恢复六个阶段的输入、内核状态、资源所有者与不变量
- 能实现最小可重放实验，注入边界或失败条件并定位首个偏离点
- 能比较正常、边界、失败和恢复轨迹，判断目录概念是否形成可迁移能力

</Objectives>

## 直觉起点

<Term def="死锁是第 8 章 死锁的第一个观察坐标，连接请求、状态和恢复证据。">死锁</Term>、<Term def="资源分配图是第 8 章 死锁的第二个观察坐标，解释资源与责任边界。">资源分配图</Term>、<Term def="安全状态是第 8 章 死锁的第三个观察坐标，需要最小实验验收。">安全状态</Term>、<Term def="银行家算法是第 8 章 死锁的第四个观察坐标，边界会暴露约束。">银行家算法</Term>、<Term def="恢复是第 8 章 死锁的第五个观察坐标，必须定位首个状态分叉。">恢复</Term>
共同构成本页坐标。从系统模型和四个必要条件进入资源图、预防、避免、银行家算法、检测与恢复，并讨论现实系统中的策略选择。
先预测下一内核状态再运行样本；最终输出正确不能证明调度、地址、设备、持久化与清理都正确。

<Osc08DeadlocksMapLab />

## 六阶段资源链

### 1. 声明资源需求

先标出逻辑流、共享变量与内核保存状态，再用调度事件和 happens-before 边解释交错，分别验证安全、活性、公平与回收。 对“第8章 死锁”先在“声明资源需求”写出输入与预测，再推进到“分配资源实例”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 2. 分配资源实例

从系统模型和四个必要条件进入资源图、预防、避免、银行家算法、检测与恢复，并讨论现实系统中的策略选择。 对“8.1 系统模型”先在“分配资源实例”写出输入与预测，再推进到“构造等待关系”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 3. 构造等待关系

先标出逻辑流、共享变量与内核保存状态，再用调度事件和 happens-before 边解释交错，分别验证安全、活性、公平与回收。 对“8.2 多线程应用程序的死锁”先在“构造等待关系”写出输入与预测，再推进到“判定安全序列”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 4. 判定安全序列

先标出逻辑流、共享变量与内核保存状态，再用调度事件和 happens-before 边解释交错，分别验证安全、活性、公平与回收。 对“8.3 死锁特点”先在“判定安全序列”写出输入与预测，再推进到“检测等待循环”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 5. 检测等待循环

从系统模型和四个必要条件进入资源图、预防、避免、银行家算法、检测与恢复，并讨论现实系统中的策略选择。 对“8.3.1 必要条件”先在“检测等待循环”写出输入与预测，再推进到“抢占终止恢复”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 6. 抢占终止恢复

从系统模型和四个必要条件进入资源图、预防、避免、银行家算法、检测与恢复，并讨论现实系统中的策略选择。 对“8.3.2 资源分配图”先在“抢占终止恢复”写出输入与预测，再推进到“声明资源需求”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

## 核心机制深挖

从系统模型和四个必要条件进入资源图、预防、避免、银行家算法、检测与恢复，并讨论现实系统中的策略选择。 每个目录概念都放回“声明资源需求 → 分配资源实例 → 构造等待关系 → 判定安全序列 → 检测等待循环 → 抢占终止恢复”资源状态机。输入可能是系统调用、地址、字节流、中断、资源请求或远程消息；状态可能位于进程表、页表、设备队列、文件元数据、虚拟机或保护域；输出同时说明返回值、所有权与下一阶段可依赖的承诺。

本页不变量是：**资源总量、可用、分配和需求守恒；避免只批准安全请求，检测识别真实等待循环，恢复最终打破循环且清理一致。**。正常和错误路径都必须保持它。一次成功只证明一个样本可用，不能证明边界安全；所以每项机制进入正常、边界、失败和恢复四条轨迹，并写清谁检测、谁阻塞或重试、谁逆序清理、谁上报。

用最小工作负载沿“声明资源需求 → 分配资源实例 → 构造等待关系 → 判定安全序列 → 检测等待循环 → 抢占终止恢复”记录系统调用、内核对象和资源状态，再注入单一边界并重放。 运行前把预期写成快照，运行后逐字段比较。并发记录同步边，地址记录位宽权限，I/O 记录缓冲区与完成事件，持久化区分提交与落盘，分布式记录丢失、重复和重排。

注入权限、容量、时序或设备故障，在“声明资源需求”之后的首个偏离点停止，逆序清理并验证同一输入恢复。 删除故障后以相同输入重放，确认返回值、内核对象、外部结果和清理状态一起恢复。只修正末端输出却留下线程、页、设备请求、文件引用、虚拟状态或权限污染，仍不通过。

第10版使用 Linux、Windows 10、多核、NVM、云和容器作为实例。现代版本会改变实现与性能数值，但进程状态、地址翻译、I/O 分层、持久化、虚拟化隔离和保护域仍可验证。现代对照不能覆盖2018英文原版与2023中文出版版的目录身份。

## 核心目录逐项讲解

### 第8章 死锁

先标出逻辑流、共享变量与内核保存状态，再用调度事件和 happens-before 边解释交错，分别验证安全、活性、公平与回收。 对“第8章 死锁”先在“声明资源需求”写出输入与预测，再推进到“分配资源实例”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 8.1 系统模型

从系统模型和四个必要条件进入资源图、预防、避免、银行家算法、检测与恢复，并讨论现实系统中的策略选择。 对“8.1 系统模型”先在“分配资源实例”写出输入与预测，再推进到“构造等待关系”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 8.2 多线程应用程序的死锁

先标出逻辑流、共享变量与内核保存状态，再用调度事件和 happens-before 边解释交错，分别验证安全、活性、公平与回收。 对“8.2 多线程应用程序的死锁”先在“构造等待关系”写出输入与预测，再推进到“判定安全序列”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 8.3 死锁特点

先标出逻辑流、共享变量与内核保存状态，再用调度事件和 happens-before 边解释交错，分别验证安全、活性、公平与回收。 对“8.3 死锁特点”先在“判定安全序列”写出输入与预测，再推进到“检测等待循环”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 8.3.1 必要条件

从系统模型和四个必要条件进入资源图、预防、避免、银行家算法、检测与恢复，并讨论现实系统中的策略选择。 对“8.3.1 必要条件”先在“检测等待循环”写出输入与预测，再推进到“抢占终止恢复”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 8.3.2 资源分配图

从系统模型和四个必要条件进入资源图、预防、避免、银行家算法、检测与恢复，并讨论现实系统中的策略选择。 对“8.3.2 资源分配图”先在“抢占终止恢复”写出输入与预测，再推进到“声明资源需求”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 8.4 死锁处理方法

先标出逻辑流、共享变量与内核保存状态，再用调度事件和 happens-before 边解释交错，分别验证安全、活性、公平与回收。 对“8.4 死锁处理方法”先在“声明资源需求”写出输入与预测，再推进到“分配资源实例”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 8.5 死锁预防

先标出逻辑流、共享变量与内核保存状态，再用调度事件和 happens-before 边解释交错，分别验证安全、活性、公平与回收。 对“8.5 死锁预防”先在“分配资源实例”写出输入与预测，再推进到“构造等待关系”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 8.5.1 互斥

从系统模型和四个必要条件进入资源图、预防、避免、银行家算法、检测与恢复，并讨论现实系统中的策略选择。 对“8.5.1 互斥”先在“构造等待关系”写出输入与预测，再推进到“判定安全序列”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 8.5.2 占有并等待

从系统模型和四个必要条件进入资源图、预防、避免、银行家算法、检测与恢复，并讨论现实系统中的策略选择。 对“8.5.2 占有并等待”先在“判定安全序列”写出输入与预测，再推进到“检测等待循环”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 8.5.3 非抢占

从系统模型和四个必要条件进入资源图、预防、避免、银行家算法、检测与恢复，并讨论现实系统中的策略选择。 对“8.5.3 非抢占”先在“检测等待循环”写出输入与预测，再推进到“抢占终止恢复”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 8.5.4 循环等待

从系统模型和四个必要条件进入资源图、预防、避免、银行家算法、检测与恢复，并讨论现实系统中的策略选择。 对“8.5.4 循环等待”先在“抢占终止恢复”写出输入与预测，再推进到“声明资源需求”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 8.6 死锁避免

先标出逻辑流、共享变量与内核保存状态，再用调度事件和 happens-before 边解释交错，分别验证安全、活性、公平与回收。 对“8.6 死锁避免”先在“声明资源需求”写出输入与预测，再推进到“分配资源实例”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 8.6.1 安全状态

从主体、对象、保护域和威胁模型开始，分别验证认证、授权、机密性、完整性与审计，最小权限不能由单层防线替代。 对“8.6.1 安全状态”先在“分配资源实例”写出输入与预测，再推进到“构造等待关系”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 8.6.2 资源分配图算法

从系统模型和四个必要条件进入资源图、预防、避免、银行家算法、检测与恢复，并讨论现实系统中的策略选择。 对“8.6.2 资源分配图算法”先在“构造等待关系”写出输入与预测，再推进到“判定安全序列”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 8.6.3 银行家算法

从系统模型和四个必要条件进入资源图、预防、避免、银行家算法、检测与恢复，并讨论现实系统中的策略选择。 对“8.6.3 银行家算法”先在“判定安全序列”写出输入与预测，再推进到“检测等待循环”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 8.7 死锁检测

先标出逻辑流、共享变量与内核保存状态，再用调度事件和 happens-before 边解释交错，分别验证安全、活性、公平与回收。 对“8.7 死锁检测”先在“检测等待循环”写出输入与预测，再推进到“抢占终止恢复”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 8.7.1 每种资源类型只有单个实例

从系统模型和四个必要条件进入资源图、预防、避免、银行家算法、检测与恢复，并讨论现实系统中的策略选择。 对“8.7.1 每种资源类型只有单个实例”先在“抢占终止恢复”写出输入与预测，再推进到“声明资源需求”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 8.7.2 每种资源类型可有多个实例

从系统模型和四个必要条件进入资源图、预防、避免、银行家算法、检测与恢复，并讨论现实系统中的策略选择。 对“8.7.2 每种资源类型可有多个实例”先在“声明资源需求”写出输入与预测，再推进到“分配资源实例”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 8.7.3 检测算法的使用

从系统模型和四个必要条件进入资源图、预防、避免、银行家算法、检测与恢复，并讨论现实系统中的策略选择。 对“8.7.3 检测算法的使用”先在“分配资源实例”写出输入与预测，再推进到“构造等待关系”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 8.8 死锁恢复

先标出逻辑流、共享变量与内核保存状态，再用调度事件和 happens-before 边解释交错，分别验证安全、活性、公平与回收。 对“8.8 死锁恢复”先在“构造等待关系”写出输入与预测，再推进到“判定安全序列”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 8.8.1 进程与线程的中止

先标出逻辑流、共享变量与内核保存状态，再用调度事件和 happens-before 边解释交错，分别验证安全、活性、公平与回收。 对“8.8.1 进程与线程的中止”先在“判定安全序列”写出输入与预测，再推进到“检测等待循环”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 8.8.2 资源抢占

从系统模型和四个必要条件进入资源图、预防、避免、银行家算法、检测与恢复，并讨论现实系统中的策略选择。 对“8.8.2 资源抢占”先在“检测等待循环”写出输入与预测，再推进到“抢占终止恢复”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 8.9 本章小结

用“资源总量、可用、分配和需求守恒；避免只批准安全请求，检测识别真实等待循环，恢复最终打破循环且清理一致。”回查正常、边界、失败和恢复，而不是只重复名词。 对“8.9 本章小结”先在“抢占终止恢复”写出输入与预测，再推进到“声明资源需求”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

## 状态与失败矩阵

| 对象         | 正常状态               | 边界样本             | 失败证据           | 恢复条件           |
| ------------ | ---------------------- | -------------------- | ------------------ | ------------------ |
| 进程与同步   | 状态转换和同步边明确   | 抢占、竞争、低容量   | 调度与等待图       | 唤醒并回收全部流   |
| 地址与存储   | 映射、权限、元数据有效 | 缺页、空间耗尽、断电 | 页表与日志轨迹     | 重建一致映射或事务 |
| 设备与虚拟化 | 请求和完成一一对应     | 超时、迁移、过量提交 | 控制器与客户机状态 | 清理后相同输入重放 |
| 分布式与安全 | 所有权、身份、协议明确 | 节点失联、越权、重放 | 消息与审计记录     | 恢复隔离和最小权限 |

## 分步可视实验

<Stepper>
  <Step title="1. 建立资源预测">
    写出声明资源需求到分配资源实例之间的输入、内核对象、所有者和失败出口。
    <Osc08DeadlocksMapLab />
  </Step>
  <Step title="2. 注入单一边界">
    只改变一个容量、权限、设备或时序条件，在首个偏离处停止。
    <Osc08DeadlocksExperimentLab />
  </Step>
  <Step title="3. 清理并恢复重放">
    删除故障，用同一输入核对状态、结果和资源回收。
    <Osc08DeadlocksEvidenceLab />
  </Step>
</Stepper>

先预测：若在“构造等待关系”失败，哪个内核字段应最先变化，后续哪个阶段必须停止提交副作用？写出答案后再操作控件。

## 可重放实验记录

```text
for stage in ["声明资源需求", "分配资源实例", "构造等待关系", "判定安全序列", "检测等待循环", "抢占终止恢复"]:
  expected = predict(stage, kernel_state)
  actual = execute(stage, kernel_state)
  trace({ stage, expected, actual, owner, cleanup })
  stop_at_first_divergence(expected, actual)
```

```yaml
book: 操作系统概念（原书第10版）
edition: 2023-09-25
page: osc-08-deadlocks
formal_scope: 24条
sample: normal | boundary | failure | recovery
predict_before_run: true
replay_same_input: true
```

```text
freeze version, workload, input, and environment
record system calls, kernel state, resources, devices, and synchronization
change exactly one condition and stop at the first divergence
clean up in reverse acquisition order
replay the same input and verify recovery
```

## 常见误区与故障注入

<Callout type="trap">
  **误区一：应用结果正确就代表系统资源正确。**
  中间仍可能有竞争、脏映射、丢中断、半提交元数据、泄漏对象或越权，必须检查首个内核状态变化和清理。
</Callout>

<Callout type="trap">
  **误区二：用几个主题页代替恐龙书第10版。**
  原书有9部分21章，分开讲进程、线程、调度、两章同步、三章文件系统、安全、保护、虚拟机、分布式及两套案例，不能合并丢失。
</Callout>

## 四类样本与验收

| 样本 | 注入方式                     | 必查证据                 | 通过条件     |
| ---- | ---------------------------- | ------------------------ | ------------ |
| 正常 | 固定版本、负载和输入         | 全阶段状态与输出         | 与预测一致   |
| 边界 | 极值、空输入、低容量         | 终止、权限与资源         | 不越界不泄漏 |
| 失败 | 错地址、设备超时、断连或竞争 | 首个错误与责任层         | 失败被隔离   |
| 恢复 | 逆序清理后重放               | 状态、输出、持久化和审计 | 回到受控结果 |

## 练习

<Exercises>

**问题 1** 为什么不能只看最终输出验收第 8 章 死锁？

<Answer>
  最终输出会隐藏调度、地址映射、设备队列、持久化、所有权和权限。必须先预测，保存首个偏离点，逆序清理后以同一输入重放。
</Answer>

**问题 2** 如何把“第8章 死锁”变成可执行证据？

<Answer>
  沿“声明资源需求、分配资源实例、构造等待关系、判定安全序列、检测等待循环、抢占终止恢复”登记输入、内核状态、责任接口、边界、失败出口和恢复条件，让四类样本都留下可比较快照。
</Answer>

**问题 3** 本页最小通过条件是什么？

<Answer>
  资源总量、可用、分配和需求守恒；避免只批准安全请求，检测识别真实等待循环，恢复最终打破循环且清理一致。
  异常路径不污染下一次运行，所有资源有唯一逆序清理路径，恢复轨迹重新到达正常状态。
</Answer>

</Exercises>

## 小结

- 死锁：连接“声明资源需求”的请求与内核状态。
- 资源分配图：连接“分配资源实例”的资源所有权。
- 安全状态：连接“构造等待关系”的同步或权限边界。
- 银行家算法：连接“判定安全序列”的失败传播。
- 恢复：连接“检测等待循环”的恢复证据。
- 24个核心目录坐标已进入本页或全书资源链，不以泛化主题替代原书章节。

## 术语表

<Glossary>
  <GlossaryItem term="死锁">
    死锁对应“声明资源需求”，连接请求、内核状态、资源边界、失败传播和恢复结果。
  </GlossaryItem>
  <GlossaryItem term="资源分配图">
    资源分配图对应“分配资源实例”，连接请求、内核状态、资源边界、失败传播和恢复结果。
  </GlossaryItem>
  <GlossaryItem term="安全状态">
    安全状态对应“构造等待关系”，连接请求、内核状态、资源边界、失败传播和恢复结果。
  </GlossaryItem>
  <GlossaryItem term="银行家算法">
    银行家算法对应“判定安全序列”，连接请求、内核状态、资源边界、失败传播和恢复结果。
  </GlossaryItem>
  <GlossaryItem term="恢复">
    恢复对应“检测等待循环”，连接请求、内核状态、资源边界、失败传播和恢复结果。
  </GlossaryItem>
</Glossary>

<Attribution
  adaptedFrom="Silberschatz、Galvin、Gagne · 操作系统概念（原书第10版）· 郑扣根、唐杰、李善平译 · 机械工业出版社 · 2023-09-25 · 出版社完整目录独立教学重构"
  adaptedUrl="https://www.cmpedu.com/books/book/5610337.htm"
/>
