---
title: "第 10 章 虚拟内存"
type: A
section: "第四部分 内存管理"
order: 10
description: "从请求调页、写时复制和页面置换进入帧分配、抖动、工作集、内存压缩、内核分配及 Linux/Windows/Solaris 实例。覆盖42个核心目录坐标。"
demo: true
math: false
sourceUrl: "https://www.cmpedu.com/books/book/5610337.htm"
draft: false
---


## 直觉起点

、、、、共同构成本页坐标。从请求调页、写时复制和页面置换进入帧分配、抖动、工作集、内存压缩、内核分配及
Linux/Windows/Solaris 实例。
先预测下一内核状态再运行样本；最终输出正确不能证明调度、地址、设备、持久化与清理都正确。

## 六阶段资源链

### 1. 访问虚拟页面

逐位解释地址，依次检查绑定、映射、权限、驻留和置换；共享、隔离、缺页恢复与释放后的失效都要有证据。 对“第10章 虚拟内存”先在“访问虚拟页面”写出输入与预测，再推进到“检测缺页”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 2. 检测缺页

从请求调页、写时复制和页面置换进入帧分配、抖动、工作集、内存压缩、内核分配及 Linux/Windows/Solaris 实例。 对“10.1 背景”先在“检测缺页”写出输入与预测，再推进到“选择物理帧”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 3. 选择物理帧

逐位解释地址，依次检查绑定、映射、权限、驻留和置换；共享、隔离、缺页恢复与释放后的失效都要有证据。 对“10.2 请求调页”先在“选择物理帧”写出输入与预测，再推进到“执行页面置换”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 4. 执行页面置换

从请求调页、写时复制和页面置换进入帧分配、抖动、工作集、内存压缩、内核分配及 Linux/Windows/Solaris 实例。 对“10.2.1 基本概念”先在“执行页面置换”写出输入与预测，再推进到“更新驻留集合”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 5. 更新驻留集合

逐位解释地址，依次检查绑定、映射、权限、驻留和置换；共享、隔离、缺页恢复与释放后的失效都要有证据。 对“10.2.2 空闲帧列表”先在“更新驻留集合”写出输入与预测，再推进到“恢复故障指令”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 6. 恢复故障指令

逐位解释地址，依次检查绑定、映射、权限、驻留和置换；共享、隔离、缺页恢复与释放后的失效都要有证据。 对“10.2.3 请求调页的性能”先在“恢复故障指令”写出输入与预测，再推进到“访问虚拟页面”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

## 核心机制深挖

从请求调页、写时复制和页面置换进入帧分配、抖动、工作集、内存压缩、内核分配及 Linux/Windows/Solaris 实例。 每个目录概念都放回“访问虚拟页面 → 检测缺页 → 选择物理帧 → 执行页面置换 → 更新驻留集合 → 恢复故障指令”资源状态机。输入可能是系统调用、地址、字节流、中断、资源请求或远程消息；状态可能位于进程表、页表、设备队列、文件元数据、虚拟机或保护域；输出同时说明返回值、所有权与下一阶段可依赖的承诺。

本页不变量是：**缺页处理可重启且不丢失程序状态；置换遵守脏页与固定页约束，驻留集和负载控制避免持续抖动。**。正常和错误路径都必须保持它。一次成功只证明一个样本可用，不能证明边界安全；所以每项机制进入正常、边界、失败和恢复四条轨迹，并写清谁检测、谁阻塞或重试、谁逆序清理、谁上报。

用最小工作负载沿“访问虚拟页面 → 检测缺页 → 选择物理帧 → 执行页面置换 → 更新驻留集合 → 恢复故障指令”记录系统调用、内核对象和资源状态，再注入单一边界并重放。 运行前把预期写成快照，运行后逐字段比较。并发记录同步边，地址记录位宽权限，I/O 记录缓冲区与完成事件，持久化区分提交与落盘，分布式记录丢失、重复和重排。

注入权限、容量、时序或设备故障，在“访问虚拟页面”之后的首个偏离点停止，逆序清理并验证同一输入恢复。 删除故障后以相同输入重放，确认返回值、内核对象、外部结果和清理状态一起恢复。只修正末端输出却留下线程、页、设备请求、文件引用、虚拟状态或权限污染，仍不通过。

第10版使用 Linux、Windows 10、多核、NVM、云和容器作为实例。现代版本会改变实现与性能数值，但进程状态、地址翻译、I/O 分层、持久化、虚拟化隔离和保护域仍可验证。现代对照不能覆盖2018英文原版与2023中文出版版的目录身份。

## 核心目录逐项讲解

### 第10章 虚拟内存

逐位解释地址，依次检查绑定、映射、权限、驻留和置换；共享、隔离、缺页恢复与释放后的失效都要有证据。 对“第10章 虚拟内存”先在“访问虚拟页面”写出输入与预测，再推进到“检测缺页”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 10.1 背景

从请求调页、写时复制和页面置换进入帧分配、抖动、工作集、内存压缩、内核分配及 Linux/Windows/Solaris 实例。 对“10.1 背景”先在“检测缺页”写出输入与预测，再推进到“选择物理帧”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 10.2 请求调页

逐位解释地址，依次检查绑定、映射、权限、驻留和置换；共享、隔离、缺页恢复与释放后的失效都要有证据。 对“10.2 请求调页”先在“选择物理帧”写出输入与预测，再推进到“执行页面置换”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 10.2.1 基本概念

从请求调页、写时复制和页面置换进入帧分配、抖动、工作集、内存压缩、内核分配及 Linux/Windows/Solaris 实例。 对“10.2.1 基本概念”先在“执行页面置换”写出输入与预测，再推进到“更新驻留集合”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 10.2.2 空闲帧列表

逐位解释地址，依次检查绑定、映射、权限、驻留和置换；共享、隔离、缺页恢复与释放后的失效都要有证据。 对“10.2.2 空闲帧列表”先在“更新驻留集合”写出输入与预测，再推进到“恢复故障指令”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 10.2.3 请求调页的性能

逐位解释地址，依次检查绑定、映射、权限、驻留和置换；共享、隔离、缺页恢复与释放后的失效都要有证据。 对“10.2.3 请求调页的性能”先在“恢复故障指令”写出输入与预测，再推进到“访问虚拟页面”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 10.3 写时复制

从请求调页、写时复制和页面置换进入帧分配、抖动、工作集、内存压缩、内核分配及 Linux/Windows/Solaris 实例。 对“10.3 写时复制”先在“访问虚拟页面”写出输入与预测，再推进到“检测缺页”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 10.4 页面置换

逐位解释地址，依次检查绑定、映射、权限、驻留和置换；共享、隔离、缺页恢复与释放后的失效都要有证据。 对“10.4 页面置换”先在“检测缺页”写出输入与预测，再推进到“选择物理帧”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 10.4.1 基本页面置换

逐位解释地址，依次检查绑定、映射、权限、驻留和置换；共享、隔离、缺页恢复与释放后的失效都要有证据。 对“10.4.1 基本页面置换”先在“选择物理帧”写出输入与预测，再推进到“执行页面置换”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 10.4.2 FIFO页面置换

逐位解释地址，依次检查绑定、映射、权限、驻留和置换；共享、隔离、缺页恢复与释放后的失效都要有证据。 对“10.4.2 FIFO页面置换”先在“执行页面置换”写出输入与预测，再推进到“更新驻留集合”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 10.4.3 最优页面置换

逐位解释地址，依次检查绑定、映射、权限、驻留和置换；共享、隔离、缺页恢复与释放后的失效都要有证据。 对“10.4.3 最优页面置换”先在“更新驻留集合”写出输入与预测，再推进到“恢复故障指令”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 10.4.4 LRU页面置换

逐位解释地址，依次检查绑定、映射、权限、驻留和置换；共享、隔离、缺页恢复与释放后的失效都要有证据。 对“10.4.4 LRU页面置换”先在“恢复故障指令”写出输入与预测，再推进到“访问虚拟页面”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 10.4.5 近似LRU页面置换

逐位解释地址，依次检查绑定、映射、权限、驻留和置换；共享、隔离、缺页恢复与释放后的失效都要有证据。 对“10.4.5 近似LRU页面置换”先在“访问虚拟页面”写出输入与预测，再推进到“检测缺页”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 10.4.6 基于计数的页面置换

逐位解释地址，依次检查绑定、映射、权限、驻留和置换；共享、隔离、缺页恢复与释放后的失效都要有证据。 对“10.4.6 基于计数的页面置换”先在“检测缺页”写出输入与预测，再推进到“选择物理帧”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 10.4.7 页面缓冲算法

逐位解释地址，依次检查绑定、映射、权限、驻留和置换；共享、隔离、缺页恢复与释放后的失效都要有证据。 对“10.4.7 页面缓冲算法”先在“选择物理帧”写出输入与预测，再推进到“执行页面置换”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 10.4.8 应用程序与页面置换

逐位解释地址，依次检查绑定、映射、权限、驻留和置换；共享、隔离、缺页恢复与释放后的失效都要有证据。 对“10.4.8 应用程序与页面置换”先在“执行页面置换”写出输入与预测，再推进到“更新驻留集合”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 10.5 帧分配

逐位解释地址，依次检查绑定、映射、权限、驻留和置换；共享、隔离、缺页恢复与释放后的失效都要有证据。 对“10.5 帧分配”先在“更新驻留集合”写出输入与预测，再推进到“恢复故障指令”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 10.5.1 帧的最小数

逐位解释地址，依次检查绑定、映射、权限、驻留和置换；共享、隔离、缺页恢复与释放后的失效都要有证据。 对“10.5.1 帧的最小数”先在“恢复故障指令”写出输入与预测，再推进到“访问虚拟页面”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 10.5.2 分配算法

从请求调页、写时复制和页面置换进入帧分配、抖动、工作集、内存压缩、内核分配及 Linux/Windows/Solaris 实例。 对“10.5.2 分配算法”先在“访问虚拟页面”写出输入与预测，再推进到“检测缺页”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 10.5.3 全局分配与局部分配

从请求调页、写时复制和页面置换进入帧分配、抖动、工作集、内存压缩、内核分配及 Linux/Windows/Solaris 实例。 对“10.5.3 全局分配与局部分配”先在“检测缺页”写出输入与预测，再推进到“选择物理帧”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 10.5.4 非均匀内存访问

逐位解释地址，依次检查绑定、映射、权限、驻留和置换；共享、隔离、缺页恢复与释放后的失效都要有证据。 对“10.5.4 非均匀内存访问”先在“选择物理帧”写出输入与预测，再推进到“执行页面置换”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 10.6 抖动

从请求调页、写时复制和页面置换进入帧分配、抖动、工作集、内存压缩、内核分配及 Linux/Windows/Solaris 实例。 对“10.6 抖动”先在“执行页面置换”写出输入与预测，再推进到“更新驻留集合”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 10.6.1 抖动的原因

从请求调页、写时复制和页面置换进入帧分配、抖动、工作集、内存压缩、内核分配及 Linux/Windows/Solaris 实例。 对“10.6.1 抖动的原因”先在“更新驻留集合”写出输入与预测，再推进到“恢复故障指令”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 10.6.2 工作集模型

逐位解释地址，依次检查绑定、映射、权限、驻留和置换；共享、隔离、缺页恢复与释放后的失效都要有证据。 对“10.6.2 工作集模型”先在“恢复故障指令”写出输入与预测，再推进到“访问虚拟页面”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 10.6.3 缺页错误频率

逐位解释地址，依次检查绑定、映射、权限、驻留和置换；共享、隔离、缺页恢复与释放后的失效都要有证据。 对“10.6.3 缺页错误频率”先在“访问虚拟页面”写出输入与预测，再推进到“检测缺页”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 10.6.4 当前做法

从请求调页、写时复制和页面置换进入帧分配、抖动、工作集、内存压缩、内核分配及 Linux/Windows/Solaris 实例。 对“10.6.4 当前做法”先在“检测缺页”写出输入与预测，再推进到“选择物理帧”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 10.7 内存压缩

逐位解释地址，依次检查绑定、映射、权限、驻留和置换；共享、隔离、缺页恢复与释放后的失效都要有证据。 对“10.7 内存压缩”先在“选择物理帧”写出输入与预测，再推进到“执行页面置换”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 10.8 分配内核内存

逐位解释地址，依次检查绑定、映射、权限、驻留和置换；共享、隔离、缺页恢复与释放后的失效都要有证据。 对“10.8 分配内核内存”先在“执行页面置换”写出输入与预测，再推进到“更新驻留集合”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 10.8.1 伙伴系统

从请求调页、写时复制和页面置换进入帧分配、抖动、工作集、内存压缩、内核分配及 Linux/Windows/Solaris 实例。 对“10.8.1 伙伴系统”先在“更新驻留集合”写出输入与预测，再推进到“恢复故障指令”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 10.8.2 slab分配

从请求调页、写时复制和页面置换进入帧分配、抖动、工作集、内存压缩、内核分配及 Linux/Windows/Solaris 实例。 对“10.8.2 slab分配”先在“恢复故障指令”写出输入与预测，再推进到“访问虚拟页面”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 10.9 其他考虑因素

从请求调页、写时复制和页面置换进入帧分配、抖动、工作集、内存压缩、内核分配及 Linux/Windows/Solaris 实例。 对“10.9 其他考虑因素”先在“访问虚拟页面”写出输入与预测，再推进到“检测缺页”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 10.9.1 预调页面

逐位解释地址，依次检查绑定、映射、权限、驻留和置换；共享、隔离、缺页恢复与释放后的失效都要有证据。 对“10.9.1 预调页面”先在“检测缺页”写出输入与预测，再推进到“选择物理帧”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 10.9.2 页面大小

逐位解释地址，依次检查绑定、映射、权限、驻留和置换；共享、隔离、缺页恢复与释放后的失效都要有证据。 对“10.9.2 页面大小”先在“选择物理帧”写出输入与预测，再推进到“执行页面置换”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 10.9.3 TLB范围

逐位解释地址，依次检查绑定、映射、权限、驻留和置换；共享、隔离、缺页恢复与释放后的失效都要有证据。 对“10.9.3 TLB范围”先在“执行页面置换”写出输入与预测，再推进到“更新驻留集合”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 10.9.4 倒置页表

逐位解释地址，依次检查绑定、映射、权限、驻留和置换；共享、隔离、缺页恢复与释放后的失效都要有证据。 对“10.9.4 倒置页表”先在“更新驻留集合”写出输入与预测，再推进到“恢复故障指令”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 10.9.5 程序结构

把用户请求、保护边界、结构通信与内核状态转移分开记录；接口、机制、策略和诊断责任需要分别说明。 对“10.9.5 程序结构”先在“恢复故障指令”写出输入与预测，再推进到“访问虚拟页面”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 10.9.6 I/O联锁与页面锁定

先标出逻辑流、共享变量与内核保存状态，再用调度事件和 happens-before 边解释交错，分别验证安全、活性、公平与回收。 对“10.9.6 I/O联锁与页面锁定”先在“访问虚拟页面”写出输入与预测，再推进到“检测缺页”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 10.10 操作系统示例

从请求调页、写时复制和页面置换进入帧分配、抖动、工作集、内存压缩、内核分配及 Linux/Windows/Solaris 实例。 对“10.10 操作系统示例”先在“检测缺页”写出输入与预测，再推进到“选择物理帧”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 10.10.1 Linux

把平台系统调用、内核对象和权限路径回连通用机制，标明版本和实现差异，避免用产品名称替代因果解释。 对“10.10.1 Linux”先在“选择物理帧”写出输入与预测，再推进到“执行页面置换”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 10.10.2 Windows

把平台系统调用、内核对象和权限路径回连通用机制，标明版本和实现差异，避免用产品名称替代因果解释。 对“10.10.2 Windows”先在“执行页面置换”写出输入与预测，再推进到“更新驻留集合”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 10.10.3 Solaris

从请求调页、写时复制和页面置换进入帧分配、抖动、工作集、内存压缩、内核分配及 Linux/Windows/Solaris 实例。 对“10.10.3 Solaris”先在“更新驻留集合”写出输入与预测，再推进到“恢复故障指令”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 10.11 本章小结

用“缺页处理可重启且不丢失程序状态；置换遵守脏页与固定页约束，驻留集和负载控制避免持续抖动。”回查正常、边界、失败和恢复，而不是只重复名词。 对“10.11 本章小结”先在“恢复故障指令”写出输入与预测，再推进到“访问虚拟页面”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

## 状态与失败矩阵

| 对象         | 正常状态               | 边界样本             | 失败证据           | 恢复条件           |
| ------------ | ---------------------- | -------------------- | ------------------ | ------------------ |
| 进程与同步   | 状态转换和同步边明确   | 抢占、竞争、低容量   | 调度与等待图       | 唤醒并回收全部流   |
| 地址与存储   | 映射、权限、元数据有效 | 缺页、空间耗尽、断电 | 页表与日志轨迹     | 重建一致映射或事务 |
| 设备与虚拟化 | 请求和完成一一对应     | 超时、迁移、过量提交 | 控制器与客户机状态 | 清理后相同输入重放 |
| 分布式与安全 | 所有权、身份、协议明确 | 节点失联、越权、重放 | 消息与审计记录     | 恢复隔离和最小权限 |

先预测：若在“选择物理帧”失败，哪个内核字段应最先变化，后续哪个阶段必须停止提交副作用？写出答案后再操作控件。

## 可重放实验记录

```text
for stage in ["访问虚拟页面", "检测缺页", "选择物理帧", "执行页面置换", "更新驻留集合", "恢复故障指令"]:
  expected = predict(stage, kernel_state)
  actual = execute(stage, kernel_state)
  trace({ stage, expected, actual, owner, cleanup })
  stop_at_first_divergence(expected, actual)
```

```yaml
book: 操作系统概念（原书第10版）
edition: 2023-09-25
page: osc-10-virtual-memory
formal_scope: 42条
sample: normal | boundary | failure | recovery
predict_before_run: true
replay_same_input: true
```

```text
freeze version, workload, input, and environment
record system calls, kernel state, resources, devices, and synchronization
change exactly one condition and stop at the first divergence
clean up in reverse acquisition order
replay the same input and verify recovery
```

## 常见误区与故障注入


## 四类样本与验收

| 样本 | 注入方式                     | 必查证据                 | 通过条件     |
| ---- | ---------------------------- | ------------------------ | ------------ |
| 正常 | 固定版本、负载和输入         | 全阶段状态与输出         | 与预测一致   |
| 边界 | 极值、空输入、低容量         | 终止、权限与资源         | 不越界不泄漏 |
| 失败 | 错地址、设备超时、断连或竞争 | 首个错误与责任层         | 失败被隔离   |
| 恢复 | 逆序清理后重放               | 状态、输出、持久化和审计 | 回到受控结果 |

## 练习


## 小结

- 请求调页：连接“访问虚拟页面”的请求与内核状态。
- 写时复制：连接“检测缺页”的资源所有权。
- 页面置换：连接“选择物理帧”的同步或权限边界。
- 工作集：连接“执行页面置换”的失败传播。
- 内核内存：连接“更新驻留集合”的恢复证据。
- 42个核心目录坐标已进入本页或全书资源链，不以泛化主题替代原书章节。

## 术语表


