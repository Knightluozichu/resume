---
title: "第 18 章 虚拟机"
type: A
section: "第八部分 高级主题"
order: 18
description: "从虚拟机概念、实现和硬件辅助进入操作系统虚拟化、容器、开发环境虚拟化、实时迁移与 VMware。覆盖28个核心目录坐标。"
demo: true
math: false
sourceUrl: "https://www.cmpedu.com/books/book/5610337.htm"
draft: true
---

import {
  Osc18VirtualMachinesMapLab,
  Osc18VirtualMachinesExperimentLab,
  Osc18VirtualMachinesEvidenceLab,
} from "@/components/mdx/os-concepts/diagrams/osc-18-virtual-machines";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第 18 章 虚拟机的核心机制，并说出它在中文原书第10版9部分、21章体系中的位置
- 能描述截获敏感指令、虚拟处理器、映射客户内存、虚拟化 I/O、隔离运行环境、迁移恢复六个阶段的输入、内核状态、资源所有者与不变量
- 能实现最小可重放实验，注入边界或失败条件并定位首个偏离点
- 能比较正常、边界、失败和恢复轨迹，判断目录概念是否形成可迁移能力

</Objectives>

## 直觉起点

<Term def="VMM是第 18 章 虚拟机的第一个观察坐标，连接请求、状态和恢复证据。">VMM</Term>、<Term def="陷入仿真是第 18 章 虚拟机的第二个观察坐标，解释资源与责任边界。">陷入仿真</Term>、<Term def="硬件辅助是第 18 章 虚拟机的第三个观察坐标，需要最小实验验收。">硬件辅助</Term>、<Term def="容器是第 18 章 虚拟机的第四个观察坐标，边界会暴露约束。">容器</Term>、<Term def="实时迁移是第 18 章 虚拟机的第五个观察坐标，必须定位首个状态分叉。">实时迁移</Term>
共同构成本页坐标。从虚拟机概念、实现和硬件辅助进入操作系统虚拟化、容器、开发环境虚拟化、实时迁移与
VMware。
先预测下一内核状态再运行样本；最终输出正确不能证明调度、地址、设备、持久化与清理都正确。

<Osc18VirtualMachinesMapLab />

## 六阶段资源链

### 1. 截获敏感指令

管理程序截获敏感操作并保持等价、隔离和记账；处理器、内存、设备和时间状态共同决定迁移恢复是否完整。 对“第18章 虚拟机”先在“截获敏感指令”写出输入与预测，再推进到“虚拟处理器”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 2. 虚拟处理器

从虚拟机概念、实现和硬件辅助进入操作系统虚拟化、容器、开发环境虚拟化、实时迁移与 VMware。 对“18.1 概述”先在“虚拟处理器”写出输入与预测，再推进到“映射客户内存”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 3. 映射客户内存

从虚拟机概念、实现和硬件辅助进入操作系统虚拟化、容器、开发环境虚拟化、实时迁移与 VMware。 对“18.2 历史”先在“映射客户内存”写出输入与预测，再推进到“虚拟化 I/O”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 4. 虚拟化 I/O

从虚拟机概念、实现和硬件辅助进入操作系统虚拟化、容器、开发环境虚拟化、实时迁移与 VMware。 对“18.3 优点与功能”先在“虚拟化 I/O”写出输入与预测，再推进到“隔离运行环境”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 5. 隔离运行环境

从虚拟机概念、实现和硬件辅助进入操作系统虚拟化、容器、开发环境虚拟化、实时迁移与 VMware。 对“18.4 构建模块”先在“隔离运行环境”写出输入与预测，再推进到“迁移恢复”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 6. 迁移恢复

从虚拟机概念、实现和硬件辅助进入操作系统虚拟化、容器、开发环境虚拟化、实时迁移与 VMware。 对“18.4.1 陷阱模拟”先在“迁移恢复”写出输入与预测，再推进到“截获敏感指令”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

## 核心机制深挖

从虚拟机概念、实现和硬件辅助进入操作系统虚拟化、容器、开发环境虚拟化、实时迁移与 VMware。 每个目录概念都放回“截获敏感指令 → 虚拟处理器 → 映射客户内存 → 虚拟化 I/O → 隔离运行环境 → 迁移恢复”资源状态机。输入可能是系统调用、地址、字节流、中断、资源请求或远程消息；状态可能位于进程表、页表、设备队列、文件元数据、虚拟机或保护域；输出同时说明返回值、所有权与下一阶段可依赖的承诺。

本页不变量是：**客户机体系结构状态等价且相互隔离；处理器、内存、设备和时间状态记账完整，迁移恢复后外部语义一致。**。正常和错误路径都必须保持它。一次成功只证明一个样本可用，不能证明边界安全；所以每项机制进入正常、边界、失败和恢复四条轨迹，并写清谁检测、谁阻塞或重试、谁逆序清理、谁上报。

用最小工作负载沿“截获敏感指令 → 虚拟处理器 → 映射客户内存 → 虚拟化 I/O → 隔离运行环境 → 迁移恢复”记录系统调用、内核对象和资源状态，再注入单一边界并重放。 运行前把预期写成快照，运行后逐字段比较。并发记录同步边，地址记录位宽权限，I/O 记录缓冲区与完成事件，持久化区分提交与落盘，分布式记录丢失、重复和重排。

注入权限、容量、时序或设备故障，在“截获敏感指令”之后的首个偏离点停止，逆序清理并验证同一输入恢复。 删除故障后以相同输入重放，确认返回值、内核对象、外部结果和清理状态一起恢复。只修正末端输出却留下线程、页、设备请求、文件引用、虚拟状态或权限污染，仍不通过。

第10版使用 Linux、Windows 10、多核、NVM、云和容器作为实例。现代版本会改变实现与性能数值，但进程状态、地址翻译、I/O 分层、持久化、虚拟化隔离和保护域仍可验证。现代对照不能覆盖2018英文原版与2023中文出版版的目录身份。

## 核心目录逐项讲解

### 第18章 虚拟机

管理程序截获敏感操作并保持等价、隔离和记账；处理器、内存、设备和时间状态共同决定迁移恢复是否完整。 对“第18章 虚拟机”先在“截获敏感指令”写出输入与预测，再推进到“虚拟处理器”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 18.1 概述

从虚拟机概念、实现和硬件辅助进入操作系统虚拟化、容器、开发环境虚拟化、实时迁移与 VMware。 对“18.1 概述”先在“虚拟处理器”写出输入与预测，再推进到“映射客户内存”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 18.2 历史

从虚拟机概念、实现和硬件辅助进入操作系统虚拟化、容器、开发环境虚拟化、实时迁移与 VMware。 对“18.2 历史”先在“映射客户内存”写出输入与预测，再推进到“虚拟化 I/O”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 18.3 优点与功能

从虚拟机概念、实现和硬件辅助进入操作系统虚拟化、容器、开发环境虚拟化、实时迁移与 VMware。 对“18.3 优点与功能”先在“虚拟化 I/O”写出输入与预测，再推进到“隔离运行环境”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 18.4 构建模块

从虚拟机概念、实现和硬件辅助进入操作系统虚拟化、容器、开发环境虚拟化、实时迁移与 VMware。 对“18.4 构建模块”先在“隔离运行环境”写出输入与预测，再推进到“迁移恢复”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 18.4.1 陷阱模拟

从虚拟机概念、实现和硬件辅助进入操作系统虚拟化、容器、开发环境虚拟化、实时迁移与 VMware。 对“18.4.1 陷阱模拟”先在“迁移恢复”写出输入与预测，再推进到“截获敏感指令”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 18.4.2 二进制翻译

从虚拟机概念、实现和硬件辅助进入操作系统虚拟化、容器、开发环境虚拟化、实时迁移与 VMware。 对“18.4.2 二进制翻译”先在“截获敏感指令”写出输入与预测，再推进到“虚拟处理器”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 18.4.3 硬件协助

从虚拟机概念、实现和硬件辅助进入操作系统虚拟化、容器、开发环境虚拟化、实时迁移与 VMware。 对“18.4.3 硬件协助”先在“虚拟处理器”写出输入与预测，再推进到“映射客户内存”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 18.5 虚拟机的主要类型及其实现

管理程序截获敏感操作并保持等价、隔离和记账；处理器、内存、设备和时间状态共同决定迁移恢复是否完整。 对“18.5 虚拟机的主要类型及其实现”先在“映射客户内存”写出输入与预测，再推进到“虚拟化 I/O”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 18.5.1 虚拟机的生命周期

管理程序截获敏感操作并保持等价、隔离和记账；处理器、内存、设备和时间状态共同决定迁移恢复是否完整。 对“18.5.1 虚拟机的生命周期”先在“虚拟化 I/O”写出输入与预测，再推进到“隔离运行环境”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 18.5.2 类型0虚拟机管理器

管理程序截获敏感操作并保持等价、隔离和记账；处理器、内存、设备和时间状态共同决定迁移恢复是否完整。 对“18.5.2 类型0虚拟机管理器”先在“隔离运行环境”写出输入与预测，再推进到“迁移恢复”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 18.5.3 类型1虚拟机管理器

管理程序截获敏感操作并保持等价、隔离和记账；处理器、内存、设备和时间状态共同决定迁移恢复是否完整。 对“18.5.3 类型1虚拟机管理器”先在“迁移恢复”写出输入与预测，再推进到“截获敏感指令”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 18.5.4 类型2虚拟机管理器

管理程序截获敏感操作并保持等价、隔离和记账；处理器、内存、设备和时间状态共同决定迁移恢复是否完整。 对“18.5.4 类型2虚拟机管理器”先在“截获敏感指令”写出输入与预测，再推进到“虚拟处理器”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 18.5.5 半虚拟化

管理程序截获敏感操作并保持等价、隔离和记账；处理器、内存、设备和时间状态共同决定迁移恢复是否完整。 对“18.5.5 半虚拟化”先在“虚拟处理器”写出输入与预测，再推进到“映射客户内存”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 18.5.6 编程环境的虚拟化

管理程序截获敏感操作并保持等价、隔离和记账；处理器、内存、设备和时间状态共同决定迁移恢复是否完整。 对“18.5.6 编程环境的虚拟化”先在“映射客户内存”写出输入与预测，再推进到“虚拟化 I/O”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 18.5.7 仿真

从虚拟机概念、实现和硬件辅助进入操作系统虚拟化、容器、开发环境虚拟化、实时迁移与 VMware。 对“18.5.7 仿真”先在“虚拟化 I/O”写出输入与预测，再推进到“隔离运行环境”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 18.5.8 应用程序遏制

从虚拟机概念、实现和硬件辅助进入操作系统虚拟化、容器、开发环境虚拟化、实时迁移与 VMware。 对“18.5.8 应用程序遏制”先在“隔离运行环境”写出输入与预测，再推进到“迁移恢复”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 18.6 虚拟化和操作系统组件

管理程序截获敏感操作并保持等价、隔离和记账；处理器、内存、设备和时间状态共同决定迁移恢复是否完整。 对“18.6 虚拟化和操作系统组件”先在“迁移恢复”写出输入与预测，再推进到“截获敏感指令”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 18.6.1 CPU调度

先标出逻辑流、共享变量与内核保存状态，再用调度事件和 happens-before 边解释交错，分别验证安全、活性、公平与回收。 对“18.6.1 CPU调度”先在“截获敏感指令”写出输入与预测，再推进到“虚拟处理器”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 18.6.2 内存管理

逐位解释地址，依次检查绑定、映射、权限、驻留和置换；共享、隔离、缺页恢复与释放后的失效都要有证据。 对“18.6.2 内存管理”先在“虚拟处理器”写出输入与预测，再推进到“映射客户内存”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 18.6.3 I/O

沿接口、请求队列、控制器、数据搬运、完成和持久化追踪，保证缓冲区生命周期、元数据和错误传播一致。 对“18.6.3 I/O”先在“映射客户内存”写出输入与预测，再推进到“虚拟化 I/O”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 18.6.4 存储管理

沿接口、请求队列、控制器、数据搬运、完成和持久化追踪，保证缓冲区生命周期、元数据和错误传播一致。 对“18.6.4 存储管理”先在“虚拟化 I/O”写出输入与预测，再推进到“隔离运行环境”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 18.6.5 实时迁移

管理程序截获敏感操作并保持等价、隔离和记账；处理器、内存、设备和时间状态共同决定迁移恢复是否完整。 对“18.6.5 实时迁移”先在“隔离运行环境”写出输入与预测，再推进到“迁移恢复”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 18.7 实例

从虚拟机概念、实现和硬件辅助进入操作系统虚拟化、容器、开发环境虚拟化、实时迁移与 VMware。 对“18.7 实例”先在“迁移恢复”写出输入与预测，再推进到“截获敏感指令”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 18.7.1 VMware

从虚拟机概念、实现和硬件辅助进入操作系统虚拟化、容器、开发环境虚拟化、实时迁移与 VMware。 对“18.7.1 VMware”先在“截获敏感指令”写出输入与预测，再推进到“虚拟处理器”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 18.7.2 Java虚拟机

管理程序截获敏感操作并保持等价、隔离和记账；处理器、内存、设备和时间状态共同决定迁移恢复是否完整。 对“18.7.2 Java虚拟机”先在“虚拟处理器”写出输入与预测，再推进到“映射客户内存”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 18.8 虚拟化研究

管理程序截获敏感操作并保持等价、隔离和记账；处理器、内存、设备和时间状态共同决定迁移恢复是否完整。 对“18.8 虚拟化研究”先在“映射客户内存”写出输入与预测，再推进到“虚拟化 I/O”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

### 18.9 本章小结

用“客户机体系结构状态等价且相互隔离；处理器、内存、设备和时间状态记账完整，迁移恢复后外部语义一致。”回查正常、边界、失败和恢复，而不是只重复名词。 对“18.9 本章小结”先在“虚拟化 I/O”写出输入与预测，再推进到“隔离运行环境”并比较内核可见状态。固定版本、负载、输入和环境，只改变一个条件；发生偏离时保存首个分叉、责任层与逆序清理结果。

## 状态与失败矩阵

| 对象         | 正常状态               | 边界样本             | 失败证据           | 恢复条件           |
| ------------ | ---------------------- | -------------------- | ------------------ | ------------------ |
| 进程与同步   | 状态转换和同步边明确   | 抢占、竞争、低容量   | 调度与等待图       | 唤醒并回收全部流   |
| 地址与存储   | 映射、权限、元数据有效 | 缺页、空间耗尽、断电 | 页表与日志轨迹     | 重建一致映射或事务 |
| 设备与虚拟化 | 请求和完成一一对应     | 超时、迁移、过量提交 | 控制器与客户机状态 | 清理后相同输入重放 |
| 分布式与安全 | 所有权、身份、协议明确 | 节点失联、越权、重放 | 消息与审计记录     | 恢复隔离和最小权限 |

## 分步可视实验

<Stepper>
  <Step title="1. 建立资源预测">
    写出截获敏感指令到虚拟处理器之间的输入、内核对象、所有者和失败出口。
    <Osc18VirtualMachinesMapLab />
  </Step>
  <Step title="2. 注入单一边界">
    只改变一个容量、权限、设备或时序条件，在首个偏离处停止。
    <Osc18VirtualMachinesExperimentLab />
  </Step>
  <Step title="3. 清理并恢复重放">
    删除故障，用同一输入核对状态、结果和资源回收。
    <Osc18VirtualMachinesEvidenceLab />
  </Step>
</Stepper>

先预测：若在“映射客户内存”失败，哪个内核字段应最先变化，后续哪个阶段必须停止提交副作用？写出答案后再操作控件。

## 可重放实验记录

```text
for stage in ["截获敏感指令", "虚拟处理器", "映射客户内存", "虚拟化 I/O", "隔离运行环境", "迁移恢复"]:
  expected = predict(stage, kernel_state)
  actual = execute(stage, kernel_state)
  trace({ stage, expected, actual, owner, cleanup })
  stop_at_first_divergence(expected, actual)
```

```yaml
book: 操作系统概念（原书第10版）
edition: 2023-09-25
page: osc-18-virtual-machines
formal_scope: 28条
sample: normal | boundary | failure | recovery
predict_before_run: true
replay_same_input: true
```

```text
freeze version, workload, input, and environment
record system calls, kernel state, resources, devices, and synchronization
change exactly one condition and stop at the first divergence
clean up in reverse acquisition order
replay the same input and verify recovery
```

## 常见误区与故障注入

<Callout type="trap">
  **误区一：应用结果正确就代表系统资源正确。**
  中间仍可能有竞争、脏映射、丢中断、半提交元数据、泄漏对象或越权，必须检查首个内核状态变化和清理。
</Callout>

<Callout type="trap">
  **误区二：用几个主题页代替恐龙书第10版。**
  原书有9部分21章，分开讲进程、线程、调度、两章同步、三章文件系统、安全、保护、虚拟机、分布式及两套案例，不能合并丢失。
</Callout>

## 四类样本与验收

| 样本 | 注入方式                     | 必查证据                 | 通过条件     |
| ---- | ---------------------------- | ------------------------ | ------------ |
| 正常 | 固定版本、负载和输入         | 全阶段状态与输出         | 与预测一致   |
| 边界 | 极值、空输入、低容量         | 终止、权限与资源         | 不越界不泄漏 |
| 失败 | 错地址、设备超时、断连或竞争 | 首个错误与责任层         | 失败被隔离   |
| 恢复 | 逆序清理后重放               | 状态、输出、持久化和审计 | 回到受控结果 |

## 练习

<Exercises>

**问题 1** 为什么不能只看最终输出验收第 18 章 虚拟机？

<Answer>
  最终输出会隐藏调度、地址映射、设备队列、持久化、所有权和权限。必须先预测，保存首个偏离点，逆序清理后以同一输入重放。
</Answer>

**问题 2** 如何把“第18章 虚拟机”变成可执行证据？

<Answer>
  沿“截获敏感指令、虚拟处理器、映射客户内存、虚拟化
  I/O、隔离运行环境、迁移恢复”登记输入、内核状态、责任接口、边界、失败出口和恢复条件，让四类样本都留下可比较快照。
</Answer>

**问题 3** 本页最小通过条件是什么？

<Answer>
  客户机体系结构状态等价且相互隔离；处理器、内存、设备和时间状态记账完整，迁移恢复后外部语义一致。
  异常路径不污染下一次运行，所有资源有唯一逆序清理路径，恢复轨迹重新到达正常状态。
</Answer>

</Exercises>

## 小结

- VMM：连接“截获敏感指令”的请求与内核状态。
- 陷入仿真：连接“虚拟处理器”的资源所有权。
- 硬件辅助：连接“映射客户内存”的同步或权限边界。
- 容器：连接“虚拟化 I/O”的失败传播。
- 实时迁移：连接“隔离运行环境”的恢复证据。
- 28个核心目录坐标已进入本页或全书资源链，不以泛化主题替代原书章节。

## 术语表

<Glossary>
  <GlossaryItem term="VMM">
    VMM对应“截获敏感指令”，连接请求、内核状态、资源边界、失败传播和恢复结果。
  </GlossaryItem>
  <GlossaryItem term="陷入仿真">
    陷入仿真对应“虚拟处理器”，连接请求、内核状态、资源边界、失败传播和恢复结果。
  </GlossaryItem>
  <GlossaryItem term="硬件辅助">
    硬件辅助对应“映射客户内存”，连接请求、内核状态、资源边界、失败传播和恢复结果。
  </GlossaryItem>
  <GlossaryItem term="容器">
    容器对应“虚拟化 I/O”，连接请求、内核状态、资源边界、失败传播和恢复结果。
  </GlossaryItem>
  <GlossaryItem term="实时迁移">
    实时迁移对应“隔离运行环境”，连接请求、内核状态、资源边界、失败传播和恢复结果。
  </GlossaryItem>
</Glossary>

<Attribution
  adaptedFrom="Silberschatz、Galvin、Gagne · 操作系统概念（原书第10版）· 郑扣根、唐杰、李善平译 · 机械工业出版社 · 2023-09-25 · 出版社完整目录独立教学重构"
  adaptedUrl="https://www.cmpedu.com/books/book/5610337.htm"
/>
