---
title: "2021纸书版权威学习地图"
description: "依据周志明2021纸书版与作者v1.0.20210629快照：建立5部分、16章、2附录的依赖图，固定2021纸书版与作者v1.0.20210629快照边界"
type: C
section: "凤凰架构：构建可靠的大型分布式系统（2021纸书版 / v1.0.20210629）"
order: 0
draft: true
math: false
sourceUrl: "https://icyfenix.cn/introduction/about-book.html"
demo: true
---

# 2021纸书版权威学习地图

本课程对应周志明著《凤凰架构：构建可靠的大型分布式系统》，机械工业出版社2021年6月出版，409页、约33万字，ISBN 9787111683919。课程以纸书正式目录和作者出版当月发布的v1.0.20210629快照为边界，不把后续持续更新的官网内容倒灌为原书章节。

全书正式分母为5部分、16章、54个正式节与附录A、B，共18个原书单元、77个唯一正式节点；另设学习地图和总复习，共20页。本页逐项覆盖23个目录节点，最终交付完整目录映射、五部分依赖图、版本边界、实验与复习索引。

<Objectives>

- 能解释“2021纸书版权威学习地图”的23个目录节点，并绘制主体、状态、控制点、数据与信任边界。
- 能比较正常、压力、超时、重复、部分失败和恢复轨迹，判断组件完成点与业务完成点。
- 能设计并复现完整目录映射、五部分依赖图、版本边界、实验与复习索引，用日志、指标、追踪、状态和业务对账支持结论。
- 能分析“把持续更新的2026官网目录或泛化分布式主题替代2021纸书正式目录”为何失败，并写出停止、恢复、回退和独立交接条件。

</Objectives>

## 从一个可证伪的架构决策开始

先预测：当请求跨越进程、网络与基础设施边界时，谁接收、谁持有状态、谁重试、谁判定完成，哪里可能超时、重复或部分成功。观察前写下业务不变量和反例，再用同一请求标识贯穿日志、追踪、指标与最终数据。

“组件健康”不是“业务完成”。HTTP 200、事务协调器提交、注册中心有实例、Pod Running或代理配置已下发，都只是局部检查点。验收必须回到“18个原书单元和77个唯一正式节点全部可达，每个结论都能回指纸书目录、作者快照和可复现实验”，并说明该结论在2021纸书版之外是否仍成立。

## 核心词汇与版本边界

<Term def="五部分结构是“2021纸书版权威学习地图”的第1个核心概念，必须说明机制、适用边界、失败反例和可观测证据">五部分结构</Term>、<Term def="77个正式节点是“2021纸书版权威学习地图”的第2个核心概念，必须说明机制、适用边界、失败反例和可观测证据">77个正式节点</Term>、<Term def="2021纸书版是“2021纸书版权威学习地图”的第3个核心概念，必须说明机制、适用边界、失败反例和可观测证据">2021纸书版</Term>、<Term def="作者快照是“2021纸书版权威学习地图”的第4个核心概念，必须说明机制、适用边界、失败反例和可观测证据">作者快照</Term>、<Term def="版本边界是“2021纸书版权威学习地图”的第5个核心概念，必须说明机制、适用边界、失败反例和可观测证据">版本边界</Term>

这些词构成本页词汇。每个词都要回答：它解决什么问题，依赖什么前提，状态由谁持有，失败如何传播，怎样恢复与观测，以及后续技术变化为何不能替换本书的正式分母。

<PhaOfficialLearningMapPathLab />

## 原书目录逐节点重构

### 第一部分 演进中的架构

**目录节点 1/23。** 本部分定义连续的知识责任与进入条件，不能按孤立技术清单阅读。

先把“第一部分 演进中的架构”放回“建立5部分、16章、2附录的依赖图，固定2021纸书版与作者v1.0.20210629快照边界”这条主线：输入来自“本单元输入与版本契约”，输出进入“第二部分 架构师的视角”。画出主体、状态、控制点、数据与信任边界，分别标注同步成功、超时、重复、部分失败和恢复，不用一条正常箭头代替机制。

实验固定2021纸书版、业务样本、请求分布、服务版本和环境，只改变一个条件。比较状态轨迹、吞吐、P95/P99、错误、资源、业务结果与恢复时间，并用“18个原书单元和77个唯一正式节点全部可达，每个结论都能回指纸书目录、作者快照和可复现实验”验收；若证据只能证明组件状态，不能证明业务不变量，本节点仍未完成。

### 第二部分 架构师的视角

**目录节点 2/23。** 本部分定义连续的知识责任与进入条件，不能按孤立技术清单阅读。

先把“第二部分 架构师的视角”放回“建立5部分、16章、2附录的依赖图，固定2021纸书版与作者v1.0.20210629快照边界”这条主线：输入来自“第一部分 演进中的架构”，输出进入“第三部分 分布式的基石”。画出主体、状态、控制点、数据与信任边界，分别标注同步成功、超时、重复、部分失败和恢复，不用一条正常箭头代替机制。

实验固定2021纸书版、业务样本、请求分布、服务版本和环境，只改变一个条件。比较状态轨迹、吞吐、P95/P99、错误、资源、业务结果与恢复时间，并用“18个原书单元和77个唯一正式节点全部可达，每个结论都能回指纸书目录、作者快照和可复现实验”验收；若证据只能证明组件状态，不能证明业务不变量，本节点仍未完成。

### 第三部分 分布式的基石

**目录节点 3/23。** 本部分定义连续的知识责任与进入条件，不能按孤立技术清单阅读。

先把“第三部分 分布式的基石”放回“建立5部分、16章、2附录的依赖图，固定2021纸书版与作者v1.0.20210629快照边界”这条主线：输入来自“第二部分 架构师的视角”，输出进入“第四部分 不可变基础设施”。画出主体、状态、控制点、数据与信任边界，分别标注同步成功、超时、重复、部分失败和恢复，不用一条正常箭头代替机制。

实验固定2021纸书版、业务样本、请求分布、服务版本和环境，只改变一个条件。比较状态轨迹、吞吐、P95/P99、错误、资源、业务结果与恢复时间，并用“18个原书单元和77个唯一正式节点全部可达，每个结论都能回指纸书目录、作者快照和可复现实验”验收；若证据只能证明组件状态，不能证明业务不变量，本节点仍未完成。

### 第四部分 不可变基础设施

**目录节点 4/23。** 本部分定义连续的知识责任与进入条件，不能按孤立技术清单阅读。

先把“第四部分 不可变基础设施”放回“建立5部分、16章、2附录的依赖图，固定2021纸书版与作者v1.0.20210629快照边界”这条主线：输入来自“第三部分 分布式的基石”，输出进入“第五部分 技术方法论”。画出主体、状态、控制点、数据与信任边界，分别标注同步成功、超时、重复、部分失败和恢复，不用一条正常箭头代替机制。

实验固定2021纸书版、业务样本、请求分布、服务版本和环境，只改变一个条件。比较状态轨迹、吞吐、P95/P99、错误、资源、业务结果与恢复时间，并用“18个原书单元和77个唯一正式节点全部可达，每个结论都能回指纸书目录、作者快照和可复现实验”验收；若证据只能证明组件状态，不能证明业务不变量，本节点仍未完成。

### 第五部分 技术方法论

**目录节点 5/23。** 本部分定义连续的知识责任与进入条件，不能按孤立技术清单阅读。

先把“第五部分 技术方法论”放回“建立5部分、16章、2附录的依赖图，固定2021纸书版与作者v1.0.20210629快照边界”这条主线：输入来自“第四部分 不可变基础设施”，输出进入“第1章 服务架构演进史”。画出主体、状态、控制点、数据与信任边界，分别标注同步成功、超时、重复、部分失败和恢复，不用一条正常箭头代替机制。

实验固定2021纸书版、业务样本、请求分布、服务版本和环境，只改变一个条件。比较状态轨迹、吞吐、P95/P99、错误、资源、业务结果与恢复时间，并用“18个原书单元和77个唯一正式节点全部可达，每个结论都能回指纸书目录、作者快照和可复现实验”验收；若证据只能证明组件状态，不能证明业务不变量，本节点仍未完成。

### 第1章 服务架构演进史

**目录节点 6/23。** 从原始分布式、单体、SOA、微服务、后微服务到无服务，解释架构演进解决了什么、又引入了什么；验收不变量是：每次架构选择都同时写出业务驱动力、收益、代价与退出条件，不能把时间顺序误写成优劣排名。

先把“第1章 服务架构演进史”放回“建立5部分、16章、2附录的依赖图，固定2021纸书版与作者v1.0.20210629快照边界”这条主线：输入来自“第五部分 技术方法论”，输出进入“第2章 访问远程服务”。画出主体、状态、控制点、数据与信任边界，分别标注同步成功、超时、重复、部分失败和恢复，不用一条正常箭头代替机制。

实验固定2021纸书版、业务样本、请求分布、服务版本和环境，只改变一个条件。比较状态轨迹、吞吐、P95/P99、错误、资源、业务结果与恢复时间，并用“18个原书单元和77个唯一正式节点全部可达，每个结论都能回指纸书目录、作者快照和可复现实验”验收；若证据只能证明组件状态，不能证明业务不变量，本节点仍未完成。

### 第2章 访问远程服务

**目录节点 7/23。** 比较RPC与REST的抽象、协议、语义和演进方式，建立远程调用绝不等价于本地调用的边界；验收不变量是：契约、超时、幂等、版本与失败语义必须跨客户端和服务端一致，传输成功不等于业务成功。

先把“第2章 访问远程服务”放回“建立5部分、16章、2附录的依赖图，固定2021纸书版与作者v1.0.20210629快照边界”这条主线：输入来自“第1章 服务架构演进史”，输出进入“第3章 事务处理”。画出主体、状态、控制点、数据与信任边界，分别标注同步成功、超时、重复、部分失败和恢复，不用一条正常箭头代替机制。

实验固定2021纸书版、业务样本、请求分布、服务版本和环境，只改变一个条件。比较状态轨迹、吞吐、P95/P99、错误、资源、业务结果与恢复时间，并用“18个原书单元和77个唯一正式节点全部可达，每个结论都能回指纸书目录、作者快照和可复现实验”验收；若证据只能证明组件状态，不能证明业务不变量，本节点仍未完成。

### 第3章 事务处理

**目录节点 8/23。** 从本地ACID走向全局、共享与分布式事务，区分原子提交、补偿与最终一致的保证范围；验收不变量是：业务不变量在成功、超时、重复、回滚失败和恢复后都可对账，协调器状态不能代替业务事实。

先把“第3章 事务处理”放回“建立5部分、16章、2附录的依赖图，固定2021纸书版与作者v1.0.20210629快照边界”这条主线：输入来自“第2章 访问远程服务”，输出进入“第4章 透明多级分流系统”。画出主体、状态、控制点、数据与信任边界，分别标注同步成功、超时、重复、部分失败和恢复，不用一条正常箭头代替机制。

实验固定2021纸书版、业务样本、请求分布、服务版本和环境，只改变一个条件。比较状态轨迹、吞吐、P95/P99、错误、资源、业务结果与恢复时间，并用“18个原书单元和77个唯一正式节点全部可达，每个结论都能回指纸书目录、作者快照和可复现实验”验收；若证据只能证明组件状态，不能证明业务不变量，本节点仍未完成。

### 第4章 透明多级分流系统

**目录节点 9/23。** 沿客户端缓存、DNS、传输、CDN、负载均衡和服务端缓存追踪请求如何逐级分流；验收不变量是：同一资源在每层都有可解释的键、版本、过期、失效和回源路径，命中率提升不能破坏正确性。

先把“第4章 透明多级分流系统”放回“建立5部分、16章、2附录的依赖图，固定2021纸书版与作者v1.0.20210629快照边界”这条主线：输入来自“第3章 事务处理”，输出进入“第5章 架构安全性”。画出主体、状态、控制点、数据与信任边界，分别标注同步成功、超时、重复、部分失败和恢复，不用一条正常箭头代替机制。

实验固定2021纸书版、业务样本、请求分布、服务版本和环境，只改变一个条件。比较状态轨迹、吞吐、P95/P99、错误、资源、业务结果与恢复时间，并用“18个原书单元和77个唯一正式节点全部可达，每个结论都能回指纸书目录、作者快照和可复现实验”验收；若证据只能证明组件状态，不能证明业务不变量，本节点仍未完成。

### 第5章 架构安全性

**目录节点 10/23。** 把认证、授权、凭证、保密、传输和验证串成端到端信任链，而不是零散安全组件清单；验收不变量是：每个敏感操作都能证明主体、权限、凭证来源、传输保护、输入验证和审计结果。

先把“第5章 架构安全性”放回“建立5部分、16章、2附录的依赖图，固定2021纸书版与作者v1.0.20210629快照边界”这条主线：输入来自“第4章 透明多级分流系统”，输出进入“第6章 分布式共识”。画出主体、状态、控制点、数据与信任边界，分别标注同步成功、超时、重复、部分失败和恢复，不用一条正常箭头代替机制。

实验固定2021纸书版、业务样本、请求分布、服务版本和环境，只改变一个条件。比较状态轨迹、吞吐、P95/P99、错误、资源、业务结果与恢复时间，并用“18个原书单元和77个唯一正式节点全部可达，每个结论都能回指纸书目录、作者快照和可复现实验”验收；若证据只能证明组件状态，不能证明业务不变量，本节点仍未完成。

### 第6章 分布式共识

**目录节点 11/23。** 用Paxos、Multi Paxos与Gossip区分强共识、复制日志和最终传播，不把算法名称等同系统保证；验收不变量是：在节点故障、消息延迟、重复和分区下，安全性不被破坏；活性结论明确依赖多数派与时序假设。

先把“第6章 分布式共识”放回“建立5部分、16章、2附录的依赖图，固定2021纸书版与作者v1.0.20210629快照边界”这条主线：输入来自“第5章 架构安全性”，输出进入“第7章 从类库到服务”。画出主体、状态、控制点、数据与信任边界，分别标注同步成功、超时、重复、部分失败和恢复，不用一条正常箭头代替机制。

实验固定2021纸书版、业务样本、请求分布、服务版本和环境，只改变一个条件。比较状态轨迹、吞吐、P95/P99、错误、资源、业务结果与恢复时间，并用“18个原书单元和77个唯一正式节点全部可达，每个结论都能回指纸书目录、作者快照和可复现实验”验收；若证据只能证明组件状态，不能证明业务不变量，本节点仍未完成。

### 第7章 从类库到服务

**目录节点 12/23。** 重建服务发现、网关路由和客户端负载均衡三条连接链，明确注册、解析、选择与转发责任；验收不变量是：服务实例变化后，注册表、客户端视图、路由和健康状态在约定窗口内收敛，陈旧节点不会无限接流量。

先把“第7章 从类库到服务”放回“建立5部分、16章、2附录的依赖图，固定2021纸书版与作者v1.0.20210629快照边界”这条主线：输入来自“第6章 分布式共识”，输出进入“第8章 流量治理”。画出主体、状态、控制点、数据与信任边界，分别标注同步成功、超时、重复、部分失败和恢复，不用一条正常箭头代替机制。

实验固定2021纸书版、业务样本、请求分布、服务版本和环境，只改变一个条件。比较状态轨迹、吞吐、P95/P99、错误、资源、业务结果与恢复时间，并用“18个原书单元和77个唯一正式节点全部可达，每个结论都能回指纸书目录、作者快照和可复现实验”验收；若证据只能证明组件状态，不能证明业务不变量，本节点仍未完成。

### 第8章 流量治理

**目录节点 13/23。** 将超时、重试、熔断、隔离、降级与限流组合成有容量上界的故障控制系统；验收不变量是：故障时请求放大受控、关键链路保留容量、恢复不会形成二次洪峰，策略结果可由指标解释。

先把“第8章 流量治理”放回“建立5部分、16章、2附录的依赖图，固定2021纸书版与作者v1.0.20210629快照边界”这条主线：输入来自“第7章 从类库到服务”，输出进入“第9章 可靠通信”。画出主体、状态、控制点、数据与信任边界，分别标注同步成功、超时、重复、部分失败和恢复，不用一条正常箭头代替机制。

实验固定2021纸书版、业务样本、请求分布、服务版本和环境，只改变一个条件。比较状态轨迹、吞吐、P95/P99、错误、资源、业务结果与恢复时间，并用“18个原书单元和77个唯一正式节点全部可达，每个结论都能回指纸书目录、作者快照和可复现实验”验收；若证据只能证明组件状态，不能证明业务不变量，本节点仍未完成。

### 第9章 可靠通信

**目录节点 14/23。** 以零信任与服务安全重构东西向通信，让身份、策略、加密和审计随每次调用传播；验收不变量是：网络位置不授予隐式信任，每次服务调用都验证工作负载身份、最小权限、传输完整性和策略版本。

先把“第9章 可靠通信”放回“建立5部分、16章、2附录的依赖图，固定2021纸书版与作者v1.0.20210629快照边界”这条主线：输入来自“第8章 流量治理”，输出进入“第10章 可观测性”。画出主体、状态、控制点、数据与信任边界，分别标注同步成功、超时、重复、部分失败和恢复，不用一条正常箭头代替机制。

实验固定2021纸书版、业务样本、请求分布、服务版本和环境，只改变一个条件。比较状态轨迹、吞吐、P95/P99、错误、资源、业务结果与恢复时间，并用“18个原书单元和77个唯一正式节点全部可达，每个结论都能回指纸书目录、作者快照和可复现实验”验收；若证据只能证明组件状态，不能证明业务不变量，本节点仍未完成。

### 第10章 可观测性

**目录节点 15/23。** 用事件日志、链路追踪和聚合度量回答发生了什么、请求经过哪里以及系统整体趋势；验收不变量是：同一请求能以稳定标识关联日志、跨度和指标，采样与聚合不会掩盖关键失败，成本有明确预算。

先把“第10章 可观测性”放回“建立5部分、16章、2附录的依赖图，固定2021纸书版与作者v1.0.20210629快照边界”这条主线：输入来自“第9章 可靠通信”，输出进入“第11章 虚拟化容器”。画出主体、状态、控制点、数据与信任边界，分别标注同步成功、超时、重复、部分失败和恢复，不用一条正常箭头代替机制。

实验固定2021纸书版、业务样本、请求分布、服务版本和环境，只改变一个条件。比较状态轨迹、吞吐、P95/P99、错误、资源、业务结果与恢复时间，并用“18个原书单元和77个唯一正式节点全部可达，每个结论都能回指纸书目录、作者快照和可复现实验”验收；若证据只能证明组件状态，不能证明业务不变量，本节点仍未完成。

### 第11章 虚拟化容器

**目录节点 16/23。** 从容器历史、镜像构建到应用封装，解释不可变交付如何改变环境一致性与责任边界；验收不变量是：镜像内容可追溯、运行时差异受控、配置与状态外置，重建实例与原实例满足同一声明。

先把“第11章 虚拟化容器”放回“建立5部分、16章、2附录的依赖图，固定2021纸书版与作者v1.0.20210629快照边界”这条主线：输入来自“第10章 可观测性”，输出进入“第12章 容器间网络”。画出主体、状态、控制点、数据与信任边界，分别标注同步成功、超时、重复、部分失败和恢复，不用一条正常箭头代替机制。

实验固定2021纸书版、业务样本、请求分布、服务版本和环境，只改变一个条件。比较状态轨迹、吞吐、P95/P99、错误、资源、业务结果与恢复时间，并用“18个原书单元和77个唯一正式节点全部可达，每个结论都能回指纸书目录、作者快照和可复现实验”验收；若证据只能证明组件状态，不能证明业务不变量，本节点仍未完成。

### 第12章 容器间网络

**目录节点 17/23。** 从Linux网络虚拟化到CNI生态追踪容器包路径、地址分配、转发与策略责任；验收不变量是：Pod或容器地址、路由、转发、封装、策略和服务发现能逐跳解释，MTU与故障域有实测证据。

先把“第12章 容器间网络”放回“建立5部分、16章、2附录的依赖图，固定2021纸书版与作者v1.0.20210629快照边界”这条主线：输入来自“第11章 虚拟化容器”，输出进入“第13章 持久化存储”。画出主体、状态、控制点、数据与信任边界，分别标注同步成功、超时、重复、部分失败和恢复，不用一条正常箭头代替机制。

实验固定2021纸书版、业务样本、请求分布、服务版本和环境，只改变一个条件。比较状态轨迹、吞吐、P95/P99、错误、资源、业务结果与恢复时间，并用“18个原书单元和77个唯一正式节点全部可达，每个结论都能回指纸书目录、作者快照和可复现实验”验收；若证据只能证明组件状态，不能证明业务不变量，本节点仍未完成。

### 第13章 持久化存储

**目录节点 18/23。** 从Kubernetes存储抽象到CSI生态，区分卷生命周期、声明绑定、供应、挂载与数据耐久性；验收不变量是：工作负载重调度、节点故障和卷重挂载后，数据、访问模式和回收策略仍符合声明。

先把“第13章 持久化存储”放回“建立5部分、16章、2附录的依赖图，固定2021纸书版与作者v1.0.20210629快照边界”这条主线：输入来自“第12章 容器间网络”，输出进入“第14章 资源与调度”。画出主体、状态、控制点、数据与信任边界，分别标注同步成功、超时、重复、部分失败和恢复，不用一条正常箭头代替机制。

实验固定2021纸书版、业务样本、请求分布、服务版本和环境，只改变一个条件。比较状态轨迹、吞吐、P95/P99、错误、资源、业务结果与恢复时间，并用“18个原书单元和77个唯一正式节点全部可达，每个结论都能回指纸书目录、作者快照和可复现实验”验收；若证据只能证明组件状态，不能证明业务不变量，本节点仍未完成。

### 第14章 资源与调度

**目录节点 19/23。** 以资源模型、QoS、驱逐与默认调度器解释请求如何从声明到节点选择和运行保障；验收不变量是：requests、limits、优先级、节点容量和实际使用量能够解释调度、节流、驱逐与恢复结果。

先把“第14章 资源与调度”放回“建立5部分、16章、2附录的依赖图，固定2021纸书版与作者v1.0.20210629快照边界”这条主线：输入来自“第13章 持久化存储”，输出进入“第15章 服务网格”。画出主体、状态、控制点、数据与信任边界，分别标注同步成功、超时、重复、部分失败和恢复，不用一条正常箭头代替机制。

实验固定2021纸书版、业务样本、请求分布、服务版本和环境，只改变一个条件。比较状态轨迹、吞吐、P95/P99、错误、资源、业务结果与恢复时间，并用“18个原书单元和77个唯一正式节点全部可达，每个结论都能回指纸书目录、作者快照和可复现实验”验收；若证据只能证明组件状态，不能证明业务不变量，本节点仍未完成。

### 第15章 服务网格

**目录节点 20/23。** 从透明通信的重现到服务网格生态，分清数据平面、控制平面和业务服务的责任；验收不变量是：流量策略、身份与遥测可由控制面下发并在数据面验证，旁路、失联和升级时业务结果可控。

先把“第15章 服务网格”放回“建立5部分、16章、2附录的依赖图，固定2021纸书版与作者v1.0.20210629快照边界”这条主线：输入来自“第14章 资源与调度”，输出进入“第16章 向微服务迈进”。画出主体、状态、控制点、数据与信任边界，分别标注同步成功、超时、重复、部分失败和恢复，不用一条正常箭头代替机制。

实验固定2021纸书版、业务样本、请求分布、服务版本和环境，只改变一个条件。比较状态轨迹、吞吐、P95/P99、错误、资源、业务结果与恢复时间，并用“18个原书单元和77个唯一正式节点全部可达，每个结论都能回指纸书目录、作者快照和可复现实验”验收；若证据只能证明组件状态，不能证明业务不变量，本节点仍未完成。

### 第16章 向微服务迈进

**目录节点 21/23。** 按目的、前提、边界与治理判断是否以及如何拆分微服务，让组织能力与技术边界同步演进；验收不变量是：每个拆分都有可量化驱动力、自治前提、业务边界、治理成本和可逆迁移路径。

先把“第16章 向微服务迈进”放回“建立5部分、16章、2附录的依赖图，固定2021纸书版与作者v1.0.20210629快照边界”这条主线：输入来自“第15章 服务网格”，输出进入“附录A 技术演示工程实践”。画出主体、状态、控制点、数据与信任边界，分别标注同步成功、超时、重复、部分失败和恢复，不用一条正常箭头代替机制。

实验固定2021纸书版、业务样本、请求分布、服务版本和环境，只改变一个条件。比较状态轨迹、吞吐、P95/P99、错误、资源、业务结果与恢复时间，并用“18个原书单元和77个唯一正式节点全部可达，每个结论都能回指纸书目录、作者快照和可复现实验”验收；若证据只能证明组件状态，不能证明业务不变量，本节点仍未完成。

### 附录A 技术演示工程实践

**目录节点 22/23。** 用同一书店业务对照单体、Spring Cloud、Kubernetes、Istio与AWS Lambda五种架构实现；验收不变量是：各实现保持相同业务行为和可比输入，只改变架构与基础设施，差异能回指部署、通信和治理责任。

先把“附录A 技术演示工程实践”放回“建立5部分、16章、2附录的依赖图，固定2021纸书版与作者v1.0.20210629快照边界”这条主线：输入来自“第16章 向微服务迈进”，输出进入“附录B 部署Kubernetes集群”。画出主体、状态、控制点、数据与信任边界，分别标注同步成功、超时、重复、部分失败和恢复，不用一条正常箭头代替机制。

实验固定2021纸书版、业务样本、请求分布、服务版本和环境，只改变一个条件。比较状态轨迹、吞吐、P95/P99、错误、资源、业务结果与恢复时间，并用“18个原书单元和77个唯一正式节点全部可达，每个结论都能回指纸书目录、作者快照和可复现实验”验收；若证据只能证明组件状态，不能证明业务不变量，本节点仍未完成。

### 附录B 部署Kubernetes集群

**目录节点 23/23。** 固定2021版演示环境，完成Kubernetes集群部署、镜像与依赖准备，并形成可重放基线；验收不变量是：节点、网络、存储、入口、证书和示例工作负载均通过检查，环境版本与书中实验可追溯。

先把“附录B 部署Kubernetes集群”放回“建立5部分、16章、2附录的依赖图，固定2021纸书版与作者v1.0.20210629快照边界”这条主线：输入来自“附录A 技术演示工程实践”，输出进入“本单元验收与跨章连接”。画出主体、状态、控制点、数据与信任边界，分别标注同步成功、超时、重复、部分失败和恢复，不用一条正常箭头代替机制。

实验固定2021纸书版、业务样本、请求分布、服务版本和环境，只改变一个条件。比较状态轨迹、吞吐、P95/P99、错误、资源、业务结果与恢复时间，并用“18个原书单元和77个唯一正式节点全部可达，每个结论都能回指纸书目录、作者快照和可复现实验”验收；若证据只能证明组件状态，不能证明业务不变量，本节点仍未完成。

## 本章机制闭环

### 输入、约束与责任

“2021纸书版权威学习地图”的输入不是一个孤立请求，而是业务目标、数据语义、容量预算、故障假设与2021技术边界的组合。先用五部分结构、77个正式节点、2021纸书版描述机制，再把作者快照、版本边界落实为可检查约束。调用者、平台、服务和依赖各自只对明确的状态负责；任何“框架会自动处理”的说法，都要展开为触发条件、持久状态、重试主体、超时上界和最终确认者。

### 状态、控制与完成点

沿“第一部分 演进中的架构”、“第二部分 架构师的视角”、“第三部分 分布式的基石”、“第四部分 不可变基础设施”、“第五部分 技术方法论”、“第1章 服务架构演进史”、“第2章 访问远程服务”、“第3章 事务处理”、“第4章 透明多级分流系统”、“第5章 架构安全性”、“第6章 分布式共识”、“第7章 从类库到服务”、“第8章 流量治理”、“第9章 可靠通信”、“第10章 可观测性”、“第11章 虚拟化容器”、“第12章 容器间网络”、“第13章 持久化存储”、“第14章 资源与调度”、“第15章 服务网格”、“第16章 向微服务迈进”、“附录A 技术演示工程实践”、“附录B 部署Kubernetes集群”建立状态机：每次转移记录输入版本、决策、输出、耗时与关联标识。同步返回、消息已接收、配置已下发、实例已健康和数据已持久是不同完成点，不能互相替代。只有组件轨迹和“18个原书单元和77个唯一正式节点全部可达，每个结论都能回指纸书目录、作者快照和可复现实验”同时成立，才能把局部成功提升为业务成功。

### 失败、恢复与反事实

至少构造超时、重复、慢依赖、部分失败和恢复中断五类反例。先固定业务样本、负载、版本和观测窗口，再单独改变一个故障条件；记录请求放大、排队、错误、P95/P99、资源饱和、恢复时间与最终数据。恢复过程还要证明旧状态停止生效、重复副作用已消除、未完成工作有归宿，而不是看到组件重新变绿就结束。

### 证据、决策与交接

把完整目录映射、五部分依赖图、版本边界、实验与复习索引装入同一证据包：目录节点说明知识分母，配置与版本说明实验前提，日志、追踪、指标和状态快照说明机制轨迹，业务对账说明结果，停止与回退记录说明风险上界。架构决策记录必须同时列出选择、拒绝方案、收益、代价、适用期与复审触发器，使下一位维护者能够复现结论，而不是继承无法证伪的经验判断。

## 可复现决策与证据格式

```yaml
decision:
  unit: pha-official-learning-map
  baseline: phoenix-book-2021-v1.0.20210629
  invariant: business-result-reconciled
  observe: [state, latency-p99, errors, resources, business]
  stop: [error-budget-exhausted, invariant-broken]
  rollback: verified-baseline
```

```text
request -> edge -> service -> dependency -> state
          |         |          |            |
          + timeout + retry    + isolate    + reconcile
evidence = version + config + trace + metrics + final business state
```

```text
comparison = fixed(business, data, load, version, observation-window)
           + change(one architecture variable)
           + measure(p50, p95, p99, errors, saturation, recovery)
           + reconcile(component state, user-visible result, durable state)
```

<Callout type="trap" title="机制陷阱">
  把持续更新的2026官网目录或泛化分布式主题替代2021纸书正式目录。必须先写业务不变量和责任边界，再选择框架、协议或平台。
</Callout>

<Callout type="trap" title="版本陷阱">
  纸书版固定于2021年6月和作者v1.0.20210629快照。作者官网会持续更新；后来的章节、产品行为与生态只能作为迁移差异，不能改变本书77个正式节点的分母。
</Callout>

<Callout type="warning" title="隔离实验">
  流量切换、故障注入、证书轮换、网络隔离、数据补偿、驱逐和集群变更只在隔离环境演练。先保留基线、规定影响上界、停止条件和回退验收。
</Callout>

## 独立证据与生产交接

目录证据保存5部分、16章、2附录与77个唯一节点映射；环境证据保存代码、运行时、中间件、集群和策略版本；运行证据用同一标识关联请求、日志、跨度、指标与最终业务状态。完整目录映射、五部分依赖图、版本边界、实验与复习索引必须能由另一位读者独立重放。

性能判断固定业务、数据、请求分布、版本和观测窗口，只改变一个架构变量。平均值会隐藏排队、慢实例、重试放大与恢复尖峰，必须保留P95/P99、错误、饱和度和恢复时间；提升吞吐但破坏不变量不算优化。

恢复判断从用户与持久状态结束。组件重新健康、代理重新连接或副本恢复只是阶段；还要核对未完成请求、重复副作用、数据收敛、旧版本停止服务和审计记录。

## 本章回顾

重新完成“建立5部分、16章、2附录的依赖图，固定2021纸书版与作者v1.0.20210629快照边界”：固定2021纸书版目录，从机制与责任边界推导方案，通过基线、压力与故障实验测量，最后交付完整目录映射、五部分依赖图、版本边界、实验与复习索引。只有“18个原书单元和77个唯一正式节点全部可达，每个结论都能回指纸书目录、作者快照和可复现实验”与最小反例都能独立重放，本页才算完成。

## 复习与运行验收

<Exercises>
**问题 1**：为什么“2021纸书版权威学习地图”必须覆盖23个目录节点？
<Answer>这些节点共同组成“建立5部分、16章、2附录的依赖图，固定2021纸书版与作者v1.0.20210629快照边界”的机制、运行与证据链；漏项会使完整目录映射、五部分依赖图、版本边界、实验与复习索引无法独立复现。</Answer>

**问题 2**：本页最小不变量是什么？

<Answer>
  18个原书单元和77个唯一正式节点全部可达，每个结论都能回指纸书目录、作者快照和可复现实验。必须由版本、配置、状态轨迹、遥测、故障与业务对账共同证明。
</Answer>

**问题 3**：怎样构造最小反例？

<Answer>
  固定2021快照、业务、数据、负载和观测窗口，只改变一个架构变量或故障条件，比较组件状态与最终业务结果。
</Answer>

**问题 4**：为什么不能用持续更新的官网直接替代纸书目录？

<Answer>
  官网内容会继续演进；课程分母是2021年6月纸书目录与作者v1.0.20210629快照，后续内容只能作为差异材料。
</Answer>

**问题 5**：如何验证性能和恢复结论？

<Answer>
  重复测量P50、P95、P99、错误、饱和度、恢复时间和最终业务集合，并保留请求到持久状态的关联证据。
</Answer>

**问题 6**：独立交接需要什么？

<Answer>需要目录映射、版本环境、决策记录、完整目录映射、五部分依赖图、版本边界、实验与复习索引、负载、故障、业务对账、停止、恢复、回退和责任人。</Answer>
</Exercises>

<Glossary>
  <GlossaryItem term="五部分结构">
    五部分结构在本页不是孤立名词，必须同时解释“18个原书单元和77个唯一正式节点全部可达，每个结论都能回指纸书目录、作者快照和可复现实验”所需的机制、边界、失败与证据。
  </GlossaryItem>
  <GlossaryItem term="77个正式节点">
    77个正式节点在本页不是孤立名词，必须同时解释“18个原书单元和77个唯一正式节点全部可达，每个结论都能回指纸书目录、作者快照和可复现实验”所需的机制、边界、失败与证据。
  </GlossaryItem>
  <GlossaryItem term="2021纸书版">
    2021纸书版在本页不是孤立名词，必须同时解释“18个原书单元和77个唯一正式节点全部可达，每个结论都能回指纸书目录、作者快照和可复现实验”所需的机制、边界、失败与证据。
  </GlossaryItem>
  <GlossaryItem term="作者快照">
    作者快照在本页不是孤立名词，必须同时解释“18个原书单元和77个唯一正式节点全部可达，每个结论都能回指纸书目录、作者快照和可复现实验”所需的机制、边界、失败与证据。
  </GlossaryItem>
  <GlossaryItem term="版本边界">
    版本边界在本页不是孤立名词，必须同时解释“18个原书单元和77个唯一正式节点全部可达，每个结论都能回指纸书目录、作者快照和可复现实验”所需的机制、边界、失败与证据。
  </GlossaryItem>
</Glossary>

[下一页：第1章 服务架构演进史 →](/learn/phoenix-architecture/01-01-architecture-evolution/pha-01-architecture-evolution)

<Attribution
  adaptedFrom="周志明《凤凰架构：构建可靠的大型分布式系统》2021纸书版"
  adaptedUrl="https://icyfenix.cn/introduction/about-book.html"
/>
