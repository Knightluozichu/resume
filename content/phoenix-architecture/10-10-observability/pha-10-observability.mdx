---
title: "第10章 可观测性"
description: "依据周志明2021纸书版与作者v1.0.20210629快照：用事件日志、链路追踪和聚合度量回答发生了什么、请求经过哪里以及系统整体趋势"
type: C
section: "凤凰架构：构建可靠的大型分布式系统（2021纸书版 / v1.0.20210629）"
order: 10
draft: false
math: false
sourceUrl: "https://icyfenix.cn/distribution/observability/"
demo: true
---

import { Objectives } from "@/components/mdx/objectives";
import { Term } from "@/components/mdx/term";
import { Callout } from "@/components/mdx/callout";
import { Answer, Exercises } from "@/components/mdx/exercises";
import { Glossary, GlossaryItem } from "@/components/mdx/glossary";
import { Attribution } from "@/components/mdx/attribution";

# 第10章 可观测性

本课程对应周志明著《凤凰架构：构建可靠的大型分布式系统》，机械工业出版社2021年6月出版，409页、约33万字，ISBN 9787111683919。课程以纸书正式目录和作者出版当月发布的v1.0.20210629快照为边界，不把后续持续更新的官网内容倒灌为原书章节。

全书正式分母为5部分、16章、54个正式节与附录A、B，共18个原书单元、77个唯一正式节点；另设学习地图和总复习，共20页。本页逐项覆盖3个目录节点，最终交付遥测关联图、Trace样本、指标基数预算、告警到根因演练。

<Objectives>

- 能解释“第10章 可观测性”的3个目录节点，并绘制主体、状态、控制点、数据与信任边界。
- 能比较正常、压力、超时、重复、部分失败和恢复轨迹，判断组件完成点与业务完成点。
- 能设计并复现遥测关联图、Trace样本、指标基数预算、告警到根因演练，用日志、指标、追踪、状态和业务对账支持结论。
- 能分析“堆积日志和面板却无法从告警定位到请求、实例、版本与业务结果”为何失败，并写出停止、恢复、回退和独立交接条件。

</Objectives>

## 从一个可证伪的架构决策开始

先预测：当请求跨越进程、网络与基础设施边界时，谁接收、谁持有状态、谁重试、谁判定完成，哪里可能超时、重复或部分成功。观察前写下业务不变量和反例，再用同一请求标识贯穿日志、追踪、指标与最终数据。

“组件健康”不是“业务完成”。HTTP 200、事务协调器提交、注册中心有实例、Pod Running或代理配置已下发，都只是局部检查点。验收必须回到“同一请求能以稳定标识关联日志、跨度和指标，采样与聚合不会掩盖关键失败，成本有明确预算”，并说明该结论在2021纸书版之外是否仍成立。

## 核心词汇与版本边界

<Term def="日志是“第10章 可观测性”的第1个核心概念，必须说明机制、适用边界、失败反例和可观测证据">日志</Term>、<Term def="Trace是“第10章 可观测性”的第2个核心概念，必须说明机制、适用边界、失败反例和可观测证据">Trace</Term>、<Term def="Span是“第10章 可观测性”的第3个核心概念，必须说明机制、适用边界、失败反例和可观测证据">Span</Term>、<Term def="指标是“第10章 可观测性”的第4个核心概念，必须说明机制、适用边界、失败反例和可观测证据">指标</Term>、<Term def="基数是“第10章 可观测性”的第5个核心概念，必须说明机制、适用边界、失败反例和可观测证据">基数</Term>

这些词构成本页词汇。每个词都要回答：它解决什么问题，依赖什么前提，状态由谁持有，失败如何传播，怎样恢复与观测，以及后续技术变化为何不能替换本书的正式分母。


## 原书目录逐节点重构

### 10.1 事件日志

**目录节点 1/3。** 结构化日志记录离散事件和上下文，级别、脱敏、轮转与检索字段共同决定可用性。

先把“10.1 事件日志”放回“用事件日志、链路追踪和聚合度量回答发生了什么、请求经过哪里以及系统整体趋势”这条主线：输入来自“本单元输入与版本契约”，输出进入“10.2 链路追踪”。画出主体、状态、控制点、数据与信任边界，分别标注同步成功、超时、重复、部分失败和恢复，不用一条正常箭头代替机制。

实验固定2021纸书版、业务样本、请求分布、服务版本和环境，只改变一个条件。比较状态轨迹、吞吐、P95/P99、错误、资源、业务结果与恢复时间，并用“同一请求能以稳定标识关联日志、跨度和指标，采样与聚合不会掩盖关键失败，成本有明确预算”验收；若证据只能证明组件状态，不能证明业务不变量，本节点仍未完成。

### 10.2 链路追踪

**目录节点 2/3。** Trace与Span还原跨服务因果路径；上下文传播、采样与异步边界决定链路是否完整。

先把“10.2 链路追踪”放回“用事件日志、链路追踪和聚合度量回答发生了什么、请求经过哪里以及系统整体趋势”这条主线：输入来自“10.1 事件日志”，输出进入“10.3 聚合度量”。画出主体、状态、控制点、数据与信任边界，分别标注同步成功、超时、重复、部分失败和恢复，不用一条正常箭头代替机制。

实验固定2021纸书版、业务样本、请求分布、服务版本和环境，只改变一个条件。比较状态轨迹、吞吐、P95/P99、错误、资源、业务结果与恢复时间，并用“同一请求能以稳定标识关联日志、跨度和指标，采样与聚合不会掩盖关键失败，成本有明确预算”验收；若证据只能证明组件状态，不能证明业务不变量，本节点仍未完成。

### 10.3 聚合度量

**目录节点 3/3。** 计数器、仪表、直方图和摘要刻画总体趋势，高基数标签与平均值误导必须受控。

先把“10.3 聚合度量”放回“用事件日志、链路追踪和聚合度量回答发生了什么、请求经过哪里以及系统整体趋势”这条主线：输入来自“10.2 链路追踪”，输出进入“本单元验收与跨章连接”。画出主体、状态、控制点、数据与信任边界，分别标注同步成功、超时、重复、部分失败和恢复，不用一条正常箭头代替机制。

实验固定2021纸书版、业务样本、请求分布、服务版本和环境，只改变一个条件。比较状态轨迹、吞吐、P95/P99、错误、资源、业务结果与恢复时间，并用“同一请求能以稳定标识关联日志、跨度和指标，采样与聚合不会掩盖关键失败，成本有明确预算”验收；若证据只能证明组件状态，不能证明业务不变量，本节点仍未完成。

## 本章机制闭环

### 输入、约束与责任

“第10章 可观测性”的输入不是一个孤立请求，而是业务目标、数据语义、容量预算、故障假设与2021技术边界的组合。先用日志、Trace、Span描述机制，再把指标、基数落实为可检查约束。调用者、平台、服务和依赖各自只对明确的状态负责；任何“框架会自动处理”的说法，都要展开为触发条件、持久状态、重试主体、超时上界和最终确认者。

### 状态、控制与完成点

沿“10.1 事件日志”、“10.2 链路追踪”、“10.3 聚合度量”建立状态机：每次转移记录输入版本、决策、输出、耗时与关联标识。同步返回、消息已接收、配置已下发、实例已健康和数据已持久是不同完成点，不能互相替代。只有组件轨迹和“同一请求能以稳定标识关联日志、跨度和指标，采样与聚合不会掩盖关键失败，成本有明确预算”同时成立，才能把局部成功提升为业务成功。

### 失败、恢复与反事实

至少构造超时、重复、慢依赖、部分失败和恢复中断五类反例。先固定业务样本、负载、版本和观测窗口，再单独改变一个故障条件；记录请求放大、排队、错误、P95/P99、资源饱和、恢复时间与最终数据。恢复过程还要证明旧状态停止生效、重复副作用已消除、未完成工作有归宿，而不是看到组件重新变绿就结束。

### 证据、决策与交接

把遥测关联图、Trace样本、指标基数预算、告警到根因演练装入同一证据包：目录节点说明知识分母，配置与版本说明实验前提，日志、追踪、指标和状态快照说明机制轨迹，业务对账说明结果，停止与回退记录说明风险上界。架构决策记录必须同时列出选择、拒绝方案、收益、代价、适用期与复审触发器，使下一位维护者能够复现结论，而不是继承无法证伪的经验判断。

## 可复现决策与证据格式

```yaml
decision:
  unit: pha-10-observability
  baseline: phoenix-book-2021-v1.0.20210629
  invariant: business-result-reconciled
  observe: [state, latency-p99, errors, resources, business]
  stop: [error-budget-exhausted, invariant-broken]
  rollback: verified-baseline
```

```text
request -> edge -> service -> dependency -> state
          |         |          |            |
          + timeout + retry    + isolate    + reconcile
evidence = version + config + trace + metrics + final business state
```

```text
comparison = fixed(business, data, load, version, observation-window)
           + change(one architecture variable)
           + measure(p50, p95, p99, errors, saturation, recovery)
           + reconcile(component state, user-visible result, durable state)
```

<Callout type="trap" title="机制陷阱">
  堆积日志和面板却无法从告警定位到请求、实例、版本与业务结果。必须先写业务不变量和责任边界，再选择框架、协议或平台。
</Callout>

<Callout type="trap" title="版本陷阱">
  纸书版固定于2021年6月和作者v1.0.20210629快照。作者官网会持续更新；后来的章节、产品行为与生态只能作为迁移差异，不能改变本书77个正式节点的分母。
</Callout>

<Callout type="warning" title="隔离实验">
  流量切换、故障注入、证书轮换、网络隔离、数据补偿、驱逐和集群变更只在隔离环境演练。先保留基线、规定影响上界、停止条件和回退验收。
</Callout>

## 独立证据与生产交接

目录证据保存5部分、16章、2附录与77个唯一节点映射；环境证据保存代码、运行时、中间件、集群和策略版本；运行证据用同一标识关联请求、日志、跨度、指标与最终业务状态。遥测关联图、Trace样本、指标基数预算、告警到根因演练必须能由另一位读者独立重放。

性能判断固定业务、数据、请求分布、版本和观测窗口，只改变一个架构变量。平均值会隐藏排队、慢实例、重试放大与恢复尖峰，必须保留P95/P99、错误、饱和度和恢复时间；提升吞吐但破坏不变量不算优化。

恢复判断从用户与持久状态结束。组件重新健康、代理重新连接或副本恢复只是阶段；还要核对未完成请求、重复副作用、数据收敛、旧版本停止服务和审计记录。

## 本章回顾

重新完成“用事件日志、链路追踪和聚合度量回答发生了什么、请求经过哪里以及系统整体趋势”：固定2021纸书版目录，从机制与责任边界推导方案，通过基线、压力与故障实验测量，最后交付遥测关联图、Trace样本、指标基数预算、告警到根因演练。只有“同一请求能以稳定标识关联日志、跨度和指标，采样与聚合不会掩盖关键失败，成本有明确预算”与最小反例都能独立重放，本页才算完成。

## 复习与运行验收

<Exercises>
**问题 1**：为什么“第10章 可观测性”必须覆盖3个目录节点？
<Answer>这些节点共同组成“用事件日志、链路追踪和聚合度量回答发生了什么、请求经过哪里以及系统整体趋势”的机制、运行与证据链；漏项会使遥测关联图、Trace样本、指标基数预算、告警到根因演练无法独立复现。</Answer>

**问题 2**：本页最小不变量是什么？

<Answer>
  同一请求能以稳定标识关联日志、跨度和指标，采样与聚合不会掩盖关键失败，成本有明确预算。必须由版本、配置、状态轨迹、遥测、故障与业务对账共同证明。
</Answer>

**问题 3**：怎样构造最小反例？

<Answer>
  固定2021快照、业务、数据、负载和观测窗口，只改变一个架构变量或故障条件，比较组件状态与最终业务结果。
</Answer>

**问题 4**：为什么不能用持续更新的官网直接替代纸书目录？

<Answer>
  官网内容会继续演进；课程分母是2021年6月纸书目录与作者v1.0.20210629快照，后续内容只能作为差异材料。
</Answer>

**问题 5**：如何验证性能和恢复结论？

<Answer>
  重复测量P50、P95、P99、错误、饱和度、恢复时间和最终业务集合，并保留请求到持久状态的关联证据。
</Answer>

**问题 6**：独立交接需要什么？

<Answer>需要目录映射、版本环境、决策记录、遥测关联图、Trace样本、指标基数预算、告警到根因演练、负载、故障、业务对账、停止、恢复、回退和责任人。</Answer>
</Exercises>

<Glossary>
  <GlossaryItem term="日志">
    日志在本页不是孤立名词，必须同时解释“同一请求能以稳定标识关联日志、跨度和指标，采样与聚合不会掩盖关键失败，成本有明确预算”所需的机制、边界、失败与证据。
  </GlossaryItem>
  <GlossaryItem term="Trace">
    Trace在本页不是孤立名词，必须同时解释“同一请求能以稳定标识关联日志、跨度和指标，采样与聚合不会掩盖关键失败，成本有明确预算”所需的机制、边界、失败与证据。
  </GlossaryItem>
  <GlossaryItem term="Span">
    Span在本页不是孤立名词，必须同时解释“同一请求能以稳定标识关联日志、跨度和指标，采样与聚合不会掩盖关键失败，成本有明确预算”所需的机制、边界、失败与证据。
  </GlossaryItem>
  <GlossaryItem term="指标">
    指标在本页不是孤立名词，必须同时解释“同一请求能以稳定标识关联日志、跨度和指标，采样与聚合不会掩盖关键失败，成本有明确预算”所需的机制、边界、失败与证据。
  </GlossaryItem>
  <GlossaryItem term="基数">
    基数在本页不是孤立名词，必须同时解释“同一请求能以稳定标识关联日志、跨度和指标，采样与聚合不会掩盖关键失败，成本有明确预算”所需的机制、边界、失败与证据。
  </GlossaryItem>
</Glossary>

[← 上一页：第9章 可靠通信](/learn/phoenix-architecture/09-09-reliable-communication/pha-09-reliable-communication) · [下一页：第11章 虚拟化容器 →](/learn/phoenix-architecture/11-11-containers/pha-11-containers)

<Attribution
  adaptedFrom="周志明《凤凰架构：构建可靠的大型分布式系统》2021纸书版"
  adaptedUrl="https://icyfenix.cn/distribution/observability/"
/>
