---
title: "第一部分 表述"
type: B
section: "企业应用架构模式（2024年中文版）"
order: 5
description: "用8章叙述建立分层、领域逻辑、关系映射、Web表示、并发、会话与分布的选择框架。"
demo: true
math: true
sourceUrl: "https://book.douban.com/subject/36867071/"
draft: false
---

import {
  Poeaa24Part01NarrativesBoundaryLab,
  Poeaa24Part01NarrativesMappingLab,
  Poeaa24Part01NarrativesTransactionLab,
} from "@/components/mdx/poeaa-enterprise-patterns/diagrams/poeaa24-part-01-narratives";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第一部分 表述在76个正式单元、119个目录节点和51个模式中的职责与前后依赖
- 能比较8个叙述章、选择问题、模式协作、技术约束、通盘考虑，并绘制“分层 → 领域逻辑 → 数据映射 → 并发会话 → 分布组合”边界链
- 能计算远程成本、映射成本、冲突风险与证据置信度，并验证正常、恰好边界和单故障样本
- 能设计独立复核，证明“能从第1至8章构造一个完整应用切片，并解释每个边界为什么存在。”而非只复述模式名称

</Objectives>

## 为什么这一单元不可压缩

本页依据马丁·福勒《企业应用架构模式》，王怀民、周斌译，机械工业出版社，2024年2月，ISBN 9787111746959，400页的公开完整中文目录，独立重构 **第一部分 表述**。第一部分 表述承担“分层 → 领域逻辑 → 数据映射 → 并发会话 → 分布组合”中的完整选择责任。用8章叙述建立分层、领域逻辑、关系映射、Web表示、并发、会话与分布的选择框架。课程不复制原书正文、插图、代码或练习答案，而是把目录命题改写为新的架构模型、应用切片、交互实验、失败样本和可重放证据。

2024年中文版目录逐项列出译者序、前言、模式列表、引言及5节、两部分、18章、第1至8章38个叙述小节、第9至18章51个模式和参考文献，共76个正式单元、119个目录节点。Martin Fowler 官方图书页确认本书围绕企业应用的分层、领域逻辑、数据库映射、Web 表示、并发、会话和分布展开；作者官方目录逐项列出51个模式，Pearson 原版目录交叉确认18章。目录坐标决定学习分母，框架流行度不能替代原书结构。

本书出版语境早于今天常见的 ORM、依赖注入容器和 Web 框架。现代框架把工作单元、身份映射、前端控制器或数据传输对象封装成默认机制，却没有消除事务范围、对象身份、调用粒度、部分失败和一致性之间的裁决。学习者必须能在框架失效、默认值变化或边界跨进程时重新推导这些决定。

## 版次、对象与验收合同

<Term def="第一部分 表述是第一部分 表述的第1个复核坐标；必须写明问题、适用语境、机制、代价和反例。">
  第一部分 表述
</Term>
、
<Term def="8个叙述章是第一部分 表述的第2个复核坐标；必须写明问题、适用语境、机制、代价和反例。">
  8个叙述章
</Term>
、
<Term def="选择问题是第一部分 表述的第3个复核坐标；必须写明问题、适用语境、机制、代价和反例。">
  选择问题
</Term>
、
<Term def="模式协作是第一部分 表述的第4个复核坐标；必须写明问题、适用语境、机制、代价和反例。">
  模式协作
</Term>
、
<Term def="技术约束是第一部分 表述的第5个复核坐标；必须写明问题、适用语境、机制、代价和反例。">
  技术约束
</Term>

先固定应用切片：一个订单请求经过表示入口、用例服务、领域规则、对象关系映射和数据库提交；只有在确有远程边界时才引入网络。输入包括对象数、关联密度、事务持续时间、调用次数、载荷、延迟和证据覆盖率。输出必须包含结果、事务状态、查询与往返计数、冲突处理、错误翻译和回退记录。

通过条件不是“代码中出现了模式名”，而是 **能从第1至8章构造一个完整应用切片，并解释每个边界为什么存在。**。同一合同必须同时约束正常输入、恰好达到预算的边界输入和只注入一个故障的输入；任一层吞掉异常、重复提交或泄漏内部对象都算失败。

## 四个可手算模型

远程调用的粗略成本把往返延迟和载荷传输分开：

$$
C_r = n \times l + p / b
$$

其中 $n$ 是远程往返次数，$l$ 是单次延迟，$p$ 是载荷字节数，$b$ 是有效带宽。优化序列化却不减少高延迟往返，通常不会改变主导项。

对象关系映射成本由身份、关联、继承和变更跟踪共同决定：

$$
C_m = i + a + h + t
$$

并发风险不是数据库行数，而是重叠概率、业务事务持续时间和热点竞争的组合：

$$
R_c = o \times d \times q
$$

发布门禁采用乘法含义：内容覆盖、正常样本、边界样本、故障样本和独立复核任何一项为零，整体就不能通过。

$$
G = c 	imes n 	imes e 	imes f 	imes r
$$

这些式子不是生产容量模型，而是迫使设计者公开假设、单位和主导变量。先手算再运行交互实验；若工具结果与预期不同，保存最早出现差异的边界，不要从最终异常反向猜测。

## 原书目录坐标与核心机制

### 第1章 分层

从职责和依赖方向建立表示、领域、数据源三层，决定物理部署而不把层与进程混同。 复核时逐项覆盖：1.1 企业应用中层次的演化、1.2 三个基本层次、1.3 为各层选择运行环境。

### 第2章 组织领域逻辑

按规则复杂度、数据结构和用例变化选择事务脚本、领域模型、表模块与服务层。 复核时逐项覆盖：2.1 抉择、2.2 服务层。

### 第3章 映射到关系数据库

系统处理对象身份、关系、加载、更新、继承、元数据和连接生命周期的阻抗失配。 复核时逐项覆盖：3.1 架构模式、3.2 行为问题、3.3 读取数据、3.4 结构映射模式、3.5 建立映射、3.6 使用元数据、3.7 数据库连接、3.8 其他问题、3.9 进一步阅读。

### 第4章 Web表示层

拆分请求控制、领域模型与视图生成，比较控制器和视图模式的变化轴。 复核时逐项覆盖：4.1 视图模式、4.2 输入控制器模式、4.3 进一步阅读。

### 第5章 并发

区分系统事务与业务事务，以隔离、不变量、锁和冲突恢复保护长会话。 复核时逐项覆盖：5.1 并发问题、5.2 执行语境、5.3 隔离与不变性、5.4 乐观并发控制和悲观并发控制、5.5 事务、5.6 离线并发控制的模式、5.7 应用服务器并发、5.8 进一步阅读。

### 第6章 会话状态

比较无状态价值以及客户端、服务器、数据库三种会话状态位置的可靠性和成本。 复核时逐项覆盖：6.1 无状态的价值、6.2 会话状态、6.3 存储会话状态的方法。

### 第7章 分布策略

抵抗透明分布诱惑，依据延迟、失败和一致性选择粗粒度远程边界。 复核时逐项覆盖：7.1 分布对象的诱惑、7.2 远程接口和本地接口、7.3 必须使用分布的情况、7.4 关于分布边界、7.5 分布接口。

### 第8章 通盘考虑

从领域层开始贯通数据源与表示层，并按问题复杂度和技术条件组合模式。 复核时逐项覆盖：8.1 从领域层开始、8.2 深入到数据源层、8.3 表示层、8.4 一些关于具体技术的建议、8.5 其他分层方式。

### 问题、语境、机制与后果

任何模式裁决先写问题：哪个变化、失败或复杂度正在伤害系统；再写语境：数据规模、团队边界、事务与部署约束是否成立；然后写机制：责任移到哪里、依赖方向怎样改变、状态由谁拥有；最后写后果：获得什么，同时增加了哪些间接层、同步成本和调试难度。

对第一部分 表述，最小证据链是“分层 → 领域逻辑 → 数据映射 → 并发会话 → 分布组合”。1. 8个叙述章必须有一个可观察指标和一个拒绝阈值；2. 选择问题必须有一个可观察指标和一个拒绝阈值；3. 模式协作必须有一个可观察指标和一个拒绝阈值；4. 技术约束必须有一个可观察指标和一个拒绝阈值；5. 通盘考虑必须有一个可观察指标和一个拒绝阈值。只画静态类图不足以证明事务、加载、远程失败或会话过期行为，因此本页把拓扑、映射和事务证据拆成三个视图。

## 三视图交互验证

<Callout type="info" title="先预测，再操作">
  先预测对象规模或耦合升高时哪一个指标最先越界，再切换正常、恰好边界和单故障。每次只改一个变量，记录首差、原因和可回退状态。
</Callout>

<Stepper>
  <Step title="1. 冻结边界拓扑">
    选择“分层”到“分布组合”中的检查点，确认责任、依赖方向和进程边界。不要把逻辑层与物理节点混为一谈。
    <Poeaa24Part01NarrativesBoundaryLab />
  </Step>
  <Step title="2. 改变映射压力">
    保持用例不变，只调对象规模和结构耦合；观察身份、关联、加载与写回成本是否在预期节点出现首差。
    <Poeaa24Part01NarrativesMappingLab />
  </Step>
  <Step title="3. 注入一个事务故障">
    固定前两步，切换边界或单故障样本；检查部分写入、冲突、重试、重复请求和证据置信是否按合同被拒绝。
    <Poeaa24Part01NarrativesTransactionLab />
  </Step>
</Stepper>

## 最小实现骨架

第一段代码让应用服务只编排用例、事务与端口，领域规则仍由领域对象承担：

```ts
type Decision = { accepted: boolean; reason: string };

async function executeUseCase(command: Command): Promise<Decision> {
  return unitOfWork.run(async () => {
    const aggregate = await repository.get(command.id);
    const decision = aggregate.apply(command);
    if (!decision.accepted) return decision;
    await repository.save(aggregate);
    return decision;
  });
}
```

第二段显式记录版本，允许离线业务事务在提交点检测冲突：

```sql
UPDATE orders
SET status = :status, version = version + 1
WHERE order_id = :id AND version = :expected_version;
-- affected_rows must equal 1
```

第三段把模式决定写成可审查配置，而不是让框架默认值成为隐含架构：

```yaml
architecture_decision:
  unit: poeaa24-part-01-narratives
  transaction: one-use-case
  remote_calls_max: 4
  conflict_policy: reject-and-reload
  evidence: [normal, exact-boundary, single-fault]
```

代码骨架故意不绑定具体框架。迁移到 Spring、Jakarta、.NET、Rails 或其他栈时，应逐项映射工作单元、身份范围、异常翻译和请求生命周期；若框架只覆盖机制的一部分，剩余责任必须由应用明确承担。

## 选择矩阵与迁移策略

| 决策轴   | 低复杂度候选         | 高复杂度候选                     | 拒绝信号                       |
| -------- | -------------------- | -------------------------------- | ------------------------------ |
| 领域规则 | 事务脚本或表模块     | 领域模型与服务层协作             | 规则在控制器、SQL和模板间重复  |
| 数据映射 | 活动记录或入口对象   | 数据映射器、工作单元、资源库     | 同一身份出现冲突对象或部分写入 |
| Web 表示 | 页面控制器与模板视图 | 前端控制器、应用控制器、两步视图 | 认证、导航或业务规则散落       |
| 分布     | 保持本地调用         | 远程外观与DTO组成粗粒度边界      | 循环远程调用、内部对象直接越界 |
| 并发会话 | 短系统事务、无状态   | 明确离线锁和权威会话位置         | 静默覆盖、锁失主或会话不可清理 |

迁移顺序从观测开始：先记录当前查询、往返、事务、冲突和错误；再把一个用例放进新边界；随后以双读或影子流量比较语义；最后才删除旧路径。数据库结构、对象模型和远程契约不要同批无证据更换，否则首差无法归因。

<Callout type="trap" title="陷阱：把模式当成必须使用的零件">
  模式描述的是反复出现的问题与取舍，不是架构完成度清单。没有对象关系阻抗时强加数据映射器，或没有远程边界时先造DTO，都会增加间接层而不解决真实问题。
</Callout>

<Callout type="trap" title="陷阱：把框架默认值当成已验证边界">
  ORM 会话、Web
  请求和数据库事务的生命周期并不天然相同。必须用关闭会话后的延迟加载、并发提交、超时重试和部分失败样本验证，不能因为正常演示成功就宣布通过。
</Callout>

## 可重放证据与独立复核

每次实验保存版次、目录坐标、需求、代码哈希、迁移版本、工具版本、输入、初始数据库快照、时钟与单位；随后保存预期链、实际链、查询与远程轨迹、事务结果、首差和回退点。敏感数据只保留结构化摘要和不可逆标识，证据仍要足以重放。

独立复核者不能读取实现者的结论，只接收合同和样本。他先手算四个模型，再运行相同正常、边界和故障输入；双方必须在相同节点接受或拒绝，并能解释差异来自模式假设、框架机制还是环境。若只能复现最终错误却不能定位首差，证据仍不合格。

## 本章回顾

第一部分 表述的掌握标准是把“分层 → 领域逻辑 → 数据映射 → 并发会话 → 分布组合”变成可观察、可比较、可撤回的架构决定。记住模式名只能帮助检索；真正的能力是从问题和语境出发比较候选，明确机制与后果，再用正常、恰好边界、单故障和独立复核关闭证据链。

<Glossary>

<GlossaryItem term="第一部分 表述">
  第一部分
  表述在本单元中对应第1个架构裁决点。复核者要同时看到输入边界、候选方案、可观察量、失败条件和撤回路径，不能只看到框架
  API 或类名。
</GlossaryItem>

<GlossaryItem term="8个叙述章">
  8个叙述章在本单元中对应第2个架构裁决点。复核者要同时看到输入边界、候选方案、可观察量、失败条件和撤回路径，不能只看到框架
  API 或类名。
</GlossaryItem>

<GlossaryItem term="选择问题">
  选择问题在本单元中对应第3个架构裁决点。复核者要同时看到输入边界、候选方案、可观察量、失败条件和撤回路径，不能只看到框架
  API 或类名。
</GlossaryItem>

<GlossaryItem term="模式协作">
  模式协作在本单元中对应第4个架构裁决点。复核者要同时看到输入边界、候选方案、可观察量、失败条件和撤回路径，不能只看到框架
  API 或类名。
</GlossaryItem>

<GlossaryItem term="技术约束">
  技术约束在本单元中对应第5个架构裁决点。复核者要同时看到输入边界、候选方案、可观察量、失败条件和撤回路径，不能只看到框架
  API 或类名。
</GlossaryItem>

</Glossary>

<Exercises>

1. 如何为第一部分 表述建立最小可重放基线？

<Answer>
  固定2024年中文版目录坐标、应用切片、代码与迁移哈希、输入、初始状态和单位，先画“分层
  → 领域逻辑 → 数据映射 → 并发会话 →
  分布组合”预期，再保存正常、恰好边界和单故障轨迹。
</Answer>

2. 怎样证明“能从第1至8章构造一个完整应用切片，并解释每个边界为什么存在。”不是一次偶然成功？

<Answer>
  先手算远程成本、映射成本和冲突风险，再让独立复核者在同一边界重建；双方必须在相同首差拒绝故障，并保留一致的回退状态与替代方案记录。
</Answer>

3. 修复一个失败后怎样做相邻回归？

<Answer>
  先重放原失败，再运行相邻边界和正常基线；逐节点检查8个叙述章、选择问题、模式协作、技术约束、通盘考虑，确认查询、事务、远程和会话证据没有出现新首差后才进入下一页。
</Answer>

</Exercises>

## 前后导航

- [上一页：引言](/learn/poeaa-enterprise-patterns/04-introduction/poeaa24-introduction)
- [下一页：第1章 分层](/learn/poeaa-enterprise-patterns/06-chapter-01-layering/poeaa24-chapter-01-layering)

<Attribution
  adaptedFrom="马丁·福勒《企业应用架构模式》，王怀民、周斌译，机械工业出版社，2024年2月，ISBN 9787111746959，400页 · 中文公开完整目录含76个正式单元、119个目录节点、18章和51个模式 · 基于公开目录独立中文教学重构，不复制原书正文、插图、代码或练习答案"
  adaptedUrl="https://book.douban.com/subject/36867071/"
/>
