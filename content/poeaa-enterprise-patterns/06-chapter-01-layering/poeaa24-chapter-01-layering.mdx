---
title: "第1章 分层"
type: "B"
section: "企业应用架构模式（2024年中文版）"
order: 6
description: "从职责和依赖方向建立表示、领域、数据源三层，决定物理部署而不把层与进程混同。"
demo: true
math: false
qualityVersion: 2
practiceMode: design
sourceMode: independent-rewrite
sourceUrl: "https://martinfowler.com/books/eaa.html"
draft: false
---

import {
  Poeaa24Chapter01LayeringBoundaryLab,
  Poeaa24Chapter01LayeringMappingLab,
  Poeaa24Chapter01LayeringTransactionLab,
} from "@/components/mdx/poeaa-enterprise-patterns/diagrams/poeaa24-chapter-01-layering";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能用自己的话解释 第1章 分层 要解决的问题，以及它不负责什么
- 能沿“请求 → 表示层 → 领域层 → 数据源层 → 运行环境”定位责任，并比较 职责边界、依赖方向、层间契约
- 能操作 分层与边界 实验，先预测变量变化，再用临界与故障场景验证因果
- 能完成设计评审：能解释分层的边界与选择轴，逐项覆盖3个目录节点，并在同一应用切片中验证

</Objectives>

## 为什么 第1章 分层 值得单独学习

从职责和依赖方向建立表示、领域、数据源三层，决定物理部署而不把层与进程混同。 第1章 分层 的核心不是套用某个框架 API，而是回答：如何让表示、领域和数据源职责沿单向依赖协作，而不把逻辑层误当成部署节点。在 订单审批分层 中，如果无法说清责任、状态和失败由谁承担，即使正常请求能够返回，架构决定也没有完成。

本页以 2024 年中文版公开目录限定 **第1章 分层** 的范围，并依据 Martin Fowler 的作者图书页和模式目录独立重写。它不复现原书正文、插图或代码；目录只决定“要讲什么”，这里的案例、实验、判断题和答案均为本课程原创。

## 先建立直觉：问题、机制与代价

第1章 分层 面对的具体压力是“跨层调用、依赖方向、部署节点与错误翻译位置”。它采用的机制可以概括为：从职责和依赖方向建立表示、领域、数据源三层，决定物理部署而不把层与进程混同。 机制带来的收益必须与新增间接层、同步责任或迁移成本同时记录，否则学习者只会得到一个没有拒绝条件的模式名称。

对 第1章 分层，优先比较的替代路线是：先划职责和依赖，再根据延迟、扩缩容与安全要求决定是否分进程。本页的通过条件是“能解释分层的边界与选择轴，逐项覆盖3个目录节点，并在同一应用切片中验证”；若实验只能显示结果而不能指出 职责边界 从何处越界，就不能据此选择 第1章 分层。

<Term def="第1章 分层 是 第1章 分层 的第 1 个裁决坐标；需要给出可观察信号、接受边界与反例。">
  第1章 分层
</Term>
、
<Term def="请求 是 第1章 分层 的第 2 个裁决坐标；需要给出可观察信号、接受边界与反例。">
  请求
</Term>
、
<Term def="运行环境 是 第1章 分层 的第 3 个裁决坐标；需要给出可观察信号、接受边界与反例。">
  运行环境
</Term>
、
<Term def="职责边界 是 第1章 分层 的第 4 个裁决坐标；需要给出可观察信号、接受边界与反例。">
  职责边界
</Term>
、
<Term def="依赖方向 是 第1章 分层 的第 5 个裁决坐标；需要给出可观察信号、接受边界与反例。">
  依赖方向
</Term>
、
<Term def="层间契约 是 第1章 分层 的第 6 个裁决坐标；需要给出可观察信号、接受边界与反例。">
  层间契约
</Term>

## 目录单元到教学证据

### 第1章 分层

在 **第1章 分层** 的学习边界里，第1章 分层 不是待背诵的目录词，而是用来检查“请求”是否把责任交给正确对象。对 订单审批分层，学习者要记录 职责边界 的可观察变化，并说明它何时支持或否定 第1章 分层。

### 1.1 企业应用中层次的演化

在 **第1章 分层** 的学习边界里，1.1 企业应用中层次的演化 不是待背诵的目录词，而是用来检查“表示层”是否把责任交给正确对象。对 订单审批分层，学习者要记录 依赖方向 的可观察变化，并说明它何时支持或否定 第1章 分层。

### 1.2 三个基本层次

在 **第1章 分层** 的学习边界里，1.2 三个基本层次 不是待背诵的目录词，而是用来检查“领域层”是否把责任交给正确对象。对 订单审批分层，学习者要记录 层间契约 的可观察变化，并说明它何时支持或否定 第1章 分层。

### 1.3 为各层选择运行环境

在 **第1章 分层** 的学习边界里，1.3 为各层选择运行环境 不是待背诵的目录词，而是用来检查“数据源层”是否把责任交给正确对象。对 订单审批分层，学习者要记录 部署位置 的可观察变化，并说明它何时支持或否定 第1章 分层。

## 专属设计案例：订单审批分层

把 订单审批分层 切成“请求 → 表示层 → 领域层 → 数据源层 → 运行环境”五个观察点。第1章 分层 的设计草案必须写出谁拥有状态、谁作出业务决定、失败怎样传播，以及 职责边界、依赖方向、层间契约、部署位置、变更触达 中哪个指标最先提示当前方案不再适用。

设计记录采用五个可换行字段：单元键为 **poeaa24-chapter-01-layering**；模式族为 **layering**；裁决是“能解释分层的边界与选择轴，逐项覆盖3个目录节点，并在同一应用切片中验证”；观测项包括 职责边界、依赖方向、层间契约、部署位置、变更触达；拒绝条件是“职责边界 超过团队为 第1章 分层 设定的边界”。

配置不是生产框架语法，而是一张评审卡。对 第1章 分层 的任何实现都要能把运行证据重新映射到这张卡；如果更换 ORM、Web 框架或部署平台后无法回答同一组问题，说明决定依赖的是工具偶然行为而不是模式语义。

## 三步交互实验

<Callout type="info" title="先预测，再操作">
  在改变滑块前，先预测 职责边界 与 依赖方向 哪个先越界。每次只改变一个量，并解释
  第1章 分层 的接受或拒绝为何随之变化。
</Callout>

<Stepper>
  <Step title="1. 核对责任边界">
    逐点选择 请求、表示层、领域层、数据源层、运行环境，确认 第1章 分层
    没有把职责悄悄推给相邻层。
    <Poeaa24Chapter01LayeringBoundaryLab />
  </Step>
  <Step title="2. 比较取舍曲线">
    保持案例不变，只提高一个压力变量，观察 职责边界
    的变化是否支持“能解释分层的边界与选择轴，逐项覆盖3个目录节点，并在同一应用切片中验证”。
    <Poeaa24Chapter01LayeringMappingLab />
  </Step>
  <Step title="3. 诊断失败并复位">
    切换临界和故障场景，找出 第1章 分层
    的第一个拒绝点；记录后点击“重置实验”恢复基线。
    <Poeaa24Chapter01LayeringTransactionLab />
  </Step>
</Stepper>

## 选择与拒绝矩阵

| 评审问题 | 选择 第1章 分层 的证据           | 应拒绝或改用其他方案的信号                      |
| -------- | -------------------------------- | ----------------------------------------------- |
| 责任     | 请求 到 运行环境 的所有者清晰    | 表示层 可以绕过边界直接改写状态                 |
| 变化     | 职责边界 的变化被局部吸收        | 一次小改动同时触及 依赖方向、层间契约、部署位置 |
| 失败     | 故障能在 运行环境 前被识别并回退 | 只能看到最终错误，无法定位 职责边界 的首个异常  |
| 替代     | 已与同族候选比较并保留撤回路径   | 因框架内置或团队习惯而跳过问题分析              |

<Callout type="trap" title="误区：每一层都能绕过相邻层直接访问数据库">
  对 第1章 分层，这种做法会让 职责边界 的真实所有者消失。修复方式是回到“请求 →
  表示层 → 领域层 → 数据源层 → 运行环境”逐点标出读取、决策、写入和错误翻译。
</Callout>

<Callout type="trap" title="误区：为了看起来分布式而把每个逻辑层部署成远程服务">
  第1章 分层 必须接受临界与故障输入；只展示成功路径会掩盖 依赖方向
  的代价，也无法证明方案可以复位或撤回。
</Callout>

## 本章小结

掌握 第1章 分层 的标志不是记住定义，而是能在 订单审批分层 中解释“请求 → 表示层 → 领域层 → 数据源层 → 运行环境”的责任链，利用 职责边界、依赖方向、层间契约、部署位置、变更触达 作出可证伪的选择，并在出现“每一层都能绕过相邻层直接访问数据库”时明确拒绝当前实现。

<Glossary>

<GlossaryItem term="第1章 分层">
  在 第1章 分层 中，第1章 分层 对应“请求”阶段的审查语言。它必须连接 职责边界
  的观测值与明确裁决，不能只作为框架类名出现。
</GlossaryItem>

<GlossaryItem term="请求">
  在 第1章 分层 中，请求 对应“表示层”阶段的审查语言。它必须连接 依赖方向
  的观测值与明确裁决，不能只作为框架类名出现。
</GlossaryItem>

<GlossaryItem term="运行环境">
  在 第1章 分层 中，运行环境 对应“领域层”阶段的审查语言。它必须连接 层间契约
  的观测值与明确裁决，不能只作为框架类名出现。
</GlossaryItem>

<GlossaryItem term="职责边界">
  在 第1章 分层 中，职责边界 对应“数据源层”阶段的审查语言。它必须连接 部署位置
  的观测值与明确裁决，不能只作为框架类名出现。
</GlossaryItem>

<GlossaryItem term="依赖方向">
  在 第1章 分层 中，依赖方向 对应“运行环境”阶段的审查语言。它必须连接 变更触达
  的观测值与明确裁决，不能只作为框架类名出现。
</GlossaryItem>

<GlossaryItem term="层间契约">
  在 第1章 分层 中，层间契约 对应“请求”阶段的审查语言。它必须连接 职责边界
  的观测值与明确裁决，不能只作为框架类名出现。
</GlossaryItem>

</Glossary>

<Exercises>

1. 为 第1章 分层 写一条最小设计合同：哪一个输入变化必须引起哪一个可观察结果？

<Answer>
  以 订单审批分层 为例，固定 请求 的输入和 运行环境 的期望结果，只改变
  职责边界；合同必须说明 第1章 分层
  在何值接受、何值拒绝，以及拒绝后怎样恢复到初始状态。
</Answer>

2. 在什么情况下不应选择 第1章 分层？

<Answer>
  当“能解释分层的边界与选择轴，逐项覆盖3个目录节点，并在同一应用切片中验证”无法成立，或
  职责边界、依赖方向
  的主要变化会同时穿透多个责任边界时，应回到同族候选比较。不能因为框架提供同名类或注解就继续使用
  第1章 分层。
</Answer>

3. 如何证明本页目录节点不是只在文字里出现？

<Answer>
  对 第1章 分层、1.1 企业应用中层次的演化、1.2 三个基本层次、1.3
  为各层选择运行环境
  逐项执行责任、取舍和失败实验；每个概念都要连接一个观察点、一条裁决和一次复位记录。若任一概念只有名称而没有
  职责边界 或 依赖方向 的验证证据，本页仍不通过。
</Answer>

</Exercises>

## 前后导航

- [上一页：第一部分 表述](/learn/poeaa-enterprise-patterns/05-part-01-narratives/poeaa24-part-01-narratives)
- [下一页：第2章 组织领域逻辑](/learn/poeaa-enterprise-patterns/07-chapter-02-organizing-domain-logic/poeaa24-chapter-02-organizing-domain-logic)

## 来源与改写范围

- [Martin Fowler 作者图书页](https://martinfowler.com/books/eaa.html)：核对全书主题、教程与模式参考结构。
- [Martin Fowler 模式目录](https://martinfowler.com/eaaCatalog/)：核对模式名称、所属模式族和作者公开摘要。
- [Pearson 出版社页面](https://www.pearson.com/en-us/subject-catalog/p/patterns-of-enterprise-application-architecture/P200000009121/9780133065213)：交叉核对英文版出版信息与目录范围。

<Attribution
  mode="independent-rewrite"
  workTitle="《企业应用架构模式》"
  adaptedFrom="2024 年中文版公开目录界定范围；作者与出版社页面核对事实；本页独立教学重写"
  adaptedUrl="https://martinfowler.com/books/eaa.html"
/>
