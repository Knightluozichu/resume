---
title: "第6章 会话状态"
type: "B"
section: "企业应用架构模式（2024年中文版）"
order: 11
description: "比较无状态价值以及客户端、服务器、数据库三种会话状态位置的可靠性和成本。"
demo: true
math: false
qualityVersion: 2
practiceMode: design
sourceMode: independent-rewrite
sourceUrl: "https://martinfowler.com/books/eaa.html"
draft: false
---

import {
  Poeaa24Chapter06SessionStateBoundaryLab,
  Poeaa24Chapter06SessionStateMappingLab,
  Poeaa24Chapter06SessionStateTransactionLab,
} from "@/components/mdx/poeaa-enterprise-patterns/diagrams/poeaa24-chapter-06-session-state";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能用自己的话解释 第6章 会话状态 要解决的问题，以及它不负责什么
- 能沿“请求 → 会话标识 → 状态读取 → 状态更新 → 过期恢复”定位责任，并比较 无状态、会话数据、存储位置
- 能操作 会话状态 实验，先预测变量变化，再用临界与故障场景验证因果
- 能完成设计评审：能解释会话状态的边界与选择轴，逐项覆盖3个目录节点，并在同一应用切片中验证

</Objectives>

## 为什么 第6章 会话状态 值得单独学习

比较无状态价值以及客户端、服务器、数据库三种会话状态位置的可靠性和成本。 第6章 会话状态 的核心不是套用某个框架 API，而是回答：如何选择会话状态的权威位置，并明确完整性、扩缩容、过期和恢复责任。在 订单向导会话 中，如果无法说清责任、状态和失败由谁承担，即使正常请求能够返回，架构决定也没有完成。

本页以 2024 年中文版公开目录限定 **第6章 会话状态** 的范围，并依据 Martin Fowler 的作者图书页和模式目录独立重写。它不复现原书正文、插图或代码；目录只决定“要讲什么”，这里的案例、实验、判断题和答案均为本课程原创。

## 先建立直觉：问题、机制与代价

第6章 会话状态 面对的具体压力是“状态大小、节点数量、粘性依赖、过期时间与恢复目标”。它采用的机制可以概括为：比较无状态价值以及客户端、服务器、数据库三种会话状态位置的可靠性和成本。 机制带来的收益必须与新增间接层、同步责任或迁移成本同时记录，否则学习者只会得到一个没有拒绝条件的模式名称。

对 第6章 会话状态，优先比较的替代路线是：按安全、可用性和运维约束比较客户端、服务器与数据库会话状态。本页的通过条件是“能解释会话状态的边界与选择轴，逐项覆盖3个目录节点，并在同一应用切片中验证”；若实验只能显示结果而不能指出 无状态 从何处越界，就不能据此选择 第6章 会话状态。

<Term def="第6章 会话状态 是 第6章 会话状态 的第 1 个裁决坐标；需要给出可观察信号、接受边界与反例。">
  第6章 会话状态
</Term>
、
<Term def="请求 是 第6章 会话状态 的第 2 个裁决坐标；需要给出可观察信号、接受边界与反例。">
  请求
</Term>
、
<Term def="过期恢复 是 第6章 会话状态 的第 3 个裁决坐标；需要给出可观察信号、接受边界与反例。">
  过期恢复
</Term>
、
<Term def="无状态 是 第6章 会话状态 的第 4 个裁决坐标；需要给出可观察信号、接受边界与反例。">
  无状态
</Term>
、
<Term def="会话数据 是 第6章 会话状态 的第 5 个裁决坐标；需要给出可观察信号、接受边界与反例。">
  会话数据
</Term>
、
<Term def="存储位置 是 第6章 会话状态 的第 6 个裁决坐标；需要给出可观察信号、接受边界与反例。">
  存储位置
</Term>

## 目录单元到教学证据

### 第6章 会话状态

在 **第6章 会话状态** 的学习边界里，第6章 会话状态 不是待背诵的目录词，而是用来检查“请求”是否把责任交给正确对象。对 订单向导会话，学习者要记录 无状态 的可观察变化，并说明它何时支持或否定 第6章 会话状态。

### 6.1 无状态的价值

在 **第6章 会话状态** 的学习边界里，6.1 无状态的价值 不是待背诵的目录词，而是用来检查“会话标识”是否把责任交给正确对象。对 订单向导会话，学习者要记录 会话数据 的可观察变化，并说明它何时支持或否定 第6章 会话状态。

### 6.2 会话状态

在 **第6章 会话状态** 的学习边界里，6.2 会话状态 不是待背诵的目录词，而是用来检查“状态读取”是否把责任交给正确对象。对 订单向导会话，学习者要记录 存储位置 的可观察变化，并说明它何时支持或否定 第6章 会话状态。

### 6.3 存储会话状态的方法

在 **第6章 会话状态** 的学习边界里，6.3 存储会话状态的方法 不是待背诵的目录词，而是用来检查“状态更新”是否把责任交给正确对象。对 订单向导会话，学习者要记录 伸缩 的可观察变化，并说明它何时支持或否定 第6章 会话状态。

## 专属设计案例：订单向导会话

把 订单向导会话 切成“请求 → 会话标识 → 状态读取 → 状态更新 → 过期恢复”五个观察点。第6章 会话状态 的设计草案必须写出谁拥有状态、谁作出业务决定、失败怎样传播，以及 无状态、会话数据、存储位置、伸缩、失效 中哪个指标最先提示当前方案不再适用。

设计记录采用五个可换行字段：单元键为 **poeaa24-chapter-06-session-state**；模式族为 **session**；裁决是“能解释会话状态的边界与选择轴，逐项覆盖3个目录节点，并在同一应用切片中验证”；观测项包括 无状态、会话数据、存储位置、伸缩、失效；拒绝条件是“无状态 超过团队为 第6章 会话状态 设定的边界”。

配置不是生产框架语法，而是一张评审卡。对 第6章 会话状态 的任何实现都要能把运行证据重新映射到这张卡；如果更换 ORM、Web 框架或部署平台后无法回答同一组问题，说明决定依赖的是工具偶然行为而不是模式语义。

## 三步交互实验

<Callout type="info" title="先预测，再操作">
  在改变滑块前，先预测 无状态 与 会话数据 哪个先越界。每次只改变一个量，并解释
  第6章 会话状态 的接受或拒绝为何随之变化。
</Callout>

<Stepper>
  <Step title="1. 核对责任边界">
    逐点选择 请求、会话标识、状态读取、状态更新、过期恢复，确认 第6章 会话状态
    没有把职责悄悄推给相邻层。
    <Poeaa24Chapter06SessionStateBoundaryLab />
  </Step>
  <Step title="2. 比较取舍曲线">
    保持案例不变，只提高一个压力变量，观察 无状态
    的变化是否支持“能解释会话状态的边界与选择轴，逐项覆盖3个目录节点，并在同一应用切片中验证”。
    <Poeaa24Chapter06SessionStateMappingLab />
  </Step>
  <Step title="3. 诊断失败并复位">
    切换临界和故障场景，找出 第6章 会话状态
    的第一个拒绝点；记录后点击“重置实验”恢复基线。
    <Poeaa24Chapter06SessionStateTransactionLab />
  </Step>
</Stepper>

## 选择与拒绝矩阵

| 评审问题 | 选择 第6章 会话状态 的证据       | 应拒绝或改用其他方案的信号                   |
| -------- | -------------------------------- | -------------------------------------------- |
| 责任     | 请求 到 过期恢复 的所有者清晰    | 会话标识 可以绕过边界直接改写状态            |
| 变化     | 无状态 的变化被局部吸收          | 一次小改动同时触及 会话数据、存储位置、伸缩  |
| 失败     | 故障能在 过期恢复 前被识别并回退 | 只能看到最终错误，无法定位 无状态 的首个异常 |
| 替代     | 已与同族候选比较并保留撤回路径   | 因框架内置或团队习惯而跳过问题分析           |

<Callout type="trap" title="误区：信任客户端状态却没有签名、版本和重放防护">
  对 第6章 会话状态，这种做法会让 无状态 的真实所有者消失。修复方式是回到“请求 →
  会话标识 → 状态读取 → 状态更新 → 过期恢复”逐点标出读取、决策、写入和错误翻译。
</Callout>

<Callout type="trap" title="误区：服务器扩容后仍假设后续请求必定命中原节点">
  第6章 会话状态 必须接受临界与故障输入；只展示成功路径会掩盖 会话数据
  的代价，也无法证明方案可以复位或撤回。
</Callout>

## 本章小结

掌握 第6章 会话状态 的标志不是记住定义，而是能在 订单向导会话 中解释“请求 → 会话标识 → 状态读取 → 状态更新 → 过期恢复”的责任链，利用 无状态、会话数据、存储位置、伸缩、失效 作出可证伪的选择，并在出现“信任客户端状态却没有签名、版本和重放防护”时明确拒绝当前实现。

<Glossary>

<GlossaryItem term="第6章 会话状态">
  在 第6章 会话状态 中，第6章 会话状态 对应“请求”阶段的审查语言。它必须连接
  无状态 的观测值与明确裁决，不能只作为框架类名出现。
</GlossaryItem>

<GlossaryItem term="请求">
  在 第6章 会话状态 中，请求 对应“会话标识”阶段的审查语言。它必须连接 会话数据
  的观测值与明确裁决，不能只作为框架类名出现。
</GlossaryItem>

<GlossaryItem term="过期恢复">
  在 第6章 会话状态 中，过期恢复 对应“状态读取”阶段的审查语言。它必须连接
  存储位置 的观测值与明确裁决，不能只作为框架类名出现。
</GlossaryItem>

<GlossaryItem term="无状态">
  在 第6章 会话状态 中，无状态 对应“状态更新”阶段的审查语言。它必须连接 伸缩
  的观测值与明确裁决，不能只作为框架类名出现。
</GlossaryItem>

<GlossaryItem term="会话数据">
  在 第6章 会话状态 中，会话数据 对应“过期恢复”阶段的审查语言。它必须连接 失效
  的观测值与明确裁决，不能只作为框架类名出现。
</GlossaryItem>

<GlossaryItem term="存储位置">
  在 第6章 会话状态 中，存储位置 对应“请求”阶段的审查语言。它必须连接 无状态
  的观测值与明确裁决，不能只作为框架类名出现。
</GlossaryItem>

</Glossary>

<Exercises>

1. 为 第6章 会话状态 写一条最小设计合同：哪一个输入变化必须引起哪一个可观察结果？

<Answer>
  以 订单向导会话 为例，固定 请求 的输入和 过期恢复 的期望结果，只改变
  无状态；合同必须说明 第6章 会话状态
  在何值接受、何值拒绝，以及拒绝后怎样恢复到初始状态。
</Answer>

2. 在什么情况下不应选择 第6章 会话状态？

<Answer>
  当“能解释会话状态的边界与选择轴，逐项覆盖3个目录节点，并在同一应用切片中验证”无法成立，或
  无状态、会话数据
  的主要变化会同时穿透多个责任边界时，应回到同族候选比较。不能因为框架提供同名类或注解就继续使用
  第6章 会话状态。
</Answer>

3. 如何证明本页目录节点不是只在文字里出现？

<Answer>
  对 第6章 会话状态、6.1 无状态的价值、6.2 会话状态、6.3 存储会话状态的方法
  逐项执行责任、取舍和失败实验；每个概念都要连接一个观察点、一条裁决和一次复位记录。若任一概念只有名称而没有
  无状态 或 会话数据 的验证证据，本页仍不通过。
</Answer>

</Exercises>

## 前后导航

- [上一页：第5章 并发](/learn/poeaa-enterprise-patterns/10-chapter-05-concurrency/poeaa24-chapter-05-concurrency)
- [下一页：第7章 分布策略](/learn/poeaa-enterprise-patterns/12-chapter-07-distribution-strategies/poeaa24-chapter-07-distribution-strategies)

## 来源与改写范围

- [Martin Fowler 作者图书页](https://martinfowler.com/books/eaa.html)：核对全书主题、教程与模式参考结构。
- [Martin Fowler 模式目录](https://martinfowler.com/eaaCatalog/)：核对模式名称、所属模式族和作者公开摘要。
- [Pearson 出版社页面](https://www.pearson.com/en-us/subject-catalog/p/patterns-of-enterprise-application-architecture/P200000009121/9780133065213)：交叉核对英文版出版信息与目录范围。

<Attribution
  mode="independent-rewrite"
  workTitle="《企业应用架构模式》"
  adaptedFrom="2024 年中文版公开目录界定范围；作者与出版社页面核对事实；本页独立教学重写"
  adaptedUrl="https://martinfowler.com/books/eaa.html"
/>
