---
title: "第二部分 模式"
type: B
section: "企业应用架构模式（2024年中文版）"
order: 14
description: "用10个模式族、51个模式形成可检索参考，每个选择都必须包含适用条件、替代方案和代价。"
demo: true
math: true
sourceUrl: "https://book.douban.com/subject/36867071/"
draft: false
---

import {
  Poeaa24Part02PatternsBoundaryLab,
  Poeaa24Part02PatternsMappingLab,
  Poeaa24Part02PatternsTransactionLab,
} from "@/components/mdx/poeaa-enterprise-patterns/diagrams/poeaa24-part-02-patterns";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第二部分 模式在76个正式单元、119个目录节点和51个模式中的职责与前后依赖
- 能比较10个模式族、51个模式、使用时机、协作模式、失败边界，并绘制“问题识别 → 模式族 → 模式机制 → 协作组合 → 取舍验证”边界链
- 能计算远程成本、映射成本、冲突风险与证据置信度，并验证正常、恰好边界和单故障样本
- 能设计独立复核，证明“能按问题而非流行框架选择模式，并用正常、边界和单故障样本验证。”而非只复述模式名称

</Objectives>

## 为什么这一单元不可压缩

本页依据马丁·福勒《企业应用架构模式》，王怀民、周斌译，机械工业出版社，2024年2月，ISBN 9787111746959，400页的公开完整中文目录，独立重构 **第二部分 模式**。第二部分 模式承担“问题识别 → 模式族 → 模式机制 → 协作组合 → 取舍验证”中的完整选择责任。用10个模式族、51个模式形成可检索参考，每个选择都必须包含适用条件、替代方案和代价。课程不复制原书正文、插图、代码或练习答案，而是把目录命题改写为新的架构模型、应用切片、交互实验、失败样本和可重放证据。

2024年中文版目录逐项列出译者序、前言、模式列表、引言及5节、两部分、18章、第1至8章38个叙述小节、第9至18章51个模式和参考文献，共76个正式单元、119个目录节点。Martin Fowler 官方图书页确认本书围绕企业应用的分层、领域逻辑、数据库映射、Web 表示、并发、会话和分布展开；作者官方目录逐项列出51个模式，Pearson 原版目录交叉确认18章。目录坐标决定学习分母，框架流行度不能替代原书结构。

本书出版语境早于今天常见的 ORM、依赖注入容器和 Web 框架。现代框架把工作单元、身份映射、前端控制器或数据传输对象封装成默认机制，却没有消除事务范围、对象身份、调用粒度、部分失败和一致性之间的裁决。学习者必须能在框架失效、默认值变化或边界跨进程时重新推导这些决定。

## 版次、对象与验收合同

<Term def="第二部分 模式是第二部分 模式的第1个复核坐标；必须写明问题、适用语境、机制、代价和反例。">
  第二部分 模式
</Term>
、
<Term def="10个模式族是第二部分 模式的第2个复核坐标；必须写明问题、适用语境、机制、代价和反例。">
  10个模式族
</Term>
、
<Term def="51个模式是第二部分 模式的第3个复核坐标；必须写明问题、适用语境、机制、代价和反例。">
  51个模式
</Term>
、
<Term def="使用时机是第二部分 模式的第4个复核坐标；必须写明问题、适用语境、机制、代价和反例。">
  使用时机
</Term>
、
<Term def="协作模式是第二部分 模式的第5个复核坐标；必须写明问题、适用语境、机制、代价和反例。">
  协作模式
</Term>

先固定应用切片：一个订单请求经过表示入口、用例服务、领域规则、对象关系映射和数据库提交；只有在确有远程边界时才引入网络。输入包括对象数、关联密度、事务持续时间、调用次数、载荷、延迟和证据覆盖率。输出必须包含结果、事务状态、查询与往返计数、冲突处理、错误翻译和回退记录。

通过条件不是“代码中出现了模式名”，而是 **能按问题而非流行框架选择模式，并用正常、边界和单故障样本验证。**。同一合同必须同时约束正常输入、恰好达到预算的边界输入和只注入一个故障的输入；任一层吞掉异常、重复提交或泄漏内部对象都算失败。

## 四个可手算模型

远程调用的粗略成本把往返延迟和载荷传输分开：

$$
C_r = n \times l + p / b
$$

其中 $n$ 是远程往返次数，$l$ 是单次延迟，$p$ 是载荷字节数，$b$ 是有效带宽。优化序列化却不减少高延迟往返，通常不会改变主导项。

对象关系映射成本由身份、关联、继承和变更跟踪共同决定：

$$
C_m = i + a + h + t
$$

并发风险不是数据库行数，而是重叠概率、业务事务持续时间和热点竞争的组合：

$$
R_c = o \times d \times q
$$

发布门禁采用乘法含义：内容覆盖、正常样本、边界样本、故障样本和独立复核任何一项为零，整体就不能通过。

$$
G = c 	imes n 	imes e 	imes f 	imes r
$$

这些式子不是生产容量模型，而是迫使设计者公开假设、单位和主导变量。先手算再运行交互实验；若工具结果与预期不同，保存最早出现差异的边界，不要从最终异常反向猜测。

## 原书目录坐标与核心机制

### 第9章 领域逻辑模式

用四种领域组织方式匹配业务复杂度和应用边界。 复核时逐项覆盖：9.1 事务脚本、9.2 领域模型、9.3 表模块、9.4 服务层。

### 第10章 数据源架构模式

选择领域对象与数据源交互的入口和隔离程度。 复核时逐项覆盖：10.1 表数据入口、10.2 行数据入口、10.3 活动记录、10.4 数据映射器。

### 第11章 对象-关系行为模式

协调一次事务中的身份、变更跟踪和按需加载。 复核时逐项覆盖：11.1 工作单元、11.2 标识映射、11.3 延迟加载。

### 第12章 对象-关系结构模式

把对象身份、关联、值与继承结构映射到关系模式。 复核时逐项覆盖：12.1 标识字段、12.2 外键映射、12.3 关联表映射、12.4 依赖映射、12.5 嵌入值、12.6 序列化LOB、12.7 单表继承、12.8 类表继承、12.9 具体表继承、12.10 继承映射器。

### 第13章 对象-关系元数据映射模式

以元数据、查询对象和资源库提升映射复用与领域表达。 复核时逐项覆盖：13.1 元数据映射、13.2 查询对象、13.3 资源库。

### 第14章 Web表现模式

分离请求处理、导航、模型和HTML生成的不同变化轴。 复核时逐项覆盖：14.1 模型-视图-控制器、14.2 页面控制器、14.3 前端控制器、14.4 模板视图、14.5 转换视图、14.6 两步视图、14.7 应用控制器。

### 第15章 分布模式

用粗粒度外观和数据载体控制网络往返与内部模型泄漏。 复核时逐项覆盖：15.1 远程外观、15.2 数据传输对象。

### 第16章 离线并发模式

在跨多个系统事务的业务事务中选择检测冲突或提前锁定。 复核时逐项覆盖：16.1 乐观离线锁、16.2 悲观离线锁、16.3 粗粒度锁、16.4 隐含锁。

### 第17章 会话状态模式

在客户端、服务器内存和数据库之间选择会话状态的权威位置。 复核时逐项覆盖：17.1 客户会话状态、17.2 服务器会话状态、17.3 数据库会话状态。

### 第18章 基本模式

以网关、映射器、接口、值对象和插件等基础构件支撑上层模式。 复核时逐项覆盖：18.1 入口、18.2 映射器、18.3 层超类型、18.4 分离接口、18.5 注册表、18.6 值对象、18.7 货币、18.8 特殊情况、18.9 插件、18.10 服务桩、18.11 记录集。

### 问题、语境、机制与后果

任何模式裁决先写问题：哪个变化、失败或复杂度正在伤害系统；再写语境：数据规模、团队边界、事务与部署约束是否成立；然后写机制：责任移到哪里、依赖方向怎样改变、状态由谁拥有；最后写后果：获得什么，同时增加了哪些间接层、同步成本和调试难度。

对第二部分 模式，最小证据链是“问题识别 → 模式族 → 模式机制 → 协作组合 → 取舍验证”。1. 10个模式族必须有一个可观察指标和一个拒绝阈值；2. 51个模式必须有一个可观察指标和一个拒绝阈值；3. 使用时机必须有一个可观察指标和一个拒绝阈值；4. 协作模式必须有一个可观察指标和一个拒绝阈值；5. 失败边界必须有一个可观察指标和一个拒绝阈值。只画静态类图不足以证明事务、加载、远程失败或会话过期行为，因此本页把拓扑、映射和事务证据拆成三个视图。

## 三视图交互验证

<Callout type="info" title="先预测，再操作">
  先预测对象规模或耦合升高时哪一个指标最先越界，再切换正常、恰好边界和单故障。每次只改一个变量，记录首差、原因和可回退状态。
</Callout>

<Stepper>
  <Step title="1. 冻结边界拓扑">
    选择“问题识别”到“取舍验证”中的检查点，确认责任、依赖方向和进程边界。不要把逻辑层与物理节点混为一谈。
    <Poeaa24Part02PatternsBoundaryLab />
  </Step>
  <Step title="2. 改变映射压力">
    保持用例不变，只调对象规模和结构耦合；观察身份、关联、加载与写回成本是否在预期节点出现首差。
    <Poeaa24Part02PatternsMappingLab />
  </Step>
  <Step title="3. 注入一个事务故障">
    固定前两步，切换边界或单故障样本；检查部分写入、冲突、重试、重复请求和证据置信是否按合同被拒绝。
    <Poeaa24Part02PatternsTransactionLab />
  </Step>
</Stepper>

## 最小实现骨架

第一段代码让应用服务只编排用例、事务与端口，领域规则仍由领域对象承担：

```ts
type Decision = { accepted: boolean; reason: string };

async function executeUseCase(command: Command): Promise<Decision> {
  return unitOfWork.run(async () => {
    const aggregate = await repository.get(command.id);
    const decision = aggregate.apply(command);
    if (!decision.accepted) return decision;
    await repository.save(aggregate);
    return decision;
  });
}
```

第二段显式记录版本，允许离线业务事务在提交点检测冲突：

```sql
UPDATE orders
SET status = :status, version = version + 1
WHERE order_id = :id AND version = :expected_version;
-- affected_rows must equal 1
```

第三段把模式决定写成可审查配置，而不是让框架默认值成为隐含架构：

```yaml
architecture_decision:
  unit: poeaa24-part-02-patterns
  transaction: one-use-case
  remote_calls_max: 4
  conflict_policy: reject-and-reload
  evidence: [normal, exact-boundary, single-fault]
```

代码骨架故意不绑定具体框架。迁移到 Spring、Jakarta、.NET、Rails 或其他栈时，应逐项映射工作单元、身份范围、异常翻译和请求生命周期；若框架只覆盖机制的一部分，剩余责任必须由应用明确承担。

## 选择矩阵与迁移策略

| 决策轴   | 低复杂度候选         | 高复杂度候选                     | 拒绝信号                       |
| -------- | -------------------- | -------------------------------- | ------------------------------ |
| 领域规则 | 事务脚本或表模块     | 领域模型与服务层协作             | 规则在控制器、SQL和模板间重复  |
| 数据映射 | 活动记录或入口对象   | 数据映射器、工作单元、资源库     | 同一身份出现冲突对象或部分写入 |
| Web 表示 | 页面控制器与模板视图 | 前端控制器、应用控制器、两步视图 | 认证、导航或业务规则散落       |
| 分布     | 保持本地调用         | 远程外观与DTO组成粗粒度边界      | 循环远程调用、内部对象直接越界 |
| 并发会话 | 短系统事务、无状态   | 明确离线锁和权威会话位置         | 静默覆盖、锁失主或会话不可清理 |

迁移顺序从观测开始：先记录当前查询、往返、事务、冲突和错误；再把一个用例放进新边界；随后以双读或影子流量比较语义；最后才删除旧路径。数据库结构、对象模型和远程契约不要同批无证据更换，否则首差无法归因。

<Callout type="trap" title="陷阱：把模式当成必须使用的零件">
  模式描述的是反复出现的问题与取舍，不是架构完成度清单。没有对象关系阻抗时强加数据映射器，或没有远程边界时先造DTO，都会增加间接层而不解决真实问题。
</Callout>

<Callout type="trap" title="陷阱：把框架默认值当成已验证边界">
  ORM 会话、Web
  请求和数据库事务的生命周期并不天然相同。必须用关闭会话后的延迟加载、并发提交、超时重试和部分失败样本验证，不能因为正常演示成功就宣布通过。
</Callout>

## 可重放证据与独立复核

每次实验保存版次、目录坐标、需求、代码哈希、迁移版本、工具版本、输入、初始数据库快照、时钟与单位；随后保存预期链、实际链、查询与远程轨迹、事务结果、首差和回退点。敏感数据只保留结构化摘要和不可逆标识，证据仍要足以重放。

独立复核者不能读取实现者的结论，只接收合同和样本。他先手算四个模型，再运行相同正常、边界和故障输入；双方必须在相同节点接受或拒绝，并能解释差异来自模式假设、框架机制还是环境。若只能复现最终错误却不能定位首差，证据仍不合格。

## 本章回顾

第二部分 模式的掌握标准是把“问题识别 → 模式族 → 模式机制 → 协作组合 → 取舍验证”变成可观察、可比较、可撤回的架构决定。记住模式名只能帮助检索；真正的能力是从问题和语境出发比较候选，明确机制与后果，再用正常、恰好边界、单故障和独立复核关闭证据链。

<Glossary>

<GlossaryItem term="第二部分 模式">
  第二部分
  模式在本单元中对应第1个架构裁决点。复核者要同时看到输入边界、候选方案、可观察量、失败条件和撤回路径，不能只看到框架
  API 或类名。
</GlossaryItem>

<GlossaryItem term="10个模式族">
  10个模式族在本单元中对应第2个架构裁决点。复核者要同时看到输入边界、候选方案、可观察量、失败条件和撤回路径，不能只看到框架
  API 或类名。
</GlossaryItem>

<GlossaryItem term="51个模式">
  51个模式在本单元中对应第3个架构裁决点。复核者要同时看到输入边界、候选方案、可观察量、失败条件和撤回路径，不能只看到框架
  API 或类名。
</GlossaryItem>

<GlossaryItem term="使用时机">
  使用时机在本单元中对应第4个架构裁决点。复核者要同时看到输入边界、候选方案、可观察量、失败条件和撤回路径，不能只看到框架
  API 或类名。
</GlossaryItem>

<GlossaryItem term="协作模式">
  协作模式在本单元中对应第5个架构裁决点。复核者要同时看到输入边界、候选方案、可观察量、失败条件和撤回路径，不能只看到框架
  API 或类名。
</GlossaryItem>

</Glossary>

<Exercises>

1. 如何为第二部分 模式建立最小可重放基线？

<Answer>
  固定2024年中文版目录坐标、应用切片、代码与迁移哈希、输入、初始状态和单位，先画“问题识别
  → 模式族 → 模式机制 → 协作组合 →
  取舍验证”预期，再保存正常、恰好边界和单故障轨迹。
</Answer>

2. 怎样证明“能按问题而非流行框架选择模式，并用正常、边界和单故障样本验证。”不是一次偶然成功？

<Answer>
  先手算远程成本、映射成本和冲突风险，再让独立复核者在同一边界重建；双方必须在相同首差拒绝故障，并保留一致的回退状态与替代方案记录。
</Answer>

3. 修复一个失败后怎样做相邻回归？

<Answer>
  先重放原失败，再运行相邻边界和正常基线；逐节点检查10个模式族、51个模式、使用时机、协作模式、失败边界，确认查询、事务、远程和会话证据没有出现新首差后才进入下一页。
</Answer>

</Exercises>

## 前后导航

- [上一页：第8章 通盘考虑](/learn/poeaa-enterprise-patterns/13-chapter-08-putting-together/poeaa24-chapter-08-putting-together)
- [下一页：第9章 领域逻辑模式](/learn/poeaa-enterprise-patterns/15-chapter-09-domain-logic-patterns/poeaa24-chapter-09-domain-logic-patterns)

<Attribution
  adaptedFrom="马丁·福勒《企业应用架构模式》，王怀民、周斌译，机械工业出版社，2024年2月，ISBN 9787111746959，400页 · 中文公开完整目录含76个正式单元、119个目录节点、18章和51个模式 · 基于公开目录独立中文教学重构，不复制原书正文、插图、代码或练习答案"
  adaptedUrl="https://book.douban.com/subject/36867071/"
/>
