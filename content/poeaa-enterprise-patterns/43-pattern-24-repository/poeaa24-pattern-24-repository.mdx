---
title: "13.3 资源库"
type: "B"
section: "企业应用架构模式（2024年中文版）"
order: 43
description: "在领域与数据映射层之间提供类似集合的接口，集中表达领域对象的获取和保存意图。"
demo: true
math: false
qualityVersion: 2
practiceMode: design
sourceMode: independent-rewrite
sourceUrl: "https://martinfowler.com/books/eaa.html"
draft: false
---

import {
  Poeaa24Pattern24RepositoryBoundaryLab,
  Poeaa24Pattern24RepositoryMappingLab,
  Poeaa24Pattern24RepositoryTransactionLab,
} from "@/components/mdx/poeaa-enterprise-patterns/diagrams/poeaa24-pattern-24-repository";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能用自己的话解释 13.3 资源库 要解决的问题，以及它不负责什么
- 能沿“领域意图 → 查询对象 → 映射元数据 → 执行 → 对象结果”定位责任，并比较 元数据校验、查询组合、资源库边界
- 能操作 对象关系映射 实验，先预测变量变化，再用临界与故障场景验证因果
- 能完成设计评审：能让领域服务使用领域词汇查询聚合，证明资源库没有承载业务规则或返回持久化细节

</Objectives>

## 为什么 13.3 资源库 值得单独学习

在领域与数据映射层之间提供类似集合的接口，集中表达领域对象的获取和保存意图。 13.3 资源库 的核心不是套用某个框架 API，而是回答：如何在对象身份、关联、继承和写回之间保住一致语义，同时控制查询与映射成本。在 订单聚合持久化 中，如果无法说清责任、状态和失败由谁承担，即使正常请求能够返回，架构决定也没有完成。

本页以 2024 年中文版公开目录限定 **13.3 资源库** 的范围，并依据 Martin Fowler 的作者图书页和模式目录独立重写。它不复现原书正文、插图或代码；目录只决定“要讲什么”，这里的案例、实验、判断题和答案均为本课程原创。

## 先建立直觉：问题、机制与代价

13.3 资源库 面对的具体压力是“同一身份实例数、查询次数、脏对象集合与关联装载范围”。它采用的机制可以概括为：在领域与数据映射层之间提供类似集合的接口，集中表达领域对象的获取和保存意图。 机制带来的收益必须与新增间接层、同步责任或迁移成本同时记录，否则学习者只会得到一个没有拒绝条件的模式名称。

对 13.3 资源库，优先比较的替代路线是：根据领域复杂度选择入口、活动记录、映射器、工作单元及结构映射组合。本页的通过条件是“能让领域服务使用领域词汇查询聚合，证明资源库没有承载业务规则或返回持久化细节”；若实验只能显示结果而不能指出 元数据校验 从何处越界，就不能据此选择 13.3 资源库。

<Term def="13.3 资源库 是 13.3 资源库 的第 1 个裁决坐标；需要给出可观察信号、接受边界与反例。">
  13.3 资源库
</Term>
、
<Term def="领域意图 是 13.3 资源库 的第 2 个裁决坐标；需要给出可观察信号、接受边界与反例。">
  领域意图
</Term>
、
<Term def="对象结果 是 13.3 资源库 的第 3 个裁决坐标；需要给出可观察信号、接受边界与反例。">
  对象结果
</Term>
、
<Term def="元数据校验 是 13.3 资源库 的第 4 个裁决坐标；需要给出可观察信号、接受边界与反例。">
  元数据校验
</Term>
、
<Term def="查询组合 是 13.3 资源库 的第 5 个裁决坐标；需要给出可观察信号、接受边界与反例。">
  查询组合
</Term>
、
<Term def="资源库边界 是 13.3 资源库 的第 6 个裁决坐标；需要给出可观察信号、接受边界与反例。">
  资源库边界
</Term>

## 目录单元到教学证据

### 13.3 资源库

在 **13.3 资源库** 的学习边界里，13.3 资源库 不是待背诵的目录词，而是用来检查“领域意图”是否把责任交给正确对象。对 订单聚合持久化，学习者要记录 元数据校验 的可观察变化，并说明它何时支持或否定 13.3 资源库。

## 专属设计案例：订单聚合持久化

把 订单聚合持久化 切成“领域意图 → 查询对象 → 映射元数据 → 执行 → 对象结果”五个观察点。13.3 资源库 的设计草案必须写出谁拥有状态、谁作出业务决定、失败怎样传播，以及 元数据校验、查询组合、资源库边界、参数化、类型恢复 中哪个指标最先提示当前方案不再适用。

设计记录采用五个可换行字段：单元键为 **poeaa24-pattern-24-repository**；模式族为 **mapping**；裁决是“能让领域服务使用领域词汇查询聚合，证明资源库没有承载业务规则或返回持久化细节”；观测项包括 元数据校验、查询组合、资源库边界、参数化、类型恢复；拒绝条件是“元数据校验 超过团队为 13.3 资源库 设定的边界”。

配置不是生产框架语法，而是一张评审卡。对 13.3 资源库 的任何实现都要能把运行证据重新映射到这张卡；如果更换 ORM、Web 框架或部署平台后无法回答同一组问题，说明决定依赖的是工具偶然行为而不是模式语义。

## 三步交互实验

<Callout type="info" title="先预测，再操作">
  在改变滑块前，先预测 元数据校验 与 查询组合
  哪个先越界。每次只改变一个量，并解释 13.3 资源库 的接受或拒绝为何随之变化。
</Callout>

<Stepper>
  <Step title="1. 核对责任边界">
    逐点选择 领域意图、查询对象、映射元数据、执行、对象结果，确认 13.3 资源库
    没有把职责悄悄推给相邻层。
    <Poeaa24Pattern24RepositoryBoundaryLab />
  </Step>
  <Step title="2. 比较取舍曲线">
    保持案例不变，只提高一个压力变量，观察 元数据校验
    的变化是否支持“能让领域服务使用领域词汇查询聚合，证明资源库没有承载业务规则或返回持久化细节”。
    <Poeaa24Pattern24RepositoryMappingLab />
  </Step>
  <Step title="3. 诊断失败并复位">
    切换临界和故障场景，找出 13.3 资源库
    的第一个拒绝点；记录后点击“重置实验”恢复基线。
    <Poeaa24Pattern24RepositoryTransactionLab />
  </Step>
</Stepper>

## 选择与拒绝矩阵

| 评审问题 | 选择 13.3 资源库 的证据           | 应拒绝或改用其他方案的信号                       |
| -------- | --------------------------------- | ------------------------------------------------ |
| 责任     | 领域意图 到 对象结果 的所有者清晰 | 查询对象 可以绕过边界直接改写状态                |
| 变化     | 元数据校验 的变化被局部吸收       | 一次小改动同时触及 查询组合、资源库边界、参数化  |
| 失败     | 故障能在 对象结果 前被识别并回退  | 只能看到最终错误，无法定位 元数据校验 的首个异常 |
| 替代     | 已与同族候选比较并保留撤回路径    | 因框架内置或团队习惯而跳过问题分析               |

<Callout
  type="trap"
  title="误区：把数据库行和内存对象的身份生命周期视为天然相同"
>
  对 13.3 资源库，这种做法会让 元数据校验
  的真实所有者消失。修复方式是回到“领域意图 → 查询对象 → 映射元数据 → 执行 →
  对象结果”逐点标出读取、决策、写入和错误翻译。
</Callout>

<Callout type="trap" title="误区：用一次正常查询掩盖 N+1、重复实例或部分写回">
  13.3 资源库 必须接受临界与故障输入；只展示成功路径会掩盖 查询组合
  的代价，也无法证明方案可以复位或撤回。
</Callout>

## 本章小结

掌握 13.3 资源库 的标志不是记住定义，而是能在 订单聚合持久化 中解释“领域意图 → 查询对象 → 映射元数据 → 执行 → 对象结果”的责任链，利用 元数据校验、查询组合、资源库边界、参数化、类型恢复 作出可证伪的选择，并在出现“把数据库行和内存对象的身份生命周期视为天然相同”时明确拒绝当前实现。

<Glossary>

<GlossaryItem term="13.3 资源库">
  在 13.3 资源库 中，13.3 资源库 对应“领域意图”阶段的审查语言。它必须连接
  元数据校验 的观测值与明确裁决，不能只作为框架类名出现。
</GlossaryItem>

<GlossaryItem term="领域意图">
  在 13.3 资源库 中，领域意图 对应“查询对象”阶段的审查语言。它必须连接 查询组合
  的观测值与明确裁决，不能只作为框架类名出现。
</GlossaryItem>

<GlossaryItem term="对象结果">
  在 13.3 资源库 中，对象结果 对应“映射元数据”阶段的审查语言。它必须连接
  资源库边界 的观测值与明确裁决，不能只作为框架类名出现。
</GlossaryItem>

<GlossaryItem term="元数据校验">
  在 13.3 资源库 中，元数据校验 对应“执行”阶段的审查语言。它必须连接 参数化
  的观测值与明确裁决，不能只作为框架类名出现。
</GlossaryItem>

<GlossaryItem term="查询组合">
  在 13.3 资源库 中，查询组合 对应“对象结果”阶段的审查语言。它必须连接 类型恢复
  的观测值与明确裁决，不能只作为框架类名出现。
</GlossaryItem>

<GlossaryItem term="资源库边界">
  在 13.3 资源库 中，资源库边界 对应“领域意图”阶段的审查语言。它必须连接
  元数据校验 的观测值与明确裁决，不能只作为框架类名出现。
</GlossaryItem>

</Glossary>

<Exercises>

1. 为 13.3 资源库 写一条最小设计合同：哪一个输入变化必须引起哪一个可观察结果？

<Answer>
  以 订单聚合持久化 为例，固定 领域意图 的输入和 对象结果 的期望结果，只改变
  元数据校验；合同必须说明 13.3 资源库
  在何值接受、何值拒绝，以及拒绝后怎样恢复到初始状态。
</Answer>

2. 在什么情况下不应选择 13.3 资源库？

<Answer>
  当“能让领域服务使用领域词汇查询聚合，证明资源库没有承载业务规则或返回持久化细节”无法成立，或
  元数据校验、查询组合
  的主要变化会同时穿透多个责任边界时，应回到同族候选比较。不能因为框架提供同名类或注解就继续使用
  13.3 资源库。
</Answer>

3. 如何证明本页目录节点不是只在文字里出现？

<Answer>
  对 13.3 资源库
  逐项执行责任、取舍和失败实验；每个概念都要连接一个观察点、一条裁决和一次复位记录。若任一概念只有名称而没有
  元数据校验 或 查询组合 的验证证据，本页仍不通过。
</Answer>

</Exercises>

## 前后导航

- [上一页：13.2 查询对象](/learn/poeaa-enterprise-patterns/42-pattern-23-query-object/poeaa24-pattern-23-query-object)
- [下一页：第14章 Web表现模式](/learn/poeaa-enterprise-patterns/44-chapter-14-web-presentation-patterns/poeaa24-chapter-14-web-presentation-patterns)

## 来源与改写范围

- [Martin Fowler 作者图书页](https://martinfowler.com/books/eaa.html)：核对全书主题、教程与模式参考结构。
- [Martin Fowler 模式目录](https://martinfowler.com/eaaCatalog/)：核对模式名称、所属模式族和作者公开摘要。
- [Pearson 出版社页面](https://www.pearson.com/en-us/subject-catalog/p/patterns-of-enterprise-application-architecture/P200000009121/9780133065213)：交叉核对英文版出版信息与目录范围。

<Attribution
  mode="independent-rewrite"
  workTitle="《企业应用架构模式》"
  adaptedFrom="2024 年中文版公开目录界定范围；作者与出版社页面核对事实；本页独立教学重写"
  adaptedUrl="https://martinfowler.com/books/eaa.html"
/>
