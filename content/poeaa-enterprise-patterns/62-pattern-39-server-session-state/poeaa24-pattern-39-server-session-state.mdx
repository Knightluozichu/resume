---
title: "17.2 服务器会话状态"
type: "B"
section: "企业应用架构模式（2024年中文版）"
order: 62
description: "在服务器系统中以序列化形式保存会话状态，由会话标识定位。"
demo: true
math: false
qualityVersion: 2
practiceMode: design
sourceMode: independent-rewrite
sourceUrl: "https://martinfowler.com/books/eaa.html"
draft: false
---

import { Poeaa24Pattern39ServerSessionState } from "@/components/mdx/poeaa-enterprise-patterns/diagrams/poeaa24-pattern-39-server-session-state-diagram";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  TermSequence,
  Exercises,
  Answer,

  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能用自己的话解释 17.2 服务器会话状态 要解决的问题，以及它不负责什么
- 能沿“请求 → 会话键 → 状态位置 → 更新 → 过期”定位责任，并比较 可信边界、容量、多节点
- 能操作 会话状态 实验，先预测变量变化，再用临界与故障场景验证因果
- 能完成设计评审：能在多节点环境定位会话、处理失效与清理，并演练节点故障后的恢复或明确丢失语义

</Objectives>

## 为什么 17.2 服务器会话状态 值得单独学习

在服务器系统中以序列化形式保存会话状态，由会话标识定位。 17.2 服务器会话状态 的核心不是套用某个框架 API，而是回答：如何选择会话状态的权威位置，并明确完整性、扩缩容、过期和恢复责任。在 订单向导会话 中，如果无法说清责任、状态和失败由谁承担，即使正常请求能够返回，架构决定也没有完成。

本页以 2024 年中文版公开目录限定 **17.2 服务器会话状态** 的范围，并依据 Martin Fowler 的作者图书页和模式目录独立重写。它不复现原书正文、插图或代码；目录只决定“要讲什么”，这里的案例、实验、判断题和答案均为本课程原创。

## 先建立直觉：问题、机制与代价

17.2 服务器会话状态 面对的具体压力是“状态大小、节点数量、粘性依赖、过期时间与恢复目标”。它采用的机制可以概括为：在服务器系统中以序列化形式保存会话状态，由会话标识定位。 机制带来的收益必须与新增间接层、同步责任或迁移成本同时记录，否则学习者只会得到一个没有拒绝条件的模式名称。

对 17.2 服务器会话状态，优先比较的替代路线是：按安全、可用性和运维约束比较客户端、服务器与数据库会话状态。本页的通过条件是“能在多节点环境定位会话、处理失效与清理，并演练节点故障后的恢复或明确丢失语义”；若实验只能显示结果而不能指出 可信边界 从何处越界，就不能据此选择 17.2 服务器会话状态。

<TermSequence
  items={[
    {
      term: "17.2 服务器会话状态",
      def: "17.2 服务器会话状态 是 17.2 服务器会话状态 的第 1 个裁决坐标；需要给出可观察信号、接受边界与反例。",
    },
    {
      term: "请求",
      def: "请求 是 17.2 服务器会话状态 的第 2 个裁决坐标；需要给出可观察信号、接受边界与反例。",
    },
    {
      term: "过期",
      def: "过期 是 17.2 服务器会话状态 的第 3 个裁决坐标；需要给出可观察信号、接受边界与反例。",
    },
    {
      term: "可信边界",
      def: "可信边界 是 17.2 服务器会话状态 的第 4 个裁决坐标；需要给出可观察信号、接受边界与反例。",
    },
    {
      term: "容量",
      def: "容量 是 17.2 服务器会话状态 的第 5 个裁决坐标；需要给出可观察信号、接受边界与反例。",
    },
    {
      term: "多节点",
      def: "多节点 是 17.2 服务器会话状态 的第 6 个裁决坐标；需要给出可观察信号、接受边界与反例。",
    },
  ]}
/>

## 目录单元到教学证据

### 17.2 服务器会话状态

在 **17.2 服务器会话状态** 的学习边界里，17.2 服务器会话状态 不是待背诵的目录词，而是用来检查“请求”是否把责任交给正确对象。对 订单向导会话，学习者要记录 可信边界 的可观察变化，并说明它何时支持或否定 17.2 服务器会话状态。

## 专属设计案例：订单向导会话

把 订单向导会话 切成“请求 → 会话键 → 状态位置 → 更新 → 过期”五个观察点。17.2 服务器会话状态 的设计草案必须写出谁拥有状态、谁作出业务决定、失败怎样传播，以及 可信边界、容量、多节点、持久性、清理 中哪个指标最先提示当前方案不再适用。

设计记录采用五个可换行字段：单元键为 **poeaa24-pattern-39-server-session-state**；模式族为 **session**；裁决是“能在多节点环境定位会话、处理失效与清理，并演练节点故障后的恢复或明确丢失语义”；观测项包括 可信边界、容量、多节点、持久性、清理；拒绝条件是“可信边界 超过团队为 17.2 服务器会话状态 设定的边界”。

配置不是生产框架语法，而是一张评审卡。对 17.2 服务器会话状态 的任何实现都要能把运行证据重新映射到这张卡；如果更换 ORM、Web 框架或部署平台后无法回答同一组问题，说明决定依赖的是工具偶然行为而不是模式语义。

## 三步交互实验

<Callout type="info" title="先看结构，再看细节">
  下图展示服务器会话状态：客户端只持 ID，状态存服务器内存，节点故障时会话丢失。
</Callout>

<Poeaa24Pattern39ServerSessionState />

## 选择与拒绝矩阵

| 评审问题 | 选择 17.2 服务器会话状态 的证据 | 应拒绝或改用其他方案的信号                     |
| -------- | ------------------------------- | ---------------------------------------------- |
| 责任     | 请求 到 过期 的所有者清晰       | 会话键 可以绕过边界直接改写状态                |
| 变化     | 可信边界 的变化被局部吸收       | 一次小改动同时触及 容量、多节点、持久性        |
| 失败     | 故障能在 过期 前被识别并回退    | 只能看到最终错误，无法定位 可信边界 的首个异常 |
| 替代     | 已与同族候选比较并保留撤回路径  | 因框架内置或团队习惯而跳过问题分析             |

<Callout type="trap" title="误区：信任客户端状态却没有签名、版本和重放防护">
  对 17.2 服务器会话状态，这种做法会让 可信边界
  的真实所有者消失。修复方式是回到“请求 → 会话键 → 状态位置 → 更新 →
  过期”逐点标出读取、决策、写入和错误翻译。
</Callout>

<Callout type="trap" title="误区：服务器扩容后仍假设后续请求必定命中原节点">
  17.2 服务器会话状态 必须接受临界与故障输入；只展示成功路径会掩盖 容量
  的代价，也无法证明方案可以复位或撤回。
</Callout>

## 本章小结

掌握 17.2 服务器会话状态 的标志不是记住定义，而是能在 订单向导会话 中解释“请求 → 会话键 → 状态位置 → 更新 → 过期”的责任链，利用 可信边界、容量、多节点、持久性、清理 作出可证伪的选择，并在出现“信任客户端状态却没有签名、版本和重放防护”时明确拒绝当前实现。

<Glossary>

<GlossaryItem term="17.2 服务器会话状态">
  在 17.2 服务器会话状态 中，17.2 服务器会话状态
  对应“请求”阶段的审查语言。它必须连接 可信边界
  的观测值与明确裁决，不能只作为框架类名出现。
</GlossaryItem>

<GlossaryItem term="请求">
  在 17.2 服务器会话状态 中，请求 对应“会话键”阶段的审查语言。它必须连接 容量
  的观测值与明确裁决，不能只作为框架类名出现。
</GlossaryItem>

<GlossaryItem term="过期">
  在 17.2 服务器会话状态 中，过期 对应“状态位置”阶段的审查语言。它必须连接
  多节点 的观测值与明确裁决，不能只作为框架类名出现。
</GlossaryItem>

<GlossaryItem term="可信边界">
  在 17.2 服务器会话状态 中，可信边界 对应“更新”阶段的审查语言。它必须连接
  持久性 的观测值与明确裁决，不能只作为框架类名出现。
</GlossaryItem>

<GlossaryItem term="容量">
  在 17.2 服务器会话状态 中，容量 对应“过期”阶段的审查语言。它必须连接 清理
  的观测值与明确裁决，不能只作为框架类名出现。
</GlossaryItem>

<GlossaryItem term="多节点">
  在 17.2 服务器会话状态 中，多节点 对应“请求”阶段的审查语言。它必须连接
  可信边界 的观测值与明确裁决，不能只作为框架类名出现。
</GlossaryItem>

</Glossary>

<Exercises>

1. 为 17.2 服务器会话状态 写一条最小设计合同：哪一个输入变化必须引起哪一个可观察结果？

<Answer>
  以 订单向导会话 为例，固定 请求 的输入和 过期 的期望结果，只改变
  可信边界；合同必须说明 17.2 服务器会话状态
  在何值接受、何值拒绝，以及拒绝后怎样恢复到初始状态。
</Answer>

2. 在什么情况下不应选择 17.2 服务器会话状态？

<Answer>
  当“能在多节点环境定位会话、处理失效与清理，并演练节点故障后的恢复或明确丢失语义”无法成立，或
  可信边界、容量
  的主要变化会同时穿透多个责任边界时，应回到同族候选比较。不能因为框架提供同名类或注解就继续使用
  17.2 服务器会话状态。
</Answer>

3. 如何证明本页目录节点不是只在文字里出现？

<Answer>
  对 17.2 服务器会话状态
  逐项执行责任、取舍和失败实验；每个概念都要连接一个观察点、一条裁决和一次复位记录。若任一概念只有名称而没有
  可信边界 或 容量 的验证证据，本页仍不通过。
</Answer>

</Exercises>

## 前后导航

- [上一页：17.1 客户会话状态](/learn/poeaa-enterprise-patterns/61-pattern-38-client-session-state/poeaa24-pattern-38-client-session-state)
- [下一页：17.3 数据库会话状态](/learn/poeaa-enterprise-patterns/63-pattern-40-database-session-state/poeaa24-pattern-40-database-session-state)

## 来源与改写范围

- [Martin Fowler 作者图书页](https://martinfowler.com/books/eaa.html)：核对全书主题、教程与模式参考结构。
- [Martin Fowler 模式目录](https://martinfowler.com/eaaCatalog/)：核对模式名称、所属模式族和作者公开摘要。
- [Pearson 出版社页面](https://www.pearson.com/en-us/subject-catalog/p/patterns-of-enterprise-application-architecture/P200000009121/9780133065213)：交叉核对英文版出版信息与目录范围。

<Attribution
  mode="independent-rewrite"
  workTitle="《企业应用架构模式》"
  adaptedFrom="2024 年中文版公开目录界定范围；作者与出版社页面核对事实；本页独立教学重写"
  adaptedUrl="https://martinfowler.com/books/eaa.html"
/>
