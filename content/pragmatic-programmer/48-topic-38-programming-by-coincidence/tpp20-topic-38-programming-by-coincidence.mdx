---
title: 38 巧合式编程
type: B
section: 程序员修炼之道（第2版·20周年版）
order: 48
description: 把偶然可运行的代码转成有意图、有假设和有边界的实现，不依赖未说明的环境巧合。
demo: true
math: true
sourceUrl: "https://www.w3cschool.cn/booklist/booklist-k3pw3fff.html"
draft: false
qualityVersion: 2
practiceMode: diagnosis
sourceMode: independent-rewrite
officialUnitId: tpp20-topic-38-programming-by-coincidence
---

import { Objectives } from "@/components/mdx/objectives";
import { Term } from "@/components/mdx/term";
import { Callout } from "@/components/mdx/callout";
import { Step, Stepper } from "@/components/mdx/stepper";
import { Answer, Exercises } from "@/components/mdx/exercises";
import { Glossary, GlossaryItem } from "@/components/mdx/glossary";
import { Attribution } from "@/components/mdx/attribution";
import {
  Tpp20Topic38ProgrammingByCoincidenceEvidenceDiagram,
  Tpp20Topic38ProgrammingByCoincidenceLoopDiagram,
  Tpp20Topic38ProgrammingByCoincidenceLab,
} from "@/components/mdx/pragmatic-programmer/diagrams/tpp20-topic-38-programming-by-coincidence-diagram";

<Objectives>

- 能找出代码依赖的默认顺序、隐含状态、环境细节和未声明的输入假设
- 能用反向顺序、边界输入和隔离环境验证因果，并把巧合改成显式契约
- 能以回归测试、可回退提交和失败记录证明修复没有把风险移到下游

</Objectives>

## 能运行不等于有因果

本页依据 David Thomas、Andrew Hunt《程序员修炼之道：通向务实的最高境界（第2版）》，云风译，电子工业出版社，2020 年 4 月，ISBN 9787121384356 的公开中文目录，独立重构 **38 巧合式编程**。正文、代码、图示、实验和练习都是本课程重新设计的教学材料，不复制原书正文、插图或答案。

代码在一个环境里运行，可能只是因为多个未声明条件恰好同时成立：字典顺序没有变化、初始化已经被别处完成、机器时区与测试数据相同，或数据库返回了你没有要求的排序。问题不在于代码曾经成功，而在于成功的因果没有写进合同。巧合式编程要做的第一件事，是把“为什么它能工作”变成一个可以被打破、验证和重建的假设。

## 三个会放大巧合的陷阱

<Callout type="trap" title="把默认顺序当成接口合同">

- **现象**：遍历结果在开发机稳定，换运行时或数据源后顺序改变。
- **原因**：调用者依赖了实现细节，却没有声明排序键和稳定性要求。
- **修法**：在边界显式排序，并用反向输入与重复键测试排序合同。

</Callout>

<Callout type="trap" title="把已经初始化当成永远初始化">

- **现象**：单独运行函数失败，完整应用流程却总能通过。
- **原因**：前置模块偷偷改变了全局状态，函数自身没有表达依赖。
- **修法**：在干净进程中直接调用，失败后把依赖放入参数、构造过程或明确的启动检查。

</Callout>

<Callout type="trap" title="用更多日志替代明确契约">

- **现象**：日志越来越多，仍不能判断哪一个状态让结果改变。
- **原因**：记录了事件，却没有定义输入、输出、允许顺序和拒绝条件。
- **修法**：先画因果链，再只记录能区分两个解释的观察点；失败样本也要可重放。

</Callout>

## 从观察到显式契约

`<Term def="代码在未声明的顺序、状态、环境或实现细节恰好成立时产生的成功。">巧合式编程</Term>`需要通过五步回路转化。每一步都有一个可失败问题：

1. **观察**：成功依赖了什么动作、数据和环境条件？
2. **列假设**：如果反转顺序、清空状态或更换区域设置，哪个结果会变？
3. **验证因果**：只改变一个条件，比较预测与实际的首个差异。
4. **显式设计**：把真实依赖写入类型、参数、排序、初始化或权限检查。
5. **回归**：保存能打破旧巧合的输入，并在修复后持续重放。

<Tpp20Topic38ProgrammingByCoincidenceLoopDiagram />

图中的回路不把“验证通过”当成永久保证。环境、数据分布和依赖版本会变化，所以回归集合必须包含曾经失败的最小输入。若修复只让一个样例通过，却没有覆盖真正的依赖，下一次变化仍会把巧合带回来。

## 38 与提示 62：不要依赖巧合编程

本单元对应中文目录的 **38 巧合式编程** 和 **提示 62：不要依赖巧合编程**。提示的可检验解释是：任何未写入调用合同、数据合同或部署合同的前提，都只能暂时当作假设，不能当作能力。

可以用四个问题审查一段“恰好能跑”的代码：

- 如果调用顺序倒过来，结果还成立吗？
- 如果所有可变状态从零开始，接口仍然自洽吗？
- 如果输入为空、重复、跨时区或超出容量，拒绝路径在哪里？
- 如果依赖升级或部署到另一台机器，谁负责重新验证？

例如，价格汇总函数依赖数据库默认返回顺序，把第一条记录当成当前价格。最小实验不是迁移整个数据库，而是给同一查询增加显式排序和反向种子，再比较结果。若结果改变，巧合已经被证实；修复要把排序键写入查询合同，并把重复价格的决定规则写成测试。

## 把隐藏依赖写出来

`<Term def="代码运行所需要但没有出现在输入、类型、配置校验或文档合同中的前提。">隐含假设</Term>`常见于时区、编码、排序、默认超时、全局单例、权限和初始化。列清假设后，不必一次消除所有风险；先挑一个最可能改变结果且最容易验证的前提。

`<Term def="明确规定输入形状、输出意义、顺序、错误和副作用的边界，使调用者不必猜测实现细节。">显式契约</Term>`可以是类型、参数、断言、配置 schema、数据库约束或端到端测试。契约不是一段解释文字，而是调用错误输入时能给出可观察拒绝的规则。

```ts
type Event = { id: string; occurredAt: string; kind: "created" | "paid" };

function latestPaid(events: ReadonlyArray<Event>): Event | null {
  const paid = events
    .filter((event) => event.kind === "paid")
    .toSorted((left, right) => left.occurredAt.localeCompare(right.occurredAt));
  return paid.at(-1) ?? null;
}
```

这段代码不依赖输入数组原本的顺序，也不把“总会有一条付款事件”当成前提。若时间格式不是明确的可比较格式，契约还应在输入边界拒绝它，而不是让排序结果看起来合理。

## 用反例验证因果

`<Term def="能在复核时指出某个条件改变后，哪一个观察点先产生不同结果的记录。">可回退证据</Term>`要同时保留预测、实际、首个差异和恢复路径。只有最终输出不同，仍然可能是多个变化叠加；只有首个差异被定位，修复才有方向。

对巧合最有效的反例通常很小：空集合、重复键、反向顺序、干净进程、不同区域设置、过期缓存或缺失权限。每次只选一个反例维度。若它没有打破结果，不要直接宣布假设错误；检查反例是否真的触达了隐藏依赖，并记录这次实验的限制。

## 练习实验：拆掉一个默认前提

<Tpp20Topic38ProgrammingByCoincidenceLab />

<Stepper>

<Step title="1. 描述一次巧合成功">

写出对象、输入、环境、调用顺序和成功结果。用事实描述，不要写“它应该能工作”；列出至少两个可能的隐含假设。

</Step>

<Step title="2. 选择最小反例">

从反向顺序、空输入、重复键、干净状态或不同区域设置中选一个。先预测哪个观察点会改变，再运行一次，不同时替换工具和架构。

</Step>

<Step title="3. 重建显式契约">

把确认过的依赖写入类型、参数、排序键、初始化检查或权限边界。拒绝条件要能被调用者看到，不能让下游继续猜测。

</Step>

<Step title="4. 保存回归与回退">

把最小反例加入回归集合，以独立提交记录结构变化；若测试失败，回到最近一次绿色提交，用相同输入重放并比较首个差异。

</Step>

</Stepper>

## 正常、边界与单一故障证据

<Tpp20Topic38ProgrammingByCoincidenceEvidenceDiagram />

| 样本     | 唯一变化                 | 预期判定                     | 必存证据             |
| -------- | ------------------------ | ---------------------------- | -------------------- |
| 正常     | 已声明契约与稳定输入     | 结果可由输入和规则重建       | 规则、输出和版本     |
| 边界     | 空、重复、反序或超时输入 | 明确接受或拒绝，不依赖默认值 | 反例、拒绝原因和阈值 |
| 单一故障 | 一个隐藏环境条件被移除   | 在首个差异处停止             | 失败输入、首差和回退 |

独立复核者不应只看到修复后的绿色测试，而应看到修复前如何成功、哪一个条件打破了它、修复把条件放到了哪里。若一个测试依然需要先运行另一条测试才能通过，说明隐含状态还没有被消除。

## 迁移与维护

依赖显式化后仍要考虑时间边界。数据系统会升级排序实现，云服务会改变默认区域，构建工具会改变模块加载，AI 辅助工具也可能生成依赖环境巧合的代码。审查生成代码时，要求它同时给出输入合同、拒绝条件、边界测试和回退路径。

当旧依赖确实是稳定平台保证时，也要把保证写入项目边界：记录版本范围、责任人和升级验证动作。这样“依赖平台保证”就不再是巧合，而是一个能被维护的部署契约。

## 术语表

<Glossary>
  <GlossaryItem term="巧合式编程">
    依赖未声明的顺序、状态、环境或实现细节而成功的代码。
  </GlossaryItem>
  <GlossaryItem term="隐含假设">
    运行所需却没有写入输入、类型、配置或测试合同的前提。
  </GlossaryItem>
  <GlossaryItem term="显式契约">
    规定输入、输出、顺序、错误和副作用的可观察边界。
  </GlossaryItem>
  <GlossaryItem term="可回退证据">
    包含预测、实际、首个差异和恢复路径、可以重放的记录。
  </GlossaryItem>
  <GlossaryItem term="最小反例">
    只改变一项条件、足以挑战某个假设的最小输入。
  </GlossaryItem>
</Glossary>

## 练习

<Exercises>

**问题 1：** 一个导入脚本依赖文件名顺序，开发机上总是先读配置文件。请设计一个反例并写出契约修复。

<Answer>

在临时目录中反转文件名顺序，并从干净进程启动脚本；若结果改变，就确认了默认顺序的巧合。修复应让入口接收明确配置路径或按文件类型显式排序，并对缺失、重复配置返回拒绝；把反序目录加入回归测试。

</Answer>

**问题 2：** 一个函数只有在完整应用启动后才能测试。你如何判断是知识缺口还是隐藏初始化？

<Answer>

先列出函数实际读取的状态，在干净进程中逐项注入最小依赖；如果注入后可独立运行，原问题是测试没有表达依赖；如果仍需某个全局初始化，再把它改成显式构造或启动检查，并用直接调用测试证明边界。

</Answer>

**问题 3：** 修复了默认排序后，另一个页面的结果发生变化。为什么这不是理由去撤销修复？

<Answer>

排序变化暴露了另一个调用者原本依赖未声明顺序。先保存两个页面的输入和首个差异，分别确认它们真正需要的排序规则；若页面需要不同顺序，就把规则写进各自查询或接口，而不是恢复一个所有调用者都猜测的默认顺序。

</Answer>

</Exercises>

<Attribution
  sources={[
    {
      label: "本单元版本与目录依据：W3Cschool《程序员修炼之道》书目页",
      url: "https://www.w3cschool.cn/booklist/booklist-k3pw3fff.html",
    },
    {
      label: "版本交叉核对：The Pragmatic Programmer 20th Anniversary Edition",
      url: "https://pragprog.com/titles/tpp20/the-pragmatic-programmer-20th-anniversary-edition/",
    },
  ]}
  note="本页仅使用公开目录与版本信息作为范围依据；正文、代码、图示、实验和练习均为独立重写。"
  license="公开目录与出版社版本信息；教学内容为本项目独立创作。"
/>

## 本单元回顾

不要依赖巧合编程，意味着要追问一次成功依赖了什么：顺序、状态、环境、权限还是数据分布。用最小反例打破未声明前提，把真实依赖写进显式契约，再以可回退证据和回归测试保护它。最终的目标不是让所有环境都一样，而是让差异出现时能尽早、明确且可恢复地失败。

## 前后导航

- [上一页：37 听从蜥蜴脑](/learn/pragmatic-programmer/47-topic-37-lizard-brain/tpp20-topic-37-lizard-brain)
- [下一页：39 算法速度](/learn/pragmatic-programmer/49-topic-39-algorithm-speed/tpp20-topic-39-algorithm-speed)
