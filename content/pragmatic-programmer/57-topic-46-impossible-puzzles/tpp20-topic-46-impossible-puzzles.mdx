---
title: 46 处理无法解决的难题
type: B
section: 程序员修炼之道（第2版·20周年版）
order: 57
description: 面对看似无解的问题先枚举真正约束，区分事实、假设和自设框框，再改变可变条件。
demo: true
math: true
sourceUrl: "https://www.w3cschool.cn/booklist/booklist-k3pw3fff.html"
draft: false
qualityVersion: 2
practiceMode: diagnosis
sourceMode: independent-rewrite
officialUnitId: tpp20-topic-46-impossible-puzzles
---

import { Objectives } from "@/components/mdx/objectives";
import { Term } from "@/components/mdx/term";
import { Callout } from "@/components/mdx/callout";
import { Step, Stepper } from "@/components/mdx/stepper";
import { Answer, Exercises } from "@/components/mdx/exercises";
import { Glossary, GlossaryItem } from "@/components/mdx/glossary";
import { Attribution } from "@/components/mdx/attribution";
import {
  Tpp20Topic46ImpossiblePuzzlesEvidenceDiagram,
  Tpp20Topic46ImpossiblePuzzlesLoopDiagram,
  Tpp20Topic46ImpossiblePuzzlesLab,
} from "@/components/mdx/pragmatic-programmer/diagrams/tpp20-topic-46-impossible-puzzles-diagram";

<Objectives>

- 能把一个看似无解的问题拆成目标、事实约束、隐含假设和可变条件
- 能用边界样本识别自设框框，区分真正必要条件与团队习惯
- 能用唯一干预、反例和重放证据验证新解没有越过真实边界

</Objectives>

## 先确认问题是不是无解

本页依据 David Thomas、Andrew Hunt《程序员修炼之道：通向务实的最高境界（第2版）》，云风译，电子工业出版社，2020 年 4 月，ISBN 9787121384356 的公开中文目录，独立重构 **46 处理无法解决的难题**。正文、代码、图示、实验和练习都是本课程重新设计的教学材料，不复制原书正文、插图或答案。

当团队说“这件事做不到”时，困难可能来自真实的资源限制，也可能来自没有被说出的旧流程。务实的处理方式不是立刻寻找更聪明的技巧，而是把目标、约束、假设和边界写出来，再一次只改变一个条件。这样才能知道问题真的无解，还是被一个可以移除的框框挡住了。

## 三个会让难题继续保持无解的陷阱

<Callout type="trap" title="把当前流程当成自然法则">

- **现象**：团队说“只能等批处理结束”，却没有检查是否真的需要完整批次。
- **原因**：历史实现被误当成业务约束，没人区分必须保持的结果和可以改变的步骤。
- **修法**：把流程拆成输入、约束、输出和责任人，逐项询问哪个条件来自用户或监管，哪个只是旧实现。

</Callout>

<Callout type="trap" title="把假设写进验收标准后再证明它">

- **现象**：需求要求“不能增加存储、不能改变接口、不能延长时间”，随后宣布没有解法。
- **原因**：三个未经验证的偏好被包装成硬约束，候选方案在开始前就被排除。
- **修法**：为每条限制标注来源、证据和可谈判范围；若是暂时假设，就为它安排反例或边界实验。

</Callout>

<Callout type="trap" title="用一个成功案例掩盖边界失效">

- **现象**：正常输入可以完成，团队便认为已经找到了通用解法。
- **原因**：没有运行容量、期限、权限或依赖缺失的边界样本，结论范围被悄悄扩大。
- **修法**：保存正常、恰好边界和只改变一个条件的故障样本；在第一个状态变化处停止并记录恢复路径。

</Callout>

## 五个词拆开一条难题链

`<Term def="希望在明确对象、时间和结果条件下达到的状态。">目标</Term>`必须描述结果，而不是描述某个既有方案。比如“让用户在当天看到账单”是目标，“必须继续使用夜间批处理”不是目标。

`<Term def="如果不满足就不能接受结果的可观察事实、资源边界或规则。">约束</Term>`应当有来源。容量、法规、数据保留、接口兼容和安全边界可能是真约束；“我们一直这样做”则需要进一步验证。

`<Term def="尚未被证明却会影响候选解空间的判断。">隐含假设</Term>`是最值得优先检查的地方。一个假设如果没有所有者、证据和失效条件，就不应直接进入拒绝理由。

`<Term def="由习惯、工具、组织边界或表达方式制造的可移动限制。">自设框框</Term>`不是虚构困难，而是把可改变的条件误写成不可改变。找到它之后，要记录谁可以改变、代价是什么、改变后哪条真实约束仍然保留。

`<Term def="为了让结果成立而必须保留的条件，且能由输入或规则验证。">必要条件</Term>`用来防止“跳出框框”变成随意放宽要求。新方案必须满足必要条件，否则只是把失败推迟到下游。

这一链条的方向是：先描述目标，再列约束，检查假设，标出可移动框框，最后验证必要条件。反例负责证明链条没有把特殊成功误认为普遍规律。

<Tpp20Topic46ImpossiblePuzzlesLoopDiagram />

## 提示81：不要跳出框框思考——找到框框

“跳出框框”不能只表示提出一个更大胆的答案。它应当先回答：框框是谁设的？它保护什么？改变它会破坏哪条必要条件？如果答案只是“大家都知道”，就还没有形成可复核的约束。

可以用一个账本区分四类陈述：事实写来源和测量；假设写验证方式和过期时间；偏好写协商对象；必要条件写失败时的拒绝动作。这个区分让团队能够对一个条件动手，而不必同时改掉整个系统。

```ts
type Constraint = {
  name: string;
  source: "fact" | "assumption" | "preference" | "required";
  evidence: string;
  movable: boolean;
};

const movable = (constraint: Constraint) =>
  constraint.movable && constraint.source !== "required";
```

这个小函数不负责解决难题，它只让“可以改变”成为显式判断。实际决策还要保存证据和责任人，并在改变后重放相同输入。

## 最小运行合同

先冻结对象、输入和目标，再指定唯一干预。一次实验只能改变一个条件，例如把“必须整批处理”改成“允许分批提交”；不能同时换存储、重写接口并扩大时间窗口。运行记录至少包含预测节点、实际首差、拒绝原因和恢复动作。

```yaml
unit: tpp20-topic-46-impossible-puzzles
baseline:
  object: 当日账单
  input: 订单事件与结算窗口
intervention:
  only_change: 是否必须等待完整批次
boundary:
  must_preserve: 金额准确、重复事件幂等、审计可追溯
evidence:
  record: 约束来源、假设验证、首差、反例、恢复
```

基线样本要能被别人重建。若新解满足速度却破坏幂等或审计，它不是“部分成功”，而是没有满足必要条件；拒绝必须发生在结果被下游接收之前。

## 选择一个难题样本

<Tpp20Topic46ImpossiblePuzzlesLab />

<Stepper>

<Step title="1. 写目标和真实约束">

选择一个团队正在争论的问题，写出对象、期望结果、约束来源、时间窗口、资源边界和真正的拒绝条件。把“我们一直这样做”单独标为待验证陈述。

</Step>

<Step title="2. 给假设安排证据">

为每个隐含假设指定验证动作、责任人和失效时间。优先测试会显著缩小解空间的假设，记录如果它为假，哪些候选方案会重新进入讨论。

</Step>

<Step title="3. 只移动一个框框">

选择一个可移动条件，先写预测的首个变化，再运行正常和边界样本。保留其它输入、版本和必要条件；如果同时改动多个因素，退回基线重新开始。

</Step>

<Step title="4. 用反例和回退收口">

构造一个满足相同前提却产生相反结果的反例，确认新解没有把风险转移给下游。保存实际首差、拒绝原因、恢复动作和仍未覆盖的输入，再让独立复核者重放。

</Step>

</Stepper>

## 正常、边界与单一故障证据

<Tpp20Topic46ImpossiblePuzzlesEvidenceDiagram />

| 样本     | 只改变的变量                 | 预期判定                     | 必存证据                     |
| -------- | ---------------------------- | ---------------------------- | ---------------------------- |
| 正常     | 已知输入、完整依赖、明确约束 | 目标达成且必要条件全部满足   | 基线、逐节点输出、结果摘要   |
| 边界     | 恰好容量、期限或资源阈值     | 接受或拒绝明确，不产生假完成 | 阈值来源、比较结果、原状态   |
| 单一故障 | 一个假设、依赖或角色失效     | 在首个异常点暴露并可恢复     | 首差、失败上下文、恢复与回归 |

证据不是为了证明每次都成功，而是为了限定结论适用范围。若边界样本显示“分批”破坏了金额一致性，就应撤回方案或增加必要条件；不能只展示速度提升的正常样本。

## 术语表

<Glossary>
  <GlossaryItem term="目标">
    在明确对象、时间和结果条件下希望达到的状态。
  </GlossaryItem>
  <GlossaryItem term="约束">
    不满足就不能接受结果的事实、资源边界或规则。
  </GlossaryItem>
  <GlossaryItem term="隐含假设">
    尚未被证明却会影响候选解空间的判断。
  </GlossaryItem>
  <GlossaryItem term="自设框框">
    由习惯、工具、组织边界或表达方式制造的可移动限制。
  </GlossaryItem>
  <GlossaryItem term="必要条件">
    为让结果成立而必须保留且能由输入或规则验证的条件。
  </GlossaryItem>
</Glossary>

## 练习

<Exercises>

**问题 1：** 团队说“本周无法交付，因为只能等完整数据集”。请列出你会追问的约束和假设。

<Answer>

先确认交付目标是完整报告、可查询的增量结果，还是某个用户可见的状态；再询问完整数据集是否来自法规、金额一致性、接口契约或旧批处理实现。对每条限制写来源、证据、责任人和失效条件，并用一组增量输入验证是否真的必须等待全部数据。

</Answer>

**问题 2：** 你把“必须整批处理”改成了“允许分批提交”，速度提高但重复事件导致金额翻倍。如何判定这次尝试？

<Answer>

它没有满足必要条件，因此不能称为可用解法。保存唯一干预、金额翻倍的首差、重复事件输入和下游影响；回退到可重放的基线，为分批方案增加幂等键或拒绝重复事件的边界，再用相同样本验证金额、速度和审计都成立。

</Answer>

**问题 3：** 一个架构限制被证明只是团队习惯，但移除它会增加运维成本。你会怎样记录决策？

<Answer>

把它从硬约束改标为可谈判偏好，记录改变者、成本估算、运维能力、保留的必要条件和回退路径。用正常、边界和单一故障样本比较两种方案，若收益不足以承担成本就保留习惯，并把决定写成有期限的选择，而不是永久真理。

</Answer>

</Exercises>

<Attribution
  sources={[
    {
      label: "本单元版本与目录依据：W3Cschool《程序员修炼之道》书目页",
      url: "https://www.w3cschool.cn/booklist/booklist-k3pw3fff.html",
    },
    {
      label: "版本交叉核对：The Pragmatic Programmer 20th Anniversary Edition",
      url: "https://pragprog.com/titles/tpp20/the-pragmatic-programmer-20th-anniversary-edition/",
    },
  ]}
  note="本页仅使用公开目录与版本信息作为范围依据；正文、代码、图示、实验和练习均为独立重写。"
  license="公开目录与出版社版本信息；教学内容为本项目独立创作。"
/>

## 本单元回顾

处理无法解决的难题，不是强行乐观，也不是把每条限制都放宽。先区分目标、真实约束、隐含假设和自设框框，再只改变一个可变条件，用边界和反例确认必要条件仍在。能说明新解何时成立、何时拒绝、如何回退，才把提示变成可迁移的工程判断。

## 前后导航

- [上一页：45 需求之坑](/learn/pragmatic-programmer/56-topic-45-requirements-pit/tpp20-topic-45-requirements-pit)
- [下一页：47 携手共建](/learn/pragmatic-programmer/58-topic-47-working-together/tpp20-topic-47-working-together)
