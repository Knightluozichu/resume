---
title: "第10章 编写项目文档"
type: C
section: "Python高级编程 · 项目生命周期"
order: 10
description: "按原书第10章 Documenting Your Project重建：技术写作七原则、reStructuredText、Sphinx构建、文档组合与受众、文档验收。"
demo: true
math: false
sourceUrl: "https://tarekziade.wordpress.com/2008/09/24/expert-python-programming-book-more-details-sample-chapter/"
draft: true
---

import { Objectives, Callout, Attribution, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step } from "@/components/mdx/mdx-components";

<Objectives>
- 能解释技术写作七原则与reStructuredText的责任和依赖
- 能区分Sphinx构建和文档组合与受众的适用边界
- 能实现并复现围绕文档验收的最小工程实验
</Objectives>

## 先做预测

先预测：一个示例在作者机器上运行成功，是否足以证明它可维护？不能。还要明确解释器与依赖、输入边界、失败路径、可观察结果和重建步骤。<Term def="先写结构再润色，面向明确读者，使用简单风格，限制主题范围，示例真实且最小，信息轻量但充分，并用模板保持一致。">技术写作七原则</Term>给出本章的第一个契约，<Term def="原书用reStructuredText表达标题、列表、内联标记、代码块和链接；标记语法服务于语义结构，不能用视觉缩进替代可解析层级。">reStructuredText</Term>把它放进可重复流程。

<PyaDocumentingProjectModelLab />

## 原书骨架与现代迁移

官方章节围绕技术写作七原则、reStructuredText、Sphinx构建、文档组合与受众、文档验收展开。原书出版于Python 2.5与早期敏捷工具生态，本章保留它解释“为什么”的结构；命令和安全默认按当前Python、标准库与PyPA维护文档迁移，不把EasyInstall、distutils安装命令、旧CI或旧项目平台直接当成新项目默认。

<Term def="Sphinx把源文档、自动API和交叉引用构建成可发布站点；构建必须在CI中把断链和严重警告作为失败。">Sphinx构建</Term>承接前两项，把局部语法或工具放回应用数据流。阅读时沿输入、协议、状态、输出和证据追踪，避免只背API名称。

```rst
Atomisator Parser
==================

Parse a feed::

   entries = parser.parse(payload)

See :class:\`atomisator.Parser\`.
```

## 机制一：责任与数据流

先写结构再润色，面向明确读者，使用简单风格，限制主题范围，示例真实且最小，信息轻量但充分，并用模板保持一致。 原书用reStructuredText表达标题、列表、内联标记、代码块和链接；标记语法服务于语义结构，不能用视觉缩进替代可解析层级。 先写出公共行为和失败类型，再选择语法或工具。这样即使实现从原书工具迁到当前生态，调用者仍能依据相同契约判断结果。

<Term def="设计、使用和运维文档回答不同问题，生产者与消费者需要不同入口；一个超长README不能替代可导航的信息架构。">文档组合与受众</Term>提醒我们：抽象不能消除成本，只会改变成本出现的位置。包装、生成器、构建系统、CI或缓存都必须说明资源、顺序和异常传播。

<PyaDocumentingProjectBoundaryLab />

```python
def parse(payload: bytes) -> list[Entry]:
    """Parse one feed payload.

    Raises:
        ParseError: If the document is malformed.
    """
    ...
```

## 机制二：失败与边界

Sphinx把源文档、自动API和交叉引用构建成可发布站点；构建必须在CI中把断链和严重警告作为失败。 设计、使用和运维文档回答不同问题，生产者与消费者需要不同入口；一个超长README不能替代可导航的信息架构。 边界实验至少包含正常、空输入、上限附近、依赖失败和重复执行。涉及网络、并发或外部制品时，再加入超时、取消、部分完成与摘要校验。

历史工具不等于无价值。正确迁移是先提取声明式配置、隔离、持续反馈和可回滚发布等不变量，再用维护中的接口重写；错误做法是机械替换命令却保留隐式环境和不可追踪副作用。

```bash
sphinx-build -W --keep-going docs docs/_build/html
python -m doctest docs/examples.txt
python -m unittest discover -s tests
```

## 机制三：证据闭环

<Term def="代码示例应可执行，版本与配置要明确，发布时文档和制品同版本；过期页面应删除或标明适用范围，而不是长期保留冲突答案。">文档验收</Term>负责收束验收。代码示例应可执行，版本与配置要明确，发布时文档和制品同版本；过期页面应删除或标明适用范围，而不是长期保留冲突答案。 保存解释器、依赖锁定、输入、命令、退出状态、关键输出和制品摘要，才能让另一台干净机器重放同一结论。

## 实战验收清单

1. 在隔离环境运行三段示例，记录解释器实现、版本和依赖来源。
2. 为核心行为增加正常、空输入、失败和重复执行测试，先看到失败再修改实现。
3. 清理缓存和临时文件后重跑，证明结果不依赖工作区残留。
4. 对历史命令写出当前替代路径，并说明保留的架构不变量与不再采用的安全默认。

## 迁移决策题

设想团队正在维护一个已经运行多年的Python服务：它仍依赖本章对应的历史工具，但业务不能停机。先不要直接重写。第一步列出技术写作七原则承担的真实输入和输出，再用reStructuredText识别构建或运行时依赖；第二步把Sphinx构建放进隔离实验，证明当前行为与失败类型；第三步用文档组合与受众设计兼容层，让旧入口和新入口在同一组契约测试下运行；最后以文档验收保存制品摘要、性能或行为差异与回滚条件。只有新路径在正常、边界和故障输入上都达到既定条件，才逐步切换流量或调用者。这样迁移的是可验证契约，而不是把一个旧命令盲目替换为一个新命令。

## 常见误区

<Callout type="trap" title="把高级Python等同于语法技巧">
原书的核心是专业软件生命周期。技巧若没有包边界、测试、文档、CI和性能证据，只会增加维护成本。
</Callout>

<Callout type="trap" title="直接复制2008年的工具命令">
旧工具用于理解问题背景。新项目应使用当前维护接口、隔离环境和构建标准，并把迁移结果放进自动验收。
</Callout>

## 本章回顾

本章逐项覆盖技术写作七原则、reStructuredText、Sphinx构建、文档组合与受众、文档验收。方法是用可读接口表达责任，用边界测试证明失败语义，再以可重建环境和制品证据完成闭环。

<Exercises>
**问题 1：技术写作七原则与reStructuredText怎样协作？**
<Answer>先写结构再润色，面向明确读者，使用简单风格，限制主题范围，示例真实且最小，信息轻量但充分，并用模板保持一致。 原书用reStructuredText表达标题、列表、内联标记、代码块和链接；标记语法服务于语义结构，不能用视觉缩进替代可解析层级。</Answer>

**问题 2：Sphinx构建与文档组合与受众的边界是什么？**
<Answer>Sphinx把源文档、自动API和交叉引用构建成可发布站点；构建必须在CI中把断链和严重警告作为失败。 设计、使用和运维文档回答不同问题，生产者与消费者需要不同入口；一个超长README不能替代可导航的信息架构。</Answer>

**问题 3：如何验收文档验收？**
<Answer>代码示例应可执行，版本与配置要明确，发布时文档和制品同版本；过期页面应删除或标明适用范围，而不是长期保留冲突答案。 使用正常、空输入、失败、重复执行和干净环境重建，保存命令、状态与制品摘要。</Answer>
</Exercises>

## 术语表

<Glossary>
  <GlossaryItem term="技术写作七原则" definition="先写结构再润色，面向明确读者，使用简单风格，限制主题范围，示例真实且最小，信息轻量但充分，并用模板保持一致。" />
  <GlossaryItem term="reStructuredText" definition="原书用reStructuredText表达标题、列表、内联标记、代码块和链接" />
  <GlossaryItem term="Sphinx构建" definition="Sphinx把源文档、自动API和交叉引用构建成可发布站点" />
  <GlossaryItem term="文档组合与受众" definition="设计、使用和运维文档回答不同问题，生产者与消费者需要不同入口" />
  <GlossaryItem term="文档验收" definition="代码示例应可执行，版本与配置要明确，发布时文档和制品同版本" />
  <GlossaryItem term="现代迁移边界" definition="保留原书问题与架构不变量，按当前维护接口更新实现。" />
</Glossary>

<Attribution adaptedFrom="Tarek Ziadé · Expert Python Programming · 原书第10章 Documenting Your Project" adaptedUrl="https://tarekziade.wordpress.com/2008/09/24/expert-python-programming-book-more-details-sample-chapter/" />
