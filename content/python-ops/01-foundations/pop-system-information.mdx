---
title: "第1章 系统基础信息模块详解"
type: C
section: "Python自动化运维 · 基础篇"
order: 1
description: "按刘天斯原书第1章重建：系统基础信息模块、psutil系统性能与进程管理、IPy地址与网段处理、dnspython域名解析、DNS域名轮循业务监控。"
demo: true
math: false
sourceUrl: "https://github.com/yorkoliu/pyauto"
draft: true
---

import { Objectives, Callout, Attribution, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step } from "@/components/mdx/mdx-components";

<Objectives>
- 能解释系统基础信息模块的执行链与运维价值
- 能区分psutil系统性能与进程管理和IPy地址与网段处理的责任与失败语义
- 能实现并复现dnspython域名解析到DNS域名轮循业务监控的安全实验
</Objectives>

## 从一次运维变更开始

先预测：脚本在一台测试机成功，是否就能安全用于批量生产？不能。还要说明目标、权限、超时、部分失败、证据和回滚。<Term def="系统基础信息模块把主机状态转换为结构化数据。采集器要明确采样时刻、单位和权限，返回原始指标而不是直接决定告警；策略层再根据基线和服务目标解释数据。">系统基础信息模块</Term>必须落进可观察的执行链。

<PopSystemInformationModelLab />

## 核心模型与责任

系统基础信息模块把主机状态转换为结构化数据。采集器要明确采样时刻、单位和权限，返回原始指标而不是直接决定告警；策略层再根据基线和服务目标解释数据。 <Term def="psutil系统性能与进程管理覆盖CPU、内存、磁盘、网络和进程。cpu_percent第一次调用只是建立基线，进程也会在枚举期间退出，因此代码必须处理NoSuchProcess和AccessDenied。">psutil系统性能与进程管理</Term>

本章按照原书核心模块建立最小实验，再用现代Python和安全默认重写边界。工具版本可以变化，但目标选择、输入验证、结果聚合和审计证据不能消失。

## 机制一：采集或控制入口

psutil系统性能与进程管理覆盖CPU、内存、磁盘、网络和进程。cpu_percent第一次调用只是建立基线，进程也会在枚举期间退出，因此代码必须处理NoSuchProcess和AccessDenied。 <Term def="IPy地址与网段处理用于计算网络、广播、前缀和包含关系。原书使用IPy，现代Python也可用标准库ipaddress；不要用字符串前缀判断地址归属，IPv4与IPv6必须由地址对象验证。">IPy地址与网段处理</Term>

```python
import psutil
snapshot = {
    'cpu': psutil.cpu_percent(interval=0.2),
    'memory': psutil.virtual_memory().percent,
    'disk': psutil.disk_usage('/').percent,
}
print(snapshot)
```

运行前固定测试目标与时间窗，运行后保存结构化结果、耗时和异常类型。不要把打印了几行输出当成成功，也不要让示例凭据进入源码。

## 机制二：状态、网络与批量

IPy地址与网段处理用于计算网络、广播、前缀和包含关系。原书使用IPy，现代Python也可用标准库ipaddress；不要用字符串前缀判断地址归属，IPv4与IPv6必须由地址对象验证。 <Term def="dnspython域名解析返回A、AAAA、MX、CNAME等记录及TTL。解析失败要区分NXDOMAIN、无记录、超时和SERVFAIL，缓存时间受TTL约束，不能把一次答案当成永久配置。">dnspython域名解析</Term>

<PopSystemInformationBoundaryLab />

```python
from ipaddress import ip_network, ip_address
network = ip_network('10.20.0.0/20')
assert ip_address('10.20.3.8') in network
print(network.network_address, network.broadcast_address)
```

批量任务要先预览匹配目标，再限制并发并逐目标汇总。成功、失败、超时和未返回是不同状态；只展示总成功率会掩盖关键主机失败。

## 机制三：证据与恢复

dnspython域名解析返回A、AAAA、MX、CNAME等记录及TTL。解析失败要区分NXDOMAIN、无记录、超时和SERVFAIL，缓存时间受TTL约束，不能把一次答案当成永久配置。 <Term def="DNS域名轮循业务监控要同时验证每个答案是否出现、是否可连接以及服务内容是否正确。只看域名能解析会漏掉后端故障，探针还要记录解析器、时延和本次答案集合。">DNS域名轮循业务监控</Term>

DNS域名轮循业务监控要同时验证每个答案是否出现、是否可连接以及服务内容是否正确。只看域名能解析会漏掉后端故障，探针还要记录解析器、时延和本次答案集合。 <Term def="验证系统基础信息模块详解的授权、结果和失败路径。">系统基础信息模块详解验收边界</Term>

```python
import dns.resolver
answers = dns.resolver.resolve('example.com', 'A', lifetime=3)
records = [(answer.address, answers.rrset.ttl) for answer in answers]
print(records)
```

## 分步验收

<Stepper>
  <Step title="建立执行链"><PopSystemInformationModelLab /></Step>
  <Step title="注入边界"><PopSystemInformationBoundaryLab /></Step>
  <Step title="证明可恢复">用正常、超时、权限拒绝和部分失败四组输入运行；核对状态、日志、临时资源与回滚点，并保留可复查证据。</Step>
</Stepper>

## 实战验证清单

在隔离环境执行三段代码：先只读采集，再模拟一台目标超时，最后验证失败不会改变未授权对象。记录命令或API参数、开始结束时间、目标清单、返回码、标准错误、制品摘要和回滚结果。涉及网络扫描、远程命令或配置发布时，必须使用经过批准的测试资产。

## 常见误区

<Callout type="trap" title="只要系统基础信息模块能运行就可以批量">
单机成功没有证明目标选择、并发预算、部分失败和回滚。扩大范围前必须完成预览、限流、逐主机结果与停止条件。
</Callout>

<Callout type="trap" title="原书工具API可以原样用于今天的生产">
原书出版于2014年，部分工具和安全默认已经变化。应保留架构思想，依据当前官方文档迁移API、主机密钥、TLS、权限和依赖版本。
</Callout>

## 本章回顾

官方核心单元核对：系统基础信息模块；psutil系统性能与进程管理；IPy地址与网段处理；dnspython域名解析；DNS域名轮循业务监控。本章把原书模块放进现代运维闭环：先授权与预览，再执行与收集，最后判断、审计和恢复。

<Exercises>
**问题 1：如何解释“系统基础信息模块”的执行链？**<Answer>系统基础信息模块把主机状态转换为结构化数据。采集器要明确采样时刻、单位和权限，返回原始指标而不是直接决定告警；策略层再根据基线和服务目标解释数据。 psutil系统性能与进程管理覆盖CPU、内存、磁盘、网络和进程。cpu_percent第一次调用只是建立基线，进程也会在枚举期间退出，因此代码必须处理NoSuchProcess和AccessDenied。</Answer>
**问题 2：IPy地址与网段处理与dnspython域名解析如何协作？**<Answer>IPy地址与网段处理用于计算网络、广播、前缀和包含关系。原书使用IPy，现代Python也可用标准库ipaddress；不要用字符串前缀判断地址归属，IPv4与IPv6必须由地址对象验证。 dnspython域名解析返回A、AAAA、MX、CNAME等记录及TTL。解析失败要区分NXDOMAIN、无记录、超时和SERVFAIL，缓存时间受TTL约束，不能把一次答案当成永久配置。</Answer>
**问题 3：如何验收DNS域名轮循业务监控？**<Answer>DNS域名轮循业务监控要同时验证每个答案是否出现、是否可连接以及服务内容是否正确。只看域名能解析会漏掉后端故障，探针还要记录解析器、时延和本次答案集合。 增加正常、超时、权限拒绝和部分失败输入，核对目标、返回码、证据和回滚。</Answer>
</Exercises>

## 术语表
<Glossary>
  <GlossaryItem term="系统基础信息模块" definition="系统基础信息模块把主机状态转换为结构化数据。" />
  <GlossaryItem term="psutil系统性能与进程管理" definition="psutil系统性能与进程管理覆盖CPU、内存、磁盘、网络和进程。" />
  <GlossaryItem term="IPy地址与网段处理" definition="IPy地址与网段处理用于计算网络、广播、前缀和包含关系。" />
  <GlossaryItem term="dnspython域名解析" definition="dnspython域名解析返回A、AAAA、MX、CNAME等记录及TTL。" />
  <GlossaryItem term="DNS域名轮循业务监控" definition="DNS域名轮循业务监控要同时验证每个答案是否出现、是否可连接以及服务内容是否正确。" />
  <GlossaryItem term="系统基础信息模块详解验收边界" definition="验证系统基础信息模块详解的授权、结果和失败路径。" />
</Glossary>

<Attribution adaptedFrom="刘天斯《Python自动化运维：技术与最佳实践》第1章 系统基础信息模块详解" adaptedUrl="https://github.com/yorkoliu/pyauto" />
