---
title: "《Python自动化运维：技术与最佳实践》总复习"
type: C
section: "Python自动化运维 · 总复习"
order: 99
description: "按刘天斯原书三篇16章建立Python自动化运维学习与验收路线。"
demo: true
math: false
sourceUrl: "https://github.com/yorkoliu/pyauto"
draft: false
---

import { Objectives, Callout, Attribution, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step } from "@/components/mdx/mdx-components";
import { PopFinalReviewModelLab, PopFinalReviewBoundaryLab, PopFinalReviewEvidenceLab } from "@/components/mdx/python-ops/diagrams/pop-final-review";

<Objectives>
- 能解释采集到行动闭环如何组织原书16章
- 能区分批量执行安全与声明式配置收敛的依赖关系
- 能设计并复现分布式监控证据到平台权限与审计的综合验收
</Objectives>

## 从运维闭环开始

先预测：自动化是否只是把手工命令写成Python？不是。成熟自动化还要目标清晰、执行有界、证据完整并能恢复。<Term def="采集到行动闭环要求指标、差异、报表、告警和执行共享目标与事件ID，任何结论都能下钻到原始证据。">采集到行动闭环</Term>把原书三篇连成系统。

<PopFinalReviewModelLab />

## 基础篇：采集与监控

采集到行动闭环要求指标、差异、报表、告警和执行共享目标与事件ID，任何结论都能下钻到原始证据。 <Term def="批量执行安全由授权目标、主机指纹、有界并发、逐主机结果和可回滚发布共同保证，批量不等于同时无条件执行。">批量执行安全</Term>

```python
def observe(target, collector):
    return {'target': target, 'sample': collector(target)}
```

## 高级篇：批量与集中管理

批量执行安全由授权目标、主机指纹、有界并发、逐主机结果和可回滚发布共同保证，批量不等于同时无条件执行。 <Term def="声明式配置收敛用Ansible或Salt把期望状态、差异和handler写成可重复计划，临时命令则保留审批和审计。">声明式配置收敛</Term>

<PopFinalReviewBoundaryLab />

```python
def bounded_run(targets, action, limit=10):
    approved = authorize_and_preview(targets, action)
    return dispatch_with_limit(approved, action, limit)
```

## 大数据与报表

声明式配置收敛用Ansible或Salt把期望状态、差异和handler写成可重复计划，临时命令则保留审批和审计。 <Term def="分布式监控证据要区分采集位置、发生时间、接收时间、未知值和部分失败，再按一致算法计算可用率与分位数。">分布式监控证据</Term>

## 案例篇：平台化

分布式监控证据要区分采集位置、发生时间、接收时间、未知值和部分失败，再按一致算法计算可用率与分位数。 <Term def="平台权限与审计贯穿B/S、Linux审计、质量监控和C/S案例，Web或桌面客户端都不能绕过服务端身份与任务模型。">平台权限与审计</Term>

## 现代化迁移

平台权限与审计贯穿B/S、Linux审计、质量监控和C/S案例，Web或桌面客户端都不能绕过服务端身份与任务模型。 <Term def="历史技术迁移先提取原书的不变量，再用现代库和安全默认实现；Func、旧Fabric、CGI等不作为新项目默认选型。">历史技术迁移</Term>

```python
def accept(results, rollback):
    if any(item['status'] != 'ok' for item in results):
        rollback()
        raise RuntimeError('automation did not converge')
```

<PopFinalReviewEvidenceLab />

## 分步验收

<Stepper>
  <Step title="画出闭环"><PopFinalReviewModelLab /></Step>
  <Step title="注入部分失败"><PopFinalReviewBoundaryLab /></Step>
  <Step title="完成恢复">以测试资产运行采集、批量执行和平台任务，确认所有结果可审计且失败能回滚。</Step>
</Stepper>

## 综合实验

选择一个测试服务：采集CPU与HTTP质量，生成差异和报表，通过受信SSH发布配置，再从两个探针验证结果。每一步保存事件ID、目标、时间、返回码和摘要；任一步失败都停止后续动作并运行恢复。这个实验把原书模块从脚本清单变成工程闭环。

## 常见误区

<Callout type="trap" title="把自动化等同于无确认执行">高风险变更仍需授权、预览、批次、停止条件和回滚；自动化提高一致性，不取消控制。</Callout>
<Callout type="trap" title="把2014年的工具版本当成当前默认">保留原书系统思想，API与安全配置必须按当前官方文档迁移，历史项目只用于理解演进。</Callout>

## 本章回顾

采集到行动闭环要求指标、差异、报表、告警和执行共享目标与事件ID，任何结论都能下钻到原始证据。 批量执行安全由授权目标、主机指纹、有界并发、逐主机结果和可回滚发布共同保证，批量不等于同时无条件执行。 声明式配置收敛用Ansible或Salt把期望状态、差异和handler写成可重复计划，临时命令则保留审批和审计。 分布式监控证据要区分采集位置、发生时间、接收时间、未知值和部分失败，再按一致算法计算可用率与分位数。 平台权限与审计贯穿B/S、Linux审计、质量监控和C/S案例，Web或桌面客户端都不能绕过服务端身份与任务模型。 历史技术迁移先提取原书的不变量，再用现代库和安全默认实现；Func、旧Fabric、CGI等不作为新项目默认选型。 本页把采集到行动闭环、批量执行安全、声明式配置收敛、分布式监控证据、平台权限与审计、历史技术迁移连接成可执行路线。

<Exercises>
**问题 1：三篇16章如何递进？**<Answer>基础篇建立采集与监控，高级篇扩大到批量和集中控制，案例篇把执行、数据、权限与界面组织成平台。</Answer>
**问题 2：自动化的最低安全闭环是什么？**<Answer>授权目标、预览、受限执行、逐项结果、审计证据、停止条件和可验证回滚。</Answer>
**问题 3：怎样处理历史工具？**<Answer>提取架构不变量，用现代支持版本重写API和安全默认，并通过相同失败实验验证迁移。</Answer>
</Exercises>

## 术语表
<Glossary>
  <GlossaryItem term="采集到行动闭环" definition="采集到行动闭环要求指标、差异、报表、告警和执行共享目标与事件ID，任何结论都能下钻到原始证据。"/>
  <GlossaryItem term="批量执行安全" definition="批量执行安全由授权目标、主机指纹、有界并发、逐主机结果和可回滚发布共同保证，批量不等于同时无条件执行。"/>
  <GlossaryItem term="声明式配置收敛" definition="声明式配置收敛用Ansible或Salt把期望状态、差异和handler写成可重复计划，临时命令则保留审批和审计。"/>
  <GlossaryItem term="分布式监控证据" definition="分布式监控证据要区分采集位置、发生时间、接收时间、未知值和部分失败，再按一致算法计算可用率与分位数。"/>
  <GlossaryItem term="平台权限与审计" definition="平台权限与审计贯穿B/S、Linux审计、质量监控和C/S案例，Web或桌面客户端都不能绕过服务端身份与任务模型。"/>
  <GlossaryItem term="历史技术迁移" definition="历史技术迁移先提取原书的不变量，再用现代库和安全默认实现；Func、旧Fabric、CGI等不作为新项目默认选型。"/>
</Glossary>
<Attribution adaptedFrom="刘天斯《Python自动化运维：技术与最佳实践》总复习" adaptedUrl="https://github.com/yorkoliu/pyauto" />
