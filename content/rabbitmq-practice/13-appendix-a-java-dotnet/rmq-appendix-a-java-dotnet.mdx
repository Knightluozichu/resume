---
title: "附录A 在Java和.NET上使用Rabbit"
description: "依据Manning原版与电子工业出版社中文版完整目录：比较Java与.NET客户端库，以Hello World、事件驱动告警和Java AMQP RPC复现跨语言消息模式"
type: C
section: "RabbitMQ实战：高效部署分布式消息队列（RabbitMQ 2.7 / 2015）"
draft: false
math: false
sourceUrl: "https://livebook.manning.com/book/rabbitmq-in-action/table-of-contents"
demo: true
order: 0
---

import { Objectives } from "@/components/mdx/objectives";
import { Term } from "@/components/mdx/term";
import { Callout } from "@/components/mdx/callout";
import { Answer, Exercises } from "@/components/mdx/exercises";
import { Glossary, GlossaryItem } from "@/components/mdx/glossary";
import { Attribution } from "@/components/mdx/attribution";

# 附录A 在Java和.NET上使用Rabbit

本课程对应Alvaro Videla、Jason J. W. Williams著《RabbitMQ实战：高效部署分布式消息队列》，汪佳南译，电子工业出版社2015年10月出版，332页，ISBN 9787121271649；原版RabbitMQ in Action由Manning于2012年4月出版。官方第8章样章明确以RabbitMQ 2.7.0为当时最新版本，本课程据此固定版本边界。

全书正式分母为12章和附录A、B、C，不把后来出现的quorum queue、stream queue、现代federation、Kubernetes operator或新CLI倒灌进原书。本页逐项覆盖4个目录节点，最终交付Java/.NET API对照、Hello World、事件告警、Java RPC和跨语言消息互操作测试。

<Objectives>

- 能解释“附录A 在Java和.NET上使用Rabbit”的4个目录节点，并绘制connection、channel、exchange、binding、queue与consumer边界。
- 能比较正常、压力、断线、节点故障和客户端重启轨迹，判断confirm、ack、持久化与业务完成点。
- 能设计并复现Java/.NET API对照、Hello World、事件告警、Java RPC和跨语言消息互操作测试，用吞吐、分位延迟、ready/unacked、日志和消息ID对账支持结论。
- 能分析“只比较API名称，不核对声明参数、属性编码、确认模式、异常和连接channel释放差异”为何失败，并写出停止、恢复、回退与责任交接条件。

</Objectives>

## 从一条AMQP消息的旅程开始

先预测：生产者打开connection和channel后声明哪些交换器、队列与绑定，basic.publish如何按exchange type和routing key选择队列，broker何时发送publisher confirm，消费者何时收到delivery并basic.ack。随后预测连接中断、队列主节点故障或消费者处理一半崩溃时，消息处于ready、unacked、已重投还是未知状态。观察前必须留下预测。

RabbitMQ 2.7的可靠性不是单开关。durable控制交换器与队列定义是否跨broker重启，delivery_mode控制消息是否请求持久化，publisher confirm描述发布到broker的确认，consumer ack描述处理后的确认，经典镜像队列描述队列副本。五者边界必须分别验证。

## 核心词汇与版本边界

<Term def="Java客户端是“比较Java与.NET客户端库，以Hello World、事件驱动告警和Java AMQP RPC复现跨语言消息模式”中的第1个RabbitMQ 2.7概念，定义必须同时给出AMQP状态、故障边界和可观测证据">Java客户端</Term>、<Term def=".NET客户端是“比较Java与.NET客户端库，以Hello World、事件驱动告警和Java AMQP RPC复现跨语言消息模式”中的第2个RabbitMQ 2.7概念，定义必须同时给出AMQP状态、故障边界和可观测证据">.NET客户端</Term>、<Term def="事件驱动是“比较Java与.NET客户端库，以Hello World、事件驱动告警和Java AMQP RPC复现跨语言消息模式”中的第3个RabbitMQ 2.7概念，定义必须同时给出AMQP状态、故障边界和可观测证据">事件驱动</Term>、<Term def="AMQP RPC是“比较Java与.NET客户端库，以Hello World、事件驱动告警和Java AMQP RPC复现跨语言消息模式”中的第4个RabbitMQ 2.7概念，定义必须同时给出AMQP状态、故障边界和可观测证据">AMQP RPC</Term>、<Term def="互操作是“比较Java与.NET客户端库，以Hello World、事件驱动告警和Java AMQP RPC复现跨语言消息模式”中的第5个RabbitMQ 2.7概念，定义必须同时给出AMQP状态、故障边界和可观测证据">互操作</Term>
构成本页词汇。每个词都要回答：由谁声明，属于哪个vhost，在哪个channel发生，改变什么broker或客户端状态，断线与重试后会留下什么，以及RabbitMQ
2.7之后哪些变化不属于本书。


## 原书目录逐节点重构

### 再说Hello：库选项与Hello World

**目录节点 1/4。** “再说Hello：库选项与Hello World”服务于“比较Java与.NET客户端库，以Hello World、事件驱动告警和Java AMQP RPC复现跨语言消息模式”。先确定它作用于connection、channel、exchange、binding、queue、consumer、cluster node还是插件，再标出声明、路由、确认、持久化与释放边界。结论必须能从配置、rabbitmqctl或Management状态、日志、消息ID和客户端事件中复核。

机制轨迹从“本章输入与版本契约”进入“再说Hello：库选项与Hello World”，再到“重温告警：把告警应用移植到事件驱动.NET”。逐步记录AMQP方法、broker状态和客户端回调，区分“方法已发送”“broker已接收或确认”“消费者已处理”“业务结果已对账”。连接关闭、节点故障、重试和应用重启必须有独立分支，不能只画正常箭头。

实验固定RabbitMQ 2.7环境、vhost、交换器、队列、路由键和消息ID，只改变持久化、confirm、ack、消费者速度、节点或网络故障之一。比较吞吐、P95/P99、ready/unacked、内存、Erlang进程与最终消息集合，用“两种客户端对交换器队列绑定、content properties、ack、reply_to和correlation id的映射一致，资源关闭与失败语义明确”验收。

### 重温告警：把告警应用移植到事件驱动.NET

**目录节点 2/4。** “重温告警：把告警应用移植到事件驱动.NET”服务于“比较Java与.NET客户端库，以Hello World、事件驱动告警和Java AMQP RPC复现跨语言消息模式”。先确定它作用于connection、channel、exchange、binding、queue、consumer、cluster node还是插件，再标出声明、路由、确认、持久化与释放边界。结论必须能从配置、rabbitmqctl或Management状态、日志、消息ID和客户端事件中复核。

机制轨迹从“再说Hello：库选项与Hello World”进入“重温告警：把告警应用移植到事件驱动.NET”，再到“咖啡时间的RPC：用Java实现AMQP RPC”。逐步记录AMQP方法、broker状态和客户端回调，区分“方法已发送”“broker已接收或确认”“消费者已处理”“业务结果已对账”。连接关闭、节点故障、重试和应用重启必须有独立分支，不能只画正常箭头。

实验固定RabbitMQ 2.7环境、vhost、交换器、队列、路由键和消息ID，只改变持久化、confirm、ack、消费者速度、节点或网络故障之一。比较吞吐、P95/P99、ready/unacked、内存、Erlang进程与最终消息集合，用“两种客户端对交换器队列绑定、content properties、ack、reply_to和correlation id的映射一致，资源关闭与失败语义明确”验收。

### 咖啡时间的RPC：用Java实现AMQP RPC

**目录节点 3/4。** “咖啡时间的RPC：用Java实现AMQP RPC”服务于“比较Java与.NET客户端库，以Hello World、事件驱动告警和Java AMQP RPC复现跨语言消息模式”。先确定它作用于connection、channel、exchange、binding、queue、consumer、cluster node还是插件，再标出声明、路由、确认、持久化与释放边界。结论必须能从配置、rabbitmqctl或Management状态、日志、消息ID和客户端事件中复核。

机制轨迹从“重温告警：把告警应用移植到事件驱动.NET”进入“咖啡时间的RPC：用Java实现AMQP RPC”，再到“总结”。逐步记录AMQP方法、broker状态和客户端回调，区分“方法已发送”“broker已接收或确认”“消费者已处理”“业务结果已对账”。连接关闭、节点故障、重试和应用重启必须有独立分支，不能只画正常箭头。

实验固定RabbitMQ 2.7环境、vhost、交换器、队列、路由键和消息ID，只改变持久化、confirm、ack、消费者速度、节点或网络故障之一。比较吞吐、P95/P99、ready/unacked、内存、Erlang进程与最终消息集合，用“两种客户端对交换器队列绑定、content properties、ack、reply_to和correlation id的映射一致，资源关闭与失败语义明确”验收。

### 总结

**目录节点 4/4。** “总结”服务于“比较Java与.NET客户端库，以Hello World、事件驱动告警和Java AMQP RPC复现跨语言消息模式”。先确定它作用于connection、channel、exchange、binding、queue、consumer、cluster node还是插件，再标出声明、路由、确认、持久化与释放边界。结论必须能从配置、rabbitmqctl或Management状态、日志、消息ID和客户端事件中复核。

机制轨迹从“咖啡时间的RPC：用Java实现AMQP RPC”进入“总结”，再到“本章输出与消息对账”。逐步记录AMQP方法、broker状态和客户端回调，区分“方法已发送”“broker已接收或确认”“消费者已处理”“业务结果已对账”。连接关闭、节点故障、重试和应用重启必须有独立分支，不能只画正常箭头。

实验固定RabbitMQ 2.7环境、vhost、交换器、队列、路由键和消息ID，只改变持久化、confirm、ack、消费者速度、节点或网络故障之一。比较吞吐、P95/P99、ready/unacked、内存、Erlang进程与最终消息集合，用“两种客户端对交换器队列绑定、content properties、ack、reply_to和correlation id的映射一致，资源关闭与失败语义明确”验收。

## 机制模型：拓扑、路由与确认

connection承担TCP与认证，channel在一条连接上复用AMQP会话；channel级协议错误可能关闭channel而不是整个节点。交换器不存储消息，它依据direct、fanout、topic或headers规则把发布路由到绑定队列；无匹配路由的消息是否可感知取决于mandatory等机制，不能默认为已入队。

队列保存ready消息并跟踪已投递未确认消息。消费者确认意味着客户端声明处理完成，断线前未确认消息通常会重新排队，因此业务处理必须能识别重复。prefetch约束未确认在途量，过大可能造成单消费者囤积，过小可能限制吞吐；实验要同时看吞吐、尾延迟与公平性。

持久化与复制需要分别证明。消息写入持久队列并标记persistent，仍不等于每次确认都完成磁盘同步；publisher confirm提供broker侧可观察边界。RabbitMQ 2.7经典镜像队列与当代quorum queue不同，本页只讨论原书策略和当时故障语义。

<Callout type="trap">
  **语义陷阱。**
  只比较API名称，不核对声明参数、属性编码、确认模式、异常和连接channel释放差异。先写清发布确认点、消费处理点、ack点和最终业务不变量，再选择配置。
</Callout>

<Callout type="trap">
  **版本陷阱。** 原书以RabbitMQ
  2.7.0为基线，经典镜像队列、旧插件目录和rabbitmqadmin均有时代语境；当代RabbitMQ结果可作迁移差异，不能证明本书行为。
</Callout>

<Callout type="warning">
  节点停止、网络隔离、磁盘故障、队列清空、权限变更、证书轮换和插件卸载只在隔离RabbitMQ
  2.7环境演练。先备份配置与消息样本并规定停止上界。
</Callout>

## 独立证据与生产交接

目录证据保存Manning 12章3附录与中文版节点映射；环境证据保存Erlang、RabbitMQ 2.7、客户端、节点名、cookie、vhost和插件；拓扑证据保存交换器、绑定、队列属性、消费者与经典镜像策略；运行证据保存消息生成规则、吞吐、分位延迟、ready/unacked、内存和Erlang进程。

性能判断固定消息大小、路由键分布、交换器类型、绑定数量、持久化、confirm、ack与副本语义。关闭可靠性换来的吞吐不是等价优化。平均值会隐藏磁盘刷写、垃圾回收、消费者停顿和节点切换，必须保留P95/P99与队列积压。

恢复判断从消息集合结束。节点重启、连接恢复、队列重新可见、Shovel继续运行或插件重新加载都只是阶段；还要核对每个message_id的出现次数、业务结果和未确认状态，并确认旧节点、旧消费者或旧脚本不再错误操作。

## 本章回顾

重新完成“比较Java与.NET客户端库，以Hello World、事件驱动告警和Java AMQP RPC复现跨语言消息模式”：固定RabbitMQ 2.7和原书目录，从AMQP拓扑推导状态与确认边界，通过基线、压力与故障实验测量，最后交付Java/.NET API对照、Hello World、事件告警、Java RPC和跨语言消息互操作测试。只有“两种客户端对交换器队列绑定、content properties、ack、reply_to和correlation id的映射一致，资源关闭与失败语义明确”与反例都可独立重放，本页才算完成。

## 复习与运行验收

<Exercises>
**问题 1**：为什么“附录A 在Java和.NET上使用Rabbit”必须覆盖4个目录节点？
<Answer>这些节点共同组成“比较Java与.NET客户端库，以Hello World、事件驱动告警和Java AMQP RPC复现跨语言消息模式”的拓扑、状态与证据链；漏项会使Java/.NET API对照、Hello World、事件告警、Java RPC和跨语言消息互操作测试无法独立复现。</Answer>

**问题 2**：本页最小不变量是什么？

<Answer>
  两种客户端对交换器队列绑定、content properties、ack、reply_to和correlation
  id的映射一致，资源关闭与失败语义明确。需要版本、拓扑、confirm/ack轨迹、指标、故障与消息对账共同证明。
</Answer>

**问题 3**：怎样构造最小反例？

<Answer>
  固定消息、vhost、交换器、队列与路由键，只改变持久化、confirm、ack、消费者速度、节点或网络故障之一，再比较broker状态与最终消息集合。
</Answer>

**问题 4**：为什么不能直接用当代RabbitMQ替代本章实验？

<Answer>
  原书以RabbitMQ
  2.7.0为基线；队列类型、插件布局、CLI、安全和集群行为后来均有变化，新版本只能作为差异材料。
</Answer>

**问题 5**：如何验证性能结论？

<Answer>
  固定消息大小、路由分布、拓扑与可靠语义，重复测吞吐、P50/P95/P99、ready/unacked、内存、进程、网络和磁盘，并独立对账。
</Answer>

**问题 6**：独立交接需要什么？

<Answer>需要完整目录映射、版本环境、AMQP拓扑、Java/.NET API对照、Hello World、事件告警、Java RPC和跨语言消息互操作测试、负载、故障、对账、停止、恢复、回退和责任人。</Answer>
</Exercises>

<Glossary>
  <GlossaryItem term="Java客户端">
    Java客户端在本页对应RabbitMQ 2.7与AMQP
    0-9-1中的具体对象、方法或状态；适用范围由“两种客户端对交换器队列绑定、content
    properties、ack、reply_to和correlation
    id的映射一致，资源关闭与失败语义明确”和故障反例共同限定。
  </GlossaryItem>
  <GlossaryItem term=".NET客户端">
    .NET客户端在本页对应RabbitMQ 2.7与AMQP
    0-9-1中的具体对象、方法或状态；适用范围由“两种客户端对交换器队列绑定、content
    properties、ack、reply_to和correlation
    id的映射一致，资源关闭与失败语义明确”和故障反例共同限定。
  </GlossaryItem>
  <GlossaryItem term="事件驱动">
    事件驱动在本页对应RabbitMQ 2.7与AMQP
    0-9-1中的具体对象、方法或状态；适用范围由“两种客户端对交换器队列绑定、content
    properties、ack、reply_to和correlation
    id的映射一致，资源关闭与失败语义明确”和故障反例共同限定。
  </GlossaryItem>
  <GlossaryItem term="AMQP RPC">
    AMQP RPC在本页对应RabbitMQ 2.7与AMQP
    0-9-1中的具体对象、方法或状态；适用范围由“两种客户端对交换器队列绑定、content
    properties、ack、reply_to和correlation
    id的映射一致，资源关闭与失败语义明确”和故障反例共同限定。
  </GlossaryItem>
  <GlossaryItem term="互操作">
    互操作在本页对应RabbitMQ 2.7与AMQP
    0-9-1中的具体对象、方法或状态；适用范围由“两种客户端对交换器队列绑定、content
    properties、ack、reply_to和correlation
    id的映射一致，资源关闭与失败语义明确”和故障反例共同限定。
  </GlossaryItem>
</Glossary>

[← 上一页：第12章 聪明的Rabbit：扩展RabbitMQ](/learn/rabbitmq-practice/12-extending-rabbitmq/rmq-12-extending-rabbitmq) · [下一页：附录B 在线资源 →](/learn/rabbitmq-practice/14-appendix-b-online-resources/rmq-appendix-b-online-resources)

<Attribution
  adaptedFrom="Alvaro Videla、Jason J. W. Williams《RabbitMQ实战》"
  adaptedUrl="https://livebook.manning.com/book/rabbitmq-in-action/table-of-contents"
/>
