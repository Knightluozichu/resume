---
title: "Ray Tracing in One Weekend 全书学习地图"
type: A
section: "计算机图形学与渲染"
order: 0
description: "十章脉络——从一条射线到最终渲染的完整学习路径"
demo: true
math: false
sourceUrl: ""
draft: true
---

import { RtwLearningMapDiagram } from "@/components/mdx/ray-tracing-weekend/diagrams/rtw-learning-map";
import { Objectives, Callout, Attribution, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step } from "@/components/mdx/mdx-components";

<Objectives>
- 能说出全书十章的递进关系与四大阶段
- 能解释光线追踪与光栅化在渲染思路上的根本差异
- 能根据目标效果判断需要用到哪几章的知识
- 能回答：为什么「一个周末」能做出真实感渲染？
</Objectives>

## 直觉引入

把渲染想成「反向追光」：真实世界里光从光源出发，弹射无数次后进入眼睛；而光线追踪反过来——从眼睛（相机）发射射线，穿过像素，撞到物体后计算它「看见了什么颜色」。这样不需要猜物体该亮还是该暗，只要老老实实模拟光的弹射路径，阴影、反射、折射自然就出现了。

这本书用一个周末的时间，带你从「画一个渐变背景」开始，一步步加上球体、相机、材质、散射，最后渲染出一张布满玻璃球与金属球的真实感图片。没有第三方引擎，全部用几百行 C++ 从零写出来。

## 概念讲解

全书分四个阶段：**基础**（射线与相交）、**几何**（球体与可命中对象）、**材质**（散射模型）、**收尾**（景深与最终场景）。

<Term def="从相机出发、穿过像素的数学直线">光线</Term>是追踪的最小单位。<Term def="光线与物体表面相交并改变方向的过程">散射</Term>是材质的本质。<Term def="光线在场景中反复弹射直到停止">递归追踪</Term>是真实感的来源。<Term def="把多条光线的颜色平均以消除噪点">采样</Term>是画质的关键。

### 阶段依赖
- 基础 → 几何：有了射线方程，才能求它与球体的交点。
- 几何 → 材质：有了相交点和法线，材质才知道往哪散射。
- 材质 → 收尾：散射加上景深与多采样，画面才真实。

### 学习节奏建议
- 第一章通读全图，建立全局观；
- 第 2–4 章是「能渲染出球」的骨架，必须动手写；
- 第 5–8 章是「能渲染出材质」的血肉，逐个加；
- 第 9–10 章是组装与复盘。

## 交互Demo

> 猜一猜：把光线弹射深度从 10 调到 50，画面会更清晰还是更暗？（提示：每弹一次能量都会损失）

<RtwLearningMapDiagram />

<Stepper>
<Step title="第一步：发射光线">
从相机出发，对每个像素发射一条射线，穿过视口进入场景。这就是光线追踪的「起点」。
</Step>
<Step title="第二步：求交与散射">
射线撞到球体后，记录交点与法线，交给材质决定散射方向：漫反射随机弹、金属镜面弹、玻璃折射弹。
</Step>
<Step title="第三步：递归与采样">
散射后的新射线继续追踪，递归若干层；每个像素发射多条射线取平均，得到无噪点的最终颜色。
</Step>
</Stepper>

## 代码对照

全书核心循环的骨架（C++），三件事：生成光线、求交散射、累加颜色。

```cpp
// 主渲染循环：逐像素发射光线
for (int j = image_height - 1; j >= 0; --j) {
  for (int i = 0; i < image_width; ++i) {
    color pixel_color(0, 0, 0);
    for (int s = 0; s < samples_per_pixel; ++s) {
      auto u = (i + random_double()) / (image_width - 1);
      auto v = (j + random_double()) / (image_height - 1);
      ray r = cam.get_ray(u, v);
      pixel_color += ray_color(r, world, max_depth);
    }
    write_color(std::cout, pixel_color, samples_per_pixel);
  }
}

// 递归着色：撞到物体就按材质散射，没撞到就返回背景
color ray_color(const ray& r, const hittable& world, int depth) {
  hit_record rec;
  if (depth <= 0) return color(0, 0, 0);          // 递归上限
  if (world.hit(r, 0.001, infinity, rec)) {
    ray scattered; color attenuation;
    if (rec.mat_ptr->scatter(r, rec, attenuation, scattered))
      return attenuation * ray_color(scattered, world, depth - 1);
    return color(0, 0, 0);
  }
  vec3 unit_dir = unit_vector(r.direction());
  auto t = 0.5 * (unit_dir.y() + 1.0);
  return (1.0 - t) * color(1.0, 1.0, 1.0) + t * color(0.5, 0.7, 1.0);
}
```

`samples_per_pixel` 控制抗锯齿与降噪，`max_depth` 控制反射弹射次数，两者是画质与耗时的旋钮。

## 常见误区

<Callout type="trap">
**现象 →** 以为光线追踪「一步到位」渲染出真实图片。
**原因 →** 真实感来自递归散射与多次采样的累加，单条光线只能得到一个噪点。
**修法 →** 接受「每像素发射几十上百条光线」是常态，理解画质是统计平均出来的，不是单次计算出来的。
</Callout>

<Callout type="trap">
**现象 →** 跳过求交与法线，直接套材质公式，结果颜色全黑或方向乱飞。
**原因 →** 材质的 scatter 依赖相交点的法线与入射方向，没有正确的 hit_record 就无从散射。
**修法 →** 严格按「先射线 → 再求交 → 后材质」的顺序，每一章都建立在前一章的 hit_record 之上。
</Callout>

## 练习

<Exercises>
**问题**：光线追踪与光栅化在「光的传播方向」上有什么根本区别？这个区别带来了什么代价？

<Answer>
光栅化是把几何体投影到屏幕（从物体到像素）；光线追踪是从相机发射射线穿过像素去求交（从像素到物体），并递归模拟光的弹射。代价是光线追踪对每条射线都要做场景求交，计算量远大于光栅化，但能自然得到反射、折射、阴影等全局光照效果。
</Answer>

**问题**：全书四个阶段分别解决什么问题？如果只能保留一个阶段来「看到一张图」，你保留哪个？

<Answer>
基础（射线与相交）解决「光怎么走」；几何（球体）解决「光撞到了什么」；材质（散射）解决「撞到后变成什么颜色」；收尾（景深与场景）解决「画面够不够真实」。若只要看到一张图，必须保留基础——没有射线方程就没有光线追踪，其余都是在这条射线上叠加效果。
</Answer>
</Exercises>

<Glossary>
<GlossaryItem term="光线">从相机出发、穿过某个像素的数学直线，是光线追踪的基本单位，用参数方程 P(t)=A+t·b 表示。</GlossaryItem>
<GlossaryItem term="散射">光线击中物体表面后改变方向的过程，不同材质（漫反射、金属、玻璃）有不同散射规律。</GlossaryItem>
<GlossaryItem term="递归追踪">散射后的新射线继续在场景中求交与散射，层层递归，直到达到深度上限或射向天空。</GlossaryItem>
<GlossaryItem term="采样">对同一像素发射多条略有差异的光线并取平均，用于消除边缘锯齿与画面噪点。</GlossaryItem>
</Glossary>

<Attribution adaptedFrom="Ray Tracing in One Weekend (Peter Shirley)" adaptedUrl="https://raytracing.github.io/books/RayTracingInOneWeekend.html" />
