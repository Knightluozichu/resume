---
title: "散焦模糊与景深"
type: B
section: "计算机图形学与渲染"
order: 7
description: "用薄透镜近似模拟相机的景深效果"
demo: true
math: true
sourceUrl: ""
draft: false
---

import { RtwDefocusBlurDiagram } from "@/components/mdx/ray-tracing-weekend/diagrams/rtw-defocus-blur";
import { RtwStepFlowDiagram } from "@/components/mdx/ray-tracing-weekend/diagrams/rtw-step-flow";
import { Objectives, Callout, Attribution, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step } from "@/components/mdx/mdx-components";

<Objectives>
- 能解释针孔相机与薄透镜相机的区别
- 能写出从散焦圆盘采样起点构造光线的流程
- 能说明焦距（focus_dist）如何决定清晰平面
- 能回答：为什么远离焦平面的物体会变模糊？
</Objectives>

## 直觉引入

之前的相机是个「针孔」：所有光线都从同一个点出发，所以远近物体都一样清晰。真实相机有镜头，镜头有一定大小，光线从镜头各处进入并汇聚——只有恰好在「对焦距离」上的物体落在焦平面上最清晰，太近或太远的物体会发散成一团光斑，这就是景深/散焦模糊。

光线追踪用「薄透镜近似」模拟它：相机不再是单点，而是一个小圆盘；每条光线的起点在圆盘上随机取一点，但方向仍指向视口上同一像素对应的对焦点。对焦点固定在焦平面上，于是焦平面上的物体清晰，前后物体因不同起点出发的光线落点错开而模糊。

## 概念讲解

<Term def="假设光线穿过薄透镜中心不改变方向的简化模型">薄透镜近似</Term>。设<Term def="镜头开孔大小决定模糊强度">光圈</Term>半径 $r$、<Term def="对焦平面到相机的距离">焦距</Term> $f_d$。

### 构造散焦光线
1. 视口仍按焦距 $f_d$ 摆放（而非单位 1），使视口正好落在焦平面上。
2. 对像素 $(u,v)$，先算焦平面上的目标点 $\mathbf{t}$ = `lower_left_corner + u·horizontal + v·vertical`。
3. 在相机所在圆盘上随机取起点 $\mathbf{o}'=\mathbf{o}+\vec{p}_{disk}\cdot r$。
4. 该像素的这条采样光线 = `ray(o', t − o')`。

### 先建立能移动的相机坐标基

“圆盘在相机位置”不是指它永远躺在世界 XY 平面。<Term def="相机正交基：由 w、u、v 三个相互垂直的单位向量组成的相机局部坐标系。w 指向视线反方向，u 向右，v 向上；它把二维透镜圆盘正确放到任意朝向的三维相机上。">相机正交基</Term>应从 `lookfrom`、`lookat`、`vup` 构造：

$$
\mathbf{w}=\frac{\mathbf{lookfrom}-\mathbf{lookat}}{\|\mathbf{lookfrom}-\mathbf{lookat}\|},\qquad
\mathbf{u}=\frac{\mathbf{vup}\times\mathbf{w}}{\|\mathbf{vup}\times\mathbf{w}\|},\qquad
\mathbf{v}=\mathbf{w}\times\mathbf{u}
$$

`w` 指向镜头的后方，所以焦平面中心位于 `origin - focus_dist * w`。`u` 和 `v` 是透镜平面的两个轴；将单位圆盘里的二维样本 `(x,y)` 写成 `x*u + y*v`，光圈就会随相机旋转。若直接把 `(x,y,0)` 加到世界坐标，只有相机恰好朝 -Z 时景深才看起来正确。

### 光圈半径与离焦角是同一件事的两种参数

<Term def="离焦角：从焦平面中心看向透镜圆盘边缘的张角。它比直接使用世界单位的 aperture 更稳定，因为 focus_dist 改变时，角度保持不变就能保持相似的模糊观感。">离焦角</Term>是官方新版更推荐的控制量；旧式 `aperture` 则直接给出透镜直径。两者的关系是：

$$
r_{lens}=\frac{\mathrm{aperture}}{2}
=f_d\tan\left(\frac{\mathrm{defocus\_angle}}{2}\right)
$$

当 `focus_dist` 变大而 aperture 半径固定时，构图尺度改变会连带改变离焦观感；用固定离焦角时，透镜半径随焦距比例调整，镜头语言更稳定。两种参数化都能工作，但项目里必须只选一种作为源参数，不能同时乘两次 `focus_dist`。

### 两类随机样本不能混用

<Term def="透镜采样：在散焦圆盘上随机选择射线起点，用来模拟有限口径导致的景深；它与像素内的随机抖动不同，后者只负责抗锯齿。">透镜采样</Term>与像素采样都发生在 `get_ray` 附近，却解决不同问题。像素样本改变焦平面上的目标点：

$$
\mathbf{p}_{focus}=\mathbf{pixel}_{00}+(i+\xi)\Delta\mathbf{u}+(j+\eta)\Delta\mathbf{v}
$$

透镜样本改变起点但不改变该次样本对应的焦点：

$$
\mathbf{o}'=\mathbf{o}+r_{lens}(x\mathbf{u}+y\mathbf{v}),\qquad
\mathbf{r}(t)=\mathbf{o}'+t(\mathbf{p}_{focus}-\mathbf{o}')
$$

若只做像素抖动，边缘会平滑但景深仍为零；若只做透镜采样，焦外会模糊但边缘锯齿仍存在。两者都要在同一像素的多次路径中平均，且样本数不足时，散焦区域会先表现为颗粒噪点而不是柔滑光斑。

### 为什么产生模糊
焦平面上的点：无论从圆盘哪个起点出发，方向都指向同一目标点，最终汇聚到同一像素——清晰。不在焦平面的点：不同起点出发的光线指向同一像素目标但实际经过该点的不同位置，叠加后散成一团——模糊。距离焦平面越远，散得越开，越模糊。

<Term def="离焦圆：一个离开焦平面的三维点，经有限大小透镜投到传感器后不再收敛为一点，而是扩展成的圆形模糊区域。它的直径随光圈增大和偏离焦平面的程度增加。">离焦圆</Term>是景深可见效果的几何原因。不要把“背景模糊”误解为统一高斯滤镜：近景和远景会以不同尺度发散，遮挡边缘也会出现前景遮住背景光斑的复杂关系，这些都是逐路径几何采样自然产生的结果。

## 交互Demo

> 猜一猜：光圈半径调大，景深是变浅（模糊更明显）还是变深？

<RtwDefocusBlurDiagram />

<Stepper>
<Step title="第一步：设焦平面">
<RtwStepFlowDiagram caption="景深第 1 步：先把图像平面放在指定的对焦距离上。" stage={1} stages={["设定 focus_dist 与焦平面", "在透镜圆盘采样起点", "连向焦点并平均光线"]} />

选定对焦距离 focus_dist，把视口摆在该距离处，视口上的点即对焦点。
</Step>
<Step title="第二步：圆盘采样">
<RtwStepFlowDiagram caption="景深第 2 步：同一个像素从不同透镜位置出发，才会产生离焦圆。" stage={2} stages={["设定 focus_dist 与焦平面", "在透镜圆盘采样起点", "连向焦点并平均光线"]} />

在相机所在的光圈圆盘内随机取一点 o' 作为这条光线的起点（替代原来的单点 O）。
</Step>
<Step title="第三步：发射与累加">
<RtwStepFlowDiagram caption="景深第 3 步：焦平面交汇、其他深度分离的样本平均成真实模糊。" stage={3} stages={["设定 focus_dist 与焦平面", "在透镜圆盘采样起点", "连向焦点并平均光线"]} />

光线方向 = 对焦点 t − o'。每像素多条采样平均，焦平面上清晰，前后物体因起点不同而发散模糊。
</Step>
</Stepper>

## 代码对照

C++ 散焦相机：

```cpp
class camera {
  public:
    camera(double vfov, double aspect, double focus_dist, double aperture) {
      auto theta = degrees_to_radians(vfov);
      auto h = tan(theta/2);
      viewport_height = 2 * h;
      viewport_width  = aspect * viewport_height;

      w = unit_vector(lookfrom - lookat);     // 视线反方向
      u = unit_vector(cross(vup, w));          // 右
      v = cross(w, u);                          // 上

      origin = lookfrom;
      horizontal = focus_dist * viewport_width * u;
      vertical   = focus_dist * viewport_height * v;
      lower_left_corner = origin - horizontal/2 - vertical/2
                          - focus_dist*w;
      lens_radius = aperture / 2;
    }
    ray get_ray(double s, double t) const {
      vec3 rd = lens_radius * random_in_unit_disk();   // 圆盘采样
      vec3 offset = u*rd.x() + v*rd.y();
      // 起点偏移，方向仍指向焦平面上的同一点
      return ray(origin + offset,
                 lower_left_corner + s*horizontal + t*vertical
                 - origin - offset);
    }
  private:
    point3 origin, lower_left_corner;
    vec3 horizontal, vertical, u, v, w;
    double lens_radius;
};
```

`random_in_unit_disk` 在单位圆内均匀采样；`offset` 让起点在光圈圆盘内游走，方向始终锁向焦平面对焦点。

```cpp
vec3 random_in_unit_disk() {
  while (true) {
    auto p = vec3(random_double(-1, 1), random_double(-1, 1), 0);
    if (p.length_squared() < 1) return p;
  }
}

double defocus_radius(double focus_dist, double defocus_angle_degrees) {
  return focus_dist * std::tan(degrees_to_radians(defocus_angle_degrees / 2));
}
```

拒绝采样得到的是圆盘**面积**内均匀样本，而不是只在圆周上取点；若误用单位圆周，模糊能量会集中在离焦圆边缘，形成不自然的空心轮廓。将圆盘样本乘以 `u`、`v` 后再加到 origin，才是任意朝向相机的透镜偏移。

## 常见误区

<Callout type="trap">
**现象 →** 调了光圈但画面毫无模糊，远近一样清晰。
**原因 →** lens_radius 算成 0（aperture 没传或除错），或 offset 没加到 ray 的起点上，光线仍从单点出发。
**修法 →** 确认 lens_radius=aperture/2 且 get_ray 把 offset 加到 origin 上、同时从方向里减去 offset，使方向锁向焦平面同一点。
</Callout>

<Callout type="trap">
**现象 →** 整张图都模糊，连对焦物体也糊。
**原因 →** 视口没按 focus_dist 缩放，对焦点不在焦平面上，所有物体都偏离对焦距离。
**修法 →** horizontal、vertical、lower_left_corner 都乘 focus_dist，让视口正好落在焦平面上，对焦物体才会清晰。
</Callout>

## 练习

<Exercises>
**问题 1**：散焦模糊的光线起点和方向分别怎么确定？为什么焦平面上的物体会清晰？

<Answer>
起点在相机光圈圆盘内随机取 o'=O+offset；方向指向焦平面上该像素的对焦点 t，即 t−o'。焦平面上的物体恰好在焦距处，不同起点出发的光线都汇聚到同一像素的同一像点，叠加不发散，所以清晰；偏离焦平面的物体则因不同起点的光线落点错开而模糊。
</Answer>

**问题 2**：光圈半径变大，景深如何变化？focus_dist 改变影响什么？

<Answer>
光圈半径变大，散焦圆盘更大，偏离焦平面的物体散得更开、模糊更强，景深变浅（清晰范围变窄）。focus_dist 决定焦平面到相机的距离，改变它就是「对焦」到不同远近的物体：该距离上的物体清晰，其他距离更模糊。
</Answer>

**问题 3**：为什么在同一条 `get_ray` 中既需要像素随机数 `(xi, eta)`，又需要透镜圆盘样本 `(x, y)`？只保留其中一个各会遗漏什么现象？

<Answer>
像素随机数改变焦平面上的目标点，用于积分像素面积并减少边缘锯齿；透镜样本改变射线起点，用于积分有限口径并产生景深。只保留像素抖动时，边缘平滑但所有距离都清晰；只保留透镜样本时，焦外会模糊但几何轮廓仍有锯齿。两种随机性都要跨样本平均，不能互相替代。
</Answer>
</Exercises>

## 小结

- `w/u/v` 将相机的焦平面和透镜圆盘从世界坐标中解耦，使任意朝向都可对焦。
- focus_dist 决定焦平面的位置；aperture 半径或 defocus_angle 决定透镜圆盘的大小。
- 像素采样和透镜采样分别处理抗锯齿与景深，两者都应在路径平均之前完成。
- 焦外模糊来自不同透镜起点的几何视差，而不是后处理模糊滤镜。

<Glossary>
<GlossaryItem term="薄透镜近似">假设光线过透镜中心不改变方向的简化模型，用于模拟相机景深。</GlossaryItem>
<GlossaryItem term="光圈">镜头开孔大小（半径），决定散焦模糊强度；越大景深越浅。</GlossaryItem>
<GlossaryItem term="焦距">focus_dist，对焦平面到相机的距离，该距离上的物体最清晰。</GlossaryItem>
<GlossaryItem term="散焦圆盘">相机所在的光圈圆盘，每条光线的起点在其中随机采样。</GlossaryItem>
<GlossaryItem term="相机正交基">由 w、u、v 三个相互垂直的单位向量组成的相机局部坐标系，用来放置视口与透镜圆盘。</GlossaryItem>
<GlossaryItem term="离焦角">从焦平面中心看向透镜圆盘边缘的张角；相比固定 aperture，它随 focus_dist 变化时更容易保持一致观感。</GlossaryItem>
<GlossaryItem term="透镜采样">在散焦圆盘上随机选择射线起点，用来模拟有限口径导致的景深，与像素内抗锯齿采样不同。</GlossaryItem>
<GlossaryItem term="离焦圆">离开焦平面的点经有限透镜投到传感器后扩展成的圆形模糊区域，随光圈和失焦程度增大。</GlossaryItem>
</Glossary>

<Attribution adaptedFrom="Ray Tracing in One Weekend (Peter Shirley)" adaptedUrl="https://raytracing.github.io/books/RayTracingInOneWeekend.html" />
