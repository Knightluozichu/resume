---
title: Dielectrics
description: Dielectrics：在反射、折射和全内反射之间做概率选择，以公式、章专属交互和可重放故障证据独立重写。
type: B
section: 周末光线追踪 · C++路径追踪 · materials
order: 9
sourceUrl: 'https://raytracing.github.io/books/RayTracingInOneWeekend.html'
demo: true
math: true
draft: false
qualityVersion: 2
practiceMode: code
sourceMode: independent-rewrite
officialUnitId: rtw-09
---
import { Rtw09DielectricsGeometryLab, Rtw09DielectricsSamplingLab, Rtw09DielectricsEvidenceLab } from "@/components/mdx/ray-tracing-weekend/v2/rtw-09-dielectrics";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能说明Dielectrics中的“在反射、折射和全内反射之间做概率选择”，并指出公式中每个量的空间、单位或概率含义
- 能比较正常基线与“正反面使用同一折射率比，或在全内反射条件下仍计算折射根”反例，定位折射率比、cosθ、全内反射判据和随机选择结果中的首个差异
- 能操作三个章专属实验，让电介质、折射、全内反射同时具备解释、视觉和练习证据
- 能修改最小实现、固定输入重放，并判断“Dielectrics的输入、公式中间量、输出与恢复结果可用同一基线复算”是否重新成立

</Objectives>

{/* GRAPHICS_FOUNDATIONS_QUALITY_V2 */}

## 从一个会失败的图像实验开始

Dielectrics要回答的不是“这个名词是什么”，而是：在固定输入下，如何验证“在反射、折射和全内反射之间做概率选择”，并让一个反例可重复地推翻错误实现？先写下预期像素、几何或估计量，再运行基线；只要折射率比、cosθ、全内反射判据和随机选择结果无法指出差异来自哪一步，图像看起来正确也不能通过。

本页的不变量是：Dielectrics的输入、公式中间量、输出与恢复结果可用同一基线复算。反例“正反面使用同一折射率比，或在全内反射条件下仍计算折射根”只改变一个条件，因而正常、故障与恢复三次运行应能用同一输入直接比较。

## 原版、版次与事实来源

Dielectrics以[官方4.0.2全文](https://raytracing.github.io/books/RayTracingInOneWeekend.html)核对实现顺序、公式与调试边界，并以[官方源码仓库](https://github.com/RayTracing/raytracing.github.io)交叉检查可编译接口。课程示例为独立教学重写；固定版本后不把Vulkan、GPU管线或其他书的模板倒灌进这个纯C++路径追踪器。

## 六个检查词

<Term def="电介质在Dielectrics中用于在反射、折射和全内反射之间做概率选择；应由折射率比、cosθ、全内反射判据和随机选择结果确认，而不是只凭术语出现。">电介质</Term>、<Term def="折射在Dielectrics中用于在反射、折射和全内反射之间做概率选择；应由折射率比、cosθ、全内反射判据和随机选择结果确认，而不是只凭术语出现。">折射</Term>、<Term def="全内反射在Dielectrics中用于在反射、折射和全内反射之间做概率选择；应由折射率比、cosθ、全内反射判据和随机选择结果确认，而不是只凭术语出现。">全内反射</Term>、<Term def="ray parameter：射线参数决定半直线上采样点与前后顺序。">ray parameter</Term>、<Term def="hit interval：命中区间排除相机背后、过近或更远的候选根。">hit interval</Term>、<Term def="sample estimator：样本估计器用明确PDF和权重近似目标积分。">sample estimator</Term>。这些词共同约束在反射、折射和全内反射之间做概率选择，每个词都必须落到公式、交互状态或可重放输出，不能只出现在术语表。

## 原书单元与独立机制解释

### Dielectrics

电介质通常不吸收颜色，但会依据折射率改变方向；折射由Snell定律约束；当几何条件触发全内反射时只能反射，否则可用Schlick近似在两条路径之间采样。

本单元的正式坐标是电介质、折射、全内反射。验证Dielectrics时，先制造“正反面使用同一折射率比，或在全内反射条件下仍计算折射根”，再检查折射率比、cosθ、全内反射判据和随机选择结果能否把错误定位到在反射、折射和全内反射之间做概率选择的首个分叉。

$$
\eta_i\sin\theta_i=\eta_t\sin\theta_t,\qquad R(\theta)\approx R_0+(1-R_0)(1-\cos\theta)^5
$$

## 先预测，再操作三个章专属实验

<Stepper>
  <Step title="1. 概念与因果路径">
    在Dielectrics中选择任一正式坐标，沿机制链查看它的输入、状态变化和可推翻探针。

    <Rtw09DielectricsGeometryLab />

  </Step>
  <Step title="2. 单变量算法实验">
    保持在反射、折射和全内反射之间做概率选择的其余条件不变，只切换样本、坐标、路径或故障模式，并解释结果为何改变。

    <Rtw09DielectricsSamplingLab />

  </Step>
  <Step title="3. 基线、故障、恢复与复位">
    保存Dielectrics的首个分叉，撤销“正反面使用同一折射率比，或在全内反射条件下仍计算折射根”，以同输入重放后点击重置核对初值。

    <Rtw09DielectricsEvidenceLab />

  </Step>
</Stepper>

## 最小可重现实验

```cpp
double ratio = rec.front_face ? (1.0/refraction_index) : refraction_index;
double cos_theta = std::min(dot(-unit_direction, rec.normal), 1.0);
bool cannot_refract = ratio*std::sqrt(1-cos_theta*cos_theta) > 1;
vec3 direction = cannot_refract ? reflect(unit_direction, rec.normal)
                                : refract(unit_direction, rec.normal, ratio);
```

运行Dielectrics时固定随机种子、输入几何、坐标约定和数值精度；记录编译命令或算法版本、输出摘要与失败条件。实验输出若依赖隐藏全局状态，必须先消除该依赖再比较。

<Callout type="trap" title="本页核心误区">
  正反面使用同一折射率比，或在全内反射条件下仍计算折射根。这个错误会破坏“Dielectrics的输入、公式中间量、输出与恢复结果可用同一基线复算”，应先由折射率比、cosθ、全内反射判据和随机选择结果定位，再讨论性能或视觉风格。
</Callout>

<Callout type="trap" title="不要把别的图形API模板套进来">
  Dielectrics只解释在反射、折射和全内反射之间做概率选择。若原始资料讨论纯C++路径追踪、OpenGL时代图形学或光传输积分，就不能用VkBuffer、Fence与Semaphore替代相应的几何、光栅化或蒙特卡洛机制。
</Callout>

## 练习与答案

<Exercises>

**问题 1：公式与边界。** 怎样用一个数值样本证明 $\eta_i\sin\theta_i=\eta_t\sin\theta_t,\qquad R(\theta)\approx R_0+(1-R_0)(1-\cos\theta)^5$ 中的量没有混用空间、单位或概率密度？

<Answer>
  先写出每个量的定义域与单位，选择能手算的输入，分别计算中间量和最终结果；再只扰动一个量。若折射率比、cosθ、全内反射判据和随机选择结果的变化方向与公式不一致，应拒绝实现而不是调整截图。
</Answer>

**问题 2：正式坐标。** 电介质、折射、全内反射如何进入可操作验证？

<Answer>
  1. **电介质**：在专属实验中选中该节点，保存输入与折射率比、cosθ、全内反射判据和随机选择结果，注入“正反面使用同一折射率比，或在全内反射条件下仍计算折射根”后恢复并重放。
  2. **折射**：在专属实验中选中该节点，保存输入与折射率比、cosθ、全内反射判据和随机选择结果，注入“正反面使用同一折射率比，或在全内反射条件下仍计算折射根”后恢复并重放。
  3. **全内反射**：在专属实验中选中该节点，保存输入与折射率比、cosθ、全内反射判据和随机选择结果，注入“正反面使用同一折射率比，或在全内反射条件下仍计算折射根”后恢复并重放。
</Answer>

**问题 3：故障恢复。** 如何证明“正反面使用同一折射率比，或在全内反射条件下仍计算折射根”已经真正修复？

<Answer>
  保存正常基线，注入故障并标记折射率比、cosθ、全内反射判据和随机选择结果中的首个分叉；撤销故障后以完全相同输入重放。只有Dielectrics的输入、公式中间量、输出与恢复结果可用同一基线复算重新成立、结果可复现且实验重置回初值，修复才可交接。
</Answer>

</Exercises>

## 本章回顾

掌握Dielectrics意味着能将“在反射、折射和全内反射之间做概率选择”写成明确公式或算法，能操作正式坐标，能制造“正反面使用同一折射率比，或在全内反射条件下仍计算折射根”，还能凭折射率比、cosθ、全内反射判据和随机选择结果恢复同输入结果，而不是把一段Vulkan生命周期模板换上本章标题。

<Glossary>
  <GlossaryItem term="电介质">
    电介质在Dielectrics中用于在反射、折射和全内反射之间做概率选择；应由折射率比、cosθ、全内反射判据和随机选择结果确认，而不是只凭术语出现。
  </GlossaryItem>
  <GlossaryItem term="折射">
    折射在Dielectrics中用于在反射、折射和全内反射之间做概率选择；应由折射率比、cosθ、全内反射判据和随机选择结果确认，而不是只凭术语出现。
  </GlossaryItem>
  <GlossaryItem term="全内反射">
    全内反射在Dielectrics中用于在反射、折射和全内反射之间做概率选择；应由折射率比、cosθ、全内反射判据和随机选择结果确认，而不是只凭术语出现。
  </GlossaryItem>
  <GlossaryItem term="ray parameter">
    ray parameter：射线参数决定半直线上采样点与前后顺序。
  </GlossaryItem>
  <GlossaryItem term="hit interval">
    hit interval：命中区间排除相机背后、过近或更远的候选根。
  </GlossaryItem>
  <GlossaryItem term="sample estimator">
    sample estimator：样本估计器用明确PDF和权重近似目标积分。
  </GlossaryItem>
</Glossary>

## 阅读导航

[← Metal](/learn/ray-tracing-weekend/03-materials/rtw-08-metal) · [Positionable Camera →](/learn/ray-tracing-weekend/04-camera/rtw-10-positionable-camera)

<Attribution
  mode="independent-rewrite"
  sourceBasis="full-text-primary"
  workTitle="Peter Shirley、Trevor David Black、Steve Hollasch《Ray Tracing in One Weekend》4.0.2"
  adaptedUrl="https://raytracing.github.io/books/RayTracingInOneWeekend.html"
/>
