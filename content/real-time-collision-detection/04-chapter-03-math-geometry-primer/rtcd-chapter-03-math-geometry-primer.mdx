---
title: "Chapter 3 A Math and Geometry Primer"
type: B
section: "实时碰撞检测算法技术（清华2010 / Morgan Kaufmann 2005）"
order: 4
description: "本章建立矩阵、向量、行列式谓词、重心坐标、凸包、Voronoi区与Minkowski运算的统一几何语言。 逐项覆盖35个权威目录坐标，并用退化几何、误差边界和目标机轨迹复核。"
demo: true
math: true
sourceUrl: "https://www.oreilly.com/library/view/real-time-collision-detection/9781558607323/"
draft: true
---

import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释Chapter 3 A Math and Geometry Primer在15个正式单元、13章与357个目录节点中的位置
- 能绘制“统一坐标约定 → 构造几何谓词 → 表达基本图元 → 建立凸性结构 → 验证退化输入”机制链并标出几何域、误差界和提前退出条件
- 能推导最近点、区间重叠、层次代价与浮点容差，并复算复杂度
- 能设计正常、退化、压力与故障实验，让第三方在目标机上独立复现

</Objectives>

## 为什么碰撞检测必须从查询合同开始

本章建立矩阵、向量、行列式谓词、重心坐标、凸包、Voronoi区与Minkowski运算的统一几何语言。 本课程不复制原书正文、插图或配套代码，而把公开目录中的算法组织成输入合同、几何谓词、交互实验、退化样本和验收证据。本页逐项覆盖35个目录坐标：Chapter 3 A Math and Geometry Primer、3.1 Matrices、3.1.1 Matrix Arithmetic、3.1.2 Algebraic Identities Involving Matrices、3.1.3 Determinants、3.1.4 Solving Small Systems of Linear Equation Using Cramer’s Rule、3.1.5 Matrix Inverses for 2 × 2 and 3 × 3 Matrices、3.1.6 Determinant Predicates、3.1.6.1 ORIENT2D(A, B, C)、3.1.6.2 ORIENT3D(A, B, C, D)、3.1.6.3 INCIRCLE2D(A, B, C, D)、3.1.6.4 INSPHERE(A, B, C, D, E)、3.2 Coordinate Systems and Points、3.3 Vectors、3.3.1 Vector Arithmetic、3.3.2 Algebraic Identities Involving Vectors、3.3.3 The Dot Product、3.3.4 Algebraic Identities Involving Dot Products、3.3.5 The Cross Product、3.3.6 Algebraic Identities Involving Cross Products、3.3.7 The Scalar Triple Product、3.3.8 Algebraic Identities Involving Scalar Triple Products、3.4 Barycentric Coordinates、3.5 Lines, Rays, and Segments、3.6 Planes and Halfspaces、3.7 Polygons、3.7.1 Testing Polygonal Convexity、3.8 Polyhedra、3.8.1 Testing Polyhedral Convexity、3.9 Computing Convex Hulls、3.9.1 Andrew’s Algorithm、3.9.2 The Quickhull Algorithm、3.10 Voronoi Regions、3.11 Minkowski Sum and Difference、3.12 Summary。

实时碰撞检测不是“两个盒子是否相交”的单一布尔问题。上层可能需要最近距离、命中顺序、首次接触时间、穿透方向或稳定接触流形；下层又受到坐标尺度、浮点误差、非流形网格、缓存布局和候选对规模约束。只有先冻结查询类型、输入域、边界包含规则和失败状态，才能比较算法是否正确且足够快。

## 版次、目录与改编合同

本路径同时固定原版与中译本：Christer Ericson《Real-Time Collision Detection》，Morgan Kaufmann，2005，ISBN 9781558607323；刘天慧译《实时碰撞检测算法技术》，清华大学出版社，2010年6月，ISBN 9787302224112。O’Reilly授权电子书目录与Morgan Kaufmann原书目录预览列出前置资料、13章所有层级小节，以及参考文献、索引和光盘说明，共357个正式目录节点。课程按15个正式单元逐项覆盖，另设学习地图与全书复核，共17页。

原书代码与GPU接口来自2004至2005年前后的硬件和编译器语境。课程保留几何证明、数据结构和优化推理，但接口、SIMD宽度与GPU同步必须在当前目标平台重新测量；旧代码的运行结果不能替代今天的鲁棒性证据。

## 五个贯穿全书的术语

<Term def="上层调用者对输入几何、查询类型、边界语义、输出字段和失败状态的完整约定。">查询合同</Term>、<Term def="用较廉价结构排除不可能相交对象后留下的待精确检测对象对。">候选对</Term>、<Term def="依据方向、内外或区间关系做离散分类且带误差边界的计算。">几何谓词</Term>、<Term def="移动对象在给定时间区间内第一次接触时对应的参数值。">首次接触时间</Term>、<Term def="包含输入、构建、目标机、谓词轨迹、指标和最终分类的复现记录。">碰撞证据包</Term>。

查询合同决定算法；候选对连接粗筛与精算；几何谓词决定拓扑分类；首次接触时间避免高速穿透；碰撞证据包则让退化场景可以回放。类名和成功截图都不能代替这些状态合同。

## 四个可复算模型

点到线段的参数投影与钳制为：

$$
t=operatorname{clamp}left(rac{(p-a)cdot(b-a)}{lVert b-a
Vert^2},0,1
ight),qquad q=a+t(b-a)
$$

两个AABB在每个轴都重叠时才相交：

$$
A_{min}^{i}le B_{max}^{i};land;B_{min}^{i}le A_{max}^{i},qquad iin{x,y,z}
$$

BVH构建和遍历可用表面积启发式比较：

$$
C=C_t+rac{S_L}{S_P}N_LC_i+rac{S_R}{S_P}N_RC_i
$$

浮点比较的容差同时包含绝对项与相对项：

$$
|a-b|le arepsilon_{abs}+arepsilon_{rel}max(|a|,|b|)
$$

最近点必须处理零长度线段；AABB边界要明确相切是否算碰撞；SAH只是查询分布近似；容差不能脱离几何尺度。每项公式都要通过一般、退化和极端尺度样本复算。

## 分步可视化：结构、规模与故障

<Stepper>
  <Step title="先画查询和数据依赖">
    </Step>
  <Step title="再改变图元与候选规模">
    </Step>
  <Step title="最后注入退化几何">
    </Step>
</Stepper>

### Chapter 3 A Math and Geometry Primer

**目录节点 1/35。** 本章建立矩阵、向量、行列式谓词、重心坐标、凸包、Voronoi区与Minkowski运算的统一几何语言。

实现“Chapter 3 A Math and Geometry Primer”时，沿“统一坐标约定 → 构造几何谓词 → 表达基本图元 → 建立凸性结构 → 验证退化输入”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把坐标约定变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 3.1 Matrices

**目录节点 2/35。** 矩阵运算要固定行列约定和空间方向；小系统求解同时检测奇异与近奇异条件。

实现“3.1 Matrices”时，沿“统一坐标约定 → 构造几何谓词 → 表达基本图元 → 建立凸性结构 → 验证退化输入”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把方向谓词变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 3.1.1 Matrix Arithmetic

**目录节点 3/35。** 矩阵运算要固定行列约定和空间方向；小系统求解同时检测奇异与近奇异条件。

实现“3.1.1 Matrix Arithmetic”时，沿“统一坐标约定 → 构造几何谓词 → 表达基本图元 → 建立凸性结构 → 验证退化输入”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把重心坐标变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 3.1.2 Algebraic Identities Involving Matrices

**目录节点 4/35。** 矩阵运算要固定行列约定和空间方向；小系统求解同时检测奇异与近奇异条件。

实现“3.1.2 Algebraic Identities Involving Matrices”时，沿“统一坐标约定 → 构造几何谓词 → 表达基本图元 → 建立凸性结构 → 验证退化输入”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把凸包结构变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 3.1.3 Determinants

**目录节点 5/35。** 行列式谓词决定方向、内外与共面分类，接近零时必须使用尺度相关误差界或精确回退。

实现“3.1.3 Determinants”时，沿“统一坐标约定 → 构造几何谓词 → 表达基本图元 → 建立凸性结构 → 验证退化输入”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把退化输入变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 3.1.4 Solving Small Systems of Linear Equation Using Cramer’s Rule

**目录节点 6/35。** 矩阵运算要固定行列约定和空间方向；小系统求解同时检测奇异与近奇异条件。

实现“3.1.4 Solving Small Systems of Linear Equation Using Cramer’s Rule”时，沿“统一坐标约定 → 构造几何谓词 → 表达基本图元 → 建立凸性结构 → 验证退化输入”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把坐标约定变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 3.1.5 Matrix Inverses for 2 × 2 and 3 × 3 Matrices

**目录节点 7/35。** 矩阵运算要固定行列约定和空间方向；小系统求解同时检测奇异与近奇异条件。

实现“3.1.5 Matrix Inverses for 2 × 2 and 3 × 3 Matrices”时，沿“统一坐标约定 → 构造几何谓词 → 表达基本图元 → 建立凸性结构 → 验证退化输入”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把方向谓词变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 3.1.6 Determinant Predicates

**目录节点 8/35。** 行列式谓词决定方向、内外与共面分类，接近零时必须使用尺度相关误差界或精确回退。

实现“3.1.6 Determinant Predicates”时，沿“统一坐标约定 → 构造几何谓词 → 表达基本图元 → 建立凸性结构 → 验证退化输入”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把重心坐标变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 3.1.6.1 ORIENT2D(A, B, C)

**目录节点 9/35。** 行列式谓词决定方向、内外与共面分类，接近零时必须使用尺度相关误差界或精确回退。

实现“3.1.6.1 ORIENT2D(A, B, C)”时，沿“统一坐标约定 → 构造几何谓词 → 表达基本图元 → 建立凸性结构 → 验证退化输入”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把凸包结构变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 3.1.6.2 ORIENT3D(A, B, C, D)

**目录节点 10/35。** 行列式谓词决定方向、内外与共面分类，接近零时必须使用尺度相关误差界或精确回退。

实现“3.1.6.2 ORIENT3D(A, B, C, D)”时，沿“统一坐标约定 → 构造几何谓词 → 表达基本图元 → 建立凸性结构 → 验证退化输入”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把退化输入变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 3.1.6.3 INCIRCLE2D(A, B, C, D)

**目录节点 11/35。** 行列式谓词决定方向、内外与共面分类，接近零时必须使用尺度相关误差界或精确回退。

实现“3.1.6.3 INCIRCLE2D(A, B, C, D)”时，沿“统一坐标约定 → 构造几何谓词 → 表达基本图元 → 建立凸性结构 → 验证退化输入”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把坐标约定变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 3.1.6.4 INSPHERE(A, B, C, D, E)

**目录节点 12/35。** 行列式谓词决定方向、内外与共面分类，接近零时必须使用尺度相关误差界或精确回退。

实现“3.1.6.4 INSPHERE(A, B, C, D, E)”时，沿“统一坐标约定 → 构造几何谓词 → 表达基本图元 → 建立凸性结构 → 验证退化输入”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把方向谓词变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 3.2 Coordinate Systems and Points

**目录节点 13/35。** 向量点积表达投影，叉积表达有向面积，混合积表达有向体积；单位、手性和零长度都需显式处理。

实现“3.2 Coordinate Systems and Points”时，沿“统一坐标约定 → 构造几何谓词 → 表达基本图元 → 建立凸性结构 → 验证退化输入”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把重心坐标变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 3.3 Vectors

**目录节点 14/35。** 向量点积表达投影，叉积表达有向面积，混合积表达有向体积；单位、手性和零长度都需显式处理。

实现“3.3 Vectors”时，沿“统一坐标约定 → 构造几何谓词 → 表达基本图元 → 建立凸性结构 → 验证退化输入”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把凸包结构变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 3.3.1 Vector Arithmetic

**目录节点 15/35。** “3.3.1 Vector Arithmetic”位于“统一坐标约定 → 构造几何谓词 → 表达基本图元 → 建立凸性结构 → 验证退化输入”链路中；实现时必须声明输入域、退化情况、误差界、输出状态与复杂度。

实现“3.3.1 Vector Arithmetic”时，沿“统一坐标约定 → 构造几何谓词 → 表达基本图元 → 建立凸性结构 → 验证退化输入”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把退化输入变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 3.3.2 Algebraic Identities Involving Vectors

**目录节点 16/35。** 向量点积表达投影，叉积表达有向面积，混合积表达有向体积；单位、手性和零长度都需显式处理。

实现“3.3.2 Algebraic Identities Involving Vectors”时，沿“统一坐标约定 → 构造几何谓词 → 表达基本图元 → 建立凸性结构 → 验证退化输入”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把坐标约定变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 3.3.3 The Dot Product

**目录节点 17/35。** 向量点积表达投影，叉积表达有向面积，混合积表达有向体积；单位、手性和零长度都需显式处理。

实现“3.3.3 The Dot Product”时，沿“统一坐标约定 → 构造几何谓词 → 表达基本图元 → 建立凸性结构 → 验证退化输入”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把方向谓词变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 3.3.4 Algebraic Identities Involving Dot Products

**目录节点 18/35。** 向量点积表达投影，叉积表达有向面积，混合积表达有向体积；单位、手性和零长度都需显式处理。

实现“3.3.4 Algebraic Identities Involving Dot Products”时，沿“统一坐标约定 → 构造几何谓词 → 表达基本图元 → 建立凸性结构 → 验证退化输入”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把重心坐标变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 3.3.5 The Cross Product

**目录节点 19/35。** 向量点积表达投影，叉积表达有向面积，混合积表达有向体积；单位、手性和零长度都需显式处理。

实现“3.3.5 The Cross Product”时，沿“统一坐标约定 → 构造几何谓词 → 表达基本图元 → 建立凸性结构 → 验证退化输入”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把凸包结构变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 3.3.6 Algebraic Identities Involving Cross Products

**目录节点 20/35。** 向量点积表达投影，叉积表达有向面积，混合积表达有向体积；单位、手性和零长度都需显式处理。

实现“3.3.6 Algebraic Identities Involving Cross Products”时，沿“统一坐标约定 → 构造几何谓词 → 表达基本图元 → 建立凸性结构 → 验证退化输入”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把退化输入变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 3.3.7 The Scalar Triple Product

**目录节点 21/35。** 向量点积表达投影，叉积表达有向面积，混合积表达有向体积；单位、手性和零长度都需显式处理。

实现“3.3.7 The Scalar Triple Product”时，沿“统一坐标约定 → 构造几何谓词 → 表达基本图元 → 建立凸性结构 → 验证退化输入”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把坐标约定变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 3.3.8 Algebraic Identities Involving Scalar Triple Products

**目录节点 22/35。** 向量点积表达投影，叉积表达有向面积，混合积表达有向体积；单位、手性和零长度都需显式处理。

实现“3.3.8 Algebraic Identities Involving Scalar Triple Products”时，沿“统一坐标约定 → 构造几何谓词 → 表达基本图元 → 建立凸性结构 → 验证退化输入”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把方向谓词变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 3.4 Barycentric Coordinates

**目录节点 23/35。** 重心坐标把点表示为单纯形顶点的仿射组合，可同时完成最近点、插值和内外测试。

实现“3.4 Barycentric Coordinates”时，沿“统一坐标约定 → 构造几何谓词 → 表达基本图元 → 建立凸性结构 → 验证退化输入”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把重心坐标变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 3.5 Lines, Rays, and Segments

**目录节点 24/35。** 直线、射线、线段和平面共享参数方程，但参数区间和边界包含语义必须分别声明。

实现“3.5 Lines, Rays, and Segments”时，沿“统一坐标约定 → 构造几何谓词 → 表达基本图元 → 建立凸性结构 → 验证退化输入”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把凸包结构变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 3.6 Planes and Halfspaces

**目录节点 25/35。** 直线、射线、线段和平面共享参数方程，但参数区间和边界包含语义必须分别声明。

实现“3.6 Planes and Halfspaces”时，沿“统一坐标约定 → 构造几何谓词 → 表达基本图元 → 建立凸性结构 → 验证退化输入”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把退化输入变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 3.7 Polygons

**目录节点 26/35。** 凸性允许用半空间和支持点简化查询；非凸几何通常先分解或交给层次结构。

实现“3.7 Polygons”时，沿“统一坐标约定 → 构造几何谓词 → 表达基本图元 → 建立凸性结构 → 验证退化输入”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把坐标约定变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 3.7.1 Testing Polygonal Convexity

**目录节点 27/35。** 凸性允许用半空间和支持点简化查询；非凸几何通常先分解或交给层次结构。

实现“3.7.1 Testing Polygonal Convexity”时，沿“统一坐标约定 → 构造几何谓词 → 表达基本图元 → 建立凸性结构 → 验证退化输入”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把方向谓词变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 3.8 Polyhedra

**目录节点 28/35。** 凸性允许用半空间和支持点简化查询；非凸几何通常先分解或交给层次结构。

实现“3.8 Polyhedra”时，沿“统一坐标约定 → 构造几何谓词 → 表达基本图元 → 建立凸性结构 → 验证退化输入”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把重心坐标变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 3.8.1 Testing Polyhedral Convexity

**目录节点 29/35。** 凸性允许用半空间和支持点简化查询；非凸几何通常先分解或交给层次结构。

实现“3.8.1 Testing Polyhedral Convexity”时，沿“统一坐标约定 → 构造几何谓词 → 表达基本图元 → 建立凸性结构 → 验证退化输入”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把凸包结构变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 3.9 Computing Convex Hulls

**目录节点 30/35。** 凸包是包围与支持映射的基础，构建过程应删除重复点并稳定处理共线/共面集合。

实现“3.9 Computing Convex Hulls”时，沿“统一坐标约定 → 构造几何谓词 → 表达基本图元 → 建立凸性结构 → 验证退化输入”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把退化输入变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 3.9.1 Andrew’s Algorithm

**目录节点 31/35。** 凸包是包围与支持映射的基础，构建过程应删除重复点并稳定处理共线/共面集合。

实现“3.9.1 Andrew’s Algorithm”时，沿“统一坐标约定 → 构造几何谓词 → 表达基本图元 → 建立凸性结构 → 验证退化输入”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把坐标约定变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 3.9.2 The Quickhull Algorithm

**目录节点 32/35。** 凸包是包围与支持映射的基础，构建过程应删除重复点并稳定处理共线/共面集合。

实现“3.9.2 The Quickhull Algorithm”时，沿“统一坐标约定 → 构造几何谓词 → 表达基本图元 → 建立凸性结构 → 验证退化输入”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把方向谓词变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 3.10 Voronoi Regions

**目录节点 33/35。** Voronoi区域把空间按最近特征划分，是三角形最近点、V-Clip与GJK单纯形更新的共同直觉。

实现“3.10 Voronoi Regions”时，沿“统一坐标约定 → 构造几何谓词 → 表达基本图元 → 建立凸性结构 → 验证退化输入”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把重心坐标变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 3.11 Minkowski Sum and Difference

**目录节点 34/35。** Minkowski差把两个凸体相交转化为原点是否落入差集，支持映射可避免显式构造。

实现“3.11 Minkowski Sum and Difference”时，沿“统一坐标约定 → 构造几何谓词 → 表达基本图元 → 建立凸性结构 → 验证退化输入”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把凸包结构变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 3.12 Summary

**目录节点 35/35。** 小结应把“统一坐标约定 → 构造几何谓词 → 表达基本图元 → 建立凸性结构 → 验证退化输入”压缩成可检查的不变量、复杂度和失败边界。

实现“3.12 Summary”时，沿“统一坐标约定 → 构造几何谓词 → 表达基本图元 → 建立凸性结构 → 验证退化输入”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把退化输入变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

## 可迁移实现骨架

返回值要携带分类、距离/时间、特征和诊断，不要让布尔值吞掉退化状态。

```cpp
struct CollisionResult {
  enum class State { Separated, Touching, Penetrating, Degenerate } state;
  double distance;
  double toi;
  Vec3 point_a, point_b, normal;
  uint32_t iterations;
  double error_bound;
};
```

碰撞管线从稳定候选开始，精确查询可按类型分派，但所有实现共享同一边界语义。

```text
freeze transforms and geometry generations
  -> update conservative bounding volumes
  -> generate and deduplicate candidate pairs
  -> run exact static or continuous query
  -> record predicates, error bounds and first-contact state
```

机器可读证据让同一退化样本跨编译器、标量精度和优化版本比较。

```json
{
  "seed": 7319,
  "scale": 1000.0,
  "query": "closest|overlap|ray|toi",
  "expected": "touching",
  "observed": "touching",
  "errorBound": 1.2e-8
}
```

## 本单元的验收工件

第一件工件是查询合同：为“统一坐标约定 → 构造几何谓词 → 表达基本图元 → 建立凸性结构 → 验证退化输入”逐步写出输入域、坐标空间、边界包含、误差界、复杂度和返回状态。第二件是退化语料库：覆盖零长度、共线、共面、相切、近并行、巨大/微小尺度和高速跨越。第三件是目标机轨迹：记录对象数、候选对、节点访问、图元测试、迭代、缓存未命中和P50/P95/P99。第四件是差分报告：用高精度或独立实现作参照，定位第一处分歧而不是只统计最终错误率。

<Callout type="trap" title="把固定epsilon当成鲁棒性">
  单一常数无法同时适配厘米与公里尺度。容差应来自输入尺度和运算误差界，不确定时升级到区间或精确谓词。
</Callout>

<Callout type="trap" title="只测试随机一般位置">
  随机数据几乎不会命中共线、共面、相切和零尺寸。必须定向构造退化样本并在微扰两侧检查分类连续性。
</Callout>

<Callout type="trap" title="微基准更快就宣布优化成功">
  数据布局、SIMD和GPU卸载可能增加候选、同步或长尾。验收要看完整管线和真实查询分布，而不是孤立函数平均值。
</Callout>

## 先预测再运行

运行交互前先预测：对象规模翻倍时候选对、节点访问还是精确测试先增长；几何接近共面时哪一个谓词先进入不确定区；SIMD宽度增加后数据整理和尾处理是否抵消收益。观察不一致时先检查查询合同、尺度归一、缓存代际和统计口径。

## 本章回顾

Chapter 3 A Math and Geometry Primer是“统一坐标约定 → 构造几何谓词 → 表达基本图元 → 建立凸性结构 → 验证退化输入”中的完整算法合同。掌握本页意味着能逐项定位35个权威目录节点，解释坐标约定、方向谓词、重心坐标、凸包结构、退化输入的边界，复算几何与复杂度指标，并让一般、退化、压力和故障轨迹可重现。

<Exercises>

**问题 1：合同与退化。** 怎样为Chapter 3 A Math and Geometry Primer定义输入域和相切/共面语义？

<Answer>
  先声明坐标、标量、闭开边界、退化返回和误差界，再沿“统一坐标约定 → 构造几何谓词
  → 表达基本图元 → 建立凸性结构 →
  验证退化输入”构造一般位置及边界两侧样本。无法区分Separated、Touching、Penetrating和Degenerate的接口不完整。
</Answer>

**问题 2：复杂度实验。** 怎样验证坐标约定在真实查询分布下满足预算？

<Answer>
  固定目标机、构建、场景和种子，按对象数与尺度分层采样，记录候选对、节点访问、图元测试、迭代、缓存事件及P50/P95/P99，并和暴力或高精度参照核对正确性。
</Answer>

**问题 3：迁移复核。** 怎样证明Chapter 3 A Math and Geometry Primer不是只记住原书伪代码？

<Answer>
  更换数据布局、标量精度或后端实现，但保持查询合同、退化语料、误差界和证据字段；由独立复核者重放同一输入并得到一致分类与可解释性能边界。
</Answer>

</Exercises>

## 术语复核

<Glossary>
  <GlossaryItem term="查询合同">
    上层调用者对输入几何、查询类型、边界语义、输出字段和失败状态的完整约定。
  </GlossaryItem>
  <GlossaryItem term="候选对">
    用较廉价结构排除不可能相交对象后留下的待精确检测对象对。
  </GlossaryItem>
  <GlossaryItem term="几何谓词">
    依据方向、内外或区间关系做离散分类且带误差边界的计算。
  </GlossaryItem>
  <GlossaryItem term="首次接触时间">
    移动对象在给定时间区间内第一次接触时对应的参数值。
  </GlossaryItem>
  <GlossaryItem term="碰撞证据包">
    包含输入、构建、目标机、谓词轨迹、指标和最终分类的复现记录。
  </GlossaryItem>
</Glossary>

<Attribution
  adaptedFrom="Real-Time Collision Detection / 实时碰撞检测算法技术"
  adaptedUrl="https://www.oreilly.com/library/view/real-time-collision-detection/9781558607323/"
/>

## 阅读导航

- [上一页：Chapter 2 Collision Detection Design Issues](/learn/real-time-collision-detection/03-chapter-02-design-issues/rtcd-chapter-02-design-issues)
- [下一页：Chapter 4 Bounding Volumes](/learn/real-time-collision-detection/05-chapter-04-bounding-volumes/rtcd-chapter-04-bounding-volumes)
