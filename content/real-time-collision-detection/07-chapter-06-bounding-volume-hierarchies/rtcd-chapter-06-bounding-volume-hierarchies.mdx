---
title: "Chapter 6 Bounding Volume Hierarchies"
type: B
section: "实时碰撞检测算法技术（清华2010 / Morgan Kaufmann 2005）"
order: 7
description: "本章从代价函数与构建策略走到双树遍历、包围体合并、紧凑布局和时空缓存。 逐项覆盖45个权威目录坐标，并用退化几何、误差边界和目标机轨迹复核。"
demo: true
math: true
sourceUrl: "https://www.oreilly.com/library/view/real-time-collision-detection/9781558607323/"
draft: false
---


## 为什么碰撞检测必须从查询合同开始

本章从代价函数与构建策略走到双树遍历、包围体合并、紧凑布局和时空缓存。 本课程不复制原书正文、插图或配套代码，而把公开目录中的算法组织成输入合同、几何谓词、交互实验、退化样本和验收证据。本页逐项覆盖45个目录坐标：Chapter 6 Bounding Volume Hierarchies、6.1 Hierarchy Design Issues、6.1.1 Desired BVH Characteristics、6.1.2 Cost Functions、6.1.3 Tree Degree、6.2 Building Strategies for Hierarchy Construction、6.2.1 Top-down Construction、6.2.1.1 Partitioning Strategies、6.2.1.2 Choice of Partitioning Axis、6.2.1.3 Choice of Split Point、6.2.2 Bottom-up Construction、6.2.2.1 Improved Bottom-up Construction、6.2.2.2 Other Bottom-up Construction Strategies、6.2.2.3 Bottom-up n-ary Clustering Trees、6.2.3 Incremental (Insertion) Construction、6.2.3.1 The Goldsmith–Salmon Incremental Construction Method、6.3 Hierarchy Traversal、6.3.1 Descent Rules、6.3.2 Generic Informed Depth-first Traversal、6.3.3 Simultaneous Depth-first Traversal、6.3.4 Optimized Leaf-direct Depth-first Traversal、6.4 Sample Bounding Volume Hierarchies、6.4.1 OBB Trees、6.4.2 AABB Trees and BoxTrees、6.4.3 Sphere Tree Through Octree Subdivision、6.4.4 Sphere Tree from Sphere-covered Surfaces、6.4.5 Generate-and-Prune Sphere Covering、6.4.6 k-DOP Trees、6.5 Merging Bounding Volumes、6.5.1 Merging Two AABBs、6.5.2 Merging Two Spheres、6.5.3 Merging Two OBBs、6.5.4 Merging Two k-DOPs、6.6 Efficient Tree Representation and Traversal、6.6.1 Array Representation、6.6.2 Preorder Traversal Order、6.6.3 Offsets Instead of Pointers、6.6.4 Cache-friendlier Structures (Nonbinary Trees)、6.6.5 Tree Node and Primitive Ordering、6.6.6 On Recursion、6.6.7 Grouping Queries、6.7 Improved Queries Through Caching、6.7.1 Surface Caching: Caching Intersecting Primitives、6.7.2 Front Tracking、6.8 Summary。

实时碰撞检测不是“两个盒子是否相交”的单一布尔问题。上层可能需要最近距离、命中顺序、首次接触时间、穿透方向或稳定接触流形；下层又受到坐标尺度、浮点误差、非流形网格、缓存布局和候选对规模约束。只有先冻结查询类型、输入域、边界包含规则和失败状态，才能比较算法是否正确且足够快。

## 版次、目录与改编合同

本路径同时固定原版与中译本：Christer Ericson《Real-Time Collision Detection》，Morgan Kaufmann，2005，ISBN 9781558607323；刘天慧译《实时碰撞检测算法技术》，清华大学出版社，2010年6月，ISBN 9787302224112。O’Reilly授权电子书目录与Morgan Kaufmann原书目录预览列出前置资料、13章所有层级小节，以及参考文献、索引和光盘说明，共357个正式目录节点。课程按15个正式单元逐项覆盖，另设学习地图与全书复核，共17页。

原书代码与GPU接口来自2004至2005年前后的硬件和编译器语境。课程保留几何证明、数据结构和优化推理，但接口、SIMD宽度与GPU同步必须在当前目标平台重新测量；旧代码的运行结果不能替代今天的鲁棒性证据。

## 五个贯穿全书的术语

、、、、。

查询合同决定算法；候选对连接粗筛与精算；几何谓词决定拓扑分类；首次接触时间避免高速穿透；碰撞证据包则让退化场景可以回放。类名和成功截图都不能代替这些状态合同。

## 四个可复算模型

点到线段的参数投影与钳制为：

$$
t=operatorname{clamp}left(rac{(p-a)cdot(b-a)}{lVert b-a
Vert^2},0,1
ight),qquad q=a+t(b-a)
$$

两个AABB在每个轴都重叠时才相交：

$$
A_{min}^{i}le B_{max}^{i};land;B_{min}^{i}le A_{max}^{i},qquad iin{x,y,z}
$$

BVH构建和遍历可用表面积启发式比较：

$$
C=C_t+rac{S_L}{S_P}N_LC_i+rac{S_R}{S_P}N_RC_i
$$

浮点比较的容差同时包含绝对项与相对项：

$$
|a-b|le arepsilon_{abs}+arepsilon_{rel}max(|a|,|b|)
$$

最近点必须处理零长度线段；AABB边界要明确相切是否算碰撞；SAH只是查询分布近似；容差不能脱离几何尺度。每项公式都要通过一般、退化和极端尺度样本复算。

## 分步可视化：结构、规模与故障


### Chapter 6 Bounding Volume Hierarchies

**目录节点 1/45。** 本章从代价函数与构建策略走到双树遍历、包围体合并、紧凑布局和时空缓存。

实现“Chapter 6 Bounding Volume Hierarchies”时，沿“定义层次代价 → 构建树结构 → 执行双树遍历 → 压紧节点布局 → 利用时空一致性”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把层次代价变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 6.1 Hierarchy Design Issues

**目录节点 2/45。** BVH代价函数组合节点访问概率、包围体测试和叶图元成本；构建质量必须用真实查询分布复核。

实现“6.1 Hierarchy Design Issues”时，沿“定义层次代价 → 构建树结构 → 执行双树遍历 → 压紧节点布局 → 利用时空一致性”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把构建策略变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 6.1.1 Desired BVH Characteristics

**目录节点 3/45。** BVH代价函数组合节点访问概率、包围体测试和叶图元成本；构建质量必须用真实查询分布复核。

实现“6.1.1 Desired BVH Characteristics”时，沿“定义层次代价 → 构建树结构 → 执行双树遍历 → 压紧节点布局 → 利用时空一致性”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把双树遍历变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 6.1.2 Cost Functions

**目录节点 4/45。** BVH代价函数组合节点访问概率、包围体测试和叶图元成本；构建质量必须用真实查询分布复核。

实现“6.1.2 Cost Functions”时，沿“定义层次代价 → 构建树结构 → 执行双树遍历 → 压紧节点布局 → 利用时空一致性”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把紧凑布局变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 6.1.3 Tree Degree

**目录节点 5/45。** “6.1.3 Tree Degree”位于“定义层次代价 → 构建树结构 → 执行双树遍历 → 压紧节点布局 → 利用时空一致性”链路中；实现时必须声明输入域、退化情况、误差界、输出状态与复杂度。

实现“6.1.3 Tree Degree”时，沿“定义层次代价 → 构建树结构 → 执行双树遍历 → 压紧节点布局 → 利用时空一致性”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把时空缓存变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 6.2 Building Strategies for Hierarchy Construction

**目录节点 6/45。** “6.2 Building Strategies for Hierarchy Construction”位于“定义层次代价 → 构建树结构 → 执行双树遍历 → 压紧节点布局 → 利用时空一致性”链路中；实现时必须声明输入域、退化情况、误差界、输出状态与复杂度。

实现“6.2 Building Strategies for Hierarchy Construction”时，沿“定义层次代价 → 构建树结构 → 执行双树遍历 → 压紧节点布局 → 利用时空一致性”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把层次代价变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 6.2.1 Top-down Construction

**目录节点 7/45。** 自顶向下构建按轴和分割点划分图元，需控制空子树、深度与表面积代价。

实现“6.2.1 Top-down Construction”时，沿“定义层次代价 → 构建树结构 → 执行双树遍历 → 压紧节点布局 → 利用时空一致性”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把构建策略变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 6.2.1.1 Partitioning Strategies

**目录节点 8/45。** 自顶向下构建按轴和分割点划分图元，需控制空子树、深度与表面积代价。

实现“6.2.1.1 Partitioning Strategies”时，沿“定义层次代价 → 构建树结构 → 执行双树遍历 → 压紧节点布局 → 利用时空一致性”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把双树遍历变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 6.2.1.2 Choice of Partitioning Axis

**目录节点 9/45。** 自顶向下构建按轴和分割点划分图元，需控制空子树、深度与表面积代价。

实现“6.2.1.2 Choice of Partitioning Axis”时，沿“定义层次代价 → 构建树结构 → 执行双树遍历 → 压紧节点布局 → 利用时空一致性”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把紧凑布局变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 6.2.1.3 Choice of Split Point

**目录节点 10/45。** 自顶向下构建按轴和分割点划分图元，需控制空子树、深度与表面积代价。

实现“6.2.1.3 Choice of Split Point”时，沿“定义层次代价 → 构建树结构 → 执行双树遍历 → 压紧节点布局 → 利用时空一致性”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把时空缓存变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 6.2.2 Bottom-up Construction

**目录节点 11/45。** 自底向上聚类反复合并低代价邻居，质量较高但构建成本与候选合并搜索更大。

实现“6.2.2 Bottom-up Construction”时，沿“定义层次代价 → 构建树结构 → 执行双树遍历 → 压紧节点布局 → 利用时空一致性”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把层次代价变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 6.2.2.1 Improved Bottom-up Construction

**目录节点 12/45。** 自底向上聚类反复合并低代价邻居，质量较高但构建成本与候选合并搜索更大。

实现“6.2.2.1 Improved Bottom-up Construction”时，沿“定义层次代价 → 构建树结构 → 执行双树遍历 → 压紧节点布局 → 利用时空一致性”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把构建策略变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 6.2.2.2 Other Bottom-up Construction Strategies

**目录节点 13/45。** 自底向上聚类反复合并低代价邻居，质量较高但构建成本与候选合并搜索更大。

实现“6.2.2.2 Other Bottom-up Construction Strategies”时，沿“定义层次代价 → 构建树结构 → 执行双树遍历 → 压紧节点布局 → 利用时空一致性”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把双树遍历变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 6.2.2.3 Bottom-up n-ary Clustering Trees

**目录节点 14/45。** 自底向上聚类反复合并低代价邻居，质量较高但构建成本与候选合并搜索更大。

实现“6.2.2.3 Bottom-up n-ary Clustering Trees”时，沿“定义层次代价 → 构建树结构 → 执行双树遍历 → 压紧节点布局 → 利用时空一致性”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把紧凑布局变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 6.2.3 Incremental (Insertion) Construction

**目录节点 15/45。** 增量构建沿最小代价路径插入叶并向上重拟合，动态场景还需旋转或重建抑制退化。

实现“6.2.3 Incremental (Insertion) Construction”时，沿“定义层次代价 → 构建树结构 → 执行双树遍历 → 压紧节点布局 → 利用时空一致性”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把时空缓存变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 6.2.3.1 The Goldsmith–Salmon Incremental Construction Method

**目录节点 16/45。** 增量构建沿最小代价路径插入叶并向上重拟合，动态场景还需旋转或重建抑制退化。

实现“6.2.3.1 The Goldsmith–Salmon Incremental Construction Method”时，沿“定义层次代价 → 构建树结构 → 执行双树遍历 → 压紧节点布局 → 利用时空一致性”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把层次代价变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 6.3 Hierarchy Traversal

**目录节点 17/45。** 层次遍历先测父包围体再选择子节点顺序，距离或射线查询应以当前最优界剪枝。

实现“6.3 Hierarchy Traversal”时，沿“定义层次代价 → 构建树结构 → 执行双树遍历 → 压紧节点布局 → 利用时空一致性”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把构建策略变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 6.3.1 Descent Rules

**目录节点 18/45。** 层次遍历先测父包围体再选择子节点顺序，距离或射线查询应以当前最优界剪枝。

实现“6.3.1 Descent Rules”时，沿“定义层次代价 → 构建树结构 → 执行双树遍历 → 压紧节点布局 → 利用时空一致性”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把双树遍历变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 6.3.2 Generic Informed Depth-first Traversal

**目录节点 19/45。** 层次遍历先测父包围体再选择子节点顺序，距离或射线查询应以当前最优界剪枝。

实现“6.3.2 Generic Informed Depth-first Traversal”时，沿“定义层次代价 → 构建树结构 → 执行双树遍历 → 压紧节点布局 → 利用时空一致性”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把紧凑布局变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 6.3.3 Simultaneous Depth-first Traversal

**目录节点 20/45。** 层次遍历先测父包围体再选择子节点顺序，距离或射线查询应以当前最优界剪枝。

实现“6.3.3 Simultaneous Depth-first Traversal”时，沿“定义层次代价 → 构建树结构 → 执行双树遍历 → 压紧节点布局 → 利用时空一致性”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把时空缓存变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 6.3.4 Optimized Leaf-direct Depth-first Traversal

**目录节点 21/45。** 层次遍历先测父包围体再选择子节点顺序，距离或射线查询应以当前最优界剪枝。

实现“6.3.4 Optimized Leaf-direct Depth-first Traversal”时，沿“定义层次代价 → 构建树结构 → 执行双树遍历 → 压紧节点布局 → 利用时空一致性”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把层次代价变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 6.4 Sample Bounding Volume Hierarchies

**目录节点 22/45。** “6.4 Sample Bounding Volume Hierarchies”位于“定义层次代价 → 构建树结构 → 执行双树遍历 → 压紧节点布局 → 利用时空一致性”链路中；实现时必须声明输入域、退化情况、误差界、输出状态与复杂度。

实现“6.4 Sample Bounding Volume Hierarchies”时，沿“定义层次代价 → 构建树结构 → 执行双树遍历 → 压紧节点布局 → 利用时空一致性”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把构建策略变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 6.4.1 OBB Trees

**目录节点 23/45。** OBB通过局部轴提高紧致性，重叠使用15个候选分离轴；近平行轴需要稳定绝对旋转矩阵。

实现“6.4.1 OBB Trees”时，沿“定义层次代价 → 构建树结构 → 执行双树遍历 → 压紧节点布局 → 利用时空一致性”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把双树遍历变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 6.4.2 AABB Trees and BoxTrees

**目录节点 24/45。** AABB以逐轴区间重叠实现廉价测试；旋转对象更新需在重算紧致性与保守变换成本之间取舍。

实现“6.4.2 AABB Trees and BoxTrees”时，沿“定义层次代价 → 构建树结构 → 执行双树遍历 → 压紧节点布局 → 利用时空一致性”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把紧凑布局变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 6.4.3 Sphere Tree Through Octree Subdivision

**目录节点 25/45。** 球体旋转不变且测试廉价，但细长物体紧致性差；拟合算法应报告半径冗余与更新时间。

实现“6.4.3 Sphere Tree Through Octree Subdivision”时，沿“定义层次代价 → 构建树结构 → 执行双树遍历 → 压紧节点布局 → 利用时空一致性”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把时空缓存变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 6.4.4 Sphere Tree from Sphere-covered Surfaces

**目录节点 26/45。** 球体旋转不变且测试廉价，但细长物体紧致性差；拟合算法应报告半径冗余与更新时间。

实现“6.4.4 Sphere Tree from Sphere-covered Surfaces”时，沿“定义层次代价 → 构建树结构 → 执行双树遍历 → 压紧节点布局 → 利用时空一致性”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把层次代价变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 6.4.5 Generate-and-Prune Sphere Covering

**目录节点 27/45。** 固定载体、版次、权利页和系列坐标，防止把不同印次或二手摘要混作权威目录。

实现“6.4.5 Generate-and-Prune Sphere Covering”时，沿“定义层次代价 → 构建树结构 → 执行双树遍历 → 压紧节点布局 → 利用时空一致性”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把构建策略变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 6.4.6 k-DOP Trees

**目录节点 28/45。** k-DOP在固定方向集合上保存投影区间，以更多标量换取比AABB更紧的包围。

实现“6.4.6 k-DOP Trees”时，沿“定义层次代价 → 构建树结构 → 执行双树遍历 → 压紧节点布局 → 利用时空一致性”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把双树遍历变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 6.5 Merging Bounding Volumes

**目录节点 29/45。** 合并包围体必须保守包含两个子体，并在体积/面积膨胀和计算成本之间取舍。

实现“6.5 Merging Bounding Volumes”时，沿“定义层次代价 → 构建树结构 → 执行双树遍历 → 压紧节点布局 → 利用时空一致性”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把紧凑布局变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 6.5.1 Merging Two AABBs

**目录节点 30/45。** AABB以逐轴区间重叠实现廉价测试；旋转对象更新需在重算紧致性与保守变换成本之间取舍。

实现“6.5.1 Merging Two AABBs”时，沿“定义层次代价 → 构建树结构 → 执行双树遍历 → 压紧节点布局 → 利用时空一致性”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把时空缓存变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 6.5.2 Merging Two Spheres

**目录节点 31/45。** 球体旋转不变且测试廉价，但细长物体紧致性差；拟合算法应报告半径冗余与更新时间。

实现“6.5.2 Merging Two Spheres”时，沿“定义层次代价 → 构建树结构 → 执行双树遍历 → 压紧节点布局 → 利用时空一致性”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把层次代价变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 6.5.3 Merging Two OBBs

**目录节点 32/45。** OBB通过局部轴提高紧致性，重叠使用15个候选分离轴；近平行轴需要稳定绝对旋转矩阵。

实现“6.5.3 Merging Two OBBs”时，沿“定义层次代价 → 构建树结构 → 执行双树遍历 → 压紧节点布局 → 利用时空一致性”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把构建策略变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 6.5.4 Merging Two k-DOPs

**目录节点 33/45。** k-DOP在固定方向集合上保存投影区间，以更多标量换取比AABB更紧的包围。

实现“6.5.4 Merging Two k-DOPs”时，沿“定义层次代价 → 构建树结构 → 执行双树遍历 → 压紧节点布局 → 利用时空一致性”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把双树遍历变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 6.6 Efficient Tree Representation and Traversal

**目录节点 34/45。** 层次遍历先测父包围体再选择子节点顺序，距离或射线查询应以当前最优界剪枝。

实现“6.6 Efficient Tree Representation and Traversal”时，沿“定义层次代价 → 构建树结构 → 执行双树遍历 → 压紧节点布局 → 利用时空一致性”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把紧凑布局变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 6.6.1 Array Representation

**目录节点 35/45。** 紧凑数组、预序布局与相对偏移减少指针和缓存未命中；遍历栈上限必须可证明。

实现“6.6.1 Array Representation”时，沿“定义层次代价 → 构建树结构 → 执行双树遍历 → 压紧节点布局 → 利用时空一致性”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把时空缓存变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 6.6.2 Preorder Traversal Order

**目录节点 36/45。** 层次遍历先测父包围体再选择子节点顺序，距离或射线查询应以当前最优界剪枝。

实现“6.6.2 Preorder Traversal Order”时，沿“定义层次代价 → 构建树结构 → 执行双树遍历 → 压紧节点布局 → 利用时空一致性”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把层次代价变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 6.6.3 Offsets Instead of Pointers

**目录节点 37/45。** 紧凑数组、预序布局与相对偏移减少指针和缓存未命中；遍历栈上限必须可证明。

实现“6.6.3 Offsets Instead of Pointers”时，沿“定义层次代价 → 构建树结构 → 执行双树遍历 → 压紧节点布局 → 利用时空一致性”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把构建策略变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 6.6.4 Cache-friendlier Structures (Nonbinary Trees)

**目录节点 38/45。** “6.6.4 Cache-friendlier Structures (Nonbinary Trees)”位于“定义层次代价 → 构建树结构 → 执行双树遍历 → 压紧节点布局 → 利用时空一致性”链路中；实现时必须声明输入域、退化情况、误差界、输出状态与复杂度。

实现“6.6.4 Cache-friendlier Structures (Nonbinary Trees)”时，沿“定义层次代价 → 构建树结构 → 执行双树遍历 → 压紧节点布局 → 利用时空一致性”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把双树遍历变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 6.6.5 Tree Node and Primitive Ordering

**目录节点 39/45。** 紧凑数组、预序布局与相对偏移减少指针和缓存未命中；遍历栈上限必须可证明。

实现“6.6.5 Tree Node and Primitive Ordering”时，沿“定义层次代价 → 构建树结构 → 执行双树遍历 → 压紧节点布局 → 利用时空一致性”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把紧凑布局变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 6.6.6 On Recursion

**目录节点 40/45。** 紧凑数组、预序布局与相对偏移减少指针和缓存未命中；遍历栈上限必须可证明。

实现“6.6.6 On Recursion”时，沿“定义层次代价 → 构建树结构 → 执行双树遍历 → 压紧节点布局 → 利用时空一致性”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把时空缓存变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 6.6.7 Grouping Queries

**目录节点 41/45。** “6.6.7 Grouping Queries”位于“定义层次代价 → 构建树结构 → 执行双树遍历 → 压紧节点布局 → 利用时空一致性”链路中；实现时必须声明输入域、退化情况、误差界、输出状态与复杂度。

实现“6.6.7 Grouping Queries”时，沿“定义层次代价 → 构建树结构 → 执行双树遍历 → 压紧节点布局 → 利用时空一致性”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把层次代价变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 6.7 Improved Queries Through Caching

**目录节点 42/45。** “6.7 Improved Queries Through Caching”位于“定义层次代价 → 构建树结构 → 执行双树遍历 → 压紧节点布局 → 利用时空一致性”链路中；实现时必须声明输入域、退化情况、误差界、输出状态与复杂度。

实现“6.7 Improved Queries Through Caching”时，沿“定义层次代价 → 构建树结构 → 执行双树遍历 → 压紧节点布局 → 利用时空一致性”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把构建策略变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 6.7.1 Surface Caching: Caching Intersecting Primitives

**目录节点 43/45。** 时空缓存从上一帧相交特征或树前沿开始，但每次使用都要验证对象代际和拓扑版本。

实现“6.7.1 Surface Caching: Caching Intersecting Primitives”时，沿“定义层次代价 → 构建树结构 → 执行双树遍历 → 压紧节点布局 → 利用时空一致性”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把双树遍历变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 6.7.2 Front Tracking

**目录节点 44/45。** 时空缓存从上一帧相交特征或树前沿开始，但每次使用都要验证对象代际和拓扑版本。

实现“6.7.2 Front Tracking”时，沿“定义层次代价 → 构建树结构 → 执行双树遍历 → 压紧节点布局 → 利用时空一致性”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把紧凑布局变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 6.8 Summary

**目录节点 45/45。** 小结应把“定义层次代价 → 构建树结构 → 执行双树遍历 → 压紧节点布局 → 利用时空一致性”压缩成可检查的不变量、复杂度和失败边界。

实现“6.8 Summary”时，沿“定义层次代价 → 构建树结构 → 执行双树遍历 → 压紧节点布局 → 利用时空一致性”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把时空缓存变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

## 可迁移实现骨架

返回值要携带分类、距离/时间、特征和诊断，不要让布尔值吞掉退化状态。

```cpp
struct CollisionResult {
  enum class State { Separated, Touching, Penetrating, Degenerate } state;
  double distance;
  double toi;
  Vec3 point_a, point_b, normal;
  uint32_t iterations;
  double error_bound;
};
```

碰撞管线从稳定候选开始，精确查询可按类型分派，但所有实现共享同一边界语义。

```text
freeze transforms and geometry generations
  -> update conservative bounding volumes
  -> generate and deduplicate candidate pairs
  -> run exact static or continuous query
  -> record predicates, error bounds and first-contact state
```

机器可读证据让同一退化样本跨编译器、标量精度和优化版本比较。

```json
{
  "seed": 7319,
  "scale": 1000.0,
  "query": "closest|overlap|ray|toi",
  "expected": "touching",
  "observed": "touching",
  "errorBound": 1.2e-8
}
```

## 本单元的验收工件

第一件工件是查询合同：为“定义层次代价 → 构建树结构 → 执行双树遍历 → 压紧节点布局 → 利用时空一致性”逐步写出输入域、坐标空间、边界包含、误差界、复杂度和返回状态。第二件是退化语料库：覆盖零长度、共线、共面、相切、近并行、巨大/微小尺度和高速跨越。第三件是目标机轨迹：记录对象数、候选对、节点访问、图元测试、迭代、缓存未命中和P50/P95/P99。第四件是差分报告：用高精度或独立实现作参照，定位第一处分歧而不是只统计最终错误率。


## 先预测再运行

运行交互前先预测：对象规模翻倍时候选对、节点访问还是精确测试先增长；几何接近共面时哪一个谓词先进入不确定区；SIMD宽度增加后数据整理和尾处理是否抵消收益。观察不一致时先检查查询合同、尺度归一、缓存代际和统计口径。

## 本章回顾

Chapter 6 Bounding Volume Hierarchies是“定义层次代价 → 构建树结构 → 执行双树遍历 → 压紧节点布局 → 利用时空一致性”中的完整算法合同。掌握本页意味着能逐项定位45个权威目录节点，解释层次代价、构建策略、双树遍历、紧凑布局、时空缓存的边界，复算几何与复杂度指标，并让一般、退化、压力和故障轨迹可重现。


## 术语复核


## 阅读导航

- [上一页：Chapter 5 Basic Primitive Tests](/learn/real-time-collision-detection/06-chapter-05-basic-primitive-tests/rtcd-chapter-05-basic-primitive-tests)
- [下一页：Chapter 7 Spatial Partitioning](/learn/real-time-collision-detection/08-chapter-07-spatial-partitioning/rtcd-chapter-07-spatial-partitioning)
