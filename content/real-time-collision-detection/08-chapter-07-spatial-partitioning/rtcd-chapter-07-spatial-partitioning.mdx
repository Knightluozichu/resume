---
title: "Chapter 7 Spatial Partitioning"
type: B
section: "实时碰撞检测算法技术（清华2010 / Morgan Kaufmann 2005）"
order: 8
description: "本章比较均匀/层次网格、八叉树、k-d树、扫掠排序、单元门户和去重策略。 逐项覆盖36个权威目录坐标，并用退化几何、误差边界和目标机轨迹复核。"
demo: true
math: true
sourceUrl: "https://www.oreilly.com/library/view/real-time-collision-detection/9781558607323/"
draft: false
---


## 为什么碰撞检测必须从查询合同开始

本章比较均匀/层次网格、八叉树、k-d树、扫掠排序、单元门户和去重策略。 本课程不复制原书正文、插图或配套代码，而把公开目录中的算法组织成输入合同、几何谓词、交互实验、退化样本和验收证据。本页逐项覆盖36个目录坐标：Chapter 7 Spatial Partitioning、7.1 Uniform Grids、7.1.1 Cell Size Issues、7.1.2 Grids as Arrays of Linked Lists、7.1.3 Hashed Storage and Infinite Grids、7.1.4 Storing Static Data、7.1.5 Implicit Grids、7.1.6 Uniform Grid Object-Object Test、7.1.6.1 One Test at a Time、7.1.6.2 All Tests at a Time、7.1.7 Additional Grid Considerations、7.2 Hierarchical Grids、7.2.1 Basic Hgrid Implementation、7.2.2 Alternative Hierarchical Grid Representations、7.2.3 Other Hierarchical Grids、7.3 Trees、7.3.1 Octrees (and Quadtrees)、7.3.2 Octree Object Assignment、7.3.3 Locational Codes and Finding the Octant for a Point、7.3.4 Linear Octrees (Hash-based)、7.3.5 Computing the Morton Key、7.3.6 Loose Octrees、7.3.7 k-d Trees、7.3.8 Hybrid Schemes、7.4 Ray and Directed Line Segment Traversals、7.4.1 k-d Tree Intersection Test、7.4.2 Uniform Grid Intersection Test、7.5 Sort and Sweep Methods、7.5.1 Sorted Linked-list Implementation、7.5.2 Array-based Sorting、7.6 Cells and Portals、7.7 Avoiding Retesting、7.7.1 Bit Flags、7.7.2 Time Stamping、7.7.3 Amortized Time Stamp Clearing、7.8 Summary。

实时碰撞检测不是“两个盒子是否相交”的单一布尔问题。上层可能需要最近距离、命中顺序、首次接触时间、穿透方向或稳定接触流形；下层又受到坐标尺度、浮点误差、非流形网格、缓存布局和候选对规模约束。只有先冻结查询类型、输入域、边界包含规则和失败状态，才能比较算法是否正确且足够快。

## 版次、目录与改编合同

本路径同时固定原版与中译本：Christer Ericson《Real-Time Collision Detection》，Morgan Kaufmann，2005，ISBN 9781558607323；刘天慧译《实时碰撞检测算法技术》，清华大学出版社，2010年6月，ISBN 9787302224112。O’Reilly授权电子书目录与Morgan Kaufmann原书目录预览列出前置资料、13章所有层级小节，以及参考文献、索引和光盘说明，共357个正式目录节点。课程按15个正式单元逐项覆盖，另设学习地图与全书复核，共17页。

原书代码与GPU接口来自2004至2005年前后的硬件和编译器语境。课程保留几何证明、数据结构和优化推理，但接口、SIMD宽度与GPU同步必须在当前目标平台重新测量；旧代码的运行结果不能替代今天的鲁棒性证据。

## 五个贯穿全书的术语

、、、、。

查询合同决定算法；候选对连接粗筛与精算；几何谓词决定拓扑分类；首次接触时间避免高速穿透；碰撞证据包则让退化场景可以回放。类名和成功截图都不能代替这些状态合同。

## 四个可复算模型

点到线段的参数投影与钳制为：

$$
t=operatorname{clamp}left(rac{(p-a)cdot(b-a)}{lVert b-a
Vert^2},0,1
ight),qquad q=a+t(b-a)
$$

两个AABB在每个轴都重叠时才相交：

$$
A_{min}^{i}le B_{max}^{i};land;B_{min}^{i}le A_{max}^{i},qquad iin{x,y,z}
$$

BVH构建和遍历可用表面积启发式比较：

$$
C=C_t+rac{S_L}{S_P}N_LC_i+rac{S_R}{S_P}N_RC_i
$$

浮点比较的容差同时包含绝对项与相对项：

$$
|a-b|le arepsilon_{abs}+arepsilon_{rel}max(|a|,|b|)
$$

最近点必须处理零长度线段；AABB边界要明确相切是否算碰撞；SAH只是查询分布近似；容差不能脱离几何尺度。每项公式都要通过一般、退化和极端尺度样本复算。

## 分步可视化：结构、规模与故障


### Chapter 7 Spatial Partitioning

**目录节点 1/36。** 自顶向下构建按轴和分割点划分图元，需控制空子树、深度与表面积代价。

实现“Chapter 7 Spatial Partitioning”时，沿“选择空间尺度 → 分配对象到单元 → 遍历查询路径 → 生成候选对 → 消除重复测试”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把空间尺度变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 7.1 Uniform Grids

**目录节点 2/36。** 均匀网格的单元尺度决定占用与候选数量；大小差异显著时需要层次网格或多结构混合。

实现“7.1 Uniform Grids”时，沿“选择空间尺度 → 分配对象到单元 → 遍历查询路径 → 生成候选对 → 消除重复测试”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把对象分桶变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 7.1.1 Cell Size Issues

**目录节点 3/36。** 均匀网格的单元尺度决定占用与候选数量；大小差异显著时需要层次网格或多结构混合。

实现“7.1.1 Cell Size Issues”时，沿“选择空间尺度 → 分配对象到单元 → 遍历查询路径 → 生成候选对 → 消除重复测试”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把查询遍历变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 7.1.2 Grids as Arrays of Linked Lists

**目录节点 4/36。** “7.1.2 Grids as Arrays of Linked Lists”位于“选择空间尺度 → 分配对象到单元 → 遍历查询路径 → 生成候选对 → 消除重复测试”链路中；实现时必须声明输入域、退化情况、误差界、输出状态与复杂度。

实现“7.1.2 Grids as Arrays of Linked Lists”时，沿“选择空间尺度 → 分配对象到单元 → 遍历查询路径 → 生成候选对 → 消除重复测试”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把候选对变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 7.1.3 Hashed Storage and Infinite Grids

**目录节点 5/36。** 哈希网格只存活跃单元，键必须稳定处理负坐标与碰撞，链表或桶还要限制最坏长度。

实现“7.1.3 Hashed Storage and Infinite Grids”时，沿“选择空间尺度 → 分配对象到单元 → 遍历查询路径 → 生成候选对 → 消除重复测试”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把测试去重变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 7.1.4 Storing Static Data

**目录节点 6/36。** “7.1.4 Storing Static Data”位于“选择空间尺度 → 分配对象到单元 → 遍历查询路径 → 生成候选对 → 消除重复测试”链路中；实现时必须声明输入域、退化情况、误差界、输出状态与复杂度。

实现“7.1.4 Storing Static Data”时，沿“选择空间尺度 → 分配对象到单元 → 遍历查询路径 → 生成候选对 → 消除重复测试”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把空间尺度变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 7.1.5 Implicit Grids

**目录节点 7/36。** 均匀网格的单元尺度决定占用与候选数量；大小差异显著时需要层次网格或多结构混合。

实现“7.1.5 Implicit Grids”时，沿“选择空间尺度 → 分配对象到单元 → 遍历查询路径 → 生成候选对 → 消除重复测试”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把对象分桶变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 7.1.6 Uniform Grid Object-Object Test

**目录节点 8/36。** “7.1.6 Uniform Grid Object-Object Test”位于“选择空间尺度 → 分配对象到单元 → 遍历查询路径 → 生成候选对 → 消除重复测试”链路中；实现时必须声明输入域、退化情况、误差界、输出状态与复杂度。

实现“7.1.6 Uniform Grid Object-Object Test”时，沿“选择空间尺度 → 分配对象到单元 → 遍历查询路径 → 生成候选对 → 消除重复测试”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把查询遍历变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 7.1.6.1 One Test at a Time

**目录节点 9/36。** “7.1.6.1 One Test at a Time”位于“选择空间尺度 → 分配对象到单元 → 遍历查询路径 → 生成候选对 → 消除重复测试”链路中；实现时必须声明输入域、退化情况、误差界、输出状态与复杂度。

实现“7.1.6.1 One Test at a Time”时，沿“选择空间尺度 → 分配对象到单元 → 遍历查询路径 → 生成候选对 → 消除重复测试”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把候选对变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 7.1.6.2 All Tests at a Time

**目录节点 10/36。** “7.1.6.2 All Tests at a Time”位于“选择空间尺度 → 分配对象到单元 → 遍历查询路径 → 生成候选对 → 消除重复测试”链路中；实现时必须声明输入域、退化情况、误差界、输出状态与复杂度。

实现“7.1.6.2 All Tests at a Time”时，沿“选择空间尺度 → 分配对象到单元 → 遍历查询路径 → 生成候选对 → 消除重复测试”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把测试去重变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 7.1.7 Additional Grid Considerations

**目录节点 11/36。** “7.1.7 Additional Grid Considerations”位于“选择空间尺度 → 分配对象到单元 → 遍历查询路径 → 生成候选对 → 消除重复测试”链路中；实现时必须声明输入域、退化情况、误差界、输出状态与复杂度。

实现“7.1.7 Additional Grid Considerations”时，沿“选择空间尺度 → 分配对象到单元 → 遍历查询路径 → 生成候选对 → 消除重复测试”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把空间尺度变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 7.2 Hierarchical Grids

**目录节点 12/36。** 层次网格按对象尺寸选择层级，在少量邻域生成候选，并用时间戳避免跨层重复。

实现“7.2 Hierarchical Grids”时，沿“选择空间尺度 → 分配对象到单元 → 遍历查询路径 → 生成候选对 → 消除重复测试”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把对象分桶变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 7.2.1 Basic Hgrid Implementation

**目录节点 13/36。** 层次网格按对象尺寸选择层级，在少量邻域生成候选，并用时间戳避免跨层重复。

实现“7.2.1 Basic Hgrid Implementation”时，沿“选择空间尺度 → 分配对象到单元 → 遍历查询路径 → 生成候选对 → 消除重复测试”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把查询遍历变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 7.2.2 Alternative Hierarchical Grid Representations

**目录节点 14/36。** “7.2.2 Alternative Hierarchical Grid Representations”位于“选择空间尺度 → 分配对象到单元 → 遍历查询路径 → 生成候选对 → 消除重复测试”链路中；实现时必须声明输入域、退化情况、误差界、输出状态与复杂度。

实现“7.2.2 Alternative Hierarchical Grid Representations”时，沿“选择空间尺度 → 分配对象到单元 → 遍历查询路径 → 生成候选对 → 消除重复测试”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把候选对变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 7.2.3 Other Hierarchical Grids

**目录节点 15/36。** 层次网格按对象尺寸选择层级，在少量邻域生成候选，并用时间戳避免跨层重复。

实现“7.2.3 Other Hierarchical Grids”时，沿“选择空间尺度 → 分配对象到单元 → 遍历查询路径 → 生成候选对 → 消除重复测试”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把测试去重变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 7.3 Trees

**目录节点 16/36。** “7.3 Trees”位于“选择空间尺度 → 分配对象到单元 → 遍历查询路径 → 生成候选对 → 消除重复测试”链路中；实现时必须声明输入域、退化情况、误差界、输出状态与复杂度。

实现“7.3 Trees”时，沿“选择空间尺度 → 分配对象到单元 → 遍历查询路径 → 生成候选对 → 消除重复测试”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把空间尺度变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 7.3.1 Octrees (and Quadtrees)

**目录节点 17/36。** 八叉树用空间码和层级定位节点；松散边界减少频繁迁移，但会增加候选重叠。

实现“7.3.1 Octrees (and Quadtrees)”时，沿“选择空间尺度 → 分配对象到单元 → 遍历查询路径 → 生成候选对 → 消除重复测试”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把对象分桶变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 7.3.2 Octree Object Assignment

**目录节点 18/36。** 八叉树用空间码和层级定位节点；松散边界减少频繁迁移，但会增加候选重叠。

实现“7.3.2 Octree Object Assignment”时，沿“选择空间尺度 → 分配对象到单元 → 遍历查询路径 → 生成候选对 → 消除重复测试”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把查询遍历变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 7.3.3 Locational Codes and Finding the Octant for a Point

**目录节点 19/36。** 八叉树用空间码和层级定位节点；松散边界减少频繁迁移，但会增加候选重叠。

实现“7.3.3 Locational Codes and Finding the Octant for a Point”时，沿“选择空间尺度 → 分配对象到单元 → 遍历查询路径 → 生成候选对 → 消除重复测试”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把候选对变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 7.3.4 Linear Octrees (Hash-based)

**目录节点 20/36。** 八叉树用空间码和层级定位节点；松散边界减少频繁迁移，但会增加候选重叠。

实现“7.3.4 Linear Octrees (Hash-based)”时，沿“选择空间尺度 → 分配对象到单元 → 遍历查询路径 → 生成候选对 → 消除重复测试”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把测试去重变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 7.3.5 Computing the Morton Key

**目录节点 21/36。** 八叉树用空间码和层级定位节点；松散边界减少频繁迁移，但会增加候选重叠。

实现“7.3.5 Computing the Morton Key”时，沿“选择空间尺度 → 分配对象到单元 → 遍历查询路径 → 生成候选对 → 消除重复测试”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把空间尺度变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 7.3.6 Loose Octrees

**目录节点 22/36。** 八叉树用空间码和层级定位节点；松散边界减少频繁迁移，但会增加候选重叠。

实现“7.3.6 Loose Octrees”时，沿“选择空间尺度 → 分配对象到单元 → 遍历查询路径 → 生成候选对 → 消除重复测试”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把对象分桶变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 7.3.7 k-d Trees

**目录节点 23/36。** k-d树按轴平面划分空间，射线遍历用参数区间决定近远子树并以最近命中剪枝。

实现“7.3.7 k-d Trees”时，沿“选择空间尺度 → 分配对象到单元 → 遍历查询路径 → 生成候选对 → 消除重复测试”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把查询遍历变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 7.3.8 Hybrid Schemes

**目录节点 24/36。** “7.3.8 Hybrid Schemes”位于“选择空间尺度 → 分配对象到单元 → 遍历查询路径 → 生成候选对 → 消除重复测试”链路中；实现时必须声明输入域、退化情况、误差界、输出状态与复杂度。

实现“7.3.8 Hybrid Schemes”时，沿“选择空间尺度 → 分配对象到单元 → 遍历查询路径 → 生成候选对 → 消除重复测试”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把候选对变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 7.4 Ray and Directed Line Segment Traversals

**目录节点 25/36。** 层次遍历先测父包围体再选择子节点顺序，距离或射线查询应以当前最优界剪枝。

实现“7.4 Ray and Directed Line Segment Traversals”时，沿“选择空间尺度 → 分配对象到单元 → 遍历查询路径 → 生成候选对 → 消除重复测试”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把测试去重变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 7.4.1 k-d Tree Intersection Test

**目录节点 26/36。** k-d树按轴平面划分空间，射线遍历用参数区间决定近远子树并以最近命中剪枝。

实现“7.4.1 k-d Tree Intersection Test”时，沿“选择空间尺度 → 分配对象到单元 → 遍历查询路径 → 生成候选对 → 消除重复测试”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把空间尺度变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 7.4.2 Uniform Grid Intersection Test

**目录节点 27/36。** “7.4.2 Uniform Grid Intersection Test”位于“选择空间尺度 → 分配对象到单元 → 遍历查询路径 → 生成候选对 → 消除重复测试”链路中；实现时必须声明输入域、退化情况、误差界、输出状态与复杂度。

实现“7.4.2 Uniform Grid Intersection Test”时，沿“选择空间尺度 → 分配对象到单元 → 遍历查询路径 → 生成候选对 → 消除重复测试”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把对象分桶变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 7.5 Sort and Sweep Methods

**目录节点 28/36。** 扫掠排序利用端点时空一致性维护轴序，只在区间开始/结束事件中更新候选对。

实现“7.5 Sort and Sweep Methods”时，沿“选择空间尺度 → 分配对象到单元 → 遍历查询路径 → 生成候选对 → 消除重复测试”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把查询遍历变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 7.5.1 Sorted Linked-list Implementation

**目录节点 29/36。** 扫掠排序利用端点时空一致性维护轴序，只在区间开始/结束事件中更新候选对。

实现“7.5.1 Sorted Linked-list Implementation”时，沿“选择空间尺度 → 分配对象到单元 → 遍历查询路径 → 生成候选对 → 消除重复测试”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把候选对变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 7.5.2 Array-based Sorting

**目录节点 30/36。** 扫掠排序利用端点时空一致性维护轴序，只在区间开始/结束事件中更新候选对。

实现“7.5.2 Array-based Sorting”时，沿“选择空间尺度 → 分配对象到单元 → 遍历查询路径 → 生成候选对 → 消除重复测试”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把测试去重变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 7.6 Cells and Portals

**目录节点 31/36。** 单元与门户把静态环境连通性编码为图，查询先沿可穿越门户裁剪潜在空间。

实现“7.6 Cells and Portals”时，沿“选择空间尺度 → 分配对象到单元 → 遍历查询路径 → 生成候选对 → 消除重复测试”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把空间尺度变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 7.7 Avoiding Retesting

**目录节点 32/36。** 位标志或时间戳为每个对象对提供本查询去重，溢出与清理策略必须明确。

实现“7.7 Avoiding Retesting”时，沿“选择空间尺度 → 分配对象到单元 → 遍历查询路径 → 生成候选对 → 消除重复测试”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把对象分桶变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 7.7.1 Bit Flags

**目录节点 33/36。** 位标志或时间戳为每个对象对提供本查询去重，溢出与清理策略必须明确。

实现“7.7.1 Bit Flags”时，沿“选择空间尺度 → 分配对象到单元 → 遍历查询路径 → 生成候选对 → 消除重复测试”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把查询遍历变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 7.7.2 Time Stamping

**目录节点 34/36。** 位标志或时间戳为每个对象对提供本查询去重，溢出与清理策略必须明确。

实现“7.7.2 Time Stamping”时，沿“选择空间尺度 → 分配对象到单元 → 遍历查询路径 → 生成候选对 → 消除重复测试”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把候选对变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 7.7.3 Amortized Time Stamp Clearing

**目录节点 35/36。** “7.7.3 Amortized Time Stamp Clearing”位于“选择空间尺度 → 分配对象到单元 → 遍历查询路径 → 生成候选对 → 消除重复测试”链路中；实现时必须声明输入域、退化情况、误差界、输出状态与复杂度。

实现“7.7.3 Amortized Time Stamp Clearing”时，沿“选择空间尺度 → 分配对象到单元 → 遍历查询路径 → 生成候选对 → 消除重复测试”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把测试去重变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 7.8 Summary

**目录节点 36/36。** 小结应把“选择空间尺度 → 分配对象到单元 → 遍历查询路径 → 生成候选对 → 消除重复测试”压缩成可检查的不变量、复杂度和失败边界。

实现“7.8 Summary”时，沿“选择空间尺度 → 分配对象到单元 → 遍历查询路径 → 生成候选对 → 消除重复测试”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把空间尺度变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

## 可迁移实现骨架

返回值要携带分类、距离/时间、特征和诊断，不要让布尔值吞掉退化状态。

```cpp
struct CollisionResult {
  enum class State { Separated, Touching, Penetrating, Degenerate } state;
  double distance;
  double toi;
  Vec3 point_a, point_b, normal;
  uint32_t iterations;
  double error_bound;
};
```

碰撞管线从稳定候选开始，精确查询可按类型分派，但所有实现共享同一边界语义。

```text
freeze transforms and geometry generations
  -> update conservative bounding volumes
  -> generate and deduplicate candidate pairs
  -> run exact static or continuous query
  -> record predicates, error bounds and first-contact state
```

机器可读证据让同一退化样本跨编译器、标量精度和优化版本比较。

```json
{
  "seed": 7319,
  "scale": 1000.0,
  "query": "closest|overlap|ray|toi",
  "expected": "touching",
  "observed": "touching",
  "errorBound": 1.2e-8
}
```

## 本单元的验收工件

第一件工件是查询合同：为“选择空间尺度 → 分配对象到单元 → 遍历查询路径 → 生成候选对 → 消除重复测试”逐步写出输入域、坐标空间、边界包含、误差界、复杂度和返回状态。第二件是退化语料库：覆盖零长度、共线、共面、相切、近并行、巨大/微小尺度和高速跨越。第三件是目标机轨迹：记录对象数、候选对、节点访问、图元测试、迭代、缓存未命中和P50/P95/P99。第四件是差分报告：用高精度或独立实现作参照，定位第一处分歧而不是只统计最终错误率。


## 先预测再运行

运行交互前先预测：对象规模翻倍时候选对、节点访问还是精确测试先增长；几何接近共面时哪一个谓词先进入不确定区；SIMD宽度增加后数据整理和尾处理是否抵消收益。观察不一致时先检查查询合同、尺度归一、缓存代际和统计口径。

## 本章回顾

Chapter 7 Spatial Partitioning是“选择空间尺度 → 分配对象到单元 → 遍历查询路径 → 生成候选对 → 消除重复测试”中的完整算法合同。掌握本页意味着能逐项定位36个权威目录节点，解释空间尺度、对象分桶、查询遍历、候选对、测试去重的边界，复算几何与复杂度指标，并让一般、退化、压力和故障轨迹可重现。


## 术语复核


## 阅读导航

- [上一页：Chapter 6 Bounding Volume Hierarchies](/learn/real-time-collision-detection/07-chapter-06-bounding-volume-hierarchies/rtcd-chapter-06-bounding-volume-hierarchies)
- [下一页：Chapter 8 BSP Tree Hierarchies](/learn/real-time-collision-detection/09-chapter-08-bsp-tree-hierarchies/rtcd-chapter-08-bsp-tree-hierarchies)
