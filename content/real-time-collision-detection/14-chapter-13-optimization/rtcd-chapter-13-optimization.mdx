---
title: "Chapter 13 Optimization"
type: B
section: "实时碰撞检测算法技术（清华2010 / Morgan Kaufmann 2005）"
order: 14
description: "本章从CPU缓存、数据布局与紧凑树走到软件缓存、别名控制、SIMD和分支优化。 逐项覆盖24个权威目录坐标，并用退化几何、误差边界和目标机轨迹复核。"
demo: true
math: true
sourceUrl: "https://www.oreilly.com/library/view/real-time-collision-detection/9781558607323/"
draft: false
---

import {
  RtcdChapter13OptimizationMapLab,
  RtcdChapter13OptimizationExperimentLab,
  RtcdChapter13OptimizationEvidenceLab,
} from "@/components/mdx/real-time-collision-detection/diagrams/rtcd-chapter-13-optimization";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释Chapter 13 Optimization在15个正式单元、13章与357个目录节点中的位置
- 能绘制“建立性能基线 → 压紧代码与数据 → 重排缓存友好结构 → 向量化批量测试 → 以分位数复核”机制链并标出几何域、误差界和提前退出条件
- 能推导最近点、区间重叠、层次代价与浮点容差，并复算复杂度
- 能设计正常、退化、压力与故障实验，让第三方在目标机上独立复现

</Objectives>

## 为什么碰撞检测必须从查询合同开始

本章从CPU缓存、数据布局与紧凑树走到软件缓存、别名控制、SIMD和分支优化。 本课程不复制原书正文、插图或配套代码，而把公开目录中的算法组织成输入合同、几何谓词、交互实验、退化样本和验收证据。本页逐项覆盖24个目录坐标：Chapter 13 Optimization、13.1 CPU Caches、13.2 Instruction Cache Optimizations、13.3 Data Cache Optimizations、13.3.1 Structure Optimizations、13.3.2 Quantized and Compressed Vertex Data、13.3.3 Prefetching and Preloading、13.4 Cache-aware Data Structures and Algorithms、13.4.1 A Compact Static k-d Tree、13.4.2 A Compact AABB Tree、13.4.3 Cache Obliviousness、13.5 Software Caching、13.5.1 Cached Linearization Example、13.5.2 Amortized Predictive Linearization Caching、13.6 Aliasing、13.6.1 Type-based Alias Analysis、13.6.2 Restricted Pointers、13.6.3 Avoiding Aliasing、13.7 Parallelism Through SIMD Optimizations、13.7.1 Four Spheres Versus Four Spheres SIMD Test、13.7.2 Four Spheres Versus Four AABBs SIMD Test、13.7.3 Four AABBs Versus Four AABBs SIMD Test、13.8 Branching、13.9 Summary。

实时碰撞检测不是“两个盒子是否相交”的单一布尔问题。上层可能需要最近距离、命中顺序、首次接触时间、穿透方向或稳定接触流形；下层又受到坐标尺度、浮点误差、非流形网格、缓存布局和候选对规模约束。只有先冻结查询类型、输入域、边界包含规则和失败状态，才能比较算法是否正确且足够快。

## 版次、目录与改编合同

本路径同时固定原版与中译本：Christer Ericson《Real-Time Collision Detection》，Morgan Kaufmann，2005，ISBN 9781558607323；刘天慧译《实时碰撞检测算法技术》，清华大学出版社，2010年6月，ISBN 9787302224112。O’Reilly授权电子书目录与Morgan Kaufmann原书目录预览列出前置资料、13章所有层级小节，以及参考文献、索引和光盘说明，共357个正式目录节点。课程按15个正式单元逐项覆盖，另设学习地图与全书复核，共17页。

原书代码与GPU接口来自2004至2005年前后的硬件和编译器语境。课程保留几何证明、数据结构和优化推理，但接口、SIMD宽度与GPU同步必须在当前目标平台重新测量；旧代码的运行结果不能替代今天的鲁棒性证据。

## 五个贯穿全书的术语

<Term def="上层调用者对输入几何、查询类型、边界语义、输出字段和失败状态的完整约定。">
  查询合同
</Term>
、
<Term def="用较廉价结构排除不可能相交对象后留下的待精确检测对象对。">
  候选对
</Term>
、
<Term def="依据方向、内外或区间关系做离散分类且带误差边界的计算。">
  几何谓词
</Term>
、
<Term def="移动对象在给定时间区间内第一次接触时对应的参数值。">
  首次接触时间
</Term>
、
<Term def="包含输入、构建、目标机、谓词轨迹、指标和最终分类的复现记录。">
  碰撞证据包
</Term>
。

查询合同决定算法；候选对连接粗筛与精算；几何谓词决定拓扑分类；首次接触时间避免高速穿透；碰撞证据包则让退化场景可以回放。类名和成功截图都不能代替这些状态合同。

## 四个可复算模型

点到线段的参数投影与钳制为：

$$
t=operatorname{clamp}left(rac{(p-a)cdot(b-a)}{lVert b-a
Vert^2},0,1
ight),qquad q=a+t(b-a)
$$

两个AABB在每个轴都重叠时才相交：

$$
A_{min}^{i}le B_{max}^{i};land;B_{min}^{i}le A_{max}^{i},qquad iin{x,y,z}
$$

BVH构建和遍历可用表面积启发式比较：

$$
C=C_t+rac{S_L}{S_P}N_LC_i+rac{S_R}{S_P}N_RC_i
$$

浮点比较的容差同时包含绝对项与相对项：

$$
|a-b|le arepsilon_{abs}+arepsilon_{rel}max(|a|,|b|)
$$

最近点必须处理零长度线段；AABB边界要明确相切是否算碰撞；SAH只是查询分布近似；容差不能脱离几何尺度。每项公式都要通过一般、退化和极端尺度样本复算。

## 分步可视化：结构、规模与故障

<Stepper>
  <Step title="先画查询和数据依赖">
    <RtcdChapter13OptimizationMapLab />
  </Step>
  <Step title="再改变图元与候选规模">
    <RtcdChapter13OptimizationExperimentLab />
  </Step>
  <Step title="最后注入退化几何">
    <RtcdChapter13OptimizationEvidenceLab />
  </Step>
</Stepper>

### Chapter 13 Optimization

**目录节点 1/24。** 本章从CPU缓存、数据布局与紧凑树走到软件缓存、别名控制、SIMD和分支优化。

实现“Chapter 13 Optimization”时，沿“建立性能基线 → 压紧代码与数据 → 重排缓存友好结构 → 向量化批量测试 → 以分位数复核”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把缓存基线变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 13.1 CPU Caches

**目录节点 2/24。** 先用硬件计数器建立缓存基线，再按热点压紧代码与数据；平均时间无法解释长尾未命中。

实现“13.1 CPU Caches”时，沿“建立性能基线 → 压紧代码与数据 → 重排缓存友好结构 → 向量化批量测试 → 以分位数复核”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把数据布局变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 13.2 Instruction Cache Optimizations

**目录节点 3/24。** 先用硬件计数器建立缓存基线，再按热点压紧代码与数据；平均时间无法解释长尾未命中。

实现“13.2 Instruction Cache Optimizations”时，沿“建立性能基线 → 压紧代码与数据 → 重排缓存友好结构 → 向量化批量测试 → 以分位数复核”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把紧凑树变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 13.3 Data Cache Optimizations

**目录节点 4/24。** 先用硬件计数器建立缓存基线，再按热点压紧代码与数据；平均时间无法解释长尾未命中。

实现“13.3 Data Cache Optimizations”时，沿“建立性能基线 → 压紧代码与数据 → 重排缓存友好结构 → 向量化批量测试 → 以分位数复核”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把SIMD批处理变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 13.3.1 Structure Optimizations

**目录节点 5/24。** “13.3.1 Structure Optimizations”位于“建立性能基线 → 压紧代码与数据 → 重排缓存友好结构 → 向量化批量测试 → 以分位数复核”链路中；实现时必须声明输入域、退化情况、误差界、输出状态与复杂度。

实现“13.3.1 Structure Optimizations”时，沿“建立性能基线 → 压紧代码与数据 → 重排缓存友好结构 → 向量化批量测试 → 以分位数复核”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把分支成本变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 13.3.2 Quantized and Compressed Vertex Data

**目录节点 6/24。** 量化降低带宽但引入几何误差；预取只对可预测访问有效，并需把无用流量计入。

实现“13.3.2 Quantized and Compressed Vertex Data”时，沿“建立性能基线 → 压紧代码与数据 → 重排缓存友好结构 → 向量化批量测试 → 以分位数复核”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把缓存基线变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 13.3.3 Prefetching and Preloading

**目录节点 7/24。** 量化降低带宽但引入几何误差；预取只对可预测访问有效，并需把无用流量计入。

实现“13.3.3 Prefetching and Preloading”时，沿“建立性能基线 → 压紧代码与数据 → 重排缓存友好结构 → 向量化批量测试 → 以分位数复核”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把数据布局变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 13.4 Cache-aware Data Structures and Algorithms

**目录节点 8/24。** 紧凑树用线性布局和偏移减少节点大小，构建顺序应匹配主查询的遍历局部性。

实现“13.4 Cache-aware Data Structures and Algorithms”时，沿“建立性能基线 → 压紧代码与数据 → 重排缓存友好结构 → 向量化批量测试 → 以分位数复核”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把紧凑树变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 13.4.1 A Compact Static k-d Tree

**目录节点 9/24。** k-d树按轴平面划分空间，射线遍历用参数区间决定近远子树并以最近命中剪枝。

实现“13.4.1 A Compact Static k-d Tree”时，沿“建立性能基线 → 压紧代码与数据 → 重排缓存友好结构 → 向量化批量测试 → 以分位数复核”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把SIMD批处理变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 13.4.2 A Compact AABB Tree

**目录节点 10/24。** AABB以逐轴区间重叠实现廉价测试；旋转对象更新需在重算紧致性与保守变换成本之间取舍。

实现“13.4.2 A Compact AABB Tree”时，沿“建立性能基线 → 压紧代码与数据 → 重排缓存友好结构 → 向量化批量测试 → 以分位数复核”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把分支成本变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 13.4.3 Cache Obliviousness

**目录节点 11/24。** 紧凑树用线性布局和偏移减少节点大小，构建顺序应匹配主查询的遍历局部性。

实现“13.4.3 Cache Obliviousness”时，沿“建立性能基线 → 压紧代码与数据 → 重排缓存友好结构 → 向量化批量测试 → 以分位数复核”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把缓存基线变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 13.5 Software Caching

**目录节点 12/24。** 软件缓存保存昂贵线性化或支持结果，键必须包含几何代际、方向域和误差条件。

实现“13.5 Software Caching”时，沿“建立性能基线 → 压紧代码与数据 → 重排缓存友好结构 → 向量化批量测试 → 以分位数复核”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把数据布局变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 13.5.1 Cached Linearization Example

**目录节点 13/24。** 软件缓存保存昂贵线性化或支持结果，键必须包含几何代际、方向域和误差条件。

实现“13.5.1 Cached Linearization Example”时，沿“建立性能基线 → 压紧代码与数据 → 重排缓存友好结构 → 向量化批量测试 → 以分位数复核”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把紧凑树变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 13.5.2 Amortized Predictive Linearization Caching

**目录节点 14/24。** 软件缓存保存昂贵线性化或支持结果，键必须包含几何代际、方向域和误差条件。

实现“13.5.2 Amortized Predictive Linearization Caching”时，沿“建立性能基线 → 压紧代码与数据 → 重排缓存友好结构 → 向量化批量测试 → 以分位数复核”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把SIMD批处理变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 13.6 Aliasing

**目录节点 15/24。** 明确不别名合同可释放编译器向量化空间，但错误承诺会产生未定义行为。

实现“13.6 Aliasing”时，沿“建立性能基线 → 压紧代码与数据 → 重排缓存友好结构 → 向量化批量测试 → 以分位数复核”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把分支成本变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 13.6.1 Type-based Alias Analysis

**目录节点 16/24。** “13.6.1 Type-based Alias Analysis”位于“建立性能基线 → 压紧代码与数据 → 重排缓存友好结构 → 向量化批量测试 → 以分位数复核”链路中；实现时必须声明输入域、退化情况、误差界、输出状态与复杂度。

实现“13.6.1 Type-based Alias Analysis”时，沿“建立性能基线 → 压紧代码与数据 → 重排缓存友好结构 → 向量化批量测试 → 以分位数复核”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把缓存基线变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 13.6.2 Restricted Pointers

**目录节点 17/24。** 明确不别名合同可释放编译器向量化空间，但错误承诺会产生未定义行为。

实现“13.6.2 Restricted Pointers”时，沿“建立性能基线 → 压紧代码与数据 → 重排缓存友好结构 → 向量化批量测试 → 以分位数复核”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把数据布局变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 13.6.3 Avoiding Aliasing

**目录节点 18/24。** 明确不别名合同可释放编译器向量化空间，但错误承诺会产生未定义行为。

实现“13.6.3 Avoiding Aliasing”时，沿“建立性能基线 → 压紧代码与数据 → 重排缓存友好结构 → 向量化批量测试 → 以分位数复核”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把紧凑树变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 13.7 Parallelism Through SIMD Optimizations

**目录节点 19/24。** SIMD把同构图元测试转为结构化数组批处理，处理尾元素、掩码和对齐后再比较端到端收益。

实现“13.7 Parallelism Through SIMD Optimizations”时，沿“建立性能基线 → 压紧代码与数据 → 重排缓存友好结构 → 向量化批量测试 → 以分位数复核”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把SIMD批处理变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 13.7.1 Four Spheres Versus Four Spheres SIMD Test

**目录节点 20/24。** 球体旋转不变且测试廉价，但细长物体紧致性差；拟合算法应报告半径冗余与更新时间。

实现“13.7.1 Four Spheres Versus Four Spheres SIMD Test”时，沿“建立性能基线 → 压紧代码与数据 → 重排缓存友好结构 → 向量化批量测试 → 以分位数复核”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把分支成本变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 13.7.2 Four Spheres Versus Four AABBs SIMD Test

**目录节点 21/24。** AABB以逐轴区间重叠实现廉价测试；旋转对象更新需在重算紧致性与保守变换成本之间取舍。

实现“13.7.2 Four Spheres Versus Four AABBs SIMD Test”时，沿“建立性能基线 → 压紧代码与数据 → 重排缓存友好结构 → 向量化批量测试 → 以分位数复核”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把缓存基线变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 13.7.3 Four AABBs Versus Four AABBs SIMD Test

**目录节点 22/24。** AABB以逐轴区间重叠实现廉价测试；旋转对象更新需在重算紧致性与保守变换成本之间取舍。

实现“13.7.3 Four AABBs Versus Four AABBs SIMD Test”时，沿“建立性能基线 → 压紧代码与数据 → 重排缓存友好结构 → 向量化批量测试 → 以分位数复核”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把数据布局变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 13.8 Branching

**目录节点 23/24。** 分支优化关注可预测性和批次一致性；无分支表达式若增加指令或数值风险不一定更快。

实现“13.8 Branching”时，沿“建立性能基线 → 压紧代码与数据 → 重排缓存友好结构 → 向量化批量测试 → 以分位数复核”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把紧凑树变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

### 13.9 Summary

**目录节点 24/24。** 小结应把“建立性能基线 → 压紧代码与数据 → 重排缓存友好结构 → 向量化批量测试 → 以分位数复核”压缩成可检查的不变量、复杂度和失败边界。

实现“13.9 Summary”时，沿“建立性能基线 → 压紧代码与数据 → 重排缓存友好结构 → 向量化批量测试 → 以分位数复核”写出输入几何、坐标空间、参数区间、所有权和返回状态，并把SIMD批处理变成可观测字段。至少覆盖一般位置、相切/共面、零尺寸、极端尺度和高速运动，任何提前退出都要能说明所依据的保守界。

验收必须保存固定种子、原始几何、归一化尺度、中间谓词、误差界、迭代次数和最终接触。若不同优化路径给出不同分类，先定位第一处分支差异，再判断是数值误差、几何缺陷、缓存失效还是算法适用域被破坏。

## 可迁移实现骨架

返回值要携带分类、距离/时间、特征和诊断，不要让布尔值吞掉退化状态。

```cpp
struct CollisionResult {
  enum class State { Separated, Touching, Penetrating, Degenerate } state;
  double distance;
  double toi;
  Vec3 point_a, point_b, normal;
  uint32_t iterations;
  double error_bound;
};
```

碰撞管线从稳定候选开始，精确查询可按类型分派，但所有实现共享同一边界语义。

```text
freeze transforms and geometry generations
  -> update conservative bounding volumes
  -> generate and deduplicate candidate pairs
  -> run exact static or continuous query
  -> record predicates, error bounds and first-contact state
```

机器可读证据让同一退化样本跨编译器、标量精度和优化版本比较。

```json
{
  "seed": 7319,
  "scale": 1000.0,
  "query": "closest|overlap|ray|toi",
  "expected": "touching",
  "observed": "touching",
  "errorBound": 1.2e-8
}
```

## 本单元的验收工件

第一件工件是查询合同：为“建立性能基线 → 压紧代码与数据 → 重排缓存友好结构 → 向量化批量测试 → 以分位数复核”逐步写出输入域、坐标空间、边界包含、误差界、复杂度和返回状态。第二件是退化语料库：覆盖零长度、共线、共面、相切、近并行、巨大/微小尺度和高速跨越。第三件是目标机轨迹：记录对象数、候选对、节点访问、图元测试、迭代、缓存未命中和P50/P95/P99。第四件是差分报告：用高精度或独立实现作参照，定位第一处分歧而不是只统计最终错误率。

<Callout type="trap" title="把固定epsilon当成鲁棒性">
  单一常数无法同时适配厘米与公里尺度。容差应来自输入尺度和运算误差界，不确定时升级到区间或精确谓词。
</Callout>

<Callout type="trap" title="只测试随机一般位置">
  随机数据几乎不会命中共线、共面、相切和零尺寸。必须定向构造退化样本并在微扰两侧检查分类连续性。
</Callout>

<Callout type="trap" title="微基准更快就宣布优化成功">
  数据布局、SIMD和GPU卸载可能增加候选、同步或长尾。验收要看完整管线和真实查询分布，而不是孤立函数平均值。
</Callout>

## 先预测再运行

运行交互前先预测：对象规模翻倍时候选对、节点访问还是精确测试先增长；几何接近共面时哪一个谓词先进入不确定区；SIMD宽度增加后数据整理和尾处理是否抵消收益。观察不一致时先检查查询合同、尺度归一、缓存代际和统计口径。

## 本章回顾

Chapter 13 Optimization是“建立性能基线 → 压紧代码与数据 → 重排缓存友好结构 → 向量化批量测试 → 以分位数复核”中的完整算法合同。掌握本页意味着能逐项定位24个权威目录节点，解释缓存基线、数据布局、紧凑树、SIMD批处理、分支成本的边界，复算几何与复杂度指标，并让一般、退化、压力和故障轨迹可重现。

<Exercises>

**问题 1：合同与退化。** 怎样为Chapter 13 Optimization定义输入域和相切/共面语义？

<Answer>
  先声明坐标、标量、闭开边界、退化返回和误差界，再沿“建立性能基线 →
  压紧代码与数据 → 重排缓存友好结构 → 向量化批量测试 →
  以分位数复核”构造一般位置及边界两侧样本。无法区分Separated、Touching、Penetrating和Degenerate的接口不完整。
</Answer>

**问题 2：复杂度实验。** 怎样验证缓存基线在真实查询分布下满足预算？

<Answer>
  固定目标机、构建、场景和种子，按对象数与尺度分层采样，记录候选对、节点访问、图元测试、迭代、缓存事件及P50/P95/P99，并和暴力或高精度参照核对正确性。
</Answer>

**问题 3：迁移复核。** 怎样证明Chapter 13 Optimization不是只记住原书伪代码？

<Answer>
  更换数据布局、标量精度或后端实现，但保持查询合同、退化语料、误差界和证据字段；由独立复核者重放同一输入并得到一致分类与可解释性能边界。
</Answer>

</Exercises>

## 术语复核

<Glossary>
  <GlossaryItem term="查询合同">
    上层调用者对输入几何、查询类型、边界语义、输出字段和失败状态的完整约定。
  </GlossaryItem>
  <GlossaryItem term="候选对">
    用较廉价结构排除不可能相交对象后留下的待精确检测对象对。
  </GlossaryItem>
  <GlossaryItem term="几何谓词">
    依据方向、内外或区间关系做离散分类且带误差边界的计算。
  </GlossaryItem>
  <GlossaryItem term="首次接触时间">
    移动对象在给定时间区间内第一次接触时对应的参数值。
  </GlossaryItem>
  <GlossaryItem term="碰撞证据包">
    包含输入、构建、目标机、谓词轨迹、指标和最终分类的复现记录。
  </GlossaryItem>
</Glossary>

<Attribution
  adaptedFrom="Real-Time Collision Detection / 实时碰撞检测算法技术"
  adaptedUrl="https://www.oreilly.com/library/view/real-time-collision-detection/9781558607323/"
/>

## 阅读导航

- [上一页：Chapter 12 Geometrical Robustness](/learn/real-time-collision-detection/13-chapter-12-geometrical-robustness/rtcd-chapter-12-geometrical-robustness)
- [下一页：后置资料：参考文献、索引与配套光盘](/learn/real-time-collision-detection/15-back-matter/rtcd-back-matter)
