---
title: "帧管线、GPU 执行与图形硬件"
type: B
section: "计算机图形学与渲染"
order: 1
description: "承载官方 Ch1-3、Ch23：应用阶段、几何/光栅/像素管线、SIMT 执行、缓存、带宽与帧系统"
demo: true
math: true
sourceUrl: "https://www.realtimerendering.com/"
draft: true
---

import { RtrGraphicsPipelineDiagram, RtrGpuExecutionDiagram, RtrFrameBudgetDiagram } from "@/components/mdx/real-time-rendering-4e/diagrams/rtr-graphics-pipeline";
import { RtrStepFlowDiagram } from "@/components/mdx/real-time-rendering-4e/diagrams/rtr-step-flow";
import { Objectives, Callout, Attribution, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step } from "@/components/mdx/mdx-components";

<Objectives>

- 能描述 Introduction、The Graphics Rendering Pipeline、The Graphics Processing Unit 和 Graphics Hardware 的职责边界
- 能从应用阶段追踪 draw/dispatch、vertex/primitive/raster/fragment/output 到 present 的数据流
- 能区分 GPU latency、throughput、occupancy、divergence、bandwidth 与 CPU submission 瓶颈
- 能设计控制变量实验，证明一帧受哪个阶段限制并保留正确性证据

</Objectives>

## 直觉：实时渲染是一套异步 producer-consumer 系统

CPU 更新世界并记录命令，GPU 以批次/波前执行 shader，固定功能单元装配、光栅、测试和混合，显示系统消费呈现图像。阶段可以重叠，也会因同步、队列和资源依赖串行。

先预测：GPU timer 显示 8 ms、CPU render thread 5 ms，帧一定是 8 ms 吗？不一定；present、queue depth、同步和其他 CPU 工作会改变端到端帧时间。

<Term def="在目标设备和质量约束下持续生成并及时呈现图像，同时响应输入和场景变化的系统过程">实时渲染</Term>是 Ch1 的范围，不等于一个 shader 或固定 FPS 数字。

<RtrGraphicsPipelineDiagram />

## Ch1-3、Ch23 的统一边界

- **Introduction**：实时目标、应用领域、视觉/交互质量和全书问题空间；
- **The Graphics Rendering Pipeline**：application、geometry processing、rasterization、pixel processing 的概念阶段；
- **The Graphics Processing Unit**：shader core、SIMT/wavefront/warp、调度、存储层次和执行效率；
- **Graphics Hardware**：架构、带宽、缓存、ROP/texture/raster 单元、CPU-GPU 接口和显示。

<Term def="CPU 侧执行场景更新、可见性/LOD 决策、资源流送、命令记录和高层调度的管线部分">应用阶段</Term>不是只调用 draw；其数据结构和并行化决定 GPU 是否有工作可做。

## 从场景到呈现的完整路径

1. simulation/input 更新 scene state；
2. visibility/LOD/build render list；
3. command recording 与 resource transitions/synchronization；
4. vertex/mesh/primitive processing；
5. clipping、setup、rasterization；
6. fragment/pixel shading；
7. depth/stencil/blend/output；
8. post-processing、UI、present/compositor/display。

<Term def="把图元覆盖转换为离散样本/片段，并生成插值属性供后续着色的固定功能过程">光栅化</Term>通常由专用硬件实现，但 mesh/compute/software raster 等路径可改变图元准备甚至替代部分传统流程。不能说整个“光栅化阶段”都不可编程且永远固定。

```cpp
FrameGraph frame;
frame.add("visibility", scene, visibleSet);
frame.add("depth", visibleSet, depth);
frame.add("shading", visibleSet, depth, hdrColor);
frame.add("post", hdrColor, displayColor);
frame.add("ui-present", displayColor, swapImage);
frame.compileDependenciesAndQueues();
frame.execute();
```

## GPU 执行：SIMT 不等于每线程独立核心

<Term def="多个 lane 以执行组共同发射指令、各自维护数据状态的 GPU 执行方式；厂商术语包括 warp 与 wavefront">SIMT 执行组</Term>在相干控制流和访问下利用率高。分支发散可能按路径掩码执行，但具体代价依架构和编译器；不能用“分支必然减半”概括。

<RtrGpuExecutionDiagram />

GPU 通过大量 resident warps/waves 隐藏 memory latency。occupancy 是可驻留执行组比例，不是性能目标本身；register/shared-memory 压力会限制驻留，但过度降低寄存器也可能 spill。

理论吞吐上限受多个 roof 约束。若工作算术量为 $F$、传输字节 $B$，算术强度 $I=F/B$，roofline 近似：

$$
P\le\min(P_{compute}, I\cdot BW_{memory}).
$$

它帮助区分 compute-bound 与 bandwidth-bound，但 cache 命中、压缩、固定单元和 occupancy 仍需实测。

## 图形硬件与存储层次

GPU 包含 command processor/front end、shader arrays、texture units、raster/setup、ROP/output、caches 和 memory controllers；不同架构可 unified 或 tile-based。API 抽象不保证底层组织相同。

<Term def="单位时间可在存储层次间传输的有效数据量；受访问合并、缓存、压缩、格式和争用影响">有效带宽</Term>不同于显存标称峰值。G-buffer、MSAA、blend、纹理与 resolve 都可能放大流量。

字节预算可粗算：

$$
B_{frame}=W\cdot H\cdot\sum_i(bytesPerSample_i\cdot samples_i\cdot accesses_i).
$$

实际带宽还受 early-Z、compression、cache 和 tile locality 降低。预算用于假设，counter/时间用于证明。

## Latency、throughput 与帧重叠

<Term def="完成单项工作所需时间">延迟</Term>与单位时间完成多少工作的 throughput 不同。GPU 可能有高单项 latency 但靠并行拥有高 throughput；实时交互同时关心帧 throughput 和 motion/input latency。

若 CPU/GPU/present 完全重叠，稳态近似最大值；若存在同步：

$$
T_{frame}\approx\max(T_{CPU},T_{GPU},T_{present})+T_{serialized}.
$$

<RtrFrameBudgetDiagram />

queue 太深会提高吞吐却增加输入到显示延迟；VR/AR 还需 late update、prediction 和 reprojection。只让 GPU “一直忙”并非唯一目标。

## 控制变量定位瓶颈

```cpp
ExperimentSet tests = {
  reduceResolution(), simplifyFragmentShader(), reduceTextureBandwidth(),
  reduceVertexCount(), reduceDrawCount(), disableExpensivePass()
};
for (auto test : tests) {
  runWarm(test);
  collectCpuGpuQueriesAndCounters(test);
  compareOutputAgainstBaseline(test);
}
```

降低分辨率显著加速通常指向 pixel/fill/bandwidth；降低顶点数显著加速指向 geometry；减少 draw 对 CPU 影响大则 submission-bound。一次改变多个因素无法归因。

## 三步帧诊断

<Stepper>
<Step title="第一步：画 producer-consumer 图">

列出 CPU tasks、GPU passes、queues、resources 和 present，标出 wait/fence/readback。

<RtrStepFlowDiagram caption="先定位可重叠和被强制串行的边。" stage={1} stages={["Map：CPU tasks、GPU passes、resources、queues、present"]} />

</Step>
<Step title="第二步：采集分层证据">

同时记录 CPU wall/profile、GPU timestamps/statistics、memory counters 和帧输出哈希。

<RtrStepFlowDiagram caption="单一 FPS 无法区分提交、着色、带宽和呈现。" stage={2} stages={["Map：CPU tasks、GPU passes、resources、queues、present", "Measure：CPU/GPU time、counters、traffic、output"]} />

</Step>
<Step title="第三步：控制变量验证">

分别缩放 pixels、vertices、draws、samples 和 memory traffic，比较时间斜率并回归 P95/P99。

<RtrStepFlowDiagram caption="瓶颈结论必须能预测工作量变化后的时间响应。" stage={3} stages={["Map：CPU tasks、GPU passes、resources、queues、present", "Measure：CPU/GPU time、counters、traffic、output", "Validate：controlled scaling、percentiles、latency、correctness"]} />

</Step>
</Stepper>

## 常见误区

<Callout type="trap">
**现象 →** 把管线分成 CPU、顶点、光栅化三段，遗漏 output、post、present 和资源依赖。
**原因 →** 教学简图被当成完整帧系统。
**修法 →** 用 frame graph/时间线标出所有 producer-consumer 和同步边。
</Callout>

<Callout type="trap">
**现象 →** 看到 branch 就改成两边全算的 `mix`。
**原因 →** 假设所有分支都严重发散，忽略相干性和额外工作。
**修法 →** 用真实分支分布和计数器测量，比较输出与时间，不按口诀改写。
</Callout>

<Callout type="trap">
**现象 →** occupancy 未满就认为性能差。
**原因 →** 把隐藏延迟的手段当最终指标。
**修法 →** 结合 IPC、memory stalls、spill、cache 和时间；达到足够 occupancy 后其他瓶颈更重要。
</Callout>

## 练习

<Exercises>

**问题 1：为什么 GPU 8 ms 不等于帧 8 ms？**

<Answer>
CPU simulation/recording、GPU queue wait、present/compositor 和 serialized readback 也参与端到端；阶段可能重叠也可能串行。要从时间线和同步边计算，而不是取单个 timer。
</Answer>

**问题 2：怎样区分 bandwidth-bound 与 compute-bound？**

<Answer>
固定输出，分别降低纹理/attachment 字节和算术指令；结合分辨率缩放、memory transactions、cache、ALU utilization 与时间斜率。roofline 提供假设，实测给结论。
</Answer>

**问题 3：应用阶段的最小验收是什么？**

<Answer>
检查场景更新、可见集/LOD、render list/state sort、资源驻留、command count 和 CPU 时间；用已知场景验证不漏对象，并确认没有同步 readback 阻塞 GPU/CPU 重叠。
</Answer>

</Exercises>

## 小结

Ch1-3 与 Ch23 共同定义实时帧：应用产生工作，管线处理几何与样本，GPU 以执行组和存储层次追求吞吐，硬件与显示决定带宽/延迟边界。只有时间线、计数器、控制变量和输出正确性共同支持，才能宣称定位了瓶颈。

<Glossary>
<GlossaryItem term="实时渲染">在质量和交互预算内持续生成并及时呈现图像的系统过程。</GlossaryItem>
<GlossaryItem term="应用阶段">CPU 侧场景、可见性、资源和命令调度阶段。</GlossaryItem>
<GlossaryItem term="光栅化">把图元覆盖转换为离散样本并插值属性的过程。</GlossaryItem>
<GlossaryItem term="SIMT 执行组">以 warp/wave 共同发射指令、lane 各持数据的执行方式。</GlossaryItem>
<GlossaryItem term="有效带宽">受访问、缓存、压缩和争用影响的实际数据吞吐。</GlossaryItem>
<GlossaryItem term="延迟">完成单项工作从开始到结束所需时间。</GlossaryItem>
</Glossary>

<Attribution adaptedFrom="Real-Time Rendering 4e, Chapters 1-3 and 23" adaptedUrl="https://www.realtimerendering.com/" />
