---
title: 第2版权威学习地图
description: >-
  沿数据结构与对象、单机数据库、多机数据库和独立功能四部分完成24章Redis 3.0实现 覆盖24个正式节点，并以Redis
  3.0源码、故障和恢复对账验收。
type: C
section: Redis设计与实现（第2版 / Redis 3.0 / 2014）
draft: false
math: false
sourceUrl: 'https://huangz.works/redisbook1e/'
demo: true
order: 0
qualityVersion: 2
practiceMode: design
sourceMode: independent-rewrite
---


{/* RDI_QUALITY_V2 */}

## 为什么从“24章源码依赖导航台”开始

第2版权威学习地图的核心任务是沿数据结构与对象、单机数据库、多机数据库和独立功能四部分完成24章Redis 3.0实现。命令返回值只暴露外层合同；实现解释还必须连接内存结构、写入与读取路径、事件顺序以及失败后的旧状态回收。24章源码依赖导航台把这些关系放进同一条可复位轨迹。

先写预测：当追踪跨度由“单章闭环”进入“跨四部分”时，哪个可观察状态最先变化？再规定什么结果会推翻当前解释。交互中的分数只表达透明因果方向，不冒充真实Redis测量。

## 来源、版次与独立重写边界

黄健宏[作者读者服务页](https://huangz.works/redisbook1e/)确认正式出版新版以Redis 3.0为源码基线，列出4部分、24章及完整小节，并链接[Redis 3.0中文注释源码](https://github.com/huangzworks/redis-3.0-annotated)。本课程据此映射24个正式单元、145个目录节点，另设学习地图和总复习；未取得出版正文授权，目录只界定范围，不宣称复现原书正文。

本章字段与控制流由[Redis官方3.0源码：server.c](https://github.com/redis/redis/blob/3.0/src/server.c)和作者注释源码交叉核对；[Redis当前持久化文档](https://redis.io/docs/latest/operate/oss_and_stack/management/persistence/)只用于辨认版本差异。中文解释、图示、交互、实验与答案均为独立教学重写，不把目录页或代码仓库许可证误报为原书许可证。

## 本章术语与源码合同

、、、、、。

第2版权威学习地图必须守住“24章都有正式页面、完整小节、源码结构图、运行实验、失败反例和独立交付物”。观察同时记录24章源码依赖导航台一致率与证据层级分叉风险；只有结构快照、函数入口、运行结果和故障反例互相一致，才接受实现结论。

## 作者目录逐项深读

### 第1章 简介

**四级证据 1/24。** 第1章 简介必须在Redis 3.0的server.c中定位结构或函数入口，并说明它怎样参与“沿数据结构与对象、单机数据库、多机数据库和独立功能四部分完成24章Redis 3.0实现”。本节点用“第1章 简介”作观察点，执行`git -C redis-3.0-annotated rev-parse HEAD`或等价源码探针，保存版本、输入、前后状态和能推翻解释的反例。

验证第1章 简介时，先预测追踪跨度从“单章闭环”切到“跨四部分”会改变哪个字段、偏移、文件或消息；再固定证据层级做一次对照。若故障注入没有破坏“24章都有正式页面、完整小节、源码结构图、运行实验、失败反例和独立交付物”，就撤回当前解释而不是补写故事。

### 第2章 简单动态字符串

**四级证据 2/24。** 第2章 简单动态字符串必须在Redis 3.0的server.c中定位结构或函数入口，并说明它怎样参与“沿数据结构与对象、单机数据库、多机数据库和独立功能四部分完成24章Redis 3.0实现”。本节点用“第2章 简单动态字符串”作观察点，执行`git -C redis-3.0-annotated rev-parse HEAD`或等价源码探针，保存版本、输入、前后状态和能推翻解释的反例。

验证第2章 简单动态字符串时，先预测追踪跨度从“单章闭环”切到“跨四部分”会改变哪个字段、偏移、文件或消息；再固定证据层级做一次对照。若故障注入没有破坏“24章都有正式页面、完整小节、源码结构图、运行实验、失败反例和独立交付物”，就撤回当前解释而不是补写故事。

### 第3章 链表

**四级证据 3/24。** 第3章 链表必须在Redis 3.0的server.c中定位结构或函数入口，并说明它怎样参与“沿数据结构与对象、单机数据库、多机数据库和独立功能四部分完成24章Redis 3.0实现”。本节点用“第3章 链表”作观察点，执行`git -C redis-3.0-annotated rev-parse HEAD`或等价源码探针，保存版本、输入、前后状态和能推翻解释的反例。

验证第3章 链表时，先预测追踪跨度从“单章闭环”切到“跨四部分”会改变哪个字段、偏移、文件或消息；再固定证据层级做一次对照。若故障注入没有破坏“24章都有正式页面、完整小节、源码结构图、运行实验、失败反例和独立交付物”，就撤回当前解释而不是补写故事。

### 第4章 字典

**四级证据 4/24。** 第4章 字典必须在Redis 3.0的server.c中定位结构或函数入口，并说明它怎样参与“沿数据结构与对象、单机数据库、多机数据库和独立功能四部分完成24章Redis 3.0实现”。本节点用“第4章 字典”作观察点，执行`git -C redis-3.0-annotated rev-parse HEAD`或等价源码探针，保存版本、输入、前后状态和能推翻解释的反例。

验证第4章 字典时，先预测追踪跨度从“单章闭环”切到“跨四部分”会改变哪个字段、偏移、文件或消息；再固定证据层级做一次对照。若故障注入没有破坏“24章都有正式页面、完整小节、源码结构图、运行实验、失败反例和独立交付物”，就撤回当前解释而不是补写故事。

### 第5章 跳跃表

**四级证据 5/24。** 第5章 跳跃表必须在Redis 3.0的server.c中定位结构或函数入口，并说明它怎样参与“沿数据结构与对象、单机数据库、多机数据库和独立功能四部分完成24章Redis 3.0实现”。本节点用“第5章 跳跃表”作观察点，执行`git -C redis-3.0-annotated rev-parse HEAD`或等价源码探针，保存版本、输入、前后状态和能推翻解释的反例。

验证第5章 跳跃表时，先预测追踪跨度从“单章闭环”切到“跨四部分”会改变哪个字段、偏移、文件或消息；再固定证据层级做一次对照。若故障注入没有破坏“24章都有正式页面、完整小节、源码结构图、运行实验、失败反例和独立交付物”，就撤回当前解释而不是补写故事。

### 第6章 整数集合

**四级证据 6/24。** 第6章 整数集合必须在Redis 3.0的server.c中定位结构或函数入口，并说明它怎样参与“沿数据结构与对象、单机数据库、多机数据库和独立功能四部分完成24章Redis 3.0实现”。本节点用“第6章 整数集合”作观察点，执行`git -C redis-3.0-annotated rev-parse HEAD`或等价源码探针，保存版本、输入、前后状态和能推翻解释的反例。

验证第6章 整数集合时，先预测追踪跨度从“单章闭环”切到“跨四部分”会改变哪个字段、偏移、文件或消息；再固定证据层级做一次对照。若故障注入没有破坏“24章都有正式页面、完整小节、源码结构图、运行实验、失败反例和独立交付物”，就撤回当前解释而不是补写故事。

### 第7章 压缩列表

**四级证据 7/24。** 第7章 压缩列表必须在Redis 3.0的server.c中定位结构或函数入口，并说明它怎样参与“沿数据结构与对象、单机数据库、多机数据库和独立功能四部分完成24章Redis 3.0实现”。本节点用“第1章 简介”作观察点，执行`git -C redis-3.0-annotated rev-parse HEAD`或等价源码探针，保存版本、输入、前后状态和能推翻解释的反例。

验证第7章 压缩列表时，先预测追踪跨度从“单章闭环”切到“跨四部分”会改变哪个字段、偏移、文件或消息；再固定证据层级做一次对照。若故障注入没有破坏“24章都有正式页面、完整小节、源码结构图、运行实验、失败反例和独立交付物”，就撤回当前解释而不是补写故事。

### 第8章 对象

**四级证据 8/24。** 第8章 对象必须在Redis 3.0的server.c中定位结构或函数入口，并说明它怎样参与“沿数据结构与对象、单机数据库、多机数据库和独立功能四部分完成24章Redis 3.0实现”。本节点用“第2章 简单动态字符串”作观察点，执行`git -C redis-3.0-annotated rev-parse HEAD`或等价源码探针，保存版本、输入、前后状态和能推翻解释的反例。

验证第8章 对象时，先预测追踪跨度从“单章闭环”切到“跨四部分”会改变哪个字段、偏移、文件或消息；再固定证据层级做一次对照。若故障注入没有破坏“24章都有正式页面、完整小节、源码结构图、运行实验、失败反例和独立交付物”，就撤回当前解释而不是补写故事。

### 第9章 数据库

**四级证据 9/24。** 第9章 数据库必须在Redis 3.0的server.c中定位结构或函数入口，并说明它怎样参与“沿数据结构与对象、单机数据库、多机数据库和独立功能四部分完成24章Redis 3.0实现”。本节点用“第3章 链表”作观察点，执行`git -C redis-3.0-annotated rev-parse HEAD`或等价源码探针，保存版本、输入、前后状态和能推翻解释的反例。

验证第9章 数据库时，先预测追踪跨度从“单章闭环”切到“跨四部分”会改变哪个字段、偏移、文件或消息；再固定证据层级做一次对照。若故障注入没有破坏“24章都有正式页面、完整小节、源码结构图、运行实验、失败反例和独立交付物”，就撤回当前解释而不是补写故事。

### 第10章 RDB持久化

**四级证据 10/24。** 第10章 RDB持久化必须在Redis 3.0的server.c中定位结构或函数入口，并说明它怎样参与“沿数据结构与对象、单机数据库、多机数据库和独立功能四部分完成24章Redis 3.0实现”。本节点用“第4章 字典”作观察点，执行`git -C redis-3.0-annotated rev-parse HEAD`或等价源码探针，保存版本、输入、前后状态和能推翻解释的反例。

验证第10章 RDB持久化时，先预测追踪跨度从“单章闭环”切到“跨四部分”会改变哪个字段、偏移、文件或消息；再固定证据层级做一次对照。若故障注入没有破坏“24章都有正式页面、完整小节、源码结构图、运行实验、失败反例和独立交付物”，就撤回当前解释而不是补写故事。

### 第11章 AOF持久化

**四级证据 11/24。** 第11章 AOF持久化必须在Redis 3.0的server.c中定位结构或函数入口，并说明它怎样参与“沿数据结构与对象、单机数据库、多机数据库和独立功能四部分完成24章Redis 3.0实现”。本节点用“第5章 跳跃表”作观察点，执行`git -C redis-3.0-annotated rev-parse HEAD`或等价源码探针，保存版本、输入、前后状态和能推翻解释的反例。

验证第11章 AOF持久化时，先预测追踪跨度从“单章闭环”切到“跨四部分”会改变哪个字段、偏移、文件或消息；再固定证据层级做一次对照。若故障注入没有破坏“24章都有正式页面、完整小节、源码结构图、运行实验、失败反例和独立交付物”，就撤回当前解释而不是补写故事。

### 第12章 事件

**四级证据 12/24。** 第12章 事件必须在Redis 3.0的server.c中定位结构或函数入口，并说明它怎样参与“沿数据结构与对象、单机数据库、多机数据库和独立功能四部分完成24章Redis 3.0实现”。本节点用“第6章 整数集合”作观察点，执行`git -C redis-3.0-annotated rev-parse HEAD`或等价源码探针，保存版本、输入、前后状态和能推翻解释的反例。

验证第12章 事件时，先预测追踪跨度从“单章闭环”切到“跨四部分”会改变哪个字段、偏移、文件或消息；再固定证据层级做一次对照。若故障注入没有破坏“24章都有正式页面、完整小节、源码结构图、运行实验、失败反例和独立交付物”，就撤回当前解释而不是补写故事。

### 第13章 客户端

**四级证据 13/24。** 第13章 客户端必须在Redis 3.0的server.c中定位结构或函数入口，并说明它怎样参与“沿数据结构与对象、单机数据库、多机数据库和独立功能四部分完成24章Redis 3.0实现”。本节点用“第1章 简介”作观察点，执行`git -C redis-3.0-annotated rev-parse HEAD`或等价源码探针，保存版本、输入、前后状态和能推翻解释的反例。

验证第13章 客户端时，先预测追踪跨度从“单章闭环”切到“跨四部分”会改变哪个字段、偏移、文件或消息；再固定证据层级做一次对照。若故障注入没有破坏“24章都有正式页面、完整小节、源码结构图、运行实验、失败反例和独立交付物”，就撤回当前解释而不是补写故事。

### 第14章 服务器

**四级证据 14/24。** 第14章 服务器必须在Redis 3.0的server.c中定位结构或函数入口，并说明它怎样参与“沿数据结构与对象、单机数据库、多机数据库和独立功能四部分完成24章Redis 3.0实现”。本节点用“第2章 简单动态字符串”作观察点，执行`git -C redis-3.0-annotated rev-parse HEAD`或等价源码探针，保存版本、输入、前后状态和能推翻解释的反例。

验证第14章 服务器时，先预测追踪跨度从“单章闭环”切到“跨四部分”会改变哪个字段、偏移、文件或消息；再固定证据层级做一次对照。若故障注入没有破坏“24章都有正式页面、完整小节、源码结构图、运行实验、失败反例和独立交付物”，就撤回当前解释而不是补写故事。

### 第15章 复制

**四级证据 15/24。** 第15章 复制必须在Redis 3.0的server.c中定位结构或函数入口，并说明它怎样参与“沿数据结构与对象、单机数据库、多机数据库和独立功能四部分完成24章Redis 3.0实现”。本节点用“第3章 链表”作观察点，执行`git -C redis-3.0-annotated rev-parse HEAD`或等价源码探针，保存版本、输入、前后状态和能推翻解释的反例。

验证第15章 复制时，先预测追踪跨度从“单章闭环”切到“跨四部分”会改变哪个字段、偏移、文件或消息；再固定证据层级做一次对照。若故障注入没有破坏“24章都有正式页面、完整小节、源码结构图、运行实验、失败反例和独立交付物”，就撤回当前解释而不是补写故事。

### 第16章 Sentinel

**四级证据 16/24。** 第16章 Sentinel必须在Redis 3.0的server.c中定位结构或函数入口，并说明它怎样参与“沿数据结构与对象、单机数据库、多机数据库和独立功能四部分完成24章Redis 3.0实现”。本节点用“第4章 字典”作观察点，执行`git -C redis-3.0-annotated rev-parse HEAD`或等价源码探针，保存版本、输入、前后状态和能推翻解释的反例。

验证第16章 Sentinel时，先预测追踪跨度从“单章闭环”切到“跨四部分”会改变哪个字段、偏移、文件或消息；再固定证据层级做一次对照。若故障注入没有破坏“24章都有正式页面、完整小节、源码结构图、运行实验、失败反例和独立交付物”，就撤回当前解释而不是补写故事。

### 第17章 集群

**四级证据 17/24。** 第17章 集群必须在Redis 3.0的server.c中定位结构或函数入口，并说明它怎样参与“沿数据结构与对象、单机数据库、多机数据库和独立功能四部分完成24章Redis 3.0实现”。本节点用“第5章 跳跃表”作观察点，执行`git -C redis-3.0-annotated rev-parse HEAD`或等价源码探针，保存版本、输入、前后状态和能推翻解释的反例。

验证第17章 集群时，先预测追踪跨度从“单章闭环”切到“跨四部分”会改变哪个字段、偏移、文件或消息；再固定证据层级做一次对照。若故障注入没有破坏“24章都有正式页面、完整小节、源码结构图、运行实验、失败反例和独立交付物”，就撤回当前解释而不是补写故事。

### 第18章 发布与订阅

**四级证据 18/24。** 第18章 发布与订阅必须在Redis 3.0的server.c中定位结构或函数入口，并说明它怎样参与“沿数据结构与对象、单机数据库、多机数据库和独立功能四部分完成24章Redis 3.0实现”。本节点用“第6章 整数集合”作观察点，执行`git -C redis-3.0-annotated rev-parse HEAD`或等价源码探针，保存版本、输入、前后状态和能推翻解释的反例。

验证第18章 发布与订阅时，先预测追踪跨度从“单章闭环”切到“跨四部分”会改变哪个字段、偏移、文件或消息；再固定证据层级做一次对照。若故障注入没有破坏“24章都有正式页面、完整小节、源码结构图、运行实验、失败反例和独立交付物”，就撤回当前解释而不是补写故事。

### 第19章 事务

**四级证据 19/24。** 第19章 事务必须在Redis 3.0的server.c中定位结构或函数入口，并说明它怎样参与“沿数据结构与对象、单机数据库、多机数据库和独立功能四部分完成24章Redis 3.0实现”。本节点用“第1章 简介”作观察点，执行`git -C redis-3.0-annotated rev-parse HEAD`或等价源码探针，保存版本、输入、前后状态和能推翻解释的反例。

验证第19章 事务时，先预测追踪跨度从“单章闭环”切到“跨四部分”会改变哪个字段、偏移、文件或消息；再固定证据层级做一次对照。若故障注入没有破坏“24章都有正式页面、完整小节、源码结构图、运行实验、失败反例和独立交付物”，就撤回当前解释而不是补写故事。

### 第20章 Lua脚本

**四级证据 20/24。** 第20章 Lua脚本必须在Redis 3.0的server.c中定位结构或函数入口，并说明它怎样参与“沿数据结构与对象、单机数据库、多机数据库和独立功能四部分完成24章Redis 3.0实现”。本节点用“第2章 简单动态字符串”作观察点，执行`git -C redis-3.0-annotated rev-parse HEAD`或等价源码探针，保存版本、输入、前后状态和能推翻解释的反例。

验证第20章 Lua脚本时，先预测追踪跨度从“单章闭环”切到“跨四部分”会改变哪个字段、偏移、文件或消息；再固定证据层级做一次对照。若故障注入没有破坏“24章都有正式页面、完整小节、源码结构图、运行实验、失败反例和独立交付物”，就撤回当前解释而不是补写故事。

### 第21章 排序

**四级证据 21/24。** 第21章 排序必须在Redis 3.0的server.c中定位结构或函数入口，并说明它怎样参与“沿数据结构与对象、单机数据库、多机数据库和独立功能四部分完成24章Redis 3.0实现”。本节点用“第3章 链表”作观察点，执行`git -C redis-3.0-annotated rev-parse HEAD`或等价源码探针，保存版本、输入、前后状态和能推翻解释的反例。

验证第21章 排序时，先预测追踪跨度从“单章闭环”切到“跨四部分”会改变哪个字段、偏移、文件或消息；再固定证据层级做一次对照。若故障注入没有破坏“24章都有正式页面、完整小节、源码结构图、运行实验、失败反例和独立交付物”，就撤回当前解释而不是补写故事。

### 第22章 二进制位数组

**四级证据 22/24。** 第22章 二进制位数组必须在Redis 3.0的server.c中定位结构或函数入口，并说明它怎样参与“沿数据结构与对象、单机数据库、多机数据库和独立功能四部分完成24章Redis 3.0实现”。本节点用“第4章 字典”作观察点，执行`git -C redis-3.0-annotated rev-parse HEAD`或等价源码探针，保存版本、输入、前后状态和能推翻解释的反例。

验证第22章 二进制位数组时，先预测追踪跨度从“单章闭环”切到“跨四部分”会改变哪个字段、偏移、文件或消息；再固定证据层级做一次对照。若故障注入没有破坏“24章都有正式页面、完整小节、源码结构图、运行实验、失败反例和独立交付物”，就撤回当前解释而不是补写故事。

### 第23章 慢查询日志

**四级证据 23/24。** 第23章 慢查询日志必须在Redis 3.0的server.c中定位结构或函数入口，并说明它怎样参与“沿数据结构与对象、单机数据库、多机数据库和独立功能四部分完成24章Redis 3.0实现”。本节点用“第5章 跳跃表”作观察点，执行`git -C redis-3.0-annotated rev-parse HEAD`或等价源码探针，保存版本、输入、前后状态和能推翻解释的反例。

验证第23章 慢查询日志时，先预测追踪跨度从“单章闭环”切到“跨四部分”会改变哪个字段、偏移、文件或消息；再固定证据层级做一次对照。若故障注入没有破坏“24章都有正式页面、完整小节、源码结构图、运行实验、失败反例和独立交付物”，就撤回当前解释而不是补写故事。

### 第24章 监视器

**四级证据 24/24。** 第24章 监视器必须在Redis 3.0的server.c中定位结构或函数入口，并说明它怎样参与“沿数据结构与对象、单机数据库、多机数据库和独立功能四部分完成24章Redis 3.0实现”。本节点用“第6章 整数集合”作观察点，执行`git -C redis-3.0-annotated rev-parse HEAD`或等价源码探针，保存版本、输入、前后状态和能推翻解释的反例。

验证第24章 监视器时，先预测追踪跨度从“单章闭环”切到“跨四部分”会改变哪个字段、偏移、文件或消息；再固定证据层级做一次对照。若故障注入没有破坏“24章都有正式页面、完整小节、源码结构图、运行实验、失败反例和独立交付物”，就撤回当前解释而不是补写故事。

## 最小源码与运行切片

```bash
+git clone --branch 3.0 --depth 1 https://github.com/redis/redis.git redis-3.0
+cd redis-3.0
+git -C redis-3.0-annotated rev-parse HEAD
```

该切片固定Redis 3.0分支、编译器、配置、数据集和命令序列；先保存结构或函数位置，再运行隔离实例。任何持久化损坏、断线、故障转移、大键或高流量实验都必须使用临时数据并规定CPU、内存、磁盘、延迟和停止上限。

```yaml
unit: rdi-official-learning-map
source_file: server.c
axis_a: "追踪跨度"
axis_b: "证据层级"
fault: "把24章当成互不相关的命令清单，无法从一次请求追到持久化与复制"
invariant: "24章都有正式页面、完整小节、源码结构图、运行实验、失败反例和独立交付物"
replay: same_version_same_input
```

## 三个必须主动触发的误区


## 练习、答案与节点验证


## 术语复核与本章回顾


完成第2版权威学习地图意味着能从server.c解释沿数据结构与对象、单机数据库、多机数据库和独立功能四部分完成24章Redis 3.0实现，能运行章专属状态实验，能制造反例并在复位后证明旧状态没有残留。

