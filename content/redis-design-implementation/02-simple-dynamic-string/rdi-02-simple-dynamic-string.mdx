---
title: 第2章 简单动态字符串
description: >-
  从sdshdr的len、free和buf理解常数时间取长、二进制安全、空间预分配与惰性释放 覆盖5个正式节点，并以Redis
  3.0源码、故障和恢复对账验收。
type: C
section: Redis设计与实现（第2版 / Redis 3.0 / 2014）
draft: false
math: false
sourceUrl: 'https://huangz.works/redisbook1e/'
demo: true
order: 0
qualityVersion: 2
practiceMode: calculation
sourceMode: independent-rewrite
---


{/* RDI_QUALITY_V2 */}

## 为什么从“SDS字节与扩容轨迹台”开始

第2章 简单动态字符串的核心任务是从sdshdr的len、free和buf理解常数时间取长、二进制安全、空间预分配与惰性释放。命令返回值只暴露外层合同；实现解释还必须连接内存结构、写入与读取路径、事件顺序以及失败后的旧状态回收。SDS字节与扩容轨迹台把这些关系放进同一条可复位轨迹。

先写预测：当写入长度由“跨预留空间”进入“含零字节”时，哪个可观察状态最先变化？再规定什么结果会推翻当前解释。交互中的分数只表达透明因果方向，不冒充真实Redis测量。

## 来源、版次与独立重写边界

黄健宏[作者读者服务页](https://huangz.works/redisbook1e/)确认正式出版新版以Redis 3.0为源码基线，列出4部分、24章及完整小节，并链接[Redis 3.0中文注释源码](https://github.com/huangzworks/redis-3.0-annotated)。本课程据此映射24个正式单元、145个目录节点，另设学习地图和总复习；未取得出版正文授权，目录只界定范围，不宣称复现原书正文。

本章字段与控制流由[Redis官方3.0源码：sds.c](https://github.com/redis/redis/blob/3.0/src/sds.c)和作者注释源码交叉核对；[Redis当前持久化文档](https://redis.io/docs/latest/operate/oss_and_stack/management/persistence/)只用于辨认版本差异。中文解释、图示、交互、实验与答案均为独立教学重写，不把目录页或代码仓库许可证误报为原书许可证。

## 本章术语与源码合同

、、、、、。

第2章 简单动态字符串必须守住“字符串长度、终止字节和可用空间始终一致，扩容不会溢出，二进制数据不被截断”。观察同时记录SDS字节与扩容轨迹台一致率与空间状态分叉风险；只有结构快照、函数入口、运行结果和故障反例互相一致，才接受实现结论。

## 作者目录逐项深读

### SDS的定义

**四级证据 1/5。** sdshdr在buf前保存len与free，buf仍以零字节结尾，因此可兼容部分C API又能常数时间取长。本节点用“SDS的定义”作观察点，执行`rg 'sdsMakeRoomFor|sdslen|sdsRemoveFreeSpace' src/sds.c`或等价源码探针，保存版本、输入、前后状态和能推翻解释的反例。

验证SDS的定义时，先预测写入长度从“跨预留空间”切到“含零字节”会改变哪个字段、偏移、文件或消息；再固定空间状态做一次对照。若故障注入没有破坏“字符串长度、终止字节和可用空间始终一致，扩容不会溢出，二进制数据不被截断”，就撤回当前解释而不是补写故事。

### SDS与C字符串的区别

**四级证据 2/5。** SDS按len处理二进制数据并在写入前检查容量，避免strlen线性扫描、缓冲区溢出与嵌入零截断。本节点用“SDS与C字符串的区别”作观察点，执行`rg 'sdsMakeRoomFor|sdslen|sdsRemoveFreeSpace' src/sds.c`或等价源码探针，保存版本、输入、前后状态和能推翻解释的反例。

验证SDS与C字符串的区别时，先预测写入长度从“跨预留空间”切到“含零字节”会改变哪个字段、偏移、文件或消息；再固定空间状态做一次对照。若故障注入没有破坏“字符串长度、终止字节和可用空间始终一致，扩容不会溢出，二进制数据不被截断”，就撤回当前解释而不是补写故事。

### SDS API

**四级证据 3/5。** sdsMakeRoomFor先保证空间，拼接更新len/free，sdsRemoveFreeSpace可主动收缩预留区。本节点用“SDS API”作观察点，执行`rg 'sdsMakeRoomFor|sdslen|sdsRemoveFreeSpace' src/sds.c`或等价源码探针，保存版本、输入、前后状态和能推翻解释的反例。

验证SDS API时，先预测写入长度从“跨预留空间”切到“含零字节”会改变哪个字段、偏移、文件或消息；再固定空间状态做一次对照。若故障注入没有破坏“字符串长度、终止字节和可用空间始终一致，扩容不会溢出，二进制数据不被截断”，就撤回当前解释而不是补写故事。

### 重点回顾

**四级证据 4/5。** 第2章 简单动态字符串的回顾要重新证明“字符串长度、终止字节和可用空间始终一致，扩容不会溢出，二进制数据不被截断”，并用同一输入比较结构前态、变更轨迹、故障首错与恢复后态

验证重点回顾时，先预测写入长度从“跨预留空间”切到“含零字节”会改变哪个字段、偏移、文件或消息；再固定空间状态做一次对照。若故障注入没有破坏“字符串长度、终止字节和可用空间始终一致，扩容不会溢出，二进制数据不被截断”，就撤回当前解释而不是补写故事。

### 参考资料

**四级证据 5/5。** 本章目录范围由作者页面确认，字段和控制流回到Redis 3.0的sds.c与黄健宏注释源码核验；新版资料只承担差异说明

验证参考资料时，先预测写入长度从“跨预留空间”切到“含零字节”会改变哪个字段、偏移、文件或消息；再固定空间状态做一次对照。若故障注入没有破坏“字符串长度、终止字节和可用空间始终一致，扩容不会溢出，二进制数据不被截断”，就撤回当前解释而不是补写故事。

## 最小源码与运行切片

```bash
+git clone --branch 3.0 --depth 1 https://github.com/redis/redis.git redis-3.0
+cd redis-3.0
+rg 'sdsMakeRoomFor|sdslen|sdsRemoveFreeSpace' src/sds.c
```

该切片固定Redis 3.0分支、编译器、配置、数据集和命令序列；先保存结构或函数位置，再运行隔离实例。任何持久化损坏、断线、故障转移、大键或高流量实验都必须使用临时数据并规定CPU、内存、磁盘、延迟和停止上限。

```yaml
unit: rdi-02-simple-dynamic-string
source_file: sds.c
axis_a: "写入长度"
axis_b: "空间状态"
fault: "按C字符串的strlen和终止零推断SDS长度，截断二进制载荷"
invariant: "字符串长度、终止字节和可用空间始终一致，扩容不会溢出，二进制数据不被截断"
replay: same_version_same_input
```

## 三个必须主动触发的误区


## 练习、答案与节点验证


## 术语复核与本章回顾


完成第2章 简单动态字符串意味着能从sds.c解释从sdshdr的len、free和buf理解常数时间取长、二进制安全、空间预分配与惰性释放，能运行章专属状态实验，能制造反例并在复位后证明旧状态没有残留。

