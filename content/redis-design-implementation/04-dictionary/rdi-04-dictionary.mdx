---
title: 第4章 字典
description: 追踪dict、dictht和dictEntry的哈希、冲突链、双表rehash与渐进迁移 覆盖7个正式节点，并以Redis 3.0源码、故障和恢复对账验收。
type: C
section: Redis设计与实现（第2版 / Redis 3.0 / 2014）
draft: true
math: false
sourceUrl: 'https://huangz.works/redisbook1e/'
demo: true
order: 0
qualityVersion: 2
practiceMode: simulation
sourceMode: independent-rewrite
---
import {
  Rdi04DictionaryStructureLab,
  Rdi04DictionaryTraceLab,
  Rdi04DictionaryEvidenceLab,
} from "@/components/mdx/redis-design-implementation/diagrams/rdi-04-dictionary";
import { Objectives, Callout, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step, Attribution } from "@/components/mdx/mdx-components";

<Objectives>

- 能在Redis 3.0源码中解释追踪dict、dictht和dictEntry的哈希、冲突链、双表rehash与渐进迁移
- 能沿冻结3.0基线、定位结构入口、执行单变量变更、注入边界故障、恢复并对账重建第4章 字典的字段与控制流
- 能操作双表渐进rehash台，一次只改变负载状态或访问动作
- 能注入“rehash期间只查询ht[0]，或一次迁移整表造成长停顿”，用“迁移期间查找覆盖两张表，rehashidx单调推进，键只存在于正确位置且负载因子受控”判断恢复是否通过

</Objectives>

{/* RDI_QUALITY_V2 */}

## 为什么从“双表渐进rehash台”开始

第4章 字典的核心任务是追踪dict、dictht和dictEntry的哈希、冲突链、双表rehash与渐进迁移。命令返回值只暴露外层合同；实现解释还必须连接内存结构、写入与读取路径、事件顺序以及失败后的旧状态回收。双表渐进rehash台把这些关系放进同一条可复位轨迹。

先写预测：当负载状态由“触发扩缩”进入“正在迁移”时，哪个可观察状态最先变化？再规定什么结果会推翻当前解释。交互中的分数只表达透明因果方向，不冒充真实Redis测量。

## 来源、版次与独立重写边界

黄健宏[作者读者服务页](https://huangz.works/redisbook1e/)确认正式出版新版以Redis 3.0为源码基线，列出4部分、24章及完整小节，并链接[Redis 3.0中文注释源码](https://github.com/huangzworks/redis-3.0-annotated)。本课程据此映射24个正式单元、145个目录节点，另设学习地图和总复习；未取得出版正文授权，目录只界定范围，不宣称复现原书正文。

本章字段与控制流由[Redis官方3.0源码：dict.c](https://github.com/redis/redis/blob/3.0/src/dict.c)和作者注释源码交叉核对；[Redis当前持久化文档](https://redis.io/docs/latest/operate/oss_and_stack/management/persistence/)只用于辨认版本差异。中文解释、图示、交互、实验与答案均为独立教学重写，不把目录页或代码仓库许可证误报为原书许可证。

## 本章术语与源码合同

<Term def="字典的实现是第4章 字典中连接冻结3.0基线与“迁移期间查找覆盖两张表，rehashidx单调推进，键只存在于正确位置且负载因子受控”的源码坐标；必须以Redis 3.0字段、函数或运行输出定义。">字典的实现</Term>、<Term def="哈希算法是第4章 字典中连接定位结构入口与“迁移期间查找覆盖两张表，rehashidx单调推进，键只存在于正确位置且负载因子受控”的源码坐标；必须以Redis 3.0字段、函数或运行输出定义。">哈希算法</Term>、<Term def="解决键冲突是第4章 字典中连接执行单变量变更与“迁移期间查找覆盖两张表，rehashidx单调推进，键只存在于正确位置且负载因子受控”的源码坐标；必须以Redis 3.0字段、函数或运行输出定义。">解决键冲突</Term>、<Term def="rehash是第4章 字典中连接注入边界故障与“迁移期间查找覆盖两张表，rehashidx单调推进，键只存在于正确位置且负载因子受控”的源码坐标；必须以Redis 3.0字段、函数或运行输出定义。">rehash</Term>、<Term def="渐进式rehash是第4章 字典中连接恢复并对账与“迁移期间查找覆盖两张表，rehashidx单调推进，键只存在于正确位置且负载因子受控”的源码坐标；必须以Redis 3.0字段、函数或运行输出定义。">渐进式rehash</Term>、<Term def="字典API是第4章 字典中连接冻结3.0基线与“迁移期间查找覆盖两张表，rehashidx单调推进，键只存在于正确位置且负载因子受控”的源码坐标；必须以Redis 3.0字段、函数或运行输出定义。">字典API</Term>。

第4章 字典必须守住“迁移期间查找覆盖两张表，rehashidx单调推进，键只存在于正确位置且负载因子受控”。观察同时记录双表渐进rehash台一致率与访问动作分叉风险；只有结构快照、函数入口、运行结果和故障反例互相一致，才接受实现结论。

## 先预测，再操作三层章专属实验

<Stepper>
  <Step title="1. 目录节点与结构地图">
    选择正式节点，指出它在dict.c中的结构或函数入口、上游输入和下游状态。

    <Rdi04DictionaryStructureLab />
  </Step>
  <Step title="2. 单变量状态轨迹">
    固定Redis 3.0版本与输入，只切换负载状态或访问动作，比较变更前后状态。

    <Rdi04DictionaryTraceLab />
  </Step>
  <Step title="3. 故障、恢复与复位">
    注入“rehash期间只查询ht[0]，或一次迁移整表造成长停顿”，保存首个不变量破坏点；修复后用同一输入重放并点击重置。

    <Rdi04DictionaryEvidenceLab />
  </Step>
</Stepper>

## 作者目录逐项深读

### 字典的实现

**四级证据 1/7。** dict持有ht[0]、ht[1]与rehashidx；dictht保存桶数组、大小、掩码和已用节点数，dictEntry以链表解决冲突。本节点用“字典的实现”作观察点，执行`rg 'dictRehash|_dictKeyIndex|dictFind' src/dict.c`或等价源码探针，保存版本、输入、前后状态和能推翻解释的反例。

验证字典的实现时，先预测负载状态从“触发扩缩”切到“正在迁移”会改变哪个字段、偏移、文件或消息；再固定访问动作做一次对照。若故障注入没有破坏“迁移期间查找覆盖两张表，rehashidx单调推进，键只存在于正确位置且负载因子受控”，就撤回当前解释而不是补写故事。

### 哈希算法

**四级证据 2/7。** 哈希值与sizemask按位与得到桶索引；扩容后的掩码改变，所以迁移必须重新计算目标桶。本节点用“哈希算法”作观察点，执行`rg 'dictRehash|_dictKeyIndex|dictFind' src/dict.c`或等价源码探针，保存版本、输入、前后状态和能推翻解释的反例。

验证哈希算法时，先预测负载状态从“触发扩缩”切到“正在迁移”会改变哪个字段、偏移、文件或消息；再固定访问动作做一次对照。若故障注入没有破坏“迁移期间查找覆盖两张表，rehashidx单调推进，键只存在于正确位置且负载因子受控”，就撤回当前解释而不是补写故事。

### 解决键冲突

**四级证据 3/7。** 同桶键以链地址法连接，查找还要用类型回调比较键，不能只比较哈希值。本节点用“解决键冲突”作观察点，执行`rg 'dictRehash|_dictKeyIndex|dictFind' src/dict.c`或等价源码探针，保存版本、输入、前后状态和能推翻解释的反例。

验证解决键冲突时，先预测负载状态从“触发扩缩”切到“正在迁移”会改变哪个字段、偏移、文件或消息；再固定访问动作做一次对照。若故障注入没有破坏“迁移期间查找覆盖两张表，rehashidx单调推进，键只存在于正确位置且负载因子受控”，就撤回当前解释而不是补写故事。

### rehash

**四级证据 4/7。** rehash建立ht[1]并迁移ht[0]桶；完成后释放旧表、把ht[1]提升为ht[0]并把rehashidx复位为-1。本节点用“rehash”作观察点，执行`rg 'dictRehash|_dictKeyIndex|dictFind' src/dict.c`或等价源码探针，保存版本、输入、前后状态和能推翻解释的反例。

验证rehash时，先预测负载状态从“触发扩缩”切到“正在迁移”会改变哪个字段、偏移、文件或消息；再固定访问动作做一次对照。若故障注入没有破坏“迁移期间查找覆盖两张表，rehashidx单调推进，键只存在于正确位置且负载因子受控”，就撤回当前解释而不是补写故事。

### 渐进式rehash

**四级证据 5/7。** 普通字典操作顺带迁移有限桶；迁移期新增只写ht[1]，查找和删除需要覆盖两表。本节点用“渐进式rehash”作观察点，执行`rg 'dictRehash|_dictKeyIndex|dictFind' src/dict.c`或等价源码探针，保存版本、输入、前后状态和能推翻解释的反例。

验证渐进式rehash时，先预测负载状态从“触发扩缩”切到“正在迁移”会改变哪个字段、偏移、文件或消息；再固定访问动作做一次对照。若故障注入没有破坏“迁移期间查找覆盖两张表，rehashidx单调推进，键只存在于正确位置且负载因子受控”，就撤回当前解释而不是补写故事。

### 字典API

**四级证据 6/7。** dictAdd、dictFind、dictDelete和迭代器共同受rehash状态约束，安全迭代器会阻止不兼容的迁移动作。本节点用“字典API”作观察点，执行`rg 'dictRehash|_dictKeyIndex|dictFind' src/dict.c`或等价源码探针，保存版本、输入、前后状态和能推翻解释的反例。

验证字典API时，先预测负载状态从“触发扩缩”切到“正在迁移”会改变哪个字段、偏移、文件或消息；再固定访问动作做一次对照。若故障注入没有破坏“迁移期间查找覆盖两张表，rehashidx单调推进，键只存在于正确位置且负载因子受控”，就撤回当前解释而不是补写故事。

### 重点回顾

**四级证据 7/7。** 第4章 字典的回顾要重新证明“迁移期间查找覆盖两张表，rehashidx单调推进，键只存在于正确位置且负载因子受控”，并用同一输入比较结构前态、变更轨迹、故障首错与恢复后态

验证重点回顾时，先预测负载状态从“触发扩缩”切到“正在迁移”会改变哪个字段、偏移、文件或消息；再固定访问动作做一次对照。若故障注入没有破坏“迁移期间查找覆盖两张表，rehashidx单调推进，键只存在于正确位置且负载因子受控”，就撤回当前解释而不是补写故事。

## 最小源码与运行切片

```bash
+git clone --branch 3.0 --depth 1 https://github.com/redis/redis.git redis-3.0
+cd redis-3.0
+rg 'dictRehash|_dictKeyIndex|dictFind' src/dict.c
```

该切片固定Redis 3.0分支、编译器、配置、数据集和命令序列；先保存结构或函数位置，再运行隔离实例。任何持久化损坏、断线、故障转移、大键或高流量实验都必须使用临时数据并规定CPU、内存、磁盘、延迟和停止上限。

```yaml
unit: rdi-04-dictionary
source_file: dict.c
axis_a: "负载状态"
axis_b: "访问动作"
fault: "rehash期间只查询ht[0]，或一次迁移整表造成长停顿"
invariant: "迁移期间查找覆盖两张表，rehashidx单调推进，键只存在于正确位置且负载因子受控"
replay: same_version_same_input
```

## 三个必须主动触发的误区

<Callout type="trap" title="命令表象不等于实现">
  第4章 字典不能从一次redis-cli结果反推全部控制流；必须在dict.c定位结构、写入路径和读取路径，并保存能推翻解释的边界输入。
</Callout>

<Callout type="trap" title="版本倒灌">
  Redis 4.0以后新增或替换的结构与功能不能倒灌到3.0。新版资料只作差异说明，正式验收仍以本章源码文件与作者目录为准。
</Callout>

<Callout type="trap" title="恢复不等于进程在线">
  注入rehash期间只查询ht[0]，或一次迁移整表造成长停顿后，要用同一命令序列核对结构、回复、文件或偏移以及最终键值；只有“迁移期间查找覆盖两张表，rehashidx单调推进，键只存在于正确位置且负载因子受控”恢复才算通过。
</Callout>

## 练习、答案与节点验证

<Exercises>

**问题 1：单变量因果。** 怎样验证负载状态而不让访问动作混入结论？

<Answer>固定Redis 3.0提交、配置、数据与访问动作，只把负载状态从“触发扩缩”切到“正在迁移”；比较前后字段、函数轨迹、命令结果与访问动作分叉风险。</Answer>

**问题 2：逐节点四级证据。** 怎样证明7个正式节点不是只在目录中出现？

<Answer>
1. 字典的实现：在dict.c定位字典的实现，保存出现、解释、实验和结果断言四项证据。
2. 哈希算法：在dict.c定位哈希算法，保存出现、解释、实验和结果断言四项证据。
3. 解决键冲突：在dict.c定位解决键冲突，保存出现、解释、实验和结果断言四项证据。
4. rehash：在dict.c定位rehash，保存出现、解释、实验和结果断言四项证据。
5. 渐进式rehash：在dict.c定位渐进式rehash，保存出现、解释、实验和结果断言四项证据。
6. 字典API：在dict.c定位字典API，保存出现、解释、实验和结果断言四项证据。
7. 重点回顾：在dict.c定位字典的实现，保存出现、解释、实验和结果断言四项证据。
</Answer>

**问题 3：故障恢复。** 如何证明“rehash期间只查询ht[0]，或一次迁移整表造成长停顿”已经被修复？

<Answer>沿冻结3.0基线、定位结构入口、执行单变量变更、注入边界故障、恢复并对账找到第一个破坏点，只改变最小因果前提；随后以同版本同输入重放稳定、故障、恢复和复位四条轨迹，并让未参与者依据双哈希表图、冲突链样本、渐进rehash轨迹与负载测试独立确认“迁移期间查找覆盖两张表，rehashidx单调推进，键只存在于正确位置且负载因子受控”。</Answer>

</Exercises>

## 术语复核与本章回顾

<Glossary>
  <GlossaryItem term="字典的实现">字典的实现是第4章 字典中连接冻结3.0基线与“迁移期间查找覆盖两张表，rehashidx单调推进，键只存在于正确位置且负载因子受控”的源码坐标；必须以Redis 3.0字段、函数或运行输出定义。</GlossaryItem>
  <GlossaryItem term="哈希算法">哈希算法是第4章 字典中连接定位结构入口与“迁移期间查找覆盖两张表，rehashidx单调推进，键只存在于正确位置且负载因子受控”的源码坐标；必须以Redis 3.0字段、函数或运行输出定义。</GlossaryItem>
  <GlossaryItem term="解决键冲突">解决键冲突是第4章 字典中连接执行单变量变更与“迁移期间查找覆盖两张表，rehashidx单调推进，键只存在于正确位置且负载因子受控”的源码坐标；必须以Redis 3.0字段、函数或运行输出定义。</GlossaryItem>
  <GlossaryItem term="rehash">rehash是第4章 字典中连接注入边界故障与“迁移期间查找覆盖两张表，rehashidx单调推进，键只存在于正确位置且负载因子受控”的源码坐标；必须以Redis 3.0字段、函数或运行输出定义。</GlossaryItem>
  <GlossaryItem term="渐进式rehash">渐进式rehash是第4章 字典中连接恢复并对账与“迁移期间查找覆盖两张表，rehashidx单调推进，键只存在于正确位置且负载因子受控”的源码坐标；必须以Redis 3.0字段、函数或运行输出定义。</GlossaryItem>
  <GlossaryItem term="字典API">字典API是第4章 字典中连接冻结3.0基线与“迁移期间查找覆盖两张表，rehashidx单调推进，键只存在于正确位置且负载因子受控”的源码坐标；必须以Redis 3.0字段、函数或运行输出定义。</GlossaryItem>
</Glossary>

完成第4章 字典意味着能从dict.c解释追踪dict、dictht和dictEntry的哈希、冲突链、双表rehash与渐进迁移，能运行章专属状态实验，能制造反例并在复位后证明旧状态没有残留。

<Attribution
  mode="independent-rewrite"
  sourceBasis="outline-only"
  workTitle="黄健宏《Redis设计与实现》（正式出版新版，Redis 3.0源码基线）"
  adaptedUrl="https://huangz.works/redisbook1e/"
/>
