---
title: 第17章 集群
description: >-
  用16384槽、节点握手、MOVED与ASK、重新分片、复制和Gossip消息解释Redis Cluster 覆盖8个正式节点，并以Redis
  3.0源码、故障和恢复对账验收。
type: C
section: Redis设计与实现（第2版 / Redis 3.0 / 2014）
draft: true
math: false
sourceUrl: 'https://huangz.works/redisbook1e/'
demo: true
order: 0
qualityVersion: 2
practiceMode: simulation
sourceMode: independent-rewrite
---
import { Objectives, Callout, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step, Attribution } from "@/components/mdx/mdx-components";

<Objectives>

- 能在Redis 3.0源码中解释用16384槽、节点握手、MOVED与ASK、重新分片、复制和Gossip消息解释Redis Cluster
- 能沿冻结3.0基线、定位结构入口、执行单变量变更、注入边界故障、恢复并对账重建第17章 集群的字段与控制流
- 能操作16384槽迁移路由台，一次只改变槽状态或请求位置
- 能注入“迁槽时混淆ASK与MOVED，或让同一槽同时拥有两个可写主”，用“每个槽恰有有效所有者，迁移状态可路由请求，故障转移不产生两个合法写主”判断恢复是否通过

</Objectives>

{/* RDI_QUALITY_V2 */}

## 为什么从“16384槽迁移路由台”开始

第17章 集群的核心任务是用16384槽、节点握手、MOVED与ASK、重新分片、复制和Gossip消息解释Redis Cluster。命令返回值只暴露外层合同；实现解释还必须连接内存结构、写入与读取路径、事件顺序以及失败后的旧状态回收。16384槽迁移路由台把这些关系放进同一条可复位轨迹。

先写预测：当槽状态由“migrating/importing”进入“已转移”时，哪个可观察状态最先变化？再规定什么结果会推翻当前解释。交互中的分数只表达透明因果方向，不冒充真实Redis测量。

## 来源、版次与独立重写边界

黄健宏[作者读者服务页](https://huangz.works/redisbook1e/)确认正式出版新版以Redis 3.0为源码基线，列出4部分、24章及完整小节，并链接[Redis 3.0中文注释源码](https://github.com/huangzworks/redis-3.0-annotated)。本课程据此映射24个正式单元、145个目录节点，另设学习地图和总复习；未取得出版正文授权，目录只界定范围，不宣称复现原书正文。

本章字段与控制流由[Redis官方3.0源码：cluster.c](https://github.com/redis/redis/blob/3.0/src/cluster.c)和作者注释源码交叉核对；[Redis当前持久化文档](https://redis.io/docs/latest/operate/oss_and_stack/management/persistence/)只用于辨认版本差异。中文解释、图示、交互、实验与答案均为独立教学重写，不把目录页或代码仓库许可证误报为原书许可证。

## 本章术语与源码合同

<Term def="节点是第17章 集群中连接冻结3.0基线与“每个槽恰有有效所有者，迁移状态可路由请求，故障转移不产生两个合法写主”的源码坐标；必须以Redis 3.0字段、函数或运行输出定义。">节点</Term>、<Term def="槽指派是第17章 集群中连接定位结构入口与“每个槽恰有有效所有者，迁移状态可路由请求，故障转移不产生两个合法写主”的源码坐标；必须以Redis 3.0字段、函数或运行输出定义。">槽指派</Term>、<Term def="在集群中执行命令是第17章 集群中连接执行单变量变更与“每个槽恰有有效所有者，迁移状态可路由请求，故障转移不产生两个合法写主”的源码坐标；必须以Redis 3.0字段、函数或运行输出定义。">在集群中执行命令</Term>、<Term def="重新分片是第17章 集群中连接注入边界故障与“每个槽恰有有效所有者，迁移状态可路由请求，故障转移不产生两个合法写主”的源码坐标；必须以Redis 3.0字段、函数或运行输出定义。">重新分片</Term>、<Term def="ASK错误是第17章 集群中连接恢复并对账与“每个槽恰有有效所有者，迁移状态可路由请求，故障转移不产生两个合法写主”的源码坐标；必须以Redis 3.0字段、函数或运行输出定义。">ASK错误</Term>、<Term def="复制与故障转移是第17章 集群中连接冻结3.0基线与“每个槽恰有有效所有者，迁移状态可路由请求，故障转移不产生两个合法写主”的源码坐标；必须以Redis 3.0字段、函数或运行输出定义。">复制与故障转移</Term>。

第17章 集群必须守住“每个槽恰有有效所有者，迁移状态可路由请求，故障转移不产生两个合法写主”。观察同时记录16384槽迁移路由台一致率与请求位置分叉风险；只有结构快照、函数入口、运行结果和故障反例互相一致，才接受实现结论。

## 作者目录逐项深读

### 节点

**四级证据 1/8。** clusterNode记录名字、纪元、地址、标志、槽位与连接；握手消息把新节点加入已知拓扑。本节点用“节点”作观察点，执行`rg 'getNodeByQuery|clusterRedirectClient|clusterCron' src/cluster.c`或等价源码探针，保存版本、输入、前后状态和能推翻解释的反例。

验证节点时，先预测槽状态从“migrating/importing”切到“已转移”会改变哪个字段、偏移、文件或消息；再固定请求位置做一次对照。若故障注入没有破坏“每个槽恰有有效所有者，迁移状态可路由请求，故障转移不产生两个合法写主”，就撤回当前解释而不是补写故事。

### 槽指派

**四级证据 2/8。** 集群固定16384槽，节点位图与全局slots映射必须一致；槽归属通过配置纪元解决新旧声明。本节点用“槽指派”作观察点，执行`rg 'getNodeByQuery|clusterRedirectClient|clusterCron' src/cluster.c`或等价源码探针，保存版本、输入、前后状态和能推翻解释的反例。

验证槽指派时，先预测槽状态从“migrating/importing”切到“已转移”会改变哪个字段、偏移、文件或消息；再固定请求位置做一次对照。若故障注入没有破坏“每个槽恰有有效所有者，迁移状态可路由请求，故障转移不产生两个合法写主”，就撤回当前解释而不是补写故事。

### 在集群中执行命令

**四级证据 3/8。** 节点先计算所有键的槽并检查归属；稳定槽返回MOVED，迁移中的局部键可能引导ASK。本节点用“在集群中执行命令”作观察点，执行`rg 'getNodeByQuery|clusterRedirectClient|clusterCron' src/cluster.c`或等价源码探针，保存版本、输入、前后状态和能推翻解释的反例。

验证在集群中执行命令时，先预测槽状态从“migrating/importing”切到“已转移”会改变哪个字段、偏移、文件或消息；再固定请求位置做一次对照。若故障注入没有破坏“每个槽恰有有效所有者，迁移状态可路由请求，故障转移不产生两个合法写主”，就撤回当前解释而不是补写故事。

### 重新分片

**四级证据 4/8。** 迁槽依次设置importing/migrating状态并搬迁键，完成后广播新所有者，过程不能丢失路由边界。本节点用“重新分片”作观察点，执行`rg 'getNodeByQuery|clusterRedirectClient|clusterCron' src/cluster.c`或等价源码探针，保存版本、输入、前后状态和能推翻解释的反例。

验证重新分片时，先预测槽状态从“migrating/importing”切到“已转移”会改变哪个字段、偏移、文件或消息；再固定请求位置做一次对照。若故障注入没有破坏“每个槽恰有有效所有者，迁移状态可路由请求，故障转移不产生两个合法写主”，就撤回当前解释而不是补写故事。

### ASK错误

**四级证据 5/8。** ASK只授权客户端对下一条命令先发ASKING访问目标节点，不应像MOVED那样永久更新槽缓存。本节点用“ASK错误”作观察点，执行`rg 'getNodeByQuery|clusterRedirectClient|clusterCron' src/cluster.c`或等价源码探针，保存版本、输入、前后状态和能推翻解释的反例。

验证ASK错误时，先预测槽状态从“migrating/importing”切到“已转移”会改变哪个字段、偏移、文件或消息；再固定请求位置做一次对照。若故障注入没有破坏“每个槽恰有有效所有者，迁移状态可路由请求，故障转移不产生两个合法写主”，就撤回当前解释而不是补写故事。

### 复制与故障转移

**四级证据 6/8。** 领头者选择合格副本晋升，重配其余副本并最终让旧主恢复后成为新主副本。本节点用“复制与故障转移”作观察点，执行`rg 'getNodeByQuery|clusterRedirectClient|clusterCron' src/cluster.c`或等价源码探针，保存版本、输入、前后状态和能推翻解释的反例。

验证复制与故障转移时，先预测槽状态从“migrating/importing”切到“已转移”会改变哪个字段、偏移、文件或消息；再固定请求位置做一次对照。若故障注入没有破坏“每个槽恰有有效所有者，迁移状态可路由请求，故障转移不产生两个合法写主”，就撤回当前解释而不是补写故事。

### 消息

**四级证据 7/8。** cluster bus的PING、PONG、MEET与FAIL等消息传播节点状态、槽位和故障意见，Gossip是最终收敛信号而非瞬时真相。本节点用“节点”作观察点，执行`rg 'getNodeByQuery|clusterRedirectClient|clusterCron' src/cluster.c`或等价源码探针，保存版本、输入、前后状态和能推翻解释的反例。

验证消息时，先预测槽状态从“migrating/importing”切到“已转移”会改变哪个字段、偏移、文件或消息；再固定请求位置做一次对照。若故障注入没有破坏“每个槽恰有有效所有者，迁移状态可路由请求，故障转移不产生两个合法写主”，就撤回当前解释而不是补写故事。

### 重点回顾

**四级证据 8/8。** 第17章 集群的回顾要重新证明“每个槽恰有有效所有者，迁移状态可路由请求，故障转移不产生两个合法写主”，并用同一输入比较结构前态、变更轨迹、故障首错与恢复后态

验证重点回顾时，先预测槽状态从“migrating/importing”切到“已转移”会改变哪个字段、偏移、文件或消息；再固定请求位置做一次对照。若故障注入没有破坏“每个槽恰有有效所有者，迁移状态可路由请求，故障转移不产生两个合法写主”，就撤回当前解释而不是补写故事。

## 最小源码与运行切片

```bash
+git clone --branch 3.0 --depth 1 https://github.com/redis/redis.git redis-3.0
+cd redis-3.0
+rg 'getNodeByQuery|clusterRedirectClient|clusterCron' src/cluster.c
```

该切片固定Redis 3.0分支、编译器、配置、数据集和命令序列；先保存结构或函数位置，再运行隔离实例。任何持久化损坏、断线、故障转移、大键或高流量实验都必须使用临时数据并规定CPU、内存、磁盘、延迟和停止上限。

```yaml
unit: rdi-17-cluster
source_file: cluster.c
axis_a: "槽状态"
axis_b: "请求位置"
fault: "迁槽时混淆ASK与MOVED，或让同一槽同时拥有两个可写主"
invariant: "每个槽恰有有效所有者，迁移状态可路由请求，故障转移不产生两个合法写主"
replay: same_version_same_input
```

## 三个必须主动触发的误区

<Callout type="trap" title="命令表象不等于实现">
  第17章 集群不能从一次redis-cli结果反推全部控制流；必须在cluster.c定位结构、写入路径和读取路径，并保存能推翻解释的边界输入。
</Callout>

<Callout type="trap" title="版本倒灌">
  Redis 4.0以后新增或替换的结构与功能不能倒灌到3.0。新版资料只作差异说明，正式验收仍以本章源码文件与作者目录为准。
</Callout>

<Callout type="trap" title="恢复不等于进程在线">
  注入迁槽时混淆ASK与MOVED，或让同一槽同时拥有两个可写主后，要用同一命令序列核对结构、回复、文件或偏移以及最终键值；只有“每个槽恰有有效所有者，迁移状态可路由请求，故障转移不产生两个合法写主”恢复才算通过。
</Callout>

## 练习、答案与节点验证

<Exercises>

**问题 1：单变量因果。** 怎样验证槽状态而不让请求位置混入结论？

<Answer>固定Redis 3.0提交、配置、数据与请求位置，只把槽状态从“migrating/importing”切到“已转移”；比较前后字段、函数轨迹、命令结果与请求位置分叉风险。</Answer>

**问题 2：逐节点四级证据。** 怎样证明8个正式节点不是只在目录中出现？

<Answer>
1. 节点：在cluster.c定位节点，保存出现、解释、实验和结果断言四项证据。
2. 槽指派：在cluster.c定位槽指派，保存出现、解释、实验和结果断言四项证据。
3. 在集群中执行命令：在cluster.c定位在集群中执行命令，保存出现、解释、实验和结果断言四项证据。
4. 重新分片：在cluster.c定位重新分片，保存出现、解释、实验和结果断言四项证据。
5. ASK错误：在cluster.c定位ASK错误，保存出现、解释、实验和结果断言四项证据。
6. 复制与故障转移：在cluster.c定位复制与故障转移，保存出现、解释、实验和结果断言四项证据。
7. 消息：在cluster.c定位节点，保存出现、解释、实验和结果断言四项证据。
8. 重点回顾：在cluster.c定位槽指派，保存出现、解释、实验和结果断言四项证据。
</Answer>

**问题 3：故障恢复。** 如何证明“迁槽时混淆ASK与MOVED，或让同一槽同时拥有两个可写主”已经被修复？

<Answer>沿冻结3.0基线、定位结构入口、执行单变量变更、注入边界故障、恢复并对账找到第一个破坏点，只改变最小因果前提；随后以同版本同输入重放稳定、故障、恢复和复位四条轨迹，并让未参与者依据槽位图、MOVED/ASK轨迹、在线迁槽演练、Gossip与故障转移记录独立确认“每个槽恰有有效所有者，迁移状态可路由请求，故障转移不产生两个合法写主”。</Answer>

</Exercises>

## 术语复核与本章回顾

<Glossary>
  <GlossaryItem term="节点">节点是第17章 集群中连接冻结3.0基线与“每个槽恰有有效所有者，迁移状态可路由请求，故障转移不产生两个合法写主”的源码坐标；必须以Redis 3.0字段、函数或运行输出定义。</GlossaryItem>
  <GlossaryItem term="槽指派">槽指派是第17章 集群中连接定位结构入口与“每个槽恰有有效所有者，迁移状态可路由请求，故障转移不产生两个合法写主”的源码坐标；必须以Redis 3.0字段、函数或运行输出定义。</GlossaryItem>
  <GlossaryItem term="在集群中执行命令">在集群中执行命令是第17章 集群中连接执行单变量变更与“每个槽恰有有效所有者，迁移状态可路由请求，故障转移不产生两个合法写主”的源码坐标；必须以Redis 3.0字段、函数或运行输出定义。</GlossaryItem>
  <GlossaryItem term="重新分片">重新分片是第17章 集群中连接注入边界故障与“每个槽恰有有效所有者，迁移状态可路由请求，故障转移不产生两个合法写主”的源码坐标；必须以Redis 3.0字段、函数或运行输出定义。</GlossaryItem>
  <GlossaryItem term="ASK错误">ASK错误是第17章 集群中连接恢复并对账与“每个槽恰有有效所有者，迁移状态可路由请求，故障转移不产生两个合法写主”的源码坐标；必须以Redis 3.0字段、函数或运行输出定义。</GlossaryItem>
  <GlossaryItem term="复制与故障转移">复制与故障转移是第17章 集群中连接冻结3.0基线与“每个槽恰有有效所有者，迁移状态可路由请求，故障转移不产生两个合法写主”的源码坐标；必须以Redis 3.0字段、函数或运行输出定义。</GlossaryItem>
</Glossary>

完成第17章 集群意味着能从cluster.c解释用16384槽、节点握手、MOVED与ASK、重新分片、复制和Gossip消息解释Redis Cluster，能运行章专属状态实验，能制造反例并在复位后证明旧状态没有残留。

<Attribution
  mode="independent-rewrite"
  sourceBasis="outline-only"
  workTitle="黄健宏《Redis设计与实现》（正式出版新版，Redis 3.0源码基线）"
  adaptedUrl="https://huangz.works/redisbook1e/"
/>
