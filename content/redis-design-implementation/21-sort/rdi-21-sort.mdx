---
title: 第21章 排序
description: >-
  还原SORT对象数组、数值与字典序比较、BY外部键、LIMIT、GET、STORE和选项执行顺序 覆盖10个正式节点，并以Redis
  3.0源码、故障和恢复对账验收。
type: C
section: Redis设计与实现（第2版 / Redis 3.0 / 2014）
draft: true
math: false
sourceUrl: 'https://huangz.works/redisbook1e/'
demo: true
order: 0
qualityVersion: 2
practiceMode: simulation
sourceMode: independent-rewrite
---
import {
  Rdi21SortStructureLab,
  Rdi21SortTraceLab,
  Rdi21SortEvidenceLab,
} from "@/components/mdx/redis-design-implementation/diagrams/rdi-21-sort";
import { Objectives, Callout, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step, Attribution } from "@/components/mdx/mdx-components";

<Objectives>

- 能在Redis 3.0源码中解释还原SORT对象数组、数值与字典序比较、BY外部键、LIMIT、GET、STORE和选项执行顺序
- 能沿冻结3.0基线、定位结构入口、执行单变量变更、注入边界故障、恢复并对账重建第21章 排序的字段与控制流
- 能操作SORT选项执行管线台，一次只改变比较方式或输出阶段
- 能注入“按参数书写顺序执行所有选项，导致比较、分页、投影和保存的语义错位”，用“输入元素、比较键、排序方向、分页与输出投影按固定顺序组合，缺失外部键语义一致”判断恢复是否通过

</Objectives>

{/* RDI_QUALITY_V2 */}

## 为什么从“SORT选项执行管线台”开始

第21章 排序的核心任务是还原SORT对象数组、数值与字典序比较、BY外部键、LIMIT、GET、STORE和选项执行顺序。命令返回值只暴露外层合同；实现解释还必须连接内存结构、写入与读取路径、事件顺序以及失败后的旧状态回收。SORT选项执行管线台把这些关系放进同一条可复位轨迹。

先写预测：当比较方式由“ALPHA”进入“BY外部键”时，哪个可观察状态最先变化？再规定什么结果会推翻当前解释。交互中的分数只表达透明因果方向，不冒充真实Redis测量。

## 来源、版次与独立重写边界

黄健宏[作者读者服务页](https://huangz.works/redisbook1e/)确认正式出版新版以Redis 3.0为源码基线，列出4部分、24章及完整小节，并链接[Redis 3.0中文注释源码](https://github.com/huangzworks/redis-3.0-annotated)。本课程据此映射24个正式单元、145个目录节点，另设学习地图和总复习；未取得出版正文授权，目录只界定范围，不宣称复现原书正文。

本章字段与控制流由[Redis官方3.0源码：sort.c](https://github.com/redis/redis/blob/3.0/src/sort.c)和作者注释源码交叉核对；[Redis当前持久化文档](https://redis.io/docs/latest/operate/oss_and_stack/management/persistence/)只用于辨认版本差异。中文解释、图示、交互、实验与答案均为独立教学重写，不把目录页或代码仓库许可证误报为原书许可证。

## 本章术语与源码合同

<Term def="SORT &lt;key&gt;命令的实现是第21章 排序中连接冻结3.0基线与“输入元素、比较键、排序方向、分页与输出投影按固定顺序组合，缺失外部键语义一致”的源码坐标；必须以Redis 3.0字段、函数或运行输出定义。">SORT &lt;key&gt;命令的实现</Term>、<Term def="ALPHA选项的实现是第21章 排序中连接定位结构入口与“输入元素、比较键、排序方向、分页与输出投影按固定顺序组合，缺失外部键语义一致”的源码坐标；必须以Redis 3.0字段、函数或运行输出定义。">ALPHA选项的实现</Term>、<Term def="ASC选项和DESC选项的实现是第21章 排序中连接执行单变量变更与“输入元素、比较键、排序方向、分页与输出投影按固定顺序组合，缺失外部键语义一致”的源码坐标；必须以Redis 3.0字段、函数或运行输出定义。">ASC选项和DESC选项的实现</Term>、<Term def="BY选项的实现是第21章 排序中连接注入边界故障与“输入元素、比较键、排序方向、分页与输出投影按固定顺序组合，缺失外部键语义一致”的源码坐标；必须以Redis 3.0字段、函数或运行输出定义。">BY选项的实现</Term>、<Term def="带有ALPHA选项的BY选项的实现是第21章 排序中连接恢复并对账与“输入元素、比较键、排序方向、分页与输出投影按固定顺序组合，缺失外部键语义一致”的源码坐标；必须以Redis 3.0字段、函数或运行输出定义。">带有ALPHA选项的BY选项的实现</Term>、<Term def="LIMIT选项的实现是第21章 排序中连接冻结3.0基线与“输入元素、比较键、排序方向、分页与输出投影按固定顺序组合，缺失外部键语义一致”的源码坐标；必须以Redis 3.0字段、函数或运行输出定义。">LIMIT选项的实现</Term>。

第21章 排序必须守住“输入元素、比较键、排序方向、分页与输出投影按固定顺序组合，缺失外部键语义一致”。观察同时记录SORT选项执行管线台一致率与输出阶段分叉风险；只有结构快照、函数入口、运行结果和故障反例互相一致，才接受实现结论。

## 先预测，再操作三层章专属实验

<Stepper>
  <Step title="1. 目录节点与结构地图">
    选择正式节点，指出它在sort.c中的结构或函数入口、上游输入和下游状态。

    <Rdi21SortStructureLab />
  </Step>
  <Step title="2. 单变量状态轨迹">
    固定Redis 3.0版本与输入，只切换比较方式或输出阶段，比较变更前后状态。

    <Rdi21SortTraceLab />
  </Step>
  <Step title="3. 故障、恢复与复位">
    注入“按参数书写顺序执行所有选项，导致比较、分页、投影和保存的语义错位”，保存首个不变量破坏点；修复后用同一输入重放并点击重置。

    <Rdi21SortEvidenceLab />
  </Step>
</Stepper>

## 作者目录逐项深读

### SORT &lt;key&gt;命令的实现

**四级证据 1/10。** SORT先把输入元素包装为待排序对象，再计算比较键；后续分页、GET投影和STORE基于排序结果。本节点用“SORT &lt;key&gt;命令的实现”作观察点，执行`rg 'sortCommand|lookupKeyByPattern' src/sort.c`或等价源码探针，保存版本、输入、前后状态和能推翻解释的反例。

验证SORT &lt;key&gt;命令的实现时，先预测比较方式从“ALPHA”切到“BY外部键”会改变哪个字段、偏移、文件或消息；再固定输出阶段做一次对照。若故障注入没有破坏“输入元素、比较键、排序方向、分页与输出投影按固定顺序组合，缺失外部键语义一致”，就撤回当前解释而不是补写故事。

### ALPHA选项的实现

**四级证据 2/10。** ALPHA把比较从数值转换为字符串字典序，不能与默认数值解析结果混用。本节点用“ALPHA选项的实现”作观察点，执行`rg 'sortCommand|lookupKeyByPattern' src/sort.c`或等价源码探针，保存版本、输入、前后状态和能推翻解释的反例。

验证ALPHA选项的实现时，先预测比较方式从“ALPHA”切到“BY外部键”会改变哪个字段、偏移、文件或消息；再固定输出阶段做一次对照。若故障注入没有破坏“输入元素、比较键、排序方向、分页与输出投影按固定顺序组合，缺失外部键语义一致”，就撤回当前解释而不是补写故事。

### ASC选项和DESC选项的实现

**四级证据 3/10。** ASC或DESC只反转比较方向，不改变BY键解析、LIMIT窗口或GET投影规则。本节点用“ASC选项和DESC选项的实现”作观察点，执行`rg 'sortCommand|lookupKeyByPattern' src/sort.c`或等价源码探针，保存版本、输入、前后状态和能推翻解释的反例。

验证ASC选项和DESC选项的实现时，先预测比较方式从“ALPHA”切到“BY外部键”会改变哪个字段、偏移、文件或消息；再固定输出阶段做一次对照。若故障注入没有破坏“输入元素、比较键、排序方向、分页与输出投影按固定顺序组合，缺失外部键语义一致”，就撤回当前解释而不是补写故事。

### BY选项的实现

**四级证据 4/10。** BY按模式为每个元素查外部比较键；BY nosort可跳过排序但仍执行后续LIMIT和GET。本节点用“BY选项的实现”作观察点，执行`rg 'sortCommand|lookupKeyByPattern' src/sort.c`或等价源码探针，保存版本、输入、前后状态和能推翻解释的反例。

验证BY选项的实现时，先预测比较方式从“ALPHA”切到“BY外部键”会改变哪个字段、偏移、文件或消息；再固定输出阶段做一次对照。若故障注入没有破坏“输入元素、比较键、排序方向、分页与输出投影按固定顺序组合，缺失外部键语义一致”，就撤回当前解释而不是补写故事。

### 带有ALPHA选项的BY选项的实现

**四级证据 5/10。** ALPHA把比较从数值转换为字符串字典序，不能与默认数值解析结果混用。本节点用“带有ALPHA选项的BY选项的实现”作观察点，执行`rg 'sortCommand|lookupKeyByPattern' src/sort.c`或等价源码探针，保存版本、输入、前后状态和能推翻解释的反例。

验证带有ALPHA选项的BY选项的实现时，先预测比较方式从“ALPHA”切到“BY外部键”会改变哪个字段、偏移、文件或消息；再固定输出阶段做一次对照。若故障注入没有破坏“输入元素、比较键、排序方向、分页与输出投影按固定顺序组合，缺失外部键语义一致”，就撤回当前解释而不是补写故事。

### LIMIT选项的实现

**四级证据 6/10。** LIMIT在完成排序后按offset与count截取窗口，负数和越界必须按命令语义处理。本节点用“LIMIT选项的实现”作观察点，执行`rg 'sortCommand|lookupKeyByPattern' src/sort.c`或等价源码探针，保存版本、输入、前后状态和能推翻解释的反例。

验证LIMIT选项的实现时，先预测比较方式从“ALPHA”切到“BY外部键”会改变哪个字段、偏移、文件或消息；再固定输出阶段做一次对照。若故障注入没有破坏“输入元素、比较键、排序方向、分页与输出投影按固定顺序组合，缺失外部键语义一致”，就撤回当前解释而不是补写故事。

### GET选项的实现

**四级证据 7/10。** GET为窗口内每个元素按模式投影一个或多个值，#表示元素本身。本节点用“SORT &lt;key&gt;命令的实现”作观察点，执行`rg 'sortCommand|lookupKeyByPattern' src/sort.c`或等价源码探针，保存版本、输入、前后状态和能推翻解释的反例。

验证GET选项的实现时，先预测比较方式从“ALPHA”切到“BY外部键”会改变哪个字段、偏移、文件或消息；再固定输出阶段做一次对照。若故障注入没有破坏“输入元素、比较键、排序方向、分页与输出投影按固定顺序组合，缺失外部键语义一致”，就撤回当前解释而不是补写故事。

### STORE选项的实现

**四级证据 8/10。** STORE把最终投影保存为列表并返回元素数；写入目标键属于命令副作用和传播范围。本节点用“ALPHA选项的实现”作观察点，执行`rg 'sortCommand|lookupKeyByPattern' src/sort.c`或等价源码探针，保存版本、输入、前后状态和能推翻解释的反例。

验证STORE选项的实现时，先预测比较方式从“ALPHA”切到“BY外部键”会改变哪个字段、偏移、文件或消息；再固定输出阶段做一次对照。若故障注入没有破坏“输入元素、比较键、排序方向、分页与输出投影按固定顺序组合，缺失外部键语义一致”，就撤回当前解释而不是补写故事。

### 多个选项的执行顺序

**四级证据 9/10。** 语义顺序是构造元素、计算比较键、排序、LIMIT、GET投影、STORE，而不是按命令文本中选项出现顺序。本节点用“ASC选项和DESC选项的实现”作观察点，执行`rg 'sortCommand|lookupKeyByPattern' src/sort.c`或等价源码探针，保存版本、输入、前后状态和能推翻解释的反例。

验证多个选项的执行顺序时，先预测比较方式从“ALPHA”切到“BY外部键”会改变哪个字段、偏移、文件或消息；再固定输出阶段做一次对照。若故障注入没有破坏“输入元素、比较键、排序方向、分页与输出投影按固定顺序组合，缺失外部键语义一致”，就撤回当前解释而不是补写故事。

### 重点回顾

**四级证据 10/10。** 第21章 排序的回顾要重新证明“输入元素、比较键、排序方向、分页与输出投影按固定顺序组合，缺失外部键语义一致”，并用同一输入比较结构前态、变更轨迹、故障首错与恢复后态

验证重点回顾时，先预测比较方式从“ALPHA”切到“BY外部键”会改变哪个字段、偏移、文件或消息；再固定输出阶段做一次对照。若故障注入没有破坏“输入元素、比较键、排序方向、分页与输出投影按固定顺序组合，缺失外部键语义一致”，就撤回当前解释而不是补写故事。

## 最小源码与运行切片

```bash
+git clone --branch 3.0 --depth 1 https://github.com/redis/redis.git redis-3.0
+cd redis-3.0
+rg 'sortCommand|lookupKeyByPattern' src/sort.c
```

该切片固定Redis 3.0分支、编译器、配置、数据集和命令序列；先保存结构或函数位置，再运行隔离实例。任何持久化损坏、断线、故障转移、大键或高流量实验都必须使用临时数据并规定CPU、内存、磁盘、延迟和停止上限。

```yaml
unit: rdi-21-sort
source_file: sort.c
axis_a: "比较方式"
axis_b: "输出阶段"
fault: "按参数书写顺序执行所有选项，导致比较、分页、投影和保存的语义错位"
invariant: "输入元素、比较键、排序方向、分页与输出投影按固定顺序组合，缺失外部键语义一致"
replay: same_version_same_input
```

## 三个必须主动触发的误区

<Callout type="trap" title="命令表象不等于实现">
  第21章 排序不能从一次redis-cli结果反推全部控制流；必须在sort.c定位结构、写入路径和读取路径，并保存能推翻解释的边界输入。
</Callout>

<Callout type="trap" title="版本倒灌">
  Redis 4.0以后新增或替换的结构与功能不能倒灌到3.0。新版资料只作差异说明，正式验收仍以本章源码文件与作者目录为准。
</Callout>

<Callout type="trap" title="恢复不等于进程在线">
  注入按参数书写顺序执行所有选项，导致比较、分页、投影和保存的语义错位后，要用同一命令序列核对结构、回复、文件或偏移以及最终键值；只有“输入元素、比较键、排序方向、分页与输出投影按固定顺序组合，缺失外部键语义一致”恢复才算通过。
</Callout>

## 练习、答案与节点验证

<Exercises>

**问题 1：单变量因果。** 怎样验证比较方式而不让输出阶段混入结论？

<Answer>固定Redis 3.0提交、配置、数据与输出阶段，只把比较方式从“ALPHA”切到“BY外部键”；比较前后字段、函数轨迹、命令结果与输出阶段分叉风险。</Answer>

**问题 2：逐节点四级证据。** 怎样证明10个正式节点不是只在目录中出现？

<Answer>
1. SORT &lt;key&gt;命令的实现：在sort.c定位SORT &lt;key&gt;命令的实现，保存出现、解释、实验和结果断言四项证据。
2. ALPHA选项的实现：在sort.c定位ALPHA选项的实现，保存出现、解释、实验和结果断言四项证据。
3. ASC选项和DESC选项的实现：在sort.c定位ASC选项和DESC选项的实现，保存出现、解释、实验和结果断言四项证据。
4. BY选项的实现：在sort.c定位BY选项的实现，保存出现、解释、实验和结果断言四项证据。
5. 带有ALPHA选项的BY选项的实现：在sort.c定位带有ALPHA选项的BY选项的实现，保存出现、解释、实验和结果断言四项证据。
6. LIMIT选项的实现：在sort.c定位LIMIT选项的实现，保存出现、解释、实验和结果断言四项证据。
7. GET选项的实现：在sort.c定位SORT &lt;key&gt;命令的实现，保存出现、解释、实验和结果断言四项证据。
8. STORE选项的实现：在sort.c定位ALPHA选项的实现，保存出现、解释、实验和结果断言四项证据。
9. 多个选项的执行顺序：在sort.c定位ASC选项和DESC选项的实现，保存出现、解释、实验和结果断言四项证据。
10. 重点回顾：在sort.c定位BY选项的实现，保存出现、解释、实验和结果断言四项证据。
</Answer>

**问题 3：故障恢复。** 如何证明“按参数书写顺序执行所有选项，导致比较、分页、投影和保存的语义错位”已经被修复？

<Answer>沿冻结3.0基线、定位结构入口、执行单变量变更、注入边界故障、恢复并对账找到第一个破坏点，只改变最小因果前提；随后以同版本同输入重放稳定、故障、恢复和复位四条轨迹，并让未参与者依据SORT执行管线、选项组合矩阵、稳定性与复杂度测试独立确认“输入元素、比较键、排序方向、分页与输出投影按固定顺序组合，缺失外部键语义一致”。</Answer>

</Exercises>

## 术语复核与本章回顾

<Glossary>
  <GlossaryItem term="SORT &lt;key&gt;命令的实现">SORT &lt;key&gt;命令的实现是第21章 排序中连接冻结3.0基线与“输入元素、比较键、排序方向、分页与输出投影按固定顺序组合，缺失外部键语义一致”的源码坐标；必须以Redis 3.0字段、函数或运行输出定义。</GlossaryItem>
  <GlossaryItem term="ALPHA选项的实现">ALPHA选项的实现是第21章 排序中连接定位结构入口与“输入元素、比较键、排序方向、分页与输出投影按固定顺序组合，缺失外部键语义一致”的源码坐标；必须以Redis 3.0字段、函数或运行输出定义。</GlossaryItem>
  <GlossaryItem term="ASC选项和DESC选项的实现">ASC选项和DESC选项的实现是第21章 排序中连接执行单变量变更与“输入元素、比较键、排序方向、分页与输出投影按固定顺序组合，缺失外部键语义一致”的源码坐标；必须以Redis 3.0字段、函数或运行输出定义。</GlossaryItem>
  <GlossaryItem term="BY选项的实现">BY选项的实现是第21章 排序中连接注入边界故障与“输入元素、比较键、排序方向、分页与输出投影按固定顺序组合，缺失外部键语义一致”的源码坐标；必须以Redis 3.0字段、函数或运行输出定义。</GlossaryItem>
  <GlossaryItem term="带有ALPHA选项的BY选项的实现">带有ALPHA选项的BY选项的实现是第21章 排序中连接恢复并对账与“输入元素、比较键、排序方向、分页与输出投影按固定顺序组合，缺失外部键语义一致”的源码坐标；必须以Redis 3.0字段、函数或运行输出定义。</GlossaryItem>
  <GlossaryItem term="LIMIT选项的实现">LIMIT选项的实现是第21章 排序中连接冻结3.0基线与“输入元素、比较键、排序方向、分页与输出投影按固定顺序组合，缺失外部键语义一致”的源码坐标；必须以Redis 3.0字段、函数或运行输出定义。</GlossaryItem>
</Glossary>

完成第21章 排序意味着能从sort.c解释还原SORT对象数组、数值与字典序比较、BY外部键、LIMIT、GET、STORE和选项执行顺序，能运行章专属状态实验，能制造反例并在复位后证明旧状态没有残留。

<Attribution
  mode="independent-rewrite"
  sourceBasis="outline-only"
  workTitle="黄健宏《Redis设计与实现》（正式出版新版，Redis 3.0源码基线）"
  adaptedUrl="https://huangz.works/redisbook1e/"
/>
