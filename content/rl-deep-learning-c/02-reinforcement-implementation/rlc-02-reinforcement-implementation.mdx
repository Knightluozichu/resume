---
title: "第2章 强化学习的实现"
type: C
section: "第2章 强化学习的实现"
order: 2
description: "第2章 强化学习的实现逐项覆盖官方目录，以C程序、手算更新、交互实验和失败对照重建原书证据链。"
demo: true
math: true
sourceUrl: "https://www.ohmsha.co.jp/book/9784274221149/"
draft: false
---

import {
  Rlc02ReinforcementImplementationMapLab,
  Rlc02ReinforcementImplementationExperimentLab,
  Rlc02ReinforcementImplementationEvidenceLab,
} from "@/components/mdx/rl-deep-learning-c/diagrams/rlc-02-reinforcement-implementation";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第2章 强化学习的实现中的输入数组、状态、参数、目标与终止边界
- 能推导并计算四个核心方程中的索引、TD目标、局部误差或梯度
- 能实现并运行三个 C 代码片段，定位第一条状态、Q值、激活或权重证据分叉
- 能判断“q21.c 与寻找目标程序必须在合法动作掩码下重放；每次更新能独立算出 TD 目标、误差和新 Q 值，终止状态不再引入未来回报。”是否成立并设计单一故障样本

</Objectives>

## 为什么先冻结数组与状态合同

<Term def="Q学习是本页第1个可执行证据坐标，必须记录数组shape、状态版本和失败边界。">
  Q学习
</Term>
、
<Term def="Q表是本页第2个可执行证据坐标，必须记录数组shape、状态版本和失败边界。">
  Q表
</Term>
、
<Term def="TD误差是本页第3个可执行证据坐标，必须记录数组shape、状态版本和失败边界。">
  TD误差
</Term>
、
<Term def="探索利用是本页第4个可执行证据坐标，必须记录数组shape、状态版本和失败边界。">
  探索利用
</Term>
、
<Term def="状态转移是本页第5个可执行证据坐标，必须记录数组shape、状态版本和失败边界。">
  状态转移
</Term>
、
<Term def="终止状态是本页第6个可执行证据坐标，必须记录数组shape、状态版本和失败边界。">
  终止状态
</Term>
共同构成本页坐标。用 Q
学习把“试错—奖励—价值修正”落到数组更新：先推导学习规则，再逐行复核 q21.c
的分支迷宫和第二个寻找目标的程序，保留状态编号、动作合法性与终止条件。

本书的价值不在于使用 C 语言追求大规模训练，而在于把算法压缩到可逐行检查的内存和循环。所有状态、动作、神经元、卷积位置和权重都必须有明确索引；所有随机选择都必须可复现；所有 episode 与训练样本开始前都必须明确哪些数组清零、哪些参数保留。

证据账本至少保存输入、合法动作掩码、环境转移、即时奖励、更新前 Q 值、TD 目标、各层激活、输出误差、隐藏层误差、权重更新和终止标记。最终累计奖励只是结果，不足以排除数组越界、未初始化内存、错误动作索引、符号反转或终止状态继续 bootstrap。

## 数学骨架

$$
Q_{t+1}(s_t,a_t)=Q_t(s_t,a_t)+\alpha\delta_t
$$

$$
\delta_t=r_{t+1}+\gamma\max_a Q_t(s_{t+1},a)-Q_t(s_t,a_t)
$$

$$
a_t=\begin{cases}\text{random legal action},&u\lt\varepsilon\\\arg\max_aQ(s_t,a),&u\ge\varepsilon\end{cases}
$$

$$
\max_a Q(s_{\mathrm{terminal}},a)=0
$$

公式进入 C 循环前逐项映射变量：写清标量与数组、输入和输出长度、旧值和新值、学习率与折扣因子的范围。浮点运算后检查有限值；argmax 只遍历合法动作；网络目标只修改被选择动作的输出，其他输出保持当前预测。

## 三段可编译核心

```c
double update_q(double q, double reward, double next_max,
                double alpha, double gamma) {
    const double td = reward + gamma * next_max - q;
    return q + alpha * td;
}
```

```c
int greedy_action(const double q[][4], int state,
                  const unsigned char legal[][4]) {
    int best = -1;
    for (int a = 0; a < 4; ++a)
        if (legal[state][a] && (best < 0 || q[state][a] > q[state][best])) best = a;
    return best;
}
```

```c
for (int step = 0; step < MAX_STEPS && state != goal; ++step) {
    int action = choose_action(state, epsilon);
    Step next = environment_step(state, action);
    q[state][action] = update_q(q[state][action], next.reward,
                                max_legal_q(next.state), alpha, gamma);
    state = next.state;
}
```

代码片段刻意保留显式循环和状态结构。复现时使用编译器告警、地址/未定义行为检测和固定种子；对每个数组维度增加断言，并将环境、学习器和评价器分开，避免评价过程继续更新 Q 表或网络权重。

## 权威目录逐项讲解

### 强化学习与Q学习（2.1 強化学習とQ学習）

官方目录项“2.1 強化学習とQ学習”对应强化学习与Q学习。它把Q学习连接到TD误差：先写输入数组、索引含义和状态版本，再逐行说明循环、分支与赋值对应哪一步算法。C 程序没有框架替你检查广播、自动微分或 episode 生命周期，所以数组边界、合法动作、初始化和终止条件本身就是算法的一部分。

最小复现固定迷宫、样本、权重初值和随机种子，先预测改变一个变量后最早变化的状态、Q 值、激活、局部误差或卷积元素，再保存更新前后值。针对“强化学习与Q学习”，验收标准是：q21.c 与寻找目标程序必须在合法动作掩码下重放；每次更新能独立算出 TD 目标、误差和新 Q 值，终止状态不再引入未来回报。若首个证据不一致，停止长期训练，从零初始化内存并检查索引、符号、学习率和 bootstrap 边界。

### 强化学习的思路（2.1.1 強化学習の考え方）

官方目录项“2.1.1 強化学習の考え方”对应强化学习的思路。它把Q表连接到探索利用：先写输入数组、索引含义和状态版本，再逐行说明循环、分支与赋值对应哪一步算法。C 程序没有框架替你检查广播、自动微分或 episode 生命周期，所以数组边界、合法动作、初始化和终止条件本身就是算法的一部分。

最小复现固定迷宫、样本、权重初值和随机种子，先预测改变一个变量后最早变化的状态、Q 值、激活、局部误差或卷积元素，再保存更新前后值。针对“强化学习的思路”，验收标准是：q21.c 与寻找目标程序必须在合法动作掩码下重放；每次更新能独立算出 TD 目标、误差和新 Q 值，终止状态不再引入未来回报。若首个证据不一致，停止长期训练，从零初始化内存并检查索引、符号、学习率和 bootstrap 边界。

### Q学习算法（2.1.2 Q学習のアルゴリズム）

官方目录项“2.1.2 Q学習のアルゴリズム”对应Q学习算法。它把TD误差连接到状态转移：先写输入数组、索引含义和状态版本，再逐行说明循环、分支与赋值对应哪一步算法。C 程序没有框架替你检查广播、自动微分或 episode 生命周期，所以数组边界、合法动作、初始化和终止条件本身就是算法的一部分。

最小复现固定迷宫、样本、权重初值和随机种子，先预测改变一个变量后最早变化的状态、Q 值、激活、局部误差或卷积元素，再保存更新前后值。针对“Q学习算法”，验收标准是：q21.c 与寻找目标程序必须在合法动作掩码下重放；每次更新能独立算出 TD 目标、误差和新 Q 值，终止状态不再引入未来回报。若首个证据不一致，停止长期训练，从零初始化内存并检查索引、符号、学习率和 bootstrap 边界。

### Q学习的实现（2.2 Q学習の実装）

官方目录项“2.2 Q学習の実装”对应Q学习的实现。它把探索利用连接到终止状态：先写输入数组、索引含义和状态版本，再逐行说明循环、分支与赋值对应哪一步算法。C 程序没有框架替你检查广播、自动微分或 episode 生命周期，所以数组边界、合法动作、初始化和终止条件本身就是算法的一部分。

最小复现固定迷宫、样本、权重初值和随机种子，先预测改变一个变量后最早变化的状态、Q 值、激活、局部误差或卷积元素，再保存更新前后值。针对“Q学习的实现”，验收标准是：q21.c 与寻找目标程序必须在合法动作掩码下重放；每次更新能独立算出 TD 目标、误差和新 Q 值，终止状态不再引入未来回报。若首个证据不一致，停止长期训练，从零初始化内存并检查索引、符号、学习率和 bootstrap 边界。

### q21.c程序的实现（2.2.1 q21.cプログラムの実装）

官方目录项“2.2.1 q21.cプログラムの実装”对应q21.c程序的实现。它把状态转移连接到Q学习：先写输入数组、索引含义和状态版本，再逐行说明循环、分支与赋值对应哪一步算法。C 程序没有框架替你检查广播、自动微分或 episode 生命周期，所以数组边界、合法动作、初始化和终止条件本身就是算法的一部分。

最小复现固定迷宫、样本、权重初值和随机种子，先预测改变一个变量后最早变化的状态、Q 值、激活、局部误差或卷积元素，再保存更新前后值。针对“q21.c程序的实现”，验收标准是：q21.c 与寻找目标程序必须在合法动作掩码下重放；每次更新能独立算出 TD 目标、误差和新 Q 值，终止状态不再引入未来回报。若首个证据不一致，停止长期训练，从零初始化内存并检查索引、符号、学习率和 bootstrap 边界。

### 例题2：寻找目标的学习程序（2.2.2 例題（2） ゴールを見つける学習プログラム）

官方目录项“2.2.2 例題（2） ゴールを見つける学習プログラム”对应例题2：寻找目标的学习程序。它把终止状态连接到Q表：先写输入数组、索引含义和状态版本，再逐行说明循环、分支与赋值对应哪一步算法。C 程序没有框架替你检查广播、自动微分或 episode 生命周期，所以数组边界、合法动作、初始化和终止条件本身就是算法的一部分。

最小复现固定迷宫、样本、权重初值和随机种子，先预测改变一个变量后最早变化的状态、Q 值、激活、局部误差或卷积元素，再保存更新前后值。针对“例题2：寻找目标的学习程序”，验收标准是：q21.c 与寻找目标程序必须在合法动作掩码下重放；每次更新能独立算出 TD 目标、误差和新 Q 值，终止状态不再引入未来回报。若首个证据不一致，停止长期训练，从零初始化内存并检查索引、符号、学习率和 bootstrap 边界。

## 七类实现证据

### 输入与内存合同

固定数据、迷宫和随机种子，声明所有静态数组的容量、有效长度与所有权。每次 episode 开始前写明状态、轨迹和梯度是否清零；释放或复用动态内存时保留唯一责任方，编译器告警必须视为失败。

### 环境转移合同

环境函数只根据当前状态与动作产生下一状态、奖励和终止标记，不读取 Q 表或网络参数。非法动作在进入学习器前拒绝；同一状态动作在确定性迷宫中必须得到相同转移，随机环境则保存随机数序列。

### Q学习合同

保存旧 Q、即时奖励、下一状态合法动作最大值、TD 目标、TD 误差和新 Q。终止状态的未来值固定为零，argmax 与探索采样必须使用同一合法动作集合，更新后的值不能被误用于同一步目标。

### 网络前向合同

逐层保存加权和与激活，偏置只加一次，输入和输出节点次序与状态/动作编码一致。对 nn1.c、nn2.c、nn3.c 和深度强化程序使用相同的激活定义与数值范围，防止训练和评价路径分叉。

### 反向传播合同

先计算输出层局部误差，再用更新前权重传播到隐藏层，最后统一更新参数。用中心有限差分核对一个权重；若符号、量级或相对误差不满足容差，禁止增加隐藏单元或训练轮数。

### 卷积与融合合同

卷积要声明步幅、边界、核翻转约定与输出尺寸，手算一个窗口。Q 学习与网络融合时，网络所有动作输出先复制到目标数组，只替换所选动作的 TD 目标，并确保终止转移不访问下一状态网络输出。

### 回退与评价合同

评价从固定初态开始，冻结探索率和学习开关，不读取上一 episode 的隐藏状态或梯度。出现分叉时依次回退环境、Q 目标、前向、反向和参数更新；最小样例一致后才运行书中的完整迷宫。

## 三步交互实验

先预测单变量变化会最早影响状态、TD 目标、神经元激活、卷积元素还是权重，再切换基线、边界和故障模式。

<Stepper>
  <Step title="画出状态与数组图">
    标出环境、Q表、网络各层和动作输出，逐边注明长度、索引和状态版本。
    <Rlc02ReinforcementImplementationMapLab />
  </Step>
  <Step title="运行单变量C实验">
    固定迷宫、初值和种子，只改变探索率、学习率、隐藏单元或卷积核中的一个变量。
    <Rlc02ReinforcementImplementationExperimentLab />
  </Step>
  <Step title="核对独立证据">
    比较正常与故障路径、程序更新与手算结果，定位第一条不一致并给出回退点。
    <Rlc02ReinforcementImplementationEvidenceLab />
  </Step>
</Stepper>

## 常见失败与边界

<Callout type="trap">
  **累计奖励上升不等于实现正确：** 越界写入、非法动作、终止状态 bootstrap
  或评价时继续学习都可能制造看似改善的曲线。
</Callout>

<Callout type="trap">
  **用现代 DQN 概念覆盖原书程序：** 本书展示的是 Q
  学习与前馈网络的直接融合，没有经验回放、目标网络或策略梯度；必须忠于 q21dl.c
  和 q22dl.c 的实际边界。
</Callout>

<Callout type="tip">
  **最小重放顺序：** 先用两状态两动作手算 Q
  更新，再用单神经元核对梯度，最后才把网络接入迷宫。
</Callout>

## 本章回顾

用 Q 学习把“试错—奖励—价值修正”落到数组更新：先推导学习规则，再逐行复核 q21.c 的分支迷宫和第二个寻找目标的程序，保留状态编号、动作合法性与终止条件。验收底线是：q21.c 与寻找目标程序必须在合法动作掩码下重放；每次更新能独立算出 TD 目标、误差和新 Q 值，终止状态不再引入未来回报。掌握标准是能在运行前预测首个变化，逐行解释 C 循环，复现实验，解释失败样本，并证明评价不改变学习状态。

<Glossary>

<GlossaryItem term="Q学习">
  “Q学习”对应证据链第1站。记录输入、数组长度、索引语义、参数版本、数值容差和清理责任；只看到最终奖励上升不能证明该站正确。
</GlossaryItem>

<GlossaryItem term="Q表">
  “Q表”对应证据链第2站。记录输入、数组长度、索引语义、参数版本、数值容差和清理责任；只看到最终奖励上升不能证明该站正确。
</GlossaryItem>

<GlossaryItem term="TD误差">
  “TD误差”对应证据链第3站。记录输入、数组长度、索引语义、参数版本、数值容差和清理责任；只看到最终奖励上升不能证明该站正确。
</GlossaryItem>

<GlossaryItem term="探索利用">
  “探索利用”对应证据链第4站。记录输入、数组长度、索引语义、参数版本、数值容差和清理责任；只看到最终奖励上升不能证明该站正确。
</GlossaryItem>

<GlossaryItem term="状态转移">
  “状态转移”对应证据链第5站。记录输入、数组长度、索引语义、参数版本、数值容差和清理责任；只看到最终奖励上升不能证明该站正确。
</GlossaryItem>

<GlossaryItem term="终止状态">
  “终止状态”对应证据链第6站。记录输入、数组长度、索引语义、参数版本、数值容差和清理责任；只看到最终奖励上升不能证明该站正确。
</GlossaryItem>

</Glossary>

<Exercises>

1. 如何为第2章 强化学习的实现建立可手算、可重放的最小 C 程序？

<Answer>
  选择两状态、两动作或单神经元，固定数组、初值和种子，逐步写出环境转移、TD目标或前向激活、局部误差和更新后值，再从清零状态独立重放。
</Answer>

2. 如何证明“q21.c 与寻找目标程序必须在合法动作掩码下重放；每次更新能独立算出 TD 目标、误差和新 Q 值，终止状态不再引入未来回报。”不是由一次高奖励碰巧满足？

<Answer>
  同时运行正常路径和只破坏一个索引、合法动作、终止标记、梯度符号或状态清理的失败路径。检测器必须在预测的首个中间量区分两者，并跨固定评价轨迹报告结果。
</Answer>

3. 修复第2章 强化学习的实现后应按什么顺序回归？

<Answer>
  开启编译器告警与运行时检测，清空 episode
  状态和梯度，从固定配置重放：先查环境与索引，再查目标和前向，然后查反向与更新，最后才跑完整迷宫和独立评价。
</Answer>

</Exercises>

<Attribution
  adaptedFrom="小高知宏著、张小猛译《强化学习与深度学习：通过C语言模拟》，机械工业出版社，2019年7月，159页，ISBN 9787111627180；原版《強化学習と深層学習 C言語によるシミュレーション》，Ohmsha，2017年10月14日，208页，ISBN 9784274221149 · 基于官方4章与32个编号节/小节完整目录的独立教学重构"
  adaptedUrl="https://www.ohmsha.co.jp/book/9784274221149/"
/>
