---
title: "第1章 Ruby初探"
type: C
section: "たのしいRuby 第5版 · Ruby初体验"
order: 1
description: "从ruby与irb两种运行方式出发，建立对象接收消息的执行模型，掌握字符串、输出、数值、变量、注释以及if、while、times的最小程序闭环。"
demo: true
math: false
sourceUrl: "https://tanoshiiruby.github.io/5/"
draft: false
---

import { Objectives, Callout, Attribution, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step } from "@/components/mdx/mdx-components";
import { RubyExecutionModeLab, RubyObjectMessageLab, RubyFirstControlLab } from "@/components/mdx/ruby-programming/diagrams/first-ruby";

<Objectives>

- 能区分`ruby`命令、`ruby -e`、`irb`与`require`的输入、输出和加载边界
- 能解释Ruby程序中的对象、方法、变量引用以及`puts`和`p`观察结果的差异
- 能实现包含字符串、数值计算、`if`、`while`和`times`的可重复命令行程序

</Objectives>

## 从一次可重复运行开始：Ruby的运行方法

学习Ruby的第一个障碍不是语法，而是没有区分“程序产生了输出”和“交互环境替你显示了表达式的值”。把`1 + 2`输入IRB会看到`=> 3`，因为IRB执行read-eval-print loop；把同样一行保存到`hello.rb`后运行，程序正常结束，却不会自动显示3。<Term def="执行环境对源码、参数、自动显示、异常与退出状态分别承担什么责任的约定。">execution mode contract</Term>要求每次实验先写明由谁读取source、谁显示结果、谁决定进程退出状态。

```ruby
# hello.rb
name = ARGV.fetch(0, "Ruby")
puts "Hello, #{name}!"
p({ ruby: RUBY_VERSION, argument_count: ARGV.length })
```

运行`ruby hello.rb Matz`会由程序明确输出问候与诊断Hash。`ARGV`保存脚本参数，不包含Ruby可执行文件和脚本名；`fetch(0, "Ruby")`在没有第一个参数时给出默认值。真正可复现的最小记录应包括Ruby版本、运行命令、当前目录、输入参数、标准输出、标准错误和exit status。

先预测：`ruby -e '1 + 2'`会不会显示3？不会，因为`-e`只提供source，不改变普通文件模式的输出规则；写成`ruby -e 'p 1 + 2'`才显示。这个小差异能发现大量“在IRB能运行、放进脚本却什么也没有”的误判。

<RubyExecutionModeLab />

<Stepper>
  <Step title="切换四种运行方式"><RubyExecutionModeLab /></Step>
  <Step title="建立运行记录">分别执行文件、`-e`与IRB表达式，记录source owner、自动显示、`ARGV`、stdout、stderr和status。</Step>
  <Step title="验证require只加载一次"><RubyExecutionModeLab /></Step>
</Stepper>

## ruby命令的执行方法与irb命令的执行方法

`ruby file.rb`适合完整程序；`ruby -c file.rb`只检查语法，不执行顶层副作用；`ruby -w`启用常见warning；`ruby -Ilib`把目录加入load path。IRB适合探索对象和方法，但历史状态会污染实验：前一条定义的局部变量、方法或被加载的库仍然存在。遇到可疑结果应重启IRB，或者把最小复现写成独立文件。

`load "config.rb"`每次都会重新执行文件，`require "json"`通常每个feature只加载一次；它们不是等价的“引用文件”。本章先记住边界，第3章再把命令与文件读取连成完整工具。不要让业务逻辑依赖当前目录偶然存在某个文件，路径归属必须明确。

<Callout type="trap" title="IRB显示了结果，所以脚本也会自动显示">
IRB额外执行print阶段；普通Ruby脚本只保留程序自己写入stdout/stderr的内容。验收命令行工具时必须执行真实文件并检查exit status，不能拿IRB截图代替。
</Callout>

## 程序解说：对象与方法

Ruby把字符串、整数、数组、类乃至方法返回值都作为对象处理。表达式`"ruby".upcase`可读成：以字符串对象为receiver，发送`upcase`方法消息，得到新的`"RUBY"`对象。<Term def="方法调用中接收消息、决定从哪条方法查找链开始的对象。">receiver object</Term>比“一切皆对象”的口号更有操作性，因为调试时要同时回答receiver是什么、method是什么、arguments是什么、result是什么。

```ruby
word = "Ruby"
length = word.length
upper = word.upcase

p word: word, length: length, upper: upper
p word.class          # String
p length.class        # Integer
p word.equal?(upper)  # false: upcase返回另一个对象
```

括号常可省略，例如`puts("hello")`与`puts "hello"`都合法；但在嵌套调用或参数边界不清时保留括号。`object_id`和`equal?`观察identity，`==`通常观察value equality。不要因为两个字符串内容相等就假定它们是同一个对象，也不要在业务代码里把`object_id`当持久标识。

<RubyObjectMessageLab />

<Stepper>
  <Step title="选择Receiver"><RubyObjectMessageLab /></Step>
  <Step title="画消息记录">为表达式写出receiver、method、arguments、result class和是否修改receiver。</Step>
  <Step title="比较等值与同一性"><RubyObjectMessageLab /></Step>
</Stepper>

## 字符串与中文的输出：引号、插值、换行和编码

双引号字符串处理`\n`等escape并执行`#{...}`插值；单引号字符串只对少数escape特殊处理，不执行插值。<Term def="在双引号字符串中先求值表达式，再把结果转换并嵌入字符串的过程。">string interpolation</Term>把数据和展示模板连接起来，但不能替代SQL参数、shell escaping或HTML escaping。

```ruby
language = "Ruby"
version = RUBY_VERSION

puts "#{language} #{version}\nready"
puts '#{language} is not expanded'
puts "2 + 3 = #{2 + 3}"
```

Source文件按目标Ruby与编辑器约定保存为UTF-8，现代Ruby通常默认UTF-8 source encoding；第19章会系统处理Encoding。所谓“中文的输出”至少跨越source bytes、String encoding、stdout external encoding与终端字体四层。出现乱码时记录`__ENCODING__`、`string.encoding`和`STDOUT.external_encoding`，不能只反复改字面量。

## puts方法与p方法：面向用户和面向诊断

`puts`调用对象的字符串表示并追加换行，数组会逐元素输出；`p`输出更接近`inspect`，保留引号、escape和容器结构，并返回被输出对象。<Term def="为了诊断而保留类型线索、转义和结构边界的对象表示。">inspection representation</Term>适合开发日志，却不保证是稳定序列化格式。

```ruby
value = "line 1\nline 2"
items = ["a", "b"]

puts value
p value
puts items
p items
warn "diagnostic goes to stderr"
```

面向用户的CLI输出使用明确格式；机器接口使用JSON、CSV等协议；调试才依赖`p`/`inspect`。`warn`默认写stderr，因此shell可以把正常数据和诊断分开。测试应分别捕获stdout与stderr，不要只观察终端混合后的画面。

## 数值表示与计算

整数支持任意精度语义，浮点数通常是IEEE 754 double。`+ - * / % **`分别表示加减乘除、余数与幂；整数相除`5 / 2`得到2，而只要一边是浮点数，`5 / 2.0`得到2.5。方法`div`、`fdiv`、`remainder`与`%`表达不同意图，负数边界必须用实例验证。

`Math.sqrt(9)`返回3.0；调用Math模块方法通常使用`Math.`。先决定结果domain再选operator：金额不要直接依赖binary float，计数不要意外做整数截断，除数来自输入时先验证零。第12章会展开Numeric层次、随机数、bit operation与近似值误差。

```ruby
subtotal = 1250
tax_rate = 0.10
total = subtotal + (subtotal * tax_rate).round

p integer_division: 5 / 2
p floating_division: 5.fdiv(2)
p square_root: Math.sqrt(9)
p total: total
```

## 变量与注释：名字引用对象

局部变量名通常以小写字母或下划线开头；赋值让名字引用一个对象，之后可以rebind到另一个class的对象。<Term def="让已有变量名改为引用另一个对象，而不是改变变量本身的静态类型。">variable rebinding</Term>说明Ruby是动态类型，但不表示接口无需约束。边界仍需验证输入类型、可调用方法和业务不变量。

`#`到行末是comment。Comment应解释约束、原因或非显然选择，不应逐字翻译代码。特殊位置的encoding comment、shebang或工具directive有语义，不是普通说明。删除一条comment前先判断它是否被Ruby、shell或工具读取。

赋值有返回值，因此能嵌入表达式，但`if item = lookup`容易与`==`混淆并触发warning。Production代码优先分成“获得值”和“判断值”两步，使nil、false和错误处理清楚可测。

## 条件判断：if～then～end

Ruby条件中只有`false`和`nil`为falsey，0、空字符串与空数组都truthy。`if`本身是expression，会返回执行分支最后一个表达式的值。第一次练习可以使用`if ... then ... end`，实际代码常省略`then`并用换行建立结构。

```ruby
count = Integer(ARGV.fetch(0, "3"), 10)

if count.negative?
  warn "count must be non-negative"
  exit 2
end

count.times do |index|
  puts "step #{index + 1}/#{count}"
end
```

### 循环：while语句与times方法

`while`适合终止条件依赖不断变化状态的循环；必须能证明每次迭代向终止前进。`times`由Integer receiver决定固定次数，并把从0开始的index传给block。<Term def="循环每次迭代都维持且最终推动终止条件成立的可说明约束。">loop progress invariant</Term>能把“看起来会停”改成可检查的状态关系。

<RubyFirstControlLab />

<Stepper>
  <Step title="切换if、while与times"><RubyFirstControlLab /></Step>
  <Step title="预测真假与次数">先写出nil、false、0、空字符串在condition中的结果，以及`3.times`收到的三个index。</Step>
  <Step title="注入非法输入"><RubyFirstControlLab /></Step>
</Stepper>

<Callout type="trap" title="0和空字符串等价于false">
Ruby只有false与nil为falsey。`if count`不能判断count是否为零，`name || default`也不会替换空字符串；业务有效性要用`zero?`、`empty?`或专门validation表达。
</Callout>

## 本章回顾：运行、对象、观察、控制

1. `ruby`执行可重复程序，`irb`额外显示表达式结果；`-e`只改变source入口，`require`负责按feature加载。
2. 方法调用从receiver object开始，经method与arguments得到result；变量只保存对象引用并可rebind。
3. 双引号支持escape与interpolation，中文输出还受String与外部编码影响。
4. `puts`面向正常文本，`p`面向结构诊断，`warn`写stderr；三者不是序列化协议。
5. 数值operator要先明确整数、浮点和业务domain；除法与近似值边界必须测试。
6. `if`、`while`与`times`分别表达分支、状态循环与固定次数；只有false和nil为falsey。

<Exercises>

**问题 1：为什么`1 + 2`在IRB里显示3，在脚本里却没有输出？**

<Answer>IRB实现read-eval-print loop，会自动inspect每个表达式的结果；普通脚本只执行表达式，不自动展示返回值。把脚本改为`p 1 + 2`或`puts 1 + 2`，并在真实`ruby file.rb`进程中检查stdout、stderr和exit status。</Answer>

**问题 2：怎样证明变量重新赋值不是改变对象的class？**

<Answer>先保存旧对象及其`object_id`/`class`，再把变量赋给另一个对象；旧引用仍指向原对象且class不变，只是变量名改指向新对象。对可变String另做`upcase!`实验，区分rebind与mutate。</Answer>

**问题 3：把用户输入转换为重复次数时需要哪些错误边界？**

<Answer>用`Integer(text, 10)`做严格转换，捕获或向上交给`ArgumentError`；拒绝负数并设置非零exit status，必要时限制最大次数。分别测试缺省、0、正数、负数、非数字和超大值，正常结果写stdout，诊断写stderr。</Answer>

</Exercises>

## 术语表

<Glossary>
  <GlossaryItem term="execution mode contract" definition="执行入口对源码、自动显示、错误与退出状态的职责约定。" />
  <GlossaryItem term="receiver object" definition="方法调用中接收消息并决定方法查找起点的对象。" />
  <GlossaryItem term="string interpolation" definition="求值双引号字符串中的表达式并把结果嵌入文本。" />
  <GlossaryItem term="inspection representation" definition="为诊断保留转义、类型与结构线索的对象表示。" />
  <GlossaryItem term="variable rebinding" definition="让变量名改为引用另一个对象。" />
  <GlossaryItem term="loop progress invariant" definition="每轮都维持并推动循环到达终止条件的约束。" />
</Glossary>

<Attribution adaptedFrom="たのしいRuby 第5版 / Ruby基础教程（第5版），第1章 Ruby初探" adaptedUrl="https://tanoshiiruby.github.io/5/" />
