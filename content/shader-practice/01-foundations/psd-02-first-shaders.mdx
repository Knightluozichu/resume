---
title: "第2章 第一个着色器"
type: C
section: "Shader开发实战 · 基础效果"
order: 2
description: "沿Practical Shader Development正式目录掌握GLSL版本、顶点属性、gl_Position、插值变量、uniform。"
demo: true
math: true
sourceUrl: "https://link.springer.com/book/10.1007/978-1-4842-4457-9"
draft: true
---

import { Objectives, Callout, Attribution, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step } from "@/components/mdx/mdx-components";
import { PsdFirstShadersPipelineLab, PsdFirstShadersCompareLab, PsdFirstShadersEvidenceLab } from "@/components/mdx/shader-practice/diagrams/psd-02-first-shaders";

<Objectives>
- 能解释GLSL版本、顶点属性和gl_Position在渲染数据流中的作用
- 能推导本页公式并用GLSL、应用状态或引擎材质实现最小实验
- 能复现uniform，以参考图、GPU捕获、边界输入和反例验收
</Objectives>

## 从画面开始

先预测顶点、片元、深度和帧时间会如何变化，再只调整一个参数观察变化。顶点着色器读取in属性并写gl_Position，片元着色器为每个片元输出颜色。顶点颜色通过out和in接口跨阶段，在三角形内部插值；uniform则由C++应用按绘制设置，不随顶点改变。每一步都先用常量输出确认程序链接成功。 本页用<Term def="用version预处理指令声明着色器语言和功能基线。">GLSL版本</Term>、<Term def="按顶点从顶点缓冲输入位置、颜色或纹理坐标的数据。">顶点属性</Term>、<Term def="顶点着色器必须写入的裁剪空间位置。">gl_Position</Term>、<Term def="从顶点输出并在图元内部透视插值后送入片元的数据。">插值变量</Term>、<Term def="一次Draw中保持一致、由应用设置的着色器参数。">uniform</Term>建立对象、阶段、状态和证据之间的联系。

<PsdFirstShadersPipelineLab />

## 第一版权威目录定位

本页一一对应Kyle Halladay《Practical Shader Development: Vertex and Fragment Shaders for Game Developers》第一版第2章 第一个着色器。Springer/Apress正式目录确认2019年第一版、18章、附录代码、ISBN 978-1-4842-4456-2和XXI+381页；清华大学出版社官方样章确认2021中文第一版《Shader开发实战》、ISBN 9787302583349及对应中文目录。

### 原书小节覆盖

1. 2.1 安装与创建项目
2. 2.3 创建第一个三角形
3. 2.4 第一个顶点着色器
4. 2.5 第一个片元着色器
5. 2.7 顶点属性颜色
6. 2.8 片元插值
7. 2.9 统一变量

### 核心对象

1. **GLSL版本**：用version预处理指令声明着色器语言和功能基线。
2. **顶点属性**：按顶点从顶点缓冲输入位置、颜色或纹理坐标的数据。
3. **gl_Position**：顶点着色器必须写入的裁剪空间位置。
4. **插值变量**：从顶点输出并在图元内部透视插值后送入片元的数据。
5. **uniform**：一次Draw中保持一致、由应用设置的着色器参数。

## 数据流与公式

顶点着色器读取in属性并写gl_Position，片元着色器为每个片元输出颜色。顶点颜色通过out和in接口跨阶段，在三角形内部插值；uniform则由C++应用按绘制设置，不随顶点改变。每一步都先用常量输出确认程序链接成功。 前15章以OpenGL 4.1、GLSL和openFrameworks为实验环境；第16至18章再映射到Unity、UE4与Godot。所有代码都要把可编程Shader与应用侧资源绑定、Draw状态、深度和混合分开记录。

$$
p_{clip}=M p
$$

$$
c(p)=\lambda_0c_0+\lambda_1c_1+\lambda_2c_2
$$

$$
\sum_i\lambda_i=1
$$

$$
uniform(t)=constant\ during\ draw
$$

公式中的位置、方向、颜色、深度和时间必须标注空间、范围与单位。片元输出只是候选颜色，最终结果还经过裁剪、深度、模板、混合与颜色编码。

## 最小实现

```glsl
#version 410
layout(location=0) in vec3 position;
void main(){ gl_Position=vec4(position,1.0); }
```

实现顺序固定为常量输出、输入可视化、核心公式、应用状态和优化。先验证Shader编译与链接，再检查属性位置、uniform、纹理单元和Framebuffer；只有画面与状态都正确后才比较性能。

<PsdFirstShadersCompareLab />

## 应用与引擎侧契约

```cpp
shader.begin();
shader.setUniform1f("time", elapsedSeconds);
shader.setUniformTexture("mainTex", texture, 0);
mesh.draw();
shader.end();
```

应用侧保存网格布局、索引、纹理格式、采样器、矩阵顺序、混合与深度状态。引擎章节则把同一概念映射到Material、节点图或ShaderMaterial；语法可以变化，输入空间与合成语义不能靠猜。

## 边界、精度与性能

测试零向量、退化UV、负缩放、近远裁剪面、透明交叉、纹理边缘、大世界坐标、长时间运行和高过度绘制。性能实验固定硬件、分辨率、VSync与场景，先判断CPU还是GPU受限，再用捕获定位具体Draw和Shader阶段。

```text
renderer: OpenGL 4.1 / named engine and version
scene: fixed mesh, camera, lights, textures
state: depth, cull, blend, target
evidence: image diff, frame capture, CPU/GPU ms
counterexample: smallest reproducible failure
```

<PsdFirstShadersEvidenceLab />

## 分步实验

<Stepper>
  <Step title="固定输入和状态"><PsdFirstShadersPipelineLab /></Step>
  <Step title="逐阶段实现与比较"><PsdFirstShadersCompareLab /></Step>
  <Step title="捕获画面和性能"><PsdFirstShadersEvidenceLab /></Step>
</Stepper>

1. 用最小网格与常量颜色验证Shader编译、链接和Draw状态。
2. 可视化位置、UV、法线、深度或光照项，确认范围和空间。
3. 打开本章核心效果并保存基线与候选画面。
4. 构造边界与失败输入，用GPU捕获和计时解释差异。

## 验收证书

证书包含源码与编译版本、应用绑定、资源哈希、状态、参考图、GPU事件、CPU与GPU帧时间和失败反例。顶点输出名或类型与片元输入不匹配，却只在最终黑屏上猜测错误。 修复后必须重放同一输入并说明差异来自哪个阶段。

## 常见误区

<Callout type="trap" title="把语法当原理">顶点输出名或类型与片元输入不匹配，却只在最终黑屏上猜测错误。</Callout>

<Callout type="trap" title="只看最终画面">画面相似不证明空间、深度、混合和性能等价。必须保存中间调试视图、固定状态和GPU捕获。</Callout>

## 本章回顾

本页覆盖GLSL版本、顶点属性、gl_Position、插值变量、uniform。掌握标准是能画出数据流，写出最小实现，说明应用或引擎状态，再用边界、精度与性能证据复现结论。

<Exercises>
**问题1：GLSL版本与顶点属性如何连接？**
<Answer>用version预处理指令声明着色器语言和功能基线。 按顶点从顶点缓冲输入位置、颜色或纹理坐标的数据。 回答时标注阶段和输入来源。</Answer>

**问题2：怎样调试gl_Position？**
<Answer>先输出常量，再把相关中间值映射为颜色；随后核对应用绑定、固定状态和GPU事件。</Answer>

**问题3：如何验收uniform？**
<Answer>一次Draw中保持一致、由应用设置的着色器参数。 同时保存正常例、边界例、失败反例和CPU/GPU计时。</Answer>
</Exercises>

## 术语表

<Glossary>
  <GlossaryItem term="GLSL版本" definition="用version预处理指令声明着色器语言和功能基线。" />
  <GlossaryItem term="顶点属性" definition="按顶点从顶点缓冲输入位置、颜色或纹理坐标的数据。" />
  <GlossaryItem term="gl_Position" definition="顶点着色器必须写入的裁剪空间位置。" />
  <GlossaryItem term="插值变量" definition="从顶点输出并在图元内部透视插值后送入片元的数据。" />
  <GlossaryItem term="uniform" definition="一次Draw中保持一致、由应用设置的着色器参数。" />
  <GlossaryItem term="可复现实验" definition="固定源码、应用状态、资源、硬件和捕获后可再次得到同一结论的实验。" />
</Glossary>

<Attribution adaptedFrom="Kyle Halladay · Practical Shader Development · 第2章 第一个着色器" adaptedUrl="https://link.springer.com/book/10.1007/978-1-4842-4457-9" />
