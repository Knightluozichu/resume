---
title: "第3章 使用纹理"
type: C
section: "Shader开发实战 · 基础效果"
order: 3
description: "沿Practical Shader Development正式目录掌握UV坐标、sampler2D、UV滚动、颜色数学、mix。"
demo: true
math: true
sourceUrl: "https://link.springer.com/book/10.1007/978-1-4842-4457-9"
draft: false
---

import { Objectives, Callout, Attribution, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step } from "@/components/mdx/mdx-components";
import { PsdUsingTexturesPipelineLab, PsdUsingTexturesCompareLab, PsdUsingTexturesEvidenceLab } from "@/components/mdx/shader-practice/diagrams/psd-03-using-textures";

<Objectives>
- 能解释UV坐标、sampler2D和UV滚动在渲染数据流中的作用
- 能推导本页公式并用GLSL、应用状态或引擎材质实现最小实验
- 能复现mix，以参考图、GPU捕获、边界输入和反例验收
</Objectives>

## 从画面开始

先预测顶点、片元、深度和帧时间会如何变化，再只调整一个参数观察变化。四边形由两个三角形组成并共享连续UV。纹理采样结果可与顶点颜色或uniform相乘，滚动UV使用时间参数但仍需处理wrap模式。mix的权重零返回第一输入，一返回第二输入；若颜色纹理使用sRGB，混合应在合适色彩空间执行。 本页用<Term def="把网格表面位置映射到二维纹理域的参数。">UV坐标</Term>、<Term def="组合二维纹理与采样状态并供GLSL texture函数读取的类型。">sampler2D</Term>、<Term def="给纹理坐标增加随时间变化偏移以形成运动。">UV滚动</Term>、<Term def="按分量缩放、相加或相乘颜色以控制亮度和调色。">颜色数学</Term>、<Term def="按权重在线性插值两个标量、向量或颜色的GLSL函数。">mix</Term>建立对象、阶段、状态和证据之间的联系。

<PsdUsingTexturesPipelineLab />

## 第一版权威目录定位

本页一一对应Kyle Halladay《Practical Shader Development: Vertex and Fragment Shaders for Game Developers》第一版第3章 使用纹理。Springer/Apress正式目录确认2019年第一版、18章、附录代码、ISBN 978-1-4842-4456-2和XXI+381页；清华大学出版社官方样章确认2021中文第一版《Shader开发实战》、ISBN 9787302583349及对应中文目录。

### 原书小节覆盖

1. 3.1 创建四边形
2. 3.2 UV坐标
3. 3.3 在着色器中使用纹理
4. 3.4 滚动UV
5. 3.5 uniform调节亮度
6. 3.6 颜色数学
7. 3.7 mix混合纹理

### 核心对象

1. **UV坐标**：把网格表面位置映射到二维纹理域的参数。
2. **sampler2D**：组合二维纹理与采样状态并供GLSL texture函数读取的类型。
3. **UV滚动**：给纹理坐标增加随时间变化偏移以形成运动。
4. **颜色数学**：按分量缩放、相加或相乘颜色以控制亮度和调色。
5. **mix**：按权重在线性插值两个标量、向量或颜色的GLSL函数。

## 数据流与公式

四边形由两个三角形组成并共享连续UV。纹理采样结果可与顶点颜色或uniform相乘，滚动UV使用时间参数但仍需处理wrap模式。mix的权重零返回第一输入，一返回第二输入；若颜色纹理使用sRGB，混合应在合适色彩空间执行。 前15章以OpenGL 4.1、GLSL和openFrameworks为实验环境；第16至18章再映射到Unity、UE4与Godot。所有代码都要把可编程Shader与应用侧资源绑定、Draw状态、深度和混合分开记录。

$$
uv'=uv+t\,speed
$$

$$
C'=brightness\,C
$$

$$
mix(a,b,w)=(1-w)a+wb
$$

$$
C=C_1C_2
$$

公式中的位置、方向、颜色、深度和时间必须标注空间、范围与单位。片元输出只是候选颜色，最终结果还经过裁剪、深度、模板、混合与颜色编码。

## 最小实现

```glsl
vec2 uv2 = uv + vec2(time * speed, 0.0);
vec4 color = mix(texture(texA,uv2), texture(texB,uv), blend);
```

实现顺序固定为常量输出、输入可视化、核心公式、应用状态和优化。先验证Shader编译与链接，再检查属性位置、uniform、纹理单元和Framebuffer；只有画面与状态都正确后才比较性能。

<PsdUsingTexturesCompareLab />

## 应用与引擎侧契约

```cpp
shader.begin();
shader.setUniform1f("time", elapsedSeconds);
shader.setUniformTexture("mainTex", texture, 0);
mesh.draw();
shader.end();
```

应用侧保存网格布局、索引、纹理格式、采样器、矩阵顺序、混合与深度状态。引擎章节则把同一概念映射到Material、节点图或ShaderMaterial；语法可以变化，输入空间与合成语义不能靠猜。

## 边界、精度与性能

测试零向量、退化UV、负缩放、近远裁剪面、透明交叉、纹理边缘、大世界坐标、长时间运行和高过度绘制。性能实验固定硬件、分辨率、VSync与场景，先判断CPU还是GPU受限，再用捕获定位具体Draw和Shader阶段。

```text
renderer: OpenGL 4.1 / named engine and version
scene: fixed mesh, camera, lights, textures
state: depth, cull, blend, target
evidence: image diff, frame capture, CPU/GPU ms
counterexample: smallest reproducible failure
```

<PsdUsingTexturesEvidenceLab />

## 分步实验

<Stepper>
  <Step title="固定输入和状态"><PsdUsingTexturesPipelineLab /></Step>
  <Step title="逐阶段实现与比较"><PsdUsingTexturesCompareLab /></Step>
  <Step title="捕获画面和性能"><PsdUsingTexturesEvidenceLab /></Step>
</Stepper>

1. 用最小网格与常量颜色验证Shader编译、链接和Draw状态。
2. 可视化位置、UV、法线、深度或光照项，确认范围和空间。
3. 打开本章核心效果并保存基线与候选画面。
4. 构造边界与失败输入，用GPU捕获和计时解释差异。

## 验收证书

证书包含源码与编译版本、应用绑定、资源哈希、状态、参考图、GPU事件、CPU与GPU帧时间和失败反例。纹理坐标正确但采样器绑定到错误纹理单元，画面异常时仍只修改GLSL。 修复后必须重放同一输入并说明差异来自哪个阶段。

## 常见误区

<Callout type="trap" title="把语法当原理">纹理坐标正确但采样器绑定到错误纹理单元，画面异常时仍只修改GLSL。</Callout>

<Callout type="trap" title="只看最终画面">画面相似不证明空间、深度、混合和性能等价。必须保存中间调试视图、固定状态和GPU捕获。</Callout>

## 本章回顾

本页覆盖UV坐标、sampler2D、UV滚动、颜色数学、mix。掌握标准是能画出数据流，写出最小实现，说明应用或引擎状态，再用边界、精度与性能证据复现结论。

<Exercises>
**问题1：UV坐标与sampler2D如何连接？**
<Answer>把网格表面位置映射到二维纹理域的参数。 组合二维纹理与采样状态并供GLSL texture函数读取的类型。 回答时标注阶段和输入来源。</Answer>

**问题2：怎样调试UV滚动？**
<Answer>先输出常量，再把相关中间值映射为颜色；随后核对应用绑定、固定状态和GPU事件。</Answer>

**问题3：如何验收mix？**
<Answer>按权重在线性插值两个标量、向量或颜色的GLSL函数。 同时保存正常例、边界例、失败反例和CPU/GPU计时。</Answer>
</Exercises>

## 术语表

<Glossary>
  <GlossaryItem term="UV坐标" definition="把网格表面位置映射到二维纹理域的参数。" />
  <GlossaryItem term="sampler2D" definition="组合二维纹理与采样状态并供GLSL texture函数读取的类型。" />
  <GlossaryItem term="UV滚动" definition="给纹理坐标增加随时间变化偏移以形成运动。" />
  <GlossaryItem term="颜色数学" definition="按分量缩放、相加或相乘颜色以控制亮度和调色。" />
  <GlossaryItem term="mix" definition="按权重在线性插值两个标量、向量或颜色的GLSL函数。" />
  <GlossaryItem term="可复现实验" definition="固定源码、应用状态、资源、硬件和捕获后可再次得到同一结论的实验。" />
</Glossary>

<Attribution adaptedFrom="Kyle Halladay · Practical Shader Development · 第3章 使用纹理" adaptedUrl="https://link.springer.com/book/10.1007/978-1-4842-4457-9" />
