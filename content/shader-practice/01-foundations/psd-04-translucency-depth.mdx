---
title: "第4章 半透明与深度"
type: C
section: "Shader开发实战 · 基础效果"
order: 4
description: "沿Practical Shader Development正式目录掌握alpha测试、深度测试、alpha混合、加法混合、精灵表。"
demo: true
math: true
sourceUrl: "https://link.springer.com/book/10.1007/978-1-4842-4457-9"
draft: true
---

import { Objectives, Callout, Attribution, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step } from "@/components/mdx/mdx-components";
import { PsdTranslucencyDepthPipelineLab, PsdTranslucencyDepthCompareLab, PsdTranslucencyDepthEvidenceLab } from "@/components/mdx/shader-practice/diagrams/psd-04-translucency-depth";

<Objectives>
- 能解释alpha测试、深度测试和alpha混合在渲染数据流中的作用
- 能推导本页公式并用GLSL、应用状态或引擎材质实现最小实验
- 能复现精灵表，以参考图、GPU捕获、边界输入和反例验收
</Objectives>

## 从画面开始

先预测顶点、片元、深度和帧时间会如何变化，再只调整一个参数观察变化。透明度测试仍可写深度，适合硬边遮罩；alpha混合常需关闭深度写入并按远到近排序。加法混合对顺序不敏感但会累积亮度。精灵表动画把时间映射到行列索引，必须避免最后一帧越界。 本页用<Term def="按透明度阈值discard片元的二值透明方法。">alpha测试</Term>、<Term def="比较片元深度与深度缓冲并决定可见性的过程。">深度测试</Term>、<Term def="按源alpha组合源颜色与目标颜色的半透明方法。">alpha混合</Term>、<Term def="把源颜色加到目标颜色、常用于发光粒子的混合模式。">加法混合</Term>、<Term def="在一张纹理中排列多帧并按时间选择UV子区域的动画资源。">精灵表</Term>建立对象、阶段、状态和证据之间的联系。

<PsdTranslucencyDepthPipelineLab />

## 第一版权威目录定位

本页一一对应Kyle Halladay《Practical Shader Development: Vertex and Fragment Shaders for Game Developers》第一版第4章 半透明与深度。Springer/Apress正式目录确认2019年第一版、18章、附录代码、ISBN 978-1-4842-4456-2和XXI+381页；清华大学出版社官方样章确认2021中文第一版《Shader开发实战》、ISBN 9787302583349及对应中文目录。

### 原书小节覆盖

1. 4.1 设置示例项目
2. 4.3 alpha测试和丢弃
3. 4.4 深度测试
4. 4.5 alpha混合
5. 4.7 alpha混合原理
6. 4.8 加法混合
7. 4.9 精灵表动画

### 核心对象

1. **alpha测试**：按透明度阈值discard片元的二值透明方法。
2. **深度测试**：比较片元深度与深度缓冲并决定可见性的过程。
3. **alpha混合**：按源alpha组合源颜色与目标颜色的半透明方法。
4. **加法混合**：把源颜色加到目标颜色、常用于发光粒子的混合模式。
5. **精灵表**：在一张纹理中排列多帧并按时间选择UV子区域的动画资源。

## 数据流与公式

透明度测试仍可写深度，适合硬边遮罩；alpha混合常需关闭深度写入并按远到近排序。加法混合对顺序不敏感但会累积亮度。精灵表动画把时间映射到行列索引，必须避免最后一帧越界。 前15章以OpenGL 4.1、GLSL和openFrameworks为实验环境；第16至18章再映射到Unity、UE4与Godot。所有代码都要把可编程Shader与应用侧资源绑定、Draw状态、深度和混合分开记录。

$$
C_o=\alpha C_s+(1-\alpha)C_d
$$

$$
C_o=C_s+C_d
$$

$$
discard\ if\ \alpha<\tau
$$

$$
frame=\lfloor t f\rfloor\bmod N
$$

公式中的位置、方向、颜色、深度和时间必须标注空间、范围与单位。片元输出只是候选颜色，最终结果还经过裁剪、深度、模板、混合与颜色编码。

## 最小实现

```glsl
if(color.a < cutoff) discard;
fragColor = color; // blend state is configured by the application
```

实现顺序固定为常量输出、输入可视化、核心公式、应用状态和优化。先验证Shader编译与链接，再检查属性位置、uniform、纹理单元和Framebuffer；只有画面与状态都正确后才比较性能。

<PsdTranslucencyDepthCompareLab />

## 应用与引擎侧契约

```cpp
shader.begin();
shader.setUniform1f("time", elapsedSeconds);
shader.setUniformTexture("mainTex", texture, 0);
mesh.draw();
shader.end();
```

应用侧保存网格布局、索引、纹理格式、采样器、矩阵顺序、混合与深度状态。引擎章节则把同一概念映射到Material、节点图或ShaderMaterial；语法可以变化，输入空间与合成语义不能靠猜。

## 边界、精度与性能

测试零向量、退化UV、负缩放、近远裁剪面、透明交叉、纹理边缘、大世界坐标、长时间运行和高过度绘制。性能实验固定硬件、分辨率、VSync与场景，先判断CPU还是GPU受限，再用捕获定位具体Draw和Shader阶段。

```text
renderer: OpenGL 4.1 / named engine and version
scene: fixed mesh, camera, lights, textures
state: depth, cull, blend, target
evidence: image diff, frame capture, CPU/GPU ms
counterexample: smallest reproducible failure
```

<PsdTranslucencyDepthEvidenceLab />

## 分步实验

<Stepper>
  <Step title="固定输入和状态"><PsdTranslucencyDepthPipelineLab /></Step>
  <Step title="逐阶段实现与比较"><PsdTranslucencyDepthCompareLab /></Step>
  <Step title="捕获画面和性能"><PsdTranslucencyDepthEvidenceLab /></Step>
</Stepper>

1. 用最小网格与常量颜色验证Shader编译、链接和Draw状态。
2. 可视化位置、UV、法线、深度或光照项，确认范围和空间。
3. 打开本章核心效果并保存基线与候选画面。
4. 构造边界与失败输入，用GPU捕获和计时解释差异。

## 验收证书

证书包含源码与编译版本、应用绑定、资源哈希、状态、参考图、GPU事件、CPU与GPU帧时间和失败反例。只改片元alpha却没有启用正确混合状态或排序，误以为着色器独自决定透明。 修复后必须重放同一输入并说明差异来自哪个阶段。

## 常见误区

<Callout type="trap" title="把语法当原理">只改片元alpha却没有启用正确混合状态或排序，误以为着色器独自决定透明。</Callout>

<Callout type="trap" title="只看最终画面">画面相似不证明空间、深度、混合和性能等价。必须保存中间调试视图、固定状态和GPU捕获。</Callout>

## 本章回顾

本页覆盖alpha测试、深度测试、alpha混合、加法混合、精灵表。掌握标准是能画出数据流，写出最小实现，说明应用或引擎状态，再用边界、精度与性能证据复现结论。

<Exercises>
**问题1：alpha测试与深度测试如何连接？**
<Answer>按透明度阈值discard片元的二值透明方法。 比较片元深度与深度缓冲并决定可见性的过程。 回答时标注阶段和输入来源。</Answer>

**问题2：怎样调试alpha混合？**
<Answer>先输出常量，再把相关中间值映射为颜色；随后核对应用绑定、固定状态和GPU事件。</Answer>

**问题3：如何验收精灵表？**
<Answer>在一张纹理中排列多帧并按时间选择UV子区域的动画资源。 同时保存正常例、边界例、失败反例和CPU/GPU计时。</Answer>
</Exercises>

## 术语表

<Glossary>
  <GlossaryItem term="alpha测试" definition="按透明度阈值discard片元的二值透明方法。" />
  <GlossaryItem term="深度测试" definition="比较片元深度与深度缓冲并决定可见性的过程。" />
  <GlossaryItem term="alpha混合" definition="按源alpha组合源颜色与目标颜色的半透明方法。" />
  <GlossaryItem term="加法混合" definition="把源颜色加到目标颜色、常用于发光粒子的混合模式。" />
  <GlossaryItem term="精灵表" definition="在一张纹理中排列多帧并按时间选择UV子区域的动画资源。" />
  <GlossaryItem term="可复现实验" definition="固定源码、应用状态、资源、硬件和捕获后可再次得到同一结论的实验。" />
</Glossary>

<Attribution adaptedFrom="Kyle Halladay · Practical Shader Development · 第4章 半透明与深度" adaptedUrl="https://link.springer.com/book/10.1007/978-1-4842-4457-9" />
