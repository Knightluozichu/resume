---
title: "第5章 使物体动起来"
type: C
section: "Shader开发实战 · 基础效果"
order: 5
description: "沿Practical Shader Development正式目录掌握平移矩阵、缩放矩阵、旋转矩阵、齐次坐标、单位矩阵。"
demo: true
math: true
sourceUrl: "https://link.springer.com/book/10.1007/978-1-4842-4457-9"
draft: false
---

import { Objectives, Callout, Attribution, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step } from "@/components/mdx/mdx-components";
import { PsdMakingThingsMovePipelineLab, PsdMakingThingsMoveCompareLab, PsdMakingThingsMoveEvidenceLab } from "@/components/mdx/shader-practice/diagrams/psd-05-making-things-move";

<Objectives>
- 能解释平移矩阵、缩放矩阵和旋转矩阵在渲染数据流中的作用
- 能推导本页公式并用GLSL、应用状态或引擎材质实现最小实验
- 能复现单位矩阵，以参考图、GPU捕获、边界输入和反例验收
</Objectives>

## 从画面开始

先预测顶点、片元、深度和帧时间会如何变化，再只调整一个参数观察变化。顶点运动应区分修改模型顶点与移动摄像机。矩阵组合顺序决定先缩放、旋转还是平移；围绕任意中心旋转需先移到原点、旋转再移回。时间只驱动参数，不应让帧率直接影响运动速度。 本页用<Term def="在齐次坐标中把位置沿指定方向移动的矩阵。">平移矩阵</Term>、<Term def="按轴改变位置相对原点距离的矩阵。">缩放矩阵</Term>、<Term def="保持长度并围绕原点或轴改变方向的矩阵。">旋转矩阵</Term>、<Term def="用第四分量统一表达线性变换与平移的表示。">齐次坐标</Term>、<Term def="乘以任何兼容向量或矩阵都保持不变的矩阵。">单位矩阵</Term>建立对象、阶段、状态和证据之间的联系。

<PsdMakingThingsMovePipelineLab />

## 第一版权威目录定位

本页一一对应Kyle Halladay《Practical Shader Development: Vertex and Fragment Shaders for Game Developers》第一版第5章 使物体动起来。Springer/Apress正式目录确认2019年第一版、18章、附录代码、ISBN 978-1-4842-4456-2和XXI+381页；清华大学出版社官方样章确认2021中文第一版《Shader开发实战》、ISBN 9787302583349及对应中文目录。

### 原书小节覆盖

1. 5.1 让角色前进
2. 5.2 缩放云朵
3. 5.3 顶点着色器旋转物体
4. 5.4 变换矩阵
5. 5.5 为矩阵添加动态
6. 5.6 单位矩阵

### 核心对象

1. **平移矩阵**：在齐次坐标中把位置沿指定方向移动的矩阵。
2. **缩放矩阵**：按轴改变位置相对原点距离的矩阵。
3. **旋转矩阵**：保持长度并围绕原点或轴改变方向的矩阵。
4. **齐次坐标**：用第四分量统一表达线性变换与平移的表示。
5. **单位矩阵**：乘以任何兼容向量或矩阵都保持不变的矩阵。

## 数据流与公式

顶点运动应区分修改模型顶点与移动摄像机。矩阵组合顺序决定先缩放、旋转还是平移；围绕任意中心旋转需先移到原点、旋转再移回。时间只驱动参数，不应让帧率直接影响运动速度。 前15章以OpenGL 4.1、GLSL和openFrameworks为实验环境；第16至18章再映射到Unity、UE4与Godot。所有代码都要把可编程Shader与应用侧资源绑定、Draw状态、深度和混合分开记录。

$$
p'=T R S p
$$

$$
R(\theta)^TR(\theta)=I
$$

$$
p_{pivot}'=T(c)R T(-c)p
$$

$$
\theta(t)=\omega t
$$

公式中的位置、方向、颜色、深度和时间必须标注空间、范围与单位。片元输出只是候选颜色，最终结果还经过裁剪、深度、模板、混合与颜色编码。

## 最小实现

```glsl
mat4 model = translate(offset) * rotate(angle) * scale(scaleValue);
gl_Position = projection * view * model * vec4(position,1.0);
```

实现顺序固定为常量输出、输入可视化、核心公式、应用状态和优化。先验证Shader编译与链接，再检查属性位置、uniform、纹理单元和Framebuffer；只有画面与状态都正确后才比较性能。

<PsdMakingThingsMoveCompareLab />

## 应用与引擎侧契约

```cpp
shader.begin();
shader.setUniform1f("time", elapsedSeconds);
shader.setUniformTexture("mainTex", texture, 0);
mesh.draw();
shader.end();
```

应用侧保存网格布局、索引、纹理格式、采样器、矩阵顺序、混合与深度状态。引擎章节则把同一概念映射到Material、节点图或ShaderMaterial；语法可以变化，输入空间与合成语义不能靠猜。

## 边界、精度与性能

测试零向量、退化UV、负缩放、近远裁剪面、透明交叉、纹理边缘、大世界坐标、长时间运行和高过度绘制。性能实验固定硬件、分辨率、VSync与场景，先判断CPU还是GPU受限，再用捕获定位具体Draw和Shader阶段。

```text
renderer: OpenGL 4.1 / named engine and version
scene: fixed mesh, camera, lights, textures
state: depth, cull, blend, target
evidence: image diff, frame capture, CPU/GPU ms
counterexample: smallest reproducible failure
```

<PsdMakingThingsMoveEvidenceLab />

## 分步实验

<Stepper>
  <Step title="固定输入和状态"><PsdMakingThingsMovePipelineLab /></Step>
  <Step title="逐阶段实现与比较"><PsdMakingThingsMoveCompareLab /></Step>
  <Step title="捕获画面和性能"><PsdMakingThingsMoveEvidenceLab /></Step>
</Stepper>

1. 用最小网格与常量颜色验证Shader编译、链接和Draw状态。
2. 可视化位置、UV、法线、深度或光照项，确认范围和空间。
3. 打开本章核心效果并保存基线与候选画面。
4. 构造边界与失败输入，用GPU捕获和计时解释差异。

## 验收证书

证书包含源码与编译版本、应用绑定、资源哈希、状态、参考图、GPU事件、CPU与GPU帧时间和失败反例。矩阵乘法顺序与GLSL列向量约定混淆，物体围绕世界原点而非自身中心旋转。 修复后必须重放同一输入并说明差异来自哪个阶段。

## 常见误区

<Callout type="trap" title="把语法当原理">矩阵乘法顺序与GLSL列向量约定混淆，物体围绕世界原点而非自身中心旋转。</Callout>

<Callout type="trap" title="只看最终画面">画面相似不证明空间、深度、混合和性能等价。必须保存中间调试视图、固定状态和GPU捕获。</Callout>

## 本章回顾

本页覆盖平移矩阵、缩放矩阵、旋转矩阵、齐次坐标、单位矩阵。掌握标准是能画出数据流，写出最小实现，说明应用或引擎状态，再用边界、精度与性能证据复现结论。

<Exercises>
**问题1：平移矩阵与缩放矩阵如何连接？**
<Answer>在齐次坐标中把位置沿指定方向移动的矩阵。 按轴改变位置相对原点距离的矩阵。 回答时标注阶段和输入来源。</Answer>

**问题2：怎样调试旋转矩阵？**
<Answer>先输出常量，再把相关中间值映射为颜色；随后核对应用绑定、固定状态和GPU事件。</Answer>

**问题3：如何验收单位矩阵？**
<Answer>乘以任何兼容向量或矩阵都保持不变的矩阵。 同时保存正常例、边界例、失败反例和CPU/GPU计时。</Answer>
</Exercises>

## 术语表

<Glossary>
  <GlossaryItem term="平移矩阵" definition="在齐次坐标中把位置沿指定方向移动的矩阵。" />
  <GlossaryItem term="缩放矩阵" definition="按轴改变位置相对原点距离的矩阵。" />
  <GlossaryItem term="旋转矩阵" definition="保持长度并围绕原点或轴改变方向的矩阵。" />
  <GlossaryItem term="齐次坐标" definition="用第四分量统一表达线性变换与平移的表示。" />
  <GlossaryItem term="单位矩阵" definition="乘以任何兼容向量或矩阵都保持不变的矩阵。" />
  <GlossaryItem term="可复现实验" definition="固定源码、应用状态、资源、硬件和捕获后可再次得到同一结论的实验。" />
</Glossary>

<Attribution adaptedFrom="Kyle Halladay · Practical Shader Development · 第5章 使物体动起来" adaptedUrl="https://link.springer.com/book/10.1007/978-1-4842-4457-9" />
