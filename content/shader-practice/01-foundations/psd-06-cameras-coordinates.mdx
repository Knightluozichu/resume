---
title: "第6章 摄像机和坐标"
type: C
section: "Shader开发实战 · 基础效果"
order: 6
description: "沿Practical Shader Development正式目录掌握视图矩阵、模型矩阵、投影矩阵、视锥体、透视除法。"
demo: true
math: true
sourceUrl: "https://link.springer.com/book/10.1007/978-1-4842-4457-9"
draft: true
---

import { Objectives, Callout, Attribution, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step } from "@/components/mdx/mdx-components";
import { PsdCamerasCoordinatesPipelineLab, PsdCamerasCoordinatesCompareLab, PsdCamerasCoordinatesEvidenceLab } from "@/components/mdx/shader-practice/diagrams/psd-06-cameras-coordinates";

<Objectives>
- 能解释视图矩阵、模型矩阵和投影矩阵在渲染数据流中的作用
- 能推导本页公式并用GLSL、应用状态或引擎材质实现最小实验
- 能复现透视除法，以参考图、GPU捕获、边界输入和反例验收
</Objectives>

## 从画面开始

先预测顶点、片元、深度和帧时间会如何变化，再只调整一个参数观察变化。模型、世界、观察、裁剪和NDC空间必须逐段记录。视图矩阵可看作摄像机世界变换的逆，投影矩阵把透视视锥映射到标准裁剪体；GPU再做透视除法。近裁剪面过小会恶化深度精度。 本页用<Term def="把世界空间坐标变换到摄像机观察空间的矩阵。">视图矩阵</Term>、<Term def="把模型局部坐标放置到世界空间的矩阵。">模型矩阵</Term>、<Term def="把观察空间视锥映射到裁剪空间的矩阵。">投影矩阵</Term>、<Term def="由视野、宽高比和近远裁剪面定义的可见体积。">视锥体</Term>、<Term def="用裁剪坐标除以w得到归一化设备坐标的步骤。">透视除法</Term>建立对象、阶段、状态和证据之间的联系。

<PsdCamerasCoordinatesPipelineLab />

## 第一版权威目录定位

本页一一对应Kyle Halladay《Practical Shader Development: Vertex and Fragment Shaders for Game Developers》第一版第6章 摄像机和坐标。Springer/Apress正式目录确认2019年第一版、18章、附录代码、ISBN 978-1-4842-4456-2和XXI+381页；清华大学出版社官方样章确认2021中文第一版《Shader开发实战》、ISBN 9787302583349及对应中文目录。

### 原书小节覆盖

1. 6.1 使用视图矩阵
2. 6.2 变换矩阵和坐标空间
3. 6.3 摄像机视锥体和投影

### 核心对象

1. **视图矩阵**：把世界空间坐标变换到摄像机观察空间的矩阵。
2. **模型矩阵**：把模型局部坐标放置到世界空间的矩阵。
3. **投影矩阵**：把观察空间视锥映射到裁剪空间的矩阵。
4. **视锥体**：由视野、宽高比和近远裁剪面定义的可见体积。
5. **透视除法**：用裁剪坐标除以w得到归一化设备坐标的步骤。

## 数据流与公式

模型、世界、观察、裁剪和NDC空间必须逐段记录。视图矩阵可看作摄像机世界变换的逆，投影矩阵把透视视锥映射到标准裁剪体；GPU再做透视除法。近裁剪面过小会恶化深度精度。 前15章以OpenGL 4.1、GLSL和openFrameworks为实验环境；第16至18章再映射到Unity、UE4与Godot。所有代码都要把可编程Shader与应用侧资源绑定、Draw状态、深度和混合分开记录。

$$
p_c=PVMp_o
$$

$$
p_{ndc}=p_c/p_c.w
$$

$$
aspect=w/h
$$

$$
V=C^{-1}
$$

公式中的位置、方向、颜色、深度和时间必须标注空间、范围与单位。片元输出只是候选颜色，最终结果还经过裁剪、深度、模板、混合与颜色编码。

## 最小实现

```glsl
vec4 worldPos = model * vec4(position,1.0);
vec4 viewPos = view * worldPos;
gl_Position = projection * viewPos;
```

实现顺序固定为常量输出、输入可视化、核心公式、应用状态和优化。先验证Shader编译与链接，再检查属性位置、uniform、纹理单元和Framebuffer；只有画面与状态都正确后才比较性能。

<PsdCamerasCoordinatesCompareLab />

## 应用与引擎侧契约

```cpp
shader.begin();
shader.setUniform1f("time", elapsedSeconds);
shader.setUniformTexture("mainTex", texture, 0);
mesh.draw();
shader.end();
```

应用侧保存网格布局、索引、纹理格式、采样器、矩阵顺序、混合与深度状态。引擎章节则把同一概念映射到Material、节点图或ShaderMaterial；语法可以变化，输入空间与合成语义不能靠猜。

## 边界、精度与性能

测试零向量、退化UV、负缩放、近远裁剪面、透明交叉、纹理边缘、大世界坐标、长时间运行和高过度绘制。性能实验固定硬件、分辨率、VSync与场景，先判断CPU还是GPU受限，再用捕获定位具体Draw和Shader阶段。

```text
renderer: OpenGL 4.1 / named engine and version
scene: fixed mesh, camera, lights, textures
state: depth, cull, blend, target
evidence: image diff, frame capture, CPU/GPU ms
counterexample: smallest reproducible failure
```

<PsdCamerasCoordinatesEvidenceLab />

## 分步实验

<Stepper>
  <Step title="固定输入和状态"><PsdCamerasCoordinatesPipelineLab /></Step>
  <Step title="逐阶段实现与比较"><PsdCamerasCoordinatesCompareLab /></Step>
  <Step title="捕获画面和性能"><PsdCamerasCoordinatesEvidenceLab /></Step>
</Stepper>

1. 用最小网格与常量颜色验证Shader编译、链接和Draw状态。
2. 可视化位置、UV、法线、深度或光照项，确认范围和空间。
3. 打开本章核心效果并保存基线与候选画面。
4. 构造边界与失败输入，用GPU捕获和计时解释差异。

## 验收证书

证书包含源码与编译版本、应用绑定、资源哈希、状态、参考图、GPU事件、CPU与GPU帧时间和失败反例。把NDC、裁剪空间和屏幕像素空间混为一谈，调试深度与UV时得到错误范围。 修复后必须重放同一输入并说明差异来自哪个阶段。

## 常见误区

<Callout type="trap" title="把语法当原理">把NDC、裁剪空间和屏幕像素空间混为一谈，调试深度与UV时得到错误范围。</Callout>

<Callout type="trap" title="只看最终画面">画面相似不证明空间、深度、混合和性能等价。必须保存中间调试视图、固定状态和GPU捕获。</Callout>

## 本章回顾

本页覆盖视图矩阵、模型矩阵、投影矩阵、视锥体、透视除法。掌握标准是能画出数据流，写出最小实现，说明应用或引擎状态，再用边界、精度与性能证据复现结论。

<Exercises>
**问题1：视图矩阵与模型矩阵如何连接？**
<Answer>把世界空间坐标变换到摄像机观察空间的矩阵。 把模型局部坐标放置到世界空间的矩阵。 回答时标注阶段和输入来源。</Answer>

**问题2：怎样调试投影矩阵？**
<Answer>先输出常量，再把相关中间值映射为颜色；随后核对应用绑定、固定状态和GPU事件。</Answer>

**问题3：如何验收透视除法？**
<Answer>用裁剪坐标除以w得到归一化设备坐标的步骤。 同时保存正常例、边界例、失败反例和CPU/GPU计时。</Answer>
</Exercises>

## 术语表

<Glossary>
  <GlossaryItem term="视图矩阵" definition="把世界空间坐标变换到摄像机观察空间的矩阵。" />
  <GlossaryItem term="模型矩阵" definition="把模型局部坐标放置到世界空间的矩阵。" />
  <GlossaryItem term="投影矩阵" definition="把观察空间视锥映射到裁剪空间的矩阵。" />
  <GlossaryItem term="视锥体" definition="由视野、宽高比和近远裁剪面定义的可见体积。" />
  <GlossaryItem term="透视除法" definition="用裁剪坐标除以w得到归一化设备坐标的步骤。" />
  <GlossaryItem term="可复现实验" definition="固定源码、应用状态、资源、硬件和捕获后可再次得到同一结论的实验。" />
</Glossary>

<Attribution adaptedFrom="Kyle Halladay · Practical Shader Development · 第6章 摄像机和坐标" adaptedUrl="https://link.springer.com/book/10.1007/978-1-4842-4457-9" />
