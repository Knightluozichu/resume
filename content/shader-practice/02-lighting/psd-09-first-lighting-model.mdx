---
title: "第9章 第一个光照模型"
type: C
section: "Shader开发实战 · 三维光照"
order: 9
description: "沿Practical Shader Development正式目录掌握镜面反射、Phong模型、Blinn-Phong、环境光、光照纹理。"
demo: true
math: true
sourceUrl: "https://link.springer.com/book/10.1007/978-1-4842-4457-9"
draft: true
---

import { Objectives, Callout, Attribution, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step } from "@/components/mdx/mdx-components";

<Objectives>
- 能解释镜面反射、Phong模型和Blinn-Phong在渲染数据流中的作用
- 能推导本页公式并用GLSL、应用状态或引擎材质实现最小实验
- 能复现光照纹理，以参考图、GPU捕获、边界输入和反例验收
</Objectives>

## 从画面开始

先预测顶点、片元、深度和帧时间会如何变化，再只调整一个参数观察变化。完整经验光照由环境、漫反射和镜面组成。Phong反射向量与Blinn半程向量不可混用同一指数解释；纹理可以分别调制材质颜色和高光强度。所有光照项应在线性空间累计。 本页用<Term def="沿反射方向集中并随观察方向变化的高光项。">镜面反射</Term>、<Term def="以反射向量和观察向量夹角幂次计算高光的经验模型。">Phong模型</Term>、<Term def="以半程向量和法线夹角幂次计算高光的模型。">Blinn-Phong</Term>、<Term def="用常量近似未显式建模的间接光照。">环境光</Term>、<Term def="用纹理通道控制漫反射、高光颜色或强度的材质数据。">光照纹理</Term>建立对象、阶段、状态和证据之间的联系。

<PsdFirstLightingModelPipelineLab />

## 第一版权威目录定位

本页一一对应Kyle Halladay《Practical Shader Development: Vertex and Fragment Shaders for Game Developers》第一版第9章 第一个光照模型。Springer/Apress正式目录确认2019年第一版、18章、附录代码、ISBN 978-1-4842-4456-2和XXI+381页；清华大学出版社官方样章确认2021中文第一版《Shader开发实战》、ISBN 9787302583349及对应中文目录。

### 原书小节覆盖

1. 9.1 镜面光照
2. 9.2 第一个镜面着色器
3. 9.3 组合漫反射和镜面
4. 9.4 环境光
5. 9.5 Phong模型
6. 9.6 Blinn-Phong
7. 9.7 纹理控制光照

### 核心对象

1. **镜面反射**：沿反射方向集中并随观察方向变化的高光项。
2. **Phong模型**：以反射向量和观察向量夹角幂次计算高光的经验模型。
3. **Blinn-Phong**：以半程向量和法线夹角幂次计算高光的模型。
4. **环境光**：用常量近似未显式建模的间接光照。
5. **光照纹理**：用纹理通道控制漫反射、高光颜色或强度的材质数据。

## 数据流与公式

完整经验光照由环境、漫反射和镜面组成。Phong反射向量与Blinn半程向量不可混用同一指数解释；纹理可以分别调制材质颜色和高光强度。所有光照项应在线性空间累计。 前15章以OpenGL 4.1、GLSL和openFrameworks为实验环境；第16至18章再映射到Unity、UE4与Godot。所有代码都要把可编程Shader与应用侧资源绑定、Draw状态、深度和混合分开记录。

$$
r=reflect(-l,n)
$$

$$
L_s=max(0,r\cdot v)^g
$$

$$
h=normalize(l+v)
$$

$$
C=C_a+C_d+C_s
$$

公式中的位置、方向、颜色、深度和时间必须标注空间、范围与单位。片元输出只是候选颜色，最终结果还经过裁剪、深度、模板、混合与颜色编码。

## 最小实现

```glsl
vec3 h=normalize(lightDir+viewDir);
float spec=pow(max(dot(n,h),0.0),shininess);
vec3 lit=ambient+albedo*diffuse+specColor*spec;
```

实现顺序固定为常量输出、输入可视化、核心公式、应用状态和优化。先验证Shader编译与链接，再检查属性位置、uniform、纹理单元和Framebuffer；只有画面与状态都正确后才比较性能。

<PsdFirstLightingModelCompareLab />

## 应用与引擎侧契约

```cpp
shader.begin();
shader.setUniform1f("time", elapsedSeconds);
shader.setUniformTexture("mainTex", texture, 0);
mesh.draw();
shader.end();
```

应用侧保存网格布局、索引、纹理格式、采样器、矩阵顺序、混合与深度状态。引擎章节则把同一概念映射到Material、节点图或ShaderMaterial；语法可以变化，输入空间与合成语义不能靠猜。

## 边界、精度与性能

测试零向量、退化UV、负缩放、近远裁剪面、透明交叉、纹理边缘、大世界坐标、长时间运行和高过度绘制。性能实验固定硬件、分辨率、VSync与场景，先判断CPU还是GPU受限，再用捕获定位具体Draw和Shader阶段。

```text
renderer: OpenGL 4.1 / named engine and version
scene: fixed mesh, camera, lights, textures
state: depth, cull, blend, target
evidence: image diff, frame capture, CPU/GPU ms
counterexample: smallest reproducible failure
```

1. 用最小网格与常量颜色验证Shader编译、链接和Draw状态。
2. 可视化位置、UV、法线、深度或光照项，确认范围和空间。
3. 打开本章核心效果并保存基线与候选画面。
4. 构造边界与失败输入，用GPU捕获和计时解释差异。

## 验收证书

证书包含源码与编译版本、应用绑定、资源哈希、状态、参考图、GPU事件、CPU与GPU帧时间和失败反例。用未归一化光线和观察向量计算半程向量，高光位置随距离异常漂移。 修复后必须重放同一输入并说明差异来自哪个阶段。

## 常见误区

<Callout type="trap" title="把语法当原理">用未归一化光线和观察向量计算半程向量，高光位置随距离异常漂移。</Callout>

<Callout type="trap" title="只看最终画面">画面相似不证明空间、深度、混合和性能等价。必须保存中间调试视图、固定状态和GPU捕获。</Callout>

## 本章回顾

本页覆盖镜面反射、Phong模型、Blinn-Phong、环境光、光照纹理。掌握标准是能画出数据流，写出最小实现，说明应用或引擎状态，再用边界、精度与性能证据复现结论。

<Exercises>
**问题1：镜面反射与Phong模型如何连接？**
<Answer>沿反射方向集中并随观察方向变化的高光项。 以反射向量和观察向量夹角幂次计算高光的经验模型。 回答时标注阶段和输入来源。</Answer>

**问题2：怎样调试Blinn-Phong？**
<Answer>先输出常量，再把相关中间值映射为颜色；随后核对应用绑定、固定状态和GPU事件。</Answer>

**问题3：如何验收光照纹理？**
<Answer>用纹理通道控制漫反射、高光颜色或强度的材质数据。 同时保存正常例、边界例、失败反例和CPU/GPU计时。</Answer>
</Exercises>

## 术语表

<Glossary>
  <GlossaryItem term="镜面反射" definition="沿反射方向集中并随观察方向变化的高光项。" />
  <GlossaryItem term="Phong模型" definition="以反射向量和观察向量夹角幂次计算高光的经验模型。" />
  <GlossaryItem term="Blinn-Phong" definition="以半程向量和法线夹角幂次计算高光的模型。" />
  <GlossaryItem term="环境光" definition="用常量近似未显式建模的间接光照。" />
  <GlossaryItem term="光照纹理" definition="用纹理通道控制漫反射、高光颜色或强度的材质数据。" />
  <GlossaryItem term="可复现实验" definition="固定源码、应用状态、资源、硬件和捕获后可再次得到同一结论的实验。" />
</Glossary>

<Attribution adaptedFrom="Kyle Halladay · Practical Shader Development · 第9章 第一个光照模型" adaptedUrl="https://link.springer.com/book/10.1007/978-1-4842-4457-9" />
