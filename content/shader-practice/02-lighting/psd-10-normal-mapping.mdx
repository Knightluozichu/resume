---
title: "第10章 法线贴图"
type: C
section: "Shader开发实战 · 三维光照"
order: 10
description: "沿Practical Shader Development正式目录掌握法线贴图、切线空间、切线向量、副切线、TBN矩阵。"
demo: true
math: true
sourceUrl: "https://link.springer.com/book/10.1007/978-1-4842-4457-9"
draft: false
---

import { Objectives, Callout, Attribution, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step } from "@/components/mdx/mdx-components";
import { PsdNormalMappingPipelineLab, PsdNormalMappingCompareLab, PsdNormalMappingEvidenceLab } from "@/components/mdx/shader-practice/diagrams/psd-10-normal-mapping";

<Objectives>
- 能解释法线贴图、切线空间和切线向量在渲染数据流中的作用
- 能推导本页公式并用GLSL、应用状态或引擎材质实现最小实验
- 能复现TBN矩阵，以参考图、GPU捕获、边界输入和反例验收
</Objectives>

## 从画面开始

先预测顶点、片元、深度和帧时间会如何变化，再只调整一个参数观察变化。法线纹理采样值需从零到一解码到负一到一。网格切线、法线和手性构成TBN；可把法线变到世界空间，也可把光线和视线变到切线空间。水面通过滚动和混合多张法线图形成动态细节。 本页用<Term def="在纹理中编码表面微小方向变化以改变光照而不增加几何的技术。">法线贴图</Term>、<Term def="由切线、副切线和法线构成并随表面UV变化的局部坐标系。">切线空间</Term>、<Term def="沿纹理u方向的表面方向，常作为网格顶点属性。">切线向量</Term>、<Term def="由法线与切线叉积并结合手性得到的v方向。">副切线</Term>、<Term def="在切线空间与模型或世界空间间变换方向的基矩阵。">TBN矩阵</Term>建立对象、阶段、状态和证据之间的联系。

<PsdNormalMappingPipelineLab />

## 第一版权威目录定位

本页一一对应Kyle Halladay《Practical Shader Development: Vertex and Fragment Shaders for Game Developers》第一版第10章 法线贴图。Springer/Apress正式目录确认2019年第一版、18章、附录代码、ISBN 978-1-4842-4456-2和XXI+381页；清华大学出版社官方样章确认2021中文第一版《Shader开发实战》、ISBN 9787302583349及对应中文目录。

### 原书小节覆盖

1. 10.1 什么是法线贴图
2. 10.2 切线空间
3. 10.3 使用切线向量
4. 10.4 叉积
5. 10.5 法线贴图原理
6. 10.6 编写水面着色器
7. 10.7 更多信息

### 核心对象

1. **法线贴图**：在纹理中编码表面微小方向变化以改变光照而不增加几何的技术。
2. **切线空间**：由切线、副切线和法线构成并随表面UV变化的局部坐标系。
3. **切线向量**：沿纹理u方向的表面方向，常作为网格顶点属性。
4. **副切线**：由法线与切线叉积并结合手性得到的v方向。
5. **TBN矩阵**：在切线空间与模型或世界空间间变换方向的基矩阵。

## 数据流与公式

法线纹理采样值需从零到一解码到负一到一。网格切线、法线和手性构成TBN；可把法线变到世界空间，也可把光线和视线变到切线空间。水面通过滚动和混合多张法线图形成动态细节。 前15章以OpenGL 4.1、GLSL和openFrameworks为实验环境；第16至18章再映射到Unity、UE4与Godot。所有代码都要把可编程Shader与应用侧资源绑定、Draw状态、深度和混合分开记录。

$$
n_t=2T-1
$$

$$
b=handedness(n\times t)
$$

$$
TBN=[t,b,n]
$$

$$
n_w=normalize(TBNn_t)
$$

公式中的位置、方向、颜色、深度和时间必须标注空间、范围与单位。片元输出只是候选颜色，最终结果还经过裁剪、深度、模板、混合与颜色编码。

## 最小实现

```glsl
vec3 nt=normalize(texture(normalMap,uv).xyz*2.0-1.0);
vec3 nw=normalize(mat3(T,B,N)*nt);
```

实现顺序固定为常量输出、输入可视化、核心公式、应用状态和优化。先验证Shader编译与链接，再检查属性位置、uniform、纹理单元和Framebuffer；只有画面与状态都正确后才比较性能。

<PsdNormalMappingCompareLab />

## 应用与引擎侧契约

```cpp
shader.begin();
shader.setUniform1f("time", elapsedSeconds);
shader.setUniformTexture("mainTex", texture, 0);
mesh.draw();
shader.end();
```

应用侧保存网格布局、索引、纹理格式、采样器、矩阵顺序、混合与深度状态。引擎章节则把同一概念映射到Material、节点图或ShaderMaterial；语法可以变化，输入空间与合成语义不能靠猜。

## 边界、精度与性能

测试零向量、退化UV、负缩放、近远裁剪面、透明交叉、纹理边缘、大世界坐标、长时间运行和高过度绘制。性能实验固定硬件、分辨率、VSync与场景，先判断CPU还是GPU受限，再用捕获定位具体Draw和Shader阶段。

```text
renderer: OpenGL 4.1 / named engine and version
scene: fixed mesh, camera, lights, textures
state: depth, cull, blend, target
evidence: image diff, frame capture, CPU/GPU ms
counterexample: smallest reproducible failure
```

<PsdNormalMappingEvidenceLab />

## 分步实验

<Stepper>
  <Step title="固定输入和状态"><PsdNormalMappingPipelineLab /></Step>
  <Step title="逐阶段实现与比较"><PsdNormalMappingCompareLab /></Step>
  <Step title="捕获画面和性能"><PsdNormalMappingEvidenceLab /></Step>
</Stepper>

1. 用最小网格与常量颜色验证Shader编译、链接和Draw状态。
2. 可视化位置、UV、法线、深度或光照项，确认范围和空间。
3. 打开本章核心效果并保存基线与候选画面。
4. 构造边界与失败输入，用GPU捕获和计时解释差异。

## 验收证书

证书包含源码与编译版本、应用绑定、资源哈希、状态、参考图、GPU事件、CPU与GPU帧时间和失败反例。网格UV镜像却忽略切线手性，接缝两侧法线方向翻转。 修复后必须重放同一输入并说明差异来自哪个阶段。

## 常见误区

<Callout type="trap" title="把语法当原理">网格UV镜像却忽略切线手性，接缝两侧法线方向翻转。</Callout>

<Callout type="trap" title="只看最终画面">画面相似不证明空间、深度、混合和性能等价。必须保存中间调试视图、固定状态和GPU捕获。</Callout>

## 本章回顾

本页覆盖法线贴图、切线空间、切线向量、副切线、TBN矩阵。掌握标准是能画出数据流，写出最小实现，说明应用或引擎状态，再用边界、精度与性能证据复现结论。

<Exercises>
**问题1：法线贴图与切线空间如何连接？**
<Answer>在纹理中编码表面微小方向变化以改变光照而不增加几何的技术。 由切线、副切线和法线构成并随表面UV变化的局部坐标系。 回答时标注阶段和输入来源。</Answer>

**问题2：怎样调试切线向量？**
<Answer>先输出常量，再把相关中间值映射为颜色；随后核对应用绑定、固定状态和GPU事件。</Answer>

**问题3：如何验收TBN矩阵？**
<Answer>在切线空间与模型或世界空间间变换方向的基矩阵。 同时保存正常例、边界例、失败反例和CPU/GPU计时。</Answer>
</Exercises>

## 术语表

<Glossary>
  <GlossaryItem term="法线贴图" definition="在纹理中编码表面微小方向变化以改变光照而不增加几何的技术。" />
  <GlossaryItem term="切线空间" definition="由切线、副切线和法线构成并随表面UV变化的局部坐标系。" />
  <GlossaryItem term="切线向量" definition="沿纹理u方向的表面方向，常作为网格顶点属性。" />
  <GlossaryItem term="副切线" definition="由法线与切线叉积并结合手性得到的v方向。" />
  <GlossaryItem term="TBN矩阵" definition="在切线空间与模型或世界空间间变换方向的基矩阵。" />
  <GlossaryItem term="可复现实验" definition="固定源码、应用状态、资源、硬件和捕获后可再次得到同一结论的实验。" />
</Glossary>

<Attribution adaptedFrom="Kyle Halladay · Practical Shader Development · 第10章 法线贴图" adaptedUrl="https://link.springer.com/book/10.1007/978-1-4842-4457-9" />
