---
title: "第15章 精度"
type: C
section: "Shader开发实战 · 性能与精度"
order: 15
description: "沿Practical Shader Development正式目录掌握浮点精度、有效位、灾难性消减、低精度变量、精度预算。"
demo: true
math: true
sourceUrl: "https://link.springer.com/book/10.1007/978-1-4842-4457-9"
draft: false
---


## 从画面开始

先预测顶点、片元、深度和帧时间会如何变化，再只调整一个参数观察变化。时间不断增大后相邻浮点值间距变粗，微小动画增量会消失；用局部时间或周期取模可缓解。点光源中大世界坐标相减也会丢精度，应转到相机或对象局部空间。低精度只有在范围与误差测试通过后才使用。 本页用、、、、建立对象、阶段、状态和证据之间的联系。


## 第一版权威目录定位

本页一一对应Kyle Halladay《Practical Shader Development: Vertex and Fragment Shaders for Game Developers》第一版第15章 精度。Springer/Apress正式目录确认2019年第一版、18章、附录代码、ISBN 978-1-4842-4456-2和XXI+381页；清华大学出版社官方样章确认2021中文第一版《Shader开发实战》、ISBN 9787302583349及对应中文目录。

### 原书小节覆盖

1. 15.1 什么是浮点精度
2. 15.2 时间轴动画案例
3. 15.3 使用低精度变量
4. 15.4 点光源问题
5. 15.5 小结

### 核心对象

1. **浮点精度**：由符号、指数和尾数组成、相对间距随数量级变化的表示能力。
2. **有效位**：决定相邻可表示数之间相对精细程度的尾数位。
3. **灾难性消减**：两个接近大数相减导致有效位大量丢失的现象。
4. **低精度变量**：以更少位数换取寄存器、带宽或吞吐收益的类型。
5. **精度预算**：按变量范围、误差传播和平台能力选择类型的证据。

## 数据流与公式

时间不断增大后相邻浮点值间距变粗，微小动画增量会消失；用局部时间或周期取模可缓解。点光源中大世界坐标相减也会丢精度，应转到相机或对象局部空间。低精度只有在范围与误差测试通过后才使用。 前15章以OpenGL 4.1、GLSL和openFrameworks为实验环境；第16至18章再映射到Unity、UE4与Godot。所有代码都要把可编程Shader与应用侧资源绑定、Draw状态、深度和混合分开记录。

$$
x=(-1)^s(1.m)2^e
$$

$$
ulp(x)\propto2^{e-p}
$$

$$
t_{local}=t\bmod P
$$

$$
error\le tolerance
$$

公式中的位置、方向、颜色、深度和时间必须标注空间、范围与单位。片元输出只是候选颜色，最终结果还经过裁剪、深度、模板、混合与颜色编码。

## 最小实现

```glsl
float localTime=mod(globalTime,period);
vec3 localLight=lightPos-objectOrigin;
vec3 localPoint=worldPos-objectOrigin;
```

实现顺序固定为常量输出、输入可视化、核心公式、应用状态和优化。先验证Shader编译与链接，再检查属性位置、uniform、纹理单元和Framebuffer；只有画面与状态都正确后才比较性能。


## 应用与引擎侧契约

```cpp
shader.begin();
shader.setUniform1f("time", elapsedSeconds);
shader.setUniformTexture("mainTex", texture, 0);
mesh.draw();
shader.end();
```

应用侧保存网格布局、索引、纹理格式、采样器、矩阵顺序、混合与深度状态。引擎章节则把同一概念映射到Material、节点图或ShaderMaterial；语法可以变化，输入空间与合成语义不能靠猜。

## 边界、精度与性能

测试零向量、退化UV、负缩放、近远裁剪面、透明交叉、纹理边缘、大世界坐标、长时间运行和高过度绘制。性能实验固定硬件、分辨率、VSync与场景，先判断CPU还是GPU受限，再用捕获定位具体Draw和Shader阶段。

```text
renderer: OpenGL 4.1 / named engine and version
scene: fixed mesh, camera, lights, textures
state: depth, cull, blend, target
evidence: image diff, frame capture, CPU/GPU ms
counterexample: smallest reproducible failure
```

1. 用最小网格与常量颜色验证Shader编译、链接和Draw状态。
2. 可视化位置、UV、法线、深度或光照项，确认范围和空间。
3. 打开本章核心效果并保存基线与候选画面。
4. 构造边界与失败输入，用GPU捕获和计时解释差异。

## 验收证书

证书包含源码与编译版本、应用绑定、资源哈希、状态、参考图、GPU事件、CPU与GPU帧时间和失败反例。把全局累计时间直接传入低精度Shader，运行数小时后动画开始跳帧。 修复后必须重放同一输入并说明差异来自哪个阶段。

## 常见误区


## 本章回顾

本页覆盖浮点精度、有效位、灾难性消减、低精度变量、精度预算。掌握标准是能画出数据流，写出最小实现，说明应用或引擎状态，再用边界、精度与性能证据复现结论。


## 术语表


