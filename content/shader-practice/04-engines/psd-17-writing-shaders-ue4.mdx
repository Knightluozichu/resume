---
title: "第17章 在UE4中编写着色器"
type: C
section: "Shader开发实战 · 引擎落地"
order: 17
description: "沿Practical Shader Development正式目录掌握UE4材质、材质实例、节点图、着色模型、参数集合。"
demo: true
math: true
sourceUrl: "https://link.springer.com/book/10.1007/978-1-4842-4457-9"
draft: true
---

import { Objectives, Callout, Attribution, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step } from "@/components/mdx/mdx-components";
import { PsdWritingShadersUe4PipelineLab, PsdWritingShadersUe4CompareLab, PsdWritingShadersUe4EvidenceLab } from "@/components/mdx/shader-practice/diagrams/psd-17-writing-shaders-ue4";

<Objectives>
- 能解释UE4材质、材质实例和节点图在渲染数据流中的作用
- 能推导本页公式并用GLSL、应用状态或引擎材质实现最小实验
- 能复现参数集合，以参考图、GPU捕获、边界输入和反例验收
</Objectives>

## 从画面开始

先预测顶点、片元、深度和帧时间会如何变化，再只调整一个参数观察变化。UE4材质图仍对应顶点和片元数据流，只是引擎生成底层代码。边缘光可用Fresnel或一减N点V构造，Default Lit输入遵循PBR语义；透明Blend Mode与Shading Model共同影响生成变体。材质实例减少重复配置但静态开关仍会产生变体。 本页用<Term def="通过材质图生成引擎Shader并定义表面输入和渲染模式的资源。">UE4材质</Term>、<Term def="共享父材质编译结果并覆盖暴露参数的轻量资源。">材质实例</Term>、<Term def="以节点和连线表达数学、纹理与材质输出的数据流。">节点图</Term>、<Term def="决定引擎如何解释Base Color、Roughness等输入并计算光照的模式。">着色模型</Term>、<Term def="把运行时代码或全局值传入多个材质的UE4机制。">参数集合</Term>建立对象、阶段、状态和证据之间的联系。

<PsdWritingShadersUe4PipelineLab />

## 第一版权威目录定位

本页一一对应Kyle Halladay《Practical Shader Development: Vertex and Fragment Shaders for Game Developers》第一版第17章 在UE4中编写着色器。Springer/Apress正式目录确认2019年第一版、18章、附录代码、ISBN 978-1-4842-4456-2和XXI+381页；清华大学出版社官方样章确认2021中文第一版《Shader开发实战》、ISBN 9787302583349及对应中文目录。

### 原书小节覆盖

1. 17.1 Shader、材质和实例
2. 17.2 使物体变红
3. 17.3 Material Node Graph
4. 17.4 边缘光材质
5. 17.5 Default Lit输入
6. 17.6 顶点还是片元
7. 17.7 着色模型
8. 17.8 混合与纹理节点
9. 17.9 代码传数据

### 核心对象

1. **UE4材质**：通过材质图生成引擎Shader并定义表面输入和渲染模式的资源。
2. **材质实例**：共享父材质编译结果并覆盖暴露参数的轻量资源。
3. **节点图**：以节点和连线表达数学、纹理与材质输出的数据流。
4. **着色模型**：决定引擎如何解释Base Color、Roughness等输入并计算光照的模式。
5. **参数集合**：把运行时代码或全局值传入多个材质的UE4机制。

## 数据流与公式

UE4材质图仍对应顶点和片元数据流，只是引擎生成底层代码。边缘光可用Fresnel或一减N点V构造，Default Lit输入遵循PBR语义；透明Blend Mode与Shading Model共同影响生成变体。材质实例减少重复配置但静态开关仍会产生变体。 前15章以OpenGL 4.1、GLSL和openFrameworks为实验环境；第16至18章再映射到Unity、UE4与Godot。所有代码都要把可编程Shader与应用侧资源绑定、Draw状态、深度和混合分开记录。

$$
rim=(1-N\cdot V)^p
$$

$$
BaseColor\in[0,1]^3
$$

$$
Roughness\in[0,1]
$$

$$
variants=static\ switches
$$

公式中的位置、方向、颜色、深度和时间必须标注空间、范围与单位。片元输出只是候选颜色，最终结果还经过裁剪、深度、模板、混合与颜色编码。

## 最小实现

```glsl
// Material graph equivalent
Rim = pow(1 - saturate(dot(PixelNormalWS, CameraVectorWS)), Power);
EmissiveColor = Rim * RimColor;
```

实现顺序固定为常量输出、输入可视化、核心公式、应用状态和优化。先验证Shader编译与链接，再检查属性位置、uniform、纹理单元和Framebuffer；只有画面与状态都正确后才比较性能。

<PsdWritingShadersUe4CompareLab />

## 应用与引擎侧契约

```cpp
shader.begin();
shader.setUniform1f("time", elapsedSeconds);
shader.setUniformTexture("mainTex", texture, 0);
mesh.draw();
shader.end();
```

应用侧保存网格布局、索引、纹理格式、采样器、矩阵顺序、混合与深度状态。引擎章节则把同一概念映射到Material、节点图或ShaderMaterial；语法可以变化，输入空间与合成语义不能靠猜。

## 边界、精度与性能

测试零向量、退化UV、负缩放、近远裁剪面、透明交叉、纹理边缘、大世界坐标、长时间运行和高过度绘制。性能实验固定硬件、分辨率、VSync与场景，先判断CPU还是GPU受限，再用捕获定位具体Draw和Shader阶段。

```text
renderer: OpenGL 4.1 / named engine and version
scene: fixed mesh, camera, lights, textures
state: depth, cull, blend, target
evidence: image diff, frame capture, CPU/GPU ms
counterexample: smallest reproducible failure
```

<PsdWritingShadersUe4EvidenceLab />

## 分步实验

<Stepper>
  <Step title="固定输入和状态"><PsdWritingShadersUe4PipelineLab /></Step>
  <Step title="逐阶段实现与比较"><PsdWritingShadersUe4CompareLab /></Step>
  <Step title="捕获画面和性能"><PsdWritingShadersUe4EvidenceLab /></Step>
</Stepper>

1. 用最小网格与常量颜色验证Shader编译、链接和Draw状态。
2. 可视化位置、UV、法线、深度或光照项，确认范围和空间。
3. 打开本章核心效果并保存基线与候选画面。
4. 构造边界与失败输入，用GPU捕获和计时解释差异。

## 验收证书

证书包含源码与编译版本、应用绑定、资源哈希、状态、参考图、GPU事件、CPU与GPU帧时间和失败反例。把节点图当没有成本的可视化配置，忽略它生成的采样、分支和Shader变体。 修复后必须重放同一输入并说明差异来自哪个阶段。

## 常见误区

<Callout type="trap" title="把语法当原理">把节点图当没有成本的可视化配置，忽略它生成的采样、分支和Shader变体。</Callout>

<Callout type="trap" title="只看最终画面">画面相似不证明空间、深度、混合和性能等价。必须保存中间调试视图、固定状态和GPU捕获。</Callout>

## 本章回顾

本页覆盖UE4材质、材质实例、节点图、着色模型、参数集合。掌握标准是能画出数据流，写出最小实现，说明应用或引擎状态，再用边界、精度与性能证据复现结论。

<Exercises>
**问题1：UE4材质与材质实例如何连接？**
<Answer>通过材质图生成引擎Shader并定义表面输入和渲染模式的资源。 共享父材质编译结果并覆盖暴露参数的轻量资源。 回答时标注阶段和输入来源。</Answer>

**问题2：怎样调试节点图？**
<Answer>先输出常量，再把相关中间值映射为颜色；随后核对应用绑定、固定状态和GPU事件。</Answer>

**问题3：如何验收参数集合？**
<Answer>把运行时代码或全局值传入多个材质的UE4机制。 同时保存正常例、边界例、失败反例和CPU/GPU计时。</Answer>
</Exercises>

## 术语表

<Glossary>
  <GlossaryItem term="UE4材质" definition="通过材质图生成引擎Shader并定义表面输入和渲染模式的资源。" />
  <GlossaryItem term="材质实例" definition="共享父材质编译结果并覆盖暴露参数的轻量资源。" />
  <GlossaryItem term="节点图" definition="以节点和连线表达数学、纹理与材质输出的数据流。" />
  <GlossaryItem term="着色模型" definition="决定引擎如何解释Base Color、Roughness等输入并计算光照的模式。" />
  <GlossaryItem term="参数集合" definition="把运行时代码或全局值传入多个材质的UE4机制。" />
  <GlossaryItem term="可复现实验" definition="固定源码、应用状态、资源、硬件和捕获后可再次得到同一结论的实验。" />
</Glossary>

<Attribution adaptedFrom="Kyle Halladay · Practical Shader Development · 第17章 在UE4中编写着色器" adaptedUrl="https://link.springer.com/book/10.1007/978-1-4842-4457-9" />
