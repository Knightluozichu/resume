---
title: "《Shader开发实战》总复习"
type: C
section: "Shader开发实战 · 总复习"
order: 0
description: "沿Practical Shader Development正式目录掌握跨阶段证书、空间证书、画面证书、性能证书、跨引擎映射。"
demo: true
math: true
sourceUrl: "https://link.springer.com/book/10.1007/978-1-4842-4457-9"
draft: true
---

import { Objectives, Callout, Attribution, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step } from "@/components/mdx/mdx-components";

<Objectives>
- 能解释跨阶段证书、空间证书和画面证书在渲染数据流中的作用
- 能推导本页公式并用GLSL、应用状态或引擎材质实现最小实验
- 能复现跨引擎映射，以参考图、GPU捕获、边界输入和反例验收
</Objectives>

## 从画面开始

先预测顶点、片元、深度和帧时间会如何变化，再只调整一个参数观察变化。总复习用同一水面与光照场景贯穿全书：GLSL先完成纹理、透明、运动和摄像机，再加入漫反射、高光、法线贴图、cubemap与多光源；随后捕获GPU帧、优化并验证精度；最后在三种引擎中复现同一数据流。 本页用<Term def="记录顶点输入、插值、片元输出和固定状态的数据流。">跨阶段证书</Term>、<Term def="标注每个位置和方向所处空间及变换矩阵。">空间证书</Term>、<Term def="固定资源、相机、灯光与参数得到的参考输出。">画面证书</Term>、<Term def="保存CPU与GPU帧时间、捕获事件、瓶颈与优化对比。">性能证书</Term>、<Term def="把GLSL概念对应到Unity、UE4与Godot而不混淆语法和语义。">跨引擎映射</Term>建立对象、阶段、状态和证据之间的联系。

<PsdOfficialFinalReviewPipelineLab />

## 第一版权威目录定位

本页一一对应Kyle Halladay《Practical Shader Development: Vertex and Fragment Shaders for Game Developers》第一版《Shader开发实战》总复习。Springer/Apress正式目录确认2019年第一版、18章、附录代码、ISBN 978-1-4842-4456-2和XXI+381页；清华大学出版社官方样章确认2021中文第一版《Shader开发实战》、ISBN 9787302583349及对应中文目录。

### 原书小节覆盖

1. 第1至6章 基础效果
2. 第7至12章 三维光照
3. 第13至15章 性能与精度
4. 第16至18章 三引擎落地
5. 附录A 支持代码

### 核心对象

1. **跨阶段证书**：记录顶点输入、插值、片元输出和固定状态的数据流。
2. **空间证书**：标注每个位置和方向所处空间及变换矩阵。
3. **画面证书**：固定资源、相机、灯光与参数得到的参考输出。
4. **性能证书**：保存CPU与GPU帧时间、捕获事件、瓶颈与优化对比。
5. **跨引擎映射**：把GLSL概念对应到Unity、UE4与Godot而不混淆语法和语义。

## 数据流与公式

总复习用同一水面与光照场景贯穿全书：GLSL先完成纹理、透明、运动和摄像机，再加入漫反射、高光、法线贴图、cubemap与多光源；随后捕获GPU帧、优化并验证精度；最后在三种引擎中复现同一数据流。 前15章以OpenGL 4.1、GLSL和openFrameworks为实验环境；第16至18章再映射到Unity、UE4与Godot。所有代码都要把可编程Shader与应用侧资源绑定、Draw状态、深度和混合分开记录。

$$
C=Blend(Light(Texture(Transform(mesh))))
$$

$$
T=\max(T_{cpu},T_{gpu})
$$

$$
error=\|I-I_{ref}\|
$$

$$
pass=correct\land fast\land portable
$$

公式中的位置、方向、颜色、深度和时间必须标注空间、范围与单位。片元输出只是候选颜色，最终结果还经过裁剪、深度、模板、混合与颜色编码。

## 最小实现

```glsl
vec4 debugStage(int mode){
 if(mode==0) return vec4(uv,0,1);
 if(mode==1) return vec4(normal*0.5+0.5,1);
 return finalColor;
}
```

实现顺序固定为常量输出、输入可视化、核心公式、应用状态和优化。先验证Shader编译与链接，再检查属性位置、uniform、纹理单元和Framebuffer；只有画面与状态都正确后才比较性能。

<PsdOfficialFinalReviewCompareLab />

## 应用与引擎侧契约

```cpp
shader.begin();
shader.setUniform1f("time", elapsedSeconds);
shader.setUniformTexture("mainTex", texture, 0);
mesh.draw();
shader.end();
```

应用侧保存网格布局、索引、纹理格式、采样器、矩阵顺序、混合与深度状态。引擎章节则把同一概念映射到Material、节点图或ShaderMaterial；语法可以变化，输入空间与合成语义不能靠猜。

## 边界、精度与性能

测试零向量、退化UV、负缩放、近远裁剪面、透明交叉、纹理边缘、大世界坐标、长时间运行和高过度绘制。性能实验固定硬件、分辨率、VSync与场景，先判断CPU还是GPU受限，再用捕获定位具体Draw和Shader阶段。

```text
renderer: OpenGL 4.1 / named engine and version
scene: fixed mesh, camera, lights, textures
state: depth, cull, blend, target
evidence: image diff, frame capture, CPU/GPU ms
counterexample: smallest reproducible failure
```

1. 用最小网格与常量颜色验证Shader编译、链接和Draw状态。
2. 可视化位置、UV、法线、深度或光照项，确认范围和空间。
3. 打开本章核心效果并保存基线与候选画面。
4. 构造边界与失败输入，用GPU捕获和计时解释差异。

## 验收证书

证书包含源码与编译版本、应用绑定、资源哈希、状态、参考图、GPU事件、CPU与GPU帧时间和失败反例。三个引擎都出现类似画面就宣称等价，却没有核对色彩空间、光照模型、深度和混合状态。 修复后必须重放同一输入并说明差异来自哪个阶段。

## 常见误区

<Callout type="trap" title="把语法当原理">三个引擎都出现类似画面就宣称等价，却没有核对色彩空间、光照模型、深度和混合状态。</Callout>

<Callout type="trap" title="只看最终画面">画面相似不证明空间、深度、混合和性能等价。必须保存中间调试视图、固定状态和GPU捕获。</Callout>

## 本章回顾

本页覆盖跨阶段证书、空间证书、画面证书、性能证书、跨引擎映射。掌握标准是能画出数据流，写出最小实现，说明应用或引擎状态，再用边界、精度与性能证据复现结论。

<Exercises>
**问题1：跨阶段证书与空间证书如何连接？**
<Answer>记录顶点输入、插值、片元输出和固定状态的数据流。 标注每个位置和方向所处空间及变换矩阵。 回答时标注阶段和输入来源。</Answer>

**问题2：怎样调试画面证书？**
<Answer>先输出常量，再把相关中间值映射为颜色；随后核对应用绑定、固定状态和GPU事件。</Answer>

**问题3：如何验收跨引擎映射？**
<Answer>把GLSL概念对应到Unity、UE4与Godot而不混淆语法和语义。 同时保存正常例、边界例、失败反例和CPU/GPU计时。</Answer>
</Exercises>

## 术语表

<Glossary>
  <GlossaryItem term="跨阶段证书" definition="记录顶点输入、插值、片元输出和固定状态的数据流。" />
  <GlossaryItem term="空间证书" definition="标注每个位置和方向所处空间及变换矩阵。" />
  <GlossaryItem term="画面证书" definition="固定资源、相机、灯光与参数得到的参考输出。" />
  <GlossaryItem term="性能证书" definition="保存CPU与GPU帧时间、捕获事件、瓶颈与优化对比。" />
  <GlossaryItem term="跨引擎映射" definition="把GLSL概念对应到Unity、UE4与Godot而不混淆语法和语义。" />
  <GlossaryItem term="可复现实验" definition="固定源码、应用状态、资源、硬件和捕获后可再次得到同一结论的实验。" />
</Glossary>

<Attribution adaptedFrom="Kyle Halladay · Practical Shader Development · 《Shader开发实战》总复习" adaptedUrl="https://link.springer.com/book/10.1007/978-1-4842-4457-9" />
