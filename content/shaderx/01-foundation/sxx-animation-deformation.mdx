---
title: "动画、蒙皮与动态形变：14篇复现"
type: B
section: "ShaderX 1-7 · 基础与几何"
order: 3
description: "还原布料、蒙皮、morph、皱纹、粒子和损伤形变的状态与时间一致性。"
demo: true
math: true
sourceUrl: "https://www.realtimerendering.com/resources/shaderx/"
draft: false
---


## 先预测，再建立证据

先预测：如果只把ShaderX中的DirectX 8/9、早期GLSL或固定功能接口替换成现代API，哪些数据顺序、精度、过滤和同步假设会改变？动态形变不是只改位置；法线、切线、速度、碰撞和历史数据必须沿同一状态链更新。 本页用、、、和建立统一验收语言。

## 官方范围与唯一归属

来源：[ShaderX 1](https://www.realtimerendering.com/resources/shaderx/Direct3D.ShaderX.Vertex.and.Pixel.Shader.Tips.and.Tricks_Wolfgang.F.Engel_Wordware.Pub_2002.pdf) · [ShaderX 2 · Tips & Tricks](https://www.realtimerendering.com/resources/shaderx/Tips_and_Tricks_with_DirectX_9.pdf) · [ShaderX 3](http://shaderx3.com/Tables%20of%20Content.htm) · [ShaderX 4](http://shaderx4.com/TOC.html) · [ShaderX 5](http://shaderx5.com/TOC.html)。以下原篇只在本主题拥有一个主归属；其他主题可以引用，但不得重复计入329篇覆盖。

### ShaderX 1 · 3篇

- **SXX-SX1-05 · Character Animation with Direct3D Vertex Shaders**：沿位置、法线、切线、速度和上一帧状态追踪形变，验证退化姿态与时间一致性。
- **SXX-SX1-21 · Animated Grass with Pixel and Vertex Shaders**：围绕形变状态拆出输入、核心变换、输出、失败边界和现代GPU证据，避免只复述效果名称。
- **SXX-SX1-30 · Real-Time Simulation and Rendering of Particle Flows**：定义密度或粒子状态、积分步长、透射与合成顺序，再做步长收敛和重叠压力测试。

### ShaderX 2 · Tips & Tricks · 1篇

- **SXX-SX2T-05 · Cloth Animation with Pixel and Vertex Shader 3.0**：沿位置、法线、切线、速度和上一帧状态追踪形变，验证退化姿态与时间一致性。

### ShaderX 3 · 2篇

- **SXX-SX3-04 · Morphing between two different Objects**：沿位置、法线、切线、速度和上一帧状态追踪形变，验证退化姿态与时间一致性。
- **SXX-SX3-18 · Animation and display of water**：沿位置、法线、切线、速度和上一帧状态追踪形变，验证退化姿态与时间一致性。

### ShaderX 4 · 5篇

- **SXX-SX4-02 · Practical Cloth Simulation for Modern GPU**：沿位置、法线、切线、速度和上一帧状态追踪形变，验证退化姿态与时间一致性。
- **SXX-SX4-04 · Morph Target Animation**：沿位置、法线、切线、速度和上一帧状态追踪形变，验证退化姿态与时间一致性。
- **SXX-SX4-05 · Real-Time Character Animation on the GPU**：沿位置、法线、切线、速度和上一帧状态追踪形变，验证退化姿态与时间一致性。
- **SXX-SX4-06 · Skinning with AniTextures**：沿位置、法线、切线、速度和上一帧状态追踪形变，验证退化姿态与时间一致性。
- **SXX-SX4-41 · True-to-life Real-time Animation of Shallow Water on Today s GPUs**：沿位置、法线、切线、速度和上一帧状态追踪形变，验证退化姿态与时间一致性。

### ShaderX 5 · 3篇

- **SXX-SX5-03 · Dynamic Wrinkle Patterns and Hatching on Animated Meshes**：沿位置、法线、切线、速度和上一帧状态追踪形变，验证退化姿态与时间一致性。
- **SXX-SX5-04 · Cloth without Cloth**：沿位置、法线、切线、速度和上一帧状态追踪形变，验证退化姿态与时间一致性。
- **SXX-SX5-26 · Animation and Rendering of Underwater God Rays**：沿位置、法线、切线、速度和上一帧状态追踪形变，验证退化姿态与时间一致性。

## 核心模型与不变量

动态形变不是只改位置；法线、切线、速度、碰撞和历史数据必须沿同一状态链更新。 复现时先写输入、输出、坐标或数据域、迭代顺序、误差判据和资源生命周期，再讨论接口迁移。任何优化都要指出它保持、近似或放弃了哪条关系。

$$
output = stage(input, constants, resources)
$$

$$
error = norm(reference - candidate)
$$

$$
cost = ALU + samples + bandwidth + synchronization
$$

$$
accept = correctness * compatibility * performance
$$

公式里的量必须给出范围、单位、空间、精度和生产者。画面相似不代表算法等价；数值接近也不代表带宽、时序和设备边界相同。

## 最小现代探针

```hlsl
float4 ProbeStage(float4 inputValue, float4 constantsValue) {
    float4 candidate = EvaluateInvariant(inputValue, constantsValue);
    return float4(candidate.xyz, 1.0);
}
```

探针一次只暴露一个阶段。先使用常量、解析输入和小规模CPU参考确认关系，再逐步恢复纹理、变体、并行和历史数据。

## 历史载体到现代路径

```text
original article -> assumptions -> invariant -> reference
reference -> modern shader/compute path -> intermediate evidence
evidence -> image or numeric diff -> target-device acceptance
```

旧shader model、寄存器语义、effect框架、render-to-texture和多pass接口可以变化，但读写依赖、采样边界、精度和同步不能被省略。迁移报告要明确标记完全等价、受控近似和不可直接迁移。

## 性能、兼容与失败边界

固定设备、驱动、分辨率、场景、帧阶段和统计窗口，分别报告CPU提交、GPU时间、外存流量、显存峰值与画质误差。历史技巧在现代GPU上可能被编译器、缓存或专用硬件替代，必须以捕获而不是名称判断。

```json
{"source":"volume + article id","correctness":"reference diff","compatibility":"api and device matrix","performance":"end-to-end metrics","failure":"minimal replay"}
```

1. 固定卷号、篇名、来源、原始平台和输入输出。
2. 写出核心不变量、CPU或离线参考以及现代GPU路径。
3. 构造正常、边界、退化和超容量输入，保存中间证据。
4. 在目标设备重放，记录图像或数值误差和端到端成本。

## 常见误区


## 练习


## 本章回顾

动画、蒙皮与动态形变页承载14篇唯一原文；每篇都回到问题、不变量、现代实现、边界和证据，而不是只保留一个效果名称。


