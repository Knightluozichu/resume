---
title: "几何数据、拓扑与细分：21篇复现"
type: B
section: "ShaderX 1-7 · 基础与几何"
order: 2
description: "覆盖压缩、拓扑、细分、实例、LOD和批处理的生产者到消费者链。"
demo: true
math: true
sourceUrl: "https://www.realtimerendering.com/resources/shaderx/"
draft: false
---

import { Objectives, Callout, Attribution, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step } from "@/components/mdx/mdx-components";
import { SxxGeometryDataMapLab, SxxGeometryDataExperimentLab, SxxGeometryDataEvidenceLab } from "@/components/mdx/shaderx/diagrams/sxx-geometry-data";

<Objectives>
- 能解释几何表示、屏幕误差与共享边约束的生产者到消费者关系
- 能绘制原篇方案、历史GPU载体和现代实现之间的等价或近似边界
- 能判断几何证书是否包含正确性、兼容、性能和失败重放证据
</Objectives>

## 先预测，再建立证据

先预测：如果只把ShaderX中的DirectX 8/9、早期GLSL或固定功能接口替换成现代API，哪些数据顺序、精度、过滤和同步假设会改变？几何技巧必须同时保持拓扑合法性、投影误差和数据带宽，顶点数量只是其中一项。 本页用<Term def="顶点、索引、邻接、patch和程序生成规则的完整数据契约。">几何表示</Term>、<Term def="以投影后的可见误差驱动LOD和细分。">屏幕误差</Term>、<Term def="相邻patch对公共边使用一致的细分与位移边界。">共享边约束</Term>、<Term def="统计顶点读取、生成、回写和重复变换的外存成本。">带宽预算</Term>和<Term def="同时验证位置、法线、轮廓、接缝、运动向量和GPU成本。">几何证书</Term>建立统一验收语言。

<SxxGeometryDataMapLab />

## 官方范围与唯一归属

来源：[ShaderX 1](https://www.realtimerendering.com/resources/shaderx/Direct3D.ShaderX.Vertex.and.Pixel.Shader.Tips.and.Tricks_Wolfgang.F.Engel_Wordware.Pub_2002.pdf) · [ShaderX 2 · Tips & Tricks](https://www.realtimerendering.com/resources/shaderx/Tips_and_Tricks_with_DirectX_9.pdf) · [ShaderX 3](http://shaderx3.com/Tables%20of%20Content.htm) · [ShaderX 4](http://shaderx4.com/TOC.html) · [ShaderX 5](http://shaderx5.com/TOC.html) · [ShaderX 6](http://shaderx6.com/TOC.html) · [ShaderX 7](http://shaderx7.com/TOC.html)。以下原篇只在本主题拥有一个主归属；其他主题可以引用，但不得重复计入329篇覆盖。

### ShaderX 1 · 4篇

- **SXX-SX1-03 · Vertex Decompression in a Shader**：固定采样域、导数、LOD、格式和边界，分别测表示误差、缓存行为与外存流量。
- **SXX-SX1-06 · Lighting a Single-Surface Object**：标出能量来源、可见性、方向表示与近似缺失信息，用解析或离线参考检查漏光和能量。
- **SXX-SX1-08 · Compendium of Vertex Shader Tricks**：围绕几何表示拆出输入、核心变换、输出、失败边界和现代GPU证据，避免只复述效果名称。
- **SXX-SX1-09 · Perlin Noise and Returning Results from Shader Programs**：围绕几何表示拆出输入、核心变换、输出、失败边界和现代GPU证据，避免只复述效果名称。

### ShaderX 2 · Tips & Tricks · 4篇

- **SXX-SX2T-01 · Using Vertex Shaders for Geometry Compression**：固定采样域、导数、LOD、格式和边界，分别测表示误差、缓存行为与外存流量。
- **SXX-SX2T-02 · Using Lookup Tables in Vertex Shaders**：固定采样域、导数、LOD、格式和边界，分别测表示误差、缓存行为与外存流量。
- **SXX-SX2T-04 · 3D Planets on the GPU**：围绕几何表示拆出输入、核心变换、输出、失败边界和现代GPU证据，避免只复述效果名称。
- **SXX-SX2T-06 · Collision Shaders**：围绕几何表示拆出输入、核心变换、输出、失败边界和现代GPU证据，避免只复述效果名称。

### ShaderX 3 · 5篇

- **SXX-SX3-01 · Accessing and modifying topology on the GPU**：围绕几何表示拆出输入、核心变换、输出、失败边界和现代GPU证据，避免只复述效果名称。
- **SXX-SX3-02 · Rendering of Complex Formulas**：围绕几何表示拆出输入、核心变换、输出、失败边界和现代GPU证据，避免只复述效果名称。
- **SXX-SX3-03 · Deforming of Mesh Objects using HLSL**：沿位置、法线、切线、速度和上一帧状态追踪形变，验证退化姿态与时间一致性。
- **SXX-SX3-05 · Silhouette Geometry Shaders**：围绕几何表示拆出输入、核心变换、输出、失败边界和现代GPU证据，避免只复述效果名称。
- **SXX-SX3-06 · GLSL Real-time Shader Development**：围绕几何表示拆出输入、核心变换、输出、失败边界和现代GPU证据，避免只复述效果名称。

### ShaderX 4 · 2篇

- **SXX-SX4-01 · Better geometry batching using light buffers**：围绕几何表示拆出输入、核心变换、输出、失败边界和现代GPU证据，避免只复述效果名称。
- **SXX-SX4-03 · Shader Implementation of Discrete Wavelet Transform**：围绕几何表示拆出输入、核心变换、输出、失败边界和现代GPU证据，避免只复述效果名称。

### ShaderX 5 · 2篇

- **SXX-SX5-01 · Smoothed N-Patches**：围绕几何表示拆出输入、核心变换、输出、失败边界和现代GPU证据，避免只复述效果名称。
- **SXX-SX5-02 · Micro-beveled Edges**：围绕几何表示拆出输入、核心变换、输出、失败边界和现代GPU证据，避免只复述效果名称。

### ShaderX 6 · 3篇

- **SXX-SX6-01 · Fast Evaluation of Subdivision Surfaces on Direct3D 10 Graphics Hardware**：围绕几何表示拆出输入、核心变换、输出、失败边界和现代GPU证据，避免只复述效果名称。
- **SXX-SX6-02 · Improved Appearance Variety for Geometry Instancing**：围绕几何表示拆出输入、核心变换、输出、失败边界和现代GPU证据，避免只复述效果名称。
- **SXX-SX6-03 · Implementing Real-Time Mesh Simplification Using Programmable Geometry Pipeline on GPU**：围绕几何表示拆出输入、核心变换、输出、失败边界和现代GPU证据，避免只复述效果名称。

### ShaderX 7 · 1篇

- **SXX-SX7-02 · Fast Tessellation of Quadrilateral Patches for Dynamic Level of Details**：写出几何或高度输入、屏幕误差、共享边和轮廓限制，并比较真实几何与采样近似。

## 核心模型与不变量

几何技巧必须同时保持拓扑合法性、投影误差和数据带宽，顶点数量只是其中一项。 复现时先写输入、输出、坐标或数据域、迭代顺序、误差判据和资源生命周期，再讨论接口迁移。任何优化都要指出它保持、近似或放弃了哪条关系。

$$
output = stage(input, constants, resources)
$$

$$
error = norm(reference - candidate)
$$

$$
cost = ALU + samples + bandwidth + synchronization
$$

$$
accept = correctness * compatibility * performance
$$

公式里的量必须给出范围、单位、空间、精度和生产者。画面相似不代表算法等价；数值接近也不代表带宽、时序和设备边界相同。

## 最小现代探针

```hlsl
float4 ProbeStage(float4 inputValue, float4 constantsValue) {
    float4 candidate = EvaluateInvariant(inputValue, constantsValue);
    return float4(candidate.xyz, 1.0);
}
```

探针一次只暴露一个阶段。先使用常量、解析输入和小规模CPU参考确认关系，再逐步恢复纹理、变体、并行和历史数据。

<SxxGeometryDataExperimentLab />

## 历史载体到现代路径

```text
original article -> assumptions -> invariant -> reference
reference -> modern shader/compute path -> intermediate evidence
evidence -> image or numeric diff -> target-device acceptance
```

旧shader model、寄存器语义、effect框架、render-to-texture和多pass接口可以变化，但读写依赖、采样边界、精度和同步不能被省略。迁移报告要明确标记完全等价、受控近似和不可直接迁移。

## 性能、兼容与失败边界

固定设备、驱动、分辨率、场景、帧阶段和统计窗口，分别报告CPU提交、GPU时间、外存流量、显存峰值与画质误差。历史技巧在现代GPU上可能被编译器、缓存或专用硬件替代，必须以捕获而不是名称判断。

```json
{"source":"volume + article id","correctness":"reference diff","compatibility":"api and device matrix","performance":"end-to-end metrics","failure":"minimal replay"}
```

<SxxGeometryDataEvidenceLab />

## 三步复现实验

<Stepper>
  <Step title="还原原篇问题与假设"><SxxGeometryDataMapLab /></Step>
  <Step title="建立参考与现代双路径"><SxxGeometryDataExperimentLab /></Step>
  <Step title="捕获边界、失败和成本"><SxxGeometryDataEvidenceLab /></Step>
</Stepper>

1. 固定卷号、篇名、来源、原始平台和输入输出。
2. 写出核心不变量、CPU或离线参考以及现代GPU路径。
3. 构造正常、边界、退化和超容量输入，保存中间证据。
4. 在目标设备重放，记录图像或数值误差和端到端成本。

## 常见误区

<Callout type="trap" title="范围或身份错误">只比较三角形数量，没有检查共享边、法线、阴影和运动向量是否一致。</Callout>

<Callout type="trap" title="证据或边界缺失">把实例或压缩当作零成本，遗漏解码ALU、缓存命中和最坏视角。</Callout>

## 练习

<Exercises>
**问题1：如何从几何表示建立最小可核查实验？**
<Answer>固定官方篇章身份，列出输入、输出和一条核心关系；先用解析或CPU参考验证单阶段，再加入GPU并保存中间缓冲和误差。</Answer>

**问题2：怎样证明现代实现没有偷换原篇问题？**
<Answer>对相同输入比较历史模型与现代路径的数据顺序、精度、边界和中间量；差异必须标记为等价、近似或不支持，并给出容差。</Answer>

**问题3：几何证书至少包含什么？**
<Answer>包含来源与版本、参考结果、实现配置、正确性差异、兼容矩阵、GPU捕获、端到端指标以及最小失败配置和修复重放。</Answer>
</Exercises>

## 本章回顾

几何数据、拓扑与细分页承载21篇唯一原文；每篇都回到问题、不变量、现代实现、边界和证据，而不是只保留一个效果名称。

<Glossary>
  <GlossaryItem term="几何表示" definition="顶点、索引、邻接、patch和程序生成规则的完整数据契约。" />
  <GlossaryItem term="屏幕误差" definition="以投影后的可见误差驱动LOD和细分。" />
  <GlossaryItem term="共享边约束" definition="相邻patch对公共边使用一致的细分与位移边界。" />
  <GlossaryItem term="带宽预算" definition="统计顶点读取、生成、回写和重复变换的外存成本。" />
  <GlossaryItem term="几何证书" definition="同时验证位置、法线、轮廓、接缝、运动向量和GPU成本。" />
</Glossary>

<Attribution adaptedFrom="ShaderX 1-7 editor-authorized PDFs and official volume tables of contents" adaptedUrl="https://www.realtimerendering.com/resources/shaderx/" />
