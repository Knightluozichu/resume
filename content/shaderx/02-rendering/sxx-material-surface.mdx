---
title: "材质、BRDF 与风格化表面：31篇复现"
type: B
section: "ShaderX 1-7 · 表面与光照"
order: 5
description: "覆盖BRDF、皮肤、头发、金属、车漆、bump和NPR，并标注经验模型的适用域。"
demo: true
math: true
sourceUrl: "https://www.realtimerendering.com/resources/shaderx/"
draft: true
---

import { Objectives, Callout, Attribution, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step } from "@/components/mdx/mdx-components";
import { SxxMaterialSurfaceMapLab, SxxMaterialSurfaceExperimentLab, SxxMaterialSurfaceEvidenceLab } from "@/components/mdx/shaderx/diagrams/sxx-material-surface";

<Objectives>
- 能解释反射模型、能量边界与法线约定的生产者到消费者关系
- 能绘制原篇方案、历史GPU载体和现代实现之间的等价或近似边界
- 能判断材质证书是否包含正确性、兼容、性能和失败重放证据
</Objectives>

## 先预测，再建立证据

先预测：如果只把ShaderX中的DirectX 8/9、早期GLSL或固定功能接口替换成现代API，哪些数据顺序、精度、过滤和同步假设会改变？材质技巧必须说明它在模拟什么反射现象、违反了哪些物理假设，以及为何视觉上仍可接受。 本页用<Term def="从入射方向、出射方向和材质参数到反射能量的映射。">反射模型</Term>、<Term def="反射、透射和吸收之和不能无解释地超过输入能量。">能量边界</Term>、<Term def="几何、顶点、纹理和着色法线所处空间及朝向约定。">法线约定</Term>、<Term def="固定灯光、视角、曝光和参考材质的可比较场景。">外观基线</Term>和<Term def="包含参数扫描、角度响应、能量、时间稳定和性能证据。">材质证书</Term>建立统一验收语言。

<SxxMaterialSurfaceMapLab />

## 官方范围与唯一归属

来源：[ShaderX 2 · Tips & Tricks](https://www.realtimerendering.com/resources/shaderx/Tips_and_Tricks_with_DirectX_9.pdf) · [ShaderX 3](http://shaderx3.com/Tables%20of%20Content.htm) · [ShaderX 4](http://shaderx4.com/TOC.html) · [ShaderX 5](http://shaderx5.com/TOC.html) · [ShaderX 6](http://shaderx6.com/TOC.html) · [ShaderX 7](http://shaderx7.com/TOC.html)。以下原篇只在本主题拥有一个主归属；其他主题可以引用，但不得重复计入329篇覆盖。

### ShaderX 2 · Tips & Tricks · 9篇

- **SXX-SX2T-12 · Turbulent Sun**：围绕反射模型拆出输入、核心变换、输出、失败边界和现代GPU证据，避免只复述效果名称。
- **SXX-SX2T-14 · Specular Bump Mapping on Pre-ps_1_4 Hardware**：围绕反射模型拆出输入、核心变换、输出、失败边界和现代GPU证据，避免只复述效果名称。
- **SXX-SX2T-15 · Voxel Rendering with PS_3_0**：围绕反射模型拆出输入、核心变换、输出、失败边界和现代GPU证据，避免只复述效果名称。
- **SXX-SX2T-16 · Simulating Blending Operations on Floating-point Render Targets**：围绕反射模型拆出输入、核心变换、输出、失败边界和现代GPU证据，避免只复述效果名称。
- **SXX-SX2T-24 · Deferred Shading with Multiple Render Targets**：围绕反射模型拆出输入、核心变换、输出、失败边界和现代GPU证据，避免只复述效果名称。
- **SXX-SX2T-25 · Meshuggah's Effects Explained**：围绕反射模型拆出输入、核心变换、输出、失败边界和现代GPU证据，避免只复述效果名称。
- **SXX-SX2T-26 · Layered Car Paint Shader**：围绕反射模型拆出输入、核心变换、输出、失败边界和现代GPU证据，避免只复述效果名称。
- **SXX-SX2T-28 · Simulation of Iridescence and Translucency on Thin Surfaces**：围绕反射模型拆出输入、核心变换、输出、失败边界和现代GPU证据，避免只复述效果名称。
- **SXX-SX2T-30 · Stereoscopic Rendering in Hardware Using Shaders**：围绕反射模型拆出输入、核心变换、输出、失败边界和现代GPU证据，避免只复述效果名称。

### ShaderX 3 · 5篇

- **SXX-SX3-09 · Reflections from Bumpy Surfaces**：围绕反射模型拆出输入、核心变换、输出、失败边界和现代GPU证据，避免只复述效果名称。
- **SXX-SX3-13 · A Simulation of Thermal Imaging**：围绕反射模型拆出输入、核心变换、输出、失败边界和现代GPU证据，避免只复述效果名称。
- **SXX-SX3-15 · Dot3 Cel Shading**：围绕反射模型拆出输入、核心变换、输出、失败边界和现代GPU证据，避免只复述效果名称。
- **SXX-SX3-16 · Hardware Accelerated Charcoal Rendering**：围绕反射模型拆出输入、核心变换、输出、失败边界和现代GPU证据，避免只复述效果名称。
- **SXX-SX3-20 · Hair Rendering and Shading**：围绕反射模型拆出输入、核心变换、输出、失败边界和现代GPU证据，避免只复述效果名称。

### ShaderX 4 · 3篇

- **SXX-SX4-08 · Triangle mesh tangent space calculation**：围绕反射模型拆出输入、核心变换、输出、失败边界和现代GPU证据，避免只复述效果名称。
- **SXX-SX4-13 · Bump My Shiny Metal**：围绕反射模型拆出输入、核心变换、输出、失败边界和现代GPU证据，避免只复述效果名称。
- **SXX-SX4-17 · Dynamic Glossy Environment Reflections Using Summed-Area Tables**：围绕反射模型拆出输入、核心变换、输出、失败边界和现代GPU证据，避免只复述效果名称。

### ShaderX 5 · 6篇

- **SXX-SX5-05 · A Simple Area Light Model for GPUs**：围绕反射模型拆出输入、核心变换、输出、失败边界和现代GPU证据，避免只复述效果名称。
- **SXX-SX5-08 · Pre-Processing of Complex, Static Scenes for Fast Real Time Graphics**：围绕反射模型拆出输入、核心变换、输出、失败边界和现代GPU证据，避免只复述效果名称。
- **SXX-SX5-09 · Overcoming Deferred Shading Drawbacks**：围绕反射模型拆出输入、核心变换、输出、失败边界和现代GPU证据，避免只复述效果名称。
- **SXX-SX5-10 · Normal Mapping without Pre-Computed Tangents**：围绕反射模型拆出输入、核心变换、输出、失败边界和现代GPU证据，避免只复述效果名称。
- **SXX-SX5-11 · Animating Vegetation Using GPU Programs**：围绕反射模型拆出输入、核心变换、输出、失败边界和现代GPU证据，避免只复述效果名称。
- **SXX-SX5-12 · ZT-Buffer Algorithm**：围绕反射模型拆出输入、核心变换、输出、失败边界和现代GPU证据，避免只复述效果名称。

### ShaderX 6 · 3篇

- **SXX-SX6-04 · Care and Feeding of Normal Vectors**：围绕反射模型拆出输入、核心变换、输出、失败边界和现代GPU证据，避免只复述效果名称。
- **SXX-SX6-06 · Filtered Tilemaps**：明确颜色域、像素足迹、滤波核与历史来源，用频率图和运动序列验证重建。
- **SXX-SX6-11 · Efficient and Practical TileTrees**：围绕反射模型拆出输入、核心变换、输出、失败边界和现代GPU证据，避免只复述效果名称。

### ShaderX 7 · 5篇

- **SXX-SX7-05 · Quick Noise for GPUs**：围绕反射模型拆出输入、核心变换、输出、失败边界和现代GPU证据，避免只复述效果名称。
- **SXX-SX7-08 · Fast Skin Shading**：沿位置、法线、切线、速度和上一帧状态追踪形变，验证退化姿态与时间一致性。
- **SXX-SX7-09 · An Efficient and Physically Plausible Real Time Shading Model**：围绕反射模型拆出输入、核心变换、输出、失败边界和现代GPU证据，避免只复述效果名称。
- **SXX-SX7-10 · Graphics Techniques in Crackdown**：围绕反射模型拆出输入、核心变换、输出、失败边界和现代GPU证据，避免只复述效果名称。
- **SXX-SX7-13 · Light Indexed Deferred Rendering**：围绕反射模型拆出输入、核心变换、输出、失败边界和现代GPU证据，避免只复述效果名称。

## 核心模型与不变量

材质技巧必须说明它在模拟什么反射现象、违反了哪些物理假设，以及为何视觉上仍可接受。 复现时先写输入、输出、坐标或数据域、迭代顺序、误差判据和资源生命周期，再讨论接口迁移。任何优化都要指出它保持、近似或放弃了哪条关系。

$$
output = stage(input, constants, resources)
$$

$$
error = norm(reference - candidate)
$$

$$
cost = ALU + samples + bandwidth + synchronization
$$

$$
accept = correctness * compatibility * performance
$$

公式里的量必须给出范围、单位、空间、精度和生产者。画面相似不代表算法等价；数值接近也不代表带宽、时序和设备边界相同。

## 最小现代探针

```hlsl
float4 ProbeStage(float4 inputValue, float4 constantsValue) {
    float4 candidate = EvaluateInvariant(inputValue, constantsValue);
    return float4(candidate.xyz, 1.0);
}
```

探针一次只暴露一个阶段。先使用常量、解析输入和小规模CPU参考确认关系，再逐步恢复纹理、变体、并行和历史数据。

<SxxMaterialSurfaceExperimentLab />

## 历史载体到现代路径

```text
original article -> assumptions -> invariant -> reference
reference -> modern shader/compute path -> intermediate evidence
evidence -> image or numeric diff -> target-device acceptance
```

旧shader model、寄存器语义、effect框架、render-to-texture和多pass接口可以变化，但读写依赖、采样边界、精度和同步不能被省略。迁移报告要明确标记完全等价、受控近似和不可直接迁移。

## 性能、兼容与失败边界

固定设备、驱动、分辨率、场景、帧阶段和统计窗口，分别报告CPU提交、GPU时间、外存流量、显存峰值与画质误差。历史技巧在现代GPU上可能被编译器、缓存或专用硬件替代，必须以捕获而不是名称判断。

```json
{"source":"volume + article id","correctness":"reference diff","compatibility":"api and device matrix","performance":"end-to-end metrics","failure":"minimal replay"}
```

<SxxMaterialSurfaceEvidenceLab />

## 三步复现实验

<Stepper>
  <Step title="还原原篇问题与假设"><SxxMaterialSurfaceMapLab /></Step>
  <Step title="建立参考与现代双路径"><SxxMaterialSurfaceExperimentLab /></Step>
  <Step title="捕获边界、失败和成本"><SxxMaterialSurfaceEvidenceLab /></Step>
</Stepper>

1. 固定卷号、篇名、来源、原始平台和输入输出。
2. 写出核心不变量、CPU或离线参考以及现代GPU路径。
3. 构造正常、边界、退化和超容量输入，保存中间证据。
4. 在目标设备重放，记录图像或数值误差和端到端成本。

## 常见误区

<Callout type="trap" title="范围或身份错误">只在一张宣传图上匹配颜色，没有扫描粗糙度、视角、光源大小和曝光。</Callout>

<Callout type="trap" title="证据或边界缺失">混用切线空间与世界空间法线，靠归一化掩盖坐标错误。</Callout>

## 练习

<Exercises>
**问题1：如何从反射模型建立最小可核查实验？**
<Answer>固定官方篇章身份，列出输入、输出和一条核心关系；先用解析或CPU参考验证单阶段，再加入GPU并保存中间缓冲和误差。</Answer>

**问题2：怎样证明现代实现没有偷换原篇问题？**
<Answer>对相同输入比较历史模型与现代路径的数据顺序、精度、边界和中间量；差异必须标记为等价、近似或不支持，并给出容差。</Answer>

**问题3：材质证书至少包含什么？**
<Answer>包含来源与版本、参考结果、实现配置、正确性差异、兼容矩阵、GPU捕获、端到端指标以及最小失败配置和修复重放。</Answer>
</Exercises>

## 本章回顾

材质、BRDF 与风格化表面页承载31篇唯一原文；每篇都回到问题、不变量、现代实现、边界和证据，而不是只保留一个效果名称。

<Glossary>
  <GlossaryItem term="反射模型" definition="从入射方向、出射方向和材质参数到反射能量的映射。" />
  <GlossaryItem term="能量边界" definition="反射、透射和吸收之和不能无解释地超过输入能量。" />
  <GlossaryItem term="法线约定" definition="几何、顶点、纹理和着色法线所处空间及朝向约定。" />
  <GlossaryItem term="外观基线" definition="固定灯光、视角、曝光和参考材质的可比较场景。" />
  <GlossaryItem term="材质证书" definition="包含参数扫描、角度响应、能量、时间稳定和性能证据。" />
</Glossary>

<Attribution adaptedFrom="ShaderX 1-7 editor-authorized PDFs and official volume tables of contents" adaptedUrl="https://www.realtimerendering.com/resources/shaderx/" />
