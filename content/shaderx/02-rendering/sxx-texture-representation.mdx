---
title: "纹理、立方体图与数据表示：13篇复现"
type: B
section: "ShaderX 1-7 · 表面与光照"
order: 7
description: "复现纹理混合、atlas、cube map、压缩、程序纹理和查找表的采样契约。"
demo: true
math: true
sourceUrl: "https://www.realtimerendering.com/resources/shaderx/"
draft: true
---

import { Objectives, Callout, Attribution, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step } from "@/components/mdx/mdx-components";
import { SxxTextureRepresentationMapLab, SxxTextureRepresentationExperimentLab, SxxTextureRepresentationEvidenceLab } from "@/components/mdx/shaderx/diagrams/sxx-texture-representation";

<Objectives>
- 能解释采样域、表示误差与边界连续的生产者到消费者关系
- 能绘制原篇方案、历史GPU载体和现代实现之间的等价或近似边界
- 能判断纹理证书是否包含正确性、兼容、性能和失败重放证据
</Objectives>

## 先预测，再建立证据

先预测：如果只把ShaderX中的DirectX 8/9、早期GLSL或固定功能接口替换成现代API，哪些数据顺序、精度、过滤和同步假设会改变？纹理技术的核心是采样域和数据表示，压缩率、分辨率或贴图数量都不能单独代表质量。 本页用<Term def="纹理坐标、导数、LOD、寻址和过滤共同定义的读取规则。">采样域</Term>、<Term def="压缩、量化或投影造成的数值与视觉差异。">表示误差</Term>、<Term def="atlas、tile、cube face和周期纹理边界的值与导数连续性。">边界连续</Term>、<Term def="纹理容量、mip、流送和采样带宽的联合成本。">驻留预算</Term>和<Term def="包含误差图、极值、导数、边界、缓存与设备格式支持。">纹理证书</Term>建立统一验收语言。

<SxxTextureRepresentationMapLab />

## 官方范围与唯一归属

来源：[ShaderX 1](https://www.realtimerendering.com/resources/shaderx/Direct3D.ShaderX.Vertex.and.Pixel.Shader.Tips.and.Tricks_Wolfgang.F.Engel_Wordware.Pub_2002.pdf) · [ShaderX 2 · Tips & Tricks](https://www.realtimerendering.com/resources/shaderx/Tips_and_Tricks_with_DirectX_9.pdf) · [ShaderX 3](http://shaderx3.com/Tables%20of%20Content.htm) · [ShaderX 4](http://shaderx4.com/TOC.html) · [ShaderX 6](http://shaderx6.com/TOC.html)。以下原篇只在本主题拥有一个主归属；其他主题可以引用，但不得重复计入329篇覆盖。

### ShaderX 1 · 3篇

- **SXX-SX1-16 · Diffuse Cube Mapping**：固定采样域、导数、LOD、格式和边界，分别测表示误差、缓存行为与外存流量。
- **SXX-SX1-22 · Texture Perturbation Effects**：固定采样域、导数、LOD、格式和边界，分别测表示误差、缓存行为与外存流量。
- **SXX-SX1-31 · 3D Textures and Pixel Shaders**：固定采样域、导数、LOD、格式和边界，分别测表示误差、缓存行为与外存流量。

### ShaderX 2 · Tips & Tricks · 4篇

- **SXX-SX2T-11 · Galaxy Textures**：固定采样域、导数、LOD、格式和边界，分别测表示误差、缓存行为与外存流量。
- **SXX-SX2T-18 · Normal Map Compression**：固定采样域、导数、LOD、格式和边界，分别测表示误差、缓存行为与外存流量。
- **SXX-SX2T-29 · Floating-point Cube Maps**：固定采样域、导数、LOD、格式和边界，分别测表示误差、缓存行为与外存流量。
- **SXX-SX2T-31 · Hatching, Stroke Styles, and Pointillism**：围绕采样域拆出输入、核心变换、输出、失败边界和现代GPU证据，避免只复述效果名称。

### ShaderX 3 · 2篇

- **SXX-SX3-12 · Improved Batching Via Texture Atlases**：固定采样域、导数、LOD、格式和边界，分别测表示误差、缓存行为与外存流量。
- **SXX-SX3-14 · Real-Time Texture-Space Skin Rendering**：沿位置、法线、切线、速度和上一帧状态追踪形变，验证退化姿态与时间一致性。

### ShaderX 4 · 2篇

- **SXX-SX4-19 · Dot-Product for efficient detail texture mapping**：固定采样域、导数、LOD、格式和边界，分别测表示误差、缓存行为与外存流量。
- **SXX-SX4-20 · Real-Time Environment Mapping with Equal Solid-Angle Spherical Quad-Map**：围绕采样域拆出输入、核心变换、输出、失败边界和现代GPU证据，避免只复述效果名称。

### ShaderX 6 · 2篇

- **SXX-SX6-08 · Uniform Cubemap for Dynamic Environments**：固定采样域、导数、LOD、格式和边界，分别测表示误差、缓存行为与外存流量。
- **SXX-SX6-09 · Isocube A Cubemap with Uniformly Distributed and Equally Important Texels**：固定采样域、导数、LOD、格式和边界，分别测表示误差、缓存行为与外存流量。

## 核心模型与不变量

纹理技术的核心是采样域和数据表示，压缩率、分辨率或贴图数量都不能单独代表质量。 复现时先写输入、输出、坐标或数据域、迭代顺序、误差判据和资源生命周期，再讨论接口迁移。任何优化都要指出它保持、近似或放弃了哪条关系。

$$
output = stage(input, constants, resources)
$$

$$
error = norm(reference - candidate)
$$

$$
cost = ALU + samples + bandwidth + synchronization
$$

$$
accept = correctness * compatibility * performance
$$

公式里的量必须给出范围、单位、空间、精度和生产者。画面相似不代表算法等价；数值接近也不代表带宽、时序和设备边界相同。

## 最小现代探针

```hlsl
float4 ProbeStage(float4 inputValue, float4 constantsValue) {
    float4 candidate = EvaluateInvariant(inputValue, constantsValue);
    return float4(candidate.xyz, 1.0);
}
```

探针一次只暴露一个阶段。先使用常量、解析输入和小规模CPU参考确认关系，再逐步恢复纹理、变体、并行和历史数据。

<SxxTextureRepresentationExperimentLab />

## 历史载体到现代路径

```text
original article -> assumptions -> invariant -> reference
reference -> modern shader/compute path -> intermediate evidence
evidence -> image or numeric diff -> target-device acceptance
```

旧shader model、寄存器语义、effect框架、render-to-texture和多pass接口可以变化，但读写依赖、采样边界、精度和同步不能被省略。迁移报告要明确标记完全等价、受控近似和不可直接迁移。

## 性能、兼容与失败边界

固定设备、驱动、分辨率、场景、帧阶段和统计窗口，分别报告CPU提交、GPU时间、外存流量、显存峰值与画质误差。历史技巧在现代GPU上可能被编译器、缓存或专用硬件替代，必须以捕获而不是名称判断。

```json
{"source":"volume + article id","correctness":"reference diff","compatibility":"api and device matrix","performance":"end-to-end metrics","failure":"minimal replay"}
```

<SxxTextureRepresentationEvidenceLab />

## 三步复现实验

<Stepper>
  <Step title="还原原篇问题与假设"><SxxTextureRepresentationMapLab /></Step>
  <Step title="建立参考与现代双路径"><SxxTextureRepresentationExperimentLab /></Step>
  <Step title="捕获边界、失败和成本"><SxxTextureRepresentationEvidenceLab /></Step>
</Stepper>

1. 固定卷号、篇名、来源、原始平台和输入输出。
2. 写出核心不变量、CPU或离线参考以及现代GPU路径。
3. 构造正常、边界、退化和超容量输入，保存中间证据。
4. 在目标设备重放，记录图像或数值误差和端到端成本。

## 常见误区

<Callout type="trap" title="范围或身份错误">只报平均PSNR，没有检查HDR峰值、法线方向、cube边界和时间闪烁。</Callout>

<Callout type="trap" title="证据或边界缺失">atlas减少了draw call，却因导数和mip污染产生远景接缝。</Callout>

## 练习

<Exercises>
**问题1：如何从采样域建立最小可核查实验？**
<Answer>固定官方篇章身份，列出输入、输出和一条核心关系；先用解析或CPU参考验证单阶段，再加入GPU并保存中间缓冲和误差。</Answer>

**问题2：怎样证明现代实现没有偷换原篇问题？**
<Answer>对相同输入比较历史模型与现代路径的数据顺序、精度、边界和中间量；差异必须标记为等价、近似或不支持，并给出容差。</Answer>

**问题3：纹理证书至少包含什么？**
<Answer>包含来源与版本、参考结果、实现配置、正确性差异、兼容矩阵、GPU捕获、端到端指标以及最小失败配置和修复重放。</Answer>
</Exercises>

## 本章回顾

纹理、立方体图与数据表示页承载13篇唯一原文；每篇都回到问题、不变量、现代实现、边界和证据，而不是只保留一个效果名称。

<Glossary>
  <GlossaryItem term="采样域" definition="纹理坐标、导数、LOD、寻址和过滤共同定义的读取规则。" />
  <GlossaryItem term="表示误差" definition="压缩、量化或投影造成的数值与视觉差异。" />
  <GlossaryItem term="边界连续" definition="atlas、tile、cube face和周期纹理边界的值与导数连续性。" />
  <GlossaryItem term="驻留预算" definition="纹理容量、mip、流送和采样带宽的联合成本。" />
  <GlossaryItem term="纹理证书" definition="包含误差图、极值、导数、边界、缓存与设备格式支持。" />
</Glossary>

<Attribution adaptedFrom="ShaderX 1-7 editor-authorized PDFs and official volume tables of contents" adaptedUrl="https://www.realtimerendering.com/resources/shaderx/" />
