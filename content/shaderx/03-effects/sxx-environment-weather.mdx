---
title: "天空、天气与自然环境：11篇复现"
type: B
section: "ShaderX 1-7 · 图像与环境"
order: 12
description: "复现天空、日光、彩虹、雨雪、冰、水面、god ray和动态天气。"
demo: true
math: true
sourceUrl: "https://www.realtimerendering.com/resources/shaderx/"
draft: false
---

import { Objectives, Callout, Attribution, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step } from "@/components/mdx/mdx-components";
import { SxxEnvironmentWeatherMapLab, SxxEnvironmentWeatherExperimentLab, SxxEnvironmentWeatherEvidenceLab } from "@/components/mdx/shaderx/diagrams/sxx-environment-weather";

<Objectives>
- 能解释环境尺度、光学深度与边界耦合的生产者到消费者关系
- 能绘制原篇方案、历史GPU载体和现代实现之间的等价或近似边界
- 能判断环境证书是否包含正确性、兼容、性能和失败重放证据
</Objectives>

## 先预测，再建立证据

先预测：如果只把ShaderX中的DirectX 8/9、早期GLSL或固定功能接口替换成现代API，哪些数据顺序、精度、过滤和同步假设会改变？环境效果跨越巨大尺度与多个介质，必须统一单位、光照、深度和状态转换，而不是叠加独立特效。 本页用<Term def="大气、地形、水体和天气所使用的空间单位与相机范围。">环境尺度</Term>、<Term def="沿路径积分密度与消光得到的无量纲衰减。">光学深度</Term>、<Term def="天空、地平线、水面、雾和地形之间的颜色与深度连续。">边界耦合</Term>、<Term def="风、降水、湿度、云量和时间驱动的连续状态。">天气状态</Term>和<Term def="覆盖昼夜、视高、天气转换、地平线、反射和性能。">环境证书</Term>建立统一验收语言。

<SxxEnvironmentWeatherMapLab />

## 官方范围与唯一归属

来源：[ShaderX 2 · Tips & Tricks](https://www.realtimerendering.com/resources/shaderx/Tips_and_Tricks_with_DirectX_9.pdf) · [ShaderX 3](http://shaderx3.com/Tables%20of%20Content.htm) · [ShaderX 4](http://shaderx4.com/TOC.html) · [ShaderX 6](http://shaderx6.com/TOC.html) · [ShaderX 7](http://shaderx7.com/TOC.html)。以下原篇只在本主题拥有一个主归属；其他主题可以引用，但不得重复计入329篇覆盖。

### ShaderX 2 · Tips & Tricks · 1篇

- **SXX-SX2T-23 · Advanced Sky Dome Rendering**：围绕环境尺度拆出输入、核心变换、输出、失败边界和现代GPU证据，避免只复述效果名称。

### ShaderX 3 · 3篇

- **SXX-SX3-44 · Light Shaft Rendering**：围绕环境尺度拆出输入、核心变换、输出、失败边界和现代GPU证据，避免只复述效果名称。
- **SXX-SX3-45 · Rendering Rainbows**：围绕环境尺度拆出输入、核心变换、输出、失败边界和现代GPU证据，避免只复述效果名称。
- **SXX-SX3-46 · A Practical Analytic Model for Daylight with Shaders**：围绕环境尺度拆出输入、核心变换、输出、失败边界和现代GPU证据，避免只复述效果名称。

### ShaderX 4 · 4篇

- **SXX-SX4-16 · Real-time Rendering and Simulation of the Airbrush Media for Metallic Appearance**：围绕环境尺度拆出输入、核心变换、输出、失败边界和现代GPU证据，避免只复述效果名称。
- **SXX-SX4-37 · Winter Wonderland**：围绕环境尺度拆出输入、核心变换、输出、失败边界和现代GPU证据，避免只复述效果名称。
- **SXX-SX4-38 · Rendering Snow Cover**：围绕环境尺度拆出输入、核心变换、输出、失败边界和现代GPU证据，避免只复述效果名称。
- **SXX-SX4-40 · A Dynamic Sky System**：围绕环境尺度拆出输入、核心变换、输出、失败边界和现代GPU证据，避免只复述效果名称。

### ShaderX 6 · 1篇

- **SXX-SX6-25 · Rendering Realistic Ice Objects**：围绕环境尺度拆出输入、核心变换、输出、失败边界和现代GPU证据，避免只复述效果名称。

### ShaderX 7 · 2篇

- **SXX-SX7-21 · Dynamic Weather Effects**：围绕环境尺度拆出输入、核心变换、输出、失败边界和现代GPU证据，避免只复述效果名称。
- **SXX-SX7-23 · Advanced Geometry for Complex Sky Representation**：围绕环境尺度拆出输入、核心变换、输出、失败边界和现代GPU证据，避免只复述效果名称。

## 核心模型与不变量

环境效果跨越巨大尺度与多个介质，必须统一单位、光照、深度和状态转换，而不是叠加独立特效。 复现时先写输入、输出、坐标或数据域、迭代顺序、误差判据和资源生命周期，再讨论接口迁移。任何优化都要指出它保持、近似或放弃了哪条关系。

$$
output = stage(input, constants, resources)
$$

$$
error = norm(reference - candidate)
$$

$$
cost = ALU + samples + bandwidth + synchronization
$$

$$
accept = correctness * compatibility * performance
$$

公式里的量必须给出范围、单位、空间、精度和生产者。画面相似不代表算法等价；数值接近也不代表带宽、时序和设备边界相同。

## 最小现代探针

```hlsl
float4 ProbeStage(float4 inputValue, float4 constantsValue) {
    float4 candidate = EvaluateInvariant(inputValue, constantsValue);
    return float4(candidate.xyz, 1.0);
}
```

探针一次只暴露一个阶段。先使用常量、解析输入和小规模CPU参考确认关系，再逐步恢复纹理、变体、并行和历史数据。

<SxxEnvironmentWeatherExperimentLab />

## 历史载体到现代路径

```text
original article -> assumptions -> invariant -> reference
reference -> modern shader/compute path -> intermediate evidence
evidence -> image or numeric diff -> target-device acceptance
```

旧shader model、寄存器语义、effect框架、render-to-texture和多pass接口可以变化，但读写依赖、采样边界、精度和同步不能被省略。迁移报告要明确标记完全等价、受控近似和不可直接迁移。

## 性能、兼容与失败边界

固定设备、驱动、分辨率、场景、帧阶段和统计窗口，分别报告CPU提交、GPU时间、外存流量、显存峰值与画质误差。历史技巧在现代GPU上可能被编译器、缓存或专用硬件替代，必须以捕获而不是名称判断。

```json
{"source":"volume + article id","correctness":"reference diff","compatibility":"api and device matrix","performance":"end-to-end metrics","failure":"minimal replay"}
```

<SxxEnvironmentWeatherEvidenceLab />

## 三步复现实验

<Stepper>
  <Step title="还原原篇问题与假设"><SxxEnvironmentWeatherMapLab /></Step>
  <Step title="建立参考与现代双路径"><SxxEnvironmentWeatherExperimentLab /></Step>
  <Step title="捕获边界、失败和成本"><SxxEnvironmentWeatherEvidenceLab /></Step>
</Stepper>

1. 固定卷号、篇名、来源、原始平台和输入输出。
2. 写出核心不变量、CPU或离线参考以及现代GPU路径。
3. 构造正常、边界、退化和超容量输入，保存中间证据。
4. 在目标设备重放，记录图像或数值误差和端到端成本。

## 常见误区

<Callout type="trap" title="范围或身份错误">天空和雾使用不同曝光或色域，地平线处出现无法消除的接缝。</Callout>

<Callout type="trap" title="证据或边界缺失">天气参数瞬时切换，没有验证时间连续、粒子重置和反射同步。</Callout>

## 练习

<Exercises>
**问题1：如何从环境尺度建立最小可核查实验？**
<Answer>固定官方篇章身份，列出输入、输出和一条核心关系；先用解析或CPU参考验证单阶段，再加入GPU并保存中间缓冲和误差。</Answer>

**问题2：怎样证明现代实现没有偷换原篇问题？**
<Answer>对相同输入比较历史模型与现代路径的数据顺序、精度、边界和中间量；差异必须标记为等价、近似或不支持，并给出容差。</Answer>

**问题3：环境证书至少包含什么？**
<Answer>包含来源与版本、参考结果、实现配置、正确性差异、兼容矩阵、GPU捕获、端到端指标以及最小失败配置和修复重放。</Answer>
</Exercises>

## 本章回顾

天空、天气与自然环境页承载11篇唯一原文；每篇都回到问题、不变量、现代实现、边界和证据，而不是只保留一个效果名称。

<Glossary>
  <GlossaryItem term="环境尺度" definition="大气、地形、水体和天气所使用的空间单位与相机范围。" />
  <GlossaryItem term="光学深度" definition="沿路径积分密度与消光得到的无量纲衰减。" />
  <GlossaryItem term="边界耦合" definition="天空、地平线、水面、雾和地形之间的颜色与深度连续。" />
  <GlossaryItem term="天气状态" definition="风、降水、湿度、云量和时间驱动的连续状态。" />
  <GlossaryItem term="环境证书" definition="覆盖昼夜、视高、天气转换、地平线、反射和性能。" />
</Glossary>

<Attribution adaptedFrom="ShaderX 1-7 editor-authorized PDFs and official volume tables of contents" adaptedUrl="https://www.realtimerendering.com/resources/shaderx/" />
