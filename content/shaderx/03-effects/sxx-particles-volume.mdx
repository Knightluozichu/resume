---
title: "粒子、体积、雾与流体：20篇复现"
type: B
section: "ShaderX 1-7 · 图像与环境"
order: 8
description: "覆盖粒子流、厚体积、雾、烟、云、水和流体的步进、积分与混合。"
demo: true
math: true
sourceUrl: "https://www.realtimerendering.com/resources/shaderx/"
draft: true
---

import { Objectives, Callout, Attribution, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step } from "@/components/mdx/mdx-components";

<Objectives>
- 能解释介质状态、步进积分与透射率的生产者到消费者关系
- 能绘制原篇方案、历史GPU载体和现代实现之间的等价或近似边界
- 能判断体积证书是否包含正确性、兼容、性能和失败重放证据
</Objectives>

## 先预测，再建立证据

先预测：如果只把ShaderX中的DirectX 8/9、早期GLSL或固定功能接口替换成现代API，哪些数据顺序、精度、过滤和同步假设会改变？体积和粒子效果必须区分密度表示、沿程积分、合成顺序与时间更新，不能只凭柔和外观判断。 本页用<Term def="密度、消光、散射、发射和速度场的时空定义。">介质状态</Term>、<Term def="沿射线或时间离散积分，并明确步长与误差。">步进积分</Term>、<Term def="光线穿过介质后剩余能量的乘性累积。">透射率</Term>、<Term def="半透明粒子和体积贡献的排序、近似与合成规则。">混合顺序</Term>和<Term def="以解析介质、步长收敛、时间稳定、过度绘制和带宽验收。">体积证书</Term>建立统一验收语言。

## 官方范围与唯一归属

来源：[ShaderX 1](https://www.realtimerendering.com/resources/shaderx/Direct3D.ShaderX.Vertex.and.Pixel.Shader.Tips.and.Tricks_Wolfgang.F.Engel_Wordware.Pub_2002.pdf) · [ShaderX 2 · Introductions & Tutorials](https://www.realtimerendering.com/resources/shaderx/Introductions_and_Tutorials_with_DirectX_9.pdf) · [ShaderX 2 · Tips & Tricks](https://www.realtimerendering.com/resources/shaderx/Tips_and_Tricks_with_DirectX_9.pdf) · [ShaderX 3](http://shaderx3.com/Tables%20of%20Content.htm) · [ShaderX 4](http://shaderx4.com/TOC.html) · [ShaderX 5](http://shaderx5.com/TOC.html) · [ShaderX 6](http://shaderx6.com/TOC.html) · [ShaderX 7](http://shaderx7.com/TOC.html)。以下原篇只在本主题拥有一个主归属；其他主题可以引用，但不得重复计入329篇覆盖。

### ShaderX 1 · 3篇

- **SXX-SX1-23 · Rendering Ocean Water**：定义密度或粒子状态、积分步长、透射与合成顺序，再做步长收敛和重叠压力测试。
- **SXX-SX1-24 · Rippling Reflective and Refractive Water**：定义密度或粒子状态、积分步长、透射与合成顺序，再做步长收敛和重叠压力测试。
- **SXX-SX1-32 · Truly Volumetric Effects**：定义密度或粒子状态、积分步长、透射与合成顺序，再做步长收敛和重叠压力测试。

### ShaderX 2 · Introductions & Tutorials · 1篇

- **SXX-SX2I-04 · Introduction to Different Fog Effects**：定义密度或粒子状态、积分步长、透射与合成顺序，再做步长收敛和重叠压力测试。

### ShaderX 2 · Tips & Tricks · 6篇

- **SXX-SX2T-08 · Rendering Objects as Thick Volumes**：定义密度或粒子状态、积分步长、透射与合成顺序，再做步长收敛和重叠压力测试。
- **SXX-SX2T-09 · Screen-aligned Particles with Minimal VertexBuffer Locking**：定义密度或粒子状态、积分步长、透射与合成顺序，再做步长收敛和重叠压力测试。
- **SXX-SX2T-17 · Rendering Volumes in a Vertex & Pixel Program by Ray Tracing**：定义密度或粒子状态、积分步长、透射与合成顺序，再做步长收敛和重叠压力测试。
- **SXX-SX2T-19 · Drops of Water and Texture Sprites**：固定采样域、导数、LOD、格式和边界，分别测表示误差、缓存行为与外存流量。
- **SXX-SX2T-20 · Advanced Water Effects**：定义密度或粒子状态、积分步长、透射与合成顺序，再做步长收敛和重叠压力测试。
- **SXX-SX2T-32 · Layered Fog**：定义密度或粒子状态、积分步长、透射与合成顺序，再做步长收敛和重叠压力测试。

### ShaderX 3 · 2篇

- **SXX-SX3-10 · Massively Parallel Particle Systems on the GPU**：定义密度或粒子状态、积分步长、透射与合成顺序，再做步长收敛和重叠压力测试。
- **SXX-SX3-47 · Volumetric Clouds**：定义密度或粒子状态、积分步长、透射与合成顺序，再做步长收敛和重叠压力测试。

### ShaderX 4 · 1篇

- **SXX-SX4-14 · Fog Volumes**：定义密度或粒子状态、积分步长、透射与合成顺序，再做步长收敛和重叠压力测试。

### ShaderX 5 · 3篇

- **SXX-SX5-22 · Spherical Billboards for Rendering Volumetric Data**：定义密度或粒子状态、积分步长、透射与合成顺序，再做步长收敛和重叠压力测试。
- **SXX-SX5-23 · Per-Pixel Lit, Light Scattering Smoke**：定义密度或粒子状态、积分步长、透射与合成顺序，再做步长收敛和重叠压力测试。
- **SXX-SX5-24 · Volumetric Clouds and Mega Particles**：定义密度或粒子状态、积分步长、透射与合成顺序，再做步长收敛和重叠压力测试。

### ShaderX 6 · 3篇

- **SXX-SX6-05 · Computing Per-Pixel Object Thickness in a Single Render Pass**：围绕介质状态拆出输入、核心变换、输出、失败边界和现代GPU证据，避免只复述效果名称。
- **SXX-SX6-26 · Sunlight with Volumetric Light Rays**：定义密度或粒子状态、积分步长、透射与合成顺序，再做步长收敛和重叠压力测试。
- **SXX-SX6-27 · Procedural Ocean Effects**：定义密度或粒子状态、积分步长、透射与合成顺序，再做步长收敛和重叠压力测试。

### ShaderX 7 · 1篇

- **SXX-SX7-06 · Efficient Soft Particles**：定义密度或粒子状态、积分步长、透射与合成顺序，再做步长收敛和重叠压力测试。

## 核心模型与不变量

体积和粒子效果必须区分密度表示、沿程积分、合成顺序与时间更新，不能只凭柔和外观判断。 复现时先写输入、输出、坐标或数据域、迭代顺序、误差判据和资源生命周期，再讨论接口迁移。任何优化都要指出它保持、近似或放弃了哪条关系。

$$
output = stage(input, constants, resources)
$$

$$
error = norm(reference - candidate)
$$

$$
cost = ALU + samples + bandwidth + synchronization
$$

$$
accept = correctness * compatibility * performance
$$

公式里的量必须给出范围、单位、空间、精度和生产者。画面相似不代表算法等价；数值接近也不代表带宽、时序和设备边界相同。

## 最小现代探针

```hlsl
float4 ProbeStage(float4 inputValue, float4 constantsValue) {
    float4 candidate = EvaluateInvariant(inputValue, constantsValue);
    return float4(candidate.xyz, 1.0);
}
```

探针一次只暴露一个阶段。先使用常量、解析输入和小规模CPU参考确认关系，再逐步恢复纹理、变体、并行和历史数据。

## 历史载体到现代路径

```text
original article -> assumptions -> invariant -> reference
reference -> modern shader/compute path -> intermediate evidence
evidence -> image or numeric diff -> target-device acceptance
```

旧shader model、寄存器语义、effect框架、render-to-texture和多pass接口可以变化，但读写依赖、采样边界、精度和同步不能被省略。迁移报告要明确标记完全等价、受控近似和不可直接迁移。

## 性能、兼容与失败边界

固定设备、驱动、分辨率、场景、帧阶段和统计窗口，分别报告CPU提交、GPU时间、外存流量、显存峰值与画质误差。历史技巧在现代GPU上可能被编译器、缓存或专用硬件替代，必须以捕获而不是名称判断。

```json
{"source":"volume + article id","correctness":"reference diff","compatibility":"api and device matrix","performance":"end-to-end metrics","failure":"minimal replay"}
```

1. 固定卷号、篇名、来源、原始平台和输入输出。
2. 写出核心不变量、CPU或离线参考以及现代GPU路径。
3. 构造正常、边界、退化和超容量输入，保存中间证据。
4. 在目标设备重放，记录图像或数值误差和端到端成本。

## 常见误区

<Callout type="trap" title="范围或身份错误">改变步数后亮度也改变，说明积分没有按步长缩放或透射率累积错误。</Callout>

<Callout type="trap" title="证据或边界缺失">只测空旷镜头，近景粒子重叠时过度绘制和排序成本失控。</Callout>

## 练习

<Exercises>
**问题1：如何从介质状态建立最小可核查实验？**
<Answer>固定官方篇章身份，列出输入、输出和一条核心关系；先用解析或CPU参考验证单阶段，再加入GPU并保存中间缓冲和误差。</Answer>

**问题2：怎样证明现代实现没有偷换原篇问题？**
<Answer>对相同输入比较历史模型与现代路径的数据顺序、精度、边界和中间量；差异必须标记为等价、近似或不支持，并给出容差。</Answer>

**问题3：体积证书至少包含什么？**
<Answer>包含来源与版本、参考结果、实现配置、正确性差异、兼容矩阵、GPU捕获、端到端指标以及最小失败配置和修复重放。</Answer>
</Exercises>

## 本章回顾

粒子、体积、雾与流体页承载20篇唯一原文；每篇都回到问题、不变量、现代实现、边界和证据，而不是只保留一个效果名称。

<Glossary>
  <GlossaryItem term="介质状态" definition="密度、消光、散射、发射和速度场的时空定义。" />
  <GlossaryItem term="步进积分" definition="沿射线或时间离散积分，并明确步长与误差。" />
  <GlossaryItem term="透射率" definition="光线穿过介质后剩余能量的乘性累积。" />
  <GlossaryItem term="混合顺序" definition="半透明粒子和体积贡献的排序、近似与合成规则。" />
  <GlossaryItem term="体积证书" definition="以解析介质、步长收敛、时间稳定、过度绘制和带宽验收。" />
</Glossary>

<Attribution adaptedFrom="ShaderX 1-7 editor-authorized PDFs and official volume tables of contents" adaptedUrl="https://www.realtimerendering.com/resources/shaderx/" />
