---
title: 第5章 保护Spring应用
description: 第5章 保护Spring应用覆盖14个正式目录节点，以章专属代码、边界交互、故障恢复和版本迁移证据验收。
type: C
section: Spring in Action（第6版 / Spring 5.3与Boot 2.4基线）
draft: false
math: true
demo: true
order: 0
sourceUrl: 'https://www.manning.com/books/spring-in-action-sixth-edition'
qualityVersion: 2
practiceMode: code
sourceMode: independent-rewrite
---


{/* SIA_QUALITY_V2 */}

## 为什么从“过滤链与授权决策台”开始

第5章 保护Spring应用不是starter、注解或API清单，而是一条从“credentials → authentication → authorization → controller”到业务结果的可执行合同。过滤链与授权决策台把框架决策和领域结果放在同一时间线，先预测第一处变化，再运行、反证、恢复和复位。

第5章 保护Spring应用的单变量实验固定提交、JDK、依赖、输入和外部服务，只改变调用身份或保护位置；面板中的方向性数字用于比较条件，不冒充真实压测、生产SLA或安全证明。

## 来源、版次与独立重写边界

第5章 保护Spring应用依据Manning[第6版产品页](https://www.manning.com/books/spring-in-action-sixth-edition)核定作者、2022年、520页和ISBN 9781617297571，并用[官方liveBook完整目录](https://livebook.manning.com/book/spring-in-action-sixth-edition/contents)定位4个Part、18章、附录及255个正式节点。第5章 保护Spring应用未取得出版正文授权，目录只限定范围，中文解释、代码、图示、实验、故障与答案均为独立教学重写。

第5章 保护Spring应用保留原书Spring Framework 5·3 / Boot 2·4历史语境；迁移复核以[Spring Framework 7·0·8参考](https://docs.spring.io/spring-framework/reference/index.html)、[Spring Boot 4·1·0系统要求](https://docs.spring.io/spring-boot/system-requirements.html)、[Spring Security](https://docs.spring.io/spring-security/reference/index.html)、[Spring Data](https://docs.spring.io/spring-data/commons/reference/index.html)、[Spring Integration](https://docs.spring.io/spring-integration/reference/index.html)与[Reactor](https://projectreactor.io/docs/core/release/reference/index.html)官方资料为事实依据。当前注记不冒充原书内容，也不允许把新API机械回填到旧基线。

## 本章合同、版本与代码

、、、、。

第5章 保护Spring应用的通过不变量是“默认拒绝，身份来源可追溯，请求与方法授权对同一资源保持一致”。第5章 保护Spring应用的实验档案至少保存commit、Java/Framework/Boot版本、配置来源、输入、关联ID、领域结果、外部副作用、终止原因和回滚触发器；版本迁移一次只改Java、Framework、Boot或第三方依赖中的一项。

本页从旧实现中保留并重新验收的章专属最小边界如下；第5章 保护Spring应用要求读者修改它、触发失败并用SecurityContext、决策事件与403断言证明结果，而不是只阅读代码：

```java
@Bean
SecurityFilterChain web(HttpSecurity http) throws Exception {
  return http.authorizeHttpRequests(auth -> auth
      .requestMatchers("/", "/login").permitAll()
      .anyRequest().authenticated()).formLogin(Customizer.withDefaults()).build();
}
```


## 官方目录逐项深读

### 5 Securing Spring

**四级证据 1/14。** 第5章 保护Spring应用中的5 Securing Spring：列出身份来源、信任边界、默认拒绝规则和401/403反例，再核对方法与对象级授权。在第5章 保护Spring应用中固定保护位置为“方法”，只把调用身份从“匿名”切到“越权用户”，用SecurityContext、决策事件与403断言找到第一项因果变化；该节点的练习断言是“默认拒绝，身份来源可追溯，请求与方法授权对同一资源保持一致”。

### 5.1 Enabling Spring Security

**四级证据 2/14。** 第5章 保护Spring应用中的5.1 Enabling Spring Security：列出身份来源、信任边界、默认拒绝规则和401/403反例，再核对方法与对象级授权。在第5章 保护Spring应用中固定保护位置为“方法”，只把调用身份从“匿名”切到“越权用户”，用SecurityContext、决策事件与403断言找到第一项因果变化；该节点的练习断言是“默认拒绝，身份来源可追溯，请求与方法授权对同一资源保持一致”。

### 5.2 Configuring authentication

**四级证据 3/14。** 第5章 保护Spring应用中的5.2 Configuring authentication：列出身份来源、信任边界、默认拒绝规则和401/403反例，再核对方法与对象级授权。在第5章 保护Spring应用中固定保护位置为“方法”，只把调用身份从“匿名”切到“越权用户”，用SecurityContext、决策事件与403断言找到第一项因果变化；该节点的练习断言是“默认拒绝，身份来源可追溯，请求与方法授权对同一资源保持一致”。

### 5.2.1 In-memory user details service

**四级证据 4/14。** 第5章 保护Spring应用中的5.2.1 In-memory user details service：把入口、容器决策、领域状态、外部副作用和验收信号连成可重放的最小切片。在第5章 保护Spring应用中固定保护位置为“方法”，只把调用身份从“匿名”切到“越权用户”，用SecurityContext、决策事件与403断言找到第一项因果变化；该节点的练习断言是“默认拒绝，身份来源可追溯，请求与方法授权对同一资源保持一致”。

### 5.2.2 Customizing user authentication

**四级证据 5/14。** 第5章 保护Spring应用中的5.2.2 Customizing user authentication：列出身份来源、信任边界、默认拒绝规则和401/403反例，再核对方法与对象级授权。在第5章 保护Spring应用中固定保护位置为“方法”，只把调用身份从“匿名”切到“越权用户”，用SecurityContext、决策事件与403断言找到第一项因果变化；该节点的练习断言是“默认拒绝，身份来源可追溯，请求与方法授权对同一资源保持一致”。

### 5.3 Securing web requests

**四级证据 6/14。** 第5章 保护Spring应用中的5.3 Securing web requests：列出身份来源、信任边界、默认拒绝规则和401/403反例，再核对方法与对象级授权。在第5章 保护Spring应用中固定保护位置为“方法”，只把调用身份从“匿名”切到“越权用户”，用SecurityContext、决策事件与403断言找到第一项因果变化；该节点的练习断言是“默认拒绝，身份来源可追溯，请求与方法授权对同一资源保持一致”。

### 5.3.1 Securing requests

**四级证据 7/14。** 第5章 保护Spring应用中的5.3.1 Securing requests：列出身份来源、信任边界、默认拒绝规则和401/403反例，再核对方法与对象级授权。在第5章 保护Spring应用中固定保护位置为“方法”，只把调用身份从“匿名”切到“越权用户”，用SecurityContext、决策事件与403断言找到第一项因果变化；该节点的练习断言是“默认拒绝，身份来源可追溯，请求与方法授权对同一资源保持一致”。

### 5.3.2 Creating a custom login page

**四级证据 8/14。** 第5章 保护Spring应用中的5.3.2 Creating a custom login page：把入口、容器决策、领域状态、外部副作用和验收信号连成可重放的最小切片。在第5章 保护Spring应用中固定保护位置为“方法”，只把调用身份从“匿名”切到“越权用户”，用SecurityContext、决策事件与403断言找到第一项因果变化；该节点的练习断言是“默认拒绝，身份来源可追溯，请求与方法授权对同一资源保持一致”。

### 5.3.3 Enabling third-party authentication

**四级证据 9/14。** 第5章 保护Spring应用中的5.3.3 Enabling third-party authentication：列出身份来源、信任边界、默认拒绝规则和401/403反例，再核对方法与对象级授权。在第5章 保护Spring应用中固定保护位置为“方法”，只把调用身份从“匿名”切到“越权用户”，用SecurityContext、决策事件与403断言找到第一项因果变化；该节点的练习断言是“默认拒绝，身份来源可追溯，请求与方法授权对同一资源保持一致”。

### 5.3.4 Logging out

**四级证据 10/14。** 第5章 保护Spring应用中的5.3.4 Logging out：把入口、容器决策、领域状态、外部副作用和验收信号连成可重放的最小切片。在第5章 保护Spring应用中固定保护位置为“方法”，只把调用身份从“匿名”切到“越权用户”，用SecurityContext、决策事件与403断言找到第一项因果变化；该节点的练习断言是“默认拒绝，身份来源可追溯，请求与方法授权对同一资源保持一致”。

### 5.3.5 Preventing cross-site request forgery

**四级证据 11/14。** 第5章 保护Spring应用中的5.3.5 Preventing cross-site request forgery：冻结HTTP输入，跟踪绑定、校验、领域状态和表示，区分成功、客户端错误、冲突和依赖失败。在第5章 保护Spring应用中固定保护位置为“方法”，只把调用身份从“匿名”切到“越权用户”，用SecurityContext、决策事件与403断言找到第一项因果变化；该节点的练习断言是“默认拒绝，身份来源可追溯，请求与方法授权对同一资源保持一致”。

### 5.4 Applying method-level security

**四级证据 12/14。** 第5章 保护Spring应用中的5.4 Applying method-level security：列出身份来源、信任边界、默认拒绝规则和401/403反例，再核对方法与对象级授权。在第5章 保护Spring应用中固定保护位置为“方法”，只把调用身份从“匿名”切到“越权用户”，用SecurityContext、决策事件与403断言找到第一项因果变化；该节点的练习断言是“默认拒绝，身份来源可追溯，请求与方法授权对同一资源保持一致”。

### 5.5 Knowing your user

**四级证据 13/14。** 第5章 保护Spring应用中的5.5 Knowing your user：把入口、容器决策、领域状态、外部副作用和验收信号连成可重放的最小切片。在第5章 保护Spring应用中固定保护位置为“方法”，只把调用身份从“匿名”切到“越权用户”，用SecurityContext、决策事件与403断言找到第一项因果变化；该节点的练习断言是“默认拒绝，身份来源可追溯，请求与方法授权对同一资源保持一致”。

### 5.6 Summary

**四级证据 14/14。** 第5章 保护Spring应用中的5.6 Summary：把入口、容器决策、领域状态、外部副作用和验收信号连成可重放的最小切片。在第5章 保护Spring应用中固定保护位置为“方法”，只把调用身份从“匿名”切到“越权用户”，用SecurityContext、决策事件与403断言找到第一项因果变化；该节点的练习断言是“默认拒绝，身份来源可追溯，请求与方法授权对同一资源保持一致”。

## 三个必须主动触发的误区


## 练习、答案与255节点验证


