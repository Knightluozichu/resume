---
title: "第10章 Spring Integration"
description: "依据Craig Walls《Spring in Action》第6版官方目录重构：用消息、通道与端点组合过滤、转换、路由、拆分、网关和适配器，显式表达企业集成模式"
type: C
section: "Spring in Action（第6版 / Spring 5.3与Boot 2.4基线）"
draft: false
math: true
demo: true
order: 0
sourceUrl: "https://www.manning.com/books/spring-in-action-sixth-edition"
---

import { Sia6ArchitectureLab, Sia6FlowLab, Sia6EvidenceLab } from "@/components/mdx/spring-in-action/diagrams/sia-6e-10-integrating-spring";

# 第10章 Spring Integration

本页对应官方目录中的 **10 Integrating Spring**。学习目标不是逐行复制原文，而是在Taco Cloud连续案例中一比一覆盖核心节点，并为每个设计留下可运行客户端、失败反例、可观察信号和回滚条件。核心任务是：用消息、通道与端点组合过滤、转换、路由、拆分、网关和适配器，显式表达企业集成模式。

<Callout type="info">**版本边界。** 原书由Manning于2022年1月出版，520页，ISBN 9781617297571，技术基线为Spring Framework 5.3与Spring Boot 2.4。课程先还原该语境，再以2026年7月的Spring Framework 7.0.7、Spring Boot 4.0.7复核迁移；现代注记不是原书新增章节。</Callout>

## 可验收学习目标

<Objectives>
- 能解释本单元全部17个正式目录节点在连续案例中的责任与前置关系。
- 能实现一个最小垂直切片，使输入、Spring边界、领域不变量和外部副作用均可观察。
- 能比较原书基线与当前版本，列出稳定合同、API迁移点和不可机械替换项。
- 能设计成功、边界、故障与回滚四条路径，并让另一位读者仅凭证据重放结论。
</Objectives>

预期交付是“集成流拓扑、消息模式、通道容量预算、端点合同测试和邮件故障回放”。最危险的误区是“DSL看似线性却隐藏异步通道、消息头丢失、无界拆分和错误通道无人消费”。

## 官方目录逐节点复刻

### 10 Integrating Spring

**正式节点 1/17。** 把“10 Integrating Spring”放回Part 2. Integrated Spring的连续案例，明确输入、Spring边界、领域不变量、外部副作用和验收证据。先预测一次条件缺失或依赖失败，再运行最小切片验证。

验收时将该节点绑定到“集成流拓扑、消息模式、通道容量预算、端点合同测试和邮件故障回放”，并写出一个能够推翻当前设计的条件。若只能复述API名称，不能从失败症状定位到边界，则返回本节点补做实验。

### 10.1 Declaring a simple integration flow

**正式节点 2/17。** 为“10.1 Declaring a simple integration flow”标出生产者、代理、消费者、确认、重试和死信责任。使用重复、乱序、超时和不可反序列化消息证明交付语义，而不是只看发送API返回。

验收时将该节点绑定到“集成流拓扑、消息模式、通道容量预算、端点合同测试和邮件故障回放”，并写出一个能够推翻当前设计的条件。若只能复述API名称，不能从失败症状定位到边界，则返回本节点补做实验。

### 10.1.1 Defining integration flows with XML

**正式节点 3/17。** 为“10.1.1 Defining integration flows with XML”标出生产者、代理、消费者、确认、重试和死信责任。使用重复、乱序、超时和不可反序列化消息证明交付语义，而不是只看发送API返回。

验收时将该节点绑定到“集成流拓扑、消息模式、通道容量预算、端点合同测试和邮件故障回放”，并写出一个能够推翻当前设计的条件。若只能复述API名称，不能从失败症状定位到边界，则返回本节点补做实验。

### 10.1.2 Configuring integration flows in Java

**正式节点 4/17。** 为“10.1.2 Configuring integration flows in Java”标出生产者、代理、消费者、确认、重试和死信责任。使用重复、乱序、超时和不可反序列化消息证明交付语义，而不是只看发送API返回。

验收时将该节点绑定到“集成流拓扑、消息模式、通道容量预算、端点合同测试和邮件故障回放”，并写出一个能够推翻当前设计的条件。若只能复述API名称，不能从失败症状定位到边界，则返回本节点补做实验。

### 10.1.3 Using Spring Integration’s DSL configuration

**正式节点 5/17。** 为“10.1.3 Using Spring Integration’s DSL configuration”标出生产者、代理、消费者、确认、重试和死信责任。使用重复、乱序、超时和不可反序列化消息证明交付语义，而不是只看发送API返回。

验收时将该节点绑定到“集成流拓扑、消息模式、通道容量预算、端点合同测试和邮件故障回放”，并写出一个能够推翻当前设计的条件。若只能复述API名称，不能从失败症状定位到边界，则返回本节点补做实验。

### 10.2 Surveying the Spring Integration landscape

**正式节点 6/17。** 为“10.2 Surveying the Spring Integration landscape”标出生产者、代理、消费者、确认、重试和死信责任。使用重复、乱序、超时和不可反序列化消息证明交付语义，而不是只看发送API返回。

验收时将该节点绑定到“集成流拓扑、消息模式、通道容量预算、端点合同测试和邮件故障回放”，并写出一个能够推翻当前设计的条件。若只能复述API名称，不能从失败症状定位到边界，则返回本节点补做实验。

### 10.2.1 Message channels

**正式节点 7/17。** 为“10.2.1 Message channels”标出生产者、代理、消费者、确认、重试和死信责任。使用重复、乱序、超时和不可反序列化消息证明交付语义，而不是只看发送API返回。

验收时将该节点绑定到“集成流拓扑、消息模式、通道容量预算、端点合同测试和邮件故障回放”，并写出一个能够推翻当前设计的条件。若只能复述API名称，不能从失败症状定位到边界，则返回本节点补做实验。

### 10.2.2 Filters

**正式节点 8/17。** 把“10.2.2 Filters”放回Part 2. Integrated Spring的连续案例，明确输入、Spring边界、领域不变量、外部副作用和验收证据。先预测一次条件缺失或依赖失败，再运行最小切片验证。

验收时将该节点绑定到“集成流拓扑、消息模式、通道容量预算、端点合同测试和邮件故障回放”，并写出一个能够推翻当前设计的条件。若只能复述API名称，不能从失败症状定位到边界，则返回本节点补做实验。

### 10.2.3 Transformers

**正式节点 9/17。** 沿输入解析、校验、领域命令、状态变化和响应表示拆解“10.2.3 Transformers”。成功、客户端错误、并发冲突与服务失败必须拥有不同且稳定的可观察结果。

验收时将该节点绑定到“集成流拓扑、消息模式、通道容量预算、端点合同测试和邮件故障回放”，并写出一个能够推翻当前设计的条件。若只能复述API名称，不能从失败症状定位到边界，则返回本节点补做实验。

### 10.2.4 Routers

**正式节点 10/17。** 为“10.2.4 Routers”标出生产者、代理、消费者、确认、重试和死信责任。使用重复、乱序、超时和不可反序列化消息证明交付语义，而不是只看发送API返回。

验收时将该节点绑定到“集成流拓扑、消息模式、通道容量预算、端点合同测试和邮件故障回放”，并写出一个能够推翻当前设计的条件。若只能复述API名称，不能从失败症状定位到边界，则返回本节点补做实验。

### 10.2.5 Splitters

**正式节点 11/17。** 把“10.2.5 Splitters”放回Part 2. Integrated Spring的连续案例，明确输入、Spring边界、领域不变量、外部副作用和验收证据。先预测一次条件缺失或依赖失败，再运行最小切片验证。

验收时将该节点绑定到“集成流拓扑、消息模式、通道容量预算、端点合同测试和邮件故障回放”，并写出一个能够推翻当前设计的条件。若只能复述API名称，不能从失败症状定位到边界，则返回本节点补做实验。

### 10.2.6 Service activators

**正式节点 12/17。** 把“10.2.6 Service activators”放回Part 2. Integrated Spring的连续案例，明确输入、Spring边界、领域不变量、外部副作用和验收证据。先预测一次条件缺失或依赖失败，再运行最小切片验证。

验收时将该节点绑定到“集成流拓扑、消息模式、通道容量预算、端点合同测试和邮件故障回放”，并写出一个能够推翻当前设计的条件。若只能复述API名称，不能从失败症状定位到边界，则返回本节点补做实验。

### 10.2.7 Gateways

**正式节点 13/17。** 为“10.2.7 Gateways”标出生产者、代理、消费者、确认、重试和死信责任。使用重复、乱序、超时和不可反序列化消息证明交付语义，而不是只看发送API返回。

验收时将该节点绑定到“集成流拓扑、消息模式、通道容量预算、端点合同测试和邮件故障回放”，并写出一个能够推翻当前设计的条件。若只能复述API名称，不能从失败症状定位到边界，则返回本节点补做实验。

### 10.2.8 Channel adapters

**正式节点 14/17。** 为“10.2.8 Channel adapters”标出生产者、代理、消费者、确认、重试和死信责任。使用重复、乱序、超时和不可反序列化消息证明交付语义，而不是只看发送API返回。

验收时将该节点绑定到“集成流拓扑、消息模式、通道容量预算、端点合同测试和邮件故障回放”，并写出一个能够推翻当前设计的条件。若只能复述API名称，不能从失败症状定位到边界，则返回本节点补做实验。

### 10.2.9 Endpoint modules

**正式节点 15/17。** 把“10.2.9 Endpoint modules”放回Part 2. Integrated Spring的连续案例，明确输入、Spring边界、领域不变量、外部副作用和验收证据。先预测一次条件缺失或依赖失败，再运行最小切片验证。

验收时将该节点绑定到“集成流拓扑、消息模式、通道容量预算、端点合同测试和邮件故障回放”，并写出一个能够推翻当前设计的条件。若只能复述API名称，不能从失败症状定位到边界，则返回本节点补做实验。

### 10.3 Creating an email integration flow

**正式节点 16/17。** 为“10.3 Creating an email integration flow”标出生产者、代理、消费者、确认、重试和死信责任。使用重复、乱序、超时和不可反序列化消息证明交付语义，而不是只看发送API返回。

验收时将该节点绑定到“集成流拓扑、消息模式、通道容量预算、端点合同测试和邮件故障回放”，并写出一个能够推翻当前设计的条件。若只能复述API名称，不能从失败症状定位到边界，则返回本节点补做实验。

### Summary

**正式节点 17/17。** 把“Summary”放回Part 2. Integrated Spring的连续案例，明确输入、Spring边界、领域不变量、外部副作用和验收证据。先预测一次条件缺失或依赖失败，再运行最小切片验证。

验收时将该节点绑定到“集成流拓扑、消息模式、通道容量预算、端点合同测试和邮件故障回放”，并写出一个能够推翻当前设计的条件。若只能复述API名称，不能从失败症状定位到边界，则返回本节点补做实验。

## 从运行症状开始

Spring应用可视为一组由容器建立、由请求或消息驱动、受配置和身份约束、向外部系统产生副作用的组件图。先用<Term def="根据类路径、Bean、属性和运行环境决定是否创建组件的机制">条件装配</Term>解释Bean为什么存在，再用<Term def="跨Web、业务、数据或消息边界保持的可观察承诺">边界合同</Term>限定输入输出；只有这样，自动配置才是可检查的决策，不是魔法。

对每条执行链记录五种状态：尚未接收、已验证、已改变领域状态、已提交外部副作用、已向调用方确认。同步HTTP、事务数据库、消息代理和响应式流对这些状态的确认时点不同。设计必须指出哪个状态可以重试、哪个需要<Term def="重复执行仍得到相同业务结果的性质">幂等</Term>键、哪个需要补偿，以及超时发生时调用方究竟知道什么。

测试按由窄到宽排列：纯领域测试证明不变量，Spring切片测试证明框架边界，容器化集成测试证明真实驱动或代理合同，端到端测试只保留关键旅程。测试上下文启动得越多，反馈越慢且故障定位越宽；但过度mock也会跳过序列化、事务、过滤链和条件装配。每层都要写明它能证明什么、不能证明什么。

可观察性同样属于设计。日志回答单次事件，指标回答时间序列，追踪回答跨边界因果，审计回答谁在何时做了什么。任何一项都不能替代业务结果。为第10章 Spring Integration选择低基数标签、稳定关联ID和失败分类，并用<Term def="在受控位置制造依赖、延迟、权限或资源异常以验证系统行为">故障注入</Term>确认信号确实出现，而不是只配置采集器。

最后建立<Term def="从原书技术组合迁往当前Spring世代时逐项记录合同、API、运行与回滚差异的清单">版本迁移账本</Term>。每项变化标注编译期、启动期、请求期或生产运行期风险；先建立第6版可重复基线，再一次只改变Java、Framework、Boot或外部组件中的一项。否则迁移成功也无法判断是哪项变化生效，失败更难定位。

## 证据链最小档案

每次实验创建一条不可变记录，至少包含提交版本、JDK、Framework、Boot、外部组件、配置摘要、输入数据、开始与结束时间、关联ID、预期结果、实际结果和回滚触发器。日志片段只保留定位所需字段，指标必须注明单位与聚合窗口，追踪必须指出采样策略；否则不同环境的数字不能直接比较。

把成功和失败放在同一时间线上：请求何时被接收，身份何时建立，事务何时开始，外部调用何时发出，状态何时提交，响应或消息何时确认。超时尤其要记录“调用方不知道什么”，因为未知结果会决定能否自动重试。对于重复执行，保存业务幂等键、数据库约束和代理确认三层证据，不能只依赖内存标志。

交接前做一次冷启动重放和一次依赖降级重放。冷启动检查类路径扫描、条件装配、配置绑定、模式迁移和就绪信号；降级重放检查超时预算、线程或连接释放、错误分类、告警和恢复。若另一个环境无法用同一命令得到相同合同结果，先修复可复现性，再讨论性能或框架优化。

## 原书基线与现代复核

**第6版基线：** 对照XML、Java配置和Integration DSL三种声明方式，完成邮件集成流。先保存依赖树、自动配置报告、数据库或代理版本、测试结果和关键运行信号，形成可重复参照。

**当前复核：** 在当前Spring Integration与Jakarta Mail上复核适配器模块，不改变消息通道和端点的概念边界。Spring Framework 7建立在Jakarta EE 11之上，Boot 4进行了模块化并支持Java 25，同时保留Java 17基线。迁移不能只修改版本号；需要检查<code>javax</code>到<code>jakarta</code>、Security DSL、序列化、驱动、管理端点和第三方项目兼容性。

**保持不变的判断：** 集成流每一跳的输入输出、线程边界、错误通道与重试责任都可测试。框架API会变化，但输入验证、最小权限、事务边界、消息交付、背压、资源释放和回滚证据仍是稳定验收面。

独立教学示例先展示本单元的最小公开边界：

```java
@Bean
IntegrationFlow orders() {
  return IntegrationFlow.from("orders.in")
      .filter(Order.class, Order::isValid)
      .transform(Order.class, OrderPlaced::from)
      .channel("orders.out").get();
}
```

客户端合同测试不复制实现，只观察结果和副作用：

```java
@Test
void boundaryKeepsFailureObservable() {
  var evidence = runScenario("baseline", "boundary", "failure", "rollback");
  assertAll(
      () -> assertTrue(evidence.contractVisible()),
      () -> assertTrue(evidence.failureClassified()),
      () -> assertTrue(evidence.rollbackReplayed())
  );
}
```

交接验收使用同一条行为描述：

```gherkin
Feature: 10 Integrating Spring
  Scenario: replay the Spring boundary with evidence
    Given the original version, input contract, dependencies, and expected side effects
    When the success path and one controlled failure are executed
    Then state, identity, resource use, and external effects are observable
    And a reviewer can keep, narrow, migrate, or roll back the design
```

## 定量预算而非直觉

端到端延迟先按串行关键路径建模，异步只改变等待位置，不会让成本消失：

$$
L_{e2e}=L_{queue}+L_{spring}+L_{domain}+L_{io}+L_{serialize}
$$

若一次请求产生多个远程或数据库调用，最小调用放大为：

$$
A=N_{request}\times(C_{db}+C_{remote}+C_{message})
$$

响应式或消息消费者的安全在途量受处理速率与确认延迟约束：

$$
I_{safe}\leq R_{consume}\times T_{ack}
$$

证据完整度不是平均分；任一关键门禁缺失都不能交付：

$$
G=G_{contract}\land G_{failure}\land G_{observe}\land G_{rollback}
$$

这些公式用于提出预测。真实结论必须由固定数据集、预热、并发、版本和故障条件下的测量支持，并报告P50、P95、P99、错误率、资源占用与恢复时间。

先预测：在运行实验前写下延迟、调用放大、在途量和失败位置的方向性变化；运行后只允许用保存的证据修正模型，不能删除不符合预期的样本。

## 三步实验闭环

<Steps>
  <Step title="声明边界与预测">写出让过滤器拒绝、转换器抛错、路由无目标、邮件适配器超时，观察错误是否到达预定恢复路径的输入、前置条件、状态变化、外部副作用和预期信号；禁止先运行再解释。<Sia6ArchitectureLab /></Step>
  <Step title="只改变一个条件">保持数据、版本和负载固定，只切换一项配置、实现、失败或版本，保存日志、指标、追踪、数据库与代理证据。<Sia6FlowLab /></Step>
  <Step title="反证、回滚与交接">主动寻找推翻当前方案的边界；若触发“DSL看似线性却隐藏异步通道、消息头丢失、无界拆分和错误通道无人消费”，缩小适用域或回滚，并让未参与实现的人重放。<Sia6EvidenceLab /></Step>
</Steps>

## 高频陷阱

<Callout type="trap">**把starter或注解当功能证明。** 类路径只触发候选配置；还要检查条件评估、实际Bean、过滤链、驱动与运行信号。</Callout>

<Callout type="trap">**只测成功路径。** DSL看似线性却隐藏异步通道、消息头丢失、无界拆分和错误通道无人消费。至少加入错误输入、依赖超时、重复请求或消息、权限不足与资源耗尽中的适用项。</Callout>

<Callout type="trap">**把升级编译通过当迁移完成。** Framework 7与Boot 4改变Jakarta基线、模块和部分API；必须重放公开合同、数据、身份、观测和关闭流程。</Callout>

## 本章回顾

第10章 Spring Integration的主线是“用消息、通道与端点组合过滤、转换、路由、拆分、网关和适配器，显式表达企业集成模式”。完成标准不是示例启动，而是形成集成流拓扑、消息模式、通道容量预算、端点合同测试和邮件故障回放，并证明“集成流每一跳的输入输出、线程边界、错误通道与重试责任都可测试”。若证据无法由他人复现，就回到官方节点补齐条件、反例和可观察结果。

<Exercises>

**问题 1**：如何为第10章 Spring Integration建立最小可执行切片？

<Answer>选择一个真实输入，明确Spring边界、领域不变量与外部副作用；按“让过滤器拒绝、转换器抛错、路由无目标、邮件适配器超时，观察错误是否到达预定恢复路径”执行成功和失败对照，把结果写入集成流拓扑、消息模式、通道容量预算、端点合同测试和邮件故障回放。</Answer>

**问题 2**：为什么能启动或收到200响应仍不足以证明本单元正确？

<Answer>启动只证明部分条件成立，200也可能掩盖错误状态、重复副作用、越权或阻塞。还需验证边界合同、失败分类、运行信号与回滚，并覆盖“DSL看似线性却隐藏异步通道、消息头丢失、无界拆分和错误通道无人消费”。</Answer>

**问题 3**：从第6版迁移到当前Spring世代时应怎样控制变量？

<Answer>先固定原书基线和证据，一次只升级Java、Framework、Boot或外部组件之一；核对Jakarta、Security、驱动、管理端点和第三方兼容，原反例全部通过后再进入下一步。</Answer>

</Exercises>

<Glossary>
  <GlossaryItem term="条件装配">依据类路径、Bean、属性与环境决定配置是否生效，并能由条件报告解释。</GlossaryItem>
  <GlossaryItem term="边界合同">跨Web、业务、数据、消息或管理边界保持的输入、输出、状态和失败承诺。</GlossaryItem>
  <GlossaryItem term="幂等">同一业务意图重复执行仍产生一次有效状态变化，是重试设计的前提之一。</GlossaryItem>
  <GlossaryItem term="故障注入">在可控位置制造延迟、拒绝、断线、资源耗尽或无效输入以验证恢复与观测。</GlossaryItem>
  <GlossaryItem term="版本迁移账本">逐项记录原书与当前版本间合同、API、运行证据、风险和回滚条件。</GlossaryItem>
</Glossary>

[← 上一页：第9章 异步消息](/learn/spring-in-action/11-sia-6e-09-asynchronous-messaging/sia-6e-09-asynchronous-messaging) · [下一页：Part 3 响应式Spring →](/learn/spring-in-action/13-sia-6e-part-3-reactive-spring/sia-6e-part-3-reactive-spring)

<Attribution adaptedFrom="Craig Walls, Spring in Action, Sixth Edition" adaptedUrl="https://www.manning.com/books/spring-in-action-sixth-edition" />
