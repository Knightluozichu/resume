---
title: "SQL基础与SELECT"
type: A
section: "数据库"
order: 1
description: "数据库核心概念与SELECT语句——掌握SQL查询的基石与执行顺序"
demo: true
math: false
sourceUrl: ""
draft: false
---

import { Objectives, Callout, Glossary, GlossaryItem, Term, Exercises, Answer } from "@/components/mdx/mdx-components";
import { SqtSqlBasicsDiagram } from "@/components/mdx/sql-ten-minutes/diagrams/sqt-sql-basics";

<Objectives>

- 理解数据库、表、列、行、主键等核心概念
- 掌握SELECT语句的基本语法和常用要素
- 理解SQL语句的书写顺序与实际执行顺序的区别
- 了解SQL语言的四大分类（DQL/DML/DDL/DCL）

</Objectives>

## SELECT语句结构与执行顺序

<SqtSqlBasicsDiagram />

SQL（Structured Query Language，结构化查询语言）是与关系型数据库通信的标准语言。全书聚焦DQL（数据查询语言），核心是SELECT语句。

## 数据库核心概念

```
// 数据库（Database）：存储数据的容器
//   └── 表（Table）：结构化的数据集合
//        └── 列（Column）：表中每个字段（垂直方向）
//        └── 行（Row）：表中每条记录（水平方向）
//   └── 主键（Primary Key）：唯一标识每行的列
//        · 唯一性：不重复
//        · 非空性：不能为NULL
//        · 可复合：多列组合做主键
```

<Callout type="info" title="关系型数据库的本质">
关系型数据库通过"表"存储数据，通过"主键-外键"建立表间关系。理解表、行、列的概念是写SQL的基础——SELECT选列，WHERE选行，JOIN连表。
</Callout>

## SELECT基本查询

```
-- 查询单列
SELECT prod_name FROM products;

-- 查询多列（逗号分隔）
SELECT prod_name, prod_price, vend_id FROM products;

-- 查询所有列（* 通配符）
SELECT * FROM products;

-- 去重查询（DISTINCT）
SELECT DISTINCT vend_id FROM products;

-- 限制结果行数（LIMIT）
SELECT prod_name FROM products LIMIT 5;
SELECT prod_name FROM products LIMIT 5 OFFSET 5;  -- 跳过前5行

-- 使用注释
-- 单行注释
/* 多行
   注释 */
```

<Callout type="warning" title="SELECT * 的使用建议">
生产环境避免使用 `SELECT *`：①浪费带宽和内存（传输不需要的列）；②表结构变化时可能破坏应用；③无法利用覆盖索引。应明确指定需要的列名。
</Callout>

## SQL执行顺序（全书核心）

```
// 书写顺序（你写SQL的顺序）
// SELECT → FROM → WHERE → GROUP BY → HAVING → ORDER BY → LIMIT

// 实际执行顺序（数据库处理的顺序）
// FROM    → 确定数据来源（表/JOIN）
// WHERE   → 逐行过滤
// GROUP BY → 分组
// HAVING  → 组级过滤
// SELECT  → 选取列/计算
// DISTINCT → 去重
// ORDER BY → 排序
// LIMIT   → 限制行数

// 关键推论：
// ① WHERE 中不能用 SELECT 定义的别名（WHERE 先执行）
// ② HAVING 可以用聚合函数，WHERE 不行
// ③ ORDER BY 可以用 SELECT 定义的别名（最后执行）
```

## SQL四大分类

| 分类 | 全称 | 核心语句 | 用途 |
|------|------|----------|------|
| DQL | Data Query Language | SELECT | 查询数据 |
| DML | Data Manipulation Language | INSERT/UPDATE/DELETE | 修改数据 |
| DDL | Data Definition Language | CREATE/ALTER/DROP | 定义结构 |
| DCL | Data Control Language | GRANT/REVOKE | 控制权限 |

<Callout type="success" title="本书聚焦">
《SQL必知必会》以DQL（数据查询）为核心，重点讲解SELECT语句的各种用法。DML/DDL/DCL作为补充知识，帮助理解完整的SQL生态。
</Callout>

<Glossary>

<GlossaryItem term="SELECT">
SQL中最核心的语句，用于从数据库表中检索数据。基本语法：`SELECT 列名 FROM 表名`。可配合WHERE（过滤）、ORDER BY（排序）、GROUP BY（分组）、LIMIT（限制行数）等子句实现复杂查询。
</GlossaryItem>

<GlossaryItem term="主键">
表中唯一标识每一行的列（或列组合），具有唯一性（不重复）和非空性（不为NULL）两个特性。主键是关系型数据库建立表间关系的基础，也是高效查询的依据。
</GlossaryItem>

<GlossaryItem term="执行顺序">
SQL语句的书写顺序与数据库实际执行顺序不同。执行顺序为FROM → WHERE → GROUP BY → HAVING → SELECT → DISTINCT → ORDER BY → LIMIT。理解执行顺序是写对SQL的关键，决定了别名、聚合函数等在哪一步可以使用。
</GlossaryItem>

<GlossaryItem term="DISTINCT">
SELECT的关键字，用于对查询结果去重。`SELECT DISTINCT 列名` 返回该列的所有不重复值。注意DISTINCT作用于所有选中列的组合，不是单列去重。
</GlossaryItem>

</Glossary>

<Exercises>

**问题 1**：SQL语句的书写顺序和执行顺序分别是什么？为什么这个区别很重要？

<Answer>
书写顺序：`SELECT → FROM → WHERE → GROUP BY → HAVING → ORDER BY → LIMIT`。执行顺序：`FROM → WHERE → GROUP BY → HAVING → SELECT → DISTINCT → ORDER BY → LIMIT`。区别的核心在于SELECT在执行时排在WHERE和GROUP BY之后。重要性：①WHERE中不能用SELECT定义的别名（WHERE先执行）；②HAVING可以用聚合函数而WHERE不行（HAVING在GROUP BY之后）；③ORDER BY可以用SELECT定义的别名（最后执行）；④理解执行顺序才能正确排查查询逻辑错误。
</Answer>

**问题 2**：`SELECT *` 在生产环境中为什么不被推荐？应该怎么写？

<Answer>
`SELECT *` 不被推荐的原因：①浪费带宽和内存——传输了不需要的列；②表结构变化时可能破坏应用——新增列导致结果集结构变化；③无法利用覆盖索引——查询所有列必然需要回表。正确做法是明确指定需要的列名，如 `SELECT prod_name, prod_price FROM products`，这样更安全、更高效、更可读。
</Answer>

**问题 3**：什么是主键？它有哪些特性？为什么关系型数据库需要主键？

<Answer>
主键是表中唯一标识每一行的列（或列组合），具有两个核心特性：①唯一性——每行的主键值不重复；②非空性——主键不能为NULL。关系型数据库需要主键的原因：①确保数据的唯一性——通过主键可以精确定位每一行；②建立表间关系——外键引用的就是另一张表的主键；③提高查询效率——主键自动创建索引，主键查询速度最快；④保证数据完整性——防止插入重复或空值记录。
</Answer>

**问题 4**：SQL语言分为哪四大类？《SQL必知必会》主要聚焦哪一类？

<Answer>
SQL四大分类：①DQL（数据查询语言）——SELECT语句，用于检索数据；②DML（数据操作语言）——INSERT/UPDATE/DELETE，用于修改数据；③DDL（数据定义语言）——CREATE/ALTER/DROP，用于定义和修改表结构；④DCL（数据控制语言）——GRANT/REVOKE，用于管理权限。《SQL必知必会》主要聚焦DQL，以SELECT语句为核心，系统讲解数据检索的各种技巧，DML/DDL/DCL作为补充知识。
</Answer>

</Exercises>
