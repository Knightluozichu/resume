---
title: "第4课 过滤数据"
description: "依据第5版完整目录覆盖8个节点：用WHERE、比较、范围和空值判断定义目标行集"
type: C
section: "SQL必知必会（第5版 / 2020）"
draft: true
math: false
sourceUrl: "https://www.informit.com/store/sql-in-10-minutes-a-day-sams-teach-yourself-9780135182871"
demo: true
order: 0
---

import {
  SqtLesson04FilteringDataQueryLab,
  SqtLesson04FilteringDataDialectLab,
  SqtLesson04FilteringDataEvidenceLab,
} from "@/components/mdx/sql-ten-minutes/diagrams/sqt-lesson04-filtering-data";

# 第4课 过滤数据

本页对应Ben Forta《SQL in 10 Minutes a Day》第5版及钟鸣、刘晓霞译《SQL必知必会》第5版，人民邮电出版社2020年8月出版，239页，ISBN 9787115539168。课程依据原版出版社完整目录与中文版目录交叉核对后独立重构，不复制原文；正式分母为22课、4个附录与常用语句速查，另设学习地图和总复习，共24页。

这不是MySQL专册。原书以可跨数据库理解的SQL为主，同时提醒函数、限制行、拼接、过程、游标等存在DBMS差异。本课程统一使用Customers、Vendors、Products、Orders和OrderItems样例模型；标准写法与DB2、MariaDB/MySQL、SQL Server、Oracle、PostgreSQL、SQLite方言分开记录。

<Objectives>

- 能解释“第4课 过滤数据”的8个正式节点，并把它们连接到“用WHERE、比较、范围和空值判断定义目标行集”主线。
- 能复现谓词真值表、边界数据集和NULL反例，保存样例数据、SQL、预期结果、实际结果、错误和版本。
- 能比较至少两种DBMS的语法与行为，证明“过滤谓词对等值、不等值、范围端点和NULL分别给出可预测结果”。
- 能设计反例推翻“用等号比较NULL，或忘记BETWEEN通常包含两个端点”，并给出可重放修复和回退条件。

</Objectives>

## 从结果问题开始

先预测结果合同：需要哪些列、哪些行、是否去重、NULL如何处理、按什么顺序、预计多少行。再写SQL，并在至少一个边界样本上推翻错误查询。SQL声明的是结果，不保证存储顺序，也不会自动替你定义业务口径。

## 学习合同与证据

<Term def="用于用WHERE、比较、范围和空值判断定义目标行集的第1个核心概念；必须用样例数据、结果和失败边界定义">WHERE</Term>、<Term def="用于用WHERE、比较、范围和空值判断定义目标行集的第2个核心概念；必须用样例数据、结果和失败边界定义">谓词</Term>、<Term def="用于用WHERE、比较、范围和空值判断定义目标行集的第3个核心概念；必须用样例数据、结果和失败边界定义">比较操作符</Term>、<Term def="用于用WHERE、比较、范围和空值判断定义目标行集的第4个核心概念；必须用样例数据、结果和失败边界定义">BETWEEN</Term>、<Term def="用于用WHERE、比较、范围和空值判断定义目标行集的第5个核心概念；必须用样例数据、结果和失败边界定义">NULL</Term>
构成本页的核心词汇。主线是“用WHERE、比较、范围和空值判断定义目标行集”，交付物是谓词真值表、边界数据集和NULL反例。最小不变量是：过滤谓词对等值、不等值、范围端点和NULL分别给出可预测结果。每次实验都记录DBMS名称与版本、样例脚本版本、会话设置和完整结果，避免同一句语句因方言或数据不同而无法比较。

<SqtLesson04FilteringDataQueryLab />

## 第5版目录逐节点映射

### 4.1 使用WHERE子句

**节点合同 1/8 · 定义输入。** 先说明“4.1 使用WHERE子句”改变的是列、行、组、对象还是事务状态，再把它接入“用WHERE、比较、范围和空值判断定义目标行集”。执行前写出结果列、预计行数、排序、NULL和重复值口径；执行后保存SQL、输出、影响行数、警告与错误。验收以“过滤谓词对等值、不等值、范围端点和NULL分别给出可预测结果”为准；只背关键字而无法用样例表证明结果，不算掌握。

### 4.2 WHERE子句操作符

**节点合同 2/8 · 预测行集。** 先说明“4.2 WHERE子句操作符”改变的是列、行、组、对象还是事务状态，再把它接入“用WHERE、比较、范围和空值判断定义目标行集”。执行前写出结果列、预计行数、排序、NULL和重复值口径；执行后保存SQL、输出、影响行数、警告与错误。验收以“过滤谓词对等值、不等值、范围端点和NULL分别给出可预测结果”为准；只背关键字而无法用样例表证明结果，不算掌握。

### 4.2.1 检查单个值

**节点合同 3/8 · 构造SQL。** 先说明“4.2.1 检查单个值”改变的是列、行、组、对象还是事务状态，再把它接入“用WHERE、比较、范围和空值判断定义目标行集”。执行前写出结果列、预计行数、排序、NULL和重复值口径；执行后保存SQL、输出、影响行数、警告与错误。验收以“过滤谓词对等值、不等值、范围端点和NULL分别给出可预测结果”为准；只背关键字而无法用样例表证明结果，不算掌握。

### 4.2.2 不匹配检查

**节点合同 4/8 · 核对结果。** 先说明“4.2.2 不匹配检查”改变的是列、行、组、对象还是事务状态，再把它接入“用WHERE、比较、范围和空值判断定义目标行集”。执行前写出结果列、预计行数、排序、NULL和重复值口径；执行后保存SQL、输出、影响行数、警告与错误。验收以“过滤谓词对等值、不等值、范围端点和NULL分别给出可预测结果”为准；只背关键字而无法用样例表证明结果，不算掌握。

### 4.2.3 范围值检查

**节点合同 5/8 · 注入失败。** 先说明“4.2.3 范围值检查”改变的是列、行、组、对象还是事务状态，再把它接入“用WHERE、比较、范围和空值判断定义目标行集”。执行前写出结果列、预计行数、排序、NULL和重复值口径；执行后保存SQL、输出、影响行数、警告与错误。验收以“过滤谓词对等值、不等值、范围端点和NULL分别给出可预测结果”为准；只背关键字而无法用样例表证明结果，不算掌握。

### 4.2.4 空值检查

**节点合同 6/8 · 比较方言。** 先说明“4.2.4 空值检查”改变的是列、行、组、对象还是事务状态，再把它接入“用WHERE、比较、范围和空值判断定义目标行集”。执行前写出结果列、预计行数、排序、NULL和重复值口径；执行后保存SQL、输出、影响行数、警告与错误。验收以“过滤谓词对等值、不等值、范围端点和NULL分别给出可预测结果”为准；只背关键字而无法用样例表证明结果，不算掌握。

### 4.3 小结

**节点合同 7/8 · 定义输入。** 先说明“4.3 小结”改变的是列、行、组、对象还是事务状态，再把它接入“用WHERE、比较、范围和空值判断定义目标行集”。执行前写出结果列、预计行数、排序、NULL和重复值口径；执行后保存SQL、输出、影响行数、警告与错误。验收以“过滤谓词对等值、不等值、范围端点和NULL分别给出可预测结果”为准；只背关键字而无法用样例表证明结果，不算掌握。

### 4.4 挑战题

**节点合同 8/8 · 预测行集。** 先说明“4.4 挑战题”改变的是列、行、组、对象还是事务状态，再把它接入“用WHERE、比较、范围和空值判断定义目标行集”。执行前写出结果列、预计行数、排序、NULL和重复值口径；执行后保存SQL、输出、影响行数、警告与错误。验收以“过滤谓词对等值、不等值、范围端点和NULL分别给出可预测结果”为准；只背关键字而无法用样例表证明结果，不算掌握。

## 机制推演：集合、结果与方言

第一层是集合语义。FROM确定候选行源，WHERE过滤行，GROUP BY形成组，HAVING过滤组，SELECT计算输出列，DISTINCT去重，ORDER BY最后建立显示顺序。虽然优化器可以改变物理执行方式，但不能改变合法SQL的结果语义。定位错误时先找口径属于哪一层，避免靠不断追加条件碰运气。

第二层是基数。单表过滤、子查询、联结和分组都会改变行数。写SQL前估算最小、典型和最大行数；执行后核对总行数、主键去重数与关键金额。多表联结尤其要标注一对一、一对多和多对多，否则重复行可能在聚集时放大，却仍给出看似合理的数字。

第三层是NULL与三值逻辑。未知值既不等于普通值，也不等于另一个NULL。比较、NOT IN、外联结、聚集和字符串拼接都可能传播或忽略NULL。每课至少保留一个NULL样本，并明确结果是TRUE、FALSE还是UNKNOWN；缺少这一项，查询只能在理想数据上成立。

第四层是方言账本。LIMIT、TOP和FETCH FIRST，双竖线、加号和CONCAT，日期函数、过程体、游标、触发器和标识符引用都有差异。先写语义合同，再为目标DBMS实现语法；迁移时保持数据与期望结果不变，一次替换一个方言点，记录不支持、改写和回退。

## 最小可重放实验

```sql
-- 先保存目标DBMS、版本与样例数据版本
SELECT prod_id, prod_price FROM Products WHERE prod_price BETWEEN 5 AND 10 OR prod_price IS NULL;
```

先手算一小组结果，再执行查询。查询类实验保存列名、完整行集、顺序和NULL；写入类实验先用等价SELECT预览目标行，再放进可回滚事务；DDL类实验保存定义前后元数据与依赖。不要用“语句执行成功”替代结果正确性。

```text
dbms=PostgreSQL | MySQL | SQLServer | Oracle | SQLite
lesson=sqt-lesson04-filtering-data
sample_tables=Customers,Vendors,Products,Orders,OrderItems
expected=过滤谓词对等值、不等值、范围端点和NULL分别给出可预测结果
negative_case=null | duplicate | empty-set | one-to-many | denied | rollback
```

```sql
-- 通用证据查询：至少核对结果行数与主键去重数
SELECT COUNT(*) AS rows_n FROM Products;
SELECT COUNT(DISTINCT prod_id) AS distinct_ids FROM Products;
```

## 三步交互实验

<Stepper>
  <Step title="1. 预测结果合同">
    选择正式目录节点，先写列、行、NULL、重复与顺序，再查看查询模型。
    <SqtLesson04FilteringDataQueryLab />
  </Step>
  <Step title="2. 切换DBMS方言">
    保持数据和预期结果不变，比较限制、拼接、日期、过程或事务语法。
    <SqtLesson04FilteringDataDialectLab />
  </Step>
  <Step title="3. 通过证据门">
    登记样例、SQL、结果、失败、方言与回退证据，全部通过后才交付。
    <SqtLesson04FilteringDataEvidenceLab />
  </Step>
</Stepper>

<Callout type="trap">
  **结果陷阱。**
  用等号比较NULL，或忘记BETWEEN通常包含两个端点。先恢复结果合同和最小反例，再修改SQL；不要用新增DISTINCT或随意GROUP
  BY掩盖基数错误。
</Callout>

<Callout type="trap">
  **方言陷阱。**
  能在一个DBMS执行不代表符合标准或能移植。限制行、函数、过程、游标和DDL必须在方言账本中标出目标平台、版本与等价写法。
</Callout>

<Callout type="warning">
  UPDATE、DELETE、DROP、触发器、过程与事务实验只在隔离样例库进行。先备份或开启事务，明确影响行数上限与停止条件。
</Callout>

## 证据解释

数据证据证明输入一致。保存建表脚本、插入脚本、表行数、主键去重数和关键外键计数。相同SQL在不同数据上得到不同结果毫无可比性；样例数据一旦变化，先更新基线而不是修改查询迎合新输出。

结果证据证明口径正确。对查询保存列、行、顺序、NULL和重复值；对写入保存前后快照和影响行数；对DDL保存元数据与依赖。用独立对账查询验证总数、分组数和金额，不让待测SQL自己证明自己。

失败证据证明边界有效。准备空集、NULL、重复键、孤立引用、一对多放大、权限拒绝和事务回滚样本。预期失败要记录错误码或状态；意外成功通常意味着约束、谓词、权限或事务边界缺失。

方言证据证明可移植范围。至少选择两种DBMS，用同一输入和结果合同运行；若语法不同，分别保存实现。若语义不同，明确无法移植的原因与调用方处理方式，而不是把差异隐藏在注释中。

## 本章回顾

重新完成“用WHERE、比较、范围和空值判断定义目标行集”：先定义结果合同和基数，选择正式目录节点，写最小SQL，加入NULL与重复值反例，核对完整结果，再切换DBMS。最终交付谓词真值表、边界数据集和NULL反例，由另一位学习者证明“过滤谓词对等值、不等值、范围端点和NULL分别给出可预测结果”。

## 复习与独立交接

<Exercises>
**问题 1**：为什么“第4课 过滤数据”必须覆盖8个正式节点？
<Answer>这些节点共同组成“用WHERE、比较、范围和空值判断定义目标行集”的语义、语法、结果、失败和方言链；缺项会让谓词真值表、边界数据集和NULL反例无法独立复现。</Answer>

**问题 2**：本页最小正确性合同是什么？

<Answer>
  过滤谓词对等值、不等值、范围端点和NULL分别给出可预测结果。必须由相同样例数据、完整结果、边界样本和方言记录共同证明。
</Answer>

**问题 3**：怎样设计能推翻错误SQL的反例？

<Answer>
  先让“用等号比较NULL，或忘记BETWEEN通常包含两个端点”成为待证假设，再只加入NULL、重复、一对多、空集或权限不足中的一个样本，比较行数、键数和结果差异。
</Answer>

**问题 4**：为什么只看前几行输出不够？

<Answer>
  前几行不能证明总行数、重复、NULL、排序稳定性和联结基数。必须核对完整结果或使用独立计数与校验查询。
</Answer>

**问题 5**：如何验证跨DBMS可移植性？

<Answer>
  保持样例数据和结果合同不变，在两种以上DBMS分别实现并运行，记录语法、返回类型、NULL、错误和执行限制的差异。
</Answer>

**问题 6**：独立交接至少包含什么？

<Answer>需要DBMS版本卡、样例脚本、目标SQL、谓词真值表、边界数据集和NULL反例、预期与实际结果、失败反例、方言账本和回退条件。</Answer>
</Exercises>

<Glossary>
  <GlossaryItem term="WHERE">
    WHERE必须由样例输入、结果集合和失败边界共同定义。
  </GlossaryItem>
  <GlossaryItem term="谓词">
    谓词必须由样例输入、结果集合和失败边界共同定义。
  </GlossaryItem>
  <GlossaryItem term="比较操作符">
    比较操作符必须由样例输入、结果集合和失败边界共同定义。
  </GlossaryItem>
  <GlossaryItem term="BETWEEN">
    BETWEEN必须由样例输入、结果集合和失败边界共同定义。
  </GlossaryItem>
  <GlossaryItem term="NULL">
    NULL必须由样例输入、结果集合和失败边界共同定义。
  </GlossaryItem>
</Glossary>

[← 上一页：第3课 排序检索数据](/learn/sql-ten-minutes/03-sorting-data/sqt-lesson03-sorting-data) · [下一页：第5课 高级数据过滤 →](/learn/sql-ten-minutes/05-advanced-filtering/sqt-lesson05-advanced-filtering)

<Attribution
  adaptedFrom="《SQL必知必会》第5版"
  adaptedUrl="https://www.96192.com/product/detail/761039"
/>
