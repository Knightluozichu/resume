---
title: "《统计学习方法（第2版）》权威学习地图"
type: B
section: "导读 · 两篇22章完整路线"
order: 0
description: "《统计学习方法（第2版）》权威学习地图覆盖29个权威目录层级，以模型-策略-算法、数学推导、交互实验和失败对照重建核心。"
demo: true
math: true
sourceUrl: "https://www.tup.tsinghua.edu.cn/bookscenter/book_08132901.html"
draft: false
---

import {
  SlmOfficialLearningMapMapLab,
  SlmOfficialLearningMapExperimentLab,
  SlmOfficialLearningMapEvidenceLab,
} from "@/components/mdx/statistical-learning-methods/diagrams/slm-official-learning-map";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释《统计学习方法（第2版）》权威学习地图中的模型、学习策略、优化算法与适用边界
- 能计算目标函数、充分统计、参数更新和独立评价中的关键量
- 能复现三个互动视图，并定位第一条概率、几何、矩阵或优化证据分叉
- 能判断“2篇、22章、256个编号节/小节和5个附录必须全部可导航；每种方法都有定义、目标、算法、中间证据、独立评价和失败边界。”是否成立

</Objectives>

## 为什么坚持模型、策略与算法三要素

<Term def="监督学习是本页第1个核查坐标，用于连接统计假设、目标、算法和泛化证据。">
  监督学习
</Term>
、
<Term def="无监督学习是本页第2个核查坐标，用于连接统计假设、目标、算法和泛化证据。">
  无监督学习
</Term>
、
<Term def="模型是本页第3个核查坐标，用于连接统计假设、目标、算法和泛化证据。">
  模型
</Term>
、
<Term def="策略是本页第4个核查坐标，用于连接统计假设、目标、算法和泛化证据。">
  策略
</Term>
、
<Term def="算法是本页第5个核查坐标，用于连接统计假设、目标、算法和泛化证据。">
  算法
</Term>
、
<Term def="独立评价是本页第6个核查坐标，用于连接统计假设、目标、算法和泛化证据。">
  独立评价
</Term>
共同构成本页坐标。沿官方两篇22章与附录A至E建立完整路线，以模型、策略、算法为统一坐标，贯通监督判别、序列模型、矩阵分解、潜在语义、采样、话题和图排序。

统计学习从有限样本出发。模型规定允许选择的函数、概率分布或潜在结构，策略规定什么叫更好，算法把策略转成有限计算。相同模型配不同损失会产生不同估计，相同策略用不同优化器可能只改变计算效率，也可能因近似、初始化和停止条件落入不同解。阅读每个方法时必须分开记录三者，不能用算法名称代替完整学习合同。

实验先冻结样本标识、训练验证测试折分、输入输出空间、预处理统计、特征顺序、随机种子、浮点精度和停止规则。训练阶段保存目标分解、充分统计、梯度或动态规划状态、更新前后参数；模型选择阶段保存所有候选和折级预测；测试集直到全部决定冻结后只打开一次。无监督任务则使用留出似然、稳定性、重构、外部标签或下游任务建立独立证据。

先预测单变量扰动会在哪里最早显现：样本表示、概率归一、几何间隔、矩阵谱、隐变量责任度、动态规划消息、采样接受率还是验证风险。随后在可枚举的小样本上运行一步并立即比较。若首个中间量已经错误，继续观察末端准确率或可视化没有诊断价值；修复后必须从原始输入重放，不能沿用污染的缓存和模型状态。

## 数学骨架与可核查量

经验风险聚合冻结训练样本上的损失：

$$
\widehat R(f)=\frac1N\sum_{i=1}^{N}L\bigl(y_i,f(x_i)\bigr).
$$

结构风险把复杂度或先验写进选择准则：

$$
\widehat R_{\lambda}(f)=\widehat R(f)+\lambda J(f).
$$

模型选择和最终测试严格隔离：

$$
\hat\lambda=\arg\min_{\lambda}\widehat R_{valid}(f_\lambda),\qquad \widehat R_{test}(f_{\hat\lambda})\text{ once}.
$$

本页专属可核查关系为：

$$
\operatorname{pass}=\bigwedge_{k=1}^{22}(M_k\land S_k\land A_k\land V_k\land F_k).
$$

全书通过要求22章逐章具备模型、策略、算法、验证和失效证据，不能用平均分掩盖缺章。 实现还要检查张量或矩阵形状、有限值、概率归一、正交或KKT残差、目标单调性、梯度方向和停止容差。

## 核心机制

1. 第一篇从监督学习概论进入分类、标注和潜变量方法，并以横向总结收束。
2. 第二篇从无监督学习概论进入聚类、矩阵分解、潜在语义、采样、话题和图排序。
3. 模型规定函数、概率分布、矩阵分解或图上的假设空间。
4. 策略通过风险、似然、间隔、重构、散度或平稳性定义优劣。
5. 算法包括梯度、对偶、动态规划、交替优化、采样和幂迭代。
6. 附录工具用于核对优化和矩阵概率前提，不是可跳过的公式清单。

同一个末端分数可能来自不同机制。训练风险下降可能来自容量、泄漏、额外预算或随机好运；概率归一不代表校准，数值收敛不代表统计假设成立，二维图形清楚也不代表表示对未见数据有用。对照实验保持数据、预处理、搜索预算和评价协议可比，只替换一个模型、策略或算法条件，并报告多折或多种子分布。

## 权威目录逐项讲解

### 第1篇 监督学习

复现“第1篇 监督学习”时固定初始化和访问顺序，保存每轮输入、充分统计、中间状态、目标差和停止残差，并与枚举或直接计算基线对照。

在“冻结样本与符号”固定样本身份、符号、形状、随机状态和适用条件，再进入“写出模型空间”只改变一个变量。记录训练目标、关键中间量、约束残差、更新前后状态和独立风险；若最终分数改善但理论预期的第一处变化没有发生，先排查泄漏、索引、尺度、符号和容差。

通过标准包含定义、推导、算法和反例四层：能够准确复述该目录项的对象与前提；能够在极小数据上手算关键量；能够固定种子复现一步状态转移；能够构造破坏前提的失败样本并指出停止位置。修复后清空派生特征、缓存、参数和评价，从原始样本完整重放。

### 第1章 统计学习及监督学习概论

复现“第1章 统计学习及监督学习概论”时固定初始化和访问顺序，保存每轮输入、充分统计、中间状态、目标差和停止残差，并与枚举或直接计算基线对照。

在“写出模型空间”固定样本身份、符号、形状、随机状态和适用条件，再进入“核对学习策略”只改变一个变量。记录训练目标、关键中间量、约束残差、更新前后状态和独立风险；若最终分数改善但理论预期的第一处变化没有发生，先排查泄漏、索引、尺度、符号和容差。

通过标准包含定义、推导、算法和反例四层：能够准确复述该目录项的对象与前提；能够在极小数据上手算关键量；能够固定种子复现一步状态转移；能够构造破坏前提的失败样本并指出停止位置。修复后清空派生特征、缓存、参数和评价，从原始样本完整重放。

### 第2章 感知机

学习“第2章 感知机”时将其放回“模型-策略-算法”框架，明确它改变哪一项、依赖哪些前提，以及第一条可观察证据应出现在哪里。

在“核对学习策略”固定样本身份、符号、形状、随机状态和适用条件，再进入“执行优化算法”只改变一个变量。记录训练目标、关键中间量、约束残差、更新前后状态和独立风险；若最终分数改善但理论预期的第一处变化没有发生，先排查泄漏、索引、尺度、符号和容差。

通过标准包含定义、推导、算法和反例四层：能够准确复述该目录项的对象与前提；能够在极小数据上手算关键量；能够固定种子复现一步状态转移；能够构造破坏前提的失败样本并指出停止位置。修复后清空派生特征、缓存、参数和评价，从原始样本完整重放。

### 第3章 k近邻法

学习“第3章 k近邻法”时将其放回“模型-策略-算法”框架，明确它改变哪一项、依赖哪些前提，以及第一条可观察证据应出现在哪里。

在“执行优化算法”固定样本身份、符号、形状、随机状态和适用条件，再进入“独立评价风险”只改变一个变量。记录训练目标、关键中间量、约束残差、更新前后状态和独立风险；若最终分数改善但理论预期的第一处变化没有发生，先排查泄漏、索引、尺度、符号和容差。

通过标准包含定义、推导、算法和反例四层：能够准确复述该目录项的对象与前提；能够在极小数据上手算关键量；能够固定种子复现一步状态转移；能够构造破坏前提的失败样本并指出停止位置。修复后清空派生特征、缓存、参数和评价，从原始样本完整重放。

### 第4章 朴素贝叶斯法

学习“第4章 朴素贝叶斯法”时将其放回“模型-策略-算法”框架，明确它改变哪一项、依赖哪些前提，以及第一条可观察证据应出现在哪里。

在“独立评价风险”固定样本身份、符号、形状、随机状态和适用条件，再进入“定位首个分叉”只改变一个变量。记录训练目标、关键中间量、约束残差、更新前后状态和独立风险；若最终分数改善但理论预期的第一处变化没有发生，先排查泄漏、索引、尺度、符号和容差。

通过标准包含定义、推导、算法和反例四层：能够准确复述该目录项的对象与前提；能够在极小数据上手算关键量；能够固定种子复现一步状态转移；能够构造破坏前提的失败样本并指出停止位置。修复后清空派生特征、缓存、参数和评价，从原始样本完整重放。

### 第5章 决策树

学习“第5章 决策树”时将其放回“模型-策略-算法”框架，明确它改变哪一项、依赖哪些前提，以及第一条可观察证据应出现在哪里。

在“定位首个分叉”固定样本身份、符号、形状、随机状态和适用条件，再进入“冻结样本与符号”只改变一个变量。记录训练目标、关键中间量、约束残差、更新前后状态和独立风险；若最终分数改善但理论预期的第一处变化没有发生，先排查泄漏、索引、尺度、符号和容差。

通过标准包含定义、推导、算法和反例四层：能够准确复述该目录项的对象与前提；能够在极小数据上手算关键量；能够固定种子复现一步状态转移；能够构造破坏前提的失败样本并指出停止位置。修复后清空派生特征、缓存、参数和评价，从原始样本完整重放。

### 第6章 逻辑斯谛回归与最大熵模型

先把“第6章 逻辑斯谛回归与最大熵模型”写成输入域、输出域、参数或潜变量、条件独立与不变性，给出一个满足定义的最小样本和一个只差一条前提的反例。

在“冻结样本与符号”固定样本身份、符号、形状、随机状态和适用条件，再进入“写出模型空间”只改变一个变量。记录训练目标、关键中间量、约束残差、更新前后状态和独立风险；若最终分数改善但理论预期的第一处变化没有发生，先排查泄漏、索引、尺度、符号和容差。

通过标准包含定义、推导、算法和反例四层：能够准确复述该目录项的对象与前提；能够在极小数据上手算关键量；能够固定种子复现一步状态转移；能够构造破坏前提的失败样本并指出停止位置。修复后清空派生特征、缓存、参数和评价，从原始样本完整重放。

### 第7章 支持向量机

学习“第7章 支持向量机”时将其放回“模型-策略-算法”框架，明确它改变哪一项、依赖哪些前提，以及第一条可观察证据应出现在哪里。

在“写出模型空间”固定样本身份、符号、形状、随机状态和适用条件，再进入“核对学习策略”只改变一个变量。记录训练目标、关键中间量、约束残差、更新前后状态和独立风险；若最终分数改善但理论预期的第一处变化没有发生，先排查泄漏、索引、尺度、符号和容差。

通过标准包含定义、推导、算法和反例四层：能够准确复述该目录项的对象与前提；能够在极小数据上手算关键量；能够固定种子复现一步状态转移；能够构造破坏前提的失败样本并指出停止位置。修复后清空派生特征、缓存、参数和评价，从原始样本完整重放。

### 第8章 提升方法

学习“第8章 提升方法”时将其放回“模型-策略-算法”框架，明确它改变哪一项、依赖哪些前提，以及第一条可观察证据应出现在哪里。

在“核对学习策略”固定样本身份、符号、形状、随机状态和适用条件，再进入“执行优化算法”只改变一个变量。记录训练目标、关键中间量、约束残差、更新前后状态和独立风险；若最终分数改善但理论预期的第一处变化没有发生，先排查泄漏、索引、尺度、符号和容差。

通过标准包含定义、推导、算法和反例四层：能够准确复述该目录项的对象与前提；能够在极小数据上手算关键量；能够固定种子复现一步状态转移；能够构造破坏前提的失败样本并指出停止位置。修复后清空派生特征、缓存、参数和评价，从原始样本完整重放。

### 第9章 EM算法及其推广

复现“第9章 EM算法及其推广”时固定初始化和访问顺序，保存每轮输入、充分统计、中间状态、目标差和停止残差，并与枚举或直接计算基线对照。

在“执行优化算法”固定样本身份、符号、形状、随机状态和适用条件，再进入“独立评价风险”只改变一个变量。记录训练目标、关键中间量、约束残差、更新前后状态和独立风险；若最终分数改善但理论预期的第一处变化没有发生，先排查泄漏、索引、尺度、符号和容差。

通过标准包含定义、推导、算法和反例四层：能够准确复述该目录项的对象与前提；能够在极小数据上手算关键量；能够固定种子复现一步状态转移；能够构造破坏前提的失败样本并指出停止位置。修复后清空派生特征、缓存、参数和评价，从原始样本完整重放。

### 第10章 隐马尔可夫模型

先把“第10章 隐马尔可夫模型”写成输入域、输出域、参数或潜变量、条件独立与不变性，给出一个满足定义的最小样本和一个只差一条前提的反例。

在“独立评价风险”固定样本身份、符号、形状、随机状态和适用条件，再进入“定位首个分叉”只改变一个变量。记录训练目标、关键中间量、约束残差、更新前后状态和独立风险；若最终分数改善但理论预期的第一处变化没有发生，先排查泄漏、索引、尺度、符号和容差。

通过标准包含定义、推导、算法和反例四层：能够准确复述该目录项的对象与前提；能够在极小数据上手算关键量；能够固定种子复现一步状态转移；能够构造破坏前提的失败样本并指出停止位置。修复后清空派生特征、缓存、参数和评价，从原始样本完整重放。

### 第11章 条件随机场

学习“第11章 条件随机场”时将其放回“模型-策略-算法”框架，明确它改变哪一项、依赖哪些前提，以及第一条可观察证据应出现在哪里。

在“定位首个分叉”固定样本身份、符号、形状、随机状态和适用条件，再进入“冻结样本与符号”只改变一个变量。记录训练目标、关键中间量、约束残差、更新前后状态和独立风险；若最终分数改善但理论预期的第一处变化没有发生，先排查泄漏、索引、尺度、符号和容差。

通过标准包含定义、推导、算法和反例四层：能够准确复述该目录项的对象与前提；能够在极小数据上手算关键量；能够固定种子复现一步状态转移；能够构造破坏前提的失败样本并指出停止位置。修复后清空派生特征、缓存、参数和评价，从原始样本完整重放。

### 第12章 监督学习方法总结

复现“第12章 监督学习方法总结”时固定初始化和访问顺序，保存每轮输入、充分统计、中间状态、目标差和停止残差，并与枚举或直接计算基线对照。

在“冻结样本与符号”固定样本身份、符号、形状、随机状态和适用条件，再进入“写出模型空间”只改变一个变量。记录训练目标、关键中间量、约束残差、更新前后状态和独立风险；若最终分数改善但理论预期的第一处变化没有发生，先排查泄漏、索引、尺度、符号和容差。

通过标准包含定义、推导、算法和反例四层：能够准确复述该目录项的对象与前提；能够在极小数据上手算关键量；能够固定种子复现一步状态转移；能够构造破坏前提的失败样本并指出停止位置。修复后清空派生特征、缓存、参数和评价，从原始样本完整重放。

### 第2篇 无监督学习

复现“第2篇 无监督学习”时固定初始化和访问顺序，保存每轮输入、充分统计、中间状态、目标差和停止残差，并与枚举或直接计算基线对照。

在“写出模型空间”固定样本身份、符号、形状、随机状态和适用条件，再进入“核对学习策略”只改变一个变量。记录训练目标、关键中间量、约束残差、更新前后状态和独立风险；若最终分数改善但理论预期的第一处变化没有发生，先排查泄漏、索引、尺度、符号和容差。

通过标准包含定义、推导、算法和反例四层：能够准确复述该目录项的对象与前提；能够在极小数据上手算关键量；能够固定种子复现一步状态转移；能够构造破坏前提的失败样本并指出停止位置。修复后清空派生特征、缓存、参数和评价，从原始样本完整重放。

### 第13章 无监督学习概论

复现“第13章 无监督学习概论”时固定初始化和访问顺序，保存每轮输入、充分统计、中间状态、目标差和停止残差，并与枚举或直接计算基线对照。

在“核对学习策略”固定样本身份、符号、形状、随机状态和适用条件，再进入“执行优化算法”只改变一个变量。记录训练目标、关键中间量、约束残差、更新前后状态和独立风险；若最终分数改善但理论预期的第一处变化没有发生，先排查泄漏、索引、尺度、符号和容差。

通过标准包含定义、推导、算法和反例四层：能够准确复述该目录项的对象与前提；能够在极小数据上手算关键量；能够固定种子复现一步状态转移；能够构造破坏前提的失败样本并指出停止位置。修复后清空派生特征、缓存、参数和评价，从原始样本完整重放。

### 第14章 聚类方法

学习“第14章 聚类方法”时将其放回“模型-策略-算法”框架，明确它改变哪一项、依赖哪些前提，以及第一条可观察证据应出现在哪里。

在“执行优化算法”固定样本身份、符号、形状、随机状态和适用条件，再进入“独立评价风险”只改变一个变量。记录训练目标、关键中间量、约束残差、更新前后状态和独立风险；若最终分数改善但理论预期的第一处变化没有发生，先排查泄漏、索引、尺度、符号和容差。

通过标准包含定义、推导、算法和反例四层：能够准确复述该目录项的对象与前提；能够在极小数据上手算关键量；能够固定种子复现一步状态转移；能够构造破坏前提的失败样本并指出停止位置。修复后清空派生特征、缓存、参数和评价，从原始样本完整重放。

### 第15章 奇异值分解

学习“第15章 奇异值分解”时将其放回“模型-策略-算法”框架，明确它改变哪一项、依赖哪些前提，以及第一条可观察证据应出现在哪里。

在“独立评价风险”固定样本身份、符号、形状、随机状态和适用条件，再进入“定位首个分叉”只改变一个变量。记录训练目标、关键中间量、约束残差、更新前后状态和独立风险；若最终分数改善但理论预期的第一处变化没有发生，先排查泄漏、索引、尺度、符号和容差。

通过标准包含定义、推导、算法和反例四层：能够准确复述该目录项的对象与前提；能够在极小数据上手算关键量；能够固定种子复现一步状态转移；能够构造破坏前提的失败样本并指出停止位置。修复后清空派生特征、缓存、参数和评价，从原始样本完整重放。

### 第16章 主成分分析

学习“第16章 主成分分析”时将其放回“模型-策略-算法”框架，明确它改变哪一项、依赖哪些前提，以及第一条可观察证据应出现在哪里。

在“定位首个分叉”固定样本身份、符号、形状、随机状态和适用条件，再进入“冻结样本与符号”只改变一个变量。记录训练目标、关键中间量、约束残差、更新前后状态和独立风险；若最终分数改善但理论预期的第一处变化没有发生，先排查泄漏、索引、尺度、符号和容差。

通过标准包含定义、推导、算法和反例四层：能够准确复述该目录项的对象与前提；能够在极小数据上手算关键量；能够固定种子复现一步状态转移；能够构造破坏前提的失败样本并指出停止位置。修复后清空派生特征、缓存、参数和评价，从原始样本完整重放。

### 第17章 潜在语义分析

学习“第17章 潜在语义分析”时将其放回“模型-策略-算法”框架，明确它改变哪一项、依赖哪些前提，以及第一条可观察证据应出现在哪里。

在“冻结样本与符号”固定样本身份、符号、形状、随机状态和适用条件，再进入“写出模型空间”只改变一个变量。记录训练目标、关键中间量、约束残差、更新前后状态和独立风险；若最终分数改善但理论预期的第一处变化没有发生，先排查泄漏、索引、尺度、符号和容差。

通过标准包含定义、推导、算法和反例四层：能够准确复述该目录项的对象与前提；能够在极小数据上手算关键量；能够固定种子复现一步状态转移；能够构造破坏前提的失败样本并指出停止位置。修复后清空派生特征、缓存、参数和评价，从原始样本完整重放。

### 第18章 概率潜在语义分析

对“第18章 概率潜在语义分析”逐项展开目标函数，标明求和范围、归一因子、约束和取值边界；手算极小数据并检查尺度变化后结论是否保持。

在“写出模型空间”固定样本身份、符号、形状、随机状态和适用条件，再进入“核对学习策略”只改变一个变量。记录训练目标、关键中间量、约束残差、更新前后状态和独立风险；若最终分数改善但理论预期的第一处变化没有发生，先排查泄漏、索引、尺度、符号和容差。

通过标准包含定义、推导、算法和反例四层：能够准确复述该目录项的对象与前提；能够在极小数据上手算关键量；能够固定种子复现一步状态转移；能够构造破坏前提的失败样本并指出停止位置。修复后清空派生特征、缓存、参数和评价，从原始样本完整重放。

### 第19章 马尔可夫链蒙特卡罗法

学习“第19章 马尔可夫链蒙特卡罗法”时将其放回“模型-策略-算法”框架，明确它改变哪一项、依赖哪些前提，以及第一条可观察证据应出现在哪里。

在“核对学习策略”固定样本身份、符号、形状、随机状态和适用条件，再进入“执行优化算法”只改变一个变量。记录训练目标、关键中间量、约束残差、更新前后状态和独立风险；若最终分数改善但理论预期的第一处变化没有发生，先排查泄漏、索引、尺度、符号和容差。

通过标准包含定义、推导、算法和反例四层：能够准确复述该目录项的对象与前提；能够在极小数据上手算关键量；能够固定种子复现一步状态转移；能够构造破坏前提的失败样本并指出停止位置。修复后清空派生特征、缓存、参数和评价，从原始样本完整重放。

### 第20章 潜在狄利克雷分配

学习“第20章 潜在狄利克雷分配”时将其放回“模型-策略-算法”框架，明确它改变哪一项、依赖哪些前提，以及第一条可观察证据应出现在哪里。

在“执行优化算法”固定样本身份、符号、形状、随机状态和适用条件，再进入“独立评价风险”只改变一个变量。记录训练目标、关键中间量、约束残差、更新前后状态和独立风险；若最终分数改善但理论预期的第一处变化没有发生，先排查泄漏、索引、尺度、符号和容差。

通过标准包含定义、推导、算法和反例四层：能够准确复述该目录项的对象与前提；能够在极小数据上手算关键量；能够固定种子复现一步状态转移；能够构造破坏前提的失败样本并指出停止位置。修复后清空派生特征、缓存、参数和评价，从原始样本完整重放。

### 第21章 PageRank算法

复现“第21章 PageRank算法”时固定初始化和访问顺序，保存每轮输入、充分统计、中间状态、目标差和停止残差，并与枚举或直接计算基线对照。

在“独立评价风险”固定样本身份、符号、形状、随机状态和适用条件，再进入“定位首个分叉”只改变一个变量。记录训练目标、关键中间量、约束残差、更新前后状态和独立风险；若最终分数改善但理论预期的第一处变化没有发生，先排查泄漏、索引、尺度、符号和容差。

通过标准包含定义、推导、算法和反例四层：能够准确复述该目录项的对象与前提；能够在极小数据上手算关键量；能够固定种子复现一步状态转移；能够构造破坏前提的失败样本并指出停止位置。修复后清空派生特征、缓存、参数和评价，从原始样本完整重放。

### 第22章 无监督学习方法总结

复现“第22章 无监督学习方法总结”时固定初始化和访问顺序，保存每轮输入、充分统计、中间状态、目标差和停止残差，并与枚举或直接计算基线对照。

在“定位首个分叉”固定样本身份、符号、形状、随机状态和适用条件，再进入“冻结样本与符号”只改变一个变量。记录训练目标、关键中间量、约束残差、更新前后状态和独立风险；若最终分数改善但理论预期的第一处变化没有发生，先排查泄漏、索引、尺度、符号和容差。

通过标准包含定义、推导、算法和反例四层：能够准确复述该目录项的对象与前提；能够在极小数据上手算关键量；能够固定种子复现一步状态转移；能够构造破坏前提的失败样本并指出停止位置。修复后清空派生特征、缓存、参数和评价，从原始样本完整重放。

### 附录 A 梯度下降法

复现“附录 A 梯度下降法”时固定初始化和访问顺序，保存每轮输入、充分统计、中间状态、目标差和停止残差，并与枚举或直接计算基线对照。

在“冻结样本与符号”固定样本身份、符号、形状、随机状态和适用条件，再进入“写出模型空间”只改变一个变量。记录训练目标、关键中间量、约束残差、更新前后状态和独立风险；若最终分数改善但理论预期的第一处变化没有发生，先排查泄漏、索引、尺度、符号和容差。

通过标准包含定义、推导、算法和反例四层：能够准确复述该目录项的对象与前提；能够在极小数据上手算关键量；能够固定种子复现一步状态转移；能够构造破坏前提的失败样本并指出停止位置。修复后清空派生特征、缓存、参数和评价，从原始样本完整重放。

### 附录 B 牛顿法和拟牛顿法

复现“附录 B 牛顿法和拟牛顿法”时固定初始化和访问顺序，保存每轮输入、充分统计、中间状态、目标差和停止残差，并与枚举或直接计算基线对照。

在“写出模型空间”固定样本身份、符号、形状、随机状态和适用条件，再进入“核对学习策略”只改变一个变量。记录训练目标、关键中间量、约束残差、更新前后状态和独立风险；若最终分数改善但理论预期的第一处变化没有发生，先排查泄漏、索引、尺度、符号和容差。

通过标准包含定义、推导、算法和反例四层：能够准确复述该目录项的对象与前提；能够在极小数据上手算关键量；能够固定种子复现一步状态转移；能够构造破坏前提的失败样本并指出停止位置。修复后清空派生特征、缓存、参数和评价，从原始样本完整重放。

### 附录 C 拉格朗日对偶性

验证“附录 C 拉格朗日对偶性”必须先列定理前提，再计算可检查残差或构造边界样本；前提不满足时只报告经验现象，不套用保证。

在“核对学习策略”固定样本身份、符号、形状、随机状态和适用条件，再进入“执行优化算法”只改变一个变量。记录训练目标、关键中间量、约束残差、更新前后状态和独立风险；若最终分数改善但理论预期的第一处变化没有发生，先排查泄漏、索引、尺度、符号和容差。

通过标准包含定义、推导、算法和反例四层：能够准确复述该目录项的对象与前提；能够在极小数据上手算关键量；能够固定种子复现一步状态转移；能够构造破坏前提的失败样本并指出停止位置。修复后清空派生特征、缓存、参数和评价，从原始样本完整重放。

### 附录 D 矩阵的基本子空间

先把“附录 D 矩阵的基本子空间”写成输入域、输出域、参数或潜变量、条件独立与不变性，给出一个满足定义的最小样本和一个只差一条前提的反例。

在“执行优化算法”固定样本身份、符号、形状、随机状态和适用条件，再进入“独立评价风险”只改变一个变量。记录训练目标、关键中间量、约束残差、更新前后状态和独立风险；若最终分数改善但理论预期的第一处变化没有发生，先排查泄漏、索引、尺度、符号和容差。

通过标准包含定义、推导、算法和反例四层：能够准确复述该目录项的对象与前提；能够在极小数据上手算关键量；能够固定种子复现一步状态转移；能够构造破坏前提的失败样本并指出停止位置。修复后清空派生特征、缓存、参数和评价，从原始样本完整重放。

### 附录 E KL散度的定义和狄利克雷分布的性质

先把“附录 E KL散度的定义和狄利克雷分布的性质”写成输入域、输出域、参数或潜变量、条件独立与不变性，给出一个满足定义的最小样本和一个只差一条前提的反例。

在“独立评价风险”固定样本身份、符号、形状、随机状态和适用条件，再进入“定位首个分叉”只改变一个变量。记录训练目标、关键中间量、约束残差、更新前后状态和独立风险；若最终分数改善但理论预期的第一处变化没有发生，先排查泄漏、索引、尺度、符号和容差。

通过标准包含定义、推导、算法和反例四层：能够准确复述该目录项的对象与前提；能够在极小数据上手算关键量；能够固定种子复现一步状态转移；能够构造破坏前提的失败样本并指出停止位置。修复后清空派生特征、缓存、参数和评价，从原始样本完整重放。

## 三步交互实验

先预测样本量、模型复杂度和噪声变化会如何影响训练风险、验证风险、计算迭代和约束残差，再切换基线、边界、失败与受控模式。

<Stepper>
  <Step title="画出模型-策略-算法地图">
    标出数据、模型空间、学习准则、优化或推断、独立评价与失败边界。
    <SlmOfficialLearningMapMapLab />
  </Step>
  <Step title="运行单变量实验">
    固定数据和预算，只改变一个复杂度、平滑、核、秩、步长、链长或阻尼参数，并预测第一处分叉。
    <SlmOfficialLearningMapExperimentLab />
  </Step>
  <Step title="核对独立证据">
    并排比较目标、中间状态、约束残差、训练风险与验证风险；首错出现后从原始样本重放。
    <SlmOfficialLearningMapEvidenceLab />
  </Step>
</Stepper>

## 常见失败与边界

<Callout type="trap">
  **把算法名称当作完整方法：**
  未写清假设空间、损失、正则、数据折分和停止条件时，同名算法可以产生不可比较的模型。必须按三要素恢复完整合同。
</Callout>

<Callout type="trap">
  **让测试信息进入训练或选择：**
  全数据拟合预处理、查看测试标签调阈值、选择秩或决定停止轮次都会使风险估计乐观。所有数据依赖决定必须嵌套在训练和验证内部。
</Callout>

<Callout type="tip">
  **最小复现顺序：**
  先枚举或手算极小样本，再核对程序中间量，最后扩大数据。任何非有限值、概率不归一、矩阵残差、KKT残差、数值梯度失败或测试泄漏都应立即停止。
</Callout>

## 本章回顾

沿官方两篇22章与附录A至E建立完整路线，以模型、策略、算法为统一坐标，贯通监督判别、序列模型、矩阵分解、潜在语义、采样、话题和图排序。 验收时必须从冻结样本预测中间状态与更新，解释模型、策略或算法条件为何改变答案，并在失败样本中指出第一处违背前提的位置。最终底线是：2篇、22章、256个编号节/小节和5个附录必须全部可导航；每种方法都有定义、目标、算法、中间证据、独立评价和失败边界。

<Glossary>

<GlossaryItem term="监督学习">
  “监督学习”对应“冻结样本与符号”：保存定义、数学前提、中间状态、数值容差、独立证据和失效边界，不能只保留最终分数。
</GlossaryItem>

<GlossaryItem term="无监督学习">
  “无监督学习”对应“写出模型空间”：保存定义、数学前提、中间状态、数值容差、独立证据和失效边界，不能只保留最终分数。
</GlossaryItem>

<GlossaryItem term="模型">
  “模型”对应“核对学习策略”：保存定义、数学前提、中间状态、数值容差、独立证据和失效边界，不能只保留最终分数。
</GlossaryItem>

<GlossaryItem term="策略">
  “策略”对应“执行优化算法”：保存定义、数学前提、中间状态、数值容差、独立证据和失效边界，不能只保留最终分数。
</GlossaryItem>

<GlossaryItem term="算法">
  “算法”对应“独立评价风险”：保存定义、数学前提、中间状态、数值容差、独立证据和失效边界，不能只保留最终分数。
</GlossaryItem>

<GlossaryItem term="独立评价">
  “独立评价”对应“定位首个分叉”：保存定义、数学前提、中间状态、数值容差、独立证据和失效边界，不能只保留最终分数。
</GlossaryItem>

</Glossary>

<Exercises>

1. 如何为《统计学习方法（第2版）》权威学习地图建立不泄漏测试信息的最小复现实验？

<Answer>
先冻结输入输出空间、样本身份和任务结构，再划分训练、验证与测试。预处理、特征、模型、超参数和停止规则只在训练与验证确定；保存“冻结样本与符号 -> 写出模型空间 -> 核对学习策略 -> 执行优化算法 -> 独立评价风险 -> 定位首个分叉”的中间证据，全部选择完成后测试集只使用一次。
</Answer>

2. 如何证明复杂方法相对简单基线的改进来自本章机制，而不是搜索预算或随机波动？

<Answer>
  保持数据折分、预处理、指标和资源预算可比，只替换一个模型、策略或算法条件。先写出根据方程应最先改变的中间量，再比较多折或多种子的验证分布；首个证据不符合机制时先修复实现，不采信末端分数。
</Answer>

3. 为“2篇、22章、256个编号节/小节和5个附录必须全部可导航；每种方法都有定义、目标、算法、中间证据、独立评价和失败边界。”构造一个失败样本，并说明应在何处停止。

<Answer>
  可以破坏数据独立性、概率归一、线性可分、正定性、条件独立、矩阵秩、链混合或测试隔离中的一项。运行前预测第一条被破坏的目标、状态或残差，在该处立即停止并保留原始证据；清空派生产物后从冻结输入重放。
</Answer>

</Exercises>

<Attribution
  adaptedFrom="李航著《统计学习方法（第2版）》，清华大学出版社，2019，官方版权目录标注484页，ISBN 9787302517276 · 基于清华大学出版社官方两篇22章、256个编号节/小节及5个附录完整目录的独立教学重构"
  adaptedUrl="https://www.tup.tsinghua.edu.cn/bookscenter/book_08132901.html"
/>
