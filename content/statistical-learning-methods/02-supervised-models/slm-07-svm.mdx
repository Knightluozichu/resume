---
title: 第7章 支持向量机
type: B
section: 第一篇 · 判别模型
order: 7
description: 第7章 支持向量机覆盖20个正式节点，以手算、章专属交互、前提反例和独立评价验收。
demo: true
math: true
sourceUrl: 'https://www.tup.tsinghua.edu.cn/bookscenter/book_08132901.html'
draft: false
qualityVersion: 2
practiceMode: calculation
sourceMode: independent-rewrite
---


{/* SLM_QUALITY_V2 */}

## 为什么从“间隔、核与KKT台”开始

第7章 支持向量机不能只靠术语和最终分数验收。间隔、核与KKT台先把数据与符号冻结，再把模型空间、学习目标、计算步骤和独立评价分开；这样任何异常都能回退到第一项错误中间量，而不是在准确率或可视化之后补故事。

先预测惩罚C从“适中”进入“大”时，间隔、核与KKT台命中率与核尺度偏差风险怎样变化。交互数值只表达公开的因果方向，不冒充真实训练结果。

## 来源、版次与独立重写边界

清华大学出版社[官方产品页](https://www.tup.tsinghua.edu.cn/bookscenter/book_08132901.html)确认李航《统计学习方法》第2版、2019年、ISBN 9787302517276和监督/无监督两篇结构；[官方完整目录](https://www.tup.tsinghua.edu.cn/bookscenter/bookcatalog?id=08132901)逐项核定22章、256个编号节/小节与附录A–E，共285个正式层级。第7章 支持向量机未取得出版正文授权，目录只界定范围，不宣称复现原书正文。

第7章 支持向量机的计算语义还与[scikit-learn官方用户指南](https://scikit-learn.org/stable/user_guide.html)、[NumPy SVD文档](https://numpy.org/doc/stable/reference/generated/numpy.linalg.svd.html)和[SciPy统计分布文档](https://docs.scipy.org/doc/scipy/reference/stats.html)中适用部分交叉核对；它们不替代本章推导，也不被误报为原书授权。中文解释、手算、图示、实验、反例与答案均为独立教学重写。

## 本章术语与数学合同

、、、、、。

本章不变量是“缩放、核、C和容差必须在训练与验证内确定；解同时满足原始可行、对偶可行、等式约束和互补松弛残差。”。数据身份、训练/验证/测试折分、预处理统计、特征顺序、随机种子、浮点精度与停止准则必须随实验保存；修复后从原始数据重算，不复用污染的缓存、责任度、矩阵分解、链状态或评价结果。

## 章专属数学骨架

$$
\min_{w,b,\xi}\frac12\lVert w\rVert^2+C\sum_i\xi_i,\quad y_i(w^Tx_i+b)\ge1-\xi_i.
$$

软间隔目标在大间隔和违例损失之间折中，C越大越强调训练违例而不代表泛化必然更好。 实现还要检查张量或矩阵形状、有限值、概率归一、正交或KKT残差、目标单调性、梯度方向和停止容差。

## 核心机制与适用边界

1. 硬间隔支持向量机适用于线性可分数据，规范化函数间隔后最小化权重范数等价于最大化几何间隔。
2. 对偶问题只依赖样本内积，非零乘子对应支持向量，KKT条件连接边界位置与乘子状态。
3. 软间隔通过松弛变量允许误分类和间隔内样本，并与合页损失加L2正则等价。
4. 核技巧用隐式特征空间内积替代显式映射，合法核对应半正定Gram矩阵。
5. 常用线性、多项式、高斯和字符串核表达不同相似性假设，核参数改变有效模型复杂度。
6. SMO每次优化两个乘子以保持等式约束，并通过违反KKT程度和启发式选择工作集。

## 官方目录逐项深读

### 第7章 支持向量机

**四级证据 1/20。** 第7章 支持向量机：把该节点放回模型—策略—算法链，说明输入、输出、假设和第一项可观察中间量。本节点固定核尺度，只改变惩罚C，用“缩放、核、C和容差必须在训练与验证内确定；解同时满足原始可行、对偶可行、等式约束和互补松弛残差。”判断计算是否仍在适用边界内；观察点为第7章 支持向量机。

对于第7章 支持向量机这个节点，先预测从“适中”切到“大”后，哪一个概率、距离、间隔、谱、目标或残差最先变化。运行极小样本后若方向不符，优先检查数据折分、尺度、索引、归一和符号，而不是用末端分数掩盖第一处错误。

### 7.1 线性可分支持向量机与硬间隔最大化

**四级证据 2/20。** 7.1 线性可分支持向量机与硬间隔最大化：列出定理前提，计算一项残差，并构造破坏前提后保证不再成立的反例。本节点固定核尺度，只改变惩罚C，用“缩放、核、C和容差必须在训练与验证内确定；解同时满足原始可行、对偶可行、等式约束和互补松弛残差。”判断计算是否仍在适用边界内；观察点为7.1 线性可分支持向量机与硬间隔最大化。

对于7.1 线性可分支持向量机与硬间隔最大化这个节点，先预测从“适中”切到“大”后，哪一个概率、距离、间隔、谱、目标或残差最先变化。运行极小样本后若方向不符，优先检查数据折分、尺度、索引、归一和符号，而不是用末端分数掩盖第一处错误。

### 7.1.1 线性可分支持向量机

**四级证据 3/20。** 7.1.1 线性可分支持向量机：把该节点放回模型—策略—算法链，说明输入、输出、假设和第一项可观察中间量。本节点固定核尺度，只改变惩罚C，用“缩放、核、C和容差必须在训练与验证内确定；解同时满足原始可行、对偶可行、等式约束和互补松弛残差。”判断计算是否仍在适用边界内；观察点为7.1.1 线性可分支持向量机。

对于7.1.1 线性可分支持向量机这个节点，先预测从“适中”切到“大”后，哪一个概率、距离、间隔、谱、目标或残差最先变化。运行极小样本后若方向不符，优先检查数据折分、尺度、索引、归一和符号，而不是用末端分数掩盖第一处错误。

### 7.1.2 函数间隔和几何间隔

**四级证据 4/20。** 7.1.2 函数间隔和几何间隔：列出定理前提，计算一项残差，并构造破坏前提后保证不再成立的反例。本节点固定核尺度，只改变惩罚C，用“缩放、核、C和容差必须在训练与验证内确定；解同时满足原始可行、对偶可行、等式约束和互补松弛残差。”判断计算是否仍在适用边界内；观察点为7.1.2 函数间隔和几何间隔。

对于7.1.2 函数间隔和几何间隔这个节点，先预测从“适中”切到“大”后，哪一个概率、距离、间隔、谱、目标或残差最先变化。运行极小样本后若方向不符，优先检查数据折分、尺度、索引、归一和符号，而不是用末端分数掩盖第一处错误。

### 7.1.3 间隔最大化

**四级证据 5/20。** 7.1.3 间隔最大化：列出定理前提，计算一项残差，并构造破坏前提后保证不再成立的反例。本节点固定核尺度，只改变惩罚C，用“缩放、核、C和容差必须在训练与验证内确定；解同时满足原始可行、对偶可行、等式约束和互补松弛残差。”判断计算是否仍在适用边界内；观察点为7.1.3 间隔最大化。

对于7.1.3 间隔最大化这个节点，先预测从“适中”切到“大”后，哪一个概率、距离、间隔、谱、目标或残差最先变化。运行极小样本后若方向不符，优先检查数据折分、尺度、索引、归一和符号，而不是用末端分数掩盖第一处错误。

### 7.1.4 学习的对偶算法

**四级证据 6/20。** 7.1.4 学习的对偶算法：在可枚举小样本上执行一步更新，保存输入、充分统计、参数前后值、目标差和停止残差。本节点固定核尺度，只改变惩罚C，用“缩放、核、C和容差必须在训练与验证内确定；解同时满足原始可行、对偶可行、等式约束和互补松弛残差。”判断计算是否仍在适用边界内；观察点为7.1.4 学习的对偶算法。

对于7.1.4 学习的对偶算法这个节点，先预测从“适中”切到“大”后，哪一个概率、距离、间隔、谱、目标或残差最先变化。运行极小样本后若方向不符，优先检查数据折分、尺度、索引、归一和符号，而不是用末端分数掩盖第一处错误。

### 7.2 线性支持向量机与软间隔最大化

**四级证据 7/20。** 7.2 线性支持向量机与软间隔最大化：列出定理前提，计算一项残差，并构造破坏前提后保证不再成立的反例。本节点固定核尺度，只改变惩罚C，用“缩放、核、C和容差必须在训练与验证内确定；解同时满足原始可行、对偶可行、等式约束和互补松弛残差。”判断计算是否仍在适用边界内；观察点为第7章 支持向量机。

对于7.2 线性支持向量机与软间隔最大化这个节点，先预测从“适中”切到“大”后，哪一个概率、距离、间隔、谱、目标或残差最先变化。运行极小样本后若方向不符，优先检查数据折分、尺度、索引、归一和符号，而不是用末端分数掩盖第一处错误。

### 7.2.1 线性支持向量机

**四级证据 8/20。** 7.2.1 线性支持向量机：把该节点放回模型—策略—算法链，说明输入、输出、假设和第一项可观察中间量。本节点固定核尺度，只改变惩罚C，用“缩放、核、C和容差必须在训练与验证内确定；解同时满足原始可行、对偶可行、等式约束和互补松弛残差。”判断计算是否仍在适用边界内；观察点为7.1 线性可分支持向量机与硬间隔最大化。

对于7.2.1 线性支持向量机这个节点，先预测从“适中”切到“大”后，哪一个概率、距离、间隔、谱、目标或残差最先变化。运行极小样本后若方向不符，优先检查数据折分、尺度、索引、归一和符号，而不是用末端分数掩盖第一处错误。

### 7.2.2 学习的对偶算法

**四级证据 9/20。** 7.2.2 学习的对偶算法：在可枚举小样本上执行一步更新，保存输入、充分统计、参数前后值、目标差和停止残差。本节点固定核尺度，只改变惩罚C，用“缩放、核、C和容差必须在训练与验证内确定；解同时满足原始可行、对偶可行、等式约束和互补松弛残差。”判断计算是否仍在适用边界内；观察点为7.1.1 线性可分支持向量机。

对于7.2.2 学习的对偶算法这个节点，先预测从“适中”切到“大”后，哪一个概率、距离、间隔、谱、目标或残差最先变化。运行极小样本后若方向不符，优先检查数据折分、尺度、索引、归一和符号，而不是用末端分数掩盖第一处错误。

### 7.2.3 支持向量

**四级证据 10/20。** 7.2.3 支持向量：把该节点放回模型—策略—算法链，说明输入、输出、假设和第一项可观察中间量。本节点固定核尺度，只改变惩罚C，用“缩放、核、C和容差必须在训练与验证内确定；解同时满足原始可行、对偶可行、等式约束和互补松弛残差。”判断计算是否仍在适用边界内；观察点为7.1.2 函数间隔和几何间隔。

对于7.2.3 支持向量这个节点，先预测从“适中”切到“大”后，哪一个概率、距离、间隔、谱、目标或残差最先变化。运行极小样本后若方向不符，优先检查数据折分、尺度、索引、归一和符号，而不是用末端分数掩盖第一处错误。

### 7.2.4 合页损失函数

**四级证据 11/20。** 7.2.4 合页损失函数：把该节点放回模型—策略—算法链，说明输入、输出、假设和第一项可观察中间量。本节点固定核尺度，只改变惩罚C，用“缩放、核、C和容差必须在训练与验证内确定；解同时满足原始可行、对偶可行、等式约束和互补松弛残差。”判断计算是否仍在适用边界内；观察点为7.1.3 间隔最大化。

对于7.2.4 合页损失函数这个节点，先预测从“适中”切到“大”后，哪一个概率、距离、间隔、谱、目标或残差最先变化。运行极小样本后若方向不符，优先检查数据折分、尺度、索引、归一和符号，而不是用末端分数掩盖第一处错误。

### 7.3 非线性支持向量机与核函数

**四级证据 12/20。** 7.3 非线性支持向量机与核函数：把该节点放回模型—策略—算法链，说明输入、输出、假设和第一项可观察中间量。本节点固定核尺度，只改变惩罚C，用“缩放、核、C和容差必须在训练与验证内确定；解同时满足原始可行、对偶可行、等式约束和互补松弛残差。”判断计算是否仍在适用边界内；观察点为7.1.4 学习的对偶算法。

对于7.3 非线性支持向量机与核函数这个节点，先预测从“适中”切到“大”后，哪一个概率、距离、间隔、谱、目标或残差最先变化。运行极小样本后若方向不符，优先检查数据折分、尺度、索引、归一和符号，而不是用末端分数掩盖第一处错误。

### 7.3.1 核技巧

**四级证据 13/20。** 7.3.1 核技巧：把该节点放回模型—策略—算法链，说明输入、输出、假设和第一项可观察中间量。本节点固定核尺度，只改变惩罚C，用“缩放、核、C和容差必须在训练与验证内确定；解同时满足原始可行、对偶可行、等式约束和互补松弛残差。”判断计算是否仍在适用边界内；观察点为第7章 支持向量机。

对于7.3.1 核技巧这个节点，先预测从“适中”切到“大”后，哪一个概率、距离、间隔、谱、目标或残差最先变化。运行极小样本后若方向不符，优先检查数据折分、尺度、索引、归一和符号，而不是用末端分数掩盖第一处错误。

### 7.3.2 正定核

**四级证据 14/20。** 7.3.2 正定核：列出定理前提，计算一项残差，并构造破坏前提后保证不再成立的反例。本节点固定核尺度，只改变惩罚C，用“缩放、核、C和容差必须在训练与验证内确定；解同时满足原始可行、对偶可行、等式约束和互补松弛残差。”判断计算是否仍在适用边界内；观察点为7.1 线性可分支持向量机与硬间隔最大化。

对于7.3.2 正定核这个节点，先预测从“适中”切到“大”后，哪一个概率、距离、间隔、谱、目标或残差最先变化。运行极小样本后若方向不符，优先检查数据折分、尺度、索引、归一和符号，而不是用末端分数掩盖第一处错误。

### 7.3.3 常用核函数

**四级证据 15/20。** 7.3.3 常用核函数：把该节点放回模型—策略—算法链，说明输入、输出、假设和第一项可观察中间量。本节点固定核尺度，只改变惩罚C，用“缩放、核、C和容差必须在训练与验证内确定；解同时满足原始可行、对偶可行、等式约束和互补松弛残差。”判断计算是否仍在适用边界内；观察点为7.1.1 线性可分支持向量机。

对于7.3.3 常用核函数这个节点，先预测从“适中”切到“大”后，哪一个概率、距离、间隔、谱、目标或残差最先变化。运行极小样本后若方向不符，优先检查数据折分、尺度、索引、归一和符号，而不是用末端分数掩盖第一处错误。

### 7.3.4 非线性支持向量分类机

**四级证据 16/20。** 7.3.4 非线性支持向量分类机：固定数据、基线、损失和错误代价，区分训练目标改善与独立任务收益。本节点固定核尺度，只改变惩罚C，用“缩放、核、C和容差必须在训练与验证内确定；解同时满足原始可行、对偶可行、等式约束和互补松弛残差。”判断计算是否仍在适用边界内；观察点为7.1.2 函数间隔和几何间隔。

对于7.3.4 非线性支持向量分类机这个节点，先预测从“适中”切到“大”后，哪一个概率、距离、间隔、谱、目标或残差最先变化。运行极小样本后若方向不符，优先检查数据折分、尺度、索引、归一和符号，而不是用末端分数掩盖第一处错误。

### 7.4 序列最小最优化算法

**四级证据 17/20。** 7.4 序列最小最优化算法：在可枚举小样本上执行一步更新，保存输入、充分统计、参数前后值、目标差和停止残差。本节点固定核尺度，只改变惩罚C，用“缩放、核、C和容差必须在训练与验证内确定；解同时满足原始可行、对偶可行、等式约束和互补松弛残差。”判断计算是否仍在适用边界内；观察点为7.1.3 间隔最大化。

对于7.4 序列最小最优化算法这个节点，先预测从“适中”切到“大”后，哪一个概率、距离、间隔、谱、目标或残差最先变化。运行极小样本后若方向不符，优先检查数据折分、尺度、索引、归一和符号，而不是用末端分数掩盖第一处错误。

### 7.4.1 两个变量二次规划的求解方法

**四级证据 18/20。** 7.4.1 两个变量二次规划的求解方法：把该节点放回模型—策略—算法链，说明输入、输出、假设和第一项可观察中间量。本节点固定核尺度，只改变惩罚C，用“缩放、核、C和容差必须在训练与验证内确定；解同时满足原始可行、对偶可行、等式约束和互补松弛残差。”判断计算是否仍在适用边界内；观察点为7.1.4 学习的对偶算法。

对于7.4.1 两个变量二次规划的求解方法这个节点，先预测从“适中”切到“大”后，哪一个概率、距离、间隔、谱、目标或残差最先变化。运行极小样本后若方向不符，优先检查数据折分、尺度、索引、归一和符号，而不是用末端分数掩盖第一处错误。

### 7.4.2 变量的选择方法

**四级证据 19/20。** 7.4.2 变量的选择方法：把该节点放回模型—策略—算法链，说明输入、输出、假设和第一项可观察中间量。本节点固定核尺度，只改变惩罚C，用“缩放、核、C和容差必须在训练与验证内确定；解同时满足原始可行、对偶可行、等式约束和互补松弛残差。”判断计算是否仍在适用边界内；观察点为第7章 支持向量机。

对于7.4.2 变量的选择方法这个节点，先预测从“适中”切到“大”后，哪一个概率、距离、间隔、谱、目标或残差最先变化。运行极小样本后若方向不符，优先检查数据折分、尺度、索引、归一和符号，而不是用末端分数掩盖第一处错误。

### 7.4.3 SMO算法

**四级证据 20/20。** 7.4.3 SMO算法：在可枚举小样本上执行一步更新，保存输入、充分统计、参数前后值、目标差和停止残差。本节点固定核尺度，只改变惩罚C，用“缩放、核、C和容差必须在训练与验证内确定；解同时满足原始可行、对偶可行、等式约束和互补松弛残差。”判断计算是否仍在适用边界内；观察点为7.1 线性可分支持向量机与硬间隔最大化。

对于7.4.3 SMO算法这个节点，先预测从“适中”切到“大”后，哪一个概率、距离、间隔、谱、目标或残差最先变化。运行极小样本后若方向不符，优先检查数据折分、尺度、索引、归一和符号，而不是用末端分数掩盖第一处错误。

## 三个必须主动触发的误区


## 练习、答案与285节点验证


