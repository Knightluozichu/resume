---
title: 第10章 隐马尔可夫模型
type: B
section: 第一篇 · 潜变量与序列
order: 10
description: 第10章 隐马尔可夫模型覆盖17个正式节点，以手算、章专属交互、前提反例和独立评价验收。
demo: true
math: true
sourceUrl: 'https://www.tup.tsinghua.edu.cn/bookscenter/book_08132901.html'
draft: false
qualityVersion: 2
practiceMode: calculation
sourceMode: independent-rewrite
---
import { Slm10HmmMapLab, Slm10HmmExperimentLab, Slm10HmmEvidenceLab } from "@/components/mdx/statistical-learning-methods/diagrams/slm-10-hmm";
import { Objectives, Callout, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step, Attribution } from "@/components/mdx/mdx-components";

<Objectives>

- 能区分第10章 隐马尔可夫模型的模型对象、学习策略、算法步骤与成立前提
- 能在极小数据上手算目标、概率、距离、矩阵、动态规划或采样中的关键量
- 能操作HMM三问题动态规划台，一次只改变序列长度或计算域
- 能注入“长序列直接连乘下溢，或前向、后向和维特比递推索引错一位”，清空派生状态后重算并证明“状态与观测索引、概率归一、缩放因子和终止约定必须一致；总概率、后验边缘和最优路径是不同对象。”

</Objectives>

{/* SLM_QUALITY_V2 */}

## 为什么从“HMM三问题动态规划台”开始

第10章 隐马尔可夫模型不能只靠术语和最终分数验收。HMM三问题动态规划台先把数据与符号冻结，再把模型空间、学习目标、计算步骤和独立评价分开；这样任何异常都能回退到第一项错误中间量，而不是在准确率或可视化之后补故事。

先预测序列长度从“中”进入“长”时，HMM三问题动态规划台命中率与计算域偏差风险怎样变化。交互数值只表达公开的因果方向，不冒充真实训练结果。

## 来源、版次与独立重写边界

清华大学出版社[官方产品页](https://www.tup.tsinghua.edu.cn/bookscenter/book_08132901.html)确认李航《统计学习方法》第2版、2019年、ISBN 9787302517276和监督/无监督两篇结构；[官方完整目录](https://www.tup.tsinghua.edu.cn/bookscenter/bookcatalog?id=08132901)逐项核定22章、256个编号节/小节与附录A–E，共285个正式层级。第10章 隐马尔可夫模型未取得出版正文授权，目录只界定范围，不宣称复现原书正文。

第10章 隐马尔可夫模型的计算语义还与[scikit-learn官方用户指南](https://scikit-learn.org/stable/user_guide.html)、[NumPy SVD文档](https://numpy.org/doc/stable/reference/generated/numpy.linalg.svd.html)和[SciPy统计分布文档](https://docs.scipy.org/doc/scipy/reference/stats.html)中适用部分交叉核对；它们不替代本章推导，也不被误报为原书授权。中文解释、手算、图示、实验、反例与答案均为独立教学重写。

## 本章术语与数学合同

<Term def="第10章 隐马尔可夫模型是第10章 隐马尔可夫模型中连接冻结数据与符号与独立评价的数学坐标，必须写明对象、尺度、前提和残差。">第10章 隐马尔可夫模型</Term>、<Term def="10.1 隐马尔可夫模型的基本概念是第10章 隐马尔可夫模型中连接写出模型与独立评价的数学坐标，必须写明对象、尺度、前提和残差。">10.1 隐马尔可夫模型的基本概念</Term>、<Term def="10.1.1 隐马尔可夫模型的定义是第10章 隐马尔可夫模型中连接核对目标与独立评价的数学坐标，必须写明对象、尺度、前提和残差。">10.1.1 隐马尔可夫模型的定义</Term>、<Term def="10.1.2 观测序列的生成过程是第10章 隐马尔可夫模型中连接执行一步算法与独立评价的数学坐标，必须写明对象、尺度、前提和残差。">10.1.2 观测序列的生成过程</Term>、<Term def="10.1.3 隐马尔可夫模型的 3个基本问题是第10章 隐马尔可夫模型中连接独立评价与反例与独立评价的数学坐标，必须写明对象、尺度、前提和残差。">10.1.3 隐马尔可夫模型的 3个基本问题</Term>、<Term def="10.2 概率计算算法是第10章 隐马尔可夫模型中连接冻结数据与符号与独立评价的数学坐标，必须写明对象、尺度、前提和残差。">10.2 概率计算算法</Term>。

本章不变量是“状态与观测索引、概率归一、缩放因子和终止约定必须一致；总概率、后验边缘和最优路径是不同对象。”。数据身份、训练/验证/测试折分、预处理统计、特征顺序、随机种子、浮点精度与停止准则必须随实验保存；修复后从原始数据重算，不复用污染的缓存、责任度、矩阵分解、链状态或评价结果。

## 章专属数学骨架

$$
\alpha_{t+1}(j)=\left[\sum_i\alpha_t(i)a_{ij}\right]b_j(o_{t+1}).
$$

前向递推汇总所有到达状态j的历史路径概率，再乘当前观测似然，将指数枚举降为动态规划。 实现还要检查张量或矩阵形状、有限值、概率归一、正交或KKT残差、目标单调性、梯度方向和停止容差。

## 核心机制与适用边界

1. HMM假设隐藏状态满足一阶马尔可夫性，观测在给定当前状态后条件独立。
2. 生成过程先采样初始状态，再交替采样观测与下一状态，三个参数矩阵都应归一。
3. 前向与后向算法计算观测概率和状态后验，缩放或对数域用于避免长序列下溢。
4. 监督学习可用已知状态序列计数，未知状态时Baum-Welch即HMM专用EM。
5. Baum-Welch使用单状态和相邻状态后验期望更新初始、转移与发射概率。
6. Viterbi保留每个状态的最佳前驱并回溯单条最优路径，不等于逐时刻最大后验标签。

## 先预测，再操作三层章专属实验

<Stepper>
  <Step title="1. 模型—策略—算法地图">选择正式节点，写出对象、目标、算法和前提。<Slm10HmmMapLab /></Step>
  <Step title="2. 极小样本单变量计算">固定折分与随机状态，只改变序列长度或计算域。<Slm10HmmExperimentLab /></Step>
  <Step title="3. 前提破坏、修复与复位">注入“长序列直接连乘下溢，或前向、后向和维特比递推索引错一位”，定位首错，清空派生状态后同输入重算。<Slm10HmmEvidenceLab /></Step>
</Stepper>

## 官方目录逐项深读

### 第10章 隐马尔可夫模型

**四级证据 1/17。** 第10章 隐马尔可夫模型：写出随机变量、空间、参数、归一条件与可识别性，不用算法名称替代数学对象。本节点固定计算域，只改变序列长度，用“状态与观测索引、概率归一、缩放因子和终止约定必须一致；总概率、后验边缘和最优路径是不同对象。”判断计算是否仍在适用边界内；观察点为第10章 隐马尔可夫模型。

对于第10章 隐马尔可夫模型这个节点，先预测从“中”切到“长”后，哪一个概率、距离、间隔、谱、目标或残差最先变化。运行极小样本后若方向不符，优先检查数据折分、尺度、索引、归一和符号，而不是用末端分数掩盖第一处错误。

### 10.1 隐马尔可夫模型的基本概念

**四级证据 2/17。** 10.1 隐马尔可夫模型的基本概念：写出随机变量、空间、参数、归一条件与可识别性，不用算法名称替代数学对象。本节点固定计算域，只改变序列长度，用“状态与观测索引、概率归一、缩放因子和终止约定必须一致；总概率、后验边缘和最优路径是不同对象。”判断计算是否仍在适用边界内；观察点为10.1 隐马尔可夫模型的基本概念。

对于10.1 隐马尔可夫模型的基本概念这个节点，先预测从“中”切到“长”后，哪一个概率、距离、间隔、谱、目标或残差最先变化。运行极小样本后若方向不符，优先检查数据折分、尺度、索引、归一和符号，而不是用末端分数掩盖第一处错误。

### 10.1.1 隐马尔可夫模型的定义

**四级证据 3/17。** 10.1.1 隐马尔可夫模型的定义：写出随机变量、空间、参数、归一条件与可识别性，不用算法名称替代数学对象。本节点固定计算域，只改变序列长度，用“状态与观测索引、概率归一、缩放因子和终止约定必须一致；总概率、后验边缘和最优路径是不同对象。”判断计算是否仍在适用边界内；观察点为10.1.1 隐马尔可夫模型的定义。

对于10.1.1 隐马尔可夫模型的定义这个节点，先预测从“中”切到“长”后，哪一个概率、距离、间隔、谱、目标或残差最先变化。运行极小样本后若方向不符，优先检查数据折分、尺度、索引、归一和符号，而不是用末端分数掩盖第一处错误。

### 10.1.2 观测序列的生成过程

**四级证据 4/17。** 10.1.2 观测序列的生成过程：在可枚举小样本上执行一步更新，保存输入、充分统计、参数前后值、目标差和停止残差。本节点固定计算域，只改变序列长度，用“状态与观测索引、概率归一、缩放因子和终止约定必须一致；总概率、后验边缘和最优路径是不同对象。”判断计算是否仍在适用边界内；观察点为10.1.2 观测序列的生成过程。

对于10.1.2 观测序列的生成过程这个节点，先预测从“中”切到“长”后，哪一个概率、距离、间隔、谱、目标或残差最先变化。运行极小样本后若方向不符，优先检查数据折分、尺度、索引、归一和符号，而不是用末端分数掩盖第一处错误。

### 10.1.3 隐马尔可夫模型的 3个基本问题

**四级证据 5/17。** 10.1.3 隐马尔可夫模型的 3个基本问题：写出随机变量、空间、参数、归一条件与可识别性，不用算法名称替代数学对象。本节点固定计算域，只改变序列长度，用“状态与观测索引、概率归一、缩放因子和终止约定必须一致；总概率、后验边缘和最优路径是不同对象。”判断计算是否仍在适用边界内；观察点为10.1.3 隐马尔可夫模型的 3个基本问题。

对于10.1.3 隐马尔可夫模型的 3个基本问题这个节点，先预测从“中”切到“长”后，哪一个概率、距离、间隔、谱、目标或残差最先变化。运行极小样本后若方向不符，优先检查数据折分、尺度、索引、归一和符号，而不是用末端分数掩盖第一处错误。

### 10.2 概率计算算法

**四级证据 6/17。** 10.2 概率计算算法：在可枚举小样本上执行一步更新，保存输入、充分统计、参数前后值、目标差和停止残差。本节点固定计算域，只改变序列长度，用“状态与观测索引、概率归一、缩放因子和终止约定必须一致；总概率、后验边缘和最优路径是不同对象。”判断计算是否仍在适用边界内；观察点为10.2 概率计算算法。

对于10.2 概率计算算法这个节点，先预测从“中”切到“长”后，哪一个概率、距离、间隔、谱、目标或残差最先变化。运行极小样本后若方向不符，优先检查数据折分、尺度、索引、归一和符号，而不是用末端分数掩盖第一处错误。

### 10.2.1 直接计算法

**四级证据 7/17。** 10.2.1 直接计算法：在可枚举小样本上执行一步更新，保存输入、充分统计、参数前后值、目标差和停止残差。本节点固定计算域，只改变序列长度，用“状态与观测索引、概率归一、缩放因子和终止约定必须一致；总概率、后验边缘和最优路径是不同对象。”判断计算是否仍在适用边界内；观察点为第10章 隐马尔可夫模型。

对于10.2.1 直接计算法这个节点，先预测从“中”切到“长”后，哪一个概率、距离、间隔、谱、目标或残差最先变化。运行极小样本后若方向不符，优先检查数据折分、尺度、索引、归一和符号，而不是用末端分数掩盖第一处错误。

### 10.2.2 前向算法

**四级证据 8/17。** 10.2.2 前向算法：在可枚举小样本上执行一步更新，保存输入、充分统计、参数前后值、目标差和停止残差。本节点固定计算域，只改变序列长度，用“状态与观测索引、概率归一、缩放因子和终止约定必须一致；总概率、后验边缘和最优路径是不同对象。”判断计算是否仍在适用边界内；观察点为10.1 隐马尔可夫模型的基本概念。

对于10.2.2 前向算法这个节点，先预测从“中”切到“长”后，哪一个概率、距离、间隔、谱、目标或残差最先变化。运行极小样本后若方向不符，优先检查数据折分、尺度、索引、归一和符号，而不是用末端分数掩盖第一处错误。

### 10.2.3 后向算法

**四级证据 9/17。** 10.2.3 后向算法：在可枚举小样本上执行一步更新，保存输入、充分统计、参数前后值、目标差和停止残差。本节点固定计算域，只改变序列长度，用“状态与观测索引、概率归一、缩放因子和终止约定必须一致；总概率、后验边缘和最优路径是不同对象。”判断计算是否仍在适用边界内；观察点为10.1.1 隐马尔可夫模型的定义。

对于10.2.3 后向算法这个节点，先预测从“中”切到“长”后，哪一个概率、距离、间隔、谱、目标或残差最先变化。运行极小样本后若方向不符，优先检查数据折分、尺度、索引、归一和符号，而不是用末端分数掩盖第一处错误。

### 10.2.4 一些概率与期望值的计算

**四级证据 10/17。** 10.2.4 一些概率与期望值的计算：在可枚举小样本上执行一步更新，保存输入、充分统计、参数前后值、目标差和停止残差。本节点固定计算域，只改变序列长度，用“状态与观测索引、概率归一、缩放因子和终止约定必须一致；总概率、后验边缘和最优路径是不同对象。”判断计算是否仍在适用边界内；观察点为10.1.2 观测序列的生成过程。

对于10.2.4 一些概率与期望值的计算这个节点，先预测从“中”切到“长”后，哪一个概率、距离、间隔、谱、目标或残差最先变化。运行极小样本后若方向不符，优先检查数据折分、尺度、索引、归一和符号，而不是用末端分数掩盖第一处错误。

### 10.3 学习算法

**四级证据 11/17。** 10.3 学习算法：在可枚举小样本上执行一步更新，保存输入、充分统计、参数前后值、目标差和停止残差。本节点固定计算域，只改变序列长度，用“状态与观测索引、概率归一、缩放因子和终止约定必须一致；总概率、后验边缘和最优路径是不同对象。”判断计算是否仍在适用边界内；观察点为10.1.3 隐马尔可夫模型的 3个基本问题。

对于10.3 学习算法这个节点，先预测从“中”切到“长”后，哪一个概率、距离、间隔、谱、目标或残差最先变化。运行极小样本后若方向不符，优先检查数据折分、尺度、索引、归一和符号，而不是用末端分数掩盖第一处错误。

### 10.3.1 监督学习方法

**四级证据 12/17。** 10.3.1 监督学习方法：在可枚举小样本上执行一步更新，保存输入、充分统计、参数前后值、目标差和停止残差。本节点固定计算域，只改变序列长度，用“状态与观测索引、概率归一、缩放因子和终止约定必须一致；总概率、后验边缘和最优路径是不同对象。”判断计算是否仍在适用边界内；观察点为10.2 概率计算算法。

对于10.3.1 监督学习方法这个节点，先预测从“中”切到“长”后，哪一个概率、距离、间隔、谱、目标或残差最先变化。运行极小样本后若方向不符，优先检查数据折分、尺度、索引、归一和符号，而不是用末端分数掩盖第一处错误。

### 10.3.2 Baum-Welch算法

**四级证据 13/17。** 10.3.2 Baum-Welch算法：在可枚举小样本上执行一步更新，保存输入、充分统计、参数前后值、目标差和停止残差。本节点固定计算域，只改变序列长度，用“状态与观测索引、概率归一、缩放因子和终止约定必须一致；总概率、后验边缘和最优路径是不同对象。”判断计算是否仍在适用边界内；观察点为第10章 隐马尔可夫模型。

对于10.3.2 Baum-Welch算法这个节点，先预测从“中”切到“长”后，哪一个概率、距离、间隔、谱、目标或残差最先变化。运行极小样本后若方向不符，优先检查数据折分、尺度、索引、归一和符号，而不是用末端分数掩盖第一处错误。

### 10.3.3 Baum-Welch模型参数估计公式

**四级证据 14/17。** 10.3.3 Baum-Welch模型参数估计公式：写出随机变量、空间、参数、归一条件与可识别性，不用算法名称替代数学对象。本节点固定计算域，只改变序列长度，用“状态与观测索引、概率归一、缩放因子和终止约定必须一致；总概率、后验边缘和最优路径是不同对象。”判断计算是否仍在适用边界内；观察点为10.1 隐马尔可夫模型的基本概念。

对于10.3.3 Baum-Welch模型参数估计公式这个节点，先预测从“中”切到“长”后，哪一个概率、距离、间隔、谱、目标或残差最先变化。运行极小样本后若方向不符，优先检查数据折分、尺度、索引、归一和符号，而不是用末端分数掩盖第一处错误。

### 10.4 预测算法

**四级证据 15/17。** 10.4 预测算法：在可枚举小样本上执行一步更新，保存输入、充分统计、参数前后值、目标差和停止残差。本节点固定计算域，只改变序列长度，用“状态与观测索引、概率归一、缩放因子和终止约定必须一致；总概率、后验边缘和最优路径是不同对象。”判断计算是否仍在适用边界内；观察点为10.1.1 隐马尔可夫模型的定义。

对于10.4 预测算法这个节点，先预测从“中”切到“长”后，哪一个概率、距离、间隔、谱、目标或残差最先变化。运行极小样本后若方向不符，优先检查数据折分、尺度、索引、归一和符号，而不是用末端分数掩盖第一处错误。

### 10.4.1 近似算法

**四级证据 16/17。** 10.4.1 近似算法：在可枚举小样本上执行一步更新，保存输入、充分统计、参数前后值、目标差和停止残差。本节点固定计算域，只改变序列长度，用“状态与观测索引、概率归一、缩放因子和终止约定必须一致；总概率、后验边缘和最优路径是不同对象。”判断计算是否仍在适用边界内；观察点为10.1.2 观测序列的生成过程。

对于10.4.1 近似算法这个节点，先预测从“中”切到“长”后，哪一个概率、距离、间隔、谱、目标或残差最先变化。运行极小样本后若方向不符，优先检查数据折分、尺度、索引、归一和符号，而不是用末端分数掩盖第一处错误。

### 10.4.2 维特比算法

**四级证据 17/17。** 10.4.2 维特比算法：在可枚举小样本上执行一步更新，保存输入、充分统计、参数前后值、目标差和停止残差。本节点固定计算域，只改变序列长度，用“状态与观测索引、概率归一、缩放因子和终止约定必须一致；总概率、后验边缘和最优路径是不同对象。”判断计算是否仍在适用边界内；观察点为10.1.3 隐马尔可夫模型的 3个基本问题。

对于10.4.2 维特比算法这个节点，先预测从“中”切到“长”后，哪一个概率、距离、间隔、谱、目标或残差最先变化。运行极小样本后若方向不符，优先检查数据折分、尺度、索引、归一和符号，而不是用末端分数掩盖第一处错误。

## 三个必须主动触发的误区

<Callout type="trap" title="训练目标不等于应用质量">第10章 隐马尔可夫模型的训练风险、似然、重构或固定点残差只证明优化对象；泛化、稳定性、校准或下游收益必须用冻结的独立协议评价。</Callout>

<Callout type="trap" title="前提被数据处理破坏">长序列直接连乘下溢，或前向、后向和维特比递推索引错一位。任何跨折统计、标签泄漏、尺度错位、索引错位或随机状态变化都会让末端分数失去可比性。</Callout>

<Callout type="trap" title="数值收敛不等于统计正确">第10章 隐马尔可夫模型即使目标停止变化仍要检查概率归一、有限值、矩阵形状、约束、梯度/KKT/固定点残差与多起点稳定性。</Callout>

## 练习、答案与285节点验证

<Exercises>

**问题1：单变量。** 如何隔离序列长度的因果影响？

<Answer>固定数据身份、折分、预处理、随机种子、预算和计算域，只切换“中”到“长”，同时保存第一项中间量、目标差、残差和独立风险。</Answer>

**问题2：四级证据。** 怎样证明本页17个节点都被真正覆盖？

<Answer>
1. 第10章 隐马尔可夫模型：写出定义或公式，完成一轮手算，操作章专属实验，并用第10章 隐马尔可夫模型断言结果。
2. 10.1 隐马尔可夫模型的基本概念：写出定义或公式，完成一轮手算，操作章专属实验，并用10.1 隐马尔可夫模型的基本概念断言结果。
3. 10.1.1 隐马尔可夫模型的定义：写出定义或公式，完成一轮手算，操作章专属实验，并用10.1.1 隐马尔可夫模型的定义断言结果。
4. 10.1.2 观测序列的生成过程：写出定义或公式，完成一轮手算，操作章专属实验，并用10.1.2 观测序列的生成过程断言结果。
5. 10.1.3 隐马尔可夫模型的 3个基本问题：写出定义或公式，完成一轮手算，操作章专属实验，并用10.1.3 隐马尔可夫模型的 3个基本问题断言结果。
6. 10.2 概率计算算法：写出定义或公式，完成一轮手算，操作章专属实验，并用10.2 概率计算算法断言结果。
7. 10.2.1 直接计算法：写出定义或公式，完成一轮手算，操作章专属实验，并用第10章 隐马尔可夫模型断言结果。
8. 10.2.2 前向算法：写出定义或公式，完成一轮手算，操作章专属实验，并用10.1 隐马尔可夫模型的基本概念断言结果。
9. 10.2.3 后向算法：写出定义或公式，完成一轮手算，操作章专属实验，并用10.1.1 隐马尔可夫模型的定义断言结果。
10. 10.2.4 一些概率与期望值的计算：写出定义或公式，完成一轮手算，操作章专属实验，并用10.1.2 观测序列的生成过程断言结果。
11. 10.3 学习算法：写出定义或公式，完成一轮手算，操作章专属实验，并用10.1.3 隐马尔可夫模型的 3个基本问题断言结果。
12. 10.3.1 监督学习方法：写出定义或公式，完成一轮手算，操作章专属实验，并用10.2 概率计算算法断言结果。
13. 10.3.2 Baum-Welch算法：写出定义或公式，完成一轮手算，操作章专属实验，并用第10章 隐马尔可夫模型断言结果。
14. 10.3.3 Baum-Welch模型参数估计公式：写出定义或公式，完成一轮手算，操作章专属实验，并用10.1 隐马尔可夫模型的基本概念断言结果。
15. 10.4 预测算法：写出定义或公式，完成一轮手算，操作章专属实验，并用10.1.1 隐马尔可夫模型的定义断言结果。
16. 10.4.1 近似算法：写出定义或公式，完成一轮手算，操作章专属实验，并用10.1.2 观测序列的生成过程断言结果。
17. 10.4.2 维特比算法：写出定义或公式，完成一轮手算，操作章专属实验，并用10.1.3 隐马尔可夫模型的 3个基本问题断言结果。
</Answer>

**问题3：恢复闭环。** 如何证明失败样本已修复？

<Answer>沿冻结数据与符号、写出模型、核对目标、执行一步算法、独立评价与反例定位第一处偏离，只改最小因果条件；丢弃所有派生状态，以同数据同种子重算基线、边界和恢复轨迹，直到“状态与观测索引、概率归一、缩放因子和终止约定必须一致；总概率、后验边缘和最优路径是不同对象。”重新成立。</Answer>

</Exercises>

<Glossary>
  <GlossaryItem term="第10章 隐马尔可夫模型">第10章 隐马尔可夫模型是第10章 隐马尔可夫模型中连接冻结数据与符号与独立评价的数学坐标，必须写明对象、尺度、前提和残差。</GlossaryItem>
  <GlossaryItem term="10.1 隐马尔可夫模型的基本概念">10.1 隐马尔可夫模型的基本概念是第10章 隐马尔可夫模型中连接写出模型与独立评价的数学坐标，必须写明对象、尺度、前提和残差。</GlossaryItem>
  <GlossaryItem term="10.1.1 隐马尔可夫模型的定义">10.1.1 隐马尔可夫模型的定义是第10章 隐马尔可夫模型中连接核对目标与独立评价的数学坐标，必须写明对象、尺度、前提和残差。</GlossaryItem>
  <GlossaryItem term="10.1.2 观测序列的生成过程">10.1.2 观测序列的生成过程是第10章 隐马尔可夫模型中连接执行一步算法与独立评价的数学坐标，必须写明对象、尺度、前提和残差。</GlossaryItem>
  <GlossaryItem term="10.1.3 隐马尔可夫模型的 3个基本问题">10.1.3 隐马尔可夫模型的 3个基本问题是第10章 隐马尔可夫模型中连接独立评价与反例与独立评价的数学坐标，必须写明对象、尺度、前提和残差。</GlossaryItem>
  <GlossaryItem term="10.2 概率计算算法">10.2 概率计算算法是第10章 隐马尔可夫模型中连接冻结数据与符号与独立评价的数学坐标，必须写明对象、尺度、前提和残差。</GlossaryItem>
</Glossary>

<Attribution mode="independent-rewrite" sourceBasis="outline-only" workTitle="李航《统计学习方法（第2版）》" adaptedUrl="https://www.tup.tsinghua.edu.cn/bookscenter/book_08132901.html" />
