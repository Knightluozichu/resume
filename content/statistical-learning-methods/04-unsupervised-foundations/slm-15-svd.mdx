---
title: 第15章 奇异值分解
type: B
section: 第二篇 · 无监督基础
order: 15
description: 第15章 奇异值分解覆盖11个正式节点，以手算、章专属交互、前提反例和独立评价验收。
demo: true
math: true
sourceUrl: 'https://www.tup.tsinghua.edu.cn/bookscenter/book_08132901.html'
draft: false
qualityVersion: 2
practiceMode: calculation
sourceMode: independent-rewrite
---


{/* SLM_QUALITY_V2 */}

## 为什么从“SVD谱与低秩近似台”开始

第15章 奇异值分解不能只靠术语和最终分数验收。SVD谱与低秩近似台先把数据与符号冻结，再把模型空间、学习目标、计算步骤和独立评价分开；这样任何异常都能回退到第一项错误中间量，而不是在准确率或可视化之后补故事。

先预测截断秩k从“拐点”进入“满秩”时，SVD谱与低秩近似台命中率与矩阵条件偏差风险怎样变化。交互数值只表达公开的因果方向，不冒充真实训练结果。

## 来源、版次与独立重写边界

清华大学出版社[官方产品页](https://www.tup.tsinghua.edu.cn/bookscenter/book_08132901.html)确认李航《统计学习方法》第2版、2019年、ISBN 9787302517276和监督/无监督两篇结构；[官方完整目录](https://www.tup.tsinghua.edu.cn/bookscenter/bookcatalog?id=08132901)逐项核定22章、256个编号节/小节与附录A–E，共285个正式层级。第15章 奇异值分解未取得出版正文授权，目录只界定范围，不宣称复现原书正文。

第15章 奇异值分解的计算语义还与[scikit-learn官方用户指南](https://scikit-learn.org/stable/user_guide.html)、[NumPy SVD文档](https://numpy.org/doc/stable/reference/generated/numpy.linalg.svd.html)和[SciPy统计分布文档](https://docs.scipy.org/doc/scipy/reference/stats.html)中适用部分交叉核对；它们不替代本章推导，也不被误报为原书授权。中文解释、手算、图示、实验、反例与答案均为独立教学重写。

## 本章术语与数学合同

、、、、、。

本章不变量是“矩阵形状、奇异值排序、正交性和重构残差必须核对；重复奇异值下奇异向量不唯一，符号翻转不构成错误。”。数据身份、训练/验证/测试折分、预处理统计、特征顺序、随机种子、浮点精度与停止准则必须随实验保存；修复后从原始数据重算，不复用污染的缓存、责任度、矩阵分解、链状态或评价结果。

## 章专属数学骨架

$$
A=U\Sigma V^T=\sum_{i=1}^{r}\sigma_i u_i v_i^T.
$$

SVD把任意矩阵分解为左右正交方向和非负尺度，并以秩一外积之和表示。 实现还要检查张量或矩阵形状、有限值、概率归一、正交或KKT残差、目标单调性、梯度方向和停止容差。

## 核心机制与适用边界

1. SVD适用于任意矩形矩阵，左右奇异向量分别是AA转置与A转置A的特征向量。
2. 紧SVD只保留非零奇异值，截断SVD只保留前k项以压缩和去噪。
3. 几何上矩阵变换由旋转或反射、沿正交轴缩放、再次旋转组成。
4. 数值计算通常经双对角化和迭代完成，不应通过显式平方矩阵放大条件数。
5. Frobenius范数等于奇异值平方和的平方根，便于量化重构误差。
6. Eckart-Young定理说明截断SVD在2范数或Frobenius范数下给出最佳秩k近似。

## 官方目录逐项深读

### 第15章 奇异值分解

**四级证据 1/11。** 第15章 奇异值分解：把该节点放回模型—策略—算法链，说明输入、输出、假设和第一项可观察中间量。本节点固定矩阵条件，只改变截断秩k，用“矩阵形状、奇异值排序、正交性和重构残差必须核对；重复奇异值下奇异向量不唯一，符号翻转不构成错误。”判断计算是否仍在适用边界内；观察点为第15章 奇异值分解。

对于第15章 奇异值分解这个节点，先预测从“拐点”切到“满秩”后，哪一个概率、距离、间隔、谱、目标或残差最先变化。运行极小样本后若方向不符，优先检查数据折分、尺度、索引、归一和符号，而不是用末端分数掩盖第一处错误。

### 15.1 奇异值分解的定义与性质

**四级证据 2/11。** 15.1 奇异值分解的定义与性质：写出随机变量、空间、参数、归一条件与可识别性，不用算法名称替代数学对象。本节点固定矩阵条件，只改变截断秩k，用“矩阵形状、奇异值排序、正交性和重构残差必须核对；重复奇异值下奇异向量不唯一，符号翻转不构成错误。”判断计算是否仍在适用边界内；观察点为15.1 奇异值分解的定义与性质。

对于15.1 奇异值分解的定义与性质这个节点，先预测从“拐点”切到“满秩”后，哪一个概率、距离、间隔、谱、目标或残差最先变化。运行极小样本后若方向不符，优先检查数据折分、尺度、索引、归一和符号，而不是用末端分数掩盖第一处错误。

### 15.1.1 定义与定理

**四级证据 3/11。** 15.1.1 定义与定理：写出随机变量、空间、参数、归一条件与可识别性，不用算法名称替代数学对象。本节点固定矩阵条件，只改变截断秩k，用“矩阵形状、奇异值排序、正交性和重构残差必须核对；重复奇异值下奇异向量不唯一，符号翻转不构成错误。”判断计算是否仍在适用边界内；观察点为15.1.1 定义与定理。

对于15.1.1 定义与定理这个节点，先预测从“拐点”切到“满秩”后，哪一个概率、距离、间隔、谱、目标或残差最先变化。运行极小样本后若方向不符，优先检查数据折分、尺度、索引、归一和符号，而不是用末端分数掩盖第一处错误。

### 15.1.2 紧奇异值分解与截断奇异值分解

**四级证据 4/11。** 15.1.2 紧奇异值分解与截断奇异值分解：把该节点放回模型—策略—算法链，说明输入、输出、假设和第一项可观察中间量。本节点固定矩阵条件，只改变截断秩k，用“矩阵形状、奇异值排序、正交性和重构残差必须核对；重复奇异值下奇异向量不唯一，符号翻转不构成错误。”判断计算是否仍在适用边界内；观察点为15.1.2 紧奇异值分解与截断奇异值分解。

对于15.1.2 紧奇异值分解与截断奇异值分解这个节点，先预测从“拐点”切到“满秩”后，哪一个概率、距离、间隔、谱、目标或残差最先变化。运行极小样本后若方向不符，优先检查数据折分、尺度、索引、归一和符号，而不是用末端分数掩盖第一处错误。

### 15.1.3 几何解释

**四级证据 5/11。** 15.1.3 几何解释：把该节点放回模型—策略—算法链，说明输入、输出、假设和第一项可观察中间量。本节点固定矩阵条件，只改变截断秩k，用“矩阵形状、奇异值排序、正交性和重构残差必须核对；重复奇异值下奇异向量不唯一，符号翻转不构成错误。”判断计算是否仍在适用边界内；观察点为15.1.3 几何解释。

对于15.1.3 几何解释这个节点，先预测从“拐点”切到“满秩”后，哪一个概率、距离、间隔、谱、目标或残差最先变化。运行极小样本后若方向不符，优先检查数据折分、尺度、索引、归一和符号，而不是用末端分数掩盖第一处错误。

### 15.1.4 主要性质

**四级证据 6/11。** 15.1.4 主要性质：列出定理前提，计算一项残差，并构造破坏前提后保证不再成立的反例。本节点固定矩阵条件，只改变截断秩k，用“矩阵形状、奇异值排序、正交性和重构残差必须核对；重复奇异值下奇异向量不唯一，符号翻转不构成错误。”判断计算是否仍在适用边界内；观察点为15.1.4 主要性质。

对于15.1.4 主要性质这个节点，先预测从“拐点”切到“满秩”后，哪一个概率、距离、间隔、谱、目标或残差最先变化。运行极小样本后若方向不符，优先检查数据折分、尺度、索引、归一和符号，而不是用末端分数掩盖第一处错误。

### 15.2 奇异值分解的计算

**四级证据 7/11。** 15.2 奇异值分解的计算：在可枚举小样本上执行一步更新，保存输入、充分统计、参数前后值、目标差和停止残差。本节点固定矩阵条件，只改变截断秩k，用“矩阵形状、奇异值排序、正交性和重构残差必须核对；重复奇异值下奇异向量不唯一，符号翻转不构成错误。”判断计算是否仍在适用边界内；观察点为第15章 奇异值分解。

对于15.2 奇异值分解的计算这个节点，先预测从“拐点”切到“满秩”后，哪一个概率、距离、间隔、谱、目标或残差最先变化。运行极小样本后若方向不符，优先检查数据折分、尺度、索引、归一和符号，而不是用末端分数掩盖第一处错误。

### 15.3 奇异值分解与矩阵近似

**四级证据 8/11。** 15.3 奇异值分解与矩阵近似：把该节点放回模型—策略—算法链，说明输入、输出、假设和第一项可观察中间量。本节点固定矩阵条件，只改变截断秩k，用“矩阵形状、奇异值排序、正交性和重构残差必须核对；重复奇异值下奇异向量不唯一，符号翻转不构成错误。”判断计算是否仍在适用边界内；观察点为15.1 奇异值分解的定义与性质。

对于15.3 奇异值分解与矩阵近似这个节点，先预测从“拐点”切到“满秩”后，哪一个概率、距离、间隔、谱、目标或残差最先变化。运行极小样本后若方向不符，优先检查数据折分、尺度、索引、归一和符号，而不是用末端分数掩盖第一处错误。

### 15.3.1 弗罗贝尼乌斯范数

**四级证据 9/11。** 15.3.1 弗罗贝尼乌斯范数：把该节点放回模型—策略—算法链，说明输入、输出、假设和第一项可观察中间量。本节点固定矩阵条件，只改变截断秩k，用“矩阵形状、奇异值排序、正交性和重构残差必须核对；重复奇异值下奇异向量不唯一，符号翻转不构成错误。”判断计算是否仍在适用边界内；观察点为15.1.1 定义与定理。

对于15.3.1 弗罗贝尼乌斯范数这个节点，先预测从“拐点”切到“满秩”后，哪一个概率、距离、间隔、谱、目标或残差最先变化。运行极小样本后若方向不符，优先检查数据折分、尺度、索引、归一和符号，而不是用末端分数掩盖第一处错误。

### 15.3.2 矩阵的最优近似

**四级证据 10/11。** 15.3.2 矩阵的最优近似：把该节点放回模型—策略—算法链，说明输入、输出、假设和第一项可观察中间量。本节点固定矩阵条件，只改变截断秩k，用“矩阵形状、奇异值排序、正交性和重构残差必须核对；重复奇异值下奇异向量不唯一，符号翻转不构成错误。”判断计算是否仍在适用边界内；观察点为15.1.2 紧奇异值分解与截断奇异值分解。

对于15.3.2 矩阵的最优近似这个节点，先预测从“拐点”切到“满秩”后，哪一个概率、距离、间隔、谱、目标或残差最先变化。运行极小样本后若方向不符，优先检查数据折分、尺度、索引、归一和符号，而不是用末端分数掩盖第一处错误。

### 15.3.3 矩阵的外积展开式

**四级证据 11/11。** 15.3.3 矩阵的外积展开式：把该节点放回模型—策略—算法链，说明输入、输出、假设和第一项可观察中间量。本节点固定矩阵条件，只改变截断秩k，用“矩阵形状、奇异值排序、正交性和重构残差必须核对；重复奇异值下奇异向量不唯一，符号翻转不构成错误。”判断计算是否仍在适用边界内；观察点为15.1.3 几何解释。

对于15.3.3 矩阵的外积展开式这个节点，先预测从“拐点”切到“满秩”后，哪一个概率、距离、间隔、谱、目标或残差最先变化。运行极小样本后若方向不符，优先检查数据折分、尺度、索引、归一和符号，而不是用末端分数掩盖第一处错误。

## 三个必须主动触发的误区


## 练习、答案与285节点验证


