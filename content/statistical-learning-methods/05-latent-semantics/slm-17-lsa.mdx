---
title: 第17章 潜在语义分析
type: B
section: 第二篇 · 潜在语义
order: 17
description: 第17章 潜在语义分析覆盖12个正式节点，以手算、章专属交互、前提反例和独立评价验收。
demo: true
math: true
sourceUrl: 'https://www.tup.tsinghua.edu.cn/bookscenter/book_08132901.html'
draft: false
qualityVersion: 2
practiceMode: calculation
sourceMode: independent-rewrite
---


{/* SLM_QUALITY_V2 */}

## 为什么从“词项矩阵与潜在空间台”开始

第17章 潜在语义分析不能只靠术语和最终分数验收。词项矩阵与潜在空间台先把数据与符号冻结，再把模型空间、学习目标、计算步骤和独立评价分开；这样任何异常都能回退到第一项错误中间量，而不是在准确率或可视化之后补故事。

先预测潜在秩从“适中”进入“高”时，词项矩阵与潜在空间台命中率与词项权重偏差风险怎样变化。交互数值只表达公开的因果方向，不冒充真实训练结果。

## 来源、版次与独立重写边界

清华大学出版社[官方产品页](https://www.tup.tsinghua.edu.cn/bookscenter/book_08132901.html)确认李航《统计学习方法》第2版、2019年、ISBN 9787302517276和监督/无监督两篇结构；[官方完整目录](https://www.tup.tsinghua.edu.cn/bookscenter/bookcatalog?id=08132901)逐项核定22章、256个编号节/小节与附录A–E，共285个正式层级。第17章 潜在语义分析未取得出版正文授权，目录只界定范围，不宣称复现原书正文。

第17章 潜在语义分析的计算语义还与[scikit-learn官方用户指南](https://scikit-learn.org/stable/user_guide.html)、[NumPy SVD文档](https://numpy.org/doc/stable/reference/generated/numpy.linalg.svd.html)和[SciPy统计分布文档](https://docs.scipy.org/doc/scipy/reference/stats.html)中适用部分交叉核对；它们不替代本章推导，也不被误报为原书授权。中文解释、手算、图示、实验、反例与答案均为独立教学重写。

## 本章术语与数学合同

、、、、、。

本章不变量是“词表、计数或权重、截断秩和归一化只在训练语料确定；SVD符号与NMF置换不影响模型，话题解释需跨种子稳定。”。数据身份、训练/验证/测试折分、预处理统计、特征顺序、随机种子、浮点精度与停止准则必须随实验保存；修复后从原始数据重算，不复用污染的缓存、责任度、矩阵分解、链状态或评价结果。

## 章专属数学骨架

$$
X\approx U_k\Sigma_kV_k^T.
$$

LSA用词项-文档矩阵的最佳低秩近似把词和文档映射到共享潜在话题空间。 实现还要检查张量或矩阵形状、有限值、概率归一、正交或KKT残差、目标单调性、梯度方向和停止容差。

## 核心机制与适用边界

1. 单词向量空间用词项维度表示文档，稀疏高维且同义词与多义词造成表面失配。
2. 话题向量空间用较少潜在维度表示共现结构，使相似文档可通过隐含概念接近。
3. 截断SVD同时给出词-话题和文档-话题坐标，奇异向量符号可整体翻转。
4. 秩过低会合并语义，秩过高会保留噪声，必须用留出重构或检索任务选择。
5. NMF要求两个因子非负，产生加性部件表示，通常更便于话题词解释。
6. 乘法更新保持非负并使目标不增，但受初始化和尺度置换歧义影响。

## 官方目录逐项深读

### 第17章 潜在语义分析

**四级证据 1/12。** 第17章 潜在语义分析：把该节点放回模型—策略—算法链，说明输入、输出、假设和第一项可观察中间量。本节点固定词项权重，只改变潜在秩，用“词表、计数或权重、截断秩和归一化只在训练语料确定；SVD符号与NMF置换不影响模型，话题解释需跨种子稳定。”判断计算是否仍在适用边界内；观察点为第17章 潜在语义分析。

对于第17章 潜在语义分析这个节点，先预测从“适中”切到“高”后，哪一个概率、距离、间隔、谱、目标或残差最先变化。运行极小样本后若方向不符，优先检查数据折分、尺度、索引、归一和符号，而不是用末端分数掩盖第一处错误。

### 17.1 单词向量空间与话题向量空间

**四级证据 2/12。** 17.1 单词向量空间与话题向量空间：把该节点放回模型—策略—算法链，说明输入、输出、假设和第一项可观察中间量。本节点固定词项权重，只改变潜在秩，用“词表、计数或权重、截断秩和归一化只在训练语料确定；SVD符号与NMF置换不影响模型，话题解释需跨种子稳定。”判断计算是否仍在适用边界内；观察点为17.1 单词向量空间与话题向量空间。

对于17.1 单词向量空间与话题向量空间这个节点，先预测从“适中”切到“高”后，哪一个概率、距离、间隔、谱、目标或残差最先变化。运行极小样本后若方向不符，优先检查数据折分、尺度、索引、归一和符号，而不是用末端分数掩盖第一处错误。

### 17.1.1 单词向量空间

**四级证据 3/12。** 17.1.1 单词向量空间：把该节点放回模型—策略—算法链，说明输入、输出、假设和第一项可观察中间量。本节点固定词项权重，只改变潜在秩，用“词表、计数或权重、截断秩和归一化只在训练语料确定；SVD符号与NMF置换不影响模型，话题解释需跨种子稳定。”判断计算是否仍在适用边界内；观察点为17.1.1 单词向量空间。

对于17.1.1 单词向量空间这个节点，先预测从“适中”切到“高”后，哪一个概率、距离、间隔、谱、目标或残差最先变化。运行极小样本后若方向不符，优先检查数据折分、尺度、索引、归一和符号，而不是用末端分数掩盖第一处错误。

### 17.1.2 话题向量空间

**四级证据 4/12。** 17.1.2 话题向量空间：把该节点放回模型—策略—算法链，说明输入、输出、假设和第一项可观察中间量。本节点固定词项权重，只改变潜在秩，用“词表、计数或权重、截断秩和归一化只在训练语料确定；SVD符号与NMF置换不影响模型，话题解释需跨种子稳定。”判断计算是否仍在适用边界内；观察点为17.1.2 话题向量空间。

对于17.1.2 话题向量空间这个节点，先预测从“适中”切到“高”后，哪一个概率、距离、间隔、谱、目标或残差最先变化。运行极小样本后若方向不符，优先检查数据折分、尺度、索引、归一和符号，而不是用末端分数掩盖第一处错误。

### 17.2 潜在语义分析算法

**四级证据 5/12。** 17.2 潜在语义分析算法：在可枚举小样本上执行一步更新，保存输入、充分统计、参数前后值、目标差和停止残差。本节点固定词项权重，只改变潜在秩，用“词表、计数或权重、截断秩和归一化只在训练语料确定；SVD符号与NMF置换不影响模型，话题解释需跨种子稳定。”判断计算是否仍在适用边界内；观察点为17.2 潜在语义分析算法。

对于17.2 潜在语义分析算法这个节点，先预测从“适中”切到“高”后，哪一个概率、距离、间隔、谱、目标或残差最先变化。运行极小样本后若方向不符，优先检查数据折分、尺度、索引、归一和符号，而不是用末端分数掩盖第一处错误。

### 17.2.1 矩阵奇异值分解算法

**四级证据 6/12。** 17.2.1 矩阵奇异值分解算法：在可枚举小样本上执行一步更新，保存输入、充分统计、参数前后值、目标差和停止残差。本节点固定词项权重，只改变潜在秩，用“词表、计数或权重、截断秩和归一化只在训练语料确定；SVD符号与NMF置换不影响模型，话题解释需跨种子稳定。”判断计算是否仍在适用边界内；观察点为17.2.1 矩阵奇异值分解算法。

对于17.2.1 矩阵奇异值分解算法这个节点，先预测从“适中”切到“高”后，哪一个概率、距离、间隔、谱、目标或残差最先变化。运行极小样本后若方向不符，优先检查数据折分、尺度、索引、归一和符号，而不是用末端分数掩盖第一处错误。

### 17.2.2 例子

**四级证据 7/12。** 17.2.2 例子：固定数据、基线、损失和错误代价，区分训练目标改善与独立任务收益。本节点固定词项权重，只改变潜在秩，用“词表、计数或权重、截断秩和归一化只在训练语料确定；SVD符号与NMF置换不影响模型，话题解释需跨种子稳定。”判断计算是否仍在适用边界内；观察点为第17章 潜在语义分析。

对于17.2.2 例子这个节点，先预测从“适中”切到“高”后，哪一个概率、距离、间隔、谱、目标或残差最先变化。运行极小样本后若方向不符，优先检查数据折分、尺度、索引、归一和符号，而不是用末端分数掩盖第一处错误。

### 17.3 非负矩阵分解算法

**四级证据 8/12。** 17.3 非负矩阵分解算法：在可枚举小样本上执行一步更新，保存输入、充分统计、参数前后值、目标差和停止残差。本节点固定词项权重，只改变潜在秩，用“词表、计数或权重、截断秩和归一化只在训练语料确定；SVD符号与NMF置换不影响模型，话题解释需跨种子稳定。”判断计算是否仍在适用边界内；观察点为17.1 单词向量空间与话题向量空间。

对于17.3 非负矩阵分解算法这个节点，先预测从“适中”切到“高”后，哪一个概率、距离、间隔、谱、目标或残差最先变化。运行极小样本后若方向不符，优先检查数据折分、尺度、索引、归一和符号，而不是用末端分数掩盖第一处错误。

### 17.3.1 非负矩阵分解

**四级证据 9/12。** 17.3.1 非负矩阵分解：把该节点放回模型—策略—算法链，说明输入、输出、假设和第一项可观察中间量。本节点固定词项权重，只改变潜在秩，用“词表、计数或权重、截断秩和归一化只在训练语料确定；SVD符号与NMF置换不影响模型，话题解释需跨种子稳定。”判断计算是否仍在适用边界内；观察点为17.1.1 单词向量空间。

对于17.3.1 非负矩阵分解这个节点，先预测从“适中”切到“高”后，哪一个概率、距离、间隔、谱、目标或残差最先变化。运行极小样本后若方向不符，优先检查数据折分、尺度、索引、归一和符号，而不是用末端分数掩盖第一处错误。

### 17.3.2 潜在语义分析模型

**四级证据 10/12。** 17.3.2 潜在语义分析模型：写出随机变量、空间、参数、归一条件与可识别性，不用算法名称替代数学对象。本节点固定词项权重，只改变潜在秩，用“词表、计数或权重、截断秩和归一化只在训练语料确定；SVD符号与NMF置换不影响模型，话题解释需跨种子稳定。”判断计算是否仍在适用边界内；观察点为17.1.2 话题向量空间。

对于17.3.2 潜在语义分析模型这个节点，先预测从“适中”切到“高”后，哪一个概率、距离、间隔、谱、目标或残差最先变化。运行极小样本后若方向不符，优先检查数据折分、尺度、索引、归一和符号，而不是用末端分数掩盖第一处错误。

### 17.3.3 非负矩阵分解的形式化

**四级证据 11/12。** 17.3.3 非负矩阵分解的形式化：把该节点放回模型—策略—算法链，说明输入、输出、假设和第一项可观察中间量。本节点固定词项权重，只改变潜在秩，用“词表、计数或权重、截断秩和归一化只在训练语料确定；SVD符号与NMF置换不影响模型，话题解释需跨种子稳定。”判断计算是否仍在适用边界内；观察点为17.2 潜在语义分析算法。

对于17.3.3 非负矩阵分解的形式化这个节点，先预测从“适中”切到“高”后，哪一个概率、距离、间隔、谱、目标或残差最先变化。运行极小样本后若方向不符，优先检查数据折分、尺度、索引、归一和符号，而不是用末端分数掩盖第一处错误。

### 17.3.4 算法

**四级证据 12/12。** 17.3.4 算法：在可枚举小样本上执行一步更新，保存输入、充分统计、参数前后值、目标差和停止残差。本节点固定词项权重，只改变潜在秩，用“词表、计数或权重、截断秩和归一化只在训练语料确定；SVD符号与NMF置换不影响模型，话题解释需跨种子稳定。”判断计算是否仍在适用边界内；观察点为17.2.1 矩阵奇异值分解算法。

对于17.3.4 算法这个节点，先预测从“适中”切到“高”后，哪一个概率、距离、间隔、谱、目标或残差最先变化。运行极小样本后若方向不符，优先检查数据折分、尺度、索引、归一和符号，而不是用末端分数掩盖第一处错误。

## 三个必须主动触发的误区


## 练习、答案与285节点验证


