---
title: "第21章 PageRank算法"
type: B
section: "第二篇 · 图排序与总结"
order: 21
description: "第21章 PageRank算法覆盖10个权威目录层级，以模型-策略-算法、数学推导、交互实验和失败对照重建核心。"
demo: true
math: true
sourceUrl: "https://www.tup.tsinghua.edu.cn/bookscenter/book_08132901.html"
draft: false
---

import {
  Slm21PageRankMapLab,
  Slm21PageRankExperimentLab,
  Slm21PageRankEvidenceLab,
} from "@/components/mdx/statistical-learning-methods/diagrams/slm-21-pagerank";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第21章 PageRank算法中的模型、学习策略、优化算法与适用边界
- 能计算目标函数、充分统计、参数更新和独立评价中的关键量
- 能复现三个互动视图，并定位第一条概率、几何、矩阵或优化证据分叉
- 能判断“转移矩阵方向、列或行归一约定、悬挂节点修复和阻尼系数必须一致；最终向量非负、归一且满足固定点残差。”是否成立

</Objectives>

## 为什么坚持模型、策略与算法三要素

<Term def="第21章 PageRank算法是本页第1个核查坐标，用于连接统计假设、目标、算法和泛化证据。">
  第21章 PageRank算法
</Term>
、
<Term def="21.1 PageRank的定义是本页第2个核查坐标，用于连接统计假设、目标、算法和泛化证据。">
  21.1 PageRank的定义
</Term>
、
<Term def="21.1.1 基本想法是本页第3个核查坐标，用于连接统计假设、目标、算法和泛化证据。">
  21.1.1 基本想法
</Term>
、
<Term def="21.1.2 有向图和随机游走模型是本页第4个核查坐标，用于连接统计假设、目标、算法和泛化证据。">
  21.1.2 有向图和随机游走模型
</Term>
、
<Term def="21.1.3 PageRank的基本定义是本页第5个核查坐标，用于连接统计假设、目标、算法和泛化证据。">
  21.1.3 PageRank的基本定义
</Term>
、
<Term def="21.1.4 PageRank的一般定义是本页第6个核查坐标，用于连接统计假设、目标、算法和泛化证据。">
  21.1.4 PageRank的一般定义
</Term>
共同构成本页坐标。从有向图随机游走和平稳分布定义基本PageRank，用阻尼与随机跳转处理终止点和陷阱，并比较迭代、幂法和代数求解。

统计学习从有限样本出发。模型规定允许选择的函数、概率分布或潜在结构，策略规定什么叫更好，算法把策略转成有限计算。相同模型配不同损失会产生不同估计，相同策略用不同优化器可能只改变计算效率，也可能因近似、初始化和停止条件落入不同解。阅读每个方法时必须分开记录三者，不能用算法名称代替完整学习合同。

实验先冻结样本标识、训练验证测试折分、输入输出空间、预处理统计、特征顺序、随机种子、浮点精度和停止规则。训练阶段保存目标分解、充分统计、梯度或动态规划状态、更新前后参数；模型选择阶段保存所有候选和折级预测；测试集直到全部决定冻结后只打开一次。无监督任务则使用留出似然、稳定性、重构、外部标签或下游任务建立独立证据。

先预测单变量扰动会在哪里最早显现：样本表示、概率归一、几何间隔、矩阵谱、隐变量责任度、动态规划消息、采样接受率还是验证风险。随后在可枚举的小样本上运行一步并立即比较。若首个中间量已经错误，继续观察末端准确率或可视化没有诊断价值；修复后必须从原始输入重放，不能沿用污染的缓存和模型状态。

## 数学骨架与可核查量

经验风险聚合冻结训练样本上的损失：

$$
\widehat R(f)=\frac1N\sum_{i=1}^{N}L\bigl(y_i,f(x_i)\bigr).
$$

结构风险把复杂度或先验写进选择准则：

$$
\widehat R_{\lambda}(f)=\widehat R(f)+\lambda J(f).
$$

模型选择和最终测试严格隔离：

$$
\hat\lambda=\arg\min_{\lambda}\widehat R_{valid}(f_\lambda),\qquad \widehat R_{test}(f_{\hat\lambda})\text{ once}.
$$

本页专属可核查关系为：

$$
r=dMr+(1-d)\frac{\boldsymbol1}{n}.
$$

一般PageRank以概率d沿链接转移，以1-d随机跳转，使链通常不可约且存在唯一平稳排序。 实现还要检查张量或矩阵形状、有限值、概率归一、正交或KKT残差、目标单调性、梯度方向和停止容差。

## 核心机制

1. 基本PageRank把网页看作有向图节点，把链接随机游走的平稳概率解释为重要性。
2. 无出边节点使转移列缺失，封闭子图会形成排名陷阱，需要随机跳转修正。
3. 阻尼模型在链接转移和均匀跳转间混合，阻尼越大越依赖图结构也越慢混合。
4. 迭代算法反复应用固定点映射，以L1差或残差判断收敛。
5. 幂法把PageRank视为主特征向量问题，稀疏矩阵乘法适合大图。
6. 代数法解线性方程适合小图核对，但大规模显式求逆成本高且数值不必要。

同一个末端分数可能来自不同机制。训练风险下降可能来自容量、泄漏、额外预算或随机好运；概率归一不代表校准，数值收敛不代表统计假设成立，二维图形清楚也不代表表示对未见数据有用。对照实验保持数据、预处理、搜索预算和评价协议可比，只替换一个模型、策略或算法条件，并报告多折或多种子分布。

## 权威目录逐项讲解

### 第21章 PageRank算法

复现“第21章 PageRank算法”时固定初始化和访问顺序，保存每轮输入、充分统计、中间状态、目标差和停止残差，并与枚举或直接计算基线对照。

在“冻结样本与符号”固定样本身份、符号、形状、随机状态和适用条件，再进入“写出模型空间”只改变一个变量。记录训练目标、关键中间量、约束残差、更新前后状态和独立风险；若最终分数改善但理论预期的第一处变化没有发生，先排查泄漏、索引、尺度、符号和容差。

通过标准包含定义、推导、算法和反例四层：能够准确复述该目录项的对象与前提；能够在极小数据上手算关键量；能够固定种子复现一步状态转移；能够构造破坏前提的失败样本并指出停止位置。修复后清空派生特征、缓存、参数和评价，从原始样本完整重放。

### 21.1 PageRank的定义

先把“21.1 PageRank的定义”写成输入域、输出域、参数或潜变量、条件独立与不变性，给出一个满足定义的最小样本和一个只差一条前提的反例。

在“写出模型空间”固定样本身份、符号、形状、随机状态和适用条件，再进入“核对学习策略”只改变一个变量。记录训练目标、关键中间量、约束残差、更新前后状态和独立风险；若最终分数改善但理论预期的第一处变化没有发生，先排查泄漏、索引、尺度、符号和容差。

通过标准包含定义、推导、算法和反例四层：能够准确复述该目录项的对象与前提；能够在极小数据上手算关键量；能够固定种子复现一步状态转移；能够构造破坏前提的失败样本并指出停止位置。修复后清空派生特征、缓存、参数和评价，从原始样本完整重放。

### 21.1.1 基本想法

先把“21.1.1 基本想法”写成输入域、输出域、参数或潜变量、条件独立与不变性，给出一个满足定义的最小样本和一个只差一条前提的反例。

在“核对学习策略”固定样本身份、符号、形状、随机状态和适用条件，再进入“执行优化算法”只改变一个变量。记录训练目标、关键中间量、约束残差、更新前后状态和独立风险；若最终分数改善但理论预期的第一处变化没有发生，先排查泄漏、索引、尺度、符号和容差。

通过标准包含定义、推导、算法和反例四层：能够准确复述该目录项的对象与前提；能够在极小数据上手算关键量；能够固定种子复现一步状态转移；能够构造破坏前提的失败样本并指出停止位置。修复后清空派生特征、缓存、参数和评价，从原始样本完整重放。

### 21.1.2 有向图和随机游走模型

先把“21.1.2 有向图和随机游走模型”写成输入域、输出域、参数或潜变量、条件独立与不变性，给出一个满足定义的最小样本和一个只差一条前提的反例。

在“执行优化算法”固定样本身份、符号、形状、随机状态和适用条件，再进入“独立评价风险”只改变一个变量。记录训练目标、关键中间量、约束残差、更新前后状态和独立风险；若最终分数改善但理论预期的第一处变化没有发生，先排查泄漏、索引、尺度、符号和容差。

通过标准包含定义、推导、算法和反例四层：能够准确复述该目录项的对象与前提；能够在极小数据上手算关键量；能够固定种子复现一步状态转移；能够构造破坏前提的失败样本并指出停止位置。修复后清空派生特征、缓存、参数和评价，从原始样本完整重放。

### 21.1.3 PageRank的基本定义

先把“21.1.3 PageRank的基本定义”写成输入域、输出域、参数或潜变量、条件独立与不变性，给出一个满足定义的最小样本和一个只差一条前提的反例。

在“独立评价风险”固定样本身份、符号、形状、随机状态和适用条件，再进入“定位首个分叉”只改变一个变量。记录训练目标、关键中间量、约束残差、更新前后状态和独立风险；若最终分数改善但理论预期的第一处变化没有发生，先排查泄漏、索引、尺度、符号和容差。

通过标准包含定义、推导、算法和反例四层：能够准确复述该目录项的对象与前提；能够在极小数据上手算关键量；能够固定种子复现一步状态转移；能够构造破坏前提的失败样本并指出停止位置。修复后清空派生特征、缓存、参数和评价，从原始样本完整重放。

### 21.1.4 PageRank的一般定义

先把“21.1.4 PageRank的一般定义”写成输入域、输出域、参数或潜变量、条件独立与不变性，给出一个满足定义的最小样本和一个只差一条前提的反例。

在“定位首个分叉”固定样本身份、符号、形状、随机状态和适用条件，再进入“冻结样本与符号”只改变一个变量。记录训练目标、关键中间量、约束残差、更新前后状态和独立风险；若最终分数改善但理论预期的第一处变化没有发生，先排查泄漏、索引、尺度、符号和容差。

通过标准包含定义、推导、算法和反例四层：能够准确复述该目录项的对象与前提；能够在极小数据上手算关键量；能够固定种子复现一步状态转移；能够构造破坏前提的失败样本并指出停止位置。修复后清空派生特征、缓存、参数和评价，从原始样本完整重放。

### 21.2 PageRank的计算

复现“21.2 PageRank的计算”时固定初始化和访问顺序，保存每轮输入、充分统计、中间状态、目标差和停止残差，并与枚举或直接计算基线对照。

在“冻结样本与符号”固定样本身份、符号、形状、随机状态和适用条件，再进入“写出模型空间”只改变一个变量。记录训练目标、关键中间量、约束残差、更新前后状态和独立风险；若最终分数改善但理论预期的第一处变化没有发生，先排查泄漏、索引、尺度、符号和容差。

通过标准包含定义、推导、算法和反例四层：能够准确复述该目录项的对象与前提；能够在极小数据上手算关键量；能够固定种子复现一步状态转移；能够构造破坏前提的失败样本并指出停止位置。修复后清空派生特征、缓存、参数和评价，从原始样本完整重放。

### 21.2.1 迭代算法

复现“21.2.1 迭代算法”时固定初始化和访问顺序，保存每轮输入、充分统计、中间状态、目标差和停止残差，并与枚举或直接计算基线对照。

在“写出模型空间”固定样本身份、符号、形状、随机状态和适用条件，再进入“核对学习策略”只改变一个变量。记录训练目标、关键中间量、约束残差、更新前后状态和独立风险；若最终分数改善但理论预期的第一处变化没有发生，先排查泄漏、索引、尺度、符号和容差。

通过标准包含定义、推导、算法和反例四层：能够准确复述该目录项的对象与前提；能够在极小数据上手算关键量；能够固定种子复现一步状态转移；能够构造破坏前提的失败样本并指出停止位置。修复后清空派生特征、缓存、参数和评价，从原始样本完整重放。

### 21.2.2 幂法

复现“21.2.2 幂法”时固定初始化和访问顺序，保存每轮输入、充分统计、中间状态、目标差和停止残差，并与枚举或直接计算基线对照。

在“核对学习策略”固定样本身份、符号、形状、随机状态和适用条件，再进入“执行优化算法”只改变一个变量。记录训练目标、关键中间量、约束残差、更新前后状态和独立风险；若最终分数改善但理论预期的第一处变化没有发生，先排查泄漏、索引、尺度、符号和容差。

通过标准包含定义、推导、算法和反例四层：能够准确复述该目录项的对象与前提；能够在极小数据上手算关键量；能够固定种子复现一步状态转移；能够构造破坏前提的失败样本并指出停止位置。修复后清空派生特征、缓存、参数和评价，从原始样本完整重放。

### 21.2.3 代数算法

复现“21.2.3 代数算法”时固定初始化和访问顺序，保存每轮输入、充分统计、中间状态、目标差和停止残差，并与枚举或直接计算基线对照。

在“执行优化算法”固定样本身份、符号、形状、随机状态和适用条件，再进入“独立评价风险”只改变一个变量。记录训练目标、关键中间量、约束残差、更新前后状态和独立风险；若最终分数改善但理论预期的第一处变化没有发生，先排查泄漏、索引、尺度、符号和容差。

通过标准包含定义、推导、算法和反例四层：能够准确复述该目录项的对象与前提；能够在极小数据上手算关键量；能够固定种子复现一步状态转移；能够构造破坏前提的失败样本并指出停止位置。修复后清空派生特征、缓存、参数和评价，从原始样本完整重放。

## 三步交互实验

先预测样本量、模型复杂度和噪声变化会如何影响训练风险、验证风险、计算迭代和约束残差，再切换基线、边界、失败与受控模式。

<Stepper>
  <Step title="画出模型-策略-算法地图">
    标出数据、模型空间、学习准则、优化或推断、独立评价与失败边界。
    <Slm21PageRankMapLab />
  </Step>
  <Step title="运行单变量实验">
    固定数据和预算，只改变一个复杂度、平滑、核、秩、步长、链长或阻尼参数，并预测第一处分叉。
    <Slm21PageRankExperimentLab />
  </Step>
  <Step title="核对独立证据">
    并排比较目标、中间状态、约束残差、训练风险与验证风险；首错出现后从原始样本重放。
    <Slm21PageRankEvidenceLab />
  </Step>
</Stepper>

## 常见失败与边界

<Callout type="trap">
  **把算法名称当作完整方法：**
  未写清假设空间、损失、正则、数据折分和停止条件时，同名算法可以产生不可比较的模型。必须按三要素恢复完整合同。
</Callout>

<Callout type="trap">
  **让测试信息进入训练或选择：**
  全数据拟合预处理、查看测试标签调阈值、选择秩或决定停止轮次都会使风险估计乐观。所有数据依赖决定必须嵌套在训练和验证内部。
</Callout>

<Callout type="tip">
  **最小复现顺序：**
  先枚举或手算极小样本，再核对程序中间量，最后扩大数据。任何非有限值、概率不归一、矩阵残差、KKT残差、数值梯度失败或测试泄漏都应立即停止。
</Callout>

## 本章回顾

从有向图随机游走和平稳分布定义基本PageRank，用阻尼与随机跳转处理终止点和陷阱，并比较迭代、幂法和代数求解。 验收时必须从冻结样本预测中间状态与更新，解释模型、策略或算法条件为何改变答案，并在失败样本中指出第一处违背前提的位置。最终底线是：转移矩阵方向、列或行归一约定、悬挂节点修复和阻尼系数必须一致；最终向量非负、归一且满足固定点残差。

<Glossary>

<GlossaryItem term="第21章 PageRank算法">
  “第21章
  PageRank算法”对应“冻结样本与符号”：保存定义、数学前提、中间状态、数值容差、独立证据和失效边界，不能只保留最终分数。
</GlossaryItem>

<GlossaryItem term="21.1 PageRank的定义">
  “21.1
  PageRank的定义”对应“写出模型空间”：保存定义、数学前提、中间状态、数值容差、独立证据和失效边界，不能只保留最终分数。
</GlossaryItem>

<GlossaryItem term="21.1.1 基本想法">
  “21.1.1
  基本想法”对应“核对学习策略”：保存定义、数学前提、中间状态、数值容差、独立证据和失效边界，不能只保留最终分数。
</GlossaryItem>

<GlossaryItem term="21.1.2 有向图和随机游走模型">
  “21.1.2
  有向图和随机游走模型”对应“执行优化算法”：保存定义、数学前提、中间状态、数值容差、独立证据和失效边界，不能只保留最终分数。
</GlossaryItem>

<GlossaryItem term="21.1.3 PageRank的基本定义">
  “21.1.3
  PageRank的基本定义”对应“独立评价风险”：保存定义、数学前提、中间状态、数值容差、独立证据和失效边界，不能只保留最终分数。
</GlossaryItem>

<GlossaryItem term="21.1.4 PageRank的一般定义">
  “21.1.4
  PageRank的一般定义”对应“定位首个分叉”：保存定义、数学前提、中间状态、数值容差、独立证据和失效边界，不能只保留最终分数。
</GlossaryItem>

</Glossary>

<Exercises>

1. 如何为第21章 PageRank算法建立不泄漏测试信息的最小复现实验？

<Answer>
先冻结输入输出空间、样本身份和任务结构，再划分训练、验证与测试。预处理、特征、模型、超参数和停止规则只在训练与验证确定；保存“冻结样本与符号 -> 写出模型空间 -> 核对学习策略 -> 执行优化算法 -> 独立评价风险 -> 定位首个分叉”的中间证据，全部选择完成后测试集只使用一次。
</Answer>

2. 如何证明复杂方法相对简单基线的改进来自本章机制，而不是搜索预算或随机波动？

<Answer>
  保持数据折分、预处理、指标和资源预算可比，只替换一个模型、策略或算法条件。先写出根据方程应最先改变的中间量，再比较多折或多种子的验证分布；首个证据不符合机制时先修复实现，不采信末端分数。
</Answer>

3. 为“转移矩阵方向、列或行归一约定、悬挂节点修复和阻尼系数必须一致；最终向量非负、归一且满足固定点残差。”构造一个失败样本，并说明应在何处停止。

<Answer>
  可以破坏数据独立性、概率归一、线性可分、正定性、条件独立、矩阵秩、链混合或测试隔离中的一项。运行前预测第一条被破坏的目标、状态或残差，在该处立即停止并保留原始证据；清空派生产物后从冻结输入重放。
</Answer>

</Exercises>

<Attribution
  adaptedFrom="李航著《统计学习方法（第2版）》，清华大学出版社，2019，官方版权目录标注484页，ISBN 9787302517276 · 基于清华大学出版社官方两篇22章、256个编号节/小节及5个附录完整目录的独立教学重构"
  adaptedUrl="https://www.tup.tsinghua.edu.cn/bookscenter/book_08132901.html"
/>
