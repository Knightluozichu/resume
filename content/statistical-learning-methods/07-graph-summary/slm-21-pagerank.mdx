---
title: 第21章 PageRank算法
type: B
section: 第二篇 · 图排序与总结
order: 21
description: 第21章 PageRank算法覆盖10个正式节点，以手算、章专属交互、前提反例和独立评价验收。
demo: true
math: true
sourceUrl: 'https://www.tup.tsinghua.edu.cn/bookscenter/book_08132901.html'
draft: false
qualityVersion: 2
practiceMode: calculation
sourceMode: independent-rewrite
---


{/* SLM_QUALITY_V2 */}

## 为什么从“PageRank随机游走台”开始

第21章 PageRank算法不能只靠术语和最终分数验收。PageRank随机游走台先把数据与符号冻结，再把模型空间、学习目标、计算步骤和独立评价分开；这样任何异常都能回退到第一项错误中间量，而不是在准确率或可视化之后补故事。

先预测阻尼系数从“0.85”进入“接近1”时，PageRank随机游走台命中率与图缺陷偏差风险怎样变化。交互数值只表达公开的因果方向，不冒充真实训练结果。

## 来源、版次与独立重写边界

清华大学出版社[官方产品页](https://www.tup.tsinghua.edu.cn/bookscenter/book_08132901.html)确认李航《统计学习方法》第2版、2019年、ISBN 9787302517276和监督/无监督两篇结构；[官方完整目录](https://www.tup.tsinghua.edu.cn/bookscenter/bookcatalog?id=08132901)逐项核定22章、256个编号节/小节与附录A–E，共285个正式层级。第21章 PageRank算法未取得出版正文授权，目录只界定范围，不宣称复现原书正文。

第21章 PageRank算法的计算语义还与[scikit-learn官方用户指南](https://scikit-learn.org/stable/user_guide.html)、[NumPy SVD文档](https://numpy.org/doc/stable/reference/generated/numpy.linalg.svd.html)和[SciPy统计分布文档](https://docs.scipy.org/doc/scipy/reference/stats.html)中适用部分交叉核对；它们不替代本章推导，也不被误报为原书授权。中文解释、手算、图示、实验、反例与答案均为独立教学重写。

## 本章术语与数学合同

、、、、、。

本章不变量是“转移矩阵方向、列或行归一约定、悬挂节点修复和阻尼系数必须一致；最终向量非负、归一且满足固定点残差。”。数据身份、训练/验证/测试折分、预处理统计、特征顺序、随机种子、浮点精度与停止准则必须随实验保存；修复后从原始数据重算，不复用污染的缓存、责任度、矩阵分解、链状态或评价结果。

## 章专属数学骨架

$$
r=dMr+(1-d)\frac{\boldsymbol1}{n}.
$$

一般PageRank以概率d沿链接转移，以1-d随机跳转，使链通常不可约且存在唯一平稳排序。 实现还要检查张量或矩阵形状、有限值、概率归一、正交或KKT残差、目标单调性、梯度方向和停止容差。

## 核心机制与适用边界

1. 基本PageRank把网页看作有向图节点，把链接随机游走的平稳概率解释为重要性。
2. 无出边节点使转移列缺失，封闭子图会形成排名陷阱，需要随机跳转修正。
3. 阻尼模型在链接转移和均匀跳转间混合，阻尼越大越依赖图结构也越慢混合。
4. 迭代算法反复应用固定点映射，以L1差或残差判断收敛。
5. 幂法把PageRank视为主特征向量问题，稀疏矩阵乘法适合大图。
6. 代数法解线性方程适合小图核对，但大规模显式求逆成本高且数值不必要。

## 官方目录逐项深读

### 第21章 PageRank算法

**四级证据 1/10。** 第21章 PageRank算法：在可枚举小样本上执行一步更新，保存输入、充分统计、参数前后值、目标差和停止残差。本节点固定图缺陷，只改变阻尼系数，用“转移矩阵方向、列或行归一约定、悬挂节点修复和阻尼系数必须一致；最终向量非负、归一且满足固定点残差。”判断计算是否仍在适用边界内；观察点为第21章 PageRank算法。

对于第21章 PageRank算法这个节点，先预测从“0.85”切到“接近1”后，哪一个概率、距离、间隔、谱、目标或残差最先变化。运行极小样本后若方向不符，优先检查数据折分、尺度、索引、归一和符号，而不是用末端分数掩盖第一处错误。

### 21.1 PageRank的定义

**四级证据 2/10。** 21.1 PageRank的定义：写出随机变量、空间、参数、归一条件与可识别性，不用算法名称替代数学对象。本节点固定图缺陷，只改变阻尼系数，用“转移矩阵方向、列或行归一约定、悬挂节点修复和阻尼系数必须一致；最终向量非负、归一且满足固定点残差。”判断计算是否仍在适用边界内；观察点为21.1 PageRank的定义。

对于21.1 PageRank的定义这个节点，先预测从“0.85”切到“接近1”后，哪一个概率、距离、间隔、谱、目标或残差最先变化。运行极小样本后若方向不符，优先检查数据折分、尺度、索引、归一和符号，而不是用末端分数掩盖第一处错误。

### 21.1.1 基本想法

**四级证据 3/10。** 21.1.1 基本想法：写出随机变量、空间、参数、归一条件与可识别性，不用算法名称替代数学对象。本节点固定图缺陷，只改变阻尼系数，用“转移矩阵方向、列或行归一约定、悬挂节点修复和阻尼系数必须一致；最终向量非负、归一且满足固定点残差。”判断计算是否仍在适用边界内；观察点为21.1.1 基本想法。

对于21.1.1 基本想法这个节点，先预测从“0.85”切到“接近1”后，哪一个概率、距离、间隔、谱、目标或残差最先变化。运行极小样本后若方向不符，优先检查数据折分、尺度、索引、归一和符号，而不是用末端分数掩盖第一处错误。

### 21.1.2 有向图和随机游走模型

**四级证据 4/10。** 21.1.2 有向图和随机游走模型：写出随机变量、空间、参数、归一条件与可识别性，不用算法名称替代数学对象。本节点固定图缺陷，只改变阻尼系数，用“转移矩阵方向、列或行归一约定、悬挂节点修复和阻尼系数必须一致；最终向量非负、归一且满足固定点残差。”判断计算是否仍在适用边界内；观察点为21.1.2 有向图和随机游走模型。

对于21.1.2 有向图和随机游走模型这个节点，先预测从“0.85”切到“接近1”后，哪一个概率、距离、间隔、谱、目标或残差最先变化。运行极小样本后若方向不符，优先检查数据折分、尺度、索引、归一和符号，而不是用末端分数掩盖第一处错误。

### 21.1.3 PageRank的基本定义

**四级证据 5/10。** 21.1.3 PageRank的基本定义：写出随机变量、空间、参数、归一条件与可识别性，不用算法名称替代数学对象。本节点固定图缺陷，只改变阻尼系数，用“转移矩阵方向、列或行归一约定、悬挂节点修复和阻尼系数必须一致；最终向量非负、归一且满足固定点残差。”判断计算是否仍在适用边界内；观察点为21.1.3 PageRank的基本定义。

对于21.1.3 PageRank的基本定义这个节点，先预测从“0.85”切到“接近1”后，哪一个概率、距离、间隔、谱、目标或残差最先变化。运行极小样本后若方向不符，优先检查数据折分、尺度、索引、归一和符号，而不是用末端分数掩盖第一处错误。

### 21.1.4 PageRank的一般定义

**四级证据 6/10。** 21.1.4 PageRank的一般定义：写出随机变量、空间、参数、归一条件与可识别性，不用算法名称替代数学对象。本节点固定图缺陷，只改变阻尼系数，用“转移矩阵方向、列或行归一约定、悬挂节点修复和阻尼系数必须一致；最终向量非负、归一且满足固定点残差。”判断计算是否仍在适用边界内；观察点为21.1.4 PageRank的一般定义。

对于21.1.4 PageRank的一般定义这个节点，先预测从“0.85”切到“接近1”后，哪一个概率、距离、间隔、谱、目标或残差最先变化。运行极小样本后若方向不符，优先检查数据折分、尺度、索引、归一和符号，而不是用末端分数掩盖第一处错误。

### 21.2 PageRank的计算

**四级证据 7/10。** 21.2 PageRank的计算：在可枚举小样本上执行一步更新，保存输入、充分统计、参数前后值、目标差和停止残差。本节点固定图缺陷，只改变阻尼系数，用“转移矩阵方向、列或行归一约定、悬挂节点修复和阻尼系数必须一致；最终向量非负、归一且满足固定点残差。”判断计算是否仍在适用边界内；观察点为第21章 PageRank算法。

对于21.2 PageRank的计算这个节点，先预测从“0.85”切到“接近1”后，哪一个概率、距离、间隔、谱、目标或残差最先变化。运行极小样本后若方向不符，优先检查数据折分、尺度、索引、归一和符号，而不是用末端分数掩盖第一处错误。

### 21.2.1 迭代算法

**四级证据 8/10。** 21.2.1 迭代算法：在可枚举小样本上执行一步更新，保存输入、充分统计、参数前后值、目标差和停止残差。本节点固定图缺陷，只改变阻尼系数，用“转移矩阵方向、列或行归一约定、悬挂节点修复和阻尼系数必须一致；最终向量非负、归一且满足固定点残差。”判断计算是否仍在适用边界内；观察点为21.1 PageRank的定义。

对于21.2.1 迭代算法这个节点，先预测从“0.85”切到“接近1”后，哪一个概率、距离、间隔、谱、目标或残差最先变化。运行极小样本后若方向不符，优先检查数据折分、尺度、索引、归一和符号，而不是用末端分数掩盖第一处错误。

### 21.2.2 幂法

**四级证据 9/10。** 21.2.2 幂法：把该节点放回模型—策略—算法链，说明输入、输出、假设和第一项可观察中间量。本节点固定图缺陷，只改变阻尼系数，用“转移矩阵方向、列或行归一约定、悬挂节点修复和阻尼系数必须一致；最终向量非负、归一且满足固定点残差。”判断计算是否仍在适用边界内；观察点为21.1.1 基本想法。

对于21.2.2 幂法这个节点，先预测从“0.85”切到“接近1”后，哪一个概率、距离、间隔、谱、目标或残差最先变化。运行极小样本后若方向不符，优先检查数据折分、尺度、索引、归一和符号，而不是用末端分数掩盖第一处错误。

### 21.2.3 代数算法

**四级证据 10/10。** 21.2.3 代数算法：在可枚举小样本上执行一步更新，保存输入、充分统计、参数前后值、目标差和停止残差。本节点固定图缺陷，只改变阻尼系数，用“转移矩阵方向、列或行归一约定、悬挂节点修复和阻尼系数必须一致；最终向量非负、归一且满足固定点残差。”判断计算是否仍在适用边界内；观察点为21.1.2 有向图和随机游走模型。

对于21.2.3 代数算法这个节点，先预测从“0.85”切到“接近1”后，哪一个概率、距离、间隔、谱、目标或残差最先变化。运行极小样本后若方向不符，优先检查数据折分、尺度、索引、归一和符号，而不是用末端分数掩盖第一处错误。

## 三个必须主动触发的误区


## 练习、答案与285节点验证


