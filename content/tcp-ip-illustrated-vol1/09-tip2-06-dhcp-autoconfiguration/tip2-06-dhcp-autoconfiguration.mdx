---
title: "第6章 系统配置：DHCP和自动配置"
description: "重建DHCPv4/v6租约、选项、中继、认证、SLAAC、DNS交互与PPPoE"
type: A
section: "TCP/IP详解 卷1：协议（原书第2版 / 2016）"
order: 9
draft: false
math: false
sourceUrl: "https://www.informit.com/store/tcp-ip-illustrated-volume-1-the-protocols-9780321336316"
demo: true
---

# 第6章 系统配置：DHCP和自动配置

本课程锁定Kevin R. Fall、W. Richard Stevens著《TCP/IP Illustrated, Volume 1: The Protocols, Second Edition》，Addison-Wesley Professional，2011年11月首印、版权2012年，1056页，ISBN 9780321336316；吴英、张玉、许昱玮译《TCP/IP详解 卷1：协议（原书第2版）》，机械工业出版社，2016年6月，683页，ISBN 9787111453833。InformIT/Pearson正式目录与出版社预览确认3篇前置材料、18章、468个编号节/小节、缩略语表和索引，共23个正式单元、491个正式节点；旧10页泛化课程已整体替换。


## 从可证伪的协议预测开始

本页主问题是：重建DHCPv4/v6租约、选项、中继、认证、SLAAC、DNS交互与PPPoE。先写预测，再固定端点、接口、路由、MTU、时钟、实现版本与抓包位置。验收不变量是：配置结果能回溯到事务标识、消息序列、租约计时器、前缀来源与中继路径。反例是：只看最终地址而不看状态机，会混淆续租、重绑定、中继、SLAAC和伪造响应。最终应用错误、协议标签或单张截图都只是线索，不能独立证明根因。

## 核心词汇与第2版边界

、、、、
版次固定在2011年英文第2版与2016年中译本。保留原书明确讲授的IPv6、DNS64、TLS 1.2、DTLS、BIC/CUBIC、EAP、IPsec、DNSSEC和DKIM；QUIC、HTTP/3、TLS 1.3、WPA3、BBR等后续主题只能另作比较，不能替换正式节点。


## 五层协议证据模型

### 第一层：先确定观察边界

画出端点、接口、交换/路由节点、NAT或隧道，并标出抓包点能看到和看不到的流量。保存接口、链路类型、MTU、地址、路由表、时间源和实现版本；否则本机卸载、镜像丢包或非对称路径会被误判为协议行为。

### 第二层：让原始字节约束解释

对关键报文同时保留时间、方向、五元组、链路封装、十六进制偏移和解析字段。校验长度、标志、校验和与上下层边界；解析器、名称解析和专家提示都是可变解释层，必须接受原始字节反证。

### 第三层：按状态和计时器重建因果

把请求/响应、Seq/Ack、租约、成员关系、缓存、重传或密钥协商排成有方向的时间线。明确每个输入触发的状态转移、计时器启动/取消和输出报文，先找到首个不满足预测的事件，再解释传播。

### 第四层：做单变量对照

固定拓扑、流量和实现，只改变一个地址、选项、计时器、规则、窗口或丢包条件。保存正常、故障和恢复三段记录，并列出至少一个竞争解释；只有能排除竞争解释，才能把相关性提升为根因。

### 第五层：关闭独立证据门

复核者只拿只读报文、哈希、命令、配置和预期输出，必须能定位同一个首个偏差并得到同一结论。历史工具或字段与现代实现不同时，分别记录第2版行为和现代差异，不能静默改写原书。

```text
observation = edition + topology + capture_point + direction + raw_bytes
mechanism   = input + protocol_state + timer + output + invariant
claim       = prediction + first_deviation + competing_hypothesis + recovery
```

## 原书目录逐节点重构

### 第6章 系统配置：DHCP和自动配置

为“第6章 系统配置：DHCP和自动配置”画出客户端、中继与服务器消息序列，标记事务标识、选项、租约计时器、地址状态和失败回退。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“第6章 系统配置：DHCP和自动配置”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。 同时画出本单元与上一层、下一层协议的接口。

### 6.1 引言

用“6.1 引言”校准本章问题、证据范围和标准来源；总结必须回到数据包字段、状态转移或实现观察，不能只列术语。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“6.1 引言”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

### 6.2 动态主机配置协议

把“6.2 动态主机配置协议”放回“重建DHCPv4/v6租约、选项、中继、认证、SLAAC、DNS交互与PPPoE”的协议路径，标注输入、状态、报文字段、输出与最小失败反例。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“6.2 动态主机配置协议”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 6.2.1 地址池和租用

对“6.2.1 地址池和租用”写出位级表示、前缀边界、作用域和正反例，并用两个相邻前缀验证聚合或匹配结果。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“6.2.1 地址池和租用”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 6.2.2 DHCP和BOOTP消息格式

为“6.2.2 DHCP和BOOTP消息格式”画出客户端、中继与服务器消息序列，标记事务标识、选项、租约计时器、地址状态和失败回退。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“6.2.2 DHCP和BOOTP消息格式”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 6.2.3 DHCP和BOOTP选项

为“6.2.3 DHCP和BOOTP选项”画出客户端、中继与服务器消息序列，标记事务标识、选项、租约计时器、地址状态和失败回退。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“6.2.3 DHCP和BOOTP选项”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 6.2.4 DHCP协议操作

为“6.2.4 DHCP协议操作”画出客户端、中继与服务器消息序列，标记事务标识、选项、租约计时器、地址状态和失败回退。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“6.2.4 DHCP协议操作”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 6.2.5 DHCPv6

为“6.2.5 DHCPv6”画出客户端、中继与服务器消息序列，标记事务标识、选项、租约计时器、地址状态和失败回退。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“6.2.5 DHCPv6”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 6.2.6 使用DHCP中继

为“6.2.6 使用DHCP中继”画出客户端、中继与服务器消息序列，标记事务标识、选项、租约计时器、地址状态和失败回退。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“6.2.6 使用DHCP中继”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 6.2.7 DHCP认证

为“6.2.7 DHCP认证”画出客户端、中继与服务器消息序列，标记事务标识、选项、租约计时器、地址状态和失败回退。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“6.2.7 DHCP认证”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 6.2.8 重新配置扩展

把“6.2.8 重新配置扩展”放回“重建DHCPv4/v6租约、选项、中继、认证、SLAAC、DNS交互与PPPoE”的协议路径，标注输入、状态、报文字段、输出与最小失败反例。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“6.2.8 重新配置扩展”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 6.2.9 快速确认

把“6.2.9 快速确认”放回“重建DHCPv4/v6租约、选项、中继、认证、SLAAC、DNS交互与PPPoE”的协议路径，标注输入、状态、报文字段、输出与最小失败反例。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“6.2.9 快速确认”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 6.2.10 位置信息（LCI和LoST）

把“6.2.10 位置信息（LCI和LoST）”放回“重建DHCPv4/v6租约、选项、中继、认证、SLAAC、DNS交互与PPPoE”的协议路径，标注输入、状态、报文字段、输出与最小失败反例。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“6.2.10 位置信息（LCI和LoST）”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 6.2.11 移动和切换信息（MoS和ANDSF）

把“6.2.11 移动和切换信息（MoS和ANDSF）”放回“重建DHCPv4/v6租约、选项、中继、认证、SLAAC、DNS交互与PPPoE”的协议路径，标注输入、状态、报文字段、输出与最小失败反例。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“6.2.11 移动和切换信息（MoS和ANDSF）”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 6.2.12 DHCP嗅探

为“6.2.12 DHCP嗅探”画出客户端、中继与服务器消息序列，标记事务标识、选项、租约计时器、地址状态和失败回退。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“6.2.12 DHCP嗅探”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

### 6.3 无状态地址自动配置

对“6.3 无状态地址自动配置”写出位级表示、前缀边界、作用域和正反例，并用两个相邻前缀验证聚合或匹配结果。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“6.3 无状态地址自动配置”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 6.3.1 IPv4链路本地地址的动态配置

对“6.3.1 IPv4链路本地地址的动态配置”写出位级表示、前缀边界、作用域和正反例，并用两个相邻前缀验证聚合或匹配结果。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“6.3.1 IPv4链路本地地址的动态配置”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 6.3.2 链路本地地址的IPv6 SLAAC

对“6.3.2 链路本地地址的IPv6 SLAAC”写出位级表示、前缀边界、作用域和正反例，并用两个相邻前缀验证聚合或匹配结果。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“6.3.2 链路本地地址的IPv6 SLAAC”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

### 6.4 DHCP和DNS交互

为“6.4 DHCP和DNS交互”画出客户端、中继与服务器消息序列，标记事务标识、选项、租约计时器、地址状态和失败回退。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“6.4 DHCP和DNS交互”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

### 6.5 以太网上的PPP

把“6.5 以太网上的PPP”定位到同一链路的帧格式、邻接关系、介质访问和MTU边界；同时记录抓包位置看不到什么。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“6.5 以太网上的PPP”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

### 6.6 与系统配置相关的攻击

为“6.6 与系统配置相关的攻击”先写资产、攻击者能力与信任根，再标明认证对象、密钥材料、保护层、重放边界和失败处置。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“6.6 与系统配置相关的攻击”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

### 6.7 总结

用“6.7 总结”校准本章问题、证据范围和标准来源；总结必须回到数据包字段、状态转移或实现观察，不能只列术语。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“6.7 总结”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

### 6.8 参考文献

用“6.8 参考文献”校准本章问题、证据范围和标准来源；总结必须回到数据包字段、状态转移或实现观察，不能只列术语。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“6.8 参考文献”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

## 本页完整节点清单

- 第6章 系统配置：DHCP和自动配置
- 6.1 引言
- 6.2 动态主机配置协议
- 6.2.1 地址池和租用
- 6.2.2 DHCP和BOOTP消息格式
- 6.2.3 DHCP和BOOTP选项
- 6.2.4 DHCP协议操作
- 6.2.5 DHCPv6
- 6.2.6 使用DHCP中继
- 6.2.7 DHCP认证
- 6.2.8 重新配置扩展
- 6.2.9 快速确认
- 6.2.10 位置信息（LCI和LoST）
- 6.2.11 移动和切换信息（MoS和ANDSF）
- 6.2.12 DHCP嗅探
- 6.3 无状态地址自动配置
- 6.3.1 IPv4链路本地地址的动态配置
- 6.3.2 链路本地地址的IPv6 SLAAC
- 6.4 DHCP和DNS交互
- 6.5 以太网上的PPP
- 6.6 与系统配置相关的攻击
- 6.7 总结
- 6.8 参考文献


## 可复现实验记录

```text
packet = timestamp + direction + five_tuple + header_fields + raw_offset
state  = before + event + guard + action + timer + after
audit  = expected + observed + first_difference + alternative + recovery
```

```bash
tcpdump -nn -s0 -w tip2-lab.pcap 'host 192.0.2.10'
tshark -r tip2-lab.pcap -T fields -e frame.number -e frame.time_relative -e ip.src -e ip.dst -e tcp.seq -e tcp.ack
```

动手试：采集一次正常事务、一次单变量故障和一次恢复事务。对每条结论记录文件哈希、关键帧、字节偏移、状态前后值、竞争解释和独立复核结果。


## 独立证据门

最小证据包包含：正式节点、版次、拓扑、抓包位置、原始报文哈希、字段偏移、状态/计时器、单变量故障、恢复、偏差、责任人与复核人。

## 小结

- DHCP 自动配置网络参数
- 四步交互获取租约
- 中继代理跨网段转发
- 无状态自动配置补充 IPv6
- 地址冲突需要检测处理

## 练习


## 本章回顾

“第6章 系统配置：DHCP和自动配置”的核心是重建DHCPv4/v6租约、选项、中继、认证、SLAAC、DNS交互与PPPoE。真正掌握不是记住名词，而是能从原始字节、状态机、计时器和单变量反例证明协议为何如此运行。


[← 上一页：第5章 Internet协议](/learn/tcp-ip-illustrated-vol1/08-tip2-05-internet-protocol/tip2-05-internet-protocol) · [下一页：第7章 防火墙和网络地址转换 →](/learn/tcp-ip-illustrated-vol1/10-tip2-07-firewalls-nat/tip2-07-firewalls-nat)

<Attribution
  adaptedFrom="《TCP/IP详解 卷1：协议》原书第2版"
  adaptedUrl="https://www.informit.com/store/tcp-ip-illustrated-volume-1-the-protocols-9780321336316"
/>
