---
title: "第7章 防火墙和网络地址转换"
description: "以状态表和五元组解释包过滤、代理、防火墙规则、NAT行为与穿越机制"
type: A
section: "TCP/IP详解 卷1：协议（原书第2版 / 2016）"
order: 10
draft: true
math: false
sourceUrl: "https://www.informit.com/store/tcp-ip-illustrated-volume-1-the-protocols-9780321336316"
demo: true
---

import {
  Objectives,
  Term,
  Stepper,
  Step,
  Callout,
  Exercises,
  Answer,
  Glossary,
  GlossaryItem,
  Attribution,
} from "@/components/mdx/mdx-components";
# 第7章 防火墙和网络地址转换

本课程锁定Kevin R. Fall、W. Richard Stevens著《TCP/IP Illustrated, Volume 1: The Protocols, Second Edition》，Addison-Wesley Professional，2011年11月首印、版权2012年，1056页，ISBN 9780321336316；吴英、张玉、许昱玮译《TCP/IP详解 卷1：协议（原书第2版）》，机械工业出版社，2016年6月，683页，ISBN 9787111453833。InformIT/Pearson正式目录与出版社预览确认3篇前置材料、18章、468个编号节/小节、缩略语表和索引，共23个正式单元、491个正式节点；旧10页泛化课程已整体替换。

<Objectives>

- 能解释“第7章 防火墙和网络地址转换”的全部正式节点，并指出其在端到端协议路径中的位置。
- 能从原始字节、首部字段、状态、计时器和实现观察重建协议行为。
- 能围绕“每次允许、拒绝或转换都能指出入站元组、规则、状态、出站元组与回程映射”设计正常样本与单变量失败实验。
- 能提交含版本、抓包位置、命令、首个偏差、恢复和复核人的证据包。

</Objectives>

## 从可证伪的协议预测开始

本页主问题是：以状态表和五元组解释包过滤、代理、防火墙规则、NAT行为与穿越机制。先写预测，再固定端点、接口、路由、MTU、时钟、实现版本与抓包位置。验收不变量是：每次允许、拒绝或转换都能指出入站元组、规则、状态、出站元组与回程映射。反例是：把NAT等同于防火墙，或只观察单向元组，会误判过滤、发夹和穿越行为。最终应用错误、协议标签或单张截图都只是线索，不能独立证明根因。

## 核心词汇与第2版边界

<Term def="本页协议证据链中的关键对象、状态或判定条件">包过滤</Term>、<Term def="本页协议证据链中的关键对象、状态或判定条件">NAPT</Term>、<Term def="本页协议证据链中的关键对象、状态或判定条件">NAT穿越</Term>、<Term def="本页协议证据链中的关键对象、状态或判定条件">STUN</Term>、<Term def="本页协议证据链中的关键对象、状态或判定条件">IPv4/IPv6转换</Term>

版次固定在2011年英文第2版与2016年中译本。保留原书明确讲授的IPv6、DNS64、TLS 1.2、DTLS、BIC/CUBIC、EAP、IPsec、DNSSEC和DKIM；QUIC、HTTP/3、TLS 1.3、WPA3、BBR等后续主题只能另作比较，不能替换正式节点。

<Tip207FirewallsNatProtocolLab />

## 五层协议证据模型

### 第一层：先确定观察边界

画出端点、接口、交换/路由节点、NAT或隧道，并标出抓包点能看到和看不到的流量。保存接口、链路类型、MTU、地址、路由表、时间源和实现版本；否则本机卸载、镜像丢包或非对称路径会被误判为协议行为。

### 第二层：让原始字节约束解释

对关键报文同时保留时间、方向、五元组、链路封装、十六进制偏移和解析字段。校验长度、标志、校验和与上下层边界；解析器、名称解析和专家提示都是可变解释层，必须接受原始字节反证。

### 第三层：按状态和计时器重建因果

把请求/响应、Seq/Ack、租约、成员关系、缓存、重传或密钥协商排成有方向的时间线。明确每个输入触发的状态转移、计时器启动/取消和输出报文，先找到首个不满足预测的事件，再解释传播。

### 第四层：做单变量对照

固定拓扑、流量和实现，只改变一个地址、选项、计时器、规则、窗口或丢包条件。保存正常、故障和恢复三段记录，并列出至少一个竞争解释；只有能排除竞争解释，才能把相关性提升为根因。

### 第五层：关闭独立证据门

复核者只拿只读报文、哈希、命令、配置和预期输出，必须能定位同一个首个偏差并得到同一结论。历史工具或字段与现代实现不同时，分别记录第2版行为和现代差异，不能静默改写原书。

```text
observation = edition + topology + capture_point + direction + raw_bytes
mechanism   = input + protocol_state + timer + output + invariant
claim       = prediction + first_deviation + competing_hypothesis + recovery
```

## 原书目录逐节点重构

### 第7章 防火墙和网络地址转换

对“第7章 防火墙和网络地址转换”写出位级表示、前缀边界、作用域和正反例，并用两个相邻前缀验证聚合或匹配结果。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“第7章 防火墙和网络地址转换”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。 同时画出本单元与上一层、下一层协议的接口。

### 7.1 引言

用“7.1 引言”校准本章问题、证据范围和标准来源；总结必须回到数据包字段、状态转移或实现观察，不能只列术语。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“7.1 引言”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

### 7.2 防火墙

用入站五元组→规则/状态→出站五元组解释“7.2 防火墙”，再以回程流量和超时验证映射是否成立。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“7.2 防火墙”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 7.2.1 包过滤防火墙

用入站五元组→规则/状态→出站五元组解释“7.2.1 包过滤防火墙”，再以回程流量和超时验证映射是否成立。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“7.2.1 包过滤防火墙”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 7.2.2 代理防火墙

用入站五元组→规则/状态→出站五元组解释“7.2.2 代理防火墙”，再以回程流量和超时验证映射是否成立。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“7.2.2 代理防火墙”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

### 7.3 网络地址转换

对“7.3 网络地址转换”写出位级表示、前缀边界、作用域和正反例，并用两个相邻前缀验证聚合或匹配结果。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“7.3 网络地址转换”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 7.3.1 传统的NAT：基本NAT和NAPT

用入站五元组→规则/状态→出站五元组解释“7.3.1 传统的NAT：基本NAT和NAPT”，再以回程流量和超时验证映射是否成立。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“7.3.1 传统的NAT：基本NAT和NAPT”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 7.3.2 地址和端口转换行为

对“7.3.2 地址和端口转换行为”写出位级表示、前缀边界、作用域和正反例，并用两个相邻前缀验证聚合或匹配结果。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“7.3.2 地址和端口转换行为”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 7.3.3 过滤行为

用入站五元组→规则/状态→出站五元组解释“7.3.3 过滤行为”，再以回程流量和超时验证映射是否成立。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“7.3.3 过滤行为”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 7.3.4 位于NAT之后的服务器

用入站五元组→规则/状态→出站五元组解释“7.3.4 位于NAT之后的服务器”，再以回程流量和超时验证映射是否成立。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“7.3.4 位于NAT之后的服务器”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 7.3.5 发夹和NAT环回

用入站五元组→规则/状态→出站五元组解释“7.3.5 发夹和NAT环回”，再以回程流量和超时验证映射是否成立。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“7.3.5 发夹和NAT环回”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 7.3.6 NAT编辑器

用入站五元组→规则/状态→出站五元组解释“7.3.6 NAT编辑器”，再以回程流量和超时验证映射是否成立。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“7.3.6 NAT编辑器”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 7.3.7 服务提供者NAT和服务提供者IPv6转换

用入站五元组→规则/状态→出站五元组解释“7.3.7 服务提供者NAT和服务提供者IPv6转换”，再以回程流量和超时验证映射是否成立。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“7.3.7 服务提供者NAT和服务提供者IPv6转换”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

### 7.4 NAT穿越

用入站五元组→规则/状态→出站五元组解释“7.4 NAT穿越”，再以回程流量和超时验证映射是否成立。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“7.4 NAT穿越”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 7.4.1 针孔和打孔

把“7.4.1 针孔和打孔”放回“以状态表和五元组解释包过滤、代理、防火墙规则、NAT行为与穿越机制”的协议路径，标注输入、状态、报文字段、输出与最小失败反例。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“7.4.1 针孔和打孔”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 7.4.2 单边的自地址确定

对“7.4.2 单边的自地址确定”写出位级表示、前缀边界、作用域和正反例，并用两个相邻前缀验证聚合或匹配结果。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“7.4.2 单边的自地址确定”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 7.4.3 NAT的会话穿越工具

用入站五元组→规则/状态→出站五元组解释“7.4.3 NAT的会话穿越工具”，再以回程流量和超时验证映射是否成立。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“7.4.3 NAT的会话穿越工具”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 7.4.4 利用NAT中继的穿越

为“7.4.4 利用NAT中继的穿越”画出客户端、中继与服务器消息序列，标记事务标识、选项、租约计时器、地址状态和失败回退。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“7.4.4 利用NAT中继的穿越”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 7.4.5 交互连接建立

把“7.4.5 交互连接建立”放进TCP字节序号和状态机：记录四元组、Seq/Ack、标志、窗口、选项、计时器和应用读写边界。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“7.4.5 交互连接建立”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

### 7.5 配置包过滤防火墙和NAT

用入站五元组→规则/状态→出站五元组解释“7.5 配置包过滤防火墙和NAT”，再以回程流量和超时验证映射是否成立。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“7.5 配置包过滤防火墙和NAT”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 7.5.1 防火墙规则

用入站五元组→规则/状态→出站五元组解释“7.5.1 防火墙规则”，再以回程流量和超时验证映射是否成立。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“7.5.1 防火墙规则”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 7.5.2 NAT规则

用入站五元组→规则/状态→出站五元组解释“7.5.2 NAT规则”，再以回程流量和超时验证映射是否成立。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“7.5.2 NAT规则”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 7.5.3 与NAT和防火墙的直接交互：UPnP、NAT-PMP和PCP

用入站五元组→规则/状态→出站五元组解释“7.5.3 与NAT和防火墙的直接交互：UPnP、NAT-PMP和PCP”，再以回程流量和超时验证映射是否成立。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“7.5.3 与NAT和防火墙的直接交互：UPnP、NAT-PMP和PCP”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

### 7.6 IPv4/IPv6共存和过渡中的NAT

用入站五元组→规则/状态→出站五元组解释“7.6 IPv4/IPv6共存和过渡中的NAT”，再以回程流量和超时验证映射是否成立。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“7.6 IPv4/IPv6共存和过渡中的NAT”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 7.6.1 双协议栈精简版

把“7.6.1 双协议栈精简版”放回“以状态表和五元组解释包过滤、代理、防火墙规则、NAT行为与穿越机制”的协议路径，标注输入、状态、报文字段、输出与最小失败反例。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“7.6.1 双协议栈精简版”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 7.6.2 使用NAT和ALG的IPv4/IPv6转换

用入站五元组→规则/状态→出站五元组解释“7.6.2 使用NAT和ALG的IPv4/IPv6转换”，再以回程流量和超时验证映射是否成立。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“7.6.2 使用NAT和ALG的IPv4/IPv6转换”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

### 7.7 与防火墙和NAT相关的攻击

用入站五元组→规则/状态→出站五元组解释“7.7 与防火墙和NAT相关的攻击”，再以回程流量和超时验证映射是否成立。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“7.7 与防火墙和NAT相关的攻击”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

### 7.8 总结

用“7.8 总结”校准本章问题、证据范围和标准来源；总结必须回到数据包字段、状态转移或实现观察，不能只列术语。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“7.8 总结”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

### 7.9 参考文献

用“7.9 参考文献”校准本章问题、证据范围和标准来源；总结必须回到数据包字段、状态转移或实现观察，不能只列术语。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“7.9 参考文献”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

## 本页完整节点清单

- 第7章 防火墙和网络地址转换
- 7.1 引言
- 7.2 防火墙
- 7.2.1 包过滤防火墙
- 7.2.2 代理防火墙
- 7.3 网络地址转换
- 7.3.1 传统的NAT：基本NAT和NAPT
- 7.3.2 地址和端口转换行为
- 7.3.3 过滤行为
- 7.3.4 位于NAT之后的服务器
- 7.3.5 发夹和NAT环回
- 7.3.6 NAT编辑器
- 7.3.7 服务提供者NAT和服务提供者IPv6转换
- 7.4 NAT穿越
- 7.4.1 针孔和打孔
- 7.4.2 单边的自地址确定
- 7.4.3 NAT的会话穿越工具
- 7.4.4 利用NAT中继的穿越
- 7.4.5 交互连接建立
- 7.5 配置包过滤防火墙和NAT
- 7.5.1 防火墙规则
- 7.5.2 NAT规则
- 7.5.3 与NAT和防火墙的直接交互：UPnP、NAT-PMP和PCP
- 7.6 IPv4/IPv6共存和过渡中的NAT
- 7.6.1 双协议栈精简版
- 7.6.2 使用NAT和ALG的IPv4/IPv6转换
- 7.7 与防火墙和NAT相关的攻击
- 7.8 总结
- 7.9 参考文献

<Stepper>
  <Step title="复原报文与协议边界">
    固定版次、拓扑和观察点，逐层标注原始字节、字段、方向与封装。
    <Tip207FirewallsNatProtocolLab />
  </Step>
  <Step title="推演状态与首个偏差">
    只改变一个变量，比较正常与故障流程的首个不同报文、状态或计时器。
    <Tip207FirewallsNatStateLab />
  </Step>
  <Step title="关闭独立证据门">
    让复核者从原始记录独立重放诊断、修复和恢复过程。
    </Step>
</Stepper>

## 可复现实验记录

```text
packet = timestamp + direction + five_tuple + header_fields + raw_offset
state  = before + event + guard + action + timer + after
audit  = expected + observed + first_difference + alternative + recovery
```

```bash
tcpdump -nn -s0 -w tip2-lab.pcap 'host 192.0.2.10'
tshark -r tip2-lab.pcap -T fields -e frame.number -e frame.time_relative -e ip.src -e ip.dst -e tcp.seq -e tcp.ack
```

动手试：采集一次正常事务、一次单变量故障和一次恢复事务。对每条结论记录文件哈希、关键帧、字节偏移、状态前后值、竞争解释和独立复核结果。

<Callout type="trap" title="从最终错误倒推根因">
  RST、超时、名称解析失败或应用报错通常是传播后的症状。必须先定位第一个违反协议预测的报文、状态或计时器。
</Callout>

<Callout type="trap" title="用现代协议覆盖第2版">
  原书包含TLS 1.2与BIC/CUBIC，但不包含QUIC、TLS
  1.3或BBR。现代差异应独立标注，不能删除或改写491个正式节点。
</Callout>

## 独立证据门

最小证据包包含：正式节点、版次、拓扑、抓包位置、原始报文哈希、字段偏移、状态/计时器、单变量故障、恢复、偏差、责任人与复核人。

## 练习

<Exercises>

**问题 1：为什么“第7章 防火墙和网络地址转换”必须锁定第2版？**

<Answer>
  固定版次才能让3篇前置材料、18章、468个编号节/小节、缩略语表和索引与2011/2012协议语境一一对应；现代主题只能独立比较。
</Answer>

**问题 2：怎样构造“把NAT等同于防火墙，或只观察单向元组，会误判过滤、发夹和穿越行为”的最小反例？**

<Answer>
  固定版次、拓扑、原始流量和其他条件，只改变一个字段、状态、计时器或路径变量；比较首个不同报文、字节和状态转移，再恢复变量复测。
</Answer>

**问题 3：何时可以认为本页完成独立交接？**

<Answer>
  另一位学习者无需口头补充即可定位全部29个本页节点，复现正常、故障与恢复流程，并依据“每次允许、拒绝或转换都能指出入站元组、规则、状态、出站元组与回程映射”得到相同结论。
</Answer>

</Exercises>

## 本章回顾

“第7章 防火墙和网络地址转换”的核心是以状态表和五元组解释包过滤、代理、防火墙规则、NAT行为与穿越机制。真正掌握不是记住名词，而是能从原始字节、状态机、计时器和单变量反例证明协议为何如此运行。

<Glossary>
  <GlossaryItem term="包过滤">
    包过滤是“第7章
    防火墙和网络地址转换”中的关键证据入口。掌握标准是能在原始报文、状态转换或规范边界中定位，并构造一个最小失败反例。
  </GlossaryItem>
  <GlossaryItem term="NAPT">
    NAPT是“第7章
    防火墙和网络地址转换”中的关键证据入口。掌握标准是能在原始报文、状态转换或规范边界中定位，并构造一个最小失败反例。
  </GlossaryItem>
  <GlossaryItem term="NAT穿越">
    NAT穿越是“第7章
    防火墙和网络地址转换”中的关键证据入口。掌握标准是能在原始报文、状态转换或规范边界中定位，并构造一个最小失败反例。
  </GlossaryItem>
  <GlossaryItem term="STUN">
    STUN是“第7章
    防火墙和网络地址转换”中的关键证据入口。掌握标准是能在原始报文、状态转换或规范边界中定位，并构造一个最小失败反例。
  </GlossaryItem>
  <GlossaryItem term="IPv4/IPv6转换">
    IPv4/IPv6转换是“第7章
    防火墙和网络地址转换”中的关键证据入口。掌握标准是能在原始报文、状态转换或规范边界中定位，并构造一个最小失败反例。
  </GlossaryItem>
</Glossary>

[← 上一页：第6章 系统配置：DHCP和自动配置](/learn/tcp-ip-illustrated-vol1/09-tip2-06-dhcp-autoconfiguration/tip2-06-dhcp-autoconfiguration) · [下一页：第8章 ICMPv4和ICMPv6：Internet控制报文协议 →](/learn/tcp-ip-illustrated-vol1/11-tip2-08-icmp/tip2-08-icmp)

<Attribution
  adaptedFrom="Fall与Stevens《TCP/IP Illustrated, Volume 1, Second Edition》"
  adaptedUrl="https://www.informit.com/store/tcp-ip-illustrated-volume-1-the-protocols-9780321336316"
/>
