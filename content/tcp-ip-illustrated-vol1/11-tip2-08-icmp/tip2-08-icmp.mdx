---
title: "第8章 ICMPv4和ICMPv6：Internet控制报文协议"
description: "对照ICMPv4/v6类型与代码、差错引用、查询报文、邻居发现和协议转换"
type: A
section: "TCP/IP详解 卷1：协议（原书第2版 / 2016）"
order: 11
draft: false
math: false
sourceUrl: "https://www.informit.com/store/tcp-ip-illustrated-volume-1-the-protocols-9780321336316"
demo: true
---

# 第8章 ICMPv4和ICMPv6：Internet控制报文协议

本课程锁定Kevin R. Fall、W. Richard Stevens著《TCP/IP Illustrated, Volume 1: The Protocols, Second Edition》，Addison-Wesley Professional，2011年11月首印、版权2012年，1056页，ISBN 9780321336316；吴英、张玉、许昱玮译《TCP/IP详解 卷1：协议（原书第2版）》，机械工业出版社，2016年6月，683页，ISBN 9787111453833。InformIT/Pearson正式目录与出版社预览确认3篇前置材料、18章、468个编号节/小节、缩略语表和索引，共23个正式单元、491个正式节点；旧10页泛化课程已整体替换。


## 从可证伪的协议预测开始

本页主问题是：对照ICMPv4/v6类型与代码、差错引用、查询报文、邻居发现和协议转换。先写预测，再固定端点、接口、路由、MTU、时钟、实现版本与抓包位置。验收不变量是：ICMP判断包含版本、类型、代码、被引用报文、发送限制和触发条件。反例是：笼统封禁ICMP会破坏PMTUD、邻居发现和错误诊断，也不能自动消除攻击面。最终应用错误、协议标签或单张截图都只是线索，不能独立证明根因。

## 核心词汇与第2版边界

、、、、
版次固定在2011年英文第2版与2016年中译本。保留原书明确讲授的IPv6、DNS64、TLS 1.2、DTLS、BIC/CUBIC、EAP、IPsec、DNSSEC和DKIM；QUIC、HTTP/3、TLS 1.3、WPA3、BBR等后续主题只能另作比较，不能替换正式节点。


## 五层协议证据模型

### 第一层：先确定观察边界

画出端点、接口、交换/路由节点、NAT或隧道，并标出抓包点能看到和看不到的流量。保存接口、链路类型、MTU、地址、路由表、时间源和实现版本；否则本机卸载、镜像丢包或非对称路径会被误判为协议行为。

### 第二层：让原始字节约束解释

对关键报文同时保留时间、方向、五元组、链路封装、十六进制偏移和解析字段。校验长度、标志、校验和与上下层边界；解析器、名称解析和专家提示都是可变解释层，必须接受原始字节反证。

### 第三层：按状态和计时器重建因果

把请求/响应、Seq/Ack、租约、成员关系、缓存、重传或密钥协商排成有方向的时间线。明确每个输入触发的状态转移、计时器启动/取消和输出报文，先找到首个不满足预测的事件，再解释传播。

### 第四层：做单变量对照

固定拓扑、流量和实现，只改变一个地址、选项、计时器、规则、窗口或丢包条件。保存正常、故障和恢复三段记录，并列出至少一个竞争解释；只有能排除竞争解释，才能把相关性提升为根因。

### 第五层：关闭独立证据门

复核者只拿只读报文、哈希、命令、配置和预期输出，必须能定位同一个首个偏差并得到同一结论。历史工具或字段与现代实现不同时，分别记录第2版行为和现代差异，不能静默改写原书。

```text
observation = edition + topology + capture_point + direction + raw_bytes
mechanism   = input + protocol_state + timer + output + invariant
claim       = prediction + first_deviation + competing_hypothesis + recovery
```

## 原书目录逐节点重构

### 第8章 ICMPv4和ICMPv6：Internet控制报文协议

对“第8章 ICMPv4和ICMPv6：Internet控制报文协议”记录协议版本、类型、代码、引用的原报文与发送限制；用一个允许样本和一个禁止样本验证触发条件。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“第8章 ICMPv4和ICMPv6：Internet控制报文协议”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。 同时画出本单元与上一层、下一层协议的接口。

### 8.1 引言

用“8.1 引言”校准本章问题、证据范围和标准来源；总结必须回到数据包字段、状态转移或实现观察，不能只列术语。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“8.1 引言”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 8.1.1 在IPv4和IPv6中的封装

把“8.1.1 在IPv4和IPv6中的封装”放回“对照ICMPv4/v6类型与代码、差错引用、查询报文、邻居发现和协议转换”的协议路径，标注输入、状态、报文字段、输出与最小失败反例。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“8.1.1 在IPv4和IPv6中的封装”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

### 8.2 ICMP报文

对“8.2 ICMP报文”记录协议版本、类型、代码、引用的原报文与发送限制；用一个允许样本和一个禁止样本验证触发条件。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“8.2 ICMP报文”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 8.2.1 ICMPv4报文

对“8.2.1 ICMPv4报文”记录协议版本、类型、代码、引用的原报文与发送限制；用一个允许样本和一个禁止样本验证触发条件。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“8.2.1 ICMPv4报文”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 8.2.2 ICMPv6报文

对“8.2.2 ICMPv6报文”记录协议版本、类型、代码、引用的原报文与发送限制；用一个允许样本和一个禁止样本验证触发条件。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“8.2.2 ICMPv6报文”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 8.2.3 处理ICMP报文

对“8.2.3 处理ICMP报文”记录协议版本、类型、代码、引用的原报文与发送限制；用一个允许样本和一个禁止样本验证触发条件。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“8.2.3 处理ICMP报文”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

### 8.3 ICMP差错报文

对“8.3 ICMP差错报文”记录协议版本、类型、代码、引用的原报文与发送限制；用一个允许样本和一个禁止样本验证触发条件。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“8.3 ICMP差错报文”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 8.3.1 扩展的ICMP和多部报文

对“8.3.1 扩展的ICMP和多部报文”记录协议版本、类型、代码、引用的原报文与发送限制；用一个允许样本和一个禁止样本验证触发条件。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“8.3.1 扩展的ICMP和多部报文”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 8.3.2 目的不可达（ICMPv4类型3,ICMPv6类型1）和数据包太大（ICMPv6类型2）

对“8.3.2 目的不可达（ICMPv4类型3,ICMPv6类型1）和数据包太大（ICMPv6类型2）”记录协议版本、类型、代码、引用的原报文与发送限制；用一个允许样本和一个禁止样本验证触发条件。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“8.3.2 目的不可达（ICMPv4类型3,ICMPv6类型1）和数据包太大（ICMPv6类型2）”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 8.3.3 重定向（ICMPv4类型5,ICMPv6类型137）

对“8.3.3 重定向（ICMPv4类型5,ICMPv6类型137）”记录协议版本、类型、代码、引用的原报文与发送限制；用一个允许样本和一个禁止样本验证触发条件。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“8.3.3 重定向（ICMPv4类型5,ICMPv6类型137）”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 8.3.4 ICMP超时（ICMPv4类型11,ICMPv6类型3）

对“8.3.4 ICMP超时（ICMPv4类型11,ICMPv6类型3）”记录协议版本、类型、代码、引用的原报文与发送限制；用一个允许样本和一个禁止样本验证触发条件。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“8.3.4 ICMP超时（ICMPv4类型11,ICMPv6类型3）”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 8.3.5 参数问题（ICMPv4类型12,ICMPv6类型4）

对“8.3.5 参数问题（ICMPv4类型12,ICMPv6类型4）”记录协议版本、类型、代码、引用的原报文与发送限制；用一个允许样本和一个禁止样本验证触发条件。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“8.3.5 参数问题（ICMPv4类型12,ICMPv6类型4）”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

### 8.4 ICMP查询／信息类报文

对“8.4 ICMP查询／信息类报文”记录协议版本、类型、代码、引用的原报文与发送限制；用一个允许样本和一个禁止样本验证触发条件。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“8.4 ICMP查询／信息类报文”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 8.4.1 回显请求／应答（ping）（ICMPv4类型0/8,ICMPv6类型129/128）

对“8.4.1 回显请求／应答（ping）（ICMPv4类型0/8,ICMPv6类型129/128）”记录协议版本、类型、代码、引用的原报文与发送限制；用一个允许样本和一个禁止样本验证触发条件。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“8.4.1 回显请求／应答（ping）（ICMPv4类型0/8,ICMPv6类型129/128）”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 8.4.2 路由器发现：路由器请求和通告（ICMPv4类型9,10）

对“8.4.2 路由器发现：路由器请求和通告（ICMPv4类型9,10）”记录协议版本、类型、代码、引用的原报文与发送限制；用一个允许样本和一个禁止样本验证触发条件。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“8.4.2 路由器发现：路由器请求和通告（ICMPv4类型9,10）”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 8.4.3 本地代理地址发现请求／应答（ICMPv6类型144/145）

对“8.4.3 本地代理地址发现请求／应答（ICMPv6类型144/145）”写出位级表示、前缀边界、作用域和正反例，并用两个相邻前缀验证聚合或匹配结果。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“8.4.3 本地代理地址发现请求／应答（ICMPv6类型144/145）”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 8.4.4 移动前缀请求／通告（ICMPv6类型146/147）

对“8.4.4 移动前缀请求／通告（ICMPv6类型146/147）”写出位级表示、前缀边界、作用域和正反例，并用两个相邻前缀验证聚合或匹配结果。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“8.4.4 移动前缀请求／通告（ICMPv6类型146/147）”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 8.4.5 移动IPv6快速切换报文（ICMPv6类型154）

对“8.4.5 移动IPv6快速切换报文（ICMPv6类型154）”记录协议版本、类型、代码、引用的原报文与发送限制；用一个允许样本和一个禁止样本验证触发条件。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“8.4.5 移动IPv6快速切换报文（ICMPv6类型154）”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 8.4.6 组播侦听查询／报告／完成（ICMPv6类型130/131/132）

对“8.4.6 组播侦听查询／报告／完成（ICMPv6类型130/131/132）”记录协议版本、类型、代码、引用的原报文与发送限制；用一个允许样本和一个禁止样本验证触发条件。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“8.4.6 组播侦听查询／报告／完成（ICMPv6类型130/131/132）”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 8.4.7 版本2组播侦听发现（ICMPv6类型143）

对“8.4.7 版本2组播侦听发现（ICMPv6类型143）”记录协议版本、类型、代码、引用的原报文与发送限制；用一个允许样本和一个禁止样本验证触发条件。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“8.4.7 版本2组播侦听发现（ICMPv6类型143）”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 8.4.8 组播路由器发现（IGMP类型48/49/50,ICMPv6类型151/152/153）

对“8.4.8 组播路由器发现（IGMP类型48/49/50,ICMPv6类型151/152/153）”记录协议版本、类型、代码、引用的原报文与发送限制；用一个允许样本和一个禁止样本验证触发条件。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“8.4.8 组播路由器发现（IGMP类型48/49/50,ICMPv6类型151/152/153）”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

### 8.5 IPv6中的邻居发现

对“8.5 IPv6中的邻居发现”记录协议版本、类型、代码、引用的原报文与发送限制；用一个允许样本和一个禁止样本验证触发条件。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“8.5 IPv6中的邻居发现”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 8.5.1 ICMPv6路由器请求和通告（ICMPv6类型133,134）

对“8.5.1 ICMPv6路由器请求和通告（ICMPv6类型133,134）”记录协议版本、类型、代码、引用的原报文与发送限制；用一个允许样本和一个禁止样本验证触发条件。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“8.5.1 ICMPv6路由器请求和通告（ICMPv6类型133,134）”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 8.5.2 ICMPv6邻居请求和通告（ICMPv6类型135,136）

对“8.5.2 ICMPv6邻居请求和通告（ICMPv6类型135,136）”记录协议版本、类型、代码、引用的原报文与发送限制；用一个允许样本和一个禁止样本验证触发条件。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“8.5.2 ICMPv6邻居请求和通告（ICMPv6类型135,136）”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 8.5.3 ICMPv6反向邻居发现请求／通告（ICMPv6类型141/142）

对“8.5.3 ICMPv6反向邻居发现请求／通告（ICMPv6类型141/142）”记录协议版本、类型、代码、引用的原报文与发送限制；用一个允许样本和一个禁止样本验证触发条件。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“8.5.3 ICMPv6反向邻居发现请求／通告（ICMPv6类型141/142）”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 8.5.4 邻居不可达检测

对“8.5.4 邻居不可达检测”记录协议版本、类型、代码、引用的原报文与发送限制；用一个允许样本和一个禁止样本验证触发条件。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“8.5.4 邻居不可达检测”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 8.5.5 安全邻居发现

对“8.5.5 安全邻居发现”记录协议版本、类型、代码、引用的原报文与发送限制；用一个允许样本和一个禁止样本验证触发条件。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“8.5.5 安全邻居发现”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 8.5.6 ICMPv6邻居发现选项

对“8.5.6 ICMPv6邻居发现选项”记录协议版本、类型、代码、引用的原报文与发送限制；用一个允许样本和一个禁止样本验证触发条件。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“8.5.6 ICMPv6邻居发现选项”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

### 8.6 ICMPv4和ICMPv6转换

对“8.6 ICMPv4和ICMPv6转换”记录协议版本、类型、代码、引用的原报文与发送限制；用一个允许样本和一个禁止样本验证触发条件。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“8.6 ICMPv4和ICMPv6转换”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 8.6.1 从ICMPv4转换到ICMPv6

对“8.6.1 从ICMPv4转换到ICMPv6”记录协议版本、类型、代码、引用的原报文与发送限制；用一个允许样本和一个禁止样本验证触发条件。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“8.6.1 从ICMPv4转换到ICMPv6”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 8.6.2 从ICMPv6转换到ICMPv4

对“8.6.2 从ICMPv6转换到ICMPv4”记录协议版本、类型、代码、引用的原报文与发送限制；用一个允许样本和一个禁止样本验证触发条件。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“8.6.2 从ICMPv6转换到ICMPv4”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

### 8.7 与ICMP相关的攻击

对“8.7 与ICMP相关的攻击”记录协议版本、类型、代码、引用的原报文与发送限制；用一个允许样本和一个禁止样本验证触发条件。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“8.7 与ICMP相关的攻击”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

### 8.8 总结

用“8.8 总结”校准本章问题、证据范围和标准来源；总结必须回到数据包字段、状态转移或实现观察，不能只列术语。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“8.8 总结”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

### 8.9 参考文献

用“8.9 参考文献”校准本章问题、证据范围和标准来源；总结必须回到数据包字段、状态转移或实现观察，不能只列术语。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“8.9 参考文献”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

## 本页完整节点清单

- 第8章 ICMPv4和ICMPv6：Internet控制报文协议
- 8.1 引言
- 8.1.1 在IPv4和IPv6中的封装
- 8.2 ICMP报文
- 8.2.1 ICMPv4报文
- 8.2.2 ICMPv6报文
- 8.2.3 处理ICMP报文
- 8.3 ICMP差错报文
- 8.3.1 扩展的ICMP和多部报文
- 8.3.2 目的不可达（ICMPv4类型3,ICMPv6类型1）和数据包太大（ICMPv6类型2）
- 8.3.3 重定向（ICMPv4类型5,ICMPv6类型137）
- 8.3.4 ICMP超时（ICMPv4类型11,ICMPv6类型3）
- 8.3.5 参数问题（ICMPv4类型12,ICMPv6类型4）
- 8.4 ICMP查询／信息类报文
- 8.4.1 回显请求／应答（ping）（ICMPv4类型0/8,ICMPv6类型129/128）
- 8.4.2 路由器发现：路由器请求和通告（ICMPv4类型9,10）
- 8.4.3 本地代理地址发现请求／应答（ICMPv6类型144/145）
- 8.4.4 移动前缀请求／通告（ICMPv6类型146/147）
- 8.4.5 移动IPv6快速切换报文（ICMPv6类型154）
- 8.4.6 组播侦听查询／报告／完成（ICMPv6类型130/131/132）
- 8.4.7 版本2组播侦听发现（ICMPv6类型143）
- 8.4.8 组播路由器发现（IGMP类型48/49/50,ICMPv6类型151/152/153）
- 8.5 IPv6中的邻居发现
- 8.5.1 ICMPv6路由器请求和通告（ICMPv6类型133,134）
- 8.5.2 ICMPv6邻居请求和通告（ICMPv6类型135,136）
- 8.5.3 ICMPv6反向邻居发现请求／通告（ICMPv6类型141/142）
- 8.5.4 邻居不可达检测
- 8.5.5 安全邻居发现
- 8.5.6 ICMPv6邻居发现选项
- 8.6 ICMPv4和ICMPv6转换
- 8.6.1 从ICMPv4转换到ICMPv6
- 8.6.2 从ICMPv6转换到ICMPv4
- 8.7 与ICMP相关的攻击
- 8.8 总结
- 8.9 参考文献


## 可复现实验记录

```text
packet = timestamp + direction + five_tuple + header_fields + raw_offset
state  = before + event + guard + action + timer + after
audit  = expected + observed + first_difference + alternative + recovery
```

```bash
tcpdump -nn -s0 -w tip2-lab.pcap 'host 192.0.2.10'
tshark -r tip2-lab.pcap -T fields -e frame.number -e frame.time_relative -e ip.src -e ip.dst -e tcp.seq -e tcp.ack
```

动手试：采集一次正常事务、一次单变量故障和一次恢复事务。对每条结论记录文件哈希、关键帧、字节偏移、状态前后值、竞争解释和独立复核结果。


## 独立证据门

最小证据包包含：正式节点、版次、拓扑、抓包位置、原始报文哈希、字段偏移、状态/计时器、单变量故障、恢复、偏差、责任人与复核人。

## 小结

- ICMP 传递差错与控制消息
- ping 与 traceroute 基于 ICMP
- 差错报告不嵌套递归
- 路径 MTU 发现依赖 ICMP
- 过滤 ICMP 要权衡诊断能力

## 练习


## 本章回顾

“第8章 ICMPv4和ICMPv6：Internet控制报文协议”的核心是对照ICMPv4/v6类型与代码、差错引用、查询报文、邻居发现和协议转换。真正掌握不是记住名词，而是能从原始字节、状态机、计时器和单变量反例证明协议为何如此运行。


[← 上一页：第7章 防火墙和网络地址转换](/learn/tcp-ip-illustrated-vol1/10-tip2-07-firewalls-nat/tip2-07-firewalls-nat) · [下一页：第9章 广播和本地组播（IGMP和MLD） →](/learn/tcp-ip-illustrated-vol1/12-tip2-09-broadcast-multicast/tip2-09-broadcast-multicast)

<Attribution
  adaptedFrom="《TCP/IP详解 卷1：协议》原书第2版"
  adaptedUrl="https://www.informit.com/store/tcp-ip-illustrated-volume-1-the-protocols-9780321336316"
/>
