---
title: "第13章 TCP连接管理"
description: "推演建立、终止、选项、PMTUD、状态转换、复位、TIME_WAIT与服务器队列"
type: A
section: "TCP/IP详解 卷1：协议（原书第2版 / 2016）"
order: 16
draft: false
math: false
sourceUrl: "https://www.informit.com/store/tcp-ip-illustrated-volume-1-the-protocols-9780321336316"
demo: true
---

# 第13章 TCP连接管理

本课程锁定Kevin R. Fall、W. Richard Stevens著《TCP/IP Illustrated, Volume 1: The Protocols, Second Edition》，Addison-Wesley Professional，2011年11月首印、版权2012年，1056页，ISBN 9780321336316；吴英、张玉、许昱玮译《TCP/IP详解 卷1：协议（原书第2版）》，机械工业出版社，2016年6月，683页，ISBN 9787111453833。InformIT/Pearson正式目录与出版社预览确认3篇前置材料、18章、468个编号节/小节、缩略语表和索引，共23个正式单元、491个正式节点；旧10页泛化课程已整体替换。


## 从可证伪的协议预测开始

本页主问题是：推演建立、终止、选项、PMTUD、状态转换、复位、TIME_WAIT与服务器队列。先写预测，再固定端点、接口、路由、MTU、时钟、实现版本与抓包位置。验收不变量是：连接状态与SYN/FIN/RST、序号空间、选项协商、计时器和端点四元组一致。反例是：只背三次握手和四次挥手会遗漏同时打开、半关闭、复位、半开与监听队列。最终应用错误、协议标签或单张截图都只是线索，不能独立证明根因。

## 核心词汇与第2版边界

、、、、
版次固定在2011年英文第2版与2016年中译本。保留原书明确讲授的IPv6、DNS64、TLS 1.2、DTLS、BIC/CUBIC、EAP、IPsec、DNSSEC和DKIM；QUIC、HTTP/3、TLS 1.3、WPA3、BBR等后续主题只能另作比较，不能替换正式节点。


## 五层协议证据模型

### 第一层：先确定观察边界

画出端点、接口、交换/路由节点、NAT或隧道，并标出抓包点能看到和看不到的流量。保存接口、链路类型、MTU、地址、路由表、时间源和实现版本；否则本机卸载、镜像丢包或非对称路径会被误判为协议行为。

### 第二层：让原始字节约束解释

对关键报文同时保留时间、方向、五元组、链路封装、十六进制偏移和解析字段。校验长度、标志、校验和与上下层边界；解析器、名称解析和专家提示都是可变解释层，必须接受原始字节反证。

### 第三层：按状态和计时器重建因果

把请求/响应、Seq/Ack、租约、成员关系、缓存、重传或密钥协商排成有方向的时间线。明确每个输入触发的状态转移、计时器启动/取消和输出报文，先找到首个不满足预测的事件，再解释传播。

### 第四层：做单变量对照

固定拓扑、流量和实现，只改变一个地址、选项、计时器、规则、窗口或丢包条件。保存正常、故障和恢复三段记录，并列出至少一个竞争解释；只有能排除竞争解释，才能把相关性提升为根因。

### 第五层：关闭独立证据门

复核者只拿只读报文、哈希、命令、配置和预期输出，必须能定位同一个首个偏差并得到同一结论。历史工具或字段与现代实现不同时，分别记录第2版行为和现代差异，不能静默改写原书。

```text
observation = edition + topology + capture_point + direction + raw_bytes
mechanism   = input + protocol_state + timer + output + invariant
claim       = prediction + first_deviation + competing_hypothesis + recovery
```

## 原书目录逐节点重构

### 第13章 TCP连接管理

把“第13章 TCP连接管理”放进TCP字节序号和状态机：记录四元组、Seq/Ack、标志、窗口、选项、计时器和应用读写边界。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“第13章 TCP连接管理”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。 同时画出本单元与上一层、下一层协议的接口。

### 13.1 引言

用“13.1 引言”校准本章问题、证据范围和标准来源；总结必须回到数据包字段、状态转移或实现观察，不能只列术语。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“13.1 引言”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

### 13.2 TCP连接的建立与终止

把“13.2 TCP连接的建立与终止”放进TCP字节序号和状态机：记录四元组、Seq/Ack、标志、窗口、选项、计时器和应用读写边界。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“13.2 TCP连接的建立与终止”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 13.2.1 TCP半关闭

把“13.2.1 TCP半关闭”放进TCP字节序号和状态机：记录四元组、Seq/Ack、标志、窗口、选项、计时器和应用读写边界。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“13.2.1 TCP半关闭”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 13.2.2 同时打开与关闭

把“13.2.2 同时打开与关闭”放回“推演建立、终止、选项、PMTUD、状态转换、复位、TIME_WAIT与服务器队列”的协议路径，标注输入、状态、报文字段、输出与最小失败反例。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“13.2.2 同时打开与关闭”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 13.2.3 初始序列号

把“13.2.3 初始序列号”放进TCP字节序号和状态机：记录四元组、Seq/Ack、标志、窗口、选项、计时器和应用读写边界。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“13.2.3 初始序列号”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 13.2.4 例子

把“13.2.4 例子”放回“推演建立、终止、选项、PMTUD、状态转换、复位、TIME_WAIT与服务器队列”的协议路径，标注输入、状态、报文字段、输出与最小失败反例。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“13.2.4 例子”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 13.2.5 连接建立超时

围绕“13.2.5 连接建立超时”排列原始发送、ACK/SACK、RTT样本、计时器到期与重发，加入重排或抓包重复作为竞争解释。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“13.2.5 连接建立超时”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 13.2.6 连接与转换器

把“13.2.6 连接与转换器”放进TCP字节序号和状态机：记录四元组、Seq/Ack、标志、窗口、选项、计时器和应用读写边界。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“13.2.6 连接与转换器”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

### 13.3 TCP选项

把“13.3 TCP选项”放进TCP字节序号和状态机：记录四元组、Seq/Ack、标志、窗口、选项、计时器和应用读写边界。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“13.3 TCP选项”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 13.3.1 最大段大小选项

把“13.3.1 最大段大小选项”放回“推演建立、终止、选项、PMTUD、状态转换、复位、TIME_WAIT与服务器队列”的协议路径，标注输入、状态、报文字段、输出与最小失败反例。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“13.3.1 最大段大小选项”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 13.3.2 选择确认选项

把“13.3.2 选择确认选项”放回“推演建立、终止、选项、PMTUD、状态转换、复位、TIME_WAIT与服务器队列”的协议路径，标注输入、状态、报文字段、输出与最小失败反例。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“13.3.2 选择确认选项”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 13.3.3 窗口缩放选项

把“13.3.3 窗口缩放选项”放进TCP字节序号和状态机：记录四元组、Seq/Ack、标志、窗口、选项、计时器和应用读写边界。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“13.3.3 窗口缩放选项”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 13.3.4 时间戳选项与防回绕序列号

把“13.3.4 时间戳选项与防回绕序列号”放进TCP字节序号和状态机：记录四元组、Seq/Ack、标志、窗口、选项、计时器和应用读写边界。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“13.3.4 时间戳选项与防回绕序列号”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 13.3.5 用户超时选项

围绕“13.3.5 用户超时选项”排列原始发送、ACK/SACK、RTT样本、计时器到期与重发，加入重排或抓包重复作为竞争解释。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“13.3.5 用户超时选项”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 13.3.6 认证选项

为“13.3.6 认证选项”先写资产、攻击者能力与信任根，再标明认证对象、密钥材料、保护层、重放边界和失败处置。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“13.3.6 认证选项”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

### 13.4 TCP的路径最大传输单元发现

把“13.4 TCP的路径最大传输单元发现”放进TCP字节序号和状态机：记录四元组、Seq/Ack、标志、窗口、选项、计时器和应用读写边界。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“13.4 TCP的路径最大传输单元发现”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 13.4.1 例子

把“13.4.1 例子”放回“推演建立、终止、选项、PMTUD、状态转换、复位、TIME_WAIT与服务器队列”的协议路径，标注输入、状态、报文字段、输出与最小失败反例。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“13.4.1 例子”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

### 13.5 TCP状态转换

把“13.5 TCP状态转换”放进TCP字节序号和状态机：记录四元组、Seq/Ack、标志、窗口、选项、计时器和应用读写边界。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“13.5 TCP状态转换”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 13.5.1 TCP状态转换图

把“13.5.1 TCP状态转换图”放进TCP字节序号和状态机：记录四元组、Seq/Ack、标志、窗口、选项、计时器和应用读写边界。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“13.5.1 TCP状态转换图”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 13.5.2 TIME_WAIT状态

把“13.5.2 TIME_WAIT状态”放回“推演建立、终止、选项、PMTUD、状态转换、复位、TIME_WAIT与服务器队列”的协议路径，标注输入、状态、报文字段、输出与最小失败反例。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“13.5.2 TIME_WAIT状态”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 13.5.3 静默时间的概念

把“13.5.3 静默时间的概念”放回“推演建立、终止、选项、PMTUD、状态转换、复位、TIME_WAIT与服务器队列”的协议路径，标注输入、状态、报文字段、输出与最小失败反例。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“13.5.3 静默时间的概念”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 13.5.4 FIN_WAIT_2状态

把“13.5.4 FIN_WAIT_2状态”放进TCP字节序号和状态机：记录四元组、Seq/Ack、标志、窗口、选项、计时器和应用读写边界。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“13.5.4 FIN_WAIT_2状态”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 13.5.5 同时打开与关闭的转换

把“13.5.5 同时打开与关闭的转换”放回“推演建立、终止、选项、PMTUD、状态转换、复位、TIME_WAIT与服务器队列”的协议路径，标注输入、状态、报文字段、输出与最小失败反例。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“13.5.5 同时打开与关闭的转换”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

### 13.6 重置报文段

把“13.6 重置报文段”放回“推演建立、终止、选项、PMTUD、状态转换、复位、TIME_WAIT与服务器队列”的协议路径，标注输入、状态、报文字段、输出与最小失败反例。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“13.6 重置报文段”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 13.6.1 针对不存在端口的连接请求

把“13.6.1 针对不存在端口的连接请求”放进TCP字节序号和状态机：记录四元组、Seq/Ack、标志、窗口、选项、计时器和应用读写边界。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“13.6.1 针对不存在端口的连接请求”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 13.6.2 终止一条连接

把“13.6.2 终止一条连接”放进TCP字节序号和状态机：记录四元组、Seq/Ack、标志、窗口、选项、计时器和应用读写边界。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“13.6.2 终止一条连接”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 13.6.3 半开连接

把“13.6.3 半开连接”放进TCP字节序号和状态机：记录四元组、Seq/Ack、标志、窗口、选项、计时器和应用读写边界。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“13.6.3 半开连接”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 13.6.4 时间等待错误

把“13.6.4 时间等待错误”放回“推演建立、终止、选项、PMTUD、状态转换、复位、TIME_WAIT与服务器队列”的协议路径，标注输入、状态、报文字段、输出与最小失败反例。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“13.6.4 时间等待错误”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

### 13.7 TCP服务器选项

把“13.7 TCP服务器选项”放进TCP字节序号和状态机：记录四元组、Seq/Ack、标志、窗口、选项、计时器和应用读写边界。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“13.7 TCP服务器选项”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 13.7.1 TCP端口号

把“13.7.1 TCP端口号”放进TCP字节序号和状态机：记录四元组、Seq/Ack、标志、窗口、选项、计时器和应用读写边界。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“13.7.1 TCP端口号”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 13.7.2 限制本地IP地址

对“13.7.2 限制本地IP地址”写出位级表示、前缀边界、作用域和正反例，并用两个相邻前缀验证聚合或匹配结果。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“13.7.2 限制本地IP地址”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 13.7.3 限制外部节点

把“13.7.3 限制外部节点”放回“推演建立、终止、选项、PMTUD、状态转换、复位、TIME_WAIT与服务器队列”的协议路径，标注输入、状态、报文字段、输出与最小失败反例。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“13.7.3 限制外部节点”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 13.7.4 进入连接队列

对“13.7.4 进入连接队列”逐事件更新cwnd、ssthresh与flight size，说明ACK或丢失信号为何触发当前分支，并与接收窗口分离。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“13.7.4 进入连接队列”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

### 13.8 与TCP连接管理相关的攻击

把“13.8 与TCP连接管理相关的攻击”放进TCP字节序号和状态机：记录四元组、Seq/Ack、标志、窗口、选项、计时器和应用读写边界。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“13.8 与TCP连接管理相关的攻击”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

### 13.9 总结

用“13.9 总结”校准本章问题、证据范围和标准来源；总结必须回到数据包字段、状态转移或实现观察，不能只列术语。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“13.9 总结”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

### 13.10 参考文献

用“13.10 参考文献”校准本章问题、证据范围和标准来源；总结必须回到数据包字段、状态转移或实现观察，不能只列术语。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“13.10 参考文献”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

## 本页完整节点清单

- 第13章 TCP连接管理
- 13.1 引言
- 13.2 TCP连接的建立与终止
- 13.2.1 TCP半关闭
- 13.2.2 同时打开与关闭
- 13.2.3 初始序列号
- 13.2.4 例子
- 13.2.5 连接建立超时
- 13.2.6 连接与转换器
- 13.3 TCP选项
- 13.3.1 最大段大小选项
- 13.3.2 选择确认选项
- 13.3.3 窗口缩放选项
- 13.3.4 时间戳选项与防回绕序列号
- 13.3.5 用户超时选项
- 13.3.6 认证选项
- 13.4 TCP的路径最大传输单元发现
- 13.4.1 例子
- 13.5 TCP状态转换
- 13.5.1 TCP状态转换图
- 13.5.2 TIME_WAIT状态
- 13.5.3 静默时间的概念
- 13.5.4 FIN_WAIT_2状态
- 13.5.5 同时打开与关闭的转换
- 13.6 重置报文段
- 13.6.1 针对不存在端口的连接请求
- 13.6.2 终止一条连接
- 13.6.3 半开连接
- 13.6.4 时间等待错误
- 13.7 TCP服务器选项
- 13.7.1 TCP端口号
- 13.7.2 限制本地IP地址
- 13.7.3 限制外部节点
- 13.7.4 进入连接队列
- 13.8 与TCP连接管理相关的攻击
- 13.9 总结
- 13.10 参考文献


## 可复现实验记录

```text
packet = timestamp + direction + five_tuple + header_fields + raw_offset
state  = before + event + guard + action + timer + after
audit  = expected + observed + first_difference + alternative + recovery
```

```bash
tcpdump -nn -s0 -w tip2-lab.pcap 'host 192.0.2.10'
tshark -r tip2-lab.pcap -T fields -e frame.number -e frame.time_relative -e ip.src -e ip.dst -e tcp.seq -e tcp.ack
```

动手试：采集一次正常事务、一次单变量故障和一次恢复事务。对每条结论记录文件哈希、关键帧、字节偏移、状态前后值、竞争解释和独立复核结果。


## 独立证据门

最小证据包包含：正式节点、版次、拓扑、抓包位置、原始报文哈希、字段偏移、状态/计时器、单变量故障、恢复、偏差、责任人与复核人。

## 练习


## 本章回顾

“第13章 TCP连接管理”的核心是推演建立、终止、选项、PMTUD、状态转换、复位、TIME_WAIT与服务器队列。真正掌握不是记住名词，而是能从原始字节、状态机、计时器和单变量反例证明协议为何如此运行。


[← 上一页：第12章 TCP：传输控制协议（初步）](/learn/tcp-ip-illustrated-vol1/15-tip2-12-tcp-preliminaries/tip2-12-tcp-preliminaries) · [下一页：第14章 TCP超时与重传 →](/learn/tcp-ip-illustrated-vol1/17-tip2-14-tcp-timeout-retransmission/tip2-14-tcp-timeout-retransmission)

