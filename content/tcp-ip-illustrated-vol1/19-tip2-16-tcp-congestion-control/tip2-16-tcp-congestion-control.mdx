---
title: "第16章 TCP拥塞控制"
description: "推演慢启动、拥塞避免、Reno/NewReno、SACK、Eifel、BIC/CUBIC、Vegas与ECN"
type: A
section: "TCP/IP详解 卷1：协议（原书第2版 / 2016）"
order: 19
draft: false
math: false
sourceUrl: "https://www.informit.com/store/tcp-ip-illustrated-volume-1-the-protocols-9780321336316"
demo: true
---

# 第16章 TCP拥塞控制

本课程锁定Kevin R. Fall、W. Richard Stevens著《TCP/IP Illustrated, Volume 1: The Protocols, Second Edition》，Addison-Wesley Professional，2011年11月首印、版权2012年，1056页，ISBN 9780321336316；吴英、张玉、许昱玮译《TCP/IP详解 卷1：协议（原书第2版）》，机械工业出版社，2016年6月，683页，ISBN 9787111453833。InformIT/Pearson正式目录与出版社预览确认3篇前置材料、18章、468个编号节/小节、缩略语表和索引，共23个正式单元、491个正式节点；旧10页泛化课程已整体替换。


## 从可证伪的协议预测开始

本页主问题是：推演慢启动、拥塞避免、Reno/NewReno、SACK、Eifel、BIC/CUBIC、Vegas与ECN。先写预测，再固定端点、接口、路由、MTU、时钟、实现版本与抓包位置。验收不变量是：拥塞结论记录cwnd、ssthresh、flight size、ACK事件、丢失信号和恢复分支。反例是：把接收窗口、拥塞窗口和带宽混为一谈，会误读吞吐下降与算法阶段。最终应用错误、协议标签或单张截图都只是线索，不能独立证明根因。

## 核心词汇与第2版边界

、、、、
版次固定在2011年英文第2版与2016年中译本。保留原书明确讲授的IPv6、DNS64、TLS 1.2、DTLS、BIC/CUBIC、EAP、IPsec、DNSSEC和DKIM；QUIC、HTTP/3、TLS 1.3、WPA3、BBR等后续主题只能另作比较，不能替换正式节点。


## 五层协议证据模型

### 第一层：先确定观察边界

画出端点、接口、交换/路由节点、NAT或隧道，并标出抓包点能看到和看不到的流量。保存接口、链路类型、MTU、地址、路由表、时间源和实现版本；否则本机卸载、镜像丢包或非对称路径会被误判为协议行为。

### 第二层：让原始字节约束解释

对关键报文同时保留时间、方向、五元组、链路封装、十六进制偏移和解析字段。校验长度、标志、校验和与上下层边界；解析器、名称解析和专家提示都是可变解释层，必须接受原始字节反证。

### 第三层：按状态和计时器重建因果

把请求/响应、Seq/Ack、租约、成员关系、缓存、重传或密钥协商排成有方向的时间线。明确每个输入触发的状态转移、计时器启动/取消和输出报文，先找到首个不满足预测的事件，再解释传播。

### 第四层：做单变量对照

固定拓扑、流量和实现，只改变一个地址、选项、计时器、规则、窗口或丢包条件。保存正常、故障和恢复三段记录，并列出至少一个竞争解释；只有能排除竞争解释，才能把相关性提升为根因。

### 第五层：关闭独立证据门

复核者只拿只读报文、哈希、命令、配置和预期输出，必须能定位同一个首个偏差并得到同一结论。历史工具或字段与现代实现不同时，分别记录第2版行为和现代差异，不能静默改写原书。

```text
observation = edition + topology + capture_point + direction + raw_bytes
mechanism   = input + protocol_state + timer + output + invariant
claim       = prediction + first_deviation + competing_hypothesis + recovery
```

## 原书目录逐节点重构

### 第16章 TCP拥塞控制

对“第16章 TCP拥塞控制”逐事件更新cwnd、ssthresh与flight size，说明ACK或丢失信号为何触发当前分支，并与接收窗口分离。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“第16章 TCP拥塞控制”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。 同时画出本单元与上一层、下一层协议的接口。

### 16.1 引言

用“16.1 引言”校准本章问题、证据范围和标准来源；总结必须回到数据包字段、状态转移或实现观察，不能只列术语。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“16.1 引言”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 16.1.1 TCP拥塞检测

对“16.1.1 TCP拥塞检测”逐事件更新cwnd、ssthresh与flight size，说明ACK或丢失信号为何触发当前分支，并与接收窗口分离。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“16.1.1 TCP拥塞检测”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 16.1.2 减缓TCP发送

把“16.1.2 减缓TCP发送”放进TCP字节序号和状态机：记录四元组、Seq/Ack、标志、窗口、选项、计时器和应用读写边界。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“16.1.2 减缓TCP发送”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

### 16.2 一些经典算法

把“16.2 一些经典算法”放回“推演慢启动、拥塞避免、Reno/NewReno、SACK、Eifel、BIC/CUBIC、Vegas与ECN”的协议路径，标注输入、状态、报文字段、输出与最小失败反例。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“16.2 一些经典算法”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 16.2.1 慢启动

对“16.2.1 慢启动”逐事件更新cwnd、ssthresh与flight size，说明ACK或丢失信号为何触发当前分支，并与接收窗口分离。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“16.2.1 慢启动”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 16.2.2 拥塞避免

对“16.2.2 拥塞避免”逐事件更新cwnd、ssthresh与flight size，说明ACK或丢失信号为何触发当前分支，并与接收窗口分离。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“16.2.2 拥塞避免”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 16.2.3 慢启动和拥塞避免的选择

对“16.2.3 慢启动和拥塞避免的选择”逐事件更新cwnd、ssthresh与flight size，说明ACK或丢失信号为何触发当前分支，并与接收窗口分离。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“16.2.3 慢启动和拥塞避免的选择”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 16.2.4 Tahoe、Reno以及快速恢复算法

对“16.2.4 Tahoe、Reno以及快速恢复算法”逐事件更新cwnd、ssthresh与flight size，说明ACK或丢失信号为何触发当前分支，并与接收窗口分离。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“16.2.4 Tahoe、Reno以及快速恢复算法”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 16.2.5 标准TCP

把“16.2.5 标准TCP”放进TCP字节序号和状态机：记录四元组、Seq/Ack、标志、窗口、选项、计时器和应用读写边界。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“16.2.5 标准TCP”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

### 16.3 对标准算法的改进

把“16.3 对标准算法的改进”放回“推演慢启动、拥塞避免、Reno/NewReno、SACK、Eifel、BIC/CUBIC、Vegas与ECN”的协议路径，标注输入、状态、报文字段、输出与最小失败反例。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“16.3 对标准算法的改进”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 16.3.1 NewReno

对“16.3.1 NewReno”逐事件更新cwnd、ssthresh与flight size，说明ACK或丢失信号为何触发当前分支，并与接收窗口分离。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“16.3.1 NewReno”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 16.3.2 采用选择确认机制的TCP拥塞控制

对“16.3.2 采用选择确认机制的TCP拥塞控制”逐事件更新cwnd、ssthresh与flight size，说明ACK或丢失信号为何触发当前分支，并与接收窗口分离。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“16.3.2 采用选择确认机制的TCP拥塞控制”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 16.3.3 转发确认（FACK）和速率减半

把“16.3.3 转发确认（FACK）和速率减半”放进TCP字节序号和状态机：记录四元组、Seq/Ack、标志、窗口、选项、计时器和应用读写边界。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“16.3.3 转发确认（FACK）和速率减半”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 16.3.4 限制传输

把“16.3.4 限制传输”放回“推演慢启动、拥塞避免、Reno/NewReno、SACK、Eifel、BIC/CUBIC、Vegas与ECN”的协议路径，标注输入、状态、报文字段、输出与最小失败反例。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“16.3.4 限制传输”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 16.3.5 拥塞窗口校验

对“16.3.5 拥塞窗口校验”逐事件更新cwnd、ssthresh与flight size，说明ACK或丢失信号为何触发当前分支，并与接收窗口分离。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“16.3.5 拥塞窗口校验”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

### 16.4 伪RTO处理——Eifel响应算法

围绕“16.4 伪RTO处理——Eifel响应算法”排列原始发送、ACK/SACK、RTT样本、计时器到期与重发，加入重排或抓包重复作为竞争解释。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“16.4 伪RTO处理——Eifel响应算法”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

### 16.5 扩展举例

把“16.5 扩展举例”放回“推演慢启动、拥塞避免、Reno/NewReno、SACK、Eifel、BIC/CUBIC、Vegas与ECN”的协议路径，标注输入、状态、报文字段、输出与最小失败反例。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“16.5 扩展举例”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 16.5.1 慢启动行为

对“16.5.1 慢启动行为”逐事件更新cwnd、ssthresh与flight size，说明ACK或丢失信号为何触发当前分支，并与接收窗口分离。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“16.5.1 慢启动行为”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 16.5.2 发送暂停和本地拥塞（事件1）

对“16.5.2 发送暂停和本地拥塞（事件1）”逐事件更新cwnd、ssthresh与flight size，说明ACK或丢失信号为何触发当前分支，并与接收窗口分离。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“16.5.2 发送暂停和本地拥塞（事件1）”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 16.5.3 延伸ACK和本地拥塞恢复

对“16.5.3 延伸ACK和本地拥塞恢复”逐事件更新cwnd、ssthresh与flight size，说明ACK或丢失信号为何触发当前分支，并与接收窗口分离。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“16.5.3 延伸ACK和本地拥塞恢复”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 16.5.4 快速重传和SACK恢复（事件2）

围绕“16.5.4 快速重传和SACK恢复（事件2）”排列原始发送、ACK/SACK、RTT样本、计时器到期与重发，加入重排或抓包重复作为竞争解释。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“16.5.4 快速重传和SACK恢复（事件2）”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 16.5.5 其他本地拥塞和快速重传事件

围绕“16.5.5 其他本地拥塞和快速重传事件”排列原始发送、ACK/SACK、RTT样本、计时器到期与重发，加入重排或抓包重复作为竞争解释。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“16.5.5 其他本地拥塞和快速重传事件”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 16.5.6 超时、重传和撤销cwnd修改

围绕“16.5.6 超时、重传和撤销cwnd修改”排列原始发送、ACK/SACK、RTT样本、计时器到期与重发，加入重排或抓包重复作为竞争解释。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“16.5.6 超时、重传和撤销cwnd修改”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 16.5.7 连接结束

把“16.5.7 连接结束”放进TCP字节序号和状态机：记录四元组、Seq/Ack、标志、窗口、选项、计时器和应用读写边界。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“16.5.7 连接结束”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

### 16.6 共享拥塞状态信息

对“16.6 共享拥塞状态信息”逐事件更新cwnd、ssthresh与flight size，说明ACK或丢失信号为何触发当前分支，并与接收窗口分离。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“16.6 共享拥塞状态信息”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

### 16.7 TCP友好性

把“16.7 TCP友好性”放进TCP字节序号和状态机：记录四元组、Seq/Ack、标志、窗口、选项、计时器和应用读写边界。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“16.7 TCP友好性”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

### 16.8 高速环境下的TCP

把“16.8 高速环境下的TCP”放进TCP字节序号和状态机：记录四元组、Seq/Ack、标志、窗口、选项、计时器和应用读写边界。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“16.8 高速环境下的TCP”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 16.8.1 高速TCP与受限的慢启动

对“16.8.1 高速TCP与受限的慢启动”逐事件更新cwnd、ssthresh与flight size，说明ACK或丢失信号为何触发当前分支，并与接收窗口分离。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“16.8.1 高速TCP与受限的慢启动”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 16.8.2 二进制增长拥塞控制（BIC和CUBIC）

对“16.8.2 二进制增长拥塞控制（BIC和CUBIC）”逐事件更新cwnd、ssthresh与flight size，说明ACK或丢失信号为何触发当前分支，并与接收窗口分离。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“16.8.2 二进制增长拥塞控制（BIC和CUBIC）”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

### 16.9 基于延迟的拥塞控制算法

对“16.9 基于延迟的拥塞控制算法”逐事件更新cwnd、ssthresh与flight size，说明ACK或丢失信号为何触发当前分支，并与接收窗口分离。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“16.9 基于延迟的拥塞控制算法”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 16.9.1 Vegas算法

对“16.9.1 Vegas算法”逐事件更新cwnd、ssthresh与flight size，说明ACK或丢失信号为何触发当前分支，并与接收窗口分离。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“16.9.1 Vegas算法”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 16.9.2 FAST算法

对“16.9.2 FAST算法”逐事件更新cwnd、ssthresh与flight size，说明ACK或丢失信号为何触发当前分支，并与接收窗口分离。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“16.9.2 FAST算法”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 16.9.3 TCP Westwood算法和Westwood＋算法

把“16.9.3 TCP Westwood算法和Westwood＋算法”放进TCP字节序号和状态机：记录四元组、Seq/Ack、标志、窗口、选项、计时器和应用读写边界。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“16.9.3 TCP Westwood算法和Westwood＋算法”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

#### 16.9.4 复合TCP

把“16.9.4 复合TCP”放进TCP字节序号和状态机：记录四元组、Seq/Ack、标志、窗口、选项、计时器和应用读写边界。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“16.9.4 复合TCP”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

### 16.10 缓冲区膨胀

把“16.10 缓冲区膨胀”放回“推演慢启动、拥塞避免、Reno/NewReno、SACK、Eifel、BIC/CUBIC、Vegas与ECN”的协议路径，标注输入、状态、报文字段、输出与最小失败反例。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“16.10 缓冲区膨胀”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

### 16.11 积极队列管理和ECN

对“16.11 积极队列管理和ECN”逐事件更新cwnd、ssthresh与flight size，说明ACK或丢失信号为何触发当前分支，并与接收窗口分离。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“16.11 积极队列管理和ECN”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

### 16.12 与TCP拥塞控制相关的攻击

对“16.12 与TCP拥塞控制相关的攻击”逐事件更新cwnd、ssthresh与flight size，说明ACK或丢失信号为何触发当前分支，并与接收窗口分离。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“16.12 与TCP拥塞控制相关的攻击”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

### 16.13 总结

用“16.13 总结”校准本章问题、证据范围和标准来源；总结必须回到数据包字段、状态转移或实现观察，不能只列术语。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“16.13 总结”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

### 16.14 参考文献

用“16.14 参考文献”校准本章问题、证据范围和标准来源；总结必须回到数据包字段、状态转移或实现观察，不能只列术语。 验收时固定其余条件，只改变一个字段、状态、计时器或路径变量；保存预测、原始报文、首个偏差、恢复结果与复核人。

**协议级核查：** 为“16.14 参考文献”给出一个正常样本、一个最小反例和一个可独立复现的判定条件，不能只引用最终应用报错。

## 本页完整节点清单

- 第16章 TCP拥塞控制
- 16.1 引言
- 16.1.1 TCP拥塞检测
- 16.1.2 减缓TCP发送
- 16.2 一些经典算法
- 16.2.1 慢启动
- 16.2.2 拥塞避免
- 16.2.3 慢启动和拥塞避免的选择
- 16.2.4 Tahoe、Reno以及快速恢复算法
- 16.2.5 标准TCP
- 16.3 对标准算法的改进
- 16.3.1 NewReno
- 16.3.2 采用选择确认机制的TCP拥塞控制
- 16.3.3 转发确认（FACK）和速率减半
- 16.3.4 限制传输
- 16.3.5 拥塞窗口校验
- 16.4 伪RTO处理——Eifel响应算法
- 16.5 扩展举例
- 16.5.1 慢启动行为
- 16.5.2 发送暂停和本地拥塞（事件1）
- 16.5.3 延伸ACK和本地拥塞恢复
- 16.5.4 快速重传和SACK恢复（事件2）
- 16.5.5 其他本地拥塞和快速重传事件
- 16.5.6 超时、重传和撤销cwnd修改
- 16.5.7 连接结束
- 16.6 共享拥塞状态信息
- 16.7 TCP友好性
- 16.8 高速环境下的TCP
- 16.8.1 高速TCP与受限的慢启动
- 16.8.2 二进制增长拥塞控制（BIC和CUBIC）
- 16.9 基于延迟的拥塞控制算法
- 16.9.1 Vegas算法
- 16.9.2 FAST算法
- 16.9.3 TCP Westwood算法和Westwood＋算法
- 16.9.4 复合TCP
- 16.10 缓冲区膨胀
- 16.11 积极队列管理和ECN
- 16.12 与TCP拥塞控制相关的攻击
- 16.13 总结
- 16.14 参考文献


## 可复现实验记录

```text
packet = timestamp + direction + five_tuple + header_fields + raw_offset
state  = before + event + guard + action + timer + after
audit  = expected + observed + first_difference + alternative + recovery
```

```bash
tcpdump -nn -s0 -w tip2-lab.pcap 'host 192.0.2.10'
tshark -r tip2-lab.pcap -T fields -e frame.number -e frame.time_relative -e ip.src -e ip.dst -e tcp.seq -e tcp.ack
```

动手试：采集一次正常事务、一次单变量故障和一次恢复事务。对每条结论记录文件哈希、关键帧、字节偏移、状态前后值、竞争解释和独立复核结果。


## 独立证据门

最小证据包包含：正式节点、版次、拓扑、抓包位置、原始报文哈希、字段偏移、状态/计时器、单变量故障、恢复、偏差、责任人与复核人。

## 练习


## 本章回顾

“第16章 TCP拥塞控制”的核心是推演慢启动、拥塞避免、Reno/NewReno、SACK、Eifel、BIC/CUBIC、Vegas与ECN。真正掌握不是记住名词，而是能从原始字节、状态机、计时器和单变量反例证明协议为何如此运行。


[← 上一页：第15章 TCP数据流与窗口管理](/learn/tcp-ip-illustrated-vol1/18-tip2-15-tcp-data-flow-window/tip2-15-tcp-data-flow-window) · [下一页：第17章 TCP保活机制 →](/learn/tcp-ip-illustrated-vol1/20-tip2-17-tcp-keepalive/tip2-17-tcp-keepalive)

