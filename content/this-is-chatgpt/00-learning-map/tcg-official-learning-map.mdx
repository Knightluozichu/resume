---
title: "《这就是ChatGPT》权威学习地图"
type: A
section: "《这就是ChatGPT》权威学习地图"
order: 0
description: "《这就是ChatGPT》权威学习地图逐项覆盖正式目录，以概率、向量、交互采样和工具证据重建原书论证。"
demo: true
math: true
sourceUrl: "https://www.wolfram-media.com/products/what-is-chatgpt-doing-and-why-does-it-work/"
draft: true
---

import { TcgOfficialLearningMapMapLab, TcgOfficialLearningMapProbabilityLab, TcgOfficialLearningMapEvidenceLab } from "@/components/mdx/this-is-chatgpt/diagrams/tcg-official-learning-map";
import { Objectives, Callout, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step, Attribution } from "@/components/mdx/mdx-components";

<Objectives>

- 能解释逐token生成、概率模型、神经网络在本节中的输入、输出与历史语境
- 能推导并计算四个核心表达式，把概率、向量或工具结果映射到实现状态
- 能实现并运行三种交互视图，定位温度、上下文、版本或工具证据的首个分叉
- 能设计正常与单一故障样本，证明“前言、两篇正文标题、主文16节、补文4节、致谢和补充资源共25个正式目录层级全部可导航并有独立证据。”

</Objectives>

## 为什么先冻结语料、模型版本与生成轨迹

<Term def="逐token生成是《这就是ChatGPT》权威学习地图的第1个可执行证据坐标。">逐token生成</Term>、<Term def="概率模型是《这就是ChatGPT》权威学习地图的第2个可执行证据坐标。">概率模型</Term>、<Term def="神经网络是《这就是ChatGPT》权威学习地图的第3个可执行证据坐标。">神经网络</Term>、<Term def="嵌入是《这就是ChatGPT》权威学习地图的第4个可执行证据坐标。">嵌入</Term>、<Term def="语义规律是《这就是ChatGPT》权威学习地图的第5个可执行证据坐标。">语义规律</Term>、<Term def="计算知识增强是《这就是ChatGPT》权威学习地图的第6个可执行证据坐标。">计算知识增强</Term>共同构成本页坐标。沿正式目录推进：前言之后用16节解释主文论证，再以4节Wolfram|Alpha补文讨论语言生成与精确计算的组合。

本书写于2023年初，主文用较小的GPT-2与Wolfram Language示例帮助读者观察机制，并以当时公开信息高层解释ChatGPT。阅读时必须分开作者亲自运行的示例、对已知神经网络结构的说明、对ChatGPT工程的合理概括，以及关于语言和意义空间的研究假设。模型产品后来更新，不会反向改变书中的论证分母。

每次实验保存原始文本、token序列、词表版本、模型标识、logit、归一化概率、温度、随机种子与采样token。涉及Wolfram|Alpha时，再保存形式查询、工具原始结果、单位、假设和最终回答。只有这些中间对象可重放，才能区分“模型生成了流畅句子”和“句子中的主张已由计算或来源验证”。

<Callout type="info">**验收命题：** 前言、两篇正文标题、主文16节、补文4节、致谢和补充资源共25个正式目录层级全部可导航并有独立证据。</Callout>

## 数学与计算骨架

$$
text\xrightarrow{tokenize}x_{1:t}
$$

$$
x_{1:t}\xrightarrow{network}p(x_{t+1})
$$

$$
p\xrightarrow{sample}x_{t+1}
$$

$$
language+computation\to verifiable\ answer
$$

公式中的条件分布不代表主观信念，而是模型在固定参数和上下文下给出的数值输出。温度改变分布形状，随机种子决定一次采样路径；两者都不改变训练所得权重。向量投影、注意力热图与工具结果也必须带版本和坐标，不能只凭一张图或最终回答推断内部机制。

## 三段可运行骨架

```python
def next_token_distribution(model, tokenizer, text, temperature):
    token_ids = tokenizer.encode(text)
    logits = model(token_ids)[-1]
    probabilities = softmax(logits / temperature)
    return token_ids, logits, probabilities
```

```python
def generate_one_step(model, tokenizer, text, temperature, rng):
    token_ids, logits, probabilities = next_token_distribution(
        model, tokenizer, text, temperature
    )
    next_id = rng.categorical(probabilities)
    return tokenizer.decode(token_ids + [next_id]), next_id, probabilities
```

```python
def answer_with_tool(question, translate, tool, compose):
    formal_query = translate(question)
    raw_result = tool(formal_query)
    assert raw_result.status == "ok"
    answer = compose(question, raw_result)
    return {"query": formal_query, "raw": raw_result, "answer": answer}
```

第一段暴露模型概率，第二段暴露自回归外层循环，第三段暴露语言模型与计算工具的责任边界。真实库接口可以不同，但不得把tokenization、概率归一化、采样或工具原始结果藏在无法审计的黑箱中。

## 固定样例与手算

设五个候选token的logit为2.4、1.8、1.15、0.45和负0.2。温度为1时，先减去最大logit避免指数溢出，再计算指数并除以总和；所有概率必须非负且相加为1。把温度降低到0.5，最高logit对应概率会集中；提高到1.5，尾部token获得更多质量。这个变化说明温度控制采样多样性，而不是让模型学到新知识。

用小语料“a b a c”建立bigram计数：a后面b与c各出现一次，因此经验概率各为二分之一；b后面只见过a。若查询从未出现的上下文，直接计数无法可靠续写。神经模型的任务正是从共享表示和参数中泛化，而不是无限扩大n-gram查表。对照实验应使用同一词表，防止未知token处理悄悄改变分母。

再取两个二维嵌入向量u=(1,0)、v=(0.8,0.6)，余弦相似度是0.8。投影到一维后可能改变邻居次序，因此二维或一维图只能提出假设。工具增强实验同样需要对照：问题先转换成形式查询，工具返回带单位结果，最终文本必须逐项蕴含原始结果；若模型擅自改数值，审计应在整合阶段失败。

最后在全新进程重放同一文本、模型摘要、温度和随机种子。首步token序列、logit摘要、概率和采样结果必须一致；如果远程服务不能位级复现，至少固定服务版本并声明允许差异。任何不可重放部分都应作为限制，而不是用新的顺畅回答替代原证据。

## 独立对照实验

先建立不可变基线：固定提示文本、tokenizer、模型摘要、解码算法、温度、随机种子和工具版本，完整保存首步token、logit、概率、采样项及最终文本。基线不是“看起来合理”的答案，而是一组可重新计算的中间对象。第二次运行若只保存最终文本，即使逐字相同也不能证明概率链、工具链和版本合同相同。

然后每轮只改变一个因素。把温度从1改为0.5时，token编号与原始logit应保持不变，温度缩放后的分布和采样结果可以变化；更换tokenizer时，最早分叉应出现在token序列，后续logit不再适合逐项比较；更换模型版本时，必须从参数标识开始建立新证据链。若一次实验同时改提示、模型和温度，就无法把输出差异归因给任何单一机制。

工具增强路径还要做断路测试。保留同一自然语言问题，分别运行正常工具、返回单位错误的工具和明确失败的工具。系统应在读取原始结果后、组织最终措辞前报告单位冲突或失败，不得让语言模型凭常识补写一个数字。对历史模型的描述也使用同样纪律：作者2023年使用的公开实例是解释材料，不是对后来产品规格的承诺。

最后由独立进程读取证据账本重算softmax、校验概率总和、核对采样项和工具单位，并输出首个不一致坐标。验收记录至少包含输入摘要、环境版本、预期分叉、实际分叉与回退动作。这样既能观察生成文字，也能回答差异从哪里开始、哪条结论仍受证据支持，以及修复后应该重放哪一段。

## 正式目录逐项讲解

### 《这就是ChatGPT》权威学习地图

“《这就是ChatGPT》权威学习地图”连接逐token生成与嵌入，需要同时说明机制、观察和未知边界。 将它放回本页主线“沿正式目录推进：前言之后用16节解释主文论证，再以4节Wolfram|Alpha补文讨论语言生成与精确计算的组合。”中，先给一个可手算或可运行的最小实验，再增加只改变一个输入、温度、权重、投影或工具结果的对照，记录第一处输出分叉。

针对“《这就是ChatGPT》权威学习地图”，把输入token、语料样本、参数版本、温度、随机种子、工具查询和原始结果写入证据账本。先预测改变一个变量后哪条概率、向量、损失或工具结果最先变化，再运行正常与故障路径；最终文字流畅不能覆盖中间证据不一致。

### What Is ChatGPT Doing ... and Why Does It Work? / ChatGPT在做什么，为什么有效？

“What Is ChatGPT Doing ... and Why Does It Work? / ChatGPT在做什么，为什么有效？”连接逐token生成与嵌入，需要同时说明机制、观察和未知边界。 将它放回本页主线“沿正式目录推进：前言之后用16节解释主文论证，再以4节Wolfram|Alpha补文讨论语言生成与精确计算的组合。”中，先给一个可手算或可运行的最小实验，再增加只改变一个输入、温度、权重、投影或工具结果的对照，记录第一处输出分叉。

针对“What Is ChatGPT Doing ... and Why Does It Work? / ChatGPT在做什么，为什么有效？”，把输入token、语料样本、参数版本、温度、随机种子、工具查询和原始结果写入证据账本。先预测改变一个变量后哪条概率、向量、损失或工具结果最先变化，再运行正常与故障路径；最终文字流畅不能覆盖中间证据不一致。

### Wolfram|Alpha as the Way to Bring Computational Knowledge Superpowers to ChatGPT / 以Wolfram|Alpha为ChatGPT带来计算知识超能力

语言模型负责理解与组织表达，Wolfram|Alpha负责结构化知识和计算；接口错误需要分阶段定位。 将它放回本页主线“沿正式目录推进：前言之后用16节解释主文论证，再以4节Wolfram|Alpha补文讨论语言生成与精确计算的组合。”中，先给一个可手算或可运行的最小实验，再增加只改变一个输入、温度、权重、投影或工具结果的对照，记录第一处输出分叉。

针对“Wolfram|Alpha as the Way to Bring Computational Knowledge Superpowers to ChatGPT / 以Wolfram|Alpha为ChatGPT带来计算知识超能力”，把输入token、语料样本、参数版本、温度、随机种子、工具查询和原始结果写入证据账本。先预测改变一个变量后哪条概率、向量、损失或工具结果最先变化，再运行正常与故障路径；最终文字流畅不能覆盖中间证据不一致。

### Preface / 前言

前言固定作者的问题尺度、历史语境与解释边界，后续所有技术细节都要回到这三个约束。 将它放回本页主线“沿正式目录推进：前言之后用16节解释主文论证，再以4节Wolfram|Alpha补文讨论语言生成与精确计算的组合。”中，先给一个可手算或可运行的最小实验，再增加只改变一个输入、温度、权重、投影或工具结果的对照，记录第一处输出分叉。

针对“Preface / 前言”，把输入token、语料样本、参数版本、温度、随机种子、工具查询和原始结果写入证据账本。先预测改变一个变量后哪条概率、向量、损失或工具结果最先变化，再运行正常与故障路径；最终文字流畅不能覆盖中间证据不一致。

### It's Just Adding One Word at a Time / 一次只添加一个词

生成器每轮读取已有token序列，输出下一token分布并采样；长文本是这一局部循环的累积结果。 将它放回本页主线“沿正式目录推进：前言之后用16节解释主文论证，再以4节Wolfram|Alpha补文讨论语言生成与精确计算的组合。”中，先给一个可手算或可运行的最小实验，再增加只改变一个输入、温度、权重、投影或工具结果的对照，记录第一处输出分叉。

针对“It's Just Adding One Word at a Time / 一次只添加一个词”，把输入token、语料样本、参数版本、温度、随机种子、工具查询和原始结果写入证据账本。先预测改变一个变量后哪条概率、向量、损失或工具结果最先变化，再运行正常与故障路径；最终文字流畅不能覆盖中间证据不一致。

### Where Do the Probabilities Come From? / 概率从何而来？

直接频数只能覆盖见过的短上下文，组合爆炸迫使模型学习可跨上下文迁移的统计结构。 将它放回本页主线“沿正式目录推进：前言之后用16节解释主文论证，再以4节Wolfram|Alpha补文讨论语言生成与精确计算的组合。”中，先给一个可手算或可运行的最小实验，再增加只改变一个输入、温度、权重、投影或工具结果的对照，记录第一处输出分叉。

针对“Where Do the Probabilities Come From? / 概率从何而来？”，把输入token、语料样本、参数版本、温度、随机种子、工具查询和原始结果写入证据账本。先预测改变一个变量后哪条概率、向量、损失或工具结果最先变化，再运行正常与故障路径；最终文字流畅不能覆盖中间证据不一致。

### What Is a Model? / 什么是模型？

模型是有限参数化的预测规则，价值在于对新输入泛化，而不在于逐条复制训练数据。 将它放回本页主线“沿正式目录推进：前言之后用16节解释主文论证，再以4节Wolfram|Alpha补文讨论语言生成与精确计算的组合。”中，先给一个可手算或可运行的最小实验，再增加只改变一个输入、温度、权重、投影或工具结果的对照，记录第一处输出分叉。

针对“What Is a Model? / 什么是模型？”，把输入token、语料样本、参数版本、温度、随机种子、工具查询和原始结果写入证据账本。先预测改变一个变量后哪条概率、向量、损失或工具结果最先变化，再运行正常与故障路径；最终文字流畅不能覆盖中间证据不一致。

### Models for Human-Like Tasks / 面向类人任务的模型

模型是有限参数化的预测规则，价值在于对新输入泛化，而不在于逐条复制训练数据。 将它放回本页主线“沿正式目录推进：前言之后用16节解释主文论证，再以4节Wolfram|Alpha补文讨论语言生成与精确计算的组合。”中，先给一个可手算或可运行的最小实验，再增加只改变一个输入、温度、权重、投影或工具结果的对照，记录第一处输出分叉。

针对“Models for Human-Like Tasks / 面向类人任务的模型”，把输入token、语料样本、参数版本、温度、随机种子、工具查询和原始结果写入证据账本。先预测改变一个变量后哪条概率、向量、损失或工具结果最先变化，再运行正常与故障路径；最终文字流畅不能覆盖中间证据不一致。

### Neural Nets / 神经网络

人工神经元只做加权、偏置和激活，复杂能力来自大量简单单元和训练所得权重的组合。 将它放回本页主线“沿正式目录推进：前言之后用16节解释主文论证，再以4节Wolfram|Alpha补文讨论语言生成与精确计算的组合。”中，先给一个可手算或可运行的最小实验，再增加只改变一个输入、温度、权重、投影或工具结果的对照，记录第一处输出分叉。

针对“Neural Nets / 神经网络”，把输入token、语料样本、参数版本、温度、随机种子、工具查询和原始结果写入证据账本。先预测改变一个变量后哪条概率、向量、损失或工具结果最先变化，再运行正常与故障路径；最终文字流畅不能覆盖中间证据不一致。

### Machine Learning, and the Training of Neural Nets / 机器学习和神经网络训练

人工神经元只做加权、偏置和激活，复杂能力来自大量简单单元和训练所得权重的组合。 将它放回本页主线“沿正式目录推进：前言之后用16节解释主文论证，再以4节Wolfram|Alpha补文讨论语言生成与精确计算的组合。”中，先给一个可手算或可运行的最小实验，再增加只改变一个输入、温度、权重、投影或工具结果的对照，记录第一处输出分叉。

针对“Machine Learning, and the Training of Neural Nets / 机器学习和神经网络训练”，把输入token、语料样本、参数版本、温度、随机种子、工具查询和原始结果写入证据账本。先预测改变一个变量后哪条概率、向量、损失或工具结果最先变化，再运行正常与故障路径；最终文字流畅不能覆盖中间证据不一致。

### The Practice and Lore of Neural Net Training / 神经网络训练的实践与经验

人工神经元只做加权、偏置和激活，复杂能力来自大量简单单元和训练所得权重的组合。 将它放回本页主线“沿正式目录推进：前言之后用16节解释主文论证，再以4节Wolfram|Alpha补文讨论语言生成与精确计算的组合。”中，先给一个可手算或可运行的最小实验，再增加只改变一个输入、温度、权重、投影或工具结果的对照，记录第一处输出分叉。

针对“The Practice and Lore of Neural Net Training / 神经网络训练的实践与经验”，把输入token、语料样本、参数版本、温度、随机种子、工具查询和原始结果写入证据账本。先预测改变一个变量后哪条概率、向量、损失或工具结果最先变化，再运行正常与故障路径；最终文字流畅不能覆盖中间证据不一致。

### "Surely a Network That's Big Enough Can Do Anything!" / “足够大的网络当然什么都能做！”

网络容量只说明某些权重可能表示目标函数，不保证有限数据和优化能找到它，也不自动保证泛化。 将它放回本页主线“沿正式目录推进：前言之后用16节解释主文论证，再以4节Wolfram|Alpha补文讨论语言生成与精确计算的组合。”中，先给一个可手算或可运行的最小实验，再增加只改变一个输入、温度、权重、投影或工具结果的对照，记录第一处输出分叉。

针对“"Surely a Network That's Big Enough Can Do Anything!" / “足够大的网络当然什么都能做！””，把输入token、语料样本、参数版本、温度、随机种子、工具查询和原始结果写入证据账本。先预测改变一个变量后哪条概率、向量、损失或工具结果最先变化，再运行正常与故障路径；最终文字流畅不能覆盖中间证据不一致。

### The Concept of Embeddings / 嵌入的概念

嵌入把离散token放入连续特征空间，邻近关系反映使用模式；低维图片只能展示高维结构的一部分。 将它放回本页主线“沿正式目录推进：前言之后用16节解释主文论证，再以4节Wolfram|Alpha补文讨论语言生成与精确计算的组合。”中，先给一个可手算或可运行的最小实验，再增加只改变一个输入、温度、权重、投影或工具结果的对照，记录第一处输出分叉。

针对“The Concept of Embeddings / 嵌入的概念”，把输入token、语料样本、参数版本、温度、随机种子、工具查询和原始结果写入证据账本。先预测改变一个变量后哪条概率、向量、损失或工具结果最先变化，再运行正常与故障路径；最终文字流畅不能覆盖中间证据不一致。

### Inside ChatGPT / ChatGPT内部

一次token推理沿Transformer前向传播，外层再把新token接回上下文；这两层循环必须分开。 将它放回本页主线“沿正式目录推进：前言之后用16节解释主文论证，再以4节Wolfram|Alpha补文讨论语言生成与精确计算的组合。”中，先给一个可手算或可运行的最小实验，再增加只改变一个输入、温度、权重、投影或工具结果的对照，记录第一处输出分叉。

针对“Inside ChatGPT / ChatGPT内部”，把输入token、语料样本、参数版本、温度、随机种子、工具查询和原始结果写入证据账本。先预测改变一个变量后哪条概率、向量、损失或工具结果最先变化，再运行正常与故障路径；最终文字流畅不能覆盖中间证据不一致。

### The Training of ChatGPT / ChatGPT的训练

训练以损失为目标迭代更新权重，大规模成功还依赖初始化、数据批次、学习率和硬件等经验性选择。 将它放回本页主线“沿正式目录推进：前言之后用16节解释主文论证，再以4节Wolfram|Alpha补文讨论语言生成与精确计算的组合。”中，先给一个可手算或可运行的最小实验，再增加只改变一个输入、温度、权重、投影或工具结果的对照，记录第一处输出分叉。

针对“The Training of ChatGPT / ChatGPT的训练”，把输入token、语料样本、参数版本、温度、随机种子、工具查询和原始结果写入证据账本。先预测改变一个变量后哪条概率、向量、损失或工具结果最先变化，再运行正常与故障路径；最终文字流畅不能覆盖中间证据不一致。

### Beyond Basic Training / 超越基础训练

训练以损失为目标迭代更新权重，大规模成功还依赖初始化、数据批次、学习率和硬件等经验性选择。 将它放回本页主线“沿正式目录推进：前言之后用16节解释主文论证，再以4节Wolfram|Alpha补文讨论语言生成与精确计算的组合。”中，先给一个可手算或可运行的最小实验，再增加只改变一个输入、温度、权重、投影或工具结果的对照，记录第一处输出分叉。

针对“Beyond Basic Training / 超越基础训练”，把输入token、语料样本、参数版本、温度、随机种子、工具查询和原始结果写入证据账本。先预测改变一个变量后哪条概率、向量、损失或工具结果最先变化，再运行正常与故障路径；最终文字流畅不能覆盖中间证据不一致。

### What Really Lets ChatGPT Work? / 真正让ChatGPT工作的是什么？

模型成功是关于语言可被有限网络压缩的一项经验发现，作者并未宣称已有终极理论解释。 将它放回本页主线“沿正式目录推进：前言之后用16节解释主文论证，再以4节Wolfram|Alpha补文讨论语言生成与精确计算的组合。”中，先给一个可手算或可运行的最小实验，再增加只改变一个输入、温度、权重、投影或工具结果的对照，记录第一处输出分叉。

针对“What Really Lets ChatGPT Work? / 真正让ChatGPT工作的是什么？”，把输入token、语料样本、参数版本、温度、随机种子、工具查询和原始结果写入证据账本。先预测改变一个变量后哪条概率、向量、损失或工具结果最先变化，再运行正常与故障路径；最终文字流畅不能覆盖中间证据不一致。

### Meaning Space and Semantic Laws of Motion / 意义空间与语义运动定律

隐藏表示形成随token推进的轨迹，寻找语义运动定律需要跨投影、层和提示保持稳定证据。 将它放回本页主线“沿正式目录推进：前言之后用16节解释主文论证，再以4节Wolfram|Alpha补文讨论语言生成与精确计算的组合。”中，先给一个可手算或可运行的最小实验，再增加只改变一个输入、温度、权重、投影或工具结果的对照，记录第一处输出分叉。

针对“Meaning Space and Semantic Laws of Motion / 意义空间与语义运动定律”，把输入token、语料样本、参数版本、温度、随机种子、工具查询和原始结果写入证据账本。先预测改变一个变量后哪条概率、向量、损失或工具结果最先变化，再运行正常与故障路径；最终文字流畅不能覆盖中间证据不一致。

### Semantic Grammar and the Power of Computational Language / 语义语法与计算语言的力量

计算语言为概念关系提供精确、可执行表示，与自然语言的开放表达形成互补。 将它放回本页主线“沿正式目录推进：前言之后用16节解释主文论证，再以4节Wolfram|Alpha补文讨论语言生成与精确计算的组合。”中，先给一个可手算或可运行的最小实验，再增加只改变一个输入、温度、权重、投影或工具结果的对照，记录第一处输出分叉。

针对“Semantic Grammar and the Power of Computational Language / 语义语法与计算语言的力量”，把输入token、语料样本、参数版本、温度、随机种子、工具查询和原始结果写入证据账本。先预测改变一个变量后哪条概率、向量、损失或工具结果最先变化，再运行正常与故障路径；最终文字流畅不能覆盖中间证据不一致。

### So ... What Is ChatGPT Doing, and Why Does It Work? / 所以，ChatGPT究竟在做什么，为什么有效？

结论把语料、训练、逐token前向和采样串联起来，同时保留为什么这种压缩足够有效的开放问题。 将它放回本页主线“沿正式目录推进：前言之后用16节解释主文论证，再以4节Wolfram|Alpha补文讨论语言生成与精确计算的组合。”中，先给一个可手算或可运行的最小实验，再增加只改变一个输入、温度、权重、投影或工具结果的对照，记录第一处输出分叉。

针对“So ... What Is ChatGPT Doing, and Why Does It Work? / 所以，ChatGPT究竟在做什么，为什么有效？”，把输入token、语料样本、参数版本、温度、随机种子、工具查询和原始结果写入证据账本。先预测改变一个变量后哪条概率、向量、损失或工具结果最先变化，再运行正常与故障路径；最终文字流畅不能覆盖中间证据不一致。

### Additional Resources / 补充资源

来源和资源用于重放作者演示，必须记录版本、输入、输出和访问日期，避免把二手截图当证据。 将它放回本页主线“沿正式目录推进：前言之后用16节解释主文论证，再以4节Wolfram|Alpha补文讨论语言生成与精确计算的组合。”中，先给一个可手算或可运行的最小实验，再增加只改变一个输入、温度、权重、投影或工具结果的对照，记录第一处输出分叉。

针对“Additional Resources / 补充资源”，把输入token、语料样本、参数版本、温度、随机种子、工具查询和原始结果写入证据账本。先预测改变一个变量后哪条概率、向量、损失或工具结果最先变化，再运行正常与故障路径；最终文字流畅不能覆盖中间证据不一致。

### ChatGPT and Wolfram|Alpha / ChatGPT与Wolfram|Alpha

语言模型负责理解与组织表达，Wolfram|Alpha负责结构化知识和计算；接口错误需要分阶段定位。 将它放回本页主线“沿正式目录推进：前言之后用16节解释主文论证，再以4节Wolfram|Alpha补文讨论语言生成与精确计算的组合。”中，先给一个可手算或可运行的最小实验，再增加只改变一个输入、温度、权重、投影或工具结果的对照，记录第一处输出分叉。

针对“ChatGPT and Wolfram|Alpha / ChatGPT与Wolfram|Alpha”，把输入token、语料样本、参数版本、温度、随机种子、工具查询和原始结果写入证据账本。先预测改变一个变量后哪条概率、向量、损失或工具结果最先变化，再运行正常与故障路径；最终文字流畅不能覆盖中间证据不一致。

### A Basic Example / 一个基本示例

基本示例应完整保留自然语言、形式查询、工具原始结果、单位假设和最终表述。 将它放回本页主线“沿正式目录推进：前言之后用16节解释主文论证，再以4节Wolfram|Alpha补文讨论语言生成与精确计算的组合。”中，先给一个可手算或可运行的最小实验，再增加只改变一个输入、温度、权重、投影或工具结果的对照，记录第一处输出分叉。

针对“A Basic Example / 一个基本示例”，把输入token、语料样本、参数版本、温度、随机种子、工具查询和原始结果写入证据账本。先预测改变一个变量后哪条概率、向量、损失或工具结果最先变化，再运行正常与故障路径；最终文字流畅不能覆盖中间证据不一致。

### A Few More Examples / 更多示例

扩展案例要同时覆盖成功、歧义、不支持和失败状态，防止只展示工具增强的理想路径。 将它放回本页主线“沿正式目录推进：前言之后用16节解释主文论证，再以4节Wolfram|Alpha补文讨论语言生成与精确计算的组合。”中，先给一个可手算或可运行的最小实验，再增加只改变一个输入、温度、权重、投影或工具结果的对照，记录第一处输出分叉。

针对“A Few More Examples / 更多示例”，把输入token、语料样本、参数版本、温度、随机种子、工具查询和原始结果写入证据账本。先预测改变一个变量后哪条概率、向量、损失或工具结果最先变化，再运行正常与故障路径；最终文字流畅不能覆盖中间证据不一致。

### The Path Forward / 前进之路

组合系统的前景来自神经生成、符号计算和验证协议协作，而不是任何一方自动变成无误真理源。 将它放回本页主线“沿正式目录推进：前言之后用16节解释主文论证，再以4节Wolfram|Alpha补文讨论语言生成与精确计算的组合。”中，先给一个可手算或可运行的最小实验，再增加只改变一个输入、温度、权重、投影或工具结果的对照，记录第一处输出分叉。

针对“The Path Forward / 前进之路”，把输入token、语料样本、参数版本、温度、随机种子、工具查询和原始结果写入证据账本。先预测改变一个变量后哪条概率、向量、损失或工具结果最先变化，再运行正常与故障路径；最终文字流畅不能覆盖中间证据不一致。

## 六类独立证据

### token与上下文合同

保存原始字符串、规范化方式、tokenizer版本、token编号和上下文截断位置。同一可见文字在不同tokenizer下可能形成不同序列，因而概率不能脱离词表直接比较。

### 概率与采样合同

保存logit、温度变换、归一化总和、候选排序、随机种子和实际采样项。贪心、top-k或其他采样规则必须明确；一次生成结果不能代表整个条件分布。

### 网络与训练合同

区分前向推理、基础下一token训练和后续行为塑造。训练证据包括数据切分、损失、梯度和参数版本；推理证据只读冻结参数，不能把对话中的新文本误称为在线训练。

### 表示与可视化合同

嵌入、隐藏状态和注意力矩阵写清层、位置和维度。降维方法、随机种子与保真指标必须保存；图上看似平滑的轨迹需要回到高维邻域验证。

### 语言与假设合同

把可观察事实、工程解释和关于语义规律的猜想分栏。文本连贯是观察，逐token网络是机制，存在简洁语义运动定律仍是研究问题，三者证据等级不同。

### 工具与事实合同

工具调用保存查询、权限、版本、原始结果、单位和来源。最终回答不得覆盖工具失败；语言模型负责表达，不应悄悄替换可计算结果或制造不存在的引用。

## 三步交互实验

<Stepper>
  <Step title="追踪生成证据链">推动阶段滑块，检查语料、权重、分布、采样与验证的先后关系。<TcgOfficialLearningMapMapLab /></Step>
  <Step title="改变token概率">调节温度，先预测分布集中或变平，再观察五个候选token概率。<TcgOfficialLearningMapProbabilityLab /></Step>
  <Step title="注入证据故障">改变上下文并注入版本或工具故障，要求审计在最终文本之前报告分叉。<TcgOfficialLearningMapEvidenceLab /></Step>
</Stepper>

## 常见失败与回退

<Callout type="trap">**本节高风险失败：** 按通用LLM课程重排为Transformer、提示工程和产品应用，丢失作者从语言规律走向计算语言的核心论证。</Callout>

<Callout type="trap">**拟人化不是机制：** “记得”“理解”“思考”等词可以是交流简写，但不能替代token、概率、权重、工具查询和验证结果。</Callout>

<Callout type="tip">**回退顺序：** 原始文本 → token序列 → 模型版本 → logit → 概率 → 采样 → 工具查询 → 原始结果 → 最终表述。第一处不一致未解释前，不看整篇文字是否顺畅。</Callout>

## 本章回顾

沿正式目录推进：前言之后用16节解释主文论证，再以4节Wolfram|Alpha补文讨论语言生成与精确计算的组合。 掌握标准是能重建输入、计算、证据和边界，而不是复述“预测下一个词”这句口号。通过标准为：前言、两篇正文标题、主文16节、补文4节、致谢和补充资源共25个正式目录层级全部可导航并有独立证据。

<Glossary>

<GlossaryItem term="逐token生成">
  “逐token生成”是本页第1个证据坐标。记录定义、输入、版本、最小计算、失败边界和独立重放结果，避免用拟人化叙述替代可检查机制。
</GlossaryItem>

<GlossaryItem term="概率模型">
  “概率模型”是本页第2个证据坐标。记录定义、输入、版本、最小计算、失败边界和独立重放结果，避免用拟人化叙述替代可检查机制。
</GlossaryItem>

<GlossaryItem term="神经网络">
  “神经网络”是本页第3个证据坐标。记录定义、输入、版本、最小计算、失败边界和独立重放结果，避免用拟人化叙述替代可检查机制。
</GlossaryItem>

<GlossaryItem term="嵌入">
  “嵌入”是本页第4个证据坐标。记录定义、输入、版本、最小计算、失败边界和独立重放结果，避免用拟人化叙述替代可检查机制。
</GlossaryItem>

<GlossaryItem term="语义规律">
  “语义规律”是本页第5个证据坐标。记录定义、输入、版本、最小计算、失败边界和独立重放结果，避免用拟人化叙述替代可检查机制。
</GlossaryItem>

<GlossaryItem term="计算知识增强">
  “计算知识增强”是本页第6个证据坐标。记录定义、输入、版本、最小计算、失败边界和独立重放结果，避免用拟人化叙述替代可检查机制。
</GlossaryItem>

</Glossary>

<Exercises>

1. 如何为《这就是ChatGPT》权威学习地图建立最小可手算实验？

<Answer>固定五个logit、一个tokenizer和一段上下文，手算温度变换、softmax和一次采样；涉及工具时再固定形式查询与原始结果。</Answer>

2. 如何证明“前言、两篇正文标题、主文16节、补文4节、致谢和补充资源共25个正式目录层级全部可导航并有独立证据。”不是一次流畅输出偶然满足？

<Answer>用相同输入并排运行正常路径与只破坏一个温度、上下文、模型版本、投影或工具结果的路径，要求检测器在预期中间对象区分两者。</Answer>

3. 修复《这就是ChatGPT》权威学习地图后怎样回归？

<Answer>先重放token与概率，再检查网络或表示，最后检查工具和文字整合；全部中间证据一致后才评价长文本。</Answer>

</Exercises>

## 前后导航

- [前言](/learn/this-is-chatgpt/01-main-essay/tcg-preface)

<Attribution adaptedFrom="斯蒂芬·沃尔弗拉姆著、WOLFRAM传媒汉化小组译《这就是ChatGPT》，人民邮电出版社，2023年7月，134页，ISBN 9787115618085；英文原版Wolfram Media，2023年3月9日，112页，ISBN 9781579550813 · 基于官方25个正式目录层级的独立教学重构" adaptedUrl="https://www.wolfram-media.com/products/what-is-chatgpt-doing-and-why-does-it-work/" />
