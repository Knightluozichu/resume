---
title: "《现代编译原理：C语言描述（修订版）》权威学习地图"
type: C
section: "导读 · 两部分完整路线"
order: 0
description: "《现代编译原理：C语言描述（修订版）》权威学习地图逐项覆盖22个核心目录层级，以C模块、Tiger中间产物和运行证据建立可复查实验。"
demo: true
math: false
sourceUrl: "https://www.cs.princeton.edu/~appel/modern/toc.html"
draft: true
---

import {
  TbcOfficialLearningMapMapLab,
  TbcOfficialLearningMapExperimentLab,
  TbcOfficialLearningMapEvidenceLab,
} from "@/components/mdx/tiger-book-compiler/diagrams/tbc-official-learning-map";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释《现代编译原理：C语言描述（修订版）》权威学习地图在Tiger编译器中的输入、输出、C接口和责任边界
- 能描述本页22个核心目录层级之间的数据结构、算法前提与工程取舍
- 能复现最小C模块或Tiger样本，保存正常、边界、失败和优化四类证据
- 能判断“每个正式目录层级都关联C接口、输入输出、结构化产物、失败样本和回归条件，第一部分最终生成可运行编译器，第二部分扩展其能力。”是否成立，而不是只看最终二进制是否运行

</Objectives>

## 为什么最终二进制正确仍然不够

<Term def="两部分路线连接“确认C修订版”的C接口、结构化产物、成本和首错证据。">两部分路线</Term>、<Term def="21章主线连接“建立目录分母”的C接口、结构化产物、成本和首错证据。">21章主线</Term>、<Term def="Tiger项目连接“连接模块接口”的C接口、结构化产物、成本和首错证据。">Tiger项目</Term>、<Term def="C模块接口连接“实现Tiger前后端”的C接口、结构化产物、成本和首错证据。">C模块接口</Term>、<Term def="高级优化连接“扩展高级主题”的C接口、结构化产物、成本和首错证据。">高级优化</Term>
共同构成本页坐标。把2部分、21章和Tiger语言附录组织为从C模块骨架到完整Tiger编译器，再到运行时、现代语言、全局优化和机器性能的工程路线。

虎书把教材组织成一项连续实现工程。扫描器、分析器、AST、环境、Frame、Tree IR、规范化、抽象汇编、流图、干涉图和运行时彼此依赖；任何阶段的错误都可能被后续样本偶然掩盖。因此每次实验都要保存阶段输入、输出和不变量，定位第一处偏离，而不是只观察最后进程的退出码。

## C版Tiger实验账本

实验固定Tiger源文件、编译器提交、C编译器与选项、目标ISA、运行时版本和随机种子。前端保存Token、语法树、作用域与类型；中端保存Frame访问、Tree IR、线性IR、基本块和轨迹；后端保存抽象汇编、def/use、CFG、活跃集合、干涉图、颜色与溢出重写；高级章保存堆图、类布局、闭包、类型约束、数据流事实、SSA、调度表与cache事件。

样本分为四组。正常样本覆盖主路径；边界样本覆盖空列表、深嵌套、递归声明、逃逸变量、临界立即数、不可归约流图、寄存器压力、循环依赖、别名和堆边界；失败样本只破坏一个词法、文法、类型、IR或机器约束；优化样本保存变换前后版本并进行解释器或本机差分执行。

诊断记录模块、源位置或IR位置、实际状态、期望状态、相关前驱和首要根因。模块失败后不得继续读取过期缓存；变换若令活跃性、支配、别名、调用或布局事实失效，必须显式重算。正确性与性能分开验收：正确性要求语言语义和ABI约束成立，性能要求给出静态成本、动态计数、代码大小、硬件和运行时环境。

每个目录项进入追溯矩阵：至少关联一个C接口、一个输入、一个算法前提、一份结构化产物、一个失败对照和一个回归断言。跨章数据同时标明生产者、消费者和失效条件，从而发现“有名词无实现”的空洞覆盖，并把回归范围限制在真实受影响模块。

## 本章核心推演

把2部分、21章和Tiger语言附录组织为从C模块骨架到完整Tiger编译器，再到运行时、现代语言、全局优化和机器性能的工程路线。 实现前先写出两部分路线与21章主线的最小合同：输入表示、输出表示、所有权、错误返回和复杂度。随后准备只有一个语言构造的Tiger样本，让确认C修订版到执行端到端回归每一步都产生可序列化证据；若中间结果无法独立检查，就说明接口还没有真正解耦。

正确样本之外必须构造一个最小反例。反例只破坏Tiger项目的单个前提，并断言诊断模块、位置和下游停止点。修复不能只更改最后输出，而应让第一处分叉消失，同时保持C模块接口和高级优化的历史快照不变。性能实验则固定输入和机器，分别记录算法操作次数、内存峰值与最终运行时间，解释模型和实测偏差。

最终验收从干净目录开始，重新编译C模块、生成全部中间产物、链接Tiger运行时并执行差分测试。任何手工保留的旧对象文件、旧分析集合或旧寄存器映射都会破坏证据链；因此构建摘要、工具版本和所有阶段摘要必须一起归档，才能复现结论。

## 六阶段证据链

### 1. 确认C修订版

围绕两部分路线先预测树、图、集合、表项或指令，只改变一个条件再运行；进入“建立目录分母”前，核对语义、接口、成本和首错位置。

### 2. 建立目录分母

围绕21章主线先预测树、图、集合、表项或指令，只改变一个条件再运行；进入“连接模块接口”前，核对语义、接口、成本和首错位置。

### 3. 连接模块接口

围绕Tiger项目先预测树、图、集合、表项或指令，只改变一个条件再运行；进入“实现Tiger前后端”前，核对语义、接口、成本和首错位置。

### 4. 实现Tiger前后端

围绕C模块接口先预测树、图、集合、表项或指令，只改变一个条件再运行；进入“扩展高级主题”前，核对语义、接口、成本和首错位置。

### 5. 扩展高级主题

围绕高级优化先预测树、图、集合、表项或指令，只改变一个条件再运行；进入“执行端到端回归”前，核对语义、接口、成本和首错位置。

### 6. 执行端到端回归

围绕两部分路线先预测树、图、集合、表项或指令，只改变一个条件再运行；进入“确认C修订版”前，核对语义、接口、成本和首错位置。

## 可运行的最小骨架

```c
Tiger source -> scanner -> parser -> AST -> semantic/IR -> codegen -> register allocation -> assembly
```

```c
Part I: working compiler; Part II: runtime, languages, optimization, scheduling, memory
```

```text
make clean && make tigerc && ./tests/run-all.sh
```

<Callout type="trap">
  **版本边界：**
  本页以中文C语言描述修订版为实现轨道。Java与ML版本共享主体算法，但模块接口、生成工具和代码片段不同，不能混用为同一实现证据。
</Callout>

## 权威目录逐项讲解

### 第1章 绪论

“第1章 绪论”必须落到C模块与结构化产物，而不只是术语。先在“确认C修订版”固定输入、接口、数据结构和算法前提，再进入“建立目录分母”比较第一条差异。围绕两部分路线记录所有权、复杂度、失败位置和下游消费者，防止后续阶段掩盖错误。

验收时分别检查语义、完备性、成本和模块边界：转换是否保持Tiger可观察行为，算法前提是否成立，时间空间代价是否符合规模，以及错误能否停在最早接口。修复后清理旧产物，从源文件重新生成并重放正常、边界、失败和优化样本。

### 第2章 词法分析

“第2章 词法分析”必须落到C模块与结构化产物，而不只是术语。先在“建立目录分母”固定输入、接口、数据结构和算法前提，再进入“连接模块接口”比较第一条差异。围绕21章主线记录所有权、复杂度、失败位置和下游消费者，防止后续阶段掩盖错误。

验收时分别检查语义、完备性、成本和模块边界：转换是否保持Tiger可观察行为，算法前提是否成立，时间空间代价是否符合规模，以及错误能否停在最早接口。修复后清理旧产物，从源文件重新生成并重放正常、边界、失败和优化样本。

### 第3章 语法分析

“第3章 语法分析”必须落到C模块与结构化产物，而不只是术语。先在“连接模块接口”固定输入、接口、数据结构和算法前提，再进入“实现Tiger前后端”比较第一条差异。围绕Tiger项目记录所有权、复杂度、失败位置和下游消费者，防止后续阶段掩盖错误。

验收时分别检查语义、完备性、成本和模块边界：转换是否保持Tiger可观察行为，算法前提是否成立，时间空间代价是否符合规模，以及错误能否停在最早接口。修复后清理旧产物，从源文件重新生成并重放正常、边界、失败和优化样本。

### 第4章 抽象语法

“第4章 抽象语法”必须落到C模块与结构化产物，而不只是术语。先在“实现Tiger前后端”固定输入、接口、数据结构和算法前提，再进入“扩展高级主题”比较第一条差异。围绕C模块接口记录所有权、复杂度、失败位置和下游消费者，防止后续阶段掩盖错误。

验收时分别检查语义、完备性、成本和模块边界：转换是否保持Tiger可观察行为，算法前提是否成立，时间空间代价是否符合规模，以及错误能否停在最早接口。修复后清理旧产物，从源文件重新生成并重放正常、边界、失败和优化样本。

### 第5章 语义分析

“第5章 语义分析”必须落到C模块与结构化产物，而不只是术语。先在“扩展高级主题”固定输入、接口、数据结构和算法前提，再进入“执行端到端回归”比较第一条差异。围绕高级优化记录所有权、复杂度、失败位置和下游消费者，防止后续阶段掩盖错误。

验收时分别检查语义、完备性、成本和模块边界：转换是否保持Tiger可观察行为，算法前提是否成立，时间空间代价是否符合规模，以及错误能否停在最早接口。修复后清理旧产物，从源文件重新生成并重放正常、边界、失败和优化样本。

### 第6章 活动记录

“第6章 活动记录”必须落到C模块与结构化产物，而不只是术语。先在“执行端到端回归”固定输入、接口、数据结构和算法前提，再进入“确认C修订版”比较第一条差异。围绕两部分路线记录所有权、复杂度、失败位置和下游消费者，防止后续阶段掩盖错误。

验收时分别检查语义、完备性、成本和模块边界：转换是否保持Tiger可观察行为，算法前提是否成立，时间空间代价是否符合规模，以及错误能否停在最早接口。修复后清理旧产物，从源文件重新生成并重放正常、边界、失败和优化样本。

### 第7章 翻译成中间代码

“第7章 翻译成中间代码”必须落到C模块与结构化产物，而不只是术语。先在“确认C修订版”固定输入、接口、数据结构和算法前提，再进入“建立目录分母”比较第一条差异。围绕21章主线记录所有权、复杂度、失败位置和下游消费者，防止后续阶段掩盖错误。

验收时分别检查语义、完备性、成本和模块边界：转换是否保持Tiger可观察行为，算法前提是否成立，时间空间代价是否符合规模，以及错误能否停在最早接口。修复后清理旧产物，从源文件重新生成并重放正常、边界、失败和优化样本。

### 第8章 基本块和轨迹

“第8章 基本块和轨迹”必须落到C模块与结构化产物，而不只是术语。先在“建立目录分母”固定输入、接口、数据结构和算法前提，再进入“连接模块接口”比较第一条差异。围绕Tiger项目记录所有权、复杂度、失败位置和下游消费者，防止后续阶段掩盖错误。

验收时分别检查语义、完备性、成本和模块边界：转换是否保持Tiger可观察行为，算法前提是否成立，时间空间代价是否符合规模，以及错误能否停在最早接口。修复后清理旧产物，从源文件重新生成并重放正常、边界、失败和优化样本。

### 第9章 指令选择

“第9章 指令选择”必须落到C模块与结构化产物，而不只是术语。先在“连接模块接口”固定输入、接口、数据结构和算法前提，再进入“实现Tiger前后端”比较第一条差异。围绕C模块接口记录所有权、复杂度、失败位置和下游消费者，防止后续阶段掩盖错误。

验收时分别检查语义、完备性、成本和模块边界：转换是否保持Tiger可观察行为，算法前提是否成立，时间空间代价是否符合规模，以及错误能否停在最早接口。修复后清理旧产物，从源文件重新生成并重放正常、边界、失败和优化样本。

### 第10章 活跃分析

“第10章 活跃分析”必须落到C模块与结构化产物，而不只是术语。先在“实现Tiger前后端”固定输入、接口、数据结构和算法前提，再进入“扩展高级主题”比较第一条差异。围绕高级优化记录所有权、复杂度、失败位置和下游消费者，防止后续阶段掩盖错误。

验收时分别检查语义、完备性、成本和模块边界：转换是否保持Tiger可观察行为，算法前提是否成立，时间空间代价是否符合规模，以及错误能否停在最早接口。修复后清理旧产物，从源文件重新生成并重放正常、边界、失败和优化样本。

### 第11章 寄存器分配

“第11章 寄存器分配”必须落到C模块与结构化产物，而不只是术语。先在“扩展高级主题”固定输入、接口、数据结构和算法前提，再进入“执行端到端回归”比较第一条差异。围绕两部分路线记录所有权、复杂度、失败位置和下游消费者，防止后续阶段掩盖错误。

验收时分别检查语义、完备性、成本和模块边界：转换是否保持Tiger可观察行为，算法前提是否成立，时间空间代价是否符合规模，以及错误能否停在最早接口。修复后清理旧产物，从源文件重新生成并重放正常、边界、失败和优化样本。

### 第12章 整合为一体

“第12章 整合为一体”必须落到C模块与结构化产物，而不只是术语。先在“执行端到端回归”固定输入、接口、数据结构和算法前提，再进入“确认C修订版”比较第一条差异。围绕21章主线记录所有权、复杂度、失败位置和下游消费者，防止后续阶段掩盖错误。

验收时分别检查语义、完备性、成本和模块边界：转换是否保持Tiger可观察行为，算法前提是否成立，时间空间代价是否符合规模，以及错误能否停在最早接口。修复后清理旧产物，从源文件重新生成并重放正常、边界、失败和优化样本。

### 第13章 垃圾收集

“第13章 垃圾收集”必须落到C模块与结构化产物，而不只是术语。先在“确认C修订版”固定输入、接口、数据结构和算法前提，再进入“建立目录分母”比较第一条差异。围绕Tiger项目记录所有权、复杂度、失败位置和下游消费者，防止后续阶段掩盖错误。

验收时分别检查语义、完备性、成本和模块边界：转换是否保持Tiger可观察行为，算法前提是否成立，时间空间代价是否符合规模，以及错误能否停在最早接口。修复后清理旧产物，从源文件重新生成并重放正常、边界、失败和优化样本。

### 第14章 面向对象的语言

“第14章 面向对象的语言”必须落到C模块与结构化产物，而不只是术语。先在“建立目录分母”固定输入、接口、数据结构和算法前提，再进入“连接模块接口”比较第一条差异。围绕C模块接口记录所有权、复杂度、失败位置和下游消费者，防止后续阶段掩盖错误。

验收时分别检查语义、完备性、成本和模块边界：转换是否保持Tiger可观察行为，算法前提是否成立，时间空间代价是否符合规模，以及错误能否停在最早接口。修复后清理旧产物，从源文件重新生成并重放正常、边界、失败和优化样本。

### 第15章 函数式程序设计语言

“第15章 函数式程序设计语言”必须落到C模块与结构化产物，而不只是术语。先在“连接模块接口”固定输入、接口、数据结构和算法前提，再进入“实现Tiger前后端”比较第一条差异。围绕高级优化记录所有权、复杂度、失败位置和下游消费者，防止后续阶段掩盖错误。

验收时分别检查语义、完备性、成本和模块边界：转换是否保持Tiger可观察行为，算法前提是否成立，时间空间代价是否符合规模，以及错误能否停在最早接口。修复后清理旧产物，从源文件重新生成并重放正常、边界、失败和优化样本。

### 第16章 多态类型

“第16章 多态类型”必须落到C模块与结构化产物，而不只是术语。先在“实现Tiger前后端”固定输入、接口、数据结构和算法前提，再进入“扩展高级主题”比较第一条差异。围绕两部分路线记录所有权、复杂度、失败位置和下游消费者，防止后续阶段掩盖错误。

验收时分别检查语义、完备性、成本和模块边界：转换是否保持Tiger可观察行为，算法前提是否成立，时间空间代价是否符合规模，以及错误能否停在最早接口。修复后清理旧产物，从源文件重新生成并重放正常、边界、失败和优化样本。

### 第17章 数据流分析

“第17章 数据流分析”必须落到C模块与结构化产物，而不只是术语。先在“扩展高级主题”固定输入、接口、数据结构和算法前提，再进入“执行端到端回归”比较第一条差异。围绕21章主线记录所有权、复杂度、失败位置和下游消费者，防止后续阶段掩盖错误。

验收时分别检查语义、完备性、成本和模块边界：转换是否保持Tiger可观察行为，算法前提是否成立，时间空间代价是否符合规模，以及错误能否停在最早接口。修复后清理旧产物，从源文件重新生成并重放正常、边界、失败和优化样本。

### 第18章 循环优化

“第18章 循环优化”必须落到C模块与结构化产物，而不只是术语。先在“执行端到端回归”固定输入、接口、数据结构和算法前提，再进入“确认C修订版”比较第一条差异。围绕Tiger项目记录所有权、复杂度、失败位置和下游消费者，防止后续阶段掩盖错误。

验收时分别检查语义、完备性、成本和模块边界：转换是否保持Tiger可观察行为，算法前提是否成立，时间空间代价是否符合规模，以及错误能否停在最早接口。修复后清理旧产物，从源文件重新生成并重放正常、边界、失败和优化样本。

### 第19章 静态单赋值形式

“第19章 静态单赋值形式”必须落到C模块与结构化产物，而不只是术语。先在“确认C修订版”固定输入、接口、数据结构和算法前提，再进入“建立目录分母”比较第一条差异。围绕C模块接口记录所有权、复杂度、失败位置和下游消费者，防止后续阶段掩盖错误。

验收时分别检查语义、完备性、成本和模块边界：转换是否保持Tiger可观察行为，算法前提是否成立，时间空间代价是否符合规模，以及错误能否停在最早接口。修复后清理旧产物，从源文件重新生成并重放正常、边界、失败和优化样本。

### 第20章 流水和调度

“第20章 流水和调度”必须落到C模块与结构化产物，而不只是术语。先在“建立目录分母”固定输入、接口、数据结构和算法前提，再进入“连接模块接口”比较第一条差异。围绕高级优化记录所有权、复杂度、失败位置和下游消费者，防止后续阶段掩盖错误。

验收时分别检查语义、完备性、成本和模块边界：转换是否保持Tiger可观察行为，算法前提是否成立，时间空间代价是否符合规模，以及错误能否停在最早接口。修复后清理旧产物，从源文件重新生成并重放正常、边界、失败和优化样本。

### 第21章 存储层次

“第21章 存储层次”必须落到C模块与结构化产物，而不只是术语。先在“连接模块接口”固定输入、接口、数据结构和算法前提，再进入“实现Tiger前后端”比较第一条差异。围绕两部分路线记录所有权、复杂度、失败位置和下游消费者，防止后续阶段掩盖错误。

验收时分别检查语义、完备性、成本和模块边界：转换是否保持Tiger可观察行为，算法前提是否成立，时间空间代价是否符合规模，以及错误能否停在最早接口。修复后清理旧产物，从源文件重新生成并重放正常、边界、失败和优化样本。

### 附录 Tiger语言参考手册

“附录 Tiger语言参考手册”必须落到C模块与结构化产物，而不只是术语。先在“实现Tiger前后端”固定输入、接口、数据结构和算法前提，再进入“扩展高级主题”比较第一条差异。围绕21章主线记录所有权、复杂度、失败位置和下游消费者，防止后续阶段掩盖错误。

验收时分别检查语义、完备性、成本和模块边界：转换是否保持Tiger可观察行为，算法前提是否成立，时间空间代价是否符合规模，以及错误能否停在最早接口。修复后清理旧产物，从源文件重新生成并重放正常、边界、失败和优化样本。

## 分步视觉验证

先预测当前阶段的树、图、集合、表项或指令，再运行C模块；若不同，停在第一处分叉并保留原始证据。

<Stepper>
  <Step title="画出模块与数据结构">
    标出两部分路线、21章主线、Tiger项目、C模块接口、高级优化的输入、输出、所有权、前提和下游消费者。
    <TbcOfficialLearningMapMapLab />
  </Step>
  <Step title="运行单变量Tiger实验">
    固定编译器与目标模型，只改变一个语言、分析、运行时或机器条件。
    <TbcOfficialLearningMapExperimentLab />
  </Step>
  <Step title="核对第一处分叉">
    比较结构化产物、复杂度和行为，清理旧输出后完整重建并重放回归样本。
    <TbcOfficialLearningMapEvidenceLab />
  </Step>
</Stepper>

## 常见误区

<Callout type="trap">
  **只看最终输出：** 没有Token、AST、环境、Tree
  IR、CFG、活跃集合、干涉图和帧布局，就无法把错误归因到具体C模块，也无法证明转换保持语义。
</Callout>

<Callout type="trap">
  **混用三种版本：**
  C版使用Lex/Flex、Yacc/Bison和C结构体接口；ML版与Java版的模块代码只能作对照，不能替代C版项目的编译、内存和ABI证据。
</Callout>

## 四类样本与通过条件

| 样本 | 单一变量               | 必查证据         | 通过条件         |
| ---- | ---------------------- | ---------------- | ---------------- |
| 正常 | 一个最小Tiger构造      | 全阶段快照       | 与预测一致       |
| 边界 | 深度、宽度、压力或布局 | 临界分支和成本   | 边界可解释       |
| 失败 | 一个语法、事实或约束   | 首错与下游停止   | 不产出伪结果     |
| 优化 | 一个变换或策略         | 前后IR和差分执行 | 等价且收益可解释 |

## 练习

<Exercises>

**问题 1** 为什么不能只凭目标程序运行成功验收《现代编译原理：C语言描述（修订版）》权威学习地图？

<Answer>
  最终行为可能隐藏错误Token、树结构、Frame访问、数据流事实或机器约束。应保存每个C模块的结构化产物，定位第一处分叉，并从Tiger源文件干净重建。
</Answer>

**问题 2** 如何把“连接模块接口”变成可回归实验？

<Answer>
  固定Tiger输入、编译器提交、C工具链、运行时和目标机，先预测两部分路线与21章主线的中间状态，只改变一个条件，并保留四类样本。
</Answer>

**问题 3** 本页最小通过条件是什么？

<Answer>
  每个正式目录层级都关联C接口、输入输出、结构化产物、失败样本和回归条件，第一部分最终生成可运行编译器，第二部分扩展其能力。
  修复后清理旧对象和中间产物，完整重建全部下游结果，并让历史样本回到基线。
</Answer>

</Exercises>

## 小结

- 两部分路线：连接“确认C修订版”的C接口、结构化产物、成本和回归证据。
- 21章主线：连接“建立目录分母”的C接口、结构化产物、成本和回归证据。
- Tiger项目：连接“连接模块接口”的C接口、结构化产物、成本和回归证据。
- C模块接口：连接“实现Tiger前后端”的C接口、结构化产物、成本和回归证据。
- 高级优化：连接“扩展高级主题”的C接口、结构化产物、成本和回归证据。
- 本页22个核心目录层级均已进入可追溯的Tiger编译证据链。

## 术语表

<Glossary>
  <GlossaryItem term="两部分路线">
    两部分路线用于解释“确认C修订版”，需要同时给出输入接口、算法前提、结构化结果、复杂度和清理重建方法。
  </GlossaryItem>
  <GlossaryItem term="21章主线">
    21章主线用于解释“建立目录分母”，需要同时给出输入接口、算法前提、结构化结果、复杂度和清理重建方法。
  </GlossaryItem>
  <GlossaryItem term="Tiger项目">
    Tiger项目用于解释“连接模块接口”，需要同时给出输入接口、算法前提、结构化结果、复杂度和清理重建方法。
  </GlossaryItem>
  <GlossaryItem term="C模块接口">
    C模块接口用于解释“实现Tiger前后端”，需要同时给出输入接口、算法前提、结构化结果、复杂度和清理重建方法。
  </GlossaryItem>
  <GlossaryItem term="高级优化">
    高级优化用于解释“扩展高级主题”，需要同时给出输入接口、算法前提、结构化结果、复杂度和清理重建方法。
  </GlossaryItem>
</Glossary>

<Attribution
  adaptedFrom="Andrew W. Appel、Maia Ginsburg著，赵克佳、黄春、沈志宇译《现代编译原理：C语言描述（修订版）》，人民邮电出版社，2018，385页，ISBN 9787115476883 · 基于Andrew W. Appel官方完整目录、Cambridge C版资料与中文版修订版目录的独立教学重构"
  adaptedUrl="https://www.cs.princeton.edu/~appel/modern/toc.html"
/>
