---
title: "第7章 翻译成中间代码"
type: C
section: "第一部分 · 运行时与中间表示"
order: 7
description: "第7章 翻译成中间代码逐项覆盖22个核心目录层级，以C模块、Tiger中间产物和运行证据建立可复查实验。"
demo: true
math: false
sourceUrl: "https://www.cs.princeton.edu/~appel/modern/toc.html"
draft: false
---

import {
  Tbc07TranslationIntermediateCodeMapLab,
  Tbc07TranslationIntermediateCodeExperimentLab,
  Tbc07TranslationIntermediateCodeEvidenceLab,
} from "@/components/mdx/tiger-book-compiler/diagrams/tbc-07-translation-intermediate-code";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第7章 翻译成中间代码在Tiger编译器中的输入、输出、C接口和责任边界
- 能描述本页22个核心目录层级之间的数据结构、算法前提与工程取舍
- 能复现最小C模块或Tiger样本，保存正常、边界、失败和优化四类证据
- 能判断“每个AST构造得到语义等价IR，求值顺序与副作用不丢失，条件跳转的真假出口最终闭合，静态链到达声明层次。”是否成立，而不是只看最终二进制是否运行

</Objectives>

## 为什么最终二进制正确仍然不够

<Term def="Tree IR连接“选择翻译形式”的C接口、结构化产物、成本和首错证据。">
  Tree IR
</Term>
、
<Term def="Ex/Nx/Cx连接“翻译左值”的C接口、结构化产物、成本和首错证据。">
  Ex/Nx/Cx
</Term>
、
<Term def="静态链连接“翻译控制流”的C接口、结构化产物、成本和首错证据。">
  静态链
</Term>
、
<Term def="片段连接“生成静态链”的C接口、结构化产物、成本和首错证据。">
  片段
</Term>
、
<Term def="运行时调用连接“收集过程和字符串片段”的C接口、结构化产物、成本和首错证据。">
  运行时调用
</Term>
共同构成本页坐标。把Tiger AST系统翻译为Tree
IR，以Ex/Nx/Cx封装表达式、语句和条件，并用静态链、片段与运行时调用落实语言语义。

虎书把教材组织成一项连续实现工程。扫描器、分析器、AST、环境、Frame、Tree IR、规范化、抽象汇编、流图、干涉图和运行时彼此依赖；任何阶段的错误都可能被后续样本偶然掩盖。因此每次实验都要保存阶段输入、输出和不变量，定位第一处偏离，而不是只观察最后进程的退出码。

## C版Tiger实验账本

实验固定Tiger源文件、编译器提交、C编译器与选项、目标ISA、运行时版本和随机种子。前端保存Token、语法树、作用域与类型；中端保存Frame访问、Tree IR、线性IR、基本块和轨迹；后端保存抽象汇编、def/use、CFG、活跃集合、干涉图、颜色与溢出重写；高级章保存堆图、类布局、闭包、类型约束、数据流事实、SSA、调度表与cache事件。

样本分为四组。正常样本覆盖主路径；边界样本覆盖空列表、深嵌套、递归声明、逃逸变量、临界立即数、不可归约流图、寄存器压力、循环依赖、别名和堆边界；失败样本只破坏一个词法、文法、类型、IR或机器约束；优化样本保存变换前后版本并进行解释器或本机差分执行。

诊断记录模块、源位置或IR位置、实际状态、期望状态、相关前驱和首要根因。模块失败后不得继续读取过期缓存；变换若令活跃性、支配、别名、调用或布局事实失效，必须显式重算。正确性与性能分开验收：正确性要求语言语义和ABI约束成立，性能要求给出静态成本、动态计数、代码大小、硬件和运行时环境。

每个目录项进入追溯矩阵：至少关联一个C接口、一个输入、一个算法前提、一份结构化产物、一个失败对照和一个回归断言。跨章数据同时标明生产者、消费者和失效条件，从而发现“有名词无实现”的空洞覆盖，并把回归范围限制在真实受影响模块。

## 本章核心推演

把Tiger AST系统翻译为Tree IR，以Ex/Nx/Cx封装表达式、语句和条件，并用静态链、片段与运行时调用落实语言语义。 实现前先写出Tree IR与Ex/Nx/Cx的最小合同：输入表示、输出表示、所有权、错误返回和复杂度。随后准备只有一个语言构造的Tiger样本，让选择翻译形式到解释或模拟IR每一步都产生可序列化证据；若中间结果无法独立检查，就说明接口还没有真正解耦。

正确样本之外必须构造一个最小反例。反例只破坏静态链的单个前提，并断言诊断模块、位置和下游停止点。修复不能只更改最后输出，而应让第一处分叉消失，同时保持片段和运行时调用的历史快照不变。性能实验则固定输入和机器，分别记录算法操作次数、内存峰值与最终运行时间，解释模型和实测偏差。

最终验收从干净目录开始，重新编译C模块、生成全部中间产物、链接Tiger运行时并执行差分测试。任何手工保留的旧对象文件、旧分析集合或旧寄存器映射都会破坏证据链；因此构建摘要、工具版本和所有阶段摘要必须一起归档，才能复现结论。

## 六阶段证据链

### 1. 选择翻译形式

围绕Tree IR先预测树、图、集合、表项或指令，只改变一个条件再运行；进入“翻译左值”前，核对语义、接口、成本和首错位置。

### 2. 翻译左值

围绕Ex/Nx/Cx先预测树、图、集合、表项或指令，只改变一个条件再运行；进入“翻译控制流”前，核对语义、接口、成本和首错位置。

### 3. 翻译控制流

围绕静态链先预测树、图、集合、表项或指令，只改变一个条件再运行；进入“生成静态链”前，核对语义、接口、成本和首错位置。

### 4. 生成静态链

围绕片段先预测树、图、集合、表项或指令，只改变一个条件再运行；进入“收集过程和字符串片段”前，核对语义、接口、成本和首错位置。

### 5. 收集过程和字符串片段

围绕运行时调用先预测树、图、集合、表项或指令，只改变一个条件再运行；进入“解释或模拟IR”前，核对语义、接口、成本和首错位置。

### 6. 解释或模拟IR

围绕Tree IR先预测树、图、集合、表项或指令，只改变一个条件再运行；进入“选择翻译形式”前，核对语义、接口、成本和首错位置。

## 可运行的最小骨架

```c
struct T_exp_ { enum { T_BINOP, T_MEM, T_TEMP, T_ESEQ, T_NAME, T_CONST, T_CALL } kind; ... };
```

```c
unCx(condition) -> { trues, falses, conditionalJump }
```

```text
Tr_procEntryExit(level, body, formals) -> fragment list
```

<Callout type="trap">
  **版本边界：**
  本页以中文C语言描述修订版为实现轨道。Java与ML版本共享主体算法，但模块接口、生成工具和代码片段不同，不能混用为同一实现证据。
</Callout>

## 权威目录逐项讲解

### 第7章 翻译成中间代码

“第7章 翻译成中间代码”必须落到C模块与结构化产物，而不只是术语。先在“选择翻译形式”固定输入、接口、数据结构和算法前提，再进入“翻译左值”比较第一条差异。围绕Tree IR记录所有权、复杂度、失败位置和下游消费者，防止后续阶段掩盖错误。

验收时分别检查语义、完备性、成本和模块边界：转换是否保持Tiger可观察行为，算法前提是否成立，时间空间代价是否符合规模，以及错误能否停在最早接口。修复后清理旧产物，从源文件重新生成并重放正常、边界、失败和优化样本。

### 7.1 中间表示树

“7.1 中间表示树”必须落到C模块与结构化产物，而不只是术语。先在“翻译左值”固定输入、接口、数据结构和算法前提，再进入“翻译控制流”比较第一条差异。围绕Ex/Nx/Cx记录所有权、复杂度、失败位置和下游消费者，防止后续阶段掩盖错误。

验收时分别检查语义、完备性、成本和模块边界：转换是否保持Tiger可观察行为，算法前提是否成立，时间空间代价是否符合规模，以及错误能否停在最早接口。修复后清理旧产物，从源文件重新生成并重放正常、边界、失败和优化样本。

### 7.2 翻译为树中间语言

“7.2 翻译为树中间语言”必须落到C模块与结构化产物，而不只是术语。先在“翻译控制流”固定输入、接口、数据结构和算法前提，再进入“生成静态链”比较第一条差异。围绕静态链记录所有权、复杂度、失败位置和下游消费者，防止后续阶段掩盖错误。

验收时分别检查语义、完备性、成本和模块边界：转换是否保持Tiger可观察行为，算法前提是否成立，时间空间代价是否符合规模，以及错误能否停在最早接口。修复后清理旧产物，从源文件重新生成并重放正常、边界、失败和优化样本。

### 7.2.1 表达式的种类

“7.2.1 表达式的种类”必须落到C模块与结构化产物，而不只是术语。先在“生成静态链”固定输入、接口、数据结构和算法前提，再进入“收集过程和字符串片段”比较第一条差异。围绕片段记录所有权、复杂度、失败位置和下游消费者，防止后续阶段掩盖错误。

验收时分别检查语义、完备性、成本和模块边界：转换是否保持Tiger可观察行为，算法前提是否成立，时间空间代价是否符合规模，以及错误能否停在最早接口。修复后清理旧产物，从源文件重新生成并重放正常、边界、失败和优化样本。

### 7.2.2 简单变量

“7.2.2 简单变量”必须落到C模块与结构化产物，而不只是术语。先在“收集过程和字符串片段”固定输入、接口、数据结构和算法前提，再进入“解释或模拟IR”比较第一条差异。围绕运行时调用记录所有权、复杂度、失败位置和下游消费者，防止后续阶段掩盖错误。

验收时分别检查语义、完备性、成本和模块边界：转换是否保持Tiger可观察行为，算法前提是否成立，时间空间代价是否符合规模，以及错误能否停在最早接口。修复后清理旧产物，从源文件重新生成并重放正常、边界、失败和优化样本。

### 7.2.3 追随静态链

“7.2.3 追随静态链”必须落到C模块与结构化产物，而不只是术语。先在“解释或模拟IR”固定输入、接口、数据结构和算法前提，再进入“选择翻译形式”比较第一条差异。围绕Tree IR记录所有权、复杂度、失败位置和下游消费者，防止后续阶段掩盖错误。

验收时分别检查语义、完备性、成本和模块边界：转换是否保持Tiger可观察行为，算法前提是否成立，时间空间代价是否符合规模，以及错误能否停在最早接口。修复后清理旧产物，从源文件重新生成并重放正常、边界、失败和优化样本。

### 7.2.4 数组变量

“7.2.4 数组变量”必须落到C模块与结构化产物，而不只是术语。先在“选择翻译形式”固定输入、接口、数据结构和算法前提，再进入“翻译左值”比较第一条差异。围绕Ex/Nx/Cx记录所有权、复杂度、失败位置和下游消费者，防止后续阶段掩盖错误。

验收时分别检查语义、完备性、成本和模块边界：转换是否保持Tiger可观察行为，算法前提是否成立，时间空间代价是否符合规模，以及错误能否停在最早接口。修复后清理旧产物，从源文件重新生成并重放正常、边界、失败和优化样本。

### 7.2.5 结构化的左值

“7.2.5 结构化的左值”必须落到C模块与结构化产物，而不只是术语。先在“翻译左值”固定输入、接口、数据结构和算法前提，再进入“翻译控制流”比较第一条差异。围绕静态链记录所有权、复杂度、失败位置和下游消费者，防止后续阶段掩盖错误。

验收时分别检查语义、完备性、成本和模块边界：转换是否保持Tiger可观察行为，算法前提是否成立，时间空间代价是否符合规模，以及错误能否停在最早接口。修复后清理旧产物，从源文件重新生成并重放正常、边界、失败和优化样本。

### 7.2.6 下标和域选择

“7.2.6 下标和域选择”必须落到C模块与结构化产物，而不只是术语。先在“翻译控制流”固定输入、接口、数据结构和算法前提，再进入“生成静态链”比较第一条差异。围绕片段记录所有权、复杂度、失败位置和下游消费者，防止后续阶段掩盖错误。

验收时分别检查语义、完备性、成本和模块边界：转换是否保持Tiger可观察行为，算法前提是否成立，时间空间代价是否符合规模，以及错误能否停在最早接口。修复后清理旧产物，从源文件重新生成并重放正常、边界、失败和优化样本。

### 7.2.7 关于安全性的劝告

“7.2.7 关于安全性的劝告”必须落到C模块与结构化产物，而不只是术语。先在“生成静态链”固定输入、接口、数据结构和算法前提，再进入“收集过程和字符串片段”比较第一条差异。围绕运行时调用记录所有权、复杂度、失败位置和下游消费者，防止后续阶段掩盖错误。

验收时分别检查语义、完备性、成本和模块边界：转换是否保持Tiger可观察行为，算法前提是否成立，时间空间代价是否符合规模，以及错误能否停在最早接口。修复后清理旧产物，从源文件重新生成并重放正常、边界、失败和优化样本。

### 7.2.8 算术操作

“7.2.8 算术操作”必须落到C模块与结构化产物，而不只是术语。先在“收集过程和字符串片段”固定输入、接口、数据结构和算法前提，再进入“解释或模拟IR”比较第一条差异。围绕Tree IR记录所有权、复杂度、失败位置和下游消费者，防止后续阶段掩盖错误。

验收时分别检查语义、完备性、成本和模块边界：转换是否保持Tiger可观察行为，算法前提是否成立，时间空间代价是否符合规模，以及错误能否停在最早接口。修复后清理旧产物，从源文件重新生成并重放正常、边界、失败和优化样本。

### 7.2.9 条件表达式

“7.2.9 条件表达式”必须落到C模块与结构化产物，而不只是术语。先在“解释或模拟IR”固定输入、接口、数据结构和算法前提，再进入“选择翻译形式”比较第一条差异。围绕Ex/Nx/Cx记录所有权、复杂度、失败位置和下游消费者，防止后续阶段掩盖错误。

验收时分别检查语义、完备性、成本和模块边界：转换是否保持Tiger可观察行为，算法前提是否成立，时间空间代价是否符合规模，以及错误能否停在最早接口。修复后清理旧产物，从源文件重新生成并重放正常、边界、失败和优化样本。

### 7.2.10 字符串

“7.2.10 字符串”必须落到C模块与结构化产物，而不只是术语。先在“选择翻译形式”固定输入、接口、数据结构和算法前提，再进入“翻译左值”比较第一条差异。围绕静态链记录所有权、复杂度、失败位置和下游消费者，防止后续阶段掩盖错误。

验收时分别检查语义、完备性、成本和模块边界：转换是否保持Tiger可观察行为，算法前提是否成立，时间空间代价是否符合规模，以及错误能否停在最早接口。修复后清理旧产物，从源文件重新生成并重放正常、边界、失败和优化样本。

### 7.2.11 记录和数组的创建

“7.2.11 记录和数组的创建”必须落到C模块与结构化产物，而不只是术语。先在“翻译左值”固定输入、接口、数据结构和算法前提，再进入“翻译控制流”比较第一条差异。围绕片段记录所有权、复杂度、失败位置和下游消费者，防止后续阶段掩盖错误。

验收时分别检查语义、完备性、成本和模块边界：转换是否保持Tiger可观察行为，算法前提是否成立，时间空间代价是否符合规模，以及错误能否停在最早接口。修复后清理旧产物，从源文件重新生成并重放正常、边界、失败和优化样本。

### 7.2.12 while循环

“7.2.12 while循环”必须落到C模块与结构化产物，而不只是术语。先在“翻译控制流”固定输入、接口、数据结构和算法前提，再进入“生成静态链”比较第一条差异。围绕运行时调用记录所有权、复杂度、失败位置和下游消费者，防止后续阶段掩盖错误。

验收时分别检查语义、完备性、成本和模块边界：转换是否保持Tiger可观察行为，算法前提是否成立，时间空间代价是否符合规模，以及错误能否停在最早接口。修复后清理旧产物，从源文件重新生成并重放正常、边界、失败和优化样本。

### 7.2.13 for循环

“7.2.13 for循环”必须落到C模块与结构化产物，而不只是术语。先在“生成静态链”固定输入、接口、数据结构和算法前提，再进入“收集过程和字符串片段”比较第一条差异。围绕Tree IR记录所有权、复杂度、失败位置和下游消费者，防止后续阶段掩盖错误。

验收时分别检查语义、完备性、成本和模块边界：转换是否保持Tiger可观察行为，算法前提是否成立，时间空间代价是否符合规模，以及错误能否停在最早接口。修复后清理旧产物，从源文件重新生成并重放正常、边界、失败和优化样本。

### 7.2.14 函数调用

“7.2.14 函数调用”必须落到C模块与结构化产物，而不只是术语。先在“收集过程和字符串片段”固定输入、接口、数据结构和算法前提，再进入“解释或模拟IR”比较第一条差异。围绕Ex/Nx/Cx记录所有权、复杂度、失败位置和下游消费者，防止后续阶段掩盖错误。

验收时分别检查语义、完备性、成本和模块边界：转换是否保持Tiger可观察行为，算法前提是否成立，时间空间代价是否符合规模，以及错误能否停在最早接口。修复后清理旧产物，从源文件重新生成并重放正常、边界、失败和优化样本。

### 7.3 声明

“7.3 声明”必须落到C模块与结构化产物，而不只是术语。先在“解释或模拟IR”固定输入、接口、数据结构和算法前提，再进入“选择翻译形式”比较第一条差异。围绕静态链记录所有权、复杂度、失败位置和下游消费者，防止后续阶段掩盖错误。

验收时分别检查语义、完备性、成本和模块边界：转换是否保持Tiger可观察行为，算法前提是否成立，时间空间代价是否符合规模，以及错误能否停在最早接口。修复后清理旧产物，从源文件重新生成并重放正常、边界、失败和优化样本。

### 7.3.1 变量定义

“7.3.1 变量定义”必须落到C模块与结构化产物，而不只是术语。先在“选择翻译形式”固定输入、接口、数据结构和算法前提，再进入“翻译左值”比较第一条差异。围绕片段记录所有权、复杂度、失败位置和下游消费者，防止后续阶段掩盖错误。

验收时分别检查语义、完备性、成本和模块边界：转换是否保持Tiger可观察行为，算法前提是否成立，时间空间代价是否符合规模，以及错误能否停在最早接口。修复后清理旧产物，从源文件重新生成并重放正常、边界、失败和优化样本。

### 7.3.2 函数定义

“7.3.2 函数定义”必须落到C模块与结构化产物，而不只是术语。先在“翻译左值”固定输入、接口、数据结构和算法前提，再进入“翻译控制流”比较第一条差异。围绕运行时调用记录所有权、复杂度、失败位置和下游消费者，防止后续阶段掩盖错误。

验收时分别检查语义、完备性、成本和模块边界：转换是否保持Tiger可观察行为，算法前提是否成立，时间空间代价是否符合规模，以及错误能否停在最早接口。修复后清理旧产物，从源文件重新生成并重放正常、边界、失败和优化样本。

### 7.3.3 片段

“7.3.3 片段”必须落到C模块与结构化产物，而不只是术语。先在“翻译控制流”固定输入、接口、数据结构和算法前提，再进入“生成静态链”比较第一条差异。围绕Tree IR记录所有权、复杂度、失败位置和下游消费者，防止后续阶段掩盖错误。

验收时分别检查语义、完备性、成本和模块边界：转换是否保持Tiger可观察行为，算法前提是否成立，时间空间代价是否符合规模，以及错误能否停在最早接口。修复后清理旧产物，从源文件重新生成并重放正常、边界、失败和优化样本。

### 程序设计：翻译成树

“程序设计：翻译成树”必须落到C模块与结构化产物，而不只是术语。先在“生成静态链”固定输入、接口、数据结构和算法前提，再进入“收集过程和字符串片段”比较第一条差异。围绕Ex/Nx/Cx记录所有权、复杂度、失败位置和下游消费者，防止后续阶段掩盖错误。

验收时分别检查语义、完备性、成本和模块边界：转换是否保持Tiger可观察行为，算法前提是否成立，时间空间代价是否符合规模，以及错误能否停在最早接口。修复后清理旧产物，从源文件重新生成并重放正常、边界、失败和优化样本。

## 分步视觉验证

先预测当前阶段的树、图、集合、表项或指令，再运行C模块；若不同，停在第一处分叉并保留原始证据。

<Stepper>
  <Step title="画出模块与数据结构">
    标出Tree
    IR、Ex/Nx/Cx、静态链、片段、运行时调用的输入、输出、所有权、前提和下游消费者。
    <Tbc07TranslationIntermediateCodeMapLab />
  </Step>
  <Step title="运行单变量Tiger实验">
    固定编译器与目标模型，只改变一个语言、分析、运行时或机器条件。
    <Tbc07TranslationIntermediateCodeExperimentLab />
  </Step>
  <Step title="核对第一处分叉">
    比较结构化产物、复杂度和行为，清理旧输出后完整重建并重放回归样本。
    <Tbc07TranslationIntermediateCodeEvidenceLab />
  </Step>
</Stepper>

## 常见误区

<Callout type="trap">
  **只看最终输出：** 没有Token、AST、环境、Tree
  IR、CFG、活跃集合、干涉图和帧布局，就无法把错误归因到具体C模块，也无法证明转换保持语义。
</Callout>

<Callout type="trap">
  **混用三种版本：**
  C版使用Lex/Flex、Yacc/Bison和C结构体接口；ML版与Java版的模块代码只能作对照，不能替代C版项目的编译、内存和ABI证据。
</Callout>

## 四类样本与通过条件

| 样本 | 单一变量               | 必查证据         | 通过条件         |
| ---- | ---------------------- | ---------------- | ---------------- |
| 正常 | 一个最小Tiger构造      | 全阶段快照       | 与预测一致       |
| 边界 | 深度、宽度、压力或布局 | 临界分支和成本   | 边界可解释       |
| 失败 | 一个语法、事实或约束   | 首错与下游停止   | 不产出伪结果     |
| 优化 | 一个变换或策略         | 前后IR和差分执行 | 等价且收益可解释 |

## 练习

<Exercises>

**问题 1** 为什么不能只凭目标程序运行成功验收第7章 翻译成中间代码？

<Answer>
  最终行为可能隐藏错误Token、树结构、Frame访问、数据流事实或机器约束。应保存每个C模块的结构化产物，定位第一处分叉，并从Tiger源文件干净重建。
</Answer>

**问题 2** 如何把“翻译控制流”变成可回归实验？

<Answer>
  固定Tiger输入、编译器提交、C工具链、运行时和目标机，先预测Tree
  IR与Ex/Nx/Cx的中间状态，只改变一个条件，并保留四类样本。
</Answer>

**问题 3** 本页最小通过条件是什么？

<Answer>
  每个AST构造得到语义等价IR，求值顺序与副作用不丢失，条件跳转的真假出口最终闭合，静态链到达声明层次。
  修复后清理旧对象和中间产物，完整重建全部下游结果，并让历史样本回到基线。
</Answer>

</Exercises>

## 小结

- Tree IR：连接“选择翻译形式”的C接口、结构化产物、成本和回归证据。
- Ex/Nx/Cx：连接“翻译左值”的C接口、结构化产物、成本和回归证据。
- 静态链：连接“翻译控制流”的C接口、结构化产物、成本和回归证据。
- 片段：连接“生成静态链”的C接口、结构化产物、成本和回归证据。
- 运行时调用：连接“收集过程和字符串片段”的C接口、结构化产物、成本和回归证据。
- 本页22个核心目录层级均已进入可追溯的Tiger编译证据链。

## 术语表

<Glossary>
  <GlossaryItem term="Tree IR">
    Tree
    IR用于解释“选择翻译形式”，需要同时给出输入接口、算法前提、结构化结果、复杂度和清理重建方法。
  </GlossaryItem>
  <GlossaryItem term="Ex/Nx/Cx">
    Ex/Nx/Cx用于解释“翻译左值”，需要同时给出输入接口、算法前提、结构化结果、复杂度和清理重建方法。
  </GlossaryItem>
  <GlossaryItem term="静态链">
    静态链用于解释“翻译控制流”，需要同时给出输入接口、算法前提、结构化结果、复杂度和清理重建方法。
  </GlossaryItem>
  <GlossaryItem term="片段">
    片段用于解释“生成静态链”，需要同时给出输入接口、算法前提、结构化结果、复杂度和清理重建方法。
  </GlossaryItem>
  <GlossaryItem term="运行时调用">
    运行时调用用于解释“收集过程和字符串片段”，需要同时给出输入接口、算法前提、结构化结果、复杂度和清理重建方法。
  </GlossaryItem>
</Glossary>

<Attribution
  adaptedFrom="Andrew W. Appel、Maia Ginsburg著，赵克佳、黄春、沈志宇译《现代编译原理：C语言描述（修订版）》，人民邮电出版社，2018，385页，ISBN 9787115476883 · 基于Andrew W. Appel官方完整目录、Cambridge C版资料与中文版修订版目录的独立教学重构"
  adaptedUrl="https://www.cs.princeton.edu/~appel/modern/toc.html"
/>
