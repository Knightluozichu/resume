---
title: 第14章 面向对象的语言
type: C
section: 第14章 面向对象的语言
order: 14
description: 把类、单/多继承、成员测试、私有访问和对象优化降到运行时布局；用C编译流水线、状态轨迹和端到端验证门交付类布局、vtable、指针调整、访问检查与分派轨迹
demo: true
math: true
sourceUrl: "https://www.cs.princeton.edu/~appel/modern/toc.html"
draft: false
qualityVersion: 2
practiceMode: simulation
sourceMode: independent-rewrite
officialUnitId: tbc-unit-14
---

import {
  Objectives,
  Term,
  Callout,
  Stepper,
  Step,
  Exercises,
  Answer,
  Glossary,
  GlossaryItem,
  Attribution,
} from "@/components/mdx/mdx-components";
import {
  Tbc14ObjectOrientedLanguagesPipelineContractLab,
  Tbc14ObjectOrientedLanguagesStateTraceLab,
  Tbc14ObjectOrientedLanguagesVerificationGateLab,
} from "@/components/mdx/tiger-book-compiler/v2/tbc-14-object-oriented-languages";

<Objectives>

- 能说明“第14章 面向对象的语言”如何把类、单/多继承、成员测试、私有访问和对象优化降到运行时布局，并区分作者C/Java/ML轨道、中文C版与本站重写
- 能先预测“字段、方法、类型测试和动态分派怎样在继承层次中保持一致？”会改变哪一个C接口、树/图状态、AST/IR、汇编或运行结果，再操作三类交互证据
- 能只注入“多继承对象指针调整遗漏一个基类偏移”，定位首个偏离“第14章 面向对象的语言的C模块接口、数据结构、状态变换、输出语义与测试始终一致”的状态，并从同一快照完成恢复

</Objectives>

## 为什么从这个问题开始

“第14章 面向对象的语言”围绕“字段、方法、类型测试和动态分派怎样在继承层次中保持一致？”建立贯穿任务：在Tiger C实现轨道中复现“第14章 面向对象的语言”的输入、状态、变换与验证。先写下哪个C接口、树/图状态、AST/IR、汇编或运行结果会最先变化，再运行参考、故障和恢复路径；运行后补理由不算预测。只有守住“第14章 面向对象的语言的C模块接口、数据结构、状态变换、输出语义与测试始终一致”并交付类布局、vtable、指针调整、访问检查与分派轨迹，单模块测试、编译成功或性能变化才构成机制证据。

## 原版书目、296个正式坐标与C轨道边界

“第14章 面向对象的语言”以[Andrew W. Appel作者官方目录](https://www.cs.princeton.edu/~appel/modern/toc.html)核对 _Modern Compiler Implementation_ 的C、Java、ML三种实现轨道、2部分、21章和Tiger语言附录；以[作者C版页面](https://www.cs.princeton.edu/~appel/modern/c/)核对Andrew·W·Appel与Maia Ginsburg、Cambridge University Press、C实现轨道、平装ISBN 0-521-60765-5、软件/练习模块与勘误入口，再以[Cambridge C版页面](https://www.cambridge.org/core/books/modern-compiler-implementation-in-c/0F85704413FC010C1D1C691C4D2A0865/listing)核对原版身份。

“第14章 面向对象的语言”以[中文版修订版完整目录](https://www.tenlong.com.tw/products/9787115476883?list_name=trs-t)核对赵克佳、黄春、沈志宇译《现代编译原理：C语言描述（修订版）》，人民邮电出版社，2018年，385页，ISBN 9787115476883。“第14章 面向对象的语言”的正式分母为2个部分标题、21个章标题、1个附录标题、251个编号节/小节、附录4节和17个正式“程序设计”项目，合计296个核心层级；每章重复的推荐阅读与习题不另计。

作者页面可访问不等于允许复制书稿，“第14章 面向对象的语言”不复制、翻译或改写原文、图表、伪码与习题；所有中文讲解、C接口示意、状态轨迹、反例、交互、练习和答案均为独立教学重写。[本页独立核对 1](https://llvm.org/docs/LangRef.html)只用于核对本页C实现或现代IR边界，不能混入Java/ML轨道或反向证明原书采用本站表述。

## 原版目录层级与C项目机制

### 第14章 面向对象的语言

<Term def="面向对象的语言对应正式目录坐标“第14章 面向对象的语言”，在“第14章 面向对象的语言”中用于把对象布局、动态分派或闭包环境与调用约定降到IR，并受C接口、内存所有权、状态、目标机与验证边界约束。">
  面向对象的语言
</Term>

**正式坐标 1/9。** 原版目录键 `第14章 面向对象的语言`。在“第14章 面向对象的语言”的第1个正式坐标中，「第14章 面向对象的语言」通过把对象布局、动态分派或闭包环境与调用约定降到IR推进对象布局、继承与动态分派；复核者保存类/vtable布局、自由变量、闭包对象、调用和求值轨迹，出现对象偏移、自由变量、环境槽或求值策略错误就撤回结论。

### 14·1 类

<Term def="类对应正式目录坐标“14.1 类”，在“第14章 面向对象的语言”中用于把对象布局、动态分派或闭包环境与调用约定降到IR，并受C接口、内存所有权、状态、目标机与验证边界约束。">
  类
</Term>

**正式坐标 2/9。** 原版目录键 `14.1 类`。围绕“字段、方法、类型测试和动态分派怎样在继承层次中保持一致？”，“第14章 面向对象的语言”在坐标2把「14·1 类」落实为把对象布局、动态分派或闭包环境与调用约定降到IR；只有类/vtable布局、自由变量、闭包对象、调用和求值轨迹可重放且反例排除对象偏移、自由变量、环境槽或求值策略错误，本节点才算掌握。

### 14·2 数据域的单继承性

<Term def="数据域的单继承性对应正式目录坐标“14.2 数据域的单继承性”，在“第14章 面向对象的语言”中用于把对象布局、动态分派或闭包环境与调用约定降到IR，并受C接口、内存所有权、状态、目标机与验证边界约束。">
  数据域的单继承性
</Term>

**正式坐标 3/9。** 原版目录键 `14.2 数据域的单继承性`。“第14章 面向对象的语言”的目录节点3「14·2 数据域的单继承性」不能停在术语或伪码：它要把对象布局、动态分派或闭包环境与调用约定降到IR，交付类/vtable布局、自由变量、闭包对象、调用和求值轨迹，并把对象偏移、自由变量、环境槽或求值策略错误设为单一反事实。

### 14·3 多继承

<Term def="多继承对应正式目录坐标“14.3 多继承”，在“第14章 面向对象的语言”中用于把对象布局、动态分派或闭包环境与调用约定降到IR，并受C接口、内存所有权、状态、目标机与验证边界约束。">
  多继承
</Term>

**正式坐标 4/9。** 原版目录键 `14.3 多继承`。对“第14章 面向对象的语言”而言，「14·3 多继承」在第4次检查中改变可观察状态，因为它负责把对象布局、动态分派或闭包环境与调用约定降到IR；类/vtable布局、自由变量、闭包对象、调用和求值轨迹必须与“第14章 面向对象的语言的C模块接口、数据结构、状态变换、输出语义与测试始终一致”对齐，不能接受对象偏移、自由变量、环境槽或求值策略错误。

### 14·4 测试类成员关系

<Term def="测试类成员关系对应正式目录坐标“14.4 测试类成员关系”，在“第14章 面向对象的语言”中用于把对象布局、动态分派或闭包环境与调用约定降到IR，并受C接口、内存所有权、状态、目标机与验证边界约束。">
  测试类成员关系
</Term>

**正式坐标 5/9。** 原版目录键 `14.4 测试类成员关系`。在“第14章 面向对象的语言”的第5个正式坐标中，「14·4 测试类成员关系」通过把对象布局、动态分派或闭包环境与调用约定降到IR推进对象布局、继承与动态分派；复核者保存类/vtable布局、自由变量、闭包对象、调用和求值轨迹，出现对象偏移、自由变量、环境槽或求值策略错误就撤回结论。

### 14·5 私有域和私有方法

<Term def="私有域和私有方法对应正式目录坐标“14.5 私有域和私有方法”，在“第14章 面向对象的语言”中用于把对象布局、动态分派或闭包环境与调用约定降到IR，并受C接口、内存所有权、状态、目标机与验证边界约束。">
  私有域和私有方法
</Term>

**正式坐标 6/9。** 原版目录键 `14.5 私有域和私有方法`。围绕“字段、方法、类型测试和动态分派怎样在继承层次中保持一致？”，“第14章 面向对象的语言”在坐标6把「14·5 私有域和私有方法」落实为把对象布局、动态分派或闭包环境与调用约定降到IR；只有类/vtable布局、自由变量、闭包对象、调用和求值轨迹可重放且反例排除对象偏移、自由变量、环境槽或求值策略错误，本节点才算掌握。

### 14·6 无类语言

<Term def="无类语言对应正式目录坐标“14.6 无类语言”，在“第14章 面向对象的语言”中用于把对象布局、动态分派或闭包环境与调用约定降到IR，并受C接口、内存所有权、状态、目标机与验证边界约束。">
  无类语言
</Term>

**正式坐标 7/9。** 原版目录键 `14.6 无类语言`。“第14章 面向对象的语言”的目录节点7「14·6 无类语言」不能停在术语或伪码：它要把对象布局、动态分派或闭包环境与调用约定降到IR，交付类/vtable布局、自由变量、闭包对象、调用和求值轨迹，并把对象偏移、自由变量、环境槽或求值策略错误设为单一反事实。

### 14·7 面向对象程序的优化

<Term def="面向对象程序的优化对应正式目录坐标“14.7 面向对象程序的优化”，在“第14章 面向对象的语言”中用于把对象布局、动态分派或闭包环境与调用约定降到IR，并受C接口、内存所有权、状态、目标机与验证边界约束。">
  面向对象程序的优化
</Term>

**正式坐标 8/9。** 原版目录键 `14.7 面向对象程序的优化`。对“第14章 面向对象的语言”而言，「14·7 面向对象程序的优化」在第8次检查中改变可观察状态，因为它负责把对象布局、动态分派或闭包环境与调用约定降到IR；类/vtable布局、自由变量、闭包对象、调用和求值轨迹必须与“第14章 面向对象的语言的C模块接口、数据结构、状态变换、输出语义与测试始终一致”对齐，不能接受对象偏移、自由变量、环境槽或求值策略错误。

### 程序设计：OBJECT-Tiger

<Term def="OBJECT-Tiger对应正式目录坐标“程序设计：OBJECT-Tiger”，在“第14章 面向对象的语言”中用于把“OBJECT-Tiger”放进对象布局、继承与动态分派的C接口—状态—变换—验证链，并受C接口、内存所有权、状态、目标机与验证边界约束。">
  OBJECT-Tiger
</Term>

**正式坐标 9/9。** 原版目录键 `程序设计：OBJECT-Tiger`。在“第14章 面向对象的语言”的第9个正式坐标中，「程序设计：OBJECT-Tiger」通过把“OBJECT-Tiger”放进对象布局、继承与动态分派的C接口—状态—变换—验证链推进对象布局、继承与动态分派；复核者保存第14章 面向对象的语言的输入角色、中间状态、输出、反例与整合证据，出现只复述“OBJECT-Tiger”名称而没有可观察状态、项目实现和恢复验证就撤回结论。

## 先预测，再操作三个C轨道实验

<Callout type="info" title="先写出哪个状态会最先变化">
  对“第14章 面向对象的语言”先冻结在Tiger C实现轨道中复现“第14章
  面向对象的语言”的输入、状态、变换与验证的C接口、源程序、规则、数据结构、目标机、预算和验证口径，再操作三类实验；结果与预测不同就修改假设，不删除失败运行。
</Callout>

<Stepper>
  <Step title="1. C模块、输入与翻译流水线">
    为“第14章 面向对象的语言”选择正式目录坐标，在参考流水线与单一故障间切换，逐阶段核对接口、状态、输出和不变量。

    <Tbc14ObjectOrientedLanguagesPipelineContractLab />

  </Step>
  <Step title="2. 状态、不变量与故障轨迹">
    保持“第14章 面向对象的语言”的在Tiger C实现轨道中复现“第14章 面向对象的语言”的输入、状态、变换与验证不变，只注入“多继承对象指针调整遗漏一个基类偏移”，逐步定位首个偏离“第14章 面向对象的语言的C模块接口、数据结构、状态变换、输出语义与测试始终一致”的位置。

    <Tbc14ObjectOrientedLanguagesStateTraceLab />

  </Step>
  <Step title="3. 整合与端到端验证门">
    在“第14章 面向对象的语言”的基线、单故障和恢复案例间切换，展开轨道、C接口、变换和端到端验证门后再决定是否交付。

    <Tbc14ObjectOrientedLanguagesVerificationGateLab />

  </Step>
</Stepper>

<Callout type="trap" title="本页单一故障：多继承对象指针调整遗漏一个基类偏移">
  “第14章
  面向对象的语言”遇到该故障时必须保持其余C接口、输入、规则、目标机、预算和执行顺序不变；单模块测试通过不能替代首个状态分岔、跨模块整合和恢复证据。
</Callout>

<Callout type="trap" title="程序设计项目不是可跳过的章末装饰">
  “第14章
  面向对象的语言”中的正式程序设计项目负责把目录概念变成C模块、状态与接口证据；只背算法、看伪码或混用Java/ML模块，不能证明C版Tiger编译器可运行。
</Callout>

<Callout type="trap" title="作者页面可访问不等于允许复制">
  “第14章
  面向对象的语言”可以用作者目录、C版资源和Cambridge页面核对范围，但没有获得复制原文、图表、伪码或习题的授权；当前内容必须保持独立表达与轨道边界。
</Callout>

## 第14章 面向对象的语言的可重放协议

| 阶段                                | 允许动作                                                                                  | 必留证据                                                | 拒绝条件                                                                                                        |
| ----------------------------------- | ----------------------------------------------------------------------------------------- | ------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------- |
| 第14章 面向对象的语言 · C模块与输入 | 冻结对象布局、继承与动态分派所需的C接口、源程序、规则、数据结构和版本                     | 第14章 面向对象的语言的模块合同、输入快照与所有权表     | 未满足“第14章 面向对象的语言的类型、所有权、源位置、名字和接口布局没有错位”                                     |
| 第14章 面向对象的语言 · 算法与状态  | 执行把类、单/多继承、成员测试、私有访问和对象优化降到运行时布局的最小算法并保存每一步状态 | 第14章 面向对象的语言的参考轨迹、故障轨迹与首个状态分岔 | 未满足“第14章 面向对象的语言每一步可由同一C接口、输入、规则和执行顺序复算”                                      |
| 第14章 面向对象的语言 · 输出与整合  | 比较变换前后AST/IR/汇编/运行时状态和跨模块传递                                            | 第14章 面向对象的语言的前后差、接口谱系与恢复路径       | 未满足“第14章 面向对象的语言没有把单模块通过或单一样例正确当作端到端正确性”                                     |
| 第14章 面向对象的语言 · 独立验证    | 重放预测、单故障、恢复和不适用边界                                                        | 第14章 面向对象的语言的接受、回退或拒绝理由             | 未满足“第14章 面向对象的语言满足“第14章 面向对象的语言的C模块接口、数据结构、状态变换、输出语义与测试始终一致”” |

```yaml
unit: "tbc-unit-14"
question: "字段、方法、类型测试和动态分派怎样在继承层次中保持一致？"
scenario: "在Tiger C实现轨道中复现“第14章 面向对象的语言”的输入、状态、变换与验证"
invariant: "第14章 面向对象的语言的C模块接口、数据结构、状态变换、输出语义与测试始终一致"
fault: "多继承对象指针调整遗漏一个基类偏移"
evidence: "类布局、vtable、指针调整、访问检查与分派轨迹"
reset: restore_concept_mode_stage_trace_step_case_gates_and_artifact
```

“第14章 面向对象的语言”要求从同一C接口、源程序、规则、数据结构、目标机、预算和执行顺序重放参考、故障与恢复路径。重置后若目录选择、模式、阶段、轨迹步骤、案例、证据门或交付包没有回到基线，本次比较已经混入状态泄漏。

## 本页回顾

掌握“第14章 面向对象的语言”不是背术语、表格或伪码，而是围绕“字段、方法、类型测试和动态分派怎样在继承层次中保持一致？”重建C接口、输入、状态变换、输出、整合和端到端验证，并用“第14章 面向对象的语言的C模块接口、数据结构、状态变换、输出语义与测试始终一致”拒绝“多继承对象指针调整遗漏一个基类偏移”。最终交付为类布局、vtable、指针调整、访问检查与分派轨迹。

## 练习与答案

<Exercises>

1. **问题 1：C实现合同。** “第14章 面向对象的语言”为什么必须先冻结C接口、源程序、规则、数据结构、目标机和验证口径？

<Answer>
  若同时改变这些条件，相同输出可能来自不同模块布局、状态、变换或目标机；先冻结合同，才能把观测连接到单一机制并定位首差。
</Answer>

2. **问题 2：目录逐项覆盖。** 怎样证明“第14章 面向对象的语言”的正式目录坐标已经进入机制、交互和程序设计项目？

<Answer>
  1. “第14章 面向对象的语言”的目录项「第14章
  面向对象的语言」：以“把对象布局、动态分派或闭包环境与调用约定降到IR”解释作用，用“类/vtable布局、自由变量、闭包对象、调用和求值轨迹”提供复核。
  2. “第14章 面向对象的语言”的目录项「14·1
  类」：以“把对象布局、动态分派或闭包环境与调用约定降到IR”解释作用，用“类/vtable布局、自由变量、闭包对象、调用和求值轨迹”提供复核。
  3. “第14章 面向对象的语言”的目录项「14·2
  数据域的单继承性」：以“把对象布局、动态分派或闭包环境与调用约定降到IR”解释作用，用“类/vtable布局、自由变量、闭包对象、调用和求值轨迹”提供复核。
  4. “第14章 面向对象的语言”的目录项「14·3
  多继承」：以“把对象布局、动态分派或闭包环境与调用约定降到IR”解释作用，用“类/vtable布局、自由变量、闭包对象、调用和求值轨迹”提供复核。
  5. “第14章 面向对象的语言”的目录项「14·4
  测试类成员关系」：以“把对象布局、动态分派或闭包环境与调用约定降到IR”解释作用，用“类/vtable布局、自由变量、闭包对象、调用和求值轨迹”提供复核。
  6. “第14章 面向对象的语言”的目录项「14·5
  私有域和私有方法」：以“把对象布局、动态分派或闭包环境与调用约定降到IR”解释作用，用“类/vtable布局、自由变量、闭包对象、调用和求值轨迹”提供复核。
  7. “第14章 面向对象的语言”的目录项「14·6
  无类语言」：以“把对象布局、动态分派或闭包环境与调用约定降到IR”解释作用，用“类/vtable布局、自由变量、闭包对象、调用和求值轨迹”提供复核。
  8. “第14章 面向对象的语言”的目录项「14·7
  面向对象程序的优化」：以“把对象布局、动态分派或闭包环境与调用约定降到IR”解释作用，用“类/vtable布局、自由变量、闭包对象、调用和求值轨迹”提供复核。
  9. “第14章
  面向对象的语言”的目录项「程序设计：OBJECT-Tiger」：以“把“OBJECT-Tiger”放进对象布局、继承与动态分派的C接口—状态—变换—验证链”解释作用，用“第14章
  面向对象的语言的输入角色、中间状态、输出、反例与整合证据”提供复核。
</Answer>

3. **问题 3：故障恢复。** 怎样证明“多继承对象指针调整遗漏一个基类偏移”已经被修正？

<Answer>
  为“第14章
  面向对象的语言”复用同一C接口、输入、规则、数据结构、目标机、预算和顺序，重放参考路径后只注入该故障；记录首个偏离，撤销故障再运行。只有流水线合同、状态轨迹、端到端验证门和类布局、vtable、指针调整、访问检查与分派轨迹重新满足“第14章
  面向对象的语言的C模块接口、数据结构、状态变换、输出语义与测试始终一致”，修正才可提交。
</Answer>

</Exercises>

## 名词解释

<Glossary>
  <GlossaryItem term="面向对象的语言">
    检索键 tbc-A 对应正式目录坐标「第14章 面向对象的语言」；在“第14章
    面向对象的语言”中用于把对象布局、动态分派或闭包环境与调用约定降到IR，需要连接C轨道范围、状态轨迹、整合证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="类">
    检索键 tbc-B 对应正式目录坐标「14·1 类」；在“第14章
    面向对象的语言”中用于把对象布局、动态分派或闭包环境与调用约定降到IR，需要连接C轨道范围、状态轨迹、整合证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="数据域的单继承性">
    检索键 tbc-C 对应正式目录坐标「14·2 数据域的单继承性」；在“第14章
    面向对象的语言”中用于把对象布局、动态分派或闭包环境与调用约定降到IR，需要连接C轨道范围、状态轨迹、整合证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="多继承">
    检索键 tbc-D 对应正式目录坐标「14·3 多继承」；在“第14章
    面向对象的语言”中用于把对象布局、动态分派或闭包环境与调用约定降到IR，需要连接C轨道范围、状态轨迹、整合证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="测试类成员关系">
    检索键 tbc-E 对应正式目录坐标「14·4 测试类成员关系」；在“第14章
    面向对象的语言”中用于把对象布局、动态分派或闭包环境与调用约定降到IR，需要连接C轨道范围、状态轨迹、整合证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="私有域和私有方法">
    检索键 tbc-F 对应正式目录坐标「14·5 私有域和私有方法」；在“第14章
    面向对象的语言”中用于把对象布局、动态分派或闭包环境与调用约定降到IR，需要连接C轨道范围、状态轨迹、整合证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="无类语言">
    检索键 tbc-G 对应正式目录坐标「14·6 无类语言」；在“第14章
    面向对象的语言”中用于把对象布局、动态分派或闭包环境与调用约定降到IR，需要连接C轨道范围、状态轨迹、整合证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="面向对象程序的优化">
    检索键 tbc-H 对应正式目录坐标「14·7 面向对象程序的优化」；在“第14章
    面向对象的语言”中用于把对象布局、动态分派或闭包环境与调用约定降到IR，需要连接C轨道范围、状态轨迹、整合证据和不适用边界。
  </GlossaryItem>
  <GlossaryItem term="OBJECT-Tiger">
    检索键 tbc-I 对应正式目录坐标「程序设计：OBJECT-Tiger」；在“第14章
    面向对象的语言”中用于把“OBJECT-Tiger”放进对象布局、继承与动态分派的C接口—状态—变换—验证链，需要连接C轨道范围、状态轨迹、整合证据和不适用边界。
  </GlossaryItem>
</Glossary>

<Attribution
  mode="independent-rewrite"
  sourceBasis="author-official-three-track-toc-plus-c-edition-and-chinese-revised-toc"
  workTitle="Andrew W. Appel、Maia Ginsburg著《Modern Compiler Implementation in C》"
  adaptedUrl="https://www.cs.princeton.edu/~appel/modern/c/"
/>
