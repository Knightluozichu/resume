---
title: "第16章 多态类型"
type: C
section: "第二部分 · 运行时与现代语言"
order: 16
description: "第16章 多态类型逐项覆盖15个核心目录层级，以C模块、Tiger中间产物和运行证据建立可复查实验。"
demo: true
math: false
sourceUrl: "https://www.cs.princeton.edu/~appel/modern/toc.html"
draft: true
---

import {
  Tbc16PolymorphicTypesMapLab,
  Tbc16PolymorphicTypesExperimentLab,
  Tbc16PolymorphicTypesEvidenceLab,
} from "@/components/mdx/tiger-book-compiler/diagrams/tbc-16-polymorphic-types";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第16章 多态类型在Tiger编译器中的输入、输出、C接口和责任边界
- 能描述本页15个核心目录层级之间的数据结构、算法前提与工程取舍
- 能复现最小C模块或Tiger样本，保存正常、边界、失败和优化四类证据
- 能判断“合一满足occurs check并产生最一般替换，泛化只作用于不受环境约束的变量，递归类型合法，表示选择与实例化类型一致。”是否成立，而不是只看最终二进制是否运行

</Objectives>

## 为什么最终二进制正确仍然不够

<Term def="参数多态连接“生成类型变量”的C接口、结构化产物、成本和首错证据。">参数多态</Term>、<Term def="类型推论连接“收集约束”的C接口、结构化产物、成本和首错证据。">类型推论</Term>、<Term def="合一连接“执行合一”的C接口、结构化产物、成本和首错证据。">合一</Term>、<Term def="Hindley-Milner连接“泛化类型方案”的C接口、结构化产物、成本和首错证据。">Hindley-Milner</Term>、<Term def="装箱连接“实例化使用点”的C接口、结构化产物、成本和首错证据。">装箱</Term>
共同构成本页坐标。从显式参数多态进入Hindley-Milner类型推论、递归数据类型和运行时表示，比较装箱、强制分析与类型参数传递。

虎书把教材组织成一项连续实现工程。扫描器、分析器、AST、环境、Frame、Tree IR、规范化、抽象汇编、流图、干涉图和运行时彼此依赖；任何阶段的错误都可能被后续样本偶然掩盖。因此每次实验都要保存阶段输入、输出和不变量，定位第一处偏离，而不是只观察最后进程的退出码。

## C版Tiger实验账本

实验固定Tiger源文件、编译器提交、C编译器与选项、目标ISA、运行时版本和随机种子。前端保存Token、语法树、作用域与类型；中端保存Frame访问、Tree IR、线性IR、基本块和轨迹；后端保存抽象汇编、def/use、CFG、活跃集合、干涉图、颜色与溢出重写；高级章保存堆图、类布局、闭包、类型约束、数据流事实、SSA、调度表与cache事件。

样本分为四组。正常样本覆盖主路径；边界样本覆盖空列表、深嵌套、递归声明、逃逸变量、临界立即数、不可归约流图、寄存器压力、循环依赖、别名和堆边界；失败样本只破坏一个词法、文法、类型、IR或机器约束；优化样本保存变换前后版本并进行解释器或本机差分执行。

诊断记录模块、源位置或IR位置、实际状态、期望状态、相关前驱和首要根因。模块失败后不得继续读取过期缓存；变换若令活跃性、支配、别名、调用或布局事实失效，必须显式重算。正确性与性能分开验收：正确性要求语言语义和ABI约束成立，性能要求给出静态成本、动态计数、代码大小、硬件和运行时环境。

每个目录项进入追溯矩阵：至少关联一个C接口、一个输入、一个算法前提、一份结构化产物、一个失败对照和一个回归断言。跨章数据同时标明生产者、消费者和失效条件，从而发现“有名词无实现”的空洞覆盖，并把回归范围限制在真实受影响模块。

## 本章核心推演

从显式参数多态进入Hindley-Milner类型推论、递归数据类型和运行时表示，比较装箱、强制分析与类型参数传递。 实现前先写出参数多态与类型推论的最小合同：输入表示、输出表示、所有权、错误返回和复杂度。随后准备只有一个语言构造的Tiger样本，让生成类型变量到选择运行时表示每一步都产生可序列化证据；若中间结果无法独立检查，就说明接口还没有真正解耦。

正确样本之外必须构造一个最小反例。反例只破坏合一的单个前提，并断言诊断模块、位置和下游停止点。修复不能只更改最后输出，而应让第一处分叉消失，同时保持Hindley-Milner和装箱的历史快照不变。性能实验则固定输入和机器，分别记录算法操作次数、内存峰值与最终运行时间，解释模型和实测偏差。

最终验收从干净目录开始，重新编译C模块、生成全部中间产物、链接Tiger运行时并执行差分测试。任何手工保留的旧对象文件、旧分析集合或旧寄存器映射都会破坏证据链；因此构建摘要、工具版本和所有阶段摘要必须一起归档，才能复现结论。

## 六阶段证据链

### 1. 生成类型变量

围绕参数多态先预测树、图、集合、表项或指令，只改变一个条件再运行；进入“收集约束”前，核对语义、接口、成本和首错位置。

### 2. 收集约束

围绕类型推论先预测树、图、集合、表项或指令，只改变一个条件再运行；进入“执行合一”前，核对语义、接口、成本和首错位置。

### 3. 执行合一

围绕合一先预测树、图、集合、表项或指令，只改变一个条件再运行；进入“泛化类型方案”前，核对语义、接口、成本和首错位置。

### 4. 泛化类型方案

围绕Hindley-Milner先预测树、图、集合、表项或指令，只改变一个条件再运行；进入“实例化使用点”前，核对语义、接口、成本和首错位置。

### 5. 实例化使用点

围绕装箱先预测树、图、集合、表项或指令，只改变一个条件再运行；进入“选择运行时表示”前，核对语义、接口、成本和首错位置。

### 6. 选择运行时表示

围绕参数多态先预测树、图、集合、表项或指令，只改变一个条件再运行；进入“生成类型变量”前，核对语义、接口、成本和首错位置。

## 可运行的最小骨架

```c
unify(t1, t2) -> mostGeneralSubstitution
```

```c
generalize(env, type) = forall(free(type) - free(env)).type
```

```text
representation = boxed | coercion-guided | runtime-type-parameter
```

<Callout type="trap">
  **版本边界：**
  本页以中文C语言描述修订版为实现轨道。Java与ML版本共享主体算法，但模块接口、生成工具和代码片段不同，不能混用为同一实现证据。
</Callout>

## 权威目录逐项讲解

### 第16章 多态类型

“第16章 多态类型”必须落到C模块与结构化产物，而不只是术语。先在“生成类型变量”固定输入、接口、数据结构和算法前提，再进入“收集约束”比较第一条差异。围绕参数多态记录所有权、复杂度、失败位置和下游消费者，防止后续阶段掩盖错误。

验收时分别检查语义、完备性、成本和模块边界：转换是否保持Tiger可观察行为，算法前提是否成立，时间空间代价是否符合规模，以及错误能否停在最早接口。修复后清理旧产物，从源文件重新生成并重放正常、边界、失败和优化样本。

### 16.1 参数多态性

“16.1 参数多态性”必须落到C模块与结构化产物，而不只是术语。先在“收集约束”固定输入、接口、数据结构和算法前提，再进入“执行合一”比较第一条差异。围绕类型推论记录所有权、复杂度、失败位置和下游消费者，防止后续阶段掩盖错误。

验收时分别检查语义、完备性、成本和模块边界：转换是否保持Tiger可观察行为，算法前提是否成立，时间空间代价是否符合规模，以及错误能否停在最早接口。修复后清理旧产物，从源文件重新生成并重放正常、边界、失败和优化样本。

### 16.1.1 显式带类型的多态语言

“16.1.1 显式带类型的多态语言”必须落到C模块与结构化产物，而不只是术语。先在“执行合一”固定输入、接口、数据结构和算法前提，再进入“泛化类型方案”比较第一条差异。围绕合一记录所有权、复杂度、失败位置和下游消费者，防止后续阶段掩盖错误。

验收时分别检查语义、完备性、成本和模块边界：转换是否保持Tiger可观察行为，算法前提是否成立，时间空间代价是否符合规模，以及错误能否停在最早接口。修复后清理旧产物，从源文件重新生成并重放正常、边界、失败和优化样本。

### 16.1.2 多态类型的检查

“16.1.2 多态类型的检查”必须落到C模块与结构化产物，而不只是术语。先在“泛化类型方案”固定输入、接口、数据结构和算法前提，再进入“实例化使用点”比较第一条差异。围绕Hindley-Milner记录所有权、复杂度、失败位置和下游消费者，防止后续阶段掩盖错误。

验收时分别检查语义、完备性、成本和模块边界：转换是否保持Tiger可观察行为，算法前提是否成立，时间空间代价是否符合规模，以及错误能否停在最早接口。修复后清理旧产物，从源文件重新生成并重放正常、边界、失败和优化样本。

### 16.2 类型推论

“16.2 类型推论”必须落到C模块与结构化产物，而不只是术语。先在“实例化使用点”固定输入、接口、数据结构和算法前提，再进入“选择运行时表示”比较第一条差异。围绕装箱记录所有权、复杂度、失败位置和下游消费者，防止后续阶段掩盖错误。

验收时分别检查语义、完备性、成本和模块边界：转换是否保持Tiger可观察行为，算法前提是否成立，时间空间代价是否符合规模，以及错误能否停在最早接口。修复后清理旧产物，从源文件重新生成并重放正常、边界、失败和优化样本。

### 16.2.1 一个隐式类型的多态语言

“16.2.1 一个隐式类型的多态语言”必须落到C模块与结构化产物，而不只是术语。先在“选择运行时表示”固定输入、接口、数据结构和算法前提，再进入“生成类型变量”比较第一条差异。围绕参数多态记录所有权、复杂度、失败位置和下游消费者，防止后续阶段掩盖错误。

验收时分别检查语义、完备性、成本和模块边界：转换是否保持Tiger可观察行为，算法前提是否成立，时间空间代价是否符合规模，以及错误能否停在最早接口。修复后清理旧产物，从源文件重新生成并重放正常、边界、失败和优化样本。

### 16.2.2 类型推论算法

“16.2.2 类型推论算法”必须落到C模块与结构化产物，而不只是术语。先在“生成类型变量”固定输入、接口、数据结构和算法前提，再进入“收集约束”比较第一条差异。围绕类型推论记录所有权、复杂度、失败位置和下游消费者，防止后续阶段掩盖错误。

验收时分别检查语义、完备性、成本和模块边界：转换是否保持Tiger可观察行为，算法前提是否成立，时间空间代价是否符合规模，以及错误能否停在最早接口。修复后清理旧产物，从源文件重新生成并重放正常、边界、失败和优化样本。

### 16.2.3 递归的数据类型

“16.2.3 递归的数据类型”必须落到C模块与结构化产物，而不只是术语。先在“收集约束”固定输入、接口、数据结构和算法前提，再进入“执行合一”比较第一条差异。围绕合一记录所有权、复杂度、失败位置和下游消费者，防止后续阶段掩盖错误。

验收时分别检查语义、完备性、成本和模块边界：转换是否保持Tiger可观察行为，算法前提是否成立，时间空间代价是否符合规模，以及错误能否停在最早接口。修复后清理旧产物，从源文件重新生成并重放正常、边界、失败和优化样本。

### 16.2.4 Hindley-Milner类型的能力

“16.2.4 Hindley-Milner类型的能力”必须落到C模块与结构化产物，而不只是术语。先在“执行合一”固定输入、接口、数据结构和算法前提，再进入“泛化类型方案”比较第一条差异。围绕Hindley-Milner记录所有权、复杂度、失败位置和下游消费者，防止后续阶段掩盖错误。

验收时分别检查语义、完备性、成本和模块边界：转换是否保持Tiger可观察行为，算法前提是否成立，时间空间代价是否符合规模，以及错误能否停在最早接口。修复后清理旧产物，从源文件重新生成并重放正常、边界、失败和优化样本。

### 16.3 多态变量的表示

“16.3 多态变量的表示”必须落到C模块与结构化产物，而不只是术语。先在“泛化类型方案”固定输入、接口、数据结构和算法前提，再进入“实例化使用点”比较第一条差异。围绕装箱记录所有权、复杂度、失败位置和下游消费者，防止后续阶段掩盖错误。

验收时分别检查语义、完备性、成本和模块边界：转换是否保持Tiger可观察行为，算法前提是否成立，时间空间代价是否符合规模，以及错误能否停在最早接口。修复后清理旧产物，从源文件重新生成并重放正常、边界、失败和优化样本。

### 16.3.1 多态函数的扩展

“16.3.1 多态函数的扩展”必须落到C模块与结构化产物，而不只是术语。先在“实例化使用点”固定输入、接口、数据结构和算法前提，再进入“选择运行时表示”比较第一条差异。围绕参数多态记录所有权、复杂度、失败位置和下游消费者，防止后续阶段掩盖错误。

验收时分别检查语义、完备性、成本和模块边界：转换是否保持Tiger可观察行为，算法前提是否成立，时间空间代价是否符合规模，以及错误能否停在最早接口。修复后清理旧产物，从源文件重新生成并重放正常、边界、失败和优化样本。

### 16.3.2 完全的装箱转换

“16.3.2 完全的装箱转换”必须落到C模块与结构化产物，而不只是术语。先在“选择运行时表示”固定输入、接口、数据结构和算法前提，再进入“生成类型变量”比较第一条差异。围绕类型推论记录所有权、复杂度、失败位置和下游消费者，防止后续阶段掩盖错误。

验收时分别检查语义、完备性、成本和模块边界：转换是否保持Tiger可观察行为，算法前提是否成立，时间空间代价是否符合规模，以及错误能否停在最早接口。修复后清理旧产物，从源文件重新生成并重放正常、边界、失败和优化样本。

### 16.3.3 基于强制的表示分析

“16.3.3 基于强制的表示分析”必须落到C模块与结构化产物，而不只是术语。先在“生成类型变量”固定输入、接口、数据结构和算法前提，再进入“收集约束”比较第一条差异。围绕合一记录所有权、复杂度、失败位置和下游消费者，防止后续阶段掩盖错误。

验收时分别检查语义、完备性、成本和模块边界：转换是否保持Tiger可观察行为，算法前提是否成立，时间空间代价是否符合规模，以及错误能否停在最早接口。修复后清理旧产物，从源文件重新生成并重放正常、边界、失败和优化样本。

### 16.3.4 将类型作为运行时参数传递

“16.3.4 将类型作为运行时参数传递”必须落到C模块与结构化产物，而不只是术语。先在“收集约束”固定输入、接口、数据结构和算法前提，再进入“执行合一”比较第一条差异。围绕Hindley-Milner记录所有权、复杂度、失败位置和下游消费者，防止后续阶段掩盖错误。

验收时分别检查语义、完备性、成本和模块边界：转换是否保持Tiger可观察行为，算法前提是否成立，时间空间代价是否符合规模，以及错误能否停在最早接口。修复后清理旧产物，从源文件重新生成并重放正常、边界、失败和优化样本。

### 16.4 静态重载的解决方法

“16.4 静态重载的解决方法”必须落到C模块与结构化产物，而不只是术语。先在“执行合一”固定输入、接口、数据结构和算法前提，再进入“泛化类型方案”比较第一条差异。围绕装箱记录所有权、复杂度、失败位置和下游消费者，防止后续阶段掩盖错误。

验收时分别检查语义、完备性、成本和模块边界：转换是否保持Tiger可观察行为，算法前提是否成立，时间空间代价是否符合规模，以及错误能否停在最早接口。修复后清理旧产物，从源文件重新生成并重放正常、边界、失败和优化样本。

## 分步视觉验证

先预测当前阶段的树、图、集合、表项或指令，再运行C模块；若不同，停在第一处分叉并保留原始证据。

<Stepper>
  <Step title="画出模块与数据结构">
    标出参数多态、类型推论、合一、Hindley-Milner、装箱的输入、输出、所有权、前提和下游消费者。
    <Tbc16PolymorphicTypesMapLab />
  </Step>
  <Step title="运行单变量Tiger实验">
    固定编译器与目标模型，只改变一个语言、分析、运行时或机器条件。
    <Tbc16PolymorphicTypesExperimentLab />
  </Step>
  <Step title="核对第一处分叉">
    比较结构化产物、复杂度和行为，清理旧输出后完整重建并重放回归样本。
    <Tbc16PolymorphicTypesEvidenceLab />
  </Step>
</Stepper>

## 常见误区

<Callout type="trap">
  **只看最终输出：** 没有Token、AST、环境、Tree
  IR、CFG、活跃集合、干涉图和帧布局，就无法把错误归因到具体C模块，也无法证明转换保持语义。
</Callout>

<Callout type="trap">
  **混用三种版本：**
  C版使用Lex/Flex、Yacc/Bison和C结构体接口；ML版与Java版的模块代码只能作对照，不能替代C版项目的编译、内存和ABI证据。
</Callout>

## 四类样本与通过条件

| 样本 | 单一变量               | 必查证据         | 通过条件         |
| ---- | ---------------------- | ---------------- | ---------------- |
| 正常 | 一个最小Tiger构造      | 全阶段快照       | 与预测一致       |
| 边界 | 深度、宽度、压力或布局 | 临界分支和成本   | 边界可解释       |
| 失败 | 一个语法、事实或约束   | 首错与下游停止   | 不产出伪结果     |
| 优化 | 一个变换或策略         | 前后IR和差分执行 | 等价且收益可解释 |

## 练习

<Exercises>

**问题 1** 为什么不能只凭目标程序运行成功验收第16章 多态类型？

<Answer>
  最终行为可能隐藏错误Token、树结构、Frame访问、数据流事实或机器约束。应保存每个C模块的结构化产物，定位第一处分叉，并从Tiger源文件干净重建。
</Answer>

**问题 2** 如何把“执行合一”变成可回归实验？

<Answer>
  固定Tiger输入、编译器提交、C工具链、运行时和目标机，先预测参数多态与类型推论的中间状态，只改变一个条件，并保留四类样本。
</Answer>

**问题 3** 本页最小通过条件是什么？

<Answer>
  合一满足occurs
  check并产生最一般替换，泛化只作用于不受环境约束的变量，递归类型合法，表示选择与实例化类型一致。
  修复后清理旧对象和中间产物，完整重建全部下游结果，并让历史样本回到基线。
</Answer>

</Exercises>

## 小结

- 参数多态：连接“生成类型变量”的C接口、结构化产物、成本和回归证据。
- 类型推论：连接“收集约束”的C接口、结构化产物、成本和回归证据。
- 合一：连接“执行合一”的C接口、结构化产物、成本和回归证据。
- Hindley-Milner：连接“泛化类型方案”的C接口、结构化产物、成本和回归证据。
- 装箱：连接“实例化使用点”的C接口、结构化产物、成本和回归证据。
- 本页15个核心目录层级均已进入可追溯的Tiger编译证据链。

## 术语表

<Glossary>
  <GlossaryItem term="参数多态">
    参数多态用于解释“生成类型变量”，需要同时给出输入接口、算法前提、结构化结果、复杂度和清理重建方法。
  </GlossaryItem>
  <GlossaryItem term="类型推论">
    类型推论用于解释“收集约束”，需要同时给出输入接口、算法前提、结构化结果、复杂度和清理重建方法。
  </GlossaryItem>
  <GlossaryItem term="合一">
    合一用于解释“执行合一”，需要同时给出输入接口、算法前提、结构化结果、复杂度和清理重建方法。
  </GlossaryItem>
  <GlossaryItem term="Hindley-Milner">
    Hindley-Milner用于解释“泛化类型方案”，需要同时给出输入接口、算法前提、结构化结果、复杂度和清理重建方法。
  </GlossaryItem>
  <GlossaryItem term="装箱">
    装箱用于解释“实例化使用点”，需要同时给出输入接口、算法前提、结构化结果、复杂度和清理重建方法。
  </GlossaryItem>
</Glossary>

<Attribution
  adaptedFrom="Andrew W. Appel、Maia Ginsburg著，赵克佳、黄春、沈志宇译《现代编译原理：C语言描述（修订版）》，人民邮电出版社，2018，385页，ISBN 9787115476883 · 基于Andrew W. Appel官方完整目录、Cambridge C版资料与中文版修订版目录的独立教学重构"
  adaptedUrl="https://www.cs.princeton.edu/~appel/modern/toc.html"
/>
