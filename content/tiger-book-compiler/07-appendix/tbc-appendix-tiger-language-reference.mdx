---
title: "附录 Tiger语言参考手册"
type: C
section: "附录 · Tiger语言参考"
order: 22
description: "附录 Tiger语言参考手册逐项覆盖5个核心目录层级，以C模块、Tiger中间产物和运行证据建立可复查实验。"
demo: true
math: false
sourceUrl: "https://www.cs.princeton.edu/~appel/modern/toc.html"
draft: false
---

import {
  TbcAppendixTigerLanguageReferenceMapLab,
  TbcAppendixTigerLanguageReferenceExperimentLab,
  TbcAppendixTigerLanguageReferenceEvidenceLab,
} from "@/components/mdx/tiger-book-compiler/diagrams/tbc-appendix-tiger-language-reference";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释附录 Tiger语言参考手册在Tiger编译器中的输入、输出、C接口和责任边界
- 能描述本页5个核心目录层级之间的数据结构、算法前提与工程取舍
- 能复现最小C模块或Tiger样本，保存正常、边界、失败和优化四类证据
- 能判断“每个合法构造有唯一语法和静态语义，标准库签名与运行时实现一致，边界和错误样本在各阶段得到一致诊断。”是否成立，而不是只看最终二进制是否运行

</Objectives>

## 为什么最终二进制正确仍然不够

<Term def="词法约定连接“冻结词法合同”的C接口、结构化产物、成本和首错证据。">
  词法约定
</Term>
、
<Term def="声明连接“列出声明作用域”的C接口、结构化产物、成本和首错证据。">
  声明
</Term>
、
<Term def="左值连接“定义变量寻址”的C接口、结构化产物、成本和首错证据。">
  左值
</Term>
、
<Term def="表达式连接“规定表达式类型”的C接口、结构化产物、成本和首错证据。">
  表达式
</Term>
、
<Term def="标准库连接“核对标准库ABI”的C接口、结构化产物、成本和首错证据。">
  标准库
</Term>
共同构成本页坐标。把Tiger词法、声明、变量、表达式与标准库整理为编译器的可执行语言合同，作为扫描、分析、类型、翻译和运行时测试的共同真值。

虎书把教材组织成一项连续实现工程。扫描器、分析器、AST、环境、Frame、Tree IR、规范化、抽象汇编、流图、干涉图和运行时彼此依赖；任何阶段的错误都可能被后续样本偶然掩盖。因此每次实验都要保存阶段输入、输出和不变量，定位第一处偏离，而不是只观察最后进程的退出码。

## C版Tiger实验账本

实验固定Tiger源文件、编译器提交、C编译器与选项、目标ISA、运行时版本和随机种子。前端保存Token、语法树、作用域与类型；中端保存Frame访问、Tree IR、线性IR、基本块和轨迹；后端保存抽象汇编、def/use、CFG、活跃集合、干涉图、颜色与溢出重写；高级章保存堆图、类布局、闭包、类型约束、数据流事实、SSA、调度表与cache事件。

样本分为四组。正常样本覆盖主路径；边界样本覆盖空列表、深嵌套、递归声明、逃逸变量、临界立即数、不可归约流图、寄存器压力、循环依赖、别名和堆边界；失败样本只破坏一个词法、文法、类型、IR或机器约束；优化样本保存变换前后版本并进行解释器或本机差分执行。

诊断记录模块、源位置或IR位置、实际状态、期望状态、相关前驱和首要根因。模块失败后不得继续读取过期缓存；变换若令活跃性、支配、别名、调用或布局事实失效，必须显式重算。正确性与性能分开验收：正确性要求语言语义和ABI约束成立，性能要求给出静态成本、动态计数、代码大小、硬件和运行时环境。

每个目录项进入追溯矩阵：至少关联一个C接口、一个输入、一个算法前提、一份结构化产物、一个失败对照和一个回归断言。跨章数据同时标明生产者、消费者和失效条件，从而发现“有名词无实现”的空洞覆盖，并把回归范围限制在真实受影响模块。

## 本章核心推演

把Tiger词法、声明、变量、表达式与标准库整理为编译器的可执行语言合同，作为扫描、分析、类型、翻译和运行时测试的共同真值。 实现前先写出词法约定与声明的最小合同：输入表示、输出表示、所有权、错误返回和复杂度。随后准备只有一个语言构造的Tiger样本，让冻结词法合同到生成语言一致性测试每一步都产生可序列化证据；若中间结果无法独立检查，就说明接口还没有真正解耦。

正确样本之外必须构造一个最小反例。反例只破坏左值的单个前提，并断言诊断模块、位置和下游停止点。修复不能只更改最后输出，而应让第一处分叉消失，同时保持表达式和标准库的历史快照不变。性能实验则固定输入和机器，分别记录算法操作次数、内存峰值与最终运行时间，解释模型和实测偏差。

最终验收从干净目录开始，重新编译C模块、生成全部中间产物、链接Tiger运行时并执行差分测试。任何手工保留的旧对象文件、旧分析集合或旧寄存器映射都会破坏证据链；因此构建摘要、工具版本和所有阶段摘要必须一起归档，才能复现结论。

## 六阶段证据链

### 1. 冻结词法合同

围绕词法约定先预测树、图、集合、表项或指令，只改变一个条件再运行；进入“列出声明作用域”前，核对语义、接口、成本和首错位置。

### 2. 列出声明作用域

围绕声明先预测树、图、集合、表项或指令，只改变一个条件再运行；进入“定义变量寻址”前，核对语义、接口、成本和首错位置。

### 3. 定义变量寻址

围绕左值先预测树、图、集合、表项或指令，只改变一个条件再运行；进入“规定表达式类型”前，核对语义、接口、成本和首错位置。

### 4. 规定表达式类型

围绕表达式先预测树、图、集合、表项或指令，只改变一个条件再运行；进入“核对标准库ABI”前，核对语义、接口、成本和首错位置。

### 5. 核对标准库ABI

围绕标准库先预测树、图、集合、表项或指令，只改变一个条件再运行；进入“生成语言一致性测试”前，核对语义、接口、成本和首错位置。

### 6. 生成语言一致性测试

围绕词法约定先预测树、图、集合、表项或指令，只改变一个条件再运行；进入“冻结词法合同”前，核对语义、接口、成本和首错位置。

## 可运行的最小骨架

```c
let type intArray = array of int var a := intArray[10] of 0 in a[0] end
```

```c
function fib(n:int):int = if n < 2 then n else fib(n-1) + fib(n-2)
```

```text
print | flush | getchar | ord | chr | size | substring | concat | not | exit
```

<Callout type="trap">
  **版本边界：**
  本页以中文C语言描述修订版为实现轨道。Java与ML版本共享主体算法，但模块接口、生成工具和代码片段不同，不能混用为同一实现证据。
</Callout>

## 权威目录逐项讲解

### 附录 Tiger语言参考手册

“附录 Tiger语言参考手册”必须落到C模块与结构化产物，而不只是术语。先在“冻结词法合同”固定输入、接口、数据结构和算法前提，再进入“列出声明作用域”比较第一条差异。围绕词法约定记录所有权、复杂度、失败位置和下游消费者，防止后续阶段掩盖错误。

验收时分别检查语义、完备性、成本和模块边界：转换是否保持Tiger可观察行为，算法前提是否成立，时间空间代价是否符合规模，以及错误能否停在最早接口。修复后清理旧产物，从源文件重新生成并重放正常、边界、失败和优化样本。

### A.1 词法问题

“A.1 词法问题”必须落到C模块与结构化产物，而不只是术语。先在“列出声明作用域”固定输入、接口、数据结构和算法前提，再进入“定义变量寻址”比较第一条差异。围绕声明记录所有权、复杂度、失败位置和下游消费者，防止后续阶段掩盖错误。

验收时分别检查语义、完备性、成本和模块边界：转换是否保持Tiger可观察行为，算法前提是否成立，时间空间代价是否符合规模，以及错误能否停在最早接口。修复后清理旧产物，从源文件重新生成并重放正常、边界、失败和优化样本。

### A.2 声明

“A.2 声明”必须落到C模块与结构化产物，而不只是术语。先在“定义变量寻址”固定输入、接口、数据结构和算法前提，再进入“规定表达式类型”比较第一条差异。围绕左值记录所有权、复杂度、失败位置和下游消费者，防止后续阶段掩盖错误。

验收时分别检查语义、完备性、成本和模块边界：转换是否保持Tiger可观察行为，算法前提是否成立，时间空间代价是否符合规模，以及错误能否停在最早接口。修复后清理旧产物，从源文件重新生成并重放正常、边界、失败和优化样本。

### A.3 变量和表达式

“A.3 变量和表达式”必须落到C模块与结构化产物，而不只是术语。先在“规定表达式类型”固定输入、接口、数据结构和算法前提，再进入“核对标准库ABI”比较第一条差异。围绕表达式记录所有权、复杂度、失败位置和下游消费者，防止后续阶段掩盖错误。

验收时分别检查语义、完备性、成本和模块边界：转换是否保持Tiger可观察行为，算法前提是否成立，时间空间代价是否符合规模，以及错误能否停在最早接口。修复后清理旧产物，从源文件重新生成并重放正常、边界、失败和优化样本。

### A.4 标准库

“A.4 标准库”必须落到C模块与结构化产物，而不只是术语。先在“核对标准库ABI”固定输入、接口、数据结构和算法前提，再进入“生成语言一致性测试”比较第一条差异。围绕标准库记录所有权、复杂度、失败位置和下游消费者，防止后续阶段掩盖错误。

验收时分别检查语义、完备性、成本和模块边界：转换是否保持Tiger可观察行为，算法前提是否成立，时间空间代价是否符合规模，以及错误能否停在最早接口。修复后清理旧产物，从源文件重新生成并重放正常、边界、失败和优化样本。

## 分步视觉验证

先预测当前阶段的树、图、集合、表项或指令，再运行C模块；若不同，停在第一处分叉并保留原始证据。

<Stepper>
  <Step title="画出模块与数据结构">
    标出词法约定、声明、左值、表达式、标准库的输入、输出、所有权、前提和下游消费者。
    <TbcAppendixTigerLanguageReferenceMapLab />
  </Step>
  <Step title="运行单变量Tiger实验">
    固定编译器与目标模型，只改变一个语言、分析、运行时或机器条件。
    <TbcAppendixTigerLanguageReferenceExperimentLab />
  </Step>
  <Step title="核对第一处分叉">
    比较结构化产物、复杂度和行为，清理旧输出后完整重建并重放回归样本。
    <TbcAppendixTigerLanguageReferenceEvidenceLab />
  </Step>
</Stepper>

## 常见误区

<Callout type="trap">
  **只看最终输出：** 没有Token、AST、环境、Tree
  IR、CFG、活跃集合、干涉图和帧布局，就无法把错误归因到具体C模块，也无法证明转换保持语义。
</Callout>

<Callout type="trap">
  **混用三种版本：**
  C版使用Lex/Flex、Yacc/Bison和C结构体接口；ML版与Java版的模块代码只能作对照，不能替代C版项目的编译、内存和ABI证据。
</Callout>

## 四类样本与通过条件

| 样本 | 单一变量               | 必查证据         | 通过条件         |
| ---- | ---------------------- | ---------------- | ---------------- |
| 正常 | 一个最小Tiger构造      | 全阶段快照       | 与预测一致       |
| 边界 | 深度、宽度、压力或布局 | 临界分支和成本   | 边界可解释       |
| 失败 | 一个语法、事实或约束   | 首错与下游停止   | 不产出伪结果     |
| 优化 | 一个变换或策略         | 前后IR和差分执行 | 等价且收益可解释 |

## 练习

<Exercises>

**问题 1** 为什么不能只凭目标程序运行成功验收附录 Tiger语言参考手册？

<Answer>
  最终行为可能隐藏错误Token、树结构、Frame访问、数据流事实或机器约束。应保存每个C模块的结构化产物，定位第一处分叉，并从Tiger源文件干净重建。
</Answer>

**问题 2** 如何把“定义变量寻址”变成可回归实验？

<Answer>
  固定Tiger输入、编译器提交、C工具链、运行时和目标机，先预测词法约定与声明的中间状态，只改变一个条件，并保留四类样本。
</Answer>

**问题 3** 本页最小通过条件是什么？

<Answer>
  每个合法构造有唯一语法和静态语义，标准库签名与运行时实现一致，边界和错误样本在各阶段得到一致诊断。
  修复后清理旧对象和中间产物，完整重建全部下游结果，并让历史样本回到基线。
</Answer>

</Exercises>

## 小结

- 词法约定：连接“冻结词法合同”的C接口、结构化产物、成本和回归证据。
- 声明：连接“列出声明作用域”的C接口、结构化产物、成本和回归证据。
- 左值：连接“定义变量寻址”的C接口、结构化产物、成本和回归证据。
- 表达式：连接“规定表达式类型”的C接口、结构化产物、成本和回归证据。
- 标准库：连接“核对标准库ABI”的C接口、结构化产物、成本和回归证据。
- 本页5个核心目录层级均已进入可追溯的Tiger编译证据链。

## 术语表

<Glossary>
  <GlossaryItem term="词法约定">
    词法约定用于解释“冻结词法合同”，需要同时给出输入接口、算法前提、结构化结果、复杂度和清理重建方法。
  </GlossaryItem>
  <GlossaryItem term="声明">
    声明用于解释“列出声明作用域”，需要同时给出输入接口、算法前提、结构化结果、复杂度和清理重建方法。
  </GlossaryItem>
  <GlossaryItem term="左值">
    左值用于解释“定义变量寻址”，需要同时给出输入接口、算法前提、结构化结果、复杂度和清理重建方法。
  </GlossaryItem>
  <GlossaryItem term="表达式">
    表达式用于解释“规定表达式类型”，需要同时给出输入接口、算法前提、结构化结果、复杂度和清理重建方法。
  </GlossaryItem>
  <GlossaryItem term="标准库">
    标准库用于解释“核对标准库ABI”，需要同时给出输入接口、算法前提、结构化结果、复杂度和清理重建方法。
  </GlossaryItem>
</Glossary>

<Attribution
  adaptedFrom="Andrew W. Appel、Maia Ginsburg著，赵克佳、黄春、沈志宇译《现代编译原理：C语言描述（修订版）》，人民邮电出版社，2018，385页，ISBN 9787115476883 · 基于Andrew W. Appel官方完整目录、Cambridge C版资料与中文版修订版目录的独立教学重构"
  adaptedUrl="https://www.cs.princeton.edu/~appel/modern/toc.html"
/>
