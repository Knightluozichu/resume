---
title: "寄存器分配与图着色"
type: B
section: "编译原理"
order: 8
description: "干涉图、Chaitin-Briggs 图着色与溢出处理"
demo: true
math: true
sourceUrl: ""
draft: false
---

import { TbcRegisterAllocationDiagram } from "@/components/mdx/tiger-book-compiler/diagrams/tbc-register-allocation";
import { Objectives, Callout, Glossary, GlossaryItem, Term, Exercises, Answer } from "@/components/mdx/mdx-components";

<Objectives>

- 理解寄存器分配的任务：TEMP 虚拟寄存器 → 物理寄存器
- 掌握干涉图的构建方法（活跃分析）
- 掌握 Chaitin-Briggs 图着色算法的 Simplify/Spill/Select 三阶段
- 理解实际溢出的处理与重写机制
- 理解 move coalescing 及 Briggs/George 安全准则

</Objectives>

## 寄存器分配的任务

指令选择后，程序中的操作数用无限多的 `TEMP`（虚拟寄存器）表示，但目标机器只有有限的 $K$ 个物理寄存器。**寄存器分配**把虚拟寄存器映射到物理寄存器，本质是**干涉图的 K 着色问题**。

虎书用 **Chaitin-Briggs 图着色算法**，这是工业级寄存器分配的经典方法。

<TbcRegisterAllocationDiagram />

## 干涉图构建

寄存器分配的第一步是构建**干涉图**：

1. **活跃变量分析**（liveness analysis）：对每条指令算出哪些 `TEMP` 在此后被使用（活跃）
2. **干涉**：两个 `TEMP` 若在同一程序点同时活跃，则干涉，在干涉图中连一条边

干涉意味着两者不能共用同一物理寄存器（否则值会互相覆盖）。寄存器分配等价于干涉图的 K 着色——给每个节点（TEMP）分配一种颜色（寄存器），相邻节点颜色不同。着色失败则需溢出。

<Callout type="info">

**图着色与寄存器分配的等价性**：干涉图的边表示「不能共用寄存器」的约束。K 着色就是用 $K$ 种颜色给节点着色使相邻不同色——恰好对应「用 $K$ 个寄存器分配使干涉的 TEMP 不同寄存器」。四色定理保证平面图可四着色，但干涉图未必平面，且 K 着色是 NP 完全问题，所以用启发式算法。

</Callout>

## Chaitin-Briggs 图着色算法

算法分三阶段循环工作：

### 1. Simplify（简化）

反复删除干涉图中**度数小于 K** 的节点，压入栈中。这些节点必然可着色——邻居少于 $K$ 个，至少剩一种颜色可用。删除后邻居度数减少，可能产生新的低度数节点，继续删除。

$$\text{若 } \deg(v) < K \Rightarrow \text{压栈 } v \text{，继续简化}$$

### 2. Spill（潜在溢出）

当图中无低度数节点时，选一个高度数节点标记为「**潜在溢出**」，仍压入栈继续简化（乐观假设它能着色）。

$$\text{无低度数节点} \Rightarrow \text{选 } v \text{ 标记潜在溢出，压栈继续}$$

### 3. Select（选择着色）

栈清空后开始弹栈，每个节点选一个与所有已着色邻居都不同的颜色。若某潜在溢出节点找不到可用颜色则发生「**实际溢出**」。

$$\text{color}(v) = \text{最小 } c \in \{1..K\},\ c \notin \{\text{color}(u) \mid u \in \text{邻居}(v)\}$$

## 实际溢出处理

**实际溢出**发生在 Select 阶段——某潜在溢出节点弹栈时，所有 $K$ 种颜色都被邻居占用，无法着色。

### 处理方法

1. 为该 `TEMP` 在栈帧中分配一个内存槽位
2. 重写指令序列——每次使用前插入 `load`，每次定义后插入 `store`
3. 重写后这些新临时变量的活跃区间变短、干涉减少
4. 重建干涉图重新跑 Simplify/Spill/Select，通常能成功

<Callout type="tip">

**为什么不直接报错？** 寄存器不足是常态——程序中的活跃变量可能远多于物理寄存器。通过溢出到内存让程序仍能正确运行（用时间换空间），只是访问变慢。重写后活跃区间变短、干涉减少，通常下一轮就能着色成功。这保证编译总能完成，不会因寄存器不足而失败。

</Callout>

## move coalescing

**move coalescing**（移动合并）合并 `MOVE` 指令相关的两个非干涉 `TEMP` 节点为同一寄存器，从而消除那条 `MOVE` 指令，减少指令数、提升性能。

但盲目合并可能增加干涉度导致溢出，所以用安全准则：

### Briggs 准则

合并后节点若度数小于 $K$ 或其高度数邻居数小于 $K$ 则安全——合并后仍能简化（不会因合并而无法着色）。

### George 准则

若源节点每个邻居要么度数小于 $K$ 要么已与目标节点干涉，则合并安全。

$$\text{George: } \forall u \in \text{邻居}(src),\ \deg(u) < K \lor u \in \text{邻居}(dst) \Rightarrow \text{安全}$$

这两个准则保证合并不引入原本不会发生的溢出，在性能（消 move）和安全（不溢出）之间平衡。

## 寄存器分配与编译流程的衔接

寄存器分配是后端的最后一大步：

- 输入：指令选择生成的含 `TEMP` 的指令序列
- 处理：活跃分析 → 干涉图 → 图着色 → 溢出重写
- 输出：用物理寄存器的目标代码

寄存器分配完成后，程序已是可执行的目标机器代码（可能再做窥孔优化），编译流程结束。

<Glossary>

<GlossaryItem term="寄存器分配">
把指令选择后无限多的虚拟寄存器（TEMP）映射到有限物理寄存器的阶段，等价于干涉图的 K 着色问题。
</GlossaryItem>

<GlossaryItem term="干涉图">
寄存器分配的核心数据结构，节点是 TEMP，同时活跃的 TEMP 之间连边（干涉），寄存器分配等价于该图的 K 着色。
</GlossaryItem>

<GlossaryItem term="Chaitin-Briggs 算法">
图着色寄存器分配的经典算法，分 Simplify（简化低度数节点）、Spill（潜在溢出）、Select（弹栈着色）三阶段循环。
</GlossaryItem>

<GlossaryItem term="实际溢出">
Select 阶段某节点找不到可用颜色时，为它在栈帧分配槽位，重写指令（load/store）后重建干涉图重新着色。
</GlossaryItem>

<GlossaryItem term="move coalescing">
合并 MOVE 相关的非干涉 TEMP 为同一寄存器以消除 MOVE 指令，用 Briggs/George 准则保证不引入溢出。
</GlossaryItem>

</Glossary>

<Exercises>

### 问题 1

寄存器分配的任务是什么？干涉图如何构建？

<Answer>

寄存器分配把指令选择后无限多的虚拟寄存器（TEMP）映射到有限的目标机器物理寄存器（K 个）。干涉图构建：先做活跃变量分析（liveness analysis）——对每条指令算出哪些 TEMP 在此后被使用（活跃），两个 TEMP 若在同一程序点同时活跃则「干涉」，在干涉图中连一条边。干涉意味着两者不能共用同一物理寄存器。寄存器分配就等价于干涉图的 K 着色——给每个节点（TEMP）分配一种颜色（寄存器），相邻节点颜色不同。着色失败则需溢出。

</Answer>

### 问题 2

Chaitin-Briggs 图着色算法的 Simplify / Spill / Select 三阶段是如何循环工作的？

<Answer>

Simplify（简化）：反复删除干涉图中度数小于 K 的节点，压入栈中——这些节点必然可着色（邻居少于 K 个，至少剩一种颜色）。Spill（潜在溢出）：当图中无低度数节点时，选一个高度数节点标记为「潜在溢出」，仍压入栈继续简化（乐观假设它能着色）。Select（选择着色）：栈清空后开始弹栈，每个节点选一个与所有已着色邻居都不同的颜色。若某潜在溢出节点找不到可用颜色则发生「实际溢出」——为它在栈帧分配槽位，重写指令（使用前 load、定义后 store），重建干涉图重新着色，直到全部着色成功。

</Answer>

### 问题 3

什么是实际溢出（actual spill）？如何处理？为什么不直接报错？

<Answer>

实际溢出发生在 Select 阶段——某潜在溢出节点弹栈时，所有 K 种颜色都被邻居占用，无法着色。处理：为该 TEMP 在栈帧中分配一个内存槽位，重写指令序列——每次使用该 TEMP 前插入一条 load 把值从栈读到新临时变量，每次定义后插入一条 store 把新临时变量写回栈。重写后这些新临时变量的活跃区间变短、干涉减少，重建干涉图重新跑 Simplify/Spill/Select，通常能成功。不直接报错是因为寄存器不足是常态——通过溢出到内存让程序仍能正确运行（用时间换空间），只是访问变慢，保证编译总能完成。

</Answer>

### 问题 4

move coalescing 的作用是什么？Briggs / George 准则如何保证合并不引入溢出？

<Answer>

move coalescing（移动合并）合并 MOVE 指令相关的两个非干涉 TEMP 节点为同一寄存器，从而消除那条 MOVE 指令，减少指令数、提升性能。但盲目合并可能增加干涉度导致溢出，所以用安全准则：Briggs 准则——合并后节点若度数小于 K 或其高度数邻居数小于 K 则安全（合并后仍能简化）；George 准则——若源节点每个邻居要么度数小于 K 要么已与目标节点干涉，则合并安全。这两个准则保证合并不引入原本不会发生的溢出，在性能（消 move）和安全（不溢出）之间平衡。

</Answer>

</Exercises>
