---
title: "第2天 设计程序设计语言"
type: C
section: "基础篇 · 语言前端"
order: 2
description: "第2天 设计程序设计语言围绕麻雀虽小、五脏俱全的程序设计语言、句尾的分号、含糊不得的语言推进Stone语言，实现从源文本、Token、AST到求值、优化或代码生成的可回归增量。 覆盖4个正式目录层级。"
demo: true
math: false
sourceUrl: "https://gihyo.jp/book/2012/978-4-7741-4974-5"
draft: true
---

import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第2天 设计程序设计语言的核心增量，并把每个正式目录项定位到Stone处理流水线和具体数据结构
- 能描述“冻结语言目标 → 写出样例程序 → 定义词法语法 → 建立AST → 选择执行方式 → 回归最小语言”的输入、输出、不变量与首个失败位置
- 能实现本日语言增量，并为正常、边界、非法与回归样本保留Token、AST、环境或字节码证据
- 能比较修改前后行为并判断“同一份Stone源程序在词法、语法、AST和执行阶段有可追踪的一一对应关系，语言边界没有隐藏歧义。”是否成立

</Objectives>

## 为什么不能只看最终输出

<Term def="麻雀虽小、五脏俱全的程序设计语言连接冻结语言目标阶段的语法、结构、运行状态和回归证据。">麻雀虽小、五脏俱全的程序设计语言</Term>、<Term def="句尾的分号连接写出样例程序阶段的语法、结构、运行状态和回归证据。">句尾的分号</Term>、<Term def="含糊不得的语言连接定义词法语法阶段的语法、结构、运行状态和回归证据。">含糊不得的语言</Term>、<Term def="语言处理器连接建立AST阶段的语法、结构、运行状态和回归证据。">语言处理器</Term>、<Term def="Stone程序连接选择执行方式阶段的语法、结构、运行状态和回归证据。">Stone程序</Term>
共同构成本页坐标。第2天
设计程序设计语言围绕麻雀虽小、五脏俱全的程序设计语言、句尾的分号、含糊不得的语言推进Stone语言，实现从源文本、Token、AST到求值、优化或代码生成的可回归增量。

最终输出相同不代表实现相同：词法器可能吞掉错误字符，解析器可能生成错误结合性的树，解释器可能从调用点而非定义点捕获环境，优化器也可能只在当前样本上碰巧得到正确值。因此每次增量都保留源文本、Token范围、AST快照、环境或帧状态、标准输出、错误输出与退出状态。

## Stone增量实现台账

先冻结上一日全部样本和公开接口，再为本日功能写一个最小成功程序、一个边界程序、一个故意失败程序和一个跨功能程序。成功样本证明新语法能到达执行层；边界样本暴露空输入、嵌套、遮蔽、短路、越界或缓存失效；失败样本必须在最早确定的位置停止；跨功能样本防止新节点破坏旧的表达式、控制流与函数语义。

处理流水线分为六个可观察检查点：源字符是否完整消费，Token是否携带类型和值及源位置，文法是否选择唯一分支，AST是否表达优先级与作用域，运行环境或虚拟机是否遵守所有权和调用约定，最终值与错误是否满足语言合同。只记录最终打印文本会丢失前五层最有诊断价值的证据。

每个错误对象至少含阶段、源文件、行列范围、实际符号、期望集合、原因和恢复建议。不要在底层捕获后返回null继续执行，因为后续空指针会掩盖真正错误；也不要把Java反射异常原样暴露给Stone程序，而应保留cause并映射为稳定的语言错误类别。

性能增量采用差分验证：同一语料分别运行基线解释器和优化版本，先比较值、输出、错误类别与位置，再比较预热后的时间、分配量与查找次数。若槽位分配、内联缓存或字节码改变异常顺序，性能再高也不能通过语义验收。

每次提交还要附带增量契约：本日新增哪些Token、文法产生式、AST节点、环境槽位、虚拟指令或类型规则，哪些旧接口保持不变，失败后如何回退到上一日可运行状态。将生成的Token、AST或字节码快照纳入版本比较，但不把快照更新本身当成修复；先解释结构变化为何符合语言设计，再接受新的基线。这样可把19天连续演进变成可二分、可撤销、可复查的工程历史。

## 六阶段证据链

### 1. 冻结语言目标

围绕麻雀虽小、五脏俱全的程序设计语言保存输入、结构化中间状态与输出。进入“写出样例程序”前，先断言当前检查点成立并记录首个偏离。

### 2. 写出样例程序

围绕句尾的分号保存输入、结构化中间状态与输出。进入“定义词法语法”前，先断言当前检查点成立并记录首个偏离。

### 3. 定义词法语法

围绕含糊不得的语言保存输入、结构化中间状态与输出。进入“建立AST”前，先断言当前检查点成立并记录首个偏离。

### 4. 建立AST

围绕语言处理器保存输入、结构化中间状态与输出。进入“选择执行方式”前，先断言当前检查点成立并记录首个偏离。

### 5. 选择执行方式

围绕Stone程序保存输入、结构化中间状态与输出。进入“回归最小语言”前，先断言当前检查点成立并记录首个偏离。

### 6. 回归最小语言

围绕麻雀虽小、五脏俱全的程序设计语言保存输入、结构化中间状态与输出。进入“冻结语言目标”前，先断言当前检查点成立并记录首个偏离。

## 可运行骨架

```text
Stone source -> Token stream -> AST -> eval / bytecode
```

```stone
even = 0
while even < 6 { even = even + 2 }
```

```bash
java stone.BasicInterpreter < samples/day02.stone
diff -u expected/day02.txt actual/day02.txt
```

<Callout type="trap">
  **版本边界：**
  本书基于Java与GluonJ构建Stone。现代JDK的模块、反射访问和类路径规则可能不同；先记录JDK、GluonJ及启动参数，再区分书中设计思想与当前平台兼容性。
</Callout>

## 正式目录逐项讲解

### 第2天 设计程序设计语言

“第2天 设计程序设计语言”要求先给出可运行正例，再列出语法、环境、对象、返回值和失败合同，最后沿处理流水线停在第一处分叉。在本日增量中，“麻雀虽小、五脏俱全的程序设计语言”服务于“冻结语言目标”：输入保存原始Stone源码、预期Token或AST、初始环境与宿主版本，输出保存结构化结果、标准错误和退出状态。

验证时只改变一个运算符、分隔符、绑定、对象布局、类型标注或输入规模，并在进入“写出样例程序”前停住第一条Token范围、语法分支、AST节点、环境绑定、虚拟指令或类型约束分叉。修复后重放此前所有日次样本；若旧语义改变，必须说明是有意语言演进还是回归。

### 2.1 麻雀虽小、五脏俱全的程序设计语言

“2.1 麻雀虽小、五脏俱全的程序设计语言”要求先给出可运行正例，再列出语法、环境、对象、返回值和失败合同，最后沿处理流水线停在第一处分叉。在本日增量中，“句尾的分号”服务于“写出样例程序”：输入保存原始Stone源码、预期Token或AST、初始环境与宿主版本，输出保存结构化结果、标准错误和退出状态。

验证时只改变一个运算符、分隔符、绑定、对象布局、类型标注或输入规模，并在进入“定义词法语法”前停住第一条Token范围、语法分支、AST节点、环境绑定、虚拟指令或类型约束分叉。修复后重放此前所有日次样本；若旧语义改变，必须说明是有意语言演进还是回归。

### 2.2 句尾的分号

“2.2 句尾的分号”要求先给出可运行正例，再列出语法、环境、对象、返回值和失败合同，最后沿处理流水线停在第一处分叉。在本日增量中，“含糊不得的语言”服务于“定义词法语法”：输入保存原始Stone源码、预期Token或AST、初始环境与宿主版本，输出保存结构化结果、标准错误和退出状态。

验证时只改变一个运算符、分隔符、绑定、对象布局、类型标注或输入规模，并在进入“建立AST”前停住第一条Token范围、语法分支、AST节点、环境绑定、虚拟指令或类型约束分叉。修复后重放此前所有日次样本；若旧语义改变，必须说明是有意语言演进还是回归。

### 2.3 含糊不得的语言

“2.3 含糊不得的语言”要求先给出可运行正例，再列出语法、环境、对象、返回值和失败合同，最后沿处理流水线停在第一处分叉。在本日增量中，“语言处理器”服务于“建立AST”：输入保存原始Stone源码、预期Token或AST、初始环境与宿主版本，输出保存结构化结果、标准错误和退出状态。

验证时只改变一个运算符、分隔符、绑定、对象布局、类型标注或输入规模，并在进入“选择执行方式”前停住第一条Token范围、语法分支、AST节点、环境绑定、虚拟指令或类型约束分叉。修复后重放此前所有日次样本；若旧语义改变，必须说明是有意语言演进还是回归。

## 分步视觉验证

<Stepper>
  <Step title="画处理流水线">
    先预测麻雀虽小、五脏俱全的程序设计语言、句尾的分号、含糊不得的语言、语言处理器、Stone程序在Token、AST、环境或虚拟机中的位置和所有权。
    </Step>
  <Step title="运行单变量实验">
    固定上一日解释器和样本，只切换一个语法、绑定、类型或优化条件。
    </Step>
  <Step title="核对首处分叉">
    比较结构化证据而非只比最终文本，修复后重放全部历史样本。
    </Step>
</Stepper>

## 常见误区

<Callout type="trap">
  **误区一：**
  只测合法输入。没有非法Token、缺失分隔符、未定义变量、错误参数、越界、类型冲突和缓存失效样本，就无法证明错误边界。
</Callout>

<Callout type="trap">
  **误区二：**
  为了减少类或代码量，把词法、语法、语义与宿主异常混成一个分支。简洁应来自稳定的数据合同，不应来自丢失阶段边界。
</Callout>

## 四类样本与验收

| 样本 | 只改变什么                 | 必查证据                 | 通过条件       |
| ---- | -------------------------- | ------------------------ | -------------- |
| 正常 | 一个最小语言功能           | Token、AST、状态与输出   | 与手工预测一致 |
| 边界 | 空值、嵌套、遮蔽或规模     | 首个边界分支与源位置     | 边界可解释     |
| 非法 | 一个字符、规则、绑定或类型 | 最早错误类别与期望集合   | 不继续污染状态 |
| 回归 | 与上一日相同语料           | 结构、值、错误与性能基线 | 旧语义保持     |

## 练习

<Exercises>

**问题 1** 为什么不能只凭最终输出验收第2天 设计程序设计语言？

<Answer>
  最终文本会隐藏Token范围、语法结合性、AST形状、环境捕获、指令栈效应与错误位置。必须同时保存各阶段结构化证据，并停在第一处分叉。
</Answer>

**问题 2** 如何把“定义词法语法”变成可回归实验？

<Answer>
  固定JDK、类路径、上一日代码与输入，先预测麻雀虽小、五脏俱全的程序设计语言和句尾的分号的中间状态，只改变一个条件，保存成功、边界、非法和跨功能样本，修复后重放全部历史语料。
</Answer>

**问题 3** 本页最小通过条件是什么？

<Answer>
  同一份Stone源程序在词法、语法、AST和执行阶段有可追踪的一一对应关系，语言边界没有隐藏歧义。
  同时要求正常结果、失败位置与历史样本一致；不能以性能提升或“程序能跑”替代语义等价证明。
</Answer>

</Exercises>

## 小结

- 麻雀虽小、五脏俱全的程序设计语言：连接“冻结语言目标”的源文本、中间结构、运行状态与回归证据。
- 句尾的分号：连接“写出样例程序”的源文本、中间结构、运行状态与回归证据。
- 含糊不得的语言：连接“定义词法语法”的源文本、中间结构、运行状态与回归证据。
- 语言处理器：连接“建立AST”的源文本、中间结构、运行状态与回归证据。
- Stone程序：连接“选择执行方式”的源文本、中间结构、运行状态与回归证据。
- 本页4个正式目录层级全部进入Stone可运行增量与差分回归链。

## 术语表

<Glossary>
  <GlossaryItem term="麻雀虽小、五脏俱全的程序设计语言">
    麻雀虽小、五脏俱全的程序设计语言用于解释“冻结语言目标”，需要同时指出输入、结构化中间状态、失败位置和回归样本。
  </GlossaryItem>
  <GlossaryItem term="句尾的分号">
    句尾的分号用于解释“写出样例程序”，需要同时指出输入、结构化中间状态、失败位置和回归样本。
  </GlossaryItem>
  <GlossaryItem term="含糊不得的语言">
    含糊不得的语言用于解释“定义词法语法”，需要同时指出输入、结构化中间状态、失败位置和回归样本。
  </GlossaryItem>
  <GlossaryItem term="语言处理器">
    语言处理器用于解释“建立AST”，需要同时指出输入、结构化中间状态、失败位置和回归样本。
  </GlossaryItem>
  <GlossaryItem term="Stone程序">
    Stone程序用于解释“选择执行方式”，需要同时指出输入、结构化中间状态、失败位置和回归样本。
  </GlossaryItem>
</Glossary>

<Attribution
  adaptedFrom="千叶滋著，陈筱烟译《两周自制脚本语言》，人民邮电出版社，2014-06，第1版，285页，ISBN 9787115355645 · 中文版完整目录与日文原版出版社目录的独立教学重构"
  adaptedUrl="https://gihyo.jp/book/2012/978-4-7741-4974-5"
/>
