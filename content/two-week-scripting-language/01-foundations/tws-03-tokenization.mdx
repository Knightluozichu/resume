---
title: "第3天 分割单词"
type: C
section: "基础篇 · 语言前端"
order: 3
description: "第3天 分割单词围绕Token对象、通过正则表达式定义单词、借助java.util.regex设计词法分析器、词法分析器试运行推进Stone语言，实现从源文本、Token、AST到求值、优化或代码生成的可回归增量。 覆盖5个正式目录层级。"
demo: true
math: false
sourceUrl: "https://gihyo.jp/book/2012/978-4-7741-4974-5"
draft: false
---


## 为什么不能只看最终输出

、、、、共同构成本页坐标。第3天
分割单词围绕Token对象、通过正则表达式定义单词、借助java.util.regex设计词法分析器、词法分析器试运行推进Stone语言，实现从源文本、Token、AST到求值、优化或代码生成的可回归增量。

最终输出相同不代表实现相同：词法器可能吞掉错误字符，解析器可能生成错误结合性的树，解释器可能从调用点而非定义点捕获环境，优化器也可能只在当前样本上碰巧得到正确值。因此每次增量都保留源文本、Token范围、AST快照、环境或帧状态、标准输出、错误输出与退出状态。

## Stone增量实现台账

先冻结上一日全部样本和公开接口，再为本日功能写一个最小成功程序、一个边界程序、一个故意失败程序和一个跨功能程序。成功样本证明新语法能到达执行层；边界样本暴露空输入、嵌套、遮蔽、短路、越界或缓存失效；失败样本必须在最早确定的位置停止；跨功能样本防止新节点破坏旧的表达式、控制流与函数语义。

处理流水线分为六个可观察检查点：源字符是否完整消费，Token是否携带类型和值及源位置，文法是否选择唯一分支，AST是否表达优先级与作用域，运行环境或虚拟机是否遵守所有权和调用约定，最终值与错误是否满足语言合同。只记录最终打印文本会丢失前五层最有诊断价值的证据。

每个错误对象至少含阶段、源文件、行列范围、实际符号、期望集合、原因和恢复建议。不要在底层捕获后返回null继续执行，因为后续空指针会掩盖真正错误；也不要把Java反射异常原样暴露给Stone程序，而应保留cause并映射为稳定的语言错误类别。

性能增量采用差分验证：同一语料分别运行基线解释器和优化版本，先比较值、输出、错误类别与位置，再比较预热后的时间、分配量与查找次数。若槽位分配、内联缓存或字节码改变异常顺序，性能再高也不能通过语义验收。

每次提交还要附带增量契约：本日新增哪些Token、文法产生式、AST节点、环境槽位、虚拟指令或类型规则，哪些旧接口保持不变，失败后如何回退到上一日可运行状态。将生成的Token、AST或字节码快照纳入版本比较，但不把快照更新本身当成修复；先解释结构变化为何符合语言设计，再接受新的基线。这样可把19天连续演进变成可二分、可撤销、可复查的工程历史。

## 六阶段证据链

### 1. 列出Token种类

围绕Token对象保存输入、结构化中间状态与输出。进入“规定最长匹配”前，先断言当前检查点成立并记录首个偏离。

### 2. 规定最长匹配

围绕通过正则表达式定义单词保存输入、结构化中间状态与输出。进入“扫描源字符”前，先断言当前检查点成立并记录首个偏离。

### 3. 扫描源字符

围绕借助java.util.regex设计词法分析器保存输入、结构化中间状态与输出。进入“保留行列位置”前，先断言当前检查点成立并记录首个偏离。

### 4. 保留行列位置

围绕词法分析器试运行保存输入、结构化中间状态与输出。进入“拒绝非法输入”前，先断言当前检查点成立并记录首个偏离。

### 5. 拒绝非法输入

围绕Token保存输入、结构化中间状态与输出。进入“重放Token流”前，先断言当前检查点成立并记录首个偏离。

### 6. 重放Token流

围绕Token对象保存输入、结构化中间状态与输出。进入“列出Token种类”前，先断言当前检查点成立并记录首个偏离。

## 可运行骨架

```java
Pattern token = Pattern.compile("\\s*((//.*)|([0-9]+)|(\"(?:\\\\.|[^\"])*\")|([A-Za-z_][A-Za-z0-9_]*)|(.))");
```

```text
name=Token.IDENT line=3 span=[18,22) lexeme=total
```

```bash
java stone.LexerTest < samples/tokens.stone
java stone.LexerTest < samples/bad-string.stone 2> error.txt
```


## 正式目录逐项讲解

### 第3天 分割单词

“第3天 分割单词”要求规定Token类别、最长匹配、跳过规则、转义与源位置，确保每个字符要么被消费一次，要么产生明确词法错误。在本日增量中，“Token对象”服务于“列出Token种类”：输入保存原始Stone源码、预期Token或AST、初始环境与宿主版本，输出保存结构化结果、标准错误和退出状态。

验证时只改变一个运算符、分隔符、绑定、对象布局、类型标注或输入规模，并在进入“规定最长匹配”前停住第一条Token范围、语法分支、AST节点、环境绑定、虚拟指令或类型约束分叉。修复后重放此前所有日次样本；若旧语义改变，必须说明是有意语言演进还是回归。

### 3.1 Token对象

“3.1 Token对象”要求规定Token类别、最长匹配、跳过规则、转义与源位置，确保每个字符要么被消费一次，要么产生明确词法错误。在本日增量中，“通过正则表达式定义单词”服务于“规定最长匹配”：输入保存原始Stone源码、预期Token或AST、初始环境与宿主版本，输出保存结构化结果、标准错误和退出状态。

验证时只改变一个运算符、分隔符、绑定、对象布局、类型标注或输入规模，并在进入“扫描源字符”前停住第一条Token范围、语法分支、AST节点、环境绑定、虚拟指令或类型约束分叉。修复后重放此前所有日次样本；若旧语义改变，必须说明是有意语言演进还是回归。

### 3.2 通过正则表达式定义单词

“3.2 通过正则表达式定义单词”要求规定Token类别、最长匹配、跳过规则、转义与源位置，确保每个字符要么被消费一次，要么产生明确词法错误。在本日增量中，“借助java.util.regex设计词法分析器”服务于“扫描源字符”：输入保存原始Stone源码、预期Token或AST、初始环境与宿主版本，输出保存结构化结果、标准错误和退出状态。

验证时只改变一个运算符、分隔符、绑定、对象布局、类型标注或输入规模，并在进入“保留行列位置”前停住第一条Token范围、语法分支、AST节点、环境绑定、虚拟指令或类型约束分叉。修复后重放此前所有日次样本；若旧语义改变，必须说明是有意语言演进还是回归。

### 3.3 借助java.util.regex设计词法分析器

“3.3 借助java.util.regex设计词法分析器”要求规定Token类别、最长匹配、跳过规则、转义与源位置，确保每个字符要么被消费一次，要么产生明确词法错误。在本日增量中，“词法分析器试运行”服务于“保留行列位置”：输入保存原始Stone源码、预期Token或AST、初始环境与宿主版本，输出保存结构化结果、标准错误和退出状态。

验证时只改变一个运算符、分隔符、绑定、对象布局、类型标注或输入规模，并在进入“拒绝非法输入”前停住第一条Token范围、语法分支、AST节点、环境绑定、虚拟指令或类型约束分叉。修复后重放此前所有日次样本；若旧语义改变，必须说明是有意语言演进还是回归。

### 3.4 词法分析器试运行

“3.4 词法分析器试运行”要求规定Token类别、最长匹配、跳过规则、转义与源位置，确保每个字符要么被消费一次，要么产生明确词法错误。在本日增量中，“Token”服务于“拒绝非法输入”：输入保存原始Stone源码、预期Token或AST、初始环境与宿主版本，输出保存结构化结果、标准错误和退出状态。

验证时只改变一个运算符、分隔符、绑定、对象布局、类型标注或输入规模，并在进入“重放Token流”前停住第一条Token范围、语法分支、AST节点、环境绑定、虚拟指令或类型约束分叉。修复后重放此前所有日次样本；若旧语义改变，必须说明是有意语言演进还是回归。

## 分步视觉验证


## 常见误区


## 四类样本与验收

| 样本 | 只改变什么                 | 必查证据                 | 通过条件       |
| ---- | -------------------------- | ------------------------ | -------------- |
| 正常 | 一个最小语言功能           | Token、AST、状态与输出   | 与手工预测一致 |
| 边界 | 空值、嵌套、遮蔽或规模     | 首个边界分支与源位置     | 边界可解释     |
| 非法 | 一个字符、规则、绑定或类型 | 最早错误类别与期望集合   | 不继续污染状态 |
| 回归 | 与上一日相同语料           | 结构、值、错误与性能基线 | 旧语义保持     |

## 练习


## 小结

- Token对象：连接“列出Token种类”的源文本、中间结构、运行状态与回归证据。
- 通过正则表达式定义单词：连接“规定最长匹配”的源文本、中间结构、运行状态与回归证据。
- 借助java.util.regex设计词法分析器：连接“扫描源字符”的源文本、中间结构、运行状态与回归证据。
- 词法分析器试运行：连接“保留行列位置”的源文本、中间结构、运行状态与回归证据。
- Token：连接“拒绝非法输入”的源文本、中间结构、运行状态与回归证据。
- 本页5个正式目录层级全部进入Stone可运行增量与差分回归链。

## 术语表


