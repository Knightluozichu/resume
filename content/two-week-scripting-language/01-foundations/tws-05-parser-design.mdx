---
title: "第5天 设计语法分析器"
type: C
section: "基础篇 · 语言前端"
order: 5
description: "第5天 设计语法分析器围绕Stone语言的语法、使用解析器与组合子、由语法分析器生成的抽象语法树、测试语法分析器推进Stone语言，实现从源文本、Token、AST到求值、优化或代码生成的可回归增量。 覆盖6个正式目录层级。"
demo: true
math: false
sourceUrl: "https://gihyo.jp/book/2012/978-4-7741-4974-5"
draft: true
---

import {
  Tws05ParserDesignMapLab,
  Tws05ParserDesignExperimentLab,
  Tws05ParserDesignEvidenceLab,
} from "@/components/mdx/two-week-scripting-language/diagrams/tws-05-parser-design";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第5天 设计语法分析器的核心增量，并把每个正式目录项定位到Stone处理流水线和具体数据结构
- 能描述“写出BNF → 消除入口歧义 → 组合解析器 → 生成AST → 定位语法错误 → 快照树结构”的输入、输出、不变量与首个失败位置
- 能实现本日语言增量，并为正常、边界、非法与回归样本保留Token、AST、环境或字节码证据
- 能比较修改前后行为并判断“合法Token流只生成一种符合优先级与结合性的AST，非法输入在最早无法继续的位置给出期望符号和源位置。”是否成立

</Objectives>

## 为什么不能只看最终输出

<Term def="Stone语言的语法连接写出BNF阶段的语法、结构、运行状态和回归证据。">Stone语言的语法</Term>、<Term def="使用解析器与组合子连接消除入口歧义阶段的语法、结构、运行状态和回归证据。">使用解析器与组合子</Term>、<Term def="由语法分析器生成的抽象语法树连接组合解析器阶段的语法、结构、运行状态和回归证据。">由语法分析器生成的抽象语法树</Term>、<Term def="测试语法分析器连接生成AST阶段的语法、结构、运行状态和回归证据。">测试语法分析器</Term>、<Term def="BNF连接定位语法错误阶段的语法、结构、运行状态和回归证据。">BNF</Term>
共同构成本页坐标。第5天
设计语法分析器围绕Stone语言的语法、使用解析器与组合子、由语法分析器生成的抽象语法树、测试语法分析器推进Stone语言，实现从源文本、Token、AST到求值、优化或代码生成的可回归增量。

最终输出相同不代表实现相同：词法器可能吞掉错误字符，解析器可能生成错误结合性的树，解释器可能从调用点而非定义点捕获环境，优化器也可能只在当前样本上碰巧得到正确值。因此每次增量都保留源文本、Token范围、AST快照、环境或帧状态、标准输出、错误输出与退出状态。

## Stone增量实现台账

先冻结上一日全部样本和公开接口，再为本日功能写一个最小成功程序、一个边界程序、一个故意失败程序和一个跨功能程序。成功样本证明新语法能到达执行层；边界样本暴露空输入、嵌套、遮蔽、短路、越界或缓存失效；失败样本必须在最早确定的位置停止；跨功能样本防止新节点破坏旧的表达式、控制流与函数语义。

处理流水线分为六个可观察检查点：源字符是否完整消费，Token是否携带类型和值及源位置，文法是否选择唯一分支，AST是否表达优先级与作用域，运行环境或虚拟机是否遵守所有权和调用约定，最终值与错误是否满足语言合同。只记录最终打印文本会丢失前五层最有诊断价值的证据。

每个错误对象至少含阶段、源文件、行列范围、实际符号、期望集合、原因和恢复建议。不要在底层捕获后返回null继续执行，因为后续空指针会掩盖真正错误；也不要把Java反射异常原样暴露给Stone程序，而应保留cause并映射为稳定的语言错误类别。

性能增量采用差分验证：同一语料分别运行基线解释器和优化版本，先比较值、输出、错误类别与位置，再比较预热后的时间、分配量与查找次数。若槽位分配、内联缓存或字节码改变异常顺序，性能再高也不能通过语义验收。

每次提交还要附带增量契约：本日新增哪些Token、文法产生式、AST节点、环境槽位、虚拟指令或类型规则，哪些旧接口保持不变，失败后如何回退到上一日可运行状态。将生成的Token、AST或字节码快照纳入版本比较，但不把快照更新本身当成修复；先解释结构变化为何符合语言设计，再接受新的基线。这样可把19天连续演进变成可二分、可撤销、可复查的工程历史。

## 六阶段证据链

### 1. 写出BNF

围绕Stone语言的语法保存输入、结构化中间状态与输出。进入“消除入口歧义”前，先断言当前检查点成立并记录首个偏离。

### 2. 消除入口歧义

围绕使用解析器与组合子保存输入、结构化中间状态与输出。进入“组合解析器”前，先断言当前检查点成立并记录首个偏离。

### 3. 组合解析器

围绕由语法分析器生成的抽象语法树保存输入、结构化中间状态与输出。进入“生成AST”前，先断言当前检查点成立并记录首个偏离。

### 4. 生成AST

围绕测试语法分析器保存输入、结构化中间状态与输出。进入“定位语法错误”前，先断言当前检查点成立并记录首个偏离。

### 5. 定位语法错误

围绕BNF保存输入、结构化中间状态与输出。进入“快照树结构”前，先断言当前检查点成立并记录首个偏离。

### 6. 快照树结构

围绕Stone语言的语法保存输入、结构化中间状态与输出。进入“写出BNF”前，先断言当前检查点成立并记录首个偏离。

## 可运行骨架

```bnf
statement : 'if' expression block ('else' block)? | expression
expression: primary (OP primary)*
```

```json
{ \"kind\": \"BinaryExpr\", \"op\": \"+\", \"left\": \"a\", \"right\": 1 }
```

```bash
java stone.ParserTest < samples/precedence.stone > ast.json
diff -u expected/precedence.json ast.json
```

<Callout type="trap">
  **版本边界：**
  本书基于Java与GluonJ构建Stone。现代JDK的模块、反射访问和类路径规则可能不同；先记录JDK、GluonJ及启动参数，再区分书中设计思想与当前平台兼容性。
</Callout>

## 正式目录逐项讲解

### 第5天 设计语法分析器

“第5天 设计语法分析器”要求把规则写成可执行文法，检查递归、歧义、优先级、结合性、前看集合和失败位置，再用AST快照证明结果。在本日增量中，“Stone语言的语法”服务于“写出BNF”：输入保存原始Stone源码、预期Token或AST、初始环境与宿主版本，输出保存结构化结果、标准错误和退出状态。

验证时只改变一个运算符、分隔符、绑定、对象布局、类型标注或输入规模，并在进入“消除入口歧义”前停住第一条Token范围、语法分支、AST节点、环境绑定、虚拟指令或类型约束分叉。修复后重放此前所有日次样本；若旧语义改变，必须说明是有意语言演进还是回归。

### 5.1 Stone语言的语法

“5.1 Stone语言的语法”要求先给出可运行正例，再列出语法、环境、对象、返回值和失败合同，最后沿处理流水线停在第一处分叉。在本日增量中，“使用解析器与组合子”服务于“消除入口歧义”：输入保存原始Stone源码、预期Token或AST、初始环境与宿主版本，输出保存结构化结果、标准错误和退出状态。

验证时只改变一个运算符、分隔符、绑定、对象布局、类型标注或输入规模，并在进入“组合解析器”前停住第一条Token范围、语法分支、AST节点、环境绑定、虚拟指令或类型约束分叉。修复后重放此前所有日次样本；若旧语义改变，必须说明是有意语言演进还是回归。

### 5.2 使用解析器与组合子

“5.2 使用解析器与组合子”要求把规则写成可执行文法，检查递归、歧义、优先级、结合性、前看集合和失败位置，再用AST快照证明结果。在本日增量中，“由语法分析器生成的抽象语法树”服务于“组合解析器”：输入保存原始Stone源码、预期Token或AST、初始环境与宿主版本，输出保存结构化结果、标准错误和退出状态。

验证时只改变一个运算符、分隔符、绑定、对象布局、类型标注或输入规模，并在进入“生成AST”前停住第一条Token范围、语法分支、AST节点、环境绑定、虚拟指令或类型约束分叉。修复后重放此前所有日次样本；若旧语义改变，必须说明是有意语言演进还是回归。

### 5.3 由语法分析器生成的抽象语法树

“5.3 由语法分析器生成的抽象语法树”要求把规则写成可执行文法，检查递归、歧义、优先级、结合性、前看集合和失败位置，再用AST快照证明结果。在本日增量中，“测试语法分析器”服务于“生成AST”：输入保存原始Stone源码、预期Token或AST、初始环境与宿主版本，输出保存结构化结果、标准错误和退出状态。

验证时只改变一个运算符、分隔符、绑定、对象布局、类型标注或输入规模，并在进入“定位语法错误”前停住第一条Token范围、语法分支、AST节点、环境绑定、虚拟指令或类型约束分叉。修复后重放此前所有日次样本；若旧语义改变，必须说明是有意语言演进还是回归。

### 5.4 测试语法分析器

“5.4 测试语法分析器”要求把规则写成可执行文法，检查递归、歧义、优先级、结合性、前看集合和失败位置，再用AST快照证明结果。在本日增量中，“BNF”服务于“定位语法错误”：输入保存原始Stone源码、预期Token或AST、初始环境与宿主版本，输出保存结构化结果、标准错误和退出状态。

验证时只改变一个运算符、分隔符、绑定、对象布局、类型标注或输入规模，并在进入“快照树结构”前停住第一条Token范围、语法分支、AST节点、环境绑定、虚拟指令或类型约束分叉。修复后重放此前所有日次样本；若旧语义改变，必须说明是有意语言演进还是回归。

### 专栏第2话 写书只需3个月！？

“专栏第2话 写书只需3个月！？”要求提炼专栏对实现取舍的工程启示，并用复杂度、可读性、进度与可回归性四项证据判断它何时成立。在本日增量中，“Stone语言的语法”服务于“快照树结构”：输入保存原始Stone源码、预期Token或AST、初始环境与宿主版本，输出保存结构化结果、标准错误和退出状态。

验证时只改变一个运算符、分隔符、绑定、对象布局、类型标注或输入规模，并在进入“写出BNF”前停住第一条Token范围、语法分支、AST节点、环境绑定、虚拟指令或类型约束分叉。修复后重放此前所有日次样本；若旧语义改变，必须说明是有意语言演进还是回归。

## 分步视觉验证

<Stepper>
  <Step title="画处理流水线">
    先预测Stone语言的语法、使用解析器与组合子、由语法分析器生成的抽象语法树、测试语法分析器、BNF在Token、AST、环境或虚拟机中的位置和所有权。
    <Tws05ParserDesignMapLab />
  </Step>
  <Step title="运行单变量实验">
    固定上一日解释器和样本，只切换一个语法、绑定、类型或优化条件。
    <Tws05ParserDesignExperimentLab />
  </Step>
  <Step title="核对首处分叉">
    比较结构化证据而非只比最终文本，修复后重放全部历史样本。
    <Tws05ParserDesignEvidenceLab />
  </Step>
</Stepper>

## 常见误区

<Callout type="trap">
  **误区一：**
  只测合法输入。没有非法Token、缺失分隔符、未定义变量、错误参数、越界、类型冲突和缓存失效样本，就无法证明错误边界。
</Callout>

<Callout type="trap">
  **误区二：**
  为了减少类或代码量，把词法、语法、语义与宿主异常混成一个分支。简洁应来自稳定的数据合同，不应来自丢失阶段边界。
</Callout>

## 四类样本与验收

| 样本 | 只改变什么                 | 必查证据                 | 通过条件       |
| ---- | -------------------------- | ------------------------ | -------------- |
| 正常 | 一个最小语言功能           | Token、AST、状态与输出   | 与手工预测一致 |
| 边界 | 空值、嵌套、遮蔽或规模     | 首个边界分支与源位置     | 边界可解释     |
| 非法 | 一个字符、规则、绑定或类型 | 最早错误类别与期望集合   | 不继续污染状态 |
| 回归 | 与上一日相同语料           | 结构、值、错误与性能基线 | 旧语义保持     |

## 练习

<Exercises>

**问题 1** 为什么不能只凭最终输出验收第5天 设计语法分析器？

<Answer>
  最终文本会隐藏Token范围、语法结合性、AST形状、环境捕获、指令栈效应与错误位置。必须同时保存各阶段结构化证据，并停在第一处分叉。
</Answer>

**问题 2** 如何把“组合解析器”变成可回归实验？

<Answer>
  固定JDK、类路径、上一日代码与输入，先预测Stone语言的语法和使用解析器与组合子的中间状态，只改变一个条件，保存成功、边界、非法和跨功能样本，修复后重放全部历史语料。
</Answer>

**问题 3** 本页最小通过条件是什么？

<Answer>
  合法Token流只生成一种符合优先级与结合性的AST，非法输入在最早无法继续的位置给出期望符号和源位置。
  同时要求正常结果、失败位置与历史样本一致；不能以性能提升或“程序能跑”替代语义等价证明。
</Answer>

</Exercises>

## 小结

- Stone语言的语法：连接“写出BNF”的源文本、中间结构、运行状态与回归证据。
- 使用解析器与组合子：连接“消除入口歧义”的源文本、中间结构、运行状态与回归证据。
- 由语法分析器生成的抽象语法树：连接“组合解析器”的源文本、中间结构、运行状态与回归证据。
- 测试语法分析器：连接“生成AST”的源文本、中间结构、运行状态与回归证据。
- BNF：连接“定位语法错误”的源文本、中间结构、运行状态与回归证据。
- 本页6个正式目录层级全部进入Stone可运行增量与差分回归链。

## 术语表

<Glossary>
  <GlossaryItem term="Stone语言的语法">
    Stone语言的语法用于解释“写出BNF”，需要同时指出输入、结构化中间状态、失败位置和回归样本。
  </GlossaryItem>
  <GlossaryItem term="使用解析器与组合子">
    使用解析器与组合子用于解释“消除入口歧义”，需要同时指出输入、结构化中间状态、失败位置和回归样本。
  </GlossaryItem>
  <GlossaryItem term="由语法分析器生成的抽象语法树">
    由语法分析器生成的抽象语法树用于解释“组合解析器”，需要同时指出输入、结构化中间状态、失败位置和回归样本。
  </GlossaryItem>
  <GlossaryItem term="测试语法分析器">
    测试语法分析器用于解释“生成AST”，需要同时指出输入、结构化中间状态、失败位置和回归样本。
  </GlossaryItem>
  <GlossaryItem term="BNF">
    BNF用于解释“定位语法错误”，需要同时指出输入、结构化中间状态、失败位置和回归样本。
  </GlossaryItem>
</Glossary>

<Attribution
  adaptedFrom="千叶滋著，陈筱烟译《两周自制脚本语言》，人民邮电出版社，2014-06，第1版，285页，ISBN 9787115355645 · 中文版完整目录与日文原版出版社目录的独立教学重构"
  adaptedUrl="https://gihyo.jp/book/2012/978-4-7741-4974-5"
/>
