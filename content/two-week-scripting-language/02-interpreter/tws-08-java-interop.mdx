---
title: "第8天 关联Java语言"
type: C
section: "基础篇 · 解释器与语言功能"
order: 8
description: "第8天 关联Java语言围绕原生函数、编写使用原生函数的程序推进Stone语言，实现从源文本、Token、AST到求值、优化或代码生成的可回归增量。 覆盖3个正式目录层级。"
demo: true
math: false
sourceUrl: "https://gihyo.jp/book/2012/978-4-7741-4974-5"
draft: true
---

import {
  Tws08JavaInteropMapLab,
  Tws08JavaInteropExperimentLab,
  Tws08JavaInteropEvidenceLab,
} from "@/components/mdx/two-week-scripting-language/diagrams/tws-08-java-interop";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第8天 关联Java语言的核心增量，并把每个正式目录项定位到Stone处理流水线和具体数据结构
- 能描述“声明宿主边界 → 解析Java成员 → 转换参数类型 → 调用宿主代码 → 映射异常 → 验证类路径”的输入、输出、不变量与首个失败位置
- 能实现本日语言增量，并为正常、边界、非法与回归样本保留Token、AST、环境或字节码证据
- 能比较修改前后行为并判断“Stone到Java的每次跨界调用都能说明目标成员、参数转换、返回包装与异常映射，类路径变化不会静默改变语言语义。”是否成立

</Objectives>

## 为什么不能只看最终输出

<Term def="原生函数连接声明宿主边界阶段的语法、结构、运行状态和回归证据。">原生函数</Term>、<Term def="编写使用原生函数的程序连接解析Java成员阶段的语法、结构、运行状态和回归证据。">编写使用原生函数的程序</Term>、<Term def="Java互操作连接转换参数类型阶段的语法、结构、运行状态和回归证据。">Java互操作</Term>、<Term def="GluonJ连接调用宿主代码阶段的语法、结构、运行状态和回归证据。">GluonJ</Term>、<Term def="类路径连接映射异常阶段的语法、结构、运行状态和回归证据。">类路径</Term>
共同构成本页坐标。第8天
关联Java语言围绕原生函数、编写使用原生函数的程序推进Stone语言，实现从源文本、Token、AST到求值、优化或代码生成的可回归增量。

最终输出相同不代表实现相同：词法器可能吞掉错误字符，解析器可能生成错误结合性的树，解释器可能从调用点而非定义点捕获环境，优化器也可能只在当前样本上碰巧得到正确值。因此每次增量都保留源文本、Token范围、AST快照、环境或帧状态、标准输出、错误输出与退出状态。

## Stone增量实现台账

先冻结上一日全部样本和公开接口，再为本日功能写一个最小成功程序、一个边界程序、一个故意失败程序和一个跨功能程序。成功样本证明新语法能到达执行层；边界样本暴露空输入、嵌套、遮蔽、短路、越界或缓存失效；失败样本必须在最早确定的位置停止；跨功能样本防止新节点破坏旧的表达式、控制流与函数语义。

处理流水线分为六个可观察检查点：源字符是否完整消费，Token是否携带类型和值及源位置，文法是否选择唯一分支，AST是否表达优先级与作用域，运行环境或虚拟机是否遵守所有权和调用约定，最终值与错误是否满足语言合同。只记录最终打印文本会丢失前五层最有诊断价值的证据。

每个错误对象至少含阶段、源文件、行列范围、实际符号、期望集合、原因和恢复建议。不要在底层捕获后返回null继续执行，因为后续空指针会掩盖真正错误；也不要把Java反射异常原样暴露给Stone程序，而应保留cause并映射为稳定的语言错误类别。

性能增量采用差分验证：同一语料分别运行基线解释器和优化版本，先比较值、输出、错误类别与位置，再比较预热后的时间、分配量与查找次数。若槽位分配、内联缓存或字节码改变异常顺序，性能再高也不能通过语义验收。

每次提交还要附带增量契约：本日新增哪些Token、文法产生式、AST节点、环境槽位、虚拟指令或类型规则，哪些旧接口保持不变，失败后如何回退到上一日可运行状态。将生成的Token、AST或字节码快照纳入版本比较，但不把快照更新本身当成修复；先解释结构变化为何符合语言设计，再接受新的基线。这样可把19天连续演进变成可二分、可撤销、可复查的工程历史。

## 六阶段证据链

### 1. 声明宿主边界

围绕原生函数保存输入、结构化中间状态与输出。进入“解析Java成员”前，先断言当前检查点成立并记录首个偏离。

### 2. 解析Java成员

围绕编写使用原生函数的程序保存输入、结构化中间状态与输出。进入“转换参数类型”前，先断言当前检查点成立并记录首个偏离。

### 3. 转换参数类型

围绕Java互操作保存输入、结构化中间状态与输出。进入“调用宿主代码”前，先断言当前检查点成立并记录首个偏离。

### 4. 调用宿主代码

围绕GluonJ保存输入、结构化中间状态与输出。进入“映射异常”前，先断言当前检查点成立并记录首个偏离。

### 5. 映射异常

围绕类路径保存输入、结构化中间状态与输出。进入“验证类路径”前，先断言当前检查点成立并记录首个偏离。

### 6. 验证类路径

围绕原生函数保存输入、结构化中间状态与输出。进入“声明宿主边界”前，先断言当前检查点成立并记录首个偏离。

## 可运行骨架

```stone
print(\"clock=\" + currentTime())
```

```java
Method target = NativeLibrary.class.getMethod(name, parameterTypes);
return target.invoke(receiver, arguments);
```

```bash
java -cp 'build/classes:lib/gluonj.jar' stone.NativeInterpreter < samples/native.stone
```

<Callout type="trap">
  **版本边界：**
  本书基于Java与GluonJ构建Stone。现代JDK的模块、反射访问和类路径规则可能不同；先记录JDK、GluonJ及启动参数，再区分书中设计思想与当前平台兼容性。
</Callout>

## 正式目录逐项讲解

### 第8天 关联Java语言

“第8天 关联Java语言”要求标明Stone与Java边界上的成员解析、参数转换、返回包装、异常映射和类路径依赖，避免宿主细节泄漏为语言语义。在本日增量中，“原生函数”服务于“声明宿主边界”：输入保存原始Stone源码、预期Token或AST、初始环境与宿主版本，输出保存结构化结果、标准错误和退出状态。

验证时只改变一个运算符、分隔符、绑定、对象布局、类型标注或输入规模，并在进入“解析Java成员”前停住第一条Token范围、语法分支、AST节点、环境绑定、虚拟指令或类型约束分叉。修复后重放此前所有日次样本；若旧语义改变，必须说明是有意语言演进还是回归。

### 8.1 原生函数

“8.1 原生函数”要求画出词法环境、调用帧、参数绑定、返回值和闭包捕获关系，逐节点记录求值顺序、副作用与异常传播。在本日增量中，“编写使用原生函数的程序”服务于“解析Java成员”：输入保存原始Stone源码、预期Token或AST、初始环境与宿主版本，输出保存结构化结果、标准错误和退出状态。

验证时只改变一个运算符、分隔符、绑定、对象布局、类型标注或输入规模，并在进入“转换参数类型”前停住第一条Token范围、语法分支、AST节点、环境绑定、虚拟指令或类型约束分叉。修复后重放此前所有日次样本；若旧语义改变，必须说明是有意语言演进还是回归。

### 8.2 编写使用原生函数的程序

“8.2 编写使用原生函数的程序”要求画出词法环境、调用帧、参数绑定、返回值和闭包捕获关系，逐节点记录求值顺序、副作用与异常传播。在本日增量中，“Java互操作”服务于“转换参数类型”：输入保存原始Stone源码、预期Token或AST、初始环境与宿主版本，输出保存结构化结果、标准错误和退出状态。

验证时只改变一个运算符、分隔符、绑定、对象布局、类型标注或输入规模，并在进入“调用宿主代码”前停住第一条Token范围、语法分支、AST节点、环境绑定、虚拟指令或类型约束分叉。修复后重放此前所有日次样本；若旧语义改变，必须说明是有意语言演进还是回归。

## 分步视觉验证

<Stepper>
  <Step title="画处理流水线">
    先预测原生函数、编写使用原生函数的程序、Java互操作、GluonJ、类路径在Token、AST、环境或虚拟机中的位置和所有权。
    <Tws08JavaInteropMapLab />
  </Step>
  <Step title="运行单变量实验">
    固定上一日解释器和样本，只切换一个语法、绑定、类型或优化条件。
    <Tws08JavaInteropExperimentLab />
  </Step>
  <Step title="核对首处分叉">
    比较结构化证据而非只比最终文本，修复后重放全部历史样本。
    <Tws08JavaInteropEvidenceLab />
  </Step>
</Stepper>

## 常见误区

<Callout type="trap">
  **误区一：**
  只测合法输入。没有非法Token、缺失分隔符、未定义变量、错误参数、越界、类型冲突和缓存失效样本，就无法证明错误边界。
</Callout>

<Callout type="trap">
  **误区二：**
  为了减少类或代码量，把词法、语法、语义与宿主异常混成一个分支。简洁应来自稳定的数据合同，不应来自丢失阶段边界。
</Callout>

## 四类样本与验收

| 样本 | 只改变什么                 | 必查证据                 | 通过条件       |
| ---- | -------------------------- | ------------------------ | -------------- |
| 正常 | 一个最小语言功能           | Token、AST、状态与输出   | 与手工预测一致 |
| 边界 | 空值、嵌套、遮蔽或规模     | 首个边界分支与源位置     | 边界可解释     |
| 非法 | 一个字符、规则、绑定或类型 | 最早错误类别与期望集合   | 不继续污染状态 |
| 回归 | 与上一日相同语料           | 结构、值、错误与性能基线 | 旧语义保持     |

## 练习

<Exercises>

**问题 1** 为什么不能只凭最终输出验收第8天 关联Java语言？

<Answer>
  最终文本会隐藏Token范围、语法结合性、AST形状、环境捕获、指令栈效应与错误位置。必须同时保存各阶段结构化证据，并停在第一处分叉。
</Answer>

**问题 2** 如何把“转换参数类型”变成可回归实验？

<Answer>
  固定JDK、类路径、上一日代码与输入，先预测原生函数和编写使用原生函数的程序的中间状态，只改变一个条件，保存成功、边界、非法和跨功能样本，修复后重放全部历史语料。
</Answer>

**问题 3** 本页最小通过条件是什么？

<Answer>
  Stone到Java的每次跨界调用都能说明目标成员、参数转换、返回包装与异常映射，类路径变化不会静默改变语言语义。
  同时要求正常结果、失败位置与历史样本一致；不能以性能提升或“程序能跑”替代语义等价证明。
</Answer>

</Exercises>

## 小结

- 原生函数：连接“声明宿主边界”的源文本、中间结构、运行状态与回归证据。
- 编写使用原生函数的程序：连接“解析Java成员”的源文本、中间结构、运行状态与回归证据。
- Java互操作：连接“转换参数类型”的源文本、中间结构、运行状态与回归证据。
- GluonJ：连接“调用宿主代码”的源文本、中间结构、运行状态与回归证据。
- 类路径：连接“映射异常”的源文本、中间结构、运行状态与回归证据。
- 本页3个正式目录层级全部进入Stone可运行增量与差分回归链。

## 术语表

<Glossary>
  <GlossaryItem term="原生函数">
    原生函数用于解释“声明宿主边界”，需要同时指出输入、结构化中间状态、失败位置和回归样本。
  </GlossaryItem>
  <GlossaryItem term="编写使用原生函数的程序">
    编写使用原生函数的程序用于解释“解析Java成员”，需要同时指出输入、结构化中间状态、失败位置和回归样本。
  </GlossaryItem>
  <GlossaryItem term="Java互操作">
    Java互操作用于解释“转换参数类型”，需要同时指出输入、结构化中间状态、失败位置和回归样本。
  </GlossaryItem>
  <GlossaryItem term="GluonJ">
    GluonJ用于解释“调用宿主代码”，需要同时指出输入、结构化中间状态、失败位置和回归样本。
  </GlossaryItem>
  <GlossaryItem term="类路径">
    类路径用于解释“映射异常”，需要同时指出输入、结构化中间状态、失败位置和回归样本。
  </GlossaryItem>
</Glossary>

<Attribution
  adaptedFrom="千叶滋著，陈筱烟译《两周自制脚本语言》，人民邮电出版社，2014-06，第1版，285页，ISBN 9787115355645 · 中文版完整目录与日文原版出版社目录的独立教学重构"
  adaptedUrl="https://gihyo.jp/book/2012/978-4-7741-4974-5"
/>
