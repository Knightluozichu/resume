---
title: "第16天 语法分析方式"
type: C
section: "解说篇 · 理论与实现机制"
order: 16
description: "第16天 语法分析方式围绕正则表达式与BNF、语法分析算法、LL语法分析、算符优先分析法与自底向上语法分析推进Stone语言，实现从源文本、Token、AST到求值、优化或代码生成的可回归增量。 覆盖6个正式目录层级。"
demo: true
math: false
sourceUrl: "https://gihyo.jp/book/2012/978-4-7741-4974-5"
draft: false
---

import {
  Tws16ParsingMethodsMapLab,
  Tws16ParsingMethodsExperimentLab,
  Tws16ParsingMethodsEvidenceLab,
} from "@/components/mdx/two-week-scripting-language/diagrams/tws-16-parsing-methods";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第16天 语法分析方式的核心增量，并把每个正式目录项定位到Stone处理流水线和具体数据结构
- 能描述“区分词法语法 → 写出形式规则 → 选择分析算法 → 计算前看集合 → 处理优先级 → 构造反例”的输入、输出、不变量与首个失败位置
- 能实现本日语言增量，并为正常、边界、非法与回归样本保留Token、AST、环境或字节码证据
- 能比较修改前后行为并判断“所选分析算法与文法类别相容，预测集合或优先级关系无冲突，反例能解释歧义、左递归或回溯代价。”是否成立

</Objectives>

## 为什么不能只看最终输出

<Term def="正则表达式与BNF连接区分词法语法阶段的语法、结构、运行状态和回归证据。">
  正则表达式与BNF
</Term>
、
<Term def="语法分析算法连接写出形式规则阶段的语法、结构、运行状态和回归证据。">
  语法分析算法
</Term>
、
<Term def="LL语法分析连接选择分析算法阶段的语法、结构、运行状态和回归证据。">
  LL语法分析
</Term>
、
<Term def="算符优先分析法与自底向上语法分析连接计算前看集合阶段的语法、结构、运行状态和回归证据。">
  算符优先分析法与自底向上语法分析
</Term>
、
<Term def="正则语言连接处理优先级阶段的语法、结构、运行状态和回归证据。">
  正则语言
</Term>
共同构成本页坐标。第16天
语法分析方式围绕正则表达式与BNF、语法分析算法、LL语法分析、算符优先分析法与自底向上语法分析推进Stone语言，实现从源文本、Token、AST到求值、优化或代码生成的可回归增量。

最终输出相同不代表实现相同：词法器可能吞掉错误字符，解析器可能生成错误结合性的树，解释器可能从调用点而非定义点捕获环境，优化器也可能只在当前样本上碰巧得到正确值。因此每次增量都保留源文本、Token范围、AST快照、环境或帧状态、标准输出、错误输出与退出状态。

## Stone增量实现台账

先冻结上一日全部样本和公开接口，再为本日功能写一个最小成功程序、一个边界程序、一个故意失败程序和一个跨功能程序。成功样本证明新语法能到达执行层；边界样本暴露空输入、嵌套、遮蔽、短路、越界或缓存失效；失败样本必须在最早确定的位置停止；跨功能样本防止新节点破坏旧的表达式、控制流与函数语义。

处理流水线分为六个可观察检查点：源字符是否完整消费，Token是否携带类型和值及源位置，文法是否选择唯一分支，AST是否表达优先级与作用域，运行环境或虚拟机是否遵守所有权和调用约定，最终值与错误是否满足语言合同。只记录最终打印文本会丢失前五层最有诊断价值的证据。

每个错误对象至少含阶段、源文件、行列范围、实际符号、期望集合、原因和恢复建议。不要在底层捕获后返回null继续执行，因为后续空指针会掩盖真正错误；也不要把Java反射异常原样暴露给Stone程序，而应保留cause并映射为稳定的语言错误类别。

性能增量采用差分验证：同一语料分别运行基线解释器和优化版本，先比较值、输出、错误类别与位置，再比较预热后的时间、分配量与查找次数。若槽位分配、内联缓存或字节码改变异常顺序，性能再高也不能通过语义验收。

每次提交还要附带增量契约：本日新增哪些Token、文法产生式、AST节点、环境槽位、虚拟指令或类型规则，哪些旧接口保持不变，失败后如何回退到上一日可运行状态。将生成的Token、AST或字节码快照纳入版本比较，但不把快照更新本身当成修复；先解释结构变化为何符合语言设计，再接受新的基线。这样可把19天连续演进变成可二分、可撤销、可复查的工程历史。

## 六阶段证据链

### 1. 区分词法语法

围绕正则表达式与BNF保存输入、结构化中间状态与输出。进入“写出形式规则”前，先断言当前检查点成立并记录首个偏离。

### 2. 写出形式规则

围绕语法分析算法保存输入、结构化中间状态与输出。进入“选择分析算法”前，先断言当前检查点成立并记录首个偏离。

### 3. 选择分析算法

围绕LL语法分析保存输入、结构化中间状态与输出。进入“计算前看集合”前，先断言当前检查点成立并记录首个偏离。

### 4. 计算前看集合

围绕算符优先分析法与自底向上语法分析保存输入、结构化中间状态与输出。进入“处理优先级”前，先断言当前检查点成立并记录首个偏离。

### 5. 处理优先级

围绕正则语言保存输入、结构化中间状态与输出。进入“构造反例”前，先断言当前检查点成立并记录首个偏离。

### 6. 构造反例

围绕正则表达式与BNF保存输入、结构化中间状态与输出。进入“区分词法语法”前，先断言当前检查点成立并记录首个偏离。

## 可运行骨架

```bnf
expr   : term (('+' | '-') term)*
term   : factor (('*' | '/') factor)*
factor : NUMBER | '(' expr ')'
```

```text
FIRST(factor) = { NUMBER, '(' }
FOLLOW(expr) = { ')', EOL, EOF }
```

```bash
java stone.GrammarProbe samples/ambiguous.bnf
java stone.GrammarProbe samples/precedence.bnf
```

<Callout type="trap">
  **版本边界：**
  本书基于Java与GluonJ构建Stone。现代JDK的模块、反射访问和类路径规则可能不同；先记录JDK、GluonJ及启动参数，再区分书中设计思想与当前平台兼容性。
</Callout>

## 正式目录逐项讲解

### 第16天 语法分析方式

“第16天 语法分析方式”要求把规则写成可执行文法，检查递归、歧义、优先级、结合性、前看集合和失败位置，再用AST快照证明结果。在本日增量中，“正则表达式与BNF”服务于“区分词法语法”：输入保存原始Stone源码、预期Token或AST、初始环境与宿主版本，输出保存结构化结果、标准错误和退出状态。

验证时只改变一个运算符、分隔符、绑定、对象布局、类型标注或输入规模，并在进入“写出形式规则”前停住第一条Token范围、语法分支、AST节点、环境绑定、虚拟指令或类型约束分叉。修复后重放此前所有日次样本；若旧语义改变，必须说明是有意语言演进还是回归。

### 16.1 正则表达式与BNF

“16.1 正则表达式与BNF”要求规定Token类别、最长匹配、跳过规则、转义与源位置，确保每个字符要么被消费一次，要么产生明确词法错误。在本日增量中，“语法分析算法”服务于“写出形式规则”：输入保存原始Stone源码、预期Token或AST、初始环境与宿主版本，输出保存结构化结果、标准错误和退出状态。

验证时只改变一个运算符、分隔符、绑定、对象布局、类型标注或输入规模，并在进入“选择分析算法”前停住第一条Token范围、语法分支、AST节点、环境绑定、虚拟指令或类型约束分叉。修复后重放此前所有日次样本；若旧语义改变，必须说明是有意语言演进还是回归。

### 16.2 语法分析算法

“16.2 语法分析算法”要求把规则写成可执行文法，检查递归、歧义、优先级、结合性、前看集合和失败位置，再用AST快照证明结果。在本日增量中，“LL语法分析”服务于“选择分析算法”：输入保存原始Stone源码、预期Token或AST、初始环境与宿主版本，输出保存结构化结果、标准错误和退出状态。

验证时只改变一个运算符、分隔符、绑定、对象布局、类型标注或输入规模，并在进入“计算前看集合”前停住第一条Token范围、语法分支、AST节点、环境绑定、虚拟指令或类型约束分叉。修复后重放此前所有日次样本；若旧语义改变，必须说明是有意语言演进还是回归。

### 16.3 LL语法分析

“16.3 LL语法分析”要求把规则写成可执行文法，检查递归、歧义、优先级、结合性、前看集合和失败位置，再用AST快照证明结果。在本日增量中，“算符优先分析法与自底向上语法分析”服务于“计算前看集合”：输入保存原始Stone源码、预期Token或AST、初始环境与宿主版本，输出保存结构化结果、标准错误和退出状态。

验证时只改变一个运算符、分隔符、绑定、对象布局、类型标注或输入规模，并在进入“处理优先级”前停住第一条Token范围、语法分支、AST节点、环境绑定、虚拟指令或类型约束分叉。修复后重放此前所有日次样本；若旧语义改变，必须说明是有意语言演进还是回归。

### 16.4 算符优先分析法与自底向上语法分析

“16.4 算符优先分析法与自底向上语法分析”要求把规则写成可执行文法，检查递归、歧义、优先级、结合性、前看集合和失败位置，再用AST快照证明结果。在本日增量中，“正则语言”服务于“处理优先级”：输入保存原始Stone源码、预期Token或AST、初始环境与宿主版本，输出保存结构化结果、标准错误和退出状态。

验证时只改变一个运算符、分隔符、绑定、对象布局、类型标注或输入规模，并在进入“构造反例”前停住第一条Token范围、语法分支、AST节点、环境绑定、虚拟指令或类型约束分叉。修复后重放此前所有日次样本；若旧语义改变，必须说明是有意语言演进还是回归。

### 专栏第6话 武勇传

“专栏第6话 武勇传”要求提炼专栏对实现取舍的工程启示，并用复杂度、可读性、进度与可回归性四项证据判断它何时成立。在本日增量中，“正则表达式与BNF”服务于“构造反例”：输入保存原始Stone源码、预期Token或AST、初始环境与宿主版本，输出保存结构化结果、标准错误和退出状态。

验证时只改变一个运算符、分隔符、绑定、对象布局、类型标注或输入规模，并在进入“区分词法语法”前停住第一条Token范围、语法分支、AST节点、环境绑定、虚拟指令或类型约束分叉。修复后重放此前所有日次样本；若旧语义改变，必须说明是有意语言演进还是回归。

## 分步视觉验证

<Stepper>
  <Step title="画处理流水线">
    先预测正则表达式与BNF、语法分析算法、LL语法分析、算符优先分析法与自底向上语法分析、正则语言在Token、AST、环境或虚拟机中的位置和所有权。
    <Tws16ParsingMethodsMapLab />
  </Step>
  <Step title="运行单变量实验">
    固定上一日解释器和样本，只切换一个语法、绑定、类型或优化条件。
    <Tws16ParsingMethodsExperimentLab />
  </Step>
  <Step title="核对首处分叉">
    比较结构化证据而非只比最终文本，修复后重放全部历史样本。
    <Tws16ParsingMethodsEvidenceLab />
  </Step>
</Stepper>

## 常见误区

<Callout type="trap">
  **误区一：**
  只测合法输入。没有非法Token、缺失分隔符、未定义变量、错误参数、越界、类型冲突和缓存失效样本，就无法证明错误边界。
</Callout>

<Callout type="trap">
  **误区二：**
  为了减少类或代码量，把词法、语法、语义与宿主异常混成一个分支。简洁应来自稳定的数据合同，不应来自丢失阶段边界。
</Callout>

## 四类样本与验收

| 样本 | 只改变什么                 | 必查证据                 | 通过条件       |
| ---- | -------------------------- | ------------------------ | -------------- |
| 正常 | 一个最小语言功能           | Token、AST、状态与输出   | 与手工预测一致 |
| 边界 | 空值、嵌套、遮蔽或规模     | 首个边界分支与源位置     | 边界可解释     |
| 非法 | 一个字符、规则、绑定或类型 | 最早错误类别与期望集合   | 不继续污染状态 |
| 回归 | 与上一日相同语料           | 结构、值、错误与性能基线 | 旧语义保持     |

## 练习

<Exercises>

**问题 1** 为什么不能只凭最终输出验收第16天 语法分析方式？

<Answer>
  最终文本会隐藏Token范围、语法结合性、AST形状、环境捕获、指令栈效应与错误位置。必须同时保存各阶段结构化证据，并停在第一处分叉。
</Answer>

**问题 2** 如何把“选择分析算法”变成可回归实验？

<Answer>
  固定JDK、类路径、上一日代码与输入，先预测正则表达式与BNF和语法分析算法的中间状态，只改变一个条件，保存成功、边界、非法和跨功能样本，修复后重放全部历史语料。
</Answer>

**问题 3** 本页最小通过条件是什么？

<Answer>
  所选分析算法与文法类别相容，预测集合或优先级关系无冲突，反例能解释歧义、左递归或回溯代价。
  同时要求正常结果、失败位置与历史样本一致；不能以性能提升或“程序能跑”替代语义等价证明。
</Answer>

</Exercises>

## 小结

- 正则表达式与BNF：连接“区分词法语法”的源文本、中间结构、运行状态与回归证据。
- 语法分析算法：连接“写出形式规则”的源文本、中间结构、运行状态与回归证据。
- LL语法分析：连接“选择分析算法”的源文本、中间结构、运行状态与回归证据。
- 算符优先分析法与自底向上语法分析：连接“计算前看集合”的源文本、中间结构、运行状态与回归证据。
- 正则语言：连接“处理优先级”的源文本、中间结构、运行状态与回归证据。
- 本页6个正式目录层级全部进入Stone可运行增量与差分回归链。

## 术语表

<Glossary>
  <GlossaryItem term="正则表达式与BNF">
    正则表达式与BNF用于解释“区分词法语法”，需要同时指出输入、结构化中间状态、失败位置和回归样本。
  </GlossaryItem>
  <GlossaryItem term="语法分析算法">
    语法分析算法用于解释“写出形式规则”，需要同时指出输入、结构化中间状态、失败位置和回归样本。
  </GlossaryItem>
  <GlossaryItem term="LL语法分析">
    LL语法分析用于解释“选择分析算法”，需要同时指出输入、结构化中间状态、失败位置和回归样本。
  </GlossaryItem>
  <GlossaryItem term="算符优先分析法与自底向上语法分析">
    算符优先分析法与自底向上语法分析用于解释“计算前看集合”，需要同时指出输入、结构化中间状态、失败位置和回归样本。
  </GlossaryItem>
  <GlossaryItem term="正则语言">
    正则语言用于解释“处理优先级”，需要同时指出输入、结构化中间状态、失败位置和回归样本。
  </GlossaryItem>
</Glossary>

<Attribution
  adaptedFrom="千叶滋著，陈筱烟译《两周自制脚本语言》，人民邮电出版社，2014-06，第1版，285页，ISBN 9787115355645 · 中文版完整目录与日文原版出版社目录的独立教学重构"
  adaptedUrl="https://gihyo.jp/book/2012/978-4-7741-4974-5"
/>
