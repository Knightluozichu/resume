---
title: "第3章 数据表"
type: C
section: "Unity3D 高级编程之进阶主程 · 第3章"
order: 3
description: "数据表是策划输入与运行时对象之间的契约。格式选择、生成流程、版本校验和多语言键必须一起设计；只把 Excel 转成某种文件不等于建立了可靠的数据系统。"
demo: true
math: false
sourceUrl: "https://www.cnblogs.com/ZhYQ-Note/articles/15493562.html"
draft: false
---

import {
  Objectives, Callout, Attribution, Glossary, GlossaryItem, Term,
  Exercises, Answer, Stepper, Step,
} from "@/components/mdx/mdx-components";
import {
  U3ap03DataTablesArchitectureLab, U3ap03DataTablesRuntimeLab, U3ap03DataTablesReleaseLab,
} from "@/components/mdx/unity-advanced-programming/diagrams/u3ap-03-data-tables";

<Objectives>
- 能解释数据表的种类的成本与适用边界
- 能比较数据表的制作方式和多语言的实现的证据差异
- 能设计第3章的正常、边界和失败样本
- 能复现“分类数据 → 声明 schema → 导出生成 → 验证引用 → 加载运行时 → 切换语言复验”全链轨迹
- 能判断第3章是否满足主程交接门
</Objectives>

## 从连载位置与先预测开始

本页对应陆泽西（Jesse Lu）《Unity3D 高级编程之进阶主程》的第3章。该作品是在线技术连载，不是有 ISBN 的正式出版书；原作者旧站已不可作为稳定入口，因此以保存下来的完整文章索引核对章节，并保留原始标题与编号。

数据表是策划输入与运行时对象之间的契约。格式选择、生成流程、版本校验和多语言键必须一起设计；只把 Excel 转成某种文件不等于建立了可靠的数据系统。

<Callout type="note" title="先预测">
  在运行实验前，先预测“分类数据”经过“声明 schema”后最先变化的模块、状态或指标，并写出一个会推翻预测的结果。
</Callout>

<U3ap03DataTablesArchitectureLab />

## 原连载文章覆盖矩阵

| # | 原文章主题或本页检查点 | 可观察解释 |
| ---: | --- | --- |
| 1 | 数据表的种类 | 区分静态配置、数值曲线、本地化文本和运行存档。 |
| 2 | 数据表的制作方式 | 定义编辑、导出、校验、生成和加载流水线。 |
| 3 | 多语言的实现 | 以稳定键连接文本、参数、字体与回退语言。 |
| 4 | 表结构约束 | 对主键、类型、范围、引用和重复项执行自动校验。 |
| 5 | 版本与兼容 | 记录 schema 与内容版本，拒绝不兼容数据。 |

数据表的种类、数据表的制作方式、多语言的实现、表结构约束、版本与兼容 不是随意补充的主程知识清单，而是沿“分类数据 → 声明 schema → 导出生成 → 验证引用 → 加载运行时 → 切换语言复验”建立因果关系。每个条目都要能定位到实验输入、首个状态变化和最终门禁。

## 核心模型与技术边界

数据表是策划输入与运行时对象之间的契约。格式选择、生成流程、版本校验和多语言键必须一起设计；只把 Excel 转成某种文件不等于建立了可靠的数据系统。

连载写作年代与今天的 Unity、.NET 后端和渲染管线存在差异。复现时保留作者要解释的机制，同时记录旧 API、当前替代路径、运行时版本和验证结果。没有版本映射的“更新版结论”不能覆盖原文章，也不能把今天的默认行为倒推成当年的事实。

<Term def="区分静态配置、数值曲线、本地化文本和运行存档。">数据表的种类</Term>必须在本页留下输入、内部状态和输出证据，不能只作为术语出现。

<Term def="定义编辑、导出、校验、生成和加载流水线。">数据表的制作方式</Term>必须在本页留下输入、内部状态和输出证据，不能只作为术语出现。

<Term def="以稳定键连接文本、参数、字体与回退语言。">多语言的实现</Term>必须在本页留下输入、内部状态和输出证据，不能只作为术语出现。

<Term def="对主键、类型、范围、引用和重复项执行自动校验。">表结构约束</Term>必须在本页留下输入、内部状态和输出证据，不能只作为术语出现。

<Term def="记录 schema 与内容版本，拒绝不兼容数据。">版本与兼容</Term>必须在本页留下输入、内部状态和输出证据，不能只作为术语出现。

## 六阶段主程证据链

<Stepper>
  <Step title="1. 分类数据 → 声明 schema">
    固定 Unity、脚本后端、平台、数据规模和输入。先保存结构快照，再运行正常样本，禁止同时修改多个策略。
    <U3ap03DataTablesArchitectureLab />
  </Step>
  <Step title="2. 导出生成 → 验证引用">
    加入边界与故障样本，记录首个偏离节点。最终画面或平均值不能代替协议、分配、重建、决策或路径轨迹。
    <U3ap03DataTablesRuntimeLab />
  </Step>
  <Step title="3. 加载运行时 → 切换语言复验">
    确认门禁能拒绝故障，修复后用同一输入复测，并归档版本、日志、捕获、截图和结论。
    <U3ap03DataTablesReleaseLab />
  </Step>
</Stepper>

## 可复现实验

建立角色配置和本地化文本两张小表，固定 schema 版本。先预测重复主键、缺失引用和缺失语言键的错误位置，再注入三种故障验证构建门。

最小实现只暴露本章关键状态，不把因果藏进通用框架。

```csharp
public sealed record HeroRow(int Id, string NameKey, int BaseHp);

static void Validate(HeroRow row, ISet<int> ids)
{
    if (!ids.Add(row.Id)) throw new InvalidOperationException("duplicate id");
    if (row.BaseHp <= 0) throw new InvalidOperationException("invalid hp");
}
```

统一证据契约把结构、版本和故障计划放在同一结果中。

```ts
type LeadProgramEvidence = {
  chapter: "u3ap-03-data-tables";
  runtimeVersion: string;
  sample: "baseline" | "boundary" | "failure";
  stages: readonly string[];
  firstDeviation: string | null;
  decision: "pass" | "rejected";
};

function canRelease(run: LeadProgramEvidence): boolean {
  return run.stages.length === 6 &&
    (run.sample !== "failure" || run.decision === "rejected");
}
```

证据日志至少保留下列字段，并与捕获文件使用同一运行编号。

```text
stage,name,sample,input,observation,result
1,分类数据,baseline,planned,observed,pass
2,声明 schema,baseline,planned,observed,pass
3,导出生成,baseline,planned,observed,pass
4,验证引用,baseline,planned,observed,pass
5,加载运行时,baseline,planned,observed,pass
6,切换语言复验,baseline,planned,observed,pass
```

## 两个必须主动制造的失败

<Callout type="trap" title="局部结论没有运行证据">
  运行时直接按列号和字符串读取表格，缺失字段被默认值吞掉，错误直到线上特定语言才出现。必须保存故障版本和首个偏离节点，再用相同输入验证修复。
</Callout>

<Callout type="trap" title="指标改善但系统边界退化">
  故意让一个平均指标变好，同时删除版本、长尾、内存、协议、画质或交接证据。只要“同一 schema 与源表生成确定性产物，重复主键、越界值、失效引用和缺失语言键在进入运行时前被拒绝。”不成立，即使演示成功也必须拒绝。
</Callout>

## 验收矩阵

| 维度 | 正常样本 | 边界样本 | 失败样本 | 通过条件 |
| --- | --- | --- | --- | --- |
| 连载 | 原主题逐项定位 | 相邻文章交叉 | 故意漏一项 | 无主题空洞 |
| 结构 | 依赖和状态可见 | 极限规模与阈值 | 注入错误边界 | 首偏离可定位 |
| 运行 | 固定输入可回放 | 长尾与平台差异 | 故障计划 | 结果可复现 |
| 交接 | 他人按证据复验 | 干净环境 | 删除隐含依赖 | 不变量成立 |

## 练习

<Exercises>

**问题 1** 第3章覆盖哪些原连载主题？选择三个写出因果链。

<Answer>
  本页覆盖 数据表的种类、数据表的制作方式、多语言的实现、表结构约束、版本与兼容。合格答案要沿“分类数据 → 声明 schema → 导出生成 → 验证引用 → 加载运行时 → 切换语言复验”写清上游输入、中间状态和下游证据，不能只复述标题。
</Answer>

**问题 2** 怎样制造“运行时直接按列号和字符串读取表格，缺失字段被默认值吞掉，错误直到线上特定语言才出现。”并证明修复有效？

<Answer>
  固定运行时版本、平台、数据规模和输入，只注入该故障；保存首个偏离节点。修复后用相同条件复测正常、边界和失败组，并确认门禁从拒绝恢复为通过。
</Answer>

**问题 3** 为什么“同一 schema 与源表生成确定性产物，重复主键、越界值、失效引用和缺失语言键在进入运行时前被拒绝。”是第3章的签发不变量？

<Answer>
  因为主程交付的是可演进系统，不是一次演示。缺少该不变量时，局部指标可能改善，却会把成本转移到其他模块、平台、团队或线上故障。
</Answer>

</Exercises>

## 小结

- 已覆盖 数据表的种类、数据表的制作方式、多语言的实现、表结构约束、版本与兼容，没有用自拟的热更新或 CI/CD 目录替代原连载。
- 已沿“分类数据 → 声明 schema → 导出生成 → 验证引用 → 加载运行时 → 切换语言复验”保存正常、边界、失败与修复证据。
- 已记录原写作年代与当前 Unity 的版本映射。
- 已以“同一 schema 与源表生成确定性产物，重复主键、越界值、失效引用和缺失语言键在进入运行时前被拒绝。”作为交接门。

## 术语表

<Glossary>
<GlossaryItem term="数据表的种类">区分静态配置、数值曲线、本地化文本和运行存档。</GlossaryItem>
<GlossaryItem term="数据表的制作方式">定义编辑、导出、校验、生成和加载流水线。</GlossaryItem>
<GlossaryItem term="多语言的实现">以稳定键连接文本、参数、字体与回退语言。</GlossaryItem>
<GlossaryItem term="表结构约束">对主键、类型、范围、引用和重复项执行自动校验。</GlossaryItem>
<GlossaryItem term="版本与兼容">记录 schema 与内容版本，拒绝不兼容数据。</GlossaryItem>
</Glossary>

## 来源与改编边界

<Attribution
  adaptedFrom="陆泽西（Jesse Lu）在线连载《Unity3D 高级编程之进阶主程》；现存完整文章索引由博客园保存，原作者旧站入口已失效"
  adaptedUrl="https://www.cnblogs.com/ZhYQ-Note/articles/15493562.html"
/>
