---
title: "第 3 章 Native Animation：Animation 窗口与粒子系统"
type: A
section: "Unity 游戏动画设计 · 原生动画与 Mecanim"
order: 3
description: "覆盖 Animation 窗口飞越镜头、多对象动画、动画事件，以及萤火粒子的全局、发射、渲染、速度、颜色和消失。"
demo: true
math: false
sourceUrl: "https://www.packtpub.com/en-us/product/unity-animation-essentials-9781782174813"
draft: false
---

import {
  Objectives,
  Callout,
  Attribution,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
} from "@/components/mdx/mdx-components";
import {
  Uan03NativeAnimationMapLab,
  Uan03NativeAnimationExperimentLab,
  Uan03NativeAnimationEvidenceLab,
} from "@/components/mdx/unity-animation/diagrams/uan-03-native-animation";

<Objectives>

- 能实现 Animation 窗口中的镜头飞越、多对象关键帧与事件

- 能判断 Animation Event 的目标、参数和触发次数是否正确

- 能设计粒子形状、发射、速度、颜色、渲染与生命周期实验

</Objectives>

## 问题：为什么一个镜头飞越和一群萤火粒子属于同一章

先预测：场景中的相机、门和灯能否写进同一个 Clip，并在 2.5 秒触发一次函数？可以。第三章把 Unity 内建动画创作分成两类：Animation 窗口记录任意可动画属性与事件，Particle System 按发射和生命周期规则生成大量短命实例。二者都要求设计时间、属性、作用对象和可观察结束条件。

<Uan03NativeAnimationMapLab />

## 原书边界与官方小节

官方目录包含 The Animation window - creating a fly-through、Animating multiple objects together、Invoking functions from animations；随后完整进入 Particle Systems：Starting a firefly particle system、global properties、Emitter shape and emission rate、Particle Renderer、Particle velocity、Particle color and disappearance。

- **The Animation window - creating a fly-through**
- **Animating multiple objects together；Invoking functions from animations**
- **Particle Systems；Starting a firefly particle system**
- **The Particle System's global properties；Emitter shape and emission rate**
- **Particle Renderer；Particle velocity；Particle color and disappearance**

这些小节按官方目录唯一归属。本站重新组织解释、代码与实验，不逐字复制原文；现代补充放在迁移段，不能替换或重复计算原始单元。

## 确定性曲线与统计性粒子

AnimationClip 对指定对象属性保存确定性曲线，同一时间应得到同一值；Animation Event 在曲线时间轴上派发函数。粒子系统则从 Shape 采样出生位置，按 Rate 生成粒子，通过 Start Lifetime、Speed、Color over Lifetime、Velocity 和 Renderer 描述群体统计。镜头路径关注单对象连续性，粒子关注分布、峰值存活数和透明消失。

<Uan03NativeAnimationExperimentLab />

## 五个核心术语与责任边界

<Term def="记录和编辑对象可动画属性、关键帧、曲线与事件的内建工具。">Animation 窗口</Term>
必须落到本章的输入、时间、求值器与输出链，指出正常值、边界值和最早失败信号。

<Term def="在 Clip 指定时间调用目标组件函数的时间标记。">Animation Event</Term>
必须落到本章的输入、时间、求值器与输出链，指出正常值、边界值和最早失败信号。

<Term def="粒子系统单位时间生成粒子的速率或 Burst 规则。">Emission Rate</Term>
必须落到本章的输入、时间、求值器与输出链，指出正常值、边界值和最早失败信号。

<Term def="单个粒子从出生到销毁的基础存活时间。">Start Lifetime</Term>
必须落到本章的输入、时间、求值器与输出链，指出正常值、边界值和最早失败信号。

<Term def="粒子颜色和透明度随归一化生命进度变化的模块。">Color over Lifetime</Term>
必须落到本章的输入、时间、求值器与输出链，指出正常值、边界值和最早失败信号。

术语若不能落到具体资产、时间点、对象属性和观测值，就仍停留在记忆层。读者应能为每个术语写出生产者、变化规则、消费者、正常样本和失败样本。

## 关键对象与数据流

先在 0、2、5 秒记录相机位置与旋转，使用曲线保证飞越构图连续；再给门和灯添加属性轨，证明一个 Clip 能协同多个绑定对象。于 2.5 秒插入事件并记录调用计数。粒子部分固定随机种子、发射率和寿命，预测稳态存活数约为 rate x lifetime，再调整 Shape、Velocity、Renderer 和透明曲线观察分布。

排查时从最早可控输入开始，逐段检查导入设置、中间状态和最终像素。最终画面正确但链路不符合预测，也不能立即签发，因为它可能依赖未记录的帧率、缓存、随机种子或编辑器状态。

## 三步可重复实验

动手前先预测：只改变当前步骤的一项输入后，哪个状态、骨骼、粒子、纹理或像素应最先变化？实验期间固定 Unity 版本、资产版本、帧率、场景、随机种子与目标平台。

<Stepper>
  <Step title="制作镜头飞越">
    <Uan03NativeAnimationMapLab />

在固定三点记录相机 Transform，切换曲线视图检查位置和旋转速度，保存 0%、50%、100% 构图。

```csharp
public void OnFlythroughCue() { cueCount++; Debug.Log($"cue={cueCount}"); }
```

  </Step>
  <Step title="协同对象与事件">
    <Uan03NativeAnimationExperimentLab />

给门和灯增加属性轨，在中点插入函数事件；循环播放三次，验证每轮事件次数和目标对象绑定。

```text
expected alive particles ~= emission rate x average lifetime
```

  </Step>
  <Step title="构建萤火粒子">
    <Uan03NativeAnimationEvidenceLab />

固定发射率、寿命与随机种子，依次调整 Shape、Velocity、Renderer、颜色和透明度，记录峰值粒子数与消失时间。

```csharp
var emission = particles.emission;
emission.rateOverTime = 12f;
var main = particles.main;
main.startLifetime = 2.5f;
```

  </Step>
</Stepper>

## 确定性曲线与统计性粒子要用不同证据

镜头飞越和多对象 Clip 在固定时间点应得到可重复 Transform 或属性值，因此可以抽取零秒、中点和终点做数值断言。多个对象写入同一 Clip 时，曲线路径绑定到层级和组件属性；重命名或移动对象可能让绑定失效。Animation Event 还依赖目标组件和方法签名，循环 Clip 会在每次经过事件时间时再次触发。验收要记录对象路径、事件时刻、循环次数和实际调用计数。

粒子系统不要求每个粒子位置完全相同，但应满足可预测统计。稳态最大存活数大致由每秒发射率与生命周期决定，Burst、随机范围和空间模拟会改变瞬时分布。以固定随机种子或足够样本记录出生数、峰值存活数、速度范围、颜色阶段和死亡时间，比截取一张“看起来像萤火虫”的画面更有诊断价值。

## 粒子模块排查顺序

- Main 定义持续时间、循环、起始生命周期、速度、尺寸和模拟空间。
- Shape 与 Emission 决定从哪里、以何种速率或 Burst 产生粒子。
- Velocity over Lifetime 与 Force 等模块决定出生后的运动，不要与起始速度混淆。
- Color over Lifetime 和 Size over Lifetime 使用归一化寿命，梯度端点必须可观察。
- Renderer 决定 billboard、材质、排序和拖尾，粒子存在不代表一定能被相机看见。

压力样本把发射率提高十倍并延长生命周期，验证峰值数量、过度绘制和 CPU/GPU 成本；失败样本关闭 Renderer、把颜色 alpha 设为零或使用错误模拟空间，确认诊断能区分“没有生成”和“生成但不可见”。现代 Particle System 面板布局变化不应改变这条生产链。

## 正常、边界与失败样本

每章至少保存一个正常样本、一个极端边界和一个故意破坏配置。正常样本证明主路径，边界样本说明适用范围，失败样本证明诊断方法真的能抓到错误。

<Callout type="trap">
  **因果陷阱**：Animation Event
  绑定字符串函数名，重命名或参数签名变化可能只在播放时失败，必须保存调用计数证据。
</Callout>

<Callout type="trap">
  **版本陷阱**：只提高发射率会同步增加过度绘制和存活粒子数，效果更亮不代表成本可控。
</Callout>

## 从 Unity 5 到现代 Unity

Animation 窗口、Clip 曲线和 Animation Event 仍存在；粒子系统模块名称与布局已现代化。原书的 Particle Renderer 等旧面板表述应映射到当前 Renderer、Emission、Shape、Velocity over Lifetime 与 Color over Lifetime 模块。VFX Graph 可处理更大 GPU 效果，但不是本章粒子实验的替代。

迁移记录采用五列：原书载体、稳定不变量、现代实现、不可等价处、目标平台证据。允许替换工具和 API，不允许抹去原始问题或倒写历史。

## 验收证据

验收保存相机三时刻截图、曲线切线、多个对象绑定表、事件调用次数，以及粒子固定种子下的发射率、寿命、理论与实际峰值、渲染材质和透明结束帧。关闭某一模块后应能预测最先变化的指标。

证据包同时记录场景路径、代码版本、资产 GUID、参数、复现步骤、预期和实际结果。交给另一位读者后不需要猜测隐藏状态即可重放，才算掌握而不只是看过。

## 小结

- Animation 窗口可记录镜头和多个对象的任意可动画属性
- Animation Event 把时间轴与函数调用连接起来
- 粒子效果由出生分布、生命过程和渲染共同决定
- 发射率与寿命可用于估算存活数量和性能上界

<Exercises>

**问题 1** 每秒 20 个粒子、平均寿命 3 秒的稳态存活数约多少？

<Answer>约 60 个，实际值还受 Burst、随机寿命和最大粒子数影响。</Answer>

**问题 2** 为什么镜头旋转曲线需要单独检查？

<Answer>位置平滑不保证朝向平滑，欧拉角跨界或切线可能造成突然旋转。</Answer>

**问题 3** 怎样证明事件没有重复触发？

<Answer>记录循环编号、事件时间和累计计数，让每轮预期次数可自动断言。</Answer>

</Exercises>

<Glossary>
  <GlossaryItem term="Animation 窗口">
    记录和编辑对象可动画属性、关键帧、曲线与事件的内建工具。
  </GlossaryItem>
  <GlossaryItem term="Animation Event">
    在 Clip 指定时间调用目标组件函数的时间标记。
  </GlossaryItem>
  <GlossaryItem term="Emission Rate">
    粒子系统单位时间生成粒子的速率或 Burst 规则。
  </GlossaryItem>
  <GlossaryItem term="Start Lifetime">
    单个粒子从出生到销毁的基础存活时间。
  </GlossaryItem>
  <GlossaryItem term="Color over Lifetime">
    粒子颜色和透明度随归一化生命进度变化的模块。
  </GlossaryItem>
</Glossary>

<Attribution
  adaptedFrom="Alan Thorn《Unity Animation Essentials》第一版官方目录与中文版《Unity游戏动画设计》身份资料"
  adaptedUrl="https://www.packtpub.com/en-us/product/unity-animation-essentials-9781782174813"
/>
