---
title: "第 4 章 Noncharacter Animation with Mecanim：门与按钮状态图"
type: A
section: "Unity 游戏动画设计 · 原生动画与 Mecanim"
order: 4
description: "覆盖原型场景准备、按钮与门的动画、Mecanim 入门、转换与参数、开门图和场景交互。"
demo: true
math: false
sourceUrl: "https://www.packtpub.com/en-us/product/unity-animation-essentials-9781782174813"
draft: false
---

import {
  Objectives,
  Callout,
  Attribution,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
} from "@/components/mdx/mdx-components";
import {
  Uan04NoncharacterAnimationMecanimMapLab,
  Uan04NoncharacterAnimationMecanimExperimentLab,
  Uan04NoncharacterAnimationMecanimEvidenceLab,
} from "@/components/mdx/unity-animation/diagrams/uan-04-noncharacter-animation-mecanim";

<Objectives>

- 能解释按钮、门、Clip、Animator 参数与转换组成的非角色状态图

- 能实现带进入条件、退出时间和交互反馈的开门流程

- 能复现并判断参数抖动、错误默认状态和转换竞争的边界

</Objectives>

## 问题：为什么 Mecanim 不只用于人物角色

先预测：一扇门是否需要骨骼 Avatar 才能使用 Animator？不需要。第四章故意选择按钮和门，证明 Mecanim 本质上是状态、Clip、参数和转换的通用求值图。玩家进入触发区、按下按钮、参数变化、按钮下压、门打开，每一段都能单独观测并验证。

<Uan04NoncharacterAnimationMecanimMapLab />

## 原书边界与官方小节

官方目录依次为 Preparing a scene with the prototyping assets、Creating animations for the button and door、Getting started with Mecanim、Mecanim transitions and parameters、Creating a door-open Mecanim graph、Creating scene interactions。重点是非角色对象，而非角色移动 Blend Tree。

- **Preparing a scene with the prototyping assets**
- **Creating animations for the button and door**
- **Getting started with Mecanim**
- **Mecanim transitions and parameters**
- **Creating a door-open Mecanim graph；Creating scene interactions**

这些小节按官方目录唯一归属。本站重新组织解释、代码与实验，不逐字复制原文；现代补充放在迁移段，不能替换或重复计算原始单元。

## 交互输入到状态图输出

门系统分为传感、决策和表现：Trigger Collider 只判断玩家是否在范围，输入处理产生 Press 事件，控制器设置 Animator 参数，状态图从 Closed 转到 Opening 再到 Open，Clip 写入门和按钮的 Transform。碰撞体与导航阻挡应在明确状态切换，而不是依赖画面看起来已经打开。Trigger 适合一次命令，Bool 适合持续开闭状态。

<Uan04NoncharacterAnimationMecanimExperimentLab />

## 五个核心术语与责任边界

<Term def="保存状态、Motion、参数与转换关系的动画图资产。">
  Animator Controller
</Term>
必须落到本章的输入、时间、求值器与输出链，指出正常值、边界值和最早失败信号。

<Term def="图中绑定 Clip 或 Blend Tree、表示当前动画行为的节点。">State</Term>
必须落到本章的输入、时间、求值器与输出链，指出正常值、边界值和最早失败信号。

<Term def="依据条件、退出时间和持续时间从一个状态混合到另一个状态的边。">
  Transition
</Term>
必须落到本章的输入、时间、求值器与输出链，指出正常值、边界值和最早失败信号。

<Term def="由代码或图设置、供转换条件读取的 Float、Int、Bool 或 Trigger。">
  Parameter
</Term>
必须落到本章的输入、时间、求值器与输出链，指出正常值、边界值和最早失败信号。

<Term def="门、按钮、机关等不依赖 humanoid 角色但仍由状态图驱动的动画。">
  非角色动画
</Term>
必须落到本章的输入、时间、求值器与输出链，指出正常值、边界值和最早失败信号。

术语若不能落到具体资产、时间点、对象属性和观测值，就仍停留在记忆层。读者应能为每个术语写出生产者、变化规则、消费者、正常样本和失败样本。

## 关键对象与数据流

场景先放置门、按钮、玩家触发区和阻挡碰撞体，分别制作 Closed/Open 与 Released/Pressed Clip。Animator 图只表达视觉状态，交互脚本负责输入资格和参数写入。测试记录玩家进入、按键、参数、当前状态哈希、normalizedTime、门角度和碰撞体启用状态；再故意缺少参数或反转转换条件，确认诊断能定位图而不是误判输入。

排查时从最早可控输入开始，逐段检查导入设置、中间状态和最终像素。最终画面正确但链路不符合预测，也不能立即签发，因为它可能依赖未记录的帧率、缓存、随机种子或编辑器状态。

## 三步可重复实验

动手前先预测：只改变当前步骤的一项输入后，哪个状态、骨骼、粒子、纹理或像素应最先变化？实验期间固定 Unity 版本、资产版本、帧率、场景、随机种子与目标平台。

<Stepper>
  <Step title="准备场景契约">
    <Uan04NoncharacterAnimationMecanimMapLab />

固定门轴心、按钮局部坐标、触发层和碰撞体职责；在无动画时先验证交互范围和阻挡。

```csharp
if (playerInRange && Input.GetButtonDown("Interact")) animator.SetTrigger("Open");
```

  </Step>
  <Step title="创建 Clip 与图">
    <Uan04NoncharacterAnimationMecanimExperimentLab />

分别制作按钮和门的端点 Clip，建立 Closed、Opening、Open 状态及 Trigger 条件，记录转换持续时间。

```csharp
var state = animator.GetCurrentAnimatorStateInfo(0);
Debug.Log($"state={state.shortNameHash}, t={state.normalizedTime:F2}");
```

  </Step>
  <Step title="验证场景交互">
    <Uan04NoncharacterAnimationMecanimEvidenceLab />

按输入链逐段记录，测试范围外按键、重复按键、动画中离开与参数缺失，确认视觉和碰撞状态一致。

```text
trigger volume -> input -> parameter -> transition -> clip -> door pose -> collider policy
```

  </Step>
</Stepper>

## 非角色对象同样需要状态契约

门的 Closed、Opening、Open、Closing 不是四段动画名称，而是四种可观察状态。按钮或触发器只产生请求，Animator 参数表达请求或事实，转换定义何时接受，Clip 才负责属性随时间变化。若交互脚本直接播放任意状态，逻辑会绕过转换条件；若用 bool 同时表达“请求开门”和“门已经打开”，中途取消和反向播放会变得含糊。先写状态表，再选择 Trigger、bool 或 float 参数。

Has Exit Time 让转换等待当前状态达到指定归一化时间，适合必须播完的动作；即时交互则可能需要关闭它。Transition Duration 会混合两个状态的属性，门位置和按钮颜色都可能在混合期被双重求值。Any State 方便全局响应，却可能与局部转换竞争。验收在每个输入时刻记录当前状态哈希、normalizedTime、参数值和正在进行的转换，才能解释为什么门没有立即响应。

## 场景交互故障矩阵

- 默认状态错误：进入场景时门从 Open 播回 Closed，检查 Entry 和对象初始属性。
- 参数未写入：交互发生但 Animator 参数不变，检查对象引用与参数名称。
- 条件永不成立：参数正确但转换无效，检查条件、退出时间和层权重。
- 重复触发：按钮停留在触发范围内每帧写入，检查边沿事件与防抖。
- 动画结束后碰撞仍关闭：视觉状态与物理状态没有同一状态所有者。

现代 Animator 仍保留这套模型。Timeline 可编排一次性镜头，却不应替代需要交互和长期状态的门图。签发场景依次测试快速连按、动画中反向请求、两个按钮竞争和场景重载，证明状态图在边界输入下仍有唯一解释。

若门的动画同时修改 Transform 而导航或物理系统也写同一对象，视觉状态会与碰撞和寻路分离。更稳妥的结构让 Animator 驱动明确的可动画子节点，门逻辑拥有状态与交互，碰撞体和 NavMesh 障碍在状态边界同步更新。验收记录画面、碰撞查询和导航可达性，防止“看起来打开但仍挡路”的跨系统错误。

## 正常、边界与失败样本

每章至少保存一个正常样本、一个极端边界和一个故意破坏配置。正常样本证明主路径，边界样本说明适用范围，失败样本证明诊断方法真的能抓到错误。

<Callout type="trap">
  **因果陷阱**：用 Animator
  状态代替交互资格会把触发范围、权限和输入时序混入表现层，难以诊断和测试。
</Callout>

<Callout type="trap">
  **版本陷阱**：门看似打开但阻挡碰撞体仍启用会造成视觉与游戏规则分裂，必须以明确状态同步。
</Callout>

## 从 Unity 5 到现代 Unity

Mecanim 在现代文档中通常称 Animator 或 Unity Animation system，但 Controller、State、Transition 和 Parameter 模型仍有效。现代输入系统可替换旧 Input 调用，StateMachineBehaviour 或事件可表达状态边界。PlayableGraph 是更底层补充，不应掩盖本章非角色状态图的基本责任。

迁移记录采用五列：原书载体、稳定不变量、现代实现、不可等价处、目标平台证据。允许替换工具和 API，不允许抹去原始问题或倒写历史。

## 验收证据

验收包含场景对象清单、四个端点姿态、Animator 图截图、参数与转换表、一次完整交互时间线，以及范围外、重复输入、缺参数和碰撞不同步样本。另一个读者应能仅凭证据重放门的每个状态。

证据包同时记录场景路径、代码版本、资产 GUID、参数、复现步骤、预期和实际结果。交给另一位读者后不需要猜测隐藏状态即可重放，才算掌握而不只是看过。

## 小结

- Animator 图同样适合门、按钮和机关等非角色对象
- 交互资格、参数写入、状态转换和属性输出应分层观察
- Trigger 表达一次命令，Bool 表达持续状态
- 视觉姿态与碰撞规则必须在明确状态保持一致

<Exercises>

**问题 1** 门为什么不需要 Avatar？

<Answer>
  它只播放普通 Transform 属性 Clip，Animator Controller 不要求所有对象都是
  humanoid。
</Answer>

**问题 2** Open 命令适合 Trigger 还是 Bool？

<Answer>
  一次开门命令通常适合 Trigger；若门需要持续跟随开关状态，则 Bool 更合适。
</Answer>

**问题 3** 怎样定位按键有效但门不动？

<Answer>
  依次检查交互日志、参数值、当前状态、转换条件、Clip
  绑定和门轴心，不跳过中间链。
</Answer>

</Exercises>

<Glossary>
  <GlossaryItem term="Animator Controller">
    保存状态、Motion、参数与转换关系的动画图资产。
  </GlossaryItem>
  <GlossaryItem term="State">
    图中绑定 Clip 或 Blend Tree、表示当前动画行为的节点。
  </GlossaryItem>
  <GlossaryItem term="Transition">
    依据条件、退出时间和持续时间从一个状态混合到另一个状态的边。
  </GlossaryItem>
  <GlossaryItem term="Parameter">
    由代码或图设置、供转换条件读取的 Float、Int、Bool 或 Trigger。
  </GlossaryItem>
  <GlossaryItem term="非角色动画">
    门、按钮、机关等不依赖 humanoid 角色但仍由状态图驱动的动画。
  </GlossaryItem>
</Glossary>

<Attribution
  adaptedFrom="Alan Thorn《Unity Animation Essentials》第一版官方目录与中文版《Unity游戏动画设计》身份资料"
  adaptedUrl="https://www.packtpub.com/en-us/product/unity-animation-essentials-9781782174813"
/>
