---
title: "第8章 酷跑类游戏——小狗快跑：碰撞监视器与道具链"
type: A
section: "Unity 游戏案例开发大全 · 酷跑与停车"
order: 8
description: "复现主菜单、游戏、教程和加载场景，以及小狗、金币、磁铁、圆球和碰撞监视器。"
demo: true
math: false
sourceUrl: "https://e.dangdang.com/products/1900670411.html"
draft: true
---

import {
  Objectives,
  Callout,
  Attribution,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第8章在原书11章结构中的场景、资源与玩法责任

- 能实现“小狗、跑道、碰撞监视器”之间可观察、可回放的数据链

- 能比较原书旧版Unity实现与当前Unity迁移方案，并设计失败注入证据

</Objectives>

## 问题：怎样证明自己复现的是第8章，而不是只做了一个同类型演示？

先预测：如果删掉原章名称、场景清单和关键对象，只留下一个能运行的Unity场景，另一位读者还能从输入、状态和结果反推出本章吗？不能。小狗快跑以持续前进、变道或跳跃、障碍碰撞、金币和限时道具形成快速循环。教程场景与正式场景复用同一动作规则，加载场景负责资源与状态过渡而非伪造进度。

## 原书边界与目录单元

本页严格对应2015年版目录中的以下单元；它们共同决定本页覆盖范围，现代补充不能替代原书位置：

- **背景以及功能概述**
- **游戏的策划及准备工作**
- **游戏的架构**
- **主菜单场景**
- **游戏场景**
- **游戏教程场景**
- **加载场景**
- **游戏的优化与改进**

全书身份由天珑书店与图书馆题录交叉核对，章节边界采用当当电子书目录。本站重新组织讲解、代码、实验和证据，不逐字复制原文。导读与总复习用于串联和验收，不重复计入11个正文章。

## 案例目标、场景与权威状态

复现先从案例合同开始。合同写明玩家目标、入口场景、玩法场景、退出条件、持久数据和目标设备；之后再分配GameObject和脚本。这样可以防止“画面相似”掩盖规则不同，也能防止菜单、玩法与结算同时拥有同一份可变状态。

小狗快跑以持续前进、变道或跳跃、障碍碰撞、金币和限时道具形成快速循环。教程场景与正式场景复用同一动作规则，加载场景负责资源与状态过渡而非伪造进度。

输入驱动小狗动作，碰撞监视器把障碍和收集事件写入回合状态，磁铁扩大金币吸附范围，圆球改变临时能力，静态数据只承载跨场景分数和选择。

本章的关键链依次经过小狗、跑道、碰撞监视器、金币、磁铁、圆球、教程加载。每个节点都要写出输入来源、状态所有者、更新时机、输出消费者和错误策略。UI只呈现或采集意图，物理、AI、回合与结算由各自权威对象决定；场景切换传递稳定ID和不可变快照，不传递失效组件引用。

## 五个核心术语与责任边界

<Term def="把特定碰撞区域转换为玩法事件的组件。">碰撞监视器</Term>
它必须落到第8章的输入、状态、输出和失败信号，不能只作为名词记忆。

<Term def="在有限时间内吸引一定范围金币的能力。">磁铁道具</Term>
它必须落到第8章的输入、状态、输出和失败信号，不能只作为名词记忆。

<Term def="改变角色临时状态并具有明确到期清理的道具。">圆球道具</Term>
它必须落到第8章的输入、状态、输出和失败信号，不能只作为名词记忆。

<Term def="使用正式规则但按步骤限制输入的教学环境。">教程场景</Term>
它必须落到第8章的输入、状态、输出和失败信号，不能只作为名词记忆。

<Term def="协调异步资源和下一场景激活的过渡场景。">加载场景</Term>
它必须落到第8章的输入、状态、输出和失败信号，不能只作为名词记忆。

把术语连成领域模型后，再为每个对象记录生命周期。创建发生在哪个场景，重开关卡时由谁复位，切到菜单时由谁释放，暂停与结算时是否继续推进，都要通过日志和断言回答。仅靠Inspector截图无法证明所有权正确。

## 从输入到结算的数据流

输入层把触摸、键盘、虚拟按钮或设备传感器转换为稳定意图，玩法层在固定更新点消费意图，物理与AI产生事件，回合状态归并事件，界面读取状态，结算冻结结果。输入驱动小狗动作，碰撞监视器把障碍和收集事件写入回合状态，磁铁扩大金币吸附范围，圆球改变临时能力，静态数据只承载跨场景分数和选择。

每条事件至少携带案例ID、场景ID、回合ID、帧号和来源对象。这样才能区分同一按钮的重复触发、场景重载后的旧回调、物理帧和渲染帧顺序差异，以及跨场景静态变量残留。最终画面正确但中间契约错误，仍不能签发。

## 正常、边界与失败样本

正常样本覆盖小狗与跑道的主路径；边界样本使用零输入、最大输入、最后一次资源、临界速度或临界时间；失败样本主动破坏教程加载之前的一个依赖。三组样本必须走同一记录器，避免成功时有证据、失败时只凭肉眼判断。

故障注入从最小变化开始：缺一个场景引用、重复一次输入、让一个对象越界、把回合ID改旧，或者在结算后继续发送命令。观察首个偏离点，而不是只看最后报错。修复后重放完全相同的输入，首个偏离应消失，其他正常证据保持不变。

## 两个常见因果陷阱

<Callout type="trap">
  **身份陷阱**：把第8章改写成通用游戏类型，会丢失原书的具名场景、对象和玩法链，即使页面很长也不算覆盖。
</Callout>

<Callout type="trap">
  **状态陷阱**：让界面、静态变量和场景对象分别修改结果，会在重开、暂停或切场景后产生重复结算与残留状态。
</Callout>

## 从旧版Unity迁移到当前Unity

原书出版于2015年，案例可能依赖旧版Unity、NGUI、Legacy Input Manager、Input.acceleration、旧资源导入设置和早期移动端SDK。迁移先冻结原项目副本和可运行结果，再逐项替换接口。uGUI或UI Toolkit可以替代NGUI，Input System可以适配触摸和体感，Addressables可以替代部分直接资源路径，但场景顺序、玩家目标和胜负契约必须保持。

旧物理参数不能只按字段名搬运。需要固定时间步、质量、摩擦、碰撞检测和求解器设置，以同一输入比较位置、速度、触发顺序和稳定状态。旧Shader迁到Built-in、URP或其他管线时，要保存材质外观、透明排序、光照条件和目标设备帧时间，不能以“成功编译”代替视觉等价。

迁移账本使用五列：原书载体、稳定不变量、当前实现、不可等价处、重放证据。API变化必须有明确适配边界，无法等价的设备能力要写成限制并提供替代输入；不得用现代术语删除背景以及功能概述或把编辑器通过当成真机通过。

## 性能、资源与发布门

优化从测量开始。为本章建立CPU主线程、渲染线程、物理、GC、Draw Call、显存与加载时间基线，再根据首个瓶颈选择对象池、批处理、资源压缩、碰撞层裁剪或异步加载。只写“使用对象池”不构成证据，必须给出改造前后同一回放的对照。

资源清单应能从小狗追到教程加载，并标出场景持有、共享缓存和回收时机。连续重开十次后，场景对象数、事件监听数与内存应回到稳定区间；后台再进入前台、分辨率变化和输入中断也不能破坏当前回合。

## 验收证据

最低证据包包含原目录映射、场景谱系、关键Prefab和脚本责任表、正常边界失败三组录像或日志、小狗、跑道、碰撞监视器、金币、磁铁、圆球、教程加载的状态快照、迁移账本、Profiler对照和目标设备构建信息。证据要让另一位读者不猜隐藏状态即可复现。

目录覆盖回答“有没有漏章”，状态日志回答“链路是否正确”，失败注入回答“诊断是否可信”，设备数据回答“发布是否成立”。四类证据缺一项都只能标记为学习草稿，不能标记为本章通过。

## 小结

- 保留第8章的原目录、具名场景与玩法身份
- 让小狗、跑道、碰撞监视器各有唯一责任和可观察状态
- 用正常、边界、失败三组同源回放定位首个偏离
- 现代接口可以替换，案例目标、规则与证据不能丢失

<Exercises>

**问题 1** 第8章的关键输入和输出分别是什么？

<Answer>
  输入驱动小狗动作，碰撞监视器把障碍和收集事件写入回合状态，磁铁扩大金币吸附范围，圆球改变临时能力，静态数据只承载跨场景分数和选择。
</Answer>

**问题 2** 本页最有诊断价值的故障注入是什么？

<Answer>
  故意破坏“碰撞监视器”对应的首个输入或门禁，保存小狗、跑道、碰撞监视器、金币、磁铁、圆球、教程加载中第一条偏离信号；修复后用完全相同输入重放。
</Answer>

**问题 3** 迁移到当前Unity时必须保留什么？

<Answer>
  必须保留原章的场景、玩法目标、状态契约和可观察结果；NGUI、旧输入接口、资源API与渲染管线可以在迁移账本中替换。
</Answer>

</Exercises>

<Glossary>
  <GlossaryItem term="碰撞监视器">
    把特定碰撞区域转换为玩法事件的组件。
  </GlossaryItem>
  <GlossaryItem term="磁铁道具">
    在有限时间内吸引一定范围金币的能力。
  </GlossaryItem>
  <GlossaryItem term="圆球道具">
    改变角色临时状态并具有明确到期清理的道具。
  </GlossaryItem>
  <GlossaryItem term="教程场景">
    使用正式规则但按步骤限制输入的教学环境。
  </GlossaryItem>
  <GlossaryItem term="加载场景">
    协调异步资源和下一场景激活的过渡场景。
  </GlossaryItem>
</Glossary>

<Attribution
  adaptedFrom="吴亚峰、杜化美、于复兴《Unity游戏案例开发大全》目录与书籍身份资料"
  adaptedUrl="https://e.dangdang.com/products/1900670411.html"
/>
