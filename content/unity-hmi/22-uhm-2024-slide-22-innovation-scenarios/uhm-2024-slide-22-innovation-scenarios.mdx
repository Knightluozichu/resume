---
title: "第22页 六大HMI创新场景"
type: C
section: "Unity for HMI（Unity中国UNITE 2024）"
order: 22
description: "场景矩阵覆盖3D车模、3D座舱、地图导航、智能驾驶、OS创新与跨域创新，每一项都要回到数据源、显示设备和安全等级。 覆盖60个正式节点，并用平台矩阵、预算实验和目标机证据复核。"
demo: true
math: true
sourceUrl: "https://learn-private.cdn.u3d.cn/attachment/3428ceb8-cee1-4f76-91c9-8780105be137______________Unity__3D_________.pdf"
draft: false
---

import {
  Uhm24Slide22InnovationScenariosMapLab,
  Uhm24Slide22InnovationScenariosExperimentLab,
  Uhm24Slide22InnovationScenariosEvidenceLab,
} from "@/components/mdx/unity-hmi/diagrams/uhm-2024-slide-22-innovation-scenarios";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第22页 六大HMI创新场景在23页官方演讲中的位置与证据边界
- 能绘制“选择场景域 → 登记信号与资产 → 定义交互反馈 → 分离安全内容 → 目标车回归”的数据、渲染、平台和责任链
- 能计算固定SoC、OS、显示拓扑与场景下的帧预算和资源峰值
- 能判断正常、压力、故障和降级四类实验应给出量产通过还是退回结论

</Objectives>

## 为什么这页必须逐项核对

场景矩阵覆盖3D车模、3D座舱、地图导航、智能驾驶、OS创新与跨域创新，每一项都要回到数据源、显示设备和安全等级。 本课程采用Unity中国肖蓓蓓《Unity for HMI：未来已来，Unity开启3D座舱新篇章》，UNITE 2024演讲稿，2024年8月1日生成，23页。PDF元数据显示材料于2024年8月1日生成；它是23页演讲稿，不是带ISBN的传统图书。课程按23页逐页建立正式单元，另加学习地图与综合验收，共25页。

演讲把采用规模、平台适配、车机运行时、URAS架构、组织能力与创新场景放在同一叙事中。工程阅读必须把三类陈述拆开：2024时点的事实快照、产品宣称的支持范围、需要项目自行证明的量产能力。前两者可以从官方材料核对，第三类只能由目标硬件、目标OS和代表性车载场景的实验给出。

## 五个贯穿全材料的术语

<Term def="固定芯片、BSP、操作系统、驱动、Unity运行时和显示拓扑的可复现版本集合。">
  平台基线
</Term>
、
<Term def="把CPU、GPU、内存、带宽、三角面、Draw Call和长尾帧时分配到场景的上限。">
  量产预算
</Term>
、
<Term def="从车辆信号、应用View到合成显示的所有权、更新频率和失败规则。">
  显示合同
</Term>
、
<Term def="URAS等共享渲染能力对客户端注册、调度、资源和故障隔离的承诺。">
  服务边界
</Term>
、
<Term def="能由另一位工程师在同一基线复现结论的日志、捕获、指标和差异记录。">
  验收证据
</Term>
。

平台基线阻止“支持某OS”被误解为所有组合都可用；量产预算把视觉目标放回有限SoC；显示合同保护信号和安全内容；服务边界控制多应用共享；验收证据负责关闭从演讲到量产实现之间的推理缺口。

## 四个可复算预算

帧时预算保留主线程、渲染线程、GPU与合成等待：

$$
T_{frame}=\max(T_{main},T_{render},T_{gpu})+T_{compose}+T_{wait}
$$

资源余量必须按峰值而非平均值计算：

$$
H_{memory}=M_{limit}-\max(M_{rss}+M_{gpu}+M_{shared})
$$

三角面只是GPU预算的一项约束：

$$
N_{tri}\leq \frac{B_{gpu}-T_{pixel}-T_{post}}{C_{vertex}}
$$

车辆信号端到端延迟包含采集、传输、绑定、渲染和合成：

$$
L_{signal}=T_{capture}+T_{transport}+T_{bind}+T_{render}+T_{compose}
$$

每个公式都要带单位、测量窗口与目标机。8155和30帧是演讲示例，不是所有项目的默认门槛；分辨率、材质、屏幕数量和视觉内容变化后必须重测。

## 三步交互实验

<Stepper>
  <Step title="先建立平台与显示路径">
    <Uhm24Slide22InnovationScenariosMapLab />
  </Step>
  <Step title="再改变场景和资源预算">
    <Uhm24Slide22InnovationScenariosExperimentLab />
  </Step>
  <Step title="最后注入客户端与平台故障">
    <Uhm24Slide22InnovationScenariosEvidenceLab />
  </Step>
</Stepper>

### 3D车模

**正式节点 1/60。** 实现“3D车模”前先登记车辆信号来源、更新频率、单位、无效值、显示设备、交互负荷和安全等级。再用录制信号、超时、跳变、断连、昼夜与多语言场景验证视觉反馈不会掩盖关键驾驶信息。

本节点要形成可复核工件：版本坐标、平台或场景参数、关键指标、失败注入、预期结果与实际结果。若只有截图而没有输入和目标机条件，或只有平均帧率而没有长尾与资源峰值，都不能判定完成。

### 转场特效

**正式节点 2/60。** “转场特效”是第22页 六大HMI创新场景的第2个正式节点。学习时把演讲陈述改写为输入、处理、输出、资源和失败合同，保存一个正常路径与一个反例；不能从营销标题直接推导量产能力。

本节点要形成可复核工件：版本坐标、平台或场景参数、关键指标、失败注入、预期结果与实际结果。若只有截图而没有输入和目标机条件，或只有平均帧率而没有长尾与资源峰值，都不能判定完成。

### 车模调节

**正式节点 3/60。** 实现“车模调节”前先登记车辆信号来源、更新频率、单位、无效值、显示设备、交互负荷和安全等级。再用录制信号、超时、跳变、断连、昼夜与多语言场景验证视觉反馈不会掩盖关键驾驶信息。

本节点要形成可复核工件：版本坐标、平台或场景参数、关键指标、失败注入、预期结果与实际结果。若只有截图而没有输入和目标机条件，或只有平均帧率而没有长尾与资源峰值，都不能判定完成。

### 车辆设置

**正式节点 4/60。** 实现“车辆设置”前先登记车辆信号来源、更新频率、单位、无效值、显示设备、交互负荷和安全等级。再用录制信号、超时、跳变、断连、昼夜与多语言场景验证视觉反馈不会掩盖关键驾驶信息。

本节点要形成可复核工件：版本坐标、平台或场景参数、关键指标、失败注入、预期结果与实际结果。若只有截图而没有输入和目标机条件，或只有平均帧率而没有长尾与资源峰值，都不能判定完成。

### 个性化涂装

**正式节点 5/60。** “个性化涂装”是第22页 六大HMI创新场景的第5个正式节点。学习时把演讲陈述改写为输入、处理、输出、资源和失败合同，保存一个正常路径与一个反例；不能从营销标题直接推导量产能力。

本节点要形成可复核工件：版本坐标、平台或场景参数、关键指标、失败注入、预期结果与实际结果。若只有截图而没有输入和目标机条件，或只有平均帧率而没有长尾与资源峰值，都不能判定完成。

### 车轮状态展示

**正式节点 6/60。** 实现“车轮状态展示”前先登记车辆信号来源、更新频率、单位、无效值、显示设备、交互负荷和安全等级。再用录制信号、超时、跳变、断连、昼夜与多语言场景验证视觉反馈不会掩盖关键驾驶信息。

本节点要形成可复核工件：版本坐标、平台或场景参数、关键指标、失败注入、预期结果与实际结果。若只有截图而没有输入和目标机条件，或只有平均帧率而没有长尾与资源峰值，都不能判定完成。

### 车辆异常预警

**正式节点 7/60。** 实现“车辆异常预警”前先登记车辆信号来源、更新频率、单位、无效值、显示设备、交互负荷和安全等级。再用录制信号、超时、跳变、断连、昼夜与多语言场景验证视觉反馈不会掩盖关键驾驶信息。

本节点要形成可复核工件：版本坐标、平台或场景参数、关键指标、失败注入、预期结果与实际结果。若只有截图而没有输入和目标机条件，或只有平均帧率而没有长尾与资源峰值，都不能判定完成。

### 充电状态展示

**正式节点 8/60。** “充电状态展示”是第22页 六大HMI创新场景的第8个正式节点。学习时把演讲陈述改写为输入、处理、输出、资源和失败合同，保存一个正常路径与一个反例；不能从营销标题直接推导量产能力。

本节点要形成可复核工件：版本坐标、平台或场景参数、关键指标、失败注入、预期结果与实际结果。若只有截图而没有输入和目标机条件，或只有平均帧率而没有长尾与资源峰值，都不能判定完成。

### 驾驶模式切换

**正式节点 9/60。** 实现“驾驶模式切换”前先登记车辆信号来源、更新频率、单位、无效值、显示设备、交互负荷和安全等级。再用录制信号、超时、跳变、断连、昼夜与多语言场景验证视觉反馈不会掩盖关键驾驶信息。

本节点要形成可复核工件：版本坐标、平台或场景参数、关键指标、失败注入、预期结果与实际结果。若只有截图而没有输入和目标机条件，或只有平均帧率而没有长尾与资源峰值，都不能判定完成。

### 配置自动适配

**正式节点 10/60。** “配置自动适配”是第22页 六大HMI创新场景的第10个正式节点。学习时把演讲陈述改写为输入、处理、输出、资源和失败合同，保存一个正常路径与一个反例；不能从营销标题直接推导量产能力。

本节点要形成可复核工件：版本坐标、平台或场景参数、关键指标、失败注入、预期结果与实际结果。若只有截图而没有输入和目标机条件，或只有平均帧率而没有长尾与资源峰值，都不能判定完成。

### 3D座舱

**正式节点 11/60。** 实现“3D座舱”前先登记车辆信号来源、更新频率、单位、无效值、显示设备、交互负荷和安全等级。再用录制信号、超时、跳变、断连、昼夜与多语言场景验证视觉反馈不会掩盖关键驾驶信息。

本节点要形成可复核工件：版本坐标、平台或场景参数、关键指标、失败注入、预期结果与实际结果。若只有截图而没有输入和目标机条件，或只有平均帧率而没有长尾与资源峰值，都不能判定完成。

### 一镜到底

**正式节点 12/60。** “一镜到底”是第22页 六大HMI创新场景的第12个正式节点。学习时把演讲陈述改写为输入、处理、输出、资源和失败合同，保存一个正常路径与一个反例；不能从营销标题直接推导量产能力。

本节点要形成可复核工件：版本坐标、平台或场景参数、关键指标、失败注入、预期结果与实际结果。若只有截图而没有输入和目标机条件，或只有平均帧率而没有长尾与资源峰值，都不能判定完成。

### 香氛特效

**正式节点 13/60。** “香氛特效”是第22页 六大HMI创新场景的第13个正式节点。学习时把演讲陈述改写为输入、处理、输出、资源和失败合同，保存一个正常路径与一个反例；不能从营销标题直接推导量产能力。

本节点要形成可复核工件：版本坐标、平台或场景参数、关键指标、失败注入、预期结果与实际结果。若只有截图而没有输入和目标机条件，或只有平均帧率而没有长尾与资源峰值，都不能判定完成。

### 按摩特效

**正式节点 14/60。** “按摩特效”是第22页 六大HMI创新场景的第14个正式节点。学习时把演讲陈述改写为输入、处理、输出、资源和失败合同，保存一个正常路径与一个反例；不能从营销标题直接推导量产能力。

本节点要形成可复核工件：版本坐标、平台或场景参数、关键指标、失败注入、预期结果与实际结果。若只有截图而没有输入和目标机条件，或只有平均帧率而没有长尾与资源峰值，都不能判定完成。

### 座椅维度控制

**正式节点 15/60。** 实现“座椅维度控制”前先登记车辆信号来源、更新频率、单位、无效值、显示设备、交互负荷和安全等级。再用录制信号、超时、跳变、断连、昼夜与多语言场景验证视觉反馈不会掩盖关键驾驶信息。

本节点要形成可复核工件：版本坐标、平台或场景参数、关键指标、失败注入、预期结果与实际结果。若只有截图而没有输入和目标机条件，或只有平均帧率而没有长尾与资源峰值，都不能判定完成。

### 空调特效展示

**正式节点 16/60。** 实现“空调特效展示”前先登记车辆信号来源、更新频率、单位、无效值、显示设备、交互负荷和安全等级。再用录制信号、超时、跳变、断连、昼夜与多语言场景验证视觉反馈不会掩盖关键驾驶信息。

本节点要形成可复核工件：版本坐标、平台或场景参数、关键指标、失败注入、预期结果与实际结果。若只有截图而没有输入和目标机条件，或只有平均帧率而没有长尾与资源峰值，都不能判定完成。

### 车辆健康展示

**正式节点 17/60。** 实现“车辆健康展示”前先登记车辆信号来源、更新频率、单位、无效值、显示设备、交互负荷和安全等级。再用录制信号、超时、跳变、断连、昼夜与多语言场景验证视觉反馈不会掩盖关键驾驶信息。

本节点要形成可复核工件：版本坐标、平台或场景参数、关键指标、失败注入、预期结果与实际结果。若只有截图而没有输入和目标机条件，或只有平均帧率而没有长尾与资源峰值，都不能判定完成。

### 车窗状态展示

**正式节点 18/60。** 实现“车窗状态展示”前先登记车辆信号来源、更新频率、单位、无效值、显示设备、交互负荷和安全等级。再用录制信号、超时、跳变、断连、昼夜与多语言场景验证视觉反馈不会掩盖关键驾驶信息。

本节点要形成可复核工件：版本坐标、平台或场景参数、关键指标、失败注入、预期结果与实际结果。若只有截图而没有输入和目标机条件，或只有平均帧率而没有长尾与资源峰值，都不能判定完成。

### 灯光状态展示

**正式节点 19/60。** 实现“灯光状态展示”前先登记车辆信号来源、更新频率、单位、无效值、显示设备、交互负荷和安全等级。再用录制信号、超时、跳变、断连、昼夜与多语言场景验证视觉反馈不会掩盖关键驾驶信息。

本节点要形成可复核工件：版本坐标、平台或场景参数、关键指标、失败注入、预期结果与实际结果。若只有截图而没有输入和目标机条件，或只有平均帧率而没有长尾与资源峰值，都不能判定完成。

### 电动出风口调节

**正式节点 20/60。** “电动出风口调节”是第22页 六大HMI创新场景的第20个正式节点。学习时把演讲陈述改写为输入、处理、输出、资源和失败合同，保存一个正常路径与一个反例；不能从营销标题直接推导量产能力。

本节点要形成可复核工件：版本坐标、平台或场景参数、关键指标、失败注入、预期结果与实际结果。若只有截图而没有输入和目标机条件，或只有平均帧率而没有长尾与资源峰值，都不能判定完成。

### 地图导航

**正式节点 21/60。** 接入“地图导航”时，除画面外还要验收SDK版本、数据许可、离线缓存、坐标系、主题切换、音频焦点、启动顺序和降级策略。第三方不可用时，安全相关信息仍需由受控通道可靠显示。

本节点要形成可复核工件：版本坐标、平台或场景参数、关键指标、失败注入、预期结果与实际结果。若只有截图而没有输入和目标机条件，或只有平均帧率而没有长尾与资源峰值，都不能判定完成。

### 节日运营

**正式节点 22/60。** “节日运营”是第22页 六大HMI创新场景的第22个正式节点。学习时把演讲陈述改写为输入、处理、输出、资源和失败合同，保存一个正常路径与一个反例；不能从营销标题直接推导量产能力。

本节点要形成可复核工件：版本坐标、平台或场景参数、关键指标、失败注入、预期结果与实际结果。若只有截图而没有输入和目标机条件，或只有平均帧率而没有长尾与资源峰值，都不能判定完成。

### POI定制

**正式节点 23/60。** 实现“POI定制”前先登记车辆信号来源、更新频率、单位、无效值、显示设备、交互负荷和安全等级。再用录制信号、超时、跳变、断连、昼夜与多语言场景验证视觉反馈不会掩盖关键驾驶信息。

本节点要形成可复核工件：版本坐标、平台或场景参数、关键指标、失败注入、预期结果与实际结果。若只有截图而没有输入和目标机条件，或只有平均帧率而没有长尾与资源峰值，都不能判定完成。

### 组队出行

**正式节点 24/60。** “组队出行”是第22页 六大HMI创新场景的第24个正式节点。学习时把演讲陈述改写为输入、处理、输出、资源和失败合同，保存一个正常路径与一个反例；不能从营销标题直接推导量产能力。

本节点要形成可复核工件：版本坐标、平台或场景参数、关键指标、失败注入、预期结果与实际结果。若只有截图而没有输入和目标机条件，或只有平均帧率而没有长尾与资源峰值，都不能判定完成。

### 3D地图定制

**正式节点 25/60。** 接入“3D地图定制”时，除画面外还要验收SDK版本、数据许可、离线缓存、坐标系、主题切换、音频焦点、启动顺序和降级策略。第三方不可用时，安全相关信息仍需由受控通道可靠显示。

本节点要形成可复核工件：版本坐标、平台或场景参数、关键指标、失败注入、预期结果与实际结果。若只有截图而没有输入和目标机条件，或只有平均帧率而没有长尾与资源峰值，都不能判定完成。

### 环境数字孪生

**正式节点 26/60。** “环境数字孪生”是第22页 六大HMI创新场景的第26个正式节点。学习时把演讲陈述改写为输入、处理、输出、资源和失败合同，保存一个正常路径与一个反例；不能从营销标题直接推导量产能力。

本节点要形成可复核工件：版本坐标、平台或场景参数、关键指标、失败注入、预期结果与实际结果。若只有截图而没有输入和目标机条件，或只有平均帧率而没有长尾与资源峰值，都不能判定完成。

### 定制导航界面

**正式节点 27/60。** 实现“定制导航界面”前先登记车辆信号来源、更新频率、单位、无效值、显示设备、交互负荷和安全等级。再用录制信号、超时、跳变、断连、昼夜与多语言场景验证视觉反馈不会掩盖关键驾驶信息。

本节点要形成可复核工件：版本坐标、平台或场景参数、关键指标、失败注入、预期结果与实际结果。若只有截图而没有输入和目标机条件，或只有平均帧率而没有长尾与资源峰值，都不能判定完成。

### 无极日夜切换

**正式节点 28/60。** “无极日夜切换”是第22页 六大HMI创新场景的第28个正式节点。学习时把演讲陈述改写为输入、处理、输出、资源和失败合同，保存一个正常路径与一个反例；不能从营销标题直接推导量产能力。

本节点要形成可复核工件：版本坐标、平台或场景参数、关键指标、失败注入、预期结果与实际结果。若只有截图而没有输入和目标机条件，或只有平均帧率而没有长尾与资源峰值，都不能判定完成。

### 无极天气切换

**正式节点 29/60。** 实现“无极天气切换”前先登记车辆信号来源、更新频率、单位、无效值、显示设备、交互负荷和安全等级。再用录制信号、超时、跳变、断连、昼夜与多语言场景验证视觉反馈不会掩盖关键驾驶信息。

本节点要形成可复核工件：版本坐标、平台或场景参数、关键指标、失败注入、预期结果与实际结果。若只有截图而没有输入和目标机条件，或只有平均帧率而没有长尾与资源峰值，都不能判定完成。

### 目的地信息三维展示

**正式节点 30/60。** “目的地信息三维展示”是第22页 六大HMI创新场景的第30个正式节点。学习时把演讲陈述改写为输入、处理、输出、资源和失败合同，保存一个正常路径与一个反例；不能从营销标题直接推导量产能力。

本节点要形成可复核工件：版本坐标、平台或场景参数、关键指标、失败注入、预期结果与实际结果。若只有截图而没有输入和目标机条件，或只有平均帧率而没有长尾与资源峰值，都不能判定完成。

### 智能驾驶

**正式节点 31/60。** 实现“智能驾驶”前先登记车辆信号来源、更新频率、单位、无效值、显示设备、交互负荷和安全等级。再用录制信号、超时、跳变、断连、昼夜与多语言场景验证视觉反馈不会掩盖关键驾驶信息。

本节点要形成可复核工件：版本坐标、平台或场景参数、关键指标、失败注入、预期结果与实际结果。若只有截图而没有输入和目标机条件，或只有平均帧率而没有长尾与资源峰值，都不能判定完成。

### 还原世界

**正式节点 32/60。** “还原世界”是第22页 六大HMI创新场景的第32个正式节点。学习时把演讲陈述改写为输入、处理、输出、资源和失败合同，保存一个正常路径与一个反例；不能从营销标题直接推导量产能力。

本节点要形成可复核工件：版本坐标、平台或场景参数、关键指标、失败注入、预期结果与实际结果。若只有截图而没有输入和目标机条件，或只有平均帧率而没有长尾与资源峰值，都不能判定完成。

### 行车可视化

**正式节点 33/60。** 实现“行车可视化”前先登记车辆信号来源、更新频率、单位、无效值、显示设备、交互负荷和安全等级。再用录制信号、超时、跳变、断连、昼夜与多语言场景验证视觉反馈不会掩盖关键驾驶信息。

本节点要形成可复核工件：版本坐标、平台或场景参数、关键指标、失败注入、预期结果与实际结果。若只有截图而没有输入和目标机条件，或只有平均帧率而没有长尾与资源峰值，都不能判定完成。

### 泊车可视化

**正式节点 34/60。** 实现“泊车可视化”前先登记车辆信号来源、更新频率、单位、无效值、显示设备、交互负荷和安全等级。再用录制信号、超时、跳变、断连、昼夜与多语言场景验证视觉反馈不会掩盖关键驾驶信息。

本节点要形成可复核工件：版本坐标、平台或场景参数、关键指标、失败注入、预期结果与实际结果。若只有截图而没有输入和目标机条件，或只有平均帧率而没有长尾与资源峰值，都不能判定完成。

### 决策透明化

**正式节点 35/60。** “决策透明化”是第22页 六大HMI创新场景的第35个正式节点。学习时把演讲陈述改写为输入、处理、输出、资源和失败合同，保存一个正常路径与一个反例；不能从营销标题直接推导量产能力。

本节点要形成可复核工件：版本坐标、平台或场景参数、关键指标、失败注入、预期结果与实际结果。若只有截图而没有输入和目标机条件，或只有平均帧率而没有长尾与资源峰值，都不能判定完成。

### 图像语义分割

**正式节点 36/60。** “图像语义分割”是第22页 六大HMI创新场景的第36个正式节点。学习时把演讲陈述改写为输入、处理、输出、资源和失败合同，保存一个正常路径与一个反例；不能从营销标题直接推导量产能力。

本节点要形成可复核工件：版本坐标、平台或场景参数、关键指标、失败注入、预期结果与实际结果。若只有截图而没有输入和目标机条件，或只有平均帧率而没有长尾与资源峰值，都不能判定完成。

### 空间音频警示

**正式节点 37/60。** “空间音频警示”是第22页 六大HMI创新场景的第37个正式节点。学习时把演讲陈述改写为输入、处理、输出、资源和失败合同，保存一个正常路径与一个反例；不能从营销标题直接推导量产能力。

本节点要形成可复核工件：版本坐标、平台或场景参数、关键指标、失败注入、预期结果与实际结果。若只有截图而没有输入和目标机条件，或只有平均帧率而没有长尾与资源峰值，都不能判定完成。

### 领航辅助驾驶

**正式节点 38/60。** 实现“领航辅助驾驶”前先登记车辆信号来源、更新频率、单位、无效值、显示设备、交互负荷和安全等级。再用录制信号、超时、跳变、断连、昼夜与多语言场景验证视觉反馈不会掩盖关键驾驶信息。

本节点要形成可复核工件：版本坐标、平台或场景参数、关键指标、失败注入、预期结果与实际结果。若只有截图而没有输入和目标机条件，或只有平均帧率而没有长尾与资源峰值，都不能判定完成。

### ADAS模拟仿真

**正式节点 39/60。** “ADAS模拟仿真”位于共享渲染服务合同中。要标明客户端注册、表面所有权、帧调度、输入路由、资源配额、进程死亡和重连语义；通过杀死单个客户端、重建Surface和同时显示多View证明隔离有效。

本节点要形成可复核工件：版本坐标、平台或场景参数、关键指标、失败注入、预期结果与实际结果。若只有截图而没有输入和目标机条件，或只有平均帧率而没有长尾与资源峰值，都不能判定完成。

### ADAS宣传视频

**正式节点 40/60。** “ADAS宣传视频”位于共享渲染服务合同中。要标明客户端注册、表面所有权、帧调度、输入路由、资源配额、进程死亡和重连语义；通过杀死单个客户端、重建Surface和同时显示多View证明隔离有效。

本节点要形成可复核工件：版本坐标、平台或场景参数、关键指标、失败注入、预期结果与实际结果。若只有截图而没有输入和目标机条件，或只有平均帧率而没有长尾与资源峰值，都不能判定完成。

### OS Innovation

**正式节点 41/60。** 实现“OS Innovation”前先登记车辆信号来源、更新频率、单位、无效值、显示设备、交互负荷和安全等级。再用录制信号、超时、跳变、断连、昼夜与多语言场景验证视觉反馈不会掩盖关键驾驶信息。

本节点要形成可复核工件：版本坐标、平台或场景参数、关键指标、失败注入、预期结果与实际结果。若只有截图而没有输入和目标机条件，或只有平均帧率而没有长尾与资源峰值，都不能判定完成。

### 创新架构

**正式节点 42/60。** “创新架构”是第22页 六大HMI创新场景的第42个正式节点。学习时把演讲陈述改写为输入、处理、输出、资源和失败合同，保存一个正常路径与一个反例；不能从营销标题直接推导量产能力。

本节点要形成可复核工件：版本坐标、平台或场景参数、关键指标、失败注入、预期结果与实际结果。若只有截图而没有输入和目标机条件，或只有平均帧率而没有长尾与资源峰值，都不能判定完成。

### 智能控件

**正式节点 43/60。** “智能控件”是第22页 六大HMI创新场景的第43个正式节点。学习时把演讲陈述改写为输入、处理、输出、资源和失败合同，保存一个正常路径与一个反例；不能从营销标题直接推导量产能力。

本节点要形成可复核工件：版本坐标、平台或场景参数、关键指标、失败注入、预期结果与实际结果。若只有截图而没有输入和目标机条件，或只有平均帧率而没有长尾与资源峰值，都不能判定完成。

### 4D动态壁纸

**正式节点 44/60。** 实现“4D动态壁纸”前先登记车辆信号来源、更新频率、单位、无效值、显示设备、交互负荷和安全等级。再用录制信号、超时、跳变、断连、昼夜与多语言场景验证视觉反馈不会掩盖关键驾驶信息。

本节点要形成可复核工件：版本坐标、平台或场景参数、关键指标、失败注入、预期结果与实际结果。若只有截图而没有输入和目标机条件，或只有平均帧率而没有长尾与资源峰值，都不能判定完成。

### 3D动态开场

**正式节点 45/60。** “3D动态开场”是第22页 六大HMI创新场景的第45个正式节点。学习时把演讲陈述改写为输入、处理、输出、资源和失败合同，保存一个正常路径与一个反例；不能从营销标题直接推导量产能力。

本节点要形成可复核工件：版本坐标、平台或场景参数、关键指标、失败注入、预期结果与实际结果。若只有截图而没有输入和目标机条件，或只有平均帧率而没有长尾与资源峰值，都不能判定完成。

### 智能虚拟形象

**正式节点 46/60。** “智能虚拟形象”是第22页 六大HMI创新场景的第46个正式节点。学习时把演讲陈述改写为输入、处理、输出、资源和失败合同，保存一个正常路径与一个反例；不能从营销标题直接推导量产能力。

本节点要形成可复核工件：版本坐标、平台或场景参数、关键指标、失败注入、预期结果与实际结果。若只有截图而没有输入和目标机条件，或只有平均帧率而没有长尾与资源峰值，都不能判定完成。

### 数字艺术模式

**正式节点 47/60。** “数字艺术模式”是第22页 六大HMI创新场景的第47个正式节点。学习时把演讲陈述改写为输入、处理、输出、资源和失败合同，保存一个正常路径与一个反例；不能从营销标题直接推导量产能力。

本节点要形成可复核工件：版本坐标、平台或场景参数、关键指标、失败注入、预期结果与实际结果。若只有截图而没有输入和目标机条件，或只有平均帧率而没有长尾与资源峰值，都不能判定完成。

### 3D音乐可视化

**正式节点 48/60。** 实现“3D音乐可视化”前先登记车辆信号来源、更新频率、单位、无效值、显示设备、交互负荷和安全等级。再用录制信号、超时、跳变、断连、昼夜与多语言场景验证视觉反馈不会掩盖关键驾驶信息。

本节点要形成可复核工件：版本坐标、平台或场景参数、关键指标、失败注入、预期结果与实际结果。若只有截图而没有输入和目标机条件，或只有平均帧率而没有长尾与资源峰值，都不能判定完成。

### 跨应用一镜到底

**正式节点 49/60。** “跨应用一镜到底”是第22页 六大HMI创新场景的第49个正式节点。学习时把演讲陈述改写为输入、处理、输出、资源和失败合同，保存一个正常路径与一个反例；不能从营销标题直接推导量产能力。

本节点要形成可复核工件：版本坐标、平台或场景参数、关键指标、失败注入、预期结果与实际结果。若只有截图而没有输入和目标机条件，或只有平均帧率而没有长尾与资源峰值，都不能判定完成。

### 跨引擎一镜到底

**正式节点 50/60。** “跨引擎一镜到底”是第22页 六大HMI创新场景的第50个正式节点。学习时把演讲陈述改写为输入、处理、输出、资源和失败合同，保存一个正常路径与一个反例；不能从营销标题直接推导量产能力。

本节点要形成可复核工件：版本坐标、平台或场景参数、关键指标、失败注入、预期结果与实际结果。若只有截图而没有输入和目标机条件，或只有平均帧率而没有长尾与资源峰值，都不能判定完成。

### 跨域创新

**正式节点 51/60。** 实现“跨域创新”前先登记车辆信号来源、更新频率、单位、无效值、显示设备、交互负荷和安全等级。再用录制信号、超时、跳变、断连、昼夜与多语言场景验证视觉反馈不会掩盖关键驾驶信息。

本节点要形成可复核工件：版本坐标、平台或场景参数、关键指标、失败注入、预期结果与实际结果。若只有截图而没有输入和目标机条件，或只有平均帧率而没有长尾与资源峰值，都不能判定完成。

### AR HUD

**正式节点 52/60。** 把“AR HUD”转成场景预算：固定SoC、分辨率、帧率、视角、材质和后处理，再记录CPU、GPU、显存、带宽与长尾帧时。演讲数字是估算起点，量产门槛必须由目标硬件实测确认。

本节点要形成可复核工件：版本坐标、平台或场景参数、关键指标、失败注入、预期结果与实际结果。若只有截图而没有输入和目标机条件，或只有平均帧率而没有长尾与资源峰值，都不能判定完成。

### UI即内饰

**正式节点 53/60。** “UI即内饰”是第22页 六大HMI创新场景的第53个正式节点。学习时把演讲陈述改写为输入、处理、输出、资源和失败合同，保存一个正常路径与一个反例；不能从营销标题直接推导量产能力。

本节点要形成可复核工件：版本坐标、平台或场景参数、关键指标、失败注入、预期结果与实际结果。若只有截图而没有输入和目标机条件，或只有平均帧率而没有长尾与资源峰值，都不能判定完成。

### 3D天际线

**正式节点 54/60。** “3D天际线”是第22页 六大HMI创新场景的第54个正式节点。学习时把演讲陈述改写为输入、处理、输出、资源和失败合同，保存一个正常路径与一个反例；不能从营销标题直接推导量产能力。

本节点要形成可复核工件：版本坐标、平台或场景参数、关键指标、失败注入、预期结果与实际结果。若只有截图而没有输入和目标机条件，或只有平均帧率而没有长尾与资源峰值，都不能判定完成。

### 座舱元宇宙

**正式节点 55/60。** 实现“座舱元宇宙”前先登记车辆信号来源、更新频率、单位、无效值、显示设备、交互负荷和安全等级。再用录制信号、超时、跳变、断连、昼夜与多语言场景验证视觉反馈不会掩盖关键驾驶信息。

本节点要形成可复核工件：版本坐标、平台或场景参数、关键指标、失败注入、预期结果与实际结果。若只有截图而没有输入和目标机条件，或只有平均帧率而没有长尾与资源峰值，都不能判定完成。

### 手机即车机

**正式节点 56/60。** 实现“手机即车机”前先登记车辆信号来源、更新频率、单位、无效值、显示设备、交互负荷和安全等级。再用录制信号、超时、跳变、断连、昼夜与多语言场景验证视觉反馈不会掩盖关键驾驶信息。

本节点要形成可复核工件：版本坐标、平台或场景参数、关键指标、失败注入、预期结果与实际结果。若只有截图而没有输入和目标机条件，或只有平均帧率而没有长尾与资源峰值，都不能判定完成。

### XR座舱体验

**正式节点 57/60。** 实现“XR座舱体验”前先登记车辆信号来源、更新频率、单位、无效值、显示设备、交互负荷和安全等级。再用录制信号、超时、跳变、断连、昼夜与多语言场景验证视觉反馈不会掩盖关键驾驶信息。

本节点要形成可复核工件：版本坐标、平台或场景参数、关键指标、失败注入、预期结果与实际结果。若只有截图而没有输入和目标机条件，或只有平均帧率而没有长尾与资源峰值，都不能判定完成。

### 3D情境交互

**正式节点 58/60。** “3D情境交互”是第22页 六大HMI创新场景的第58个正式节点。学习时把演讲陈述改写为输入、处理、输出、资源和失败合同，保存一个正常路径与一个反例；不能从营销标题直接推导量产能力。

本节点要形成可复核工件：版本坐标、平台或场景参数、关键指标、失败注入、预期结果与实际结果。若只有截图而没有输入和目标机条件，或只有平均帧率而没有长尾与资源峰值，都不能判定完成。

### 沉浸视听体验

**正式节点 59/60。** “沉浸视听体验”是第22页 六大HMI创新场景的第59个正式节点。学习时把演讲陈述改写为输入、处理、输出、资源和失败合同，保存一个正常路径与一个反例；不能从营销标题直接推导量产能力。

本节点要形成可复核工件：版本坐标、平台或场景参数、关键指标、失败注入、预期结果与实际结果。若只有截图而没有输入和目标机条件，或只有平均帧率而没有长尾与资源峰值，都不能判定完成。

### 游戏座舱体验

**正式节点 60/60。** 实现“游戏座舱体验”前先登记车辆信号来源、更新频率、单位、无效值、显示设备、交互负荷和安全等级。再用录制信号、超时、跳变、断连、昼夜与多语言场景验证视觉反馈不会掩盖关键驾驶信息。

本节点要形成可复核工件：版本坐标、平台或场景参数、关键指标、失败注入、预期结果与实际结果。若只有截图而没有输入和目标机条件，或只有平均帧率而没有长尾与资源峰值，都不能判定完成。

## 可迁移实现骨架

平台合同把演讲中的“支持范围”改成可执行配置，而不是散落在条件编译中。

```csharp
public sealed record HmiPlatform(
    string Soc,
    string OsImage,
    string GraphicsApi,
    int DisplayCount,
    int TargetFps,
    long MemoryLimitMb);

public sealed record HmiEvidence(
    string BuildId,
    double P95FrameMs,
    double P99SignalLatencyMs,
    long PeakMemoryMb,
    bool FaultRecovered);
```

共享渲染服务必须显式处理View注册、Surface变化和客户端死亡。

```csharp
public interface IHmiRenderService
{
    ViewHandle Register(ViewRequest request);
    void Resize(ViewHandle view, int width, int height);
    void PublishSignals(SignalBatch batch);
    void Remove(ViewHandle view, RemovalReason reason);
}
```

验收流水线先固定基线，再执行正常、压力、故障和恢复场景。

```text
load platform-lock.json
build signed target image
run baseline and capture trace
increase views, signals, assets and displays
kill one client; recreate surface; suspend and resume
compare p50/p95/p99, peaks, screenshots and recovery logs
publish pass, conditional-pass or rollback decision
```

## 量产通过矩阵

正确性门检查车辆信号的单位、无效值、超时与跨屏一致性；性能门检查主线程、渲染线程、GPU、合成、内存和带宽的P95/P99与峰值；平台门检查冷启动、热启动、休眠恢复、旋转或分辨率变化、透明图层、触控与日志；韧性门检查客户端退出、Surface重建、服务重启、磁盘不足和内存压力；交付门检查签名构建、配置锁、回滚包、测试报告和团队接管。

五道门必须使用同一构建ID和场景ID，避免把不同版本的最好结果拼成一份报告。任何安全相关显示错误、不可恢复黑屏、资源越界或无法解释的长尾都直接退回；非关键视觉差异只有在风险、负责人、修复版本和临时降级都登记后才允许条件通过。演讲材料提供能力坐标，矩阵负责把坐标转换成项目决定。

## 四类测试数据合同

车辆信号数据集保存信号名、发送方、接收方、单位、范围、更新频率、时间戳、无效值和超时策略。正常轨迹覆盖启动、行驶、停车和关机；压力轨迹提高频率并制造批量变化；故障轨迹加入乱序、重复、跳变、冻结和断连。测试不能只检查最终数字，还要检查旧值标识、动画收敛、跨屏一致性和恢复后的时间基准。

视觉数据集固定车型资产版本、相机、分辨率、色彩空间、字体、语言、主题、材质、灯光和后处理。每次运行保存关键帧截图、像素差异、Draw Call、三角面、纹理与Shader变体；昼夜、低亮、强光、动画过渡和多语言长文本都要覆盖。视觉差异要区分允许的抗锯齿噪声与会误导驾驶者的图标、颜色或层级错误。

平台数据集把同一场景部署到实际项目声明的SoC和OS组合，记录冷启动、热启动、首帧、稳定帧、休眠恢复、前后台切换、分辨率变化、触控和音频焦点。日志必须同时包含应用、Unity运行时、合成器和系统层时间戳；只在编辑器或桌面模拟器通过，不能替代QNX、Embedded Linux、Android或其他目标镜像的结果。

韧性数据集规定每个故障的注入时刻、影响范围、期望降级、资源释放上限和恢复期限。客户端死亡不能拖垮共享渲染服务，Surface重建不能留下黑层，信号断连必须进入明确的陈旧或不可用状态，内存压力需要按优先级回收非关键资源。所有恢复都要证明没有重复View、泄漏句柄、错绑输入或继续显示过期安全信息。

复核必须由未参与实现的人按证据包独立执行。证据包只提供版本锁、构建入口、场景定义、门槛和预期行为，不提供“应该看到通过”的口头提示；复核者需要自行得到指标、截图与故障轨迹，再与基准比较。若结果依赖某位开发者现场调参、私有资源或未登记设备，说明过程不可重复，必须退回补齐自动化脚本、测试资产、硬件清单和差异说明。

归档时为构建包、平台锁、测试资产、原始追踪和判定报告分别计算哈希，并用同一运行ID关联；这样后续审计能确认结论没有混入别的版本或设备结果。

分隔页同样继承后一分部的平台、预算与证据约束，不能被当作没有学习责任的装饰页面跳过。

## 三个常见陷阱

<Callout type="trap" title="把演讲数字当成当前统计">
  85%、35家和68款属于2024演讲时点。课程保留原口径；需要当前数据时另建带日期的来源，不覆盖历史证据。
</Callout>

<Callout type="trap" title="把兼容列表当成量产认证">
  SoC与OS名称不能证明具体BSP、驱动、图形API、多屏合成和休眠恢复都通过。每个实际组合都要单独验收。
</Callout>

<Callout type="trap" title="只看平均帧率">
  HMI还要观察P95/P99、信号延迟、内存峰值、Surface重建和客户端死亡恢复。平均值会隐藏驾驶过程中可感知的卡顿和黑屏。
</Callout>

## 本单元验收工件

1. 一份平台锁文件：SoC、BSP、OS、驱动、Unity版本、显示拓扑和构建ID齐全。
2. 一组预算表：三角面、纹理、Draw Call、CPU/GPU、内存与信号延迟都有门槛和实测。
3. 一条故障轨迹：客户端退出、Surface变化或信号断连后，安全内容、日志与恢复结果可追踪。
4. 一份来源台账：每个正式节点能回到PDF页码，并注明演讲时点与课程独立改编。

## 先预测再运行

先写下改变显示数量、分辨率、View数量、信号频率或目标OS后的预期瓶颈，再操作交互实验。实际结果若与预测相反，优先检查测量窗口、驱动缓存、热启动和场景内容，而不是立刻改代码。

## 本章回顾

场景矩阵覆盖3D车模、3D座舱、地图导航、智能驾驶、OS创新与跨域创新，每一项都要回到数据源、显示设备和安全等级。 掌握本页不是背诵产品列表，而是能把“选择场景域 → 登记信号与资产 → 定义交互反馈 → 分离安全内容 → 目标车回归”落成版本、合同、预算和故障证据。只有另一位工程师能在目标机复现相同判断，节点才算真正覆盖。

<Exercises>

1. 怎样证明第22页 六大HMI创新场景的陈述没有被跨版本外推？

<Answer>
  保存PDF页码、2024时点、平台锁和课程改编说明；对当前产品能力另建带日期的一手来源与目标机测试，不能静默替换演讲证据。
</Answer>

2. 怎样为本页建立最小但完整的目标机实验？

<Answer>
  固定SoC、OS、驱动、显示与场景，记录正常基线，再只改变一个资源或拓扑变量，最后注入客户端、Surface或信号故障并保存恢复轨迹。
</Answer>

3. 怎样判定某项HMI创新可以进入量产？

<Answer>
  正确性、交互负荷、P95/P99帧时、信号延迟、内存峰值、降级、安全内容分离与部署恢复全部过门；PoC画面好看不等于量产通过。
</Answer>

</Exercises>

## 术语复核

<Glossary>
  <GlossaryItem term="平台基线">
    固定芯片、BSP、操作系统、驱动、Unity运行时和显示拓扑的可复现版本集合。
  </GlossaryItem>
  <GlossaryItem term="量产预算">
    CPU、GPU、内存、带宽、三角面、Draw Call和长尾帧时的场景上限。
  </GlossaryItem>
  <GlossaryItem term="显示合同">
    车辆信号、应用View与合成显示之间的所有权、时序和失败规则。
  </GlossaryItem>
  <GlossaryItem term="服务边界">
    共享渲染服务对注册、调度、资源和故障隔离的明确承诺。
  </GlossaryItem>
  <GlossaryItem term="验收证据">
    可由他人在相同基线上复现实验结论的日志、指标、捕获和差异记录。
  </GlossaryItem>
</Glossary>

<Attribution
  adaptedFrom="Unity for HMI：未来已来，Unity开启3D座舱新篇章"
  adaptedUrl="https://learn-private.cdn.u3d.cn/attachment/3428ceb8-cee1-4f76-91c9-8780105be137______________Unity__3D_________.pdf"
/>

## 阅读导航

- [上一页：第21页 创新、实施与迭代服务模式](/learn/unity-hmi/21-uhm-2024-slide-21-service-model/uhm-2024-slide-21-service-model)
- [下一页：第23页 Thank you与证据闭环](/learn/unity-hmi/23-uhm-2024-slide-23-evidence-closure/uhm-2024-slide-23-evidence-closure)
