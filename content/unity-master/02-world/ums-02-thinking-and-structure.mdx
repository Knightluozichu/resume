---
title: "第二章 思考方式与构造"
type: C
section: "Unity 神技达人炼成记 · 起步与世界"
order: 3
description: "第二章把第一章的操作抽象成 Unity 的对象模型：世界由 GameObject 占位，Component 赋予能力，Transform 与 Hierarchy 表达空间关系，Prefab 保存可复用配置，脚本则让组件在事件中改变状态。掌握这套结构后，读者才能解释自己刚才“做了什么”。"
demo: true
math: false
sourceUrl: "https://www.tenlong.com.tw/products/9787515354781"
draft: true
---

import {
  Objectives,
  Callout,
  Attribution,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
} from "@/components/mdx/mdx-components";

<Objectives>
  - 能解释你做了什么 - 能比较在世界中配置物品的思考方式与现代 Unity 的实现边界 -
  能实现“制作一个带 Mesh Renderer、Collider、Rigidbody 和自定义脚本的箱子
  Prefab，创建三个实例，只覆盖其中一个材质，再修改模板并检查继承矩阵。”的最小复现实验
  - 能判断并修复“为整理 Hierarchy
  随意建立父子关系，导致缩放、坐标和销毁行为被父对象隐式改变”
</Objectives>

## 问题：这一单元在原书里解决什么

先预测：如果跳过“对象身份”，直接做到“运行状态”，最终结果会在哪个状态先失真？先写下输入、预期变化和拒绝条件，再操作图示。

第二章把第一章的操作抽象成 Unity 的对象模型：世界由 GameObject 占位，Component 赋予能力，Transform 与 Hierarchy 表达空间关系，Prefab 保存可复用配置，脚本则让组件在事件中改变状态。掌握这套结构后，读者才能解释自己刚才“做了什么”。

<Callout type="note" title="先预测">
  不看最终状态，先选择正常、边界或失败样本，预测“对象身份 → 组件能力 → 层级关系
  → Prefab模板 → 脚本事件 →
  运行状态”中第一个变化节点。预测必须能被日志、场景状态、构建结果或性能记录证伪。
</Callout>

## 原书位置与覆盖边界

本页对应第二章，逐项保留中文版本目录中的主题：你做了什么、在世界中配置物品的思考方式、添加组件、使其成为物理性的物体、Hierarchy（层级）的思考方式、Prefab的概念、什么是脚本、创造世界的概念（总结）。页面不把相近的现代功能冒充原书章节；对于已经失效的 API，会先解释原问题，再给出现代等价实践和迁移证据。

|   # | 官方主题                    | 最小掌握证据                     |
| --: | --------------------------- | -------------------------------- |
|   1 | 你做了什么                  | 写出对象、状态、操作和可观察结果 |
|   2 | 在世界中配置物品的思考方式  | 写出对象、状态、操作和可观察结果 |
|   3 | 添加组件                    | 写出对象、状态、操作和可观察结果 |
|   4 | 使其成为物理性的物体        | 写出对象、状态、操作和可观察结果 |
|   5 | Hierarchy（层级）的思考方式 | 写出对象、状态、操作和可观察结果 |
|   6 | Prefab的概念                | 写出对象、状态、操作和可观察结果 |
|   7 | 什么是脚本                  | 写出对象、状态、操作和可观察结果 |
|   8 | 创造世界的概念（总结）      | 写出对象、状态、操作和可观察结果 |

## 核心知识与现代 Unity 对照

### 对象与能力分离

把一个 Cube 拖进场景只创建了对象身份和若干默认组件。Mesh Renderer 负责画出来，Collider 负责碰撞边界，Rigidbody 让它参与动力学，脚本负责规则。若把这些职责混成“Cube 会动”，就无法判断故障发生在渲染、物理还是输入层。

### Hierarchy 是空间与生命周期约束

父子关系不仅整理列表，还会传播 Transform。移动父对象会改变所有子对象的世界位置；禁用父对象也会影响子层级。建立层级前应写清谁拥有谁、谁随谁移动、谁可以独立销毁，避免为了界面整齐而制造隐式耦合。

### Prefab 是实例协议

Prefab 把可复用结构从场景拷贝提升为资产协议。现代 Prefab 支持嵌套、Variant 和覆盖检查；实例修改必须明确是局部例外还是应该 Apply 回模板。正确验收不是“能复制”，而是模板更新后实例按预期继承，局部覆盖仍可解释。

在复刻时，最容易犯的错误是只追求最终画面或最终数值。正确的证据链必须同时保存环境、输入、首个状态变化、中间状态、失败原因和最终产物。版本差异可以改变按钮位置或 API 名称，却不能改变本单元要解决的对象关系和验收问题。

<Term def="场景中的身份和组件容器，本身不等于渲染模型或物理实体">GameObject</Term>
必须在实验记录中对应一个可观察状态，不能只作为名词出现。

<Term def="附着在 GameObject 上并提供渲染、物理、声音或行为能力的模块">Component</Term>
必须在实验记录中对应一个可观察状态，不能只作为名词出现。

<Term def="保存对象层级和组件配置、可实例化并可追踪覆盖差异的项目资产">Prefab</Term>
必须在实验记录中对应一个可观察状态，不能只作为名词出现。

## 三阶段操作证据链

<Stepper>
  <Step title="1. 建立基线并固定输入">
    记录 Unity 版本、目标平台、场景或资产版本，只使用一个正常样本。执行“对象身份
    → 组件能力”，保存首个可观察变化。
    </Step>
  <Step title="2. 注入边界和失败样本">
    只改变一个变量，分别运行边界与失败样本。沿“层级关系 → Prefab模板 →
    脚本事件”记录首个偏离，不能只保留最终截图。
    </Step>
  <Step title="3. 修复并完成交付验收">
    恢复同一输入重新运行，确认“每个可观察行为都能定位到明确组件，Prefab
    更新与实例覆盖之间没有未解释差异”。最后在目标运行环境复验，而不是只在编辑器中签发。
  </Step>
</Stepper>

## 可复现实验

制作一个带 Mesh Renderer、Collider、Rigidbody 和自定义脚本的箱子 Prefab，创建三个实例，只覆盖其中一个材质，再修改模板并检查继承矩阵。

先用一个小型组件记录实验身份。它不替代截图和运行日志，但能把环境与对象写进同一条证据。

```csharp
using UnityEngine;

public sealed class EvidenceStamp : MonoBehaviour
{
    [SerializeField] private string unit = "ums-02-thinking-and-structure";
    [SerializeField] private string variant = "baseline";

    private void Start()
    {
        Debug.Log($"unit={unit}; variant={variant}; unity={Application.unityVersion}; frame={Time.frameCount}");
    }
}
```

本单元的最小实现如下。运行前先预测输出，运行后记录实际状态和首个差异。

```csharp
using UnityEngine;

public sealed class ComponentAudit : MonoBehaviour
{
    private void Awake()
    {
        Debug.Assert(TryGetComponent<Collider>(out _), "缺少 Collider");
        Debug.Assert(TryGetComponent<Rigidbody>(out _), "缺少 Rigidbody");
    }
}
```

最后用签发函数拒绝缺少关键证据的结果。不要为了“绿灯”把条件删掉。

```csharp
public static class UnitGate
{
    public static bool CanShip(bool baseline, bool boundary, bool failureReplay, bool targetBuild)
    {
        return baseline && boundary && failureReplay && targetBuild;
    }
}
```

## 两个必须主动制造的失败样本

<Callout type="trap" title="单变量故障">
  故意制造“为整理 Hierarchy
  随意建立父子关系，导致缩放、坐标和销毁行为被父对象隐式改变”。保存故障前输入、首个偏离节点、实际状态和拒绝原因；修复后必须用完全相同输入复验。
</Callout>

<Callout type="trap" title="结果看似正确但证据断裂">
  故意只保留最终画面或最终数值，删除环境、平台、版本或中间状态。即使结果看似正常，只要“每个可观察行为都能定位到明确组件，Prefab
  更新与实例覆盖之间没有未解释差异”无法证明，签发门也必须拒绝。
</Callout>

## 验收矩阵

| 维度 | 正常样本       | 边界样本     | 失败样本       | 通过条件         |
| ---- | -------------- | ------------ | -------------- | ---------------- |
| 输入 | 固定版本与对象 | 只改一个阈值 | 注入明确故障   | 三组输入可重放   |
| 状态 | 逐步记录节点   | 检查临界切换 | 定位首个偏离   | 中间状态完整     |
| 结果 | 满足核心目标   | 无越界或卡死 | 被阶段门拒绝   | 修复后同输入通过 |
| 交付 | 编辑器基线     | 目标环境差异 | 构建或设备失败 | 真实目标环境复验 |

## 练习

<Exercises>

**问题 1** 第二章覆盖哪些官方主题？任选三个，说明它们怎样形成因果链。

<Answer>
  本页覆盖你做了什么、在世界中配置物品的思考方式、添加组件、使其成为物理性的物体、Hierarchy（层级）的思考方式、Prefab的概念、什么是脚本、创造世界的概念（总结）。合格答案要指出上游对象或输入如何改变中间状态，再说明下游画面、行为、构建或性能如何被观察，不能只罗列名称。
</Answer>

**问题 2** 怎样主动制造“为整理 Hierarchy 随意建立父子关系，导致缩放、坐标和销毁行为被父对象隐式改变”并证明修复有效？

<Answer>
  固定 Unity
  版本、场景、资产和输入，只注入这一故障；记录首个偏离节点与完整轨迹。修复后用相同输入重跑正常、边界和失败样本，并在目标运行环境确认没有转移性回归。
</Answer>

**问题 3** 为什么“每个可观察行为都能定位到明确组件，Prefab 更新与实例覆盖之间没有未解释差异”必须作为签发不变量？

<Answer>
  因为它把本单元的概念、操作和交付结果连接起来。缺少它时，最终画面或数字可能暂时正确，但对象职责、状态路径、平台差异或性能成本可能已经失控。
</Answer>

</Exercises>

## 小结

- 已逐项覆盖第二章的正式目录，没有用泛化高级主题替代原书。
- 已沿“对象身份 → 组件能力 → 层级关系 → Prefab模板 → 脚本事件 → 运行状态”保存输入、中间状态、失败样本和交付证据。
- 已区分原书历史 API 与现代 Unity 等价实践，并保留迁移边界。
- 已通过正常、边界、失败和修复四组样本验证“每个可观察行为都能定位到明确组件，Prefab 更新与实例覆盖之间没有未解释差异”。

## 术语表

<Glossary>
  <GlossaryItem term="GameObject">
    场景中的身份和组件容器，本身不等于渲染模型或物理实体
  </GlossaryItem>
  <GlossaryItem term="Component">
    附着在 GameObject 上并提供渲染、物理、声音或行为能力的模块
  </GlossaryItem>
  <GlossaryItem term="Prefab">
    保存对象层级和组件配置、可实例化并可追踪覆盖差异的项目资产
  </GlossaryItem>
</Glossary>

## 来源与改编边界

<Attribution
  adaptedFrom="广铁夫《Unity神技达人炼成记：成为游戏世界的造物主》，中国青年出版社，2019，ISBN 9787515354781；日文原版《Unityで神になる本。》，オーム社，2015，ISBN 9784274069222"
  adaptedUrl="https://www.tenlong.com.tw/products/9787515354781"
/>
