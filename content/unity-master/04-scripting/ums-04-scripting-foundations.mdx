---
title: "第四章 脚本基础知识"
type: C
section: "Unity 神技达人炼成记 · 资产与脚本"
order: 5
description: "第四章是全书最长的规则层：事件消息决定代码何时运行，变量和类型保存状态，条件与循环表达控制流，Transform、Rigidbody、输入、碰撞、Raycast、场景、协程和持久化 API 把状态接回世界。原书示例含 UnityScript 与 iTween，现代复刻必须改写为 C# 与受维护的等价方案，同时保留原始问题。"
demo: true
math: false
sourceUrl: "https://www.tenlong.com.tw/products/9787515354781"
draft: true
---

import {
  Objectives,
  Callout,
  Attribution,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
} from "@/components/mdx/mdx-components";

<Objectives>
  - 能解释编程这个工作 - 能比较了解主要的事件消息与现代 Unity 的实现边界 -
  能实现“制作交通灯状态机：键盘或鼠标触发，协程控制时序，Raycast
  选择灯体，碰撞对象读取当前状态；切换场景后只保留一个 GameManager，并验证
  PlayerPrefs 只保存用户选项。”的最小复现实验 - 能判断并修复“在 Update
  中直接改动态 Rigidbody 的 Transform，又在 FixedUpdate
  施力，造成帧率相关运动和碰撞穿透”
</Objectives>

## 问题：这一单元在原书里解决什么

先预测：如果跳过“事件入口”，直接做到“持久化管理”，最终结果会在哪个状态先失真？先写下输入、预期变化和拒绝条件，再操作图示。

第四章是全书最长的规则层：事件消息决定代码何时运行，变量和类型保存状态，条件与循环表达控制流，Transform、Rigidbody、输入、碰撞、Raycast、场景、协程和持久化 API 把状态接回世界。原书示例含 UnityScript 与 iTween，现代复刻必须改写为 C# 与受维护的等价方案，同时保留原始问题。

<Callout type="note" title="先预测">
  不看最终状态，先选择正常、边界或失败样本，预测“事件入口 → 状态类型 → 控制流 →
  场景引用 → 物理输入 →
  持久化管理”中第一个变化节点。预测必须能被日志、场景状态、构建结果或性能记录证伪。
</Callout>

## 原书位置与覆盖边界

本页对应第四章，逐项保留中文版本目录中的主题：编程这个工作、了解主要的事件消息、写脚本的基本规则、使用Debug.Log、使用Invoke延时调用、自定义函数、变量与类型、带值的函数（闰年）、用if语句实现分支、用return返回值、用for语句循环、数组的使用方法、用foreach语句循环、更改材质（制作信号灯）、定义类、关于变量的声明、从脚本中指定GameObject的方法、通过名称指定自己的子游戏对象、控制位置、方向和缩放（Transform类）、用Translate更改位置、利用Rotate旋转、用物理引擎操控、用AddForce施加作用力、用鼠标输入与Instantiate显示、碰撞事件、跟随相机（LookAt）、关于Tag、接受键盘事件、使用iTween、使用Raycast触碰、移动场景、使用Coroutine（协同程序）、移动场景后仍保留GameObject、PlayerPrefs保存数据、使用字符串、播放Audio、用switch case语句做分支、用脚本处理重复工作、总结与GameManager。页面不把相近的现代功能冒充原书章节；对于已经失效的 API，会先解释原问题，再给出现代等价实践和迁移证据。

|   # | 官方主题                            | 最小掌握证据                     |
| --: | ----------------------------------- | -------------------------------- |
|   1 | 编程这个工作                        | 写出对象、状态、操作和可观察结果 |
|   2 | 了解主要的事件消息                  | 写出对象、状态、操作和可观察结果 |
|   3 | 写脚本的基本规则                    | 写出对象、状态、操作和可观察结果 |
|   4 | 使用Debug.Log                       | 写出对象、状态、操作和可观察结果 |
|   5 | 使用Invoke延时调用                  | 写出对象、状态、操作和可观察结果 |
|   6 | 自定义函数、变量与类型              | 写出对象、状态、操作和可观察结果 |
|   7 | 带值的函数（闰年）                  | 写出对象、状态、操作和可观察结果 |
|   8 | 用if语句实现分支                    | 写出对象、状态、操作和可观察结果 |
|   9 | 用return返回值                      | 写出对象、状态、操作和可观察结果 |
|  10 | 用for语句循环                       | 写出对象、状态、操作和可观察结果 |
|  11 | 数组的使用方法                      | 写出对象、状态、操作和可观察结果 |
|  12 | 用foreach语句循环                   | 写出对象、状态、操作和可观察结果 |
|  13 | 更改材质（制作信号灯）              | 写出对象、状态、操作和可观察结果 |
|  14 | 定义类                              | 写出对象、状态、操作和可观察结果 |
|  15 | 关于变量的声明                      | 写出对象、状态、操作和可观察结果 |
|  16 | 从脚本中指定GameObject的方法        | 写出对象、状态、操作和可观察结果 |
|  17 | 通过名称指定自己的子游戏对象        | 写出对象、状态、操作和可观察结果 |
|  18 | 控制位置、方向和缩放（Transform类） | 写出对象、状态、操作和可观察结果 |
|  19 | 用Translate更改位置                 | 写出对象、状态、操作和可观察结果 |
|  20 | 利用Rotate旋转                      | 写出对象、状态、操作和可观察结果 |
|  21 | 用物理引擎操控                      | 写出对象、状态、操作和可观察结果 |
|  22 | 用AddForce施加作用力                | 写出对象、状态、操作和可观察结果 |
|  23 | 用鼠标输入与Instantiate显示         | 写出对象、状态、操作和可观察结果 |
|  24 | 碰撞事件                            | 写出对象、状态、操作和可观察结果 |
|  25 | 跟随相机（LookAt）                  | 写出对象、状态、操作和可观察结果 |
|  26 | 关于Tag                             | 写出对象、状态、操作和可观察结果 |
|  27 | 接受键盘事件                        | 写出对象、状态、操作和可观察结果 |
|  28 | 使用iTween                          | 写出对象、状态、操作和可观察结果 |
|  29 | 使用Raycast触碰                     | 写出对象、状态、操作和可观察结果 |
|  30 | 移动场景                            | 写出对象、状态、操作和可观察结果 |
|  31 | 使用Coroutine（协同程序）           | 写出对象、状态、操作和可观察结果 |
|  32 | 移动场景后仍保留GameObject          | 写出对象、状态、操作和可观察结果 |
|  33 | PlayerPrefs保存数据                 | 写出对象、状态、操作和可观察结果 |
|  34 | 使用字符串                          | 写出对象、状态、操作和可观察结果 |
|  35 | 播放Audio                           | 写出对象、状态、操作和可观察结果 |
|  36 | 用switch case语句做分支             | 写出对象、状态、操作和可观察结果 |
|  37 | 用脚本处理重复工作                  | 写出对象、状态、操作和可观察结果 |
|  38 | 总结与GameManager                   | 写出对象、状态、操作和可观察结果 |

## 核心知识与现代 Unity 对照

### 事件、状态与控制流

Start、Update 和碰撞回调不是随意命名的函数，而是引擎调度契约。变量必须有稳定类型和所有权，if、switch、for 与 foreach 只是根据状态选择转换。Debug.Log 应输出对象、帧和关键输入，Invoke 与 Coroutine 要记录取消条件，避免延迟逻辑在对象失效后继续执行。

### 引用对象而不是猜名字

原书展示按名称寻找 GameObject 和子对象，但生产代码优先使用序列化引用、GetComponent、Tag 或明确注册表。字符串查找容易被重命名破坏，也隐藏依赖。Transform 的位置、旋转和缩放还要区分局部与世界空间，父子层级会改变结果。

### 物理、输入与实例化

直接改 Transform 和用 Rigidbody.AddForce 属于不同运动模型，不能在同一动态刚体上混用而不说明。输入产生意图，FixedUpdate 提交物理动作，碰撞回调记录结果。Raycast 要保留起点、方向、距离和 LayerMask；Instantiate 要管理数量、生命周期和对象池边界。

### 跨场景状态与现代化边界

DontDestroyOnLoad 只解决对象不销毁，不解决重复实例和状态版本；PlayerPrefs 适合少量用户设置，不适合复杂存档。原书的 UnityScript 已移除，iTween 也应被明确标为历史工具；复刻时用 C#、Animator、Tween 库或自定义插值实现同一行为，并保存迁移前后的时间曲线。

在复刻时，最容易犯的错误是只追求最终画面或最终数值。正确的证据链必须同时保存环境、输入、首个状态变化、中间状态、失败原因和最终产物。版本差异可以改变按钮位置或 API 名称，却不能改变本单元要解决的对象关系和验收问题。

<Term def="由 Unity 生命周期或物理、输入等系统在特定时机调用的约定方法">事件消息</Term>
必须在实验记录中对应一个可观察状态，不能只作为名词出现。

<Term def="在主线程上跨多帧暂停和恢复流程的迭代器，不等同于后台线程">协程</Term>
必须在实验记录中对应一个可观察状态，不能只作为名词出现。

<Term def="集中编排跨对象规则的角色，应保存清晰状态而不是成为无边界全局容器">GameManager</Term>
必须在实验记录中对应一个可观察状态，不能只作为名词出现。

## 三阶段操作证据链

<Stepper>
  <Step title="1. 建立基线并固定输入">
    记录 Unity 版本、目标平台、场景或资产版本，只使用一个正常样本。执行“事件入口
    → 状态类型”，保存首个可观察变化。
    </Step>
  <Step title="2. 注入边界和失败样本">
    只改变一个变量，分别运行边界与失败样本。沿“控制流 → 场景引用 →
    物理输入”记录首个偏离，不能只保留最终截图。
    </Step>
  <Step title="3. 修复并完成交付验收">
    恢复同一输入重新运行，确认“同一输入序列在固定初始状态下产生可重放状态轨迹，跨场景后管理器唯一且没有隐藏字符串依赖”。最后在目标运行环境复验，而不是只在编辑器中签发。
  </Step>
</Stepper>

## 可复现实验

制作交通灯状态机：键盘或鼠标触发，协程控制时序，Raycast 选择灯体，碰撞对象读取当前状态；切换场景后只保留一个 GameManager，并验证 PlayerPrefs 只保存用户选项。

先用一个小型组件记录实验身份。它不替代截图和运行日志，但能把环境与对象写进同一条证据。

```csharp
using UnityEngine;

public sealed class EvidenceStamp : MonoBehaviour
{
    [SerializeField] private string unit = "ums-04-scripting-foundations";
    [SerializeField] private string variant = "baseline";

    private void Start()
    {
        Debug.Log($"unit={unit}; variant={variant}; unity={Application.unityVersion}; frame={Time.frameCount}");
    }
}
```

本单元的最小实现如下。运行前先预测输出，运行后记录实际状态和首个差异。

```csharp
using System.Collections;
using UnityEngine;

public sealed class TrafficLight : MonoBehaviour
{
    private enum Signal { Red, Green, Amber }
    private Signal current;

    private IEnumerator Cycle()
    {
        foreach (Signal signal in System.Enum.GetValues(typeof(Signal)))
        {
            current = signal;
            Debug.Log($"signal={current}");
            yield return new WaitForSeconds(1f);
        }
    }
}
```

最后用签发函数拒绝缺少关键证据的结果。不要为了“绿灯”把条件删掉。

```csharp
public static class UnitGate
{
    public static bool CanShip(bool baseline, bool boundary, bool failureReplay, bool targetBuild)
    {
        return baseline && boundary && failureReplay && targetBuild;
    }
}
```

## 两个必须主动制造的失败样本

<Callout type="trap" title="单变量故障">
  故意制造“在 Update 中直接改动态 Rigidbody 的 Transform，又在 FixedUpdate
  施力，造成帧率相关运动和碰撞穿透”。保存故障前输入、首个偏离节点、实际状态和拒绝原因；修复后必须用完全相同输入复验。
</Callout>

<Callout type="trap" title="结果看似正确但证据断裂">
  故意只保留最终画面或最终数值，删除环境、平台、版本或中间状态。即使结果看似正常，只要“同一输入序列在固定初始状态下产生可重放状态轨迹，跨场景后管理器唯一且没有隐藏字符串依赖”无法证明，签发门也必须拒绝。
</Callout>

## 验收矩阵

| 维度 | 正常样本       | 边界样本     | 失败样本       | 通过条件         |
| ---- | -------------- | ------------ | -------------- | ---------------- |
| 输入 | 固定版本与对象 | 只改一个阈值 | 注入明确故障   | 三组输入可重放   |
| 状态 | 逐步记录节点   | 检查临界切换 | 定位首个偏离   | 中间状态完整     |
| 结果 | 满足核心目标   | 无越界或卡死 | 被阶段门拒绝   | 修复后同输入通过 |
| 交付 | 编辑器基线     | 目标环境差异 | 构建或设备失败 | 真实目标环境复验 |

## 练习

<Exercises>

**问题 1** 第四章覆盖哪些官方主题？任选三个，说明它们怎样形成因果链。

<Answer>
  本页覆盖编程这个工作、了解主要的事件消息、写脚本的基本规则、使用Debug.Log、使用Invoke延时调用、自定义函数、变量与类型、带值的函数（闰年）、用if语句实现分支、用return返回值、用for语句循环、数组的使用方法、用foreach语句循环、更改材质（制作信号灯）、定义类、关于变量的声明、从脚本中指定GameObject的方法、通过名称指定自己的子游戏对象、控制位置、方向和缩放（Transform类）、用Translate更改位置、利用Rotate旋转、用物理引擎操控、用AddForce施加作用力、用鼠标输入与Instantiate显示、碰撞事件、跟随相机（LookAt）、关于Tag、接受键盘事件、使用iTween、使用Raycast触碰、移动场景、使用Coroutine（协同程序）、移动场景后仍保留GameObject、PlayerPrefs保存数据、使用字符串、播放Audio、用switch
  case语句做分支、用脚本处理重复工作、总结与GameManager。合格答案要指出上游对象或输入如何改变中间状态，再说明下游画面、行为、构建或性能如何被观察，不能只罗列名称。
</Answer>

**问题 2** 怎样主动制造“在 Update 中直接改动态 Rigidbody 的 Transform，又在 FixedUpdate 施力，造成帧率相关运动和碰撞穿透”并证明修复有效？

<Answer>
  固定 Unity
  版本、场景、资产和输入，只注入这一故障；记录首个偏离节点与完整轨迹。修复后用相同输入重跑正常、边界和失败样本，并在目标运行环境确认没有转移性回归。
</Answer>

**问题 3** 为什么“同一输入序列在固定初始状态下产生可重放状态轨迹，跨场景后管理器唯一且没有隐藏字符串依赖”必须作为签发不变量？

<Answer>
  因为它把本单元的概念、操作和交付结果连接起来。缺少它时，最终画面或数字可能暂时正确，但对象职责、状态路径、平台差异或性能成本可能已经失控。
</Answer>

</Exercises>

## 小结

- 已逐项覆盖第四章的正式目录，没有用泛化高级主题替代原书。
- 已沿“事件入口 → 状态类型 → 控制流 → 场景引用 → 物理输入 → 持久化管理”保存输入、中间状态、失败样本和交付证据。
- 已区分原书历史 API 与现代 Unity 等价实践，并保留迁移边界。
- 已通过正常、边界、失败和修复四组样本验证“同一输入序列在固定初始状态下产生可重放状态轨迹，跨场景后管理器唯一且没有隐藏字符串依赖”。

## 术语表

<Glossary>
  <GlossaryItem term="事件消息">
    由 Unity 生命周期或物理、输入等系统在特定时机调用的约定方法
  </GlossaryItem>
  <GlossaryItem term="协程">
    在主线程上跨多帧暂停和恢复流程的迭代器，不等同于后台线程
  </GlossaryItem>
  <GlossaryItem term="GameManager">
    集中编排跨对象规则的角色，应保存清晰状态而不是成为无边界全局容器
  </GlossaryItem>
</Glossary>

## 来源与改编边界

<Attribution
  adaptedFrom="广铁夫《Unity神技达人炼成记：成为游戏世界的造物主》，中国青年出版社，2019，ISBN 9787515354781；日文原版《Unityで神になる本。》，オーム社，2015，ISBN 9784274069222"
  adaptedUrl="https://www.tenlong.com.tw/products/9787515354781"
/>
