---
title: "第七章 输出"
type: C
section: "Unity 神技达人炼成记 · 动画、界面与输出"
order: 8
description: "第七章把编辑器内体验变成平台产品：平台切换会重新导入资源，Player Settings 与 Quality 决定运行契约，Build 生成可安装产物，多点触控、条件编译和平台差异决定同一玩法能否保持语义一致。iOS 与 Android 还要求工具链、签名、包标识和商店材料完整。"
demo: true
math: false
sourceUrl: "https://www.tenlong.com.tw/products/9787515354781"
draft: false
---


## 问题：这一单元在原书里解决什么

先预测：如果跳过“目标平台”，直接做到“商店证据”，最终结果会在哪个状态先失真？先写下输入、预期变化和拒绝条件，再操作图示。

第七章把编辑器内体验变成平台产品：平台切换会重新导入资源，Player Settings 与 Quality 决定运行契约，Build 生成可安装产物，多点触控、条件编译和平台差异决定同一玩法能否保持语义一致。iOS 与 Android 还要求工具链、签名、包标识和商店材料完整。


## 原书位置与覆盖边界

本页对应第七章，逐项保留中文版本目录中的主题：Unity的输出位置、切换平台、了解Player Settings、品质设置、进行Build、对应多平台的游戏设计、触摸画面时的不同、实现多个点击、使用平台依赖编译、手机应用程序的输出、准备iOS的开发、在Unity中进行iOS应用程序的写出、创建Android应用程序、下载Android SDK、创建Keystore文件、制作Android时的注意事项、向Play Store申请、其他输出。页面不把相近的现代功能冒充原书章节；对于已经失效的 API，会先解释原问题，再给出现代等价实践和迁移证据。

|   # | 官方主题                       | 最小掌握证据                     |
| --: | ------------------------------ | -------------------------------- |
|   1 | Unity的输出位置                | 写出对象、状态、操作和可观察结果 |
|   2 | 切换平台                       | 写出对象、状态、操作和可观察结果 |
|   3 | 了解Player Settings            | 写出对象、状态、操作和可观察结果 |
|   4 | 品质设置                       | 写出对象、状态、操作和可观察结果 |
|   5 | 进行Build                      | 写出对象、状态、操作和可观察结果 |
|   6 | 对应多平台的游戏设计           | 写出对象、状态、操作和可观察结果 |
|   7 | 触摸画面时的不同               | 写出对象、状态、操作和可观察结果 |
|   8 | 实现多个点击                   | 写出对象、状态、操作和可观察结果 |
|   9 | 使用平台依赖编译               | 写出对象、状态、操作和可观察结果 |
|  10 | 手机应用程序的输出             | 写出对象、状态、操作和可观察结果 |
|  11 | 准备iOS的开发                  | 写出对象、状态、操作和可观察结果 |
|  12 | 在Unity中进行iOS应用程序的写出 | 写出对象、状态、操作和可观察结果 |
|  13 | 创建Android应用程序            | 写出对象、状态、操作和可观察结果 |
|  14 | 下载Android SDK                | 写出对象、状态、操作和可观察结果 |
|  15 | 创建Keystore文件               | 写出对象、状态、操作和可观察结果 |
|  16 | 制作Android时的注意事项        | 写出对象、状态、操作和可观察结果 |
|  17 | 向Play Store申请               | 写出对象、状态、操作和可观察结果 |
|  18 | 其他输出                       | 写出对象、状态、操作和可观察结果 |

## 核心知识与现代 Unity 对照

### 平台切换是依赖变更

切换 Build Target 或现代 Build Profile 会触发资源重新导入，并改变图形 API、压缩格式和可用插件。构建前固定 Unity 版本、平台模块、场景列表、包标识和脚本后端。Quality 不是简单高低档，而是帧率、阴影、纹理和后处理预算的成组策略。

### 输入语义跨平台保持一致

鼠标点击只有一个指针，触摸可能同时出现多个接触点并经历 Began、Moved、Ended。平台适配应先定义“选择、拖动、缩放”等语义，再映射鼠标、触摸或手柄。条件编译只隔离平台 API，不能复制整套游戏规则形成分叉。

### 移动构建与发布证据

iOS 构建通常输出 Xcode 工程并依赖证书和 Provisioning Profile；Android 依赖 SDK、包名、目标 API、脚本后端和 Keystore。密钥不能进入仓库。验收必须包含真机安装、冷启动、权限、横竖屏、暂停恢复和升级路径，而不只是 Build Succeeded。

在复刻时，最容易犯的错误是只追求最终画面或最终数值。正确的证据链必须同时保存环境、输入、首个状态变化、中间状态、失败原因和最终产物。版本差异可以改变按钮位置或 API 名称，却不能改变本单元要解决的对象关系和验收问题。

必须在实验记录中对应一个可观察状态，不能只作为名词出现。

必须在实验记录中对应一个可观察状态，不能只作为名词出现。

必须在实验记录中对应一个可观察状态，不能只作为名词出现。

## 三阶段操作证据链


## 可复现实验

为桌面、WebGL 和 Android 建立三套构建配置，共用同一“选择与拖动”输入语义；保存构建参数、产物哈希、真机或浏览器日志和平台差异清单。

先用一个小型组件记录实验身份。它不替代截图和运行日志，但能把环境与对象写进同一条证据。

```csharp
using UnityEngine;

public sealed class EvidenceStamp : MonoBehaviour
{
    [SerializeField] private string unit = "ums-07-build-and-output";
    [SerializeField] private string variant = "baseline";

    private void Start()
    {
        Debug.Log($"unit={unit}; variant={variant}; unity={Application.unityVersion}; frame={Time.frameCount}");
    }
}
```

本单元的最小实现如下。运行前先预测输出，运行后记录实际状态和首个差异。

```csharp
using UnityEngine;

public static class PlatformCapability
{
    public static bool SupportsTouch()
    {
#if UNITY_IOS || UNITY_ANDROID
        return true;
#else
        return Input.touchSupported;
#endif
    }
}
```

最后用签发函数拒绝缺少关键证据的结果。不要为了“绿灯”把条件删掉。

```csharp
public static class UnitGate
{
    public static bool CanShip(bool baseline, bool boundary, bool failureReplay, bool targetBuild)
    {
        return baseline && boundary && failureReplay && targetBuild;
    }
}
```

## 两个必须主动制造的失败样本


## 验收矩阵

| 维度 | 正常样本       | 边界样本     | 失败样本       | 通过条件         |
| ---- | -------------- | ------------ | -------------- | ---------------- |
| 输入 | 固定版本与对象 | 只改一个阈值 | 注入明确故障   | 三组输入可重放   |
| 状态 | 逐步记录节点   | 检查临界切换 | 定位首个偏离   | 中间状态完整     |
| 结果 | 满足核心目标   | 无越界或卡死 | 被阶段门拒绝   | 修复后同输入通过 |
| 交付 | 编辑器基线     | 目标环境差异 | 构建或设备失败 | 真实目标环境复验 |

## 练习


## 小结

- 已逐项覆盖第七章的正式目录，没有用泛化高级主题替代原书。
- 已沿“目标平台 → 构建配置 → 输入适配 → 签名凭据 → 设备运行 → 商店证据”保存输入、中间状态、失败样本和交付证据。
- 已区分原书历史 API 与现代 Unity 等价实践，并保留迁移边界。
- 已通过正常、边界、失败和修复四组样本验证“平台分支只封装设备能力，核心规则和输入语义共用；签名、包标识和构建配置可审计且密钥不入库”。

## 术语表


## 来源与改编边界

