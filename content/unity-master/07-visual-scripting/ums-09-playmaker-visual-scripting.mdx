---
title: "第九章 使用playMaker插件"
type: C
section: "Unity 神技达人炼成记 · 扩展、可视化与优化"
order: 10
description: "第九章用 playMaker 把脚本规则显式化为有限状态机：State 保存当前阶段，Event 触发 Transition，Action 修改组件或调用能力，Variable 保存图内数据。分支、iTween、脚本通信和模板都服务于同一目标：让行为路径可见、可复用、可诊断，而不是用连线隐藏复杂性。"
demo: true
math: false
sourceUrl: "https://www.tenlong.com.tw/products/9787515354781"
draft: false
---


## 问题：这一单元在原书里解决什么

先预测：如果跳过“状态边界”，直接做到“模板复用”，最终结果会在哪个状态先失真？先写下输入、预期变化和拒绝条件，再操作图示。

第九章用 playMaker 把脚本规则显式化为有限状态机：State 保存当前阶段，Event 触发 Transition，Action 修改组件或调用能力，Variable 保存图内数据。分支、iTween、脚本通信和模板都服务于同一目标：让行为路径可见、可复用、可诊断，而不是用连线隐藏复杂性。


## 原书位置与覆盖边界

本页对应第九章，逐项保留中文版本目录中的主题：什么是playMaker、尝试简单的分支、通过iTween进行移动、获取事件并进行动作、Action种类一览、变更组件信息、通过变量进行条件分支、从脚本进行通信、向脚本通信、使2种状态迁移运行、使用模板、用了还不如不用的情况与诀窍。页面不把相近的现代功能冒充原书章节；对于已经失效的 API，会先解释原问题，再给出现代等价实践和迁移证据。

|   # | 官方主题                   | 最小掌握证据                     |
| --: | -------------------------- | -------------------------------- |
|   1 | 什么是playMaker            | 写出对象、状态、操作和可观察结果 |
|   2 | 尝试简单的分支             | 写出对象、状态、操作和可观察结果 |
|   3 | 通过iTween进行移动         | 写出对象、状态、操作和可观察结果 |
|   4 | 获取事件并进行动作         | 写出对象、状态、操作和可观察结果 |
|   5 | Action种类一览             | 写出对象、状态、操作和可观察结果 |
|   6 | 变更组件信息               | 写出对象、状态、操作和可观察结果 |
|   7 | 通过变量进行条件分支       | 写出对象、状态、操作和可观察结果 |
|   8 | 从脚本进行通信             | 写出对象、状态、操作和可观察结果 |
|   9 | 向脚本通信                 | 写出对象、状态、操作和可观察结果 |
|  10 | 使2种状态迁移运行          | 写出对象、状态、操作和可观察结果 |
|  11 | 使用模板                   | 写出对象、状态、操作和可观察结果 |
|  12 | 用了还不如不用的情况与诀窍 | 写出对象、状态、操作和可观察结果 |

## 核心知识与现代 Unity 对照

### 状态和事件必须具有业务语义

Idle、Moving、Alert 等状态应对应稳定行为阶段，Click、Arrived、Timeout 等事件说明发生了什么。若状态只叫 State1，迁移只靠匿名布尔值，图虽然可见却不可读。进入、每帧和退出 Action 要区分，避免副作用重复执行。

### 变量与脚本通信是接口

FSM Variable 应声明类型、默认值和所有者；脚本向 FSM 发送事件，FSM 向脚本调用明确方法。不要让双方通过对象名称和全局变量相互猜测。现代 Unity Visual Scripting 可以复现相同思想，但迁移时要保留状态、事件和副作用的对照表。

### 可视化不自动等于简单

iTween 移动、组件修改和条件分支很容易堆成长链。图超过单屏、出现大量跨图事件或同一状态承担多个职责时，应拆分层级状态机或把纯计算移回 C#。模板只提取稳定结构，过早模板化会把错误复制到所有实例。

在复刻时，最容易犯的错误是只追求最终画面或最终数值。正确的证据链必须同时保存环境、输入、首个状态变化、中间状态、失败原因和最终产物。版本差异可以改变按钮位置或 API 名称，却不能改变本单元要解决的对象关系和验收问题。

必须在实验记录中对应一个可观察状态，不能只作为名词出现。

必须在实验记录中对应一个可观察状态，不能只作为名词出现。

必须在实验记录中对应一个可观察状态，不能只作为名词出现。

## 三阶段操作证据链


## 可复现实验

用 playMaker 或 Unity Visual Scripting 制作门的 Closed、Opening、Open、Closing 四状态，脚本只发送 Use 与 Lock 事件；保存状态轨迹并注入连续点击和动画中断。

先用一个小型组件记录实验身份。它不替代截图和运行日志，但能把环境与对象写进同一条证据。

```csharp
using UnityEngine;

public sealed class EvidenceStamp : MonoBehaviour
{
    [SerializeField] private string unit = "ums-09-playmaker-visual-scripting";
    [SerializeField] private string variant = "baseline";

    private void Start()
    {
        Debug.Log($"unit={unit}; variant={variant}; unity={Application.unityVersion}; frame={Time.frameCount}");
    }
}
```

本单元的最小实现如下。运行前先预测输出，运行后记录实际状态和首个差异。

```csharp
using UnityEngine;

public sealed class DoorEventBridge : MonoBehaviour
{
    public event System.Action? UseRequested;
    public void RequestUse()
    {
        Debug.Log($"event=Use sender={name}");
        UseRequested?.Invoke();
    }
}
```

最后用签发函数拒绝缺少关键证据的结果。不要为了“绿灯”把条件删掉。

```csharp
public static class UnitGate
{
    public static bool CanShip(bool baseline, bool boundary, bool failureReplay, bool targetBuild)
    {
        return baseline && boundary && failureReplay && targetBuild;
    }
}
```

## 两个必须主动制造的失败样本


## 验收矩阵

| 维度 | 正常样本       | 边界样本     | 失败样本       | 通过条件         |
| ---- | -------------- | ------------ | -------------- | ---------------- |
| 输入 | 固定版本与对象 | 只改一个阈值 | 注入明确故障   | 三组输入可重放   |
| 状态 | 逐步记录节点   | 检查临界切换 | 定位首个偏离   | 中间状态完整     |
| 结果 | 满足核心目标   | 无越界或卡死 | 被阶段门拒绝   | 修复后同输入通过 |
| 交付 | 编辑器基线     | 目标环境差异 | 构建或设备失败 | 真实目标环境复验 |

## 练习


## 小结

- 已逐项覆盖第九章的正式目录，没有用泛化高级主题替代原书。
- 已沿“状态边界 → 事件入口 → 条件分支 → Action副作用 → 脚本通信 → 模板复用”保存输入、中间状态、失败样本和交付证据。
- 已区分原书历史 API 与现代 Unity 等价实践，并保留迁移边界。
- 已通过正常、边界、失败和修复四组样本验证“任一副作用都能追溯到状态、事件和条件，脚本通信接口有类型与发送者，模板实例无隐藏覆盖”。

## 术语表


## 来源与改编边界

