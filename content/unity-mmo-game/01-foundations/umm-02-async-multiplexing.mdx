---
title: "第2章 分身有术：异步和多路复用"
type: C
section: "Unity3D 网络游戏实战 · 扎基础"
order: 2
description: "比较异步回调、Poll 与 Select 的等待语义，用聊天室实验验证多个连接如何在不阻塞主循环的前提下推进。"
demo: true
math: false
sourceUrl: "https://luopeiyu.github.io/unity_net_book/"
draft: false
---


## 从分身有术：异步和多路复用的核心问题开始

并发网络程序的核心不是同时写更多代码，而是把等待显式化。异步回调把完成通知交给运行时，Poll 和 Select 把就绪集合交给循环；三者都不能替应用管理连接状态、部分收发、异常和取消。


## 官方目录命中与来源边界

本页按照《Unity3D网络游戏实战（第2版）》公开目录覆盖下列单元。章与节名称按第二版登记；安全、幂等、可观测性和现代 Unity 线程边界属于教学补充，用于今天重现原机制，不冒充原书新增目录。

|   # | 正式目录单元               | 本页逐项覆盖                                                                 |
| --: | -------------------------- | ---------------------------------------------------------------------------- |
|   1 | 2.1 什么样的代码是异步代码 | 本节标题即公开目录单元                                                       |
|   2 | 2.2 异步客户端             | 2.2.1 异步Connect、2.2.2 Show Me The Code、2.2.3 异步Receive、2.2.4 异步Send |
|   3 | 2.3 异步服务端             | 2.3.1 管理客户端、2.3.2 异步Accept、2.3.3 程序结构、2.3.4 代码展示           |
|   4 | 2.4 实践：做个聊天室       | 2.4.1 服务端、2.4.2 客户端、2.4.3 测试                                       |
|   5 | 2.5 状态检测Poll           | 2.5.1 什么是Poll、2.5.2 Poll客户端、2.5.3 Poll服务端                         |
|   6 | 2.6 多路复用Select         | 2.6.1 什么是多路复用、2.6.2 Select服务端、2.6.3 Select客户端                 |

公开目录的第一项是 **2.1 什么样的代码是异步代码**。本页不把相邻章节或第一版目录并入当前章；跨章依赖会在实验中引用，但仍由其正式页面负责讲解和验收。

## 核心概念与因果关系

、、、、。
这些术语必须落到输入、状态、输出和失败证据：读者应能指出谁拥有状态、哪个边界拒绝非法输入、哪个版本或序号阻止旧消息，以及哪个指标证明队列没有把成本推给下一阶段。

|   # | 机制                | 可观察解释                                                                                                         |
| --: | ------------------- | ------------------------------------------------------------------------------------------------------------------ |
|   1 | 异步不等于多线程    | 异步描述调用方无需原地等待结果；回调可能由系统线程触发，也可能由事件循环触发。业务状态仍应在明确的线程或阶段消费。 |
|   2 | 每个操作都有代际    | 连接、接收和发送可能在关闭后才回调。回调必须校验当前 Socket 与连接代际，防止旧完成写入复用后的对象。               |
|   3 | Poll 是单端点查询   | Poll 可以检查读、写或错误条件，但反复轮询多个连接会产生扫描成本，也不能替代真正的读取和错误处理。                  |
|   4 | Select 返回就绪集合 | 应用把监听端和客户端放入读集合，再只处理本轮就绪项。就绪表示现在执行不会按原条件阻塞，不表示能读出一条完整消息。   |
|   5 | 聊天室验证广播背压  | 一个慢客户端若拥有无界发送队列，会拖垮整个服务端。广播必须给每个连接独立预算并定义丢弃或断开策略。                 |

### 异步不等于多线程

异步描述调用方无需原地等待结果；回调可能由系统线程触发，也可能由事件循环触发。业务状态仍应在明确的线程或阶段消费。

验证时固定客户端版本、服务端版本、协议版本和输入脚本，先保存正常轨迹，再只改变一个边界条件。最终画面相同不代表过程正确；还要比较消息序号、状态版本、队列水位、拒绝原因和恢复动作。

### 每个操作都有代际

连接、接收和发送可能在关闭后才回调。回调必须校验当前 Socket 与连接代际，防止旧完成写入复用后的对象。

验证时固定客户端版本、服务端版本、协议版本和输入脚本，先保存正常轨迹，再只改变一个边界条件。最终画面相同不代表过程正确；还要比较消息序号、状态版本、队列水位、拒绝原因和恢复动作。

### Poll 是单端点查询

Poll 可以检查读、写或错误条件，但反复轮询多个连接会产生扫描成本，也不能替代真正的读取和错误处理。

验证时固定客户端版本、服务端版本、协议版本和输入脚本，先保存正常轨迹，再只改变一个边界条件。最终画面相同不代表过程正确；还要比较消息序号、状态版本、队列水位、拒绝原因和恢复动作。

### Select 返回就绪集合

应用把监听端和客户端放入读集合，再只处理本轮就绪项。就绪表示现在执行不会按原条件阻塞，不表示能读出一条完整消息。

验证时固定客户端版本、服务端版本、协议版本和输入脚本，先保存正常轨迹，再只改变一个边界条件。最终画面相同不代表过程正确；还要比较消息序号、状态版本、队列水位、拒绝原因和恢复动作。

### 聊天室验证广播背压

一个慢客户端若拥有无界发送队列，会拖垮整个服务端。广播必须给每个连接独立预算并定义丢弃或断开策略。

验证时固定客户端版本、服务端版本、协议版本和输入脚本，先保存正常轨迹，再只改变一个边界条件。最终画面相同不代表过程正确；还要比较消息序号、状态版本、队列水位、拒绝原因和恢复动作。

## 六阶段证据链


## 可复现实验

用同一聊天室脚本分别驱动异步回调和 Select 服务端：20 个正常客户端持续发言，1 个客户端停止读取。先预测哪种模型吞吐更高，再记录每连接发送队列、广播延迟、回调线程和关闭后未完成操作。

### 最小状态机

```csharp
public enum EvidenceStage {
  "定义连接状态",
  "发起异步操作",
  "收集就绪集合",
  "消费部分读写",
  "施加队列背压",
  "取消排空并签发"
}

public sealed record TraceEvent(
    string TraceId,
    EvidenceStage Stage,
    long Sequence,
    string State,
    string Decision);
```

### 边界驱动器

```csharp
var samples = new[] { "baseline", "boundary", "failure" };
foreach (var sample in samples) {
    ResetFixture(seed: 20260716);
    RunScenario(sample);
    AssertSingleOwner();
    AssertBoundedQueues();
    ExportTrace(sample);
}
```

### 证据契约

```text
trace_id,client_seq,server_seq,state_version,queue_depth,first_divergence,decision
baseline-01,42,42,7,3,none,pass
boundary-01,43,43,8,31,none,pass
failure-01,44,43,8,32,消费部分读写,reject
```

实现代码只是实验的一部分。每次运行还要保存客户端与服务端版本、协议摘要、随机种子、样本类型、首偏离节点、恢复动作和最终决策。若相同输入不能重放，结论不能进入交付清单。

## 两个必须主动制造的失败


## 验收矩阵

| 维度 | 正常样本     | 边界样本               | 失败样本           | 通过条件               |
| ---- | ------------ | ---------------------- | ------------------ | ---------------------- |
| 输入 | 固定合法流程 | 时序、容量或重复临界值 | 畸形、断线或旧代际 | 分类清晰且可重放       |
| 状态 | 单调推进     | 达到预算但不越界       | 在首偏离点停止     | 单一所有者、无隐式写入 |
| 输出 | 双端证据一致 | 退化可解释             | 拒绝、补偿或回滚   | 不产生第二份事实       |
| 恢复 | 下一流程正常 | 压力下降后收敛         | 同输入修复后通过   | 无幽灵状态和重复结果   |

本页签发不变量是：**同一异步操作只完成一次；业务状态只在指定消费线程修改；慢客户端不会让全局队列越过预算。** 任何截图、单次演示或平均值都不能替代它。

## 练习


## 小结

- **异步不等于多线程**：异步描述调用方无需原地等待结果；回调可能由系统线程触发，也可能由事件循环触发。业务状态仍应在明确的线程或阶段消费。
- **每个操作都有代际**：连接、接收和发送可能在关闭后才回调。回调必须校验当前 Socket 与连接代际，防止旧完成写入复用后的对象。
- **Poll 是单端点查询**：Poll 可以检查读、写或错误条件，但反复轮询多个连接会产生扫描成本，也不能替代真正的读取和错误处理。
- **Select 返回就绪集合**：应用把监听端和客户端放入读集合，再只处理本轮就绪项。就绪表示现在执行不会按原条件阻塞，不表示能读出一条完整消息。
- **聊天室验证广播背压**：一个慢客户端若拥有无界发送队列，会拖垮整个服务端。广播必须给每个连接独立预算并定义丢弃或断开策略。
- 已用正常、边界和失败样本验证“同一异步操作只完成一次；业务状态只在指定消费线程修改；慢客户端不会让全局队列越过预算。”。
- 已把第二版正式目录、第一版差异和现代教学补充分开记录。

## 术语表


## 来源与改编边界

