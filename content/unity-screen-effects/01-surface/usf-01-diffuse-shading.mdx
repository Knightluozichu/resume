---
title: "第1章 漫反射着色"
type: C
section: "Unity屏幕特效秘笈 · 表面与反射"
order: 1
description: "沿第一版正式目录掌握表面着色器、Properties、Lambert、Half Lambert、Ramp BRDF。"
demo: true
math: true
sourceUrl: "https://books.google.com/books/about/Unity%E7%9D%80%E8%89%B2%E5%99%A8%E5%92%8C%E5%B1%8F%E5%B9%95%E7%89%B9%E6%95%88%E5%BC%80%E5%8F%91%E7%A7%98.html?id=kvHbjgEACAAJ"
draft: false
---


## 先预测，再观察

先预测顶点、片元、颜色、深度、Draw次数或帧时间会如何变化，再只调整一个变量观察变化。从最小Surface Shader开始，依次加入Properties、输入结构和自定义光照函数。比较Lambert、Half Lambert、渐变控制和二维假BRDF，明确每种响应的输入、范围和美术目标。 本章用、、、、建立输入、计算、状态与证据之间的关系。


## 第一版权威目录定位

本页一一对应Kenny Lammers《Unity Shaders and Effects Cookbook》第一版中译本《Unity着色器和屏幕特效开发秘笈》第1章 漫反射着色。书目确认机械工业出版社2014年出版、晏伟译、ISBN 9787111480563；正式目录确认全书11章。原书基于早期Unity内置渲染管线，现代Unity项目应复用原理并单独映射API。

### 原章核心配方

1. 创建基本表面着色器
2. 添加并使用属性
3. 自定义漫反射模型
4. Half Lambert
5. 渐变纹理
6. 2D渐变假BRDF

### 五个核心对象

1. **表面着色器**：由Unity生成底层光照Pass的高层材质描述。
2. **Properties**：把颜色、范围和纹理暴露给材质面板的参数块。
3. **Lambert**：按法线与光线夹角余弦计算理想漫反射的模型。
4. **Half Lambert**：把负余弦平移到可见范围的非物理风格化模型。
5. **Ramp BRDF**：用一维或二维渐变纹理重新映射光照响应。

## 数据流与公式

从最小Surface Shader开始，依次加入Properties、输入结构和自定义光照函数。比较Lambert、Half Lambert、渐变控制和二维假BRDF，明确每种响应的输入、范围和美术目标。 先画出材质属性、纹理、Surface或顶点函数、生成Pass、脚本回调、RenderTexture和固定状态的生产者到消费者链，再把本章对象放到对应阶段。

$$
Ld=albedo*lightColor*max(0,dot(n,l))
$$

$$
halfLambert=0.5*dot(n,l)+0.5
$$

$$
ramp=sample(R,ndl)
$$

$$
output=diffuse+ambient
$$

公式中的方向、颜色、UV、时间与深度必须标注空间、范围和单位。Surface输出或全屏片元输出只是候选结果，最终仍受Pass选择、深度、混合、目标格式和颜色空间影响。

## 最小实现

```shaderlab
#pragma surface surf CustomDiffuse
```

实现顺序固定为常量输出、输入可视化、核心公式、材质或脚本绑定和固定状态。先确认Shader支持、实际Pass与源目标正确，再解释画面变化。


## Unity脚本与材质契约

```csharp
material.SetFloat("_DebugMode", debugMode);
material.SetTexture("_MainTex", sourceTexture);
Debug.Assert(material.shader.isSupported);
```

保存Unity版本、相机、灯光、网格、材质关键字、纹理导入设置、RenderTexture格式与调用次序。对于屏幕效果还要保存源图和每个中间目标；对于Surface Shader还要检查生成代码与实际渲染路径。

## 边界、精度与性能

测试零向量、退化UV、镜像UV、负缩放、透明交叉、纯黑纯白、HDR颜色、不同宽高比、多效果串联和高过度绘制。性能实验固定目标设备、分辨率、VSync与场景，先判断CPU还是GPU受限，再定位具体Draw、采样与Shader阶段。

```text
unity: exact editor and player version
pipeline: Built-in and named rendering path
resources: mesh, texture import, material and target format
state: queue, depth, cull, blend and pass order
evidence: image diff, frame debugger, GPU capture, CPU/GPU ms
failure: smallest replayable counterexample
```

1. 用常量颜色或原样Blit确认材质、脚本、Pass和目标可执行。
2. 可视化UV、法线、深度、遮罩、单个光照项或中间纹理。
3. 打开本章效果，保存基线、候选和参数。
4. 构造边界与失败输入，用帧调试和GPU捕获解释差异。

## 验收证书

证书包含源码版本、Unity与图形API、脚本绑定、资源哈希、Pass状态、源图和中间图、GPU事件、CPU/GPU帧时间与失败反例。把Half Lambert或渐变贴图称为物理漫反射，没有说明它们为了风格化改变了响应曲线。 修复后必须用同一输入重放，并指出变化发生在哪个阶段。

## 常见误区


## 本章回顾

本页覆盖表面着色器、Properties、Lambert、Half Lambert、Ramp BRDF。掌握标准是能画出数据流，写出最小实现，说明材质与脚本契约，再用正确性、边界、精度和性能证据复现结论。


## 术语表


