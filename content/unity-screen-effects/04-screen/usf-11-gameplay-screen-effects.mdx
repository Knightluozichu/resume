---
title: "第11章 游戏的可玩性和屏幕特效"
type: C
section: "Unity屏幕特效秘笈 · 屏幕效果"
order: 11
description: "沿第一版正式目录掌握老电影效果、夜视效果、时间噪声、暗角、玩法反馈。"
demo: true
math: true
sourceUrl: "https://books.google.com/books/about/Unity%E7%9D%80%E8%89%B2%E5%99%A8%E5%92%8C%E5%B1%8F%E5%B9%95%E7%89%B9%E6%95%88%E5%BC%80%E5%8F%91%E7%A7%98.html?id=kvHbjgEACAAJ"
draft: false
---


## 先预测，再观察

先预测顶点、片元、颜色、深度、Draw次数或帧时间会如何变化，再只调整一个变量观察变化。分别拆解老电影与夜视效果，把色调、颗粒、划痕、抖动、增益和暗角做成可开关项。效果必须服务玩法：在暗场、亮场、UI、快速移动和低性能设备上验证可读性。 本章用、、、、建立输入、计算、状态与证据之间的关系。


## 第一版权威目录定位

本页一一对应Kenny Lammers《Unity Shaders and Effects Cookbook》第一版中译本《Unity着色器和屏幕特效开发秘笈》第11章 游戏的可玩性和屏幕特效。书目确认机械工业出版社2014年出版、晏伟译、ISBN 9787111480563；正式目录确认全书11章。原书基于早期Unity内置渲染管线，现代Unity项目应复用原理并单独映射API。

### 原章核心配方

1. 老电影风格
2. 夜视风格
3. 时间噪声
4. 暗角
5. 玩法可读性

### 五个核心对象

1. **老电影效果**：组合色调、颗粒、划痕、抖动和暗角形成的风格化后处理。
2. **夜视效果**：组合绿色响应、增益、噪声和暗角模拟夜视设备的效果。
3. **时间噪声**：随时间变化但需要控制闪烁频率和重复性的随机纹理。
4. **暗角**：按屏幕中心距离压暗边缘的合成项。
5. **玩法反馈**：让视觉效果表达受伤、装备、环境或状态而不遮蔽关键信息。

## 数据流与公式

分别拆解老电影与夜视效果，把色调、颗粒、划痕、抖动、增益和暗角做成可开关项。效果必须服务玩法：在暗场、亮场、UI、快速移动和低性能设备上验证可读性。 先画出材质属性、纹理、Surface或顶点函数、生成Pass、脚本回调、RenderTexture和固定状态的生产者到消费者链，再把本章对象放到对应阶段。

$$
oldFilm=tone+grain+scratch+vignette
$$

$$
nightVision=gain*luma*green+noise
$$

$$
vignette=smoothstep(r1,r0,distance(uv,0.5))
$$

$$
final=lerp(source,effect,intensity)
$$

公式中的方向、颜色、UV、时间与深度必须标注空间、范围和单位。Surface输出或全屏片元输出只是候选结果，最终仍受Pass选择、深度、混合、目标格式和颜色空间影响。

## 最小实现

```shaderlab
float3 nightVision=float3(0.1,1.0,0.2)*luminance*gain+noise;
```

实现顺序固定为常量输出、输入可视化、核心公式、材质或脚本绑定和固定状态。先确认Shader支持、实际Pass与源目标正确，再解释画面变化。


## Unity脚本与材质契约

```csharp
material.SetFloat("_DebugMode", debugMode);
material.SetTexture("_MainTex", sourceTexture);
Debug.Assert(material.shader.isSupported);
```

保存Unity版本、相机、灯光、网格、材质关键字、纹理导入设置、RenderTexture格式与调用次序。对于屏幕效果还要保存源图和每个中间目标；对于Surface Shader还要检查生成代码与实际渲染路径。

## 边界、精度与性能

测试零向量、退化UV、镜像UV、负缩放、透明交叉、纯黑纯白、HDR颜色、不同宽高比、多效果串联和高过度绘制。性能实验固定目标设备、分辨率、VSync与场景，先判断CPU还是GPU受限，再定位具体Draw、采样与Shader阶段。

```text
unity: exact editor and player version
pipeline: Built-in and named rendering path
resources: mesh, texture import, material and target format
state: queue, depth, cull, blend and pass order
evidence: image diff, frame debugger, GPU capture, CPU/GPU ms
failure: smallest replayable counterexample
```

1. 用常量颜色或原样Blit确认材质、脚本、Pass和目标可执行。
2. 可视化UV、法线、深度、遮罩、单个光照项或中间纹理。
3. 打开本章效果，保存基线、候选和参数。
4. 构造边界与失败输入，用帧调试和GPU捕获解释差异。

## 验收证书

证书包含源码版本、Unity与图形API、脚本绑定、资源哈希、Pass状态、源图和中间图、GPU事件、CPU/GPU帧时间与失败反例。只把画面乘绿色就称为夜视，没有增益、噪声、视野边界和玩法状态控制。 修复后必须用同一输入重放，并指出变化发生在哪个阶段。

## 常见误区


## 本章回顾

本页覆盖老电影效果、夜视效果、时间噪声、暗角、玩法反馈。掌握标准是能画出数据流，写出最小实现，说明材质与脚本契约，再用正确性、边界、精度和性能证据复现结论。


## 术语表


