---
title: "第3章 Unity Shader基础"
type: C
section: "Unity Shader精要 · 基础篇"
order: 3
description: "区分材质与Shader，掌握ShaderLab的Properties、SubShader、Pass、Fallback及三种Shader形式。"
demo: true
math: true
sourceUrl: "https://www.cnblogs.com/xiaowangba/p/6314640.html"
draft: true
---

import { Objectives, Callout, Attribution, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step } from "@/components/mdx/mdx-components";

<Objectives>
- 能解释Material、ShaderLab和Properties在Unity渲染链中的位置
- 能推导本页公式并实现最小Shader，用调试视图比较中间值
- 能复现Pass，用Frame Debugger、边界场景和性能数据验收
</Objectives>

## 从效果开始

先预测画面、Draw Call和中间缓冲会如何变化，再调整一个参数观察变化。Unity先按硬件支持选择SubShader，再按Pass执行绘制。Properties只是编辑器和序列化契约，GPU变量还需在程序块中声明；Tags用于告诉渲染器队列和用途，渲染状态则控制剔除、深度与混合。Surface Shader由Unity生成底层Pass，顶点片元形式提供更直接控制，固定函数形式已属于旧路径。 本页用<Term def="保存Shader引用与属性实例值，并参与渲染状态选择的Unity资源。">Material</Term>、<Term def="Unity用于声明属性、子着色器、Pass、标签和渲染状态的描述语言。">ShaderLab</Term>、<Term def="暴露给材质面板和脚本的参数声明区。">Properties</Term>、<Term def="面向一类硬件能力、包含标签和一个或多个Pass的候选实现。">SubShader</Term>、<Term def="定义一次具体绘制的状态与GPU程序入口。">Pass</Term>串联视觉目标、GPU数据流、渲染状态和验收证据。

<UsebUnityShaderBasicsPipelineLab />

## 第一版权威目录定位

本页一一对应冯乐乐《Unity Shader入门精要》第一版第3章 Unity Shader基础。作者发布的完整目录明确列出5篇20章，人民邮电出版社书目记录确认2016年第一版、ISBN 9787115423054和369页，作者GitHub保存随书源代码与勘误入口。

### 原书小节覆盖

1. 3.1 Unity Shader概述
2. 3.2 ShaderLab
3. 3.3 Unity Shader的结构
4. 3.4 Surface、Vertex/Fragment与Fixed Function形式
5. 3.5 本书采用的形式
6. 3.6 Unity Shader与CG/HLSL的关系

### 核心对象

1. **Material**：保存Shader引用与属性实例值，并参与渲染状态选择的Unity资源。
2. **ShaderLab**：Unity用于声明属性、子着色器、Pass、标签和渲染状态的描述语言。
3. **Properties**：暴露给材质面板和脚本的参数声明区。
4. **SubShader**：面向一类硬件能力、包含标签和一个或多个Pass的候选实现。
5. **Pass**：定义一次具体绘制的状态与GPU程序入口。

## 数据流与公式

任何Shader效果都先写清五项契约：输入属性来自哪里，当前值位于哪个坐标或色彩空间，可编程阶段输出什么，ShaderLab状态如何合并结果，以及目标平台和Unity版本是什么。

$$
Material=(Shader,Properties)
$$

$$
SubShader^*=\arg\max_{supported} priority
$$

$$
Draws=\sum_{p\in Passes}1
$$

$$
Variant=Keywords\times PassTypes
$$

Unity先按硬件支持选择SubShader，再按Pass执行绘制。Properties只是编辑器和序列化契约，GPU变量还需在程序块中声明；Tags用于告诉渲染器队列和用途，渲染状态则控制剔除、深度与混合。Surface Shader由Unity生成底层Pass，顶点片元形式提供更直接控制，固定函数形式已属于旧路径。 公式不是孤立记忆项；每个向量都标注空间，每个颜色都标注线性或sRGB，每个深度都标注设备、NDC或观察距离，每个纹理通道都标注数据语义。

## 最小Shader实现

```hlsl
Shader "Custom/Minimal" {
  Properties { _Color ("Color", Color) = (1,1,1,1) }
  SubShader { Pass { /* CGPROGRAM ... */ } }
  Fallback Off
}
```

实现顺序固定为常量颜色、输入可视化、核心公式、材质参数和优化。每新增一个步骤都保留开关，这样画面异常时可以二分定位。编译成功只证明语法和部分类型成立，不证明空间、状态或色彩处理正确。

<UsebUnityShaderBasicsCompareLab />

## Unity侧设置与渲染状态

```csharp
var material = renderer.sharedMaterial;
material.SetFloat("_DebugMode", debugMode);
material.EnableKeyword(debugMode > 0 ? "DEBUG_VIEW" : "BASELINE_VIEW");
Debug.Assert(material.shader.isSupported);
```

材质、相机和光源属于实验输入，不能只保存Shader文件。用Frame Debugger检查实际选择的SubShader和Pass、关键字变体、渲染队列、RenderTarget、深度写入和混合；用Profiler或GPU捕获确认成本来自提交、顶点、片元还是带宽。

## 边界与平台差异

测试零向量、负缩放、非统一缩放、UV边界、透明物体交叉、近远裁剪面、HDR亮度、不同纹理导入设置和移动精度。原书以Unity 5时代内置管线为语境；迁移到当前Unity、URP或HDRP时，应把概念映射和API替换单独记录，不能把新管线行为倒写成原书内容。

```text
scene: fixed reference scene
unity: exact editor version
pipeline: built-in or named SRP
platform: graphics API and GPU
evidence: image diff + frame events + timings
```

1. 建立只有一个网格、一个相机和一个主光的参考场景，保存Unity版本与平台。
2. 输出UV、法线、深度或光照项等中间值，确认数据空间和范围。
3. 启用本章核心效果，用Frame Debugger逐Pass检查输入、状态和目标。
4. 构造边界与失败场景，比较参考图、像素差异、Draw Call和GPU时间。

## 验收证书

验收包至少包含Shader与脚本、材质参数、测试场景、参考图和候选图、Frame Debugger事件、编译关键字、目标平台、帧时间与反例。只在Properties声明参数却没有在HLSL程序中声明同名变量，误以为GPU会自动获得值。 修复后必须重放同一输入，确认画面和性能变化都能由证据解释。

## 常见误区

<Callout type="trap" title="只看最终截图">只在Properties声明参数却没有在HLSL程序中声明同名变量，误以为GPU会自动获得值。</Callout>

<Callout type="trap" title="忽略版本与状态">同一段HLSL在不同Unity版本、渲染管线、关键字和纹理导入设置下可能走不同Pass或解释不同数据。修复时把版本、平台、状态和资源设置纳入实验输入。</Callout>

## 本章回顾

本页覆盖Material、ShaderLab、Properties、SubShader、Pass。掌握标准是能从视觉目标画出数据流，写出最小实现，逐阶段可视化中间值，再用渲染状态、边界样例和性能数据完成复现。

<Exercises>
**问题1：Material与ShaderLab如何连接？**
<Answer>保存Shader引用与属性实例值，并参与渲染状态选择的Unity资源。 Unity用于声明属性、子着色器、Pass、标签和渲染状态的描述语言。 回答时标注执行阶段和输入空间。</Answer>

**问题2：如何调试Properties？**
<Answer>先输出常量，再把相关中间值映射为颜色；逐项关闭纹理、光照和状态，最后用Frame Debugger确认实际Pass。</Answer>

**问题3：如何验收Pass？**
<Answer>定义一次具体绘制的状态与GPU程序入口。 同时保存正常图、边界图、失败反例、状态记录和性能基线。</Answer>
</Exercises>

## 术语表

<Glossary>
  <GlossaryItem term="Material" definition="保存Shader引用与属性实例值，并参与渲染状态选择的Unity资源。" />
  <GlossaryItem term="ShaderLab" definition="Unity用于声明属性、子着色器、Pass、标签和渲染状态的描述语言。" />
  <GlossaryItem term="Properties" definition="暴露给材质面板和脚本的参数声明区。" />
  <GlossaryItem term="SubShader" definition="面向一类硬件能力、包含标签和一个或多个Pass的候选实现。" />
  <GlossaryItem term="Pass" definition="定义一次具体绘制的状态与GPU程序入口。" />
  <GlossaryItem term="Shader实验单" definition="记录Unity版本、目标平台、场景、材质、源码、截图和性能的一页式证据。" />
</Glossary>

<Attribution adaptedFrom="冯乐乐 · Unity Shader入门精要 · 第3章 Unity Shader基础" adaptedUrl="https://www.cnblogs.com/xiaowangba/p/6314640.html" />
