---
title: "第4章 学习Shader所需的数学基础"
type: C
section: "Unity Shader精要 · 基础篇"
order: 4
description: "用坐标系、向量、矩阵、齐次变换和法线变换建立Shader空间推导的可核查基础。"
demo: true
math: true
sourceUrl: "https://www.cnblogs.com/xiaowangba/p/6314640.html"
draft: true
---

import { Objectives, Callout, Attribution, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step } from "@/components/mdx/mdx-components";

<Objectives>
- 能解释坐标空间、点积和叉积在Unity渲染链中的位置
- 能推导本页公式并实现最小Shader，用调试视图比较中间值
- 能复现逆转置矩阵，用Frame Debugger、边界场景和性能数据验收
</Objectives>

## 从效果开始

先预测画面、Draw Call和中间缓冲会如何变化，再调整一个参数观察变化。点和方向不能混用：齐次分量一和零分别控制平移是否生效。矩阵乘法顺序取决于行列约定，模型、世界、观察、裁剪和屏幕空间必须逐步标注。法线在非统一缩放下不能直接乘模型矩阵，应乘模型矩阵逆矩阵的转置并重新归一化。 本页用<Term def="为点和方向选择原点、基向量与手性后形成的表示系统。">坐标空间</Term>、<Term def="衡量两个向量方向一致程度并用于投影和夹角计算的标量运算。">点积</Term>、<Term def="产生垂直于两个输入向量、方向由手性决定的向量运算。">叉积</Term>、<Term def="用额外分量统一表达线性变换与平移的坐标表示。">齐次坐标</Term>、<Term def="在非统一缩放下保持法线与切平面正交的变换矩阵。">逆转置矩阵</Term>串联视觉目标、GPU数据流、渲染状态和验收证据。

<UsebShaderMathematicsPipelineLab />

## 第一版权威目录定位

本页一一对应冯乐乐《Unity Shader入门精要》第一版第4章 学习Shader所需的数学基础。作者发布的完整目录明确列出5篇20章，人民邮电出版社书目记录确认2016年第一版、ISBN 9787115423054和369页，作者GitHub保存随书源代码与勘误入口。

### 原书小节覆盖

1. 4.1 背景：农场游戏
2. 4.2 笛卡尔坐标系
3. 4.3 点和矢量
4. 4.4 矩阵
5. 4.5 矩阵的几何意义
6. 4.6 坐标空间
7. 4.7 法线变换

### 核心对象

1. **坐标空间**：为点和方向选择原点、基向量与手性后形成的表示系统。
2. **点积**：衡量两个向量方向一致程度并用于投影和夹角计算的标量运算。
3. **叉积**：产生垂直于两个输入向量、方向由手性决定的向量运算。
4. **齐次坐标**：用额外分量统一表达线性变换与平移的坐标表示。
5. **逆转置矩阵**：在非统一缩放下保持法线与切平面正交的变换矩阵。

## 数据流与公式

任何Shader效果都先写清五项契约：输入属性来自哪里，当前值位于哪个坐标或色彩空间，可编程阶段输出什么，ShaderLab状态如何合并结果，以及目标平台和Unity版本是什么。

$$
a\cdot b=\|a\|\|b\|\cos\theta
$$

$$
n=a\times b
$$

$$
p_w=M_mp_o
$$

$$
n_w=\left(M_m^{-1}\right)^Tn_o
$$

点和方向不能混用：齐次分量一和零分别控制平移是否生效。矩阵乘法顺序取决于行列约定，模型、世界、观察、裁剪和屏幕空间必须逐步标注。法线在非统一缩放下不能直接乘模型矩阵，应乘模型矩阵逆矩阵的转置并重新归一化。 公式不是孤立记忆项；每个向量都标注空间，每个颜色都标注线性或sRGB，每个深度都标注设备、NDC或观察距离，每个纹理通道都标注数据语义。

## 最小Shader实现

```hlsl
float3 worldPos = mul(unity_ObjectToWorld, v.vertex).xyz;
float3 worldNormal = UnityObjectToWorldNormal(v.normal);
float ndotl = saturate(dot(normalize(worldNormal), lightDir));
```

实现顺序固定为常量颜色、输入可视化、核心公式、材质参数和优化。每新增一个步骤都保留开关，这样画面异常时可以二分定位。编译成功只证明语法和部分类型成立，不证明空间、状态或色彩处理正确。

<UsebShaderMathematicsCompareLab />

## Unity侧设置与渲染状态

```csharp
var material = renderer.sharedMaterial;
material.SetFloat("_DebugMode", debugMode);
material.EnableKeyword(debugMode > 0 ? "DEBUG_VIEW" : "BASELINE_VIEW");
Debug.Assert(material.shader.isSupported);
```

材质、相机和光源属于实验输入，不能只保存Shader文件。用Frame Debugger检查实际选择的SubShader和Pass、关键字变体、渲染队列、RenderTarget、深度写入和混合；用Profiler或GPU捕获确认成本来自提交、顶点、片元还是带宽。

## 边界与平台差异

测试零向量、负缩放、非统一缩放、UV边界、透明物体交叉、近远裁剪面、HDR亮度、不同纹理导入设置和移动精度。原书以Unity 5时代内置管线为语境；迁移到当前Unity、URP或HDRP时，应把概念映射和API替换单独记录，不能把新管线行为倒写成原书内容。

```text
scene: fixed reference scene
unity: exact editor version
pipeline: built-in or named SRP
platform: graphics API and GPU
evidence: image diff + frame events + timings
```

1. 建立只有一个网格、一个相机和一个主光的参考场景，保存Unity版本与平台。
2. 输出UV、法线、深度或光照项等中间值，确认数据空间和范围。
3. 启用本章核心效果，用Frame Debugger逐Pass检查输入、状态和目标。
4. 构造边界与失败场景，比较参考图、像素差异、Draw Call和GPU时间。

## 验收证书

验收包至少包含Shader与脚本、材质参数、测试场景、参考图和候选图、Frame Debugger事件、编译关键字、目标平台、帧时间与反例。对法线使用和顶点相同的模型矩阵，在非统一缩放模型上得到倾斜且长度错误的光照。 修复后必须重放同一输入，确认画面和性能变化都能由证据解释。

## 常见误区

<Callout type="trap" title="只看最终截图">对法线使用和顶点相同的模型矩阵，在非统一缩放模型上得到倾斜且长度错误的光照。</Callout>

<Callout type="trap" title="忽略版本与状态">同一段HLSL在不同Unity版本、渲染管线、关键字和纹理导入设置下可能走不同Pass或解释不同数据。修复时把版本、平台、状态和资源设置纳入实验输入。</Callout>

## 本章回顾

本页覆盖坐标空间、点积、叉积、齐次坐标、逆转置矩阵。掌握标准是能从视觉目标画出数据流，写出最小实现，逐阶段可视化中间值，再用渲染状态、边界样例和性能数据完成复现。

<Exercises>
**问题1：坐标空间与点积如何连接？**
<Answer>为点和方向选择原点、基向量与手性后形成的表示系统。 衡量两个向量方向一致程度并用于投影和夹角计算的标量运算。 回答时标注执行阶段和输入空间。</Answer>

**问题2：如何调试叉积？**
<Answer>先输出常量，再把相关中间值映射为颜色；逐项关闭纹理、光照和状态，最后用Frame Debugger确认实际Pass。</Answer>

**问题3：如何验收逆转置矩阵？**
<Answer>在非统一缩放下保持法线与切平面正交的变换矩阵。 同时保存正常图、边界图、失败反例、状态记录和性能基线。</Answer>
</Exercises>

## 术语表

<Glossary>
  <GlossaryItem term="坐标空间" definition="为点和方向选择原点、基向量与手性后形成的表示系统。" />
  <GlossaryItem term="点积" definition="衡量两个向量方向一致程度并用于投影和夹角计算的标量运算。" />
  <GlossaryItem term="叉积" definition="产生垂直于两个输入向量、方向由手性决定的向量运算。" />
  <GlossaryItem term="齐次坐标" definition="用额外分量统一表达线性变换与平移的坐标表示。" />
  <GlossaryItem term="逆转置矩阵" definition="在非统一缩放下保持法线与切平面正交的变换矩阵。" />
  <GlossaryItem term="Shader实验单" definition="记录Unity版本、目标平台、场景、材质、源码、截图和性能的一页式证据。" />
</Glossary>

<Attribution adaptedFrom="冯乐乐 · Unity Shader入门精要 · 第4章 学习Shader所需的数学基础" adaptedUrl="https://www.cnblogs.com/xiaowangba/p/6314640.html" />
