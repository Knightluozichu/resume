---
title: "第8章 透明效果"
type: C
section: "Unity Shader精要 · 初级篇"
order: 8
description: "围绕渲染顺序、透明度测试、透明度混合、深度写入和双面渲染建立透明效果判断链。"
demo: true
math: true
sourceUrl: "https://www.cnblogs.com/xiaowangba/p/6314640.html"
draft: false
---


## 从效果开始

先预测画面、Draw Call和中间缓冲会如何变化，再调整一个参数观察变化。不透明对象通常先写深度再着色，半透明对象常关闭深度写入并从后向前混合。透明度测试仍可按不透明流程排序，但边缘是硬切；混合提供连续透明，却无法仅靠对象中心排序解决相交表面。需要深度预写、拆分网格或其他近似时，应明确视觉与成本取舍。 本页用、、、、串联视觉目标、GPU数据流、渲染状态和验收证据。


## 第一版权威目录定位

本页一一对应冯乐乐《Unity Shader入门精要》第一版第8章 透明效果。作者发布的完整目录明确列出5篇20章，人民邮电出版社书目记录确认2016年第一版、ISBN 9787115423054和369页，作者GitHub保存随书源代码与勘误入口。

### 原书小节覆盖

1. 8.1 为什么渲染顺序很重要
2. 8.2 Unity Shader的渲染顺序
3. 8.3 透明度测试
4. 8.4 透明度混合
5. 8.5 开启深度写入的半透明
6. 8.6 ShaderLab的混合命令

### 核心对象

1. **渲染队列**：按材质类别和排序规则决定对象提交先后的Unity机制。
2. **透明度测试**：按阈值丢弃片元并保留深度写入的二值透明方法。
3. **透明度混合**：按源和目标因子组合当前片元与帧缓冲颜色的方法。
4. **深度写入**：通过测试后是否把当前深度写入深度缓冲的状态。
5. **排序歧义**：物体级远近排序无法正确表示互相穿插或自遮挡透明表面的情况。

## 数据流与公式

任何Shader效果都先写清五项契约：输入属性来自哪里，当前值位于哪个坐标或色彩空间，可编程阶段输出什么，ShaderLab状态如何合并结果，以及目标平台和Unity版本是什么。

$$
C_o=\alpha_sC_s+(1-\alpha_s)C_d
$$

$$
\alpha_o=\alpha_s+(1-\alpha_s)\alpha_d
$$

$$
clip(\alpha-cutoff)
$$

$$
order: opaque\rightarrow alphaTest\rightarrow transparent
$$

不透明对象通常先写深度再着色，半透明对象常关闭深度写入并从后向前混合。透明度测试仍可按不透明流程排序，但边缘是硬切；混合提供连续透明，却无法仅靠对象中心排序解决相交表面。需要深度预写、拆分网格或其他近似时，应明确视觉与成本取舍。 公式不是孤立记忆项；每个向量都标注空间，每个颜色都标注线性或sRGB，每个深度都标注设备、NDC或观察距离，每个纹理通道都标注数据语义。

## 最小Shader实现

```hlsl
Tags { "Queue"="Transparent" "RenderType"="Transparent" }
ZWrite Off
Blend SrcAlpha OneMinusSrcAlpha
// frag returns rgba
```

实现顺序固定为常量颜色、输入可视化、核心公式、材质参数和优化。每新增一个步骤都保留开关，这样画面异常时可以二分定位。编译成功只证明语法和部分类型成立，不证明空间、状态或色彩处理正确。


## Unity侧设置与渲染状态

```csharp
var material = renderer.sharedMaterial;
material.SetFloat("_DebugMode", debugMode);
material.EnableKeyword(debugMode > 0 ? "DEBUG_VIEW" : "BASELINE_VIEW");
Debug.Assert(material.shader.isSupported);
```

材质、相机和光源属于实验输入，不能只保存Shader文件。用Frame Debugger检查实际选择的SubShader和Pass、关键字变体、渲染队列、RenderTarget、深度写入和混合；用Profiler或GPU捕获确认成本来自提交、顶点、片元还是带宽。

## 边界与平台差异

测试零向量、负缩放、非统一缩放、UV边界、透明物体交叉、近远裁剪面、HDR亮度、不同纹理导入设置和移动精度。原书以Unity 5时代内置管线为语境；迁移到当前Unity、URP或HDRP时，应把概念映射和API替换单独记录，不能把新管线行为倒写成原书内容。

```text
scene: fixed reference scene
unity: exact editor version
pipeline: built-in or named SRP
platform: graphics API and GPU
evidence: image diff + frame events + timings
```

1. 建立只有一个网格、一个相机和一个主光的参考场景，保存Unity版本与平台。
2. 输出UV、法线、深度或光照项等中间值，确认数据空间和范围。
3. 启用本章核心效果，用Frame Debugger逐Pass检查输入、状态和目标。
4. 构造边界与失败场景，比较参考图、像素差异、Draw Call和GPU时间。

## 验收证书

验收包至少包含Shader与脚本、材质参数、测试场景、参考图和候选图、Frame Debugger事件、编译关键字、目标平台、帧时间与反例。关闭深度写入后仍按任意顺序绘制半透明面，模型内部出现前后关系跳变。 修复后必须重放同一输入，确认画面和性能变化都能由证据解释。

## 常见误区


## 本章回顾

本页覆盖渲染队列、透明度测试、透明度混合、深度写入、排序歧义。掌握标准是能从视觉目标画出数据流，写出最小实现，逐阶段可视化中间值，再用渲染状态、边界样例和性能数据完成复现。


## 术语表


