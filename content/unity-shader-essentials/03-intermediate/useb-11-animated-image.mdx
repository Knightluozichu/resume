---
title: "第11章 让画面动起来"
type: C
section: "Unity Shader精要 · 中级篇"
order: 11
description: "用时间变量实现序列帧、滚动纹理、河流顶点动画和广告牌，并控制空间、包围盒与批处理副作用。"
demo: true
math: true
sourceUrl: "https://www.cnblogs.com/xiaowangba/p/6314640.html"
draft: true
---

import { Objectives, Callout, Attribution, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step } from "@/components/mdx/mdx-components";
import { UsebAnimatedImagePipelineLab, UsebAnimatedImageCompareLab, UsebAnimatedImageEvidenceLab } from "@/components/mdx/unity-shader-essentials/diagrams/useb-11-animated-image";

<Objectives>
- 能解释时间变量、序列帧动画和UV滚动在Unity渲染链中的位置
- 能推导本页公式并实现最小Shader，用调试视图比较中间值
- 能复现广告牌，用Frame Debugger、边界场景和性能数据验收
</Objectives>

## 从效果开始

先预测画面、Draw Call和中间缓冲会如何变化，再调整一个参数观察变化。序列帧需要从时间计算行列索引并避免边界溢出，UV滚动要处理重复寻址。顶点动画改变可见几何却不会自动更新CPU侧包围盒，过小包围盒会被错误剔除。广告牌应明确完全朝向相机还是仅绕固定轴旋转，并处理基向量退化。 本页用<Term def="Unity按帧提供的时间、正弦和余弦组合参数。">时间变量</Term>、<Term def="按时间离散选择纹理图集单元形成动画的方法。">序列帧动画</Term>、<Term def="给纹理坐标增加随时间变化的偏移以制造流动。">UV滚动</Term>、<Term def="在顶点阶段按位置、时间和参数改变几何。">顶点动画</Term>、<Term def="让平面朝向相机或约束轴以表现粒子、植被等对象的技术。">广告牌</Term>串联视觉目标、GPU数据流、渲染状态和验收证据。

<UsebAnimatedImagePipelineLab />

## 第一版权威目录定位

本页一一对应冯乐乐《Unity Shader入门精要》第一版第11章 让画面动起来。作者发布的完整目录明确列出5篇20章，人民邮电出版社书目记录确认2016年第一版、ISBN 9787115423054和369页，作者GitHub保存随书源代码与勘误入口。

### 原书小节覆盖

1. 11.1 Unity Shader中的时间变量
2. 11.2 纹理动画
3. 11.2.1 序列帧动画
4. 11.2.2 滚动背景
5. 11.3 顶点动画
6. 11.3.1 流动的河流
7. 11.3.2 广告牌
8. 11.3.3 注意事项

### 核心对象

1. **时间变量**：Unity按帧提供的时间、正弦和余弦组合参数。
2. **序列帧动画**：按时间离散选择纹理图集单元形成动画的方法。
3. **UV滚动**：给纹理坐标增加随时间变化的偏移以制造流动。
4. **顶点动画**：在顶点阶段按位置、时间和参数改变几何。
5. **广告牌**：让平面朝向相机或约束轴以表现粒子、植被等对象的技术。

## 数据流与公式

任何Shader效果都先写清五项契约：输入属性来自哪里，当前值位于哪个坐标或色彩空间，可编程阶段输出什么，ShaderLab状态如何合并结果，以及目标平台和Unity版本是什么。

$$
frame=\lfloor t\,speed\rfloor\bmod N
$$

$$
uv'=uv+t\,velocity
$$

$$
y'=y+A\sin(kx+\omega t)
$$

$$
right=normalize(up\times look)
$$

序列帧需要从时间计算行列索引并避免边界溢出，UV滚动要处理重复寻址。顶点动画改变可见几何却不会自动更新CPU侧包围盒，过小包围盒会被错误剔除。广告牌应明确完全朝向相机还是仅绕固定轴旋转，并处理基向量退化。 公式不是孤立记忆项；每个向量都标注空间，每个颜色都标注线性或sRGB，每个深度都标注设备、NDC或观察距离，每个纹理通道都标注数据语义。

## 最小Shader实现

```hlsl
float phase = dot(v.vertex.xyz, _WaveDir.xyz) * _Frequency + _Time.y * _Speed;
v.vertex.y += sin(phase) * _Amplitude;
o.pos = UnityObjectToClipPos(v.vertex);
```

实现顺序固定为常量颜色、输入可视化、核心公式、材质参数和优化。每新增一个步骤都保留开关，这样画面异常时可以二分定位。编译成功只证明语法和部分类型成立，不证明空间、状态或色彩处理正确。

<UsebAnimatedImageCompareLab />

## Unity侧设置与渲染状态

```csharp
var material = renderer.sharedMaterial;
material.SetFloat("_DebugMode", debugMode);
material.EnableKeyword(debugMode > 0 ? "DEBUG_VIEW" : "BASELINE_VIEW");
Debug.Assert(material.shader.isSupported);
```

材质、相机和光源属于实验输入，不能只保存Shader文件。用Frame Debugger检查实际选择的SubShader和Pass、关键字变体、渲染队列、RenderTarget、深度写入和混合；用Profiler或GPU捕获确认成本来自提交、顶点、片元还是带宽。

## 边界与平台差异

测试零向量、负缩放、非统一缩放、UV边界、透明物体交叉、近远裁剪面、HDR亮度、不同纹理导入设置和移动精度。原书以Unity 5时代内置管线为语境；迁移到当前Unity、URP或HDRP时，应把概念映射和API替换单独记录，不能把新管线行为倒写成原书内容。

```text
scene: fixed reference scene
unity: exact editor version
pipeline: built-in or named SRP
platform: graphics API and GPU
evidence: image diff + frame events + timings
```

<UsebAnimatedImageEvidenceLab />

## 分步实验

<Stepper>
  <Step title="固定输入与参考图"><UsebAnimatedImagePipelineLab /></Step>
  <Step title="逐阶段打开效果"><UsebAnimatedImageCompareLab /></Step>
  <Step title="保存状态与性能"><UsebAnimatedImageEvidenceLab /></Step>
</Stepper>

1. 建立只有一个网格、一个相机和一个主光的参考场景，保存Unity版本与平台。
2. 输出UV、法线、深度或光照项等中间值，确认数据空间和范围。
3. 启用本章核心效果，用Frame Debugger逐Pass检查输入、状态和目标。
4. 构造边界与失败场景，比较参考图、像素差异、Draw Call和GPU时间。

## 验收证书

验收包至少包含Shader与脚本、材质参数、测试场景、参考图和候选图、Frame Debugger事件、编译关键字、目标平台、帧时间与反例。顶点位移超过原网格包围盒却没有扩大Bounds，动画在视锥边缘突然消失。 修复后必须重放同一输入，确认画面和性能变化都能由证据解释。

## 常见误区

<Callout type="trap" title="只看最终截图">顶点位移超过原网格包围盒却没有扩大Bounds，动画在视锥边缘突然消失。</Callout>

<Callout type="trap" title="忽略版本与状态">同一段HLSL在不同Unity版本、渲染管线、关键字和纹理导入设置下可能走不同Pass或解释不同数据。修复时把版本、平台、状态和资源设置纳入实验输入。</Callout>

## 本章回顾

本页覆盖时间变量、序列帧动画、UV滚动、顶点动画、广告牌。掌握标准是能从视觉目标画出数据流，写出最小实现，逐阶段可视化中间值，再用渲染状态、边界样例和性能数据完成复现。

<Exercises>
**问题1：时间变量与序列帧动画如何连接？**
<Answer>Unity按帧提供的时间、正弦和余弦组合参数。 按时间离散选择纹理图集单元形成动画的方法。 回答时标注执行阶段和输入空间。</Answer>

**问题2：如何调试UV滚动？**
<Answer>先输出常量，再把相关中间值映射为颜色；逐项关闭纹理、光照和状态，最后用Frame Debugger确认实际Pass。</Answer>

**问题3：如何验收广告牌？**
<Answer>让平面朝向相机或约束轴以表现粒子、植被等对象的技术。 同时保存正常图、边界图、失败反例、状态记录和性能基线。</Answer>
</Exercises>

## 术语表

<Glossary>
  <GlossaryItem term="时间变量" definition="Unity按帧提供的时间、正弦和余弦组合参数。" />
  <GlossaryItem term="序列帧动画" definition="按时间离散选择纹理图集单元形成动画的方法。" />
  <GlossaryItem term="UV滚动" definition="给纹理坐标增加随时间变化的偏移以制造流动。" />
  <GlossaryItem term="顶点动画" definition="在顶点阶段按位置、时间和参数改变几何。" />
  <GlossaryItem term="广告牌" definition="让平面朝向相机或约束轴以表现粒子、植被等对象的技术。" />
  <GlossaryItem term="Shader实验单" definition="记录Unity版本、目标平台、场景、材质、源码、截图和性能的一页式证据。" />
</Glossary>

<Attribution adaptedFrom="冯乐乐 · Unity Shader入门精要 · 第11章 让画面动起来" adaptedUrl="https://www.cnblogs.com/xiaowangba/p/6314640.html" />
