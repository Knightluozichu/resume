---
title: "第17章 Surface Shader探秘"
type: C
section: "Unity Shader精要 · 扩展篇"
order: 17
description: "拆解Surface Shader编译指令、表面函数、光照函数、Input与SurfaceOutput，以及Unity生成Pass的机制和限制。"
demo: true
math: true
sourceUrl: "https://www.cnblogs.com/xiaowangba/p/6314640.html"
draft: false
---

import { Objectives, Callout, Attribution, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step } from "@/components/mdx/mdx-components";
import { UsebSurfaceShaderPipelineLab, UsebSurfaceShaderCompareLab, UsebSurfaceShaderEvidenceLab } from "@/components/mdx/unity-shader-essentials/diagrams/useb-17-surface-shader";

<Objectives>
- 能解释Surface Shader、表面函数和光照函数在Unity渲染链中的位置
- 能推导本页公式并实现最小Shader，用调试视图比较中间值
- 能复现SurfaceOutput，用Frame Debugger、边界场景和性能数据验收
</Objectives>

## 从效果开始

先预测画面、Draw Call和中间缓冲会如何变化，再调整一个参数观察变化。Surface Shader不是硬件阶段，而是Unity的代码生成器。编译指令指定表面函数、光照模型和阴影、透明等选项，Unity据此生成前向、阴影和其他Pass。阅读生成代码可以确认插值器、变体和Pass数量；自动化方便但可能生成冗余代码，也难以表达某些屏幕或多Pass效果。 本页用<Term def="由Unity根据表面描述和光照模型生成底层顶点片元Pass的代码生成形式。">Surface Shader</Term>、<Term def="把输入纹理和几何数据写入反照率、法线、光滑度等表面属性的函数。">表面函数</Term>、<Term def="把表面输出、光线、观察方向和衰减组合为最终光照的函数。">光照函数</Term>、<Term def="声明表面函数需要的纹理坐标、世界位置、视线等数据来源。">Input结构体</Term>、<Term def="承载Albedo、Normal、Emission、Specular、Gloss与Alpha等表面属性的结构。">SurfaceOutput</Term>串联视觉目标、GPU数据流、渲染状态和验收证据。

<UsebSurfaceShaderPipelineLab />

## 第一版权威目录定位

本页一一对应冯乐乐《Unity Shader入门精要》第一版第17章 Surface Shader探秘。作者发布的完整目录明确列出5篇20章，人民邮电出版社书目记录确认2016年第一版、ISBN 9787115423054和369页，作者GitHub保存随书源代码与勘误入口。

### 原书小节覆盖

1. 17.1 一个例子
2. 17.2 编译指令
3. 17.2.1 表面函数
4. 17.2.2 光照函数
5. 17.2.3 可选参数
6. 17.3 Input与SurfaceOutput
7. 17.4 Unity背后做了什么
8. 17.5 实例分析
9. 17.6 缺点

### 核心对象

1. **Surface Shader**：由Unity根据表面描述和光照模型生成底层顶点片元Pass的代码生成形式。
2. **表面函数**：把输入纹理和几何数据写入反照率、法线、光滑度等表面属性的函数。
3. **光照函数**：把表面输出、光线、观察方向和衰减组合为最终光照的函数。
4. **Input结构体**：声明表面函数需要的纹理坐标、世界位置、视线等数据来源。
5. **SurfaceOutput**：承载Albedo、Normal、Emission、Specular、Gloss与Alpha等表面属性的结构。

## 数据流与公式

任何Shader效果都先写清五项契约：输入属性来自哪里，当前值位于哪个坐标或色彩空间，可编程阶段输出什么，ShaderLab状态如何合并结果，以及目标平台和Unity版本是什么。

$$
Surface(Input)\rightarrow SurfaceOutput
$$

$$
Lighting(SurfaceOutput,l,v,atten)\rightarrow C
$$

$$
Generated=Passes\times Variants
$$

$$
cost=interpolators+instructions+draws
$$

Surface Shader不是硬件阶段，而是Unity的代码生成器。编译指令指定表面函数、光照模型和阴影、透明等选项，Unity据此生成前向、阴影和其他Pass。阅读生成代码可以确认插值器、变体和Pass数量；自动化方便但可能生成冗余代码，也难以表达某些屏幕或多Pass效果。 公式不是孤立记忆项；每个向量都标注空间，每个颜色都标注线性或sRGB，每个深度都标注设备、NDC或观察距离，每个纹理通道都标注数据语义。

## 最小Shader实现

```hlsl
#pragma surface surf Standard fullforwardshadows
void surf(Input IN, inout SurfaceOutputStandard o) {
    o.Albedo = tex2D(_MainTex, IN.uv_MainTex).rgb;
    o.Smoothness = _Glossiness;
}
```

实现顺序固定为常量颜色、输入可视化、核心公式、材质参数和优化。每新增一个步骤都保留开关，这样画面异常时可以二分定位。编译成功只证明语法和部分类型成立，不证明空间、状态或色彩处理正确。

<UsebSurfaceShaderCompareLab />

## Unity侧设置与渲染状态

```csharp
var material = renderer.sharedMaterial;
material.SetFloat("_DebugMode", debugMode);
material.EnableKeyword(debugMode > 0 ? "DEBUG_VIEW" : "BASELINE_VIEW");
Debug.Assert(material.shader.isSupported);
```

材质、相机和光源属于实验输入，不能只保存Shader文件。用Frame Debugger检查实际选择的SubShader和Pass、关键字变体、渲染队列、RenderTarget、深度写入和混合；用Profiler或GPU捕获确认成本来自提交、顶点、片元还是带宽。

## 边界与平台差异

测试零向量、负缩放、非统一缩放、UV边界、透明物体交叉、近远裁剪面、HDR亮度、不同纹理导入设置和移动精度。原书以Unity 5时代内置管线为语境；迁移到当前Unity、URP或HDRP时，应把概念映射和API替换单独记录，不能把新管线行为倒写成原书内容。

```text
scene: fixed reference scene
unity: exact editor version
pipeline: built-in or named SRP
platform: graphics API and GPU
evidence: image diff + frame events + timings
```

<UsebSurfaceShaderEvidenceLab />

## 分步实验

<Stepper>
  <Step title="固定输入与参考图"><UsebSurfaceShaderPipelineLab /></Step>
  <Step title="逐阶段打开效果"><UsebSurfaceShaderCompareLab /></Step>
  <Step title="保存状态与性能"><UsebSurfaceShaderEvidenceLab /></Step>
</Stepper>

1. 建立只有一个网格、一个相机和一个主光的参考场景，保存Unity版本与平台。
2. 输出UV、法线、深度或光照项等中间值，确认数据空间和范围。
3. 启用本章核心效果，用Frame Debugger逐Pass检查输入、状态和目标。
4. 构造边界与失败场景，比较参考图、像素差异、Draw Call和GPU时间。

## 验收证书

验收包至少包含Shader与脚本、材质参数、测试场景、参考图和候选图、Frame Debugger事件、编译关键字、目标平台、帧时间与反例。把Surface Shader当一个单Pass片元程序，忽略它生成的变体、阴影Pass和平台差异。 修复后必须重放同一输入，确认画面和性能变化都能由证据解释。

## 常见误区

<Callout type="trap" title="只看最终截图">把Surface Shader当一个单Pass片元程序，忽略它生成的变体、阴影Pass和平台差异。</Callout>

<Callout type="trap" title="忽略版本与状态">同一段HLSL在不同Unity版本、渲染管线、关键字和纹理导入设置下可能走不同Pass或解释不同数据。修复时把版本、平台、状态和资源设置纳入实验输入。</Callout>

## 本章回顾

本页覆盖Surface Shader、表面函数、光照函数、Input结构体、SurfaceOutput。掌握标准是能从视觉目标画出数据流，写出最小实现，逐阶段可视化中间值，再用渲染状态、边界样例和性能数据完成复现。

<Exercises>
**问题1：Surface Shader与表面函数如何连接？**
<Answer>由Unity根据表面描述和光照模型生成底层顶点片元Pass的代码生成形式。 把输入纹理和几何数据写入反照率、法线、光滑度等表面属性的函数。 回答时标注执行阶段和输入空间。</Answer>

**问题2：如何调试光照函数？**
<Answer>先输出常量，再把相关中间值映射为颜色；逐项关闭纹理、光照和状态，最后用Frame Debugger确认实际Pass。</Answer>

**问题3：如何验收SurfaceOutput？**
<Answer>承载Albedo、Normal、Emission、Specular、Gloss与Alpha等表面属性的结构。 同时保存正常图、边界图、失败反例、状态记录和性能基线。</Answer>
</Exercises>

## 术语表

<Glossary>
  <GlossaryItem term="Surface Shader" definition="由Unity根据表面描述和光照模型生成底层顶点片元Pass的代码生成形式。" />
  <GlossaryItem term="表面函数" definition="把输入纹理和几何数据写入反照率、法线、光滑度等表面属性的函数。" />
  <GlossaryItem term="光照函数" definition="把表面输出、光线、观察方向和衰减组合为最终光照的函数。" />
  <GlossaryItem term="Input结构体" definition="声明表面函数需要的纹理坐标、世界位置、视线等数据来源。" />
  <GlossaryItem term="SurfaceOutput" definition="承载Albedo、Normal、Emission、Specular、Gloss与Alpha等表面属性的结构。" />
  <GlossaryItem term="Shader实验单" definition="记录Unity版本、目标平台、场景、材质、源码、截图和性能的一页式证据。" />
</Glossary>

<Attribution adaptedFrom="冯乐乐 · Unity Shader入门精要 · 第17章 Surface Shader探秘" adaptedUrl="https://www.cnblogs.com/xiaowangba/p/6314640.html" />
