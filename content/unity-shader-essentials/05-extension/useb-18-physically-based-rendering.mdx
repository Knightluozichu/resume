---
title: "第18章 基于物理的渲染"
type: C
section: "Unity Shader精要 · 扩展篇"
order: 18
description: "从BRDF、漫反射与微表面高光理解Unity 5 Standard Shader、反射探针、线性空间、GI、HDR与适用边界。"
demo: true
math: true
sourceUrl: "https://www.cnblogs.com/xiaowangba/p/6314640.html"
draft: true
---

import { Objectives, Callout, Attribution, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step } from "@/components/mdx/mdx-components";
import { UsebPhysicallyBasedRenderingPipelineLab, UsebPhysicallyBasedRenderingCompareLab, UsebPhysicallyBasedRenderingEvidenceLab } from "@/components/mdx/unity-shader-essentials/diagrams/useb-18-physically-based-rendering";

<Objectives>
- 能解释基于物理的着色、微表面模型和金属度在Unity渲染链中的位置
- 能推导本页公式并实现最小Shader，用调试视图比较中间值
- 能复现线性空间，用Frame Debugger、边界场景和性能数据验收
</Objectives>

## 从效果开始

先预测画面、Draw Call和中间缓冲会如何变化，再调整一个参数观察变化。PBR着色把漫反射和镜面反射按材质类型与Fresnel分配能量，粗糙度控制法线分布，高光遮蔽处理微表面可见性。Standard Shader把这些参数映射到Unity的光照与反射探针。输入颜色需正确标记sRGB，光照在线性空间累积，最后显示时编码；HDR保留超过一的亮度供曝光和Bloom使用。 本页用<Term def="使用能量约束、材质参数和环境光照获得跨光照条件一致外观的着色方法。">基于物理的着色</Term>、<Term def="把表面视为微小镜面集合并用分布、遮蔽和Fresnel构成高光的模型。">微表面模型</Term>、<Term def="控制基色进入漫反射还是作为导体F0颜色的材质参数。">金属度</Term>、<Term def="控制微表面法线分布宽度和高光形状的材质参数。">粗糙度</Term>、<Term def="在与光能近似线性关系的数值空间中执行光照和混合。">线性空间</Term>串联视觉目标、GPU数据流、渲染状态和验收证据。

<UsebPhysicallyBasedRenderingPipelineLab />

## 第一版权威目录定位

本页一一对应冯乐乐《Unity Shader入门精要》第一版第18章 基于物理的渲染。作者发布的完整目录明确列出5篇20章，人民邮电出版社书目记录确认2016年第一版、ISBN 9787115423054和369页，作者GitHub保存随书源代码与勘误入口。

### 原书小节覆盖

1. 18.1 PBS的理论和数学基础
2. 18.1.1 光与BRDF
3. 18.1.3 漫反射项
4. 18.1.4 高光反射项
5. 18.1.5 Unity中的PBS
6. 18.2 Standard Shader
7. 18.3 光照环境与反射探针
8. 18.4 GI、伽马校正与HDR

### 核心对象

1. **基于物理的着色**：使用能量约束、材质参数和环境光照获得跨光照条件一致外观的着色方法。
2. **微表面模型**：把表面视为微小镜面集合并用分布、遮蔽和Fresnel构成高光的模型。
3. **金属度**：控制基色进入漫反射还是作为导体F0颜色的材质参数。
4. **粗糙度**：控制微表面法线分布宽度和高光形状的材质参数。
5. **线性空间**：在与光能近似线性关系的数值空间中执行光照和混合。

## 数据流与公式

任何Shader效果都先写清五项契约：输入属性来自哪里，当前值位于哪个坐标或色彩空间，可编程阶段输出什么，ShaderLab状态如何合并结果，以及目标平台和Unity版本是什么。

$$
f_r=f_d+\frac{DFG}{4(n\cdot l)(n\cdot v)}
$$

$$
F=F_0+(1-F_0)(1-v\cdot h)^5
$$

$$
k_d=(1-F)(1-metallic)
$$

$$
L_o=\int_\Omega f_rL_i(n\cdot l)d\omega
$$

PBR着色把漫反射和镜面反射按材质类型与Fresnel分配能量，粗糙度控制法线分布，高光遮蔽处理微表面可见性。Standard Shader把这些参数映射到Unity的光照与反射探针。输入颜色需正确标记sRGB，光照在线性空间累积，最后显示时编码；HDR保留超过一的亮度供曝光和Bloom使用。 公式不是孤立记忆项；每个向量都标注空间，每个颜色都标注线性或sRGB，每个深度都标注设备、NDC或观察距离，每个纹理通道都标注数据语义。

## 最小Shader实现

```hlsl
float3 F0 = lerp(0.04.xxx, baseColor, metallic);
float3 F = F0 + (1.0 - F0) * pow(1.0 - saturate(dot(v,h)), 5.0);
float3 kd = (1.0 - F) * (1.0 - metallic);
```

实现顺序固定为常量颜色、输入可视化、核心公式、材质参数和优化。每新增一个步骤都保留开关，这样画面异常时可以二分定位。编译成功只证明语法和部分类型成立，不证明空间、状态或色彩处理正确。

<UsebPhysicallyBasedRenderingCompareLab />

## Unity侧设置与渲染状态

```csharp
var material = renderer.sharedMaterial;
material.SetFloat("_DebugMode", debugMode);
material.EnableKeyword(debugMode > 0 ? "DEBUG_VIEW" : "BASELINE_VIEW");
Debug.Assert(material.shader.isSupported);
```

材质、相机和光源属于实验输入，不能只保存Shader文件。用Frame Debugger检查实际选择的SubShader和Pass、关键字变体、渲染队列、RenderTarget、深度写入和混合；用Profiler或GPU捕获确认成本来自提交、顶点、片元还是带宽。

## 边界与平台差异

测试零向量、负缩放、非统一缩放、UV边界、透明物体交叉、近远裁剪面、HDR亮度、不同纹理导入设置和移动精度。原书以Unity 5时代内置管线为语境；迁移到当前Unity、URP或HDRP时，应把概念映射和API替换单独记录，不能把新管线行为倒写成原书内容。

```text
scene: fixed reference scene
unity: exact editor version
pipeline: built-in or named SRP
platform: graphics API and GPU
evidence: image diff + frame events + timings
```

<UsebPhysicallyBasedRenderingEvidenceLab />

## 分步实验

<Stepper>
  <Step title="固定输入与参考图"><UsebPhysicallyBasedRenderingPipelineLab /></Step>
  <Step title="逐阶段打开效果"><UsebPhysicallyBasedRenderingCompareLab /></Step>
  <Step title="保存状态与性能"><UsebPhysicallyBasedRenderingEvidenceLab /></Step>
</Stepper>

1. 建立只有一个网格、一个相机和一个主光的参考场景，保存Unity版本与平台。
2. 输出UV、法线、深度或光照项等中间值，确认数据空间和范围。
3. 启用本章核心效果，用Frame Debugger逐Pass检查输入、状态和目标。
4. 构造边界与失败场景，比较参考图、像素差异、Draw Call和GPU时间。

## 验收证书

验收包至少包含Shader与脚本、材质参数、测试场景、参考图和候选图、Frame Debugger事件、编译关键字、目标平台、帧时间与反例。在伽马空间中直接做光照与混合，再通过调材质数值弥补，换环境后外观立刻崩坏。 修复后必须重放同一输入，确认画面和性能变化都能由证据解释。

## 常见误区

<Callout type="trap" title="只看最终截图">在伽马空间中直接做光照与混合，再通过调材质数值弥补，换环境后外观立刻崩坏。</Callout>

<Callout type="trap" title="忽略版本与状态">同一段HLSL在不同Unity版本、渲染管线、关键字和纹理导入设置下可能走不同Pass或解释不同数据。修复时把版本、平台、状态和资源设置纳入实验输入。</Callout>

## 本章回顾

本页覆盖基于物理的着色、微表面模型、金属度、粗糙度、线性空间。掌握标准是能从视觉目标画出数据流，写出最小实现，逐阶段可视化中间值，再用渲染状态、边界样例和性能数据完成复现。

<Exercises>
**问题1：基于物理的着色与微表面模型如何连接？**
<Answer>使用能量约束、材质参数和环境光照获得跨光照条件一致外观的着色方法。 把表面视为微小镜面集合并用分布、遮蔽和Fresnel构成高光的模型。 回答时标注执行阶段和输入空间。</Answer>

**问题2：如何调试金属度？**
<Answer>先输出常量，再把相关中间值映射为颜色；逐项关闭纹理、光照和状态，最后用Frame Debugger确认实际Pass。</Answer>

**问题3：如何验收线性空间？**
<Answer>在与光能近似线性关系的数值空间中执行光照和混合。 同时保存正常图、边界图、失败反例、状态记录和性能基线。</Answer>
</Exercises>

## 术语表

<Glossary>
  <GlossaryItem term="基于物理的着色" definition="使用能量约束、材质参数和环境光照获得跨光照条件一致外观的着色方法。" />
  <GlossaryItem term="微表面模型" definition="把表面视为微小镜面集合并用分布、遮蔽和Fresnel构成高光的模型。" />
  <GlossaryItem term="金属度" definition="控制基色进入漫反射还是作为导体F0颜色的材质参数。" />
  <GlossaryItem term="粗糙度" definition="控制微表面法线分布宽度和高光形状的材质参数。" />
  <GlossaryItem term="线性空间" definition="在与光能近似线性关系的数值空间中执行光照和混合。" />
  <GlossaryItem term="Shader实验单" definition="记录Unity版本、目标平台、场景、材质、源码、截图和性能的一页式证据。" />
</Glossary>

<Attribution adaptedFrom="冯乐乐 · Unity Shader入门精要 · 第18章 基于物理的渲染" adaptedUrl="https://www.cnblogs.com/xiaowangba/p/6314640.html" />
