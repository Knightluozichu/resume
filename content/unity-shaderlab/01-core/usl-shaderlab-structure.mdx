---
title: "ShaderLab 文件结构"
type: B
section: "Shader 与 GPU 编程"
order: 1
description: "Shader、Properties、SubShader、Pass、Fallback 的层次关系"
demo: true
math: false
sourceUrl: ""
draft: true
---

import { UslShaderlabStructureDiagram } from "@/components/mdx/unity-shaderlab/diagrams/usl-shaderlab-structure";
import { Objectives, Callout, Attribution, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step } from "@/components/mdx/mdx-components";

<Objectives>
- 掌握 ShaderLab 文件的完整结构层次
- 理解 Unity 如何选择和执行 SubShader
- 学会使用 Tags 控制渲染行为
- 能设计多平台多质量的 Shader 结构
</Objectives>

## 直觉引入

ShaderLab 文件就像一份建筑施工图——最外层是项目名称（Shader "Name"），里面分三部分：材料清单（Properties）、施工方案（SubShader，可有多套备选）、兜底方案（Fallback）。施工方案里有施工标签（Tags）、质量等级（LOD）和具体工序（Pass）。Unity 会从上到下找第一个显卡能跑的方案。

## 概念讲解

<Term def="ShaderLab 文件最外层声明，含名称和所有内容">Shader 声明</Term><Term def="属性声明块，定义可在材质面板调节的参数">Properties</Term><Term def="渲染方案块，可含多个 Pass 和渲染状态">SubShader</Term><Term def="所有 SubShader 都不支持时的回退 Shader">Fallback</Term>

### 文件结构
Shader → Properties（属性声明）→ SubShader（可多个）→ Pass（可多个）→ Fallback（兜底）

### SubShader 选择
Unity 从上到下遍历 SubShader，选择第一个显卡支持的执行。通过 LOD 限制低端设备使用简化版本。若都不支持则用 Fallback 指定的 Shader。

### Tags 作用
Queue 设置渲染队列（Background/Geometry/Transparent/Overlay）、RenderType 用于替换着色器、DisableBatching 禁用合批。Tags 在 SubShader 和 Pass 级别都可设置。

## 交互Demo

> 猜一猜：Unity 如何选择执行哪个 SubShader？

<UslShaderlabStructureDiagram />

<Stepper>
<Step title="第一步：整体结构">
ShaderLab 文件由 Shader 声明、Properties 块、一个或多个 SubShader、Fallback 声明组成。Properties 声明面板属性，SubShader 包含实际渲染代码。
</Step>
<Step title="第二步：SubShader 选择">
Unity 从上到下遍历 SubShader，通过 LOD 和显卡能力判断是否支持。选择第一个支持的 SubShader 执行。都不支持则使用 Fallback。
</Step>
<Step title="第三步：多平台设计">
写多个 SubShader 按 LOD 从高到低排列，高 LOD 用复杂效果，低 LOD 简化算法。用 shader_feature 控制变体，设置合适的 Fallback 兜底。
</Step>
</Stepper>

## 代码对照

```shaderlab
Shader "Custom/MultiPlatform" {
    Properties {
        _Color ("Color", Color) = (1,1,1,1)
        _MainTex ("Texture", 2D) = "white" {}
        _Gloss ("Gloss", Range(0,1)) = 0.5
    }

    // 高质量 SubShader
    SubShader {
        Tags { "RenderType"="Opaque" "Queue"="Geometry" }
        LOD 300

        Pass {
            CGPROGRAM
            #pragma target 3.0
            #pragma vertex vert
            #pragma fragment frag
            // ... 复杂光照代码
            ENDCG
        }
    }

    // 低质量 SubShader
    SubShader {
        Tags { "RenderType"="Opaque" }
        LOD 100

        Pass {
            CGPROGRAM
            #pragma target 2.0
            #pragma vertex vert
            #pragma fragment frag
            // ... 简化代码
            ENDCG
        }
    }

    Fallback "Mobile/Diffuse"
}
```

多 SubShader 按 LOD 从高到低排列，Fallback 作为最终兜底。Tags 控制渲染队列和类型。

## 常见误区

<Callout type="trap">
**现象 →** 只写一个 SubShader 不设 Fallback。
**原因 →** 觉得当前设备一定能跑。
**修法 →** 不同设备支持的 Shader Model 版本不同。设置 Fallback 确保低端设备有兜底效果。用多个 SubShader 和 LOD 分级适配不同性能的设备。
</Callout>

<Callout type="trap">
**现象 →** Tags 写在 Pass 里但渲染顺序不对。
**原因 →** 混淆了 SubShader 级 Tags 和 Pass 级 Tags。
**修法 →** Queue 和 RenderType 是 SubShader 级 Tags，控制整体渲染顺序。Pass 级 Tags 如 LightMode 控制该 Pass 在哪个渲染路径执行。渲染队列设置应在 SubShader 级。
</Callout>

## 练习

<Exercises>
**问题**：Unity 如何选择执行哪个 SubShader？

<Answer>
Unity 从上到下遍历 SubShader，选择第一个显卡支持的 SubShader 执行。通过 Tags 设置渲染队列和类型，通过 LOD 限制低端设备使用简化版本。若都不支持则使用 Fallback 指定的回退 Shader。
</Answer>

**问题**：如何设计支持多平台多质量的 Shader 结构？

<Answer>
1)写多个 SubShader 按 LOD 从高到低排列 2)高 LOD 用复杂光照和纹理 3)低 LOD 简化算法和减少采样 4)用 Shader Feature/Multi Compile 控制变体 5)设置合适的 Fallback 兜底 6)用 #pragma target 控制 Shader Model 版本。
</Answer>
</Exercises>

<Glossary>
<GlossaryItem term="Shader 声明">ShaderLab 文件最外层声明，含名称和所有内容。</GlossaryItem>
<GlossaryItem term="Properties">属性声明块，定义可在材质面板调节的参数。</GlossaryItem>
<GlossaryItem term="SubShader">渲染方案块，可含多个 Pass 和渲染状态。</GlossaryItem>
<GlossaryItem term="Fallback">所有 SubShader 都不支持时的回退 Shader。</GlossaryItem>
</Glossary>

<Attribution adaptedFrom="Unity ShaderLab 开发实战详解" adaptedUrl="" />
