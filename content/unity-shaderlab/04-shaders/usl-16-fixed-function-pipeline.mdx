---
title: "第16章 固定管线"
type: C
section: "Unity 3D ShaderLab · 状态与管线"
order: 16
description: "沿郭浩瑜第一版正式目录掌握固定管线、Material块、SetTexture、ColorMaterial、Combine。"
demo: true
math: true
sourceUrl: "https://search.worldcat.org/title/Unity-3D-Shaderlab/oclc/933550649"
draft: true
---

import { Objectives, Callout, Attribution, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step } from "@/components/mdx/mdx-components";
import { Usl16FixedFunctionPipelinePipelineLab, Usl16FixedFunctionPipelineCompareLab, Usl16FixedFunctionPipelineEvidenceLab } from "@/components/mdx/unity-shaderlab/diagrams/usl-16-fixed-function-pipeline";

<Objectives>
- 能解释固定管线、Material块和SetTexture在Unity内置渲染链中的职责
- 能实现本章最小实验，并区分Shader程序、脚本绑定与固定渲染状态
- 能验收Combine，保存画面、Frame Debugger、GPU事件和失败反例
</Objectives>

## 先预测，再观察

先预测顶点、片元、深度、Draw次数或帧时间会如何变化，再只调整一个变量观察变化。按Material、Lighting、SetTexture和Combine构造最小固定管线材质，再与等价可编程Shader逐项比较。章节保留历史语境用于理解旧项目，不能推荐为现代Unity新项目的主路线。 本章用<Term def="用声明命令组合预定义变换、光照和纹理单元的旧式管线。">固定管线</Term>、<Term def="配置固定管线环境、漫反射、镜面与自发光参数。">Material块</Term>、<Term def="声明纹理采样、Combine和多级纹理组合。">SetTexture</Term>、<Term def="允许顶点颜色影响固定光照材质参数。">ColorMaterial</Term>、<Term def="用受限表达式组合纹理、常量、主色和前一级结果。">Combine</Term>建立对象、阶段、状态与证据之间的联系。

<Usl16FixedFunctionPipelinePipelineLab />

## 第一版权威目录定位

本页一一对应郭浩瑜《Unity 3D ShaderLab开发实战详解》第一版第16章 固定管线。书目记录确认人民邮电出版社2014年出版、ISBN 9787115338907；完整目录确认全书5篇33章。本书基于当时Unity内置渲染管线，涉及固定管线、Cg、VertexLit和Surface Shader时保留历史语境，现代Unity项目需另做版本映射。

### 原章核心小节

1. 固定管线基本形态
2. Material块
3. SetTexture块
4. Combine语句
5. 顶点色
6. 光照贴图
7. 嵌套Cg代码

### 五个核心对象

1. **固定管线**：用声明命令组合预定义变换、光照和纹理单元的旧式管线。
2. **Material块**：配置固定管线环境、漫反射、镜面与自发光参数。
3. **SetTexture**：声明纹理采样、Combine和多级纹理组合。
4. **ColorMaterial**：允许顶点颜色影响固定光照材质参数。
5. **Combine**：用受限表达式组合纹理、常量、主色和前一级结果。

## 数据流与公式

按Material、Lighting、SetTexture和Combine构造最小固定管线材质，再与等价可编程Shader逐项比较。章节保留历史语境用于理解旧项目，不能推荐为现代Unity新项目的主路线。 先画出应用脚本、材质属性、SubShader选择、Pass执行、顶点阶段、光栅化、片元阶段和固定状态的生产者到消费者链，再把本章对象放到对应位置。

$$
primary=lighting(material)
$$

$$
tex=sample(T,uv)
$$

$$
C=Combine(tex,primary)
$$

$$
stages=SetTexture_0ightarrow SetTexture_1
$$

公式中的位置、方向、深度、颜色和时间必须标注空间、范围与单位。片元函数输出只是候选颜色，最终结果仍经过裁剪、深度、模板、混合、目标格式与颜色编码。

## 最小实现

```shaderlab
SetTexture [_MainTex] { combine texture * primary }
```

实现顺序固定为常量输出、输入可视化、核心公式、应用侧绑定和固定状态。先确认材质没有变粉、实际SubShader和Pass正确，再判断画面差异是否来自程序计算。

<Usl16FixedFunctionPipelineCompareLab />

## 脚本与材质契约

```csharp
var material = renderer.sharedMaterial;
material.SetFloat("_DebugMode", debugMode);
material.SetTexture("_MainTex", sourceTexture);
Debug.Assert(material.shader.isSupported);
```

脚本侧保存Unity版本、渲染路径、相机、灯光、网格布局、材质关键字、纹理导入设置和RenderTexture格式。Shader侧列出Properties、uniform、Tags、LightMode与固定状态，二者缺一都不能复现实验。

## 边界、精度与性能

测试零向量、退化UV、负缩放、近远裁剪面、透明交叉、纹理边缘、相机进入体积、多光源重叠和高过度绘制。性能实验固定目标设备、分辨率、VSync和场景，先判断CPU还是GPU受限，再定位具体Draw、Pass与Shader阶段。

```text
unity: exact editor and player version
pipeline: Built-in / named rendering path
scene: fixed mesh, camera, lights and textures
state: queue, depth, stencil, cull, blend and target
evidence: reference image, frame debugger, GPU capture, CPU/GPU ms
failure: smallest replayable counterexample
```

<Usl16FixedFunctionPipelineEvidenceLab />

## 分步实验

<Stepper>
  <Step title="固定输入与路径"><Usl16FixedFunctionPipelinePipelineLab /></Step>
  <Step title="逐阶段实现与比较"><Usl16FixedFunctionPipelineCompareLab /></Step>
  <Step title="捕获画面与性能"><Usl16FixedFunctionPipelineEvidenceLab /></Step>
</Stepper>

1. 用最小网格和常量颜色确认Shader、SubShader、Pass与Draw可执行。
2. 可视化位置、UV、法线、深度、模板或光照项，确认范围和空间。
3. 打开本章核心效果，保存基线、候选、中间缓冲和参数。
4. 构造边界与失败输入，用Frame Debugger和GPU捕获解释差异。

## 验收证书

证书包含源码版本、Unity与图形API、脚本绑定、资源哈希、关键字、Pass状态、参考图、GPU事件、CPU/GPU帧时间与失败反例。在不支持固定管线的现代平台继续依赖旧命令，编辑器显示正常而目标设备回退或变粉。 修复后必须用同一输入重放，并指出变化发生在哪个阶段。

## 常见误区

<Callout type="trap" title="把语法当成执行链">在不支持固定管线的现代平台继续依赖旧命令，编辑器显示正常而目标设备回退或变粉。</Callout>

<Callout type="trap" title="只看最终画面">画面相似不证明实际SubShader、Pass、空间、深度、混合和性能等价。必须保存中间视图、固定状态和帧捕获。</Callout>

## 本章回顾

本页覆盖固定管线、Material块、SetTexture、ColorMaterial、Combine。掌握标准是能画出生产者到消费者链，写出最小实现，说明脚本和材质契约，再用正确性、边界、精度与性能证据复现结论。

<Exercises>
**问题1：固定管线与Material块如何连接？**
<Answer>用声明命令组合预定义变换、光照和纹理单元的旧式管线。 配置固定管线环境、漫反射、镜面与自发光参数。 回答时要标注实际Pass、空间和输入来源。</Answer>

**问题2：怎样调试SetTexture？**
<Answer>先输出常量，再把相关中间值映射为颜色或缓冲；随后核对脚本绑定、固定状态、Frame Debugger与GPU事件。</Answer>

**问题3：如何验收Combine？**
<Answer>用受限表达式组合纹理、常量、主色和前一级结果。 同时保存正常例、边界例、失败反例和CPU/GPU计时。</Answer>
</Exercises>

## 术语表

<Glossary>
  <GlossaryItem term="固定管线" definition="用声明命令组合预定义变换、光照和纹理单元的旧式管线。" />
  <GlossaryItem term="Material块" definition="配置固定管线环境、漫反射、镜面与自发光参数。" />
  <GlossaryItem term="SetTexture" definition="声明纹理采样、Combine和多级纹理组合。" />
  <GlossaryItem term="ColorMaterial" definition="允许顶点颜色影响固定光照材质参数。" />
  <GlossaryItem term="Combine" definition="用受限表达式组合纹理、常量、主色和前一级结果。" />
  <GlossaryItem term="可复现实验" definition="固定源码、Unity版本、渲染路径、资源、状态、设备与捕获后可再次得到同一结论的实验。" />
</Glossary>

<Attribution adaptedFrom="郭浩瑜 · Unity 3D ShaderLab开发实战详解 · 第16章 固定管线" adaptedUrl="https://search.worldcat.org/title/Unity-3D-Shaderlab/oclc/933550649" />
