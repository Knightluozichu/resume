---
title: "Unity UI 设计：官方学习地图"
type: A
section: "Unity UI 设计 · 全书导览"
order: 0
description: "按 Unity 3D UI Essentials 的 6 章与附录，建立从旧 GUI、布局、控件到空间 UI 和源码扩展的忠实学习路径。"
demo: true
math: false
sourceUrl: "https://api.pageplace.de/preview/DT0400.9781783553624_A24173589/preview-9781783553624_A24173589.pdf"
draft: false
---

import {
  Objectives,
  Callout,
  Attribution,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
} from "@/components/mdx/mdx-components";
import {
  UidOfficialLearningMapMapLab,
  UidOfficialLearningMapExperimentLab,
  UidOfficialLearningMapEvidenceLab,
} from "@/components/mdx/unity-ui-design/diagrams/uid-official-learning-map";

<Objectives>

- 能解释中文版与 2015 英文原版的对应关系，并说出 6 章加附录的唯一边界
- 能绘制布局、渲染、射线检测与事件派发组成的 UI 生产者到消费者链
- 能判断 uGUI 原书知识与 UI Toolkit、Input System 等现代技术之间的迁移边界

</Objectives>

## 问题：为什么不能用现代 Unity UI 技巧清单替代 2015 年原书目录

先预测：如果直接把 UI Toolkit、DOTween 和移动端性能优化排成原书章节，读者还能否理解作者从旧 IMGUI 走向 Canvas、Rect Transform、控件、锚点、空间模式与开放源码的原始论证？答案是否定的。那些技术可以作为现代补充，却不能改写书的身份。中文版《Unity UI设计》由 Simon Jackson 著、张骞译，清华大学出版社 2017 年出版，ISBN 9787302460107；对应英文原版是 Packt 2015 年的 Unity 3D UI Essentials，印刷 ISBN 9781783553617。

<UidOfficialLearningMapMapLab />

## 原书边界与小节库存

出版社预览的目录页明确给出 6 个正文章和 Appendix: The 3D Scene Sample。导读与总复习只负责导航和综合验收，不计入原书覆盖率；附录虽然可选，却承担第五章 3D 场景的可复现资产，因此必须单列。全书的逻辑不是从组件名堆砌技巧，而是先解释旧 GUI 的限制，再建立 retained-mode 布局，随后学习控件、响应式锚点、屏幕与世界空间，最后进入事件源码和可扩展边界。

- **Chapter 1. Looking Back, Looking Forward：旧 GUI、新 UI、Unity 2D 后端与编辑器变化**
- **Chapter 2. Building Layouts：Rect Transform、Canvas、自动布局、缩放与 UnityEvent**
- **Chapter 3. Control, Control, You Must Learn Control：文本、图像、Selectable、滚动与导航**
- **Chapter 4. Anchors Away：锚点、拉伸、Canvas Scaler 与分辨率策略**
- **Chapter 5. Screen Space, World Space, and the Camera：三种 Canvas 模式、相机与空间 UI**
- **Chapter 6. Working with the UI Source：EventSystem、事件接口、自定义事件与开放源码**
- **Appendix. The 3D Scene Sample：搭建第五章依赖的 3D 展示场景**

这份库存用于唯一归属，而不是逐字转载。每个条目只在一个原章中计数，页面用重新组织的解释、实验和图示复现其核心问题。读者应能从条目回到页面证据，也能从页面结论指出对应的原始范围。

## 全书生产者到消费者链

输入设备先被 Input Module 解释为指针或导航状态，EventSystem 再通过 Raycaster 找到目标，Selectable 或事件接口消费交互，Rect Transform 与布局组件计算几何，CanvasRenderer 最终把图元提交给相机或覆盖层。原书六章分别揭开这条链上的历史来源、布局约束、控件状态、响应式关系、空间投影和源码扩展。只会拖 Button 不代表理解 UI；能沿这条链解释一次点击为何命中、元素为何位于此处、最终为何这样绘制，才算掌握。

<UidOfficialLearningMapExperimentLab />

## 五个核心术语与责任边界

<Term def="由出版社预览、作者说明、ISBN 和中文版书目信息共同固定的原书结构。">
  权威目录
</Term>
不是孤立术语。它要放回本章的数据流中，说明谁产生它、谁修改它、谁消费它，以及配置错误时会出现什么可观察结果。

<Term def="只把原版六章和附录中的主题计入覆盖率，现代补充必须另行标注。">
  原书边界
</Term>
不是孤立术语。它要放回本章的数据流中，说明谁产生它、谁修改它、谁消费它，以及配置错误时会出现什么可观察结果。

<Term def="输入模块、EventSystem、Raycaster、目标组件与回调依次传递交互的路径。">
  UI 事件链
</Term>
不是孤立术语。它要放回本章的数据流中，说明谁产生它、谁修改它、谁消费它，以及配置错误时会出现什么可观察结果。

<Term def="锚点、轴心、尺寸、父级矩形与布局组件共同决定几何结果的规则。">
  布局约束
</Term>
不是孤立术语。它要放回本章的数据流中，说明谁产生它、谁修改它、谁消费它，以及配置错误时会出现什么可观察结果。

<Term def="区分 2015 年历史载体、仍成立的不变量和现代 Unity 替代方案。">
  迁移边界
</Term>
不是孤立术语。它要放回本章的数据流中，说明谁产生它、谁修改它、谁消费它，以及配置错误时会出现什么可观察结果。

术语必须能落到对象和状态，而不能只背定义。阅读 Inspector、运行日志或源码时，为每个术语写出输入、拥有者、输出和失败信号；若无法指出责任对象，就还没有形成可调试的心智模型。

## 关键对象与数据流

学习时为每章保留三张证据：范围图证明没有漏章，实验图把概念变成可操作输入，验收图记录正常、边界和失败状态。读者先固定 Canvas 模式、参考分辨率、输入模块和测试场景，再一次只改变一个布局或事件因素。这样既不会把新版 API 倒写成原书事实，也不会停留在无从复现的文字摘要。

同一个视觉现象可能来自不同链路：位置错误通常来自几何写入者，点击错误来自射线与事件，显示顺序来自 Canvas 和相机，版本错误来自历史载体与现代 API 混写。实验必须先选定要证明的链，再控制无关变量。

## 三步复现实验

动手前先预测：当前步骤改变后，哪个可见状态或日志应首先变化？若结果与预测不同，停下来沿生产者到消费者链找断点，不要继续叠加修复。

<Stepper>
  <Step title="固定身份和范围">
    <UidOfficialLearningMapMapLab />

先用英文 ISBN、作者公告、出版社目录与中文版 ISBN 建立对照表。逐项勾选六章和附录，不用旧页面数量猜目录，也不把 Preface、Index 或学习地图当成原文章。

```text
book identity -> 6 chapters + appendix -> source concepts -> modern boundary
```

  </Step>
  <Step title="按链路做最小实验">
    <UidOfficialLearningMapExperimentLab />

建立一个含 Overlay Canvas、Camera Canvas、World Canvas 与 EventSystem 的测试场景。每次只改变一个锚点、缩放模式、Raycaster 或事件处理器，并保存层级、Inspector 与运行结果。

```csharp
var eventSystem = EventSystem.current;
var target = eventSystem.currentSelectedGameObject;
Debug.Log(target ? target.name : "no selection");
```

  </Step>
  <Step title="写出现代迁移证据">
    <UidOfficialLearningMapEvidenceLab />

对每项旧 API 写出稳定不变量、现代入口和不等价处。UI Toolkit 只放在迁移对照，不占原章名额；最终由总复习跨章验证布局、交互与空间投影。

```text
input -> raycast -> event dispatch -> layout geometry -> canvas render -> evidence
```

  </Step>
</Stepper>

## 正常、边界与失败样本

一个完整实验至少包含正常输入、极端输入和故意破坏配置。正常样本证明主路径可用，边界样本说明数值或设备范围，失败样本则暴露隐式依赖。只保留成功截图会让后续读者无法区分偶然成功和稳定契约。

<Callout type="trap">
  **因果陷阱**：把 UI Toolkit 当作原书章节会制造时代错误：2015 年原书围绕 Unity
  4.6/5 的 uGUI 与开放源码展开。
</Callout>

<Callout type="trap">
  **版本陷阱**：把目录覆盖等同于掌握也不成立：必须保存层级、输入、布局结果和事件命中证据，才能证明概念可复现。
</Callout>

## 从 2015 年到现代 Unity

现代 Unity 仍保留 Canvas、RectTransform、Selectable、EventSystem 和 GraphicRaycaster 这一 uGUI 主链，但 TextMeshPro 常替代旧 Text，新 Input System 可替代 StandaloneInputModule，Package 源码与公开仓库的获取方式也已改变。UI Toolkit 是新的 retained-mode 系统，适合另行比较样式、数据绑定和编辑器工具；它不能抹掉原书所讲的 uGUI 历史。迁移记录应写成“原书载体、保持的不变量、现代入口、不可等价点、测试证据”五列。

迁移记录必须把“原书说了什么”和“今天怎样实现”并排放置。允许替换 API、包和资源，不允许删除原问题或把新版能力倒写成 2015 年事实；无法等价时应保留差异和失败证据，而不是强行宣布一一对应。

## 验收证据

导读验收要求七个原始单元全部有唯一页面，题目与页面 slug 一致，每章至少能指向一个可运行场景。还要检查侧栏顺序严格按 1 到 6 再到附录，现代内容没有冒充原目录，中文版与英文版元数据可以互相追溯。

证据包还应包含 Unity 版本、目标平台、场景路径、复现步骤和预期结果。交给另一位读者后，他不需要猜测隐藏参数就能得到同样结论，才算从“理解文字”进入“掌握系统”。

## 版本与来源核对表

版本核对要把“书名相似”拆成可验证字段：英文原名、作者、出版年月、纸书 ISBN、中文版书名、译者、出版社和中文 ISBN。作者公告给出逐章主题，出版社预览给出完整页码目录与前言中的七个单元说明，中文书目页固定译本身份。三类资料互相补位：作者说明内容意图，出版社固定正式结构，中文书目确认本地书单指向哪一译本。任何后续改造若新增、删除或合并原章，都应先更新这张证据表并说明理由。

目录核对还要区分“原始单元”和“本站教学层”。导读负责建立版本、依赖和阅读方法；总复习负责跨章验收；二者可以有更多实验和现代迁移，却不能让覆盖率从七变成九。相反，附录虽然不是正文章，却是出版社正式列出的原始单元，并为第 5 章提供场景夹具，因此必须单独通过。这个计数规则能防止页面数量替代内容边界，也能让后续链接和题库保持一对一。

## 小结

- 原书身份固定为 2015 年英文版的 6 章加附录，中文版是 2017 年清华大学出版社译本
- 学习主线是历史差异、布局、控件、锚点、空间模式、事件源码与 3D 样例
- UI Toolkit、TextMeshPro 和新输入系统只作为现代迁移，不计入原始覆盖率
- 每章都用范围、实验和证据三种视图完成可复现验收

<Exercises>

**问题 1** 为什么出版社目录中的 Preface 和 Index 不应计入正文章数？

<Answer>
  它们是前置说明和检索辅助，不承担独立章节知识单元。覆盖率以六个 Chapter 和一个
  Appendix 为分母，导读与总复习同样不占名额。
</Answer>

**问题 2** 画出一次按钮点击从设备到回调的最短路径。

<Answer>
  设备输入先由 Input Module 转成 PointerEventData，EventSystem
  组织射线检测，GraphicRaycaster 返回目标，ExecuteEvents 或 Selectable
  状态机派发，最终触发 Button.onClick 监听器。
</Answer>

**问题 3** 怎样忠实加入 UI Toolkit 对照而不篡改原书？

<Answer>
  先完整讲清原书 uGUI 问题和实现，再在独立迁移段说明 UI Toolkit
  的等价目标、不同对象模型和不等价能力；目录和覆盖率仍按原书七个单元计算。
</Answer>

</Exercises>

<Glossary>
  <GlossaryItem term="权威目录">
    由出版社预览、作者说明、ISBN 和中文版书目信息共同固定的原书结构。
  </GlossaryItem>
  <GlossaryItem term="原书边界">
    只把原版六章和附录中的主题计入覆盖率，现代补充必须另行标注。
  </GlossaryItem>
  <GlossaryItem term="UI 事件链">
    输入模块、EventSystem、Raycaster、目标组件与回调依次传递交互的路径。
  </GlossaryItem>
  <GlossaryItem term="布局约束">
    锚点、轴心、尺寸、父级矩形与布局组件共同决定几何结果的规则。
  </GlossaryItem>
  <GlossaryItem term="迁移边界">
    区分 2015 年历史载体、仍成立的不变量和现代 Unity 替代方案。
  </GlossaryItem>
</Glossary>

<Attribution
  adaptedFrom="Simon Jackson《Unity 3D UI Essentials》出版社预览目录与作者官方章节说明"
  adaptedUrl="https://api.pageplace.de/preview/DT0400.9781783553624_A24173589/preview-9781783553624_A24173589.pdf"
/>
