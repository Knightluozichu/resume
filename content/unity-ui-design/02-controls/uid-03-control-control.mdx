---
title: "Chapter 3. Control, Control, You Must Learn Control"
type: A
section: Unity UI 设计 · 控件与响应式
order: 3
description: >-
  把文本、图像、Button、Selectable、Toggle、Slider、ScrollRect、Mask、导航与着色器编成控件状态链；用布局探针、事件路由和多分辨率发布门完成独立复核。
demo: true
math: false
sourceUrl: "https://darkgenesis.zenithmoon.com/announcing-unity-3d-ui-essentials.html"
draft: false
qualityVersion: 2
practiceMode: simulation
sourceMode: independent-rewrite
officialUnitId: uid-03
---

import {
  Objectives,
  Term,
  Callout,
  Stepper,
  Step,
  Exercises,
  Answer,
  Glossary,
  GlossaryItem,
  Attribution,
} from "@/components/mdx/mdx-components";
import {
  Uid03ControlControlLayoutProbe,
  Uid03ControlControlEventRouteLab,
  Uid03ControlControlReleaseGateLab,
} from "@/components/mdx/unity-ui-design/v2/uid-03-control-control";

<Objectives>

- 能说明“Chapter 3. Control, Control, You Must Learn Control”如何把文本、图像、Button、Selectable、Toggle、Slider、ScrollRect、Mask、导航与着色器编成控件状态链，并明确2015年uGUI原书与Unity 6现行资料的时代边界
- 能先预测“怎样证明控件在视觉状态、选择状态、导航状态和事件输出上保持一致，并覆盖鼠标、键盘和触摸？”的布局或事件结果，再沿层级、矩形、画布、相机、射线和处理器逐阶段核对
- 能注入“视觉上禁用按钮但Selectable仍可导航和触发，键盘用户进入不可见或无效状态”，用“控件可见状态、Selectable状态、导航邻接、交互命中和事件值指向同一对象身份”决定接受、修正或拒绝UI发布

</Objectives>

## 为什么从这个UI任务开始

控件页不以截图判定正确；视觉、选择、导航、射线和回调五条状态必须对齐，尤其要保留非指针输入证据。 “Chapter 3. Control, Control, You Must Learn Control”使用的贯穿任务是：实现音量设置页，组合文本、切片图、按钮、Toggle、Slider和滚动容器，并逐输入方式验证。 操作前先预测哪个矩形、射线目标或事件处理器会变化，运行后再补理由不算预测。

本页围绕“怎样证明控件在视觉状态、选择状态、导航状态和事件输出上保持一致，并覆盖鼠标、键盘和触摸？”建立正常、故障与恢复路径。只有“Chapter 3. Control, Control, You Must Learn Control”保持“控件可见状态、Selectable状态、导航邻接、交互命中和事件值指向同一对象身份”并交付控件层级、图像类型、文本效果、Selectable状态图、导航邻接表、事件值、遮罩命中和输入方式测试。，编辑器画面才构成UI证据。

## 书目、38组条目与UI时代

“Chapter 3. Control, Control, You Must Learn Control”以[作者公告](https://darkgenesis.zenithmoon.com/announcing-unity-3d-ui-essentials.html)核对Simon Jackson在2014年发布的《Unity 3D UI Essentials》及其围绕Unity 4.6新UI、动态缩放、效果和世界空间界面的定位；[出版社授权预览](https://api.pageplace.de/preview/DT0400.9781783553624_A24173589/preview-9781783553624_A24173589.pdf)核对第一版详细目录，[中文版书页](https://e.dangdang.com/touch/products/1901049187.html)核对《Unity UI设计》出版信息。课程分母为六章与附录合并后的38组公开目录条目。

“Chapter 3. Control, Control, You Must Learn Control”只依据作者公告、授权样章和公开目录限定范围，不逐段改写原文；解释、状态模型、交互、练习与答案均为独立教学重写。每个合并目录组保留组内全部英文小节名，不能用一个现代主题替换。

“Chapter 3. Control, Control, You Must Learn Control”另以[技术核对 1](https://docs.unity3d.com/Packages/com.unity.ugui@2.0/manual/script-Selectable.html)、[技术核对 2](https://docs.unity3d.com/Packages/com.unity.ugui@2.0/manual/UIAutoLayout.html)、[技术核对 3](https://docs.unity3d.com/Packages/com.unity.ugui@2.0/manual/EventSystem.html)、[技术核对 4](https://docs.unity3d.com/Packages/com.unity.ugui@2.0/manual/Raycasters.html)核对现行技术事实。2015年的旧GUI对照、uGUI初版面板和源码仓库流程保留为原书时代轨；Unity 6的uGUI 2.0与UI系统比较只验证稳定布局、画布和事件机制或说明迁移边界，不能反向证明原书当年的菜单、包和默认值。

## 公开目录条目与UI机制

### Dealing with text；Shadows and effects

<Term def="Dealing with text对应目录条目组“Dealing with text；Shadows and effects”，在“Chapter 3. Control, Control, You Must Learn Control”中用于由Graphic数据、网格和材质生成文字或图像视觉状态，并受Unity时代、层级、矩形、画布、输入和相机边界约束。">
  Dealing with text
</Term>

**公开坐标 1/5。** 在“Chapter 3. Control, Control, You Must Learn Control”的坐标1中，Dealing with text；Shadows and effects用于由Graphic数据、网格和材质生成文字或图像视觉状态；先锁定场景和层级，再用源资产、Image Type、边框、填充、材质和裁剪结果复核，出现拉伸资产掩盖九宫格或像素密度错误时不得发布。

### Image types；Simple Images；Sliced Images；Tiled Images；Filled Images

<Term def="Image types对应目录条目组“Image types；Simple Images；Sliced Images；Tiled Images；Filled Images”，在“Chapter 3. Control, Control, You Must Learn Control”中用于由Graphic数据、网格和材质生成文字或图像视觉状态，并受Unity时代、层级、矩形、画布、输入和相机边界约束。">
  Image types
</Term>

**公开坐标 2/5。** Image types；Simple Images；Sliced Images；Tiled Images；Filled Images进入“Chapter 3. Control, Control, You Must Learn Control”后要回答第2张布局卡：它怎样由Graphic数据、网格和材质生成文字或图像视觉状态、改变哪个矩形或目标、由哪些源资产、Image Type、边框、填充、材质和裁剪结果证明，并如何排除拉伸资产掩盖九宫格或像素密度错误。

### RawImage；Button；Selectable

<Term def="RawImage对应目录条目组“RawImage；Button；Selectable”，在“Chapter 3. Control, Control, You Must Learn Control”中用于由Graphic数据、网格和材质生成文字或图像视觉状态，并受Unity时代、层级、矩形、画布、输入和相机边界约束。">
  RawImage
</Term>

**公开坐标 3/5。** 围绕“怎样证明控件在视觉状态、选择状态、导航状态和事件输出上保持一致，并覆盖鼠标、键盘和触摸？”，条目3把RawImage；Button；Selectable解释为由Graphic数据、网格和材质生成文字或图像视觉状态；复核者先读取源资产、Image Type、边框、填充、材质和裁剪结果再判断画面，不能接受拉伸资产掩盖九宫格或像素密度错误。

### Grouping toggles；Dynamic event properties；Sliding opportunities

<Term def="Grouping toggles对应目录条目组“Grouping toggles；Dynamic event properties；Sliding opportunities”，在“Chapter 3. Control, Control, You Must Learn Control”中用于把可选择状态、值、导航邻接和视觉过渡绑定到同一控件，并受Unity时代、层级、矩形、画布、输入和相机边界约束。">
  Grouping toggles
</Term>

**公开坐标 4/5。** 对“Chapter 3. Control, Control, You Must Learn Control”而言，Grouping toggles；Dynamic event properties；Sliding opportunities的最小合同是把可选择状态、值、导航邻接和视觉过渡绑定到同一控件，第4次检查保存Selectable状态、导航表、值变化、回调参数和材质输出；若产生只改变视觉而未同步交互状态，就回到层级、布局或事件上游。

### Scroll Rect；Masking；Navigation；A word on shaders

<Term def="Scroll Rect对应目录条目组“Scroll Rect；Masking；Navigation；A word on shaders”，在“Chapter 3. Control, Control, You Must Learn Control”中用于用视口裁剪内容并按产品度量政策缩放Canvas，并受Unity时代、层级、矩形、画布、输入和相机边界约束。">
  Scroll Rect
</Term>

**公开坐标 5/5。** 第5个公开条目组Scroll Rect；Masking；Navigation；A word on shaders服务于把文本、图像、Button、Selectable、Toggle、Slider、ScrollRect、Mask、导航与着色器编成控件状态链，需要以视口、内容边界、缩放模式、参考分辨率、Match与安全区呈现用视口裁剪内容并按产品度量政策缩放Canvas；以单一16:9截图替代边界矩阵会破坏“控件可见状态、Selectable状态、导航邻接、交互命中和事件值指向同一对象身份”。

## 先预测，再操作三个UI实验

<Callout type="info" title="先写出哪个矩形或事件跳会先变化">
  对“Chapter 3. Control, Control, You Must Learn
  Control”先选择版本化场景、层级、视口、输入与预期目标，再操作布局探针、事件路由和发布门；结果与预测不一致时应修改UI假设，不删除失败运行。
</Callout>

<Stepper>
  <Step title="1. 画布锚点与布局探针">
    切换“音量滑块、开关组、导航焦点环”以及手机竖屏、横屏和桌面窗口，核对Anchor Min/Max、Pivot、offset与安全区怎样连接“Chapter 3. Control, Control, You Must Learn Control”。

    <Uid03ControlControlLayoutProbe />

  </Step>
  <Step title="2. 正常与故障事件路由">
    保持“实现音量设置页，组合文本、切片图、按钮、Toggle、Slider和滚动容器，并逐输入方式验证。”不变，切换正常和故障模式，沿Input Module、Raycaster、目标与处理器定位“视觉上禁用按钮但Selectable仍可导航和触发，键盘用户进入不可见或无效状态”的第一处偏离。

    <Uid03ControlControlEventRouteLab />

  </Step>
  <Step title="3. 多分辨率发布门">
    分别切换层级锚点、布局所有权、事件路由和目标构建验证，展开控件层级、图像类型、文本效果、Selectable状态图、导航邻接表、事件值、遮罩命中和输入方式测试。后决定是否提交。

    <Uid03ControlControlReleaseGateLab />

  </Step>
</Stepper>

<Callout
  type="trap"
  title="本页UI故障：视觉上禁用按钮但Selectable仍可导航和触发，键盘用户进入不可见或无效状态"
>
  “Chapter 3. Control, Control, You Must Learn
  Control”遇到该故障时应保持Unity版本、场景、层级、视口、输入和初始状态不变，沿布局到事件方向寻找最早偏离；用最终截图看似正常掩盖双写、错相机或不可导航状态，不能证明“控件可见状态、Selectable状态、导航邻接、交互命中和事件值指向同一对象身份”。
</Callout>

<Callout type="trap" title="Game视图截图不等于目标构建">
  实现音量设置页，组合文本、切片图、按钮、Toggle、Slider和滚动容器，并逐输入方式验证。
  在参考分辨率中看起来正确，只证明一个编辑器视口；“Chapter 3. Control, Control,
  You Must Learn
  Control”仍需窄屏、超宽屏、安全区、多输入方式、冷启动和目标平台证据。
</Callout>

<Callout type="trap" title="Unity 6不能倒填2015步骤">
  “Chapter 3. Control, Control, You Must Learn
  Control”引用现行uGUI和UI系统比较资料是为了核对稳定机制及迁移边界，不能把UI
  Toolkit、当前包结构或菜单宣称成原书已有内容。
</Callout>

## Chapter 3. Control, Control, You Must Learn Control的可重放UI协议

| 阶段                 | UI动作                                                                                                        | 必留证据                                    | 拒绝条件                                                               |
| -------------------- | ------------------------------------------------------------------------------------------------------------- | ------------------------------------------- | ---------------------------------------------------------------------- |
| 建立视觉与交互状态   | 在“Chapter 3. Control, Control, You Must Learn Control”执行建立视觉与交互状态，只允许声明组件写入布局或状态   | Unity版本、场景层级、视口与初始状态         | 层级或对象身份不可追溯                                                 |
| 连接控件值和动态事件 | 在“Chapter 3. Control, Control, You Must Learn Control”执行连接控件值和动态事件，只允许声明组件写入布局或状态 | RectTransform、Canvas、相机、射线与事件轨迹 | 视觉上禁用按钮但Selectable仍可导航和触发，键盘用户进入不可见或无效状态 |
| 验证遮罩导航与材质   | 在“Chapter 3. Control, Control, You Must Learn Control”执行验证遮罩导航与材质，只允许声明组件写入布局或状态   | 分辨率矩阵、构建截图、迁移与回退记录        | 无法重放或恢复基线                                                     |

```yaml
unit: "uid-03"
question: "怎样证明控件在视觉状态、选择状态、导航状态和事件输出上保持一致，并覆盖鼠标、键盘和触摸？"
scenario: "实现音量设置页，组合文本、切片图、按钮、Toggle、Slider和滚动容器，并逐输入方式验证。"
panels: ["音量滑块", "开关组", "导航焦点环"]
stages: ["建立视觉与交互状态", "连接控件值和动态事件", "验证遮罩导航与材质"]
invariant: "控件可见状态、Selectable状态、导航邻接、交互命中和事件值指向同一对象身份"
fault: "视觉上禁用按钮但Selectable仍可导航和触发，键盘用户进入不可见或无效状态"
evidence: "控件层级、图像类型、文本效果、Selectable状态图、导航邻接表、事件值、遮罩命中和输入方式测试。"
reset: restore_viewport_panel_route_step_gates_and_artifact
```

该协议要求“Chapter 3. Control, Control, You Must Learn Control”在相同Unity版本、场景、层级、视口、输入和初始状态下重放。重置后若视口、面板、路由模式、步骤、发布门或证据显示没有回到基线，交互状态已经污染比较，不能作为UI证据。

## 本页回顾

掌握“Chapter 3. Control, Control, You Must Learn Control”不是记住编辑器点击顺序，而是能围绕“怎样证明控件在视觉状态、选择状态、导航状态和事件输出上保持一致，并覆盖鼠标、键盘和触摸？”重建UI状态，并用“控件可见状态、Selectable状态、导航邻接、交互命中和事件值指向同一对象身份”拒绝“视觉上禁用按钮但Selectable仍可导航和触发，键盘用户进入不可见或无效状态”。最终交付为控件层级、图像类型、文本效果、Selectable状态图、导航邻接表、事件值、遮罩命中和输入方式测试。

## 练习与答案

<Exercises>

1. **问题 1：UI合同。** “Chapter 3. Control, Control, You Must Learn Control”为什么必须先声明Unity版本、场景层级、视口、布局写入者、相机与输入模块？

<Answer>
  实现音量设置页，组合文本、切片图、按钮、Toggle、Slider和滚动容器，并逐输入方式验证。
  若这些条件不固定，相同输入可能读取不同矩形、相机、排序或处理器；“Chapter 3.
  Control, Control, You Must Learn
  Control”先声明合同，才能把画面连接到可验证状态，并防止参考分辨率成功掩盖运行差异。
</Answer>

2. **问题 2：目录逐项覆盖。** 怎样证明公开条目组已经进入机制、交互和练习？

<Answer>
  1. “Chapter 3. Control, Control, You Must Learn Control”的Dealing with
  text；Shadows and
  effects：以“由Graphic数据、网格和材质生成文字或图像视觉状态”解释UI作用，用“源资产、Image
  Type、边框、填充、材质和裁剪结果”提供复核。 2. “Chapter 3. Control, Control,
  You Must Learn Control”的Image types；Simple Images；Sliced Images；Tiled
  Images；Filled
  Images：以“由Graphic数据、网格和材质生成文字或图像视觉状态”解释UI作用，用“源资产、Image
  Type、边框、填充、材质和裁剪结果”提供复核。 3. “Chapter 3. Control, Control,
  You Must Learn
  Control”的RawImage；Button；Selectable：以“由Graphic数据、网格和材质生成文字或图像视觉状态”解释UI作用，用“源资产、Image
  Type、边框、填充、材质和裁剪结果”提供复核。 4. “Chapter 3. Control, Control,
  You Must Learn Control”的Grouping toggles；Dynamic event properties；Sliding
  opportunities：以“把可选择状态、值、导航邻接和视觉过渡绑定到同一控件”解释UI作用，用“Selectable状态、导航表、值变化、回调参数和材质输出”提供复核。
  5. “Chapter 3. Control, Control, You Must Learn Control”的Scroll
  Rect；Masking；Navigation；A word on
  shaders：以“用视口裁剪内容并按产品度量政策缩放Canvas”解释UI作用，用“视口、内容边界、缩放模式、参考分辨率、Match与安全区”提供复核。
</Answer>

3. **问题 3：故障恢复。** 怎样证明“视觉上禁用按钮但Selectable仍可导航和触发，键盘用户进入不可见或无效状态”已经被修正？

<Answer>
  为“Chapter 3. Control, Control, You Must Learn
  Control”复用同一Unity版本、场景、层级、视口、输入和初始状态，重放正常路径后只注入“视觉上禁用按钮但Selectable仍可导航和触发，键盘用户进入不可见或无效状态”；记录最早偏离点，撤销故障并再次运行。只有布局探针、事件路由、发布门和控件层级、图像类型、文本效果、Selectable状态图、导航邻接表、事件值、遮罩命中和输入方式测试。重新满足“控件可见状态、Selectable状态、导航邻接、交互命中和事件值指向同一对象身份”，修正才可提交。
</Answer>

</Exercises>

## 名词解释

<Glossary>
  <GlossaryItem term="Dealing with text">
    对应“Dealing with text；Shadows and effects”；在“Chapter 3. Control,
    Control, You Must Learn
    Control”中用于由Graphic数据、网格和材质生成文字或图像视觉状态，需要连接Unity时代、层级、矩形、画布、输入与目标。
  </GlossaryItem>
  <GlossaryItem term="Image types">
    对应“Image types；Simple Images；Sliced Images；Tiled Images；Filled
    Images”；在“Chapter 3. Control, Control, You Must Learn
    Control”中用于由Graphic数据、网格和材质生成文字或图像视觉状态，需要连接Unity时代、层级、矩形、画布、输入与目标。
  </GlossaryItem>
  <GlossaryItem term="RawImage">
    对应“RawImage；Button；Selectable”；在“Chapter 3. Control, Control, You Must
    Learn
    Control”中用于由Graphic数据、网格和材质生成文字或图像视觉状态，需要连接Unity时代、层级、矩形、画布、输入与目标。
  </GlossaryItem>
  <GlossaryItem term="Grouping toggles">
    对应“Grouping toggles；Dynamic event properties；Sliding
    opportunities”；在“Chapter 3. Control, Control, You Must Learn
    Control”中用于把可选择状态、值、导航邻接和视觉过渡绑定到同一控件，需要连接Unity时代、层级、矩形、画布、输入与目标。
  </GlossaryItem>
  <GlossaryItem term="Scroll Rect">
    对应“Scroll Rect；Masking；Navigation；A word on shaders”；在“Chapter 3.
    Control, Control, You Must Learn
    Control”中用于用视口裁剪内容并按产品度量政策缩放Canvas，需要连接Unity时代、层级、矩形、画布、输入与目标。
  </GlossaryItem>
</Glossary>

<Attribution
  mode="independent-rewrite"
  sourceBasis="authorized-sample"
  workTitle="Simon Jackson《Unity 3D UI Essentials》"
  adaptedUrl="https://darkgenesis.zenithmoon.com/announcing-unity-3d-ui-essentials.html"
/>
