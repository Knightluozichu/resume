---
title: "第 5 章：屏幕空间、世界空间与相机"
type: A
section: "Unity UI 设计 · 空间与源码"
order: 5
description: "比较三种 Canvas 模式、渲染相机与事件相机，完成屏幕血条和 3D 世界 UI 的坐标与交互闭环。"
demo: true
math: false
sourceUrl: "https://api.pageplace.de/preview/DT0400.9781783553624_A24173589/preview-9781783553624_A24173589.pdf"
draft: false
---


## 问题：为什么 UI 看得见却点不到，或跟随世界目标时不断漂移

先预测：World Space Canvas 的 worldCamera 为空时，GraphicRaycaster 应用哪台相机解释指针？答案依赖 Canvas 模式和事件相机配置。第 5 章把视觉相机、事件相机、透视投影、屏幕血条与场景内 Canvas 放在一起，解决的正是“看见的像素”和“命中的几何”是否来自同一坐标链。

## 原书边界：Chapter 5 · Screen Space, World Space, and the Camera

原书 Chapter 5 包含 The Canvas and Cameras、Screen Space and World Space、Render cameras、Event Cameras、Getting some perspective，以及 big game 示例：Sprite 2D、Screen Space-Camera health bar、Canvas 内容、状态判断、从世界到屏幕、flat/deep 两种呈现、把 Canvas 放进世界、showcase、scale 问题、改进方案和最终 Event Camera 说明。

- **The Canvas and Cameras；Screen Space and World Space；Render cameras；Event Cameras**
- **Getting some perspective；Setting up for the big game；Some prerequisites；Sprite 2D work**
- **A Screen Space**：Camera health bar；What's in a Canvas?；Am I dead yet?
- **Reaching in；It's all gone a bit flat；Going deep**
- **Hang your Canvas wherever you like；The showcase；Build your UI and place it in the scene**
- **Troubles with scale；A better way；A final word on Event Cameras**

这些小节按出版社目录保持唯一归属；本站的实验、现代迁移与复习只是教学增强，不改变原章身份，也不拿一个现代主题重复补多个原始单元。

## 核心模型：从对象到可观察结果

Overlay Canvas 直接在屏幕空间末端合成，不需要渲染相机；Screen Space Camera 把 Canvas 放在指定相机前方平面，受 planeDistance、相机 viewport 和排序影响；World Space Canvas 是普通场景几何，尺寸、朝向、深度和遮挡都进入世界坐标。Render Camera 决定视觉投影，Event Camera 则让 GraphicRaycaster 把屏幕指针转换成 UI 平面射线，二者可能同一台但职责不同。

用于本章的可观察模型；判断它是否成立时，要回到输入、状态、几何或事件日志，而不是只凭最终截图。

用于本章的可观察模型；判断它是否成立时，要回到输入、状态、几何或事件日志，而不是只凭最终截图。

用于本章的可观察模型；判断它是否成立时，要回到输入、状态、几何或事件日志，而不是只凭最终截图。

用于本章的可观察模型；判断它是否成立时，要回到输入、状态、几何或事件日志，而不是只凭最终截图。

用于本章的可观察模型；判断它是否成立时，要回到输入、状态、几何或事件日志，而不是只凭最终截图。

## 关键对象与生产者到消费者链

世界目标血条通常先用 Camera.WorldToScreenPoint 得到屏幕点，再由 RectTransformUtility.ScreenPointToLocalPointInRectangle 转换到 Canvas 本地坐标。若使用 World Space Canvas，则可以直接跟随目标位置并朝向相机，但要处理世界尺度、像素密度、遮挡和近远裁剪。还要检查目标在相机后方、离开 viewport、被遮挡或死亡时的显示规则，坐标正确只是第一层。

为了避免“改完好像对了”，先写下生产者、变换规则、消费者与可观测量。每个对象都必须能在层级、Inspector、事件日志、坐标记录或渲染结果中找到证据；没有观测点的解释不能进入结论。

## 失败边界与常见误判


失败样本要保留输入、层级、关键参数和日志。只保存一张错误截图，会丢掉父矩形、相机、输入模块、射线排序或版本差异这些真正决定行为的条件。

## 从 Unity 4.6/5 到现代 Unity

现代 Unity 在 URP/HDRP、Camera Stack、XR 多眼和动态分辨率下增加了更多相机边界，但三种 Canvas 模式和视觉/事件相机分工仍适用。迁移时要在实际渲染管线与目标平台测排序、后处理、XR 眼纹理和 Input System 指针；不能用编辑器 Scene View 的可见性代替 Game Camera 证据。

迁移报告采用四列：原书对象、稳定不变量、现代载体、已知差异。若现代载体改变事件模型、坐标空间或序列化方式，就应判为“功能近似，需要重新验收”，不能写成无差别替换。

## 验收证据包

验收场景同时包含 Overlay HUD、Camera 模式菜单和 World Space 终端。记录目标 world position、WorldToScreenPoint 结果、Canvas local point、相机 viewport、planeDistance、sorting、eventCamera、raycast hit 与遮挡结果。移动相机、切换宽高比和把目标放到相机背后，确认失败规则可重放。

验收还要包含正常、边界和失败三类样本。正常样本证明主路径，边界样本证明约束，失败样本证明诊断方法确实能抓到错误。三者使用同一场景与记录格式，下一版本才能直接回归。

## 小结

- 三种 Canvas 模式决定 UI 位于屏幕链还是世界链
- Render Camera 负责视觉，Event Camera 负责输入坐标解释
- 世界跟随 UI 需要处理两段坐标转换和相机背面边界
- 世界空间 UI 还要验收尺度、朝向、深度、遮挡与命中


