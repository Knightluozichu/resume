---
title: "《Unity 6 URP内置Shader源码解析》全图"
type: B
section: "Unity 6 URP源码 · 全图"
order: 0
description: "沿Unity 6 URP固定提交掌握包入口、Lit主链、内置材质族、专用Shader族、共享库。"
demo: true
math: true
sourceUrl: "https://github.com/Unity-Technologies/Graphics/tree/275a7f9ad9330e3cf7f3ea57a0b242a551fde291/Packages/com.unity.render-pipelines.universal/Shaders"
draft: true
---

import { Objectives, Callout, Attribution, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step } from "@/components/mdx/mdx-components";

<Objectives>
- 能从固定提交和真实文件解释包入口、Lit主链与内置材质族
- 能绘制ShaderLab、HLSL include、数据结构、函数与Renderer Pass的生产者到消费者链
- 能判断共享库是否达标，同时保存源码定位、Frame Debugger、GPU事件和失败变体
</Objectives>

## 先预测，再读源码

先预测这个文件或符号会影响哪个Pass、缓冲、关键字和GPU阶段，再打开源码与帧调试观察。本课程不是出版书，而是对Unity官方URP包的源码课程。目录固定到Unity-Technologies/Graphics的6000.0/staging提交275a7f9a，21个单元按真实文件拓扑组织；任何结论都必须能回到提交、路径、符号和运行时帧证据。 本单元用<Term def="从Shaders.cs和ShaderPathID定位内置资源。">包入口</Term>、<Term def="从Properties、SurfaceData、Forward与GBuffer追踪PBR。">Lit主链</Term>、<Term def="比较SimpleLit、ComplexLit、BakedLit、Unlit与Particles。">内置材质族</Term>、<Term def="覆盖Terrain、SpeedTree、2D、Decal和PostProcessing。">专用Shader族</Term>、<Term def="以Core、Lighting、GI、Shadows和工程宏解释复用机制。">共享库</Term>建立静态源码与运行时证据之间的对应关系。

<UusOfficialLearningMapPipelineLab />

## 官方源码基线

本页一一对应 Unity Technologies 官方 Graphics 仓库 6000.0/staging 提交 275a7f9a 中的源码单元。课程核对日期为 2026-07-15；所有路径均固定到该提交，不把 master 开发代码与发布分支混用。

### 必读文件

1. `Packages/com.unity.render-pipelines.universal/Shaders`
2. `Packages/com.unity.render-pipelines.universal/ShaderLibrary`

### 五个核心符号

1. **包入口**：从Shaders.cs和ShaderPathID定位内置资源。
2. **Lit主链**：从Properties、SurfaceData、Forward与GBuffer追踪PBR。
3. **内置材质族**：比较SimpleLit、ComplexLit、BakedLit、Unlit与Particles。
4. **专用Shader族**：覆盖Terrain、SpeedTree、2D、Decal和PostProcessing。
5. **共享库**：以Core、Lighting、GI、Shadows和工程宏解释复用机制。

## 生产者到消费者链

本课程不是出版书，而是对Unity官方URP包的源码课程。目录固定到Unity-Technologies/Graphics的6000.0/staging提交275a7f9a，21个单元按真实文件拓扑组织；任何结论都必须能回到提交、路径、符号和运行时帧证据。 阅读顺序固定为资源入口、ShaderLab Properties与Pass、HLSL输入结构、核心函数、共享库、运行时Renderer选择。每个箭头都写明文件、符号、参数、关键字和目标缓冲。

$$
path=package->shader->pass->library->renderer
$$

$$
coverage=21 source units
$$

$$
evidence=commit+path+symbol+capture
$$

$$
upgrade=diff(oldCommit,newCommit)
$$

公式中的位置、方向、深度、颜色和索引必须标注空间、范围与来源。源码中能找到函数不证明它在当前变体执行，必须继续核对pragma、关键字、LightMode和Renderer配置。

## 最小源码探针

```hlsl
// Pin commit 275a7f9a before reading or modifying URP package source
```

探针顺序是常量输出、输入结构可视化、核心函数分项、固定状态和目标缓冲。先确认实际Shader变体和Pass，再解释颜色或性能差异。

<UusOfficialLearningMapCompareLab />

## 运行时互证

```text
source: commit + package path + symbol
material: shader + keywords + UnityPerMaterial layout
renderer: renderer asset + rendering path + pass event
frame: draw + LightMode + target + attachments
evidence: Frame Debugger + GPU capture + variant log
```

用Frame Debugger确认实际Pass和关键字，用GPU捕获确认RenderState、资源绑定、MRT与时序，用构建日志确认变体是否保留。修改包源码时还要记录嵌入包与上游提交的差异。

## 边界、兼容与性能

测试Forward与Deferred、主光与附加光、Lightmap与Probe、透明与Alpha Clip、XR单通道实例、动态分辨率、DOTS实例、不同图形API和移动Tile GPU。固定Unity版本、平台、分辨率、VSync和场景后再比较CPU/GPU时间。

```text
baseline: official commit 275a7f9ad933
candidate: exact local diff
correctness: image and buffer diff
compatibility: renderer modes, APIs, XR and platforms
performance: variants, SetPass, samples, bandwidth, CPU/GPU ms
failure: smallest replayable configuration
```

1. 在固定提交打开必读文件，定位入口、include、结构和核心函数。
2. 画出生产者到消费者链，并标注关键字与LightMode。
3. 用最小材质和场景触发目标变体，保存中间缓冲。
4. 构造失败配置，用源码差异和GPU事件解释原因。

## 验收证书

证书包含提交、文件、符号、调用和include链、材质与关键字、Renderer配置、Pass事件、目标缓冲、GPU计时与失败反例。拿master最新代码解释某个已发布Unity版本，文件和关键字会漂移，读者无法复现。 修复后必须重放同一配置，并指出静态源码和运行时事件各发生了什么变化。

## 常见误区

<Callout type="trap" title="源码版本没有固定">拿master最新代码解释某个已发布Unity版本，文件和关键字会漂移，读者无法复现。</Callout>

<Callout type="trap" title="静态代码等于运行时">函数被include不代表当前关键字和LightMode会执行它。必须用变体日志、Frame Debugger和GPU捕获互证。</Callout>

## 本章回顾

本页覆盖包入口、Lit主链、内置材质族、专用Shader族、共享库。掌握标准是能给出固定路径和符号，画出数据链，触发对应Pass，再用正确性、兼容性和性能证据复现结论。

<Exercises>
**问题1：包入口与Lit主链如何连接？**
<Answer>从Shaders.cs和ShaderPathID定位内置资源。 从Properties、SurfaceData、Forward与GBuffer追踪PBR。 回答时必须给出文件、符号和运行Pass。</Answer>

**问题2：怎样调试内置材质族？**
<Answer>先定位生产者，再追踪结构参数和消费者；随后核对pragma、关键字、LightMode、Frame Debugger和GPU事件。</Answer>

**问题3：如何验收共享库？**
<Answer>以Core、Lighting、GI、Shadows和工程宏解释复用机制。 同时保存正常变体、边界变体、失败配置和CPU/GPU计时。</Answer>
</Exercises>

## 术语表

<Glossary>
  <GlossaryItem term="包入口" definition="从Shaders.cs和ShaderPathID定位内置资源。" />
  <GlossaryItem term="Lit主链" definition="从Properties、SurfaceData、Forward与GBuffer追踪PBR。" />
  <GlossaryItem term="内置材质族" definition="比较SimpleLit、ComplexLit、BakedLit、Unlit与Particles。" />
  <GlossaryItem term="专用Shader族" definition="覆盖Terrain、SpeedTree、2D、Decal和PostProcessing。" />
  <GlossaryItem term="共享库" definition="以Core、Lighting、GI、Shadows和工程宏解释复用机制。" />
  <GlossaryItem term="源码证书" definition="固定提交、路径、符号、变体和运行事件后可再次验证同一结论的记录。" />
</Glossary>

<Attribution adaptedFrom="Unity Technologies · Universal Render Pipeline source · 《Unity 6 URP内置Shader源码解析》全图" adaptedUrl="https://github.com/Unity-Technologies/Graphics/tree/275a7f9ad9330e3cf7f3ea57a0b242a551fde291/Packages/com.unity.render-pipelines.universal/Shaders" />
