---
title: "源码单元1 包入口与Shader索引"
type: C
section: "Unity 6 URP源码 · 包与契约"
order: 1
description: "沿Unity 6 URP固定提交掌握ShaderPathID、Shaders目录、ShaderLibrary目录、包提交、依赖图。"
demo: true
math: true
sourceUrl: "https://github.com/Unity-Technologies/Graphics/tree/275a7f9ad9330e3cf7f3ea57a0b242a551fde291/Packages/com.unity.render-pipelines.universal/Shaders"
draft: true
---

import { Objectives, Callout, Attribution, Glossary, GlossaryItem, Term, Exercises, Answer, Stepper, Step } from "@/components/mdx/mdx-components";

<Objectives>
- 能从固定提交和真实文件解释ShaderPathID、Shaders目录与ShaderLibrary目录
- 能绘制ShaderLab、HLSL include、数据结构、函数与Renderer Pass的生产者到消费者链
- 能判断依赖图是否达标，同时保存源码定位、Frame Debugger、GPU事件和失败变体
</Objectives>

## 先预测，再读源码

先预测这个文件或符号会影响哪个Pass、缓冲、关键字和GPU阶段，再打开源码与帧调试观察。先从Shaders.cs与目录结构建立索引，再按ShaderLab入口、材质输入、Pass实现、共享库和运行时选择五层追踪。课程固定到Unity Graphics 6000.0/staging提交，避免master更新造成源码与讲解错位。 本单元用<Term def="由运行时代码定位URP内置Shader资源的稳定枚举。">ShaderPathID</Term>、<Term def="保存内置材质、Pass实现和专用渲染Shader的源码根。">Shaders目录</Term>、<Term def="保存跨材质共享的输入、光照、阴影和平台函数。">ShaderLibrary目录</Term>、<Term def="把课程内容固定到可复查Git提交的版本证书。">包提交</Term>、<Term def="从ShaderLab include到HLSL函数和运行时Pass的有向关系。">依赖图</Term>建立静态源码与运行时证据之间的对应关系。

<Uus01PackageTopologyPipelineLab />

## 官方源码基线

本页一一对应 Unity Technologies 官方 Graphics 仓库 6000.0/staging 提交 275a7f9a 中的源码单元。课程核对日期为 2026-07-15；所有路径均固定到该提交，不把 master 开发代码与发布分支混用。

### 必读文件

1. `Packages/com.unity.render-pipelines.universal/Shaders/Shaders.cs`
2. `Packages/com.unity.render-pipelines.universal/Shaders/Lit.shader`
3. `Packages/com.unity.render-pipelines.universal/Shaders/SimpleLit.shader`
4. `Packages/com.unity.render-pipelines.universal/Shaders/BakedLit.shader`
5. `Packages/com.unity.render-pipelines.universal/Shaders/Unlit.shader`

### 五个核心符号

1. **ShaderPathID**：由运行时代码定位URP内置Shader资源的稳定枚举。
2. **Shaders目录**：保存内置材质、Pass实现和专用渲染Shader的源码根。
3. **ShaderLibrary目录**：保存跨材质共享的输入、光照、阴影和平台函数。
4. **包提交**：把课程内容固定到可复查Git提交的版本证书。
5. **依赖图**：从ShaderLab include到HLSL函数和运行时Pass的有向关系。

## 生产者到消费者链

先从Shaders.cs与目录结构建立索引，再按ShaderLab入口、材质输入、Pass实现、共享库和运行时选择五层追踪。课程固定到Unity Graphics 6000.0/staging提交，避免master更新造成源码与讲解错位。 阅读顺序固定为资源入口、ShaderLab Properties与Pass、HLSL输入结构、核心函数、共享库、运行时Renderer选择。每个箭头都写明文件、符号、参数、关键字和目标缓冲。

$$
entry=ShaderPathID->ShaderAsset
$$

$$
shader=properties+subshader+passes
$$

$$
pass=includes+keywords+state
$$

$$
proof=commit+path+symbol
$$

公式中的位置、方向、深度、颜色和索引必须标注空间、范围与来源。源码中能找到函数不证明它在当前变体执行，必须继续核对pragma、关键字、LightMode和Renderer配置。

## 最小源码探针

```hlsl
Shader.Find(ShaderUtils.GetShaderPath(ShaderPathID.Lit));
```

探针顺序是常量输出、输入结构可视化、核心函数分项、固定状态和目标缓冲。先确认实际Shader变体和Pass，再解释颜色或性能差异。

<Uus01PackageTopologyCompareLab />

## 运行时互证

```text
source: commit + package path + symbol
material: shader + keywords + UnityPerMaterial layout
renderer: renderer asset + rendering path + pass event
frame: draw + LightMode + target + attachments
evidence: Frame Debugger + GPU capture + variant log
```

用Frame Debugger确认实际Pass和关键字，用GPU捕获确认RenderState、资源绑定、MRT与时序，用构建日志确认变体是否保留。修改包源码时还要记录嵌入包与上游提交的差异。

## 边界、兼容与性能

测试Forward与Deferred、主光与附加光、Lightmap与Probe、透明与Alpha Clip、XR单通道实例、动态分辨率、DOTS实例、不同图形API和移动Tile GPU。固定Unity版本、平台、分辨率、VSync和场景后再比较CPU/GPU时间。

```text
baseline: official commit 275a7f9ad933
candidate: exact local diff
correctness: image and buffer diff
compatibility: renderer modes, APIs, XR and platforms
performance: variants, SetPass, samples, bandwidth, CPU/GPU ms
failure: smallest replayable configuration
```

1. 在固定提交打开必读文件，定位入口、include、结构和核心函数。
2. 画出生产者到消费者链，并标注关键字与LightMode。
3. 用最小材质和场景触发目标变体，保存中间缓冲。
4. 构造失败配置，用源码差异和GPU事件解释原因。

## 验收证书

证书包含提交、文件、符号、调用和include链、材质与关键字、Renderer配置、Pass事件、目标缓冲、GPU计时与失败反例。只记菜单名而不记录包版本和真实路径，升级Unity后仍用旧函数签名解释新源码。 修复后必须重放同一配置，并指出静态源码和运行时事件各发生了什么变化。

## 常见误区

<Callout type="trap" title="源码版本没有固定">只记菜单名而不记录包版本和真实路径，升级Unity后仍用旧函数签名解释新源码。</Callout>

<Callout type="trap" title="静态代码等于运行时">函数被include不代表当前关键字和LightMode会执行它。必须用变体日志、Frame Debugger和GPU捕获互证。</Callout>

## 本章回顾

本页覆盖ShaderPathID、Shaders目录、ShaderLibrary目录、包提交、依赖图。掌握标准是能给出固定路径和符号，画出数据链，触发对应Pass，再用正确性、兼容性和性能证据复现结论。

<Exercises>
**问题1：ShaderPathID与Shaders目录如何连接？**
<Answer>由运行时代码定位URP内置Shader资源的稳定枚举。 保存内置材质、Pass实现和专用渲染Shader的源码根。 回答时必须给出文件、符号和运行Pass。</Answer>

**问题2：怎样调试ShaderLibrary目录？**
<Answer>先定位生产者，再追踪结构参数和消费者；随后核对pragma、关键字、LightMode、Frame Debugger和GPU事件。</Answer>

**问题3：如何验收依赖图？**
<Answer>从ShaderLab include到HLSL函数和运行时Pass的有向关系。 同时保存正常变体、边界变体、失败配置和CPU/GPU计时。</Answer>
</Exercises>

## 术语表

<Glossary>
  <GlossaryItem term="ShaderPathID" definition="由运行时代码定位URP内置Shader资源的稳定枚举。" />
  <GlossaryItem term="Shaders目录" definition="保存内置材质、Pass实现和专用渲染Shader的源码根。" />
  <GlossaryItem term="ShaderLibrary目录" definition="保存跨材质共享的输入、光照、阴影和平台函数。" />
  <GlossaryItem term="包提交" definition="把课程内容固定到可复查Git提交的版本证书。" />
  <GlossaryItem term="依赖图" definition="从ShaderLab include到HLSL函数和运行时Pass的有向关系。" />
  <GlossaryItem term="源码证书" definition="固定提交、路径、符号、变体和运行事件后可再次验证同一结论的记录。" />
</Glossary>

<Attribution adaptedFrom="Unity Technologies · Universal Render Pipeline source · 源码单元1 包入口与Shader索引" adaptedUrl="https://github.com/Unity-Technologies/Graphics/tree/275a7f9ad9330e3cf7f3ea57a0b242a551fde291/Packages/com.unity.render-pipelines.universal/Shaders" />
