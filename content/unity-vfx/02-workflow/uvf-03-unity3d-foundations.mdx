---
title: "第3章 Unity3D基础知识入门"
type: A
section: "Unity 3D 游戏特效制作典型实例 · Unity 工作流"
order: 3
description: "复现编辑器、项目、Max资源导入、粒子属性、碰撞、Prefab、材质、光源和摄像机工作流。"
demo: true
math: false
sourceUrl: "https://www.tenlong.com.tw/products/9787115455222"
draft: true
---

import {
  Objectives,
  Callout,
  Attribution,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
} from "@/components/mdx/mdx-components";
import {
  Uvf03Unity3dFoundationsMapLab,
  Uvf03Unity3dFoundationsExperimentLab,
  Uvf03Unity3dFoundationsEvidenceLab,
} from "@/components/mdx/unity-vfx/diagrams/uvf-03-unity3d-foundations";

<Objectives>

- 能解释第3章在原书9章结构中的独有责任与前后依赖
- 能实现“Max导出、Unity导入、粒子属性”之间可观察、可回放的数据链
- 能比较原书旧版Unity流程与当前Unity迁移方案，并设计失败注入证据

</Objectives>

## 问题：怎样证明自己复现的是第3章，而不是同名现代演示？

先预测：如果删掉原章名称、案例名称和资源约束，只剩一段看起来华丽的实时效果，另一位读者还能从输入、层级与结果反推出本章吗？不能。该章连接DCC资源与Unity运行时。模型、动画和贴图先通过坐标合同进入项目，粒子系统再引用网格、材质与碰撞，最后封装成Prefab并在摄像机下验证构图；任何一步的默认值都可能污染后续案例。

<Uvf03Unity3dFoundationsMapLab />

## 原书边界与目录单元

本页严格对应2017年版目录中的以下单元。它们共同固定第3章的覆盖边界；新增VFX Graph、URP或对象池只能作为迁移补充，不能替代原目录位置：

- **Unity3D界面介绍**
- **如何建立项目工程**
- **导入资源**
- **Max模型导出设置**
- **Max模型导入Unity**
- **Max动画导出设置**
- **Max动画导入Unity**
- **粒子系统的建立**
- **Particle Properties（粒子属性）**
- **拾取外部模型发射**
- **粒子的碰撞**
- **Creating Prefab（创建预制体）**
- **Output Prefab（输出预制体）**
- **Imported Prefab（导入预制体）**
- **Materials and Shaders（材质与着色器）**
- **Lights（光源）**
- **Cameras（摄像机）**
- **Unity3D插件介绍**

书目身份与详细目录采用天珑书店页面，作者、年份和馆藏由浙江大学图书馆题录交叉核对。本站重新组织讲解、代码、实验和证据，不逐字复制原文。导读与总复习用于串联和验收，不重复计入9个正文章。

## 核心模型：从视觉意图到可发布效果

该章连接DCC资源与Unity运行时。模型、动画和贴图先通过坐标合同进入项目，粒子系统再引用网格、材质与碰撞，最后封装成Prefab并在摄像机下验证构图；任何一步的默认值都可能污染后续案例。

制作前先写效果合同：触发事件是谁，效果依附世界、角色还是界面，主体轮廓是什么，前摇、爆发、持续和消散分别多长，哪些资源来自Max或贴图，目标设备允许多少粒子与透明叠加。合同让美术判断与工程门禁使用同一套语言。

同一资源重复导入的轴向、缩放、动画范围、材质引用和粒子结果必须一致。 这条不变量必须同时在编辑器预览、固定摄像机、移动设备和失败回放中成立。静帧相似只能证明构图接近，不能证明触发、生命周期和性能正确。

## 五个核心术语与责任边界

<Term def="统一Max与Unity的单位、轴向、枢轴和缩放的导入约束">资源坐标合同</Term>
它必须落实到第3章的输入、状态、输出和失败信号，不能只作为名词记忆。

<Term def="决定发射、生命周期、速度、颜色、尺寸和渲染的参数集合">粒子属性</Term>
它必须落实到第3章的输入、状态、输出和失败信号，不能只作为名词记忆。

<Term def="从外部模型顶点或表面采样粒子出生位置的方式">网格发射</Term>
它必须落实到第3章的输入、状态、输出和失败信号，不能只作为名词记忆。

<Term def="保存对象层级、组件和资源引用以便重复实例化的模板">预制体</Term>
它必须落实到第3章的输入、状态、输出和失败信号，不能只作为名词记忆。

<Term def="用视锥、层掩码和空间位置确定效果最终构图的过程">摄像机定位</Term>
它必须落实到第3章的输入、状态、输出和失败信号，不能只作为名词记忆。

把术语连成领域模型后，再记录每个效果对象的所有权：由谁创建、绑定哪个坐标空间、何时允许播放、何时停止发射、何时等待尾粒子消失、何时归还对象池。材质实例、共享贴图和临时渲染状态也必须有恢复策略。

## 数据链与最小实现

Max导出 → Unity导入 → 粒子属性 → 材质光源 → Prefab封装 → 摄像机验收 构成本章的主链。每个节点都记录输入来源、状态所有者、更新时间、输出消费者与首个失败信号。视觉系统只消费玩法事实，不自行决定命中、治疗、升级或传送结果。

```csharp
void Configure(ParticleSystem ps, Mesh mesh)
{
    var shape = ps.shape;
    shape.shapeType = ParticleSystemShapeType.Mesh;
    shape.mesh = mesh;
    var collision = ps.collision;
    collision.enabled = true;
}
```

现代实现可以替换旧粒子属性名称、材质接口或资源导入器，但迁移账本必须逐项写明“旧实现、替代实现、保留不变量、验证证据”。这样既避免把旧API当成当前最佳实践，也避免用新工具掩盖原案例缺失。

<Uvf03Unity3dFoundationsExperimentLab />

## 分三步复现并保存中间证据

<Stepper>
  <Step title="1. 固定目录边界和视觉合同">
    先为Unity3D界面介绍、如何建立项目工程、导入资源建立案例卡，写清触发、空间、时长、资源、性能和结束条件。缺少原章映射的效果不进入制作队列。
    <Uvf03Unity3dFoundationsMapLab />
  </Step>
  <Step title="2. 单变量搭建主体与附属层">
    按资源坐标合同、粒子属性和网格发射的依赖顺序逐层启用；每次只改一个参数并保存前后截图、粒子峰值和首条异常日志。
    <Uvf03Unity3dFoundationsExperimentLab />
  </Step>
  <Step title="3. 在目标场景和设备签发">
    用正常、边界、失败三组输入回放，核对空间绑定、透明排序、生命周期、回收和性能包络，再把证据链接回原目录单元。
    <Uvf03Unity3dFoundationsEvidenceLab />
  </Step>
</Stepper>

## 两个必须主动制造的失败样本

<Callout type="trap" title="目录漂移">
  直接在场景中修正错误缩放却不修导入设置，导致网格发射、碰撞半径和Prefab实例出现不同结果。
  这种结果可能看起来完整，却无法证明覆盖第3章。修复方式是先恢复原目录和命名案例，再讨论现代扩展。
</Callout>

<Callout type="trap" title="视觉正确、生命周期错误">
  故意让效果重复触发、目标提前销毁或场景切换。如果材质状态没有恢复、尾粒子被截断、旧回调继续播放或对象池出现重复归还，就说明所有权合同尚未通过。
</Callout>

## 旧版Unity到当前Unity的迁移表

| 原书工作对象     | 当前实现选择                                    | 必须保留的证据                             |
| ---------------- | ----------------------------------------------- | ------------------------------------------ |
| 旧粒子属性与模块 | 当前 Particle System，必要时另列 VFX Graph 扩展 | 发射、生命周期、速度、颜色、尺寸和碰撞对照 |
| 旧材质与透明混合 | Built-in、URP 对应材质或自定义Shader            | 颜色空间、混合、深度、排序与移动端结果     |
| Max模型与动画    | 受控FBX导入、Prefab和版本化源资产               | 单位、轴向、枢轴、动画范围和资源谱系       |
| 手工播放和销毁   | 动作事件、状态事件与对象池                      | 触发时刻、停止发射、尾粒子完成和唯一回收   |

迁移不是“全部改成最新功能”。原书重点是美术工作流与典型实例，现代补充只解决版本差异、资源可维护性和移动性能；不得把后处理、DOTween或布料等无关专题塞进原章并宣称它们属于原目录。

## 证据包、性能预算与自动检查

每次回放输出同一结构，才能比较版本和设备。至少保存原章、案例、构建号、触发ID、帧号、粒子峰值、透明叠加、Draw Call、贴图显存、回收结果和截图哈希。

```json
{
  "chapter": "第3章",
  "case": "Unity3D界面介绍",
  "path": [
    "Max导出",
    "Unity导入",
    "粒子属性",
    "材质光源",
    "Prefab封装",
    "摄像机验收"
  ],
  "particlePeak": 96,
  "drawCalls": 7,
  "recycledOnce": true
}
```

边界测试要同时覆盖低帧率、重复触发、摄像机切换、目标销毁和场景卸载。移动端不要只看平均帧率，还要检查过绘、峰值显存和首次实例化尖峰。

```text
PASS chapter=第3章 normal=true boundary=true failureReplay=true
PASS invariant=同一资源重复导入的轴向、缩放、动画范围、材质引用和粒子结果必须一致。
REJECT when=missing-original-unit | leaked-material | double-release | budget-overrun
```

<Uvf03Unity3dFoundationsEvidenceLab />

## 练习：从复现到可审计交付

<Exercises>

**问题 1** 为“Unity3D界面介绍”写出触发、空间、主体、消散和预算五项效果合同，并标记首个可观察信号。

<Answer>
  合同必须能从Max导出追踪到摄像机验收，并给出所有者、时间和阈值；只描述“好看”不合格。
</Answer>

**问题 2** 故意制造“直接在场景中修正错误缩放却不修导入设置，导致网格发射、碰撞半径和Prefab实例出现不同结果。”，记录首个偏离节点，再用同一输入证明修复。

<Answer>
  失败回放要保留版本、输入、首条日志、截图和修复后对照，不能只给最终画面。
</Answer>

**问题 3** 比较旧版与当前Unity实现，写出三条可替换技术和三条不可改变的不变量。

<Answer>
  API、材质入口和资源管线可迁移；原目录单元、案例语义、阶段关系与可观察结果不可删除。
</Answer>

</Exercises>

## 小结

- 是否能逐项指出本页覆盖的原目录单元，而非用自拟专题替代？
- 是否能解释资源坐标合同、粒子属性、网格发射之间的数据与空间关系？
- 是否保存正常、边界、失败三组回放及目标设备性能证据？
- 是否明确区分原书核心、现代迁移和额外扩展？

## 术语表

<Glossary>
  <GlossaryItem term="资源坐标合同">
    统一Max与Unity的单位、轴向、枢轴和缩放的导入约束。
  </GlossaryItem>
  <GlossaryItem term="粒子属性">
    决定发射、生命周期、速度、颜色、尺寸和渲染的参数集合。
  </GlossaryItem>
  <GlossaryItem term="网格发射">
    从外部模型顶点或表面采样粒子出生位置的方式。
  </GlossaryItem>
  <GlossaryItem term="预制体">
    保存对象层级、组件和资源引用以便重复实例化的模板。
  </GlossaryItem>
  <GlossaryItem term="摄像机定位">
    用视锥、层掩码和空间位置确定效果最终构图的过程。
  </GlossaryItem>
</Glossary>

## 来源与改编边界

<Attribution
  adaptedFrom="《Unity 3D游戏特效制作典型实例》张天骥，人民邮电出版社，2017，ISBN 9787115455222"
  adaptedUrl="https://www.tenlong.com.tw/products/9787115455222"
/>
