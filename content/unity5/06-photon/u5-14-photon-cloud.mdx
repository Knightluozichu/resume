---
title: "第14章 使用 Photon Cloud 制作网络游戏"
type: B
section: "Unity 5 权威讲解 · Photon 实战"
order: 14
description: "按李在贤《Unity 5权威讲解》正式目录第 417-557 页复现第14章 使用 Photon Cloud 制作网络游戏，围绕 TPS 项目建立可运行、可反证、可回归的证据。"
demo: true
math: false
draft: false
sourceUrl: "https://www.tenlong.com.tw/products/9787115436368"
---


## 问题：怎样证明掌握的是原书项目，而不是背过一组 Unity API？

先预测：如果只保存最后一张运行截图，不知道对象如何生成、状态怎样变化、事件由谁发送、失败时如何清理，能否证明系统正确？不能。项目能力来自连续因果链，API 名称只是其中一个实现节点。

把单机坦克战改造成 Photon Cloud 多人游戏，完成连接、随机匹配、房间、大厅、同步、HUD、排名和数据库闭环。 本页同时保留 Unity 5 的历史语境和当前工程警示：已废弃的内置网络等 API 用于忠实理解原书，不作为新项目的生产推荐。

## 原书目录边界

本页严格对应《Unity 5权威讲解》第 417-557 页。以下目录项必须在实现、实验或证据中出现：

- **第二代 Unity 网络游戏引擎 UNET**
- **第三方网络游戏引擎**
- **PhotonNetwork 游戏引擎**
- **Photon Server 与 Photon Cloud**
- **加入会员**
- **制作坦克大战游戏**
- **坦克移动逻辑**
- **履带动画**
- **摄像机追踪逻辑**
- **旋转炮塔**
- **调整炮身角度**
- **炮弹预制体和发射逻辑**
- **炮弹发射音效**
- **安装 Photon Unity Networking 插件**
- **改为 Photon Cloud 版本**
- **连接 Photon Cloud**
- **随机配对**
- **制作房间**
- **Photon View**
- **动态生成坦克**
- **构建游戏并测试连接**
- **控制自己的坦克**
- **平滑移动和旋转**
- **同步炮塔与炮身**
- **发射炮弹**
- **坦克被击中与复活**
- **制作游戏大厅**
- **制作大厅场景**
- **制作登录 UI**
- **进入游戏房间**
- **制作坦克 HUD**
- **创建并进入房间**
- **构建房间目录 UI**
- **接收房间列表**
- **动态增加按钮事件**
- **显示房间内玩家数**
- **退出房间**
- **显示连接日志**
- **事件钩子**
- **评分并显示**
- **在 Unity 中连接数据库**
- **JSON**
- **SimpleJSON**
- **保存分数到数据库**
- **从数据库获取排名**

本站以中文重新组织解释、代码与实验，不逐字复制原文。示例锁定原书 Unity 5 语境；迁移到现代 Unity 时，需要把输入、渲染、导航和网络替代方案写进版本清单。

## 核心模型与不可变条件

把单机坦克战改造成 Photon Cloud 多人游戏，完成连接、随机匹配、房间、大厅、同步、HUD、排名和数据库闭环。 主链为：**连接Photon → 匹配与房间 → 生成坦克 → 状态事件同步 → 大厅HUD → 排名持久化**。每一节点只消费前一节点已经验证的对象、状态或事件，不能从最终表现反推中间过程一定正确。

本页不可变条件是：**连接、房间、玩家、坦克、炮弹、得分和排名必须有唯一 ID、清晰所有权、幂等事件与离房清理。** 任何“看起来能跑”的结果都必须经过重复加载、异常输入和边界时序检验。

## 五个必须落到证据的术语


“Photon Cloud”必须落到“连接Photon → 匹配与房间 → 生成坦克 → 状态事件同步 → 大厅HUD → 排名持久化”中的明确节点，并能由场景状态、日志或目标构建捕获验证。


“Photon View”必须落到“连接Photon → 匹配与房间 → 生成坦克 → 状态事件同步 → 大厅HUD → 排名持久化”中的明确节点，并能由场景状态、日志或目标构建捕获验证。


“随机配对”必须落到“连接Photon → 匹配与房间 → 生成坦克 → 状态事件同步 → 大厅HUD → 排名持久化”中的明确节点，并能由场景状态、日志或目标构建捕获验证。


“游戏大厅”必须落到“连接Photon → 匹配与房间 → 生成坦克 → 状态事件同步 → 大厅HUD → 排名持久化”中的明确节点，并能由场景状态、日志或目标构建捕获验证。


“事件钩子”必须落到“连接Photon → 匹配与房间 → 生成坦克 → 状态事件同步 → 大厅HUD → 排名持久化”中的明确节点，并能由场景状态、日志或目标构建捕获验证。

## 原书概念逐项复现

### 1. 第二代 Unity 网络游戏引擎 UNET

为对象、玩家和消息定义唯一身份、所有权、发送方向、重复处理和断线清理，并用双端日志对齐同一事件。 复现时保存正常、边界和主动失败三类样本，在首个偏离节点停止扩散修改。

### 2. 第三方网络游戏引擎

为对象、玩家和消息定义唯一身份、所有权、发送方向、重复处理和断线清理，并用双端日志对齐同一事件。 复现时保存正常、边界和主动失败三类样本，在首个偏离节点停止扩散修改。

### 3. PhotonNetwork 游戏引擎

为对象、玩家和消息定义唯一身份、所有权、发送方向、重复处理和断线清理，并用双端日志对齐同一事件。 复现时保存正常、边界和主动失败三类样本，在首个偏离节点停止扩散修改。

### 4. Photon Server 与 Photon Cloud

为对象、玩家和消息定义唯一身份、所有权、发送方向、重复处理和断线清理，并用双端日志对齐同一事件。 复现时保存正常、边界和主动失败三类样本，在首个偏离节点停止扩散修改。

### 5. 加入会员

把“加入会员”放回“连接Photon → 匹配与房间 → 生成坦克 → 状态事件同步 → 大厅HUD → 排名持久化”主链，记录输入、对象状态、事件、结果和失败恢复，不能只保存最终截图。 复现时保存正常、边界和主动失败三类样本，在首个偏离节点停止扩散修改。

### 6. 制作坦克大战游戏

把“制作坦克大战游戏”放回“连接Photon → 匹配与房间 → 生成坦克 → 状态事件同步 → 大厅HUD → 排名持久化”主链，记录输入、对象状态、事件、结果和失败恢复，不能只保存最终截图。 复现时保存正常、边界和主动失败三类样本，在首个偏离节点停止扩散修改。

### 7. 坦克移动逻辑

把“坦克移动逻辑”放回“连接Photon → 匹配与房间 → 生成坦克 → 状态事件同步 → 大厅HUD → 排名持久化”主链，记录输入、对象状态、事件、结果和失败恢复，不能只保存最终截图。 复现时保存正常、边界和主动失败三类样本，在首个偏离节点停止扩散修改。

### 8. 履带动画

冻结参数、状态、过渡和事件时序，记录进入退出条件，避免动画表现与玩法结算互相抢占所有权。 复现时保存正常、边界和主动失败三类样本，在首个偏离节点停止扩散修改。

### 9. 摄像机追踪逻辑

把“摄像机追踪逻辑”放回“连接Photon → 匹配与房间 → 生成坦克 → 状态事件同步 → 大厅HUD → 排名持久化”主链，记录输入、对象状态、事件、结果和失败恢复，不能只保存最终截图。 复现时保存正常、边界和主动失败三类样本，在首个偏离节点停止扩散修改。

### 10. 旋转炮塔

把“旋转炮塔”放回“连接Photon → 匹配与房间 → 生成坦克 → 状态事件同步 → 大厅HUD → 排名持久化”主链，记录输入、对象状态、事件、结果和失败恢复，不能只保存最终截图。 复现时保存正常、边界和主动失败三类样本，在首个偏离节点停止扩散修改。

### 11. 调整炮身角度

把“调整炮身角度”放回“连接Photon → 匹配与房间 → 生成坦克 → 状态事件同步 → 大厅HUD → 排名持久化”主链，记录输入、对象状态、事件、结果和失败恢复，不能只保存最终截图。 复现时保存正常、边界和主动失败三类样本，在首个偏离节点停止扩散修改。

### 12. 炮弹预制体和发射逻辑

把“炮弹预制体和发射逻辑”放回“连接Photon → 匹配与房间 → 生成坦克 → 状态事件同步 → 大厅HUD → 排名持久化”主链，记录输入、对象状态、事件、结果和失败恢复，不能只保存最终截图。 复现时保存正常、边界和主动失败三类样本，在首个偏离节点停止扩散修改。

### 13. 炮弹发射音效

把“炮弹发射音效”放回“连接Photon → 匹配与房间 → 生成坦克 → 状态事件同步 → 大厅HUD → 排名持久化”主链，记录输入、对象状态、事件、结果和失败恢复，不能只保存最终截图。 复现时保存正常、边界和主动失败三类样本，在首个偏离节点停止扩散修改。

### 14. 安装 Photon Unity Networking 插件

为对象、玩家和消息定义唯一身份、所有权、发送方向、重复处理和断线清理，并用双端日志对齐同一事件。 复现时保存正常、边界和主动失败三类样本，在首个偏离节点停止扩散修改。

### 15. 改为 Photon Cloud 版本

为对象、玩家和消息定义唯一身份、所有权、发送方向、重复处理和断线清理，并用双端日志对齐同一事件。 复现时保存正常、边界和主动失败三类样本，在首个偏离节点停止扩散修改。

### 16. 连接 Photon Cloud

为对象、玩家和消息定义唯一身份、所有权、发送方向、重复处理和断线清理，并用双端日志对齐同一事件。 复现时保存正常、边界和主动失败三类样本，在首个偏离节点停止扩散修改。

### 17. 随机配对

把“随机配对”放回“连接Photon → 匹配与房间 → 生成坦克 → 状态事件同步 → 大厅HUD → 排名持久化”主链，记录输入、对象状态、事件、结果和失败恢复，不能只保存最终截图。 复现时保存正常、边界和主动失败三类样本，在首个偏离节点停止扩散修改。

### 18. 制作房间

把“制作房间”放回“连接Photon → 匹配与房间 → 生成坦克 → 状态事件同步 → 大厅HUD → 排名持久化”主链，记录输入、对象状态、事件、结果和失败恢复，不能只保存最终截图。 复现时保存正常、边界和主动失败三类样本，在首个偏离节点停止扩散修改。

### 19. Photon View

为对象、玩家和消息定义唯一身份、所有权、发送方向、重复处理和断线清理，并用双端日志对齐同一事件。 复现时保存正常、边界和主动失败三类样本，在首个偏离节点停止扩散修改。

### 20. 动态生成坦克

把“动态生成坦克”放回“连接Photon → 匹配与房间 → 生成坦克 → 状态事件同步 → 大厅HUD → 排名持久化”主链，记录输入、对象状态、事件、结果和失败恢复，不能只保存最终截图。 复现时保存正常、边界和主动失败三类样本，在首个偏离节点停止扩散修改。

### 21. 构建游戏并测试连接

把“构建游戏并测试连接”放回“连接Photon → 匹配与房间 → 生成坦克 → 状态事件同步 → 大厅HUD → 排名持久化”主链，记录输入、对象状态、事件、结果和失败恢复，不能只保存最终截图。 复现时保存正常、边界和主动失败三类样本，在首个偏离节点停止扩散修改。

### 22. 控制自己的坦克

把“控制自己的坦克”放回“连接Photon → 匹配与房间 → 生成坦克 → 状态事件同步 → 大厅HUD → 排名持久化”主链，记录输入、对象状态、事件、结果和失败恢复，不能只保存最终截图。 复现时保存正常、边界和主动失败三类样本，在首个偏离节点停止扩散修改。

### 23. 平滑移动和旋转

把“平滑移动和旋转”放回“连接Photon → 匹配与房间 → 生成坦克 → 状态事件同步 → 大厅HUD → 排名持久化”主链，记录输入、对象状态、事件、结果和失败恢复，不能只保存最终截图。 复现时保存正常、边界和主动失败三类样本，在首个偏离节点停止扩散修改。

### 24. 同步炮塔与炮身

把“同步炮塔与炮身”放回“连接Photon → 匹配与房间 → 生成坦克 → 状态事件同步 → 大厅HUD → 排名持久化”主链，记录输入、对象状态、事件、结果和失败恢复，不能只保存最终截图。 复现时保存正常、边界和主动失败三类样本，在首个偏离节点停止扩散修改。

### 25. 发射炮弹

把“发射炮弹”放回“连接Photon → 匹配与房间 → 生成坦克 → 状态事件同步 → 大厅HUD → 排名持久化”主链，记录输入、对象状态、事件、结果和失败恢复，不能只保存最终截图。 复现时保存正常、边界和主动失败三类样本，在首个偏离节点停止扩散修改。

### 26. 坦克被击中与复活

把“坦克被击中与复活”放回“连接Photon → 匹配与房间 → 生成坦克 → 状态事件同步 → 大厅HUD → 排名持久化”主链，记录输入、对象状态、事件、结果和失败恢复，不能只保存最终截图。 复现时保存正常、边界和主动失败三类样本，在首个偏离节点停止扩散修改。

### 27. 制作游戏大厅

把“制作游戏大厅”放回“连接Photon → 匹配与房间 → 生成坦克 → 状态事件同步 → 大厅HUD → 排名持久化”主链，记录输入、对象状态、事件、结果和失败恢复，不能只保存最终截图。 复现时保存正常、边界和主动失败三类样本，在首个偏离节点停止扩散修改。

### 28. 制作大厅场景

把“制作大厅场景”放回“连接Photon → 匹配与房间 → 生成坦克 → 状态事件同步 → 大厅HUD → 排名持久化”主链，记录输入、对象状态、事件、结果和失败恢复，不能只保存最终截图。 复现时保存正常、边界和主动失败三类样本，在首个偏离节点停止扩散修改。

### 29. 制作登录 UI

把布局、视觉、输入命中和游戏数据绑定分开验证，并覆盖目标宽高比与重复进入场景的生命周期。 复现时保存正常、边界和主动失败三类样本，在首个偏离节点停止扩散修改。

### 30. 进入游戏房间

把“进入游戏房间”放回“连接Photon → 匹配与房间 → 生成坦克 → 状态事件同步 → 大厅HUD → 排名持久化”主链，记录输入、对象状态、事件、结果和失败恢复，不能只保存最终截图。 复现时保存正常、边界和主动失败三类样本，在首个偏离节点停止扩散修改。

### 31. 制作坦克 HUD

把“制作坦克 HUD”放回“连接Photon → 匹配与房间 → 生成坦克 → 状态事件同步 → 大厅HUD → 排名持久化”主链，记录输入、对象状态、事件、结果和失败恢复，不能只保存最终截图。 复现时保存正常、边界和主动失败三类样本，在首个偏离节点停止扩散修改。

### 32. 创建并进入房间

把“创建并进入房间”放回“连接Photon → 匹配与房间 → 生成坦克 → 状态事件同步 → 大厅HUD → 排名持久化”主链，记录输入、对象状态、事件、结果和失败恢复，不能只保存最终截图。 复现时保存正常、边界和主动失败三类样本，在首个偏离节点停止扩散修改。

### 33. 构建房间目录 UI

把布局、视觉、输入命中和游戏数据绑定分开验证，并覆盖目标宽高比与重复进入场景的生命周期。 复现时保存正常、边界和主动失败三类样本，在首个偏离节点停止扩散修改。

### 34. 接收房间列表

把“接收房间列表”放回“连接Photon → 匹配与房间 → 生成坦克 → 状态事件同步 → 大厅HUD → 排名持久化”主链，记录输入、对象状态、事件、结果和失败恢复，不能只保存最终截图。 复现时保存正常、边界和主动失败三类样本，在首个偏离节点停止扩散修改。

### 35. 动态增加按钮事件

把“动态增加按钮事件”放回“连接Photon → 匹配与房间 → 生成坦克 → 状态事件同步 → 大厅HUD → 排名持久化”主链，记录输入、对象状态、事件、结果和失败恢复，不能只保存最终截图。 复现时保存正常、边界和主动失败三类样本，在首个偏离节点停止扩散修改。

### 36. 显示房间内玩家数

把“显示房间内玩家数”放回“连接Photon → 匹配与房间 → 生成坦克 → 状态事件同步 → 大厅HUD → 排名持久化”主链，记录输入、对象状态、事件、结果和失败恢复，不能只保存最终截图。 复现时保存正常、边界和主动失败三类样本，在首个偏离节点停止扩散修改。

### 37. 退出房间

把“退出房间”放回“连接Photon → 匹配与房间 → 生成坦克 → 状态事件同步 → 大厅HUD → 排名持久化”主链，记录输入、对象状态、事件、结果和失败恢复，不能只保存最终截图。 复现时保存正常、边界和主动失败三类样本，在首个偏离节点停止扩散修改。

### 38. 显示连接日志

把“显示连接日志”放回“连接Photon → 匹配与房间 → 生成坦克 → 状态事件同步 → 大厅HUD → 排名持久化”主链，记录输入、对象状态、事件、结果和失败恢复，不能只保存最终截图。 复现时保存正常、边界和主动失败三类样本，在首个偏离节点停止扩散修改。

### 39. 事件钩子

把“事件钩子”放回“连接Photon → 匹配与房间 → 生成坦克 → 状态事件同步 → 大厅HUD → 排名持久化”主链，记录输入、对象状态、事件、结果和失败恢复，不能只保存最终截图。 复现时保存正常、边界和主动失败三类样本，在首个偏离节点停止扩散修改。

### 40. 评分并显示

把“评分并显示”放回“连接Photon → 匹配与房间 → 生成坦克 → 状态事件同步 → 大厅HUD → 排名持久化”主链，记录输入、对象状态、事件、结果和失败恢复，不能只保存最终截图。 复现时保存正常、边界和主动失败三类样本，在首个偏离节点停止扩散修改。

### 41. 在 Unity 中连接数据库

把“在 Unity 中连接数据库”放回“连接Photon → 匹配与房间 → 生成坦克 → 状态事件同步 → 大厅HUD → 排名持久化”主链，记录输入、对象状态、事件、结果和失败恢复，不能只保存最终截图。 复现时保存正常、边界和主动失败三类样本，在首个偏离节点停止扩散修改。

### 42. JSON

把“JSON”放回“连接Photon → 匹配与房间 → 生成坦克 → 状态事件同步 → 大厅HUD → 排名持久化”主链，记录输入、对象状态、事件、结果和失败恢复，不能只保存最终截图。 复现时保存正常、边界和主动失败三类样本，在首个偏离节点停止扩散修改。

### 43. SimpleJSON

把“SimpleJSON”放回“连接Photon → 匹配与房间 → 生成坦克 → 状态事件同步 → 大厅HUD → 排名持久化”主链，记录输入、对象状态、事件、结果和失败恢复，不能只保存最终截图。 复现时保存正常、边界和主动失败三类样本，在首个偏离节点停止扩散修改。

### 44. 保存分数到数据库

把“保存分数到数据库”放回“连接Photon → 匹配与房间 → 生成坦克 → 状态事件同步 → 大厅HUD → 排名持久化”主链，记录输入、对象状态、事件、结果和失败恢复，不能只保存最终截图。 复现时保存正常、边界和主动失败三类样本，在首个偏离节点停止扩散修改。

### 45. 从数据库获取排名

把“从数据库获取排名”放回“连接Photon → 匹配与房间 → 生成坦克 → 状态事件同步 → 大厅HUD → 排名持久化”主链，记录输入、对象状态、事件、结果和失败恢复，不能只保存最终截图。 复现时保存正常、边界和主动失败三类样本，在首个偏离节点停止扩散修改。

## 三步复现与分步观察


## 可执行实现骨架

脚本边界显式保存状态，避免展示层直接决定玩法结果：

```csharp
using UnityEngine;

public sealed class ChapterController : MonoBehaviour
{
    [SerializeField] GameObject target;
    bool completed;

    public void ExecuteOnce()
    {
        if (completed || target == null) return;
        completed = true;
        ApplyChapterResult(target);
    }
}
```

测试记录必须能回到具体对象与事件：

```json
{
  "chapter": "u5-14-photon-cloud",
  "bookPages": "417-557",
  "chain": ["连接Photon","匹配与房间","生成坦克","状态事件同步","大厅HUD","排名持久化"],
  "unity": "5.x-pinned",
  "scene": "fixed-tps-checkpoint",
  "normal": true,
  "boundary": true,
  "failureReplay": true
}
```

签发输出同时保留通过值与拒绝原因：

```text
PASS context=version+scene+prefab+input
PASS chain=连接Photon -> 匹配与房间 -> 生成坦克 -> 状态事件同步 -> 大厅HUD -> 排名持久化
PASS invariant=连接、房间、玩家、坦克、炮弹、得分和排名必须有唯一 ID、清晰所有权、幂等事件与离房清理。
REJECT duplicate-event | stale-object | changed-scene | missing-cleanup
```

## 两个必须主动制造的失败样本


## 证据矩阵

| 维度 | 正常样本 | 边界样本 | 失败样本 | 签发条件 |
| --- | --- | --- | --- | --- |
| 环境 | 固定版本与场景 | 重复加载或低帧率 | 改变一个依赖 | 差异可解释 |
| 主链 | 所有节点有记录 | 临界输入与时序 | 首节点注入偏差 | 能定位首因 |
| 结果 | 玩法与表现一致 | 生命周期完整 | 重复或丢失事件 | 同输入可复验 |
| 清理 | 对象数量稳定 | 重连或重载 | 遗留引用和订阅 | 无状态泄漏 |

## 练习


## 小结

- 是否逐项覆盖了本页原书目录，而没有换成通用 Unity 知识？
- 是否沿“连接Photon → 匹配与房间 → 生成坦克 → 状态事件同步 → 大厅HUD → 排名持久化”保存对象、状态和事件证据？
- 是否主动制造失败并验证重载、重连或重复执行后的清理？
- 是否明确区分 Unity 5 历史复现与现代生产建议？

## 术语表


## 来源与改编边界

