---
title: "第3章 文件I/O"
type: C
section: "文件与I/O"
order: 3
description: "沿open/read/write/lseek/close追踪描述符表、打开文件表项、文件偏移、共享与原子操作。 覆盖18个正式目录层级。"
demo: true
math: false
sourceUrl: "https://www.cnblogs.com/china-pub/p/3782412.html"
draft: false
---


## 为什么从接口合同开始

、、、、共同构成本页坐标。沿open/read/write/lseek/close追踪描述符表、打开文件表项、文件偏移、共享与原子操作。

APUE第3版面向SUSv4/POSIX.1-2008，并以Solaris 10、Mac OS X 10.6.8、FreeBSD 8.0和Ubuntu 12.04为主要验证平台。正文先还原书中标准合同与实现差异；Linux当前行为只能作为显式现代对照，不能悄悄覆盖原书的版本边界。

## 六阶段可重放链

### 1. 打开对象

围绕文件描述符记录平台、功能测试宏、输入、描述符/对象表、返回值、errno和预期状态。进入“读取短计数”前保存第一条偏离，清理与恢复也写入结果。

### 2. 读取短计数

围绕打开文件描述记录平台、功能测试宏、输入、描述符/对象表、返回值、errno和预期状态。进入“移动偏移”前保存第一条偏离，清理与恢复也写入结果。

### 3. 移动偏移

围绕文件偏移记录平台、功能测试宏、输入、描述符/对象表、返回值、errno和预期状态。进入“复制描述符”前保存第一条偏离，清理与恢复也写入结果。

### 4. 复制描述符

围绕原子操作记录平台、功能测试宏、输入、描述符/对象表、返回值、errno和预期状态。进入“同步数据”前保存第一条偏离，清理与恢复也写入结果。

### 5. 同步数据

围绕fcntl记录平台、功能测试宏、输入、描述符/对象表、返回值、errno和预期状态。进入“关闭核对”前保存第一条偏离，清理与恢复也写入结果。

### 6. 关闭核对

围绕文件描述符记录平台、功能测试宏、输入、描述符/对象表、返回值、errno和预期状态。进入“打开对象”前保存第一条偏离，清理与恢复也写入结果。

## 机制、所有权与失败模型

沿open/read/write/lseek/close追踪描述符表、打开文件表项、文件偏移、共享与原子操作。

本页核心不变量是：**成功字节数、部分读写、EINTR、偏移共享和持久化语义都显式处理；每个描述符恰好关闭一次。** 它必须同时约束正常、边界、故障和恢复样本。接口返回成功只是证据之一，还要检查短计数、EINTR/EAGAIN、对象继承、并发时序、缓冲与异步清理。

实验台账至少包含平台与版本、编译宏、入口参数、对象所有者、描述符或ID、预期返回、实际返回、errno、首个状态分叉、原始轨迹和清理结果十一列。性能样本还需固定预热、CPU放置、输入规模、采样窗口与分位数；信号/线程样本必须记录掩码、取消状态和处理上下文。

## 可重放命令骨架

```c
int fd = open(path, O_RDWR | O_CLOEXEC);
if (fd == -1) err(1, "open");
```

```bash
strace -yy -e openat,read,write,lseek,close ./probe
```

```bash
stat --printf='%i %a %s %y\n' sample.dat
ls -li sample.dat
```


## 正式目录逐项讲解

### 第3章 文件I/O

“第3章 文件I/O”不是孤立API名词。画出名字、描述符、打开对象、偏移、缓存与持久对象的关系，并标明复制、继承、刷新和最终释放时刻。在本章中，它通过“文件描述符”服务于“打开对象”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“读取短计数”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 3.1 引言

“3.1 引言”不是孤立API名词。先写出输入、返回值、errno与资源所有权，再沿用户接口、C库封装、系统调用和内核对象定位责任边界。在本章中，它通过“打开文件描述”服务于“读取短计数”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“移动偏移”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 3.2 文件描述符

“3.2 文件描述符”不是孤立API名词。画出名字、描述符、打开对象、偏移、缓存与持久对象的关系，并标明复制、继承、刷新和最终释放时刻。在本章中，它通过“文件偏移”服务于“移动偏移”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“复制描述符”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 3.3 函数open和openat

“3.3 函数open和openat”不是孤立API名词。把函数原型、前置条件、成功返回、失败errno、可中断性和资源转移逐项列出，禁止把一次成功调用当成完整合同。在本章中，它通过“原子操作”服务于“复制描述符”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“同步数据”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 3.4 函数creat

“3.4 函数creat”不是孤立API名词。把函数原型、前置条件、成功返回、失败errno、可中断性和资源转移逐项列出，禁止把一次成功调用当成完整合同。在本章中，它通过“fcntl”服务于“同步数据”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“关闭核对”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 3.5 函数close

“3.5 函数close”不是孤立API名词。把函数原型、前置条件、成功返回、失败errno、可中断性和资源转移逐项列出，禁止把一次成功调用当成完整合同。在本章中，它通过“文件描述符”服务于“关闭核对”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“打开对象”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 3.6 函数lseek

“3.6 函数lseek”不是孤立API名词。把函数原型、前置条件、成功返回、失败errno、可中断性和资源转移逐项列出，禁止把一次成功调用当成完整合同。在本章中，它通过“打开文件描述”服务于“打开对象”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“读取短计数”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 3.7 函数read

“3.7 函数read”不是孤立API名词。把函数原型、前置条件、成功返回、失败errno、可中断性和资源转移逐项列出，禁止把一次成功调用当成完整合同。在本章中，它通过“文件偏移”服务于“读取短计数”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“移动偏移”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 3.8 函数write

“3.8 函数write”不是孤立API名词。把函数原型、前置条件、成功返回、失败errno、可中断性和资源转移逐项列出，禁止把一次成功调用当成完整合同。在本章中，它通过“原子操作”服务于“移动偏移”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“复制描述符”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 3.9 I/O的效率

“3.9 I/O的效率”不是孤立API名词。先写出输入、返回值、errno与资源所有权，再沿用户接口、C库封装、系统调用和内核对象定位责任边界。在本章中，它通过“fcntl”服务于“复制描述符”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“同步数据”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 3.10 文件共享

“3.10 文件共享”不是孤立API名词。画出名字、描述符、打开对象、偏移、缓存与持久对象的关系，并标明复制、继承、刷新和最终释放时刻。在本章中，它通过“文件描述符”服务于“同步数据”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“关闭核对”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 3.11 原子操作

“3.11 原子操作”不是孤立API名词。先写出输入、返回值、errno与资源所有权，再沿用户接口、C库封装、系统调用和内核对象定位责任边界。在本章中，它通过“打开文件描述”服务于“关闭核对”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“打开对象”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 3.12 函数dup和dup2

“3.12 函数dup和dup2”不是孤立API名词。把函数原型、前置条件、成功返回、失败errno、可中断性和资源转移逐项列出，禁止把一次成功调用当成完整合同。在本章中，它通过“文件偏移”服务于“打开对象”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“读取短计数”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 3.13 函数sync、fsync和fdatasync

“3.13 函数sync、fsync和fdatasync”不是孤立API名词。把函数原型、前置条件、成功返回、失败errno、可中断性和资源转移逐项列出，禁止把一次成功调用当成完整合同。在本章中，它通过“原子操作”服务于“读取短计数”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“移动偏移”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 3.14 函数fcntl

“3.14 函数fcntl”不是孤立API名词。把函数原型、前置条件、成功返回、失败errno、可中断性和资源转移逐项列出，禁止把一次成功调用当成完整合同。在本章中，它通过“fcntl”服务于“移动偏移”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“复制描述符”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 3.15 函数ioctl

“3.15 函数ioctl”不是孤立API名词。把函数原型、前置条件、成功返回、失败errno、可中断性和资源转移逐项列出，禁止把一次成功调用当成完整合同。在本章中，它通过“文件描述符”服务于“复制描述符”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“同步数据”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 3.16 /dev/fd

“3.16 /dev/fd”不是孤立API名词。先写出输入、返回值、errno与资源所有权，再沿用户接口、C库封装、系统调用和内核对象定位责任边界。在本章中，它通过“打开文件描述”服务于“同步数据”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“关闭核对”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 3.17 小结

“3.17 小结”不是孤立API名词。把对象、控制流、失败模型、原始证据和恢复条件收束为可复查清单，并用同一输入重放确认没有残留状态。在本章中，它通过“文件偏移”服务于“关闭核对”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“打开对象”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

## 分步视觉验证

先预测对象、返回值和状态，再动手试一试；若结果不同，停在第一条系统调用或对象状态分叉。


## 常见误区


## 四类样本与验收

| 样本 | 只改变什么                 | 必查证据                  | 通过条件   |
| ---- | -------------------------- | ------------------------- | ---------- |
| 基线 | 平台、宏与固定输入         | 返回值、errno、对象与轨迹 | 与预测一致 |
| 边界 | 长度、容量、权限或时序     | 短计数、拒绝或阻塞点      | 边界可解释 |
| 故障 | 一个信号、断开或失败注入   | 首错与责任层              | 故障可定位 |
| 恢复 | 关闭、回收、删除与属性还原 | 残留对象和同输入重放      | 回到基线   |

## 练习


## 小结

- 文件描述符：连接“打开对象”的接口合同与运行证据。
- 打开文件描述：连接“读取短计数”的接口合同与运行证据。
- 文件偏移：连接“移动偏移”的接口合同与运行证据。
- 原子操作：连接“复制描述符”的接口合同与运行证据。
- fcntl：连接“同步数据”的接口合同与运行证据。
- 本页18个正式目录层级全部进入可重放实验链。

## 术语表


