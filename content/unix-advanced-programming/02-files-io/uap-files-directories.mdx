---
title: "第4章 文件和目录"
type: C
section: "文件与I/O"
order: 4
description: "用stat元数据、权限、所有权、链接、目录项与文件系统结构解释路径名如何解析到对象。 覆盖27个正式目录层级。"
demo: true
math: false
sourceUrl: "https://www.cnblogs.com/china-pub/p/3782412.html"
draft: true
---

import {
  UapFilesDirectoriesMapLab,
  UapFilesDirectoriesExperimentLab,
  UapFilesDirectoriesEvidenceLab,
} from "@/components/mdx/unix-advanced-programming/diagrams/uap-files-directories";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第4章 文件和目录的核心接口、对象所有权与POSIX/SUS版本边界
- 能描述“读取元数据 → 判定类型权限 → 追踪链接 → 修改时间 → 遍历目录 → 清理路径”的输入、返回值、errno与系统可见证据
- 能实现一个单变量、可回收、可重放的最小UNIX C实验
- 能比较基线、边界、故障和恢复轨迹并判断“检查权限与执行操作之间不制造TOCTOU；链接计数、目录句柄、当前目录和时间戳变更可解释。”是否成立

</Objectives>

## 为什么从接口合同开始

<Term def="stat连接读取元数据阶段的接口合同、对象所有权、失败语义和清理证据。">stat</Term>、<Term def="文件类型连接判定类型权限阶段的接口合同、对象所有权、失败语义和清理证据。">文件类型</Term>、<Term def="访问权限连接追踪链接阶段的接口合同、对象所有权、失败语义和清理证据。">访问权限</Term>、<Term def="硬链接连接修改时间阶段的接口合同、对象所有权、失败语义和清理证据。">硬链接</Term>、<Term def="目录流连接遍历目录阶段的接口合同、对象所有权、失败语义和清理证据。">目录流</Term>
共同构成本页坐标。用stat元数据、权限、所有权、链接、目录项与文件系统结构解释路径名如何解析到对象。

APUE第3版面向SUSv4/POSIX.1-2008，并以Solaris 10、Mac OS X 10.6.8、FreeBSD 8.0和Ubuntu 12.04为主要验证平台。正文先还原书中标准合同与实现差异；Linux当前行为只能作为显式现代对照，不能悄悄覆盖原书的版本边界。

## 六阶段可重放链

### 1. 读取元数据

围绕stat记录平台、功能测试宏、输入、描述符/对象表、返回值、errno和预期状态。进入“判定类型权限”前保存第一条偏离，清理与恢复也写入结果。

### 2. 判定类型权限

围绕文件类型记录平台、功能测试宏、输入、描述符/对象表、返回值、errno和预期状态。进入“追踪链接”前保存第一条偏离，清理与恢复也写入结果。

### 3. 追踪链接

围绕访问权限记录平台、功能测试宏、输入、描述符/对象表、返回值、errno和预期状态。进入“修改时间”前保存第一条偏离，清理与恢复也写入结果。

### 4. 修改时间

围绕硬链接记录平台、功能测试宏、输入、描述符/对象表、返回值、errno和预期状态。进入“遍历目录”前保存第一条偏离，清理与恢复也写入结果。

### 5. 遍历目录

围绕目录流记录平台、功能测试宏、输入、描述符/对象表、返回值、errno和预期状态。进入“清理路径”前保存第一条偏离，清理与恢复也写入结果。

### 6. 清理路径

围绕stat记录平台、功能测试宏、输入、描述符/对象表、返回值、errno和预期状态。进入“读取元数据”前保存第一条偏离，清理与恢复也写入结果。

## 机制、所有权与失败模型

用stat元数据、权限、所有权、链接、目录项与文件系统结构解释路径名如何解析到对象。

本页核心不变量是：**检查权限与执行操作之间不制造TOCTOU；链接计数、目录句柄、当前目录和时间戳变更可解释。** 它必须同时约束正常、边界、故障和恢复样本。接口返回成功只是证据之一，还要检查短计数、EINTR/EAGAIN、对象继承、并发时序、缓冲与异步清理。

实验台账至少包含平台与版本、编译宏、入口参数、对象所有者、描述符或ID、预期返回、实际返回、errno、首个状态分叉、原始轨迹和清理结果十一列。性能样本还需固定预热、CPU放置、输入规模、采样窗口与分位数；信号/线程样本必须记录掩码、取消状态和处理上下文。

## 可重放命令骨架

```c
int fd = open(path, O_RDWR | O_CLOEXEC);
if (fd == -1) err(1, "open");
```

```bash
strace -yy -e openat,read,write,lseek,close ./probe
```

```bash
stat --printf='%i %a %s %y\n' sample.dat
ls -li sample.dat
```

<Callout type="trap">
  **安全边界：**
  权限、凭证、信号、终端、IPC清理和网络实验先在隔离用户、容器或可恢复虚拟机执行。保存原始属性与资源清单，退出处理必须可重复。
</Callout>

## 正式目录逐项讲解

### 第4章 文件和目录

“第4章 文件和目录”不是孤立API名词。画出名字、描述符、打开对象、偏移、缓存与持久对象的关系，并标明复制、继承、刷新和最终释放时刻。在本章中，它通过“stat”服务于“读取元数据”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“判定类型权限”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 4.1 引言

“4.1 引言”不是孤立API名词。先写出输入、返回值、errno与资源所有权，再沿用户接口、C库封装、系统调用和内核对象定位责任边界。在本章中，它通过“文件类型”服务于“判定类型权限”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“追踪链接”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 4.2 函数stat、fstat、fstatat和lstat

“4.2 函数stat、fstat、fstatat和lstat”不是孤立API名词。把函数原型、前置条件、成功返回、失败errno、可中断性和资源转移逐项列出，禁止把一次成功调用当成完整合同。在本章中，它通过“访问权限”服务于“追踪链接”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“修改时间”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 4.3 文件类型

“4.3 文件类型”不是孤立API名词。画出名字、描述符、打开对象、偏移、缓存与持久对象的关系，并标明复制、继承、刷新和最终释放时刻。在本章中，它通过“硬链接”服务于“修改时间”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“遍历目录”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 4.4 设置用户ID和设置组ID

“4.4 设置用户ID和设置组ID”不是孤立API名词。先写出输入、返回值、errno与资源所有权，再沿用户接口、C库封装、系统调用和内核对象定位责任边界。在本章中，它通过“目录流”服务于“遍历目录”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“清理路径”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 4.5 文件访问权限

“4.5 文件访问权限”不是孤立API名词。画出名字、描述符、打开对象、偏移、缓存与持久对象的关系，并标明复制、继承、刷新和最终释放时刻。在本章中，它通过“stat”服务于“清理路径”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“读取元数据”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 4.6 新文件和目录的所有权

“4.6 新文件和目录的所有权”不是孤立API名词。画出名字、描述符、打开对象、偏移、缓存与持久对象的关系，并标明复制、继承、刷新和最终释放时刻。在本章中，它通过“文件类型”服务于“读取元数据”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“判定类型权限”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 4.7 函数access和faccessat

“4.7 函数access和faccessat”不是孤立API名词。把函数原型、前置条件、成功返回、失败errno、可中断性和资源转移逐项列出，禁止把一次成功调用当成完整合同。在本章中，它通过“访问权限”服务于“判定类型权限”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“追踪链接”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 4.8 函数umask

“4.8 函数umask”不是孤立API名词。把函数原型、前置条件、成功返回、失败errno、可中断性和资源转移逐项列出，禁止把一次成功调用当成完整合同。在本章中，它通过“硬链接”服务于“追踪链接”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“修改时间”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 4.9 函数chmod、fchmod和fchmodat

“4.9 函数chmod、fchmod和fchmodat”不是孤立API名词。把函数原型、前置条件、成功返回、失败errno、可中断性和资源转移逐项列出，禁止把一次成功调用当成完整合同。在本章中，它通过“目录流”服务于“修改时间”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“遍历目录”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 4.10 粘着位

“4.10 粘着位”不是孤立API名词。先写出输入、返回值、errno与资源所有权，再沿用户接口、C库封装、系统调用和内核对象定位责任边界。在本章中，它通过“stat”服务于“遍历目录”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“清理路径”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 4.11 函数chown、fchown、fchownat和lchown

“4.11 函数chown、fchown、fchownat和lchown”不是孤立API名词。把函数原型、前置条件、成功返回、失败errno、可中断性和资源转移逐项列出，禁止把一次成功调用当成完整合同。在本章中，它通过“文件类型”服务于“清理路径”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“读取元数据”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 4.12 文件长度

“4.12 文件长度”不是孤立API名词。画出名字、描述符、打开对象、偏移、缓存与持久对象的关系，并标明复制、继承、刷新和最终释放时刻。在本章中，它通过“访问权限”服务于“读取元数据”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“判定类型权限”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 4.13 文件截断

“4.13 文件截断”不是孤立API名词。画出名字、描述符、打开对象、偏移、缓存与持久对象的关系，并标明复制、继承、刷新和最终释放时刻。在本章中，它通过“硬链接”服务于“判定类型权限”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“追踪链接”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 4.14 文件系统

“4.14 文件系统”不是孤立API名词。画出名字、描述符、打开对象、偏移、缓存与持久对象的关系，并标明复制、继承、刷新和最终释放时刻。在本章中，它通过“目录流”服务于“追踪链接”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“修改时间”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 4.15 函数link、linkat、unlink、unlinkat和remove

“4.15 函数link、linkat、unlink、unlinkat和remove”不是孤立API名词。把函数原型、前置条件、成功返回、失败errno、可中断性和资源转移逐项列出，禁止把一次成功调用当成完整合同。在本章中，它通过“stat”服务于“修改时间”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“遍历目录”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 4.16 函数rename和renameat

“4.16 函数rename和renameat”不是孤立API名词。把函数原型、前置条件、成功返回、失败errno、可中断性和资源转移逐项列出，禁止把一次成功调用当成完整合同。在本章中，它通过“文件类型”服务于“遍历目录”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“清理路径”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 4.17 符号链接

“4.17 符号链接”不是孤立API名词。画出名字、描述符、打开对象、偏移、缓存与持久对象的关系，并标明复制、继承、刷新和最终释放时刻。在本章中，它通过“访问权限”服务于“清理路径”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“读取元数据”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 4.18 创建和读取符号链接

“4.18 创建和读取符号链接”不是孤立API名词。画出名字、描述符、打开对象、偏移、缓存与持久对象的关系，并标明复制、继承、刷新和最终释放时刻。在本章中，它通过“硬链接”服务于“读取元数据”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“判定类型权限”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 4.19 文件的时间

“4.19 文件的时间”不是孤立API名词。画出名字、描述符、打开对象、偏移、缓存与持久对象的关系，并标明复制、继承、刷新和最终释放时刻。在本章中，它通过“目录流”服务于“判定类型权限”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“追踪链接”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 4.20 函数futimens、utimensat和utimes

“4.20 函数futimens、utimensat和utimes”不是孤立API名词。把函数原型、前置条件、成功返回、失败errno、可中断性和资源转移逐项列出，禁止把一次成功调用当成完整合同。在本章中，它通过“stat”服务于“追踪链接”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“修改时间”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 4.21 函数mkdir、mkdirat和rmdir

“4.21 函数mkdir、mkdirat和rmdir”不是孤立API名词。把函数原型、前置条件、成功返回、失败errno、可中断性和资源转移逐项列出，禁止把一次成功调用当成完整合同。在本章中，它通过“文件类型”服务于“修改时间”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“遍历目录”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 4.22 读目录

“4.22 读目录”不是孤立API名词。画出名字、描述符、打开对象、偏移、缓存与持久对象的关系，并标明复制、继承、刷新和最终释放时刻。在本章中，它通过“访问权限”服务于“遍历目录”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“清理路径”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 4.23 函数chdir、fchdir和getcwd

“4.23 函数chdir、fchdir和getcwd”不是孤立API名词。把函数原型、前置条件、成功返回、失败errno、可中断性和资源转移逐项列出，禁止把一次成功调用当成完整合同。在本章中，它通过“硬链接”服务于“清理路径”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“读取元数据”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 4.24 设备特殊文件

“4.24 设备特殊文件”不是孤立API名词。画出名字、描述符、打开对象、偏移、缓存与持久对象的关系，并标明复制、继承、刷新和最终释放时刻。在本章中，它通过“目录流”服务于“读取元数据”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“判定类型权限”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 4.25 文件访问权限位小结

“4.25 文件访问权限位小结”不是孤立API名词。画出名字、描述符、打开对象、偏移、缓存与持久对象的关系，并标明复制、继承、刷新和最终释放时刻。在本章中，它通过“stat”服务于“判定类型权限”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“追踪链接”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 4.26 小结

“4.26 小结”不是孤立API名词。把对象、控制流、失败模型、原始证据和恢复条件收束为可复查清单，并用同一输入重放确认没有残留状态。在本章中，它通过“文件类型”服务于“追踪链接”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“修改时间”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

## 分步视觉验证

先预测对象、返回值和状态，再动手试一试；若结果不同，停在第一条系统调用或对象状态分叉。

<Stepper>
  <Step title="画接口与对象图">
    先预测stat、文件类型、访问权限、硬链接、目录流的所有权、入口、返回值和状态转换。
    <UapFilesDirectoriesMapLab />
  </Step>
  <Step title="运行单变量实验">
    固定平台、宏、输入和初始资源，只切换一个边界或失败条件。
    <UapFilesDirectoriesExperimentLab />
  </Step>
  <Step title="核对首错与恢复">
    保存原始轨迹，关闭对象并恢复属性后用相同输入重放。
    <UapFilesDirectoriesEvidenceLab />
  </Step>
</Stepper>

## 常见误区

<Callout type="trap">
  **误区一：**
  只检查最终输出，不检查短计数、errno、对象继承、信号掩码和并发时序。
</Callout>

<Callout type="trap">
  **误区二：**
  把Linux当前行为当成POSIX保证，或在失败路径遗漏描述符、线程、IPC名字和终端属性的清理。
</Callout>

## 四类样本与验收

| 样本 | 只改变什么                 | 必查证据                  | 通过条件   |
| ---- | -------------------------- | ------------------------- | ---------- |
| 基线 | 平台、宏与固定输入         | 返回值、errno、对象与轨迹 | 与预测一致 |
| 边界 | 长度、容量、权限或时序     | 短计数、拒绝或阻塞点      | 边界可解释 |
| 故障 | 一个信号、断开或失败注入   | 首错与责任层              | 故障可定位 |
| 恢复 | 关闭、回收、删除与属性还原 | 残留对象和同输入重放      | 回到基线   |

## 练习

<Exercises>

**问题 1** 为什么不能只凭最终输出验收第4章 文件和目录？

<Answer>
  最终输出会隐藏部分读写、EINTR、描述符泄漏、进程/线程未回收、缓冲和竞态。必须同时核对标准边界、接口合同、对象生存期、首个状态分叉和恢复结果。
</Answer>

**问题 2** 如何把“追踪链接”变成运行证据？

<Answer>
  固定平台、功能测试宏、工具和输入，先预测stat与文件类型的返回值、errno、所有权和状态，只改变一个条件并停在首错。
</Answer>

**问题 3** 本页最小通过条件是什么？

<Answer>
  检查权限与执行操作之间不制造TOCTOU；链接计数、目录句柄、当前目录和时间戳变更可解释。
  关闭全部对象并恢复属性后，相同输入必须重新得到基线结果。
</Answer>

</Exercises>

## 小结

- stat：连接“读取元数据”的接口合同与运行证据。
- 文件类型：连接“判定类型权限”的接口合同与运行证据。
- 访问权限：连接“追踪链接”的接口合同与运行证据。
- 硬链接：连接“修改时间”的接口合同与运行证据。
- 目录流：连接“遍历目录”的接口合同与运行证据。
- 本页27个正式目录层级全部进入可重放实验链。

## 术语表

<Glossary>
  <GlossaryItem term="stat">
    stat用于解释“读取元数据”，需要同时标出标准版本、接口合同、对象所有权、首错和清理方法。
  </GlossaryItem>
  <GlossaryItem term="文件类型">
    文件类型用于解释“判定类型权限”，需要同时标出标准版本、接口合同、对象所有权、首错和清理方法。
  </GlossaryItem>
  <GlossaryItem term="访问权限">
    访问权限用于解释“追踪链接”，需要同时标出标准版本、接口合同、对象所有权、首错和清理方法。
  </GlossaryItem>
  <GlossaryItem term="硬链接">
    硬链接用于解释“修改时间”，需要同时标出标准版本、接口合同、对象所有权、首错和清理方法。
  </GlossaryItem>
  <GlossaryItem term="目录流">
    目录流用于解释“遍历目录”，需要同时标出标准版本、接口合同、对象所有权、首错和清理方法。
  </GlossaryItem>
</Glossary>

<Attribution
  adaptedFrom="W. Richard Stevens、Stephen A. Rago著，戚正伟、张亚英、尤晋元译《UNIX环境高级编程（第3版）》，人民邮电出版社，2014-06，812页，ISBN 9787115352118 · 完整中文目录与Pearson原版目录的独立教学重构"
  adaptedUrl="https://www.cnblogs.com/china-pub/p/3782412.html"
/>
