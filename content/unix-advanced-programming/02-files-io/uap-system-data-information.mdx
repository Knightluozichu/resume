---
title: "第6章 系统数据文件和信息"
type: C
section: "文件与I/O"
order: 6
description: "通过口令、组、登录记录、系统标识与时间例程学习系统数据库的可重入查询和时区边界。 覆盖12个正式目录层级。"
demo: true
math: false
sourceUrl: "https://www.cnblogs.com/china-pub/p/3782412.html"
draft: false
---


## 为什么从接口合同开始

、、、、共同构成本页坐标。通过口令、组、登录记录、系统标识与时间例程学习系统数据库的可重入查询和时区边界。

APUE第3版面向SUSv4/POSIX.1-2008，并以Solaris 10、Mac OS X 10.6.8、FreeBSD 8.0和Ubuntu 12.04为主要验证平台。正文先还原书中标准合同与实现差异；Linux当前行为只能作为显式现代对照，不能悄悄覆盖原书的版本边界。

## 六阶段可重放链

### 1. 查询身份

围绕口令文件记录平台、功能测试宏、输入、描述符/对象表、返回值、errno和预期状态。进入“扩展组集”前保存第一条偏离，清理与恢复也写入结果。

### 2. 扩展组集

围绕阴影口令记录平台、功能测试宏、输入、描述符/对象表、返回值、errno和预期状态。进入“读取系统标识”前保存第一条偏离，清理与恢复也写入结果。

### 3. 读取系统标识

围绕附属组记录平台、功能测试宏、输入、描述符/对象表、返回值、errno和预期状态。进入“获取时钟”前保存第一条偏离，清理与恢复也写入结果。

### 4. 获取时钟

围绕uname记录平台、功能测试宏、输入、描述符/对象表、返回值、errno和预期状态。进入“转换时区”前保存第一条偏离，清理与恢复也写入结果。

### 5. 转换时区

围绕时间转换记录平台、功能测试宏、输入、描述符/对象表、返回值、errno和预期状态。进入“核对边界”前保存第一条偏离，清理与恢复也写入结果。

### 6. 核对边界

围绕口令文件记录平台、功能测试宏、输入、描述符/对象表、返回值、errno和预期状态。进入“查询身份”前保存第一条偏离，清理与恢复也写入结果。

## 机制、所有权与失败模型

通过口令、组、登录记录、系统标识与时间例程学习系统数据库的可重入查询和时区边界。

本页核心不变量是：**不手工解析可能变化的数据库格式；缓冲区生存期、用户组集合、时区和历法转换都明确。** 它必须同时约束正常、边界、故障和恢复样本。接口返回成功只是证据之一，还要检查短计数、EINTR/EAGAIN、对象继承、并发时序、缓冲与异步清理。

实验台账至少包含平台与版本、编译宏、入口参数、对象所有者、描述符或ID、预期返回、实际返回、errno、首个状态分叉、原始轨迹和清理结果十一列。性能样本还需固定预热、CPU放置、输入规模、采样窗口与分位数；信号/线程样本必须记录掩码、取消状态和处理上下文。

## 可重放命令骨架

```bash
uname -a
getconf -a | sed -n '1,20p'
```

```bash
getconf OPEN_MAX
getconf PATH_MAX /tmp
```

```bash
cc -D_POSIX_C_SOURCE=200809L -Wall -Wextra probe.c -o probe
```


## 正式目录逐项讲解

### 第6章 系统数据文件和信息

“第6章 系统数据文件和信息”不是孤立API名词。画出名字、描述符、打开对象、偏移、缓存与持久对象的关系，并标明复制、继承、刷新和最终释放时刻。在本章中，它通过“口令文件”服务于“查询身份”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“扩展组集”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 6.1 引言

“6.1 引言”不是孤立API名词。先写出输入、返回值、errno与资源所有权，再沿用户接口、C库封装、系统调用和内核对象定位责任边界。在本章中，它通过“阴影口令”服务于“扩展组集”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“读取系统标识”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 6.2 口令文件

“6.2 口令文件”不是孤立API名词。画出名字、描述符、打开对象、偏移、缓存与持久对象的关系，并标明复制、继承、刷新和最终释放时刻。在本章中，它通过“附属组”服务于“读取系统标识”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“获取时钟”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 6.3 阴影口令

“6.3 阴影口令”不是孤立API名词。先写出输入、返回值、errno与资源所有权，再沿用户接口、C库封装、系统调用和内核对象定位责任边界。在本章中，它通过“uname”服务于“获取时钟”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“转换时区”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 6.4 组文件

“6.4 组文件”不是孤立API名词。画出名字、描述符、打开对象、偏移、缓存与持久对象的关系，并标明复制、继承、刷新和最终释放时刻。在本章中，它通过“时间转换”服务于“转换时区”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“核对边界”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 6.5 附属组ID

“6.5 附属组ID”不是孤立API名词。先写出输入、返回值、errno与资源所有权，再沿用户接口、C库封装、系统调用和内核对象定位责任边界。在本章中，它通过“口令文件”服务于“核对边界”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“查询身份”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 6.6 实现区别

“6.6 实现区别”不是孤立API名词。区分标准保证、实现选择与运行时查询结果，用功能测试宏和探测程序替代平台常量假设。在本章中，它通过“阴影口令”服务于“查询身份”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“扩展组集”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 6.7 其他数据文件

“6.7 其他数据文件”不是孤立API名词。画出名字、描述符、打开对象、偏移、缓存与持久对象的关系，并标明复制、继承、刷新和最终释放时刻。在本章中，它通过“附属组”服务于“扩展组集”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“读取系统标识”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 6.8 登录账户记录

“6.8 登录账户记录”不是孤立API名词。先写出输入、返回值、errno与资源所有权，再沿用户接口、C库封装、系统调用和内核对象定位责任边界。在本章中，它通过“uname”服务于“读取系统标识”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“获取时钟”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 6.9 系统标识

“6.9 系统标识”不是孤立API名词。先写出输入、返回值、errno与资源所有权，再沿用户接口、C库封装、系统调用和内核对象定位责任边界。在本章中，它通过“时间转换”服务于“获取时钟”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“转换时区”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 6.10 时间和日期例程

“6.10 时间和日期例程”不是孤立API名词。先写出输入、返回值、errno与资源所有权，再沿用户接口、C库封装、系统调用和内核对象定位责任边界。在本章中，它通过“口令文件”服务于“转换时区”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“核对边界”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 6.11 小结

“6.11 小结”不是孤立API名词。把对象、控制流、失败模型、原始证据和恢复条件收束为可复查清单，并用同一输入重放确认没有残留状态。在本章中，它通过“阴影口令”服务于“核对边界”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“查询身份”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

## 分步视觉验证

先预测对象、返回值和状态，再动手试一试；若结果不同，停在第一条系统调用或对象状态分叉。


## 常见误区


## 四类样本与验收

| 样本 | 只改变什么                 | 必查证据                  | 通过条件   |
| ---- | -------------------------- | ------------------------- | ---------- |
| 基线 | 平台、宏与固定输入         | 返回值、errno、对象与轨迹 | 与预测一致 |
| 边界 | 长度、容量、权限或时序     | 短计数、拒绝或阻塞点      | 边界可解释 |
| 故障 | 一个信号、断开或失败注入   | 首错与责任层              | 故障可定位 |
| 恢复 | 关闭、回收、删除与属性还原 | 残留对象和同输入重放      | 回到基线   |

## 练习


## 小结

- 口令文件：连接“查询身份”的接口合同与运行证据。
- 阴影口令：连接“扩展组集”的接口合同与运行证据。
- 附属组：连接“读取系统标识”的接口合同与运行证据。
- uname：连接“获取时钟”的接口合同与运行证据。
- 时间转换：连接“转换时区”的接口合同与运行证据。
- 本页12个正式目录层级全部进入可重放实验链。

## 术语表


