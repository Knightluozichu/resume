---
title: "第9章 进程关系"
type: C
section: "进程模型"
order: 9
description: "用会话、进程组、控制终端、作业控制和孤儿进程组解释shell与登录会话的进程关系。 覆盖13个正式目录层级。"
demo: true
math: false
sourceUrl: "https://www.cnblogs.com/china-pub/p/3782412.html"
draft: true
---

import {
  UapProcessRelationshipsMapLab,
  UapProcessRelationshipsExperimentLab,
  UapProcessRelationshipsEvidenceLab,
} from "@/components/mdx/unix-advanced-programming/diagrams/uap-process-relationships";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第9章 进程关系的核心接口、对象所有权与POSIX/SUS版本边界
- 能描述“建立会话 → 设置进程组 → 绑定终端 → 切换前后台 → 制造孤儿组 → 恢复终端”的输入、返回值、errno与系统可见证据
- 能实现一个单变量、可回收、可重放的最小UNIX C实验
- 能比较基线、边界、故障和恢复轨迹并判断“PID/PPID/PGID/SID/前台组一致，终端信号送达正确对象；后台或退出场景不会悬挂终端状态。”是否成立

</Objectives>

## 为什么从接口合同开始

<Term def="进程组连接建立会话阶段的接口合同、对象所有权、失败语义和清理证据。">进程组</Term>、<Term def="会话连接设置进程组阶段的接口合同、对象所有权、失败语义和清理证据。">会话</Term>、<Term def="控制终端连接绑定终端阶段的接口合同、对象所有权、失败语义和清理证据。">控制终端</Term>、<Term def="作业控制连接切换前后台阶段的接口合同、对象所有权、失败语义和清理证据。">作业控制</Term>、<Term def="孤儿进程组连接制造孤儿组阶段的接口合同、对象所有权、失败语义和清理证据。">孤儿进程组</Term>
共同构成本页坐标。用会话、进程组、控制终端、作业控制和孤儿进程组解释shell与登录会话的进程关系。

APUE第3版面向SUSv4/POSIX.1-2008，并以Solaris 10、Mac OS X 10.6.8、FreeBSD 8.0和Ubuntu 12.04为主要验证平台。正文先还原书中标准合同与实现差异；Linux当前行为只能作为显式现代对照，不能悄悄覆盖原书的版本边界。

## 六阶段可重放链

### 1. 建立会话

围绕进程组记录平台、功能测试宏、输入、描述符/对象表、返回值、errno和预期状态。进入“设置进程组”前保存第一条偏离，清理与恢复也写入结果。

### 2. 设置进程组

围绕会话记录平台、功能测试宏、输入、描述符/对象表、返回值、errno和预期状态。进入“绑定终端”前保存第一条偏离，清理与恢复也写入结果。

### 3. 绑定终端

围绕控制终端记录平台、功能测试宏、输入、描述符/对象表、返回值、errno和预期状态。进入“切换前后台”前保存第一条偏离，清理与恢复也写入结果。

### 4. 切换前后台

围绕作业控制记录平台、功能测试宏、输入、描述符/对象表、返回值、errno和预期状态。进入“制造孤儿组”前保存第一条偏离，清理与恢复也写入结果。

### 5. 制造孤儿组

围绕孤儿进程组记录平台、功能测试宏、输入、描述符/对象表、返回值、errno和预期状态。进入“恢复终端”前保存第一条偏离，清理与恢复也写入结果。

### 6. 恢复终端

围绕进程组记录平台、功能测试宏、输入、描述符/对象表、返回值、errno和预期状态。进入“建立会话”前保存第一条偏离，清理与恢复也写入结果。

## 机制、所有权与失败模型

用会话、进程组、控制终端、作业控制和孤儿进程组解释shell与登录会话的进程关系。

本页核心不变量是：**PID/PPID/PGID/SID/前台组一致，终端信号送达正确对象；后台或退出场景不会悬挂终端状态。** 它必须同时约束正常、边界、故障和恢复样本。接口返回成功只是证据之一，还要检查短计数、EINTR/EAGAIN、对象继承、并发时序、缓冲与异步清理。

实验台账至少包含平台与版本、编译宏、入口参数、对象所有者、描述符或ID、预期返回、实际返回、errno、首个状态分叉、原始轨迹和清理结果十一列。性能样本还需固定预热、CPU放置、输入规模、采样窗口与分位数；信号/线程样本必须记录掩码、取消状态和处理上下文。

## 可重放命令骨架

```c
pid_t pid = fork();
if (pid == -1) err(1, "fork");
```

```bash
ps -eo pid,ppid,pgid,sid,stat,comm --forest
```

```bash
strace -f -e clone,fork,vfork,execve,wait4 ./probe
```

<Callout type="trap">
  **安全边界：**
  权限、凭证、信号、终端、IPC清理和网络实验先在隔离用户、容器或可恢复虚拟机执行。保存原始属性与资源清单，退出处理必须可重复。
</Callout>

## 正式目录逐项讲解

### 第9章 进程关系

“第9章 进程关系”不是孤立API名词。记录PID/线程ID、父子或共享关系、状态转换、调度点、信号掩码与回收责任，优先观察竞态发生前的第一处分叉。在本章中，它通过“进程组”服务于“建立会话”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“设置进程组”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 9.1 引言

“9.1 引言”不是孤立API名词。先写出输入、返回值、errno与资源所有权，再沿用户接口、C库封装、系统调用和内核对象定位责任边界。在本章中，它通过“会话”服务于“设置进程组”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“绑定终端”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 9.2 终端登录

“9.2 终端登录”不是孤立API名词。明确端点命名、消息边界、容量与背压、权限、同步和断开语义，并为异常退出设计可发现的清理步骤。在本章中，它通过“控制终端”服务于“绑定终端”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“切换前后台”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 9.3 网络登录

“9.3 网络登录”不是孤立API名词。先写出输入、返回值、errno与资源所有权，再沿用户接口、C库封装、系统调用和内核对象定位责任边界。在本章中，它通过“作业控制”服务于“切换前后台”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“制造孤儿组”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 9.4 进程组

“9.4 进程组”不是孤立API名词。记录PID/线程ID、父子或共享关系、状态转换、调度点、信号掩码与回收责任，优先观察竞态发生前的第一处分叉。在本章中，它通过“孤儿进程组”服务于“制造孤儿组”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“恢复终端”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 9.5 会话

“9.5 会话”不是孤立API名词。记录PID/线程ID、父子或共享关系、状态转换、调度点、信号掩码与回收责任，优先观察竞态发生前的第一处分叉。在本章中，它通过“进程组”服务于“恢复终端”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“建立会话”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 9.6 控制终端

“9.6 控制终端”不是孤立API名词。明确端点命名、消息边界、容量与背压、权限、同步和断开语义，并为异常退出设计可发现的清理步骤。在本章中，它通过“会话”服务于“建立会话”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“设置进程组”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 9.7 函数tcgetpgrp、tcsetpgrp和tcgetsid

“9.7 函数tcgetpgrp、tcsetpgrp和tcgetsid”不是孤立API名词。把函数原型、前置条件、成功返回、失败errno、可中断性和资源转移逐项列出，禁止把一次成功调用当成完整合同。在本章中，它通过“控制终端”服务于“设置进程组”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“绑定终端”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 9.8 作业控制

“9.8 作业控制”不是孤立API名词。记录PID/线程ID、父子或共享关系、状态转换、调度点、信号掩码与回收责任，优先观察竞态发生前的第一处分叉。在本章中，它通过“作业控制”服务于“绑定终端”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“切换前后台”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 9.9 shell执行程序

“9.9 shell执行程序”不是孤立API名词。先写出输入、返回值、errno与资源所有权，再沿用户接口、C库封装、系统调用和内核对象定位责任边界。在本章中，它通过“孤儿进程组”服务于“切换前后台”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“制造孤儿组”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 9.10 孤儿进程组

“9.10 孤儿进程组”不是孤立API名词。记录PID/线程ID、父子或共享关系、状态转换、调度点、信号掩码与回收责任，优先观察竞态发生前的第一处分叉。在本章中，它通过“进程组”服务于“制造孤儿组”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“恢复终端”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 9.11 FreeBSD实现

“9.11 FreeBSD实现”不是孤立API名词。区分标准保证、实现选择与运行时查询结果，用功能测试宏和探测程序替代平台常量假设。在本章中，它通过“会话”服务于“恢复终端”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“建立会话”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 9.12 小结

“9.12 小结”不是孤立API名词。把对象、控制流、失败模型、原始证据和恢复条件收束为可复查清单，并用同一输入重放确认没有残留状态。在本章中，它通过“控制终端”服务于“建立会话”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“设置进程组”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

## 分步视觉验证

先预测对象、返回值和状态，再动手试一试；若结果不同，停在第一条系统调用或对象状态分叉。

<Stepper>
  <Step title="画接口与对象图">
    先预测进程组、会话、控制终端、作业控制、孤儿进程组的所有权、入口、返回值和状态转换。
    <UapProcessRelationshipsMapLab />
  </Step>
  <Step title="运行单变量实验">
    固定平台、宏、输入和初始资源，只切换一个边界或失败条件。
    <UapProcessRelationshipsExperimentLab />
  </Step>
  <Step title="核对首错与恢复">
    保存原始轨迹，关闭对象并恢复属性后用相同输入重放。
    <UapProcessRelationshipsEvidenceLab />
  </Step>
</Stepper>

## 常见误区

<Callout type="trap">
  **误区一：**
  只检查最终输出，不检查短计数、errno、对象继承、信号掩码和并发时序。
</Callout>

<Callout type="trap">
  **误区二：**
  把Linux当前行为当成POSIX保证，或在失败路径遗漏描述符、线程、IPC名字和终端属性的清理。
</Callout>

## 四类样本与验收

| 样本 | 只改变什么                 | 必查证据                  | 通过条件   |
| ---- | -------------------------- | ------------------------- | ---------- |
| 基线 | 平台、宏与固定输入         | 返回值、errno、对象与轨迹 | 与预测一致 |
| 边界 | 长度、容量、权限或时序     | 短计数、拒绝或阻塞点      | 边界可解释 |
| 故障 | 一个信号、断开或失败注入   | 首错与责任层              | 故障可定位 |
| 恢复 | 关闭、回收、删除与属性还原 | 残留对象和同输入重放      | 回到基线   |

## 练习

<Exercises>

**问题 1** 为什么不能只凭最终输出验收第9章 进程关系？

<Answer>
  最终输出会隐藏部分读写、EINTR、描述符泄漏、进程/线程未回收、缓冲和竞态。必须同时核对标准边界、接口合同、对象生存期、首个状态分叉和恢复结果。
</Answer>

**问题 2** 如何把“绑定终端”变成运行证据？

<Answer>
  固定平台、功能测试宏、工具和输入，先预测进程组与会话的返回值、errno、所有权和状态，只改变一个条件并停在首错。
</Answer>

**问题 3** 本页最小通过条件是什么？

<Answer>
  PID/PPID/PGID/SID/前台组一致，终端信号送达正确对象；后台或退出场景不会悬挂终端状态。
  关闭全部对象并恢复属性后，相同输入必须重新得到基线结果。
</Answer>

</Exercises>

## 小结

- 进程组：连接“建立会话”的接口合同与运行证据。
- 会话：连接“设置进程组”的接口合同与运行证据。
- 控制终端：连接“绑定终端”的接口合同与运行证据。
- 作业控制：连接“切换前后台”的接口合同与运行证据。
- 孤儿进程组：连接“制造孤儿组”的接口合同与运行证据。
- 本页13个正式目录层级全部进入可重放实验链。

## 术语表

<Glossary>
  <GlossaryItem term="进程组">
    进程组用于解释“建立会话”，需要同时标出标准版本、接口合同、对象所有权、首错和清理方法。
  </GlossaryItem>
  <GlossaryItem term="会话">
    会话用于解释“设置进程组”，需要同时标出标准版本、接口合同、对象所有权、首错和清理方法。
  </GlossaryItem>
  <GlossaryItem term="控制终端">
    控制终端用于解释“绑定终端”，需要同时标出标准版本、接口合同、对象所有权、首错和清理方法。
  </GlossaryItem>
  <GlossaryItem term="作业控制">
    作业控制用于解释“切换前后台”，需要同时标出标准版本、接口合同、对象所有权、首错和清理方法。
  </GlossaryItem>
  <GlossaryItem term="孤儿进程组">
    孤儿进程组用于解释“制造孤儿组”，需要同时标出标准版本、接口合同、对象所有权、首错和清理方法。
  </GlossaryItem>
</Glossary>

<Attribution
  adaptedFrom="W. Richard Stevens、Stephen A. Rago著，戚正伟、张亚英、尤晋元译《UNIX环境高级编程（第3版）》，人民邮电出版社，2014-06，812页，ISBN 9787115352118 · 完整中文目录与Pearson原版目录的独立教学重构"
  adaptedUrl="https://www.cnblogs.com/china-pub/p/3782412.html"
/>
