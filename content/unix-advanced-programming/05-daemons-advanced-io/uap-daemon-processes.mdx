---
title: "第13章 守护进程"
type: C
section: "守护进程与高级I/O"
order: 13
description: "从会话脱离、工作目录、文件模式、描述符、日志、单实例和客户端服务器惯例构造守护进程。 覆盖9个正式目录层级。"
demo: true
math: false
sourceUrl: "https://www.cnblogs.com/china-pub/p/3782412.html"
draft: true
---

import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第13章 守护进程的核心接口、对象所有权与POSIX/SUS版本边界
- 能描述“派生并脱离 → 重设环境 → 关闭描述符 → 获取实例锁 → 记录服务状态 → 终止清理”的输入、返回值、errno与系统可见证据
- 能实现一个单变量、可回收、可重放的最小UNIX C实验
- 能比较基线、边界、故障和恢复轨迹并判断“守护进程无意外控制终端，描述符与权限最小化；PID锁、日志和终止清理在崩溃后仍可诊断。”是否成立

</Objectives>

## 为什么从接口合同开始

<Term def="setsid连接派生并脱离阶段的接口合同、对象所有权、失败语义和清理证据。">setsid</Term>、<Term def="守护进程连接重设环境阶段的接口合同、对象所有权、失败语义和清理证据。">守护进程</Term>、<Term def="syslog连接关闭描述符阶段的接口合同、对象所有权、失败语义和清理证据。">syslog</Term>、<Term def="单实例连接获取实例锁阶段的接口合同、对象所有权、失败语义和清理证据。">单实例</Term>、<Term def="客户端服务器连接记录服务状态阶段的接口合同、对象所有权、失败语义和清理证据。">客户端服务器</Term>
共同构成本页坐标。从会话脱离、工作目录、文件模式、描述符、日志、单实例和客户端服务器惯例构造守护进程。

APUE第3版面向SUSv4/POSIX.1-2008，并以Solaris 10、Mac OS X 10.6.8、FreeBSD 8.0和Ubuntu 12.04为主要验证平台。正文先还原书中标准合同与实现差异；Linux当前行为只能作为显式现代对照，不能悄悄覆盖原书的版本边界。

## 六阶段可重放链

### 1. 派生并脱离

围绕setsid记录平台、功能测试宏、输入、描述符/对象表、返回值、errno和预期状态。进入“重设环境”前保存第一条偏离，清理与恢复也写入结果。

### 2. 重设环境

围绕守护进程记录平台、功能测试宏、输入、描述符/对象表、返回值、errno和预期状态。进入“关闭描述符”前保存第一条偏离，清理与恢复也写入结果。

### 3. 关闭描述符

围绕syslog记录平台、功能测试宏、输入、描述符/对象表、返回值、errno和预期状态。进入“获取实例锁”前保存第一条偏离，清理与恢复也写入结果。

### 4. 获取实例锁

围绕单实例记录平台、功能测试宏、输入、描述符/对象表、返回值、errno和预期状态。进入“记录服务状态”前保存第一条偏离，清理与恢复也写入结果。

### 5. 记录服务状态

围绕客户端服务器记录平台、功能测试宏、输入、描述符/对象表、返回值、errno和预期状态。进入“终止清理”前保存第一条偏离，清理与恢复也写入结果。

### 6. 终止清理

围绕setsid记录平台、功能测试宏、输入、描述符/对象表、返回值、errno和预期状态。进入“派生并脱离”前保存第一条偏离，清理与恢复也写入结果。

## 机制、所有权与失败模型

从会话脱离、工作目录、文件模式、描述符、日志、单实例和客户端服务器惯例构造守护进程。

本页核心不变量是：**守护进程无意外控制终端，描述符与权限最小化；PID锁、日志和终止清理在崩溃后仍可诊断。** 它必须同时约束正常、边界、故障和恢复样本。接口返回成功只是证据之一，还要检查短计数、EINTR/EAGAIN、对象继承、并发时序、缓冲与异步清理。

实验台账至少包含平台与版本、编译宏、入口参数、对象所有者、描述符或ID、预期返回、实际返回、errno、首个状态分叉、原始轨迹和清理结果十一列。性能样本还需固定预热、CPU放置、输入规模、采样窗口与分位数；信号/线程样本必须记录掩码、取消状态和处理上下文。

## 可重放命令骨架

```bash
./probe & pid=$!
ps -o pid,ppid,pgid,sid,tty,stat -p "$pid"
```

```bash
logger -t apue-probe 'daemon checkpoint'
journalctl -t apue-probe -n 5
```

```bash
lsof -p "$pid"
kill -TERM "$pid"; wait "$pid"
```

<Callout type="trap">
  **安全边界：**
  权限、凭证、信号、终端、IPC清理和网络实验先在隔离用户、容器或可恢复虚拟机执行。保存原始属性与资源清单，退出处理必须可重复。
</Callout>

## 正式目录逐项讲解

### 第13章 守护进程

“第13章 守护进程”不是孤立API名词。记录PID/线程ID、父子或共享关系、状态转换、调度点、信号掩码与回收责任，优先观察竞态发生前的第一处分叉。在本章中，它通过“setsid”服务于“派生并脱离”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“重设环境”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 13.1 引言

“13.1 引言”不是孤立API名词。先写出输入、返回值、errno与资源所有权，再沿用户接口、C库封装、系统调用和内核对象定位责任边界。在本章中，它通过“守护进程”服务于“重设环境”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“关闭描述符”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 13.2 守护进程的特征

“13.2 守护进程的特征”不是孤立API名词。记录PID/线程ID、父子或共享关系、状态转换、调度点、信号掩码与回收责任，优先观察竞态发生前的第一处分叉。在本章中，它通过“syslog”服务于“关闭描述符”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“获取实例锁”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 13.3 编程规则

“13.3 编程规则”不是孤立API名词。先写出输入、返回值、errno与资源所有权，再沿用户接口、C库封装、系统调用和内核对象定位责任边界。在本章中，它通过“单实例”服务于“获取实例锁”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“记录服务状态”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 13.4 出错记录

“13.4 出错记录”不是孤立API名词。先写出输入、返回值、errno与资源所有权，再沿用户接口、C库封装、系统调用和内核对象定位责任边界。在本章中，它通过“客户端服务器”服务于“记录服务状态”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“终止清理”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 13.5 单实例守护进程

“13.5 单实例守护进程”不是孤立API名词。记录PID/线程ID、父子或共享关系、状态转换、调度点、信号掩码与回收责任，优先观察竞态发生前的第一处分叉。在本章中，它通过“setsid”服务于“终止清理”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“派生并脱离”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 13.6 守护进程的惯例

“13.6 守护进程的惯例”不是孤立API名词。记录PID/线程ID、父子或共享关系、状态转换、调度点、信号掩码与回收责任，优先观察竞态发生前的第一处分叉。在本章中，它通过“守护进程”服务于“派生并脱离”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“重设环境”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 13.7 客户进程-服务器进程模型

“13.7 客户进程-服务器进程模型”不是孤立API名词。记录PID/线程ID、父子或共享关系、状态转换、调度点、信号掩码与回收责任，优先观察竞态发生前的第一处分叉。在本章中，它通过“syslog”服务于“重设环境”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“关闭描述符”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 13.8 小结

“13.8 小结”不是孤立API名词。把对象、控制流、失败模型、原始证据和恢复条件收束为可复查清单，并用同一输入重放确认没有残留状态。在本章中，它通过“单实例”服务于“关闭描述符”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“获取实例锁”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

## 分步视觉验证

先预测对象、返回值和状态，再动手试一试；若结果不同，停在第一条系统调用或对象状态分叉。

<Stepper>
  <Step title="画接口与对象图">
    先预测setsid、守护进程、syslog、单实例、客户端服务器的所有权、入口、返回值和状态转换。
    </Step>
  <Step title="运行单变量实验">
    固定平台、宏、输入和初始资源，只切换一个边界或失败条件。
    </Step>
  <Step title="核对首错与恢复">
    保存原始轨迹，关闭对象并恢复属性后用相同输入重放。
    </Step>
</Stepper>

## 常见误区

<Callout type="trap">
  **误区一：**
  只检查最终输出，不检查短计数、errno、对象继承、信号掩码和并发时序。
</Callout>

<Callout type="trap">
  **误区二：**
  把Linux当前行为当成POSIX保证，或在失败路径遗漏描述符、线程、IPC名字和终端属性的清理。
</Callout>

## 四类样本与验收

| 样本 | 只改变什么                 | 必查证据                  | 通过条件   |
| ---- | -------------------------- | ------------------------- | ---------- |
| 基线 | 平台、宏与固定输入         | 返回值、errno、对象与轨迹 | 与预测一致 |
| 边界 | 长度、容量、权限或时序     | 短计数、拒绝或阻塞点      | 边界可解释 |
| 故障 | 一个信号、断开或失败注入   | 首错与责任层              | 故障可定位 |
| 恢复 | 关闭、回收、删除与属性还原 | 残留对象和同输入重放      | 回到基线   |

## 练习

<Exercises>

**问题 1** 为什么不能只凭最终输出验收第13章 守护进程？

<Answer>
  最终输出会隐藏部分读写、EINTR、描述符泄漏、进程/线程未回收、缓冲和竞态。必须同时核对标准边界、接口合同、对象生存期、首个状态分叉和恢复结果。
</Answer>

**问题 2** 如何把“关闭描述符”变成运行证据？

<Answer>
  固定平台、功能测试宏、工具和输入，先预测setsid与守护进程的返回值、errno、所有权和状态，只改变一个条件并停在首错。
</Answer>

**问题 3** 本页最小通过条件是什么？

<Answer>
  守护进程无意外控制终端，描述符与权限最小化；PID锁、日志和终止清理在崩溃后仍可诊断。
  关闭全部对象并恢复属性后，相同输入必须重新得到基线结果。
</Answer>

</Exercises>

## 小结

- setsid：连接“派生并脱离”的接口合同与运行证据。
- 守护进程：连接“重设环境”的接口合同与运行证据。
- syslog：连接“关闭描述符”的接口合同与运行证据。
- 单实例：连接“获取实例锁”的接口合同与运行证据。
- 客户端服务器：连接“记录服务状态”的接口合同与运行证据。
- 本页9个正式目录层级全部进入可重放实验链。

## 术语表

<Glossary>
  <GlossaryItem term="setsid">
    setsid用于解释“派生并脱离”，需要同时标出标准版本、接口合同、对象所有权、首错和清理方法。
  </GlossaryItem>
  <GlossaryItem term="守护进程">
    守护进程用于解释“重设环境”，需要同时标出标准版本、接口合同、对象所有权、首错和清理方法。
  </GlossaryItem>
  <GlossaryItem term="syslog">
    syslog用于解释“关闭描述符”，需要同时标出标准版本、接口合同、对象所有权、首错和清理方法。
  </GlossaryItem>
  <GlossaryItem term="单实例">
    单实例用于解释“获取实例锁”，需要同时标出标准版本、接口合同、对象所有权、首错和清理方法。
  </GlossaryItem>
  <GlossaryItem term="客户端服务器">
    客户端服务器用于解释“记录服务状态”，需要同时标出标准版本、接口合同、对象所有权、首错和清理方法。
  </GlossaryItem>
</Glossary>

<Attribution
  adaptedFrom="W. Richard Stevens、Stephen A. Rago著，戚正伟、张亚英、尤晋元译《UNIX环境高级编程（第3版）》，人民邮电出版社，2014-06，812页，ISBN 9787115352118 · 完整中文目录与Pearson原版目录的独立教学重构"
  adaptedUrl="https://www.cnblogs.com/china-pub/p/3782412.html"
/>
