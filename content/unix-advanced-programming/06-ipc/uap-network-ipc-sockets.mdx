---
title: "第16章 网络IPC：套接字"
type: C
section: "进程间通信"
order: 16
description: "从套接字描述符、寻址、连接建立、数据传输、选项、带外数据与非阻塞模式构造网络IPC。 覆盖14个正式目录层级。"
demo: true
math: false
sourceUrl: "https://www.cnblogs.com/china-pub/p/3782412.html"
draft: false
---

import {
  UapNetworkIpcSocketsMapLab,
  UapNetworkIpcSocketsExperimentLab,
  UapNetworkIpcSocketsEvidenceLab,
} from "@/components/mdx/unix-advanced-programming/diagrams/uap-network-ipc-sockets";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第16章 网络IPC：套接字的核心接口、对象所有权与POSIX/SUS版本边界
- 能描述“解析地址 → 创建套接字 → 绑定或连接 → 传输分帧 → 处理半关闭 → 关闭核对”的输入、返回值、errno与系统可见证据
- 能实现一个单变量、可回收、可重放的最小UNIX C实验
- 能比较基线、边界、故障和恢复轨迹并判断“地址长度、字节序、流式分帧、部分发送、半关闭和超时全部处理；失败连接不泄漏描述符。”是否成立

</Objectives>

## 为什么从接口合同开始

<Term def="套接字描述符连接解析地址阶段的接口合同、对象所有权、失败语义和清理证据。">
  套接字描述符
</Term>
、
<Term def="字节序连接创建套接字阶段的接口合同、对象所有权、失败语义和清理证据。">
  字节序
</Term>
、
<Term def="地址查询连接绑定或连接阶段的接口合同、对象所有权、失败语义和清理证据。">
  地址查询
</Term>
、
<Term def="连接建立连接传输分帧阶段的接口合同、对象所有权、失败语义和清理证据。">
  连接建立
</Term>
、
<Term def="套接字选项连接处理半关闭阶段的接口合同、对象所有权、失败语义和清理证据。">
  套接字选项
</Term>
共同构成本页坐标。从套接字描述符、寻址、连接建立、数据传输、选项、带外数据与非阻塞模式构造网络IPC。

APUE第3版面向SUSv4/POSIX.1-2008，并以Solaris 10、Mac OS X 10.6.8、FreeBSD 8.0和Ubuntu 12.04为主要验证平台。正文先还原书中标准合同与实现差异；Linux当前行为只能作为显式现代对照，不能悄悄覆盖原书的版本边界。

## 六阶段可重放链

### 1. 解析地址

围绕套接字描述符记录平台、功能测试宏、输入、描述符/对象表、返回值、errno和预期状态。进入“创建套接字”前保存第一条偏离，清理与恢复也写入结果。

### 2. 创建套接字

围绕字节序记录平台、功能测试宏、输入、描述符/对象表、返回值、errno和预期状态。进入“绑定或连接”前保存第一条偏离，清理与恢复也写入结果。

### 3. 绑定或连接

围绕地址查询记录平台、功能测试宏、输入、描述符/对象表、返回值、errno和预期状态。进入“传输分帧”前保存第一条偏离，清理与恢复也写入结果。

### 4. 传输分帧

围绕连接建立记录平台、功能测试宏、输入、描述符/对象表、返回值、errno和预期状态。进入“处理半关闭”前保存第一条偏离，清理与恢复也写入结果。

### 5. 处理半关闭

围绕套接字选项记录平台、功能测试宏、输入、描述符/对象表、返回值、errno和预期状态。进入“关闭核对”前保存第一条偏离，清理与恢复也写入结果。

### 6. 关闭核对

围绕套接字描述符记录平台、功能测试宏、输入、描述符/对象表、返回值、errno和预期状态。进入“解析地址”前保存第一条偏离，清理与恢复也写入结果。

## 机制、所有权与失败模型

从套接字描述符、寻址、连接建立、数据传输、选项、带外数据与非阻塞模式构造网络IPC。

本页核心不变量是：**地址长度、字节序、流式分帧、部分发送、半关闭和超时全部处理；失败连接不泄漏描述符。** 它必须同时约束正常、边界、故障和恢复样本。接口返回成功只是证据之一，还要检查短计数、EINTR/EAGAIN、对象继承、并发时序、缓冲与异步清理。

实验台账至少包含平台与版本、编译宏、入口参数、对象所有者、描述符或ID、预期返回、实际返回、errno、首个状态分叉、原始轨迹和清理结果十一列。性能样本还需固定预热、CPU放置、输入规模、采样窗口与分位数；信号/线程样本必须记录掩码、取消状态和处理上下文。

## 可重放命令骨架

```c
int fd = socket(AF_INET, SOCK_STREAM | SOCK_CLOEXEC, 0);
if (fd == -1) err(1, "socket");
```

```bash
ss -ntap
ip -br address
```

```bash
strace -f -e socket,bind,listen,accept,connect,sendto,recvfrom ./probe
```

<Callout type="trap">
  **安全边界：**
  权限、凭证、信号、终端、IPC清理和网络实验先在隔离用户、容器或可恢复虚拟机执行。保存原始属性与资源清单，退出处理必须可重复。
</Callout>

## 正式目录逐项讲解

### 第16章 网络IPC：套接字

“第16章 网络IPC：套接字”不是孤立API名词。明确端点命名、消息边界、容量与背压、权限、同步和断开语义，并为异常退出设计可发现的清理步骤。在本章中，它通过“套接字描述符”服务于“解析地址”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“创建套接字”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 16.1 引言

“16.1 引言”不是孤立API名词。先写出输入、返回值、errno与资源所有权，再沿用户接口、C库封装、系统调用和内核对象定位责任边界。在本章中，它通过“字节序”服务于“创建套接字”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“绑定或连接”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 16.2 套接字描述符

“16.2 套接字描述符”不是孤立API名词。画出名字、描述符、打开对象、偏移、缓存与持久对象的关系，并标明复制、继承、刷新和最终释放时刻。在本章中，它通过“地址查询”服务于“绑定或连接”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“传输分帧”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 16.3 寻址

“16.3 寻址”不是孤立API名词。先写出输入、返回值、errno与资源所有权，再沿用户接口、C库封装、系统调用和内核对象定位责任边界。在本章中，它通过“连接建立”服务于“传输分帧”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“处理半关闭”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 16.3.1 字节序

“16.3.1 字节序”不是孤立API名词。区分标准保证、实现选择与运行时查询结果，用功能测试宏和探测程序替代平台常量假设。在本章中，它通过“套接字选项”服务于“处理半关闭”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“关闭核对”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 16.3.2 地址格式

“16.3.2 地址格式”不是孤立API名词。先写出输入、返回值、errno与资源所有权，再沿用户接口、C库封装、系统调用和内核对象定位责任边界。在本章中，它通过“套接字描述符”服务于“关闭核对”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“解析地址”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 16.3.3 地址查询

“16.3.3 地址查询”不是孤立API名词。先写出输入、返回值、errno与资源所有权，再沿用户接口、C库封装、系统调用和内核对象定位责任边界。在本章中，它通过“字节序”服务于“解析地址”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“创建套接字”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 16.3.4 将套接字与地址关联

“16.3.4 将套接字与地址关联”不是孤立API名词。明确端点命名、消息边界、容量与背压、权限、同步和断开语义，并为异常退出设计可发现的清理步骤。在本章中，它通过“地址查询”服务于“创建套接字”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“绑定或连接”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 16.4 建立连接

“16.4 建立连接”不是孤立API名词。先写出输入、返回值、errno与资源所有权，再沿用户接口、C库封装、系统调用和内核对象定位责任边界。在本章中，它通过“连接建立”服务于“绑定或连接”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“传输分帧”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 16.5 数据传输

“16.5 数据传输”不是孤立API名词。先写出输入、返回值、errno与资源所有权，再沿用户接口、C库封装、系统调用和内核对象定位责任边界。在本章中，它通过“套接字选项”服务于“传输分帧”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“处理半关闭”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 16.6 套接字选项

“16.6 套接字选项”不是孤立API名词。明确端点命名、消息边界、容量与背压、权限、同步和断开语义，并为异常退出设计可发现的清理步骤。在本章中，它通过“套接字描述符”服务于“处理半关闭”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“关闭核对”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 16.7 带外数据

“16.7 带外数据”不是孤立API名词。先写出输入、返回值、errno与资源所有权，再沿用户接口、C库封装、系统调用和内核对象定位责任边界。在本章中，它通过“字节序”服务于“关闭核对”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“解析地址”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 16.8 非阻塞和异步I/O

“16.8 非阻塞和异步I/O”不是孤立API名词。先写出输入、返回值、errno与资源所有权，再沿用户接口、C库封装、系统调用和内核对象定位责任边界。在本章中，它通过“地址查询”服务于“解析地址”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“创建套接字”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

### 16.9 小结

“16.9 小结”不是孤立API名词。把对象、控制流、失败模型、原始证据和恢复条件收束为可复查清单，并用同一输入重放确认没有残留状态。在本章中，它通过“连接建立”服务于“创建套接字”：先固定平台、编译宏、输入与初始资源表，再记录原始返回值和系统可见状态。

验证时只改变一个长度、容量、时序、权限、信号或失败点，并在进入“绑定或连接”前停住第一条返回值、errno、系统调用轨迹、对象计数或状态分叉。最后关闭描述符、回收进程/线程、删除命名对象并恢复终端或信号掩码；同输入重放回到基线，结论才成立。

## 分步视觉验证

先预测对象、返回值和状态，再动手试一试；若结果不同，停在第一条系统调用或对象状态分叉。

<Stepper>
  <Step title="画接口与对象图">
    先预测套接字描述符、字节序、地址查询、连接建立、套接字选项的所有权、入口、返回值和状态转换。
    <UapNetworkIpcSocketsMapLab />
  </Step>
  <Step title="运行单变量实验">
    固定平台、宏、输入和初始资源，只切换一个边界或失败条件。
    <UapNetworkIpcSocketsExperimentLab />
  </Step>
  <Step title="核对首错与恢复">
    保存原始轨迹，关闭对象并恢复属性后用相同输入重放。
    <UapNetworkIpcSocketsEvidenceLab />
  </Step>
</Stepper>

## 常见误区

<Callout type="trap">
  **误区一：**
  只检查最终输出，不检查短计数、errno、对象继承、信号掩码和并发时序。
</Callout>

<Callout type="trap">
  **误区二：**
  把Linux当前行为当成POSIX保证，或在失败路径遗漏描述符、线程、IPC名字和终端属性的清理。
</Callout>

## 四类样本与验收

| 样本 | 只改变什么                 | 必查证据                  | 通过条件   |
| ---- | -------------------------- | ------------------------- | ---------- |
| 基线 | 平台、宏与固定输入         | 返回值、errno、对象与轨迹 | 与预测一致 |
| 边界 | 长度、容量、权限或时序     | 短计数、拒绝或阻塞点      | 边界可解释 |
| 故障 | 一个信号、断开或失败注入   | 首错与责任层              | 故障可定位 |
| 恢复 | 关闭、回收、删除与属性还原 | 残留对象和同输入重放      | 回到基线   |

## 练习

<Exercises>

**问题 1** 为什么不能只凭最终输出验收第16章 网络IPC：套接字？

<Answer>
  最终输出会隐藏部分读写、EINTR、描述符泄漏、进程/线程未回收、缓冲和竞态。必须同时核对标准边界、接口合同、对象生存期、首个状态分叉和恢复结果。
</Answer>

**问题 2** 如何把“绑定或连接”变成运行证据？

<Answer>
  固定平台、功能测试宏、工具和输入，先预测套接字描述符与字节序的返回值、errno、所有权和状态，只改变一个条件并停在首错。
</Answer>

**问题 3** 本页最小通过条件是什么？

<Answer>
  地址长度、字节序、流式分帧、部分发送、半关闭和超时全部处理；失败连接不泄漏描述符。
  关闭全部对象并恢复属性后，相同输入必须重新得到基线结果。
</Answer>

</Exercises>

## 小结

- 套接字描述符：连接“解析地址”的接口合同与运行证据。
- 字节序：连接“创建套接字”的接口合同与运行证据。
- 地址查询：连接“绑定或连接”的接口合同与运行证据。
- 连接建立：连接“传输分帧”的接口合同与运行证据。
- 套接字选项：连接“处理半关闭”的接口合同与运行证据。
- 本页14个正式目录层级全部进入可重放实验链。

## 术语表

<Glossary>
  <GlossaryItem term="套接字描述符">
    套接字描述符用于解释“解析地址”，需要同时标出标准版本、接口合同、对象所有权、首错和清理方法。
  </GlossaryItem>
  <GlossaryItem term="字节序">
    字节序用于解释“创建套接字”，需要同时标出标准版本、接口合同、对象所有权、首错和清理方法。
  </GlossaryItem>
  <GlossaryItem term="地址查询">
    地址查询用于解释“绑定或连接”，需要同时标出标准版本、接口合同、对象所有权、首错和清理方法。
  </GlossaryItem>
  <GlossaryItem term="连接建立">
    连接建立用于解释“传输分帧”，需要同时标出标准版本、接口合同、对象所有权、首错和清理方法。
  </GlossaryItem>
  <GlossaryItem term="套接字选项">
    套接字选项用于解释“处理半关闭”，需要同时标出标准版本、接口合同、对象所有权、首错和清理方法。
  </GlossaryItem>
</Glossary>

<Attribution
  adaptedFrom="W. Richard Stevens、Stephen A. Rago著，戚正伟、张亚英、尤晋元译《UNIX环境高级编程（第3版）》，人民邮电出版社，2014-06，812页，ISBN 9787115352118 · 完整中文目录与Pearson原版目录的独立教学重构"
  adaptedUrl="https://www.cnblogs.com/china-pub/p/3782412.html"
/>
