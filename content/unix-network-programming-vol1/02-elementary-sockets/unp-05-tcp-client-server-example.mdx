---
title: "第5章 TCP客户/服务器程序示例"
type: C
section: "基本套接字编程"
order: 5
description: "第5章 TCP客户/服务器程序示例围绕TCP回射服务器程序：main函数、TCP回射服务器程序：str_echo函数、TCP回射客户程序：main函数、TCP回射客户程序：str_cli函数、正常启动、正常终止建立从套接字接口到分组、内核状态与清理结果的证据链。 覆盖20个正式目录层级。"
demo: true
math: false
sourceUrl: "https://www.informit.com/store/unix-network-programming-volume-1-the-sockets-networking-9780131411555"
draft: true
qualityVersion: 2
practiceMode: code
sourceMode: independent-rewrite
---

import {
  Unp05TcpClientServerExampleMapLab,
  Unp05TcpClientServerExampleExperimentLab,
  Unp05TcpClientServerExampleEvidenceLab,
} from "@/components/mdx/unix-network-programming-vol1/diagrams/unp-05-tcp-client-server-example";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第5章 TCP客户/服务器程序示例的协议边界、套接字对象、状态机和平台版本限制
- 能描述“解析监听地址 → 创建绑定监听 → 接受连接 → 传输字节流 → 处理半关闭 → 回收连接”的地址、返回值、errno、系统调用与分组证据
- 能实现一个隔离、单变量、可清理、可重放的UNIX网络实验
- 能比较基线、边界、故障和恢复轨迹并判断“监听与已连接套接字职责分离，短读写、SIGPIPE、半关闭、子进程和TIME_WAIT都可解释。”是否成立

</Objectives>

## 为什么同时看API、内核状态与分组

<Term def="TCP回射服务器程序：main函数连接解析监听地址阶段的接口合同、套接字状态、分组轨迹和清理证据。">TCP回射服务器程序：main函数</Term>、<Term def="TCP回射服务器程序：str_echo函数连接创建绑定监听阶段的接口合同、套接字状态、分组轨迹和清理证据。">TCP回射服务器程序：str_echo函数</Term>、<Term def="TCP回射客户程序：main函数连接接受连接阶段的接口合同、套接字状态、分组轨迹和清理证据。">TCP回射客户程序：main函数</Term>、<Term def="TCP回射客户程序：str_cli函数连接传输字节流阶段的接口合同、套接字状态、分组轨迹和清理证据。">TCP回射客户程序：str_cli函数</Term>、<Term def="正常启动连接处理半关闭阶段的接口合同、套接字状态、分组轨迹和清理证据。">正常启动</Term>
共同构成本页坐标。第5章
TCP客户/服务器程序示例围绕TCP回射服务器程序：main函数、TCP回射服务器程序：str_echo函数、TCP回射客户程序：main函数、TCP回射客户程序：str_cli函数、正常启动、正常终止建立从套接字接口到分组、内核状态与清理结果的证据链。

在“第5章 TCP客户/服务器程序示例”中，第3版覆盖IPv4、IPv6、TCP、UDP、SCTP、路由与密钥管理套接字，并保留STREAMS等历史接口。正文先还原书中标准和测试平台语境；Linux专有工具只用于观测与现代对照，不能冒充POSIX或协议标准保证。SCTP、STREAMS、T/TCP和历史IPv6 API的可用性必须由当前平台实测确认。

“第5章 TCP客户/服务器程序示例”未取得未获授权的完整中文正文；以 [InformIT 第三版官方目录与合法样章](https://www.informit.com/store/unix-network-programming-volume-1-the-sockets-networking-9780131411555) 界定 3 部分、31 章和 5 个附录范围，中文解释、代码、实验与练习均为独立教学重写。“第5章 TCP客户/服务器程序示例”的接口与协议事实分别以 [POSIX socket](https://pubs.opengroup.org/onlinepubs/9799919799/functions/socket.html)、[RFC 9293 TCP](https://www.rfc-editor.org/rfc/rfc9293.html)、[RFC 768 UDP](https://www.rfc-editor.org/rfc/rfc768.html)、[RFC 9260 SCTP](https://www.rfc-editor.org/rfc/rfc9260.html)、[RFC 6458 SCTP sockets](https://www.rfc-editor.org/rfc/rfc6458.html) 和 [RFC 8200 IPv6](https://www.rfc-editor.org/rfc/rfc8200.html) 核对；现代差异不覆盖第三版历史语境。

## 六阶段网络实验链

### 1. 解析监听地址

围绕TCP回射服务器程序：main函数记录内核、命名空间、接口、地址、路由、端口、描述符、返回值、errno和预期分组。进入“创建绑定监听”前保存第一条偏离，清理与恢复同样纳入结果。

### 2. 创建绑定监听

围绕TCP回射服务器程序：str_echo函数记录内核、命名空间、接口、地址、路由、端口、描述符、返回值、errno和预期分组。进入“接受连接”前保存第一条偏离，清理与恢复同样纳入结果。

### 3. 接受连接

围绕TCP回射客户程序：main函数记录内核、命名空间、接口、地址、路由、端口、描述符、返回值、errno和预期分组。进入“传输字节流”前保存第一条偏离，清理与恢复同样纳入结果。

### 4. 传输字节流

围绕TCP回射客户程序：str_cli函数记录内核、命名空间、接口、地址、路由、端口、描述符、返回值、errno和预期分组。进入“处理半关闭”前保存第一条偏离，清理与恢复同样纳入结果。

### 5. 处理半关闭

围绕正常启动记录内核、命名空间、接口、地址、路由、端口、描述符、返回值、errno和预期分组。进入“回收连接”前保存第一条偏离，清理与恢复同样纳入结果。

### 6. 回收连接

围绕TCP回射服务器程序：main函数记录内核、命名空间、接口、地址、路由、端口、描述符、返回值、errno和预期分组。进入“解析监听地址”前保存第一条偏离，清理与恢复同样纳入结果。

## 协议、对象与故障模型

第5章 TCP客户/服务器程序示例围绕TCP回射服务器程序：main函数、TCP回射服务器程序：str_echo函数、TCP回射客户程序：main函数、TCP回射客户程序：str_cli函数、正常启动、正常终止建立从套接字接口到分组、内核状态与清理结果的证据链。

本页核心不变量是：**监听与已连接套接字职责分离，短读写、SIGPIPE、半关闭、子进程和TIME_WAIT都可解释。** 它同时约束正常、边界、故障和恢复样本。应用函数返回成功只是一条证据，还要检查地址长度、字节序、短计数、EINTR/EAGAIN、套接字状态、内核队列、分组顺序与异步清理。

“第5章 TCP客户/服务器程序示例”的“第5章 TCP客户/服务器程序示例”的实验台账至少包含内核/库版本、命名空间、接口与路由、地址端口、入口参数、描述符所有者、预期状态、实际返回与errno、首个分组分叉、内核队列和清理结果十一列。性能样本还固定并发度、连接复用、预热、输入规模、采样窗口和延迟分位；抓包必须记录过滤器、snaplen与时间基准。

## 可重放命令骨架

```c
int fd = socket(AF_INET6, SOCK_STREAM, 0);
if (fd < 0) err_sys("socket");
```

```bash
strace -f -e socket,bind,listen,accept,connect,read,write,shutdown ./server
```

```bash
ss -ntap
tcpdump -ni lo 'tcp[tcpflags] != 0'
```

<Callout type="trap">
  **安全边界：**
  “第5章 TCP客户/服务器程序示例”涉及的“第5章 TCP客户/服务器程序示例”涉及的原始套接字、路由、密钥管理、广播多播和故障注入只在隔离网络命名空间、容器或可恢复虚拟机执行。先保存原始接口、路由和流量规则。
</Callout>

## 第三版机制逐项深读

### 第5章 TCP客户/服务器程序示例

在“第5章 TCP客户/服务器程序示例”中，分析“第5章 TCP客户/服务器程序示例”同时对齐 connect/accept/read/write 返回、内核状态与 SYN、ACK、FIN、RST 分组，最终文本不能替代中间证据。

### 5.1 概述

在“第5章 TCP客户/服务器程序示例”中，“5.1 概述”是结构节点，用来组织“第5章 TCP客户/服务器程序示例围绕TCP回射服务器程序：main函数、TCP回射服务器程序：str_echo函数、TCP回射客户程序：main函数、TCP回射客户程序：str_cli函数、正常启动、正常终止建立从套接字接口到分组、内核状态与清理结果的证据链。 覆盖20个正式目录层级。”的接口、内核与分组证据；若只能复述标题而不能给出返回值或状态因果，本节点不通过。

### 5.2 TCP回射服务器程序：main函数

在“第5章 TCP客户/服务器程序示例”中，验证“5.2 TCP回射服务器程序：main函数”只改变一项关闭、复位、缓冲或并发条件，定位首个 errno、短计数、状态转换和重传差异。

### 5.3 TCP回射服务器程序：str_echo函数

在“第5章 TCP客户/服务器程序示例”中，分析“5.3 TCP回射服务器程序：str_echo函数”同时对齐 connect/accept/read/write 返回、内核状态与 SYN、ACK、FIN、RST 分组，最终文本不能替代中间证据。

### 5.4 TCP回射客户程序：main函数

在“第5章 TCP客户/服务器程序示例”中，“5.4 TCP回射客户程序：main函数”的资源所有权跨监听端点、已连接端点、父子进程或线程；每条异常路径都要证明描述符最终关闭且可重新绑定。

### 5.5 TCP回射客户程序：str_cli函数

在“第5章 TCP客户/服务器程序示例”中，“5.5 TCP回射客户程序：str_cli函数”的资源所有权跨监听端点、已连接端点、父子进程或线程；每条异常路径都要证明描述符最终关闭且可重新绑定。

### 5.6 正常启动

在“第5章 TCP客户/服务器程序示例”中，“5.6 正常启动”的交接证据包含可编译代码、系统调用轨迹、端点状态、pcap 与清理日志；缺少任一层时要明确结论边界。

### 5.7 正常终止

在“第5章 TCP客户/服务器程序示例”中，“5.7 正常终止”区分释放描述符引用与关闭通信方向；shutdown 可表达半关闭，close 受引用计数影响，线上 FIN、RST 与 TIME_WAIT 要同应用返回一起解释。

### 5.8 POSIX信号处理

在“第5章 TCP客户/服务器程序示例”中，“5.8 POSIX信号处理”是异步通知而非工作队列；处理函数只执行异步信号安全操作，主循环通过标志或自管道消费状态，并处理信号合并与竞态。

### 5.9 处理SIGCHLD信号

在“第5章 TCP客户/服务器程序示例”中，“5.9 处理SIGCHLD信号”分配并发执行与描述符所有权；fork 复制引用、exec 保留未设 close-on-exec 的描述符，线程则共享进程描述符表。

### 5.10 wait和waitpid函数

在“第5章 TCP客户/服务器程序示例”中，“5.10 wait和waitpid函数”分配并发执行与描述符所有权；fork 复制引用、exec 保留未设 close-on-exec 的描述符，线程则共享进程描述符表。

### 5.11 accept返回前连接中止

在“第5章 TCP客户/服务器程序示例”中，验证“5.11 accept返回前连接中止”只改变一项关闭、复位、缓冲或并发条件，定位首个 errno、短计数、状态转换和重传差异。

### 5.12 服务器进程终止

在“第5章 TCP客户/服务器程序示例”中，“5.12 服务器进程终止”服务于“第5章 TCP客户/服务器程序示例围绕TCP回射服务器程序：main函数、TCP回射服务器程序：str_echo函数、TCP回射客户程序：main函数、TCP回射客户程序：str_cli函数、正常启动、正常终止建立从套接字接口到分组、内核状态与清理结果的证据链。 覆盖20个正式目录层级。”。解释时必须写清输入、系统调用合同、内核对象、线上分组与清理结果，并用一个单变量反例定位首个分叉。

### 5.13 SIGPIPE信号

在“第5章 TCP客户/服务器程序示例”中，“5.13 SIGPIPE信号”建立可靠有序字节流，握手、序号、确认、流量/拥塞控制和双向关闭共同决定状态；应用层必须自行恢复消息边界。

### 5.14 服务器主机崩溃

在“第5章 TCP客户/服务器程序示例”中，“5.14 服务器主机崩溃”的交接证据包含可编译代码、系统调用轨迹、端点状态、pcap 与清理日志；缺少任一层时要明确结论边界。

### 5.15 服务器主机崩溃后重启

在“第5章 TCP客户/服务器程序示例”中，验证“5.15 服务器主机崩溃后重启”固定平台、地址与输入，只改变一个返回条件或网络事件，保存首个 errno、队列变化和分组差异。

### 5.16 服务器主机关机

在“第5章 TCP客户/服务器程序示例”中，验证“5.16 服务器主机关机”固定平台、地址与输入，只改变一个返回条件或网络事件，保存首个 errno、队列变化和分组差异。

### 5.17 TCP程序例子小结

在“第5章 TCP客户/服务器程序示例”中，分析“5.17 TCP程序例子小结”同时对齐 connect/accept/read/write 返回、内核状态与 SYN、ACK、FIN、RST 分组，最终文本不能替代中间证据。

### 5.18 数据格式

在“第5章 TCP客户/服务器程序示例”中，分析“5.18 数据格式”要把应用调用、描述符所有权、内核状态与线上分组放在同一时间轴；最终输出只能证明终点，不能证明中间因果。

### 5.19 小结

在“第5章 TCP客户/服务器程序示例”中，“5.19 小结”是结构节点，用来组织“第5章 TCP客户/服务器程序示例围绕TCP回射服务器程序：main函数、TCP回射服务器程序：str_echo函数、TCP回射客户程序：main函数、TCP回射客户程序：str_cli函数、正常启动、正常终止建立从套接字接口到分组、内核状态与清理结果的证据链。 覆盖20个正式目录层级。”的接口、内核与分组证据；若只能复述标题而不能给出返回值或状态因果，本节点不通过。
## 分步视觉验证

先预测地址、套接字状态和分组，再动手试一试；若结果不同，停在第一条API、内核队列或线上分组分叉。

<Stepper>
  <Step title="画端点与状态机">
    先预测TCP回射服务器程序：main函数、TCP回射服务器程序：str_echo函数、TCP回射客户程序：main函数、TCP回射客户程序：str_cli函数、正常启动的所有权、状态转换和分组方向。
    <Unp05TcpClientServerExampleMapLab />
  </Step>
  <Step title="运行单变量网络实验">
    固定命名空间、接口、路由、地址和输入，只切换一个边界或故障。
    <Unp05TcpClientServerExampleExperimentLab />
  </Step>
  <Step title="核对首包与恢复">
    保存原始轨迹，关闭端点并撤销网络设置后用相同输入重放。
    <Unp05TcpClientServerExampleEvidenceLab />
  </Step>
</Stepper>

## 常见误区

<Callout type="trap">
  **误区一：**
  只看应用最终文本，不检查短读写、errno、套接字状态、内核队列与线上分组是否一致。
</Callout>

<Callout type="trap">
  **误区二：**
  在“第5章 TCP客户/服务器程序示例”中，把TCP当消息协议、把UDP当可靠流，或把Linux专有行为当成跨UNIX与协议标准保证，都会直接判错。
</Callout>

## 四类样本与验收

| 样本 | 只改变什么                 | 必查证据              | 通过条件   |
| ---- | -------------------------- | --------------------- | ---------- |
| 基线 | 固定网络、地址与输入       | API、状态、队列与分组 | 与预测一致 |
| 边界 | 缓冲、MTU、并发或时序      | 首个阻塞、截断或拒绝  | 边界可解释 |
| 故障 | 丢包、复位、信号或路径故障 | errno、首包与责任层   | 故障可定位 |
| 恢复 | 关闭、回收、撤销网络设置   | 残留端点与同输入重放  | 回到基线   |

## 练习

<Exercises>

**问题 1** 为什么不能只凭最终输出验收第5章 TCP客户/服务器程序示例？

<Answer>
  “第5章 TCP客户/服务器程序示例”的“第5章 TCP客户/服务器程序示例”的最终输出会隐藏短读写、errno、地址错误、队列积压、重传、分组重排和描述符泄漏。必须同时核对接口合同、套接字生存期、内核状态、首个分组分叉和恢复结果。
</Answer>

**问题 2** 如何把“接受连接”变成网络证据？

<Answer>
  固定内核、命名空间、接口、路由、地址端口和输入，预测TCP回射服务器程序：main函数与TCP回射服务器程序：str_echo函数的返回值、状态和分组，只改变一个条件并停在首错或首包分叉。
</Answer>

**问题 3** 本页最小通过条件是什么？

<Answer>
  监听与已连接套接字职责分离，短读写、SIGPIPE、半关闭、子进程和TIME_WAIT都可解释。
  关闭端点并恢复网络设置后，相同输入必须重新得到基线结果。
</Answer>

</Exercises>

## 小结

- TCP回射服务器程序：main函数：连接“解析监听地址”的接口、状态与分组证据。
- TCP回射服务器程序：str_echo函数：连接“创建绑定监听”的接口、状态与分组证据。
- TCP回射客户程序：main函数：连接“接受连接”的接口、状态与分组证据。
- TCP回射客户程序：str_cli函数：连接“传输字节流”的接口、状态与分组证据。
- 正常启动：连接“处理半关闭”的接口、状态与分组证据。
- 本页20个正式目录层级全部进入可重放网络实验链。

## 术语表

<Glossary>
  <GlossaryItem term="TCP回射服务器程序：main函数">
    TCP回射服务器程序：main函数用于解释“解析监听地址”，需要同时标出协议版本、地址与端点、状态机、首包分叉和清理方法。
  </GlossaryItem>
  <GlossaryItem term="TCP回射服务器程序：str_echo函数">
    TCP回射服务器程序：str_echo函数用于解释“创建绑定监听”，需要同时标出协议版本、地址与端点、状态机、首包分叉和清理方法。
  </GlossaryItem>
  <GlossaryItem term="TCP回射客户程序：main函数">
    TCP回射客户程序：main函数用于解释“接受连接”，需要同时标出协议版本、地址与端点、状态机、首包分叉和清理方法。
  </GlossaryItem>
  <GlossaryItem term="TCP回射客户程序：str_cli函数">
    TCP回射客户程序：str_cli函数用于解释“传输字节流”，需要同时标出协议版本、地址与端点、状态机、首包分叉和清理方法。
  </GlossaryItem>
  <GlossaryItem term="正常启动">
    正常启动用于解释“处理半关闭”，需要同时标出协议版本、地址与端点、状态机、首包分叉和清理方法。
  </GlossaryItem>
</Glossary>

<Attribution
  mode="independent-rewrite"
  sourceBasis="authorized-sample"
  workTitle="W. Richard Stevens, Bill Fenner, Andrew M. Rudoff, UNIX Network Programming, Volume 1: The Sockets Networking API, Third Edition"
  adaptedUrl="https://www.informit.com/store/unix-network-programming-volume-1-the-sockets-networking-9780131411555"
/>
