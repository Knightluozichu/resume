---
title: "第8章 基本UDP套接字编程"
type: C
section: "基本套接字编程"
order: 8
description: "第8章 基本UDP套接字编程围绕recvfrom和sendto函数、UDP回射服务器程序：main函数、UDP回射服务器程序：dg_echo函数、UDP回射客户程序：main函数、UDP回射客户程序：dg_cli函数、数据报的丢失建立从套接字接口到分组、内核状态与清理结果的证据链。 覆盖17个正式目录层级。"
demo: true
math: false
sourceUrl: "https://www.informit.com/store/unix-network-programming-volume-1-the-sockets-networking-9780131411555"
draft: false
qualityVersion: 2
practiceMode: code
sourceMode: independent-rewrite
---

import {
  Unp08ElementaryUdpSocketsMapLab,
  Unp08ElementaryUdpSocketsExperimentLab,
  Unp08ElementaryUdpSocketsEvidenceLab,
} from "@/components/mdx/unix-network-programming-vol1/diagrams/unp-08-elementary-udp-sockets";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第8章 基本UDP套接字编程的协议边界、套接字对象、状态机和平台版本限制
- 能描述“绑定数据报端点 → 发送单个报文 → 核对来源地址 → 检测截断丢失 → 施加流量边界 → 关闭并重放”的地址、返回值、errno、系统调用与分组证据
- 能实现一个隔离、单变量、可清理、可重放的UNIX网络实验
- 能比较基线、边界、故障和恢复轨迹并判断“每个数据报的来源、目的、长度和截断状态明确，丢失、乱序与重复不被误当成字节流语义。”是否成立

</Objectives>

## 为什么同时看API、内核状态与分组

<Term def="recvfrom和sendto函数连接绑定数据报端点阶段的接口合同、套接字状态、分组轨迹和清理证据。">
  recvfrom和sendto函数
</Term>
、
<Term def="UDP回射服务器程序：main函数连接发送单个报文阶段的接口合同、套接字状态、分组轨迹和清理证据。">
  UDP回射服务器程序：main函数
</Term>
、
<Term def="UDP回射服务器程序：dg_echo函数连接核对来源地址阶段的接口合同、套接字状态、分组轨迹和清理证据。">
  UDP回射服务器程序：dg_echo函数
</Term>
、
<Term def="UDP回射客户程序：main函数连接检测截断丢失阶段的接口合同、套接字状态、分组轨迹和清理证据。">
  UDP回射客户程序：main函数
</Term>
、
<Term def="UDP回射客户程序：dg_cli函数连接施加流量边界阶段的接口合同、套接字状态、分组轨迹和清理证据。">
  UDP回射客户程序：dg_cli函数
</Term>
共同构成本页坐标。第8章
基本UDP套接字编程围绕recvfrom和sendto函数、UDP回射服务器程序：main函数、UDP回射服务器程序：dg_echo函数、UDP回射客户程序：main函数、UDP回射客户程序：dg_cli函数、数据报的丢失建立从套接字接口到分组、内核状态与清理结果的证据链。

在“第8章 基本UDP套接字编程”中，第3版覆盖IPv4、IPv6、TCP、UDP、SCTP、路由与密钥管理套接字，并保留STREAMS等历史接口。正文先还原书中标准和测试平台语境；Linux专有工具只用于观测与现代对照，不能冒充POSIX或协议标准保证。SCTP、STREAMS、T/TCP和历史IPv6 API的可用性必须由当前平台实测确认。

“第8章 基本UDP套接字编程”未取得未获授权的完整中文正文；以 [InformIT 第三版官方目录与合法样章](https://www.informit.com/store/unix-network-programming-volume-1-the-sockets-networking-9780131411555) 界定 3 部分、31 章和 5 个附录范围，中文解释、代码、实验与练习均为独立教学重写。“第8章 基本UDP套接字编程”的接口与协议事实分别以 [POSIX socket](https://pubs.opengroup.org/onlinepubs/9799919799/functions/socket.html)、[RFC 9293 TCP](https://www.rfc-editor.org/rfc/rfc9293.html)、[RFC 768 UDP](https://www.rfc-editor.org/rfc/rfc768.html)、[RFC 9260 SCTP](https://www.rfc-editor.org/rfc/rfc9260.html)、[RFC 6458 SCTP sockets](https://www.rfc-editor.org/rfc/rfc6458.html) 和 [RFC 8200 IPv6](https://www.rfc-editor.org/rfc/rfc8200.html) 核对；现代差异不覆盖第三版历史语境。

## 六阶段网络实验链

### 1. 绑定数据报端点

围绕recvfrom和sendto函数记录内核、命名空间、接口、地址、路由、端口、描述符、返回值、errno和预期分组。进入“发送单个报文”前保存第一条偏离，清理与恢复同样纳入结果。

### 2. 发送单个报文

围绕UDP回射服务器程序：main函数记录内核、命名空间、接口、地址、路由、端口、描述符、返回值、errno和预期分组。进入“核对来源地址”前保存第一条偏离，清理与恢复同样纳入结果。

### 3. 核对来源地址

围绕UDP回射服务器程序：dg_echo函数记录内核、命名空间、接口、地址、路由、端口、描述符、返回值、errno和预期分组。进入“检测截断丢失”前保存第一条偏离，清理与恢复同样纳入结果。

### 4. 检测截断丢失

围绕UDP回射客户程序：main函数记录内核、命名空间、接口、地址、路由、端口、描述符、返回值、errno和预期分组。进入“施加流量边界”前保存第一条偏离，清理与恢复同样纳入结果。

### 5. 施加流量边界

围绕UDP回射客户程序：dg_cli函数记录内核、命名空间、接口、地址、路由、端口、描述符、返回值、errno和预期分组。进入“关闭并重放”前保存第一条偏离，清理与恢复同样纳入结果。

### 6. 关闭并重放

围绕recvfrom和sendto函数记录内核、命名空间、接口、地址、路由、端口、描述符、返回值、errno和预期分组。进入“绑定数据报端点”前保存第一条偏离，清理与恢复同样纳入结果。

## 协议、对象与故障模型

第8章 基本UDP套接字编程围绕recvfrom和sendto函数、UDP回射服务器程序：main函数、UDP回射服务器程序：dg_echo函数、UDP回射客户程序：main函数、UDP回射客户程序：dg_cli函数、数据报的丢失建立从套接字接口到分组、内核状态与清理结果的证据链。

本页核心不变量是：**每个数据报的来源、目的、长度和截断状态明确，丢失、乱序与重复不被误当成字节流语义。** 它同时约束正常、边界、故障和恢复样本。应用函数返回成功只是一条证据，还要检查地址长度、字节序、短计数、EINTR/EAGAIN、套接字状态、内核队列、分组顺序与异步清理。

“第8章 基本UDP套接字编程”的“第8章 基本UDP套接字编程”的实验台账至少包含内核/库版本、命名空间、接口与路由、地址端口、入口参数、描述符所有者、预期状态、实际返回与errno、首个分组分叉、内核队列和清理结果十一列。性能样本还固定并发度、连接复用、预热、输入规模、采样窗口和延迟分位；抓包必须记录过滤器、snaplen与时间基准。

## 可重放命令骨架

```c
ssize_t n = recvfrom(fd, buf, sizeof(buf), 0, (SA *)&peer, &len);
if (n < 0) err_sys("recvfrom");
```

```bash
strace -e socket,bind,sendto,recvfrom,connect ./udp-client
```

```bash
tc qdisc add dev lo root netem loss 5% reorder 10%
tc qdisc del dev lo root
```

<Callout type="trap">
  **安全边界：**
  “第8章 基本UDP套接字编程”涉及的“第8章 基本UDP套接字编程”涉及的原始套接字、路由、密钥管理、广播多播和故障注入只在隔离网络命名空间、容器或可恢复虚拟机执行。先保存原始接口、路由和流量规则。
</Callout>

## 第三版机制逐项深读

### 第8章 基本UDP套接字编程

在“第8章 基本UDP套接字编程”中，“第8章 基本UDP套接字编程”的可靠性若由应用补充，必须同时处理请求标识、重传、重复抑制、乱序与拥塞，不能只加一个无限重试循环。

### 8.1 概述

在“第8章 基本UDP套接字编程”中，“8.1 概述”是结构节点，用来组织“第8章 基本UDP套接字编程围绕recvfrom和sendto函数、UDP回射服务器程序：main函数、UDP回射服务器程序：dg_echo函数、UDP回射客户程序：main函数、UDP回射客户程序：dg_cli函数、数据报的丢失建立从套接字接口到分组、内核状态与清理结果的证据链。 覆盖17个正式目录层级。”的接口、内核与分组证据；若只能复述标题而不能给出返回值或状态因果，本节点不通过。

### 8.2 recvfrom和sendto函数

在“第8章 基本UDP套接字编程”中，“8.2 recvfrom和sendto函数”在描述符上搬运字节或数据报，并通过标志、地址和返回计数暴露边界；流没有消息边界，数据报则可能整报截断。

### 8.3 UDP回射服务器程序：main函数

在“第8章 基本UDP套接字编程”中，“8.3 UDP回射服务器程序：main函数”使用无连接数据报语义：每次发送保留一条消息边界，但协议不保证送达、顺序或去重，应用必须定义超时与重试责任。

### 8.4 UDP回射服务器程序：dg_echo函数

在“第8章 基本UDP套接字编程”中，分析“8.4 UDP回射服务器程序：dg_echo函数”要保存源/目的地址、数据报长度、校验和与丢失/重复样本；connect 只固定默认对端并让异步错误更易关联。

### 8.5 UDP回射客户程序：main函数

在“第8章 基本UDP套接字编程”中，分析“8.5 UDP回射客户程序：main函数”要保存源/目的地址、数据报长度、校验和与丢失/重复样本；connect 只固定默认对端并让异步错误更易关联。

### 8.6 UDP回射客户程序：dg_cli函数

在“第8章 基本UDP套接字编程”中，“8.6 UDP回射客户程序：dg_cli函数”的可靠性若由应用补充，必须同时处理请求标识、重传、重复抑制、乱序与拥塞，不能只加一个无限重试循环。

### 8.7 数据报的丢失

在“第8章 基本UDP套接字编程”中，“8.7 数据报的丢失”的可靠性若由应用补充，必须同时处理请求标识、重传、重复抑制、乱序与拥塞，不能只加一个无限重试循环。

### 8.8 验证接收到的响应

在“第8章 基本UDP套接字编程”中，“8.8 验证接收到的响应”的交接证据包含可编译代码、系统调用轨迹、端点状态、pcap 与清理日志；缺少任一层时要明确结论边界。

### 8.9 服务器进程未运行

在“第8章 基本UDP套接字编程”中，分析“8.9 服务器进程未运行”要把应用调用、描述符所有权、内核状态与线上分组放在同一时间轴；最终输出只能证明终点，不能证明中间因果。

### 8.10 UDP程序例子小结

在“第8章 基本UDP套接字编程”中，“8.10 UDP程序例子小结”的可靠性若由应用补充，必须同时处理请求标识、重传、重复抑制、乱序与拥塞，不能只加一个无限重试循环。

### 8.11 UDP的connect函数

在“第8章 基本UDP套接字编程”中，“8.11 UDP的connect函数”为流式端点发起连接；阻塞调用等待完成，非阻塞调用常以 EINPROGRESS 返回，随后必须用可写事件和 SO_ERROR 判定真实结果。

### 8.12 dg_cli函数（修订版）

在“第8章 基本UDP套接字编程”中，分析“8.12 dg_cli函数（修订版）”要把应用调用、描述符所有权、内核状态与线上分组放在同一时间轴；最终输出只能证明终点，不能证明中间因果。

### 8.13 UDP缺乏流量控制

在“第8章 基本UDP套接字编程”中，分析“8.13 UDP缺乏流量控制”要保存源/目的地址、数据报长度、校验和与丢失/重复样本；connect 只固定默认对端并让异步错误更易关联。

### 8.14 UDP中的外出接口的确定

在“第8章 基本UDP套接字编程”中，分析“8.14 UDP中的外出接口的确定”要保存源/目的地址、数据报长度、校验和与丢失/重复样本；connect 只固定默认对端并让异步错误更易关联。

### 8.15 使用select函数的TCP和UDP回射服务器程序

在“第8章 基本UDP套接字编程”中，“8.15 使用select函数的TCP和UDP回射服务器程序”把多个描述符的就绪状态交给一个执行流管理；就绪只保证调用不会按原条件阻塞，返回后仍需处理短计数、EOF、错误和集合重建。

### 8.16 小结

在“第8章 基本UDP套接字编程”中，“8.16 小结”是结构节点，用来组织“第8章 基本UDP套接字编程围绕recvfrom和sendto函数、UDP回射服务器程序：main函数、UDP回射服务器程序：dg_echo函数、UDP回射客户程序：main函数、UDP回射客户程序：dg_cli函数、数据报的丢失建立从套接字接口到分组、内核状态与清理结果的证据链。 覆盖17个正式目录层级。”的接口、内核与分组证据；若只能复述标题而不能给出返回值或状态因果，本节点不通过。
## 分步视觉验证

先预测地址、套接字状态和分组，再动手试一试；若结果不同，停在第一条API、内核队列或线上分组分叉。

<Stepper>
  <Step title="画端点与状态机">
    先预测recvfrom和sendto函数、UDP回射服务器程序：main函数、UDP回射服务器程序：dg_echo函数、UDP回射客户程序：main函数、UDP回射客户程序：dg_cli函数的所有权、状态转换和分组方向。
    <Unp08ElementaryUdpSocketsMapLab />
  </Step>
  <Step title="运行单变量网络实验">
    固定命名空间、接口、路由、地址和输入，只切换一个边界或故障。
    <Unp08ElementaryUdpSocketsExperimentLab />
  </Step>
  <Step title="核对首包与恢复">
    保存原始轨迹，关闭端点并撤销网络设置后用相同输入重放。
    <Unp08ElementaryUdpSocketsEvidenceLab />
  </Step>
</Stepper>

## 常见误区

<Callout type="trap">
  **误区一：**
  只看应用最终文本，不检查短读写、errno、套接字状态、内核队列与线上分组是否一致。
</Callout>

<Callout type="trap">
  **误区二：**
  在“第8章 基本UDP套接字编程”中，把TCP当消息协议、把UDP当可靠流，或把Linux专有行为当成跨UNIX与协议标准保证，都会直接判错。
</Callout>

## 四类样本与验收

| 样本 | 只改变什么                 | 必查证据              | 通过条件   |
| ---- | -------------------------- | --------------------- | ---------- |
| 基线 | 固定网络、地址与输入       | API、状态、队列与分组 | 与预测一致 |
| 边界 | 缓冲、MTU、并发或时序      | 首个阻塞、截断或拒绝  | 边界可解释 |
| 故障 | 丢包、复位、信号或路径故障 | errno、首包与责任层   | 故障可定位 |
| 恢复 | 关闭、回收、撤销网络设置   | 残留端点与同输入重放  | 回到基线   |

## 练习

<Exercises>

**问题 1** 为什么不能只凭最终输出验收第8章 基本UDP套接字编程？

<Answer>
  “第8章 基本UDP套接字编程”的“第8章 基本UDP套接字编程”的最终输出会隐藏短读写、errno、地址错误、队列积压、重传、分组重排和描述符泄漏。必须同时核对接口合同、套接字生存期、内核状态、首个分组分叉和恢复结果。
</Answer>

**问题 2** 如何把“核对来源地址”变成网络证据？

<Answer>
  固定内核、命名空间、接口、路由、地址端口和输入，预测recvfrom和sendto函数与UDP回射服务器程序：main函数的返回值、状态和分组，只改变一个条件并停在首错或首包分叉。
</Answer>

**问题 3** 本页最小通过条件是什么？

<Answer>
  每个数据报的来源、目的、长度和截断状态明确，丢失、乱序与重复不被误当成字节流语义。
  关闭端点并恢复网络设置后，相同输入必须重新得到基线结果。
</Answer>

</Exercises>

## 小结

- recvfrom和sendto函数：连接“绑定数据报端点”的接口、状态与分组证据。
- UDP回射服务器程序：main函数：连接“发送单个报文”的接口、状态与分组证据。
- UDP回射服务器程序：dg_echo函数：连接“核对来源地址”的接口、状态与分组证据。
- UDP回射客户程序：main函数：连接“检测截断丢失”的接口、状态与分组证据。
- UDP回射客户程序：dg_cli函数：连接“施加流量边界”的接口、状态与分组证据。
- 本页17个正式目录层级全部进入可重放网络实验链。

## 术语表

<Glossary>
  <GlossaryItem term="recvfrom和sendto函数">
    recvfrom和sendto函数用于解释“绑定数据报端点”，需要同时标出协议版本、地址与端点、状态机、首包分叉和清理方法。
  </GlossaryItem>
  <GlossaryItem term="UDP回射服务器程序：main函数">
    UDP回射服务器程序：main函数用于解释“发送单个报文”，需要同时标出协议版本、地址与端点、状态机、首包分叉和清理方法。
  </GlossaryItem>
  <GlossaryItem term="UDP回射服务器程序：dg_echo函数">
    UDP回射服务器程序：dg_echo函数用于解释“核对来源地址”，需要同时标出协议版本、地址与端点、状态机、首包分叉和清理方法。
  </GlossaryItem>
  <GlossaryItem term="UDP回射客户程序：main函数">
    UDP回射客户程序：main函数用于解释“检测截断丢失”，需要同时标出协议版本、地址与端点、状态机、首包分叉和清理方法。
  </GlossaryItem>
  <GlossaryItem term="UDP回射客户程序：dg_cli函数">
    UDP回射客户程序：dg_cli函数用于解释“施加流量边界”，需要同时标出协议版本、地址与端点、状态机、首包分叉和清理方法。
  </GlossaryItem>
</Glossary>

<Attribution
  mode="independent-rewrite"
  sourceBasis="authorized-sample"
  workTitle="W. Richard Stevens, Bill Fenner, Andrew M. Rudoff, UNIX Network Programming, Volume 1: The Sockets Networking API, Third Edition"
  adaptedUrl="https://www.informit.com/store/unix-network-programming-volume-1-the-sockets-networking-9780131411555"
/>
