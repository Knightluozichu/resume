---
title: "第12章 IPv4与IPv6的互操作性"
type: C
section: "高级套接字 · 内核接口"
order: 12
description: "第12章 IPv4与IPv6的互操作性围绕IPv4客户与IPv6服务器、IPv6客户与IPv4服务器、IPv6地址测试宏、源代码可移植性建立从套接字接口到分组、内核状态与清理结果的证据链。 覆盖8个正式目录层级。"
demo: true
math: false
sourceUrl: "https://www.informit.com/store/unix-network-programming-volume-1-the-sockets-networking-9780131411555"
draft: false
qualityVersion: 2
practiceMode: code
sourceMode: independent-rewrite
---

import {
  Unp12Ipv4Ipv6InteroperabilityMapLab,
  Unp12Ipv4Ipv6InteroperabilityExperimentLab,
  Unp12Ipv4Ipv6InteroperabilityEvidenceLab,
} from "@/components/mdx/unix-network-programming-vol1/diagrams/unp-12-ipv4-ipv6-interoperability";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第12章 IPv4与IPv6的互操作性的协议边界、套接字对象、状态机和平台版本限制
- 能描述“解析名字服务 → 遍历候选地址 → 创建匹配套接字 → 尝试连接绑定 → 反向格式化 → 释放地址链”的地址、返回值、errno、系统调用与分组证据
- 能实现一个隔离、单变量、可清理、可重放的UNIX网络实验
- 能比较基线、边界、故障和恢复轨迹并判断“地址族、长度、作用域和字节序与套接字匹配，候选链完整遍历并恰好释放一次。”是否成立

</Objectives>

## 为什么同时看API、内核状态与分组

<Term def="IPv4客户与IPv6服务器连接解析名字服务阶段的接口合同、套接字状态、分组轨迹和清理证据。">IPv4客户与IPv6服务器</Term>、<Term def="IPv6客户与IPv4服务器连接遍历候选地址阶段的接口合同、套接字状态、分组轨迹和清理证据。">IPv6客户与IPv4服务器</Term>、<Term def="IPv6地址测试宏连接创建匹配套接字阶段的接口合同、套接字状态、分组轨迹和清理证据。">IPv6地址测试宏</Term>、<Term def="源代码可移植性连接尝试连接绑定阶段的接口合同、套接字状态、分组轨迹和清理证据。">源代码可移植性</Term>、<Term def="getaddrinfo连接反向格式化阶段的接口合同、套接字状态、分组轨迹和清理证据。">getaddrinfo</Term>
共同构成本页坐标。第12章
IPv4与IPv6的互操作性围绕IPv4客户与IPv6服务器、IPv6客户与IPv4服务器、IPv6地址测试宏、源代码可移植性建立从套接字接口到分组、内核状态与清理结果的证据链。

在“第12章 IPv4与IPv6的互操作性”中，第3版覆盖IPv4、IPv6、TCP、UDP、SCTP、路由与密钥管理套接字，并保留STREAMS等历史接口。正文先还原书中标准和测试平台语境；Linux专有工具只用于观测与现代对照，不能冒充POSIX或协议标准保证。SCTP、STREAMS、T/TCP和历史IPv6 API的可用性必须由当前平台实测确认。

“第12章 IPv4与IPv6的互操作性”未取得未获授权的完整中文正文；以 [InformIT 第三版官方目录与合法样章](https://www.informit.com/store/unix-network-programming-volume-1-the-sockets-networking-9780131411555) 界定 3 部分、31 章和 5 个附录范围，中文解释、代码、实验与练习均为独立教学重写。“第12章 IPv4与IPv6的互操作性”的接口与协议事实分别以 [POSIX socket](https://pubs.opengroup.org/onlinepubs/9799919799/functions/socket.html)、[RFC 9293 TCP](https://www.rfc-editor.org/rfc/rfc9293.html)、[RFC 768 UDP](https://www.rfc-editor.org/rfc/rfc768.html)、[RFC 9260 SCTP](https://www.rfc-editor.org/rfc/rfc9260.html)、[RFC 6458 SCTP sockets](https://www.rfc-editor.org/rfc/rfc6458.html) 和 [RFC 8200 IPv6](https://www.rfc-editor.org/rfc/rfc8200.html) 核对；现代差异不覆盖第三版历史语境。

## 六阶段网络实验链

### 1. 解析名字服务

围绕IPv4客户与IPv6服务器记录内核、命名空间、接口、地址、路由、端口、描述符、返回值、errno和预期分组。进入“遍历候选地址”前保存第一条偏离，清理与恢复同样纳入结果。

### 2. 遍历候选地址

围绕IPv6客户与IPv4服务器记录内核、命名空间、接口、地址、路由、端口、描述符、返回值、errno和预期分组。进入“创建匹配套接字”前保存第一条偏离，清理与恢复同样纳入结果。

### 3. 创建匹配套接字

围绕IPv6地址测试宏记录内核、命名空间、接口、地址、路由、端口、描述符、返回值、errno和预期分组。进入“尝试连接绑定”前保存第一条偏离，清理与恢复同样纳入结果。

### 4. 尝试连接绑定

围绕源代码可移植性记录内核、命名空间、接口、地址、路由、端口、描述符、返回值、errno和预期分组。进入“反向格式化”前保存第一条偏离，清理与恢复同样纳入结果。

### 5. 反向格式化

围绕getaddrinfo记录内核、命名空间、接口、地址、路由、端口、描述符、返回值、errno和预期分组。进入“释放地址链”前保存第一条偏离，清理与恢复同样纳入结果。

### 6. 释放地址链

围绕IPv4客户与IPv6服务器记录内核、命名空间、接口、地址、路由、端口、描述符、返回值、errno和预期分组。进入“解析名字服务”前保存第一条偏离，清理与恢复同样纳入结果。

## 协议、对象与故障模型

第12章 IPv4与IPv6的互操作性围绕IPv4客户与IPv6服务器、IPv6客户与IPv4服务器、IPv6地址测试宏、源代码可移植性建立从套接字接口到分组、内核状态与清理结果的证据链。

本页核心不变量是：**地址族、长度、作用域和字节序与套接字匹配，候选链完整遍历并恰好释放一次。** 它同时约束正常、边界、故障和恢复样本。应用函数返回成功只是一条证据，还要检查地址长度、字节序、短计数、EINTR/EAGAIN、套接字状态、内核队列、分组顺序与异步清理。

“第12章 IPv4与IPv6的互操作性”的“第12章 IPv4与IPv6的互操作性”的实验台账至少包含内核/库版本、命名空间、接口与路由、地址端口、入口参数、描述符所有者、预期状态、实际返回与errno、首个分组分叉、内核队列和清理结果十一列。性能样本还固定并发度、连接复用、预热、输入规模、采样窗口和延迟分位；抓包必须记录过滤器、snaplen与时间基准。

## 可重放命令骨架

```bash
getent ahosts localhost
getent services http
```

```c
int e = getaddrinfo(host, service, &hints, &res);
if (e != 0) err_quit("%s", gai_strerror(e));
```

```bash
strace -e socket,connect,bind ./resolver localhost 80
```

<Callout type="trap">
  **安全边界：**
  “第12章 IPv4与IPv6的互操作性”涉及的“第12章 IPv4与IPv6的互操作性”涉及的原始套接字、路由、密钥管理、广播多播和故障注入只在隔离网络命名空间、容器或可恢复虚拟机执行。先保存原始接口、路由和流量规则。
</Callout>

## 第三版机制逐项深读

### 第三部分 高级套接字编程

在“第12章 IPv4与IPv6的互操作性”中，验证“第三部分 高级套接字编程”固定平台、地址与输入，只改变一个返回条件或网络事件，保存首个 errno、队列变化和分组差异。

### 第12章 IPv4与IPv6的互操作性

在“第12章 IPv4与IPv6的互操作性”中，“第12章 IPv4与IPv6的互操作性”的现代语义以 RFC 8200 等规范核对，但原书中的历史 API 仍按第三版语境说明，不用新名称改写旧接口。

### 12.1 概述

在“第12章 IPv4与IPv6的互操作性”中，“12.1 概述”是结构节点，用来组织“第12章 IPv4与IPv6的互操作性围绕IPv4客户与IPv6服务器、IPv6客户与IPv4服务器、IPv6地址测试宏、源代码可移植性建立从套接字接口到分组、内核状态与清理结果的证据链。 覆盖8个正式目录层级。”的接口、内核与分组证据；若只能复述标题而不能给出返回值或状态因果，本节点不通过。

### 12.2 IPv4客户与IPv6服务器

在“第12章 IPv4与IPv6的互操作性”中，“12.2 IPv4客户与IPv6服务器”明确地址族、首部与路径 MTU 的协议差异；双栈映射和 IPv6 扩展首部的可用性不能靠结构体外观猜测。

### 12.3 IPv6客户与IPv4服务器

在“第12章 IPv4与IPv6的互操作性”中，分析“12.3 IPv6客户与IPv4服务器”要保存地址、作用域、接口索引、首部链与内核选项，区分源代码可移植和运行时互操作。

### 12.4 IPv6地址测试宏

在“第12章 IPv4与IPv6的互操作性”中，分析“12.4 IPv6地址测试宏”要保存地址、作用域、接口索引、首部链与内核选项，区分源代码可移植和运行时互操作。

### 12.5 源代码可移植性

在“第12章 IPv4与IPv6的互操作性”中，“12.5 源代码可移植性”服务于“第12章 IPv4与IPv6的互操作性围绕IPv4客户与IPv6服务器、IPv6客户与IPv4服务器、IPv6地址测试宏、源代码可移植性建立从套接字接口到分组、内核状态与清理结果的证据链。 覆盖8个正式目录层级。”。解释时必须写清输入、系统调用合同、内核对象、线上分组与清理结果，并用一个单变量反例定位首个分叉。

### 12.6 小结

在“第12章 IPv4与IPv6的互操作性”中，“12.6 小结”是结构节点，用来组织“第12章 IPv4与IPv6的互操作性围绕IPv4客户与IPv6服务器、IPv6客户与IPv4服务器、IPv6地址测试宏、源代码可移植性建立从套接字接口到分组、内核状态与清理结果的证据链。 覆盖8个正式目录层级。”的接口、内核与分组证据；若只能复述标题而不能给出返回值或状态因果，本节点不通过。
## 分步视觉验证

先预测地址、套接字状态和分组，再动手试一试；若结果不同，停在第一条API、内核队列或线上分组分叉。

<Stepper>
  <Step title="画端点与状态机">
    先预测IPv4客户与IPv6服务器、IPv6客户与IPv4服务器、IPv6地址测试宏、源代码可移植性、getaddrinfo的所有权、状态转换和分组方向。
    <Unp12Ipv4Ipv6InteroperabilityMapLab />
  </Step>
  <Step title="运行单变量网络实验">
    固定命名空间、接口、路由、地址和输入，只切换一个边界或故障。
    <Unp12Ipv4Ipv6InteroperabilityExperimentLab />
  </Step>
  <Step title="核对首包与恢复">
    保存原始轨迹，关闭端点并撤销网络设置后用相同输入重放。
    <Unp12Ipv4Ipv6InteroperabilityEvidenceLab />
  </Step>
</Stepper>

## 常见误区

<Callout type="trap">
  **误区一：**
  只看应用最终文本，不检查短读写、errno、套接字状态、内核队列与线上分组是否一致。
</Callout>

<Callout type="trap">
  **误区二：**
  在“第12章 IPv4与IPv6的互操作性”中，把TCP当消息协议、把UDP当可靠流，或把Linux专有行为当成跨UNIX与协议标准保证，都会直接判错。
</Callout>

## 四类样本与验收

| 样本 | 只改变什么                 | 必查证据              | 通过条件   |
| ---- | -------------------------- | --------------------- | ---------- |
| 基线 | 固定网络、地址与输入       | API、状态、队列与分组 | 与预测一致 |
| 边界 | 缓冲、MTU、并发或时序      | 首个阻塞、截断或拒绝  | 边界可解释 |
| 故障 | 丢包、复位、信号或路径故障 | errno、首包与责任层   | 故障可定位 |
| 恢复 | 关闭、回收、撤销网络设置   | 残留端点与同输入重放  | 回到基线   |

## 练习

<Exercises>

**问题 1** 为什么不能只凭最终输出验收第12章 IPv4与IPv6的互操作性？

<Answer>
  “第12章 IPv4与IPv6的互操作性”的“第12章 IPv4与IPv6的互操作性”的最终输出会隐藏短读写、errno、地址错误、队列积压、重传、分组重排和描述符泄漏。必须同时核对接口合同、套接字生存期、内核状态、首个分组分叉和恢复结果。
</Answer>

**问题 2** 如何把“创建匹配套接字”变成网络证据？

<Answer>
  固定内核、命名空间、接口、路由、地址端口和输入，预测IPv4客户与IPv6服务器与IPv6客户与IPv4服务器的返回值、状态和分组，只改变一个条件并停在首错或首包分叉。
</Answer>

**问题 3** 本页最小通过条件是什么？

<Answer>
  地址族、长度、作用域和字节序与套接字匹配，候选链完整遍历并恰好释放一次。
  关闭端点并恢复网络设置后，相同输入必须重新得到基线结果。
</Answer>

</Exercises>

## 小结

- IPv4客户与IPv6服务器：连接“解析名字服务”的接口、状态与分组证据。
- IPv6客户与IPv4服务器：连接“遍历候选地址”的接口、状态与分组证据。
- IPv6地址测试宏：连接“创建匹配套接字”的接口、状态与分组证据。
- 源代码可移植性：连接“尝试连接绑定”的接口、状态与分组证据。
- getaddrinfo：连接“反向格式化”的接口、状态与分组证据。
- 本页8个正式目录层级全部进入可重放网络实验链。

## 术语表

<Glossary>
  <GlossaryItem term="IPv4客户与IPv6服务器">
    IPv4客户与IPv6服务器用于解释“解析名字服务”，需要同时标出协议版本、地址与端点、状态机、首包分叉和清理方法。
  </GlossaryItem>
  <GlossaryItem term="IPv6客户与IPv4服务器">
    IPv6客户与IPv4服务器用于解释“遍历候选地址”，需要同时标出协议版本、地址与端点、状态机、首包分叉和清理方法。
  </GlossaryItem>
  <GlossaryItem term="IPv6地址测试宏">
    IPv6地址测试宏用于解释“创建匹配套接字”，需要同时标出协议版本、地址与端点、状态机、首包分叉和清理方法。
  </GlossaryItem>
  <GlossaryItem term="源代码可移植性">
    源代码可移植性用于解释“尝试连接绑定”，需要同时标出协议版本、地址与端点、状态机、首包分叉和清理方法。
  </GlossaryItem>
  <GlossaryItem term="getaddrinfo">
    getaddrinfo用于解释“反向格式化”，需要同时标出协议版本、地址与端点、状态机、首包分叉和清理方法。
  </GlossaryItem>
</Glossary>

<Attribution
  mode="independent-rewrite"
  sourceBasis="authorized-sample"
  workTitle="W. Richard Stevens, Bill Fenner, Andrew M. Rudoff, UNIX Network Programming, Volume 1: The Sockets Networking API, Third Edition"
  adaptedUrl="https://www.informit.com/store/unix-network-programming-volume-1-the-sockets-networking-9780131411555"
/>
