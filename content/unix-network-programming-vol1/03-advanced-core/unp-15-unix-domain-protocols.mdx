---
title: "第15章 UNIX域协议"
type: C
section: "高级套接字 · 内核接口"
order: 15
description: "第15章 UNIX域协议围绕UNIX域套接字地址结构、socketpair函数、套接字函数、UNIX域字节流客户/服务器程序、UNIX域数据报客户/服务器程序、描述符传递建立从套接字接口到分组、内核状态与清理结果的证据链。 覆盖10个正式目录层级。"
demo: true
math: false
sourceUrl: "https://www.informit.com/store/unix-network-programming-volume-1-the-sockets-networking-9780131411555"
draft: true
qualityVersion: 2
practiceMode: code
sourceMode: independent-rewrite
---

import {
  Unp15UnixDomainProtocolsMapLab,
  Unp15UnixDomainProtocolsExperimentLab,
  Unp15UnixDomainProtocolsEvidenceLab,
} from "@/components/mdx/unix-network-programming-vol1/diagrams/unp-15-unix-domain-protocols";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第15章 UNIX域协议的协议边界、套接字对象、状态机和平台版本限制
- 能描述“解析监听地址 → 创建绑定监听 → 接受连接 → 传输字节流 → 处理半关闭 → 回收连接”的地址、返回值、errno、系统调用与分组证据
- 能实现一个隔离、单变量、可清理、可重放的UNIX网络实验
- 能比较基线、边界、故障和恢复轨迹并判断“监听与已连接套接字职责分离，短读写、SIGPIPE、半关闭、子进程和TIME_WAIT都可解释。”是否成立

</Objectives>

## 为什么同时看API、内核状态与分组

<Term def="UNIX域套接字地址结构连接解析监听地址阶段的接口合同、套接字状态、分组轨迹和清理证据。">UNIX域套接字地址结构</Term>、<Term def="socketpair函数连接创建绑定监听阶段的接口合同、套接字状态、分组轨迹和清理证据。">socketpair函数</Term>、<Term def="套接字函数连接接受连接阶段的接口合同、套接字状态、分组轨迹和清理证据。">套接字函数</Term>、<Term def="UNIX域字节流客户/服务器程序连接传输字节流阶段的接口合同、套接字状态、分组轨迹和清理证据。">UNIX域字节流客户/服务器程序</Term>、<Term def="UNIX域数据报客户/服务器程序连接处理半关闭阶段的接口合同、套接字状态、分组轨迹和清理证据。">UNIX域数据报客户/服务器程序</Term>
共同构成本页坐标。第15章
UNIX域协议围绕UNIX域套接字地址结构、socketpair函数、套接字函数、UNIX域字节流客户/服务器程序、UNIX域数据报客户/服务器程序、描述符传递建立从套接字接口到分组、内核状态与清理结果的证据链。

在“第15章 UNIX域协议”中，第3版覆盖IPv4、IPv6、TCP、UDP、SCTP、路由与密钥管理套接字，并保留STREAMS等历史接口。正文先还原书中标准和测试平台语境；Linux专有工具只用于观测与现代对照，不能冒充POSIX或协议标准保证。SCTP、STREAMS、T/TCP和历史IPv6 API的可用性必须由当前平台实测确认。

“第15章 UNIX域协议”未取得未获授权的完整中文正文；以 [InformIT 第三版官方目录与合法样章](https://www.informit.com/store/unix-network-programming-volume-1-the-sockets-networking-9780131411555) 界定 3 部分、31 章和 5 个附录范围，中文解释、代码、实验与练习均为独立教学重写。“第15章 UNIX域协议”的接口与协议事实分别以 [POSIX socket](https://pubs.opengroup.org/onlinepubs/9799919799/functions/socket.html)、[RFC 9293 TCP](https://www.rfc-editor.org/rfc/rfc9293.html)、[RFC 768 UDP](https://www.rfc-editor.org/rfc/rfc768.html)、[RFC 9260 SCTP](https://www.rfc-editor.org/rfc/rfc9260.html)、[RFC 6458 SCTP sockets](https://www.rfc-editor.org/rfc/rfc6458.html) 和 [RFC 8200 IPv6](https://www.rfc-editor.org/rfc/rfc8200.html) 核对；现代差异不覆盖第三版历史语境。

## 六阶段网络实验链

### 1. 解析监听地址

围绕UNIX域套接字地址结构记录内核、命名空间、接口、地址、路由、端口、描述符、返回值、errno和预期分组。进入“创建绑定监听”前保存第一条偏离，清理与恢复同样纳入结果。

### 2. 创建绑定监听

围绕socketpair函数记录内核、命名空间、接口、地址、路由、端口、描述符、返回值、errno和预期分组。进入“接受连接”前保存第一条偏离，清理与恢复同样纳入结果。

### 3. 接受连接

围绕套接字函数记录内核、命名空间、接口、地址、路由、端口、描述符、返回值、errno和预期分组。进入“传输字节流”前保存第一条偏离，清理与恢复同样纳入结果。

### 4. 传输字节流

围绕UNIX域字节流客户/服务器程序记录内核、命名空间、接口、地址、路由、端口、描述符、返回值、errno和预期分组。进入“处理半关闭”前保存第一条偏离，清理与恢复同样纳入结果。

### 5. 处理半关闭

围绕UNIX域数据报客户/服务器程序记录内核、命名空间、接口、地址、路由、端口、描述符、返回值、errno和预期分组。进入“回收连接”前保存第一条偏离，清理与恢复同样纳入结果。

### 6. 回收连接

围绕UNIX域套接字地址结构记录内核、命名空间、接口、地址、路由、端口、描述符、返回值、errno和预期分组。进入“解析监听地址”前保存第一条偏离，清理与恢复同样纳入结果。

## 协议、对象与故障模型

第15章 UNIX域协议围绕UNIX域套接字地址结构、socketpair函数、套接字函数、UNIX域字节流客户/服务器程序、UNIX域数据报客户/服务器程序、描述符传递建立从套接字接口到分组、内核状态与清理结果的证据链。

本页核心不变量是：**监听与已连接套接字职责分离，短读写、SIGPIPE、半关闭、子进程和TIME_WAIT都可解释。** 它同时约束正常、边界、故障和恢复样本。应用函数返回成功只是一条证据，还要检查地址长度、字节序、短计数、EINTR/EAGAIN、套接字状态、内核队列、分组顺序与异步清理。

“第15章 UNIX域协议”的“第15章 UNIX域协议”的实验台账至少包含内核/库版本、命名空间、接口与路由、地址端口、入口参数、描述符所有者、预期状态、实际返回与errno、首个分组分叉、内核队列和清理结果十一列。性能样本还固定并发度、连接复用、预热、输入规模、采样窗口和延迟分位；抓包必须记录过滤器、snaplen与时间基准。

## 可重放命令骨架

```c
int fd = socket(AF_INET6, SOCK_STREAM, 0);
if (fd < 0) err_sys("socket");
```

```bash
strace -f -e socket,bind,listen,accept,connect,read,write,shutdown ./server
```

```bash
ss -ntap
tcpdump -ni lo 'tcp[tcpflags] != 0'
```

<Callout type="trap">
  **安全边界：**
  “第15章 UNIX域协议”涉及的“第15章 UNIX域协议”涉及的原始套接字、路由、密钥管理、广播多播和故障注入只在隔离网络命名空间、容器或可恢复虚拟机执行。先保存原始接口、路由和流量规则。
</Callout>

## 第三版机制逐项深读

### 第15章 UNIX域协议

在“第15章 UNIX域协议”中，“第15章 UNIX域协议”在本机内核中提供字节流或数据报 IPC；路径名/抽象地址、SCM_RIGHTS 描述符传递与对端凭证各有独立所有权和安全边界。

### 15.1 概述

在“第15章 UNIX域协议”中，“15.1 概述”是结构节点，用来组织“第15章 UNIX域协议围绕UNIX域套接字地址结构、socketpair函数、套接字函数、UNIX域字节流客户/服务器程序、UNIX域数据报客户/服务器程序、描述符传递建立从套接字接口到分组、内核状态与清理结果的证据链。 覆盖10个正式目录层级。”的接口、内核与分组证据；若只能复述标题而不能给出返回值或状态因果，本节点不通过。

### 15.2 UNIX域套接字地址结构

在“第15章 UNIX域协议”中，“15.2 UNIX域套接字地址结构”决定用户空间结构如何与内核 ABI 交换：多字节整数使用网络字节序，地址长度在进入和返回时含义不同，填充字节不能携带未初始化数据。

### 15.3 socketpair函数

在“第15章 UNIX域协议”中，“15.3 socketpair函数”在本机内核中提供字节流或数据报 IPC；路径名/抽象地址、SCM_RIGHTS 描述符传递与对端凭证各有独立所有权和安全边界。

### 15.4 套接字函数

在“第15章 UNIX域协议”中，“15.4 套接字函数”用 domain、type 与 protocol 创建通信端点，成功返回描述符、失败返回 -1 并设置 errno；创建本身不绑定地址，也不建立连接。

### 15.5 UNIX域字节流客户/服务器程序

在“第15章 UNIX域协议”中，“15.5 UNIX域字节流客户/服务器程序”在本机内核中提供字节流或数据报 IPC；路径名/抽象地址、SCM_RIGHTS 描述符传递与对端凭证各有独立所有权和安全边界。

### 15.6 UNIX域数据报客户/服务器程序

在“第15章 UNIX域协议”中，“15.6 UNIX域数据报客户/服务器程序”使用无连接数据报语义：每次发送保留一条消息边界，但协议不保证送达、顺序或去重，应用必须定义超时与重试责任。

### 15.7 描述符传递

在“第15章 UNIX域协议”中，“15.7 描述符传递”在本机内核中提供字节流或数据报 IPC；路径名/抽象地址、SCM_RIGHTS 描述符传递与对端凭证各有独立所有权和安全边界。

### 15.8 接收发送者的凭证

在“第15章 UNIX域协议”中，“15.8 接收发送者的凭证”在本机内核中提供字节流或数据报 IPC；路径名/抽象地址、SCM_RIGHTS 描述符传递与对端凭证各有独立所有权和安全边界。

### 15.9 小结

在“第15章 UNIX域协议”中，“15.9 小结”是结构节点，用来组织“第15章 UNIX域协议围绕UNIX域套接字地址结构、socketpair函数、套接字函数、UNIX域字节流客户/服务器程序、UNIX域数据报客户/服务器程序、描述符传递建立从套接字接口到分组、内核状态与清理结果的证据链。 覆盖10个正式目录层级。”的接口、内核与分组证据；若只能复述标题而不能给出返回值或状态因果，本节点不通过。
## 分步视觉验证

先预测地址、套接字状态和分组，再动手试一试；若结果不同，停在第一条API、内核队列或线上分组分叉。

<Stepper>
  <Step title="画端点与状态机">
    先预测UNIX域套接字地址结构、socketpair函数、套接字函数、UNIX域字节流客户/服务器程序、UNIX域数据报客户/服务器程序的所有权、状态转换和分组方向。
    <Unp15UnixDomainProtocolsMapLab />
  </Step>
  <Step title="运行单变量网络实验">
    固定命名空间、接口、路由、地址和输入，只切换一个边界或故障。
    <Unp15UnixDomainProtocolsExperimentLab />
  </Step>
  <Step title="核对首包与恢复">
    保存原始轨迹，关闭端点并撤销网络设置后用相同输入重放。
    <Unp15UnixDomainProtocolsEvidenceLab />
  </Step>
</Stepper>

## 常见误区

<Callout type="trap">
  **误区一：**
  只看应用最终文本，不检查短读写、errno、套接字状态、内核队列与线上分组是否一致。
</Callout>

<Callout type="trap">
  **误区二：**
  在“第15章 UNIX域协议”中，把TCP当消息协议、把UDP当可靠流，或把Linux专有行为当成跨UNIX与协议标准保证，都会直接判错。
</Callout>

## 四类样本与验收

| 样本 | 只改变什么                 | 必查证据              | 通过条件   |
| ---- | -------------------------- | --------------------- | ---------- |
| 基线 | 固定网络、地址与输入       | API、状态、队列与分组 | 与预测一致 |
| 边界 | 缓冲、MTU、并发或时序      | 首个阻塞、截断或拒绝  | 边界可解释 |
| 故障 | 丢包、复位、信号或路径故障 | errno、首包与责任层   | 故障可定位 |
| 恢复 | 关闭、回收、撤销网络设置   | 残留端点与同输入重放  | 回到基线   |

## 练习

<Exercises>

**问题 1** 为什么不能只凭最终输出验收第15章 UNIX域协议？

<Answer>
  “第15章 UNIX域协议”的“第15章 UNIX域协议”的最终输出会隐藏短读写、errno、地址错误、队列积压、重传、分组重排和描述符泄漏。必须同时核对接口合同、套接字生存期、内核状态、首个分组分叉和恢复结果。
</Answer>

**问题 2** 如何把“接受连接”变成网络证据？

<Answer>
  固定内核、命名空间、接口、路由、地址端口和输入，预测UNIX域套接字地址结构与socketpair函数的返回值、状态和分组，只改变一个条件并停在首错或首包分叉。
</Answer>

**问题 3** 本页最小通过条件是什么？

<Answer>
  监听与已连接套接字职责分离，短读写、SIGPIPE、半关闭、子进程和TIME_WAIT都可解释。
  关闭端点并恢复网络设置后，相同输入必须重新得到基线结果。
</Answer>

</Exercises>

## 小结

- UNIX域套接字地址结构：连接“解析监听地址”的接口、状态与分组证据。
- socketpair函数：连接“创建绑定监听”的接口、状态与分组证据。
- 套接字函数：连接“接受连接”的接口、状态与分组证据。
- UNIX域字节流客户/服务器程序：连接“传输字节流”的接口、状态与分组证据。
- UNIX域数据报客户/服务器程序：连接“处理半关闭”的接口、状态与分组证据。
- 本页10个正式目录层级全部进入可重放网络实验链。

## 术语表

<Glossary>
  <GlossaryItem term="UNIX域套接字地址结构">
    UNIX域套接字地址结构用于解释“解析监听地址”，需要同时标出协议版本、地址与端点、状态机、首包分叉和清理方法。
  </GlossaryItem>
  <GlossaryItem term="socketpair函数">
    socketpair函数用于解释“创建绑定监听”，需要同时标出协议版本、地址与端点、状态机、首包分叉和清理方法。
  </GlossaryItem>
  <GlossaryItem term="套接字函数">
    套接字函数用于解释“接受连接”，需要同时标出协议版本、地址与端点、状态机、首包分叉和清理方法。
  </GlossaryItem>
  <GlossaryItem term="UNIX域字节流客户/服务器程序">
    UNIX域字节流客户/服务器程序用于解释“传输字节流”，需要同时标出协议版本、地址与端点、状态机、首包分叉和清理方法。
  </GlossaryItem>
  <GlossaryItem term="UNIX域数据报客户/服务器程序">
    UNIX域数据报客户/服务器程序用于解释“处理半关闭”，需要同时标出协议版本、地址与端点、状态机、首包分叉和清理方法。
  </GlossaryItem>
</Glossary>

<Attribution
  mode="independent-rewrite"
  sourceBasis="authorized-sample"
  workTitle="W. Richard Stevens, Bill Fenner, Andrew M. Rudoff, UNIX Network Programming, Volume 1: The Sockets Networking API, Third Edition"
  adaptedUrl="https://www.informit.com/store/unix-network-programming-volume-1-the-sockets-networking-9780131411555"
/>
