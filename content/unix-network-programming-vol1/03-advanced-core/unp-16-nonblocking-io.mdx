---
title: "第16章 非阻塞式I/O"
type: C
section: "高级套接字 · 内核接口"
order: 16
description: "第16章 非阻塞式I/O围绕非阻塞读和写：str_cli函数（修订版）、非阻塞connect、非阻塞connect：时间获取客户程序、非阻塞connect：Web客户程序、非阻塞accept建立从套接字接口到分组、内核状态与清理结果的证据链。 覆盖8个正式目录层级。"
demo: true
math: false
sourceUrl: "https://www.informit.com/store/unix-network-programming-volume-1-the-sockets-networking-9780131411555"
draft: true
qualityVersion: 2
practiceMode: code
sourceMode: independent-rewrite
---

import {
  Unp16NonblockingIoMapLab,
  Unp16NonblockingIoExperimentLab,
  Unp16NonblockingIoEvidenceLab,
} from "@/components/mdx/unix-network-programming-vol1/diagrams/unp-16-nonblocking-io";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第16章 非阻塞式I/O的协议边界、套接字对象、状态机和平台版本限制
- 能描述“列出描述符 → 注册兴趣集合 → 等待就绪 → 推进连接状态 → 处理短计数 → 注销并关闭”的地址、返回值、errno、系统调用与分组证据
- 能实现一个隔离、单变量、可清理、可重放的UNIX网络实验
- 能比较基线、边界、故障和恢复轨迹并判断“就绪、完成和EOF明确区分，每个描述符的读写兴趣、缓冲水位与关闭状态始终一致。”是否成立

</Objectives>

## 为什么同时看API、内核状态与分组

<Term def="非阻塞读和写：str_cli函数（修订版）连接列出描述符阶段的接口合同、套接字状态、分组轨迹和清理证据。">非阻塞读和写：str_cli函数（修订版）</Term>、<Term def="非阻塞connect连接注册兴趣集合阶段的接口合同、套接字状态、分组轨迹和清理证据。">非阻塞connect</Term>、<Term def="非阻塞connect：时间获取客户程序连接等待就绪阶段的接口合同、套接字状态、分组轨迹和清理证据。">非阻塞connect：时间获取客户程序</Term>、<Term def="非阻塞connect：Web客户程序连接推进连接状态阶段的接口合同、套接字状态、分组轨迹和清理证据。">非阻塞connect：Web客户程序</Term>、<Term def="非阻塞accept连接处理短计数阶段的接口合同、套接字状态、分组轨迹和清理证据。">非阻塞accept</Term>
共同构成本页坐标。第16章
非阻塞式I/O围绕非阻塞读和写：str_cli函数（修订版）、非阻塞connect、非阻塞connect：时间获取客户程序、非阻塞connect：Web客户程序、非阻塞accept建立从套接字接口到分组、内核状态与清理结果的证据链。

在“第16章 非阻塞式I/O”中，第3版覆盖IPv4、IPv6、TCP、UDP、SCTP、路由与密钥管理套接字，并保留STREAMS等历史接口。正文先还原书中标准和测试平台语境；Linux专有工具只用于观测与现代对照，不能冒充POSIX或协议标准保证。SCTP、STREAMS、T/TCP和历史IPv6 API的可用性必须由当前平台实测确认。

“第16章 非阻塞式I/O”未取得未获授权的完整中文正文；以 [InformIT 第三版官方目录与合法样章](https://www.informit.com/store/unix-network-programming-volume-1-the-sockets-networking-9780131411555) 界定 3 部分、31 章和 5 个附录范围，中文解释、代码、实验与练习均为独立教学重写。“第16章 非阻塞式I/O”的接口与协议事实分别以 [POSIX socket](https://pubs.opengroup.org/onlinepubs/9799919799/functions/socket.html)、[RFC 9293 TCP](https://www.rfc-editor.org/rfc/rfc9293.html)、[RFC 768 UDP](https://www.rfc-editor.org/rfc/rfc768.html)、[RFC 9260 SCTP](https://www.rfc-editor.org/rfc/rfc9260.html)、[RFC 6458 SCTP sockets](https://www.rfc-editor.org/rfc/rfc6458.html) 和 [RFC 8200 IPv6](https://www.rfc-editor.org/rfc/rfc8200.html) 核对；现代差异不覆盖第三版历史语境。

## 六阶段网络实验链

### 1. 列出描述符

围绕非阻塞读和写：str_cli函数（修订版）记录内核、命名空间、接口、地址、路由、端口、描述符、返回值、errno和预期分组。进入“注册兴趣集合”前保存第一条偏离，清理与恢复同样纳入结果。

### 2. 注册兴趣集合

围绕非阻塞connect记录内核、命名空间、接口、地址、路由、端口、描述符、返回值、errno和预期分组。进入“等待就绪”前保存第一条偏离，清理与恢复同样纳入结果。

### 3. 等待就绪

围绕非阻塞connect：时间获取客户程序记录内核、命名空间、接口、地址、路由、端口、描述符、返回值、errno和预期分组。进入“推进连接状态”前保存第一条偏离，清理与恢复同样纳入结果。

### 4. 推进连接状态

围绕非阻塞connect：Web客户程序记录内核、命名空间、接口、地址、路由、端口、描述符、返回值、errno和预期分组。进入“处理短计数”前保存第一条偏离，清理与恢复同样纳入结果。

### 5. 处理短计数

围绕非阻塞accept记录内核、命名空间、接口、地址、路由、端口、描述符、返回值、errno和预期分组。进入“注销并关闭”前保存第一条偏离，清理与恢复同样纳入结果。

### 6. 注销并关闭

围绕非阻塞读和写：str_cli函数（修订版）记录内核、命名空间、接口、地址、路由、端口、描述符、返回值、errno和预期分组。进入“列出描述符”前保存第一条偏离，清理与恢复同样纳入结果。

## 协议、对象与故障模型

第16章 非阻塞式I/O围绕非阻塞读和写：str_cli函数（修订版）、非阻塞connect、非阻塞connect：时间获取客户程序、非阻塞connect：Web客户程序、非阻塞accept建立从套接字接口到分组、内核状态与清理结果的证据链。

本页核心不变量是：**就绪、完成和EOF明确区分，每个描述符的读写兴趣、缓冲水位与关闭状态始终一致。** 它同时约束正常、边界、故障和恢复样本。应用函数返回成功只是一条证据，还要检查地址长度、字节序、短计数、EINTR/EAGAIN、套接字状态、内核队列、分组顺序与异步清理。

“第16章 非阻塞式I/O”的“第16章 非阻塞式I/O”的实验台账至少包含内核/库版本、命名空间、接口与路由、地址端口、入口参数、描述符所有者、预期状态、实际返回与errno、首个分组分叉、内核队列和清理结果十一列。性能样本还固定并发度、连接复用、预热、输入规模、采样窗口和延迟分位；抓包必须记录过滤器、snaplen与时间基准。

## 可重放命令骨架

```c
int flags = fcntl(fd, F_GETFL);
fcntl(fd, F_SETFL, flags | O_NONBLOCK);
```

```bash
strace -ttT -e select,pselect6,poll,read,write ./server
```

```bash
ss -tin
perf stat -r 5 ./load-client
```

<Callout type="trap">
  **安全边界：**
  “第16章 非阻塞式I/O”涉及的“第16章 非阻塞式I/O”涉及的原始套接字、路由、密钥管理、广播多播和故障注入只在隔离网络命名空间、容器或可恢复虚拟机执行。先保存原始接口、路由和流量规则。
</Callout>

## 第三版机制逐项深读

### 第16章 非阻塞式I/O

在“第16章 非阻塞式I/O”中，“第16章 非阻塞式I/O”把等待责任移给事件循环；EAGAIN 表示当前不能完成，EINPROGRESS 表示连接尚在进行，后续必须从就绪与 SO_ERROR 收束状态。

### 16.1 概述

在“第16章 非阻塞式I/O”中，“16.1 概述”是结构节点，用来组织“第16章 非阻塞式I/O围绕非阻塞读和写：str_cli函数（修订版）、非阻塞connect、非阻塞connect：时间获取客户程序、非阻塞connect：Web客户程序、非阻塞accept建立从套接字接口到分组、内核状态与清理结果的证据链。 覆盖8个正式目录层级。”的接口、内核与分组证据；若只能复述标题而不能给出返回值或状态因果，本节点不通过。

### 16.2 非阻塞读和写：str_cli函数（修订版）

在“第16章 非阻塞式I/O”中，“16.2 非阻塞读和写：str_cli函数（修订版）”在描述符上搬运字节或数据报，并通过标志、地址和返回计数暴露边界；流没有消息边界，数据报则可能整报截断。

### 16.3 非阻塞connect

在“第16章 非阻塞式I/O”中，“16.3 非阻塞connect”为流式端点发起连接；阻塞调用等待完成，非阻塞调用常以 EINPROGRESS 返回，随后必须用可写事件和 SO_ERROR 判定真实结果。

### 16.4 非阻塞connect：时间获取客户程序

在“第16章 非阻塞式I/O”中，“16.4 非阻塞connect：时间获取客户程序”为流式端点发起连接；阻塞调用等待完成，非阻塞调用常以 EINPROGRESS 返回，随后必须用可写事件和 SO_ERROR 判定真实结果。

### 16.5 非阻塞connect：Web客户程序

在“第16章 非阻塞式I/O”中，“16.5 非阻塞connect：Web客户程序”为流式端点发起连接；阻塞调用等待完成，非阻塞调用常以 EINPROGRESS 返回，随后必须用可写事件和 SO_ERROR 判定真实结果。

### 16.6 非阻塞accept

在“第16章 非阻塞式I/O”中，“16.6 非阻塞accept”从已完成连接队列取得一个新的已连接描述符；监听描述符继续接收连接，返回地址长度是值—结果参数，失败时不能泄漏所有权。

### 16.7 小结

在“第16章 非阻塞式I/O”中，“16.7 小结”是结构节点，用来组织“第16章 非阻塞式I/O围绕非阻塞读和写：str_cli函数（修订版）、非阻塞connect、非阻塞connect：时间获取客户程序、非阻塞connect：Web客户程序、非阻塞accept建立从套接字接口到分组、内核状态与清理结果的证据链。 覆盖8个正式目录层级。”的接口、内核与分组证据；若只能复述标题而不能给出返回值或状态因果，本节点不通过。
## 分步视觉验证

先预测地址、套接字状态和分组，再动手试一试；若结果不同，停在第一条API、内核队列或线上分组分叉。

<Stepper>
  <Step title="画端点与状态机">
    先预测非阻塞读和写：str_cli函数（修订版）、非阻塞connect、非阻塞connect：时间获取客户程序、非阻塞connect：Web客户程序、非阻塞accept的所有权、状态转换和分组方向。
    <Unp16NonblockingIoMapLab />
  </Step>
  <Step title="运行单变量网络实验">
    固定命名空间、接口、路由、地址和输入，只切换一个边界或故障。
    <Unp16NonblockingIoExperimentLab />
  </Step>
  <Step title="核对首包与恢复">
    保存原始轨迹，关闭端点并撤销网络设置后用相同输入重放。
    <Unp16NonblockingIoEvidenceLab />
  </Step>
</Stepper>

## 常见误区

<Callout type="trap">
  **误区一：**
  只看应用最终文本，不检查短读写、errno、套接字状态、内核队列与线上分组是否一致。
</Callout>

<Callout type="trap">
  **误区二：**
  在“第16章 非阻塞式I/O”中，把TCP当消息协议、把UDP当可靠流，或把Linux专有行为当成跨UNIX与协议标准保证，都会直接判错。
</Callout>

## 四类样本与验收

| 样本 | 只改变什么                 | 必查证据              | 通过条件   |
| ---- | -------------------------- | --------------------- | ---------- |
| 基线 | 固定网络、地址与输入       | API、状态、队列与分组 | 与预测一致 |
| 边界 | 缓冲、MTU、并发或时序      | 首个阻塞、截断或拒绝  | 边界可解释 |
| 故障 | 丢包、复位、信号或路径故障 | errno、首包与责任层   | 故障可定位 |
| 恢复 | 关闭、回收、撤销网络设置   | 残留端点与同输入重放  | 回到基线   |

## 练习

<Exercises>

**问题 1** 为什么不能只凭最终输出验收第16章 非阻塞式I/O？

<Answer>
  “第16章 非阻塞式I/O”的“第16章 非阻塞式I/O”的最终输出会隐藏短读写、errno、地址错误、队列积压、重传、分组重排和描述符泄漏。必须同时核对接口合同、套接字生存期、内核状态、首个分组分叉和恢复结果。
</Answer>

**问题 2** 如何把“等待就绪”变成网络证据？

<Answer>
  固定内核、命名空间、接口、路由、地址端口和输入，预测非阻塞读和写：str_cli函数（修订版）与非阻塞connect的返回值、状态和分组，只改变一个条件并停在首错或首包分叉。
</Answer>

**问题 3** 本页最小通过条件是什么？

<Answer>
  就绪、完成和EOF明确区分，每个描述符的读写兴趣、缓冲水位与关闭状态始终一致。
  关闭端点并恢复网络设置后，相同输入必须重新得到基线结果。
</Answer>

</Exercises>

## 小结

- 非阻塞读和写：str_cli函数（修订版）：连接“列出描述符”的接口、状态与分组证据。
- 非阻塞connect：连接“注册兴趣集合”的接口、状态与分组证据。
- 非阻塞connect：时间获取客户程序：连接“等待就绪”的接口、状态与分组证据。
- 非阻塞connect：Web客户程序：连接“推进连接状态”的接口、状态与分组证据。
- 非阻塞accept：连接“处理短计数”的接口、状态与分组证据。
- 本页8个正式目录层级全部进入可重放网络实验链。

## 术语表

<Glossary>
  <GlossaryItem term="非阻塞读和写：str_cli函数（修订版）">
    非阻塞读和写：str_cli函数（修订版）用于解释“列出描述符”，需要同时标出协议版本、地址与端点、状态机、首包分叉和清理方法。
  </GlossaryItem>
  <GlossaryItem term="非阻塞connect">
    非阻塞connect用于解释“注册兴趣集合”，需要同时标出协议版本、地址与端点、状态机、首包分叉和清理方法。
  </GlossaryItem>
  <GlossaryItem term="非阻塞connect：时间获取客户程序">
    非阻塞connect：时间获取客户程序用于解释“等待就绪”，需要同时标出协议版本、地址与端点、状态机、首包分叉和清理方法。
  </GlossaryItem>
  <GlossaryItem term="非阻塞connect：Web客户程序">
    非阻塞connect：Web客户程序用于解释“推进连接状态”，需要同时标出协议版本、地址与端点、状态机、首包分叉和清理方法。
  </GlossaryItem>
  <GlossaryItem term="非阻塞accept">
    非阻塞accept用于解释“处理短计数”，需要同时标出协议版本、地址与端点、状态机、首包分叉和清理方法。
  </GlossaryItem>
</Glossary>

<Attribution
  mode="independent-rewrite"
  sourceBasis="authorized-sample"
  workTitle="W. Richard Stevens, Bill Fenner, Andrew M. Rudoff, UNIX Network Programming, Volume 1: The Sockets Networking API, Third Edition"
  adaptedUrl="https://www.informit.com/store/unix-network-programming-volume-1-the-sockets-networking-9780131411555"
/>
