---
title: "第20章 广播"
type: C
section: "广播多播与高级传输"
order: 20
description: "第20章 广播围绕广播地址、单播和广播的比较、使用广播的dg_cli函数、竞争状态建立从套接字接口到分组、内核状态与清理结果的证据链。 覆盖7个正式目录层级。"
demo: true
math: false
sourceUrl: "https://www.informit.com/store/unix-network-programming-volume-1-the-sockets-networking-9780131411555"
draft: false
qualityVersion: 2
practiceMode: code
sourceMode: independent-rewrite
---

import {
  Unp20BroadcastingMapLab,
  Unp20BroadcastingExperimentLab,
  Unp20BroadcastingEvidenceLab,
} from "@/components/mdx/unix-network-programming-vol1/diagrams/unp-20-broadcasting";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第20章 广播的协议边界、套接字对象、状态机和平台版本限制
- 能描述“选择接口地址 → 配置广播多播 → 加入接收组 → 发送数据报 → 观察复制范围 → 离组并关闭”的地址、返回值、errno、系统调用与分组证据
- 能实现一个隔离、单变量、可清理、可重放的UNIX网络实验
- 能比较基线、边界、故障和恢复轨迹并判断“发送接口、TTL/跳数、组成员和接收范围明确，广播竞争与多播离组不会留下隐藏成员状态。”是否成立

</Objectives>

## 为什么同时看API、内核状态与分组

<Term def="广播地址连接选择接口地址阶段的接口合同、套接字状态、分组轨迹和清理证据。">
  广播地址
</Term>
、
<Term def="单播和广播的比较连接配置广播多播阶段的接口合同、套接字状态、分组轨迹和清理证据。">
  单播和广播的比较
</Term>
、
<Term def="使用广播的dg_cli函数连接加入接收组阶段的接口合同、套接字状态、分组轨迹和清理证据。">
  使用广播的dg_cli函数
</Term>
、
<Term def="竞争状态连接发送数据报阶段的接口合同、套接字状态、分组轨迹和清理证据。">
  竞争状态
</Term>
、
<Term def="广播连接观察复制范围阶段的接口合同、套接字状态、分组轨迹和清理证据。">
  广播
</Term>
共同构成本页坐标。第20章
广播围绕广播地址、单播和广播的比较、使用广播的dg_cli函数、竞争状态建立从套接字接口到分组、内核状态与清理结果的证据链。

在“第20章 广播”中，第3版覆盖IPv4、IPv6、TCP、UDP、SCTP、路由与密钥管理套接字，并保留STREAMS等历史接口。正文先还原书中标准和测试平台语境；Linux专有工具只用于观测与现代对照，不能冒充POSIX或协议标准保证。SCTP、STREAMS、T/TCP和历史IPv6 API的可用性必须由当前平台实测确认。

“第20章 广播”未取得未获授权的完整中文正文；以 [InformIT 第三版官方目录与合法样章](https://www.informit.com/store/unix-network-programming-volume-1-the-sockets-networking-9780131411555) 界定 3 部分、31 章和 5 个附录范围，中文解释、代码、实验与练习均为独立教学重写。“第20章 广播”的接口与协议事实分别以 [POSIX socket](https://pubs.opengroup.org/onlinepubs/9799919799/functions/socket.html)、[RFC 9293 TCP](https://www.rfc-editor.org/rfc/rfc9293.html)、[RFC 768 UDP](https://www.rfc-editor.org/rfc/rfc768.html)、[RFC 9260 SCTP](https://www.rfc-editor.org/rfc/rfc9260.html)、[RFC 6458 SCTP sockets](https://www.rfc-editor.org/rfc/rfc6458.html) 和 [RFC 8200 IPv6](https://www.rfc-editor.org/rfc/rfc8200.html) 核对；现代差异不覆盖第三版历史语境。

## 六阶段网络实验链

### 1. 选择接口地址

围绕广播地址记录内核、命名空间、接口、地址、路由、端口、描述符、返回值、errno和预期分组。进入“配置广播多播”前保存第一条偏离，清理与恢复同样纳入结果。

### 2. 配置广播多播

围绕单播和广播的比较记录内核、命名空间、接口、地址、路由、端口、描述符、返回值、errno和预期分组。进入“加入接收组”前保存第一条偏离，清理与恢复同样纳入结果。

### 3. 加入接收组

围绕使用广播的dg_cli函数记录内核、命名空间、接口、地址、路由、端口、描述符、返回值、errno和预期分组。进入“发送数据报”前保存第一条偏离，清理与恢复同样纳入结果。

### 4. 发送数据报

围绕竞争状态记录内核、命名空间、接口、地址、路由、端口、描述符、返回值、errno和预期分组。进入“观察复制范围”前保存第一条偏离，清理与恢复同样纳入结果。

### 5. 观察复制范围

围绕广播记录内核、命名空间、接口、地址、路由、端口、描述符、返回值、errno和预期分组。进入“离组并关闭”前保存第一条偏离，清理与恢复同样纳入结果。

### 6. 离组并关闭

围绕广播地址记录内核、命名空间、接口、地址、路由、端口、描述符、返回值、errno和预期分组。进入“选择接口地址”前保存第一条偏离，清理与恢复同样纳入结果。

## 协议、对象与故障模型

第20章 广播围绕广播地址、单播和广播的比较、使用广播的dg_cli函数、竞争状态建立从套接字接口到分组、内核状态与清理结果的证据链。

本页核心不变量是：**发送接口、TTL/跳数、组成员和接收范围明确，广播竞争与多播离组不会留下隐藏成员状态。** 它同时约束正常、边界、故障和恢复样本。应用函数返回成功只是一条证据，还要检查地址长度、字节序、短计数、EINTR/EAGAIN、套接字状态、内核队列、分组顺序与异步清理。

“第20章 广播”的“第20章 广播”的实验台账至少包含内核/库版本、命名空间、接口与路由、地址端口、入口参数、描述符所有者、预期状态、实际返回与errno、首个分组分叉、内核队列和清理结果十一列。性能样本还固定并发度、连接复用、预热、输入规模、采样窗口和延迟分位；抓包必须记录过滤器、snaplen与时间基准。

## 可重放命令骨架

```bash
ip maddr show
ip route show table local type broadcast
```

```bash
tcpdump -ni any 'multicast or broadcast'
```

```bash
ss -uap
cat /proc/net/igmp
```

<Callout type="trap">
  **安全边界：**
  “第20章 广播”涉及的“第20章 广播”涉及的原始套接字、路由、密钥管理、广播多播和故障注入只在隔离网络命名空间、容器或可恢复虚拟机执行。先保存原始接口、路由和流量规则。
</Callout>

## 第三版机制逐项深读

### 第20章 广播

在“第20章 广播”中，“第20章 广播”把一份 IPv4 数据报交给广播域内多个主机，需要 SO_BROADCAST 且受路由器边界限制；多响应会形成竞争与放大风险。

### 20.1 概述

在“第20章 广播”中，“20.1 概述”是结构节点，用来组织“第20章 广播围绕广播地址、单播和广播的比较、使用广播的dg_cli函数、竞争状态建立从套接字接口到分组、内核状态与清理结果的证据链。 覆盖7个正式目录层级。”的接口、内核与分组证据；若只能复述标题而不能给出返回值或状态因果，本节点不通过。

### 20.2 广播地址

在“第20章 广播”中，“20.2 广播地址”把一份 IPv4 数据报交给广播域内多个主机，需要 SO_BROADCAST 且受路由器边界限制；多响应会形成竞争与放大风险。

### 20.3 单播和广播的比较

在“第20章 广播”中，“20.3 单播和广播的比较”把一份 IPv4 数据报交给广播域内多个主机，需要 SO_BROADCAST 且受路由器边界限制；多响应会形成竞争与放大风险。

### 20.4 使用广播的dg_cli函数

在“第20章 广播”中，“20.4 使用广播的dg_cli函数”把一份 IPv4 数据报交给广播域内多个主机，需要 SO_BROADCAST 且受路由器边界限制；多响应会形成竞争与放大风险。

### 20.5 竞争状态

在“第20章 广播”中，“20.5 竞争状态”的交接证据包含可编译代码、系统调用轨迹、端点状态、pcap 与清理日志；缺少任一层时要明确结论边界。

### 20.6 小结

在“第20章 广播”中，“20.6 小结”是结构节点，用来组织“第20章 广播围绕广播地址、单播和广播的比较、使用广播的dg_cli函数、竞争状态建立从套接字接口到分组、内核状态与清理结果的证据链。 覆盖7个正式目录层级。”的接口、内核与分组证据；若只能复述标题而不能给出返回值或状态因果，本节点不通过。
## 分步视觉验证

先预测地址、套接字状态和分组，再动手试一试；若结果不同，停在第一条API、内核队列或线上分组分叉。

<Stepper>
  <Step title="画端点与状态机">
    先预测广播地址、单播和广播的比较、使用广播的dg_cli函数、竞争状态、广播的所有权、状态转换和分组方向。
    <Unp20BroadcastingMapLab />
  </Step>
  <Step title="运行单变量网络实验">
    固定命名空间、接口、路由、地址和输入，只切换一个边界或故障。
    <Unp20BroadcastingExperimentLab />
  </Step>
  <Step title="核对首包与恢复">
    保存原始轨迹，关闭端点并撤销网络设置后用相同输入重放。
    <Unp20BroadcastingEvidenceLab />
  </Step>
</Stepper>

## 常见误区

<Callout type="trap">
  **误区一：**
  只看应用最终文本，不检查短读写、errno、套接字状态、内核队列与线上分组是否一致。
</Callout>

<Callout type="trap">
  **误区二：**
  在“第20章 广播”中，把TCP当消息协议、把UDP当可靠流，或把Linux专有行为当成跨UNIX与协议标准保证，都会直接判错。
</Callout>

## 四类样本与验收

| 样本 | 只改变什么                 | 必查证据              | 通过条件   |
| ---- | -------------------------- | --------------------- | ---------- |
| 基线 | 固定网络、地址与输入       | API、状态、队列与分组 | 与预测一致 |
| 边界 | 缓冲、MTU、并发或时序      | 首个阻塞、截断或拒绝  | 边界可解释 |
| 故障 | 丢包、复位、信号或路径故障 | errno、首包与责任层   | 故障可定位 |
| 恢复 | 关闭、回收、撤销网络设置   | 残留端点与同输入重放  | 回到基线   |

## 练习

<Exercises>

**问题 1** 为什么不能只凭最终输出验收第20章 广播？

<Answer>
  “第20章 广播”的“第20章 广播”的最终输出会隐藏短读写、errno、地址错误、队列积压、重传、分组重排和描述符泄漏。必须同时核对接口合同、套接字生存期、内核状态、首个分组分叉和恢复结果。
</Answer>

**问题 2** 如何把“加入接收组”变成网络证据？

<Answer>
  固定内核、命名空间、接口、路由、地址端口和输入，预测广播地址与单播和广播的比较的返回值、状态和分组，只改变一个条件并停在首错或首包分叉。
</Answer>

**问题 3** 本页最小通过条件是什么？

<Answer>
  发送接口、TTL/跳数、组成员和接收范围明确，广播竞争与多播离组不会留下隐藏成员状态。
  关闭端点并恢复网络设置后，相同输入必须重新得到基线结果。
</Answer>

</Exercises>

## 小结

- 广播地址：连接“选择接口地址”的接口、状态与分组证据。
- 单播和广播的比较：连接“配置广播多播”的接口、状态与分组证据。
- 使用广播的dg_cli函数：连接“加入接收组”的接口、状态与分组证据。
- 竞争状态：连接“发送数据报”的接口、状态与分组证据。
- 广播：连接“观察复制范围”的接口、状态与分组证据。
- 本页7个正式目录层级全部进入可重放网络实验链。

## 术语表

<Glossary>
  <GlossaryItem term="广播地址">
    广播地址用于解释“选择接口地址”，需要同时标出协议版本、地址与端点、状态机、首包分叉和清理方法。
  </GlossaryItem>
  <GlossaryItem term="单播和广播的比较">
    单播和广播的比较用于解释“配置广播多播”，需要同时标出协议版本、地址与端点、状态机、首包分叉和清理方法。
  </GlossaryItem>
  <GlossaryItem term="使用广播的dg_cli函数">
    使用广播的dg_cli函数用于解释“加入接收组”，需要同时标出协议版本、地址与端点、状态机、首包分叉和清理方法。
  </GlossaryItem>
  <GlossaryItem term="竞争状态">
    竞争状态用于解释“发送数据报”，需要同时标出协议版本、地址与端点、状态机、首包分叉和清理方法。
  </GlossaryItem>
  <GlossaryItem term="广播">
    广播用于解释“观察复制范围”，需要同时标出协议版本、地址与端点、状态机、首包分叉和清理方法。
  </GlossaryItem>
</Glossary>

<Attribution
  mode="independent-rewrite"
  sourceBasis="authorized-sample"
  workTitle="W. Richard Stevens, Bill Fenner, Andrew M. Rudoff, UNIX Network Programming, Volume 1: The Sockets Networking API, Third Edition"
  adaptedUrl="https://www.informit.com/store/unix-network-programming-volume-1-the-sockets-networking-9780131411555"
/>
