---
title: "第22章 高级UDP套接字编程"
type: C
section: "广播多播与高级传输"
order: 22
description: "第22章 高级UDP套接字编程围绕接收标志、目的IP地址和接口索引、数据报截断、何时用UDP代替TCP、给UDP应用增加可靠性、捆绑接口地址、并发UDP服务器建立从套接字接口到分组、内核状态与清理结果的证据链。 覆盖11个正式目录层级。"
demo: true
math: false
sourceUrl: "https://www.informit.com/store/unix-network-programming-volume-1-the-sockets-networking-9780131411555"
draft: false
---

import {
  Unp22AdvancedUdpMapLab,
  Unp22AdvancedUdpExperimentLab,
  Unp22AdvancedUdpEvidenceLab,
} from "@/components/mdx/unix-network-programming-vol1/diagrams/unp-22-advanced-udp";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第22章 高级UDP套接字编程的协议边界、套接字对象、状态机和平台版本限制
- 能描述“绑定数据报端点 → 发送单个报文 → 核对来源地址 → 检测截断丢失 → 施加流量边界 → 关闭并重放”的地址、返回值、errno、系统调用与分组证据
- 能实现一个隔离、单变量、可清理、可重放的UNIX网络实验
- 能比较基线、边界、故障和恢复轨迹并判断“每个数据报的来源、目的、长度和截断状态明确，丢失、乱序与重复不被误当成字节流语义。”是否成立

</Objectives>

## 为什么同时看API、内核状态与分组

<Term def="接收标志、目的IP地址和接口索引连接绑定数据报端点阶段的接口合同、套接字状态、分组轨迹和清理证据。">
  接收标志、目的IP地址和接口索引
</Term>
、
<Term def="数据报截断连接发送单个报文阶段的接口合同、套接字状态、分组轨迹和清理证据。">
  数据报截断
</Term>
、
<Term def="何时用UDP代替TCP连接核对来源地址阶段的接口合同、套接字状态、分组轨迹和清理证据。">
  何时用UDP代替TCP
</Term>
、
<Term def="给UDP应用增加可靠性连接检测截断丢失阶段的接口合同、套接字状态、分组轨迹和清理证据。">
  给UDP应用增加可靠性
</Term>
、
<Term def="捆绑接口地址连接施加流量边界阶段的接口合同、套接字状态、分组轨迹和清理证据。">
  捆绑接口地址
</Term>
共同构成本页坐标。第22章
高级UDP套接字编程围绕接收标志、目的IP地址和接口索引、数据报截断、何时用UDP代替TCP、给UDP应用增加可靠性、捆绑接口地址、并发UDP服务器建立从套接字接口到分组、内核状态与清理结果的证据链。

本书第3版覆盖IPv4、IPv6、TCP、UDP、SCTP、路由与密钥管理套接字，并保留STREAMS等历史接口。正文先还原书中标准和测试平台语境；Linux专有工具只用于观测与现代对照，不能冒充POSIX或协议标准保证。SCTP、STREAMS、T/TCP和历史IPv6 API的可用性必须由当前平台实测确认。

## 六阶段网络实验链

### 1. 绑定数据报端点

围绕接收标志、目的IP地址和接口索引记录内核、命名空间、接口、地址、路由、端口、描述符、返回值、errno和预期分组。进入“发送单个报文”前保存第一条偏离，清理与恢复同样纳入结果。

### 2. 发送单个报文

围绕数据报截断记录内核、命名空间、接口、地址、路由、端口、描述符、返回值、errno和预期分组。进入“核对来源地址”前保存第一条偏离，清理与恢复同样纳入结果。

### 3. 核对来源地址

围绕何时用UDP代替TCP记录内核、命名空间、接口、地址、路由、端口、描述符、返回值、errno和预期分组。进入“检测截断丢失”前保存第一条偏离，清理与恢复同样纳入结果。

### 4. 检测截断丢失

围绕给UDP应用增加可靠性记录内核、命名空间、接口、地址、路由、端口、描述符、返回值、errno和预期分组。进入“施加流量边界”前保存第一条偏离，清理与恢复同样纳入结果。

### 5. 施加流量边界

围绕捆绑接口地址记录内核、命名空间、接口、地址、路由、端口、描述符、返回值、errno和预期分组。进入“关闭并重放”前保存第一条偏离，清理与恢复同样纳入结果。

### 6. 关闭并重放

围绕接收标志、目的IP地址和接口索引记录内核、命名空间、接口、地址、路由、端口、描述符、返回值、errno和预期分组。进入“绑定数据报端点”前保存第一条偏离，清理与恢复同样纳入结果。

## 协议、对象与故障模型

第22章 高级UDP套接字编程围绕接收标志、目的IP地址和接口索引、数据报截断、何时用UDP代替TCP、给UDP应用增加可靠性、捆绑接口地址、并发UDP服务器建立从套接字接口到分组、内核状态与清理结果的证据链。

本页核心不变量是：**每个数据报的来源、目的、长度和截断状态明确，丢失、乱序与重复不被误当成字节流语义。** 它同时约束正常、边界、故障和恢复样本。应用函数返回成功只是一条证据，还要检查地址长度、字节序、短计数、EINTR/EAGAIN、套接字状态、内核队列、分组顺序与异步清理。

实验台账至少包含内核/库版本、命名空间、接口与路由、地址端口、入口参数、描述符所有者、预期状态、实际返回与errno、首个分组分叉、内核队列和清理结果十一列。性能样本还固定并发度、连接复用、预热、输入规模、采样窗口和延迟分位；抓包必须记录过滤器、snaplen与时间基准。

## 可重放命令骨架

```c
ssize_t n = recvfrom(fd, buf, sizeof(buf), 0, (SA *)&peer, &len);
if (n < 0) err_sys("recvfrom");
```

```bash
strace -e socket,bind,sendto,recvfrom,connect ./udp-client
```

```bash
tc qdisc add dev lo root netem loss 5% reorder 10%
tc qdisc del dev lo root
```

<Callout type="trap">
  **安全边界：**
  原始套接字、路由、密钥管理、广播多播和故障注入只在隔离网络命名空间、容器或可恢复虚拟机执行。先保存原始接口、路由和流量规则。
</Callout>

## 正式目录逐项讲解

### 第22章 高级UDP套接字编程

“第22章 高级UDP套接字编程”要求保留每个报文的来源、目的、长度与消息边界，显式处理丢失、乱序、重复、截断、MTU和接口选择。在本页中，它通过“接收标志、目的IP地址和接口索引”服务于“绑定数据报端点”：固定内核、网络命名空间、接口、路由、地址、端口、输入与初始描述符表，然后同时保存应用日志、系统调用轨迹和分组。

验证只改变一个延迟、丢包、重排、缓冲水位、地址、信号或并发度，并在进入“发送单个报文”前停住第一条返回值、errno、套接字状态、队列计数或分组分叉。最后关闭端点、回收进程线程、撤销流量规则与网络配置；同输入重放回到基线，结论才成立。

### 22.1 概述

“22.1 概述”要求先写出协议层、地址族、输入输出与错误合同，再沿应用调用、套接字状态、内核队列和线上分组定位责任边界。在本页中，它通过“数据报截断”服务于“发送单个报文”：固定内核、网络命名空间、接口、路由、地址、端口、输入与初始描述符表，然后同时保存应用日志、系统调用轨迹和分组。

验证只改变一个延迟、丢包、重排、缓冲水位、地址、信号或并发度，并在进入“核对来源地址”前停住第一条返回值、errno、套接字状态、队列计数或分组分叉。最后关闭端点、回收进程线程、撤销流量规则与网络配置；同输入重放回到基线，结论才成立。

### 22.2 接收标志、目的IP地址和接口索引

“22.2 接收标志、目的IP地址和接口索引”要求记录地址族、结构长度、字节序、作用域、接口索引与路由选择，用协议无关API替代硬编码布局。在本页中，它通过“何时用UDP代替TCP”服务于“核对来源地址”：固定内核、网络命名空间、接口、路由、地址、端口、输入与初始描述符表，然后同时保存应用日志、系统调用轨迹和分组。

验证只改变一个延迟、丢包、重排、缓冲水位、地址、信号或并发度，并在进入“检测截断丢失”前停住第一条返回值、errno、套接字状态、队列计数或分组分叉。最后关闭端点、回收进程线程、撤销流量规则与网络配置；同输入重放回到基线，结论才成立。

### 22.3 数据报截断

“22.3 数据报截断”要求保留每个报文的来源、目的、长度与消息边界，显式处理丢失、乱序、重复、截断、MTU和接口选择。在本页中，它通过“给UDP应用增加可靠性”服务于“检测截断丢失”：固定内核、网络命名空间、接口、路由、地址、端口、输入与初始描述符表，然后同时保存应用日志、系统调用轨迹和分组。

验证只改变一个延迟、丢包、重排、缓冲水位、地址、信号或并发度，并在进入“施加流量边界”前停住第一条返回值、errno、套接字状态、队列计数或分组分叉。最后关闭端点、回收进程线程、撤销流量规则与网络配置；同输入重放回到基线，结论才成立。

### 22.4 何时用UDP代替TCP

“22.4 何时用UDP代替TCP”要求画出握手、状态转换、序号或流、缓冲与关闭路径，并把应用返回值和抓包中的首个状态分叉对齐。在本页中，它通过“捆绑接口地址”服务于“施加流量边界”：固定内核、网络命名空间、接口、路由、地址、端口、输入与初始描述符表，然后同时保存应用日志、系统调用轨迹和分组。

验证只改变一个延迟、丢包、重排、缓冲水位、地址、信号或并发度，并在进入“关闭并重放”前停住第一条返回值、errno、套接字状态、队列计数或分组分叉。最后关闭端点、回收进程线程、撤销流量规则与网络配置；同输入重放回到基线，结论才成立。

### 22.5 给UDP应用增加可靠性

“22.5 给UDP应用增加可靠性”要求保留每个报文的来源、目的、长度与消息边界，显式处理丢失、乱序、重复、截断、MTU和接口选择。在本页中，它通过“接收标志、目的IP地址和接口索引”服务于“关闭并重放”：固定内核、网络命名空间、接口、路由、地址、端口、输入与初始描述符表，然后同时保存应用日志、系统调用轨迹和分组。

验证只改变一个延迟、丢包、重排、缓冲水位、地址、信号或并发度，并在进入“绑定数据报端点”前停住第一条返回值、errno、套接字状态、队列计数或分组分叉。最后关闭端点、回收进程线程、撤销流量规则与网络配置；同输入重放回到基线，结论才成立。

### 22.6 捆绑接口地址

“22.6 捆绑接口地址”要求记录地址族、结构长度、字节序、作用域、接口索引与路由选择，用协议无关API替代硬编码布局。在本页中，它通过“数据报截断”服务于“绑定数据报端点”：固定内核、网络命名空间、接口、路由、地址、端口、输入与初始描述符表，然后同时保存应用日志、系统调用轨迹和分组。

验证只改变一个延迟、丢包、重排、缓冲水位、地址、信号或并发度，并在进入“发送单个报文”前停住第一条返回值、errno、套接字状态、队列计数或分组分叉。最后关闭端点、回收进程线程、撤销流量规则与网络配置；同输入重放回到基线，结论才成立。

### 22.7 并发UDP服务器

“22.7 并发UDP服务器”要求保留每个报文的来源、目的、长度与消息边界，显式处理丢失、乱序、重复、截断、MTU和接口选择。在本页中，它通过“何时用UDP代替TCP”服务于“发送单个报文”：固定内核、网络命名空间、接口、路由、地址、端口、输入与初始描述符表，然后同时保存应用日志、系统调用轨迹和分组。

验证只改变一个延迟、丢包、重排、缓冲水位、地址、信号或并发度，并在进入“核对来源地址”前停住第一条返回值、errno、套接字状态、队列计数或分组分叉。最后关闭端点、回收进程线程、撤销流量规则与网络配置；同输入重放回到基线，结论才成立。

### 22.8 IPv6分组信息

“22.8 IPv6分组信息”要求保留每个报文的来源、目的、长度与消息边界，显式处理丢失、乱序、重复、截断、MTU和接口选择。在本页中，它通过“给UDP应用增加可靠性”服务于“核对来源地址”：固定内核、网络命名空间、接口、路由、地址、端口、输入与初始描述符表，然后同时保存应用日志、系统调用轨迹和分组。

验证只改变一个延迟、丢包、重排、缓冲水位、地址、信号或并发度，并在进入“检测截断丢失”前停住第一条返回值、errno、套接字状态、队列计数或分组分叉。最后关闭端点、回收进程线程、撤销流量规则与网络配置；同输入重放回到基线，结论才成立。

### 22.9 IPv6路径MTU控制

“22.9 IPv6路径MTU控制”要求记录地址族、结构长度、字节序、作用域、接口索引与路由选择，用协议无关API替代硬编码布局。在本页中，它通过“捆绑接口地址”服务于“检测截断丢失”：固定内核、网络命名空间、接口、路由、地址、端口、输入与初始描述符表，然后同时保存应用日志、系统调用轨迹和分组。

验证只改变一个延迟、丢包、重排、缓冲水位、地址、信号或并发度，并在进入“施加流量边界”前停住第一条返回值、errno、套接字状态、队列计数或分组分叉。最后关闭端点、回收进程线程、撤销流量规则与网络配置；同输入重放回到基线，结论才成立。

### 22.10 小结

“22.10 小结”要求把对象、状态机、失败模型、原始分组与恢复条件收束为可复查清单，并在干净网络命名空间重放。在本页中，它通过“接收标志、目的IP地址和接口索引”服务于“施加流量边界”：固定内核、网络命名空间、接口、路由、地址、端口、输入与初始描述符表，然后同时保存应用日志、系统调用轨迹和分组。

验证只改变一个延迟、丢包、重排、缓冲水位、地址、信号或并发度，并在进入“关闭并重放”前停住第一条返回值、errno、套接字状态、队列计数或分组分叉。最后关闭端点、回收进程线程、撤销流量规则与网络配置；同输入重放回到基线，结论才成立。

## 分步视觉验证

先预测地址、套接字状态和分组，再动手试一试；若结果不同，停在第一条API、内核队列或线上分组分叉。

<Stepper>
  <Step title="画端点与状态机">
    先预测接收标志、目的IP地址和接口索引、数据报截断、何时用UDP代替TCP、给UDP应用增加可靠性、捆绑接口地址的所有权、状态转换和分组方向。
    <Unp22AdvancedUdpMapLab />
  </Step>
  <Step title="运行单变量网络实验">
    固定命名空间、接口、路由、地址和输入，只切换一个边界或故障。
    <Unp22AdvancedUdpExperimentLab />
  </Step>
  <Step title="核对首包与恢复">
    保存原始轨迹，关闭端点并撤销网络设置后用相同输入重放。
    <Unp22AdvancedUdpEvidenceLab />
  </Step>
</Stepper>

## 常见误区

<Callout type="trap">
  **误区一：**
  只看应用最终文本，不检查短读写、errno、套接字状态、内核队列与线上分组是否一致。
</Callout>

<Callout type="trap">
  **误区二：**
  把TCP当消息协议、把UDP当可靠流，或把Linux专有行为当成跨UNIX与协议标准保证。
</Callout>

## 四类样本与验收

| 样本 | 只改变什么                 | 必查证据              | 通过条件   |
| ---- | -------------------------- | --------------------- | ---------- |
| 基线 | 固定网络、地址与输入       | API、状态、队列与分组 | 与预测一致 |
| 边界 | 缓冲、MTU、并发或时序      | 首个阻塞、截断或拒绝  | 边界可解释 |
| 故障 | 丢包、复位、信号或路径故障 | errno、首包与责任层   | 故障可定位 |
| 恢复 | 关闭、回收、撤销网络设置   | 残留端点与同输入重放  | 回到基线   |

## 练习

<Exercises>

**问题 1** 为什么不能只凭最终输出验收第22章 高级UDP套接字编程？

<Answer>
  最终输出会隐藏短读写、errno、地址错误、队列积压、重传、分组重排和描述符泄漏。必须同时核对接口合同、套接字生存期、内核状态、首个分组分叉和恢复结果。
</Answer>

**问题 2** 如何把“核对来源地址”变成网络证据？

<Answer>
  固定内核、命名空间、接口、路由、地址端口和输入，预测接收标志、目的IP地址和接口索引与数据报截断的返回值、状态和分组，只改变一个条件并停在首错或首包分叉。
</Answer>

**问题 3** 本页最小通过条件是什么？

<Answer>
  每个数据报的来源、目的、长度和截断状态明确，丢失、乱序与重复不被误当成字节流语义。
  关闭端点并恢复网络设置后，相同输入必须重新得到基线结果。
</Answer>

</Exercises>

## 小结

- 接收标志、目的IP地址和接口索引：连接“绑定数据报端点”的接口、状态与分组证据。
- 数据报截断：连接“发送单个报文”的接口、状态与分组证据。
- 何时用UDP代替TCP：连接“核对来源地址”的接口、状态与分组证据。
- 给UDP应用增加可靠性：连接“检测截断丢失”的接口、状态与分组证据。
- 捆绑接口地址：连接“施加流量边界”的接口、状态与分组证据。
- 本页11个正式目录层级全部进入可重放网络实验链。

## 术语表

<Glossary>
  <GlossaryItem term="接收标志、目的IP地址和接口索引">
    接收标志、目的IP地址和接口索引用于解释“绑定数据报端点”，需要同时标出协议版本、地址与端点、状态机、首包分叉和清理方法。
  </GlossaryItem>
  <GlossaryItem term="数据报截断">
    数据报截断用于解释“发送单个报文”，需要同时标出协议版本、地址与端点、状态机、首包分叉和清理方法。
  </GlossaryItem>
  <GlossaryItem term="何时用UDP代替TCP">
    何时用UDP代替TCP用于解释“核对来源地址”，需要同时标出协议版本、地址与端点、状态机、首包分叉和清理方法。
  </GlossaryItem>
  <GlossaryItem term="给UDP应用增加可靠性">
    给UDP应用增加可靠性用于解释“检测截断丢失”，需要同时标出协议版本、地址与端点、状态机、首包分叉和清理方法。
  </GlossaryItem>
  <GlossaryItem term="捆绑接口地址">
    捆绑接口地址用于解释“施加流量边界”，需要同时标出协议版本、地址与端点、状态机、首包分叉和清理方法。
  </GlossaryItem>
</Glossary>

<Attribution
  adaptedFrom="W. Richard Stevens、Bill Fenner、Andrew M. Rudoff著，杨继张译《UNIX网络编程 卷1：套接字联网API（第3版）》，人民邮电出版社，2010-06，804页，ISBN 9787115228406 · 完整中文目录与InformIT原版目录的独立教学重构"
  adaptedUrl="https://www.informit.com/store/unix-network-programming-volume-1-the-sockets-networking-9780131411555"
/>
