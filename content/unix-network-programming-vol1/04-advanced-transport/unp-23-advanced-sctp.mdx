---
title: "第23章 高级SCTP套接字编程"
type: C
section: "广播多播与高级传输"
order: 23
description: "第23章 高级SCTP套接字编程围绕自动关闭的一到多式服务器程序、部分递送、通知、无序的数据、捆绑地址子集、确定对端和本端地址信息建立从套接字接口到分组、内核状态与清理结果的证据链。 覆盖14个正式目录层级。"
demo: true
math: false
sourceUrl: "https://www.informit.com/store/unix-network-programming-volume-1-the-sockets-networking-9780131411555"
draft: true
qualityVersion: 2
practiceMode: code
sourceMode: independent-rewrite
---

import {
  Unp23AdvancedSctpMapLab,
  Unp23AdvancedSctpExperimentLab,
  Unp23AdvancedSctpEvidenceLab,
} from "@/components/mdx/unix-network-programming-vol1/diagrams/unp-23-advanced-sctp";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第23章 高级SCTP套接字编程的协议边界、套接字对象、状态机和平台版本限制
- 能描述“建立多宿主端点 → 创建关联 → 选择流 → 接收通知 → 注入路径故障 → 终止关联”的地址、返回值、errno、系统调用与分组证据
- 能实现一个隔离、单变量、可清理、可重放的UNIX网络实验
- 能比较基线、边界、故障和恢复轨迹并判断“关联、路径、流和消息边界分别可追踪，通知与用户数据不混淆，终止后地址资源全部释放。”是否成立

</Objectives>

## 为什么同时看API、内核状态与分组

<Term def="自动关闭的一到多式服务器程序连接建立多宿主端点阶段的接口合同、套接字状态、分组轨迹和清理证据。">自动关闭的一到多式服务器程序</Term>、<Term def="部分递送连接创建关联阶段的接口合同、套接字状态、分组轨迹和清理证据。">部分递送</Term>、<Term def="通知连接选择流阶段的接口合同、套接字状态、分组轨迹和清理证据。">通知</Term>、<Term def="无序的数据连接接收通知阶段的接口合同、套接字状态、分组轨迹和清理证据。">无序的数据</Term>、<Term def="捆绑地址子集连接注入路径故障阶段的接口合同、套接字状态、分组轨迹和清理证据。">捆绑地址子集</Term>
共同构成本页坐标。第23章
高级SCTP套接字编程围绕自动关闭的一到多式服务器程序、部分递送、通知、无序的数据、捆绑地址子集、确定对端和本端地址信息建立从套接字接口到分组、内核状态与清理结果的证据链。

在“第23章 高级SCTP套接字编程”中，第3版覆盖IPv4、IPv6、TCP、UDP、SCTP、路由与密钥管理套接字，并保留STREAMS等历史接口。正文先还原书中标准和测试平台语境；Linux专有工具只用于观测与现代对照，不能冒充POSIX或协议标准保证。SCTP、STREAMS、T/TCP和历史IPv6 API的可用性必须由当前平台实测确认。

“第23章 高级SCTP套接字编程”未取得未获授权的完整中文正文；以 [InformIT 第三版官方目录与合法样章](https://www.informit.com/store/unix-network-programming-volume-1-the-sockets-networking-9780131411555) 界定 3 部分、31 章和 5 个附录范围，中文解释、代码、实验与练习均为独立教学重写。“第23章 高级SCTP套接字编程”的接口与协议事实分别以 [POSIX socket](https://pubs.opengroup.org/onlinepubs/9799919799/functions/socket.html)、[RFC 9293 TCP](https://www.rfc-editor.org/rfc/rfc9293.html)、[RFC 768 UDP](https://www.rfc-editor.org/rfc/rfc768.html)、[RFC 9260 SCTP](https://www.rfc-editor.org/rfc/rfc9260.html)、[RFC 6458 SCTP sockets](https://www.rfc-editor.org/rfc/rfc6458.html) 和 [RFC 8200 IPv6](https://www.rfc-editor.org/rfc/rfc8200.html) 核对；现代差异不覆盖第三版历史语境。

## 六阶段网络实验链

### 1. 建立多宿主端点

围绕自动关闭的一到多式服务器程序记录内核、命名空间、接口、地址、路由、端口、描述符、返回值、errno和预期分组。进入“创建关联”前保存第一条偏离，清理与恢复同样纳入结果。

### 2. 创建关联

围绕部分递送记录内核、命名空间、接口、地址、路由、端口、描述符、返回值、errno和预期分组。进入“选择流”前保存第一条偏离，清理与恢复同样纳入结果。

### 3. 选择流

围绕通知记录内核、命名空间、接口、地址、路由、端口、描述符、返回值、errno和预期分组。进入“接收通知”前保存第一条偏离，清理与恢复同样纳入结果。

### 4. 接收通知

围绕无序的数据记录内核、命名空间、接口、地址、路由、端口、描述符、返回值、errno和预期分组。进入“注入路径故障”前保存第一条偏离，清理与恢复同样纳入结果。

### 5. 注入路径故障

围绕捆绑地址子集记录内核、命名空间、接口、地址、路由、端口、描述符、返回值、errno和预期分组。进入“终止关联”前保存第一条偏离，清理与恢复同样纳入结果。

### 6. 终止关联

围绕自动关闭的一到多式服务器程序记录内核、命名空间、接口、地址、路由、端口、描述符、返回值、errno和预期分组。进入“建立多宿主端点”前保存第一条偏离，清理与恢复同样纳入结果。

## 协议、对象与故障模型

第23章 高级SCTP套接字编程围绕自动关闭的一到多式服务器程序、部分递送、通知、无序的数据、捆绑地址子集、确定对端和本端地址信息建立从套接字接口到分组、内核状态与清理结果的证据链。

本页核心不变量是：**关联、路径、流和消息边界分别可追踪，通知与用户数据不混淆，终止后地址资源全部释放。** 它同时约束正常、边界、故障和恢复样本。应用函数返回成功只是一条证据，还要检查地址长度、字节序、短计数、EINTR/EAGAIN、套接字状态、内核队列、分组顺序与异步清理。

“第23章 高级SCTP套接字编程”的“第23章 高级SCTP套接字编程”的实验台账至少包含内核/库版本、命名空间、接口与路由、地址端口、入口参数、描述符所有者、预期状态、实际返回与errno、首个分组分叉、内核队列和清理结果十一列。性能样本还固定并发度、连接复用、预热、输入规模、采样窗口和延迟分位；抓包必须记录过滤器、snaplen与时间基准。

## 可重放命令骨架

```c
int fd = socket(AF_INET6, SOCK_SEQPACKET, IPPROTO_SCTP);
if (fd < 0) err_sys("socket");
```

```bash
ss -S -a
modinfo sctp | head
```

```bash
tcpdump -ni any -w sctp.pcap 'ip proto 132'
tshark -r sctp.pcap -Y sctp
```

<Callout type="trap">
  **安全边界：**
  “第23章 高级SCTP套接字编程”涉及的“第23章 高级SCTP套接字编程”涉及的原始套接字、路由、密钥管理、广播多播和故障注入只在隔离网络命名空间、容器或可恢复虚拟机执行。先保存原始接口、路由和流量规则。
</Callout>

## 第三版机制逐项深读

### 第23章 高级SCTP套接字编程

在“第23章 高级SCTP套接字编程”中，验证“第23章 高级SCTP套接字编程”固定关联，只改变流号、顺序标志或活动路径，观察头端阻塞、通知与故障切换是否符合预测。

### 23.1 概述

在“第23章 高级SCTP套接字编程”中，“23.1 概述”是结构节点，用来组织“第23章 高级SCTP套接字编程围绕自动关闭的一到多式服务器程序、部分递送、通知、无序的数据、捆绑地址子集、确定对端和本端地址信息建立从套接字接口到分组、内核状态与清理结果的证据链。 覆盖14个正式目录层级。”的接口、内核与分组证据；若只能复述标题而不能给出返回值或状态因果，本节点不通过。

### 23.2 自动关闭的一到多式服务器程序

在“第23章 高级SCTP套接字编程”中，“23.2 自动关闭的一到多式服务器程序”服务于“第23章 高级SCTP套接字编程围绕自动关闭的一到多式服务器程序、部分递送、通知、无序的数据、捆绑地址子集、确定对端和本端地址信息建立从套接字接口到分组、内核状态与清理结果的证据链。 覆盖14个正式目录层级。”。解释时必须写清输入、系统调用合同、内核对象、线上分组与清理结果，并用一个单变量反例定位首个分叉。

### 23.3 部分递送

在“第23章 高级SCTP套接字编程”中，“23.3 部分递送”的交接证据包含可编译代码、系统调用轨迹、端点状态、pcap 与清理日志；缺少任一层时要明确结论边界。

### 23.4 通知

在“第23章 高级SCTP套接字编程”中，验证“23.4 通知”固定平台、地址与输入，只改变一个返回条件或网络事件，保存首个 errno、队列变化和分组差异。

### 23.5 无序的数据

在“第23章 高级SCTP套接字编程”中，分析“23.5 无序的数据”要把应用调用、描述符所有权、内核状态与线上分组放在同一时间轴；最终输出只能证明终点，不能证明中间因果。

### 23.6 捆绑地址子集

在“第23章 高级SCTP套接字编程”中，验证“23.6 捆绑地址子集”固定平台、地址与输入，只改变一个返回条件或网络事件，保存首个 errno、队列变化和分组差异。

### 23.7 确定对端和本端地址信息

在“第23章 高级SCTP套接字编程”中，分析“23.7 确定对端和本端地址信息”要把应用调用、描述符所有权、内核状态与线上分组放在同一时间轴；最终输出只能证明终点，不能证明中间因果。

### 23.8 给定IP地址找出关联ID

在“第23章 高级SCTP套接字编程”中，“23.8 给定IP地址找出关联ID”在原书与现代平台的 API 可用性差异较大；代码必须记录 SCTP 实现、头文件与 RFC 6458 映射版本。

### 23.9 心搏和地址不可达

在“第23章 高级SCTP套接字编程”中，“23.9 心搏和地址不可达”讨论 TCP 紧急指针与应用通知边界；只有一个紧急标记位置，普通数据仍在同一字节流中，不能把它当独立可靠消息通道。

### 23.10 关联剥离

在“第23章 高级SCTP套接字编程”中，“23.10 关联剥离”在原书与现代平台的 API 可用性差异较大；代码必须记录 SCTP 实现、头文件与 RFC 6458 映射版本。

### 23.11 定时控制

在“第23章 高级SCTP套接字编程”中，验证“23.11 定时控制”固定平台、地址与输入，只改变一个返回条件或网络事件，保存首个 errno、队列变化和分组差异。

### 23.12 何时改用SCTP代替TCP

在“第23章 高级SCTP套接字编程”中，分析“23.12 何时改用SCTP代替TCP”同时对齐 connect/accept/read/write 返回、内核状态与 SYN、ACK、FIN、RST 分组，最终文本不能替代中间证据。

### 23.13 小结

在“第23章 高级SCTP套接字编程”中，“23.13 小结”是结构节点，用来组织“第23章 高级SCTP套接字编程围绕自动关闭的一到多式服务器程序、部分递送、通知、无序的数据、捆绑地址子集、确定对端和本端地址信息建立从套接字接口到分组、内核状态与清理结果的证据链。 覆盖14个正式目录层级。”的接口、内核与分组证据；若只能复述标题而不能给出返回值或状态因果，本节点不通过。
## 分步视觉验证

先预测地址、套接字状态和分组，再动手试一试；若结果不同，停在第一条API、内核队列或线上分组分叉。

<Stepper>
  <Step title="画端点与状态机">
    先预测自动关闭的一到多式服务器程序、部分递送、通知、无序的数据、捆绑地址子集的所有权、状态转换和分组方向。
    <Unp23AdvancedSctpMapLab />
  </Step>
  <Step title="运行单变量网络实验">
    固定命名空间、接口、路由、地址和输入，只切换一个边界或故障。
    <Unp23AdvancedSctpExperimentLab />
  </Step>
  <Step title="核对首包与恢复">
    保存原始轨迹，关闭端点并撤销网络设置后用相同输入重放。
    <Unp23AdvancedSctpEvidenceLab />
  </Step>
</Stepper>

## 常见误区

<Callout type="trap">
  **误区一：**
  只看应用最终文本，不检查短读写、errno、套接字状态、内核队列与线上分组是否一致。
</Callout>

<Callout type="trap">
  **误区二：**
  在“第23章 高级SCTP套接字编程”中，把TCP当消息协议、把UDP当可靠流，或把Linux专有行为当成跨UNIX与协议标准保证，都会直接判错。
</Callout>

## 四类样本与验收

| 样本 | 只改变什么                 | 必查证据              | 通过条件   |
| ---- | -------------------------- | --------------------- | ---------- |
| 基线 | 固定网络、地址与输入       | API、状态、队列与分组 | 与预测一致 |
| 边界 | 缓冲、MTU、并发或时序      | 首个阻塞、截断或拒绝  | 边界可解释 |
| 故障 | 丢包、复位、信号或路径故障 | errno、首包与责任层   | 故障可定位 |
| 恢复 | 关闭、回收、撤销网络设置   | 残留端点与同输入重放  | 回到基线   |

## 练习

<Exercises>

**问题 1** 为什么不能只凭最终输出验收第23章 高级SCTP套接字编程？

<Answer>
  “第23章 高级SCTP套接字编程”的“第23章 高级SCTP套接字编程”的最终输出会隐藏短读写、errno、地址错误、队列积压、重传、分组重排和描述符泄漏。必须同时核对接口合同、套接字生存期、内核状态、首个分组分叉和恢复结果。
</Answer>

**问题 2** 如何把“选择流”变成网络证据？

<Answer>
  固定内核、命名空间、接口、路由、地址端口和输入，预测自动关闭的一到多式服务器程序与部分递送的返回值、状态和分组，只改变一个条件并停在首错或首包分叉。
</Answer>

**问题 3** 本页最小通过条件是什么？

<Answer>
  关联、路径、流和消息边界分别可追踪，通知与用户数据不混淆，终止后地址资源全部释放。
  关闭端点并恢复网络设置后，相同输入必须重新得到基线结果。
</Answer>

</Exercises>

## 小结

- 自动关闭的一到多式服务器程序：连接“建立多宿主端点”的接口、状态与分组证据。
- 部分递送：连接“创建关联”的接口、状态与分组证据。
- 通知：连接“选择流”的接口、状态与分组证据。
- 无序的数据：连接“接收通知”的接口、状态与分组证据。
- 捆绑地址子集：连接“注入路径故障”的接口、状态与分组证据。
- 本页14个正式目录层级全部进入可重放网络实验链。

## 术语表

<Glossary>
  <GlossaryItem term="自动关闭的一到多式服务器程序">
    自动关闭的一到多式服务器程序用于解释“建立多宿主端点”，需要同时标出协议版本、地址与端点、状态机、首包分叉和清理方法。
  </GlossaryItem>
  <GlossaryItem term="部分递送">
    部分递送用于解释“创建关联”，需要同时标出协议版本、地址与端点、状态机、首包分叉和清理方法。
  </GlossaryItem>
  <GlossaryItem term="通知">
    通知用于解释“选择流”，需要同时标出协议版本、地址与端点、状态机、首包分叉和清理方法。
  </GlossaryItem>
  <GlossaryItem term="无序的数据">
    无序的数据用于解释“接收通知”，需要同时标出协议版本、地址与端点、状态机、首包分叉和清理方法。
  </GlossaryItem>
  <GlossaryItem term="捆绑地址子集">
    捆绑地址子集用于解释“注入路径故障”，需要同时标出协议版本、地址与端点、状态机、首包分叉和清理方法。
  </GlossaryItem>
</Glossary>

<Attribution
  mode="independent-rewrite"
  sourceBasis="authorized-sample"
  workTitle="W. Richard Stevens, Bill Fenner, Andrew M. Rudoff, UNIX Network Programming, Volume 1: The Sockets Networking API, Third Edition"
  adaptedUrl="https://www.informit.com/store/unix-network-programming-volume-1-the-sockets-networking-9780131411555"
/>
