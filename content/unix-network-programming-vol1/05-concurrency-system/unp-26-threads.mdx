---
title: "第26章 线程"
type: C
section: "并发、原始网络与设计范式"
order: 26
description: "第26章 线程围绕基本线程函数：创建和终止、使用线程的str_cli函数、使用线程的TCP回射服务器程序、线程特定数据、Web客户与同时连接、互斥锁建立从套接字接口到分组、内核状态与清理结果的证据链。 覆盖11个正式目录层级。"
demo: true
math: false
sourceUrl: "https://www.informit.com/store/unix-network-programming-volume-1-the-sockets-networking-9780131411555"
draft: true
qualityVersion: 2
practiceMode: code
sourceMode: independent-rewrite
---

import {
  Unp26ThreadsMapLab,
  Unp26ThreadsExperimentLab,
  Unp26ThreadsEvidenceLab,
} from "@/components/mdx/unix-network-programming-vol1/diagrams/unp-26-threads";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第26章 线程的协议边界、套接字对象、状态机和平台版本限制
- 能描述“选择并发范式 → 建立工作单元 → 分派连接 → 同步共享状态 → 施加过载 → 停机回收”的地址、返回值、errno、系统调用与分组证据
- 能实现一个隔离、单变量、可清理、可重放的UNIX网络实验
- 能比较基线、边界、故障和恢复轨迹并判断“连接所有权唯一，锁序和条件谓词明确，过载有界，停机后进程、线程和描述符全部归零。”是否成立

</Objectives>

## 为什么同时看API、内核状态与分组

<Term def="基本线程函数：创建和终止连接选择并发范式阶段的接口合同、套接字状态、分组轨迹和清理证据。">基本线程函数：创建和终止</Term>、<Term def="使用线程的str_cli函数连接建立工作单元阶段的接口合同、套接字状态、分组轨迹和清理证据。">使用线程的str_cli函数</Term>、<Term def="使用线程的TCP回射服务器程序连接分派连接阶段的接口合同、套接字状态、分组轨迹和清理证据。">使用线程的TCP回射服务器程序</Term>、<Term def="线程特定数据连接同步共享状态阶段的接口合同、套接字状态、分组轨迹和清理证据。">线程特定数据</Term>、<Term def="Web客户与同时连接连接施加过载阶段的接口合同、套接字状态、分组轨迹和清理证据。">Web客户与同时连接</Term>
共同构成本页坐标。第26章
线程围绕基本线程函数：创建和终止、使用线程的str_cli函数、使用线程的TCP回射服务器程序、线程特定数据、Web客户与同时连接、互斥锁建立从套接字接口到分组、内核状态与清理结果的证据链。

在“第26章 线程”中，第3版覆盖IPv4、IPv6、TCP、UDP、SCTP、路由与密钥管理套接字，并保留STREAMS等历史接口。正文先还原书中标准和测试平台语境；Linux专有工具只用于观测与现代对照，不能冒充POSIX或协议标准保证。SCTP、STREAMS、T/TCP和历史IPv6 API的可用性必须由当前平台实测确认。

“第26章 线程”未取得未获授权的完整中文正文；以 [InformIT 第三版官方目录与合法样章](https://www.informit.com/store/unix-network-programming-volume-1-the-sockets-networking-9780131411555) 界定 3 部分、31 章和 5 个附录范围，中文解释、代码、实验与练习均为独立教学重写。“第26章 线程”的接口与协议事实分别以 [POSIX socket](https://pubs.opengroup.org/onlinepubs/9799919799/functions/socket.html)、[RFC 9293 TCP](https://www.rfc-editor.org/rfc/rfc9293.html)、[RFC 768 UDP](https://www.rfc-editor.org/rfc/rfc768.html)、[RFC 9260 SCTP](https://www.rfc-editor.org/rfc/rfc9260.html)、[RFC 6458 SCTP sockets](https://www.rfc-editor.org/rfc/rfc6458.html) 和 [RFC 8200 IPv6](https://www.rfc-editor.org/rfc/rfc8200.html) 核对；现代差异不覆盖第三版历史语境。

## 六阶段网络实验链

### 1. 选择并发范式

围绕基本线程函数：创建和终止记录内核、命名空间、接口、地址、路由、端口、描述符、返回值、errno和预期分组。进入“建立工作单元”前保存第一条偏离，清理与恢复同样纳入结果。

### 2. 建立工作单元

围绕使用线程的str_cli函数记录内核、命名空间、接口、地址、路由、端口、描述符、返回值、errno和预期分组。进入“分派连接”前保存第一条偏离，清理与恢复同样纳入结果。

### 3. 分派连接

围绕使用线程的TCP回射服务器程序记录内核、命名空间、接口、地址、路由、端口、描述符、返回值、errno和预期分组。进入“同步共享状态”前保存第一条偏离，清理与恢复同样纳入结果。

### 4. 同步共享状态

围绕线程特定数据记录内核、命名空间、接口、地址、路由、端口、描述符、返回值、errno和预期分组。进入“施加过载”前保存第一条偏离，清理与恢复同样纳入结果。

### 5. 施加过载

围绕Web客户与同时连接记录内核、命名空间、接口、地址、路由、端口、描述符、返回值、errno和预期分组。进入“停机回收”前保存第一条偏离，清理与恢复同样纳入结果。

### 6. 停机回收

围绕基本线程函数：创建和终止记录内核、命名空间、接口、地址、路由、端口、描述符、返回值、errno和预期分组。进入“选择并发范式”前保存第一条偏离，清理与恢复同样纳入结果。

## 协议、对象与故障模型

第26章 线程围绕基本线程函数：创建和终止、使用线程的str_cli函数、使用线程的TCP回射服务器程序、线程特定数据、Web客户与同时连接、互斥锁建立从套接字接口到分组、内核状态与清理结果的证据链。

本页核心不变量是：**连接所有权唯一，锁序和条件谓词明确，过载有界，停机后进程、线程和描述符全部归零。** 它同时约束正常、边界、故障和恢复样本。应用函数返回成功只是一条证据，还要检查地址长度、字节序、短计数、EINTR/EAGAIN、套接字状态、内核队列、分组顺序与异步清理。

“第26章 线程”的“第26章 线程”的实验台账至少包含内核/库版本、命名空间、接口与路由、地址端口、入口参数、描述符所有者、预期状态、实际返回与errno、首个分组分叉、内核队列和清理结果十一列。性能样本还固定并发度、连接复用、预热、输入规模、采样窗口和延迟分位；抓包必须记录过滤器、snaplen与时间基准。

## 可重放命令骨架

```bash
ps -eLf | grep '[s]erver'
ss -lntp
```

```bash
strace -f -e clone,fork,accept,close,futex ./server
```

```bash
valgrind --tool=helgrind --error-exitcode=1 ./thread-server
```

<Callout type="trap">
  **安全边界：**
  “第26章 线程”涉及的“第26章 线程”涉及的原始套接字、路由、密钥管理、广播多播和故障注入只在隔离网络命名空间、容器或可恢复虚拟机执行。先保存原始接口、路由和流量规则。
</Callout>

## 第三版机制逐项深读

### 第26章 线程

在“第26章 线程”中，验证“第26章 线程”固定请求，只改变并发度或退出时序，比较吞吐、排队、wait 结果、锁竞争和未回收执行单元。

### 26.1 概述

在“第26章 线程”中，“26.1 概述”是结构节点，用来组织“第26章 线程围绕基本线程函数：创建和终止、使用线程的str_cli函数、使用线程的TCP回射服务器程序、线程特定数据、Web客户与同时连接、互斥锁建立从套接字接口到分组、内核状态与清理结果的证据链。 覆盖11个正式目录层级。”的接口、内核与分组证据；若只能复述标题而不能给出返回值或状态因果，本节点不通过。

### 26.2 基本线程函数：创建和终止

在“第26章 线程”中，验证“26.2 基本线程函数：创建和终止”固定请求，只改变并发度或退出时序，比较吞吐、排队、wait 结果、锁竞争和未回收执行单元。

### 26.3 使用线程的str_cli函数

在“第26章 线程”中，“26.3 使用线程的str_cli函数”的过载边界需要有界队列、拒绝策略与可终止路径；创建更多进程线程不是自动扩容方案。

### 26.4 使用线程的TCP回射服务器程序

在“第26章 线程”中，“26.4 使用线程的TCP回射服务器程序”建立可靠有序字节流，握手、序号、确认、流量/拥塞控制和双向关闭共同决定状态；应用层必须自行恢复消息边界。

### 26.5 线程特定数据

在“第26章 线程”中，验证“26.5 线程特定数据”固定请求，只改变并发度或退出时序，比较吞吐、排队、wait 结果、锁竞争和未回收执行单元。

### 26.6 Web客户与同时连接

在“第26章 线程”中，验证“26.6 Web客户与同时连接”只改变一项关闭、复位、缓冲或并发条件，定位首个 errno、短计数、状态转换和重传差异。

### 26.7 互斥锁

在“第26章 线程”中，分析“26.7 互斥锁”要画出监听端点、连接端点、子进程或线程、信号与锁的生存期，避免重复 close、僵尸进程和竞态。

### 26.8 条件变量

在“第26章 线程”中，“26.8 条件变量”服务于“第26章 线程围绕基本线程函数：创建和终止、使用线程的str_cli函数、使用线程的TCP回射服务器程序、线程特定数据、Web客户与同时连接、互斥锁建立从套接字接口到分组、内核状态与清理结果的证据链。 覆盖11个正式目录层级。”。解释时必须写清输入、系统调用合同、内核对象、线上分组与清理结果，并用一个单变量反例定位首个分叉。

### 26.9 Web客户与同时连接（续）

在“第26章 线程”中，“26.9 Web客户与同时连接（续）”的资源所有权跨监听端点、已连接端点、父子进程或线程；每条异常路径都要证明描述符最终关闭且可重新绑定。

### 26.10 小结

在“第26章 线程”中，“26.10 小结”是结构节点，用来组织“第26章 线程围绕基本线程函数：创建和终止、使用线程的str_cli函数、使用线程的TCP回射服务器程序、线程特定数据、Web客户与同时连接、互斥锁建立从套接字接口到分组、内核状态与清理结果的证据链。 覆盖11个正式目录层级。”的接口、内核与分组证据；若只能复述标题而不能给出返回值或状态因果，本节点不通过。
## 分步视觉验证

先预测地址、套接字状态和分组，再动手试一试；若结果不同，停在第一条API、内核队列或线上分组分叉。

<Stepper>
  <Step title="画端点与状态机">
    先预测基本线程函数：创建和终止、使用线程的str_cli函数、使用线程的TCP回射服务器程序、线程特定数据、Web客户与同时连接的所有权、状态转换和分组方向。
    <Unp26ThreadsMapLab />
  </Step>
  <Step title="运行单变量网络实验">
    固定命名空间、接口、路由、地址和输入，只切换一个边界或故障。
    <Unp26ThreadsExperimentLab />
  </Step>
  <Step title="核对首包与恢复">
    保存原始轨迹，关闭端点并撤销网络设置后用相同输入重放。
    <Unp26ThreadsEvidenceLab />
  </Step>
</Stepper>

## 常见误区

<Callout type="trap">
  **误区一：**
  只看应用最终文本，不检查短读写、errno、套接字状态、内核队列与线上分组是否一致。
</Callout>

<Callout type="trap">
  **误区二：**
  在“第26章 线程”中，把TCP当消息协议、把UDP当可靠流，或把Linux专有行为当成跨UNIX与协议标准保证，都会直接判错。
</Callout>

## 四类样本与验收

| 样本 | 只改变什么                 | 必查证据              | 通过条件   |
| ---- | -------------------------- | --------------------- | ---------- |
| 基线 | 固定网络、地址与输入       | API、状态、队列与分组 | 与预测一致 |
| 边界 | 缓冲、MTU、并发或时序      | 首个阻塞、截断或拒绝  | 边界可解释 |
| 故障 | 丢包、复位、信号或路径故障 | errno、首包与责任层   | 故障可定位 |
| 恢复 | 关闭、回收、撤销网络设置   | 残留端点与同输入重放  | 回到基线   |

## 练习

<Exercises>

**问题 1** 为什么不能只凭最终输出验收第26章 线程？

<Answer>
  “第26章 线程”的“第26章 线程”的最终输出会隐藏短读写、errno、地址错误、队列积压、重传、分组重排和描述符泄漏。必须同时核对接口合同、套接字生存期、内核状态、首个分组分叉和恢复结果。
</Answer>

**问题 2** 如何把“分派连接”变成网络证据？

<Answer>
  固定内核、命名空间、接口、路由、地址端口和输入，预测基本线程函数：创建和终止与使用线程的str_cli函数的返回值、状态和分组，只改变一个条件并停在首错或首包分叉。
</Answer>

**问题 3** 本页最小通过条件是什么？

<Answer>
  连接所有权唯一，锁序和条件谓词明确，过载有界，停机后进程、线程和描述符全部归零。
  关闭端点并恢复网络设置后，相同输入必须重新得到基线结果。
</Answer>

</Exercises>

## 小结

- 基本线程函数：创建和终止：连接“选择并发范式”的接口、状态与分组证据。
- 使用线程的str_cli函数：连接“建立工作单元”的接口、状态与分组证据。
- 使用线程的TCP回射服务器程序：连接“分派连接”的接口、状态与分组证据。
- 线程特定数据：连接“同步共享状态”的接口、状态与分组证据。
- Web客户与同时连接：连接“施加过载”的接口、状态与分组证据。
- 本页11个正式目录层级全部进入可重放网络实验链。

## 术语表

<Glossary>
  <GlossaryItem term="基本线程函数：创建和终止">
    基本线程函数：创建和终止用于解释“选择并发范式”，需要同时标出协议版本、地址与端点、状态机、首包分叉和清理方法。
  </GlossaryItem>
  <GlossaryItem term="使用线程的str_cli函数">
    使用线程的str_cli函数用于解释“建立工作单元”，需要同时标出协议版本、地址与端点、状态机、首包分叉和清理方法。
  </GlossaryItem>
  <GlossaryItem term="使用线程的TCP回射服务器程序">
    使用线程的TCP回射服务器程序用于解释“分派连接”，需要同时标出协议版本、地址与端点、状态机、首包分叉和清理方法。
  </GlossaryItem>
  <GlossaryItem term="线程特定数据">
    线程特定数据用于解释“同步共享状态”，需要同时标出协议版本、地址与端点、状态机、首包分叉和清理方法。
  </GlossaryItem>
  <GlossaryItem term="Web客户与同时连接">
    Web客户与同时连接用于解释“施加过载”，需要同时标出协议版本、地址与端点、状态机、首包分叉和清理方法。
  </GlossaryItem>
</Glossary>

<Attribution
  mode="independent-rewrite"
  sourceBasis="authorized-sample"
  workTitle="W. Richard Stevens, Bill Fenner, Andrew M. Rudoff, UNIX Network Programming, Volume 1: The Sockets Networking API, Third Edition"
  adaptedUrl="https://www.informit.com/store/unix-network-programming-volume-1-the-sockets-networking-9780131411555"
/>
