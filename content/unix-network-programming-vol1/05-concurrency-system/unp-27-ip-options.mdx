---
title: "第27章 IP选项"
type: C
section: "并发、原始网络与设计范式"
order: 27
description: "第27章 IP选项围绕IPv4选项、IPv4源路径选项、IPv6扩展首部、IPv6步跳选项和目的地选项、IPv6路由首部、IPv6粘附选项建立从套接字接口到分组、内核状态与清理结果的证据链。 覆盖10个正式目录层级。"
demo: true
math: false
sourceUrl: "https://www.informit.com/store/unix-network-programming-volume-1-the-sockets-networking-9780131411555"
draft: false
qualityVersion: 2
practiceMode: code
sourceMode: independent-rewrite
---

import {
  Unp27IpOptionsMapLab,
  Unp27IpOptionsExperimentLab,
  Unp27IpOptionsEvidenceLab,
} from "@/components/mdx/unix-network-programming-vol1/diagrams/unp-27-ip-options";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第27章 IP选项的协议边界、套接字对象、状态机和平台版本限制
- 能描述“确认最小权限 → 构造首部 → 计算长度校验 → 发送捕获 → 解析响应 → 撤销权限”的地址、返回值、errno、系统调用与分组证据
- 能实现一个隔离、单变量、可清理、可重放的UNIX网络实验
- 能比较基线、边界、故障和恢复轨迹并判断“首部长度、字节序、校验和与链路类型一致，捕获过滤边界明确，实验只使用最小网络权限。”是否成立

</Objectives>

## 为什么同时看API、内核状态与分组

<Term def="IPv4选项连接确认最小权限阶段的接口合同、套接字状态、分组轨迹和清理证据。">
  IPv4选项
</Term>
、
<Term def="IPv4源路径选项连接构造首部阶段的接口合同、套接字状态、分组轨迹和清理证据。">
  IPv4源路径选项
</Term>
、
<Term def="IPv6扩展首部连接计算长度校验阶段的接口合同、套接字状态、分组轨迹和清理证据。">
  IPv6扩展首部
</Term>
、
<Term def="IPv6步跳选项和目的地选项连接发送捕获阶段的接口合同、套接字状态、分组轨迹和清理证据。">
  IPv6步跳选项和目的地选项
</Term>
、
<Term def="IPv6路由首部连接解析响应阶段的接口合同、套接字状态、分组轨迹和清理证据。">
  IPv6路由首部
</Term>
共同构成本页坐标。第27章
IP选项围绕IPv4选项、IPv4源路径选项、IPv6扩展首部、IPv6步跳选项和目的地选项、IPv6路由首部、IPv6粘附选项建立从套接字接口到分组、内核状态与清理结果的证据链。

在“第27章 IP选项”中，第3版覆盖IPv4、IPv6、TCP、UDP、SCTP、路由与密钥管理套接字，并保留STREAMS等历史接口。正文先还原书中标准和测试平台语境；Linux专有工具只用于观测与现代对照，不能冒充POSIX或协议标准保证。SCTP、STREAMS、T/TCP和历史IPv6 API的可用性必须由当前平台实测确认。

“第27章 IP选项”未取得未获授权的完整中文正文；以 [InformIT 第三版官方目录与合法样章](https://www.informit.com/store/unix-network-programming-volume-1-the-sockets-networking-9780131411555) 界定 3 部分、31 章和 5 个附录范围，中文解释、代码、实验与练习均为独立教学重写。“第27章 IP选项”的接口与协议事实分别以 [POSIX socket](https://pubs.opengroup.org/onlinepubs/9799919799/functions/socket.html)、[RFC 9293 TCP](https://www.rfc-editor.org/rfc/rfc9293.html)、[RFC 768 UDP](https://www.rfc-editor.org/rfc/rfc768.html)、[RFC 9260 SCTP](https://www.rfc-editor.org/rfc/rfc9260.html)、[RFC 6458 SCTP sockets](https://www.rfc-editor.org/rfc/rfc6458.html) 和 [RFC 8200 IPv6](https://www.rfc-editor.org/rfc/rfc8200.html) 核对；现代差异不覆盖第三版历史语境。

## 六阶段网络实验链

### 1. 确认最小权限

围绕IPv4选项记录内核、命名空间、接口、地址、路由、端口、描述符、返回值、errno和预期分组。进入“构造首部”前保存第一条偏离，清理与恢复同样纳入结果。

### 2. 构造首部

围绕IPv4源路径选项记录内核、命名空间、接口、地址、路由、端口、描述符、返回值、errno和预期分组。进入“计算长度校验”前保存第一条偏离，清理与恢复同样纳入结果。

### 3. 计算长度校验

围绕IPv6扩展首部记录内核、命名空间、接口、地址、路由、端口、描述符、返回值、errno和预期分组。进入“发送捕获”前保存第一条偏离，清理与恢复同样纳入结果。

### 4. 发送捕获

围绕IPv6步跳选项和目的地选项记录内核、命名空间、接口、地址、路由、端口、描述符、返回值、errno和预期分组。进入“解析响应”前保存第一条偏离，清理与恢复同样纳入结果。

### 5. 解析响应

围绕IPv6路由首部记录内核、命名空间、接口、地址、路由、端口、描述符、返回值、errno和预期分组。进入“撤销权限”前保存第一条偏离，清理与恢复同样纳入结果。

### 6. 撤销权限

围绕IPv4选项记录内核、命名空间、接口、地址、路由、端口、描述符、返回值、errno和预期分组。进入“确认最小权限”前保存第一条偏离，清理与恢复同样纳入结果。

## 协议、对象与故障模型

第27章 IP选项围绕IPv4选项、IPv4源路径选项、IPv6扩展首部、IPv6步跳选项和目的地选项、IPv6路由首部、IPv6粘附选项建立从套接字接口到分组、内核状态与清理结果的证据链。

本页核心不变量是：**首部长度、字节序、校验和与链路类型一致，捕获过滤边界明确，实验只使用最小网络权限。** 它同时约束正常、边界、故障和恢复样本。应用函数返回成功只是一条证据，还要检查地址长度、字节序、短计数、EINTR/EAGAIN、套接字状态、内核队列、分组顺序与异步清理。

“第27章 IP选项”的“第27章 IP选项”的实验台账至少包含内核/库版本、命名空间、接口与路由、地址端口、入口参数、描述符所有者、预期状态、实际返回与errno、首个分组分叉、内核队列和清理结果十一列。性能样本还固定并发度、连接复用、预热、输入规模、采样窗口和延迟分位；抓包必须记录过滤器、snaplen与时间基准。

## 可重放命令骨架

```bash
getcap ./raw-probe
capsh --print | grep cap_net_raw
```

```bash
tcpdump -ni any -s0 -w raw.pcap 'icmp or arp'
```

```bash
tshark -r raw.pcap -V | sed -n '1,120p'
```

<Callout type="trap">
  **安全边界：**
  “第27章 IP选项”涉及的“第27章 IP选项”涉及的原始套接字、路由、密钥管理、广播多播和故障注入只在隔离网络命名空间、容器或可恢复虚拟机执行。先保存原始接口、路由和流量规则。
</Callout>

## 第三版机制逐项深读

### 第27章 IP选项

在“第27章 IP选项”中，“第27章 IP选项”明确地址族、首部与路径 MTU 的协议差异；双栈映射和 IPv6 扩展首部的可用性不能靠结构体外观猜测。

### 27.1 概述

在“第27章 IP选项”中，“27.1 概述”是结构节点，用来组织“第27章 IP选项围绕IPv4选项、IPv4源路径选项、IPv6扩展首部、IPv6步跳选项和目的地选项、IPv6路由首部、IPv6粘附选项建立从套接字接口到分组、内核状态与清理结果的证据链。 覆盖10个正式目录层级。”的接口、内核与分组证据；若只能复述标题而不能给出返回值或状态因果，本节点不通过。

### 27.2 IPv4选项

在“第27章 IP选项”中，验证“27.2 IPv4选项”固定应用负载，只改变地址族、接口或 MTU，比较解析候选、套接字选项、ICMP 错误和实际分组。

### 27.3 IPv4源路径选项

在“第27章 IP选项”中，验证“27.3 IPv4源路径选项”固定应用负载，只改变地址族、接口或 MTU，比较解析候选、套接字选项、ICMP 错误和实际分组。

### 27.4 IPv6扩展首部

在“第27章 IP选项”中，“27.4 IPv6扩展首部”明确地址族、首部与路径 MTU 的协议差异；双栈映射和 IPv6 扩展首部的可用性不能靠结构体外观猜测。

### 27.5 IPv6步跳选项和目的地选项

在“第27章 IP选项”中，“27.5 IPv6步跳选项和目的地选项”的现代语义以 RFC 8200 等规范核对，但原书中的历史 API 仍按第三版语境说明，不用新名称改写旧接口。

### 27.6 IPv6路由首部

在“第27章 IP选项”中，“27.6 IPv6路由首部”明确地址族、首部与路径 MTU 的协议差异；双栈映射和 IPv6 扩展首部的可用性不能靠结构体外观猜测。

### 27.7 IPv6粘附选项

在“第27章 IP选项”中，分析“27.7 IPv6粘附选项”要保存地址、作用域、接口索引、首部链与内核选项，区分源代码可移植和运行时互操作。

### 27.8 历史性IPv6高级API

在“第27章 IP选项”中，“27.8 历史性IPv6高级API”明确地址族、首部与路径 MTU 的协议差异；双栈映射和 IPv6 扩展首部的可用性不能靠结构体外观猜测。

### 27.9 小结

在“第27章 IP选项”中，“27.9 小结”是结构节点，用来组织“第27章 IP选项围绕IPv4选项、IPv4源路径选项、IPv6扩展首部、IPv6步跳选项和目的地选项、IPv6路由首部、IPv6粘附选项建立从套接字接口到分组、内核状态与清理结果的证据链。 覆盖10个正式目录层级。”的接口、内核与分组证据；若只能复述标题而不能给出返回值或状态因果，本节点不通过。
## 分步视觉验证

先预测地址、套接字状态和分组，再动手试一试；若结果不同，停在第一条API、内核队列或线上分组分叉。

<Stepper>
  <Step title="画端点与状态机">
    先预测IPv4选项、IPv4源路径选项、IPv6扩展首部、IPv6步跳选项和目的地选项、IPv6路由首部的所有权、状态转换和分组方向。
    <Unp27IpOptionsMapLab />
  </Step>
  <Step title="运行单变量网络实验">
    固定命名空间、接口、路由、地址和输入，只切换一个边界或故障。
    <Unp27IpOptionsExperimentLab />
  </Step>
  <Step title="核对首包与恢复">
    保存原始轨迹，关闭端点并撤销网络设置后用相同输入重放。
    <Unp27IpOptionsEvidenceLab />
  </Step>
</Stepper>

## 常见误区

<Callout type="trap">
  **误区一：**
  只看应用最终文本，不检查短读写、errno、套接字状态、内核队列与线上分组是否一致。
</Callout>

<Callout type="trap">
  **误区二：**
  在“第27章 IP选项”中，把TCP当消息协议、把UDP当可靠流，或把Linux专有行为当成跨UNIX与协议标准保证，都会直接判错。
</Callout>

## 四类样本与验收

| 样本 | 只改变什么                 | 必查证据              | 通过条件   |
| ---- | -------------------------- | --------------------- | ---------- |
| 基线 | 固定网络、地址与输入       | API、状态、队列与分组 | 与预测一致 |
| 边界 | 缓冲、MTU、并发或时序      | 首个阻塞、截断或拒绝  | 边界可解释 |
| 故障 | 丢包、复位、信号或路径故障 | errno、首包与责任层   | 故障可定位 |
| 恢复 | 关闭、回收、撤销网络设置   | 残留端点与同输入重放  | 回到基线   |

## 练习

<Exercises>

**问题 1** 为什么不能只凭最终输出验收第27章 IP选项？

<Answer>
  “第27章 IP选项”的“第27章 IP选项”的最终输出会隐藏短读写、errno、地址错误、队列积压、重传、分组重排和描述符泄漏。必须同时核对接口合同、套接字生存期、内核状态、首个分组分叉和恢复结果。
</Answer>

**问题 2** 如何把“计算长度校验”变成网络证据？

<Answer>
  固定内核、命名空间、接口、路由、地址端口和输入，预测IPv4选项与IPv4源路径选项的返回值、状态和分组，只改变一个条件并停在首错或首包分叉。
</Answer>

**问题 3** 本页最小通过条件是什么？

<Answer>
  首部长度、字节序、校验和与链路类型一致，捕获过滤边界明确，实验只使用最小网络权限。
  关闭端点并恢复网络设置后，相同输入必须重新得到基线结果。
</Answer>

</Exercises>

## 小结

- IPv4选项：连接“确认最小权限”的接口、状态与分组证据。
- IPv4源路径选项：连接“构造首部”的接口、状态与分组证据。
- IPv6扩展首部：连接“计算长度校验”的接口、状态与分组证据。
- IPv6步跳选项和目的地选项：连接“发送捕获”的接口、状态与分组证据。
- IPv6路由首部：连接“解析响应”的接口、状态与分组证据。
- 本页10个正式目录层级全部进入可重放网络实验链。

## 术语表

<Glossary>
  <GlossaryItem term="IPv4选项">
    IPv4选项用于解释“确认最小权限”，需要同时标出协议版本、地址与端点、状态机、首包分叉和清理方法。
  </GlossaryItem>
  <GlossaryItem term="IPv4源路径选项">
    IPv4源路径选项用于解释“构造首部”，需要同时标出协议版本、地址与端点、状态机、首包分叉和清理方法。
  </GlossaryItem>
  <GlossaryItem term="IPv6扩展首部">
    IPv6扩展首部用于解释“计算长度校验”，需要同时标出协议版本、地址与端点、状态机、首包分叉和清理方法。
  </GlossaryItem>
  <GlossaryItem term="IPv6步跳选项和目的地选项">
    IPv6步跳选项和目的地选项用于解释“发送捕获”，需要同时标出协议版本、地址与端点、状态机、首包分叉和清理方法。
  </GlossaryItem>
  <GlossaryItem term="IPv6路由首部">
    IPv6路由首部用于解释“解析响应”，需要同时标出协议版本、地址与端点、状态机、首包分叉和清理方法。
  </GlossaryItem>
</Glossary>

<Attribution
  mode="independent-rewrite"
  sourceBasis="authorized-sample"
  workTitle="W. Richard Stevens, Bill Fenner, Andrew M. Rudoff, UNIX Network Programming, Volume 1: The Sockets Networking API, Third Edition"
  adaptedUrl="https://www.informit.com/store/unix-network-programming-volume-1-the-sockets-networking-9780131411555"
/>
