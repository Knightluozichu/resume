---
title: "第30章 客户/服务器程序设计范式"
type: C
section: "并发、原始网络与设计范式"
order: 30
description: "第30章 客户/服务器程序设计范式围绕TCP客户程序设计范式、TCP测试用客户程序、TCP迭代服务器程序、TCP并发服务器程序，每个客户一个子进程、TCP预先派生子进程服务器程序，accept无上锁保护、TCP预先派生子进程服务器程序，accept使用文件上锁保护建立从套接字接口到分组、内核状态与清理结果的证据链。 覆盖14个正式目录层级。"
demo: true
math: false
sourceUrl: "https://www.informit.com/store/unix-network-programming-volume-1-the-sockets-networking-9780131411555"
draft: true
qualityVersion: 2
practiceMode: code
sourceMode: independent-rewrite
---

import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第30章 客户/服务器程序设计范式的协议边界、套接字对象、状态机和平台版本限制
- 能描述“选择并发范式 → 建立工作单元 → 分派连接 → 同步共享状态 → 施加过载 → 停机回收”的地址、返回值、errno、系统调用与分组证据
- 能实现一个隔离、单变量、可清理、可重放的UNIX网络实验
- 能比较基线、边界、故障和恢复轨迹并判断“连接所有权唯一，锁序和条件谓词明确，过载有界，停机后进程、线程和描述符全部归零。”是否成立

</Objectives>

## 为什么同时看API、内核状态与分组

<Term def="TCP客户程序设计范式连接选择并发范式阶段的接口合同、套接字状态、分组轨迹和清理证据。">TCP客户程序设计范式</Term>、<Term def="TCP测试用客户程序连接建立工作单元阶段的接口合同、套接字状态、分组轨迹和清理证据。">TCP测试用客户程序</Term>、<Term def="TCP迭代服务器程序连接分派连接阶段的接口合同、套接字状态、分组轨迹和清理证据。">TCP迭代服务器程序</Term>、<Term def="TCP并发服务器程序，每个客户一个子进程连接同步共享状态阶段的接口合同、套接字状态、分组轨迹和清理证据。">TCP并发服务器程序，每个客户一个子进程</Term>、<Term def="TCP预先派生子进程服务器程序，accept无上锁保护连接施加过载阶段的接口合同、套接字状态、分组轨迹和清理证据。">TCP预先派生子进程服务器程序，accept无上锁保护</Term>
共同构成本页坐标。第30章
客户/服务器程序设计范式围绕TCP客户程序设计范式、TCP测试用客户程序、TCP迭代服务器程序、TCP并发服务器程序，每个客户一个子进程、TCP预先派生子进程服务器程序，accept无上锁保护、TCP预先派生子进程服务器程序，accept使用文件上锁保护建立从套接字接口到分组、内核状态与清理结果的证据链。

在“第30章 客户/服务器程序设计范式”中，第3版覆盖IPv4、IPv6、TCP、UDP、SCTP、路由与密钥管理套接字，并保留STREAMS等历史接口。正文先还原书中标准和测试平台语境；Linux专有工具只用于观测与现代对照，不能冒充POSIX或协议标准保证。SCTP、STREAMS、T/TCP和历史IPv6 API的可用性必须由当前平台实测确认。

“第30章 客户/服务器程序设计范式”未取得未获授权的完整中文正文；以 [InformIT 第三版官方目录与合法样章](https://www.informit.com/store/unix-network-programming-volume-1-the-sockets-networking-9780131411555) 界定 3 部分、31 章和 5 个附录范围，中文解释、代码、实验与练习均为独立教学重写。“第30章 客户/服务器程序设计范式”的接口与协议事实分别以 [POSIX socket](https://pubs.opengroup.org/onlinepubs/9799919799/functions/socket.html)、[RFC 9293 TCP](https://www.rfc-editor.org/rfc/rfc9293.html)、[RFC 768 UDP](https://www.rfc-editor.org/rfc/rfc768.html)、[RFC 9260 SCTP](https://www.rfc-editor.org/rfc/rfc9260.html)、[RFC 6458 SCTP sockets](https://www.rfc-editor.org/rfc/rfc6458.html) 和 [RFC 8200 IPv6](https://www.rfc-editor.org/rfc/rfc8200.html) 核对；现代差异不覆盖第三版历史语境。

## 常见误区

<Callout type="trap">
  **误区一：**
  只看应用最终文本，不检查短读写、errno、套接字状态、内核队列与线上分组是否一致。
</Callout>

<Callout type="trap">
  **误区二：**
  在“第30章 客户/服务器程序设计范式”中，把TCP当消息协议、把UDP当可靠流，或把Linux专有行为当成跨UNIX与协议标准保证，都会直接判错。
</Callout>

## 四类样本与验收

| 样本 | 只改变什么                 | 必查证据              | 通过条件   |
| ---- | -------------------------- | --------------------- | ---------- |
| 基线 | 固定网络、地址与输入       | API、状态、队列与分组 | 与预测一致 |
| 边界 | 缓冲、MTU、并发或时序      | 首个阻塞、截断或拒绝  | 边界可解释 |
| 故障 | 丢包、复位、信号或路径故障 | errno、首包与责任层   | 故障可定位 |
| 恢复 | 关闭、回收、撤销网络设置   | 残留端点与同输入重放  | 回到基线   |

## 练习

<Exercises>

**问题 1** 为什么不能只凭最终输出验收第30章 客户/服务器程序设计范式？

<Answer>
  “第30章 客户/服务器程序设计范式”的“第30章 客户/服务器程序设计范式”的最终输出会隐藏短读写、errno、地址错误、队列积压、重传、分组重排和描述符泄漏。必须同时核对接口合同、套接字生存期、内核状态、首个分组分叉和恢复结果。
</Answer>

**问题 2** 如何把“分派连接”变成网络证据？

<Answer>
  固定内核、命名空间、接口、路由、地址端口和输入，预测TCP客户程序设计范式与TCP测试用客户程序的返回值、状态和分组，只改变一个条件并停在首错或首包分叉。
</Answer>

**问题 3** 本页最小通过条件是什么？

<Answer>
  连接所有权唯一，锁序和条件谓词明确，过载有界，停机后进程、线程和描述符全部归零。
  关闭端点并恢复网络设置后，相同输入必须重新得到基线结果。
</Answer>

</Exercises>

## 小结

- TCP客户程序设计范式：连接“选择并发范式”的接口、状态与分组证据。
- TCP测试用客户程序：连接“建立工作单元”的接口、状态与分组证据。
- TCP迭代服务器程序：连接“分派连接”的接口、状态与分组证据。
- TCP并发服务器程序，每个客户一个子进程：连接“同步共享状态”的接口、状态与分组证据。
- TCP预先派生子进程服务器程序，accept无上锁保护：连接“施加过载”的接口、状态与分组证据。
- 本页14个正式目录层级全部进入可重放网络实验链。

## 术语表

<Glossary>
  <GlossaryItem term="TCP客户程序设计范式">
    TCP客户程序设计范式用于解释“选择并发范式”，需要同时标出协议版本、地址与端点、状态机、首包分叉和清理方法。
  </GlossaryItem>
  <GlossaryItem term="TCP测试用客户程序">
    TCP测试用客户程序用于解释“建立工作单元”，需要同时标出协议版本、地址与端点、状态机、首包分叉和清理方法。
  </GlossaryItem>
  <GlossaryItem term="TCP迭代服务器程序">
    TCP迭代服务器程序用于解释“分派连接”，需要同时标出协议版本、地址与端点、状态机、首包分叉和清理方法。
  </GlossaryItem>
  <GlossaryItem term="TCP并发服务器程序，每个客户一个子进程">
    TCP并发服务器程序，每个客户一个子进程用于解释“同步共享状态”，需要同时标出协议版本、地址与端点、状态机、首包分叉和清理方法。
  </GlossaryItem>
  <GlossaryItem term="TCP预先派生子进程服务器程序，accept无上锁保护">
    TCP预先派生子进程服务器程序，accept无上锁保护用于解释“施加过载”，需要同时标出协议版本、地址与端点、状态机、首包分叉和清理方法。
  </GlossaryItem>
</Glossary>

<Attribution
  mode="independent-rewrite"
  sourceBasis="authorized-sample"
  workTitle="W. Richard Stevens, Bill Fenner, Andrew M. Rudoff, UNIX Network Programming, Volume 1: The Sockets Networking API, Third Edition"
  adaptedUrl="https://www.informit.com/store/unix-network-programming-volume-1-the-sockets-networking-9780131411555"
/>
