---
title: 网络安全
type: A
section: 规控与系统工程
order: 11
description: 车载攻击面、SecOC 报文安全、安全启动、HSM 与入侵检测，回答车如何防被黑。
demo: true
math: false
sourceUrl: "https://www.autosar.org/standards"
draft: false
---

import { VsiCybersecurityDiagram } from "@/components/mdx/vehicle-software/diagrams/vsi-cybersecurity";
import {
  OfficialVehicleSoftwareIntelligenceCybersecurityMapLab,
  OfficialVehicleSoftwareIntelligenceCybersecurityExperimentLab,
  OfficialVehicleSoftwareIntelligenceCybersecurityEvidenceLab,
} from "@/components/mdx/vehicle-software-intelligence/diagrams/cybersecurity";
import {
  Objectives,
  Callout,
  Attribution,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
} from "@/components/mdx/mdx-components";

{/* OFFICIAL_COURSE_ENHANCEMENT */}

<Objectives>

- 能解释网络安全的输入、核心结构、状态变化和输出合同
- 能绘制并修改网络安全的最小模型，比较至少一种替代方案
- 能复现正常、边界和单故障样本，定位第一条证据分叉
- 能根据成本与风险判断方案适用范围，并完成清理重建

</Objectives>

## 为什么只记结论不足以掌握网络安全

学习“网络安全”时，第一步不是记住结论，而是冻结输入、上下文、版本和成功标准。只有这些条件明确，网络安全的含义才不会随着样例变化。正文已有的概念说明要与结构图、运行轨迹和失败样本互相印证，不能只凭最终输出看似正确就宣布完成。

结构分析从车载攻击面开始：列出参与者、职责、连接方向、生命周期和所有权，再沿正常路径追踪数据或控制流。每一条边都要说明为什么存在、谁创建、谁消费、谁负责清理；如果边界被跨越，必须能在证据中找到第一处异常。

机制验证要把三道防线写成可以执行的条件。正常样本证明主路径，恰好边界样本验证等号和空值，单故障样本只破坏一个假设。三类样本使用同一份观察指标，避免因为测试口径变化而把偶然结果误认成规律。

成本分析同时记录时间、空间、延迟、耦合、可维护性和不可逆操作。入侵检测不是一句“可能失败”，而是可复现输入、预期停点、实际轨迹、错误分类与清理步骤。任何自动重试都要有次数、预算和幂等边界。

方案比较不能只列优点。需要给出直接实现、当前方案和至少一个替代方案，逐项比较复杂度、扩展点、故障隔离和团队认知成本。当问题规模很小或变化轴稳定时，更简单的实现往往更好；模式与框架必须由真实变化压力证明。

实现阶段把大结论拆成可检查的中间产物：配置快照、结构清单、状态转移、输入输出样本、日志摘要和测试结果。每个产物带来源与生成命令，下一阶段只消费已通过门禁的版本，避免旧缓存或隐式默认值污染结论。

解释结果时必须区分相关性与因果性、接口承诺与实现细节、设计意图与运行事实。对“小结与练习”的判断要由独立证据支持，并明确适用范围；一旦输入分布、版本、硬件或组织边界改变，就重新运行最小实验。

复盘从首个分叉开始，而不是从最后一个报错倒推。先比较冻结输入，再比较第一份结构化中间产物，随后检查状态、约束和副作用。这样可以把复杂系统的排错范围收缩到一个阶段，避免在多个层次同时修改造成新的不确定性。

迁移到真实项目时，先选择一个最小但有代表性的切片，保存改造前基线，再逐步引入“网络安全”中的机制。每一步只改变一个变量并保留回滚点；性能、正确性、安全性和可理解性至少各有一项可量化指标。

最终验收要求读者能脱离页面重新画出结构、口述关键链路、实现最小版本、构造一个反例并解释失败位置。若只能复述名词而不能预测中间状态，说明知识仍停留在识记层，需要回到图示和实验重新验证。

本页用<Term def="网络安全连接网络安全的机制、边界与可复查证据。">网络安全</Term>、

<Term def="车载攻击面连接网络安全的机制、边界与可复查证据。">车载攻击面</Term>、<Term def="三道防线连接网络安全的机制、边界与可复查证据。">三道防线</Term>、<Term def="入侵检测连接网络安全的机制、边界与可复查证据。">入侵检测</Term>、<Term def="小结与练习连接网络安全的机制、边界与可复查证据。">小结与练习</Term>
建立统一坐标。先预测这些概念在结构图和运行轨迹中的位置，再操作实验控件；如果结果与预测不一致，停止在首个分叉，不要用后续补丁掩盖早期错误。

## 权威目录与核心概念逐项对照

- 网络安全
- 车载攻击面
- 三道防线
- 入侵检测
- 小结与练习
- 名词解释

## 三视图实验：结构、变量与证据

<Stepper>
  <Step title="第一步：冻结结构并先预测">
    先预测参与者、连接方向与关键状态，再点击结构节点核对。
    <OfficialVehicleSoftwareIntelligenceCybersecurityMapLab />
  </Step>
  <Step title="第二步：只改变一个变量">
    保持其他条件不变，移动覆盖率或风险滑块，观察门禁变化。
    <OfficialVehicleSoftwareIntelligenceCybersecurityExperimentLab />
  </Step>
  <Step title="第三步：注入单故障并保存证据">
    注入一个边界故障，记录第一处偏离、拒绝原因和恢复条件。
    <OfficialVehicleSoftwareIntelligenceCybersecurityEvidenceLab />
  </Step>
</Stepper>

<Callout type="trap">
  <strong>误区：</strong>
  看到最终结果正确就认为机制正确。中间状态可能已经偏离，只是被后续默认值或重试偶然修补。应保存首个结构化产物并逐阶段比较。
</Callout>

<Callout type="trap">
  <strong>误区：</strong>
  为了使用模式或框架而制造抽象。若变化轴、失败边界和替代方案没有写清，新增层次只会扩大认知成本和故障面。
</Callout>

## 可复现的最小实现

先把决策记录写成机器可读结构：

```json
{
  "unit": "网络安全",
  "inputFrozen": true,
  "scenario": "normal | boundary | single-fault",
  "firstDivergence": null,
  "cleanupRequired": true
}
```

再用同一条执行链处理三类样本：

```ts
type Evidence = { stage: string; expected: string; actual: string };

function verify(sample: unknown, expected: readonly Evidence[]) {
  const trace = runFromCleanState(sample);
  return expected.find((item, index) => trace[index]?.actual !== item.expected);
}
```

最后保存回归门禁，禁止失败样本静默通过：

```text
normal      -> complete, invariant preserved
boundary    -> complete or explicit rejection
singleFault -> stop at first divergence, no stale output
```

用统一评分解释实验结果：

$$
Q = C_{correct} + C_{trace} + C_{recover} - R_{residual}
$$

$$
C_{coverage} = \frac{N_{verified}}{N_{official}}
$$

$$
R_{residual} = P(failure) \times I(impact)
$$

$$
Accept = (C_{coverage} \ge 0.90) \land (R_{residual} \le R_{budget})
$$

## 本章回顾

- 网络安全必须绑定冻结输入和明确成功标准。
- 车载攻击面必须能画成结构并沿边追踪责任。
- 三道防线要由正常、边界和单故障样本共同验证。
- 入侵检测必须保存第一处偏离和清理重建步骤。
- 小结与练习决定方案是否可以进入下一阶段。

<Exercises>

**问题 1**：为“网络安全”写出最小输入合同，并指出一个不能省略的边界条件。

<Answer>
  输入合同至少包含样本身份、版本、配置、成功标准和清理方式；边界条件必须能改变结构或状态，而不是只换一段描述。
</Answer>

**问题 2**：构造一个只破坏“三道防线”的单故障样本，第一条预期证据是什么？

<Answer>
  先冻结正常轨迹，只改变该条件；第一条证据应是最早依赖它的结构化中间产物，而不是最终错误消息。
</Answer>

**问题 3**：什么情况下不应采用本页方案？

<Answer>
  当变化轴稳定、直接实现更清晰、额外层次无法降低风险或提供独立测试边界时，应选择更简单方案并记录重新评估触发条件。
</Answer>

</Exercises>

## 术语表

<Glossary>
  <GlossaryItem term="网络安全">
    网络安全用于描述“网络安全”中的输入、结构、约束或证据边界，必须能在实验记录中定位。
  </GlossaryItem>
  <GlossaryItem term="车载攻击面">
    车载攻击面用于描述“网络安全”中的输入、结构、约束或证据边界，必须能在实验记录中定位。
  </GlossaryItem>
  <GlossaryItem term="三道防线">
    三道防线用于描述“网络安全”中的输入、结构、约束或证据边界，必须能在实验记录中定位。
  </GlossaryItem>
  <GlossaryItem term="入侵检测">
    入侵检测用于描述“网络安全”中的输入、结构、约束或证据边界，必须能在实验记录中定位。
  </GlossaryItem>
  <GlossaryItem term="小结与练习">
    小结与练习用于描述“网络安全”中的输入、结构、约束或证据边界，必须能在实验记录中定位。
  </GlossaryItem>
</Glossary>

<Attribution
  adaptedFrom="站内车载软件专题《车载软件与智能化》，依据AUTOSAR Classic/Adaptive/Foundation R25-11官方资料，2026 · 独立教学重构"
  adaptedUrl="https://www.autosar.org/standards"
/>

<Objectives>
  - 能列出车载主要攻击面（OTA、OBD、WiFi、T-Box）及其威胁特征 - 能解释
  SecOC、安全启动、HSM 三道防线各自防护的环节 - 能回答：IDS/IPS
  入侵检测为什么是车载安全的最后一道纵深防线？
</Objectives>

## 网络安全

功能安全防「自己出错」，网络安全防「被人攻击」。智能汽车联网后，攻击面从物理接触扩展到远程——一辆车可能被千里之外的黑客接管。车载网络安全用纵深防御把攻击层层拦截。

<VsiCybersecurityDiagram />

## 车载攻击面

<Term def="攻击者可利用来入侵车载系统的物理与远程入口的集合。">攻击面</Term>
是防御的起点。OTA
升级通道是远程入口——若升级验证被绕过，攻击者可刷入恶意固件。OBD
诊断口是物理入口——维修时直连总线，可注入报文。车机 WiFi
与蓝牙是近场入口——可被中间人或欺骗。T-Box（车载联网单元）是远程通信入口——连蜂窝网络，若被攻破可直达车内总线。

这些入口的共同威胁是「以点破面」：攻破一个弱入口后，若车内网络没分段隔离，攻击者可横向移动到刹车、转向等关键 ECU。所以防御第一原则是纵深与隔离——入口要硬，内部要分。

## 三道防线

<Term def="为车载报文加签名与新鲜度计数以防伪造与重放的安全机制。">SecOC</Term>
（Secure Onboard Communication）护报文安全。传统 CAN 报文无认证，谁都能发。SecOC
给每条关键报文附加
MAC（消息认证码）与计数器：发送方用密钥签名，接收方验签才执行；计数器防重放——旧报文重发计数器对不上即拒收。代价是增加总线负载与
ECU 算力开销，所以只用于安全关键报文。

<Term def="启动时逐级验证固件签名、拒绝运行被篡改固件的信任链机制。">安全启动</Term>
护固件完整。从 bootloader 起逐级验证下一级镜像的签名：bootloader
验内核、内核验驱动与应用，任一级验签失败即拒绝启动。信任根是烧在芯片里不可改的根公钥。安全启动保证车上跑的软件是
OEM 签名的正版，防刷恶意固件。

<Term def="隔离存储密钥、提供加解密与签名硬件加速的车载安全芯片。">HSM</Term>
（Hardware Security Module）护密钥。密钥不能存在普通内存里被随便读取，而要锁进
HSM 这个独立安全芯片——私钥永不出 HSM，签名与解密在 HSM 内完成。HSM 是 SecOC
签名与安全启动验签的信任根载体，被攻破软件也拿不到密钥。

## 入侵检测

<Term def="监测车内网络异常报文与行为、发现并响应攻击的纵深防御机制。">IDS/IPS</Term>
是最后一道纵深防线。前三道防「未授权的访问与篡改」，但零日漏洞或内部被攻破时仍可能漏过。IDS（入侵检测系统）监测车内总线流量——异常高频报文、未知
ID、不合规周期——发现可疑行为告警；IPS（入侵防御系统）进一步主动阻断，如切断被攻破
ECU 的总线通信。

IDS/IPS 的价值是「假定防线已被突破」的兜底：即使攻击者绕过了认证与签名，异常行为模式仍会被检测。它是纵深防御的最后一层，把「防不住」变成「至少能发现」。

<Callout type="tip">
  <strong>建议 →</strong> 设计车载安全按「入口加固 → 内部分隔 → 报文认证 →
  启动验签 → 密钥隔离 →
  异常检测」六层纵深。任何单层都会被绕过，多层叠加让攻击者要同时突破多道才得手，成本指数级上升。纵深比单点加固更可靠。
</Callout>

<Callout type="trap">
  <strong>现象 →</strong> 攻破一个弱 ECU 后横向控制刹车，整车被远程接管。
  <strong>原因 →</strong> 车内网络没分段隔离，弱入口（如车机）与关键
  ECU（刹车转向）在同一总线，攻击者可横向移动；且关键报文没上 SecOC、可被伪造。
  <strong>修法 →</strong> 车内网络按安全域分段、用网关隔离关键总线；关键报文上
  SecOC 验签防伪造；部署 IDS
  监测异常横向流量。单点加固挡不住横向移动，必须纵深隔离。
</Callout>

## 小结与练习

- 车载攻击面：OTA（远程固件）、OBD（物理总线）、WiFi/蓝牙（近场）、T-Box（远程通信）
- SecOC 给报文加签名与计数器防伪造重放；安全启动逐级验签防篡改固件；HSM 隔离密钥防窃取
- IDS/IPS 监测异常报文与行为，是「假定防线已被突破」的兜底纵深防线
- 纵深防御六层：入口加固→内部分隔→报文认证→启动验签→密钥隔离→异常检测

<Exercises>
  <Answer question="车载主要攻击面有哪些？为什么车内网络必须分段隔离？">
    攻击面包括 OTA 升级通道（远程刷固件）、OBD 诊断口（物理连总线）、车机
    WiFi/蓝牙（近场欺骗）、T-Box（远程通信直达车内）。共同威胁是「以点破面」：攻破一个弱入口后，若车内网络没分段，攻击者可横向移动到刹车转向等关键
    ECU。所以必须按安全域分段、用网关隔离关键总线，让攻破弱入口也无法直达关键控制，把单点突破限制在局部。
  </Answer>
  <Answer question="SecOC、安全启动、HSM、IDS/IPS 各自防护哪个环节？为什么 IDS 是最后防线？">
    SecOC 护报文——给关键报文加 MAC 签名与计数器防伪造与重放；安全启动护固件——从
    bootloader 逐级验签拒绝篡改镜像；HSM
    护密钥——密钥锁在独立芯片永不出、签名解密在内部完成；IDS/IPS
    护行为——监测异常报文与横向流量、发现并阻断攻击。前三道防未授权访问与篡改，但零日漏洞或内部被攻破时仍可能漏过，IDS
    假定防线已被突破、靠异常行为模式兜底，是纵深防御的最后一层，把「防不住」变成「至少能发现」。
  </Answer>
</Exercises>

## 名词解释

<Glossary>
  <GlossaryItem term="攻击面">
    攻击者可利用来入侵车载系统的物理与远程入口集合，含 OTA、OBD、WiFi、T-Box
    等，是防御的起点。
  </GlossaryItem>
  <GlossaryItem term="SecOC">
    Secure Onboard Communication，为车载报文加 MAC
    签名与新鲜度计数器以防伪造与重放的安全机制。
  </GlossaryItem>
  <GlossaryItem term="安全启动">
    启动时从 bootloader
    逐级验证固件签名、拒绝运行被篡改固件的信任链机制，信任根是芯片内不可改的根公钥。
  </GlossaryItem>
  <GlossaryItem term="HSM">
    Hardware Security
    Module，隔离存储密钥、提供加解密与签名硬件加速的车载安全芯片，是密钥不被窃取的信任根载体。
  </GlossaryItem>
  <GlossaryItem term="IDS/IPS">
    监测车内网络异常报文与行为、发现并响应攻击的纵深防御机制，是假定防线已被突破的兜底防线。
  </GlossaryItem>
</Glossary>

<Attribution
  adaptedFrom="智能汽车软件工程与自动驾驶技术专业资料"
  adaptedUrl=""
/>
