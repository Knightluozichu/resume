---
title: "第 14 章 内建组件和模块"
type: A
section: "Vue.js设计与实现 · 组件化"
order: 14
description: "KeepAlive、Teleport和Transition都需要渲染器配合：分别改写组件寿命、宿主位置和挂载卸载时序。 本页按权威目录独立教学重构。"
demo: true
math: false
sourceUrl: "https://time.geekbang.org/column/article/740758"
draft: false
---

import {
  Vdi14BuiltInComponentsModulesMapLab,
  Vdi14BuiltInComponentsModulesExperimentLab,
  Vdi14BuiltInComponentsModulesEvidenceLab,
} from "@/components/mdx/vuejs-design-implementation/diagrams/vdi-14-built-in-components-modules";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第 14 章 内建组件和模块的核心机制，并把它放回6篇18章的实现主线
- 能实现本页最小算法，比较正常、边界、失败和恢复轨迹
- 能指出依赖、VNode、组件实例、AST或请求状态的唯一所有者
- 能设计证据回答“缓存身份与淘汰明确，激活失活不重复挂载，Teleport目标切换保留逻辑父子关系，过渡结束或取消后类名和回调全部清理。”是否成立

</Objectives>

## 直觉：从输入、状态到提交

KeepAlive、Teleport和Transition都需要渲染器配合：分别改写组件寿命、宿主位置和挂载卸载时序。 本书不是对最终源码逐行翻译，而是从规范与最小模型逐步构造框架。只看最终DOM会隐藏错误依赖、重复移动、陈旧任务和激活错位；先画输入、状态所有者、调度点、输出和清理责任。

<Vdi14BuiltInComponentsModulesMapLab />

## 权威目录定位

作者授权数字版前言明确全书分为6篇18章，作者GitHub仓库提供配套代码。本页依照公开完整目录逐项覆盖 **11** 个条目；目录名称仅用于追踪，正文为独立教学重构。

- 14.1 KeepAlive组件的实现原理
- 14.1.1 组件的激活与失活
- 14.1.2 include和exclude
- 14.1.3 缓存管理
- 14.2 Teleport组件的实现原理
- 14.2.1 Teleport组件要解决的问题
- 14.2.2 实现Teleport组件
- 14.3 Transition组件的实现原理
- 14.3.1 原生DOM的过渡
- 14.3.2 实现Transition组件
- 14.4 总结

## 核心概念与所有权

<Term def="缓存非活动组件实例并协调激活与失活的内建组件。">KeepAlive</Term>、
<Term def="把子节点渲染到组件树之外指定宿主容器的内建组件。">Teleport</Term>、
<Term def="为节点进入与离开注入过渡钩子和类名的内建组件。">Transition</Term>、
<Term def="缓存组件重新进入或离开活动子树时的生命周期状态。">激活与失活</Term>、
<Term def="按include、exclude、key和容量决定组件实例保留与淘汰的策略。">
  缓存管理
</Term>
。每个概念都要回答：谁创建输入，哪个结构保存状态，何种操作可以修改，错误如何传播，生命周期结束后谁清理。

|   # | 概念       | 精确定义                                                    |
| --: | ---------- | ----------------------------------------------------------- |
|   1 | KeepAlive  | 缓存非活动组件实例并协调激活与失活的内建组件。              |
|   2 | Teleport   | 把子节点渲染到组件树之外指定宿主容器的内建组件。            |
|   3 | Transition | 为节点进入与离开注入过渡钩子和类名的内建组件。              |
|   4 | 激活与失活 | 缓存组件重新进入或离开活动子树时的生命周期状态。            |
|   5 | 缓存管理   | 按include、exclude、key和容量决定组件实例保留与淘汰的策略。 |

## 机制拆解

### 1. 建立机制边界

KeepAlive缓存组件VNode与实例，失活时移动到隐藏容器而非卸载，激活时移回。include/exclude按组件名筛选，缓存key结合类型和VNode key，容量策略决定淘汰时真正卸载。

先预测：如果省略这一层，依赖、节点身份、任务顺序或请求状态会在哪里首次偏离？

### 2. 处理关键算法

Teleport在逻辑组件树保留VNode关系，却把children挂载到to指定容器；目标变化时移动已有节点。它必须由渲染器调用process，否则普通组件无法控制宿主插入位置。

先预测：如果省略这一层，依赖、节点身份、任务顺序或请求状态会在哪里首次偏离？

### 3. 签发失败与恢复

Transition为单个子VNode注入beforeEnter、enter和leave钩子。浏览器双帧切换类名建立过渡起点，transitionend后清理；leave完成前不能卸载，取消和快速切换要消除旧回调。

先预测：如果省略这一层，依赖、节点身份、任务顺序或请求状态会在哪里首次偏离？

## 三阶段可视化推演

<Stepper>
  <Step title="1. 建立最小模型">
    固定运行时、输入和初始状态，先预测数据结构与执行顺序。
    <Vdi14BuiltInComponentsModulesMapLab />
  </Step>
  <Step title="2. 注入算法边界">
    只改变一个变量，观察分支切换、重复key、请求乱序、非法模板或激活不匹配的首偏离点。
    <Vdi14BuiltInComponentsModulesExperimentLab />
  </Step>
  <Step title="3. 清除故障并重放">
    用相同输入恢复，核对依赖集合、任务队列、宿主节点、组件实例和请求状态是否收敛。
    <Vdi14BuiltInComponentsModulesEvidenceLab />
  </Step>
</Stepper>

## 最小可运行实验

```js
const cache = new Map();
function deactivate(vnode) {
  move(vnode, storageContainer);
}
function activate(vnode, container, anchor) {
  move(vnode, container, anchor);
  patch(vnode, vnode);
}
```

保存结构化证据，不以一次console输出或最终截图替代过程：

```yaml
book: Vue.js设计与实现
page: vdi-14-built-in-components-modules
sample: normal | boundary | failure | recovery
runtime: pinned
input_fixture: fixed
first_divergence: null
cleanup_complete: true
```

故障注入一次只改变一个变量：

```text
baseline -> inject(one_fault) -> stop_at_first_divergence
recover -> replay_same_input
assert -> precise_dependency && stable_identity && ordered_tasks && no_retained_resource
```

## 常见误区与故障注入

<Callout type="trap">
  **误区一：KeepAlive只缓存DOM不缓存组件实例，重新激活后状态和生命周期不一致。**
  必须保存输入、执行轨迹和首偏离点，而不是只比较最终结果。
</Callout>

<Callout type="trap">
  **误区二：Transition立即卸载离开节点，CSS类名存在却没有任何可过渡的宿主对象。**
  必须加入失败与恢复样本，并检查依赖、节点、任务和状态是否清理。
</Callout>

## 验收矩阵

| 样本 | 变化                 | 预期         | 证据           |
| ---- | -------------------- | ------------ | -------------- |
| 正常 | 合法输入与稳定身份   | 主链完成     | 输出与轨迹一致 |
| 边界 | 空值、重复或极限序列 | 有界退化     | 不越界、不重复 |
| 失败 | 异常、乱序、非法结构 | 首偏离点停止 | 错误显式传播   |
| 恢复 | 清除故障后同输入重放 | 收敛到正常   | 无旧依赖和资源 |

<Vdi14BuiltInComponentsModulesEvidenceLab />

本页签发不变量：**缓存身份与淘汰明确，激活失活不重复挂载，Teleport目标切换保留逻辑父子关系，过渡结束或取消后类名和回调全部清理。**

## 练习

<Exercises>

**问题 1** 第 14 章 内建组件和模块的核心主张是什么？

<Answer>
  KeepAlive、Teleport和Transition都需要渲染器配合：分别改写组件寿命、宿主位置和挂载卸载时序。
</Answer>

**问题 2** 如何把本页目录变成可执行证据？

<Answer>
  按公开顺序建立追踪表，至少覆盖14.1 KeepAlive组件的实现原理、14.1.1
  组件的激活与失活、14.1.2 include和exclude、14.1.3 缓存管理、14.2
  Teleport组件的实现原理、14.2.1 Teleport组件要解决的问题、14.2.2
  实现Teleport组件；其余条目已在上方逐项列出，并为每组条目记录输入、状态、输出、失败与恢复条件。
</Answer>

**问题 3** 本页怎样签发？

<Answer>
  缓存身份与淘汰明确，激活失活不重复挂载，Teleport目标切换保留逻辑父子关系，过渡结束或取消后类名和回调全部清理。
  同一运行时与输入必须能重放正常、边界、失败和恢复轨迹。
</Answer>

</Exercises>

## 小结

- **KeepAlive**：缓存非活动组件实例并协调激活与失活的内建组件。
- **Teleport**：把子节点渲染到组件树之外指定宿主容器的内建组件。
- **Transition**：为节点进入与离开注入过渡钩子和类名的内建组件。
- **激活与失活**：缓存组件重新进入或离开活动子树时的生命周期状态。
- **缓存管理**：按include、exclude、key和容量决定组件实例保留与淘汰的策略。
- 官方目录、最小实现、故障注入与恢复证据已经放进同一验收链。

## 术语表

<Glossary>
  <GlossaryItem term="KeepAlive">
    缓存非活动组件实例并协调激活与失活的内建组件。
  </GlossaryItem>
  <GlossaryItem term="Teleport">
    把子节点渲染到组件树之外指定宿主容器的内建组件。
  </GlossaryItem>
  <GlossaryItem term="Transition">
    为节点进入与离开注入过渡钩子和类名的内建组件。
  </GlossaryItem>
  <GlossaryItem term="激活与失活">
    缓存组件重新进入或离开活动子树时的生命周期状态。
  </GlossaryItem>
  <GlossaryItem term="缓存管理">
    按include、exclude、key和容量决定组件实例保留与淘汰的策略。
  </GlossaryItem>
</Glossary>

## 来源与改编边界

<Attribution
  adaptedFrom="霍春阳著，人民邮电出版社，2022年1月，501页，ISBN 9787115583864。作者授权数字版前言确认6篇18章的结构与逐章范围，作者GitHub仓库提供配套代码，公开完整目录用于二三级条目追踪；正文为独立教学重构。"
  adaptedUrl="https://time.geekbang.org/column/article/740758"
/>

配套代码：https://github.com/HcySunYang/code-for-vue-3-book
