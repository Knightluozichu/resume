---
title: "第 15 章 编译器核心技术概览"
type: A
section: "Vue.js设计与实现 · 编译器"
order: 15
description: "模板编译器沿解析、模板AST转换、JavaScript AST生成和代码输出推进，插件通过进入与退出阶段安全改写树。 本页按权威目录独立教学重构。"
demo: true
math: false
sourceUrl: "https://time.geekbang.org/column/article/740758"
draft: false
---


## 直觉：从输入、状态到提交

模板编译器沿解析、模板AST转换、JavaScript AST生成和代码输出推进，插件通过进入与退出阶段安全改写树。 本书不是对最终源码逐行翻译，而是从规范与最小模型逐步构造框架。只看最终DOM会隐藏错误依赖、重复移动、陈旧任务和激活错位；先画输入、状态所有者、调度点、输出和清理责任。

## 权威目录定位

作者授权数字版前言明确全书分为6篇18章，作者GitHub仓库提供配套代码。本页依照公开完整目录逐项覆盖 **10** 个条目；目录名称仅用于追踪，正文为独立教学重构。

- 15.1 模板DSL的编译器
- 15.2 parser的实现原理与状态机
- 15.3 构造AST
- 15.4 AST的转换与插件化架构
- 15.4.1 节点的访问
- 15.4.2 转换上下文与节点操作
- 15.4.3 进入与退出
- 15.5 将模板AST转为JavaScript AST
- 15.6 代码生成
- 15.7 总结

## 核心概念与所有权

、、、、。
每个概念都要回答：谁创建输入，哪个结构保存状态，何种操作可以修改，错误如何传播，生命周期结束后谁清理。

|   # | 概念       | 精确定义                                                      |
| --: | ---------- | ------------------------------------------------------------- |
|   1 | DSL        | 为特定领域设计、可被专用编译器理解的描述语言。                |
|   2 | parser     | 把模板字符流按语法规则转换为模板AST的解析器。                 |
|   3 | AST        | 保留语法结构、位置和语义信息的抽象语法树。                    |
|   4 | 转换上下文 | 遍历AST时提供当前节点、父节点、替换删除和退出回调的状态对象。 |
|   5 | 代码生成   | 把JavaScript AST按语法和格式输出为可执行渲染函数源码的阶段。  |

## 机制拆解

### 1. 建立机制边界

parser把字符流识别为元素、文本、插值等节点，并保存位置。模板AST表达模板语义，不应在解析阶段过早混入JavaScript代码字符串，否则后续优化无法基于结构工作。

先预测：如果省略这一层，依赖、节点身份、任务顺序或请求状态会在哪里首次偏离？

### 2. 处理关键算法

transform深度优先遍历，转换插件可在进入阶段注册退出函数。退出阶段子节点已完成转换，适合根据children构造父节点codegenNode；上下文提供替换、删除和当前节点更新。

先预测：如果省略这一层，依赖、节点身份、任务顺序或请求状态会在哪里首次偏离？

### 3. 签发失败与恢复

模板AST转换为JavaScript AST后，generate只负责按节点类型输出函数声明、调用、数组和字符串。把语义决策留在转换阶段，让代码生成可以用结构化快照测试。

先预测：如果省略这一层，依赖、节点身份、任务顺序或请求状态会在哪里首次偏离？

## 三阶段可视化推演


## 最小可运行实验

```js
function traverseNode(node, context) {
  const exits = context.transforms.map((t) => t(node, context)).filter(Boolean);
  traverseChildren(node, context);
  let i = exits.length;
  while (i--) exits[i]();
}
```

保存结构化证据，不以一次console输出或最终截图替代过程：

```yaml
book: Vue.js设计与实现
page: vdi-15-compiler-core-overview
sample: normal | boundary | failure | recovery
runtime: pinned
input_fixture: fixed
first_divergence: null
cleanup_complete: true
```

故障注入一次只改变一个变量：

```text
baseline -> inject(one_fault) -> stop_at_first_divergence
recover -> replay_same_input
assert -> precise_dependency && stable_identity && ordered_tasks && no_retained_resource
```

## 常见误区与故障注入


## 验收矩阵

| 样本 | 变化                 | 预期         | 证据           |
| ---- | -------------------- | ------------ | -------------- |
| 正常 | 合法输入与稳定身份   | 主链完成     | 输出与轨迹一致 |
| 边界 | 空值、重复或极限序列 | 有界退化     | 不越界、不重复 |
| 失败 | 异常、乱序、非法结构 | 首偏离点停止 | 错误显式传播   |
| 恢复 | 清除故障后同输入重放 | 收敛到正常   | 无旧依赖和资源 |

本页签发不变量：**解析与生成分层，AST节点位置可追踪，插件遍历顺序确定，替换删除不跳节点，生成代码可重新解析并执行。**

## 练习


## 小结

- **DSL**：为特定领域设计、可被专用编译器理解的描述语言。
- **parser**：把模板字符流按语法规则转换为模板AST的解析器。
- **AST**：保留语法结构、位置和语义信息的抽象语法树。
- **转换上下文**：遍历AST时提供当前节点、父节点、替换删除和退出回调的状态对象。
- **代码生成**：把JavaScript AST按语法和格式输出为可执行渲染函数源码的阶段。
- 官方目录、最小实现、故障注入与恢复证据已经放进同一验收链。

## 术语表


## 来源与改编边界


配套代码：https://github.com/HcySunYang/code-for-vue-3-book
