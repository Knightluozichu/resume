---
title: "第 16 章 解析器"
type: A
section: "Vue.js设计与实现 · 编译器"
order: 16
description: "符合HTML语义的模板解析器必须让文本模式、祖先栈、递归下降、属性解析和字符引用解码共同约束何时开始与停止。 本页按权威目录独立教学重构。"
demo: true
math: false
sourceUrl: "https://time.geekbang.org/column/article/740758"
draft: false
---

import {
  Vdi16ParserMapLab,
  Vdi16ParserExperimentLab,
  Vdi16ParserEvidenceLab,
} from "@/components/mdx/vuejs-design-implementation/diagrams/vdi-16-parser";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第 16 章 解析器的核心机制，并把它放回6篇18章的实现主线
- 能实现本页最小算法，比较正常、边界、失败和恢复轨迹
- 能指出依赖、VNode、组件实例、AST或请求状态的唯一所有者
- 能设计证据回答“每次循环都消费输入或显式失败，结束标签与祖先匹配，文本模式切换恢复，属性和字符引用按上下文解码，错误包含位置。”是否成立

</Objectives>

## 直觉：从输入、状态到提交

符合HTML语义的模板解析器必须让文本模式、祖先栈、递归下降、属性解析和字符引用解码共同约束何时开始与停止。 本书不是对最终源码逐行翻译，而是从规范与最小模型逐步构造框架。只看最终DOM会隐藏错误依赖、重复移动、陈旧任务和激活错位；先画输入、状态所有者、调度点、输出和清理责任。

<Vdi16ParserMapLab />

## 权威目录定位

作者授权数字版前言明确全书分为6篇18章，作者GitHub仓库提供配套代码。本页依照公开完整目录逐项覆盖 **11** 个条目；目录名称仅用于追踪，正文为独立教学重构。

- 16.1 文本模式及其对解析器的影响
- 16.2 递归下降算法构造模板AST
- 16.3 状态机的开启与停止
- 16.4 解析标签节点
- 16.5 解析属性
- 16.6 解析文本与解码HTML实体
- 16.6.1 解析文本
- 16.6.2 解码命名字符引用
- 16.6.3 解码数字字符引用
- 16.7 解析插值与注释
- 16.8 总结

## 核心概念与所有权

<Term def="决定解析器当前如何识别标签、插值和字符引用的上下文状态。">文本模式</Term>、<Term def="每个语法结构由对应函数解析并递归处理嵌套内容的算法。">递归下降</Term>、<Term def="根据当前状态和输入字符决定转换与输出的解析模型。">状态机</Term>、<Term def="HTML文本中以命名或数字形式表示其他字符的编码。">字符引用</Term>、<Term def="模板中把表达式结果嵌入文本或属性位置的语法节点。">插值</Term>。
每个概念都要回答：谁创建输入，哪个结构保存状态，何种操作可以修改，错误如何传播，生命周期结束后谁清理。

|   # | 概念     | 精确定义                                                 |
| --: | -------- | -------------------------------------------------------- |
|   1 | 文本模式 | 决定解析器当前如何识别标签、插值和字符引用的上下文状态。 |
|   2 | 递归下降 | 每个语法结构由对应函数解析并递归处理嵌套内容的算法。     |
|   3 | 状态机   | 根据当前状态和输入字符决定转换与输出的解析模型。         |
|   4 | 字符引用 | HTML文本中以命名或数字形式表示其他字符的编码。           |
|   5 | 插值     | 模板中把表达式结果嵌入文本或属性位置的语法节点。         |

## 机制拆解

### 1. 建立机制边界

DATA、RCDATA、RAWTEXT和CDATA模式决定标签与插值是否有特殊含义。parseChildren根据当前模式和祖先栈判断是否遇到匹配结束标签，不能简单在看到任意&lt;/时停止。

先预测：如果省略这一层，依赖、节点身份、任务顺序或请求状态会在哪里首次偏离？

### 2. 处理关键算法

parseElement先解析开始标签，压入祖先，按标签切换文本模式，递归解析children，再要求对应结束标签。属性要处理引号值、无引号值、重复键和指令前缀，并保存源码位置。

先预测：如果省略这一层，依赖、节点身份、任务顺序或请求状态会在哪里首次偏离？

### 3. 签发失败与恢复

文本解析寻找下一个标签或插值边界，字符引用解码要区分命名和数字形式、分号和上下文歧义。插值提取表达式并修剪，注释按终止符消费；错误输入要前进避免死循环。

先预测：如果省略这一层，依赖、节点身份、任务顺序或请求状态会在哪里首次偏离？

## 三阶段可视化推演

<Stepper>
  <Step title="1. 建立最小模型">
    固定运行时、输入和初始状态，先预测数据结构与执行顺序。
    <Vdi16ParserMapLab />
  </Step>
  <Step title="2. 注入算法边界">
    只改变一个变量，观察分支切换、重复key、请求乱序、非法模板或激活不匹配的首偏离点。
    <Vdi16ParserExperimentLab />
  </Step>
  <Step title="3. 清除故障并重放">
    用相同输入恢复，核对依赖集合、任务队列、宿主节点、组件实例和请求状态是否收敛。
    <Vdi16ParserEvidenceLab />
  </Step>
</Stepper>

## 最小可运行实验

```js
function isEnd(context, ancestors) {
  if (!context.source) return true;
  return ancestors.some((node) => context.source.startsWith(`</${node.tag}`));
}
```

保存结构化证据，不以一次console输出或最终截图替代过程：

```yaml
book: Vue.js设计与实现
page: vdi-16-parser
sample: normal | boundary | failure | recovery
runtime: pinned
input_fixture: fixed
first_divergence: null
cleanup_complete: true
```

故障注入一次只改变一个变量：

```text
baseline -> inject(one_fault) -> stop_at_first_divergence
recover -> replay_same_input
assert -> precise_dependency && stable_identity && ordered_tasks && no_retained_resource
```

## 常见误区与故障注入

<Callout type="trap">
  **误区一：任何&lt;/都结束当前元素，嵌套标签遇到兄弟结束符时提前截断AST。** 必须保存输入、执行轨迹和首偏离点，而不是只比较最终结果。
</Callout>

<Callout type="trap">
  **误区二：非法输入不消费字符也不抛错，parseChildren在同一位置无限循环。**
  必须加入失败与恢复样本，并检查依赖、节点、任务和状态是否清理。
</Callout>

## 验收矩阵

| 样本 | 变化                 | 预期         | 证据           |
| ---- | -------------------- | ------------ | -------------- |
| 正常 | 合法输入与稳定身份   | 主链完成     | 输出与轨迹一致 |
| 边界 | 空值、重复或极限序列 | 有界退化     | 不越界、不重复 |
| 失败 | 异常、乱序、非法结构 | 首偏离点停止 | 错误显式传播   |
| 恢复 | 清除故障后同输入重放 | 收敛到正常   | 无旧依赖和资源 |

<Vdi16ParserEvidenceLab />

本页签发不变量：**每次循环都消费输入或显式失败，结束标签与祖先匹配，文本模式切换恢复，属性和字符引用按上下文解码，错误包含位置。**

## 练习

<Exercises>

**问题 1** 第 16 章 解析器的核心主张是什么？

<Answer>
  符合HTML语义的模板解析器必须让文本模式、祖先栈、递归下降、属性解析和字符引用解码共同约束何时开始与停止。
</Answer>

**问题 2** 如何把本页目录变成可执行证据？

<Answer>
  按公开顺序建立追踪表，至少覆盖16.1 文本模式及其对解析器的影响、16.2
  递归下降算法构造模板AST、16.3 状态机的开启与停止、16.4 解析标签节点、16.5
  解析属性、16.6 解析文本与解码HTML实体、16.6.1
  解析文本；其余条目已在上方逐项列出，并为每组条目记录输入、状态、输出、失败与恢复条件。
</Answer>

**问题 3** 本页怎样签发？

<Answer>
  每次循环都消费输入或显式失败，结束标签与祖先匹配，文本模式切换恢复，属性和字符引用按上下文解码，错误包含位置。
  同一运行时与输入必须能重放正常、边界、失败和恢复轨迹。
</Answer>

</Exercises>

## 小结

- **文本模式**：决定解析器当前如何识别标签、插值和字符引用的上下文状态。
- **递归下降**：每个语法结构由对应函数解析并递归处理嵌套内容的算法。
- **状态机**：根据当前状态和输入字符决定转换与输出的解析模型。
- **字符引用**：HTML文本中以命名或数字形式表示其他字符的编码。
- **插值**：模板中把表达式结果嵌入文本或属性位置的语法节点。
- 官方目录、最小实现、故障注入与恢复证据已经放进同一验收链。

## 术语表

<Glossary>
  <GlossaryItem term="文本模式">
    决定解析器当前如何识别标签、插值和字符引用的上下文状态。
  </GlossaryItem>
  <GlossaryItem term="递归下降">
    每个语法结构由对应函数解析并递归处理嵌套内容的算法。
  </GlossaryItem>
  <GlossaryItem term="状态机">
    根据当前状态和输入字符决定转换与输出的解析模型。
  </GlossaryItem>
  <GlossaryItem term="字符引用">
    HTML文本中以命名或数字形式表示其他字符的编码。
  </GlossaryItem>
  <GlossaryItem term="插值">
    模板中把表达式结果嵌入文本或属性位置的语法节点。
  </GlossaryItem>
</Glossary>

## 来源与改编边界

<Attribution
  adaptedFrom="霍春阳著，人民邮电出版社，2022年1月，501页，ISBN 9787115583864。作者授权数字版前言确认6篇18章的结构与逐章范围，作者GitHub仓库提供配套代码，公开完整目录用于二三级条目追踪；正文为独立教学重构。"
  adaptedUrl="https://time.geekbang.org/column/article/740758"
/>

配套代码：https://github.com/HcySunYang/code-for-vue-3-book
