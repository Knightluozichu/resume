---
title: "第 18 章 同构渲染"
type: A
section: "Vue.js设计与实现 · 服务端渲染"
order: 18
description: "同构渲染让服务器生成HTML、客户端激活交互；正确实现依赖安全字符串化、节点匹配、跨平台代码和逐请求状态隔离。 本页按权威目录独立教学重构。"
demo: true
math: false
sourceUrl: "https://time.geekbang.org/column/article/740758"
draft: false
---


## 直觉：从输入、状态到提交

同构渲染让服务器生成HTML、客户端激活交互；正确实现依赖安全字符串化、节点匹配、跨平台代码和逐请求状态隔离。 本书不是对最终源码逐行翻译，而是从规范与最小模型逐步构造框架。只看最终DOM会隐藏错误依赖、重复移动、陈旧任务和激活错位；先画输入、状态所有者、调度点、输出和清理责任。

## 权威目录定位

作者授权数字版前言明确全书分为6篇18章，作者GitHub仓库提供配套代码。本页依照公开完整目录逐项覆盖 **11** 个条目；目录名称仅用于追踪，正文为独立教学重构。

- 18.1 CSR、SSR以及同构渲染
- 18.2 将虚拟DOM渲染为HTML字符串
- 18.3 将组件渲染为HTML字符串
- 18.4 客户端激活的原理
- 18.5 编写同构的代码
- 18.5.1 组件的生命周期
- 18.5.2 使用跨平台的API
- 18.5.3 只在某一端引入模块
- 18.5.4 避免交叉请求引起的状态污染
- 18.5.5 组件
- 18.6 总结

## 核心概念与所有权

、、、、。
每个概念都要回答：谁创建输入，哪个结构保存状态，何种操作可以修改，错误如何传播，生命周期结束后谁清理。

|   # | 概念       | 精确定义                                                       |
| --: | ---------- | -------------------------------------------------------------- |
|   1 | CSR        | 在浏览器中获取数据并创建主要页面内容的客户端渲染方案。         |
|   2 | SSR        | 在服务器把组件树渲染为HTML并发送给浏览器的服务端渲染方案。     |
|   3 | 同构渲染   | 服务器生成首屏HTML，客户端复用同一组件逻辑并激活交互的方案。   |
|   4 | 客户端激活 | 把现有服务端HTML与客户端VNode对应并安装事件和组件状态的过程。  |
|   5 | 状态隔离   | 为每次服务器请求创建独立应用状态、避免用户数据交叉污染的约束。 |

## 机制拆解

### 1. 建立机制边界

renderElementVNode输出标签、属性和children，文本与属性必须按HTML上下文转义，布尔属性和危险名称需要规则过滤。组件服务端执行setup或render并递归渲染subTree，但不会运行依赖DOM的挂载生命周期。

先预测：如果省略这一层，依赖、节点身份、任务顺序或请求状态会在哪里首次偏离？

### 2. 处理关键算法

客户端激活遍历现有DOM与VNode，类型和文本匹配时复用节点并安装事件；不匹配时给出诊断并局部修复。服务端使用随机数、时间或环境分支会造成首屏结构不一致。

先预测：如果省略这一层，依赖、节点身份、任务顺序或请求状态会在哪里首次偏离？

### 3. 签发失败与恢复

每次请求必须创建新的应用、路由和store，模块级单例状态会把一个用户数据泄露给另一个请求。浏览器API只在客户端钩子中使用，端特定模块通过条件入口加载。

先预测：如果省略这一层，依赖、节点身份、任务顺序或请求状态会在哪里首次偏离？

## 三阶段可视化推演


## 最小可运行实验

```js
export function createAppForRequest(url) {
  const app = createSSRApp(Root);
  const store = createStore();
  const router = createRouter(url);
  return { app, store, router };
}
```

保存结构化证据，不以一次console输出或最终截图替代过程：

```yaml
book: Vue.js设计与实现
page: vdi-18-isomorphic-rendering
sample: normal | boundary | failure | recovery
runtime: pinned
input_fixture: fixed
first_divergence: null
cleanup_complete: true
```

故障注入一次只改变一个变量：

```text
baseline -> inject(one_fault) -> stop_at_first_divergence
recover -> replay_same_input
assert -> precise_dependency && stable_identity && ordered_tasks && no_retained_resource
```

## 常见误区与故障注入


## 验收矩阵

| 样本 | 变化                 | 预期         | 证据           |
| ---- | -------------------- | ------------ | -------------- |
| 正常 | 合法输入与稳定身份   | 主链完成     | 输出与轨迹一致 |
| 边界 | 空值、重复或极限序列 | 有界退化     | 不越界、不重复 |
| 失败 | 异常、乱序、非法结构 | 首偏离点停止 | 错误显式传播   |
| 恢复 | 清除故障后同输入重放 | 收敛到正常   | 无旧依赖和资源 |

本页签发不变量：**HTML正确转义，服务端和客户端首屏结构一致，激活复用节点并安装事件，浏览器API不在服务端执行，每次请求状态独立。**

## 练习


## 小结

- **CSR**：在浏览器中获取数据并创建主要页面内容的客户端渲染方案。
- **SSR**：在服务器把组件树渲染为HTML并发送给浏览器的服务端渲染方案。
- **同构渲染**：服务器生成首屏HTML，客户端复用同一组件逻辑并激活交互的方案。
- **客户端激活**：把现有服务端HTML与客户端VNode对应并安装事件和组件状态的过程。
- **状态隔离**：为每次服务器请求创建独立应用状态、避免用户数据交叉污染的约束。
- 官方目录、最小实现、故障注入与恢复证据已经放进同一验收链。

## 术语表


## 来源与改编边界


配套代码：https://github.com/HcySunYang/code-for-vue-3-book
