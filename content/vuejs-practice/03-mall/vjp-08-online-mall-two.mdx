---
title: "第 8 章 打造线上商城（二）"
type: A
section: "Vue.js从入门到项目实战 · 商城实战"
order: 8
description: "从项目目录与webpack延伸到动态资源、数据存储、幻灯片和复选框组件，完成商城工程实现。"
demo: true
math: false
sourceUrl: "https://www.tup.com.cn/bookscenter/book_08226301.html"
draft: true
---

import {
  Vjp08OnlineMallTwoMapLab,
  Vjp08OnlineMallTwoExperimentLab,
  Vjp08OnlineMallTwoEvidenceLab,
} from "@/components/mdx/vuejs-practice/diagrams/vjp-08-online-mall-two";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第 8 章 打造线上商城（二）的核心机制，并把它放回11章、5个附录的学习主线
- 能比较正常、边界、失败和恢复样本，指出首个状态、身份、任务或资源偏离点
- 能实现本页最小Vue 2实验，记录运行时、输入、事件顺序、状态所有者与清理动作
- 能设计证据回答“构建产物可追溯到依赖和环境，资源失败有降级，业务数据所有权明确，自定义组件在空值、键盘和重复挂载下无泄漏。”是否成立

</Objectives>

## 直觉：先画状态、身份与交付链

从项目目录与webpack延伸到动态资源、数据存储、幻灯片和复选框组件，完成商城工程实现。 商城工程化把目录、webpack、图标、动态资源、数据存储和自定义组件放进同一构建与运行契约。 真正掌握本页，不是让一次演示碰巧得到正确截图，而是能解释输入从哪里来、谁拥有可变状态、哪次事件触发更新、失败后如何停止，以及组件和外部资源何时释放。先预测：只看最终页面，最容易遗漏哪条陈旧请求、重复事件或错误身份？

<Vjp08OnlineMallTwoMapLab />

## 权威目录定位

本书依据清华大学出版社官方书目与官方目录核对：刘汉伟著，2019年4月出版，ISBN 9787302523888。原书处于Vue 2、Vue CLI、Vuex和当时webpack的技术语境；Vue 3、Pinia、Vite或Nuxt只会作为明确的迁移注记，不会冒充原书正式分节。

本页按官方目录顺序逐项覆盖 **11** 个公开条目；目录名称只用于一对一追踪，正文为独立教学重构。

- 8.1 项目构建
- 8.1.1 目录结构
- 8.1.2 webpack是什么？
- 8.1.3 Font Awesome图标库
- 8.2 动态资源和数据
- 8.2.1 关于配置
- 8.2.2 动态资源
- 8.2.3 动态数据的存储
- 8.3 自定义组件
- 8.3.1 幻灯片
- 8.3.2 复选框

## 核心概念与所有权

<Term def="从入口建立模块依赖图并通过loader和plugin产出可部署资源的构建工具。">webpack</Term>、<Term def="地址或内容由配置、数据和运行环境共同决定的图片、图标等资源。">动态资源</Term>、<Term def="随开发、测试和生产环境变化但不应写死在业务组件中的参数。">环境配置</Term>、<Term def="封装轮播数据、索引、定时切换、手动导航和清理行为的组件。">幻灯片组件</Term>、<Term def="在布尔或集合模型与可访问输入控件之间建立值和事件契约的组件。">复选框组件</Term>。
这些概念不是孤立名词：每一个都要回答“输入由谁创建、状态保存在哪里、哪个事件可以修改、失败如何传播、离开页面后由谁清理”。

|   # | 概念       | 工程含义                                                           |
| --: | ---------- | ------------------------------------------------------------------ |
|   1 | webpack    | 从入口建立模块依赖图并通过loader和plugin产出可部署资源的构建工具。 |
|   2 | 动态资源   | 地址或内容由配置、数据和运行环境共同决定的图片、图标等资源。       |
|   3 | 环境配置   | 随开发、测试和生产环境变化但不应写死在业务组件中的参数。           |
|   4 | 幻灯片组件 | 封装轮播数据、索引、定时切换、手动导航和清理行为的组件。           |
|   5 | 复选框组件 | 在布尔或集合模型与可访问输入控件之间建立值和事件契约的组件。       |

## 机制拆解

### 1. 构建从依赖图开始

webpack从入口追踪JavaScript、样式和图片依赖，loader把不同资源转成模块，plugin处理跨模块产物。理解依赖图后才能解释哈希、缓存、代码分割和资源路径；只会复制配置无法诊断生产路径错误。

先预测：若省略这一层，状态、组件身份、异步任务或构建产物会在哪里首次偏离？

### 2. 配置、资源和业务数据分层

API根地址和功能开关属于环境配置，商品图片属于可版本化资源，购物车和商品列表属于业务数据。三者加载失败的回退不同：配置失败阻止启动，资源失败给占位，业务数据失败保留重试和旧数据边界。

先预测：若省略这一层，状态、组件身份、异步任务或构建产物会在哪里首次偏离？

### 3. 自定义组件签发交互契约

幻灯片要定义自动播放、悬停、离屏和销毁行为；复选框要定义value、选中值、禁用和键盘操作。组件不仅要在鼠标正常路径可用，还要在空列表、快速切换和重复挂载下保持状态与资源有界。

先预测：若省略这一层，状态、组件身份、异步任务或构建产物会在哪里首次偏离？

## 三阶段可视化推演

<Stepper>
  <Step title="1. 建立目录与状态地图">
    固定Vue运行时、工具链、输入数据和初始URL，先画出正常路径与唯一状态所有者。
    <Vjp08OnlineMallTwoMapLab />
  </Step>
  <Step title="2. 注入边界与失败">
    只改变一个变量，观察空值、重复事件、请求乱序、资源拒绝或旧缓存造成的首偏离点。
    <Vjp08OnlineMallTwoExperimentLab />
  </Step>
  <Step title="3. 清除故障并重放">
    使用相同输入恢复，核对DOM摘要、状态版本、监听器、定时器、请求和构建资源是否收敛。
    <Vjp08OnlineMallTwoEvidenceLab />
  </Step>
</Stepper>

## 最小可运行实验

下面的最小实现聚焦本页核心。先在独立页面运行，再加入空输入、快速重复操作和销毁重建；不要直接把演示代码当成完整生产方案。

```js
Vue.component("app-checkbox", {
  props: { value: Boolean, disabled: Boolean },
  template:
    '<input type="checkbox" :checked="value" :disabled="disabled" @change="$emit("input", $event.target.checked)">',
});
```

证据记录必须同时保存目录坐标、运行时和状态摘要：

```yaml
book: Vue.js从入门到项目实战
page: vjp-08-online-mall-two
sample: normal | boundary | failure | recovery
runtime: Vue 2.x + pinned toolchain
input_fixture: fixed
first_divergence: null
cleanup_complete: true
```

故障脚本保持一次只改变一个变量：

```text
baseline -> run_same_input
inject -> stale-state | duplicate-event | denied-resource | broken-build
observe -> stop_at_first_divergence
recover -> clear_fault_and_replay
assert -> one_state_owner && stable_output && no_stale_task && no_resource_leak
```

## 常见误区与故障注入

<Callout type="trap">
  **误区一：把生产API地址和资源域名写死在组件中，环境切换只能重新修改业务代码。**
  最终截图可能仍正确，但状态轨迹、资源数量或下一次交互已经偏离；必须保存首次偏离前后的输入与事件。
</Callout>

<Callout type="trap">
  **误区二：轮播组件每次激活都创建定时器，却没有在停用或销毁时清理。**
  用正常样本无法暴露这一问题，要增加边界、失败和恢复样本，并在销毁后检查残留任务。
</Callout>

## 验收矩阵

| 样本 | 输入与环境变化               | 预期状态       | 通过条件               |
| ---- | ---------------------------- | -------------- | ---------------------- |
| 正常 | 合法数据、稳定身份、固定版本 | 完成主链       | 输出与状态摘要一致     |
| 边界 | 空值、极限数量、快速重复事件 | 有界退化       | 不越界、不重复、不阻塞 |
| 失败 | 请求拒绝、旧缓存、资源缺失   | 首偏离点停止   | 错误显式传播或降级     |
| 恢复 | 清除故障后重放同一输入       | 收敛到正常轨迹 | 无陈旧任务和资源泄漏   |

<Vjp08OnlineMallTwoEvidenceLab />

本页签发不变量是：**构建产物可追溯到依赖和环境，资源失败有降级，业务数据所有权明确，自定义组件在空值、键盘和重复挂载下无泄漏。** 单次成功、平均耗时或最终截图不能代替运行时版本、事件轨迹、首偏离点和清理证据。

## 练习

<Exercises>

**问题 1** 第 8 章 打造线上商城（二）的核心主张是什么？为什么只看最终页面不足以验收？

<Answer>
  商城工程化把目录、webpack、图标、动态资源、数据存储和自定义组件放进同一构建与运行契约。
  最终页面会隐藏重复副作用、旧任务覆盖、错误吞没和资源泄漏，所以必须比较输入、状态所有者、事件顺序、首偏离点与清理结果。
</Answer>

**问题 2** 如何把本页公开目录变成可执行学习证据？

<Answer>
  按官方顺序建立追踪表，为每组条目写出输入、状态、输出、失败与恢复条件。本页首先覆盖8.1
  项目构建、8.1.1 目录结构、8.1.2 webpack是什么？、8.1.3 Font Awesome图标库、8.2
  动态资源和数据、8.2.1 关于配置、8.2.2 动态资源、8.2.3
  动态数据的存储；其余条目已在上方逐项列出。
</Answer>

**问题 3** 本页的最小通过条件是什么？

<Answer>
  构建产物可追溯到依赖和环境，资源失败有降级，业务数据所有权明确，自定义组件在空值、键盘和重复挂载下无泄漏。
  并且同一运行时、输入与初始状态可以重放正常、边界、失败和恢复轨迹。
</Answer>

</Exercises>

## 小结

- **webpack**：从入口建立模块依赖图并通过loader和plugin产出可部署资源的构建工具。
- **动态资源**：地址或内容由配置、数据和运行环境共同决定的图片、图标等资源。
- **环境配置**：随开发、测试和生产环境变化但不应写死在业务组件中的参数。
- **幻灯片组件**：封装轮播数据、索引、定时切换、手动导航和清理行为的组件。
- **复选框组件**：在布尔或集合模型与可访问输入控件之间建立值和事件契约的组件。
- 本页已经把官方目录、机制链、最小实现、故障注入和恢复证据放在同一条验收路径上。
- 原书Vue 2技术语境与现代迁移说明保持清晰边界。

## 术语表

<Glossary>
  <GlossaryItem term="webpack">
    从入口建立模块依赖图并通过loader和plugin产出可部署资源的构建工具。
  </GlossaryItem>
  <GlossaryItem term="动态资源">
    地址或内容由配置、数据和运行环境共同决定的图片、图标等资源。
  </GlossaryItem>
  <GlossaryItem term="环境配置">
    随开发、测试和生产环境变化但不应写死在业务组件中的参数。
  </GlossaryItem>
  <GlossaryItem term="幻灯片组件">
    封装轮播数据、索引、定时切换、手动导航和清理行为的组件。
  </GlossaryItem>
  <GlossaryItem term="复选框组件">
    在布尔或集合模型与可访问输入控件之间建立值和事件契约的组件。
  </GlossaryItem>
</Glossary>

## 来源与改编边界

<Attribution
  adaptedFrom="刘汉伟著，清华大学出版社，2019年4月，ISBN 9787302523888。清华大学出版社官方书目与官方目录确认全书分为概念篇和实战篇，共11章、5个附录；正文为独立教学重构，不复制原书段落。"
  adaptedUrl="https://www.tup.com.cn/bookscenter/book_08226301.html"
/>

官方目录：https://www.tup.com.cn/bookscenter/bookcatalog?id=08226301
