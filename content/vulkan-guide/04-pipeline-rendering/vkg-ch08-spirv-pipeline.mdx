---
title: SPIR-V着色器与图形流水线
type: B
section: 计算机图形学与渲染
order: 8
description: >-
  SPIR-V着色器与图形流水线：以实例版本、验证层与SPIR-V能力建立Vulkan运行边界；让shader资源声明、descriptor与pipeline
  layout逐项一致；区分静态管线、动态状态与dynamic rendering兼容条件，以章专属交互与可重放证据独立重写。
demo: true
math: true
sourceUrl: 'https://docs.vulkan.org/guide/latest/'
draft: false
qualityVersion: 2
practiceMode: diagnosis
sourceMode: independent-rewrite
officialUnitIds:
  - vkg-01
  - vkg-05
  - vkg-06
---
import { VkgCh08SpirvPipelineExecutionLab, VkgCh08SpirvPipelineHazardLab, VkgCh08SpirvPipelineEvidenceLab } from "@/components/mdx/vulkan-guide/v2/vkg-ch08-spirv-pipeline";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能说明“SPIR-V着色器与图形流水线”中的以实例版本、验证层与SPIR-V能力建立Vulkan运行边界；让shader资源声明、descriptor与pipeline layout逐项一致；区分静态管线、动态状态与dynamic rendering兼容条件，并给出对应对象、状态或概率边界
- 能用正常基线与“请求未支持实例版本，或把验证层通过当成程序逻辑正确；descriptor类型或数组长度与shader不符，或push constant范围越界；漏设已声明动态的viewport/scissor，或pipeline附件格式与begin rendering不符”反例定位loader版本、实例扩展、验证消息、SPIR-V环境和VUID、SPIR-V反射、set/binding、layout、写入资源、动态偏移和VUID、pipeline create info、动态状态、附件格式、命令顺序和VUID中的首个差异
- 能操作三个章专属实验，让explicit api、vulkan version、validation layer、spir-v、shader、descriptor set、descriptor set layout、pipeline layout、push constant、sampler、graphics pipeline、pipeline state、dynamic state、dynamic rendering、vkcmdbeginrendering、pipeline cache具备解释、视觉和练习证据
- 能修改最小实现、固定输入重放，并判断“SPIR-V着色器与图形流水线的输入、状态、输出与恢复结果可用同一证据包重放”是否恢复

</Objectives>

{/* GRAPHICS_API_ENGINES_QUALITY_V2 */}

## 从一个可推翻的运行假设开始

“SPIR-V着色器与图形流水线”先固定一个问题：如何证明以实例版本、验证层与SPIR-V能力建立Vulkan运行边界；让shader资源声明、descriptor与pipeline layout逐项一致；区分静态管线、动态状态与dynamic rendering兼容条件？先写下预期对象、状态或像素，再运行正常基线；若loader版本、实例扩展、验证消息、SPIR-V环境和VUID、SPIR-V反射、set/binding、layout、写入资源、动态偏移和VUID、pipeline create info、动态状态、附件格式、命令顺序和VUID不能指出第一个分叉，最终画面看起来正确也不能通过。

本页不变量是：SPIR-V着色器与图形流水线的输入、状态、输出与恢复结果可用同一证据包重放。故障模式“请求未支持实例版本，或把验证层通过当成程序逻辑正确；descriptor类型或数组长度与shader不符，或push constant范围越界；漏设已声明动态的viewport/scissor，或pipeline附件格式与begin rendering不符”每次只改变一个条件，正常、故障、恢复三次运行必须共享输入与版本。

## 来源、版本与课程边界

“SPIR-V着色器与图形流水线”以[Khronos Vulkan Guide latest](https://docs.vulkan.org/guide/latest/)这套持续更新的官方完整指南为主，并由[Vulkan规范](https://docs.vulkan.org/spec/latest/)和[官方Samples](https://docs.vulkan.org/samples/latest/)核对VUID、对象合同与可运行路径。课程的16页是教学聚合，不宣称它们是官方固定章节。

## 六个检查词

在“SPIR-V着色器与图形流水线”的六词核对中，<Term def="explicit api在“SPIR-V着色器与图形流水线”的“Vulkan Architecture, Versions, Validation, and SPIR-V”检查中用于以实例版本、验证层与SPIR-V能力建立Vulkan运行边界；应由loader版本、实例扩展、验证消息、SPIR-V环境和VUID确认。">explicit api</Term>、<Term def="vulkan version在“SPIR-V着色器与图形流水线”的“Vulkan Architecture, Versions, Validation, and SPIR-V”检查中用于以实例版本、验证层与SPIR-V能力建立Vulkan运行边界；应由loader版本、实例扩展、验证消息、SPIR-V环境和VUID确认。">vulkan version</Term>、<Term def="validation layer在“SPIR-V着色器与图形流水线”的“Vulkan Architecture, Versions, Validation, and SPIR-V”检查中用于以实例版本、验证层与SPIR-V能力建立Vulkan运行边界；应由loader版本、实例扩展、验证消息、SPIR-V环境和VUID确认。">validation layer</Term>、<Term def="spir-v在“SPIR-V着色器与图形流水线”的“Vulkan Architecture, Versions, Validation, and SPIR-V”检查中用于以实例版本、验证层与SPIR-V能力建立Vulkan运行边界；应由loader版本、实例扩展、验证消息、SPIR-V环境和VUID确认。">spir-v</Term>、<Term def="shader在“SPIR-V着色器与图形流水线”的“Shaders, Descriptors, Push Constants, and Resource Mapping”检查中用于让shader资源声明、descriptor与pipeline layout逐项一致；应由SPIR-V反射、set/binding、layout、写入资源、动态偏移和VUID确认。">shader</Term>、<Term def="descriptor set在“SPIR-V着色器与图形流水线”的“Shaders, Descriptors, Push Constants, and Resource Mapping”检查中用于让shader资源声明、descriptor与pipeline layout逐项一致；应由SPIR-V反射、set/binding、layout、写入资源、动态偏移和VUID确认。">descriptor set</Term>。术语只有进入公式、交互状态、代码和可重放输出，才算真正覆盖。

## 正式单元与机制解释

### Vulkan Architecture, Versions, Validation, and SPIR-V

在“SPIR-V着色器与图形流水线”的“Vulkan Architecture, Versions, Validation, and SPIR-V”检查中，Vulkan是显式API，但应用仍必须查询loader支持版本、实例扩展和设备能力。在“SPIR-V着色器与图形流水线”的“Vulkan Architecture, Versions, Validation, and SPIR-V”检查中，validation layer是开发期诊断层，不能改变规范语义；SPIR-V是shader中间表示，其capability与目标环境必须兼容设备。

在“SPIR-V着色器与图形流水线”本页，正式坐标是explicit api、vulkan version、validation layer、spir-v。在“SPIR-V着色器与图形流水线”的反例核对中，反事实为“请求未支持实例版本，或把验证层通过当成程序逻辑正确”；应从loader版本、实例扩展、验证消息、SPIR-V环境和VUID找到第一个分叉。

$$
Usable=Version\cap Extensions\cap Features
$$

### Shaders, Descriptors, Push Constants, and Resource Mapping

在“SPIR-V着色器与图形流水线”的“Shaders, Descriptors, Push Constants, and Resource Mapping”检查中，descriptor set layout定义每个binding的类型、数量和stage可见性，pipeline layout组合set layout与push constant范围。在“SPIR-V着色器与图形流水线”的“Shaders, Descriptors, Push Constants, and Resource Mapping”检查中，写descriptor只更新资源引用，绑定时还要满足动态偏移、数组索引和图像layout合同。

在“SPIR-V着色器与图形流水线”本页，正式坐标是shader、descriptor set、descriptor set layout、pipeline layout、push constant、sampler。在“SPIR-V着色器与图形流水线”的反例核对中，反事实为“descriptor类型或数组长度与shader不符，或push constant范围越界”；应从SPIR-V反射、set/binding、layout、写入资源、动态偏移和VUID找到第一个分叉。

$$
ShaderInterface=PipelineLayout=BoundSets
$$

### Graphics Pipeline, Dynamic State, and Dynamic Rendering

在“SPIR-V着色器与图形流水线”的“Graphics Pipeline, Dynamic State, and Dynamic Rendering”检查中，graphics pipeline把shader、顶点输入、光栅化和逐片元状态组合为可绑定对象；明确声明的dynamic state在录制时设置。在“SPIR-V着色器与图形流水线”的“Graphics Pipeline, Dynamic State, and Dynamic Rendering”检查中，dynamic rendering省去预建render pass对象，但pipeline rendering info与实际附件格式仍必须兼容。

在“SPIR-V着色器与图形流水线”本页，正式坐标是graphics pipeline、pipeline state、dynamic state、dynamic rendering、vkcmdbeginrendering、pipeline cache。在“SPIR-V着色器与图形流水线”的反例核对中，反事实为“漏设已声明动态的viewport/scissor，或pipeline附件格式与begin rendering不符”；应从pipeline create info、动态状态、附件格式、命令顺序和VUID找到第一个分叉。

$$
Draw=Pipeline_{static}+State_{dynamic}+Attachments
$$

## 先预测，再操作三个章专属实验

<Stepper>
  <Step title="1. 对象与执行路径">
    选择任一正式坐标，沿真实对象、状态、队列或像素路径查看因果关系。

    <VkgCh08SpirvPipelineExecutionLab />

  </Step>
  <Step title="2. 单变量故障">
    保持其余条件不变，只切换一个绑定、能力、访问或同步错误，预测首个差异。

    <VkgCh08SpirvPipelineHazardLab />

  </Step>
  <Step title="3. 证据与恢复">
    保存基线，注入“请求未支持实例版本，或把验证层通过当成程序逻辑正确；descriptor类型或数组长度与shader不符，或push constant范围越界；漏设已声明动态的viewport/scissor，或pipeline附件格式与begin rendering不符”，撤销后以相同输入重放并重置。

    <VkgCh08SpirvPipelineEvidenceLab />

  </Step>
</Stepper>

## 最小可重现实验

```cpp
reflectSpirv(shader, descriptorContract);
VkPipelineLayout layout = createPipelineLayout(descriptorContract);
VkPipeline pipeline = createGraphicsPipeline(shader, layout, attachmentFormats);
```

运行“SPIR-V着色器与图形流水线”时记录API/规范版本、设备或浏览器能力、固定输入、关键中间状态与输出摘要。不得用一次漂亮截图替代对象合同、验证消息、时间戳或像素差异。

<Callout type="trap" title="本页核心误区">
  请求未支持实例版本，或把验证层通过当成程序逻辑正确；descriptor类型或数组长度与shader不符，或push constant范围越界；漏设已声明动态的viewport/scissor，或pipeline附件格式与begin rendering不符。它会破坏“SPIR-V着色器与图形流水线的输入、状态、输出与恢复结果可用同一证据包重放”，应先由loader版本、实例扩展、验证消息、SPIR-V环境和VUID、SPIR-V反射、set/binding、layout、写入资源、动态偏移和VUID、pipeline create info、动态状态、附件格式、命令顺序和VUID定位。
</Callout>

<Callout type="trap" title="不要跨API套生命周期模板">
  “SPIR-V着色器与图形流水线”中的Vulkan显式对象与同步只在自己的规范语义内解释本页。不能把另一套API的Buffer、Fence、bind或隐式同步模型换上标题后当作正文。
</Callout>

## 练习与答案

<Exercises>

**问题 1：公式边界。** 在“SPIR-V着色器与图形流水线”中，如何用一个可手算样本验证 $Usable=Version\cap Extensions\cap Features$，并标明每个量属于哪一对象、stage、空间或时间点？

<Answer>
  针对“SPIR-V着色器与图形流水线”固定最小输入，逐项记录loader版本、实例扩展、验证消息、SPIR-V环境和VUID、SPIR-V反射、set/binding、layout、写入资源、动态偏移和VUID、pipeline create info、动态状态、附件格式、命令顺序和VUID；只扰动一个量并比较变化方向。若公式、验证消息和输出不一致，先拒绝实现再调查隐藏状态。
</Answer>

**问题 2：正式坐标。** 在“SPIR-V着色器与图形流水线”中，explicit api、vulkan version、validation layer、spir-v、shader、descriptor set、descriptor set layout、pipeline layout、push constant、sampler、graphics pipeline、pipeline state、dynamic state、dynamic rendering、vkcmdbeginrendering、pipeline cache如何进入可操作验证？

<Answer>
  1. **explicit api**：在“SPIR-V着色器与图形流水线”章专属实验中保存loader版本、实例扩展、验证消息、SPIR-V环境和VUID、SPIR-V反射、set/binding、layout、写入资源、动态偏移和VUID、pipeline create info、动态状态、附件格式、命令顺序和VUID，注入“请求未支持实例版本，或把验证层通过当成程序逻辑正确；descriptor类型或数组长度与shader不符，或push constant范围越界；漏设已声明动态的viewport/scissor，或pipeline附件格式与begin rendering不符”，恢复后以相同输入重放。
  2. **vulkan version**：在“SPIR-V着色器与图形流水线”章专属实验中保存loader版本、实例扩展、验证消息、SPIR-V环境和VUID、SPIR-V反射、set/binding、layout、写入资源、动态偏移和VUID、pipeline create info、动态状态、附件格式、命令顺序和VUID，注入“请求未支持实例版本，或把验证层通过当成程序逻辑正确；descriptor类型或数组长度与shader不符，或push constant范围越界；漏设已声明动态的viewport/scissor，或pipeline附件格式与begin rendering不符”，恢复后以相同输入重放。
  3. **validation layer**：在“SPIR-V着色器与图形流水线”章专属实验中保存loader版本、实例扩展、验证消息、SPIR-V环境和VUID、SPIR-V反射、set/binding、layout、写入资源、动态偏移和VUID、pipeline create info、动态状态、附件格式、命令顺序和VUID，注入“请求未支持实例版本，或把验证层通过当成程序逻辑正确；descriptor类型或数组长度与shader不符，或push constant范围越界；漏设已声明动态的viewport/scissor，或pipeline附件格式与begin rendering不符”，恢复后以相同输入重放。
  4. **spir-v**：在“SPIR-V着色器与图形流水线”章专属实验中保存loader版本、实例扩展、验证消息、SPIR-V环境和VUID、SPIR-V反射、set/binding、layout、写入资源、动态偏移和VUID、pipeline create info、动态状态、附件格式、命令顺序和VUID，注入“请求未支持实例版本，或把验证层通过当成程序逻辑正确；descriptor类型或数组长度与shader不符，或push constant范围越界；漏设已声明动态的viewport/scissor，或pipeline附件格式与begin rendering不符”，恢复后以相同输入重放。
  5. **shader**：在“SPIR-V着色器与图形流水线”章专属实验中保存loader版本、实例扩展、验证消息、SPIR-V环境和VUID、SPIR-V反射、set/binding、layout、写入资源、动态偏移和VUID、pipeline create info、动态状态、附件格式、命令顺序和VUID，注入“请求未支持实例版本，或把验证层通过当成程序逻辑正确；descriptor类型或数组长度与shader不符，或push constant范围越界；漏设已声明动态的viewport/scissor，或pipeline附件格式与begin rendering不符”，恢复后以相同输入重放。
  6. **descriptor set**：在“SPIR-V着色器与图形流水线”章专属实验中保存loader版本、实例扩展、验证消息、SPIR-V环境和VUID、SPIR-V反射、set/binding、layout、写入资源、动态偏移和VUID、pipeline create info、动态状态、附件格式、命令顺序和VUID，注入“请求未支持实例版本，或把验证层通过当成程序逻辑正确；descriptor类型或数组长度与shader不符，或push constant范围越界；漏设已声明动态的viewport/scissor，或pipeline附件格式与begin rendering不符”，恢复后以相同输入重放。
  7. **descriptor set layout**：在“SPIR-V着色器与图形流水线”章专属实验中保存loader版本、实例扩展、验证消息、SPIR-V环境和VUID、SPIR-V反射、set/binding、layout、写入资源、动态偏移和VUID、pipeline create info、动态状态、附件格式、命令顺序和VUID，注入“请求未支持实例版本，或把验证层通过当成程序逻辑正确；descriptor类型或数组长度与shader不符，或push constant范围越界；漏设已声明动态的viewport/scissor，或pipeline附件格式与begin rendering不符”，恢复后以相同输入重放。
  8. **pipeline layout**：在“SPIR-V着色器与图形流水线”章专属实验中保存loader版本、实例扩展、验证消息、SPIR-V环境和VUID、SPIR-V反射、set/binding、layout、写入资源、动态偏移和VUID、pipeline create info、动态状态、附件格式、命令顺序和VUID，注入“请求未支持实例版本，或把验证层通过当成程序逻辑正确；descriptor类型或数组长度与shader不符，或push constant范围越界；漏设已声明动态的viewport/scissor，或pipeline附件格式与begin rendering不符”，恢复后以相同输入重放。
  9. **push constant**：在“SPIR-V着色器与图形流水线”章专属实验中保存loader版本、实例扩展、验证消息、SPIR-V环境和VUID、SPIR-V反射、set/binding、layout、写入资源、动态偏移和VUID、pipeline create info、动态状态、附件格式、命令顺序和VUID，注入“请求未支持实例版本，或把验证层通过当成程序逻辑正确；descriptor类型或数组长度与shader不符，或push constant范围越界；漏设已声明动态的viewport/scissor，或pipeline附件格式与begin rendering不符”，恢复后以相同输入重放。
  10. **sampler**：在“SPIR-V着色器与图形流水线”章专属实验中保存loader版本、实例扩展、验证消息、SPIR-V环境和VUID、SPIR-V反射、set/binding、layout、写入资源、动态偏移和VUID、pipeline create info、动态状态、附件格式、命令顺序和VUID，注入“请求未支持实例版本，或把验证层通过当成程序逻辑正确；descriptor类型或数组长度与shader不符，或push constant范围越界；漏设已声明动态的viewport/scissor，或pipeline附件格式与begin rendering不符”，恢复后以相同输入重放。
  11. **graphics pipeline**：在“SPIR-V着色器与图形流水线”章专属实验中保存loader版本、实例扩展、验证消息、SPIR-V环境和VUID、SPIR-V反射、set/binding、layout、写入资源、动态偏移和VUID、pipeline create info、动态状态、附件格式、命令顺序和VUID，注入“请求未支持实例版本，或把验证层通过当成程序逻辑正确；descriptor类型或数组长度与shader不符，或push constant范围越界；漏设已声明动态的viewport/scissor，或pipeline附件格式与begin rendering不符”，恢复后以相同输入重放。
  12. **pipeline state**：在“SPIR-V着色器与图形流水线”章专属实验中保存loader版本、实例扩展、验证消息、SPIR-V环境和VUID、SPIR-V反射、set/binding、layout、写入资源、动态偏移和VUID、pipeline create info、动态状态、附件格式、命令顺序和VUID，注入“请求未支持实例版本，或把验证层通过当成程序逻辑正确；descriptor类型或数组长度与shader不符，或push constant范围越界；漏设已声明动态的viewport/scissor，或pipeline附件格式与begin rendering不符”，恢复后以相同输入重放。
  13. **dynamic state**：在“SPIR-V着色器与图形流水线”章专属实验中保存loader版本、实例扩展、验证消息、SPIR-V环境和VUID、SPIR-V反射、set/binding、layout、写入资源、动态偏移和VUID、pipeline create info、动态状态、附件格式、命令顺序和VUID，注入“请求未支持实例版本，或把验证层通过当成程序逻辑正确；descriptor类型或数组长度与shader不符，或push constant范围越界；漏设已声明动态的viewport/scissor，或pipeline附件格式与begin rendering不符”，恢复后以相同输入重放。
  14. **dynamic rendering**：在“SPIR-V着色器与图形流水线”章专属实验中保存loader版本、实例扩展、验证消息、SPIR-V环境和VUID、SPIR-V反射、set/binding、layout、写入资源、动态偏移和VUID、pipeline create info、动态状态、附件格式、命令顺序和VUID，注入“请求未支持实例版本，或把验证层通过当成程序逻辑正确；descriptor类型或数组长度与shader不符，或push constant范围越界；漏设已声明动态的viewport/scissor，或pipeline附件格式与begin rendering不符”，恢复后以相同输入重放。
  15. **vkcmdbeginrendering**：在“SPIR-V着色器与图形流水线”章专属实验中保存loader版本、实例扩展、验证消息、SPIR-V环境和VUID、SPIR-V反射、set/binding、layout、写入资源、动态偏移和VUID、pipeline create info、动态状态、附件格式、命令顺序和VUID，注入“请求未支持实例版本，或把验证层通过当成程序逻辑正确；descriptor类型或数组长度与shader不符，或push constant范围越界；漏设已声明动态的viewport/scissor，或pipeline附件格式与begin rendering不符”，恢复后以相同输入重放。
  16. **pipeline cache**：在“SPIR-V着色器与图形流水线”章专属实验中保存loader版本、实例扩展、验证消息、SPIR-V环境和VUID、SPIR-V反射、set/binding、layout、写入资源、动态偏移和VUID、pipeline create info、动态状态、附件格式、命令顺序和VUID，注入“请求未支持实例版本，或把验证层通过当成程序逻辑正确；descriptor类型或数组长度与shader不符，或push constant范围越界；漏设已声明动态的viewport/scissor，或pipeline附件格式与begin rendering不符”，恢复后以相同输入重放。
</Answer>

**问题 3：恢复证据。** 怎样证明“请求未支持实例版本，或把验证层通过当成程序逻辑正确；descriptor类型或数组长度与shader不符，或push constant范围越界；漏设已声明动态的viewport/scissor，或pipeline附件格式与begin rendering不符”已真正修复？

<Answer>
  保存正常基线，注入故障并标记loader版本、实例扩展、验证消息、SPIR-V环境和VUID、SPIR-V反射、set/binding、layout、写入资源、动态偏移和VUID、pipeline create info、动态状态、附件格式、命令顺序和VUID中的首个分叉；撤销故障后以完全相同输入重放。只有SPIR-V着色器与图形流水线的输入、状态、输出与恢复结果可用同一证据包重放重新成立且重置回到初值，修复才可交接。
</Answer>

</Exercises>

## 本章回顾

掌握“SPIR-V着色器与图形流水线”意味着能把“以实例版本、验证层与SPIR-V能力建立Vulkan运行边界；让shader资源声明、descriptor与pipeline layout逐项一致；区分静态管线、动态状态与dynamic rendering兼容条件”落到具体对象、状态与输出，能制造“请求未支持实例版本，或把验证层通过当成程序逻辑正确；descriptor类型或数组长度与shader不符，或push constant范围越界；漏设已声明动态的viewport/scissor，或pipeline附件格式与begin rendering不符”，还能凭loader版本、实例扩展、验证消息、SPIR-V环境和VUID、SPIR-V反射、set/binding、layout、写入资源、动态偏移和VUID、pipeline create info、动态状态、附件格式、命令顺序和VUID恢复同输入结果。

<Glossary>
  <GlossaryItem term="explicit api">
    explicit api在“SPIR-V着色器与图形流水线”的“Vulkan Architecture, Versions, Validation, and SPIR-V”检查中用于以实例版本、验证层与SPIR-V能力建立Vulkan运行边界；应由loader版本、实例扩展、验证消息、SPIR-V环境和VUID确认。
  </GlossaryItem>
  <GlossaryItem term="vulkan version">
    vulkan version在“SPIR-V着色器与图形流水线”的“Vulkan Architecture, Versions, Validation, and SPIR-V”检查中用于以实例版本、验证层与SPIR-V能力建立Vulkan运行边界；应由loader版本、实例扩展、验证消息、SPIR-V环境和VUID确认。
  </GlossaryItem>
  <GlossaryItem term="validation layer">
    validation layer在“SPIR-V着色器与图形流水线”的“Vulkan Architecture, Versions, Validation, and SPIR-V”检查中用于以实例版本、验证层与SPIR-V能力建立Vulkan运行边界；应由loader版本、实例扩展、验证消息、SPIR-V环境和VUID确认。
  </GlossaryItem>
  <GlossaryItem term="spir-v">
    spir-v在“SPIR-V着色器与图形流水线”的“Vulkan Architecture, Versions, Validation, and SPIR-V”检查中用于以实例版本、验证层与SPIR-V能力建立Vulkan运行边界；应由loader版本、实例扩展、验证消息、SPIR-V环境和VUID确认。
  </GlossaryItem>
  <GlossaryItem term="shader">
    shader在“SPIR-V着色器与图形流水线”的“Shaders, Descriptors, Push Constants, and Resource Mapping”检查中用于让shader资源声明、descriptor与pipeline layout逐项一致；应由SPIR-V反射、set/binding、layout、写入资源、动态偏移和VUID确认。
  </GlossaryItem>
  <GlossaryItem term="descriptor set">
    descriptor set在“SPIR-V着色器与图形流水线”的“Shaders, Descriptors, Push Constants, and Resource Mapping”检查中用于让shader资源声明、descriptor与pipeline layout逐项一致；应由SPIR-V反射、set/binding、layout、写入资源、动态偏移和VUID确认。
  </GlossaryItem>
</Glossary>

## 阅读导航

[← 缓存资源、渲染通道与帧缓存](/learn/vulkan-guide/04-pipeline-rendering/vkg-ch07-buffer-renderpass) · [绘制几何体 →](/learn/vulkan-guide/04-pipeline-rendering/vkg-ch09-draw-geometry)

<Attribution
  mode="independent-rewrite"
  sourceBasis="full-text-primary"
  workTitle="Khronos Vulkan Guide"
  adaptedUrl="https://docs.vulkan.org/guide/latest/"
/>
