---
title: 移动端Vulkan
type: B
section: 计算机图形学与渲染
order: 16
description: >-
  移动端Vulkan：以实例版本、验证层与SPIR-V能力建立Vulkan运行边界；让surface、swapchain、acquire与present形成可恢复帧循环；按扩展依赖与设备feature启用计算、光追、mesh等高级路径，以章专属交互与可重放证据独立重写。
demo: true
math: true
sourceUrl: 'https://docs.vulkan.org/guide/latest/'
draft: false
qualityVersion: 2
practiceMode: diagnosis
sourceMode: independent-rewrite
officialUnitIds:
  - vkg-01
  - vkg-03
  - vkg-10
---
import { VkgCh16MobileVulkanExecutionLab, VkgCh16MobileVulkanHazardLab, VkgCh16MobileVulkanEvidenceLab } from "@/components/mdx/vulkan-guide/v2/vkg-ch16-mobile-vulkan";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能说明“移动端Vulkan”中的以实例版本、验证层与SPIR-V能力建立Vulkan运行边界；让surface、swapchain、acquire与present形成可恢复帧循环；按扩展依赖与设备feature启用计算、光追、mesh等高级路径，并给出对应对象、状态或概率边界
- 能用正常基线与“请求未支持实例版本，或把验证层通过当成程序逻辑正确；复用仍被present等待的binary semaphore，或resize后继续使用旧extent；只检查扩展名便创建管线，或没有为不支持设备准备可验证fallback”反例定位loader版本、实例扩展、验证消息、SPIR-V环境和VUID、acquire结果、image index、等待信号量、present结果和重建代次、扩展依赖、features链、limits、shader capability、管线结果和回退路径中的首个差异
- 能操作三个章专属实验，让explicit api、vulkan version、validation layer、spir-v、wsi、vksurfacekhr、vkswapchainkhr、vkacquirenextimage、vkqueuepresent、out_of_date、compute shader、ray tracing、mesh shader、descriptor indexing、variable rate shading、extension具备解释、视觉和练习证据
- 能修改最小实现、固定输入重放，并判断“移动端Vulkan的输入、状态、输出与恢复结果可用同一证据包重放”是否恢复

</Objectives>

{/* GRAPHICS_API_ENGINES_QUALITY_V2 */}

## 从一个可推翻的运行假设开始

“移动端Vulkan”先固定一个问题：如何证明以实例版本、验证层与SPIR-V能力建立Vulkan运行边界；让surface、swapchain、acquire与present形成可恢复帧循环；按扩展依赖与设备feature启用计算、光追、mesh等高级路径？先写下预期对象、状态或像素，再运行正常基线；若loader版本、实例扩展、验证消息、SPIR-V环境和VUID、acquire结果、image index、等待信号量、present结果和重建代次、扩展依赖、features链、limits、shader capability、管线结果和回退路径不能指出第一个分叉，最终画面看起来正确也不能通过。

本页不变量是：移动端Vulkan的输入、状态、输出与恢复结果可用同一证据包重放。故障模式“请求未支持实例版本，或把验证层通过当成程序逻辑正确；复用仍被present等待的binary semaphore，或resize后继续使用旧extent；只检查扩展名便创建管线，或没有为不支持设备准备可验证fallback”每次只改变一个条件，正常、故障、恢复三次运行必须共享输入与版本。

## 来源、版本与课程边界

“移动端Vulkan”以[Khronos Vulkan Guide latest](https://docs.vulkan.org/guide/latest/)这套持续更新的官方完整指南为主，并由[Vulkan规范](https://docs.vulkan.org/spec/latest/)和[官方Samples](https://docs.vulkan.org/samples/latest/)核对VUID、对象合同与可运行路径。课程的16页是教学聚合，不宣称它们是官方固定章节。

## 六个检查词

在“移动端Vulkan”的六词核对中，<Term def="explicit api在“移动端Vulkan”的“Vulkan Architecture, Versions, Validation, and SPIR-V”检查中用于以实例版本、验证层与SPIR-V能力建立Vulkan运行边界；应由loader版本、实例扩展、验证消息、SPIR-V环境和VUID确认。">explicit api</Term>、<Term def="vulkan version在“移动端Vulkan”的“Vulkan Architecture, Versions, Validation, and SPIR-V”检查中用于以实例版本、验证层与SPIR-V能力建立Vulkan运行边界；应由loader版本、实例扩展、验证消息、SPIR-V环境和VUID确认。">vulkan version</Term>、<Term def="validation layer在“移动端Vulkan”的“Vulkan Architecture, Versions, Validation, and SPIR-V”检查中用于以实例版本、验证层与SPIR-V能力建立Vulkan运行边界；应由loader版本、实例扩展、验证消息、SPIR-V环境和VUID确认。">validation layer</Term>、<Term def="spir-v在“移动端Vulkan”的“Vulkan Architecture, Versions, Validation, and SPIR-V”检查中用于以实例版本、验证层与SPIR-V能力建立Vulkan运行边界；应由loader版本、实例扩展、验证消息、SPIR-V环境和VUID确认。">spir-v</Term>、<Term def="wsi在“移动端Vulkan”的“Window System Integration, Surface, Swapchain, Acquire, and Present”检查中用于让surface、swapchain、acquire与present形成可恢复帧循环；应由acquire结果、image index、等待信号量、present结果和重建代次确认。">wsi</Term>、<Term def="vksurfacekhr在“移动端Vulkan”的“Window System Integration, Surface, Swapchain, Acquire, and Present”检查中用于让surface、swapchain、acquire与present形成可恢复帧循环；应由acquire结果、image index、等待信号量、present结果和重建代次确认。">vksurfacekhr</Term>。术语只有进入公式、交互状态、代码和可重放输出，才算真正覆盖。

## 正式单元与机制解释

### Vulkan Architecture, Versions, Validation, and SPIR-V

在“移动端Vulkan”的“Vulkan Architecture, Versions, Validation, and SPIR-V”检查中，Vulkan是显式API，但应用仍必须查询loader支持版本、实例扩展和设备能力。在“移动端Vulkan”的“Vulkan Architecture, Versions, Validation, and SPIR-V”检查中，validation layer是开发期诊断层，不能改变规范语义；SPIR-V是shader中间表示，其capability与目标环境必须兼容设备。

在“移动端Vulkan”本页，正式坐标是explicit api、vulkan version、validation layer、spir-v。在“移动端Vulkan”的反例核对中，反事实为“请求未支持实例版本，或把验证层通过当成程序逻辑正确”；应从loader版本、实例扩展、验证消息、SPIR-V环境和VUID找到第一个分叉。

$$
Usable=Version\cap Extensions\cap Features
$$

### Window System Integration, Surface, Swapchain, Acquire, and Present

在“移动端Vulkan”的“Window System Integration, Surface, Swapchain, Acquire, and Present”检查中，WSI把平台窗口surface连接到VkSwapchainKHR。在“移动端Vulkan”的“Window System Integration, Surface, Swapchain, Acquire, and Present”检查中，acquire只取得可用图像索引，present等待渲染完成后交给呈现引擎；OUT_OF_DATE与SUBOPTIMAL要求应用按surface capabilities重新选择extent、format与present mode。

在“移动端Vulkan”本页，正式坐标是wsi、vksurfacekhr、vkswapchainkhr、vkacquirenextimage、vkqueuepresent、out_of_date。在“移动端Vulkan”的反例核对中，反事实为“复用仍被present等待的binary semaphore，或resize后继续使用旧extent”；应从acquire结果、image index、等待信号量、present结果和重建代次找到第一个分叉。

$$
Acquire\rightarrow Render\rightarrow Present\rightarrow Recreate?
$$

### Extensions and Advanced Compute, Ray Tracing, Mesh, Descriptor, and Shading Features

在“移动端Vulkan”的“Extensions and Advanced Compute, Ray Tracing, Mesh, Descriptor, and Shading Features”检查中，compute、ray tracing、mesh shader、descriptor indexing与VRS都有各自feature、extension、limit和shader capability。在“移动端Vulkan”的“Extensions and Advanced Compute, Ray Tracing, Mesh, Descriptor, and Shading Features”检查中，能力存在不等于默认启用；应用还要准备同步、资源布局与回退实现。

在“移动端Vulkan”本页，正式坐标是compute shader、ray tracing、mesh shader、descriptor indexing、variable rate shading、extension。在“移动端Vulkan”的反例核对中，反事实为“只检查扩展名便创建管线，或没有为不支持设备准备可验证fallback”；应从扩展依赖、features链、limits、shader capability、管线结果和回退路径找到第一个分叉。

$$
AdvancedPath=Extension\land Feature\land Limits\land Shader
$$

## 先预测，再操作三个章专属实验

<Stepper>
  <Step title="1. 对象与执行路径">
    选择任一正式坐标，沿真实对象、状态、队列或像素路径查看因果关系。

    <VkgCh16MobileVulkanExecutionLab />

  </Step>
  <Step title="2. 单变量故障">
    保持其余条件不变，只切换一个绑定、能力、访问或同步错误，预测首个差异。

    <VkgCh16MobileVulkanHazardLab />

  </Step>
  <Step title="3. 证据与恢复">
    保存基线，注入“请求未支持实例版本，或把验证层通过当成程序逻辑正确；复用仍被present等待的binary semaphore，或resize后继续使用旧extent；只检查扩展名便创建管线，或没有为不支持设备准备可验证fallback”，撤销后以相同输入重放并重置。

    <VkgCh16MobileVulkanEvidenceLab />

  </Step>
</Stepper>

## 最小可重现实验

```cpp
SurfaceProfile surface = querySurface(physicalDevice, surfaceHandle);
RenderPath path = selectMobilePath(features, surface, thermalBudget);
runFramesInFlight(path, /* count */ 2);
verifyPresentAndRecovery(path);
```

运行“移动端Vulkan”时记录API/规范版本、设备或浏览器能力、固定输入、关键中间状态与输出摘要。不得用一次漂亮截图替代对象合同、验证消息、时间戳或像素差异。

<Callout type="trap" title="本页核心误区">
  请求未支持实例版本，或把验证层通过当成程序逻辑正确；复用仍被present等待的binary semaphore，或resize后继续使用旧extent；只检查扩展名便创建管线，或没有为不支持设备准备可验证fallback。它会破坏“移动端Vulkan的输入、状态、输出与恢复结果可用同一证据包重放”，应先由loader版本、实例扩展、验证消息、SPIR-V环境和VUID、acquire结果、image index、等待信号量、present结果和重建代次、扩展依赖、features链、limits、shader capability、管线结果和回退路径定位。
</Callout>

<Callout type="trap" title="不要跨API套生命周期模板">
  “移动端Vulkan”中的Vulkan显式对象与同步只在自己的规范语义内解释本页。不能把另一套API的Buffer、Fence、bind或隐式同步模型换上标题后当作正文。
</Callout>

## 练习与答案

<Exercises>

**问题 1：公式边界。** 在“移动端Vulkan”中，如何用一个可手算样本验证 $Usable=Version\cap Extensions\cap Features$，并标明每个量属于哪一对象、stage、空间或时间点？

<Answer>
  针对“移动端Vulkan”固定最小输入，逐项记录loader版本、实例扩展、验证消息、SPIR-V环境和VUID、acquire结果、image index、等待信号量、present结果和重建代次、扩展依赖、features链、limits、shader capability、管线结果和回退路径；只扰动一个量并比较变化方向。若公式、验证消息和输出不一致，先拒绝实现再调查隐藏状态。
</Answer>

**问题 2：正式坐标。** 在“移动端Vulkan”中，explicit api、vulkan version、validation layer、spir-v、wsi、vksurfacekhr、vkswapchainkhr、vkacquirenextimage、vkqueuepresent、out_of_date、compute shader、ray tracing、mesh shader、descriptor indexing、variable rate shading、extension如何进入可操作验证？

<Answer>
  1. **explicit api**：在“移动端Vulkan”章专属实验中保存loader版本、实例扩展、验证消息、SPIR-V环境和VUID、acquire结果、image index、等待信号量、present结果和重建代次、扩展依赖、features链、limits、shader capability、管线结果和回退路径，注入“请求未支持实例版本，或把验证层通过当成程序逻辑正确；复用仍被present等待的binary semaphore，或resize后继续使用旧extent；只检查扩展名便创建管线，或没有为不支持设备准备可验证fallback”，恢复后以相同输入重放。
  2. **vulkan version**：在“移动端Vulkan”章专属实验中保存loader版本、实例扩展、验证消息、SPIR-V环境和VUID、acquire结果、image index、等待信号量、present结果和重建代次、扩展依赖、features链、limits、shader capability、管线结果和回退路径，注入“请求未支持实例版本，或把验证层通过当成程序逻辑正确；复用仍被present等待的binary semaphore，或resize后继续使用旧extent；只检查扩展名便创建管线，或没有为不支持设备准备可验证fallback”，恢复后以相同输入重放。
  3. **validation layer**：在“移动端Vulkan”章专属实验中保存loader版本、实例扩展、验证消息、SPIR-V环境和VUID、acquire结果、image index、等待信号量、present结果和重建代次、扩展依赖、features链、limits、shader capability、管线结果和回退路径，注入“请求未支持实例版本，或把验证层通过当成程序逻辑正确；复用仍被present等待的binary semaphore，或resize后继续使用旧extent；只检查扩展名便创建管线，或没有为不支持设备准备可验证fallback”，恢复后以相同输入重放。
  4. **spir-v**：在“移动端Vulkan”章专属实验中保存loader版本、实例扩展、验证消息、SPIR-V环境和VUID、acquire结果、image index、等待信号量、present结果和重建代次、扩展依赖、features链、limits、shader capability、管线结果和回退路径，注入“请求未支持实例版本，或把验证层通过当成程序逻辑正确；复用仍被present等待的binary semaphore，或resize后继续使用旧extent；只检查扩展名便创建管线，或没有为不支持设备准备可验证fallback”，恢复后以相同输入重放。
  5. **wsi**：在“移动端Vulkan”章专属实验中保存loader版本、实例扩展、验证消息、SPIR-V环境和VUID、acquire结果、image index、等待信号量、present结果和重建代次、扩展依赖、features链、limits、shader capability、管线结果和回退路径，注入“请求未支持实例版本，或把验证层通过当成程序逻辑正确；复用仍被present等待的binary semaphore，或resize后继续使用旧extent；只检查扩展名便创建管线，或没有为不支持设备准备可验证fallback”，恢复后以相同输入重放。
  6. **vksurfacekhr**：在“移动端Vulkan”章专属实验中保存loader版本、实例扩展、验证消息、SPIR-V环境和VUID、acquire结果、image index、等待信号量、present结果和重建代次、扩展依赖、features链、limits、shader capability、管线结果和回退路径，注入“请求未支持实例版本，或把验证层通过当成程序逻辑正确；复用仍被present等待的binary semaphore，或resize后继续使用旧extent；只检查扩展名便创建管线，或没有为不支持设备准备可验证fallback”，恢复后以相同输入重放。
  7. **vkswapchainkhr**：在“移动端Vulkan”章专属实验中保存loader版本、实例扩展、验证消息、SPIR-V环境和VUID、acquire结果、image index、等待信号量、present结果和重建代次、扩展依赖、features链、limits、shader capability、管线结果和回退路径，注入“请求未支持实例版本，或把验证层通过当成程序逻辑正确；复用仍被present等待的binary semaphore，或resize后继续使用旧extent；只检查扩展名便创建管线，或没有为不支持设备准备可验证fallback”，恢复后以相同输入重放。
  8. **vkacquirenextimage**：在“移动端Vulkan”章专属实验中保存loader版本、实例扩展、验证消息、SPIR-V环境和VUID、acquire结果、image index、等待信号量、present结果和重建代次、扩展依赖、features链、limits、shader capability、管线结果和回退路径，注入“请求未支持实例版本，或把验证层通过当成程序逻辑正确；复用仍被present等待的binary semaphore，或resize后继续使用旧extent；只检查扩展名便创建管线，或没有为不支持设备准备可验证fallback”，恢复后以相同输入重放。
  9. **vkqueuepresent**：在“移动端Vulkan”章专属实验中保存loader版本、实例扩展、验证消息、SPIR-V环境和VUID、acquire结果、image index、等待信号量、present结果和重建代次、扩展依赖、features链、limits、shader capability、管线结果和回退路径，注入“请求未支持实例版本，或把验证层通过当成程序逻辑正确；复用仍被present等待的binary semaphore，或resize后继续使用旧extent；只检查扩展名便创建管线，或没有为不支持设备准备可验证fallback”，恢复后以相同输入重放。
  10. **out_of_date**：在“移动端Vulkan”章专属实验中保存loader版本、实例扩展、验证消息、SPIR-V环境和VUID、acquire结果、image index、等待信号量、present结果和重建代次、扩展依赖、features链、limits、shader capability、管线结果和回退路径，注入“请求未支持实例版本，或把验证层通过当成程序逻辑正确；复用仍被present等待的binary semaphore，或resize后继续使用旧extent；只检查扩展名便创建管线，或没有为不支持设备准备可验证fallback”，恢复后以相同输入重放。
  11. **compute shader**：在“移动端Vulkan”章专属实验中保存loader版本、实例扩展、验证消息、SPIR-V环境和VUID、acquire结果、image index、等待信号量、present结果和重建代次、扩展依赖、features链、limits、shader capability、管线结果和回退路径，注入“请求未支持实例版本，或把验证层通过当成程序逻辑正确；复用仍被present等待的binary semaphore，或resize后继续使用旧extent；只检查扩展名便创建管线，或没有为不支持设备准备可验证fallback”，恢复后以相同输入重放。
  12. **ray tracing**：在“移动端Vulkan”章专属实验中保存loader版本、实例扩展、验证消息、SPIR-V环境和VUID、acquire结果、image index、等待信号量、present结果和重建代次、扩展依赖、features链、limits、shader capability、管线结果和回退路径，注入“请求未支持实例版本，或把验证层通过当成程序逻辑正确；复用仍被present等待的binary semaphore，或resize后继续使用旧extent；只检查扩展名便创建管线，或没有为不支持设备准备可验证fallback”，恢复后以相同输入重放。
  13. **mesh shader**：在“移动端Vulkan”章专属实验中保存loader版本、实例扩展、验证消息、SPIR-V环境和VUID、acquire结果、image index、等待信号量、present结果和重建代次、扩展依赖、features链、limits、shader capability、管线结果和回退路径，注入“请求未支持实例版本，或把验证层通过当成程序逻辑正确；复用仍被present等待的binary semaphore，或resize后继续使用旧extent；只检查扩展名便创建管线，或没有为不支持设备准备可验证fallback”，恢复后以相同输入重放。
  14. **descriptor indexing**：在“移动端Vulkan”章专属实验中保存loader版本、实例扩展、验证消息、SPIR-V环境和VUID、acquire结果、image index、等待信号量、present结果和重建代次、扩展依赖、features链、limits、shader capability、管线结果和回退路径，注入“请求未支持实例版本，或把验证层通过当成程序逻辑正确；复用仍被present等待的binary semaphore，或resize后继续使用旧extent；只检查扩展名便创建管线，或没有为不支持设备准备可验证fallback”，恢复后以相同输入重放。
  15. **variable rate shading**：在“移动端Vulkan”章专属实验中保存loader版本、实例扩展、验证消息、SPIR-V环境和VUID、acquire结果、image index、等待信号量、present结果和重建代次、扩展依赖、features链、limits、shader capability、管线结果和回退路径，注入“请求未支持实例版本，或把验证层通过当成程序逻辑正确；复用仍被present等待的binary semaphore，或resize后继续使用旧extent；只检查扩展名便创建管线，或没有为不支持设备准备可验证fallback”，恢复后以相同输入重放。
  16. **extension**：在“移动端Vulkan”章专属实验中保存loader版本、实例扩展、验证消息、SPIR-V环境和VUID、acquire结果、image index、等待信号量、present结果和重建代次、扩展依赖、features链、limits、shader capability、管线结果和回退路径，注入“请求未支持实例版本，或把验证层通过当成程序逻辑正确；复用仍被present等待的binary semaphore，或resize后继续使用旧extent；只检查扩展名便创建管线，或没有为不支持设备准备可验证fallback”，恢复后以相同输入重放。
</Answer>

**问题 3：恢复证据。** 怎样证明“请求未支持实例版本，或把验证层通过当成程序逻辑正确；复用仍被present等待的binary semaphore，或resize后继续使用旧extent；只检查扩展名便创建管线，或没有为不支持设备准备可验证fallback”已真正修复？

<Answer>
  保存正常基线，注入故障并标记loader版本、实例扩展、验证消息、SPIR-V环境和VUID、acquire结果、image index、等待信号量、present结果和重建代次、扩展依赖、features链、limits、shader capability、管线结果和回退路径中的首个分叉；撤销故障后以完全相同输入重放。只有移动端Vulkan的输入、状态、输出与恢复结果可用同一证据包重放重新成立且重置回到初值，修复才可交接。
</Answer>

</Exercises>

## 本章回顾

掌握“移动端Vulkan”意味着能把“以实例版本、验证层与SPIR-V能力建立Vulkan运行边界；让surface、swapchain、acquire与present形成可恢复帧循环；按扩展依赖与设备feature启用计算、光追、mesh等高级路径”落到具体对象、状态与输出，能制造“请求未支持实例版本，或把验证层通过当成程序逻辑正确；复用仍被present等待的binary semaphore，或resize后继续使用旧extent；只检查扩展名便创建管线，或没有为不支持设备准备可验证fallback”，还能凭loader版本、实例扩展、验证消息、SPIR-V环境和VUID、acquire结果、image index、等待信号量、present结果和重建代次、扩展依赖、features链、limits、shader capability、管线结果和回退路径恢复同输入结果。

<Glossary>
  <GlossaryItem term="explicit api">
    explicit api在“移动端Vulkan”的“Vulkan Architecture, Versions, Validation, and SPIR-V”检查中用于以实例版本、验证层与SPIR-V能力建立Vulkan运行边界；应由loader版本、实例扩展、验证消息、SPIR-V环境和VUID确认。
  </GlossaryItem>
  <GlossaryItem term="vulkan version">
    vulkan version在“移动端Vulkan”的“Vulkan Architecture, Versions, Validation, and SPIR-V”检查中用于以实例版本、验证层与SPIR-V能力建立Vulkan运行边界；应由loader版本、实例扩展、验证消息、SPIR-V环境和VUID确认。
  </GlossaryItem>
  <GlossaryItem term="validation layer">
    validation layer在“移动端Vulkan”的“Vulkan Architecture, Versions, Validation, and SPIR-V”检查中用于以实例版本、验证层与SPIR-V能力建立Vulkan运行边界；应由loader版本、实例扩展、验证消息、SPIR-V环境和VUID确认。
  </GlossaryItem>
  <GlossaryItem term="spir-v">
    spir-v在“移动端Vulkan”的“Vulkan Architecture, Versions, Validation, and SPIR-V”检查中用于以实例版本、验证层与SPIR-V能力建立Vulkan运行边界；应由loader版本、实例扩展、验证消息、SPIR-V环境和VUID确认。
  </GlossaryItem>
  <GlossaryItem term="wsi">
    wsi在“移动端Vulkan”的“Window System Integration, Surface, Swapchain, Acquire, and Present”检查中用于让surface、swapchain、acquire与present形成可恢复帧循环；应由acquire结果、image index、等待信号量、present结果和重建代次确认。
  </GlossaryItem>
  <GlossaryItem term="vksurfacekhr">
    vksurfacekhr在“移动端Vulkan”的“Window System Integration, Surface, Swapchain, Acquire, and Present”检查中用于让surface、swapchain、acquire与present形成可恢复帧循环；应由acquire结果、image index、等待信号量、present结果和重建代次确认。
  </GlossaryItem>
</Glossary>

## 阅读导航

[← 高级渲染技术简介](/learn/vulkan-guide/07-advanced-topics/vkg-ch15-advanced-rendering)

<Attribution
  mode="independent-rewrite"
  sourceBasis="full-text-primary"
  workTitle="Khronos Vulkan Guide"
  adaptedUrl="https://docs.vulkan.org/guide/latest/"
/>
