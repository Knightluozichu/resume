---
title: 第5章 遮羞的艺术——Windows游戏绘图技巧
type: C
section: 第二篇 · GDI 2D游戏编程
order: 5
description: >-
  第5章 遮羞的艺术——Windows游戏绘图技巧：用内存 DC、兼容位图和 BitBlt
  控制精灵合成与透明掩码，以历史—现代对照、故障恢复和逐节点证据验收。
demo: true
math: false
sourceUrl: 'https://book.douban.com/subject/25756435/'
draft: false
qualityVersion: 2
practiceMode: simulation
sourceMode: independent-rewrite
---

# 第5章 遮羞的艺术——Windows游戏绘图技巧


{/* WINDOWS_JOURNEY_QUALITY_V2 */}

## 问题先于 API

逐对象直接画到窗口 DC 会暴露中间帧并产生闪烁。本章先保存问题、初态和输入，再讨论 API；通过条件是“前台只观察到完整帧，遮罩和源图合成不污染背景”，不是画面偶然出现一次。

## 来源范围、技术年代与独立重写

公开[完整目录](https://book.douban.com/subject/25756435/)只用于界定本页题目和层级，[出版记录](https://www.sinobook.com.cn/b2c/scrp/bookdetail.cfm?iBookNo=646210)与[书目记录](https://books.google.com/books/about/%E9%80%90%E6%A2%A6%E6%97%85%E7%A8%8B.html?id=6O-F0AEACAAJ)用于交叉核对 ISBN、出版社和年代；没有把目录核对表述成正文忠实。本页关于 Windows/DirectX 的机制由[Microsoft 一手文档](https://learn.microsoft.com/en-us/windows/win32/gdi/memory-device-contexts)核验，以下中文讲解、图示、实验、代码和练习均为独立教学重写。

原书处于 Visual Studio 2010、Win32/GDI、DirectX 9 与固定功能管线语境。现代 API 只作迁移对照：GDI 双缓冲和现代离屏表面名称可以不同，但必须消费相同输入并守住同一不变量。

## 机制与术语

、、、。四个术语共同约束“用内存 DC、兼容位图和 BitBlt 控制精灵合成与透明掩码”，结论必须回到缓冲代际、BitBlt 次数、透明像素和帧差分。

## 正式目录逐项深读

### 第5章 遮羞的艺术——Windows游戏绘图技巧

围绕用内存 DC、兼容位图和 BitBlt 控制精灵合成与透明掩码，对 第5章 遮羞的艺术——Windows游戏绘图技巧，把前述机制装入一个可运行场景：固定构建、资源和输入，仅改变一个条件，并保留缓冲代际、BitBlt 次数、透明像素和帧差分。

第5章 遮羞的艺术——Windows游戏绘图技巧 进入“创建后备 DC”阶段时，先标出输入、状态拥有者和可见输出，再解释所有绘制先在兼容内存 DC 完成，再以一次 BitBlt 发布完整帧。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 第5章 遮羞的艺术——Windows游戏绘图技巧 时固定其余条件，记录缓冲代际、BitBlt 次数、透明像素和帧差分，随后注入“源 DC 与目标 DC 尺寸或 ROP 顺序错误”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 5.1 透明贴图的两套体系

围绕用内存 DC、兼容位图和 BitBlt 控制精灵合成与透明掩码，5.1 透明贴图的两套体系 限定本章的一个知识坐标；独立解释围绕“所有绘制先在兼容内存 DC 完成，再以一次 BitBlt 发布完整帧”展开，并以“前台只观察到完整帧，遮罩和源图合成不污染背景”结束。

5.1 透明贴图的两套体系 进入“清理背景”阶段时，先标出输入、状态拥有者和可见输出，再解释所有绘制先在兼容内存 DC 完成，再以一次 BitBlt 发布完整帧。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 5.1 透明贴图的两套体系 时固定其余条件，记录缓冲代际、BitBlt 次数、透明像素和帧差分，随后注入“源 DC 与目标 DC 尺寸或 ROP 顺序错误”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 5.2 透明遮罩法

围绕用内存 DC、兼容位图和 BitBlt 控制精灵合成与透明掩码，5.2 透明遮罩法 限定本章的一个知识坐标；独立解释围绕“所有绘制先在兼容内存 DC 完成，再以一次 BitBlt 发布完整帧”展开，并以“前台只观察到完整帧，遮罩和源图合成不污染背景”结束。

5.2 透明遮罩法 进入“绘制精灵”阶段时，先标出输入、状态拥有者和可见输出，再解释所有绘制先在兼容内存 DC 完成，再以一次 BitBlt 发布完整帧。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 5.2 透明遮罩法 时固定其余条件，记录缓冲代际、BitBlt 次数、透明像素和帧差分，随后注入“源 DC 与目标 DC 尺寸或 ROP 顺序错误”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 5.2.1 具体实现细节

围绕用内存 DC、兼容位图和 BitBlt 控制精灵合成与透明掩码，5.2.1 具体实现细节 限定本章的一个知识坐标；独立解释围绕“所有绘制先在兼容内存 DC 完成，再以一次 BitBlt 发布完整帧”展开，并以“前台只观察到完整帧，遮罩和源图合成不污染背景”结束。

5.2.1 具体实现细节 进入“完成合成”阶段时，先标出输入、状态拥有者和可见输出，再解释所有绘制先在兼容内存 DC 完成，再以一次 BitBlt 发布完整帧。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 5.2.1 具体实现细节 时固定其余条件，记录缓冲代际、BitBlt 次数、透明像素和帧差分，随后注入“源 DC 与目标 DC 尺寸或 ROP 顺序错误”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 5.2.2 示例程序GDIdemo4

围绕用内存 DC、兼容位图和 BitBlt 控制精灵合成与透明掩码，对 5.2.2 示例程序GDIdemo4，把前述机制装入一个可运行场景：固定构建、资源和输入，仅改变一个条件，并保留缓冲代际、BitBlt 次数、透明像素和帧差分。

5.2.2 示例程序GDIdemo4 进入“一次发布”阶段时，先标出输入、状态拥有者和可见输出，再解释所有绘制先在兼容内存 DC 完成，再以一次 BitBlt 发布完整帧。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 5.2.2 示例程序GDIdemo4 时固定其余条件，记录缓冲代际、BitBlt 次数、透明像素和帧差分，随后注入“源 DC 与目标 DC 尺寸或 ROP 顺序错误”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 5.3 透明色彩法

围绕用内存 DC、兼容位图和 BitBlt 控制精灵合成与透明掩码，5.3 透明色彩法 限定本章的一个知识坐标；独立解释围绕“所有绘制先在兼容内存 DC 完成，再以一次 BitBlt 发布完整帧”展开，并以“前台只观察到完整帧，遮罩和源图合成不污染背景”结束。

5.3 透明色彩法 进入“创建后备 DC”阶段时，先标出输入、状态拥有者和可见输出，再解释所有绘制先在兼容内存 DC 完成，再以一次 BitBlt 发布完整帧。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 5.3 透明色彩法 时固定其余条件，记录缓冲代际、BitBlt 次数、透明像素和帧差分，随后注入“源 DC 与目标 DC 尺寸或 ROP 顺序错误”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 5.3.1 具体实现细节

围绕用内存 DC、兼容位图和 BitBlt 控制精灵合成与透明掩码，5.3.1 具体实现细节 限定本章的一个知识坐标；独立解释围绕“所有绘制先在兼容内存 DC 完成，再以一次 BitBlt 发布完整帧”展开，并以“前台只观察到完整帧，遮罩和源图合成不污染背景”结束。

5.3.1 具体实现细节 进入“清理背景”阶段时，先标出输入、状态拥有者和可见输出，再解释所有绘制先在兼容内存 DC 完成，再以一次 BitBlt 发布完整帧。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 5.3.1 具体实现细节 时固定其余条件，记录缓冲代际、BitBlt 次数、透明像素和帧差分，随后注入“源 DC 与目标 DC 尺寸或 ROP 顺序错误”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 5.3.2 示例程序GDIdemo5

围绕用内存 DC、兼容位图和 BitBlt 控制精灵合成与透明掩码，对 5.3.2 示例程序GDIdemo5，把前述机制装入一个可运行场景：固定构建、资源和输入，仅改变一个条件，并保留缓冲代际、BitBlt 次数、透明像素和帧差分。

5.3.2 示例程序GDIdemo5 进入“绘制精灵”阶段时，先标出输入、状态拥有者和可见输出，再解释所有绘制先在兼容内存 DC 完成，再以一次 BitBlt 发布完整帧。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 5.3.2 示例程序GDIdemo5 时固定其余条件，记录缓冲代际、BitBlt 次数、透明像素和帧差分，随后注入“源 DC 与目标 DC 尺寸或 ROP 顺序错误”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 5.4 自己动手处理图片素材

围绕用内存 DC、兼容位图和 BitBlt 控制精灵合成与透明掩码，5.4 自己动手处理图片素材 限定本章的一个知识坐标；独立解释围绕“所有绘制先在兼容内存 DC 完成，再以一次 BitBlt 发布完整帧”展开，并以“前台只观察到完整帧，遮罩和源图合成不污染背景”结束。

5.4 自己动手处理图片素材 进入“完成合成”阶段时，先标出输入、状态拥有者和可见输出，再解释所有绘制先在兼容内存 DC 完成，再以一次 BitBlt 发布完整帧。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 5.4 自己动手处理图片素材 时固定其余条件，记录缓冲代际、BitBlt 次数、透明像素和帧差分，随后注入“源 DC 与目标 DC 尺寸或 ROP 顺序错误”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 5.4.1 游戏素材的来源

围绕用内存 DC、兼容位图和 BitBlt 控制精灵合成与透明掩码，对 5.4.1 游戏素材的来源，把前述机制装入一个可运行场景：固定构建、资源和输入，仅改变一个条件，并保留缓冲代际、BitBlt 次数、透明像素和帧差分。

5.4.1 游戏素材的来源 进入“一次发布”阶段时，先标出输入、状态拥有者和可见输出，再解释所有绘制先在兼容内存 DC 完成，再以一次 BitBlt 发布完整帧。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 5.4.1 游戏素材的来源 时固定其余条件，记录缓冲代际、BitBlt 次数、透明像素和帧差分，随后注入“源 DC 与目标 DC 尺寸或 ROP 顺序错误”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 5.4.2 Photoshop图像处理软件

围绕用内存 DC、兼容位图和 BitBlt 控制精灵合成与透明掩码，5.4.2 Photoshop图像处理软件 限定本章的一个知识坐标；独立解释围绕“所有绘制先在兼容内存 DC 完成，再以一次 BitBlt 发布完整帧”展开，并以“前台只观察到完整帧，遮罩和源图合成不污染背景”结束。

5.4.2 Photoshop图像处理软件 进入“创建后备 DC”阶段时，先标出输入、状态拥有者和可见输出，再解释所有绘制先在兼容内存 DC 完成，再以一次 BitBlt 发布完整帧。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 5.4.2 Photoshop图像处理软件 时固定其余条件，记录缓冲代际、BitBlt 次数、透明像素和帧差分，随后注入“源 DC 与目标 DC 尺寸或 ROP 顺序错误”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 5.4.3 处理游戏素材图片

围绕用内存 DC、兼容位图和 BitBlt 控制精灵合成与透明掩码，对 5.4.3 处理游戏素材图片，把前述机制装入一个可运行场景：固定构建、资源和输入，仅改变一个条件，并保留缓冲代际、BitBlt 次数、透明像素和帧差分。

5.4.3 处理游戏素材图片 进入“清理背景”阶段时，先标出输入、状态拥有者和可见输出，再解释所有绘制先在兼容内存 DC 完成，再以一次 BitBlt 发布完整帧。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 5.4.3 处理游戏素材图片 时固定其余条件，记录缓冲代际、BitBlt 次数、透明像素和帧差分，随后注入“源 DC 与目标 DC 尺寸或 ROP 顺序错误”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 5.5 章节小憩

围绕用内存 DC、兼容位图和 BitBlt 控制精灵合成与透明掩码，对 5.5 章节小憩，收尾不是装饰，而是要求用缓冲代际、BitBlt 次数、透明像素和帧差分复盘“前台只观察到完整帧，遮罩和源图合成不污染背景”是否在正常和失败路径同时成立。

5.5 章节小憩 进入“绘制精灵”阶段时，先标出输入、状态拥有者和可见输出，再解释所有绘制先在兼容内存 DC 完成，再以一次 BitBlt 发布完整帧。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 5.5 章节小憩 时固定其余条件，记录缓冲代际、BitBlt 次数、透明像素和帧差分，随后注入“源 DC 与目标 DC 尺寸或 ROP 顺序错误”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

## 可重放实现或计算骨架

```text
freeze -> 创建后备 DC -> 清理背景 -> 绘制精灵 -> 完成合成 -> 一次发布
fault -> 源 DC 与目标 DC 尺寸或 ROP 顺序错误
evidence -> 缓冲代际、BitBlt 次数、透明像素和帧差分
reset -> replay identical input
```

真实程序还要固定编译器、SDK、窗口尺寸、资源、随机种子、时间步和输入记录；否则历史实现与现代迁移不能公平比较。


## 练习与 4 级证据矩阵


## 术语复核与本章回顾


掌握“第5章 遮羞的艺术——Windows游戏绘图技巧”意味着能解释所有绘制先在兼容内存 DC 完成，再以一次 BitBlt 发布完整帧，区分历史坐标与现代迁移，并用缓冲代际、BitBlt 次数、透明像素和帧差分推翻或保留实现。

## 阅读导航

[← 上一页：第4章 入门心法——Windows游戏图形基础](/learn/windows-journey/03-gdi-2d/wj-04-gdi-foundations) · [下一页：第6章 光与影的交汇——Windows游戏动画技术 →](/learn/windows-journey/03-gdi-2d/wj-06-windows-animation)

