---
title: 第6章 光与影的交汇——Windows游戏动画技术
type: C
section: 第二篇 · GDI 2D游戏编程
order: 6
description: 第6章 光与影的交汇——Windows游戏动画技术：把真实时间、固定更新步长、动画帧索引和发布频率解耦，以历史—现代对照、故障恢复和逐节点证据验收。
demo: true
math: false
sourceUrl: 'https://book.douban.com/subject/25756435/'
draft: false
qualityVersion: 2
practiceMode: calculation
sourceMode: independent-rewrite
---

# 第6章 光与影的交汇——Windows游戏动画技术


{/* WINDOWS_JOURNEY_QUALITY_V2 */}

## 问题先于 API

每次循环移动固定距离会让动画速度随机器和窗口消息负载变化。本章先保存问题、初态和输入，再讨论 API；通过条件是“相同真实时长和输入产生相同模拟时间与动画帧序列”，不是画面偶然出现一次。

## 来源范围、技术年代与独立重写

公开[完整目录](https://book.douban.com/subject/25756435/)只用于界定本页题目和层级，[出版记录](https://www.sinobook.com.cn/b2c/scrp/bookdetail.cfm?iBookNo=646210)与[书目记录](https://books.google.com/books/about/%E9%80%90%E6%A2%A6%E6%97%85%E7%A8%8B.html?id=6O-F0AEACAAJ)用于交叉核对 ISBN、出版社和年代；没有把目录核对表述成正文忠实。本页关于 Windows/DirectX 的机制由[Microsoft 一手文档](https://learn.microsoft.com/en-us/windows/win32/winmsg/about-messages-and-message-queues)核验，以下中文讲解、图示、实验、代码和练习均为独立教学重写。

原书处于 Visual Studio 2010、Win32/GDI、DirectX 9 与固定功能管线语境。现代 API 只作迁移对照：Sleep 定时循环和固定步长循环名称可以不同，但必须消费相同输入并守住同一不变量。

## 机制与术语

、、、。四个术语共同约束“把真实时间、固定更新步长、动画帧索引和发布频率解耦”，结论必须回到真实时间、模拟时间、更新次数、丢帧和帧时分位数。

## 正式目录逐项深读

### 第6章 光与影的交汇——Windows游戏动画技术

围绕把真实时间、固定更新步长、动画帧索引和发布频率解耦，对 第6章 光与影的交汇——Windows游戏动画技术，把前述机制装入一个可运行场景：固定构建、资源和输入，仅改变一个条件，并保留真实时间、模拟时间、更新次数、丢帧和帧时分位数。

第6章 光与影的交汇——Windows游戏动画技术 进入“采样时间”阶段时，先标出输入、状态拥有者和可见输出，再解释累积真实经过时间，执行零到多次固定步更新，再绘制当前状态。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 第6章 光与影的交汇——Windows游戏动画技术 时固定其余条件，记录真实时间、模拟时间、更新次数、丢帧和帧时分位数，随后注入“长帧后无限追赶形成死亡螺旋”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 6.1 定时器动画显示

围绕把真实时间、固定更新步长、动画帧索引和发布频率解耦，对 6.1 定时器动画显示，重点检查事件与时间顺序，必须区分采集、状态快照、固定更新和可见发布，不能由最终画面倒推过程正确。

6.1 定时器动画显示 进入“累积差值”阶段时，先标出输入、状态拥有者和可见输出，再解释累积真实经过时间，执行零到多次固定步更新，再绘制当前状态。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 6.1 定时器动画显示 时固定其余条件，记录真实时间、模拟时间、更新次数、丢帧和帧时分位数，随后注入“长帧后无限追赶形成死亡螺旋”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 6.1.1 创建定时器

围绕把真实时间、固定更新步长、动画帧索引和发布频率解耦，对 6.1.1 创建定时器，重点检查资源生命周期：创建成功只完成一半，失败回滚、逆序释放和同输入重建同样属于通过条件。

6.1.1 创建定时器 进入“固定更新”阶段时，先标出输入、状态拥有者和可见输出，再解释累积真实经过时间，执行零到多次固定步更新，再绘制当前状态。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 6.1.1 创建定时器 时固定其余条件，记录真实时间、模拟时间、更新次数、丢帧和帧时分位数，随后注入“长帧后无限追赶形成死亡螺旋”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 6.1.2 WM_TIMER消息响应

围绕把真实时间、固定更新步长、动画帧索引和发布频率解耦，对 6.1.2 WM_TIMER消息响应，重点检查事件与时间顺序，必须区分采集、状态快照、固定更新和可见发布，不能由最终画面倒推过程正确。

6.1.2 WM_TIMER消息响应 进入“选择画格”阶段时，先标出输入、状态拥有者和可见输出，再解释累积真实经过时间，执行零到多次固定步更新，再绘制当前状态。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 6.1.2 WM_TIMER消息响应 时固定其余条件，记录真实时间、模拟时间、更新次数、丢帧和帧时分位数，随后注入“长帧后无限追赶形成死亡螺旋”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 6.1.3 删除定时器

围绕把真实时间、固定更新步长、动画帧索引和发布频率解耦，6.1.3 删除定时器 限定本章的一个知识坐标；独立解释围绕“累积真实经过时间，执行零到多次固定步更新，再绘制当前状态”展开，并以“相同真实时长和输入产生相同模拟时间与动画帧序列”结束。

6.1.3 删除定时器 进入“发布帧”阶段时，先标出输入、状态拥有者和可见输出，再解释累积真实经过时间，执行零到多次固定步更新，再绘制当前状态。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 6.1.3 删除定时器 时固定其余条件，记录真实时间、模拟时间、更新次数、丢帧和帧时分位数，随后注入“长帧后无限追赶形成死亡螺旋”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 6.1.4 示例程序GDIdemo6

围绕把真实时间、固定更新步长、动画帧索引和发布频率解耦，对 6.1.4 示例程序GDIdemo6，把前述机制装入一个可运行场景：固定构建、资源和输入，仅改变一个条件，并保留真实时间、模拟时间、更新次数、丢帧和帧时分位数。

6.1.4 示例程序GDIdemo6 进入“采样时间”阶段时，先标出输入、状态拥有者和可见输出，再解释累积真实经过时间，执行零到多次固定步更新，再绘制当前状态。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 6.1.4 示例程序GDIdemo6 时固定其余条件，记录真实时间、模拟时间、更新次数、丢帧和帧时分位数，随后注入“长帧后无限追赶形成死亡螺旋”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 6.2 游戏循环动画显示

围绕把真实时间、固定更新步长、动画帧索引和发布频率解耦，对 6.2 游戏循环动画显示，把前述机制装入一个可运行场景：固定构建、资源和输入，仅改变一个条件，并保留真实时间、模拟时间、更新次数、丢帧和帧时分位数。

6.2 游戏循环动画显示 进入“累积差值”阶段时，先标出输入、状态拥有者和可见输出，再解释累积真实经过时间，执行零到多次固定步更新，再绘制当前状态。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 6.2 游戏循环动画显示 时固定其余条件，记录真实时间、模拟时间、更新次数、丢帧和帧时分位数，随后注入“长帧后无限追赶形成死亡螺旋”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 6.3 透明动画

围绕把真实时间、固定更新步长、动画帧索引和发布频率解耦，对 6.3 透明动画，重点检查事件与时间顺序，必须区分采集、状态快照、固定更新和可见发布，不能由最终画面倒推过程正确。

6.3 透明动画 进入“固定更新”阶段时，先标出输入、状态拥有者和可见输出，再解释累积真实经过时间，执行零到多次固定步更新，再绘制当前状态。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 6.3 透明动画 时固定其余条件，记录真实时间、模拟时间、更新次数、丢帧和帧时分位数，随后注入“长帧后无限追赶形成死亡螺旋”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 6.4 排序贴图

围绕把真实时间、固定更新步长、动画帧索引和发布频率解耦，6.4 排序贴图 限定本章的一个知识坐标；独立解释围绕“累积真实经过时间，执行零到多次固定步更新，再绘制当前状态”展开，并以“相同真实时长和输入产生相同模拟时间与动画帧序列”结束。

6.4 排序贴图 进入“选择画格”阶段时，先标出输入、状态拥有者和可见输出，再解释累积真实经过时间，执行零到多次固定步更新，再绘制当前状态。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 6.4 排序贴图 时固定其余条件，记录真实时间、模拟时间、更新次数、丢帧和帧时分位数，随后注入“长帧后无限追赶形成死亡螺旋”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 6.5 章节小憩

围绕把真实时间、固定更新步长、动画帧索引和发布频率解耦，对 6.5 章节小憩，收尾不是装饰，而是要求用真实时间、模拟时间、更新次数、丢帧和帧时分位数复盘“相同真实时长和输入产生相同模拟时间与动画帧序列”是否在正常和失败路径同时成立。

6.5 章节小憩 进入“发布帧”阶段时，先标出输入、状态拥有者和可见输出，再解释累积真实经过时间，执行零到多次固定步更新，再绘制当前状态。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 6.5 章节小憩 时固定其余条件，记录真实时间、模拟时间、更新次数、丢帧和帧时分位数，随后注入“长帧后无限追赶形成死亡螺旋”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

## 可重放实现或计算骨架

```text
initial = fixed_state + fixed_input + fixed_timestep
historical = run(initial, "Sleep 定时循环")
modern = run(initial, "固定步长循环")
compare(historical, modern, "ms 偏差")
pass = "相同真实时长和输入产生相同模拟时间与动画帧序列"
```

真实程序还要固定编译器、SDK、窗口尺寸、资源、随机种子、时间步和输入记录；否则历史实现与现代迁移不能公平比较。


## 练习与 4 级证据矩阵


## 术语复核与本章回顾


掌握“第6章 光与影的交汇——Windows游戏动画技术”意味着能解释累积真实经过时间，执行零到多次固定步更新，再绘制当前状态，区分历史坐标与现代迁移，并用真实时间、模拟时间、更新次数、丢帧和帧时分位数推翻或保留实现。

## 阅读导航

[← 上一页：第5章 遮羞的艺术——Windows游戏绘图技巧](/learn/windows-journey/03-gdi-2d/wj-05-gdi-drawing) · [下一页：第7章 做游戏的主人——Windows游戏输入消息处理 →](/learn/windows-journey/03-gdi-2d/wj-07-input-messages)

