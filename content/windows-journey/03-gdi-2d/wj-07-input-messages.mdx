---
title: 第7章 做游戏的主人——Windows游戏输入消息处理
type: C
section: 第二篇 · GDI 2D游戏编程
order: 7
description: 第7章 做游戏的主人——Windows游戏输入消息处理：区分离散按键消息、连续按住状态、鼠标坐标和输入焦点，以历史—现代对照、故障恢复和逐节点证据验收。
demo: true
math: false
sourceUrl: 'https://book.douban.com/subject/25756435/'
draft: true
qualityVersion: 2
practiceMode: simulation
sourceMode: independent-rewrite
---
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

# 第7章 做游戏的主人——Windows游戏输入消息处理

<Objectives>

- 能从“把按键消息当成连续状态会丢失按住语义，把屏幕坐标当客户区坐标会错位”建立本章的历史问题基线。
- 能解释“消息更新输入快照，固定更新阶段只消费已归一化的动作状态”的输入、状态、所有权和可见结果。
- 能操作专属实验，对照Win32 输入消息与动作映射层，复算丢失动作。
- 能注入“窗口失焦后按键状态未清零导致角色持续移动”，用消息时间戳、动作快照、坐标换算和焦点转换定位首错、恢复并重放。

</Objectives>

{/* WINDOWS_JOURNEY_QUALITY_V2 */}

## 问题先于 API

把按键消息当成连续状态会丢失按住语义，把屏幕坐标当客户区坐标会错位。本章先保存问题、初态和输入，再讨论 API；通过条件是“同一输入记录与窗口尺寸产生相同动作序列”，不是画面偶然出现一次。

## 来源范围、技术年代与独立重写

公开[完整目录](https://book.douban.com/subject/25756435/)只用于界定本页题目和层级，[出版记录](https://www.sinobook.com.cn/b2c/scrp/bookdetail.cfm?iBookNo=646210)与[书目记录](https://books.google.com/books/about/%E9%80%90%E6%A2%A6%E6%97%85%E7%A8%8B.html?id=6O-F0AEACAAJ)用于交叉核对 ISBN、出版社和年代；没有把目录核对表述成正文忠实。本页关于 Windows/DirectX 的机制由[Microsoft 一手文档](https://learn.microsoft.com/en-us/windows/win32/winmsg/about-messages-and-message-queues)核验，以下中文讲解、图示、实验、代码和练习均为独立教学重写。

原书处于 Visual Studio 2010、Win32/GDI、DirectX 9 与固定功能管线语境。现代 API 只作迁移对照：Win32 输入消息和动作映射层名称可以不同，但必须消费相同输入并守住同一不变量。

## 机制与术语

<Term def="键盘消息在“第7章 做游戏的主人——Windows游戏输入消息处理”中标记输入进入系统时的责任边界。">键盘消息</Term>、<Term def="鼠标消息在“第7章 做游戏的主人——Windows游戏输入消息处理”中标记状态被创建、修改与释放的生命周期。">鼠标消息</Term>、<Term def="客户区坐标在“第7章 做游戏的主人——Windows游戏输入消息处理”中标记历史实现与现代迁移之间必须保持的机制。">客户区坐标</Term>、<Term def="输入快照在“第7章 做游戏的主人——Windows游戏输入消息处理”中标记能够复算本章结论的原始证据。">输入快照</Term>。四个术语共同约束“区分离散按键消息、连续按住状态、鼠标坐标和输入焦点”，结论必须回到消息时间戳、动作快照、坐标换算和焦点转换。

## 正式目录逐项深读

### 第7章 做游戏的主人——Windows游戏输入消息处理

围绕区分离散按键消息、连续按住状态、鼠标坐标和输入焦点，对 第7章 做游戏的主人——Windows游戏输入消息处理，把前述机制装入一个可运行场景：固定构建、资源和输入，仅改变一个条件，并保留消息时间戳、动作快照、坐标换算和焦点转换。

第7章 做游戏的主人——Windows游戏输入消息处理 进入“接收消息”阶段时，先标出输入、状态拥有者和可见输出，再解释消息更新输入快照，固定更新阶段只消费已归一化的动作状态。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 第7章 做游戏的主人——Windows游戏输入消息处理 时固定其余条件，记录消息时间戳、动作快照、坐标换算和焦点转换，随后注入“窗口失焦后按键状态未清零导致角色持续移动”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 7.1 Windows键盘消息处理

围绕区分离散按键消息、连续按住状态、鼠标坐标和输入焦点，对 7.1 Windows键盘消息处理，重点检查事件与时间顺序，必须区分采集、状态快照、固定更新和可见发布，不能由最终画面倒推过程正确。

7.1 Windows键盘消息处理 进入“换算坐标”阶段时，先标出输入、状态拥有者和可见输出，再解释消息更新输入快照，固定更新阶段只消费已归一化的动作状态。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 7.1 Windows键盘消息处理 时固定其余条件，记录消息时间戳、动作快照、坐标换算和焦点转换，随后注入“窗口失焦后按键状态未清零导致角色持续移动”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 7.1.1 虚拟键码与键盘消息

围绕区分离散按键消息、连续按住状态、鼠标坐标和输入焦点，对 7.1.1 虚拟键码与键盘消息，重点检查事件与时间顺序，必须区分采集、状态快照、固定更新和可见发布，不能由最终画面倒推过程正确。

7.1.1 虚拟键码与键盘消息 进入“更新快照”阶段时，先标出输入、状态拥有者和可见输出，再解释消息更新输入快照，固定更新阶段只消费已归一化的动作状态。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 7.1.1 虚拟键码与键盘消息 时固定其余条件，记录消息时间戳、动作快照、坐标换算和焦点转换，随后注入“窗口失焦后按键状态未清零导致角色持续移动”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 7.1.2 键盘消息处理

围绕区分离散按键消息、连续按住状态、鼠标坐标和输入焦点，对 7.1.2 键盘消息处理，重点检查事件与时间顺序，必须区分采集、状态快照、固定更新和可见发布，不能由最终画面倒推过程正确。

7.1.2 键盘消息处理 进入“消费动作”阶段时，先标出输入、状态拥有者和可见输出，再解释消息更新输入快照，固定更新阶段只消费已归一化的动作状态。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 7.1.2 键盘消息处理 时固定其余条件，记录消息时间戳、动作快照、坐标换算和焦点转换，随后注入“窗口失焦后按键状态未清零导致角色持续移动”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 7.1.3 示例程序GDIdemo10

围绕区分离散按键消息、连续按住状态、鼠标坐标和输入焦点，对 7.1.3 示例程序GDIdemo10，把前述机制装入一个可运行场景：固定构建、资源和输入，仅改变一个条件，并保留消息时间戳、动作快照、坐标换算和焦点转换。

7.1.3 示例程序GDIdemo10 进入“焦点复位”阶段时，先标出输入、状态拥有者和可见输出，再解释消息更新输入快照，固定更新阶段只消费已归一化的动作状态。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 7.1.3 示例程序GDIdemo10 时固定其余条件，记录消息时间戳、动作快照、坐标换算和焦点转换，随后注入“窗口失焦后按键状态未清零导致角色持续移动”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 7.2 Windows鼠标消息处理

围绕区分离散按键消息、连续按住状态、鼠标坐标和输入焦点，对 7.2 Windows鼠标消息处理，重点检查事件与时间顺序，必须区分采集、状态快照、固定更新和可见发布，不能由最终画面倒推过程正确。

7.2 Windows鼠标消息处理 进入“接收消息”阶段时，先标出输入、状态拥有者和可见输出，再解释消息更新输入快照，固定更新阶段只消费已归一化的动作状态。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 7.2 Windows鼠标消息处理 时固定其余条件，记录消息时间戳、动作快照、坐标换算和焦点转换，随后注入“窗口失焦后按键状态未清零导致角色持续移动”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 7.2.1 鼠标消息的处理方式

围绕区分离散按键消息、连续按住状态、鼠标坐标和输入焦点，对 7.2.1 鼠标消息的处理方式，重点检查事件与时间顺序，必须区分采集、状态快照、固定更新和可见发布，不能由最终画面倒推过程正确。

7.2.1 鼠标消息的处理方式 进入“换算坐标”阶段时，先标出输入、状态拥有者和可见输出，再解释消息更新输入快照，固定更新阶段只消费已归一化的动作状态。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 7.2.1 鼠标消息的处理方式 时固定其余条件，记录消息时间戳、动作快照、坐标换算和焦点转换，随后注入“窗口失焦后按键状态未清零导致角色持续移动”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 7.2.2 鼠标相关常用函数讲解

围绕区分离散按键消息、连续按住状态、鼠标坐标和输入焦点，对 7.2.2 鼠标相关常用函数讲解，API 名称属于具体年代；学习重点是输入、返回值、所有权和失败出口，技术事实以微软文档核对。

7.2.2 鼠标相关常用函数讲解 进入“更新快照”阶段时，先标出输入、状态拥有者和可见输出，再解释消息更新输入快照，固定更新阶段只消费已归一化的动作状态。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 7.2.2 鼠标相关常用函数讲解 时固定其余条件，记录消息时间戳、动作快照、坐标换算和焦点转换，随后注入“窗口失焦后按键状态未清零导致角色持续移动”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 7.3 章节小憩

围绕区分离散按键消息、连续按住状态、鼠标坐标和输入焦点，对 7.3 章节小憩，收尾不是装饰，而是要求用消息时间戳、动作快照、坐标换算和焦点转换复盘“同一输入记录与窗口尺寸产生相同动作序列”是否在正常和失败路径同时成立。

7.3 章节小憩 进入“消费动作”阶段时，先标出输入、状态拥有者和可见输出，再解释消息更新输入快照，固定更新阶段只消费已归一化的动作状态。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 7.3 章节小憩 时固定其余条件，记录消息时间戳、动作快照、坐标换算和焦点转换，随后注入“窗口失焦后按键状态未清零导致角色持续移动”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

## 可重放实现或计算骨架

```text
freeze -> 接收消息 -> 换算坐标 -> 更新快照 -> 消费动作 -> 焦点复位
fault -> 窗口失焦后按键状态未清零导致角色持续移动
evidence -> 消息时间戳、动作快照、坐标换算和焦点转换
reset -> replay identical input
```

真实程序还要固定编译器、SDK、窗口尺寸、资源、随机种子、时间步和输入记录；否则历史实现与现代迁移不能公平比较。

<Callout type="trap" title="把历史 API 当成当前默认方案">
  本章保留 Win32 输入消息 的教学身份，但新项目必须核对平台支持并解释为何选择 动作映射层 或其他方案。
</Callout>

<Callout type="trap" title="只看最终画面">
  第7章 做游戏的主人——Windows游戏输入消息处理必须说明接收消息、换算坐标、更新快照、消费动作、焦点复位的先后关系；最终像素相同不代表消息、资源、矩阵或状态安全。
</Callout>

<Callout type="trap" title="失败后继续运行">
  注入“窗口失焦后按键状态未清零导致角色持续移动”后应停在第一个因果分叉；若消息时间戳、动作快照、坐标换算和焦点转换无法解释并复位，当前实现失败。
</Callout>

## 练习与 4 级证据矩阵

<Exercises>

**问题 1：历史边界。** 怎样避免把 2013 年接口示例误报成今天的默认架构？

<Answer>
  分别记录Win32 输入消息与动作映射层的版本、责任和资源模型；只迁移机制，不改写历史事实，并以[Microsoft 文档](https://learn.microsoft.com/en-us/windows/win32/winmsg/about-messages-and-message-queues)核对接口身份。
</Answer>

**问题 2：逐节点验证。** 正式目录节点怎样从“出现”升级到可复查证据？

<Answer>
  - **第7章 做游戏的主人——Windows游戏输入消息处理**：在“接收消息”记录消息时间戳、动作快照、坐标换算和焦点转换，注入“窗口失焦后按键状态未清零导致角色持续移动”后复位并重放。
  - **7.1 Windows键盘消息处理**：在“换算坐标”记录消息时间戳、动作快照、坐标换算和焦点转换，注入“窗口失焦后按键状态未清零导致角色持续移动”后复位并重放。
  - **7.1.1 虚拟键码与键盘消息**：在“更新快照”记录消息时间戳、动作快照、坐标换算和焦点转换，注入“窗口失焦后按键状态未清零导致角色持续移动”后复位并重放。
  - **7.1.2 键盘消息处理**：在“消费动作”记录消息时间戳、动作快照、坐标换算和焦点转换，注入“窗口失焦后按键状态未清零导致角色持续移动”后复位并重放。
  - **7.1.3 示例程序GDIdemo10**：在“焦点复位”记录消息时间戳、动作快照、坐标换算和焦点转换，注入“窗口失焦后按键状态未清零导致角色持续移动”后复位并重放。
  - **7.2 Windows鼠标消息处理**：在“接收消息”记录消息时间戳、动作快照、坐标换算和焦点转换，注入“窗口失焦后按键状态未清零导致角色持续移动”后复位并重放。
  - **7.2.1 鼠标消息的处理方式**：在“换算坐标”记录消息时间戳、动作快照、坐标换算和焦点转换，注入“窗口失焦后按键状态未清零导致角色持续移动”后复位并重放。
  - **7.2.2 鼠标相关常用函数讲解**：在“更新快照”记录消息时间戳、动作快照、坐标换算和焦点转换，注入“窗口失焦后按键状态未清零导致角色持续移动”后复位并重放。
  - **7.3 章节小憩**：在“消费动作”记录消息时间戳、动作快照、坐标换算和焦点转换，注入“窗口失焦后按键状态未清零导致角色持续移动”后复位并重放。
</Answer>

**问题 3：故障闭环。** 注入“窗口失焦后按键状态未清零导致角色持续移动”后怎样判定恢复成功？

<Answer>
  保存首错阶段，逆序清理或恢复必要状态，以相同初态和输入重放；只有消息时间戳、动作快照、坐标换算和焦点转换回到基线且“同一输入记录与窗口尺寸产生相同动作序列”再次成立，才算恢复。
</Answer>

</Exercises>

## 术语复核与本章回顾

<Glossary>
  <GlossaryItem term="键盘消息">
    键盘消息在“第7章 做游戏的主人——Windows游戏输入消息处理”中标记输入进入系统时的责任边界。
  </GlossaryItem>
  <GlossaryItem term="鼠标消息">
    鼠标消息在“第7章 做游戏的主人——Windows游戏输入消息处理”中标记状态被创建、修改与释放的生命周期。
  </GlossaryItem>
  <GlossaryItem term="客户区坐标">
    客户区坐标在“第7章 做游戏的主人——Windows游戏输入消息处理”中标记历史实现与现代迁移之间必须保持的机制。
  </GlossaryItem>
  <GlossaryItem term="输入快照">
    输入快照在“第7章 做游戏的主人——Windows游戏输入消息处理”中标记能够复算本章结论的原始证据。
  </GlossaryItem>
</Glossary>

掌握“第7章 做游戏的主人——Windows游戏输入消息处理”意味着能解释消息更新输入快照，固定更新阶段只消费已归一化的动作状态，区分历史坐标与现代迁移，并用消息时间戳、动作快照、坐标换算和焦点转换推翻或保留实现。

## 阅读导航

[← 上一页：第6章 光与影的交汇——Windows游戏动画技术](/learn/windows-journey/03-gdi-2d/wj-06-windows-animation) · [下一页：第8章 玄妙的物理——物理建模与粒子系统初步 →](/learn/windows-journey/03-gdi-2d/wj-08-physics-particles)

<Attribution
  mode="independent-rewrite"
  sourceBasis="outline-only"
  workTitle="毛星云《逐梦旅程：Windows游戏编程之从零开始》（清华大学出版社，2013）"
  adaptedUrl="https://book.douban.com/subject/25756435/"
/>
