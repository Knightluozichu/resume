---
title: 第11章 三维内功心法——Direct3D编程基础
type: C
section: 第三篇 · DirectX游戏编程基础
order: 11
description: >-
  第11章 三维内功心法——Direct3D编程基础：从 IDirect3D9 到设备、交换链、后备缓冲和 Present
  建立帧生命周期，以历史—现代对照、故障恢复和逐节点证据验收。
demo: true
math: false
sourceUrl: 'https://book.douban.com/subject/25756435/'
draft: false
qualityVersion: 2
practiceMode: code
sourceMode: independent-rewrite
---

# 第11章 三维内功心法——Direct3D编程基础


{/* WINDOWS_JOURNEY_QUALITY_V2 */}

## 问题先于 API

设备创建成功不等于适配器、格式、呈现参数和丢失恢复正确。本章先保存问题、初态和输入，再讨论 API；通过条件是“设备状态、场景配对和资源释放在成功与失败路径都完整”，不是画面偶然出现一次。

## 来源范围、技术年代与独立重写

公开[完整目录](https://book.douban.com/subject/25756435/)只用于界定本页题目和层级，[出版记录](https://www.sinobook.com.cn/b2c/scrp/bookdetail.cfm?iBookNo=646210)与[书目记录](https://books.google.com/books/about/%E9%80%90%E6%A2%A6%E6%97%85%E7%A8%8B.html?id=6O-F0AEACAAJ)用于交叉核对 ISBN、出版社和年代；没有把目录核对表述成正文忠实。本页关于 Windows/DirectX 的机制由[Microsoft 一手文档](https://learn.microsoft.com/en-us/windows/win32/direct3d9/lost-devices)核验，以下中文讲解、图示、实验、代码和练习均为独立教学重写。

原书处于 Visual Studio 2010、Win32/GDI、DirectX 9 与固定功能管线语境。现代 API 只作迁移对照：Direct3D 9 设备和显式资源设备名称可以不同，但必须消费相同输入并守住同一不变量。

## 机制与术语

、、、。四个术语共同约束“从 IDirect3D9 到设备、交换链、后备缓冲和 Present 建立帧生命周期”，结论必须回到HRESULT、呈现参数、设备状态、COM 引用和恢复轨迹。

## 正式目录逐项深读

### 第11章 三维内功心法——Direct3D编程基础

围绕从 IDirect3D9 到设备、交换链、后备缓冲和 Present 建立帧生命周期，对 第11章 三维内功心法——Direct3D编程基础，API 名称属于具体年代；学习重点是输入、返回值、所有权和失败出口，技术事实以微软文档核对。

第11章 三维内功心法——Direct3D编程基础 进入“创建 D3D”阶段时，先标出输入、状态拥有者和可见输出，再解释枚举能力并创建设备，按 Clear、BeginScene、绘制、EndScene、Present 发布一帧。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 第11章 三维内功心法——Direct3D编程基础 时固定其余条件，记录HRESULT、呈现参数、设备状态、COM 引用和恢复轨迹，随后注入“全屏焦点切换后忽略 D3DERR_DEVICELOST”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 11.1 典型Direct3D程序流程分析

围绕从 IDirect3D9 到设备、交换链、后备缓冲和 Present 建立帧生命周期，对 11.1 典型Direct3D程序流程分析，把前述机制装入一个可运行场景：固定构建、资源和输入，仅改变一个条件，并保留HRESULT、呈现参数、设备状态、COM 引用和恢复轨迹。

11.1 典型Direct3D程序流程分析 进入“选择设备”阶段时，先标出输入、状态拥有者和可见输出，再解释枚举能力并创建设备，按 Clear、BeginScene、绘制、EndScene、Present 发布一帧。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 11.1 典型Direct3D程序流程分析 时固定其余条件，记录HRESULT、呈现参数、设备状态、COM 引用和恢复轨迹，随后注入“全屏焦点切换后忽略 D3DERR_DEVICELOST”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 11.2 对COM接口对象的一些介绍

围绕从 IDirect3D9 到设备、交换链、后备缓冲和 Present 建立帧生命周期，对 11.2 对COM接口对象的一些介绍，API 名称属于具体年代；学习重点是输入、返回值、所有权和失败出口，技术事实以微软文档核对。

11.2 对COM接口对象的一些介绍 进入“配置呈现”阶段时，先标出输入、状态拥有者和可见输出，再解释枚举能力并创建设备，按 Clear、BeginScene、绘制、EndScene、Present 发布一帧。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 11.2 对COM接口对象的一些介绍 时固定其余条件，记录HRESULT、呈现参数、设备状态、COM 引用和恢复轨迹，随后注入“全屏焦点切换后忽略 D3DERR_DEVICELOST”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 11.3 写一个DirectX程序通用框架

围绕从 IDirect3D9 到设备、交换链、后备缓冲和 Present 建立帧生命周期，对 11.3 写一个DirectX程序通用框架，把前述机制装入一个可运行场景：固定构建、资源和输入，仅改变一个条件，并保留HRESULT、呈现参数、设备状态、COM 引用和恢复轨迹。

11.3 写一个DirectX程序通用框架 进入“提交场景”阶段时，先标出输入、状态拥有者和可见输出，再解释枚举能力并创建设备，按 Clear、BeginScene、绘制、EndScene、Present 发布一帧。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 11.3 写一个DirectX程序通用框架 时固定其余条件，记录HRESULT、呈现参数、设备状态、COM 引用和恢复轨迹，随后注入“全屏焦点切换后忽略 D3DERR_DEVICELOST”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 11.4 化腐朽为神奇——Direct3D初始化四步曲

围绕从 IDirect3D9 到设备、交换链、后备缓冲和 Present 建立帧生命周期，对 11.4 化腐朽为神奇——Direct3D初始化四步曲，API 名称属于具体年代；学习重点是输入、返回值、所有权和失败出口，技术事实以微软文档核对。

11.4 化腐朽为神奇——Direct3D初始化四步曲 进入“检测丢失”阶段时，先标出输入、状态拥有者和可见输出，再解释枚举能力并创建设备，按 Clear、BeginScene、绘制、EndScene、Present 发布一帧。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 11.4 化腐朽为神奇——Direct3D初始化四步曲 时固定其余条件，记录HRESULT、呈现参数、设备状态、COM 引用和恢复轨迹，随后注入“全屏焦点切换后忽略 D3DERR_DEVICELOST”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 11.4.1 Direct3D初始化四步曲概述

围绕从 IDirect3D9 到设备、交换链、后备缓冲和 Present 建立帧生命周期，对 11.4.1 Direct3D初始化四步曲概述，API 名称属于具体年代；学习重点是输入、返回值、所有权和失败出口，技术事实以微软文档核对。

11.4.1 Direct3D初始化四步曲概述 进入“创建 D3D”阶段时，先标出输入、状态拥有者和可见输出，再解释枚举能力并创建设备，按 Clear、BeginScene、绘制、EndScene、Present 发布一帧。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 11.4.1 Direct3D初始化四步曲概述 时固定其余条件，记录HRESULT、呈现参数、设备状态、COM 引用和恢复轨迹，随后注入“全屏焦点切换后忽略 D3DERR_DEVICELOST”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 11.4.2 Direct3D初始化四步曲之一：创接口

围绕从 IDirect3D9 到设备、交换链、后备缓冲和 Present 建立帧生命周期，对 11.4.2 Direct3D初始化四步曲之一：创接口，API 名称属于具体年代；学习重点是输入、返回值、所有权和失败出口，技术事实以微软文档核对。

11.4.2 Direct3D初始化四步曲之一：创接口 进入“选择设备”阶段时，先标出输入、状态拥有者和可见输出，再解释枚举能力并创建设备，按 Clear、BeginScene、绘制、EndScene、Present 发布一帧。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 11.4.2 Direct3D初始化四步曲之一：创接口 时固定其余条件，记录HRESULT、呈现参数、设备状态、COM 引用和恢复轨迹，随后注入“全屏焦点切换后忽略 D3DERR_DEVICELOST”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 11.4.3 Direct3D初始化四步曲之二：取信息

围绕从 IDirect3D9 到设备、交换链、后备缓冲和 Present 建立帧生命周期，对 11.4.3 Direct3D初始化四步曲之二：取信息，API 名称属于具体年代；学习重点是输入、返回值、所有权和失败出口，技术事实以微软文档核对。

11.4.3 Direct3D初始化四步曲之二：取信息 进入“配置呈现”阶段时，先标出输入、状态拥有者和可见输出，再解释枚举能力并创建设备，按 Clear、BeginScene、绘制、EndScene、Present 发布一帧。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 11.4.3 Direct3D初始化四步曲之二：取信息 时固定其余条件，记录HRESULT、呈现参数、设备状态、COM 引用和恢复轨迹，随后注入“全屏焦点切换后忽略 D3DERR_DEVICELOST”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 11.4.4 Direct3D初始化四步曲之三：填内容

围绕从 IDirect3D9 到设备、交换链、后备缓冲和 Present 建立帧生命周期，对 11.4.4 Direct3D初始化四步曲之三：填内容，API 名称属于具体年代；学习重点是输入、返回值、所有权和失败出口，技术事实以微软文档核对。

11.4.4 Direct3D初始化四步曲之三：填内容 进入“提交场景”阶段时，先标出输入、状态拥有者和可见输出，再解释枚举能力并创建设备，按 Clear、BeginScene、绘制、EndScene、Present 发布一帧。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 11.4.4 Direct3D初始化四步曲之三：填内容 时固定其余条件，记录HRESULT、呈现参数、设备状态、COM 引用和恢复轨迹，随后注入“全屏焦点切换后忽略 D3DERR_DEVICELOST”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 11.4.5 Direct3D初始化四步曲之四：创设备

围绕从 IDirect3D9 到设备、交换链、后备缓冲和 Present 建立帧生命周期，对 11.4.5 Direct3D初始化四步曲之四：创设备，API 名称属于具体年代；学习重点是输入、返回值、所有权和失败出口，技术事实以微软文档核对。

11.4.5 Direct3D初始化四步曲之四：创设备 进入“检测丢失”阶段时，先标出输入、状态拥有者和可见输出，再解释枚举能力并创建设备，按 Clear、BeginScene、绘制、EndScene、Present 发布一帧。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 11.4.5 Direct3D初始化四步曲之四：创设备 时固定其余条件，记录HRESULT、呈现参数、设备状态、COM 引用和恢复轨迹，随后注入“全屏焦点切换后忽略 D3DERR_DEVICELOST”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 11.4.6 Direct3D初始化四步曲代码赏析

围绕从 IDirect3D9 到设备、交换链、后备缓冲和 Present 建立帧生命周期，对 11.4.6 Direct3D初始化四步曲代码赏析，API 名称属于具体年代；学习重点是输入、返回值、所有权和失败出口，技术事实以微软文档核对。

11.4.6 Direct3D初始化四步曲代码赏析 进入“创建 D3D”阶段时，先标出输入、状态拥有者和可见输出，再解释枚举能力并创建设备，按 Clear、BeginScene、绘制、EndScene、Present 发布一帧。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 11.4.6 Direct3D初始化四步曲代码赏析 时固定其余条件，记录HRESULT、呈现参数、设备状态、COM 引用和恢复轨迹，随后注入“全屏焦点切换后忽略 D3DERR_DEVICELOST”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 11.4.7 示例程序D3Ddemo1

围绕从 IDirect3D9 到设备、交换链、后备缓冲和 Present 建立帧生命周期，对 11.4.7 示例程序D3Ddemo1，把前述机制装入一个可运行场景：固定构建、资源和输入，仅改变一个条件，并保留HRESULT、呈现参数、设备状态、COM 引用和恢复轨迹。

11.4.7 示例程序D3Ddemo1 进入“选择设备”阶段时，先标出输入、状态拥有者和可见输出，再解释枚举能力并创建设备，按 Clear、BeginScene、绘制、EndScene、Present 发布一帧。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 11.4.7 示例程序D3Ddemo1 时固定其余条件，记录HRESULT、呈现参数、设备状态、COM 引用和恢复轨迹，随后注入“全屏焦点切换后忽略 D3DERR_DEVICELOST”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 11.5 深入理解Direct3D动画显示技术——交换链

围绕从 IDirect3D9 到设备、交换链、后备缓冲和 Present 建立帧生命周期，对 11.5 深入理解Direct3D动画显示技术——交换链，API 名称属于具体年代；学习重点是输入、返回值、所有权和失败出口，技术事实以微软文档核对。

11.5 深入理解Direct3D动画显示技术——交换链 进入“配置呈现”阶段时，先标出输入、状态拥有者和可见输出，再解释枚举能力并创建设备，按 Clear、BeginScene、绘制、EndScene、Present 发布一帧。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 11.5 深入理解Direct3D动画显示技术——交换链 时固定其余条件，记录HRESULT、呈现参数、设备状态、COM 引用和恢复轨迹，随后注入“全屏焦点切换后忽略 D3DERR_DEVICELOST”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 11.6 对固定功能渲染流水线体系的理解

围绕从 IDirect3D9 到设备、交换链、后备缓冲和 Present 建立帧生命周期，11.6 对固定功能渲染流水线体系的理解 限定本章的一个知识坐标；独立解释围绕“枚举能力并创建设备，按 Clear、BeginScene、绘制、EndScene、Present 发布一帧”展开，并以“设备状态、场景配对和资源释放在成功与失败路径都完整”结束。

11.6 对固定功能渲染流水线体系的理解 进入“提交场景”阶段时，先标出输入、状态拥有者和可见输出，再解释枚举能力并创建设备，按 Clear、BeginScene、绘制、EndScene、Present 发布一帧。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 11.6 对固定功能渲染流水线体系的理解 时固定其余条件，记录HRESULT、呈现参数、设备状态、COM 引用和恢复轨迹，随后注入“全屏焦点切换后忽略 D3DERR_DEVICELOST”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 11.7 Direct3D中的“绘制金钥匙”—— Direct3D设备接口

围绕从 IDirect3D9 到设备、交换链、后备缓冲和 Present 建立帧生命周期，对 11.7 Direct3D中的“绘制金钥匙”—— Direct3D设备接口，API 名称属于具体年代；学习重点是输入、返回值、所有权和失败出口，技术事实以微软文档核对。

11.7 Direct3D中的“绘制金钥匙”—— Direct3D设备接口 进入“检测丢失”阶段时，先标出输入、状态拥有者和可见输出，再解释枚举能力并创建设备，按 Clear、BeginScene、绘制、EndScene、Present 发布一帧。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 11.7 Direct3D中的“绘制金钥匙”—— Direct3D设备接口 时固定其余条件，记录HRESULT、呈现参数、设备状态、COM 引用和恢复轨迹，随后注入“全屏焦点切换后忽略 D3DERR_DEVICELOST”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 11.8 Direct3D中二维文本的绘制

围绕从 IDirect3D9 到设备、交换链、后备缓冲和 Present 建立帧生命周期，对 11.8 Direct3D中二维文本的绘制，API 名称属于具体年代；学习重点是输入、返回值、所有权和失败出口，技术事实以微软文档核对。

11.8 Direct3D中二维文本的绘制 进入“创建 D3D”阶段时，先标出输入、状态拥有者和可见输出，再解释枚举能力并创建设备，按 Clear、BeginScene、绘制、EndScene、Present 发布一帧。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 11.8 Direct3D中二维文本的绘制 时固定其余条件，记录HRESULT、呈现参数、设备状态、COM 引用和恢复轨迹，随后注入“全屏焦点切换后忽略 D3DERR_DEVICELOST”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 11.8.1 D3DXCreateFont函数

围绕从 IDirect3D9 到设备、交换链、后备缓冲和 Present 建立帧生命周期，对 11.8.1 D3DXCreateFont函数，API 名称属于具体年代；学习重点是输入、返回值、所有权和失败出口，技术事实以微软文档核对。

11.8.1 D3DXCreateFont函数 进入“选择设备”阶段时，先标出输入、状态拥有者和可见输出，再解释枚举能力并创建设备，按 Clear、BeginScene、绘制、EndScene、Present 发布一帧。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 11.8.1 D3DXCreateFont函数 时固定其余条件，记录HRESULT、呈现参数、设备状态、COM 引用和恢复轨迹，随后注入“全屏焦点切换后忽略 D3DERR_DEVICELOST”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 11.8.2 DrawText函数

围绕从 IDirect3D9 到设备、交换链、后备缓冲和 Present 建立帧生命周期，对 11.8.2 DrawText函数，API 名称属于具体年代；学习重点是输入、返回值、所有权和失败出口，技术事实以微软文档核对。

11.8.2 DrawText函数 进入“配置呈现”阶段时，先标出输入、状态拥有者和可见输出，再解释枚举能力并创建设备，按 Clear、BeginScene、绘制、EndScene、Present 发布一帧。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 11.8.2 DrawText函数 时固定其余条件，记录HRESULT、呈现参数、设备状态、COM 引用和恢复轨迹，随后注入“全屏焦点切换后忽略 D3DERR_DEVICELOST”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 11.9 起承转合的艺术：Direct3D渲染五步曲

围绕从 IDirect3D9 到设备、交换链、后备缓冲和 Present 建立帧生命周期，对 11.9 起承转合的艺术：Direct3D渲染五步曲，API 名称属于具体年代；学习重点是输入、返回值、所有权和失败出口，技术事实以微软文档核对。

11.9 起承转合的艺术：Direct3D渲染五步曲 进入“提交场景”阶段时，先标出输入、状态拥有者和可见输出，再解释枚举能力并创建设备，按 Clear、BeginScene、绘制、EndScene、Present 发布一帧。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 11.9 起承转合的艺术：Direct3D渲染五步曲 时固定其余条件，记录HRESULT、呈现参数、设备状态、COM 引用和恢复轨迹，随后注入“全屏焦点切换后忽略 D3DERR_DEVICELOST”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 11.9.1 Direct3D渲染五步曲概述

围绕从 IDirect3D9 到设备、交换链、后备缓冲和 Present 建立帧生命周期，对 11.9.1 Direct3D渲染五步曲概述，API 名称属于具体年代；学习重点是输入、返回值、所有权和失败出口，技术事实以微软文档核对。

11.9.1 Direct3D渲染五步曲概述 进入“检测丢失”阶段时，先标出输入、状态拥有者和可见输出，再解释枚举能力并创建设备，按 Clear、BeginScene、绘制、EndScene、Present 发布一帧。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 11.9.1 Direct3D渲染五步曲概述 时固定其余条件，记录HRESULT、呈现参数、设备状态、COM 引用和恢复轨迹，随后注入“全屏焦点切换后忽略 D3DERR_DEVICELOST”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 11.9.2 五步曲之一：清屏操作

围绕从 IDirect3D9 到设备、交换链、后备缓冲和 Present 建立帧生命周期，11.9.2 五步曲之一：清屏操作 限定本章的一个知识坐标；独立解释围绕“枚举能力并创建设备，按 Clear、BeginScene、绘制、EndScene、Present 发布一帧”展开，并以“设备状态、场景配对和资源释放在成功与失败路径都完整”结束。

11.9.2 五步曲之一：清屏操作 进入“创建 D3D”阶段时，先标出输入、状态拥有者和可见输出，再解释枚举能力并创建设备，按 Clear、BeginScene、绘制、EndScene、Present 发布一帧。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 11.9.2 五步曲之一：清屏操作 时固定其余条件，记录HRESULT、呈现参数、设备状态、COM 引用和恢复轨迹，随后注入“全屏焦点切换后忽略 D3DERR_DEVICELOST”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 11.9.3 五步曲之二：开始绘制

围绕从 IDirect3D9 到设备、交换链、后备缓冲和 Present 建立帧生命周期，11.9.3 五步曲之二：开始绘制 限定本章的一个知识坐标；独立解释围绕“枚举能力并创建设备，按 Clear、BeginScene、绘制、EndScene、Present 发布一帧”展开，并以“设备状态、场景配对和资源释放在成功与失败路径都完整”结束。

11.9.3 五步曲之二：开始绘制 进入“选择设备”阶段时，先标出输入、状态拥有者和可见输出，再解释枚举能力并创建设备，按 Clear、BeginScene、绘制、EndScene、Present 发布一帧。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 11.9.3 五步曲之二：开始绘制 时固定其余条件，记录HRESULT、呈现参数、设备状态、COM 引用和恢复轨迹，随后注入“全屏焦点切换后忽略 D3DERR_DEVICELOST”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 11.9.4 五步曲之三：正式绘制

围绕从 IDirect3D9 到设备、交换链、后备缓冲和 Present 建立帧生命周期，11.9.4 五步曲之三：正式绘制 限定本章的一个知识坐标；独立解释围绕“枚举能力并创建设备，按 Clear、BeginScene、绘制、EndScene、Present 发布一帧”展开，并以“设备状态、场景配对和资源释放在成功与失败路径都完整”结束。

11.9.4 五步曲之三：正式绘制 进入“配置呈现”阶段时，先标出输入、状态拥有者和可见输出，再解释枚举能力并创建设备，按 Clear、BeginScene、绘制、EndScene、Present 发布一帧。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 11.9.4 五步曲之三：正式绘制 时固定其余条件，记录HRESULT、呈现参数、设备状态、COM 引用和恢复轨迹，随后注入“全屏焦点切换后忽略 D3DERR_DEVICELOST”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 11.9.5 五步曲之四：结束绘制

围绕从 IDirect3D9 到设备、交换链、后备缓冲和 Present 建立帧生命周期，11.9.5 五步曲之四：结束绘制 限定本章的一个知识坐标；独立解释围绕“枚举能力并创建设备，按 Clear、BeginScene、绘制、EndScene、Present 发布一帧”展开，并以“设备状态、场景配对和资源释放在成功与失败路径都完整”结束。

11.9.5 五步曲之四：结束绘制 进入“提交场景”阶段时，先标出输入、状态拥有者和可见输出，再解释枚举能力并创建设备，按 Clear、BeginScene、绘制、EndScene、Present 发布一帧。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 11.9.5 五步曲之四：结束绘制 时固定其余条件，记录HRESULT、呈现参数、设备状态、COM 引用和恢复轨迹，随后注入“全屏焦点切换后忽略 D3DERR_DEVICELOST”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 11.9.6 五步曲之五：翻转显示

围绕从 IDirect3D9 到设备、交换链、后备缓冲和 Present 建立帧生命周期，11.9.6 五步曲之五：翻转显示 限定本章的一个知识坐标；独立解释围绕“枚举能力并创建设备，按 Clear、BeginScene、绘制、EndScene、Present 发布一帧”展开，并以“设备状态、场景配对和资源释放在成功与失败路径都完整”结束。

11.9.6 五步曲之五：翻转显示 进入“检测丢失”阶段时，先标出输入、状态拥有者和可见输出，再解释枚举能力并创建设备，按 Clear、BeginScene、绘制、EndScene、Present 发布一帧。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 11.9.6 五步曲之五：翻转显示 时固定其余条件，记录HRESULT、呈现参数、设备状态、COM 引用和恢复轨迹，随后注入“全屏焦点切换后忽略 D3DERR_DEVICELOST”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 11.9.7 Direct3D渲染五步曲代码整体赏析

围绕从 IDirect3D9 到设备、交换链、后备缓冲和 Present 建立帧生命周期，对 11.9.7 Direct3D渲染五步曲代码整体赏析，API 名称属于具体年代；学习重点是输入、返回值、所有权和失败出口，技术事实以微软文档核对。

11.9.7 Direct3D渲染五步曲代码整体赏析 进入“创建 D3D”阶段时，先标出输入、状态拥有者和可见输出，再解释枚举能力并创建设备，按 Clear、BeginScene、绘制、EndScene、Present 发布一帧。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 11.9.7 Direct3D渲染五步曲代码整体赏析 时固定其余条件，记录HRESULT、呈现参数、设备状态、COM 引用和恢复轨迹，随后注入“全屏焦点切换后忽略 D3DERR_DEVICELOST”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 11.9.8 示例程序D3Ddemo2

围绕从 IDirect3D9 到设备、交换链、后备缓冲和 Present 建立帧生命周期，对 11.9.8 示例程序D3Ddemo2，把前述机制装入一个可运行场景：固定构建、资源和输入，仅改变一个条件，并保留HRESULT、呈现参数、设备状态、COM 引用和恢复轨迹。

11.9.8 示例程序D3Ddemo2 进入“选择设备”阶段时，先标出输入、状态拥有者和可见输出，再解释枚举能力并创建设备，按 Clear、BeginScene、绘制、EndScene、Present 发布一帧。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 11.9.8 示例程序D3Ddemo2 时固定其余条件，记录HRESULT、呈现参数、设备状态、COM 引用和恢复轨迹，随后注入“全屏焦点切换后忽略 D3DERR_DEVICELOST”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 11.10 章节小憩

围绕从 IDirect3D9 到设备、交换链、后备缓冲和 Present 建立帧生命周期，对 11.10 章节小憩，收尾不是装饰，而是要求用HRESULT、呈现参数、设备状态、COM 引用和恢复轨迹复盘“设备状态、场景配对和资源释放在成功与失败路径都完整”是否在正常和失败路径同时成立。

11.10 章节小憩 进入“配置呈现”阶段时，先标出输入、状态拥有者和可见输出，再解释枚举能力并创建设备，按 Clear、BeginScene、绘制、EndScene、Present 发布一帧。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 11.10 章节小憩 时固定其余条件，记录HRESULT、呈现参数、设备状态、COM 引用和恢复轨迹，随后注入“全屏焦点切换后忽略 D3DERR_DEVICELOST”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

## 可重放实现或计算骨架

```cpp
struct JourneyProbe {
  const char* chapter = "第11章 三维内功心法——Direct3D编程基础";
  unsigned generation = 0;
  bool resourcesReady = false;
  bool resetVerified = false;
};

// 每个 HRESULT/句柄/COM 引用都进入证据日志；失败时逆序回滚。
```

真实程序还要固定编译器、SDK、窗口尺寸、资源、随机种子、时间步和输入记录；否则历史实现与现代迁移不能公平比较。


## 练习与 4 级证据矩阵


## 术语复核与本章回顾


掌握“第11章 三维内功心法——Direct3D编程基础”意味着能解释枚举能力并创建设备，按 Clear、BeginScene、绘制、EndScene、Present 发布一帧，区分历史坐标与现代迁移，并用HRESULT、呈现参数、设备状态、COM 引用和恢复轨迹推翻或保留实现。

## 阅读导航

[← 上一页：第10章 快到碗里来——DirectX大局观认知](/learn/windows-journey/04-directx-foundation/wj-10-directx-overview) · [下一页：第12章 腾飞前的助跑——Direct3D 绘制基础 →](/learn/windows-journey/04-directx-foundation/wj-12-direct3d-drawing)

