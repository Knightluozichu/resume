---
title: 第12章 腾飞前的助跑——Direct3D 绘制基础
type: C
section: 第三篇 · DirectX游戏编程基础
order: 12
description: 第12章 腾飞前的助跑——Direct3D 绘制基础：定义顶点格式、缓冲区、图元拓扑和绘制调用的数据合同，以历史—现代对照、故障恢复和逐节点证据验收。
demo: true
math: false
sourceUrl: 'https://book.douban.com/subject/25756435/'
draft: true
qualityVersion: 2
practiceMode: code
sourceMode: independent-rewrite
---
import {
  Wj12Direct3dDrawingMapLab,
  Wj12Direct3dDrawingExperimentLab,
  Wj12Direct3dDrawingEvidenceLab,
} from "@/components/mdx/windows-journey/diagrams/wj-12-direct3d-drawing";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

# 第12章 腾飞前的助跑——Direct3D 绘制基础

<Objectives>

- 能从“屏幕没有图形时，根因可能在顶点布局、缓冲锁定、图元数或变换状态”建立本章的历史问题基线。
- 能解释“让顶点声明与内存布局一致，写入缓冲并在正确流和拓扑上绘制”的输入、状态、所有权和可见结果。
- 能操作专属实验，对照D3D9 FVF 绘制与显式顶点布局，复算错误图元。
- 能注入“FVF 声明的步长与 C++ 顶点结构不匹配”，用顶点字节、流步长、图元计数、绘制返回值和帧捕获定位首错、恢复并重放。

</Objectives>

{/* WINDOWS_JOURNEY_QUALITY_V2 */}

## 问题先于 API

屏幕没有图形时，根因可能在顶点布局、缓冲锁定、图元数或变换状态。本章先保存问题、初态和输入，再讨论 API；通过条件是“顶点字节、FVF/声明、步长和图元数量彼此一致”，不是画面偶然出现一次。

## 来源范围、技术年代与独立重写

公开[完整目录](https://book.douban.com/subject/25756435/)只用于界定本页题目和层级，[出版记录](https://www.sinobook.com.cn/b2c/scrp/bookdetail.cfm?iBookNo=646210)与[书目记录](https://books.google.com/books/about/%E9%80%90%E6%A2%A6%E6%97%85%E7%A8%8B.html?id=6O-F0AEACAAJ)用于交叉核对 ISBN、出版社和年代；没有把目录核对表述成正文忠实。本页关于 Windows/DirectX 的机制由[Microsoft 一手文档](https://learn.microsoft.com/en-us/windows/win32/direct3d9/direct3d-architecture)核验，以下中文讲解、图示、实验、代码和练习均为独立教学重写。

原书处于 Visual Studio 2010、Win32/GDI、DirectX 9 与固定功能管线语境。现代 API 只作迁移对照：D3D9 FVF 绘制和显式顶点布局名称可以不同，但必须消费相同输入并守住同一不变量。

## 机制与术语

<Term def="顶点缓冲在“第12章 腾飞前的助跑——Direct3D 绘制基础”中标记输入进入系统时的责任边界。">顶点缓冲</Term>、<Term def="索引缓冲在“第12章 腾飞前的助跑——Direct3D 绘制基础”中标记状态被创建、修改与释放的生命周期。">索引缓冲</Term>、<Term def="FVF在“第12章 腾飞前的助跑——Direct3D 绘制基础”中标记历史实现与现代迁移之间必须保持的机制。">FVF</Term>、<Term def="图元拓扑在“第12章 腾飞前的助跑——Direct3D 绘制基础”中标记能够复算本章结论的原始证据。">图元拓扑</Term>。四个术语共同约束“定义顶点格式、缓冲区、图元拓扑和绘制调用的数据合同”，结论必须回到顶点字节、流步长、图元计数、绘制返回值和帧捕获。

## 先预测，再操作三层专属实验

操作前写下预测：哪一阶段先改变错误图元；压力扩大四倍时哪条边界先失效；注入“FVF 声明的步长与 C++ 顶点结构不匹配”后怎样回到初态。

<Stepper>
  <Step title="1. 正式节点与状态链">
    选择目录节点和机制阶段，检查输入、状态拥有者、可见结果与验证证据。

    <Wj12Direct3dDrawingMapLab />

  </Step>
  <Step title="2. 历史实现与现代迁移">
    一次只改变技术坐标、场景负载或故障之一，读取可复算结果。

    <Wj12Direct3dDrawingExperimentLab />

  </Step>
  <Step title="3. 故障、恢复与同输入重放">
    比较正常、故障和恢复轨迹；最后点击重置，确认所有控制和状态回到初值。

    <Wj12Direct3dDrawingEvidenceLab />

  </Step>
</Stepper>

## 正式目录逐项深读

### 第12章 腾飞前的助跑——Direct3D 绘制基础

围绕定义顶点格式、缓冲区、图元拓扑和绘制调用的数据合同，对 第12章 腾飞前的助跑——Direct3D 绘制基础，API 名称属于具体年代；学习重点是输入、返回值、所有权和失败出口，技术事实以微软文档核对。

第12章 腾飞前的助跑——Direct3D 绘制基础 进入“定义顶点”阶段时，先标出输入、状态拥有者和可见输出，再解释让顶点声明与内存布局一致，写入缓冲并在正确流和拓扑上绘制。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 第12章 腾飞前的助跑——Direct3D 绘制基础 时固定其余条件，记录顶点字节、流步长、图元计数、绘制返回值和帧捕获，随后注入“FVF 声明的步长与 C++ 顶点结构不匹配”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 12.1 顶点缓存的逆袭

围绕定义顶点格式、缓冲区、图元拓扑和绘制调用的数据合同，12.1 顶点缓存的逆袭 限定本章的一个知识坐标；独立解释围绕“让顶点声明与内存布局一致，写入缓冲并在正确流和拓扑上绘制”展开，并以“顶点字节、FVF/声明、步长和图元数量彼此一致”结束。

12.1 顶点缓存的逆袭 进入“创建缓冲”阶段时，先标出输入、状态拥有者和可见输出，再解释让顶点声明与内存布局一致，写入缓冲并在正确流和拓扑上绘制。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 12.1 顶点缓存的逆袭 时固定其余条件，记录顶点字节、流步长、图元计数、绘制返回值和帧捕获，随后注入“FVF 声明的步长与 C++ 顶点结构不匹配”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 12.1.1 引言

围绕定义顶点格式、缓冲区、图元拓扑和绘制调用的数据合同，12.1.1 引言 限定本章的一个知识坐标；独立解释围绕“让顶点声明与内存布局一致，写入缓冲并在正确流和拓扑上绘制”展开，并以“顶点字节、FVF/声明、步长和图元数量彼此一致”结束。

12.1.1 引言 进入“锁定写入”阶段时，先标出输入、状态拥有者和可见输出，再解释让顶点声明与内存布局一致，写入缓冲并在正确流和拓扑上绘制。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 12.1.1 引言 时固定其余条件，记录顶点字节、流步长、图元计数、绘制返回值和帧捕获，随后注入“FVF 声明的步长与 C++ 顶点结构不匹配”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 12.1.2 顶点缓存相关基础知识

围绕定义顶点格式、缓冲区、图元拓扑和绘制调用的数据合同，12.1.2 顶点缓存相关基础知识 限定本章的一个知识坐标；独立解释围绕“让顶点声明与内存布局一致，写入缓冲并在正确流和拓扑上绘制”展开，并以“顶点字节、FVF/声明、步长和图元数量彼此一致”结束。

12.1.2 顶点缓存相关基础知识 进入“绑定数据”阶段时，先标出输入、状态拥有者和可见输出，再解释让顶点声明与内存布局一致，写入缓冲并在正确流和拓扑上绘制。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 12.1.2 顶点缓存相关基础知识 时固定其余条件，记录顶点字节、流步长、图元计数、绘制返回值和帧捕获，随后注入“FVF 声明的步长与 C++ 顶点结构不匹配”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 12.1.3 顶点缓存使用四步曲之一：设计顶点缓存

围绕定义顶点格式、缓冲区、图元拓扑和绘制调用的数据合同，12.1.3 顶点缓存使用四步曲之一：设计顶点缓存 限定本章的一个知识坐标；独立解释围绕“让顶点声明与内存布局一致，写入缓冲并在正确流和拓扑上绘制”展开，并以“顶点字节、FVF/声明、步长和图元数量彼此一致”结束。

12.1.3 顶点缓存使用四步曲之一：设计顶点缓存 进入“提交图元”阶段时，先标出输入、状态拥有者和可见输出，再解释让顶点声明与内存布局一致，写入缓冲并在正确流和拓扑上绘制。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 12.1.3 顶点缓存使用四步曲之一：设计顶点缓存 时固定其余条件，记录顶点字节、流步长、图元计数、绘制返回值和帧捕获，随后注入“FVF 声明的步长与 C++ 顶点结构不匹配”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 12.1.4 顶点缓存使用四步曲之二：创建顶点缓存

围绕定义顶点格式、缓冲区、图元拓扑和绘制调用的数据合同，对 12.1.4 顶点缓存使用四步曲之二：创建顶点缓存，重点检查资源生命周期：创建成功只完成一半，失败回滚、逆序释放和同输入重建同样属于通过条件。

12.1.4 顶点缓存使用四步曲之二：创建顶点缓存 进入“定义顶点”阶段时，先标出输入、状态拥有者和可见输出，再解释让顶点声明与内存布局一致，写入缓冲并在正确流和拓扑上绘制。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 12.1.4 顶点缓存使用四步曲之二：创建顶点缓存 时固定其余条件，记录顶点字节、流步长、图元计数、绘制返回值和帧捕获，随后注入“FVF 声明的步长与 C++ 顶点结构不匹配”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 12.1.5 顶点缓存使用四步曲之三：访问顶点缓存

围绕定义顶点格式、缓冲区、图元拓扑和绘制调用的数据合同，12.1.5 顶点缓存使用四步曲之三：访问顶点缓存 限定本章的一个知识坐标；独立解释围绕“让顶点声明与内存布局一致，写入缓冲并在正确流和拓扑上绘制”展开，并以“顶点字节、FVF/声明、步长和图元数量彼此一致”结束。

12.1.5 顶点缓存使用四步曲之三：访问顶点缓存 进入“创建缓冲”阶段时，先标出输入、状态拥有者和可见输出，再解释让顶点声明与内存布局一致，写入缓冲并在正确流和拓扑上绘制。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 12.1.5 顶点缓存使用四步曲之三：访问顶点缓存 时固定其余条件，记录顶点字节、流步长、图元计数、绘制返回值和帧捕获，随后注入“FVF 声明的步长与 C++ 顶点结构不匹配”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 12.1.6 顶点缓存使用四步曲之四：图形的绘制

围绕定义顶点格式、缓冲区、图元拓扑和绘制调用的数据合同，12.1.6 顶点缓存使用四步曲之四：图形的绘制 限定本章的一个知识坐标；独立解释围绕“让顶点声明与内存布局一致，写入缓冲并在正确流和拓扑上绘制”展开，并以“顶点字节、FVF/声明、步长和图元数量彼此一致”结束。

12.1.6 顶点缓存使用四步曲之四：图形的绘制 进入“锁定写入”阶段时，先标出输入、状态拥有者和可见输出，再解释让顶点声明与内存布局一致，写入缓冲并在正确流和拓扑上绘制。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 12.1.6 顶点缓存使用四步曲之四：图形的绘制 时固定其余条件，记录顶点字节、流步长、图元计数、绘制返回值和帧捕获，随后注入“FVF 声明的步长与 C++ 顶点结构不匹配”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 12.1.7 示例程序D3Ddemo3

围绕定义顶点格式、缓冲区、图元拓扑和绘制调用的数据合同，对 12.1.7 示例程序D3Ddemo3，把前述机制装入一个可运行场景：固定构建、资源和输入，仅改变一个条件，并保留顶点字节、流步长、图元计数、绘制返回值和帧捕获。

12.1.7 示例程序D3Ddemo3 进入“绑定数据”阶段时，先标出输入、状态拥有者和可见输出，再解释让顶点声明与内存布局一致，写入缓冲并在正确流和拓扑上绘制。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 12.1.7 示例程序D3Ddemo3 时固定其余条件，记录顶点字节、流步长、图元计数、绘制返回值和帧捕获，随后注入“FVF 声明的步长与 C++ 顶点结构不匹配”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 12.2 索引缓存——顶点缓存的红颜知己

围绕定义顶点格式、缓冲区、图元拓扑和绘制调用的数据合同，12.2 索引缓存——顶点缓存的红颜知己 限定本章的一个知识坐标；独立解释围绕“让顶点声明与内存布局一致，写入缓冲并在正确流和拓扑上绘制”展开，并以“顶点字节、FVF/声明、步长和图元数量彼此一致”结束。

12.2 索引缓存——顶点缓存的红颜知己 进入“提交图元”阶段时，先标出输入、状态拥有者和可见输出，再解释让顶点声明与内存布局一致，写入缓冲并在正确流和拓扑上绘制。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 12.2 索引缓存——顶点缓存的红颜知己 时固定其余条件，记录顶点字节、流步长、图元计数、绘制返回值和帧捕获，随后注入“FVF 声明的步长与 C++ 顶点结构不匹配”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 12.2.1 引言

围绕定义顶点格式、缓冲区、图元拓扑和绘制调用的数据合同，12.2.1 引言 限定本章的一个知识坐标；独立解释围绕“让顶点声明与内存布局一致，写入缓冲并在正确流和拓扑上绘制”展开，并以“顶点字节、FVF/声明、步长和图元数量彼此一致”结束。

12.2.1 引言 进入“定义顶点”阶段时，先标出输入、状态拥有者和可见输出，再解释让顶点声明与内存布局一致，写入缓冲并在正确流和拓扑上绘制。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 12.2.1 引言 时固定其余条件，记录顶点字节、流步长、图元计数、绘制返回值和帧捕获，随后注入“FVF 声明的步长与 C++ 顶点结构不匹配”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 12.2.2 索引缓存的使用思路

围绕定义顶点格式、缓冲区、图元拓扑和绘制调用的数据合同，12.2.2 索引缓存的使用思路 限定本章的一个知识坐标；独立解释围绕“让顶点声明与内存布局一致，写入缓冲并在正确流和拓扑上绘制”展开，并以“顶点字节、FVF/声明、步长和图元数量彼此一致”结束。

12.2.2 索引缓存的使用思路 进入“创建缓冲”阶段时，先标出输入、状态拥有者和可见输出，再解释让顶点声明与内存布局一致，写入缓冲并在正确流和拓扑上绘制。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 12.2.2 索引缓存的使用思路 时固定其余条件，记录顶点字节、流步长、图元计数、绘制返回值和帧捕获，随后注入“FVF 声明的步长与 C++ 顶点结构不匹配”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 12.2.3 相濡以沫的顶点缓存与索引缓存

围绕定义顶点格式、缓冲区、图元拓扑和绘制调用的数据合同，12.2.3 相濡以沫的顶点缓存与索引缓存 限定本章的一个知识坐标；独立解释围绕“让顶点声明与内存布局一致，写入缓冲并在正确流和拓扑上绘制”展开，并以“顶点字节、FVF/声明、步长和图元数量彼此一致”结束。

12.2.3 相濡以沫的顶点缓存与索引缓存 进入“锁定写入”阶段时，先标出输入、状态拥有者和可见输出，再解释让顶点声明与内存布局一致，写入缓冲并在正确流和拓扑上绘制。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 12.2.3 相濡以沫的顶点缓存与索引缓存 时固定其余条件，记录顶点字节、流步长、图元计数、绘制返回值和帧捕获，随后注入“FVF 声明的步长与 C++ 顶点结构不匹配”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 12.2.4 双剑合璧：顶点缓存、索引缓存使用四步曲

围绕定义顶点格式、缓冲区、图元拓扑和绘制调用的数据合同，12.2.4 双剑合璧：顶点缓存、索引缓存使用四步曲 限定本章的一个知识坐标；独立解释围绕“让顶点声明与内存布局一致，写入缓冲并在正确流和拓扑上绘制”展开，并以“顶点字节、FVF/声明、步长和图元数量彼此一致”结束。

12.2.4 双剑合璧：顶点缓存、索引缓存使用四步曲 进入“绑定数据”阶段时，先标出输入、状态拥有者和可见输出，再解释让顶点声明与内存布局一致，写入缓冲并在正确流和拓扑上绘制。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 12.2.4 双剑合璧：顶点缓存、索引缓存使用四步曲 时固定其余条件，记录顶点字节、流步长、图元计数、绘制返回值和帧捕获，随后注入“FVF 声明的步长与 C++ 顶点结构不匹配”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 12.2.5 示例程序D3Ddemo4

围绕定义顶点格式、缓冲区、图元拓扑和绘制调用的数据合同，对 12.2.5 示例程序D3Ddemo4，把前述机制装入一个可运行场景：固定构建、资源和输入，仅改变一个条件，并保留顶点字节、流步长、图元计数、绘制返回值和帧捕获。

12.2.5 示例程序D3Ddemo4 进入“提交图元”阶段时，先标出输入、状态拥有者和可见输出，再解释让顶点声明与内存布局一致，写入缓冲并在正确流和拓扑上绘制。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 12.2.5 示例程序D3Ddemo4 时固定其余条件，记录顶点字节、流步长、图元计数、绘制返回值和帧捕获，随后注入“FVF 声明的步长与 C++ 顶点结构不匹配”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 12.3 章节小憩

围绕定义顶点格式、缓冲区、图元拓扑和绘制调用的数据合同，对 12.3 章节小憩，收尾不是装饰，而是要求用顶点字节、流步长、图元计数、绘制返回值和帧捕获复盘“顶点字节、FVF/声明、步长和图元数量彼此一致”是否在正常和失败路径同时成立。

12.3 章节小憩 进入“定义顶点”阶段时，先标出输入、状态拥有者和可见输出，再解释让顶点声明与内存布局一致，写入缓冲并在正确流和拓扑上绘制。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 12.3 章节小憩 时固定其余条件，记录顶点字节、流步长、图元计数、绘制返回值和帧捕获，随后注入“FVF 声明的步长与 C++ 顶点结构不匹配”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

## 可重放实现或计算骨架

```cpp
struct JourneyProbe {
  const char* chapter = "第12章 腾飞前的助跑——Direct3D 绘制基础";
  unsigned generation = 0;
  bool resourcesReady = false;
  bool resetVerified = false;
};

// 每个 HRESULT/句柄/COM 引用都进入证据日志；失败时逆序回滚。
```

真实程序还要固定编译器、SDK、窗口尺寸、资源、随机种子、时间步和输入记录；否则历史实现与现代迁移不能公平比较。

<Callout type="trap" title="把历史 API 当成当前默认方案">
  本章保留 D3D9 FVF 绘制 的教学身份，但新项目必须核对平台支持并解释为何选择 显式顶点布局 或其他方案。
</Callout>

<Callout type="trap" title="只看最终画面">
  第12章 腾飞前的助跑——Direct3D 绘制基础必须说明定义顶点、创建缓冲、锁定写入、绑定数据、提交图元的先后关系；最终像素相同不代表消息、资源、矩阵或状态安全。
</Callout>

<Callout type="trap" title="失败后继续运行">
  注入“FVF 声明的步长与 C++ 顶点结构不匹配”后应停在第一个因果分叉；若顶点字节、流步长、图元计数、绘制返回值和帧捕获无法解释并复位，当前实现失败。
</Callout>

## 练习与 4 级证据矩阵

<Exercises>

**问题 1：历史边界。** 怎样避免把 2013 年接口示例误报成今天的默认架构？

<Answer>
  分别记录D3D9 FVF 绘制与显式顶点布局的版本、责任和资源模型；只迁移机制，不改写历史事实，并以[Microsoft 文档](https://learn.microsoft.com/en-us/windows/win32/direct3d9/direct3d-architecture)核对接口身份。
</Answer>

**问题 2：逐节点验证。** 正式目录节点怎样从“出现”升级到可复查证据？

<Answer>
  - **第12章 腾飞前的助跑——Direct3D 绘制基础**：在“定义顶点”记录顶点字节、流步长、图元计数、绘制返回值和帧捕获，注入“FVF 声明的步长与 C++ 顶点结构不匹配”后复位并重放。
  - **12.1 顶点缓存的逆袭**：在“创建缓冲”记录顶点字节、流步长、图元计数、绘制返回值和帧捕获，注入“FVF 声明的步长与 C++ 顶点结构不匹配”后复位并重放。
  - **12.1.1 引言**：在“锁定写入”记录顶点字节、流步长、图元计数、绘制返回值和帧捕获，注入“FVF 声明的步长与 C++ 顶点结构不匹配”后复位并重放。
  - **12.1.2 顶点缓存相关基础知识**：在“绑定数据”记录顶点字节、流步长、图元计数、绘制返回值和帧捕获，注入“FVF 声明的步长与 C++ 顶点结构不匹配”后复位并重放。
  - **12.1.3 顶点缓存使用四步曲之一：设计顶点缓存**：在“提交图元”记录顶点字节、流步长、图元计数、绘制返回值和帧捕获，注入“FVF 声明的步长与 C++ 顶点结构不匹配”后复位并重放。
  - **12.1.4 顶点缓存使用四步曲之二：创建顶点缓存**：在“定义顶点”记录顶点字节、流步长、图元计数、绘制返回值和帧捕获，注入“FVF 声明的步长与 C++ 顶点结构不匹配”后复位并重放。
  - **12.1.5 顶点缓存使用四步曲之三：访问顶点缓存**：在“创建缓冲”记录顶点字节、流步长、图元计数、绘制返回值和帧捕获，注入“FVF 声明的步长与 C++ 顶点结构不匹配”后复位并重放。
  - **12.1.6 顶点缓存使用四步曲之四：图形的绘制**：在“锁定写入”记录顶点字节、流步长、图元计数、绘制返回值和帧捕获，注入“FVF 声明的步长与 C++ 顶点结构不匹配”后复位并重放。
  - **12.1.7 示例程序D3Ddemo3**：在“绑定数据”记录顶点字节、流步长、图元计数、绘制返回值和帧捕获，注入“FVF 声明的步长与 C++ 顶点结构不匹配”后复位并重放。
  - **12.2 索引缓存——顶点缓存的红颜知己**：在“提交图元”记录顶点字节、流步长、图元计数、绘制返回值和帧捕获，注入“FVF 声明的步长与 C++ 顶点结构不匹配”后复位并重放。
  - **12.2.1 引言**：在“定义顶点”记录顶点字节、流步长、图元计数、绘制返回值和帧捕获，注入“FVF 声明的步长与 C++ 顶点结构不匹配”后复位并重放。
  - **12.2.2 索引缓存的使用思路**：在“创建缓冲”记录顶点字节、流步长、图元计数、绘制返回值和帧捕获，注入“FVF 声明的步长与 C++ 顶点结构不匹配”后复位并重放。
  - **12.2.3 相濡以沫的顶点缓存与索引缓存**：在“锁定写入”记录顶点字节、流步长、图元计数、绘制返回值和帧捕获，注入“FVF 声明的步长与 C++ 顶点结构不匹配”后复位并重放。
  - **12.2.4 双剑合璧：顶点缓存、索引缓存使用四步曲**：在“绑定数据”记录顶点字节、流步长、图元计数、绘制返回值和帧捕获，注入“FVF 声明的步长与 C++ 顶点结构不匹配”后复位并重放。
  - **12.2.5 示例程序D3Ddemo4**：在“提交图元”记录顶点字节、流步长、图元计数、绘制返回值和帧捕获，注入“FVF 声明的步长与 C++ 顶点结构不匹配”后复位并重放。
  - **12.3 章节小憩**：在“定义顶点”记录顶点字节、流步长、图元计数、绘制返回值和帧捕获，注入“FVF 声明的步长与 C++ 顶点结构不匹配”后复位并重放。
</Answer>

**问题 3：故障闭环。** 注入“FVF 声明的步长与 C++ 顶点结构不匹配”后怎样判定恢复成功？

<Answer>
  保存首错阶段，逆序清理或恢复必要状态，以相同初态和输入重放；只有顶点字节、流步长、图元计数、绘制返回值和帧捕获回到基线且“顶点字节、FVF/声明、步长和图元数量彼此一致”再次成立，才算恢复。
</Answer>

</Exercises>

## 术语复核与本章回顾

<Glossary>
  <GlossaryItem term="顶点缓冲">
    顶点缓冲在“第12章 腾飞前的助跑——Direct3D 绘制基础”中标记输入进入系统时的责任边界。
  </GlossaryItem>
  <GlossaryItem term="索引缓冲">
    索引缓冲在“第12章 腾飞前的助跑——Direct3D 绘制基础”中标记状态被创建、修改与释放的生命周期。
  </GlossaryItem>
  <GlossaryItem term="FVF">
    FVF在“第12章 腾飞前的助跑——Direct3D 绘制基础”中标记历史实现与现代迁移之间必须保持的机制。
  </GlossaryItem>
  <GlossaryItem term="图元拓扑">
    图元拓扑在“第12章 腾飞前的助跑——Direct3D 绘制基础”中标记能够复算本章结论的原始证据。
  </GlossaryItem>
</Glossary>

掌握“第12章 腾飞前的助跑——Direct3D 绘制基础”意味着能解释让顶点声明与内存布局一致，写入缓冲并在正确流和拓扑上绘制，区分历史坐标与现代迁移，并用顶点字节、流步长、图元计数、绘制返回值和帧捕获推翻或保留实现。

## 阅读导航

[← 上一页：第11章 三维内功心法——Direct3D编程基础](/learn/windows-journey/04-directx-foundation/wj-11-direct3d-foundations) · [下一页：第13章 迈向三维世界——Direct3D 的四大变换 →](/learn/windows-journey/04-directx-foundation/wj-13-four-transforms)

<Attribution
  mode="independent-rewrite"
  sourceBasis="outline-only"
  workTitle="毛星云《逐梦旅程：Windows游戏编程之从零开始》（清华大学出版社，2013）"
  adaptedUrl="https://book.douban.com/subject/25756435/"
/>
