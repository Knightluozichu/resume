---
title: 第13章 迈向三维世界——Direct3D 的四大变换
type: C
section: 第三篇 · DirectX游戏编程基础
order: 13
description: 第13章 迈向三维世界——Direct3D 的四大变换：把模型顶点依次变换到世界、观察、投影和视口坐标，以历史—现代对照、故障恢复和逐节点证据验收。
demo: true
math: false
sourceUrl: 'https://book.douban.com/subject/25756435/'
draft: false
qualityVersion: 2
practiceMode: calculation
sourceMode: independent-rewrite
---
import {
  Wj13FourTransformsMapLab,
  Wj13FourTransformsExperimentLab,
  Wj13FourTransformsEvidenceLab,
} from "@/components/mdx/windows-journey/diagrams/wj-13-four-transforms";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

# 第13章 迈向三维世界——Direct3D 的四大变换

<Objectives>

- 能从“矩阵乘法顺序、左右手系或齐次除法任一错误都会让物体消失或镜像”建立本章的历史问题基线。
- 能解释“明确向量约定与矩阵顺序，并逐空间记录测试点和裁剪结果”的输入、状态、所有权和可见结果。
- 能操作专属实验，对照固定功能变换与着色器矩阵变换，复算像素误差。
- 能注入“交换世界矩阵与观察矩阵或混用左右手投影”，用矩阵、齐次坐标、裁剪标志、NDC 和屏幕像素定位首错、恢复并重放。

</Objectives>

{/* WINDOWS_JOURNEY_QUALITY_V2 */}

## 问题先于 API

矩阵乘法顺序、左右手系或齐次除法任一错误都会让物体消失或镜像。本章先保存问题、初态和输入，再讨论 API；通过条件是“每个测试顶点在五个坐标空间都有可复算位置”，不是画面偶然出现一次。

## 来源范围、技术年代与独立重写

公开[完整目录](https://book.douban.com/subject/25756435/)只用于界定本页题目和层级，[出版记录](https://www.sinobook.com.cn/b2c/scrp/bookdetail.cfm?iBookNo=646210)与[书目记录](https://books.google.com/books/about/%E9%80%90%E6%A2%A6%E6%97%85%E7%A8%8B.html?id=6O-F0AEACAAJ)用于交叉核对 ISBN、出版社和年代；没有把目录核对表述成正文忠实。本页关于 Windows/DirectX 的机制由[Microsoft 一手文档](https://learn.microsoft.com/en-us/windows/win32/direct3d9/transforms)核验，以下中文讲解、图示、实验、代码和练习均为独立教学重写。

原书处于 Visual Studio 2010、Win32/GDI、DirectX 9 与固定功能管线语境。现代 API 只作迁移对照：固定功能变换和着色器矩阵变换名称可以不同，但必须消费相同输入并守住同一不变量。

## 机制与术语

<Term def="世界矩阵在“第13章 迈向三维世界——Direct3D 的四大变换”中标记输入进入系统时的责任边界。">世界矩阵</Term>、<Term def="观察矩阵在“第13章 迈向三维世界——Direct3D 的四大变换”中标记状态被创建、修改与释放的生命周期。">观察矩阵</Term>、<Term def="投影矩阵在“第13章 迈向三维世界——Direct3D 的四大变换”中标记历史实现与现代迁移之间必须保持的机制。">投影矩阵</Term>、<Term def="齐次除法在“第13章 迈向三维世界——Direct3D 的四大变换”中标记能够复算本章结论的原始证据。">齐次除法</Term>。四个术语共同约束“把模型顶点依次变换到世界、观察、投影和视口坐标”，结论必须回到矩阵、齐次坐标、裁剪标志、NDC 和屏幕像素。

## 先预测，再操作三层专属实验

操作前写下预测：哪一阶段先改变像素误差；压力扩大四倍时哪条边界先失效；注入“交换世界矩阵与观察矩阵或混用左右手投影”后怎样回到初态。

<Stepper>
  <Step title="1. 正式节点与状态链">
    选择目录节点和机制阶段，检查输入、状态拥有者、可见结果与验证证据。

    <Wj13FourTransformsMapLab />

  </Step>
  <Step title="2. 历史实现与现代迁移">
    一次只改变技术坐标、场景负载或故障之一，读取可复算结果。

    <Wj13FourTransformsExperimentLab />

  </Step>
  <Step title="3. 故障、恢复与同输入重放">
    比较正常、故障和恢复轨迹；最后点击重置，确认所有控制和状态回到初值。

    <Wj13FourTransformsEvidenceLab />

  </Step>
</Stepper>

## 正式目录逐项深读

### 第13章 迈向三维世界——Direct3D 的四大变换

围绕把模型顶点依次变换到世界、观察、投影和视口坐标，对 第13章 迈向三维世界——Direct3D 的四大变换，API 名称属于具体年代；学习重点是输入、返回值、所有权和失败出口，技术事实以微软文档核对。

第13章 迈向三维世界——Direct3D 的四大变换 进入“模型空间”阶段时，先标出输入、状态拥有者和可见输出，再解释明确向量约定与矩阵顺序，并逐空间记录测试点和裁剪结果。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 第13章 迈向三维世界——Direct3D 的四大变换 时固定其余条件，记录矩阵、齐次坐标、裁剪标志、NDC 和屏幕像素，随后注入“交换世界矩阵与观察矩阵或混用左右手投影”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 13.1 四大变换的基本认知

围绕把模型顶点依次变换到世界、观察、投影和视口坐标，对 13.1 四大变换的基本认知，必须给出可复算中间量，先在纸面预测空间或像素结果，再用矩阵、齐次坐标、裁剪标志、NDC 和屏幕像素查找首个数值分叉。

13.1 四大变换的基本认知 进入“世界变换”阶段时，先标出输入、状态拥有者和可见输出，再解释明确向量约定与矩阵顺序，并逐空间记录测试点和裁剪结果。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 13.1 四大变换的基本认知 时固定其余条件，记录矩阵、齐次坐标、裁剪标志、NDC 和屏幕像素，随后注入“交换世界矩阵与观察矩阵或混用左右手投影”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 13.2 四大变换之一：世界变换

围绕把模型顶点依次变换到世界、观察、投影和视口坐标，对 13.2 四大变换之一：世界变换，必须给出可复算中间量，先在纸面预测空间或像素结果，再用矩阵、齐次坐标、裁剪标志、NDC 和屏幕像素查找首个数值分叉。

13.2 四大变换之一：世界变换 进入“观察变换”阶段时，先标出输入、状态拥有者和可见输出，再解释明确向量约定与矩阵顺序，并逐空间记录测试点和裁剪结果。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 13.2 四大变换之一：世界变换 时固定其余条件，记录矩阵、齐次坐标、裁剪标志、NDC 和屏幕像素，随后注入“交换世界矩阵与观察矩阵或混用左右手投影”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 13.2.1 矩阵的平移

围绕把模型顶点依次变换到世界、观察、投影和视口坐标，对 13.2.1 矩阵的平移，必须给出可复算中间量，先在纸面预测空间或像素结果，再用矩阵、齐次坐标、裁剪标志、NDC 和屏幕像素查找首个数值分叉。

13.2.1 矩阵的平移 进入“投影裁剪”阶段时，先标出输入、状态拥有者和可见输出，再解释明确向量约定与矩阵顺序，并逐空间记录测试点和裁剪结果。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 13.2.1 矩阵的平移 时固定其余条件，记录矩阵、齐次坐标、裁剪标志、NDC 和屏幕像素，随后注入“交换世界矩阵与观察矩阵或混用左右手投影”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 13.2.2 矩阵的旋转

围绕把模型顶点依次变换到世界、观察、投影和视口坐标，对 13.2.2 矩阵的旋转，必须给出可复算中间量，先在纸面预测空间或像素结果，再用矩阵、齐次坐标、裁剪标志、NDC 和屏幕像素查找首个数值分叉。

13.2.2 矩阵的旋转 进入“视口映射”阶段时，先标出输入、状态拥有者和可见输出，再解释明确向量约定与矩阵顺序，并逐空间记录测试点和裁剪结果。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 13.2.2 矩阵的旋转 时固定其余条件，记录矩阵、齐次坐标、裁剪标志、NDC 和屏幕像素，随后注入“交换世界矩阵与观察矩阵或混用左右手投影”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 13.2.3 矩阵的缩放

围绕把模型顶点依次变换到世界、观察、投影和视口坐标，对 13.2.3 矩阵的缩放，必须给出可复算中间量，先在纸面预测空间或像素结果，再用矩阵、齐次坐标、裁剪标志、NDC 和屏幕像素查找首个数值分叉。

13.2.3 矩阵的缩放 进入“模型空间”阶段时，先标出输入、状态拥有者和可见输出，再解释明确向量约定与矩阵顺序，并逐空间记录测试点和裁剪结果。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 13.2.3 矩阵的缩放 时固定其余条件，记录矩阵、齐次坐标、裁剪标志、NDC 和屏幕像素，随后注入“交换世界矩阵与观察矩阵或混用左右手投影”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 13.3 四大变换之二：取景变换

围绕把模型顶点依次变换到世界、观察、投影和视口坐标，对 13.3 四大变换之二：取景变换，必须给出可复算中间量，先在纸面预测空间或像素结果，再用矩阵、齐次坐标、裁剪标志、NDC 和屏幕像素查找首个数值分叉。

13.3 四大变换之二：取景变换 进入“世界变换”阶段时，先标出输入、状态拥有者和可见输出，再解释明确向量约定与矩阵顺序，并逐空间记录测试点和裁剪结果。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 13.3 四大变换之二：取景变换 时固定其余条件，记录矩阵、齐次坐标、裁剪标志、NDC 和屏幕像素，随后注入“交换世界矩阵与观察矩阵或混用左右手投影”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 13.4 四大变换之三：投影变换

围绕把模型顶点依次变换到世界、观察、投影和视口坐标，对 13.4 四大变换之三：投影变换，必须给出可复算中间量，先在纸面预测空间或像素结果，再用矩阵、齐次坐标、裁剪标志、NDC 和屏幕像素查找首个数值分叉。

13.4 四大变换之三：投影变换 进入“观察变换”阶段时，先标出输入、状态拥有者和可见输出，再解释明确向量约定与矩阵顺序，并逐空间记录测试点和裁剪结果。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 13.4 四大变换之三：投影变换 时固定其余条件，记录矩阵、齐次坐标、裁剪标志、NDC 和屏幕像素，随后注入“交换世界矩阵与观察矩阵或混用左右手投影”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 13.5 四大变换之四：视口变换

围绕把模型顶点依次变换到世界、观察、投影和视口坐标，对 13.5 四大变换之四：视口变换，必须给出可复算中间量，先在纸面预测空间或像素结果，再用矩阵、齐次坐标、裁剪标志、NDC 和屏幕像素查找首个数值分叉。

13.5 四大变换之四：视口变换 进入“投影裁剪”阶段时，先标出输入、状态拥有者和可见输出，再解释明确向量约定与矩阵顺序，并逐空间记录测试点和裁剪结果。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 13.5 四大变换之四：视口变换 时固定其余条件，记录矩阵、齐次坐标、裁剪标志、NDC 和屏幕像素，随后注入“交换世界矩阵与观察矩阵或混用左右手投影”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 13.6 总结

围绕把模型顶点依次变换到世界、观察、投影和视口坐标，对 13.6 总结，收尾不是装饰，而是要求用矩阵、齐次坐标、裁剪标志、NDC 和屏幕像素复盘“每个测试顶点在五个坐标空间都有可复算位置”是否在正常和失败路径同时成立。

13.6 总结 进入“视口映射”阶段时，先标出输入、状态拥有者和可见输出，再解释明确向量约定与矩阵顺序，并逐空间记录测试点和裁剪结果。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 13.6 总结 时固定其余条件，记录矩阵、齐次坐标、裁剪标志、NDC 和屏幕像素，随后注入“交换世界矩阵与观察矩阵或混用左右手投影”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 13.7 示例程序D3Ddemo5

围绕把模型顶点依次变换到世界、观察、投影和视口坐标，对 13.7 示例程序D3Ddemo5，把前述机制装入一个可运行场景：固定构建、资源和输入，仅改变一个条件，并保留矩阵、齐次坐标、裁剪标志、NDC 和屏幕像素。

13.7 示例程序D3Ddemo5 进入“模型空间”阶段时，先标出输入、状态拥有者和可见输出，再解释明确向量约定与矩阵顺序，并逐空间记录测试点和裁剪结果。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 13.7 示例程序D3Ddemo5 时固定其余条件，记录矩阵、齐次坐标、裁剪标志、NDC 和屏幕像素，随后注入“交换世界矩阵与观察矩阵或混用左右手投影”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 13.8 章节小憩

围绕把模型顶点依次变换到世界、观察、投影和视口坐标，对 13.8 章节小憩，收尾不是装饰，而是要求用矩阵、齐次坐标、裁剪标志、NDC 和屏幕像素复盘“每个测试顶点在五个坐标空间都有可复算位置”是否在正常和失败路径同时成立。

13.8 章节小憩 进入“世界变换”阶段时，先标出输入、状态拥有者和可见输出，再解释明确向量约定与矩阵顺序，并逐空间记录测试点和裁剪结果。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 13.8 章节小憩 时固定其余条件，记录矩阵、齐次坐标、裁剪标志、NDC 和屏幕像素，随后注入“交换世界矩阵与观察矩阵或混用左右手投影”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

## 可重放实现或计算骨架

```text
initial = fixed_state + fixed_input + fixed_timestep
historical = run(initial, "固定功能变换")
modern = run(initial, "着色器矩阵变换")
compare(historical, modern, "像素误差")
pass = "每个测试顶点在五个坐标空间都有可复算位置"
```

真实程序还要固定编译器、SDK、窗口尺寸、资源、随机种子、时间步和输入记录；否则历史实现与现代迁移不能公平比较。

<Callout type="trap" title="把历史 API 当成当前默认方案">
  本章保留 固定功能变换 的教学身份，但新项目必须核对平台支持并解释为何选择 着色器矩阵变换 或其他方案。
</Callout>

<Callout type="trap" title="只看最终画面">
  第13章 迈向三维世界——Direct3D 的四大变换必须说明模型空间、世界变换、观察变换、投影裁剪、视口映射的先后关系；最终像素相同不代表消息、资源、矩阵或状态安全。
</Callout>

<Callout type="trap" title="失败后继续运行">
  注入“交换世界矩阵与观察矩阵或混用左右手投影”后应停在第一个因果分叉；若矩阵、齐次坐标、裁剪标志、NDC 和屏幕像素无法解释并复位，当前实现失败。
</Callout>

## 练习与 4 级证据矩阵

<Exercises>

**问题 1：历史边界。** 怎样避免把 2013 年接口示例误报成今天的默认架构？

<Answer>
  分别记录固定功能变换与着色器矩阵变换的版本、责任和资源模型；只迁移机制，不改写历史事实，并以[Microsoft 文档](https://learn.microsoft.com/en-us/windows/win32/direct3d9/transforms)核对接口身份。
</Answer>

**问题 2：逐节点验证。** 正式目录节点怎样从“出现”升级到可复查证据？

<Answer>
  - **第13章 迈向三维世界——Direct3D 的四大变换**：在“模型空间”记录矩阵、齐次坐标、裁剪标志、NDC 和屏幕像素，注入“交换世界矩阵与观察矩阵或混用左右手投影”后复位并重放。
  - **13.1 四大变换的基本认知**：在“世界变换”记录矩阵、齐次坐标、裁剪标志、NDC 和屏幕像素，注入“交换世界矩阵与观察矩阵或混用左右手投影”后复位并重放。
  - **13.2 四大变换之一：世界变换**：在“观察变换”记录矩阵、齐次坐标、裁剪标志、NDC 和屏幕像素，注入“交换世界矩阵与观察矩阵或混用左右手投影”后复位并重放。
  - **13.2.1 矩阵的平移**：在“投影裁剪”记录矩阵、齐次坐标、裁剪标志、NDC 和屏幕像素，注入“交换世界矩阵与观察矩阵或混用左右手投影”后复位并重放。
  - **13.2.2 矩阵的旋转**：在“视口映射”记录矩阵、齐次坐标、裁剪标志、NDC 和屏幕像素，注入“交换世界矩阵与观察矩阵或混用左右手投影”后复位并重放。
  - **13.2.3 矩阵的缩放**：在“模型空间”记录矩阵、齐次坐标、裁剪标志、NDC 和屏幕像素，注入“交换世界矩阵与观察矩阵或混用左右手投影”后复位并重放。
  - **13.3 四大变换之二：取景变换**：在“世界变换”记录矩阵、齐次坐标、裁剪标志、NDC 和屏幕像素，注入“交换世界矩阵与观察矩阵或混用左右手投影”后复位并重放。
  - **13.4 四大变换之三：投影变换**：在“观察变换”记录矩阵、齐次坐标、裁剪标志、NDC 和屏幕像素，注入“交换世界矩阵与观察矩阵或混用左右手投影”后复位并重放。
  - **13.5 四大变换之四：视口变换**：在“投影裁剪”记录矩阵、齐次坐标、裁剪标志、NDC 和屏幕像素，注入“交换世界矩阵与观察矩阵或混用左右手投影”后复位并重放。
  - **13.6 总结**：在“视口映射”记录矩阵、齐次坐标、裁剪标志、NDC 和屏幕像素，注入“交换世界矩阵与观察矩阵或混用左右手投影”后复位并重放。
  - **13.7 示例程序D3Ddemo5**：在“模型空间”记录矩阵、齐次坐标、裁剪标志、NDC 和屏幕像素，注入“交换世界矩阵与观察矩阵或混用左右手投影”后复位并重放。
  - **13.8 章节小憩**：在“世界变换”记录矩阵、齐次坐标、裁剪标志、NDC 和屏幕像素，注入“交换世界矩阵与观察矩阵或混用左右手投影”后复位并重放。
</Answer>

**问题 3：故障闭环。** 注入“交换世界矩阵与观察矩阵或混用左右手投影”后怎样判定恢复成功？

<Answer>
  保存首错阶段，逆序清理或恢复必要状态，以相同初态和输入重放；只有矩阵、齐次坐标、裁剪标志、NDC 和屏幕像素回到基线且“每个测试顶点在五个坐标空间都有可复算位置”再次成立，才算恢复。
</Answer>

</Exercises>

## 术语复核与本章回顾

<Glossary>
  <GlossaryItem term="世界矩阵">
    世界矩阵在“第13章 迈向三维世界——Direct3D 的四大变换”中标记输入进入系统时的责任边界。
  </GlossaryItem>
  <GlossaryItem term="观察矩阵">
    观察矩阵在“第13章 迈向三维世界——Direct3D 的四大变换”中标记状态被创建、修改与释放的生命周期。
  </GlossaryItem>
  <GlossaryItem term="投影矩阵">
    投影矩阵在“第13章 迈向三维世界——Direct3D 的四大变换”中标记历史实现与现代迁移之间必须保持的机制。
  </GlossaryItem>
  <GlossaryItem term="齐次除法">
    齐次除法在“第13章 迈向三维世界——Direct3D 的四大变换”中标记能够复算本章结论的原始证据。
  </GlossaryItem>
</Glossary>

掌握“第13章 迈向三维世界——Direct3D 的四大变换”意味着能解释明确向量约定与矩阵顺序，并逐空间记录测试点和裁剪结果，区分历史坐标与现代迁移，并用矩阵、齐次坐标、裁剪标志、NDC 和屏幕像素推翻或保留实现。

## 阅读导航

[← 上一页：第12章 腾飞前的助跑——Direct3D 绘制基础](/learn/windows-journey/04-directx-foundation/wj-12-direct3d-drawing) · [下一页：第14章 绘制出质感的世界—光照与材质 →](/learn/windows-journey/04-directx-foundation/wj-14-lighting-materials)

<Attribution
  mode="independent-rewrite"
  sourceBasis="outline-only"
  workTitle="毛星云《逐梦旅程：Windows游戏编程之从零开始》（清华大学出版社，2013）"
  adaptedUrl="https://book.douban.com/subject/25756435/"
/>
