---
title: 第14章 绘制出质感的世界—光照与材质
type: C
section: 第三篇 · DirectX游戏编程基础
order: 14
description: 第14章 绘制出质感的世界—光照与材质：分离光源、法线、材质反射分量与固定功能逐顶点光照，以历史—现代对照、故障恢复和逐节点证据验收。
demo: true
math: false
sourceUrl: 'https://book.douban.com/subject/25756435/'
draft: false
qualityVersion: 2
practiceMode: calculation
sourceMode: independent-rewrite
---

# 第14章 绘制出质感的世界—光照与材质


{/* WINDOWS_JOURNEY_QUALITY_V2 */}

## 问题先于 API

只调亮度参数会掩盖法线方向、空间和归一化错误。本章先保存问题、初态和输入，再讨论 API；通过条件是“光照结果能由法线、光向、视向和材质参数逐项复算”，不是画面偶然出现一次。

## 来源范围、技术年代与独立重写

公开[完整目录](https://book.douban.com/subject/25756435/)只用于界定本页题目和层级，[出版记录](https://www.sinobook.com.cn/b2c/scrp/bookdetail.cfm?iBookNo=646210)与[书目记录](https://books.google.com/books/about/%E9%80%90%E6%A2%A6%E6%97%85%E7%A8%8B.html?id=6O-F0AEACAAJ)用于交叉核对 ISBN、出版社和年代；没有把目录核对表述成正文忠实。本页关于 Windows/DirectX 的机制由[Microsoft 一手文档](https://learn.microsoft.com/en-us/windows/win32/direct3d9/lights-and-materials)核验，以下中文讲解、图示、实验、代码和练习均为独立教学重写。

原书处于 Visual Studio 2010、Win32/GDI、DirectX 9 与固定功能管线语境。现代 API 只作迁移对照：D3D9 固定光照和着色器显式光照名称可以不同，但必须消费相同输入并守住同一不变量。

## 机制与术语

、、、。四个术语共同约束“分离光源、法线、材质反射分量与固定功能逐顶点光照”，结论必须回到点积、各光照分量、法线长度和最终颜色。

## 正式目录逐项深读

### 第14章 绘制出质感的世界—光照与材质

围绕分离光源、法线、材质反射分量与固定功能逐顶点光照，对 第14章 绘制出质感的世界—光照与材质，必须给出可复算中间量，先在纸面预测空间或像素结果，再用点积、各光照分量、法线长度和最终颜色查找首个数值分叉。

第14章 绘制出质感的世界—光照与材质 进入“准备法线”阶段时，先标出输入、状态拥有者和可见输出，再解释在同一空间计算环境光、漫反射和镜面项，再由材质决定反射比例。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 第14章 绘制出质感的世界—光照与材质 时固定其余条件，记录点积、各光照分量、法线长度和最终颜色，随后注入“非均匀缩放后法线未正确变换或归一化”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 14.1 引言

围绕分离光源、法线、材质反射分量与固定功能逐顶点光照，14.1 引言 限定本章的一个知识坐标；独立解释围绕“在同一空间计算环境光、漫反射和镜面项，再由材质决定反射比例”展开，并以“光照结果能由法线、光向、视向和材质参数逐项复算”结束。

14.1 引言 进入“变换光向”阶段时，先标出输入、状态拥有者和可见输出，再解释在同一空间计算环境光、漫反射和镜面项，再由材质决定反射比例。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 14.1 引言 时固定其余条件，记录点积、各光照分量、法线长度和最终颜色，随后注入“非均匀缩放后法线未正确变换或归一化”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 14.2 四大光照类型

围绕分离光源、法线、材质反射分量与固定功能逐顶点光照，对 14.2 四大光照类型，必须给出可复算中间量，先在纸面预测空间或像素结果，再用点积、各光照分量、法线长度和最终颜色查找首个数值分叉。

14.2 四大光照类型 进入“计算漫反射”阶段时，先标出输入、状态拥有者和可见输出，再解释在同一空间计算环境光、漫反射和镜面项，再由材质决定反射比例。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 14.2 四大光照类型 时固定其余条件，记录点积、各光照分量、法线长度和最终颜色，随后注入“非均匀缩放后法线未正确变换或归一化”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 14.3 三大光源类型

围绕分离光源、法线、材质反射分量与固定功能逐顶点光照，14.3 三大光源类型 限定本章的一个知识坐标；独立解释围绕“在同一空间计算环境光、漫反射和镜面项，再由材质决定反射比例”展开，并以“光照结果能由法线、光向、视向和材质参数逐项复算”结束。

14.3 三大光源类型 进入“计算镜面”阶段时，先标出输入、状态拥有者和可见输出，再解释在同一空间计算环境光、漫反射和镜面项，再由材质决定反射比例。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 14.3 三大光源类型 时固定其余条件，记录点积、各光照分量、法线长度和最终颜色，随后注入“非均匀缩放后法线未正确变换或归一化”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 14.4 材质

围绕分离光源、法线、材质反射分量与固定功能逐顶点光照，对 14.4 材质，必须给出可复算中间量，先在纸面预测空间或像素结果，再用点积、各光照分量、法线长度和最终颜色查找首个数值分叉。

14.4 材质 进入“合成材质”阶段时，先标出输入、状态拥有者和可见输出，再解释在同一空间计算环境光、漫反射和镜面项，再由材质决定反射比例。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 14.4 材质 时固定其余条件，记录点积、各光照分量、法线长度和最终颜色，随后注入“非均匀缩放后法线未正确变换或归一化”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 14.5 关于顶点法线

围绕分离光源、法线、材质反射分量与固定功能逐顶点光照，14.5 关于顶点法线 限定本章的一个知识坐标；独立解释围绕“在同一空间计算环境光、漫反射和镜面项，再由材质决定反射比例”展开，并以“光照结果能由法线、光向、视向和材质参数逐项复算”结束。

14.5 关于顶点法线 进入“准备法线”阶段时，先标出输入、状态拥有者和可见输出，再解释在同一空间计算环境光、漫反射和镜面项，再由材质决定反射比例。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 14.5 关于顶点法线 时固定其余条件，记录点积、各光照分量、法线长度和最终颜色，随后注入“非均匀缩放后法线未正确变换或归一化”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 14.6 总结与升华

围绕分离光源、法线、材质反射分量与固定功能逐顶点光照，对 14.6 总结与升华，收尾不是装饰，而是要求用点积、各光照分量、法线长度和最终颜色复盘“光照结果能由法线、光向、视向和材质参数逐项复算”是否在正常和失败路径同时成立。

14.6 总结与升华 进入“变换光向”阶段时，先标出输入、状态拥有者和可见输出，再解释在同一空间计算环境光、漫反射和镜面项，再由材质决定反射比例。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 14.6 总结与升华 时固定其余条件，记录点积、各光照分量、法线长度和最终颜色，随后注入“非均匀缩放后法线未正确变换或归一化”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 14.7 几何体的快捷绘制

围绕分离光源、法线、材质反射分量与固定功能逐顶点光照，14.7 几何体的快捷绘制 限定本章的一个知识坐标；独立解释围绕“在同一空间计算环境光、漫反射和镜面项，再由材质决定反射比例”展开，并以“光照结果能由法线、光向、视向和材质参数逐项复算”结束。

14.7 几何体的快捷绘制 进入“计算漫反射”阶段时，先标出输入、状态拥有者和可见输出，再解释在同一空间计算环境光、漫反射和镜面项，再由材质决定反射比例。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 14.7 几何体的快捷绘制 时固定其余条件，记录点积、各光照分量、法线长度和最终颜色，随后注入“非均匀缩放后法线未正确变换或归一化”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 14.7.1 D3D中内置的几何体概述

围绕分离光源、法线、材质反射分量与固定功能逐顶点光照，对 14.7.1 D3D中内置的几何体概述，API 名称属于具体年代；学习重点是输入、返回值、所有权和失败出口，技术事实以微软文档核对。

14.7.1 D3D中内置的几何体概述 进入“计算镜面”阶段时，先标出输入、状态拥有者和可见输出，再解释在同一空间计算环境光、漫反射和镜面项，再由材质决定反射比例。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 14.7.1 D3D中内置的几何体概述 时固定其余条件，记录点积、各光照分量、法线长度和最终颜色，随后注入“非均匀缩放后法线未正确变换或归一化”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 14.7.2 D3D中几种内置的几何体绘制四步曲

围绕分离光源、法线、材质反射分量与固定功能逐顶点光照，对 14.7.2 D3D中几种内置的几何体绘制四步曲，API 名称属于具体年代；学习重点是输入、返回值、所有权和失败出口，技术事实以微软文档核对。

14.7.2 D3D中几种内置的几何体绘制四步曲 进入“合成材质”阶段时，先标出输入、状态拥有者和可见输出，再解释在同一空间计算环境光、漫反射和镜面项，再由材质决定反射比例。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 14.7.2 D3D中几种内置的几何体绘制四步曲 时固定其余条件，记录点积、各光照分量、法线长度和最终颜色，随后注入“非均匀缩放后法线未正确变换或归一化”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 14.7.3 D3D中几种内置几何体的创建

围绕分离光源、法线、材质反射分量与固定功能逐顶点光照，对 14.7.3 D3D中几种内置几何体的创建，API 名称属于具体年代；学习重点是输入、返回值、所有权和失败出口，技术事实以微软文档核对。

14.7.3 D3D中几种内置几何体的创建 进入“准备法线”阶段时，先标出输入、状态拥有者和可见输出，再解释在同一空间计算环境光、漫反射和镜面项，再由材质决定反射比例。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 14.7.3 D3D中几种内置几何体的创建 时固定其余条件，记录点积、各光照分量、法线长度和最终颜色，随后注入“非均匀缩放后法线未正确变换或归一化”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 14.8 示例程序D3Ddemo7

围绕分离光源、法线、材质反射分量与固定功能逐顶点光照，对 14.8 示例程序D3Ddemo7，把前述机制装入一个可运行场景：固定构建、资源和输入，仅改变一个条件，并保留点积、各光照分量、法线长度和最终颜色。

14.8 示例程序D3Ddemo7 进入“变换光向”阶段时，先标出输入、状态拥有者和可见输出，再解释在同一空间计算环境光、漫反射和镜面项，再由材质决定反射比例。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 14.8 示例程序D3Ddemo7 时固定其余条件，记录点积、各光照分量、法线长度和最终颜色，随后注入“非均匀缩放后法线未正确变换或归一化”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 14.9 章节小憩

围绕分离光源、法线、材质反射分量与固定功能逐顶点光照，对 14.9 章节小憩，收尾不是装饰，而是要求用点积、各光照分量、法线长度和最终颜色复盘“光照结果能由法线、光向、视向和材质参数逐项复算”是否在正常和失败路径同时成立。

14.9 章节小憩 进入“计算漫反射”阶段时，先标出输入、状态拥有者和可见输出，再解释在同一空间计算环境光、漫反射和镜面项，再由材质决定反射比例。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 14.9 章节小憩 时固定其余条件，记录点积、各光照分量、法线长度和最终颜色，随后注入“非均匀缩放后法线未正确变换或归一化”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

## 可重放实现或计算骨架

```text
initial = fixed_state + fixed_input + fixed_timestep
historical = run(initial, "D3D9 固定光照")
modern = run(initial, "着色器显式光照")
compare(historical, modern, "颜色误差")
pass = "光照结果能由法线、光向、视向和材质参数逐项复算"
```

真实程序还要固定编译器、SDK、窗口尺寸、资源、随机种子、时间步和输入记录；否则历史实现与现代迁移不能公平比较。


## 练习与 4 级证据矩阵


## 术语复核与本章回顾


掌握“第14章 绘制出质感的世界—光照与材质”意味着能解释在同一空间计算环境光、漫反射和镜面项，再由材质决定反射比例，区分历史坐标与现代迁移，并用点积、各光照分量、法线长度和最终颜色推翻或保留实现。

## 阅读导航

[← 上一页：第13章 迈向三维世界——Direct3D 的四大变换](/learn/windows-journey/04-directx-foundation/wj-13-four-transforms) · [下一页：第15章 做游戏的主人——输出控制利器DirectInput →](/learn/windows-journey/04-directx-foundation/wj-15-directinput)

