---
title: 第15章 做游戏的主人——输出控制利器DirectInput
type: C
section: 第三篇 · DirectX游戏编程基础
order: 15
description: >-
  第15章 做游戏的主人——输出控制利器DirectInput：理解 DirectInput
  设备创建、数据格式、协作级别、获取与丢失恢复，以历史—现代对照、故障恢复和逐节点证据验收。
demo: true
math: false
sourceUrl: 'https://book.douban.com/subject/25756435/'
draft: false
qualityVersion: 2
practiceMode: diagnosis
sourceMode: independent-rewrite
---

# 第15章 做游戏的主人——输出控制利器DirectInput


{/* WINDOWS_JOURNEY_QUALITY_V2 */}

## 问题先于 API

轮询成功一次不能证明焦点切换、设备丢失和动作映射能够恢复。本章先保存问题、初态和输入，再讨论 API；通过条件是“设备丢失和重新获取不会制造卡键或重复动作”，不是画面偶然出现一次。

## 来源范围、技术年代与独立重写

公开[完整目录](https://book.douban.com/subject/25756435/)只用于界定本页题目和层级，[出版记录](https://www.sinobook.com.cn/b2c/scrp/bookdetail.cfm?iBookNo=646210)与[书目记录](https://books.google.com/books/about/%E9%80%90%E6%A2%A6%E6%97%85%E7%A8%8B.html?id=6O-F0AEACAAJ)用于交叉核对 ISBN、出版社和年代；没有把目录核对表述成正文忠实。本页关于 Windows/DirectX 的机制由[Microsoft 一手文档](https://learn.microsoft.com/en-us/previous-versions/windows/desktop/ee416842%28v%3Dvs.85%29)核验，以下中文讲解、图示、实验、代码和练习均为独立教学重写。

原书处于 Visual Studio 2010、Win32/GDI、DirectX 9 与固定功能管线语境。现代 API 只作迁移对照：DirectInput 轮询和消息/XInput 动作层名称可以不同，但必须消费相同输入并守住同一不变量。

## 机制与术语

、、、。四个术语共同约束“理解 DirectInput 设备创建、数据格式、协作级别、获取与丢失恢复”，结论必须回到HRESULT、获取状态、原始输入、动作边沿和焦点轨迹。

## 正式目录逐项深读

### 第15章 做游戏的主人——输出控制利器DirectInput

围绕理解 DirectInput 设备创建、数据格式、协作级别、获取与丢失恢复，对 第15章 做游戏的主人——输出控制利器DirectInput，把前述机制装入一个可运行场景：固定构建、资源和输入，仅改变一个条件，并保留HRESULT、获取状态、原始输入、动作边沿和焦点轨迹。

第15章 做游戏的主人——输出控制利器DirectInput 进入“枚举设备”阶段时，先标出输入、状态拥有者和可见输出，再解释创建并配置设备，Acquire 后读取状态，失败时重新获取并归一化为动作。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 第15章 做游戏的主人——输出控制利器DirectInput 时固定其余条件，记录HRESULT、获取状态、原始输入、动作边沿和焦点轨迹，随后注入“DIERR_INPUTLOST 后继续消费旧缓冲状态”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 15.1 引言

围绕理解 DirectInput 设备创建、数据格式、协作级别、获取与丢失恢复，15.1 引言 限定本章的一个知识坐标；独立解释围绕“创建并配置设备，Acquire 后读取状态，失败时重新获取并归一化为动作”展开，并以“设备丢失和重新获取不会制造卡键或重复动作”结束。

15.1 引言 进入“设置格式”阶段时，先标出输入、状态拥有者和可见输出，再解释创建并配置设备，Acquire 后读取状态，失败时重新获取并归一化为动作。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 15.1 引言 时固定其余条件，记录HRESULT、获取状态、原始输入、动作边沿和焦点轨迹，随后注入“DIERR_INPUTLOST 后继续消费旧缓冲状态”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 15.2 DirectInput接口概述

围绕理解 DirectInput 设备创建、数据格式、协作级别、获取与丢失恢复，对 15.2 DirectInput接口概述，API 名称属于具体年代；学习重点是输入、返回值、所有权和失败出口，技术事实以微软文档核对。

15.2 DirectInput接口概述 进入“设置协作”阶段时，先标出输入、状态拥有者和可见输出，再解释创建并配置设备，Acquire 后读取状态，失败时重新获取并归一化为动作。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 15.2 DirectInput接口概述 时固定其余条件，记录HRESULT、获取状态、原始输入、动作边沿和焦点轨迹，随后注入“DIERR_INPUTLOST 后继续消费旧缓冲状态”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 15.3 DirectInput使用步骤详解

围绕理解 DirectInput 设备创建、数据格式、协作级别、获取与丢失恢复，对 15.3 DirectInput使用步骤详解，API 名称属于具体年代；学习重点是输入、返回值、所有权和失败出口，技术事实以微软文档核对。

15.3 DirectInput使用步骤详解 进入“轮询状态”阶段时，先标出输入、状态拥有者和可见输出，再解释创建并配置设备，Acquire 后读取状态，失败时重新获取并归一化为动作。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 15.3 DirectInput使用步骤详解 时固定其余条件，记录HRESULT、获取状态、原始输入、动作边沿和焦点轨迹，随后注入“DIERR_INPUTLOST 后继续消费旧缓冲状态”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 15.3.1 头文件和库文件的包含

围绕理解 DirectInput 设备创建、数据格式、协作级别、获取与丢失恢复，15.3.1 头文件和库文件的包含 限定本章的一个知识坐标；独立解释围绕“创建并配置设备，Acquire 后读取状态，失败时重新获取并归一化为动作”展开，并以“设备丢失和重新获取不会制造卡键或重复动作”结束。

15.3.1 头文件和库文件的包含 进入“丢失重获”阶段时，先标出输入、状态拥有者和可见输出，再解释创建并配置设备，Acquire 后读取状态，失败时重新获取并归一化为动作。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 15.3.1 头文件和库文件的包含 时固定其余条件，记录HRESULT、获取状态、原始输入、动作边沿和焦点轨迹，随后注入“DIERR_INPUTLOST 后继续消费旧缓冲状态”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 15.3.2 创建DirectInput接口和设备

围绕理解 DirectInput 设备创建、数据格式、协作级别、获取与丢失恢复，对 15.3.2 创建DirectInput接口和设备，API 名称属于具体年代；学习重点是输入、返回值、所有权和失败出口，技术事实以微软文档核对。

15.3.2 创建DirectInput接口和设备 进入“枚举设备”阶段时，先标出输入、状态拥有者和可见输出，再解释创建并配置设备，Acquire 后读取状态，失败时重新获取并归一化为动作。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 15.3.2 创建DirectInput接口和设备 时固定其余条件，记录HRESULT、获取状态、原始输入、动作边沿和焦点轨迹，随后注入“DIERR_INPUTLOST 后继续消费旧缓冲状态”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 15.3.3 设置数据格式

围绕理解 DirectInput 设备创建、数据格式、协作级别、获取与丢失恢复，15.3.3 设置数据格式 限定本章的一个知识坐标；独立解释围绕“创建并配置设备，Acquire 后读取状态，失败时重新获取并归一化为动作”展开，并以“设备丢失和重新获取不会制造卡键或重复动作”结束。

15.3.3 设置数据格式 进入“设置格式”阶段时，先标出输入、状态拥有者和可见输出，再解释创建并配置设备，Acquire 后读取状态，失败时重新获取并归一化为动作。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 15.3.3 设置数据格式 时固定其余条件，记录HRESULT、获取状态、原始输入、动作边沿和焦点轨迹，随后注入“DIERR_INPUTLOST 后继续消费旧缓冲状态”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 15.3.4 设置协作级别

围绕理解 DirectInput 设备创建、数据格式、协作级别、获取与丢失恢复，15.3.4 设置协作级别 限定本章的一个知识坐标；独立解释围绕“创建并配置设备，Acquire 后读取状态，失败时重新获取并归一化为动作”展开，并以“设备丢失和重新获取不会制造卡键或重复动作”结束。

15.3.4 设置协作级别 进入“设置协作”阶段时，先标出输入、状态拥有者和可见输出，再解释创建并配置设备，Acquire 后读取状态，失败时重新获取并归一化为动作。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 15.3.4 设置协作级别 时固定其余条件，记录HRESULT、获取状态、原始输入、动作边沿和焦点轨迹，随后注入“DIERR_INPUTLOST 后继续消费旧缓冲状态”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 15.3.5 设置特殊属性

围绕理解 DirectInput 设备创建、数据格式、协作级别、获取与丢失恢复，15.3.5 设置特殊属性 限定本章的一个知识坐标；独立解释围绕“创建并配置设备，Acquire 后读取状态，失败时重新获取并归一化为动作”展开，并以“设备丢失和重新获取不会制造卡键或重复动作”结束。

15.3.5 设置特殊属性 进入“轮询状态”阶段时，先标出输入、状态拥有者和可见输出，再解释创建并配置设备，Acquire 后读取状态，失败时重新获取并归一化为动作。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 15.3.5 设置特殊属性 时固定其余条件，记录HRESULT、获取状态、原始输入、动作边沿和焦点轨迹，随后注入“DIERR_INPUTLOST 后继续消费旧缓冲状态”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 15.3.6 获取和轮询设备

围绕理解 DirectInput 设备创建、数据格式、协作级别、获取与丢失恢复，15.3.6 获取和轮询设备 限定本章的一个知识坐标；独立解释围绕“创建并配置设备，Acquire 后读取状态，失败时重新获取并归一化为动作”展开，并以“设备丢失和重新获取不会制造卡键或重复动作”结束。

15.3.6 获取和轮询设备 进入“丢失重获”阶段时，先标出输入、状态拥有者和可见输出，再解释创建并配置设备，Acquire 后读取状态，失败时重新获取并归一化为动作。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 15.3.6 获取和轮询设备 时固定其余条件，记录HRESULT、获取状态、原始输入、动作边沿和焦点轨迹，随后注入“DIERR_INPUTLOST 后继续消费旧缓冲状态”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 15.3.7 读取设备信息

围绕理解 DirectInput 设备创建、数据格式、协作级别、获取与丢失恢复，15.3.7 读取设备信息 限定本章的一个知识坐标；独立解释围绕“创建并配置设备，Acquire 后读取状态，失败时重新获取并归一化为动作”展开，并以“设备丢失和重新获取不会制造卡键或重复动作”结束。

15.3.7 读取设备信息 进入“枚举设备”阶段时，先标出输入、状态拥有者和可见输出，再解释创建并配置设备，Acquire 后读取状态，失败时重新获取并归一化为动作。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 15.3.7 读取设备信息 时固定其余条件，记录HRESULT、获取状态、原始输入、动作边沿和焦点轨迹，随后注入“DIERR_INPUTLOST 后继续消费旧缓冲状态”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 15.4 精炼：DirectInput使用五步曲

围绕理解 DirectInput 设备创建、数据格式、协作级别、获取与丢失恢复，对 15.4 精炼：DirectInput使用五步曲，API 名称属于具体年代；学习重点是输入、返回值、所有权和失败出口，技术事实以微软文档核对。

15.4 精炼：DirectInput使用五步曲 进入“设置格式”阶段时，先标出输入、状态拥有者和可见输出，再解释创建并配置设备，Acquire 后读取状态，失败时重新获取并归一化为动作。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 15.4 精炼：DirectInput使用五步曲 时固定其余条件，记录HRESULT、获取状态、原始输入、动作边沿和焦点轨迹，随后注入“DIERR_INPUTLOST 后继续消费旧缓冲状态”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 15.5 DirectInput键盘按键键值总结

围绕理解 DirectInput 设备创建、数据格式、协作级别、获取与丢失恢复，对 15.5 DirectInput键盘按键键值总结，收尾不是装饰，而是要求用HRESULT、获取状态、原始输入、动作边沿和焦点轨迹复盘“设备丢失和重新获取不会制造卡键或重复动作”是否在正常和失败路径同时成立。

15.5 DirectInput键盘按键键值总结 进入“设置协作”阶段时，先标出输入、状态拥有者和可见输出，再解释创建并配置设备，Acquire 后读取状态，失败时重新获取并归一化为动作。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 15.5 DirectInput键盘按键键值总结 时固定其余条件，记录HRESULT、获取状态、原始输入、动作边沿和焦点轨迹，随后注入“DIERR_INPUTLOST 后继续消费旧缓冲状态”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 15.6 DirectInput鼠标按键键值总结

围绕理解 DirectInput 设备创建、数据格式、协作级别、获取与丢失恢复，对 15.6 DirectInput鼠标按键键值总结，收尾不是装饰，而是要求用HRESULT、获取状态、原始输入、动作边沿和焦点轨迹复盘“设备丢失和重新获取不会制造卡键或重复动作”是否在正常和失败路径同时成立。

15.6 DirectInput鼠标按键键值总结 进入“轮询状态”阶段时，先标出输入、状态拥有者和可见输出，再解释创建并配置设备，Acquire 后读取状态，失败时重新获取并归一化为动作。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 15.6 DirectInput鼠标按键键值总结 时固定其余条件，记录HRESULT、获取状态、原始输入、动作边沿和焦点轨迹，随后注入“DIERR_INPUTLOST 后继续消费旧缓冲状态”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 15.7 示例程序D3Ddemo8

围绕理解 DirectInput 设备创建、数据格式、协作级别、获取与丢失恢复，对 15.7 示例程序D3Ddemo8，把前述机制装入一个可运行场景：固定构建、资源和输入，仅改变一个条件，并保留HRESULT、获取状态、原始输入、动作边沿和焦点轨迹。

15.7 示例程序D3Ddemo8 进入“丢失重获”阶段时，先标出输入、状态拥有者和可见输出，再解释创建并配置设备，Acquire 后读取状态，失败时重新获取并归一化为动作。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 15.7 示例程序D3Ddemo8 时固定其余条件，记录HRESULT、获取状态、原始输入、动作边沿和焦点轨迹，随后注入“DIERR_INPUTLOST 后继续消费旧缓冲状态”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 15.8 手把手封装DirectInput到类中

围绕理解 DirectInput 设备创建、数据格式、协作级别、获取与丢失恢复，对 15.8 手把手封装DirectInput到类中，API 名称属于具体年代；学习重点是输入、返回值、所有权和失败出口，技术事实以微软文档核对。

15.8 手把手封装DirectInput到类中 进入“枚举设备”阶段时，先标出输入、状态拥有者和可见输出，再解释创建并配置设备，Acquire 后读取状态，失败时重新获取并归一化为动作。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 15.8 手把手封装DirectInput到类中 时固定其余条件，记录HRESULT、获取状态、原始输入、动作边沿和焦点轨迹，随后注入“DIERR_INPUTLOST 后继续消费旧缓冲状态”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 15.9 封装好的DirectInput类的使用

围绕理解 DirectInput 设备创建、数据格式、协作级别、获取与丢失恢复，对 15.9 封装好的DirectInput类的使用，API 名称属于具体年代；学习重点是输入、返回值、所有权和失败出口，技术事实以微软文档核对。

15.9 封装好的DirectInput类的使用 进入“设置格式”阶段时，先标出输入、状态拥有者和可见输出，再解释创建并配置设备，Acquire 后读取状态，失败时重新获取并归一化为动作。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 15.9 封装好的DirectInput类的使用 时固定其余条件，记录HRESULT、获取状态、原始输入、动作边沿和焦点轨迹，随后注入“DIERR_INPUTLOST 后继续消费旧缓冲状态”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 15.10 示例程序D3Ddemo9

围绕理解 DirectInput 设备创建、数据格式、协作级别、获取与丢失恢复，对 15.10 示例程序D3Ddemo9，把前述机制装入一个可运行场景：固定构建、资源和输入，仅改变一个条件，并保留HRESULT、获取状态、原始输入、动作边沿和焦点轨迹。

15.10 示例程序D3Ddemo9 进入“设置协作”阶段时，先标出输入、状态拥有者和可见输出，再解释创建并配置设备，Acquire 后读取状态，失败时重新获取并归一化为动作。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 15.10 示例程序D3Ddemo9 时固定其余条件，记录HRESULT、获取状态、原始输入、动作边沿和焦点轨迹，随后注入“DIERR_INPUTLOST 后继续消费旧缓冲状态”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 15.11 章节小憩

围绕理解 DirectInput 设备创建、数据格式、协作级别、获取与丢失恢复，对 15.11 章节小憩，收尾不是装饰，而是要求用HRESULT、获取状态、原始输入、动作边沿和焦点轨迹复盘“设备丢失和重新获取不会制造卡键或重复动作”是否在正常和失败路径同时成立。

15.11 章节小憩 进入“轮询状态”阶段时，先标出输入、状态拥有者和可见输出，再解释创建并配置设备，Acquire 后读取状态，失败时重新获取并归一化为动作。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 15.11 章节小憩 时固定其余条件，记录HRESULT、获取状态、原始输入、动作边沿和焦点轨迹，随后注入“DIERR_INPUTLOST 后继续消费旧缓冲状态”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

## 可重放实现或计算骨架

```text
freeze -> 枚举设备 -> 设置格式 -> 设置协作 -> 轮询状态 -> 丢失重获
fault -> DIERR_INPUTLOST 后继续消费旧缓冲状态
evidence -> HRESULT、获取状态、原始输入、动作边沿和焦点轨迹
reset -> replay identical input
```

真实程序还要固定编译器、SDK、窗口尺寸、资源、随机种子、时间步和输入记录；否则历史实现与现代迁移不能公平比较。


## 练习与 4 级证据矩阵


## 术语复核与本章回顾


掌握“第15章 做游戏的主人——输出控制利器DirectInput”意味着能解释创建并配置设备，Acquire 后读取状态，失败时重新获取并归一化为动作，区分历史坐标与现代迁移，并用HRESULT、获取状态、原始输入、动作边沿和焦点轨迹推翻或保留实现。

## 阅读导航

[← 上一页：第14章 绘制出质感的世界—光照与材质](/learn/windows-journey/04-directx-foundation/wj-14-lighting-materials) · [下一页：第16章 起舞不落幕——与纹理映射的华丽邂逅 →](/learn/windows-journey/04-directx-foundation/wj-16-texture-mapping)

