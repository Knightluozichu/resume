---
title: 第16章 起舞不落幕——与纹理映射的华丽邂逅
type: C
section: 第三篇 · DirectX游戏编程基础
order: 16
description: 第16章 起舞不落幕——与纹理映射的华丽邂逅：从 UV 坐标、采样寻址、过滤到纹理阶段状态解释像素来源，以历史—现代对照、故障恢复和逐节点证据验收。
demo: true
math: false
sourceUrl: 'https://book.douban.com/subject/25756435/'
draft: false
qualityVersion: 2
practiceMode: simulation
sourceMode: independent-rewrite
---

# 第16章 起舞不落幕——与纹理映射的华丽邂逅


{/* WINDOWS_JOURNEY_QUALITY_V2 */}

## 问题先于 API

纹理出现不等于 UV、过滤、寻址和资源生命周期正确。本章先保存问题、初态和输入，再讨论 API；通过条件是“选定像素的 UV、采样 texel 和组合颜色可以追溯”，不是画面偶然出现一次。

## 来源范围、技术年代与独立重写

公开[完整目录](https://book.douban.com/subject/25756435/)只用于界定本页题目和层级，[出版记录](https://www.sinobook.com.cn/b2c/scrp/bookdetail.cfm?iBookNo=646210)与[书目记录](https://books.google.com/books/about/%E9%80%90%E6%A2%A6%E6%97%85%E7%A8%8B.html?id=6O-F0AEACAAJ)用于交叉核对 ISBN、出版社和年代；没有把目录核对表述成正文忠实。本页关于 Windows/DirectX 的机制由[Microsoft 一手文档](https://learn.microsoft.com/en-us/windows/win32/direct3d9/direct3d-textures)核验，以下中文讲解、图示、实验、代码和练习均为独立教学重写。

原书处于 Visual Studio 2010、Win32/GDI、DirectX 9 与固定功能管线语境。现代 API 只作迁移对照：D3D9 纹理阶段和采样器/着色器名称可以不同，但必须消费相同输入并守住同一不变量。

## 机制与术语

、、、。四个术语共同约束“从 UV 坐标、采样寻址、过滤到纹理阶段状态解释像素来源”，结论必须回到UV、采样坐标、过滤模式、阶段状态和像素颜色。

## 正式目录逐项深读

### 第16章 起舞不落幕——与纹理映射的华丽邂逅

围绕从 UV 坐标、采样寻址、过滤到纹理阶段状态解释像素来源，对 第16章 起舞不落幕——与纹理映射的华丽邂逅，必须给出可复算中间量，先在纸面预测空间或像素结果，再用UV、采样坐标、过滤模式、阶段状态和像素颜色查找首个数值分叉。

第16章 起舞不落幕——与纹理映射的华丽邂逅 进入“载入资源”阶段时，先标出输入、状态拥有者和可见输出，再解释将顶点 UV 插值到像素，按寻址和过滤状态采样后与当前颜色组合。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 第16章 起舞不落幕——与纹理映射的华丽邂逅 时固定其余条件，记录UV、采样坐标、过滤模式、阶段状态和像素颜色，随后注入“UV 超界但寻址模式错误，或纹理状态泄漏到下一物体”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 16.1 纹理映射的概念

围绕从 UV 坐标、采样寻址、过滤到纹理阶段状态解释像素来源，对 16.1 纹理映射的概念，必须给出可复算中间量，先在纸面预测空间或像素结果，再用UV、采样坐标、过滤模式、阶段状态和像素颜色查找首个数值分叉。

16.1 纹理映射的概念 进入“绑定纹理”阶段时，先标出输入、状态拥有者和可见输出，再解释将顶点 UV 插值到像素，按寻址和过滤状态采样后与当前颜色组合。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 16.1 纹理映射的概念 时固定其余条件，记录UV、采样坐标、过滤模式、阶段状态和像素颜色，随后注入“UV 超界但寻址模式错误，或纹理状态泄漏到下一物体”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 16.2 纹理映射使用四步曲

围绕从 UV 坐标、采样寻址、过滤到纹理阶段状态解释像素来源，对 16.2 纹理映射使用四步曲，必须给出可复算中间量，先在纸面预测空间或像素结果，再用UV、采样坐标、过滤模式、阶段状态和像素颜色查找首个数值分叉。

16.2 纹理映射使用四步曲 进入“插值 UV”阶段时，先标出输入、状态拥有者和可见输出，再解释将顶点 UV 插值到像素，按寻址和过滤状态采样后与当前颜色组合。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 16.2 纹理映射使用四步曲 时固定其余条件，记录UV、采样坐标、过滤模式、阶段状态和像素颜色，随后注入“UV 超界但寻址模式错误，或纹理状态泄漏到下一物体”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 16.3 总结与升华

围绕从 UV 坐标、采样寻址、过滤到纹理阶段状态解释像素来源，对 16.3 总结与升华，收尾不是装饰，而是要求用UV、采样坐标、过滤模式、阶段状态和像素颜色复盘“选定像素的 UV、采样 texel 和组合颜色可以追溯”是否在正常和失败路径同时成立。

16.3 总结与升华 进入“寻址采样”阶段时，先标出输入、状态拥有者和可见输出，再解释将顶点 UV 插值到像素，按寻址和过滤状态采样后与当前颜色组合。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 16.3 总结与升华 时固定其余条件，记录UV、采样坐标、过滤模式、阶段状态和像素颜色，随后注入“UV 超界但寻址模式错误，或纹理状态泄漏到下一物体”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 16.4 示例程序D3Ddemo10

围绕从 UV 坐标、采样寻址、过滤到纹理阶段状态解释像素来源，对 16.4 示例程序D3Ddemo10，把前述机制装入一个可运行场景：固定构建、资源和输入，仅改变一个条件，并保留UV、采样坐标、过滤模式、阶段状态和像素颜色。

16.4 示例程序D3Ddemo10 进入“组合输出”阶段时，先标出输入、状态拥有者和可见输出，再解释将顶点 UV 插值到像素，按寻址和过滤状态采样后与当前颜色组合。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 16.4 示例程序D3Ddemo10 时固定其余条件，记录UV、采样坐标、过滤模式、阶段状态和像素颜色，随后注入“UV 超界但寻址模式错误，或纹理状态泄漏到下一物体”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 16.5 四大纹理过滤方式精讲

围绕从 UV 坐标、采样寻址、过滤到纹理阶段状态解释像素来源，对 16.5 四大纹理过滤方式精讲，必须给出可复算中间量，先在纸面预测空间或像素结果，再用UV、采样坐标、过滤模式、阶段状态和像素颜色查找首个数值分叉。

16.5 四大纹理过滤方式精讲 进入“载入资源”阶段时，先标出输入、状态拥有者和可见输出，再解释将顶点 UV 插值到像素，按寻址和过滤状态采样后与当前颜色组合。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 16.5 四大纹理过滤方式精讲 时固定其余条件，记录UV、采样坐标、过滤模式、阶段状态和像素颜色，随后注入“UV 超界但寻址模式错误，或纹理状态泄漏到下一物体”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 16.6 四大纹理寻址方式精讲

围绕从 UV 坐标、采样寻址、过滤到纹理阶段状态解释像素来源，对 16.6 四大纹理寻址方式精讲，必须给出可复算中间量，先在纸面预测空间或像素结果，再用UV、采样坐标、过滤模式、阶段状态和像素颜色查找首个数值分叉。

16.6 四大纹理寻址方式精讲 进入“绑定纹理”阶段时，先标出输入、状态拥有者和可见输出，再解释将顶点 UV 插值到像素，按寻址和过滤状态采样后与当前颜色组合。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 16.6 四大纹理寻址方式精讲 时固定其余条件，记录UV、采样坐标、过滤模式、阶段状态和像素颜色，随后注入“UV 超界但寻址模式错误，或纹理状态泄漏到下一物体”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 16.7 纹理映射知识总结

围绕从 UV 坐标、采样寻址、过滤到纹理阶段状态解释像素来源，对 16.7 纹理映射知识总结，收尾不是装饰，而是要求用UV、采样坐标、过滤模式、阶段状态和像素颜色复盘“选定像素的 UV、采样 texel 和组合颜色可以追溯”是否在正常和失败路径同时成立。

16.7 纹理映射知识总结 进入“插值 UV”阶段时，先标出输入、状态拥有者和可见输出，再解释将顶点 UV 插值到像素，按寻址和过滤状态采样后与当前颜色组合。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 16.7 纹理映射知识总结 时固定其余条件，记录UV、采样坐标、过滤模式、阶段状态和像素颜色，随后注入“UV 超界但寻址模式错误，或纹理状态泄漏到下一物体”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 16.8 示例程序D3Ddemo11

围绕从 UV 坐标、采样寻址、过滤到纹理阶段状态解释像素来源，对 16.8 示例程序D3Ddemo11，把前述机制装入一个可运行场景：固定构建、资源和输入，仅改变一个条件，并保留UV、采样坐标、过滤模式、阶段状态和像素颜色。

16.8 示例程序D3Ddemo11 进入“寻址采样”阶段时，先标出输入、状态拥有者和可见输出，再解释将顶点 UV 插值到像素，按寻址和过滤状态采样后与当前颜色组合。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 16.8 示例程序D3Ddemo11 时固定其余条件，记录UV、采样坐标、过滤模式、阶段状态和像素颜色，随后注入“UV 超界但寻址模式错误，或纹理状态泄漏到下一物体”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 16.9 章节小憩

围绕从 UV 坐标、采样寻址、过滤到纹理阶段状态解释像素来源，对 16.9 章节小憩，收尾不是装饰，而是要求用UV、采样坐标、过滤模式、阶段状态和像素颜色复盘“选定像素的 UV、采样 texel 和组合颜色可以追溯”是否在正常和失败路径同时成立。

16.9 章节小憩 进入“组合输出”阶段时，先标出输入、状态拥有者和可见输出，再解释将顶点 UV 插值到像素，按寻址和过滤状态采样后与当前颜色组合。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 16.9 章节小憩 时固定其余条件，记录UV、采样坐标、过滤模式、阶段状态和像素颜色，随后注入“UV 超界但寻址模式错误，或纹理状态泄漏到下一物体”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

## 可重放实现或计算骨架

```text
freeze -> 载入资源 -> 绑定纹理 -> 插值 UV -> 寻址采样 -> 组合输出
fault -> UV 超界但寻址模式错误，或纹理状态泄漏到下一物体
evidence -> UV、采样坐标、过滤模式、阶段状态和像素颜色
reset -> replay identical input
```

真实程序还要固定编译器、SDK、窗口尺寸、资源、随机种子、时间步和输入记录；否则历史实现与现代迁移不能公平比较。


## 练习与 4 级证据矩阵


## 术语复核与本章回顾


掌握“第16章 起舞不落幕——与纹理映射的华丽邂逅”意味着能解释将顶点 UV 插值到像素，按寻址和过滤状态采样后与当前颜色组合，区分历史坐标与现代迁移，并用UV、采样坐标、过滤模式、阶段状态和像素颜色推翻或保留实现。

## 阅读导航

[← 上一页：第15章 做游戏的主人——输出控制利器DirectInput](/learn/windows-journey/04-directx-foundation/wj-15-directinput) · [下一页：第17章 他山之石可攻玉——三维游戏模型的载入 →](/learn/windows-journey/04-directx-foundation/wj-17-mesh-loading)

