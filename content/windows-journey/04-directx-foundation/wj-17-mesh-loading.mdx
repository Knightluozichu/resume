---
title: 第17章 他山之石可攻玉——三维游戏模型的载入
type: C
section: 第三篇 · DirectX游戏编程基础
order: 17
description: 第17章 他山之石可攻玉——三维游戏模型的载入：验证网格几何、子集、材质、纹理路径和对象所有权，以历史—现代对照、故障恢复和逐节点证据验收。
demo: true
math: false
sourceUrl: 'https://book.douban.com/subject/25756435/'
draft: false
qualityVersion: 2
practiceMode: diagnosis
sourceMode: independent-rewrite
---
import {
  Wj17MeshLoadingMapLab,
  Wj17MeshLoadingExperimentLab,
  Wj17MeshLoadingEvidenceLab,
} from "@/components/mdx/windows-journey/diagrams/wj-17-mesh-loading";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

# 第17章 他山之石可攻玉——三维游戏模型的载入

<Objectives>

- 能从“模型能加载但子集材质错位、纹理缺失或 COM 引用泄漏仍会潜伏”建立本章的历史问题基线。
- 能解释“解析几何与属性表，逐子集绑定对应材质和纹理并记录资源来源”的输入、状态、所有权和可见结果。
- 能操作专属实验，对照D3DX .x 网格与现代资产管线，复算失配子集。
- 能注入“材质数量与子集属性 ID 不一致或相对纹理路径失效”，用顶点索引范围、属性 ID、材质表、纹理路径和引用计数定位首错、恢复并重放。

</Objectives>

{/* WINDOWS_JOURNEY_QUALITY_V2 */}

## 问题先于 API

模型能加载但子集材质错位、纹理缺失或 COM 引用泄漏仍会潜伏。本章先保存问题、初态和输入，再讨论 API；通过条件是“每个子集只引用存在且归属明确的材质、纹理和几何范围”，不是画面偶然出现一次。

## 来源范围、技术年代与独立重写

公开[完整目录](https://book.douban.com/subject/25756435/)只用于界定本页题目和层级，[出版记录](https://www.sinobook.com.cn/b2c/scrp/bookdetail.cfm?iBookNo=646210)与[书目记录](https://books.google.com/books/about/%E9%80%90%E6%A2%A6%E6%97%85%E7%A8%8B.html?id=6O-F0AEACAAJ)用于交叉核对 ISBN、出版社和年代；没有把目录核对表述成正文忠实。本页关于 Windows/DirectX 的机制由[Microsoft 一手文档](https://learn.microsoft.com/en-us/windows/win32/direct3d9/direct3d-textures)核验，以下中文讲解、图示、实验、代码和练习均为独立教学重写。

原书处于 Visual Studio 2010、Win32/GDI、DirectX 9 与固定功能管线语境。现代 API 只作迁移对照：D3DX .x 网格和现代资产管线名称可以不同，但必须消费相同输入并守住同一不变量。

## 机制与术语

<Term def="网格在“第17章 他山之石可攻玉——三维游戏模型的载入”中标记输入进入系统时的责任边界。">网格</Term>、<Term def="子集在“第17章 他山之石可攻玉——三维游戏模型的载入”中标记状态被创建、修改与释放的生命周期。">子集</Term>、<Term def="属性表在“第17章 他山之石可攻玉——三维游戏模型的载入”中标记历史实现与现代迁移之间必须保持的机制。">属性表</Term>、<Term def="材质纹理在“第17章 他山之石可攻玉——三维游戏模型的载入”中标记能够复算本章结论的原始证据。">材质纹理</Term>。四个术语共同约束“验证网格几何、子集、材质、纹理路径和对象所有权”，结论必须回到顶点索引范围、属性 ID、材质表、纹理路径和引用计数。

## 先预测，再操作三层专属实验

操作前写下预测：哪一阶段先改变失配子集；压力扩大四倍时哪条边界先失效；注入“材质数量与子集属性 ID 不一致或相对纹理路径失效”后怎样回到初态。

<Stepper>
  <Step title="1. 正式节点与状态链">
    选择目录节点和机制阶段，检查输入、状态拥有者、可见结果与验证证据。

    <Wj17MeshLoadingMapLab />

  </Step>
  <Step title="2. 历史实现与现代迁移">
    一次只改变技术坐标、场景负载或故障之一，读取可复算结果。

    <Wj17MeshLoadingExperimentLab />

  </Step>
  <Step title="3. 故障、恢复与同输入重放">
    比较正常、故障和恢复轨迹；最后点击重置，确认所有控制和状态回到初值。

    <Wj17MeshLoadingEvidenceLab />

  </Step>
</Stepper>

## 正式目录逐项深读

### 第17章 他山之石可攻玉——三维游戏模型的载入

围绕验证网格几何、子集、材质、纹理路径和对象所有权，对 第17章 他山之石可攻玉——三维游戏模型的载入，把前述机制装入一个可运行场景：固定构建、资源和输入，仅改变一个条件，并保留顶点索引范围、属性 ID、材质表、纹理路径和引用计数。

第17章 他山之石可攻玉——三维游戏模型的载入 进入“读取网格”阶段时，先标出输入、状态拥有者和可见输出，再解释解析几何与属性表，逐子集绑定对应材质和纹理并记录资源来源。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 第17章 他山之石可攻玉——三维游戏模型的载入 时固定其余条件，记录顶点索引范围、属性 ID、材质表、纹理路径和引用计数，随后注入“材质数量与子集属性 ID 不一致或相对纹理路径失效”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 17.1 网格模型技术的前生今世

围绕验证网格几何、子集、材质、纹理路径和对象所有权，17.1 网格模型技术的前生今世 限定本章的一个知识坐标；独立解释围绕“解析几何与属性表，逐子集绑定对应材质和纹理并记录资源来源”展开，并以“每个子集只引用存在且归属明确的材质、纹理和几何范围”结束。

17.1 网格模型技术的前生今世 进入“校验几何”阶段时，先标出输入、状态拥有者和可见输出，再解释解析几何与属性表，逐子集绑定对应材质和纹理并记录资源来源。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 17.1 网格模型技术的前生今世 时固定其余条件，记录顶点索引范围、属性 ID、材质表、纹理路径和引用计数，随后注入“材质数量与子集属性 ID 不一致或相对纹理路径失效”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 17.2 认识三维建模软件3DSMax和Maya

围绕验证网格几何、子集、材质、纹理路径和对象所有权，17.2 认识三维建模软件3DSMax和Maya 限定本章的一个知识坐标；独立解释围绕“解析几何与属性表，逐子集绑定对应材质和纹理并记录资源来源”展开，并以“每个子集只引用存在且归属明确的材质、纹理和几何范围”结束。

17.2 认识三维建模软件3DSMax和Maya 进入“解析子集”阶段时，先标出输入、状态拥有者和可见输出，再解释解析几何与属性表，逐子集绑定对应材质和纹理并记录资源来源。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 17.2 认识三维建模软件3DSMax和Maya 时固定其余条件，记录顶点索引范围、属性 ID、材质表、纹理路径和引用计数，随后注入“材质数量与子集属性 ID 不一致或相对纹理路径失效”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 17.3 对X文件的认识

围绕验证网格几何、子集、材质、纹理路径和对象所有权，17.3 对X文件的认识 限定本章的一个知识坐标；独立解释围绕“解析几何与属性表，逐子集绑定对应材质和纹理并记录资源来源”展开，并以“每个子集只引用存在且归属明确的材质、纹理和几何范围”结束。

17.3 对X文件的认识 进入“绑定材质”阶段时，先标出输入、状态拥有者和可见输出，再解释解析几何与属性表，逐子集绑定对应材质和纹理并记录资源来源。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 17.3 对X文件的认识 时固定其余条件，记录顶点索引范围、属性 ID、材质表、纹理路径和引用计数，随后注入“材质数量与子集属性 ID 不一致或相对纹理路径失效”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 17.4 从3DSMax中导出X文件方法详解

围绕验证网格几何、子集、材质、纹理路径和对象所有权，17.4 从3DSMax中导出X文件方法详解 限定本章的一个知识坐标；独立解释围绕“解析几何与属性表，逐子集绑定对应材质和纹理并记录资源来源”展开，并以“每个子集只引用存在且归属明确的材质、纹理和几何范围”结束。

17.4 从3DSMax中导出X文件方法详解 进入“释放资源”阶段时，先标出输入、状态拥有者和可见输出，再解释解析几何与属性表，逐子集绑定对应材质和纹理并记录资源来源。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 17.4 从3DSMax中导出X文件方法详解 时固定其余条件，记录顶点索引范围、属性 ID、材质表、纹理路径和引用计数，随后注入“材质数量与子集属性 ID 不一致或相对纹理路径失效”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 17.5 网格模型接口ID3DXMESH

围绕验证网格几何、子集、材质、纹理路径和对象所有权，对 17.5 网格模型接口ID3DXMESH，API 名称属于具体年代；学习重点是输入、返回值、所有权和失败出口，技术事实以微软文档核对。

17.5 网格模型接口ID3DXMESH 进入“读取网格”阶段时，先标出输入、状态拥有者和可见输出，再解释解析几何与属性表，逐子集绑定对应材质和纹理并记录资源来源。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 17.5 网格模型接口ID3DXMESH 时固定其余条件，记录顶点索引范围、属性 ID、材质表、纹理路径和引用计数，随后注入“材质数量与子集属性 ID 不一致或相对纹理路径失效”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 17.6 文件模型载入三步曲

围绕验证网格几何、子集、材质、纹理路径和对象所有权，对 17.6 文件模型载入三步曲，重点检查资源生命周期：创建成功只完成一半，失败回滚、逆序释放和同输入重建同样属于通过条件。

17.6 文件模型载入三步曲 进入“校验几何”阶段时，先标出输入、状态拥有者和可见输出，再解释解析几何与属性表，逐子集绑定对应材质和纹理并记录资源来源。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 17.6 文件模型载入三步曲 时固定其余条件，记录顶点索引范围、属性 ID、材质表、纹理路径和引用计数，随后注入“材质数量与子集属性 ID 不一致或相对纹理路径失效”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 17.6.1 三步曲之一：通过X文件加载网格模型

围绕验证网格几何、子集、材质、纹理路径和对象所有权，对 17.6.1 三步曲之一：通过X文件加载网格模型，重点检查资源生命周期：创建成功只完成一半，失败回滚、逆序释放和同输入重建同样属于通过条件。

17.6.1 三步曲之一：通过X文件加载网格模型 进入“解析子集”阶段时，先标出输入、状态拥有者和可见输出，再解释解析几何与属性表，逐子集绑定对应材质和纹理并记录资源来源。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 17.6.1 三步曲之一：通过X文件加载网格模型 时固定其余条件，记录顶点索引范围、属性 ID、材质表、纹理路径和引用计数，随后注入“材质数量与子集属性 ID 不一致或相对纹理路径失效”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 17.6.2 三步曲之二：载入材质和纹理

围绕验证网格几何、子集、材质、纹理路径和对象所有权，对 17.6.2 三步曲之二：载入材质和纹理，必须给出可复算中间量，先在纸面预测空间或像素结果，再用顶点索引范围、属性 ID、材质表、纹理路径和引用计数查找首个数值分叉。

17.6.2 三步曲之二：载入材质和纹理 进入“绑定材质”阶段时，先标出输入、状态拥有者和可见输出，再解释解析几何与属性表，逐子集绑定对应材质和纹理并记录资源来源。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 17.6.2 三步曲之二：载入材质和纹理 时固定其余条件，记录顶点索引范围、属性 ID、材质表、纹理路径和引用计数，随后注入“材质数量与子集属性 ID 不一致或相对纹理路径失效”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 17.6.3 三步曲之三：绘制网格模型

围绕验证网格几何、子集、材质、纹理路径和对象所有权，17.6.3 三步曲之三：绘制网格模型 限定本章的一个知识坐标；独立解释围绕“解析几何与属性表，逐子集绑定对应材质和纹理并记录资源来源”展开，并以“每个子集只引用存在且归属明确的材质、纹理和几何范围”结束。

17.6.3 三步曲之三：绘制网格模型 进入“释放资源”阶段时，先标出输入、状态拥有者和可见输出，再解释解析几何与属性表，逐子集绑定对应材质和纹理并记录资源来源。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 17.6.3 三步曲之三：绘制网格模型 时固定其余条件，记录顶点索引范围、属性 ID、材质表、纹理路径和引用计数，随后注入“材质数量与子集属性 ID 不一致或相对纹理路径失效”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 17.6.4 总结与升华

围绕验证网格几何、子集、材质、纹理路径和对象所有权，对 17.6.4 总结与升华，收尾不是装饰，而是要求用顶点索引范围、属性 ID、材质表、纹理路径和引用计数复盘“每个子集只引用存在且归属明确的材质、纹理和几何范围”是否在正常和失败路径同时成立。

17.6.4 总结与升华 进入“读取网格”阶段时，先标出输入、状态拥有者和可见输出，再解释解析几何与属性表，逐子集绑定对应材质和纹理并记录资源来源。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 17.6.4 总结与升华 时固定其余条件，记录顶点索引范围、属性 ID、材质表、纹理路径和引用计数，随后注入“材质数量与子集属性 ID 不一致或相对纹理路径失效”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 17.7 示例程序D3demo12

围绕验证网格几何、子集、材质、纹理路径和对象所有权，对 17.7 示例程序D3demo12，把前述机制装入一个可运行场景：固定构建、资源和输入，仅改变一个条件，并保留顶点索引范围、属性 ID、材质表、纹理路径和引用计数。

17.7 示例程序D3demo12 进入“校验几何”阶段时，先标出输入、状态拥有者和可见输出，再解释解析几何与属性表，逐子集绑定对应材质和纹理并记录资源来源。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 17.7 示例程序D3demo12 时固定其余条件，记录顶点索引范围、属性 ID、材质表、纹理路径和引用计数，随后注入“材质数量与子集属性 ID 不一致或相对纹理路径失效”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 17.8 章节小憩

围绕验证网格几何、子集、材质、纹理路径和对象所有权，对 17.8 章节小憩，收尾不是装饰，而是要求用顶点索引范围、属性 ID、材质表、纹理路径和引用计数复盘“每个子集只引用存在且归属明确的材质、纹理和几何范围”是否在正常和失败路径同时成立。

17.8 章节小憩 进入“解析子集”阶段时，先标出输入、状态拥有者和可见输出，再解释解析几何与属性表，逐子集绑定对应材质和纹理并记录资源来源。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 17.8 章节小憩 时固定其余条件，记录顶点索引范围、属性 ID、材质表、纹理路径和引用计数，随后注入“材质数量与子集属性 ID 不一致或相对纹理路径失效”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

## 可重放实现或计算骨架

```text
freeze -> 读取网格 -> 校验几何 -> 解析子集 -> 绑定材质 -> 释放资源
fault -> 材质数量与子集属性 ID 不一致或相对纹理路径失效
evidence -> 顶点索引范围、属性 ID、材质表、纹理路径和引用计数
reset -> replay identical input
```

真实程序还要固定编译器、SDK、窗口尺寸、资源、随机种子、时间步和输入记录；否则历史实现与现代迁移不能公平比较。

<Callout type="trap" title="把历史 API 当成当前默认方案">
  本章保留 D3DX .x 网格 的教学身份，但新项目必须核对平台支持并解释为何选择 现代资产管线 或其他方案。
</Callout>

<Callout type="trap" title="只看最终画面">
  第17章 他山之石可攻玉——三维游戏模型的载入必须说明读取网格、校验几何、解析子集、绑定材质、释放资源的先后关系；最终像素相同不代表消息、资源、矩阵或状态安全。
</Callout>

<Callout type="trap" title="失败后继续运行">
  注入“材质数量与子集属性 ID 不一致或相对纹理路径失效”后应停在第一个因果分叉；若顶点索引范围、属性 ID、材质表、纹理路径和引用计数无法解释并复位，当前实现失败。
</Callout>

## 练习与 4 级证据矩阵

<Exercises>

**问题 1：历史边界。** 怎样避免把 2013 年接口示例误报成今天的默认架构？

<Answer>
  分别记录D3DX .x 网格与现代资产管线的版本、责任和资源模型；只迁移机制，不改写历史事实，并以[Microsoft 文档](https://learn.microsoft.com/en-us/windows/win32/direct3d9/direct3d-textures)核对接口身份。
</Answer>

**问题 2：逐节点验证。** 正式目录节点怎样从“出现”升级到可复查证据？

<Answer>
  - **第17章 他山之石可攻玉——三维游戏模型的载入**：在“读取网格”记录顶点索引范围、属性 ID、材质表、纹理路径和引用计数，注入“材质数量与子集属性 ID 不一致或相对纹理路径失效”后复位并重放。
  - **17.1 网格模型技术的前生今世**：在“校验几何”记录顶点索引范围、属性 ID、材质表、纹理路径和引用计数，注入“材质数量与子集属性 ID 不一致或相对纹理路径失效”后复位并重放。
  - **17.2 认识三维建模软件3DSMax和Maya**：在“解析子集”记录顶点索引范围、属性 ID、材质表、纹理路径和引用计数，注入“材质数量与子集属性 ID 不一致或相对纹理路径失效”后复位并重放。
  - **17.3 对X文件的认识**：在“绑定材质”记录顶点索引范围、属性 ID、材质表、纹理路径和引用计数，注入“材质数量与子集属性 ID 不一致或相对纹理路径失效”后复位并重放。
  - **17.4 从3DSMax中导出X文件方法详解**：在“释放资源”记录顶点索引范围、属性 ID、材质表、纹理路径和引用计数，注入“材质数量与子集属性 ID 不一致或相对纹理路径失效”后复位并重放。
  - **17.5 网格模型接口ID3DXMESH**：在“读取网格”记录顶点索引范围、属性 ID、材质表、纹理路径和引用计数，注入“材质数量与子集属性 ID 不一致或相对纹理路径失效”后复位并重放。
  - **17.6 文件模型载入三步曲**：在“校验几何”记录顶点索引范围、属性 ID、材质表、纹理路径和引用计数，注入“材质数量与子集属性 ID 不一致或相对纹理路径失效”后复位并重放。
  - **17.6.1 三步曲之一：通过X文件加载网格模型**：在“解析子集”记录顶点索引范围、属性 ID、材质表、纹理路径和引用计数，注入“材质数量与子集属性 ID 不一致或相对纹理路径失效”后复位并重放。
  - **17.6.2 三步曲之二：载入材质和纹理**：在“绑定材质”记录顶点索引范围、属性 ID、材质表、纹理路径和引用计数，注入“材质数量与子集属性 ID 不一致或相对纹理路径失效”后复位并重放。
  - **17.6.3 三步曲之三：绘制网格模型**：在“释放资源”记录顶点索引范围、属性 ID、材质表、纹理路径和引用计数，注入“材质数量与子集属性 ID 不一致或相对纹理路径失效”后复位并重放。
  - **17.6.4 总结与升华**：在“读取网格”记录顶点索引范围、属性 ID、材质表、纹理路径和引用计数，注入“材质数量与子集属性 ID 不一致或相对纹理路径失效”后复位并重放。
  - **17.7 示例程序D3demo12**：在“校验几何”记录顶点索引范围、属性 ID、材质表、纹理路径和引用计数，注入“材质数量与子集属性 ID 不一致或相对纹理路径失效”后复位并重放。
  - **17.8 章节小憩**：在“解析子集”记录顶点索引范围、属性 ID、材质表、纹理路径和引用计数，注入“材质数量与子集属性 ID 不一致或相对纹理路径失效”后复位并重放。
</Answer>

**问题 3：故障闭环。** 注入“材质数量与子集属性 ID 不一致或相对纹理路径失效”后怎样判定恢复成功？

<Answer>
  保存首错阶段，逆序清理或恢复必要状态，以相同初态和输入重放；只有顶点索引范围、属性 ID、材质表、纹理路径和引用计数回到基线且“每个子集只引用存在且归属明确的材质、纹理和几何范围”再次成立，才算恢复。
</Answer>

</Exercises>

## 术语复核与本章回顾

<Glossary>
  <GlossaryItem term="网格">
    网格在“第17章 他山之石可攻玉——三维游戏模型的载入”中标记输入进入系统时的责任边界。
  </GlossaryItem>
  <GlossaryItem term="子集">
    子集在“第17章 他山之石可攻玉——三维游戏模型的载入”中标记状态被创建、修改与释放的生命周期。
  </GlossaryItem>
  <GlossaryItem term="属性表">
    属性表在“第17章 他山之石可攻玉——三维游戏模型的载入”中标记历史实现与现代迁移之间必须保持的机制。
  </GlossaryItem>
  <GlossaryItem term="材质纹理">
    材质纹理在“第17章 他山之石可攻玉——三维游戏模型的载入”中标记能够复算本章结论的原始证据。
  </GlossaryItem>
</Glossary>

掌握“第17章 他山之石可攻玉——三维游戏模型的载入”意味着能解释解析几何与属性表，逐子集绑定对应材质和纹理并记录资源来源，区分历史坐标与现代迁移，并用顶点索引范围、属性 ID、材质表、纹理路径和引用计数推翻或保留实现。

## 阅读导航

[← 上一页：第16章 起舞不落幕——与纹理映射的华丽邂逅](/learn/windows-journey/04-directx-foundation/wj-16-texture-mapping) · [下一页：第18章 水乳交融的艺术——Alpha混合技术 →](/learn/windows-journey/04-directx-foundation/wj-18-alpha-blending)

<Attribution
  mode="independent-rewrite"
  sourceBasis="outline-only"
  workTitle="毛星云《逐梦旅程：Windows游戏编程之从零开始》（清华大学出版社，2013）"
  adaptedUrl="https://book.douban.com/subject/25756435/"
/>
