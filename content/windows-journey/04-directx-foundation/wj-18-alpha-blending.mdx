---
title: 第18章 水乳交融的艺术——Alpha混合技术
type: C
section: 第三篇 · DirectX游戏编程基础
order: 18
description: 第18章 水乳交融的艺术——Alpha混合技术：用源色、目标色和混合因子计算透明结果并解释绘制顺序，以历史—现代对照、故障恢复和逐节点证据验收。
demo: true
math: false
sourceUrl: 'https://book.douban.com/subject/25756435/'
draft: false
qualityVersion: 2
practiceMode: calculation
sourceMode: independent-rewrite
---

# 第18章 水乳交融的艺术——Alpha混合技术


{/* WINDOWS_JOURNEY_QUALITY_V2 */}

## 问题先于 API

透明度值正确但混合因子或排序错误仍会产生光晕和遮挡。本章先保存问题、初态和输入，再讨论 API；通过条件是“每个透明像素的源、目标、因子和绘制顺序都有记录”，不是画面偶然出现一次。

## 来源范围、技术年代与独立重写

公开[完整目录](https://book.douban.com/subject/25756435/)只用于界定本页题目和层级，[出版记录](https://www.sinobook.com.cn/b2c/scrp/bookdetail.cfm?iBookNo=646210)与[书目记录](https://books.google.com/books/about/%E9%80%90%E6%A2%A6%E6%97%85%E7%A8%8B.html?id=6O-F0AEACAAJ)用于交叉核对 ISBN、出版社和年代；没有把目录核对表述成正文忠实。本页关于 Windows/DirectX 的机制由[Microsoft 一手文档](https://learn.microsoft.com/en-us/windows/win32/direct3d9/frame-buffer-alpha)核验，以下中文讲解、图示、实验、代码和练习均为独立教学重写。

原书处于 Visual Studio 2010、Win32/GDI、DirectX 9 与固定功能管线语境。现代 API 只作迁移对照：D3D9 Alpha 状态和管线混合状态名称可以不同，但必须消费相同输入并守住同一不变量。

## 机制与术语

、、、。四个术语共同约束“用源色、目标色和混合因子计算透明结果并解释绘制顺序”，结论必须回到源目标 RGBA、混合状态、深度状态、排序键和最终像素。

## 正式目录逐项深读

### 第18章 水乳交融的艺术——Alpha混合技术

围绕用源色、目标色和混合因子计算透明结果并解释绘制顺序，对 第18章 水乳交融的艺术——Alpha混合技术，必须给出可复算中间量，先在纸面预测空间或像素结果，再用源目标 RGBA、混合状态、深度状态、排序键和最终像素查找首个数值分叉。

第18章 水乳交融的艺术——Alpha混合技术 进入“准备源色”阶段时，先标出输入、状态拥有者和可见输出，再解释按最终色等于源色乘源因子加目标色乘目标因子复算像素。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 第18章 水乳交融的艺术——Alpha混合技术 时固定其余条件，记录源目标 RGBA、混合状态、深度状态、排序键和最终像素，随后注入“半透明对象开启深度写入或按近到远顺序绘制”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 18.1 初识Alpha通道与混合技术

围绕用源色、目标色和混合因子计算透明结果并解释绘制顺序，对 18.1 初识Alpha通道与混合技术，必须给出可复算中间量，先在纸面预测空间或像素结果，再用源目标 RGBA、混合状态、深度状态、排序键和最终像素查找首个数值分叉。

18.1 初识Alpha通道与混合技术 进入“读取目标色”阶段时，先标出输入、状态拥有者和可见输出，再解释按最终色等于源色乘源因子加目标色乘目标因子复算像素。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 18.1 初识Alpha通道与混合技术 时固定其余条件，记录源目标 RGBA、混合状态、深度状态、排序键和最终像素，随后注入“半透明对象开启深度写入或按近到远顺序绘制”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 18.2 Direct3D中的融合套路——融合因子

围绕用源色、目标色和混合因子计算透明结果并解释绘制顺序，对 18.2 Direct3D中的融合套路——融合因子，API 名称属于具体年代；学习重点是输入、返回值、所有权和失败出口，技术事实以微软文档核对。

18.2 Direct3D中的融合套路——融合因子 进入“选择因子”阶段时，先标出输入、状态拥有者和可见输出，再解释按最终色等于源色乘源因子加目标色乘目标因子复算像素。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 18.2 Direct3D中的融合套路——融合因子 时固定其余条件，记录源目标 RGBA、混合状态、深度状态、排序键和最终像素，随后注入“半透明对象开启深度写入或按近到远顺序绘制”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 18.3 融合运算方式的取法

围绕用源色、目标色和混合因子计算透明结果并解释绘制顺序，18.3 融合运算方式的取法 限定本章的一个知识坐标；独立解释围绕“按最终色等于源色乘源因子加目标色乘目标因子复算像素”展开，并以“每个透明像素的源、目标、因子和绘制顺序都有记录”结束。

18.3 融合运算方式的取法 进入“执行混合”阶段时，先标出输入、状态拥有者和可见输出，再解释按最终色等于源色乘源因子加目标色乘目标因子复算像素。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 18.3 融合运算方式的取法 时固定其余条件，记录源目标 RGBA、混合状态、深度状态、排序键和最终像素，随后注入“半透明对象开启深度写入或按近到远顺序绘制”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 18.4 融合因子的取法

围绕用源色、目标色和混合因子计算透明结果并解释绘制顺序，18.4 融合因子的取法 限定本章的一个知识坐标；独立解释围绕“按最终色等于源色乘源因子加目标色乘目标因子复算像素”展开，并以“每个透明像素的源、目标、因子和绘制顺序都有记录”结束。

18.4 融合因子的取法 进入“验证排序”阶段时，先标出输入、状态拥有者和可见输出，再解释按最终色等于源色乘源因子加目标色乘目标因子复算像素。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 18.4 融合因子的取法 时固定其余条件，记录源目标 RGBA、混合状态、深度状态、排序键和最终像素，随后注入“半透明对象开启深度写入或按近到远顺序绘制”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 18.5 Alpha的三处来源

围绕用源色、目标色和混合因子计算透明结果并解释绘制顺序，对 18.5 Alpha的三处来源，必须给出可复算中间量，先在纸面预测空间或像素结果，再用源目标 RGBA、混合状态、深度状态、排序键和最终像素查找首个数值分叉。

18.5 Alpha的三处来源 进入“准备源色”阶段时，先标出输入、状态拥有者和可见输出，再解释按最终色等于源色乘源因子加目标色乘目标因子复算像素。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 18.5 Alpha的三处来源 时固定其余条件，记录源目标 RGBA、混合状态、深度状态、排序键和最终像素，随后注入“半透明对象开启深度写入或按近到远顺序绘制”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 18.6 Alpha融合使用三步曲

围绕用源色、目标色和混合因子计算透明结果并解释绘制顺序，对 18.6 Alpha融合使用三步曲，必须给出可复算中间量，先在纸面预测空间或像素结果，再用源目标 RGBA、混合状态、深度状态、排序键和最终像素查找首个数值分叉。

18.6 Alpha融合使用三步曲 进入“读取目标色”阶段时，先标出输入、状态拥有者和可见输出，再解释按最终色等于源色乘源因子加目标色乘目标因子复算像素。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 18.6 Alpha融合使用三步曲 时固定其余条件，记录源目标 RGBA、混合状态、深度状态、排序键和最终像素，随后注入“半透明对象开启深度写入或按近到远顺序绘制”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 18.7 示例程序D3Ddemo13

围绕用源色、目标色和混合因子计算透明结果并解释绘制顺序，对 18.7 示例程序D3Ddemo13，把前述机制装入一个可运行场景：固定构建、资源和输入，仅改变一个条件，并保留源目标 RGBA、混合状态、深度状态、排序键和最终像素。

18.7 示例程序D3Ddemo13 进入“选择因子”阶段时，先标出输入、状态拥有者和可见输出，再解释按最终色等于源色乘源因子加目标色乘目标因子复算像素。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 18.7 示例程序D3Ddemo13 时固定其余条件，记录源目标 RGBA、混合状态、深度状态、排序键和最终像素，随后注入“半透明对象开启深度写入或按近到远顺序绘制”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 18.8 章节小憩

围绕用源色、目标色和混合因子计算透明结果并解释绘制顺序，对 18.8 章节小憩，收尾不是装饰，而是要求用源目标 RGBA、混合状态、深度状态、排序键和最终像素复盘“每个透明像素的源、目标、因子和绘制顺序都有记录”是否在正常和失败路径同时成立。

18.8 章节小憩 进入“执行混合”阶段时，先标出输入、状态拥有者和可见输出，再解释按最终色等于源色乘源因子加目标色乘目标因子复算像素。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 18.8 章节小憩 时固定其余条件，记录源目标 RGBA、混合状态、深度状态、排序键和最终像素，随后注入“半透明对象开启深度写入或按近到远顺序绘制”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

## 可重放实现或计算骨架

```text
initial = fixed_state + fixed_input + fixed_timestep
historical = run(initial, "D3D9 Alpha 状态")
modern = run(initial, "管线混合状态")
compare(historical, modern, "颜色差")
pass = "每个透明像素的源、目标、因子和绘制顺序都有记录"
```

真实程序还要固定编译器、SDK、窗口尺寸、资源、随机种子、时间步和输入记录；否则历史实现与现代迁移不能公平比较。


## 练习与 4 级证据矩阵


## 术语复核与本章回顾


掌握“第18章 水乳交融的艺术——Alpha混合技术”意味着能解释按最终色等于源色乘源因子加目标色乘目标因子复算像素，区分历史坐标与现代迁移，并用源目标 RGBA、混合状态、深度状态、排序键和最终像素推翻或保留实现。

## 阅读导航

[← 上一页：第17章 他山之石可攻玉——三维游戏模型的载入](/learn/windows-journey/04-directx-foundation/wj-17-mesh-loading) · [下一页：第19章 横看成岭侧成峰——深度测试与Z缓存 →](/learn/windows-journey/04-directx-foundation/wj-19-depth-z-buffer)

