---
title: 第19章 横看成岭侧成峰——深度测试与Z缓存
type: C
section: 第三篇 · DirectX游戏编程基础
order: 19
description: 第19章 横看成岭侧成峰——深度测试与Z缓存：用深度测试函数、写入开关和近远平面解释遮挡与精度，以历史—现代对照、故障恢复和逐节点证据验收。
demo: true
math: false
sourceUrl: 'https://book.douban.com/subject/25756435/'
draft: false
qualityVersion: 2
practiceMode: calculation
sourceMode: independent-rewrite
---
import {
  Wj19DepthZBufferMapLab,
  Wj19DepthZBufferExperimentLab,
  Wj19DepthZBufferEvidenceLab,
} from "@/components/mdx/windows-journey/diagrams/wj-19-depth-z-buffer";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

# 第19章 横看成岭侧成峰——深度测试与Z缓存

<Objectives>

- 能从“开启 Z 缓冲仍可能因清除、比较、写入或投影精度错误而穿插”建立本章的历史问题基线。
- 能解释“光栅化像素与已存深度比较，通过后按状态更新颜色和深度”的输入、状态、所有权和可见结果。
- 能操作专属实验，对照D3D9 深度状态与现代深度附件，复算遮挡错误。
- 能注入“每帧未清深度或近远平面比过大造成 Z fighting”，用深度值、比较函数、写掩码、清除值和投影参数定位首错、恢复并重放。

</Objectives>

{/* WINDOWS_JOURNEY_QUALITY_V2 */}

## 问题先于 API

开启 Z 缓冲仍可能因清除、比较、写入或投影精度错误而穿插。本章先保存问题、初态和输入，再讨论 API；通过条件是“每个争议像素的候选深度、旧深度、比较结果和写入结果可复算”，不是画面偶然出现一次。

## 来源范围、技术年代与独立重写

公开[完整目录](https://book.douban.com/subject/25756435/)只用于界定本页题目和层级，[出版记录](https://www.sinobook.com.cn/b2c/scrp/bookdetail.cfm?iBookNo=646210)与[书目记录](https://books.google.com/books/about/%E9%80%90%E6%A2%A6%E6%97%85%E7%A8%8B.html?id=6O-F0AEACAAJ)用于交叉核对 ISBN、出版社和年代；没有把目录核对表述成正文忠实。本页关于 Windows/DirectX 的机制由[Microsoft 一手文档](https://learn.microsoft.com/en-us/windows/win32/direct3d9/depth-buffers)核验，以下中文讲解、图示、实验、代码和练习均为独立教学重写。

原书处于 Visual Studio 2010、Win32/GDI、DirectX 9 与固定功能管线语境。现代 API 只作迁移对照：D3D9 深度状态和现代深度附件名称可以不同，但必须消费相同输入并守住同一不变量。

## 机制与术语

<Term def="Z 缓冲在“第19章 横看成岭侧成峰——深度测试与Z缓存”中标记输入进入系统时的责任边界。">Z 缓冲</Term>、<Term def="深度测试在“第19章 横看成岭侧成峰——深度测试与Z缓存”中标记状态被创建、修改与释放的生命周期。">深度测试</Term>、<Term def="深度写入在“第19章 横看成岭侧成峰——深度测试与Z缓存”中标记历史实现与现代迁移之间必须保持的机制。">深度写入</Term>、<Term def="Z fighting在“第19章 横看成岭侧成峰——深度测试与Z缓存”中标记能够复算本章结论的原始证据。">Z fighting</Term>。四个术语共同约束“用深度测试函数、写入开关和近远平面解释遮挡与精度”，结论必须回到深度值、比较函数、写掩码、清除值和投影参数。

## 先预测，再操作三层专属实验

操作前写下预测：哪一阶段先改变遮挡错误；压力扩大四倍时哪条边界先失效；注入“每帧未清深度或近远平面比过大造成 Z fighting”后怎样回到初态。

<Stepper>
  <Step title="1. 正式节点与状态链">
    选择目录节点和机制阶段，检查输入、状态拥有者、可见结果与验证证据。

    <Wj19DepthZBufferMapLab />

  </Step>
  <Step title="2. 历史实现与现代迁移">
    一次只改变技术坐标、场景负载或故障之一，读取可复算结果。

    <Wj19DepthZBufferExperimentLab />

  </Step>
  <Step title="3. 故障、恢复与同输入重放">
    比较正常、故障和恢复轨迹；最后点击重置，确认所有控制和状态回到初值。

    <Wj19DepthZBufferEvidenceLab />

  </Step>
</Stepper>

## 正式目录逐项深读

### 第19章 横看成岭侧成峰——深度测试与Z缓存

围绕用深度测试函数、写入开关和近远平面解释遮挡与精度，对 第19章 横看成岭侧成峰——深度测试与Z缓存，必须给出可复算中间量，先在纸面预测空间或像素结果，再用深度值、比较函数、写掩码、清除值和投影参数查找首个数值分叉。

第19章 横看成岭侧成峰——深度测试与Z缓存 进入“清除深度”阶段时，先标出输入、状态拥有者和可见输出，再解释光栅化像素与已存深度比较，通过后按状态更新颜色和深度。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 第19章 横看成岭侧成峰——深度测试与Z缓存 时固定其余条件，记录深度值、比较函数、写掩码、清除值和投影参数，随后注入“每帧未清深度或近远平面比过大造成 Z fighting”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 19.1 形象化理解深度测试

围绕用深度测试函数、写入开关和近远平面解释遮挡与精度，对 19.1 形象化理解深度测试，必须给出可复算中间量，先在纸面预测空间或像素结果，再用深度值、比较函数、写掩码、清除值和投影参数查找首个数值分叉。

19.1 形象化理解深度测试 进入“生成深度”阶段时，先标出输入、状态拥有者和可见输出，再解释光栅化像素与已存深度比较，通过后按状态更新颜色和深度。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 19.1 形象化理解深度测试 时固定其余条件，记录深度值、比较函数、写掩码、清除值和投影参数，随后注入“每帧未清深度或近远平面比过大造成 Z fighting”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 19.2 深度测试相关概念讲解

围绕用深度测试函数、写入开关和近远平面解释遮挡与精度，对 19.2 深度测试相关概念讲解，必须给出可复算中间量，先在纸面预测空间或像素结果，再用深度值、比较函数、写掩码、清除值和投影参数查找首个数值分叉。

19.2 深度测试相关概念讲解 进入“执行比较”阶段时，先标出输入、状态拥有者和可见输出，再解释光栅化像素与已存深度比较，通过后按状态更新颜色和深度。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 19.2 深度测试相关概念讲解 时固定其余条件，记录深度值、比较函数、写掩码、清除值和投影参数，随后注入“每帧未清深度或近远平面比过大造成 Z fighting”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 19.3 深度测试使用四步曲

围绕用深度测试函数、写入开关和近远平面解释遮挡与精度，对 19.3 深度测试使用四步曲，必须给出可复算中间量，先在纸面预测空间或像素结果，再用深度值、比较函数、写掩码、清除值和投影参数查找首个数值分叉。

19.3 深度测试使用四步曲 进入“更新像素”阶段时，先标出输入、状态拥有者和可见输出，再解释光栅化像素与已存深度比较，通过后按状态更新颜色和深度。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 19.3 深度测试使用四步曲 时固定其余条件，记录深度值、比较函数、写掩码、清除值和投影参数，随后注入“每帧未清深度或近远平面比过大造成 Z fighting”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 19.4 示例程序D3Ddemo14

围绕用深度测试函数、写入开关和近远平面解释遮挡与精度，对 19.4 示例程序D3Ddemo14，把前述机制装入一个可运行场景：固定构建、资源和输入，仅改变一个条件，并保留深度值、比较函数、写掩码、清除值和投影参数。

19.4 示例程序D3Ddemo14 进入“检查精度”阶段时，先标出输入、状态拥有者和可见输出，再解释光栅化像素与已存深度比较，通过后按状态更新颜色和深度。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 19.4 示例程序D3Ddemo14 时固定其余条件，记录深度值、比较函数、写掩码、清除值和投影参数，随后注入“每帧未清深度或近远平面比过大造成 Z fighting”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 19.5 章节小憩

围绕用深度测试函数、写入开关和近远平面解释遮挡与精度，对 19.5 章节小憩，收尾不是装饰，而是要求用深度值、比较函数、写掩码、清除值和投影参数复盘“每个争议像素的候选深度、旧深度、比较结果和写入结果可复算”是否在正常和失败路径同时成立。

19.5 章节小憩 进入“清除深度”阶段时，先标出输入、状态拥有者和可见输出，再解释光栅化像素与已存深度比较，通过后按状态更新颜色和深度。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 19.5 章节小憩 时固定其余条件，记录深度值、比较函数、写掩码、清除值和投影参数，随后注入“每帧未清深度或近远平面比过大造成 Z fighting”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

## 可重放实现或计算骨架

```text
initial = fixed_state + fixed_input + fixed_timestep
historical = run(initial, "D3D9 深度状态")
modern = run(initial, "现代深度附件")
compare(historical, modern, "遮挡错误")
pass = "每个争议像素的候选深度、旧深度、比较结果和写入结果可复算"
```

真实程序还要固定编译器、SDK、窗口尺寸、资源、随机种子、时间步和输入记录；否则历史实现与现代迁移不能公平比较。

<Callout type="trap" title="把历史 API 当成当前默认方案">
  本章保留 D3D9 深度状态 的教学身份，但新项目必须核对平台支持并解释为何选择 现代深度附件 或其他方案。
</Callout>

<Callout type="trap" title="只看最终画面">
  第19章 横看成岭侧成峰——深度测试与Z缓存必须说明清除深度、生成深度、执行比较、更新像素、检查精度的先后关系；最终像素相同不代表消息、资源、矩阵或状态安全。
</Callout>

<Callout type="trap" title="失败后继续运行">
  注入“每帧未清深度或近远平面比过大造成 Z fighting”后应停在第一个因果分叉；若深度值、比较函数、写掩码、清除值和投影参数无法解释并复位，当前实现失败。
</Callout>

## 练习与 4 级证据矩阵

<Exercises>

**问题 1：历史边界。** 怎样避免把 2013 年接口示例误报成今天的默认架构？

<Answer>
  分别记录D3D9 深度状态与现代深度附件的版本、责任和资源模型；只迁移机制，不改写历史事实，并以[Microsoft 文档](https://learn.microsoft.com/en-us/windows/win32/direct3d9/depth-buffers)核对接口身份。
</Answer>

**问题 2：逐节点验证。** 正式目录节点怎样从“出现”升级到可复查证据？

<Answer>
  - **第19章 横看成岭侧成峰——深度测试与Z缓存**：在“清除深度”记录深度值、比较函数、写掩码、清除值和投影参数，注入“每帧未清深度或近远平面比过大造成 Z fighting”后复位并重放。
  - **19.1 形象化理解深度测试**：在“生成深度”记录深度值、比较函数、写掩码、清除值和投影参数，注入“每帧未清深度或近远平面比过大造成 Z fighting”后复位并重放。
  - **19.2 深度测试相关概念讲解**：在“执行比较”记录深度值、比较函数、写掩码、清除值和投影参数，注入“每帧未清深度或近远平面比过大造成 Z fighting”后复位并重放。
  - **19.3 深度测试使用四步曲**：在“更新像素”记录深度值、比较函数、写掩码、清除值和投影参数，注入“每帧未清深度或近远平面比过大造成 Z fighting”后复位并重放。
  - **19.4 示例程序D3Ddemo14**：在“检查精度”记录深度值、比较函数、写掩码、清除值和投影参数，注入“每帧未清深度或近远平面比过大造成 Z fighting”后复位并重放。
  - **19.5 章节小憩**：在“清除深度”记录深度值、比较函数、写掩码、清除值和投影参数，注入“每帧未清深度或近远平面比过大造成 Z fighting”后复位并重放。
</Answer>

**问题 3：故障闭环。** 注入“每帧未清深度或近远平面比过大造成 Z fighting”后怎样判定恢复成功？

<Answer>
  保存首错阶段，逆序清理或恢复必要状态，以相同初态和输入重放；只有深度值、比较函数、写掩码、清除值和投影参数回到基线且“每个争议像素的候选深度、旧深度、比较结果和写入结果可复算”再次成立，才算恢复。
</Answer>

</Exercises>

## 术语复核与本章回顾

<Glossary>
  <GlossaryItem term="Z 缓冲">
    Z 缓冲在“第19章 横看成岭侧成峰——深度测试与Z缓存”中标记输入进入系统时的责任边界。
  </GlossaryItem>
  <GlossaryItem term="深度测试">
    深度测试在“第19章 横看成岭侧成峰——深度测试与Z缓存”中标记状态被创建、修改与释放的生命周期。
  </GlossaryItem>
  <GlossaryItem term="深度写入">
    深度写入在“第19章 横看成岭侧成峰——深度测试与Z缓存”中标记历史实现与现代迁移之间必须保持的机制。
  </GlossaryItem>
  <GlossaryItem term="Z fighting">
    Z fighting在“第19章 横看成岭侧成峰——深度测试与Z缓存”中标记能够复算本章结论的原始证据。
  </GlossaryItem>
</Glossary>

掌握“第19章 横看成岭侧成峰——深度测试与Z缓存”意味着能解释光栅化像素与已存深度比较，通过后按状态更新颜色和深度，区分历史坐标与现代迁移，并用深度值、比较函数、写掩码、清除值和投影参数推翻或保留实现。

## 阅读导航

[← 上一页：第18章 水乳交融的艺术——Alpha混合技术](/learn/windows-journey/04-directx-foundation/wj-18-alpha-blending) · [下一页：第20章 虚实结合的光影——模板技术 →](/learn/windows-journey/04-directx-foundation/wj-20-stencil-techniques)

<Attribution
  mode="independent-rewrite"
  sourceBasis="outline-only"
  workTitle="毛星云《逐梦旅程：Windows游戏编程之从零开始》（清华大学出版社，2013）"
  adaptedUrl="https://book.douban.com/subject/25756435/"
/>
