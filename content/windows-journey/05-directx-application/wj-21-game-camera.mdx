---
title: 第21章 翱翔于三维空间——游戏摄像机的构建
type: C
section: 第四篇 · DirectX游戏编程应用
order: 21
description: 第21章 翱翔于三维空间——游戏摄像机的构建：由位置、朝向、上向量和视锥参数构造稳定观察与投影矩阵，以历史—现代对照、故障恢复和逐节点证据验收。
demo: true
math: false
sourceUrl: 'https://book.douban.com/subject/25756435/'
draft: true
qualityVersion: 2
practiceMode: calculation
sourceMode: independent-rewrite
---
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

# 第21章 翱翔于三维空间——游戏摄像机的构建

<Objectives>

- 能从“直接累加欧拉角或混用坐标手性会产生翻转、漂移和视锥错误”建立本章的历史问题基线。
- 能解释“维护正交相机基向量，将世界反向变换到相机空间后再投影”的输入、状态、所有权和可见结果。
- 能操作专属实验，对照D3DX 相机矩阵与显式相机组件，复算方向误差。
- 能注入“朝向与上向量共线或旋转后未重新正交化”，用相机基向量、观察矩阵、视锥参数和测试点坐标定位首错、恢复并重放。

</Objectives>

{/* WINDOWS_JOURNEY_QUALITY_V2 */}

## 问题先于 API

直接累加欧拉角或混用坐标手性会产生翻转、漂移和视锥错误。本章先保存问题、初态和输入，再讨论 API；通过条件是“相机基向量正交归一，测试点的观察坐标和屏幕方向可复算”，不是画面偶然出现一次。

## 来源范围、技术年代与独立重写

公开[完整目录](https://book.douban.com/subject/25756435/)只用于界定本页题目和层级，[出版记录](https://www.sinobook.com.cn/b2c/scrp/bookdetail.cfm?iBookNo=646210)与[书目记录](https://books.google.com/books/about/%E9%80%90%E6%A2%A6%E6%97%85%E7%A8%8B.html?id=6O-F0AEACAAJ)用于交叉核对 ISBN、出版社和年代；没有把目录核对表述成正文忠实。本页关于 Windows/DirectX 的机制由[Microsoft 一手文档](https://learn.microsoft.com/en-us/windows/win32/direct3d9/view-transform)核验，以下中文讲解、图示、实验、代码和练习均为独立教学重写。

原书处于 Visual Studio 2010、Win32/GDI、DirectX 9 与固定功能管线语境。现代 API 只作迁移对照：D3DX 相机矩阵和显式相机组件名称可以不同，但必须消费相同输入并守住同一不变量。

## 机制与术语

<Term def="相机基在“第21章 翱翔于三维空间——游戏摄像机的构建”中标记输入进入系统时的责任边界。">相机基</Term>、<Term def="观察矩阵在“第21章 翱翔于三维空间——游戏摄像机的构建”中标记状态被创建、修改与释放的生命周期。">观察矩阵</Term>、<Term def="视锥在“第21章 翱翔于三维空间——游戏摄像机的构建”中标记历史实现与现代迁移之间必须保持的机制。">视锥</Term>、<Term def="投影参数在“第21章 翱翔于三维空间——游戏摄像机的构建”中标记能够复算本章结论的原始证据。">投影参数</Term>。四个术语共同约束“由位置、朝向、上向量和视锥参数构造稳定观察与投影矩阵”，结论必须回到相机基向量、观察矩阵、视锥参数和测试点坐标。

## 正式目录逐项深读

### 第21章 翱翔于三维空间——游戏摄像机的构建

围绕由位置、朝向、上向量和视锥参数构造稳定观察与投影矩阵，对 第21章 翱翔于三维空间——游戏摄像机的构建，把前述机制装入一个可运行场景：固定构建、资源和输入，仅改变一个条件，并保留相机基向量、观察矩阵、视锥参数和测试点坐标。

第21章 翱翔于三维空间——游戏摄像机的构建 进入“更新姿态”阶段时，先标出输入、状态拥有者和可见输出，再解释维护正交相机基向量，将世界反向变换到相机空间后再投影。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 第21章 翱翔于三维空间——游戏摄像机的构建 时固定其余条件，记录相机基向量、观察矩阵、视锥参数和测试点坐标，随后注入“朝向与上向量共线或旋转后未重新正交化”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 21.1 对摄像机的一些概述

围绕由位置、朝向、上向量和视锥参数构造稳定观察与投影矩阵，21.1 对摄像机的一些概述 限定本章的一个知识坐标；独立解释围绕“维护正交相机基向量，将世界反向变换到相机空间后再投影”展开，并以“相机基向量正交归一，测试点的观察坐标和屏幕方向可复算”结束。

21.1 对摄像机的一些概述 进入“正交基向量”阶段时，先标出输入、状态拥有者和可见输出，再解释维护正交相机基向量，将世界反向变换到相机空间后再投影。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 21.1 对摄像机的一些概述 时固定其余条件，记录相机基向量、观察矩阵、视锥参数和测试点坐标，随后注入“朝向与上向量共线或旋转后未重新正交化”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 21.2 开始设计摄像机类

围绕由位置、朝向、上向量和视锥参数构造稳定观察与投影矩阵，21.2 开始设计摄像机类 限定本章的一个知识坐标；独立解释围绕“维护正交相机基向量，将世界反向变换到相机空间后再投影”展开，并以“相机基向量正交归一，测试点的观察坐标和屏幕方向可复算”结束。

21.2 开始设计摄像机类 进入“构造观察”阶段时，先标出输入、状态拥有者和可见输出，再解释维护正交相机基向量，将世界反向变换到相机空间后再投影。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 21.2 开始设计摄像机类 时固定其余条件，记录相机基向量、观察矩阵、视锥参数和测试点坐标，随后注入“朝向与上向量共线或旋转后未重新正交化”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 21.3 关于向量计算的函数讲解

围绕由位置、朝向、上向量和视锥参数构造稳定观察与投影矩阵，对 21.3 关于向量计算的函数讲解，API 名称属于具体年代；学习重点是输入、返回值、所有权和失败出口，技术事实以微软文档核对。

21.3 关于向量计算的函数讲解 进入“构造投影”阶段时，先标出输入、状态拥有者和可见输出，再解释维护正交相机基向量，将世界反向变换到相机空间后再投影。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 21.3 关于向量计算的函数讲解 时固定其余条件，记录相机基向量、观察矩阵、视锥参数和测试点坐标，随后注入“朝向与上向量共线或旋转后未重新正交化”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 21.4 计算取景变换矩阵

围绕由位置、朝向、上向量和视锥参数构造稳定观察与投影矩阵，对 21.4 计算取景变换矩阵，必须给出可复算中间量，先在纸面预测空间或像素结果，再用相机基向量、观察矩阵、视锥参数和测试点坐标查找首个数值分叉。

21.4 计算取景变换矩阵 进入“验证视锥”阶段时，先标出输入、状态拥有者和可见输出，再解释维护正交相机基向量，将世界反向变换到相机空间后再投影。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 21.4 计算取景变换矩阵 时固定其余条件，记录相机基向量、观察矩阵、视锥参数和测试点坐标，随后注入“朝向与上向量共线或旋转后未重新正交化”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 21.5 类的其余实现细节

围绕由位置、朝向、上向量和视锥参数构造稳定观察与投影矩阵，21.5 类的其余实现细节 限定本章的一个知识坐标；独立解释围绕“维护正交相机基向量，将世界反向变换到相机空间后再投影”展开，并以“相机基向量正交归一，测试点的观察坐标和屏幕方向可复算”结束。

21.5 类的其余实现细节 进入“更新姿态”阶段时，先标出输入、状态拥有者和可见输出，再解释维护正交相机基向量，将世界反向变换到相机空间后再投影。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 21.5 类的其余实现细节 时固定其余条件，记录相机基向量、观察矩阵、视锥参数和测试点坐标，随后注入“朝向与上向量共线或旋转后未重新正交化”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 21.6 示例程序D3Ddemo16

围绕由位置、朝向、上向量和视锥参数构造稳定观察与投影矩阵，对 21.6 示例程序D3Ddemo16，把前述机制装入一个可运行场景：固定构建、资源和输入，仅改变一个条件，并保留相机基向量、观察矩阵、视锥参数和测试点坐标。

21.6 示例程序D3Ddemo16 进入“正交基向量”阶段时，先标出输入、状态拥有者和可见输出，再解释维护正交相机基向量，将世界反向变换到相机空间后再投影。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 21.6 示例程序D3Ddemo16 时固定其余条件，记录相机基向量、观察矩阵、视锥参数和测试点坐标，随后注入“朝向与上向量共线或旋转后未重新正交化”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 21.7 章节小憩

围绕由位置、朝向、上向量和视锥参数构造稳定观察与投影矩阵，对 21.7 章节小憩，收尾不是装饰，而是要求用相机基向量、观察矩阵、视锥参数和测试点坐标复盘“相机基向量正交归一，测试点的观察坐标和屏幕方向可复算”是否在正常和失败路径同时成立。

21.7 章节小憩 进入“构造观察”阶段时，先标出输入、状态拥有者和可见输出，再解释维护正交相机基向量，将世界反向变换到相机空间后再投影。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 21.7 章节小憩 时固定其余条件，记录相机基向量、观察矩阵、视锥参数和测试点坐标，随后注入“朝向与上向量共线或旋转后未重新正交化”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

## 可重放实现或计算骨架

```text
initial = fixed_state + fixed_input + fixed_timestep
historical = run(initial, "D3DX 相机矩阵")
modern = run(initial, "显式相机组件")
compare(historical, modern, "方向误差")
pass = "相机基向量正交归一，测试点的观察坐标和屏幕方向可复算"
```

真实程序还要固定编译器、SDK、窗口尺寸、资源、随机种子、时间步和输入记录；否则历史实现与现代迁移不能公平比较。

<Callout type="trap" title="把历史 API 当成当前默认方案">
  本章保留 D3DX 相机矩阵 的教学身份，但新项目必须核对平台支持并解释为何选择 显式相机组件 或其他方案。
</Callout>

<Callout type="trap" title="只看最终画面">
  第21章 翱翔于三维空间——游戏摄像机的构建必须说明更新姿态、正交基向量、构造观察、构造投影、验证视锥的先后关系；最终像素相同不代表消息、资源、矩阵或状态安全。
</Callout>

<Callout type="trap" title="失败后继续运行">
  注入“朝向与上向量共线或旋转后未重新正交化”后应停在第一个因果分叉；若相机基向量、观察矩阵、视锥参数和测试点坐标无法解释并复位，当前实现失败。
</Callout>

## 练习与 4 级证据矩阵

<Exercises>

**问题 1：历史边界。** 怎样避免把 2013 年接口示例误报成今天的默认架构？

<Answer>
  分别记录D3DX 相机矩阵与显式相机组件的版本、责任和资源模型；只迁移机制，不改写历史事实，并以[Microsoft 文档](https://learn.microsoft.com/en-us/windows/win32/direct3d9/view-transform)核对接口身份。
</Answer>

**问题 2：逐节点验证。** 正式目录节点怎样从“出现”升级到可复查证据？

<Answer>
  - **第21章 翱翔于三维空间——游戏摄像机的构建**：在“更新姿态”记录相机基向量、观察矩阵、视锥参数和测试点坐标，注入“朝向与上向量共线或旋转后未重新正交化”后复位并重放。
  - **21.1 对摄像机的一些概述**：在“正交基向量”记录相机基向量、观察矩阵、视锥参数和测试点坐标，注入“朝向与上向量共线或旋转后未重新正交化”后复位并重放。
  - **21.2 开始设计摄像机类**：在“构造观察”记录相机基向量、观察矩阵、视锥参数和测试点坐标，注入“朝向与上向量共线或旋转后未重新正交化”后复位并重放。
  - **21.3 关于向量计算的函数讲解**：在“构造投影”记录相机基向量、观察矩阵、视锥参数和测试点坐标，注入“朝向与上向量共线或旋转后未重新正交化”后复位并重放。
  - **21.4 计算取景变换矩阵**：在“验证视锥”记录相机基向量、观察矩阵、视锥参数和测试点坐标，注入“朝向与上向量共线或旋转后未重新正交化”后复位并重放。
  - **21.5 类的其余实现细节**：在“更新姿态”记录相机基向量、观察矩阵、视锥参数和测试点坐标，注入“朝向与上向量共线或旋转后未重新正交化”后复位并重放。
  - **21.6 示例程序D3Ddemo16**：在“正交基向量”记录相机基向量、观察矩阵、视锥参数和测试点坐标，注入“朝向与上向量共线或旋转后未重新正交化”后复位并重放。
  - **21.7 章节小憩**：在“构造观察”记录相机基向量、观察矩阵、视锥参数和测试点坐标，注入“朝向与上向量共线或旋转后未重新正交化”后复位并重放。
</Answer>

**问题 3：故障闭环。** 注入“朝向与上向量共线或旋转后未重新正交化”后怎样判定恢复成功？

<Answer>
  保存首错阶段，逆序清理或恢复必要状态，以相同初态和输入重放；只有相机基向量、观察矩阵、视锥参数和测试点坐标回到基线且“相机基向量正交归一，测试点的观察坐标和屏幕方向可复算”再次成立，才算恢复。
</Answer>

</Exercises>

## 术语复核与本章回顾

<Glossary>
  <GlossaryItem term="相机基">
    相机基在“第21章 翱翔于三维空间——游戏摄像机的构建”中标记输入进入系统时的责任边界。
  </GlossaryItem>
  <GlossaryItem term="观察矩阵">
    观察矩阵在“第21章 翱翔于三维空间——游戏摄像机的构建”中标记状态被创建、修改与释放的生命周期。
  </GlossaryItem>
  <GlossaryItem term="视锥">
    视锥在“第21章 翱翔于三维空间——游戏摄像机的构建”中标记历史实现与现代迁移之间必须保持的机制。
  </GlossaryItem>
  <GlossaryItem term="投影参数">
    投影参数在“第21章 翱翔于三维空间——游戏摄像机的构建”中标记能够复算本章结论的原始证据。
  </GlossaryItem>
</Glossary>

掌握“第21章 翱翔于三维空间——游戏摄像机的构建”意味着能解释维护正交相机基向量，将世界反向变换到相机空间后再投影，区分历史坐标与现代迁移，并用相机基向量、观察矩阵、视锥参数和测试点坐标推翻或保留实现。

## 阅读导航

[← 上一页：第20章 虚实结合的光影——模板技术](/learn/windows-journey/04-directx-foundation/wj-20-stencil-techniques) · [下一页：第22章 钟灵毓秀的世界——三维地形的构建 →](/learn/windows-journey/05-directx-application/wj-22-terrain)

<Attribution
  mode="independent-rewrite"
  sourceBasis="outline-only"
  workTitle="毛星云《逐梦旅程：Windows游戏编程之从零开始》（清华大学出版社，2013）"
  adaptedUrl="https://book.douban.com/subject/25756435/"
/>
