---
title: 第23章 向碧蓝的苍穹致敬——三维天空的构建
type: C
section: 第四篇 · DirectX游戏编程应用
order: 23
description: 第23章 向碧蓝的苍穹致敬——三维天空的构建：让天空随相机平移但不产生近处视差，并隔离深度和剔除状态，以历史—现代对照、故障恢复和逐节点证据验收。
demo: true
math: false
sourceUrl: 'https://book.douban.com/subject/25756435/'
draft: false
qualityVersion: 2
practiceMode: simulation
sourceMode: independent-rewrite
---
import {
  Wj23SkyboxMapLab,
  Wj23SkyboxExperimentLab,
  Wj23SkyboxEvidenceLab,
} from "@/components/mdx/windows-journey/diagrams/wj-23-skybox";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

# 第23章 向碧蓝的苍穹致敬——三维天空的构建

<Objectives>

- 能从“天空盒若参与普通世界平移或错误写深度，会暴露立方体边界并遮住场景”建立本章的历史问题基线。
- 能解释“以相机为中心绘制天空几何，使用一致贴图接缝并在受控深度状态下提交”的输入、状态、所有权和可见结果。
- 能操作专属实验，对照六面天空盒与立方体贴图天空，复算接缝像素。
- 能注入“天空开启深度写入或忘记恢复剔除模式”，用相机位置抵消、纹理方向、深度状态、绘制顺序和接缝像素定位首错、恢复并重放。

</Objectives>

{/* WINDOWS_JOURNEY_QUALITY_V2 */}

## 问题先于 API

天空盒若参与普通世界平移或错误写深度，会暴露立方体边界并遮住场景。本章先保存问题、初态和输入，再讨论 API；通过条件是“相机平移不改变天空方向采样，天空绘制后所有渲染状态恢复”，不是画面偶然出现一次。

## 来源范围、技术年代与独立重写

公开[完整目录](https://book.douban.com/subject/25756435/)只用于界定本页题目和层级，[出版记录](https://www.sinobook.com.cn/b2c/scrp/bookdetail.cfm?iBookNo=646210)与[书目记录](https://books.google.com/books/about/%E9%80%90%E6%A2%A6%E6%97%85%E7%A8%8B.html?id=6O-F0AEACAAJ)用于交叉核对 ISBN、出版社和年代；没有把目录核对表述成正文忠实。本页关于 Windows/DirectX 的机制由[Microsoft 一手文档](https://learn.microsoft.com/en-us/windows/win32/direct3d9/direct3d-textures)核验，以下中文讲解、图示、实验、代码和练习均为独立教学重写。

原书处于 Visual Studio 2010、Win32/GDI、DirectX 9 与固定功能管线语境。现代 API 只作迁移对照：六面天空盒和立方体贴图天空名称可以不同，但必须消费相同输入并守住同一不变量。

## 机制与术语

<Term def="天空盒在“第23章 向碧蓝的苍穹致敬——三维天空的构建”中标记输入进入系统时的责任边界。">天空盒</Term>、<Term def="方向采样在“第23章 向碧蓝的苍穹致敬——三维天空的构建”中标记状态被创建、修改与释放的生命周期。">方向采样</Term>、<Term def="视差在“第23章 向碧蓝的苍穹致敬——三维天空的构建”中标记历史实现与现代迁移之间必须保持的机制。">视差</Term>、<Term def="状态隔离在“第23章 向碧蓝的苍穹致敬——三维天空的构建”中标记能够复算本章结论的原始证据。">状态隔离</Term>。四个术语共同约束“让天空随相机平移但不产生近处视差，并隔离深度和剔除状态”，结论必须回到相机位置抵消、纹理方向、深度状态、绘制顺序和接缝像素。

## 先预测，再操作三层专属实验

操作前写下预测：哪一阶段先改变接缝像素；压力扩大四倍时哪条边界先失效；注入“天空开启深度写入或忘记恢复剔除模式”后怎样回到初态。

<Stepper>
  <Step title="1. 正式节点与状态链">
    选择目录节点和机制阶段，检查输入、状态拥有者、可见结果与验证证据。

    <Wj23SkyboxMapLab />

  </Step>
  <Step title="2. 历史实现与现代迁移">
    一次只改变技术坐标、场景负载或故障之一，读取可复算结果。

    <Wj23SkyboxExperimentLab />

  </Step>
  <Step title="3. 故障、恢复与同输入重放">
    比较正常、故障和恢复轨迹；最后点击重置，确认所有控制和状态回到初值。

    <Wj23SkyboxEvidenceLab />

  </Step>
</Stepper>

## 正式目录逐项深读

### 第23章 向碧蓝的苍穹致敬——三维天空的构建

围绕让天空随相机平移但不产生近处视差，并隔离深度和剔除状态，第23章 向碧蓝的苍穹致敬——三维天空的构建 限定本章的一个知识坐标；独立解释围绕“以相机为中心绘制天空几何，使用一致贴图接缝并在受控深度状态下提交”展开，并以“相机平移不改变天空方向采样，天空绘制后所有渲染状态恢复”结束。

第23章 向碧蓝的苍穹致敬——三维天空的构建 进入“读取相机”阶段时，先标出输入、状态拥有者和可见输出，再解释以相机为中心绘制天空几何，使用一致贴图接缝并在受控深度状态下提交。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 第23章 向碧蓝的苍穹致敬——三维天空的构建 时固定其余条件，记录相机位置抵消、纹理方向、深度状态、绘制顺序和接缝像素，随后注入“天空开启深度写入或忘记恢复剔除模式”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 23.1 三维天空技术阐述

围绕让天空随相机平移但不产生近处视差，并隔离深度和剔除状态，23.1 三维天空技术阐述 限定本章的一个知识坐标；独立解释围绕“以相机为中心绘制天空几何，使用一致贴图接缝并在受控深度状态下提交”展开，并以“相机平移不改变天空方向采样，天空绘制后所有渲染状态恢复”结束。

23.1 三维天空技术阐述 进入“居中几何”阶段时，先标出输入、状态拥有者和可见输出，再解释以相机为中心绘制天空几何，使用一致贴图接缝并在受控深度状态下提交。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 23.1 三维天空技术阐述 时固定其余条件，记录相机位置抵消、纹理方向、深度状态、绘制顺序和接缝像素，随后注入“天空开启深度写入或忘记恢复剔除模式”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 23.2 天空盒的设计

围绕让天空随相机平移但不产生近处视差，并隔离深度和剔除状态，23.2 天空盒的设计 限定本章的一个知识坐标；独立解释围绕“以相机为中心绘制天空几何，使用一致贴图接缝并在受控深度状态下提交”展开，并以“相机平移不改变天空方向采样，天空绘制后所有渲染状态恢复”结束。

23.2 天空盒的设计 进入“采样天空”阶段时，先标出输入、状态拥有者和可见输出，再解释以相机为中心绘制天空几何，使用一致贴图接缝并在受控深度状态下提交。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 23.2 天空盒的设计 时固定其余条件，记录相机位置抵消、纹理方向、深度状态、绘制顺序和接缝像素，随后注入“天空开启深度写入或忘记恢复剔除模式”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 23.3 天空盒类的实现

围绕让天空随相机平移但不产生近处视差，并隔离深度和剔除状态，23.3 天空盒类的实现 限定本章的一个知识坐标；独立解释围绕“以相机为中心绘制天空几何，使用一致贴图接缝并在受控深度状态下提交”展开，并以“相机平移不改变天空方向采样，天空绘制后所有渲染状态恢复”结束。

23.3 天空盒类的实现 进入“限制深度”阶段时，先标出输入、状态拥有者和可见输出，再解释以相机为中心绘制天空几何，使用一致贴图接缝并在受控深度状态下提交。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 23.3 天空盒类的实现 时固定其余条件，记录相机位置抵消、纹理方向、深度状态、绘制顺序和接缝像素，随后注入“天空开启深度写入或忘记恢复剔除模式”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 23.4 天空盒类的使用

围绕让天空随相机平移但不产生近处视差，并隔离深度和剔除状态，23.4 天空盒类的使用 限定本章的一个知识坐标；独立解释围绕“以相机为中心绘制天空几何，使用一致贴图接缝并在受控深度状态下提交”展开，并以“相机平移不改变天空方向采样，天空绘制后所有渲染状态恢复”结束。

23.4 天空盒类的使用 进入“恢复状态”阶段时，先标出输入、状态拥有者和可见输出，再解释以相机为中心绘制天空几何，使用一致贴图接缝并在受控深度状态下提交。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 23.4 天空盒类的使用 时固定其余条件，记录相机位置抵消、纹理方向、深度状态、绘制顺序和接缝像素，随后注入“天空开启深度写入或忘记恢复剔除模式”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 23.5 示例程序D3Ddemo18

围绕让天空随相机平移但不产生近处视差，并隔离深度和剔除状态，对 23.5 示例程序D3Ddemo18，把前述机制装入一个可运行场景：固定构建、资源和输入，仅改变一个条件，并保留相机位置抵消、纹理方向、深度状态、绘制顺序和接缝像素。

23.5 示例程序D3Ddemo18 进入“读取相机”阶段时，先标出输入、状态拥有者和可见输出，再解释以相机为中心绘制天空几何，使用一致贴图接缝并在受控深度状态下提交。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 23.5 示例程序D3Ddemo18 时固定其余条件，记录相机位置抵消、纹理方向、深度状态、绘制顺序和接缝像素，随后注入“天空开启深度写入或忘记恢复剔除模式”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

### 23.6 章节小憩

围绕让天空随相机平移但不产生近处视差，并隔离深度和剔除状态，对 23.6 章节小憩，收尾不是装饰，而是要求用相机位置抵消、纹理方向、深度状态、绘制顺序和接缝像素复盘“相机平移不改变天空方向采样，天空绘制后所有渲染状态恢复”是否在正常和失败路径同时成立。

23.6 章节小憩 进入“居中几何”阶段时，先标出输入、状态拥有者和可见输出，再解释以相机为中心绘制天空几何，使用一致贴图接缝并在受控深度状态下提交。2013 年的 API 名称作为历史坐标保留；迁移到现代工具时只替换实现，不能改变本章问题与证据口径。

验证 23.6 章节小憩 时固定其余条件，记录相机位置抵消、纹理方向、深度状态、绘制顺序和接缝像素，随后注入“天空开启深度写入或忘记恢复剔除模式”并执行复位。该节点由此同时具备出现、解释、专属实验和练习验证四级证据。

## 可重放实现或计算骨架

```text
freeze -> 读取相机 -> 居中几何 -> 采样天空 -> 限制深度 -> 恢复状态
fault -> 天空开启深度写入或忘记恢复剔除模式
evidence -> 相机位置抵消、纹理方向、深度状态、绘制顺序和接缝像素
reset -> replay identical input
```

真实程序还要固定编译器、SDK、窗口尺寸、资源、随机种子、时间步和输入记录；否则历史实现与现代迁移不能公平比较。

<Callout type="trap" title="把历史 API 当成当前默认方案">
  本章保留 六面天空盒 的教学身份，但新项目必须核对平台支持并解释为何选择 立方体贴图天空 或其他方案。
</Callout>

<Callout type="trap" title="只看最终画面">
  第23章 向碧蓝的苍穹致敬——三维天空的构建必须说明读取相机、居中几何、采样天空、限制深度、恢复状态的先后关系；最终像素相同不代表消息、资源、矩阵或状态安全。
</Callout>

<Callout type="trap" title="失败后继续运行">
  注入“天空开启深度写入或忘记恢复剔除模式”后应停在第一个因果分叉；若相机位置抵消、纹理方向、深度状态、绘制顺序和接缝像素无法解释并复位，当前实现失败。
</Callout>

## 练习与 4 级证据矩阵

<Exercises>

**问题 1：历史边界。** 怎样避免把 2013 年接口示例误报成今天的默认架构？

<Answer>
  分别记录六面天空盒与立方体贴图天空的版本、责任和资源模型；只迁移机制，不改写历史事实，并以[Microsoft 文档](https://learn.microsoft.com/en-us/windows/win32/direct3d9/direct3d-textures)核对接口身份。
</Answer>

**问题 2：逐节点验证。** 正式目录节点怎样从“出现”升级到可复查证据？

<Answer>
  - **第23章 向碧蓝的苍穹致敬——三维天空的构建**：在“读取相机”记录相机位置抵消、纹理方向、深度状态、绘制顺序和接缝像素，注入“天空开启深度写入或忘记恢复剔除模式”后复位并重放。
  - **23.1 三维天空技术阐述**：在“居中几何”记录相机位置抵消、纹理方向、深度状态、绘制顺序和接缝像素，注入“天空开启深度写入或忘记恢复剔除模式”后复位并重放。
  - **23.2 天空盒的设计**：在“采样天空”记录相机位置抵消、纹理方向、深度状态、绘制顺序和接缝像素，注入“天空开启深度写入或忘记恢复剔除模式”后复位并重放。
  - **23.3 天空盒类的实现**：在“限制深度”记录相机位置抵消、纹理方向、深度状态、绘制顺序和接缝像素，注入“天空开启深度写入或忘记恢复剔除模式”后复位并重放。
  - **23.4 天空盒类的使用**：在“恢复状态”记录相机位置抵消、纹理方向、深度状态、绘制顺序和接缝像素，注入“天空开启深度写入或忘记恢复剔除模式”后复位并重放。
  - **23.5 示例程序D3Ddemo18**：在“读取相机”记录相机位置抵消、纹理方向、深度状态、绘制顺序和接缝像素，注入“天空开启深度写入或忘记恢复剔除模式”后复位并重放。
  - **23.6 章节小憩**：在“居中几何”记录相机位置抵消、纹理方向、深度状态、绘制顺序和接缝像素，注入“天空开启深度写入或忘记恢复剔除模式”后复位并重放。
</Answer>

**问题 3：故障闭环。** 注入“天空开启深度写入或忘记恢复剔除模式”后怎样判定恢复成功？

<Answer>
  保存首错阶段，逆序清理或恢复必要状态，以相同初态和输入重放；只有相机位置抵消、纹理方向、深度状态、绘制顺序和接缝像素回到基线且“相机平移不改变天空方向采样，天空绘制后所有渲染状态恢复”再次成立，才算恢复。
</Answer>

</Exercises>

## 术语复核与本章回顾

<Glossary>
  <GlossaryItem term="天空盒">
    天空盒在“第23章 向碧蓝的苍穹致敬——三维天空的构建”中标记输入进入系统时的责任边界。
  </GlossaryItem>
  <GlossaryItem term="方向采样">
    方向采样在“第23章 向碧蓝的苍穹致敬——三维天空的构建”中标记状态被创建、修改与释放的生命周期。
  </GlossaryItem>
  <GlossaryItem term="视差">
    视差在“第23章 向碧蓝的苍穹致敬——三维天空的构建”中标记历史实现与现代迁移之间必须保持的机制。
  </GlossaryItem>
  <GlossaryItem term="状态隔离">
    状态隔离在“第23章 向碧蓝的苍穹致敬——三维天空的构建”中标记能够复算本章结论的原始证据。
  </GlossaryItem>
</Glossary>

掌握“第23章 向碧蓝的苍穹致敬——三维天空的构建”意味着能解释以相机为中心绘制天空几何，使用一致贴图接缝并在受控深度状态下提交，区分历史坐标与现代迁移，并用相机位置抵消、纹理方向、深度状态、绘制顺序和接缝像素推翻或保留实现。

## 阅读导航

[← 上一页：第22章 钟灵毓秀的世界——三维地形的构建](/learn/windows-journey/05-directx-application/wj-22-terrain) · [下一页：第24章 让唯美的雪花飘扬——三维粒子系统的实现 →](/learn/windows-journey/05-directx-application/wj-24-particle-system)

<Attribution
  mode="independent-rewrite"
  sourceBasis="outline-only"
  workTitle="毛星云《逐梦旅程：Windows游戏编程之从零开始》（清华大学出版社，2013）"
  adaptedUrl="https://book.douban.com/subject/25756435/"
/>
