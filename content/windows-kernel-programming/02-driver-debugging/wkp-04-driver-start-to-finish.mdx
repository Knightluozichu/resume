---
title: "第4章 驱动程序：从头到尾"
type: C
section: "实战 · 完整驱动与调试"
order: 4
description: "从初始化、注册表参数和设备对象进入客户端/驱动通信协议、Create/Close、DeviceIoControl、安装和端到端测试。 覆盖11个本页目录坐标。"
demo: true
math: false
sourceUrl: "https://leanpub.com/windowskernelprogramming"
draft: true
---

import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第4章 驱动程序：从头到尾的核心内核对象、控制流和全书位置
- 能描述“定义协议结构 → 初始化驱动 → 创建设备符号链接 → 打开关闭句柄 → 分发IOCTL → 安装端到端测试”的输入输出、IRQL与资源生存期
- 能实现一个在隔离虚拟机中可重放、可故障注入的最小驱动实验
- 能比较基线、边界、失败与恢复轨迹并判断不变量是否成立

</Objectives>

## 直觉起点

<Term def="控制设备连接定义协议结构阶段的IRQL、对象、请求和恢复证据。">控制设备</Term>、<Term def="IOCTL连接初始化驱动阶段的IRQL、对象、请求和恢复证据。">IOCTL</Term>、<Term def="DeviceIoControl连接创建设备符号链接阶段的IRQL、对象、请求和恢复证据。">DeviceIoControl</Term>、<Term def="分发例程连接打开关闭句柄阶段的IRQL、对象、请求和恢复证据。">分发例程</Term>、<Term def="通信协议连接分发IOCTL阶段的IRQL、对象、请求和恢复证据。">通信协议</Term>
共同构成本页坐标。从初始化、注册表参数和设备对象进入客户端/驱动通信协议、Create/Close、DeviceIoControl、安装和端到端测试。

内核编程必须同时画三张图：调用图说明用户请求、系统回调或I/O如何进入驱动；对象图说明DriverObject、DeviceObject、IRP、回调Cookie、上下文和池块由谁拥有；时间图说明注册、并发回调、注销、排空和卸载的先后。少一张图，表面可用的驱动也可能在压力或卸载时蓝屏。

## 常见误区与故障注入

<Callout type="trap">
  **误区一：驱动加载成功就说明内核代码正确。**
  很多错误只在缓冲边界、并发回调、错误IRQL或卸载时出现。必须启用定向Verifier并验证完成、引用、池和锁序。
</Callout>

<Callout type="trap">
  **误区二：把这本书改写成WDM/WDF或硬件驱动通论。**
  正式目录的主线是软件内核驱动、系统通知、小过滤器和设备过滤；PnP、MDL和WDF只能在相关机制处作对照，不能冒充原章节。
</Callout>

## 四类样本与验收

| 样本 | 注入方式                   | 必查证据                 | 通过条件     |
| ---- | -------------------------- | ------------------------ | ------------ |
| 基线 | 固定快照、sys/PDB和输入    | 状态码、IRQL、对象与日志 | 与预测一致   |
| 边界 | 空缓冲、短输出、断开客户端 | 长度、Information、引用  | 不越界不泄漏 |
| 失败 | 错IRQL、竞态、Verifier规则 | 首错栈和责任层           | 故障可定位   |
| 恢复 | 清理或快照后同输入重放     | 模块、服务、池、回调状态 | 回到基线     |

## 练习

<Exercises>

**问题 1** 为什么不能只看用户模式输出验收第4章 驱动程序：从头到尾？

<Answer>
  输出可能隐藏错误IRQL、重复完成、悬空回调、池泄漏和卸载竞态。必须同时检查NTSTATUS/Information、内核序号日志、对象引用、Verifier和恢复重放。
</Answer>

**问题 2** 如何把“第4章 驱动程序：从头到尾”变成可执行证据？

<Answer>
  沿“定义协议结构、初始化驱动、创建设备符号链接、打开关闭句柄、分发IOCTL、安装端到端测试”固定虚拟机、WDK、sys/PDB和输入，预测调用线程、IRQL、对象与状态码，只改变一个条件并保存首错；恢复后比较IRP、回调、池或过滤上下文。
</Answer>

**问题 3** 本页最小通过条件是什么？

<Answer>
  IOCTL编码、输入输出长度、访问位、共享结构版本和BytesReturned在用户/内核两侧完全一致。
  失败路径不得污染下一轮，卸载与清理明确，相同输入和事件序列能够重新到达基线。
</Answer>

</Exercises>

## 小结

- 控制设备：连接“定义协议结构”的内核契约与证据。
- IOCTL：连接“初始化驱动”的内核契约与证据。
- DeviceIoControl：连接“创建设备符号链接”的内核契约与证据。
- 分发例程：连接“打开关闭句柄”的内核契约与证据。
- 通信协议：连接“分发IOCTL”的内核契约与证据。
- 本页11个目录坐标全部进入实验链，不以泛化驱动主题替代原书内容。

## 术语表

<Glossary>
  <GlossaryItem term="控制设备">
    控制设备用于解释“定义协议结构”，需要同时说明调用契约、IRQL、对象或缓冲生存期、首错与清理方法。
  </GlossaryItem>
  <GlossaryItem term="IOCTL">
    IOCTL用于解释“初始化驱动”，需要同时说明调用契约、IRQL、对象或缓冲生存期、首错与清理方法。
  </GlossaryItem>
  <GlossaryItem term="DeviceIoControl">
    DeviceIoControl用于解释“创建设备符号链接”，需要同时说明调用契约、IRQL、对象或缓冲生存期、首错与清理方法。
  </GlossaryItem>
  <GlossaryItem term="分发例程">
    分发例程用于解释“打开关闭句柄”，需要同时说明调用契约、IRQL、对象或缓冲生存期、首错与清理方法。
  </GlossaryItem>
  <GlossaryItem term="通信协议">
    通信协议用于解释“分发IOCTL”，需要同时说明调用契约、IRQL、对象或缓冲生存期、首错与清理方法。
  </GlossaryItem>
</Glossary>

<Attribution
  adaptedFrom="Pavel Yosifovich 著、李亮译 · Windows内核编程 · 机械工业出版社 · 权威目录独立教学重构"
  adaptedUrl="https://leanpub.com/windowskernelprogramming"
/>
