---
title: 第8章 进程和线程通知
type: C
section: 监控 · 系统通知
order: 8
description: 第8章 进程和线程通知覆盖10个正式目录坐标，用版本/IRQL合同、可执行对象探针与安全门交付回调注册表、事件模式、队列/丢弃轨迹、用户态协议与注销证明
demo: true
math: false
sourceUrl: "https://leanpub.com/windowskernelprogramming"
draft: false
qualityVersion: 2
practiceMode: simulation
sourceMode: independent-rewrite
officialUnitId: wkp-unit-08
---

import {
  Objectives,
  Term,
  Callout,
  Stepper,
  Step,
  Exercises,
  Answer,
  Glossary,
  GlossaryItem,
  Attribution,
} from "@/components/mdx/mdx-components";
import {
  Wkp08ProcessThreadNotificationsVersionContextLab,
  Wkp08ProcessThreadNotificationsExecutableProbeLab,
  Wkp08ProcessThreadNotificationsSafetyGateLab,
} from "@/components/mdx/windows-kernel-programming/v2/wkp-08-process-thread-notifications";

<Objectives>

- 把进程通知、创建/退出、用户态数据、线程通知与映像加载通知落实为Windows build、驱动模型、内核对象、执行上下文、调用链与恢复条件
- 只注入“回调内阻塞、事件队列无界、PID复用误关联或Unload时回调仍可达”，定位第8章 进程和线程通知相对签名参考驱动的首个分岔
- 交付回调注册表、事件模式、队列/丢弃轨迹、用户态协议与注销证明，明确区分中文版一版、作者二版迁移轨与目标Microsoft平台轨

</Objectives>

## 为什么从这个问题开始

第8章 进程和线程通知围绕“进程、线程与映像通知怎样以短回调、显式队列、丢弃统计和注销顺序送达用户态？”建立贯穿任务。先冻结出版轨、目标build、驱动包和对象身份，再以小输入观察状态与调用链，最后用唯一故障和同输入恢复验收；只有守住“注册代次内事件不重不乱，丢弃可计数，停止新事件后队列与引用归零”并交付回调注册表、事件模式、队列/丢弃轨迹、用户态协议与注销证明，目录术语、DDI名字或一次成功加载才可能升级为机制证据。

## 来源合同、版本差异与安全边界

第8章 进程和线程通知以[作者一版Leanpub完整公开目录](https://leanpub.com/windowskernelprogramming)核对Pavel Yosifovich《Windows Kernel Programming》2020-12-10版本、455页和11章结构；中文版采用李亮译《Windows内核编程》，机械工业出版社2021年版，ISBN 9787111684756。中文版治理清单是11章185个正式坐标，章根已计入；作者一版公开目录按同口径是204个坐标，两者相差19项。

[官方18页样章](https://s3.amazonaws.com/samples.leanpub.com/windowskernelprogramming-sample.pdf)开放一版身份、第一章目录以及进程、虚拟内存、线程和系统调用的局部正文，因此本站来源访问级别是authorized-sample。样章不授权其余正文、代码、图表或练习；第8章 进程和线程通知是中文独立教学重构，不复制未开放内容。

第8章 进程和线程通知登记的19项差异是：第1章：Thread Stacks；第2章：Exercises；第5章：Tutorial: User mode debugging basics；第6章：Using DPC with a Timer；第6章：Critical Regions and Guarded Regions；第6章：The Spin Lock；第7章：IRP Flow；第7章：Viewing IRP Information；第7章：Completing a Request；第8章：The User Mode Client；第8章：Exercises；第9章：Desktop Objects；第9章：Exercises；第10章：Pipes and Mailslots；第10章：Direct Access Volume (DAX or DAS)；第10章：Context Types；第10章：Managing Contexts；第10章：Exercises；第11章：Example Driver Verifier Sessions。它们可能来自译本底稿、修订时点或目录裁剪；没有出版社正文证据就保持“来源差异”，不补成中文版坐标，也不把作者页面的后续更新倒写给译者。

第8章 进程和线程通知把三条轨道分开：中文版/作者一版解释成书范围；[作者二版](https://leanpub.com/windowskernelprogrammingsecondedition/)及[MIT示例仓库](https://github.com/zodiacon/windowskernelprogrammingbook2e)只做迁移参考；当前平台轨以Microsoft Learn和目标系统公开DDI核对。第8章 进程和线程通知的实验必须记录完整Windows build、架构、SDK、WDK、Visual Studio、驱动模型、签名、VBS/HVCI、驱动包摘要和符号，不能只写Windows 11或latest。

第8章 进程和线程通知涉及内核代码、驱动部署、回调、用户缓冲区、Minifilter、Verifier和故障调试。第8章 进程和线程通知页面只给实验协议；执行仅允许在可丢弃VM或专用测试机，并预备快照、备份、宿主内核调试、崩溃转储、超时、恢复启动和Verifier reset。生产设备、日常主机和无法承受蓝屏或数据损坏的对象不在实验范围内。

### 本页独立事实来源

- [作者一版Leanpub页面与完整公开目录](https://leanpub.com/windowskernelprogramming)：第8章 进程和线程通知用它核对Pavel Yosifovich、2020-12-10版本、455页、11章公开目录与软件内核驱动范围。
- [作者一版Leanpub官方18页授权样章](https://s3.amazonaws.com/samples.leanpub.com/windowskernelprogramming-sample.pdf)：第8章 进程和线程通知用它局部核对一版身份、第一章目录及进程、虚拟内存、线程和系统调用开放正文；不扩大整书授权。
- [作者二版Leanpub页面与完整公开目录](https://leanpub.com/windowskernelprogrammingsecondedition/)：第8章 进程和线程通知用它建立二版迁移轨，识别新增章节和接口；不把二版倒写成2021年中文版。
- [作者二版MIT示例仓库](https://github.com/zodiacon/windowskernelprogrammingbook2e)：第8章 进程和线程通知用它核对当前作者示例的章节归属、提交与许可；仓库只对应二版。
- [Microsoft Windows内核DDI参考](https://learn.microsoft.com/en-us/windows-hardware/drivers/ddi/_kernel/)：第8章 进程和线程通知用它核对目标build公开DDI、对象、IRP和例程合同。
- [Microsoft ObCallback官方示例](https://learn.microsoft.com/en-us/samples/microsoft/windows-driver-samples/obcallback-callback-registration-driver/)：第8章 进程和线程通知用它核对进程通知与对象回调的受支持示例边界。
- [Microsoft驱动安全清单](https://learn.microsoft.com/en-us/windows-hardware/drivers/driversecurity/driver-security-checklist)：第8章 进程和线程通知用它核对内核必要性、最小权限、WDF、HVCI、测试、签名和发布门。

## 正式目录坐标逐项解释

### 第8章 进程和线程通知

**坐标 1/10：第8章 进程和线程通知。稳定证据键 WKP2-C08-A。** 第8章 进程和线程通知把第8章 进程和线程通知映射到“进程通知、创建/退出、用户态数据、线程通知与映像加载通知”中的对象身份、所有者、引用、IRQL和调用线程。 第8章 进程和线程通知必须交付回调注册表、事件模式、队列/丢弃轨迹、用户态协议与注销证明中的对应记录；目录标题限定范围，授权样章只支持开放页，Microsoft文档支持当前平台，三者都不能替代目标build的运行证据。

### 8.1 进程通知

**坐标 2/10：8·1 进程通知。稳定证据键 WKP2-C08-B。** 第8章 进程和线程通知为8·1 进程通知分别记录中文版一版对象、作者二版迁移和目标Microsoft DDI，形成显式版本差分。 第8章 进程和线程通知必须交付回调注册表、事件模式、队列/丢弃轨迹、用户态协议与注销证明中的对应记录；目录标题限定范围，授权样章只支持开放页，Microsoft文档支持当前平台，三者都不能替代目标build的运行证据。

### 8.2 实现进程通知

**坐标 3/10：8·2 实现进程通知。稳定证据键 WKP2-C08-C。** 第8章 进程和线程通知对8·2 实现进程通知预注册输入、唯一控制与首个WinDbg/ETW/状态信号，不以最终蓝屏或成功加载替代调用链。 第8章 进程和线程通知必须交付回调注册表、事件模式、队列/丢弃轨迹、用户态协议与注销证明中的对应记录；目录标题限定范围，授权样章只支持开放页，Microsoft文档支持当前平台，三者都不能替代目标build的运行证据。

### 8.2.1 DriverEntry例程

**坐标 4/10：8·2·1 DriverEntry例程。稳定证据键 WKP2-C08-D。** 第8章 进程和线程通知用8·2·1 DriverEntry例程构造可撤销反例，安全卸载或恢复快照后以同驱动包和输入重新验收“注册代次内事件不重不乱，丢弃可计数，停止新事件后队列与引用归零”。 第8章 进程和线程通知必须交付回调注册表、事件模式、队列/丢弃轨迹、用户态协议与注销证明中的对应记录；目录标题限定范围，授权样章只支持开放页，Microsoft文档支持当前平台，三者都不能替代目标build的运行证据。

### 8.2.2 处理进程退出通知

**坐标 5/10：8·2·2 处理进程退出通知。稳定证据键 WKP2-C08-E。** 第8章 进程和线程通知把8·2·2 处理进程退出通知映射到“进程通知、创建/退出、用户态数据、线程通知与映像加载通知”中的对象身份、所有者、引用、IRQL和调用线程。 第8章 进程和线程通知必须交付回调注册表、事件模式、队列/丢弃轨迹、用户态协议与注销证明中的对应记录；目录标题限定范围，授权样章只支持开放页，Microsoft文档支持当前平台，三者都不能替代目标build的运行证据。

### 8.2.3 处理进程创建通知

**坐标 6/10：8·2·3 处理进程创建通知。稳定证据键 WKP2-C08-F。** 第8章 进程和线程通知为8·2·3 处理进程创建通知分别记录中文版一版对象、作者二版迁移和目标Microsoft DDI，形成显式版本差分。 第8章 进程和线程通知必须交付回调注册表、事件模式、队列/丢弃轨迹、用户态协议与注销证明中的对应记录；目录标题限定范围，授权样章只支持开放页，Microsoft文档支持当前平台，三者都不能替代目标build的运行证据。

### 8.3 将数据提供给用户模式

**坐标 7/10：8·3 将数据提供给用户模式。稳定证据键 WKP2-C08-G。** 第8章 进程和线程通知对8·3 将数据提供给用户模式预注册输入、唯一控制与首个WinDbg/ETW/状态信号，不以最终蓝屏或成功加载替代调用链。 第8章 进程和线程通知必须交付回调注册表、事件模式、队列/丢弃轨迹、用户态协议与注销证明中的对应记录；目录标题限定范围，授权样章只支持开放页，Microsoft文档支持当前平台，三者都不能替代目标build的运行证据。

### 8.4 线程通知

**坐标 8/10：8·4 线程通知。稳定证据键 WKP2-C08-H。** 第8章 进程和线程通知用8·4 线程通知构造可撤销反例，安全卸载或恢复快照后以同驱动包和输入重新验收“注册代次内事件不重不乱，丢弃可计数，停止新事件后队列与引用归零”。 第8章 进程和线程通知必须交付回调注册表、事件模式、队列/丢弃轨迹、用户态协议与注销证明中的对应记录；目录标题限定范围，授权样章只支持开放页，Microsoft文档支持当前平台，三者都不能替代目标build的运行证据。

### 8.5 映像加载通知

**坐标 9/10：8·5 映像加载通知。稳定证据键 WKP2-C08-I。** 第8章 进程和线程通知把8·5 映像加载通知映射到“进程通知、创建/退出、用户态数据、线程通知与映像加载通知”中的对象身份、所有者、引用、IRQL和调用线程。 第8章 进程和线程通知必须交付回调注册表、事件模式、队列/丢弃轨迹、用户态协议与注销证明中的对应记录；目录标题限定范围，授权样章只支持开放页，Microsoft文档支持当前平台，三者都不能替代目标build的运行证据。

### 8.7 总结

**坐标 10/10：8·7 总结。稳定证据键 WKP2-C08-J。** 第8章 进程和线程通知为8·7 总结分别记录中文版一版对象、作者二版迁移和目标Microsoft DDI，形成显式版本差分。 第8章 进程和线程通知必须交付回调注册表、事件模式、队列/丢弃轨迹、用户态协议与注销证明中的对应记录；目录标题限定范围，授权样章只支持开放页，Microsoft文档支持当前平台，三者都不能替代目标build的运行证据。

## 三个可操作证据视图

第8章 进程和线程通知先选择正式坐标、版本轨和执行上下文，再调整小输入复算对象数量、队列或缓冲边界，最后逐阶段切换参考、单故障与恢复。每个视图都保留重置，不生成驱动成熟度、风险或性能综合分。

<Stepper>
  <Step title="版本合同：选择坐标、出版轨与IRQL">
    <Wkp08ProcessThreadNotificationsVersionContextLab />
  </Step>
  <Step title="可执行探针：调整输入并复算对象状态">
    <Wkp08ProcessThreadNotificationsExecutableProbeLab />
  </Step>
  <Step title="安全门：重放基线、单故障与恢复">
    <Wkp08ProcessThreadNotificationsSafetyGateLab />
  </Step>
</Stepper>

第8章 进程和线程通知的探针真正计算对象/句柄、构建矩阵、池载荷、IOCTL请求、转储二分提示、DPC预算、IRP栈位置、通知队列、回调关联、Minifilter消息或Verifier请求。公式公开且只用于小模型；真实延迟、内存、安全和并发结论必须来自目标build、驱动包、测试机与捕获。

## 最小可重现实验协议

1. 第8章 进程和线程通知先冻结Windows产品与完整build、架构、SDK、WDK、Visual Studio、驱动模型、SYS/INF/CAT摘要、签名、VBS/HVCI、符号、VM快照、输入和预期结果。
2. 第8章 进程和线程通知只在可丢弃VM或专用测试机运行签名参考驱动，保存回调注册表、事件模式、队列/丢弃轨迹、用户态协议与注销证明；基线不稳、符号不匹配、对象身份不明或调试宿主不可用时停止。
3. 第8章 进程和线程通知保持其余条件不变，只注入“回调内阻塞、事件队列无界、PID复用误关联或Unload时回调仍可达”，记录首个对象状态、NTSTATUS、IRQL、线程、CPU、时间、调用链、竞争性解释和停止条件。
4. 第8章 进程和线程通知停止新请求与回调，反向排空IRP、队列、端口、实例、引用和设备，必要时执行Verifier reset或恢复快照；以同输入重放，无法恢复“注册代次内事件不重不乱，丢弃可计数，停止新事件后队列与引用归零”则标记失败或未知。

<Callout
  type="trap"
  title="第8章 进程和线程通知误区一：作者样章或二版等于中文版整书"
>
  第8章
  进程和线程通知只把18页样章当开放页授权，把二版与MIT仓库当迁移来源；中文版185坐标与作者一版204坐标之间的19项差异必须单列，不能复制、补写或倒写。
</Callout>

<Callout
  type="trap"
  title="第8章 进程和线程通知误区二：接口同名就说明当前合同未变"
>
  第8章
  进程和线程通知中的IRQL、池、WDM/KMDF、IRP、通知、Minifilter、签名和Verifier都必须回到目标build与当前Microsoft文档确认；Windows
  11、x64或latest都不是足够身份。
</Callout>

<Callout
  type="trap"
  title="第8章 进程和线程通知误区三：能加载、能蓝屏或有日志就证明机制"
>
  第8章 进程和线程通知不以一次加载、最终bug
  check、单条DbgPrint或综合分裁决；必须保存正确符号、稳定基线、唯一故障首错、竞争性解释、反向卸载和同输入恢复。
</Callout>

## 小结与上架门

第8章 进程和线程通知把进程通知、创建/退出、用户态数据、线程通知与映像加载通知连接成可复核链：中文版目录给185正式坐标，作者一版页面暴露19项目录差异，18页样章限定局部授权，二版与MIT仓库只支持迁移，当前Microsoft文档和目标DDI限定实际合同。版本/IRQL合同、透明对象探针、唯一故障首错、反向卸载和同输入恢复共同决定能否上架。最终交付回调注册表、事件模式、队列/丢弃轨迹、用户态协议与注销证明并同时报告未测build、未开放正文和安全边界。

## 练习与答案

<Exercises>

**问题 1：8.1 进程通知**

为第8章 进程和线程通知的 WKP2-C08-B 设计一个中文版一版/作者二版/目标平台差分、签名参考基线、唯一故障、首个对象或调用链信号和恢复断言，并说明8·1 进程通知的IRQL或用户边界。

<Answer>
  第8章
  进程和线程通知先冻结完整build、架构、工具链、驱动包、签名、VBS/HVCI、符号、VM和输入，把
  WKP2-C08-B
  映射到对象身份、所有者、IRQL、控制前值与预期信号；只注入“回调内阻塞、事件队列无界、PID复用误关联或Unload时回调仍可达”。第8章
  进程和线程通知保存首个分岔与竞争性解释，停止新请求并反向清理后以同输入重新满足“注册代次内事件不重不乱，丢弃可计数，停止新事件后队列与引用归零”。样章外原文、未测build和未捕获竞态保留为未知。
</Answer>

**问题 2：8.2 实现进程通知**

为第8章 进程和线程通知的 WKP2-C08-C 设计一个中文版一版/作者二版/目标平台差分、签名参考基线、唯一故障、首个对象或调用链信号和恢复断言，并说明8·2 实现进程通知的IRQL或用户边界。

<Answer>
  第8章
  进程和线程通知先冻结完整build、架构、工具链、驱动包、签名、VBS/HVCI、符号、VM和输入，把
  WKP2-C08-C
  映射到对象身份、所有者、IRQL、控制前值与预期信号；只注入“回调内阻塞、事件队列无界、PID复用误关联或Unload时回调仍可达”。第8章
  进程和线程通知保存首个分岔与竞争性解释，停止新请求并反向清理后以同输入重新满足“注册代次内事件不重不乱，丢弃可计数，停止新事件后队列与引用归零”。样章外原文、未测build和未捕获竞态保留为未知。
</Answer>

**问题 3：8.2.1 DriverEntry例程**

为第8章 进程和线程通知的 WKP2-C08-D 设计一个中文版一版/作者二版/目标平台差分、签名参考基线、唯一故障、首个对象或调用链信号和恢复断言，并说明8·2·1 DriverEntry例程的IRQL或用户边界。

<Answer>
  第8章
  进程和线程通知先冻结完整build、架构、工具链、驱动包、签名、VBS/HVCI、符号、VM和输入，把
  WKP2-C08-D
  映射到对象身份、所有者、IRQL、控制前值与预期信号；只注入“回调内阻塞、事件队列无界、PID复用误关联或Unload时回调仍可达”。第8章
  进程和线程通知保存首个分岔与竞争性解释，停止新请求并反向清理后以同输入重新满足“注册代次内事件不重不乱，丢弃可计数，停止新事件后队列与引用归零”。样章外原文、未测build和未捕获竞态保留为未知。
</Answer>

**问题 4：8.2.2 处理进程退出通知**

为第8章 进程和线程通知的 WKP2-C08-E 设计一个中文版一版/作者二版/目标平台差分、签名参考基线、唯一故障、首个对象或调用链信号和恢复断言，并说明8·2·2 处理进程退出通知的IRQL或用户边界。

<Answer>
  第8章
  进程和线程通知先冻结完整build、架构、工具链、驱动包、签名、VBS/HVCI、符号、VM和输入，把
  WKP2-C08-E
  映射到对象身份、所有者、IRQL、控制前值与预期信号；只注入“回调内阻塞、事件队列无界、PID复用误关联或Unload时回调仍可达”。第8章
  进程和线程通知保存首个分岔与竞争性解释，停止新请求并反向清理后以同输入重新满足“注册代次内事件不重不乱，丢弃可计数，停止新事件后队列与引用归零”。样章外原文、未测build和未捕获竞态保留为未知。
</Answer>

**问题 5：8.2.3 处理进程创建通知**

为第8章 进程和线程通知的 WKP2-C08-F 设计一个中文版一版/作者二版/目标平台差分、签名参考基线、唯一故障、首个对象或调用链信号和恢复断言，并说明8·2·3 处理进程创建通知的IRQL或用户边界。

<Answer>
  第8章
  进程和线程通知先冻结完整build、架构、工具链、驱动包、签名、VBS/HVCI、符号、VM和输入，把
  WKP2-C08-F
  映射到对象身份、所有者、IRQL、控制前值与预期信号；只注入“回调内阻塞、事件队列无界、PID复用误关联或Unload时回调仍可达”。第8章
  进程和线程通知保存首个分岔与竞争性解释，停止新请求并反向清理后以同输入重新满足“注册代次内事件不重不乱，丢弃可计数，停止新事件后队列与引用归零”。样章外原文、未测build和未捕获竞态保留为未知。
</Answer>

**问题 6：8.3 将数据提供给用户模式**

为第8章 进程和线程通知的 WKP2-C08-G 设计一个中文版一版/作者二版/目标平台差分、签名参考基线、唯一故障、首个对象或调用链信号和恢复断言，并说明8·3 将数据提供给用户模式的IRQL或用户边界。

<Answer>
  第8章
  进程和线程通知先冻结完整build、架构、工具链、驱动包、签名、VBS/HVCI、符号、VM和输入，把
  WKP2-C08-G
  映射到对象身份、所有者、IRQL、控制前值与预期信号；只注入“回调内阻塞、事件队列无界、PID复用误关联或Unload时回调仍可达”。第8章
  进程和线程通知保存首个分岔与竞争性解释，停止新请求并反向清理后以同输入重新满足“注册代次内事件不重不乱，丢弃可计数，停止新事件后队列与引用归零”。样章外原文、未测build和未捕获竞态保留为未知。
</Answer>

**问题 7：8.4 线程通知**

为第8章 进程和线程通知的 WKP2-C08-H 设计一个中文版一版/作者二版/目标平台差分、签名参考基线、唯一故障、首个对象或调用链信号和恢复断言，并说明8·4 线程通知的IRQL或用户边界。

<Answer>
  第8章
  进程和线程通知先冻结完整build、架构、工具链、驱动包、签名、VBS/HVCI、符号、VM和输入，把
  WKP2-C08-H
  映射到对象身份、所有者、IRQL、控制前值与预期信号；只注入“回调内阻塞、事件队列无界、PID复用误关联或Unload时回调仍可达”。第8章
  进程和线程通知保存首个分岔与竞争性解释，停止新请求并反向清理后以同输入重新满足“注册代次内事件不重不乱，丢弃可计数，停止新事件后队列与引用归零”。样章外原文、未测build和未捕获竞态保留为未知。
</Answer>

**问题 8：8.5 映像加载通知**

为第8章 进程和线程通知的 WKP2-C08-I 设计一个中文版一版/作者二版/目标平台差分、签名参考基线、唯一故障、首个对象或调用链信号和恢复断言，并说明8·5 映像加载通知的IRQL或用户边界。

<Answer>
  第8章
  进程和线程通知先冻结完整build、架构、工具链、驱动包、签名、VBS/HVCI、符号、VM和输入，把
  WKP2-C08-I
  映射到对象身份、所有者、IRQL、控制前值与预期信号；只注入“回调内阻塞、事件队列无界、PID复用误关联或Unload时回调仍可达”。第8章
  进程和线程通知保存首个分岔与竞争性解释，停止新请求并反向清理后以同输入重新满足“注册代次内事件不重不乱，丢弃可计数，停止新事件后队列与引用归零”。样章外原文、未测build和未捕获竞态保留为未知。
</Answer>

**问题 9：8.7 总结**

为第8章 进程和线程通知的 WKP2-C08-J 设计一个中文版一版/作者二版/目标平台差分、签名参考基线、唯一故障、首个对象或调用链信号和恢复断言，并说明8·7 总结的IRQL或用户边界。

<Answer>
  第8章
  进程和线程通知先冻结完整build、架构、工具链、驱动包、签名、VBS/HVCI、符号、VM和输入，把
  WKP2-C08-J
  映射到对象身份、所有者、IRQL、控制前值与预期信号；只注入“回调内阻塞、事件队列无界、PID复用误关联或Unload时回调仍可达”。第8章
  进程和线程通知保存首个分岔与竞争性解释，停止新请求并反向清理后以同输入重新满足“注册代次内事件不重不乱，丢弃可计数，停止新事件后队列与引用归零”。样章外原文、未测build和未捕获竞态保留为未知。
</Answer>

**问题 10：为什么中文版185坐标不能静默扩成作者目录204坐标**

第8章 进程和线程通知应怎样处理19项来源差异、18页样章和作者二版？

<Answer>
  中文版185坐标是当前译本治理分母；作者一版公开目录204坐标用来发现19项差异，18页样章只支持开放页，二版和MIT仓库只支持迁移。第8章
  进程和线程通知必须把差异登记为待出版社正文核实，不能把作者更新、开放样章或二版示例冒充中文版缺失内容。
</Answer>

**问题 11：什么时候必须停止内核实验**

第8章 进程和线程通知缺少哪些条件时不能继续？

<Answer>
  第8章
  进程和线程通知缺少可丢弃VM或专用测试机、快照、备份、宿主内核调试器、转储、恢复启动、Verifier
  reset、正确符号、完整build/驱动包身份或稳定基线中的任一项就停止。生产设备、日常主机以及无法承受蓝屏、数据损坏、设备失联或启动失败的对象不在实验范围内。
</Answer>

</Exercises>

## 六个裁决术语

第8章 进程和线程通知使用<Term def="第8章 进程和线程通知的Windows完整build、架构、SDK、WDK、工具链、驱动包、签名与安全状态">目标身份</Term>、<Term def="第8章 进程和线程通知对象所处的线程/进程、IRQL、可分页性、等待、锁与用户缓冲区边界">执行上下文</Term>、<Term def="第8章 进程和线程通知中Driver、Device、File、IRP、callback、filter、instance或context的所有权关系">驱动对象链</Term>、<Term def="第8章 进程和线程通知单故障轨迹相对签名参考驱动最早出现状态、NTSTATUS或调用链差异的位置">首个分岔</Term>、<Term def="第8章 进程和线程通知把中文版185坐标、作者一版204坐标、二版与目标平台分开的显式记录">出版轨差异</Term>、<Term def="第8章 进程和线程通知反向清理或恢复快照后用原驱动包与输入恢复对象、引用、队列和信号的断言">同输入恢复</Term>构成最小证据语言；这些术语指向真实build、对象、IRQL、调用链与恢复，不生成成熟度、风险或性能综合分。

<Glossary>
  <GlossaryItem term="目标身份">
    第8章
    进程和线程通知的Windows完整build、架构、SDK、WDK、工具链、驱动包、签名与安全状态。
  </GlossaryItem>
  <GlossaryItem term="执行上下文">
    第8章
    进程和线程通知对象所处的线程/进程、IRQL、可分页性、等待、锁与用户缓冲区边界。
  </GlossaryItem>
  <GlossaryItem term="驱动对象链">
    第8章
    进程和线程通知中Driver、Device、File、IRP、callback、filter、instance或context的所有权关系。
  </GlossaryItem>
  <GlossaryItem term="首个分岔">
    第8章
    进程和线程通知单故障轨迹相对签名参考驱动最早出现状态、NTSTATUS或调用链差异的位置。
  </GlossaryItem>
  <GlossaryItem term="出版轨差异">
    第8章
    进程和线程通知把中文版185坐标、作者一版204坐标、二版与目标平台分开的显式记录。
  </GlossaryItem>
  <GlossaryItem term="同输入恢复">
    第8章
    进程和线程通知反向清理或恢复快照后用原驱动包与输入恢复对象、引用、队列和信号的断言。
  </GlossaryItem>
</Glossary>

<Attribution
  mode="independent-rewrite"
  sourceBasis="authorized-sample"
  workTitle="Pavel Yosifovich《Windows Kernel Programming》与李亮译《Windows内核编程》"
  adaptedUrl="https://leanpub.com/windowskernelprogramming"
/>
