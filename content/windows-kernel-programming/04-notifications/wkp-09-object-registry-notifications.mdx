---
title: "第9章 对象和注册表通知"
type: C
section: "监控 · 系统通知"
order: 9
description: "用对象操作前后回调构建进程保护驱动，再实现注册表前后通知、策略管理、客户端控制和性能约束。 覆盖17个本页目录坐标。"
demo: true
math: false
sourceUrl: "https://leanpub.com/windowskernelprogramming"
draft: true
---

import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第9章 对象和注册表通知的核心内核对象、控制流和全书位置
- 能描述“注册对象回调 → 管理保护集合 → 过滤句柄权限 → 注册表前后观察 → 客户端更新策略 → 注销并等待回调”的输入输出、IRQL与资源生存期
- 能实现一个在隔离虚拟机中可重放、可故障注入的最小驱动实验
- 能比较基线、边界、失败与恢复轨迹并判断不变量是否成立

</Objectives>

## 直觉起点

<Term def="Ob回调连接注册对象回调阶段的IRQL、对象、请求和恢复证据。">Ob回调</Term>、<Term def="进程保护连接管理保护集合阶段的IRQL、对象、请求和恢复证据。">进程保护</Term>、<Term def="访问掩码连接过滤句柄权限阶段的IRQL、对象、请求和恢复证据。">访问掩码</Term>、<Term def="注册表回调连接注册表前后观察阶段的IRQL、对象、请求和恢复证据。">注册表回调</Term>、<Term def="策略客户端连接客户端更新策略阶段的IRQL、对象、请求和恢复证据。">策略客户端</Term>
共同构成本页坐标。用对象操作前后回调构建进程保护驱动，再实现注册表前后通知、策略管理、客户端控制和性能约束。

内核编程必须同时画三张图：调用图说明用户请求、系统回调或I/O如何进入驱动；对象图说明DriverObject、DeviceObject、IRP、回调Cookie、上下文和池块由谁拥有；时间图说明注册、并发回调、注销、排空和卸载的先后。少一张图，表面可用的驱动也可能在压力或卸载时蓝屏。

## 常见误区与故障注入

<Callout type="trap">
  **误区一：驱动加载成功就说明内核代码正确。**
  很多错误只在缓冲边界、并发回调、错误IRQL或卸载时出现。必须启用定向Verifier并验证完成、引用、池和锁序。
</Callout>

<Callout type="trap">
  **误区二：把这本书改写成WDM/WDF或硬件驱动通论。**
  正式目录的主线是软件内核驱动、系统通知、小过滤器和设备过滤；PnP、MDL和WDF只能在相关机制处作对照，不能冒充原章节。
</Callout>

## 四类样本与验收

| 样本 | 注入方式                   | 必查证据                 | 通过条件     |
| ---- | -------------------------- | ------------------------ | ------------ |
| 基线 | 固定快照、sys/PDB和输入    | 状态码、IRQL、对象与日志 | 与预测一致   |
| 边界 | 空缓冲、短输出、断开客户端 | 长度、Information、引用  | 不越界不泄漏 |
| 失败 | 错IRQL、竞态、Verifier规则 | 首错栈和责任层           | 故障可定位   |
| 恢复 | 清理或快照后同输入重放     | 模块、服务、池、回调状态 | 回到基线     |

## 练习

<Exercises>

**问题 1** 为什么不能只看用户模式输出验收第9章 对象和注册表通知？

<Answer>
  输出可能隐藏错误IRQL、重复完成、悬空回调、池泄漏和卸载竞态。必须同时检查NTSTATUS/Information、内核序号日志、对象引用、Verifier和恢复重放。
</Answer>

**问题 2** 如何把“第9章 对象和注册表通知”变成可执行证据？

<Answer>
  沿“注册对象回调、管理保护集合、过滤句柄权限、注册表前后观察、客户端更新策略、注销并等待回调”固定虚拟机、WDK、sys/PDB和输入，预测调用线程、IRQL、对象与状态码，只改变一个条件并保存首错；恢复后比较IRP、回调、池或过滤上下文。
</Answer>

**问题 3** 本页最小通过条件是什么？

<Answer>
  保护策略快照并发安全，前回调只削减允许权限，后回调不伪造结果；注册表回调尊重通知类别和递归边界。
  失败路径不得污染下一轮，卸载与清理明确，相同输入和事件序列能够重新到达基线。
</Answer>

</Exercises>

## 小结

- Ob回调：连接“注册对象回调”的内核契约与证据。
- 进程保护：连接“管理保护集合”的内核契约与证据。
- 访问掩码：连接“过滤句柄权限”的内核契约与证据。
- 注册表回调：连接“注册表前后观察”的内核契约与证据。
- 策略客户端：连接“客户端更新策略”的内核契约与证据。
- 本页17个目录坐标全部进入实验链，不以泛化驱动主题替代原书内容。

## 术语表

<Glossary>
  <GlossaryItem term="Ob回调">
    Ob回调用于解释“注册对象回调”，需要同时说明调用契约、IRQL、对象或缓冲生存期、首错与清理方法。
  </GlossaryItem>
  <GlossaryItem term="进程保护">
    进程保护用于解释“管理保护集合”，需要同时说明调用契约、IRQL、对象或缓冲生存期、首错与清理方法。
  </GlossaryItem>
  <GlossaryItem term="访问掩码">
    访问掩码用于解释“过滤句柄权限”，需要同时说明调用契约、IRQL、对象或缓冲生存期、首错与清理方法。
  </GlossaryItem>
  <GlossaryItem term="注册表回调">
    注册表回调用于解释“注册表前后观察”，需要同时说明调用契约、IRQL、对象或缓冲生存期、首错与清理方法。
  </GlossaryItem>
  <GlossaryItem term="策略客户端">
    策略客户端用于解释“客户端更新策略”，需要同时说明调用契约、IRQL、对象或缓冲生存期、首错与清理方法。
  </GlossaryItem>
</Glossary>

<Attribution
  adaptedFrom="Pavel Yosifovich 著、李亮译 · Windows内核编程 · 机械工业出版社 · 权威目录独立教学重构"
  adaptedUrl="https://leanpub.com/windowskernelprogramming"
/>
