---
title: "第11章 其他主题"
type: C
section: "项目 · 过滤驱动与验证"
order: 11
description: "综合驱动签名、Driver Verifier、原生API、传统过滤驱动、设备监视器、驱动挂钩和内核库，明确支持边界。 覆盖20个本页目录坐标。"
demo: true
math: false
sourceUrl: "https://leanpub.com/windowskernelprogramming"
draft: true
---

import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第11章 其他主题的核心内核对象、控制流和全书位置
- 能描述“建立签名信任 → 启用定向Verifier → 调用原生API → 附加过滤设备 → 监视转发请求 → 评估挂钩与内核库”的输入输出、IRQL与资源生存期
- 能实现一个在隔离虚拟机中可重放、可故障注入的最小驱动实验
- 能比较基线、边界、失败与恢复轨迹并判断不变量是否成立

</Objectives>

## 直觉起点

<Term def="驱动签名连接建立签名信任阶段的IRQL、对象、请求和恢复证据。">驱动签名</Term>、<Term def="Driver Verifier连接启用定向Verifier阶段的IRQL、对象、请求和恢复证据。">Driver Verifier</Term>、<Term def="Native API连接调用原生API阶段的IRQL、对象、请求和恢复证据。">Native API</Term>、<Term def="过滤设备连接附加过滤设备阶段的IRQL、对象、请求和恢复证据。">过滤设备</Term>、<Term def="驱动挂钩连接监视转发请求阶段的IRQL、对象、请求和恢复证据。">驱动挂钩</Term>
共同构成本页坐标。综合驱动签名、Driver
Verifier、原生API、传统过滤驱动、设备监视器、驱动挂钩和内核库，明确支持边界。

内核编程必须同时画三张图：调用图说明用户请求、系统回调或I/O如何进入驱动；对象图说明DriverObject、DeviceObject、IRP、回调Cookie、上下文和池块由谁拥有；时间图说明注册、并发回调、注销、排空和卸载的先后。少一张图，表面可用的驱动也可能在压力或卸载时蓝屏。

## 常见误区与故障注入

<Callout type="trap">
  **误区一：驱动加载成功就说明内核代码正确。**
  很多错误只在缓冲边界、并发回调、错误IRQL或卸载时出现。必须启用定向Verifier并验证完成、引用、池和锁序。
</Callout>

<Callout type="trap">
  **误区二：把这本书改写成WDM/WDF或硬件驱动通论。**
  正式目录的主线是软件内核驱动、系统通知、小过滤器和设备过滤；PnP、MDL和WDF只能在相关机制处作对照，不能冒充原章节。
</Callout>

## 四类样本与验收

| 样本 | 注入方式                   | 必查证据                 | 通过条件     |
| ---- | -------------------------- | ------------------------ | ------------ |
| 基线 | 固定快照、sys/PDB和输入    | 状态码、IRQL、对象与日志 | 与预测一致   |
| 边界 | 空缓冲、短输出、断开客户端 | 长度、Information、引用  | 不越界不泄漏 |
| 失败 | 错IRQL、竞态、Verifier规则 | 首错栈和责任层           | 故障可定位   |
| 恢复 | 清理或快照后同输入重放     | 模块、服务、池、回调状态 | 回到基线     |

## 练习

<Exercises>

**问题 1** 为什么不能只看用户模式输出验收第11章 其他主题？

<Answer>
  输出可能隐藏错误IRQL、重复完成、悬空回调、池泄漏和卸载竞态。必须同时检查NTSTATUS/Information、内核序号日志、对象引用、Verifier和恢复重放。
</Answer>

**问题 2** 如何把“第11章 其他主题”变成可执行证据？

<Answer>
  沿“建立签名信任、启用定向Verifier、调用原生API、附加过滤设备、监视转发请求、评估挂钩与内核库”固定虚拟机、WDK、sys/PDB和输入，预测调用线程、IRQL、对象与状态码，只改变一个条件并保存首错；恢复后比较IRP、回调、池或过滤上下文。
</Answer>

**问题 3** 本页最小通过条件是什么？

<Answer>
  签名链与启动策略匹配，Verifier配置可复现，过滤设备附加/移除有序；不使用不受支持的挂钩作为生产方案。
  失败路径不得污染下一轮，卸载与清理明确，相同输入和事件序列能够重新到达基线。
</Answer>

</Exercises>

## 小结

- 驱动签名：连接“建立签名信任”的内核契约与证据。
- Driver Verifier：连接“启用定向Verifier”的内核契约与证据。
- Native API：连接“调用原生API”的内核契约与证据。
- 过滤设备：连接“附加过滤设备”的内核契约与证据。
- 驱动挂钩：连接“监视转发请求”的内核契约与证据。
- 本页20个目录坐标全部进入实验链，不以泛化驱动主题替代原书内容。

## 术语表

<Glossary>
  <GlossaryItem term="驱动签名">
    驱动签名用于解释“建立签名信任”，需要同时说明调用契约、IRQL、对象或缓冲生存期、首错与清理方法。
  </GlossaryItem>
  <GlossaryItem term="Driver Verifier">
    Driver
    Verifier用于解释“启用定向Verifier”，需要同时说明调用契约、IRQL、对象或缓冲生存期、首错与清理方法。
  </GlossaryItem>
  <GlossaryItem term="Native API">
    Native
    API用于解释“调用原生API”，需要同时说明调用契约、IRQL、对象或缓冲生存期、首错与清理方法。
  </GlossaryItem>
  <GlossaryItem term="过滤设备">
    过滤设备用于解释“附加过滤设备”，需要同时说明调用契约、IRQL、对象或缓冲生存期、首错与清理方法。
  </GlossaryItem>
  <GlossaryItem term="驱动挂钩">
    驱动挂钩用于解释“监视转发请求”，需要同时说明调用契约、IRQL、对象或缓冲生存期、首错与清理方法。
  </GlossaryItem>
</Glossary>

<Attribution
  adaptedFrom="Pavel Yosifovich 著、李亮译 · Windows内核编程 · 机械工业出版社 · 权威目录独立教学重构"
  adaptedUrl="https://leanpub.com/windowskernelprogramming"
/>
