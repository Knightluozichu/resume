---
title: "第 2 章 this 全面解析"
type: A
section: "上卷 · 第二部分 this 和对象原型"
order: 10
description: "this 绑定按 new、显式、隐式、默认的优先级判定，并受绑定丢失、间接引用、装箱和箭头函数等例外影响；判断必须从调用点开始。"
demo: true
math: false
sourceUrl: "https://github.com/getify/You-Dont-Know-JS/blob/1st-ed/this%20%26%20object%20prototypes/ch2.md"
draft: false
---


## 直觉：先预测，再运行

this 绑定按 new、显式、隐式、默认的优先级判定，并受绑定丢失、间接引用、装箱和箭头函数等例外影响；判断必须从调用点开始。

先预测：只看最后输出而不记录中间绑定与调度顺序，最可能把哪个机制误判成语法偶然？请先写下预期，再切换样本比较首个偏离点。

## 权威目录定位

本页对应作者开放的一版仓库目录。目录标题仅用于一对一追踪，正文为独立教学重构：

- Chapter 2: this All Makes Sense Now!
- Call-site
- Nothing But Rules
- Everything In Order
- Binding Exceptions
- Lexical this

本页逐项覆盖 6 个公开目录条目；全系列基线为中文版三卷、英文一版六册、40 个正式单元和 211 个条目。学习地图与总复习是站内导航页，不冒充原书章节。

## 核心概念与状态所有权

。

。

。

。

。

这些概念不是孤立名词。每一个都要回答五个问题：输入由谁创建，绑定或状态保存在哪里，哪一步读取或修改它，失败怎样传播，结束后由谁清理。只有五项同时可观察，机制解释才算闭环。

|   # | 观察量     | 本页判定方式                                               |
| --: | ---------- | ---------------------------------------------------------- |
|   1 | 默认绑定   | 在“定位调用点”阶段保存前后状态并核对不变量                 |
|   2 | 隐式绑定   | 在“先检查 new 调用”阶段保存前后状态并核对不变量            |
|   3 | 显式绑定   | 在“再检查 call、apply 或 bind”阶段保存前后状态并核对不变量 |
|   4 | new 绑定   | 在“再检查对象成员调用”阶段保存前后状态并核对不变量         |
|   5 | 绑定优先级 | 在“否则应用默认绑定”阶段保存前后状态并核对不变量           |

## 机制拆解

### 1. 定位调用点

定位调用点不是口号，而是可执行检查点。输入必须来自上一阶段已经签发的状态；执行时记录当前绑定、接收者、值类型、队列位置或规范版本；输出必须能被下一阶段消费。若本阶段失败，应立即保存首个偏离点，不能让错误状态继续传播。

结合本章主张来看：this 绑定按 new、显式、隐式、默认的优先级判定，并受绑定丢失、间接引用、装箱和箭头函数等例外影响；判断必须从调用点开始。 因此本阶段至少要保留一份正常样本和一份反例。两者可以得到相同最终值，但只要中间查找路径、调用点、转换步骤或时间顺序不同，就不能判为同一机制。

### 2. 先检查 new 调用

先检查 new 调用不是口号，而是可执行检查点。输入必须来自上一阶段已经签发的状态；执行时记录当前绑定、接收者、值类型、队列位置或规范版本；输出必须能被下一阶段消费。若本阶段失败，应立即保存首个偏离点，不能让错误状态继续传播。

结合本章主张来看：this 绑定按 new、显式、隐式、默认的优先级判定，并受绑定丢失、间接引用、装箱和箭头函数等例外影响；判断必须从调用点开始。 因此本阶段至少要保留一份正常样本和一份反例。两者可以得到相同最终值，但只要中间查找路径、调用点、转换步骤或时间顺序不同，就不能判为同一机制。

### 3. 再检查 call、apply 或 bind

再检查 call、apply 或 bind不是口号，而是可执行检查点。输入必须来自上一阶段已经签发的状态；执行时记录当前绑定、接收者、值类型、队列位置或规范版本；输出必须能被下一阶段消费。若本阶段失败，应立即保存首个偏离点，不能让错误状态继续传播。

结合本章主张来看：this 绑定按 new、显式、隐式、默认的优先级判定，并受绑定丢失、间接引用、装箱和箭头函数等例外影响；判断必须从调用点开始。 因此本阶段至少要保留一份正常样本和一份反例。两者可以得到相同最终值，但只要中间查找路径、调用点、转换步骤或时间顺序不同，就不能判为同一机制。

### 4. 再检查对象成员调用

再检查对象成员调用不是口号，而是可执行检查点。输入必须来自上一阶段已经签发的状态；执行时记录当前绑定、接收者、值类型、队列位置或规范版本；输出必须能被下一阶段消费。若本阶段失败，应立即保存首个偏离点，不能让错误状态继续传播。

结合本章主张来看：this 绑定按 new、显式、隐式、默认的优先级判定，并受绑定丢失、间接引用、装箱和箭头函数等例外影响；判断必须从调用点开始。 因此本阶段至少要保留一份正常样本和一份反例。两者可以得到相同最终值，但只要中间查找路径、调用点、转换步骤或时间顺序不同，就不能判为同一机制。

### 5. 否则应用默认绑定

否则应用默认绑定不是口号，而是可执行检查点。输入必须来自上一阶段已经签发的状态；执行时记录当前绑定、接收者、值类型、队列位置或规范版本；输出必须能被下一阶段消费。若本阶段失败，应立即保存首个偏离点，不能让错误状态继续传播。

结合本章主张来看：this 绑定按 new、显式、隐式、默认的优先级判定，并受绑定丢失、间接引用、装箱和箭头函数等例外影响；判断必须从调用点开始。 因此本阶段至少要保留一份正常样本和一份反例。两者可以得到相同最终值，但只要中间查找路径、调用点、转换步骤或时间顺序不同，就不能判为同一机制。

### 6. 最后核对箭头与软绑定例外

最后核对箭头与软绑定例外不是口号，而是可执行检查点。输入必须来自上一阶段已经签发的状态；执行时记录当前绑定、接收者、值类型、队列位置或规范版本；输出必须能被下一阶段消费。若本阶段失败，应立即保存首个偏离点，不能让错误状态继续传播。

结合本章主张来看：this 绑定按 new、显式、隐式、默认的优先级判定，并受绑定丢失、间接引用、装箱和箭头函数等例外影响；判断必须从调用点开始。 因此本阶段至少要保留一份正常样本和一份反例。两者可以得到相同最终值，但只要中间查找路径、调用点、转换步骤或时间顺序不同，就不能判为同一机制。

## 目录主题逐项讲解

### Chapter 2: this All Makes Sense Now!

Chapter 2: this All Makes Sense Now!在本单元中落到“定位调用点”这一观察点。阅读时先标出参与者与状态，再写出执行前、执行中和执行后的差异；随后主动制造空值、重复调用、错误接收者、迟到结果或旧环境能力缺失，验证解释是否仍成立。

### Call-site

Call-site在本单元中落到“先检查 new 调用”这一观察点。阅读时先标出参与者与状态，再写出执行前、执行中和执行后的差异；随后主动制造空值、重复调用、错误接收者、迟到结果或旧环境能力缺失，验证解释是否仍成立。

### Nothing But Rules

Nothing But Rules在本单元中落到“再检查 call、apply 或 bind”这一观察点。阅读时先标出参与者与状态，再写出执行前、执行中和执行后的差异；随后主动制造空值、重复调用、错误接收者、迟到结果或旧环境能力缺失，验证解释是否仍成立。

### Everything In Order

Everything In Order在本单元中落到“再检查对象成员调用”这一观察点。阅读时先标出参与者与状态，再写出执行前、执行中和执行后的差异；随后主动制造空值、重复调用、错误接收者、迟到结果或旧环境能力缺失，验证解释是否仍成立。

### Binding Exceptions

Binding Exceptions在本单元中落到“否则应用默认绑定”这一观察点。阅读时先标出参与者与状态，再写出执行前、执行中和执行后的差异；随后主动制造空值、重复调用、错误接收者、迟到结果或旧环境能力缺失，验证解释是否仍成立。

### Lexical this

Lexical this在本单元中落到“最后核对箭头与软绑定例外”这一观察点。阅读时先标出参与者与状态，再写出执行前、执行中和执行后的差异；随后主动制造空值、重复调用、错误接收者、迟到结果或旧环境能力缺失，验证解释是否仍成立。

## 六阶段证据链

本页的可执行链条是：定位调用点 → 先检查 new 调用 → 再检查 call、apply 或 bind → 再检查对象成员调用 → 否则应用默认绑定 → 最后核对箭头与软绑定例外。正常样本证明主路径，边界样本证明容量与身份，失败样本定位停止点，恢复样本证明旧绑定、旧值、旧任务和旧资源没有污染下一次执行。


## 最小可运行实验

下面的片段只保留本章最小机制。运行前先预测每一行的绑定、类型与输出，再修改一个条件观察首个偏离点：

```js
function show() {
  return this.value;
}
const obj = { value: 2, show };
console.log(obj.show());
console.log(show.call({ value: 3 }));
const bound = show.bind({ value: 4 });
```

```yaml
book: 你不知道的 JavaScript（一版三卷六册）
page: ydk-this-02-this-all-makes-sense
role: chapter
official_entries: 6
sample: normal | boundary | failure | recovery
first_divergence: null
bindings_stable: true
ordering_explained: true
cleanup_complete: true
```

```text
freeze source + runtime + input
predict bindings + values + call-site + queue
inject one controlled fault
stop at first divergence
recover and replay the same sample
assert stable state and no stale work
```

## 常见误区与故障注入


## 四类样本与验收

| 样本 | 注入方式                           | 必查证据               | 通过条件                 |
| ---- | ---------------------------------- | ---------------------- | ------------------------ |
| 正常 | 规范输入与单次执行                 | 绑定、值、调用点、队列 | 六阶段按预期完成         |
| 边界 | 空值、重复键、极值或嵌套调用       | 查找终点与类型边界     | 不发生隐式越界           |
| 失败 | 错误接收者、拒绝、旧提案或迟到结果 | 首个偏离点             | 明确停止、传播或降级     |
| 恢复 | 修复后重放完全相同输入             | 清理后的状态与任务数   | 无旧绑定、旧任务和旧资源 |

本页签发不变量是：**this 绑定按 new、显式、隐式、默认的优先级判定，并受绑定丢失、间接引用、装箱和箭头函数等例外影响；判断必须从调用点开始。** 最终输出、单次截图或一次性能数字都不能替代作用域查找、值转换、调用绑定、任务顺序和版本状态的完整轨迹。

## 版本与原书边界

本单元以 2014 至 2015 年一版目录为结构基线。正文使用现代可运行示例独立重构核心机制；现代规范差异单独披露，不修改或伪造原书目录。

站内示例优先使用今天可运行且语义稳定的写法，但目录清单、章节归属和历史判断仍以作者一版仓库为准。对已经撤回、替代或标准化后发生变化的能力，本页要求同时写出原书快照和当前状态。

## 练习


## 小结

- **默认绑定**：对应“定位调用点”阶段的核心观察量。
- **隐式绑定**：对应“先检查 new 调用”阶段的核心观察量。
- **显式绑定**：对应“再检查 call、apply 或 bind”阶段的核心观察量。
- **new 绑定**：对应“再检查对象成员调用”阶段的核心观察量。
- **绑定优先级**：对应“否则应用默认绑定”阶段的核心观察量。
- 六阶段机制链已映射到正常、边界、失败与恢复证据。
- 一版目录与现代规范状态分栏记录，后续变化不会冒充原书章节。

## 术语表


## 来源与改编边界

