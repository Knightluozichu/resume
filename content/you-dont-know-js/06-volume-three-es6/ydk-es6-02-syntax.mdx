---
title: "第 2 章 语法"
type: A
section: "下卷 · 第二部分 ES6 及未来"
order: 34
description: "ES6 语法围绕更精确的作用域、结构化数据交换、可迭代协议、函数上下文和 Unicode 正确性展开；每种简写都对应明确的求值顺序与边界。"
demo: true
math: false
sourceUrl: "https://github.com/getify/You-Dont-Know-JS/blob/1st-ed/es6%20%26%20beyond/ch2.md"
draft: false
---

import {
  YdkEs602SyntaxMapLab,
  YdkEs602SyntaxExperimentLab,
  YdkEs602SyntaxEvidenceLab,
} from "@/components/mdx/you-dont-know-js/diagrams/ydk-es6-02-syntax";
import {
  Objectives,
  Callout,
  Glossary,
  GlossaryItem,
  Term,
  Exercises,
  Answer,
  Stepper,
  Step,
  Attribution,
} from "@/components/mdx/mdx-components";

<Objectives>

- 能解释第 2 章 语法的核心主张，并把它放回三卷六册的机制主线
- 能比较正常、边界、失败和恢复样本，指出第一个作用域、值、调用或时间偏离点
- 能实现本单元最小实验，记录输入、绑定、求值顺序、输出与资源所有权
- 能设计证据回答“ES6 语法围绕更精确的作用域、结构化数据交换、可迭代协议、函数上下文和 Unicode 正确性展开；每种简写都对应明确的求值顺序与边界。”是否成立

</Objectives>

## 直觉：先预测，再运行

ES6 语法围绕更精确的作用域、结构化数据交换、可迭代协议、函数上下文和 Unicode 正确性展开；每种简写都对应明确的求值顺序与边界。

先预测：只看最后输出而不记录中间绑定与调度顺序，最可能把哪个机制误判成语法偶然？请先写下预期，再切换样本比较首个偏离点。

<YdkEs602SyntaxMapLab />

## 权威目录定位

本页对应作者开放的一版仓库目录。目录标题仅用于一对一追踪，正文为独立教学重构：

- Chapter 2: Syntax
- Block-Scoped Declarations
- Spread / Rest
- Default Parameter Values
- Destructuring
- Object Literal Extensions
- Template Literals
- Arrow Functions
- for..of Loops
- Regular Expression Extensions
- Number Literal Extensions
- Unicode
- Symbols

本页逐项覆盖 13 个公开目录条目；全系列基线为中文版三卷、英文一版六册、40 个正式单元和 211 个条目。学习地图与总复习是站内导航页，不冒充原书章节。

## 核心概念与状态所有权

<Term def="块级声明是本单元机制模型中的关键观察量，位于“用 let 与 const 建立块边界”阶段。判断它是否正确，必须同时记录输入、当前状态、输出、失败路径与清理责任，不能只依据最终打印值。">块级声明</Term>。

<Term def="展开与剩余是本单元机制模型中的关键观察量，位于“用解构声明数据形状”阶段。判断它是否正确，必须同时记录输入、当前状态、输出、失败路径与清理责任，不能只依据最终打印值。">展开与剩余</Term>。

<Term def="解构是本单元机制模型中的关键观察量，位于“用默认值处理 undefined”阶段。判断它是否正确，必须同时记录输入、当前状态、输出、失败路径与清理责任，不能只依据最终打印值。">解构</Term>。

<Term def="模板字面量是本单元机制模型中的关键观察量，位于“用展开和剩余搬运序列”阶段。判断它是否正确，必须同时记录输入、当前状态、输出、失败路径与清理责任，不能只依据最终打印值。">模板字面量</Term>。

<Term def="Symbol是本单元机制模型中的关键观察量，位于“用迭代协议遍历”阶段。判断它是否正确，必须同时记录输入、当前状态、输出、失败路径与清理责任，不能只依据最终打印值。">Symbol</Term>。

这些概念不是孤立名词。每一个都要回答五个问题：输入由谁创建，绑定或状态保存在哪里，哪一步读取或修改它，失败怎样传播，结束后由谁清理。只有五项同时可观察，机制解释才算闭环。

|   # | 观察量     | 本页判定方式                                               |
| --: | ---------- | ---------------------------------------------------------- |
|   1 | 块级声明   | 在“用 let 与 const 建立块边界”阶段保存前后状态并核对不变量 |
|   2 | 展开与剩余 | 在“用解构声明数据形状”阶段保存前后状态并核对不变量         |
|   3 | 解构       | 在“用默认值处理 undefined”阶段保存前后状态并核对不变量     |
|   4 | 模板字面量 | 在“用展开和剩余搬运序列”阶段保存前后状态并核对不变量       |
|   5 | Symbol     | 在“用迭代协议遍历”阶段保存前后状态并核对不变量             |

## 机制拆解

### 1. 用 let 与 const 建立块边界

用 let 与 const 建立块边界不是口号，而是可执行检查点。输入必须来自上一阶段已经签发的状态；执行时记录当前绑定、接收者、值类型、队列位置或规范版本；输出必须能被下一阶段消费。若本阶段失败，应立即保存首个偏离点，不能让错误状态继续传播。

结合本章主张来看：ES6 语法围绕更精确的作用域、结构化数据交换、可迭代协议、函数上下文和 Unicode 正确性展开；每种简写都对应明确的求值顺序与边界。 因此本阶段至少要保留一份正常样本和一份反例。两者可以得到相同最终值，但只要中间查找路径、调用点、转换步骤或时间顺序不同，就不能判为同一机制。

### 2. 用解构声明数据形状

用解构声明数据形状不是口号，而是可执行检查点。输入必须来自上一阶段已经签发的状态；执行时记录当前绑定、接收者、值类型、队列位置或规范版本；输出必须能被下一阶段消费。若本阶段失败，应立即保存首个偏离点，不能让错误状态继续传播。

结合本章主张来看：ES6 语法围绕更精确的作用域、结构化数据交换、可迭代协议、函数上下文和 Unicode 正确性展开；每种简写都对应明确的求值顺序与边界。 因此本阶段至少要保留一份正常样本和一份反例。两者可以得到相同最终值，但只要中间查找路径、调用点、转换步骤或时间顺序不同，就不能判为同一机制。

### 3. 用默认值处理 undefined

用默认值处理 undefined不是口号，而是可执行检查点。输入必须来自上一阶段已经签发的状态；执行时记录当前绑定、接收者、值类型、队列位置或规范版本；输出必须能被下一阶段消费。若本阶段失败，应立即保存首个偏离点，不能让错误状态继续传播。

结合本章主张来看：ES6 语法围绕更精确的作用域、结构化数据交换、可迭代协议、函数上下文和 Unicode 正确性展开；每种简写都对应明确的求值顺序与边界。 因此本阶段至少要保留一份正常样本和一份反例。两者可以得到相同最终值，但只要中间查找路径、调用点、转换步骤或时间顺序不同，就不能判为同一机制。

### 4. 用展开和剩余搬运序列

用展开和剩余搬运序列不是口号，而是可执行检查点。输入必须来自上一阶段已经签发的状态；执行时记录当前绑定、接收者、值类型、队列位置或规范版本；输出必须能被下一阶段消费。若本阶段失败，应立即保存首个偏离点，不能让错误状态继续传播。

结合本章主张来看：ES6 语法围绕更精确的作用域、结构化数据交换、可迭代协议、函数上下文和 Unicode 正确性展开；每种简写都对应明确的求值顺序与边界。 因此本阶段至少要保留一份正常样本和一份反例。两者可以得到相同最终值，但只要中间查找路径、调用点、转换步骤或时间顺序不同，就不能判为同一机制。

### 5. 用迭代协议遍历

用迭代协议遍历不是口号，而是可执行检查点。输入必须来自上一阶段已经签发的状态；执行时记录当前绑定、接收者、值类型、队列位置或规范版本；输出必须能被下一阶段消费。若本阶段失败，应立即保存首个偏离点，不能让错误状态继续传播。

结合本章主张来看：ES6 语法围绕更精确的作用域、结构化数据交换、可迭代协议、函数上下文和 Unicode 正确性展开；每种简写都对应明确的求值顺序与边界。 因此本阶段至少要保留一份正常样本和一份反例。两者可以得到相同最终值，但只要中间查找路径、调用点、转换步骤或时间顺序不同，就不能判为同一机制。

### 6. 用 Symbol 建立非字符串键

用 Symbol 建立非字符串键不是口号，而是可执行检查点。输入必须来自上一阶段已经签发的状态；执行时记录当前绑定、接收者、值类型、队列位置或规范版本；输出必须能被下一阶段消费。若本阶段失败，应立即保存首个偏离点，不能让错误状态继续传播。

结合本章主张来看：ES6 语法围绕更精确的作用域、结构化数据交换、可迭代协议、函数上下文和 Unicode 正确性展开；每种简写都对应明确的求值顺序与边界。 因此本阶段至少要保留一份正常样本和一份反例。两者可以得到相同最终值，但只要中间查找路径、调用点、转换步骤或时间顺序不同，就不能判为同一机制。

## 目录主题逐项讲解

### Chapter 2: Syntax

Chapter 2: Syntax在本单元中落到“用 let 与 const 建立块边界”这一观察点。阅读时先标出参与者与状态，再写出执行前、执行中和执行后的差异；随后主动制造空值、重复调用、错误接收者、迟到结果或旧环境能力缺失，验证解释是否仍成立。

### Block-Scoped Declarations

Block-Scoped Declarations在本单元中落到“用解构声明数据形状”这一观察点。阅读时先标出参与者与状态，再写出执行前、执行中和执行后的差异；随后主动制造空值、重复调用、错误接收者、迟到结果或旧环境能力缺失，验证解释是否仍成立。

### Spread / Rest

Spread / Rest在本单元中落到“用默认值处理 undefined”这一观察点。阅读时先标出参与者与状态，再写出执行前、执行中和执行后的差异；随后主动制造空值、重复调用、错误接收者、迟到结果或旧环境能力缺失，验证解释是否仍成立。

### Default Parameter Values

Default Parameter Values在本单元中落到“用展开和剩余搬运序列”这一观察点。阅读时先标出参与者与状态，再写出执行前、执行中和执行后的差异；随后主动制造空值、重复调用、错误接收者、迟到结果或旧环境能力缺失，验证解释是否仍成立。

### Destructuring

Destructuring在本单元中落到“用迭代协议遍历”这一观察点。阅读时先标出参与者与状态，再写出执行前、执行中和执行后的差异；随后主动制造空值、重复调用、错误接收者、迟到结果或旧环境能力缺失，验证解释是否仍成立。

### Object Literal Extensions

Object Literal Extensions在本单元中落到“用 Symbol 建立非字符串键”这一观察点。阅读时先标出参与者与状态，再写出执行前、执行中和执行后的差异；随后主动制造空值、重复调用、错误接收者、迟到结果或旧环境能力缺失，验证解释是否仍成立。

### Template Literals

Template Literals在本单元中落到“用 let 与 const 建立块边界”这一观察点。阅读时先标出参与者与状态，再写出执行前、执行中和执行后的差异；随后主动制造空值、重复调用、错误接收者、迟到结果或旧环境能力缺失，验证解释是否仍成立。

### Arrow Functions

Arrow Functions在本单元中落到“用解构声明数据形状”这一观察点。阅读时先标出参与者与状态，再写出执行前、执行中和执行后的差异；随后主动制造空值、重复调用、错误接收者、迟到结果或旧环境能力缺失，验证解释是否仍成立。

### for..of Loops

for..of Loops在本单元中落到“用默认值处理 undefined”这一观察点。阅读时先标出参与者与状态，再写出执行前、执行中和执行后的差异；随后主动制造空值、重复调用、错误接收者、迟到结果或旧环境能力缺失，验证解释是否仍成立。

### Regular Expression Extensions

Regular Expression Extensions在本单元中落到“用展开和剩余搬运序列”这一观察点。阅读时先标出参与者与状态，再写出执行前、执行中和执行后的差异；随后主动制造空值、重复调用、错误接收者、迟到结果或旧环境能力缺失，验证解释是否仍成立。

### Number Literal Extensions

Number Literal Extensions在本单元中落到“用迭代协议遍历”这一观察点。阅读时先标出参与者与状态，再写出执行前、执行中和执行后的差异；随后主动制造空值、重复调用、错误接收者、迟到结果或旧环境能力缺失，验证解释是否仍成立。

### Unicode

Unicode在本单元中落到“用 Symbol 建立非字符串键”这一观察点。阅读时先标出参与者与状态，再写出执行前、执行中和执行后的差异；随后主动制造空值、重复调用、错误接收者、迟到结果或旧环境能力缺失，验证解释是否仍成立。

### Symbols

Symbols在本单元中落到“用 let 与 const 建立块边界”这一观察点。阅读时先标出参与者与状态，再写出执行前、执行中和执行后的差异；随后主动制造空值、重复调用、错误接收者、迟到结果或旧环境能力缺失，验证解释是否仍成立。

## 六阶段证据链

本页的可执行链条是：用 let 与 const 建立块边界 → 用解构声明数据形状 → 用默认值处理 undefined → 用展开和剩余搬运序列 → 用迭代协议遍历 → 用 Symbol 建立非字符串键。正常样本证明主路径，边界样本证明容量与身份，失败样本定位停止点，恢复样本证明旧绑定、旧值、旧任务和旧资源没有污染下一次执行。

<Stepper>
  <Step title="1. 用 let 与 const 建立块边界与用解构声明数据形状">
    固定源码、运行时、输入和初始状态，逐行记录第一组机制证据。
    <YdkEs602SyntaxMapLab />
  </Step>
  <Step title="2. 用默认值处理 undefined与用展开和剩余搬运序列">
    注入边界与失败样本，在第一个无法满足契约的位置停止，不用最终输出掩盖过程。
    <YdkEs602SyntaxExperimentLab />
  </Step>
  <Step title="3. 用迭代协议遍历与用 Symbol 建立非字符串键">
    清除故障后重放相同输入，核对所有绑定、值、队列和资源已经收敛。
    <YdkEs602SyntaxEvidenceLab />
  </Step>
</Stepper>

## 最小可运行实验

下面的片段只保留本章最小机制。运行前先预测每一行的绑定、类型与输出，再修改一个条件观察首个偏离点：

```js
const source = { id: 7, tags: ["js", "es6"] };
const {
  id,
  tags: [first],
  missing = "default",
} = source;
const key = Symbol("private");
console.log({ id, first, missing, [key]: true });
```

```yaml
book: 你不知道的 JavaScript（一版三卷六册）
page: ydk-es6-02-syntax
role: chapter
official_entries: 13
sample: normal | boundary | failure | recovery
first_divergence: null
bindings_stable: true
ordering_explained: true
cleanup_complete: true
```

```text
freeze source + runtime + input
predict bindings + values + call-site + queue
inject one controlled fault
stop at first divergence
recover and replay the same sample
assert stable state and no stale work
```

## 常见误区与故障注入

<Callout type="trap">
  **误区一：记住“块级声明”的结论，却不展开“用 let 与 const 建立块边界”到“用
  Symbol 建立非字符串键”的过程。**
  相同输出可能来自错误绑定、隐式转换或迟到任务，必须保存中间证据。
</Callout>

<Callout type="trap">
  **误区二：把现代引擎结果直接写回 2014 至 2015 年原书目录。**
  目录忠实度与现代可用性是两条证据；前者锁定一版，后者在版本边界中单独说明。
</Callout>

## 四类样本与验收

| 样本 | 注入方式                           | 必查证据               | 通过条件                 |
| ---- | ---------------------------------- | ---------------------- | ------------------------ |
| 正常 | 规范输入与单次执行                 | 绑定、值、调用点、队列 | 六阶段按预期完成         |
| 边界 | 空值、重复键、极值或嵌套调用       | 查找终点与类型边界     | 不发生隐式越界           |
| 失败 | 错误接收者、拒绝、旧提案或迟到结果 | 首个偏离点             | 明确停止、传播或降级     |
| 恢复 | 修复后重放完全相同输入             | 清理后的状态与任务数   | 无旧绑定、旧任务和旧资源 |

<YdkEs602SyntaxEvidenceLab />

本页签发不变量是：**ES6 语法围绕更精确的作用域、结构化数据交换、可迭代协议、函数上下文和 Unicode 正确性展开；每种简写都对应明确的求值顺序与边界。** 最终输出、单次截图或一次性能数字都不能替代作用域查找、值转换、调用绑定、任务顺序和版本状态的完整轨迹。

## 版本与原书边界

本单元忠实保留原书讨论的 ES6 语境，同时把年度规范、现代浏览器实现和转译工具的差异放入版本栏；后续规范补充不会冒充原书正式分节。

站内示例优先使用今天可运行且语义稳定的写法，但目录清单、章节归属和历史判断仍以作者一版仓库为准。对已经撤回、替代或标准化后发生变化的能力，本页要求同时写出原书快照和当前状态。

## 练习

<Exercises>

**问题 1** 为什么不能只以最终输出正确作为第 2 章 语法的验收？

<Answer>
  最终输出会隐藏作用域命中位置、this 接收者、抽象类型转换、微任务顺序和历史 API
  差异。必须比较输入、绑定、求值步骤、首个偏离点与清理结果。
</Answer>

**问题 2** 如何把本页权威目录变成可执行学习证据？

<Answer>
  按公开顺序登记“Chapter 2: Syntax、Block-Scoped Declarations、Spread /
  Rest、Default Parameter Values、Destructuring、Object Literal
  Extensions、Template Literals、Arrow Functions、for..of Loops、Regular
  Expression Extensions、Number Literal
  Extensions、Unicode、Symbols”，为每项写出输入、状态、输出、失败与恢复条件，再以相同运行时重放正常和反例。
</Answer>

**问题 3** 本页的最小通过条件是什么？

<Answer>
  ES6 语法围绕更精确的作用域、结构化数据交换、可迭代协议、函数上下文和 Unicode
  正确性展开；每种简写都对应明确的求值顺序与边界。
  同时，六阶段可以被同一组输入重放，所有旧绑定、旧任务和旧资源在恢复后都已清除。
</Answer>

</Exercises>

## 小结

- **块级声明**：对应“用 let 与 const 建立块边界”阶段的核心观察量。
- **展开与剩余**：对应“用解构声明数据形状”阶段的核心观察量。
- **解构**：对应“用默认值处理 undefined”阶段的核心观察量。
- **模板字面量**：对应“用展开和剩余搬运序列”阶段的核心观察量。
- **Symbol**：对应“用迭代协议遍历”阶段的核心观察量。
- 六阶段机制链已映射到正常、边界、失败与恢复证据。
- 一版目录与现代规范状态分栏记录，后续变化不会冒充原书章节。

## 术语表

<Glossary>
  <GlossaryItem term="块级声明">
    块级声明是本单元机制模型中的关键观察量，位于“用 let 与 const
    建立块边界”阶段。判断它是否正确，必须同时记录输入、当前状态、输出、失败路径与清理责任，不能只依据最终打印值。
  </GlossaryItem>
  <GlossaryItem term="展开与剩余">
    展开与剩余是本单元机制模型中的关键观察量，位于“用解构声明数据形状”阶段。判断它是否正确，必须同时记录输入、当前状态、输出、失败路径与清理责任，不能只依据最终打印值。
  </GlossaryItem>
  <GlossaryItem term="解构">
    解构是本单元机制模型中的关键观察量，位于“用默认值处理
    undefined”阶段。判断它是否正确，必须同时记录输入、当前状态、输出、失败路径与清理责任，不能只依据最终打印值。
  </GlossaryItem>
  <GlossaryItem term="模板字面量">
    模板字面量是本单元机制模型中的关键观察量，位于“用展开和剩余搬运序列”阶段。判断它是否正确，必须同时记录输入、当前状态、输出、失败路径与清理责任，不能只依据最终打印值。
  </GlossaryItem>
  <GlossaryItem term="Symbol">
    Symbol是本单元机制模型中的关键观察量，位于“用迭代协议遍历”阶段。判断它是否正确，必须同时记录输入、当前状态、输出、失败路径与清理责任，不能只依据最终打印值。
  </GlossaryItem>
</Glossary>

## 来源与改编边界

<Attribution
  adaptedFrom="Kyle Simpson 著，You Don't Know JS 一版六册；简体中文版《你不知道的 JavaScript》上卷（人民邮电出版社，2015，ISBN 9787115385734）、中卷（2016，ISBN 9787115431165）、下卷（2018，ISBN 9787115471659）。本页按作者开放仓库目录独立教学重构，不复制原书段落。"
  adaptedUrl="https://github.com/getify/You-Dont-Know-JS/blob/1st-ed/es6%20%26%20beyond/ch2.md"
/>
