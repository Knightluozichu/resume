# 质量审计 v1 历史快照

v1 只统计字符数、标签数量和组件命名，不能证明正文正确、图示专属或页面可用。它曾把带有 `content_missing` 和通用 `OfficialCourseLab` 的章节评为 97–100 分，因此其“通过”状态不得迁移到 v2 台账。

- 旧摘要：`content-quality-summary.md`
- 旧任务索引：`tasks-README.md`
- 完整旧报告和逐体系任务仍保留在工作区原位置 `quality/reports/`、`quality/tasks/`，v2 工具不会删除或覆盖它们。
