"use client";

import { SuspensionLab as SuspensionLabCore } from "../../auto/why-car-runs-lab";
import { withChapterReset } from "./chapter-lab-frame";

export const SuspensionLab = withChapterReset(SuspensionLabCore);
