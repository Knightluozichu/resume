"use client";

import { TransmissionLab as TransmissionLabCore } from "../../auto/why-car-runs-lab";
import { withChapterReset } from "./chapter-lab-frame";

export const TransmissionLab = withChapterReset(TransmissionLabCore);
