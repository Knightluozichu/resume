"use client";

import { DlsEvidenceLab, type DlsEvidenceModel } from "./dls-evidence-lab";

const model = {
  unitId: "dls-02",
  title: "第2章 感知机",
  question:
    "怎样用权重、偏置与决策边界解释AND/OR/NAND，并证明单层感知机为什么不能表示XOR？",
  concepts: [
    "第2章 感知机",
    "2.1 感知机是什么",
    "2.2 简单逻辑电路",
    "2.2.1 与门",
    "2.2.2 与非门和或门",
    "2.3 感知机的实现",
    "2.3.1 简单的实现",
    "2.3.2 导入权重和偏置",
    "2.3.3 使用权重和偏置的实现",
    "2.4 感知机的局限性",
    "2.4.1 异或门",
    "2.4.2 线性和非线性",
    "2.5 多层感知机",
    "2.5.1 已有门电路的组合",
    "2.5.2 异或门的实现",
    "2.6 从与非门到计算机",
    "2.7 小结",
  ],
  stages: [
    {
      name: "列出真值表",
      input:
        "第2章 感知机：锁定解释器、依赖、输入与数据角色，保持其余实现合同不变",
      operation:
        "冻结版本、dtype、shape、轴、种子和允许读取的信息，并持续满足“输入真值表、权重、偏置、阈值、输出编码和组合顺序固定”",
      output: "列出真值表产生可追溯输入状态",
      check:
        "可追溯输入状态、shape与数值断言；出现“为每个样本单独修改权重，或用隐藏层实现XOR却仍宣称单层线性可分”时停止",
    },
    {
      name: "计算加权和",
      input: "第2章 感知机：执行本页的前向代码或数组变换，保持其余实现合同不变",
      operation:
        "保存输入输出shape、参数、缓存和数值范围，并持续满足“输入真值表、权重、偏置、阈值、输出编码和组合顺序固定”",
      output: "计算加权和产生可重放前向状态",
      check:
        "可重放前向状态、shape与数值断言；出现“为每个样本单独修改权重，或用隐藏层实现XOR却仍宣称单层线性可分”时停止",
    },
    {
      name: "应用阈值",
      input:
        "第2章 感知机：计算局部导数、数值梯度或状态更新，保持其余实现合同不变",
      operation:
        "保存上游梯度、归约轴、差分步长和相对误差，并持续满足“输入真值表、权重、偏置、阈值、输出编码和组合顺序固定”",
      output: "应用阈值产生可复核梯度状态",
      check:
        "可复核梯度状态、shape与数值断言；出现“为每个样本单独修改权重，或用隐藏层实现XOR却仍宣称单层线性可分”时停止",
    },
    {
      name: "组合隐藏门",
      input:
        "第2章 感知机：按固定batch顺序更新参数或组合网络，保持其余实现合同不变",
      operation:
        "保存优化器状态、损失、训练/推理模式和曲线，并持续满足“输入真值表、权重、偏置、阈值、输出编码和组合顺序固定”",
      output: "组合隐藏门产生可比较训练状态",
      check:
        "可比较训练状态、shape与数值断言；出现“为每个样本单独修改权重，或用隐藏层实现XOR却仍宣称单层线性可分”时停止",
    },
    {
      name: "检查全部输入",
      input:
        "第2章 感知机：执行形状、梯度、复现和独立评估检查，保持其余实现合同不变",
      operation:
        "隔离测试角色并登记版本、失败反例和时间边界，并持续满足“输入真值表、权重、偏置、阈值、输出编码和组合顺序固定”",
      output: "检查全部输入产生独立实现证据包",
      check:
        "独立实现证据包、shape与数值断言；出现“为每个样本单独修改权重，或用隐藏层实现XOR却仍宣称单层线性可分”时停止",
    },
  ],
  cases: [
    {
      name: "参考运行",
      observation:
        "手算四个二值输入的门电路，并组合多层感知机实现XOR。 固定环境、数据、shape、初值、顺序、容差和种子。",
      prediction:
        "沿“列出真值表 → 计算加权和 → 应用阈值 → 组合隐藏门 → 检查全部输入”得到可复核实现结果。",
      boundary:
        "全过程必须满足“输入真值表、权重、偏置、阈值、输出编码和组合顺序固定”。",
    },
    {
      name: "边界反例",
      observation:
        "手算四个二值输入的门电路，并组合多层感知机实现XOR。 其余条件不变，只注入“为每个样本单独修改权重，或用隐藏层实现XOR却仍宣称单层线性可分”。",
      prediction:
        "定位第一处环境、shape、前向、梯度、训练或评估状态偏离，并拒绝结论。",
      boundary: "失败轨迹必须保留；撤销故障后以相同输入重放。",
    },
  ],
  referenceTrace: [
    "为“第2章 感知机”冻结环境、数据角色、dtype/shape、初值、顺序、容差和随机种子",
    "执行列出真值表、计算加权和，保存输入输出、参数、缓存与数值范围",
    "推进应用阈值、组合隐藏门，记录梯度、更新、损失与训练状态",
    "在检查全部输入交付真值表、权重/偏置、加权和、阈值输出、二维决策边界、XOR反例、隐藏门和全输入断言。",
  ],
  faultTrace: [
    "“第2章 感知机”复用相同环境、数据角色、dtype/shape、初值、顺序、容差和种子",
    "只改变一个条件：为每个样本单独修改权重，或用隐藏层实现XOR却仍宣称单层线性可分",
    "沿“列出真值表 → 计算加权和 → 应用阈值 → 组合隐藏门 → 检查全部输入”寻找最早的代码、shape、梯度或评估分叉",
    "撤销故障重放；只有“输入真值表、权重、偏置、阈值、输出编码和组合顺序固定”恢复才接受修正",
  ],
  invariant: "输入真值表、权重、偏置、阈值、输出编码和组合顺序固定",
  fault: "为每个样本单独修改权重，或用隐藏层实现XOR却仍宣称单层线性可分",
  artifact:
    "真值表、权重/偏置、加权和、阈值输出、二维决策边界、XOR反例、隐藏门和全输入断言。",
  gates: [
    {
      label: "环境、输入与数据角色",
      detail:
        "“第2章 感知机”的解释器、依赖、dtype/shape、采样/切分、允许读取的信息和版本可追溯。",
    },
    {
      label: "前向代码与形状",
      detail:
        "“第2章 感知机”的层、参数、轴、缓存、激活、损失和输出shape已冻结。",
    },
    {
      label: "梯度与训练复现",
      detail:
        "“第2章 感知机”的数值梯度、反向梯度、初值、batch顺序、优化器状态和误差可重放。",
    },
    {
      label: "独立评估与边界",
      detail:
        "“第2章 感知机”归档训练/验证曲线、独立测试、反例、复现环境、适用域和时间标签。",
    },
  ],
} as const satisfies DlsEvidenceModel;

export function Dls02PerceptronTensorPathLab() {
  return <DlsEvidenceLab model={model} view="tensor-path" />;
}

export function Dls02PerceptronGradientTraceLab() {
  return <DlsEvidenceLab model={model} view="gradient-trace" />;
}

export function Dls02PerceptronTrainingGateLab() {
  return <DlsEvidenceLab model={model} view="training-gate" />;
}
