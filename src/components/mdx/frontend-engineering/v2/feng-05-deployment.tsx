"use client";

import {
  FrontendPipelineEvidenceLab,
  type FrontendPipelineEvidenceModel,
} from "./frontend-pipeline-evidence-lab";

const model = {
  unitId: "feng-unit-05",
  title: "第 5 章 部署",
  question:
    "怎样让HTML引用新资源、旧资源继续可用，并在出错时只切换入口而不是覆盖文件？",
  concepts: [
    "第5章 部署",
    "5.1 部署流程的设计原则",
    "5.1.1 速度——化繁为简",
    "5.1.2 协作——代码审查和部署队列",
    "5.1.3 安全——严格审查和权限控制",
    "5.2 流程之外：前端静态资源的部署策略",
    "5.2.1 协商缓存与强制缓存",
    "5.2.2 Apache设置缓存策略",
    "5.3 总结",
  ],
  nodeCards: [
    {
      name: "变更审查",
      input: "“第 5 章 部署”的变更审查读取已版本化需求、模板或源码。",
      action: "按审查并排队发布处理变更审查，不得读取未声明的可变输入。",
      output: "变更审查输出带版本、哈希或运行ID的工程证据，供下一节点验证。",
      gate: "若发生“在同一URL覆盖静态文件并设置长期强缓存，导致用户拿到版本混合页面”，变更审查必须停止而非越过“内容地址化资源不可变，发布动作有授权、审查、原子入口与回滚记录”。",
    },
    {
      name: "部署队列",
      input: "“第 5 章 部署”的部署队列读取上游已验证的状态与产物。",
      action: "按上传验证不可变资源处理部署队列，不得读取未声明的可变输入。",
      output: "部署队列输出带版本、哈希或运行ID的工程证据，供下一节点验证。",
      gate: "若发生“在同一URL覆盖静态文件并设置长期强缓存，导致用户拿到版本混合页面”，部署队列必须停止而非越过“内容地址化资源不可变，发布动作有授权、审查、原子入口与回滚记录”。",
    },
    {
      name: "权限门",
      input: "“第 5 章 部署”的权限门读取上游已验证的状态与产物。",
      action: "按原子切换入口与回滚处理权限门，不得读取未声明的可变输入。",
      output: "权限门输出带版本、哈希或运行ID的工程证据，供下一节点验证。",
      gate: "若发生“在同一URL覆盖静态文件并设置长期强缓存，导致用户拿到版本混合页面”，权限门必须停止而非越过“内容地址化资源不可变，发布动作有授权、审查、原子入口与回滚记录”。",
    },
    {
      name: "静态资源",
      input: "“第 5 章 部署”的静态资源读取上游已验证的状态与产物。",
      action: "按审查并排队发布处理静态资源，不得读取未声明的可变输入。",
      output: "静态资源输出带版本、哈希或运行ID的工程证据，供下一节点验证。",
      gate: "若发生“在同一URL覆盖静态文件并设置长期强缓存，导致用户拿到版本混合页面”，静态资源必须停止而非越过“内容地址化资源不可变，发布动作有授权、审查、原子入口与回滚记录”。",
    },
    {
      name: "缓存与入口",
      input: "“第 5 章 部署”的缓存与入口读取上游已验证的状态与产物。",
      action: "按上传验证不可变资源处理缓存与入口，不得读取未声明的可变输入。",
      output: "缓存与入口输出带版本、哈希或运行ID的工程证据，供下一节点验证。",
      gate: "若发生“在同一URL覆盖静态文件并设置长期强缓存，导致用户拿到版本混合页面”，缓存与入口必须停止而非越过“内容地址化资源不可变，发布动作有授权、审查、原子入口与回滚记录”。",
    },
  ],
  normalTrace: [
    "为“第 5 章 部署”记录源码提交、依赖锁、配置版本和责任边界",
    "执行审查并排队发布，保存输入与产生的工程状态",
    "推进上传验证不可变资源，核对日志、清单和产物身份",
    "完成原子切换入口与回滚，交付审批者、构建产物ID、资源哈希、缓存策略、上传校验、入口版本、发布时间和回滚目标。",
  ],
  failureTrace: [
    "复用“第 5 章 部署”相同的提交、依赖、配置和外部服务",
    "只注入工程故障：在同一URL覆盖静态文件并设置长期强缓存，导致用户拿到版本混合页面",
    "沿项目创建到交付方向记录最早发生身份或契约偏离的阶段",
    "依据“内容地址化资源不可变，发布动作有授权、审查、原子入口与回滚记录”拒绝运行并恢复已验证状态",
  ],
  invariant: "内容地址化资源不可变，发布动作有授权、审查、原子入口与回滚记录",
  fault: "在同一URL覆盖静态文件并设置长期强缓存，导致用户拿到版本混合页面",
  artifact:
    "审批者、构建产物ID、资源哈希、缓存策略、上传校验、入口版本、发布时间和回滚目标。",
  gates: [
    {
      label: "输入锁定",
      detail: "“第 5 章 部署”的提交、模板、依赖和配置都有版本。",
    },
    {
      label: "确定性构建",
      detail: "“第 5 章 部署”不读取时间戳、浮动网络内容或未声明环境。",
    },
    {
      label: "产物完整性",
      detail: "“第 5 章 部署”的清单、哈希、测试和部署对象保持一致。",
    },
    {
      label: "回滚准备",
      detail: "“第 5 章 部署”可以切回先前已验证的入口和不可变产物。",
    },
  ],
} satisfies FrontendPipelineEvidenceModel;

export function Feng05DeploymentArchitectureLab() {
  return <FrontendPipelineEvidenceLab model={model} view="architecture" />;
}

export function Feng05DeploymentExecutionTraceLab() {
  return <FrontendPipelineEvidenceLab model={model} view="execution-trace" />;
}

export function Feng05DeploymentReleaseGateLab() {
  return <FrontendPipelineEvidenceLab model={model} view="release-gate" />;
}
