import type { ReactNode } from "react";
function Frame({caption,children}:{caption:string;children:ReactNode}){return <figure className="mdx-figure not-prose mx-auto my-6"><div className="overflow-hidden rounded-card border border-border bg-elevated p-4 sm:p-5">{children}</div><figcaption className="mt-2 text-center text-sm text-secondary">{caption}</figcaption></figure>}
export function GpgAdvancedTechniquesDiagram(){const groups=[["Measure","pipeline bottleneck · query"],["Author","FX Composer · parameters"],["Integrate","interfaces · RenderMan · DCC · app"],["Represent","octree · deferred data · coverage"],["Compute","GI · vision · radiosity"]];return <Frame caption="15 章连接性能测量、创作、集成、表示和计算，而非单一渲染路径。"><div role="img" aria-label="GPU Gems 高级工程五层结构" className="grid gap-2 md:grid-cols-5">{groups.map(([a,b],i)=><div key={a} className="grid min-h-24 content-center rounded-control border border-border bg-bg/45 p-3 text-center"><span className="text-xs font-bold text-accent">0{i+1}</span><strong className="mt-1 text-xs text-primary">{a}</strong><span className="mt-2 text-xs text-secondary">{b}</span></div>)}</div></Frame>}
export function GpgAdvancedChapterMatrixDiagram(){const rows=[["G1 28-36","9","performance · occlusion · FX Composer · interfaces · production integration"],["G2 37-42","6","octree texture · raster GI · radiosity · vision · deferred filter · conservative raster"]];return <Frame caption="第一卷解决生产管线，第二卷把复杂数据和计算映射到 GPU。"><div role="img" aria-label="GPU Gems 高级工程十五章矩阵" className="overflow-hidden rounded-control border border-border text-xs"><div className="grid grid-cols-[.8fr_.4fr_2.8fr] gap-px bg-border">{['Range','Count','Core cases'].map(x=><strong key={x} className="bg-bg p-3 text-primary">{x}</strong>)}{rows.flatMap(r=>r.map((x,i)=><span key={`${r[0]}-${x}`} className={i===0?"bg-accent/10 p-3 font-semibold text-accent":"bg-elevated p-3 text-secondary"}>{x}</span>))}</div></div></Frame>}
export function GpgEngineeringEvidenceDiagram(){const rows=[["Performance","controlled scaling","CPU/GPU time slopes"],["Tool/interface","reference asset","parameter + image consistency"],["Sparse data","known octree","address + boundary checks"],["GI/radiosity","small analytic scene","energy + residual"],["Conservative coverage","triangle-cell oracle","zero false negatives"],["Vision/filter","recorded signal","accuracy + end-to-end latency"]];return <Frame caption="每类系统用独立 oracle 和规模指标验收。"><div role="img" aria-label="GPU 高级工程证据矩阵" className="overflow-hidden rounded-control border border-border text-xs"><div className="grid grid-cols-[1fr_1.3fr_1.7fr] gap-px bg-border">{['System','Probe','Evidence'].map(x=><strong key={x} className="bg-bg p-3 text-primary">{x}</strong>)}{rows.flatMap(r=>r.map((x,i)=><span key={`${r[0]}-${x}`} className={i===0?"bg-accent/10 p-3 font-semibold text-accent":"bg-elevated p-3 text-secondary"}>{x}</span>))}</div></div></Frame>}
