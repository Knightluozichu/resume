import type { ReactNode } from "react";
function Frame({caption,children}:{caption:string;children:ReactNode}){return <figure className="mdx-figure not-prose mx-auto my-6"><div className="overflow-hidden rounded-card border border-border bg-elevated p-4 sm:p-5">{children}</div><figcaption className="mt-2 text-center text-sm text-secondary">{caption}</figcaption></figure>}
export function GpgFinalReviewDiagram(){const themes=[["Natural","8"],["Light","24"],["Material","12"],["Image","24"],["Engineering","15"],["Geometry","15"],["Compute","20"],["Numeric/Physics","13"]];return <Frame caption="八个唯一主题集合合计 131 个原章；总复习只验收，不重复计数。"><div role="img" aria-label="GPU Gems 八主题与章节数" className="grid gap-2 sm:grid-cols-2 lg:grid-cols-4">{themes.map(([a,b])=><div key={a} className="flex min-h-20 items-center justify-between rounded-control border border-border bg-bg/45 p-3"><strong className="text-xs text-primary">{a}</strong><span className="grid size-9 place-items-center rounded-full bg-accent/15 text-sm font-bold text-accent">{b}</span></div>)}</div></Frame>}
export function GpgFinalEvidenceDiagram(){const rows=[["Visual","wave/shadow/filter/appearance","image · intermediate buffers · temporal"],["Geometry","clipmap/refine/crowd/field","topology · residency · pop"],["Parallel","reduce/scan/sort/FFT","value error · work · traffic"],["Numeric","solver/physics/Monte Carlo","residual · invariant · CI"],["System","tools/interfaces/performance","consistency · latency · scaling"]];return <Frame caption="不同章节必须选择匹配问题类型的 oracle，不能共享截图判据。"><div role="img" aria-label="GPU Gems 最终证据类型矩阵" className="overflow-hidden rounded-control border border-border text-xs"><div className="grid grid-cols-[.8fr_1.6fr_1.6fr] gap-px bg-border">{['Type','Examples','Required evidence'].map(x=><strong key={x} className="bg-bg p-3 text-primary">{x}</strong>)}{rows.flatMap(r=>r.map((x,i)=><span key={`${r[0]}-${x}`} className={i===0?"bg-accent/10 p-3 font-semibold text-accent":"bg-elevated p-3 text-secondary"}>{x}</span>))}</div></div></Frame>}
export function GpgCapstoneDiagram(){const labs=[["A · Living world","natural + geometry + light + material + image","render evidence"],["B · Parallel lab","map + reduce + scan + sort + compact + FFT","primitive evidence"],["C · Numeric workbench","linear + options + fluid + collision + SDF + matching","domain evidence"],["D · Production loop","authoring + interfaces + offline/runtime + profiling","system evidence"]];return <Frame caption="四个综合实验覆盖渲染、原语、领域数值和生产系统四种不可互换的证据。"><div role="img" aria-label="GPU Gems 四个综合验收实验" className="grid gap-3 md:grid-cols-2">{labs.map(([a,b,c])=><div key={a} className="rounded-control border border-border bg-bg/45 p-4"><strong className="text-sm text-primary">{a}</strong><p className="mb-0 mt-2 text-xs leading-5 text-secondary">{b}</p><span className="mt-3 block text-xs font-semibold text-accent">{c}</span></div>)}</div></Frame>}
