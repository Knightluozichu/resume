import type { ReactNode } from "react";
function Frame({caption,children}:{caption:string;children:ReactNode}){return <figure className="mdx-figure not-prose mx-auto my-6"><div className="overflow-hidden rounded-card border border-border bg-elevated p-4 sm:p-5">{children}</div><figcaption className="mt-2 text-center text-sm text-secondary">{caption}</figcaption></figure>}
export function GpgGeometryDiagram(){const groups=[["Generate","terrain · plants · field"],["Resident","clipmap · streams · segments"],["Reuse / cull","instances · query hierarchy"],["Refine / deform","subdivision · displacement · blend · wind"],["Validate","topology · pop · stall · throughput"]];return <Frame caption="几何系统从生成、驻留、复用/剔除到细化变形，最终以稳定性和吞吐验收。"><div role="img" aria-label="GPU Gems 几何五层数据流" className="grid gap-2 md:grid-cols-5">{groups.map(([a,b],i)=><div key={a} className="grid min-h-24 content-center rounded-control border border-border bg-bg/45 p-3 text-center"><span className="text-xs font-bold text-accent">0{i+1}</span><strong className="mt-1 text-xs text-primary">{a}</strong><span className="mt-2 text-xs text-secondary">{b}</span></div>)}</div></Frame>}
export function GpgGeometryChapterMatrixDiagram(){const rows=[["G2 1-8","8","botany · clipmap · instancing · streams · query · subdivision · displacement"],["G3 1-7","7","terrain · crowd · blend shape · SpeedTree · refinement · wind · metaball"]];return <Frame caption="两卷十五章覆盖数据组织和可见表面生成的不同层级。"><div role="img" aria-label="GPU Gems 几何十五章矩阵" className="overflow-hidden rounded-control border border-border text-xs"><div className="grid grid-cols-[.8fr_.4fr_2.8fr] gap-px bg-border">{['Range','Count','Core cases'].map(x=><strong key={x} className="bg-bg p-3 text-primary">{x}</strong>)}{rows.flatMap(r=>r.map((x,i)=><span key={`${r[0]}-${x}`} className={i===0?"bg-accent/10 p-3 font-semibold text-accent":"bg-elevated p-3 text-secondary"}>{x}</span>))}</div></div></Frame>}
export function GpgGeometryDecisionDiagram(){const rows=[["Huge regular terrain","geometry clipmap","ring updates · layer transition"],["Repeated objects/crowd","instancing + streams","batch compatibility · animation data"],["Hidden hierarchy","occlusion query","latency · conservative fallback"],["Curved/displaced surface","adaptive refinement","shared edges · error metric"],["Implicit blobs","point/metaball field","density · depth · gradient"]];return <Frame caption="几何技术按数据结构和误差边界选择，不能只按距离套一个 LOD。"><div role="img" aria-label="几何表示选择矩阵" className="overflow-hidden rounded-control border border-border text-xs"><div className="grid grid-cols-[1.1fr_1.3fr_1.6fr] gap-px bg-border">{['Problem','Representation','Dominant boundary'].map(x=><strong key={x} className="bg-bg p-3 text-primary">{x}</strong>)}{rows.flatMap(r=>r.map((x,i)=><span key={`${r[0]}-${x}`} className={i===0?"bg-accent/10 p-3 font-semibold text-accent":"bg-elevated p-3 text-secondary"}>{x}</span>))}</div></div></Frame>}
