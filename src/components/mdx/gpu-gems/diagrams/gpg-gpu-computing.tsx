import type { ReactNode } from "react";
function Frame({caption,children}:{caption:string;children:ReactNode}){return <figure className="mdx-figure not-prose mx-auto my-6"><div className="overflow-hidden rounded-card border border-border bg-elevated p-4 sm:p-5">{children}</div><figcaption className="mt-2 text-center text-sm text-secondary">{caption}</figcaption></figure>}
export function GpgGpuComputingDiagram(){const eras=[["GPU Gems 1","texture → fragment pass → render target","gather + ping-pong"],["GPU Gems 2","stream → kernel → stream","mapping + data structures"],["GPU Gems 3","grid/block/thread → memory hierarchy","CUDA + scatter/sync"]];return <Frame caption="三卷对应三代可编程模型；算法迁移必须记录载体能力变化。"><div role="img" aria-label="GPU Gems 三代 GPU 计算模型" className="grid gap-3 md:grid-cols-3">{eras.map(([a,b,c])=><div key={a} className="rounded-control border border-border bg-bg/45 p-4"><strong className="text-sm text-primary">{a}</strong><p className="mb-0 mt-2 text-xs leading-5 text-secondary">{b}</p><span className="mt-3 block text-xs font-semibold text-accent">{c}</span></div>)}</div></Frame>}
export function GpgComputeChapterMatrixDiagram(){const rows=[["G1 37-42","6","toolkit · fluid · volume · ultrasound · stereogram · deformers"],["G2 29-36","8","stream architecture · mapping · data structures · flow · optimize · reduction"],["G3 36-41","6","AES · RNG · seismic · scan · Gaussian · variable feedback"]];return <Frame caption="20 章从基础原语延伸到图形、医学、安全与科学计算应用。"><div role="img" aria-label="GPU Gems GPU 计算二十章矩阵" className="overflow-hidden rounded-control border border-border text-xs"><div className="grid grid-cols-[.8fr_.4fr_2.8fr] gap-px bg-border">{['Range','Count','Core cases'].map(x=><strong key={x} className="bg-bg p-3 text-primary">{x}</strong>)}{rows.flatMap(r=>r.map((x,i)=><span key={`${r[0]}-${x}`} className={i===0?"bg-accent/10 p-3 font-semibold text-accent":"bg-elevated p-3 text-secondary"}>{x}</span>))}</div></div></Frame>}
export function GpgParallelPrimitiveDiagram(){const rows=[["Map","O(N)","independent element transform"],["Reduction","O(N) work · O(log N) span","tree combine"],["Scan","O(N) work · O(log N) span","up/down sweep"],["Compact","predicate + scan + scatter","variable output"],["Stencil","O(N·K)","tile + halo reuse"]];return <Frame caption="应用由少数并行原语组合；工作量、跨度和存储访问要分别验收。"><div role="img" aria-label="GPU 并行原语矩阵" className="overflow-hidden rounded-control border border-border text-xs"><div className="grid grid-cols-[1fr_1.2fr_1.8fr] gap-px bg-border">{['Primitive','Complexity','Contract'].map(x=><strong key={x} className="bg-bg p-3 text-primary">{x}</strong>)}{rows.flatMap(r=>r.map((x,i)=><span key={`${r[0]}-${x}`} className={i===0?"bg-accent/10 p-3 font-semibold text-accent":"bg-elevated p-3 text-secondary"}>{x}</span>))}</div></div></Frame>}
