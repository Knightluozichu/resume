import type { ReactNode } from "react";
function Frame({caption,children}:{caption:string;children:ReactNode}){return <figure className="mdx-figure not-prose mx-auto my-6"><div className="overflow-hidden rounded-card border border-border bg-elevated p-4 sm:p-5">{children}</div><figcaption className="mt-2 text-center text-sm text-secondary">{caption}</figcaption></figure>}
export function GpgImageProcessingDiagram(){const groups=[["Signal","HDR · color · depth · velocity"],["Sample","footprint · coverage · time"],["Operate","filter · LUT · detect · bake"],["Represent","EXR · impostor · normal · vector"],["Validate","reference · error · latency"]];return <Frame caption="图像处理从信号和采样开始，经算子与表示得到可验证输出。"><div role="img" aria-label="GPU Gems 图像处理五层合同" className="grid gap-2 md:grid-cols-5">{groups.map(([a,b],i)=><div key={a} className="grid min-h-24 content-center rounded-control border border-border bg-bg/45 p-3 text-center"><span className="text-xs font-bold text-accent">0{i+1}</span><strong className="mt-1 text-xs text-primary">{a}</strong><span className="mt-2 text-xs text-secondary">{b}</span></div>)}</div></Frame>}
export function GpgImageChapterMatrixDiagram(){const rows=[["G1 21-27","7","glow · color · DOF · filter · footprint · EXR · graph"],["G2 20-28","9","cubic · AA · lines · hair · LUT · Motion · noise · mip"],["G3 21-28","8","impostor · normal bake · particles · linear · vector · video · blur · DOF"]];return <Frame caption="24 章覆盖空间、颜色、几何图像化和时间四类问题。"><div role="img" aria-label="GPU Gems 图像处理二十四章矩阵" className="overflow-hidden rounded-control border border-border text-xs"><div className="grid grid-cols-[.8fr_.4fr_2.8fr] gap-px bg-border">{['Range','Count','Core cases'].map(x=><strong key={x} className="bg-bg p-3 text-primary">{x}</strong>)}{rows.flatMap(r=>r.map((x,i)=><span key={`${r[0]}-${x}`} className={i===0?"bg-accent/10 p-3 font-semibold text-accent":"bg-elevated p-3 text-secondary"}>{x}</span>))}</div></div></Frame>}
export function GpgImageEvidenceDiagram(){const rows=[["Spatial filter","impulse/edge","kernel response · ringing"],["Coverage/vector","subpixel reference","area error · stability"],["Color/LUT/EXR","HDR color chart","linear value · color difference"],["Depth/time","layered ground truth","leak · disocclusion · temporal error"],["Video/particles","recorded sequence","latency · throughput · false detection"]];return <Frame caption="不同图像问题需要不同参考信号，不能只用最终截图验收。"><div role="img" aria-label="图像处理证据矩阵" className="overflow-hidden rounded-control border border-border text-xs"><div className="grid grid-cols-[1fr_1.3fr_1.7fr] gap-px bg-border">{['Problem','Probe','Evidence'].map(x=><strong key={x} className="bg-bg p-3 text-primary">{x}</strong>)}{rows.flatMap(r=>r.map((x,i)=><span key={`${r[0]}-${x}`} className={i===0?"bg-accent/10 p-3 font-semibold text-accent":"bg-elevated p-3 text-secondary"}>{x}</span>))}</div></div></Frame>}
