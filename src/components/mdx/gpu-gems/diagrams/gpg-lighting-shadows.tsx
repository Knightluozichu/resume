import type { ReactNode } from "react";
function Frame({caption,children}:{caption:string;children:ReactNode}){return <figure className="mdx-figure not-prose mx-auto my-6"><div className="overflow-hidden rounded-card border border-border bg-elevated p-4 sm:p-5">{children}</div><figcaption className="mt-2 text-center text-sm text-secondary">{caption}</figcaption></figure>}
export function GpgLightingShadowsDiagram(){const groups=[["Visibility","map · volume · moments · cascade"],["Architecture","forward · deferred · visibility sets"],["Indirect","irradiance · AO · relighting"],["Media","atmosphere · refraction · god rays"],["Evidence","depth · moments · buffers · timing"]];return <Frame caption="24 章分属可见性、架构、间接光、介质和证据五层，而非一条“软硬阴影”轴。"><div role="img" aria-label="GPU Gems 光照阴影五层结构" className="grid gap-2 md:grid-cols-5">{groups.map(([a,b],i)=><div key={a} className="grid min-h-24 content-center rounded-control border border-border bg-bg/45 p-3 text-center"><span className="text-xs font-bold text-accent">0{i+1}</span><strong className="mt-1 text-xs text-primary">{a}</strong><span className="mt-2 text-xs text-secondary">{b}</span></div>)}</div></Frame>}
export function GpgLightingChapterMatrixDiagram(){const rows=[["G1 9-15","7","volume · cinematic · PCF · cube · interval · PSM · visibility"],["G2 9-19","11","deferred · irradiance · BTF · tiles · AO · NPR · atmosphere · water"],["G3 8-13","6","SAT/VSM · relighting · PSSM · robust volume · AO · scattering"]];return <Frame caption="每个范围保持原章身份；正文按表示聚类讲解但不合并计数。"><div role="img" aria-label="三卷二十四个光照阴影章节矩阵" className="overflow-hidden rounded-control border border-border text-xs"><div className="grid grid-cols-[.8fr_.4fr_2.8fr] gap-px bg-border">{['Range','Count','Core cases'].map(x=><strong key={x} className="bg-bg p-3 text-primary">{x}</strong>)}{rows.flatMap(r=>r.map((x,i)=><span key={`${r[0]}-${x}`} className={i===0?"bg-accent/10 p-3 font-semibold text-accent":"bg-elevated p-3 text-secondary"}>{x}</span>))}</div></div></Frame>}
export function GpgShadowDecisionDiagram(){const choices=[["Exact silhouette","shadow volume","stencil + geometry/fill cost"],["Depth compare","shadow map / PCF","bias + sampling alias"],["Large filter","VSM/SAT","moment leaking"],["Large view range","PSSM/CSM","splits + stability"],["Screen media","post-process scattering","off-screen/occlusion limits"]];return <Frame caption="选择表示时先看可用信息和容许伪影，再看过滤速度。"><div role="img" aria-label="阴影与介质技术选择矩阵" className="overflow-hidden rounded-control border border-border text-xs"><div className="grid grid-cols-[1fr_1.2fr_1.8fr] gap-px bg-border">{['Need','Technique','Dominant boundary'].map(x=><strong key={x} className="bg-bg p-3 text-primary">{x}</strong>)}{choices.flatMap(r=>r.map((x,i)=><span key={`${r[0]}-${x}`} className={i===0?"bg-accent/10 p-3 font-semibold text-accent":"bg-elevated p-3 text-secondary"}>{x}</span>))}</div></div></Frame>}
