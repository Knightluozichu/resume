import type { ReactNode } from "react";
function Frame({caption,children}:{caption:string;children:ReactNode}){return <figure className="mdx-figure not-prose mx-auto my-6"><div className="overflow-hidden rounded-card border border-border bg-elevated p-4 sm:p-5">{children}</div><figcaption className="mt-2 text-center text-sm text-secondary">{caption}</figcaption></figure>}
export function GpgMaterialsShadersDiagram(){const layers=[["Represent","BRDF · SSS · PUC · texture"],["Illuminate","IBL · direct · AO"],["Deform / intersect","wind · relief · specular paths"],["Sample","PDF · random sequence"],["Compose","deferred · transparency · output"]];return <Frame caption="外观系统从表示、照明、几何求交、采样到架构合成，而非三个固定 BRDF 分量。"><div role="img" aria-label="GPU Gems 材质外观五层结构" className="grid gap-2 md:grid-cols-5">{layers.map(([a,b],i)=><div key={a} className="grid min-h-24 content-center rounded-control border border-border bg-bg/45 p-3 text-center"><span className="text-xs font-bold text-accent">0{i+1}</span><strong className="mt-1 text-xs text-primary">{a}</strong><span className="mt-2 text-xs text-secondary">{b}</span></div>)}</div></Frame>}
export function GpgMaterialChapterMatrixDiagram(){const rows=[["G1 16-20","5","SSS · AO · Spatial BRDF · IBL · bombing"],["G3 14-20","7","skin · PUC · vegetation · reflection/refraction · relief · deferred · sampling"]];return <Frame caption="十二章分别覆盖局部材质、捕获外观、几何细节和渲染/采样架构。"><div role="img" aria-label="GPU Gems 材质着色十二章矩阵" className="overflow-hidden rounded-control border border-border text-xs"><div className="grid grid-cols-[.8fr_.4fr_2.8fr] gap-px bg-border">{['Range','Count','Core cases'].map(x=><strong key={x} className="bg-bg p-3 text-primary">{x}</strong>)}{rows.flatMap(r=>r.map((x,i)=><span key={`${r[0]}-${x}`} className={i===0?"bg-accent/10 p-3 font-semibold text-accent":"bg-elevated p-3 text-secondary"}>{x}</span>))}</div></div></Frame>}
export function GpgAppearancePipelineDiagram(){const rows=[["Measured/captured","Spatial BRDF · PUC","compress + reconstruct"],["Procedural","Texture Bombing · vegetation","stable random + temporal"],["Geometric","relief · multi specular paths","intersection + layers"],["Integrated","deferred · importance sampling","buffer + PDF evidence"]];return <Frame caption="不同外观来源需要不同参考判据，最终才在渲染架构中组合。"><div role="img" aria-label="外观表示到渲染路径" className="overflow-hidden rounded-control border border-border text-xs"><div className="grid grid-cols-[1fr_1.5fr_1.5fr] gap-px bg-border">{['Source','Examples','Validation'].map(x=><strong key={x} className="bg-bg p-3 text-primary">{x}</strong>)}{rows.flatMap(r=>r.map((x,i)=><span key={`${r[0]}-${x}`} className={i===0?"bg-accent/10 p-3 font-semibold text-accent":"bg-elevated p-3 text-secondary"}>{x}</span>))}</div></div></Frame>}
