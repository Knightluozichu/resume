import type { ReactNode } from "react";
function Frame({caption,children}:{caption:string;children:ReactNode}){return <figure className="mdx-figure not-prose mx-auto my-6"><div className="overflow-hidden rounded-card border border-border bg-elevated p-4 sm:p-5">{children}</div><figcaption className="mt-2 text-center text-sm text-secondary">{caption}</figcaption></figure>}
export function GpgNaturalEffectsDiagram(){const layers=[["Driver","wave · noise · animation"],["State","surface · mesh · density"],["Optics","reflect · scatter · emit · diffract"],["Compose","depth · alpha · energy"],["Evidence","numeric · image · temporal"]];return <Frame caption="自然效果不是单个 noise shader，而是驱动、状态、光学、合成和证据五层。"><div role="img" aria-label="自然效果五层复现模型" className="grid gap-2 md:grid-cols-5">{layers.map(([a,b],i)=><div key={a} className="grid min-h-24 content-center rounded-control border border-border bg-bg/45 p-3 text-center"><span className="text-xs font-bold text-accent">0{i+1}</span><strong className="mt-1 text-xs text-primary">{a}</strong><span className="mt-2 text-xs text-secondary">{b}</span></div>)}</div></Frame>}
export function GpgNaturalChapterMatrixDiagram(){const rows=[["1","Water simulation","wave + derivative"],["2","Water caustics","refraction + area focusing"],["3","Dawn skin","layered reflection/scattering"],["4","Dawn animation","skinning + blend shape"],["5","Improved noise","gradient + smooth fade"],["6","Vulcan fire","particle volume + compose"],["7","Waving grass","cluster LOD + wind"],["8","Diffraction","grating + wavelength"]];return <Frame caption="八章各自保留一个不可被“噪声”替代的核心合同。"><div role="img" aria-label="GPU Gems 1 自然效果八章矩阵" className="overflow-hidden rounded-control border border-border text-xs"><div className="grid grid-cols-[.4fr_1.4fr_2fr] gap-px bg-border">{['Ch','Case','Invariant'].map(x=><strong key={x} className="bg-bg p-3 text-primary">{x}</strong>)}{rows.flatMap(r=>r.map((x,i)=><span key={`${r[0]}-${x}`} className={i===0?"bg-accent/10 p-3 font-semibold text-accent":"bg-elevated p-3 text-secondary"}>{x}</span>))}</div></div></Frame>}
export function GpgSignalToImageDiagram(){const paths=[["Surface","waves / skin / diffraction","geometry + directional optics"],["Deformation","Dawn / grass","weights + spatial coherence"],["Field / volume","noise / fire / caustics","filter + accumulation + compose"]];return <Frame caption="三类数据流决定现代实现的资源、过滤和参考判据。"><div role="img" aria-label="自然效果三类数据流" className="grid gap-3 md:grid-cols-3">{paths.map(([a,b,c])=><div key={a} className="rounded-control border border-border bg-bg/45 p-4"><strong className="text-sm text-primary">{a}</strong><p className="mb-0 mt-2 text-xs leading-5 text-secondary">{b}</p><span className="mt-3 block text-xs font-semibold text-accent">{c}</span></div>)}</div></Frame>}
