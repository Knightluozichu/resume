import type { ReactNode } from "react";
function Frame({caption,children}:{caption:string;children:ReactNode}){return <figure className="mdx-figure not-prose mx-auto my-6"><div className="overflow-hidden rounded-card border border-border bg-elevated p-4 sm:p-5">{children}</div><figcaption className="mt-2 text-center text-sm text-secondary">{caption}</figcaption></figure>}
export function GpgParticleSystemsDiagram(){const groups=[["Model","equations · probability · geometry"],["Discretize","matrix · grid · bodies · strings"],["Parallelize","sort · FFT · stencil · pairs"],["Iterate","solver · contact · simulation"],["Validate","residual · CI · invariant · throughput"]];return <Frame caption="数值应用从领域模型到离散、并行、迭代和领域证据，粒子只是其中一类数据。"><div role="img" aria-label="GPU 数值与物理应用五层结构" className="grid gap-2 md:grid-cols-5">{groups.map(([a,b],i)=><div key={a} className="grid min-h-24 content-center rounded-control border border-border bg-bg/45 p-3 text-center"><span className="text-xs font-bold text-accent">0{i+1}</span><strong className="mt-1 text-xs text-primary">{a}</strong><span className="mt-2 text-xs text-secondary">{b}</span></div>)}</div></Frame>}
export function GpgPhysicsChapterMatrixDiagram(){const rows=[["G2 43-48","6","protein · linear solve · options · sort · boundary flow · medical FFT"],["G3 29-35","7","rigid · 3D fluid · N-body · broad phase · LCP · SDF · virus match"]];return <Frame caption="十三章覆盖六个领域，必须分别定义正确性而非共享粒子计数指标。"><div role="img" aria-label="GPU Gems 数值物理十三章矩阵" className="overflow-hidden rounded-control border border-border text-xs"><div className="grid grid-cols-[.8fr_.4fr_2.8fr] gap-px bg-border">{['Range','Count','Core cases'].map(x=><strong key={x} className="bg-bg p-3 text-primary">{x}</strong>)}{rows.flatMap(r=>r.map((x,i)=><span key={`${r[0]}-${x}`} className={i===0?"bg-accent/10 p-3 font-semibold text-accent":"bg-elevated p-3 text-secondary"}>{x}</span>))}</div></div></Frame>}
export function GpgSimulationEvidenceDiagram(){const rows=[["Linear/FFT","known solution/phantom","residual · spectral error"],["Monte Carlo","analytic/high-sample","bias · variance · CI"],["Fluid/N-body","small physical case","divergence · mass · energy"],["Collision/LCP","brute-force/contact oracle","no missed pairs · complementarity"],["SDF","analytic shapes","sign · distance · gradient"],["Virus match","CPU automaton","complete offsets · latency"]];return <Frame caption="每个领域使用自己的 oracle 与不变量，性能不能替代正确性。"><div role="img" aria-label="GPU 数值物理证据矩阵" className="overflow-hidden rounded-control border border-border text-xs"><div className="grid grid-cols-[1fr_1.3fr_1.7fr] gap-px bg-border">{['Domain','Oracle','Evidence'].map(x=><strong key={x} className="bg-bg p-3 text-primary">{x}</strong>)}{rows.flatMap(r=>r.map((x,i)=><span key={`${r[0]}-${x}`} className={i===0?"bg-accent/10 p-3 font-semibold text-accent":"bg-elevated p-3 text-secondary"}>{x}</span>))}</div></div></Frame>}
