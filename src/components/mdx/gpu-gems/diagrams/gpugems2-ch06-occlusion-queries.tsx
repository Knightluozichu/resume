"use client";

import { useMemo, useState } from "react";
import type { ReactNode } from "react";

const accent = "var(--accent)";
const success = "var(--success)";
const warning = "var(--warning)";
const danger = "var(--danger)";
const primary = "var(--text-primary)";
const secondary = "var(--text-secondary)";
const border = "var(--border)";
const surface = "var(--surface)";

function Frame({ ariaLabel, caption, children }: { ariaLabel: string; caption: string; children: ReactNode }) {
  return <figure className="mdx-figure not-prose mx-auto my-6"><div className="overflow-hidden rounded-card border border-border bg-elevated p-5"><svg viewBox="0 0 720 390" role="img" aria-label={ariaLabel} className="mx-auto block h-auto w-full max-w-[720px]">{children}</svg></div><figcaption className="mt-2 text-center text-sm text-secondary">{caption}</figcaption></figure>;
}

function Arrow({ x1, y1, x2, y2, color = accent }: { x1: number; y1: number; x2: number; y2: number; color?: string }) {
  return <><line x1={x1} y1={y1} x2={x2} y2={y2} stroke={color} strokeWidth={2.5}/><path d={`M ${x2 - 8} ${y2 - 5} L ${x2} ${y2} L ${x2 - 8} ${y2 + 5}`} fill="none" stroke={color} strokeWidth={2.5}/></>;
}

export function GpuGems2Ch06QueryLifecycleDiagram() {
  const steps = [["1", "Issue", "开始 query", accent], ["2", "Bounds", "绘制保守包围盒", warning], ["3", "Result", "读取可见像素数", success], ["4", "Render", "可见才画复杂物体", danger]] as const;
  return <Frame ariaLabel="hardware occlusion query 四步生命周期：发起、绘制保守包围盒、读取可见像素、决定是否绘制" caption="query 只测试便宜且保守的近似几何；真正的复杂物体只在结果允许时绘制。"><text x={36} y={34} fontSize={14} fontWeight={700} fill={primary}>Occlusion query：用便宜的包围盒回答“会不会有像素”</text><g transform="translate(40 94)">{steps.map(([number, title, detail, color], index) => <g key={number} transform={`translate(${index * 164} 0)`}><rect width={136} height={164} rx={16} fill={surface} stroke={color} strokeWidth={2}/><circle cx={68} cy={32} r={18} fill={color} fillOpacity={0.14} stroke={color}/><text x={68} y={38} textAnchor="middle" fontSize={14} fontWeight={700} fill={color}>{number}</text><text x={68} y={76} textAnchor="middle" fontSize={14} fontWeight={700} fill={primary}>{title}</text><text x={68} y={112} textAnchor="middle" fontSize={12} fill={secondary}>{detail.slice(0, 6)}</text><text x={68} y={132} textAnchor="middle" fontSize={12} fill={secondary}>{detail.slice(6)}</text></g>)}</g><Arrow x1={182} y1={176} x2={204} y2={176}/><Arrow x1={346} y1={176} x2={368} y2={176} color={warning}/><Arrow x1={510} y1={176} x2={532} y2={176} color={success}/><text x={360} y={324} textAnchor="middle" fontSize={13} fill={secondary}>包围盒必须覆盖原物体的屏幕范围，否则会产生错误的“不可见”</text></Frame>;
}

export function GpuGems2Ch06HierarchyDiagram() {
  return <Frame ariaLabel="空间层次结构按 front-to-back 访问：内部节点可一次排除大量子物体" caption="在层次结构中测试大的 occluded 内部节点，一次 query 可以跳过它下面的许多 draw calls。"><text x={36} y={34} fontSize={14} fontWeight={700} fill={primary}>Spatial hierarchy：让一次 query 覆盖一组对象</text><g transform="translate(68 78)"><rect x={226} y={0} width={132} height={58} rx={12} fill={accent} fillOpacity={0.12} stroke={accent} strokeWidth={2}/><text x={292} y={35} textAnchor="middle" fontSize={14} fontWeight={700} fill={accent}>root</text><line x1={292} y1={58} x2={172} y2={96} stroke={border} strokeWidth={2}/><line x1={292} y1={58} x2={412} y2={96} stroke={border} strokeWidth={2}/><rect x={96} y={96} width={152} height={58} rx={12} fill={success} fillOpacity={0.12} stroke={success} strokeWidth={2}/><text x={172} y={131} textAnchor="middle" fontSize={13} fontWeight={700} fill={success}>visible node</text><rect x={336} y={96} width={152} height={58} rx={12} fill={danger} fillOpacity={0.12} stroke={danger} strokeWidth={2}/><text x={412} y={131} textAnchor="middle" fontSize={13} fontWeight={700} fill={danger}>occluded node</text>{[0,1].map((branch) => <g key={branch}>{[0,1,2].map((leaf) => <g key={leaf} transform={`translate(${branch * 240 + 30 + leaf * 58} 188)`}><rect width={44} height={44} rx={8} fill={branch ? danger : success} fillOpacity={0.12} stroke={branch ? danger : success}/><text x={22} y={27} textAnchor="middle" fontSize={11} fill={secondary}>leaf</text></g>)}</g>)}</g><text x={360} y={342} textAnchor="middle" fontSize={13} fill={secondary}>front-to-back 访问让近处可见物体先填充深度，远处节点更容易被排除</text></Frame>;
}

export function GpuGems2Ch06CoherenceDiagram() {
  return <Frame ariaLabel="时间连续性：上一帧可见节点在本帧先按可见处理，query 结果稍后到达并修正分类" caption="预测错为可见只会多做工作；预测错为不可见可能漏画，因此不可见猜测必须在结果到达时拉回遍历。"><text x={36} y={34} fontSize={14} fontWeight={700} fill={primary}>Temporal coherence：用上一帧分类填充 query latency</text><g transform="translate(54 78)"><text x={0} y={18} fontSize={13} fontWeight={700} fill={secondary}>Frame N</text><rect x={100} y={0} width={220} height={50} rx={10} fill={success} fillOpacity={0.14} stroke={success}/><text x={210} y={31} textAnchor="middle" fontSize={13} fill={success}>node visible → render now</text><text x={0} y={98} fontSize={13} fontWeight={700} fill={secondary}>Frame N + 1</text><rect x={100} y={80} width={220} height={50} rx={10} fill={warning} fillOpacity={0.14} stroke={warning}/><text x={210} y={111} textAnchor="middle" fontSize={13} fill={warning}>issue query → queue</text><Arrow x1={330} y1={105} x2={400} y2={105} color={warning}/><rect x={422} y={80} width={220} height={50} rx={10} fill={accent} fillOpacity={0.14} stroke={accent}/><text x={532} y={111} textAnchor="middle" fontSize={13} fill={accent}>result → correct</text><path d="M 320 24 C 388 24 388 160 422 160" fill="none" stroke={success} strokeWidth={2} strokeDasharray="6 5"/><text x={360} y={222} textAnchor="middle" fontSize={12} fill={secondary}>可见猜测：先做有用工作；结果回来后若变不可见，只更新下一帧状态</text></g><rect x={102} y={286} width={516} height={40} rx={10} fill={surface} stroke={border}/><text x={360} y={312} textAnchor="middle" fontSize={13} fill={primary}>关键：不能把“上一帧不可见”直接当作本帧最终答案</text></Frame>;
}

export function GpuGems2Ch06QueryPolicyDiagram() {
  return <Frame ariaLabel="coherent hierarchical culling 的 query 策略：跳过上一帧可见内部节点，只测试可见叶和最大的不可见区域" caption="pull up 用子节点结果推导父节点可见性，避免为已经知道可见的内部节点反复发 query。"><text x={36} y={34} fontSize={14} fontWeight={700} fill={primary}>Query policy：该测的测，该跳的跳</text><g transform="translate(58 80)"><rect width={604} height={214} rx={18} fill={surface} stroke={border}/><g transform="translate(28 28)"><circle cx={15} cy={15} r={7} fill={success}/><text x={32} y={20} fontSize={13} fill={secondary}>上一帧可见 interior：直接遍历，不发 query</text><circle cx={15} cy={64} r={7} fill={warning}/><text x={32} y={69} fontSize={13} fill={secondary}>上一帧可见 leaf：可 query，同时先绘制</text><circle cx={15} cy={113} r={7} fill={danger}/><text x={32} y={118} fontSize={13} fill={secondary}>上一帧不可见的最大区域：发 query 验证</text><circle cx={15} cy={162} r={7} fill={accent}/><text x={32} y={167} fontSize={13} fill={secondary}>结果可用：pull up / pull down 更新层次状态</text></g></g><text x={360} y={340} textAnchor="middle" fontSize={13} fill={secondary}>query 数量接近可见对象规模，而不是总对象数或树深度</text></Frame>;
}

type SceneMode = "city" | "flyover";
export function GpuGems2Ch06OcclusionQueryLab() {
  const [occlusion, setOcclusion] = useState(68);
  const [coherence, setCoherence] = useState(80);
  const [mode, setMode] = useState<SceneMode>("city");
  const estimates = useMemo(() => { const cityFactor = mode === "city" ? 0.58 : 0.94; const queries = Math.max(2, Math.round((100 - occlusion) * 0.12 + (100 - coherence) * 0.08)); const skipped = Math.round(occlusion * cityFactor); const stalls = Math.max(0, Math.round((100 - coherence) * 0.18 + (mode === "flyover" ? 7 : 1))); return { queries, skipped, stalls }; }, [occlusion, coherence, mode]);
  function reset() { setOcclusion(68); setCoherence(80); setMode("city"); }
  return <section className="not-prose my-8 overflow-hidden rounded-card border border-border bg-elevated" aria-label="GPU Gems 2 Chapter 6 hardware occlusion query 实验：调整遮挡、时间连续性和场景模式" data-visual-kind="gpu-gems2-ch06-occlusion-queries"><div className="border-b border-border px-5 py-4"><p className="text-sm font-semibold text-primary">Coherent hierarchical culling 实验</p><p className="mt-1 text-sm text-secondary">用场景遮挡和上一帧连续性观察 query 与等待的变化趋势。</p></div><div className="grid gap-5 p-5 lg:grid-cols-[1fr_230px]"><div className="overflow-hidden rounded-card border border-border bg-[var(--bg)] p-3"><svg viewBox="0 0 540 330" role="img" aria-label="城市层次结构中可见与遮挡节点的预览" className="block h-auto w-full"><text x={270} y={25} textAnchor="middle" fontSize={13} fontWeight={700} fill={primary}>{mode === "city" ? "城市街区：有大块遮挡" : "高空飞越：遮挡很少"}</text><rect x={22} y={48} width={496} height={122} rx={14} fill={surface} stroke={border}/>{Array.from({length: 14}, (_, index) => { const covered = mode === "city" && index % 4 !== 0 && index % 5 !== 0; return <rect key={index} x={42 + (index % 7) * 65} y={72 + Math.floor(index / 7) * 51} width={44} height={32} rx={6} fill={covered ? danger : success} fillOpacity={covered ? 0.13 : 0.22} stroke={covered ? danger : success}/>; })}<rect x={218} y={82} width={104} height={52} rx={10} fill={warning} fillOpacity={0.2} stroke={warning} strokeWidth={2}/><text x={270} y={113} textAnchor="middle" fontSize={12} fontWeight={700} fill={warning}>occluder</text><text x={270} y={198} textAnchor="middle" fontSize={12} fill={secondary}>可跳过节点：{estimates.skipped} · queries：{estimates.queries} · stall：{estimates.stalls}</text><rect x={84} y={226} width={372} height={38} rx={9} fill={accent} fillOpacity={0.12} stroke={accent}/><text x={270} y={250} textAnchor="middle" fontSize={12} fill={accent}>queue 让 GPU 工作与结果等待交错</text><text x={270} y={304} textAnchor="middle" fontSize={12} fill={secondary}>趋势示意，不替代真实 GPU profile</text></svg></div><div className="space-y-4"><label className="block text-sm text-secondary">场景模式<select className="mt-2 block h-11 w-full rounded-md border border-border bg-[var(--bg)] px-3 text-sm text-primary" value={mode} onChange={(event) => setMode(event.target.value as SceneMode)}><option value="city">城市街区</option><option value="flyover">高空飞越</option></select></label><label className="block text-sm text-secondary">遮挡程度：{occlusion}%<input className="mt-2 block h-11 w-full accent-[var(--accent)]" type="range" min="0" max="100" value={occlusion} onChange={(event) => setOcclusion(Number(event.target.value))}/></label><label className="block text-sm text-secondary">帧间连续性：{coherence}%<input className="mt-2 block h-11 w-full accent-[var(--accent)]" type="range" min="0" max="100" value={coherence} onChange={(event) => setCoherence(Number(event.target.value))}/></label><p className="rounded-md border border-border bg-[var(--bg)] p-3 text-xs leading-5 text-secondary" aria-live="polite">{mode === "city" ? "遮挡充分时，层次节点能一次排除多个物体。" : "遮挡不足时，query 本身可能成为额外开销，应关闭该路径。"}</p><button type="button" className="min-h-11 w-full rounded-md border border-border px-3 py-2 text-sm font-semibold text-primary transition hover:border-[var(--accent)]" onClick={reset}>重置</button></div></div></section>;
}
