"use client";

import { useMemo, useState } from "react";
import type { ReactNode } from "react";

const accent = "var(--accent)";
const success = "var(--success)";
const warning = "var(--warning)";
const danger = "var(--danger)";
const primary = "var(--text-primary)";
const secondary = "var(--text-secondary)";
const border = "var(--border)";
const surface = "var(--surface)";
function Frame({ ariaLabel, caption, children }: { ariaLabel: string; caption: string; children: ReactNode }) { return <figure className="mdx-figure not-prose mx-auto my-6"><div className="overflow-hidden rounded-card border border-border bg-elevated p-5"><svg viewBox="0 0 720 390" role="img" aria-label={ariaLabel} className="mx-auto block h-auto w-full max-w-[720px]">{children}</svg></div><figcaption className="mt-2 text-center text-sm text-secondary">{caption}</figcaption></figure>; }
function Arrow({ x1, y1, x2, y2, color = accent }: { x1: number; y1: number; x2: number; y2: number; color?: string }) { return <><line x1={x1} y1={y1} x2={x2} y2={y2} stroke={color} strokeWidth={2.5}/><path d={`M ${x2 - 8} ${y2 - 5} L ${x2} ${y2} L ${x2 - 8} ${y2 + 5}`} fill="none" stroke={color} strokeWidth={2.5}/></>; }

export function GpuGems2Ch09ForwardDeferredDiagram() { return <Frame ariaLabel="forward shading 每个物体重复计算光照，deferred shading 先生成 G-buffer 再按屏幕像素做 lighting pass" caption="deferred shading 把几何与光照解耦：同一个可见像素的法线、位置和材质属性只生成一次。"><text x={36} y={34} fontSize={14} fontWeight={700} fill={primary}>Forward：object × lights　vs　Deferred：geometry → G-buffer → lights</text><g transform="translate(44 78)"><rect width={266} height={206} rx={16} fill={surface} stroke={danger}/><text x={133} y={30} textAnchor="middle" fontSize={14} fontWeight={700} fill={danger}>forward shading</text>{Array.from({length: 4}, (_, index) => <g key={index} transform={`translate(22 ${58 + index * 34})`}><rect width={82} height={24} rx={6} fill={accent} fillOpacity={0.14} stroke={accent}/><text x={41} y={17} textAnchor="middle" fontSize={11} fill={secondary}>object {index + 1}</text><Arrow x1={90} y1={12} x2={134} y2={12} color={danger}/><rect x={142} width={102} height={24} rx={6} fill={danger} fillOpacity={0.14} stroke={danger}/><text x={193} y={17} textAnchor="middle" fontSize={11} fill={secondary}>light work</text></g>)}</g><Arrow x1={332} y1={184} x2={382} y2={184}/><g transform="translate(408 78)"><rect width={264} height={206} rx={16} fill={surface} stroke={success}/><text x={132} y={30} textAnchor="middle" fontSize={14} fontWeight={700} fill={success}>deferred shading</text><rect x={20} y={54} width={102} height={38} rx={7} fill={accent} fillOpacity={0.14} stroke={accent}/><text x={71} y={78} textAnchor="middle" fontSize={12} fill={secondary}>G-buffer</text><Arrow x1={130} y1={73} x2={158} y2={73} color={success}/><rect x={166} y={54} width={78} height={38} rx={7} fill={success} fillOpacity={0.14} stroke={success}/><text x={205} y={78} textAnchor="middle" fontSize={12} fill={secondary}>lights</text><text x={132} y={132} textAnchor="middle" fontSize={12} fill={secondary}>屏幕空间处理</text><text x={132} y={160} textAnchor="middle" fontSize={12} fontWeight={700} fill={success}>降低 CPU / vertex 重复工作</text></g></Frame>; }

export function GpuGems2Ch09GBufferDiagram() { const buffers = [["position", "eye-space position", accent], ["normal", "surface normal", success], ["albedo + gloss", "base color / specular", warning], ["material + AO", "light response controls", danger]] as const; return <Frame ariaLabel="deferred shading G-buffer 通过 MRT 保存位置、法线、albedo、gloss、material 和 ambient occlusion 属性" caption="MRT 的每个通道都要服务后续 lighting；剩余组件可以编码材质 ID、gloss 或 AO 等控制量。"><text x={36} y={34} fontSize={14} fontWeight={700} fill={primary}>G-buffer：一次 geometry pass，保存多种光照输入</text><g transform="translate(50 78)">{buffers.map(([name, detail, color], index) => <g key={name} transform={`translate(${index * 158} 0)`}><rect width={132} height={196} rx={15} fill={surface} stroke={color} strokeWidth={2}/><text x={66} y={34} textAnchor="middle" fontSize={13} fontWeight={700} fill={color}>{name}</text><rect x={22} y={60} width={88} height={72} rx={8} fill={color} fillOpacity={0.12} stroke={color}/><text x={66} y={90} textAnchor="middle" fontSize={11} fill={secondary}>{detail.split(" /")[0]}</text><text x={66} y={110} textAnchor="middle" fontSize={11} fill={secondary}>{detail.split(" /")[1] || ""}</text><text x={66} y={160} textAnchor="middle" fontSize={11} fill={secondary}>MRT target {index}</text></g>)}</g><text x={360} y={332} textAnchor="middle" fontSize={13} fill={secondary}>布局是质量、带宽、解码成本和材质灵活性的折中</text></Frame>; }

export function GpuGems2Ch09LightingPassDiagram() { return <Frame ariaLabel="deferred lighting pass：读取 G-buffer，筛选影响屏幕的 light，使用 stencil mask 对受影响像素累积光照" caption="光源不必覆盖全屏；层次剔除、occlusion query 和 stencil mask 共同限制实际 shading 区域。"><text x={36} y={34} fontSize={14} fontWeight={700} fill={primary}>Lighting pass：只给受影响的像素算光</text><g transform="translate(50 82)"><rect width={142} height={168} rx={15} fill={surface} stroke={accent}/><text x={71} y={32} textAnchor="middle" fontSize={14} fontWeight={700} fill={accent}>G-buffer</text><text x={71} y={76} textAnchor="middle" fontSize={12} fill={secondary}>position</text><text x={71} y={100} textAnchor="middle" fontSize={12} fill={secondary}>normal</text><text x={71} y={124} textAnchor="middle" fontSize={12} fill={secondary}>material / AO</text></g><Arrow x1={210} y1={166} x2={264} y2={166}/><g transform="translate(282 82)"><rect width={164} height={168} rx={15} fill={surface} stroke={warning}/><text x={82} y={32} textAnchor="middle" fontSize={14} fontWeight={700} fill={warning}>light culling</text><text x={82} y={78} textAnchor="middle" fontSize={12} fill={secondary}>sector / portal</text><text x={82} y={102} textAnchor="middle" fontSize={12} fill={secondary}>CPU / GPU occlusion</text><text x={82} y={126} textAnchor="middle" fontSize={12} fill={secondary}>light volume</text></g><Arrow x1={464} y1={166} x2={518} y2={166} color={success}/><g transform="translate(536 82)"><rect width={132} height={168} rx={15} fill={surface} stroke={success}/><text x={66} y={32} textAnchor="middle" fontSize={14} fontWeight={700} fill={success}>stencil</text><text x={66} y={78} textAnchor="middle" fontSize={12} fill={secondary}>affected</text><text x={66} y={102} textAnchor="middle" fontSize={12} fill={secondary}>pixels only</text><text x={66} y={126} textAnchor="middle" fontSize={12} fill={secondary}>accumulate</text></g><text x={360} y={322} textAnchor="middle" fontSize={13} fill={secondary}>光照成本随屏幕影响区域增长，而不是随所有物体重复增长</text></Frame>; }

export function GpuGems2Ch09TransparencyDiagram() { return <Frame ariaLabel="deferred shading 对透明和 alpha blending 不天然兼容，透明 primitive 使用 forward fallback" caption="G-buffer 通常每像素保存一层表面；透明需要多层排序或深度 peeling，因此常保留单独 forward 路径。"><text x={36} y={34} fontSize={14} fontWeight={700} fill={primary}>Deferred 的边界：透明需要另一条路径</text><g transform="translate(58 82)"><rect width={236} height={192} rx={16} fill={surface} stroke={success}/><text x={118} y={30} textAnchor="middle" fontSize={14} fontWeight={700} fill={success}>opaque</text><rect x={26} y={56} width={184} height={44} rx={8} fill={success} fillOpacity={0.12} stroke={success}/><text x={118} y={83} textAnchor="middle" fontSize={12} fill={secondary}>one surface → G-buffer</text><rect x={26} y={120} width={184} height={44} rx={8} fill={accent} fillOpacity={0.12} stroke={accent}/><text x={118} y={147} textAnchor="middle" fontSize={12} fill={secondary}>many lights → 2D pass</text></g><Arrow x1={326} y1={178} x2={382} y2={178}/><g transform="translate(410 82)"><rect width={252} height={192} rx={16} fill={surface} stroke={danger}/><text x={126} y={30} textAnchor="middle" fontSize={14} fontWeight={700} fill={danger}>transparent</text><rect x={28} y={56} width={196} height={44} rx={8} fill={danger} fillOpacity={0.12} stroke={danger}/><text x={126} y={83} textAnchor="middle" fontSize={12} fill={secondary}>multiple layers / order</text><rect x={28} y={120} width={196} height={44} rx={8} fill={warning} fillOpacity={0.12} stroke={warning}/><text x={126} y={147} textAnchor="middle" fontSize={12} fill={secondary}>forward fallback</text></g></Frame>; }

type Quality = "balanced" | "shadows" | "cheap";
export function GpuGems2Ch09DeferredShadingLab() { const [lights, setLights] = useState(8); const [overdraw, setOverdraw] = useState(36); const [quality, setQuality] = useState<Quality>("balanced"); const data = useMemo(() => { const shadowFactor = quality === "shadows" ? 1.45 : quality === "cheap" ? 0.55 : 1; const affected = Math.round(Math.min(96, lights * (100 - overdraw) * 0.12)); const gbuffer = Math.round(28 + overdraw * 0.12); const lighting = Math.round(affected * shadowFactor); return { affected, gbuffer, lighting }; }, [lights, overdraw, quality]); function reset() { setLights(8); setOverdraw(36); setQuality("balanced"); } return <section className="not-prose my-8 overflow-hidden rounded-card border border-border bg-elevated" aria-label="GPU Gems 2 Chapter 9 deferred shading 实验：调整光源数、overdraw 和阴影质量" data-visual-kind="gpu-gems2-ch09-deferred-shading"><div className="border-b border-border px-5 py-4"><p className="text-sm font-semibold text-primary">Deferred shading 实验</p><p className="mt-1 text-sm text-secondary">比较固定 geometry/G-buffer 成本与受屏幕影响的 lighting 成本。</p></div><div className="grid gap-5 p-5 lg:grid-cols-[1fr_230px]"><div className="overflow-hidden rounded-card border border-border bg-[var(--bg)] p-3"><svg viewBox="0 0 540 330" role="img" aria-label="G-buffer、光照和 stencil 影响区域的预览" className="block h-auto w-full"><text x={270} y={24} textAnchor="middle" fontSize={13} fontWeight={700} fill={primary}>frame passes · {quality === "shadows" ? "shadow-heavy" : quality === "cheap" ? "diffuse-only" : "balanced"}</text><rect x={26} y={48} width={488} height={102} rx={14} fill={surface} stroke={border}/>{Array.from({length: 8}, (_, index) => <rect key={index} x={44 + index * 58} y={72} width={42} height={52} rx={6} fill={index < Math.ceil(data.affected / 12) ? accent : border} fillOpacity={index < Math.ceil(data.affected / 12) ? 0.2 : 0.25} stroke={index < Math.ceil(data.affected / 12) ? accent : border}/>) }<text x={270} y={178} textAnchor="middle" fontSize={12} fill={secondary}>stencil affected pixels：{data.affected}%</text><g transform="translate(58 210)"><rect width={126} height={42} rx={8} fill={accent} fillOpacity={0.12} stroke={accent}/><text x={63} y={26} textAnchor="middle" fontSize={11} fill={secondary}>G-buffer {data.gbuffer}</text><rect x={148} width={126} height={42} rx={8} fill={warning} fillOpacity={0.12} stroke={warning}/><text x={211} y={26} textAnchor="middle" fontSize={11} fill={secondary}>lighting {data.lighting}</text><rect x={296} width={126} height={42} rx={8} fill={success} fillOpacity={0.12} stroke={success}/><text x={359} y={26} textAnchor="middle" fontSize={11} fill={secondary}>post-process</text></g><text x={270} y={304} textAnchor="middle" fontSize={12} fill={secondary}>趋势示意，不替代真实 GPU profile</text></svg></div><div className="space-y-4"><label className="block text-sm text-secondary">光源数量：{lights}<input className="mt-2 block h-11 w-full accent-[var(--accent)]" type="range" min="1" max="16" value={lights} onChange={(event) => setLights(Number(event.target.value))}/></label><label className="block text-sm text-secondary">几何 overdraw：{overdraw}%<input className="mt-2 block h-11 w-full accent-[var(--accent)]" type="range" min="0" max="80" value={overdraw} onChange={(event) => setOverdraw(Number(event.target.value))}/></label><label className="block text-sm text-secondary">光照质量<select className="mt-2 block h-11 w-full rounded-md border border-border bg-[var(--bg)] px-3 text-sm text-primary" value={quality} onChange={(event) => setQuality(event.target.value as Quality)}><option value="cheap">diffuse-only</option><option value="balanced">balanced</option><option value="shadows">shadow-heavy</option></select></label><p className="rounded-md border border-border bg-[var(--bg)] p-3 text-xs leading-5 text-secondary" aria-live="polite">增加光源主要提高 lighting pass；增加 overdraw 主要提高 geometry/G-buffer 成本。阴影质量会放大受影响像素的代价。</p><button type="button" className="min-h-11 w-full rounded-md border border-border px-3 py-2 text-sm font-semibold text-primary transition hover:border-[var(--accent)]" onClick={reset}>重置</button></div></div></section>; }
