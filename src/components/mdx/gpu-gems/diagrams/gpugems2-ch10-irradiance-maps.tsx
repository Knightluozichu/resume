"use client";

import { useMemo, useState } from "react";
import type { ReactNode } from "react";

const accent = "var(--accent)";
const success = "var(--success)";
const warning = "var(--warning)";
const danger = "var(--danger)";
const primary = "var(--text-primary)";
const secondary = "var(--text-secondary)";
const border = "var(--border)";
const surface = "var(--surface)";
function Frame({ ariaLabel, caption, children }: { ariaLabel: string; caption: string; children: ReactNode }) { return <figure className="mdx-figure not-prose mx-auto my-6"><div className="overflow-hidden rounded-card border border-border bg-elevated p-5"><svg viewBox="0 0 720 390" role="img" aria-label={ariaLabel} className="mx-auto block h-auto w-full max-w-[720px]">{children}</svg></div><figcaption className="mt-2 text-center text-sm text-secondary">{caption}</figcaption></figure>; }
function Arrow({ x1, y1, x2, y2, color = accent }: { x1: number; y1: number; x2: number; y2: number; color?: string }) { return <><line x1={x1} y1={y1} x2={x2} y2={y2} stroke={color} strokeWidth={2.5}/><path d={`M ${x2 - 8} ${y2 - 5} L ${x2} ${y2} L ${x2 - 8} ${y2 + 5}`} fill="none" stroke={color} strokeWidth={2.5}/></>; }

export function GpuGems2Ch10ConvolutionDiagram() { return <Frame ariaLabel="环境 map 与 Lambertian reflection function 做 convolution，输出按 surface normal 索引的 irradiance map" caption="irradiance map 把复杂光照环境预先折叠成一次按 normal 的 lookup；动态环境只需重新生成它。"><text x={36} y={34} fontSize={14} fontWeight={700} fill={primary}>Many light texels → convolution → one irradiance lookup</text><g transform="translate(44 88)"><rect width={198} height={188} rx={16} fill={surface} stroke={accent}/><text x={99} y={30} textAnchor="middle" fontSize={14} fontWeight={700} fill={accent}>environment map</text><circle cx={99} cy={96} r={54} fill={accent} fillOpacity={0.12} stroke={accent}/>{Array.from({length: 8}, (_, index) => { const a = index * Math.PI / 4; return <circle key={index} cx={99 + Math.cos(a) * 40} cy={96 + Math.sin(a) * 40} r={5} fill={accent}/>; })}<text x={99} y={164} textAnchor="middle" fontSize={12} fill={secondary}>many directions</text></g><Arrow x1={270} y1={182} x2={330} y2={182}/><g transform="translate(350 88)"><rect width={144} height={188} rx={16} fill={surface} stroke={warning}/><text x={72} y={30} textAnchor="middle" fontSize={14} fontWeight={700} fill={warning}>convolution</text><text x={72} y={82} textAnchor="middle" fontSize={12} fill={secondary}>L · max(0, N·L)</text><text x={72} y={112} textAnchor="middle" fontSize={12} fill={secondary}>sum / filter</text><text x={72} y={164} textAnchor="middle" fontSize={12} fill={secondary}>frequency</text></g><Arrow x1={522} y1={182} x2={570} y2={182} color={success}/><g transform="translate(586 88)"><rect width={94} height={188} rx={16} fill={surface} stroke={success}/><text x={47} y={30} textAnchor="middle" fontSize={13} fontWeight={700} fill={success}>irradiance</text><circle cx={47} cy={98} r={38} fill={success} fillOpacity={0.14} stroke={success}/><text x={47} y={102} textAnchor="middle" fontSize={11} fill={secondary}>N → B</text><text x={47} y={164} textAnchor="middle" fontSize={11} fill={secondary}>one lookup</text></g></Frame>; }

export function GpuGems2Ch10SphericalHarmonicsDiagram() { return <Frame ariaLabel="spherical harmonics 把环境函数投影到少量系数，低频 diffuse lighting 只需少数 band" caption="diffuse irradiance 变化缓慢，低阶 spherical harmonics 就能保留主要能量，避免对每个输出 texel 遍历全部输入 texel。"><text x={36} y={34} fontSize={14} fontWeight={700} fill={primary}>Spatial domain ↔ frequency domain</text><g transform="translate(54 88)"><rect width={244} height={184} rx={16} fill={surface} stroke={accent}/><text x={122} y={30} textAnchor="middle" fontSize={14} fontWeight={700} fill={accent}>environment samples</text><path d="M 28 136 Q 64 58 104 132 T 216 96" fill="none" stroke={accent} strokeWidth={3}/><text x={122} y={166} textAnchor="middle" fontSize={12} fill={secondary}>M × N brute force</text></g><Arrow x1={326} y1={180} x2={384} y2={180}/><g transform="translate(404 88)"><rect width={260} height={184} rx={16} fill={surface} stroke={success}/><text x={130} y={30} textAnchor="middle" fontSize={14} fontWeight={700} fill={success}>SH coefficients</text>{Array.from({length: 9}, (_, index) => <rect key={index} x={32 + (index % 3) * 66} y={60 + Math.floor(index / 3) * 27} width={50} height={18} rx={4} fill={index < 5 ? success : border} fillOpacity={index < 5 ? 0.18 : 0.35} stroke={border}/>) }<text x={130} y={166} textAnchor="middle" fontSize={12} fill={secondary}>K terms · convolution = multiply</text></g></Frame>; }

export function GpuGems2Ch10GpuPassDiagram() { const steps = [["Project", "environment → SH", accent], ["Convolve", "SH × BRDF coeff", warning], ["Evaluate", "inverse transform", success]] as const; return <Frame ariaLabel="GPU 两阶段 irradiance map 生成：投影环境到 SH、乘以预计算 BRDF 系数并 inverse transform" caption="reflection-function 系数可预计算；动态变化只更新环境投影和最终 evaluation。"><text x={36} y={34} fontSize={14} fontWeight={700} fill={primary}>GPU path：projection → frequency convolution → evaluation</text><g transform="translate(68 100)">{steps.map(([title, detail, color], index) => <g key={title} transform={`translate(${index * 210} 0)`}><rect width={170} height={144} rx={16} fill={surface} stroke={color} strokeWidth={2}/><text x={85} y={48} textAnchor="middle" fontSize={15} fontWeight={700} fill={color}>{title}</text><text x={85} y={88} textAnchor="middle" fontSize={12} fill={secondary}>{detail.split(" → ")[0]}</text><text x={85} y={110} textAnchor="middle" fontSize={12} fill={secondary}>{detail.split(" → ")[1]}</text></g>)}</g><Arrow x1={244} y1={172} x2={276} y2={172}/><Arrow x1={454} y1={172} x2={486} y2={172} color={warning}/><text x={360} y={314} textAnchor="middle" fontSize={13} fill={secondary}>tiled lookup textures 将多个 (l,m) coefficient 组织成并行输出像素</text></Frame>; }

export function GpuGems2Ch10DynamicEnvironmentDiagram() { return <Frame ariaLabel="动态灯光或对象更新输入 environment map，GPU 重新生成 irradiance map，表面只做低成本 lookup" caption="动态环境不再等于离线光照：render-to-texture 加频域压缩让环境 map 能在运行时更新。"><text x={36} y={34} fontSize={14} fontWeight={700} fill={primary}>Dynamic scene：更新环境，不重做每个表面的积分</text><g transform="translate(48 92)"><rect width={174} height={164} rx={16} fill={surface} stroke={accent}/><text x={87} y={32} textAnchor="middle" fontSize={14} fontWeight={700} fill={accent}>lights / objects</text><text x={87} y={78} textAnchor="middle" fontSize={12} fill={secondary}>sun · flare</text><text x={87} y={102} textAnchor="middle" fontSize={12} fill={secondary}>moving actor</text><text x={87} y={136} textAnchor="middle" fontSize={12} fill={secondary}>render to env</text></g><Arrow x1={246} y1={174} x2={300} y2={174}/><g transform="translate(318 92)"><rect width={174} height={164} rx={16} fill={surface} stroke={warning}/><text x={87} y={32} textAnchor="middle" fontSize={14} fontWeight={700} fill={warning}>GPU update</text><text x={87} y={78} textAnchor="middle" fontSize={12} fill={secondary}>SH project</text><text x={87} y={102} textAnchor="middle" fontSize={12} fill={secondary}>convolve</text><text x={87} y={136} textAnchor="middle" fontSize={12} fill={secondary}>evaluate map</text></g><Arrow x1={516} y1={174} x2={570} y2={174} color={success}/><g transform="translate(588 92)"><rect width={92} height={164} rx={16} fill={surface} stroke={success}/><text x={46} y={32} textAnchor="middle" fontSize={13} fontWeight={700} fill={success}>surface</text><text x={46} y={80} textAnchor="middle" fontSize={12} fill={secondary}>N</text><text x={46} y={104} textAnchor="middle" fontSize={12} fill={secondary}>lookup</text><text x={46} y={136} textAnchor="middle" fontSize={12} fill={secondary}>shade</text></g></Frame>; }

export function GpuGems2Ch10IrradianceLab() { const [bands, setBands] = useState(2); const [lights, setLights] = useState(6); const [gloss, setGloss] = useState(20); const ops = useMemo(() => { const coeffs = (bands + 1) * (bands + 1); const projection = coeffs * lights; const evaluate = coeffs * 8; return { coeffs, projection, evaluate }; }, [bands, lights]); function reset() { setBands(2); setLights(6); setGloss(20); } return <section className="not-prose my-8 overflow-hidden rounded-card border border-border bg-elevated" aria-label="GPU Gems 2 Chapter 10 irradiance environment map 实验：调整 spherical harmonic 阶数、光源数量和 gloss" data-visual-kind="gpu-gems2-ch10-irradiance-maps"><div className="border-b border-border px-5 py-4"><p className="text-sm font-semibold text-primary">Dynamic irradiance map 实验</p><p className="mt-1 text-sm text-secondary">比较 SH band、动态光源和 reflection gloss 对系数预算的影响。</p></div><div className="grid gap-5 p-5 lg:grid-cols-[1fr_230px]"><div className="overflow-hidden rounded-card border border-border bg-[var(--bg)] p-3"><svg viewBox="0 0 540 330" role="img" aria-label="spherical harmonic coefficient tiles 和输出 irradiance map 预览" className="block h-auto w-full"><text x={270} y={25} textAnchor="middle" fontSize={13} fontWeight={700} fill={primary}>SH projection · {ops.coeffs} coefficients</text><rect x={28} y={52} width={212} height={132} rx={14} fill={surface} stroke={accent}/>{Array.from({length: ops.coeffs}, (_, index) => <rect key={index} x={44 + (index % (bands + 1)) * 44} y={70 + Math.floor(index / (bands + 1)) * 28} width={32} height={18} rx={4} fill={index < Math.ceil(ops.coeffs * 0.65) ? accent : border} fillOpacity={index < Math.ceil(ops.coeffs * 0.65) ? 0.2 : 0.32} stroke={border}/>) }<text x={134} y={212} textAnchor="middle" fontSize={12} fill={secondary}>projection ops ≈ {ops.projection}</text><Arrow x1={260} y1={118} x2={314} y2={118}/><rect x={330} y={52} width={182} height={132} rx={14} fill={surface} stroke={success}/><circle cx={421} cy={118} r={48} fill={success} fillOpacity={0.14} stroke={success}/><path d="M 378 136 Q 418 74 467 124" fill="none" stroke={warning} strokeWidth={3}/><text x={421} y={212} textAnchor="middle" fontSize={12} fill={secondary}>evaluation ops ≈ {ops.evaluate}</text><text x={270} y={278} textAnchor="middle" fontSize={12} fill={secondary}>gloss {gloss}% · diffuse uses low bands, glossy needs more detail</text></svg></div><div className="space-y-4"><label className="block text-sm text-secondary">SH band order：{bands}<input className="mt-2 block h-11 w-full accent-[var(--accent)]" type="range" min="1" max="4" value={bands} onChange={(event) => setBands(Number(event.target.value))}/></label><label className="block text-sm text-secondary">动态光源：{lights}<input className="mt-2 block h-11 w-full accent-[var(--accent)]" type="range" min="1" max="12" value={lights} onChange={(event) => setLights(Number(event.target.value))}/></label><label className="block text-sm text-secondary">reflection gloss：{gloss}%<input className="mt-2 block h-11 w-full accent-[var(--accent)]" type="range" min="0" max="100" step="10" value={gloss} onChange={(event) => setGloss(Number(event.target.value))}/></label><p className="rounded-md border border-border bg-[var(--bg)] p-3 text-xs leading-5 text-secondary" aria-live="polite">coefficients = (band + 1)²。diffuse lighting 可用低阶近似；gloss 越高，频域需要保留更多细节。</p><button type="button" className="min-h-11 w-full rounded-md border border-border px-3 py-2 text-sm font-semibold text-primary transition hover:border-[var(--accent)]" onClick={reset}>重置</button></div></div></section>; }
