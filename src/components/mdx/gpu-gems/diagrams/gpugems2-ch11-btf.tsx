"use client";

import { useMemo, useState } from "react";
import type { ReactNode } from "react";

const accent = "var(--accent)";
const success = "var(--success)";
const warning = "var(--warning)";
const danger = "var(--danger)";
const primary = "var(--text-primary)";
const secondary = "var(--text-secondary)";
const border = "var(--border)";
const surface = "var(--surface)";
function Frame({ ariaLabel, caption, children }: { ariaLabel: string; caption: string; children: ReactNode }) { return <figure className="mdx-figure not-prose mx-auto my-6"><div className="overflow-hidden rounded-card border border-border bg-elevated p-5"><svg viewBox="0 0 720 390" role="img" aria-label={ariaLabel} className="mx-auto block h-auto w-full max-w-[720px]">{children}</svg></div><figcaption className="mt-2 text-center text-sm text-secondary">{caption}</figcaption></figure>; }
function Arrow({ x1, y1, x2, y2, color = accent }: { x1: number; y1: number; x2: number; y2: number; color?: string }) { return <><line x1={x1} y1={y1} x2={x2} y2={y2} stroke={color} strokeWidth={2.5}/><path d={`M ${x2 - 8} ${y2 - 5} L ${x2} ${y2} L ${x2 - 8} ${y2 + 5}`} fill="none" stroke={color} strokeWidth={2.5}/></>; }

export function GpuGems2Ch11DimensionalityDiagram() { return <Frame ariaLabel="BRDF 依赖光照和视线方向，加入表面空间变化后 BTF 变成更高维；近似方法采集少量切片" caption="完整 BTF 的采集与存储代价很高；近似方法只捕捉小尺度结构造成的亮度变化。"><text x={36} y={34} fontSize={14} fontWeight={700} fill={primary}>BRDF / BTF：从高维函数到少量可采集切片</text><g transform="translate(48 88)"><rect width={238} height={188} rx={16} fill={surface} stroke={danger}/><text x={119} y={30} textAnchor="middle" fontSize={14} fontWeight={700} fill={danger}>full BTF</text><text x={119} y={78} textAnchor="middle" fontSize={12} fill={secondary}>view + light</text><text x={119} y={104} textAnchor="middle" fontSize={12} fill={secondary}>surface position</text><text x={119} y={130} textAnchor="middle" fontSize={12} fill={secondary}>many samples</text><text x={119} y={166} textAnchor="middle" fontSize={12} fill={danger}>高采集 / 高存储</text></g><Arrow x1={316} y1={182} x2={374} y2={182}/><g transform="translate(396 88)"><rect width={270} height={188} rx={16} fill={surface} stroke={success}/><text x={135} y={30} textAnchor="middle" fontSize={14} fontWeight={700} fill={success}>approximate BTF</text><text x={135} y={78} textAnchor="middle" fontSize={12} fill={secondary}>fixed view</text><text x={135} y={104} textAnchor="middle" fontSize={12} fill={secondary}>light elevation slices</text><text x={135} y={130} textAnchor="middle" fontSize={12} fill={secondary}>volume lookup</text><text x={135} y={166} textAnchor="middle" fontSize={12} fill={success}>快采集 / 实时渲染</text></g></Frame>; }

export function GpuGems2Ch11AcquisitionDiagram() { return <Frame ariaLabel="approximate BTF acquisition：正交相机观察平面材料，平行光沿一条弧线改变入射 elevation，采集多张图像" caption="光源沿不绕法线的弧线移动，样本主要捕捉小尺度结构随入射 elevation 变化的 self-shadowing。"><text x={36} y={34} fontSize={14} fontWeight={700} fill={primary}>Acquisition：相机固定，光源沿 arc 采样</text><g transform="translate(54 78)"><rect x={36} y={154} width={300} height={24} rx={8} fill={surface} stroke={border}/><path d="M 70 154 Q 186 24 302 154" fill="none" stroke={accent} strokeWidth={2} strokeDasharray="7 5"/>{[0,1,2,3,4].map((index) => { const x = 74 + index * 56; const y = 134 - Math.abs(index - 2) * 22; return <g key={index}><line x1={x} y1={y} x2={186} y2={154} stroke={warning} strokeWidth={2}/><circle cx={x} cy={y} r={7} fill={warning}/><text x={x} y={y - 12} textAnchor="middle" fontSize={11} fill={secondary}>L{index}</text></g>; })}<path d="M 186 62 L 186 20 L 220 20 L 220 62" fill="none" stroke={success} strokeWidth={3}/><circle cx={203} cy={15} r={8} fill={success}/><text x={203} y={-2} textAnchor="middle" fontSize={12} fill={secondary}>camera</text><text x={186} y={208} textAnchor="middle" fontSize={12} fill={secondary}>flat material patch</text></g><g transform="translate(432 84)"><rect width={226} height={164} rx={16} fill={surface} stroke={success}/><text x={113} y={32} textAnchor="middle" fontSize={14} fontWeight={700} fill={success}>sample set</text>{Array.from({length: 5}, (_, index) => <rect key={index} x={30 + (index % 2) * 78} y={58 + Math.floor(index / 2) * 30} width={62} height={20} rx={5} fill={index === 4 ? warning : accent} fillOpacity={0.15} stroke={border}/>) }<text x={113} y={148} textAnchor="middle" fontSize={12} fill={secondary}>≈ 10 slices, not thousands</text></g></Frame>; }

export function GpuGems2Ch11VolumeLookupDiagram() { return <Frame ariaLabel="runtime BTF shading：Phong 等简单 BRDF 算亮度 r，以 s,t,r 作为三维 volume texture 坐标，取回细节并乘光源强度" caption="volume texture 的第三维代表平均亮度 slice；trilinear filtering 让 slice 之间连续。"><text x={36} y={34} fontSize={14} fontWeight={700} fill={primary}>Runtime：rough intensity → volume texture slice</text><g transform="translate(46 94)"><rect width={166} height={156} rx={16} fill={surface} stroke={accent}/><text x={83} y={30} textAnchor="middle" fontSize={14} fontWeight={700} fill={accent}>BRDF model</text><text x={83} y={76} textAnchor="middle" fontSize={12} fill={secondary}>view / light</text><text x={83} y={100} textAnchor="middle" fontSize={12} fill={secondary}>normal / gloss</text><text x={83} y={132} textAnchor="middle" fontSize={12} fill={secondary}>r ≈ brightness</text></g><Arrow x1={232} y1={172} x2={284} y2={172}/><g transform="translate(302 76)"><rect width={198} height={196} rx={16} fill={surface} stroke={warning}/><text x={99} y={30} textAnchor="middle" fontSize={14} fontWeight={700} fill={warning}>3D shading map</text>{Array.from({length: 3}, (_, layer) => <g key={layer} transform={`translate(${28 + layer * 8} ${52 + layer * 8})`}><rect width={126} height={92} rx={8} fill={layer === 2 ? warning : accent} fillOpacity={0.1} stroke={border}/>{Array.from({length: 4}, (_, row) => <line key={row} x1={12} y1={20 + row * 16} x2={114} y2={20 + row * 16} stroke={border} strokeOpacity={0.6}/>)}</g>)}<text x={99} y={174} textAnchor="middle" fontSize={12} fill={secondary}>s, t, r / a max</text></g><Arrow x1={524} y1={172} x2={578} y2={172} color={success}/><g transform="translate(594 94)"><rect width={86} height={156} rx={16} fill={surface} stroke={success}/><text x={43} y={34} textAnchor="middle" fontSize={13} fontWeight={700} fill={success}>fine</text><text x={43} y={76} textAnchor="middle" fontSize={12} fill={secondary}>self</text><text x={43} y={98} textAnchor="middle" fontSize={12} fill={secondary}>shadow</text><text x={43} y={132} textAnchor="middle" fontSize={12} fill={secondary}>detail</text></g></Frame>; }

export function GpuGems2Ch11LimitationsDiagram() { return <Frame ariaLabel="approximate BTF 擅长小尺度 isotropic 材料，面对大尺度结构、明显 anisotropy 或强 grazing self-shadowing 会失真" caption="近似技术保留典型的细尺度外观，但不是完整 BTF；适用假设本身就是质量边界。"><text x={36} y={34} fontSize={14} fontWeight={700} fill={primary}>适用边界：mesostructure 的尺度与方向性</text><g transform="translate(58 86)"><rect width={258} height={184} rx={16} fill={surface} stroke={success}/><text x={129} y={30} textAnchor="middle" fontSize={14} fontWeight={700} fill={success}>works well</text><text x={129} y={78} textAnchor="middle" fontSize={12} fill={secondary}>cloth / suede / jeans</text><text x={129} y={104} textAnchor="middle" fontSize={12} fill={secondary}>small-scale features</text><text x={129} y={130} textAnchor="middle" fontSize={12} fill={secondary}>approximately isotropic</text><text x={129} y={164} textAnchor="middle" fontSize={12} fill={success}>快速预览与实时材质</text></g><g transform="translate(404 86)"><rect width={258} height={184} rx={16} fill={surface} stroke={danger}/><text x={129} y={30} textAnchor="middle" fontSize={14} fontWeight={700} fill={danger}>needs care</text><text x={129} y={78} textAnchor="middle" fontSize={12} fill={secondary}>large self-shadow</text><text x={129} y={104} textAnchor="middle" fontSize={12} fill={secondary}>strong anisotropy</text><text x={129} y={130} textAnchor="middle" fontSize={12} fill={secondary}>grazing-angle masking</text><text x={129} y={164} textAnchor="middle" fontSize={12} fill={danger}>近似会漏掉方向性</text></g></Frame>; }

export function GpuGems2Ch11BtfLab() { const [slices, setSlices] = useState(8); const [light, setLight] = useState(48); const [specular, setSpecular] = useState(35); const [anisotropic, setAnisotropic] = useState(false); const data = useMemo(() => ({ layer: Math.min(slices - 1, Math.round(light / 100 * (slices - 1))), detail: Math.round(30 + specular * 0.45 + slices * 1.8), warning: anisotropic ? "方向性超出 arc 采样假设" : "切片插值保持连续" }), [light, slices, specular, anisotropic]); function reset() { setSlices(8); setLight(48); setSpecular(35); setAnisotropic(false); } return <section className="not-prose my-8 overflow-hidden rounded-card border border-border bg-elevated" aria-label="GPU Gems 2 Chapter 11 approximate BTF 实验：调整切片数量、光照亮度、specular 和 anisotropy" data-visual-kind="gpu-gems2-ch11-btf"><div className="border-b border-border px-5 py-4"><p className="text-sm font-semibold text-primary">Approximate BTF 实验</p><p className="mt-1 text-sm text-secondary">观察粗略 BRDF 如何选 volume slice，再叠加采集到的细尺度纹理。</p></div><div className="grid gap-5 p-5 lg:grid-cols-[1fr_230px]"><div className="overflow-hidden rounded-card border border-border bg-[var(--bg)] p-3"><svg viewBox="0 0 540 330" role="img" aria-label="3D shading map slice 和细尺度材质预览" className="block h-auto w-full"><text x={270} y={25} textAnchor="middle" fontSize={13} fontWeight={700} fill={primary}>volume slice {data.layer} / {slices - 1}</text><rect x={26} y={52} width={488} height={156} rx={15} fill={surface} stroke={border}/>{Array.from({length: 8}, (_, row) => <g key={row}>{Array.from({length: 12}, (_, col) => <rect key={col} x={42 + col * 38} y={70 + row * 16} width={29} height={10} rx={3} fill={(row + col + data.layer) % 4 === 0 ? warning : accent} fillOpacity={(row + col + data.layer) % 4 === 0 ? 0.3 : 0.12} stroke={border}/>)}</g>)}<path d="M 48 260 Q 146 212 248 258 T 500 244" fill="none" stroke={success} strokeWidth={3}/><text x={270} y={298} textAnchor="middle" fontSize={12} fill={secondary}>detail {data.detail}% · {data.warning}</text></svg></div><div className="space-y-4"><label className="block text-sm text-secondary">采集切片：{slices}<input className="mt-2 block h-11 w-full accent-[var(--accent)]" type="range" min="4" max="12" value={slices} onChange={(event) => setSlices(Number(event.target.value))}/></label><label className="block text-sm text-secondary">粗略亮度：{light}%<input className="mt-2 block h-11 w-full accent-[var(--accent)]" type="range" min="0" max="100" value={light} onChange={(event) => setLight(Number(event.target.value))}/></label><label className="block text-sm text-secondary">specular：{specular}%<input className="mt-2 block h-11 w-full accent-[var(--accent)]" type="range" min="0" max="100" value={specular} onChange={(event) => setSpecular(Number(event.target.value))}/></label><label className="flex min-h-11 items-center gap-2 text-sm text-secondary"><input type="checkbox" checked={anisotropic} onChange={(event) => setAnisotropic(event.target.checked)}/>模拟 anisotropic 材料</label><p className="rounded-md border border-border bg-[var(--bg)] p-3 text-xs leading-5 text-secondary" aria-live="polite">{anisotropic ? "该方法只沿入射 elevation 采样，强方向性会表现不完整。" : "trilinear slice lookup 让 brightness 变化不出现硬 contour。"}</p><button type="button" className="min-h-11 w-full rounded-md border border-border px-3 py-2 text-sm font-semibold text-primary transition hover:border-[var(--accent)]" onClick={reset}>重置</button></div></div></section>; }
