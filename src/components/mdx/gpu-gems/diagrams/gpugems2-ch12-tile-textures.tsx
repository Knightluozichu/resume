"use client";

import { useMemo, useState } from "react";
import type { ReactNode } from "react";

const accent = "var(--accent)";
const success = "var(--success)";
const warning = "var(--warning)";
const danger = "var(--danger)";
const primary = "var(--text-primary)";
const secondary = "var(--text-secondary)";
const border = "var(--border)";
const surface = "var(--surface)";
function Frame({ ariaLabel, caption, children }: { ariaLabel: string; caption: string; children: ReactNode }) { return <figure className="mdx-figure not-prose mx-auto my-6"><div className="overflow-hidden rounded-card border border-border bg-elevated p-5"><svg viewBox="0 0 720 390" role="img" aria-label={ariaLabel} className="mx-auto block h-auto w-full max-w-[720px]">{children}</svg></div><figcaption className="mt-2 text-center text-sm text-secondary">{caption}</figcaption></figure>; }
function Arrow({ x1, y1, x2, y2, color = accent }: { x1: number; y1: number; x2: number; y2: number; color?: string }) { return <><line x1={x1} y1={y1} x2={x2} y2={y2} stroke={color} strokeWidth={2.5}/><path d={`M ${x2 - 8} ${y2 - 5} L ${x2} ${y2} L ${x2 - 8} ${y2 + 5}`} fill="none" stroke={color} strokeWidth={2.5}/></>; }

export function GpuGems2Ch12VirtualTextureDiagram() { return <Frame ariaLabel="大虚拟纹理由少量 texture tiles 通过 GPU fragment shader 合成，原始 geometry 和 UV 不变" caption="tile-based mapping 把大纹理的存储问题转成 shader 的两次查样：先查 tile 位置，再查 tile 内 texel。"><text x={36} y={34} fontSize={14} fontWeight={700} fill={primary}>Small tile set → large virtual texture</text><g transform="translate(48 86)"><rect width={226} height={188} rx={16} fill={surface} stroke={accent}/><text x={113} y={30} textAnchor="middle" fontSize={14} fontWeight={700} fill={accent}>tile set</text>{Array.from({length: 4}, (_, index) => <rect key={index} x={30 + (index % 2) * 88} y={58 + Math.floor(index / 2) * 54} width={72} height={42} rx={7} fill={index % 2 ? warning : success} fillOpacity={0.16} stroke={border}/>) }<text x={113} y={166} textAnchor="middle" fontSize={12} fill={secondary}>少量输入纹理</text></g><Arrow x1={304} y1={180} x2={360} y2={180}/><g transform="translate(382 86)"><rect width={286} height={188} rx={16} fill={surface} stroke={success}/><text x={143} y={30} textAnchor="middle" fontSize={14} fontWeight={700} fill={success}>virtual texture</text>{Array.from({length: 24}, (_, index) => <rect key={index} x={24 + (index % 6) * 39} y={54 + Math.floor(index / 6) * 29} width={32} height={22} rx={4} fill={(index + Math.floor(index / 6)) % 3 === 0 ? warning : accent} fillOpacity={0.16} stroke={border}/>) }<text x={143} y={166} textAnchor="middle" fontSize={12} fill={secondary}>大输出，重复少、可扩展</text></g></Frame>; }

export function GpuGems2Ch12WangTileDiagram() { return <Frame ariaLabel="Wang tile 四条边编码 edge color，只有相邻 tile 的共享边颜色相同才能无缝拼接" caption="边缘颜色是约束而不是像素颜色；匹配的边经过连续图案设计后，硬件双线性过滤也能跨 tile。"><text x={36} y={34} fontSize={14} fontWeight={700} fill={primary}>Wang tiles：边缘编码让拼接连续</text><g transform="translate(78 84)"><rect width={184} height={184} rx={16} fill={surface} stroke={accent}/><rect x={42} y={48} width={100} height={100} rx={8} fill={accent} fillOpacity={0.1} stroke={accent} strokeWidth={3}/><rect x={82} y={34} width={20} height={14} rx={4} fill={warning}/><rect x={82} y={148} width={20} height={14} rx={4} fill={success}/><rect x={28} y={88} width={14} height={20} rx={4} fill={danger}/><rect x={142} y={88} width={14} height={20} rx={4} fill={warning}/><text x={92} y={178} textAnchor="middle" fontSize={12} fill={secondary}>N / S / W / E edge IDs</text></g><Arrow x1={304} y1={176} x2={360} y2={176}/><g transform="translate(382 84)"><rect width={282} height={184} rx={16} fill={surface} stroke={success}/><rect x={34} y={58} width={88} height={88} rx={8} fill={accent} fillOpacity={0.12} stroke={accent} strokeWidth={2}/><rect x={122} y={58} width={88} height={88} rx={8} fill={warning} fillOpacity={0.12} stroke={warning} strokeWidth={2}/><rect x={114} y={58} width={16} height={88} fill={warning}/><text x={141} y={178} textAnchor="middle" fontSize={12} fill={secondary}>共享边颜色相同 → 连续 pattern</text></g></Frame>; }

export function GpuGems2Ch12LookupDiagram() { return <Frame ariaLabel="tile mapping lookup：输出 UV 缩放后先读取 index texture 得到 tile 坐标，再使用 frac UV 读取 packed tile texture" caption="index texture 不需要 mipmap，因为它存的是离散 tile 行列；最终 tile 样本需要显式 derivatives 保持正确 LOD。"><text x={36} y={34} fontSize={14} fontWeight={700} fill={primary}>Fragment lookup：index first, texel second</text><g transform="translate(42 98)"><rect width={150} height={150} rx={16} fill={surface} stroke={accent}/><text x={75} y={32} textAnchor="middle" fontSize={14} fontWeight={700} fill={accent}>UV (s,t)</text><text x={75} y={78} textAnchor="middle" fontSize={12} fill={secondary}>scale by output tiles</text><text x={75} y={108} textAnchor="middle" fontSize={12} fill={secondary}>tile coord + frac</text></g><Arrow x1={220} y1={174} x2={270} y2={174}/><g transform="translate(290 98)"><rect width={154} height={150} rx={16} fill={surface} stroke={warning}/><text x={77} y={32} textAnchor="middle" fontSize={14} fontWeight={700} fill={warning}>index texture</text><text x={77} y={78} textAnchor="middle" fontSize={12} fill={secondary}>which tile?</text><text x={77} y={108} textAnchor="middle" fontSize={12} fill={secondary}>row / column</text></g><Arrow x1={472} y1={174} x2={522} y2={174} color={success}/><g transform="translate(542 98)"><rect width={136} height={150} rx={16} fill={surface} stroke={success}/><text x={68} y={32} textAnchor="middle" fontSize={13} fontWeight={700} fill={success}>packed tile</text><text x={68} y={78} textAnchor="middle" fontSize={12} fill={secondary}>frac UV</text><text x={68} y={108} textAnchor="middle" fontSize={12} fill={secondary}>ddx / ddy LOD</text></g><text x={360} y={322} textAnchor="middle" fontSize={13} fill={secondary}>一次 virtual sample 需要两次纹理查样，但节省大纹理存储</text></Frame>; }

export function GpuGems2Ch12MipmapDiagram() { return <Frame ariaLabel="tile packing 的低分辨率 mipmap 若直接自动生成会把不相邻 tile 混在一起，需要按 edge color sanitize" caption="高分辨率拼接连续不代表低 mip 合法；要让同色边在每层仍然匹配，可对边缘做统一平均。"><text x={36} y={34} fontSize={14} fontWeight={700} fill={primary}>Mipmap：tile 的边界问题会在低分辨率放大</text><g transform="translate(56 86)"><rect width={260} height={184} rx={16} fill={surface} stroke={danger}/><text x={130} y={30} textAnchor="middle" fontSize={14} fontWeight={700} fill={danger}>naive mip</text><rect x={34} y={58} width={86} height={86} rx={8} fill={accent} fillOpacity={0.18} stroke={accent}/><rect x={120} y={58} width={86} height={86} rx={8} fill={warning} fillOpacity={0.18} stroke={warning}/><path d="M 110 58 L 154 144" stroke={danger} strokeWidth={4}/><text x={130} y={170} textAnchor="middle" fontSize={12} fill={secondary}>错误跨 tile 过滤</text></g><Arrow x1={336} y1={178} x2={386} y2={178}/><g transform="translate(410 86)"><rect width={252} height={184} rx={16} fill={surface} stroke={success}/><text x={126} y={30} textAnchor="middle" fontSize={14} fontWeight={700} fill={success}>sanitized mip</text><rect x={34} y={58} width={86} height={86} rx={8} fill={accent} fillOpacity={0.18} stroke={accent}/><rect x={120} y={58} width={86} height={86} rx={8} fill={warning} fillOpacity={0.18} stroke={warning}/><line x1={120} y1={58} x2={120} y2={144} stroke={success} strokeWidth={4}/><text x={126} y={170} textAnchor="middle" fontSize={12} fill={secondary}>同色 edge 平均校正</text></g></Frame>; }

export function GpuGems2Ch12TileTextureLab() { const [grid, setGrid] = useState(4); const [mip, setMip] = useState(1); const [sanitize, setSanitize] = useState(true); const [pattern, setPattern] = useState("organic"); const visible = useMemo(() => Math.max(2, grid - mip + 1), [grid, mip]); function reset() { setGrid(4); setMip(1); setSanitize(true); setPattern("organic"); } return <section className="not-prose my-8 overflow-hidden rounded-card border border-border bg-elevated" aria-label="GPU Gems 2 Chapter 12 tile-based texture mapping 实验：调整 tile 网格、mipmap、边缘校正和 pattern" data-visual-kind="gpu-gems2-ch12-tile-texture"><div className="border-b border-border px-5 py-4"><p className="text-sm font-semibold text-primary">Tile-based texture 实验</p><p className="mt-1 text-sm text-secondary">观察小 tile set 如何合成大纹理，以及 mipmap sanitation 对边缘连续性的影响。</p></div><div className="grid gap-5 p-5 lg:grid-cols-[1fr_230px]"><div className="overflow-hidden rounded-card border border-border bg-[var(--bg)] p-3"><svg viewBox="0 0 540 330" role="img" aria-label="Wang tiles 组成 virtual texture 的预览" className="block h-auto w-full"><text x={270} y={24} textAnchor="middle" fontSize={13} fontWeight={700} fill={primary}>virtual texture · {pattern} · mip {mip}</text><rect x={24} y={48} width={492} height={194} rx={14} fill={surface} stroke={border}/>{Array.from({length: visible * visible}, (_, index) => { const row = Math.floor(index / visible); const col = index % visible; const warm = (row + col + (pattern === "organic" ? 1 : 0)) % 3 === 0; return <rect key={index} x={44 + col * (444 / visible)} y={66 + row * (158 / visible)} width={Math.max(20, 422 / visible)} height={Math.max(20, 138 / visible)} rx={5} fill={warm ? warning : accent} fillOpacity={0.16} stroke={sanitize ? success : border} strokeOpacity={sanitize ? 0.75 : 0.45}/>; })}<text x={270} y={274} textAnchor="middle" fontSize={12} fill={secondary}>{sanitize ? "edge colors sanitized · filter continuous" : "naive mip · possible seam / popping"}</text><text x={270} y={308} textAnchor="middle" fontSize={12} fill={secondary}>tiles {grid}×{grid} · effective regions {visible}×{visible}</text></svg></div><div className="space-y-4"><label className="block text-sm text-secondary">tile 网格：{grid}×{grid}<input className="mt-2 block h-11 w-full accent-[var(--accent)]" type="range" min="2" max="6" value={grid} onChange={(event) => setGrid(Number(event.target.value))}/></label><label className="block text-sm text-secondary">mipmap level：{mip}<input className="mt-2 block h-11 w-full accent-[var(--accent)]" type="range" min="0" max="3" value={mip} onChange={(event) => setMip(Number(event.target.value))}/></label><label className="block text-sm text-secondary">pattern<select className="mt-2 block h-11 w-full rounded-md border border-border bg-[var(--bg)] px-3 text-sm text-primary" value={pattern} onChange={(event) => setPattern(event.target.value)}><option value="organic">organic</option><option value="line-art">line art</option></select></label><label className="flex min-h-11 items-center gap-2 text-sm text-secondary"><input type="checkbox" checked={sanitize} onChange={(event) => setSanitize(event.target.checked)}/>sanitize lower mip edges</label><p className="rounded-md border border-border bg-[var(--bg)] p-3 text-xs leading-5 text-secondary" aria-live="polite">{sanitize ? "共享 edge color 的 tile 在低 mip 仍保持合法连接。" : "自动 mip 可能把不相邻 tile 混合，出现 seam 或 temporal popping。"}</p><button type="button" className="min-h-11 w-full rounded-md border border-border px-3 py-2 text-sm font-semibold text-primary transition hover:border-[var(--accent)]" onClick={reset}>重置</button></div></div></section>; }
