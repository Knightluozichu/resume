"use client";

import { useMemo, useState } from "react";
import type { ReactNode } from "react";

const accent = "var(--accent)";
const success = "var(--success)";
const warning = "var(--warning)";
const danger = "var(--danger)";
const primary = "var(--text-primary)";
const secondary = "var(--text-secondary)";
const border = "var(--border)";
const surface = "var(--surface)";
function Frame({ ariaLabel, caption, children }: { ariaLabel: string; caption: string; children: ReactNode }) { return <figure className="mdx-figure not-prose mx-auto my-6"><div className="overflow-hidden rounded-card border border-border bg-elevated p-5"><svg viewBox="0 0 720 390" role="img" aria-label={ariaLabel} className="mx-auto block h-auto w-full max-w-[720px]">{children}</svg></div><figcaption className="mt-2 text-center text-sm text-secondary">{caption}</figcaption></figure>; }
function Arrow({ x1, y1, x2, y2, color = accent }: { x1: number; y1: number; x2: number; y2: number; color?: string }) { return <><line x1={x1} y1={y1} x2={x2} y2={y2} stroke={color} strokeWidth={2.5}/><path d={`M ${x2 - 8} ${y2 - 5} L ${x2} ${y2} L ${x2 - 8} ${y2 + 5}`} fill="none" stroke={color} strokeWidth={2.5}/></>; }

export function GpuGems2Ch13ShaderGraphDiagram() { return <Frame ariaLabel="Phenomenon graph 由 ambient、diffuse、specular、light 等 shader 节点组成，输入连到 root shader 输出" caption="生产渲染器把材质、灯光、纹理和 volume 拆成可复用节点，再在场景中按图连接。"><text x={36} y={34} fontSize={14} fontWeight={700} fill={primary}>Phenomenon：可组合的 shader graph</text><g transform="translate(48 88)"><rect width={148} height={56} rx={12} fill={surface} stroke={accent}/><text x={74} y={34} textAnchor="middle" fontSize={13} fontWeight={700} fill={accent}>texture</text><rect y={78} width={148} height={56} rx={12} fill={surface} stroke={warning}/><text x={74} y={112} textAnchor="middle" fontSize={13} fontWeight={700} fill={warning}>light</text><rect y={156} width={148} height={56} rx={12} fill={surface} stroke={success}/><text x={74} y={190} textAnchor="middle" fontSize={13} fontWeight={700} fill={success}>normal / volume</text></g><Arrow x1={218} y1={116} x2={282} y2={116}/><Arrow x1={218} y1={194} x2={282} y2={194} color={warning}/><g transform="translate(302 104)"><rect width={174} height={126} rx={16} fill={surface} stroke={danger} strokeWidth={2}/><text x={87} y={38} textAnchor="middle" fontSize={14} fontWeight={700} fill={danger}>root material</text><text x={87} y={72} textAnchor="middle" fontSize={12} fill={secondary}>Phong / custom</text><text x={87} y={98} textAnchor="middle" fontSize={12} fill={secondary}>eval(params)</text></g><Arrow x1={498} y1={166} x2={554} y2={166} color={success}/><g transform="translate(574 104)"><rect width={106} height={126} rx={16} fill={surface} stroke={success}/><text x={53} y={38} textAnchor="middle" fontSize={13} fontWeight={700} fill={success}>color</text><text x={53} y={72} textAnchor="middle" fontSize={12} fill={secondary}>fragment</text><text x={53} y={98} textAnchor="middle" fontSize={12} fill={secondary}>result</text></g></Frame>; }

export function GpuGems2Ch13InterfaceDiagram() { return <Frame ariaLabel="Cg shader interface 以 eval 方法连接抽象参数类型，具体 shader struct 可插入 miiColor、miiScalar 等接口" caption="接口让组合发生在编译前：输出类型与输入类型匹配，runtime 再把具体 struct 接到 graph 上。"><text x={36} y={34} fontSize={14} fontWeight={700} fill={primary}>Interface：用 eval 把抽象参数接成具体实现</text><g transform="translate(50 90)"><rect width={194} height={174} rx={16} fill={surface} stroke={accent}/><text x={97} y={30} textAnchor="middle" fontSize={14} fontWeight={700} fill={accent}>miiColor</text><text x={97} y={78} textAnchor="middle" fontSize={12} fill={secondary}>float4 eval(p)</text><text x={97} y={112} textAnchor="middle" fontSize={12} fill={secondary}>abstract input</text><text x={97} y={146} textAnchor="middle" fontSize={12} fill={secondary}>return type</text></g><Arrow x1={270} y1={176} x2={326} y2={176}/><g transform="translate(344 90)"><rect width={164} height={174} rx={16} fill={surface} stroke={warning}/><text x={82} y={30} textAnchor="middle" fontSize={14} fontWeight={700} fill={warning}>miScalar</text><text x={82} y={78} textAnchor="middle" fontSize={12} fill={secondary}>constant / texture</text><text x={82} y={112} textAnchor="middle" fontSize={12} fill={secondary}>concrete struct</text><text x={82} y={146} textAnchor="middle" fontSize={12} fill={secondary}>eval(p)</text></g><Arrow x1={532} y1={176} x2={588} y2={176} color={success}/><g transform="translate(606 90)"><rect width={74} height={174} rx={16} fill={surface} stroke={success}/><text x={37} y={30} textAnchor="middle" fontSize={12} fontWeight={700} fill={success}>root</text><text x={37} y={80} textAnchor="middle" fontSize={12} fill={secondary}>linked</text><text x={37} y={112} textAnchor="middle" fontSize={12} fill={secondary}>program</text></g><text x={360} y={324} textAnchor="middle" fontSize={13} fill={secondary}>same shader graph can connect constants, textures or other Phenomena</text></Frame>; }

export function GpuGems2Ch13CompilePipelineDiagram() { const steps = [["Collect", "root + subshaders", accent], ["Connect", "interfaces / uniforms", warning], ["Compile", "one fragment program", success], ["Fallback", "software if unsupported", danger]] as const; return <Frame ariaLabel="mental ray GPU conversion pipeline：收集 Phenomenon graph，连接 interfaces，编译单一 GPU program，失败则 software fallback" caption="GPU 不能在运行时调用多个独立 shader；graph 必须在下载前组成一个可编译的 fragment program。"><text x={36} y={34} fontSize={14} fontWeight={700} fill={primary}>Scene graph → generated GPU program → fallback</text><g transform="translate(38 100)">{steps.map(([title, detail, color], index) => <g key={title} transform={`translate(${index * 166} 0)`}><rect width={138} height={150} rx={16} fill={surface} stroke={color} strokeWidth={2}/><text x={69} y={50} textAnchor="middle" fontSize={14} fontWeight={700} fill={color}>{title}</text><text x={69} y={90} textAnchor="middle" fontSize={11} fill={secondary}>{detail.split(" + ")[0]}</text><text x={69} y={112} textAnchor="middle" fontSize={11} fill={secondary}>{detail.split(" + ")[1] || ""}</text></g>)}</g><Arrow x1={180} y1={174} x2={202} y2={174}/><Arrow x1={346} y1={174} x2={368} y2={174} color={warning}/><Arrow x1={512} y1={174} x2={534} y2={174} color={success}/><text x={360} y={318} textAnchor="middle" fontSize={13} fill={secondary}>compile result 和资源状态共同决定本对象走 hardware 还是 software pass</text></Frame>; }

export function GpuGems2Ch13VaryingStateDiagram() { return <Frame ariaLabel="vertex program 将 position、normal、多个 UV、derivU/derivV 和 face 等 varying 参数传给 fragment program，同时绑定 global state" caption="统一的 vert2frag 和 global uniforms 让多数 Phenomenon 共享输入语义，节点只专注自己的 eval。"><text x={36} y={34} fontSize={14} fontWeight={700} fill={primary}>Shared inputs：varyings + global state</text><g transform="translate(54 86)"><rect width={228} height={184} rx={16} fill={surface} stroke={accent}/><text x={114} y={30} textAnchor="middle" fontSize={14} fontWeight={700} fill={accent}>vertex → fragment</text><text x={114} y={72} textAnchor="middle" fontSize={12} fill={secondary}>position / normal</text><text x={114} y={98} textAnchor="middle" fontSize={12} fill={secondary}>texCoord 0–3</text><text x={114} y={124} textAnchor="middle" fontSize={12} fill={secondary}>derivU / derivV</text><text x={114} y={158} textAnchor="middle" fontSize={12} fill={secondary}>screen / face</text></g><Arrow x1={320} y1={178} x2={378} y2={178}/><g transform="translate(398 86)"><rect width={266} height={184} rx={16} fill={surface} stroke={success}/><text x={133} y={30} textAnchor="middle" fontSize={14} fontWeight={700} fill={success}>global uniforms</text><text x={133} y={72} textAnchor="middle" fontSize={12} fill={secondary}>miToWorld / miFromWorld</text><text x={133} y={98} textAnchor="middle" fontSize={12} fill={secondary}>camera resolution</text><text x={133} y={124} textAnchor="middle" fontSize={12} fill={secondary}>shadow map state</text><text x={133} y={158} textAnchor="middle" fontSize={12} fill={secondary}>shared across nodes</text></g></Frame>; }

export function GpuGems2Ch13PhenomenaLab() { const [nodes, setNodes] = useState(4); const [transparent, setTransparent] = useState(false); const [gpuCap, setGpuCap] = useState(80); const compiled = useMemo(() => nodes <= 5 && gpuCap >= 55, [nodes, gpuCap]); const passes = transparent ? 2 : 1; function reset() { setNodes(4); setTransparent(false); setGpuCap(80); } return <section className="not-prose my-8 overflow-hidden rounded-card border border-border bg-elevated" aria-label="GPU Gems 2 Chapter 13 Phenomena renderer 实验：调整 shader nodes、透明 pass 和 GPU 能力" data-visual-kind="gpu-gems2-ch13-phenomena"><div className="border-b border-border px-5 py-4"><p className="text-sm font-semibold text-primary">Phenomena GPU composition 实验</p><p className="mt-1 text-sm text-secondary">观察 graph 规模、透明 fallback 和硬件能力如何决定编译与渲染路径。</p></div><div className="grid gap-5 p-5 lg:grid-cols-[1fr_230px]"><div className="overflow-hidden rounded-card border border-border bg-[var(--bg)] p-3"><svg viewBox="0 0 540 330" role="img" aria-label="Phenomenon graph 组合编译与 fallback 预览" className="block h-auto w-full"><text x={270} y={24} textAnchor="middle" fontSize={13} fontWeight={700} fill={primary}>{nodes} shader nodes → {compiled ? "GPU program" : "software fallback"}</text><rect x={24} y={52} width={492} height={120} rx={14} fill={surface} stroke={border}/>{Array.from({length: nodes}, (_, index) => <g key={index}><rect x={44 + index * (420 / Math.max(nodes, 1))} y={84 + (index % 2) * 28} width={Math.max(45, 62 - nodes * 2)} height={22} rx={6} fill={index === nodes - 1 ? warning : accent} fillOpacity={0.16} stroke={index === nodes - 1 ? warning : accent}/><text x={44 + index * (420 / Math.max(nodes, 1)) + Math.max(45, 62 - nodes * 2) / 2} y={99 + (index % 2) * 28} textAnchor="middle" fontSize={11} fill={secondary}>node {index + 1}</text></g>)}<text x={270} y={204} textAnchor="middle" fontSize={12} fill={secondary}>passes {passes} · {transparent ? "opaque GPU + transparent depth peel" : "opaque GPU"}</text><rect x={94} y={234} width={352} height={36} rx={9} fill={compiled ? success : danger} fillOpacity={0.14} stroke={compiled ? success : danger}/><text x={270} y={257} textAnchor="middle" fontSize={12} fontWeight={700} fill={compiled ? success : danger}>{compiled ? "interfaces connected · compile succeeds" : "profile / memory limit · render in software"}</text><text x={270} y={304} textAnchor="middle" fontSize={12} fill={secondary}>示意：最终应以真实 Cg compiler 和 GPU profile 验证</text></svg></div><div className="space-y-4"><label className="block text-sm text-secondary">shader nodes：{nodes}<input className="mt-2 block h-11 w-full accent-[var(--accent)]" type="range" min="2" max="8" value={nodes} onChange={(event) => setNodes(Number(event.target.value))}/></label><label className="block text-sm text-secondary">GPU capability：{gpuCap}%<input className="mt-2 block h-11 w-full accent-[var(--accent)]" type="range" min="20" max="100" value={gpuCap} onChange={(event) => setGpuCap(Number(event.target.value))}/></label><label className="flex min-h-11 items-center gap-2 text-sm text-secondary"><input type="checkbox" checked={transparent} onChange={(event) => setTransparent(event.target.checked)}/>包含透明对象</label><p className="rounded-md border border-border bg-[var(--bg)] p-3 text-xs leading-5 text-secondary" aria-live="polite">{compiled ? "可组合节点被编译成一个 GPU program；透明对象仍需要额外 pass。" : "保留 software fallback，避免为了 GPU 限制牺牲整帧正确性。"}</p><button type="button" className="min-h-11 w-full rounded-md border border-border px-3 py-2 text-sm font-semibold text-primary transition hover:border-[var(--accent)]" onClick={reset}>重置</button></div></div></section>; }
