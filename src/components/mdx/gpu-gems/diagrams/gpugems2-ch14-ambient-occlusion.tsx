"use client";

import { useMemo, useState } from "react";
import type { ReactNode } from "react";

const accent = "var(--accent)";
const success = "var(--success)";
const warning = "var(--warning)";
const danger = "var(--warning)";
const primary = "var(--text-primary)";
const secondary = "var(--text-secondary)";
const border = "var(--border)";
const surface = "var(--surface)";

function Frame({ ariaLabel, caption, children }: { ariaLabel: string; caption: string; children: ReactNode }) {
  return <figure className="mdx-figure not-prose mx-auto my-6"><div className="overflow-hidden rounded-card border border-border bg-elevated p-5"><svg viewBox="0 0 720 390" role="img" aria-label={ariaLabel} className="mx-auto block h-auto w-full max-w-[720px]">{children}</svg></div><figcaption className="mt-2 text-center text-sm text-secondary">{caption}</figcaption></figure>;
}

function Arrow({ x1, y1, x2, y2, color = accent }: { x1: number; y1: number; x2: number; y2: number; color?: string }) {
  return <><line x1={x1} y1={y1} x2={x2} y2={y2} stroke={color} strokeWidth={2.5}/><path d={`M ${x2 - 8} ${y2 - 5} L ${x2} ${y2} L ${x2 - 8} ${y2 + 5}`} fill="none" stroke={color} strokeWidth={2.5}/></>;
}

export function GpuGems2Ch14SurfaceElementsDiagram() {
  const vertices = [[86, 230], [188, 112], [300, 230], [412, 112], [524, 230]];
  return <Frame ariaLabel="多边形网格转换为带位置、法线和面积的定向圆盘 surface elements" caption="每个顶点变成一个带位置、法线和面积的定向圆盘；共享三角形面积按三分之一分摊。"><text x={36} y={34} fontSize={14} fontWeight={700} fill={primary}>mesh vertices → oriented surface elements</text><path d="M 86 230 L 188 112 L 300 230 L 412 112 L 524 230" fill="none" stroke={border} strokeWidth={3}/>{vertices.map(([x, y], index) => <g key={index}><circle cx={x} cy={y} r={index === 2 ? 27 : 21} fill={accent} fillOpacity={0.18} stroke={accent} strokeWidth={2}/><line x1={x} y1={y} x2={x + 24} y2={y - 35} stroke={success} strokeWidth={2.5}/><path d={`M ${x + 18} ${y - 38} L ${x + 26} ${y - 36} L ${x + 24} ${y - 28}`} fill="none" stroke={success} strokeWidth={2}/><text x={x} y={y + 48} textAnchor="middle" fontSize={12} fill={secondary}>p{index + 1} · A{index + 1}</text></g>)}<rect x={78} y={300} width={560} height={48} rx={12} fill={surface} stroke={warning}/><text x={358} y={321} textAnchor="middle" fontSize={12} fill={warning}>element = position + normal + area</text><text x={358} y={339} textAnchor="middle" fontSize={12} fill={secondary}>面积可按相邻三角形的 1/3 累加，动画时位置与法线更新</text></Frame>;
}

export function GpuGems2Ch14OcclusionPassDiagram() {
  return <Frame ariaLabel="ambient occlusion 两遍算法：第一遍累加所有 emitter，第二遍使用 emitter accessibility 修正重叠阴影并计算 bent normal" caption="第二遍让已经被遮挡的 emitter 少投射阴影，同时在最终遍计算 bent normal。"><text x={36} y={34} fontSize={14} fontWeight={700} fill={primary}>AO：two passes without explicit visibility rays</text><g transform="translate(46 88)"><rect width={250} height={176} rx={16} fill={surface} stroke={accent} strokeWidth={2}/><text x={125} y={34} textAnchor="middle" fontSize={14} fontWeight={700} fill={accent}>pass 1 · raw shadow</text><circle cx={72} cy={112} r={24} fill={warning} fillOpacity={0.2} stroke={warning}/><circle cx={182} cy={112} r={24} fill={accent} fillOpacity={0.2} stroke={accent}/><line x1={96} y1={112} x2={158} y2={112} stroke={warning} strokeWidth={3}/><text x={125} y={153} textAnchor="middle" fontSize={12} fill={secondary}>accessibility₁ = 1 − Σ shadow</text></g><Arrow x1={322} y1={176} x2={382} y2={176}/><g transform="translate(406 88)"><rect width={268} height={176} rx={16} fill={surface} stroke={success} strokeWidth={2}/><text x={134} y={34} textAnchor="middle" fontSize={14} fontWeight={700} fill={success}>pass 2 · corrected</text><circle cx={76} cy={112} r={24} fill={warning} fillOpacity={0.2} stroke={warning}/><circle cx={192} cy={112} r={24} fill={accent} fillOpacity={0.2} stroke={accent}/><line x1={100} y1={112} x2={168} y2={112} stroke={success} strokeWidth={3}/><line x1={76} y1={112} x2={52} y2={76} stroke={success} strokeWidth={2}/><text x={134} y={153} textAnchor="middle" fontSize={12} fill={secondary}>shadow × accessibility₁ → bent normal</text></g><text x={360} y={324} textAnchor="middle" fontSize={13} fill={secondary}>将结果写回 texture，再用 fragment pass 逐 element 读取</text></Frame>;
}

export function GpuGems2Ch14HierarchyDiagram() {
  return <Frame ariaLabel="surface element 层级：远处使用较大的 parent element，近处展开 children，复杂度由 O(n²) 近似降到 O(n log n)" caption="远处的大圆盘足够近似软阴影；只有接近 receiver 时才展开子节点，避免每个 receiver 遍历所有叶子。"><text x={36} y={34} fontSize={14} fontWeight={700} fill={primary}>hierarchy traversal：远处聚合，近处展开</text><g transform="translate(72 88)"><circle cx={100} cy={98} r={76} fill={accent} fillOpacity={0.14} stroke={accent} strokeWidth={2}/><text x={100} y={94} textAnchor="middle" fontSize={14} fontWeight={700} fill={accent}>parent</text><text x={100} y={118} textAnchor="middle" fontSize={12} fill={secondary}>far emitter</text></g><Arrow x1={230} y1={186} x2={290} y2={186}/><g transform="translate(316 76)"><circle cx={74} cy={74} r={30} fill={warning} fillOpacity={0.15} stroke={warning}/><circle cx={148} cy={74} r={30} fill={warning} fillOpacity={0.15} stroke={warning}/><circle cx={74} cy={150} r={30} fill={warning} fillOpacity={0.15} stroke={warning}/><circle cx={148} cy={150} r={30} fill={warning} fillOpacity={0.15} stroke={warning}/><text x={111} y={236} textAnchor="middle" fontSize={13} fill={warning}>children · near emitter</text></g><path d="M 548 186 C 588 186 606 140 636 126" fill="none" stroke={success} strokeWidth={2.5}/><path d="M 548 186 C 588 186 606 232 636 246" fill="none" stroke={success} strokeWidth={2.5}/><text x={660} y={121} textAnchor="end" fontSize={12} fill={success}>d² ≥ 4A: accept parent</text><text x={660} y={247} textAnchor="end" fontSize={12} fill={secondary}>otherwise: descend</text><rect x={74} y={302} width={572} height={44} rx={11} fill={surface} stroke={border}/><text x={360} y={329} textAnchor="middle" fontSize={13} fill={secondary}>树上采样让实际遍历接近 O(n log n)，而不是 O(n²)</text></Frame>;
}

export function GpuGems2Ch14IndirectLightingDiagram() {
  return <Frame ariaLabel="indirect lighting：surface element 接收直接或环境光，按材质颜色反射，再通过 disk-to-disk form factor 传给 receiver" caption="一次 bounce 的间接光可以复用 element 数据结构：只需把标量 shadow 换成 RGB radiance transfer。"><text x={36} y={34} fontSize={14} fontWeight={700} fill={primary}>direct light → reflected radiance → receiver</text><g transform="translate(54 98)"><rect width={156} height={126} rx={14} fill={surface} stroke={warning}/><text x={78} y={38} textAnchor="middle" fontSize={14} fontWeight={700} fill={warning}>source</text><text x={78} y={70} textAnchor="middle" fontSize={12} fill={secondary}>environment / area</text><text x={78} y={94} textAnchor="middle" fontSize={12} fill={secondary}>light × albedo</text></g><Arrow x1={238} y1={160} x2={302} y2={160} color={warning}/><g transform="translate(324 98)"><rect width={166} height={126} rx={14} fill={surface} stroke={accent}/><text x={83} y={38} textAnchor="middle" fontSize={14} fontWeight={700} fill={accent}>emitter</text><text x={83} y={70} textAnchor="middle" fontSize={12} fill={secondary}>radiance RGB</text><text x={83} y={94} textAnchor="middle" fontSize={12} fill={secondary}>form factor</text></g><Arrow x1={518} y1={160} x2={582} y2={160} color={success}/><g transform="translate(604 98)"><rect width={76} height={126} rx={14} fill={surface} stroke={success}/><text x={38} y={38} textAnchor="middle" fontSize={12} fontWeight={700} fill={success}>receiver</text><text x={38} y={72} textAnchor="middle" fontSize={12} fill={secondary}>+ AO</text><text x={38} y={96} textAnchor="middle" fontSize={12} fill={secondary}>color</text></g><text x={360} y={298} textAnchor="middle" fontSize={13} fill={secondary}>F ≈ A · saturate(Nₑ·V) · saturate(Nᵣ·V) / (d² + A)</text><text x={360} y={330} textAnchor="middle" fontSize={12} fill={secondary}>先 transfer，再用同一套 shadowing pass 去掉被遮挡的光</text></Frame>;
}

export function GpuGems2Ch14AmbientOcclusionLab() {
  const [elements, setElements] = useState(1800);
  const [passes, setPasses] = useState(2);
  const [hierarchy, setHierarchy] = useState(true);
  const [bounce, setBounce] = useState(1);
  const result = useMemo(() => {
    const raw = Math.min(1, 0.28 + elements / 8000);
    const correction = passes > 1 ? 0.16 : 0;
    const accessibility = Math.max(0.18, 1 - raw + correction);
    const cost = hierarchy ? Math.round(elements * Math.log2(Math.max(2, elements)) / 120) : Math.round(elements * elements / 3000);
    return { accessibility, cost };
  }, [elements, hierarchy, passes]);
  function reset() { setElements(1800); setPasses(2); setHierarchy(true); setBounce(1); }
  return <section className="not-prose my-8 overflow-hidden rounded-card border border-border bg-elevated" aria-label="GPU Gems 2 Chapter 14 dynamic ambient occlusion 实验：调整 surface elements、pass、层级和 indirect bounce" data-visual-kind="gpu-gems2-ch14-ambient-occlusion"><div className="border-b border-border px-5 py-4"><p className="text-sm font-semibold text-primary">Dynamic AO / indirect lighting 实验</p><p className="mt-1 text-sm text-secondary">对比两遍修正和 element hierarchy 对 accessibility、间接光和估算成本的影响。</p></div><div className="grid gap-5 p-5 lg:grid-cols-[1fr_240px]"><div className="overflow-hidden rounded-card border border-border bg-[var(--bg)] p-3"><svg viewBox="0 0 560 350" role="img" aria-label="可视化 ambient accessibility、bent normal、hierarchy cost 和 indirect bounce" className="block h-auto w-full"><text x={280} y={25} textAnchor="middle" fontSize={13} fontWeight={700} fill={primary}>{elements.toLocaleString()} elements · {hierarchy ? "hierarchical" : "flat"} traversal</text><rect x={30} y={52} width={500} height={116} rx={14} fill={surface} stroke={border}/>{Array.from({length: 9}, (_, index) => { const x = 70 + index * 52; const blocked = index === 3 || index === 4 || index === 5; return <g key={index}><circle cx={x} cy={112} r={blocked ? 21 : 16} fill={blocked ? danger : accent} fillOpacity={0.18} stroke={blocked ? danger : accent}/>{blocked && <line x1={x} y1={112} x2={x + 20} y2={82} stroke={warning} strokeWidth={2}/>}</g>; })}<line x1={260} y1={148} x2={260 + result.accessibility * 90} y2={148} stroke={success} strokeWidth={4}/><text x={40} y={194} fontSize={12} fill={secondary}>accessibility ≈ {(result.accessibility * 100).toFixed(0)}%</text><text x={520} y={194} textAnchor="end" fontSize={12} fill={secondary}>bent normal ← least occluded</text><rect x={30} y={218} width={500} height={42} rx={10} fill={bounce > 1 ? success : warning} fillOpacity={0.14} stroke={bounce > 1 ? success : warning}/><text x={280} y={244} textAnchor="middle" fontSize={12} fontWeight={700} fill={bounce > 1 ? success : warning}>{bounce} bounce · {bounce > 1 ? "secondary radiance added" : "direct + one transfer pass"}</text><text x={280} y={302} textAnchor="middle" fontSize={12} fill={secondary}>estimated traversal work: {result.cost.toLocaleString()} units</text><text x={280} y={326} textAnchor="middle" fontSize={11} fill={secondary}>示意值：实际画质由 element 分布和材质/光源决定</text></svg></div><div className="space-y-4"><label className="block text-sm text-secondary">surface elements：{elements.toLocaleString()}<input className="mt-2 block h-11 w-full accent-[var(--accent)]" type="range" min="400" max="5000" step="100" value={elements} onChange={(event) => setElements(Number(event.target.value))}/></label><label className="block text-sm text-secondary">AO passes：{passes}<input className="mt-2 block h-11 w-full accent-[var(--accent)]" type="range" min="1" max="3" value={passes} onChange={(event) => setPasses(Number(event.target.value))}/></label><label className="block text-sm text-secondary">indirect bounces：{bounce}<input className="mt-2 block h-11 w-full accent-[var(--accent)]" type="range" min="0" max="2" value={bounce} onChange={(event) => setBounce(Number(event.target.value))}/></label><label className="flex min-h-11 items-center gap-2 text-sm text-secondary"><input type="checkbox" checked={hierarchy} onChange={(event) => setHierarchy(event.target.checked)}/>启用 element hierarchy</label><p className="rounded-md border border-border bg-[var(--bg)] p-3 text-xs leading-5 text-secondary" aria-live="polite">{passes > 1 ? "第二遍抑制 double shadowing，并可计算 bent normal。" : "单遍结果容易过暗；增加一遍让 emitter accessibility 参与修正。"} {hierarchy ? "远处节点聚合，遍历成本可控。" : "平铺遍历会迅速接近 O(n²)。"}</p><button type="button" className="min-h-11 w-full rounded-md border border-border px-3 py-2 text-sm font-semibold text-primary transition hover:border-[var(--accent)]" onClick={reset}>重置</button></div></div></section>;
}
