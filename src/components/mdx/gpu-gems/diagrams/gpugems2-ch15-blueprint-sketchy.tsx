"use client";

import { useMemo, useState } from "react";
import type { ReactNode } from "react";

const accent = "var(--accent)";
const success = "var(--success)";
const warning = "var(--warning)";
const danger = "var(--danger)";
const primary = "var(--text-primary)";
const secondary = "var(--text-secondary)";
const border = "var(--border)";
const surface = "var(--surface)";

function Frame({ ariaLabel, caption, children }: { ariaLabel: string; caption: string; children: ReactNode }) {
  return <figure className="mdx-figure not-prose mx-auto my-6"><div className="overflow-hidden rounded-card border border-border bg-elevated p-5"><svg viewBox="0 0 720 390" role="img" aria-label={ariaLabel} className="mx-auto block h-auto w-full max-w-[720px]">{children}</svg></div><figcaption className="mt-2 text-center text-sm text-secondary">{caption}</figcaption></figure>;
}

function Arrow({ x1, y1, x2, y2, color = accent }: { x1: number; y1: number; x2: number; y2: number; color?: string }) {
  return <><line x1={x1} y1={y1} x2={x2} y2={y2} stroke={color} strokeWidth={2.5}/><path d={`M ${x2 - 8} ${y2 - 5} L ${x2} ${y2} L ${x2 - 8} ${y2 + 5}`} fill="none" stroke={color} strokeWidth={2.5}/></>;
}

export function GpuGems2Ch15IntermediateBuffersDiagram() {
  return <Frame ariaLabel="blueprint rendering 中从几何生成 normal buffer 和 z-buffer，再通过邻域采样生成 edge map" caption="先保存可以复用的中间渲染结果：normal 和 z 的不连续性提供 silhouette、border、crease 等边缘。"><text x={36} y={34} fontSize={14} fontWeight={700} fill={primary}>geometry → G-buffers → edge map</text><g transform="translate(48 92)"><rect width={148} height={156} rx={15} fill={surface} stroke={accent}/><text x={74} y={34} textAnchor="middle" fontSize={14} fontWeight={700} fill={accent}>3D object</text><path d="M 36 112 L 74 66 L 112 112 Z" fill={accent} fillOpacity={0.16} stroke={accent} strokeWidth={2}/><circle cx={74} cy={66} r={6} fill={success}/></g><Arrow x1={228} y1={170} x2={278} y2={170}/><g transform="translate(300 78)"><rect width={154} height={82} rx={13} fill={surface} stroke={warning}/><text x={77} y={32} textAnchor="middle" fontSize={14} fontWeight={700} fill={warning}>normal buffer</text><text x={77} y={58} textAnchor="middle" fontSize={12} fill={secondary}>encoded N</text><rect y={94} width={154} height={82} rx={13} fill={surface} stroke={warning}/><text x={77} y={126} textAnchor="middle" fontSize={14} fontWeight={700} fill={warning}>z-buffer</text><text x={77} y={152} textAnchor="middle" fontSize={12} fill={secondary}>high precision Z</text></g><Arrow x1={488} y1={170} x2={540} y2={170} color={success}/><g transform="translate(558 104)"><rect width={112} height={132} rx={14} fill={surface} stroke={success} strokeWidth={2}/><text x={56} y={36} textAnchor="middle" fontSize={14} fontWeight={700} fill={success}>edge map</text><path d="M 22 94 L 50 56 L 82 96" fill="none" stroke={success} strokeWidth={5}/><text x={56} y={119} textAnchor="middle" fontSize={12} fill={secondary}>normal / Z Δ</text></g><text x={360} y={314} textAnchor="middle" fontSize={13} fill={secondary}>screen-aligned quad 采样邻居 texels，输出每像素 edge intensity</text></Frame>;
}

export function GpuGems2Ch15DepthPeelingDiagram() {
  return <Frame ariaLabel="depth peeling 逐层提取屏幕像素的唯一深度层，保存每层 depth map 和 color map，直到满足终止条件" caption="普通 depth test 只得到最小 z；depth peeling 让后续 pass 找到第二层、第三层等被遮挡结构。"><text x={36} y={34} fontSize={14} fontWeight={700} fill={primary}>depth peeling：layer 1 → layer 2 → layer 3</text><g transform="translate(46 86)">{[[accent, "layer 1", "visible"], [warning, "layer 2", "occluded"], [success, "layer 3", "deep"]].map(([color, title, detail], index) => <g key={title} transform={`translate(${index * 208} 0)`}><rect width={166} height={184} rx={15} fill={surface} stroke={color} strokeWidth={2}/><text x={83} y={34} textAnchor="middle" fontSize={14} fontWeight={700} fill={color}>{title}</text><path d="M 40 132 L 82 68 L 126 132 Z" fill={color} fillOpacity={0.15} stroke={color} strokeWidth={2}/><line x1={24} y1={150} x2={142} y2={150} stroke={border} strokeWidth={3}/><text x={83} y={174} textAnchor="middle" fontSize={12} fill={secondary}>{detail} · depth map</text></g>)}</g><Arrow x1={212} y1={178} x2={250} y2={178} color={warning}/><Arrow x1={420} y1={178} x2={458} y2={178} color={success}/><text x={360} y={324} textAnchor="middle" fontSize={13} fill={secondary}>每层同时保存 z 与 color，直到 no fragment 或达到 max depth complexity</text></Frame>;
}

export function GpuGems2Ch15BlueprintCompositionDiagram() {
  return <Frame ariaLabel="blueprint composition 将可见和不可见 edge maps 按深度顺序作为 depth sprites 合成，并用边缘强度做 alpha blending" caption="每层 edge map 先以 depth sprite 恢复正确 z，再以 edge intensity 控制蓝图颜色与透明度。"><text x={36} y={34} fontSize={14} fontWeight={700} fill={primary}>edge layers + depth sprites → blueprint</text><g transform="translate(48 96)"><rect width={174} height={132} rx={14} fill={surface} stroke={accent}/><text x={87} y={34} textAnchor="middle" fontSize={14} fontWeight={700} fill={accent}>visible edges</text><path d="M 32 96 L 72 50 L 132 100" fill="none" stroke={accent} strokeWidth={5}/><text x={87} y={116} textAnchor="middle" fontSize={12} fill={secondary}>layer 1</text></g><g transform="translate(48 246)"><rect width={174} height={60} rx={12} fill={surface} stroke={warning}/><text x={87} y={36} textAnchor="middle" fontSize={12} fill={warning}>occluded edges · layer 2+</text></g><Arrow x1={260} y1={170} x2={316} y2={170}/><g transform="translate(338 86)"><rect width={156} height={162} rx={15} fill={surface} stroke={success}/><text x={78} y={34} textAnchor="middle" fontSize={14} fontWeight={700} fill={success}>depth sprite</text><text x={78} y={70} textAnchor="middle" fontSize={12} fill={secondary}>replace quad z</text><text x={78} y={98} textAnchor="middle" fontSize={12} fill={secondary}>discard z = 1</text><text x={78} y={126} textAnchor="middle" fontSize={12} fill={secondary}>ordinary depth test</text></g><Arrow x1={532} y1={170} x2={582} y2={170} color={success}/><g transform="translate(604 98)"><rect width={76} height={144} rx={14} fill={surface} stroke={warning}/><text x={38} y={36} textAnchor="middle" fontSize={12} fontWeight={700} fill={warning}>blueprint</text><path d="M 18 106 L 36 68 L 58 106" fill="none" stroke={warning} strokeWidth={4}/><text x={38} y={132} textAnchor="middle" fontSize={11} fill={secondary}>blend</text></g><text x={360} y={342} textAnchor="middle" fontSize={13} fill={secondary}>depth masking 可用目标组件的可见比例提前终止 peeling</text></Frame>;
}

export function GpuGems2Ch15SketchyUncertaintyDiagram() {
  return <Frame ariaLabel="sketchy rendering 通过 edge map、shade map 和几何属性驱动的 uncertainty 对纹理坐标做不同扰动，形成稳定的手绘效果" caption="edge 与 shade 使用不同的扰动函数；加入 object-space 几何属性后，笔触不再只在屏幕上漂移。"><text x={36} y={34} fontSize={14} fontWeight={700} fill={primary}>geometry-aware uncertainty → sketchy drawing</text><g transform="translate(46 96)"><rect width={150} height={132} rx={14} fill={surface} stroke={accent}/><text x={75} y={34} textAnchor="middle" fontSize={14} fontWeight={700} fill={accent}>edge map</text><path d="M 28 96 L 72 48 L 126 98" fill="none" stroke={accent} strokeWidth={5}/><text x={75} y={118} textAnchor="middle" fontSize={12} fill={secondary}>fEdge(s,t)</text></g><g transform="translate(46 246)"><rect width={150} height={60} rx={12} fill={surface} stroke={warning}/><text x={75} y={36} textAnchor="middle" fontSize={12} fill={warning}>shade map · fShade</text></g><Arrow x1={236} y1={170} x2={286} y2={170}/><g transform="translate(308 78)"><rect width={178} height={176} rx={15} fill={surface} stroke={success}/><text x={89} y={34} textAnchor="middle" fontSize={14} fontWeight={700} fill={success}>uncertainty</text><text x={89} y={72} textAnchor="middle" fontSize={12} fill={secondary}>noise texture</text><text x={89} y={100} textAnchor="middle" fontSize={12} fill={secondary}>object position / normal</text><text x={89} y={128} textAnchor="middle" fontSize={12} fill={secondary}>2×2 perturb matrix</text><text x={89} y={156} textAnchor="middle" fontSize={12} fill={secondary}>s,t → s′,t′</text></g><Arrow x1={526} y1={170} x2={576} y2={170} color={warning}/><g transform="translate(596 98)"><rect width={84} height={144} rx={14} fill={surface} stroke={warning}/><text x={42} y={38} textAnchor="middle" fontSize={12} fontWeight={700} fill={warning}>sketch</text><path d="M 16 108 L 34 70 L 58 110" fill="none" stroke={warning} strokeWidth={4}/><circle cx={18} cy={92} r={4} fill={warning}/><circle cx={62} cy={84} r={4} fill={warning}/></g><text x={360} y={326} textAnchor="middle" fontSize={13} fill={secondary}>enlarged geometry 把可靠的 uncertainty 延伸到边界附近的 exterior regions</text></Frame>;
}

export function GpuGems2Ch15BlueprintSketchyLab() {
  const [layers, setLayers] = useState(3);
  const [threshold, setThreshold] = useState(24);
  const [uncertainty, setUncertainty] = useState(42);
  const [anchored, setAnchored] = useState(true);
  const result = useMemo(() => {
    const visibleEdges = Math.min(100, threshold + layers * 12);
    const stability = anchored ? Math.max(20, 100 - uncertainty / 2) : 100 - uncertainty;
    const cost = layers * (anchored ? 18 : 24);
    return { visibleEdges, stability, cost };
  }, [anchored, layers, threshold, uncertainty]);
  function reset() { setLayers(3); setThreshold(24); setUncertainty(42); setAnchored(true); }
  return <section className="not-prose my-8 overflow-hidden rounded-card border border-border bg-elevated" aria-label="GPU Gems 2 Chapter 15 blueprint 和 sketchy rendering 实验：调整 depth layers、终止阈值、uncertainty 和几何锚定" data-visual-kind="gpu-gems2-ch15-blueprint-sketchy"><div className="border-b border-border px-5 py-4"><p className="text-sm font-semibold text-primary">Blueprint / sketchy rendering 实验</p><p className="mt-1 text-sm text-secondary">观察 peeling 层数、遮挡阈值与 geometry-aware uncertainty 如何改变覆盖率、成本和稳定度。</p></div><div className="grid gap-5 p-5 lg:grid-cols-[1fr_240px]"><div className="overflow-hidden rounded-card border border-border bg-[var(--bg)] p-3"><svg viewBox="0 0 560 350" role="img" aria-label="可视化 blueprint edge layers 与 sketchy uncertainty 的结果" className="block h-auto w-full"><text x={280} y={25} textAnchor="middle" fontSize={13} fontWeight={700} fill={primary}>{layers} depth layers · {anchored ? "geometry anchored" : "screen-space only"}</text><rect x={30} y={52} width={500} height={132} rx={14} fill={surface} stroke={border}/><path d="M 92 150 L 178 76 L 264 150 M 146 103 L 214 103" fill="none" stroke={accent} strokeWidth={5}/><path d="M 316 150 L 382 82 L 468 150" fill="none" stroke={warning} strokeWidth={5} strokeDasharray={uncertainty > 55 ? "3 10" : "12 6"}/>{Array.from({length: 7}, (_, index) => <circle key={index} cx={320 + index * 25} cy={68 + (index % 3) * 30} r={index % 2 ? 4 : 3} fill={warning} fillOpacity={0.7}/>)}<text x={155} y={214} textAnchor="middle" fontSize={12} fill={accent}>blueprint edges · {result.visibleEdges}%</text><text x={405} y={214} textAnchor="middle" fontSize={12} fill={warning}>sketch stability · {result.stability.toFixed(0)}%</text><rect x={30} y={238} width={500} height={42} rx={10} fill={success} fillOpacity={0.14} stroke={success}/><text x={280} y={264} textAnchor="middle" fontSize={12} fontWeight={700} fill={success}>estimated passes {result.cost} · threshold {threshold}% fragments</text><text x={280} y={322} textAnchor="middle" fontSize={11} fill={secondary}>示意值：真实效果还取决于 edge map、noise 和目标几何</text></svg></div><div className="space-y-4"><label className="block text-sm text-secondary">depth layers：{layers}<input className="mt-2 block h-11 w-full accent-[var(--accent)]" type="range" min="1" max="6" value={layers} onChange={(event) => setLayers(Number(event.target.value))}/></label><label className="block text-sm text-secondary">终止阈值：{threshold}%<input className="mt-2 block h-11 w-full accent-[var(--accent)]" type="range" min="5" max="60" value={threshold} onChange={(event) => setThreshold(Number(event.target.value))}/></label><label className="block text-sm text-secondary">uncertainty：{uncertainty}%<input className="mt-2 block h-11 w-full accent-[var(--accent)]" type="range" min="0" max="90" value={uncertainty} onChange={(event) => setUncertainty(Number(event.target.value))}/></label><label className="flex min-h-11 items-center gap-2 text-sm text-secondary"><input type="checkbox" checked={anchored} onChange={(event) => setAnchored(event.target.checked)}/>使用 object-space 几何锚定</label><p className="rounded-md border border-border bg-[var(--bg)] p-3 text-xs leading-5 text-secondary" aria-live="polite">{anchored ? "扰动使用位置/法线，减少 shower-door effect。" : "纯屏幕噪声容易随相机或动画漂移。"} layers 越多，隐藏结构越完整但 pass 成本越高。</p><button type="button" className="min-h-11 w-full rounded-md border border-border px-3 py-2 text-sm font-semibold text-primary transition hover:border-[var(--accent)]" onClick={reset}>重置</button></div></div></section>;
}
