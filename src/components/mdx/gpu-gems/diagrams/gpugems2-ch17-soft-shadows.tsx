"use client";

import { useMemo, useState } from "react";
import type { ReactNode } from "react";

const accent = "var(--accent)";
const success = "var(--success)";
const warning = "var(--warning)";
const danger = "var(--danger)";
const primary = "var(--text-primary)";
const secondary = "var(--text-secondary)";
const border = "var(--border)";
const surface = "var(--surface)";

function Frame({ ariaLabel, caption, children }: { ariaLabel: string; caption: string; children: ReactNode }) {
  return <figure className="mdx-figure not-prose mx-auto my-6"><div className="overflow-hidden rounded-card border border-border bg-elevated p-5"><svg viewBox="0 0 720 390" role="img" aria-label={ariaLabel} className="mx-auto block h-auto w-full max-w-[720px]">{children}</svg></div><figcaption className="mt-2 text-center text-sm text-secondary">{caption}</figcaption></figure>;
}

function Arrow({ x1, y1, x2, y2, color = accent }: { x1: number; y1: number; x2: number; y2: number; color?: string }) {
  return <><line x1={x1} y1={y1} x2={x2} y2={y2} stroke={color} strokeWidth={2.5}/><path d={`M ${x2 - 8} ${y2 - 5} L ${x2} ${y2} L ${x2 - 8} ${y2 + 5}`} fill="none" stroke={color} strokeWidth={2.5}/></>;
}

export function GpuGems2Ch17PcfDiagram() {
  const samples = Array.from({ length: 12 }, (_, index) => { const a = index * Math.PI / 6; return [Math.round(110 + Math.cos(a) * 48), Math.round(142 + Math.sin(a) * 48)]; });
  return <Frame ariaLabel="percentage-closer filtering 在一张 shadow map 的投影坐标周围采样并平均比较结果，形成 soft penumbra" caption="PCF 不模糊 depth 值，而是对多个偏移坐标的深度比较结果求平均；偏移半径决定边缘柔软度。"><text x={36} y={34} fontSize={14} fontWeight={700} fill={primary}>one shadow map → many comparisons → soft edge</text><g transform="translate(40 84)"><rect width={190} height={172} rx={15} fill={surface} stroke={accent}/><text x={95} y={32} textAnchor="middle" fontSize={14} fontWeight={700} fill={accent}>shadow map</text><rect x={40} y={58} width={110} height={84} fill={accent} fillOpacity={0.08} stroke={border}/><line x1={48} y1={132} x2={142} y2={68} stroke={warning} strokeWidth={3}/><circle cx={95} cy={100} r={7} fill={success}/><text x={95} y={160} textAnchor="middle" fontSize={12} fill={secondary}>depth compare</text></g><Arrow x1={260} y1={170} x2={322} y2={170}/><g transform="translate(344 84)"><rect width={176} height={172} rx={15} fill={surface} stroke={warning}/><text x={88} y={32} textAnchor="middle" fontSize={14} fontWeight={700} fill={warning}>offset samples</text><circle cx={110} cy={142} r={58} fill={warning} fillOpacity={0.08} stroke={warning} strokeDasharray="5 5"/>{samples.map(([x, y], index) => <circle key={index} cx={x} cy={y} r={4} fill={index % 3 ? warning : success}/>) }<text x={88} y={160} textAnchor="middle" fontSize={12} fill={secondary}>compare → average</text></g><Arrow x1={550} y1={170} x2={604} y2={170} color={success}/><g transform="translate(622 98)"><rect width={58} height={144} rx={14} fill={surface} stroke={success}/><text x={29} y={36} textAnchor="middle" fontSize={12} fontWeight={700} fill={success}>soft</text><path d="M 14 108 C 28 92 35 84 46 66" fill="none" stroke={success} strokeWidth={7} strokeOpacity={0.55}/><text x={29} y={132} textAnchor="middle" fontSize={11} fill={secondary}>shadow</text></g><text x={360} y={326} textAnchor="middle" fontSize={13} fill={secondary}>样本数 ↑ 与 filter radius ↑ 会改善 penumbra，也会增加 bandwidth</text></Frame>;
}

export function GpuGems2Ch17JitterDiagram() {
  const grid = Array.from({ length: 16 }, (_, index) => [index % 4, Math.floor(index / 4)]);
  return <Frame ariaLabel="stratified jitter sampling 将 square domain 分成等面积 strata，在每格随机取样，再 area-preserving warp 到 disk" caption="规则 grid 会形成 banding；每个 strata 内做 jitter，再映射到 disk，可把规律误差换成更容易隐藏的高频噪声。"><text x={36} y={34} fontSize={14} fontWeight={700} fill={primary}>regular grid → stratified jitter → disk offsets</text><g transform="translate(48 92)"><rect width={154} height={154} rx={13} fill={surface} stroke={accent}/>{grid.map(([x, y], index) => <g key={index}><rect x={12 + x * 33} y={12 + y * 33} width={33} height={33} fill={accent} fillOpacity={0.05} stroke={border}/><circle cx={28 + x * 33} cy={28 + y * 33} r={3.5} fill={accent}/></g>)}<text x={77} y={178} textAnchor="middle" fontSize={12} fill={secondary}>regular strata</text></g><Arrow x1={238} y1={168} x2={292} y2={168}/><g transform="translate(314 92)"><rect width={154} height={154} rx={13} fill={surface} stroke={warning}/>{grid.map(([x, y], index) => <g key={index}><rect x={12 + x * 33} y={12 + y * 33} width={33} height={33} fill={warning} fillOpacity={0.05} stroke={border}/><circle cx={20 + x * 33 + (index * 7) % 15} cy={20 + y * 33 + (index * 11) % 15} r={3.5} fill={warning}/></g>)}<text x={77} y={178} textAnchor="middle" fontSize={12} fill={secondary}>jitter in each cell</text></g><Arrow x1={504} y1={168} x2={556} y2={168} color={success}/><g transform="translate(580 92)"><rect width={98} height={154} rx={13} fill={surface} stroke={success}/><circle cx={49} cy={82} r={58} fill={success} fillOpacity={0.07} stroke={success}/>{grid.map(([x, y], index) => { const a = (index + 0.5) * Math.PI * 2 / 16; const radius = 15 + ((index * 13) % 36); return <circle key={index} cx={49 + Math.cos(a) * radius} cy={82 + Math.sin(a) * radius} r={3.5} fill={success}/>; })}<text x={49} y={136} textAnchor="middle" fontSize={12} fill={secondary}>disk warp</text></g><text x={360} y={326} textAnchor="middle" fontSize={13} fill={secondary}>strata 保证覆盖，jitter 隐藏采样规则带来的 color bands</text></Frame>;
}

export function GpuGems2Ch17AdaptiveBranchDiagram() {
  return <Frame ariaLabel="adaptive PCF 先用少量估计样本判断像素在 fully lit、fully shadowed 还是 penumbra，再对 penumbra 扩大采样" caption="只在 penumbra 做高成本 PCF；完全亮或完全暗的像素可以 early-out，省下纹理带宽和 fragment ALU。"><text x={36} y={34} fontSize={14} fontWeight={700} fill={primary}>estimate → branch → adaptive sample budget</text><g transform="translate(48 92)"><rect width={164} height={168} rx={15} fill={surface} stroke={accent}/><text x={82} y={32} textAnchor="middle" fontSize={14} fontWeight={700} fill={accent}>estimate</text><circle cx={52} cy={96} r={18} fill={success} fillOpacity={0.2} stroke={success}/><circle cx={112} cy={96} r={18} fill={danger} fillOpacity={0.2} stroke={danger}/><circle cx={82} cy={140} r={18} fill={warning} fillOpacity={0.2} stroke={warning}/><text x={52} y={101} textAnchor="middle" fontSize={11} fill={success}>lit</text><text x={112} y={101} textAnchor="middle" fontSize={11} fill={danger}>dark</text><text x={82} y={145} textAnchor="middle" fontSize={11} fill={warning}>soft</text></g><Arrow x1={248} y1={176} x2={302} y2={176}/><g transform="translate(324 76)"><rect width={176} height={200} rx={15} fill={surface} stroke={warning}/><text x={88} y={32} textAnchor="middle" fontSize={14} fontWeight={700} fill={warning}>branch</text><path d="M 88 56 L 48 108 L 48 154" fill="none" stroke={success} strokeWidth={2}/><path d="M 88 56 L 128 108 L 128 154" fill="none" stroke={danger} strokeWidth={2}/><text x={42} y={180} textAnchor="middle" fontSize={12} fill={success}>early out</text><text x={134} y={180} textAnchor="middle" fontSize={12} fill={warning}>full PCF</text></g><Arrow x1={532} y1={176} x2={584} y2={176} color={success}/><g transform="translate(606 98)"><rect width={74} height={154} rx={14} fill={surface} stroke={success}/><text x={37} y={38} textAnchor="middle" fontSize={12} fontWeight={700} fill={success}>result</text><text x={37} y={82} textAnchor="middle" fontSize={11} fill={secondary}>low cost</text><text x={37} y={108} textAnchor="middle" fontSize={11} fill={secondary}>where possible</text></g><text x={360} y={328} textAnchor="middle" fontSize={13} fill={secondary}>branch 只有在局部像素路径一致时才真正高效</text></Frame>;
}

export function GpuGems2Ch17CoherenceDiagram() {
  return <Frame ariaLabel="GPU fragment branch 的收益取决于局部像素是否沿同一路径；coherent block 可以一起 early-out，divergent block 会损失并行度" caption="动态 branching 并非免费：邻域像素选择同一分支时能保留并行，路径发散时收益会被 pipeline 和 SIMD 利用率抵消。"><text x={36} y={34} fontSize={14} fontWeight={700} fill={primary}>branch coherence：same path good, divergence expensive</text><g transform="translate(48 90)"><rect width={250} height={166} rx={15} fill={surface} stroke={success}/><text x={125} y={32} textAnchor="middle" fontSize={14} fontWeight={700} fill={success}>coherent tile</text>{Array.from({length: 12}, (_, index) => <rect key={index} x={38 + (index % 4) * 44} y={58 + Math.floor(index / 4) * 28} width={28} height={18} rx={4} fill={success} fillOpacity={0.2} stroke={success}/>)}<text x={125} y={148} textAnchor="middle" fontSize={12} fill={secondary}>all early-out together</text></g><Arrow x1={330} y1={174} x2={388} y2={174} color={warning}/><g transform="translate(414 90)"><rect width={250} height={166} rx={15} fill={surface} stroke={warning}/><text x={125} y={32} textAnchor="middle" fontSize={14} fontWeight={700} fill={warning}>divergent tile</text>{Array.from({length: 12}, (_, index) => <rect key={index} x={38 + (index % 4) * 44} y={58 + Math.floor(index / 4) * 28} width={28} height={18} rx={4} fill={index % 3 ? warning : accent} fillOpacity={0.2} stroke={index % 3 ? warning : accent}/>)}<text x={125} y={148} textAnchor="middle" fontSize={12} fill={secondary}>paths serialize / lose benefit</text></g><text x={360} y={326} textAnchor="middle" fontSize={13} fill={secondary}>让 estimator 具有空间连续性，尽量让相邻 pixels 选择相同 branch</text></Frame>;
}

export function GpuGems2Ch17SoftShadowsLab() {
  const [samples, setSamples] = useState(32);
  const [penumbra, setPenumbra] = useState(46);
  const [coherence, setCoherence] = useState(78);
  const [adaptive, setAdaptive] = useState(true);
  const result = useMemo(() => {
    const smoothness = Math.min(100, 38 + samples * 1.6 + penumbra * 0.22);
    const banding = Math.max(4, 76 - samples * 1.8 - (adaptive ? 12 : 0));
    const activePixels = adaptive ? Math.round(100 - coherence * 0.62) : 100;
    const cost = Math.round(samples * activePixels / 100 + (adaptive ? 4 : 0));
    return { smoothness, banding, cost };
  }, [adaptive, coherence, penumbra, samples]);
  function reset() { setSamples(32); setPenumbra(46); setCoherence(78); setAdaptive(true); }
  return <section className="not-prose my-8 overflow-hidden rounded-card border border-border bg-elevated" aria-label="GPU Gems 2 Chapter 17 soft shadow 实验：调整 PCF samples、penumbra、branch coherence 和 adaptive sampling" data-visual-kind="gpu-gems2-ch17-soft-shadows"><div className="border-b border-border px-5 py-4"><p className="text-sm font-semibold text-primary">Adaptive soft-shadow 实验</p><p className="mt-1 text-sm text-secondary">观察 PCF 样本、penumbra、分支一致性与 early-out 如何共同影响阴影质量和成本。</p></div><div className="grid gap-5 p-5 lg:grid-cols-[1fr_240px]"><div className="overflow-hidden rounded-card border border-border bg-[var(--bg)] p-3"><svg viewBox="0 0 560 350" role="img" aria-label="可视化软阴影 penumbra、banding、PCF 样本和 adaptive 分支成本" className="block h-auto w-full"><text x={280} y={25} textAnchor="middle" fontSize={13} fontWeight={700} fill={primary}>{samples} PCF samples · {adaptive ? "adaptive" : "fixed"}</text><rect x={30} y={52} width={500} height={112} rx={14} fill={surface} stroke={border}/><path d="M 52 136 C 142 120 180 78 262 104 S 388 138 508 84" fill="none" stroke={danger} strokeWidth={18} strokeOpacity={0.16}/><path d="M 52 136 C 142 120 180 78 262 104 S 388 138 508 84" fill="none" stroke={accent} strokeWidth={5} strokeDasharray={result.banding > 35 ? "5 8" : "18 4"}/>{Array.from({length: 9}, (_, index) => <circle key={index} cx={70 + index * 50} cy={134 - index * 5} r={3 + (samples > 24 ? 1 : 0)} fill={warning} fillOpacity={0.75}/>) }<text x={45} y={195} fontSize={12} fill={success}>softness {result.smoothness.toFixed(0)}%</text><text x={515} y={195} textAnchor="end" fontSize={12} fill={warning}>banding risk {result.banding.toFixed(0)}%</text><rect x={30} y={222} width={500} height={42} rx={10} fill={success} fillOpacity={0.14} stroke={success}/><text x={280} y={248} textAnchor="middle" fontSize={12} fontWeight={700} fill={success}>estimated active work {result.cost} · coherence {coherence}%</text><text x={280} y={314} textAnchor="middle" fontSize={11} fill={secondary}>示意值：真实质量还受 shadow-map resolution、bias 与投影方式影响</text></svg></div><div className="space-y-4"><label className="block text-sm text-secondary">PCF samples：{samples}<input className="mt-2 block h-11 w-full accent-[var(--accent)]" type="range" min="4" max="64" step="4" value={samples} onChange={(event) => setSamples(Number(event.target.value))}/></label><label className="block text-sm text-secondary">penumbra radius：{penumbra}%<input className="mt-2 block h-11 w-full accent-[var(--accent)]" type="range" min="10" max="90" value={penumbra} onChange={(event) => setPenumbra(Number(event.target.value))}/></label><label className="block text-sm text-secondary">branch coherence：{coherence}%<input className="mt-2 block h-11 w-full accent-[var(--accent)]" type="range" min="10" max="100" value={coherence} onChange={(event) => setCoherence(Number(event.target.value))}/></label><label className="flex min-h-11 items-center gap-2 text-sm text-secondary"><input type="checkbox" checked={adaptive} onChange={(event) => setAdaptive(event.target.checked)}/>启用 adaptive early-out</label><p className="rounded-md border border-border bg-[var(--bg)] p-3 text-xs leading-5 text-secondary" aria-live="polite">{adaptive ? "先估计再扩采样；局部路径越 coherent，early-out 越有价值。" : "固定采样画质稳定但每个像素都承担完整 PCF 成本。"} 高 penumbra 仍需要更多 offsets。</p><button type="button" className="min-h-11 w-full rounded-md border border-border px-3 py-2 text-sm font-semibold text-primary transition hover:border-[var(--accent)]" onClick={reset}>重置</button></div></div></section>;
}
