"use client";

import {
  LearningSystemEvidenceLab,
  type LearningSystemEvidenceModel,
} from "./learning-system-evidence-lab";

const model = {
  unitId: "idl-03",
  title: "第3章 卷积神经网络",
  question:
    "怎样根据输入、核、步幅和填充推导特征图形状，并证明局部连接和共享参数实际生效？",
  concepts: [
    "第3章 卷积神经网络",
    "3.1 卷积神经网络的结构",
    "3.2 卷积层",
    "3.3 池化层",
    "3.4 全连接层",
    "3.5 输出层",
    "3.6 神经网络的训练方法",
    "3.7 小结",
  ],
  stages: [
    {
      name: "图像张量",
      input: "第3章 卷积神经网络：冻结的样本、任务或上游张量",
      operation:
        "登记形状、版本和边界，并守住“张量布局、核尺寸、步幅、填充、池化、头部与标签契约固定，前向形状和反向梯度一致”",
      output: "可追踪输入",
      evidence:
        "数据卡、哈希与形状；出现“错误理解填充或通道维导致特征图尺寸不符，却在全连接层硬改输入维度掩盖上游错误”时保留失败记录",
    },
    {
      name: "卷积",
      input: "第3章 卷积神经网络：上一阶段输出",
      operation:
        "执行“第3章 卷积神经网络”的核心变换，并守住“张量布局、核尺寸、步幅、填充、池化、头部与标签契约固定，前向形状和反向梯度一致”",
      output: "中间表示或状态",
      evidence:
        "中间张量与参数；出现“错误理解填充或通道维导致特征图尺寸不符，却在全连接层硬改输入维度掩盖上游错误”时保留失败记录",
    },
    {
      name: "激活与池化",
      input: "第3章 卷积神经网络：表示、标签或采样状态",
      operation:
        "计算本阶段目标或条件量，并守住“张量布局、核尺寸、步幅、填充、池化、头部与标签契约固定，前向形状和反向梯度一致”",
      output: "标量目标或概率",
      evidence:
        "公式、数值与对照；出现“错误理解填充或通道维导致特征图尺寸不符，却在全连接层硬改输入维度掩盖上游错误”时保留失败记录",
    },
    {
      name: "分类头",
      input: "第3章 卷积神经网络：目标、参数和随机状态",
      operation:
        "只改变声明的学习变量，并守住“张量布局、核尺寸、步幅、填充、池化、头部与标签契约固定，前向形状和反向梯度一致”",
      output: "更新后参数或样本",
      evidence:
        "梯度、种子与差分；出现“错误理解填充或通道维导致特征图尺寸不符，却在全连接层硬改输入维度掩盖上游错误”时保留失败记录",
    },
    {
      name: "损失与反传",
      input: "第3章 卷积神经网络：冻结模型与留出数据",
      operation:
        "按预注册协议评估并归档，并守住“张量布局、核尺寸、步幅、填充、池化、头部与标签契约固定，前向形状和反向梯度一致”",
      output: "结论、拒绝或迁移决定",
      evidence:
        "指标、反例与环境锁；出现“错误理解填充或通道维导致特征图尺寸不符，却在全连接层硬改输入维度掩盖上游错误”时保留失败记录",
    },
  ],
  scenarios: [
    {
      name: "冻结基线",
      condition:
        "对一幅小图像手算卷积与池化，再用同一核平移比较输出位置，最后接分类头和损失。 固定数据、代码、依赖、初值与随机种子。",
      expectation:
        "沿“图像张量 → 卷积 → 激活与池化 → 分类头 → 损失与反传”得到满足“张量布局、核尺寸、步幅、填充、池化、头部与标签契约固定，前向形状和反向梯度一致”的完整证据。",
    },
    {
      name: "边界反例",
      condition:
        "对一幅小图像手算卷积与池化，再用同一核平移比较输出位置，最后接分类头和损失。 其余条件不变，只注入“错误理解填充或通道维导致特征图尺寸不符，却在全连接层硬改输入维度掩盖上游错误”。",
      expectation:
        "最早偏离应出现在对应阶段；若只能从最终指标猜测，证据链不通过。",
    },
  ],
  normalTrace: [
    "为“第3章 卷积神经网络”冻结任务、数据切分、代码、环境、参数初值与随机种子",
    "依次执行图像张量、卷积，保存输入和中间状态",
    "继续执行激活与池化、分类头，记录目标、梯度、采样或更新",
    "在损失与反传阶段交付输入布局、卷积核、步幅、填充、输出形状、特征图、池化索引、分类头、损失、梯度与边界样例。",
  ],
  failureTrace: [
    "“第3章 卷积神经网络”复用同一任务、数据、代码、环境、参数初值与随机种子",
    "只注入单一故障：错误理解填充或通道维导致特征图尺寸不符，却在全连接层硬改输入维度掩盖上游错误",
    "沿“图像张量 → 卷积 → 激活与池化 → 分类头 → 损失与反传”定位第一处数值、状态或边界偏离",
    "撤销故障并重放；仅当“张量布局、核尺寸、步幅、填充、池化、头部与标签契约固定，前向形状和反向梯度一致”恢复才接受修正",
  ],
  invariant:
    "张量布局、核尺寸、步幅、填充、池化、头部与标签契约固定，前向形状和反向梯度一致",
  fault:
    "错误理解填充或通道维导致特征图尺寸不符，却在全连接层硬改输入维度掩盖上游错误",
  artifact:
    "输入布局、卷积核、步幅、填充、输出形状、特征图、池化索引、分类头、损失、梯度与边界样例。",
  gates: [
    {
      label: "数据切分",
      detail: "“第3章 卷积神经网络”的训练、验证、测试边界和预处理统计可追溯。",
    },
    {
      label: "目标与梯度",
      detail:
        "“第3章 卷积神经网络”的前向值、目标、梯度或采样更新经过数值核对。",
    },
    {
      label: "基线与消融",
      detail:
        "“第3章 卷积神经网络”保留简单基线，只改变一个变量并保存失败样本。",
    },
    {
      label: "复现与历史",
      detail:
        "“第3章 卷积神经网络”归档环境、种子和工件，并分开2016语境与当前迁移。",
    },
  ],
} as const satisfies LearningSystemEvidenceModel;

export function Idl03ConvolutionalNeuralNetworksSignalPathLab() {
  return <LearningSystemEvidenceLab model={model} view="signal-path" />;
}

export function Idl03ConvolutionalNeuralNetworksTrainingTraceLab() {
  return <LearningSystemEvidenceLab model={model} view="training-trace" />;
}

export function Idl03ConvolutionalNeuralNetworksExperimentGateLab() {
  return <LearningSystemEvidenceLab model={model} view="experiment-gate" />;
}
