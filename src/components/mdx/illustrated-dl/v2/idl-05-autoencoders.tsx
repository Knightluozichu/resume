"use client";

import {
  LearningSystemEvidenceLab,
  type LearningSystemEvidenceModel,
} from "./learning-system-evidence-lab";

const model = {
  unitId: "idl-05",
  title: "第5章 自编码器",
  question: "怎样证明编码器学到可迁移表示，而不是通过容量过大简单复制输入？",
  concepts: [
    "第5章 自编码器",
    "5.1 自编码器",
    "5.2 降噪自编码器",
    "5.3 稀疏自编码器",
    "5.4 栈式自编码器",
    "5.5 在预训练中的应用",
    "5.6 小结",
  ],
  stages: [
    {
      name: "输入与破坏",
      input: "第5章 自编码器：冻结的样本、任务或上游张量",
      operation:
        "登记形状、版本和边界，并守住“训练与验证划分、破坏过程、瓶颈或稀疏约束、重构目标和下游评估固定，泄漏样本隔离”",
      output: "可追踪输入",
      evidence:
        "数据卡、哈希与形状；出现“模型容量足以学习恒等映射，重构误差很低却没有瓶颈、降噪、稀疏或下游迁移证据”时保留失败记录",
    },
    {
      name: "编码器",
      input: "第5章 自编码器：上一阶段输出",
      operation:
        "执行“第5章 自编码器”的核心变换，并守住“训练与验证划分、破坏过程、瓶颈或稀疏约束、重构目标和下游评估固定，泄漏样本隔离”",
      output: "中间表示或状态",
      evidence:
        "中间张量与参数；出现“模型容量足以学习恒等映射，重构误差很低却没有瓶颈、降噪、稀疏或下游迁移证据”时保留失败记录",
    },
    {
      name: "潜在表示",
      input: "第5章 自编码器：表示、标签或采样状态",
      operation:
        "计算本阶段目标或条件量，并守住“训练与验证划分、破坏过程、瓶颈或稀疏约束、重构目标和下游评估固定，泄漏样本隔离”",
      output: "标量目标或概率",
      evidence:
        "公式、数值与对照；出现“模型容量足以学习恒等映射，重构误差很低却没有瓶颈、降噪、稀疏或下游迁移证据”时保留失败记录",
    },
    {
      name: "解码器",
      input: "第5章 自编码器：目标、参数和随机状态",
      operation:
        "只改变声明的学习变量，并守住“训练与验证划分、破坏过程、瓶颈或稀疏约束、重构目标和下游评估固定，泄漏样本隔离”",
      output: "更新后参数或样本",
      evidence:
        "梯度、种子与差分；出现“模型容量足以学习恒等映射，重构误差很低却没有瓶颈、降噪、稀疏或下游迁移证据”时保留失败记录",
    },
    {
      name: "重构与预训练",
      input: "第5章 自编码器：冻结模型与留出数据",
      operation:
        "按预注册协议评估并归档，并守住“训练与验证划分、破坏过程、瓶颈或稀疏约束、重构目标和下游评估固定，泄漏样本隔离”",
      output: "结论、拒绝或迁移决定",
      evidence:
        "指标、反例与环境锁；出现“模型容量足以学习恒等映射，重构误差很低却没有瓶颈、降噪、稀疏或下游迁移证据”时保留失败记录",
    },
  ],
  scenarios: [
    {
      name: "冻结基线",
      condition:
        "对输入添加受控噪声，比较普通、降噪和稀疏自编码器，再把编码器用于小样本分类预训练。 固定数据、代码、依赖、初值与随机种子。",
      expectation:
        "沿“输入与破坏 → 编码器 → 潜在表示 → 解码器 → 重构与预训练”得到满足“训练与验证划分、破坏过程、瓶颈或稀疏约束、重构目标和下游评估固定，泄漏样本隔离”的完整证据。",
    },
    {
      name: "边界反例",
      condition:
        "对输入添加受控噪声，比较普通、降噪和稀疏自编码器，再把编码器用于小样本分类预训练。 其余条件不变，只注入“模型容量足以学习恒等映射，重构误差很低却没有瓶颈、降噪、稀疏或下游迁移证据”。",
      expectation:
        "最早偏离应出现在对应阶段；若只能从最终指标猜测，证据链不通过。",
    },
  ],
  normalTrace: [
    "为“第5章 自编码器”冻结任务、数据切分、代码、环境、参数初值与随机种子",
    "依次执行输入与破坏、编码器，保存输入和中间状态",
    "继续执行潜在表示、解码器，记录目标、梯度、采样或更新",
    "在重构与预训练阶段交付原始与破坏样本、编码器/解码器结构、潜变量、稀疏度、重构损失、验证误差、下游基线和预训练消融。",
  ],
  failureTrace: [
    "“第5章 自编码器”复用同一任务、数据、代码、环境、参数初值与随机种子",
    "只注入单一故障：模型容量足以学习恒等映射，重构误差很低却没有瓶颈、降噪、稀疏或下游迁移证据",
    "沿“输入与破坏 → 编码器 → 潜在表示 → 解码器 → 重构与预训练”定位第一处数值、状态或边界偏离",
    "撤销故障并重放；仅当“训练与验证划分、破坏过程、瓶颈或稀疏约束、重构目标和下游评估固定，泄漏样本隔离”恢复才接受修正",
  ],
  invariant:
    "训练与验证划分、破坏过程、瓶颈或稀疏约束、重构目标和下游评估固定，泄漏样本隔离",
  fault:
    "模型容量足以学习恒等映射，重构误差很低却没有瓶颈、降噪、稀疏或下游迁移证据",
  artifact:
    "原始与破坏样本、编码器/解码器结构、潜变量、稀疏度、重构损失、验证误差、下游基线和预训练消融。",
  gates: [
    {
      label: "数据切分",
      detail: "“第5章 自编码器”的训练、验证、测试边界和预处理统计可追溯。",
    },
    {
      label: "目标与梯度",
      detail: "“第5章 自编码器”的前向值、目标、梯度或采样更新经过数值核对。",
    },
    {
      label: "基线与消融",
      detail: "“第5章 自编码器”保留简单基线，只改变一个变量并保存失败样本。",
    },
    {
      label: "复现与历史",
      detail:
        "“第5章 自编码器”归档环境、种子和工件，并分开2016语境与当前迁移。",
    },
  ],
} as const satisfies LearningSystemEvidenceModel;

export function Idl05AutoencodersSignalPathLab() {
  return <LearningSystemEvidenceLab model={model} view="signal-path" />;
}

export function Idl05AutoencodersTrainingTraceLab() {
  return <LearningSystemEvidenceLab model={model} view="training-trace" />;
}

export function Idl05AutoencodersExperimentGateLab() {
  return <LearningSystemEvidenceLab model={model} view="experiment-gate" />;
}
