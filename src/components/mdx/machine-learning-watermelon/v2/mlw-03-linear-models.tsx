"use client";

import { MlEvidenceLab, type MlEvidenceModel } from "./ml-evidence-lab";

const model = {
  unitId: "mlw-03",
  title: "第3章 线性模型",
  question:
    "怎样把线性打分转换成回归或分类决策，并区分阈值、损失与数据分布变化？",
  concepts: [
    "第3章 线性模型",
    "3.1 基本形式",
    "3.2 线性回归",
    "3.3 对数几率回归",
    "3.4 线性判别分析",
    "3.5 多分类学习",
    "3.6 类别不平衡问题",
    "3.7 阅读材料",
  ],
  stages: [
    {
      name: "设计矩阵",
      input: "第3章 线性模型：冻结版本、实体与样本角色，不得越过样本角色",
      operation:
        "只读取任务允许的信息，并持续满足“设计矩阵、尺度、目标编码、损失、正则、阈值和类别代价由训练协议决定”",
      output: "设计矩阵产生可追踪输入状态",
      evidence:
        "可追踪输入状态、索引和版本；出现“在测试标签上移动分类阈值以提高召回率，却仍报告原测试集性能”时拒绝推进",
    },
    {
      name: "目标与损失",
      input: "第3章 线性模型：构造表示、假设或结构，不得越过样本角色",
      operation:
        "保存训练折内统计与选择，并持续满足“设计矩阵、尺度、目标编码、损失、正则、阈值和类别代价由训练协议决定”",
      output: "目标与损失产生候选模型状态",
      evidence:
        "候选模型状态、索引和版本；出现“在测试标签上移动分类阈值以提高召回率，却仍报告原测试集性能”时拒绝推进",
    },
    {
      name: "参数估计",
      input: "第3章 线性模型：执行拟合、搜索或推断，不得越过样本角色",
      operation:
        "记录参数、随机性和收敛，并持续满足“设计矩阵、尺度、目标编码、损失、正则、阈值和类别代价由训练协议决定”",
      output: "参数估计产生可重放运行轨迹",
      evidence:
        "可重放运行轨迹、索引和版本；出现“在测试标签上移动分类阈值以提高召回率，却仍报告原测试集性能”时拒绝推进",
    },
    {
      name: "阈值决策",
      input: "第3章 线性模型：使用验证证据做一次选择，不得越过样本角色",
      operation:
        "保存候选比较与失败理由，并持续满足“设计矩阵、尺度、目标编码、损失、正则、阈值和类别代价由训练协议决定”",
      output: "阈值决策产生锁定模型与阈值",
      evidence:
        "锁定模型与阈值、索引和版本；出现“在测试标签上移动分类阈值以提高召回率，却仍报告原测试集性能”时拒绝推进",
    },
    {
      name: "残差诊断",
      input: "第3章 线性模型：在封存角色上验收并归档，不得越过样本角色",
      operation:
        "输出误差、不确定性和边界，并持续满足“设计矩阵、尺度、目标编码、损失、正则、阈值和类别代价由训练协议决定”",
      output: "残差诊断产生独立复核包",
      evidence:
        "独立复核包、索引和版本；出现“在测试标签上移动分类阈值以提高召回率，却仍报告原测试集性能”时拒绝推进",
    },
  ],
  experiments: [
    {
      name: "冻结基线",
      setup:
        "用密度、含糖率和纹理特征拟合回归与二分类模型，再检查少数类代价。 固定数据版本、实体切分、流水线、预算和随机种子。",
      expected:
        "沿“设计矩阵 → 目标与损失 → 参数估计 → 阈值决策 → 残差诊断”得到可重放结论。",
      boundary:
        "全过程保持“设计矩阵、尺度、目标编码、损失、正则、阈值和类别代价由训练协议决定”。",
    },
    {
      name: "单故障反例",
      setup:
        "用密度、含糖率和纹理特征拟合回归与二分类模型，再检查少数类代价。 其余不变，只注入“在测试标签上移动分类阈值以提高召回率，却仍报告原测试集性能”。",
      expected: "定位第一处信息越界或学习状态偏离，并拒绝下游结论。",
      boundary: "失败运行必须保留；撤销故障后使用同一输入重放。",
    },
  ],
  baselineTrace: [
    "为“第3章 线性模型”冻结任务、数据版本、实体切分、流水线、预算和随机种子",
    "执行设计矩阵、目标与损失，保存输入角色与候选假设",
    "推进参数估计、阈值决策，记录拟合、选择和失败轨迹",
    "在残差诊断交付特征字典、尺度统计、设计矩阵、目标编码、系数、截距、损失曲线、阈值、代价矩阵和残差。",
  ],
  faultTrace: [
    "“第3章 线性模型”复用相同任务、数据版本、实体切分、流水线、预算和种子",
    "仅改变一个条件：在测试标签上移动分类阈值以提高召回率，却仍报告原测试集性能",
    "沿“设计矩阵 → 目标与损失 → 参数估计 → 阈值决策 → 残差诊断”寻找最早的信息越界或状态分叉",
    "撤销故障重放；只有“设计矩阵、尺度、目标编码、损失、正则、阈值和类别代价由训练协议决定”恢复才接受修正",
  ],
  invariant:
    "设计矩阵、尺度、目标编码、损失、正则、阈值和类别代价由训练协议决定",
  fault: "在测试标签上移动分类阈值以提高召回率，却仍报告原测试集性能",
  artifact:
    "特征字典、尺度统计、设计矩阵、目标编码、系数、截距、损失曲线、阈值、代价矩阵和残差。",
  gates: [
    {
      label: "数据角色",
      detail:
        "“第3章 线性模型”的来源、实体、切分、标记可见性和测试封存可追溯。",
    },
    {
      label: "流水线与假设",
      detail:
        "“第3章 线性模型”的表示、预处理、模型、损失、超参数和版本已冻结。",
    },
    {
      label: "选择与评估",
      detail: "“第3章 线性模型”的候选比较、度量、验证决策和一次测试没有越界。",
    },
    {
      label: "复现与边界",
      detail:
        "“第3章 线性模型”归档种子、运行轨迹、错误样本、故障恢复和时间标签。",
    },
  ],
} as const satisfies MlEvidenceModel;

export function Mlw03LinearModelsDataProtocolLab() {
  return <MlEvidenceLab model={model} view="data-protocol" />;
}

export function Mlw03LinearModelsExperimentTraceLab() {
  return <MlEvidenceLab model={model} view="experiment-trace" />;
}

export function Mlw03LinearModelsAcceptanceGateLab() {
  return <MlEvidenceLab model={model} view="acceptance-gate" />;
}
