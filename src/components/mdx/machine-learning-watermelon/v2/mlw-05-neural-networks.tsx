"use client";

import { MlEvidenceLab, type MlEvidenceModel } from "./ml-evidence-lab";

const model = {
  unitId: "mlw-05",
  title: "第5章 神经网络",
  question:
    "怎样沿张量形状、前向值、损失和梯度复核一次训练更新，并识别验证集过拟合？",
  concepts: [
    "第5章 神经网络",
    "5.1 神经元模型",
    "5.2 感知机与多层网络",
    "5.3 误差逆传播算法",
    "5.4 全局最小与局部极小",
    "5.5 其他常见神经网络",
    "5.6 深度学习",
    "5.7 阅读材料",
  ],
  stages: [
    {
      name: "张量与拓扑",
      input: "第5章 神经网络：冻结版本、实体与样本角色，不得越过样本角色",
      operation:
        "只读取任务允许的信息，并持续满足“网络拓扑、初始化、激活、损失、优化器、批次顺序、种子和早停角色固定”",
      output: "张量与拓扑产生可追踪输入状态",
      evidence:
        "可追踪输入状态、索引和版本；出现“每轮根据测试损失早停，再把最低测试损失当最终泛化结果”时拒绝推进",
    },
    {
      name: "前向计算",
      input: "第5章 神经网络：构造表示、假设或结构，不得越过样本角色",
      operation:
        "保存训练折内统计与选择，并持续满足“网络拓扑、初始化、激活、损失、优化器、批次顺序、种子和早停角色固定”",
      output: "前向计算产生候选模型状态",
      evidence:
        "候选模型状态、索引和版本；出现“每轮根据测试损失早停，再把最低测试损失当最终泛化结果”时拒绝推进",
    },
    {
      name: "损失与反传",
      input: "第5章 神经网络：执行拟合、搜索或推断，不得越过样本角色",
      operation:
        "记录参数、随机性和收敛，并持续满足“网络拓扑、初始化、激活、损失、优化器、批次顺序、种子和早停角色固定”",
      output: "损失与反传产生可重放运行轨迹",
      evidence:
        "可重放运行轨迹、索引和版本；出现“每轮根据测试损失早停，再把最低测试损失当最终泛化结果”时拒绝推进",
    },
    {
      name: "参数更新",
      input: "第5章 神经网络：使用验证证据做一次选择，不得越过样本角色",
      operation:
        "保存候选比较与失败理由，并持续满足“网络拓扑、初始化、激活、损失、优化器、批次顺序、种子和早停角色固定”",
      output: "参数更新产生锁定模型与阈值",
      evidence:
        "锁定模型与阈值、索引和版本；出现“每轮根据测试损失早停，再把最低测试损失当最终泛化结果”时拒绝推进",
    },
    {
      name: "验证早停",
      input: "第5章 神经网络：在封存角色上验收并归档，不得越过样本角色",
      operation:
        "输出误差、不确定性和边界，并持续满足“网络拓扑、初始化、激活、损失、优化器、批次顺序、种子和早停角色固定”",
      output: "验证早停产生独立复核包",
      evidence:
        "独立复核包、索引和版本；出现“每轮根据测试损失早停，再把最低测试损失当最终泛化结果”时拒绝推进",
    },
  ],
  experiments: [
    {
      name: "冻结基线",
      setup:
        "训练一个小型多层感知机识别西瓜类别，检查梯度、学习曲线与不同初始化。 固定数据版本、实体切分、流水线、预算和随机种子。",
      expected:
        "沿“张量与拓扑 → 前向计算 → 损失与反传 → 参数更新 → 验证早停”得到可重放结论。",
      boundary:
        "全过程保持“网络拓扑、初始化、激活、损失、优化器、批次顺序、种子和早停角色固定”。",
    },
    {
      name: "单故障反例",
      setup:
        "训练一个小型多层感知机识别西瓜类别，检查梯度、学习曲线与不同初始化。 其余不变，只注入“每轮根据测试损失早停，再把最低测试损失当最终泛化结果”。",
      expected: "定位第一处信息越界或学习状态偏离，并拒绝下游结论。",
      boundary: "失败运行必须保留；撤销故障后使用同一输入重放。",
    },
  ],
  baselineTrace: [
    "为“第5章 神经网络”冻结任务、数据版本、实体切分、流水线、预算和随机种子",
    "执行张量与拓扑、前向计算，保存输入角色与候选假设",
    "推进损失与反传、参数更新，记录拟合、选择和失败轨迹",
    "在验证早停交付数据切分、张量形状、拓扑、初始化、激活、损失、梯度检查、批次、学习率、训练/验证曲线和检查点。",
  ],
  faultTrace: [
    "“第5章 神经网络”复用相同任务、数据版本、实体切分、流水线、预算和种子",
    "仅改变一个条件：每轮根据测试损失早停，再把最低测试损失当最终泛化结果",
    "沿“张量与拓扑 → 前向计算 → 损失与反传 → 参数更新 → 验证早停”寻找最早的信息越界或状态分叉",
    "撤销故障重放；只有“网络拓扑、初始化、激活、损失、优化器、批次顺序、种子和早停角色固定”恢复才接受修正",
  ],
  invariant:
    "网络拓扑、初始化、激活、损失、优化器、批次顺序、种子和早停角色固定",
  fault: "每轮根据测试损失早停，再把最低测试损失当最终泛化结果",
  artifact:
    "数据切分、张量形状、拓扑、初始化、激活、损失、梯度检查、批次、学习率、训练/验证曲线和检查点。",
  gates: [
    {
      label: "数据角色",
      detail:
        "“第5章 神经网络”的来源、实体、切分、标记可见性和测试封存可追溯。",
    },
    {
      label: "流水线与假设",
      detail:
        "“第5章 神经网络”的表示、预处理、模型、损失、超参数和版本已冻结。",
    },
    {
      label: "选择与评估",
      detail: "“第5章 神经网络”的候选比较、度量、验证决策和一次测试没有越界。",
    },
    {
      label: "复现与边界",
      detail:
        "“第5章 神经网络”归档种子、运行轨迹、错误样本、故障恢复和时间标签。",
    },
  ],
} as const satisfies MlEvidenceModel;

export function Mlw05NeuralNetworksDataProtocolLab() {
  return <MlEvidenceLab model={model} view="data-protocol" />;
}

export function Mlw05NeuralNetworksExperimentTraceLab() {
  return <MlEvidenceLab model={model} view="experiment-trace" />;
}

export function Mlw05NeuralNetworksAcceptanceGateLab() {
  return <MlEvidenceLab model={model} view="acceptance-gate" />;
}
