"use client";

import { MlEvidenceLab, type MlEvidenceModel } from "./ml-evidence-lab";

const model = {
  unitId: "mlw-06",
  title: "第6章 支持向量机",
  question:
    "怎样从几何间隔构造约束优化，并证明核矩阵、支持向量和正则选择没有越界？",
  concepts: [
    "第6章 支持向量机",
    "6.1 间隔与支持向量",
    "6.2 对偶问题",
    "6.3 核函数",
    "6.4 软间隔与正则化",
    "6.5 支持向量回归",
    "6.6 核方法",
    "6.7 阅读材料",
  ],
  stages: [
    {
      name: "尺度与样本",
      input: "第6章 支持向量机：冻结版本、实体与样本角色，不得越过样本角色",
      operation:
        "只读取任务允许的信息，并持续满足“特征尺度、核、超参数搜索域、对偶约束、训练索引和模型选择折固定”",
      output: "尺度与样本产生可追踪输入状态",
      evidence:
        "可追踪输入状态、索引和版本；出现“在全量含测试样本的数据上估计尺度与核宽度，导致间隔评估虚高”时拒绝推进",
    },
    {
      name: "间隔原问题",
      input: "第6章 支持向量机：构造表示、假设或结构，不得越过样本角色",
      operation:
        "保存训练折内统计与选择，并持续满足“特征尺度、核、超参数搜索域、对偶约束、训练索引和模型选择折固定”",
      output: "间隔原问题产生候选模型状态",
      evidence:
        "候选模型状态、索引和版本；出现“在全量含测试样本的数据上估计尺度与核宽度，导致间隔评估虚高”时拒绝推进",
    },
    {
      name: "对偶与核",
      input: "第6章 支持向量机：执行拟合、搜索或推断，不得越过样本角色",
      operation:
        "记录参数、随机性和收敛，并持续满足“特征尺度、核、超参数搜索域、对偶约束、训练索引和模型选择折固定”",
      output: "对偶与核产生可重放运行轨迹",
      evidence:
        "可重放运行轨迹、索引和版本；出现“在全量含测试样本的数据上估计尺度与核宽度，导致间隔评估虚高”时拒绝推进",
    },
    {
      name: "软间隔求解",
      input: "第6章 支持向量机：使用验证证据做一次选择，不得越过样本角色",
      operation:
        "保存候选比较与失败理由，并持续满足“特征尺度、核、超参数搜索域、对偶约束、训练索引和模型选择折固定”",
      output: "软间隔求解产生锁定模型与阈值",
      evidence:
        "锁定模型与阈值、索引和版本；出现“在全量含测试样本的数据上估计尺度与核宽度，导致间隔评估虚高”时拒绝推进",
    },
    {
      name: "支持向量复核",
      input: "第6章 支持向量机：在封存角色上验收并归档，不得越过样本角色",
      operation:
        "输出误差、不确定性和边界，并持续满足“特征尺度、核、超参数搜索域、对偶约束、训练索引和模型选择折固定”",
      output: "支持向量复核产生独立复核包",
      evidence:
        "独立复核包、索引和版本；出现“在全量含测试样本的数据上估计尺度与核宽度，导致间隔评估虚高”时拒绝推进",
    },
  ],
  experiments: [
    {
      name: "冻结基线",
      setup:
        "对非线性西瓜边界比较线性核与RBF核，检查支持向量和软间隔违约。 固定数据版本、实体切分、流水线、预算和随机种子。",
      expected:
        "沿“尺度与样本 → 间隔原问题 → 对偶与核 → 软间隔求解 → 支持向量复核”得到可重放结论。",
      boundary:
        "全过程保持“特征尺度、核、超参数搜索域、对偶约束、训练索引和模型选择折固定”。",
    },
    {
      name: "单故障反例",
      setup:
        "对非线性西瓜边界比较线性核与RBF核，检查支持向量和软间隔违约。 其余不变，只注入“在全量含测试样本的数据上估计尺度与核宽度，导致间隔评估虚高”。",
      expected: "定位第一处信息越界或学习状态偏离，并拒绝下游结论。",
      boundary: "失败运行必须保留；撤销故障后使用同一输入重放。",
    },
  ],
  baselineTrace: [
    "为“第6章 支持向量机”冻结任务、数据版本、实体切分、流水线、预算和随机种子",
    "执行尺度与样本、间隔原问题，保存输入角色与候选假设",
    "推进对偶与核、软间隔求解，记录拟合、选择和失败轨迹",
    "在支持向量复核交付缩放统计、核定义、Gram矩阵、C与核宽度搜索、拉格朗日乘子、KKT残差、支持向量和外推样本。",
  ],
  faultTrace: [
    "“第6章 支持向量机”复用相同任务、数据版本、实体切分、流水线、预算和种子",
    "仅改变一个条件：在全量含测试样本的数据上估计尺度与核宽度，导致间隔评估虚高",
    "沿“尺度与样本 → 间隔原问题 → 对偶与核 → 软间隔求解 → 支持向量复核”寻找最早的信息越界或状态分叉",
    "撤销故障重放；只有“特征尺度、核、超参数搜索域、对偶约束、训练索引和模型选择折固定”恢复才接受修正",
  ],
  invariant: "特征尺度、核、超参数搜索域、对偶约束、训练索引和模型选择折固定",
  fault: "在全量含测试样本的数据上估计尺度与核宽度，导致间隔评估虚高",
  artifact:
    "缩放统计、核定义、Gram矩阵、C与核宽度搜索、拉格朗日乘子、KKT残差、支持向量和外推样本。",
  gates: [
    {
      label: "数据角色",
      detail:
        "“第6章 支持向量机”的来源、实体、切分、标记可见性和测试封存可追溯。",
    },
    {
      label: "流水线与假设",
      detail:
        "“第6章 支持向量机”的表示、预处理、模型、损失、超参数和版本已冻结。",
    },
    {
      label: "选择与评估",
      detail:
        "“第6章 支持向量机”的候选比较、度量、验证决策和一次测试没有越界。",
    },
    {
      label: "复现与边界",
      detail:
        "“第6章 支持向量机”归档种子、运行轨迹、错误样本、故障恢复和时间标签。",
    },
  ],
} as const satisfies MlEvidenceModel;

export function Mlw06SupportVectorMachinesDataProtocolLab() {
  return <MlEvidenceLab model={model} view="data-protocol" />;
}

export function Mlw06SupportVectorMachinesExperimentTraceLab() {
  return <MlEvidenceLab model={model} view="experiment-trace" />;
}

export function Mlw06SupportVectorMachinesAcceptanceGateLab() {
  return <MlEvidenceLab model={model} view="acceptance-gate" />;
}
