"use client";

import { MlEvidenceLab, type MlEvidenceModel } from "./ml-evidence-lab";

const model = {
  unitId: "mlw-11",
  title: "第11章 特征选择与稀疏学习",
  question: "怎样在嵌套评估中选择特征，并区分预测稀疏、表示稀疏与测量恢复？",
  concepts: [
    "第11章 特征选择与稀疏学习",
    "11.1 子集搜索与评价",
    "11.2 过滤式选择",
    "11.3 包裹式选择",
    "11.4 嵌入式选择与L1正则化",
    "11.5 稀疏表示与字典学习",
    "11.6 压缩感知",
    "11.7 阅读材料",
  ],
  stages: [
    {
      name: "候选特征",
      input:
        "第11章 特征选择与稀疏学习：冻结版本、实体与样本角色，不得越过样本角色",
      operation:
        "只读取任务允许的信息，并持续满足“特征候选、搜索预算、选择评分、内外层切分、正则和稀疏阈值固定”",
      output: "候选特征产生可追踪输入状态",
      evidence:
        "可追踪输入状态、索引和版本；出现“先用全量标签筛特征再交叉验证，导致每一折的验证标签已经进入表示”时拒绝推进",
    },
    {
      name: "内层选择",
      input:
        "第11章 特征选择与稀疏学习：构造表示、假设或结构，不得越过样本角色",
      operation:
        "保存训练折内统计与选择，并持续满足“特征候选、搜索预算、选择评分、内外层切分、正则和稀疏阈值固定”",
      output: "内层选择产生候选模型状态",
      evidence:
        "候选模型状态、索引和版本；出现“先用全量标签筛特征再交叉验证，导致每一折的验证标签已经进入表示”时拒绝推进",
    },
    {
      name: "稀疏拟合",
      input:
        "第11章 特征选择与稀疏学习：执行拟合、搜索或推断，不得越过样本角色",
      operation:
        "记录参数、随机性和收敛，并持续满足“特征候选、搜索预算、选择评分、内外层切分、正则和稀疏阈值固定”",
      output: "稀疏拟合产生可重放运行轨迹",
      evidence:
        "可重放运行轨迹、索引和版本；出现“先用全量标签筛特征再交叉验证，导致每一折的验证标签已经进入表示”时拒绝推进",
    },
    {
      name: "外层评估",
      input:
        "第11章 特征选择与稀疏学习：使用验证证据做一次选择，不得越过样本角色",
      operation:
        "保存候选比较与失败理由，并持续满足“特征候选、搜索预算、选择评分、内外层切分、正则和稀疏阈值固定”",
      output: "外层评估产生锁定模型与阈值",
      evidence:
        "锁定模型与阈值、索引和版本；出现“先用全量标签筛特征再交叉验证，导致每一折的验证标签已经进入表示”时拒绝推进",
    },
    {
      name: "稳定性解释",
      input:
        "第11章 特征选择与稀疏学习：在封存角色上验收并归档，不得越过样本角色",
      operation:
        "输出误差、不确定性和边界，并持续满足“特征候选、搜索预算、选择评分、内外层切分、正则和稀疏阈值固定”",
      output: "稳定性解释产生独立复核包",
      evidence:
        "独立复核包、索引和版本；出现“先用全量标签筛特征再交叉验证，导致每一折的验证标签已经进入表示”时拒绝推进",
    },
  ],
  experiments: [
    {
      name: "冻结基线",
      setup:
        "比较过滤、包裹与L1嵌入式选择，再用稀疏字典重构西瓜属性。 固定数据版本、实体切分、流水线、预算和随机种子。",
      expected:
        "沿“候选特征 → 内层选择 → 稀疏拟合 → 外层评估 → 稳定性解释”得到可重放结论。",
      boundary:
        "全过程保持“特征候选、搜索预算、选择评分、内外层切分、正则和稀疏阈值固定”。",
    },
    {
      name: "单故障反例",
      setup:
        "比较过滤、包裹与L1嵌入式选择，再用稀疏字典重构西瓜属性。 其余不变，只注入“先用全量标签筛特征再交叉验证，导致每一折的验证标签已经进入表示”。",
      expected: "定位第一处信息越界或学习状态偏离，并拒绝下游结论。",
      boundary: "失败运行必须保留；撤销故障后使用同一输入重放。",
    },
  ],
  baselineTrace: [
    "为“第11章 特征选择与稀疏学习”冻结任务、数据版本、实体切分、流水线、预算和随机种子",
    "执行候选特征、内层选择，保存输入角色与候选假设",
    "推进稀疏拟合、外层评估，记录拟合、选择和失败轨迹",
    "在稳定性解释交付特征清单、过滤统计、搜索路径、内外折索引、正则路径、非零支持集、字典与编码、选择稳定性和外层性能。",
  ],
  faultTrace: [
    "“第11章 特征选择与稀疏学习”复用相同任务、数据版本、实体切分、流水线、预算和种子",
    "仅改变一个条件：先用全量标签筛特征再交叉验证，导致每一折的验证标签已经进入表示",
    "沿“候选特征 → 内层选择 → 稀疏拟合 → 外层评估 → 稳定性解释”寻找最早的信息越界或状态分叉",
    "撤销故障重放；只有“特征候选、搜索预算、选择评分、内外层切分、正则和稀疏阈值固定”恢复才接受修正",
  ],
  invariant: "特征候选、搜索预算、选择评分、内外层切分、正则和稀疏阈值固定",
  fault: "先用全量标签筛特征再交叉验证，导致每一折的验证标签已经进入表示",
  artifact:
    "特征清单、过滤统计、搜索路径、内外折索引、正则路径、非零支持集、字典与编码、选择稳定性和外层性能。",
  gates: [
    {
      label: "数据角色",
      detail:
        "“第11章 特征选择与稀疏学习”的来源、实体、切分、标记可见性和测试封存可追溯。",
    },
    {
      label: "流水线与假设",
      detail:
        "“第11章 特征选择与稀疏学习”的表示、预处理、模型、损失、超参数和版本已冻结。",
    },
    {
      label: "选择与评估",
      detail:
        "“第11章 特征选择与稀疏学习”的候选比较、度量、验证决策和一次测试没有越界。",
    },
    {
      label: "复现与边界",
      detail:
        "“第11章 特征选择与稀疏学习”归档种子、运行轨迹、错误样本、故障恢复和时间标签。",
    },
  ],
} as const satisfies MlEvidenceModel;

export function Mlw11FeatureSelectionSparseLearningDataProtocolLab() {
  return <MlEvidenceLab model={model} view="data-protocol" />;
}

export function Mlw11FeatureSelectionSparseLearningExperimentTraceLab() {
  return <MlEvidenceLab model={model} view="experiment-trace" />;
}

export function Mlw11FeatureSelectionSparseLearningAcceptanceGateLab() {
  return <MlEvidenceLab model={model} view="acceptance-gate" />;
}
