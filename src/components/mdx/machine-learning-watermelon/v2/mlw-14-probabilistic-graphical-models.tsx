"use client";

import { MlEvidenceLab, type MlEvidenceModel } from "./ml-evidence-lab";

const model = {
  unitId: "mlw-14",
  title: "第14章 概率图模型",
  question:
    "怎样用图结构表达条件独立，分别验证参数学习、精确或近似推断和预测？",
  concepts: [
    "第14章 概率图模型",
    "14.1 隐马尔可夫模型",
    "14.2 马尔可夫随机场",
    "14.3 条件随机场",
    "14.4 学习与推断",
    "14.5 近似推断",
    "14.6 话题模型",
    "14.7 阅读材料",
  ],
  stages: [
    {
      name: "变量与图",
      input: "第14章 概率图模型：冻结版本、实体与样本角色，不得越过样本角色",
      operation:
        "只读取任务允许的信息，并持续满足“变量域、图结构、因子、参数、观测/隐变量角色、推断算法和收敛诊断固定”",
      output: "变量与图产生可追踪输入状态",
      evidence:
        "可追踪输入状态、索引和版本；出现“近似推断未收敛仍输出概率，并把算法误差误认为模型不确定性”时拒绝推进",
    },
    {
      name: "因子与参数",
      input: "第14章 概率图模型：构造表示、假设或结构，不得越过样本角色",
      operation:
        "保存训练折内统计与选择，并持续满足“变量域、图结构、因子、参数、观测/隐变量角色、推断算法和收敛诊断固定”",
      output: "因子与参数产生候选模型状态",
      evidence:
        "候选模型状态、索引和版本；出现“近似推断未收敛仍输出概率，并把算法误差误认为模型不确定性”时拒绝推进",
    },
    {
      name: "学习过程",
      input: "第14章 概率图模型：执行拟合、搜索或推断，不得越过样本角色",
      operation:
        "记录参数、随机性和收敛，并持续满足“变量域、图结构、因子、参数、观测/隐变量角色、推断算法和收敛诊断固定”",
      output: "学习过程产生可重放运行轨迹",
      evidence:
        "可重放运行轨迹、索引和版本；出现“近似推断未收敛仍输出概率，并把算法误差误认为模型不确定性”时拒绝推进",
    },
    {
      name: "推断过程",
      input: "第14章 概率图模型：使用验证证据做一次选择，不得越过样本角色",
      operation:
        "保存候选比较与失败理由，并持续满足“变量域、图结构、因子、参数、观测/隐变量角色、推断算法和收敛诊断固定”",
      output: "推断过程产生锁定模型与阈值",
      evidence:
        "锁定模型与阈值、索引和版本；出现“近似推断未收敛仍输出概率，并把算法误差误认为模型不确定性”时拒绝推进",
    },
    {
      name: "诊断与预测",
      input: "第14章 概率图模型：在封存角色上验收并归档，不得越过样本角色",
      operation:
        "输出误差、不确定性和边界，并持续满足“变量域、图结构、因子、参数、观测/隐变量角色、推断算法和收敛诊断固定”",
      output: "诊断与预测产生独立复核包",
      evidence:
        "独立复核包、索引和版本；出现“近似推断未收敛仍输出概率，并把算法误差误认为模型不确定性”时拒绝推进",
    },
  ],
  experiments: [
    {
      name: "冻结基线",
      setup:
        "构建西瓜生长状态的HMM与属性MRF，对比消息传递和采样近似。 固定数据版本、实体切分、流水线、预算和随机种子。",
      expected:
        "沿“变量与图 → 因子与参数 → 学习过程 → 推断过程 → 诊断与预测”得到可重放结论。",
      boundary:
        "全过程保持“变量域、图结构、因子、参数、观测/隐变量角色、推断算法和收敛诊断固定”。",
    },
    {
      name: "单故障反例",
      setup:
        "构建西瓜生长状态的HMM与属性MRF，对比消息传递和采样近似。 其余不变，只注入“近似推断未收敛仍输出概率，并把算法误差误认为模型不确定性”。",
      expected: "定位第一处信息越界或学习状态偏离，并拒绝下游结论。",
      boundary: "失败运行必须保留；撤销故障后使用同一输入重放。",
    },
  ],
  baselineTrace: [
    "为“第14章 概率图模型”冻结任务、数据版本、实体切分、流水线、预算和随机种子",
    "执行变量与图、因子与参数，保存输入角色与候选假设",
    "推进学习过程、推断过程，记录拟合、选择和失败轨迹",
    "在诊断与预测交付变量表、图、因子、归一化检查、参数版本、观测证据、消息/样本轨迹、收敛诊断、边际概率和预测检验。",
  ],
  faultTrace: [
    "“第14章 概率图模型”复用相同任务、数据版本、实体切分、流水线、预算和种子",
    "仅改变一个条件：近似推断未收敛仍输出概率，并把算法误差误认为模型不确定性",
    "沿“变量与图 → 因子与参数 → 学习过程 → 推断过程 → 诊断与预测”寻找最早的信息越界或状态分叉",
    "撤销故障重放；只有“变量域、图结构、因子、参数、观测/隐变量角色、推断算法和收敛诊断固定”恢复才接受修正",
  ],
  invariant:
    "变量域、图结构、因子、参数、观测/隐变量角色、推断算法和收敛诊断固定",
  fault: "近似推断未收敛仍输出概率，并把算法误差误认为模型不确定性",
  artifact:
    "变量表、图、因子、归一化检查、参数版本、观测证据、消息/样本轨迹、收敛诊断、边际概率和预测检验。",
  gates: [
    {
      label: "数据角色",
      detail:
        "“第14章 概率图模型”的来源、实体、切分、标记可见性和测试封存可追溯。",
    },
    {
      label: "流水线与假设",
      detail:
        "“第14章 概率图模型”的表示、预处理、模型、损失、超参数和版本已冻结。",
    },
    {
      label: "选择与评估",
      detail:
        "“第14章 概率图模型”的候选比较、度量、验证决策和一次测试没有越界。",
    },
    {
      label: "复现与边界",
      detail:
        "“第14章 概率图模型”归档种子、运行轨迹、错误样本、故障恢复和时间标签。",
    },
  ],
} as const satisfies MlEvidenceModel;

export function Mlw14ProbabilisticGraphicalModelsDataProtocolLab() {
  return <MlEvidenceLab model={model} view="data-protocol" />;
}

export function Mlw14ProbabilisticGraphicalModelsExperimentTraceLab() {
  return <MlEvidenceLab model={model} view="experiment-trace" />;
}

export function Mlw14ProbabilisticGraphicalModelsAcceptanceGateLab() {
  return <MlEvidenceLab model={model} view="acceptance-gate" />;
}
