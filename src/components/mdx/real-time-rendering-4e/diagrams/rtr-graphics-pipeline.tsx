import type { ReactNode } from "react";
function Frame({caption,children}:{caption:string;children:ReactNode}){return <figure className="mdx-figure not-prose mx-auto my-6"><div className="overflow-hidden rounded-card border border-border bg-elevated p-4 sm:p-5">{children}</div><figcaption className="mt-2 text-center text-sm text-secondary">{caption}</figcaption></figure>}
export function RtrGraphicsPipelineDiagram(){const stages=[["Application","scene · visibility · commands"],["Geometry","vertex/mesh · primitive · clip"],["Raster","coverage · interpolation"],["Pixel/Output","shade · depth · blend"],["Present","post · UI · compositor · display"]];return <Frame caption="完整帧从应用到显示；阶段之间通过命令、资源和同步连接。"><div role="img" aria-label="实时渲染完整帧管线" className="grid gap-2 md:grid-cols-5">{stages.map(([a,b],i)=><div key={a} className="grid min-h-24 content-center rounded-control border border-border bg-bg/45 p-3 text-center"><span className="text-xs font-bold text-accent">0{i+1}</span><strong className="mt-1 text-xs text-primary">{a}</strong><span className="mt-2 text-xs text-secondary">{b}</span></div>)}</div></Frame>}
export function RtrGpuExecutionDiagram(){const rows=[["Warp / wave","lanes share issue","divergence / masking"],["Scheduler","many resident groups","latency hiding"],["Registers","per lane state","occupancy / spill"],["Shared/cache","local reuse","bank/cache behavior"],["Global memory","large storage","coalescing / bandwidth"]];return <Frame caption="GPU 性能来自执行组和存储层次的协作，不是“一个线程一个核心”。"><div role="img" aria-label="GPU 执行与存储层次" className="overflow-hidden rounded-control border border-border text-xs"><div className="grid grid-cols-[1fr_1.4fr_1.6fr] gap-px bg-border">{['Layer','Role','Boundary'].map(x=><strong key={x} className="bg-bg p-3 text-primary">{x}</strong>)}{rows.flatMap(r=>r.map((x,i)=><span key={`${r[0]}-${x}`} className={i===0?"bg-accent/10 p-3 font-semibold text-accent":"bg-elevated p-3 text-secondary"}>{x}</span>))}</div></div></Frame>}
export function RtrFrameBudgetDiagram(){const rows=[["CPU","update · visibility · record","submission / sync"],["GPU","passes · shader · fixed units","compute / bandwidth"],["Present","queue · compositor · scanout","latency / refresh"],["Serialized","readback · waits · dependencies","adds to critical path"]];return <Frame caption="重叠阶段取最大值，强制串行部分进入关键路径；高分位比平均值更能暴露 hitch。"><div role="img" aria-label="CPU GPU 呈现帧预算" className="grid gap-3 md:grid-cols-4">{rows.map(([a,b,c])=><div key={a} className="rounded-control border border-border bg-bg/45 p-4"><strong className="text-sm text-primary">{a}</strong><p className="mb-0 mt-2 text-xs leading-5 text-secondary">{b}</p><span className="mt-3 block text-xs font-semibold text-accent">{c}</span></div>)}</div></Frame>}
