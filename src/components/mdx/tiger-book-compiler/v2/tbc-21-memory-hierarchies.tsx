"use client";

import {
  CompilerEvidenceLab,
  type CompilerEvidenceModel,
} from "@/components/mdx/dragon-book-compilers/v2/compiler-evidence-lab";

const model = {
  unitId: "tbc-unit-21",
  title: "第21章 存储层次",
  question: "访问模式怎样映射缓存块、复用距离和内存带宽？",
  concepts: [
    "第21章 存储层次",
    "21.1 cache的组织结构",
    "21.2 cache块对齐",
    "21.3 预取",
    "21.4 循环交换",
    "21.5 分块",
    "21.6 垃圾收集和存储层次",
  ],
  invariant:
    "第21章 存储层次的C模块接口、数据结构、状态变换、输出语义与测试始终一致",
  fault: "分块尺寸只为单一缓存调优，并在真实层次产生冲突抖动",
  artifact: "地址轨迹、缓存映射、缺失分类、带宽与分块消融",
  stages: [
    {
      name: "第21章 存储层次 · C模块与输入",
      input: "在Tiger C实现轨道中复现“第21章 存储层次”的输入、状态、变换与验证",
      operation:
        "冻结缓存层次、数据布局与局部性所需的C接口、源程序、规则、数据结构和版本",
      output: "第21章 存储层次的模块合同、输入快照与所有权表",
      check: "第21章 存储层次的类型、所有权、源位置、名字和接口布局没有错位",
    },
    {
      name: "第21章 存储层次 · 算法与状态",
      input: "第21章 存储层次的冻结模块和输入",
      operation:
        "执行把缓存组织、对齐、预取、循环交换、分块和GC连接到存储层次的最小算法并保存每一步状态",
      output: "第21章 存储层次的参考轨迹、故障轨迹与首个状态分岔",
      check: "第21章 存储层次每一步可由同一C接口、输入、规则和执行顺序复算",
    },
    {
      name: "第21章 存储层次 · 输出与整合",
      input: "第21章 存储层次的中间状态、输出与下游模块合同",
      operation: "比较变换前后AST/IR/汇编/运行时状态和跨模块传递",
      output: "第21章 存储层次的前后差、接口谱系与恢复路径",
      check: "第21章 存储层次没有把单模块通过或单一样例正确当作端到端正确性",
    },
    {
      name: "第21章 存储层次 · 独立验证",
      input: "第21章 存储层次的冻结候选与黄金测试、差分执行或不变量检查",
      operation: "重放预测、单故障、恢复和不适用边界",
      output: "第21章 存储层次的接受、回退或拒绝理由",
      check:
        "第21章 存储层次满足“第21章 存储层次的C模块接口、数据结构、状态变换、输出语义与测试始终一致”",
    },
  ],
  cases: [
    {
      name: "第21章 存储层次 · 基线",
      setup:
        "固定在Tiger C实现轨道中复现“第21章 存储层次”的输入、状态、变换与验证的C接口、输入、版本、预算和顺序",
      prediction:
        "第21章 存储层次的参考轨迹应持续满足“第21章 存储层次的C模块接口、数据结构、状态变换、输出语义与测试始终一致”",
      boundary: "第21章 存储层次只回答本页正式坐标和C实现轨道内的问题",
    },
    {
      name: "第21章 存储层次 · 单故障",
      setup:
        "保持其余条件不变，只注入“分块尺寸只为单一缓存调优，并在真实层次产生冲突抖动”",
      prediction:
        "第21章 存储层次应出现可定位的首个状态分岔，而不是只在末端输出异常",
      boundary:
        "第21章 存储层次的故障结论不能外推到未运行的语言特性、目标机或程序",
    },
    {
      name: "第21章 存储层次 · 恢复",
      setup:
        "撤销故障并从同一快照重放在Tiger C实现轨道中复现“第21章 存储层次”的输入、状态、变换与验证",
      prediction: "第21章 存储层次的状态、整合测试和交付证据应恢复基线",
      boundary: "第21章 存储层次若不能复现恢复结果，就不能把异常归因给单一故障",
    },
  ],
  referenceTrace: [
    "第21章 存储层次参考步骤1：冻结缓存层次、数据布局与局部性所需的C接口、源程序、规则、数据结构和版本；保存第21章 存储层次的模块合同、输入快照与所有权表。",
    "第21章 存储层次参考步骤2：执行把缓存组织、对齐、预取、循环交换、分块和GC连接到存储层次的最小算法并保存每一步状态；保存第21章 存储层次的参考轨迹、故障轨迹与首个状态分岔。",
    "第21章 存储层次参考步骤3：比较变换前后AST/IR/汇编/运行时状态和跨模块传递；保存第21章 存储层次的前后差、接口谱系与恢复路径。",
    "第21章 存储层次参考步骤4：重放预测、单故障、恢复和不适用边界；保存第21章 存储层次的接受、回退或拒绝理由。",
  ],
  faultTrace: [
    "第21章 存储层次故障步骤1：保持在Tiger C实现轨道中复现“第21章 存储层次”的输入、状态、变换与验证不变，检查第21章 存储层次的模块合同、输入快照与所有权表如何受单一故障传播。",
    "第21章 存储层次故障步骤2：只注入“分块尺寸只为单一缓存调优，并在真实层次产生冲突抖动”，记录首个偏离“第21章 存储层次每一步可由同一C接口、输入、规则和执行顺序复算”的状态。",
    "第21章 存储层次故障步骤3：保持第21章 存储层次的中间状态、输出与下游模块合同不变，检查第21章 存储层次的前后差、接口谱系与恢复路径如何受单一故障传播。",
    "第21章 存储层次故障步骤4：保持第21章 存储层次的冻结候选与黄金测试、差分执行或不变量检查不变，检查第21章 存储层次的接受、回退或拒绝理由如何受单一故障传播。",
  ],
  gates: [
    {
      label: "原版轨道与译本边界",
      detail:
        "第21章 存储层次区分作者C/Java/ML三条轨道、Cambridge C版、中文修订版与本站C轨道独立重写。",
    },
    {
      label: "C接口、状态与所有权",
      detail:
        "第21章 存储层次的头文件、实现、数据结构、内存所有权、源位置和中间状态可复算。",
    },
    {
      label: "变换、整合与恢复",
      detail:
        "第21章 存储层次的规则、状态、跨模块接口、前后表示、诊断和恢复路径已归档。",
    },
    {
      label: "端到端语义与边界",
      detail:
        "第21章 存储层次以黄金测试或差分执行复核“第21章 存储层次的C模块接口、数据结构、状态变换、输出语义与测试始终一致”，并报告“分块尺寸只为单一缓存调优，并在真实层次产生冲突抖动”的恢复结果。",
    },
  ],
} as const satisfies CompilerEvidenceModel;

export function Tbc21MemoryHierarchiesPipelineContractLab() {
  return <CompilerEvidenceLab model={model} view="pipeline-contract" />;
}

export function Tbc21MemoryHierarchiesStateTraceLab() {
  return <CompilerEvidenceLab model={model} view="state-trace" />;
}

export function Tbc21MemoryHierarchiesVerificationGateLab() {
  return <CompilerEvidenceLab model={model} view="verification-gate" />;
}
