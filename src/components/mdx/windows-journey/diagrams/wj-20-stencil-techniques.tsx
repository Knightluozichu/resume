import {
  WindowsJourneyMigrationLab,
  WindowsJourneyPipelineLab,
  WindowsJourneyRecoveryLab,
  type WindowsJourneyCausalModel,
  type WindowsJourneyCoverageNode,
} from "./official-windows-journey-book-lab";

const title = "第20章 虚实结合的光影——模板技术";
const focus = "用模板比较、通过/失败操作和绘制遍次构造像素级遮罩";
const stages = [
  "清除模板",
  "写入遮罩",
  "执行比较",
  "应用操作",
  "受限绘制"
];
const nodes = [
  {
    "label": "第20章 虚实结合的光影——模板技术",
    "mechanism": "围绕用模板比较、通过/失败操作和绘制遍次构造像素级遮罩，对 第20章 虚实结合的光影——模板技术，必须给出可复算中间量，先在纸面预测空间或像素结果，再用模板值、引用值、掩码、比较结果、每遍颜色写入查找首个数值分叉。",
    "probe": "记录模板值、引用值、掩码、比较结果、每遍颜色写入"
  },
  {
    "label": "20.1 对模板技术中概念的理解",
    "mechanism": "围绕用模板比较、通过/失败操作和绘制遍次构造像素级遮罩，对 20.1 对模板技术中概念的理解，必须给出可复算中间量，先在纸面预测空间或像素结果，再用模板值、引用值、掩码、比较结果、每遍颜色写入查找首个数值分叉。",
    "probe": "记录模板值、引用值、掩码、比较结果、每遍颜色写入"
  },
  {
    "label": "20.1.1 模板缓存",
    "mechanism": "围绕用模板比较、通过/失败操作和绘制遍次构造像素级遮罩，对 20.1.1 模板缓存，必须给出可复算中间量，先在纸面预测空间或像素结果，再用模板值、引用值、掩码、比较结果、每遍颜色写入查找首个数值分叉。",
    "probe": "记录模板值、引用值、掩码、比较结果、每遍颜色写入"
  },
  {
    "label": "20.1.2 模板测试",
    "mechanism": "围绕用模板比较、通过/失败操作和绘制遍次构造像素级遮罩，对 20.1.2 模板测试，必须给出可复算中间量，先在纸面预测空间或像素结果，再用模板值、引用值、掩码、比较结果、每遍颜色写入查找首个数值分叉。",
    "probe": "记录模板值、引用值、掩码、比较结果、每遍颜色写入"
  },
  {
    "label": "20.2 模板测试精细讲解",
    "mechanism": "围绕用模板比较、通过/失败操作和绘制遍次构造像素级遮罩，对 20.2 模板测试精细讲解，必须给出可复算中间量，先在纸面预测空间或像素结果，再用模板值、引用值、掩码、比较结果、每遍颜色写入查找首个数值分叉。",
    "probe": "记录模板值、引用值、掩码、比较结果、每遍颜色写入"
  },
  {
    "label": "20.2.1 创建模板缓冲区",
    "mechanism": "围绕用模板比较、通过/失败操作和绘制遍次构造像素级遮罩，对 20.2.1 创建模板缓冲区，必须给出可复算中间量，先在纸面预测空间或像素结果，再用模板值、引用值、掩码、比较结果、每遍颜色写入查找首个数值分叉。",
    "probe": "记录模板值、引用值、掩码、比较结果、每遍颜色写入"
  },
  {
    "label": "20.2.2 清除模板缓冲区",
    "mechanism": "围绕用模板比较、通过/失败操作和绘制遍次构造像素级遮罩，对 20.2.2 清除模板缓冲区，必须给出可复算中间量，先在纸面预测空间或像素结果，再用模板值、引用值、掩码、比较结果、每遍颜色写入查找首个数值分叉。",
    "probe": "记录模板值、引用值、掩码、比较结果、每遍颜色写入"
  },
  {
    "label": "20.2.3 模板测试相关参数介绍",
    "mechanism": "围绕用模板比较、通过/失败操作和绘制遍次构造像素级遮罩，对 20.2.3 模板测试相关参数介绍，必须给出可复算中间量，先在纸面预测空间或像素结果，再用模板值、引用值、掩码、比较结果、每遍颜色写入查找首个数值分叉。",
    "probe": "记录模板值、引用值、掩码、比较结果、每遍颜色写入"
  },
  {
    "label": "20.2.4 对模板测试的一些理解",
    "mechanism": "围绕用模板比较、通过/失败操作和绘制遍次构造像素级遮罩，对 20.2.4 对模板测试的一些理解，必须给出可复算中间量，先在纸面预测空间或像素结果，再用模板值、引用值、掩码、比较结果、每遍颜色写入查找首个数值分叉。",
    "probe": "记录模板值、引用值、掩码、比较结果、每遍颜色写入"
  },
  {
    "label": "20.3 镜面特效的实现",
    "mechanism": "围绕用模板比较、通过/失败操作和绘制遍次构造像素级遮罩，20.3 镜面特效的实现 限定本章的一个知识坐标；独立解释围绕“第一遍按条件写模板，后续遍用引用值和比较函数限制颜色写入”展开，并以“每个像素的模板旧值、比较、操作和新值均能逐遍追踪”结束。",
    "probe": "记录模板值、引用值、掩码、比较结果、每遍颜色写入"
  },
  {
    "label": "20.4 通过实例程序讲解",
    "mechanism": "围绕用模板比较、通过/失败操作和绘制遍次构造像素级遮罩，对 20.4 通过实例程序讲解，把前述机制装入一个可运行场景：固定构建、资源和输入，仅改变一个条件，并保留模板值、引用值、掩码、比较结果、每遍颜色写入。",
    "probe": "记录模板值、引用值、掩码、比较结果、每遍颜色写入"
  },
  {
    "label": "20.5 示例程序D3Ddemo15",
    "mechanism": "围绕用模板比较、通过/失败操作和绘制遍次构造像素级遮罩，对 20.5 示例程序D3Ddemo15，把前述机制装入一个可运行场景：固定构建、资源和输入，仅改变一个条件，并保留模板值、引用值、掩码、比较结果、每遍颜色写入。",
    "probe": "记录模板值、引用值、掩码、比较结果、每遍颜色写入"
  },
  {
    "label": "20.6 章节小憩",
    "mechanism": "围绕用模板比较、通过/失败操作和绘制遍次构造像素级遮罩，对 20.6 章节小憩，收尾不是装饰，而是要求用模板值、引用值、掩码、比较结果、每遍颜色写入复盘“每个像素的模板旧值、比较、操作和新值均能逐遍追踪”是否在正常和失败路径同时成立。",
    "probe": "记录模板值、引用值、掩码、比较结果、每遍颜色写入"
  }
] satisfies WindowsJourneyCoverageNode[];
const model = {
  "historicalLabel": "D3D9 模板状态",
  "modernLabel": "现代模板附件",
  "unit": "错误像素",
  "historicalBase": 21,
  "historicalSlope": 6.5,
  "modernBase": 10,
  "modernSlope": 1.7,
  "faultPenalty": 28,
  "invariant": "每个像素的模板旧值、比较、操作和新值均能逐遍追踪",
  "fault": "未清模板或写掩码/读掩码混淆导致遮罩跨帧残留",
  "evidence": "模板值、引用值、掩码、比较结果、每遍颜色写入"
} satisfies WindowsJourneyCausalModel;
const props = { title, focus, stages, nodes, model };

export function Wj20StencilTechniquesMapLab() {
  return <WindowsJourneyPipelineLab {...props} />;
}

export function Wj20StencilTechniquesExperimentLab() {
  return <WindowsJourneyMigrationLab {...props} />;
}

export function Wj20StencilTechniquesEvidenceLab() {
  return <WindowsJourneyRecoveryLab {...props} />;
}
