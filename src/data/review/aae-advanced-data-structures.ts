import type { ReviewQuestion } from "./types";

/** 高级数据结构复习题 */
export const aaeAdvancedDataStructuresQuestions: ReviewQuestion[] = [
  {
    id: "aae-advanced-data-structures-1",
    chapter: "aae-advanced-data-structures",
    level: 1,
    question:
      "跳表（Skip List）的核心思想是什么？它的查找、插入、删除的时间复杂度各是多少？",
    answer:
      "跳表的核心思想是用「多级索引」给有序链表加速，以概率替代平衡操作，实现与平衡树相当的性能但实现更简单。\n\n结构：\n- 底层（Level 0）是完整的有序链表，包含所有节点。\n- 上层是下层的「索引」——从下层节点中按概率（通常 p = 1/2）随机选取，用跨度更大的指针连接。\n- 顶层节点最少，底层最全。整体像金字塔。\n\n查找过程：\n从最高层的头节点开始，向右走直到下一个节点大于目标，然后下降一层继续。重复直到到达底层。类似于在地铁线路图上先坐快车（高层）、再换慢车（底层）逼近目的地。\n\n复杂度：\n- 查找：O(log n) 期望。每层期望前进约 2 步（几何分布），共 O(log n) 层。\n- 插入：O(log n) 期望。先查找到插入位置，然后以概率 p 决定是否提升到上层，直到不提升为止。\n- 删除：O(log n) 期望。查找到目标节点后，在所有包含该节点的层上删除。\n\n空间复杂度：O(n) 期望（每个节点平均出现在 1/(1-p) 层，p=1/2 时平均 2 层）。\n\n与平衡树的对比：跳表用随机性保证平衡（概率上平衡），不需要旋转操作；实现更简单，适合并发场景（局部锁即可）。Redis 的有序集合底层就用跳表。",
    tags: ["跳表", "多级索引", "概率平衡", "复杂度"],
  },
  {
    id: "aae-advanced-data-structures-2",
    chapter: "aae-advanced-data-structures",
    level: 2,
    question:
      "B 树和 B+ 树为什么适合磁盘/数据库存储？它们与二叉搜索树（如红黑树）在相同复杂度 O(log n) 下为什么性能差异巨大？",
    answer:
      "核心原因：B 树/B+ 树是「I/O 感知」的数据结构，针对磁盘的块读取特性优化，而二叉搜索树是「RAM 模型」下的结构，忽略了访存层次。\n\n磁盘访问特性：\n- 磁盘以「页/块」（通常 4KB~16KB）为单位读取，读取 1 字节和读取 1 页的时间几乎相同（约 0.1ms 寻道 + 传输）。\n- 随机 I/O 远贵于顺序 I/O（HDD 上差距可达 100 倍）。\n- 内存访问约 100ns，磁盘访问约 100μs~10ms，差距 1000~100000 倍。\n\nB 树/B+ 树的设计：\n- 每个节点的大小匹配一页磁盘（如 16KB），一次 I/O 读入一个节点。\n- 节点内包含大量关键字（如 16KB 页可存约 1000 个 key），分支因子很大。\n- 树高极低：100 万 key 的 B+ 树只需 2~3 层，即 2~3 次磁盘 I/O 即可定位。\n- B+ 树的非叶子节点只存关键字（不存数据），进一步增大分支因子、降低树高；数据全在叶子节点，且叶子节点用链表相连，利于范围查询。\n\n红黑树的问题：\n- 每个节点只存 1 个 key、2 个子节点，分支因子 = 2。\n- 100 万 key 的红黑树高约 20 层，最坏需要 20 次磁盘 I/O。\n- 每个 I/O 只读 1 个节点（几十字节），浪费了磁盘页的传输能力。\n\n复杂度分析（I/O 模型）：\n- B+ 树：O(log_B n) 次 I/O，B 为分支因子（页面大小/关键字大小）。n = 10 亿、B = 100 时只需 4~5 次 I/O。\n- 红黑树：O(log_2 n) 次 I/O = 30 次 I/O。两者大O同为 O(log n)，但底数不同，I/O 次数差 5~7 倍。\n\n一句话：B+ 树把「树高」从 O(log_2 n) 降到 O(log_B n)，让磁盘 I/O 次数最小化。这是用「增大节点」换「降低树高」的经典空间-时间权衡——节点大占内存但减少 I/O 次数，而 I/O 比内存访问贵千倍，所以值得。",
    tags: ["B树", "B+树", "磁盘存储", "I/O模型", "红黑树对比"],
  },
  {
    id: "aae-advanced-data-structures-3",
    chapter: "aae-advanced-data-structures",
    level: 3,
    question:
      "布隆过滤器（Bloom Filter）的误判率如何计算？如果要在 1 亿元素集合上实现 1% 误判率，需要多少内存和多少个哈希函数？",
    answer:
      "布隆过滤器误判率推导与参数计算：\n\n参数定义：\n- n：元素个数\n- m：位数组大小（bit）\n- k：哈希函数个数\n- p：误判率（假阳性率）\n\n误判率公式推导：\n\n1. 插入 1 个元素后，某一位被某个哈希函数置 1 的概率 = 1/m，不被置 1 的概率 = 1 − 1/m。\n2. 经过 k 个哈希函数后，某一位仍为 0 的概率 = (1 − 1/m)^k。\n3. 插入 n 个元素后，某一位仍为 0 的概率 = (1 − 1/m)^(kn) ≈ e^(−kn/m)（当 m 大时近似）。\n4. 某一位为 1 的概率 = 1 − e^(−kn/m)。\n5. 误判 = 一个不在集合中的元素，其 k 个哈希位置全为 1，概率：\n   p = (1 − e^(−kn/m))^k\n\n最优参数：\n- 给定 n 和目标误判率 p，最优位数组大小：m = −(n × ln p) / (ln 2)²\n- 最优哈希函数个数：k = (m/n) × ln 2\n\n计算 1 亿元素、1% 误判率的参数：\n\n1. 位数组大小：\n   m = −(10^8 × ln(0.01)) / (ln 2)²\n   = −(10^8 × (−4.605)) / (0.693)²\n   = (10^8 × 4.605) / 0.480\n   ≈ 9.585 × 10^8 bit\n   ≈ 114 MB\n\n2. 哈希函数个数：\n   k = (m/n) × ln 2 = (9.585 × 10^8 / 10^8) × 0.693 ≈ 9.585 × 0.693 ≈ 6.64\n   取 k = 7\n\n3. 验证误判率：\n   p = (1 − e^(−7×10^8 / 9.585×10^8))^7 = (1 − e^(−0.730))^7 = (1 − 0.482)^7 = (0.518)^7 ≈ 0.0103 ≈ 1%\n   符合目标。\n\n结果：约 114 MB 内存、7 个哈希函数，即可在 1 亿元素集合上实现 1% 误判率。对比直接存储 1 亿条 URL（约 8GB），空间节省约 70 倍。\n\n工程注意：\n- 哈希函数用 k 个独立哈希开销大，实际用 2 个独立哈希（如 murmur3 和 fnv）通过双重哈希（double hashing）生成 k 个：h_i(x) = h1(x) + i × h2(x)。\n- 布隆过滤器不支持删除（某位可能被多个元素共享）。需要删除时用计数布隆过滤器（Counting Bloom Filter），每位用计数器替代 bit。\n- 误判率随元素增多而上升，需预留冗余（实际 m 按 1.2×n 计算）。",
    tags: ["布隆过滤器", "误判率", "参数计算", "哈希函数", "应用"],
  },
  {
    id: "aae-advanced-data-structures-4",
    chapter: "aae-advanced-data-structures",
    level: 4,
    question:
      "LSM 树（Log-Structured Merge Tree）的读写放大分别是多少？它与 B+ 树在读写性能上的本质差异是什么？什么场景下该选 LSM 树？",
    answer:
      "LSM 树的读写放大分析与 B+ 树对比：\n\nLSM 树结构：\n- 数据先写入内存中的 MemTable（通常是跳表/红黑树），同时追加写 WAL（Write-Ahead Log）保证持久性。\n- MemTable 写满后冻结为 Immutable MemTable，异步刷盘为 SSTable（Level 0）。\n- 后台 Compaction 将多层 SSTable 合并：Level 0 → Level 1 → ... → Level L，每层容量递增（通常 10 倍）。\n\n写放大（Write Amplification）：\n- 写放大 = 实际写入磁盘的数据量 / 用户写入的数据量。\n- 一次写入先写 WAL（1×），再刷盘为 SSTable（1×），再经过 L 次 Compaction，每次合并时数据被重写一遍。\n- 典型写放大 = 1（WAL）+ 1（L0 刷盘）+ L（Compaction 次数）≈ O(L)，L 通常 4~7 层，写放大约 10~30 倍。\n- 对比 B+ 树：每次写入需要先读取页（1 次随机 I/O）、修改、写回（1 次随机 I/O），写放大约 1~2 倍，但每次都是随机 I/O。\n\n读放大（Read Amplification）：\n- 读放大 = 读取一次数据需要访问的磁盘次数。\n- LSM 树读取：先查 MemTable（内存），未命中则从 Level 0 到 Level L 逐层查找 SSTable。Level 0 可能有多个重叠的 SSTable，最坏需查全部。\n- 最坏读放大 = 1（MemTable）+ Level 0 的 SSTable 数 + L（每层至少查 1 次）≈ O(L + S0)，S0 为 Level 0 文件数。\n- 用布隆过滤器可大幅减少不必要的磁盘读取，实际读放大接近 O(1)。\n- 对比 B+ 树：读取 = O(log_B n) 次 I/O，每次确定路径，无冗余查找。\n\n本质差异：\n\n1. 写路径：\n- B+ 树：随机写（找到页 → 修改 → 写回），每次写都是随机 I/O。\n- LSM 树：顺序写（追加 WAL + 顺序刷 SSTable），把随机写转化为顺序写。用 Compaction 在后台整理。\n\n2. 读路径：\n- B+ 树：读路径确定（树高 log_B n 次随机 I/O），读放大低。\n- LSM 树：读路径不确定（可能查多层），读放大高，但用布隆过滤器和缓存弥补。\n\n3. 空间放大：\n- B+ 树：空间放大低（页利用率约 60~70%）。 \n- LSM 树：同一份数据可能存在于多层的多个 SSTable 中，空间放大约 1.5~2 倍，需 Compaction 回收。\n\n选型决策：\n\n选 LSM 树的场景：\n- 写多读少（如日志系统、时序数据库、消息队列）——顺序写的优势最大化。\n- 写延迟敏感——LSM 写入只追加 WAL + 内存写，延迟稳定且低（微秒级）；B+ 树写入需随机 I/O，延迟波动大。\n- SSD 场景——SSD 顺序写寿命远长于随机写，LSM 的顺序写模式延长 SSD 寿命。\n- 典型系统：RocksDB、LevelDB、Cassandra、HBase。\n\n选 B+ 树的场景：\n- 读多写少（如 OLTP 交易系统）——读路径确定，读延迟低且稳定。\n- 范围查询多——B+ 树叶子链表顺序扫描效率高。\n- 写放大不可接受——SSD 写入寿命有限时，LSM 的 10~30 倍写放大是隐患。\n- 典型系统：MySQL InnoDB、PostgreSQL。\n\n一句话：LSM 树用「写放大 + 空间放大 + 读放大」换「顺序写 + 低写延迟」，B+ 树用「随机写」换「低读放大 + 低空间放大」。写密集选 LSM，读密集选 B+ 树。",
    tags: ["综合", "LSM树", "B+树", "写放大", "读放大", "空间放大", "选型"],
  },
];
