import type { ReviewQuestion } from "./types";

/** 算法工程化实践复习题 */
export const aaeAlgorithmEngineeringQuestions: ReviewQuestion[] = [
  {
    id: "aae-algorithm-engineering-1",
    chapter: "aae-algorithm-engineering",
    level: 1,
    question:
      `算法工程化的四个阶段是什么？每个阶段的核心目标和产出物分别是什么？`,
    answer:
      `算法工程化的四个阶段：原型 → 调优 → 压测 → 部署，构成从「算法正确」到「系统可用」的闭环。\n\n第一阶段：原型（Prototype）\n- 目标：验证算法正确性和可行性，用最简单的方式实现。\n- 做法：选择最直观的实现（不考虑性能），用小规模数据验证正确性。\n- 产出物：可运行的算法原型 + 单元测试 + 正确性证明/验证。\n- 关键原则：先做对，再做快。原型阶段不追求性能，追求「算法逻辑正确」。\n- 例如：实现一个排序算法原型，先确认小数组排序结果正确，不考虑 cache、并行等优化。\n\n第二阶段：调优（Tuning）\n- 目标：把原型的性能提升到满足要求。\n- 做法：用 Profiler 找热点，针对性优化——缓存优化、SIMD 向量化、减少分支、内存布局调整、算法替换。\n- 产出物：优化后的代码 + 性能基准对比（优化前后数据）。\n- 关键原则：数据驱动优化。不要凭直觉优化，用 Profiler 找到真正的热点再动手。\n- 例如：原型用冒泡排序 O(n²)，Profiling 发现排序是热点，替换为快排 O(n log n)；进一步发现快排在特定输入上退化，加入 IntroSort 策略。\n\n第三阶段：压测（Stress Testing）\n- 目标：验证算法在真实负载和边界条件下的表现。\n- 做法：构造大规模数据、极端输入（空数据、全相同、已排序、超大值）、并发场景、长时间运行（检测内存泄漏）。测量延迟分布（P50/P95/P99/P999）、吞吐量、资源占用。\n- 产出物：压测报告（延迟分布、吞吐曲线、资源占用）+ 退化分析 + 已知限制文档。\n- 关键原则：测最坏情况而非平均情况。生产系统被 P99 甚至 P999.9 的长尾拖垮，而非平均值。\n- 例如：排序算法在随机数据上 P99 = 10ms，但在几乎有序数据上 P99 = 200ms（快排退化），需要用 IntroSort 消除长尾。\n\n第四阶段：部署（Deployment）\n- 目标：把算法上线运行，并保证可观测、可降级、可回滚。\n- 做法：灰度发布（先小流量验证）、监控指标埋点（延迟、错误率、吞吐）、降级策略（超时降级到备选算法、限流、熔断）、回滚预案。\n- 产出物：线上部署版本 + 监控大盘 + 降级/回滚 SOP。\n- 关键原则：假设会出问题，提前准备好应对。算法在压测中正常不代表线上正常——真实流量模式、数据分布、硬件差异都可能导致新问题。\n- 例如：上线一个新的推荐排序算法，配置超时 50ms 降级到旧的排序算法，监控 P99 延迟和点击率指标。\n\n四阶段的关系：\n- 原型保证「正确」，调优保证「快」，压测保证「稳」，部署保证「可控」。\n- 不是一次性线性流程，而是迭代循环——线上发现的问题可能回到原型阶段重新选算法。\n- 每个阶段都有明确的「出口标准」——不达标不进入下一阶段。`,
    tags: ["算法工程化", "原型", "调优", "压测", "部署", "四阶段"],
  },
  {
    id: "aae-algorithm-engineering-2",
    chapter: "aae-algorithm-engineering",
    level: 2,
    question:
      `什么是缓存友好的编程？为什么「数据局部性」对算法性能如此重要？请用矩阵乘法的例子说明缓存优化的效果。`,
    answer:
      `缓存友好的编程：\n\n利用 CPU 多级缓存（L1/L2/L3）的层次结构，让算法访问数据的模式匹配缓存的特性，减少缓存未命中（cache miss），从而提升实际性能。\n\n核心原理——数据局部性：\n\n1. 空间局部性（Spatial Locality）：\n访问了地址 A，很可能接下来访问 A 附近的地址。因为缓存以缓存行（cache line，通常 64 字节）为单位加载——访问 A 时，A 所在的整个缓存行被加载到缓存。顺序访问数组的下一个元素就是缓存命中。\n\n2. 时间局部性（Temporal Locality）：\n访问了地址 A，很可能在不久的将来再次访问 A。把频繁访问的数据保留在缓存中。\n\n为什么数据局部性重要：\n- L1 缓存命中：~1ns（1 个周期）\n- L2 缓存命中：~4ns（~12 个周期）\n- L3 缓存命中：~12ns（~40 个周期）\n- 主内存访问：~100ns（~200+ 个周期）\n- 缓存命中和缓存未命中差 100~200 倍。即使算法复杂度相同，缓存命中率不同会导致实际性能差 10~100 倍。\n\n矩阵乘法的缓存优化例子：\n\n朴素三重循环：\n\`\`\`\nfor i in 0..N:\n  for j in 0..N:\n    for k in 0..N:\n      C[i][j] += A[i][k] * B[k][j]\n\`\`\`\n\n访问模式分析（假设 N=1024，float 类型，缓存行 64 字节 = 16 个 float）：\n- A[i][k]：k 递增，行内顺序访问 → 空间局部性好，缓存命中率高。\n- B[k][j]：k 递增但 j 固定，每次跨行访问（步长 = N × 4 字节）→ 空间局部性极差，几乎每次都缓存未命中。\n- C[i][j]：j 递增，行内顺序 → 好。\n\n当 N=1024 时，B 的一列占 4KB，远超 L1 缓存（通常 32KB 但多路共享）。B 的每次访问几乎都是缓存未命中。\n\n优化 1：交换循环顺序（i-k-j）\n\`\`\`\nfor i in 0..N:\n  for k in 0..N:\n    for j in 0..N:\n      C[i][j] += A[i][k] * B[k][j]\n\`\`\`\n- B[k][j]：j 递增，行内顺序 → 缓存友好。\n- C[i][j]：j 递增，行内顺序 → 缓存友好。\n- A[i][k]：k 变化但在内层循环外，每次外层迭代读一次 → 可寄存器缓存。\n效果：缓存命中率大幅提升，实际快 2~3 倍。\n\n优化 2：分块（Blocking / Tiling）\n把矩阵分成小块（如 32×32），使每块能放入缓存：\n\`\`\`\nfor ii in 0..N step B:\n  for jj in 0..N step B:\n    for kk in 0..N step B:\n      for i in ii..ii+B:\n        for k in kk..kk+B:\n          for j in jj..jj+B:\n            C[i][j] += A[i][k] * B[k][j]\n\`\`\`\n- 32×32 的块占 32×32×4 = 4KB，三个块（A, B, C）共 12KB，可放入 L1 缓存。\n- 块内访问完全缓存命中。\n效果：比朴素版本快 5~10 倍（N=1024 时）。\n\n优化 3：SIMD 向量化 + 分块\n结合分块和 SIMD（如 AVX2 一次处理 8 个 float），进一步提升。\n\n实测对比（N=1024，单线程）：\n- 朴素 i-j-k：~3 秒\n- 交换为 i-k-j：~1.5 秒\n- 分块 32×32：~0.5 秒\n- 分块 + SIMD：~0.15 秒\n- 理论峰值：~0.1 秒\n\n从 3 秒到 0.15 秒，提升 20 倍，算法复杂度始终是 O(n³)。差异完全来自缓存和 SIMD。\n\n缓存优化的一般原则：\n1. 顺序访问优于随机访问（数组优于链表）。\n2. 数据紧凑——减小结构体大小，提高缓存行利用率。\n3. 分块——把大数据集切成缓存大小的块。\n4. 数据布局——SoA（Structure of Arrays）优于 AoS（Array of Structures），利于 SIMD 和缓存行填充。\n5. 预取——显式或编译器隐式预取数据到缓存。`,
    tags: ["缓存友好", "数据局部性", "矩阵乘法", "分块", "SIMD", "缓存行"],
  },
  {
    id: "aae-algorithm-engineering-3",
    chapter: "aae-algorithm-engineering",
    level: 3,
    question:
      `什么是 SIMD（Single Instruction Multiple Data）？它如何加速算法？请描述用 SIMD 优化一个数组求和算法的过程，并说明什么条件下 SIMD 优化才有效。`,
    answer:
      `SIMD（单指令多数据）：\n\n一种并行计算范式——一条指令同时对多个数据元素执行相同操作。现代 CPU 的 SIMD 指令集包括 SSE（128 位，4 个 float）、AVX2（256 位，8 个 float）、AVX-512（512 位，16 个 float）。\n\n原理：\n- 标量处理：一条 ADD 指令加一对 float，需要 n 次指令处理 n 对数据。\n- SIMD 处理：一条 VADDPS 指令同时加 8 对 float（AVX2），只需 n/8 次指令。\n- 理论加速比 = SIMD 宽度 / 数据宽度（AVX2 float 理论 8 倍加速）。\n\n用 SIMD 优化数组求和：\n\n标量版本：\n\`\`\`\nfloat sum = 0;\nfor (int i = 0; i < n; i++)\n    sum += arr[i];\n\`\`\`\n每轮加一个元素，n 次加法，n 次循环开销。\n\nSIMD 版本（AVX2，8 路并行）：\n\`\`\`\n__m256 vsum = _mm256_setzero_ps();  // 8 路累加器初始化为 0\nint i = 0;\n// 主循环：每次处理 8 个元素\nfor (; i + 7 < n; i += 8) {\n    __m256 v = _mm256_loadu_ps(&arr[i]);  // 加载 8 个 float\n    vsum = _mm256_add_ps(vsum, v);         // 8 路并行加法\n}\n// 水平求和：把 8 路累加器合并为 1 个标量\nfloat temp[8];\n_mm256_storeu_ps(temp, vsum);\nfloat sum = temp[0] + temp[1] + temp[2] + temp[3] +\n            temp[4] + temp[5] + temp[6] + temp[7];\n// 处理尾部不足 8 个的剩余元素\nfor (; i < n; i++)\n    sum += arr[i];\n\`\`\`\n\n优化要点：\n1. 向量加载：_mm256_loadu_ps 一次加载 8 个连续 float 到 256 位寄存器。\n2. 向量加法：_mm256_add_ps 一条指令同时加 8 对 float。\n3. 循环展开：主循环步长为 8，减少循环次数和分支开销。\n4. 水平归约：最后把 8 路累加器合并为标量（可用 _mm256_hadd_ps 优化）。\n5. 尾部处理：n 不是 8 的倍数时，剩余元素用标量处理。\n\n进一步优化——多累加器避免数据依赖：\n\`\`\`\n__m256 vsum0 = _mm256_setzero_ps();\n__m256 vsum1 = _mm256_setzero_ps();\nfor (int i = 0; i + 15 < n; i += 16) {\n    vsum0 = _mm256_add_ps(vsum0, _mm256_loadu_ps(&arr[i]));\n    vsum1 = _mm256_add_ps(vsum1, _mm256_loadu_ps(&arr[i+8]));\n}\n\`\`\`\n两个独立累加器打破流水线依赖（vsum0 和 vsum1 无依赖，可并行执行），进一步提升吞吐。\n\nSIMD 优化有效的条件：\n\n1. 数据连续（Contiguous Memory）：\nSIMD 要求一次加载的数据在内存中连续。数组/向量天然满足；链表、稀疏矩阵不满足。数据布局需是 SoA 或紧凑数组。\n\n2. 无分支或分支可消除（Branch-Free）：\nSIMD 指令对所有数据元素执行相同操作。如果循环内有 if-else 分支，不同元素走不同路径，SIMD 无法直接处理。需要用位掩码（mask）替代分支。\n\n3. 数据对齐（Alignment）：\nSIMD 加载指令在对齐时更快（如 AVX2 要求 32 字节对齐）。未对齐的加载有性能惩罚（虽然现代 CPU 已大幅缓解）。\n\n4. 数据量足够大：\nSIMD 有加载/存储/水平归约的固定开销。数据量太小（如 n < 16）时，SIMD 开销可能超过收益。需要标量快速路径处理小数据。\n\n5. 操作可向量化：\n并非所有操作都能向量化。复杂控制流、数据依赖强的操作（如前缀和的朴素实现）难以直接向量化，需要特殊算法（如并行前缀和的 SIMD 版本）。\n\n自动向量化：\n现代编译器（GCC -O3 -ftree-vectorize、Clang）能自动将简单循环向量化。但手动 SIMD（intrinsics）通常比自动向量化快 1.5~3 倍，因为可以控制对齐、多累加器、掩码等细节。\n\n实测效果（数组求和，n = 10^8）：\n- 标量：~35ms\n- 自动向量化：~12ms\n- 手动 AVX2 + 双累加器：~7ms\n- 理论极限（内存带宽瓶颈）：~5ms`,
    tags: ["SIMD", "向量化", "数组求和", "AVX", "缓存对齐", "自动向量化"],
  },
  {
    id: "aae-algorithm-engineering-4",
    chapter: "aae-algorithm-engineering",
    level: 4,
    question:
      `什么是降级策略（Degradation / Fallback Strategy）？在算法工程中如何设计一个多级降级方案？请以搜索引擎排序系统为例说明。`,
    answer:
      `降级策略：\n\n当系统面临异常（流量突增、依赖故障、超时、资源不足）时，主动牺牲部分功能或质量以保全核心功能的策略。核心思想：「宁可降级服务，不可完全不可用」。\n\n降级 vs 熔断 vs 限流：\n- 降级：主动切换到更轻量/更简单的备选方案。\n- 熔断：直接拒绝请求，防止级联故障。\n- 限流：限制请求速率，超过阈值的请求排队或拒绝。\n三者常组合使用。\n\n多级降级设计原则：\n\n1. 分级：按「质量损失从小到大」设计多个降级级别，逐级触发。\n2. 可观测：每级降级有明确的触发条件和监控指标。\n3. 可逆：异常解除后能自动恢复到更高级别。\n4. 核心保全：最深的降级也保证核心功能可用。\n5. 用户无感或弱感：降级后的体验降级尽量不影响核心操作。\n\n搜索引擎排序系统的多级降级方案：\n\n正常状态（Level 0）——全功能排序：\n- 完整倒排索引检索 + BM25 打分 + 个性化重排序模型（深度学习）+ 多样性控制 + 实时点击反馈。\n- 延迟目标：P99 < 200ms。\n- 质量：最佳排序效果。\n\n降级 Level 1——关闭个性化重排序：\n- 触发条件：深度学习排序服务 P99 > 500ms 或错误率 > 1%。\n- 策略：跳过个性化重排序模型，只用 BM25 倒排索引打分 + 简单的业务规则排序。\n- 质量损失：排序个性化程度降低，但结果相关性仍有保证（BM25 兜底）。\n- 延迟：P99 降到 < 100ms（去掉最重的模型推理环节）。\n- 用户感知：轻微——搜索结果可能不如之前「懂你」，但仍然相关。\n\n降级 Level 2——减少候选集 + 简化打分：\n- 触发条件：索引服务负载过高（CPU > 80%）或延迟 P99 > 300ms。\n- 策略：\n  - 减少检索的候选文档数（从 Top 10000 降到 Top 1000）。\n  - 关闭短语查询和邻近度计算（只用布尔匹配 + TF-IDF）。\n  - 关闭分面统计和高亮。\n- 质量损失：排序精度进一步下降，长尾查询可能漏掉部分结果。\n- 延迟：P99 降到 < 50ms。\n- 用户感知：中等——搜索结果可能不够全面，但核心查询仍有结果。\n\n降级 Level 3——缓存兜底：\n- 触发条件：索引服务完全不可用或超时率 > 10%。\n- 策略：\n  - 只返回缓存中的历史查询结果（热门查询缓存命中率 > 30%）。\n  - 非缓存查询返回「搜索服务繁忙，请稍后重试」或默认推荐结果。\n- 质量损失：非热门查询无实时结果。\n- 延迟：P99 < 10ms（纯缓存读取）。\n- 用户感知：明显——但热门查询仍可用，系统不会完全宕机。\n\n降级 Level 4——静态页面兜底：\n- 触发条件：整个搜索服务不可用。\n- 策略：返回预生成的静态搜索页面（如热门关键词的预计算结果页面）或搜索不可用的友好提示页。\n- 质量损失：无实时搜索能力。\n- 用户感知：搜索功能不可用，但网站其他功能不受影响。\n\n降级触发与恢复机制：\n\n触发：\n- 基于实时指标（延迟、错误率、CPU/内存使用率）自动触发。\n- 使用滑动窗口统计（如最近 30 秒的 P99 延迟）避免抖动。\n- 触发阈值有「冷却时间」防止频繁切换。\n\n恢复：\n- 异常指标恢复后，逐步升级（Level 3 → Level 2 → Level 1 → Level 0）。\n- 升级比降级更保守——需要连续 N 个窗口指标正常才升级，避免「乒乓」抖动。\n- 灰度恢复：先放 10% 流量到高级别，观察无异常再全量恢复。\n\n工程实现要点：\n1. 降级开关用配置中心动态控制（如 Apollo、Nacos），无需重启服务。\n2. 每级降级有独立的监控大盘和告警。\n3. 定期演练降级流程（混沌工程），确保降级路径可用。\n4. 降级逻辑与业务逻辑解耦——用策略模式或中间件统一管理，不散落在代码各处。\n5. 记录降级事件日志，用于事后复盘和降级阈值调优。\n\n一句话：降级策略是算法工程的「安全气囊」——正常时全速运行，异常时逐级收缩，保证「核心功能永不停机」。设计关键是分级、可观测、可逆、保核心。`,
    tags: ["综合", "降级策略", "多级降级", "搜索引擎", "熔断", "限流", "工程闭环"],
  },
];
