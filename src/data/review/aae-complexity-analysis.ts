import type { ReviewQuestion } from "./types";

/** 复杂度分析与工程权衡复习题 */
export const aaeComplexityAnalysisQuestions: ReviewQuestion[] = [
  {
    id: "aae-complexity-analysis-1",
    chapter: "aae-complexity-analysis",
    level: 1,
    question:
      `什么是摊还分析（Amortized Analysis）？它有哪三种方法？分别适用于什么场景？`,
    answer:
      `摊还分析是对一系列操作的总代价求平均，而非单独分析每个操作的最坏代价。关键区别：摊还分析不是平均情况分析（不涉及概率），而是保证「任意操作序列的总代价有上界」，即使单个操作偶尔很贵。\n\n三种方法：\n\n1. 聚合分析（Aggregate Method）：\n直接计算 n 个操作的总代价上界 T(n)，再除以 n 得到每个操作的摊还代价。适用场景：操作序列结构简单，能直接求和。例如 n 次 push 到动态数组的总代价是 O(n)，摊还 O(1)。\n\n2. 核算法（Accounting Method / 记账法）：\n给每次操作「收费」一个摊还代价，其中一部分用于实际执行，剩余「存入银行」作为信用。当后续操作实际代价高于摊还代价时，消耗信用补差。要求任意时刻信用 ≥ 0。适用场景：操作有「便宜」和「昂贵」交替的模式。例如动态数组扩容：每次 push 收费 3（1 用于写入，2 存银行），扩容时消费银行信用。\n\n3. 势能法（Potential Method）：\n定义势能函数 Φ(D) 将数据结构状态 D 映射为非负实数。摊还代价 = 实际代价 + ΔΦ。若 Φ 始终非负，则总摊还代价是总实际代价的上界。适用场景：状态有清晰的「势能」概念。例如二进制计数器中 Φ = 1 的位数，动态数组中 Φ = 2×size − capacity。\n\n记忆线索：聚合法「先求总再除」，核算法「先收费再报销」，势能法「定义能量看变化」。`,
    tags: ["摊还分析", "聚合分析", "核算法", "势能法"],
  },
  {
    id: "aae-complexity-analysis-2",
    chapter: "aae-complexity-analysis",
    level: 2,
    question:
      `为什么理论复杂度更优的算法在实际运行中可能更慢？请列举至少三个导致「大O欺骗」的因素。`,
    answer:
      `大O记号隐藏了常数因子和低阶项，而真实硬件有复杂的层次结构，导致理论更优的算法实际可能更慢。主要因素：\n\n1. 常数因子差异：\n大O记号忽略常数。一个 O(n) 算法如果常数是 1000，在 n < 1000 时比常数是 1 的 O(n log n) 算法还慢。例如线性查找 O(n) 在小数组上往往比二分查找 O(log n) 快，因为二分查找的分支预测失败率高、每次比较开销大。\n\n2. 缓存局部性（Cache Locality）：\n现代 CPU 有 L1/L2/L3 多级缓存，访问缓存中的数据比访问内存快 10~100 倍。一个缓存友好的 O(n log n) 算法（如归并排序的顺序访问）可能比缓存不友好的 O(n) 算法（如链表遍历的随机访问）更快。B 树 O(log n) 比红黑树 O(log n) 在磁盘上快得多，就是因为 B 树的节点大小匹配磁盘页，减少了 I/O 次数。\n\n3. 分支预测（Branch Prediction）：\nCPU 流水线依赖分支预测，预测失败会冲刷流水线，代价 10~20 周期。分支密集的算法（如快排的 partition、二分查找）如果分支模式不可预测，性能会大幅下降。排序后的数据上快排比随机数据慢，就是因为 partition 分支变得不可预测。\n\n4. SIMD 与向量化：\n现代 CPU 支持 SIMD（一条指令处理多个数据），但只有数据连续且无分支的代码才能被自动向量化。一个理论上更优但无法向量化的算法，可能被一个能向量化的「次优」算法碾压。\n\n5. 内存分配开销：\n频繁动态分配内存的算法（如递归创建临时数组）会触发 malloc/free 开销和内存碎片。原地算法虽然复杂度相同甚至更高，但没有分配开销，实际更快。\n\n一句话：大O衡量「渐近增长率」，但真实性能取决于「常数 + 缓存 + 分支 + 向量化 + 分配」。工程上必须用 Profiler 实测，不能只看大O。`,
    tags: ["实际性能", "理论复杂度", "缓存局部性", "分支预测", "常数因子"],
  },
  {
    id: "aae-complexity-analysis-3",
    chapter: "aae-complexity-analysis",
    level: 3,
    question:
      `用势能法分析动态数组（可扩容数组）的 push_back 操作的摊还时间复杂度。要求给出势能函数和推导过程。`,
    answer:
      `动态数组的 push_back 分析（势能法）：\n\n设定：\n- 数组容量为 capacity，当前元素数为 size。\n- 当 size == capacity 时触发扩容：分配 2×capacity 的新数组，拷贝所有元素，释放旧数组。扩容代价 = O(size)。\n- 非扩容的 push_back 代价 = O(1)。\n\n势能函数定义：\nΦ(D) = 2 × size − capacity\n\n验证势能非负：\n- 初始状态 size = 0, capacity = 0, Φ = 0。\n- 扩容后 size = capacity/2（刚拷贝完，新 capacity = 2 × 旧 capacity = 2 × size），所以 Φ = 2 × size − 2 × size = 0。\n- 每次非扩容 push 后 size 增加 1，capacity 不变，Φ 增加 2，始终 ≥ 0。满足势能非负条件。\n\n分析第 i 次 push_back 的摊还代价：\n\n情况 1：不触发扩容（size < capacity）\n- 实际代价 c_i = 1（写入一个元素）\n- ΔΦ = 2 × (size+1) − capacity − (2 × size − capacity) = 2\n- 摊还代价 â_i = c_i + ΔΦ = 1 + 2 = 3 = O(1)\n\n情况 2：触发扩容（size == capacity）\n- 扩容前：size = capacity，Φ_before = 2 × size − capacity = capacity\n- 实际代价 c_i = size + 1（拷贝 size 个元素 + 写入 1 个新元素）\n- 扩容后：新 capacity = 2 × capacity，新 size = size + 1\n- Φ_after = 2 × (size+1) − 2 × capacity = 2 × size + 2 − 2 × size = 2（因为 capacity = size）\n- ΔΦ = Φ_after − Φ_before = 2 − capacity = 2 − size\n- 摊还代价 â_i = c_i + ΔΦ = (size + 1) + (2 − size) = 3 = O(1)\n\n结论：\n无论是否扩容，每次 push_back 的摊还代价都是 3 = O(1)。因此 n 次 push_back 的总摊还代价 = O(n)，即每次操作摊还 O(1)。\n\n直观理解：势能函数像「银行存款」——非扩容时每次存 2 单位信用，扩容时 capacity 个元素的拷贝代价由之前积累的 capacity 单位信用支付，恰好够用。`,
    tags: ["摊还分析", "势能法", "动态数组", "push_back", "推导"],
  },
  {
    id: "aae-complexity-analysis-4",
    chapter: "aae-complexity-analysis",
    level: 4,
    question:
      `在工程实践中，如何系统地做「时间复杂度与空间复杂度的权衡决策」？请给出一个决策框架并用具体案例说明。`,
    answer:
      `时间-空间权衡决策框架：\n\n第一步：明确约束条件\n- 时间约束：延迟上限（如 P99 < 100ms）、吞吐量要求（如 10万 QPS）。\n- 空间约束：内存上限（如单实例 4GB）、存储成本预算。\n- 可用性约束：是否允许预计算、是否容忍最终一致。\n\n第二步：分析算法的时间-空间帕累托前沿\n对同一个问题，列出不同算法的时间和空间复杂度，画出帕累托前沿：\n- 哈希表：查找 O(1)，空间 O(n)\n- 排序数组 + 二分：查找 O(log n)，空间 O(n)\n- 布隆过滤器：查找 O(k)，空间 O(n/8 × m/n)（远小于 n），但有假阳性\n- 前缀树（Trie）：查找 O(L)，空间 O(字符集大小 × 平均长度 × n)\n\n第三步：根据约束选择帕累托最优点\n- 空间充裕、延迟敏感 → 选时间最优（如哈希表）\n- 空间紧张、可容忍误差 → 选空间最优（如布隆过滤器）\n- 介于两者之间 → 选折中方案\n\n第四步：用实测验证\n理论选定后在真实数据上 Profiler 实测，因为常数因子和缓存效应可能颠覆理论分析。\n\n案例：URL 黑名单检测系统\n\n约束：\n- 黑名单 10 亿条 URL，每条平均 80 字节\n- 查询 QPS 10 万，延迟 < 1ms\n- 内存预算 4GB\n- 允许极小概率误判（假阳性），但不允许漏判（假阴性）\n\n方案分析：\n\n方案 A：哈希表存储所有 URL\n- 空间：10 亿 × 80 字节 ≈ 80GB → 远超 4GB 预算，不可行\n\n方案 B：排序数组 + 二分\n- 空间：同样 80GB → 不可行\n\n方案 C：布隆过滤器\n- 空间：10 亿 URL，要求假阳性率 < 1%，需要约 10 亿 × 10 bit ≈ 1.2GB → 在预算内\n- 时间：k 个哈希函数，k ≈ 7，每次查询 O(7) ≈ O(1) → 满足延迟要求\n- 代价：约 1% 的合法 URL 被误判为黑名单，需二次确认（查真实数据库）\n\n方案 D：布隆过滤器 + 分级缓存\n- 第一级：布隆过滤器（1.2GB，快速过滤 99% 的合法 URL）\n- 第二级：LRU 缓存最近确认的黑名单 URL（100MB，命中率 > 90%）\n- 第三级：持久化数据库（准确但慢，仅处理布隆过滤器的阳性结果）\n- 总空间 ≈ 1.3GB，延迟 < 1ms（绝大多数请求在第一级就返回）\n\n决策：选方案 D。用布隆过滤器的小空间换哈希表的大空间（空间权衡），用多级缓存换单次查询的确定性（时间换准确性的妥协 + 用缓存弥补）。\n\n关键原则总结：\n1. 没有免费的午餐：时间换空间、空间换时间、精度换空间、预计算换运行时——都是交易。\n2. 先看约束再选方案：脱离约束谈复杂度没有意义。80GB 内存够的话哈希表最简单。\n3. 帕累托最优而非全局最优：通常不存在时间和空间同时最优的方案，要在前沿上选最匹配约束的点。\n4. 允许妥协换取可行性：布隆过滤器用「允许假阳性」换「空间可行」，这是工程中常见的「降级精度以换取可行性」思维。\n5. 实测兜底：理论分析定方向，Profiler 定终方案。`,
    tags: ["综合", "时间空间权衡", "决策框架", "布隆过滤器", "帕累托", "案例"],
  },
];
