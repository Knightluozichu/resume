import type { ReviewQuestion } from "./types";

/** 整洁架构复习题 */
export const addCleanArchitectureQuestions: ReviewQuestion[] = [
  {
    id: "add-clean-architecture-01",
    chapter: "add-clean-architecture",
    level: 1,
    question: "整洁架构（Clean Architecture）由哪四层组成？每层的职责是什么？依赖方向有什么规则？",
    answer:
      "整洁架构由 Robert C. Martin（Uncle Bob）提出，用同心圆表示四层，从内到外依次为：\n\n1. Entities（实体层 / 企业业务规则层）：\n最内层，包含核心业务实体和业务规则。这些实体是系统的「企业级」对象——即使没有系统，这些业务概念也存在（如「订单」「用户」「账户」）。它们是最稳定、最不变化的，独立于任何外部系统。\n\n2. Use Cases（用例层 / 应用业务规则层）：\n包含系统的业务用例——特定应用场景的业务规则。如「创建订单」「用户注册」「处理退款」。它编排 Entities 层的对象来完成具体业务流程。它知道 Entities 的规则，但不知道 UI、数据库等外部细节。\n\n3. Interface Adapters（接口适配器层）：\n负责数据格式的转换——把外部（UI、数据库、外部 API）的数据格式转换成内层（Use Cases / Entities）需要的格式，反之亦然。包含控制器（Controller）、展示器（Presenter）、网关（Gateway）、Repository 实现等。\n\n4. Frameworks & Drivers（框架与驱动层）：\n最外层，包含具体的技术实现——Web 框架、数据库、UI 框架、第三方库。这一层是「细节」，应该尽量薄，只负责把外部技术连接到内层的接口上。\n\n依赖方向规则（核心铁律）：\n\n「依赖方向必须向内指向。」\n- 外层可以依赖内层，内层绝不依赖外层。\n- Entities 不依赖任何层；Use Cases 只依赖 Entities；Interface Adapters 依赖 Use Cases 和 Entities；Frameworks 依赖所有内层。\n- 内层定义接口，外层实现接口（依赖倒置）——如 Use Cases 层定义 Repository 接口，Frameworks 层实现它。\n\n这条规则确保了：核心业务逻辑（Entities + Use Cases）不依赖任何外部技术（数据库、框架、UI），可以被独立测试、独立复用、独立替换。",
    tags: ["整洁架构", "Clean Architecture", "四层", "Entities", "Use Cases", "依赖方向", "定义"],
  },
  {
    id: "add-clean-architecture-02",
    chapter: "add-clean-architecture",
    level: 2,
    question: "整洁架构为什么要用「同心圆」来表示？「依赖方向向内」解决了传统分层架构的什么问题？",
    answer:
      "用同心圆表示的原因：\n\n1. 直观表达「层次与重要性」：\n同心圆从内到外，内圈是「核心」，外圈是「外围」。这直观传达了一个核心理念——越内层越重要、越稳定、越不变化；越外层越次要、越易变、越可替换。圆心是系统的灵魂（业务规则），外围是系统的壳（技术实现）。\n\n2. 表达「单向依赖」：\n同心圆天然有方向性——外圈包着内圈，箭头只能从外指向内。如果内层依赖外层，就等于「圆心依赖圆周」，视觉上和逻辑上都不合理。同心圆让依赖方向一目了然。\n\n3. 表达「可替换性」：\n外层可以替换而不影响内层——换数据库（换一圈外层）、换 UI 框架（换另一圈外层），内核不变。同心圆传达了「外层是可拔插的细节」。\n\n「依赖方向向内」解决了传统分层架构的核心问题：\n\n传统分层架构的问题：业务逻辑层依赖数据访问层（依赖向下/向外）。核心业务被技术细节绑架——换数据库要改业务代码，测试业务逻辑必须连数据库。\n\n整洁架构的解法：\n1. 业务核心（Entities + Use Cases）在最内层，不依赖任何外层。\n2. 数据库访问在 Frameworks 层（最外层），实现 Use Cases 层定义的 Repository 接口。\n3. 依赖方向从「业务→数据库」反转为「数据库→业务定义的接口」（依赖倒置）。\n4. 业务核心完全不知道数据库的存在——它只知道一个接口（Repository），谁实现这个接口、用什么数据库实现，核心不关心。\n\n解决的具体问题：\n- 可测试性：测试 Use Cases 时注入 Mock Repository，不连数据库。\n- 可替换性：换数据库只改 Frameworks 层的实现，Use Cases 和 Entities 一行不改。\n- 独立性：业务核心可以独立编译、独立部署、独立演化，不受技术变更影响。\n- 延迟决策：「用什么数据库/框架」的决策可以推迟到项目后期，先写核心业务逻辑跑通。\n\n一句话：同心圆把「业务核心」放在不可触碰的圆心，把「技术细节」放在可替换的外圈，用依赖方向向内的铁律保证核心永远不被外围绑架。",
    tags: ["整洁架构", "同心圆", "依赖方向", "传统分层问题", "可测试性", "可替换性"],
  },
  {
    id: "add-clean-architecture-03",
    chapter: "add-clean-architecture",
    level: 3,
    question: "用具体代码结构说明整洁架构的落地：一个「用户注册」用例，各层的代码应该怎么组织？Repository 接口定义在哪层？实现在哪层？",
    answer:
      "以「用户注册」用例为例，整洁架构的落地代码组织：\n\n目录结构：\n```\nsrc/\n  entities/                    ← Entities 层（最内层）\n    user.ts                    // User 实体（含业务规则）\n  usecases/                    ← Use Cases 层\n    register-user/\n      register-user.ts         // 注册用例的输入输出定义\n      register-user-interactor.ts  // 用例的执行逻辑\n  interface-adapters/          ← Interface Adapters 层\n    controllers/\n      register-user-controller.ts  // HTTP 控制器\n    presenters/\n      register-user-presenter.ts   // 响应格式化\n    gateways/\n      user-repository.ts       // Repository 接口定义（在适配器层定义接口也可）\n  frameworks/                  ← Frameworks & Drivers 层（最外层）\n    database/\n      user-repository-mysql.ts  // Repository 的 MySQL 实现\n    web/\n      server.ts                // Express/Koa 服务器\n      routes.ts                // 路由配置\n```\n\n各层代码：\n\n1. Entities 层——User 实体（含业务规则，不依赖任何外层）：\n```typescript\n// entities/user.ts\nexport class User {\n  constructor(\n    public readonly id: string,\n    public readonly email: string,\n    public readonly passwordHash: string,\n  ) {}\n\n  // 业务规则：邮箱格式验证（实体自带行为）\n  static validateEmail(email: string): boolean {\n    return /^[^@]+@[^@]+\\.[^@]+$/.test(email);\n  }\n}\n```\n\n2. Use Cases 层——注册用例（依赖 Entities 和 Repository 接口，不依赖具体实现）：\n```typescript\n// usecases/register-user/register-user.ts\n// 定义用例的输入输出（DTO）\nexport interface RegisterUserInput {\n  email: string;\n  password: string;\n}\nexport interface RegisterUserOutput {\n  id: string;\n  email: string;\n}\n\n// Repository 接口——定义在 Use Cases 能看到的地方\n// （接口属于「抽象」，由高层定义，低层实现）\nexport interface UserRepository {\n  findByEmail(email: string): Promise<User | null>;\n  save(user: User): Promise<void>;\n}\n\n// usecases/register-user/register-user-interactor.ts\nexport class RegisterUserInteractor {\n  constructor(\n    private userRepo: UserRepository,  // 依赖接口，不依赖具体实现\n    private hasher: PasswordHasher,     // 密码哈希也是一个接口\n  ) {}\n\n  async execute(input: RegisterUserInput): Promise<RegisterUserOutput> {\n    // 1. 业务规则验证\n    if (!User.validateEmail(input.email)) {\n      throw new Error('邮箱格式不合法');\n    }\n    // 2. 检查邮箱是否已注册\n    const existing = await this.userRepo.findByEmail(input.email);\n    if (existing) {\n      throw new Error('邮箱已注册');\n    }\n    // 3. 创建实体（业务规则封装在实体里）\n    const passwordHash = this.hasher.hash(input.password);\n    const user = new User(uuid(), input.email, passwordHash);\n    // 4. 持久化\n    await this.userRepo.save(user);\n    // 5. 返回输出\n    return { id: user.id, email: user.email };\n  }\n}\n```\n\n3. Interface Adapters 层——控制器（把 HTTP 请求翻译成用例调用）：\n```typescript\n// interface-adapters/controllers/register-user-controller.ts\nexport class RegisterUserController {\n  constructor(\n    private interactor: RegisterUserInteractor,\n    private presenter: RegisterUserPresenter,\n  ) {}\n\n  async handle(httpRequest: HttpRequest): Promise<HttpResponse> {\n    const input: RegisterUserInput = {\n      email: httpRequest.body.email,\n      password: httpRequest.body.password,\n    };\n    try {\n      const output = await this.interactor.execute(input);\n      return this.presenter.presentSuccess(output);\n    } catch (e) {\n      return this.presenter.presentError(e);\n    }\n  }\n}\n```\n\n4. Frameworks 层——Repository 的 MySQL 实现（实现接口，依赖方向向内）：\n```typescript\n// frameworks/database/user-repository-mysql.ts\nexport class UserMySQLRepository implements UserRepository {\n  constructor(private db: MySQLConnection) {}\n\n  async findByEmail(email: string): Promise<User | null> {\n    const row = await this.db.query('SELECT * FROM users WHERE email = ?', [email]);\n    return row ? new User(row.id, row.email, row.password_hash) : null;\n  }\n\n  async save(user: User): Promise<void> {\n    await this.db.execute(\n      'INSERT INTO users (id, email, password_hash) VALUES (?, ?, ?)',\n      [user.id, user.email, user.passwordHash]\n    );\n  }\n}\n```\n\n5. 依赖注入组装（在 Frameworks 层，把所有东西连起来）：\n```typescript\n// frameworks/web/server.ts\nconst db = new MySQLConnection(/* ... */);\nconst userRepo = new UserMySQLRepository(db);        // 外层实现\nconst hasher = new BcryptHasher();                    // 外层实现\nconst interactor = new RegisterUserInteractor(userRepo, hasher);  // 注入到内层\nconst presenter = new RegisterUserPresenter();\nconst controller = new RegisterUserController(interactor, presenter);\n```\n\n关键点：\n- UserRepository 接口定义在 Use Cases 层（或更内层），UserMySQLRepository 实现在 Frameworks 层。依赖方向：Frameworks → Use Cases 定义的接口。\n- RegisterUserInteractor 只依赖 UserRepository 接口和 PasswordHasher 接口，完全不知道 MySQL 和 Bcrypt 的存在。\n- 单元测试 RegisterUserInteractor 时，注入 Mock Repository 和 Mock Hasher，不依赖任何外部系统。\n- 换数据库：只写一个新的 Repository 实现类，在组装处替换注入，Use Cases 和 Entities 一行不改。",
    tags: ["整洁架构", "代码示例", "落地", "Repository", "依赖注入", "用户注册"],
  },
  {
    id: "add-clean-architecture-04",
    chapter: "add-clean-architecture",
    level: 4,
    question: "整洁架构在实际落地时常被批评「太重」「模板代码太多」。如何判断项目是否需要整洁架构？在简单 CRUD 项目中是否该用？如何务实地裁剪？",
    answer:
      "整洁架构确实有成本——多层间接、DTO 转换、接口定义、依赖注入组装，这些都是「模板代码」。是否采用要看项目的复杂度和生命周期。判断和裁剪框架如下：\n\n判断是否需要整洁架构：\n\n该用的信号（项目有足够复杂度）：\n1. 业务规则复杂且持续演化：系统核心有大量业务规则（如电商的定价、促销、风控规则），且业务规则频繁变化。核心需要保护，不能被技术变更干扰。\n2. 多种外部集成：系统对接多种外部系统（多种数据库、多个第三方 API、消息队列），需要清晰的边界来隔离这些集成的变化。\n3. 长生命周期：系统预期维护 3-5 年以上，技术栈可能中途更换（换框架、换数据库）。整洁架构的保护层能降低技术迁移成本。\n4. 团队较大：多人协作时，清晰的分层和依赖规则能减少互相干扰，让团队按层分工。\n5. 可测试性要求高：核心业务逻辑需要高覆盖率的单元测试，且测试不能依赖外部系统。\n\n不该用的信号（项目简单或短期）：\n1. 纯 CRUD 无复杂业务规则：系统只是对数据库表的增删改查包装，几乎没有业务逻辑。整洁架构的多层间接是纯开销——业务逻辑那么薄，分四层没有内容可放。\n2. 短期/一次性项目：预期寿命几个月的项目，投入整洁架构的搭建成本收不回来。\n3. 技术栈确定不变：如内部工具，确定用某个框架和数据库，不会换。保护层没有发挥价值。\n4. 单人/小团队：团队 1-2 人，沟通成本不是问题，多层间接反而降低开发速度。\n5. 快速验证的原型：探索期需要快速试错，整洁架构的规范化流程拖慢验证速度。\n\n简单 CRUD 项目是否该用：\n\n一般不该全量用。一个简单的「管理后台 CRUD 接口」，业务逻辑薄到只是校验字段 + 存数据库，套四层架构是杀鸡用牛刀。直接用框架的 Controller → Service → Repository 三层即可，甚至 Controller 直接调 Repository 都行。\n\n但即使 CRUD 项目，也应该遵守一条底线：依赖方向向内。Service 不应该直接依赖具体的 ORM 类，而是通过接口访问数据。这样即使不上完整整洁架构，也保留了可测试和可替换的能力。\n\n务实的裁剪策略：\n\n1. 合并 Entities 和 Use Cases 层：\n如果业务实体没有复杂的内置规则，可以把实体和用例合并成一层「领域层」。不强制分出独立的 Entities 层。大部分中等复杂度的项目，两层（领域 + 基础设施）足够。\n\n2. 省略 Presenter，直接在 Controller 返回：\n整洁架构理论上有独立的 Presenter 做响应格式化。但如果响应格式简单（如标准 JSON），Controller 直接返回即可，不需要单独的 Presenter 类。等响应格式变复杂时再提取。\n\n3. 用框架的内置能力替代手写层：\n如用 NestJS 的依赖注入、TypeORM 的 Repository 模式，替代手写的 DI 和 Repository。不必什么都手写——框架提供的结构化能力如果能满足依赖方向向内的要求，直接用。\n\n4. 按需引入，而非全量套用：\n核心模块（业务规则复杂、需要保护）用完整整洁架构。外围模块（简单 CRUD）用轻量分层。同一个系统可以不同模块不同深度——不必全局统一。\n\n5. 先简单后演化：\n项目初期用简单的三层（Controller → Service → Repository），但保持依赖方向向内（Repository 接口在 Service 层定义）。等业务规则变复杂、外部集成增多时，再逐步拆出 Use Cases 层、Entities 层。演化式引入比一步到位更务实。\n\n6. 只保「依赖向内」这一条铁律：\n如果只做一件事，就是保证依赖方向向内——核心不依赖外围。即使不严格分四层，只要依赖方向是对的，核心就安全。分多少层、用什么命名，都是次要的。\n\n核心判断标准：整洁架构的成本是「模板代码和间接层」，收益是「核心保护、可测试、可替换」。只有当收益（由项目复杂度和生命周期决定）大于成本时才全量用。简单项目用简单结构，复杂项目才需要完整的保护层。架构是为业务服务的，不是业务为架构服务的。",
    tags: ["综合", "整洁架构", "太重", "CRUD", "裁剪", "务实", "演化式引入"],
  },
];
