import type { ReviewQuestion } from "./types";

/** CQRS 与事件溯源复习题 */
export const addCqrsEventSourcingQuestions: ReviewQuestion[] = [
  {
    id: "add-cqrs-event-sourcing-01",
    chapter: "add-cqrs-event-sourcing",
    level: 1,
    question: "什么是 CQRS？Command（命令）和 Query（查询）分别是什么？为什么要分离它们？",
    answer:
      "CQRS（Command Query Responsibility Segregation，命令查询职责分离）是一种架构模式，核心思想是「把写操作（Command）和读操作（Query）分离到不同的模型中」。\n\nCommand（命令）：\n- 表示「要做某件事」的意图——创建订单、修改地址、取消订阅。\n- 有副作用——会改变系统状态。\n- 通常返回 void 或操作结果（成功/失败），不返回业务数据。\n- 命名通常是动词：`CreateOrder`、`ConfirmPayment`、`CancelSubscription`。\n- 命令需要经过领域模型（聚合根）执行业务规则，确保不变量成立。\n\nQuery（查询）：\n- 表示「想知道某件事」的意图——查订单列表、查用户详情、统计销售额。\n- 无副作用——不改变系统状态。\n- 返回业务数据（DTO/视图模型），不执行业务规则。\n- 命名通常是名词或疑问句：`GetOrderList`、`FindUserById`、`CountActiveUsers`。\n- 查询不经过领域模型，直接从读模型（可能是物化视图、缓存、搜索引擎）取数据。\n\n为什么要分离 Command 和 Query：\n\n1. 读写需求不对称：\n在大多数业务系统中，读远多于写（如电商浏览量是下单量的 100 倍）。读和写的需求完全不同：\n- 读需要：多表 JOIN、聚合统计、按各种条件搜索、分页、高并发。读模型通常需要扁平化的宽表来优化查询性能。\n- 写需要：严格的业务规则校验、事务一致性、聚合不变量保护。写模型需要规范化的领域模型来保证正确性。\n如果读写共用一个模型，要么读性能差（领域模型不擅长复杂查询），要么写规则被绕过（为查询优化而扁平化的模型不适合承载业务规则）。\n\n2. 可独立优化：\n分离后，读模型可以针对查询优化（如建宽表、用 Elasticsearch、用 Redis 缓存），写模型可以针对业务规则优化（聚合、领域事件）。两者互不影响。\n\n3. 可独立伸缩：\n读多写少的系统，可以给读模型部署更多副本，写模型只需少量实例。\n\n4. 安全性：\n查询不经过领域模型，天然无法修改状态——即使查询接口有 bug，也不会破坏数据一致性。",
    tags: ["CQRS", "Command", "Query", "读写分离", "定义"],
  },
  {
    id: "add-cqrs-event-sourcing-02",
    chapter: "add-cqrs-event-sourcing",
    level: 2,
    question: "CQRS 的读写模型分离后，数据是怎么同步的？什么是「最终一致性」？这种方式有什么风险？",
    answer:
      "CQRS 读写模型分离后，数据同步流程：\n\n1. 写入流程（Command 侧）：\n- 客户端发送 Command（如 `CreateOrder`）到 Command 处理器。\n- Command 处理器加载聚合根（通过 Repository），调用聚合根的方法执行业务规则。\n- 聚合根执行成功后，状态变更 + 产生领域事件（如 `OrderCreated`）。\n- 聚合根通过 Repository 保存到写数据库（规范化模型）。\n- 领域事件被发布到消息总线（如 Kafka、RabbitMQ）。\n\n2. 同步流程（事件驱动）：\n- 读模型（查询侧）订阅领域事件。\n- 收到事件后，更新读数据库（如把 `OrderCreated` 事件翻译成一行宽表记录，写入 Elasticsearch 或 Redis 或物化视图）。\n- 这个同步是异步的——事件发布后，读模型的更新有延迟。\n\n3. 读取流程（Query 侧）：\n- 客户端发送 Query（如 `GetOrderList`）到 Query 处理器。\n- Query 处理器直接从读数据库取数据（不经过领域模型）。\n- 返回扁平化的 DTO/视图模型。\n\n最终一致性（Eventual Consistency）：\n\n在 CQRS 中，写数据库和读数据库不是同步更新的。写入成功后，读数据库需要等事件传播 + 处理后才更新。在这个时间窗口内（通常毫秒到秒级），读数据库的数据是「旧的」——用户刚下了单，立刻查订单列表可能看不到新订单。\n\n这种「不要求立刻一致，但保证最终会一致」的特性就是最终一致性。与之相对的是强一致性（写入后立刻能读到最新值）。\n\n最终一致性的风险：\n\n1. 用户体验问题：\n用户执行操作后立刻查看结果，可能看到旧数据。如「下单后立刻看订单列表，新订单没出现」。用户会以为操作失败，重复提交。\n解法：前端做乐观更新（操作后立刻在前端模拟更新，不等服务端确认），或提示「处理中，请稍后刷新」。\n\n2. 业务逻辑依赖读数据：\n如果某个业务流程依赖「写完立刻读」——如「下单后立刻查库存判断是否超卖」，最终一致性会导致读到旧库存，做出错误判断。\n解法：这种逻辑不应该走读模型，而应该在写模型（聚合根）内部处理——下单时直接在聚合根里检查库存不变量，而不是查读模型。\n\n3. 事件丢失或乱序：\n如果消息总线不可靠，事件可能丢失（读模型永久不更新）或乱序（读模型状态错乱）。\n解法：使用可靠的消息基础设施（如 Kafka 的 exactly-once 语义），或在事件中携带版本号/时间戳保证顺序。\n\n4. 事件处理失败：\n读模型消费事件时可能出错（如读数据库暂时不可用）。如果错误处理不当，读模型会永久落后。\n解法：消息总线支持重试和死信队列；读模型消费失败的事件进入重试队列，而非丢弃。\n\n5. 调试困难：\n数据流向是「写库 → 事件 → 读库」，链路长，出问题时排查困难——是写库没写对？是事件没发出来？是读模型消费失败？\n解法：完善的链路追踪和日志，每个环节可观测。\n\n何时可以接受最终一致性：\n- 读写不在同一用户操作的关键路径上（如用户下单后不需要立刻查到）。\n- 业务允许短暂的数据延迟（如报表、搜索索引）。\n- 系统规模大到强一致性的代价不可接受（如高并发电商）。\n\n何时不能接受：\n- 金融交易（必须强一致，不能允许余额短暂不一致）。\n- 业务逻辑依赖「写完立刻读」。\n- 延迟敏感的实时系统。",
    tags: ["CQRS", "数据同步", "最终一致性", "领域事件", "风险", "Eventual Consistency"],
  },
  {
    id: "add-cqrs-event-sourcing-03",
    chapter: "add-cqrs-event-sourcing",
    level: 3,
    question: "什么是事件溯源（Event Sourcing）？它与传统的「只存当前状态」有什么区别？事件溯源有什么优势和代价？",
    answer:
      "事件溯源（Event Sourcing, ES）：\n事件溯源是一种持久化模式——不存储对象的当前状态，而是存储「导致状态变化的所有事件」。对象的当前状态通过「回放所有历史事件」来重建。\n\n传统方式 vs 事件溯源：\n\n传统方式（只存当前状态）：\n```\n数据库表 orders:\n  id: ORDER-001\n  status: confirmed\n  totalAmount: 500\n  confirmedAt: 2024-01-15 10:30\n```\n只存最终状态。不知道这个订单经历了什么——什么时候创建的？什么时候支付的？什么时候确认的？改过几次地址？\n\n事件溯源（存所有事件）：\n```\n事件流（按顺序存储）:\n  1. OrderCreated     { orderId: ORDER-001, items: [...], totalAmount: 500, createdAt: 2024-01-15 10:00 }\n  2. PaymentCompleted { orderId: ORDER-001, paymentId: PAY-001, paidAt: 2024-01-15 10:15 }\n  3. OrderConfirmed   { orderId: ORDER-001, confirmedAt: 2024-01-15 10:30 }\n```\n不存当前状态。当前状态通过回放事件重建：\n```typescript\nfunction replayOrder(events: Event[]): Order {\n  let order = new Order();  // 空订单\n  for (const event of events) {\n    order.apply(event);  // 逐个应用事件，更新状态\n  }\n  return order;  // 最终状态\n}\n```\n\n事件溯源的优势：\n\n1. 完整审计日志：所有状态变化都有记录——谁在什么时候做了什么操作。天然满足审计需求。传统方式要实现审计需要额外建审计表。\n\n2. 时间旅行：可以重建任意时间点的状态——「上周三下午 3 点这个订单是什么状态？」只需回放到那个时间点的事件。\n\n3. 事件驱动的自然集成：事件溯源天然产生领域事件，与 CQRS、消息驱动架构无缝集成。不需要额外「从状态变更中推导事件」。\n\n4. 模型与存储对齐：领域模型本来就在用事件表达状态变化（聚合根方法产生领域事件），事件溯源让持久化层也存事件，模型与存储天然对齐。\n\n5. 不可变性：事件是不可变的——已经发生的事实不可篡改。存储层只有追加操作（append-only），没有更新和删除，并发冲突少。\n\n6. 业务洞察：事件流是业务过程的完整记录，可以用于数据分析、用户行为挖掘、bug 复现。\n\n事件溯源的代价：\n\n1. 查询困难：事件流不擅长查询当前状态——「查所有已确认订单」需要回放所有订单的所有事件来重建状态，极其低效。必须配合 CQRS + 读模型来支持查询。\n\n2. 事件版本演化：业务变化导致事件结构变化（如 `OrderCreated` 加了一个字段）。旧事件的格式和新代码不兼容。需要事件版本管理（如 upcast——把旧版本事件转换成新版本）和迁移策略。\n\n3. 回放性能：随着事件累积，回放一个聚合的所有事件越来越慢。需要定期做快照（Snapshot）——每隔 N 个事件存一个当前状态的快照，回放时从最近的快照开始。\n\n4. 模型设计难度高：事件设计需要预见性——事件一旦发布就不能改（不可变性）。如果事件粒度设计不当（太粗或太细），后期调整困难。\n\n5. 删除与隐私：事件不可删，但 GDPR 等法规要求「被遗忘权」——用户要求删除个人数据。事件溯源下需要复杂的「加密遗忘」或「匿名化」策略。\n\n6. 复杂度高：引入事件存储、快照、版本管理、CQRS 读模型同步等一系列基础设施。团队需要较高的技术成熟度。\n\n适用场景：\n- 需要完整审计日志的领域（金融、医疗、法律）。\n- 业务逻辑本身就是事件驱动的（如订单状态流转）。\n- 需要时间旅行或历史状态重建。\n- 团队有足够的技术能力驾驭复杂度。\n\n不适用场景：\n- 简单 CRUD 无复杂业务逻辑。\n- 查询需求远多于写入，且查询模式复杂多变。\n- 团队规模小、技术成熟度不足。\n- 隐私法规要求严格的数据删除。",
    tags: ["事件溯源", "Event Sourcing", "当前状态", "审计日志", "时间旅行", "优势代价"],
  },
  {
    id: "add-cqrs-event-sourcing-04",
    chapter: "add-cqrs-event-sourcing",
    level: 4,
    question: "CQRS 和事件溯源经常被一起提到，但它们是同一回事吗？它们各自解决什么问题？在什么情况下该用 CQRS 而不用事件溯源？什么情况下两者一起用？",
    answer:
      "CQRS 和事件溯源（ES）不是同一回事，它们解决不同层面的问题，可以独立使用，也可以组合使用。\n\n各自解决的问题：\n\nCQRS 解决的是「读写需求不对称」的问题：\n- 读和写有不同的模型需求——写需要领域模型保证业务规则，读需要扁平化视图保证查询性能。\n- CQRS 把读写模型分离，各自优化。写模型用聚合/领域模型，读模型用宽表/搜索/缓存。\n- CQRS 不关心写模型怎么存数据——可以存当前状态（传统数据库），也可以存事件（事件溯源）。\n\n事件溯源解决的是「怎么存数据」的问题：\n- 不存当前状态，存导致状态变化的事件流。\n- 通过回放事件重建当前状态。\n- 事件溯源不关心读模型怎么建——可以共用写模型查询（虽然很慢），也可以配合 CQRS 建独立读模型。\n\n两者的关系矩阵：\n\n1. 不用 CQRS，不用 ES（传统架构）：\n- 读写共用一个模型，存当前状态。最简单，适合大部分中小项目。\n\n2. 用 CQRS，不用 ES：\n- 读写模型分离。写模型存当前状态（传统数据库 + Repository）。读模型存扁平化视图。\n- 写入后通过领域事件同步读模型。写数据库和读数据库各自独立。\n- 适合：读写需求差异大、需要独立伸缩读写、但不需要完整事件审计的项目。\n\n3. 不用 CQRS，用 ES（罕见）：\n- 存事件流，但读写共用一个模型。查询时回放事件重建状态。\n- 几乎不可行——回放事件查询性能极差。这种组合基本不存在于实践中。\n\n4. 用 CQRS + ES（完整组合）：\n- 写模型存事件流（事件溯源），读模型存扁平化视图（CQRS）。\n- 写入时产生事件，事件既用于持久化（ES），又用于同步读模型（CQRS）。\n- 最强大的组合——完整审计 + 高性能查询 + 事件驱动。\n- 也是最复杂的组合——需要事件存储、事件版本管理、快照、读模型同步、最终一致性处理。\n\n什么时候用 CQRS 而不用 ES：\n\n这是最常见的 CQRS 落地方式。条件：\n1. 读写需求确实不对称——读多写少、查询复杂、需要独立伸缩读写。\n2. 不需要完整事件审计——传统数据库的更新日志/审计表已经够用。\n3. 团队不想承担事件溯源的复杂度（版本管理、快照、回放）。\n4. 写模型用传统数据库存当前状态就够好——Repository 保存聚合的当前状态，领域事件只用于同步读模型（不作为主存储）。\n\n实现方式：\n- 写侧：聚合根 → Repository → 写数据库（存当前状态）。\n- 同步：聚合根产生领域事件 → 发布到消息总线 → 读模型消费事件更新读数据库。\n- 读侧：Query 处理器 → 读数据库（Elasticsearch/Redis/物化视图）。\n\n好处：读写分离的收益（性能、伸缩性）拿到了，但避免了事件溯源的复杂度。这是务实的 CQRS。\n\n什么时候 CQRS + ES 一起用：\n\n条件：\n1. 需要完整审计日志——金融、医疗等法规要求记录所有状态变化。\n2. 业务逻辑天然是事件流——如订单状态机、账户交易流水。\n3. 需要时间旅行——重建历史状态、回溯分析。\n4. 团队有足够技术能力——能驾驭事件存储、版本管理、快照等复杂度。\n5. 系统规模和生命周期足够长——值得投入这些基础设施成本。\n\n实现方式：\n- 写侧：聚合根 → 事件存储（append-only 事件流）。当前状态通过回放事件 + 快照重建。\n- 同步：事件本身就是存储——事件存储发布事件到消息总线，读模型消费更新读数据库。\n- 读侧：同 CQRS。\n\n实际决策框架：\n\n1. 先问「读写需求是否不对称」：\n- 不对称（读多写少、查询复杂）→ 考虑 CQRS。\n- 对称（读写差不多、查询简单）→ 传统架构，不用 CQRS。\n\n2. 再问「是否需要完整事件审计」：\n- 需要 → 考虑加事件溯源（CQRS + ES）。\n- 不需要 → CQRS 不用 ES（写模型存当前状态）。\n\n3. 最后问「团队和项目能否承担复杂度」：\n- CQRS 不用 ES：中等复杂度，大部分有经验的团队能驾驭。\n- CQRS + ES：高复杂度，需要专门的 event store 基础设施和丰富的分布式系统经验。\n\n常见误区：\n- 把 CQRS 和 ES 混为一谈——以为 CQRS 就必须用事件溯源。实际上 CQRS 不用 ES 是更常见的落地方式。\n- 为了「先进」而用 CQRS + ES——简单项目不需要读写分离也不需要事件审计，硬套只会增加复杂度。\n- 低估最终一致性的影响——CQRS（无论是否 ES）都引入最终一致性，必须评估业务能否接受。\n\n一句话：CQRS 是读写分离，ES 是用事件存状态。CQRS 不用 ES 是务实之选，CQRS + ES 是重器，只在确有需要且有足够能力时才用。先问需不需要 CQRS，再问需不需要 ES，不要捆绑决策。",
    tags: ["综合", "CQRS", "事件溯源", "Event Sourcing", "关系", "决策框架", "权衡"],
  },
];
