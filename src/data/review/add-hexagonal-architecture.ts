import type { ReviewQuestion } from "./types";

/** 六边形架构复习题 */
export const addHexagonalArchitectureQuestions: ReviewQuestion[] = [
  {
    id: "add-hexagonal-architecture-01",
    chapter: "add-hexagonal-architecture",
    level: 1,
    question: `什么是六边形架构（Hexagonal Architecture）？它的核心思想是什么？`,
    answer:
      `六边形架构（Hexagonal Architecture），又称端口与适配器架构（Ports and Adapters），由 Alistair Cockburn 于 2005 年提出。核心思想是「让应用程序的核心逻辑与外部系统（数据库、UI、消息队列、第三方 API）完全解耦」。\n\n核心思想：\n\n1. 应用程序分为「内部」（核心）和「外部」（基础设施）：\n- 内部 = 领域模型 + 应用逻辑（业务规则）。这是系统的核心，不依赖任何外部技术。\n- 外部 = 数据库、Web 框架、消息队列、第三方 API、UI 等。这些是「外围」，通过适配器连接到内部。\n\n2. 端口（Port）：\n端口是内部定义的接口，描述了「内部需要什么能力」或「外部可以怎么调用内部」。\n- 驱动端口（Driving Port）：外部调用内部用的接口。如 \`OrderService.placeOrder()\` ——外部（如 Controller）通过这个端口调用内部逻辑。\n- 被动端口（Driven Port）：内部调用外部用的接口。如 \`OrderRepository.save()\` ——内部需要持久化能力，定义这个端口，由外部适配器实现。\n\n3. 适配器（Adapter）：\n适配器是外部技术的具体实现，连接外部系统和内部端口。\n- 驱动适配器（Driving Adapter）：把外部请求翻译成对内部端口的调用。如 HTTP Controller 是驱动适配器——接收 HTTP 请求，调用 \`OrderService.placeOrder()\`。\n- 被动适配器（Driven Adapter）：实现内部的被动端口。如 MySQL Repository 是被动适配器——实现 \`OrderRepository\` 接口，把内部的对象操作翻译成 MySQL SQL。\n\n4. 依赖方向：\n- 外部适配器依赖内部端口，内部端口不依赖外部适配器。\n- 内部核心不依赖任何外部技术。\n- 换数据库 = 换一个被动适配器；换 Web 框架 = 换一个驱动适配器。核心不变。\n\n为什么叫「六边形」：\nCockburn 用六边形来表示这个架构——六边形的中心是应用核心，六边形的每条边是一个端口，边外是适配器。六边形不是固定六条边——边的数量取决于有多少种外部交互。用六边形只是因为它比圆形更容易画多条边，象征「核心可以从多个方向被多种适配器接入」。\n\n一句话：六边形架构 = 端口定义接口 + 适配器实现技术细节 + 核心不依赖外部。核心是「可替换性」——外部技术是可拔插的适配器，核心是稳定的端口和逻辑。`,
    tags: ["六边形架构", "Hexagonal Architecture", "端口与适配器", "Ports and Adapters", "定义"],
  },
  {
    id: "add-hexagonal-architecture-02",
    chapter: "add-hexagonal-architecture",
    level: 2,
    question: `六边形架构中的「驱动端口」和「被动端口」有什么区别？「驱动适配器」和「被动适配器」呢？各举一个例子。`,
    answer:
      `驱动端口 vs 被动端口：\n\n驱动端口（Driving Port / Primary Port / Inbound Port）：\n- 定义「外部怎么调用内部」——是内部暴露给外部的 API。\n- 外部通过驱动端口「驱动」内部执行业务逻辑。\n- 通常对应应用服务（Application Service）接口。\n- 例子：\`OrderUseCase\` 接口，定义了 \`placeOrder(input)\`、\`confirmOrder(orderId)\`、\`cancelOrder(orderId)\` 等方法。外部（Controller、CLI、消息消费者）通过这个端口驱动内部执行订单操作。\n\n被动端口（Driven Port / Secondary Port / Outbound Port）：\n- 定义「内部需要外部提供什么能力」——是内部对外部的依赖声明。\n- 内部通过被动端口「被驱动」——调用外部资源来完成业务。\n- 通常对应 Repository、Gateway、通知服务等接口。\n- 例子：\`OrderRepository\` 接口，定义了 \`findById(id)\`、\`save(order)\` 等方法。内部需要持久化能力，定义这个端口，由外部适配器实现。\n\n核心区别：\n- 驱动端口 = 外部→内部（外部主动调用内部）。\n- 被动端口 = 内部→外部（内部主动调用外部）。\n- 驱动端口是内部「暴露」的接口，被动端口是内部「需要」的接口。\n\n驱动适配器 vs 被动适配器：\n\n驱动适配器（Driving Adapter / Primary Adapter / Inbound Adapter）：\n- 实现外部技术到驱动端口的翻译。\n- 把外部请求格式翻译成对驱动端口的调用。\n- 例子：\n  - HTTP Controller：接收 HTTP POST 请求，解析 body，调用 \`OrderUseCase.placeOrder(input)\`。\n  - CLI 命令：接收命令行参数，调用 \`OrderUseCase.placeOrder(input)\`。\n  - 消息消费者：消费 Kafka 消息，调用 \`OrderUseCase.confirmOrder(orderId)\`。\n  - 同一个驱动端口可以被多个驱动适配器调用——HTTP、CLI、消息都能触发同一个 \`placeOrder\`。\n\n被动适配器（Driven Adapter / Secondary Adapter / Outbound Adapter）：\n- 实现被动端口——把内部的接口调用翻译成外部技术的操作。\n- 例子：\n  - MySQL Repository：实现 \`OrderRepository\` 接口，\`save(order)\` 方法把 Order 对象翻译成 MySQL INSERT 语句。\n  - MongoDB Repository：也实现 \`OrderRepository\` 接口，但 \`save(order)\` 方法把 Order 对象翻译成 MongoDB 文档操作。\n  - 内存 Repository：也实现 \`OrderRepository\` 接口，\`save(order)\` 方法把 Order 存到内存数组。\n  - 同一个被动端口可以有多个被动适配器——换数据库只换适配器，核心不变。\n\n完整示例——订单系统的端口与适配器：\n\n\`\`\`\n                         [驱动适配器]              [驱动端口]                    [被动端口]           [被动适配器]\nHTTP Controller ──→  OrderUseCase.placeOrder()  ──→  Order(聚合根)  ──→  OrderRepository.save()  ←── MySQL Repository\nCLI Command     ──→         ↑                          ↑                   ↑                    ←── MongoDB Repository\nKafka Consumer  ──→    （内部核心不依赖外部）           │                   └─→  EmailSender.send()  ←── SMTP Adapter\n                                                     └─→  PaymentGateway.charge()  ←── Stripe Adapter\n\`\`\`\n\n关键点：\n- 驱动适配器（HTTP/CLI/Kafka）从外部「推」进来，通过驱动端口调用核心。\n- 被动适配器（MySQL/SMTP/Stripe）从外部「被拉」进来，实现被动端口供核心调用。\n- 核心只知道端口接口，不知道适配器的存在。\n- 测试时：驱动适配器用测试替身触发（如直接调 UseCase）；被动适配器用 Mock 实现（如 InMemoryRepository）。`,
    tags: ["驱动端口", "被动端口", "驱动适配器", "被动适配器", "六边形架构", "区别"],
  },
  {
    id: "add-hexagonal-architecture-03",
    chapter: "add-hexagonal-architecture",
    level: 3,
    question: `六边形架构如何提升可测试性？请用测试场景说明：如何在不连数据库的情况下测试核心业务逻辑？`,
    answer:
      `六边形架构的可测试性源于「核心不依赖外部」——所有外部依赖都通过被动端口注入，测试时用 Mock/替身替换真实适配器。\n\n测试场景：测试「下单」业务逻辑，不连数据库、不连支付网关、不发邮件。\n\n系统结构：\n\`\`\`\n[HTTP Controller] → [OrderUseCase（驱动端口）] → [Order 聚合根] → [OrderRepository（被动端口）]\n                                                        ↓        → [PaymentGateway（被动端口）]\n                                                        ↓        → [EmailSender（被动端口）]\n\`\`\`\n\n被测对象：OrderUseCase（应用服务，编排业务逻辑）\n\n真实环境依赖：\n- OrderRepository → MySQLRepository（连 MySQL）\n- PaymentGateway → StripeGateway（连 Stripe API）\n- EmailSender → SMTPEmailSender（连 SMTP 服务器）\n\n测试环境：用替身替换所有被动端口的实现：\n\`\`\`typescript\n// 1. 创建测试替身\nconst inMemoryOrderRepo = new InMemoryOrderRepository();  // 内存替身，不连数据库\nconst mockPaymentGateway = new MockPaymentGateway();        // Mock 替身，模拟支付成功\nconst mockEmailSender = new MockEmailSender();              // Mock 替身，记录调用但不发邮件\n\n// 2. 注入替身到被测对象\nconst orderUseCase = new OrderUseCase(\n  inMemoryOrderRepo,    // 注入内存 Repository\n  mockPaymentGateway,    // 注入 Mock 支付\n  mockEmailSender,       // 注入 Mock 邮件\n);\n\n// 3. 执行测试\ntest('下单成功：创建订单、扣款、发邮件', async () => {\n  // 执行下单\n  const result = await orderUseCase.placeOrder({\n    customerId: 'CUST-001',\n    items: [{ productId: 'PROD-001', quantity: 2 }],\n  });\n\n  // 验证：订单已保存（内存替身中有记录）\n  const savedOrder = await inMemoryOrderRepo.findById(result.orderId);\n  expect(savedOrder).toBeDefined();\n  expect(savedOrder.status).toBe('pending');\n\n  // 验证：支付网关被调用\n  expect(mockPaymentGateway.chargeCalls).toHaveLength(1);\n  expect(mockPaymentGateway.chargeCalls[0].amount).toBe(500);\n\n  // 验证：邮件被发送（Mock 记录了调用）\n  expect(mockEmailSender.sentEmails).toHaveLength(1);\n  expect(mockEmailSender.sentEmails[0].subject).toContain('订单确认');\n});\n\ntest('支付失败时不创建订单', async () => {\n  // 让 Mock 支付返回失败\n  mockPaymentGateway.shouldFail = true;\n\n  // 执行下单，应抛异常\n  await expect(orderUseCase.placeOrder({\n    customerId: 'CUST-001',\n    items: [{ productId: 'PROD-001', quantity: 2 }],\n  })).rejects.toThrow('支付失败');\n\n  // 验证：订单没有保存\n  const orders = await inMemoryOrderRepo.findAll();\n  expect(orders).toHaveLength(0);\n\n  // 验证：邮件没有发送\n  expect(mockEmailSender.sentEmails).toHaveLength(0);\n});\n\`\`\`\n\n六边形架构为什么能这样测试：\n\n1. 核心只依赖接口（端口），不依赖具体实现：\nOrderUseCase 依赖 \`OrderRepository\`（接口），不是 \`MySQLRepository\`（实现）。测试时注入 \`InMemoryOrderRepository\`（另一个实现），OrderUseCase 完全不知道差异。\n\n2. 端口由核心定义，适配器可替换：\n端口是核心声明的「我需要什么」，适配器是「怎么提供」。只要适配器满足端口接口，核心就能工作。生产用 MySQL 适配器，测试用内存适配器。\n\n3. 不需要启动外部系统：\n内存替身和 Mock 完全在进程内运行，不需要启动 MySQL、Stripe、SMTP 服务器。测试速度快（毫秒级）、稳定（不受网络/外部服务影响）、可并行（无共享资源竞争）。\n\n4. 可测试各种边界场景：\n- 支付失败 → Mock 支付返回失败，测试核心的错误处理。\n- 数据库超时 → Mock Repository 抛超时异常，测试核心的容错。\n- 并发冲突 → Mock Repository 模拟并发冲突，测试核心的重试逻辑。\n这些场景在真实环境中很难触发，用 Mock 可以精确控制。\n\n对比传统架构的测试困境：\n传统架构中，业务逻辑直接依赖数据库/外部服务。测试业务逻辑必须：\n- 连真实数据库（慢、不稳定）。\n- 或用复杂的 Mock 框架在方法内部 Mock（侵入性强、维护难）。\n- 业务逻辑和外部依赖纠缠在一起，难以隔离测试。\n\n六边形架构通过端口适配器分离，让测试变得简单——核心逻辑可以完全在隔离环境中测试。这也是六边形架构最大的实战价值之一。`,
    tags: ["六边形架构", "可测试性", "Mock", "测试替身", "端口适配器", "单元测试"],
  },
  {
    id: "add-hexagonal-architecture-04",
    chapter: "add-hexagonal-architecture",
    level: 4,
    question: `六边形架构和整洁架构（Clean Architecture）是什么关系？它们的核心思想相同吗？在实际项目中应该选哪个？`,
    answer:
      `六边形架构和整洁架构的核心思想高度一致——都是「依赖方向向内、核心不依赖外围、通过接口隔离外部技术」。它们是同一思想的不同表达，而非竞争关系。\n\n核心思想的共同点：\n\n1. 依赖方向向内：\n- 六边形：适配器依赖端口，端口在核心内部定义。外部依赖内部。\n- 整洁架构：外层依赖内层，内层不依赖外层。Frameworks 层依赖 Use Cases 层定义的接口。\n- 本质相同：核心定义接口，外围实现接口。\n\n2. 核心不依赖外部技术：\n- 六边形：核心只包含领域模型和应用逻辑，不 import 数据库/框架代码。\n- 整洁架构：Entities + Use Cases 不依赖 Frameworks 层的任何东西。\n- 本质相同：业务核心与技术细节解耦。\n\n3. 可替换性：\n- 六边形：换数据库 = 换被动适配器。\n- 整洁架构：换数据库 = 换 Frameworks 层的 Repository 实现。\n- 本质相同：外部技术是可拔插的。\n\n4. 可测试性：\n- 六边形：注入 Mock 适配器测试核心。\n- 整洁架构：注入 Mock Repository 测试 Use Cases。\n- 本质相同：核心可隔离测试。\n\n两者的差异（表达层面，非思想层面）：\n\n1. 概念命名不同：\n- 六边形：端口（Port）+ 适配器（Adapter）。\n- 整洁架构：接口（Interface）+ 层（Layer）。\n- 端口就是整洁架构中内层定义的接口；适配器就是整洁架构中外层的实现。\n\n2. 结构表达不同：\n- 六边形：用「内部 + 外部」两分法。核心是一个整体，外部是多个适配器。\n- 整洁架构：用「同心圆四层」细分。核心分为 Entities 和 Use Cases，外围分为 Interface Adapters 和 Frameworks。\n- 整洁架构比六边形多了一层「内部细分」——把核心拆成 Entities（实体）和 Use Cases（用例）两层。\n\n3. 侧重点不同：\n- 六边形：强调「对称性」——应用可以从多个方向（HTTP、CLI、消息、定时任务）被驱动，也可以调用多种外部系统（数据库、邮件、API）。强调核心与所有外部系统的对等隔离。\n- 整洁架构：强调「层次性」——按抽象级别从内到外排列，强调哪层更重要、哪层是细节。\n\n4. 历史关系：\n- 六边形架构（2005）更早提出。它确立了「端口+适配器」的核心思想。\n- 整洁架构（2012）是 Uncle Bob 在六边形架构等前辈思想基础上的综合和精化。整洁架构的同心圆图比六边形的图更具操作性（明确分了四层）。\n- 可以说整洁架构是六边形架构的「演进版本」——继承了核心思想，增加了层次细分。\n\n概念映射表：\n| 六边形架构      | 整洁架构                |\n|---------------|------------------------|\n| 内部核心        | Entities + Use Cases    |\n| 驱动端口        | Use Cases 接口          |\n| 被动端口        | Use Cases 定义的 Repository 接口 |\n| 驱动适配器      | Interface Adapters 层的 Controller |\n| 被动适配器      | Frameworks 层的 Repository 实现   |\n| 外部           | Frameworks & Drivers 层  |\n\n实际项目中应该选哪个：\n\n1. 它们不是「二选一」的关系：\n两者思想一致，落地时不需要纠结「用六边形还是整洁架构」。实际项目中，它们通常融合使用——用整洁架构的四层结构组织代码，同时遵循六边形架构的端口+适配器原则。\n\n2. 选择建议：\n- 如果团队偏好清晰的层次结构：用整洁架构。四层同心圆给出了明确的代码组织方式——哪层放什么、依赖方向怎么走。更适合作为团队规范。\n- 如果团队偏好灵活的端口适配器思维：用六边形架构。不强制分四层，只要求「核心+端口+适配器」。更灵活，适合项目结构不需要那么严格的场景。\n- 实际融合：大部分成熟的 DDD 项目同时体现了两者——代码按整洁架构分层（Entities / Use Cases / Adapters / Frameworks），但讨论接口设计时用六边形的端口/适配器语言（「这是一个驱动端口」「这是被动适配器」）。\n\n3. 不要纠结形式，抓住本质：\n无论叫六边形还是整洁架构，核心都是三条铁律：\n- 依赖方向向内（核心不依赖外围）。\n- 核心通过接口与外部通信（端口/接口由核心定义）。\n- 外部技术是可替换的适配器（换数据库/框架只换适配器，核心不变）。\n满足这三条，用哪种命名、画什么图，都是次要的。\n\n4. 和 DDD 的关系：\n六边形/整洁架构提供「技术骨架」，DDD 提供「业务血肉」。\n- 六边形/整洁架构的「核心」= DDD 的领域模型（聚合、实体、值对象）+ 应用服务（用例）。\n- 六边形/整洁架构的「被动端口」= DDD 的 Repository 接口。\n- 六边形/整洁架构的「被动适配器」= DDD 的 Repository 实现 + 防腐层。\n- 两者天然配合——DDD 建模业务，六边形/整洁架构安放模型。\n\n一句话：六边形架构和整洁架构是同一思想的两种表达——前者强调端口适配器的对称性，后者强调层次的抽象级别。实际项目不需要二选一，抓住「依赖向内、核心独立、外部可替换」的本质即可。它们与 DDD 配合使用，构成「架构骨架 + 业务血肉」的完整方案。`,
    tags: ["综合", "六边形架构", "整洁架构", "关系", "对比", "DDD", "选择"],
  },
];
