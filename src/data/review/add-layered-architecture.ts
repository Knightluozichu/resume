import type { ReviewQuestion } from "./types";

/** 分层架构复习题 */
export const addLayeredArchitectureQuestions: ReviewQuestion[] = [
  {
    id: "add-layered-architecture-01",
    chapter: "add-layered-architecture",
    level: 1,
    question: `传统三层架构（三层架构）由哪三层组成？每层的职责是什么？`,
    answer:
      `传统三层架构（Three-Tier Architecture）由以下三层组成，依赖方向从上到下：\n\n1. 表现层（Presentation Layer / UI 层）：\n负责与用户交互——接收用户输入、展示数据给用户。包含控制器（Controller）、视图（View）、API 端点等。它不包含业务逻辑，只负责「把请求翻译成对业务层的调用，把业务层的结果翻译成响应」。\n\n2. 业务逻辑层（Business Logic Layer / Domain Layer / Service 层）：\n负责系统的核心业务规则——订单怎么计算价格、用户怎么注册、库存怎么扣减。它是系统的「大脑」，所有业务规则集中于此。表现层调用业务层，业务层调用数据层。\n\n3. 数据访问层（Data Access Layer / Persistence Layer / DAO 层）：\n负责数据的持久化——增删改查数据库。包含 Repository、DAO、ORM 映射等。它不包含业务逻辑，只负责「把业务对象存进数据库、从数据库取出业务对象」。\n\n依赖方向（传统）：表现层 → 业务逻辑层 → 数据访问层 → 数据库。上层依赖下层，调用方向从上到下。\n\n这是最经典的分层模式，几乎所有企业应用都从三层架构起步。它的价值在于「关注点分离」——UI、业务、数据各管各的，改一层不影响其他层。`,
    tags: ["分层架构", "三层架构", "表现层", "业务逻辑层", "数据访问层", "定义"],
  },
  {
    id: "add-layered-architecture-02",
    chapter: "add-layered-architecture",
    level: 2,
    question: `传统分层架构有什么问题？「业务逻辑层依赖数据访问层」会导致什么后果？`,
    answer:
      `传统分层架构虽然实现了基本的关注点分离，但存在几个结构性问题：\n\n1. 业务逻辑层依赖数据访问层（依赖方向向下）：\n这是最核心的问题。业务逻辑层（核心）依赖数据访问层（技术细节），导致：\n- 业务逻辑无法独立测试——测试业务逻辑必须连真实数据库或 Mock 数据访问层。\n- 数据访问层的技术变更（换数据库、换 ORM）会波及业务逻辑层。\n- 业务逻辑层「知道」数据怎么存的，容易把数据访问逻辑（如 SQL 拼接、事务管理）泄漏到业务层，导致职责模糊。\n\n2. 跨层依赖与「泄漏」：\n实际项目中，业务逻辑层经常直接操作数据访问层返回的数据传输对象（DTO），导致业务逻辑和数据结构耦合。表现层也可能直接绕过业务层调数据层（捷径），分层形同虚设。\n\n3. 数据库驱动设计：\n传统分层架构往往以数据库为中心——先设计数据库表，再写数据访问层，最后写业务逻辑。业务逻辑变成了对数据库表的增删改查的包装，真正的业务规则散落在各处，形成「贫血模型」——业务对象只有 getter/setter，没有行为。\n\n4. 业务逻辑分散：\n由于业务层依赖数据层，业务逻辑经常被「拉」到数据层（存储过程）或「推」到表现层（控制器里写业务规则），导致业务规则散落在三层各处，难以集中管理和维护。\n\n5. 分层刚性：\n严格的三层分层在某些场景下过于死板。如一个简单的 CRUD 操作，强制经过三层是多余的间接。而复杂的业务逻辑又可能需要跨越多层，三层结构表达力不足。\n\n后果总结：传统分层架构最大的问题是「依赖方向向下」——核心业务依赖技术细节。这违反了依赖倒置原则（DIP），使得系统核心被外围技术绑架，难以测试、难以替换、难以演化。这也正是整洁架构、六边形架构等后续架构模式要解决的核心问题——反转依赖方向，让核心不依赖外围。`,
    tags: ["分层架构", "问题", "依赖方向", "贫血模型", "数据库驱动", "DIP违反"],
  },
  {
    id: "add-layered-architecture-03",
    chapter: "add-layered-architecture",
    level: 3,
    question: `分层架构是如何演进的？从「严格分层」到「依赖反转的分层」解决了什么问题？用依赖方向的变化说明。`,
    answer:
      `分层架构的演进经历了几个阶段，核心变化是依赖方向的调整：\n\n阶段一：严格分层（Strict Layering）\n\n依赖方向：表现层 → 业务层 → 数据访问层 → 数据库\n规则：每层只能调用正下方的层，不能跨层调用。\n问题：业务层依赖数据访问层，核心被技术细节绑架。\n\n阶段二：松散分层（Relaxed Layering）\n\n依赖方向：同上，但允许跨层调用（如表现层可直接调数据访问层做简单查询）。\n动机：减少不必要的间接层，提高灵活性。\n问题：分层边界被破坏，职责泄漏加剧，系统结构更容易腐烂。\n\n阶段三：依赖反转的分层（Inverted Dependency Layering）\n\n核心改变：把依赖方向反转。\n\n重构前（传统）：\n\`\`\`\n表现层 → 业务层 → 数据访问层 → 数据库\n         （依赖↓）   （依赖↓）\n\`\`\`\n业务层依赖数据访问层（具体实现）。\n\n重构后（依赖反转）：\n\`\`\`\n表现层 → 业务层 ← 数据访问层 → 数据库\n         （依赖↓）   （依赖↑）\n                    接口在业务层定义\n\`\`\`\n\n具体做法：\n1. 在业务层定义 Repository 接口（抽象），声明业务需要的数据操作方法（如 \`findById\`、\`save\`）。\n2. 数据访问层实现这个接口（细节依赖抽象）。\n3. 业务层只依赖接口，不依赖具体实现。依赖注入在运行时把具体实现注入业务层。\n\n依赖方向变化：\n- 传统：业务层 → 数据访问层（向下依赖具体实现）\n- 反转后：数据访问层 → 业务层定义的接口（向上依赖抽象）\n- 业务层不再依赖数据访问层，而是数据访问层依赖业务层定义的接口。\n\n这个演进解决了什么问题：\n\n1. 核心独立：业务层不再依赖数据访问的具体实现，可以独立编译、独立测试。\n2. 可替换性：换数据库（MySQL → MongoDB）只需写新的 Repository 实现，业务层一行不改。\n3. 可测试性：测试业务层时注入内存 Repository 或 Mock，不依赖真实数据库。\n4. 业务优先设计：接口在业务层定义，意味着先想「业务需要什么数据操作」，再想「怎么从数据库取」，扭转了数据库驱动的设计思维。\n\n这个演进本质上是 DIP（依赖倒置原则）在架构层面的应用——把「高层依赖低层」反转为「低层依赖高层定义的抽象」。它也是整洁架构「依赖方向向内」的直接前身。`,
    tags: ["分层架构", "演进", "依赖反转", "DIP", "依赖方向", "可测试性"],
  },
  {
    id: "add-layered-architecture-04",
    chapter: "add-layered-architecture",
    level: 4,
    question: `在实际项目中，分层架构和微服务架构是什么关系？「先分层后拆服务」为什么是推荐的演进路径？什么时候该从分层单体走向微服务？`,
    answer:
      `分层架构与微服务架构不是对立的，而是不同维度的组织方式——分层是「纵向分层」（按技术职责分），微服务是「横向分块」（按业务边界分）。两者可以叠加：每个微服务内部仍然是分层架构。\n\n「先分层后拆服务」推荐的演进路径及理由：\n\n第一阶段：分层单体（Layered Monolith）\n- 系统内部按表现层、业务层、数据层分层，但部署为一个单体。\n- 依赖方向通过 DIP 反转，核心业务独立于基础设施。\n- 内部模块按业务领域划分（如订单模块、用户模块、库存模块），但共享同一数据库或同一进程。\n\n为什么先不分微服务：\n1. 早期业务边界不清晰：DDD 的限界上下文需要通过与业务深入沟通才能准确识别。项目初期对业务理解不深，贸然拆服务很容易拆错边界，后期合并/重拆成本极高。\n2. 分布式复杂度高：微服务引入网络通信、分布式事务、服务发现、配置管理、链路追踪等复杂度。早期团队和业务规模都不足以支撑这些复杂度。\n3. 单体内的模块化足够：如果内部模块边界清晰、依赖方向正确，单体内部的模块化就能满足大部分需求，不需要跨进程通信的额外代价。\n4. YAGNI：没有规模压力就不需要微服务的伸缩性和独立部署性。过早拆分是在为想象中的规模买单。\n\n第二阶段：识别边界，模块内高内聚、模块间低耦合\n- 在分层单体内，持续用 DDD 方法识别限界上下文，把代码按上下文组织成模块。\n- 模块间通过接口通信（而非直接访问对方的数据库表或内部类）。\n- 这一步不涉及部署变化，只是代码组织的改善，为未来拆服务做准备。\n- 关键信号：模块间的接口已经稳定，模块内部高度内聚，模块间几乎没有直接的数据访问——这时拆服务的成本最低。\n\n第三阶段：按需拆分微服务\n当出现以下信号时，才从分层单体走向微服务：\n\n1. 规模压力：\n- 某个模块的负载远超其他模块，需要独立伸缩（如秒杀模块需要 100 台机器，其他模块只需 2 台）。单体无法按模块独立伸缩。\n\n2. 团队规模压力：\n- 团队人数增长到单体协作困难（通常 8-10 人以上），多个团队在同一代码库上修改互相冲突。拆服务让各团队独立开发、独立部署。\n\n3. 技术异构需求：\n- 某个模块需要用不同技术栈（如推荐引擎用 Python，核心交易用 Java）。单体技术栈统一，微服务允许异构。\n\n4. 故障隔离需求：\n- 某个不稳定模块（如对接第三方 API）经常出问题，需要隔离故障域，避免拖垮整个系统。\n\n5. 独立部署节奏：\n- 不同模块需要不同的发布频率（如营销活动模块每周发布，核心交易模块每月发布）。单体统一部署，限制了发布灵活性。\n\n关键原则：拆服务要沿限界上下文的边界拆，不沿技术层的边界拆。\n- 正确拆法：按业务边界拆——订单服务、用户服务、库存服务。每个服务内部是完整的分层架构。\n- 错误拆法：按技术层拆——一个「业务逻辑服务」、一个「数据访问服务」。这把本应在一个进程内的层间调用变成了网络调用，性能和复杂度都爆炸。\n\n总结：分层架构是微服务的基础——每个微服务内部都应该分层。演进路径是「分层单体 → 模块化单体 → 按需拆分微服务」。先在单体内把分层和边界做对，再在有真实规模/团队/技术需求时才拆服务。微服务是解决规模问题的手段，不是解决设计烂的药——单体内部设计烂，拆成微服务只会变成一堆烂服务。`,
    tags: ["综合", "分层架构", "微服务", "演进路径", "限界上下文", "单体到微服务"],
  },
];
