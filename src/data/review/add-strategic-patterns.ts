import type { ReviewQuestion } from "./types";

/** 上下文映射复习题 */
export const addStrategicPatternsQuestions: ReviewQuestion[] = [
  {
    id: "add-strategic-patterns-01",
    chapter: "add-strategic-patterns",
    level: 1,
    question: `什么是上下文映射（Context Map）？它列出的 9 种上下文集成模式分别是什么？`,
    answer:
      `上下文映射（Context Map）是 DDD 战略设计的工具，用于描述不同限界上下文之间的关系和集成方式。它画出了各上下文之间的「谁依赖谁」「谁主导」「怎么协作」的地图。\n\n9 种上下文集成模式：\n\n1. Partnership（合作关系）：两个上下文紧密协作，共同成功或共同失败。双方联合制定计划，协调开发节奏。适用于利益高度绑定的上下文。\n\n2. Customer-Supplier（客户-供应商）：下游（客户）依赖上游（供应商）的产出。上游优先考虑下游的需求，但上游有最终决策权。双方有明确的上下游关系。\n\n3. Conformist（追随者）：下游完全服从上游的模型，不做翻译。上游说什么就是什么，下游照单全收。适用于上游很强大且模型合理的情况。\n\n4. Anticorruption Layer / ACL（防腐层）：下游建立翻译层，把上游的模型翻译成自己的模型。保护下游模型不被上游「腐蚀」。适用于上游模型不合理或不可控的情况。\n\n5. Open Host Service / OHS（开放主机服务）：上游为所有下游提供统一的开放接口（如 REST API、gRPC），而不是为每个下游定制翻译。适用于上游有很多下游消费者的情况。\n\n6. Shared Kernel（共享内核）：两个上下文共享一小部分模型（如共享一个 Customer 值对象）。共享部分变更需要双方协商。减少翻译成本，但增加了耦合。\n\n7. Published Language（发布语言）：上下文之间使用一种标准化的、公开发布的数据交换格式（如标准 XML schema、行业标准 API）。与 OHS 常配合使用。\n\n8. Separate Ways（各行其道）：两个上下文完全不集成，各自独立实现。适用于集成成本高于价值、或两者无真实业务关联的情况。\n\n9. Big Ball of Mud（大泥球）：一个混乱的、没有清晰边界的上下文，与它集成时需要特别小心。通常指遗留系统。\n\n记忆线索：按「协作紧密度」排列——Partnership（最紧）→ Customer-Supplier → Conformist → ACL（隔离最强）→ Separate Ways（不集成）。OHS/Published Language 是通信方式，Shared Kernel 是共享模型，Big Ball of Mud 是要避坑的。`,
    tags: ["上下文映射", "Context Map", "9种模式", "DDD", "集成模式"],
  },
  {
    id: "add-strategic-patterns-02",
    chapter: "add-strategic-patterns",
    level: 2,
    question: `防腐层（ACL）和追随者（Conformist）有什么区别？什么时候该用 ACL，什么时候该用 Conformist？`,
    answer:
      `防腐层（ACL）和追随者（Conformist）都是处理上下游上下文关系的模式，但核心区别在于「下游是否保护自己的模型」。\n\n追随者（Conformist）：\n- 下游完全接受上游的模型，不做翻译。上游的 \`User\` 有什么字段，下游的 \`User\` 就有什么字段。\n- 下游「追随」上游——上游模型变了，下游跟着改。\n- 优势：简单，没有翻译成本。代码直接对齐上游。\n- 劣势：下游模型被上游绑架——如果上游模型不合理，下游也得忍受。下游失去了建模的自主性。\n\n防腐层（ACL）：\n- 下游建立翻译层，把上游的模型翻译成自己的模型。上游的 \`UserDTO\`（含 30 个字段）翻译成下游的 \`Account\`（只含 3 个需要的字段）。\n- 下游「保护」自己的模型——上游模型怎么变，翻译层吸收变化，下游内部模型不受影响。\n- 优势：下游模型自主、干净，只包含自己需要的东西。上游变化被翻译层隔离。\n- 劣势：翻译层有开发和维护成本。多一层间接。\n\n什么时候用 Conformist：\n1. 上游模型合理且稳定：上游的模型设计得好，下游直接用没有问题。\n2. 下游对上游高度依赖：下游的业务完全建立在上游模型之上，自己没有独立的模型需求。\n3. 上游强大且不可控：如对接一个大型遗留系统或行业标准系统，改不了它，只能服从。\n4. 翻译成本不值得：如果上下游模型差异很小，建翻译层的成本大于收益。\n5. 团队能力或时间有限：没有精力维护翻译层，先服从快速上线。\n\n什么时候用 ACL：\n1. 上游模型不合理：上游是一个大泥球，模型混乱。直接用会「腐蚀」下游的模型，必须翻译。\n2. 上游频繁变化：上游经常改模型，下游需要一个缓冲层来吸收变化，避免每次上游改、下游就跟着改。\n3. 下游有独立的统一语言：下游的业务概念与上游不同（同名异义），必须翻译。如上游叫「User」含 30 个字段，下游只需「Buyer」含 3 个字段。\n4. 下游是核心域：核心域的模型必须干净、精准，不能被外部污染。任何与核心域集成的上游都应该经过 ACL。\n5. 遗留系统集成：对接老旧系统时，ACL 是标配——用翻译层把遗留系统的模型与现代模型隔开，为未来替换遗留系统做准备。\n6. 多个上游集成：如果下游要对接多个不同的上游系统，每个上游模型不同。用 ACL 统一翻译成下游模型，屏蔽多上游的差异。\n\n实际案例：\n- Conformist 案例：一个内部管理后台对接公司的统一用户中心。用户中心的 User 模型设计合理且稳定，管理后台直接用它的模型，不做翻译。简单高效。\n- ACL 案例：电商系统对接一个老旧的 ERP 系统。ERP 的数据格式是扁平的、字段名是缩写的（如 CUST_NO、ORD_AMT），完全不符合电商领域的统一语言。建 ACL 把 ERP 的格式翻译成电商领域的 \`Customer\`、\`OrderAmount\` 模型。将来换 ERP 时只改 ACL，核心域不受影响。\n\n一句话：Conformist 是「无条件接受」，ACL 是「有选择地翻译」。上游模型好且稳定就用 Conformist 图省事，上游模型差或常变就用 ACL 保自己。`,
    tags: ["ACL", "Conformist", "防腐层", "追随者", "对比", "集成模式", "DDD"],
  },
  {
    id: "add-strategic-patterns-03",
    chapter: "add-strategic-patterns",
    level: 3,
    question: `在一个电商平台中，订单上下文、支付上下文、库存上下文之间应该用哪种集成模式？请分析并给出建议。`,
    answer:
      `电商平台中订单、支付、库存三个上下文的集成模式分析：\n\n一、订单 → 支付（Customer-Supplier + ACL）\n\n关系分析：\n- 订单上下文需要调用支付上下文来完成支付。订单是客户（发起支付请求），支付是供应商（提供支付能力）。\n- 支付上下文有成熟的模型（支付单、支付渠道、退款单），但它不一定与订单上下文的模型一致。\n- 订单上下文不应该直接依赖支付上下文的内部模型——支付的 PaymentDTO 可能包含大量订单不需要的字段（渠道流水号、回调地址等）。\n\n建议模式：\n- Customer-Supplier：订单（客户）依赖支付（供应商）。支付优先考虑订单的支付需求，但支付有自己的技术约束（如对接第三方支付渠道）。\n- 加 ACL：订单上下文建立防腐层，把支付上下文的返回模型翻译成订单需要的模型（如只提取 \`paymentId\` 和 \`paymentStatus\`）。这样支付上下文的模型变化不影响订单上下文。\n- 可加 OHS：如果支付上下文有多个消费者（订单、退款、对账），支付应该提供统一的开放主机服务（标准化支付 API），而非为每个消费者定制接口。\n\n二、订单 → 库存（Customer-Supplier + 领域事件）\n\n关系分析：\n- 订单确认后需要扣减库存。库存是供应商（提供库存数据），订单是客户（需要扣减库存）。\n- 库存扣减不需要在订单确认的同一事务内完成——可以异步处理（最终一致性）。\n- 但库存必须保证不超卖（库存 ≥ 0 的不变量）。\n\n建议模式：\n- Customer-Supplier：订单（客户）依赖库存（供应商）。库存根据订单的扣减需求来设计接口。\n- 领域事件异步集成：订单确认后发布 \`OrderConfirmed\` 事件，库存上下文消费事件来扣减库存。不在同一事务内——订单确认成功后，库存异步扣减。如果库存不足，发布 \`InventoryShortage\` 事件，订单上下文消费后处理（如通知用户、取消订单）。\n- ACL（可选）：如果库存上下文的模型与订单需要的库存信息差异大，加翻译层。但如果只是简单的「扣减某商品的库存」，接口足够简单时可以不加 ACL。\n\n三、支付 → 订单（领域事件反馈）\n\n关系分析：\n- 支付完成后需要通知订单更新状态（从「待支付」变为「已支付」）。\n- 这个方向是支付 → 订单，与第一点（订单 → 支付）方向相反。但不是循环依赖——两者通过事件解耦。\n\n建议模式：\n- 领域事件：支付完成后发布 \`PaymentCompleted\` 事件，订单上下文消费事件更新订单状态。不在同一事务内——支付成功后，订单异步更新状态。\n- ACL：订单上下文消费 \`PaymentCompleted\` 事件时，做一层翻译——把支付事件的字段翻译成订单需要的字段。保护订单模型不依赖支付事件的具体结构。\n\n四、总结：集成模式全景\n\n\`\`\`\n订单上下文\n  │\n  ├── [Customer-Supplier + ACL] → 调用支付 API（同步）\n  │                                    ↓\n  │                              支付上下文\n  │                                    │\n  ├── ← [领域事件] ← PaymentCompleted ←┘（异步反馈）\n  │\n  ├── [领域事件] → OrderConfirmed → 库存上下文（异步扣减）\n  │                                    │\n  └── ← [领域事件] ← InventoryShortage ┘（异步反馈）\n\`\`\`\n\n设计原则总结：\n1. 跨上下文的同步调用加 ACL：保护调用方模型不被被调用方腐蚀。\n2. 跨上下文的异步通信用领域事件：解耦，实现最终一致性。\n3. 一次事务只操作一个聚合（一个上下文）：不在一个事务里同时改订单和库存。\n4. 核心域（订单）加 ACL 保护：与支付、库存的集成都经过翻译层。\n5. Supplier 上下文可提供 OHS：如支付提供标准化支付 API 给所有消费者。\n\n注意事项：\n- 不要用 Shared Kernel：订单和库存共享「商品」概念看起来很诱人，但商品在两个上下文中含义不同（订单关注价格，库存关注数量），共享内核会制造耦合。\n- 不要用 Conformist：支付上下文的模型（支付单、渠道）与订单模型差异大，直接接受会污染订单模型。\n- Separate Ways 不适用：三个上下文有真实的业务关联，必须集成。`,
    tags: ["上下文映射", "集成模式", "电商", "ACL", "领域事件", "Customer-Supplier", "DDD"],
  },
  {
    id: "add-strategic-patterns-04",
    chapter: "add-strategic-patterns",
    level: 4,
    question: `「Shared Kernel 看似省事实则危险」——请分析 Shared Kernel 的利弊，以及什么情况下值得用它，什么情况下应该避免。`,
    answer:
      `Shared Kernel（共享内核）是 9 种上下文集成模式中最需要谨慎使用的模式——它通过共享一部分模型来减少翻译成本，但代价是两个上下文在这部分模型上产生了强耦合。\n\nShared Kernel 的运作方式：\n两个上下文共享一小部分领域模型（代码/数据结构），这部分共享模型由双方共同维护。任何一方要修改共享部分，必须与另一方协商。共享部分之外，各自有独立的模型。\n\nShared Kernel 的利（好处）：\n\n1. 减少翻译成本：两个上下文共享同一套模型，不需要在边界建翻译层。如订单和物流共享一个 \`ShippingAddress\` 值对象，不需要在订单→物流时翻译地址格式。\n\n2. 模型一致性：共享部分在两个上下文中含义完全一致，不会出现同名异义。如 \`Money\` 值对象在所有上下文中都是同一个实现，金额计算逻辑统一。\n\n3. 代码复用：共享模型只写一份，两个上下文都用。减少重复代码。\n\nShared Kernel 的弊（风险）：\n\n1. 强耦合：两个上下文在共享部分绑死。一方改共享模型，另一方必须跟着改。如果两个团队节奏不同，修改共享模型需要跨团队协调，拖慢双方。\n\n2. 模型膨胀压力：共享模型要满足两个上下文的需求，容易逐渐膨胀——订单要加一个字段，物流也要加一个字段，共享模型越来越大，最终变成两个上下文都不满意的折中模型。\n\n3. 丧失独立性：上下文本应独立演化，但 Shared Kernel 让它们在共享部分无法独立变化。这削弱了限界上下文「独立自治」的核心价值。\n\n4. 依赖蔓延：共享一个模型后，往往会共享更多——共享数据库表、共享工具函数、共享配置。共享范围不知不觉扩大，边界形同虚设。\n\n5. 跨团队协商成本：每次改共享模型都要两个团队协商同意。如果团队多（3 个以上共享同一内核），协商成本指数级增长。\n\n6. 部署耦合：如果共享内核是代码库（共享包/模块），两个上下文的部署可能被绑定——共享包升级，所有使用方都要跟着升级。\n\n什么情况下值得用 Shared Kernel：\n\n1. 共享部分极其稳定：共享的模型是领域中最不容易变化的部分。如 \`Money\`（金额）、\`Address\`（地址）这类值对象，概念稳定，很少需要修改。共享稳定的东西，耦合风险低。\n\n2. 两个上下文由同一个团队维护：如果是同一个团队维护两个上下文，协商成本几乎为零。团队内部改共享模型不需要跨团队协调。Shared Kernel 的主要风险（跨团队协商）消失了。\n\n3. 共享部分范围极小：只共享一两个值对象，不共享实体、不共享聚合根。范围越小，耦合面越小，风险越可控。\n\n4. 两个上下文高度内聚：如果两个上下文实际上是同一个子域的两个方面，共享内核只是把「本应在一起的东西」放在一起。如「订单创建」和「订单查询」可以视为两个上下文，但共享 \`Order\` 实体是合理的。\n\n什么情况下应该避免 Shared Kernel：\n\n1. 两个上下文由不同团队维护：跨团队协商共享模型的变更成本极高，且容易产生「谁说了算」的冲突。用 ACL 替代——各自有独立模型，通过翻译层协作。\n\n2. 共享部分包含实体或聚合根：实体有生命周期、有行为，比值对象复杂得多，变化频率更高。共享实体意味着共享业务逻辑，耦合风险极大。\n\n3. 两个上下文的业务语义有差异：即使在概念上「相同」的东西，在不同上下文中关注的维度不同。如「商品」在销售和仓储中关注点不同，共享一个 \`Product\` 模型会迫使它包含两个上下文的字段，导致膨胀。\n\n4. 上下文可能独立部署/演化：如果未来可能把一个上下文拆成独立微服务，Shared Kernel 是障碍——共享代码库让拆分变难。提前用 ACL 隔离，未来拆分更容易。\n\n5. 团队规模大：超过 2 个团队共享同一内核时，协商成本已经超过翻译成本。用 Published Language 或 OHS 替代——定义一个标准的交换格式，各上下文独立翻译。\n\n替代方案优先级：\n1. 优先用 ACL：安全、隔离好。翻译成本是可控的一次性投入，而 Shared Kernel 的耦合成本是持续的。\n2. 其次用 Published Language + OHS：定义标准交换格式，上游开放服务，下游各自翻译。比 Shared Kernel 松散。\n3. 最后才考虑 Shared Kernel：只在共享部分极小、极稳定、同团队维护时才用。\n\n一句话：Shared Kernel 是「用耦合换便利」的权衡。短期看省了翻译层的成本，长期看耦合的利息可能远超省下的本金。除非共享部分足够稳定、团队足够小，否则优先选 ACL。DDD 的核心价值是「边界内的自治」，Shared Kernel 削弱了自治，违背了限界上下文的初衷。`,
    tags: ["综合", "Shared Kernel", "共享内核", "耦合", "ACL", "权衡", "DDD"],
  },
];
