import type { ReviewQuestion } from "./types";

/** 实体、值对象与聚合复习题 */
export const addTacticalPatternsQuestions: ReviewQuestion[] = [
  {
    id: "add-tactical-patterns-01",
    chapter: "add-tactical-patterns",
    level: 1,
    question: "实体（Entity）和值对象（Value Object）有什么区别？各举一个例子。",
    answer:
      "实体（Entity）和值对象（Value Object）是 DDD 战术设计的两个基础构建块，核心区别在于「是否需要唯一标识」。\n\n实体（Entity）：\n- 有唯一标识（ID），通过 ID 判断两个实体是否相同。\n- 有生命周期——会被创建、修改、最终销毁。\n- 属性可以变化，但 ID 不变。\n- 两个属性完全相同的实体，如果 ID 不同，就是不同的实体。\n- 例子：订单（Order）。两个订单的商品、金额、收货地址完全一样，但订单号不同，就是两个不同的订单。订单会经历「创建→支付→发货→签收」的生命周期，状态在变但订单号不变。\n\n值对象（Value Object）：\n- 没有唯一标识，通过属性值判断是否相同。\n- 不可变（Immutable）——创建后属性不能改变，要「修改」就创建新实例。\n- 两个属性完全相同的值对象，就是同一个值对象（值相等即相同）。\n- 通常用于描述实体的某个属性维度。\n- 例子：金额（Money）。`Money(100, 'CNY')` 和另一个 `Money(100, 'CNY')` 完全等价——100 元人民币就是 100 元人民币，没有「哪个 100 元」的区别。地址（Address）、坐标（Coordinate）、颜色（Color）也是值对象。\n\n对比总结：\n- 标识：实体有 ID，值对象没有 ID。\n- 相等性：实体按 ID 比较，值对象按值比较。\n- 可变性：实体可变（改属性），值对象不可变（建新的）。\n- 生命周期：实体有生命周期，值对象随实体的创建/销毁而存在/消亡。\n\n设计准则：优先用值对象。只有需要唯一标识和生命周期时才用实体。值对象不可变、无副作用、好测试，应该尽量多用。",
    tags: ["实体", "值对象", "Entity", "Value Object", "区别", "DDD"],
  },
  {
    id: "add-tactical-patterns-02",
    chapter: "add-tactical-patterns",
    level: 2,
    question: "什么是聚合（Aggregate）和聚合根（Aggregate Root）？聚合根的职责是什么？聚合边界如何保证一致性？",
    answer:
      "聚合（Aggregate）：\n聚合是一组相关领域对象的集合，作为一个整体被加载、修改和持久化。聚合定义了一致性边界——聚合内部的对象必须保持一致，聚合之间通过最终一致性协调。\n\n聚合根（Aggregate Root）：\n聚合根是聚合的入口点——一个特定的实体，外部只能通过它来访问聚合内部的对象。聚合根是聚合内唯一的「对外接口」。\n\n聚合根的职责：\n\n1. 控制访问：外部代码不能绕过聚合根直接操作聚合内部的对象。如一个 Order 聚合包含 OrderItem，外部不能直接 `orderItem.changeQuantity(5)`，必须通过 `order.changeItemQuantity(itemId, 5)`。\n\n2. 保证不变量（Invariant）：聚合根负责确保聚合内部始终满足业务规则。如「订单总金额 = 所有商品金额之和」这个不变量，由 Order 聚合根在每次修改商品数量时重新计算总额来保证。\n\n3. 管理生命周期：聚合根负责创建和删除聚合内部的对象。如 `order.addItem(product, quantity)` 由 Order 决定怎么创建 OrderItem。\n\n4. 发布领域事件：聚合根是领域事件的发布点。如订单确认时 `order.confirm()` 发布 `OrderConfirmed` 事件。\n\n聚合边界如何保证一致性：\n\n1. 聚合内强一致性（事务一致性）：\n聚合内部的不变量在每次操作后必须立即满足。修改聚合的操作在一个数据库事务中完成——要么全部成功，要么全部回滚。如修改订单商品数量 + 重新计算总额 + 检查库存，在一个事务内完成。\n\n2. 聚合间最终一致性：\n不同聚合之间不要求强一致。如「订单确认」后需要「扣减库存」，订单和库存是两个不同的聚合（甚至在不同的限界上下文中）。订单确认后发布 `OrderConfirmed` 事件，库存上下文异步消费事件来扣减库存。中间有时间差（最终一致），但不需要在同一个事务里。\n\n3. 引用通过 ID 而非对象引用：\n聚合之间不直接持有对方的对象引用，而是持有对方的 ID。如 Order 聚合持有 `customerId: string`（ID），而不是 `customer: Customer`（对象）。这样聚合之间没有直接依赖，各自独立加载和持久化。\n\n4. 一次事务只修改一个聚合：\nDDD 的原则是「一个事务内只修改一个聚合」。需要修改多个聚合时，通过领域事件 + 异步处理来实现，而非在一个事务里改多个聚合。这保证了事务粒度小、性能好、不会长时间锁数据。\n\n聚合设计的关键决策——聚合边界划多大：\n- 太大：所有对象放在一个聚合里，事务粒度大，并发冲突多，性能差。\n- 太小：每个对象一个聚合，跨聚合一致性难以保证。\n- 原则：只把「必须一起保持一致」的对象放在一个聚合里。能独立的就独立成聚合。",
    tags: ["聚合", "Aggregate", "聚合根", "Aggregate Root", "一致性", "不变量", "DDD"],
  },
  {
    id: "add-tactical-patterns-03",
    chapter: "add-tactical-patterns",
    level: 3,
    question: "什么是 Repository（仓储）模式？Repository 接口应该定义在哪一层？为什么不能直接在领域对象中操作数据库？",
    answer:
      "Repository（仓储）模式：\nRepository 为聚合提供持久化和检索的接口，让领域层可以「像操作内存集合一样」操作持久化数据。它隐藏了数据库的具体技术细节，领域层只知道 Repository 接口，不知道底层用的是 MySQL、MongoDB 还是内存。\n\nRepository 的核心方法：\n```typescript\ninterface OrderRepository {\n  findById(id: string): Promise<Order | null>;   // 按 ID 查找\n  save(order: Order): Promise<void>;              // 保存（新增或更新）\n  delete(id: string): Promise<void>;              // 删除\n}\n```\n\n关键特征：\n- Repository 面向聚合根——一个聚合根对应一个 Repository。如 OrderRepository 管理 Order 聚合。\n- Repository 返回的是领域对象（如 Order），不是数据库行（如 row）。它负责数据格式转换。\n- Repository 接口只定义「领域需要什么操作」，不暴露数据库细节（如不暴露 SQL、不暴露分页参数）。\n\nRepository 接口应该定义在哪一层：\n\nRepository 接口定义在领域层（整洁架构的 Entities / Use Cases 层），实现在基础设施层（Frameworks 层）。\n\n```typescript\n// 领域层：定义接口\ndomain/order/order-repository.ts\n  export interface OrderRepository {\n    findById(id: string): Promise<Order | null>;\n    save(order: Order): Promise<void>;\n  }\n\n// 基础设施层：实现接口\ninfrastructure/order/order-repository-mysql.ts\n  export class OrderMySQLRepository implements OrderRepository {\n    async findById(id: string): Promise<Order | null> {\n      const row = await this.db.query('SELECT * FROM orders WHERE id = ?', [id]);\n      return row ? this.toDomain(row) : null;  // 数据库行 → 领域对象\n    }\n    async save(order: Order): Promise<void> {\n      await this.db.execute('INSERT INTO orders ...', this.toRow(order));  // 领域对象 → 数据库行\n    }\n  }\n```\n\n为什么接口在领域层：这是依赖倒置原则（DIP）的应用。领域层需要持久化能力，但不应该依赖具体的数据库实现。接口由领域层定义（表达「我需要什么」），实现在基础设施层（表达「怎么做到」），依赖方向从基础设施指向领域层的接口。\n\n为什么不能直接在领域对象中操作数据库：\n\n1. 职责分离：领域对象的职责是「封装业务规则」，不是「管理持久化」。如果 Order 对象里写 SQL 语句或调 ORM，它的职责就被污染了——既管业务规则又管数据存储，违反单一职责。\n\n2. 可测试性：如果领域对象直接操作数据库，单元测试就必须连数据库。通过 Repository 接口注入 Mock，测试领域对象时不需要任何外部依赖。\n\n3. 可替换性：领域对象不绑死在某种数据库上。换数据库只改 Repository 实现，领域对象一行不改。\n\n4. 依赖方向正确：领域对象是最内层、最稳定的。如果它依赖数据库，依赖方向就向外了，核心被技术细节绑架。Repository 模式反转了这个依赖。\n\n5. 聚合边界的保护：Repository 以聚合为单位操作，确保聚合作为一个整体被加载和保存。如果领域对象直接操作数据库，可能只更新聚合的一部分，破坏聚合一致性。\n\nRepository 与 DAO 的区别：\n- DAO（Data Access Object）面向数据表，一个表一个 DAO，返回数据行/DTO。\n- Repository 面向聚合，一个聚合根一个 Repository，返回领域对象。\n- Repository 在 DAO 之上——Repository 可以调用多个 DAO 来组装一个聚合。\n- Repository 是领域层概念，DAO 是数据层概念。",
    tags: ["Repository", "仓储模式", "DIP", "依赖方向", "可测试性", "聚合", "DDD"],
  },
  {
    id: "add-tactical-patterns-04",
    chapter: "add-tactical-patterns",
    level: 4,
    question: "什么是聚合的不变量（Invariant）？如何设计一个合理的聚合？请以「订单 + 订单项」为例说明聚合设计过程。",
    answer:
      "不变量（Invariant）：\n不变量是聚合必须始终满足的业务规则——无论聚合经历什么操作，操作完成后不变量必须成立。不变量是决定聚合边界的关键依据——「必须一起满足的不变量」决定了哪些对象必须在同一个聚合内。\n\n常见的聚合不变量例子：\n- 订单总金额 = 所有订单项金额之和。\n- 订单项数量 > 0。\n- 一个订单最多 50 个订单项。\n- 账户余额 ≥ 0（不能透支）。\n- 帖子被删除时，所有评论也必须删除。\n\n聚合设计过程——以「订单 + 订单项」为例：\n\n第一步：识别领域对象\n- Order（订单）：id、status、totalAmount、confirmedAt\n- OrderItem（订单项）：id、productId、quantity、unitPrice、subTotal\n\n第二步：识别不变量\n1. 订单总金额 = 所有订单项的 subTotal 之和。→ 每次增删改订单项，总额必须重新计算。\n2. 订单项数量必须 > 0。→ 修改数量时必须校验。\n3. 订单项的 unitPrice 必须与下单时的商品价格一致。→ 不能随意改价。\n4. 订单状态为「已确认」后，不能增删改订单项。→ 状态约束。\n\n第三步：判断这些不变量是否需要「一起」满足\n- 不变量 1-4 都涉及 Order 和 OrderItem 的协调关系。\n- 如果 Order 和 OrderItem 是独立的聚合，修改 OrderItem 数量时无法自动更新 Order 的总额（跨聚合，不在一个事务里）。\n- 因此，Order 和 OrderItem 必须在同一个聚合里，才能在同一个事务内保证不变量始终成立。\n\n第四步：确定聚合根\n- Order 是聚合根（有独立生命周期、是外部访问的入口）。\n- OrderItem 是聚合内部对象（不能独立存在，必须依附于 Order）。\n- 外部不能直接操作 OrderItem，必须通过 Order。\n\n第五步：设计聚合根接口\n```typescript\nclass Order {  // 聚合根\n  private _id: string;\n  private _status: OrderStatus;\n  private _items: OrderItem[];   // 聚合内部对象\n  private _totalAmount: Money;\n\n  // 不变量由聚合根保证\n  addItem(productId: string, productName: string, quantity: number, unitPrice: Money): void {\n    this._ensureModifiable();  // 不变量4：状态约束\n    this._ensureMaxItems();    // 不变量：最多50项\n    if (quantity <= 0) throw new Error('数量必须大于零');  // 不变量2\n    \n    const existing = this._items.find(i => i.productId === productId);\n    if (existing) {\n      existing.increaseQuantity(quantity);\n    } else {\n      this._items.push(new OrderItem(uuid(), productId, productName, quantity, unitPrice));\n    }\n    this._recalculateTotal();  // 不变量1：总额重算\n  }\n\n  changeItemQuantity(itemId: string, newQuantity: number): void {\n    this._ensureModifiable();\n    if (newQuantity <= 0) throw new Error('数量必须大于零');\n    const item = this._findItem(itemId);\n    item.changeQuantity(newQuantity);\n    this._recalculateTotal();  // 不变量1\n  }\n\n  removeItem(itemId: string): void {\n    this._ensureModifiable();\n    this._items = this._items.filter(i => i.id !== itemId);\n    this._recalculateTotal();  // 不变量1\n  }\n\n  private _recalculateTotal(): void {\n    this._totalAmount = this._items.reduce(\n      (sum, item) => sum.add(item.subTotal),  // Money 值对象不可变\n      Money.zero()\n    );\n  }\n\n  private _ensureModifiable(): void {\n    if (this._status !== OrderStatus.Pending) {\n      throw new Error('订单已确认，不能修改');  // 不变量4\n    }\n  }\n}\n\nclass OrderItem {  // 聚合内部对象，不对外暴露\n  constructor(\n    private _id: string,\n    private _productId: string,\n    private _productName: string,\n    private _quantity: number,\n    private _unitPrice: Money,\n  ) {}\n\n  get subTotal(): Money {\n    return this._unitPrice.multiply(this._quantity);  // 不变量3：基于下单时价格\n  }\n\n  increaseQuantity(qty: number): void {\n    this._quantity += qty;\n  }\n\n  changeQuantity(newQty: number): void {\n    this._quantity = newQty;\n  }\n}\n```\n\n第六步：Repository 以聚合为单位\n```typescript\ninterface OrderRepository {\n  findById(id: string): Promise<Order | null>;  // 加载整个聚合（含 items）\n  save(order: Order): Promise<void>;             // 保存整个聚合\n}\n```\n\n聚合设计的关键原则总结：\n\n1. 不变量决定边界：必须一起满足的不变量 → 同一个聚合。可以独立满足的 → 不同聚合。\n2. 聚合要小：只包含必须一起一致的对象。OrderItem 离开 Order 没意义 → 在同一聚合。但 Product 有独立生命周期（商品目录管理）→ 不同聚合，Order 只存 productId。\n3. 跨聚合用 ID 引用：Order 存 `productId: string`，不存 `product: Product`。\n4. 聚合根是唯一入口：所有对聚合内部对象的操作都经过聚合根。\n5. 一次事务一个聚合：不在一个事务里改多个聚合。跨聚合一致性用领域事件 + 最终一致性。\n6. 优先值对象：OrderItem 的 unitPrice 用 Money 值对象，不用 number。值对象不可变、好测试、类型安全。\n\n反模式警示：\n- 巨型聚合：把所有相关对象塞进一个聚合（如 Order 里包含 Customer、Product、Shipping）→ 事务太大、并发冲突多。只放必须一起一致的对象。\n- 空聚合：聚合根只是数据容器，不变量写在 Service 里 → 聚合形同虚设，回到贫血模型。不变量必须在聚合根的方法里保证。",
    tags: ["综合", "聚合", "不变量", "Invariant", "聚合设计", "订单", "DDD"],
  },
];
