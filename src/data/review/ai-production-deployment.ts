/** 复习题库 · 生产化部署（ai-production-deployment）。AI Agent 开发实战·原创改编企业级应用篇第 3 章（全书收官）。 */

import type { ReviewQuestion } from "./types";

export const aiProductionDeploymentQuestions: ReviewQuestion[] = [
  // ── L1 认记：术语 / 定义 ──
  {
    id: "ai-prod-1",
    chapter: "ai-production-deployment",
    level: 1,
    question:
      "为什么说 Agent 上线「不是一个脚本，而是一套架构」？生产架构典型有哪些组件？",
    answer:
      "因为上线要扛**海量用户同时请求**，光靠本地那个串行跑一遍的脚本根本撑不住，得有一套**有分工的架构**。典型组件：① **API 网关**——统一入口，负责收请求、鉴权（你是谁、有没有权）、限流（每人每秒最多几次）；② **队列**——突发请求先排队，削峰、把『收请求』和『处理请求』解耦成异步；③ **Agent 服务**——真干活的地方，从队列取请求、调 LLM / 工具，可水平扩多个实例分摊压力；④ **缓存**——重复 / 相似请求命中就直接返回、不调模型；⑤ **可观测**——trace / 指标全程记录（第12章那套），哪步慢 / 错 / 贵都看得见。",
    tags: ["生产架构", "定义"],
  },
  {
    id: "ai-prod-2",
    chapter: "ai-production-deployment",
    level: 1,
    question: "API 网关在生产架构里负责哪几件事？",
    answer:
      "API 网关是**所有请求的统一入口**，主要负责三件事：① **收请求**——作为对外的统一接入点；② **鉴权**——确认请求方是谁、有没有权限访问（挡住未授权的访问）；③ **限流**——限制每个用户的调用频率（每人每秒最多几次），挡住滥用和突发洪峰，保护后端不被冲垮。一句话：网关是门卫 + 收发室，所有请求先过它这一关。",
    tags: ["API 网关", "生产架构", "定义"],
  },
  {
    id: "ai-prod-3",
    chapter: "ai-production-deployment",
    level: 1,
    question: "并发与异步要解决什么问题？它靠哪三招？",
    answer:
      "要解决的核心矛盾是：**海量请求同时到，而 Agent 每次处理又很慢**（LLM 延迟好几秒）——一个个串着处理后面的人排到天荒地老。三招：① **异步**——把『收请求』和『处理请求』拆开，请求先入队、立刻回应『收到了』，后台慢慢处理，不让用户连接挂着干等；② **队列削峰**——突发洪峰先在队列排队，后端按自己能力匀速取，别让瞬时高峰砸垮 Agent 服务；③ **并发控制**——限制同时在跑的请求数（如最多 50 个并发），别一股脑把所有请求丢给下游，否则会把 LLM / 工具打爆。",
    tags: ["并发与异步", "定义"],
  },
  {
    id: "ai-prod-4",
    chapter: "ai-production-deployment",
    level: 1,
    question: "可靠性三件套「超时 / 重试 / 降级兜底」各是什么？",
    answer:
      "① **超时**——给一次调用设『最多等多久』的上限，到点还没返回就主动掐断、不再傻等，避免一个卡住的慢请求拖垮整个服务；② **重试**——失败 / 超时了自动再试几次（通常配退避：等得越来越久），救偶发的瞬时失败，但只对**幂等**操作安全；③ **降级 / 兜底**——主模型挂了切**备用模型**，备用也不行就返回一个**安全兜底答案**（如『服务繁忙，请稍后再试』），核心原则是**永远不让用户撞见原始报错**。三者一起，把生产里必然发生的失败接住。",
    tags: ["超时", "重试", "降级兜底", "定义"],
  },
  {
    id: "ai-prod-5",
    chapter: "ai-production-deployment",
    level: 1,
    question: "什么是「幂等」？它和「能不能重试」有什么关系？",
    answer:
      "幂等 = 一个操作**执行一次和执行多次，效果完全一样**。比如『查天气 / 查订单』是幂等的（查几次结果都一样、无副作用）；而『下单 / 扣款 / 发邮件』**不是**（重复执行会重复下单 / 扣款）。和重试的关系：重试 = 失败了自动再做一次，所以**只能对幂等操作无脑重试**——重试只是想多试出一个成功结果，不会造成额外后果。对非幂等操作盲目重试，会造成重复下单 / 重复扣款等事故，必须先用唯一请求 ID 等手段去重，确认重试不会被重复执行，才能重试。",
    tags: ["幂等", "重试", "定义"],
  },
  {
    id: "ai-prod-6",
    chapter: "ai-production-deployment",
    level: 1,
    question: "什么是灰度发布（金丝雀发布）？什么是回滚？",
    answer:
      "**灰度发布（canary）**：新版本上线不一上来就全量，而是**先放一小部分流量**（如 5%）给新版本，盯住关键指标（成功率 / 延迟 / 成本），确认没问题再逐步放量 5% → 50% → 100%。名字来自矿工带金丝雀下井探毒气——用一小撮流量当『探子』先试。**回滚（rollback）**：发现新版本有问题时**立刻把流量切回稳定的老版本**、撤下新版本，是灰度的『安全绳』。两者配合：灰度负责**早发现**（小流量探路）、回滚负责**快止损**（出事秒撤、损失封顶）。",
    tags: ["灰度发布", "回滚", "定义"],
  },
  // ── L2 理解：分辨 / 解释为什么 ──
  {
    id: "ai-prod-7",
    chapter: "ai-production-deployment",
    level: 2,
    question:
      "为什么 Agent 这种场景特别适合用异步（async / await）？同步串行处理会怎样？",
    answer:
      "因为 Agent **大部分时间在『等』**——等 LLM 回话、等工具返回，这几秒里 CPU 其实闲着。异步（async / await）让这段等待可以被**别的请求利用**：一个 worker 等着第一条请求的 LLM 返回时，正好去启动第二条，从而『同时推进很多请求』。如果用**同步串行**：必须第一条请求彻底处理完（包括傻等它那几秒 LLM）才能开始第二条，期间 CPU 干等、吞吐极低，海量用户会排成长龙、大批超时。所以『慢 + 高并发』的 Agent 场景，异步几乎是必选。",
    tags: ["并发与异步", "理解"],
  },
  {
    id: "ai-prod-8",
    chapter: "ai-production-deployment",
    level: 2,
    question: "为什么必须给外部调用设超时？「不设超时」会引发什么连锁后果？",
    answer:
      "因为 LLM / 工具偶尔会**卡住很久甚至永不返回**。不设超时的连锁后果：① 用户的请求一直挂着**干等**，体验极差；② 更要命的是，这个卡住的慢请求**占着连接 / 线程 / 协程不释放**，攒多了把服务的资源池占满；③ 资源占满后，**新来的正常请求也进不来**——于是一个慢请求最终**拖垮整个服务**、大批用户一起转圈。这就是『没有超时的调用 ≈ 定时炸弹』。修法：给每次外部调用都设超时（如 asyncio.wait_for），到点掐断、转去重试或降级。",
    tags: ["超时", "理解"],
  },
  {
    id: "ai-prod-9",
    chapter: "ai-production-deployment",
    level: 2,
    question:
      "为什么「把原始异常 / 错误堆栈直接返回给用户」既砸体验又有安全风险？该怎么做？",
    answer:
      "**砸体验**：用户看到的是一屏看不懂的技术报错（堆栈、异常类型），而不是一个能用的回答或一句体面的解释，观感像『系统崩了』。**安全风险**：原始报错里常**泄露内部信息**——API key、文件路径、数据库表结构、内部服务地址等，这些落到潜在攻击者手里，等于给了他们摸清系统、发起进一步攻击的线索。正确做法：**永远不让用户撞见原始错误**——出错时降级（切备用模型）或返回一个安全、笼统的**兜底答案**；详细错误只记进内部 trace / 日志供排查，绝不外泄。",
    tags: ["降级兜底", "安全", "理解"],
  },
  {
    id: "ai-prod-10",
    chapter: "ai-production-deployment",
    level: 2,
    question:
      "「重试」配的「退避（backoff）」是干什么的？为什么失败后不立刻、反复猛重试？",
    answer:
      "退避 = 每次重试之间**等待的时间越来越久**（如 1s、2s、4s…），而不是失败后立刻、连续猛重试。为什么要退避：失败往往是因为**对方正忙 / 过载 / 触发了限流**——这时你如果立刻反复猛捶，只会让本就忙不过来的对方**雪上加霜**，更难恢复，甚至把它彻底压垮（重试风暴）。退避给对方留出**喘息和恢复**的时间：等一会儿再试，对方可能已经缓过来了，成功率反而更高。所以重试要『温柔』——退避着试，而不是失败了就死命捶。",
    tags: ["重试", "退避", "理解"],
  },
  {
    id: "ai-prod-11",
    chapter: "ai-production-deployment",
    level: 2,
    question:
      "为什么要用 Semaphore（信号量）限制并发？「不限并发、请求全砸给下游」会怎样？",
    answer:
      "因为下游（LLM API、数据库、工具服务）有**自己的承受上限**。Semaphore 像个『令牌池』——同时只发 N 个令牌（如 50），第 N+1 个请求得等前面还回令牌才能开跑，从而把**同时在跑的请求数框住**。如果**不限并发、把上千个请求一股脑全砸过去**：会**触发对方限流**（大批请求被拒）、甚至**把下游打趴**（它过载崩溃），结果谁都别想成功，还可能引发全线雪崩。限并发是**主动给自己踩刹车，换来整体的稳**——宁可让一部分请求稍等，也别把下游冲垮导致全盘皆输。",
    tags: ["并发控制", "Semaphore", "理解"],
  },
  {
    id: "ai-prod-12",
    chapter: "ai-production-deployment",
    level: 2,
    question:
      "为什么说「回滚是灰度的安全绳」？没有快速回滚能力，灰度发布还剩多少价值？",
    answer:
      "灰度的逻辑分两半：**小流量探路**（灰度本身）+ **发现问题能撤**（回滚）。回滚是安全绳，因为正是『知道一旦出事几秒钟就能切回已知良好的老版本、损失封顶』，你才**敢**大胆灰度、敢上新版本。**没有快速回滚**时灰度价值大打折扣：你能在 5% / 50% 时**看见**指标变差，却**撤不回去**——要么硬着头皮继续放量让问题扩大，要么手忙脚乱手动改配置重新部署老版本，期间问题一直在线上发酵，等于『探到了毒气却跑不出矿井』。所以灰度与回滚必须成对：灰度负责早发现、回滚负责快止损，缺一不可。",
    tags: ["灰度发布", "回滚", "理解"],
  },
  {
    id: "ai-prod-13",
    chapter: "ai-production-deployment",
    level: 2,
    question: "缓存在生产架构里既省时又省钱，它具体是怎么省的？放在哪个位置？",
    answer:
      "缓存把**问过的、重复 / 相似的请求结果存起来**，下次同样的请求来了**直接返回缓存、根本不调模型**。怎么省：① **省时**——命中缓存是毫秒级返回，不用再等 LLM 那几秒；② **省钱**——不调模型就不烧 token，重复的活只花一次钱。位置：放在 Agent 服务**之前 / 之内**当一道前置关——请求进来先查缓存，命中直接返回、未命中才真去跑 Agent 调模型。生产里很多请求其实高度重复，缓存往往是性价比最高的一道优化。",
    tags: ["缓存", "生产架构", "理解"],
  },
  // ── L3 应用：判断 / 套用到场景 / 写代码 ──
  {
    id: "ai-prod-14",
    chapter: "ai-production-deployment",
    level: 3,
    question:
      "（写代码）写一个带超时 + 重试的异步调用 call_with_retry：单次调用超 3 秒就掐断，失败 / 超时重试 2 次、每次退避。为什么强调它『只适合幂等操作』？",
    answer:
      '```python\nimport asyncio\n\n\nasync def call_with_retry(coro_fn, *, timeout=3.0, retries=2):\n    """带超时的调用 + 失败重试。只对幂等操作这么用！"""\n    last_err = None\n    for attempt in range(retries + 1):\n        try:\n            return await asyncio.wait_for(coro_fn(), timeout=timeout)\n        except (asyncio.TimeoutError, TransientError) as err:\n            last_err = err\n            if attempt < retries:\n                await asyncio.sleep(2**attempt)  # 退避 1s,2s,4s…\n    raise last_err  # 重试都用完仍不行，抛出去让上层降级\n```\n要点：`asyncio.wait_for(coro_fn(), timeout=timeout)` 给单次调用套超时闹钟、到点掐断；外层 `for` 捕获超时 / 瞬时错误后重试，`2**attempt` 做退避（别在对方正忙时猛捶）；重试用完仍失败就抛给上层降级。**为什么只适合幂等操作**：重试 = 自动再做一次。幂等操作（查询）重试几次结果一样、无副作用，安全。但若拿它去重试下单 / 扣款这类非幂等操作——网络抖一下让你以为失败、实际对方已执行，一重试就重复下单 / 重复扣款。非幂等操作必须先用唯一请求 ID 去重，确认不会被重复执行才能重试。',
    tags: ["代码", "超时", "重试", "幂等", "应用"],
  },
  {
    id: "ai-prod-15",
    chapter: "ai-production-deployment",
    level: 3,
    question:
      "（写代码）写一个 ask_with_fallback(prompt)：主模型挂了切备用模型，备用也挂了返回安全兜底答案，保证用户撞不见原始异常。",
    answer:
      '```python\nasync def ask_with_fallback(prompt):\n    """主模型 → 备用模型 → 安全兜底答案，逐层降级。"""\n    try:\n        return await call_with_retry(lambda: call_llm("main", prompt))\n    except Exception:\n        pass  # 主模型彻底不行，往下降级（这里要记 trace / 告警）\n    try:\n        return await call_with_retry(lambda: call_llm("backup", prompt))\n    except Exception:\n        pass  # 备用也挂了，走兜底\n    return "抱歉，服务暂时繁忙，请稍后再试。"  # 安全兜底，绝不抛原始报错\n```\n要点：三层 `try` 逐级退让——主模型失败切 `backup` 备用、备用也失败就 `return` 那句安全兜底答案。无论哪层挂，用户拿到的都是『一个能用的结果或一句体面的话』，而不是吓人的报错堆栈，所以**用户撞不见原始异常**。注意每次降级处生产里都该**记一条 trace / 触发告警**（第12章那套），别让降级悄悄发生而你不知道主模型已经挂了。这正是降级 Demo 演示的三条路径。',
    tags: ["代码", "降级兜底", "应用"],
  },
  {
    id: "ai-prod-16",
    chapter: "ai-production-deployment",
    level: 3,
    question:
      "（场景判断）你的 Agent 有 get_order_status(order_id) 和 place_order(items) 两个工具，给两者都套了「失败自动重试 3 次」。各会发生什么？哪个危险，怎么改？",
    answer:
      "**get_order_status → 安全。** 查订单状态是**幂等**操作——查一次查三次结果都一样、无副作用，失败重试只是想多试出一个成功结果，没问题。\n**place_order → 危险！** 下单是**非幂等**操作——执行一次就下一单。一旦遇到『网络抖动让客户端以为失败、但服务端其实已下单成功』，重试 3 次就可能**下出 3 单、扣 3 次钱**，酿成事故。\n**危险的怎么改**：不能无脑重试，要**幂等化**——下单时带一个**唯一请求 ID（幂等键）**，服务端据此识别『这个 ID 的单已处理过』，重复请求直接返回上次结果而不再下单。这样即使客户端重试也不会重复下单。确认有了这层去重保护，才可以安全重试。一句话：重试只对幂等操作无脑安全，非幂等的必须先去重。",
    tags: ["场景判断", "幂等", "重试", "应用"],
  },
  {
    id: "ai-prod-17",
    chapter: "ai-production-deployment",
    level: 3,
    question:
      "（场景判断）你要给线上 Agent 换一个新模型。对比「直接 100% 全量切换」和「灰度 5% → 50% → 100% + 回滚」两种上线方式：同样遇到新模型有问题时，影响面和止损能力差多少？",
    answer:
      "**直接 100% 全量切换**：新模型一上线就接管全部流量，万一它有个没测出来的问题（成功率掉、答错、延迟飙），**瞬间炸到 100% 用户**——全体用户同时遭殃，且你只能在大面积翻车后才发现，再手忙脚乱地回退，影响面和损失都拉满。\n**灰度 + 回滚**：先放 5% 流量盯指标，问题在 5%（或 50%）这一小撮流量上就**暴露**，此时只有这一小部分用户受了点影响；发现指标变差立刻**回滚**切回老版本，**绝大多数用户始终用着稳定的老版本**，损失被牢牢封顶。\n差距：全量切换是『拿全体用户当第一批小白鼠、出事影响面 100%』；灰度 + 回滚是『用一小撮流量探路、早发现 + 快止损，影响面压到最小』。所以换模型 / 改 prompt 这类变更，生产里几乎都该走灰度 + 备好回滚。",
    tags: ["场景判断", "灰度发布", "回滚", "应用"],
  },
  // ── L4 综合：贯通多个概念 + 收官串全书 ──
  {
    id: "ai-prod-18",
    chapter: "ai-production-deployment",
    level: 4,
    question:
      "把本章串起来：小特要上线当客服 Agent，面对海量用户、依赖会偶发失败的 LLM 和工具、近期还要频繁换模型 / 改 prompt。请用生产架构、并发异步、超时重试降级、灰度回滚几样，设计一套让它「能稳稳上线」的方案，说清各解决什么、怎么配合。",
    answer:
      "走一遍这套生产化方案。**先搭架构（生产架构）**：请求先进 **API 网关**（鉴权 + 限流，挡住滥用和洪峰）→ 入**队列**（突发流量先排队削峰）→ **Agent 服务**（真干活，可水平扩多个实例）→ 前置**缓存**（重复 / 相似问题命中直接返回、不调模型，省时省钱）→ 全程接**可观测**（trace / 指标，第12章那套，哪步慢 / 错 / 贵都看得见、能报警）。这套架构是地基——它把『一个脚本』变成『一支能扛海量用户的正规军』。\n**再扛并发（并发异步）**：Agent 服务用 **async / await** 异步处理——等 LLM 那几秒不占住资源、能去推进别的请求；用 **Semaphore 限并发**（如最多 50）别把下游 LLM / 工具打爆；队列负责削峰，三者一起让系统在洪峰下不被冲垮。\n**接住失败（超时 / 重试 / 降级兜底）**：每次调 LLM / 工具都设**超时**（卡太久掐断，别拖垮服务）；幂等的查询失败了**重试**（带退避）；主模型挂了**降级**到备用模型、连备用也挂了返回**安全兜底答案**——客服 Agent 永远不把原始报错甩给用户。注意：『提交工单 / 发通知』这类**非幂等**操作要先用唯一请求 ID 去重，不能无脑重试。\n**控住上线风险（灰度 + 回滚）**：要频繁换模型 / 改 prompt，每次都走**灰度发布**——先 5% 流量盯成功率 / 延迟 / 成本，好再 50% → 100%；备好**一键回滚**，哪档指标变差立刻切回老版本，把『坏更新』的影响面和损失封顶。\n**怎么配合**：架构是地基（扛得住并发、看得见状态）→ 可靠性手段在地基上接住每一次必然发生的失败（用户体验不崩）→ 灰度 + 回滚守住每一次变更（上线从豪赌变小步）。三层叠起来，小特才从『能在你电脑上跑』变成『能稳稳上线服务真实用户』。**而这一章，也是小特成长的最后一块拼图**：篇1 它有了脑子（LLM）和提示、篇2 学会用 ReAct + 工具 + 记忆 + 规划办事、篇3 靠 RAG + 上下文工程查资料、篇4 会多智能体协作编排、篇5 装上评估可观测 + 安全护栏 + 这章的生产化部署——至此，一个从原理到能真正上线服务真实用户的完整 AI Agent，成型了。",
    tags: ["综合", "收官", "生产架构", "并发异步", "降级兜底", "灰度回滚"],
  },
];
