/** 复习题库 · RAG 检索增强生成（ai-rag）。AI Agent 开发实战·原创改编知识增强篇第 1 章。 */

import type { ReviewQuestion } from "./types";

export const aiRagQuestions: ReviewQuestion[] = [
  // ── L1 认记：术语 / 定义 ──
  {
    id: "ai-rag-1",
    chapter: "ai-rag",
    level: 1,
    question: "RAG 是什么的缩写？它的核心思路一句话怎么说？",
    answer:
      "RAG = **Retrieval-Augmented Generation**，检索增强生成。核心思路：给模型外挂一个**可检索的外部知识库**——回答前先**检索**出和问题相关的资料段落，再把它们**拼进提示**让模型基于这些段落生成答案。相当于把模型从「闭卷凭记忆答」改成「**开卷先翻资料再答**」。",
    tags: ["RAG", "定义"],
  },
  {
    id: "ai-rag-2",
    chapter: "ai-rag",
    level: 1,
    question: "什么是幻觉（hallucination）？RAG 为什么能减少它？",
    answer:
      "幻觉是模型在**不知道答案时不肯认错、反而编一个**听起来合理实则错误的回答（被「必须答点什么」逼出来的，尤其问到训练时没见过的私有 / 最新内容时）。RAG 减少幻觉的办法：**先检索真实资料、再让模型照着资料答**——从「闭卷瞎蒙」改成「开卷照抄」，答案有据可依，自然就不容易瞎编。",
    tags: ["幻觉", "定义"],
  },
  {
    id: "ai-rag-3",
    chapter: "ai-rag",
    level: 1,
    question: "什么是 embedding？它最关键的性质是什么？",
    answer:
      "embedding（嵌入）是把一段文本变成一串固定长度数字（一个**向量**）的过程，这串数字也叫 embedding。最关键的性质：**意思相近的文本，算出来的向量在空间里离得近**；意思无关的离得远。所以「两段文本意思有多近」就变成了「两个向量离得有多近」这个能算的问题。由专门的 embedding 模型算出，维度通常几百到几千。",
    tags: ["embedding", "向量", "定义"],
  },
  {
    id: "ai-rag-4",
    chapter: "ai-rag",
    level: 1,
    question: "什么是 chunk 切分？为什么要切，不能整篇文档直接用？",
    answer:
      "chunk 切分是把一份长文档切成一个个小片段（chunk）、每块再各自 embed 成向量。要切是因为**整篇算成一个向量既不精准也塞不下**——整篇的「意思」糊成一个点，问具体问题命中的是整篇而非相关那段，还塞不进上下文窗口。切成小块后，检索能精确命中相关的那一小块。",
    tags: ["chunk", "切分", "定义"],
  },
  {
    id: "ai-rag-5",
    chapter: "ai-rag",
    level: 1,
    question: "RAG 在线问答的核心两步是什么？分别对应 RAG 全名的哪部分？",
    answer:
      "① **检索（Retrieval）**——把用户问题也 embed 成向量，在库里找离它最近的 **top-k** 个 chunk（意思最相关的几段资料）；② **拼接生成（Augmented Generation）**——把检索到的 chunk 拼进提示「基于以下资料回答：…，问题：…」，让模型基于真实资料生成答案。对应 RAG 全名：R = 检索，A+G = 拿资料增强提示并生成。",
    tags: ["检索", "拼接生成", "定义"],
  },
  {
    id: "ai-rag-6",
    chapter: "ai-rag",
    level: 1,
    question: "什么是 top-k？什么是余弦相似度（cosine similarity）？",
    answer:
      "**top-k**：检索时取「最相似的前 k 个」结果——算出问题向量和每个 chunk 向量的相似度，按高到低排序，取前 k 个 chunk 作为命中。**余弦相似度**：衡量两个向量「方向有多一致」的相似度，等于夹角余弦值，范围 -1~1，越接近 1 越相似（意思越近）；算法上 = 两向量**点积 ÷ 各自长度的乘积**。RAG 检索几乎都用它打分、再取 top-k。",
    tags: ["top-k", "余弦相似度", "定义"],
  },
  {
    id: "ai-rag-7",
    chapter: "ai-rag",
    level: 1,
    question: "RAG 的「离线建库」和「在线问答」各包含哪些步骤？",
    answer:
      "**离线建库（一次性）**：文档 → 切成 chunk → 每块 embed 成向量 → 存进向量库（建索引）。**在线问答（每次提问才走）**：用户问题 → 问题也 embed 成向量 → 在库里检索 top-k 相似 chunk → 把 chunk 拼进提示 → LLM 基于资料生成答案。关键区别：建库文档不变就不用重建，问答每次提问都得走一遍。",
    tags: ["管线", "离线", "在线", "定义"],
  },
  // ── L2 理解：分辨 / 解释为什么 ──
  {
    id: "ai-rag-8",
    chapter: "ai-rag",
    level: 2,
    question: "LLM 有哪两个硬伤，导致它需要 RAG？",
    answer:
      "① **知识在训练那一刻就冻住了**——训练之后发生的事、你的私有资料，它统统没见过；② **不知道也不说「不知道」，而是编一个**（幻觉）。两条合起来，模型像个「只能闭卷、还不许交白卷」的考生，超纲题只能瞎蒙。RAG 给它外挂可检索的知识库，让私有 / 最新内容能被查到、让它照真实资料答，正好对症这两个硬伤。",
    tags: ["RAG", "动机", "理解"],
  },
  {
    id: "ai-rag-9",
    chapter: "ai-rag",
    level: 2,
    question:
      "问题和 chunk 用了不同的 embedding 模型会怎样？为什么必须用同一个？",
    answer:
      "会导致**检索结果一团乱、相关的根本捞不回来**。因为不同 embedding 模型算出的向量**不在同一个坐标系**里，跨坐标系算出的「距离」毫无意义。embedding 的魔法（意思近则向量近）只在**同一套坐标系**内成立。所以建库和查询必须用**同一个 embedding 模型 / 同一个 `embed` 函数**；换了模型，整个索引要重新 embed 重建，不能新旧混用。",
    tags: ["embedding", "一致性", "理解"],
  },
  {
    id: "ai-rag-10",
    chapter: "ai-rag",
    level: 2,
    question: "chunk 切太大和切太小各有什么毛病？为什么常要加重叠（overlap）？",
    answer:
      "**切太大**：一块塞了一大段，检索时命中里混进无关内容、不精准，还更占上下文窗口。**切太小**：一句完整的话被拦腰切断，丢了上下文。**加重叠**：让相邻块共享开头几个字，免得一句话刚好卡在两块接缝处被切散——保住跨块句子的连贯。所以「块大小 + 重叠」是检索质量的第一道闸，要按效果调，不是随便切。",
    tags: ["chunk", "重叠", "理解"],
  },
  {
    id: "ai-rag-11",
    chapter: "ai-rag",
    level: 2,
    question:
      "「检索回来的 top-k 不等于都相关」——为什么？会带来什么问题、怎么缓解？",
    answer:
      "因为 **top-k 只保证「最近的 k 个」，不保证「都相关」**——`k` 设大了、或库里压根没有相关资料时，照样会把离题的 chunk 凑进 top-k。问题：不相关的内容塞进提示会把模型带偏、还占窗口。缓解：`k` 取适中；给相似度设个**阈值**，太低的命中直接丢掉；提示里写明「**资料里没有就说不知道**」，别逼模型硬答。",
    tags: ["top-k", "阈值", "理解"],
  },
  {
    id: "ai-rag-12",
    chapter: "ai-rag",
    level: 2,
    question:
      "本章兑现了第 6 章《记忆系统》留的什么坑？语义检索的底层到底是什么？",
    answer:
      "第 6 章只给了「**按意思找回，不是按关键词**」这个直觉，并说「向量、embedding 怎么算、怎么比相似度，留到 RAG 这章掰开」。本章掰开了：底层就是 **embedding + 余弦相似度**——把文本 embed 成**向量**（意思近则向量近），再用 **cosine** 算问题向量和记忆 / chunk 向量的距离，取最近的 top-k。所以「比意思」= 「比向量距离」，这就是语义检索能凭意思找回字面不同内容的原因。",
    tags: ["承接", "语义检索", "理解"],
  },
  {
    id: "ai-rag-13",
    chapter: "ai-rag",
    level: 2,
    question:
      "为什么 RAG 不是「把整篇文档塞进 prompt 让模型读」？它和这种做法的关键区别在哪？",
    answer:
      "因为整篇文档（几万字）**塞不进上下文窗口**——会超长报错、又慢又贵，而且大量无关内容会淹没重点。RAG 的关键恰恰是**只检索相关的几个 chunk** 拼进提示，而不是把全部资料灌进去。区别：整文档塞 = 不加筛选地全灌；RAG = 先**切块建库**、提问时只**检索 top-k 相关片段**。检索相关片段才是 RAG 省窗口、又精准的核心。",
    tags: ["RAG", "误区", "理解"],
  },
  {
    id: "ai-rag-14",
    chapter: "ai-rag",
    level: 2,
    question:
      "`rag_answer` 的提示里写「只用资料里的信息，资料里没有就说不知道」——这句有什么用？去掉会怎样？",
    answer:
      "这句是**把模型摁在「开卷」上的关键**——明确要求它只依据检索回来的资料答、查不到就老实说不知道，不准自由发挥。去掉这句，模型容易**脱离资料、回到凭记忆瞎编的老路**（幻觉）：检索到的 chunk 只是参考，它可能掺进训练记忆甚至无视资料。所以好的 RAG 提示一定要约束模型「基于资料、资料没有就别硬答」。",
    tags: ["提示", "幻觉", "理解"],
  },
  // ── L3 应用：判断 / 套用到场景 / 写代码 ──
  {
    id: "ai-rag-15",
    chapter: "ai-rag",
    level: 3,
    question:
      "用户问「退货政策是几天？」，库里有「自收货起 7 天内可无理由退货」——两句没一个共同关键词。RAG 凭什么检索回来？换成关键词匹配会怎样？",
    answer:
      "凭 **embedding + 余弦相似度的语义检索**：把问题和每个 chunk 用**同一个** embedding 模型变成**向量**，用 **cosine** 算「问题向量和 chunk 向量有多近」。「退货政策几天」和「7 天内可退货」字面不重叠但**意思很近**，向量离得近、cosine 分高，被排进 top-k 捞回，再拼进提示让模型照它答。「检索」比的是**意思（向量距离）**，不是字面。换成**关键词匹配**：只找含「政策 / 几天」字样的条目，而那句写的是「七天内」，于是**一无所获**，模型只能退回闭卷瞎编（如「一般 30 天」）。",
    tags: ["语义检索", "场景", "应用"],
  },
  {
    id: "ai-rag-16",
    chapter: "ai-rag",
    level: 3,
    question:
      "「动手二」语义空间 Demo 里，查询「出门要带伞吗？」把 k 从 1 调到 3，命中里多出来的点很可能不在天气簇里。为什么？这说明 k 调大有什么风险？",
    answer:
      "因为天气簇里真正相关的点就那么几个，k=1、k=2 命中的都是天气簇（意思最近）；k=3 时簇里不够选，**第 3 近的点很可能已经是隔壁簇里离查询相对没那么远的一个**——它其实没那么相关，只是「排第 3 近」而已。这说明 `k` 调大的风险：**top-k 只保证「最近的 k 个」，不保证「都相关」**——簇里相关资料就那么几条，k 一旦超过它，多出的名额只能拿不相关 chunk 来凑，把无关内容塞进提示、干扰模型。所以 k 要适中、必要时加相似度阈值。",
    tags: ["top-k", "改Demo", "应用"],
  },
  {
    id: "ai-rag-17",
    chapter: "ai-rag",
    level: 3,
    question:
      "请写出一个最简 RAG 的核心逻辑（伪代码即可）：建库（切块 + embed 存索引）、检索（cosine 取 top-k）、拼接生成（拼进提示调模型）。",
    answer:
      "① **建库（离线）**：`INDEX = [(c, embed(c)) for c in chunk(DOC)]`——把文档 `chunk` 切块、每块 `embed` 成向量，存成 `(chunk, 向量)` 列表（真实里存进向量库）。② **检索（在线）**：`retrieve(query, k)` 把 `query` 也 `embed`，对 `INDEX` 每条算 `cosine(qv, vec)`，按分数从高到低排序，返回 top-k 个 chunk。③ **拼接生成（在线）**：`rag_answer` 调 `retrieve` 拿回命中，拼成提示「基于以下资料回答：{chunks}，问题：{query}（资料没有就说不知道）」，调 LLM 生成。要点：建库一次性、问答每次走；问题和 chunk 必须用**同一个 embed**；`k` 适中。",
    tags: ["独立实现", "最简RAG", "应用"],
  },
  {
    id: "ai-rag-18",
    chapter: "ai-rag",
    level: 3,
    question:
      "一个 RAG 系统检索老是命中不准：要么捞回一大段里只有一句相关，要么相关句子根本捞不全。最可能是哪个环节的问题？怎么改？",
    answer:
      "最可能是 **chunk 切分**这一环——它是检索质量的**第一道闸**。捞回「一大段里只一句相关」是**块切太大**（一块塞太多内容、检索不精准）；相关句子「捞不全」往往是**块太小或没重叠**（一句话被接缝切散）。改法：把块大小调到让每块大致是「一两句完整的话」，并加一点**重叠（overlap）**让跨块句子不被切断；再按检索效果反复调这两个旋钮。（其次才排查 embedding 是否一致、k 与阈值是否合理。）",
    tags: ["chunk", "调试", "应用"],
  },
  // ── L4 综合：贯通多个概念 ──
  {
    id: "ai-rag-19",
    chapter: "ai-rag",
    level: 4,
    question:
      "把本章串起来：以「让小特照公司手册回答退货政策」为例，完整讲清 RAG 为什么能把闭卷改成开卷、整条管线哪段离线哪段在线、embedding/chunk/cosine/top-k 各在哪一步起作用，并说清它怎么兑现第 6 章语义检索的承诺、怎么减少幻觉。",
    answer:
      "走一遍。**为什么需要**：小特的知识训练时就冻住、没见过你们公司手册，硬问退货政策只会**闭卷瞎编**（幻觉，如「一般 30 天」）。RAG 给它外挂可检索的知识库，改成**开卷**。**离线建库（一次性）**：把手册 **chunk 切分**成小块（块太大不精准 / 太小丢上下文，常加 overlap——检索质量第一道闸），每块 **embed 成向量**（embedding：意思近则向量近，兑现第 6 章「按意思找回」欠的底层），存成 `(chunk, 向量)` 索引。**在线问答（每次提问才走）**：用户问「退货政策几天？」→ 问题也 **embed**（必须用**同一个 embedding 模型**，才在同一坐标系比远近）→ **检索**：用 **余弦相似度** 算问题向量和每个 chunk 向量的距离，取 **top-k** 最相关的（「退货几天」命中「7 天内可退货」靠的是意思近、cosine 分高，字面不重叠也行）→ **拼接生成**：把命中拼进提示「基于以下资料回答：自收货起 7 天内可退货，问题：退货政策几天？（资料没有就说不知道）」→ LLM 照真实资料答「7 天」。**怎么减少幻觉**：模型不再凭训练记忆硬撑，而是照检索回来的真实手册答，还被提示约束「资料没有就说不知道」，自然不瞎编。**怎么兑现第 6 章承诺**：第 6 章只给「按意思找回」的直觉，这里把它落成 embedding（文本→向量）+ cosine（比向量距离）+ top-k（取最近几个）的真实代码——「比意思」就是「比向量距离」。一句话：离线把手册变成可查的向量库，在线检索相关片段拼进提示让模型开卷答。",
    tags: ["综合", "管线", "承接", "幻觉"],
  },
];
