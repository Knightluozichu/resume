import type { ReviewQuestion } from "./types";

export const aupMinifaceQuestions: ReviewQuestion[] = [
  {
    id: "aup-miniface-01",
    chapter: "aup-miniface",
    level: 1,
    question: "「机制与策略分离」是什么意思？举例说明。",
    answer: "机制与策略分离：机制提供「如何做」的能力（通用、稳定），策略决定「做什么」的选择（具体、灵活）。举例：① 文件系统提供读写机制（open/read/write），备份策略（何时备份、备份到哪）由用户决定；② Shell 提供管道机制，具体用哪些工具组合是用户的策略；③ 正则引擎提供匹配机制，匹配什么模式是用户的策略；④ sort 提供排序机制，按什么字段排序、升序还是降序是用户的策略。分离的好处：机制稳定不需频繁修改，策略灵活可由用户自定义。违反此原则的后果：机制中硬编码策略，导致灵活性丧失，每换一个场景就要改机制代码。",
    tags: ["机制与策略", "分离原则", "接口设计"],
  },
  {
    id: "aup-miniface-02",
    chapter: "aup-miniface",
    level: 1,
    question: "CLI 设计中「沉默是金」原则的含义是什么？",
    answer: "「沉默是金」含义：程序成功执行时不产生多余输出，只在出错时才输出错误信息。原因：① 便于管道处理——成功时输出干净的数据，不被装饰性内容污染；② 适合脚本自动化——脚本可通过退出码判断成功与否，不需要解析输出；③ 减少噪音——用户不需要看到每次成功的确认。举例：rm 删除文件成功时不输出任何内容，只有出错时才报错；mkdir 创建目录成功时也沉默。如果需要确认输出，应提供 -v/--verbose 选项让用户主动选择。反面例子：某些程序成功时输出大量「Done!」「Success!」等信息，破坏了管道数据流。原则：成功沉默，失败有用。",
    tags: ["沉默是金", "CLI设计", "管道友好"],
  },
  {
    id: "aup-miniface-03",
    chapter: "aup-miniface",
    level: 2,
    question: "Postel 法则（宽容原则）在接口设计中如何应用？",
    answer: "Postel 法则：对自己发送的东西要保守，对自己接受的东西要宽容（Be conservative in what you send, be liberal in what you accept）。在接口设计中应用：① 输入宽容——接受大小写不敏感的参数（--HELP 和 --help 都可）；忽略多余的空白；接受多种日期格式；对缺失的可选参数提供默认值。② 输出严格——输出格式严格遵循规范（JSON 格式正确、字段类型一致）；退出码遵循标准约定；错误信息格式统一。好处：降低使用门槛，用户不需要精确记住每个参数格式；同时保证输出可被下游工具可靠解析。注意：宽容不等于无底线——对危险操作仍需严格确认，对明显错误的输入应给出清晰提示而非默默接受。",
    tags: ["Postel法则", "宽容原则", "接口设计"],
  },
  {
    id: "aup-miniface-04",
    chapter: "aup-miniface",
    level: 3,
    question: "设计一个命令行工具的接口时，如何决定一个功能是默认行为还是可选选项？",
    answer: "决策流程：核心问题是「这个功能是否所有用户都需要？」。如果是——设为默认行为，不需要 flag；如果否——设为可选选项，通过 flag 开启。具体判断：① 使用频率——80% 以上场景需要的功能设为默认，否则设为选项；② 安全性——破坏性操作（删除、覆盖）即使常用也应设为选项而非默认，需用户显式确认；③ 可预测性——默认行为应是最不令人惊讶的（最小惊讶原则）；④ 可组合性——默认行为应支持管道（输出到 stdout 而非直接修改文件）。举例：grep 默认输出匹配行（所有用户都需要），-i 忽略大小写是选项（部分场景需要）；sort 默认升序（最常见），-r 降序是选项。如果不确定，宁可设为选项而非默认——默认行为一旦确定就很难改变（向后兼容）。",
    tags: ["接口最小化", "默认行为", "CLI设计", "决策流程"],
  },
];
