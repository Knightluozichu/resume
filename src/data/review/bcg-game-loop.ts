import type { ReviewQuestion } from "./types";

/** C++ 游戏编程入门 · 游戏循环复习题 */
export const bcgGameLoopQuestions: ReviewQuestion[] = [
  {
    id: "bcg-game-loop-1",
    chapter: "bcg-game-loop",
    level: 1,
    question: `游戏循环的标准五个阶段是什么？为什么游戏要循环而不是顺序执行到底？`,
    answer:
      `五阶段：初始化 → 处理输入 → 更新状态 → 渲染 → 检测退出（未退出则回到处理输入）。\n\n游戏要循环因为游戏是**持续运行、实时响应**的程序：画面要每帧重画、玩家输入要随时响应、世界状态要不断推进（敌人移动、计时流逝）。顺序执行到底只能跑一次就结束，无法实现「持续变化的世界」。循环让程序在「玩家未退出」这个条件下反复执行输入-更新-渲染，形成持续的游戏体验。\n\n本质：游戏循环是游戏的「心跳」，没有循环就没有实时游戏。`,
    tags: ["游戏循环", "五阶段", "实时", "心跳"],
  },
  {
    id: "bcg-game-loop-2",
    chapter: "bcg-game-loop",
    level: 2,
    question: `为什么「更新状态」和「渲染」要分成两步，而不是边更新边画？`,
    answer:
      `分离更新与渲染是关键设计：\n1. 逻辑清晰：更新阶段只管「世界变成什么样」（移动、碰撞、AI），渲染阶段只管「怎么画出来」。职责单一，调试容易——逻辑错了查 update，画面错了查 draw。\n2. 数据一致：一帧内所有实体先统一更新完，再统一渲染，保证画出来的是「同一时刻」的世界快照。若边更新边画，先更新的实体已移动、后更新的还在旧位置，画面撕裂、状态不一致。\n3. 可扩展：未来可让更新频率和渲染频率不同（固定时间步长更新 + 自由帧率渲染），分离后才好做这种优化。\n4. 复用：同一份更新逻辑可配不同渲染（如正常/调试视图），不分离则耦合死。\n\n一句话：更新算「是什么」，渲染算「画什么」，分开才不乱。`,
    tags: ["更新", "渲染", "分离", "一致性"],
  },
  {
    id: "bcg-game-loop-3",
    chapter: "bcg-game-loop",
    level: 3,
    question: `什么是帧率（FPS）？为什么不同机器帧率不同会让游戏速度不一致？如何用「时间增量 delta time」解决？`,
    answer:
      `FPS = 每秒渲染帧数。若游戏循环每帧把物体移动固定量（如 \`x += 5\`），快机器 FPS 高、每秒移动多，慢机器 FPS 低、每秒移动少——游戏速度随机器变化，这在竞技游戏里不可接受。\n\n用 delta time（dt，上一帧到本帧的实际耗时秒数）解决：移动改为 \`x += speed * dt\`，speed 是「每秒移动多少」。这样无论 FPS 多少，每秒移动量恒定等于 speed。例如 speed=100 像素/秒，60fps 时 dt≈0.0167 每帧移 1.67 像素，30fps 时 dt≈0.0333 每帧移 3.33 像素，一秒都移 100 像素。\n\nSFML 用 \`clock.restart().asSeconds()\` 取 dt。这是「帧率无关」移动的标准做法。进一步可用固定时间步长（fixed timestep）保证物理确定性。`,
    tags: ["帧率", "FPS", "delta time", "帧率无关", "应用"],
  },
  {
    id: "bcg-game-loop-4",
    chapter: "bcg-game-loop",
    level: 4,
    question: `综合分析：一个游戏循环里如果「处理输入 + 更新」耗时过长导致掉帧，会引发什么连锁问题？如何从循环结构和时间管理上缓解？`,
    answer:
      `连锁问题：\n1. 渲染延迟：单帧耗时超过 16.7ms（60fps 预算），FPS 掉到 30 以下，画面卡顿。\n2. 输入延迟：输入要等当前帧更新完才处理，玩家操作响应慢，手感差。\n3. 死亡螺旋：若用 dt 直接缩放移动，掉帧使 dt 变大，物体单帧位移变大，可能穿透碰撞体（tunneling），碰撞检测失效；下一帧 AI/物理因穿透做更多修正，耗时更长，dt 更大，越陷越深。\n4. 逻辑溢出：超大 dt 可能让计时器跳过事件、状态机错过转移。\n\n缓解手段：\n1. 固定时间步长更新：把更新拆成固定 dt（如 1/60）的多次小步，渲染用插值。逻辑确定、不受帧率影响，是治本方案。\n2. dt 封顶：\`dt = min(dt, 0.05)\` 防止单帧 dt 过大引发死亡螺旋，宁可慢放也不爆炸。\n3. 子步进碰撞：对高速物体在一帧内细分多步做碰撞检测，防穿透。\n4. 性能预算：把重活（AI、物理）分摊到多帧或异步，避免单帧爆掉。\n5. 输入早处理：循环开头先 poll 输入，缩短响应链路。\n\n综合：掉帧不只是「卡」，会通过 dt 放大破坏逻辑正确性。核心对策是把「逻辑更新」与「渲染帧率」解耦——固定步长更新 + 渲染插值 + dt 封顶，三者配合让游戏在性能波动下仍稳定。这也是从「能跑的游戏循环」升级到「健壮游戏循环」的分水岭。`,
    tags: ["综合", "掉帧", "死亡螺旋", "固定时间步长", "dt 封顶", "性能"],
  },
];
