import type { ReviewQuestion } from "./types";

/** C++ 游戏编程入门 · 综合游戏项目复习题 */
export const bcgGameProjectQuestions: ReviewQuestion[] = [
  {
    id: "bcg-game-project-1",
    chapter: "bcg-game-project",
    level: 1,
    question: "一个游戏项目通常分哪四大模块？Game 主类在其中扮演什么角色？",
    answer:
      "四大模块：\n1. 实体管理：管所有游戏对象（玩家、敌人、子弹）的集合与生命周期。\n2. 状态机：管游戏阶段切换（菜单/游玩/暂停/结束）。\n3. 资源管理：加载、缓存、复用纹理/字体/音效。\n4. 输入与渲染：读玩家输入、把画面画到窗口。\n\nGame 主类是「骨架」，持有这四模块的实例并在主循环里调度它们：每帧先让输入模块读输入、状态机决定当前阶段、实体管理更新所有对象、渲染模块画出结果。Game 自己不写具体游戏逻辑，只负责把模块串起来。这样分工后，新增关卡/敌人只动对应模块，不动 Game 主循环。",
    tags: ["架构", "四大模块", "Game 主类", "分工"],
  },
  {
    id: "bcg-game-project-2",
    chapter: "bcg-game-project",
    level: 2,
    question: "用状态机管理游戏阶段相比用一堆布尔标志（isMenu/isPlaying/isGameOver）有什么好处？",
    answer:
      "状态机把「当前处于哪个阶段」抽象成单一状态变量 + 每个状态独立的更新/渲染/输入逻辑，好处：\n1. 互斥保证：任一时刻只有一个状态，不会出现「同时是菜单又是游戏」的非法组合。布尔标志组合可能出现多个为真的矛盾状态。\n2. 转移明确：状态间转移写清楚（菜单→开始→游玩→死亡→结束→菜单），转移时可执行进入/退出动作（如进入暂停时存档、退出时清理）。\n3. 扩展容易：加新阶段（如设置界面）只需新增一个 State 子类，不动其他状态逻辑。布尔标志法要在每个 if 分支里塞新条件，越加越乱。\n4. 逻辑隔离：每个状态的 update/draw/handleInput 分开写，菜单逻辑不与战斗逻辑搅在一起，可读性强。\n\n布尔标志适合极简游戏（两三状态）；状态多、转移复杂时状态机远胜。",
    tags: ["状态机", "布尔标志", "状态转移", "扩展"],
  },
  {
    id: "bcg-game-project-3",
    chapter: "bcg-game-project",
    level: 3,
    question: "资源管理器为什么要把资源「按 id 缓存复用」？如果每次用纹理都 loadFromFile 会怎样？",
    answer:
      "按 id 缓存复用：资源管理器维护一个 map<id, 资源>，首次请求时加载并存入，后续请求同 id 直接返回已加载的，全程只加载一次。\n\n每次 loadFromFile 的问题：\n1. 慢：从磁盘读文件 + 解码图片是毫秒级操作，每帧或频繁加载会卡顿。\n2. 浪费内存：同一图片加载多份占多倍显存，100 个敌人就是 100 份相同纹理。\n3. 句柄失控：Texture 必须在使用期间存活，散落各处的 Texture 生命周期难管，易出现「纹理先销毁精灵画错乱」。\n\n缓存复用解决全部：加载一次省时、共享一份省内存、管理器持有保证存活。典型实现是 TextureHolder 类，`get(id)` 返回引用，`load(id, path)` 加载。游戏初始化时预加载常用资源，运行时按需补载。\n\n这是从「能跑」到「不卡不漏」的关键一环。",
    tags: ["资源管理", "缓存", "复用", "loadFromFile", "性能"],
  },
  {
    id: "bcg-game-project-4",
    chapter: "bcg-game-project",
    level: 4,
    question: "综合分析：把前面学的 C++ 基础、游戏循环、SFML、碰撞检测整合成一个完整项目时，最容易在哪几处出错？如何用架构预防？",
    answer:
      "易错处与预防：\n\n1. 指针与生命周期：实体用 `vector<Entity*>` 多态管理，new 了忘 delete 会泄漏，delete 后仍访问会崩溃。\n预防：用智能指针（unique_ptr/shared_ptr）或统一在实体管理器里集中管理生死，配合虚析构保证派生类正确清理。\n\n2. 资源悬空：Sprite 引用的 Texture 被提前销毁，画面错乱。\n预防：资源管理器持有所有 Texture 的所有权，生命周期与游戏一致；Sprite 只引用不拥有。\n\n3. 游戏循环未帧率无关：移动用固定量，快慢机速度不同。\n预防：循环里取 delta time，所有移动用 `speed * dt`；进阶用固定时间步长更新。\n\n4. 状态转移遗漏清理：从游玩切到菜单时残留子弹/敌人。\n预防：状态机设进入/退出钩子，退出 PlayState 时清空实体列表、重置计时器。\n\n5. 碰撞穿透/漏检：高速子弹跨过敌人。\n预防：粗筛 + 必要时子步进；碰撞响应与检测分离。\n\n6. 头文件循环依赖：Entity 引用 Game，Game 引用 Entity，编译不过。\n预防：前向声明 + 指针/引用成员，实现放 cpp；或用接口（纯虚基类）解耦。\n\n7. 上帝对象：Game 类塞满所有逻辑，变得臃肿难改。\n预防：Game 只做调度，具体逻辑下沉到各模块/实体；遵循单一职责。\n\n架构层面的总原则：用「拥有者-引用者」分清所有权（智能指针管拥有、裸指针/引用管查看）、用「状态机」管阶段、用「资源管理器」管共享资源、用「虚函数接口」管多态、用「delta time」管时间。把这几条做对，整合时的大部分坑都被架构本身挡住，而不是靠写代码时小心。这正是本书从零散知识走向工程化项目管理的核心收获。",
    tags: ["综合", "整合", "生命周期", "智能指针", "循环依赖", "上帝对象", "架构预防"],
  },
];
