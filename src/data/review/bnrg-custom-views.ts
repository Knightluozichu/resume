import type { ReviewQuestion } from "./types";
export const bnrgCustomViewsQuestions: ReviewQuestion[] = [
  { id: "bnrg-custom-1", chapter: "bnrg-custom-views", level: 1, question: "自定义 View 的三个核心方法是什么？各自的作用？", answer: "`onMeasure(widthMeasureSpec, heightMeasureSpec)`：测量自己应该多大——解析父容器给的 MeasureSpec 约束，调用 `setMeasuredDimension(w, h)` 确定最终尺寸。`onDraw(canvas: Canvas)`：在 Canvas 上绘制内容——drawRect、drawCircle、drawText、drawPath、drawBitmap。`onTouchEvent(event: MotionEvent)`：处理触摸事件——ACTION_DOWN/MOVE/UP。", tags: ["onMeasure", "onDraw", "onTouchEvent"] },
  { id: "bnrg-custom-2", chapter: "bnrg-custom-views", level: 1, question: "MeasureSpec 的三种模式各代表什么？XML 中的 layout_width/height 分别对应哪种模式？", answer: "EXACTLY：精确尺寸——对应 `match_parent` 或具体 dp 值（如 100dp）。AT_MOST：上限——对应 `wrap_content`，View 不能超过父容器给的上限。UNSPECIFIED：无限制——如 ScrollView 内的 View 在滚动方向上没有限制。通过 `MeasureSpec.getMode(spec)` 和 `MeasureSpec.getSize(spec)` 解析。", tags: ["MeasureSpec", "EXACTLY", "AT_MOST", "UNSPECIFIED"] },
  { id: "bnrg-custom-3", chapter: "bnrg-custom-views", level: 2, question: "requestLayout() 和 invalidate() 分别触发什么流程？什么时候该用哪个？", answer: "`requestLayout()`：触发 onMeasure → onLayout → onDraw 完整流程——当 View 的尺寸或位置发生变化时调用。`invalidate()`：仅触发 onDraw 重绘——当只有视觉内容（颜色、文本、透明度）发生变化时调用。判断法则：影响尺寸 → requestLayout()；只影响视觉 → invalidate()。", tags: ["requestLayout", "invalidate", "重绘"] },
  { id: "bnrg-custom-4", chapter: "bnrg-custom-views", level: 2, question: "GestureDetector 能识别哪些常见手势？onDown() 为什么必须返回 true？", answer: "支持的手势：`onSingleTapUp`（单击）、`onDoubleTap`（双击）、`onLongPress`（长按）、`onFling`（快速滑动）、`onScroll`（拖动）。`onDown()` 必须返回 true——因为所有后续手势都以 DOWN 事件为起点。如果 onDown 返回 false，GestureDetector 会认为「用户不关心这个 View」，后续所有手势回调都不会触发。", tags: ["GestureDetector", "手势识别"] },
  { id: "bnrg-custom-5", chapter: "bnrg-custom-views", level: 3, question: "自定义 View 设置了 wrap_content 但实际效果跟 match_parent 一样——为什么？怎么修？", answer: "默认 View 的 onMeasure 实现里，AT_MOST 模式下也会调用 `setMeasuredDimension(specSize, specSize)` 填满父容器。修复方法：在 onMeasure 中判断——当 specMode 为 AT_MOST 时，把 measuredSize 设为内容实际需要的尺寸（而非 specSize）。例如：`val w = if (widthMode == MeasureSpec.AT_MOST) desiredContentWidth else widthSize`。", tags: ["wrap_content", "onMeasure", "bug"] },
  { id: "bnrg-custom-6", chapter: "bnrg-custom-views", level: 4, question: "说出自定义 View 中自定义属性（declare-styleable）的完整实现步骤——从 attrs.xml 到布局 XML。", answer: "① `res/values/attrs.xml` 中声明 `<declare-styleable name=\"MyView\"><attr name=\"myColor\" format=\"color\"/></declare-styleable>`。② View 构造方法中：`context.obtainStyledAttributes(attrs, R.styleable.MyView).apply { myColor = getColor(R.styleable.MyView_myColor, defaultColor); recycle() }`。③ 布局 XML 中：`<com.example.MyView app:myColor=\"#FF0000\" .../>`。注意 `recycle()` 必须调用，否则内存泄漏。", tags: ["declare-styleable", "自定义属性"] },
];
