import type { ReviewQuestion } from "./types";
export const bnrgPropertyAnimationQuestions: ReviewQuestion[] = [
  { id: "bnrg-anim-1", chapter: "bnrg-property-animation", level: 1, question: "属性动画和补间动画（View Animation）的本质区别是什么？为什么说属性动画是「真正的动画」？", answer: "补间动画只修改 View 的绘制矩阵（视觉位置），View 的真实属性和点击区域不变——你看着按钮飘到右边，但点击原位置才有反应。属性动画直接修改对象的真实属性（translationX、alpha 等），位置和点击区域都跟着走。一句话：补间动画「看起来动了」，属性动画「真的动了」。", tags: ["属性动画", "补间动画", "对比"] },
  { id: "bnrg-anim-2", chapter: "bnrg-property-animation", level: 1, question: "ValueAnimator 和 ObjectAnimator 有什么区别？分别适用于什么场景？", answer: "`ValueAnimator`：只生成值（from → to），通过 addUpdateListener 获取当前值，由你决定怎么用——适合非 View 对象或需要自定义更新逻辑的场景。`ObjectAnimator`：ValueAnimator 的子类，通过反射自动调用目标对象的 setter 修改属性——适合直接对 View 属性做动画（alpha、translationX 等）。选择：要对非 View 对象动画 → ValueAnimator；要对 View 属性做简单动画 → ObjectAnimator 或 view.animate()。", tags: ["ValueAnimator", "ObjectAnimator", "对比"] },
  { id: "bnrg-anim-3", chapter: "bnrg-property-animation", level: 2, question: "下列插值器各用在什么场景：AccelerateDecelerate / Bounce / Overshoot / Linear？", answer: "AccelerateDecelerate：慢→快→慢，最自然的过渡，适用大部分入场/出场动画。Bounce：末尾弹跳，适合元素「落下弹起」（如列表项添加、消息到达）。Overshoot：冲过头再回来，俏皮弹性感（如按钮弹入、卡片入场）——注意别滥用。Linear：匀速，适合进度条、旋转加载等需要恒速连续动的场景。", tags: ["Interpolator", "插值器", "场景选择"] },
  { id: "bnrg-anim-4", chapter: "bnrg-property-animation", level: 2, question: "AnimatorSet 的 playTogether 和 playSequentially 有什么区别？play(a).with(b).after(c) 的执行顺序是什么？", answer: "`playTogether`：包含的所有动画同时开始播放。`playSequentially`：动画按传入顺序一个接一个播放。`play(a).with(b).after(c)` 表示 c 先播放 → c 结束后 a 和 b 同时开始。build 方法：`play(a).with(b)` = a 和 b 同时播放；`play(b).after(a)` = b 在 a 之后播放。", tags: ["AnimatorSet", "编排"] },
  { id: "bnrg-anim-5", chapter: "bnrg-property-animation", level: 3, question: "ObjectAnimator 调了但不动、也不报错——最可能的原因是什么？", answer: "目标对象缺少对应属性的 setter 方法。ObjectAnimator 通过反射调用 setter——如 `ofFloat(view, \"progress\", 0f, 1f)` 要求 view 有 `setProgress(Float)` 方法。如果没有对应 setter，动画静默失败（不崩溃、不生效）。解决：① 给目标对象添加对应的 setter；② 改用 ValueAnimator 在 UpdateListener 中手动更新。", tags: ["ObjectAnimator", "静默失败", "setter"] },
  { id: "bnrg-anim-6", chapter: "bnrg-property-animation", level: 4, question: "Activity 退出后属性动画可能导致内存泄漏——原因是什么？如何防止？", answer: "原因：属性动画持有对 View 的强引用（Animator → View → Activity），动画不取消→View 不释放→Activity 无法 GC。解决方案：① Activity 的 `onDestroy()` 中调 `animator.cancel()`，CancelListener 中置空 View 引用；② 对于 View 的简单动画，优先用 `view.animate()`——它自动跟随 View 生命周期，View detach 时自动取消。", tags: ["内存泄漏", "Animator", "生命周期"] },
];
