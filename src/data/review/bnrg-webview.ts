import type { ReviewQuestion } from "./types";
export const bnrgWebviewQuestions: ReviewQuestion[] = [
  { id: "bnrg-webview-1", chapter: "bnrg-webview", level: 1, question: "WebViewClient 和 WebChromeClient 分别负责什么？各举一个最常用的回调方法。", answer: "WebViewClient 负责页面导航：`shouldOverrideUrlLoading`（拦截 URL）、`onPageStarted/Finished`（页面加载事件）、`onReceivedSslError`（SSL 错误）。WebChromeClient 负责 UI 相关：`onProgressChanged`（加载进度）、`onReceivedTitle`（页面标题）、`onJsAlert`（JS 弹窗）。记忆方法：Client 管「导航逻辑」，ChromeClient 管「浏览器 UI 外观」。", tags: ["WebView", "WebViewClient", "WebChromeClient"] },
  { id: "bnrg-webview-2", chapter: "bnrg-webview", level: 2, question: "addJavascriptInterface 和 evaluateJavascript 有什么区别？JS 调 Android 和 Android 调 JS 分别用哪个？", answer: "`addJavascriptInterface`：把 Kotlin 对象暴露到 JS 全局作用域，JS 可以直接调 Kotlin 方法（JS→Android）。`evaluateJavascript`：在 WebView 的 JS 引擎中执行一段 JS 代码字符串并获取返回值（Android→JS）。addJavascriptInterface 必须在 loadUrl 之前调用；evaluateJavascript 必须在 onPageFinished 之后调用。", tags: ["JavaScript", "addJavascriptInterface", "evaluateJavascript"] },
  { id: "bnrg-webview-3", chapter: "bnrg-webview", level: 2, question: "WebView 中出现 SSL 证书错误时，为什么不能直接调用 handler.proceed()？正确做法是什么？", answer: "`handler.proceed()` 会忽略证书错误、继续加载页面——等于把用户暴露在中间人攻击之下，攻击者可以伪造任意 HTTPS 页面。正确做法：Debug 阶段可以用 `if (BuildConfig.DEBUG) handler.proceed()`；Release 生产环境必须 `handler.cancel()` 并提示用户页面不安全、无法加载。", tags: ["SSL", "安全", "onReceivedSslError"] },
  { id: "bnrg-webview-4", chapter: "bnrg-webview", level: 3, question: "WebView 最常见的内存泄漏场景是什么？如何在 Activity 的 onDestroy 中正确释放？", answer: "WebView 持有 Activity 的 Context 引用，内部有独立渲染线程和 JS 引擎——退出 Activity 后如果不销毁，这些线程和引擎还活着，引用链不断导致 Activity 无法被 GC。解决方案：① `onDestroy()` 中调用 `webView.destroy()`；② 更彻底的做法：不在 XML 中声明 WebView，用代码动态创建并传入 `applicationContext`；③ 必要时把 WebView 放在独立进程中。", tags: ["内存泄漏", "onDestroy", "WebView"] },
  { id: "bnrg-webview-5", chapter: "bnrg-webview", level: 3, question: "WebView 加载 HTTPS 页面出现空白——可能是什么原因？有哪些排查方向？", answer: "最常见原因：Android 5.0+ 默认阻止混合内容（HTTPS 页面内嵌 HTTP 资源如图片/CSS/JS）。排查方向：① 检查 `webView.settings.mixedContentMode` 是否为 NEVER_ALLOW；② 打开 Chrome 的远程调试（`chrome://inspect`）查看 JS Console 错误；③ 检查网络权限和 HTTPS 证书链是否完整。临时解决：设为 `MIXED_CONTENT_COMPATIBILITY_MODE`（仅开发环境）。", tags: ["混合内容", "HTTPS", "调试"] },
  { id: "bnrg-webview-6", chapter: "bnrg-webview", level: 4, question: "WebView 的返回键处理：用户浏览多页后按返回键应该回到上一页而不是退出 Activity——怎么写？", answer: "覆写 Activity 的 `onBackPressed()`：`if (webView.canGoBack()) { webView.goBack() } else { super.onBackPressed() }`。canGoBack() 检查 WebView 是否有浏览历史；goBack() 回到上一页。对应的还有 canGoForward() 和 goForward()。", tags: ["返回键", "canGoBack", "onBackPressed"] },
];
