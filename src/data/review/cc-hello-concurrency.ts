/** 复习题库 · 你好并发世界（cc-hello-concurrency）。《C++ 并发编程实战》第2版 §1 改编章节。 */

import type { ReviewQuestion } from "./types";

export const ccHelloConcurrencyQuestions: ReviewQuestion[] = [
  // ── L1 认记：术语 / 定义 ──
  {
    id: "cc-hc-1",
    chapter: "cc-hello-concurrency",
    level: 1,
    question: "用一句话分别说出「并发」和「并行」是什么。",
    answer:
      "并发：多个任务在同一段时间里都在推进，但不一定真的同时动手——单核机器靠在任务之间飞快切换造出「同时」的错觉（一个厨师一个灶台轮流做几道菜）。并行：同一时刻真的有多件事在同时发生，靠多个 CPU 核各干各的（两个厨师两个灶台同时炒菜）。",
    tags: ["并发", "并行", "定义"],
  },
  {
    id: "cc-hc-2",
    chapter: "cc-hello-concurrency",
    level: 1,
    question: "进程和线程在「内存」上的根本区别是什么？",
    answer:
      "进程各有一整套独立的内存地址空间，彼此隔离、谁也读不到对方的数据（像两家独立的餐厅）。同一进程内的多个线程则共享这个进程的代码、全局数据和堆，但每个线程各有自己独立的栈（像同一家餐厅里共用厨房、各拿一份备忘纸条的多个厨师）。",
    tags: ["进程", "线程", "内存"],
  },
  {
    id: "cc-hc-3",
    chapter: "cc-hello-concurrency",
    level: 1,
    question:
      "`std::thread t(hello);` 这一句做了什么？需要 `#include` 哪个头文件？",
    answer:
      "它创建并**启动**一个新线程，新线程一诞生就去执行函数 `hello`。此时程序里有两个执行流在并发运行：主线程和这个新线程。`std::thread` 声明在头文件 `<thread>` 里，所以要 `#include <thread>`。",
    tags: ["std::thread", "头文件"],
  },
  {
    id: "cc-hc-4",
    chapter: "cc-hello-concurrency",
    level: 1,
    question: "`t.join()` 是干什么用的？「join」这个词怎么理解？",
    answer:
      "`t.join()` 让调用它的线程（通常是主线程）**停下来等** `t` 这个线程跑完，然后再继续往下。join 字面意思是「会合」——主厨师在这里等新厨师把那道菜做完，两人会合后再一起走。",
    tags: ["join", "std::thread"],
  },
  {
    id: "cc-hc-5",
    chapter: "cc-hello-concurrency",
    level: 1,
    question: "用并发主要图哪两件事？",
    answer:
      "一是**性能**——把活儿铺开同时干，又分任务并行（把一件大活拆几块分头做）和数据并行（同一种处理套在一大堆数据上）。二是**关注点分离**——把性质不同的活交给不同线程（如一个管界面响应、一个跑耗时计算），让代码更清晰、界面不卡死。",
    tags: ["为什么并发", "性能", "关注点分离"],
  },
  {
    id: "cc-hc-6",
    chapter: "cc-hello-concurrency",
    level: 1,
    question: "什么是「竞态条件（race condition）」？",
    answer:
      "多个线程同时读写同一份共享数据、且至少有一个在写时，最终结果取决于它们碰巧谁先谁后——执行顺序一变结果就变。像两个厨师同时往同一张订单上写字，谁的盖了谁的全凭手快手慢。这是并发编程最经典的 bug。",
    tags: ["竞态条件", "共享数据"],
  },

  // ── L2 理解：为什么 / 机制 ──
  {
    id: "cc-hc-7",
    chapter: "cc-hello-concurrency",
    level: 2,
    question: "「并行一定是并发，但并发不一定并行」这句话怎么理解？",
    answer:
      "并行（多个核同一刻真的一起动手）本身就满足「多个任务在同一段时间推进」，所以它一定是一种并发。但反过来不成立：单核机器上一个核轮流切换着跑多个任务，也是并发，却没有任何两件事在同一刻真正同时发生，所以不是并行。并行是并发里「真同时」的那个更强的子集。",
    tags: ["并发", "并行", "包含关系"],
  },
  {
    id: "cc-hc-8",
    chapter: "cc-hello-concurrency",
    level: 2,
    question: "只有一个 CPU 核的机器，能不能用并发？写多线程对它还有意义吗？",
    answer:
      "能用并发——一个核（一个灶台）轮流切换着跑多个线程，就是并发；它做不到的只是并行（真同时）。即便如此多线程仍有意义：比如让一个线程专门盯界面响应、另一个跑耗时计算，界面就不会因为后台那个耗时任务而卡死。这是「关注点分离」带来的好处，跟有几个核无关。",
    tags: ["单核", "并发", "关注点分离"],
  },
  {
    id: "cc-hc-9",
    chapter: "cc-hello-concurrency",
    level: 2,
    question: "同一进程里的两个线程「共享」哪些东西、又「各有各的」哪些东西？",
    answer:
      "共享：进程的代码段、全局/静态数据、堆——这些是同一份，任一线程都能读写。各有各的：每个线程有自己独立的**栈**（存局部变量、函数调用记录等）。所以线程间传数据可以直接读写共享内存，很快；但局部变量是各自的，互不干扰。",
    tags: ["线程", "共享", "栈"],
  },
  {
    id: "cc-hc-10",
    chapter: "cc-hello-concurrency",
    level: 2,
    question: "线程间沟通比进程间沟通快，为什么？这个「快」又带来了什么麻烦？",
    answer:
      "快，是因为同一进程的线程共享同一份内存，传数据直接读写共享变量就行，不用像进程那样隔着内存墙「打电话」（进程间通信）。麻烦是：正因为大家读写的是同一份数据，两个线程同时去改同一个变量就可能引发竞态条件——这是进程因内存隔离而天然避免、线程却必须小心防范的问题。",
    tags: ["线程", "进程", "竞态条件"],
  },
  {
    id: "cc-hc-11",
    chapter: "cc-hello-concurrency",
    level: 2,
    question: "「任务并行」和「数据并行」有什么区别？各举一个后厨里的例子。",
    answer:
      "任务并行：把一件大活儿拆成**不同的几块**、分给不同线程同时干，例如一个厨师备料、一个厨师炒菜、一个厨师摆盘，各做各的工序。数据并行：对**一大堆同类数据**套同一种处理、分给多个线程各处理一份，例如十个厨师每人洗一筐一模一样的菜。前者按「工序」分，后者按「数据」分。",
    tags: ["任务并行", "数据并行", "性能"],
  },
  {
    id: "cc-hc-12",
    chapter: "cc-hello-concurrency",
    level: 2,
    question:
      "为什么两个线程一起往 `std::cout` 打印时，输出谁先谁后是不确定的？",
    answer:
      "因为两个线程谁先抢到 CPU、谁先把字推上屏，是由操作系统的调度临时决定的，标准没有任何保证。每条线程内部的语句顺序是确定的（自己第 1 句一定在第 2 句前），但两条线程之间如何交织、谁先冒出来，每次运行都可能不一样。",
    tags: ["输出顺序", "调度", "不确定性"],
  },

  // ── L3 应用：读代码 / 排错 ──
  {
    id: "cc-hc-13",
    chapter: "cc-hello-concurrency",
    level: 3,
    question:
      "下面代码运行时报 `terminate called without an active exception` 然后崩溃。问题在哪？怎么改？\n`std::thread t(hello);`\n`return 0;`",
    answer:
      "问题：创建了 `std::thread t` 却既没 `join()` 也没 `detach()`，线程对象在 `main` 结束析构时发现线程还没安顿好，就调用 `std::terminate` 把程序掐了。改法：在 `t` 析构前明确二选一——`t.join();`（等它跑完）或 `t.detach();`（放它自己后台跑）。这里通常加 `t.join();` 再 `return 0;`。",
    tags: ["join", "detach", "排错"],
  },
  {
    id: "cc-hc-14",
    chapter: "cc-hello-concurrency",
    level: 3,
    question:
      "有人写：「我先让主线程打印 A，再 `std::thread t(printB); t.join();`，所以 A 一定在 B 前面打印。」这个推理对吗？",
    answer:
      "在这个**特定写法**下结论碰巧成立——因为主线程的 `cout << A` 写在创建线程**之前**就执行了，而 B 在新线程里、`join` 又等它跑完。但「推理」本身是危险的：如果把打印 A 的语句挪到 `join` 之前、创建线程之后，A 和 B 就会并发执行，先后又不确定了。正确做法是不去假设线程间顺序，需要定序就用同步手段显式保证。",
    tags: ["执行顺序", "join", "读代码"],
  },
  {
    id: "cc-hc-15",
    chapter: "cc-hello-concurrency",
    level: 3,
    question:
      '把单线程程序 `int main(){ std::cout << "hi\\n"; return 0; }` 改成：用一个新线程打印 `"hi"`，主线程不打印，程序正常退出。写出改后的代码。',
    answer:
      '需要引入 `<thread>`、把打印放进一个函数交给新线程、并 `join`：\n`#include <iostream>`\n`#include <thread>`\n`void say(){ std::cout << "hi\\n"; }`\n`int main(){ std::thread t(say); t.join(); return 0; }`\n关键是 `t.join();` 不能漏，否则线程析构时程序会崩溃。',
    tags: ["std::thread", "改代码", "join"],
  },
  {
    id: "cc-hc-16",
    chapter: "cc-hello-concurrency",
    level: 3,
    question:
      "两个线程都执行 `++counter;`（counter 是共享的全局变量），跑完后 counter 的值时对时错、每次还不一样。这是什么问题？本章层面该怎么认识它？",
    answer:
      "这是**竞态条件**：`++counter` 看似一句，实际是「读出当前值 → 加一 → 写回」三步，两个线程的这三步交错穿插时，可能都读到同一个旧值、各自加一再写回，结果只加了一次（丢更新）。本章层面只要认识到「共享数据 + 至少一个在写 = 危险」即可；具体怎么给共享数据上锁保护，是后续「线程间共享数据」章节的主题。",
    tags: ["竞态条件", "共享数据", "排错"],
  },

  // ── L4 综合：陷阱 / 全流程 ──
  {
    id: "cc-hc-17",
    chapter: "cc-hello-concurrency",
    level: 4,
    question:
      "你的程序要：① 边下载文件边保持界面能点；② 把一个 1000 万元素的数组求和算快点。这两个需求各更偏向「关注点分离」「任务并行」还是「数据并行」？分别怎么用线程实现思路？",
    answer:
      "① 边下载边保持界面响应——偏**关注点分离**：开一个 worker 线程专门跑下载这件耗时事，主线程继续处理界面事件，界面就不会被下载卡住（哪怕单核也有意义）。② 数组求和算快——偏**数据并行**：把 1000 万元素切成若干段，每个线程负责一段各自求和，最后把各段的部分和加起来；这种「同一种处理套在一大堆数据上、按数据切分」正是数据并行，在多核上能真正并行加速。",
    tags: ["关注点分离", "数据并行", "综合"],
  },
  {
    id: "cc-hc-18",
    chapter: "cc-hello-concurrency",
    level: 4,
    question:
      "同一段双线程打印程序，在你的四核机器上跑和在一台单核老机器上跑，输出可能不一样吗？两台机器上分别是并发还是并行？请说清「不确定性」来自哪里。",
    answer:
      "输出在两台机器上都可能不一样、且同一台机器反复跑也可能不一样。单核机器上是**并发**（一个核轮流切换跑两个线程），四核机器上可以是**并行**（两个线程分到不同核上同一刻真同时跑）。但无论并发还是并行，输出顺序的**不确定性**都来自同一个根源：两个线程谁先把字推上屏，由操作系统调度临时决定，标准不做任何保证——并行只是让「真同时」更常发生，并不能让顺序变确定。要定序必须靠同步手段。",
    tags: ["并发", "并行", "不确定性", "综合"],
  },
];

export default ccHelloConcurrencyQuestions;
