/** 复习题库 · 基于锁的并发数据结构（cc-lock-based）。《C++ 并发编程实战》第2版 §7 改编章节。 */

import type { ReviewQuestion } from "./types";

export const ccLockBasedQuestions: ReviewQuestion[] = [
  // ── L1 认记：术语 / 定义 ──
  {
    id: "cc-lb-1",
    chapter: "cc-lock-based",
    level: 1,
    question: "什么叫「线程安全的数据结构」？",
    answer:
      "指：多个线程可以同时调用它的成员函数，而不会破坏数据的不变量、不会产生数据竞争、也不会读到「改了一半」的中间状态——使用者不必在外面再额外加锁。通常靠在内部用互斥量保护每个会读写共享数据的操作来实现。",
    tags: ["线程安全", "定义"],
  },
  {
    id: "cc-lb-2",
    chapter: "cc-lock-based",
    level: 1,
    question:
      "「锁粒度（lock granularity）」指什么？粗粒度和细粒度各是什么意思？",
    answer:
      "锁粒度指「一把锁保护多大范围的数据」。**粗粒度**：一把大锁锁住整个结构——像后厨只有一把总钥匙，谁进谁锁全场，别人全等；实现简单但并发度低。**细粒度**：把数据拆成多块、每块各一把小锁——像每个操作台一把小锁，动不同台子的厨师可并行；并发度高但实现更复杂、更容易出错。",
    tags: ["锁粒度", "粗粒度", "细粒度", "定义"],
  },
  {
    id: "cc-lb-3",
    chapter: "cc-lock-based",
    level: 1,
    question: "什么是「hand-over-hand locking（交替加锁）」？",
    answer:
      "细粒度链表的遍历手法：沿链表逐节点走，**握住下一个节点的锁，才松开当前节点的锁**（像沿一长条料理台逐格挪，抓住下一格才放手当前格）。任一时刻最多只锁住相邻两个节点，从不锁整张表，所以别的线程能同时操作你没握住的节点。",
    tags: ["hand-over-hand", "交替加锁", "定义"],
  },
  {
    id: "cc-lb-4",
    chapter: "cc-lock-based",
    level: 1,
    question: "什么是「分桶锁（bucket lock）」？",
    answer:
      "线程安全查找表（哈希表）的细粒度方案：把数据按 key 的哈希分到若干个桶，**每个桶各配一把独立的锁**。访问某 key 时只锁它所在的那个桶。落到不同桶的访问用的是不同的锁，因此可以并行；只有落到同一个桶才争同一把锁。像按食材种类把后厨分成几个独立料理区，各区各一把小锁。",
    tags: ["分桶锁", "查找表", "定义"],
  },
  {
    id: "cc-lb-5",
    chapter: "cc-lock-based",
    level: 1,
    question: "什么是「阻塞队列」？它和「非阻塞地尝试取」有什么区别？",
    answer:
      "阻塞队列：当队列为空时，取元素的线程不是立刻返回失败，而是**挂起等待**，直到有元素被放进来才被唤醒——通常用条件变量（`std::condition_variable`）实现 `wait_and_pop`。与之相对的是 `try_pop`：队列空时**立刻返回**（返回空 `optional` 或 false），不等待。前者适合「消费者等着干活」，后者适合「轮询、不想阻塞」。",
    tags: ["阻塞队列", "condition_variable", "定义"],
  },
  // ── L2 理解：为什么 / 区别 ──
  {
    id: "cc-lb-6",
    chapter: "cc-lock-based",
    level: 2,
    question:
      "线程安全栈的 `pop()` 为什么常设计成「返回 `std::optional<T>`（或传出参数）」，而不是像 `std::stack` 那样 `top()` 取值、`pop()` 单独弹出？",
    answer:
      "因为把「取顶」和「弹出」拆成两步，会留下**接口竞态**的缝隙：`top()` 之后、`pop()` 之前，别的线程可能已经把栈改了。把「判空 + 取顶 + 弹出」合并成**一个**加锁的 `pop()`，在同一把锁内一气呵成，外部无缝可乘；空栈时返回 `std::nullopt`（而不是对空栈 `top()` 触发未定义行为）。这就是「最小化接口竞态」的设计原则。",
    tags: ["线程安全栈", "接口竞态", "optional", "理解"],
  },
  {
    id: "cc-lb-7",
    chapter: "cc-lock-based",
    level: 2,
    question:
      "为什么线程安全的数据结构「绝不能把指向内部受保护数据的指针或引用泄露给调用方」？",
    answer:
      "因为锁只在成员函数内部生效。一旦返回了指向内部数据的指针/引用，调用方就能在**锁之外**访问那份数据——此时别的线程可能正持锁修改它，保护被「漏」到了临界区之外，数据竞争又回来了。正确做法是返回**值拷贝**，或把「拿到数据后要做的事」以回调形式传进临界区内执行。",
    tags: ["设计原则", "内部引用", "理解"],
  },
  {
    id: "cc-lb-8",
    chapter: "cc-lock-based",
    level: 2,
    question: "细粒度锁链表为什么用 hand-over-hand 而不是「一把锁锁整张表」？",
    answer:
      "一把锁锁整张表 = 粗粒度：任一时刻只有一个线程能碰链表，多线程退化成串行，并发度为零。hand-over-hand 让每个节点各有一把锁、遍历时只锁住相邻两个节点，于是**多个线程能在链表的不同位置同时操作**（一个在表头附近、一个在表尾附近），大幅提高并发。代价是加锁解锁更频繁、实现更复杂。",
    tags: ["细粒度链表", "hand-over-hand", "锁粒度", "理解"],
  },
  {
    id: "cc-lb-9",
    chapter: "cc-lock-based",
    level: 2,
    question:
      "分桶查找表里，读操作用 `std::shared_lock`、写操作用 `std::unique_lock`（配 `std::shared_mutex`）有什么好处？",
    answer:
      "查找表通常**读多写少**。`shared_mutex` 区分两种锁：多个读线程可同时持有**共享锁**（`shared_lock`）并行查找，写线程独占（`unique_lock`，写时连读也挡）。这样同一个桶上的并发读不必互相排斥，只有写才独占——比「读也互斥」的普通 `mutex` 并发度更高。再叠加分桶，不同桶之间还能完全并行。",
    tags: ["shared_mutex", "读写锁", "分桶", "理解"],
  },
  {
    id: "cc-lb-10",
    chapter: "cc-lock-based",
    level: 2,
    question:
      "阻塞队列的 `wait_and_pop` 里，`cv.wait(lock, pred)` 为什么要传一个判断「队列非空」的谓词 `pred`，而不是只 `cv.wait(lock)`？",
    answer:
      "为了应对**虚假唤醒**和「通知早于等待」：`cv.wait(lock, pred)` 等价于 `while (!pred()) cv.wait(lock);`——被唤醒后会**重新检查**条件，只有 `pred()` 真（队列确实非空）才继续，否则继续等。若只 `cv.wait(lock)`，一次虚假唤醒就可能让线程在空队列上往下走、`pop` 一个不存在的元素。谓词把「等待 + 条件复查」绑在一起，杜绝这个坑。",
    tags: ["阻塞队列", "condition_variable", "虚假唤醒", "理解"],
  },
  // ── L3 应用：场景 / 判断 / 改代码 ──
  {
    id: "cc-lb-11",
    chapter: "cc-lock-based",
    level: 3,
    question:
      "下面这个「线程安全栈」的用法对吗？`if (!s.empty()) { auto v = s.top(); s.pop(); use(v); }`（`empty/top/pop` 内部各自加了锁）。指出隐患并给修法。",
    answer:
      "不对，是典型的**接口竞态（check-then-act）**：`empty()` 返回 false 之后、`top()` 之前，别的线程可能已经把栈弹空——于是 `top()`/`pop()` 操作空栈，未定义行为。单操作加锁只保证操作**内部**无竞争，挡不住操作**之间**的空隙。修法：把「判空 + 取顶 + 弹出」合并成**一个**加锁的成员，如 `std::optional<T> pop();`（空则返回 `nullopt`），调用方写成 `if (auto v = s.pop()) use(*v);`。",
    tags: ["接口竞态", "线程安全栈", "改代码", "应用"],
  },
  {
    id: "cc-lb-12",
    chapter: "cc-lock-based",
    level: 3,
    question:
      "实现细粒度锁链表的 `remove` 时，如果不按 hand-over-hand 的「单向逐节点」顺序加锁，而是随手先锁后继、再锁前驱，会出什么问题？",
    answer:
      "会出**死锁**。链表上若两个线程能以**相反方向/不一致顺序**获取相邻节点的锁，就可能各持一把、互等对方手里的那把，形成环形等待。修法：约定**统一的加锁顺序**——hand-over-hand 永远「先持当前节点锁、再获取下一个（后继）节点锁，拿到后才释放当前节点锁」，所有线程都沿同一方向逐节点锁，环就连不起来。",
    tags: ["细粒度链表", "死锁", "加锁顺序", "应用"],
  },
  {
    id: "cc-lb-13",
    chapter: "cc-lock-based",
    level: 3,
    question:
      "需求：一个被多线程频繁查、偶尔改的「配置查找表」，希望并发度尽量高。你会怎么设计它的锁？",
    answer:
      "用**分桶 + 读写锁**：① 按 key 哈希分成 N 个桶，每桶各一把锁——落不同桶的访问天然并行，降低争用；② 每桶用 `std::shared_mutex`：查找用 `std::shared_lock`（多读并行）、修改用 `std::unique_lock`（写独占）。这样「读多写少 + 分桶」两层并发叠加：不同桶完全并行，同一桶的并发读也不互斥，只有写才独占该桶。",
    tags: ["分桶锁", "shared_mutex", "选型", "应用"],
  },
  {
    id: "cc-lb-14",
    chapter: "cc-lock-based",
    level: 3,
    question:
      "判断对错：「我给这个共享 map 配了一把全局 mutex，每个成员函数都正确加了锁，所以它既线程安全、并发度又高。」",
    answer:
      "**前半对、后半错。** 一把全局 mutex 确实能做到线程安全，但它是**粗粒度**：任一时刻只有一个线程能碰这个 map，所有访问被串行化，多线程并发度趋近于零——「粒度太粗 = 等于没并发」。要提高并发，得改细粒度（如分桶，每桶一把锁），让落不同桶的访问能并行。线程安全和高并发是两回事。",
    tags: ["锁粒度", "粗粒度", "判断", "应用"],
  },
  {
    id: "cc-lb-15",
    chapter: "cc-lock-based",
    level: 3,
    question:
      "线程安全队列同时提供 `wait_and_pop`（阻塞）和 `try_pop`（不阻塞）。什么场景该用哪个？",
    answer:
      "① **`wait_and_pop`**：消费者线程「没活干就该睡、有活立刻醒」——队列空时挂起等待，由生产者 `push` 后 `notify` 唤醒。适合后台工作线程、任务分发。② **`try_pop`**：调用方不想阻塞，比如要在没取到时去做别的事、或带超时轮询——队列空时立刻返回空 `optional`/false。原则：要「等到有为止」用 `wait_and_pop`，要「拿不到就走」用 `try_pop`。",
    tags: ["阻塞队列", "wait_and_pop", "try_pop", "选型", "应用"],
  },
  // ── L4 分析：综合 / 推理 ──
  {
    id: "cc-lb-16",
    chapter: "cc-lock-based",
    level: 4,
    question:
      "细粒度（多把小锁）的并发数据结构，为什么往往比粗粒度（一把大锁）更难写对？列举它新引入的几类风险。",
    answer:
      "因为「锁多了」会冒出粗粒度下不存在的问题：① **死锁**——多把锁顺序不一致就环形等待（细粒度链表必须单向逐节点锁、查找表按固定顺序锁多桶）；② **不变量跨锁被破坏**——一个逻辑操作横跨多个加了不同锁的节点/桶时，中间态可能被别的线程看到（如链表 remove 改了前驱的 next、还没改完）；③ **接口竞态**仍在——单节点加锁挡不住跨节点的复合操作空隙；④ 还要保证**异常安全**（持多把锁时某步抛异常，已锁的要正确释放、不变量不破）。粗粒度用一把大锁把这些都「锁没了」，代价是并发度为零。所以选型是「实现复杂度 / 出错风险」与「并发度」之间的权衡。",
    tags: ["细粒度", "死锁", "不变量", "异常安全", "分析"],
  },
  {
    id: "cc-lb-17",
    chapter: "cc-lock-based",
    level: 4,
    question:
      "为什么说「最小化锁的持有时间」既能提高并发、又可能引入新 bug？举例说明该怎么把握。",
    answer:
      "**好处**：临界区越短，别的线程等锁的时间越短，并发度越高——所以应把不需要锁的耗时计算（如哈希计算、构造大对象、I/O）挪到临界区外做。**风险**：一旦把「读出共享数据」和「使用它」拆成两段、中间放开了锁，就可能在锁外看到过期/被改的数据，或引入接口竞态——比如「锁内读出值、解锁、用这个值去改 map」中间 map 已被别人改了。把握：只把**真正与共享数据无关**的工作移出临界区；凡是依赖「读到的值仍然有效」的复合操作，必须整段在同一把锁内原子完成，不能中途放锁。",
    tags: ["设计原则", "锁持有时间", "接口竞态", "分析"],
  },
];
