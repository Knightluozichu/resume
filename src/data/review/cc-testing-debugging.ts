/** 复习题库 · 多线程应用的测试与调试（cc-testing-debugging）。《C++ 并发编程实战》第2版 §11 改编章节（全书收官）。 */

import type { ReviewQuestion } from "./types";

export const ccTestingDebuggingQuestions: ReviewQuestion[] = [
  // ── L1 认记：术语 / 定义 ──
  {
    id: "cc-td-1",
    chapter: "cc-testing-debugging",
    level: 1,
    question:
      "什么是「条件竞争（race condition / 竞态条件）」？典型症状是什么？",
    answer:
      "条件竞争是指程序的正确性**依赖多个线程操作的相对时序**，而这个时序又无法保证。典型症状：结果**时对时错**、重跑会变、换台机器或加点负载就翻车。它常是更具体问题（数据竞争、破坏不变量）的统称。像两个厨师同时改同一张单子，谁后写谁覆盖。",
    tags: ["条件竞争", "竞态条件", "定义"],
  },
  {
    id: "cc-td-2",
    chapter: "cc-testing-debugging",
    level: 1,
    question: "什么是「死锁（deadlock）」？此时 CPU 占用通常是高还是低？",
    answer:
      "死锁是多个线程各持一把锁、又都在等对方手里的锁，形成**环形等待**，谁也不肯先放手，于是全部**冻结不动**。此时线程被挂起，CPU 占用**通常很低**（几乎闲着）。像两个厨师各攥一件对方要的工具僵在原地。经典破法：让所有线程按同一固定顺序加锁。",
    tags: ["死锁", "deadlock", "CPU 占用"],
  },
  {
    id: "cc-td-3",
    chapter: "cc-testing-debugging",
    level: 1,
    question: "什么是「活锁（livelock）」？它和死锁最关键的区别是什么？",
    answer:
      "活锁是线程**没被锁住、反而都在不停地动**——检测到冲突就退避、重试，但因为退避动作总同时发生，反复礼让、**谁也没真正前进**。和死锁最关键的区别：死锁是**都不动·CPU 闲**，活锁是**都在动·CPU 忙却零进展**。一句话：死锁「不动」、活锁「白动」。",
    tags: ["活锁", "livelock", "死锁对比"],
  },
  {
    id: "cc-td-4",
    chapter: "cc-testing-debugging",
    level: 1,
    question:
      "C++ 里「数据竞争（data race）」的精确定义是什么？它是什么级别的问题？",
    answer:
      "数据竞争 = 两个或更多线程**并发访问同一块内存** + **至少一个是写** + 这些访问之间**没有 happens-before 关系**（没有任何同步把它们排出先后）。在 C++ 里数据竞争是**未定义行为（UB）**。它是条件竞争最底层、最常见的一种，能被 ThreadSanitizer 自动抓出。",
    tags: ["数据竞争", "data race", "未定义行为"],
  },
  {
    id: "cc-td-5",
    chapter: "cc-testing-debugging",
    level: 1,
    question: "什么是「破坏不变量（broken invariant）」？举一个例子。",
    answer:
      "一个对象本应**始终**满足某条「不变量」（如「链表的 size 等于结点数」「两个字段要么都更新、要么都不更新」）。当一个线程更新到**一半**的非法「半成品」状态被别的线程看见，或更新被异常打断，不变量就破了——表现为断言失败、链表 `next` 指向已释放内存、`size` 与实际结点数对不上等。",
    tags: ["破坏不变量", "broken invariant", "定义"],
  },
  {
    id: "cc-td-6",
    chapter: "cc-testing-debugging",
    level: 1,
    question: "什么是「ThreadSanitizer（TSan）」？用什么编译选项开启？",
    answer:
      "TSan 是 GCC / Clang 自带的**动态分析工具**，用 `-fsanitize=thread` 开启（如 `g++ -std=c++17 -fsanitize=thread -g a.cpp`）。它在每次内存访问处**插桩**，记录访问的线程、锁集、向量时钟，再用 happens-before 关系判断两次访问有没有同步——发现无 happens-before、又至少一个是写，就**判定数据竞争**并报告冲突的两处代码。代价：程序慢几倍、吃更多内存，故在测试/CI 跑、不进生产。",
    tags: ["ThreadSanitizer", "TSan", "-fsanitize=thread"],
  },
  {
    id: "cc-td-7",
    chapter: "cc-testing-debugging",
    level: 1,
    question: "什么是「heisenbug」？为什么并发 bug 常是 heisenbug？",
    answer:
      "heisenbug（海森堡 bug）是指**一旦你试图观察它**（加打印、挂调试器、单步），它就因为时序被改变而**消失**。名字取自海森堡测不准原理——观察行为本身改变了被观察的系统。并发 bug 常是 heisenbug，因为加 `printf` / 断点会拖慢或重排线程时序，恰好让原本会撞上的竞争窗口错开。对策：别靠手动复现，用 TSan + 压力测试。",
    tags: ["heisenbug", "定义", "观察改变时序"],
  },
  {
    id: "cc-td-8",
    chapter: "cc-testing-debugging",
    level: 1,
    question:
      "什么是「压力测试（stress testing）」？它通过哪几种手段逼出 bug？",
    answer:
      "压力测试是故意把程序逼到**极端繁忙**来逼出偶发的并发 bug。三种手段：① 开**远超 CPU 核数**的线程（制造高竞争）；② 跑**海量迭代**（放大偶发窗口）；③ 用随机让出 / 随机延时**主动扰动时序**。让原本极小概率才撞上的竞争窗口频繁发生。配 TSan 一起跑效果最好。",
    tags: ["压力测试", "stress testing", "定义"],
  },

  // ── L2 理解：原理 / 对比 ──
  {
    id: "cc-td-9",
    chapter: "cc-testing-debugging",
    level: 2,
    question: "为什么说「CPU 占用率」是现场快速区分死锁与活锁的最好线索？",
    answer:
      "因为「动还是不动」直接反映在 CPU 上。死锁的线程被挂起、停在等锁上，不占 CPU → 现场表现为**程序卡住但 CPU 几乎闲着**；活锁的线程在忙等空转、不停退避重试 → 现场表现为**程序卡住但 CPU 跑满**。所以一看 CPU：闲→往死锁查（加锁顺序、环形等待）；忙→往活锁查（退避/重试逻辑）。不必盲目都去找锁的环。",
    tags: ["死锁", "活锁", "CPU 占用", "区分线索"],
  },
  {
    id: "cc-td-10",
    chapter: "cc-testing-debugging",
    level: 2,
    question:
      "ThreadSanitizer 是怎么做到「每跑必抓」数据竞争、而不靠运气复现的？",
    answer:
      "它不靠「这一次恰好撞上」来发现 bug，而是做**关系分析**：在每次内存访问处插桩，记下访问的线程、锁集、向量时钟（happens-before 信息），把两条访问记录摆到一起，判断它们之间**是否存在 happens-before**。只要发现无 happens-before、又至少一个是写，无论这一次有没有真的并发交错，都判定为数据竞争。所以它对「逻辑上可能竞争」的访问都能抓，比靠运气复现可靠得多。",
    tags: ["TSan", "happens-before", "原理"],
  },
  {
    id: "cc-td-11",
    chapter: "cc-testing-debugging",
    level: 2,
    question:
      "「结构化测试」为什么强调先单线程测逻辑、再多线程压测？这样分两步有什么好处？",
    answer:
      "因为它把两类完全不同的错误**分离**开来：单线程那一步只验证「算法逻辑本身对不对」（如 `inc` 两次是否等于 2），与并发无关；逻辑都过了，多线程压测才只需盯「同步」这一层。好处是**一旦挂了能立刻定位**——单线程步挂=逻辑错（跟并发无关），多线程步挂=并发错（同步问题），不必在一团乱麻里猜是哪种。",
    tags: ["结构化测试", "先逻辑后并发", "好处"],
  },
  {
    id: "cc-td-12",
    chapter: "cc-testing-debugging",
    level: 2,
    question: "「为可测试性设计」有哪三条核心原则？它们的共同主线是什么？",
    answer:
      "三条：① **减少共享状态**（能不共享就不共享，让线程多在自己的数据上独立干活）；② **把并发依赖抽成可注入/可替换接口**（别把开线程、取时间、调度硬编码进逻辑，测试时换成可控假实现）；③ **让逻辑能单线程先测对**（把「算什么」和「怎么并行算」拆开）。共同主线：**缩小要测的并发面**——共享越少、依赖越能替换、逻辑越能单独验证，bug 的藏身之处越小。",
    tags: ["为可测试性设计", "三原则", "缩小并发面"],
  },
  {
    id: "cc-td-13",
    chapter: "cc-testing-debugging",
    level: 2,
    question:
      "为什么「并发代码跑了一次（甚至几十次）没出错」不能证明它是正确的？",
    answer:
      "因为并发 bug 是**偶发**的，是否触发取决于线程的相对时序，而时序受核数、负载、调度等影响。测试环境（核少、负载低）下竞争窗口可能根本撞不上，于是「跑很多次都没错」只是这些次都没撞上而已，并不代表无 bug——到生产高负载下时序一变就可能爆。所以并发正确性不能靠「碰巧没错」来证明，必须高压多次 + 消毒器 + 关键路径推理。",
    tags: ["跑一次过≠正确", "偶发性", "测试不足"],
  },

  // ── L3 应用：场景判断 / 选型 ──
  {
    id: "cc-td-14",
    chapter: "cc-testing-debugging",
    level: 3,
    question:
      "现象：程序运行中突然完全卡住、不再有任何输出，此时 CPU 占用几乎为 0。最可能是哪类 bug？下一步往哪查？",
    answer:
      "最可能是**死锁**。判断依据：程序卡死 + **CPU 占用接近 0**——线程都被挂起、停在等锁上、谁也不动。下一步：检查是不是有多个线程各持一锁、又互等对方的锁（环形等待），以及不同线程的**加锁顺序是否一致**（不一致最易成环）。破法是让所有线程按同一固定顺序加锁。",
    tags: ["死锁", "症状判断", "CPU 闲"],
  },
  {
    id: "cc-td-15",
    chapter: "cc-testing-debugging",
    level: 3,
    question:
      "现象：程序卡住不出结果，但 CPU 四个核全部跑满 100%、风扇狂转。最可能是哪类 bug？为什么不要先去查锁的环？",
    answer:
      "最可能是**活锁**。判断依据：程序卡死 + **CPU 全核跑满**——线程没被挂起，反而都在忙着退避/重试，却因动作总同时发生而谁也没前进。不要先去查锁的环，是因为活锁里线程**根本没卡在锁上**（它们在动），查加锁顺序、找环形等待方向就错了。应去查退避/重试逻辑，给退避加**随机化**错开节奏。",
    tags: ["活锁", "症状判断", "CPU 忙", "误判防范"],
  },
  {
    id: "cc-td-16",
    chapter: "cc-testing-debugging",
    level: 3,
    question:
      "一个并行求和程序，结果每次跑都不一样、有时偏小，用 `-fsanitize=thread` 一编译运行就报 data race。这是哪类 bug？怎么修？",
    answer:
      "这是**数据竞争**（属于条件竞争里最底层的一种）。依据：结果时对时错、重跑会变（条件竞争典型症状）+ TSan 直接报 data race（确凿证据：存在无 happens-before 的并发访问且至少一个是写）。修法：给共享累加器**加锁**保护读改写；或改用 `std::atomic` 的 `fetch_add`；或更优——每个线程在**私有局部变量**里累加、最后只合并一次（既无竞争、又避免 false sharing）。",
    tags: ["数据竞争", "并行求和", "修法"],
  },

  // ── L4 综合：方法论 / 排错链路 ──
  {
    id: "cc-td-17",
    chapter: "cc-testing-debugging",
    level: 4,
    question:
      "同事的无锁队列「本地 4 线程跑 100 次全过」就提 PR。请指出这个测试的不足，并给出更靠谱的测试方案。",
    answer:
      "不足：① **「跑过=正确」是错觉**——并发 bug 偶发，本地低负载下竞争窗口可能根本撞不上，100 次全过证明不了无 bug；② **没用消毒器、也没做高压**——只靠运气复现，4 线程（多半 ≤ 核数）压力也不足。\n更靠谱方案：① **压力测试**（开 64+ 远超核数的线程、每线程海量迭代、随机让出/延时扰动时序，把窗口频繁撞开）；② **配 TSan**（`-fsanitize=thread`，让无 happens-before 的访问每跑必抓、定位到代码行）；③ **先单线程测逻辑**（push/pop 顺序语义先测对，再上并发压测，挂了能分清逻辑错还是并发错）；④ 关键内存序路径再人脑/形式化推理一遍（无锁结构尤其需要）。",
    tags: ["测试方案", "压力测试", "TSan", "综合"],
  },
  {
    id: "cc-td-18",
    chapter: "cc-testing-debugging",
    level: 4,
    question:
      "一个偶发崩溃的 bug，你想加 `printf` 复现，结果一加就不崩了；同事建议挂 gdb 单步也抓不到。请解释为什么，并给出正确的排错路径。",
    answer:
      "因为这是**heisenbug**：`printf`、断点、单步都会**改变线程的相对时序**（拖慢或重排执行），恰好把原本会撞上的竞争窗口错开了——观察行为本身改变了被观察的程序，于是 bug「躲」起来。\n正确路径：① **别靠手动加 print / 断点复现**；② 用 **ThreadSanitizer**（不改语义地插桩，把数据竞争确定地抓出、报到代码行）；③ 用**压力测试**（高压 + 扰动时序把竞争窗口频繁撞开）；④ 若必须记录现场，用对时序影响极小的手段（如**环形缓冲事后转储**，而不是同步的 `printf`）；⑤ 关键路径结合代码审查与内存序推理。",
    tags: ["heisenbug", "排错路径", "TSan", "综合"],
  },
];
