import type { ReviewQuestion } from "./types";

/** C++ 高性能编程 · 算法与复杂度复习题 */
export const chpAlgorithmsComplexityQuestions: ReviewQuestion[] = [
  {
    id: "chp-algorithms-complexity-1",
    chapter: "chp-algorithms-complexity",
    level: 1,
    question: `\`O(n log n)\` 和 \`O(n²)\` 在 n=10000 时操作量分别是多少？这说明了什么工程原则？`,
    answer:
      `n=10000：\n- \`O(n log n)\` ≈ 10000 × 13.3 ≈ 1.3×10⁵ 次操作。\n- \`O(n²)\` = 10000² = 10⁸ 次操作。\n\n差距约 770 倍。若每次操作 1ns，前者约 0.13ms，后者约 100ms。\n\n工程原则：算法层一个数量级的差异，远超任何微优化。把 \`O(n²)\` 换成 \`O(n log n)\`（如冒泡换快排/归并）能拿几百倍提升；而缓存对齐、循环展开这类微优化通常只有 10–30%。所以性能优化的第一动作永远是「确认算法层级对不对」，而不是纠结常数。在错误算法上做微优化是徒劳。`,
    tags: ["复杂度", "数量级", "工程原则"],
  },
  {
    id: "chp-algorithms-complexity-2",
    chapter: "chp-algorithms-complexity",
    level: 2,
    question: `什么是摊还复杂度（amortized complexity）？\`std::vector\` 的 \`push_back\` 为什么是「均摊 O(1)」而非严格 O(1)？`,
    answer:
      `摊还复杂度指对一系列操作求平均后的每操作复杂度，允许个别操作较慢，只要整体平均代价低。它衡量「做 N 次操作的总成本 / N」。\n\n\`vector::push_back\` 均摊 \`O(1)\` 的原因：vector 容量不够时会扩容（通常翻倍），把旧元素拷贝到新内存。这次扩容是 \`O(n)\`，但扩容后能再容纳 n 次 push_back 才需再次扩容。把一次 \`O(n)\` 扩容摊到 n 次插入上，每次均摊 \`O(1)\`。\n\n几何级数证明：n 次插入的总拷贝次数 = 1+2+4+...+n < 2n，所以总成本 \`O(n)\`，均摊 \`O(1)\`。\n\n注意：均摊 \`O(1)\` 不等于每次都快。单次扩容时会有一次 \`O(n)\` 抖动，对延迟敏感的场景（实时系统）需用 \`reserve()\` 预分配消除扩容，或用 \`std::deque\`（分段连续、扩容不拷贝）。`,
    tags: ["摊还复杂度", "vector", "push_back", "扩容"],
  },
  {
    id: "chp-algorithms-complexity-3",
    chapter: "chp-algorithms-complexity",
    level: 3,
    question: `一段查找代码在 n=1000 的已排序数组上用线性扫描，剖析显示是热点。你会如何选择优化方案？需要哪些额外信息？`,
    answer:
      `候选方案：\n1. 改二分查找 \`std::lower_bound\`：\`O(n)\` → \`O(log n)\`，n=1000 时从 1000 次降到 10 次。但二分是随机访问，cache miss 多。\n2. 改哈希 \`std::unordered_map\`：\`O(n)\` → 均摊 \`O(1)\`，但构建有开销、不保序、内存大。\n3. 改插值查找：若 key 分布均匀，\`O(log log n)\`。\n4. 改 Bloom filter 前置过滤：负命中快速拒绝。\n5. 直接缓存结果：若重复查询多，加 memoization。\n\n需要的额外信息：\n1. 数据是否真有序？有序才能二分。\n2. 查找频率 vs 数据变更频率？只查不变 → 可构建一次哈希/排序后复用；频繁变更 → 增删成本要算进去。\n3. key 分布？均匀适合插值/哈希；稀疏或字符串适合 trie。\n4. 查找的 key 是否重复查询同一批？重复多 → 缓存命中率高的 memoization 收益大。\n5. 元素大小？小元素连续数组二分快（缓存）；大元素用指针索引数组二分。\n6. 是否需范围查询/有序遍历？需要 → 排序数组/树；不需要 → 哈希。\n\n决策：先确认有序与访问比例，n=1000 时排序数组 + 二分通常是首选（缓存红利让 \`O(log n)\` 常数极小）；若 key 重复查询多且无序，先试哈希。每个方案都用 benchmark 在真实数据上验证。`,
    tags: ["查找", "二分", "哈希", "选型", "应用"],
  },
  {
    id: "chp-algorithms-complexity-4",
    chapter: "chp-algorithms-complexity",
    level: 4,
    question: `综合分析：为什么「换算法」通常比「在原算法上做常量优化」收益更大？但又在什么情况下常量优化反而更重要？`,
    answer:
      `换算法收益更大的原因：\n\n1. 数量级差异：算法层级决定增长曲线斜率。\`O(n²)\` 与 \`O(n log n)\` 在大 n 下是平方对线性对数，差几百上千倍；常量优化（循环展开、向量化）通常 2–10 倍封顶。量纲不同。\n2. 可扩展性：算法优化让代码随 n 增大仍可承受；常量优化只是把「撑不住」的临界点往后挪一点，n 翻倍照样崩。\n3. 根因解决：换算法往往换了思路（如暴力→分治、递归→迭代+备忘录），从源头消除冗余工作；常量优化是在冗余工作上榨取硬件，冗余仍在。\n\n常量优化反而更重要的情况：\n\n1. 算法已是该问题的最优层级：如排序已是 \`O(n log n)\`（理论下界），或查找已是 \`O(1)\` 哈希。此时层级到顶，唯一提升空间在常量（缓存、SIMD、分支消除）。\n2. n 固定且小：n 几十到几百时，\`O(n²)\` 可能比 \`O(n log n)\` 还快（常数小、缓存好）。此时常量决定胜负，如插入排序在小数组上跑赢快排（STL 的 introsort 末段就是插入排序）。\n3. 热点已极致：剖析显示某函数吃掉 80% 时间且算法无法再换，常量优化（对齐、预取、SIMD、避免别名）是唯一出路。\n4. 实时约束：算法层级够低但抖动超标（如扩容抖动），需常量优化消除尾部延迟。\n\n统一框架：先确保算法层级正确（拿数量级），再在热点上做常量优化（拿倍数）。脱离算法谈常量是雕花，脱离常量谈算法是空谈——两者是性能优化的两个阶段，不是对立选项。`,
    tags: ["综合", "算法优化", "常量优化", "权衡"],
  },
];
