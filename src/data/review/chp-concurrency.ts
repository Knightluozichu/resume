import type { ReviewQuestion } from "./types";

/** C++ 高性能编程 · 并发优化复习题 */
export const chpConcurrencyQuestions: ReviewQuestion[] = [
  {
    id: "chp-concurrency-1",
    chapter: "chp-concurrency",
    level: 1,
    question: `什么是 false sharing（伪共享）？它为什么会让多线程代码变慢？`,
    answer:
      `false sharing 指多个线程各自修改不同变量，但这些变量恰好落在同一个缓存行（通常 64 字节）里。CPU 缓存以缓存行为最小一致性单位，一个核改了行内任意字节，整行在其他核的缓存里都失效，其他核再访问自己的变量时要重新从内存或兄弟核拉取。\n\n变慢原因：虽然逻辑上线程互不干扰（不同变量），但硬件层把整个缓存行当一致性单元，导致反复 invalidate 与 reload，缓存形同虚设。最坏情况下多线程比单线程还慢（核越多争用越激烈）。\n\n修法：把会被不同线程写的变量隔到不同缓存行——用 \`alignas(64)\` 让每个变量独占一行，或在结构体里插入填充字段。阿姆达尔定律的隐形杀手常是 false sharing 而非锁。`,
    tags: ["false sharing", "缓存行", "多线程"],
  },
  {
    id: "chp-concurrency-2",
    chapter: "chp-concurrency",
    level: 2,
    question: `线程池相比「每次任务起一个 \`std::thread\`」有什么性能优势？它解决的是什么开销？`,
    answer:
      `线程池预创建固定数量线程，任务到来时投递到队列由空闲线程领取，任务完成后线程不销毁、等下一个任务。\n\n解决的 开销：\n1. 线程创建/销毁开销：起一个线程要分配栈（默认 MB 级）、内核数据结构、调度器登记，约几十 μs；销毁同理。海量短任务下，这些开销可能超过任务本身。线程池把创建摊到启动期，运行期零创建开销。\n2. 调度抖动：频繁起停线程让 OS 调度器反复上下文切换。池里线程常驻，切换更可控。\n3. 资源上限：裸起线程在请求洪峰下可能起几万个线程，耗尽内存或触发调度颠簸。池固定线程数，背压自然。\n\n附加优势：线程数可调到等于核数，避免过度竞争；任务队列可批量、可优先级；配合 thread-local 缓存减少锁争用。\n\n适用：海量短任务（请求处理、并行计算粒度小的循环）。不适合超长任务（占住池里线程让其他任务饿死）或需严格实时响应的任务。`,
    tags: ["线程池", "创建开销", "调度"],
  },
  {
    id: "chp-concurrency-3",
    chapter: "chp-concurrency",
    level: 3,
    question: `什么是无锁（lock-free）数据结构？它和「不用锁」是一回事吗？为什么本书建议「无锁只在剖析定位的热点上用」？`,
    answer:
      `无锁数据结构指用原子操作（如 CAS：compare-and-swap）保证多线程访问正确性，且系统整体总能前进（至少一个线程在推进）。它和「不用锁」不完全等同：\n- 不用锁可能只是「单线程」或「错误的多线程」（数据竞争）。无锁是「无互斥锁且保证 progress」。\n- 无锁内部仍可能 busy-wait（CAS 重试），只是不阻塞睡眠。\n\n只在剖析热点用的原因：\n1. 复杂度极高：正确实现一个无锁队列要处理 ABA、内存序（\`memory_order_acquire/release\`）、析构安全，极容易写错且 bug 难复现。一个内存序写错可能在百万次执行后崩一次。\n2. 收益集中在热点：锁的开销主要在争用。非热点路径锁基本无争用，开销近似原子操作，换无锁收益可忽略。只有高争用热点（如全局计数器、共享队列）才值得。\n3. 常有更好替代：很多「锁热点」可靠分区（每核一个计数器最后求和）、无共享架构（sharding）、读写锁、RCU 解决，比无锁简单且收益相当。\n4. 性能未必更好：高争用下 CAS 风暴会让总线协议成为瓶颈，反而比一把锁慢。\n\n工程原则：先用锁 + 剖析，确认某锁是热点且无法靠分区/读写锁缓解，再考虑无锁，且优先用成熟库（如 \`boost::lockfree\`、Folly）而非手写。`,
    tags: ["无锁", "CAS", "内存序", "应用", "权衡"],
  },
  {
    id: "chp-concurrency-4",
    chapter: "chp-concurrency",
    level: 4,
    question: `综合分析：一段并行化后的代码加速比远低于核数（如 8 核只快了 3 倍）。请系统列出可能原因并给出排查与改进路线。`,
    answer:
      `可能原因分四类：\n\n1. 串行部分占比（阿姆达尔定律）：\n- 总有无法并行的串行段（初始化、归并结果、IO）。若串行占 20%，8 核理论上限 = 1/(0.2+0.8/8) ≈ 3.3 倍。3 倍可能已接近极限。\n- 排查：剖析看串行段占比，想办法把串行也并行化或重叠（如异步 IO）。\n\n2. 同步开销：\n- 锁争用：多线程抢同一把锁，实际退化成串行。排查：perf 看锁等待时间，\`pthread_mutex\` 采样热点。\n- 频繁同步：每步都 barrier/condition_variable，同步开销吃掉并行收益。\n- 改进：减少同步粒度（分块后局部计算再归并）、用 lock-free/分区、增大任务粒度。\n\n3. 共享资源争用（隐性）：\n- false sharing：不同线程写的变量共处一缓存行。排查：perf c2c 或 VTune 看 HITM 计数。改进：\`alignas(64)\` 隔离。\n- 内存带宽饱和：8 核同时遍历大数组，DRAM 带宽成瓶颈，加核不加速。排查：看 STREAM 带宽与 perf 的内存带宽利用。改进：改访问模式提缓存命中（减少带宽需求）、用压缩/SIMD。\n- 分配器锁：多线程 malloc 争用全局锁。改进：thread-local 缓冲或 jemalloc/tcmalloc。\n\n4. 负载不均与粒度：\n- 任务粒度太小：调度开销超过计算。改进：增大粒度（如 OpenMP \`schedule(static, chunk)\`）。\n- 负载不均：某些线程分到重活，其他空等。改进：动态调度或任务队列均衡。\n- 核被占：后台进程、超线程兄弟核竞争执行单元。排查：独占核、关超线程对比。\n\n排查路线：先测串行占比（定上限）→ 看锁等待（最常见）→ 看 false sharing/带宽（隐性）→ 看负载均衡。每步用剖析数据驱动，别盲调。`,
    tags: ["综合", "加速比", "阿姆达尔", "false sharing", "带宽", "排查"],
  },
];
