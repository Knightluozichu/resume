import type { ReviewQuestion } from "./types";

/** C++ 高性能编程 · CPU 缓存优化复习题 */
export const chpCpuCacheQuestions: ReviewQuestion[] = [
  {
    id: "chp-cpu-cache-1",
    chapter: "chp-cpu-cache",
    level: 1,
    question: "L1、L2、L3 缓存和主存的典型容量与访问延迟各是多少？这个数量级差异对编程意味着什么？",
    answer:
      "典型值（桌面/服务器 CPU）：\n- L1：32–64 KB，每核私有，访问约 1 ns（几个时钟周期）。\n- L2：256 KB–1 MB，每核私有，约 4 ns（~10 周期）。\n- L3：几 MB–几十 MB，多核共享，约 12–40 ns。\n- 主存 DRAM：GB 级，约 80–100 ns（~200 周期）。\n\n数量级差异：主存比 L1 慢约 100 倍。一次 cache miss 等于白做上百次 L1 访问。\n\n编程意味：\n1. 数据布局决定快慢：连续数据（vector）顺次访问，一个缓存行装多个元素，后续命中 L1；散落数据（list）每次跳转都可能 miss，慢 100 倍。\n2. 「访问多少字节」不如「访问多少缓存行」重要：读 8 字节但跨行，照样花 100ns。\n3. 算法要看访存模式：`O(n)` 连续遍历可能比 `O(1)` 链表跳转快，因为前者命中缓存。\n\n第一性原理：让数据连续、让访问有序，把访存尽量留在 L1/L2。",
    tags: ["缓存层级", "延迟", "容量", "访存"],
  },
  {
    id: "chp-cpu-cache-2",
    chapter: "chp-cpu-cache",
    level: 2,
    question: "什么是缓存行（cache line）？为什么「访问 a[0] 后访问 a[1] 几乎免费」？这叫什么局部性？",
    answer:
      "缓存行是 CPU 与主存交换数据的最小单位，通常 64 字节。CPU 不按字节加载，而是一次把包含目标地址的整个 64 字节行加载到缓存。\n\n「访问 a[0] 后访问 a[1] 几乎免费」：a[0] 与 a[1] 若是连续的 int（4 字节），它们大概率落在同一 64 字节缓存行内。访问 a[0] 时整行已被加载进 L1，访问 a[1] 直接命中缓存，无需再访主存，耗时从 ~100ns 降到 ~1ns。\n\n这叫空间局部性（spatial locality）：被访问地址附近的内存，近期很可能也被访问。与之相对的是时间局部性：刚访问过的地址近期很可能再被访问（如循环变量、热点数据应留在缓存）。\n\n工程含义：连续遍历数组（步长 1）能最大化空间局部性，缓存行内后续元素都「白捡」；而步长大（如按列优先遍历行优先矩阵）会让每次访问落在新缓存行，miss 频繁。所以遍历多维数组要按其内存布局的顺序（C++ 行优先则外层循环行、内层循环列）。",
    tags: ["缓存行", "空间局部性", "时间局部性", "连续访问"],
  },
  {
    id: "chp-cpu-cache-3",
    chapter: "chp-cpu-cache",
    level: 3,
    question: "什么是预取（prefetch）？软件预取（`__builtin_prefetch`）在什么场景下有用，什么场景下反而有害？",
    answer:
      "预取指在数据真正被需要之前，提前把它从主存搬到缓存，掩盖访存延迟。硬件预取器会自动识别顺序/步长访问模式并预取；软件预取是程序员用 `__builtin_prefetch`/`_mm_prefetch` 显式提示 CPU 提前加载某地址。\n\n软件预取有用场景：\n1. 访问模式硬件预取器识别不了：如链表遍历（下一个地址在当前节点的指针里，硬件无法预测）、图/树遍历、哈希表跳转。提前预取下一个节点能在处理当前节点时把下一个拉进缓存。\n2. 计算与访存可重叠：当前元素有足够计算量（几十周期以上）能掩盖预取延迟，预取才有意义。\n3. 能提前知道未来地址：如已知下个要访问的索引。\n\n反而有害场景：\n1. 访问模式硬件已能预取：顺序数组遍历时手动预取是多余的，还占带宽、污染缓存。\n2. 预取太早：数据被预取进缓存后又因容量不够被驱逐，等真访问时又 miss（prefetch evicted）。\n3. 预取太晚：没足够计算掩盖延迟，等于没预取。\n4. 预取太多：占满内存带宽，挤掉有用预取，或污染缓存驱逐热点数据。\n5. 计算太少：每个元素就几条指令，预取的延迟根本掩盖不了。\n\n实践：先用剖析确认是 cache miss 热点且硬件预取无效（如链表），再小范围试软件预取，调预取距离（提前几个元素），用 benchmark 验证收益。多数情况硬件预取已足够，软件预取是最后手段。",
    tags: ["预取", "软件预取", "链表", "应用", "权衡"],
  },
  {
    id: "chp-cpu-cache-4",
    chapter: "chp-cpu-cache",
    level: 4,
    question: "综合分析：缓存友好是「免费的性能」，请从数据布局、访问顺序、缓存污染三个方面，论述如何系统性地让一段热点代码缓存友好。",
    answer:
      "缓存友好让现有算力「白捡」性能，系统性三方面：\n\n1. 数据布局（让数据本身利于缓存）：\n- 连续存储：用 `vector`/`array` 代替 `list`/`map`/`vector<unique_ptr>`，对象连续排布，一个缓存行装多个。\n- SoA 替代 AoS：只访问少数字段时，把热字段拆成独立连续数组，缓存行全是有效数据。\n- 字段顺序：结构体里高频同访的字段相邻，冷字段靠后或拆出，让一次缓存行加载覆盖一组相关字段。\n- 对齐：热点结构体 `alignas(64)` 对齐缓存行边界，避免一个对象跨两行。\n- 紧凑表示：用更小类型（`float` 代替 `double`、`uint16_t` 代替 `int`）、位域，让一个缓存行装更多元素。\n\n2. 访问顺序（让访问模式顺数据布局）：\n- 按内存布局遍历：C++ 行优先矩阵，外层行内层列；避免「步长 N」的跳访。\n- 顺序优于随机：能排序后顺序扫就别哈希跳转；批量处理同区域数据。\n- 循环 tiling/分块：大矩阵运算（如矩阵乘）切小块让每块 fits in L1/L2，复用后再换块，提升时间局部性。\n- 合并遍历：若多次遍历同一数据，尽量合并成一次，或把多次访问的数据预先聚拢。\n\n3. 缓存污染（不让无用数据挤走热数据）：\n- 避免大块冷数据扫过挤掉热点：处理大缓冲时用非临时存储指令（`_mm_stream_*`）绕过缓存，不污染。\n- 隔离冷热数据：长生命周期大对象（如配置表）别和频繁访问的小对象混在同一结构，免得冷数据把热数据挤出缓存。\n- 控制工作集大小：让热点工作集 ≤ L2 大小，超出就会频繁与主存交换。剖析看 LLC miss 率，超阈值就分块或换布局。\n- 谨慎用软件预取：不当预取会驱逐有用数据。\n\n落地流程：先剖析确认 cache miss 是热点（perf stat 看 L1/LLC miss 率，VTune 看 memory bound）→ 先改访问顺序（成本最低）→ 再改布局（重构成本）→ 最后控制工作集与污染。每步用 miss 率与吞吐验证。缓存优化的精髓：让每次昂贵的访存搬回的数据都被用满。",
    tags: ["综合", "数据布局", "访问顺序", "缓存污染", "tiling"],
  },
];
