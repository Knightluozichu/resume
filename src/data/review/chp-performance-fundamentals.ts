import type { ReviewQuestion } from "./types";

/** C++ 高性能编程 · 性能基础复习题 */
export const chpPerformanceFundamentalsQuestions: ReviewQuestion[] = [
  {
    id: "chp-performance-fundamentals-1",
    chapter: "chp-performance-fundamentals",
    level: 1,
    question: `什么是大 O 表示法？为什么它对性能分析既重要又不充分？`,
    answer:
      `大 O 表示法描述算法耗时/空间随输入规模 n 增长的渐近上界，忽略常数因子与低阶项。如 \`O(n)\` 表示耗时与 n 成正比，\`O(n²)\` 表示与 n 平方成正比。\n\n重要性：它给出「规模变大时谁会先撑不住」的判断。n=100 万时，\`O(n²)\` 要 1e12 次操作而 \`O(n log n)\` 仅约 2e7 次，差 5 个数量级。选错算法层级的代价是任何微优化都救不回来的。\n\n不充分：大 O 忽略常数与硬件。一个 \`O(n)\` 的链表遍历可能因 cache miss 比 \`O(n)\` 的数组遍历慢几十倍；一个 \`O(1)\` 的哈希查表在 cache miss 时可能比 \`O(log n)\` 的有序数组二分还慢。所以大 O 定方向，真实耗时还得靠测量。`,
    tags: ["大 O", "复杂度", "测量"],
  },
  {
    id: "chp-performance-fundamentals-2",
    chapter: "chp-performance-fundamentals",
    level: 2,
    question: `性能优化的标准闭环是什么？为什么「验证」这一步不能省？`,
    answer:
      `标准闭环：测量 → 分析 → 优化 → 验证。\n\n1. 测量：用 benchmark 建立基线，得到「当前多快、目标多快」的数字。\n2. 分析：用剖析器（perf/VTune）定位热点，确定 80% 时间花在哪 20% 代码。\n3. 优化：针对热点改算法/布局/并发。\n4. 验证：回到 benchmark，确认提升达标且无回归。\n\n验证不能省的原因：\n- 优化可能没真提速：改了算法但缓存变差，净收益为零甚至负。\n- 优化可能引入回归：在别的输入或场景下变慢，或破坏正确性。\n- 优化可能优化了非热点：凭感觉改的地方本就不在关键路径。\n\n没有验证的「优化」只是改代码，不是性能工程。验证把主观的「我觉得快了」变成客观的「提速 X 倍、无回归」。`,
    tags: ["闭环", "验证", "回归"],
  },
  {
    id: "chp-performance-fundamentals-3",
    chapter: "chp-performance-fundamentals",
    level: 3,
    question: `假设一段代码剖析显示 90% 时间花在一个 \`O(n²)\` 排序上，但改用 \`O(n log n)\` 后实测只快了 2 倍而非预期的几十倍。可能的原因有哪些？该如何排查？`,
    answer:
      `可能原因：\n\n1. n 不够大：大 O 是渐近行为，n 小时常数项主导。若实际 n 才几百，\`O(n²)\` 与 \`O(n log n)\` 的常数差距没拉开。排查：打印 n 分布，确认热点段 n 的量级。\n\n2. 瓶颈不在比较次数而在数据移动/缓存：新算法虽比较少了，但访问模式不连续（如链式结构、跳跃访问），cache miss 抵消了算法优势。排查：看剖析里的 cache-miss 指标，对比两版的 L1/L2 miss 率。\n\n3. 测量误差：未预热、被 OS 调度打断、编译器把原版向量化了而新版没有。排查：用 google-benchmark 多次重复 + 关闭其他进程 + 看 assembly 是否都被优化。\n\n4. 热点被误判：剖析的 90% 可能包含函数被内联后的其他代码。排查：用 \`-fno-omit-frame-pointer\` + perf record 看精确符号。\n\n5. 输入特殊：原 \`O(n²)\` 在「几乎有序」输入下接近 \`O(n)\`（如插入排序），而新算法恒定 \`O(n log n)\`。排查：用真实输入分布测，别只用随机数据。\n\n排查顺序：先确认 n 与输入分布 → 看 cache miss → 验证测量方法 → 看汇编。`,
    tags: ["复杂度", "剖析", "排查", "应用"],
  },
  {
    id: "chp-performance-fundamentals-4",
    chapter: "chp-performance-fundamentals",
    level: 4,
    question: `综合分析：「过早优化是万恶之源」与「性能必须测量驱动」这两个原则如何统一？在工程中如何把握优化的时机？`,
    answer:
      `两条原则其实不矛盾，统一于「在正确的阶段用正确的方式优化」：\n\n「过早优化」反对的是：在还没测量、还不清楚瓶颈时，凭直觉到处微调（手写汇编、过度抽象避免虚函数、为省一次拷贝牺牲可读性）。这类优化：\n- 改的多是非热点，收益为零；\n- 污染代码可读性，增加维护成本；\n- 给后续真正需要的优化设障。\n\n「测量驱动」要求的是：优化必须基于剖析数据。它不反对优化本身，而反对「无数据的优化」。\n\n工程中的把握：\n1. 设计期：做「性能意识」而非「微优化」——选对数据结构与算法层级（\`O(n log n)\` vs \`O(n²)\`）、保持数据布局合理（连续优先）、避免明显的 pessimization（如循环内重复分配）。这些是低成本高收益的。\n2. 实现期：先写正确、清晰的代码。接口设计留好优化空间（如用模板、传 span 而非容器拷贝），但实现先求对。\n3. 优化期：功能稳定且有性能需求时，测量→定位热点→针对性优化→验证。此时优化集中、有据、可回滚。\n\n判断标准：这个优化是基于剖析数据（晚优化）还是基于猜测（过早优化）？基于数据就做，基于猜测就等测量。二者统一的本质：优化要讲证据，而不是讲情怀。`,
    tags: ["综合", "过早优化", "测量驱动", "工程时机"],
  },
];
