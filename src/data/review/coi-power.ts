import type { ReviewQuestion } from "./types";

export const coiPowerQuestions: ReviewQuestion[] = [
  {
    id: "coi-pow-1",
    chapter: "coi-power",
    level: 1,
    question: "数值的整数次方（快速幂）的核心直觉和基本思想是什么？",
    answer:
      "快速幂的核心思想是『分治与二进制分解』。计算 $x^n$ 时，无需连续乘以 $x$ 共 $n$ 次，而是通过将指数 $n$ 拆分为二进制表示，或者利用 $x^n = (x^{2})^{n/2}$ 的等式，在每次迭代中将底数平方、指数减半，从而以指数级速度减少乘法次数。",
    tags: ["快速幂", "核心直觉"],
  },
  {
    id: "coi-pow-2",
    chapter: "coi-power",
    level: 1,
    question: "快速幂算法相比于 $O(n)$ 的朴素乘法，其时间复杂度降低到了多少？它是如何实现的？",
    answer:
      "快速幂的时间复杂度为 $O(\\log n)$。朴素乘法需要做 $n-1$ 次乘法。而快速幂在每一步中都对指数 $n$ 进行除以 2（右移一位）的操作，因此底数最多进行 $\\log n$ 次平方，乘法操作次数与 $n$ 的二进制位数成正比，即为 $O(\\log n)$。",
    tags: ["时间复杂度", "算法效率"],
  },
  {
    id: "coi-pow-3",
    chapter: "coi-power",
    level: 2,
    question: "在计算 $x^n$ 时，底数 $x=0$ 且指数 $n \\le 0$ 会面临哪些数学与代码边界问题？应如何处理？",
    answer:
      "1. 当底数 $x=0$ 且指数 $n < 0$ 时，数学上等价于对 $0$ 求倒数后计算正次方（即 $1/0^{-n}$），这会导致『除以零』的非法数学错误。在代码中必须做前置校验，抛出异常或设置错误标志/返回 $0.0$。\n2. 当底数 $x=0$ 且指数 $n=0$ 时，数学上 $0^0$ 是无定义的，但在计算机工程实现中通常约定 $0^0 = 1$ 或返回 $0.0$（C++ `std::pow` 返回 1）。面试中需主动与面试官确认此边界的期望表现。",
    tags: ["边界条件", "除零错误"],
  },
  {
    id: "coi-pow-4",
    chapter: "coi-power",
    level: 2,
    question: "为什么在 C++ 中判断底数 `base` 是否为 0 时不能直接写 `base == 0.0`？正确的做法是什么？",
    answer:
      "因为浮点数（如 `double` 和 `float`）在计算机中以二进制分数近似表示，存在计算精度误差，直接使用 `==` 进行等值判断可能会因为极其微小的精度漂移导致判断失败。正确的做法是检查其绝对值是否小于一个预设的极小阀值（如 `1e-9`）：`std::abs(base - 0.0) < 1e-9`。",
    tags: ["浮点数精度", "C++"],
  },
  {
    id: "coi-pow-5",
    chapter: "coi-power",
    level: 3,
    question: "在快速幂的迭代实现中，如何利用位运算优化『判断指数奇偶性』和『指数减半』的操作？",
    answer:
      "1. 判断奇偶性：可以用位与运算 `(n & 1) == 1` 代替模运算 `n % 2 == 1`，通过检查二进制最低位是否为 1 快速判断 $n$ 是否为奇数。\n2. 指数减半：可以用右移运算 `n >>= 1` 代替除法运算 `n = n / 2`。这两项位运算在底层执行速度通常快于传统的算术除法和模运算。",
    tags: ["位运算", "性能优化"],
  },
  {
    id: "coi-pow-6",
    chapter: "coi-power",
    level: 3,
    question: "如果指数为 32 位有符号整数，当 $n = \\text{INT\\_MIN}$（即 `-2147483648`）时，直接取绝对值 `-n` 会发生什么灾难？在 C++ 中应如何防御？",
    answer:
      "在 32 位有符号整数中，`INT_MAX` 是 `2147483647`，而 `INT_MIN` 是 `-2147483648`。由于正负值不对称，`-INT_MIN` 会发生『有符号整数溢出』，在 C++ 中属于未定义行为（通常仍会溢出回 `-2147483648`），导致无限死循环或计算错误。\n**防御方案**：在求绝对值之前，先将指数强转为 64 位有符号整数 `long long` 或无符号整数 `unsigned int` 进行存储，如：`long long absN = std::abs(static_cast<long long>(n));` 从而安全承接正的 `2147483648`。",
    tags: ["整数溢出", "防御性编程", "C++"],
  },
  {
    id: "coi-pow-7",
    chapter: "coi-power",
    level: 3,
    question: "请对比递归快速幂与迭代快速幂在空间效率上的差异。为什么在生产环境中更推荐迭代法？",
    answer:
      "递归快速幂的时间复杂度为 $O(\\log n)$，但由于伴随 $\\log n$ 次递归调用，会消耗 $O(\\log n)$ 的系统函数栈空间。如果指数极大，存在栈溢出的隐患。\n而迭代快速幂只需要常数个辅助变量，空间复杂度为 $O(1)$。同时，迭代消除了递归调用栈的额外开销（如上下文保存与恢复），因此效率更高，在生产环境中是更优的选择。",
    tags: ["递归与迭代", "空间复杂度"],
  },
  {
    id: "coi-pow-8",
    chapter: "coi-power",
    level: 4,
    question: "请写出在 TypeScript 中完整实现 $x^n$（考虑负数指数、底数零、以及 JS 浮点数安全）的迭代快速幂代码。",
    answer:
      "```typescript\nfunction myPow(x: number, n: number): number {\n  // 边界防御：若底数为 0 且指数小于 0，抛出或返回 Infinity/0，此处按常规 1/0 抛出/返回处理\n  if (x === 0) return n < 0 ? Infinity : 0;\n  if (n === 0) return 1;\n\n  let absN = Math.abs(n);\n  let res = 1.0;\n  let base = x;\n\n  while (absN > 0) {\n    // JS 位运算仅限 32 位有符号整型，若 n 较大，应使用算术运算避免溢出\n    if (absN % 2 === 1) {\n      res *= base;\n    }\n    base *= base;\n    absN = Math.floor(absN / 2);\n  }\n\n  return n < 0 ? 1.0 / res : res;\n}\n```",
    tags: ["代码实现", "TypeScript"],
  },
  {
    id: "coi-pow-9",
    chapter: "coi-power",
    level: 4,
    question: "使用迭代快速幂计算 $2^{10}$ 时，请详细追踪其循环体内部变量 `res`, `base` 与剩余指数 `absN` 的每步状态变化。",
    answer:
      "初始状态：`res = 1.0`, `base = 2.0`, `absN = 10`。\n- **第 1 次循环**：`absN = 10` 为偶数，`res` 保持 `1.0`；`base` 平方变为 `2.0 * 2.0 = 4.0`；`absN` 减半为 `5`。\n- **第 2 次循环**：`absN = 5` 为奇数，`res` 乘底数变为 `1.0 * 4.0 = 4.0`；`base` 平方变为 `4.0 * 4.0 = 16.0`；`absN` 减半（向下取整）为 `2`。\n- **第 3 次循环**：`absN = 2` 为偶数，`res` 保持 `4.0`；`base` 平方变为 `16.0 * 16.0 = 256.0`；`absN` 减半为 `1`。\n- **第 4 次循环**：`absN = 1` 为奇数，`res` 乘底数变为 `4.0 * 256.0 = 1024.0`；`base` 平方变为 `256.0 * 256.0 = 65536.0`；`absN` 减半（向下取整）为 `0`。\n循环退出，最终返回 `res = 1024.0`。",
    tags: ["状态追踪", "模拟执行"],
  },
  {
    id: "coi-pow-10",
    chapter: "coi-power",
    level: 4,
    question: "在递归实现快速幂时，如何保证不退化为 $O(n)$ 时间复杂度？请指出常见的递归错误实现方式并给出正确的分治公式。",
    answer:
      "常见的递归错误是重复计算子问题，例如写成：`power(x, n) = power(x, n/2) * power(x, n/2)`，这会导致每一次分治都分裂成两个子任务，重复计算使得时间复杂度退化为 $O(n)$。\n**正确的分治公式**：\n- 当 $n$ 为偶数时：$x^n = (x^{n/2})^2$\n- 当 $n$ 为奇数时：$x^n = x \\cdot (x^{(n-1)/2})^2$\n在代码中，应先用一个临时变量存储单次递归的结果：`double temp = power(x, n / 2);`，然后返回 `temp * temp`（偶数）或 `x * temp * temp`（奇数），以确保分支不分叉，保持 $O(\\log n)$ 的单链递归复杂度。",
    tags: ["递归公式", "时间复杂度退化"],
  },
];
