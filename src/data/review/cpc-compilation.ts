import type { ReviewQuestion } from "./types";

/** CPU 眼里的 C++ · 编译过程复习题 */
export const cpcCompilationQuestions: ReviewQuestion[] = [
  {
    id: "cpc-compilation-1",
    chapter: "cpc-compilation",
    level: 1,
    question: "C++ 源码到可执行文件要经过哪四个阶段？每个阶段的输入和输出分别是什么？",
    answer:
      "四个阶段：\n\n1. 预处理（Preprocessing）：输入 `.cpp` 源文件，输出展开后的纯 C++ 代码。处理 `#include`（头文件展开）、`#define`（宏替换）、条件编译（`#ifdef`）。\n\n2. 编译（Compilation）：输入预处理后的代码，输出汇编代码 `.s`。做词法/语法/语义分析、生成中间表示、优化，最终转成汇编。\n\n3. 汇编（Assembly）：输入 `.s` 汇编代码，输出目标文件 `.o`（机器码 + 符号表）。\n\n4. 链接（Linking）：输入多个 `.o` 与库，输出可执行文件。合并符号表、重定位地址、解析外部引用。\n\n记忆线索：预处理展开文本 → 编译生成汇编 → 汇编生成机器码 → 链接拼装成可执行文件。",
    tags: ["编译过程", "四阶段", "预处理"],
  },
  {
    id: "cpc-compilation-2",
    chapter: "cpc-compilation",
    level: 2,
    question: "链接器（linker）的主要职责是什么？为什么 C++ 要把编译和链接分成两个阶段？",
    answer:
      "链接器两大职责：\n1. 符号解析：每个 `.o` 里有未定义的符号引用（如调用了另一个文件里的函数），链接器把引用绑定到定义。\n2. 重定位：把每个 `.o` 里相对地址合并成最终可执行文件的绝对/虚拟地址。\n\n分开的原因：\n- 支持分别编译：大型项目只重编改动的文件再链接，不必整体重编，大幅缩短构建时间。\n- 模块化：不同文件可独立编写、复用，通过头文件声明接口。\n- 节省内存：编译器一次只处理一个翻译单元，不必把整个项目载入内存。\n\nC++ 因模板和内联带来挑战：模板实例化可能在不同 `.o` 里重复生成，链接器需去重（COMDAT 折叠）；内联函数定义在头文件，被多个单元包含也会重复，同样靠链接器折叠。",
    tags: ["链接", "符号解析", "重定位", "分别编译"],
  },
  {
    id: "cpc-compilation-3",
    chapter: "cpc-compilation",
    level: 3,
    question: "你在编译时遇到「undefined reference to `foo`」链接错误，但头文件里明明声明了 `foo`，这是为什么？该如何排查？",
    answer:
      "这是链接错误，不是编译错误。头文件声明只告诉编译器「`foo` 存在，签名是这样」，编译阶段通过；但链接阶段找不到 `foo` 的定义。\n\n排查步骤：\n1. 检查定义是否存在：`foo` 的实现是否真的写在了某个 `.cpp` 里？是否只写了声明没写实现？\n2. 检查是否参与编译：定义 `foo` 的 `.cpp` 是否被加入构建目标？是否漏加到 Makefile/CMake 的源文件列表？\n3. 检查命名空间/签名：定义的命名空间、参数类型、const 修饰是否和声明完全一致？C++ 名称修饰（name mangling）会因签名差异生成不同符号。\n4. 检查库链接：若 `foo` 在第三方库，是否 `-l` 链接了该库？链接顺序是否正确（依赖库要在引用它的目标之后）？\n5. 检查条件编译：定义是否被 `#ifdef` 排除了？\n\n关键区分：编译错误是「语法/类型不过」，链接错误是「符号找不到定义」。",
    tags: ["链接错误", "undefined reference", "排查"],
  },
  {
    id: "cpc-compilation-4",
    chapter: "cpc-compilation",
    level: 4,
    question: "综合分析：C++ 的 name mangling（名称修饰）机制在编译与链接中扮演什么角色？为什么 C 不能有同名函数而 C++ 可以重载？",
    answer:
      "name mangling 是 C++ 编译器把函数签名（名字 + 参数类型 + 命名空间 + const 等）编码成一个唯一的内部符号名的过程。\n\n在编译阶段：编译器遇到函数定义/声明时，按规则把 `void foo(int)` 修饰成类似 `_Z3fooi` 的符号写入 `.o`。这样重载函数 `foo(int)` 与 `foo(double)` 被修饰成不同符号，互不冲突。\n\n在链接阶段：链接器看到的不是源码层面的 `foo`，而是修饰后的唯一符号。只要每个引用都能匹配到一个定义的修饰符号，链接就通过。C 没有重载，函数名就是符号名（不做修饰），所以同名函数会冲突。\n\n工程意义：\n1. `extern \"C\"` 关闭修饰：让 C++ 函数用 C 链接约定（不修饰），以便 C 代码或动态库符号表里按原名找到它。\n2. 跨编译器 ABI：不同编译器修饰规则不同（Itanium ABI vs MSVC），导致 C++ 二进制不能跨编译器混链。\n3. 调试与剖析：`nm`/`objdump` 看到的是修饰名，需 `c++filt` 还原成可读签名。\n\n本质：mangling 让链接器这个「只认符号名」的工具能支持 C++ 的类型系统与重载，是高级语言特性落到底层符号机制的桥梁。",
    tags: ["综合", "name mangling", "重载", "链接"],
  },
];
