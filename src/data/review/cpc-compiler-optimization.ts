import type { ReviewQuestion } from "./types";

/** CPU 眼里的 C++ · 编译器优化复习题 */
export const cpcCompilerOptimizationQuestions: ReviewQuestion[] = [
  {
    id: "cpc-compiler-optimization-1",
    chapter: "cpc-compiler-optimization",
    level: 1,
    question: `什么是常量折叠（constant folding）和死代码消除（DCE）？各举一个例子。`,
    answer:
      `常量折叠：编译期就能算出结果的表达式，直接用结果替换。例如 \`int x = 2 * 3 + 1;\` 编译器在编译期算出 7，生成的机器码里根本没有乘法和加法指令，直接是 \`mov x, 7\`。\n\n死代码消除（DCE）：删除永远不会执行的代码。例如 \`if (false) { foo(); }\` 因条件恒假，整段被删；又如 \`int x = 0; if (x) { ... }\` 经常量传播后 x 为 0，分支恒不执行，删除。\n\n两者常协同：常量折叠让条件变成编译期常量，DCE 据此删除死分支。这是「编译期把能算的都算了，把不可能的都删了」的基本优化。\n\n注意：常量折叠要求操作数都是编译期常量；若含运行期变量，编译器只能保留运行期计算。`,
    tags: ["常量折叠", "死代码消除", "DCE"],
  },
  {
    id: "cpc-compiler-optimization-2",
    chapter: "cpc-compiler-optimization",
    level: 2,
    question: `什么是循环展开（loop unrolling）？它为什么能提速？过度展开有什么副作用？`,
    answer:
      `循环展开：把循环体复制若干次，减少循环次数。例如 \`for (i=0;i<4;i++) a[i]=0;\` 展开成 \`a[0]=0;a[1]=0;a[2]=0;a[3]=0;\`。\n\n提速原因：\n1. 减少循环控制开销：每次循环的 \`i++\`、\`i<N\` 比较、条件跳转都要执行。展开后这些开销按比例减少。\n2. 降低分支预测失败：循环跳转每次都是一次分支，展开后分支数减少。\n3. 暴露指令级并行：展开后多条独立的赋值/计算指令并排，CPU 流水线与乱序执行可同时处理。\n4. 利于向量化：展开后相邻几组操作模式一致，编译器可合并成 SIMD 指令。\n\n过度展开副作用：\n1. 代码膨胀：二进制变大，icache 压力增大，可能反而变慢。\n2. 寄存器压力：展开后同时活跃的变量增多，寄存器不够用就溢出到栈。\n3. 通用性下降：循环次数不是展开因子整数倍时要处理「尾部」，增加复杂度。\n\n编译器在 \`-O2\`/\`-O3\` 下会自动决定展开因子，权衡收益与膨胀。\`#pragma unroll\` 可手动干预，但通常让编译器自行决策更好。`,
    tags: ["循环展开", "指令级并行", "向量化", "代码膨胀"],
  },
  {
    id: "cpc-compiler-optimization-3",
    chapter: "cpc-compiler-optimization",
    level: 3,
    question: `什么是自动向量化（auto-vectorization）？编译器在什么条件下才能把循环向量化？什么会阻碍它？`,
    answer:
      `自动向量化：编译器自动把标量循环改写成 SIMD（单指令多数据）指令，一条指令同时处理多个元素。如把 4 次 \`a[i]=b[i]+c[i]\` 合并成一条 128 位 SSE/AVX 指令同时算 4 个 int。\n\n向量化条件：\n1. 循环体无跨迭代数据依赖：每次迭代只写自己的 \`a[i]\`，不依赖前一次迭代的写（除非是可识别的归约模式如求和）。\n2. 访问模式连续且步长固定：\`a[i]\`、\`a[i+stride]\` 这类可被向量化；随机下标 \`a[index[i]]\` 是 gather/scatter，多数架构低效或不向量化。\n3. 循环次数可确定或可处理尾部：需知道何时停，剩几组用标量处理尾部。\n4. 无函数调用：循环内的调用（尤其虚函数、外部函数）会阻碍，除非内联后无副作用。\n5. 无别名阻碍：若编译器不能证明 \`a\` 和 \`b\` 不重叠（C 的 \`restrict\`、C++ 难证明），保守起见不向量化。\n\n阻碍因素：\n- 指针别名：C++ 默认指针可能别名，编译器保守。\`__restrict\` 或编译选项 \`-fno-strict-aliasing\` 影响。\n- 复杂控制流：循环里有 if/switch 分支，向量化需用掩码，开销大。\n- 数据依赖：\`a[i] = a[i-1] + 1\` 是循环携带依赖，无法并行。\n- 非对齐访问：虽现代 CPU 支持非对齐，但对齐访问更高效。\n\n工程做法：保持循环简单、连续访问、无依赖、少分支；用 \`__restrict\` 标注无别名；开启 \`-O3 -march=native\`；必要时手写 SIMD intrinsics 或用 \`#pragma omp simd\`。`,
    tags: ["向量化", "SIMD", "数据依赖", "指针别名", "auto-vectorization"],
  },
  {
    id: "cpc-compiler-optimization-4",
    chapter: "cpc-compiler-optimization",
    level: 4,
    question: `综合分析：编译器优化受「可观察行为不可改变」（as-if 规则）约束。这意味着什么？为什么 volatile、原子操作和有副作用的调用会成为优化的屏障？`,
    answer:
      `as-if 规则：编译器可以对代码做任意变换，只要「可观察行为」（observable behavior）不变。可观察行为包括：对 volatile 内存的访问、I/O 操作、程序终止前对外的写入等。纯计算的重排、消除只要结果一样就行。\n\n这意味着编译器有很大自由：删死代码、常量折叠、重排无依赖指令、内联、向量化——只要不影响可观察行为。\n\n优化屏障：\n1. volatile：告诉编译器「每次访问都要真实访存，不许缓存到寄存器、不许重排、不许消除」。常用于 MMIO 寄存器、信号处理。volatile 访问是可观察行为，编译器不能越过它优化。\n2. 原子操作（std::atomic）：带内存序语义，编译器不能把原子操作前后有依赖的访存重排越过它。acquire/release 屏障限制了重排范围，保证多线程可见性。\n3. 有副作用的调用：编译器无法证明外部函数没有可观察副作用（如改全局、做 I/O），不能消除或随意重排。只有内联后确认无副作用才能继续优化。\n4. 编译屏障（asm volatile 加 memory clobber）：强制编译器在该点刷新所有内存假设，不跨越重排。\n\n工程含义：\n- 想让编译器优化，就保持代码「纯」（无副作用、无 volatile、无原子），给优化器最大空间。\n- 想阻止优化（如 benchmark 防止死代码消除），用 DoNotOptimize 之类技巧或 volatile。\n- 多线程正确性靠原子/屏障，不能指望编译器「恰好不重排」——它只要单线程 as-if 成立就会重排。\n\n本质：as-if 规则是优化自由与正确性的边界。编译器在边界内尽情优化，volatile/原子/副作用是边界上的「不可越雷池」，标记着「这里的行为外部可见，不许动」。`,
    tags: ["综合", "as-if 规则", "volatile", "原子操作", "优化屏障"],
  },
];
