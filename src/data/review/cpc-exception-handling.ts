import type { ReviewQuestion } from "./types";

/** CPU 眼里的 C++ · 异常处理机制复习题 */
export const cpcExceptionHandlingQuestions: ReviewQuestion[] = [
  {
    id: "cpc-exception-handling-1",
    chapter: "cpc-exception-handling",
    level: 1,
    question: "C++ 异常处理机制所谓的「零开销（zero-cost）」是什么意思？它真的完全零成本吗？",
    answer:
      "「零开销」指的是：在异常不抛出的正常路径上，异常处理几乎没有运行期开销——没有 `try` 块的额外检查、没有为每个可能抛出的点插入分支判断。代价被推迟到异常真正抛出时才支付。\n\n实现方式：表驱动（table-driven）。编译器为每个可能抛出异常的函数生成一张 unwind 表，记录「若异常发生在这段指令区间，该调用哪个析构、如何回滚栈」。正常执行时不查表，只有抛异常时才用表来指导栈展开。\n\n不是完全零成本：\n1. 代码体积：unwind 表占用二进制空间（有时增大 10%-20%）。\n2. 抛异常极慢：抛出异常要分配异常对象、查 unwind 表、逐帧调用析构、跳转 catch——比一次普通函数调用慢几个数量级。\n3. 阻碍部分优化：编译器不能随意重排可能抛出的代码。\n\n所以「零开销」是「正常路径零开销、异常路径高开销」。适合异常真正罕见的情况；若把异常当常规控制流，性能会崩。",
    tags: ["零开销异常", "unwind 表", "表驱动"],
  },
  {
    id: "cpc-exception-handling-2",
    chapter: "cpc-exception-handling",
    level: 2,
    question: "什么是栈展开（stack unwinding）？抛出异常时 CPU 是如何逐层找到匹配的 catch 的？",
    answer:
      "栈展开：异常抛出后，从当前栈帧开始逐层向上回退，每退出一层就调用该层局部对象的析构函数，直到找到一个能匹配该异常类型的 catch 块。\n\n查找过程：\n1. 运行时（libc++/libstdc++ 的 `__cxa_throw`）拿到异常对象与类型，记录在异常头里。\n2. 用当前指令地址查 unwind 表，定位当前栈帧的析构动作与返回地址。\n3. 执行该帧内已构造局部对象的析构（按构造逆序）。\n4. 恢复到调用方栈帧，用返回地址再查 unwind 表，看调用方是否有 catch。\n5. 若有 catch，检查类型是否匹配；匹配则跳转执行 catch 体，不匹配则继续展开上一帧。\n6. 若展开到栈底仍无匹配，调用 `std::terminate` 终止程序。\n\n关键：unwind 表是静态的、按指令区间索引的「回滚指南」。CPU 不需要在每个函数里插检查代码，只在抛异常时按表逐帧回溯。这就是「正常路径零开销」的实现原理。",
    tags: ["栈展开", "unwind", "catch 匹配"],
  },
  {
    id: "cpc-exception-handling-3",
    chapter: "cpc-exception-handling",
    level: 3,
    question: "在性能敏感的热路径循环里，用异常处理错误 vs 用返回码处理错误，两者在 CPU 层面的差异是什么？",
    answer:
      "热路径差异：\n\n返回码方式：\n- 每次调用后要检查返回值，产生一条条件分支。\n- 分支预测器对「正常/错误」这种高度偏斜的分支预测准确率极高（99%+），预测正确时几乎无开销。\n- 代码内联后编译器还能把检查与后续逻辑融合优化。\n\n异常方式（异常不抛出时）：\n- 正常路径无检查分支，理论更快。\n- 但异常会阻碍编译器某些优化（不能跨 throw 自由重排），且二进制增大。\n\n异常方式（异常抛出时）：\n- 抛出要分配对象、查 unwind 表、逐帧析构、栈展开——慢几十到几百倍。\n- 若错误频繁发生（如解析脏数据），异常当控制流用会让程序慢得不可接受。\n\n结论：\n- 异常适合「真正异常、几乎不发生」的情况（如内存耗尽、配置错误）。正常路径零开销的优势发挥，抛出慢无所谓。\n- 返回码适合「预期会发生的错误」（如网络超时、解析失败、用户输入非法）。每次检查的成本被分支预测消化，且无抛出惩罚。\n- 热路径循环里若错误率不低，坚决用返回码；若错误极罕见，异常的零开销正常路径更优。\n\n工程铁律：绝不把异常当常规控制流。",
    tags: ["异常 vs 返回码", "热路径", "分支预测", "性能"],
  },
  {
    id: "cpc-exception-handling-4",
    chapter: "cpc-exception-handling",
    level: 4,
    question: "综合分析：`noexcept` 关键字在编译期和运行期分别如何影响 CPU 的执行路径与编译器优化？",
    answer:
      "`noexcept` 告诉编译器「这个函数不会抛异常」。\n\n编译期影响：\n1. 优化空间更大：编译器知道函数不会抛，可以省略掉调用方为异常准备的 unwind 伪代码、不必保守地保留某些寄存器状态，能更自由地重排和内联。\n2. 调用方代码更精简：调用 `noexcept` 函数后不必维护「若它抛了我该怎么回滚」的状态，减少栈帧里的异常处理元数据。\n3. 移动语义提速：C++11 后移动构造/移动赋值默认不是 noexcept，但标注 noexcept 后，`std::vector` 扩容时会直接移动而非拷贝，性能成倍提升。\n\n运行期影响：\n- 若 `noexcept` 函数真的抛了异常（理论上不该发生），运行时直接调用 `std::terminate` 终止程序，不会沿栈展开找 catch。\n- 这意味着 noexcept 是一份「硬合约」：违反即程序死亡，不给你恢复机会。\n\n使用原则：\n- 确定不抛且被频繁调用（尤其是移动操作、析构、swap）的函数，标 noexcept 既给编译器优化空间又给标准库算法选优路径。\n- 不能保证不抛的别乱标，否则一旦抛出就是程序终止。\n- 析构函数默认 noexcept（C++11 起），抛异常的析构极其危险。\n\n从 CPU 视角：noexcept 省掉的不是「运行期检查」，而是「编译器为可能抛而保留的保守余地」——让正常路径更短更紧凑。",
    tags: ["综合", "noexcept", "编译器优化", "移动语义"],
  },
];
