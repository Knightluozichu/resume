import type { ReviewQuestion } from "./types";

/** CPU 眼里的 C++ · 总复习题 */
export const cpcFinalReviewQuestions: ReviewQuestion[] = [
  {
    id: "cpc-final-review-1",
    chapter: "cpc-final-review",
    level: 1,
    question: "用一句话串联本书三段结构：编译与链接、CPU 运行时机制、CPU 性能优化之间的因果链。",
    answer:
      "C++ 源码经编译与链接变成机器码（怎么来的），运行时机制决定机器码如何操作内存与控制流（怎么跑的），性能优化把对底层的理解转化为更快代码（怎么变快）——三段是「诞生 → 运行 → 提速」的因果链。\n\n精炼版：编译决定代码长什么样，运行时机制决定 CPU 怎么执行它，优化决定执行得有多快。不懂前两段，优化就是盲猜。",
    tags: ["总复习", "三段结构", "因果链"],
  },
  {
    id: "cpc-final-review-2",
    chapter: "cpc-final-review",
    level: 2,
    question: "「零成本抽象」是 C++ 的设计哲学。但从 CPU 视角看，虚函数和异常真的是零成本吗？如何让它们尽量接近零成本？",
    answer:
      "虚函数：\n- 非零成本：每对象一个 vptr（内存）、调用多两次访存、间接跳转阻碍分支预测、阻碍内联与后续优化链。\n- 接近零成本：编译期去虚化（devirtualization）——若编译器能证明对象类型确定（final 类、局部对象直接调），则去掉间接跳转转为直接调用并可内联。设计上用 final 标注不再继承的类、用 CRTP 在编译期完成多态、热路径避免虚调用。\n\n异常：\n- 非零成本：unwind 表增大二进制、抛出异常极慢、阻碍部分优化。\n- 接近零成本：表驱动让正常路径零开销，只要异常真正罕见。标注 noexcept 让编译器放弃为可能抛保留的保守余地。热路径用返回码而非异常。\n\n本质：零成本抽象的准确含义是「不为未使用的特性付费」。虚函数你用了就付间接跳转的费，异常你抛了就付展开的费；但不用时（去虚化成功、异常不抛）几乎不付费。让抽象接近零成本的关键是给编译器足够信息做编译期决议。",
    tags: ["零成本抽象", "虚函数", "异常", "去虚化", "noexcept"],
  },
  {
    id: "cpc-final-review-3",
    chapter: "cpc-final-review",
    level: 3,
    question: "给定一个性能不达标的 C++ 程序，请基于本书的知识给出一条从底层到优化的系统化排查与改进路径。",
    answer:
      "系统化路径（测量驱动、逐层下钻）：\n\n1. 测量与定位：先用 profiler（perf/VTune/perf-counter）定位热点，不要凭直觉。确认瓶颈是 CPU bound（计算）还是 memory bound（访存）还是其他。\n\n2. 算法与数据结构层：\n- 复杂度对不对？O(n²) 能否换 O(n log n)？\n- 数据结构是否缓存友好？list 换 vector、AoS 换 SoA。\n- 热数据是否集中连续？\n\n3. 内存布局层：\n- 对象大小与对齐：多余填充？vptr 是否必要？\n- 栈 vs 堆：热路径能否避免堆分配？对象池？\n- false sharing：多线程可写数据是否跨缓存行？\n\n4. 控制流层：\n- 虚调用：热路径能否去虚化或改静态多态（CRTP）？\n- 分支：高度偏斜的分支能否用likely/unlikely提示？能否用查表替代分支？\n- 异常：热路径是否误用异常当控制流？\n\n5. 编译器优化层：\n- 是否开了 -O2/-O3？-march=native？\n- 循环能否被向量化？有没有阻碍（指针别名、复杂分支、数据依赖）？用 __restrict、简化循环体、#pragma omp simd。\n- 小函数是否内联？LTO 是否开启？\n\n6. 验证：每改一处重新测量，确认提升且无回归。\n\n关键原则：先测量再优化、先算法后常量、先单线程后多核、先正确后快速。跳层（不测量就改、算法没对就抠缓存）是常见错误。",
    tags: ["性能排查", "系统化", "测量驱动", "逐层优化"],
  },
  {
    id: "cpc-final-review-4",
    chapter: "cpc-final-review",
    level: 4,
    question: "综合分析：为什么「懂 CPU」对写好 C++ 至关重要？结合全书内容说明：一个只懂语法不懂底层的 C++ 程序员，最可能在哪些地方写出低效代码？",
    answer:
      "C++ 的设计哲学是「零成本抽象」与「硬件可控制」，这两点都要求程序员理解底层。只懂语法不懂底层，会系统性踩坑：\n\n1. 抽象成本盲区：\n- 滥用虚函数：以为和普通函数一样快，不知间接跳转切断内联与优化链。在热路径里塞虚调用，性能悄悄腰斩。\n- 异常当控制流：以为 try/catch 和 if 一样轻，不知抛出要栈展开、查 unwind 表，频繁抛出让程序慢几十倍。\n- std::function/lambda 滥用：不知类型擦除带来间接调用与堆分配。\n\n2. 内存布局盲区：\n- 不知对象里有 vptr、有对齐填充，sizeof 算不对，容器占用估错。\n- 用链表代替数组，以为 O(1) 插入更快，不知缓存 miss 让常数因子暴增。\n- AoS 处理批量数据，不知热字段被冷字段稀释，缓存有效载荷仅 25%。\n\n3. 并发盲区：\n- 多线程共享计数器不隔离，false sharing 让缓存失效，多核比单核还慢。\n- 不懂内存序与屏障，要么过度同步（锁粒度过大）要么同步不足（数据竞争 UB）。\n\n4. 优化预期盲区：\n- 不知编译器能做什么，写出阻碍向量化的代码（指针别名、复杂分支、循环携带依赖）却以为编译器会优化。\n- 不知 as-if 规则，在多线程里依赖「编译器不会重排」的错觉，写出看似正确实则 UB 的代码。\n- 不知内联是优化链入口，把热函数藏在不同翻译单元且不开 LTO，关上整条优化链。\n\n5. 测量盲区：\n- 凭直觉优化而非 profiler 定位，在非热点上白费功夫。\n- 不懂缓存层级，看不出「memory bound」与「CPU bound」的区别，用错优化手段。\n\n本质：C++ 把硬件暴露给程序员，懂底层的人能把硬件红利榨干，不懂的人则被底层细节反噬。本书的价值正在于此——把语法层与 CPU 层打通，让每一行 C++ 都映射回真实的指令、访存与缓存行为。",
    tags: ["综合", "懂 CPU", "低效代码", "盲区", "全书总结"],
  },
];
