import type { ReviewQuestion } from "./types";

/** CPU 眼里的 C++ · 函数调用机制复习题 */
export const cpcFunctionCallsQuestions: ReviewQuestion[] = [
  {
    id: "cpc-function-calls-1",
    chapter: "cpc-function-calls",
    level: 1,
    question: "什么是栈帧（stack frame）？函数调用时栈帧里通常存放哪些内容？",
    answer:
      "栈帧是每次函数调用在调用栈上分配的一块内存区域，记录该次调用的上下文。通常包含：\n\n1. 函数参数：调用方传入的实参（或通过寄存器传递后由被调方保存到栈上）。\n2. 返回地址：函数执行完后该跳回调用方的哪条指令。\n3. 保存的帧指针：上一个栈帧的基址，用于恢复调用方的栈帧。\n4. 局部变量：函数内声明的非静态局部变量。\n5. 临时值：表达式计算的中间结果。\n\n布局上，栈帧由帧指针（BP）和栈指针（SP）界定：BP 指向当前帧基址，SP 指向栈顶。函数返回时，SP 恢复到调用前位置，整块栈帧「弹出」。\n\n记忆线索：函数调用 = 压入一个栈帧；函数返回 = 弹出这个栈帧。",
    tags: ["栈帧", "函数调用", "调用栈"],
  },
  {
    id: "cpc-function-calls-2",
    chapter: "cpc-function-calls",
    level: 2,
    question: "什么是调用约定（calling convention）？为什么 x86-64 的 System V ABI 与 MSVC ABI 传参方式不同会导致跨编译器问题？",
    answer:
      "调用约定是调用方与被调方之间关于「参数怎么传、返回值怎么返、谁保存寄存器、栈谁清理」的协议。\n\n核心要素：\n1. 参数传递：前几个参数用寄存器（System V AMD64 用 rdi/rsi/rdx/rcx/r8/r9 传整数/指针，xmm0-7 传浮点），多余的压栈。\n2. 返回值：整数/指针走 rax，浮点走 xmm0，大对象走隐藏指针。\n3. 调用方保存 vs 被调方保存寄存器：哪些寄存器函数可自由用，哪些必须保存恢复。\n4. 栈清理：调用方清理（cdecl）还是被调方清理（stdcall）。\n\n跨编译器问题：System V ABI（Linux/macOS GCC/Clang）与 MSVC ABI（Windows）寄存器分配不同——MSVC 只用 rcx/rdx/r8/r9 传前四个参数且不管整型浮点，System V 用 6 个且区分整型/浮点。若一个用 GCC 编译的 `.so` 调用 MSVC 编译的函数，参数会进错寄存器，行为未定义。所以 C++ 二进制不能跨 ABI 混链，需要统一用 C ABI（`extern \"C\"`）或重新编译。",
    tags: ["调用约定", "ABI", "寄存器", "传参"],
  },
  {
    id: "cpc-function-calls-3",
    chapter: "cpc-function-calls",
    level: 3,
    question: "函数返回一个大型结构体时，CPU 层面是如何把返回值交给调用方的？为什么不在栈帧里直接返回？",
    answer:
      "大结构体（超过两个寄存器容量，通常 16 字节以上）不能直接用 rax/xmm0 返回，而是通过「隐藏指针」机制：\n\n1. 调用方在栈上预留一块空间存放返回值，把这块空间的地址作为「隐藏第一参数」传给被调函数（通常放在 rdi）。\n2. 被调函数在返回前，把结构体内容拷贝到该地址指向的内存。\n3. 调用方从那块空间读取返回值。\n\n为什么不在被调方栈帧里直接返回：被调方栈帧在返回时就被弹出了，调用方无法访问。所以必须由调用方提供存放空间。\n\n优化：\n- RVO/NRVO：编译器直接把调用方预留空间作为局部对象的构造位置，省掉拷贝。\n- C++11 移动语义：把深拷贝降为指针转移。\n\n从 CPU 视角：返回大对象本质是「调用方出内存、被调方填数据」，寄存器只够返回小值，大值必须走内存。",
    tags: ["返回值", "隐藏指针", "RVO", "大对象"],
  },
  {
    id: "cpc-function-calls-4",
    chapter: "cpc-function-calls",
    level: 4,
    question: "综合分析：函数调用在 CPU 层面的真实成本有哪些？为什么深度递归容易爆栈而迭代不会？",
    answer:
      "函数调用的 CPU 成本：\n\n1. 栈帧建立/拆除：压入参数、保存寄存器、调整 SP/BP，返回时恢复——多条指令开销。\n2. 寄存器溢出：参数和局部变量超出寄存器容量时溢出到栈，增加访存。\n3. 分支预测惩罚：call/ret 是间接控制流，ret 尤其依赖返回地址预测器（RSB），预测失败会冲刷流水线。\n4. 缓存影响：深调用栈跨多个缓存行，频繁的 call/ret 抖动栈顶缓存。\n5. ABI 限制：跨调用边界要遵守调用约定，编译器不能随意跨函数分配寄存器。\n\n递归爆栈原因：\n- 每层递归压一个栈帧，N 层递归占 N 个栈帧的栈空间。栈大小有限（Linux 默认 8MB），超出即段错误。\n- 迭代用循环，始终一个栈帧，空间 O(1)。\n\n尾递归优化（TCO）：若递归调用是函数最后一步且无后续操作，编译器可把它改成「跳转」而非「调用」，复用当前栈帧，把 O(N) 栈空间降为 O(1)。但 C++ 标准不保证 TCO，且副作用/析构会阻碍。\n\n工程含义：热路径小函数靠内联消除调用成本；大深度递归改迭代或尾递归；避免在递归里分配大局部数组。",
    tags: ["综合", "调用成本", "递归", "栈溢出", "尾递归"],
  },
];
