import type { ReviewQuestion } from "./types";

/** CPU 眼里的 C++ · 内联与优化复习题 */
export const cpcInlineQuestions: ReviewQuestion[] = [
  {
    id: "cpc-inline-1",
    chapter: "cpc-inline",
    level: 1,
    question: "`inline` 关键字的作用是什么？它和编译器实际是否内联有什么关系？",
    answer:
      "`inline` 关键字有两个作用：\n\n1. 允许函数定义在头文件中而不违反 ODR（单一定义规则）：`inline` 函数可在多个翻译单元里被包含并定义，链接器会合并这些重复定义而不报错（COMDAT 折叠）。\n2. 给编译器一个「建议」：这个函数适合内联。\n\n与实际内联的关系：\n- `inline` 只是建议，编译器有权忽略。编译器会根据函数体大小、调用频率、是否递归、是否虚函数等自行决定。\n- 现代 C++（C++17）的 `inline` 变量也允许变量定义在头文件被多单元包含。\n- 即使没标 `inline`，编译器也会在 `-O2` 等优化级别主动内联适合的小函数（LTO 链接期优化甚至能跨翻译单元内联）。\n- 反之，标了 `inline` 的函数若太大或递归，编译器照样不内联。\n\n记忆线索：`inline` 主要解决「头文件定义」的链接问题，内联与否是编译器的决定，关键字只是建议。",
    tags: ["inline", "ODR", "内联建议", "COMDAT"],
  },
  {
    id: "cpc-inline-2",
    chapter: "cpc-inline",
    level: 2,
    question: "编译器决定是否内联一个函数时，会考虑哪些因素？为什么递归函数和虚函数通常难以内联？",
    answer:
      "编译器考虑的因素：\n1. 函数体大小：小函数（几行）优先内联，大函数内联后代码膨胀反而降低 icache 命中。\n2. 调用频率：热路径上的小函数优先。\n3. 优化收益：内联后能否暴露常量传播、死代码消除等后续优化机会。\n4. 代码膨胀代价：内联 N 处会让二进制变大，icache 压力增大，编译器有权拒。\n5. 是否有 `[[noreturn]]`、`__attribute__((always_inline))` 等强约束。\n\n递归难内联：\n- 递归调用次数在编译期未知，无法静态展开到底。编译器最多内联有限几层（如 2-3 层），之后仍保留递归调用。无限内联会让代码无限膨胀。\n- 尾递归可被优化为循环（TCO），那不是内联而是改写控制流。\n\n虚函数难内联：\n- 虚调用在编译期无法确定实际目标（取决于运行期对象类型），编译器只能通过 devirtualization（去虚化）尝试：若能证明对象类型确定（如局部对象直接调、final 类的指针），则去虚化后内联。\n- 否则保留间接调用，无法内联。\n\n所以内联的前提是「编译期能确定调用目标」，递归（次数不定）与虚函数（目标不定）都因不确定性而受阻。",
    tags: ["内联决策", "递归", "虚函数", "去虚化"],
  },
  {
    id: "cpc-inline-3",
    chapter: "cpc-inline",
    level: 3,
    question: "内联不是越多越好。过度内联会带来哪些问题？编译器如何平衡代码膨胀与性能收益？",
    answer:
      "过度内联的问题：\n\n1. 代码膨胀：每处内联都把函数体复制一份，二进制体积增大。一个 50 行函数被内联到 100 处，多出近 5000 行机器码。\n2. icache 压力：指令缓存（icache）容量有限，代码膨胀后热点指令跨多个缓存行，icache miss 上升。一次 miss 惩罚几十到上百周期，可能抵消内联省下的调用开销。\n3. 编译时间：更多代码要优化，编译变慢。\n4. 寄存器压力：内联后单个函数的寄存器需求增大，溢出到栈的变量变多，增加访存。\n\n编译器平衡方式：\n1. 启发式评分：按函数大小、调用点数、循环深度给内联打分，超阈值不内联。GCC/Clang 有 `-finline-limit`、`--param max-inline-insns-*` 等参数。\n2. PGO（Profile-Guided Optimization）：用运行 profile 知道哪些是真正的热点，只内联热路径，冷路径保留调用。\n3. LTO（Link-Time Optimization）：链接期能看到全局调用图，做更准的内联决策，跨翻译单元内联。\n4. 部分内联：把函数的快路径内联、慢路径保留为独立函数（函数分裂）。\n5. 反内联：若内联后发现某处膨胀严重，优化器可撤销内联。\n\n工程含义：不要盲目 `__attribute__((always_inline))`；让编译器在 `-O2`/`-O3` 自行决策；热路径用 PGO 精准指导；小而频繁的函数放头文件给内联机会。",
    tags: ["过度内联", "代码膨胀", "icache", "PGO", "LTO"],
  },
  {
    id: "cpc-inline-4",
    chapter: "cpc-inline",
    level: 4,
    question: "综合分析：内联如何与常量传播、死代码消除、循环展开等后续优化协同，产生超越「省一次调用」的收益？",
    answer:
      "内联的真正价值不在省掉 call/ret 的几条指令，而在于「打开后续优化的门」。内联后调用边界消失，编译器能看到调用点与函数体的完整上下文，触发连锁优化：\n\n1. 常量传播（Constant Propagation）：\n若调用点传入常量参数，内联后该参数在函数体内被替换为常量。如 `f(0)` 内联后，函数体里 `if (flag)` 的 flag 变成 0，该分支可被静态消除。\n\n2. 死代码消除（DCE）：\n常量传播后，恒不执行的分支成了死代码，编译器直接删除。原本函数里为通用参数准备的各分支，因内联后参数确定而整段消除。\n\n3. 循环展开与向量化：\n若函数体含循环，内联后循环边界可能因调用点常量而确定（如 `process<N>` 的 N 是编译期常量），编译器可展开循环、做 SIMD 向量化。不内联时编译器看不到调用点的 N。\n\n4. 寄存器分配全局化：\n内联后调用方与被调方合并成一个函数，寄存器分配跨原边界进行，减少溢出。\n\n5. 消除抽象惩罚：\n模板、`std::function`、lambda 等高层抽象经内联后塌缩成直连代码，让「零成本抽象」名副其实。\n\n协同案例：`std::accumulate(begin, end, 0)` 内联迭代器与仿函数后，常量传播消除类型分发，循环展开 + SIMD 向量化，最终塌缩成几条向量指令——比手写循环还快。这就是内联作为「优化管道入口」的威力。\n\n结论：内联不是孤立的优化，而是编译器优化链的第一环。单看省一次调用收益微小，但它解锁的常量传播、DCE、向量化能带来数量级提升。这也解释了为何虚函数（阻碍内联）的真正成本不是间接跳转，而是切断了整条优化链。",
    tags: ["综合", "内联协同", "常量传播", "死代码消除", "向量化"],
  },
];
