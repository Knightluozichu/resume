import type { ReviewQuestion } from "./types";

/** CPU 眼里的 C++ · 内存模型复习题 */
export const cpcMemoryModelQuestions: ReviewQuestion[] = [
  {
    id: "cpc-memory-model-1",
    chapter: "cpc-memory-model",
    level: 1,
    question: `C++ 程序运行时的虚拟地址空间分为哪几个主要段？各段存放什么？`,
    answer:
      `主要分五段：\n\n1. 代码段（Text）：存放编译后的机器指令，通常只读、可执行。\n2. 只读数据段（RODATA）：存放 \`const\` 常量、字符串字面量，只读。\n3. 数据段（Data）：存放已初始化的全局变量和静态变量，可读写。\n4. BSS 段：存放未初始化（或初始化为 0）的全局/静态变量，不占可执行文件空间，加载时由 OS 清零。\n5. 堆（Heap）：\`new\`/\`malloc\` 分配的动态内存，向上增长，由程序员管理。\n6. 栈（Stack）：局部变量、函数参数、返回地址，向下增长，自动管理。\n\n记忆线索：代码在最低地址，栈在最高地址向下长，堆在 BSS 之上向上长，中间是数据。`,
    tags: ["内存模型", "段", "栈堆"],
  },
  {
    id: "cpc-memory-model-2",
    chapter: "cpc-memory-model",
    level: 2,
    question: `为什么 BSS 段不占用可执行文件的磁盘空间，而 Data 段却占用？`,
    answer:
      `BSS 段存的是初始化为 0 或未初始化的全局/静态变量。因为它们的值全是 0，没必要在文件里存一堆 0，只需在程序加载时由操作系统按段大小分配内存并清零即可。所以可执行文件里 BSS 只记录「起始地址 + 大小」，不存实际数据。\n\nData 段存的是非 0 初值的全局/静态变量，这些初值必须保存在文件里，加载时原样拷贝到内存，否则程序恢复不了初始状态。\n\n工程意义：把一个大数组声明为全局并初始化为 0（进 BSS）比初始化为非 0（进 Data）可执行文件更小。例如 \`int arr[1000000] = {0}\` 进 BSS，文件几乎不增大；\`int arr[1000000] = {1}\` 第一个元素非 0 则整个数组进 Data，文件增大数 MB。`,
    tags: ["BSS", "Data 段", "可执行文件"],
  },
  {
    id: "cpc-memory-model-3",
    chapter: "cpc-memory-model",
    level: 3,
    question: `一个含虚函数的类 \`struct A { int x; virtual void f(); };\`，在 64 位系统下 \`sizeof(A)\` 是多少？为什么？`,
    answer:
      `\`sizeof(A)\` 通常是 16 字节（64 位系统下）。\n\n布局：\n1. 虚指针（vptr）：8 字节，放在对象起始位置，指向类的虚表。\n2. 数据成员 \`x\`：4 字节。\n3. 对齐填充：为使对象大小是最大成员（vptr，8 字节）对齐的整数倍，末尾补 4 字节填充。\n\n总计 8 + 4 + 4 = 16 字节。\n\n关键点：\n- 有虚函数就有 vptr，这是「虚函数的成本」之一——每个对象多 8 字节。\n- 对齐填充是为了 CPU 访存效率：未对齐的 8 字节访问可能跨缓存行甚至页边界，触发性能惩罚或硬件异常。\n- 成员声明顺序影响填充：把 \`x\` 放前面、再加一个 \`char\`，仍可能因对齐补齐到 16。\n\n这体现了「从 CPU 视角看对象」：语法上一个 \`int\` + 一个虚函数，内存里却是 vptr + int + 填充。`,
    tags: ["对象布局", "vptr", "对齐", "sizeof"],
  },
  {
    id: "cpc-memory-model-4",
    chapter: "cpc-memory-model",
    level: 4,
    question: `综合分析：栈和堆在分配速度、局部性、生命周期管理上的差异，如何影响 C++ 程序的性能设计？`,
    answer:
      `栈与堆的差异决定了 C++ 性能设计的核心取舍：\n\n1. 分配速度：\n- 栈：移动栈指针一条指令完成，O(1) 且极快。\n- 堆：\`new\` 要走内存分配器（查找空闲块、可能的系统调用 \`mmap\`/\`brk\`、维护元数据），慢几十到几百倍。\n\n2. 缓存局部性：\n- 栈：连续且紧凑，栈顶常驻 L1 缓存，命中率极高。\n- 堆：碎片化，对象散布在不连续地址，cache miss 频繁。\n\n3. 生命周期：\n- 栈：作用域结束自动回收，无泄漏风险，RAII 天然适配。\n- 堆：手动或靠智能指针管理，易泄漏或悬垂。\n\n性能设计含义：\n- 优先栈分配：能用局部变量就别 \`new\`，能用 \`std::array\` 就别 \`std::vector\` 动态分配。\n- 批量堆分配：必须用堆时，用对象池或自定义分配器把碎片化转连续，恢复局部性。\n- 数据布局：热路径对象集中放连续内存（SoA），减少 cache miss。\n- RAII + move：用移动语义转移堆资源所有权，避免深拷贝。\n\n本质：栈是「硬件原生支持的线性区」，堆是「软件模拟的自由区」。把热数据尽量留在栈或连续堆区，是榨取 CPU 的第一步。`,
    tags: ["综合", "栈堆对比", "局部性", "性能设计"],
  },
];
