import type { ReviewQuestion } from "./types";

/** CPU 眼里的 C++ · 虚函数实现复习题 */
export const cpcVirtualFunctionsQuestions: ReviewQuestion[] = [
  {
    id: "cpc-virtual-functions-1",
    chapter: "cpc-virtual-functions",
    level: 1,
    question: "什么是虚表（vtable）和虚指针（vptr）？它们分别存放在哪里？",
    answer:
      "虚表（vtable）：编译器为每个含虚函数的类生成的一张函数指针表，存放该类所有虚函数的实际地址。虚表本身是静态数据，存放在只读数据段（RODATA），程序运行期间不变，一个类一张。\n\n虚指针（vptr）：每个含虚函数的对象内部的一个隐藏指针，指向该对象所属类的虚表。vptr 存放在对象内存布局的起始位置（通常是前 8 字节），每个对象一个。\n\n工作机制：\n- 构造对象时，编译器在构造函数里把 vptr 设为指向本类的虚表。\n- 调用虚函数 `obj->f()` 时，CPU 先通过 obj 的 vptr 找到虚表，再按偏移取到 `f` 的实际地址，间接跳转过去。\n\n记忆线索：vtable 是「类的函数地址表」（共享、静态），vptr 是「对象的门牌」（每对象、指向本类表）。",
    tags: ["虚表", "虚指针", "vtable", "vptr"],
  },
  {
    id: "cpc-virtual-functions-2",
    chapter: "cpc-virtual-functions",
    level: 2,
    question: "虚函数调用 `p->f()` 相比普通函数调用，在 CPU 层面多出了哪些开销？",
    answer:
      "虚函数调用的额外开销：\n\n1. 一次额外访存取 vptr：要先从对象地址处读取 vptr（8 字节），才知道虚表在哪。\n2. 一次额外访存取函数指针：再从虚表对应偏移处读取实际函数地址。\n3. 间接跳转：`call [寄存器]` 而非 `call 直接地址`，CPU 无法在编译期确定目标，分支预测器压力增大（返回地址预测器 RSB 还能帮忙，但目标预测较难）。\n4. 阻碍内联：编译器在编译期不知道实际调用哪个派生类的函数，无法内联。\n\n普通调用：`call 直接地址`，编译器知道目标，可内联、可精确预测。\n\n实际影响：单次虚调用只多两次访存（若 vptr/虚表在缓存里则几纳秒），但若在紧密循环里调用数百万次，且虚表不在缓存，累积开销可观。更重要的是阻碍内联——内联能消除整个调用开销并开启后续优化，虚调用切断了这条优化链。\n\n所以虚函数的真正成本不在那两次访存，而在「关上了内联与跨函数优化的门」。",
    tags: ["虚函数成本", "间接跳转", "内联", "访存"],
  },
  {
    id: "cpc-virtual-functions-3",
    chapter: "cpc-virtual-functions",
    level: 3,
    question: "构造函数为什么不能是虚函数？而析构函数在什么情况下必须是虚函数？",
    answer:
      "构造函数不能虚：\n虚调用依赖 vptr 指向虚表，而 vptr 是在构造函数执行过程中才被设置好的。调用构造函数前，对象的 vptr 还未指向正确的虚表，无法进行虚分派——这是个「鸡生蛋」问题：要虚调构造函数需先有 vptr，要有 vptr 需先执行构造函数。所以构造函数不能虚。\n\n析构函数必须虚的场景：\n当通过基类指针 `delete` 一个派生类对象时，若析构函数非虚，编译器只看到基类指针，只调用基类析构函数——派生类析构函数不会被调用，派生类成员泄漏，UB。\n\n若析构函数是虚的：`delete p` 时通过虚表找到实际派生类的析构函数，先析构派生部分再析构基类部分，完整清理。\n\n规则：只要一个类打算被多态使用（有其他虚函数，或可能被继承并通过基类指针删除），其析构函数就必须是虚的。反之，final 类或不被多态删除的类（如 `std::vector` 内部）可用非虚析构省掉 vptr 开销。\n\n从 CPU 视角：虚析构只是虚表里多一个槽，与其他虚函数调用机制一致；非虚析构是直接 call，省一次间接跳转。",
    tags: ["构造函数", "虚析构", "多态", "vptr"],
  },
  {
    id: "cpc-virtual-functions-4",
    chapter: "cpc-virtual-functions",
    level: 4,
    question: "综合分析：多重继承下虚表是如何布局的？为什么多重继承的对象指针调整（thunk）会带来额外成本？",
    answer:
      "多重继承虚表布局：\n\n单继承时对象只有一个 vptr，指向一张虚表。多重继承时，对象内部按声明顺序包含每个基类的子对象，每个有虚函数的基类子对象各带一个 vptr，对应多张虚表（或一张虚表的多个子表）。\n\n例如 `class D : public A, public B {}`，D 对象内存里先是 A 子对象（含 vptr_A），再是 B 子对象（含 vptr_B），最后是 D 自己的成员。D 的构造函数把 vptr_A 指向 A-in-D 的虚表，vptr_B 指向 B-in-D 的虚表。\n\nthunk 与指针调整：\n当 `B* pb = new D;` 时，pb 不能指向对象起始地址（那里是 A 子对象），而要偏移到 B 子对象的位置。`delete pb` 调用虚析构时，虚表里的析构函数地址对应的可能是一个 thunk——一段先做 `this` 指针减偏移（把 B 子对象指针还原为 D 起始指针），再跳转到真正析构函数的小代码。\n\n额外成本：\n1. 对象更大：每个虚基类子对象各一个 vptr。\n2. 指针转换有偏移计算：基类指针转派生类指针可能要加减常量。\n3. thunk 多一次跳转：虚调用经过 thunk 时多一层间接。\n4. 缓存压力：多张虚表散布在内存，cache miss 概率上升。\n\n工程含义：多重继承（尤其菱形虚继承）的运行期成本显著高于单继承。能用组合替代继承就别用多继承；必须多继承时，优先单继承 + 接口（纯虚、无数据成员）降低布局复杂度。",
    tags: ["综合", "多重继承", "虚表布局", "thunk", "指针调整"],
  },
];
