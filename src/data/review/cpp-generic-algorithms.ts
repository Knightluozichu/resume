/** 复习题库 · C++ 泛型算法（cpp-generic-algorithms）。C++ Primer §10 改编章节。 */

import type { ReviewQuestion } from "./types";

export const cppGenericAlgorithmsQuestions: ReviewQuestion[] = [
  // ── L1 认记 ──
  {
    id: "cga-1",
    chapter: "cpp-generic-algorithms",
    level: 1,
    question: "C++ 泛型算法的「泛型」是什么意思？为什么说算法与容器是解耦的？",
    answer: "泛型指算法可以用于任意容器类型——只要容器提供符合要求的迭代器。算法不直接操作容器本身，而是通过迭代器范围 [begin, end) 来访问元素。这样同一个 find 可以用在 vector、list、deque、数组上，无需为每种容器重写。",
    tags: ["泛型算法", "解耦"],
  },
  {
    id: "cga-2",
    chapter: "cpp-generic-algorithms",
    level: 1,
    question: "标准库算法大多定义在哪个头文件？数值算法在哪个头文件？",
    answer: "泛型算法大多在 `<algorithm>` 头文件（如 find、sort、copy、count 等）。数值算法（accumulate、inner_product 等）在 `<numeric>` 头文件。",
    tags: ["头文件", "algorithm", "numeric"],
  },
  {
    id: "cga-3",
    chapter: "cpp-generic-algorithms",
    level: 1,
    question: "`find(v.begin(), v.end(), val)` 的三个参数分别是什么意思？返回值是什么？",
    answer: "前两个参数 `[begin, end)` 构成一个迭代器范围——要搜索的区间（左闭右开）。第三个参数是要查找的目标值。返回值是一个迭代器：找到时指向第一个匹配元素的位置，未找到时返回 end()。",
    tags: ["find", "迭代器范围"],
  },
  {
    id: "cga-4",
    chapter: "cpp-generic-algorithms",
    level: 1,
    question: "`count` 和 `accumulate` 分别在干什么？它们需要哪个头文件？",
    answer: "`count(beg, end, val)` 统计区间内等于 val 的元素个数。`accumulate(beg, end, init)` 对区间内所有元素求和（初始值 init）。`count` 在 `<algorithm>`，`accumulate` 在 `<numeric>`。",
    tags: ["count", "accumulate"],
  },
  {
    id: "cga-5",
    chapter: "cpp-generic-algorithms",
    level: 1,
    question: "`fill` 和 `copy` 两个算法分别做什么？",
    answer: "`fill(beg, end, val)` 把区间内所有元素都设为 val。`copy(beg, end, dest)` 把区间内元素拷贝到 dest 开始的位置，返回拷贝后 dest 的尾后迭代器。两者都需要目标区间有足够的空间。",
    tags: ["fill", "copy"],
  },
  {
    id: "cga-6",
    chapter: "cpp-generic-algorithms",
    level: 1,
    question: "`sort` 用什么排序算法？它的时间复杂度是多少？",
    answer: "`sort` 通常使用快速排序的变体（内省排序 introsort），平均时间复杂度 O(n log n)。它要求迭代器是随机访问迭代器（如 vector、deque、array 的迭代器），不能用于 list。",
    tags: ["sort", "时间复杂度"],
  },
  {
    id: "cga-7",
    chapter: "cpp-generic-algorithms",
    level: 1,
    question: "`unique` 算法做什么？它真的删除了重复元素吗？",
    answer: "`unique` 把相邻重复元素移到末尾，返回指向不重复区域尾后位置的迭代器。它**不删除**任何元素——只是重新排列，尾部元素的值是未定义的。要真正删除需要配合 `erase`：`v.erase(unique(v.begin(), v.end()), v.end())`（erase-remove idiom）。",
    tags: ["unique", "删除"],
  },
  {
    id: "cga-8",
    chapter: "cpp-generic-algorithms",
    level: 1,
    question: "lambda 表达式的基本语法是什么？各部分叫什么？",
    answer: "`[capture](params) -> ret { body }`。`[capture]` 捕获列表（决定外部变量怎样被 lambda 使用），`(params)` 参数列表（可省略括号），`-> ret` 返回类型（可省略让编译器推导），`{ body }` 函数体。",
    tags: ["lambda", "语法"],
  },

  // ── L2 理解 ──
  {
    id: "cga-9",
    chapter: "cpp-generic-algorithms",
    level: 2,
    question: "为什么 list 有自己的 `sort` 成员函数，而不能用标准库的 `sort`？",
    answer: "标准库 `sort` 要求随机访问迭代器（支持 `it + n`、`it1 - it2` 等操作）。list 提供的是双向迭代器——只能 `++`/`--`，不满足要求。list 的 `sort()` 成员函数内部使用归并排序，适合链表的节点交换。",
    tags: ["sort", "list", "迭代器"],
  },
  {
    id: "cga-10",
    chapter: "cpp-generic-algorithms",
    level: 2,
    question: "`copy(beg, end, dest)` 不会自动扩容 dest 容器。为什么会踩这个坑？正确写法是什么？",
    answer: "`copy` 只是通过迭代器赋值——它假设 dest 已经有足够的元素。如果 dest 是空的 vector，`copy` 就是在往不存在的内存写东西（未定义行为）。正确写法：用 `back_inserter(dest)` 作为目标迭代器——`copy(v.begin(), v.end(), back_inserter(dest));`。back_inserter 在每次赋值时自动调用 push_back 扩容。",
    tags: ["copy", "back_inserter"],
  },
  {
    id: "cga-11",
    chapter: "cpp-generic-algorithms",
    level: 2,
    question: "lambda 的捕获列表 `[=]` 和 `[&]` 分别是什么意思？`[=, &x]` 呢？",
    answer: "`[=]` 按值捕获所有外部变量（拷贝一份，lambda 内的修改不影响外部）。`[&]` 按引用捕获所有外部变量（lambda 内的修改直接影响外部）。`[=, &x]` 默认按值捕获，但 x 按引用捕获。反过来 `[&, x]` 默认按引用捕获，但 x 按值捕获。",
    tags: ["lambda", "捕获"],
  },
  {
    id: "cga-12",
    chapter: "cpp-generic-algorithms",
    level: 2,
    question: "`stable_sort` 和 `sort` 的区别是什么？什么时候需要 `stable_sort`？",
    answer: "`sort` 不保证相等元素的相对顺序不变。`stable_sort` 保证相等元素保持排序前的相对顺序。当你按一个标准排序、且想保留另一个标准的顺序时（如先按成绩排序、同成绩的按学号序），需要 `stable_sort`。代价是稳定排序通常比 sort 稍慢。",
    tags: ["stable_sort", "稳定性"],
  },
  {
    id: "cga-13",
    chapter: "cpp-generic-algorithms",
    level: 2,
    question: "什么是「谓词」？`sort` 的第三个参数是什么类型？",
    answer: "谓词（predicate）是一个可调用对象，接受参数并返回 bool——表示「是否满足某条件」。`sort` 的第三个参数是一个二元谓词，接受两个元素返回 bool，用于定义排序规则。如 `sort(v.begin(), v.end(), [](int a, int b) { return a > b; })` 按降序排列。",
    tags: ["谓词", "sort"],
  },
  {
    id: "cga-14",
    chapter: "cpp-generic-algorithms",
    level: 2,
    question: "算法的「迭代器范围」为什么是左闭右开 `[begin, end)`？",
    answer: "左闭右开的设计让迭代器算术很自然：`end - begin` 正好是元素个数、空范围就是 `begin == end`。且对于尾后位置 end()（不存在的数据），用 `<` 比较自然终止。如果范围是闭区间，空范围没法表示（因为一个空的容器 begin() 就等于 end()）。",
    tags: ["迭代器范围", "左闭右开"],
  },

  // ── L3 应用 ──
  {
    id: "cga-15",
    chapter: "cpp-generic-algorithms",
    level: 3,
    question: "下面代码想删除 vector 中所有等于 0 的元素，有什么问题？写出正确写法。\n`vector<int> v{1, 0, 2, 0, 3}; for (auto it = v.begin(); it != v.end(); ++it) if (*it == 0) v.erase(it);`",
    answer: "在遍历过程中 erase 会使当前迭代器失效——`++it` 在失效的迭代器上操作是未定义行为。正确写法：① `v.erase(remove(v.begin(), v.end(), 0), v.end());`（erase-remove idiom）；② 或用 `v.erase(remove_if(...))`；③ 手动 erase 时要写 `it = v.erase(it);`（erase 返回被删元素之后的有效迭代器），并只在没 erase 时才 ++it。",
    tags: ["erase", "迭代器失效", "remove"],
  },
  {
    id: "cga-16",
    chapter: "cpp-generic-algorithms",
    level: 3,
    question: "写一个 lambda，接受 `vector<string>`，返回按字符串长度从短到长排序后的新 vector。用 `sort` 实现。",
    answer: "```cpp\nvector<string> v = {\"apple\", \"pie\", \"a\", \"banana\"};\nsort(v.begin(), v.end(),\n     [](const string &a, const string &b) {\n         return a.size() < b.size();\n     });\n// 结果: {\"a\", \"pie\", \"apple\", \"banana\"}\n```\nlambda 接受两个 `const string&`，返回 `a.size() < b.size()`，按长度升序排列。",
    tags: ["lambda", "sort", "字符串"],
  },
  {
    id: "cga-17",
    chapter: "cpp-generic-algorithms",
    level: 3,
    question: "用 `find_if` 和一个合适的谓词，在 `vector<int>` 中找到第一个大于 10 的元素。写出代码。",
    answer: "```cpp\nauto it = find_if(v.begin(), v.end(),\n                [](int x) { return x > 10; });\nif (it != v.end())\n    cout << \"first > 10: \" << *it << endl;\n```\n`find_if` 接受一元谓词——对每个元素调用，第一个返回 true 的就是匹配元素。",
    tags: ["find_if", "一元谓词"],
  },
  {
    id: "cga-18",
    chapter: "cpp-generic-algorithms",
    level: 3,
    question: "有一个 `vector<int> src{1,2,3,4,5}`，用 `transform` 和 lambda 生成一个新 vector，每个元素变为原元素的平方。",
    answer: "```cpp\nvector<int> src{1,2,3,4,5};\nvector<int> dest;\ntransform(src.begin(), src.end(), back_inserter(dest),\n          [](int x) { return x * x; });\n// dest = {1, 4, 9, 16, 25}\n```\n`back_inserter` 确保 dest 自动扩容。如果 dest 已预留大小可以不用 back_inserter，写 `dest.begin()`。",
    tags: ["transform", "back_inserter"],
  },
  {
    id: "cga-19",
    chapter: "cpp-generic-algorithms",
    level: 3,
    question: "`v.erase(unique(v.begin(), v.end()), v.end());` 这行 erase-remove idiom 为什么能删除所有重复元素？分步解释。",
    answer: "`unique(v.begin(), v.end())` 把所有不重复的元素紧凑排列到前面，返回一个指向「不重复区域尾后」的迭代器。后面被移走的元素值未定义。`v.erase(返回的迭代器, v.end())` 把尾部那些废弃元素真正从容器中删除。两步合并：先去重→再截断。",
    tags: ["erase-remove", "unique"],
  },
  {
    id: "cga-20",
    chapter: "cpp-generic-algorithms",
    level: 3,
    question: "`count_if(v.begin(), v.end(), pred)` 和 `find_if(v.begin(), v.end(), pred)` 的区别是什么？",
    answer: "`count_if` 返回满足谓词的元素**个数**（int）。`find_if` 返回指向第一个满足谓词元素的**迭代器**。一个是统计数量，一个是定位位置。",
    tags: ["count_if", "find_if"],
  },

  // ── L4 综合 ──
  {
    id: "cga-21",
    chapter: "cpp-generic-algorithms",
    level: 4,
    question: "你需要从一个 `vector<string>` 中去掉所有空字符串，然后将剩下的按长度降序排列（长度相同按字典序升序），最后只保留前 5 个。写出完整代码，解释每步用的算法和原因。",
    answer: "```cpp\n// 1. remove_if + erase 去掉空字符串\nv.erase(remove_if(v.begin(), v.end(),\n    [](const string &s) { return s.empty(); }), v.end());\n// 2. sort 按长度降序 + 字典序升序\nsort(v.begin(), v.end(), [](const string &a, const string &b) {\n    return a.size() != b.size() ? a.size() > b.size() : a < b;\n});\n// 3. 截断前 5 个\nif (v.size() > 5) v.resize(5);\n```\n步骤 1 用 erase-remove idiom 安全删除。步骤 2 用 sort + 复合比较 lambda。步骤 3 用 resize 截断比 erase 更直观。",
    tags: ["remove_if", "sort", "综合"],
  },
  {
    id: "cga-22",
    chapter: "cpp-generic-algorithms",
    level: 4,
    question: "`partition`、`stable_partition`、`sort` 三者的区别是什么？给你一个数组 `{3,1,4,1,5,2}`，用 `<=2` 作谓词分区，`partition` 和 `stable_partition` 的结果分别是怎样的？",
    answer: "三者都按谓词把元素分两组，区别在于是否保序。`stable_partition` 保持各组内部的原始相对顺序——对 `{3,1,4,1,5,2}` 用 `<=2`，满足的 `1,1,2` 按原序在前、不满足的 `3,4,5` 按原序在后，结果**确定**为 `{1,1,2,3,4,5}`。`partition` 只保证满足谓词的元素在前、不满足的在后，**不保证各组内部顺序**——前段一定是 `{1,1,2}` 的某种排列、后段是 `{3,4,5}` 的某种排列，具体顺序由实现决定、不可依赖（例如可能得到 `{2,1,1,5,3,4}`）。`sort` 是全排序、完全重排。三者复杂度：partition O(n)，stable_partition O(n log n)（可能需要额外内存），sort O(n log n)。",
    tags: ["partition", "stable_partition", "综合"],
  },
  {
    id: "cga-23",
    chapter: "cpp-generic-algorithms",
    level: 4,
    question: "写一个函数 `top_n`，接受 `vector<int>` 和 int n，返回 vector 中最大的 n 个元素（无序）。要求时间复杂度优于 O(n log n)，并解释为什么。",
    answer: "```cpp\nvector<int> top_n(vector<int> v, int n) {\n    nth_element(v.begin(), v.begin() + n, v.end(), greater<int>());\n    return vector<int>(v.begin(), v.begin() + n);\n}\n```\n`nth_element` 的平均复杂度 O(n)：它把第 n 个位置放上「如果全排序应该放那的元素」，前面全是 ≥ 它的（因为我们用了 greater<int>()），后面是 ≤ 它的。然后取前 n 个即可。比 sort（O(n log n)）快。",
    tags: ["nth_element", "top_n", "复杂度"],
  },
  {
    id: "cga-24",
    chapter: "cpp-generic-algorithms",
    level: 4,
    question: "有 100 万个 `pair<string, int>`（姓名, 分数）。先按分数降序排列、同分按姓名字典序升序。然后遍历输出前 100 个。哪种组合最高效：sort + 排序规则 vs partial_sort？为什么？",
    answer: "`partial_sort` 最高效。它只需把前 100 个排到位，剩余 99.99 万个元素不需要完整排序。时间复杂度 O(N log K) = O(1M × log 100) ≈ O(1M × 7)，而 sort 是 O(N log N) = O(1M × 20)。代码：`partial_sort(v.begin(), v.begin()+100, v.end(), comp);`。如果只需要前 100 个但不在乎它们之间是否有序，还能用 `nth_element` 进一步降到 O(N)。",
    tags: ["partial_sort", "性能", "综合"],
  },
];

export default cppGenericAlgorithmsQuestions;
