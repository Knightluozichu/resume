import type { ReviewQuestion } from "./types";

/** 类与组织复习题 */
export const cqrClassesQuestions: ReviewQuestion[] = [
  {
    id: "cqr-classes-01",
    chapter: "cqr-classes",
    level: 1,
    question: `SRP（单一职责原则）在类设计中如何体现？`,
    answer:
      `SRP（Single Responsibility Principle，单一职责原则）：一个类应该只有一个「引起它变化的原因」，即只承担一个职责。\n\n在类设计中的体现：\n\n1. 一个类只代表一个「角色/概念」：\n类应该围绕一个清晰的业务概念或职责来组织。如 \`User\` 类只表示「用户」这个概念，\`UserRepository\` 只负责「用户的存取」，\`EmailSender\` 只负责「发邮件」。不要让 \`User\` 既管用户数据、又管存数据库、又管发邮件——那是三个职责。\n\n2. 「变化的原因」是判断标准：\nSRP 的精确定义不是「只做一件事」，而是「只有一个变化轴」。问自己：「这个类的哪些部分会因同一个原因而变化？哪些会因不同原因而变化？」不同原因驱动变化的部分应拆到不同类。\n- 例：一个 \`Employee\` 类有「计算工资」和「保存到数据库」两块逻辑。工资计算规则会因「财务政策」变化，数据库保存会因「存储技术」变化——两个变化轴，应拆成 \`Employee\`（业务）和 \`EmployeeRepository\`（持久化）。\n\n3. 职责的「粒度」要恰当：\nSRP 不是把类拆得越小越好。一个职责可以包含多个相关方法。如 \`UserRepository\` 有 \`findById\`、\`save\`、\`delete\` 等多个方法，但它们都服务于「用户持久化」这一个职责，属于同一个变化轴，不必再拆。\n\n4. 识别「职责过载」的信号：\n- 类名含「And」「Manager」「Helper」「Util」往往意味着多职责。\n- 一个类有几十个公开方法、几千行代码。\n- 类的某些方法从不被一起使用（说明它们可能属于不同职责）。\n- 改动这个类时常常牵连「不相关」的部分。\n\nSRP 的价值：\n1. 可维护性：一个职责变化只影响对应类，不牵连其他。改动范围小、风险低。\n2. 可复用性：职责单一的类容易在新场景复用；多职责的类带一堆不需要的依赖，难以复用。\n3. 可测试性：职责单一好构造测试场景；多职责类要 mock 一堆依赖才能测某个方法。\n4. 可读性：读者一眼知道「这个类是干什么的」，不必在杂乱的代码里找相关部分。\n\n注意：SRP 是设计原则而非教条。早期需求不清晰时，可以先有一个稍大的类，等「多个变化轴」真正显现再拆分——避免过度设计。判断的核心是「变化轴」，凭经验与对业务的理解。`,
    tags: ["SRP", "单一职责", "类设计", "变化轴"],
  },
  {
    id: "cqr-classes-02",
    chapter: "cqr-classes",
    level: 2,
    question: `内聚度（Cohesion）与耦合度（Coupling）是什么关系？好的设计应追求什么？`,
    answer:
      `内聚度与耦合度是衡量软件模块化质量的两个核心指标，方向相反——好的设计追求「高内聚、低耦合」。\n\n内聚度（Cohesion）——模块「内部」的紧密度：\n指一个模块（类/函数）内部各元素「为同一目标协作」的程度。内聚度高，意味着模块内所有部分都紧密服务于同一个职责，没有「搭便车」的不相关代码。\n- 高内聚（好）：\`EmailSender\` 类里只有发邮件相关的方法和字段，所有元素都围绕「发邮件」。\n- 低内聚（坏）：一个 \`Utils\` 类里既有发邮件、又有算税、又有格式化日期——元素之间毫无关系，只是被塞在一起。\n\n耦合度（Coupling）——模块「之间」的依赖程度：\n指一个模块对其他模块的依赖有多强、多复杂。耦合度低，意味着模块独立、改动一个不容易波及另一个。\n- 低耦合（好）：\`OrderService\` 通过 \`PaymentGateway\` 接口依赖支付，不关心具体实现，可替换。\n- 高耦合（坏）：\`OrderService\` 直接 \`new AlipaySDK()\`、直接读 \`Order\` 的私有字段、调用十几个其他类——改动任何一个都可能影响它。\n\n两者的关系：\n\n1. 互为表里、相互影响：\n高内聚往往带来低耦合。一个职责清晰的模块（高内聚）不需要到处依赖其他模块来完成自己的事，自然耦合低。反之，低内聚的「杂物间」类为了完成各种杂事，必然要依赖很多模块，耦合高。\n\n2. 共同决定「可维护性」：\n- 高内聚让「修改某个职责」只动一个模块，改动集中。\n- 低耦合让「修改一个模块」不波及其他模块，影响范围小。\n两者结合，系统的「改动局部化」——改一处只影响一处，这正是可维护性的核心。\n\n3. 共同决定「可复用性」与「可测试性」：\n- 高内聚模块功能聚焦，复用时不会带入一堆无关依赖。\n- 低耦合模块独立，测试时容易隔离（少 mock）。\n\n好的设计追求——高内聚、低耦合：\n\n1. 用「职责」划分模块，而非「层级」或「类型」：\n围绕业务能力组织模块（如 \`Order\`、\`Payment\`、\`Shipping\`），每个模块内聚完整。避免按技术分层把相关逻辑分散到各处（如所有数据访问塞进一个 \`DataAccess\` 大类）。\n\n2. 通过「接口/抽象」降低耦合：\n依赖接口而非具体实现（依赖倒置）。这样具体实现可替换、可 mock，模块间解耦。\n\n3. 用「信息隐藏」降低耦合：\n只暴露必要的最小接口，隐藏内部实现细节（封装）。其他模块只依赖你的「契约」，不依赖你的「实现」，实现可自由变化。\n\n4. 警惕「数据耦合」与「控制耦合」：\n- 数据耦合：模块间通过传递大量数据结构耦合，一旦数据结构变，全受影响。应封装成对象、暴露行为而非裸数据。\n- 控制耦合：模块间传递「标志位」控制对方行为，说明职责划分不清。应重新划分职责。\n\n5. 平衡——不是「零耦合」：\n模块化必然有耦合（否则就是一盘散沙）。目标是「耦合尽可能低、且耦合点清晰可控」。完全无耦合的模块也无法协作。关键是让耦合「通过明确的接口」发生，而非「通过隐式的实现细节」。\n\n记忆口诀：内聚看「内部是否团结」，耦合看「外部是否纠缠」。好设计是「内部团结、外部清爽」。`,
    tags: ["内聚度", "耦合度", "高内聚低耦合", "模块化"],
  },
  {
    id: "cqr-classes-03",
    chapter: "cqr-classes",
    level: 3,
    question: `如何识别和拆分 God Class（上帝类）？`,
    answer:
      `God Class（上帝类）是承担过多职责的巨型类，是低内聚高耦合的典型反模式。识别与拆分方法：\n\n如何识别 God Class：\n\n1. 体积信号：\n- 类行数过多（如数千行）、公开方法几十个、字段几十个。\n- 一个文件大到 IDE 卡顿、grep 结果占满屏幕。\n\n2. 命名信号：\n- 类名含 \`Manager\`、\`Helper\`、\`Util\`、\`Processor\`、\`Service\`（且没有具体限定）、\`God\`、\`Everything\` 等空泛词。\n- 名字没法说清「它到底负责什么」，因为太多。\n\n3. 职责信号——「变化轴」多于一个：\n- 不同需求改动都落到这个类。如「改工资算法」「改数据库表结构」「改邮件模板」都要动 \`Employee\` 类——说明它扛了多职责。\n- 类内有「互不相关」的方法组，某些方法从不被一起调用。\n\n4. 依赖信号：\n- import 了一长串不相关的模块（既 import 数据库、又 import 邮件、又 import 日志）。\n- 很多不同模块都依赖它，改它牵动全系统。\n\n5. 字段信号：\n- 字段可分成「几组互不相关」的集合，每组只被部分方法使用。这是「多个职责挤一个类」的强证据。\n\n6. 测试信号：\n- 测试这个类要 mock 一堆依赖、构造复杂场景，测试本身巨大且脆弱。\n\n如何拆分 God Class：\n\n1. 先建立安全网——补测试：\nGod Class 通常缺测试（因为难测）。拆分前先写「特征测试（characterization tests）」覆盖其当前行为——不判断对错，只钉住「现在输入产生什么输出」。拆分时跑这些测试确保行为不变。这是安全拆分的前提。\n\n2. 分析职责，按「变化轴」分组：\n通读类，把字段和方法按「它们服务的职责」分组。一组里通常是「一起被使用的字段 + 操作这些字段的方法」。每组就是一个候选的新类。\n- 例：\`Employee\` 类里，工资相关字段+方法是一组，持久化相关是另一组，邮件通知又是一组。\n\n3. 用「提取类」手法逐组拆出：\n从最独立的一组开始（依赖最少的）。手法：\n- 把这组字段和方法移到一个新类，起一个体现职责的名字（如 \`PayrollCalculator\`）。\n- 原 God Class 持有新类的引用，把相关调用委托过去。\n- 每拆一组，跑测试验证行为不变。\n- 渐进拆分，每步可提交、可回退。\n\n4. 处理「跨组共享数据」：\nGod Class 拆分的主要难点是字段被多组方法共用。处理：\n- 共享的「核心数据」留在原类（如 \`Employee\` 的基本属性），各组通过参数传入或通过接口访问。\n- 共享的「辅助数据」移到使用最频繁的那组，其他组按需传递。\n- 避免「数据泥团」——若几个字段总是一起被多组使用，考虑封装成一个值对象。\n\n5. 消除「临时的内容耦合」：\n拆分后如果新类之间仍频繁互调对方内部，说明职责边界划得不对，应调整——可能某些方法该回到一起，或需要重新分组。\n\n6. 用接口进一步解耦：\n拆出的类如果被多处依赖，定义接口（如 \`PayrollCalculator\` 接口），原 God Class 依赖接口而非具体类，便于替换和测试。\n\n7. 渐进式，不要一次拆完：\nGod Class 往往是多年积累，一次大拆风险高、评审难。采用「童子军规则」——每次改动时顺手拆出一部分，几次迭代后逐步瓦解。每步都跑测试、都提交。\n\n示例：一个 \`UserManager\` God Class（3000 行，含用户数据、数据库存取、邮件通知、权限校验、日志记录），拆成：\n- \`User\`（数据模型）\n- \`UserRepository\`（持久化）\n- \`EmailNotifier\`（邮件）\n- \`PermissionChecker\`（权限）\n- \`UserService\`（协调者，只编排上述类）\n每个类几百行、职责单一，可独立测试与演化。\n\n注意：拆分后要警惕「过度拆分」——不要拆成无数个只有一个方法的小类，那会让系统碎片化、跳转频繁。目标是「每个类职责清晰且大小适中」，不是「越小越好」。`,
    tags: ["God Class", "上帝类", "拆分", "提取类", "重构"],
  },
  {
    id: "cqr-classes-04",
    chapter: "cqr-classes",
    level: 4,
    question: `依赖注入（Dependency Injection）如何降低类间耦合？它有哪些形式和代价？`,
    answer:
      `依赖注入（Dependency Injection，DI）：一个类「需要依赖的对象」不由自己创建（不 \`new\`），而是由外部「注入」进来。它是实现「依赖倒置」、降低类间耦合的核心手段。\n\n如何降低耦合：\n\n1. 从「依赖具体」变为「依赖抽象」：\n不注入时：\`class OrderService { private payment = new AlipaySDK(); }\`——\`OrderService\` 硬编码依赖 \`AlipaySDK\` 这个具体类，换支付方式就要改 \`OrderService\` 源码，耦合死。\n注入后：\`class OrderService { constructor(private payment: PaymentGateway) {} }\`——\`OrderService\` 只依赖 \`PaymentGateway\` 接口，具体用 Alipay 还是 WeChat 由外部决定。\`OrderService\` 与具体实现解耦，可替换、可扩展。\n\n2. 把「创建」与「使用」分离：\n类不再负责「创建依赖」，只负责「使用依赖」。创建逻辑集中到外部（容器/工厂/组装代码），类本身只关心业务。这样：\n- 依赖的实现变化不影响类（换实现不改类）。\n- 类可独立测试（注入 mock 而非真实依赖）。\n\n3. 显式化依赖关系：\n类的依赖通过构造函数参数「显式」声明，看签名就知道「这个类需要什么」。隐藏的依赖（在方法内部 \`new\` 的）难以发现、难以管理；注入让依赖「可见、可审计」。\n\n4. 支持单一职责：\n当一个类自己 \`new\` 一堆依赖时，它往往承担了「组装」职责之外的本职工作。注入后，类只做业务，组装由专门的地方负责，职责更清晰。\n\n依赖注入的形式：\n\n1. 构造函数注入（最推荐）：\n依赖通过构造函数参数传入，存为字段。\n- 优点：依赖在对象创建时就齐全，对象一旦构造完成就处于「可用」状态，不变量易保证；依赖关系一目了然。\n- 缺点：依赖多时构造函数参数列表长（往往是「类职责过多」的信号）。\n\n2. Setter 注入：\n依赖通过 setter 方法注入，对象先创建后设置。\n- 优点：可灵活设置/替换依赖；适合可选依赖。\n- 缺点：对象可能在「依赖未设置」时被使用，处于不完整状态；依赖是隐式的（不看 setter 不知需要什么）。\n\n3. 接口注入：\n类实现一个接口，容器通过接口方法注入依赖。较少用，语法重。\n\n4. 方法参数注入：\n依赖作为方法参数传入，不存为字段。适合「只在这个方法用一次」的依赖，避免污染类状态。\n\n5. 容器/框架自动注入：\n用 DI 容器（Spring、.NET DI、NestJS）或 IoC 框架自动解析依赖图、注入。开发者只声明依赖，容器负责构造和装配。适合大型应用，但增加「魔法」和调试复杂度。\n\n代价：\n\n1. 增加间接性，理解成本上升：\n代码里看到的是接口，真实实现要追到「组装配置」才能找到。对新人或调试来说，「这个依赖到底是谁」不那么直观。过度使用容器时尤甚——依赖图藏在配置里，像黑盒。\n\n2. 样板代码增加：\n构造函数、字段、注入配置都要写，简单类会变啰嗦。小项目用容器可能是「杀鸡用牛刀」。\n\n3. 初始化顺序与生命周期复杂：\n依赖之间可能有顺序、循环依赖、生命周期（单例/请求级/瞬态）问题。容器要正确管理这些，配置错了会出现「依赖未就绪」「循环依赖报错」等问题。\n\n4. 可能被滥用为「全局变量」：\n如果把什么都注入成「单例依赖」，本质就是全局变量换了个名字，耦合只是被藏起来而非消除。注入的是「抽象」才有意义，注入「具体单例到处用」没解决耦合。\n\n5. 运行时开销（容器场景）：\n反射式注入（如 Spring）有反射开销，启动时构建依赖图也有耗时。对性能极致敏感的场景需谨慎。\n\n何时用、何时不用：\n- 用：类依赖「会变化、需替换、需 mock 测试」的协作对象。尤其是业务逻辑类依赖外部资源（数据库、网络、第三方服务）时，注入让它们可替换可测试。\n- 不用：依赖是「稳定不变、无副作用」的（如纯函数工具、值对象），直接用即可，注入是过度设计。\n- 原则：注入「跨边界、易变、有副作用」的依赖；不注入「稳定、纯、无副作用」的依赖。\n\n与依赖倒置（DIP）的关系：\nDI 是「手段」，DIP 是「目标」。DIP 说「高层不应依赖低层，都依赖抽象」；DI 通过「从外部注入抽象」来实现这一点。没有 DIP 思想的 DI（注入具体类）只是搬弄语法，没有真正解耦。\n\n总结：依赖注入把「创建依赖」的权力交出类外，让类只「依赖抽象」。它降低耦合、提升可测性与可替换性，代价是增加间接性和配置复杂度。用得好的标志是「类变得好测、依赖可替换」，用得坏的标志是「容器配置比业务代码还复杂、调试要追三层魔法」。`,
    tags: ["依赖注入", "DI", "耦合", "依赖倒置", "可测试性"],
  },
];
