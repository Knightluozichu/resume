import type { ReviewQuestion } from "./types";

/** 注释与格式复习题 */
export const cqrCommentsFormatQuestions: ReviewQuestion[] = [
  {
    id: "cqr-comments-format-01",
    chapter: "cqr-comments-format",
    level: 1,
    question: `好注释和坏注释各有哪些类型？`,
    answer:
      `注释本身不是「好」或「坏」，关键看它「为什么存在」。分类如下：\n\n好注释（值得写）：\n1. 法律/版权声明：文件头部的 license 信息，由法务要求，必须保留。\n2. 解释意图（Why）：说明「为什么这么做」，尤其是看起来奇怪但有理由的代码。如「// 用位运算代替乘法，因为这里是性能热路径」。\n3. 警示：提醒后续维护者注意的陷阱。如「// 不要在这里加锁，会导致死锁，见 issue #123」。\n4. TODO/FIXME：标记待办事项，但要带责任人和具体内容，不能是模糊的「以后再说」。\n5. 公开 API 的文档注释：对外接口的契约说明（参数含义、返回值、异常、前置条件），供调用方参考。\n6. 对「无法自解释」的外部约束的说明：如「// 第三方库 v2.3 的 bug，必须先 reset 再调用」。\n\n坏注释（应避免或删除）：\n1. 多余的废话：把代码字面意思翻译一遍。如 \`i++; // i 加 1\`——代码本身已说明，注释是噪音。\n2. 误导性注释：注释说的和代码做的已经不一致（代码改了注释没改），比没注释更危险。\n3. 注释掉的死代码：被注释掉的历史代码堆在文件里，没人敢删。版本控制已经记住历史，应当直接删除。\n4. 位置标记/署名/闲聊：\`// ====== 这里是用户模块 ======\`、\`// added by Tom 2021\`、\`// 哈哈终于跑通了\`——都是噪音。\n5. 闭括号注释：\`} // end of for\`、\`} // end of if\`——如果嵌套深到需要这种注释，说明该提取函数了，而不是加注释。\n6. 用注释「解释」烂代码：注释在为「代码本身不清楚」辩护。正解是改好代码，让注释变得多余。\n7. 强制但无意义的注释：模板生成的空 \`@param\` 占位、为了过 lint 凑数的注释——形式主义，无信息量。\n\n判断准则：好注释提供「代码无法表达的信息」（意图、约束、警示）；坏注释重复代码已表达的、或掩盖代码没表达好的。`,
    tags: ["好注释", "坏注释", "注释类型", "意图"],
  },
  {
    id: "cqr-comments-format-02",
    chapter: "cqr-comments-format",
    level: 2,
    question: `为什么「好代码自解释」比「用注释解释烂代码」更好？`,
    answer:
      `「好代码自解释」优于「注释解释烂代码」，原因在于信息源的可靠性与维护成本：\n\n1. 代码是事实，注释是「关于事实的声明」：\n代码会被编译器/运行时执行，错了就出 bug，所以它会一直被修正以保持「正确」。而注释不会被编译器校验，错了也没人发现。当代码和注释冲突时，读者不知该信谁——而现实中往往注释是错的那一方（因为代码改了注释没更新）。自解释的代码消除了「两个信息源」的对立风险。\n\n2. 注释会随时间腐烂：\n每次代码改动都要同步更新注释，但人很容易忘记。时间一长，注释就从「准确说明」变成「误导性历史遗留」。自解释的代码不需要这种同步维护——代码就是它自己的说明书。\n\n3. 注释「解释烂代码」是在治标：\n如果代码需要注释才能看懂，根因是「代码本身不清晰」——命名差、函数过长、结构混乱。加注释只是贴了张「说明书」，烂代码还在那儿，后续每次改动都要先读懂烂代码、再读懂注释、还要判断注释是否还准。而把代码改清楚（重命名、提取函数、简化逻辑）是治本——后续维护成本永久降低。\n\n4. 自解释代码降低「上下文切换」成本：\n读注释好的烂代码，要在「代码」和「注释」之间来回切换对照；读自解释代码，顺着代码读下去即可，认知流不被打断。\n\n5. 注释无法被工具利用：\n编译器、linter、IDE 重构都基于代码本身，看不懂注释。代码自解释意味着「工具能帮你」，而注释解释的烂代码工具也救不了。\n\n澄清——这不意味着「不要写注释」：\n注释无法取代的领域仍然必要：\n- 为什么这么做的「意图」和「约束」（代码很难表达「为什么」）。\n- 外部依赖的怪癖、性能权衡、业务背景。\n- 公开 API 的契约文档。\n\n原则：先用「改代码」让代码尽量自解释（命名、结构、抽象），实在表达不了的「为什么」再用注释补充。注释是「代码表达不了的补充」，而不是「代码表达不好的辩护」。`,
    tags: ["自解释", "注释腐烂", "信息源", "治本治标"],
  },
  {
    id: "cqr-comments-format-03",
    chapter: "cqr-comments-format",
    level: 3,
    question: `什么时候注释是必要的？请给出具体场景和写法。`,
    answer:
      `当代码「无法」表达某些信息时，注释是必要的。具体场景与写法：\n\n1. 解释「为什么」而非「是什么」：\n代码能表达「做了什么」，但表达不了「为什么这么做」。当某个决定有非显然的理由时，必须注释。\n- 场景：用了一个看似低效的算法、绕了一个弯路、避开了某个常规写法。\n- 写法：\`// 这里用线性查找而非二分，因为列表长度通常 < 10，且 cache 友好，实测更快\`。说清楚「决策依据」和「权衡的考量」。\n\n2. 警示后续维护者的陷阱：\n当代码有「不能动」「动了会出问题」的部分时。\n- 场景：某个调用顺序不能换、某段代码依赖外部库的 bug、删除某行会导致死锁。\n- 写法：\`// 警告：必须先 close 输入流再 close 输出流，否则第三方库 v2.3 会泄漏句柄（见 issue #456）\`。要具体到「后果」和「依据」。\n\n3. 说明外部约束/业务背景：\n当代码逻辑依赖某个业务规则或外部系统行为，光看代码看不出来时。\n- 场景：税务计算的特殊公式、与监管要求对应的校验、与某协议规范的对应关系。\n- 写法：\`// 依据 GB/T xxxx-2020，销售额超过 10 万需加征 3% 附加税\`。引用「来源」让维护者可追溯。\n\n4. 公开 API 的契约文档：\n对外暴露的接口，调用方看不到实现，必须用文档注释说明「怎么用」。\n- 场景：库的公开方法、SDK 接口、微服务 API。\n- 写法：用语言支持的文档格式（JSDoc/JavaDoc/docstring），说明参数含义、取值范围、返回值、可能抛出的异常、前置后置条件、副作用、线程安全性。这是「给机器和人都能读」的注释。\n\n5. 标记待办与已知缺陷：\n当暂时无法完美实现，要标记「这里欠着」。\n- 场景：临时方案、待优化点、已知但暂不修的 bug。\n- 写法：\`// TODO(张三 2024-Q1): 这里硬编码了超时时间，应改为可配置\`。带责任人、时间、具体内容，而不是空泛的 \`// TODO\`。\n\n6. 法律/合规要求：\n- 场景：开源协议头、版权声明、合规审计要求的标记。\n- 写法：按法务要求的标准格式，不动。\n\n7. 复杂正则/位运算的「人话翻译」：\n- 场景：一个复杂的正则或位掩码操作，代码本身就是「最简表达」，但人很难读懂。\n- 写法：\`// 正则匹配 IPv4：四组 0-255，用点分隔\` + 逐段注释。这类是「代码已是最简形式，但人脑难解析」的合理注释。\n\n好注释的共同特征：\n- 提供「代码无法表达」的信息。\n- 简洁、具体、可追溯（带 issue 号/规范来源/责任人）。\n- 维护成本低——描述的是「不易变的决策/约束」，而非「易变的实现细节」。\n\n反例：注释描述「当前实现细节」（如「// 这里用了 HashMap」）会随重构失效，应避免；只描述「不随实现变化的意图/约束」才经得起时间。`,
    tags: ["必要注释", "意图", "警示", "API 文档", "TODO"],
  },
  {
    id: "cqr-comments-format-04",
    chapter: "cqr-comments-format",
    level: 4,
    question: `代码格式为什么重要？团队如何统一格式？`,
    answer:
      `代码格式的重要性与统一方法：\n\n为什么格式重要：\n\n1. 格式是「沟通」的一部分，代码主要被「人」读：\n代码被阅读的次数远多于被编写的次数。一致的格式让读者不必「每次都适应不同风格」，把注意力集中在逻辑而非排版上。混乱的格式像一篇错别字连篇的文章——即使内容对，也让人怀疑作者的严谨性。\n\n2. 一致性降低「认知切换」成本：\n当全库格式统一，读者形成「模式预期」——括号在哪、缩进几格、空行怎么用。一旦符合预期，大脑就能跳过格式层直接读逻辑。格式不一致则迫使大脑不断「重新校准」，消耗注意力。\n\n3. 格式反映「结构」与「层级」：\n好的格式用空行、缩进、对齐来表达代码的逻辑分组和抽象层级——相关代码紧凑、不同概念间留空行、嵌套清晰可数。格式不只是美观，是「视觉化的结构」。\n\n4. 减少无意义的「格式争论」：\n团队把时间花在「大括号该不该换行」上是浪费。统一格式后，这类争论消失，精力转向真正的问题。\n\n5. 影响 diff 质量和代码审查：\n格式不统一时，一次「改逻辑」的提交会混入大量「格式调整」，审查者难以分辨「真正改了什么」。统一格式让 diff 只反映逻辑变化，审查更高效、回溯更清晰。\n\n团队如何统一格式：\n\n1. 用工具自动化，而非靠人记规则：\n- 格式化器（Formatter）：如 Prettier（JS/TS）、clang-format（C/C++）、gofmt（Go）、rustfmt（Rust）。配置一份规则文件，全队共用。这类工具「一键格式化」，消除人为差异。\n- Linter：如 ESLint、SonarQube。检查格式之外还能抓潜在 bug、坏味道。linter 比 formatter 更「严格」，能拒绝不符合规范的代码。\n- 编辑器集成：在 IDE 配置「保存时自动格式化」，让格式化无感融入开发流程。\n\n2. 把规则「固化」到工程里：\n- 格式化配置文件（如 \`.prettierrc\`、\`.clang-format\`）提交到仓库，全队共用同一份。\n- 在 CI 里加格式检查步骤：\`pnpm format:check\` 或 \`clang-format --dry-run\`，格式不通过的 PR 直接拒绝合并。这是「强制力」的来源——靠人自觉不如靠流水线。\n- 用 Git pre-commit hook 在提交前自动格式化，把问题挡在本地。\n\n3. 规则要少而精，避免过度定制：\n- 优先用工具的「默认配置」——默认值往往是社区最佳实践，且新人易上手。\n- 只在「团队有明确理由」时定制（如行宽限制、特定命名）。每条定制规则都要能说出「为什么」，不要为了定制而定制。\n- 规则越少，记忆负担越低，遵循率越高。\n\n4. 区分「格式」（机械可统一）与「风格」（需人工判断）：\n- 格式（缩进、括号位置、空格）：100% 交给工具，无需人工。\n- 风格（函数多长该拆、变量命名好不好、抽象层级是否一致）：工具管不了，靠代码审查把关。把工具能做的都给工具，让人专注工具做不了的。\n\n5. 处理「历史代码」的迁移：\n- 一次性大规模格式化会产生巨大 diff，干扰 blame/history。建议「逐文件迁移」——只在改动某文件时顺手格式化该文件，或在专门的「格式化提交」里集中处理（与逻辑改动分开提交，commit message 注明）。\n- 用 \`git blame --ignore-rev <格式化commit>\` 忽略纯格式化提交，保留有意义的 blame。\n\n总结：格式重要是因为它服务于「人读代码」这件高频事。统一格式的最佳路径是「工具自动化 + CI 强制 + 默认配置优先」，把格式从「人的负担」变成「机器的责任」。`,
    tags: ["代码格式", "一致性", "格式化工具", "Linter", "CI"],
  },
];
