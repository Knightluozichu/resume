import type { ReviewQuestion } from "./types";

/** 错误处理复习题 */
export const cqrErrorHandlingQuestions: ReviewQuestion[] = [
  {
    id: "cqr-error-handling-01",
    chapter: "cqr-error-handling",
    level: 1,
    question: `异常处理（Exception）与返回码（Return Code）有什么区别？`,
    answer:
      `异常处理与返回码是两种不同的错误传递机制，核心区别在于「错误信息走什么通道」：\n\n1. 返回码（Return Code）：\n函数用返回值本身表达「成功/失败」。常见形式：返回整数状态码（如 C 的 \`0\` 表成功、非 \`0\` 表错误码）、返回 \`null\`/\`-1\` 表失败、返回布尔值表成败。\n- 特点：错误信息和正常返回值共用「同一通道」，调用者必须主动检查返回值才能知道是否出错。\n- 优点：控制流简单直白，没有隐式跳转；适合性能敏感、无异常机制的语言（如 C）。\n- 缺点：调用者容易「忘记检查」返回码，错误被静默吞掉；正常返回值和错误码混在一起，类型设计受限（比如返回 \`-1\` 表失败就无法用 \`-1\` 作为正常值）。\n\n2. 异常（Exception）：\n函数出错时「抛出」异常，异常沿调用栈向上传播，直到被某层的 \`try-catch\` 捕获。错误信息走「独立于返回值的通道」。\n- 特点：正常返回值只承载「成功结果」，错误通过抛出传播；调用者要么捕获处理、要么让向上传播。\n- 优点：正常代码路径干净（不夹杂 \`if (err)\` 检查）；错误不会因「忘记检查」而被吞掉——未捕获的异常会让程序显式失败；可携带丰富的错误类型和堆栈信息。\n- 缺点：控制流隐式（一个 \`throw\` 可能跨越多层跳转，难以静态追踪）；有性能开销；若被滥用（用异常做正常控制流）会让代码难懂。\n\n关键区别总结：\n- 错误传播方式：返回码是「显式、需主动检查」；异常是「隐式、自动传播」。\n- 遗漏错误的后果：返回码易被忽略（静默错误）；异常未被捕获会显式崩溃（显式失败，更容易发现）。\n- 正常路径的整洁度：返回码污染正常逻辑（到处 \`if (err)\`）；异常让正常路径干净。\n- 适用场景：返回码适合「可预期的、频繁的、非异常的情况」（如文件读到末尾）；异常适合「真正异常的、不该发生的情况」（如配置错误、网络中断）。\n\n现代趋势：很多语言（Rust、Swift、Haskell）用 \`Result\`/\`Optional\` 类型结合模式匹配，既保留「显式」又不污染返回值，融合两者优点。`,
    tags: ["异常处理", "返回码", "错误传播", "对比"],
  },
  {
    id: "cqr-error-handling-02",
    chapter: "cqr-error-handling",
    level: 2,
    question: `try-catch 的最佳实践是什么？为什么说「先写 try-catch 再写逻辑」？`,
    answer:
      `try-catch 的最佳实践与「先写 try-catch 再写逻辑」的理由：\n\ntry-catch 最佳实践：\n\n1. try 块要尽量小：\n只把「可能抛异常」的那几行放进 \`try\`，不要把整个函数体都包进去。try 块越小，越容易定位是哪一行出错、catch 的处理越精准。大 try 块会捕获到「你没想到的异常类型」，掩盖真实问题。\n\n2. 捕获具体的异常类型，不要 catch-all：\n\`catch (Exception e)\` 或 \`catch (Throwable)\` 这种宽泛捕获会吞掉所有错误（包括你不该处理的），让 bug 难以暴露。应捕获「你能处理」的具体类型（如 \`FileNotFoundException\`），其余让它向上传播。\n\n3. 不要吞掉异常：\n\`catch (e) { /* 空 */ }\` 是大忌——错误被静默丢弃，问题永远潜伏。即使暂时不处理，也要记录日志或重新抛出。\n\n4. 不要用异常做控制流：\n异常是「异常情况」的通道，不是「正常分支」的工具。用 \`try { ... } catch { 走另一条业务路径 }\` 替代 \`if\` 是反模式——可读性差且性能低。\n\n5. 能恢复就恢复，不能恢复就向上传播：\n如果你能合理处理这个异常（如重试、用默认值、降级），就在 catch 里处理；如果处理不了，重新抛出或包装后抛出，让上层决策。不要在底层「假装处理」了你其实处理不了的错误。\n\n6. 资源用 try-with-resources / finally 释放：\n打开的文件、连接、锁，必须在 \`finally\` 或 try-with-resources 里释放，防止异常导致资源泄漏。\n\n为什么「先写 try-catch 再写逻辑」：\n\n这是 TDD 思想在错误处理上的应用。具体做法：在写函数主体逻辑前，先写出 \`try { ... } catch (SpecificException e) { ... }\` 的骨架，明确「这个操作可能抛什么异常、我打算怎么处理」。\n\n这样做的好处：\n1. 强制先想清楚「边界与失败」：先写 try-catch 让你在写「正常路径」之前就思考「出错怎么办」，避免写完正常逻辑再硬塞错误处理（往往塞得乱、漏掉边界）。\n2. 让 try 块的范围天然合理：先确定「哪段逻辑需要被 try 包住」，写出来的 try 块就是「最小必要范围」，而不是事后把一大坨代码都包进去。\n3. 定义了异常处理的契约：catch 块先写好，相当于声明了「这个函数对某某异常的处理策略」，正常逻辑就在这个「安全框架」内填充，目标明确。\n4. 便于测试驱动：先写 try-catch 意味着你已经想好了「异常路径」的测试用例，正常和异常路径都有覆盖。\n\n实践示例：写一个读配置的函数，先写：\n\`try { 内容 = readFile(path); 解析 } catch (IOException) { 用默认配置 } catch (ParseException) { 报错 }\`，再填充 try 内的具体读取解析逻辑。这样错误处理是「设计的一部分」而非「事后补丁」。`,
    tags: ["try-catch", "最佳实践", "异常处理", "TDD"],
  },
  {
    id: "cqr-error-handling-03",
    chapter: "cqr-error-handling",
    level: 3,
    question: `如何避免「异常地狱」（多层嵌套 try-catch）？`,
    answer:
      `「异常地狱」指 try-catch 层层嵌套，如 \`try { try { try { ... } catch } catch } catch\`，可读性极差、错误处理分散。避免与消除的方法：\n\n1. 让异常「向上传播」而非层层捕获：\n不是每一层都要 catch。如果一个层「处理不了」这个异常，就不要 catch，让它自动向上抛给能处理的层。只有「能恢复」或「需要加上下文」的层才 catch。这样多层嵌套往往能塌缩成「一处抛、一处捕」。\n\n2. 用「提取函数」把 try 块拆小：\n嵌套的 try 往往是一个函数里塞了多段「各自可能出错」的逻辑。把每段提取成独立函数，每个函数自己 try-catch 自己的错误。外层调用变成线性的函数调用序列，嵌套消失。\n- 坏：\`try { a(); try { b(); try { c(); } catch ... } catch ... } catch ...\`\n- 好：\`a(); b(); c();\`，其中 a/b/c 各自处理自己的异常。\n\n3. 统一的错误处理边界（集中捕获）：\n在架构的某一层（如控制器、中间件、顶层调度）设置统一的异常处理边界，所有未处理的异常都汇聚到这里集中处理（记日志、转 HTTP 状态码、发告警）。底层只抛不 catch，避免每个函数都写 catch。\n- 例：Web 框架的全局异常处理器、React 的错误边界（Error Boundary）。\n\n4. 用「异常包装」传递上下文而非层层 catch-rethrow：\n有时需要 catch 后重新抛出（为了加上下文）。但 \`catch { throw e }\`（重新抛同一个）是浪费——丢失了原始堆栈。应 \`catch (e) { throw new WrapperException(\"做X时失败\", e) }\`，包装一层带新上下文的异常再抛出。注意保留 \`cause\` 链，避免堆栈丢失。这样信息越抛越丰富，而不是每层都 catch 了又原样抛。\n\n5. 用 Result/Optional 类型替代异常（函数式风格）：\n把「可能失败的操作」的返回类型改成 \`Result<T, Error>\`，用 \`map\`/\`flatMap\` 链式处理，错误通过类型系统传递，不存在「嵌套 catch」。详见 L4 题。\n\n6. 区分「可恢复」与「不可恢复」错误：\n- 可恢复错误（如用户输入无效、网络超时）：用返回值/Result 表达，调用方决定怎么办。这类不该用异常，否则到处 catch。\n- 不可恢复错误（如内存耗尽、配置错误）：直接抛异常或 panic，让程序快速失败，不在每层处理。\n混用两种机制能让「可恢复」走显式返回、「不可恢复」走异常，异常数量大幅减少，嵌套自然消失。\n\n7. 用「早返回 / 守卫语句」减少嵌套层级：\n即使是普通控制流，用「先校验、不满足就提前 return/throw」代替层层 \`if-else\` 嵌套，整体嵌套深度下降，try-catch 也跟着变浅。\n\n示例重构——把：\n\`try { open; try { read; try { parse; } catch(Parse) {} } catch(IO) {} } catch {} \`\n重构为：\n\`config = loadConfig(path)\`，其中 \`loadConfig\` 内部线性调用 \`open()\` → \`read()\` → \`parse()\`，每步失败各自抛出带上下文的异常，由顶层统一捕获。嵌套消失，逻辑线性化。`,
    tags: ["异常地狱", "嵌套", "异常传播", "提取函数", "Result"],
  },
  {
    id: "cqr-error-handling-04",
    chapter: "cqr-error-handling",
    level: 4,
    question: `Optional/Result 类型如何比异常更安全？它的代价是什么？`,
    answer:
      `Optional/Result 类型是函数式语言（Haskell 的 \`Maybe\`/\`Either\`、Rust 的 \`Option\`/\`Result\`、Swift 的 \`Optional\`/\`Result\`）的核心错误处理机制，相比异常有独特的安全性与代价。\n\n它是什么：\n- \`Optional<T>\`（又叫 \`Maybe\`/\`Option\`）：表示「可能有值，也可能没有」。有两种状态 \`Some(T)\` 和 \`None\`。替代 \`null\`，把「缺失」变成类型的一部分。\n- \`Result<T, E>\`（又叫 \`Either\`）：表示「可能成功带 T，也可能失败带错误 E」。两种状态 \`Ok(T)\` 和 \`Err(E)\`。把「错误」变成返回类型的一部分。\n\n为什么更安全：\n\n1. 错误成为「类型」的一部分，编译器强制处理：\n函数返回 \`Result<T, E>\` 时，调用者拿到的是一个 \`Result\` 类型，不是直接的 \`T\`。要取出 \`T\` 必须显式处理 \`Err\` 分支（用模式匹配或 \`?\` 操作符）。编译器会检查「你是否处理了错误情况」——没处理就编译不过。这把「忘记处理错误」从运行时 bug 变成了编译时错误，从源头消除「静默吞错」。\n- 对比异常：异常可以被「忘记 catch」，运行时才崩；Result 忘记处理则编译不过。\n\n2. 错误路径显式化在函数签名上：\n看函数签名 \`fn load() -> Result<Config, LoadError>\` 就知道「这个函数会失败、失败返回什么类型」，错误不再是隐藏的副作用。异常则藏在函数体里，签名看不出来，调用者得查文档或读实现才知道可能抛什么。\n\n3. 没有「隐式控制流跳转」：\n异常的 \`throw\` 会跨越多层栈帧跳转，控制流是隐式的——读代码不知道哪一行会「飞走」。Result 是普通的返回值，控制流线性、显式，每一行的执行顺序清晰可读。\n\n4. 错误可组合、可链式处理：\n\`Result\` 支持 \`map\`/\`and_then\`/\`?\` 等组合子，多个可能失败的操作可以链式串联，错误自动短路传播，代码比嵌套 try-catch 简洁：\n\`load_config()?.parse()?.validate()?\`——任一步失败立即返回 Err，无需层层 catch。\n\n5. 区分「可恢复」与「不存在」更精确：\n\`null\` 既可能表示「缺失」也可能表示「未初始化」也可能表示「出错」，语义混乱。\`Optional\` 专门表达「有或无」，\`Result\` 专门表达「成功或带原因的失败」，各司其职，语义清晰。\n\n代价：\n\n1. 语法噪音：\n每个可能失败的操作都要 \`match\` 或 \`?\` 处理，相比异常的「不 catch 就自动传播」，调用点代码更啰嗦。虽然有 \`?\` 操作符简化，但仍比异常「写起来轻」。\n\n2. 性能开销（取决于实现）：\n\`Result\` 是普通值，需要构造和传递（通常是个枚举/结构体），在热路径上比异常的「零成本快乐路径」（成功时不分配）可能略慢——不过现代编译器优化后差距很小，且异常在抛出时开销更大。\n\n3. 与「不可恢复错误」仍需异常/panic：\n\`Result\` 适合「可恢复错误」。对于「程序状态已损坏、无法继续」的不可恢复错误（如不变量被破坏），仍需 \`panic\`/异常立即终止。两套机制并存增加认知负担。\n\n4. 与现有生态的摩擦：\n在以异常为主的语言（Java/C#）里引入 Result 风格，会和标准库、第三方库风格不一致，需要桥接层，团队也要学习成本。\n\n5. 容易「过度使用」：\n把所有错误都塞进 Result 会把简单的「不可能失败」操作也包装成 Result，徒增复杂度。应只对「真正可能失败且可恢复」的操作用 Result。\n\n综合判断：\n- Result/Optional 的核心价值是「把错误从运行时问题提升为编译时类型问题」，用类型系统强制开发者处理错误，这是它比异常「更安全」的本质。\n- 它最适合「错误是常规情况、必须显式处理」的场景（IO、解析、查找）。对于「错误是真正的异常、罕见且不可恢复」的场景，异常/panic 仍更合适。\n- 现代趋势是「可恢复用 Result、不可恢复用异常/panic」的混合策略，各取所长。Rust 是这套理念的典范实践。`,
    tags: ["Optional", "Result", "类型安全", "异常对比", "代价"],
  },
];
