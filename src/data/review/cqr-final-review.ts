import type { ReviewQuestion } from "./types";

/** 代码质量总复习复习题 */
export const cqrFinalReviewQuestions: ReviewQuestion[] = [
  {
    id: "cqr-final-review-01",
    chapter: "cqr-final-review",
    level: 1,
    question: `整洁代码和重构的核心思想各是什么？`,
    answer:
      `两者的核心思想分别回答「好代码长什么样」和「怎么把代码变好」：\n\n整洁代码的核心思想——「代码是写给人读的，顺便能让机器执行」：\n1. 可读性优先：代码被读的次数远多于被写的次数。命名、结构、格式都服务于「让人一眼看懂意图」。\n2. 单一职责：每个函数、类、模块只做一件事，没有隐藏副作用。读名字就知道它干什么。\n3. 简洁与诚实：没有重复、没有死代码、没有魔法数字；名字与行为一致，不误导。\n4. 质量内建：可读、可测、可改是「写代码时就该做到」的，而非事后修补。测试是代码的一部分，不是附属品。\n5. 持续整洁：通过童子军规则——每次改动让代码比之前更干净一点，避免腐烂累积。\n\n重构的核心思想——「在不改变外部行为的前提下，改善代码内部结构」：\n1. 行为不变是铁律：重构是「等价变换」，外部可观察行为必须保持。靠测试来保证「行为没变」。\n2. 小步前进：每一步都很小（提取一个函数、重命名一个变量），每步跑测试。风险可控、可随时停止、可回退。\n3. 异味驱动：先识别代码异味（症状），再诊断问题，再选对应手法。重构不是「推倒重来」，是「精准手术」。\n4. 测试先行：没有测试的重构是赌博。先补测试锁住行为，再动结构。\n5. 持续进行：重构不是「专门阶段」，而是融入每次日常改动的习惯。代码会随需求演化而腐烂，重构是「持续对抗腐烂」的日常实践。\n\n两者的关系：\n- 整洁代码定义「目标状态」（好代码长什么样），重构提供「到达路径」（怎么从烂代码走到好代码）。\n- 它们循环往复：写整洁代码 → 需求演化使代码变烂 → 测试保护下重构 → 恢复整洁。质量不是一次性的，是持续的。\n- 共同信念：代码质量是「可以也必须被主动管理」的，而非「听天由命」。开发者对代码质量负责，质量内建于每一次提交。\n\n一句话总结：整洁代码教「规矩」，重构教「补救」；前者预防新债，后者偿还旧债，合起来构成代码质量的完整闭环。`,
    tags: ["核心思想", "整洁代码", "重构", "总复习"],
  },
  {
    id: "cqr-final-review-02",
    chapter: "cqr-final-review",
    level: 2,
    question: `代码异味、重构手法、测试三者之间是什么关系？`,
    answer:
      `代码异味、重构手法、测试构成「重构实践」的三角，三者环环相扣：\n\n1. 代码异味——「发现问题的雷达」：\n- 异味是「代码可能有问题」的表面信号（重复代码、过长函数、God Class 等）。\n- 它告诉你「哪里值得看看」，是重构的「入口」。没有异味识别，重构就变成「凭感觉乱改」，无处下手或方向错误。\n- 类比：异味像体检的「异常指标」，提示你「这里可能有问题，该深入查」。\n\n2. 重构手法——「解决问题的工具」：\n- 手法是「具体的、有名字的、小步的代码变换」（提取函数、内联函数、搬移字段、重命名等）。\n- 每种异味都对应一组「对症」的手法。如「重复代码」对应「提取函数」「提取超类」；「过长函数」对应「提取函数」「分解条件表达式」。\n- 手法的「小步、可验证」特性，让重构从「高风险大改」变成「可控的渐进改善」。\n- 类比：手法像「治疗方案」，针对诊断出的病给出具体操作。\n\n3. 测试——「保障安全的网」：\n- 测试在重构中扮演「行为守卫」——它锁住代码「应该做什么」，重构过程中一旦行为变了，测试立刻失败报警。\n- 没有测试的重构是赌博——你不知道有没有改坏。测试是「重构可行」的前提条件。\n- 测试也是「重构的受益者」——好重构让代码更可测，更多测试又让后续重构更安全，形成正循环。\n- 类比：测试像「手术中的生命体征监护」，确保手术不伤到要害。\n\n三者的协作流程（重构的标准操作）：\n\n异味 → 测试 → 手法 → 异味（循环）：\n1. 识别异味：在代码审查或日常开发中发现某处有异味（如「这个函数 80 行，做了三件事」）。\n2. 评估与准备：判断这个异味是否值得改（成本收益）、是否有测试覆盖。如果没测试，先补测试（特征测试）锁住当前行为。\n3. 应用手法：选对症的重构手法，小步执行——每一步都让代码结构更好一点，每步跑测试确认行为不变。如「提取函数」把三件事拆成三个函数。\n4. 验证与迭代：重构后异味消失或减弱，跑全量测试确认无回归。可能暴露出更深的异味，进入下一轮循环。\n\n两两关系深入：\n\n异味 ↔ 手法（症状 ↔ 治疗）：\n- 异味是「症状」，手法是「治疗」。Fowler 的《重构》把两者配对整理成「异味目录 + 手法目录」，让重构从「艺术」变成「可教学的工程」。\n- 一种异味可能对应多种手法，选哪种取决于上下文；一种手法也可能消除多种异味。\n- 不是所有异味都要立刻治——要权衡成本收益（见异味章）。\n\n异味 ↔ 测试（信号 ↔ 信心）：\n- 异味往往伴随「测试缺失或难写」。难测本身就是一种异味信号（耦合重、职责多）。\n- 异味严重的代码，补测试本身可能需要先做「轻量重构」让它可测——这是「为可测性重构」。\n- 测试覆盖率高的地方，异味更容易被发现和治理——测试让「改」变得安全。\n\n手法 ↔ 测试（操作 ↔ 守卫）：\n- 每个重构手法都假设「有测试在守护」。手法的「小步」设计就是为了让「每步都能被测试验证」。\n- 手法执行中如果测试失败，说明这步改错了，立即回退——这正是测试的价值。\n- 手法改善代码后，测试本身也更容易维护（依赖少、结构清晰），形成良性循环。\n\n三者失衡的后果：\n- 有异味识别但无手法：知道有问题但不会改，停留在「抱怨」。\n- 有手法但无测试：敢改但危险，改了不知道有没有改坏，回归风险高。\n- 有测试但无异味识别：测试齐全但不知道「哪里值得重构」，盲目改可能过度或无的放矢。\n- 三者齐备：能「发现问题、安全地、用对方法解决问题」——这是成熟重构实践的标志。\n\n一句话：异味告诉你「哪里有病」，手法告诉你「怎么治」，测试保证「治的过程不伤身」。三者缺一不可，共同支撑「安全、有效、持续」的重构。`,
    tags: ["异味", "重构手法", "测试", "三角关系", "总复习"],
  },
  {
    id: "cqr-final-review-03",
    chapter: "cqr-final-review",
    level: 3,
    question: `给一个烂代码示例，分析其中有哪些代码异味，并说明如何重构。`,
    answer:
      `下面是一段典型的烂代码（订单处理），先展示，再分析异味，最后给出重构方案。\n\n烂代码示例（伪代码）：\n\`\`\`\nclass OrderManager {\n  List<Order> orders = new ArrayList();\n  // 处理订单：校验、计算、保存、发邮件、记日志\n  public int processOrder(String n, String e, String a, int qty, double price, boolean vip) {\n    if (n != null && !n.isEmpty()) {\n      if (e != null && e.contains(\"@\")) {\n        if (qty > 0 && price > 0) {\n          double total = qty * price;\n          if (vip) { total = total * 0.9; }\n          if (total > 1000) { total = total - 50; }\n          Order o = new Order(n, e, a, qty, price, total);\n          // 保存到数据库\n          Connection c = DriverManager.getConnection(\"jdbc:...\", \"user\", \"pwd\");\n          String sql = \"INSERT INTO orders VALUES('\" + n + \"',\" + qty + \",\" + total + \")\";\n          c.createStatement().execute(sql);\n          orders.add(o);\n          // 发邮件\n          String body = \"亲爱的\" + n + \"，你的订单\" + qty + \"件，总价\" + total;\n          SmtpClient.send(e, \"订单确认\", body);\n          // 记日志\n          System.out.println(\"订单已处理: \" + n + \" \" + total);\n          logger.info(\"order processed: \" + n);\n          return 0;\n        } else { return -3; }\n      } else { return -2; }\n    } else { return -1; }\n  }\n}\n\`\`\`\n\n一、异味分析：\n\n1. 过长函数（Long Method）：\n\`processOrder\` 一个方法做了校验、计算、保存、发邮件、记日志五件事，几十行。违反「单一职责」。\n\n2. 过长参数列表（Long Parameter List）：\n6 个参数（n, e, a, qty, price, vip），含布尔标志。调用者易传错位，参数含义不明（\`n\` 是什么？\`e\` 是什么？）。\n\n3. 神秘命名（Mysterious Name）：\n\`n\`/\`e\`/\`a\`/\`o\` 等单字母或缩写，不读实现不知道是 name/email/address。\`OrderManager\` 名字空泛。\n\n4. 深层嵌套（Deeply Nested）：\n校验用 4 层 \`if\` 嵌套，箭头型代码，难以阅读和维护。\n\n5. 神秘常量/魔法数字（Magic Numbers）：\n\`0.9\`（VIP 折扣）、\`1000\`（满减门槛）、\`50\`（满减额）、\`-1\`/\`-2\`/\`-3\`（错误码）都是魔法数字，没有语义。\n\n6. 重复代码（Duplicated Code）：\n日志记了两遍（\`System.out.println\` + \`logger.info\`），且邮件正文与订单信息重复拼接。\n\n7. God Class（发散修改）：\n\`OrderManager\` 同时负责业务规则、数据库、邮件、日志——多个变化轴。改折扣规则、换数据库、调邮件模板都要动它。\n\n8. 数据类与行为分离的隐患：\n\`Order\` 只是数据容器，所有逻辑都在 \`OrderManager\` 里（缺乏封装）。\n\n9. 错误处理用返回码（且含糊）：\n用 \`-1\`/\`-2\`/\`-3\` 表错误，调用者要查文档才知道含义，且易被忽略。\n\n10. 安全问题（SQL 注入）：\n字符串拼接 SQL，是注入漏洞。虽不属「异味」严格范畴，但反映了「缺乏抽象」的结构问题。\n\n二、重构方案（按手法逐步）：\n\n第 0 步：补测试。先写特征测试，覆盖典型输入（普通订单、VIP、满减、各种校验失败），钉住当前行为。\n\n第 1 步：重命名（消除神秘命名）：\n\`n\`→\`customerName\`，\`e\`→\`email\`，\`a\`→\`address\`，\`o\`→\`order\`。用 IDE 全量替换。\n\n第 2 步：参数对象化（消除过长参数列表）：\n把 6 个参数封装成 \`OrderRequest { customerName, email, address, quantity, unitPrice, isVip }\`。签名变成 \`processOrder(OrderRequest req)\`。\n\n第 3 步：用「卫语句」替代嵌套（消除深层嵌套）：\n把 \`if (valid) { ... } else { return -1; }\` 改成「先校验、不满足就早返回」：\n\`if (customerName 为空) return 失败;\nif (email 不合法) return 失败;\n...\`。嵌套塌缩成线性。\n\n第 4 步：提取函数（消除过长函数）：\n把五件事分别提取成独立方法/类：\n- \`validate(req)\` → 校验\n- \`calculateTotal(req)\` → 计算（含折扣、满减）\n- \`save(order)\` → 持久化\n- \`sendConfirmation(order)\` → 发邮件\n- \`logOrder(order)\` → 记日志\n\`processOrder\` 变成线性的步骤调用：\`validate → calculate → save → send → log\`。\n\n第 5 步：引入常量与命名（消除魔法数字）：\n\`VIP_DISCOUNT_RATE = 0.9\`、\`FREE_SHIPPING_THRESHOLD = 1000\`、\`FREE_SHIPPING_DISCOUNT = 50\`。错误码改成枚举 \`OrderError { EMPTY_NAME, INVALID_EMAIL, ... }\`。\n\n第 6 步：拆分 God Class（消除发散修改）：\n按变化轴拆成：\n- \`OrderService\`：编排业务流程（调用各协作对象）。\n- \`OrderPricing\`：纯计算，含折扣与满减规则（可单测）。\n- \`OrderRepository\`：负责持久化，用参数化查询消除 SQL 注入。\n- \`EmailNotifier\`：发邮件。\n- \`OrderLogger\`：记日志。\n每个类单一职责，各自可独立演化与测试。\n\n第 7 步：行为归位到数据类（封装）：\n把「计算总价」放到 \`Order\` 或 \`OrderRequest\` 上（\`order.total()\`），而非散在 Service 里。让对象「自己知道自己的事」。\n\n第 8 步：消除重复（DRY）：\n统一日志（只用 \`logger.info\`，删 \`System.out\`），邮件正文用模板提取。\n\n第 9 步：用异常/Result 替代返回码：\n校验失败抛 \`InvalidOrderException\` 或返回 \`Result<Order, OrderError>\`，调用者无法忽略错误。\n\n重构后的结构示意：\n\`\`\`\nclass OrderService {\n  constructor(pricing, repo, notifier, logger) {...}  // 依赖注入\n  Result<Order> processOrder(OrderRequest req) {\n    error = validate(req);  if (error) return Err(error);\n    order = Order.from(req);  // 含计算总价\n    repo.save(order);\n    notifier.sendConfirmation(order);\n    logger.log(order);\n    return Ok(order);\n  }\n}\nclass OrderPricing { double total(req) {...} }  // 纯计算，可测\nclass OrderRepository { void save(order) { /* 参数化查询 */ } }\nclass EmailNotifier { void sendConfirmation(order) {...} }\nclass Order { double total() {...} }  // 行为归位\n\`\`\`\n\n三、重构的要点回顾：\n1. 每一步都很小，每步跑测试，行为始终不变。\n2. 异味与手法一一对应：异味是「诊断」，手法是「治疗」。\n3. 重构后每个类职责单一、可测、可替换；\`processOrder\` 从一坨变成「清晰的步骤序列」。\n4. 重构不仅改善结构，还顺带修复了 SQL 注入、错误处理含糊等问题——结构改善往往带来正确性与安全性的连带提升。\n\n这个例子展示了「异味识别 → 测试保护 → 手法应用」的完整重构闭环，是整洁代码与重构思想的一次综合演练。`,
    tags: ["烂代码", "异味分析", "重构示例", "综合应用"],
  },
  {
    id: "cqr-final-review-04",
    chapter: "cqr-final-review",
    level: 4,
    question: `「让它工作，让它正确，让它快速」（Make it work, make it right, make it fast）——三个阶段如何平衡？`,
    answer:
      `这句 Kent Beck 的名言揭示了软件开发的「阶段性优先级」：先求「能用」，再求「做好」，最后求「快」。三个阶段的平衡是工程智慧的核心。\n\n三个阶段的含义：\n\n1. 让它工作（Make it work）——先解决「有没有」：\n- 目标：让功能跑起来，满足核心需求，哪怕代码粗糙。\n- 优先级：正确性 > 一切。先把「最关键的业务路径」打通，验证「这个思路可行」。\n- 允许的妥协：代码可以重复、命名可以临时、抽象可以缺失、测试可以不全、性能可以不优。先「能跑」。\n- 价值：快速获得反馈——验证需求理解对不对、方向走不走得通。在需求不确定时，过早优化结构是浪费（可能要推翻重来）。\n- 风险提示：「工作」不等于「胡来」。基本正确性（核心逻辑对）、关键路径的测试仍要保，否则后续无从验证「重构后还对不对」。\n\n2. 让它正确（Make it right）——再解决「好不好」：\n- 目标：把「能跑」的代码重构成「整洁、可维护、可测」的代码。\n- 优先级：质量。应用整洁代码原则（命名、函数、类、注释）与重构手法（消除异味），让代码从「能跑」变成「能长期维护」。\n- 前提：此时「工作」已验证，方向稳定，可以放心投入结构改善，不怕「白改」。\n- 价值：为「后续演化」打地基。代码会被反复修改，结构好则改动快、bug 少；结构差则每次改动都痛苦、债务累积。\n- 与第一阶段的关系：第一阶段的「能跑」是第二阶段的「安全网」——有可工作的功能 + 测试，重构才敢做。所以第一阶段不能完全没测试。\n\n3. 让它快速（Make it fast）——最后解决「快不快」：\n- 目标：在「正确」的基础上优化性能。\n- 优先级：性能。但「快」是最后的、按需的优化，而非一开始就追求。\n- 原则——「优化的黄金法则」：\n  a. 先测量再优化：用 profiler 找到真正的瓶颈，而非凭直觉优化（直觉常错，90% 的性能问题往往集中在 10% 的代码）。\n  b. 只优化「值得优化」的部分：只有「被证明是瓶颈、且影响用户感知」的地方才优化。很多代码「慢一点」根本无所谓。\n  c. 优化后仍要保持「正确」：性能优化常引入复杂度（缓存、并发、特殊算法），不能破坏「正确」阶段建立的整洁结构。优化的代码也要可读、可测。\n- 与前两阶段的关系：在「正确」的代码上优化，瓶颈清晰、改动安全（有测试）；在「能跑但乱」的代码上优化，往往越优化越乱，且优化错了地方。\n\n三个阶段如何平衡：\n\n1. 顺序不可随意颠倒——「工作 → 正确 → 快速」是默认顺序：\n- 先优化再求正确：在还没「工作」的代码上优化，方向可能错，优化白做；且优化让代码变复杂，更难「改对」。\n- 先正确再工作：在「还不确定需求对不对」时就精雕细琢结构，需求一变结构就白搭。过度设计是常见陷阱。\n- 顺序的内核：「降低不确定性」——先消除「需求/方向」的不确定性（work），再消除「结构」的不确定性（right），最后才针对「性能」这个最具体的约束优化（fast）。\n\n2. 阶段不是「绝对割裂」，而是「主次分明」：\n- 工作阶段：以「能跑」为主，但保持「最低限度的整洁」——命名基本清楚、关键路径有测试。不要写成「完全无法读」的屎山，否则第二阶段无从下手。\n- 正确阶段：以「结构」为主，但若发现明显性能问题可记录（不在此时优化）。重构本身可能顺带提升性能（如减少重复计算）。\n- 快速阶段：以「性能」为主，但每个优化都要守住「正确」（不破坏行为、不牺牲可读性到无法维护）。\n- 经验法则：每个阶段 80% 精力放在本阶段目标，20% 兼顾下一阶段的基础。\n\n3. 阶段是「循环」而非「一次性」：\n- 需求会持续变化：新需求让「已正确」的代码变烂 → 回到「让它正确」（重构）→ 有性能问题再「让它快速」。\n- 三个阶段在软件生命周期里反复上演，不是「走一遍就结束」。这就是为什么重构是「持续实践」而非「一次性项目」。\n\n4. 不同上下文的阶段侧重不同：\n- 探索期/原型：重「工作」，轻「正确」，几乎不「快速」。验证想法为主。\n- 成长期产品：三者并重，但「正确」投入最大——代码要支撑快速迭代，结构烂会拖慢一切。\n- 成熟期/高负载：在「正确」基础上重「快速」——性能成为核心竞争力，值得专项优化。\n- 一次性脚本：只「工作」即可，不必「正确」「快速」。\n- 让阶段的投入与「代码预期寿命」匹配。\n\n5. 警惕常见的「跨阶段」错误：\n- 过早优化（premature optimization）：在「工作/正确」阶段就为「可能根本不需要」的性能做复杂设计。Knuth 说「过早优化是万恶之源」。先写清楚，再证明需要快，再优化。\n- 过度设计（over-engineering）：在「工作」阶段就堆砌「未来可能用到」的抽象。YAGNI 原则——「你不会需要它」，先满足当前需求，演化出抽象再说。\n- 拖延重构：「工作」完后不进入「正确」，债务累积，最终「正确」的成本高到无法承受。应在债务还小时及时转阶段。\n- 为「正确」牺牲「工作」：执着于完美结构而迟迟交付不出功能。记住「工作」是第一位的，没有能跑的代码，再正确也没用。\n\n6. 「快」的终极理解——三个层次的「快」：\n- 阶段三的「快」是「运行快」（性能）。\n- 但「让它正确」其实让「开发快」（改得动、bug 少）。\n- 「让它工作」让「反馈快」（早验证方向）。\n- 三者结合才是「整体快」——既能早交付、又能持续快迭代、关键路径还跑得快。只追求某一层的「快」会损害其他层。\n\n综合结论：\n- 「工作 → 正确 → 快速」是「降低不确定性、按优先级投入」的工程方法论。\n- 顺序是默认的，但不是僵化的——每阶段保留下一阶段的基础，且随需求演化循环。\n- 平衡的核心是「时机」：在「对的阶段」做「对的事」。过早优化、过早抽象是浪费；过晚重构、过晚优化是债务。\n- 这句话的智慧在于「承认每件事都有时机」——不是「什么都重要」，而是「现在什么最重要」。专注当下阶段，但为下一阶段留好基础。\n\n一句话：先让代码「活下来」（work），再让它「活得体面」（right），最后让它「活得高效」（fast）。顺序、时机、循环——三者兼备，方为工程。`,
    tags: ["三阶段", "Make it work right fast", "过早优化", "平衡", "工程智慧"],
  },
];
