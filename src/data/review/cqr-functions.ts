import type { ReviewQuestion } from "./types";

/** 函数复习题 */
export const cqrFunctionsQuestions: ReviewQuestion[] = [
  {
    id: "cqr-functions-01",
    chapter: "cqr-functions",
    level: 1,
    question: "好函数有哪些特征？",
    answer:
      "好函数的特征可概括为「短小、单一职责、无副作用」三原则，外加几个辅助要求：\n\n1. 短小（Short）：\n函数应该尽量短——通常 20 行以内为宜，超过 40 行就该警惕。短到一眼能看完整、`if`/`for`/`while` 的块内通常只有一两行。短小让函数易于理解、易于测试。\n\n2. 单一职责（Do One Thing）：\n函数只做一件事——它的所有语句都在为「同一目的」服务，没有「顺便」做的其他事。判断方法：能否把函数里的某些语句提取成一个独立的、有意义的函数？如果能，说明它在做不止一件事。\n\n3. 无副作用（No Side Effects）：\n函数只做它「承诺」做的事。不会偷偷修改入参、改全局状态、写文件、发网络请求、或留下持久化痕迹。一个叫 `checkPassword` 的函数不该顺便「初始化用户会话」——那是另一件事。\n\n4. 只做一层抽象：\n函数内的语句应该处于「同一抽象层级」。不要在高层的 `renderPage()` 里夹一句底层的 `socket.write(...)`——要么提取成 `sendResponse()` 这样的同层抽象。\n\n5. 命名能说明意图：\n好函数名让人「读完名字就知道它做什么」，甚至不需要看实现。名字要描述「做什么」而非「怎么做」。\n\n6. 参数少、有明确的返回：\n参数尽量少（理想 0-2 个），有清晰的返回值，避免「既返回值又改入参」的双轨行为。\n\n一句话：好函数像一个好句子——短、只表达一个意思、不夹带私货。",
    tags: ["函数", "短小", "单一职责", "无副作用", "抽象层级"],
  },
  {
    id: "cqr-functions-02",
    chapter: "cqr-functions",
    level: 2,
    question: "函数参数为什么应该少？最多几个合适？参数过多意味着什么？",
    answer:
      "函数参数应当尽量少，理想是 0 个，其次 1 个、2 个；3 个要谨慎，尽量避免；超过 3 个通常意味着设计有问题。\n\n参数多的代价：\n\n1. 认知负担重：参数越多，调用者要记住的「顺序、类型、含义」越多。`createUser(name, email, age, role, active, sendWelcome, trialDays)` 调用时极易传错位置，且 IDE 提示也救不了「类型相同但语义不同」的参数。\n\n2. 测试组合爆炸：参数是测试的「输入维度」。3 个布尔参数意味着 2³=8 种组合要测；4 个就是 16 种。覆盖成本指数上升。\n\n3. 副作用风险高：参数多往往意味着函数要协调很多状态，容易产生隐藏依赖和副作用，违反单一职责。\n\n4. 暗示职责过载：参数多常是「函数做了太多事」的信号——每个参数对应一个职责，挤在一个函数里。\n\n参数过多意味着什么、怎么改：\n\n1. 把多个参数封装成对象：\n相关参数合并成一个参数对象/结构体。`createUser(name, email, age, role, ...)` → `createUser(UserRequest req)`。对象有字段名，调用更清晰，也便于扩展（加字段不改签名）。\n\n2. 拆分成多个函数：\n如果参数服务于不同职责，拆成多个单一职责函数。`registerUser(name, email, sendEmail, createProfile)` 拆成 `registerUser` + `sendWelcomeEmail` + `createProfile`。\n\n3. 用流畅接口/建造者模式：\n参数多且可选时，用链式调用：`new UserBuilder().withName(...).withEmail(...).build()`，比一长串位置参数清晰。\n\n4. 区分「标志参数」：\n布尔参数尤其危险——`saveUser(user, true)` 里的 `true` 谁看得懂？这通常意味着「一个函数做了两件事」，应拆成两个函数：`saveUser()` 和 `saveUserSilently()`。\n\n例外：参数顺序天然成对（如 `setPoint(x, y)`、`copy(src, dst)`）时 2-3 个参数是合理的，不必强行封装。判断标准不是「数量」而是「认知负荷」。",
    tags: ["函数参数", "参数数量", "参数对象", "职责过载"],
  },
  {
    id: "cqr-functions-03",
    chapter: "cqr-functions",
    level: 3,
    question: "如何把一个 50 行的函数拆小？请给出可操作的拆分步骤。",
    answer:
      "拆分大函数是重构的核心手法之一。可操作步骤如下：\n\n1. 先建立安全网——补测试：\n拆分前必须有用例覆盖这个函数的「行为」。如果没有测试，先写几个针对典型输入/输出的测试，把当前行为「钉住」。拆分时跑测试，确保行为不变。没有测试就拆分等于盲飞。\n\n2. 通读函数，标注「职责块」：\n逐行阅读，找出「逻辑上属于同一目的」的语句块——通常是：输入校验、数据准备、核心计算、结果转换、副作用（写库/发消息）、返回。每一块就是一个候选的「子函数」。\n\n3. 用「提取函数」手法逐块拆出：\n从最独立、最低风险的一块开始（通常是校验或纯计算）。手法：\n- 选中这块语句，提取成新函数，起一个「说明它做什么」的名字。\n- 这块用到的局部变量成为新函数的参数；它产生的、外层还要用的值成为返回值。\n- 用 IDE 的「Extract Method」重构功能，自动处理变量传递。\n- 每提取一块，立刻跑测试验证行为不变。\n\n4. 处理「局部变量」的纠缠：\n这是拆分的主要难点。常见情况：\n- 变量只在块内用 → 直接移进新函数，作为局部变量。\n- 变量在块外初始化、块内修改、块外还要用 → 作为参数传入、作为返回值传出。若多个值要返回，可封装成临时结构体。\n- 变量在多处修改 → 先用「以查询取代临时变量」等手法减少可变状态，再提取。\n\n5. 调整抽象层级，让函数「读起来像步骤列表」：\n拆完后，原函数应该只剩下一串「高层调用」：`validateInput(); prepareData(); computeResult(); persistResult(); return result;`。如果原函数里还夹杂底层细节，说明这些细节也该被提取。\n\n6. 检查并消除「参数列表过长」的副作用：\n如果某个提取出的子函数参数很多，说明它和原函数耦合仍紧——可能需要把相关参数合并成对象，或进一步拆分。\n\n7. 为每个新函数起好名字：\n名字要描述「做什么」。如果某个子函数很难命名，说明它职责不清，可能拆得不对，应重新切分。\n\n8. 反复跑测试 + 提交：\n每拆一块、跑通测试、就提交一次。小步前进，出问题容易回退。不要一次拆完再测——出错时无法定位是哪步引入的。\n\n示例：一个 50 行的 `processOrder` 函数，拆完可能变成：\n`processOrder()` → 调用 `validateOrder()` → `calculateTotals()` → `applyDiscounts()` → `saveOrder()` → `sendConfirmation()` → 返回。每个子函数 5-10 行，整体行为不变，但可读性、可测性大幅提升。",
    tags: ["函数拆分", "提取函数", "重构手法", "测试先行"],
  },
  {
    id: "cqr-functions-04",
    chapter: "cqr-functions",
    level: 4,
    question: "命令查询分离（CQS）原则是什么？它为什么重要？在哪些场景下会冲突？",
    answer:
      "命令查询分离（Command-Query Separation，CQS）：一个函数应该要么是「命令」（执行副作用、改变状态，不返回值），要么是「查询」（返回值、不改状态），但不能两者兼具。\n\n- 命令（Command/Modifier）：做某件事，有副作用，通常返回 `void`。如 `setAge(20)`、`saveUser()`、`sendEmail()`。\n- 查询（Query）：回答一个问题，无副作用，返回值。如 `getAge()`、`userExists()`、`getCurrentTime()`。\n\n为什么重要：\n\n1. 消除「双重含义」的歧义：\n如果一个函数既改状态又返回值，调用者会困惑——「这个返回值是改之前的状态还是改之后？它到底改了没有？」例如 `set(attribute, value)` 返回旧值，调用者搞不清它是「设了新值并顺便告诉你旧值」还是「没设成功」。分离后，`set` 只管设、`get` 只管查，语义清晰。\n\n2. 让查询可自由组合、可缓存、可重复调用：\n纯查询没有副作用，调用多少次结果一致（在状态不变时），可以缓存、可以并发调用、可以放进表达式任意位置。若查询夹带副作用，重复调用就会出 bug。\n\n3. 让副作用显式化，便于追踪：\n当所有「改变状态」的操作都集中在命令函数里，调试时只需关注这些函数，状态变更点清晰可查。若查询也改状态，「状态从哪变来的」就难以追踪。\n\n4. 配合测试：\n纯查询好测（给定输入断言输出）；纯命令也好测（断言状态变化）。混合函数测起来既要断言返回值又要断言状态，耦合重。\n\n冲突与例外场景：\n\n1. 栈/队列的 `pop()`：\n`pop()` 既移除栈顶又返回它——既命令又查询。这是数据结构本身的语义需要。强行分离会写成 `peek()` + `removeTop()` 两步，在并发场景下不原子。这类是「合理违背 CQS」的经典例子。\n\n2. 「取并递增」的原子操作：\n`fetchAndAdd(counter, 1)` 既要返回旧值又要递增，在并发编程中必须原子完成，分离会引入竞态。这类操作用 CQS 之外的方式（如 CAS、原子原语）保证安全。\n\n3. 迭代器的 `next()`：\n`next()` 既推进游标又返回当前元素，也是合理违背。\n\n4. 「创建并返回」的工厂方法：\n`createUser()` 既创建（副作用：写库）又返回新对象。这可看作「命令返回它创建的东西的引用」，是常见且可接受的折中。\n\n如何权衡：\n- 默认遵循 CQS：日常业务函数尽量分离，`get` 不改状态、`set` 不返回值。\n- 识别「语义必然耦合」的例外：当两个动作「必须原子完成」或「分离会引入竞态/不一致」时，可合并，但要在命名和文档里说清楚它「既做又查」。\n- 进阶——CQRS（命令查询职责分离）：在架构层面把「写模型」和「读模型」彻底分开（不同数据库/不同服务），是 CQS 在系统级的延伸，适用于读写负载差异大的系统。\n\n总结：CQS 是「默认规则」而非「绝对教条」。它的价值在于让 95% 的函数语义清晰；剩下 5% 的合理违背，要能讲清楚为什么必须违背、以及如何保证安全。",
    tags: ["CQS", "命令查询分离", "副作用", "并发", "CQRS"],
  },
];
