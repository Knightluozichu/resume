import type { ReviewQuestion } from "./types";

/** 单元测试复习题 */
export const cqrTestingQuestions: ReviewQuestion[] = [
  {
    id: "cqr-testing-01",
    chapter: "cqr-testing",
    level: 1,
    question: "测试金字塔的三层是什么？各层的特点和比例如何？",
    answer:
      "测试金字塔（Test Pyramid）是 Mike Cohn 提出的测试分层模型，分三层，从下到上数量递减、速度递减、成本递增：\n\n1. 单元测试（Unit Tests，底层，最多）：\n- 测试「单个函数/类/模块」的行为，把它与外部依赖隔离（用 mock/stub）。\n- 特点：快（毫秒级）、独立、可并行、数量最多（占 70%-80%）。\n- 价值：精确定位 bug、随时跑、重构的安全网。\n- 比喻：金字塔的底座，量大、稳。\n\n2. 集成测试（Integration Tests，中层，适中）：\n- 测试「多个模块协作」的行为，验证它们的接口契合、数据流转正确。可能涉及真实数据库、外部服务的测试替身。\n- 特点：中等速度（秒级）、数量适中（占 10%-20%）。\n- 价值：捕获「单元都正确但拼起来出错」的集成问题（接口不匹配、数据格式、事务边界）。\n- 比喻：金字塔的中段，连接单元与端到端。\n\n3. 端到端测试（End-to-End / E2E / UI Tests，顶层，最少）：\n- 测试「整个系统从头到尾」的业务流程，模拟真实用户操作（如通过浏览器点击、调真实 API、读真实数据库）。\n- 特点：慢（分钟级）、脆弱（依赖环境）、数量最少（占 5%-10%）。\n- 价值：验证「整体业务能跑通」，给团队信心。但维护成本高、失败时难定位是哪一层的问题。\n- 比喻：金字塔的塔尖，少而精。\n\n为什么是「金字塔」而非「倒金字塔」：\n- 底层多：单元测试快而稳，能覆盖大量边界和分支，提供密集的回归保护。\n- 顶层少：E2E 慢且脆，跑一次成本高、失败诊断难，多了会拖慢开发、让 CI 变得不可靠。\n- 倒金字塔（E2E 多、单元少）是反模式——慢、脆、维护噩梦，常出现在「不写单元测试、只靠手动或 E2E 验证」的团队。\n\n变形与补充：\n- 测试奖杯（Testing Trophy）：Kent C. Dodds 提出，在单元和集成之间强调「集成测试」占比最大，因为现代前端组件难以纯单元化。形态是「奖杯」而非严格金字塔。\n- 实践要点：比例不是死的，原则是「尽量把测试下沉到最快最稳的层级」，只在必要时才上移到更慢的层级。",
    tags: ["测试金字塔", "单元测试", "集成测试", "端到端测试", "分层"],
  },
  {
    id: "cqr-testing-02",
    chapter: "cqr-testing",
    level: 2,
    question: "单元测试的 AAA 模式（Arrange-Act-Assert）是什么？为什么要这样组织？",
    answer:
      "AAA 模式是组织单个单元测试用例的标准结构，把一个测试分成三段：\n\n1. Arrange（准备）：\n设置测试的前置条件——构造输入数据、初始化被测对象、准备 mock/stub、配置环境。这一段建立「给定（Given）」的状态。\n- 例：`user = new User(\"alice\", 0); repository = mock(); repository.save = mock();`\n\n2. Act（执行）：\n调用被测函数，执行「被测试的那个动作」。这是测试的核心，通常只有一行。对应「当（When）」。\n- 例：`user.upgradeToPremium(repository);`\n\n3. Assert（断言）：\n验证「执行后的结果是否符合预期」——检查返回值、状态变化、mock 是否被正确调用。对应「那么（Then）」。\n- 例：`assert(user.isPremium == true); assert(repository.save was called with user);`\n\n完整示例：\n`// Arrange\nuser = new User(\"alice\", 0);\nrepo = mock();\n// Act\nresult = user.upgradeToPremium(repo);\n// Assert\nassert(result == true);\nassert(user.isPremium == true);\nassert(repo.save was called once);`\n\n为什么要这样组织：\n\n1. 结构清晰、可读性高：\n三段分明，读者一眼看出「准备什么、做什么、验证什么」。测试像一句规整的句子：「给定 X，当做 Y，那么应该 Z」。没有 AAA 的测试往往把准备、执行、断言混在一起，逻辑混乱。\n\n2. 关注点分离：\n每段只做一件事——Arrange 不做断言、Assert 不做额外准备。这让每段都简短聚焦，出问题时容易定位是「准备错了」「执行错了」还是「断言错了」。\n\n3. 一个测试只验证一个行为：\nAAA 自然引导你写「单一断言」的测试——一个 Act 对应一个核心断言。如果一个测试需要很多 Assert，往往说明它在测多个行为，应拆成多个测试。\n\n4. 失败时诊断友好：\n当测试失败，按 AAA 结构能快速判断：是 Arrange 的前置没满足、Act 抛了异常、还是 Assert 的预期不对。混乱结构的测试失败时难以定位。\n\n5. 便于复用与维护：\nArrange 部分常可提取成「测试夹具（fixture）」或 builder 共享；Act/Assert 保持独立。结构统一后，全队测试风格一致，维护成本低。\n\n常见变体与补充：\n- Given-When-Then：BDD 风格的等价表达，语义相同，措辞更接近业务语言（`Given ... When ... Then ...`）。\n- 有时加 Cleanup（清理）作为第四段，释放资源、重置全局状态，保证测试隔离。\n- 如果 Act 可能抛异常，Assert 段变成「断言抛出了特定异常」。\n\n反模式：\n- 把多个 AAA 循环塞进一个测试（一个测试里 Arrange-Act-Assert 多次），难读、失败难定位，应拆成多个测试。\n- Arrange 过长——说明前置太复杂，可能被测对象依赖太多，应简化或用 fixture。\n- Act 不明显——执行藏在一堆准备里，读者找不到「到底测的是什么动作」。",
    tags: ["AAA 模式", "单元测试", "Arrange-Act-Assert", "测试结构"],
  },
  {
    id: "cqr-testing-03",
    chapter: "cqr-testing",
    level: 3,
    question: "什么是 TDD？红-绿-重构循环的具体步骤是什么？",
    answer:
      "TDD（Test-Driven Development，测试驱动开发）：在写「生产代码」之前先写「测试」的开发方法。由 Kent Beck 提出，核心是「让测试引导设计」，而非「写完代码再补测试」。\n\n核心循环——红-绿-重构（Red-Green-Refactor）：\n\n1. 红（Red）——先写一个失败的测试：\n在还没实现功能时，先写一个描述「期望行为」的测试。因为功能还没实现，这个测试必然失败（红色）。\n- 这一步定义了「要做什么」——测试就是需求规格。\n- 关键：测试要小而具体，一次只描述一个行为。写完确认它「因正确的原因失败」（编译错或断言错，而不是环境问题）。\n\n2. 绿（Green）——写最简单的代码让测试通过：\n用最直接、最简单（甚至最丑）的方式实现功能，让测试变绿。不追求完美设计，只求「通过」。\n- 这一步定义了「做出来了」。\n- 关键：不要过度设计、不要写测试没要求的功能（YAGNI）。哪怕硬编码返回值也行——只要测试通过。\n- 目的：快速进入「可用」状态，建立反馈。\n\n3. 重构（Refactor）——在测试保护下改善代码：\n测试是绿的，意味着行为被锁定。此时放心地重构——消除重复、改善命名、提取函数、优化结构。每改一点就跑测试，确保始终是绿的。\n- 这一步定义了「做好」。\n- 关键：行为不变（测试始终绿），只改内部结构。重构和「加功能」严格分开——这一步不加新测试、不改行为。\n- 目的：让代码从「能跑」变成「整洁」。\n\n循环往复：\n写下一个测试（红）→ 实现（绿）→ 重构 → 再写下一个测试……每个循环几分钟，小步前进。整个开发过程是无数个红-绿-重构小循环的累积。\n\n为什么要这么做（TDD 的价值）：\n\n1. 测试先行保证「可测性」：\n先写测试迫使你从「调用者视角」设计接口——如果难测，说明耦合重、依赖多，你会自然改进设计。事后补测试往往发现「代码根本没法测」。\n\n2. 即时反馈，减少调试：\n每写一点就有测试验证，错误在几分钟内暴露，定位范围小。而非「写一大堆再测」，出错时不知道是哪里的问题。\n\n3. 测试即文档：\n测试用例就是「代码该怎么用」的活文档，比注释更可靠（因为它会被执行验证）。\n\n4. 重构有安全网：\n因为每段功能都有测试，重构时随时跑测试，不怕改坏。没有测试的重构是赌博。\n\n5. 避免过度设计：\n「只写让测试通过的代码」天然抑制「提前实现用不到的功能」。需求由测试驱动，而非凭空想象。\n\n常见误区：\n- 跳过「红」：先写代码再补测试，失去了「测试引导设计」的价值，退化为「事后验证」。\n- 跳过「重构」：绿了就接着写下一个，代码很快腐化。重构是 TDD 不可缺的一环。\n- 一次写太多测试：一个循环只针对一个行为，写一大坨测试会让「红」难以定位、实现难以聚焦。\n- 测试太依赖实现细节：测试应验证「行为」而非「实现」，否则重构时测试会大量失败，反而成为负担。\n\n适用边界：\nTDD 适合「逻辑密集、可单元化」的代码（算法、业务规则、状态机）。对「探索性原型」「UI 布局」「与外部系统强耦合」的代码，TDD 收益有限，可先探索再补测试。它是工具不是教条。",
    tags: ["TDD", "红绿重构", "测试驱动", "重构"],
  },
  {
    id: "cqr-testing-04",
    chapter: "cqr-testing",
    level: 4,
    question: "测试覆盖率 100% 是好事吗？如何真正衡量测试质量？",
    answer:
      "测试覆盖率 100% 本身既不是好事也不是坏事——它是「必要不充分」指标，盲目追求 100% 往往有害。\n\n为什么 100% 覆盖率不等于好测试：\n\n1. 覆盖率衡量的是「执行过」，不是「验证过」：\n覆盖率工具只统计「这行代码在测试里跑过没有」，不判断「测试是否真正断言了正确行为」。你可以写一个 `assert(true)` 的测试跑遍所有代码，覆盖率 100% 但零价值。覆盖率高只说明「代码被碰过」，不说明「行为被验证过」。\n\n2. 100% 可能是「为了指标而写无用测试」：\n当团队把 100% 设为硬性 KPI，开发者会写大量「为覆盖率而存在」的测试——测 getter/setter、测框架生成的代码、测不可能失败的分支。这些测试增加维护负担却不增加安全感，是「指标游戏」。\n\n3. 边缘情况的覆盖比行覆盖更重要：\n100% 行覆盖率可能仍漏掉关键边界——循环的 `off-by-one`、空输入、并发竞态、异常路径。一个 `if (x > 0)` 即使两条分支都跑过，也没测 `x == 0`、`x == MAX_INT`。真正出 bug 的往往是这些「行覆盖不到的语义边界」。\n\n4. 追求 100% 会扭曲优先级：\n把时间花在「把覆盖率从 90% 提到 100%」上，往往是在测那些「简单且不会出错」的代码，而忽视了「复杂核心逻辑」的深度测试。投入产出比极低。\n\n5. 某些代码本不该追求覆盖：\n- 平台/框架代码、调试代码、防御性代码（如 `assert unreachable`）测它没意义。\n- 错误处理中「不可能发生」的分支，强行覆盖要造很假的环境，测试脆弱。\n\n如何真正衡量测试质量：\n\n1. 看测试的「断言质量」而非覆盖率：\n好测试有「强断言」——不仅跑过代码，还验证了「关键输出/状态/副作用」。审查时问：「如果被测代码改坏了，这个测试会失败吗？」不会失败的测试就是无效的，无论覆盖率多高。\n\n2. 变异测试（Mutation Testing）：\n更深的衡量——工具自动修改被测代码（如把 `+` 改成 `-`、`>` 改成 `>=`、删一行），看测试是否能捕获。如果改坏了代码测试还能过，说明测试没真正覆盖这个逻辑。变异测试得分比行覆盖率更接近「真实有效性」。\n\n3. 看「缺陷捕获率」与「回归防护」：\n测试的最终目的是「防止 bug」。衡量「测试是否抓到了真实引入的 bug」（过去一段时间的 bug 有多少是被测试拦截的、多少是漏到线上的）。这是最真实的质量指标，但滞后、难量化。\n\n4. 看测试的「信心度」：\n问团队：「如果你删掉某个测试套件，敢不敢直接发布？」敢删的说明测试没提供信心。好测试让团队「敢改、敢发」——这是测试存在意义的终极体现。\n\n5. 看测试的「健康度」：\n- 速度：单元测试是否秒级跑完？慢测试会被跳过，失去价值。\n- 稳定性：有没有 flaky test（时好时坏）？flaky 测试侵蚀信任，应清零。\n- 维护成本：测试是否易读、易改？难维护的测试会拖慢开发。\n\n6. 覆盖率作为「下限」而非「目标」：\n合理的做法是设一个「下限门槛」（如核心模块 80%、整体 60%），低于门槛报警，但不强制 100%。重点放在「核心逻辑、复杂分支、错误路径」的深度覆盖，而非全库均匀覆盖。\n\n综合结论：\n- 覆盖率是有用的「负向指标」——覆盖率低（如 30%）几乎肯定意味着测试不足，应提高。\n- 覆盖率是糟糕的「正向目标」——追求 100% 会催生无效测试、扭曲优先级。\n- 真正衡量测试质量要看：断言强度、变异测试得分、缺陷捕获率、团队信心、测试健康度。覆盖率只是众多信号之一，不是终点。\n\n一句话：100% 覆盖率让你「以为」安全，好测试让你「真的」安全。前者是数字，后者是能力。",
    tags: ["测试覆盖率", "测试质量", "变异测试", "断言质量", "指标"],
  },
];
