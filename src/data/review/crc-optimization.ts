import { ReviewQuestion } from "./types";

export const crcOptimizationQuestions: ReviewQuestion[] = [
  {
    id: "crc-optimization-1",
    chapter: "crc-optimization",
    level: 1,
    question: `代码优化的目标是什么？局部优化、全局优化和循环优化各自的范围是什么？`,
    answer:
      `代码优化目标是在不改变程序语义的前提下，生成更高效的目标代码（更快、更小）。局部优化在单个基本块（无内部跳转的连续指令序列）内进行，如常量折叠（3*4→12）、代数化简（x+0→x）。全局优化跨基本块进行，需要数据流分析支撑，如死代码消除（删除结果未被使用的计算）、公共子表达式消除（重复计算只算一次）。循环优化针对循环结构，效果最显著，如循环不变量外提（将循环内不变的计算提到循环外）、归纳变量强度削弱（用加法替代乘法）。优化范围越大，分析越复杂，但优化效果越好。`,
    tags: ["代码优化", "局部优化", "全局优化", "循环优化", "基本块"],
  },
  {
    id: "crc-optimization-2",
    chapter: "crc-optimization",
    level: 2,
    question: `什么是数据流分析？到达定值、活跃变量和可用表达式分别解决什么问题？`,
    answer:
      `数据流分析是通过在控制流图上传播信息来推导程序运行时性质的技术，是全局优化的理论基础。到达定值（Reaching Definition）：分析哪些变量的赋值（定值）能到达程序的某个点——用于未定义变量检测、UD链（use-define chain）构建，是前向数据流。活跃变量（Liveness）：分析变量在某个点之后是否还会被使用——用于死代码消除（不活跃的赋值可删除）和寄存器分配（不活跃的寄存器可重用），是反向数据流。可用表达式（Available Expression）：分析哪些表达式已被计算且操作数未变——用于公共子表达式消除（可直接复用已有结果），是前向数据流。`,
    tags: ["数据流分析", "到达定值", "活跃变量", "可用表达式", "控制流图"],
  },
  {
    id: "crc-optimization-3",
    chapter: "crc-optimization",
    level: 3,
    question: `以 \`x = 3 * 4 + a\` 为例说明常量折叠。死代码消除如何工作？`,
    answer:
      `常量折叠：在编译期直接计算常量表达式的值。x = 3 * 4 + a 中 3 * 4 全部是常量，编译期计算为 12，优化为 x = 12 + a。如果 a 也已知为常量（如 a = 5），则进一步折叠为 x = 17，甚至可在编译期完全消除该计算。死代码消除：基于活跃变量分析，如果一条赋值指令的结果变量在后续不被任何路径使用（不活跃），则该赋值是死代码，可以安全删除。例如 x = 1; y = 2; z = x + y; 若 z 后续从未使用，则三条指令全部是死代码，可全部删除。删除后可能产生新的死代码（级联删除），需迭代到不动点。`,
    tags: ["常量折叠", "死代码消除", "活跃变量", "编译期计算", "不动点"],
  },
  {
    id: "crc-optimization-4",
    chapter: "crc-optimization",
    level: 2,
    question: `循环不变量外提（LICM）和强度削弱是什么？各举一个例子。`,
    answer:
      `循环不变量外提（Loop Invariant Code Motion）：如果循环体内有计算结果不依赖循环变量的表达式（循环不变量），将其提到循环外面只计算一次。例如 for (i=0; i&lt;n; i++) { x = a * b; ... } 中 a*b 不随 i 变化，外提为 x = a*b; for (i=0; i&lt;n; i++) { ... }，将 n 次乘法降为 1 次。强度削弱：用代价更低的运算替代代价更高的运算。例如循环中 i = i + 1; x = i * 4，由于 i 每次加 1，x 每次加 4，可将乘法替换为加法：x = x + 4（在循环外初始化 x = i * 4）。加法比乘法快，降低循环内运算开销。两者都依赖循环分析（识别循环、归纳变量）。`,
    tags: ["循环优化", "循环不变量外提", "强度削弱", "归纳变量", "LICM"],
  },
];
