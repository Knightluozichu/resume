import type { ReviewQuestion } from "./types";

export const cseOfficialChapterQuestions: ReviewQuestion[] = [
  {
    id: "cse-cpp-must-know-1",
    chapter: "cse-cpp-must-know",
    level: 1,
    question: "RAII 与 pimpl 分别把什么责任绑定到类型边界，为什么 unique_ptr 指向 incomplete Impl 时 destructor 常要放在 cpp 文件定义？",
    answer: "RAII 把 resource acquire/release 与 object constructor/destructor 绑定，并用 deleted copy、explicit move、empty state 保证单 owner；pimpl 把 layout 和 implementation includes 隔离到 Impl。unique_ptr 可在 header 持有 forward-declared Impl，但默认 deleter 真正删除时需要 complete type，所以 owning class destructor 要在看见 Impl 定义的 cpp 中 out-of-line default。",
    tags: ["RAII", "pimpl", "unique_ptr", "incomplete type", "destructor"],
  },
  {
    id: "cse-cpp-must-know-2",
    chapter: "cse-cpp-must-know",
    level: 2,
    question: "auto/range-for/structured binding/emplace 与 unique/shared/weak pointer 各自最容易隐藏什么 contract，怎样在服务器代码 review 中验证？",
    answer: "auto 和 binding 的 reference qualifier 决定 copy/alias；range-for 依赖 begin/end 和 loop variable；emplace/try_emplace/insert_or_assign 的 construction 与 duplicate-key policy 不同。unique_ptr 应是默认 exclusive owner；shared_ptr 共享 control block 但不保护 pointee；weak_ptr 表达 observer/cycle breaker。review 要画 owner/control-block edges，检查 raw-this 第二 control block、strong cycle、shared_from_this 前置条件，并对 copy/allocation 做测量。",
    tags: ["auto", "structured binding", "emplace", "shared_ptr", "weak_ptr"],
  },
  {
    id: "cse-backend-tools-debugging-1",
    chapter: "cse-backend-tools-debugging",
    level: 1,
    question: "线上 core 只有可执行文件却无法可靠定位源码时，应该怎样重建 binary、symbol、source 三者的证据链？RelWithDebInfo、strip 与 build-id 各自解决什么问题？",
    answer: "先用 build-id、提交号和构建产物清单确认 core 对应的原始 binary，再加载同一次构建导出的独立 debug symbols，最后用 dir 或 set substitute-path 映射完全一致的源码。RelWithDebInfo 保留优化行为并生成调试信息；strip 只从交付 binary 分离符号，不能丢弃归档；build-id 用来精确匹配 binary 与 symbol，不能用相近版本替代。",
    tags: ["core dump", "DWARF", "RelWithDebInfo", "strip", "build-id"],
  },
  {
    id: "cse-backend-tools-debugging-2",
    chapter: "cse-backend-tools-debugging",
    level: 2,
    question: "调试多线程、多进程服务器时，为什么只看当前线程的一帧通常会误判？如何组合 stop、thread、inferior 与 signal 上下文收敛故障？",
    answer: "断点或信号只说明停下位置，不等于根因位置。应先记录信号及 stop reason，再用 thread apply all bt full 查看所有线程的锁、调用栈和等待关系，必要时通过 scheduler-locking 控制单步；对 fork/exec 场景确认 follow-fork-mode、detach-on-fork 和当前 inferior，分别检查父子进程；最后核对 GDB 对该 signal 的 stop/print/pass 策略，避免调试器改变程序原有行为。",
    tags: ["GDB", "thread", "inferior", "fork", "signal"],
  },
  {
    id: "cse-multithreading-resource-sync-1",
    chapter: "cse-multithreading-resource-sync",
    level: 1,
    question: "为什么线程 owner 不能只在 destructor 中 detach？怎样把 start、ready、stop、join 与 worker exception 设计成完整 lifecycle contract？",
    answer: "detach 只丢弃等待句柄，不延长捕获对象 lifetime，也无法确认任务完成。owner 应创建 joinable thread，用 promise/future 或受 mutex 保护的 ready/error 做启动握手；停止时先关闭 admission，发布 stopping state 并 notify，worker 捕获异常并传给 owner，owner 最终 join，再销毁 worker 使用的共享状态。",
    tags: ["thread lifecycle", "join", "detach", "startup handshake", "exception"],
  },
  {
    id: "cse-multithreading-resource-sync-2",
    chapter: "cse-multithreading-resource-sync",
    level: 2,
    question: "如何从共享不变量推导 atomic、mutex、condition variable、semaphore、rwlock 和 bounded queue 的选择，而不是按 API 名称试错？",
    answer: "单变量原子 RMW 可用 atomic，并按是否发布旁边数据选择 relaxed 或 release/acquire；复合不变量用 mutex 和 RAII；等待状态变化用同一 mutex 下的 predicate 加 condition variable；计数 permit 用 semaphore；读多写少且实测有收益才用 rwlock。任务系统还必须用 bounded queue 配置 reject/timeout/drop/degrade/backpressure，不能用无界积压掩盖服务率不足。",
    tags: ["atomic", "condition variable", "semaphore", "rwlock", "backpressure"],
  },
  {
    id: "cse-network-programming-hard-points-1",
    chapter: "cse-network-programming-hard-points",
    level: 1,
    question: "为什么 nonblocking send/recv/connect 不能统一成 success/failure？列出连接状态机必须保留的返回语义。",
    answer: "send/recv 可能产生 partial progress、would-block、EINTR、peer FIN 或 fatal error；connect 的 EINPROGRESS/WSAEWOULDBLOCK 表示 pending，writable 后还要读取 SO_ERROR。状态机必须保留 n-byte offset、read/write interest、connect-pending、peer-closed 和 platform error，bool 会造成重复发送、丢数据或误断线。",
    tags: ["nonblocking", "partial I/O", "EINTR", "SO_ERROR", "state machine"],
  },
  {
    id: "cse-network-programming-hard-points-2",
    chapter: "cse-network-programming-hard-points",
    level: 2,
    question: "select、poll、epoll LT、ET 与 EPOLLONESHOT 的 readiness contract 有何差异？ET worker 应怎样读、写和交还 fd ownership？",
    answer: "select/poll 每轮提交并扫描集合，select 还会改写 fd_set；LT 在 fd 保持 ready 时可重复报告；ET 主要报告状态边沿，必须配 nonblocking fd 并 drain 到 EAGAIN；ONESHOT 交付一次后禁用。worker 应在公平预算内推进 input/output buffer 到 would-block，更新 interest/state，再用 EPOLL_CTL_MOD rearm，避免多 worker 同时处理同一 fd。",
    tags: ["select", "poll", "epoll", "ET", "EPOLLONESHOT"],
  },
  {
    id: "cse-network-troubleshooting-commands-1",
    chapter: "cse-network-troubleshooting-commands",
    level: 1,
    question: "面对 connect timeout，怎样用 ifconfig/ip、ping、nc、ss/netstat、lsof、curl 与 tcpdump 建立最小 network evidence chain？",
    answer: "先确认 client interface/address/route 与实际解析 IP；ping 只作 ICMP 旁证；用 nc 对目标 TCP port 定时复现；server 用 ss/netstat 核对 listener、tuple/state/queue，lsof 关联 PID/fd；若 transport 成功再用 curl 拆 DNS/connect/TLS/TTFB；两端用精确 BPF 和同一时间窗口抓包，定位 SYN、SYN-ACK、RST 或应用时序在哪一段中断。",
    tags: ["network evidence", "nc", "ss", "lsof", "tcpdump"],
  },
  {
    id: "cse-network-troubleshooting-commands-2",
    chapter: "cse-network-troubleshooting-commands",
    level: 2,
    question: "为什么 ping 通、端口通和 curl 200 都不能单独证明服务健康？每项分别缺少哪一层证据？",
    answer: "ping 只证明有限 ICMP reachability，不覆盖 TCP/TLS/application；nc/telnet 端口通只证明 handshake 或少量 transport，handler、dependency 和响应内容仍可能失败；curl 200 只证明该请求路径在该时刻成功，不覆盖其他来源、长连接、丢包或容量。结论必须绑定五元组、时间、方向和真实协议，并结合 socket/process/packet 证据。",
    tags: ["ping", "telnet", "curl", "five-tuple", "hypothesis"],
  },
  {
    id: "cse-network-protocol-design-1",
    chapter: "cse-network-protocol-design",
    level: 1,
    question: "为什么 TCP 的一次 recv 不能作为消息边界？一个安全的 length-prefix incremental decoder 必须按什么顺序处理？",
    answer: "TCP 只保留有序 bytes，header/body 可拆分，多帧可合并。decoder 先累积固定 header；读取 fixed-width network-order length；在分配前检查 zero/min/max 和加法溢出；body 不足就保留状态；足够时提取一帧并循环解析剩余 bytes。protocol error 与 NeedMore 必须分开。",
    tags: ["TCP stream", "framing", "length prefix", "incremental decoder", "limits"],
  },
  {
    id: "cse-network-protocol-design-2",
    chapter: "cse-network-protocol-design",
    level: 2,
    question: "从 raw struct 演化到 TLV 时，怎样同时处理版本升级、unknown field、整数压缩、分片和 WebSocket/HTTP 等上层状态？",
    answer: "wire schema 显式固定 width/endian/tag/type/length，不发送 ABI struct；定义 required/optional/unknown/duplicate policy和 version negotiation，用 old/new 双向 golden vectors 验证。varint 限制最大 bytes，分片限制 count/total/time/memory。HTTP 按 Content-Length/chunked 等 framing，WebSocket Upgrade 后切换 frame parser并验证 mask/opcode/length/compression budget。",
    tags: ["TLV", "versioning", "varint", "fragmentation", "WebSocket"],
  },
  {
    id: "cse-single-service-structure-1",
    chapter: "cse-single-service-structure",
    level: 1,
    question: "one thread one loop 中，为什么业务 worker 只能提交 command，不能直接 send/close socket？wakeup 与 generation check 各解决什么问题？",
    answer: "owner loop 独占 fd、input/output offset、interest mask、close 和 timer state，可避免多线程顺序竞态与 fd reuse。worker enqueue command 后写 eventfd/pipe，让 poller 立即醒来；command 携带 stable connection id/generation，loop 验证连接仍是同一代再执行，防止异步结果落到已关闭或复用的 fd。",
    tags: ["one thread one loop", "ownership", "wakeup", "fd reuse", "generation"],
  },
  {
    id: "cse-single-service-structure-2",
    chapter: "cse-single-service-structure",
    level: 2,
    question: "如何把 Reactor、Connection/Buffer/Session、timer 和 high-water backpressure 组合成一个可安全关闭的经典服务结构？",
    answer: "acceptor 把 nonblocking fd 分配给 I/O loop；Poller/Channel 报 readiness，Connection 管 fd/state 与 buffers，Codec 解帧，Session 管协议/业务 identity；慢任务进 bounded worker pool，结果回 owner loop。timer 按 monotonic deadline 绑定 connection。关闭时停止 admission，标记 closing，取消 timer并从 poller/map 移除，丢弃过期 worker result，event batch 后 deferred destroy；high-water 限制慢端 output 积压。",
    tags: ["Reactor", "Connection", "Session", "timer", "backpressure"],
  },
  {
    id: "cse-redis-network-module-source-analysis-1",
    chapter: "cse-redis-network-module-source-analysis",
    level: 1,
    question: "沿 Redis 6.0 server 源码，一条新连接的 SET 请求从 listen fd 到 +OK 写出经过哪些关键函数和 client state？",
    answer: "initServer 创建 ae loop/listen fd并把 AE_READABLE 绑定 acceptTcpHandler；acceptCommonHandler/createClient 初始化 client/querybuf/reply并注册 readQueryFromClient。read 追加 querybuf，processInputBuffer 调 processMultibulkBuffer 形成 argv，processCommandAndResetClient/processCommand 执行 SET；addReply* 写 static buf或reply list，clientInstallWriteHandler 进入 pending-write，beforeSleep 批处理或 sendReplyToClient/writeToClient flush。",
    tags: ["Redis 6.0", "acceptTcpHandler", "readQueryFromClient", "processInputBuffer", "writeToClient"],
  },
  {
    id: "cse-redis-network-module-source-analysis-2",
    chapter: "cse-redis-network-module-source-analysis",
    level: 2,
    question: "Redis 6.0 threaded I/O、RESP pipeline 与 asynchronous client free 的 owner 边界如何衔接？",
    answer: "主线程组织 clients_pending_read/write 并把 batch 分到 io_threads_list；workers 做 socket read/write，主线程等待完成后解析 querybuf并执行 commands。RESP parser 用 qb_pos/multibulklen/bulklen 跨 read 保留状态并循环执行 pipeline，replies 按序进入 buffers。断开可先 freeClientAsync，安全阶段 freeClientsInAsyncFreeQueue→freeClient→unlinkClient，清 pending lists/events/index 后释放 state，避免当前 callback 使用悬空 client。",
    tags: ["threaded I/O", "RESP", "pipeline", "freeClientAsync", "unlinkClient"],
  },
  {
    id: "cse-common-server-module-design-1",
    chapter: "cse-common-server-module-design",
    level: 1,
    question: "自动重连与心跳怎样共享 failure/lifecycle contract，避免重试风暴和误判？",
    answer: "心跳用 sequence、monotonic deadline、连续 miss 与 scheduler lag 区分暂时延迟/失活，并把 disconnect reason 交给重连状态机。transient failure 用 capped exponential backoff + jitter、single pending timer、全局 connect limit和 network/Retry-After 信号；auth/config/cancel 是 permanent/terminal。成功 reset attempt，shutdown取消 timer/in-flight connect，未确认业务写按 idempotency policy处理。",
    tags: ["reconnect", "heartbeat", "backoff", "jitter", "lifecycle"],
  },
  {
    id: "cse-common-server-module-design-2",
    chapter: "cse-common-server-module-design",
    level: 2,
    question: "日志、错误码和监控端口如何组合，才能在 production incident 中既可定位又不泄露或拖垮服务？",
    answer: "结构化日志使用稳定 event/error code、trace/request/connection id 与受限 context，bounded async queue并按 severity/sampling处理满队列；packet只记方向/type/length/hash/脱敏片段，生产逐步降低debug。error code稳定且带retryability/mapping/cause。monitor port与data plane隔离，受ACL/auth/TLS/rate limit保护，live/ready/metrics/debug语义分开，动态提级自动过期并审计/清理。",
    tags: ["structured logging", "error code", "monitoring port", "redaction", "readiness"],
  },
];
