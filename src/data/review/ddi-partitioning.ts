import type { ReviewQuestion } from "./types";

export const ddiPartitioningQuestions: ReviewQuestion[] = [
  {
    id: "ddi-pt-1",
    chapter: "ddi-partitioning",
    level: 2,
    question: "按键范围分区和按键哈希分区各有什么优缺点？",
    answer: "按键范围分区：键排序后按连续范围分配到分区。优点——范围查询友好（同一分区内连续扫描）、可做范围扫描。缺点——热点风险（如时间戳前缀导致所有新写入集中到一个分区）。适用需要范围查询的场景。按键哈希分区：hash(key) mod N分配到分区。优点——分布均匀，减少热点。缺点——范围查询不友好（相邻键可能在不同分区，需Scatter-Gather）、增删节点时大量数据迁移（一致性哈希可缓解）。适用不需要范围查询、要求均匀分布的场景。实践常组合：复合键的第一部分用哈希打散，第二部分用范围排序（如Cassandra）。",
    tags: ["范围分区", "哈希分区", "一致性哈希", "热点", "范围查询"],
  },
  {
    id: "ddi-pt-2",
    chapter: "ddi-partitioning",
    level: 2,
    question: "分区再平衡有哪些策略？各自有什么优缺点？",
    answer: "三种策略：①固定分区数——创建远多于节点的固定分区数，每个节点负责多个分区，增减节点时迁移分区。优点——分区数不变，键到分区的映射稳定；缺点——分区数选难选（太小限制扩展，太大管理开销大）。②动态分区——类似B-Tree分裂合并，分区太大时分裂，太小时合并。优点——自动适应数据分布；缺点——分裂/合并有开销，空表时单分区热点。③按节点比例分区——每个节点固定数量的分区，增减节点时从现有节点迁移。优点——简单；缺点——数据不均时可能不均衡。选择关键：分区迁移时是否影响在线服务（需要平滑迁移）。",
    tags: ["再平衡", "固定分区", "动态分区", "一致性哈希", "分裂合并"],
  },
  {
    id: "ddi-pt-3",
    chapter: "ddi-partitioning",
    level: 2,
    question: "二级索引在分区环境下有哪两种方式？各自的优缺点是什么？",
    answer: "两种方式：①本地索引（文档分区索引，Local Index）——每个分区维护自己的二级索引，只索引该分区的数据。优点——写快（索引更新只涉及本分区，无需跨节点）。缺点——读慢（查询条件不包含分区键时需查所有分区，即Scatter-Gather，增加延迟和负载）。②全局索引（词条分区索引，Global Index）——二级索引独立于主数据分区，按索引值本身分区。优点——读快（查询直接定位到对应索引分区）。缺点——写慢（一次写入需更新多个索引分区，跨节点事务）。实践中通常用本地索引（简单），全局索引需异步维护（最终一致）以避免写延迟。",
    tags: ["二级索引", "本地索引", "全局索引", "Scatter-Gather", "文档分区"],
  },
  {
    id: "ddi-pt-4",
    chapter: "ddi-partitioning",
    level: 2,
    question: "复制和分区如何联合工作？它们各自解决什么问题？",
    answer: "联合方式：每个分区有多个副本（通常一个主副本+若干从副本），主副本处理写请求并复制到从副本，读请求可走主副本或从副本。分区决定数据放在哪个节点（水平拆分解决容量和性能扩展），复制决定数据有几份副本（解决高可用和读扩展）。两者关系：先分区（数据分片），再对每个分区的副本进行复制。请求路由：客户端/路由层需要知道哪个分区在哪个节点（分区映射表），通常用ZooKeeper/etcd等协调服务维护映射。核心认知：分区+复制=大规模分布式存储的两大支柱，分区解决「数据太多放不下」，复制解决「节点会坏」。",
    tags: ["复制", "分区", "联合工作", "请求路由", "主副本"],
  },
];
