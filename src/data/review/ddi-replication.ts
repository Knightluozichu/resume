import type { ReviewQuestion } from "./types";

export const ddiReplicationQuestions: ReviewQuestion[] = [
  {
    id: "ddi-rp-1",
    chapter: "ddi-replication",
    level: 2,
    question: "主从复制、多主复制和无主复制各自的特点和适用场景是什么？",
    answer: "主从复制（单主）：一个主库接收所有写，异步/同步复制到从库。最常见方案，简单可靠，但写单点瓶颈，故障切换复杂。适合读多写少场景。多主复制：多个主库互相同步写入，适用多数据中心、离线协作场景。优势是写可用性和低延迟，难点是写冲突处理（LWW/自定义合并）和复制拓扑管理（环形/星形/全部互联）。无主复制（Dynamo风格）：客户端直写多副本，读时从多副本读并修复。Quorum机制（w+r>n）保证一致性，高可用但最终一致。适合对可用性要求极高、能容忍最终一致性的场景。",
    tags: ["主从复制", "多主复制", "无主复制", "Quorum", "复制拓扑"],
  },
  {
    id: "ddi-rp-2",
    chapter: "ddi-replication",
    level: 2,
    question: "复制滞后会带来哪三类问题？各自对应什么一致性保证？如何缓解？",
    answer: "三类问题：①读己之写（Read Your Writes）——用户写后立即读不到自己的更新，因为读请求落到落后的从库。一致性保证：读写一致性。缓解：从主库读用户自己的数据、用时间戳判断从库是否赶上。②单调读（Monotonic Reads）——用户两次读看到数据「时光倒流」（先看到新数据后看到旧数据），因为读请求落到不同进度的从库。一致性保证：单调读一致性。缓解：同一用户的请求固定路由到同一副本。③一致前缀读（Consistent Prefix Reads）——有因果关系的读操作顺序被打乱（先读到结果后读到原因），因为不同分区的复制进度不同。一致性保证：一致前缀读。缓解：因果相关的写放到同一分区。",
    tags: ["复制滞后", "读写一致性", "单调读", "一致前缀读", "最终一致性"],
  },
  {
    id: "ddi-rp-3",
    chapter: "ddi-replication",
    level: 3,
    question: "Quorum读写的w+r>n意味着什么？为什么它能保证读到最新值？它有什么局限性？",
    answer: "Quorum机制：n个副本中，写需要w个副本确认，读需要r个副本响应。w+r>n保证读写集合有交集——读的r个副本中至少有一个包含最新写入，因此能读到最新值。典型配置：n=3, w=2, r=2。局限性：①Quorum不能保证线性一致性——并发写之间可能冲突，读到的「最新」可能只是某次写，不是全局顺序的最新；②延迟依赖最慢副本——w和r个副本中最慢的决定了延迟；③w或r降低（如r=1）可以提速但牺牲一致性保证；④Quorum只是必要条件不是充分条件——还需要冲突检测和解决策略。Quorum适合Dynamo风格的最终一致性系统，要强一致仍需共识算法。",
    tags: ["Quorum", "w+r>n", "读写交集", "一致性局限", "Dynamo"],
  },
  {
    id: "ddi-rp-4",
    chapter: "ddi-replication",
    level: 2,
    question: "同步复制和异步复制各有什么优缺点？在实际系统中如何选择？",
    answer: "同步复制：主库写后等待从库确认才返回客户端。优点——从库数据一定与主库一致，故障切换不丢数据。缺点——写延迟高（等最慢从库），任一从库故障会阻塞写。实际中通常半同步（只等一个从库）。异步复制：主库写后立即返回，从库后台追赶。优点——写延迟低，从库故障不影响主库。缺点——故障切换可能丢数据（主库宕机时未同步的写入丢失），复制滞后大。选择依据：①金融等不能丢数据的场景用同步/半同步；②对延迟敏感、能容忍少量丢数据的场景用异步；③多数据中心复制通常异步（跨DC同步延迟太高）。核心是数据丢失风险与写入延迟之间的取舍。",
    tags: ["同步复制", "异步复制", "半同步", "故障切换", "数据丢失"],
  },
];
