import type { ReviewQuestion } from "./types";

export const ddiTransactionsQuestions: ReviewQuestion[] = [
  {
    id: "ddi-tx-1",
    chapter: "ddi-transactions",
    level: 2,
    question: `ACID四特性分别是什么？原子性和隔离性在实现上各自意味着什么？`,
    answer: `ACID：①原子性（Atomicity）——事务中的操作要么全部成功要么全部回滚，不存在部分执行。实现：WAL（预写日志）+ 回滚机制，出错时用日志撤销未完成的操作。②一致性（Consistency）——事务执行前后数据满足约束（如外键、唯一性）。一致性是应用层的责任，数据库提供约束检查工具。③隔离性（Isolation）——并发事务互不干扰，像串行执行一样。实现：锁（2PL）或MVCC（多版本并发控制），不同隔离级别对应不同的隔离强度。④持久性（Durability）——事务提交后数据不丢，即使硬件故障。实现：WAL刷盘 + 副本复制。原子性是「出错怎么办」，隔离性是「并发怎么办」。`,
    tags: ["ACID", "原子性", "隔离性", "WAL", "MVCC"],
  },
  {
    id: "ddi-tx-2",
    chapter: "ddi-transactions",
    level: 2,
    question: `读未提交、读已提交、快照隔离和可序列化这四个隔离级别分别防止什么并发问题？`,
    answer: `四个隔离级别（从弱到强）：①读未提交（Read Uncommitted）——无隔离，可能脏读（读到未提交的数据）。②读已提交（Read Committed）——防止脏读和脏写（覆盖未提交的写入），但可能有不可重复读（同一事务两次读结果不同）。实现：行级锁+可见性判断。③快照隔离（Snapshot Isolation, SI）——基于MVCC，每个事务看到一致的快照。防止脏读、不可重复读，但无法防止写倾斜（Write Skew，事务读前提被另一事务改变）。④可序列化（Serializable）——最强隔离，效果等同串行执行。防止所有并发异常包括写倾斜和幻读。实现：实际串行执行、两阶段锁定（2PL）或可序列化快照隔离（SSI）。`,
    tags: ["隔离级别", "读已提交", "快照隔离", "可序列化", "脏读", "写倾斜"],
  },
  {
    id: "ddi-tx-3",
    chapter: "ddi-transactions",
    level: 3,
    question: `什么是写倾斜（Write Skew）？它与幻读有什么区别？快照隔离为什么无法防止写倾斜？`,
    answer: `写倾斜：两个事务各自基于相同的数据前提做决策，但各自写入不同数据，导致合在一起违反约束。经典案例：值班医生——两个医生同时请假，各自查到还有2人值班（满足至少1人值班的约束），各自提交请假，结果无人值班。与幻读的区别：写倾斜涉及的是固定行集合的读前提被改变，幻读涉及查询条件匹配的行集合本身变化（新行被插入或删除）。快照隔离无法防止写倾斜的原因：SI基于MVCC，每个事务看到事务开始时的快照，两个并发事务都看到「2人值班」的快照，各自写入（请假）时没有检测到对方改变了前提——因为它们写的是不同的行，SI不检测这种「读-写依赖」。SSI（可序列化快照隔离）通过追踪事务的读写依赖来检测写倾斜。`,
    tags: ["写倾斜", "幻读", "快照隔离", "SSI", "并发异常"],
  },
  {
    id: "ddi-tx-4",
    chapter: "ddi-transactions",
    level: 2,
    question: `实现可序列化有哪三种方式？各自有什么优缺点？`,
    answer: `三种方式：①实际串行执行——所有事务在一个线程上排队执行，无需锁。优点——简单、无死锁。缺点——无法利用多核、吞吐低、事务必须短小。适用于Redis等内存数据库。②两阶段锁定（2PL）——事务分增长阶段（只加锁）和收缩阶段（只释放锁）。优点——保证可序列化。缺点——性能差（锁竞争大）、可能死锁、并发度低。实践中常用SS2PL（强严格2PL）。③可序列化快照隔离（SSI）——基于SI乐观并发，事务执行时追踪读写依赖（哪些读可能被其他事务的写影响），提交时检测是否存在危险的依赖结构。优点——乐观、无锁等待、性能好于2PL。缺点——需要回滚冲突事务。SSI是现代数据库（如PostgreSQL）的推荐方案，兼顾正确性和性能。`,
    tags: ["可序列化", "2PL", "SSI", "实际串行", "乐观并发"],
  },
];
