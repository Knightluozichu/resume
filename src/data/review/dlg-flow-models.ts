import { ReviewQuestion } from "./types";

export const dlgFlowModelsQuestions: ReviewQuestion[] = [
  {
    id: "dlg-flow-models-1",
    chapter: "dlg-flow-models",
    level: 1,
    question: `什么是标准化流（Normalizing Flow）？它的核心思想是什么？`,
    answer:
      `标准化流是一种显式密度生成模型。核心思想：通过一系列可逆变换将简单分布（如标准高斯 N(0,I)）逐步变换为复杂的数据分布 P_data(x)。正向（编码）：数据 x 经过 K 个可逆变换 f_1, f_2, ..., f_K 映射到隐变量 z（z 服从简单分布）。反向（生成）：从简单分布采样 z，经过逆变换 f_K^(-1), ..., f_1^(-1) 生成数据 x。关键优势：通过变量替换定理可以精确计算数据的对数似然 log P(x) = log P(z) + sum log|det(df_i/dz_i)|，不像 VAE 只能用下界近似。这意味着流模型可以直接用最大似然训练，且有精确的概率密度评估。代表模型：NICE（加性耦合层）、RealNVP（仿射耦合层）、Glow（可逆 1x1 卷积）。`,
    tags: ["标准化流", "可逆变换", "精确似然"],
  },
  {
    id: "dlg-flow-models-2",
    chapter: "dlg-flow-models",
    level: 2,
    question: `流模型的变量替换定理是什么？为什么需要雅可比行列式？`,
    answer:
      `变量替换定理：如果 z = f(x) 是可逆变换，则 x 的概率密度可以通过 z 的密度表示：P(x) = P(z) * |det(df/dx)|，其中 |det(df/dx)| 是变换 f 的雅可比矩阵的行列式绝对值。取对数：log P(x) = log P(z) + log|det(df/dx)|。对于 K 个变换的链：log P(x) = log P(z_K) + sum_{i=1}^{K} log|det(df_i/dz_{i-1})|。为什么需要雅可比行列式：概率密度在变换时会「拉伸」或「压缩」——雅可比行列式度量了变换对概率密度的体积变化率。如果变换将空间放大 2 倍，密度需要除以 2（概率总量归一化）。行列式正是多维空间中体积变化率的度量。如果雅可比是三角矩阵（如耦合层设计），行列式等于对角元素之积，计算复杂度从 O(D^3) 降为 O(D)，使高维数据可行。`,
    tags: ["变量替换", "雅可比行列式", "概率密度"],
  },
  {
    id: "dlg-flow-models-3",
    chapter: "dlg-flow-models",
    level: 2,
    question: `流模型的耦合层（Coupling Layer）是如何设计的？为什么它能让雅可比行列式易于计算？`,
    answer:
      `耦合层设计（以 RealNVP 仿射耦合为例）：将输入向量 x 分为两部分 x = (x_a, x_b)。①x_a 直接复制到输出：y_a = x_a。②x_b 通过由 x_a 参数化的仿射变换：y_b = x_b * exp(s(x_a)) + t(x_a)，其中 s 和 t 是神经网络。这样设计的优势：①可逆——给定 y 可以精确求逆：x_a = y_a，x_b = (y_b - t(y_a)) * exp(-s(y_a))。②雅可比是三角矩阵——因为 y_a = x_a（对角为 1），y_b 只依赖 x_b（对角为 exp(s(x_a))），雅可比矩阵是下三角，行列式 = prod exp(s(x_a)) = exp(sum s(x_a))，计算复杂度 O(D)。③多层堆叠——单层耦合中 x_a 不变，通过多层交替分割不同维度（如奇偶交替），确保所有维度都被变换。NICE 是加性耦合（y_b = x_b + t(x_a)），RealNVP 增加了缩放（exp(s)），Glow 进一步引入可逆 1x1 卷积处理通道维度。`,
    tags: ["耦合层", "RealNVP", "三角雅可比"],
  },
  {
    id: "dlg-flow-models-4",
    chapter: "dlg-flow-models",
    level: 3,
    question: `流模型与 VAE、GAN 相比有什么优劣？为什么流模型在实践中不如扩散模型流行？`,
    answer:
      `流模型的优势：①精确似然——可以精确计算 P(x)，不像 VAE 用 ELBO 下界，GAN 完全没有密度估计。②双向映射——编码（x→z）和解码（z→x）都精确可逆，适合异常检测、数据压缩等需要精确编码的场景。③训练稳定——直接最大似然训练，无对抗博弈。流模型的劣势：①架构约束——变换必须可逆且维度保持，限制了网络设计自由度（不能任意使用 ReLU、池化等不可逆操作）。②表达能力受限——可逆约束使模型难以学习非常复杂的分布，生成质量通常不如 GAN 和扩散模型。③内存开销大——可逆网络需要在反向传播中重建中间激活值，内存消耗较高。流模型不如扩散模型流行的原因：扩散模型虽然采样慢，但生成质量远超流模型，且架构设计自由（任何网络都可作为去噪网络），无需可逆约束。流模型更适合需要精确概率密度的场景（如异常检测、似然评估），而非纯生成任务。`,
    tags: ["流模型对比", "优劣分析", "实践选择"],
  },
];
