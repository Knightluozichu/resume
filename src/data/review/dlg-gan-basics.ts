import { ReviewQuestion } from "../types";

export const dlgGanBasicsQuestions: ReviewQuestion[] = [
  {
    id: "dlg-gan-basics-1",
    chapter: "dlg-gan-basics",
    level: 1,
    question: "生成对抗网络（GAN）的基本原理是什么？生成器和判别器各自的作用是什么？",
    answer:
      "GAN 由生成器 G 和判别器 D 两个网络组成，通过对抗博弈训练。生成器 G：输入随机噪声 z ~ N(0,I)，输出假样本 G(z)，目标是生成足够逼真的样本欺骗判别器。判别器 D：输入样本（真实数据 x 或生成数据 G(z)），输出该样本为真实的概率 D(x)，目标是正确区分真实样本和生成样本。对抗博弈：G 试图最小化 D 的判别能力（让 D(G(z)) 接近 1），D 试图最大化判别能力（让 D(x) 接近 1，D(G(z)) 接近 0）。形式化为极小极大博弈：min_G max_D E[log D(x)] + E[log(1 - D(G(z)))]。训练时交替优化 D 和 G，当达到纳什均衡时，G 生成的样本分布等于真实数据分布，D 无法区分（输出 0.5）。GAN 是隐式密度模型——不显式建模 P(x)，而是通过对抗训练让生成分布逼近数据分布。",
    tags: ["GAN", "生成器", "判别器", "对抗博弈"],
  },
  {
    id: "dlg-gan-basics-2",
    chapter: "dlg-gan-basics",
    level: 2,
    question: "GAN 的损失函数是什么？为什么生成器不直接最小化 log(1-D(G(z)))？",
    answer:
      "GAN 的原始损失函数：min_G max_D V(D,G) = E_{x~P_data}[log D(x)] + E_{z~P_z}[log(1 - D(G(z)))]。判别器 D 最大化 V（正确分类），生成器 G 最小化 V（欺骗 D）。生成器不直接最小化 log(1 - D(G(z))) 的原因：在训练初期，生成器 G 很差，D 很容易区分真假，D(G(z)) 接近 0，此时 log(1 - D(G(z))) 接近 log(1) = 0，梯度几乎为零，G 无法有效学习（梯度消失问题）。解决方案（非饱和 GAN 损失）：生成器改为最大化 log(D(G(z)))。当 D(G(z)) 接近 0 时，log(D(G(z))) 趋向负无穷，梯度很大，G 能快速学习。这个改动在理论目标上等价（都是让 D(G(z)) 增大），但在实践中提供了更强的梯度信号，解决了训练初期的梯度消失问题。",
    tags: ["GAN损失", "梯度消失", "非饱和损失"],
  },
  {
    id: "dlg-gan-basics-3",
    chapter: "dlg-gan-basics",
    level: 2,
    question: "GAN 训练中的模式崩溃（Mode Collapse）是什么？如何缓解？",
    answer:
      "模式崩溃：生成器只学会生成少数几种样本（甚至只生成一种），缺乏多样性。判别器无法惩罚这种「偷懒」行为——因为少量逼真样本就能骗过 D。原因：生成器的梯度只告诉它「如何让当前样本更逼真」，而非「如何覆盖更多模式」。生成器找到判别器的「弱点」后不断利用，忽略了数据分布的其他模式。缓解方法：①WGAN——用 Wasserstein 距离替代 JS 散度，提供更平滑的梯度，缓解模式崩溃和训练不稳定。②Minibatch discrimination——判别器同时看一批样本，检测生成样本的多样性。③Unrolled GAN——生成器考虑判别器未来几步的更新，避免「钻牛角尖」。④经验回放——保留历史生成器，增加判别器看到的样本多样性。⑤标签平滑——用 0.9 替代 1 作为真实标签，防止判别器过自信。模式崩溃是 GAN 最棘手的问题之一，这也是扩散模型逐渐取代 GAN 的重要原因。",
    tags: ["模式崩溃", "WGAN", "训练不稳定"],
  },
  {
    id: "dlg-gan-basics-4",
    chapter: "dlg-gan-basics",
    level: 3,
    question: "GAN 的训练为什么不稳定？与 VAE 和扩散模型相比，GAN 的训练有什么本质困难？",
    answer:
      "GAN 训练不稳定的本质原因：①博弈论困境——GAN 是两人零和博弈，需要找到纳什均衡。但梯度下降只保证找到局部最优，不保证找到博弈均衡。D 和 G 的交替更新可能陷入振荡或发散。②梯度问题——如果 D 太强（D(G(z)) 约等于 0），G 的梯度消失；如果 D 太弱，G 的梯度无意义（D 无法提供有用的反馈）。需要保持 D 和 G 的能力平衡。③JS 散度缺陷——当真实分布和生成分布不重叠时，JS 散度为常数 log2，梯度为零，G 无法学习。这在高维空间中经常发生。④评估困难——没有像 VAE 的 ELBO 或扩散模型的去噪损失那样的明确训练目标，难以判断训练是否在正确进行。与 VAE 和扩散模型对比：VAE 优化明确的 ELBO 下界（变分推断），训练稳定但生成模糊。扩散模型优化明确的去噪 MSE 损失，训练稳定且质量高。GAN 用对抗博弈替代显式损失函数，虽然能生成清晰图像，但训练本质上是博弈问题而非优化问题，缺乏收敛保证。这是扩散模型取代 GAN 成为主流的根本原因。",
    tags: ["GAN训练", "训练不稳定", "博弈论", "模型对比"],
  },
];
