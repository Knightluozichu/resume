import { ReviewQuestion } from "../types";

export const dltConvRnnQuestions: ReviewQuestion[] = [
  {
    id: "dlt-conv-rnn-1",
    chapter: "dlt-conv-rnn",
    level: 2,
    question: "CNN 的三个核心思想是什么？它们为什么使 CNN 比全连接网络更适合处理图像？",
    answer:
      "CNN 的三个核心思想：①局部连接——每个输出只与输入的局部区域（感受野）相连，匹配图像的局部相关性。②参数共享——同一卷积核在图像所有位置共享权重，实现平移等变（特征出现在任意位置都能检测到）。③层次化特征——多层卷积堆叠使浅层捕获简单模式（边缘），深层组合出复杂语义（物体）。比全连接网络更适合处理图像的原因：①参数量大幅减少——全连接层需要 H*W*C_in*C_out 个参数，卷积层只需 k*k*C_in*C_out（如 3x3 卷积少 100 倍以上），减少过拟合和计算量。②利用空间结构——图像具有平移不变性和局部性，CNN 的归纳偏置匹配这些结构。③层次化表示——多层卷积自然构建从低级到高级的特征层次，符合视觉系统的层次结构。",
    tags: ["CNN", "局部连接", "参数共享", "平移等变", "层次化特征"],
  },
  {
    id: "dlt-conv-rnn-2",
    chapter: "dlt-conv-rnn",
    level: 3,
    question: "RNN 的梯度消失问题是什么？LSTM 如何解决？",
    answer:
      "RNN 梯度消失问题：通过时间反向传播（BPTT）时，梯度需连乘 W_hh^T（T 是序列长度）。当 W_hh 特征值小于 1 时梯度指数衰减（梯度消失），长序列中浅层时间步几乎不更新，无法建模长程依赖。当特征值大于 1 时梯度爆炸。根本原因是 RNN 的隐藏状态更新用乘法 h(t) = tanh(W_hh*h(t-1) + ...)，梯度通过 tanh 和 W_hh 连乘回传。LSTM 的解决方案：引入细胞状态 c(t)，其更新用加法而非乘法——c(t) = f(t)*c(t-1) + i(t)*g(t)。梯度对 c(t-1) 的偏导 d(c(t))/d(c(t-1)) = f(t)，当遗忘门 f(t) 接近 1 时梯度几乎无损通过（加法通路），不受 tanh 连乘衰减。三个门：遗忘门 f（保留旧信息）、输入门 i（写入新信息）、输出门 o（输出多少）。LSTM 通过门控让网络学习何时记忆、何时遗忘。",
    tags: ["RNN", "梯度消失", "LSTM", "门控机制", "BPTT"],
  },
  {
    id: "dlt-conv-rnn-3",
    chapter: "dlt-conv-rnn",
    level: 3,
    question: "残差连接的作用是什么？它为什么能帮助训练超深网络？",
    answer:
      "残差连接的公式 h^(l+1) = F(h^(l)) + h^(l)，将输入直接加到输出上。作用：①缓解梯度消失——梯度 d(h^(l+1))/d(h^(l)) = dF/dh + 1，即使 dF/dh 趋于零梯度至少为 1（通过加法通路），保证深层网络梯度能有效回传。②使恒等映射容易学习——当最优映射接近恒等映射时只需让 F(h) 趋于零，网络学习残差 F(h) = h^(l+1) - h^(l) 而非完整映射，学习更容易。③平滑损失曲面——残差连接使损失曲面更平滑，减少鞍点和尖锐区域。帮助训练超深网络的原因：没有残差连接超过 30 层的网络梯度严重消失几乎无法训练。残差连接通过加法通路保证梯度至少为 1，使 152 层的 ResNet 甚至上千层网络都能有效训练。残差连接是 ResNet、Transformer、扩散模型 U-Net 的标准组件。",
    tags: ["残差连接", "梯度消失", "ResNet", "超深网络", "恒等映射"],
  },
  {
    id: "dlt-conv-rnn-4",
    chapter: "dlt-conv-rnn",
    level: 4,
    question: "CNN 和 RNN 各自适合什么类型的数据？它们的设计哲学有什么共同点？",
    answer:
      "CNN 适合网格数据（图像、音频频谱图、棋盘状态），具有空间局部性和平移不变性。RNN 适合序列数据（文本、语音、时间序列），具有时序依赖性。设计哲学的共同点：①参数共享——CNN 在所有空间位置共享卷积核权重，RNN 在所有时间步共享转移权重。使模型能用有限参数处理任意大小/长度的输入，在数据不同位置/时刻复用学到的模式。②引入领域先验——CNN 假设「局部相关+平移不变」，RNN 假设「时序依赖+时间不变」，这些归纳偏置使模型用更少数据学到更好的特征。③层次化/递归结构——CNN 通过多层堆叠构建从局部到全局的特征层次，RNN 通过时间步递归累积从短期到长期的信息。两者都将「全连接」这种无结构的通用模型替换为「有结构的专用模型」，用领域知识换取参数效率和性能。",
    tags: ["CNN", "RNN", "参数共享", "归纳偏置", "设计哲学"],
  },
];
