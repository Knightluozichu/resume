import { ReviewQuestion } from "../types";

export const dnaRnnLstmQuestions: ReviewQuestion[] = [
  {
    id: "dna-rnn-lstm-1",
    chapter: "dna-rnn-lstm",
    level: 1,
    question: "RNN 的基本结构是什么？它如何处理变长序列？",
    answer:
      "RNN（循环神经网络）基本结构：在每个时间步 t，RNN 接收当前输入 x_t 和上一时间步的隐状态 h_{t-1}，计算当前隐状态 h_t = tanh(W_x · x_t + W_h · h_{t-1} + b)。隐状态 h_t 是对「截至当前时间步所有历史信息」的压缩表示。RNN 处理变长序列的方式：权重矩阵 W_x 和 W_h 在所有时间步共享（参数共享），无论输入序列多长，模型参数量不变。处理时将序列逐个时间步输入，每步更新隐状态。输出可以是每步的 h_t（用于序列标注）或最后一步的 h_N（用于序列分类）。时间展开：RNN 在时间维度上展开后等价于一个深度网络，不同层共享同一组权重，反向传播沿时间维度进行（BPTT，Backpropagation Through Time）。",
    tags: ["RNN", "隐状态", "参数共享", "变长序列"],
  },
  {
    id: "dna-rnn-lstm-2",
    chapter: "dna-rnn-lstm",
    level: 2,
    question: "RNN 的梯度消失/爆炸问题是什么？为什么会发生？",
    answer:
      "RNN 梯度消失/爆炸问题：在长序列上反向传播时，梯度会沿时间维度反复乘以权重矩阵 W_h，导致梯度要么趋近于0（消失），要么趋向无穷（爆炸），使 RNN 无法学习长距离依赖。发生原因：反向传播时，梯度链式法则要求将各时间步的雅可比矩阵连乘：梯度 = ∏ (∂h_t/∂h_{t-1})。而 ∂h_t/∂h_{t-1} 包含 W_h 和 tanh 的导数。当 W_h 的特征值小于1时，连乘导致梯度指数衰减→消失；当 W_h 的特征值大于1时，连乘导致梯度指数增长→爆炸。tanh 的导数最大值为1（在输入为0时），大部分情况下小于1，进一步加剧梯度消失。结果：RNN 只能记住近期信息（约10-20步），远距离的依赖关系丢失。梯度爆炸可通过梯度裁剪（gradient clipping）缓解，但梯度消失更难处理，需要 LSTM 等门控结构。",
    tags: ["梯度消失", "梯度爆炸", "BPTT", "长距离依赖"],
  },
  {
    id: "dna-rnn-lstm-3",
    chapter: "dna-rnn-lstm",
    level: 2,
    question: "LSTM 的三个门和细胞状态各自的作用是什么？它如何解决梯度消失？",
    answer:
      "LSTM 的三个门和细胞状态：①遗忘门 f_t = σ(W_f·[h_{t-1}, x_t])——决定从细胞状态 C_{t-1} 中丢弃多少信息（0全忘，1全留）。②输入门 i_t = σ(W_i·[h_{t-1}, x_t])——决定将多少新信息写入细胞状态。③输出门 o_t = σ(W_o·[h_{t-1}, x_t])——决定从更新后的细胞状态中输出多少到隐状态。候选值 g_t = tanh(W_g·[h_{t-1}, x_t])——生成候选新信息。细胞状态更新：C_t = f_t * C_{t-1} + i_t * g_t，隐状态 h_t = o_t * tanh(C_t)。解决梯度消失的原理：细胞状态 C 的更新是加法操作（C_t = f_t * C_{t-1} + ...）而非乘法替换，梯度可以从 C_t 直接流回 C_{t-1}（通过 f_t），只要遗忘门 f_t 接近1，梯度就能无衰减地传播很长距离。这被称为「梯度高速公路」——细胞状态提供了一条梯度可以直接流动的路径，绕过了反复乘以 W_h 的问题。",
    tags: ["LSTM", "门控", "细胞状态", "梯度消失"],
  },
  {
    id: "dna-rnn-lstm-4",
    chapter: "dna-rnn-lstm",
    level: 3,
    question: "LSTM 与普通 RNN 在结构上的核心区别是什么？为什么 LSTM 在实际中几乎总是优于普通 RNN？",
    answer:
      "核心区别：①状态分裂——RNN 只有一个隐状态 h_t，既是记忆又是输出；LSTM 分裂为细胞状态 C_t（长期记忆）和隐状态 h_t（短期输出），职责分离。②更新方式——RNN 的 h_t 完全被新输入覆盖（h_t = tanh(...)，是「替换」式更新）；LSTM 的 C_t 是加法更新（C_t = f_t*C_{t-1} + i_t*g_t，是「累加」式更新），旧信息不会被完全覆盖。③门控机制——RNN 无门控，信息流不受控制；LSTM 有三个门精确控制信息的遗忘、写入和输出。④梯度路径——RNN 梯度必须经过 W_h 和 tanh 反复相乘；LSTM 梯度可通过细胞状态的加法路径直接流动。LSTM 几乎总是优于 RNN 的原因：梯度高速公路解决了长距离依赖问题，门控机制让模型学会「什么时候记住、什么时候忘记」，在机器翻译、文本生成等需要长程上下文的任务上表现远超 RNN。RNN 仅在极短序列或计算资源极度受限时才有优势。",
    tags: ["LSTM", "RNN", "门控", "结构对比"],
  },
];
