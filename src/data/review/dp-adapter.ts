import type { ReviewQuestion } from "./types";

/** 适配器模式章复习题 */
export const dpAdapterQuestions: ReviewQuestion[] = [
  {
    id: "dp-adapter-01",
    chapter: "dp-adapter",
    level: 1,
    question: `适配器模式的意图是什么？`,
    answer: `适配器模式将一个类的接口转换成客户端期望的另一个接口，使得原本因接口不兼容而无法一起工作的类可以协同工作。\n\n核心意图：让接口不兼容的类能互相合作。适配器作为中间层，包装一个已有对象，把它对外暴露的接口「翻译」成调用方期望的接口。调用方无需改自己的代码，被适配的类也无需改动，两者通过适配器间接对接。`,
    tags: ["意图", "基础概念"],
  },
  {
    id: "dp-adapter-02",
    chapter: "dp-adapter",
    level: 2,
    question: `类适配器和对象适配器有什么区别？工程中首选哪一种？`,
    answer: `类适配器：通过「多重继承」同时继承目标接口和被适配类（如 Java 中实现 \`Target\` 接口 + 继承 \`Adaptee\`）。优点是可以重写 \`Adaptee\` 的行为；缺点是受限于单继承语言（Java 不能同时继承两个类），且与被适配类是静态绑定，适配其子类不便。\n\n对象适配器：通过「组合」持有被适配类的实例，实现目标接口，把方法调用委托给被适配对象。优点是更灵活——可以适配 \`Adaptee\` 及其子类，不依赖具体类型，符合「组合优于继承」；缺点是无法重写被适配类的方法。\n\n工程首选：对象适配器。它耦合更低、适应面更广，绝大多数语言和实际项目都采用对象适配器；类适配器仅在需要重写被适配类行为且语言支持多继承（如 C++）时才考虑。`,
    tags: ["类适配器", "对象适配器", "对比"],
  },
  {
    id: "dp-adapter-03",
    chapter: "dp-adapter",
    level: 3,
    question: `旧版日志组件接口 \`writeLog(msg)\` 与新系统期望的 \`log(level, message)\` 不兼容，如何用适配器模式解决？`,
    answer: `1. 定义目标接口 \`LogTarget\`，声明 \`log(level: string, message: string)\`，这是新系统面向的抽象。\n2. 旧组件 \`OldLogger\` 只有 \`writeLog(msg: string)\`，不能改它的源码（可能是第三方或历史包袱）。\n3. 创建适配器 \`LoggerAdapter implements LogTarget\`，构造时注入一个 \`OldLogger\` 实例。\n4. 在 \`log(level, message)\` 内部做格式转换后委托：\`oldLogger.writeLog(\`[\${level}] \${message}\`)\`。\n5. 新系统只面向 \`LogTarget\` 编程，传入 \`new LoggerAdapter(oldLogger)\` 即可复用旧组件，无需改老代码也无需改新系统。\n\n关键点：适配器只做「接口翻译」，不增不减业务能力。如果未来旧组件被替换，只需换一个新适配器，新系统一行都不用改——这就是适配器对「接口变化」的隔离价值。`,
    tags: ["应用", "日志组件", "接口转换"],
  },
  {
    id: "dp-adapter-04",
    chapter: "dp-adapter",
    level: 4,
    question: `适配器模式、代理模式、装饰器模式三者都「包了一层」，本质区别是什么？`,
    answer: `三者结构上都持有被包装对象的引用，但意图完全不同：\n\n- 适配器：目的是「转换接口」，让不兼容的接口变得兼容。被包装对象的接口与目标接口不同，适配器做翻译。解决的是「接口不匹配」。\n- 代理：目的是「控制访问」，代理与被代理对象实现同一接口，在其中加入延迟加载、权限校验、远程调用、缓存等控制逻辑，调用方通常无感知。解决的是「访问管控」。\n- 装饰器：目的是「增强行为」，装饰器与被装饰对象实现同一接口，在不改变接口的前提下动态叠加职责，调用方可主动组装装饰链。解决的是「功能扩展」。\n\n判断维度：\n① 接口是否相同：适配器的两端接口不同，代理和装饰器两端接口相同。\n② 意图：适配器改接口、代理加控制、装饰器加功能。\n③ 对象生命周期：代理常自己决定何时创建真实对象，装饰器必须接收已存在的被装饰对象，适配器在构造时即持有被适配对象。\n\n一句话区分：适配器让旧的能用、代理管能不能用、装饰器让用得更强。`,
    tags: ["对比", "适配器", "代理", "装饰器"],
  },
];
