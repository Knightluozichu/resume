import type { ReviewQuestion } from "./types";

/** 享元模式章复习题 */
export const dpFlyweightQuestions: ReviewQuestion[] = [
  {
    id: "dp-flyweight-01",
    chapter: "dp-flyweight",
    level: 1,
    question: `享元模式的意图是什么？`,
    answer: `享元模式通过共享对象来最大限度地减少内存使用，适合大量细粒度对象的场景。\n\n核心意图：把对象的状态分成「可共享的内部状态」和「不可共享的外部状态」，内部状态相同的对象复用同一个享元实例，外部状态由调用方在使用时传入。从而用少量共享对象支撑海量逻辑实例，避免每个对象都重复存储相同数据。`,
    tags: ["意图", "基础概念", "内存优化"],
  },
  {
    id: "dp-flyweight-02",
    chapter: "dp-flyweight",
    level: 2,
    question: `享元模式中「内部状态」和「外部状态」的区别是什么？为什么要这样拆分？`,
    answer: `- 内部状态（intrinsic）：存储在享元对象内部、不随环境变化、可被所有调用方共享的部分。例如棋子的「颜色/形状」、树的「种类/纹理」、字符的「字形位图」。它独立于使用场景，是对象「是什么」的固有属性。\n- 外部状态（extrinsic）：随对象实例、随上下文变化、不可共享的部分，由调用方在调用时传入，不存储在享元里。例如每棵树的「坐标 (x,y,z)」、每个棋子的「在棋盘上的位置」。\n\n为什么要拆分：享元对象只持有内部状态，外部状态作为方法参数临时传入。这样「纹理相同的 1 万棵树」可以共享同一个享元实例——内部状态（纹理）只存一份，外部状态（坐标）各存各的。拆分是实现「共享」的前提：只有把可共享的部分剥离出来，才能让不同逻辑实例复用同一对象，从而把内存从 O(实例数) 降到 O(种类数)。`,
    tags: ["内部状态", "外部状态", "共享"],
  },
  {
    id: "dp-flyweight-03",
    chapter: "dp-flyweight",
    level: 3,
    question: `游戏场景：1 万棵树，每棵有坐标和纹理，如何用享元模式省内存？`,
    answer: `不优化的问题：每棵树一个对象，内含自己的纹理数据（可能几 MB），1 万棵 = 1 万份重复纹理，内存爆炸。\n\n用享元设计：\n1. 定义享元 \`TreeType\`，持有内部状态：\`name\`、\`color\`、\`texture\`（纹理数据，体积大、可共享）。它只存「树这种东西长什么样」。\n2. 定义上下文 \`Tree\`，持有外部状态：\`x, y, z\` 坐标 + 一个对 \`TreeType\` 的引用。它只存「这棵树长在哪」。\n3. 用 \`TreeFactory\` 维护享元池 \`Map<name, TreeType>\`：\`getTreeType(name, color, texture)\` 时，若池中已有同名 \`TreeType\` 就复用，没有才新建并放入池中。\n4. 渲染时：\`tree.draw()\` 把自己的坐标传给 \`treeType.render(x, y, z)\`，享元用共享的纹理 + 传入的坐标画出这棵树。\n\n效果：1 万棵 \`Tree\` 各自只存 3 个坐标（几十字节），纹理数据只按树种数量存几份。内存从「1 万 × 纹理」降到「树种数 × 纹理 + 1 万 × 坐标」，节省可达几个数量级。`,
    tags: ["应用", "游戏", "享元池", "内存优化"],
  },
  {
    id: "dp-flyweight-04",
    chapter: "dp-flyweight",
    level: 4,
    question: `享元模式和单例模式都涉及「对象共享」，它们有什么本质区别？`,
    answer: `相似点：都涉及「对象复用」「共享」，都是结构型/创建型的对象管理手段。\n\n本质区别：\n- 目的不同：单例是「保证全系统只有一个实例」并提供全局访问点；享元是「让内部状态相同的多个逻辑实例共享同一个对象」。\n- 实例数量不同：单例全局唯一 1 个；享元可以有多个，每种内部状态对应一个享元实例，享元池里常驻多个对象。\n- 状态不同：单例通常带全局可变状态，所有调用方共享同一份状态；享元对象本身尽量无状态或只有不可变的内部状态，外部状态由调用方传入、绝不存储在享元内。\n- 关注点不同：单例关注「唯一访问点」和「全局唯一」；享元关注「内存优化」和「对象复用率」。\n- 生命周期不同：单例一旦创建通常活到进程结束；享元可按需创建、按需缓存，甚至用弱引用在内存紧张时回收。\n\n一句话：单例是「一个对象服务所有人」，享元是「按状态分类，同类共享一个」。`,
    tags: ["对比", "享元", "单例"],
  },
];
