import type { ReviewQuestion } from "./types";

/** 中介者模式章复习题 */
export const dpMediatorQuestions: ReviewQuestion[] = [
  {
    id: "dp-mediator-01",
    chapter: "dp-mediator",
    level: 1,
    question: "中介者模式的意图是什么？",
    answer: "中介者模式用一个中介对象来封装一系列对象之间的交互，使各对象之间不需要显式地相互引用，从而使其耦合松散，而且可以独立地改变它们之间的交互。\n\n核心意图：把「对象之间的多对多网状交互」收敛到「中介者一个对象」上。各个同事（Colleague）对象不再直接互相调用，而是通过中介者转发消息。这样任何一个同事的变化只需要通知中介者，不需要知道其他同事的存在，对象间耦合从网状降为星型。",
    tags: ["意图", "基础概念", "解耦"],
  },
  {
    id: "dp-mediator-02",
    chapter: "dp-mediator",
    level: 2,
    question: "中介者如何将「多对多」通信变为「星型」通信？",
    answer: "没有中介者时，N 个对象互相通信，任意两个对象之间都可能有直接引用，形成 N(N-1)/2 条边的网状耦合——每新增一个对象都要和已有所有对象建立连接，改一个对象的接口会波及所有相关对象。\n\n引入中介者后：\n- 每个同事对象只持有中介者的引用，不持有其他同事的引用。\n- 同事 A 想和同事 B 通信时，A 调用 `mediator.notify(event, data)`，中介者根据事件类型决定调用哪些同事的方法。\n- 所有交互逻辑集中在中介者内部，同事之间不再直接认识。\n\n拓扑结构从「网状」变成「星型」：N 个同事都连向中心的中介者，边数从 N(N-1)/2 降为 N。同事可独立变化（只需和中介者约定事件协议），中介者集中编排交互。代价是交互复杂度全部转移到中介者身上。",
    tags: ["星型通信", "网状耦合", "结构"],
  },
  {
    id: "dp-mediator-03",
    chapter: "dp-mediator",
    level: 3,
    question: "聊天室场景：用户之间通过聊天室中转消息，用中介者模式设计。",
    answer: "1. 定义中介者接口 `ChatMediator`，声明 `sendMessage(msg, user)` 和 `registerUser(user)`。\n2. 实现具体中介者 `ChatRoom implements ChatMediator`：内部维护用户列表 `users: User[]`；`sendMessage()` 遍历所有用户，把消息投递给除发送者外的所有人（调用 `user.receive(msg)`）。\n3. 定义同事类 `User`：持有 `name` 和 `mediator` 引用。\n- `send(msg)`：调用 `mediator.sendMessage(msg, this)`，自己不直接联系其他用户。\n- `receive(msg)`：显示收到的消息。\n4. 客户端：创建 `ChatRoom`，注册若干 `User`，任意用户调 `send()` 即可广播。\n\n效果：用户之间互不引用，新增用户只需注册到聊天室，删除用户只需移除注册，都不影响其他用户。聊天室集中控制消息路由（可扩展为私聊、群发、过滤、审计）。如果改成直接互调，每加一个用户就要让所有已有用户知道它，耦合会爆炸。",
    tags: ["应用", "聊天室", "消息中转"],
  },
  {
    id: "dp-mediator-04",
    chapter: "dp-mediator",
    level: 4,
    question: "中介者模式的缺点是什么？为什么中介者本身可能变成上帝对象？",
    answer: "中介者把所有交互逻辑集中到自己身上，当同事对象多、交互规则复杂时，中介者会膨胀成一个「上帝对象」：\n\n1. 体积庞大：知道所有同事的所有事件和所有处理逻辑，方法众多、代码巨长，难以维护。\n2. 过度集中：任何交互的改动都要改中介者，中介者成了整个系统的修改热点，违背了「单一职责」——它既管路由又管业务规则。\n3. 难以扩展：新增一种交互往往要在中介者里加一堆 `if-else` 或 `switch`，中介者和所有同事耦合，违反开闭原则。\n4. 单点风险：中介者一旦出 bug，所有交互都受影响；中介者也可能成为性能瓶颈。\n\n应对：\n- 按职责拆分多个中介者（如「事件中介者」「状态中介者」各管一摊），避免一个中介者包揽一切。\n- 用事件总线/消息队列替代集中式中介者，把路由逻辑从代码硬编码变为订阅关系，更松耦合。\n- 控制中介者的职责边界，只做「转发与协调」，业务逻辑尽量留在同事内部。\n\n一句话：中介者用「集中」换「解耦」，但集中过度就会走向反面——从解放者变成新的上帝对象。",
    tags: ["缺点", "上帝对象", "工程实践"],
  },
];
