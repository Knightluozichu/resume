import type { ReviewQuestion } from "./types";

/** 代理模式章复习题 */
export const dpProxyQuestions: ReviewQuestion[] = [
  {
    id: "dp-proxy-01",
    chapter: "dp-proxy",
    level: 1,
    question: `代理模式的意图是什么？`,
    answer: `代理模式为其他对象提供一种代理以控制对这个对象的访问。\n\n核心意图：在客户端和真实对象之间加一层代理，代理与真实对象实现同一接口，代理可以在访问前后加入额外控制（延迟加载、权限校验、远程调用、缓存、日志等）。客户端通常感知不到自己用的是代理，对外行为与直接使用真实对象一致。`,
    tags: ["意图", "基础概念", "访问控制"],
  },
  {
    id: "dp-proxy-02",
    chapter: "dp-proxy",
    level: 2,
    question: `虚代理、远程代理、保护代理分别是什么？三者的区别在哪？`,
    answer: `- 虚代理（Virtual Proxy）：延迟创建/加载开销大的真实对象，直到真正需要时才实例化。典型：图片懒加载、大文档按需加载、ORM 延迟加载关联对象。控制的是「何时创建」。\n- 远程代理（Remote Proxy）：为位于不同地址空间（不同进程/机器）的对象提供本地代表，代理负责网络通信、序列化、结果反序列化，调用方像调用本地对象一样调用远程对象。典型：RPC Stub、Web Service 客户端。控制的是「在哪访问」。\n- 保护代理（Protection Proxy）：在访问前做权限校验，根据调用者身份/角色决定是否允许访问真实对象的某个方法。典型：权限控制、访问审计、读写分离。控制的是「谁能访问」。\n\n三者结构完全相同（都包一层、实现同一接口），区别仅在于「加的控制逻辑」不同：虚代理管创建时机、远程代理管通信位置、保护代理管访问权限。`,
    tags: ["虚代理", "远程代理", "保护代理", "对比"],
  },
  {
    id: "dp-proxy-03",
    chapter: "dp-proxy",
    level: 3,
    question: `图片懒加载场景：如何用虚代理实现图片延迟加载？`,
    answer: `1. 定义接口 \`Image\`，声明 \`display()\`。\n2. 真实对象 \`RealImage implements Image\`：构造时从磁盘加载大图（耗时 IO），\`display()\` 绘制图片。直接用它在初始化阶段加载大量图片会卡顿。\n3. 代理 \`ImageProxy implements Image\`：\n- 构造时只记录文件 \`path\`，不加载图片，持有 \`realImage: RealImage | null\`。\n- \`display()\` 时：若 \`realImage\` 为空，才 \`new RealImage(path)\` 触发真正的磁盘加载并缓存引用；随后调用 \`realImage.display()\`。后续再次 \`display()\` 直接复用已加载的真实对象，不再重复 IO。\n4. 调用方持有 \`ImageProxy\`，在图片真正要显示之前不会触发任何磁盘 IO，避免一次性加载大量图片导致卡顿。\n\n关键点：代理与真实对象同接口，调用方无感知，可透明替换；「延迟实例化 + 首次访问时加载 + 缓存复用」是虚代理的核心模式。进一步还可扩展为「占位图先显示、后台加载完成后切换」的体验优化。`,
    tags: ["应用", "虚代理", "懒加载"],
  },
  {
    id: "dp-proxy-04",
    chapter: "dp-proxy",
    level: 4,
    question: `代理模式和装饰器模式结构几乎一样（都同接口、都持有被包装对象），意图有什么不同？如何区分？`,
    answer: `结构上几乎一样：都实现同一接口、都持有一个被包装对象的引用、都可在调用前后加逻辑，UML 类图难以区分。\n\n意图不同：\n- 代理：目的是「控制访问」，关注对象的生命周期与访问管控（何时创建、谁能访问、从哪访问）。代理对象往往「自己决定何时创建真实对象」，调用方通常不知道有代理存在（透明替换）。\n- 装饰器：目的是「增强行为」，关注在不改原对象的前提下叠加新职责（加日志、加缓存、加校验、加计费）。装饰器「必须接收一个已存在的被装饰对象」，调用方通常主动组装装饰链（显式组合）。\n\n判断依据：\n① 看包装层是为「管理对象的创建/访问/远程」服务 → 代理；是为「给对象加新行为」服务 → 装饰器。\n② 看是否自己创建被包装对象：代理常自己创建/延迟创建真实对象；装饰器只接收并包装已存在的对象。\n③ 看调用方是否知情：代理对调用方更「隐形」（透明替换真实对象）；装饰器对调用方更「显式」（主动拼装链 \`new A(new B(new C()))\`）。\n④ 看是否可层层嵌套：装饰器常多层嵌套叠加职责；代理一般不嵌套（多层代理会模糊控制语义）。\n\n一句话：代理管「能不能/什么时候用」，装饰器管「用得更强」。`,
    tags: ["对比", "代理", "装饰器"],
  },
];
