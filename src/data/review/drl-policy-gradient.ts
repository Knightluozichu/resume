import type { ReviewQuestion } from "./types";

export const drlPolicyGradientQuestions: ReviewQuestion[] = [
  {
    id: "drl-policy-gradient-1",
    chapter: "drl-policy-gradient",
    level: 1,
    question: "策略梯度方法的核心思想是什么？与基于价值的方法有何区别？",
    answer:
      "策略梯度方法的核心思想：直接参数化策略 π_θ(a|s)，用神经网络输出动作的概率分布，通过梯度上升最大化期望回报 J(θ)。与基于价值方法的区别：①学习对象——价值法学Q(s,a)，间接得策略 argmax Q；策略法学π_θ(a|s)，直接输出动作概率。②动作空间——价值法适合离散动作（需遍历所有动作取argmax）；策略法天然支持连续动作（输出高斯分布的μ和σ）。③策略类型——价值法学确定性策略；策略法学随机策略（输出概率分布），有利于探索。④数据使用——价值法off-policy（可用旧数据）；纯策略梯度on-policy（必须用当前策略采的数据）。⑤方差——价值法方差较小（TD目标）；纯策略梯度方差大（用蒙特卡洛回报G_t）。⑥收敛性——价值法不保证收敛到最优（函数近似+自举）；策略法保证收敛到局部最优。实际中Actor-Critic融合两者优势。",
    tags: ["策略梯度", "基于价值", "对比", "核心思想"],
  },
  {
    id: "drl-policy-gradient-2",
    chapter: "drl-policy-gradient",
    level: 2,
    question: "策略梯度定理 ∇J(θ) = E[∇log π_θ(a|s) · G_t] 的含义是什么？",
    answer:
      "策略梯度定理的含义：∇J(θ) = E_τ~π_θ [ Σ_t ∇_θ log π_θ(a_t|s_t) · G_t ]。逐项解读：①J(θ)是期望累积回报，是策略π_θ的性能指标。②∇_θ log π_θ(a_t|s_t) 是log似然梯度——策略参数θ如何变化才能增大在状态s_t选择动作a_t的概率。③G_t是回报——从t时刻开始的累积折扣奖励，衡量这个动作序列「有多好」。④乘积的含义——如果G_t为正（好结果），则增大 a_t 的概率；如果G_t为负（坏结果），则降低 a_t 的概率。梯度方向 = log似然梯度 × 回报，好动作被强化、坏动作被弱化。⑤期望E——对所有可能的轨迹τ取平均（用蒙特卡洛采样近似）。关键洞察：不需要知道环境的转移概率P(s'|s,a)（model-free），只需采样轨迹即可估计梯度。这是策略梯度定理的优美之处——将复杂的梯度计算简化为「采样+加权log似然」。",
    tags: ["策略梯度定理", "log似然梯度", "回报", "公式理解"],
  },
  {
    id: "drl-policy-gradient-3",
    chapter: "drl-policy-gradient",
    level: 2,
    question: "REINFORCE算法的步骤是什么？为什么它的方差很大？如何改进？",
    answer:
      "REINFORCE算法步骤：①用当前策略π_θ跑完一整个回合，记录轨迹 (s_0,a_0,r_1, s_1,a_1,r_2, ..., s_T)。②计算每步的回报 G_t = Σ_{k=0}^{T-t-1} γ^k r_{t+k+1}（折扣累积奖励）。③计算梯度 ∇_θ log π_θ(a_t|s_t) · G_t（log似然梯度乘回报）。④梯度上升更新 θ ← θ + α · ∇J(θ)。⑤重复。方差大的原因：①蒙特卡洛估计——G_t是单条轨迹的回报，不同轨迹的回报差异极大（如Atari中一局得分从0到1000+），导致梯度估计方差极大。②没有基线——即使所有动作都好，G_t仍可能因初始状态差而为负，导致好动作被错误惩罚。改进方法：①引入基线 b(s)——用 G_t - b(s) 代替G_t，通常b(s) = V(s)（状态值），减去基线不改变梯度期望但大幅降低方差（优势函数 A = G - V）。②因果性回报——只用t时刻之后的奖励计算G_t（过去动作不依赖未来回报），减少不相关噪声。③时序差分替代蒙特卡洛——用TD误差代替G_t（Actor-Critic方法），单步更新进一步降方差。④方差缩减技术——如GAE（广义优势估计）在偏差和方差间取平衡。",
    tags: ["REINFORCE", "方差", "基线", "改进"],
  },
  {
    id: "drl-policy-gradient-4",
    chapter: "drl-policy-gradient",
    level: 3,
    question: "策略梯度方法如何处理连续动作空间？网络输出和训练方式有什么不同？",
    answer:
      "策略梯度处理连续动作空间的方法：①网络输出——离散动作用softmax输出概率向量（每个动作一个概率）；连续动作用高斯分布，网络输出均值μ(s)和标准差σ(s)（或log σ），动作从N(μ,σ²)采样。也可用Beta分布（有界动作）或混合高斯（多峰分布）。②动作采样——训练时从分布中采样 a ~ π_θ(·|s) 保证探索；执行时可选均值（确定性策略）或继续采样（随机策略）。③log似然——离散：log π(a|s) = log softmax(logits)[a]；连续高斯：log π(a|s) = -0.5 * ((a-μ)/σ)² - log σ - 0.5*log(2π)。梯度计算方式相同：∇log π · G_t。④优势——无需离散化动作空间（避免维数灾难），能输出任意精度的连续值（如关节角度、油门大小）。⑤挑战——连续空间探索更难（高维高斯采样覆盖范围有限），需配合熵正则化（如SAC）或参数空间噪声。⑥确定性策略梯度（DPG/DDPG/TD3）——另一条路线：直接输出确定性动作 a = μ_θ(s)，用确定性策略梯度定理 ∇J = E[∇_a Q(s,a) · ∇_θ μ_θ(s)]，适合高维连续控制但探索需外部噪声（OU噪声或高斯噪声）。",
    tags: ["连续动作", "高斯分布", "策略参数化", "DPG"],
  },
];
