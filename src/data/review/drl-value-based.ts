import type { ReviewQuestion } from "./types";

export const drlValueBasedQuestions: ReviewQuestion[] = [
  {
    id: "drl-value-based-1",
    chapter: "drl-value-based",
    level: 1,
    question: "DQN的核心思想是什么？它是如何用神经网络近似Q函数的？",
    answer:
      "DQN（Deep Q-Network）的核心思想：用神经网络替代Q表，近似Q函数 Q(s,a;θ) ≈ Q*(s,a)，将「查表」变为「网络前向传播」。具体做法：①输入状态s（如Atari游戏的4帧堆叠图像84x84x4），经过CNN提取特征 + 全连接层，输出每个动作的Q值。②动作选择：argmax_a Q(s,a;θ)，配合ε-贪心进行探索。③训练目标：用贝尔曼方程构造TD目标 y = r + γ max_a' Q(s',a';θ⁻)，定义损失 L(θ) = E[(Q(s,a;θ) - y)²]，用梯度下降更新θ。④关键创新：经验回放（存转移到缓冲区随机采样训练）+ 目标网络（θ⁻定期同步，稳定TD目标）。DQN解决了传统Q-Learning无法处理高维状态空间（如图像）的问题，是深度RL的开山之作。",
    tags: ["DQN", "Q函数近似", "贝尔曼方程", "神经网络"],
  },
  {
    id: "drl-value-based-2",
    chapter: "drl-value-based",
    level: 2,
    question: "经验回放和目标网络分别解决什么问题？为什么DQN需要这两个技术？",
    answer:
      "经验回放和目标网络是DQN的两大稳定化技术：①经验回放（Experience Replay）——问题：RL数据是高度相关的连续转移序列（相邻步的状态非常相似），直接训练会导致网络过拟合到局部、梯度估计方差大。解决：将转移 (s,a,r,s') 存入环形缓冲区，训练时随机采样minibatch。好处：打破数据时间相关性、提高样本利用率（每条转移可多次使用）、使数据分布更接近i.i.d.。②目标网络（Target Network）——问题：TD目标 y = r + γ max Q(s',a';θ) 中的Q用同一个网络θ计算，θ每步都在变，导致目标y不断漂移（「追着自己影子跑」），训练不稳定甚至发散。解决：用独立的θ⁻（目标网络参数）计算TD目标，θ⁻每隔N步从θ硬拷贝一次（或软更新 θ⁻ ← τθ + (1-τ)θ⁻），期间θ⁻固定不变。好处：目标在一段时间内稳定，梯度方向一致，训练可收敛。两者缺一不可——没有经验回放数据相关性太强，没有目标网络目标值不稳定。",
    tags: ["经验回放", "目标网络", "稳定化", "DQN"],
  },
  {
    id: "drl-value-based-3",
    chapter: "drl-value-based",
    level: 2,
    question: "Double DQN和Dueling DQN各改进了DQN的什么问题？原理是什么？",
    answer:
      "Double DQN和Dueling DQN的改进：①Double DQN——问题：标准DQN的TD目标 y = r + γ max_a' Q(s',a';θ⁻) 中，max操作会导致Q值系统性过估计（因为噪声中max总是偏高）。原理：将「选动作」和「估Q值」解耦——用主网络θ选动作 a* = argmax_a' Q(s',a';θ)，用目标网络θ⁻估Q值 y = r + γ Q(s',a*;θ⁻)。由于θ和θ⁻不完全相同，选动作的偏差和估值的偏差不完全正相关，从而降低过估计。②Dueling DQN——问题：标准DQN直接输出每个动作的Q值，但很多状态下动作选择不影响值（如已经到达终点，所有动作Q值差不多），直接学Q值效率低。原理：将Q值分解为 Q(s,a) = V(s) + A(s,a)，V(s)是状态值（与动作无关），A(s,a)是优势函数（动作相对平均值的好坏）。网络输出两个分支：一个学V(s)，一个学A(s,a)，合成Q值。好处：V(s)不需要对每个动作分别学习，状态值学习更高效；A(s,a)只需学差异，收敛更快。",
    tags: ["Double DQN", "Dueling DQN", "过估计", "Q值分解"],
  },
  {
    id: "drl-value-based-4",
    chapter: "drl-value-based",
    level: 3,
    question: "DQN训练中如果Q值发散不收敛，可能的原因和解决方案是什么？",
    answer:
      "DQN的Q值发散不收敛的可能原因和解决方案：①学习率过大——梯度更新步长太大导致震荡。解决：降低学习率（如从1e-3降到1e-4），或使用Adam自适应优化器 + 学习率衰减。②目标网络更新太频繁或太稀疏——太频繁则目标不稳定，太稀疏则目标过时。解决：调整更新频率（通常每500-1000步硬更新，或软更新τ=0.005~0.01）。③奖励尺度问题——奖励绝对值太大导致Q值爆炸。解决：奖励裁剪（clip to [-1,1]）或归一化。④经验回放太小——数据不够多样。解决：增大缓冲区（1万~100万），确保开始训练前缓冲区有足够数据（如先随机采样10000步）。⑤探索不足——智能体只访问了少量状态，Q值估计不准。解决：增大ε起始值、减慢ε衰减速度。⑥梯度爆炸——网络太深或激活函数选择不当。解决：梯度裁剪（clip grad norm）、使用ReLU而非sigmoid、添加BatchNorm/LayerNorm。⑦状态编码问题——输入未归一化。解决：图像归一化到[0,1]、连续状态标准化（减均值除标准差）。调试方法：打印max Q值曲线、梯度范数、loss曲线，定位发散起点。",
    tags: ["DQN调试", "Q值发散", "训练不稳定", "工程实践"],
  },
];
