import type { ReviewQuestion } from "./types";

export const dscConcurrencyControlQuestions: ReviewQuestion[] = [
  {
    id: "dsc-cc-1",
    chapter: "dsc-concurrency-control",
    level: 2,
    question: "解释共享锁和排他锁的区别，并说明相容性矩阵。",
    answer: "共享锁 S（读锁）：读数据时加，允许多个事务同时持有 S 锁读同一数据。排他锁 X（写锁）：写数据时加，独占，与任何其他锁都互斥。相容性矩阵：请求 S/持有 S → 相容（读读共享）；请求 X/持有 S → 冲突；请求 S/持有 X → 冲突；请求 X/持有 X → 冲突。即读读共享，读写互斥，写写互斥。规则核心：读操作不修改数据可共享，写操作修改数据必须独占。",
    tags: ["锁", "共享锁", "排他锁", "相容性矩阵", "并发控制"],
  },
  {
    id: "dsc-cc-2",
    chapter: "dsc-concurrency-control",
    level: 3,
    question: "什么是两阶段封锁？严格 2PL 与基本 2PL 有什么区别？为什么实际多用严格 2PL？",
    answer: "两阶段封锁 2PL 要求每个事务分两阶段：增长阶段只加锁不解锁，收缩阶段只解锁不加锁，一旦开始解锁就不能再加锁，保证可串行化。严格 2PL 与基本 2PL 的区别：严格 2PL 要求 X 锁持有到事务结束（commit/abort 才释放），而基本 2PL 在收缩阶段即可释放。实际多用严格 2PL 的原因：它避免级联中止——若事务在收缩阶段释放 X 锁后还未提交，其他事务可能读到其未提交的修改，若该事务随后回滚则其他读它的事务也需回滚（级联）。严格 2PL 持有 X 锁到结束，保证其他事务读不到未提交数据，避免级联中止。",
    tags: ["2PL", "严格2PL", "级联中止", "可串行化", "封锁协议"],
  },
  {
    id: "dsc-cc-3",
    chapter: "dsc-concurrency-control",
    level: 3,
    question: "什么是死锁？描述三种死锁处理策略。",
    answer: "死锁是两个或多个事务互相等待对方持有的锁，导致永久阻塞。例如 T1 持有 A 的 X 锁等待 B，T2 持有 B 的 X 锁等待 A。三种处理：①预防——破坏死锁条件，如一次性加所有锁（破坏请求和保持）、所有事务按固定顺序加锁（破坏循环等待）、时间戳策略 wound-wait/wait-die；②检测——定期构造等待图（节点是事务，等待关系是边），若有环则存在死锁，选一个代价最小的事务回滚解除；③超时——事务等待超过阈值则回滚重启（简单但可能误杀非死锁的长时间等待）。",
    tags: ["死锁", "死锁预防", "死锁检测", "等待图", "超时"],
  },
  {
    id: "dsc-cc-4",
    chapter: "dsc-concurrency-control",
    level: 3,
    question: "对比封锁 2PL、MVCC、乐观 OCC 三种并发控制协议的优缺点和适用场景。",
    answer: "①封锁 2PL（悲观）：先加锁再操作，读阻塞写，保证强一致可串行化，但并发度低且需处理死锁。适合对一致性要求高、写冲突多的场景。②MVCC（多版本）：每行多版本，读看快照不加锁、写创建新版本，读不阻塞写写不阻塞读，高并发读为主，无死锁，但需清理旧版本有存储开销，SI 下可能有写偏斜异常。适合读多写少场景，是主流数据库（PostgreSQL/MySQL/Oracle）的选择。③乐观 OCC：假设冲突少，无锁执行，提交时校验冲突，无冲突提交有冲突回滚重启。适合冲突少/只读场景，冲突多时回滚开销大。三者核心权衡：一致性强度 vs 并发度 vs 实现复杂度。",
    tags: ["2PL", "MVCC", "OCC", "并发控制对比", "写偏斜"],
  },
];
