import type { ReviewQuestion } from "./types";

export const dujCompileOptimizeQuestions: ReviewQuestion[] = [
  {
    id: "duj-co-1",
    chapter: "duj-compile-optimize",
    level: 2,
    question: `分层编译的五个层级分别做什么？为什么要分层而不是直接用C2？`,
    answer: `五个层级：第0层解释执行+采集profile；第1层C1编译带profiling做简单优化；第2层C1编译带profiling不做额外优化；第3层C1编译完整优化不带profiling；第4层C2编译基于C1采集的profile做激进优化。不直接用C2的原因：C2编译耗时长（数百毫秒到秒级），直接用C2预热期过长。分层编译的平衡：先用C1快速编译让热代码尽快获得性能提升，C1同时采集更精确profile，当profile足够精确且方法确实很热时再用C2深度编译。这样预热期短（C1快速介入）峰值性能高（C2深度优化）。C2乐观假设失败时会逆优化回退到C1或解释执行，重新采集profile后再次编译。`,
    tags: ["分层编译", "C1", "C2", "逆优化"],
  },
  {
    id: "duj-co-2",
    chapter: "duj-compile-optimize",
    level: 2,
    question: `逃逸分析的三种逃逸级别是什么？每种级别可以做哪些优化？`,
    answer: `三种级别：①NoEscape（未逃逸）——对象仅在方法内使用不被外部访问。可做标量替换（拆解为成员标量放入寄存器/栈帧，对象无需创建）、栈上分配（效果等同栈帧分配，方法返回自动释放，零GC）、锁消除（不可能被其他线程访问，synchronized无意义）。②ArgEscape（方法逃逸）——对象作为参数传递给外部方法或被返回。有限优化，需堆分配但可做部分锁优化。③GlobalEscape（线程逃逸）——对象被其他线程访问或存入静态字段。需堆分配且需考虑线程安全。逃逸分析由-XX:+DoEscapeAnalysis控制（JDK 8默认开启），是C2最重要的优化之一，很多临时对象（StringBuilder/迭代器）被标量替换消除堆分配。`,
    tags: ["逃逸分析", "NoEscape", "标量替换", "锁消除"],
  },
  {
    id: "duj-co-3",
    chapter: "duj-compile-optimize",
    level: 3,
    question: `方法内联为什么是JIT最重要的优化？内联的条件是什么？`,
    answer: `最重要原因：①消除方法调用开销（创建栈帧/参数传递/PC跳转）；②为其他优化铺路——内联后更大范围代码可做逃逸分析和循环优化，这是最关键价值；③常量传播——参数是常量时编译期可直接计算；④消除虚方法调用——CHA分析只有一个实现时可去虚拟化内联。内联条件：①方法体不能太大——-XX:MaxInlineSize（默认35字节）控制非热点方法，-XX:FreqInlineSize（默认325字节）控制热点方法；②需频繁调用——热点优先；③虚方法需可去虚拟化——CHA确认只有一个实现才能内联；④不能内联递归——防止代码爆炸。JVM限制总内联深度和内联后代码大小防止膨胀。`,
    tags: ["方法内联", "JIT优化", "CHA", "去虚拟化"],
  },
  {
    id: "duj-co-4",
    chapter: "duj-compile-optimize",
    level: 3,
    question: `标量替换与栈上分配的关系是什么？为什么说HotSpot没有真正的栈上分配？`,
    answer: `标量替换是逃逸分析的核心优化——将未逃逸的聚合对象拆解为成员标量，分散到寄存器/栈帧局部变量表，对象本身不需要在堆上创建。栈上分配的概念：如果对象所有字段都在栈帧上（通过标量替换实现），效果等同栈上分配——方法返回栈帧销毁，对象自动释放，零GC。HotSpot没有真正栈上分配的原因：HotSpot没有直接在栈帧上分配完整对象的机制，而是通过标量替换间接实现效果——对象被拆散后字段作为标量放入局部变量表/寄存器，从JVM角度根本没有创建对象。标量替换比栈上分配更激进：不仅避免堆分配，还消除对象头开销（12-16字节），字段可直接放入寄存器。但局限：只有未逃逸对象才能标量替换，对象太大（字段太多）可能不做。`,
    tags: ["标量替换", "栈上分配", "逃逸分析", "HotSpot"],
  },
];
