import type { ReviewQuestion } from "./types";

export const dujExecutionEngineQuestions: ReviewQuestion[] = [
  {
    id: "duj-ee-1",
    chapter: "duj-execution-engine",
    level: 2,
    question: "JVM为什么要用解释器+JIT混合模式？纯解释或纯编译有什么问题？",
    answer: "混合模式（-Xmixed，HotSpot默认）结合两者优点：启动时用解释器逐条执行——启动快、内存少；热点代码触发JIT编译为机器码——长期运行效率高。纯解释的问题：每条字节码反复解释，执行效率低，循环体高频方法浪费CPU。纯JIT（-Xcomp）的问题：启动慢——所有方法首次调用都要编译；内存高——编译所有方法需大量CodeCache；浪费——冷方法也编译但只执行一次。分层编译进一步细化：第0层解释+采profile，第1-3层C1快速编译，第4层C2激进优化。服务端应用启动时解释开销被摊薄，大部分时间运行C2机器码。",
    tags: ["混合模式", "解释器", "JIT", "分层编译"],
  },
  {
    id: "duj-ee-2",
    chapter: "duj-execution-engine",
    level: 2,
    question: "热点探测是如何工作的？方法调用计数器和回边计数器有什么区别？",
    answer: "HotSpot用基于计数器的热点探测：①方法调用计数器——统计方法被调用次数，超过阈值（-XX:CompileThreshold，C2默认10000）提交JIT编译。②回边计数器——统计循环回边次数，超过阈值触发OSR编译（栈上替换，方法还在执行时替换为编译版本）。区别：方法调用计数器针对整个方法需多次调用触发；回边计数器针对循环体，方法只调用一次但循环频繁也能触发OSR。重要细节：方法调用计数器会衰减（半衰期机制，防止偶尔频繁的方法被浪费编译），回边计数器不衰减；分层编译开启时阈值动态调整；JIT编译异步提交到编译队列后台执行。",
    tags: ["热点探测", "方法调用计数器", "回边计数器", "OSR"],
  },
  {
    id: "duj-ee-3",
    chapter: "duj-execution-engine",
    level: 2,
    question: "栈帧由哪几部分组成？各部分的作用是什么？",
    answer: "栈帧是虚拟机栈元素，方法调用入栈、返回出栈。四部分：①局部变量表——以slot为单位存储方法参数和局部变量，实例方法slot[0]是this引用，容量编译期确定。②操作数栈——字节码执行的工作区，大多数字节码通过压栈/弹栈操作数据（如iadd弹出两个int压入和）。③动态链接——指向运行时常量池中该栈帧所属方法的引用，支持虚方法分发实现多态。④方法返回地址——正常返回时PC跳回调用者下一条指令；异常返回时查异常表。字节码执行本质就是不断操作局部变量表和操作数栈。字节码是栈式指令集架构（操作数通过操作数栈传递），跨平台但指令数比寄存器架构多。",
    tags: ["栈帧", "局部变量表", "操作数栈", "动态链接"],
  },
  {
    id: "duj-ee-4",
    chapter: "duj-execution-engine",
    level: 3,
    question: "什么是OSR（栈上替换）？它解决了什么问题？",
    answer: "OSR（On Stack Replacement）是在方法执行过程中（栈帧还在栈上）将解释执行替换为JIT编译后的机器码执行的机制。解决的问题：普通JIT在方法调用次数达阈值后触发，编译完成后下次调用使用编译版本。但如果方法只被调用一次但内部有千万次循环，普通JIT永远不触发（方法调用计数器只有1），热点循环只能被解释执行。OSR通过回边计数器统计循环次数，超阈值触发OSR编译，在循环执行中替换为编译版本继续执行。限制：OSR编译入口在循环开头而非方法开头，只能覆盖循环体部分，优化效果可能不如普通JIT完整。应用场景：服务端长时间运行的事件循环，方法调用计数器=1但循环执行极多。",
    tags: ["OSR", "栈上替换", "回边计数器", "JIT"],
  },
];
