import { ReviewQuestion } from "./types";

export const eacCodeOptimizationQuestions: ReviewQuestion[] = [
  {
    id: "eac-code-optimization-1",
    chapter: "eac-code-optimization",
    level: 3,
    question: `数据流分析框架的四要素是什么？以「可用表达式分析」为例说明。`,
    answer:
      `数据流分析框架四要素：①方向 D——前向（沿控制流传播）或后向（逆控制流传播）②值域 V——可能取值的集合，通常是格（Lattice），如 {⊥, T, F, ⊤} 或可用表达式集合的幂集 ③传递函数 F——每个基本块如何变换数据流值，\`OUT[b] = F_b(IN[b])\` ④交汇运算 ∧——多个前驱（或后继）的值如何合并，通常是交集（必须性质）或并集（可能性质）。以可用表达式分析为例：①方向：前向（表达式计算后向下传播）②值域：可用表达式集合的幂集 ③传递函数：\`OUT[b] = (IN[b] ∪ GEN[b]) - KILL[b]\`，GEN[b] 是块内新计算的表达式，KILL[b] 是因操作数被重定义而失效的表达式 ④交汇运算：交集 \`IN[b] = ∩ OUT[p]\`（所有前驱都可用才可用）。迭代到不动点即得每个块的可用表达式集，用于公共子表达式消除。`,
    tags: ["数据流分析", "四要素", "可用表达式", "格", "传递函数", "交汇运算"],
  },
  {
    id: "eac-code-optimization-2",
    chapter: "eac-code-optimization",
    level: 2,
    question: `局部值编号（LVN）、可用表达式分析和死代码消除（DCE）分别做什么？`,
    answer:
      `局部值编号（LVN）：基本块内的优化。给每个计算出的值分配一个编号，相同表达式（操作数编号+运算符相同）复用之前的计算结果，消除块内冗余计算。如 \`a = b+c; d = b+c\` 中第二个 \`b+c\` 与第一个编号相同，直接替换为 \`d = a\`。可用表达式分析：全局前向数据流分析，找出「已计算且操作数未被重定义」的表达式。若表达式在程序某点可用，则后续重复计算可替换为之前的值，实现全局公共子表达式消除。死代码消除（DCE）：后向活跃变量分析驱动的优化。若某条指令的计算结果从未被使用（不活跃），则该指令是死代码，可安全删除。删除一条后可能暴露更多死代码，需级联迭代删除。三者配合：LVN 处理块内冗余，可用表达式处理跨块冗余，DCE 清理无用计算，共同压缩 IR。`,
    tags: ["局部值编号", "LVN", "可用表达式", "死代码消除", "DCE", "冗余消除"],
  },
  {
    id: "eac-code-optimization-3",
    chapter: "eac-code-optimization",
    level: 3,
    question: `循环优化中的归纳变量简化、循环不变量外提（LICM）和强度削弱各是什么？`,
    answer:
      `归纳变量简化（Induction Variable Simplification）：循环中常有多个相关的归纳变量（如 \`i++\` 和 \`p += 4\` 同步递增），可删除冗余的归纳变量，只保留最便宜的一个。通过分析归纳变量的线性关系，用更便宜的变量替换昂贵的。循环不变量外提（LICM，Loop-Invariant Code Motion）：将循环中每次迭代结果都相同的计算移到循环外，只算一次。需先做循环分析（识别循环、计算支配关系），且要确保外提不改变语义（如不在异常路径上、不影响副作用）。强度削弱（Strength Reduction）：用更便宜的运算替代昂贵的运算。最典型的场景是循环内的乘法：\`i * 4\` 在每次迭代中 i 递增 1，则 \`i*4\` 每次递增 4，可用临时变量 \`t += 4\`（加法）替代 \`i*4\`（乘法）。加法通常比乘法快，尤其在循环热点中。三者都依赖循环结构识别和支配关系分析，是中端优化的核心。`,
    tags: ["循环优化", "归纳变量", "LICM", "强度削弱", "循环不变量"],
  },
  {
    id: "eac-code-optimization-4",
    chapter: "eac-code-optimization",
    level: 2,
    question: `什么是数据流分析的不动点迭代？为什么数据流分析一定能收敛？`,
    answer:
      `不动点迭代：数据流分析从初始值开始，反复应用传递函数和交汇运算更新每个基本块的 IN/OUT 值，直到所有值不再变化（达到不动点），此时得到精确的保守近似解。每轮迭代遍历所有基本块一次。一定能收敛的原因：①值域是有限格（如可用表达式集合是有限幂集），值只能沿格向上或向下单调变化 ②传递函数和交汇运算是单调函数——单调函数在有限格上迭代必到达不动点 ③每次更新要么保持不变要么沿格移动，有限格保证移动次数有限，故迭代必终止。收敛速度取决于格的高度和 CFG 结构，实践中通常几轮到几十轮即收敛。保守性：数据流分析追求「安全近似」——宁可漏报优化机会（少优化），也不能误报（改变语义），因此分析结果偏向保守方向（如可用表达式用交集而非并集）。`,
    tags: ["不动点迭代", "收敛性", "格", "单调函数", "保守近似", "数据流分析"],
  },
];
