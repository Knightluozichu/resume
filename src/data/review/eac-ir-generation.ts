import { ReviewQuestion } from "../types";

export const eacIrGenerationQuestions: ReviewQuestion[] = [
  {
    id: "eac-ir-generation-1",
    chapter: "eac-ir-generation",
    level: 2,
    question: "中间表示（IR）的作用是什么？为什么不在 AST 上直接生成目标代码？",
    answer:
      "中间表示是介于 AST 和目标代码之间的机器无关表示。作用：①解耦前端与后端——前端输出 IR，后端从 IR 生成代码，两端独立 ②优化友好——IR 是线性指令序列，比树形 AST 更适合做数据流分析和各种优化 ③多平台复用——同一份 IR 可生成不同平台的目标代码，优化只需做一次。不在 AST 上直接生成目标代码的原因：AST 层次结构复杂、与源语言耦合紧密，直接生成会混杂语法结构与机器细节，难以做机器无关优化，也无法复用后端。引入 IR 作为中间层，使优化在机器无关层完成，后端只负责 IR→机器码的映射，职责清晰。",
    tags: ["中间表示", "IR", "AST", "解耦", "优化", "多平台复用"],
  },
  {
    id: "eac-ir-generation-2",
    chapter: "eac-ir-generation",
    level: 2,
    question: "三地址码（TAC）的格式是什么？以 `a = b + c * 2` 为例说明 AST 如何翻译为三地址码。",
    answer:
      "三地址码每条指令最多包含三个操作数（两个源 + 一个目的），形式如 `x = y op z`。以 `a = b + c * 2` 为例，AST 的 Assign 节点右子树是 BinOp(+)，其右操作数是 BinOp(*)。翻译过程（语法制导，自底向上）：①为 `c * 2` 生成临时变量 t1，emit `t1 = c * 2` ②为 `b + t1` 生成临时变量 t2，emit `t2 = b + t1` ③生成赋值 `a = t2`。临时变量 t1、t2 存储中间结果，将嵌套的树形表达式展平为线性指令序列。三地址码的简单形式使后续优化（如常量折叠 `c*2`→直接算出）和后端指令选择都更容易。",
    tags: ["三地址码", "TAC", "AST翻译", "临时变量", "语法制导"],
  },
  {
    id: "eac-ir-generation-3",
    chapter: "eac-ir-generation",
    level: 3,
    question: "什么是 SSA（静态单赋值）形式？φ 函数的作用是什么？为什么 SSA 使优化更高效？",
    answer:
      "SSA（Static Single Assignment，静态单赋值）：在 IR 中要求每个变量只被赋值一次。如果同一变量在程序中被多次赋值，SSA 为每次赋值创建新的版本（如 x1, x2, x3）。在控制流汇合点（多个前驱基本块的汇合处），用 φ 函数合并不同路径的值：`x3 = φ(x1, x2)` 表示根据到达该点的路径选择 x1 或 x2 的值。φ 函数的作用是显式表达控制流汇合处的数据流合并。SSA 使优化更高效的原因：①每个变量只有一个定义点，def-use 链直接且精确，无需复杂的数据流分析即可知道每个值的来源和去向 ②常量传播、死代码消除、公共子表达式消除等优化在 SSA 上实现更简单 ③避免了「同一变量多次定值」带来的模糊性。LLVM IR、GCC GIMPLE、Java HotSpot 都采用 SSA 形式。",
    tags: ["SSA", "静态单赋值", "φ函数", "def-use链", "LLVM", "数据流"],
  },
  {
    id: "eac-ir-generation-4",
    chapter: "eac-ir-generation",
    level: 2,
    question: "控制流图（CFG）和基本块是什么？它们在 IR 中的作用是什么？",
    answer:
      "基本块（Basic Block）：一段连续的指令序列，只有一个入口（第一条指令）和一个出口（最后一条指令），块内没有分支也没有跳转目标。基本块的入口是：程序入口、跳转目标、跳转/分支指令的下一条。控制流图（CFG）：将每个基本块作为节点，基本块之间的跳转关系作为有向边构成的图。如果块 B1 末尾跳转到 B2，则有一条边 B1→B2。CFG 在 IR 中的作用：①显式表达程序的控制流结构（分支、循环、异常），是数据流分析（到达定值、活跃变量、可用表达式）的基础数据结构 ②划分基本块使分析可在块内局部做、块间全局做，降低分析复杂度 ③循环检测（通过支配关系和回边）是循环优化的前提 ④CFG + SSA 是现代编译器优化管道的标准 IR 形态。",
    tags: ["控制流图", "CFG", "基本块", "数据流分析", "循环检测"],
  },
];
