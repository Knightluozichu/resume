import { ReviewQuestion } from "../types";

export const eacScanningQuestions: ReviewQuestion[] = [
  {
    id: "eac-scanning-1",
    chapter: "eac-scanning",
    level: 2,
    question: "词法扫描器的生成流水线是什么？从正则表达式到表驱动 DFA 经历了哪些步骤？",
    answer:
      "生成流水线四步：①正则表达式——用正则描述每种 Token 的词法模式（如 `[a-z][a-z0-9]*` 描述标识符）②Thompson 构造法——将正则转为 NFA（非确定有限自动机），引入 ε 转移和多路分支 ③子集构造法（Subset Construction）——将 NFA 转为 DFA（确定有限自动机），消除 ε 转移，将 NFA 状态集合合并为 DFA 单状态 ④Hopcroft 算法——最小化 DFA，划分等价状态类，得到状态数最少的 DFA。最终将最小 DFA 编码为转移表，运行时扫描器只需查表驱动，无需理解正则。",
    tags: ["扫描器", "正则表达式", "NFA", "DFA", "Thompson", "子集构造", "Hopcroft"],
  },
  {
    id: "eac-scanning-2",
    chapter: "eac-scanning",
    level: 2,
    question: "什么是最长匹配原则（Maximal Munch）和优先规则？以输入 \"iffy\" 为例说明。",
    answer:
      "最长匹配原则：扫描器在遇到接受状态后不立即停止，而是继续读入字符直到无法转移，然后回退到上一个接受状态，取最长可匹配的子串作为 Token。优先规则：当多个模式都能匹配同一输入时，按模式声明顺序选择（通常关键字模式声明在标识符之前，使关键字优先）。以 `iffy` 为例：扫描器不会在 `if` 处停止识别为关键字 IF，而是继续读入 `fy`，发现 `iffy` 完整匹配标识符模式 `[a-z][a-z0-9]*`，于是识别为一个 ID token。只有输入恰好是 `if` 后跟非标识符字符（如空格或括号）时，才识别为关键字 IF。",
    tags: ["最长匹配", "优先规则", "Token识别", "关键字"],
  },
  {
    id: "eac-scanning-3",
    chapter: "eac-scanning",
    level: 3,
    question: "NFA 和 DFA 有什么区别？子集构造法如何将 NFA 转为 DFA？",
    answer:
      "NFA（非确定有限自动机）：对一个输入字符可能有多个转移目标，允许 ε 转移（不消耗输入即可跳转），需维护状态集合或回溯。DFA（确定有限自动机）：对每个状态和输入字符只有唯一转移，无 ε 转移，运行时无需回溯，查表即可。子集构造法：核心思想是「NFA 的状态集合 ↔ DFA 的单状态」。①计算每个 NFA 状态的 ε-闭包（通过 ε 能到达的所有状态）②对每个状态集合和每个输入字符，计算转移后的新状态集合的 ε-闭包 ③将每个唯一的状态集合映射为一个新的 DFA 状态 ④包含 NFA 接受状态的集合对应 DFA 接受状态。这样 DFA 的每步转移都是确定的，运行效率高。",
    tags: ["NFA", "DFA", "子集构造", "ε闭包", "自动机"],
  },
  {
    id: "eac-scanning-4",
    chapter: "eac-scanning",
    level: 1,
    question: "为什么编译器要将词法分析独立为扫描器阶段，而不是在语法分析中直接处理字符？",
    answer:
      "独立扫描器的理由：①分离关注点——词法规则（正则）与语法规则（上下文无关文法）属于不同层次，分开实现更清晰 ②效率——DFA 扫描器是 O(n) 线性时间，比在语法分析中逐字符处理更快 ③可复用——扫描器生成器（如 flex/lex）可自动从正则生成高效 DFA，减少手写工作量 ④预处理——扫描器可同时处理注释剥离、空白跳过、行号跟踪等词法级任务，让语法分析器只关注 Token 结构 ⑤错误定位——词法错误（非法字符）在扫描阶段就能精确定位。语法分析器接收的是干净的 Token 序列，不必关心字符级细节。",
    tags: ["扫描器", "分离关注点", "DFA效率", "词法分析", "语法分析"],
  },
];
