import type { ReviewQuestion } from "./types";

/** Effective C# 总复习题 */
export const ecsFinalReviewQuestions: ReviewQuestion[] = [
  {
    id: "ecs-final-review-1",
    chapter: "ecs-final-review",
    level: 1,
    question: "Effective C# 四大板块各自的核心准则是什么？",
    answer:
      "1. 语言习惯：属性优先于字段（封装）、readonly 优于 const（版本安全）、is/as 优于强制转换（类型安全）。\n2. 资源管理：IDisposable 标准模式区分确定性释放与终结器兜底，using 让释放成为默认，区分托管与非托管资源。\n3. 泛型与 LINQ：泛型约束最小化保持复用性，LINQ 延迟执行让查询反映最新数据但需物化避免重复执行。\n4. 并发设计：异常过滤器精确 catch 不丢栈，Task.WhenAll 并行独立任务，避免 async void，相等性三者协调。\n\n记忆线索：习惯奠基 → 资源管控 → 泛型抽象 → 并发收口。",
    tags: ["四大板块", "核心准则", "总复习"],
  },
  {
    id: "ecs-final-review-2",
    chapter: "ecs-final-review",
    level: 2,
    question:
      "贯穿 Effective C# 全书的主线是什么？请举例说明。",
    answer:
      "主线是「让类型系统替你兜底」——把人为容易犯的错误交给编译器和运行时，让正确的做法成为默认，让错误编译不通过。\n\n举例：\n- 属性让封装成为默认（字段永远 private，无法绕过校验）。\n- readonly 让版本安全成为默认（跨程序集不烤入，升级即生效）。\n- IDisposable 让释放成为默认（using 确定性释放，忘 using 也有终结器兜底）。\n- 泛型约束让类型安全成为默认（编译期检查替代运行时强转，类型不对编译不过）。\n- 异常过滤器让精确 catch 成为默认（when 为 false 自动跳过，不丢栈）。\n- Task.WhenAll 让并行成为默认（独立任务不排队）。\n- 相等性协调让哈希正确成为默认（Equals 和 GetHashCode 一致保证字典查找正确）。\n\n每条建议都在「让正确成为默认」，这是 Bill Wagner 整理经验的核心视角。",
    tags: ["贯穿主线", "类型系统兜底", "让正确成为默认"],
  },
  {
    id: "ecs-final-review-3",
    chapter: "ecs-final-review",
    level: 3,
    question:
      "遇到一个具体问题，如何对应到正确的板块和条款？请用「字典查不到相等键」举例。",
    answer:
      "定位方法：先按问题类型定板块，再按「让什么成为默认」找条款。\n\n板块定位：\n- 封装/版本/类型转换 → 语言习惯\n- 资源泄漏/释放 → 资源管理\n- 集合操作/查询/泛型 → 泛型与 LINQ\n- 多线程/异常/相等性 → 并发设计\n\n「字典查不到相等键」示例：\n1. 问题类型是相等性/哈希 → 并发设计板块。\n2. 在板块内按「让什么成为默认」找 → 相等性协调（让哈希正确成为默认）。\n3. 定位到条款：重写 Equals 必须同时重写 GetHashCode。\n4. 根因：重写了 Equals 为值比较但没重写 GetHashCode，两个 Equals 相等的对象哈希不同被分到不同桶，字典按哈希找桶找不到。\n5. 修法：重写 GetHashCode 保证 Equals 相等的对象返回相同哈希码（用 HashCode.Combine）。\n\n这个定位流程可推广到所有问题：问题现象 → 问题类型 → 板块 → 条款 → 根因 → 修法。",
    tags: ["问题定位", "板块", "相等性", "GetHashCode"],
  },
  {
    id: "ecs-final-review-4",
    chapter: "ecs-final-review",
    level: 4,
    question:
      "为什么说 Effective C# 的 50 条建议不是孤立规则，而是一条主线的展开？请从「让正确成为默认」角度串联至少四个板块。",
    answer:
      "50 条建议的统一哲学是「让类型系统替你兜底，让正确成为默认」。每条建议都是这条主线在不同场景的展开，四大板块层层递进：\n\n语言习惯——让封装和类型安全成为默认：\n属性让封装成为默认（字段 private，校验自动生效）；readonly 让版本安全成为默认（不烤入，升级即生效）；is/as 让类型安全成为默认（编译期检查替代运行时强转）。这层确立了「对外契约、对内存储、编译期保证」的地基。\n\n资源管理——让释放成为默认：\nIDisposable 让释放成为默认（using 确定性释放，终结器兜底）。这层把资源生命周期的负担交给框架——开发者只要用 using，正确释放就是默认行为，忘了也有兜底。\n\n泛型与 LINQ——让抽象成为默认：\n约束最小化让类型安全复用成为默认（编译期保证 T 的能力）；延迟执行让查询反映最新数据成为默认。这层把集合操作的复杂度交给库，开发者写声明式查询，正确执行是默认。\n\n并发设计——让并发安全成为默认：\n异常过滤器让精确 catch 成为默认（不丢栈）；WhenAll 让并行成为默认（独立任务不排队）；ConfigureAwait(false) 让库不死锁成为默认；相等性协调让哈希正确成为默认。这层把并发正确性交给模式与类型系统。\n\n递进逻辑：封装（语言习惯）是资源安全（资源管理）的前提，资源安全是泛型容器（泛型 LINQ）的前提，泛型容器是并发集合（并发设计）的前提。每层都在前层的默认安全基础上做更高级的抽象，主线始终是「让正确成为默认、让错误编译不通过」。所以 50 条不是孤立的，而是同一条主线在封装、资源、抽象、并发四个层次的递进展开。",
    tags: ["综合", "主线展开", "递进", "让正确成为默认", "四大板块"],
  },
];
