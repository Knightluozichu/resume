import type { ReviewQuestion } from "./types";

/** Effective C++ 继承与多态复习题 */
export const efcInheritancePolymorphismQuestions: ReviewQuestion[] = [
  {
    id: "efc-inheritance-polymorphism-1",
    chapter: "efc-inheritance-polymorphism",
    level: 1,
    question: `public 继承表达什么关系？为什么说「public 继承 = is-a」？`,
    answer:
      `public 继承表达 is-a 关系：派生类是一种基类。\n\n含义：每个派生类对象都是一个基类对象。基类能出现的地方，派生类对象都能出现（里氏替换原则）。\n\n例如：\`class Student : public Person\` 表示「学生是一种人」，任何接受 \`Person\` 的函数都能接受 \`Student\`。\n\nis-a 的推论：\n- 基类的接口（public 函数）也是派生类的接口——派生类继承接口\n- 基类的不变式在派生类中仍然成立\n- 对基类为真的断言对派生类也为真\n\n常见的 is-a 误用：\n- 企鹅不是鸟（如果鸟有 \`fly()\` 方法）——is-a 要求所有基类操作对派生类都有意义\n- 正方形不是长方形——长方形可以独立改变长和宽，但正方形不能，行为不兼容\n\n关键区分：public 继承是接口继承，不是实现继承。派生类继承的是「能做什么」（接口），而非「怎么做」（实现可以覆盖）。`,
    tags: ["is-a", "public继承", "里氏替换", "基本概念"],
  },
  {
    id: "efc-inheritance-polymorphism-2",
    chapter: "efc-inheritance-polymorphism",
    level: 2,
    question:
      `纯虚函数、虚函数、非虚函数分别代表什么继承语义？如何选择？`,
    answer:
      `三种函数对应三种继承语义：\n\n1. 纯虚函数（\`virtual void f() = 0;\`）\n- 语义：只继承接口，不继承实现\n- 派生类必须提供实现（否则也是抽象类）\n- 用于：强制派生类定义行为，基类没有合理默认实现\n\n2. 虚函数（\`virtual void f() {}\`）\n- 语义：继承接口 + 默认实现，派生类可覆盖也可不覆盖\n- 用于：基类有合理默认行为，派生类可能需要定制\n- 注意：如果派生类忘记覆盖，会静默使用基类默认实现——可以考虑把默认实现设为纯虚 + 单独的非虚辅助函数\n\n3. 非虚函数（\`void f() {}\`）\n- 语义：继承接口 + 强制实现，派生类不应重新定义\n- 用于：行为在所有派生类中必须一致，不因类型而异\n- 如果派生类重新定义非虚函数，不会多态——通过基类指针调用的是基类版本，通过派生类指针调用的是派生类版本，行为不一致\n\n选择原则：\n- 需要派生类必须实现 → 纯虚\n- 需要派生类可以定制但有合理默认 → 虚\n- 行为不变 → 非虚\n- 不确定 → 默认用非虚，等需要多态时再改虚（过早抽象是负担）`,
    tags: ["纯虚函数", "虚函数", "非虚函数", "继承语义", "多态"],
  },
  {
    id: "efc-inheritance-polymorphism-3",
    chapter: "efc-inheritance-polymorphism",
    level: 3,
    question:
      `条款 36 说「绝不重新定义继承而来的 non-virtual 函数」，条款 37 说「绝不重新定义继承而来的缺省参数值」。请分析这两条禁令的共同本质是什么？`,
    answer:
      `两条禁令的共同本质：都是因为「静态绑定 vs 动态绑定」的不一致导致行为分裂。\n\n条款 36：不重新定义 non-virtual 函数\n- non-virtual 函数是静态绑定的——调用哪个版本由指针/引用的静态类型决定\n- 如果派生类重新定义了基类的 non-virtual 函数 \`f()\`：\n  - 通过基类指针调用 → 基类版本\n  - 通过派生类指针调用 → 派生类版本\n- 同一个对象，不同类型的指针调用，行为不同——这违反了 is-a 关系的核心假设\n\n条款 37：不重新定义继承的缺省参数值\n- 缺省参数值也是静态绑定的——由指针/引用的静态类型决定\n- virtual 函数是动态绑定的（由对象实际类型决定）\n- 如果派生类覆盖了 virtual 函数但改了缺省参数值：\n  - 函数体是派生类的（动态绑定）\n  - 缺省参数值是基类的（静态绑定）\n  - 结果：调用派生类函数却用了基类的参数值——语义混乱\n\n共同本质：\n- C++ 的绑定规则是「函数动态绑定，参数静态绑定」\n- non-virtual 函数整体静态绑定\n- virtual 函数的函数体动态绑定，但缺省参数静态绑定\n- 任何在派生类中改变静态绑定的行为都会导致「同一个对象在不同上下文中行为不一致」\n- 这种不一致破坏了多态的透明性——用户无法通过基类指针安全地操作派生类对象\n\n解决方案：\n- 条款 36 的解法：如果需要不同行为，用 virtual 函数\n- 条款 37 的解法：用 NVI 模式——public 非虚函数提供缺省参数，private 虚函数做实际工作。缺省参数只在非虚函数中定义，派生类覆盖的是虚函数（不带缺省参数），从根上消除不一致。\n\n两条条款的核心教训：理解 C++ 的静态绑定 vs 动态绑定机制，避免在同一继承层次中创造行为分裂。`,
    tags: ["条款36", "条款37", "non-virtual", "缺省参数", "静态绑定", "动态绑定"],
  },
  {
    id: "efc-inheritance-polymorphism-4",
    chapter: "efc-inheritance-polymorphism",
    level: 4,
    question:
      `条款 35 介绍了 virtual 函数的替代方案（NVI、Strategy 模式等）。请综合论述在什么情况下应该用 virtual 函数，什么情况下应该用替代方案？`,
    answer:
      `Scott Meyers 在条款 35 中提出四种 virtual 函数的替代方案，选择依据是「灵活性需求 vs 设计复杂度」的权衡：\n\n方案一：NVI（Non-Virtual Interface）模式\n- 做法：public 非虚函数调用 private 虚函数\n- 适合：大部分需要多态的场景——保持 virtual 语义但增加前后置检查的控制\n- 优势：统一检查入口，派生类只覆盖实现不改接口\n- 代价：几乎无额外代价，应作为默认选择\n\n方案二：函数指针成员（Strategy 的函数版）\n- 做法：类中持有 \`void (*strategy)()\` 函数指针，构造时注入\n- 适合：同一类型的不同实例需要不同行为，且行为可在运行期切换\n- 优势：运行期灵活，比继承更轻量\n- 代价：失去类型安全，函数指针难以内联\n\n方案三：tr1::function（现代版 Strategy）\n- 做法：类中持有 \`std::function<void()>\` 成员，可接受函数对象、lambda、bind 表达式\n- 适合：需要更灵活的回调——带状态、带参数绑定\n- 优势：类型安全，可携带状态，比函数指针强大得多\n- 代价：有调用开销和内存分配\n\n方案四：经典 Strategy 模式（继承体系）\n- 做法：定义策略接口类，具体策略派生，类中持有策略指针\n- 适合：策略本身有复杂的状态和行为，需要继承层次\n- 优势：最灵活，策略可以有自己的继承体系\n- 代价：最重——需要定义接口类、多个派生类、管理策略对象生命周期\n\n选择决策树：\n1. 行为是否因派生类类型而异？否 → 非虚函数\n2. 是。是否需要前后置控制？是 → NVI（默认选择）\n3. 否。是否需要运行期切换行为？否 → 直接用 private virtual\n4. 是。行为是否简单（无状态）？是 → 函数指针\n5. 否。是否需要携带状态/参数绑定？是 → \`std::function\`\n6. 否。策略本身是否复杂？是 → 经典 Strategy 模式\n\n深层原则：virtual 函数把「行为差异」绑死在「类型差异」上——派生类类型决定了行为。但如果同一类型的不同实例需要不同行为，或行为需要运行期切换，virtual 就力不从心了。Strategy 方案解耦了「类型」和「行为」，代价是设计复杂度增加。选择的核心标准是：是否真的需要运行期灵活性，如果不需要，virtual（或 NVI）是最简单正确的选择。`,
    tags: ["综合", "条款35", "NVI", "Strategy", "virtual替代方案", "设计模式"],
  },
];
