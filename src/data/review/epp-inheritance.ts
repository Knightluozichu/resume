import type { ReviewQuestion } from "./types";

/** C++ Primer Plus · 继承与多态复习题 */
export const eppInheritanceQuestions: ReviewQuestion[] = [
  {
    id: "epp-inheritance-1",
    chapter: "epp-inheritance",
    level: 1,
    question: "C++ 继承的基本语法是什么？虚函数和纯虚函数有什么区别？什么是抽象类？",
    answer:
      "继承语法：`class Derived : public Base { ... };`。派生类继承基类的成员，冒号后是访问说明符（public/protected/private 继承，常用 public），决定基类成员在派生里的访问级别。\n\n虚函数 vs 纯虚函数：\n- 虚函数 `virtual void f();`：声明可被派生类覆盖，基类提供默认实现。基类可实例化。\n- 纯虚函数 `virtual void f() = 0;`：只声明接口，不提供实现（或可在类外定义供派生调用）。含纯虚函数的类是抽象类，不可实例化。\n\n抽象类：含有至少一个纯虚函数的类。它定义接口契约，强制派生类必须实现纯虚函数才能实例化。抽象类只能作基类、只能用指针/引用访问，不能直接创建对象。\n\n多态：用基类指针/引用调用虚函数时，实际执行的是「指针所指对象的实际类型」的版本，而非指针声明类型的版本。这是运行期多态，靠 vtable（虚函数表）动态绑定实现。\n\n记忆线索：继承复用接口与实现，virtual 开启覆盖，纯虚 = 0 强制派生实现并定义抽象。",
    tags: ["继承", "虚函数", "纯虚函数", "抽象类"],
  },
  {
    id: "epp-inheritance-2",
    chapter: "epp-inheritance",
    level: 2,
    question: "为什么基类的析构函数通常要声明为 virtual？不声明会有什么后果？",
    answer:
      "原因：保证通过基类指针 delete 派生类对象时，派生类的析构函数会被调用。\n\n不声明的后果——内存泄漏与未定义行为：\n若基类析构非虚，`Base* p = new Derived; delete p;` 时，delete 看到指针类型是 Base*，只调用 Base 的析构函数，Derived 的析构函数不被调用。如果 Derived 在析构里需要释放资源（如它有成员是动态分配的指针），这些资源就泄漏了。更糟的是，C++ 标准规定这种情况下行为未定义，可能崩溃。\n\n声明为虚后：`delete p` 时，因为是虚函数，走 vtable 找到实际对象类型 Derived 的析构，先调 Derived 析构（释放派生资源），再自动调 Base 析构（释放基类资源），顺序正确，资源全部释放。\n\n规则：只要一个类打算被继承（即可能有人 `Base* p = new Derived`），它的析构函数就应声明 virtual。即使派生类无额外资源，也养成习惯避免未来加资源时遗漏。反之，明确不被继承的类（如 final 类、工具类）析构可不虚（省一点 vtable 开销）。\n\n注意：纯虚析构也要提供定义（在类外），因为派生类析构会隐式调用基类析构，链接器需要其定义。",
    tags: ["虚析构", "内存泄漏", "析构"],
  },
  {
    id: "epp-inheritance-3",
    chapter: "epp-inheritance",
    level: 3,
    question: "你的基类有 `virtual void f()`，派生类也定义了 `void f()` 但忘了写 override，后来基类把 f 改成 `f(int)`，代码还能编译但行为错了，发生了什么？",
    answer:
      "这是「忘记 override 关键字」导致的隐蔽 bug：\n\n原始情况：基类 `virtual void f()`，派生类 `void f()`（没写 override）。派生类的 f 确实覆盖了基类的 f（签名相同），多态正常工作——但编译器没检查派生类是否真的在「覆盖」，因为没写 override。\n\n基类改签名后：基类变成 `virtual void f(int)`。此时派生类的 `void f()` 与基类的 `void f(int)` 签名不同（参数个数不同），不再是覆盖，而是「隐藏」——派生类的 f 隐藏了基类继承来的 f(int)，但不是覆盖。\n\n后果：\n1. 代码能编译通过（隐藏不是错误，只是名字查找行为）。\n2. 通过基类指针 `Base* p = new Derived; p->f(1);` 调用时，因基类 f(int) 是虚的，去 vtable 找派生类版本——但派生类没有 f(int)，找到的是基类自己的 f(int)，派生类的 f() 没被调用。多态失效，行为错误。\n3. 这类 bug 极难发现：编译通过、运行不报错，只是「该走派生却走了基类」。\n\n修法与预防：派生类覆盖虚函数时永远写 `override` 关键字（C++11）。override 让编译器检查「这个函数确实覆盖了基类的虚函数」，签名不匹配时编译报错。基类改签名后，派生类写 override 会立刻编译失败，逼你同步更新。\n\n关键习惯：所有覆盖虚函数都加 override，让编译器替你把关。这是现代 C++ 最重要的防 bug 习惯之一。",
    tags: ["override", "隐藏", "排查"],
  },
  {
    id: "epp-inheritance-4",
    chapter: "epp-inheritance",
    level: 4,
    question: "综合分析：C++ 的多态靠 vtable 动态绑定实现，相比编译期绑定（如函数重载）有什么代价？为什么 C++ 仍选择「默认不虚、按需开启」而非「默认虚」？",
    answer:
      "vtable 动态绑定的代价：\n1. 运行期开销：每次虚函数调用要先从对象的 vptr 读 vtable 地址，再从 vtable 读函数指针，多一次间接跳转。比直接调用多一次访存（vtable 可能不在缓存里）。\n2. 空间开销：每个含虚函数的类有一张 vtable（每个虚函数一个指针条目）；每个该类对象有一个 vptr（一个指针大小）。类多、对象多时累积可观。\n3. 阻断内联：虚函数调用是间接跳转，编译器通常无法内联（除非编译期能确定实际类型），失去内联优化。\n4. 阻断去虚化：多数情况下编译器无法证明「这个基类指针实际只指向一种类型」，无法把虚调用退化成直接调用。\n\nC++ 选择「默认不虚」的原因：\n1. 零成本抽象哲学：不为未使用的特性付费。绝大多数函数不需要多态，默认不虚省掉 vtable/vptr 开销，让 C++ 的普通函数与 C 一样高效。\n2. 兼容 C 的性能预期：C++ 要在性能敏感领域（系统编程、游戏、嵌入式）立足，不能让所有函数都背上间接调用的税。\n3. 明确设计意图：virtual 是「我设计这个类要被继承、这个函数要被多态调用」的显式声明。默认不虚让继承体系的设计决策可见，避免意外的多态。\n\n对比 Java/C# 的「默认虚」：它们为简化模型让所有方法默认可覆盖，代价是所有方法调用都可能有间接开销（虽有 JIT 去虚化补救）。C++ 把选择权交给开发者，要求你理解「这个函数需要多态吗」，需要才开 virtual。\n\n工程指导：只在「需要运行期按实际类型分派」时用 virtual（如继承体系的接口方法、需要覆盖的策略方法）。纯算法函数、工具函数、叶子类的方法不要虚。这是 C++ 性能文化的一部分——多态是工具不是默认。",
    tags: ["综合", "vtable", "多态", "零成本抽象"],
  },
];
