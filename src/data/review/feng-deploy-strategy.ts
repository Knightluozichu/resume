import type { ReviewQuestion } from "./types";

export const fengDeployStrategyQuestions: ReviewQuestion[] = [
  {
    id: "feng-deploy-strategy-1",
    chapter: "feng-deploy-strategy",
    level: 2,
    question: "蓝绿部署和灰度发布的核心区别是什么？",
    answer:
      "蓝绿部署：维护两套完全相同的环境（蓝=旧版、绿=新版），新版本先部署到绿环境，验证通过后流量入口一次性从蓝切到绿，秒级全量切换。旧版（蓝）保留作为回滚兜底。核心是「全量切换」，代价是双倍资源。灰度发布：新旧版本同时在线，按比例渐进放量（如 10% → 50% → 100%），通过用户/地域/cookie 分流，观察指标无异常再逐步扩大。核心是「渐进放量」，资源增量小。区别：蓝绿是「一刀切」（0% → 100%），灰度是「缓放量」（渐进增长）。蓝绿适合需要立即全量切换且回滚要秒级的场景；灰度适合想控制爆炸半径、观察影响的场景。生产中常组合使用：灰度渐进验证 + 蓝绿保底兜底。",
    tags: ["部署", "蓝绿", "灰度"],
  },
  {
    id: "feng-deploy-strategy-2",
    chapter: "feng-deploy-strategy",
    level: 3,
    question: "纯前端静态站点如何利用 CDN 实现秒级版本回滚？",
    answer:
      "前提是静态资源必须版本化——每次构建产物以内容哈希命名（如 app.[hash].js），不同版本文件并存于 CDN。HTML 入口文件（index.html）引用特定版本的资源。回滚机制：①CDN/Origin 同时保留多个历史版本的产物文件（不过期清理旧版本）；②回滚时只需将 HTML 入口指向旧版本的资源引用（修改 index.html 中的 script/link 标签或版本映射），CDN 边缘节点秒级生效；③无需重新构建、无需重新部署，只需切换入口指向。关键实践：HTML 文件不缓存或短缓存（s-maxage 小），JS/CSS 等资源长缓存（immutable）；回滚操作脚本化（一键切换版本号）。这种方式让纯前端站点获得秒级回滚能力，比重新构建发布快两个数量级。",
    tags: ["CDN", "回滚", "静态部署"],
  },
  {
    id: "feng-deploy-strategy-3",
    chapter: "feng-deploy-strategy",
    level: 3,
    question: "灰度发布中如何做流量分流？有哪些常见维度？",
    answer:
      "灰度分流常见维度：①按用户标识——用户 ID 哈希取模（如 ID % 100 < 10 走新版，即 10%），稳定（同一用户始终在同一组）；②按 cookie/feature flag——特定标记用户先体验（内测、白名单）；③按地域/IP——部分地区先放量，观察地域性差异；④按设备/浏览器——部分设备先适配（如先 iOS 后 Android）；⑤按流量百分比——随机按比例分流（不稳定，同一用户可能来回切换，需配合 sticky session）。实现层：前端可通过网关层（Nginx/网关）按规则路由到不同版本后端；纯前端可通过 feature flag SDK（如 LaunchDarkly / Unleash）在运行时决定加载哪版组件。灰度需配合监控：每个灰度组独立看性能和错误指标，异常立即停灰度回旧版。",
    tags: ["灰度", "分流", "feature flag"],
  },
  {
    id: "feng-deploy-strategy-4",
    chapter: "feng-deploy-strategy",
    level: 4,
    question: "为什么说「可回滚的发布才是安全发布」？回滚方案设计要考虑哪些因素？",
    answer:
      "发布引入变更，变更有风险——代码再测也无法覆盖所有线上环境变量。可回滚意味着「出问题时能快速恢复到已知可用状态」，把故障影响时间（MTTR）从「排查+修复+重新发布」（小时级）压缩到「切回旧版」（秒/分钟级）。回滚方案设计考虑因素：①数据兼容——如果新版本做了数据库 migration，回滚旧版可能不兼容新数据结构（最难的回滚场景），需保证 migration 可逆或向后兼容；②缓存一致性——回滚后 CDN/浏览器缓存可能混用新旧版本资源，需设计资源版本化避免冲突；③回滚速度——蓝绿秒级、CDN 秒级、重新构建分钟级，按业务容忍度选；④回滚决策——什么指标触发回滚（错误率超阈值、核心指标下降），谁有权决定，避免犹豫放大故障；⑤回滚演练——平时演练回滚流程，确保紧急时能执行而非抓瞎。核心：回滚不是失败后的补救，而是发布方案的一部分，必须在发布前就设计好。",
    tags: ["部署", "回滚", "安全发布", "MTTR"],
  },
];
