import type { ReviewQuestion } from "./types";

/** 序列类型 复习题 */
export const flpSequencesQuestions: ReviewQuestion[] = [
  {
    id: "flp-sequences-1",
    chapter: "flp-sequences",
    level: 1,
    question: "Python 序列按「容器 vs 扁平」和「可变 vs 不可变」可分四类，请各举一个代表类型。",
    answer:
      "四类及代表：\n1. 容器 + 可变：`list`（存任意对象引用，长度可增减）\n2. 容器 + 不可变：`tuple`（存引用但本身不可变，可作字典键）\n3. 扁平 + 可变：`array.array`（存同质原始数值，紧凑省内存）\n4. 扁平 + 不可变：`str`（存字符，不可变）\n\n容器序列存的是对象的引用（可含异构类型），扁平序列存的是原始值（同质、紧凑）。",
    tags: ["序列分类", "容器序列", "扁平序列"],
  },
  {
    id: "flp-sequences-2",
    chapter: "flp-sequences",
    level: 2,
    question: "列表推导式和生成器表达式在语法和内存行为上有什么区别？为什么后者能处理大数据流？",
    answer:
      "语法区别：列表推导式用方括号 `[x for x in seq]`，生成器表达式用圆括号 `(x for x in seq)`。\n\n内存行为区别：\n- 列表推导式立即求值，把所有结果收集到一个 `list`，占用 O(n) 内存。\n- 生成器表达式返回一个生成器对象，**惰性求值**，每次只产出一个值，内存占用 O(1)。\n\n生成器表达式能处理大数据流的原因：它不预先生成全部结果，而是按需逐个产出。例如 `sum(x for x in big_file)` 即使文件有上亿行也只占常量内存，因为它逐行读取、累加后即丢弃，不像 `[x for x in big_file]` 会把全部值塞进内存。代价是生成器只能迭代一次，且不支持随机索引。",
    tags: ["列表推导式", "生成器表达式", "惰性求值", "内存"],
  },
  {
    id: "flp-sequences-3",
    chapter: "flp-sequences",
    level: 3,
    question: "`a = [1, 2, 3]; b = a * 2` 与 `c = [[0]] * 3; c[0][0] = 9` 各自的结果是什么？解释后者为何会「全部变成 9」。",
    answer:
      "`a * 2` 结果是 `[1, 2, 3, 1, 2, 3]`——`*` 对不可变元素做值复制，重复拼接，没问题。\n\n`c = [[0]] * 3` 结果看似是 `[[0], [0], [0]]`，但三个子列表其实是**同一个对象的引用**——`*` 复制的是引用而非对象。所以 `c[0][0] = 9` 修改的是那个共享的内层列表，三个位置都指向它，结果 `c` 变成 `[[9], [9], [9]]`。\n\n正确写法是列表推导式：`c = [[0] for _ in range(3)]`，每次循环都新建一个内层列表，三个子列表相互独立。这是序列「容器存引用」语义的直接后果——`*` 等便捷语法对可变内层对象会埋下共享引用的坑。",
    tags: ["序列乘法", "共享引用", "可变对象", "陷阱"],
  },
  {
    id: "flp-sequences-4",
    chapter: "flp-sequences",
    level: 4,
    question: "在什么场景下应该用 `array.array` 或 `bytes`/`bytearray` 而非 `list`？请从内存、类型约束、互操作三个维度综合论证。",
    answer:
      "`array.array`、`bytes`/`bytearray` 是扁平序列，适合以下场景：\n\n1. 内存维度：扁平序列在内存中紧凑存放原始值（如 C 的 `int` 数组），而 `list` 存的是指向 Python 对象的指针（每个对象还有独立的对象头）。存 1000 万个整数，`array('l')` 约占 80MB，`list` 可能占 280MB+。大数据量时扁平序列省内存显著。\n\n2. 类型约束维度：`array.array` 强制所有元素同类型（如 `'i'` 有符号整型），`bytes` 只存 0-255。这既是约束也是保障——能提前发现类型混用错误，且类型固定让二进制布局确定。\n\n3. 互操作维度：`bytes`/`bytearray` 是二进制协议（网络包、文件格式、加密）的通用语言，能直接和 C 库、`struct` 模块、`socket` 交互；`array.array` 可通过缓冲区协议与 NumPy、`memoryview` 零拷贝共享内存。\n\n不该用的场景：需要存异构类型（字符串、对象混装）时必须用 `list`；需要频繁中间插入删除时 `list` 也更方便（`array` 没有额外优势）。所以选型口诀：同质大量数值/字节用扁平，异构灵活用容器。",
    tags: ["array", "bytes", "扁平序列", "选型", "内存"],
  },
];
