import type { ReviewQuestion } from "./types";

export const gep1SceneGraphQuestions: ReviewQuestion[] = [
  {
    id: "gep1-scene-graph-1",
    chapter: "gep1-scene-graph",
    level: 1,
    question: `空间分割如何把视锥剔除从 O(n) 降到 O(log n)？`,
    answer: `空间分割把物体按空间位置组织成树（八叉树/BVH），每个内部节点的包围体包含所有子节点。剔除时从根开始测节点包围盒与视锥的关系：节点完全在外则整棵子树跳过（早剪枝），可能一跳砍掉上万个物体。每层只测少数节点，大部分子树被跳过，复杂度降到 O(log n) 量级。十万个物体的场景可能只需测几百个节点。`,
    tags: ["空间分割", "视锥剔除", "早剪枝"],
  },
  {
    id: "gep1-scene-graph-2",
    chapter: "gep1-scene-graph",
    level: 2,
    question: `静态场景用八叉树、动态场景用 BVH，为什么？`,
    answer: `八叉树按固定空间划分，物体移动后可能跨到相邻区域，需要从旧节点删除、插入新节点，频繁移动时维护开销大。BVH 按包围球组织，物体移动只需更新自己的包围球并向上传播（父包围球重算），不需要重新划分结构，增量更新更轻。所以静态场景用八叉树（划分稳定、查询快），动态场景用 BVH（更新便宜）。`,
    tags: ["八叉树", "BVH", "动态场景"],
  },
  {
    id: "gep1-scene-graph-3",
    chapter: "gep1-scene-graph",
    level: 3,
    question: `为什么父节点包围球要包含所有子节点包围球？这对剔除有什么意义？`,
    answer: `包含关系保证：如果父节点包围球在视锥外，所有子节点也一定在外（因为子节点在父包围球内部）。这样测父节点一次就能跳过整棵子树，不必逐个测子节点——这就是早剪枝。如果没有这个包含保证，父节点在外不代表子节点在外，就无法跳过，退化回 O(n)。父包围球 = 所有子包围球的并集，从叶节点向上合并计算。`,
    tags: ["包围球", "包含关系", "早剪枝"],
  },
  {
    id: "gep1-scene-graph-4",
    chapter: "gep1-scene-graph",
    level: 4,
    question: `场景图每帧遍历的四步是什么？它们之间为什么是这个顺序？`,
    answer: `四步：① 更新世界变换（脏标记递归累积父链矩阵）→ ② 更新包围球（从叶向上合并，父包围球 = 子包围球并集）→ ③ 视锥剔除（用空间树早剪枝）→ ④ 收集可见物体进渲染列表。顺序原因：世界变换必须先更新，否则包围球算的是旧位置；包围球必须先于剔除更新，否则剔除用的是过期包围体导致漏剔或误剔；剔除后才能知道哪些可见，才能收集进渲染列表交给渲染管线。四步有严格数据依赖，不能乱序。`,
    tags: ["综合", "遍历流程", "场景图"],
  },
];
