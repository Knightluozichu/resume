import type { ReviewQuestion } from "./types";

export const goplOfficialChapterQuestions: ReviewQuestion[] = [
  {
    id: "gopl-tutorial-1",
    chapter: "gopl-tutorial",
    level: 1,
    question: "把 dup 从 stdin 扩展为多个文件时，os.Args、Scanner、map、文件 ownership 与确定性输出分别承担什么责任？",
    answer: "os.Args[1:] 给出输入文件名；main 逐个 Open 并在当次迭代明确 Close，countLines 只消费 io.Reader、用 Scanner 逐行读取并返回 Scanner.Err。共享 map 利用 int zero value 累加所有来源，最后筛选 count > 1 的 key 并排序输出，不能依赖 map 的随机迭代顺序。",
    tags: ["os.Args", "bufio.Scanner", "map", "io.Reader", "ownership"],
  },
  {
    id: "gopl-tutorial-2",
    chapter: "gopl-tutorial",
    level: 2,
    question: "并发抓取多个 URL 并把结果用于 Web handler 时，怎样设计 goroutine、channel、HTTP body 与 ResponseWriter 的生命周期，避免死锁和资源泄漏？",
    answer: "每个 URL 启动一个 goroutine，并且无论成功失败都恰好向 typed result channel 发送一次；collector 接收启动数量次，或由 WaitGroup 在全部完成后关闭 channel。http.Get 成功后由 caller 关闭 response body，并检查 status、timeout 与 request context。ResponseWriter 只在 handler 调用期间有效，不能在 handler 返回后由后台 goroutine 继续写；长任务要在 context 内收敛或返回 job id。",
    tags: ["goroutine", "channel", "http.Response", "ResponseWriter", "lifecycle"],
  },
  {
    id: "gopl-program-structure-1",
    chapter: "gopl-program-structure",
    level: 1,
    question: "`var`、`:=`、`=` 分别建立什么 binding，怎样判断 `x, err := f()` 是更新、声明还是 shadowing？",
    answer: "var declaration 创建变量并可使用显式 initializer 或 zero value；短声明在 function 内创建至少一个当前 lexical block 的新变量，同时对同 block 已有名字赋值；等号只给已有变量赋值。判断时必须画出当前 block：x或err若仅在outer block存在，短声明会在inner block创建同名变量并shadow；若两者在当前block都已有，:=因没有新名字而编译失败。",
    tags: ["declaration", "short declaration", "assignment", "scope", "shadowing"],
  },
  {
    id: "gopl-program-structure-2",
    chapter: "gopl-program-structure",
    level: 2,
    question: "named type、package initialization、scope 与 lifetime 如何共同解释一个跨文件温度转换 package？",
    answer: "Celsius和Fahrenheit用type declarations建立独立identity，显式conversion跨静态边界，CToF/FToC负责真正数值公式。package files共享package-level declarations，但imports只在各自file block；先初始化import dependencies，再按变量依赖求值并执行init，最后main。scope决定name在源码何处可见，lifetime决定storage在运行时多久可达，returned pointer或closure可让local在scope结束后继续存活。",
    tags: ["named type", "package initialization", "scope", "lifetime", "conversion"],
  },
  {
    id: "gopl-basic-data-types-1",
    chapter: "gopl-basic-data-types",
    level: 1,
    question: "整数、floating point 与 complex values 在 conversion、overflow 和 special values 上分别需要什么边界检查？",
    answer: "integer conversion先验证target range，fixed-width arithmetic可能按宽度wrap，signed/unsigned同一bits有不同解释；float32/64需要考虑rounding并用math.IsNaN/IsInf识别special values，NaN不能用equality判断；complex64/128的real/imag components分别受对应float precision约束，输入和结果都要检查finite，再处理magnitude/phase或pixel mapping。",
    tags: ["integer", "overflow", "float64", "NaN", "complex128"],
  },
  {
    id: "gopl-basic-data-types-2",
    chapter: "gopl-basic-data-types",
    level: 2,
    question: "为什么 string length、rune count、用户可见字符数与 untyped constant 的数值范围不能用同一种直觉处理？",
    answer: "string是bytes，len和index按byte；range/utf8按Unicode code point解码runes，grapheme还可能由多个runes组成，因此输入限制必须说明粒度和invalid encoding/normalization policy。untyped numeric constant在compile time保持精确值，不受某个machine width限制；只有进入assignment/call等typed context时才检查target representability，iota序列若持久化还必须显式固定兼容值。",
    tags: ["string", "UTF-8", "rune", "untyped constant", "iota"],
  },
  {
    id: "gopl-composite-types-1",
    chapter: "gopl-composite-types",
    level: 1,
    question: "array、slice 与 map 在复制、aliasing、membership 和稳定输出上有哪些不同 contract？",
    answer: "array length属于type，assignment/argument默认复制全部elements；slice只复制pointer/len/cap descriptor并可能共享backing array，append可能复用或重新分配，所以必须接收返回slice并追踪aliases；map lookup missing返回V zero value，需value+ok保留membership，iteration order未指定，稳定输出先排序keys，并发访问还需同步。",
    tags: ["array", "slice", "backing array", "map", "comma-ok"],
  },
  {
    id: "gopl-composite-types-2",
    chapter: "gopl-composite-types",
    level: 2,
    question: "struct model 经 JSON 和 HTML template 输出时，哪些边界必须显式设计？",
    answer: "struct fields/tags定义record与wire schema，只有exported fields参与encoding/json；absent/null/zero、number precision、unknown fields和domain validation需独立策略。embedding只是selector promotion，冲突显式选path。HTML输出使用trusted html/template source并把外部值作为data传入，让contextual escaping按HTML/attribute/URL等上下文编码；不能拼接用户输入为template source。",
    tags: ["struct", "embedding", "JSON", "struct tags", "html/template"],
  },
  {
    id: "gopl-functions-1",
    chapter: "gopl-functions",
    level: 1,
    question: "recursive function、multiple return values 与 error protocol 怎样组成可定位且有界的失败路径？",
    answer: "每次recursive call有独立frame，必须有base case并对depth/nodes/bytes/cycles设budget。function以(value,error)返回普通失败，parent在有意义的operation/resource边界用%w加context并保留cause，top-level用errors.Is/As分类；partial value是否可用必须文档化。retry受context/deadline/attempt/backoff/idempotency约束，library不应log.Fatal。",
    tags: ["recursion", "multiple returns", "error", "wrapping", "budget"],
  },
  {
    id: "gopl-functions-2",
    chapter: "gopl-functions",
    level: 2,
    question: "function value/closure、defer 与 panic/recover 的 lifetime 和 control-flow contract 有何不同？",
    answer: "function value可传递，closure还持有captured lexical variables并可能延长其lifetime；loop binding要结合go.mod语言版本，Go1.22+每轮新变量。defer在注册点求callee/arguments、函数退出时LIFO执行。panic只沿当前goroutine展开并执行defers；recover必须由同goroutine的deferred function直接调用，应只转换已知内部panic，ordinary errors和未知runtime panic不能盲目吞掉。",
    tags: ["function value", "closure", "defer", "panic", "recover"],
  },
  {
    id: "gopl-methods-1",
    chapter: "gopl-methods",
    level: 1,
    question: "value receiver、pointer receiver、addressable call sugar 与 interface method set 如何共同决定 Point/\*Point 的可调用性？",
    answer: "T method set只含receiver T的方法，*T method set同时含T与*T methods。addressable Point变量调用pointer method时compiler可自动取址，*Point调用value method可自动解引用；这只影响selector call。interface satisfaction严格按method set，不做取址糖，因此只有*Point满足包含pointer-receiver method的interface，应以compile-time assertion固定。",
    tags: ["receiver", "method set", "pointer receiver", "addressability", "interface"],
  },
  {
    id: "gopl-methods-2",
    chapter: "gopl-methods",
    level: 2,
    question: "embedding、method values/expressions 与 IntSet encapsulation 分别怎样影响 selector、receiver lifetime 和 representation ownership？",
    answer: "embedding只promote selectors，nested storage仍存在；同一depth同名method冲突必须显式path。method value x.M绑定receiver，pointer receiver通常保存live alias；method expression T.M把receiver变首参数，二者都不自动snapshot。IntSet用unexported []uint和methods守住bit mapping/negative input/zero-value invariants，Copy与Snapshot必须deep-copy，不能泄露words，并另行定义concurrency。",
    tags: ["embedding", "method value", "method expression", "IntSet", "encapsulation"],
  },
  {
    id: "gopl-interfaces-1",
    chapter: "gopl-interfaces",
    level: 1,
    question: "implicit satisfaction、interface dynamic pair 与 typed nil pointer 怎样共同决定 assignment、nil check 和 method call？",
    answer: "concrete T或*T凭method set隐式满足interface，direct-call的addressability糖不参与。assignment后interface保存dynamic type/value pair；只有两者都nil时interface才等于nil。把nil *T装入后dynamic type仍是*T，所以interface非nil且可满足contract，但method若解引用nilreceiver会panic。成功error路径应直接返回nil，compile-time assertions固定 intended implementer。",
    tags: ["interface", "method set", "dynamic type", "dynamic value", "typed nil"],
  },
  {
    id: "gopl-interfaces-2",
    chapter: "gopl-interfaces",
    level: 2,
    question: "sort.Interface、http.Handler、error、expression evaluator 与 XML token decoder 分别展示了哪些 adapter/assertion/dispatch 选择？",
    answer: "sort.Interface把Len/Less/Swap适配到算法，HandlerFunc把普通function适配Handler，custom Error适配failure contract。可选能力用assertion到小behavior interface并保留base path；error分类用errors.Is/As。Expr.Eval适合开放实现的polymorphic dispatch；closed syntax operation或xml.Token有限variants适合type switch，并必须处理default、stream limits与unknown implementation。",
    tags: ["sort.Interface", "http.Handler", "error", "type assertion", "type switch"],
  },
  {
    id: "gopl-goroutines-and-channels-1",
    chapter: "gopl-goroutines-and-channels",
    level: 1,
    question: "unbuffered/buffered channel、close 与 directional type 怎样组成可证明的 pipeline ownership？",
    answer: "unbuffered send/receive必须rendezvous并同步；buffered channel只允许有限queued progress，full/empty仍阻塞。每个stage从inbound receive、向outbound send，producer/sender owner完成后唯一close自己的outbound，receiver只drain到close。send after close或重复close会panic；directional chan<-/<-chan限制权限，但lifecycle owner仍需文档化。所有blocking boundaries同时监听cancellation。",
    tags: ["goroutine", "unbuffered channel", "buffered channel", "close", "pipeline"],
  },
  {
    id: "gopl-goroutines-and-channels-2",
    chapter: "gopl-goroutines-and-channels",
    level: 2,
    question: "crawler、directory traversal 与 chat server 如何用 select、bounded work、single owner 和 cancellation 避免泄漏与慢消费者拖垮系统？",
    answer: "crawler coordinator单独拥有seen/outstanding，固定workers和bounded frontier限制并发/队列；directory traversal用semaphore限制fds并由WaitGroup owner关闭results；chat broadcaster独占clients map，每client有bounded outgoing queue与disconnect/drop/backpressure策略。select监听work/result/timer/done，closed channel禁用或退出；cancel只发信号，orchestrator还要等待所有workers清理完成。",
    tags: ["select", "crawler", "backpressure", "single owner", "cancellation"],
  },
  {
    id: "gopl-concurrency-with-shared-variables-1",
    chapter: "gopl-concurrency-with-shared-variables",
    level: 1,
    question: "data race、logical race、Mutex/RWMutex 与 happens-before 应怎样从 shared invariant 推导？",
    answer: "同址并发access且至少一个write、无同步即data race；即使每个method各自加锁，跨method check-then-act仍可能是logical race。把完整validate/read-modify-write invariant放进同一owner或critical section，所有read/write/aliases遵守同一lock。先用Mutex，RWMutex只在read-heavy且实测有益时使用；Unlock/后续Lock、channel、Once等happens-before edges才保证visibility，sleep或word-size不算同步。",
    tags: ["data race", "logical race", "Mutex", "RWMutex", "happens-before"],
  },
  {
    id: "gopl-concurrency-with-shared-variables-2",
    chapter: "gopl-concurrency-with-shared-variables",
    level: 2,
    question: "sync.Once、race detector、duplicate-suppressing memo 与 goroutine scheduler 各自提供什么保证，又不保证什么？",
    answer: "Once最多执行一次并安全发布writes，但panic也算完成，不适合retry state machine。-race只检测实际执行interleavings的conflicting accesses，不证明未覆盖paths、deadlock或leak。memo用per-keyready让一个owner fetch、followers等待，允许不同keys并发，并需cancel/eviction/error policy。goroutines用growable stacks在OS threads上调度，GOMAXPROCS限制并行Go code而非goroutine/thread总数，且没有可依赖identity。",
    tags: ["sync.Once", "race detector", "memo", "GOMAXPROCS", "goroutine scheduling"],
  },
  {
    id: "gopl-packages-and-the-go-tool-1",
    chapter: "gopl-packages-and-the-go-tool",
    level: 1,
    question: "import path、package clause、import alias/blank import 与 initialization 各自定义什么边界？",
    answer: "import path标识dependency package，现代环境通常是module path加subdirectory；package clause给默认selector name和package scope；alias只改变当前file binding，_不建立selector而仅触发package initialization side effect。dependencies先初始化，importgraph必须acyclic；blank import只用于documented bounded registration并由integration test证明，不能拿来掩盖unused dependency。",
    tags: ["import path", "package declaration", "import alias", "blank import", "initialization"],
  },
  {
    id: "gopl-packages-and-the-go-tool-2",
    chapter: "gopl-packages-and-the-go-tool",
    level: 2,
    question: "如何把原书 GOPATH workspace 核心映射到现代 modules/workspaces，并用 Go tool 建立可部署证据？",
    answer: "原书package/import/build graph仍成立，但source不再必须位于GOPATH/src；go.mod声明module path/language/dependencies，go.sum校验内容，go.work用于本地多module。用go list -deps -json查询真实graph，go mod tidy/verify校准metadata，build/test/race/vet分别验证编译和行为，go doc/examples验证API，internal限制parent tree可见性。记录GOOS/GOARCH/tags/toolchain，cache hit不替代干净构建。",
    tags: ["GOPATH", "go.mod", "go.work", "go list", "go build"],
  },
  {
    id: "gopl-testing-1",
    chapter: "gopl-testing",
    level: 1,
    question: "table-driven、randomized、command、white-box 与 external tests 应如何组合，避免 brittle oracle？",
    answer: "table records承载name/input/want并复用harness；randomized tests验证property并输出seed/input；command提取run(args,stdin,stdout,stderr)避免不必要subprocess；same-package white-box只测关键internal invariant，external x_test按真实client使用exported API。oracle断言observable contract，failure给context/want/got，不依赖map order、sleep、private layout或环境偶然值。",
    tags: ["table-driven test", "randomized testing", "command test", "white-box", "external test"],
  },
  {
    id: "gopl-testing-2",
    chapter: "gopl-testing",
    level: 2,
    question: "coverage、benchmark、profile 与 Example 各回答什么问题，如何组成性能改造闭环？",
    answer: "coverage只映射executed statements，用于发现空白，不证明oracle/branches/races；Benchmark由b.N校准并报告ns/op、B/op、allocs/op，setup与workload必须一致；CPU/memory/block profiles解释成本归属。先profile找hotspot，改一项，再多次benchmark统计比较并跑correctness/race/end-to-end。Example编译公开用法，Output comment还验证deterministic stdout。",
    tags: ["coverage", "benchmark", "profiling", "benchmem", "Example"],
  },
  {
    id: "gopl-reflection-1",
    chapter: "gopl-reflection",
    level: 1,
    question: "reflect.Type、Value、Kind、invalid/typed nil 与 addressable/settable 分别说明什么，怎样避免panic？",
    answer: "Type描述dynamic concrete type，Value持有value，Kind只归类representation；nilinterface产生nil Type/Invalid Value，typednil pointer是valid Ptr并IsNil。先IsValid和switch Kind再调用kind-specific methods。ValueOf(x)是interface copy不可Set，ValueOf(&x).Elem通常addressable/settable；还要CanSet与exported field检查，Set前验证assignable/convertible和overflow，把失败转成带path error。",
    tags: ["reflect.Type", "reflect.Value", "Kind", "CanSet", "typed nil"],
  },
  {
    id: "gopl-reflection-2",
    chapter: "gopl-reflection",
    level: 2,
    question: "Display/S-expression codec、struct tags 与 method enumeration 如何形成受限 reflection boundary？",
    answer: "recursive Kind dispatch处理primitive/aggregate/pointer/interface，维护path、cycle set、depth/nodes/bytes limits并拒绝unsupported kinds；decoder要求non-nil pointer，写temporary后成功commit并检查numeric range。StructTag.Lookup按consumer grammar解释schema且需compatibility tests；Type.Method只列exported methods并遵守T/*T method sets。runtime shape未知才用reflection，其他优先interface/generics/generated code。",
    tags: ["Display", "S-expression", "struct tag", "method set", "reflection boundary"],
  },
  {
    id: "gopl-low-level-programming-1",
    chapter: "gopl-low-level-programming",
    level: 1,
    question: "Sizeof/Alignof/Offsetof、unsafe.Pointer 与 uintptr 分别提供什么能力，必须维持哪些layout/lifetime条件？",
    answer: "layout constants描述当前target下Go value本身，slice/string等只算descriptor，不递归backing data，不能作portable wire format。unsafe.Pointer可桥接typed pointers；field/element arithmetic必须在original allocation内、不越界、满足alignment/representation并保持object live。uintptr只是integer，GC不追踪或保活，不能跨safe point长期存储后再还原；KeepAlive/checkptr也不修复非法conversion。",
    tags: ["unsafe.Sizeof", "unsafe.Alignof", "unsafe.Offsetof", "unsafe.Pointer", "uintptr"],
  },
  {
    id: "gopl-low-level-programming-2",
    chapter: "gopl-low-level-programming",
    level: 2,
    question: "deep equivalence 与 cgo wrapper 为什么都必须先定义domain/ownership policy，如何验证？",
    answer: "deep equality需明确type、nil/empty、NaN、functions、unexported fields、map、cycle与alias topology，visited pointer pairs防无限递归，但reflect.DeepEqual不自动等于业务语义。cgo明确Go/C allocation、copy/borrow/pin/retain/free、errno/status、thread/callback；CString由caller C.free，C不得长期保留未合法pin的Go pointer，复杂Go value用copy或cgo.Handle。封装internal typed API并跨target运行race/checkptr/C tools/golden tests。",
    tags: ["deep equality", "cycle", "cgo", "C.CString", "pointer passing"],
  },
];
