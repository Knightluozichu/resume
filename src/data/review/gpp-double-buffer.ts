import type { ReviewQuestion } from "./types";

/** 双缓冲复习题 */
export const gppDoubleBufferQuestions: ReviewQuestion[] = [
  {
    id: "gpp-double-buffer-01",
    chapter: "gpp-double-buffer",
    level: 1,
    question: "双缓冲（Double Buffer）解决什么问题？",
    answer:
      "解决的问题：当一个状态在被逐个更新时，读取者可能读到「半更新」的中间状态——一部分已更新、一部分还是旧的，导致不一致。\n\n双缓冲的核心意图：\n- 维护两份缓冲（buffer A、buffer B）。\n- 更新时往「写缓冲」写新值，读取者从「读缓冲」读旧值——两者互不干扰。\n- 更新完成后，原子地交换两个缓冲，让读取者下一轮看到完整的新状态。\n\n效果：\n- 读取者永远看到「完整的一帧状态」，要么全是旧的、要么全是新的，不会是半新半旧的中间态。\n- 更新过程可以安全地引用旧状态（如「根据当前位置算下一个位置」），不会因为自己刚改了状态而误读。\n\n典型场景：图形渲染（前/后帧缓冲）、细胞自动机、粒子系统状态推进、棋盘类游戏的全局同步更新。",
    tags: ["意图", "状态一致性", "半更新", "缓冲"],
  },
  {
    id: "gpp-double-buffer-02",
    chapter: "gpp-double-buffer",
    level: 2,
    question: "双缓冲的工作机制是什么（读旧缓冲、写新缓冲、交换）？",
    answer:
      "三步工作机制：\n\n1. 读旧写新：\n- 任意时刻，一个缓冲是「当前缓冲」（current，对外可读），另一个是「下一缓冲」（next，正在写入）。\n- 更新时，从 current 读旧值参与计算，把结果写入 next。\n- 因为读写是两个不同缓冲，更新过程中读 current 的人看到的始终是稳定的旧状态，不受写入影响。\n\n2. 持续更新：\n- 所有需要更新的单元（像素、细胞、粒子）依次执行「读 current → 计算 → 写 next」。\n- 期间 current 不变，next 逐渐填满新值。\n\n3. 交换（Swap）：\n- 全部更新完后，原子交换两个指针：`current, next = next, current`。\n- 交换后，原 next 成为新的 current（对外可读，包含完整新状态），原 current 成为下次的 next（下轮会被覆盖写入）。\n- 交换必须是原子的——要么完成要么没发生，不能让读取者在交换中途看到混合状态。\n\n关键点：\n- 「双」是核心——单缓冲做不到「边读旧边写新」，要么锁住不让读、要么读到脏数据。\n- 交换用指针交换（O(1)）而非拷贝整个缓冲——两份内存轮流使用，不额外分配。\n\n用代码概括：\n```\nbuffer = [A, B]\ncurrent = 0\nwhile (running) {\n  next = 1 - current\n  for (cell of allCells) {\n    buffer[next][cell] = compute(buffer[current], cell)\n  }\n  current = next  // 交换：下轮读这个，写另一个\n}\n```",
    tags: ["工作机制", "读旧写新", "交换", "指针交换"],
  },
  {
    id: "gpp-double-buffer-03",
    chapter: "gpp-double-buffer",
    level: 3,
    question: "举一个游戏中需要双缓冲的例子（如粒子系统或细胞自动机）。",
    answer:
      "例子：生命游戏（Conway's Game of Life）式的网格模拟。\n\n问题（不用双缓冲）：\n网格上每个细胞根据「周围 8 格的邻居状态」决定下一代死活。如果就地更新——左上角细胞先算完变活了，它右边的细胞再数邻居时就会把这个「本代不该活的新值」数进去，导致结果错误。状态污染会沿着遍历方向扩散，最终结果完全不对。\n\n用双缓冲解决：\n```\ngrid = [newGrid[N][N], newGrid[N][N]]\ncur = 0\nfor (generation) {\n  nxt = 1 - cur\n  for (x, y in grid) {\n    neighbors = countAlive(grid[cur], x, y)  // 读旧网格\n    grid[nxt][x][y] = applyRules(neighbors, grid[cur][x][y])  // 写新网格\n  }\n  cur = nxt  // 交换，下一代从新网格读\n}\n```\n- 所有细胞都从 `grid[cur]`（旧）读邻居，写到 `grid[nxt]`（新）。计算某格时邻居还没被改，结果正确。\n- 整代算完交换，下一代开始读新网格。\n\n游戏中的类似场景：\n1. 粒子系统的全局力场：每个粒子根据「当前所有粒子的位置」算受力。若就地更新位置，先更新的粒子会影响后更新粒子的受力计算，顺序敏感且错误。双缓冲让所有粒子都基于「上一帧的位置」算力，再统一写入新位置。\n2. 群体行为（boids）：鸟群中每只鸟根据邻居位置调整方向，同样需基于一致的旧状态计算。\n3. 图形渲染：GPU 渲染当前帧时往「后缓冲」画，前缓冲给显示器读；画完交换（VSync），避免画面撕裂——这是双缓冲最经典的应用。\n\n核心判断标准：当「更新某单元时需要读取其他单元的状态，且这些状态在本轮更新中也会变」时，就需要双缓冲保证读到的是一致的上轮快照。",
    tags: ["应用", "生命游戏", "粒子系统", "渲染", "状态快照"],
  },
  {
    id: "gpp-double-buffer-04",
    chapter: "gpp-double-buffer",
    level: 4,
    question: "双缓冲与游戏循环是什么关系？为什么说它是「序列型模式」？",
    answer:
      "与游戏循环的关系：\n双缓冲是游戏循环里「更新步骤」的一种保障机制，专门解决「帧内状态一致性」问题。\n\n游戏循环每帧做：输入 → 更新 → 渲染。其中「更新」会把世界从「上一帧的状态」推进到「这一帧的状态」。如果更新过程中各实体互相读取状态，就会读到「半推半旧」的混乱状态。双缓冲让更新过程始终基于「上一帧的完整快照」计算，写入新缓冲，帧末交换——把「连续的状态推进」切成「离散的帧间跳变」。\n\n层次关系：\n- 游戏循环管「时间分帧」——决定何时推进。\n- 更新方法管「谁来动」——遍历实体调用 update。\n- 双缓冲管「动的时候读什么」——保证读的是一致的旧快照，写的是新快照。\n三者是序列型模式的递进：游戏循环定义帧，更新方法填充帧内的实体调度，双缓冲保障帧内调度的状态一致性。\n\n为什么是「序列型模式」：\n本书把双缓冲归入「序列型模式（Sequencing Patterns）」，与游戏循环、更新方法并列。这一类的共同主题是「时间与顺序」——处理「什么时候做」「按什么顺序做」「做的时候状态怎么保证一致」。\n- 游戏循环：何时推进（时间切片）。\n- 更新方法：按什么顺序推进（逐实体）。\n- 双缓冲：推进时如何避免读到半更新状态（双份缓冲）。\n\n与「解耦型」「优化型」的区别：\n- 解耦型（服务定位器、事件队列）关注模块间依赖关系。\n- 优化型（对象池、空间分区）关注性能。\n- 双缓冲不优化性能也不解耦依赖，它解决的是「时序一致性」——这是实时模拟特有的问题，所以归入序列型。\n\n一句话：双缓冲是游戏循环框架下、更新方法执行过程中、保证状态读取一致性的序列保障手段。没有游戏循环的「帧」概念，就没有「上一帧/这一帧」的缓冲交换节奏。",
    tags: ["综合", "游戏循环", "序列型模式", "状态一致性", "关系"],
  },
];
