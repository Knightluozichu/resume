import type { ReviewQuestion } from "./types";

/** 事件队列复习题 */
export const gppEventQueueQuestions: ReviewQuestion[] = [
  {
    id: "gpp-event-queue-01",
    chapter: "gpp-event-queue",
    level: 1,
    question: `事件队列（Event Queue）的意图是什么？`,
    answer:
      `意图：用一个队列把「事件的生产者」和「事件的消费者」在时间上解耦——生产者把事件塞进队列就返回，消费者按自己的节奏从队列取出处理。\n\n核心机制：\n- 生产者调用 \`queue.enqueue(event)\`，瞬间返回，不等消费者处理。\n- 消费者（可同线程也可另一线程）循环 \`queue.dequeue()\` 取出事件处理。\n- 队列作为缓冲，吸收生产速度和消费速度的差异。\n\n效果：\n- 生产者不被消费者拖慢——即使消费者处理慢，生产者只管往队列塞。\n- 消费者按自己的节奏处理——可批量、可延迟、可分优先级。\n- 两者不直接依赖——生产者不知道谁消费，消费者不知道谁生产。\n\n区别于「直接调用」：\n- 直接调用：\`audio.play(sound)\` 同步执行，调用方等到播放完才返回（或至少等到音频系统响应）。\n- 事件队列：\`queue.enqueue(PlaySoundEvent)\` 立即返回，音频系统稍后从队列取出播放。\n\n典型用途：游戏音频系统（避免 playSound 阻塞主线程）、网络消息处理、任务调度、日志异步写入。`,
    tags: ["意图", "生产者消费者", "解耦", "队列"],
  },
  {
    id: "gpp-event-queue-02",
    chapter: "gpp-event-queue",
    level: 2,
    question: `事件队列与观察者模式有什么区别？`,
    answer:
      `相似点：都用于「解耦事件的生产与消费」，都让发送方不直接知道接收方。\n\n核心区别：\n\n1. 通信模型：\n- 观察者：「推（push）」模型——Subject 状态变化时，同步地、主动地遍历调用所有观察者的 \`onNotify()\`。通知是即时的、同步的。\n- 事件队列：「拉（pull）/缓冲」模型——生产者把事件塞队列就返回，消费者自己择时取出处理。事件在队列里「等」，是异步的、有缓冲的。\n\n2. 时间关系：\n- 观察者：通知和处理几乎同时发生（同一调用栈），无延迟。\n- 事件队列：生产与消费之间可有时间差——事件排队等消费者来取，可能延迟到下一帧或另一线程。\n\n3. 消费者数量：\n- 观察者：天然一对多——一个事件通知给所有订阅的观察者。\n- 事件队列：通常一对一——一个事件被一个消费者取走后出队（除非广播式队列复制事件给多消费者）。\n\n4. 顺序与持久性：\n- 观察者：通知顺序由注册顺序决定，事件不持久（通知完就没了），错过的观察者收不到。\n- 事件队列：事件按入队顺序处理（FIFO），事件在队列里持久存在直到被消费——即使消费者当时不在，之后也能补处理。\n\n5. 阻塞与解耦程度：\n- 观察者：同步阻塞——某个观察者慢或抛异常会拖累整个通知链。\n- 事件队列：异步解耦——消费者慢只让队列变长，不阻塞生产者。\n\n6. 跨线程：\n- 观察者：通常同线程同步通知，跨线程需额外同步机制。\n- 事件队列：天然适合跨线程——生产者和消费者可在不同线程，队列用锁/无锁结构保证线程安全。\n\n7. 适用场景：\n- 观察者：需即时、一对多、同线程通知的场景（如 UI 刷新、成就触发）。\n- 事件队列：需异步、缓冲、解耦时间、可能跨线程的场景（如音频播放、网络消息、任务排队）。\n\n一句话：观察者是「同步广播」，事件队列是「异步排队」。前者强调「一对多即时通知」，后者强调「生产消费时间解耦」。`,
    tags: ["对比", "观察者", "事件队列", "同步vs异步", "一对多vs一对一"],
  },
  {
    id: "gpp-event-queue-03",
    chapter: "gpp-event-queue",
    level: 3,
    question:
      `设计一个游戏音频系统：用事件队列避免 \`playSound()\` 阻塞主线程。`,
    answer:
      `问题（不用事件队列）：\n\`playSound('explosion')\` 同步执行——加载音频文件、解码、提交给声卡，整个过程在主线程完成。解码几毫秒到几十毫秒，主线程卡顿掉帧。同帧触发多个音效（爆炸+惨叫+环境音）时尤其严重。\n\n用事件队列解耦：\n\n1. 定义事件：\n\`AudioEvent\` 接口含 \`type\`（'play'/'stop'/'setVolume'）、\`soundName\`、\`volume\`、\`loop\` 等字段。\n\n2. 队列（线程安全）：\n\`AudioQueue\` 内部维护 \`AudioEvent[]\` 和 \`Mutex\`。\`enqueue(e)\` 加锁 push，\`dequeue()\` 加锁 shift 取出。\n\n3. 主线程侧 API（生产者，非阻塞）：\n\`Audio\` 类给游戏逻辑用，\`playSound(name, loop)\` 内部调 \`q.enqueue({ type: 'play', soundName: name, loop })\`——立即返回！\`stopSound(name)\` 同理入队。\n\n4. 音频线程侧（消费者）：\n\`AudioThread.run()\` 循环 \`dequeue()\` 取事件：'play' 类型则 \`loadAndDecode(soundName)\`（耗时操作，但不影响主线程）再 \`soundEngine.play(sound)\`；'stop' 类型则停止对应音效。队列空则 \`sleep(1)\` 稍歇。\n\n5. 使用：\n战斗代码（主线程）调 \`audio.playSound('explosion')\`、\`audio.playSound('scream')\`、\`audio.playSound('debris')\`——三个事件瞬间入队，主线程继续跑战斗逻辑和渲染。音频线程慢慢取出解码播放。\n\n效果：\n- 主线程的 \`playSound()\` 只是把事件塞队列（微秒级），绝不阻塞。\n- 解码、加载、声卡交互全在音频线程，掉帧与主线程无关。\n- 多个音效同时触发不会累积卡顿——它们排队，音频线程按序处理。\n- 若音频线程处理不过来，队列变长，音效稍延迟播放（通常可接受），而非主线程卡死。\n\n扩展：\n- 优先级队列：重要音效（UI 点击）插队，避免被背景音排队堵住。\n- 合并/去重：同帧多个相同音效可合并为一个，避免叠音爆音。\n- 环形缓冲：固定大小队列，满了丢弃最旧事件（防止内存增长）——比无界队列更安全。\n- 单线程消费也可（不一定要独立线程）：主线程每帧末批量处理队列里的音频事件，仍避免在战斗逻辑中途阻塞。`,
    tags: ["应用", "音频系统", "非阻塞", "生产者消费者", "线程安全"],
  },
  {
    id: "gpp-event-queue-04",
    chapter: "gpp-event-queue",
    level: 4,
    question: `事件队列的有序性保证是什么？它带来哪些潜在问题？`,
    answer:
      `有序性保证：\n事件队列通常是 FIFO（先进先出）——生产者入队的顺序就是消费者处理的顺序。这个有序性是事件队列的核心契约之一。\n\n为什么需要有序：\n- 因果关系：事件 B 依赖事件 A 的结果（如「捡起道具」必须在「道具掉落」之后处理），乱序会导致逻辑错误。\n- 状态一致性：「扣血」事件必须在「死亡判定」事件之前，否则可能先判死亡再扣血，状态错乱。\n- 玩家感知：音效顺序有因果（开枪声→击中声），乱序会违和。\nFIFO 保证生产者表达的事件顺序被消费者尊重。\n\n有序性带来的潜在问题：\n\n1. 队头阻塞（Head-of-line blocking）：\n- 队首事件处理慢（如一个音频要解码大文件），后面所有事件都被堵住，即使它们本可快速处理。\n- 缓解：优先级队列（紧急事件插队）、多队列分通道（UI 音效和背景音乐分两条队列）。\n\n2. 延迟累积：\n- 消费者跟不上生产速度时，队列变长，事件从「入队」到「被处理」的延迟越来越大。\n- 玩家可能看到「先开枪，半秒后才听到枪声」——事件排队太久。\n- 缓解：监控队列长度，超阈值时丢弃低优先级事件（如多余的脚步声）、限流生产者。\n\n3. 有序与并行的矛盾：\n- 若想多线程并行处理事件提速，FIFO 有序性会限制——不能并行处理有依赖的事件。\n- 缓解：按「可并行分组」分队列，组内有序、组间并行；或用依赖图调度。\n\n4. 确定性与调试：\n- FIFO 让事件处理顺序确定，利于录像回放、联机同步。\n- 但事件在队列里「排队等待」的时间是非确定的（取决于消费速度），若事件处理依赖「当前真实时间」会出问题。应让事件携带「逻辑时间戳」而非依赖处理时刻。\n\n5. 丢失与溢出：\n- 无界队列：生产过快会内存爆炸。\n- 有界队列：满了要么阻塞生产者（违背解耦初衷）、要么丢事件（丢哪个？丢旧的还是新的？需策略）。\n- 丢事件可能破坏因果——若丢了「扣血」只留「死亡判定」，逻辑错乱。需保证关键事件不丢（分优先级/持久化关键事件）。\n\n6. 有序性 vs 优先级：\n- 严格 FIFO 有时不是想要的——紧急事件（玩家受伤反馈音）应插队优先。\n- 引入优先级后「严格有序」被打破，需在「因果有序」和「优先调度」间平衡：同一优先级内 FIFO，跨优先级高优先先处理。\n\n7. 跨线程有序的代价：\n- 多生产者入队需加锁或用无锁队列，锁竞争本身是性能瓶颈。\n- 单生产者单消费者可用无锁环形缓冲（SPSC），多对多则需更复杂结构。\n\n综合：事件队列的 FIFO 有序性是它的「正确性基础」（保证因果），但也带来队头阻塞、延迟累积、并行受限等代价。工程上常通过「分优先级、分通道、限流、监控队列长度」来在有序性和实时性间取得平衡——没有纯粹的最优，只有场景适配的取舍。`,
    tags: ["综合", "有序性", "FIFO", "队头阻塞", "延迟", "优先级", "潜在问题"],
  },
];
