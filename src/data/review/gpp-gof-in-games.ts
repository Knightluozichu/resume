import type { ReviewQuestion } from "./types";

/** GoF 模式游戏速览复习题 */
export const gppGofInGamesQuestions: ReviewQuestion[] = [
  {
    id: "gpp-gof-in-games-01",
    chapter: "gpp-gof-in-games",
    level: 1,
    question: "本书从 GoF 23 模式中挑了哪 6 个在游戏中高频使用的模式来讲？",
    answer:
      "本书专门用一章讲 6 个在游戏中最常用的 GoF 模式：\n\n1. 命令模式（Command）：\n把「请求」封装成对象。游戏中用于输入映射（按键→命令）、撤销/重做、录像回放、AI 行为排队。\n\n2. 享元模式（Flyweight）：\n共享细粒度对象的内部状态。游戏中用于海量同类实体（树木、粒子、棋子）共享纹理/网格数据，省内存。\n\n3. 观察者模式（Observer）：\n一对多通知。游戏中用于成就系统、事件广播、UI 监听游戏状态变化。\n\n4. 状态模式（State）：\n对象行为随内部状态变化。游戏中用于角色 AI 的有限状态机（idle/patrol/chase/attack）。\n\n5. 单例模式（Singleton）：\n全局唯一实例。本书对它持批判态度——指出它在游戏中常被滥用，带来隐式耦合和测试困难，只在「确需全局唯一且无更好替代」时才用。\n\n6. 类型对象（Type Object）：\nGoF 原型/抽象工厂的游戏变体——用数据对象（如 Breed）描述类型，运行时可创建新类型而无需改代码。\n\n这 6 个的共同点：都解决游戏中「海量实体 + 频繁变化 + 性能压力」的典型问题，比 GoF 原书的通用例子更贴合游戏语境。",
    tags: ["GoF", "6个模式", "游戏高频"],
  },
  {
    id: "gpp-gof-in-games-02",
    chapter: "gpp-gof-in-games",
    level: 2,
    question: "命令模式在游戏中的典型应用有哪些？",
    answer:
      "命令模式把「做什么」封装成对象，在游戏中有三大典型应用：\n\n1. 输入映射（解耦按键与逻辑）：\n- 不用命令：`if (key == A) player.moveLeft()`，按键和逻辑绑死。\n- 用命令：按键 A 映射成 `MoveLeftCommand`，按键 B 映射成 `JumpCommand`。逻辑只认 `command.execute(actor)`。\n- 收益：支持按键重映射（只改映射表）、手柄/键盘/触屏统一成命令、AI 也可以产生命令操控角色。\n\n2. 撤销/重做（回合制、编辑器）：\n- 每个命令实现 `execute()` 和 `undo()`。执行时压栈，撤销时弹栈反向执行。\n- 适用于关卡编辑器（撤销移动/删除）、回合制游戏（撤回上一步）、策略游戏。\n\n3. 录像与回放（demo / replay）：\n- 录像：不存每帧画面，只存「输入的命令序列 + 随机种子」。回放时按序重放命令即可精确还原。\n- 体积小、可跨平台、可快进。FPS 游戏 demo、RTS 录像都是这个思路。\n\n额外应用：\n- AI 行为排队：AI 把「移动到 X」「攻击 Y」封装成命令入队，逐个执行。\n- 网络同步：把玩家命令序列化发送，远端按序执行，实现 lockstep 联机。\n\n核心收益：把「触发」和「执行」解耦，让同一个执行逻辑能被多种来源（玩家输入、AI、网络、录像）驱动，并支持时序操作（撤销、排队、回放）。",
    tags: ["命令模式", "输入映射", "撤销", "录像"],
  },
  {
    id: "gpp-gof-in-games-03",
    chapter: "gpp-gof-in-games",
    level: 3,
    question: "观察者模式在游戏中如何使用？它有什么局限？",
    answer:
      "使用方式：\n被观察对象（Subject）维护一个观察者列表，状态变化时调用每个观察者的 `onNotify(event)`。游戏里典型场景：\n- 成就系统：`AchievementSystem` 观察游戏事件（如 `EnemyKilledEvent`），达到条件解锁成就。\n- UI 同步：血条 UI 观察玩家血量变化，自动刷新。\n- 解耦通知：战斗系统发事件，音效系统、特效系统、任务系统各自响应，互不依赖。\n\n观察者模式让「事件源」和「响应方」解耦——发事件的不需要知道有几个响应方、各自是谁。\n\n局限与问题：\n\n1. 执行顺序不可控：\n多个观察者的通知顺序取决于注册顺序，是隐式的。如果两个观察者有依赖（B 要在 A 之后跑），这个时序约束藏在注册顺序里，脆弱难维护。\n\n2. 同步阻塞：\n`notify()` 是同步调用，所有观察者在当前帧、当前线程依次执行。某个观察者耗时或抛异常会拖累整个通知链甚至主线程。\n\n3. 调试困难（「幽灵事件流」）：\n事件流转是隐式的——你看到血量变了，但不知道是哪个 Subject 发的通知、哪些观察者响应了。没有显式调用链可追。调试时要靠日志或断点一个个查。\n\n4. 内存泄漏风险：\n观察者忘记取消订阅，Subject 还持有它的引用，导致观察者无法被 GC。长期运行会泄漏。\n\n5. 全局耦合：\n如果是「事件总线」式（所有事件都发到一个中心），各模块通过事件隐式耦合，难以静态分析依赖关系。\n\n6. 过度使用导致「意大利面条」：\n大量事件交织，数据流难以追踪，新人接手时摸不清「这个状态是被谁改的」。\n\n作者建议：观察者适合「真正的一对多广播」场景（如成就、UI 刷新）；如果是固定的一对一通信，直接调用更清晰；如果需要时序保证或异步，考虑事件队列。",
    tags: ["观察者", "应用", "局限", "调试", "事件"],
  },
  {
    id: "gpp-gof-in-games-04",
    chapter: "gpp-gof-in-games",
    level: 4,
    question: "享元模式与对象池都涉及「对象复用」，它们有什么本质区别？",
    answer:
      "相似点：都是为了减少对象开销、提升性能，都涉及「复用」。\n\n本质区别：\n\n1. 复用的维度不同：\n- 享元：复用「内部状态相同」的对象。多个逻辑实例共享同一个享元，省的是「存储」——1 万棵树共享 3 种树的纹理数据。复用是空间维度。\n- 对象池：复用「可重置」的对象实例。一个对象用完归还池，下次再取出来重置复用，省的是「创建/销毁开销」。复用是时间维度。\n\n2. 解决的问题不同：\n- 享元解决「内存占用」——大量对象各存一份重复数据太占内存。\n- 对象池解决「分配/GC 开销」——频繁 new/destroy 导致 GC 停顿或内存碎片。\n\n3. 对象状态的处理不同：\n- 享元对象本身尽量无状态或只有不可变内部状态，可变状态由调用方临时传入。多个调用方「同时」共享一个享元（线程安全需注意）。\n- 对象池里的对象「同一时刻只被一个调用方使用」，用完归还。可以带可变状态，归还前重置即可。\n\n4. 生命周期不同：\n- 享元对象通常长期存在（随种类常驻池中），多个实例同时引用。\n- 对象池对象是「借出-归还」循环，同一实例在不同时刻服务不同调用方。\n\n5. 典型场景：\n- 享元：1 万棵树共享 3 种纹理、1000 个棋子共享 2 种颜色、大量字符共享字形位图。\n- 对象池：子弹每帧创建销毁、粒子系统频繁生成、数据库连接复用。\n\n6. 能否叠加：\n可以——子弹对象池里的每颗子弹引用共享的「子弹类型」享元（含纹理/伤害值），既省内存又省分配开销。\n\n一句话：享元是「同类共享一份」，省内存；对象池是「用完归还复用」，省分配开销。一个管空间，一个管时间。",
    tags: ["对比", "享元", "对象池", "复用"],
  },
];
