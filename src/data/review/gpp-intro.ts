import type { ReviewQuestion } from "./types";

/** 架构、性能与游戏复习题 */
export const gppIntroQuestions: ReviewQuestion[] = [
  {
    id: "gpp-intro-01",
    chapter: "gpp-intro",
    level: 1,
    question: `游戏开发面临的两个核心约束是什么？`,
    answer:
      `两个核心约束：\n\n1. 架构的灵活性（Flexibility / 可维护性）：\n游戏开发充满不确定性——玩法会改、需求会变、内容会膨胀。好的架构要让代码易于理解、易于修改、易于扩展，避免「改一个功能牵连一片」。这要求低耦合、高内聚、清晰的抽象。\n\n2. 运行时性能（Performance）：\n游戏是实时模拟，每帧要在 16ms 内跑完。CPU/GPU/内存都吃紧，不能有明显的卡顿、掉帧、GC 停顿。这要求高效的数据布局、避免热路径上的分配、利用 cache 局部性。\n\n关键矛盾：这两个约束经常互相拉扯——为了性能做的高度内联、紧凑数据布局往往损害可读性和可扩展性；为了架构优雅做的层层抽象、虚函数、间接调用又往往引入性能开销。作者在序章的核心论点就是：游戏程序员要在这两者间做持续的权衡，而不是单方面追求某一边。`,
    tags: ["核心约束", "架构", "性能", "权衡"],
  },
  {
    id: "gpp-intro-02",
    chapter: "gpp-intro",
    level: 2,
    question: `为什么「快速迭代」对游戏开发至关重要？`,
    answer:
      `游戏开发本质是「探索式」的——好玩不好玩只能玩过才知道，没法靠需求文档 upfront 确定。这决定了快速迭代是核心竞争力：\n\n1. 玩法需要试错：\n游戏的「手感」「节奏」「难度曲线」无法在设计文档里算出来，只能通过反复「改一版→玩一版→再改」逼近。迭代越快，试错次数越多，最终质量越高。一天能跑 20 次迭代 vs 一周只能跑 1 次，最终成品差距巨大。\n\n2. 架构是迭代速度的底座：\n如果代码耦合严重，改一个技能要动战斗、UI、存档三个模块，每次改动都引入 bug，迭代速度就被架构拖死。解耦良好的架构让「加一个新技能」「调一个数值」只改局部，分钟级完成。\n\n3. 反例——「用速度换架构」的陷阱：\n很多团队为了赶里程碑，短期内堆砌 if-else、复制粘贴、跳过抽象，确实当周快了。但随着代码腐化，每次新改动的成本指数上升，几周后迭代速度反而崩溃。作者强调：不要为了「当下的速度」牺牲「持续迭代的速度」。\n\n4. 编译/构建时间也是迭代速度：\n改一行代码要等 5 分钟编译，一天迭代不了几次。所以游戏团队也重视增量编译、热重载、脚本化——都是为了让「改→看效果」的回路最短。\n\n一句话：游戏是迭代出来的，不是设计出来的；架构的价值就是让迭代可持续地快。`,
    tags: ["快速迭代", "试错", "架构", "开发流程"],
  },
  {
    id: "gpp-intro-03",
    chapter: "gpp-intro",
    level: 3,
    question: `举例说明「解耦」如何具体地帮助游戏开发。`,
    answer:
      `解耦的核心收益是「改动局部化」——改 A 不会牵连 B。三个具体例子：\n\n例子 1：输入与逻辑解耦（命令模式）：\n- 不解耦：按键处理直接写 \`if (key == SPACE) player.jump()\`，逻辑和输入绑死。想支持手柄、重映射、AI 操控、录像回放都得改一堆地方。\n- 解耦后：按键映射成 \`Command\` 对象，逻辑只认 \`command.execute(player)\`。换手柄只改映射表；撤销只反向执行；录像只存命令序列。输入、逻辑、回放各自独立演进。\n\n例子 2：音频与主线程解耦（事件队列）：\n- 不解耦：\`playSound()\` 同步加载解码播放，主线程卡顿掉帧。\n- 解耦后：\`playSound()\` 只往队列里塞一个 \`PlaySoundEvent\`，独立的音频线程消费队列并执行实际播放。主线程不被阻塞，音频模块可独立优化。\n\n例子 3：实体行为解耦（组件模式）：\n- 不解耦：用继承 \`Entity → Character → Player\`，玩家、敌人、NPC 共用一堆父类字段，加一个「会飞的角色」就要在父类塞 \`canFly\` 标志，污染所有子类。\n- 解耦后：\`GameObject\` 持有 \`Physics\`/\`Render\`/\`Input\`/\`Fly\` 等组件，按需组合。加飞行能力只需挂 \`FlyComponent\`，不碰其他实体。\n\n总结：解耦让「加功能」「换实现」「做调试工具」「支持新平台」都变成局部修改。代价是多一层抽象，但在需要长期迭代的游戏项目里，这个代价值得付。`,
    tags: ["应用", "解耦", "命令模式", "事件队列", "组件模式"],
  },
  {
    id: "gpp-intro-04",
    chapter: "gpp-intro",
    level: 4,
    question:
      `架构灵活性和运行时性能经常互相矛盾，如何在游戏项目中做权衡？`,
    answer:
      `两者矛盾的本质：架构灵活性追求「抽象、间接、可替换」，性能追求「内联、紧凑、直连」。两者方向相反。权衡框架如下：\n\n1. 先测量，再优化——用数据驱动决策：\n不要凭直觉为「可能的性能问题」牺牲架构。先用 Profiler 找到真正的热点（往往只有 10% 的代码占 90% 时间），只在这 10% 做性能化，其余 90% 优先保持架构清晰。把好架构用在非热点，把性能 hack 限定在热点。\n\n2. 分层策略——热点内联，边缘解耦：\n- 热路径（每帧百万次调用，如粒子更新、碰撞检测）：优先性能，用 SoA 数据布局、内联、对象池、空间分区，可以牺牲一些可读性。\n- 冷路径（偶发调用，如技能系统、UI、存档）：优先架构，用组件、事件队列、命令模式，享受解耦红利。\n- 同一份代码不必一个标准，按「调用频率」分区施策。\n\n3. 抽象的「零成本」边界：\n有些抽象在现代编译器下几乎零开销——如 \`inline\` 的模板、编译期多态、值类型的组件数组。优先选这些「不付运行时代价也能拿架构收益」的方案，而不是一上来就用虚函数。即「设计时按接口思考，实现时按数据落地」。\n\n4. 保留可回退的开关：\n性能敏感的优化做成可配置——如对象池开关、SoA/AoS 切换、LOD 分级。开发期用架构友好的版本，发布时切到性能版本。用抽象隔离「开发态」和「发布态」。\n\n5. 团队与阶段因素：\n- 原型期：玩法还没定，优先灵活架构，快速试玩法。\n- 生产期：玩法锁定，逐步对热点做性能化。\n- 上线后：用线上 Profiling 数据持续微调。\n- 小团队/独立游戏：人少，架构烂了没人维护得动，更要保架构；大团队 3A：性能压力大，热点化更激进。\n\n6. 反模式警惕：\n- 「全面性能化」：到处手动内联、全局变量、裸指针，架构崩溃后没法迭代。\n- 「全面抽象」：每处都虚函数、每处都接口，热点跑不动。\n\n一句话：不是「架构 vs 性能」二选一，而是「在热路径上为性能让步，在冷路径上为架构让步，用 Profiler 划清边界」。`,
    tags: ["综合", "权衡", "架构vs性能", "分层策略", "Profiler"],
  },
];
