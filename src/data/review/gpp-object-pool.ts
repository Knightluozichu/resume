import type { ReviewQuestion } from "./types";

/** 对象池复习题 */
export const gppObjectPoolQuestions: ReviewQuestion[] = [
  {
    id: "gpp-object-pool-01",
    chapter: "gpp-object-pool",
    level: 1,
    question: `对象池（Object Pool）的意图是什么？`,
    answer:
      `意图：预先创建一批对象放入「池」中，需要时从池里借出，用完归还池里复用，而不是反复 new 创建和 destroy 销毁——避免频繁内存分配/GC 带来的性能开销。\n\n核心机制：\n- 启动时预分配 N 个对象放入空闲池。\n- \`acquire()\`：从空闲池取一个对象，初始化后交给调用方使用。\n- \`release(obj)\`：用完后归还，重置状态，放回空闲池。\n- 下次 \`acquire()\` 复用归还的对象，不再 new。\n\n解决的问题：\n- 游戏中子弹、粒子等对象每帧大量创建销毁。频繁 new 触发内存分配（慢），频繁销毁触发 GC（停顿卡帧）。\n- 对象池把「创建/销毁」变成「借出/归还」，分配开销摊薄到启动时一次，运行时零分配、零 GC。\n\n典型用途：子弹、粒子、敌人、网络消息包、数据库连接、线程。任何「短期、高频创建销毁」的对象都适合池化。`,
    tags: ["意图", "复用", "避免GC", "借出归还"],
  },
  {
    id: "gpp-object-pool-02",
    chapter: "gpp-object-pool",
    level: 2,
    question: `对象池与「每帧 new」相比，性能差异体现在哪里？`,
    answer:
      `每帧 new 的问题：\n\n1. 内存分配开销：\n- 每次 new 向堆申请内存，涉及空闲链表查找、可能的系统调用，微秒级开销。高频（每帧几百次）累积成毫秒级，吃掉帧预算。\n\n2. 内存碎片：\n- 反复 new/destroy 使堆碎片化——空闲内存被割成小块，虽总量够但找不到连续大块，最终分配失败或触发整理。\n\n3. GC 停顿（托管语言如 C#/Java 尤甚）：\n- 大量短期对象快速进入新生代，填满触发 minor GC。GC 时需暂停所有线程（STW），几毫秒到几十毫秒，造成卡顿掉帧。\n- 对象越多、创建越快，GC 越频繁，停顿越长——形成「越忙越卡」的恶性循环。\n\n4. cache 不友好：\n- new 的对象散布在堆各处，遍历时地址跳跃，cache miss。\n\n对象池的改善：\n\n1. 零运行时分配：\n- 启动时一次性预分配 N 个对象，运行时 \`acquire/release\` 只是移动指针/索引，无堆分配。\n- 帧预算内「零分配」是高性能游戏的关键指标。\n\n2. 无 GC 压力：\n- 对象不被销毁（一直被池引用），不进 GC 回收链。GC 看到的是稳定的长生命周期对象，几乎不触发因短生命周期对象导致的 minor GC。\n- C# 中可进一步用「非托管内存」或值类型数组彻底避开 GC。\n\n3. 内存紧凑：\n- 池内对象预分配在连续数组中（或连续内存块），cache 友好。遍历子弹时地址连续，命中率高。\n\n4. 稳定的内存占用：\n- 池大小固定，内存占用可预测，不会因瞬时大量 new 导致内存峰值。\n\n性能对比（示意，子弹系统每帧 500 颗）：\n- 每帧 new：500 次分配 + 触发 GC，假设每帧多花 2ms，60fps 下吃掉 12% 帧预算，且 GC 停顿随机掉帧。\n- 对象池：预分配 500 个，每帧 \`acquire/release\` 仅索引操作，开销 <0.1ms，零 GC，帧时间稳定。\n\n代价（对象池不是免费午餐）：\n- 预分配内存常驻：即使空闲也占内存（用空间换时间）。\n- 对象需手动 \`reset()\`：归还前要清理状态，忘了会脏数据。\n- 生命周期管理变复杂：忘记归还会泄漏（池空了借不到），归还后还持有引用会误用已归还对象。\n- 池容量固定，峰值超容量需策略（见 L4）。\n\n综合：对象池用「预分配 + 复用」把运行时分配/GC 开销降到接近零，是游戏热路径对象管理的标配。代价是手动生命周期管理和常驻内存，但对「高频短期对象」绝对物超所值。`,
    tags: ["对比", "每帧new", "GC", "内存分配", "性能差异"],
  },
  {
    id: "gpp-object-pool-03",
    chapter: "gpp-object-pool",
    level: 3,
    question: `设计一个游戏子弹系统，用对象池避免每帧创建/销毁子弹。`,
    answer:
      `设计：\n\n1. 子弹对象（可重置）：\n\`Bullet\` 含 \`x, y, vx, vy\`（位置速度）、\`active\`（是否在用，默认 false）、\`damage\`、\`owner\`。\`reset(x, y, vx, vy, damage, owner)\` 初始化字段并设 \`active = true\`。\`update(dt)\` 里若 \`active\` 则移动（\`x += vx * dt\`），出界则 \`deactivate()\`。\`onHit(target)\` 造成伤害后 \`deactivate()\`。\`deactivate()\` 设 \`active = false\` 并清理 \`owner\` 引用——标记空闲，留在数组里等复用。\n\n2. 对象池：\n\`BulletPool\` 构造时预分配 \`capacity\` 个 \`Bullet\` 存入数组。\`spawn(x, y, vx, vy, damage, owner)\` 线性扫描找 \`!active\` 的子弹，调 \`reset\` 后返回；池满返回 null。\`update(dt)\` 遍历所有子弹调 \`update\`（数组连续，cache 友好）。\`render()\` 遍历画 \`active\` 的子弹。\n\n3. 使用（射击）：\n\`const bulletPool = new BulletPool(500)\`——预分配 500 颗。\`onShoot()\` 调 \`bulletPool.spawn(player.x, player.y, dirX*500, dirY*500, 10, player)\`——不 new，从池借。每帧 \`gameLoop(dt)\` 调 \`bulletPool.update(dt)\`（移动 + 出界/击中自动 deactivate 归还）和 \`bulletPool.render()\`。\n\n效果：\n- 射击时 \`spawn()\` 复用空闲子弹，零 new、零分配。\n- 子弹出界或击中时 \`deactivate()\` 标记空闲，下次 spawn 复用——无 destroy、无 GC。\n- 500 颗子弹数组连续，update/render 遍历 cache 友好。\n- 整局游戏运行期间，子弹相关内存分配只有启动时的 500 次 new。\n\n优化点：\n1. 空闲查找优化：线性扫描 O(N) 找空闲慢。维护一个「空闲栈」，acquire 弹栈取索引，deactivate 压栈，O(1)。\n2. 紧凑布局：活跃子弹聚到数组前部（swap 到尾部 deactivate），遍历只扫活跃部分，减少 \`if active\` 分支。\n3. 分类型池：不同子弹类型（普通/激光/导弹）分池，避免类型混杂的 reset 开销。\n4. 与享元结合：子弹对象引用共享的「子弹类型」享元（纹理/伤害表），池里的子弹只存实例状态（位置速度），省内存。\n\n关键原则：池化对象必须「可重置」——\`reset()\` 清干净上次的残留状态，保证复用时像新的一样。漏 reset 是脏数据 bug 的常见来源。`,
    tags: ["应用", "子弹系统", "对象池", "预分配", "deactivate", "空闲栈"],
  },
  {
    id: "gpp-object-pool-04",
    chapter: "gpp-object-pool",
    level: 4,
    question: `对象池的容量是固定的，当池满了（借不到对象）时该怎么办？有哪些策略及取舍？`,
    answer:
      `池满场景：\`acquire()\` 时空闲池为空，所有对象都在用。不同策略影响游戏表现和正确性：\n\n1. 直接丢弃新请求（静默失败）：\n- \`acquire()\` 返回 null，调用方放弃创建。\n- 适合：对象非关键，丢了无伤（如多余的粒子、装饰特效）。少几颗粒子玩家察觉不到。\n- 取舍：简单无副作用，但关键对象（子弹、技能）被丢弃会导致「开枪没子弹」的严重体验问题。\n\n2. 复用最旧/最低优先级的活跃对象：\n- 池满时强制「驱逐」一个在用对象（最旧的子弹、最小的粒子、最远的特效），重置后给新请求用。\n- 适合：对象「新旧有优先级」且旧的失效可接受（如子弹飞远了本来就快失效，复用它无感）。\n- 取舍：被驱逐的对象「消失」可能被察觉（远处的子弹突然没了），但比「新子弹出不来」好。需选对「驱逐目标」避免踢掉关键对象。\n\n3. 阻塞等待（直到有对象归还）：\n- acquire 阻塞调用方，直到有人 release。\n- 适合：非实时场景（如服务器连接池），对象必须拿到且可等。\n- 取舍：游戏主线程绝不能阻塞——会卡帧。游戏里基本不用此策略。\n\n4. 动态扩容（突破固定容量）：\n- 池满时临时 new 新对象加入池，容量弹性增长。\n- 适合：峰值不可预测、且偶尔超容可接受。\n- 取舍：违背「零运行时分配」初衷，峰值时仍会 new/GC。可设上限避免无限增长。适合「平均负载低、偶发峰值」的场景。\n\n5. 预设足够大的容量（容量规划）：\n- 根据设计上限设池大小（如同屏最多 500 颗子弹，池设 600 留余量），保证正常运行永不超容。\n- 适合：能预估上限的场景。\n- 取舍：内存常驻可能浪费（设大了平时用不满）；设小了仍会满。需结合 Profiler 实测调整。\n\n6. 降级表现（减少生成频率）：\n- 池接近满时，生成端限流——如机枪射速随池占用率下降，或粒子 emitter 减少发射率。\n- 适合：可接受「动态降画质保稳定」的场景。\n- 取舍：体验随负载变化，需精心调参避免玩家察觉突兀。\n\n综合取舍框架：\n- 关键对象（子弹、玩家投射物）：策略 5（容量规划足量）+ 策略 2（极端时复用最旧的）兜底。绝不丢弃（策略 1 不可接受）。\n- 次要对象（粒子、装饰特效）：策略 1（丢弃）或 6（限流降级）——玩家无感，简单。\n- 不可预测峰值：策略 4（动态扩容）配合上限，平均零分配、峰值容许少量 new。\n\n实践建议：\n1. 监控池占用率：记录 \`acquire 失败次数\`、\`峰值占用\`，用 Profiler 数据驱动容量调整。\n2. 容量留 20% 余量：设计上限 + 余量，避免临界频繁触发策略逻辑。\n3. 明确失败策略的语义：\`acquire\` 返回 null 时调用方必须处理（而非忽略），日志记录失败频率以便优化。\n4. 分优先级多池：关键子弹和装饰粒子分池，各自策略不同，互不影响。\n\n一句话：没有万能策略。按「对象重要性」分级——关键的保量（容量规划+复用旧者），次要的容丢（丢弃/限流），不可预测的弹性扩容。关键是「池满」是可预期、可处理的，而非崩溃。`,
    tags: ["综合", "池满", "容量", "驱逐", "动态扩容", "限流", "取舍"],
  },
];
