import type { ReviewQuestion } from "./types";

/** 空间分区复习题 */
export const gppSpatialPartitionQuestions: ReviewQuestion[] = [
  {
    id: "gpp-spatial-partition-01",
    chapter: "gpp-spatial-partition",
    level: 1,
    question: "空间分区（Spatial Partition）的意图是什么？",
    answer:
      "意图：把游戏空间划分成若干区域（格子/树节点），让「找附近对象」时只检查同一区域及相邻区域，避免与全场所有对象两两比较，把碰撞检测等空间查询从 O(N²) 降到接近 O(N)。\n\n核心思路：\n- 把空间分成网格或树形结构，每个对象根据位置归入对应区域。\n- 查询「某对象附近有谁」时，只遍历它所在区域及邻近区域的对象，而非全场。\n- 因为大部分对象离查询对象很远，分区域后远距离对象直接被剔除，比较次数大减。\n\n解决的问题：\n- 碰撞检测：N 个对象两两比较是 O(N²)，1000 个对象 = 100 万次比较，每帧跑不动。\n- 邻近查询：找「某点半径内的敌人」「视野内的目标」同样需避免全量扫描。\n- 空间分区利用「空间局部性」——相近的对象才可能碰撞/相关，远的不用比。\n\n典型用途：碰撞检测、视锥剔除、范围伤害判定、AI 感知（找最近敌人）、光线投射。",
    tags: ["意图", "减少比较", "空间查询", "O(N²)→O(N)"],
  },
  {
    id: "gpp-spatial-partition-02",
    chapter: "gpp-spatial-partition",
    level: 2,
    question: "均匀网格分区与四叉树/八叉树/BVH 有什么区别？",
    answer:
      "均匀网格（Uniform Grid）：\n- 把空间切成大小相同的方格，每格维护一个对象列表。对象按坐标归入对应格。\n- 查询：只查所在格及周围 8 格（3×3）的对象。\n- 优点：实现极简单，查询 O(1) 找格 + O(格内对象数) 比较。对象均匀分布时高效。\n- 缺点：对象大小/密度差异大时不佳——大对象跨多格需登记多次；稀疏区空格浪费、密集区单格对象爆多。空间无界时网格也需处理边界。\n- 适合：2D 游戏、对象大小相近、分布均匀（如弹幕、粒子碰撞）。\n\n四叉树（Quadtree，2D）/ 八叉树（Octree，3D）：\n- 递归细分空间：根节点是整个空间，对象多了就四分（2D）/八分（3D）成子节点，子节点对象多了再分，直到每节点对象数低于阈值。\n- 查询：从根递归找与查询范围相交的节点，只比这些节点里的对象。\n- 优点：自适应——密集区细分（每节点对象少），稀疏区粗分（节点少）。对象大小差异大也较好处理（大对象留在浅层节点）。\n- 缺点：实现复杂；对象移动时需在树间迁移（增删开销）；树不平衡时退化。查询是 O(log N + 结果数)，但常数比网格大。\n- 适合：3D 游戏、对象分布不均、对象大小有差异（如开放世界）。\n\nBVH（Bounding Volume Hierarchy，层次包围盒）：\n- 不是分空间，而是分「对象」——把对象用包围盒（AABB/球）包住，递归合并成树，每个内部节点的包围盒包住其子节点的包围盒。\n- 查询：从根递归，若查询范围与节点包围盒不相交则剪掉整棵子树。\n- 优点：对象驱动（不依赖空间均匀），适合动态场景——对象移动只需更新它的包围盒并可能重平衡树，不必像网格/四叉树那样改空间结构。适合大小差异极大的对象。\n- 缺点：树需动态维护/重平衡（对象移动后包围盒变了，可能需重构）；实现最复杂。\n- 适合：光线追踪、动态 3D 场景碰撞、对象大小差异大（如一棵树和一颗粒子共存）。\n\n对比表：\n- 划分对象：网格=分空间（固定格），四叉树=分空间（自适应），BVH=分对象（包围盒树）。\n- 自适应：网格=否，四叉树/BVH=是。\n- 动态对象维护：网格=O(1) 改格归属，四叉树=O(log N) 迁移，BVH=更新包围盒+可能重平衡。\n- 实现复杂度：网格<四叉树<BVH。\n- 适合场景：网格=2D 均匀，四叉树=3D 不均，BVH=动态/大小差异大。\n\n选择：简单 2D 用网格；3D 不均匀用四叉/八叉树；动态复杂场景用 BVH。也可混合（粗层网格+细层四叉树）。",
    tags: ["对比", "网格", "四叉树", "八叉树", "BVH", "空间结构"],
  },
  {
    id: "gpp-spatial-partition-03",
    chapter: "gpp-spatial-partition",
    level: 3,
    question:
      "设计一个 2D 游戏的均匀网格碰撞检测，说明如何把 O(N²) 降到接近 O(N)。",
    answer:
      "问题（不用空间分区）：\nN 个子弹/敌人，碰撞检测两两比较：\n```\nfor (i = 0; i < N; i++)\n  for (j = i+1; j < N; j++)\n    if (collide(objects[i], objects[j])) handleCollision()\n```\n- O(N²)，1000 对象 = 50 万次比较，每帧跑不动。\n\n均匀网格设计：\n\n1. 划分网格：\n```\nconst CELL_SIZE = 64   // 格子边长，通常约等于对象平均大小\nclass Grid {\n  cells: Map<number, Set<GameObject>>  // key = 格子坐标编码\n  // 格子坐标 = floor(x / CELL_SIZE), floor(y / CELL_SIZE)\n}\n```\n\n2. 插入对象（按位置归格）：\n```\ninsert(obj) {\n  const cx = Math.floor(obj.x / CELL_SIZE)\n  const cy = Math.floor(obj.y / CELL_SIZE)\n  const key = encode(cx, cy)   // 如 cy * 100000 + cx\n  this.cells.getOrInit(key).add(obj)\n}\n```\n- 每帧开始清空网格，重新插入所有动态对象（或增量更新移动对象）。\n\n3. 查询碰撞（只比同格及相邻 8 格）：\n```\ncheckCollisions() {\n  for (each cell (cx, cy) with objects) {\n    // 同格内两两比\n    for (each pair in cell) checkPair(a, b)\n    // 与右、下、右下、左下 4 个邻格比（避免重复）\n    for (dir of [(1,0),(0,1),(1,1),(-1,1)]) {\n      const neighbor = cell(cx+dir.x, cy+dir.y)\n      for (a in cell) for (b in neighbor) checkPair(a, b)\n    }\n  }\n}\n```\n- 只比 3×3 范围内的对象，远处的不比。\n\n4. 复杂度分析：\n- 假设 N 个对象均匀分布在 M 个格子里，每格平均 k = N/M 个对象。\n- 每格内比较：O(k²)，全场 M 格共 O(M·k²) = O(N·k)。\n- 若对象分布均匀且 CELL_SIZE 合适，每格对象数 k 是常数（如 5~10），则总比较 O(N·k) ≈ O(N)。\n- 从 O(N²) 降到 O(N)，1000 对象从 50 万次降到几千次。\n\n5. 关键参数——CELL_SIZE 选择：\n- 太小：对象跨多格，需登记多次，查询时邻格多，开销升。\n- 太大：单格对象多，格内 O(k²) 退化。\n- 经验：CELL_SIZE ≈ 对象最大直径，保证对象最多跨 2×2 格，且每格对象数受控。\n\n6. 跨格对象处理：\n- 大对象（直径 > CELL_SIZE）会跨多格。插入时登记到所有覆盖的格，查询时去重（用对象 ID 集合避免重复比较）。\n- 或为大小差异大的对象分层网格（粗网格放大对象、细网格小对象）。\n\n7. 动态更新：\n- 简单做法：每帧清空网格，重新插入所有对象。O(N) 重建，简单可靠。\n- 优化：对象移动时，若跨格才从旧格删除、插入新格（增量更新），避免全量重建。\n\n8. 效果实测：\n- 1000 个均匀分布对象，CELL_SIZE 合适：比较次数从 ~50 万降到 ~5000，提速 100 倍。\n- 极端密集（全挤在一格）：退化回 O(N²)，但实际游戏很少这样。\n\n这就是经典弹幕游戏、2D 动作游戏碰撞检测的标准做法——简单高效，把「比较」限制在空间邻域内。",
    tags: ["应用", "均匀网格", "碰撞检测", "O(N²)→O(N)", "CELL_SIZE"],
  },
  {
    id: "gpp-spatial-partition-04",
    chapter: "gpp-spatial-partition",
    level: 4,
    question:
      "空间分区结构在对象移动时如何维护？更新成本有哪些权衡？",
    answer:
      "对象移动带来的挑战：\n空间分区的核心是「对象位置 → 所属区域」的映射。对象移动后可能跨区域，分区结构必须更新，否则对象「实际位置」与「登记区域」不符，查询会漏掉或错配。维护成本因结构而异：\n\n1. 均匀网格——最简单：\n- 对象移动后，计算新坐标对应的格子。\n- 若新格 == 旧格：无需操作（大部分情况，对象小幅移动不出格）。\n- 若跨格：从旧格列表删除，插入新格列表。O(1) 哈希操作。\n- 简单做法：每帧清空网格全量重建 O(N)——对象多时也有开销，但实现零出错。\n- 增量做法：只处理跨格对象，O(跨格对象数)，通常远小于 O(N)。\n- 权衡：网格维护最便宜，适合高频移动对象（子弹、粒子）。\n\n2. 四叉树/八叉树——中等复杂：\n- 对象移动后新位置可能落入不同子节点。\n- 需从旧节点删除，沿树找到新位置所属叶子节点插入。O(log N)。\n- 树可能需重平衡：某节点对象多了要再分裂，某节点空了或对象少了要合并（回收子节点）。分裂/合并是 O(log N) 但实现繁琐。\n- 若对象频繁跨越节点边界（来回弹跳），反复增删导致树抖动，性能波动。\n- 权衡：查询高效但维护贵，适合移动较慢、分布不均的场景（开放世界 NPC、建筑）。高频小对象用网格更划算。\n- 优化：设「懒更新」——对象移动不立刻改树，标记 dirty，定期批量重建树。牺牲一致性换维护成本降低。\n\n3. BVH——维护最复杂但适合动态：\n- 对象移动后，更新它的包围盒（O(1) 改字段），但其祖先节点的包围盒可能需向上更新（O(log N)）。\n- 更糟的是，移动后包围盒可能变大导致树质量下降（重叠增多，查询剪枝失效），需「重平衡」——旋转/合并节点或部分重构。\n- 频繁移动会让 BVH 逐渐退化（变成线性链），需周期性全局重构 O(N log N)。\n- 权衡：BVH 对「大小差异大、需光线投射」的场景不可替代，但动态维护成本最高。适合「对象移动但树能容忍」的场景（光线追踪、刚体物理）。\n\n通用维护权衡维度：\n\n1. 重建 vs 增量：\n- 全量重建（每帧/定期重建结构）：实现简单、保证结构平衡，但 O(N) 或 O(N log N) 开销固定。适合对象数稳定、重建快的网格。\n- 增量更新（只改移动对象）：O(移动对象数) 开销低，但结构可能逐渐退化（树不平衡、网格对象堆积），需定期重建补救。适合对象大部分静止、少部分移动的场景。\n\n2. 更新频率：\n- 每帧更新：保证一致，但开销大。适合所有对象都在动的场景。\n- 隔帧/分批更新：把对象分批，每帧只更新一部分，降低单帧开销。牺牲瞬时一致性（某对象可能短暂在错区域），适合可容忍几帧延迟的场景。\n- 按需更新：对象移动距离超过阈值才更新分区归属。小幅移动不更新，省开销。\n\n3. 一致性 vs 性能：\n- 严格一致（移动即更新）：查询永远准确，但移动频繁时维护成本主导。\n- 宽松一致（延迟更新/懒重建）：维护便宜，但查询可能漏掉刚移动的对象（碰撞漏检）。需根据游戏类型权衡——精确碰撞检测需严格，粗略视锥剔除可宽松。\n\n4. 结构选择对维护的影响：\n- 对象移动快、大小均匀 → 网格（维护最便宜）。\n- 对象移动慢、分布不均 → 四叉树（查询好，维护可接受）。\n- 对象大小差异大、需光线投射 → BVH（维护贵但不可替代）。\n- 混合：粗层网格（快速剔除远距离）+ 细层四叉树（密集区精确），各取所长。\n\n5. 抖动问题：\n- 对象在格/节点边界来回小幅移动，反复跨区，导致频繁增删。\n- 缓解：边界滞后（hysteresis）——跨格后设一个小阈值，回退不超过阈值不切回，避免边界抖动。\n\n综合：空间分区的维护成本是「查询效率」的对价。网格维护最便宜但适应差；树/BVH 查询好但维护贵。关键是按「对象移动频率、大小分布、查询类型」选结构，并在「严格一致」与「宽松延迟」间按游戏类型权衡。没有免费午餐——分区省下的比较次数，部分要花在维护结构上，净收益取决于场景适配。",
    tags: ["综合", "维护成本", "移动更新", "重建vs增量", "抖动", "权衡"],
  },
];
