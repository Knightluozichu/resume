import type { ReviewQuestion } from "./types";

export const gsaDbShardingQuestions: ReviewQuestion[] = [
  {
    id: "gsa-db-sharding-1",
    chapter: "gsa-db-sharding",
    level: 2,
    question: `哈希分片、范围分片、一致性哈希各自的特点与适用场景？`,
    answer:
      `哈希分片：对分片键取模，数据均匀分布，查询可精确定位单分片；缺点是加减库要全量 rehash 迁移。适合分片数固定的场景，如玩家数据按 uid 取模。范围分片：按分片键区间划分，加减库只动边界；缺点是易热点（新注册玩家全挤最新区间）。适合时间序列数据如日志、交易流水。一致性哈希：分片键和库节点都哈希到环上，顺时针找最近库；加减库只影响相邻区间，迁移量最小；缺点是数据可能不均匀（需加虚拟节点）。游戏服务器扩容频繁，多用一致性哈希。`,
    tags: ["分库分表", "哈希分片", "一致性哈希"],
  },
  {
    id: "gsa-db-sharding-2",
    chapter: "gsa-db-sharding",
    level: 3,
    question: `为什么分片键选择至关重要？举一个好分片键和坏分片键的例子。`,
    answer:
      `分片键决定数据分布和查询模式。好分片键能让大部分业务查询只命中单分片，避免跨分片归并。好的例子：玩家数据用 uid 分片——查「我的背包」「我的任务」天然单分片命中。坏的例子：用自增 id 分片——新增玩家全落最后一个库（热点），且查询「某公会成员」要扫所有库。更坏的例子：用时间分片——近期数据热点、历史库闲置，且查「某玩家近 30 天」可能跨多个时间分片。实践中常双写：玩家表按 uid 分片（查个人快），公会表按 guild_id 分片（查公会单分片），用冗余存储换查询效率。`,
    tags: ["分片键", "跨分片查询"],
  },
  {
    id: "gsa-db-sharding-3",
    chapter: "gsa-db-sharding",
    level: 3,
    question: `分库分表后跨分片 JOIN 失效，如何解决？`,
    answer:
      `方案：①冗余表/双写——玩家表按 uid 分片，公会成员表按 guild_id 分片，同一公会成员落同一库，查公会 JOIN 变单库 JOIN；②应用层归并——分别查各分片再在内存合并，适合少量数据（如查 10 个好友信息）；③数据同步到宽表——用 Canal 等工具把分片数据同步到一张汇总表或 Elasticsearch，复杂查询走汇总表；④避免 JOIN——把需要 JOIN 的字段冗余进主表（如玩家表里冗余 guild_name）。原则：90% 的高频查询靠分片键命中单分片，剩下 10% 跨分片查询靠冗余或汇总表兜底。`,
    tags: ["分库分表", "JOIN", "冗余"],
  },
  {
    id: "gsa-db-sharding-4",
    chapter: "gsa-db-sharding",
    level: 4,
    question: `分库分表后全局唯一 id 怎么生成？几种方案的取舍？`,
    answer:
      `方案：①UUID——全局唯一无协调，但 128 位太长、无序（B+树插入碎片多），不适合主键；②数据库自增段——每库分配一段（库0发 1-1000，库1发 1001-2000），用完再申请，简单但有协调开销；③雪花算法（Snowflake）——64 位 = 时间戳 + 机器 id + 序列号，单调递增、无协调、高性能，是主流方案；④Redis INCR——用 Redis 原子自增生成 id，简单但依赖 Redis 可用性。游戏服务器多用雪花算法：时间戳保证趋势递增（B+树友好），机器 id 区分分库，序列号防同毫秒冲突。注意机器 id 分配要唯一（用 ZooKeeper 或手动配置）。`,
    tags: ["分库分表", "全局 id", "雪花算法"],
  },
];
