import type { ReviewQuestion } from "./types";

export const gspThreadModelQuestions: ReviewQuestion[] = [
  {
    id: "gsp-thread-model-1",
    chapter: "gsp-thread-model",
    level: 2,
    question: `为什么逻辑线程是单线程？多线程有什么问题？`,
    answer:
      `游戏状态高度耦合——一次攻击可能修改攻击者、被攻击者、Buff、背包、排行榜等多个对象。多线程并行处理需要大量加锁，容易死锁，且竞态条件极难复现调试。单线程串行处理完全避免锁和竞态：无需加锁、执行顺序确定、bug 可复现。性能不够时按场景分片到多个逻辑线程，每个线程内仍保持串行。`,
    tags: ["线程模型", "逻辑线程", "无锁"],
  },
  {
    id: "gsp-thread-model-2",
    chapter: "gsp-thread-model",
    level: 2,
    question: `IO 线程和逻辑线程如何通信？为什么用无锁队列？`,
    answer:
      `IO 线程把收到的消息投递到逻辑线程的输入队列，逻辑线程把回包投递到 IO 线程的发送队列。这是单生产者-单消费者模型，可用 std::atomic 实现无锁环形队列。用无锁队列而非互斥锁的原因是每条消息都要入队/出队，万级 QPS 下锁竞争成为瓶颈。无锁队列避免线程阻塞和上下文切换，性能更高。`,
    tags: ["线程模型", "无锁队列", "线程通信"],
  },
  {
    id: "gsp-thread-model-3",
    chapter: "gsp-thread-model",
    level: 3,
    question: `时间轮和最小堆两种定时器实现各有什么优劣？`,
    answer:
      `时间轮将定时任务按到期时间放入环形数组槽位，添加和到期检查都是 O(1)，适合大量短周期定时任务（如 Buff 过期、心跳检测）。最小堆按到期时间构建堆，堆顶是最早到期的任务，添加是 O(log n)，适合定时任务数量适中的场景。时间轮更高效但有精度限制（槽位粒度），最小堆更灵活但大量任务时开销增大。`,
    tags: ["定时器", "时间轮", "最小堆"],
  },
  {
    id: "gsp-thread-model-4",
    chapter: "gsp-thread-model",
    level: 1,
    question: `IO 线程、逻辑线程、定时器线程各自的职责边界是什么？为什么不能混在一起？`,
    answer:
      `IO 线程只做收发数据和解包投递，不碰游戏逻辑；逻辑线程只做业务计算，不做网络 IO；定时器线程只做到期检测和投递，不执行业务逻辑。分离原因：职责不同导致优化方向不同——IO 追求高并发吞吐（epoll 事件循环），逻辑追求确定性（单线程串行无锁），定时器追求精准调度（不阻塞）。混在一起会导致：网络 IO 阻塞逻辑处理、锁竞争破坏确定性、定时任务干扰业务执行。`,
    tags: ["线程模型", "职责分离", "架构"],
  },
];
