import type { ReviewQuestion } from "./types";

export const hcwFileSystemQuestions: ReviewQuestion[] = [
  {
    id: "hcw-file-system-1",
    chapter: "hcw-file-system",
    level: 2,
    question: "文件系统的作用是什么？没有文件系统直接读写磁盘会有什么问题？",
    answer:
      "文件系统是操作系统管理磁盘上数据的机制，把磁盘的扇区（最底层的数据块）抽象成「文件」和「目录」的层级结构。核心功能：①命名——给数据起人类可读的名字（如 report.docx），而不是记住它在第 1234 扇区；②组织——用目录（文件夹）把文件分类管理，形成树形结构；③分配——跟踪哪些磁盘块空闲、哪些被占用，为新文件分配空间；④元数据——记录每个文件的大小、创建时间、权限、所有者等信息；⑤完整性——异常断电时尽量保证数据不损坏（日志机制）。没有文件系统直接读写磁盘的问题：①必须自己记住数据在哪个扇区，像在巨型仓库里没有货架标签找东西；②无法区分文件边界，所有数据连成一片；③多用户无法隔离和共享；④删除数据后无法知道哪些扇区可复用。文件系统把「磁盘扇区」的物理世界抽象成「文件目录」的逻辑世界，是操作系统最重要的抽象之一。",
    tags: ["文件系统", "抽象"],
  },
  {
    id: "hcw-file-system-2",
    chapter: "hcw-file-system",
    level: 2,
    question: "文件系统的目录结构是如何组织的？什么是 inode？",
    answer:
      "目录结构是一棵倒置的树——根目录（/）在顶部，子目录是树枝，文件是树叶。目录本质上是一个特殊文件，内容是一张「文件名 → inode 号」的映射表。inode（索引节点）是 Unix/Linux 文件系统的核心概念——每个文件有一个唯一的 inode 号，inode 存储文件的元数据（大小、权限、时间戳、数据块地址列表）但不存文件名。文件名存在目录项中，inode 存文件实体信息。这样设计的好处：①硬链接——多个文件名可以指向同一个 inode，删除一个名字不影响数据（引用计数归零才删数据）；②重命名/移动——只改目录项不动数据块，瞬间完成；③文件名可以包含任意字符（除了 / 和空字符），因为文件名和文件数据分离。`ls -i` 可以查看文件的 inode 号。Windows 的 NTFS 用 MFT（主文件表）记录文件元数据，概念类似但实现不同。",
    tags: ["目录结构", "inode", "文件系统"],
  },
  {
    id: "hcw-file-system-3",
    chapter: "hcw-file-system",
    level: 3,
    question: "文件在磁盘上的存储方式有哪几种？各自的优缺点是什么？",
    answer:
      "①连续分配——文件占用磁盘上一段连续的扇区。优点：读快（一次寻道连续读）、实现简单。缺点：产生外部碎片（文件删除后留下的空洞无法利用）、文件无法动态增长。②链式分配——每个磁盘块末尾存指向下一个块的指针，文件的数据块可以分散在磁盘各处。优点：无外部碎片、文件可动态增长。缺点：随机访问慢（要从头遍历链表）、指针损坏导致数据丢失、指针占空间。③索引分配——为每个文件建一个索引表（如 inode），记录所有数据块的位置。优点：随机访问快（直接查索引表）、无外部碎片。缺点：索引表本身占空间、大文件索引表可能要多级（一级索引→二级索引→三级索引）。现代文件系统（ext4/NTFS）大多用索引分配的变体：ext4 用 extent（一段连续区的起始块+长度）代替逐块索引，大幅减少元数据量并提升连续性；NTFS 用 MFT 中的 data run 描述连续区域。此外 ext4/XFS 还支持延迟分配（delayed allocation）——写数据时先缓存，flush 时再选择最佳磁盘位置，减少碎片。",
    tags: ["文件存储", "磁盘分配", "索引"],
  },
  {
    id: "hcw-file-system-4",
    chapter: "hcw-file-system",
    level: 3,
    question: "FAT、NTFS 和 ext4 文件系统有什么主要区别？日志文件系统解决了什么问题？",
    answer:
      "FAT（File Allocation Table）——最古老的文件系统，用一张FAT表记录每个簇的下一个簇号（链式分配）。优点：简单、兼容性极好（U盘/SD卡标配）。缺点：无文件权限、无日志、碎片严重、单文件最大 4GB（FAT32）。NTFS——Windows 的现代文件系统，用 MFT 记录所有文件元数据，支持权限、压缩、加密、硬链接、日志。ext4——Linux 的现代文件系统，用 inode + extent 索引，支持日志、延迟分配、大文件。日志文件系统解决的核心问题：异常断电时的数据一致性。传统文件系统写文件时要同时更新数据块和元数据（inode/FAT表），如果在这两步之间断电，文件系统可能处于不一致状态（如数据写了但元数据指向旧块），开机时需要全盘扫描修复（fsck），大磁盘可能耗时数十分钟。日志文件系统在写实际数据前，先把「要做的事」记到日志区（journal），然后再执行实际操作。断电后重启只需重放或撤销日志即可恢复一致性，扫描只需几秒。ext4 默认记录元数据日志（data=ordered），NTFS 也有类似的事务日志机制。",
    tags: ["FAT", "NTFS", "ext4", "日志文件系统"],
  },
];
