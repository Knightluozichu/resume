import type { ReviewQuestion } from "./types";

export const hcwMemoryHierarchyQuestions: ReviewQuestion[] = [
  {
    id: "hcw-memory-hierarchy-1",
    chapter: "hcw-memory-hierarchy",
    level: 2,
    question: `内存层次的金字塔结构是什么？为什么要分层？`,
    answer:
      `内存层次从顶到底：寄存器→L1 缓存→L2 缓存→L3 缓存→主内存（DRAM）→固态硬盘→机械硬盘。越往上速度越快、容量越小、价格越贵；越往下速度越慢、容量越大、价格越便宜。寄存器约 1KB 纳秒级，L1 约 64KB 1ns，L2 约 256KB 3-10ns，L3 约 8MB 10-20ns，内存约 16GB 100ns，SSD 约 1TB 100μs，HDD 约 4TB 10ms。分层的原因是：没有一种存储既快又大又便宜。利用「局部性原理」（程序访问的数据有时间和空间聚集性），把常用数据放高层、不常用的放低层，让 CPU 大部分时间命中高速层，整体表现接近最快层、容量接近最慢层。`,
    tags: ["内存层次", "缓存", "局部性"],
  },
  {
    id: "hcw-memory-hierarchy-2",
    chapter: "hcw-memory-hierarchy",
    level: 2,
    question: `为什么需要缓存？缓存是如何利用局部性原理工作的？`,
    answer:
      `CPU 速度远超内存（差 100 倍以上），如果每次都直接访问内存，CPU 大部分时间在等待数据，造成「内存墙」。缓存的原理是：在 CPU 和内存之间放一块小容量 SRAM，把最近用过的数据副本存在里面。它依赖两种局部性：①时间局部性——刚访问过的数据很可能再次被访问（如循环变量），所以保留在缓存；②空间局部性——被访问数据的邻近地址很可能也被访问（如数组遍历），所以每次从内存加载一个缓存行（通常 64 字节）而非单个字节。缓存命中时 CPU 直接从 SRAM 取数据（1-3ns），不命中才去内存取（100ns+）。命中率越高，CPU 等待越少。`,
    tags: ["缓存", "局部性原理"],
  },
  {
    id: "hcw-memory-hierarchy-3",
    chapter: "hcw-memory-hierarchy",
    level: 3,
    question: `缓存映射方式有哪三种？各自的优缺点是什么？`,
    answer:
      `①直接映射——每个内存块只能映射到缓存的一个固定位置（取模）。优点：硬件简单、查找快；缺点：冲突率高，两个块映射到同一行会互相驱逐，即使缓存有空位。②全相联映射——内存块可以放在缓存的任意位置。优点：冲突率最低、命中率最高；缺点：查找时要比较所有行，硬件代价大，只适合小容量缓存。③组相联映射——折中方案，缓存分成若干组，每组 N 路（N-way），内存块映射到固定组但可在组内任意路放置。N=1 退化为直接映射，N=缓存行数退化为全相联。常见 L1 为 8 路组相联，L2 为 16 路。组相联在硬件复杂度和命中率间取得平衡，是实际 CPU 最常用的方案。`,
    tags: ["缓存", "映射方式"],
  },
  {
    id: "hcw-memory-hierarchy-4",
    chapter: "hcw-memory-hierarchy",
    level: 3,
    question: `虚拟内存的作用是什么？它是如何工作的？`,
    answer:
      `虚拟内存让每个进程拥有独立的、连续的地址空间（如 64 位系统下 2^48 字节），而实际物理内存可能远小于此。工作原理：CPU 产生虚拟地址，经 MMU（内存管理单元）查页表转换为物理地址。页表把虚拟空间分成固定大小的页（通常 4KB），每页映射到物理内存的一个页框或被换出到磁盘。当访问的页不在物理内存时触发缺页中断，操作系统从磁盘把页调入内存（可能淘汰其他页）。虚拟内存的三大作用：①隔离——每个进程地址空间独立，互不干扰，实现内存保护；②扩充——用磁盘扩展物理内存，运行超过物理内存的程序；③共享——多个进程可映射同一物理页（如共享库），节省内存。代价是地址翻译和缺页中断的开销，TLB 缓存页表项来缓解翻译开销。`,
    tags: ["虚拟内存", "分页", "操作系统"],
  },
];
