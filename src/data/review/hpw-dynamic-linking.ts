import type { ReviewQuestion } from "./types";

export const hpwDynamicLinkingQuestions: ReviewQuestion[] = [
  {
    id: "hpw-dynamic-linking-1",
    chapter: "hpw-dynamic-linking",
    level: 2,
    question: `静态链接和动态链接各有什么优缺点？动态链接如何节省内存？`,
    answer:
      `静态链接优点：可执行文件自包含、不依赖外部库、部署简单、版本固定。缺点：体积大（每个程序拷一份库代码）、更新难（库改 bug 要全部重编译）。动态链接优点：体积小、库代码内存共享、易更新（换 .so 即可）。缺点：运行时依赖库存在、启动有链接开销、可能版本不兼容（DLL 地狱）。动态链接节省内存的原理：库的代码段只读，多个进程跑同一程序/用同一库时，OS 把同一份物理页映射到各进程虚拟地址空间——100 个进程用 printf，物理内存只有 1 份 printf 代码，全部共享。只有代码段共享，数据段各进程独立（写时复制）。`,
    tags: ["静态链接", "动态链接"],
  },
  {
    id: "hpw-dynamic-linking-2",
    chapter: "hpw-dynamic-linking",
    level: 3,
    question: `什么是 PLT/GOT 的延迟绑定？相比立即绑定有什么优劣？`,
    answer:
      `延迟绑定用 PLT（过程链接表）和 GOT（全局偏移表）实现：第一次调用某库函数时跳到 PLT 项，PLT 通过 GOT 跳转——第一次 GOT 存的是「回到动态链接器解析符号」的入口，解析完把真实地址写进 GOT；之后再调用直接通过 GOT 跳到函数，无解析开销。立即绑定在加载时解析全部符号，启动慢但运行快；延迟绑定把启动成本分摊到真正用到的符号上——没调用的库函数永不解析，启动更快。代价是第一次调用某函数时有一次符号解析开销。延迟绑定是现代系统的默认选择，因为多数程序只用库的一小部分符号。`,
    tags: ["PLT", "GOT", "延迟绑定"],
  },
  {
    id: "hpw-dynamic-linking-3",
    chapter: "hpw-dynamic-linking",
    level: 3,
    question: `动态链接下，多个进程共享同一份库代码，数据段（可写的全局变量）如何处理？`,
    answer:
      `只有代码段（只读）能被多进程共享同一物理页。数据段（可写的全局变量、静态变量）每个进程必须各自独立——否则一个进程改库的全局变量会影响其他进程。实现靠写时复制（Copy-On-Write）：初始时多个进程的数据段也映射到同一份物理页（共享，省内存），但当某个进程要写这个数据段时，CPU 触发写保护异常，内核为该进程复制一份独立的物理页再让写入完成。此后该进程的数据段就是独立副本，互不影响。这样既享受了共享的内存收益，又保证了进程间数据隔离。`,
    tags: ["共享库", "写时复制"],
  },
  {
    id: "hpw-dynamic-linking-4",
    chapter: "hpw-dynamic-linking",
    level: 4,
    question: `「动态链接一定比静态链接慢」这个说法对吗？动态链接的额外开销到底在哪？`,
    answer:
      `不完全对。动态链接的额外开销其实很小：函数调用时多一次 GOT 间接跳转（多一次内存访问），延迟绑定下首次调用有一次符号解析开销（之后无）。现代 CPU 的分支预测和缓存能让这个间接跳转几乎无感。真正慢的场景是：①极高频调用极小的库函数（间接跳转开销占比大）；②库跨边界传递数据频繁（缓存失效）。而对 I/O 密集或计算密集的程序，动态链接的开销可忽略不计。权衡点在内存和可维护性：动态链接省内存、易更新，绝大多数场景值得这点开销。静态链接只在无库环境分发或对启动延迟极敏感时才优选。`,
    tags: ["性能", "链接方式"],
  },
];
