import type { ReviewQuestion } from "./types";

export const hpwPointersQuestions: ReviewQuestion[] = [
  {
    id: "hpw-pointers-1",
    chapter: "hpw-pointers",
    level: 2,
    question: `指针的本质是什么？\`int *p = &a;\` 之后 \`*p\` 和 \`a\` 是什么关系？`,
    answer:
      `指针的本质是「保存地址的变量」。声明 \`int *p\` 时，p 本身也是一个变量（64 位系统占 8 字节），里面存的是一个地址值。\`p = &a\` 把 a 的地址赋给 p；\`*p\` 是「取出 p 里存的地址，去那个地址读数据」。所以 \`*p\` 和 \`a\` 是同一块内存的两个名字——通过 p 里的地址找到 a 所在的柜子，读写的都是 a 的值。理解了「变量名只是地址的别名，CPU 只认地址」，传指针就是传地址，让被调函数能直接修改调用方的变量。`,
    tags: ["指针", "基础概念"],
  },
  {
    id: "hpw-pointers-2",
    chapter: "hpw-pointers",
    level: 3,
    question: `指针的类型决定哪两件事？为什么 \`int *p\` 的 \`p+1\` 跳 4 字节而 \`char *p\` 的 \`p+1\` 跳 1 字节？`,
    answer:
      `指针类型决定两件事：①解引用读多少字节——\`int *\` 读 4 字节，\`char *\` 读 1 字节，\`double *\` 读 8 字节；②指针运算的步长——\`p+1\` 跳过「一个元素的字节大小」。\`int *p\` 时 \`p+1\` 跳 4 字节指向下一个 int，\`char *p\` 时 \`p+1\` 只跳 1 字节。这样设计是为了让数组能用指针遍历：\`arr[i]\` 等价于 \`*(arr+i)\`，指针运算自动按元素大小跳步，无需手算字节数。同样一个地址用不同类型指针解引用，读出的二进制位被解释成不同类型的值。`,
    tags: ["指针运算", "类型系统"],
  },
  {
    id: "hpw-pointers-3",
    chapter: "hpw-pointers",
    level: 3,
    question: `野指针、悬空指针、空指针有什么区别？各有什么危害和防范？`,
    answer:
      `①野指针——声明后未初始化，指向随机地址，解引用是未定义行为，可能崩溃也可能悄悄改坏数据。防范：声明即初始化。②悬空指针——指向的内存已释放（free 后或函数返回栈变量地址），再用是访问非法内存。防范：free 后立即置 NULL，绝不返回栈变量地址。③空指针——值为 NULL/0，解引用在大多数系统触发段错误（立刻崩溃，比悄悄出错安全）。防范：用前判空。三者区别：野指针是「从未初始化」，悬空指针是「曾经有效但已失效」，空指针是「明确标记不指向任何有效地址」。`,
    tags: ["指针陷阱", "内存安全"],
  },
  {
    id: "hpw-pointers-4",
    chapter: "hpw-pointers",
    level: 4,
    question: `为什么说「指针本身不难，难的是内存管理」？现代语言如何解决这个问题？`,
    answer:
      `指针本身极简——就是一个存地址的变量。让人头疼的是「地址背后那块内存的生命周期和归属」：这块内存还活着吗？归谁管？能写吗？是不是你想的那块？真正的难点是内存管理：栈内存在函数返回后失效（返回栈变量地址=悬空指针）、堆内存要手动释放（忘记=泄漏、释放两次=崩溃）、共享内存谁负责释放（所有权不清=双释放或泄漏）。现代语言用不同方案解决：Java/Python/Go 用 GC 自动回收堆内存；Rust 用所有权系统在编译期保证内存安全；C++ 用智能指针（RAII）自动释放。它们解决的都是内存管理问题，而非指针本身。`,
    tags: ["内存管理", "语言设计"],
  },
];
