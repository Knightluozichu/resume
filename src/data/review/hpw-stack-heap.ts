import type { ReviewQuestion } from "./types";

export const hpwStackHeapQuestions: ReviewQuestion[] = [
  {
    id: "hpw-stack-heap-1",
    chapter: "hpw-stack-heap",
    level: 2,
    question: `函数调用时栈帧包含哪些内容？为什么不能返回局部变量的地址？`,
    answer:
      `栈帧包含：①返回地址（函数执行完跳回哪里）；②调用方栈帧的基址指针（用于恢复）；③参数（寄存器传参溢出的部分）；④局部变量。函数返回时整个栈帧被弹出，栈指针恢复到调用前。不能返回局部变量地址，因为局部变量在栈帧里，函数返回栈帧弹出后那块内存就被回收了（可能被下一个函数调用覆盖），再访问就是悬空指针，读到的是垃圾数据或触发崩溃。要返回数据应通过返回值、传出参数（指针）或在堆上分配。`,
    tags: ["栈帧", "函数调用"],
  },
  {
    id: "hpw-stack-heap-2",
    chapter: "hpw-stack-heap",
    level: 3,
    question: `栈和堆在分配方式、速度、大小、生命周期上有什么区别？什么时候该用堆？`,
    answer:
      `①分配方式——栈编译器自动压栈，堆程序员手动 malloc/new；②速度——栈极快（移动栈指针），堆较慢（查空闲链表）；③大小——栈有限（1-8MB），堆大（受物理内存限制）；④生命周期——栈随函数返回自动销毁，堆跨函数由释放决定。该用堆的情况：运行时才知道大小的数据（变长输入）、需要跨函数传递且生命周期超出函数的数据、非常大的数据（超过栈限额）。能用栈就用栈是局部定长数据的优化原则。`,
    tags: ["栈与堆", "内存分配"],
  },
  {
    id: "hpw-stack-heap-3",
    chapter: "hpw-stack-heap",
    level: 3,
    question: `什么是内存泄漏和栈溢出？各有什么成因和防范？`,
    answer:
      `①内存泄漏——堆上申请的内存不再使用却没释放，进程内存只增不减，长期运行后耗尽内存崩溃。常见于循环里 malloc 却循环外才 free、异常路径漏 free、数据结构存指针没遍历释放。排查靠 Valgrind/AddressSanitizer，防范靠严格配对 malloc/free 或智能指针/GC。②栈溢出——栈用超了限额（默认 8MB），典型是无限递归没有终止条件，或局部变量太大（如 int arr[10000000]），直接段错误。防范：递归要有终止条件和合理深度，大数组改用堆分配。`,
    tags: ["内存泄漏", "栈溢出"],
  },
  {
    id: "hpw-stack-heap-4",
    chapter: "hpw-stack-heap",
    level: 4,
    question: `「栈上的数据比堆上的快，所以全用栈」这个说法错在哪里？`,
    answer:
      `栈的「快」指的是分配/释放快（只需移动栈指针，不查链表），不是「访问栈上的数据更快」。访问速度取决于数据是否在 CPU 缓存里，与在栈还是堆无关——栈数据因局部性好常在缓存，但堆上连续访问的数据同样能享受缓存。全用栈的问题：①栈大小有限（8MB），大数组直接溢出；②栈数据随函数返回销毁，跨函数传递的数据无法用栈；③栈大小编译期要确定，运行时才知道大小的数据必须用堆。「能用栈就用栈」是局部、定长数据的优化原则，不是全局法则。`,
    tags: ["性能", "栈与堆"],
  },
];
