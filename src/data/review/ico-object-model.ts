/** 复习题库 · 对象模型基础（ico-object-model）。《深度探索 C++ 对象模型》第 1 章改编。 */

import type { ReviewQuestion } from "./types";

export const icoObjectModelQuestions: ReviewQuestion[] = [
  // ── L1 认记：术语 / 定义 ──
  {
    id: "ico-om-1",
    chapter: "ico-object-model",
    level: 1,
    question: "Lippman 提出的三种对象模型分别叫什么？它们各自怎么存放成员？",
    answer:
      "① 简单对象模型：每个成员占一个槽位，对象内存只存一张指向成员的指针表，所有成员存取都经一次间接。② 表格驱动对象模型：把数据成员和成员函数分成两张表，对象只存这两张表的指针。③ C++ 对象模型：非虚数据成员直接内联在对象里，只有虚函数经虚表（vptr → vtable）间接存取。",
    tags: ["三种模型", "成员存放"],
  },

  // ── L2 理解：为什么 / 机制 ──
  {
    id: "ico-om-2",
    chapter: "ico-object-model",
    level: 2,
    question: "为什么 C++ 最终选择了「数据内联 + 虚函数间接」的模型，而不是简单对象模型那样的全间接？",
    answer:
      "权衡空间与速度。简单模型全间接：每个成员都要一次指针解引用，存取慢，且每个对象要多存一张指针表，空间开销大。C++ 模型把非虚数据成员直接内联，存取就是一次偏移，零间接、零额外空间；只为「需要多态的虚函数」付一次间接代价（vptr + vtable）。这是在抽象能力与运行效率之间取的折中——绝大多数成员访问走快路径，只有虚函数走慢路径。",
    tags: ["模型取舍", "空间与速度"],
  },

  // ── L3 应用：读代码 / 推断 ──
  {
    id: "ico-om-3",
    chapter: "ico-object-model",
    level: 3,
    question:
      "一个类只有普通非虚成员函数、无虚函数，另加几个数据成员。它的对象大小会包含成员函数指针吗？为什么？",
    answer:
      "不会包含成员函数指针。在 C++ 对象模型里，非虚成员函数的代码全类共享、存在代码段，对象里只放数据成员（和可能的 vptr）。既然没有虚函数，也就没有 vptr，所以 sizeof 该类 = 数据成员布局（含对齐 padding），与成员函数的多少和大小无关。成员函数调用在编译期就绑定到具体函数，不需要对象里存指针。",
    tags: ["sizeof", "成员函数", "推断"],
  },

  // ── L4 综合：陷阱 / 全流程 ──
  {
    id: "ico-om-4",
    chapter: "ico-object-model",
    level: 4,
    question:
      "同事说「加了 virtual 函数后对象变大了一点，但只要不调虚函数就没开销」。这话哪里不对？从对象模型角度反驳。",
    answer:
      "两点不对。① 对象变大是确定的：只要类有虚函数，每个对象都会多出一个 vptr（通常一个指针大小），无论你是否调用虚函数——这是恒定存在的空间开销。② 调用开销也不是「调了才有」那么简单：虚调用本身多一次经 vptr 的间接查表；更关键的是虚函数通常无法内联，频繁调用的小虚函数会放大开销。此外 vptr 还会影响拷贝、布局、对齐。所以「加 virtual」是结构性决定，不是「用的时候才付钱」。",
    tags: ["virtual 开销", "综合", "陷阱"],
  },
];

export default icoObjectModelQuestions;
