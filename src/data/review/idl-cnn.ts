import { ReviewQuestion } from "./types";

export const idlCnnQuestions: ReviewQuestion[] = [
  {
    id: "idl-cnn-1",
    chapter: "idl-cnn",
    level: 1,
    question: `CNN 的三大核心特性是什么？它们如何使 CNN 适合处理图像数据？`,
    answer:
      `CNN 的三大核心特性：①局部连接——每个神经元只与输入的局部区域（感受野）连接，而非全部像素。这模拟了视觉皮层的工作方式：图像中相邻像素相关性高，远距离像素相关性低，局部连接能有效提取局部特征（边缘、角点、纹理）。②权值共享——同一卷积核在整张图上滑动，所有位置共享同一组权重。这基于「特征平移不变性」假设：一个能检测左上角边缘的卷积核，同样能检测右下角的边缘。权值共享大幅减少参数量：一个 3x3 卷积核只有 9 个参数，而等效的全连接层可能有数百万参数。③空间不变性——由于权值共享和池化操作，CNN 对特征在图像中的位置变化具有鲁棒性。这三点使 CNN 特别适合图像处理：局部连接捕捉空间局部模式，权值共享保证平移不变性，参数量小使深层网络可训练。对比全连接网络处理图像需要把图像展平为一维向量，丢失了空间结构信息，且参数量爆炸。`,
    tags: ["CNN", "局部连接", "权值共享", "平移不变性", "感受野"],
  },
  {
    id: "idl-cnn-2",
    chapter: "idl-cnn",
    level: 2,
    question: `卷积操作的计算过程是怎样的？输出特征图的尺寸如何确定？`,
    answer:
      `卷积操作的计算过程：①取一个尺寸为 KxK 的卷积核（权重矩阵）。②将卷积核覆盖在输入特征图的某个位置，与覆盖区域做逐元素乘法并求和，得到一个标量输出。③按步长（stride）在输入图上滑动卷积核，重复上述计算。④每个位置的计算结果组成输出特征图。⑤一个卷积层通常使用多个卷积核，每个核提取不同的特征，输出多个通道。输出特征图尺寸计算公式：O = (W - K + 2P) / S + 1，其中 W 是输入尺寸，K 是卷积核尺寸，P 是填充（padding），S 是步长。例如输入 224x224，卷积核 3x3，填充 1，步长 1，则输出 = (224-3+2)/1+1 = 224（尺寸不变）。如果步长 2，填充 1，则输出 = (224-3+2)/2+1 = 112（尺寸减半，这是下采样的常用方式）。参数量计算：每个卷积核参数量 = K*K*C_in（C_in 是输入通道数），加上 1 个偏置。C_out 个卷积核的总参数量 = C_out * (K*K*C_in + 1)。`,
    tags: ["卷积操作", "特征图", "步长", "填充", "尺寸计算"],
  },
  {
    id: "idl-cnn-3",
    chapter: "idl-cnn",
    level: 2,
    question: `池化层的作用是什么？有哪些类型？为什么现代架构有时不用池化？`,
    answer:
      `池化层的作用：①下采样——减小特征图的空间尺寸（通常减半），减少计算量和参数量。②不变性增强——池化使特征对小的平移和形变更鲁棒，因为池化取局部区域的统计值（最大值或平均值），不依赖于特征的精确位置。③扩大感受野——通过逐步减小特征图尺寸，深层卷积核在原图上的感受野更大，能看到更大范围的模式。池化类型：①最大池化（Max Pooling）——取局部区域的最大值，保留最显著的特征，是最常用的池化方式。②平均池化（Average Pooling）——取局部区域的平均值，保留更多背景信息。③全局池化（Global Pooling）——对整个特征图做池化（如全局平均池化），常用于替代全连接层，大幅减少参数。现代架构有时不用池化的原因：①步长卷积（strided convolution）可以直接用卷积层做下采样（步长 > 1），让网络自己学习如何降采样，比固定的池化操作更灵活。②池化丢失了空间位置信息，对需要精确定位的任务（如目标检测、分割）不利。③一些现代架构（如 ResNet 早期用池化，后续版本改用步长卷积）倾向用卷积统一所有操作，简化架构设计。`,
    tags: ["池化", "最大池化", "下采样", "感受野", "步长卷积"],
  },
  {
    id: "idl-cnn-4",
    chapter: "idl-cnn",
    level: 3,
    question: `ResNet 的残差连接解决了什么问题？它是如何工作的？为什么有效？`,
    answer:
      `ResNet 的残差连接解决的是「网络退化」问题：当网络深度增加时，训练误差反而上升（这不是过拟合，因为测试误差也上升）。这看起来违背直觉——理论上更深的网络至少可以退化为浅层网络（把多加的层变成恒等映射），但实践中 SGD 难以优化恒等映射。残差连接的工作方式：传统层学习 H(x) = 目标映射；残差块学习 F(x) = H(x) - x，然后输出 F(x) + x = H(x)。即网络学习的是「残差」F(x) 而非完整映射 H(x)。通过跳跃连接（shortcut/skip connection）直接把输入 x 加到层的输出上。为什么有效：①恒等映射容易学习——如果某层是冗余的，最优 F(x) = 0，只需把权重置零即可，比学习恒等映射 H(x) = x 容易得多。②梯度流动——反向传播时，梯度可以通过跳跃连接直接传到浅层（dL/dx = dL/dy * (1 + dF/dx)），其中的「1」保证了梯度至少能传回去，不会消失，使上百层网络可训练。③特征复用——跳跃连接允许浅层特征直接传到深层，深层网络可以在浅层特征基础上做增量修正。ResNet 的意义：使网络深度从几十层扩展到上百层（ResNet-152），大幅提升了性能，是深度学习的重要里程碑。残差思想后续被广泛应用于 Transformer、Diffusion 等模型。`,
    tags: ["ResNet", "残差连接", "网络退化", "梯度流动", "深层网络"],
  },
];
