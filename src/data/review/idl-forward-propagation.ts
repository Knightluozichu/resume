import { ReviewQuestion } from "../types";

export const idlForwardPropagationQuestions: ReviewQuestion[] = [
  {
    id: "idl-forward-propagation-1",
    chapter: "idl-forward-propagation",
    level: 1,
    question: "什么是前向传播？请以两层网络为例描述完整的计算流程。",
    answer:
      "前向传播是输入数据从输入层经过隐藏层到输出层的计算过程。以两层网络（一个隐藏层 + 一个输出层）为例：①输入层接收原始数据 x = [x1, x2, ..., xn]。②隐藏层计算——先做线性变换 z1 = W1 * x + b1（W1 是权重矩阵，b1 是偏置向量），再通过激活函数引入非线性 a1 = ReLU(z1)。③输出层计算——以隐藏层激活值 a1 为输入，先做线性变换 z2 = W2 * a1 + b2，再通过输出层激活函数 a2 = softmax(z2)（多分类）或 sigmoid(z2)（二分类）。④最终输出 a2 即为模型的预测结果。每一层的计算模式都是「线性变换 + 激活函数」，前一层的输出是下一层的输入。前向传播的目的是得到预测值，用于与真实标签比较计算损失。",
    tags: ["前向传播", "计算流程", "线性变换", "激活函数"],
  },
  {
    id: "idl-forward-propagation-2",
    chapter: "idl-forward-propagation",
    level: 2,
    question: "为什么神经网络需要激活函数？如果没有激活函数会怎样？",
    answer:
      "神经网络需要激活函数的核心原因是引入非线性。如果没有激活函数（或使用线性激活函数），多层网络的计算如下：隐藏层输出 a1 = W1 * x + b1，输出层 a2 = W2 * a1 + b2 = W2 * (W1 * x + b1) + b2 = (W2 * W1) * x + (W2 * b1 + b2)。可以看到，两个线性变换的叠加 W2*(W1*x) 等于一个单一的线性变换 (W2*W1)*x，整个网络等价于一个单层线性模型。这意味着无论加多少隐藏层，网络的表达能力都和一个没有隐藏层的线性模型相同，无法解决任何非线性问题（如 XOR）。激活函数通过非线性变换打破这一等价性：ReLU 将负值截断为 0，Sigmoid 将输出压缩到 (0,1)，这些非线性操作使每一层都能学习到新的特征表示，从而让网络能拟合任意复杂的函数。此外，不同激活函数还有各自的功能：Softmax 将输出转化为概率分布，Sigmoid 适合二分类，ReLU 适合隐藏层。",
    tags: ["激活函数", "非线性", "线性变换", "万能逼近"],
  },
  {
    id: "idl-forward-propagation-3",
    chapter: "idl-forward-propagation",
    level: 2,
    question: "Softmax 函数是什么？它在多分类任务中如何工作？",
    answer:
      "Softmax 函数将一个实数向量转化为概率分布。公式：softmax(z_i) = e^(z_i) / sum(e^(z_j))，其中分母是所有元素的指数之和。Softmax 的工作方式：①输入——模型输出层的原始得分（logits）z = [z1, z2, ..., zK]，K 是类别数。②计算——对每个 z_i 取指数 e^(z_i)，然后除以所有指数之和进行归一化。③输出——得到 K 个非负实数，和为 1，可解释为每个类别的预测概率。④预测——取概率最大的类别 argmax(softmax(z)) 作为最终预测。Softmax 的特点：①放大差异——指数运算放大了较大得分的优势，使预测更果断；②可微——梯度可计算，适合反向传播；③与交叉熵配合——Softmax + 交叉熵损失的梯度有优美的简化形式 dL/dz = a - y（预测值减真实标签），计算高效。适用场景：多分类任务的输出层（如图像分类的 1000 类、MNIST 的 10 类）。",
    tags: ["Softmax", "多分类", "概率分布", "交叉熵"],
  },
  {
    id: "idl-forward-propagation-4",
    chapter: "idl-forward-propagation",
    level: 3,
    question: "前向传播中的权重矩阵和偏置向量各自起什么作用？它们的维度如何确定？",
    answer:
      "权重矩阵 W 和偏置向量 b 是神经网络的可学习参数，在前向传播中起不同作用。权重矩阵 W 的作用：决定输入特征如何组合变换。W 的每一行代表一个输出神经元，每一列代表一个输入特征，W[i][j] 表示第 j 个输入对第 i 个输出的贡献权重。它实现特征的线性组合和空间变换。偏置向量 b 的作用：提供平移能力。没有偏置时，线性变换 z = W*x 只能做过原点的线性变换（缩放和旋转）；加上偏置 z = W*x + b 后，可以做任意平移，增强了模型的表达能力。偏置使激活函数的决策边界不必过原点。维度确定：假设前一层的输出维度为 n（神经元数），当前层的神经元数为 m，则 W 的维度为 m x n（m 行 n 列），b 的维度为 m x 1。例如输入层 3 个特征、隐藏层 4 个神经元，则 W1 维度为 4x3，b1 维度为 4x1。参数量计算：每层参数量 = m*n（权重） + m（偏置）。理解维度对调试代码和设计网络架构至关重要。",
    tags: ["权重矩阵", "偏置向量", "维度", "参数量"],
  },
];
