import { ReviewQuestion } from "./types";

export const idlGenerativeModelsQuestions: ReviewQuestion[] = [
  {
    id: "idl-generative-models-1",
    chapter: "idl-generative-models",
    level: 1,
    question: `自编码器的结构和原理是什么？它有哪些应用场景？`,
    answer:
      `自编码器（Autoencoder, AE）由编码器和解码器两部分组成。原理：编码器将输入 x 压缩为低维潜在表示 z = Encoder(x)，解码器从潜在表示重建输入 x' = Decoder(z)。训练目标是使重建尽可能接近原始输入：L = ||x - x'||^2（重建误差）。编码器和解码器都是神经网络（通常是全连接或卷积网络），通过反向传播联合训练。关键在于潜在空间维度小于输入维度（「瓶颈」），迫使网络学习数据的最本质特征表示而非简单复制。应用场景：①降维/特征提取——训练好的编码器可以作为特征提取器，替代手工特征。②去噪——输入加噪后的数据，目标是重建干净数据，训练后的网络能自动去噪。③异常检测——正常数据训练后，重建误差小；异常数据重建误差大，据此检测异常。④图像压缩——学习比 JPEG 更好的压缩方式（针对特定数据分布）。⑤预训练——深度网络的逐层预训练（早期 DL 的常用技巧）。自编码器的局限：潜在空间不连续——确定性映射 z = Encoder(x) 使得潜在空间中两个点之间没有语义过渡，直接从潜在空间采样生成的新样本质量差，不适合做生成模型。VAE 和 GAN 解决了这一问题。`,
    tags: ["自编码器", "编码器", "解码器", "潜在空间", "重建误差"],
  },
  {
    id: "idl-generative-models-2",
    chapter: "idl-generative-models",
    level: 2,
    question: `VAE 与普通自编码器有什么区别？VAE 如何实现生成新样本？`,
    answer:
      `VAE 与普通 AE 的核心区别：普通 AE 学习确定性的潜在表示 z = Encoder(x)，潜在空间不连续，无法直接用于生成。VAE 学习概率分布——编码器不输出确定的 z，而是输出分布的参数：均值 mu 和方差 sigma^2，然后从该分布采样 z ~ N(mu, sigma^2)。VAE 的损失函数：L = 重建项 + KL 散度项。①重建项 E[log p(x|z)]——使解码器能从 z 重建 x，与普通 AE 相同。②KL 散度 KL(q(z|x) || p(z))——使编码器输出的分布接近先验分布 N(0, 1)，保证潜在空间连续且规整。VAE 生成新样本的过程：①从标准正态分布采样 z ~ N(0, 1)（KL 散度保证了潜在空间接近此分布）。②将 z 输入解码器 x' = Decoder(z)，生成新样本。因为潜在空间是连续的，采样任意 z 都能生成有意义的样本，且两个样本之间的插值过渡平滑。VAE 的优点：训练稳定、潜在空间结构化、可做插值和算术运算（如「国王-男人+女人=女王」）。缺点：生成样本通常比 GAN 模糊（因为重建项和 KL 项的权衡导致生成的模糊性）。`,
    tags: ["VAE", "变分自编码器", "KL散度", "潜在空间", "概率分布", "生成"],
  },
  {
    id: "idl-generative-models-3",
    chapter: "idl-generative-models",
    level: 2,
    question: `GAN 的训练原理是什么？什么是模式崩溃？如何缓解？`,
    answer:
      `GAN 的训练原理：生成器 G 和判别器 D 进行对抗博弈。G 从随机噪声 z 生成假样本 G(z)，D 试图区分真样本 x 和假样本 G(z)。训练目标：D 最大化 log(D(x)) + log(1-D(G(z)))（正确分类真假），G 最小化 log(1-D(G(z)))（欺骗 D）。交替训练：先更新 D（固定 G），再更新 G（固定 D）。理想状态下，G 生成的样本越来越逼真，D 越来越难以区分，最终达到纳什均衡：D 对所有样本输出 0.5（无法区分）。模式崩溃（Mode Collapse）：G 只能生成少数几种样本（缺乏多样性），即使输入不同的 z 也生成相似的结果。例如生成人脸时只能生成一种脸型。原因：G 找到了某种能骗过 D 的「捷径」，反复利用这一模式，D 无法有效惩罚 G 的不多样性。缓解方法：①Minibatch Discrimination——让 D 同时看一批样本，判断整批样本的多样性，如果 G 只生成相似样本则惩罚。②Wasserstein GAN（WGAN）——用 Wasserstein 距离替代 JS 散度，提供更平滑的梯度，训练更稳定，缓解模式崩溃。③Unrolled GAN——G 在更新时考虑未来 D 的反应，避免短视的捷径。④经验回放——存储历史生成的样本，防止 G「遗忘」多样性。⑤标签平滑——给真实样本标签设为 0.9 而非 1.0，防止 D 过于自信。`,
    tags: ["GAN", "对抗训练", "模式崩溃", "生成器", "判别器", "WGAN"],
  },
  {
    id: "idl-generative-models-4",
    chapter: "idl-generative-models",
    level: 3,
    question: `比较 AE、VAE、GAN 三种生成模型的优缺点，并简述扩散模型的原理和优势。`,
    answer:
      `三种模型对比：①AE——确定性映射，训练简单，主要用于降维/去噪/特征提取，不适合直接生成（潜在空间不连续）。②VAE——概率映射，潜在空间连续可采样，训练稳定，可做插值；但生成样本偏模糊（重建与 KL 的权衡）。③GAN——对抗训练，生成质量最高最逼真；但训练不稳定（需要平衡 G 和 D），易模式崩溃，难以评估生成质量。扩散模型（Diffusion Model）原理：①前向过程——逐步向数据添加高斯噪声，经过 T 步后变成纯噪声（马尔可夫链）。②反向过程——训练一个神经网络学习逐步去噪，从纯噪声恢复出数据。训练目标：预测每一步添加的噪声。推理时从随机噪声开始，逐步去噪生成样本。扩散模型的优势：①训练稳定——不像 GAN 需要对抗训练，用简单的 MSE 损失训练，不崩溃。②生成质量高——超越 GAN，达到 SOTA（如 Stable Diffusion、DALL-E 3、Midjourney 的核心）。③多样性好——不存在模式崩溃问题，能覆盖数据分布的多个模式。④理论清晰——基于概率论的变分推断，有严格的理论基础。⑤可控生成——可通过条件输入（文本/图像）控制生成过程。缺点：生成速度慢（需要多步去噪），但可通过蒸馏等技术加速。扩散模型已成为图像生成的主流范式。`,
    tags: ["AE", "VAE", "GAN", "扩散模型", "生成模型对比", "Diffusion"],
  },
];
