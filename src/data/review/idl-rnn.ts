import { ReviewQuestion } from "./types";

export const idlRnnQuestions: ReviewQuestion[] = [
  {
    id: "idl-rnn-1",
    chapter: "idl-rnn",
    level: 1,
    question: `RNN 的核心思想是什么？它与前馈神经网络有什么区别？`,
    answer:
      `RNN 的核心思想是引入「记忆」——网络在处理当前输入时，会利用之前时间步的信息。RNN 的计算公式：h_t = f(W_h * h_{t-1} + W_x * x_t + b)，其中 h_{t-1} 是上一时间步的隐藏状态（记忆），x_t 是当前输入，h_t 是当前隐藏状态。同一组权重 W_h 和 W_x 在所有时间步共享，这是「循环」的含义。与前馈神经网络的区别：①数据类型——前馈网络处理固定大小的输入（如一张图片），RNN 处理变长序列（如一句话、一段语音、一时间序列）。②信息流——前馈网络的信息只从前向后流动，RNN 的信息在时间维度上循环传递，当前输出依赖于所有历史输入。③参数共享——前馈网络每层有独立权重，RNN 所有时间步共享同一组权重，使模型能处理任意长度的序列。④隐藏状态——RNN 的隐藏状态 h_t 充当「记忆」，编码了截至当前时间步的所有信息。RNN 适用于语言模型、机器翻译、语音识别、时间序列预测等序列任务。`,
    tags: ["RNN", "循环", "隐藏状态", "序列建模", "记忆"],
  },
  {
    id: "idl-rnn-2",
    chapter: "idl-rnn",
    level: 2,
    question: `RNN 为什么会出现梯度消失问题？LSTM 是如何解决的？`,
    answer:
      `RNN 梯度消失的原因：RNN 的反向传播（BPTT）需要在时间维度上展开，梯度通过链式法则连乘多个时间步的权重矩阵和激活函数导数。当序列较长时，如果权重矩阵的特征值 < 1 或激活函数导数 < 1（如 Sigmoid/Tanh），连乘后梯度以指数级衰减趋近于 0，导致网络无法学习长距离依赖——早期时间步的信息无法影响后期输出。LSTM 的解决方案：引入「细胞状态」（cell state）C_t 和三个门控机制：①遗忘门 f_t = sigmoid(W_f * [h_{t-1}, x_t])——决定从细胞状态中丢弃多少旧信息（0 全丢弃，1 全保留）。②输入门 i_t = sigmoid(W_i * [h_{t-1}, x_t])——决定写入多少新信息到细胞状态。③输出门 o_t = sigmoid(W_o * [h_{t-1}, x_t])——决定从细胞状态输出多少信息到隐藏状态。细胞状态更新：C_t = f_t * C_{t-1} + i_t * C_t_tilde。关键在于 f_t * C_{t-1} 这一项是逐元素乘法，如果遗忘门 f_t 接近 1，则 C_{t-1} 几乎完整传递到 C_t，梯度也可以沿这条路径无损回传，不受激活函数导数连乘的影响。这相当于为梯度提供了一条「高速公路」，使长距离梯度成为可能。`,
    tags: ["RNN", "梯度消失", "LSTM", "门控机制", "细胞状态", "长距离依赖"],
  },
  {
    id: "idl-rnn-3",
    chapter: "idl-rnn",
    level: 2,
    question: `LSTM 和 GRU 有什么区别？各自的优缺点是什么？`,
    answer:
      `LSTM 和 GRU 都是解决 RNN 梯度消失的门控机制，但结构不同。LSTM 结构：三个门（遗忘门、输入门、输出门）+ 独立的细胞状态 C_t。细胞状态和隐藏状态分离，信息流通过 C_t 的加法更新传递。GRU 结构：两个门（重置门、更新门），合并了细胞状态和隐藏状态。更新门 z_t 同时承担遗忘和输入的功能：h_t = (1-z_t) * h_{t-1} + z_t * h_t_tilde。重置门 r_t 控制在计算候选状态时使用多少历史信息。区别：①门控数量——LSTM 三个门，GRU 两个门，GRU 参数更少。②状态——LSTM 有独立的细胞状态和隐藏状态，GRU 只有隐藏状态。③计算复杂度——GRU 比 LSTM 少约 1/3 的参数，训练更快。④性能——在多数任务上两者性能相近，GRU 在小数据集上可能略优（参数少不易过拟合），LSTM 在大数据集和长序列上可能略优。选择建议：数据量小或需要更快训练时选 GRU；序列特别长或数据量大时选 LSTM；实际项目中建议两者都试，选择验证集表现更好的。`,
    tags: ["LSTM", "GRU", "门控", "对比", "参数量"],
  },
  {
    id: "idl-rnn-4",
    chapter: "idl-rnn",
    level: 3,
    question: `RNN 有哪些常见变体和应用模式？双向 RNN 和 Encoder-Decoder 架构分别在什么场景使用？`,
    answer:
      `RNN 常见变体和应用模式：①单向 RNN——从左到右处理序列，适用于只依赖历史信息的任务（如语言模型预测下一个词）。②双向 RNN（BiRNN）——同时从左到右和从右到左处理序列，拼接两个方向的隐藏状态。适用于需要全局上下文的任务（如命名实体识别、机器翻译的编码器），因为一个词的含义既受前文影响也受后文影响。但双向 RNN 不能用于实时生成（因为需要看到未来信息）。③Encoder-Decoder 架构——编码器用 RNN 将输入序列编码为一个向量（上下文向量），解码器用 RNN 从该向量生成输出序列。适用于输入输出长度不同的序列转换任务（如机器翻译：英文→中文）。④注意力机制增强——传统 Encoder-Decoder 的上下文向量是固定长度的信息瓶颈，注意力机制允许解码器在每个时间步「关注」输入序列的不同部分，大幅提升性能。⑤多层 RNN——将多个 RNN 层叠，低层学习基础特征，高层学习抽象特征。应用场景：语言模型（预测下一个词）、机器翻译（Seq2Seq）、语音识别（声学模型）、情感分析（取最后时间步输出分类）、文本生成（逐步生成）。后续 Transformer 用自注意力替代了 RNN 的循环结构，但 RNN 的序列建模思想仍有重要影响。`,
    tags: ["双向RNN", "Encoder-Decoder", "注意力机制", "序列转换", "应用模式"],
  },
];
