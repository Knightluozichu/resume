import { ReviewQuestion } from "../types";

export const imlSupportVectorQuestions: ReviewQuestion[] = [
  {
    id: "iml-support-vector-1",
    chapter: "iml-support-vector",
    level: 1,
    question: "SVM 的核心思想是什么？什么是支持向量？支持向量有什么特殊性质？",
    answer:
      "SVM 的核心思想是寻找一个分类超平面，使它到最近的样本点（支持向量）的间隔最大化。决策边界为 w^T * x + b = 0，间隔宽度为 2/||w||，最大化间隔等价于最小化 ||w||^2。直觉理解：在两类样本之间放一条最宽的「路」，路的中心线是决策边界，路的宽度就是间隔。支持向量是离决策边界最近的样本点，它们恰好在间隔边界上，满足 y_i * (w^T * x_i + b) = 1。支持向量的特殊性质：①决定决策边界——只有支持向量影响超平面的位置和方向，决策边界完全由支持向量确定；②非支持向量无关——移除非支持向量（离边界较远的样本）不影响模型；③稀疏性——通常只有少量样本是支持向量（离边界最近的那些），大多数样本不影响模型；④鲁棒性——对远离边界的噪声不敏感。与逻辑回归的区别：逻辑回归关注所有样本的总体概率，SVM 只关注边界附近的样本（支持向量）。",
    tags: ["SVM", "最大间隔", "支持向量", "决策边界"],
  },
  {
    id: "iml-support-vector-2",
    chapter: "iml-support-vector",
    level: 2,
    question: "核函数的作用是什么？RBF 核为什么是最常用的核函数？",
    answer:
      "核函数的作用是将线性不可分的数据隐式映射到高维空间，在高维空间中寻找线性超平面，从而实现非线性分类。原理是：原始空间中线性不可分的数据，映射到高维空间后可能变得线性可分。核技巧的好处是不需要显式计算高维映射 phi(x)，直接在原始空间中计算高维空间的内积：K(x_i, x_j) = phi(x_i)^T * phi(x_j)。这避免了高维映射的计算开销。常用核函数：①线性核 K(x,y) = x^T * y——无映射，计算快，适用于线性可分或高维稀疏数据；②多项式核 K(x,y) = (x^T*y + 1)^d——可控多项式次数，适用于中等非线性；③RBF 核（高斯核）K(x,y) = exp(-gamma * ||x-y||^2)——最常用；④Sigmoid 核——类似神经网络。RBF 核最常用的原因：①无限维映射——RBF 核将数据映射到无限维空间，理论上可以拟合任意复杂的决策边界，表达能力强；②局部性强——基于样本间的欧氏距离，距离越近核值越大，具有局部性；③超参数少——只有 gamma 一个超参数（加上正则化参数 C）；④通用性好——在不知道数据先验结构时，RBF 核通常是最好的默认选择。gamma 控制影响范围：gamma 大时每个样本影响范围小（决策边界复杂，容易过拟合），gamma 小时影响范围大（决策边界平滑，容易欠拟合）。",
    tags: ["核函数", "RBF 核", "高斯核", "gamma", "非线性分类"],
  },
  {
    id: "iml-support-vector-3",
    chapter: "iml-support-vector",
    level: 3,
    question: "什么是软间隔？正则化参数 C 的作用是什么？",
    answer:
      "软间隔是允许部分样本违反间隔约束的 SVM 变体。严格的最大间隔 SVM（硬间隔）要求所有样本必须正确分类且满足 y_i * (w^T * x_i + b) >= 1。但当数据有噪声或类别重叠时，硬间隔 SVM 会导致无解或过拟合。软间隔引入松弛变量 xi_i，允许部分样本违反间隔约束：y_i * (w^T * x_i + b) >= 1 - xi_i，xi_i >= 0。目标函数变为 minimize (1/2)*||w||^2 + C*sum(xi_i)。正则化参数 C 的作用是控制「最大化间隔」与「最小化分类错误」之间的权衡：①C 很大时——对违规的惩罚很重，模型倾向于严格分类所有样本（xi_i 尽量为 0），间隔变窄，容易过拟合（为了分对噪声而牺牲间隔宽度）；②C 适中时——在间隔宽度和分类错误之间取得平衡，泛化能力好；③C 很小时——对违规的惩罚很轻，模型倾向于放宽间隔（容忍更多分类错误），间隔变宽，容易欠拟合。C 是 SVM 最重要的超参数，通常在对数尺度上用网格搜索调优（如 C in [0.001, 0.01, 0.1, 1, 10, 100, 1000]）。对于 RBF 核 SVM，C 和 gamma 一起构成二维超参数空间，需要联合调优。",
    tags: ["软间隔", "硬间隔", "正则化参数 C", "松弛变量", "过拟合"],
  },
  {
    id: "iml-support-vector-4",
    chapter: "iml-support-vector",
    level: 4,
    question: "SVM 和逻辑回归有什么区别？各自的适用场景是什么？",
    answer:
      "SVM 和逻辑回归都是经典的二分类算法，但核心思想和适用场景不同。核心区别：①核心思想——SVM 寻找最大间隔分类超平面，关注边界附近的样本（支持向量）；逻辑回归用 sigmoid 将线性得分映射为概率，关注所有样本的总体概率。②关注样本——SVM 只有支持向量参与决策边界确定，具有稀疏性；逻辑回归的每个样本都贡献梯度。③核函数——SVM 天然支持核技巧，通过核函数优雅地实现非线性分类；逻辑回归需要手动构造非线性特征。④概率输出——逻辑回归直接输出概率；SVM 不直接输出概率，需额外校准（如 Platt scaling）。⑤非线性能力——SVM 用 RBF 核可以实现无限维映射；逻辑回归受限于多项式特征。⑥训练复杂度——SVM 的训练复杂度为 O(n^2) 到 O(n^3)，不适合大数据集；逻辑回归训练快，适合大规模数据。适用场景：SVM 适合中小数据集（几千到几万样本）、高精度分类、需要非线性决策边界的场景，尤其适合特征数与样本数接近的情况（如生物信息学）；逻辑回归适合大数据集、需要概率输出的场景（如排序、风险评估）、需要模型可解释性的场景、以及作为基线模型快速验证。实践中，数据量大时优先逻辑回归，数据量中小且需要高精度时优先 SVM（RBF 核）。",
    tags: ["SVM", "逻辑回归", "核函数", "概率输出", "训练复杂度", "算法对比"],
  },
];
