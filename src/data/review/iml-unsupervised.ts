import { ReviewQuestion } from "./types";

export const imlUnsupervisedQuestions: ReviewQuestion[] = [
  {
    id: "iml-unsupervised-1",
    chapter: "iml-unsupervised",
    level: 1,
    question: `K-Means 聚类的迭代流程是什么？它有什么优缺点？如何选择 K 值？`,
    answer:
      `K-Means 聚类的迭代流程：①初始化——随机选择 K 个点作为初始簇中心；②分配——计算每个样本到 K 个簇中心的距离，将每个样本分配到最近的簇中心；③更新——重新计算每个簇的中心，即簇内所有样本的均值；④重复步骤 2-3，直到簇中心不再变化（收敛）或达到最大迭代次数。优点：①算法简单直观，容易实现和理解；②计算效率高，时间复杂度为 O(n*K*d*iter)，适合大规模数据；③当簇为凸形（球形）且大小相近时效果好；④聚类结果易于解释。缺点：①需要预先指定簇数 K；②对初始簇中心敏感，可能陷入局部最优（K-Means++ 通过智能初始化缓解）；③只能发现球形（凸形）簇，对于非凸形状效果差；④对噪声和异常值敏感。K 值选择方法：①肘部法则——绘制 K 值 vs 簇内方差曲线，选择方差下降率骤减的「肘部」点作为 K 值；②轮廓系数——s = (b-a)/max(a,b)，其中 a 是样本到同簇其他样本的平均距离，b 是样本到最近簇的平均距离。取值 [-1, 1]，越大表示聚类越好。计算不同 K 值下的平均轮廓系数，选择使轮廓系数最大的 K。`,
    tags: ["K-Means", "聚类", "肘部法则", "轮廓系数", "K 值选择"],
  },
  {
    id: "iml-unsupervised-2",
    chapter: "iml-unsupervised",
    level: 2,
    question: `层次聚类和 DBSCAN 各有什么特点？它们与 K-Means 相比有什么优势？`,
    answer:
      `层次聚类的特点：不需要预先指定 K 值，通过构建聚类树（树状图）展示数据的层次结构。自底向上（凝聚型）的流程：每个样本自成一簇 → 合并最近的两个簇 → 重复直到所有样本合并为一簇 → 在树状图上选择切割高度确定簇数。簇间距离度量方法包括单链接（最近样本距离）、全链接（最远样本距离）、平均链接（所有样本对的平均距离）、Ward（合并后簇内方差增量最小）。与 K-Means 相比的优势：①无需预先指定 K——可以在树状图上选择不同切割高度得到不同簇数；②层次结构——树状图展示簇之间的嵌套和合并过程。DBSCAN（基于密度的空间聚类）的特点：通过密度连通性发现任意形状的簇。核心概念：核心点（eps 半径内不少于 min_samples 个邻居）、边界点、噪声点。与 K-Means 相比的优势：①可发现任意形状的簇——基于密度而非距离，可以发现环形、月牙形等非凸形状；②自动确定簇数——不需要预先指定簇数 K；③可识别噪声点——明确标记不属于任何簇的噪声点；④对异常值鲁棒——噪声点不影响簇的形成。但 DBSCAN 的缺点是对 eps 和 min_samples 参数敏感，密度不均匀时效果差。`,
    tags: ["层次聚类", "DBSCAN", "K-Means", "聚类树", "密度聚类"],
  },
  {
    id: "iml-unsupervised-3",
    chapter: "iml-unsupervised",
    level: 3,
    question: `PCA 降维的核心思想和计算步骤是什么？它有什么应用场景和局限？`,
    answer:
      `PCA（主成分分析）的核心思想是通过正交变换将高维数据投影到低维子空间，保留数据中方差最大的方向（主成分），从而在降维的同时尽可能保留原始数据的信息。直觉理解：数据在不同方向上的「展开程度」（方差）不同，方差大的方向包含更多信息，PCA 找到方差最大的几个方向作为新的坐标轴。计算步骤：①数据标准化——对每个特征去均值，必要时做缩放使不同量纲的特征可比；②计算协方差矩阵——描述各特征之间的相关性；③计算特征值和特征向量——特征值表示该方向上的方差大小，特征向量表示该方向；④选择主成分——将特征值从大到小排序，选最大的 k 个特征值对应的特征向量；⑤投影——将原始数据投影到这 k 个特征向量构成的子空间。方差解释比：每个主成分解释的方差占比 = 该特征值 / 所有特征值之和。通常选择累计方差解释比达到 95% 的主成分数量。应用场景：①数据可视化——将高维数据降到 2D/3D；②特征压缩——减少特征数量，降低存储和计算开销；③去噪——保留大方差方向（信号），丢弃小方差方向（噪声）；④加速训练——降维后特征更少，训练更快。局限性：①PCA 是线性降维——只能捕捉线性结构，无法处理非线性流形，此时需要 t-SNE/UMAP；②方差大不等于信息多——有些情况下方差小的方向可能包含重要的判别信息；③不可解释性——主成分是原始特征的线性组合，通常没有明确的物理含义；④对缩放敏感——如果特征量纲差异大且未做标准化，大量纲特征会主导主成分方向。`,
    tags: ["PCA", "降维", "协方差矩阵", "特征值", "方差解释比"],
  },
  {
    id: "iml-unsupervised-4",
    chapter: "iml-unsupervised",
    level: 4,
    question: `无监督学习与监督学习有什么本质区别？在什么场景下应该使用无监督学习？`,
    answer:
      `无监督学习与监督学习的本质区别在于是否有标签：①数据形式——监督学习使用有标签数据 (X, y)，有标准答案作为学习目标；无监督学习使用无标签数据 X，没有标准答案，需要自行发现数据中的结构。②学习目标——监督学习的目标是学习从输入 X 到输出 y 的映射关系（分类或回归），有明确的预测目标；无监督学习的目标是发现数据的内在结构（聚类找分组、降维找主成分），没有明确的预测目标。③评估方式——监督学习有客观的评估指标（准确率/MSE 等），可以用测试集评估泛化能力；无监督学习没有标签，评估更主观（轮廓系数/方差解释比等间接指标）。④任务类型——监督学习主要是分类和回归；无监督学习主要是聚类和降维。应该使用无监督学习的场景：①数据探索与理解——面对一个新数据集，不了解其结构时，先用聚类看数据有哪些自然分组，用降维可视化看数据分布；②无标签数据丰富但有标签数据稀缺——标注成本高，可用无监督学习从无标签数据中提取结构信息；③用户分群/市场细分——用聚类算法自动发现用户群体，用于精准营销；④异常检测——正常数据聚类后，远离所有簇中心的点可能是异常（如信用卡欺诈检测）；⑤特征工程/降维——用 PCA 降维作为预处理步骤，减少特征数量、加速后续监督学习；⑥推荐系统——用户和物品的聚类用于协同过滤的预处理。实践中，无监督学习常与监督学习结合：先用无监督学习做数据探索和特征工程，再用监督学习做预测。`,
    tags: ["无监督学习", "监督学习", "聚类", "降维", "异常检测"],
  },
];
