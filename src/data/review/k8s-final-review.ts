import type { ReviewQuestion } from "./types";

export const k8sFinalReviewQuestions: ReviewQuestion[] = [
  {
    id: "k8s-fr-1",
    chapter: "k8s-final-review",
    level: 2,
    question: `请描述从kubectl apply到Pod可接收流量的完整全链路流程，涉及哪些组件协作？`,
    answer: `完整全链路：①用户执行kubectl apply -f deployment.yaml → kubectl将YAML发送给API Server。②API Server接收请求 → 认证（验证客户端证书/Token，确认用户身份）→ 授权（RBAC检查用户是否有create deployments权限）→ 准入控制（MutatingAdmissionWebhook可修改资源如注入sidecar → ValidatingAdmissionWebhook校验资源如必须设置resources.limits）→ 将Deployment对象序列化写入etcd。③Deployment Controller（在Controller Manager中运行）Watch到新Deployment → 对比期望状态（replicas:3）与实际状态（0个ReplicaSet）→ 创建ReplicaSet对象写入etcd。④ReplicaSet Controller Watch到新ReplicaSet → 对比期望（3个Pod）与实际（0）→ 创建3个Pod对象（spec.nodeName为空，表示未调度）写入etcd。⑤Scheduler Watch到nodeName为空的Pod → Filter阶段过滤不满足条件的节点（资源不足：节点Allocatable - 已调度Pod requests总和 < 新Pod requests；端口冲突：hostPort被占用；Taint不容忍：NoSchedule效果的Taint无对应Toleration；nodeSelector/nodeAffinity不匹配）→ Score阶段对候选节点打分（LeastRequestedPriority资源均衡度 + NodeAffinityPriority节点亲和性权重 + InterPodAffinityPriority Pod亲和性 + TopologySpreadPriority拓扑分布）→ 选择最高分节点 → 更新Pod的spec.nodeName → 写入etcd。⑥kubelet Watch到spec.nodeName等于本节点的Pod → 调用CRI（containerd）拉取镜像并创建容器 → 调用CNI（Calico/Cilium）分配Pod IP、设置网络路由和iptables规则 → 调用CSI挂载存储卷到容器目录 → 启动容器。⑦kubelet执行探针 → startupProbe（如果配置，先等启动成功）→ startupProbe通过后启用liveness/readiness → readinessProbe通过后更新Pod状态为Ready。⑧Endpoints Controller Watch到Pod Ready → 将Pod IP:targetPort加入Service的Endpoints列表。⑨kube-proxy Watch到Endpoints变化 → 更新节点上的iptables/IPVS规则（ClusterIP → DNAT → Pod IP）。⑩Client请求 → CoreDNS解析web-svc.default.svc.cluster.local → 返回ClusterIP → Client发送请求到ClusterIP:80 → kube-proxy iptables DNAT随机选择一个Endpoint → 转发到Pod IP:8080 → Pod容器处理请求 → 响应返回Client。整个过程涉及组件：kubectl、API Server、etcd、Deployment Controller、ReplicaSet Controller、Scheduler、kubelet、CRI、CNI、CSI、Endpoints Controller、kube-proxy、CoreDNS。`,
    tags: ["全链路", "kubectl apply", "组件协作", "Pod创建", "Service访问"],
  },
  {
    id: "k8s-fr-2",
    chapter: "k8s-final-review",
    level: 3,
    question: `如果让你为一个大型电商平台设计基于K8s的生产架构，你会如何设计？请从集群、工作负载、网络、存储、安全、可观测等维度展开。`,
    answer: `①集群：多可用区部署（3个可用区），多Master高可用（3 Master + keepalived/HAProxy负载均衡API Server），etcd 3-5节点Raft集群（独立机器或与Master共置），按工作负载分节点池（CPU型跑Web/API、内存型跑Redis/Memcached、GPU型跑推荐模型），Cilium CNI（eBPF高性能 + NetworkPolicy支持 + 可观测）。②工作负载：前端React/SSR用Deployment + HPA（CPU>70%扩容，min=4 max=20）+ podAntiAffinity topologyKey=zone跨可用区分散；订单/库存微服务用Deployment + HPA；MySQL/PostgreSQL用StatefulSet + 独立PVC + 每节点一个（主从复制）；Kafka/RabbitMQ用StatefulSet；Fluent Bit日志采集用DaemonSet每节点一个；Prometheus + Grafana监控用Deployment + PVC持久化。③网络：Ingress nginx-ingress七层路由（api.example.com/users→user-svc，api.example.com/orders→order-svc）+ TLS集中终止（Cert-Manager自动证书）+ Service ClusterIP内部通信 + NetworkPolicy零信任（DB只允许backend访问5432，backend只允许frontend+ingress访问，所有Pod Egress只允许DNS+DB+必要外部API）+ CoreDNS服务发现。④存储：StorageClass动态供给（SSD StorageClass跑数据库IOPS要求高，HDD StorageClass跑日志归档）+ StatefulSet volumeClaimTemplates每Pod独立PVC + WaitForFirstConsumer同可用区 + Velero定期备份PV数据。⑤配置：ConfigMap注入非敏感配置（Volume挂载支持热更新，如feature flags、日志级别）+ Vault管理敏感数据（数据库密码、API Key，通过CSI Secret Store Driver注入Pod，密钥不落etcd）+ GitOps（ArgoCD同步Git仓库YAML，声明式配置，变更走PR流程Review）。⑥调度：topologySpreadConstraints maxSkew=1 topologyKey=zone（严格跨可用区均匀分布）+ Taint GPU节点专用（gpu=true:NoSchedule + Toleration）+ requests基于P95流量压测数据 + limits=2x requests（CPU）limits=requests（内存）+ PriorityClass（核心业务高优先级防止批处理抢占）。⑦安全：每应用独立ServiceAccount + RBAC最小权限（Role只授予get/list/watch必要资源）+ SecurityContext（runAsNonRoot=true, readOnlyRootFilesystem=true, allowPrivilegeEscalation=false, drop ALL capabilities）+ PodSecurity admission（restricted级别）+ NetworkPolicy零信任 + etcd静态加密Secret + 定期安全扫描（Trivy镜像漏洞扫描）。⑧可观测：Prometheus（kube-state-metrics + node-exporter + 应用自定义指标）+ Grafana看板（集群总览/应用性能/资源使用）+ AlertManager（Pod Failed/CPU>90%/副本不足→PagerDuty/Slack）+ ELK/Loki日志（Fluent Bit采集→Loki存储→Grafana查询）+ Jaeger分布式追踪（OpenTelemetry SDK注入traceID，全链路延迟分析）。⑨高可用：多Master + 跨可用区Pod分散 + PDB（PodDisruptionBudget minAvailable保证最少可用副本，防止维护时全部驱逐）+ HPA自动扩缩 + VPA自动调resources + Velero备份恢复。⑩CI/CD：GitOps ArgoCD同步Git → 滚动更新零停机（maxSurge=1 maxUnavailable=0 + readinessProbe）→ 金丝雀发布（Argo Rollouts 10%→50%→100%，根据错误率自动回滚）→ 蓝绿发布（双版本切换）。整体原则：声明式（一切皆YAML存Git）+ 自愈（探针+控制器）+ 可观测（三支柱）+ 零信任安全（RBAC+NetworkPolicy+SecurityContext）+ 弹性（HPA+多可用区+PDB）。`,
    tags: ["生产架构", "大型电商", "高可用", "GitOps", "零信任", "综合设计"],
  },
  {
    id: "k8s-fr-3",
    chapter: "k8s-final-review",
    level: 2,
    question: `全书四条核心主线的四个交汇点分别是什么？请详细解释每个交汇点涉及的主线交互和实际意义。`,
    answer: `四个交汇点：①Pod-Service-Endpoints（工作负载↔网络通信）——工作负载主线和网络通信主线的交汇。交互：Pod是工作负载主线的基本单元（Pod→Deployment→ReplicaSet），Service是网络通信主线的起点（Service→Endpoints→DNS→Ingress）。Pod通过metadata.labels被Service的spec.selector选中，Endpoints Controller自动维护匹配Pod的IP列表。readinessProbe是关键桥梁——只有readinessProbe通过的Pod才被加入Endpoints，实现流量只打到健康Pod。实际意义：这是服务发现的核心枢纽。Pod IP易变（每次重建变化），Service提供稳定ClusterIP，Endpoints动态维护Pod列表，kube-proxy根据Endpoints更新转发规则。没有这个交汇，Pod无法被稳定访问，流量无法负载均衡到多个Pod。②Pod-Volume-PVC（工作负载↔存储配置）——工作负载主线和存储配置主线的交汇。交互：Pod通过spec.volumes引用PVC，PVC绑定PV（或通过StorageClass动态创建），PV由CSI驱动挂载到节点。StatefulSet通过volumeClaimTemplates为每个Pod创建独立PVC，Pod重建后绑定原PVC保证数据连续。实际意义：这是数据持久化的核心路径。无状态应用（Deployment）不需要PVC（数据在外部如数据库），有状态应用（StatefulSet）通过PVC实现数据独立和持久化。没有这个交汇，Pod删除后数据丢失，数据库无法在K8s上运行。③Deployment-Scheduler-Node（工作负载↔调度运维）——工作负载主线和调度运维主线的交汇。交互：Deployment Controller创建Pod（nodeName为空），Scheduler Watch到新Pod后执行Filter+Score选择节点，kubelet在节点上启动容器。调度决策依据Pod的requests（资源）、nodeAffinity（节点选择）、podAntiAffinity（Pod分散）、Taint/Toleration（污点容忍）。实际意义：这是调度决策的核心链路。Deployment定义「要几个Pod」，Scheduler决定「Pod跑在哪个节点」。没有这个交汇，Pod无法被分配到节点运行，资源无法合理利用。④Secret-RBAC-NetworkPolicy（存储配置↔调度运维）——存储配置主线和调度运维主线的交汇。交互：Secret存储敏感数据（存储配置主线），RBAC控制谁可以读取Secret（调度运维主线），NetworkPolicy控制Pod间网络流量（调度运维主线）。三者配合构建安全治理体系：Secret加密存储敏感数据，RBAC限制只有授权ServiceAccount才能访问Secret，NetworkPolicy限制只有特定Pod可以访问数据库Pod。实际意义：这是安全治理的核心体系。没有这个交汇，敏感数据裸奔（Secret无RBAC保护任何人可读）、权限失控（无RBAC任何应用可操作任何资源）、网络全通（无NetworkPolicy任何Pod可访问数据库）。四个交汇形成闭环：Pod跑起来（工作负载）→ 被Service发现（网络通信）→ 挂载PVC持久化（存储配置）→ 被Scheduler合理调度（调度运维）→ 被RBAC和NetworkPolicy保护（安全治理）。`,
    tags: ["四主线", "交汇点", "服务发现", "数据持久化", "调度决策", "安全治理"],
  },
  {
    id: "k8s-fr-4",
    chapter: "k8s-final-review",
    level: 3,
    question: `K8s的声明式API和控制器模式如何实现自愈和最终一致性？请结合全书知识举例说明故障自愈的几种场景。`,
    answer: `声明式API和控制器模式实现自愈的原理：用户通过YAML声明期望状态（如Deployment replicas:3），控制器持续Watch资源变化并Reconcile——对比期望状态与实际状态，执行操作消除差异。这个循环不断重复，保证系统最终收敛到期望状态，即使发生故障也能自动恢复。最终一致性：不要求实时一致（Reconcile有延迟），但保证最终一致（持续Reconcile直到达成）。故障自愈场景：①Pod进程崩溃——容器内进程异常退出（如OOM、panic）→ kubelet检测到容器退出 → 按restartPolicy（Always/OnFailure）重启容器 → 恢复到Running状态。如果崩溃持续（如镜像bug），livenessProbe失败也会触发重启。②Pod所在节点故障——节点硬件故障/网络不通 → Node Controller在40秒内（默认node-monitor-grace-period）标记节点NotReady → 5分钟后（default-pod-eviction-timeout）删除该节点上的Pod（状态变为Terminating） → Deployment Controller发现实际Pod数（2）< 期望（3）→ 在其他健康节点创建新Pod → 恢复3副本。③Deployment滚动更新失败——新版本镜像有bug导致readinessProbe持续失败 → 新Pod不Ready不加入Endpoints → 旧Pod继续服务 → 可kubectl rollout undo回滚到旧版本 → 旧ReplicaSet扩容、新ReplicaSet缩容 → 恢复正常。④Service Endpoints自动更新——Pod故障readinessProbe失败 → Endpoints Controller从Endpoints移除该Pod → kube-proxy更新iptables规则 → 流量不再打到故障Pod。新Pod就绪readinessProbe通过 → 加入Endpoints → 流量自动分发到新Pod。⑤HPA自动扩缩——流量激增CPU > 70% → HPA Controller从metrics-server获取指标 → 计算需要更多副本 → 更新Deployment replicas → Deployment Controller创建新Pod → 流量分散到更多Pod → CPU回落。流量下降CPU < 70% → HPA缩容（5分钟冷却期后）→ 减少副本节省资源。⑥StatefulSet存储恢复——StatefulSet Pod所在节点故障 → Pod被重建到其他节点 → 新Pod使用相同名称（如db-1）→ 自动绑定原PVC（data-db-1）→ 原有数据可用 → 数据库恢复。⑦Scheduler重新调度——Pod被驱逐（节点维护/抢占）→ Pod重新进入Pending → Scheduler重新调度到其他节点 → 恢复运行。核心：所有自愈都是通过Watch + Reconcile循环自动完成的，无需人工干预。控制器是自愈的执行者，声明式API是自愈的依据（期望状态），etcd是状态的单一事实来源。`,
    tags: ["声明式API", "控制器模式", "自愈", "最终一致性", "故障恢复"],
  },
];
