import type { ReviewQuestion } from "./types";

export const lkdKernelSyncQuestions: ReviewQuestion[] = [
  {
    id: "lkd-sync-1",
    chapter: "lkd-kernel-sync",
    level: 2,
    question: `自旋锁和信号量的核心区别是什么？各自适用于什么场景？`,
    answer:
      `自旋锁（spinlock）：获取失败时CPU忙等待（busy-wait，不断循环检测锁状态），不睡眠、不调度。适用于持有时间极短（微秒级）的临界区，且不能在持有自旋锁时睡眠。自旋锁主要用于中断上下文和多CPU并发场景。信号量/互斥锁（mutex/semaphore）：获取失败时进程进入睡眠状态，让出CPU给其他进程，锁释放后被唤醒。适用于可能持有较长时间或需要睡眠的临界区。核心区别：自旋锁不让出CPU（忙等），信号量让出CPU（睡眠）。选择原则：临界区短且不睡眠→自旋锁；临界区长或需要睡眠→信号量/互斥锁。在可抢占内核中自旋锁还会禁用抢占，防止持锁进程被调度走导致其他CPU死等。`,
    tags: ["内核同步", "锁机制"],
  },
  {
    id: "lkd-sync-2",
    chapter: "lkd-kernel-sync",
    level: 3,
    question: `RCU（Read-Copy-Update）的核心原理是什么？它如何实现读端零开销？`,
    answer:
      `RCU原理：写端不直接修改共享数据，而是创建一个副本，修改副本，然后用原子操作替换指针指向新副本。旧数据暂时保留，读端可能看到旧数据也可能看到新数据（取决于读取时机），但都能读到一致的数据。关键：写端不能立即释放旧数据，因为可能有读者正在读它。写端调用 synchronize_rcu() 等待所有「在替换指针之前已经开始的读者」完成读操作（宽限期 grace period 过后），然后才安全释放旧数据。读端零开销的实现：读端使用 rcu_read_lock()（仅禁用抢占，不加锁）+ rcu_dereference()（仅防止编译器重排序），不获取任何锁、不原子操作、不阻塞写端。适用于读多写少的场景（如路由表、dentry缓存），是Linux内核最重要的同步创新之一。`,
    tags: ["内核同步", "RCU"],
  },
  {
    id: "lkd-sync-3",
    chapter: "lkd-kernel-sync",
    level: 3,
    question: `为什么自旋锁要同时禁用抢占？如果在持锁时被抢占会发生什么？`,
    answer:
      `自旋锁禁用抢占的原因：假设CPU A上的进程P持有自旋锁L，此时被调度器抢占（时间片耗尽），P被切换出去。CPU B上的进程Q也尝试获取锁L，因为P持有L，Q开始忙等待（spin）。但P被切换出去了，只有P重新被调度运行才能释放L。如果Q的优先级高或系统负载高，P可能长时间得不到调度，Q一直忙等占住CPU B。极端情况下所有CPU都在忙等持锁进程被调度，系统卡死。禁用抢占后，持锁期间进程不会被调度走，保证它能尽快释放锁。注意：这只是单核场景的核心问题；在多核上即使不禁用抢占，P也可能因为被抢占而延迟释放锁导致Q长时间忙等。所以自旋锁必须同时关抢占。`,
    tags: ["内核同步", "自旋锁"],
  },
  {
    id: "lkd-sync-4",
    chapter: "lkd-kernel-sync",
    level: 4,
    question: `内核中常见的死锁场景有哪些？如何避免？`,
    answer:
      `常见死锁场景：①AB-BA死锁——线程1持锁A请求锁B，线程2持锁B请求锁A，互相等待；②自死锁——线程重复获取同一个非递归自旋锁；③中断与进程上下文死锁——进程持有锁L后被中断，ISR中也尝试获取L，自死锁（ISR不能睡眠无法等待进程释放）；④持锁睡眠——持有自旋锁后调用可睡眠函数，被调度走后其他CPU等待锁忙等。避免方法：①锁排序——全局定义锁的获取顺序，所有代码按相同顺序获取锁，消除AB-BA死锁；②锁分组——用 lockdep 工具在开发期检测锁依赖图，发现潜在死锁；③中断中获取自旋锁用 spin_lock_irqsave() 同时禁用本地中断，防止进程上下文和中断上下文冲突；④持自旋锁绝不调用可睡眠函数；⑤尽量缩小临界区，减少持锁时间；⑥能用无锁结构（atomic/RCU）就不用锁。`,
    tags: ["内核同步", "死锁"],
  },
];
