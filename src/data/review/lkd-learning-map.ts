import type { ReviewQuestion } from "./types";

export const lkdLearningMapQuestions: ReviewQuestion[] = [
  {
    id: "lkd-lm-1",
    chapter: "lkd-learning-map",
    level: 2,
    question: "《Linux内核设计与实现》全书的知识递进结构是什么？为什么是这个顺序？",
    answer:
      "内核基础（简介与架构）→ 进程管理与调度 → 系统调用 → 中断处理 → 内核同步 → 内存管理 → 虚拟文件系统 → 全书复习。顺序由依赖关系决定：先理解内核整体架构和用户态/内核态分层，才能进入内核内部；进程是内核管理的核心对象，所以先讲进程管理和调度；系统调用是用户程序进入内核的唯一合法入口，是理解内核交互的前提；中断是内核响应硬件和异常的机制；多核并发要求同步保护；内存管理是进程和文件系统的底层支撑；VFS 统一了所有文件系统接口。先「认识内核」，再「管理进程」，然后「进入内核」，接着「响应事件」，再「保证并发安全」，然后「分配内存」，最后「统一文件接口」。",
    tags: ["架构", "学习路径"],
  },
  {
    id: "lkd-lm-2",
    chapter: "lkd-learning-map",
    level: 2,
    question: "Linux内核的两大设计哲学是什么？它们如何影响内核的整体结构？",
    answer:
      "两大设计哲学：①机制与策略分离——内核提供通用机制（如调度框架、VFS抽象层），具体策略由用户态或可配置模块决定（如CFS参数、具体文件系统实现）。②一切皆文件——VFS将设备、管道、socket、普通文件统一为文件描述符接口，用 open/read/write/close 操作一切。这两条原则使Linux内核保持了极高的可扩展性：新文件系统只需实现VFS接口，新设备只需注册驱动，调度器可替换为不同策略，而用户态接口保持不变。",
    tags: ["架构", "设计哲学"],
  },
  {
    id: "lkd-lm-3",
    chapter: "lkd-learning-map",
    level: 3,
    question: "用一次 read() 系统调用串联全书主要章节，列出各子系统的入场时机。",
    answer:
      "一次 read() 经过全书多个子系统：①系统调用（第4章）——用户态执行 syscall 指令进入内核态，保存上下文，分发到 sys_read；②进程管理（第2章）——当前进程的 task_struct 记录了打开的文件表，通过 fd 找到 file 对象；③VFS（第8章）——调用 file.f_op->read，经 VFS 抽象层路由到具体文件系统（如 ext4）；④内存管理（第7章）——分配缓冲页存放读取的数据，用 alloc_pages 从 Buddy 分配；⑤中断处理（第5章）——如果数据在磁盘，块设备DMA完成后触发中断，上半部应答硬件，下半部唤醒等待进程；⑥内核同步（第6章）——全程用自旋锁/RCU保护共享数据结构。一条数据流串联六章。",
    tags: ["架构", "运行时旅程"],
  },
  {
    id: "lkd-lm-4",
    chapter: "lkd-learning-map",
    level: 4,
    question: "单片内核与微内核的核心区别是什么？Linux为何选择单片内核又吸收了微内核的优点？",
    answer:
      "单片内核（monolithic）将进程管理、内存管理、文件系统、驱动全部运行在内核态同一地址空间，优点是性能高（函数直接调用，无IPC开销），缺点是庞大且一处崩溃全盘崩溃。微内核（microkernel）只把最基本功能（调度、IPC）放内核态，其余作为用户态服务进程，优点是模块化和安全性好，缺点是IPC开销大。Linux选择单片内核以获得高性能，但吸收了微内核的模块化思想：可加载内核模块（LKM）允许动态加载/卸载驱动和文件系统，无需重启，既保持内核态直接调用的性能，又获得了接近微内核的灵活性。Linux的实践证明「高性能」和「可扩展」可以兼得。",
    tags: ["架构", "内核模型"],
  },
];
