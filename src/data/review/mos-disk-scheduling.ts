import type { ReviewQuestion } from "./types";

export const mosDiskSchedulingQuestions: ReviewQuestion[] = [
  {
    id: "mos-disk-scheduling-1",
    chapter: "mos-disk-scheduling",
    level: 2,
    question: `磁盘访问的三段时间是什么？为什么寻道是主要瓶颈？`,
    answer:
      `三段时间：①寻道时间——磁臂机械移到目标柱面，平均 5-10ms；②旋转延迟——盘片旋转到目标扇区到磁头下，平均半转，4-6ms（7200rpm）；③传输时间——读扇区数据，微秒级。寻道是主要瓶颈的原因：它是纯机械运动（磁臂转动），受物理惯性限制，毫秒级且难以再缩短；旋转延迟也是机械的但随转速固定；传输是电子的，微秒级可忽略。寻道时间通常是三段中最大的，所以磁盘调度的核心是最小化寻道距离——FCFS/SSTF/SCAN/C-SCAN 全在优化磁臂的移动路径。这也是为什么顺序读远快于随机读：顺序读只寻道一次，后续扇区靠旋转连续读；随机读每次都要重新寻道。`,
    tags: ["磁盘", "寻道", "性能"],
  },
  {
    id: "mos-disk-scheduling-2",
    chapter: "mos-disk-scheduling",
    level: 3,
    question: `SSTF 为什么会导致饥饿？SCAN（电梯算法）如何避免？`,
    answer:
      `SSTF 每次选离当前磁头最近的请求，若柱面 50 附近源源不断来新请求，磁头会一直在 50 附近打转，柱面 180 的请求永远「不是最近的」而轮不到——这就是饥饿。SCAN 避免饥饿的机制是「方向锁定」：磁头沿一个方向（如向大柱面）扫过，途中的请求按柱面顺序依次服务，到边界才折返。因为方向是固定的，远端请求一定会被扫到——磁头向大方向扫时必然经过 180，不会因为中途有更近的请求而跳过它。这就像电梯——向上运行时，10 楼的请求不会让电梯跳过 9 楼，因为方向锁定按楼层顺序停。代价是刚扫过的那端请求要等磁头跑到边界再折返，等待方差大；C-SCAN 用「单向服务 + 快速返回」让等待更均匀。`,
    tags: ["SSTF", "SCAN", "饥饿"],
  },
  {
    id: "mos-disk-scheduling-3",
    chapter: "mos-disk-scheduling",
    level: 3,
    question: `SCAN、C-SCAN、LOOK、C-LOOK 之间有什么区别？现代系统常用哪个？`,
    answer:
      `SCAN（电梯算法）：磁头沿一个方向扫到物理边界再折返，途中按柱面顺序服务。C-SCAN（循环扫描）：只在一个方向服务，到边界后快速返回到起点（0 柱面）再重扫，让各请求等待时间更均匀，代价是返回段有「空跑」。LOOK：SCAN 的改进——不跑到物理边界，只到最远请求即折返，避免白跑空段。C-LOOK：C-SCAN 的改进——只返回到最小请求即重扫，同样避免白跑。现代操作系统多用 LOOK/C-LOOK 的变体——既避免白跑到物理边界（省时间），又兼顾公平与寻道效率。核心思想都是「方向锁定 + 按序服务 + 不跑空段」。`,
    tags: ["SCAN", "LOOK", "磁盘调度"],
  },
  {
    id: "mos-disk-scheduling-4",
    chapter: "mos-disk-scheduling",
    level: 4,
    question: `为什么磁盘调度算法（FCFS/SSTF/SCAN）对 SSD 意义减弱？SSD 的调度关注点是什么？`,
    answer:
      `FCFS/SSTF/SCAN/C-SCAN 的优化目标是「寻道时间」——磁臂的机械运动，这是机械盘独有的。SSD 没有磁臂，任意逻辑块访问时间几乎相同（几十微秒），不存在「柱面 1 到 100 比到 50 慢」的几何关系，所以这些算法对 SSD 意义减弱。SSD 的调度关注点完全不同：①磨损均衡（wear leveling）——让写入均匀分布到各闪存块，避免某些块先写坏（闪存块有擦写次数上限）；②垃圾回收——后台合并有效页、擦除无效块（闪存只能按块擦除不能覆写）；③TRIM——告知 SSD 哪些逻辑块已删除可提前擦除，为 GC 准备空闲块。SSD 内部的 FTL（闪存转换层）会把逻辑地址映射到物理页，对操作系统隐藏真实位置。调度算法要匹配硬件特性。`,
    tags: ["SSD", "磨损均衡", "FTL"],
  },
];
