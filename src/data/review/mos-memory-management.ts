import type { ReviewQuestion } from "./types";

export const mosMemoryManagementQuestions: ReviewQuestion[] = [
  {
    id: "mos-memory-management-1",
    chapter: "mos-memory-management",
    level: 2,
    question: `分段和分页有什么区别？各自的优缺点是什么？`,
    answer:
      `分段的翻译：虚拟地址 = (段号, 段内偏移)，段表记录每段基址和界限，PA = 段基址 + 偏移。优点是按逻辑结构切分（代码/数据/栈各自成段）；缺点是段大小可变导致外部碎片严重。分页的翻译：虚拟地址 = (页号, 页内偏移)，页表记录「虚拟页号 → 物理帧号」，PA = 帧号 × 页大小 + 偏移。优点是页固定大小、无外部碎片（任何空闲帧能放任何页）；缺点是最后一页可能浪费（内部碎片）且页表本身占内存。现代操作系统普遍采用分页（或段页式——先分段再分页），因为固定大小消除外部碎片的工程优势远大于逻辑切分的便利。`,
    tags: ["分段", "分页", "碎片"],
  },
  {
    id: "mos-memory-management-2",
    chapter: "mos-memory-management",
    level: 3,
    question: `为什么多级页表比单级页表省内存？代价是什么？`,
    answer:
      `单级页表必须为整个地址空间的每一个页都预留页表项——32 位系统 4GB/4KB = 1M 项 × 4 字节 = 4MB/进程，且必须常驻内存。但绝大多数进程只用了地址空间的一小部分，剩下都是无用的「无效」项。多级页表按需分配：页目录常驻（4KB），但二级页表只为「实际使用的地址范围」分配。未用的地址范围在页目录里就是「不存在」项，根本不分配对应的二级页表。一个只用 10MB 内存的进程，只需 3 个二级页表（12KB）+ 1 个页目录（4KB）= 16KB，远小于单级的 4MB。代价是多一次访存（page table walk），但 TLB 让这代价几乎不出现（命中率 > 99%）。`,
    tags: ["多级页表", "内存"],
  },
  {
    id: "mos-memory-management-3",
    chapter: "mos-memory-management",
    level: 3,
    question: `缺页处理的完整流程是什么？为什么按需调页让进程可以「大于物理内存」？`,
    answer:
      `缺页流程：①程序访问虚拟地址，MMU 查页表发现有效位=0，触发缺页异常陷入内核；②内核查页表项确认该页在磁盘的位置；③在物理内存找空闲帧，若无则用页面置换算法淘汰一页腾出帧（脏页需写回）；④启动磁盘 I/O 把该页读入物理帧；⑤I/O 完成后更新页表项（帧号 + 有效位=1）；⑥恢复进程上下文，重新执行触发缺页的指令。按需调页让进程大于物理内存的原理是：进程地址空间可远超物理内存，但任一时刻只有「正在使用的工作集」驻留物理帧，其余页存在磁盘。只要缺页率低（工作集 < 物理内存），靠局部性原理大部分访问命中驻留页，偶尔缺页才去磁盘换入。物理内存只是地址空间的「缓存」。`,
    tags: ["缺页", "按需调页", "虚拟内存"],
  },
  {
    id: "mos-memory-management-4",
    chapter: "mos-memory-management",
    level: 4,
    question: `TLB 在虚拟内存中扮演什么角色？为什么说没有 TLB 虚拟内存就不可行？`,
    answer:
      `TLB（Translation Lookaside Buffer）是页表项的高速缓存。MMU 翻译地址时先查 TLB，命中则一步出物理地址（约 1 个时钟周期）；未命中才遍历多级页表（page table walk，32 位系统可能 2-3 次访存，64 位更多）。说没有 TLB 虚拟内存就不可行的原因：每次访存都要先翻译地址，若每次都走多级页表，则一次访存变成多次访存（页表本身也在内存里），性能下降数倍。TLB 利用局部性原理缓存最近翻译的页表项，典型命中率 > 99%，让绝大多数访存一步命中、和物理内存几乎一样快。这也是进程切换要刷 TLB 的原因（不同进程页表不同），也是线程切换便宜的原因（同进程共享页表，TLB 不用全刷）。`,
    tags: ["TLB", "地址翻译", "性能"],
  },
];
