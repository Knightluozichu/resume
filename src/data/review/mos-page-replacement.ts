import type { ReviewQuestion } from "./types";

export const mosPageReplacementQuestions: ReviewQuestion[] = [
  {
    id: "mos-page-replacement-1",
    chapter: "mos-page-replacement",
    level: 2,
    question: "OPT、FIFO、LRU、Clock 四种页面置换算法的核心思想和各自特点是什么？",
    answer:
      "OPT（最优置换）淘汰「未来最长时间不被访问的页」——缺页率最低但需预知未来，不可实现，仅作基准。FIFO（先进先出）淘汰「最早调入的页」——实现最简单（一个队列），但完全不考虑访问模式，可能淘汰频繁使用的页，且存在 Belady 异常。LRU（最近最少使用）淘汰「最久未被访问的页」——性能接近 OPT（利用局部性），但每次访存都要更新时间戳或链表，硬件开销大。Clock（时钟/二次机会）是 LRU 的廉价近似——每页一个访问位，指针扫到时若访问位=1 则清零跳过（给二次机会），若=0 则淘汰。只需一个 bit，硬件代价极低，性能接近 LRU，现代系统普遍用其变体。",
    tags: ["页面置换", "算法对比"],
  },
  {
    id: "mos-page-replacement-2",
    chapter: "mos-page-replacement",
    level: 3,
    question: "为什么 LRU 不会出现 Belady 异常，而 FIFO 会？什么是栈式算法？",
    answer:
      "LRU 属于「栈式算法」——设 S_n 为 n 帧时的驻留页集，则 S_n ⊆ S_(n+1) 恒成立。因为 LRU 淘汰的是「最久未访问」的页，增加一帧只是让原本会被淘汰的页多留一帧，驻留集只增不减，所以缺页率随帧数单调不增。FIFO 不是栈式——它淘汰「最早进入」的页，增加帧数可能改变进入顺序导致的淘汰决策，使得某个页在 n 帧时驻留、在 n+1 帧时反而被淘汰（因为多了一帧后进入顺序的相对关系变了）。这就是 Belady 异常：增加物理帧数，缺页率反而上升。栈式性质保证了 LRU/OPT 的缺页率随帧数单调不增，这是 FIFO 所不具备的。启示：淘汰策略不能只看「进入时间」，必须反映「访问模式」。",
    tags: ["Belady 异常", "栈式算法", "LRU"],
  },
  {
    id: "mos-page-replacement-3",
    chapter: "mos-page-replacement",
    level: 3,
    question: "什么是抖动（thrashing）？操作系统如何检测和缓解？",
    answer:
      "抖动是进程的物理帧数小于其工作集大小，导致频繁缺页——CPU 大部分时间在等磁盘 I/O 而非执行指令，利用率暴跌。根本原因是多道程序度过高——太多进程争抢有限物理帧，每个进程分到的帧都不够工作集。检测方法：监控 CPU 利用率与缺页率——若 CPU 利用率随多道程序度上升而先升后降（拐点后暴跌），说明进入抖动区。缓解方法：①降低多道程序度——把部分进程整体换出到磁盘，让剩余进程的帧数 ≥ 工作集；②工作集模型——动态估算每个进程的工作集，只调度帧数足够的进程；③缺页频率（PFV）反馈——缺页率超阈值就给该进程更多帧或挂起它。核心思想是「宁缺毋滥」——少跑几个让它们跑顺，比多跑但全在抖动强。",
    tags: ["抖动", "工作集", "多道程序度"],
  },
  {
    id: "mos-page-replacement-4",
    chapter: "mos-page-replacement",
    level: 4,
    question: "「物理内存越大缺页率一定越低」和「LRU 一定最优」这两个说法分别错在哪里？",
    answer:
      "第一个说法：多数情况成立，但 FIFO 在 Belady 异常下会反例（帧数增加缺页率反升）。更深的误区是「只要加内存就能解决卡顿」——如果进程的工作集随内存增长也在增长（如数据库缓存调大），缺页率可能不降反升。真正决定缺页率的是「物理帧数 vs 工作集大小」的关系，而非绝对内存量。第二个说法：LRU 在「扫描式访问」（顺序扫过大于内存的数据集）时会失效——每个页刚被访问就成「最新」，下一轮扫描时又全是最旧，导致 100% 缺页。这就是为什么数据库用「扫描 resistant」策略（如把扫描页标记为「一次性」不进 LRU）。没有银弹，置换算法要匹配访问模式。",
    tags: ["LRU", "扫描式访问", "工程思维"],
  },
];
