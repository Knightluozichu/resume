import type { ReviewQuestion } from "./types";

export const mosSecurityProtectionQuestions: ReviewQuestion[] = [
  {
    id: "mos-security-protection-1",
    chapter: "mos-security-protection",
    level: 2,
    question: `「保护」和「安全」有什么区别？访问控制矩阵是什么？`,
    answer:
      `保护（protection）是操作系统内部的机制，确保进程对资源的访问遵循授权规则——它是技术层面的访问控制。安全（security）是更大的目标，涵盖保护机制加上外部威胁防御（认证、加密、审计）。保护是安全的基石——没有可靠的访问控制，认证再强也防不住越权。访问控制矩阵是一个二维表：行是主体（用户/进程），列是客体（文件/设备），单元格是权限（读 r/写 w/执行 x）。这个矩阵极其稀疏——大多数用户只访问少数文件——所以不直接存矩阵，而是按行或按列压缩：按列压缩为 ACL（挂在客体上），按行压缩为能力表（装在主体上）。`,
    tags: ["保护", "安全", "访问控制矩阵"],
  },
  {
    id: "mos-security-protection-2",
    chapter: "mos-security-protection",
    level: 3,
    question: `ACL 和能力表分别如何存储访问控制矩阵？各自的优势和劣势是什么？`,
    answer:
      `ACL（访问控制表）按列存储——矩阵的每一列（一个客体）存为一张清单，挂在客体上，记录「哪些主体有什么权限」。优势：撤销权限方便（直接改文件的 ACL，立即生效）；授权检查时客体信息集中。劣势：查「某主体能访问哪些客体」要扫描所有客体的 ACL（慢）。能力表（Capability）按行存储——矩阵的每一行（一个主体）存为一张清单，装在主体上，记录「我能访问哪些客体及权限」。优势：查权限快（看自己的令牌）；权限可传递（转交令牌=委托，适合分布式系统）。劣势：撤销权限难（要找到并作废所有令牌副本，可能流散在各处）；令牌伪造风险。两者是同一矩阵的行列互转，ACL 适合「撤销频繁」（如文件系统），能力表适合「委托频繁」（如分布式系统）。`,
    tags: ["ACL", "能力表", "访问控制"],
  },
  {
    id: "mos-security-protection-3",
    chapter: "mos-security-protection",
    level: 3,
    question: `为什么密码要「加盐 + 慢哈希」？这两步分别防御什么攻击？`,
    answer:
      `加盐防御「彩虹表/预计算攻击」。若不加盐，相同密码的哈希相同，攻击者可预先计算常见密码的哈希（彩虹表），拿到哈希库后直接查表批量破解。加盐后每个用户有独立随机盐，相同密码的哈希不同，彩虹表失效。慢哈希（bcrypt/scrypt/argon2）防御「暴力/字典破解」。MD5/SHA-256 是快哈希（每秒上亿次），攻击者用 GPU 每秒尝试上亿密码，弱密码秒破。慢哈希故意让单次计算耗时（如 100ms），使破解速度从每秒上亿降到每秒十次。两步组合：加盐让预计算失效（必须逐用户算），慢哈希让逐用户算也慢到不可行。缺一不可——只加盐不慢哈希，攻击者仍可对每个盐做字典攻击；只慢哈希不加盐，相同密码的慢哈希仍相同，攻击者可对整个库批量跑字典。加盐+慢哈希让攻击必须「每用户×每候选」算慢哈希，代价指数级。`,
    tags: ["加盐", "慢哈希", "密码学"],
  },
  {
    id: "mos-security-protection-4",
    chapter: "mos-security-protection",
    level: 4,
    question: `操作系统的安全设计遵循哪两条铁律？分别是什么含义？`,
    answer:
      `两条铁律：最小权限原则和纵深防御。最小权限原则（Principle of Least Privilege）：每个进程只给完成任务所需的最小权限，不多给。例如 Web 服务进程不应以 root 运行——即便被攻破，攻击者也只能获得该服务的低权限，无法提权控制系统。权限越少，被攻破后的爆炸半径越小。纵深防御（Defense in Depth）：多层独立防护，一层被破不致全盘崩溃。例如内核态/用户态隔离（一层）+ SELinux 强制访问控制（一层）+ 防火墙（一层）+ 审计日志（一层）——攻击者要突破所有层才能完全控制系统，任一层都能拦截或告警。这两条铁律的哲学是「假设会被攻破」——最小权限限制单次攻破的影响，纵深防御让单层失守不致命，组合起来大幅提高攻击成本。`,
    tags: ["最小权限", "纵深防御", "安全设计"],
  },
];
