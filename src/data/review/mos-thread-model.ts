import type { ReviewQuestion } from "./types";

export const mosThreadModelQuestions: ReviewQuestion[] = [
  {
    id: "mos-thread-model-1",
    chapter: "mos-thread-model",
    level: 2,
    question: "进程和线程的本质区别是什么？它们各自共享和隔离什么？",
    answer:
      "进程是资源分配和保护的基本单位，线程是 CPU 调度的基本单位。进程间隔离：各自独立地址空间、页表、文件表，一个进程崩溃不影响另一个，但通信要靠 IPC（开销大）。同进程线程共享：代码段、数据段、堆、文件描述符表，通信只需读写共享变量（快但要加锁防竞态）。线程独占：栈（函数调用链）、寄存器（含 PC 和栈指针）、线程局部存储（TLS）。线程切换比进程切换便宜的本质：共享页表 → 不用换页表基址 → 不用全刷 TLB。创建线程比创建进程便宜的本质：不复制页表（fork 用写时复制也有开销，但线程压根不复制）。",
    tags: ["进程", "线程", "共享与隔离"],
  },
  {
    id: "mos-thread-model-2",
    chapter: "mos-thread-model",
    level: 3,
    question: "为什么「一个用户级线程阻塞会导致整个进程阻塞」？内核级线程为什么不会？",
    answer:
      "用户级线程由用户空间的线程库管理，内核看不到它们——内核只看到一个进程在执行。当某个用户级线程发起阻塞系统调用（如 read）时，内核把整个进程标记为阻塞态，切到别的进程。此时用户态线程库无能为力——它无法让内核只阻塞一个线程，也无法在进程被阻塞时调度其他用户线程上 CPU（因为进程根本没在跑）。内核级线程则不同：内核知道每个线程的存在，一个线程阻塞时内核只标记该线程为阻塞，然后可以直接调度同进程的其他就绪线程上 CPU。本质区别是「调度归属」：用户级线程的调度在用户库、内核级线程的调度在内核，而阻塞系统调用是内核行为——谁能调度谁就能避开阻塞。",
    tags: ["用户级线程", "阻塞"],
  },
  {
    id: "mos-thread-model-3",
    chapter: "mos-thread-model",
    level: 3,
    question: "一对一、多对一、多对多三种线程模型各自的致命短板和适用场景是什么？",
    answer:
      "多对一（M:1）：致命短板是一个线程阻塞全进程阻塞，且无法利用多核。适合纯计算、无阻塞调用、需海量轻量线程的场景（早期绿色线程）。一对一（1:1）：致命短板是创建/切换要陷内核开销大，线程数受内核资源限制（默认栈 8MB）。适合通用多核并行、需要真阻塞并发的场景——Linux/Windows 的默认模型。多对多（M:N）：致命短板是实现极复杂——需要用户态调度器与内核协调（work-stealing、阻塞检测、抢占），调试困难。适合需要海量并发（百万级）又能利用多核的场景——Go goroutine 是典范，用 2KB 小栈 + 运行时调度器实现了 M:N。现代趋势：系统级用 1:1 求稳，语言级用 M:N 求极致并发。",
    tags: ["线程模型", "权衡"],
  },
  {
    id: "mos-thread-model-4",
    chapter: "mos-thread-model",
    level: 4,
    question: "「线程一定比进程快、多线程一定比多进程好」这个说法错在哪里？",
    answer:
      "三个层面反驳。第一，共享地址空间意味着一个线程写坏内存（越界、野指针）会污染整个进程，多进程则天然隔离——浏览器用多进程而非多线程跑标签页，正是为了一个标签页崩溃不拖垮整个浏览器。第二，共享变量引入竞态条件，必须加锁，锁竞争本身就是开销——高争用场景下锁的排队等待可能比多进程的 IPC 还慢。第三，多线程在单核上不会比单线程快（甚至更慢，切换有开销），只有计算能并行拆分到多核时才真正提速。选型要看「是否需要隔离」「是否真并行」「共享通信 vs IPC 的代价」三者权衡，没有银弹。",
    tags: ["线程", "多进程", "工程思维"],
  },
];
