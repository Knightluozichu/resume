import type { ReviewQuestion } from "./types";

/** Rust 内存管理 复习题 */
export const mrsMemoryMgmtQuestions: ReviewQuestion[] = [
  {
    id: "mrs-memory-mgmt-1",
    chapter: "mrs-memory-mgmt",
    level: 1,
    question: `Rust 所有权系统的三原则是什么？`,
    answer: `三原则：1) 每个值有唯一所有者——同一时刻只有一个变量拥有该值的所有权；2) 所有者离开作用域时值自动释放——编译器在作用域结尾插入 drop 调用，无需手动 free 或 GC；3) 赋值或传参会转移所有权（move 语义）——原变量失效，新变量成为所有者。这三条规则让 Rust 在无 GC 的前提下实现内存安全。`,
    tags: ["所有权", "三原则", "基础"],
  },
  {
    id: "mrs-memory-mgmt-2",
    chapter: "mrs-memory-mgmt",
    level: 2,
    question: `Rust 的借用规则是什么？为什么可变借用和不可变借用不能同时存在？`,
    answer: `借用规则：1) 可以有任意多个不可变借用 &T 同时存在；2) 只能有唯一一个可变借用 &mut T；3) 可变借用和不可变借用不能同时存在。原因：如果同时有可变借用（写入）和不可变借用（读取），不可变借用可能读到不一致的中间状态——这是数据竞争的根源。Rust 在编译期通过借用检查器强制这条规则，从源头消灭数据竞争，这是 Rust 编译期线程安全的基础。`,
    tags: ["借用规则", "数据竞争", "理解"],
  },
  {
    id: "mrs-memory-mgmt-3",
    chapter: "mrs-memory-mgmt",
    level: 3,
    question: `请解释 Rust 生命周期的概念，并编写一个需要显式生命周期标注的函数。`,
    answer: `生命周期是编译期标注引用有效期的标记，确保引用不会比被引用的数据活得更久（防止悬垂引用）。大多数情况编译器自动推导（生命周期省略规则），但函数接受多个引用参数返回引用时需显式标注。\`\`\`rust\n// 返回的引用必须与两个参数中较短的生命周期一致\nfn longest<'a>(x: &'a str, y: &'a str) -> &'a str {\n    if x.len() > y.len() { x } else { y }\n}\n// 'a 表示 x, y 和返回值至少活一样久\n// 防止返回悬垂引用\n\`\`\`\n\n如果不标注，编译器无法确定返回引用的生命周期与哪个参数关联——可能返回一个比参数活得更久的引用导致悬垂。生命周期标注让编译器验证引用安全。`,
    tags: ["生命周期", "悬垂引用", "代码编写"],
  },
  {
    id: "mrs-memory-mgmt-4",
    chapter: "mrs-memory-mgmt",
    level: 4,
    question: `Rust 的所有权系统与 C++ 的 RAII、Java 的 GC 相比，各自的优势和代价是什么？Rust 选择了什么折衷？`,
    answer: `三种内存管理方式对比：1) C++ RAII——析构函数自动释放，零开销，但靠程序员纪律保证安全（可移动后使用、双重释放等），运行时崩溃风险高。2) Java GC——自动回收，安全无负担，但牺牲运行时性能（STW 停顿、内存开销大、不可预测延迟），不适合实时系统。3) Rust 所有权——编译期保证安全（借用检查防止 UAF 和数据竞争），运行时零开销（无 GC），代价是编译期约束严格（学习曲线陡、部分场景需与借用检查器「搏斗」）。Rust 的折衷：用编译期的严格约束换取运行时的安全和性能——如果能编译通过，就不会有内存安全问题，且运行时与 C/C++ 一样快。这是 Rust 的核心价值主张，适合对安全和性能都有高要求的场景（系统编程、嵌入式、WebAssembly）。`,
    tags: ["所有权", "RAII", "GC", "对比", "综合"],
  },
];
