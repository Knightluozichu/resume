import type { ReviewQuestion } from "./types";

export const oscFinalReviewQuestions: ReviewQuestion[] = [
  {
    id: "osc-final-review-1",
    chapter: "osc-final-review",
    level: 2,
    question: `操作系统的「资源管理闭环」包含哪四个子系统？它们如何协作？`,
    answer:
      `四大子系统：CPU 管理、内存管理、文件系统、大容量存储。协作链条：①CPU 管理创建进程并调度执行（OS 结构 + 进程调度 + 线程同步 + 死锁防护）；②进程运行时访问内存，内存管理负责地址翻译和隔离（连续分配 + 分页分段 + 虚拟内存 + 页面置换）；③内存数据需持久化，文件系统负责把字节组织成文件存到磁盘（VFS + inode + 日志）；④文件系统向磁盘发 I/O 请求，大容量存储负责调度和冗余（磁盘调度 + RAID）。闭环：CPU 调度让进程运行 → 内存管理让进程隔离运行 → 文件系统让数据持久 → 存储调度让 I/O 高效。每个子系统解决前一层的遗留问题，形成「能执行→能隔离→能持久→能高效」的完整链条。`,
    tags: ["资源管理", "系统架构", "总复习"],
  },
  {
    id: "osc-final-review-2",
    chapter: "osc-final-review",
    level: 3,
    question: `用「从 boot 到 shutdown」的完整旅程串联全书核心知识点。`,
    answer:
      `Boot：bootloader 加载内核，内核初始化调度器、内存管理、文件系统（OS 结构）。进程创建：init 进程被调度执行，fork() 创建子进程（写时复制优化），exec() 载入新程序（进程调度 + 虚拟内存按需调页）。运行：进程多线程并发，互斥锁/信号量保护共享数据（线程同步），银行家算法避免死锁（死锁）。访存：MMU 经页表翻译虚拟地址，TLB 加速，缺页时从磁盘按需调页，LRU 淘汰旧页（内存策略 + 虚拟内存）。文件 I/O：open() 经 VFS 定位 inode，read() 通过数据块指针取数据，写操作先记日志保证崩溃一致（文件系统实现）。磁盘 I/O：请求经 SCAN/C-SCAN 调度优化寻道，RAID 保障冗余（大容量存储）。Shutdown：flush 脏页到磁盘，关闭文件描述符，回收 PCB 和内存帧——全程闭环。`,
    tags: ["运行时旅程", "系统启动", "总复习"],
  },
  {
    id: "osc-final-review-3",
    chapter: "osc-final-review",
    level: 3,
    question: `操作系统如何用「抽象」和「调度」两个手段管理硬件资源？分别举例说明。`,
    answer:
      `抽象：向上隐藏硬件细节，提供简化接口。进程抽象 CPU——程序不需要关心寄存器和中断，OS 给它「独占 CPU」的幻觉。地址空间抽象内存——程序不需要关心物理帧位置，MMU 翻译使每个进程有独立的虚拟内存。文件抽象磁盘——程序不需要关心柱面/扇区/块，open/read/write 操作字节流。套接字抽象网络——程序不需要关心协议栈细节。调度：向下分配物理资源，按策略共享。CPU 调度——RR/MLFQ 给每个进程时间片，公平共享 CPU。页面置换调度——LRU/Clock 决定哪个页被淘汰，有限物理帧服务所有进程。磁盘调度——SCAN/C-SCAN 重排 I/O 请求减少寻道。抽象让程序「以为独占」，调度让资源「实际共享」——两者配合实现多程序高效共存。`,
    tags: ["抽象", "调度", "资源管理"],
  },
  {
    id: "osc-final-review-4",
    chapter: "osc-final-review",
    level: 4,
    question: `操作系统的设计哲学可以总结为哪几条核心原则？它们在具体机制中如何体现？`,
    answer:
      `①机制与策略分离：机制是「怎么做」（如调度器框架、页表硬件），策略是「做什么」（如 FCFS vs RR、LRU vs FIFO）。调度器支持插入不同策略，页表支持不同置换算法——机制通用，策略可替换。②局部性原理：程序访问有时间和空间局部性。TLB 缓存近期页表项（时间），预取相邻页（空间），工作集模型跟踪活跃页面——都基于局部性。③惰性求值：fork 用写时复制（COW）延迟拷贝，虚拟内存按需调页延迟加载——只在真正需要时付出代价。④层次化设计：VFS 在文件系统之上，TLB 在页表之上，多级页表在单级页表之上——每层解决一层问题。⑤一切皆文件：Unix 把设备、管道、网络抽象为文件——统一接口降低复杂度。这五条原则贯穿全书，是 OS 设计的底层逻辑。`,
    tags: ["设计哲学", "机制与策略", "总复习"],
  },
];
