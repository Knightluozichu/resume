import type { ReviewQuestion } from "./types";

export const oscMemoryStrategiesQuestions: ReviewQuestion[] = [
  {
    id: "osc-memory-strategies-1",
    chapter: "osc-memory-strategies",
    level: 2,
    question: `外部碎片和内部碎片的区别是什么？分页为什么能消除外部碎片？`,
    answer:
      `外部碎片：内存总量够用，但空闲块太小太分散，无法满足大请求——空闲空间总量够但被碎片化切割。内部碎片：分配的块比请求的大，多余部分浪费在块内——分配粒度大于实际需求。连续分配会同时产生两种碎片：进程间的间隙是外部碎片，分配时向上取整（如按 4KB 对齐）产生内部碎片。分页消除外部碎片的原因：每个进程的页可以分散在物理内存的任意空闲帧中，不需要连续——物理帧只要有空闲就能用，不需要大块连续空间。但分页有内部碎片：最后一页可能没填满（如进程需 17KB，分 5 页 20KB，浪费 3KB）。分段消除内部碎片（段大小精确匹配），但产生外部碎片（段间间隙不连续）。段页式结合两者，折中。`,
    tags: ["内存碎片", "分页", "分段"],
  },
  {
    id: "osc-memory-strategies-2",
    chapter: "osc-memory-strategies",
    level: 3,
    question: `首次适应、最佳适应和最差适应三种分配策略各自的特点是什么？`,
    answer:
      `首次适应（First-Fit）：从头扫描空闲链表，找第一个够大的块分配。优点是快（找到即停），缺点是低地址端碎片积累。最佳适应（Best-Fit）：找最小的能容纳请求的块分配。优点是剩出的大块保留（适合大请求），缺点是剩出的小碎片极多（比请求大一点的块被切走后剩的碎片太小无法再用）。最差适应（Worst-Fit）：找最大的块分配。优点是剩出的碎片较大（可能还能用），缺点是最大的块被消耗，大请求可能无法满足。实验表明首次适应通常综合性能最好（快且碎片可接受），最佳适应碎片最多，最差适应大请求失败率高。实际系统多采用首次适应的变体（如从上次位置开始扫描）。`,
    tags: ["内存分配", "首次适应", "最佳适应"],
  },
  {
    id: "osc-memory-strategies-3",
    chapter: "osc-memory-strategies",
    level: 3,
    question: `分页机制中，虚拟地址如何翻译成物理地址？TLB 的作用是什么？`,
    answer:
      `虚拟地址由页号 p 和偏移 d 组成。翻译过程：①用 p 查页表得到帧号 f；②物理地址 = f × 帧大小 + d（偏移直接拼接，因为页和帧大小相同）。页表存物理帧号和有效位/保护位/脏位等。问题：每次访存要先查页表（一次内存访问）再取数据（又一次内存访问），开销翻倍。TLB（Translation Lookaside Buffer，快表）是 CPU 内的硬件缓存，存近期用过的页表项。流程变为：先查 TLB，命中则直接得到帧号（一个时钟周期），未命中才查内存中的页表。TLB 命中率通常 95%+（因为局部性），使平均访存开销接近 1 倍而非 2 倍。多级页表减少页表占用的内存，但增加查表级数，TLB 更加重要。`,
    tags: ["分页", "地址翻译", "TLB"],
  },
  {
    id: "osc-memory-strategies-4",
    chapter: "osc-memory-strategies",
    level: 4,
    question: `分页和分段在地址空间维度、碎片类型和对程序员透明性上有何本质区别？`,
    answer:
      `①地址空间维度：分页是一维的——程序员只看到一个线性地址空间，硬件自动切页，程序员不感知页边界。分段是二维的——程序员需指定段号和段内偏移（如 \`load [ds:offset]\`），段是逻辑单位（代码段、数据段、栈段）。②碎片类型：分页无外部碎片（帧可分散分配），有内部碎片（最后一页未填满）。分段有外部碎片（段间间隙不连续），无内部碎片（段大小精确）。③透明性：分页对程序员透明——编译器和链接器不需要考虑页边界，硬件 MMU 自动翻译。分段对程序员可见——段的概念来自程序结构（代码/数据/栈），程序员或编译器需感知段的存在。段页式结合两者：先分段（逻辑结构），再对每段分页（消除外部碎片），是现代系统的常见做法。`,
    tags: ["分页", "分段", "地址空间"],
  },
];
