import type { ReviewQuestion } from "./types";

export const oscOsStructureQuestions: ReviewQuestion[] = [
  {
    id: "osc-os-structure-1",
    chapter: "osc-os-structure",
    level: 2,
    question: "单体内核、微内核和混合内核各自的结构特点和代表系统是什么？",
    answer:
      "单体内核：所有 OS 服务（进程调度、内存管理、文件系统、网络栈、设备驱动）都运行在内核态同一地址空间。优点是性能高（函数调用而非消息传递），缺点是臃肿（一个驱动 bug 可 crash 全系统）。代表：Linux。微内核：内核只保留最小功能（IPC、调度、地址空间），文件服务、网络、驱动等运行在用户态进程，通过消息传递通信。优点是安全可扩展，缺点是消息传递开销大。代表：MINIX、QNX。混合内核：核心服务在内核态，非关键部分可加载/卸载，兼顾性能与可扩展。代表：Windows、macOS。",
    tags: ["内核结构", "单体", "微内核"],
  },
  {
    id: "osc-os-structure-2",
    chapter: "osc-os-structure",
    level: 2,
    question: "系统调用是什么？用户态程序如何通过它请求内核服务？",
    answer:
      "系统调用是用户态程序请求内核服务的受控接口——它是用户态和内核态之间的「门」。流程：用户程序调用 C 库包装函数（如 `write()`）→ 库函数把系统调用号和参数放入寄存器 → 执行陷阱指令（如 `int 0x80` / `syscall`）→ CPU 切换到内核态，跳转到内核系统调用处理程序 → 内核根据调用号分派到对应处理函数 → 执行完毕返回用户态。系统调用是 OS 保护机制的核心：用户程序不能直接访问硬件，必须通过系统调用让内核代劳，内核检查权限和参数合法性后才执行。",
    tags: ["系统调用", "用户态", "内核态"],
  },
  {
    id: "osc-os-structure-3",
    chapter: "osc-os-structure",
    level: 3,
    question: "微内核的「消息传递」机制为什么比单体内核的「函数调用」慢？这种开销能否消除？",
    answer:
      "单体内核中，服务间通信是同一地址空间的函数调用——直接跳转+传参，纳秒级。微内核中，服务在不同用户态进程中，通信要走 IPC：发送方拷贝消息到内核 → 内核转发到接收方 → 接收方拷贝出来，至少两次用户态/内核态切换和两次数据拷贝，微秒级。这种开销来自隔离本身——不同地址空间不能直接互访，必须经内核中转。可以部分缓解：L4 微内核用寄存器传递小消息避免拷贝（可减少到接近函数调用）；但完全消除不可能，因为隔离是微内核的安全基础。所以微内核适合安全关键场景（航空、医疗），而非追求极致吞吐的场景。",
    tags: ["微内核", "消息传递", "IPC"],
  },
  {
    id: "osc-os-structure-4",
    chapter: "osc-os-structure",
    level: 4,
    question: "为什么现代主流操作系统（Windows、macOS、Linux）都不采用纯粹的微内核架构？",
    answer:
      "纯粹微内核在工程上面临三重困境：①性能瓶颈——频繁的消息传递使 I/O 密集场景吞吐量下降，如把磁盘驱动放用户态，每次 I/O 都要多次 IPC；②生态迁移成本——已有海量驱动和协议栈在内核态运行，迁到用户态需重写且兼容性差；③用户感知——用户更关心「快不快」而非「微不微」，安全优势在桌面/服务器场景不敏感。所以 Windows 和 macOS 选择混合内核：性能关键路径（调度、内存、VFS 框架）在内核态，非关键部分可模块化扩展。Linux 更彻底地坚持单体+可加载模块，用工程手段（模块化、命名空间）弥补可扩展性，而非走微内核路线。性能与安全的工程权衡决定了主流选择。",
    tags: ["内核结构", "工程权衡", "混合内核"],
  },
];
