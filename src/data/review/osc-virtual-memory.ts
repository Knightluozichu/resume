import type { ReviewQuestion } from "./types";

export const oscVirtualMemoryQuestions: ReviewQuestion[] = [
  {
    id: "osc-virtual-memory-1",
    chapter: "osc-virtual-memory",
    level: 2,
    question: "什么是请求分页（demand paging）？缺页中断的处理流程是什么？",
    answer:
      "请求分页是虚拟内存的核心机制：进程的页面只在被访问时才从磁盘加载到物理内存（按需调页），而非一次性全部加载。页表中有有效位（valid bit）：1 表示页面在内存，0 表示不在。缺页中断流程：①CPU 访问虚拟地址，查页表发现有效位=0 → 触发缺页中断；②内核 trap handler 接管，找到空闲物理帧（若无则页面置换淘汰一页）；③从磁盘读入该页到物理帧；④更新页表（帧号 + 有效位置 1）；⑤重启被中断的指令。有效访问时间 EAT = (1-p) × 内存访问时间 + p × 缺页处理时间，其中 p 是缺页率。只要 p 足够小（如 0.001），EAT 接近纯内存访问——这就是虚拟内存「用磁盘扩充内存」的数学基础。",
    tags: ["请求分页", "缺页中断", "虚拟内存"],
  },
  {
    id: "osc-virtual-memory-2",
    chapter: "osc-virtual-memory",
    level: 3,
    question: "FIFO、LRU 和 OPT 三种页面置换算法的特点是什么？什么是 Belady 异常？",
    answer:
      "FIFO（先进先出）：淘汰最先进入内存的页面。简单但愚蠢——最老的不一定最不用，可能淘汰常用页。存在 Belady 异常：增加物理帧数反而缺页更多。LRU（最近最少使用）：淘汰最久未访问的页面。基于局部性原理（最近用的大概率再用），性能接近 OPT。但实现开销大——需精确记录每页的最后访问时间，硬件支持（计数器或栈）成本高。OPT（最佳置换）：淘汰未来最久不用的页面。理论最优（缺页率最低），但不可实现（无法预知未来访问序列），只作为评估基准。Belady 异常是 FIFO 独有的：帧数增多时缺页反而增多——这是因为 FIFO 不满足「栈式算法」性质（n 帧的页面集合是 n+1 帧的子集）。LRU 和 OPT 满足栈性质，无 Belady 异常。",
    tags: ["页面置换", "FIFO", "LRU", "Belady异常"],
  },
  {
    id: "osc-virtual-memory-3",
    chapter: "osc-virtual-memory",
    level: 3,
    question: "什么是工作集模型？它如何解决抖动问题？",
    answer:
      "工作集 W(t, Δ) 是进程在过去 Δ 时间窗口内访问的页面集合——它代表进程当前的「活跃内存足迹」。工作集模型的核心思想：只有当进程的全部工作集都在物理内存中时，进程才能高效运行；否则频繁缺页。抖动（thrashing）是多道程序度过高时，每个进程分到的物理帧不足以容纳其工作集，导致所有进程都在频繁缺页、CPU 利用率骤降。解法：①监控每个进程的工作集大小，确保分配的帧数 ≥ 工作集大小——不够就挂起部分进程（降低多道程序度）；②用页错误频率（PFU）控制：缺页率过高说明帧不够→多给帧，缺页率过低说明帧富裕→回收帧，维持缺页率在合理区间。工作集模型把内存分配从「固定帧数」变为「按需供给」，从根本上防止抖动。",
    tags: ["工作集", "抖动", "页面置换"],
  },
  {
    id: "osc-virtual-memory-4",
    chapter: "osc-virtual-memory",
    level: 4,
    question: "写时复制（Copy-on-Write）如何优化 fork()？它和普通复制的区别是什么？",
    answer:
      "传统 fork() 创建子进程时，完整复制父进程的地址空间——所有页面都拷贝一份，开销巨大。但 fork() 后通常立即 exec()（替换地址空间），拷贝的页面全白费。写时复制的优化：fork() 时父子共享同一物理帧，页表项标记为只读。只有当某方写入时才触发保护异常，内核此时才拷贝该页（只拷贝被写的页），更新页表为可写。区别：①普通复制 fork 时拷贝全部页面（即使大多不被修改）；COW 只拷贝被写入的页面（延迟拷贝）。②COW 利用 fork-exec 模式：exec 会丢弃旧地址空间，共享的页面无需拷贝。③多进程共享只读数据（如代码段）时，COW 让它们共用同一物理帧，零拷贝。COW 是「惰性求值」思想在 OS 中的经典应用——只在真正需要时才付出拷贝代价。",
    tags: ["写时复制", "COW", "fork"],
  },
];
