import type { ReviewQuestion } from "./types";

export const phaDistributedFundamentalsQuestions: ReviewQuestion[] = [
  {
    id: "pha-df-1",
    chapter: "pha-distributed-fundamentals",
    level: 1,
    question: `CAP定理的三要素是什么？为什么说「三选二」是误导？`,
    answer: `CAP三要素：①C（Consistency 一致性）——所有节点在同一时刻看到相同数据，读操作总能返回最新写入值。②A（Availability 可用性）——系统持续可用，每个请求都能在合理时间内收到非错误响应（不保证是最新数据）。③P（Partition Tolerance 分区容忍）——系统在网络分区（节点间通信中断）时仍能继续工作。「三选二」是误导的原因：在分布式系统中，网络分区（P）是客观存在、不可避免的——网络总会断、节点总会挂。因此P不是「选择」而是「必须保证」的前提。实际的选择是在C和A之间权衡：当分区发生时，要么选择C（拒绝写入，牺牲可用性，即CP），要么选择A（允许写入，牺牲一致性，即AP）。CA只在单机或无分区的理想情况下成立。所以CAP不是「三选二」而是「分区发生时，C和A二选一」。CP代表：ZooKeeper/etcd（配置管理/分布式锁，宁可不可用不可数据错）。AP代表：Cassandra/DynamoDB（高可用/海量数据，宁可数据暂时不一致）。`,
    tags: ["CAP", "一致性", "可用性", "分区容忍"],
  },
  {
    id: "pha-df-2",
    chapter: "pha-distributed-fundamentals",
    level: 2,
    question: `分布式系统有哪些特有的基本问题？什么是「部分失败」和「三态」？`,
    answer: `分布式系统八大基本问题：①网络不可靠——消息可能丢失、延迟、乱序。②时钟不同步——各节点物理时钟无法完全同步，不能用绝对时间判断事件先后。③消息延迟与乱序——发送顺序不等于接收顺序。④节点故障——crash-stop（崩溃后不恢复）、crash-recovery（崩溃后恢复）、拜占庭故障（节点恶意行为）。⑤拜占庭故障——节点不仅可能崩溃，还可能发送错误/恶意信息。⑥状态分歧——不同节点对同一数据有不同视图。⑦部分失败——单机系统要么全成功要么全失败，分布式系统可能出现部分节点成功部分失败。⑧三态——单机调用只有成功/失败两态，分布式调用还有第三态：超时（不知道对方是成功还是失败）。「部分失败」是分布式系统最本质的难点：一个操作涉及多个节点，可能部分成功部分失败，需要决定是回滚已成功的还是重试已失败的。「三态」的超时状态最棘手：调用方不知道对方是否执行了操作，重试可能导致重复执行（需幂等），不重试可能导致操作丢失。对策：超时+重试+幂等+补偿事务。`,
    tags: ["分布式问题", "部分失败", "三态", "拜占庭故障"],
  },
  {
    id: "pha-df-3",
    chapter: "pha-distributed-fundamentals",
    level: 2,
    question: `为什么分布式系统不能用物理时钟判断事件先后？Lamport逻辑时钟如何解决？`,
    answer: `不能用物理时钟的原因：①各节点的物理时钟（NTP同步）存在偏差，无法保证完全一致，毫秒级偏差在高速场景下会导致事件顺序判断错误。②时钟可能回拨（NTP校时或VM迁移），导致时间戳倒退。③即使时钟完美同步，由于网络延迟不确定，发送时间早的消息可能晚到达，无法用物理时间确定因果。Lamport逻辑时钟解决方案：核心思想是用「因果关系」而非「物理时间」定义事件顺序。规则：①每个节点维护一个单调递增的计数器C。②节点执行本地事件时C=C+1。③节点发送消息时附带当前C值，接收方C=max(本地C, 消息C)+1。性质：如果事件A因果先于事件B（A→B），则C(A)小于C(B)。但反过来不成立——C(A)小于C(B)不一定意味着A因果先于B（可能是并发事件）。Lamport时钟给出的是偏序（partial order）：有因果关系的事件有序，并发事件无法比较。向量时钟（Vector Clock）进一步解决了并发事件的检测问题，每个节点维护一个向量记录所有节点的时钟值，可精确判断两个事件是因果有序还是并发。`,
    tags: ["逻辑时钟", "Lamport", "因果关系", "向量时钟"],
  },
  {
    id: "pha-df-4",
    chapter: "pha-distributed-fundamentals",
    level: 3,
    question: `BASE理论与CAP定理是什么关系？最终一致性如何理解？`,
    answer: `BASE是对CAP中AP选择的工程实践延伸，是CAP的补完。BASE含义：①Basically Available（基本可用）——允许在故障时损失部分可用性（响应时间增加或功能降级），但核心功能仍可用。②Soft State（软状态）——允许系统存在中间状态，且该状态不影响系统整体可用性（如订单「处理中」状态）。③Eventually Consistent（最终一致性）——系统保证在没有新的更新操作时，最终所有副本会收敛到一致状态，但不保证任意时刻都一致。与CAP的关系：CAP是理论定理（分区时C和A二选一），BASE是实践策略（选择AP后的工程方法论）。CAP说「不能同时保证强一致和高可用」，BASE说「那就不追求强一致，接受最终一致，换取高可用」。最终一致性的理解：①不是「不管一致」，而是「暂不一致但最终收敛」。②收敛时间取决于复制延迟、网络状况、副本数量。③收敛期间存在「读旧值」窗口。④需要配合读修复、反熵（Anti-Entropy）、Merkle树等机制加速收敛。⑤最终一致性有多种变体：读己写一致（自己能读到自己的写）、单调读一致（不会读到比上次更旧的值）、会话一致（同一会话内一致）。选择哪种取决于业务容忍度。`,
    tags: ["BASE", "CAP", "最终一致性", "基本可用"],
  },
];
