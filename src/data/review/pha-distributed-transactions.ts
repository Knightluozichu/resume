import type { ReviewQuestion } from "./types";

export const phaDistributedTransactionsQuestions: ReviewQuestion[] = [
  {
    id: "pha-dt-1",
    chapter: "pha-distributed-transactions",
    level: 1,
    question: "2PC（两阶段提交）的工作流程是什么？它有哪些致命缺陷？",
    answer: "2PC工作流程：角色——协调者（Coordinator）和参与者（Participant）。第一阶段Prepare：协调者向所有参与者发送Prepare请求，参与者执行事务操作但暂不提交，将Undo/Redo日志写入磁盘，回复Yes或No。如果所有参与者都回复Yes，协调者进入第二阶段Commit；任一回复No或超时，协调者发送Rollback。第二阶段Commit/Rollback：协调者发送Commit（或Rollback），参与者执行提交（或回滚）并释放资源，回复Ack。致命缺陷：①同步阻塞——整个事务期间所有参与者持有资源锁，其他事务必须等待，高并发下性能极差。②协调者单点——协调者在第二阶段宕机，参与者将永久阻塞在锁定状态（无法决定Commit还是Rollback）。③数据不一致——第二阶段Commit消息只发给部分参与者后协调者宕机，部分提交部分未提交，数据不一致。④过于保守——任一参与者超时即整个事务失败，容错性差。3PC（三阶段提交）通过引入CanCommit→PreCommit→DoCommit三步和参与者超时机制部分缓解了阻塞和协调者单点问题，但增加了消息轮次和延迟，且仍无法完全避免不一致，实际很少使用。",
    tags: ["2PC", "两阶段提交", "协调者单点", "同步阻塞"],
  },
  {
    id: "pha-dt-2",
    chapter: "pha-distributed-transactions",
    level: 2,
    question: "TCC（Try-Confirm-Cancel）的工作原理是什么？与2PC有什么本质区别？",
    answer: "TCC工作原理：将每个操作分为三个阶段。①Try——预留业务资源（如冻结金额、预扣库存），不做真正提交。②Confirm——确认执行业务操作（扣减冻结金额、确认扣减库存），使用预留资源完成操作。③Cancel——取消预留资源（解冻金额、释放库存），在Try失败或超时时执行。Confirm和Cancel必须幂等（可能重试）。本质区别：①2PC是资源层面（数据库锁）的协调，TCC是业务层面的协调——2PC的Prepare持有数据库锁，TCC的Try是业务级预留（如冻结金额），不持有数据库锁。②2PC阻塞——Prepare后参与者持有锁直到Commit/Rollback，TCC不阻塞——Try后不锁资源，Confirm/Cancel异步执行。③2PC对业务透明（数据库XA协议自动处理），TCC需要业务侵入（每个操作必须实现Try/Confirm/Cancel三个接口）。④2PC强一致（要么全成功要么全回滚），TCC最终一致（中间状态可见，Try后数据已变更但未确认）。TCC优势：性能好（无锁）、灵活。TCC代价：业务侵入大（需写三个接口）、需保证Confirm/Cancel幂等、需处理空回滚（Cancel时Try未执行）和悬挂（Cancel先于Try到达）。",
    tags: ["TCC", "Try-Confirm-Cancel", "业务侵入", "幂等"],
  },
  {
    id: "pha-dt-3",
    chapter: "pha-distributed-transactions",
    level: 2,
    question: "Saga模式和本地消息表/事务消息各自的工作原理和适用场景是什么？",
    answer: "Saga模式：将长事务拆分为一系列短事务（正向操作），每个正向操作有对应的补偿操作。执行方式：①编排式（Orchestration）——一个中央协调器依次调用各服务的正向操作，任一失败则反向调用已执行操作的补偿操作。②编舞式（Choreography）——各服务通过事件驱动，监听前一步的事件触发自己的操作，失败时发补偿事件。特点：无锁、适合长流程（如旅行预订：订机票→订酒店→租车，任一失败则取消之前已订的）。代价：中间状态可见（不隔离）、需写补偿逻辑、补偿可能失败（需人工介入）。本地消息表：①业务操作和消息记录在同一个本地事务中写入（保证原子性）。②定时任务轮询消息表，将未发送的消息投递到MQ。③消费者消费消息执行后续操作，须幂等。特点：简单可靠、最终一致。适用：异步解耦场景（如下单后发积分/通知）。事务消息（RocketMQ）：①发送半消息（对消费者不可见）。②执行本地事务。③本地事务成功→提交消息（消费者可见）；失败→回滚消息。④超时未确认→回查本地事务状态。特点：比本地消息表更优雅（无需轮询），但依赖MQ支持。适用：同本地消息表。选型原则：能不用分布式事务就不用；优先消息最终一致；TCC用于核心资金；Saga用于长流程编排。",
    tags: ["Saga", "本地消息表", "事务消息", "补偿操作", "最终一致"],
  },
  {
    id: "pha-dt-4",
    chapter: "pha-distributed-transactions",
    level: 3,
    question: "在实际项目中如何选型分布式事务方案？请给出一个电商下单流程的完整事务设计。",
    answer: "选型原则：①能不用分布式事务就不用——优先设计避免跨服务事务（如合并强一致操作到同一服务）。②强一致+短事务+低并发→2PC（XA），如银行核心。③高并发+可写补偿+业务可侵入→TCC，如扣库存/扣余额。④长流程+多步骤+无法预留→Saga，如旅行预订。⑤异步解耦+最终一致→本地消息表/事务消息，如下单后发积分。电商下单流程事务设计：场景——下单涉及订单服务（创建订单）、库存服务（扣库存）、优惠券服务（核销券）、支付服务（扣款）、积分服务（加积分）。分层设计：①核心强一致层（TCC）——订单创建+库存扣减+券核销，这三者必须原子性。库存Try=冻结库存、Confirm=确认扣减、Cancel=释放冻结；券Try=标记已核销、Confirm=确认、Cancel=恢复。②支付层（独立事务）——支付通过支付网关完成，支付成功后发事件。支付本身是外部系统，通过回调+本地状态机保证一致性（订单状态：待支付→已支付→已发货）。③异步最终一致层（事务消息）——支付成功后通过事务消息触发：积分服务加积分、通知服务发短信、物流服务创建运单。消费者幂等（通过唯一订单号去重）。④补偿机制——支付超时未支付，订单服务发定时任务关闭订单，库存服务监听关闭事件释放冻结库存。这样核心资金操作用TCC保证强一致，非核心操作用消息最终一致，兼顾一致性和性能。",
    tags: ["事务选型", "电商下单", "TCC", "事务消息", "分层设计"],
  },
];
