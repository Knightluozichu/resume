import type { ReviewQuestion } from "./types";

export const phaMonolithToMicroserviceQuestions: ReviewQuestion[] = [
  {
    id: "pha-mm-1",
    chapter: "pha-monolith-to-microservice",
    level: 1,
    question: `单体架构和微服务架构在数据存储、通信方式和部署上有何根本区别？`,
    answer: `根本区别：①数据存储——单体共享单一数据库，所有模块读写同一数据模型，模块间通过数据库紧耦合；微服务每个服务拥有独立数据库（Database per Service），数据所有权隔离，服务间不能直接访问对方数据库。②通信方式——单体模块间通过本地方法调用（进程内调用），延迟极低且天然事务性；微服务间通过网络通信（REST/gRPC/消息队列），存在网络延迟、超时、序列化开销，且无分布式事务保证。③部署——单体所有功能打包为一个部署单元，任何改动需重新部署整个应用；微服务独立部署独立伸缩，可按需扩缩热点服务。单体优势：开发快/部署简单/易测试/本地调用高性能；痛点：扩展难/牵一发动全身/技术栈单一/团队冲突。微服务优势：独立扩展/技术异构/故障隔离/团队自治；痛点：分布式复杂性/数据一致性/网络开销/运维复杂。`,
    tags: ["单体", "微服务", "数据存储", "通信方式", "部署"],
  },
  {
    id: "pha-mm-2",
    chapter: "pha-monolith-to-microservice",
    level: 2,
    question: `微服务拆分有哪些核心原则？什么是「分布式单体」陷阱？`,
    answer: `拆分核心原则：①领域驱动（DDD限界上下文）——按业务领域边界拆分，一个限界上下文对应一个微服务，高内聚低耦合。②独立数据所有权——每服务独享数据库，禁止跨服务直接访问数据库，数据共享只能通过API。③单一职责——一个服务只做一件事，可独立替换和扩展。④服务可独立部署——不依赖其他服务即可构建、测试、部署。⑤团队自治——一个微服务由一个小团队（pizza team）全权负责。⑥合适粒度——不是越细越好，拆分过细导致网络开销和管理成本激增。「分布式单体」陷阱：拆分了服务但没做到真正解耦——服务间强依赖（同步长链路调用）、共享数据库（数据耦合）、共享代码库（代码耦合）、必须同时部署（部署耦合）。结果是：承受了分布式的所有代价（网络延迟/一致性复杂/运维负担），却没有获得独立扩展和故障隔离的好处。避免方法：异步通信代替同步链路、数据所有权彻底隔离、公共代码提取为独立包或服务、部署解耦。`,
    tags: ["服务拆分", "DDD", "分布式单体", "限界上下文"],
  },
  {
    id: "pha-mm-3",
    chapter: "pha-monolith-to-microservice",
    level: 2,
    question: `微服务架构下，服务间数据共享有哪些模式？各自的适用场景和代价是什么？`,
    answer: `数据共享模式：①API调用——服务A调用服务B的API获取数据。最常见，保持数据所有权隔离，代价是网络延迟和可用性依赖（B挂了A受影响）。适用：实时性要求高、数据量小。②数据冗余/物化视图——服务A在本地存储服务B的部分数据副本（如订单服务存用户姓名快照），通过事件同步更新。代价是数据可能短暂不一致。适用：读多写少、可容忍最终一致。③事件驱动同步——服务B数据变更后发事件到MQ，服务A订阅并更新本地副本。解耦最好，代价是最终一致和事件处理复杂性。适用：数据变更频次适中、可异步。④CQRS（命令查询职责分离）——写服务和读服务分离，写服务发事件，读服务订阅构建读模型。代价是架构复杂。适用：读写比例悬殊、读模型多样化。⑤共享数据库（反模式）——多个服务直接访问同一数据库。虽然简单但破坏了数据所有权隔离，是分布式单体的根源。禁止使用，除非过渡期。关键原则：微服务间不共享数据库，数据共享优先用API+缓存或事件驱动。`,
    tags: ["数据共享", "CQRS", "事件驱动", "API调用"],
  },
  {
    id: "pha-mm-4",
    chapter: "pha-monolith-to-microservice",
    level: 3,
    question: `如何判断微服务拆分粒度是否合适？拆分过细和过粗各有什么问题？`,
    answer: `判断标准：①一个微服务的修改原因应该单一（单一职责）——如果一个服务的修改总是涉及多个不相关的业务场景，说明拆分过粗。②一个微服务的发布周期应该独立——如果两个服务总是需要同时发布才能正确工作，说明它们应合并或存在过度耦合。③一个微服务的团队规模应在3-8人（两个披萨原则）——如果一个服务需要多于8人维护，可能拆分过粗。④服务间网络调用链不应过深——如果一个用户请求需经过5+个微服务同步调用，说明拆分过细。⑤数据一致性需求——如果两个服务的数据必须强一致且频繁联动，可能应合并。拆分过细的问题：网络开销大（一次操作多次RPC）、分布式事务复杂、运维成本高（几十上百个服务）、调试困难（链路追踪复杂）、团队沟通成本高。拆分过粗的问题：失去独立扩展能力（热点无法单独扩缩）、团队冲突（多人改同一服务）、部署耦合（小改动也要全量发布）、失去技术异构优势。实践建议：先粗后细——初期按大业务域拆分（3-7个服务），随业务发展再按需细拆，避免一上来就拆成几十个微服务。`,
    tags: ["拆分粒度", "两个披萨原则", "先粗后细"],
  },
];
