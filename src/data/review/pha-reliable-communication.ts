import type { ReviewQuestion } from "./types";

export const phaReliableCommunicationQuestions: ReviewQuestion[] = [
  {
    id: "pha-rc-1",
    chapter: "pha-reliable-communication",
    level: 1,
    question: `熔断器的三个状态是什么？状态转换的条件是什么？`,
    answer: `熔断器三态：①Closed（关闭）——正常状态，所有请求放行。统计窗口内持续监控错误率/慢调用比例。当错误率超过阈值（如50%）或慢调用比例超阈值时，状态转为Open。②Open（打开）——熔断状态，所有请求快速失败（不调用后端服务），直接执行降级逻辑（返回默认值/缓存/兜底）。经过一个熔断恢复等待时间（如5秒）后，状态转为Half-Open。③Half-Open（半开）——试探恢复状态，放行一个（或少量）请求试探后端是否恢复。如果试探请求成功，说明后端已恢复，状态转为Closed（恢复正常）；如果试探请求失败，说明后端仍未恢复，状态转回Open（继续熔断，重置等待计时器）。关键参数：错误率阈值、最小请求量（统计窗口内请求太少时不触发熔断）、熔断持续时间、半开试探请求数、恢复成功率阈值。实现：Hystrix（已停更）/Sentinel（阿里开源，主流）/Resilience4j（轻量）。熔断的目标：快速失败防止级联故障雪崩，给后端恢复时间，同时通过半开机制自动探测恢复。`,
    tags: ["熔断器", "Closed", "Open", "Half-Open", "Sentinel"],
  },
  {
    id: "pha-rc-2",
    chapter: "pha-reliable-communication",
    level: 2,
    question: `漏桶算法和令牌桶算法的区别是什么？如何实现集群限流？`,
    answer: `漏桶vs令牌桶：①漏桶——请求像水滴倒入桶中，桶以恒定速率出水（处理请求）。桶满则溢出（拒绝请求）。特点：输出速率恒定，不允许突发流量。即使瞬间来100个请求，也只按固定速率处理。适合需要严格匀速的场景。②令牌桶——桶中以恒定速率放入令牌，请求处理前需从桶中取一个令牌。桶满则令牌丢弃。特点：允许突发流量（桶中积累的令牌可瞬间消费）。只要桶中有令牌就能处理请求。适合允许短时突发的场景。区别核心：漏桶限制请求处理速率（出水恒定），令牌桶限制请求平均速率但允许瞬时突发（令牌可积累）。集群限流实现：①Redis+Lua——将限流计数器存储在Redis中，所有节点共享。Lua脚本保证「读取计数+判断+递增」的原子性。令牌桶：Redis存储上次取令牌时间和剩余令牌数，Lua脚本计算当前应补充的令牌数并判断是否放行。②网关层限流——在API网关（如Spring Cloud Gateway/Kong）统一限流，所有请求先过网关。③分布式限流服务——独立限流服务（如Sentinel Cluster模式），Token Server统一分配令牌给各节点。注意：Redis限流有网络延迟，超高并发下可能不精确（可用本地+Redis双层：本地预分配+Redis校准）。`,
    tags: ["漏桶", "令牌桶", "集群限流", "Redis Lua"],
  },
  {
    id: "pha-rc-3",
    chapter: "pha-reliable-communication",
    level: 2,
    question: `什么是幂等性？在分布式系统中如何实现幂等？`,
    answer: `幂等性：对同一操作执行一次和执行多次的效果完全相同。在分布式系统中，由于网络超时（三态）导致重试，必须保证重试不会产生副作用（如重复扣款/重复下单）。幂等实现方式：①唯一ID+去重表——每个请求携带全局唯一ID（如UUID/雪花ID），处理前查去重表，已存在则直接返回之前的结果。②Token机制——客户端先请求获取唯一Token，提交请求时携带Token，服务端消费Token后失效。重复提交时Token已失效，拒绝。③乐观锁（CAS）——更新操作带version字段，UPDATE ... WHERE version=X，更新成功则version+1。重复更新时version已变，更新失败。④状态机——只允许合法的状态转换，如订单「待支付→已支付」允许，「已支付→已支付」拒绝。防重复支付。⑤数据库唯一约束——利用数据库唯一索引（如订单号+操作类型），重复插入报错，捕获后返回已有结果。⑥幂等键+TTL——Redis存储幂等键，SETNX(key, result, TTL)，重复请求返回已有结果。关键场景：支付回调（可能重复通知）、MQ消费（At-Least-Once语义导致重复消费）、接口重试。原则：写操作必须幂等（GET天然幂等），重试前必须确保目标操作幂等。`,
    tags: ["幂等性", "唯一ID", "Token", "乐观锁", "状态机"],
  },
  {
    id: "pha-rc-4",
    chapter: "pha-reliable-communication",
    level: 3,
    question: `什么是「面向失败设计」？请设计一个防止级联雪崩的韧性方案。`,
    answer: `面向失败设计（Design for Failure）：核心假设——在分布式系统中，一切都会失败：网络会断、服务会挂、依赖会慢、数据会丢、时钟会偏。不追求「永不故障」（不可能），而是追求「故障可控、可恢复」。防雪崩韧性方案（全链路防护）：①入口层——限流（令牌桶控制总QPS）+ 降级（超载时拒绝低优先级请求，保核心）。②网关层——熔断（对每个后端服务设独立熔断器，错误率超阈值快速失败）+ 超时（网关层设全局超时如3秒，防止慢请求占用连接）。③服务间调用——超时分级（连接超时1s/读超时3s/写超时5s）+ 重试（指数退避+抖动+最大3次，仅对幂等接口重试）+ 熔断（Sentinel对下游服务熔断）+ 降级（下游不可用时返回缓存/默认值）。④资源隔离——线程池隔离（不同服务用不同线程池，避免一个服务耗尽线程池拖垮全局）或信号量隔离；舱壁模式（Bulkhead）限制每个服务的最大并发。⑤缓存层——多级缓存（本地+Redis），DB不可用时返回缓存兜底数据。⑥DB层——读写分离（读请求走从库）+ 连接池限流（最大连接数限制，防止DB被打满）。⑦消息队列——削峰填谷（同步请求转异步MQ），消费者侧限流。⑧监控告警——实时监控各层错误率/延迟/资源使用率，自动触发扩容或告警。核心思想：通过层层防护，将局部故障限制在局部，不让它沿调用链扩散为全局雪崩。系统在降级中继续提供核心服务（非核心功能降级，核心功能保活）。`,
    tags: ["面向失败设计", "防雪崩", "舱壁模式", "韧性方案", "Design for Failure"],
  },
];
