import { ReviewQuestion } from "./types";

export const prlGraphicalModelsQuestions: ReviewQuestion[] = [
  {
    id: "prl-graphical-models-1",
    chapter: "prl-graphical-models",
    level: 1,
    question: `贝叶斯网络和马尔可夫随机场有什么根本区别？各自适合什么场景？`,
    answer:
      `根本区别：①边的类型——贝叶斯网络用有向边表示因果关系（A→B 表示 A 导致 B），MRF 用无向边表示对称相关（A-B 表示 A 和 B 相关但无因果方向）。②因子分解——贝叶斯网络 \`p(x)=Πp(x_i|pa(x_i))\`（条件概率乘积），MRF \`p(x)=(1/Z)Π_C ψ_C(x_C)\`（团势能乘积/配分函数）。③条件独立性判据——贝叶斯网络用 d-分离（需考虑路径方向和 v-结构），MRF 用图连通性（移除观测变量后无路径=独立，更简单）。④环约束——贝叶斯网络不能有有向环（DAG），MRF 可有环。⑤归一化——贝叶斯网络局部条件概率自动归一化，MRF 需全局配分函数 Z（通常无解析解，计算困难）。适合场景：贝叶斯网络适合表达因果/层次依赖——医疗诊断（疾病→症状）、专家系统、贝叶斯回归的层次模型。MRF 适合表达对称相关——图像分割（相邻像素相关）、空间统计、玻尔兹曼机。选择依据：若变量间有明确因果方向选贝叶斯网络；若只有对称相关关系选 MRF；若需统一表示用因子图。`,
    tags: ["贝叶斯网络", "马尔可夫随机场", "有向图", "无向图"],
  },
  {
    id: "prl-graphical-models-2",
    chapter: "prl-graphical-models",
    level: 2,
    question: `条件独立性在图模型中起什么作用？为什么它能大幅减少参数量？`,
    answer:
      `条件独立性在图模型中的作用：①参数减少——N 个二元变量的全连接联合分布需要 \`2^N-1\` 个参数（指数级）。若变量间有条件独立性，联合分布可因子化为局部项乘积。例如贝叶斯网络 \`p(x)=Πp(x_i|pa(x_i))\`，若每个节点最多有 d 个父节点，参数量降为 \`O(N·2^d)\`（多项式级）。②计算加速——推断（求边缘/后验）只需局部计算，不必遍历所有 \`2^N\` 种变量组合。和积算法在树结构上只需线性时间。③可解释性——图结构使变量间的依赖关系一目了然，无边的变量条件独立。④指导推断算法设计——条件独立性决定消息传递的路径和顺序。例子：链式结构 \`x₁→x₂→...→x_N\`，全连接需 \`2^N\` 参数，但因子化 \`p(x)=p(x₁)Πp(x_i|x_{i-1})\` 只需 \`O(N)\` 参数。MRF 同理——图像中每个像素只与相邻像素相关（局部团），参数量从 \`O(2^{N²})\` 降到 \`O(N)\`。条件独立性是图模型区别于全连接联合分布的根本优势——它利用了真实世界中变量间稀疏的依赖结构。`,
    tags: ["条件独立性", "参数量", "因子分解", "稀疏依赖"],
  },
  {
    id: "prl-graphical-models-3",
    chapter: "prl-graphical-models",
    level: 2,
    question: `精确推断和近似推断各有什么方法？各自的优缺点是什么？`,
    answer:
      `精确推断方法：①变量消除（VE）——按顺序消除非查询变量，每次做局部求和。优点：精确。缺点：复杂度依赖消除顺序（最优顺序 NP-hard），最坏指数级。②和积算法（置信传播）——在树结构图上节点间传递消息。优点：树结构上精确高效（线性时间）。缺点：只适用于树结构（无环）。③割集调整——固定某些变量使有环图变树。缺点：子集选择是 NP-hard。近似推断方法：①变分推断（VI）——用简单分布 q 近似后验 p，最小化 \`KL(q||p)\` 或最大化 ELBO。优点：将推断转为优化（可用梯度下降）、比 MCMC 快、可扩展到大数据和深度学习（VAE）。缺点：近似有偏（q 的形式限制低估后验方差）。②环状置信传播——在有环图上强行运行和积算法。优点：简单、实践中常收敛到好的近似。缺点：不保证收敛、不保证精确。③MCMC——用马尔可夫链采样近似后验。优点：渐近精确。缺点：计算昂贵、收敛诊断困难、不易并行。选择：树结构用和积算法（精确高效）；中等规模用变分推断（快速近似）；小规模需精确用 MCMC；现代深度学习用变分推断（VAE）。`,
    tags: ["精确推断", "近似推断", "变分推断", "MCMC"],
  },
  {
    id: "prl-graphical-models-4",
    chapter: "prl-graphical-models",
    level: 3,
    question: `图模型如何与 PRML 的其他章节（贝叶斯回归、混合EM、变分推断）联系？`,
    answer:
      `图模型是 PRML 的表示工具，贯穿全书：①与 ch1 概率论的联系——图模型是概率分布的因子化表示。联合分布 \`p(x)=Πp(x_i|pa(x_i))\` 是概率乘法规则的图化，条件独立性是边际/条件概率的图编码。②与 ch2 贝叶斯回归的联系——贝叶斯回归可表示为贝叶斯网络 \`α→w→t←β\`。α 是超参（先验精度），w 是隐变量（权重），t 是观测，β 是噪声精度。证据框架对 α,β 的学习可看作图模型上的推断。③与 ch8 混合EM的联系——GMM 是隐变量图模型：\`π→z→x\`，z 是隐变量（分量标签），x 是观测。EM 算法是图模型上的推断——E 步推断隐变量后验 \`p(z|x)\`，M 步更新参数。EM 是变分推断的特例。④与变分推断的联系——图模型的条件独立性指导变分分布 q 的设计。平均场假设 \`q(Z)=Πq_i(Z_i)\`（完全分解）利用了条件独立性使变分推断可行。复杂图结构（如 VAE 的编码器-解码器）也用图模型表示。总之，图模型是 PRML 的「表示层」——它提供了编码变量依赖、指导推断算法设计的统一框架。`,
    tags: ["图模型", "贝叶斯回归", "混合EM", "变分推断", "知识联系"],
  },
];
