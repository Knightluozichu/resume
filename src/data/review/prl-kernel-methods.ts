import { ReviewQuestion } from "./types";

export const prlKernelMethodsQuestions: ReviewQuestion[] = [
  {
    id: "prl-kernel-methods-1",
    chapter: "prl-kernel-methods",
    level: 1,
    question: `对偶表示如何将线性回归的复杂度从特征维度转移到样本数量？这个转移有什么意义？`,
    answer:
      `原始形式：\`y(x)=wᵀφ(x)\`，\`w=(ΦᵀΦ+λI)⁻¹Φᵀt\`。\`ΦᵀΦ\` 是 M×M 矩阵（M=特征维度），求逆复杂度 \`O(M³)\`。对偶形式：\`y(x)=Σa_n k(x_n,x)\`，\`a=(K+λI)⁻¹t\`。K 是 N×N 核矩阵（N=样本数），求逆复杂度 \`O(N³)\`。转移方法：利用矩阵 Woodbury 恒等式 \`(ΦᵀΦ+λI)⁻¹Φᵀ = Φᵀ(ΦΦᵀ+λI)⁻¹\`，将 M×M 矩阵求逆转化为 N×N 矩阵求逆。意义：①无限维可行——当使用高斯核时，特征空间是无限维的（M=∞），原始形式不可行，但对偶形式只需 N×N 核矩阵仍可行。②大数据降维——当特征维度远大于样本数（M>>N）时，对偶形式 \`O(N³)\` 比原始形式 \`O(M³)\` 高效得多。③核技巧基础——对偶形式中所有计算都通过核函数 \`k(x,x')=φ(x)ᵀφ(x')\`，无需显式构造 \`φ(x)\`。④统一框架——SVM、GP、核 PCA 都基于对偶表示。代价：当 N>>M 时对偶反而更慢。`,
    tags: ["对偶表示", "复杂度转移", "Woodbury恒等式", "核技巧"],
  },
  {
    id: "prl-kernel-methods-2",
    chapter: "prl-kernel-methods",
    level: 2,
    question: `核技巧为什么能在无限维特征空间中工作？Mercer 条件保证了什么？`,
    answer:
      `核技巧能在无限维空间工作的原因：高斯核 \`k(x,x')=exp(-‖x-x'‖²/2σ²)\` 展开后是幂级数 \`exp(-‖x-x'‖²/2σ²)=Σ_n (1/n!)(-‖x-x'‖²/2σ²)^n\`，每项是多项式核的幂次，对应特征空间中的一个维度。由于级数无限项，高斯核对应的特征空间是无限维的。核技巧的关键：对偶形式中所有计算只需核函数 \`k(x,x')=φ(x)ᵀφ(x')\`，而不需要显式构造 \`φ(x)\`。因此即使 \`φ(x)\` 是无限维的，核函数仍是一个有限标量，计算可行。Mercer 条件保证：一个函数 \`k(·,·)\` 是合法核（即存在对应的特征映射 φ 使 \`k(x,x')=φ(x)ᵀφ(x')\`）当且仅当对任意有限输入集，核矩阵 K 是半正定对称的。这保证：①核矩阵可分解为 \`K=ΦΦᵀ\`（存在特征映射）；②对偶形式的矩阵求逆可行（\`K+λI\` 正定可逆）；③核组合律成立（\`k₁+k₂\`、\`k₁·k₂\` 等组合仍是合法核）。Mercer 条件使我们能验证核合法性而无需构造特征映射——只需检查半正定性。`,
    tags: ["核技巧", "Mercer条件", "无限维", "半正定"],
  },
  {
    id: "prl-kernel-methods-3",
    chapter: "prl-kernel-methods",
    level: 2,
    question: `高斯过程的预测均值和预测方差各有何含义？超参数如何学习？`,
    answer:
      `预测均值 \`m*=k*ᵀ(K+β⁻¹I)⁻¹y\`：与新输入 \`x*\` 相似的训练样本（\`k(x_n,x*)\` 大）对预测贡献大，不相似的贡献小。形式上与对偶线性回归一致——但 GP 的 \`(K+β⁻¹I)⁻¹y\` 考虑了噪声 \`β⁻¹\`。预测均值代表「最可能的预测值」。预测方差 \`s*²=k(x*,x*)-k*ᵀ(K+β⁻¹I)⁻¹k*\`：\`k(x*,x*)\` 是先验方差（不看数据时函数值的不确定性），减去 \`k*ᵀ(K+β⁻¹I)⁻¹k*\` 是数据带来的信息增益。训练数据附近的 \`x*\` 与训练样本核值大（\`k*\` 大），信息增益大，方差小（预测确信）；远离训练数据的 \`x*\` 核值小，信息增益小，方差大（接近先验不确定性）。预测方差量化「模型有多确信」。超参学习：核函数超参（如高斯核的 σ）通过最大化边际似然 \`ln p(y|X)=-½yᵀC⁻¹y-½ln|C|-(N/2)ln(2π)\` 学习（\`C=K+β⁻¹I\`）。数据拟合项鼓励拟合数据；复杂度惩罚 \`-½ln|C|\` 自动 Occam 剃刀。梯度优化边际似然自动平衡拟合与复杂度，无需交叉验证。`,
    tags: ["高斯过程", "预测均值", "预测方差", "边际似然"],
  },
  {
    id: "prl-kernel-methods-4",
    chapter: "prl-kernel-methods",
    level: 3,
    question: `高斯过程与线性回归的贝叶斯处理（ch2）有什么关系？GP 的优势和局限是什么？`,
    answer:
      `关系：GP 是贝叶斯线性回归在核方法上的推广。贝叶斯线性回归（ch2）：先验 \`p(w)=N(0,α⁻¹I)\`，后验 \`p(w|t)=N(m_N,S_N)\`，预测分布 \`p(t*|x*,t)=N(m_Nᵀφ*, σ_N²(x*))\`。当用基函数 \`φ(x)\` 定义核 \`k(x,x')=φ(x)ᵀφ(x')\` 时，贝叶斯线性回归的预测均值和方差可完全用核函数表示——恰为 GP 的预测公式。因此 GP = 贝叶斯线性回归 + 核技巧：①在权重空间看是贝叶斯线性回归；②在函数空间看是 GP（直接对函数值定义高斯先验）。两者数学等价但视角不同——权重空间需指定参数维度，函数空间（GP）无需指定参数数量（非参数方法）。优势：①预测自带不确定度——方差量化预测置信度；②非参数——无需指定模型复杂度，自动适应数据；③超参自动学习——边际似然自动选核参数；④理论优雅——完整的贝叶斯框架。局限：①\`O(N³)\` 训练复杂度——核矩阵求逆，大数据需稀疏近似；②核函数选择——需先验选择核类型；③非稀疏——所有训练样本参与预测（不像 SVM 只用支持向量）；④多模态后验——GP 假设高斯，不适合多模态函数分布。`,
    tags: ["高斯过程", "贝叶斯线性回归", "非参数", "核方法贝叶斯化"],
  },
];
