import { ReviewQuestion } from "./types";

export const prlLinearClassificationQuestions: ReviewQuestion[] = [
  {
    id: "prl-linear-classification-1",
    chapter: "prl-linear-classification",
    level: 1,
    question: `线性分类的三种建模路径有何区别？各自在什么场景下有优势？`,
    answer:
      `三种路径：①判别函数——直接学 \`y(x)\` 的决策边界，不建模概率。优势：最简单、计算快。劣势：不输出概率、无法衡量不确定性。适合：线性可分且不需要概率输出的简单场景。②生成模型——建模类条件密度 \`p(x|C_k)\` 和类先验 \`p(C_k)\`，用贝叶斯定理求后验。优势：可生成新数据、可处理缺失数据、可融入先验知识、小样本时利用分布假设。劣势：对分布假设敏感。适合：数据分布已知接近高斯、需要生成数据、有缺失数据、小样本场景。③判别模型（逻辑回归）——直接建模后验 \`p(C_k|x)\`，不假设数据分布。优势：假设少、数据多时更高效、实践中最常用。劣势：不能生成数据、不能处理缺失数据。适合：大多数实际分类任务。三者假设递减（生成最强、判别函数最弱），实用性递增（判别模型最常用）。`,
    tags: ["判别函数", "生成模型", "判别模型", "逻辑回归"],
  },
  {
    id: "prl-linear-classification-2",
    chapter: "prl-linear-classification",
    level: 2,
    question: `逻辑回归的交叉熵损失与线性回归的平方误差损失有何区别？为什么逻辑回归更适合分类？`,
    answer:
      `区别：①损失形式——线性回归用平方误差 \`E=(1/2)Σ(t_n-y_n)²\`，逻辑回归用交叉熵 \`E=-Σ[t_n ln y_n+(1-t_n)ln(1-y_n)]\`。②梯度——线性回归梯度 \`∇E=Σ(y_n-t_n)φ_n\`（残差×特征），逻辑回归梯度 \`∇E=Σ(y_n-t_n)φ_n\`（预测误差×特征），形式相似但 y_n 的定义不同。③凸性——两者都是凸函数（全局最优保证）。④对异常值——平方误差对异常值敏感（二次惩罚），交叉熵更鲁棒（对数惩罚）。⑤饱和——逻辑回归的 sigmoid 在两端饱和但梯度仍为预测误差×特征不受饱和影响。逻辑回归更适合分类的原因：①概率输出——sigmoid 输出 (0,1) 概率，可直接做决策和不确定性量化，而线性回归输出无界不适合分类。②最大似然一致性——逻辑回归是伯努利似然的最大似然估计，理论上有最优渐近性质。③鲁棒性——交叉熵损失对异常值更鲁棒。`,
    tags: ["交叉熵", "平方误差", "逻辑回归", "凸优化"],
  },
  {
    id: "prl-linear-classification-3",
    chapter: "prl-linear-classification",
    level: 2,
    question: `拉普拉斯近似如何工作？它在贝叶斯逻辑回归中扮演什么角色？`,
    answer:
      `拉普拉斯近似用高斯近似非高斯后验。步骤：①找到后验众数 \`w_MAP\`（梯度为零的点，即 MAP 估计，可用 IRLS/牛顿法求解）；②计算负对数后验的 Hessian \`A=-∇²ln p(w|D)|_{w_MAP}\`（曲率信息）；③近似 \`q(w)=N(w_MAP, A⁻¹)\`。直觉：在众数处做泰勒展开，二阶项恰好定义一个高斯（均值=众数，协方差=Hessian 逆）。在贝叶斯逻辑回归中的角色：逻辑回归的似然是伯努利（非高斯），加高斯先验后后验不是高斯，无法直接做边际化预测。拉普拉斯近似将非高斯后验近似为高斯，使后续计算可行：①参数不确定性——\`A⁻¹\` 给出参数的近似协方差；②边际化预测——\`p(t*|x*,D)≈∫p(t*|x*,w)q(w)dw\`，由于 \`q(w)\` 是高斯，积分有近似解析解；③模型证据——可近似计算边际似然用于模型比较。局限：①只用局部曲率信息，多峰后验效果差；②高斯近似可能不对称偏离真实后验。拉普拉斯近似是变分推断（更灵活）的前驱。`,
    tags: ["拉普拉斯近似", "贝叶斯逻辑回归", "Hessian", "高斯近似"],
  },
  {
    id: "prl-linear-classification-4",
    chapter: "prl-linear-classification",
    level: 3,
    question: `共享协方差的高斯生成模型如何退化为逻辑回归？这个关系有何意义？`,
    answer:
      `推导：假设二分类，类条件密度为高斯且共享协方差 \`p(x|C_k)=N(x|μ_k,Σ)\`。后验 \`p(C₁|x)=p(x|C₁)p(C₁)/[p(x|C₁)p(C₁)+p(x|C₂)p(C₂)]\`。取对数比 \`ln[p(C₁|x)/p(C₂|x)]=ln[p(x|C₁)/p(x|C₂)]+ln[p(C₁)/p(C₂)]\`。高斯对数密度之差中，二次项 \`xᵀΣ⁻¹x\` 被消去（共享协方差），只剩线性项 \`a=wᵀx+b\`，因此 \`p(C₁|x)=σ(wᵀx+b)\`。即共享协方差高斯生成模型的后验恰为 logistic sigmoid 的线性组合——与逻辑回归形式完全一致。意义：①理论统一——逻辑回归是高斯生成模型的「假设更少」版本。生成模型假设数据分布（高斯），逻辑回归不假设数据分布只假设后验形式。②数据效率权衡——当数据确实接近高斯时，生成模型用更少数据达到同样效果（利用了分布假设的额外信息）；当数据不接近高斯时，生成模型假设错误效果差，逻辑回归更鲁棒。③决策边界——共享协方差高斯 → 线性决策边界（逻辑回归也是线性）；不同协方差高斯 → 二次决策边界。这个关系揭示了「假设强度 vs 数据效率」的根本权衡。`,
    tags: ["生成模型", "逻辑回归", "共享协方差", "假设强度"],
  },
];
