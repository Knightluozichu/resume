import { ReviewQuestion } from "../types";

export const prlMixtureEmQuestions: ReviewQuestion[] = [
  {
    id: "prl-mixture-em-1",
    chapter: "prl-mixture-em",
    level: 1,
    question: "混合高斯模型为什么要引入隐变量？直接最大似然有什么困难？",
    answer:
      "引入隐变量的原因：GMM 的 `p(x)=Σπ_k N(x|μ_k,Σ_k)` 可自然地用隐变量 z（one-hot）表示为 `p(x)=Σ_z p(x|z)p(z)`，其中 `p(z_k=1)=π_k`，`p(x|z_k=1)=N(x|μ_k,Σ_k)`。隐变量 z 表示「样本属于哪个分量」。如果能观测到 z，最大似然很简单——每个分量独立拟合。直接最大似然的困难：对数似然 `ln p(X|π,μ,Σ)=Σ_n ln Σ_k π_k N(x_n|μ_k,Σ_k)` 中，对数内有求和 `ln Σ_k`。对数和求和不可交换，无法将各分量参数分离——令梯度为零时，`μ_k` 的方程中耦合了所有分量的 `π_j N(x_n|μ_j,Σ_j)`（通过分母 `Σ_j`），无解析解。隐变量的价值：引入 z 后，完整数据对数似然 `ln p(X,Z|θ)=Σ_n Σ_k z_nk [ln π_k + ln N(x_n|μ_k,Σ_k)]` 中 `z_nk` 将各分量分离——若 z 已知，各分量独立优化有解析解。EM 算法正是利用这一结构：E 步推断 z 的分布（责任度），M 步在已知 z 分布下优化参数。",
    tags: ["混合高斯", "隐变量", "最大似然", "EM"],
  },
  {
    id: "prl-mixture-em-2",
    chapter: "prl-mixture-em",
    level: 2,
    question: "EM 算法的 E 步和 M 步分别做什么？为什么它能保证对数似然单调上升？",
    answer:
      "E 步：用当前参数 `θ^(t)` 计算隐变量后验（责任度）`γ(z_nk)=π_k N(x_n|μ_k,Σ_k)/Σ_j π_j N(x_n|μ_j,Σ_j)`。这是「猜测」每个样本的归属概率。M 步：用责任度作为软标签加权更新参数：`μ_k=(1/N_k)Σ γ(z_nk)x_n`（责任度加权均值），`Σ_k` 责任度加权协方差，`π_k=N_k/N`。这是在已知归属分布下重新拟合各分量。单调上升的证明：对数似然可分解为 `ln p(X|θ)=L(q,θ)+KL(q(Z)||p(Z|X,θ))`，L 为 ELBO，`KL≥0`。①E 步固定 θ 最大化 L 关于 q → 最小化 KL → `q(Z)=p(Z|X,θ)`（后验精确计算时 KL=0），此时 `L=ln p(X|θ)`，下界紧贴对数似然。②M 步固定 q 最大化 L 关于 θ → 提升下界。由于下界在 `θ^(t)` 处与对数似然相切（KL=0），最大化下界必然使对数似然上升或持平：`ln p(X|θ^(t+1)) ≥ L(θ^(t+1)) ≥ L(θ^(t)) = ln p(X|θ^(t))`。关键洞察：E 步使下界紧贴（KL=0），M 步提升下界，下界提升带动对数似然提升。EM 收敛到局部最优（非全局），需多次初始化。",
    tags: ["EM算法", "E步", "M步", "单调上升", "ELBO"],
  },
  {
    id: "prl-mixture-em-3",
    chapter: "prl-mixture-em",
    level: 2,
    question: "EM 与变分推断有什么关系？当后验不可解析时如何处理？",
    answer:
      "关系：EM 是变分推断的特例。对数似然分解 `ln p(X|θ)=L(q,θ)+KL(q||p(Z|X,θ))`，L 为 ELBO，`KL≥0`。EM：①E 步固定 θ 最大化 L 关于 q → 当后验 `p(Z|X,θ)` 可精确计算时，`q=p(Z|X,θ)` 使 KL=0，ELBO=ln p(X)（下界紧致）。②M 步固定 q 最大化 L 关于 θ。变分推断：当后验 `p(Z|X,θ)` 不可精确计算时，E 步无法使 KL=0。此时用简单分布 q（如平均场 `q(Z)=Πq_i(Z_i)`）近似后验，最小化 `KL(q||p)`（但 `KL>0`，`ELBO<ln p(X)`）。这称为变分 EM：①E 步用 q 近似后验（优化 q 的变分参数使 ELBO 最大）；②M 步更新模型参数 θ。当后验不可解析时的处理方法：①平均场变分推断——假设 `q(Z)=Πq_i(Z_i)` 完全分解，每个 `q_i` 独立优化。②随机梯度变分推断（SGVI/Reparameterization）——用重参数化技巧对 ELBO 做随机梯度下降，可扩展到大数据。③VAE——用神经网络参数化 q（编码器）和 p（解码器），变分 EM 训练。④MCMC——不近似后验而是采样，渐近精确但计算昂贵。EM→变分 EM→VAE 体现了从精确到近似的渐进推广。",
    tags: ["EM", "变分推断", "变分EM", "VAE"],
  },
  {
    id: "prl-mixture-em-4",
    chapter: "prl-mixture-em",
    level: 3,
    question: "EM 算法在 PRML 中如何连接其他章节？举例说明其应用。",
    answer:
      "EM 是 PRML 的通用框架，连接多个章节：①与 ch1 概率论的联系——EM 基于贝叶斯定理计算隐变量后验（责任度 `γ=p(z|x)=p(x|z)p(z)/p(x)`），是贝叶斯推断在隐变量模型上的应用。②与 ch2 贝叶斯回归的联系——当回归模型有隐变量时（如贝叶斯混合回归），EM 可用于参数学习。证据框架（ch2 的超参学习）可视为 EM 的特例——E 步计算参数后验，M 步更新超参。③与 ch7 图模型的联系——GMM 的图模型是 `π→z→x`，EM 是图模型上的推断算法：E 步在图上做隐变量推断 `p(z|x)`，M 步更新图参数。和积算法可用于 E 步的高效推断。变分推断是 EM 的推广。④应用举例：a) GMM 聚类——客户分群、图像分割；b) HMM（隐马尔可夫模型）——语音识别用 Baum-Welch（EM 的时序版本），隐状态是隐变量；c) LDA 主题模型——文档主题分析，主题分配是隐变量；d) 因子分析/PCA——隐因子是隐变量，EM 学习降维；e) VAE 深度生成模型——编码器的潜变量是隐变量，变分 EM 训练，生成图像/文本。总之，EM 是隐变量模型的通用训练框架，从经典聚类（GMM）到现代深度生成模型（VAE）都用 EM/变分 EM 的思想。",
    tags: ["EM", "知识联系", "GMM", "HMM", "VAE"],
  },
];
