import { ReviewQuestion } from "../types";

export const prlNeuralNetworksQuestions: ReviewQuestion[] = [
  {
    id: "prl-neural-networks-1",
    chapter: "prl-neural-networks",
    level: 1,
    question: "为什么神经网络需要非线性激活函数？万能逼近定理保证了什么、不保证什么？",
    answer:
      "需要非线性激活函数的原因：没有非线性激活，多层线性变换的复合仍是线性变换 `W₂(W₁x)=(W₂W₁)x=W'x`，网络退化为单层线性模型，无论加多少层都无法逼近非线性函数。激活函数引入非线性，使每层变换 `a=h(Wz+b)` 是非线性的，多层复合能表达高度复杂的非线性映射。万能逼近定理保证：只要有一个足够大的单隐层网络（使用非多项式激活函数如 sigmoid/tanh），就能以任意精度 ε 逼近任意连续函数。即表达能力上限有保证。定理不保证：①可训练性——定理只说「存在」这样的权重，不保证梯度下降能找到（可能需要指数级数据或陷入局部最优）。②深度效率——定理不说明深度比宽度更高效，但实践中深度网络用更少参数逼近复杂函数（层次化表示的指数效率）。③泛化——定理不涉及泛化能力，需要正则化控制。万能逼近定理是必要条件（保证能表达）但非充分（还需好的优化和正则化）。",
    tags: ["激活函数", "万能逼近定理", "非线性", "表达能力"],
  },
  {
    id: "prl-neural-networks-2",
    chapter: "prl-neural-networks",
    level: 2,
    question: "反向传播算法如何工作？它的计算复杂度为什么与参数量同阶？",
    answer:
      "反向传播分三步：①前向传播——从输入层到输出层逐层计算 `a_j^(l)=Σw_ji z_i^(l-1)+b_j` 和 `z_j^(l)=h(a_j^(l))`，保存所有中间激活值。计算输出 y 和损失 E。②反向传播误差——定义误差信号 `δ_j^(l)=∂E/∂a_j^(l)`。输出层 `δ_k^(L)=y_k-t_k`。隐藏层反向递归 `δ_j^(l)=h'(a_j^(l))Σ_k w_kj^(l+1) δ_k^(l+1)`。③权重梯度——`∂E/∂w_ji^(l)=δ_j^(l) z_i^(l-1)`，即误差信号×前一层激活值。计算复杂度与参数量同阶 `O(W)` 的原因：反向传播复用前向传播的中间结果（激活值 `z_i`），每个权重梯度只需一次乘法 `δ_j×z_i`。误差反向传播的递归每层计算量与前向传播同阶。总计算量≈2×前向传播（一次前向+一次反向），与参数量 W 成正比。这是动态规划的思想——避免对每个参数独立求导的重复计算。现代框架的自动微分（autograd）自动构建计算图并实现反向传播。",
    tags: ["反向传播", "链式法则", "梯度计算", "动态规划"],
  },
  {
    id: "prl-neural-networks-3",
    chapter: "prl-neural-networks",
    level: 2,
    question: "PRML 如何从贝叶斯视角统一理解神经网络的正则化方法？",
    answer:
      "PRML 将所有正则化方法统一在贝叶斯框架下：①权重衰减——损失中加入 `(λ/2)‖w‖²` 等价于高斯先验 `N(0,α⁻¹I)` 的 MAP 估计（`λ=α/β`）。先验表达「权重应该小」的信念，抑制大权重使函数平滑。L1 正则化等价于拉普拉斯先验，产生稀疏权重。②早停——在验证误差上升时停止训练。贝叶斯解释：训练轮数对应参数偏离先验（零点）的程度。训练初期参数在先验区域（偏差大方差小），训练后期在似然区域（偏差小方差大），早停在偏差-方差平衡点停止。等价于限制先验有效范围，与 L2 正则化等价。③输入抖动（数据增强）——输入加高斯噪声。贝叶斯解释：对输入分布做边际化，等价于 Tikhonov 正则化。④证据框架——超参 α 和 β 用证据框架自动学习，在非凸神经网络中用拉普拉斯近似处理非高斯后验。统一视角：正则化不是 ad-hoc 的工程技巧，而是贝叶斯先验的注入——权重衰减=高斯先验、早停=限制先验范围、输入抖动=输入边际化。",
    tags: ["权重衰减", "早停", "贝叶斯正则化", "先验注入"],
  },
  {
    id: "prl-neural-networks-4",
    chapter: "prl-neural-networks",
    level: 3,
    question: "神经网络与线性模型（ch2-ch3）的根本区别是什么？这个区别带来了哪些优势和挑战？",
    answer:
      "根本区别：基函数是否可学习。线性模型的基函数 `φ(x)` 是固定的（手动设计），只学最后一层权重 w。神经网络的基函数由隐藏层 `h(Wx+b)` 定义，W 也是可学习参数——网络自动从数据中学习特征映射。优势：①自动特征学习——无需领域专家手动设计特征（消除特征工程瓶颈）；②层次化表示——浅层学简单模式（边缘）、深层组合复杂语义（部件），匹配真实数据层次性；③通用性——同一架构适配图像/文本/音频等多种数据。挑战：①非凸优化——线性模型损失是凸的（保证全局最优），神经网络损失非凸（梯度下降可能陷入局部最优/鞍点），需要好的初始化和优化技巧；②过拟合风险——参数量大，极易过拟合，需要正则化；③梯度消失/爆炸——深层网络的反向传播中梯度逐层衰减或放大，需要残差连接（ResNet）、归一化（BatchNorm）等技巧；④可解释性差——学习的特征映射是黑箱；⑤超参敏感——层数、宽度、学习率等需仔细调优。PRML 的视角：神经网络是线性模型的自然推广——固定基函数→可学习基函数，凸优化→非凸优化，贝叶斯正则化同样适用。",
    tags: ["神经网络", "线性模型", "特征学习", "非凸优化"],
  },
];
