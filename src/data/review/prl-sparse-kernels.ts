import { ReviewQuestion } from "./types";

export const prlSparseKernelsQuestions: ReviewQuestion[] = [
  {
    id: "prl-sparse-kernels-1",
    chapter: "prl-sparse-kernels",
    level: 1,
    question: `SVM 的最大间隔原则是什么？为什么大间隔能带来更好的泛化？`,
    answer:
      `最大间隔原则：SVM 不仅要求正确分类，还要求决策边界两侧留最大间隔。决策边界 \`wᵀφ(x)+b=0\` 到最近样本的距离为 \`1/‖w‖\`，间隔宽度 \`2/‖w‖\`。最大化间隔 ≡ 最小化 \`½‖w‖²\`。约束 \`t_n(wᵀφ(x_n)+b)≥1\` 保证所有样本在间隔边界之外且正确分类。大间隔带来更好泛化的原因：①几何鲁棒性——大间隔意味着决策边界离样本远，样本的微小扰动（噪声）不会跨越边界导致误分。②统计学习理论——泛化误差上界 = 经验风险 + 置信区间。置信区间与 VC 维相关，VC 维与间隔成反比（大间隔→低 VC 维→低置信区间→低泛化误差上界）。SVM 通过最大间隔直接最小化泛化误差上界（结构风险最小化 SRM），而非仅最小化训练误差。③贝叶斯解释——\`min ½‖w‖²\` 等价于高斯先验的 MAP，间隔最大化是一种正则化，抑制过拟合。对比：逻辑回归只最大化似然（最小化训练误差），无显式间隔约束；SVM 的最大间隔提供了额外的泛化保证，在中小样本场景尤为有效。`,
    tags: ["最大间隔", "SVM", "泛化", "结构风险最小化"],
  },
  {
    id: "prl-sparse-kernels-2",
    chapter: "prl-sparse-kernels",
    level: 2,
    question: `什么是支持向量？SVM 的稀疏性从何而来？它与高斯过程有何区别？`,
    answer:
      `支持向量是 SVM 中拉格朗日乘子 \`a_n>0\` 的训练样本，恰好落在间隔边界上（硬间隔）或间隔内/错分（软间隔 \`a_n=C\`）。KKT 条件 \`a_n[t_n(wᵀφ(x_n)+b)-1]=0\` 保证：间隔外的样本满足 \`t_n(wᵀφ(x_n)+b)>1\`，故 \`a_n=0\`，对决策边界无影响。稀疏性从何而来：优化问题的 KKT 条件使大量样本的 \`a_n=0\`，只有少数「最难分」的样本（离边界最近）\`a_n>0\` 成为支持向量。预测 \`y(x)=Σ_{n∈SV}a_n t_n k(x_n,x)+b\` 只计算支持向量的核函数，非支持向量可丢弃。与高斯过程的区别：①稀疏性——SVM 预测只依赖支持向量（少数），GP 预测依赖所有训练样本（无稀疏性），SVM 推理更高效。②概率输出——SVM 不输出概率（需 Platt 缩放后处理），GP 输出完整预测分布（均值+方差）。③优化目标——SVM 最大化间隔（结构风险最小化），GP 最大化边际似然（贝叶斯证据）。④不确定度——SVM 无不确定度量化，GP 预测方差量化置信度。两者都是核方法但哲学不同：SVM 是频率派稀疏方法，GP 是贝叶斯概率方法。`,
    tags: ["支持向量", "稀疏性", "KKT条件", "SVM vs GP"],
  },
  {
    id: "prl-sparse-kernels-3",
    chapter: "prl-sparse-kernels",
    level: 2,
    question: `软间隔 SVM 如何处理线性不可分数据？参数 C 的作用是什么？`,
    answer:
      `软间隔 SVM 引入松弛变量 \`ξ_n≥0\` 允许少量样本违反间隔约束：约束变为 \`t_n(wᵀφ(x_n)+b)≥1-ξ_n\`。\`ξ_n=0\` 表示样本在间隔外（严格满足）；\`0<ξ_n<1\` 表示样本在间隔内但分类正确；\`ξ_n>1\` 表示样本被错分。优化目标 \`min ½‖w‖²+CΣξ_n\`，\`CΣξ_n\` 惩罚总违反量。参数 C 的作用：控制间隔宽度与违反量的权衡。①C 大→惩罚重→严格（接近硬间隔）→间隔窄、违反少→可能过拟合（为避免违反而弯曲边界）。②C 小→惩罚轻→宽松→间隔宽、违反多→可能欠拟合（容忍大量越界）。对偶形式中 \`a_n\` 的约束变为 \`0≤a_n≤C\`：\`a_n=C\` 的样本越界（\`ξ_n>0\`），\`0<a_n<C\` 的样本在间隔边界上（\`ξ_n=0\`），\`a_n=0\` 的样本在间隔外。C 的选择：通常用交叉验证。C 与核参数（如高斯核 σ）共同决定模型复杂度——大 C + 小 σ → 过拟合，小 C + 大 σ → 欠拟合。软间隔使 SVM 能处理：①线性不可分数据；②噪声数据（少量标签错误不会主导模型）。`,
    tags: ["软间隔", "松弛变量", "参数C", "线性不可分"],
  },
  {
    id: "prl-sparse-kernels-4",
    chapter: "prl-sparse-kernels",
    level: 3,
    question: `SVM 与逻辑回归、高斯过程分别有什么异同？在什么场景下选择 SVM？`,
    answer:
      `SVM vs 逻辑回归：相同——都是线性分类器（可用核/基函数扩展到非线性）。不同——①优化目标：SVM 最大化间隔（\`min ½‖w‖²\`），逻辑回归最大化似然（min 交叉熵）。②损失函数：SVM 用 hinge loss \`max(0,1-ty)\`，逻辑回归用交叉熵 \`-ln σ(ty)\`。③概率输出：SVM 不输出概率，逻辑回归直接输出概率。④稀疏性：SVM 有（支持向量），逻辑回归无。⑤鲁棒性：hinge loss 只关注间隔内的样本，SVM 对远端噪声更鲁棒。SVM vs 高斯过程：相同——都是核方法。不同——①概率：SVM 非概率，GP 完全概率。②稀疏：SVM 稀疏，GP 非稀疏。③不确定度：SVM 无，GP 有（方差）。选择 SVM 的场景：①中小样本、高维特征——SVM 的最大间隔在小样本时泛化好（SRM 理论保证）。②需要稀疏高效推理——支持向量少，预测快。③不需要概率输出——只需分类标签。④非线性分类——核 SVM 简洁有效。不选 SVM 的场景：①需要概率/不确定度→选 GP 或逻辑回归。②大数据（N>10万）→SVM 二次规划太慢，选深度学习。③多分类——SVM 原生二分类，需 OvO/OvR 扩展。`,
    tags: ["SVM", "逻辑回归", "高斯过程", "方法对比"],
  },
];
