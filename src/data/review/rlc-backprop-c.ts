import { ReviewQuestion } from "../types";

export const rlcBackpropCQuestions: ReviewQuestion[] = [
  {
    id: "rlc-backprop-c-1",
    chapter: "rlc-backprop-c",
    level: 1,
    question: "反向传播算法的目的是什么？它与梯度下降的关系是什么？",
    answer:
      "反向传播（Backpropagation）的目的是高效计算神经网络损失函数对每层每个参数（权重W和偏置b）的梯度 `∂L/∂W` 和 `∂L/∂b`。它是「计算梯度的算法」，不是优化算法本身。与梯度下降的关系：梯度下降是优化算法，用反向传播算出的梯度更新参数 `W ← W - η · ∂L/∂W`。完整流程是：①前向传播——计算各层激活值a和加权和z，得到预测输出ŷ。②计算损失 L(ŷ, y)。③反向传播——从输出层开始，利用链式法则逐层向后计算梯度 `∂L/∂W_l` 和 `∂L/∂b_l`。④梯度下降——用梯度更新所有参数。反向传播之所以高效，是因为它复用了前向传播时计算的中间值（z和a），避免了重复计算。在C语言中，反向传播本质就是一组嵌套for循环做矩阵运算，没有框架的自动微分，必须手动推导每层的梯度公式，这正是理解「深度学习训练在算什么」的最佳途径。",
    tags: ["反向传播", "梯度下降", "链式法则"],
  },
  {
    id: "rlc-backprop-c-2",
    chapter: "rlc-backprop-c",
    level: 2,
    question: "用链式法则推导单隐层网络的权重梯度，写出每一步的计算公式。",
    answer:
      "设单隐层网络：输入x → 加权z1=W1·x+b1 → 激活a1=f(z1) → 加权z2=W2·a1+b2 → 激活ŷ=f(z2) → 损失L=½(ŷ-y)²。对输出层权重W2求梯度（链式法则）：`∂L/∂W2 = ∂L/∂ŷ · ∂ŷ/∂z2 · ∂z2/∂W2`。逐项：①`∂L/∂ŷ = (ŷ - y)`（MSE损失的导数）。②`∂ŷ/∂z2 = f'(z2)`（激活函数导数）。③`∂z2/∂W2 = a1`（因为z2 = W2·a1+b2）。合并：`∂L/∂W2 = (ŷ - y) · f'(z2) · a1`。定义输出层误差 `δ2 = (ŷ - y) · f'(z2)`，则 `∂L/∂W2 = δ2 · a1^T`，`∂L/∂b2 = δ2`。对隐层权重W1求梯度：`∂L/∂W1 = ∂L/∂a1 · ∂a1/∂z1 · ∂z1/∂W1`。其中 `∂L/∂a1 = W2^T · δ2`（损失对a1的梯度通过W2回传），所以 `δ1 = (W2^T · δ2) ⊙ f'(z1)`，则 `∂L/∂W1 = δ1 · x^T`，`∂L/∂b1 = δ1`。推广到多层：`δ_l = (W_{l+1}^T · δ_{l+1}) ⊙ f'(z_l)`，`∂L/∂W_l = δ_l · a_{l-1}^T`。这就是「反向传播」——误差δ从输出层向输入层逐层回传。",
    tags: ["链式法则", "梯度推导", "误差回传"],
  },
  {
    id: "rlc-backprop-c-3",
    chapter: "rlc-backprop-c",
    level: 2,
    question: "用C语言实现反向传播时，需要保存前向传播的哪些中间值？为什么？",
    answer:
      "C语言实现反向传播需要保存前向传播的中间值：①每层加权和 `z[l][j]`——计算激活函数导数 `f'(z[l][j])` 时需要，因为常用激活函数的导数可以用z表示（如sigmoid导数 `σ(z)(1-σ(z))`，ReLU导数 `z>0?1:0`）。若只存激活值a，对sigmoid需重算 `a·(1-a)`（可行但不直接），对ReLU需要符号信息（a>0即z>0，可行）。但统一存z更通用。②每层激活值 `a[l][j]`——计算权重梯度 `∂L/∂W_l = δ_l · a_{l-1}^T` 时需要上一层的激活作为输入。③权重矩阵 `W[l][j][i]`——反向传播误差时需要 `δ_l = (W_{l+1}^T · δ_{l+1}) ⊙ f'(z_l)`，用到下一层权重。原因：链式法则将梯度分解为「误差δ」和「前向输入a」的乘积，两者都来自前向传播。C语言实现中，struct需包含 `double** z; double** a;` 字段，前向传播时填充，反向传播时读取。这体现了反向传播的高效性——复用前向计算结果，避免重复求值。相比自动微分框架，C语言需要手动管理这些缓存，但能看清「训练 = 前向存中间值 + 反向用中间值算梯度」的本质。",
    tags: ["中间值保存", "C语言实现", "缓存复用"],
  },
  {
    id: "rlc-backprop-c-4",
    chapter: "rlc-backprop-c",
    level: 3,
    question: "反向传播中常见哪些数值问题？在C语言实现中如何解决？",
    answer:
      "反向传播的常见数值问题及C语言解决方案：①梯度消失——深层网络中sigmoid/tanh导数小于1，连乘后梯度趋近0，前层参数不更新。解决：隐藏层用ReLU（正区间梯度恒为1）、合理初始化权重（Xavier/He初始化）、 BatchNorm归一化激活值。C语言：`if (x > 0) return 1.0; else return 0.0;`。②梯度爆炸——梯度连乘后指数增长，参数震荡发散。解决：梯度裁剪 `if (|g| > max_norm) g = g * max_norm / |g|`。C语言需手写clip函数。③数值溢出——exp()在大输入时溢出为inf。解决：softmax用减最大值技巧 `exp(x - max(x))`；sigmoid对大负数 `exp(-x)`溢出，可分段处理。C语言：`double sigmoid_safe(double x) { if (x >= 0) return 1.0/(1.0+exp(-x)); else return exp(x)/(1.0+exp(x)); }`。④学习率过大——损失震荡或发散。解决：学习率衰减 `lr = lr0 / (1 + decay * epoch)` 或用Adam自适应优化器。⑤死神经元——ReLU负区间梯度为0，永久不更新。解决：LeakyReLU `x>0?x:0.01*x`。C语言实现需要手动加这些保护，正因如此能深刻理解「为什么深度学习训练难」。",
    tags: ["梯度消失", "梯度爆炸", "数值稳定", "C语言解决"],
  },
];
