import { ReviewQuestion } from "./types";

export const rlcFinalReviewQuestions: ReviewQuestion[] = [
  {
    id: "rlc-final-review-1",
    chapter: "rlc-final-review",
    level: 1,
    question: `用一句话概括DQN和Policy Gradient如何分别将深度学习融入强化学习，并指出它们共享的DL组件。`,
    answer:
      `DQN = Q-Learning + 神经网络——用NN作为函数近似器逼近Q函数 \`Q(s,a;θ) ≈ Q*(s,a)\`，将Q表的查表替换为网络前向传播，输出离散动作的Q值，用反向传播最小化 \`(Q(s,a;θ) - [r + γ max Q_target(s',a';θ⁻)])²\`。Policy Gradient = 策略 + 神经网络——用NN直接参数化策略 \`π_θ(a|s)\`，输出动作的概率分布（离散用softmax，连续用高斯均值方差），用反向传播沿 \`∇θ log π_θ(a|s) · G_t\` 方向梯度上升。两者共享的DL组件：①前馈神经网络结构（ch3的NeuralNetwork C实现）。②反向传播算法（ch4的链式法则+梯度计算）——DQN用它算 \`∂L/∂θ\` 做梯度下降，PG用它算 \`∇θ log π\` 做梯度上升。③激活函数（ReLU/sigmoid）。④梯度下降优化器。核心洞察：深度学习为强化学习提供了「函数近似」和「梯度优化」两大能力，使RL从表格法（只能处理小规模离散问题）跃升到深度RL（能处理连续高维状态空间）。`,
    tags: ["DQN", "Policy Gradient", "融合", "共享组件"],
  },
  {
    id: "rlc-final-review-2",
    chapter: "rlc-final-review",
    level: 2,
    question: `贝尔曼方程和梯度下降分别是RL和DL的核心，请说明它们在深度RL中如何协同工作。`,
    answer:
      `贝尔曼方程（RL核心）和梯度下降（DL核心）在深度RL中的协同：①贝尔曼方程定义「目标」——它告诉我们最优值函数满足 \`Q*(s,a) = E[r + γ max_a' Q*(s',a')]\`，即当前Q值应等于「即时奖励 + γ × 下一状态最优Q值」。这为DQN提供了回归目标：网络输出Q(s,a;θ)应逼近TD目标 \`y = r + γ max Q_target(s',a')\`。贝尔曼方程将「无限horizon的最优」转化为「一步的递归关系」，使学习可行。②梯度下降定义「优化」——有了目标y和预测Q(s,a;θ)，定义损失 \`L = (Q(s,a;θ) - y)²\`，梯度下降用反向传播算 \`∂L/∂θ\` 并更新 \`θ ← θ - α · ∂L/∂θ\`，逐步减小预测与目标的差距。梯度下降使NN能从数据中学习。③协同循环——前向传播算Q值 → 用贝尔曼方程构造TD目标 → 算损失 → 反向传播算梯度 → 梯度下降更新θ → 新的Q值更接近最优 → 重复。贝尔曼方程是「指南针」（指明最优方向），梯度下降是「引擎」（驱动参数向最优移动）。④在Policy Gradient中协同方式略不同——贝尔曼方程隐式体现在回报G_t的计算中（G_t = r + γG_{t+1}是贝尔曼方程的轨迹形式），梯度下降（上升）直接优化策略目标J(θ)。理解这种协同是掌握深度RL的关键。`,
    tags: ["贝尔曼方程", "梯度下降", "协同", "深度RL"],
  },
  {
    id: "rlc-final-review-3",
    chapter: "rlc-final-review",
    level: 2,
    question: `从C语言实现角度，Q-Learning的Q表更新和DQN的网络更新，本质计算有什么相同和不同？`,
    answer:
      `Q表更新 vs DQN网络更新的相同与不同：相同点：①都是「用TD目标修正当前估计」——Q-Learning：\`Q[s][a] += α(r + γ max Q[s'][a'] - Q[s][a])\`；DQN：\`θ ← θ - α·∂(Q(s,a;θ) - y)²/∂θ\`，其中y是TD目标。②都基于贝尔曼方程的TD误差驱动学习。③都用学习率α控制步长。④都涉及max操作（Q-Learning对Q[s'][a']取max，DQN对Q_target(s',a')取max）。不同点：①更新对象——Q表直接更新一个表格项 \`Q[s][a]\`（O(1)数组赋值）；DQN更新网络参数θ（成百上千个浮点数，需前向+反向传播，O(网络规模)计算）。②泛化能力——Q表只更新被访问的 \`(s,a)\`，其他表项不变（无泛化）；DQN更新θ后，所有状态的Q值都变了（通过NN前向传播），相似状态会得到相似Q值（泛化）。③目标稳定性——Q表的目标 \`r + γ max Q[s'][a']\` 用同一张表算（自举），但表格项离散相对稳定；DQN需目标网络（θ⁻固定）防止目标随θ变化而震荡。④数据使用——Q表每步用当前转移即时更新（on-line）；DQN存入回放缓冲区随机采样（off-line + 打破相关性）。⑤计算复杂度——Q表更新O(1)；DQN更新需一次前向（主网络）+一次前向（目标网络）+一次反向传播，复杂度高但能处理连续状态。本质：Q表是「查表+赋值」，DQN是「前向+回归+反向」，后者用计算量换来了泛化能力。`,
    tags: ["Q表更新", "DQN更新", "对比", "C语言实现"],
  },
  {
    id: "rlc-final-review-4",
    chapter: "rlc-final-review",
    level: 3,
    question: `学完全书后，如果要为一个新RL任务选择并实现算法，请描述你的完整决策与实现流程。`,
    answer:
      `新RL任务的完整决策与实现流程：①分析任务特性——状态空间（离散/连续、维度）、动作空间（离散/连续、数量）、奖励性质（稀疏/密集）、是否需要随机策略、样本获取成本（仿真器免费 vs 真实环境昂贵）。②选择算法——状态离散小→Q-Learning（ch2）；状态连续+动作离散→DQN（ch5）；动作连续→Policy Gradient（ch6）或Actor-Critic（进阶）；样本昂贵→考虑Model-based。③设计环境接口——按ch7的统一接口 \`env_reset/env_step\` 实现，定义状态编码、动作映射、奖励函数、终止条件。先实现随机策略验证环境正确性（打印轨迹、检查奖励范围）。④C语言实现骨架——定义数据结构（Q表或NeuralNetwork struct、ReplayBuffer），实现前向传播/ε-贪心/训练循环。⑤超参数选择——学习率α（Q-Learning 0.1-0.5，DQN 1e-4到1e-3）、折扣因子γ（0.9-0.99）、探索ε（0.1-0.3起始，衰减到0.01）、网络结构（2-3隐藏层，每层32-128神经元）、经验回放大小（1万-10万）、batch size（32-64）、目标网络更新频率（每500-1000步）。⑥训练与监控——按ch8的评估方法监控平均回报、收敛速度、稳定性；打印学习曲线；定期评估（关闭探索）看真实策略。⑦调试——若不收敛：检查奖励符号、状态编码、梯度是否爆炸/消失（打印max Q值/梯度范数）、降低学习率、增加探索、检查环境逻辑。⑧对比基线——与随机策略、Q-Learning（若适用）对比，确认改进。⑨迁移到框架——验证C语言版正确后，可用PyTorch重写以获GPU加速和自动微分，此时每个组件都有C语言版作为参照。⑩文档化——记录超参数、学习曲线、关键决策，形成可复现的实验报告。这一流程体现了全书的核心价值：从底层理解到工程实践的完整能力链。`,
    tags: ["决策流程", "实现流程", "综合应用", "工程实践"],
  },
];
