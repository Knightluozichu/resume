import { ReviewQuestion } from "./types";

export const rlcRlFoundationsQuestions: ReviewQuestion[] = [
  {
    id: "rlc-rl-foundations-1",
    chapter: "rlc-rl-foundations",
    level: 1,
    question: `强化学习中「智能体」「环境」「状态」「动作」「奖励」五个核心概念分别是什么？它们如何交互？`,
    answer:
      `强化学习的五个核心概念：①智能体（Agent）——做决策的学习者，内部包含策略π、值函数V/Q、可能的模型。②环境（Environment）——智能体交互的外部世界，定义了状态转移和奖励规则。③状态（State s）——环境在某一时刻的描述，智能体据此决策。④动作（Action a）——智能体在某个状态下可执行的操作。⑤奖励（Reward r）——环境对智能体动作的即时反馈标量。交互循环：智能体观察状态s_t → 根据策略π选择动作a_t → 环境执行动作并返回新状态s_{t+1}和奖励r_{t+1} → 智能体根据反馈更新策略，循环往复。目标是最大化长期累积折扣回报 \`G_t = Σ γ^k r_{t+k+1}\`。`,
    tags: ["核心概念", "交互循环", "MDP"],
  },
  {
    id: "rlc-rl-foundations-2",
    chapter: "rlc-rl-foundations",
    level: 2,
    question: `什么是马尔可夫决策过程（MDP）？写出其五元组定义和马尔可夫性质。`,
    answer:
      `马尔可夫决策过程（MDP）是强化学习的数学形式化框架，用五元组 \`(S, A, P, R, γ)\` 定义：①S——状态空间，环境的所有可能状态集合。②A——动作空间，智能体可执行的所有动作集合。③P——状态转移概率 \`P(s'|s,a)\`，在状态s执行动作a后转移到s'的概率。④R——奖励函数 \`R(s,a)\` 或 \`R(s,a,s')\`，执行动作后获得的期望奖励。⑤γ——折扣因子 \`γ ∈ [0,1]\`，权衡即时奖励与长期奖励。马尔可夫性质：「未来只取决于现在，不依赖过去」——即 \`P(s_{t+1}|s_t, a_t, s_{t-1}, ...) = P(s_{t+1}|s_t, a_t)\`，状态s_t包含了所有历史相关信息。这一性质使RL问题可解，否则需要考虑全部历史，计算不可行。`,
    tags: ["MDP", "五元组", "马尔可夫性质"],
  },
  {
    id: "rlc-rl-foundations-3",
    chapter: "rlc-rl-foundations",
    level: 2,
    question: `写出状态值函数和动作值函数的贝尔曼方程，并解释其含义。`,
    answer:
      `状态值函数 \`V^π(s)\` 是在状态s下遵循策略π的期望累积折扣回报：\`V^π(s) = E_π[ G_t | s_t = s ]\`。动作值函数 \`Q^π(s,a)\` 是在状态s执行动作a后遵循策略π的期望回报：\`Q^π(s,a) = E_π[ G_t | s_t=s, a_t=a ]\`。贝尔曼方程（递归分解）：①状态值：\`V^π(s) = Σ_a π(a|s) Σ_s' P(s'|s,a) [ R(s,a,s') + γ V^π(s') ]\`，即当前状态的值 = 对所有动作按策略加权 × 对所有下一状态按转移概率加权 × （即时奖励 + γ × 下一状态值）。②动作值：\`Q^π(s,a) = Σ_s' P(s'|s,a) [ R(s,a,s') + γ Σ_a' π(a'|s') Q^π(s',a') ]\`。贝尔曼方程将「无穷 horizon 的期望」递归分解为「即时奖励 + 下一状态值」，是值迭代、Q-Learning等算法的理论基础。最优值函数 \`V*(s) = max_π V^π(s)\` 满足 \`Q*(s,a) = Σ_s' P(s'|s,a)[R + γ max_a' Q*(s',a')]\`。`,
    tags: ["贝尔曼方程", "值函数", "递归分解"],
  },
  {
    id: "rlc-rl-foundations-4",
    chapter: "rlc-rl-foundations",
    level: 3,
    question: `RL有哪三大方法族？它们各自的核心思想、代表算法和适用场景是什么？`,
    answer:
      `RL三大方法族：①基于价值（Value-based）——核心思想是学习值函数V/Q，再从值函数间接推导策略（如贪心策略）。代表算法：Q-Learning、SARSA、DQN。适用场景：离散动作空间，目标是找到确定性最优策略。优点是稳定、样本效率高，缺点是难以处理连续动作空间。②基于策略（Policy-based）——核心思想是直接参数化策略 \`π_θ(a|s)\`，通过梯度上升直接优化期望回报 \`J(θ)\`。代表算法：REINFORCE、Actor-Critic、PPO。适用场景：连续动作空间、随机策略。优点是通用性强，缺点是方差大、收敛慢。③基于模型（Model-based）——核心思想是先学习环境模型（状态转移P和奖励R），再用模型规划或生成数据。代表算法：Dyna-Q、MCTS、世界模型。适用场景：样本昂贵（如机器人）、可离线学习。优点是样本效率极高，缺点是模型误差会累积。本书重点覆盖前两类：Q-Learning（ch2）和DQN（ch5）属于基于价值，Policy Gradient（ch6）属于基于策略。`,
    tags: ["方法族", "价值/策略/模型", "适用场景"],
  },
];
