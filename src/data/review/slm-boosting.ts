import { ReviewQuestion } from "../types";

export const slmBoostingQuestions: ReviewQuestion[] = [
  {
    id: "slm-boosting-1",
    chapter: "slm-boosting",
    level: 1,
    question: "什么是提升方法？AdaBoost 算法的基本思想是什么？",
    answer:
      "提升方法（Boosting）是一种集成学习方法，其基本思想是从弱学习算法出发，反复学习得到一系列弱分类器，然后组合这些弱分类器构成一个强分类器。AdaBoost（Adaptive Boosting）是提升方法的代表算法。基本思想：①初始化时给每个训练样本赋予相同的权值（1/N）；②每一轮训练中，用当前权值分布训练弱分类器，计算该弱分类器的加权错误率 em；③根据 em 计算该弱分类器在最终强分类器中的权重 αm = 1/2 ln((1-em)/em)——错误率越低的弱分类器权重越大；④更新样本权值——增大上一轮被误分类样本的权值，减小被正确分类样本的权值（Dm+1(i) = Dm(i)exp(-αm yi Gm(xi))/Zm），使得下一轮弱分类器更关注难分样本；⑤重复 M 轮，最终强分类器 f(x) = sign(Σ αm Gm(x))。AdaBoost 的自适应体现在：每轮根据前一轮的结果自适应地调整样本权值和弱分类器权重。",
    tags: ["提升方法", "AdaBoost", "弱分类器", "权值更新"],
  },
  {
    id: "slm-boosting-2",
    chapter: "slm-boosting",
    level: 2,
    question: "AdaBoost 算法是前向分步加法模型的特例，请解释这一关系。",
    answer:
      "前向分步加法模型的基本思想是：加法模型 f(x) = Σ αm Gm(x)（基函数的线性组合），通过前向分步算法逐步学习：每一步只学习一个基函数 Gm 和其系数 αm，使当前模型的损失最小化，即 (αm, Gm) = argmin Σ L(yi, f_{m-1}(xi) + α G(xi))。AdaBoost 是前向分步加法模型采用指数损失函数 L(y,f(x)) = exp(-y f(x)) 时的特例。证明：在第 m 轮，前向分步算法要最小化 Σ exp(-yi(f_{m-1}(xi) + α G(xi))) = Σ w_i^(m) exp(-α yi G(xi))，其中 w_i^(m) = exp(-yi f_{m-1}(xi)) 可以看作样本权值。对 α 和 G 求最优解正好对应 AdaBoost 中弱分类器的训练和权重的计算。具体来说：①使指数损失最小化的弱分类器 Gm 就是在当前权值分布下错误率最低的分类器；②最优 αm = 1/2 ln((1-em)/em)；③权值更新公式 Dm+1(i) = Dm(i)exp(-αm yi Gm(xi))/Zm 正好是指数损失下的权值更新。因此 AdaBoost 的每一步都在最小化指数损失，是前向分步加法模型在指数损失下的特殊形式。",
    tags: ["前向分步加法模型", "指数损失", "AdaBoost推导"],
  },
  {
    id: "slm-boosting-3",
    chapter: "slm-boosting",
    level: 2,
    question: "什么是提升树？梯度提升树与提升树的区别是什么？",
    answer:
      "提升树（Boosting Tree）是以决策树（通常是 CART 回归树）为基函数的前向分步加法模型。对于回归问题，提升树的第 m 轮学习的是前一轮的残差（residual）：f_m(x) = f_{m-1}(x) + Tm(x)，其中 Tm 是拟合残差 yi - f_{m-1}(xi) 的回归树。算法：①初始化 f0(x) = 0；②对 m=1,...,M：计算残差 rmi = yi - f_{m-1}(xi)，拟合残差学习回归树 Tm，更新 fm = f_{m-1} + Tm。提升树在平方损失下，残差就是负梯度，所以每轮直接拟合残差。梯度提升树（GBDT）的区别：当损失函数不是平方损失时（如绝对损失、Huber 损失等），残差不再容易直接计算。梯度提升树用损失函数的负梯度在当前模型的值作为残差的近似来拟合回归树：rmi = -[∂L(yi, f(xi))/∂f(xi)]|f=f_{m-1}。这样就将提升树推广到任意可导损失函数。梯度提升树的优势：①适用于任意可导损失函数（回归和分类均可）；②通过损失函数的选择可以增强鲁棒性（如 Huber 损失对离群点不敏感）；③是目前最强的机器学习算法之一（XGBoost、LightGBM 的理论基础）。",
    tags: ["提升树", "梯度提升树", "残差", "负梯度", "GBDT"],
  },
  {
    id: "slm-boosting-4",
    chapter: "slm-boosting",
    level: 3,
    question: "AdaBoost 的训练误差有什么性质？为什么 AdaBoost 不容易过拟合？",
    answer:
      "AdaBoost 训练误差的性质：AdaBoost 的训练误差上界随迭代次数指数下降。具体来说，训练错误率有上界 1/N Σ I(G(xi)≠yi) ≤ 1/N Σ exp(-yi f(xi)) = Π Zm，其中 Zm 是每轮的归一化因子。可以证明 Zm = 2√(em(1-em))，当 em < 1/2 时 Zm < 1，因此训练误差随迭代次数 M 指数下降。为什么不容易过拟合：①间隔理论解释——AdaBoost 虽然不断增加弱分类器使训练误差趋向 0，但它同时在最大化分类间隔（margin），间隔的增大提高了泛化能力。即使训练误差已为 0，继续迭代仍能增大间隔，改善泛化性能。②偏差-方差解释——AdaBoost 从弱分类器（高偏差低方差）出发，通过组合降低偏差同时控制方差，不容易过拟合。③正则化效果——弱分类器数量 M 起到正则化作用，通过交叉验证选择 M 可以防止过拟合。但也有例外：当数据噪声比例高时（噪声标签多），AdaBoost 会过度关注噪声样本（权值增大），导致过拟合。此时需要使用更温和的损失函数（如 LogitBoost 的对数损失）或早停策略。",
    tags: ["训练误差", "指数下降", "间隔理论", "过拟合"],
  },
];
