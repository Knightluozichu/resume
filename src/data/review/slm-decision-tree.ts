import { ReviewQuestion } from "../types";

export const slmDecisionTreeQuestions: ReviewQuestion[] = [
  {
    id: "slm-decision-tree-1",
    chapter: "slm-decision-tree",
    level: 1,
    question: "什么是决策树？决策树学习的三个步骤是什么？",
    answer:
      "决策树是一种基本的分类与回归方法，它呈树形结构，表示基于特征对实例进行分类的过程。决策树可以认为是 if-then 规则的集合，也可以认为是定义在特征空间与类空间上的条件概率分布。决策树学习包括三个步骤：①特征选择——从当前节点的训练数据中选取一个最优特征进行划分。不同的特征选择准则对应不同的决策树算法：信息增益（ID3）、信息增益比（C4.5）、基尼指数（CART）。②决策树生成——从根节点开始，递归地选择最优特征进行划分，直到所有样本属于同一类或没有可用特征为止。③决策树剪枝——由于生成的决策树可能过拟合（树太深），需要从已生成的树上剪掉一些子树或叶节点，使其简化。剪枝通过极小化损失函数 Ca(T) = C(T) + α|T| 实现，其中 C(T) 是训练误差，|T| 是叶节点数，α 是正则化参数。",
    tags: ["决策树", "特征选择", "生成", "剪枝"],
  },
  {
    id: "slm-decision-tree-2",
    chapter: "slm-decision-tree",
    level: 2,
    question: "信息增益、信息增益比和基尼指数的区别是什么？分别对应哪种算法？",
    answer:
      "三者都是特征选择的准则，区别在于度量方式和适用场景。①信息增益（ID3）：g(D,A) = H(D) - H(D|A)，表示得知特征 A 的信息后使得类 D 的不确定性减少的程度。H(D) = -Σ pk log pk 是信息熵，H(D|A) 是条件熵。信息增益倾向于选择取值数目较多的特征（因为划分后子集更纯）。②信息增益比（C4.5）：gR(D,A) = g(D,A) / HA(D)，其中 HA(D) = -Σ |Di|/|D| log(|Di|/|D|) 是特征 A 本身的熵。信息增益比对取值数目多的特征进行了惩罚（除以一个较大的 HA(D)），修正了信息增益的偏好。③基尼指数（CART）：Gini(D) = 1 - Σ pk²，表示从集合 D 中随机抽取两个样本其类别不一致的概率。基尼指数越小表示纯度越高。Gini(D,A) = Σ |Di|/|D| Gini(Di)，选择使基尼指数最小的特征。对比：信息增益和信息增益比用于多叉树，基尼指数用于二叉树（CART）；基尼指数与信息熵的计算不同但效果相似，计算更简单（不需要对数运算）。",
    tags: ["信息增益", "信息增益比", "基尼指数", "ID3", "C4.5", "CART"],
  },
  {
    id: "slm-decision-tree-3",
    chapter: "slm-decision-tree",
    level: 2,
    question: "决策树剪枝的原理是什么？预剪枝和后剪枝有什么区别？",
    answer:
      "决策树剪枝是为了防止过拟合。生成的决策树可能过于复杂，将训练数据中的噪声也学习了进去，导致泛化能力差。剪枝通过简化树结构来提高泛化能力。剪枝的损失函数：Ca(T) = C(T) + α|T|，其中 C(T) 是模型对训练数据的预测误差（如熵或基尼指数），|T| 是树的叶节点数（复杂度），α ≥ 0 是正则化参数控制两者之间的权衡。α 大时鼓励选择较简单的树，α 小时鼓励选择较复杂的树。预剪枝：在决策树生成过程中，对每个节点在划分前先进行估计，若当前节点的划分不能带来决策树泛化性能的提升，则停止划分并将当前节点标记为叶节点。优点是简单高效，缺点是可能欠拟合（有些后续划分可能带来提升但被提前终止）。后剪枝：先生成完整的决策树，然后自底向上地对非叶节点进行考察，若将该节点对应的子树替换为叶节点能带来泛化性能提升，则将该子树替换为叶节点。优点是不容易欠拟合，缺点是计算量大（需要生成完整树后再回溯）。",
    tags: ["剪枝", "预剪枝", "后剪枝", "正则化", "损失函数"],
  },
  {
    id: "slm-decision-tree-4",
    chapter: "slm-decision-tree",
    level: 3,
    question: "CART算法的特点是什么？它如何同时处理分类和回归？",
    answer:
      "CART（Classification and Regression Trees）算法的特点：①二叉树结构——CART 生成的决策树总是二叉树，每个非叶节点只有两个分支。这与 ID3/C4.5 的多叉树不同。二叉树使得树结构更简单，且可以递归地处理连续和离散特征。②既可分类也可回归——CART 分类树用基尼指数选择特征，CART 回归树用平方误差最小化选择特征。③特征选择——对回归问题，遍历所有特征和所有可能的切分点，选择使平方误差最小的切分；对分类问题，选择使基尼指数最小的切分。④剪枝——CART 采用代价复杂度剪枝（后剪枝），通过交叉验证选取最优的正则化参数 α。⑤回归树的预测——叶节点的输出值为该区域中所有训练样本输出的平均值。CART 处理回归的方式：将输入空间划分为若干个区域，每个区域输出一个常数值。划分的目标是最小化平方误差 Σ(yi - f(xi))²。对每个区域选择最优切分特征和切分点，递归地将区域一分为二，直到满足停止条件。CART 是许多集成方法（如随机森林、GBDT）的基学习器。",
    tags: ["CART", "二叉树", "分类与回归", "基尼指数", "平方误差"],
  },
];
