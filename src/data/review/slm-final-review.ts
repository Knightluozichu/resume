import { ReviewQuestion } from "./types";

export const slmFinalReviewQuestions: ReviewQuestion[] = [
  {
    id: "slm-final-review-1",
    chapter: "slm-final-review",
    level: 2,
    question: `统计学习方法各章节的模型分别属于生成式还是判别式？请分类说明。`,
    answer:
      `生成式模型（建模联合分布 P(X,Y)）：①朴素贝叶斯（ch3）——建模 P(Y)P(X|Y)，利用贝叶斯定理得到后验 P(Y|X) = P(Y)P(X|Y)/P(X)。朴素贝叶斯假设特征条件独立，是一种典型的生成式模型。②隐马尔可夫模型（ch8）——建模 P(O,I) = P(I)P(O|I)，其中 O 是观测序列，I 是隐状态序列。HMM 通过建模状态序列和观测的联合分布来处理序列数据。判别式模型（直接建模 P(Y|X) 或决策函数 f(x)）：①感知机（ch1）——直接学习决策函数 f(x)=sign(w·x+b)。②k近邻法（ch2）——直接基于距离做分类决策，不建模数据分布。③决策树（ch4）——直接学习条件概率分布或决策函数（if-then 规则）。④逻辑斯谛回归与最大熵（ch5）——直接建模 P(Y|X)。⑤支持向量机（ch6）——直接学习决策函数（间隔最大化的超平面）。⑥AdaBoost（ch7）——直接学习加法决策函数。生成式和判别式的本质区别：生成式需要建模数据分布 P(X)，判别式只关注 P(Y|X) 的决策边界。`,
    tags: ["生成式模型", "判别式模型", "方法分类", "知识整合"],
  },
  {
    id: "slm-final-review-2",
    chapter: "slm-final-review",
    level: 2,
    question: `统计学习方法中哪些方法使用了凸优化？凸优化对这些方法有什么意义？`,
    answer:
      `使用凸优化的方法：①逻辑斯谛回归——对数似然函数是参数 w 的凹函数，最大化对数似然是凸优化问题，保证全局最优解。②最大熵模型——目标函数是对数似然的凸函数，约束是线性的，整个优化问题是凸的。③支持向量机——原始问题 min 1/2||w||² + CΣξi 是凸二次规划（目标函数是凸的，约束是线性的），对偶问题也是凸二次规划，保证全局最优解。④（软间隔）SVM 的 hinge 损失是凸函数。不使用凸优化的方法：①感知机——损失函数不是凸的（非连续可导），但在线性可分数据上保证收敛。②决策树——特征选择是贪心搜索，非凸优化。③EM 算法——目标函数（对数似然）是非凸的（因含隐变量），EM 只保证收敛到局部最优。④AdaBoost——前向分步是贪心的，每步最优但整体非凸。凸优化的意义：①保证全局最优解——逻辑斯谛回归和 SVM 的解是唯一的、全局最优的，不会陷入局部最优。②理论基础坚实——凸优化有成熟的求解算法（梯度下降、牛顿法、SMO 等）和收敛性理论。③可复现性好——不同初始值得到相同的解（SVM 的最大间隔解唯一）。相比之下，神经网络等非凸优化问题可能收敛到不同的局部最优。`,
    tags: ["凸优化", "全局最优", "SVM", "逻辑斯谛回归", "方法对比"],
  },
  {
    id: "slm-final-review-3",
    chapter: "slm-final-review",
    level: 3,
    question: `从「模型→策略→算法」三要素的角度，对比感知机、逻辑斯谛回归和 SVM 的异同。`,
    answer:
      `三者都是线性分类模型，但三要素不同。模型：三者模型形式相同——线性分类器 f(x)=sign(w·x+b) 或 P(Y=1|x)=σ(w·x+b)。感知机和 SVM 直接输出类别，逻辑斯谛回归输出概率。策略（损失函数）：①感知机——误分类点到超平面的总距离 L=-Σ yi(w·xi+b)（仅对误分类点）。②逻辑斯谛回归——对数似然损失 L=-Σ[yi log π(xi)+(1-yi)log(1-π(xi))]（对所有样本）。③SVM——hinge 损失+正则化 L=1/2||w||²+CΣ max(0,1-yi(w·xi+b))（间隔最大化+软间隔）。算法：①感知机——随机梯度下降（逐个误分类点更新）。②逻辑斯谛回归——梯度下降/牛顿法（所有样本同时更新）。③SVM——SMO 算法（解对偶二次规划）。异同对比：①感知机和 SVM 都基于间隔概念，但感知机最小化误分类距离（解不唯一），SVM 最大化间隔（解唯一）。②逻辑斯谛回归和 SVM 都可输出概率/置信度，但逻辑斯谛用对数损失（处处可导），SVM 用 hinge 损失（稀疏，只关注支持向量）。③SVM 的解具有稀疏性（仅支持向量决定模型），感知机和逻辑斯谛回归的解通常不稀疏。④SVM 通过核技巧可处理非线性，感知机和逻辑斯谛回归需要手动特征工程。`,
    tags: ["三要素对比", "感知机", "逻辑斯谛回归", "SVM", "损失函数"],
  },
  {
    id: "slm-final-review-4",
    chapter: "slm-final-review",
    level: 4,
    question: `如何将统计学习方法全书的方法论用于解决一个新的机器学习问题？请给出完整的方法论框架。`,
    answer:
      `用统计学习方法的方法论解决新问题的完整框架：①明确问题——确定是分类还是回归？有标签还是无标签？数据特点（维度/规模/噪声）？这决定了选择哪些方法可能适用。②模型选择——根据「模型→策略→算法」三要素思考：模型选择哪种假设空间？（线性模型/决策树/概率模型/核方法？）策略选择什么损失函数？（0-1损失/平方损失/对数损失/hinge损失/指数损失？）算法选择什么优化方法？（梯度下降/SMO/EM/前向分步？）③生成式 vs 判别式——如果数据分布信息重要或数据量少，考虑生成式（朴素贝叶斯/HMM）；如果追求预测精度且数据充足，考虑判别式（逻辑斯谛/SVM/决策树/AdaBoost）。④模型复杂度控制——通过正则化参数（SVM 的 C，决策树的 α，逻辑斯谛的 L1/L2）、k近邻的 k 值、决策树剪枝等控制模型复杂度，防止过拟合。交叉验证选取超参数。⑤集成提升——如果单个模型不够强，考虑集成方法（AdaBoost/GBDT 组合弱分类器）。⑥评估与迭代——用独立测试集或交叉验证评估泛化性能，分析偏差-方差权衡（高偏差→增加模型复杂度或用 Boosting；高方差→降低复杂度或用正则化），迭代优化。这一框架将统计学习理论转化为可操作的工程实践。`,
    tags: ["方法论框架", "模型选择", "策略选择", "算法选择", "工程实践"],
  },
];
