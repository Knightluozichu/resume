import { ReviewQuestion } from "./types";

export const slmKnnQuestions: ReviewQuestion[] = [
  {
    id: "slm-knn-1",
    chapter: "slm-knn",
    level: 1,
    question: `什么是 k近邻法？它的三要素是什么？`,
    answer:
      `k近邻法（k-NN）是一种基本的分类与回归方法。其基本思想是：给定一个训练数据集，对新的输入实例，在训练集中找到与该实例最邻近的 k 个实例，这 k 个实例的多数属于某个类，就把该输入实例分为这个类。k近邻法的三要素：①k 值的选择——k 值的选择会对结果产生重大影响。k 值较小意味着模型较复杂（容易过拟合），k 值较大意味着模型较简单（容易欠拟合）。通常通过交叉验证选取最优 k 值。②距离度量——特征空间中两个实例点的距离反映了它们的相似度。常用 Lp 距离：Lp(xi,xj) = (Σ|xi^(l) - xj^(l)|^p)^(1/p)，p=1 为曼哈顿距离，p=2 为欧氏距离。③分类决策规则——通常采用多数表决（majority voting），即 k 个近邻中多数所属的类别作为预测结果。多数表决等价于经验风险最小化。`,
    tags: ["k近邻法", "三要素", "多数表决"],
  },
  {
    id: "slm-knn-2",
    chapter: "slm-knn",
    level: 2,
    question: `k近邻法中 k 值的大小对模型有什么影响？如何选择 k 值？`,
    answer:
      `k 值的影响：①k 值较小——相当于用较小的邻域中的实例进行预测，「学习」的近似误差小，但估计误差大。预测结果对近邻的实例点非常敏感，如果近邻恰好是噪声，预测就会出错。即 k 值减小意味着模型整体变复杂，容易发生过拟合。②k 值较大——相当于用较大的邻域中的实例进行预测，估计误差减小但近似误差增大。与输入实例较远的（不相似的）实例也会对预测起作用，使预测发生错误。k 值增大意味着模型整体变简单，容易发生欠拟合。k 值的选择：通常采用交叉验证法来选取最优的 k 值。在实际应用中，k 值一般取一个比较小的数值（如 3 或 5），然后通过交叉验证逐步调整。另外，k 值一般取奇数以避免平局（二分类时）。`,
    tags: ["k值选择", "过拟合", "欠拟合", "交叉验证"],
  },
  {
    id: "slm-knn-3",
    chapter: "slm-knn",
    level: 2,
    question: `什么是 kd 树？它是如何构造和搜索的？`,
    answer:
      `kd 树是一种对 k 维空间中的实例点进行存储以便对其进行快速检索的树形数据结构，主要用于加速 k近邻搜索。构造：kd 树是二叉树，表示对 k 维空间的一个划分。构造过程：①构造根节点，使根节点对应于包含所有实例点的 k 维超矩形区域；②选择一个坐标轴（通常交替选择 x1 轴和 x2 轴等）和以选定坐标轴的中位数为切分点，将超矩形区域切分为两个子区域；③重复上述过程直到子区域内没有实例点（叶节点）。搜索：①从根节点出发，递归地向下找到目标点的叶节点（包含目标点的子区域）；②以此叶节点中的实例点作为「当前最近点」，回溯搜索——检查兄弟节点对应的区域是否可能包含更近的点（通过比较目标点到分割超平面的距离与当前最近距离），如果可能则进入兄弟子树搜索；③回溯到根节点后搜索结束。平均搜索复杂度为 O(log N)，最坏为 O(N)。`,
    tags: ["kd树", "构造", "搜索", "最近邻搜索"],
  },
  {
    id: "slm-knn-4",
    chapter: "slm-knn",
    level: 3,
    question: `k近邻法有什么特点？它与书中其他方法相比有什么不同？`,
    answer:
      `k近邻法的特点：①没有显式的学习过程——k近邻是「惰性学习」（lazy learning），它不事先训练出模型参数，而是在预测时直接利用训练数据。这与感知机、SVM 等「急切学习」（eager learning）方法形成对比。②三要素完全决定模型行为——k 值、距离度量和决策规则一经确定，模型就完全确定了，无需优化参数。③非参数方法——k近邻不假设数据的分布形式，适用于各种分布的数据。④可解释性——预测结果可以追溯到具体的 k 个近邻实例。与书中其他方法的区别：①感知机/SVM/逻辑斯谛回归都学习一个参数化的决策函数，而 k近邻是非参数的；②决策树学习 if-then 规则，k近邻基于距离投票；③k近邻的预测时间复杂度为 O(N)（暴力搜索）或 O(log N)（kd 树），而其他方法预测时只需计算决策函数 O(d)。④k近邻是判别式方法但不需要训练，其他方法需要训练过程。`,
    tags: ["惰性学习", "非参数方法", "方法对比"],
  },
];
