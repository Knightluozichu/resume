import { ReviewQuestion } from "../types";

export const slmNaiveBayesQuestions: ReviewQuestion[] = [
  {
    id: "slm-naive-bayes-1",
    chapter: "slm-naive-bayes",
    level: 1,
    question: "什么是朴素贝叶斯法？它的核心假设是什么？",
    answer:
      "朴素贝叶斯法是基于贝叶斯定理与特征条件独立假设的分类方法。其基本思想是：对于给定的待分类项 x，计算在每个类别 yk 下 x 出现的后验概率 P(yk|x)，取后验概率最大的类别作为预测结果。核心假设是条件独立性假设：P(X1,X2,...,Xn | Y) = Π P(Xi | Y)，即假设特征在给定类别下条件独立。这一假设是「朴素」的来源——它简化了联合条件概率的计算，将 n 维联合分布分解为 n 个一维条件概率的乘积。分类规则：y = argmax_yk P(yk) Π P(xi | yk)。朴素贝叶斯法属于生成式模型，它建模联合概率 P(X,Y) = P(Y)P(X|Y)，然后通过贝叶斯定理得到后验 P(Y|X)。尽管独立性假设在实际中很少严格成立，但朴素贝叶斯在文本分类等任务中表现优秀。",
    tags: ["朴素贝叶斯", "条件独立性假设", "生成式模型", "贝叶斯定理"],
  },
  {
    id: "slm-naive-bayes-2",
    chapter: "slm-naive-bayes",
    level: 2,
    question: "朴素贝叶斯法的参数如何估计？什么是拉普拉斯平滑？",
    answer:
      "朴素贝叶斯法需要估计的参数包括先验概率 P(Y=yk) 和条件概率 P(Xi=xij | Y=yk)。①极大似然估计：先验概率 P(Y=yk) = Σ I(yi=yk) / N，即第 k 类样本在训练集中所占的比例。条件概率 P(Xi=xij | Y=yk) = Σ I(xi^(l)=xij, yi=yk) / Σ I(yi=yk)，即在第 k 类样本中，第 i 个特征取值为 xij 的样本所占的比例。②贝叶斯估计（拉普拉斯平滑）：当某个特征值在训练集中从未与某类别同时出现时，条件概率为 0，导致整个乘积为 0。为避免此问题，引入平滑参数 λ ≥ 0：Pλ(Xi=xij | Y=yk) = (Σ I(xi^(l)=xij, yi=yk) + λ) / (Σ I(yi=yk) + Sj λ)，其中 Sj 是第 j 个特征的可能取值数。λ=1 时即为拉普拉斯平滑（加一平滑），λ=0 时退化为极大似然估计。平滑保证了所有概率为正值。",
    tags: ["参数估计", "极大似然估计", "拉普拉斯平滑", "零概率问题"],
  },
  {
    id: "slm-naive-bayes-3",
    chapter: "slm-naive-bayes",
    level: 2,
    question: "朴素贝叶斯法的算法流程是什么？",
    answer:
      "朴素贝叶斯算法流程分为训练和预测两个阶段。训练阶段：①计算先验概率 P(Y=yk) = Σ I(yi=yk) / N，k=1,2,...,K。②计算条件概率 P(Xi=xij | Y=yk) = (Σ I(xi^(l)=xij, yi=yk) + λ) / (Σ I(yi=yk) + Sj λ)，对每个特征 i 和每个类别 k。预测阶段（给定新样本 x = (x1,x2,...,xn)）：③对每个类别 yk，计算后验概率 P(Y=yk) Π P(Xi=xi | Y=yk)。④取后验概率最大的类别作为预测结果：y = argmax_yk P(Y=yk) Π P(Xi=xi | Y=yk)。注意在计算中通常取对数避免下溢（多个小概率相乘导致数值下溢）：log P(yk|x) ∝ log P(yk) + Σ log P(Xi=xi | Y=yk)。朴素贝叶斯的训练时间复杂度为 O(NK)，预测时间复杂度为 O(Kn)，非常高效。",
    tags: ["算法流程", "训练", "预测", "对数概率"],
  },
  {
    id: "slm-naive-bayes-4",
    chapter: "slm-naive-bayes",
    level: 3,
    question: "朴素贝叶斯法与贝叶斯估计有什么关系？为什么独立性假设虽不严格成立但效果仍然好？",
    answer:
      "朴素贝叶斯法与贝叶斯估计的关系：朴素贝叶斯法的参数估计可以采用极大似然估计（频率派观点）或贝叶斯估计（贝叶斯派观点）。贝叶斯估计引入先验分布（通过平滑参数 λ 体现），相当于在极大似然估计的基础上加了先验，使得估计更稳健。当 λ=0 时退化为极大似然估计，λ > 0 时为贝叶斯估计，λ=1 为拉普拉斯平滑。独立性假设虽不严格成立但效果仍然好的原因：①分类只需比较后验概率的相对大小（argmax），不需要精确的概率值——即使条件概率估计有偏差，只要不影响 argmax 的结果，分类就是正确的。②独立性假设虽然引入了偏差，但也大大减少了需要估计的参数数量——从估计 O(S^n) 个联合概率降至 O(nS) 个条件概率，降低了方差。偏差-方差权衡中，偏差的增加被方差的减少所补偿。③在特征维度高、训练数据有限时，独立性假设的简化优势更加明显。④实际应用中（如文本分类），词频特征之间虽有关联但独立性近似通常足够好。",
    tags: ["贝叶斯估计", "偏差方差权衡", "独立性假设"],
  },
];
