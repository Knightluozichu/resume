import { ReviewQuestion } from "../types";

export const slmSvmQuestions: ReviewQuestion[] = [
  {
    id: "slm-svm-1",
    chapter: "slm-svm",
    level: 1,
    question: "支持向量机有哪三种模型？它们的区别是什么？",
    answer:
      "支持向量机按数据是否线性可分以及是否使用核技巧，分为三种模型：①线性可分支持向量机（硬间隔）——当训练数据线性可分时，通过间隔最大化学习一个线性的分离超平面。优化问题：min 1/2||w||²，s.t. yi(w·xi+b) ≥ 1。严格要求所有样本正确分类且在间隔边界之外。②线性支持向量机（软间隔）——当训练数据近似线性可分（有少量噪声或离群点）时，引入松弛变量 ξi ≥ 0 允许部分样本违反间隔约束。优化问题：min 1/2||w||² + CΣξi，s.t. yi(w·xi+b) ≥ 1-ξi，ξi ≥ 0。C 是惩罚参数控制对违反间隔的容忍度。③非线性支持向量机（核技巧）——当训练数据线性不可分时，通过核函数 K(xi,xj) = φ(xi)·φ(xj) 将数据隐式映射到高维特征空间，在高维空间中寻找线性超平面。三种模型的递进关系：硬间隔→软间隔（允许违反）→核技巧（映射到高维），逐步扩展适用范围。",
    tags: ["SVM", "硬间隔", "软间隔", "核技巧"],
  },
  {
    id: "slm-svm-2",
    chapter: "slm-svm",
    level: 2,
    question: "SVM 的对偶问题是什么？对偶形式有什么优势？",
    answer:
      "SVM 原始问题：min 1/2||w||² + CΣξi，s.t. yi(w·xi+b) ≥ 1-ξi，ξi ≥ 0。通过拉格朗日乘子法转化为对偶问题：max Σαi - 1/2 ΣΣ αiαj yi yj K(xi,xj)，s.t. Σαi yi = 0，0 ≤ αi ≤ C。解出 α 后，w = Σ αi yi xi（仅 αi > 0 的支持向量有贡献），b 由支持向量满足 yi(w·xi+b)=1 求出。分类判别函数：f(x) = sign(Σ αi yi K(xi,x) + b)。对偶形式的优势：①自然引入内积 K(xi,xj)，为核技巧铺垫——将内积替换为核函数即可处理非线性问题，无需显式计算映射函数 φ(·)。②求解效率与特征维度无关，只与样本数有关——在高维数据（如文本，特征维度远大于样本数）中效率更高。③KKT 条件保证解的稀疏性——非支持向量的 αi=0，只有少数支持向量参与最终决策，使预测高效。④对偶问题是凸二次规划，有全局最优解。",
    tags: ["对偶问题", "拉格朗日乘子法", "支持向量", "KKT条件"],
  },
  {
    id: "slm-svm-3",
    chapter: "slm-svm",
    level: 2,
    question: "什么是核技巧？常用的核函数有哪些？核函数有效的条件是什么？",
    answer:
      "核技巧（Kernel Trick）：在 SVM 对偶形式中，样本仅以内积形式出现。用核函数 K(xi,xj) = φ(xi)·φ(xj) 替代内积，即可隐式地将数据映射到高维特征空间，而无需显式计算映射函数 φ(·)。这使得在高维甚至无穷维空间中计算成为可能。常用核函数：①线性核 K(x,z) = x·z——不映射，等价线性 SVM。②多项式核 K(x,z) = (x·z + 1)^d——映射到多项式特征空间，d 是多项式次数。③高斯核（RBF）K(x,z) = exp(-||x-z||²/2σ²)——映射到无穷维空间，σ 控制局部性，是最常用的非线性核。④拉普拉斯核 K(x,z) = exp(-||x-z||/σ)。核函数有效的条件——Mercer 定理：函数 K 是有效核当且仅当对任意有限样本集，其核矩阵（Gram 矩阵）半正定。即 K 必须是对称且半正定的。核技巧的选择是 SVM 的关键，需要根据数据特性选择合适的核函数和参数（如 σ, d, C），通常通过交叉验证确定。",
    tags: ["核技巧", "高斯核", "Mercer定理", "Gram矩阵"],
  },
  {
    id: "slm-svm-4",
    chapter: "slm-svm",
    level: 3,
    question: "SMO 算法的基本思想是什么？它如何高效求解 SVM 对偶问题？",
    answer:
      "SMO（Sequential Minimal Optimization）算法是快速求解 SVM 对偶问题的启发式算法。基本思想：如果所有变量的解都满足 KKT 优化条件，则这个解就是最优解。SMO 每次选择两个变量（α1, α2）进行优化，固定其他变量，解析地求解这两个变量的最优值（因为约束 Σαi yi = 0 要求至少同时优化两个变量）。算法步骤：①选择两个变量 α1 和 α2——第一个变量选择违反 KKT 条件最严重的样本，第二个变量选择使目标函数下降最快的样本（通常选择使 |E1-E2| 最大的样本，Ei 是预测误差）。②固定其他变量，对 α1 和 α2 解析求解二次规划子问题——利用等式约束 Σαi yi = 0 将问题化简为单变量二次函数求极值，有闭式解。③更新 α1, α2 和阈值 b。④检查所有变量是否满足 KKT 条件，若不满足则回到①。SMO 的优势：①每次子问题只有两个变量，有解析解，避免了通用 QP 求解器的高开销。②不需要存储整个 Gram 矩阵，内存效率高。③收敛速度在实践中很快，是 LIBSVM 等库的标准算法。KKT 条件包括：αi=0 时样本被正确分类且在间隔外；0<αi<C 时样本在间隔边界上（支持向量）；αi=C 时样本在间隔内或被误分类。",
    tags: ["SMO算法", "KKT条件", "启发式", "二次规划"],
  },
];
