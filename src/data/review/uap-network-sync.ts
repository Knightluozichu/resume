import type { ReviewQuestion } from "./types";

export const uapNetworkSyncQuestions: ReviewQuestion[] = [
  {
    id: "uap-network-sync-1",
    chapter: "uap-network-sync",
    level: 1,
    question: `帧同步和状态同步的核心区别是什么？`,
    answer: `帧同步只传输入：服务器收集所有玩家输入广播，各端用相同输入跑相同逻辑，保证确定性。状态同步传结果：服务器跑权威逻辑，把对象状态（位置/血量）广播给客户端，客户端只做表现。帧同步流量小、能做海量单位、可回放，但要求严格确定性、反作弊难。状态同步反作弊强、不怕浮点误差，但流量大、服务器贵。`,
    tags: ["帧同步", "状态同步", "对比"],
  },
  {
    id: "uap-network-sync-2",
    chapter: "uap-network-sync",
    level: 2,
    question: `帧同步为什么要求逻辑和表现分离？逻辑层必须满足什么约束？`,
    answer: `表现层（动画/特效/音效）可以不确定（不同机型帧率不同），但逻辑层必须严格确定，否则各端结果不一致。约束：用定点数替代 float（不同 CPU 浮点结果可能不同）、禁用 System.Random 用固定种子、固定步长逻辑帧（不用 Time.deltaTime）、遍历集合前排序（字典顺序不确定）。表现层读取逻辑层状态来渲染，但不影响逻辑层。分离后逻辑可重放回放，表现可按帧率插值。`,
    tags: ["帧同步", "确定性", "逻辑表现分离"],
  },
  {
    id: "uap-network-sync-3",
    chapter: "uap-network-sync",
    level: 3,
    question: `状态同步角色移动一顿一顿的，怎么用插值和预测解决？`,
    answer: `插值：客户端维护快照队列，渲染时在 100ms 前的两个快照间插值（Vector3.Lerp），不直接 SetPosition。画面滞后 100ms 但绝对平滑，掩盖网络抖动。预测：本地输入立刻响应（本地先跑移动），不等服务器确认。服务器结果回来后对账，不一致则回滚纠正未确认输入。插值掩盖抖动，预测掩盖延迟，两者配合让操作流畅。注意：预测只用于本地玩家，远程玩家只用插值。`,
    tags: ["插值", "预测", "状态同步", "平滑"],
  },
  {
    id: "uap-network-sync-4",
    chapter: "uap-network-sync",
    level: 4,
    question: `射击游戏的延迟补偿是怎么工作的？对被击方公平吗？`,
    answer: `原理：服务器收到客户端「开枪」指令时带时间戳，服务器把所有玩家位置历史回溯到该时间戳，用那时的位置做命中判定。比如玩家 A 延迟 100ms，他看到敌人在 X 位置开枪，服务器收到时敌人已到 Y，但服务器回溯到 100ms 前的 X 判定命中。对被击方「不公平」：被击方画面上已躲到掩体后，但仍被判命中——因为它在 A 的画面上还露着。这是延迟下不可避免的取舍，补偿开枪方体验。竞技游戏通常设延迟上限（如 >200ms 不补偿）。`,
    tags: ["延迟补偿", "射击游戏", "公平性", "综合"],
  },
];
