import type { ReviewQuestion } from "./types";

export const uapThreadsQuestions: ReviewQuestion[] = [
  {
    id: "uap-th-1",
    chapter: "uap-threads",
    level: 2,
    question: "线程和进程的核心区别是什么？线程间共享什么、各自独有什么？",
    answer:
      "核心区别：进程是资源分配的基本单位，线程是CPU调度的基本单位。同一进程内的多个线程共享同一个地址空间，创建和切换开销远小于进程。\n线程间共享：①代码段/数据段/堆；②文件描述符表；③信号处理函数表；④当前工作目录和根目录；⑤用户ID和组ID；⑥全局变量和静态变量。\n线程各自独有：①栈（每个线程有独立栈空间）；②寄存器集合（包括PC程序计数器和SP栈指针）；③线程ID（pthread_t）；④errno（每个线程独立errno）；⑤信号屏蔽字；⑥线程局部存储（TLS，__thread关键字）。共享带来的优势：线程间通信只需读写共享变量，无需IPC系统调用。共享带来的风险：需要同步机制保护共享数据的一致性。",
    tags: ["线程", "进程", "共享资源"],
  },
  {
    id: "uap-th-2",
    chapter: "uap-threads",
    level: 3,
    question: "互斥锁和读写锁分别适用于什么场景？读写锁有什么潜在的饥饿问题？",
    answer:
      "互斥锁（Mutex）：同一时刻只允许一个线程持有锁进入临界区，无论读还是写。适用于读写都频繁、临界区短的场景。简单但读读互斥——多个线程只是读取数据也要串行等待。\n读写锁（RWLock）：允许多个线程同时读（读共享），但写时独占（写时禁止任何读和写）。适用于「读多写少」场景——如缓存、配置表，大量读操作可并行执行大幅提升吞吐量。\n读写锁的饥饿问题：①写饥饿——如果读操作持续不断，写者可能一直等待（因为总有读者在持有锁），导致写者长时间阻塞。②解决方法：设置写者优先策略——当有写者等待时，新来的读者不能立即获取锁，必须等写者完成后才能读。但写者优先又可能导致读者饥饿。实际使用中需根据场景选择策略，或使用更高级的同步机制。",
    tags: ["互斥锁", "读写锁", "同步原语"],
  },
  {
    id: "uap-th-3",
    chapter: "uap-threads",
    level: 3,
    question: "条件变量为什么要和互斥锁配合使用？为什么wait返回后要用while循环检查条件？",
    answer:
      "条件变量和互斥锁配合的原因：条件变量用于线程间通知「某条件成立」，但检查条件本身需要访问共享数据，共享数据必须用互斥锁保护。pthread_cond_wait的原子操作：①释放互斥锁（让其他线程能修改共享数据）②阻塞等待信号 ③被唤醒后重新获取互斥锁。如果不配合互斥锁，检查条件和等待之间有竞态窗口——可能条件恰好在检查后成立并发出信号，但线程还没进入wait，信号就丢失了。\n用while而非if检查条件的原因：①虚假唤醒（spurious wakeup）——POSIX允许pthread_cond_wait在没有信号的情况下返回，用while可以重新检查条件；②多线程唤醒——broadcast唤醒多个等待线程，第一个获取锁的线程处理后条件可能不再成立，后续线程必须重新检查。模式：pthread_mutex_lock(&mtx) → while (!condition) pthread_cond_wait(&cond, &mtx) → 处理 → pthread_mutex_unlock(&mtx)。",
    tags: ["条件变量", "互斥锁", "虚假唤醒"],
  },
  {
    id: "uap-th-4",
    chapter: "uap-threads",
    level: 4,
    question: "线程安全和可重入的区别是什么？为什么errno要设计成线程局部变量？",
    answer:
      "线程安全（thread-safe）：函数在多线程环境中被并发调用时能正确执行，不会因共享数据竞争产生错误结果。实现方式：①用互斥锁保护共享数据；②使用线程局部存储（如errno）。\n可重入（reentrant）：函数在执行过程中被中断（如信号），在中断处理中再次调用同一函数，两次调用都能正确执行。可重入函数不能有任何共享状态——不使用全局/静态变量，不调用不可重入函数。\n区别：可重入是线程安全的充分条件（可重入一定线程安全），但线程安全不一定是可重入的（用锁实现的线程安全在信号handler中可能死锁）。\nerrno设计为线程局部变量的原因：errno是C标准库的错误码标志，传统实现是全局int。如果多线程同时调用库函数出错，全局errno会被最后一个写入的线程覆盖，其他线程读到错误值。用__thread（TLS）让每个线程有独立的errno副本，各线程设置和读取自己的errno互不干扰。这是「用TLS把非线程安全函数变为线程安全」的经典案例。",
    tags: ["线程安全", "可重入", "errno", "TLS"],
  },
];
