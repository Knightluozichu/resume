/** 复习题库 · 网络通信（unity-advanced-network）。《Unity3D高级编程：主程手记》第6章。 */

import type { ReviewQuestion } from "./types";

export const unityAdvancedNetworkQuestions: ReviewQuestion[] = [
  // ── L1 认记：术语 / 定义 ──
  {
    id: "ua-net-1",
    chapter: "unity-advanced-network",
    level: 1,
    question: "TCP 和 UDP 的核心区别是什么？各自适合什么游戏场景？",
    answer:
      "**TCP** 是面向连接、可靠有序、基于字节流的协议——三次握手建立连接，自带重传、拥塞控制、流量控制，保证数据有序到达但延迟不可控。**UDP** 是无连接、不可靠、基于数据报的协议——不保证到达、不保证顺序、没有握手开销，延迟低但丢包需自行处理。**选型**：① TCP 适合回合制、卡牌、MMO 中非实时模块（登录、聊天、邮件、公会、交易）——数据必须完整送达、对延迟容忍度高；② UDP 适合 MOBA、FPS、赛车、格斗等强实时对战——状态可以被新数据覆盖，丢包不重传，靠应用层做可靠/有序/冗余发送。实际项目中常见做法是用 UDP 承载游戏帧同步/状态同步数据，TCP 承载非实时业务数据，或直接用可靠 UDP 库（KCP/ENet）统一管理。",
    tags: ["TCP", "UDP", "协议选型", "实时性", "可靠传输"],
  },
  {
    id: "ua-net-2",
    chapter: "unity-advanced-network",
    level: 1,
    question: "什么是粘包和拆包？为什么会发生？",
    answer:
      "TCP 是面向字节流的协议，没有消息边界——发送方连续发送两个数据包，接收方可能一次收到两个包粘在一起（**粘包**），也可能一个包被分成多次接收（**拆包/半包**）。根本原因：① Nagle 算法会将小包合并发送；② 接收方缓冲区未及时读取，数据在缓冲区堆积；③ MTU 限制导致大包在 IP 层分片。**UDP 不会粘包**——它是面向数据报的，每个 recvfrom 恰好对应一个完整的数据包。解决粘包拆包需要在应用层定义消息边界，常见方案：① 固定长度包头 + 包体（包头含包体长度，如 `[2字节长度][N字节body]`）；② 特殊分隔符（如 `\\r\\n`，但要转义）；③ 固定消息长度（不灵活，不推荐）。游戏中普遍使用「长度字段+协议ID+Protobuf包体」的 TLV（Type-Length-Value）结构。",
    tags: ["粘包", "拆包", "半包", "字节流", "消息边界", "TLV"],
  },
  {
    id: "ua-net-3",
    chapter: "unity-advanced-network",
    level: 1,
    question: "心跳机制的作用是什么？心跳间隔如何设置？",
    answer:
      "**心跳（Heartbeat/KeepAlive）** 是客户端定期向服务端发送的空/极短数据包，用于：① 检测连接是否存活（NAT 映射表超时、中间路由断开、对端崩溃都不会立即通知本端）；② 维持 NAT 映射不被淘汰（家用路由器 NAT 超时通常 30s~5min）；③ 服务端据此判断客户端在线状态，超时未收到心跳则踢下线释放资源。**间隔设置**：一般设为 5~15 秒，超时容忍 3~5 次（即 15~75 秒无心跳判定断线）。间隔太短浪费带宽，太长则断线检测慢。实践中会根据场景动态调整：大厅/主城可以 10s 一次，战斗中可以更密集（3~5s）或在关键帧带心跳标记。TCP 层有 `SO_KEEPALIVE` 选项，但默认间隔 2 小时太长，游戏不能依赖它，必须应用层自己实现。",
    tags: ["心跳", "Heartbeat", "KeepAlive", "断线检测", "NAT穿透"],
  },

  // ── L2 理解：为什么 / 区别 ──
  {
    id: "ua-net-4",
    chapter: "unity-advanced-network",
    level: 2,
    question: "为什么游戏序列化普遍使用 Protobuf 而不是 JSON/XML？Protobuf 相比它们有哪些优势和限制？",
    answer:
      "**Protobuf（Protocol Buffers）** 是 Google 开发的二进制序列化协议。相比 JSON/XML：① **体积小**——字段用 tag（field_number+wire_type）标识，varint 编码整数，没有字段名冗余，相同数据通常只有 JSON 的 1/3~1/10；② **序列化/反序列化快**——纯二进制操作，不需要字符串解析，速度通常是 JSON 的 5~20 倍；③ **强类型 + Schema 约束**——`.proto` 文件定义消息结构，前后端共享，编译期类型检查，减少接口联调错误；④ **向前/向后兼容**——新增字段使用新 field number，旧客户端忽略未知字段，平滑升级。**限制**：① 二进制格式不可读，调试需要抓包工具解析；② 必须维护 .proto 文件和生成代码，流程稍重；③ 对动态结构（如任意 JSON 对象）支持弱。游戏场景中带宽和 CPU 都极其敏感（尤其移动端弱网），Protobuf 是工业标准选型。",
    tags: ["Protobuf", "序列化", "二进制协议", "JSON", "Schema", "带宽优化"],
  },
  {
    id: "ua-net-5",
    chapter: "unity-advanced-network",
    level: 2,
    question: "状态同步和帧同步的核心区别是什么？各自适用于什么类型的游戏？",
    answer:
      "**状态同步（State Sync）**：服务端是权威，运行完整游戏逻辑，定期把各实体状态（位置、血量、状态等）广播给客户端，客户端只做表现插值。优点是防作弊能力强（服务器权威）、网络抖动影响小（可以插值平滑）、支持大地图多实体（MMO）；缺点是带宽消耗大（要同步所有实体状态）、客户端响应手感略差（有服务器往返延迟）、服务端计算压力大。**帧同步（Lockstep/Frame Sync）**：只同步玩家操作指令（如「向右移动」「释放技能」），所有客户端在相同帧号执行相同指令，各自跑逻辑得出一致结果。优点是带宽极小（只传输入指令）、反应快（本地立即执行）、服务端压力小（只做转发）；缺点是要求确定性（浮点数、随机数、遍历顺序必须严格一致，通常用定点数）、需要锁帧/等待（不同步就会漂移）、防作弊弱（逻辑在客户端）、重连成本高（需要追帧）。**选型**：MOBA（王者荣耀、LOL手游）、格斗、RTS 常用帧同步；MMO、FPS（PUBG、CSGO）、吃鸡类大地图常用状态同步；混合方案也常见（如战斗帧同步+非战斗状态同步）。",
    tags: ["状态同步", "帧同步", "Lockstep", "确定性", "权威服务器", "MOBA", "MMO"],
  },
  {
    id: "ua-net-6",
    chapter: "unity-advanced-network",
    level: 2,
    question: "什么是可靠 UDP（KCP）？它相比原生 TCP 在游戏场景中有什么优势？",
    answer:
      "**KCP** 是一个开源的 ARQ（自动重传请求）可靠传输协议，运行在 UDP 之上，实现了类似 TCP 的可靠传输但策略更激进：① **RTO 不翻倍**——TCP 超时重传 RTO 指数退避（1x→2x→4x），KCP 默认为 1.5x，弱网下重传更快；② **选择性重传**——只重传丢失的包，TCP 早期是 GBN（退后N帧重传），即使后续包到达也丢弃；③ **快速重传**——收到 2 个（可配置）后续 ACK 就直接重传，不等超时；④ **非退让流控**——KCP 默认关闭拥塞控制，只做发送窗口控制，适合游戏这种「宁可抢带宽也不能卡」的场景；⑤ **可配置窗口大小**——小窗口减少延迟，大窗口提高吞吐。**代价**：相比 TCP 额外浪费 10%~20% 带宽换取 30%~40% 的延迟降低。KCP 非常适合帧同步游戏（指令必须可靠到达但延迟敏感）。TCP 适合能容忍高延迟的非实时业务。注意：KCP 不是银弹，在强服务端权威的状态同步 MMO 中 TCP 也完全够用。",
    tags: ["KCP", "可靠UDP", "ARQ", "RTO", "重传", "弱网", "延迟优化"],
  },

  // ── L3 应用：工程实践 ──
  {
    id: "ua-net-7",
    chapter: "unity-advanced-network",
    level: 3,
    question: "断线重连需要解决哪些工程问题？如何设计重连流程？",
    answer:
      "断线重连是移动网游的标配，需解决：① **连接恢复**——Socket 重连 + 重新登录鉴权（Token 复用而非重新输入密码）；② **状态恢复**——重连后服务端下发玩家当前场景的快照（位置、血量、buff、场景实体列表等），客户端根据快照重建世界状态，而非从登录场景重来；③ **消息补发/去重**——断线期间服务端可能堆积了消息，重连后按序列号补发；客户端用消息 ID 去重防止重复处理；④ **帧同步追帧**——帧同步游戏断线期间的帧需要缓存，重连后服务端从断线帧号开始补发帧数据，客户端加速播放（fast-forward）追赶到当前帧；⑤ **UI 体验**——显示「重连中...」倒计时，超过阈值（如 10s）提示网络异常并让玩家选择重试/返回大厅；⑥ **战斗容错**——PVP 中短断线（<10s）可以暂停或让角色自动跑位/AI托管，超时再判负。**流程**：检测到断线 → 进入重连状态 → 重连+Token鉴权 → 请求场景快照/帧数据 → 客户端重建状态 → 正常同步 → 隐藏重连UI。需要在协议层设计 SessionID + 消息序列号支持这些能力。",
    tags: ["断线重连", "重连流程", "状态恢复", "追帧", "快照", "Session", "弱网体验"],
  },
  {
    id: "ua-net-8",
    chapter: "unity-advanced-network",
    level: 3,
    question: "网络消息队列（消息处理管线）应该怎么设计？为什么不能在 Socket 接收线程直接操作 Unity 对象？",
    answer:
      "Unity 的 API（GameObject、Transform、UI 等）只能在主线程调用，而 Socket 的 `BeginReceive`/`Receive` 回调运行在 .NET 线程池线程上，直接操作 Unity 对象会崩溃或产生未定义行为。标准消息管线设计：① **网络线程**：只负责收字节流 → 解包（处理粘包拆包）→ 反序列化为消息对象 → 推入线程安全的**消息队列**（`ConcurrentQueue` 或加锁的 Queue）；② **主线程（Update 轮询）**：每帧从队列中取出所有消息 → 分发到对应处理器（Handler/Dispatcher）→ 在主线程执行业务逻辑（更新 UI、驱动角色状态、触发事件）；③ **发送同理**：主线程把消息对象推入发送队列，网络线程从队列取出 → 序列化 → Send。注意点：① 队列要限长（防堆积爆内存），满了可以丢弃过期消息或压缩；② 消息对象用对象池复用避免 GC；③ 高优先级消息（如战斗操作）插队处理；④ 消息派发可用消息ID→委托字典（路由表）避免大 switch。有些项目还会引入「逻辑线程」做战斗计算，网络线程→逻辑队列→逻辑线程→渲染队列→主线程，做到帧级线程解耦。",
    tags: ["消息队列", "线程安全", "主线程", "ConcurrentQueue", "消息派发", "对象池", "网络线程"],
  },
  {
    id: "ua-net-9",
    chapter: "unity-advanced-network",
    level: 3,
    question: "网络抖动（Jitter）会导致什么现象？客户端有哪些应对手段？",
    answer:
      "**网络抖动**是指数据包到达间隔不规律（即使平均 RTT 不高，包也可能忽快忽慢），直接表现为：角色位置瞬移/卡顿、技能释放延迟感、帧同步不同步。应对手段：① **抖动缓冲区（Jitter Buffer）**——收包后先缓存一小段时间（如 50~100ms），按固定节奏取包交给逻辑层，把不规则到达变成均匀消费，代价是增加固定延迟；② **插值/外推（Interpolation/Extrapolation）**——状态同步中收到的是离散快照，客户端用 `Vector3.Lerp/Slerp` 在两个快照之间插值平滑移动；丢包时用外推（Dead Reckoning）基于速度预测短暂位置，新包到达后再纠正（会有回弹但比瞬移好）；③ **冗余发送**——关键包（如技能释放、击杀判定）连续发 2~3 次，防单次丢包；④ **输入预测（Client-Side Prediction）**——客户端本地立即响应输入（移动、技能），不等服务器确认，服务器回包后如有偏差再做纠正（Reconciliation），减少手感延迟；⑤ **帧同步中的乐观帧/等待帧**——允许不等所有玩家都收到该帧就先执行，后面追帧纠正，或设置等待窗口（如等待 3 帧内丢失的包）。这些技术要权衡延迟和一致性，FPS/MOBA 更偏向低延迟，MMO 更偏向一致性。",
    tags: ["网络抖动", "Jitter", "JitterBuffer", "插值", "外推", "DeadReckoning", "客户端预测", "Reconciliation"],
  },

  // ── L4 主程视角：技术决策 ──
  {
    id: "ua-net-10",
    chapter: "unity-advanced-network",
    level: 4,
    question: "你作为主程为一款 MOBA 手游（5v5，单局10~20分钟）设计网络层架构，请在传输协议、同步方案、断线重连、弱网优化四个维度做出技术选型并说明理由。",
    answer:
      "**传输协议**：选用 **KCP over UDP**，MTU 按 1200 字节（防 Internet 分片），KCP 模式设为 `nodelay(1, 10, 2, 1)`（快速模式，10ms tick，2次ACK快速重传，不流控退让）。理由：MOBA 指令必须可靠到达且延迟敏感，KCP 相比 TCP 在 20% 丢包网络下仍能保持 <100ms 延迟，TCP 会因重传+拥塞退避飙升到 500ms+；非实时业务（登录、匹配、聊天、商城）走 TCP/HTTPS，与战斗通道分离。**同步方案**：战斗采用**确定性帧同步 + 定点数**，逻辑帧率 15~20fps（即每 50~66ms 一帧），玩家操作（摇杆方向/技能ID/目标）打包为 `(frame, playerId, inputCmd)`，客户端发送输入到服务器帧转发服务器（不跑逻辑只做广播和帧号对齐），所有客户端在相同帧号执行相同输入。关键：使用定点数（如 FP7/FP16 的 Q 格式整数）替代 float，字典/集合遍历顺序固定，随机数用种子同步的伪随机。非战斗（大厅、匹配、英雄展示）用状态同步。**断线重连**：战斗服务器在内存中保留最近 300 帧（约 15~30 秒）的输入指令和起始快照；玩家断线后 30s 内可重连——重连时鉴权 → 服务端下发「起始快照 + 从快照帧到当前帧的所有输入」→ 客户端从快照重建状态 → 快速回放（fast-forward，不加渲染只跑逻辑，一次 Update 跑多帧）追赶到最新帧 → 恢复正常同步；断线期间服务器让英雄 AI 托管（简单回基地/跟随队友）避免 4v5。超过 30s 判负。**弱网优化**：① 输入冗余发送——每个输入连续发送 2 帧（覆盖单帧丢包）；② Jitter Buffer 设 2 帧（约 100~130ms）缓存平滑抖动；③ 客户端本地预测——移动和技能预表现，服务器回包不一致时平滑纠正；④ 关键帧状态快照——每 3 秒一次服务器权威快照，防止浮点漂移累积；⑤ 网络质量分级——RTT > 100ms 时降低冗余间隔，> 200ms 提示玩家网络差；⑥ 协议压缩——Protobuf 零值字段不传输，技能指令用位域打包（如方向用 256 向量化为 1 字节）。**架构分层**：网络层（KCP/TCP 封装，收发包、解包粘包）→ 会话层（心跳、重连、加密、Session 管理）→ 协议层（Protobuf 消息定义、消息派发）→ 业务层（战斗帧同步逻辑/非战斗状态同步逻辑）。主程关注点：所有跨线程数据通过队列传递，消息对象池化零 GC，战斗逻辑跑在独立逻辑线程（定点数无 Unity API 依赖），渲染层只插值表现。",
    tags: ["主程决策", "MOBA", "KCP", "帧同步", "定点数", "断线重连", "弱网优化", "网络架构"],
  },
];
