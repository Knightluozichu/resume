import type { ReviewQuestion } from "./types";

/** OTA升级与软件管理复习题 */
export const vsiOtaUpdatesQuestions: ReviewQuestion[] = [
  {
    id: "vsi-ota-updates-1",
    chapter: "vsi-ota-updates",
    level: 1,
    question:
      `什么是 OTA？FOTA 和 SOTA 的区别是什么？A/B 分区（双分区）机制如何实现「无缝升级」和「回滚」？`,
    answer:
      `OTA（Over-The-Air，空中下载升级）：通过无线网络（4G/5G/WiFi）从云端下载软件更新包并安装到车辆，无需到店。是软件定义汽车的核心能力。\n\nFOTA vs SOTA：\n- FOTA（Firmware Over-The-Air）：固件级升级，更新底层固件/操作系统/ECU 软件/Bootloader 等。涉及刷写 Flash 分区、重启生效，风险高，需要严格的安全验证和回滚机制。如升级智驾域控固件、座舱 OS、电机控制器固件。\n- SOTA（Software Over-The-Air）：应用级升级，更新上层应用/地图/语音模型/配置等，不涉及底层系统。风险较低，类似手机 App 更新。如更新导航 App、媒体应用。\n- 区别本质：FOTA 改固件/系统（需重启、影响功能安全），SOTA 改应用（热更新或重启应用即可）。实际项目常合称 OTA，但工程处理严格度不同。\n\nA/B 分区（双分区，Seamless/A-B Updates）机制：\n\n原理：在 Flash 上为可更新的系统镜像维护两个分区——Slot A 和 Slot B，内容相同。任一时刻一个是「活动分区」（当前启动用），另一个是「备用分区」。\n\n无缝升级流程：\n1. 当前从 Slot A 启动运行（活动=A）。\n2. 云端下发更新包，车辆下载到本地并校验签名。\n3. 在后台把更新写入 Slot B（备用），不干扰当前 Slot A 运行——用户无感知，车照常开。\n4. 写入并校验完成后，更新 Bootloader 的启动标志（boot control），把下次启动指向 Slot B（活动=B），标记 Slot A 为可回滚备份。\n5. 下次重启时从 Slot B 启动新版本。\n\n回滚机制：\n- 启动新版本（Slot B）后，系统进入「试运行」状态。若新版本能成功启动并通过健康检查（如关键服务起来、看门狗喂狗、能正常通信），则「提交（commit）」新版本，标记 Slot B 为正式，Slot A 可被下次更新覆盖。\n- 若新版本启动失败、关键功能异常、或用户/系统主动判定不可用，Bootloader 检测到「未提交 + 不健康」，自动回滚到上一个已知良好的 Slot A 重启——恢复到升级前状态。\n- 这种「先试运行再提交」+「失败自动回退」保证了升级失败不会变砖。\n\n优点：\n- 无缝：升级在备用分区后台进行，不中断当前使用；重启切换即可生效，停机时间极短。\n- 安全：失败可回滚到旧版本，降低变砖风险。\n- 可用性高：即使升级失败，车辆仍可用旧版本。\n\n代价：需要双倍存储空间（两份镜像），对 Flash 容量要求高；Bootloader 需支持 A/B 切换逻辑（如 Android 的 bootctl、Linux 的 RAUC/mtboot、AUTOSAR AP 的 UCM）。\n\n一句话：FOTA 改固件、SOTA 改应用；A/B 分区用「双镜像 + 后台写备用 + 试运行提交 + 失败回滚」实现无缝升级与防变砖。`,
    tags: ["OTA", "FOTA", "SOTA", "A/B分区", "无缝升级", "回滚", "Bootloader"],
  },
  {
    id: "vsi-ota-updates-2",
    chapter: "vsi-ota-updates",
    level: 2,
    question:
      `差分更新（Delta Update）的原理是什么？为什么它能大幅减少 OTA 更新包的大小？常用的差分算法（如 bsdiff/xdelta）各有什么特点？`,
    answer:
      `差分更新原理：\n\n全量更新：把整个新镜像完整下发给车辆，车辆直接写入分区。包大小 = 新镜像大小（可能数百 MB 到数 GB）。对带宽、流量、时间消耗大。\n\n差分更新：云端预先计算「旧版本镜像」和「新版本镜像」之间的差异（diff），只把差异部分（delta patch）下发给车辆。车辆端用「当前旧镜像 + delta patch」在本地重建出新镜像。包大小 ≈ 两版本的差异量，通常远小于全量。\n\n为什么能大幅减小包：\n\n固件镜像大部分内容在版本间是不变的（内核、库、大量资源未变），只有少部分代码/数据变化。差分只编码「变化部分」，所以包可从几百 MB 压到几 MB 甚至 KB 级。这对 4G 流量、下载时间、云端存储成本都有数量级改善，也让小版本迭代（修个 bug、改个配置）变得可行。\n\n常用差分算法：\n\n1. bsdiff / bspatch：\n- 经典二进制差分算法。bsdiff 生成 diff（含 control 段、diff 段、extra 段），bspatch 在端侧重建。\n- 原理：用「旧文件中匹配的字节 + 差值（add）」表达新增部分，对完全新增/删除的字节用 extra 段直接存。结合 bzip2/zstd 压缩。\n- 优点：通用、实现简单、对任意二进制有效，广泛用于 Android OTA（update.zip 里的 patch）、嵌入式升级。\n- 缺点：生成 diff 时内存占用大（O(n) 或更高），对大文件（GB 级镜像）生成慢；端侧 patch 也要把旧镜像读进内存。\n\n2. xdelta / xdelta3：\n- 基于 VCDIFF（RFC 3284）标准。用「复制旧文件某段 + 插入新字节」的指令序列表达差异。\n- 优点：标准化（VCDIFF），流式处理，内存占用比 bsdiff 低，适合大文件；xdelta3 支持多种压缩。\n- 缺点：对纯随机改动，压缩率有时略逊 bsdiff；但工程上更可控。\n\n3. courgette（Chrome）：\n- 针对可执行文件（含机器码、地址）优化。先对旧/新二进制做「去地址化」（把绝对地址转成相对，消除因重排导致的整段差异），再做差分。\n- 优点：对编译器重排导致的「大面积假差异」特别有效，包更小。适合频繁更新的可执行固件。\n\n4. zsync / rsync 思路：\n- 基于滚动哈希找相同块，只传变化块。更多用于文件同步而非固件差分。\n\n车载选型考量：\n- 端侧资源：车端 MCU/SoC 内存有限，patch 算法要低内存（流式）。xdelta3 流式更友好。\n- 镜像特点：若含大量可执行，courgette 思路减包明显；若是文件系统镜像，bsdiff/xdelta 通用。\n- 生成成本：云端算力充足，可承受 bsdiff 的高内存；但大镜像生成时间影响发布节奏。\n- 校验：差分重建后必须做整包哈希/签名校验，确保重建结果与目标镜像一致（防止 patch 应用错误导致镜像损坏）。\n- 与 A/B 配合：差分包写到备用分区，重建后校验，再切换。若重建失败，回滚到旧分区。\n\n一句话：差分用「只编码版本间变化」把包从全量降到差异量；bsdiff 通用、xdelta3 流式低内存、courgette 专为可执行去地址化，车载按镜像特点和端侧资源选型，重建后必做整包校验。`,
    tags: ["差分更新", "Delta Update", "bsdiff", "xdelta", "courgette", "VCDIFF", "压缩"],
  },
  {
    id: "vsi-ota-updates-3",
    chapter: "vsi-ota-updates",
    level: 3,
    question:
      `请描述 OTA 的安全验证机制。为什么 OTA 必须配合安全启动（Secure Boot）？什么是「防回滚攻击（Anti-Rollback）」，如何实现？`,
    answer:
      `OTA 安全验证机制：\n\n1. 签名与验签：\n- 云端用私钥对更新包（或其哈希）做数字签名（如 RSA/ECDSA）。更新包携带签名和证书链。\n- 车端用预置的公钥（信任根）验证签名，确认包来自可信源且未被篡改。验签失败拒绝安装。\n- 签名覆盖整个包内容（含 manifest、镜像、元数据），任何字节改动都会使签名失效。\n\n2. 完整性校验：\n- 包内含每个镜像的哈希（SHA-256/384），写入前/后校验，防止传输或写入错误。\n\n3. 加密：\n- 敏感包可用对称密钥加密（AES），密钥由车端 HSM/TEE 保护，防止包被逆向分析或被中间人窃取。OTA 包通常加密+签名组合。\n\n4. 设备认证与授权：\n- 车端向云端出示设备证书（由 OEM PKI 签发），云端确认是合法车辆、有权限升级该版本/该配置。防止非目标车辆误升级或被恶意下发。\n- 按车型/配置/区域灰度下发，避免不兼容版本装到错误硬件。\n\n5. 版本与依赖检查：\n- 检查目标版本号是否高于当前（防回滚）、前置版本依赖是否满足（如必须先装 v2.1 才能装 v2.3）。\n\n为什么 OTA 必须配合安全启动：\n\nOTA 验签只保证「下载并安装的包是可信的」。但如果攻击者绕过 OTA，直接刷写一个被篡改的固件到 Flash（如通过物理 JTAG、拆芯片离线刷写、利用 Bootloader 漏洞），OTA 的签名就形同虚设。\n\n安全启动建立「从硬件信任根到每一级固件的信任链」：\n- BootROM（固化、不可改，硬件信任根）验证 Bootloader 签名 → Bootloader 验证 OS/固件签名 → OS 验证应用签名。\n- 每一级启动前先验签下一级，任何一级被篡改都会因签名不匹配而拒绝启动（或进入恢复模式）。\n- 这样即使攻击者离线刷了篡改固件，启动时验签失败，固件无法运行。OTA 下发的合法固件也必须能通过安全启动链验证（OTA 包的签名密钥与安全启动验证密钥是同一信任体系）。\n\nOTA 与安全启动的关系：OTA 是「可信软件的入口」，安全启动是「可信软件的执行门」。两者结合形成「软件从下发到运行的端到端可信」。没有安全启动，OTA 验签可被物理刷写绕过；没有 OTA 验签，安全启动只能防运行时篡改却无法保证下发的软件可信。\n\n防回滚攻击（Anti-Rollback）及实现：\n\n威胁：攻击者把车辆固件回退到一个有已知漏洞的旧版本（即使旧版本是合法签名的），利用旧版本漏洞攻击。仅靠签名验证无法防——旧版本签名也是合法的。这就是回滚攻击。\n\n防回滚的目标：确保固件版本只能向前升级，不能回退到低于当前的安全版本。\n\n实现方式：\n\n1. 安全版本号（Security Version / Rollback Counter）：\n- 每个固件带一个单调递增的「安全版本号」（不同于功能版本号，只在修安全漏洞时递增）。\n- 车端在一个「防篡改」的位置记录「已运行过的最高安全版本号」（已信任的最低底线）。\n- 启动时，若固件的安全版本号 < 已记录的底线，拒绝启动（即便签名合法）。\n\n2. 防篡改存储：\n- 这个底线计数器必须存在无法被攻击者轻易回退的位置：\n  a. SoC 内的一次性可编程（OTP/eFuse）区域——每个安全版本对应一个 bit，只能从 0 烧到 1，不可逆，物理防回退。\n  b. HSM/TEE 内的安全计数器——受硬件保护，普通软件无法改写。\n  c. RPMB（Replay Protected Memory Block）分区——eMMC/UFS 的硬件防回滚分区，写入带认证，只能单调递增。\n- BootROM/Bootloader 在启动时读取该计数器，与固件安全版本号比较。\n\n3. 升级流程：\n- OTA 升级到更高安全版本后，系统把底线计数器提升到新版本号（烧 eFuse 或写 RPMB）。此后任何更低版本都无法启动。\n- 注意：底线提升要谨慎——一旦提升，回滚到旧版本排障就不可行。所以通常只在「确认新版本稳定」后才提升（commit 时），并保留「试运行期可回滚但回滚受底线约束」的策略。\n\n4. 与 A/B 分区配合：A/B 的试运行回滚仍受防回滚约束——若新版本安全版本号已提升底线，回滚只能回到不低于底线的高版本（通常是上一个同样高安全版本的备份），不能任意回退。\n\n一句话：OTA 验签保证「下发的包可信」，安全启动保证「运行的固件可信」，两者闭环；防回滚用「单调递增的安全版本号 + 防篡改存储（eFuse/RPMB/HSM）」阻止攻击者退回有漏洞的旧签名版本。`,
    tags: ["OTA安全", "签名验签", "安全启动", "Secure Boot", "防回滚", "Anti-Rollback", "eFuse", "RPMB", "信任链"],
  },
  {
    id: "vsi-ota-updates-4",
    chapter: "vsi-ota-updates",
    level: 4,
    question:
      `请描述一个完整的 OTA 升级全流程（从云端打包到车端激活与回滚），并说明在功能安全视角下，如何保证 OTA 过程不会让车辆进入不可接受的风险状态？如果一个 L2 智驾域控在升级后启动失败，系统应如何安全降级？`,
    answer:
      `完整 OTA 升级全流程：\n\n云端侧：\n1. 构建与打包：编译目标固件/软件，生成镜像；制作 manifest（版本号、安全版本号、目标车型/ECU/硬件配置、依赖、大小、哈希）。\n2. 差分生成：针对各源版本生成 delta patch（bsdiff/xdelta），减少包大小。\n3. 签名与加密：用私钥签名包（含 manifest），必要时对称加密镜像，打包最终 OTA 包。\n4. 灰度策略配置：按车型/区域/批次/比例配置下发策略（先 1% 灰度，验证后再扩大）。\n5. 下发：车端通过车云通道（HTTPS/MQTT）查询/接收任务，断点续传、校验完整性。\n\n车端侧：\n6. 前置检查：车型/硬件/SN 匹配、当前版本满足升级前置条件、电量/车速/档位满足安全条件（如 P 档、电量充足、不在行驶中关键控制升级）。\n7. 下载与验签：下载到本地暂存区，校验包签名、哈希、完整性。验签失败丢弃。\n8. 安装准备：进入「升级模式」（可能要求熄火、 parking、用户确认）。检查 A/B 分区可用性。\n9. 安装写入：把 patch 应用到备用分区（Slot B）或全量写入，重建新镜像；写入后做整包哈希校验，确保与目标镜像一致。更新 Bootloader 启动标志指向 Slot B，标记为「待试运行」。\n10. 激活（重启切换）：在合适时机（如熄火后下次上电）从 Slot B 启动新版本。\n11. 试运行与健康检查：新版本启动后，监控关键指标（看门狗、关键服务存活、通信正常、无 crash 风暴）。通过则提交（commit，提升防回滚底线、标记 Slot B 正式）；异常则触发回滚。\n12. 反馈上报：升级结果（成功/失败/回滚原因）上报云端，用于灰度决策和问题追溯。\n\n功能安全视角下保证 OTA 不引入不可接受风险：\n\n1. 时机与状态约束：\n- 安全关键 ECU（制动、转向、动力、智驾）的固件升级必须在「安全状态」进行——车辆静止、P 档、手刹、发动机/电机停机、高压下电。绝不在行驶中刷写安全关键固件。\n- 升级前后做「安全状态机」检查，不满足条件拒绝升级。\n\n2. 不影响在用功能：\n- A/B 分区让升级在备用分区后台进行，不干扰当前活动分区的安全关键功能。即使升级写入中车辆被使用，活动分区仍正常工作。\n\n3. 失败安全（Fail-Safe）：\n- 任何环节失败（下载中断、验签失败、写入错误、哈希不匹配）都不破坏当前活动分区，直接放弃本次升级，车辆保持原版本可用。\n- 试运行失败自动回滚到上一已知良好版本，保证「升级失败≠车辆不可用」。\n\n4. 最小化重启影响：\n- 切换新版本的重启安排在熄火后，避免行驶中功能中断。\n- 关键 ECU 升级后做功能可用性自检（如制动 ECU 自检、智驾感知自检），通过后才允许对应功能开启；未通过则限制该功能（降级）。\n\n5. 功能安全与信息安全协同：\n- 升级包验签/防回滚与安全启动结合，防止恶意/回退固件运行（信息安全风险转化为功能安全风险）。\n- 关键固件升级需 ASIL 流程（变更影响分析、回归测试、FMEA）。\n\n6. 回滚的确定性：\n- 回滚必须由 Bootloader 在最底层保证（不依赖新固件本身），否则新固件崩溃后无法自回滚。Bootloader 检测「未提交 + 不健康（如看门狗超时、启动计数异常）」后强制回 Slot A。\n- 回滚目标版本也必须是已知良好且满足防回滚底线的。\n\nL2 智驾域控升级后启动失败的安全降级：\n\n场景：新固件写入 Slot B 并设为活动，重启后新版本启动失败（如内核 panic、关键服务 crash、感知模型加载失败、与底盘通信建立失败）。\n\n降级流程：\n\n1. 底层检测（Bootloader/看门狗层）：\n- Bootloader 启动 Slot B 后启动一个「健康看门狗」。新固件需在限定时间内「喂狗」并报告「启动成功 + 关键功能就绪」。超时未喂狗或报告失败 → 判定启动失败。\n- 若连续多次启动 Slot B 失败（重启计数超阈值），Bootloader 自动回滚到 Slot A（旧版本）启动——这是最底层的保底，不依赖新固件配合。\n\n2. 回滚后状态：\n- 车辆从旧版本启动，智驾域控恢复升级前能力。上报回滚事件给云端。\n\n3. 若回滚也不可用（极端，旧版本也损坏）：\n- 进入「安全失效」模式：智驾功能完全禁用，仪表提示「智驾不可用，请人工驾驶」，车辆退化为仅基础驾驶功能。绝不允许半残的智驾系统继续工作（这是 SOTIF/功能安全原则——不确定安全时，禁用功能而非降级运行）。\n\n4. 若新版本能启动但功能不健全（如感知精度下降、某传感器失效）：\n- 新版本自检发现某子功能异常，主动降级：\n  a. 智驾等级降级（L2→L1 或仅 ACC，禁用车道居中/AEB 辅助外的功能）。\n  b. 限制 ODD（如只在低速/白天/清晰车道线时开启）。\n  c. 提示驾驶员接管。\n- 降级策略在固件设计时预设（graceful degradation），按失效严重度分级降级，而非全有全无。\n- 关键：降级要确保「剩余功能仍在安全边界内」，且降级状态明确告知驾驶员。\n\n5. 功能安全约束：\n- L2 是辅助驾驶，驾驶员始终在环，失效时驾驶员接管即可（这与 L3/L4 不同）。所以 L2 域控失效的最终安全状态是「退出智驾、人工驾驶」，相对可控。\n- 但仍要保证退出过程安全：不能在驾驶员未察觉时突然退出（要有声光预警、足够的接管时间），不能有误触发执行（如误刹车/误转向）。降级退出本身要经过安全设计。\n\n6. 信息上报与闭环：\n- 回滚/降级事件含失败日志、崩溃栈、健康指标上报云端，驱动根因分析和修复。该批次灰度暂停，防止扩大影响。\n\n一句话：OTA 全流程是「云端打包签名→车端验签写入备用分区→重启试运行→健康检查→提交或回滚」；功能安全用「安全状态升级 + A/B 不影响在用 + 失败安全回滚 + 不确定就禁用降级」保证升级不引入不可接受风险；L2 域控升级失败时由 Bootloader 底层回滚到旧版本，回滚不可用则禁用智驾退回人工驾驶，并把失效闭环到云端。`,
    tags: ["综合", "OTA全流程", "功能安全", "回滚", "降级", "L2智驾", "安全状态", "Fail-Safe", "SOTIF", "灰度"],
  },
];
