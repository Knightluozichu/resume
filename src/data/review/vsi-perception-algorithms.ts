import type { ReviewQuestion } from "./types";

/** 感知算法基础复习题 */
export const vsiPerceptionAlgorithmsQuestions: ReviewQuestion[] = [
  {
    id: "vsi-perception-algorithms-1",
    chapter: "vsi-perception-algorithms",
    level: 1,
    question:
      "目标检测（Object Detection）、语义分割（Semantic Segmentation）、实例分割（Instance Segmentation）三者的区别是什么？它们各自的输出是什么？",
    answer:
      "三者的区别在于「感知任务的粒度」和「输出形式」：\n\n目标检测（Object Detection）：\n- 任务：在图像中找出若干目标，给出每个目标的「类别」+「位置」。\n- 输出：一组检测框（bounding box，通常是矩形，用 $x,y,w,h$ 或 $x_1,y_1,x_2,y_2$ 表示）+ 类别 + 置信度。如「(100,150,200,280), car, 0.95」表示在 $(100,150)\\sim(200,280)$ 有 95% 置信度是车。\n- 粒度：粗粒度，只用矩形框定位目标，不描述目标内部形状。\n- 典型网络：YOLO、Faster R-CNN、SSD、RetinaNet、DETR。\n- 自动驾驶用途：检测车辆、行人、骑行者、交通标志等的目标级位置（用于后融合、跟踪、规控）。\n\n语义分割（Semantic Segmentation）：\n- 任务：对图像每个像素分类，给出每个像素属于哪个语义类别。\n- 输出：与图像同尺寸的「类别图」，每个像素一个类别标签。如像素 $(i,j)$ 标为「road」、$(i',j')$ 标为「sidewalk」。\n- 粒度：像素级，精细到每个像素，但不区分同类目标的不同实例——两个行人会被标成同样的「pedestrian」像素，但不区分是「行人A」还是「行人B」。\n- 典型网络：FCN、U-Net、DeepLab 系列、SegFormer。\n- 自动驾驶用途：可行驶区域分割（哪里是路）、车道线分割、道路结构理解（路面/人行道/建筑）。\n\n实例分割（Instance Segmentation）：\n- 任务：结合目标检测和语义分割——对每个目标，既给出类别，又给出像素级掩码（mask），且区分同类不同实例。\n- 输出：每个目标一个「类别 + 像素掩码」。两个行人分别有各自的 mask，标注「pedestrian #1 的这些像素」「pedestrian #2 的这些像素」。\n- 粒度：最细，像素级 + 实例区分。\n- 典型网络：Mask R-CNN、YOLACT、SOLOv2。\n- 自动驾驶用途：需要精细目标形状的场景（如精确的车辆轮廓用于碰撞预测、行人轮廓用于行为分析）。\n\n三者关系（由粗到细）：\n- 目标检测：框 + 类别（最粗）。\n- 语义分割：全像素类别，不区分实例（细但不分实例）。\n- 实例分割：全像素类别 + 实例区分（最细）。\n\n补充：全景分割（Panoptic Segmentation）= 语义分割（背景类，如路、天）+ 实例分割（前景目标类，如车、人），把整张图每个像素都分到「某个类或某个实例」，是更完整的场景理解。\n\n自动驾驶中常按需选择：规控需目标级位置用检测，可行驶区域用语义分割，精细形状用实例分割。3D 检测（在点云或 BEV 上输出 3D 框）是检测的 3D 版本，是智驾核心任务。\n\n一句话：检测给「框+类别」粗定位，语义分割给「逐像素类别」不分实例，实例分割给「逐像素+实例区分」最精细；粒度依次递增，按任务需求选型。",
    tags: ["目标检测", "语义分割", "实例分割", "全景分割", "bounding box", "mask", "像素级"],
  },
  {
    id: "vsi-perception-algorithms-2",
    chapter: "vsi-perception-algorithms",
    level: 2,
    question:
      "YOLO 为什么比两阶段检测器（如 Faster R-CNN）快？单阶段和两阶段检测器的核心 trade-off 是什么？请从网络结构和损失函数角度解释。",
    answer:
      "YOLO（You Only Look Once）快的根本原因——单阶段、密集预测：\n\nYOLO 把检测当成「单次回归」：输入图像一次前向传播，直接在一张特征图上每个位置预测所有目标的框和类别，一步到位输出检测结果。没有显式的「先提取候选区域再逐个分类」的两阶段过程。\n\n两阶段（Faster R-CNN）流程：\n1. 第一阶段：RPN（Region Proposal Network）在特征图上生成候选框（proposals，约几千个），每个候选框有一个「是否是目标」的粗评分。\n2. 第二阶段：对候选框（经 RoI Pooling/Align 抠特征）逐个做精确分类和框回归。\n- 两次前向特征处理 + 逐 proposal 计算，计算量大、速度慢。\n- 但精度高：第一阶段筛掉大量背景，第二阶段专注少量高质量候选，正负样本平衡好，定位精。\n\n单阶段（YOLO/SSD/RetinaNet）流程：\n- 一次前向，在特征图每个位置（anchor/anchor-free）直接预测「框偏移 + 类别 + 置信度」，一张图一次出全部检测。\n- 无 RPN、无逐 proposal 处理，计算量小、速度快，适合实时（车载要求 30Hz+）。\n\n网络结构角度的速度来源：\n- YOLO 用单 backbone（如 CSPDarknet）提取特征，一次卷积出多尺度特征图（FPN/PANet），每张特征图每个格子直接预测。无第二阶段的重计算。\n- 候选框处理被「密集 anchor 预测」替代：每个位置预设一组 anchor（不同尺度/长宽比），直接预测每个 anchor 的偏移和类别，并行计算，GPU 友好。\n- 两阶段要为每个 proposal 抠特征（RoI Align）再过 head 网络，proposal 数量多时是主要瓶颈；单阶段无此开销。\n\n单阶段 vs 两阶段的核心 trade-off：速度 vs 精度（尤其小目标和正负样本不平衡）：\n\n1. 速度：单阶段快（一次前向），两阶段慢（两次 + 逐 proposal）。车载实时性要求高，单阶段（YOLO 系列）更受欢迎。\n\n2. 精度：两阶段通常精度略高，尤其小目标和精确定位——第一阶段筛 proposals 让第二阶段专注高质量样本，定位回归更精。单阶段一次预测所有位置，小目标在低分辨率特征图上信息不足，定位精度略逊。\n\n3. 正负样本不平衡——单阶段的核心难题与损失函数解法：\n- 单阶段在每个位置预测大量 anchor，但绝大多数 anchor 是背景（负样本），正样本（真目标）极少。训练时正负样本极度不平衡（1:1000），背景负样本主导损失，网络学不到有效特征。\n- 两阶段靠 RPN 先筛掉大部分背景，第二阶段正负样本相对平衡，问题小。\n- 单阶段的损失函数解法：\n  a. Focal Loss（RetinaNet）：改造交叉熵，降低易分负样本的损失权重，让网络聚焦难分样本，缓解不平衡。$FL(p_t) = -(1-p_t)^γ \\log(p_t)$，$γ$ 调节聚焦程度。这是单阶段精度追上两阶段的关键。\n  b. YOLO 的策略：忽略某些背景区域（不参与损失）、按 IOU 阈值分配正负样本、置信度预测区分目标/背景。\n\n4. 定位精度：两阶段的 RoI Align + 二次回归定位更精；单阶段靠 anchor 设计、多尺度预测、更精的框回归损失（GIoU/CIoU/DIoU）弥补。\n\n演进：\n- 早期单阶段精度落后两阶段，速度领先。Focal Loss 后单阶段精度追平。YOLOv3~v8 持续优化（多尺度、anchor-free、CIoU、标签分配、数据增强），单阶段在实时检测上全面领先，车载主流。\n- 两阶段在极致精度场景（如医学、卫星图）仍有优势，但车载实时检测基本用单阶段（YOLO/DETR 类）。\n- DETR（Transformer 检测器）是另一种「单阶段」——用 Transformer 直接预测检测，无 anchor、无 NMS，端到端，精度高但早期训练慢，后续 Deformable DETR 等优化后可用于实时。\n\n一句话：YOLO 用「单次前向密集预测」省去两阶段的 RPN+逐 proposal 处理所以快；单阶段 vs 两阶段的 trade-off 是速度对精度（小目标+正负样本不平衡），单阶段靠 Focal Loss/精损失/多尺度把精度追平，成为车载实时检测主流。",
    tags: ["YOLO", "单阶段", "两阶段", "Faster R-CNN", "RPN", "Focal Loss", "正负样本不平衡", "anchor", "trade-off"],
  },
  {
    id: "vsi-perception-algorithms-3",
    chapter: "vsi-perception-algorithms",
    level: 3,
    question:
      "BEV（Bird's Eye View，鸟瞰图）感知为什么成为自动驾驶的主流范式？从 2D 图像感知到 BEV 3D 感知，需要解决哪些核心问题（视角转换、时序、多模态）？LSS（Lift-Splat-Shoot）和 BEVFormer 各自如何实现视角转换？",
    answer:
      "BEV 感知成为主流的原因：\n\n1. 自动驾驶决策在 3D/俯视空间进行：规划、控制、跟踪都在自车坐标系/BEV 空间，目标位置用 3D 坐标。传统 2D 图像检测输出的是图像平面 2D 框，需要再转换到 3D/BEV 才能用，转换有损且复杂。直接在 BEV 空间输出检测结果，与下游无缝衔接。\n\n2. 多摄像头自然融合：环视多摄像头各自有不同视角，在图像空间无法直接融合（各自 2D 平面）。转换到统一 BEV 空间后，多相机目标自然在同一坐标系下对齐，便于跨相机目标关联（避免一辆车在两个相机视角被检测成两个目标）。\n\n3. 多模态融合的统一空间：图像、激光雷达点云、雷达都可投影到 BEV，在 BEV 空间做前融合，统一表征。BEV 是天然的「融合枢纽」。\n\n4. 时序融合更自然：BEV 空间下，不同时刻的感知结果都在同一坐标系（自车补偿运动后），可直接叠加/时序卷积，做时序融合、速度估计、遮挡推理。\n\n5. 避免 2D 检测的透视歧义：2D 图像中远处的车和近处的小物体可能框一样大，深度信息丢失。BEV 直接给 3D 位置，无透视歧义。\n\n从 2D 图像感知到 BEV 3D 感知需解决的核心问题：\n\n1. 视角转换（View Transformation）：把 2D 图像特征转换到 3D/BEV 空间。这是核心难题——2D 图像丢失了深度，如何把每个像素/特征「放」到正确的 3D 位置？\n\n2. 深度估计：2D→3D 必须知道每个像素的深度。深度可来自：显式深度预测网络、几何先验（相机模型投影）、激光雷达监督、时序运动估计。\n\n3. 时序融合：单帧 BEV 信息有限，需多帧时序融合估计速度、处理遮挡、稳定检测。需把历史帧 BEV 对齐到当前帧（自车运动补偿），再用时序模块（RNN/Transformer/时序卷积）融合。\n\n4. 多相机标定与同步：多相机 BEV 融合要求精确的外参标定（每相机相对自车的位姿）和时间同步（各相机曝光时刻对齐）。\n\n5. 多模态对齐：图像 BEV 与点云 BEV 对齐需精确时空标定，模态间特征尺度/分布不同需设计融合方式。\n\nLSS（Lift-Splat-Shoot）的视角转换方式——显式深度预测 + 投影：\n- Lift（提升）：对每个图像像素（特征），网络预测一个「深度分布」（离散深度桶上的概率），把 2D 像素特征「提升」为 3D 空间的「视锥特征点云」——每个像素沿其视线方向按深度分布生成多个 3D 点特征。\n- Splat（投影）：把视锥特征点云按 3D 坐标投影到 BEV 网格（pillar 池化），得到 BEV 特征图。\n- Shoot（射击）：在 BEV 特征图上做检测（用 BEV 检测头）。\n- 关键：显式预测深度分布做 lift，深度是无监督或有监督学习。几何清晰、可解释，但深度预测不准会带误差。\n\nBEVFormer 的视角转换方式——可变形注意力 + 时序：\n- 用 Transformer 的 cross-attention 直接从图像特征「查询」BEV 特征。每个 BEV 查询点（BEV 网格位置）通过「空间交叉注意力（Spatial Cross-Attention, SCA）」从多相机图像特征中采样相关像素——具体用 Deformable Attention，在 BEV 点对应的参考线（pillar）上采样若干深度点，从图像特征取值聚合。\n- 不显式预测深度，而是用注意力机制隐式学习「BEV 位置←→图像像素」的对应，深度信息隐含在注意力权重中。\n- 时序融合：用「时序自注意力（Temporal Self-Attention, TSA）」融合历史帧 BEV 特征（经运动补偿对齐到当前帧），做速度估计和遮挡推理。\n- 优点：端到端、时序原生、性能强；缺点：计算重（Transformer 注意力），可解释性弱于 LSS。\n\n两者对比：\n- LSS：显式深度 + 几何投影，清晰可解释，深度可单独监督，但受深度预测精度制约。\n- BEVFormer：隐式注意力学习对应 + 原生时序，性能强但计算重、可解释弱。\n- 后续 BEVFusion 等把图像 BEV（LSS 类）与点云 BEV 融合，进一步提性能。\n\n一句话：BEV 主流是因为它统一了多相机/多模态/时序融合的空间、与下游 3D 决策无缝衔接；2D→BEV 要解决视角转换（深度）、时序、多模态对齐；LSS 用显式深度预测 Lift 再 Splat 投影，BEVFormer 用可变形注意力隐式查询+原生时序，分别代表几何派和注意力派两条路线。",
    tags: ["BEV", "鸟瞰图", "视角转换", "LSS", "BEVFormer", "时序融合", "多模态融合", "Deformable Attention", "深度估计"],
  },
  {
    id: "vsi-perception-algorithms-4",
    chapter: "vsi-perception-algorithms",
    level: 4,
    question:
      "Transformer 和注意力机制（Attention）在自动驾驶感知中是如何逐步替代/增强 CNN 的？请从 ViT、DETR、BEVFormer 到 Occupancy Network 说明其演进。相比 CNN，Transformer 在车载感知中的优势和挑战分别是什么？",
    answer:
      "Transformer/Attention 在感知中的演进脉络——从替换骨干到端到端感知：\n\n1. ViT（Vision Transformer）——替换 CNN 骨干：\n- 把图像切成 patch（如 16×16），每个 patch 当一个 token，加位置编码，送入 Transformer Encoder 提取特征。完全用自注意力替代卷积。\n- 意义：证明纯 Transformer 在图像识别上可超越 CNN（大数据下）。全局注意力能建模长距离依赖（图像左上和右下的关联），CNN 受限于卷积核感受野。\n- 车载影响：启发用 Transformer 骨干做检测/分割骨干（如 Swin Transformer、层级 ViT），但纯 ViT 计算重（注意力 O(N²)），车载实时部署需优化（窗口注意力、降分辨率）。\n\n2. DETR（DEtection TRansformer）——端到端检测：\n- 用 Transformer 做检测：一组「目标查询（object queries）」通过 cross-attention 从图像特征「查询」目标，直接输出检测结果（类别+框），无 anchor、无 NMS。\n- 意义：消除 anchor 设计和 NMS 后处理，检测 pipeline 端到端可微，简化工程。Transformer 的全局注意力天然处理多目标关联。\n- 挑战：早期收敛慢、小目标弱，需 Deformable DETR（用可变形注意力只在参考点周围采样，降复杂度、提小目标）等改进。车载检测开始用 DETR 类（如 RT-DETR 实时版）。\n\n3. BEVFormer ——BEV 时空 Transformer：\n- 如前述，用可变形空间交叉注意力把多相机图像特征查到 BEV，用时序自注意力融合历史帧。Transformer 是「跨视角、跨时间信息聚合」的核心工具，这是 CNN 难做的（CNN 难统一多视角多时序）。\n- 意义：Transformer 让多相机 BEV 融合和时序融合端到端学习，性能超 CNN 方案。\n\n4. Occupancy Network（占用网络）——3D 体素语义占用预测：\n- 把周围 3D 空间划分成体素网格，预测每个体素是否被占用 + 语义类别。输出密集 3D 占用而非稀疏 3D 框。\n- 用 Transformer（或稀疏卷积）从多相机图像/点云预测体素占用。每个体素查询通过注意力从图像/点云特征聚合信息。\n- 意义：突破「3D 框」表达的局限——障碍物不一定是规则框（如异形路障、悬挂物、施工锥、栅栏），占用网络用体素描述任意形状障碍物，对长尾/异形障碍物更鲁棒。Tesla 等力推，成为高阶感知趋势。\n- Transformer 价值：体素-像素/点云的稀疏对应用注意力建模，比卷积更灵活。\n\nTransformer 相比 CNN 在车载感知的优势：\n\n1. 全局/长距离建模：自注意力直接建模任意两个位置关联，不受卷积核大小限制。对大场景、远距目标关联、多视角融合有利。\n\n2. 灵活的多模态/多视角/时序融合：Transformer 的 cross-attention 天然适合「不同来源数据互查询」——图像查 BEV、BEV 查时序、点查像素。CNN 需要手动设计拼接/对齐，Transformer 端到端学习。\n\n3. 端到端、无手工组件：无 anchor、无 NMS、无 RPN，pipeline 简化，减少人工设计偏差。\n\n4. 强表达力、可扩展：大数据大模型下性能上限高，预训练（大视觉模型）可迁移车载，持续吃数据红利。\n\n5. 占用/异形障碍物表达：体素+注意力能表达任意形状障碍物，弥补框检测的长尾盲区。\n\n挑战：\n\n1. 计算量与实时性：自注意力 O(N²) 序列长则爆炸。BEV 体素数、图像 patch 数大，全注意力不可行。需 Deformable Attention（只采样参考点周围）、窗口/稀疏注意力、降采样等优化才能实时。车载要求 30Hz+、低延迟，Transformer 部署比 CNN 难。\n\n2. 部署与算子支持：Transformer 算子（attention、复杂 reshape）在车载 NPU/边缘芯片上支持不如卷积成熟，需算子优化/编译器支持。量化（INT8）后注意力精度损失比卷积大，需专门量化方案。\n\n3. 数据与训练成本：Transformer 需大数据/大算力训练，长尾场景需大量标注/弱标注数据。训练周期长、成本高。\n\n4. 可解释性与功能安全：注意力是黑盒，难解释「为什么漏检」，SOTIF（预期功能安全）评估难。CNN 至少有更成熟的可解释性和验证方法。功能安全认证 Transformer 系统是新课题。\n\n5. 内存与功耗：大模型+注意力中间激活占用显存大，车载功耗/散热受限，需剪枝/蒸馏/稀疏化。\n\n6. 时序对齐与延迟：时序 Transformer 需缓存历史帧特征，增加内存和延迟；实时系统对延迟敏感，需精调。\n\n演进趋势：\n- CNN 不会完全被替代：CNN 在局部特征提取、小目标、低算力场景仍高效。常见「CNN 骨干 + Transformer 头/融合」混合架构，兼顾效率与表达力。\n- 端到端感知-预测-规划（如 UniAD）用 Transformer 串联全栈，进一步统一。\n- 占用网络 + 端到端是高阶智驾前沿，但量产部署仍在攻坚实时性与安全性。\n\n一句话：Transformer 从 ViT 替换骨干、DETR 端到端检测、BEVFormer 跨视角时序融合、到 Occupancy 表达任意形状障碍物，逐步替代/增强 CNN；优势是全局建模、灵活融合、端到端、强表达，挑战是 O(N²) 计算重、车载部署/量化/算子难、可解释性与功能安全验证新——量产常走「CNN+Transformer 混合」兼顾效率与性能。",
    tags: ["综合", "Transformer", "Attention", "ViT", "DETR", "BEVFormer", "Occupancy Network", "端到端", "实时性", "功能安全"],
  },
];
